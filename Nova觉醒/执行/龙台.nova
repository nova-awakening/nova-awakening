《龙台》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·推理·统一调用器》。

引用《..·中枢·内核打印》。

引用《..·推理·令牌优化器》。

引用《..·标准·标准流》。

引用《..·中枢·AI状态接口》。

引用《..·中枢·整数转字符串》。

引用《..·推理·统一Provider框架》。

引用《..·记忆·文件类型宏》。

引用《..·推理·代码操作》。

可变 龙台_系统提示 为 「你是NovaOS的AI助手龙台。用中文回答。」。

可变 龙台_消息历史 为 一组：（空）。

可变 龙台_当前模式 为 「聊天」。

龙台_主函数 接受 零参：
    做 打印行 用 「╔════════════════════════════════════════╗」。
    做 打印行 用 「║  龙台 — NovaOS AI对话终端               ║」。
    做 打印行 用 「║  /work 工作模式 | /code 代码 | /help    ║」。
    做 打印行 用 「╚════════════════════════════════════════╝」。
    做 打印行 用 「」。
    可变 运行 为 真。
    只要 运行：
        做 标准输出 用 「[」 加 龙台_当前模式 加 「] 龙台> 」。
        输入 为 做 读取行。
        若 输入 等于 空 或者 做 字符串长度 用 输入 等于 0：
            继续。
        完。
        若 输入 等于 「quit」 或者 输入 等于 「exit」 或者 输入 等于 「退出」：
            做 打印行 用 「[龙台] 再见!」。
            运行 为 假。
            继续。
        完。
        若 做 字符串开始于 用 输入、「/」：
            做 龙台_处理指令 用 输入。
            继续。
        完。
        做 龙台_发送消息 用 输入。
    完。
完。

龙台_处理指令 接受 输入：
    若 输入 等于 「/help」 或者 输入 等于 「/帮助」：
        做 打印行 用 「  /work <任务>  — 工作模式(执行任务)」。
        做 打印行 用 「  /code <描述>  — 代码模式(生成Nova代码)」。
        做 打印行 用 「  /model <名称> — 切换AI模型(glm-4/qwen/Nova语言模型-4o/claude)」。
        做 打印行 用 「  /status       — 查看AI状态」。
        做 打印行 用 「  /memory       — 查看记忆状态」。
        做 打印行 用 「  /令牌        — 查看令牌使用」。
        做 打印行 用 「  /clear        — 清除对话历史」。
        做 打印行 用 「  /chat         — 切换为聊天模式」。
        返还。
    完。
    若 输入 等于 「/status」 或者 输入 等于 「/状态」：
        做 AI状态_输出。
        返还。
    完。
    若 输入 等于 「/memory」 或者 输入 等于 「/记忆」：
        统计 为 做 记忆_统计。
        做 打印行 用 「  工作记忆: 」 加 做 整数转字符串 用 统计 的 工作 加 「/7」。
        做 打印行 用 「  情景记忆: 」 加 做 整数转字符串 用 统计 的 情景 加 「/64」。
        做 打印行 用 「  语义节点: 」 加 做 整数转字符串 用 统计 的 语义节点。
        做 打印行 用 「  总写入: 」 加 做 整数转字符串 用 统计 的 写入 加 「 总巩固: 」 加 做 整数转字符串 用 统计 的 巩固。
        返还。
    完。
    若 输入 等于 「/令牌」：
        t 为 做 令牌_统计。
        做 打印行 用 「  总输入令牌: 」 加 做 整数转字符串 用 t 的 总输入。
        做 打印行 用 「  总输出令牌: 」 加 做 整数转字符串 用 t 的 总输出。
        做 打印行 用 「  已节约: 」 加 做 整数转字符串 用 t 的 总节约 加 「 (」 加 做 整数转字符串 用 t 的 节约率 加 「%)」。
        返还。
    完。
    若 输入 等于 「/clear」 或者 输入 等于 「/清除」：
        龙台_消息历史 为 一组：（空）。
        做 打印行 用 「[龙台] 对话历史已清除」。
        返还。
    完。
    若 输入 等于 「/chat」：
        龙台_当前模式 为 「聊天」。
        做 打印行 用 「[龙台] 已切换到聊天模式」。
        返还。
    完。
    若 做 字符串开始于 用 输入、「/work 」：
        龙台_当前模式 为 「工作」。
        任务 为 做 字符串子串 用 输入、6、做 字符串长度 用 输入。
        做 龙台_发送消息 用 任务。
        返还。
    完。
    若 做 字符串开始于 用 输入、「/code 」：
        龙台_当前模式 为 「代码」。
        描述 为 做 字符串子串 用 输入、6、做 字符串长度 用 输入。
        做 打印行 用 「[龙台] 生成代码中...」。
        代码 为 做 智能体代码_生成 用 描述。
        做 打印行 用 代码。
        龙台_当前模式 为 「聊天」。
        返还。
    完。
    若 做 字符串开始于 用 输入、「/model 」：
        模型名 为 做 字符串子串 用 输入、7、做 字符串长度 用 输入。
        结果 为 做 Provider_按名称选择 用 模型名。
        若 结果 大于或等于 0：
            做 打印行 用 「[龙台] 已切换到: 」 加 模型名。
        否则：
            做 打印行 用 「[龙台] 模型未找到: 」 加 模型名。
        完。
        返还。
    完。
    做 打印行 用 「[龙台] 未知指令: 」 加 输入 加 「 (输入 /help 查看帮助)」。
完。

龙台_发送消息 接受 输入：
    做 追加 用 龙台_消息历史、一组：「user」、输入。
    可变 系统提示 为 龙台_系统提示。
    若 龙台_当前模式 等于 「工作」：
        系统提示 为 「你是NovaOS的工作助手。执行用户的任务请求，给出具体操作步骤。」。
    完。
    做 打印行 用 「[AI] 思考中...」。
    结果 为 做 智能_调用_多轮 用 系统提示、龙台_消息历史。
    若 结果 的 成功 等于 1：
        做 打印行 用 「」。
        做 打印行 用 结果 的 内容。
        做 打印行 用 「」。
        做 追加 用 龙台_消息历史、一组：「assistant」、结果 的 内容。
        若 结果 的 总令牌 大于 0：
            做 打印行 用 「  [令牌: 」 加 做 整数转字符串 用 结果 的 总令牌 加 「 | 延迟: 」 加 做 整数转字符串 用 结果 的 延迟毫秒 加 「ms]」。
        完。
    否则：
        做 打印行 用 「[错误] 」 加 结果 的 错误信息。
    完。
完。

本章 收束。
