《尾部查》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·标准·文件》。

引用《..·中枢·内核打印》。

引用《..·标准·原生列表》。

引用《..·记忆·VFS读写同步》。

引用《..·中枢·整数转字符串》。

TAIL_默认行数 为 10。

TAIL_跟踪间隔毫秒 为 1000。

tail_main 接受 参数：
    可变 行数 为 TAIL_默认行数。
    可变 字节数 为 负 1。
    可变 跟踪 为 假。
    可变 安静 为 假。
    可变 从开头 为 假。
    可变 文件列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        a 为 参数 的 第 i 项。
        若 a 等于 「-n」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            值文本 为 参数 的 第 i 项。
            若 做 字符串取字符 用 值文本、0 等于 「+」：
                从开头 为 真。
                行数 为 做 字符串转整数 用 做 字符串子串 用 值文本、1、做 字符串长度 用 值文本。
            否则：
                行数 为 做 字符串转整数 用 值文本。
            完。
        否则若 a 等于 「-c」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            字节数 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-f」：
            跟踪 为 真。
        否则若 a 等于 「-q」：
            安静 为 真。
        否则若 a 等于 「--help」 或者 a 等于 「-h」：
            做 tail_显示帮助。
            返还 0。
        否则：
            做 追加 用 文件列表、a。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 文件列表 等于 0：
        做 追加 用 文件列表、「-」。
    完。
    多文件 为 做 计数 用 文件列表 大于 1。
    可变 k 为 0。
    只要 k 小于 做 计数 用 文件列表：
        若 多文件 并且 非 安静：
            做 打印行 用 「==> 」 加 文件列表 的 第 k 项 加 「 <==」。
        完。
        若 文件列表 的 第 k 项 等于 「-」：
            做 tail_从标准输入 用 行数。
        否则若 字节数 大于或等于 0：
            做 tail_按字节 用 文件列表 的 第 k 项、字节数。
        否则若 从开头：
            做 tail_从行开始 用 文件列表 的 第 k 项、行数。
        否则：
            做 tail_最后N行 用 文件列表 的 第 k 项、行数。
        完。
        若 跟踪 并且 k 等于 做 计数 用 文件列表 减 1：
            做 tail_跟踪文件 用 文件列表 的 第 k 项。
        完。
        k 为 k 加 1。
    完。
    返还 0。
完。

tail_显示帮助 接受 零参：
    做 打印行 用 「用法: tail [-f] [-n 行数] [-c 字节数] [-q] [文件...]」。
    做 打印行 用 「  -n N    显示最后N行 (默认10)」。
    做 打印行 用 「  -n +N   从第N行开始显示」。
    做 打印行 用 「  -c N    显示最后N个字节」。
    做 打印行 用 「  -f      跟踪文件追加内容」。
    做 打印行 用 「  -q      不显示文件名头部」。
完。

tail_最后N行 接受 文件路径、行数：
    内容 为 做 文件_读取全部 用 文件路径。
    若 内容 等于 空：
        做 打印行 用 「tail: 无法打开 '」 加 文件路径 加 「'」。
        返还。
    完。
    行列表 为 做 字符串分割 用 内容、「\n」。
    总行 为 做 计数 用 行列表。
    可变 起始 为 总行 减 行数。
    若 起始 小于 0：
        起始 为 0。
    完。
    可变 j 为 起始。
    只要 j 小于 总行：
        做 打印行 用 行列表 的 第 j 项。
        j 为 j 加 1。
    完。
完。

tail_从行开始 接受 文件路径、起始行：
    内容 为 做 文件_读取全部 用 文件路径。
    若 内容 等于 空：
        返还。
    完。
    行列表 为 做 字符串分割 用 内容、「\n」。
    可变 开始 为 起始行 减 1。
    若 开始 小于 0：
        开始 为 0。
    完。
    可变 j 为 开始。
    只要 j 小于 做 计数 用 行列表：
        做 打印行 用 行列表 的 第 j 项。
        j 为 j 加 1。
    完。
完。

tail_按字节 接受 文件路径、字节数：
    内容 为 做 文件_读取全部 用 文件路径。
    若 内容 等于 空：
        返还。
    完。
    长度 为 做 字符串长度 用 内容。
    可变 起始 为 长度 减 字节数。
    若 起始 小于 0：
        起始 为 0。
    完。
    做 标准输出 用 做 字符串子串 用 内容、起始、长度。
完。

tail_从标准输入 接受 行数：
    可变 缓冲 为 一组：（空）。
    可变 行 为 做 标准输入_读行。
    只要 行 不等于 空：
        做 追加 用 缓冲、行。
        若 做 计数 用 缓冲 大于 行数：
            缓冲 为 做 列表_切片 用 缓冲、1、做 计数 用 缓冲。
        完。
        行 为 做 标准输入_读行。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 缓冲：
        做 打印行 用 缓冲 的 第 i 项。
        i 为 i 加 1。
    完。
完。

tail_跟踪文件 接受 文件路径：
    可变 上次大小 为 做 文件_获取大小 用 文件路径。
    若 上次大小 小于 0：
        返还。
    完。
    只要 真：
        做 睡眠_毫秒 用 TAIL_跟踪间隔毫秒。
        当前大小 为 做 文件_获取大小 用 文件路径。
        若 当前大小 大于 上次大小：
            内容 为 做 文件_读取全部 用 文件路径。
            若 内容 不等于 空：
                新内容 为 做 字符串子串 用 内容、上次大小、当前大小。
                做 标准输出 用 新内容。
            完。
            上次大小 为 当前大小。
        否则若 当前大小 小于 上次大小：
            做 打印行 用 「tail: 文件已截断」。
            上次大小 为 当前大小。
        完。
    完。
完。

本章 收束。
