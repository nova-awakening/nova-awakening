《接口》

引用《..·中枢·阶段契约》。

引用《..·中枢·会话桥一体》。

引用《.·阶段装载》。

引用《.·列目录》。

引用《.·查找》。

引用《.·AI助手》。

引用《.·CookieJar》。

引用《.·Windows进程接口》。

引用《.·top进程查看》。

引用《.·内核初始化》。

引用《.·内核总控》。

引用《.·内核核心》。

引用《.·原子字符串》。

引用《.·反检测快捷》。

引用《.·各步骤页面》。

引用《.·回显》。

引用《.·图处资源》。

引用《.·实际安装流程》。

引用《.·工具文件》。

引用《.·平台图形层》。

引用《.·执行常量》。

引用《.·管道系统》。

引用《.·脚本引擎》。

引用《.·语法高亮》。

引用《.·连接池》。

引用《.·龙仓》。

引用《.·龙使》。

引用《.·龙台》。

引用《.·龙境》。

引用《.·龙审》。

引用《.·龙文》。

引用《.·龙析》。

引用《.·龙格》。

引用《.·龙脑》。

引用《.·龙试》。

引用《.·龙调》。

可变 执行_调用计数 为 0。

可变 执行_最近阶段 为 「」。

可变 执行_最近总线消息 为 空。

执行_阶段清单 接受 阶段：
    返还 做 执行阶段装载_清单 用 阶段。
完。

执行_加载阶段 接受 阶段：
    若 非 做 阶段契约_有效 用 阶段：
        返还 负 1。
    完。
    执行_最近阶段 为 阶段。
    返还 做 执行阶段装载_登记 用 阶段。
完。

执行_初始化 接受 零参：
    做 执行_加载阶段 用 阶段_boot。
    做 执行_加载阶段 用 阶段_浏览器外衣。
    做 配置_初始化。
    做 反检测_初始化。
    做 一体桥_桥心跳 用 做 一体桥_确保系统会话 用 「Nova觉醒」、「执行接口」、「运行」、「未连接」、「运行」。
    返还 0。
完。

执行_创建页面会话 接受 名称、网址：
    系统会话ID 为 做 一体桥_确保系统会话 用 「Nova觉醒」、「执行接口」。
    返还 做 一体桥_创建页面会话 用 系统会话ID、名称、网址。
完。

执行_打开页面 接受 网址：
    系统会话ID 为 做 一体桥_确保系统会话 用 「Nova觉醒」、「执行接口」。
    返还 做 执行_打开页面_会话 用 系统会话ID、网址。
完。

执行_打开页面_会话 接受 系统会话ID、网址：
    页面会话ID 为 做 一体桥_创建页面会话 用 系统会话ID、「页面会话」、网址。
    若 页面会话ID 等于 0：
        返还 0。
    完。
    标签ID 为 做 NTLM_创建标签 用 1、网址、0。
    若 标签ID 不等于 0：
        做 NTLM_激活标签 用 标签ID。
        做 浏览器内核_导航 用 标签ID、网址。
        做 标签页管理_绑定页面会话 用 页面会话ID、标签ID。
    完。
    做 一体桥_记录动作 用 页面会话ID、「执行_打开页面」。
    返还 页面会话ID。
完。

执行_导航页面_会话 接受 页面会话ID、网址：
    会话 为 做 一体桥_查页面会话 用 页面会话ID。
    若 会话 等于 空：
        返还 假。
    完。
    可变 标签ID 为 会话 的 标签ID。
    若 标签ID 等于 0：
        标签ID 为 做 NTLM_创建标签 用 1、网址、0。
        若 标签ID 等于 0：
            返还 假。
        完。
        做 NTLM_激活标签 用 标签ID。
        做 标签页管理_绑定页面会话 用 页面会话ID、标签ID。
    完。
    做 浏览器内核_导航 用 标签ID、网址。
    做 一体桥_设置页面网址 用 页面会话ID、网址。
    做 一体桥_记录动作 用 页面会话ID、「执行_导航页面」。
    返还 真。
完。

执行_关闭页面 接受 页面会话ID：
    会话 为 做 一体桥_查页面会话 用 页面会话ID。
    若 会话 等于 空：
        返还 负 1。
    完。
    做 NTLM_关闭标签 用 会话 的 标签ID。
    返还 0。
完。

执行_点击元素 接受 页面会话ID、选择器：
    会话 为 做 一体桥_查页面会话 用 页面会话ID。
    若 会话 等于 空：
        返还 假。
    完。
    做 一体桥_记录动作 用 页面会话ID、「执行_点击元素」。
    目标页 为 若 会话 的 标签ID 不等于 0 则 会话 的 标签ID 否则 页面会话ID。
    结果 为 做 自动编排_点击 用 目标页、选择器。
    做 一体桥_记录回执 用 页面会话ID、结果、「click」、选择器、若 结果 则 「」 否则 「点击失败」、0、「」。
    返还 结果。
完。

执行_输入文本 接受 页面会话ID、选择器、文本：
    会话 为 做 一体桥_查页面会话 用 页面会话ID。
    若 会话 等于 空：
        返还 假。
    完。
    做 一体桥_记录动作 用 页面会话ID、「执行_输入文本」。
    目标页 为 若 会话 的 标签ID 不等于 0 则 会话 的 标签ID 否则 页面会话ID。
    结果 为 做 自动编排_输入 用 目标页、选择器、文本。
    做 一体桥_记录回执 用 页面会话ID、结果、「input」、选择器、若 结果 则 「」 否则 「输入失败」、0、「」。
    返还 结果。
完。

执行_获取页面内容 接受 页面会话ID：
    会话 为 做 一体桥_查页面会话 用 页面会话ID。
    若 会话 等于 空：
        返还 0。
    完。
    做 一体桥_记录动作 用 页面会话ID、「执行_获取页面内容」。
    目标页 为 若 会话 的 标签ID 不等于 0 则 会话 的 标签ID 否则 页面会话ID。
    返还 做 自动编排_获取内容 用 目标页。
完。

执行_截图 接受 页面会话ID：
    会话 为 做 一体桥_查页面会话 用 页面会话ID。
    若 会话 等于 空：
        返还 空。
    完。
    做 一体桥_记录动作 用 页面会话ID、「执行_截图」。
    返还 做 一体桥_生成视觉帧 用 页面会话ID、0、0、0、0、一组：（空）。
完。

执行_列目录 接受 路径、选项：
    执行_最近阶段 为 「system-list」。
    做 NDLS_增强列出 用 路径、选项。
    返还 做 列目录_统计。
完。

执行_查找命令 接受 参数：
    执行_最近阶段 为 「system-find」。
    返还 做 find_main 用 参数。
完。

执行_搜索内容 接受 路径、模式、最大深度：
    执行_最近阶段 为 「system-grep」。
    返还 做 find_grep 用 路径、模式、最大深度。
完。

执行_编排任务 接受 任务描述：
    返还 做 自动编排_创建任务 用 任务描述。
完。

执行_执行步骤 接受 零参：
    做 自动编排_执行步骤。
完。

执行_任务状态 接受 零参：
    返还 做 自动编排_当前状态。
完。

执行_启用反检测 接受 零参：
    做 反检测_全局启用。
完。

执行_设置指纹 接受 配置：
    做 反检测_设置指纹配置 用 配置。
完。

执行_安全入口 接受 调用者、操作码：
    执行_调用计数 为 执行_调用计数 加 1。
    若 调用者 小于 0 或者 调用者 大于 8：
        返还 负 1。
    完。
    返还 0。
完。

执行_处理总线消息 接受 调用者、操作码、数据：
    执行_最近总线消息 为 一本字典：调用者 对应 调用者、操作码 对应 操作码、数据 对应 数据。
    若 操作码 等于 0x100：
        做 执行_执行步骤。
    完。
    若 操作码 等于 0x110 并且 数据 不等于 空：
        返还 做 执行_列目录 用 数据 的 路径、数据 的 选项。
    完。
    若 操作码 等于 0x111 并且 数据 不等于 空：
        返还 做 执行_查找命令 用 数据 的 参数。
    完。
    若 操作码 等于 0x112 并且 数据 不等于 空：
        返还 做 执行_搜索内容 用 数据 的 路径、数据 的 模式、数据 的 最大深度。
    完。
    返还 0。
完。

本章 收束。
