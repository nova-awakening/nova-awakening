《是内建命令》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内建取消》。

引用《..·中枢·硬链接》。

引用《.·文本处理环》。

引用《.·补全引擎》。

引用《..·中枢·复制描述》。

引用《..·中枢·字符串替换》。

引用《.·内建测试》。

引用《..·中枢·读取调用》。

引用《..·中枢·内核打印》。

内建列表 为 一组：「cd」、「pwd」、「echo」、「export」、「unset」、「help」、「history」、「type」、「alias」、「unalias」、「set」、「source」、「exec」、「eval」、「jobs」、「fg」、「bg」、「wait」、「kill」、「test」、「true」、「false」、「read」、「shift」、「return」、「break」、「continue」、「umask」。

是内建命令 接受 名称：
    返还 做 列表_包含 用 内建列表、名称。
完。

执行内建_完整 接受 参数、重定向：
    可变 保存stdout 为 负 1。
    可变 保存stdin 为 负 1。
    若 重定向 不等于 空：
        若 重定向 的 输出 不等于 空：
            保存stdout 为 做 sys_dup 用 1、0、0、0、0、0。
            标志 为 O_WRONLY 位或 O_CREAT。
            若 重定向 的 追加：
                标志 为 标志 位或 O_APPEND。
            否则：
                标志 为 标志 位或 O_TRUNC。
            完。
            fd 为 做 sys_open 用 重定向 的 输出、标志、420、0、0、0。
            若 fd 大于或等于 0：
                做 sys_dup2 用 fd、1、0、0、0、0。
                做 sys_close 用 fd、0、0、0、0、0。
            完。
        完。
        若 重定向 的 输入 不等于 空：
            保存stdin 为 做 sys_dup 用 0、0、0、0、0、0。
            fd 为 做 sys_open 用 重定向 的 输入、O_RDONLY、0、0、0、0。
            若 fd 大于或等于 0：
                做 sys_dup2 用 fd、0、0、0、0、0。
                做 sys_close 用 fd、0、0、0、0、0。
            完。
        完。
    完。
    结果 为 做 执行内建 用 参数。
    若 保存stdout 大于或等于 0：
        做 sys_dup2 用 保存stdout、1、0、0、0、0。
        做 sys_close 用 保存stdout、0、0、0、0、0。
    完。
    若 保存stdin 大于或等于 0：
        做 sys_dup2 用 保存stdin、0、0、0、0、0。
        做 sys_close 用 保存stdin、0、0、0、0、0。
    完。
    返还 结果。
完。

执行内建 接受 参数：
    名称 为 参数 的 第 0 项。
    argc 为 做 计数 用 参数。
    若 名称 等于 「cd」：
        返还 做 内建_cd 用 参数。
    完。
    若 名称 等于 「pwd」：
        返还 做 内建_pwd 用 参数。
    完。
    若 名称 等于 「echo」：
        返还 做 内建_echo 用 参数。
    完。
    若 名称 等于 「export」：
        返还 做 内建_export 用 参数。
    完。
    若 名称 等于 「unset」：
        返还 做 内建_unset 用 参数。
    完。
    若 名称 等于 「help」：
        返还 做 内建_help 用 参数。
    完。
    若 名称 等于 「history」：
        返还 做 内建_history 用 参数。
    完。
    若 名称 等于 「type」：
        返还 做 内建_type 用 参数。
    完。
    若 名称 等于 「alias」：
        返还 做 内建_alias 用 参数。
    完。
    若 名称 等于 「unalias」：
        返还 做 内建_unalias 用 参数。
    完。
    若 名称 等于 「jobs」：
        返还 做 内建_jobs 用 参数。
    完。
    若 名称 等于 「fg」：
        返还 做 内建_fg 用 参数。
    完。
    若 名称 等于 「bg」：
        返还 做 内建_bg 用 参数。
    完。
    若 名称 等于 「wait」：
        返还 做 内建_wait 用 参数。
    完。
    若 名称 等于 「kill」：
        返还 做 内建_kill 用 参数。
    完。
    若 名称 等于 「test」：
        返还 做 内建_test 用 参数。
    完。
    若 名称 等于 「true」：
        返还 0。
    完。
    若 名称 等于 「false」：
        返还 1。
    完。
    若 名称 等于 「read」：
        返还 做 内建_read 用 参数。
    完。
    若 名称 等于 「source」：
        返还 做 内建_source 用 参数。
    完。
    若 名称 等于 「umask」：
        返还 做 内建_umask 用 参数。
    完。
    若 名称 等于 「set」：
        返还 做 内建_set 用 参数。
    完。
    做 打印行 用 「龙壳: 」 加 名称 加 「: 未知内建命令」。
    返还 1。
完。

内建_cd 接受 参数：
    可变 目标 为 「」。
    若 做 计数 用 参数 小于或等于 1：
        目标 为 做 环境_获取 用 「HOME」。
        若 目标 等于 「」：
            做 打印行 用 「龙壳: cd: HOME未设置」。
            返还 1。
        完。
    否则若 参数 的 第 1 项 等于 「-」：
        目标 为 做 环境_获取 用 「OLDPWD」。
        若 目标 等于 「」：
            做 打印行 用 「龙壳: cd: OLDPWD未设置」。
            返还 1。
        完。
        做 打印行 用 目标。
    否则若 参数 的 第 1 项 等于 「~」：
        目标 为 做 环境_获取 用 「HOME」。
    否则：
        目标 为 参数 的 第 1 项。
    完。
    若 做 字符串开头 用 目标、「~/」：
        目标 为 做 环境_获取 用 「HOME」 加 做 字符串子串 用 目标、1、做 字符串长度 用 目标。
    完。
    结果 为 做 sys_chdir 用 目标、0、0、0、0、0。
    若 结果 小于 0：
        做 打印行 用 「龙壳: cd: 」 加 目标 加 「: 目录不存在或无权限」。
        返还 1。
    完。
    旧pwd 为 做 环境_获取 用 「PWD」。
    做 环境_设置 用 「OLDPWD」、旧pwd。
    buf 为 做 创建缓冲 用 4096。
    做 sys_getcwd 用 buf、4096、0、0、0、0。
    做 环境_设置 用 「PWD」、做 缓冲_转字符串 用 buf。
    返还 0。
完。

内建_pwd 接受 参数：
    做 打印行 用 做 环境_获取 用 「PWD」。
    返还 0。
完。

内建_echo 接受 参数：
    可变 换行 为 真。
    可变 转义 为 假。
    可变 起始 为 1。
    只要 起始 小于 做 计数 用 参数：
        若 参数 的 第 起始 项 等于 「-n」：
            换行 为 假。
            起始 为 起始 加 1。
        否则若 参数 的 第 起始 项 等于 「-e」：
            转义 为 真。
            起始 为 起始 加 1。
        否则若 参数 的 第 起始 项 等于 「-ne」 或者 参数 的 第 起始 项 等于 「-en」：
            换行 为 假。
            转义 为 真。
            起始 为 起始 加 1。
        否则：
            中断。
        完。
    完。
    可变 i 为 起始。
    只要 i 小于 做 计数 用 参数：
        若 i 大于 起始：
            做 标准输出 用 「 」。
        完。
        若 转义：
            做 标准输出 用 做 echo_转义 用 参数 的 第 i 项。
        否则：
            做 标准输出 用 参数 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 换行：
        做 标准输出 用 「\n」。
    完。
    返还 0。
完。

echo_转义 接受 文本：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        若 做 字符串取字符 用 文本、i 等于 「\\」 并且 i 加 1 小于 做 字符串长度 用 文本：
            下一个 为 做 字符串取字符 用 文本、i 加 1。
            若 下一个 等于 「n」：
                结果 为 结果 加 「\n」。
                i 为 i 加 2。
                继续。
            完。
            若 下一个 等于 「t」：
                结果 为 结果 加 「\t」。
                i 为 i 加 2。
                继续。
            完。
            若 下一个 等于 「\\」：
                结果 为 结果 加 「\\」。
                i 为 i 加 2。
                继续。
            完。
            若 下一个 等于 「a」：
                结果 为 结果 加 「\a」。
                i 为 i 加 2。
                继续。
            完。
        完。
        结果 为 结果 加 做 字符串取字符 用 文本、i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

内建_export 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        做 环境_显示全部。
        返还 0。
    完。
    可变 i 为 1。
    只要 i 小于 做 计数 用 参数：
        arg 为 参数 的 第 i 项。
        等号位 为 做 字符串索引 用 arg、「=」。
        若 等号位 大于或等于 0：
            键 为 做 字符串子串 用 arg、0、等号位。
            值 为 做 字符串子串 用 arg、等号位 加 1、做 字符串长度 用 arg。
            做 环境_设置 用 键、值。
            做 环境_标记导出 用 键。
        否则：
            做 环境_标记导出 用 arg。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

可变 AI_是内建命令_调用数 为 0。

可变 AI_是内建命令_异常数 为 0。

AI_是内建命令_监控 接受 零参：
    AI_是内建命令_调用数 为 AI_是内建命令_调用数 加 1。
    返还 AI_是内建命令_异常数 等于 0。
完。

AI_是内建命令_健康检查 接受 零参：
    若 AI_是内建命令_调用数 大于 0 并且 AI_是内建命令_异常数 乘 100 除以 AI_是内建命令_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
