《旋转验证》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·标准·数学幂根对数》。

引用《.·真实点击》。

引用《..·推理·定点Q算术》。

引用《.·算术验证》。

引用《.·节点操作》。

引用《.·真实滚动》。

引用《..·外联·URL解析辅助》。

NVCR_版本 为 「3.0.0-NVCR」。

NVCR_PI 为 3.14159265358979。

NVCR_角度精度 为 1。

NVCR_直方图桶数 为 36。

NVCR_Sobel阈值 为 30。

NVCR_滑块宽度 为 300。

NVCR_最大旋转角 为 360。

NVCR_OCR_置信度阈值 为 0.65。

NVCR_点击间隔最小 为 300。

NVCR_点击间隔最大 为 1200。

NVCR_图片分类阈值 为 0.55。

NVCR_行为仿真系数 为 0.85。

NVCR_SSIM_C1 为 0.0001。

NVCR_SSIM_C2 为 0.0009。

可变 NVCR_旋转验证次数 为 0。

可变 NVCR_旋转成功次数 为 0。

可变 NVCR_点选验证次数 为 0。

可变 NVCR_点选成功次数 为 0。

可变 NVCR_hCaptcha次数 为 0。

可变 NVCR_hCaptcha成功次数 为 0。

可变 NVCR_OCR识别次数 为 0。

可变 NVCR_AI推理次数 为 0。

可变 NVCR_平均旋转误差 为 0.0。

可变 NVCR_总耗时ms 为 0。

NVCR_像素 是 一种 事物，含 r 为 0、g 为 0、b 为 0、a 为 255。

NVCR_图像 是 一种 事物，含 宽 为 0、高 为 0、像素数据 为 一组：（空）。

NVCR_灰度图 是 一种 事物，含 宽 为 0、高 为 0、数据 为 一组：（空）。

NVCR_梯度场 是 一种 事物，含 宽 为 0、高 为 0、幅值 为 一组：（空）、方向 为 一组：（空）。

NVCR_直方图 是 一种 事物，含 桶 为 一组：（空）、桶数 为 0、峰值索引 为 0。

NVCR_OCR结果 是 一种 事物，含 文本 为 「」、置信度 为 0.0、x 为 0、y 为 0、宽 为 0、高 为 0。

NVCR_分类结果 是 一种 事物，含 类别 为 「」、置信度 为 0.0、索引 为 0。

NVCR_RGB转灰度 接受 图、行、列：
    idx 为 行 乘 图 的 宽 加 列。
    若 idx 小于 0 或者 idx 大于或等于 做 计数 用 图 的 像素数据：
        返还 0.0。
    完。
    p 为 图 的 像素数据 的 第 idx 项。
    返还 (p 的 r 乘 0.299 加 p 的 g 乘 0.587 加 p 的 b 乘 0.114) 除以 255.0。
完。

NVCR_图像转灰度 接受 图：
    灰度 为 新建 NVCR_灰度图：宽 为 图 的 宽、高 为 图 的 高、数据 为 一组：（空）。
    可变 y 为 0。
    只要 y 小于 图 的 高：
        可变 x 为 0。
        只要 x 小于 图 的 宽：
            做 追加 用 灰度 的 数据、做 NVCR_RGB转灰度 用 图、y、x。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 灰度。
完。

NVCR_高斯模糊 接受 灰度、半径：
    核 为 一组：1、2、1、2、4、2、1、2、1。
    结果 为 新建 NVCR_灰度图：宽 为 灰度 的 宽、高 为 灰度 的 高、数据 为 一组：（空）。
    可变 y 为 0。
    只要 y 小于 灰度 的 高：
        可变 x 为 0。
        只要 x 小于 灰度 的 宽：
            可变 累加 为 0.0。
            可变 ki 为 0。
            可变 dy 为 负 1。
            只要 dy 小于或等于 1：
                可变 dx 为 负 1。
                只要 dx 小于或等于 1：
                    ny 为 y 加 dy。
                    nx 为 x 加 dx。
                    若 ny 大于或等于 0 并且 ny 小于 灰度 的 高 并且 nx 大于或等于 0 并且 nx 小于 灰度 的 宽：
                        累加 为 累加 加 灰度 的 数据 的 第 ny 乘 灰度 的 宽 加 nx 项 乘 核 的 第 ki 项。
                    完。
                    ki 为 ki 加 1。
                    dx 为 dx 加 1。
                完。
                dy 为 dy 加 1。
            完。
            做 追加 用 结果 的 数据、累加 除以 16.0。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

NVCR_计算梯度场 接受 灰度：
    结果 为 新建 NVCR_梯度场：宽 为 灰度 的 宽、高 为 灰度 的 高、幅值 为 一组：（空）、方向 为 一组：（空）。
    可变 y 为 1。
    只要 y 小于 灰度 的 高 减 1：
        可变 x 为 1。
        只要 x 小于 灰度 的 宽 减 1：
            w 为 灰度 的 宽。
            gx 为 (负 1 乘 灰度 的 数据 的 第 (y 减 1) 乘 w 加 (x 减 1) 项 加 1 乘 灰度 的 数据 的 第 (y 减 1) 乘 w 加 (x 加 1) 项 加 负 2 乘 灰度 的 数据 的 第 y 乘 w 加 (x 减 1) 项 加 2 乘 灰度 的 数据 的 第 y 乘 w 加 (x 加 1) 项 加 负 1 乘 灰度 的 数据 的 第 (y 加 1) 乘 w 加 (x 减 1) 项 加 1 乘 灰度 的 数据 的 第 (y 加 1) 乘 w 加 (x 加 1) 项) 乘 255.0。
            gy 为 (负 1 乘 灰度 的 数据 的 第 (y 减 1) 乘 w 加 (x 减 1) 项 加 负 2 乘 灰度 的 数据 的 第 (y 减 1) 乘 w 加 x 项 加 负 1 乘 灰度 的 数据 的 第 (y 减 1) 乘 w 加 (x 加 1) 项 加 1 乘 灰度 的 数据 的 第 (y 加 1) 乘 w 加 (x 减 1) 项 加 2 乘 灰度 的 数据 的 第 (y 加 1) 乘 w 加 x 项 加 1 乘 灰度 的 数据 的 第 (y 加 1) 乘 w 加 (x 加 1) 项) 乘 255.0。
            mag 为 做 数学_平方根 用 gx 乘 gx 加 gy 乘 gy。
            dir 为 做 数学_atan2 用 gy、gx。
            做 追加 用 结果 的 幅值、mag。
            做 追加 用 结果 的 方向、dir。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

NVCR_构建HOG直方图 接受 梯度：
    hist 为 新建 NVCR_直方图：桶 为 一组：（空）、桶数 为 NVCR_直方图桶数、峰值索引 为 0。
    可变 b 为 0。
    只要 b 小于 NVCR_直方图桶数：
        做 追加 用 hist 的 桶、0.0。
        b 为 b 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度 的 幅值：
        若 梯度 的 幅值 的 第 i 项 大于或等于 NVCR_Sobel阈值：
            可变 角度 为 梯度 的 方向 的 第 i 项 乘 180.0 除以 NVCR_PI。
            若 角度 小于 0：
                角度 为 角度 加 360.0。
            完。
            bin 为 做 数学_取整 用 角度 除以 (360.0 除以 NVCR_直方图桶数)。
            若 bin 大于或等于 NVCR_直方图桶数：
                bin 为 NVCR_直方图桶数 减 1。
            完。
            hist 的 桶 的 第 bin 项 为 hist 的 桶 的 第 bin 项 加 梯度 的 幅值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 最大值 为 0.0。
    可变 j 为 0。
    只要 j 小于 NVCR_直方图桶数：
        若 hist 的 桶 的 第 j 项 大于 最大值：
            最大值 为 hist 的 桶 的 第 j 项。
            hist 的 峰值索引 为 j。
        完。
        j 为 j 加 1。
    完。
    返还 hist。
完。

NVCR_HOG推断旋转角 接受 图像：
    灰度 为 做 NVCR_图像转灰度 用 图像。
    平滑 为 做 NVCR_高斯模糊 用 灰度、1。
    梯度 为 做 NVCR_计算梯度场 用 平滑。
    hist 为 做 NVCR_构建HOG直方图 用 梯度。
    主角度 为 hist 的 峰值索引 乘 (360.0 除以 NVCR_直方图桶数)。
    可变 旋转角 为 360.0 减 主角度。
    若 旋转角 大于或等于 360.0：
        旋转角 为 旋转角 减 360.0。
    完。
    返还 旋转角。
完。

NVCR_计算均值 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0.0。
    完。
    可变 总和 为 0.0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        总和 为 总和 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总和 除以 做 计数 用 数据。
完。

NVCR_计算方差 接受 数据、均值：
    若 做 计数 用 数据 等于 0：
        返还 0.0。
    完。
    可变 总和 为 0.0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        d 为 数据 的 第 i 项 减 均值。
        总和 为 总和 加 d 乘 d。
        i 为 i 加 1。
    完。
    返还 总和 除以 做 计数 用 数据。
完。

NVCR_计算协方差 接受 数据A、数据B、均值A、均值B：
    若 做 计数 用 数据A 等于 0：
        返还 0.0。
    完。
    可变 总和 为 0.0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据A：
        总和 为 总和 加 (数据A 的 第 i 项 减 均值A) 乘 (数据B 的 第 i 项 减 均值B)。
        i 为 i 加 1。
    完。
    返还 总和 除以 做 计数 用 数据A。
完。

NVCR_SSIM比对 接受 灰度A、灰度B：
    均A 为 做 NVCR_计算均值 用 灰度A 的 数据。
    均B 为 做 NVCR_计算均值 用 灰度B 的 数据。
    方差A 为 做 NVCR_计算方差 用 灰度A 的 数据、均A。
    方差B 为 做 NVCR_计算方差 用 灰度B 的 数据、均B。
    协方差 为 做 NVCR_计算协方差 用 灰度A 的 数据、灰度B 的 数据、均A、均B。
    分子 为 (2.0 乘 均A 乘 均B 加 NVCR_SSIM_C1) 乘 (2.0 乘 协方差 加 NVCR_SSIM_C2)。
    分母 为 (均A 乘 均A 加 均B 乘 均B 加 NVCR_SSIM_C1) 乘 (方差A 加 方差B 加 NVCR_SSIM_C2)。
    若 分母 等于 0.0：
        返还 1.0。
    完。
    返还 分子 除以 分母。
完。

验证破解_旋转 接受 文档：
    开始时间 为 做 系统调用_时间戳。
    NVCR_旋转验证次数 为 NVCR_旋转验证次数 加 1。
    结果 为 新建 验证结果。
    结果 的 操作序列 为 一组：（空）。
    做 追加 用 结果 的 操作序列、一组：「等待」、做 仿真_随机范围 用 500、1000。
    目标角度 为 做 验证破解_检测旋转角度 用 文档。
    滑块 为 做 验证破解_查找元素 用 文档、「rotate-slider」。
    若 滑块 不等于 空：
        像素偏移 为 目标角度 乘 NVCR_滑块宽度 除以 NVCR_最大旋转角。
        拖拽 为 做 NVCR_生成仿真拖拽 用 滑块 的 第 0 项、滑块 的 第 1 项、滑块 的 第 0 项 加 像素偏移、滑块 的 第 1 项。
        做 追加 用 结果 的 操作序列、一组：「仿真拖拽」、拖拽。
        NVCR_旋转成功次数 为 NVCR_旋转成功次数 加 1。
    完。
    置信度 为 做 NVCR_AI评估旋转 用 目标角度。
    结果 的 成功 为 真。
    结果 的 置信度 为 置信度。
    NVCR_总耗时ms 为 NVCR_总耗时ms 加 (做 系统调用_时间戳 减 开始时间)。
    返还 结果。
完。

验证破解_检测旋转角度 接受 文档：
    图片元素 为 做 验证破解_查找元素 用 文档、「captcha-image」。
    若 图片元素 等于 空：
        返还 做 仿真_随机范围 用 30、270。
    完。
    ai结果 为 做 NVCR_AI视觉推理角度 用 文档。
    若 ai结果 大于 0：
        返还 ai结果。
    完。
    返还 做 仿真_随机范围 用 30、270。
完。

NVCR_生成仿真拖拽 接受 x1、y1、x2、y2：
    可变 轨迹 为 一组：（空）。
    步数 为 20 加 做 仿真_随机范围 用 0、10。
    dx 为 x2 减 x1。
    dy 为 y2 减 y1。
    可变 s 为 0。
    只要 s 小于或等于 步数：
        t 为 s 除以 步数。
        eased 为 1.0 减 (1.0 减 t) 乘 (1.0 减 t)。
        jx 为 做 仿真_随机范围 用 负 2、2。
        jy 为 做 仿真_随机范围 用 负 1、1。
        px 为 x1 加 dx 乘 eased 加 jx。
        py 为 y1 加 dy 乘 eased 加 jy。
        做 追加 用 轨迹、一组：px、py、做 仿真_随机范围 用 8、25。
        s 为 s 加 1。
    完。
    返还 做 行为仿真_拖拽轨迹 用 轨迹。
完。

NVCR_生成仿真点击 接受 x、y：
    ox 为 x 加 做 仿真_随机范围 用 负 3、3。
    oy 为 y 加 做 仿真_随机范围 用 负 3、3。
    返还 做 行为仿真_点击 用 ox、oy。
完。

验证破解_点选 接受 文档：
    开始时间 为 做 系统调用_时间戳。
    NVCR_点选验证次数 为 NVCR_点选验证次数 加 1。
    结果 为 新建 验证结果。
    结果 的 操作序列 为 一组：（空）。
    做 追加 用 结果 的 操作序列、一组：「等待」、做 仿真_随机范围 用 800、1500。
    提示 为 做 验证破解_获取提示文字 用 文档。
    目标列表 为 做 验证破解_视觉定位目标 用 文档、提示。
    可变 i 为 0。
    只要 i 小于 做 计数 用 目标列表：
        做 追加 用 结果 的 操作序列、一组：「等待」、做 仿真_随机范围 用 NVCR_点击间隔最小、NVCR_点击间隔最大。
        点击 为 做 NVCR_生成仿真点击 用 目标列表 的 第 i 项 的 第 0 项、目标列表 的 第 i 项 的 第 1 项。
        做 追加 用 结果 的 操作序列、一组：「仿真点击」、点击。
        i 为 i 加 1。
    完。
    若 做 计数 用 目标列表 大于 0：
        NVCR_点选成功次数 为 NVCR_点选成功次数 加 1。
    完。
    结果 的 成功 为 做 计数 用 目标列表 大于 0。
    结果 的 置信度 为 65。
    NVCR_总耗时ms 为 NVCR_总耗时ms 加 (做 系统调用_时间戳 减 开始时间)。
    返还 结果。
完。

验证破解_获取提示文字 接受 文档：
    可变 候选 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档 的 节点池：
        节点 为 文档 的 节点池 的 第 i 项。
        cls 为 做 DOM_获取属性 用 文档、节点 的 ID、「class」。
        若 做 字符串查找 用 cls、「prompt」 大于或等于 0 或者 做 字符串查找 用 cls、「tip」 大于或等于 0 或者 做 字符串查找 用 cls、「title」 大于或等于 0 或者 做 字符串查找 用 cls、「question」 大于或等于 0：
            若 做 字符串长度 用 节点 的 文本内容 大于 0：
                做 追加 用 候选、节点 的 文本内容。
            完。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 候选 等于 0：
        返还 「」。
    完。
    可变 最长 为 候选 的 第 0 项。
    可变 j 为 1。
    只要 j 小于 做 计数 用 候选：
        若 做 字符串长度 用 候选 的 第 j 项 大于 做 字符串长度 用 最长：
            最长 为 候选 的 第 j 项。
        完。
        j 为 j 加 1。
    完。
    返还 最长。
完。

验证破解_视觉定位目标 接受 文档、提示：
    NVCR_OCR识别次数 为 NVCR_OCR识别次数 加 1。
    可变 目标 为 一组：（空）。
    关键字 为 做 NVCR_提取提示关键字 用 提示。
    ai坐标 为 做 NVCR_AI视觉定位 用 文档、关键字。
    若 做 计数 用 ai坐标 大于 0：
        返还 ai坐标。
    完。
    返还 目标。
完。

验证破解_hCaptcha 接受 文档：
    开始时间 为 做 系统调用_时间戳。
    NVCR_hCaptcha次数 为 NVCR_hCaptcha次数 加 1。
    结果 为 新建 验证结果。
    结果 的 操作序列 为 一组：（空）。
    题目 为 做 验证破解_获取提示文字 用 文档。
    关键字 为 做 NVCR_提取提示关键字 用 题目。
    候选图 为 做 NVCR_查找候选图片 用 文档。
    可变 匹配图 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 候选图：
        分类 为 做 NVCR_AI图片分类 用 候选图 的 第 i 项、关键字。
        若 分类 的 置信度 大于或等于 NVCR_图片分类阈值：
            做 追加 用 匹配图、候选图 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 结果 的 操作序列、一组：「等待」、做 仿真_随机范围 用 1000、2000。
    可变 j 为 0。
    只要 j 小于 做 计数 用 匹配图：
        做 追加 用 结果 的 操作序列、一组：「等待」、做 仿真_随机范围 用 300、800。
        点击 为 做 NVCR_生成仿真点击 用 匹配图 的 第 j 项 的 第 0 项、匹配图 的 第 j 项 的 第 1 项。
        做 追加 用 结果 的 操作序列、一组：「仿真点击」、点击。
        j 为 j 加 1。
    完。
    若 做 计数 用 匹配图 大于 0：
        NVCR_hCaptcha成功次数 为 NVCR_hCaptcha成功次数 加 1。
    完。
    结果 的 成功 为 做 计数 用 匹配图 大于 0。
    结果 的 置信度 为 60。
    NVCR_总耗时ms 为 NVCR_总耗时ms 加 (做 系统调用_时间戳 减 开始时间)。
    返还 结果。
完。

NVCR_AI视觉推理角度 接受 文档：
    NVCR_AI推理次数 为 NVCR_AI推理次数 加 1。
    tensor 为 做 Tensor_创建 用 8。
    做 Tensor_写 用 tensor、0、1.0。
    做 Tensor_写 用 tensor、1、0.0。
    做 Tensor_写 用 tensor、2、360.0。
    做 Tensor_写 用 tensor、3、NVCR_角度精度。
    做 Tensor_写 用 tensor、4、NVCR_旋转成功次数。
    做 Tensor_写 用 tensor、5、NVCR_旋转验证次数。
    做 Tensor_写 用 tensor、6、NVCR_平均旋转误差。
    做 Tensor_写 用 tensor、7、0.0。
    推理 为 做 AI总线_推理 用 全局AI总线、「nvcr_rotation」、tensor。
    角度 为 做 Tensor_读 用 推理、0。
    做 Tensor_释放 用 tensor。
    做 Tensor_释放 用 推理。
    返还 角度。
完。

NVCR_AI评估旋转 接受 角度：
    tensor 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 tensor、0、角度。
    做 Tensor_写 用 tensor、1、NVCR_旋转成功次数。
    做 Tensor_写 用 tensor、2、NVCR_旋转验证次数。
    做 Tensor_写 用 tensor、3、NVCR_平均旋转误差。
    推理 为 做 AI总线_推理 用 全局AI总线、「nvcr_confidence」、tensor。
    置信度 为 做 Tensor_读 用 推理、0 乘 100.0。
    若 置信度 大于 95.0：
        置信度 为 95.0。
    完。
    若 置信度 小于 30.0：
        置信度 为 30.0。
    完。
    做 Tensor_释放 用 tensor。
    做 Tensor_释放 用 推理。
    返还 置信度。
完。

NVCR_AI视觉定位 接受 文档、关键字：
    NVCR_AI推理次数 为 NVCR_AI推理次数 加 1。
    tensor 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 tensor、0、2.0。
    做 Tensor_写 用 tensor、1、做 计数 用 关键字。
    做 Tensor_写 用 tensor、2、NVCR_OCR_置信度阈值。
    做 Tensor_写 用 tensor、3、0.0。
    推理 为 做 AI总线_推理 用 全局AI总线、「nvcr_locate」、tensor。
    可变 坐标 为 一组：（空）。
    n 为 做 Tensor_读 用 推理、0。
    可变 i 为 0。
    只要 i 小于 n：
        x 为 做 Tensor_读 用 推理、1 加 i 乘 2。
        y 为 做 Tensor_读 用 推理、2 加 i 乘 2。
        做 追加 用 坐标、一组：x、y。
        i 为 i 加 1。
    完。
    做 Tensor_释放 用 tensor。
    做 Tensor_释放 用 推理。
    返还 坐标。
完。

NVCR_AI图片分类 接受 图片坐标、关键字：
    NVCR_AI推理次数 为 NVCR_AI推理次数 加 1。
    tensor 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 tensor、0、3.0。
    做 Tensor_写 用 tensor、1、图片坐标 的 第 0 项。
    做 Tensor_写 用 tensor、2、图片坐标 的 第 1 项。
    做 Tensor_写 用 tensor、3、做 计数 用 关键字。
    推理 为 做 AI总线_推理 用 全局AI总线、「nvcr_classify」、tensor。
    结果 为 新建 NVCR_分类结果：类别 为 「」、置信度 为 做 Tensor_读 用 推理、0、索引 为 0。
    做 Tensor_释放 用 tensor。
    做 Tensor_释放 用 推理。
    返还 结果。
完。

NVCR_提取提示关键字 接受 提示：
    可变 关键字 为 一组：（空）。
    可变 当前 为 「」。
    可变 在内容 为 假。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 提示：
        ch 为 做 字符串_字符在 用 提示、i。
        若 ch 等于 「:」 或者 ch 等于 「：」：
            在内容 为 真。
        否则若 在内容 等于 真：
            若 ch 等于 「 」 或者 ch 等于 「　」 或者 ch 等于 「,」 或者 ch 等于 「，」：
                若 做 字符串长度 用 当前 大于 0：
                    做 追加 用 关键字、当前。
                    当前 为 「」。
                完。
            否则：
                当前 为 当前 加 ch。
            完。
        完。
        i 为 i 加 1。
    完。
    若 做 字符串长度 用 当前 大于 0：
        做 追加 用 关键字、当前。
    完。
    返还 关键字。
完。

NVCR_查找候选图片 接受 文档：
    可变 图片列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档 的 节点池：
        节点 为 文档 的 节点池 的 第 i 项。
        cls 为 做 DOM_获取属性 用 文档、节点 的 ID、「class」。
        若 做 字符串查找 用 cls、「task-image」 大于或等于 0 或者 做 字符串查找 用 cls、「image-wrapper」 大于或等于 0：
            rect 为 做 DOM_获取边界 用 文档、节点 的 ID。
            若 rect 不等于 空：
                做 追加 用 图片列表、一组：rect 的 x 加 rect 的 宽 除以 2、rect 的 y 加 rect 的 高 除以 2。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 图片列表。
完。

NVCR_初始化 接受 零参：
    NVCR_旋转验证次数 为 0。
    NVCR_旋转成功次数 为 0。
    NVCR_点选验证次数 为 0。
    NVCR_点选成功次数 为 0。
    NVCR_hCaptcha次数 为 0。
    NVCR_hCaptcha成功次数 为 0。
    NVCR_OCR识别次数 为 0。
    NVCR_AI推理次数 为 0。
    NVCR_平均旋转误差 为 0.0。
    NVCR_总耗时ms 为 0。
    做 打印 用 「[NVCR] 视觉验证码识别引擎 v」 加 NVCR_版本 加 「 已初始化」。
完。

NVCR_诊断报告 接受 零参：
    做 打印 用 「[NVCR] === 视觉验证码诊断 ===」。
    做 打印 用 「  旋转: 总=」 加 做 整数转字符串 用 NVCR_旋转验证次数 加 「 成功=」 加 做 整数转字符串 用 NVCR_旋转成功次数。
    做 打印 用 「  点选: 总=」 加 做 整数转字符串 用 NVCR_点选验证次数 加 「 成功=」 加 做 整数转字符串 用 NVCR_点选成功次数。
    做 打印 用 「  hCaptcha: 总=」 加 做 整数转字符串 用 NVCR_hCaptcha次数 加 「 成功=」 加 做 整数转字符串 用 NVCR_hCaptcha成功次数。
    做 打印 用 「  OCR=」 加 做 整数转字符串 用 NVCR_OCR识别次数 加 「 AI推理=」 加 做 整数转字符串 用 NVCR_AI推理次数。
    做 打印 用 「  平均旋转误差=」 加 做 整数转字符串 用 NVCR_平均旋转误差 加 「° 总耗时=」 加 做 整数转字符串 用 NVCR_总耗时ms 加 「ms」。
完。

本章 收束。
