《隐匿初始化》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

代理_直连 为 0。

代理_HTTP 为 1。

代理_HTTPS 为 2。

代理_SOCKS5 为 3。

代理_住宅IP 为 4。

代理_数据中心 为 5。

代理_移动4G 为 6。

代理_Tor 为 7。

代理状态_可用 为 0。

代理状态_繁忙 为 1。

代理状态_失败 为 2。

代理状态_封禁 为 3。

代理状态_过期 为 4。

代理节点 是 一种 事物，含 ID、类型、地址、端口、用户名、密码、国家、城市、ISP、状态、延迟、成功率、最后使用、使用次数、带宽、是否住宅、风险评分。

代理池 是 一种 事物，含 节点列表、国家索引、当前节点、轮换策略、轮换间隔、上次轮换、最大重试。

IP隐匿配置 是 一种 事物，含 启用代理、目标国家、仅住宅IP、防域名泄露、防WebRTC泄露、自动轮换、轮换间隔分钟、同域保持IP、排除域名。

可变 IP_代理池 为 空。

可变 IP_配置 为 空。

可变 IP_域名映射 为 一组：（空）。

可变 IP_已初始化 为 假。

可变 IP_总请求数 为 0。

可变 IP_轮换次数 为 0。

IP隐匿_初始化 接受 零参：
    IP_代理池 为 新建 代理池。
    IP_代理池 的 节点列表 为 一组：（空）。
    IP_代理池 的 国家索引 为 一组：（空）。
    IP_代理池 的 当前节点 为 0。
    IP_代理池 的 轮换策略 为 3。
    IP_代理池 的 轮换间隔 为 300000。
    IP_代理池 的 最大重试 为 3。
    IP_配置 为 新建 IP隐匿配置。
    IP_配置 的 启用代理 为 假。
    IP_配置 的 目标国家 为 「US」。
    IP_配置 的 仅住宅IP 为 真。
    IP_配置 的 防域名泄露 为 真。
    IP_配置 的 防WebRTC泄露 为 真。
    IP_配置 的 自动轮换 为 真。
    IP_配置 的 轮换间隔分钟 为 5。
    IP_配置 的 同域保持IP 为 真。
    IP_配置 的 排除域名 为 一组：「localhost」、「127.0.0.1」、「nova:」。
    IP_域名映射 为 一组：（空）。
    IP_已初始化 为 真。
    做 内核信息 用 「IP隐匿引擎: 住宅IP/SOCKS5/HTTP/Tor 多协议代理就绪」。
完。

本章 收束。
