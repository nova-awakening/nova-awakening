《龙壳入口》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《.·任务控制》。

引用《.·文本处理环》。

引用《..·中枢·字符串替换》。

引用《..·中枢·线程clone语义》。

引用《..·标准·标准流》。

引用《..·标准·原生字符串》。

引用《.·设置重定向》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内核打印》。

引用《.·是内建命令》。

引用《.·命令解析器》。

引用《.·龙壳执行管道》。

引用《..·外联·浏览器核心》。

引用《..·中枢·虚拟内存区》。

引用《..·中枢·获取进程》。

可变 龙壳_运行中 为 真。

可变 龙壳_交互模式 为 真。

可变 龙壳_上次退出码 为 0。

可变 龙壳_上次后台PID 为 0。

可变 龙壳_PID 为 0。

可变 龙壳_嵌套层数 为 0。

龙壳版本 为 「1.0.0-M1」。

龙壳名称 为 「龙壳」。

龙壳_入口 接受 argc、argv：
    龙壳_PID 为 做 sys_getpid 用 0、0、0、0、0、0。
    做 环境_初始化。
    做 历史_初始化。
    做 任务_初始化。
    做 龙壳_设置信号。
    若 argc 大于 1：
        龙壳_交互模式 为 假。
        返还 做 龙壳_执行脚本 用 argv 的 第 1 项。
    完。
    若 argc 大于 0 并且 argv 的 第 0 项 等于 「-c」：
        龙壳_交互模式 为 假。
        返还 做 龙壳_执行单行 用 argv 的 第 1 项。
    完。
    龙壳_交互模式 为 真。
    做 龙壳_欢迎。
    做 龙壳_主循环。
    返还 龙壳_上次退出码。
完。

龙壳_欢迎 接受 零参：
    做 打印行 用 「╔══════════════════════════════════════╗」。
    做 打印行 用 「║  龙壳 (Dragon终端) v」 加 龙壳版本 加 「     ║」。
    做 打印行 用 「║  NovaOS M1 原生终端                 ║」。
    做 打印行 用 「║  输入 help 查看内建命令               ║」。
    做 打印行 用 「╚══════════════════════════════════════╝」。
完。

龙壳_主循环 接受 零参：
    只要 龙壳_运行中：
        做 任务_回收后台。
        提示符 为 做 龙壳_生成提示符。
        做 标准输出 用 提示符。
        输入 为 做 读取行。
        若 输入 等于 空：
            若 龙壳_交互模式：
                做 打印行 用 「exit」。
                龙壳_运行中 为 假。
            完。
            继续。
        完。
        去空白 为 做 字符串去首尾空白 用 输入。
        若 去空白 等于 「」 或者 做 字符串取字符 用 去空白、0 等于 「#」：
            继续。
        完。
        做 历史_添加 用 去空白。
        龙壳_上次退出码 为 做 龙壳_执行行 用 去空白。
    完。
完。

龙壳_生成提示符 接受 零参：
    用户 为 做 环境_获取 用 「USER」。
    主机 为 「novaos」。
    cwd 为 做 环境_获取 用 「PWD」。
    home 为 做 环境_获取 用 「HOME」。
    若 做 字符串开头 用 cwd、home：
        cwd 为 「~」 加 做 字符串子串 用 cwd、做 字符串长度 用 home、做 字符串长度 用 cwd。
    完。
    尾符 为 做 如果选字符 用 用户 等于 「root」、「# 」、「$ 」。
    返还 用户 加 「@」 加 主机 加 「:」 加 cwd 加 尾符。
完。

龙壳_执行行 接受 行：
    语句列表 为 做 分割_分号 用 行。
    可变 最后退出码 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列表：
        语句 为 做 字符串去首尾空白 用 语句列表 的 第 i 项。
        若 语句 不等于 「」：
            最后退出码 为 做 龙壳_执行语句 用 语句。
        完。
        i 为 i 加 1。
    完。
    返还 最后退出码。
完。

龙壳_执行语句 接受 语句：
    若 做 字符串包含 用 语句、「 && 」：
        部分 为 做 字符串分割 用 语句、「 && 」。
        可变 j 为 0。
        只要 j 小于 做 计数 用 部分：
            结果 为 做 龙壳_执行语句 用 做 字符串去首尾空白 用 部分 的 第 j 项。
            若 结果 不等于 0：
                返还 结果。
            完。
            j 为 j 加 1。
        完。
        返还 0。
    完。
    若 做 字符串包含 用 语句、「 || 」：
        部分 为 做 字符串分割 用 语句、「 || 」。
        可变 j 为 0。
        只要 j 小于 做 计数 用 部分：
            结果 为 做 龙壳_执行语句 用 做 字符串去首尾空白 用 部分 的 第 j 项。
            若 结果 等于 0：
                返还 0。
            完。
            j 为 j 加 1。
        完。
        返还 1。
    完。
    可变 后台 为 假。
    若 做 字符串结尾 用 语句、「&」 并且 非 做 字符串结尾 用 语句、「&&」：
        后台 为 真。
        语句 为 做 字符串去首尾空白 用 做 字符串子串 用 语句、0、做 字符串长度 用 语句 减 1。
    完。
    若 做 字符串包含 用 语句、「 | 」：
        返还 做 龙壳_执行管道 用 语句、后台。
    完。
    返还 做 龙壳_执行单命令 用 语句、后台。
完。

龙壳_执行单命令 接受 命令行、后台：
    展开后 为 做 龙壳_变量展开 用 命令行。
    解析结果 为 做 解析命令行_完整 用 展开后。
    若 解析结果 等于 空 或者 做 计数 用 解析结果 的 参数 等于 0：
        返还 0。
    完。
    参数 为 解析结果 的 参数。
    重定向 为 解析结果 的 重定向。
    名称 为 参数 的 第 0 项。
    若 名称 等于 「exit」 或者 名称 等于 「quit」：
        若 做 计数 用 参数 大于 1：
            龙壳_上次退出码 为 做 字符串转整数 用 参数 的 第 1 项。
        完。
        龙壳_运行中 为 假。
        返还 龙壳_上次退出码。
    完。
    若 做 是内建命令 用 名称：
        返还 做 执行内建_完整 用 参数、重定向。
    完。
    pid 为 做 sys_fork 用 0、0、0、0、0、0。
    若 pid 小于 0：
        做 打印错误 用 「龙壳: fork失败」。
        返还 1。
    完。
    若 pid 等于 0：
    完。
完。

本章 收束。
