《龙启fork执行》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·服务管理》。

引用《..·中枢·线程退出》。

引用《..·中枢·退出调用》。

引用《..·中枢·线程clone语义》。

引用《..·记忆·超级块》。

引用《..·中枢·获取进程》。

引用《..·中枢·整数转字符串》。

引用《..·记忆·挂载执行》。

引用《..·记忆·挂载移动》。

引用《.·龙壳入口》。

引用《..·中枢·读取调用》。

引用《.·龙启主函数》。

龙启_fork_exec 接受 路径：
    pid 为 做 sys_fork 用 0、0、0、0、0、0。
    若 pid 等于 0：
        做 sys_setsid 用 0、0、0、0、0、0。
        argv 为 一组：路径、「--login」。
        envp 为 一组：「HOME=/root」、「PATH=/bin:/sbin:/usr/bin:/usr/sbin」、「终端=」 加 路径、「TERM=linux」、「USER=root」、「LANG=zh_CN.UTF-8」、「NOVAOS_VERSION=1.0.0-M1」。
        做 sys_execve 用 路径、argv、envp、0、0、0。
        做 sys_write 用 2、「龙启: exec失败: 」 加 路径 加 「\n」、0、0、0、0。
        做 sys_exit 用 1、0、0、0、0、0。
    完。
    做 服务_注册_运行中 用 「终端」、pid。
完。

龙启_fork_终端内置 接受 零参：
    pid 为 做 sys_fork 用 0、0、0、0、0、0。
    若 pid 等于 0：
        做 sys_setsid 用 0、0、0、0、0、0。
        做 龙壳_入口 用 0、一组：（空）。
        做 sys_exit 用 0、0、0、0、0、0。
    完。
    做 服务_注册_运行中 用 「终端」、pid。
完。

龙启_主循环 接受 零参：
    只要 init_运行中：
        只要 真：
            状态 为 一组：0。
            pid 为 做 sys_wait4 用 负 1、状态、WNOHANG、空、0、0。
            若 pid 小于或等于 0：
                中断。
            完。
            退出码 为 状态 的 第 0 项 右移 8 位与 0xFF。
            服务名 为 做 服务_按PID查找 用 pid。
            若 服务名 不等于 空：
                做 龙启_打印 用 「[龙启] 服务退出: 」 加 服务名 加 「 (PID 」 加 做 整数转字符串 用 pid 加 「 退出码=」 加 做 整数转字符串 用 退出码 加 「)」。
                做 服务_标记退出 用 服务名、退出码。
                若 服务名 等于 「终端」：
                    做 龙启_打印 用 「[龙启] 终端已退出, 5秒后重启...」。
                    做 sys_nanosleep 用 一组：5、0、空、0、0、0、0。
                    做 龙启_启动终端。
                完。
                若 做 服务_是关键 用 服务名 并且 做 服务_重启次数 用 服务名 小于 3：
                    做 龙启_打印 用 「[龙启] 关键服务崩溃, 重启: 」 加 服务名。
                    做 服务_启动 用 服务名。
                    做 服务_增加重启次数 用 服务名。
                完。
            完。
        完。
        若 init_运行级 等于 运行级_重启：
            做 龙启_打印 用 「[龙启] 收到重启请求...」。
            做 龙启_关机序列。
            返还。
        完。
        若 init_运行级 等于 运行级_关机：
            做 龙启_打印 用 「[龙启] 收到关机请求...」。
            做 龙启_关机序列。
            返还。
        完。
        做 sys_nanosleep 用 一组：1、0、空、0、0、0、0。
    完。
完。

龙启_关机序列 接受 零参：
    做 龙启_打印 用 「[龙启] ═══ 关机序列开始 ═══」。
    做 龙启_打印 用 「[龙启] 发送SIGTERM给所有服务...」。
    做 服务_停止全部。
    做 sys_nanosleep 用 一组：3、0、空、0、0、0、0。
    做 龙启_打印 用 「[龙启] 发送SIGKILL给剩余进程...」。
    做 sys_kill 用 负 1、9、0、0、0、0。
    做 龙启_打印 用 「[龙启] 同步文件系统...」。
    做 同步所有文件系统。
    做 龙启_打印 用 「[龙启] 卸载文件系统...」。
    做 do_umount 用 「/tmp」、0。
    做 do_umount 用 「/run」、0。
    做 do_umount 用 「/dev/shm」、0。
    做 do_umount 用 「/dev/pts」、0。
    做 do_umount 用 「/dev」、0。
    做 do_umount 用 「/sys」、0。
    做 do_umount 用 「/proc」、0。
    做 do_mount_带标志 用 「」、「/」、「」、MS_REMOUNT 位或 MS_RDONLY、「」。
    做 龙启_打印 用 「[龙启] ═══ 关机序列完成 ═══」。
    init_运行中 为 假。
完。

龙启_打印 接受 消息：
    做 sys_write 用 1、「[龙启] 」 加 消息 加 「\n」、0、0、0、0。
完。

龙启_执行脚本 接受 路径：
    pid 为 做 sys_fork 用 0、0、0、0、0、0。
    若 pid 等于 0：
        做 sys_execve 用 「/bin/龙壳」、一组：「/bin/龙壳」、路径、空、0、0、0。
        做 sys_exit 用 1、0、0、0、0、0。
    完。
    状态 为 一组：0。
    做 sys_wait4 用 pid、状态、0、空、0、0。
完。

WNOHANG 为 0x00000001。

MS_REMOUNT 为 0x0020。

MS_RDONLY 为 0x0001。

可变 AI_龙启分叉执_事件频率 为 0。

可变 AI_龙启分叉执_节流阈值 为 16。

AI_龙启分叉执_节流判断 接受 事件类型、间隔毫秒：
    AI_龙启分叉执_事件频率 为 AI_龙启分叉执_事件频率 加 1。
    若 事件类型 等于 「滚动」 并且 间隔毫秒 小于 AI_龙启分叉执_节流阈值：
        返还 真。
    完。
    若 事件类型 等于 「移动」 并且 间隔毫秒 小于 AI_龙启分叉执_节流阈值：
        返还 真。
    完。
    返还 假。
完。

AI_龙启分叉执_优先级排序 接受 事件类型：
    若 事件类型 等于 「点击」：
        返还 100。
    完。
    若 事件类型 等于 「输入」：
        返还 90。
    完。
    若 事件类型 等于 「滚动」：
        返还 30。
    完。
    返还 50。
完。

本章 收束。
