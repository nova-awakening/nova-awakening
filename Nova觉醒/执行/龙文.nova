《龙文》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·标准·文件》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·内核打印》。

引用《..·标准·原生字符串》。

引用《..·中枢·整数转字符串》。

文档条目 是 一种 事物，含 类型、名称、签名、注释、文件、行号。

龙文_main 接受 参数：
    若 做 计数 用 参数 等于 0：
        做 打印行 用 「龙文 v0.1 — Nova文档生成器」。
        做 打印行 用 「用法: 龙文 [源文件...] [-o 输出目录]」。
        做 打印行 用 「  龙文 src/*.nova -o docs/」。
        返还 0。
    完。
    可变 输出目录 为 「./docs」。
    可变 文件列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        若 参数 的 第 i 项 等于 「-o」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            输出目录 为 参数 的 第 i 项。
        否则：
            做 追加 用 文件列表、参数 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    做 打印行 用 「[龙文] 扫描 」 加 做 整数转字符串 用 做 计数 用 文件列表 加 「 个文件...」。
    做 目录_创建 用 输出目录。
    可变 所有条目 为 一组：（空）。
    可变 总函数 为 0。
    可变 总结构 为 0。
    可变 k 为 0。
    只要 k 小于 做 计数 用 文件列表：
        内容 为 做 文件_读全部 用 文件列表 的 第 k 项。
        若 内容 不等于 空：
            条目列表 为 做 龙文_提取条目 用 内容、文件列表 的 第 k 项。
            可变 j 为 0。
            只要 j 小于 做 计数 用 条目列表：
                做 追加 用 所有条目、条目列表 的 第 j 项。
                j 为 j 加 1。
            完。
            函数数 为 做 龙文_计数类型 用 条目列表、「函数」。
            结构数 为 做 龙文_计数类型 用 条目列表、「结构」。
            总函数 为 总函数 加 函数数。
            总结构 为 总结构 加 结构数。
            做 打印行 用 「  」 加 文件列表 的 第 k 项 加 「: 」 加 做 整数转字符串 用 函数数 加 「函数 」 加 做 整数转字符串 用 结构数 加 「结构」。
        完。
        k 为 k 加 1。
    完。
    md 为 做 龙文_生成Markdown 用 所有条目、文件列表。
    输出文件 为 输出目录 加 「/接口文档.md」。
    做 文件_写全部 用 输出文件、md。
    做 打印行 用 「」。
    做 打印行 用 「[龙文] ✓ 文档生成完成」。
    做 打印行 用 「  函数: 」 加 做 整数转字符串 用 总函数 加 「 | 结构: 」 加 做 整数转字符串 用 总结构。
    做 打印行 用 「  输出: 」 加 输出文件。
    返还 0。
完。

龙文_提取条目 接受 源码、文件名：
    行列表 为 做 字符串分割 用 源码、「\n」。
    可变 条目列表 为 一组：（空）。
    可变 上方注释 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去空白 用 行列表 的 第 i 项。
        若 做 字符串开始于 用 行、「//」：
            若 做 字符串长度 用 上方注释 大于 0：
                上方注释 为 上方注释 加 「\n」。
            完。
            上方注释 为 上方注释 加 做 字符串子串 用 行、2、做 字符串长度 用 行。
        否则：
            若 做 字符串包含 用 行、「函数 」：
                条目 为 新建 文档条目。
                条目 的 类型 为 「函数」。
                条目 的 签名 为 行。
                条目 的 注释 为 上方注释。
                条目 的 文件 为 文件名。
                条目 的 行号 为 i 加 1。
                名起 为 做 字符串查找 用 行、「函数 」 加 3。
                名止 为 做 字符串查找 用 做 字符串子串 用 行、名起、做 字符串长度 用 行、「(」。
                若 名止 大于 0：
                    条目 的 名称 为 做 字符串子串 用 行、名起、名起 加 名止。
                否则：
                    条目 的 名称 为 做 字符串子串 用 行、名起、做 字符串长度 用 行。
                完。
                做 追加 用 条目列表、条目。
            完。
            若 做 字符串开始于 用 行、「结构 」：
                条目 为 新建 文档条目。
                条目 的 类型 为 「结构」。
                条目 的 签名 为 行。
                条目 的 注释 为 上方注释。
                条目 的 文件 为 文件名。
                条目 的 行号 为 i 加 1。
                条目 的 名称 为 做 字符串子串 用 行、3、若 做 字符串查找 用 行、「 {」 大于 0 则 做 字符串查找 用 行、「 {」 否则 做 字符串长度 用 行。
                做 追加 用 条目列表、条目。
            完。
            上方注释 为 「」。
        完。
        i 为 i 加 1。
    完。
    返还 条目列表。
完。

龙文_生成Markdown 接受 条目列表、文件列表：
    可变 md 为 「# NovaOS 接口 文档\n\n」。
    md 为 md 加 「> 由龙文(DragonDoc)自动生成\n\n」。
    md 为 md 加 「## 文件列表\n\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文件列表：
        md 为 md 加 「- `」 加 文件列表 的 第 i 项 加 「`\n」。
        i 为 i 加 1。
    完。
    md 为 md 加 「\n---\n\n」。
    md 为 md 加 「## 结构体\n\n」。
    i 为 0。
    只要 i 小于 做 计数 用 条目列表：
        若 条目列表 的 第 i 项 的 类型 等于 「结构」：
            md 为 md 加 「### `」 加 条目列表 的 第 i 项 的 名称 加 「`\n\n」。
            若 做 字符串长度 用 条目列表 的 第 i 项 的 注释 大于 0：
                md 为 md 加 条目列表 的 第 i 项 的 注释 加 「\n\n」。
            完。
            md 为 md 加 「- 文件: `」 加 条目列表 的 第 i 项 的 文件 加 「:」 加 做 整数转字符串 用 条目列表 的 第 i 项 的 行号 加 「`\n\n」。
        完。
        i 为 i 加 1。
    完。
    md 为 md 加 「## 函数\n\n」。
    i 为 0。
    只要 i 小于 做 计数 用 条目列表：
        若 条目列表 的 第 i 项 的 类型 等于 「函数」：
            md 为 md 加 「### `」 加 条目列表 的 第 i 项 的 名称 加 「`\n\n」。
            md 为 md 加 「```nova\n」 加 条目列表 的 第 i 项 的 签名 加 「\n```\n\n」。
            若 做 字符串长度 用 条目列表 的 第 i 项 的 注释 大于 0：
                md 为 md 加 条目列表 的 第 i 项 的 注释 加 「\n\n」。
            完。
            md 为 md 加 「- 文件: `」 加 条目列表 的 第 i 项 的 文件 加 「:」 加 做 整数转字符串 用 条目列表 的 第 i 项 的 行号 加 「`\n\n」。
        完。
        i 为 i 加 1。
    完。
    返还 md。
完。

龙文_计数类型 接受 条目列表、类型：
    可变 计 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目列表：
        若 条目列表 的 第 i 项 的 类型 等于 类型：
            计 为 计 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计。
完。

可变 AI_龙文_调用数 为 0。

可变 AI_龙文_异常数 为 0。

AI_龙文_监控 接受 零参：
    AI_龙文_调用数 为 AI_龙文_调用数 加 1。
    返还 AI_龙文_异常数 等于 0。
完。

AI_龙文_健康检查 接受 零参：
    若 AI_龙文_调用数 大于 0 并且 AI_龙文_异常数 乘 100 除以 AI_龙文_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
