《脚本引擎》

引用《..·中枢·类型定义》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

引用《..·标准·原生字符串》。

引用《..·中枢·获取进程》。

引用《..·推理·模块元信息》。

引用《.·内存管理封装》。

可变 脚本_最后退出码 为 0。

可变 脚本_参数 为 一组：（空）。

可变 脚本_当前行号 为 0。

可变 脚本_当前文件 为 「」。

脚本_执行 接受 文件路径：
    内容 为 做 sys_read_file 用 文件路径。
    若 内容 等于 空：
        做 打印行 用 「龙壳: 无法读取脚本: 」 加 文件路径。
        返还 错误_不存在。
    完。
    脚本_当前文件 为 文件路径。
    脚本_当前行号 为 0。
    行列表 为 做 字符串_分割 用 内容、「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        脚本_当前行号 为 i 加 1。
        行 为 做 字符串_去首尾空白 用 行列表 的 第 i 项。
        若 做 字符串长度 用 行 等于 0 或者 做 字符串取字符 用 行、0 等于 「#」：
            i 为 i 加 1。
            继续。
        完。
        展开行 为 做 脚本_解析变量 用 行。
        若 做 字符串包含 用 展开行、「 && 」：
            部分 为 做 字符串_分割 用 展开行、「 && 」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 部分：
                脚本_最后退出码 为 做 执行_终端命令 用 做 字符串_去首尾空白 用 部分 的 第 j 项。
                若 脚本_最后退出码 不等于 0：
                    中断。
                完。
                j 为 j 加 1。
            完。
        否则若 做 字符串包含 用 展开行、「 || 」：
            部分 为 做 字符串_分割 用 展开行、「 || 」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 部分：
                脚本_最后退出码 为 做 执行_终端命令 用 做 字符串_去首尾空白 用 部分 的 第 j 项。
                若 脚本_最后退出码 等于 0：
                    中断。
                完。
                j 为 j 加 1。
            完。
        否则：
            脚本_最后退出码 为 做 执行_终端命令 用 展开行。
        完。
        i 为 i 加 1。
    完。
    返还 脚本_最后退出码。
完。

脚本_执行_带参数 接受 文件路径、参数：
    脚本_参数 为 参数。
    返还 做 脚本_执行 用 文件路径。
完。

脚本_解析变量 接受 行：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 行：
        c 为 做 字符串取字符 用 行、i。
        若 c 等于 「$」 并且 i 加 1 小于 做 字符串长度 用 行：
            下一个 为 做 字符串取字符 用 行、i 加 1。
            若 下一个 等于 「?」：
                结果 为 结果 加 做 整数转字符串 用 脚本_最后退出码。
                i 为 i 加 2。
                继续。
            完。
            若 下一个 等于 「$」：
                结果 为 结果 加 做 整数转字符串 用 做 sys_getpid。
                i 为 i 加 2。
                继续。
            完。
            若 下一个 等于 「#」：
                结果 为 结果 加 做 整数转字符串 用 做 计数 用 脚本_参数。
                i 为 i 加 2。
                继续。
            完。
            若 下一个 大于或等于 「0」 并且 下一个 小于或等于 「9」：
                索引 为 做 编码 用 下一个 减 48。
                若 索引 小于 做 计数 用 脚本_参数：
                    结果 为 结果 加 脚本_参数 的 第 索引 项。
                完。
                i 为 i 加 2。
                继续。
            完。
            若 下一个 等于 「{」：
                可变 名称 为 「」。
                i 为 i 加 2。
                只要 i 小于 做 字符串长度 用 行 并且 做 字符串取字符 用 行、i 不等于 「}」：
                    名称 为 名称 加 做 字符串取字符 用 行、i。
                    i 为 i 加 1。
                完。
                若 i 小于 做 字符串长度 用 行：
                    i 为 i 加 1。
                完。
                若 做 字符串包含 用 名称、「:-」：
                    部分 为 做 字符串_分割 用 名称、「:-」。
                    值 为 做 环境变量_获取 用 部分 的 第 0 项。
                    若 值 等于 「」 并且 做 计数 用 部分 大于 1：
                        值 为 部分 的 第 1 项。
                    完。
                    结果 为 结果 加 值。
                否则：
                    结果 为 结果 加 做 环境变量_获取 用 名称。
                完。
                继续。
            完。
            可变 名称 为 「」。
            i 为 i 加 1。
            只要 i 小于 做 字符串长度 用 行：
                ch 为 做 字符串取字符 用 行、i。
                若 ch 大于或等于 「a」 并且 ch 小于或等于 「z」 或者 ch 大于或等于 「A」 并且 ch 小于或等于 「Z」 或者 ch 大于或等于 「0」 并且 ch 小于或等于 「9」 或者 ch 等于 「_」：
                    名称 为 名称 加 ch。
                    i 为 i 加 1。
                否则：
                    中断。
                完。
            完。
            结果 为 结果 加 做 环境变量_获取 用 名称。
            继续。
        完。
        结果 为 结果 加 c。
        i 为 i 加 1。
    完。
    返还 结果。
完。

脚本_是龙壳脚本 接受 文件路径：
    内容 为 做 sys_read_file 用 文件路径。
    若 内容 等于 空：
        返还 假。
    完。
    返还 做 字符串_开头是 用 内容、「#!/bin/龙壳」 或者 做 字符串_开头是 用 内容、「#!/usr/bin/env 龙壳」。
完。

执行_终端命令 接受 命令行：
    若 命令行 等于 0：
        返还 负 1。
    完。
    做 打印 用 「[终端] 执行: 」 加 命令行。
    参数 为 一组：命令行、0。
    返还 做 系统调用_execve 用 「/bin/sh」、参数、0。
完。

本章 收束。
