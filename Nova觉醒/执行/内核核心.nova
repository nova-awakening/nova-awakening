《内核核心》

引用《..·感知·文档节点》。

引用《..·感知·样式解析》。

引用《..·感知·样式计算》。

引用《..·感知·文档事件》。

引用《..·执行·内核适配》。

引用《.·样式引擎》。

引用《.·表达式求值》。

引用《..·感知·事件循环主体》。

引用《.·标签处理》。

引用《.·布局计算》。

引用《.·脚本初始化》。

引用《.·用户输入处》。

引用《.·脚本值构造》。

引用《.·绘制命令》。

引用《.·节点操作》。

引用《.·定时器》。

引用《..·中枢·整数转字符串》。

引用《.·默认样式》。

引用《.·文档初始化》。

引用《.·JS执行入口》。

引用《.·浏览器事件循环》。

引用《.·渲染初始化》。

核心档 是 一种 事物，含 名称、当前地址、当前文档、当前样式档、当前适配档、当前渲染树、当前根盒、最近节点、最近命中、最近脚本、最近表达式、最近导航、最近输入、最近摘要、视口宽、视口高、初始化完成、文档数、样式数、渲染数、事件数、脚本数、适配数、命中数、失败数、健康。

核心请求 是 一种 事物，含 地址、页面源码、样式源码、脚本源码、入口节点、输入类型、输入数据。

核心摘要 是 一种 事物，含 名称、地址、文档数、样式数、渲染数、事件数、脚本数、适配数、命中数、失败数、健康。

可变 核心账本 为 一组：（空）。

可变 核心建数 为 0。

可变 核心总执行 为 0。

可变 核心总命中 为 0。

可变 核心总失败 为 0。

内核核心_建档 接受 名称、视口宽、视口高：
    档 为 做 堆_分配 用 核心档。
    档 的 名称 为 名称。
    档 的 当前地址 为 「about:blank」。
    档 的 当前文档 为 0。
    档 的 当前样式档 为 0。
    档 的 当前适配档 为 0。
    档 的 当前渲染树 为 0。
    档 的 当前根盒 为 0。
    档 的 最近节点 为 0。
    档 的 最近命中 为 0。
    档 的 最近脚本 为 「」。
    档 的 最近表达式 为 0。
    档 的 最近导航 为 「」。
    档 的 最近输入 为 0。
    档 的 最近摘要 为 0。
    档 的 视口宽 为 视口宽。
    档 的 视口高 为 视口高。
    档 的 初始化完成 为 假。
    档 的 文档数 为 0。
    档 的 样式数 为 0。
    档 的 渲染数 为 0。
    档 的 事件数 为 0。
    档 的 脚本数 为 0。
    档 的 适配数 为 0。
    档 的 命中数 为 0。
    档 的 失败数 为 0。
    档 的 健康 为 「未初始化」。
    返还 档。
完。

内核核心_登记 接受 档：
    做 追加 用 核心账本、档。
    核心建数 为 核心建数 加 1。
    返还 档。
完。

内核核心_创建 接受 名称、视口宽、视口高：
    档 为 做 内核核心_建档 用 名称、视口宽、视口高。
    做 内核核心_初始化 用 档。
    返还 做 内核核心_登记 用 档。
完。

内核核心_初始化 接受 档：
    做 DOM_初始化。
    做 CSS_初始化。
    做 CSS引擎_初始化。
    做 NCSC_初始化。
    做 渲染_初始化 用 档 的 视口宽、档 的 视口高。
    做 事件循环_初始化。
    做 表达式_初始化。
    做 JS_初始化。
    做 JS_注入全局接口。
    档 的 当前样式档 为 做 样式解析_创建 用 「样式解析」、「内核核心」。
    档 的 当前适配档 为 做 内核适配_创建 用 「内核适配」、档 的 视口宽、档 的 视口高。
    档 的 样式数 为 档 的 样式数 加 1。
    档 的 适配数 为 档 的 适配数 加 1。
    档 的 初始化完成 为 真。
    档 的 健康 为 「运行」。
    核心总执行 为 核心总执行 加 1。
    返还 档。
完。

内核核心_建请求 接受 地址、页面源码、样式源码、脚本源码：
    请求 为 做 堆_分配 用 核心请求。
    请求 的 地址 为 地址。
    请求 的 页面源码 为 页面源码。
    请求 的 样式源码 为 样式源码。
    请求 的 脚本源码 为 脚本源码。
    请求 的 入口节点 为 0。
    请求 的 输入类型 为 「」。
    请求 的 输入数据 为 0。
    返还 请求。
完。

内核核心_创建文档 接受 档、地址：
    文档 为 做 DOM_创建文档 用 地址。
    档 的 当前地址 为 地址。
    档 的 当前文档 为 文档。
    档 的 文档数 为 档 的 文档数 加 1。
    核心总执行 为 核心总执行 加 1。
    返还 文档。
完。

内核核心_解析页面 接受 档、源码、地址：
    文档 为 做 DOM_解析HTML 用 源码、地址。
    档 的 当前地址 为 地址。
    档 的 当前文档 为 文档。
    档 的 文档数 为 档 的 文档数 加 1。
    档 的 最近节点 为 文档。
    返还 文档。
完。

内核核心_创建节点 接受 档、文档、类型、标签名：
    标签ID 为 做 DOM_标签名到ID 用 标签名。
    节点 为 做 DOM_创建节点 用 文档、类型、标签名、标签ID。
    档 的 最近节点 为 节点。
    档 的 文档数 为 档 的 文档数 加 1。
    返还 节点。
完。

内核核心_追加节点 接受 档、文档、父ID、子节点：
    做 DOM_添加子节点 用 文档、父ID、子节点。
    档 的 最近节点 为 子节点。
    档 的 文档数 为 档 的 文档数 加 1。
完。

内核核心_设属性 接受 档、文档、节点ID、键、值：
    做 DOM_设置属性 用 文档、节点ID、键、值。
    档 的 最近节点 为 节点ID。
    档 的 文档数 为 档 的 文档数 加 1。
完。

内核核心_取属性 接受 档、文档、节点ID、键：
    值 为 做 DOM_获取属性 用 文档、节点ID、键。
    档 的 最近节点 为 节点ID。
    若 值 不等于 空 并且 值 不等于 0：
        档 的 命中数 为 档 的 命中数 加 1。
        核心总命中 为 核心总命中 加 1。
    完。
    返还 值。
完。

内核核心_查节点 接受 档、文档、节点ID：
    节点 为 做 DOM_查找节点 用 文档、节点ID。
    档 的 最近节点 为 节点。
    若 节点 不等于 空 并且 节点 不等于 0：
        档 的 命中数 为 档 的 命中数 加 1。
        核心总命中 为 核心总命中 加 1。
    完。
    返还 节点。
完。

内核核心_查ID 接受 档、文档、标识：
    节点 为 做 DOM_通过ID查找 用 文档、标识。
    档 的 最近节点 为 节点。
    若 节点 不等于 空 并且 节点 不等于 0：
        档 的 命中数 为 档 的 命中数 加 1。
        核心总命中 为 核心总命中 加 1。
    完。
    返还 节点。
完。

内核核心_查标签 接受 档、文档、标签名：
    节点 为 做 DOM_通过标签查找 用 文档、标签名。
    档 的 最近节点 为 节点。
    返还 节点。
完。

内核核心_查类 接受 档、文档、类名：
    节点 为 做 DOM_通过类名查找 用 文档、类名。
    档 的 最近节点 为 节点。
    返还 节点。
完。

内核核心_文档统计 接受 档：
    统计 为 做 DOM_统计。
    档 的 最近摘要 为 统计。
    返还 统计。
完。

内核核心_加载默认样式 接受 档：
    默认样式 为 做 CSS_创建默认样式。
    做 样式解析_加载文本 用 档 的 当前样式档、「默认样式」、默认样式。
    做 CSS_解析样式表 用 默认样式、「默认」。
    做 CSS_添加样式表源码 用 默认样式、「默认」。
    档 的 样式数 为 档 的 样式数 加 1。
    返还 默认样式。
完。

内核核心_加载样式 接受 档、样式源码、来源：
    做 样式解析_加载文本 用 档 的 当前样式档、来源、样式源码。
    做 CSS_解析样式表 用 样式源码、来源。
    做 CSS_添加样式表源码 用 样式源码、来源。
    档 的 样式数 为 档 的 样式数 加 1。
    档 的 最近摘要 为 做 样式解析_摘要 用 档 的 当前样式档。
    返还 档 的 最近摘要。
完。

内核核心_全树样式 接受 档：
    做 CSS_全树重算 用 档 的 当前文档。
    做 样式解析_自检 用 档 的 当前样式档。
    档 的 样式数 为 档 的 样式数 加 1。
完。

内核核心_节点样式 接受 档、节点：
    结果 为 做 NCSC_增强计算节点样式 用 档 的 当前文档、节点。
    档 的 样式数 为 档 的 样式数 加 1。
    档 的 最近节点 为 节点。
    档 的 最近摘要 为 结果。
    返还 结果。
完。

内核核心_重算子树 接受 档、节点、父样式：
    做 CSS_重算子树 用 节点、父样式。
    档 的 样式数 为 档 的 样式数 加 1。
完。

内核核心_样式诊断 接受 档：
    诊断 为 做 CSS_诊断报告。
    档 的 最近摘要 为 诊断。
    返还 诊断。
完。

内核核心_初始化渲染 接受 档：
    做 渲染_初始化 用 档 的 视口宽、档 的 视口高。
    档 的 渲染数 为 档 的 渲染数 加 1。
完。

内核核心_构建渲染树 接受 档：
    树 为 做 渲染_构建树 用 档 的 当前文档。
    档 的 当前渲染树 为 树。
    档 的 渲染数 为 档 的 渲染数 加 1。
    返还 树。
完。

内核核心_布局 接受 档：
    根 为 做 渲染_布局。
    档 的 当前根盒 为 根。
    档 的 渲染数 为 档 的 渲染数 加 1。
    返还 根。
完。

内核核心_布局节点 接受 档、盒：
    结果 为 做 渲染_布局盒 用 盒。
    档 的 当前根盒 为 结果。
    档 的 渲染数 为 档 的 渲染数 加 1。
    返还 结果。
完。

内核核心_布局块 接受 档、盒：
    结果 为 做 渲染_布局块 用 盒。
    档 的 渲染数 为 档 的 渲染数 加 1。
    返还 结果。
完。

内核核心_布局弹性 接受 档、盒：
    结果 为 做 渲染_布局弹性 用 盒。
    档 的 渲染数 为 档 的 渲染数 加 1。
    返还 结果。
完。

内核核心_布局网格 接受 档、盒：
    结果 为 做 渲染_布局网格 用 盒。
    档 的 渲染数 为 档 的 渲染数 加 1。
    返还 结果。
完。

内核核心_绘制 接受 档：
    结果 为 做 渲染_绘制 用 档 的 当前文档。
    档 的 渲染数 为 档 的 渲染数 加 1。
    档 的 最近摘要 为 结果。
    返还 结果。
完。

内核核心_绘制盒 接受 档、盒、层ID：
    做 渲染_绘制盒 用 档 的 当前文档、盒、层ID。
    档 的 渲染数 为 档 的 渲染数 加 1。
完。

内核核心_滚动 接受 档、增量X、增量Y：
    做 渲染_滚动 用 增量X、增量Y。
    档 的 渲染数 为 档 的 渲染数 加 1。
完。

内核核心_命中测试 接受 档、x、y：
    节点 为 做 渲染_命中测试 用 x、y。
    档 的 最近命中 为 节点。
    若 节点 不等于 空 并且 节点 不等于 0：
        档 的 命中数 为 档 的 命中数 加 1。
        核心总命中 为 核心总命中 加 1。
    完。
    返还 节点。
完。

内核核心_渲染统计 接受 档：
    统计 为 做 渲染_统计。
    档 的 最近摘要 为 统计。
    返还 统计。
完。

内核核心_事件初始化 接受 档：
    做 事件循环_初始化。
    档 的 事件数 为 档 的 事件数 加 1。
完。

内核核心_提交任务 接受 档、类型、优先级、回调、数据：
    任务 为 做 事件循环_提交任务 用 类型、优先级、回调、数据。
    档 的 事件数 为 档 的 事件数 加 1。
    档 的 最近输入 为 任务。
    返还 任务。
完。

内核核心_提交微任务 接受 档、回调、数据：
    任务 为 做 事件循环_提交微任务 用 回调、数据。
    档 的 事件数 为 档 的 事件数 加 1。
    档 的 最近输入 为 任务。
    返还 任务。
完。

内核核心_超时 接受 档、回调、延迟毫秒：
    定时器 为 做 事件循环_setTimeout 用 回调、延迟毫秒。
    档 的 事件数 为 档 的 事件数 加 1。
    返还 定时器。
完。

内核核心_间隔 接受 档、回调、间隔毫秒：
    定时器 为 做 事件循环_setInterval 用 回调、间隔毫秒。
    档 的 事件数 为 档 的 事件数 加 1。
    返还 定时器。
完。

内核核心_清定时器 接受 档、定时器ID：
    做 事件循环_clearTimer 用 定时器ID。
    档 的 事件数 为 档 的 事件数 加 1。
完。

内核核心_动画帧 接受 档、回调：
    帧 为 做 事件循环_requestAnimationFrame 用 回调。
    档 的 事件数 为 档 的 事件数 加 1。
    返还 帧。
完。

内核核心_导航 接受 档、地址：
    做 事件循环_导航 用 地址。
    档 的 当前地址 为 地址。
    档 的 最近导航 为 地址。
    档 的 事件数 为 档 的 事件数 加 1。
完。

内核核心_处理输入 接受 档、任务：
    做 事件循环_处理用户输入 用 任务。
    档 的 事件数 为 档 的 事件数 加 1。
    档 的 最近输入 为 任务。
完。

内核核心_处理系统事件 接受 档、任务：
    做 事件循环_处理系统事件 用 任务。
    档 的 事件数 为 档 的 事件数 加 1。
完。

内核核心_处理AI回调 接受 档、任务：
    做 事件循环_处理AI回调 用 任务。
    档 的 事件数 为 档 的 事件数 加 1。
完。

内核核心_单次迭代 接受 档：
    做 事件循环_单次迭代。
    做 事件循环_检查定时器。
    档 的 事件数 为 档 的 事件数 加 1。
完。

内核核心_事件统计 接受 档：
    统计 为 做 事件循环_统计。
    档 的 最近摘要 为 统计。
    返还 统计。
完。

内核核心_脚本初始化 接受 档：
    做 JS_初始化。
    做 JS_注入全局接口。
    做 表达式_初始化。
    档 的 脚本数 为 档 的 脚本数 加 1。
完。

内核核心_执行脚本 接受 档、代码：
    结果 为 做 JS_执行脚本 用 代码。
    档 的 脚本数 为 档 的 脚本数 加 1。
    档 的 最近脚本 为 代码。
    档 的 最近摘要 为 结果。
    返还 结果。
完。

内核核心_执行日志 接受 档、语句：
    做 JS_执行console_log 用 语句。
    档 的 脚本数 为 档 的 脚本数 加 1。
    档 的 最近脚本 为 语句。
完。

内核核心_执行取节点 接受 档、语句：
    结果 为 做 JS_执行get元素ById 用 语句。
    档 的 脚本数 为 档 的 脚本数 加 1。
    档 的 最近脚本 为 语句。
    档 的 最近节点 为 结果。
    返还 结果。
完。

内核核心_设变量 接受 档、名称、值：
    做 JS_设置全局变量 用 名称、值。
    档 的 脚本数 为 档 的 脚本数 加 1。
完。

内核核心_取变量 接受 档、名称：
    值 为 做 JS_查找变量 用 名称。
    档 的 脚本数 为 档 的 脚本数 加 1。
    返还 值。
完。

内核核心_表达式数字 接受 档、值：
    结果 为 做 表达式_创建数字 用 值。
    档 的 最近表达式 为 结果。
    档 的 脚本数 为 档 的 脚本数 加 1。
    返还 结果。
完。

内核核心_表达式字串 接受 档、哈希：
    结果 为 做 表达式_创建字串 用 哈希。
    档 的 最近表达式 为 结果。
    档 的 脚本数 为 档 的 脚本数 加 1。
    返还 结果。
完。

内核核心_表达式布尔 接受 档、值：
    结果 为 做 表达式_创建布尔 用 值。
    档 的 最近表达式 为 结果。
    档 的 脚本数 为 档 的 脚本数 加 1。
    返还 结果。
完。

内核核心_表达式加法 接受 档、左、右：
    结果 为 做 表达式_加法 用 左、右。
    档 的 最近表达式 为 结果。
    档 的 脚本数 为 档 的 脚本数 加 1。
    返还 结果。
完。

内核核心_表达式比较 接受 档、左、右：
    结果 为 做 表达式_等于 用 左、右。
    档 的 最近表达式 为 结果。
    档 的 脚本数 为 档 的 脚本数 加 1。
    返还 结果。
完。

内核核心_表达式统计 接受 档：
    统计 为 做 表达式_完整统计。
    档 的 最近摘要 为 统计。
    返还 统计。
完。

内核核心_适配自检 接受 档：
    结果 为 做 内核适配_自检 用 档 的 当前适配档。
    档 的 适配数 为 档 的 适配数 加 1。
    档 的 最近摘要 为 结果。
    返还 结果。
完。

内核核心_页面管线 接受 档、地址、页面源码、样式源码、脚本源码：
    若 非 档 的 初始化完成：
        做 内核核心_初始化 用 档。
    完。
    做 内核核心_解析页面 用 档、页面源码、地址。
    做 内核核心_加载默认样式 用 档。
    若 样式源码 不等于 「」：
        做 内核核心_加载样式 用 档、样式源码、地址。
    完。
    做 内核核心_全树样式 用 档。
    做 内核核心_构建渲染树 用 档。
    做 内核核心_布局 用 档。
    做 内核核心_绘制 用 档。
    若 脚本源码 不等于 「」：
        做 内核核心_执行脚本 用 档、脚本源码。
    完。
    做 内核核心_导航 用 档、地址。
    档 的 最近摘要 为 做 内核核心_摘要 用 档。
    返还 档 的 最近摘要。
完。

内核核心_刷新页面 接受 档：
    做 内核核心_全树样式 用 档。
    做 内核核心_布局 用 档。
    做 内核核心_绘制 用 档。
    档 的 渲染数 为 档 的 渲染数 加 1。
完。

内核核心_统一输入 接受 档、类型、数据：
    任务 为 一组：类型、数据。
    若 类型 等于 「输入」：
        做 内核核心_处理输入 用 档、任务。
    否则若 类型 等于 「系统」：
        做 内核核心_处理系统事件 用 档、任务。
    否则：
        做 内核核心_处理AI回调 用 档、任务。
    完。
    档 的 最近输入 为 任务。
    返还 任务。
完。

内核核心_命中页面 接受 档、x、y：
    节点 为 做 内核核心_命中测试 用 档、x、y。
    若 节点 不等于 空 并且 节点 不等于 0：
        档 的 最近命中 为 节点。
    否则：
        档 的 失败数 为 档 的 失败数 加 1。
        核心总失败 为 核心总失败 加 1。
    完。
    返还 节点。
完。

内核核心_摘要 接受 档：
    返还 新建 核心摘要：名称 为 档 的 名称、地址 为 档 的 当前地址、文档数 为 档 的 文档数、样式数 为 档 的 样式数、渲染数 为 档 的 渲染数、事件数 为 档 的 事件数、脚本数 为 档 的 脚本数、适配数 为 档 的 适配数、命中数 为 档 的 命中数、失败数 为 档 的 失败数、健康 为 做 内核核心_健康 用 档。
完。

内核核心_状态串 接受 档：
    返还 做 字符串拼接 用 「核心[」、做 字符串拼接 用 档 的 名称、做 字符串拼接 用 「] 文档=」、做 字符串拼接 用 做 整数转字符串 用 档 的 文档数、做 字符串拼接 用 「 渲染=」、做 字符串拼接 用 做 整数转字符串 用 档 的 渲染数、做 字符串拼接 用 「 脚本=」、做 整数转字符串 用 档 的 脚本数。
完。

内核核心_健康 接受 档：
    若 非 档 的 初始化完成：
        返还 「待初始化」。
    完。
    若 档 的 失败数 大于 0：
        返还 「退化」。
    完。
    若 档 的 当前文档 等于 0：
        返还 「待载入」。
    完。
    返还 「运行」。
完。

内核核心_自检 接受 档：
    页面 为 「<html><body><div id='app'>觉醒</div></body></html>」。
    样式 为 「body { color: #fff; background: #111; } #app { width: 100vw; }」。
    脚本 为 「console.log('core');」。
    做 内核核心_页面管线 用 档、「nova://core」、页面、样式、脚本。
    做 内核核心_查ID 用 档、档 的 当前文档、「app」。
    做 内核核心_命中页面 用 档、12、12。
    做 内核核心_单次迭代 用 档。
    做 内核核心_适配自检 用 档。
    档 的 健康 为 做 内核核心_健康 用 档。
    返还 档 的 健康。
完。

内核核心_按名查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 核心账本：
        若 核心账本 的 第 i 项 的 名称 等于 名称：
            返还 核心账本 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

内核核心_最近摘要 接受 档：
    返还 档 的 最近摘要。
完。

内核核心_最近节点 接受 档：
    返还 档 的 最近节点。
完。

内核核心_最近命中 接受 档：
    返还 档 的 最近命中。
完。

内核核心_最近脚本 接受 档：
    返还 档 的 最近脚本。
完。

内核核心_账本数 接受 零参：
    返还 做 计数 用 核心账本。
完。

内核核心_全局统计 接受 零参：
    返还 一组：核心建数、核心总执行、核心总命中、核心总失败。
完。

内核核心_示例流程 接受 零参：
    档 为 做 内核核心_创建 用 「内核核心」、1366、768。
    页面 为 「<html><body><main id='root'><h1>觉醒</h1><p>系统即浏览器</p></main></body></html>」。
    样式 为 「main { width: 80vw; margin: 0 auto; } h1 { color: #fafafa; }」。
    脚本 为 「console.log('awake');」。
    做 内核核心_页面管线 用 档、「nova://awake」、页面、样式、脚本。
    做 内核核心_统一输入 用 档、「输入」、一组：「点击」、1。
    做 内核核心_滚动 用 档、0、120。
    做 内核核心_刷新页面 用 档。
    做 内核核心_自检 用 档。
    返还 做 内核核心_摘要 用 档。
完。

本章 收束。
