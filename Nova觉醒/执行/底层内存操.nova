《底层内存操》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

内存_复制 接受 目标、源、字节数：
    可变 i 为 0。
    只要 i 加 8 小于或等于 字节数：
        做 内存_写64 用 目标 加 i、做 内存_读64 用 源 加 i。
        i 为 i 加 8。
    完。
    只要 i 小于 字节数：
        做 内存_写8 用 目标 加 i、做 内存_读8 用 源 加 i。
        i 为 i 加 1。
    完。
完。

内存_清零 接受 目标、字节数：
    可变 i 为 0。
    只要 i 加 8 小于或等于 字节数：
        做 内存_写64 用 目标 加 i、0。
        i 为 i 加 8。
    完。
    只要 i 小于 字节数：
        做 内存_写8 用 目标 加 i、0。
        i 为 i 加 1。
    完。
完。

内存_填充 接受 目标、值、字节数：
    可变 i 为 0。
    只要 i 小于 字节数：
        做 内存_写8 用 目标 加 i、值。
        i 为 i 加 1。
    完。
完。

内存_比较 接受 a、b、字节数：
    可变 i 为 0。
    只要 i 小于 字节数：
        差 为 做 内存_读8 用 a 加 i 减 做 内存_读8 用 b 加 i。
        若 差 不等于 0：
            返还 差。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

内存_读8 接受 地址：
    返还 做 __内置_读内存8 用 地址。
完。

内存_写8 接受 地址、值：
    做 __内置_写内存8 用 地址、值。
完。

内存_读16 接受 地址：
    返还 做 __内置_读内存16 用 地址。
完。

内存_写16 接受 地址、值：
    做 __内置_写内存16 用 地址、值。
完。

内存_读32 接受 地址：
    返还 做 __内置_读内存32 用 地址。
完。

内存_写32 接受 地址、值：
    做 __内置_写内存32 用 地址、值。
完。

内存_读64 接受 地址：
    返还 做 __内置_读内存64 用 地址。
完。

内存_写64 接受 地址、值：
    做 __内置_写内存64 用 地址、值。
完。

底层_字符串_长度 接受 str：
    若 str 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 做 内存_读8 用 str 加 i 不等于 0：
        i 为 i 加 1。
    完。
    返还 i。
完。

字符串_复制 接受 str：
    若 str 等于 0：
        返还 0。
    完。
    长度 为 做 字符串_长度 用 str。
    新str 为 做 堆_分配 用 长度 加 1。
    做 内存_复制 用 新str、str、长度 加 1。
    返还 新str。
完。

字符串_复制_N 接受 str、长度：
    若 str 等于 0：
        返还 0。
    完。
    新str 为 做 堆_分配 用 长度 加 1。
    做 内存_复制 用 新str、str、长度。
    做 内存_写8 用 新str 加 长度、0。
    返还 新str。
完。

字符串_相等 接受 a、b：
    若 a 等于 b：
        返还 1。
    完。
    若 a 等于 0 或者 b 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 1：
        ca 为 做 内存_读8 用 a 加 i。
        cb 为 做 内存_读8 用 b 加 i。
        若 ca 不等于 cb：
            返还 0。
        完。
        若 ca 等于 0：
            返还 1。
        完。
        i 为 i 加 1。
    完。
完。

字符串_等于N 接受 str、长度、目标：
    若 str 等于 0 或者 目标 等于 0：
        返还 0。
    完。
    目标长度 为 做 字符串_长度 用 目标。
    若 长度 不等于 目标长度：
        返还 0。
    完。
    返还 做 内存_比较 用 str、目标、长度 等于 0。
完。

字符串_连接 接受 a、b：
    长a 为 做 字符串_长度 用 a。
    长b 为 做 字符串_长度 用 b。
    结果 为 做 堆_分配 用 长a 加 长b 加 1。
    做 内存_复制 用 结果、a、长a。
    做 内存_复制 用 结果 加 长a、b、长b 加 1。
    返还 结果。
完。

字符串_子串 接受 str、偏移、长度：
    若 str 等于 0：
        返还 0。
    完。
    返还 做 字符串_复制_N 用 str 加 偏移、长度。
完。

字符串_从缓冲 接受 缓冲、长度：
    返还 做 字符串_复制_N 用 缓冲、长度。
完。

字符串_哈希 接受 str：
    若 str 等于 0：
        返还 0。
    完。
    可变 哈希 为 2166136261。
    可变 i 为 0。
    只要 做 内存_读8 用 str 加 i 不等于 0：
        哈希 为 哈希 位异或 做 内存_读8 用 str 加 i。
        哈希 为 哈希 乘 16777619。
        i 为 i 加 1。
    完。
    返还 哈希。
完。

本章 收束。
