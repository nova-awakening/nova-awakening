《龙调》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·标准·标准流》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

引用《..·标准·原生时间》。

引用《..·标准·文件》。

引用《..·推理·终端操作》。

引用《..·中枢·内核打印》。

引用《..·标准·进程接口》。

断点 是 一种 事物，含 位置、启用、命中次数。

可变 调试_断点列表 为 一组：（空）。

可变 调试_运行中 为 假。

可变 调试_目标PID 为 0。

可变 调试_程序路径 为 「」。

可变 调试_当前行 为 0。

可变 调试_源码行 为 一组：（空）。

可变 调试_已暂停 为 假。

龙调_main 接受 参数：
    若 做 计数 用 参数 等于 0：
        做 打印行 用 「龙调 v0.1 — Nova调试器」。
        做 打印行 用 「用法: 龙调 <程序.nova> [参数...]」。
        做 打印行 用 「命令: run/break/step/next/continue/print/bt/list/info/watch/quit」。
        返还 0。
    完。
    调试_程序路径 为 参数 的 第 0 项。
    做 打印行 用 「[龙调] 加载程序: 」 加 调试_程序路径。
    源码 为 做 文件_读全部 用 调试_程序路径。
    若 源码 不等于 空：
        调试_源码行 为 做 字符串分割 用 源码、「\n」。
        做 打印行 用 「[龙调] 源码: 」 加 做 整数转字符串 用 做 计数 用 调试_源码行 加 「 行」。
    否则：
        做 打印行 用 「[龙调] 警告: 无法读取源文件」。
        调试_源码行 为 一组：（空）。
    完。
    调试_运行中 为 真。
    调试_当前行 为 1。
    调试_已暂停 为 假。
    只要 调试_运行中：
        做 标准输出 用 「(龙调) 」。
        输入 为 做 读取行。
        若 输入 等于 空 或者 做 字符串长度 用 输入 等于 0：
            继续。
        完。
        部分 为 做 字符串分割 用 输入、「 」。
        命令 为 部分 的 第 0 项。
        若 命令 等于 「quit」 或者 命令 等于 「q」：
            若 调试_目标PID 大于 0：
                做 进程_kill 用 调试_目标PID、9。
            完。
            调试_运行中 为 假。
        否则若 命令 等于 「run」 或者 命令 等于 「r」：
            做 龙调_运行 用 参数。
        否则若 命令 等于 「break」 或者 命令 等于 「b」：
            做 龙调_设断点 用 部分。
        否则若 命令 等于 「delete」 或者 命令 等于 「d」：
            做 龙调_删断点 用 部分。
        否则若 命令 等于 「step」 或者 命令 等于 「s」：
            做 龙调_单步。
        否则若 命令 等于 「next」 或者 命令 等于 「n」：
            做 龙调_下一行。
        否则若 命令 等于 「continue」 或者 命令 等于 「c」：
            做 龙调_继续。
        否则若 命令 等于 「bt」 或者 命令 等于 「backtrace」：
            做 龙调_栈回溯。
        否则若 命令 等于 「print」 或者 命令 等于 「p」：
            做 龙调_打印变量 用 部分。
        否则若 命令 等于 「list」 或者 命令 等于 「l」：
            做 龙调_显示源码 用 部分。
        否则若 命令 等于 「info」：
            做 龙调_信息 用 部分。
        否则若 命令 等于 「help」 或者 命令 等于 「h」：
            做 龙调_帮助。
        否则：
            做 打印行 用 「[龙调] 未知命令: 」 加 命令 加 「 (输入help查看帮助)」。
        完。
    完。
    做 打印行 用 「[龙调] 退出」。
    返还 0。
完。

龙调_运行 接受 参数：
    做 打印行 用 「[龙调] 启动程序: 」 加 调试_程序路径。
    调试_当前行 为 1。
    调试_已暂停 为 假。
    结果 为 做 智能体终端_执行 用 「龙运 」 加 调试_程序路径。
    若 结果 的 成功：
        做 打印行 用 结果 的 输出。
        做 打印行 用 「[龙调] 程序正常退出」。
    否则：
        做 打印行 用 「[龙调] 程序异常: 」 加 结果 的 错误。
    完。
完。

龙调_设断点 接受 部分：
    若 做 计数 用 部分 小于 2：
        做 打印行 用 「用法: break <行号或函数名>」。
        返还。
    完。
    bp 为 新建 断点。
    bp 的 位置 为 部分 的 第 1 项。
    bp 的 启用 为 1。
    bp 的 命中次数 为 0。
    做 追加 用 调试_断点列表、bp。
    做 打印行 用 「断点 #」 加 做 整数转字符串 用 做 计数 用 调试_断点列表 加 「 设置于 」 加 部分 的 第 1 项。
完。

龙调_删断点 接受 部分：
    若 做 计数 用 部分 小于 2：
        做 打印行 用 「用法: delete <断点号>」。
        返还。
    完。
    索引 为 做 字符串转整数 用 部分 的 第 1 项 减 1。
    若 索引 大于或等于 0 并且 索引 小于 做 计数 用 调试_断点列表：
        做 打印行 用 「删除断点 #」 加 部分 的 第 1 项 加 「 (」 加 调试_断点列表 的 第 索引 项 的 位置 加 「)」。
        做 列表_移除 用 调试_断点列表、索引。
    完。
完。

龙调_单步 接受 零参：
    调试_当前行 为 调试_当前行 加 1。
    做 龙调_显示当前行。
完。

龙调_下一行 接受 零参：
    调试_当前行 为 调试_当前行 加 1。
    做 龙调_显示当前行。
完。

龙调_继续 接受 零参：
    做 打印行 用 「[龙调] 继续执行...」。
    可变 安全 为 0。
    只要 调试_当前行 小于 做 计数 用 调试_源码行 并且 安全 小于 10000：
        调试_当前行 为 调试_当前行 加 1。
        可变 j 为 0。
        只要 j 小于 做 计数 用 调试_断点列表：
            若 调试_断点列表 的 第 j 项 的 位置 等于 做 整数转字符串 用 调试_当前行 并且 调试_断点列表 的 第 j 项 的 启用 等于 1：
                调试_断点列表 的 第 j 项 的 命中次数 为 调试_断点列表 的 第 j 项 的 命中次数 加 1。
                做 打印行 用 「[龙调] 命中断点 #」 加 做 整数转字符串 用 j 加 1 加 「 于行 」 加 做 整数转字符串 用 调试_当前行。
                做 龙调_显示当前行。
                返还。
            完。
            j 为 j 加 1。
        完。
        安全 为 安全 加 1。
    完。
    做 打印行 用 「[龙调] 程序结束」。
完。

龙调_显示当前行 接受 零参：
    若 调试_当前行 大于 0 并且 调试_当前行 小于或等于 做 计数 用 调试_源码行：
        做 打印行 用 做 整数转字符串 用 调试_当前行 加 「  → 」 加 调试_源码行 的 第 调试_当前行 减 1 项。
    完。
完。

龙调_显示源码 接受 部分：
    可变 起始 为 调试_当前行 减 5。
    若 起始 小于 1：
        起始 为 1。
    完。
    可变 结束 为 起始 加 10。
    若 结束 大于 做 计数 用 调试_源码行：
        结束 为 做 计数 用 调试_源码行。
    完。
    可变 i 为 起始。
    只要 i 小于或等于 结束：
        标记 为 「  」。
        若 i 等于 调试_当前行：
            标记 为 「→ 」。
        完。
        可变 有断点 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 调试_断点列表：
            若 调试_断点列表 的 第 j 项 的 位置 等于 做 整数转字符串 用 i：
                有断点 为 真。
            完。
            j 为 j 加 1。
        完。
        断点标记 为 「  」。
        若 有断点：
            断点标记 为 「● 」。
        完。
        做 打印行 用 断点标记 加 做 时间_补零 用 i 加 标记 加 调试_源码行 的 第 i 减 1 项。
        i 为 i 加 1。
    完。
完。

龙调_栈回溯 接受 零参：
    做 打印行 用 「[龙调] 栈回溯:」。
    做 打印行 用 「  #0  行」 加 做 整数转字符串 用 调试_当前行 加 「  」 加 调试_程序路径。
    若 调试_当前行 大于 0 并且 调试_当前行 小于或等于 做 计数 用 调试_源码行：
        做 打印行 用 「      」 加 调试_源码行 的 第 调试_当前行 减 1 项。
    完。
完。

龙调_打印变量 接受 部分：
    若 做 计数 用 部分 小于 2：
        做 打印行 用 「用法: print <变量名>」。
        返还。
    完。
    做 打印行 用 「  」 加 部分 的 第 1 项 加 「 = (需要解释器调试支持)」。
完。

龙调_信息 接受 部分：
    若 做 计数 用 部分 小于 2：
        做 打印行 用 「用法: info breakpoints/locals」。
        返还。
    完。
    若 部分 的 第 1 项 等于 「breakpoints」 或者 部分 的 第 1 项 等于 「b」：
        若 做 计数 用 调试_断点列表 等于 0：
            做 打印行 用 「  无断点」。
            返还。
        完。
        可变 i 为 0。
        只要 i 小于 做 计数 用 调试_断点列表：
            状态 为 「启用」。
            若 调试_断点列表 的 第 i 项 的 启用 等于 0：
                状态 为 「禁用」。
            完。
            做 打印行 用 「  #」 加 做 整数转字符串 用 i 加 1 加 「 」 加 调试_断点列表 的 第 i 项 的 位置 加 「 [」 加 状态 加 「] 命中」 加 做 整数转字符串 用 调试_断点列表 的 第 i 项 的 命中次数 加 「次」。
            i 为 i 加 1。
        完。
    完。
完。

龙调_帮助 接受 零参：
    做 打印行 用 「命令:」。
    做 打印行 用 「  run (r)       启动程序」。
    做 打印行 用 「  break N (b)   设置断点于行N」。
    做 打印行 用 「  delete N (d)  删除断点N」。
    做 打印行 用 「  step (s)      单步(进入函数)」。
    做 打印行 用 「  next (n)      下一行(跳过函数)」。
    做 打印行 用 「  continue (c)  继续到下一断点」。
    做 打印行 用 「  print X (p)   打印变量X」。
    做 打印行 用 「  list (l)      显示源码」。
    做 打印行 用 「  bt            栈回溯」。
    做 打印行 用 「  info b        列出断点」。
    做 打印行 用 「  quit (q)      退出」。
完。

本章 收束。
