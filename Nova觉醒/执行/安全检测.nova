《安全检测》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·外联·URL解析辅助》。

威胁_钓鱼 为 0。

威胁_恶意软件 为 1。

威胁_挖矿 为 2。

威胁_指纹追踪 为 3。

威胁_隐私泄露 为 4。

威胁_广告欺诈 为 5。

威胁_社工攻击 为 6。

安全扫描结果 是 一种 事物，含 威胁类型、威胁等级、描述、建议、阻止。

行为特征 是 一种 事物，含 CPU占用异常、大量DOM操作、频繁网络请求、访问敏感接口、隐藏iframe、密码框伪造。

可变 安检_总扫描数 为 0。

可变 安检_总拦截数 为 0。

可变 安检_威胁日志 为 一组：（空）。

可变 安检_已初始化 为 假。

可变 安检_广告域名列表 为 一组：（空）。

可变 安检_追踪器列表 为 一组：（空）。

安全检测_初始化 接受 零参：
    安检_威胁日志 为 一组：（空）。
    安检_广告域名列表 为 一组：「doubleclick.net」、「googlesyndication.com」、「adnxs.com」、「facebook.net/tr」、「google-analytics.com」、「hotjar.com」。
    安检_追踪器列表 为 一组：「fingerprint」、「canvas.toDataURL」、「navigator.plugins」、「图绘渲染ingContext」、「音频Context」。
    安检_已初始化 为 真。
    做 内核信息 用 「AI安全检测: 初始化完成, 钓鱼/挖矿/追踪全链路防护」。
完。

安全检测_扫描页面 接受 url、html内容：
    安检_总扫描数 为 安检_总扫描数 加 1。
    可变 威胁列表 为 一组：（空）。
    钓鱼结果 为 做 安全检测_钓鱼检测 用 url、html内容。
    若 钓鱼结果 的 威胁等级 大于 60：
        做 追加 用 威胁列表、钓鱼结果。
    完。
    挖矿结果 为 做 安全检测_挖矿检测 用 html内容。
    若 挖矿结果 的 威胁等级 大于 50：
        做 追加 用 威胁列表、挖矿结果。
    完。
    追踪结果 为 做 安全检测_追踪检测 用 html内容。
    若 追踪结果 的 威胁等级 大于 40：
        做 追加 用 威胁列表、追踪结果。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 威胁列表：
        做 追加 用 安检_威胁日志、一组：url、威胁列表 的 第 i 项。
        若 威胁列表 的 第 i 项 的 阻止：
            安检_总拦截数 为 安检_总拦截数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 威胁列表。
完。

安全检测_钓鱼检测 接受 url、html：
    结果 为 新建 安全扫描结果。
    结果 的 威胁类型 为 威胁_钓鱼。
    结果 的 威胁等级 为 0。
    结果 的 阻止 为 假。
    品牌词 为 一组：「google」、「facebook」、「apple」、「microsoft」、「amazon」、「paypal」、「bank」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 品牌词：
        若 做 字符串查找 用 url、品牌词 的 第 i 项 大于或等于 0：
            若 做 字符串查找 用 url、品牌词 的 第 i 项 加 「.com」 小于 0 并且 做 字符串查找 用 url、品牌词 的 第 i 项 加 「.cn」 小于 0：
                结果 的 威胁等级 为 结果 的 威胁等级 加 30。
            完。
        完。
        i 为 i 加 1。
    完。
    若 做 字符串查找 用 html、「type=\"password\"」 大于或等于 0 或者 做 字符串查找 用 html、「type='password'」 大于或等于 0：
        结果 的 威胁等级 为 结果 的 威胁等级 加 15。
    完。
    若 做 字符串查找 用 html、「urgent」 大于或等于 0 或者 做 字符串查找 用 html、「立即」 大于或等于 0 或者 做 字符串查找 用 html、「verify」 大于或等于 0 或者 做 字符串查找 用 html、「suspended」 大于或等于 0：
        结果 的 威胁等级 为 结果 的 威胁等级 加 10。
    完。
    若 做 字符串查找 用 html、「action=\"http」 大于或等于 0：
        结果 的 威胁等级 为 结果 的 威胁等级 加 20。
    完。
    若 结果 的 威胁等级 大于 100：
        结果 的 威胁等级 为 100。
    完。
    若 结果 的 威胁等级 大于或等于 70：
        结果 的 阻止 为 真。
        结果 的 描述 为 「疑似钓鱼网站」。
        结果 的 建议 为 「此页面具有钓鱼特征, 已阻止访问」。
    否则若 结果 的 威胁等级 大于或等于 40：
        结果 的 描述 为 「可疑页面」。
        结果 的 建议 为 「请注意验证此网站的真实性」。
    完。
    返还 结果。
完。

安全检测_挖矿检测 接受 html：
    结果 为 新建 安全扫描结果。
    结果 的 威胁类型 为 威胁_挖矿。
    结果 的 威胁等级 为 0。
    结果 的 阻止 为 假。
    挖矿特征 为 一组：「coinhive」、「cryptonight」、「CoinImp」、「minero」、「WebAssembly.instantiate」、「wasm-miner」、「deepMiner」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 挖矿特征：
        若 做 字符串查找 用 html、挖矿特征 的 第 i 项 大于或等于 0：
            结果 的 威胁等级 为 结果 的 威胁等级 加 40。
        完。
        i 为 i 加 1。
    完。
    若 做 字符串查找 用 html、「new 工线」 大于或等于 0 并且 做 字符串查找 用 html、「WebAssembly」 大于或等于 0：
        结果 的 威胁等级 为 结果 的 威胁等级 加 25。
    完。
    若 结果 的 威胁等级 大于 100：
        结果 的 威胁等级 为 100。
    完。
    若 结果 的 威胁等级 大于或等于 60：
        结果 的 阻止 为 真。
        结果 的 描述 为 「检测到挖矿脚本」。
        结果 的 建议 为 「已阻止挖矿脚本执行」。
    完。
    返还 结果。
完。

安全检测_追踪检测 接受 html：
    结果 为 新建 安全扫描结果。
    结果 的 威胁类型 为 威胁_指纹追踪。
    结果 的 威胁等级 为 0。
    结果 的 阻止 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 安检_追踪器列表：
        若 做 字符串查找 用 html、安检_追踪器列表 的 第 i 项 大于或等于 0：
            结果 的 威胁等级 为 结果 的 威胁等级 加 15。
        完。
        i 为 i 加 1。
    完。
    若 结果 的 威胁等级 大于 100：
        结果 的 威胁等级 为 100。
    完。
    若 结果 的 威胁等级 大于或等于 40：
        结果 的 描述 为 「检测到浏览器指纹追踪」。
        结果 的 建议 为 「此页面尝试收集设备指纹信息」。
    完。
    返还 结果。
完。

安全检测_是否拦截URL 接受 url：
    可变 i 为 0。
    只要 i 小于 做 计数 用 安检_广告域名列表：
        若 做 字符串查找 用 url、安检_广告域名列表 的 第 i 项 大于或等于 0：
            安检_总拦截数 为 安检_总拦截数 加 1。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

安全检测_统计 接受 零参：
    返还 一组：安检_总扫描数、安检_总拦截数、做 计数 用 安检_威胁日志。
完。

安全检测_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 做 计数 用 安检_威胁日志 大于 10000：
        做 追加 用 问题、「安检_威胁日志膨胀」。
    完。
    若 安检_已初始化 等于 假：
        做 追加 用 问题、「安检_已初始化未就绪」。
    完。
    若 做 计数 用 安检_广告域名列表 大于 10000：
        做 追加 用 问题、「安检_广告域名列表膨胀」。
    完。
    若 做 计数 用 安检_追踪器列表 大于 10000：
        做 追加 用 问题、「安检_追踪器列表膨胀」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

安全检测_综合报告 接受 零参：
    做 打印 用 「[安全检测] ═══ 综合报告 ═══」。
    做 打印 用 「  安检_总扫描数: 」 加 安检_总扫描数。
    做 打印 用 「  安检_总拦截数: 」 加 安检_总拦截数。
    做 打印 用 「  安检_已初始化: 」 加 安检_已初始化。
    做 打印 用 「  健康: 」 加 若 做 安全检测_健康评估 则 「正常」 否则 「异常」。
完。

安全检测_全局重置 接受 零参：
    安检_总扫描数 为 0。
    安检_总拦截数 为 0。
    安检_已初始化 为 0。
    做 打印 用 「[安全检测] 统计已重置」。
    返还 0。
完。

本章 收束。
