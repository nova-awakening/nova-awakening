《磁盘查》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·内核打印》。

引用《..·中枢·整数转字符串》。

引用《.·控制台输出》。

DF_颜色_正常 为 「\x1b[0m」。

DF_颜色_红 为 「\x1b[31m」。

DF_颜色_黄 为 「\x1b[33m」。

DF_颜色_绿 为 「\x1b[32m」。

DF_颜色_青 为 「\x1b[36m」。

df_main 接受 参数：
    可变 人类可读 为 假。
    可变 显示inode 为 假。
    可变 显示类型 为 假。
    可变 仅本地 为 假。
    可变 指定路径 为 0。
    可变 显示进度条 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        若 参数 的 第 i 项 等于 「-h」 或者 参数 的 第 i 项 等于 「--human-readable」：
            人类可读 为 真。
        否则若 参数 的 第 i 项 等于 「-i」 或者 参数 的 第 i 项 等于 「--inodes」：
            显示inode 为 真。
        否则若 参数 的 第 i 项 等于 「-T」 或者 参数 的 第 i 项 等于 「--type」：
            显示类型 为 真。
        否则若 参数 的 第 i 项 等于 「-l」 或者 参数 的 第 i 项 等于 「--local」：
            仅本地 为 真。
        否则若 参数 的 第 i 项 等于 「-b」 或者 参数 的 第 i 项 等于 「--bar」：
            显示进度条 为 真。
        否则若 参数 的 第 i 项 等于 「--help」：
            做 df_显示帮助。
            返还 0。
        否则若 做 字节取 用 参数 的 第 i 项、0 不等于 45：
            指定路径 为 参数 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 显示inode：
        做 打印行 用 「文件系统       Inodes  已用I  可用I 已用% 挂载点」。
    否则若 显示类型：
        做 打印行 用 「文件系统       类型    大小  已用  可用 已用% 挂载点」。
    否则：
        做 打印行 用 「文件系统       大小  已用  可用 已用% 挂载点」。
    完。
    挂载列表 为 做 获取挂载列表。
    若 挂载列表 等于 空：
        做 打印行 用 「df: 无法获取挂载信息」。
        返还 1。
    完。
    可变 k 为 0。
    只要 k 小于 做 计数 用 挂载列表：
        项 为 挂载列表 的 第 k 项。
        设备 为 项 的 第 0 项。
        挂载点 为 项 的 第 1 项。
        总量 为 项 的 第 2 项。
        已用 为 项 的 第 3 项。
        类型 为 项 的 第 4 项。
        总inode 为 项 的 第 5 项。
        已用inode 为 项 的 第 6 项。
        是本地 为 项 的 第 7 项。
        若 仅本地 并且 非 是本地：
            k 为 k 加 1。
            继续。
        完。
        若 指定路径 不等于 0：
            若 非 做 df_路径匹配 用 指定路径、挂载点：
                k 为 k 加 1。
                继续。
            完。
        完。
        若 显示inode：
            做 df_打印inode行 用 设备、挂载点、总inode、已用inode、人类可读。
        否则：
            可用 为 总量 减 已用。
            可变 百分比 为 0。
            若 总量 大于 0：
                百分比 为 已用 乘 100 除以 总量。
            完。
            可变 总文本 为 做 整数转字符串 用 总量。
            可变 用文本 为 做 整数转字符串 用 已用。
            可变 闲文本 为 做 整数转字符串 用 可用。
            若 人类可读：
                总文本 为 做 格式化大小 用 总量。
                用文本 为 做 格式化大小 用 已用。
                闲文本 为 做 格式化大小 用 可用。
            完。
            颜色 为 做 df_使用率颜色 用 百分比。
            百分比文本 为 做 字符串拼接 用 做 整数转字符串 用 百分比、「%」。
            可变 行 为 做 df_填充 用 设备、14。
            若 显示类型：
                行 为 做 字符串拼接 用 行、「 」。
                行 为 做 字符串拼接 用 行、做 df_填充 用 类型、7。
            完。
            行 为 做 字符串拼接 用 行、「 」。
            行 为 做 字符串拼接 用 行、做 df_右对齐 用 总文本、6。
            行 为 做 字符串拼接 用 行、「 」。
            行 为 做 字符串拼接 用 行、做 df_右对齐 用 用文本、6。
            行 为 做 字符串拼接 用 行、「 」。
            行 为 做 字符串拼接 用 行、做 df_右对齐 用 闲文本、6。
            行 为 做 字符串拼接 用 行、「 」。
            行 为 做 字符串拼接 用 行、颜色。
            行 为 做 字符串拼接 用 行、做 df_右对齐 用 百分比文本、4。
            行 为 做 字符串拼接 用 行、DF_颜色_正常。
            若 显示进度条：
                行 为 做 字符串拼接 用 行、「 」。
                行 为 做 字符串拼接 用 行、做 df_进度条 用 百分比、20。
            完。
            行 为 做 字符串拼接 用 行、「 」。
            行 为 做 字符串拼接 用 行、挂载点。
            做 打印行 用 行。
        完。
        k 为 k 加 1。
    完。
    返还 0。
完。

df_打印inode行 接受 设备、挂载点、总inode、已用inode、人类可读：
    可用inode 为 总inode 减 已用inode。
    可变 百分比 为 0。
    若 总inode 大于 0：
        百分比 为 已用inode 乘 100 除以 总inode。
    完。
    可变 总文本 为 做 整数转字符串 用 总inode。
    可变 用文本 为 做 整数转字符串 用 已用inode。
    可变 闲文本 为 做 整数转字符串 用 可用inode。
    若 人类可读：
        总文本 为 做 格式化数量 用 总inode。
        用文本 为 做 格式化数量 用 已用inode。
        闲文本 为 做 格式化数量 用 可用inode。
    完。
    颜色 为 做 df_使用率颜色 用 百分比。
    行 为 做 字符串拼接 用 做 df_填充 用 设备、14、做 字符串拼接 用 「 」、做 字符串拼接 用 做 df_右对齐 用 总文本、7、做 字符串拼接 用 「 」、做 字符串拼接 用 做 df_右对齐 用 用文本、7、做 字符串拼接 用 「 」、做 字符串拼接 用 做 df_右对齐 用 闲文本、7、做 字符串拼接 用 「 」、做 字符串拼接 用 颜色、做 字符串拼接 用 做 df_右对齐 用 做 字符串拼接 用 做 整数转字符串 用 百分比、「%」、4、做 字符串拼接 用 DF_颜色_正常、做 字符串拼接 用 「 」、挂载点。
    做 打印行 用 行。
完。

df_进度条 接受 百分比、宽度：
    填充数 为 百分比 乘 宽度 除以 100。
    可变 结果 为 「[」。
    可变 i 为 0。
    只要 i 小于 宽度：
        若 i 小于 填充数：
            结果 为 做 字符串拼接 用 结果、「#」。
        否则：
            结果 为 做 字符串拼接 用 结果、「-」。
        完。
        i 为 i 加 1。
    完。
    结果 为 做 字符串拼接 用 结果、「]」。
    返还 结果。
完。

df_使用率颜色 接受 百分比：
    若 百分比 大于或等于 90：
        返还 DF_颜色_红。
    完。
    若 百分比 大于或等于 70：
        返还 DF_颜色_黄。
    完。
    若 百分比 大于或等于 50：
        返还 DF_颜色_绿。
    完。
    返还 「」。
完。

df_路径匹配 接受 路径、挂载点：
    若 做 字符串_相等 用 挂载点、「/」：
        返还 真。
    完。
    返还 做 字符串查找 用 路径、挂载点 等于 0。
完。

df_填充 接受 文本、宽度：
    可变 结果 为 文本。
    只要 做 字符串长度 用 结果 小于 宽度：
        结果 为 做 字符串拼接 用 结果、「 」。
    完。
    返还 结果。
完。

df_右对齐 接受 文本、宽度：
    可变 结果 为 「」。
    差 为 宽度 减 做 字符串长度 用 文本。
    可变 i 为 0。
    只要 i 小于 差：
        结果 为 做 字符串拼接 用 结果、「 」。
        i 为 i 加 1。
    完。
    结果 为 做 字符串拼接 用 结果、文本。
    返还 结果。
完。

格式化大小 接受 字节：
    若 字节 小于 1024：
        返还 做 字符串拼接 用 做 整数转字符串 用 字节、「B」。
    完。
    若 字节 小于 1048576：
        返还 做 字符串拼接 用 做 整数转字符串 用 字节 除以 1024、「K」。
    完。
    若 字节 小于 1073741824：
        返还 做 字符串拼接 用 做 整数转字符串 用 字节 除以 1048576、「M」。
    完。
    返还 做 字符串拼接 用 做 整数转字符串 用 字节 除以 1073741824、「G」。
完。

格式化数量 接受 n：
    若 n 小于 1000：
        返还 做 整数转字符串 用 n。
    完。
    若 n 小于 1000000：
        返还 做 字符串拼接 用 做 整数转字符串 用 n 除以 1000、「K」。
    完。
    返还 做 字符串拼接 用 做 整数转字符串 用 n 除以 1000000、「M」。
完。

df_显示帮助 接受 零参：
    做 打印行 用 「用法: df [选项] [文件路径]」。
    做 打印行 用 「显示文件系统磁盘空间使用情况」。
    做 打印行 用 「」。
    做 打印行 用 「选项:」。
    做 打印行 用 「  -h, --human-readable  以人类可读格式显示大小」。
    做 打印行 用 「  -i, --inodes          显示inode使用情况」。
    做 打印行 用 「  -T, --type            显示文件系统类型」。
    做 打印行 用 「  -l, --local           仅显示本地文件系统」。
    做 打印行 用 「  -b, --bar             显示使用率进度条」。
    做 打印行 用 「  --help                显示此帮助信息」。
完。

获取挂载列表 接受 零参：
    可变 列表 为 一组：（空）。
    VFS列表 为 做 VFS_获取挂载列表。
    若 VFS列表 不等于 0：
        可变 i 为 0。
        只要 i 小于 做 计数 用 VFS列表：
            m 为 VFS列表 的 第 i 项。
            stat 为 做 VFS_statfs 用 m 的 挂载点。
            总量 为 0。
            已用 为 0。
            总inode 为 0。
            已用inode 为 0。
            若 stat 不等于 0：
                总量 为 stat 的 块数 乘 stat 的 块大小。
                已用 为 (stat 的 块数 减 stat 的 可用块数) 乘 stat 的 块大小。
                总inode 为 stat 的 总inode。
                已用inode 为 stat 的 总inode 减 stat 的 可用inode。
            完。
            做 追加 用 列表、一组：m 的 设备、m 的 挂载点、总量、已用、m 的 文件系统类型、总inode、已用inode、m 的 是本地。
            i 为 i 加 1。
        完。
        返还 列表。
    完。
    返还 一组：一组：「rootfs」、「/」、268435456、52428800、「NovaFS」、65536、4200、真、一组：「devtmpfs」、「/dev」、0、0、「devtmpfs」、0、0、真、一组：「proc」、「/proc」、0、0、「proc」、0、0、假、一组：「tmpfs」、「/tmp」、134217728、1048576、「tmpfs」、32768、12、真。
完。

可变 AI_磁盘查_热文件表 为 一组：（空）。

AI_磁盘查_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_磁盘查_热文件表：
        若 AI_磁盘查_热文件表 的 第 i 项 的 路径 等于 路径：
            AI_磁盘查_热文件表 的 第 i 项 的 频率 为 AI_磁盘查_热文件表 的 第 i 项 的 频率 加 1。
            返还 AI_磁盘查_热文件表 的 第 i 项 的 频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_磁盘查_热文件表 小于 128：
        做 追加 用 AI_磁盘查_热文件表、一本字典：路径 对应 路径、频率 对应 1。
    完。
    返还 1。
完。

AI_磁盘查_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
        返还 0。
    完。
    返还 32768。
完。

本章 收束。
