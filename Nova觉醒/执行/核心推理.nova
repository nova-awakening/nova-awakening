《核心推理》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

引用《.·深度语义》。

意图推理_分析 接受 输入：
    意图_总推理次数 为 意图_总推理次数 加 1。
    结果 为 新建 意图结果。
    结果 的 参数 为 一组：（空）。
    结果 的 需要确认 为 假。
    输入小写 为 做 字符串小写 用 输入。
    输入修剪 为 做 字符串修剪 用 输入小写。
    若 做 意图推理_是URL 用 输入修剪：
        结果 的 意图类型 为 意图_导航。
        结果 的 置信度 为 95。
        做 追加 用 结果 的 参数、一组：「url」、输入修剪。
        结果 的 回复文本 为 「正在导航到: 」 加 输入修剪。
        返还 结果。
    完。
    若 做 字符串查找 用 输入修剪、「nova://」 等于 0：
        结果 的 意图类型 为 意图_导航。
        结果 的 置信度 为 99。
        做 追加 用 结果 的 参数、一组：「url」、输入修剪。
        返还 结果。
    完。
    命令结果 为 做 意图推理_匹配命令 用 输入修剪。
    若 命令结果 不等于 空：
        意图_命中次数 为 意图_命中次数 加 1。
        返还 命令结果。
    完。
    若 做 意图推理_是计算 用 输入修剪：
        结果 的 意图类型 为 意图_计算。
        结果 的 置信度 为 85。
        做 追加 用 结果 的 参数、一组：「expr」、输入修剪。
        结果 的 回复文本 为 「计算结果: 」 加 做 意图推理_计算 用 输入修剪。
        返还 结果。
    完。
    TNF结果 为 做 意图推理_TNF深度分析 用 输入修剪。
    若 TNF结果 的 置信度 大于 60：
        返还 TNF结果。
    完。
    结果 的 意图类型 为 意图_搜索。
    结果 的 置信度 为 50。
    做 追加 用 结果 的 参数、一组：「query」、输入。
    做 追加 用 结果 的 参数、一组：「url」、「https://search.novaos.dev/?q=」 加 输入。
    结果 的 回复文本 为 「搜索: 」 加 输入。
    返还 结果。
完。

意图推理_匹配命令 接受 输入：
    可变 最佳 为 空。
    可变 最高分 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 意图_命令表：
        映射 为 意图_命令表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 映射 的 关键词列表：
            关键词 为 映射 的 关键词列表 的 第 j 项。
            匹配分 为 做 意图推理_计算匹配度 用 输入、关键词。
            若 匹配分 大于 最高分：
                最高分 为 匹配分。
                最佳 为 映射。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 最佳 不等于 空 并且 最高分 大于或等于 70：
        结果 为 新建 意图结果。
        结果 的 意图类型 为 最佳 的 意图类型。
        结果 的 置信度 为 最高分。
        结果 的 内核操作 为 最佳 的 内核操作。
        结果 的 回复文本 为 最佳 的 描述。
        结果 的 参数 为 一组：（空）。
        结果 的 需要确认 为 假。
        若 最佳 的 内核操作 等于 「系统_关机」 或者 最佳 的 内核操作 等于 「系统_重启」 或者 最佳 的 内核操作 等于 「进程_终止」 或者 最佳 的 内核操作 等于 「VFS_删除」：
            结果 的 需要确认 为 真。
        完。
        返还 结果。
    完。
    返还 空。
完。

意图推理_计算匹配度 接受 输入、关键词：
    若 输入 等于 关键词：
        返还 100。
    完。
    若 做 字符串查找 用 输入、关键词 大于或等于 0：
        返还 85。
    完。
    若 做 字符串查找 用 关键词、输入 大于或等于 0：
        返还 75。
    完。
    短 为 关键词。
    若 做 字符串长度 用 输入 小于 做 字符串长度 用 关键词：
        短 为 输入。
    完。
    匹配长度 为 0。
    可变 k 为 0。
    只要 k 小于 做 字符串长度 用 短：
        若 做 字符串子串 用 输入、k、1 等于 做 字符串子串 用 关键词、k、1：
            匹配长度 为 匹配长度 加 1。
        否则：
            中断。
        完。
        k 为 k 加 1。
    完。
    若 匹配长度 大于 0 并且 做 字符串长度 用 关键词 大于 0：
        返还 匹配长度 乘 70 除以 做 字符串长度 用 关键词。
    完。
    返还 0。
完。

本章 收束。
