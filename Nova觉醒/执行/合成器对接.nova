《合成器对接》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·资源调度》。

引用《.·渲染层管理》。

窗口类型_主窗口 为 0。

窗口类型_弹窗 为 1。

窗口类型_DevTools 为 2。

窗口类型_下载 为 3。

窗口类型_设置 为 4。

窗口类型_全屏 为 5。

浏览器窗口 是 一种 事物，含 ID、类型、宿主窗口ID、标签页、当前标签、全屏、x、y、宽、高、标题、图处层。

可变 合成_窗口列表 为 一组：（空）。

可变 合成_下一个ID 为 1。

可变 合成_已初始化 为 假。

合成器对接_初始化 接受 零参：
    合成_窗口列表 为 一组：（空）。
    合成_已初始化 为 真。
    做 内核信息 用 「合成器对接: 浏览器窗口由壳层合成器自管」。
完。

合成器对接_创建窗口 接受 类型、标题、x、y、宽、高：
    浏览器窗 为 新建 浏览器窗口。
    浏览器窗 的 ID 为 合成_下一个ID。
    合成_下一个ID 为 合成_下一个ID 加 1。
    浏览器窗 的 类型 为 类型。
    浏览器窗 的 标签页 为 一组：（空）。
    浏览器窗 的 当前标签 为 0。
    浏览器窗 的 全屏 为 假。
    浏览器窗 的 x 为 x。
    浏览器窗 的 y 为 y。
    浏览器窗 的 宽 为 宽。
    浏览器窗 的 高 为 高。
    浏览器窗 的 标题 为 标题。
    浏览器窗 的 宿主窗口ID 为 0。
    浏览器窗 的 图处层 为 做 浏览器图处_创建层 用 x、y、宽、高。
    做 追加 用 合成_窗口列表、浏览器窗。
    返还 浏览器窗 的 ID。
完。

合成器对接_关闭窗口 接受 窗口ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 合成_窗口列表：
        若 合成_窗口列表 的 第 i 项 的 ID 等于 窗口ID：
            做 浏览器图处_销毁层 用 合成_窗口列表 的 第 i 项 的 图处层。
            做 列表_移除 用 合成_窗口列表、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

合成器对接_移动窗口 接受 窗口ID、x、y：
    窗 为 做 合成器对接_查找窗口 用 窗口ID。
    若 窗 等于 空：
        返还。
    完。
    窗 的 x 为 x。
    窗 的 y 为 y。
    做 浏览器图处_销毁层 用 窗 的 图处层。
    窗 的 图处层 为 做 浏览器图处_创建层 用 x、y、窗 的 宽、窗 的 高。
完。

合成器对接_调整大小 接受 窗口ID、宽、高：
    窗 为 做 合成器对接_查找窗口 用 窗口ID。
    若 窗 等于 空：
        返还。
    完。
    窗 的 宽 为 宽。
    窗 的 高 为 高。
    做 浏览器图处_销毁层 用 窗 的 图处层。
    窗 的 图处层 为 做 浏览器图处_创建层 用 窗 的 x、窗 的 y、宽、高。
完。

合成器对接_全屏切换 接受 窗口ID：
    窗 为 做 合成器对接_查找窗口 用 窗口ID。
    若 窗 等于 空：
        返还。
    完。
    窗 的 全屏 为 非 窗 的 全屏。
完。

合成器对接_设置标题 接受 窗口ID、标题：
    窗 为 做 合成器对接_查找窗口 用 窗口ID。
    若 窗 等于 空：
        返还。
    完。
    窗 的 标题 为 标题。
完。

合成器对接_查找窗口 接受 窗口ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 合成_窗口列表：
        若 合成_窗口列表 的 第 i 项 的 ID 等于 窗口ID：
            返还 合成_窗口列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

合成器对接_添加标签 接受 窗口ID、标签ID：
    窗 为 做 合成器对接_查找窗口 用 窗口ID。
    若 窗 等于 空：
        返还。
    完。
    做 追加 用 窗 的 标签页、标签ID。
    若 做 计数 用 窗 的 标签页 等于 1：
        窗 的 当前标签 为 标签ID。
    完。
完。

合成器对接_切换标签 接受 窗口ID、标签ID：
    窗 为 做 合成器对接_查找窗口 用 窗口ID。
    若 窗 等于 空：
        返还。
    完。
    窗 的 当前标签 为 标签ID。
    做 资源调度_标签切换 用 标签ID。
完。

本章 收束。
