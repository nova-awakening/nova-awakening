《标签处理》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·节点操作》。

引用《..·外联·URL解析辅助》。

DOM_处理标签 接受 文档、标签名、属性、是闭合、栈：
    小写名 为 做 字符串小写 用 标签名。
    若 是闭合：
        可变 i 为 做 计数 用 栈 减 1。
        只要 i 大于或等于 0：
            节点 为 做 DOM_查找节点 用 文档、栈 的 第 i 项。
            若 节点 不等于 空 并且 节点 的 标签名 等于 小写名：
                只要 做 计数 用 栈 大于 i：
                    做 列表_移除 用 栈、做 计数 用 栈 减 1。
                完。
                返还。
            完。
            i 为 i 减 1。
        完。
        返还。
    完。
    标签ID 为 做 DOM_标签名到ID 用 小写名。
    节点 为 做 DOM_创建节点 用 文档、节点_元素、小写名、标签ID。
    可变 ai 为 0。
    只要 ai 小于 做 计数 用 属性：
        做 追加 用 节点 的 属性、属性 的 第 ai 项。
        ai 为 ai 加 1。
    完。
    若 做 计数 用 栈 大于 0：
        做 DOM_添加子节点 用 文档、栈 的 第 做 计数 用 栈 减 1 项、节点。
    完。
    若 标签ID 等于 标签_HEAD：
        文档 的 头节点 为 节点 的 ID。
    完。
    若 标签ID 等于 标签_BODY：
        文档 的 体节点 为 节点 的 ID。
    完。
    若 标签ID 等于 标签_TITLE：
        文档 的 标题 为 「」。
    完。
    若 非 做 DOM_是自闭合标签 用 小写名：
        做 追加 用 栈、节点 的 ID。
    完。
完。

DOM_标签名到ID 接受 名：
    若 名 等于 「html」：
        返还 标签_HTML。
    完。
    若 名 等于 「head」：
        返还 标签_HEAD。
    完。
    若 名 等于 「body」：
        返还 标签_BODY。
    完。
    若 名 等于 「div」：
        返还 标签_DIV。
    完。
    若 名 等于 「span」：
        返还 标签_SPAN。
    完。
    若 名 等于 「p」：
        返还 标签_P。
    完。
    若 名 等于 「a」：
        返还 标签_A。
    完。
    若 名 等于 「img」：
        返还 标签_IMG。
    完。
    若 名 等于 「input」：
        返还 标签_INPUT。
    完。
    若 名 等于 「button」：
        返还 标签_BUTTON。
    完。
    若 名 等于 「script」：
        返还 标签_SCRIPT。
    完。
    若 名 等于 「style」：
        返还 标签_STYLE。
    完。
    若 名 等于 「link」：
        返还 标签_LINK。
    完。
    若 名 等于 「h1」：
        返还 标签_H1。
    完。
    若 名 等于 「h2」：
        返还 标签_H2。
    完。
    若 名 等于 「h3」：
        返还 标签_H3。
    完。
    若 名 等于 「title」：
        返还 标签_TITLE。
    完。
    若 名 等于 「canvas」：
        返还 标签_CANVAS。
    完。
    若 名 等于 「video」：
        返还 标签_VIDEO。
    完。
    若 名 等于 「nav」：
        返还 标签_NAV。
    完。
    若 名 等于 「section」：
        返还 标签_SECTION。
    完。
    返还 标签_UNKNOWN。
完。

DOM_是自闭合标签 接受 名：
    若 名 等于 「br」：
        返还 真。
    完。
    若 名 等于 「hr」：
        返还 真。
    完。
    若 名 等于 「img」：
        返还 真。
    完。
    若 名 等于 「input」：
        返还 真。
    完。
    若 名 等于 「meta」：
        返还 真。
    完。
    若 名 等于 「link」：
        返还 真。
    完。
    返还 假。
完。

DOM_通过ID查找 接受 文档、id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档 的 节点池：
        节点 为 文档 的 节点池 的 第 i 项。
        若 节点 的 类型 等于 节点_元素：
            若 做 DOM_获取属性 用 文档、节点 的 ID、「id」 等于 id：
                返还 节点。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

DOM_通过标签查找 接受 文档、标签名：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档 的 节点池：
        若 文档 的 节点池 的 第 i 项 的 标签名 等于 标签名：
            做 追加 用 结果、文档 的 节点池 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

DOM_通过类名查找 接受 文档、类名：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档 的 节点池：
        cls 为 做 DOM_获取属性 用 文档、文档 的 节点池 的 第 i 项 的 ID、「class」。
        若 做 字符串查找 用 cls、类名 大于或等于 0：
            做 追加 用 结果、文档 的 节点池 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

DOM_统计 接受 零参：
    若 DOM_当前文档 不等于 空：
        返还 一组：DOM_当前文档 的 总节点数、做 计数 用 DOM_当前文档 的 脚本列表、做 计数 用 DOM_当前文档 的 样式列表。
    完。
    返还 一组：0、0、0。
完。

本章 收束。
