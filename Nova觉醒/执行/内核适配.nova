《内核适配》

引用《..·外联·网络子系统入口》。

引用《.·浏览器图处》。

引用《.·处理软件渲》。

引用《.·渲染层管理》。

引用《.·下载管理》。

引用《.·同源策略检查》。

引用《.·安全适配》。

引用《.·威胁评估》。

引用《.·存储初始化》。

引用《..·中枢·整数转字符串》。

引用《.·统计接口》。

引用《.·内存适配》。

引用《.·请求接口》。

引用《.·浏览器网络》。

引用《.·浏览器Cookie存储》。

适配档 是 一种 事物，含 名称、宽、高、最近层、最近请求、最近响应、最近下载、最近域、最近风险、最近统计、初始化完成、内存数、图处数、存储数、安全数、网络数、命中数、失败数、健康。

适配摘要 是 一种 事物，含 名称、内存数、图处数、存储数、安全数、网络数、命中数、失败数、健康。

适配请求 是 一种 事物，含 方法、地址、头、体、下载名、域。

可变 适配账本 为 一组：（空）。

可变 适配建数 为 0。

可变 适配总执行 为 0。

可变 适配总命中 为 0。

可变 适配总失败 为 0。

内核适配_建档 接受 名称、宽、高：
    档 为 做 堆_分配 用 适配档。
    档 的 名称 为 名称。
    档 的 宽 为 宽。
    档 的 高 为 高。
    档 的 最近层 为 0。
    档 的 最近请求 为 0。
    档 的 最近响应 为 0。
    档 的 最近下载 为 0。
    档 的 最近域 为 「」。
    档 的 最近风险 为 0。
    档 的 最近统计 为 0。
    档 的 初始化完成 为 假。
    档 的 内存数 为 0。
    档 的 图处数 为 0。
    档 的 存储数 为 0。
    档 的 安全数 为 0。
    档 的 网络数 为 0。
    档 的 命中数 为 0。
    档 的 失败数 为 0。
    档 的 健康 为 「未初始化」。
    返还 档。
完。

内核适配_登记 接受 档：
    做 追加 用 适配账本、档。
    适配建数 为 适配建数 加 1。
    返还 档。
完。

内核适配_创建 接受 名称、宽、高：
    档 为 做 内核适配_建档 用 名称、宽、高。
    做 内核适配_初始化 用 档。
    返还 做 内核适配_登记 用 档。
完。

内核适配_初始化 接受 档：
    做 内核适配_初始化内存 用 档。
    做 内核适配_初始化图处 用 档、档 的 宽、档 的 高。
    做 内核适配_初始化存储 用 档。
    做 内核适配_初始化安全 用 档。
    做 内核适配_初始化网络 用 档。
    档 的 初始化完成 为 真。
    档 的 健康 为 「运行」。
    返还 档。
完。

内核适配_初始化内存 接受 档：
    做 浏览器内存_初始化。
    档 的 内存数 为 档 的 内存数 加 1。
    适配总执行 为 适配总执行 加 1。
完。

内核适配_分配 接受 档、池类型、大小：
    地址 为 做 浏览器内存_分配 用 池类型、大小。
    档 的 内存数 为 档 的 内存数 加 1。
    档 的 最近统计 为 做 浏览器内存_获取统计。
    返还 地址。
完。

内核适配_释放 接受 档、池类型、地址、大小：
    做 浏览器内存_释放 用 池类型、地址、大小。
    档 的 内存数 为 档 的 内存数 加 1。
    档 的 最近统计 为 做 浏览器内存_获取统计。
完。

内核适配_AI预分配 接受 档、池类型、预测数量：
    做 浏览器内存_AI预分配 用 池类型、预测数量。
    档 的 内存数 为 档 的 内存数 加 1。
完。

内核适配_检查压力 接受 档：
    压力 为 做 浏览器内存_检查压力。
    档 的 最近统计 为 压力。
    若 压力 大于 1：
        做 浏览器内存_主动回收。
    完。
    返还 压力。
完。

内核适配_紧急回收 接受 档：
    做 浏览器内存_紧急回收。
    档 的 内存数 为 档 的 内存数 加 1。
完。

内核适配_内存统计 接受 档：
    统计 为 做 浏览器内存_获取统计。
    档 的 最近统计 为 统计。
    返还 统计。
完。

内核适配_初始化图处 接受 档、宽、高：
    做 浏览器图处_初始化 用 宽、高。
    做 软渲_初始化。
    档 的 图处数 为 档 的 图处数 加 1。
    适配总执行 为 适配总执行 加 1。
完。

内核适配_创建层 接受 档、x、y、宽、高：
    层 为 做 浏览器图处_创建层 用 x、y、宽、高。
    档 的 图处数 为 档 的 图处数 加 1。
    档 的 最近层 为 层。
    返还 层。
完。

内核适配_填充矩形 接受 档、层ID、x、y、宽、高、颜色、透明度：
    做 浏览器图处_填充矩形 用 层ID、x、y、宽、高、颜色、透明度。
    档 的 图处数 为 档 的 图处数 加 1。
完。

内核适配_绘制文字 接受 档、层ID、x、y、文字、字号、颜色：
    做 浏览器图处_绘制文字 用 层ID、x、y、文字、字号、颜色。
    档 的 图处数 为 档 的 图处数 加 1。
完。

内核适配_绘制图像 接受 档、层ID、x、y、宽、高、图像地址、图像宽、图像高：
    做 浏览器图处_绘制图像 用 层ID、x、y、宽、高、图像地址、图像宽、图像高。
    档 的 图处数 为 档 的 图处数 加 1。
完。

内核适配_圆角矩形 接受 档、层ID、x、y、宽、高、颜色、圆角、透明度：
    做 浏览器图处_圆角矩形 用 层ID、x、y、宽、高、颜色、圆角、透明度。
    档 的 图处数 为 档 的 图处数 加 1。
完。

内核适配_阴影 接受 档、层ID、x、y、宽、高、颜色、模糊：
    做 浏览器图处_绘制阴影 用 层ID、x、y、宽、高、颜色、模糊。
    档 的 图处数 为 档 的 图处数 加 1。
完。

内核适配_提交层命令 接受 档、层ID、命令：
    做 浏览器图处_提交命令 用 层ID、命令。
    档 的 图处数 为 档 的 图处数 加 1。
完。

内核适配_合成帧 接受 档：
    做 浏览器图处_合成帧。
    做 软渲_交换缓冲。
    档 的 图处数 为 档 的 图处数 加 1。
完。

内核适配_软件回退 接受 档、层：
    做 浏览器图处_软件合成 用 层。
    档 的 图处数 为 档 的 图处数 加 1。
    档 的 最近层 为 层。
完。

内核适配_脏矩 接受 档、x、y、宽、高：
    做 软渲_标记脏矩 用 x、y、宽、高。
    档 的 图处数 为 档 的 图处数 加 1。
完。

内核适配_图处统计 接受 档：
    统计 为 做 浏览器图处_统计。
    档 的 最近统计 为 统计。
    返还 统计。
完。

内核适配_初始化存储 接受 档：
    做 浏览器存储_初始化。
    档 的 存储数 为 档 的 存储数 加 1。
    适配总执行 为 适配总执行 加 1。
完。

内核适配_开始下载 接受 档、地址、文件名：
    任务 为 做 浏览器存储_开始下载 用 地址、文件名。
    档 的 最近下载 为 任务。
    档 的 存储数 为 档 的 存储数 加 1。
    返还 任务。
完。

内核适配_写块 接受 档、任务ID、数据、大小：
    做 浏览器存储_下载写入块 用 任务ID、数据、大小。
    档 的 存储数 为 档 的 存储数 加 1。
完。

内核适配_下载完成 接受 档、任务ID：
    做 浏览器存储_下载完成 用 任务ID。
    档 的 存储数 为 档 的 存储数 加 1。
完。

内核适配_暂停下载 接受 档、任务ID：
    做 浏览器存储_暂停下载 用 任务ID。
    档 的 存储数 为 档 的 存储数 加 1。
完。

内核适配_恢复下载 接受 档、任务ID：
    做 浏览器存储_恢复下载 用 任务ID。
    档 的 存储数 为 档 的 存储数 加 1。
完。

内核适配_下载进度 接受 档、任务ID：
    进度 为 做 浏览器存储_下载进度 用 任务ID。
    档 的 最近下载 为 进度。
    返还 进度。
完。

内核适配_缓存查找 接受 档、地址：
    缓存 为 做 浏览器存储_缓存查找 用 地址。
    若 缓存 不等于 0 并且 缓存 不等于 空：
        档 的 命中数 为 档 的 命中数 加 1。
        适配总命中 为 适配总命中 加 1。
    完。
    档 的 存储数 为 档 的 存储数 加 1。
    返还 缓存。
完。

内核适配_缓存写入 接受 档、地址、内容类型、数据、过期时间：
    做 浏览器存储_缓存写入 用 地址、内容类型、数据、过期时间。
    档 的 存储数 为 档 的 存储数 加 1。
完。

内核适配_Cookie设置 接受 档、名称、值、域、路径、过期、安全、仅HTTP：
    做 浏览器存储_Cookie设置 用 名称、值、域、路径、过期、安全、仅HTTP。
    档 的 存储数 为 档 的 存储数 加 1。
完。

内核适配_Cookie获取 接受 档、域、路径：
    结果 为 做 浏览器存储_Cookie获取 用 域、路径。
    档 的 存储数 为 档 的 存储数 加 1。
    返还 结果。
完。

内核适配_本地设置 接受 档、域、键、值：
    做 浏览器存储_本地设置 用 域、键、值。
    档 的 存储数 为 档 的 存储数 加 1。
完。

内核适配_本地获取 接受 档、域、键：
    值 为 做 浏览器存储_本地获取 用 域、键。
    档 的 存储数 为 档 的 存储数 加 1。
    返还 值。
完。

内核适配_存储统计 接受 档：
    统计 为 做 浏览器存储_统计。
    档 的 最近统计 为 统计。
    返还 统计。
完。

内核适配_初始化安全 接受 档：
    做 浏览器安全_初始化。
    档 的 安全数 为 档 的 安全数 加 1。
    适配总执行 为 适配总执行 加 1。
完。

内核适配_评估域 接受 档、域：
    结果 为 做 浏览器安全_评估域 用 域。
    档 的 最近域 为 域。
    档 的 最近风险 为 结果。
    档 的 安全数 为 档 的 安全数 加 1。
    返还 结果。
完。

内核适配_AI威胁评估 接受 档、域：
    结果 为 做 浏览器安全_AI威胁评估 用 域。
    档 的 最近域 为 域。
    档 的 最近风险 为 结果。
    档 的 安全数 为 档 的 安全数 加 1。
    返还 结果。
完。

内核适配_评估网址 接受 档、地址：
    结果 为 做 浏览器安全_评估URL 用 地址。
    档 的 最近域 为 地址。
    档 的 最近风险 为 结果。
    档 的 安全数 为 档 的 安全数 加 1。
    返还 结果。
完。

内核适配_速率检查 接受 档、域：
    结果 为 做 浏览器安全_速率检查 用 域。
    档 的 安全数 为 档 的 安全数 加 1。
    档 的 最近风险 为 结果。
    返还 结果。
完。

内核适配_白名单 接受 档、域：
    做 浏览器安全_添加白名单 用 域。
    档 的 安全数 为 档 的 安全数 加 1。
完。

内核适配_黑名单 接受 档、域：
    做 浏览器安全_添加黑名单 用 域。
    档 的 安全数 为 档 的 安全数 加 1。
完。

内核适配_安全统计 接受 档：
    统计 为 做 浏览器安全_统计。
    档 的 最近统计 为 统计。
    返还 统计。
完。

内核适配_初始化网络 接受 档：
    做 浏览器网络_初始化。
    档 的 网络数 为 档 的 网络数 加 1。
    适配总执行 为 适配总执行 加 1。
完。

内核适配_建请求 接受 方法、地址、头、体、下载名、域：
    请求 为 做 堆_分配 用 适配请求。
    请求 的 方法 为 方法。
    请求 的 地址 为 地址。
    请求 的 头 为 头。
    请求 的 体 为 体。
    请求 的 下载名 为 下载名。
    请求 的 域 为 域。
    返还 请求。
完。

内核适配_解析地址 接受 档、地址：
    结果 为 做 浏览器网络_解析URL 用 地址。
    档 的 最近请求 为 结果。
    档 的 网络数 为 档 的 网络数 加 1。
    返还 结果。
完。

内核适配_发起请求 接受 档、方法、地址、请求头、请求体：
    请求 为 做 浏览器网络_发起请求 用 方法、地址、请求头、请求体。
    档 的 最近请求 为 请求。
    档 的 网络数 为 档 的 网络数 加 1。
    返还 请求。
完。

内核适配_构造请求 接受 档、请求、解析结果：
    结果 为 做 浏览器网络_构造HTTP请求 用 请求、解析结果。
    档 的 最近响应 为 结果。
    档 的 网络数 为 档 的 网络数 加 1。
    返还 结果。
完。

内核适配_接收响应 接受 档、请求：
    做 浏览器网络_接收响应 用 请求。
    档 的 最近响应 为 请求。
    档 的 网络数 为 档 的 网络数 加 1。
    返还 请求。
完。

内核适配_网络探活 接受 档、地址、端口：
    结果 为 做 浏览器网络_请求 用 地址、端口。
    档 的 最近响应 为 结果。
    档 的 网络数 为 档 的 网络数 加 1。
    返还 结果。
完。

内核适配_预取 接受 档、地址、优先级：
    做 浏览器网络_预取 用 地址、优先级。
    档 的 网络数 为 档 的 网络数 加 1。
完。

内核适配_活跃连接数 接受 档：
    数量 为 做 浏览器网络_活跃连接数。
    档 的 最近统计 为 数量。
    返还 数量。
完。

内核适配_网络统计 接受 档：
    统计 为 做 浏览器网络_统计。
    档 的 最近统计 为 统计。
    返还 统计。
完。

内核适配_执行请求 接受 档、请求：
    风险 为 做 内核适配_评估网址 用 档、请求 的 地址。
    若 风险 不等于 0 并且 风险 大于 80：
        档 的 失败数 为 档 的 失败数 加 1。
        适配总失败 为 适配总失败 加 1。
        档 的 最近风险 为 风险。
        返还 0。
    完。
    解析结果 为 做 内核适配_解析地址 用 档、请求 的 地址。
    发起 为 做 内核适配_发起请求 用 档、请求 的 方法、请求 的 地址、请求 的 头、请求 的 体。
    做 内核适配_构造请求 用 档、发起、解析结果。
    做 内核适配_接收响应 用 档、发起。
    若 请求 的 下载名 不等于 「」：
        做 内核适配_开始下载 用 档、请求 的 地址、请求 的 下载名。
    完。
    若 请求 的 域 不等于 「」：
        做 内核适配_Cookie设置 用 档、「会话」、「已接入」、请求 的 域、「/」、0、真、假。
    完。
    返还 发起。
完。

内核适配_摘要 接受 档：
    返还 新建 适配摘要：名称 为 档 的 名称、内存数 为 档 的 内存数、图处数 为 档 的 图处数、存储数 为 档 的 存储数、安全数 为 档 的 安全数、网络数 为 档 的 网络数、命中数 为 档 的 命中数、失败数 为 档 的 失败数、健康 为 做 内核适配_健康 用 档。
完。

内核适配_状态串 接受 档：
    返还 做 字符串拼接 用 「适配[」、做 字符串拼接 用 档 的 名称、做 字符串拼接 用 「] 图处=」、做 字符串拼接 用 做 整数转字符串 用 档 的 图处数、做 字符串拼接 用 「 网络=」、做 字符串拼接 用 做 整数转字符串 用 档 的 网络数、做 字符串拼接 用 「 安全=」、做 整数转字符串 用 档 的 安全数。
完。

内核适配_健康 接受 档：
    若 非 档 的 初始化完成：
        返还 「待初始化」。
    完。
    若 档 的 失败数 大于 0：
        返还 「退化」。
    完。
    若 档 的 最近风险 不等于 0 并且 档 的 最近风险 大于 80：
        返还 「阻断」。
    完。
    返还 「运行」。
完。

内核适配_自检 接受 档：
    做 内核适配_初始化 用 档。
    地址 为 做 内核适配_分配 用 档、池类型_DOM节点、256。
    若 地址 不等于 0：
        做 内核适配_释放 用 档、池类型_DOM节点、地址、256。
        档 的 命中数 为 档 的 命中数 加 1。
        适配总命中 为 适配总命中 加 1。
    完。
    层 为 做 内核适配_创建层 用 档、0、0、640、360。
    做 内核适配_填充矩形 用 档、层、0、0、640、360、0x202020FF、255。
    做 内核适配_绘制文字 用 档、层、24、24、「内核适配自检」、18、0xFFFFFFFF。
    做 内核适配_合成帧 用 档。
    做 内核适配_缓存写入 用 档、「nova://health」、「text/plain」、「ok」、60000。
    做 内核适配_评估域 用 档、「nova」。
    做 内核适配_网络探活 用 档、「127.0.0.1」、80。
    档 的 健康 为 做 内核适配_健康 用 档。
    返还 档 的 健康。
完。

内核适配_按名查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 适配账本：
        若 适配账本 的 第 i 项 的 名称 等于 名称：
            返还 适配账本 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

内核适配_最近请求 接受 档：
    返还 档 的 最近请求。
完。

内核适配_最近响应 接受 档：
    返还 档 的 最近响应。
完。

内核适配_最近下载 接受 档：
    返还 档 的 最近下载。
完。

内核适配_最近风险 接受 档：
    返还 档 的 最近风险。
完。

内核适配_账本数 接受 零参：
    返还 做 计数 用 适配账本。
完。

内核适配_全局统计 接受 零参：
    返还 一组：适配建数、适配总执行、适配总命中、适配总失败。
完。

内核适配_示例流程 接受 零参：
    档 为 做 内核适配_创建 用 「内核适配」、1280、720。
    层 为 做 内核适配_创建层 用 档、0、0、1280、720。
    做 内核适配_圆角矩形 用 档、层、32、32、320、180、0x0F172AFF、24、255。
    做 内核适配_阴影 用 档、层、32、32、320、180、0x00000088、18。
    做 内核适配_绘制文字 用 档、层、56、80、「一体化适配」、22、0xF8FAFCFF。
    做 内核适配_软件回退 用 档、层。
    请求 为 做 内核适配_建请求 用 「GET」、「nova://system/health」、一组：（空）、「」、「系统健康.json」、「nova」。
    做 内核适配_执行请求 用 档、请求。
    做 内核适配_本地设置 用 档、「nova」、「模式」、「一体」。
    做 内核适配_下载进度 用 档、档 的 最近下载。
    做 内核适配_网络统计 用 档。
    返还 做 内核适配_摘要 用 档。
完。

本章 收束。
