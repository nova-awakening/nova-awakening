《AI页面访问》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《.·窗口管理每》。

引用《..·外联·URL解析辅助》。

引用《.·标签处理》。

引用《.·请求接口》。

引用《.·节点操作》。

引用《.·用户输入处》。

引用《.·生成窗口元》。

Nova原生桌面_AI页面_截断 接受 文本、最大长度：
    若 文本 等于 空：
        返还 「」。
    完。
    若 做 字符串长度 用 文本 小于或等于 最大长度：
        返还 文本。
    完。
    若 最大长度 小于或等于 3：
        返还 做 子字符串 用 文本、0、最大长度。
    完。
    返还 做 子字符串 用 文本、0、最大长度 减 3 加 「...」。
完。

Nova原生桌面_AI页面_获取焦点窗口索引 接受 零参：
    若 非 桌面_已初始化：
        返还 负 1。
    完。
    若 桌面 的 焦点窗口 小于或等于 0：
        返还 负 1。
    完。
    返还 做 Nova原生桌面_查找窗口索引 用 桌面 的 焦点窗口。
完。

Nova原生桌面_AI页面_判断类型 接受 url：
    若 做 字符串查找 用 url、「nova://」 等于 0：
        返还 「system」。
    完。
    若 做 字符串查找 用 url、「http://」 等于 0 或者 做 字符串查找 用 url、「https://」 等于 0：
        返还 「web」。
    完。
    返还 「workspace」。
完。

Nova原生桌面_AI页面_加载窗口上下文_按索引 接受 窗口索引：
    若 非 桌面_已初始化：
        返还 假。
    完。
    若 窗口索引 小于 0 或者 窗口索引 大于或等于 做 计数 用 桌面 的 窗口列表：
        返还 假。
    完。
    url 为 桌面 的 窗口列表 的 第 窗口索引 项 的 URL。
    请求 为 做 浏览器网络_发起请求 用 方法_GET、url、一组：（空）、「」。
    若 请求 的 状态 不等于 请求状态_完成 或者 做 计数 用 请求 的 响应体 小于或等于 0：
        桌面 的 窗口列表 的 第 窗口索引 项 的 页面源码 为 「」。
        桌面 的 窗口列表 的 第 窗口索引 项 的 页面DOM 为 空。
        桌面 的 窗口列表 的 第 窗口索引 项 的 页面类型 为 做 Nova原生桌面_AI页面_判断类型 用 url。
        桌面 的 窗口列表 的 第 窗口索引 项 的 页面标题 为 桌面 的 窗口列表 的 第 窗口索引 项 的 标题。
        桌面 的 窗口列表 的 第 窗口索引 项 的 页面最后动作 为 「page-load-failed」。
        返还 假。
    完。
    页面源码 为 请求 的 响应体 的 第 0 项。
    页面DOM 为 做 DOM_解析HTML 用 页面源码、url。
    桌面 的 窗口列表 的 第 窗口索引 项 的 页面源码 为 页面源码。
    桌面 的 窗口列表 的 第 窗口索引 项 的 页面DOM 为 页面DOM。
    桌面 的 窗口列表 的 第 窗口索引 项 的 页面类型 为 做 Nova原生桌面_AI页面_判断类型 用 url。
    桌面 的 窗口列表 的 第 窗口索引 项 的 页面标题 为 桌面 的 窗口列表 的 第 窗口索引 项 的 标题。
    若 页面DOM 不等于 空：
        DOM_当前文档 为 页面DOM。
        若 做 字符串长度 用 页面DOM 的 标题 大于 0：
            桌面 的 窗口列表 的 第 窗口索引 项 的 页面标题 为 页面DOM 的 标题。
        完。
    完。
    桌面 的 窗口列表 的 第 窗口索引 项 的 页面最后动作 为 「page-loaded」。
    返还 真。
完。

Nova原生桌面_AI页面_确保窗口上下文_按索引 接受 窗口索引：
    若 非 桌面_已初始化：
        返还 假。
    完。
    若 窗口索引 小于 0 或者 窗口索引 大于或等于 做 计数 用 桌面 的 窗口列表：
        返还 假。
    完。
    若 桌面 的 窗口列表 的 第 窗口索引 项 的 页面DOM 不等于 空 并且 做 字符串长度 用 桌面 的 窗口列表 的 第 窗口索引 项 的 页面源码 大于 0：
        DOM_当前文档 为 桌面 的 窗口列表 的 第 窗口索引 项 的 页面DOM。
        返还 真。
    完。
    返还 做 Nova原生桌面_AI页面_加载窗口上下文_按索引 用 窗口索引。
完。

Nova原生桌面_AI页面_DOM摘要 接受 文档、最大节点数：
    若 文档 等于 空：
        返还 「」。
    完。
    可变 摘要 为 「」。
    可变 已记录 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档 的 节点池 并且 已记录 小于 最大节点数：
        节点 为 文档 的 节点池 的 第 i 项。
        若 节点 的 类型 不等于 节点_元素：
            i 为 i 加 1。
            继续。
        完。
        若 节点 的 标签名 等于 「html」 或者 节点 的 标签名 等于 「head」 或者 节点 的 标签名 等于 「body」 或者 节点 的 标签名 等于 「script」 或者 节点 的 标签名 等于 「style」：
            i 为 i 加 1。
            继续。
        完。
        可变 条目 为 节点 的 标签名。
        id 为 做 DOM_获取属性 用 文档、节点 的 ID、「id」。
        cls 为 做 DOM_获取属性 用 文档、节点 的 ID、「class」。
        href 为 做 DOM_获取属性 用 文档、节点 的 ID、「href」。
        value 为 做 DOM_获取属性 用 文档、节点 的 ID、「value」。
        若 做 字符串长度 用 id 大于 0：
            条目 为 条目 加 「#」 加 id。
        完。
        若 做 字符串长度 用 cls 大于 0：
            条目 为 条目 加 「.」 加 做 Nova原生桌面_AI页面_截断 用 cls、20。
        完。
        若 做 字符串长度 用 节点 的 文本内容 大于 0：
            条目 为 条目 加 「 \"」 加 做 Nova原生桌面_AI页面_截断 用 节点 的 文本内容、24 加 「\"」。
        完。
        若 做 字符串长度 用 value 大于 0：
            条目 为 条目 加 「 = 」 加 做 Nova原生桌面_AI页面_截断 用 value、24。
        完。
        若 做 字符串长度 用 href 大于 0：
            条目 为 条目 加 「 -> 」 加 做 Nova原生桌面_AI页面_截断 用 href、48。
        完。
        若 做 字符串长度 用 摘要 大于 0：
            摘要 为 摘要 加 「\n」。
        完。
        摘要 为 摘要 加 条目。
        已记录 为 已记录 加 1。
        i 为 i 加 1。
    完。
    若 做 字符串长度 用 摘要 等于 0：
        返还 「页面DOM已解析, 暂无可摘要节点」。
    完。
    返还 摘要。
完。

Nova原生桌面_AI页面_导航当前窗口 接受 窗口索引、url、标题：
    若 非 桌面_已初始化：
        返还 假。
    完。
    若 窗口索引 小于 0 或者 窗口索引 大于或等于 做 计数 用 桌面 的 窗口列表：
        返还 假。
    完。
    可变 新标题 为 标题。
    若 做 字符串长度 用 新标题 等于 0：
        新标题 为 url。
    完。
    桌面 的 窗口列表 的 第 窗口索引 项 的 URL 为 url。
    桌面 的 窗口列表 的 第 窗口索引 项 的 标题 为 新标题。
    桌面 的 窗口列表 的 第 窗口索引 项 的 页面类型 为 做 Nova原生桌面_AI页面_判断类型 用 url。
    桌面 的 窗口列表 的 第 窗口索引 项 的 页面标题 为 新标题。
    桌面 的 窗口列表 的 第 窗口索引 项 的 页面源码 为 「」。
    桌面 的 窗口列表 的 第 窗口索引 项 的 页面DOM 为 空。
    桌面 的 窗口列表 的 第 窗口索引 项 的 页面最后动作 为 「navigate-current」。
    桌面 的 窗口列表 的 第 窗口索引 项 的 DOM文档 为 做 Nova原生桌面_构建窗口文档 用 桌面 的 窗口列表 的 第 窗口索引 项。
    桌面 的 壳最后动作 为 「navigate-window」。
    桌面 的 壳最后结果 为 「窗口导航到: 」 加 url。
    做 Nova原生桌面_同步文档状态。
    做 事件循环_导航 用 url。
    返还 真。
完。

Nova原生桌面_获取当前页面上下文 接受 零参：
    若 非 桌面_已初始化：
        返还 一组：0、「」、「」、「」、「」、「」、「」、0、0、0。
    完。
    窗口索引 为 做 Nova原生桌面_AI页面_获取焦点窗口索引。
    若 窗口索引 小于 0：
        返还 一组：0、「」、「」、「」、「」、「」、「」、0、0、0。
    完。
    做 Nova原生桌面_AI页面_确保窗口上下文_按索引 用 窗口索引。
    窗口 为 桌面 的 窗口列表 的 第 窗口索引 项。
    可变 摘要 为 「页面源码未就绪」。
    可变 总节点数 为 0。
    可变 脚本数 为 0。
    可变 样式数 为 0。
    若 窗口 的 页面DOM 不等于 空：
        摘要 为 做 Nova原生桌面_AI页面_DOM摘要 用 窗口 的 页面DOM、12。
        总节点数 为 窗口 的 页面DOM 的 总节点数。
        脚本数 为 做 计数 用 窗口 的 页面DOM 的 脚本列表。
        样式数 为 做 计数 用 窗口 的 页面DOM 的 样式列表。
    完。
    返还 一组：窗口 的 ID、窗口 的 标题、窗口 的 URL、窗口 的 页面类型、窗口 的 页面标题、窗口 的 页面源码、摘要、总节点数、脚本数、样式数。
完。

Nova原生桌面_执行页面动作 接受 动作、参数1、参数2：
    若 非 桌面_已初始化：
        返还 「desktop-not-ready」。
    完。
    若 动作 等于 「open-url」：
        几何 为 做 Nova原生桌面_建议窗口几何 用 做 计数 用 桌面 的 窗口列表 加 1。
        窗口ID 为 做 Nova原生桌面_打开或聚焦窗口 用 参数1、参数1、几何 的 第 0 项、几何 的 第 1 项、几何 的 第 2 项、几何 的 第 3 项。
        若 窗口ID 大于 0：
            返还 「open-url: 」 加 参数1。
        完。
        返还 「open-url-failed: 」 加 参数1。
    完。
    若 动作 等于 「focus-url」：
        窗口ID 为 做 Nova原生桌面_查找窗口ID_按URL 用 参数1。
        若 窗口ID 大于 0：
            做 Nova原生桌面_聚焦窗口 用 窗口ID。
            返还 「focus-url: 」 加 参数1。
        完。
        返还 「focus-url-miss: 」 加 参数1。
    完。
    窗口索引 为 做 Nova原生桌面_AI页面_获取焦点窗口索引。
    若 窗口索引 小于 0：
        返还 「no-focused-window」。
    完。
    若 动作 等于 「navigate-current」：
        若 做 Nova原生桌面_AI页面_导航当前窗口 用 窗口索引、参数1、参数1：
            返还 「navigate-current: 」 加 参数1。
        完。
        返还 「navigate-current-failed: 」 加 参数1。
    完。
    若 非 做 Nova原生桌面_AI页面_确保窗口上下文_按索引 用 窗口索引：
        返还 「page-context-unavailable」。
    完。
    文档 为 桌面 的 窗口列表 的 第 窗口索引 项 的 页面DOM。
    若 文档 等于 空：
        返还 「page-dom-unavailable」。
    完。
    若 动作 等于 「click-id」：
        节点 为 做 DOM_通过ID查找 用 文档、参数1。
        若 节点 等于 空：
            返还 「click-id-miss: 」 加 参数1。
        完。
        href 为 做 DOM_获取属性 用 文档、节点 的 ID、「href」。
        若 做 字符串长度 用 href 大于 0：
            可变 目标标题 为 节点 的 文本内容。
            若 做 字符串长度 用 目标标题 等于 0：
                目标标题 为 href。
            完。
            做 Nova原生桌面_AI页面_导航当前窗口 用 窗口索引、href、目标标题。
            返还 「click-id: 」 加 参数1 加 「 -> 」 加 href。
        完。
        做 DOM_设置属性 用 文档、节点 的 ID、「data-ai-clicked」、「1」。
        桌面 的 窗口列表 的 第 窗口索引 项 的 页面最后动作 为 「click-id:」 加 参数1。
        返还 「click-id: 」 加 参数1。
    完。
    若 动作 等于 「set-input-id」：
        节点 为 做 DOM_通过ID查找 用 文档、参数1。
        若 节点 等于 空：
            返还 「set-input-id-miss: 」 加 参数1。
        完。
        若 节点 的 标签名 不等于 「input」 并且 节点 的 标签名 不等于 「textarea」：
            返还 「set-input-id-unsupported: 」 加 参数1。
        完。
        做 DOM_设置属性 用 文档、节点 的 ID、「value」、参数2。
        桌面 的 窗口列表 的 第 窗口索引 项 的 页面最后动作 为 「set-input-id:」 加 参数1。
        返还 「set-input-id: 」 加 参数1。
    完。
    返还 「unsupported-page-action: 」 加 动作。
完。

本章 收束。
