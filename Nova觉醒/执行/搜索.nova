《搜索》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内核打印》。

引用《..·标准·原生字符串》。

引用《..·标准·原生正则表达》。

引用《..·标准·文件》。

引用《..·记忆·VFS读写同步》。

引用《..·中枢·整数转字符串》。

GREP_忽略大小写 为 1。

GREP_行号 为 2。

GREP_计数 为 4。

GREP_反转 为 8。

GREP_递归 为 16。

GREP_全词 为 32。

GREP_仅文件名 为 64。

GREP_正则 为 128。

GREP_静默 为 256。

Grep配置 是 一种 事物，含 选项、模式、文件列表、前上下文、后上下文、最大匹配、包含模式、排除模式。

Grep统计 是 一种 事物，含 搜索文件数、匹配文件数、匹配行数、扫描行数、递归目录数。

可变 全局_grep统计 为 空。

grep统计_初始化 接受 零参：
    若 全局_grep统计 等于 空：
        全局_grep统计 为 新建 Grep统计。
        全局_grep统计 的 搜索文件数 为 0。
        全局_grep统计 的 匹配文件数 为 0。
        全局_grep统计 的 匹配行数 为 0。
        全局_grep统计 的 扫描行数 为 0。
        全局_grep统计 的 递归目录数 为 0。
    完。
完。

grep_解析参数 接受 参数：
    配置 为 新建 Grep配置。
    配置 的 选项 为 0。
    配置 的 模式 为 「」。
    配置 的 文件列表 为 一组：（空）。
    配置 的 前上下文 为 0。
    配置 的 后上下文 为 0。
    配置 的 最大匹配 为 0。
    配置 的 包含模式 为 「」。
    配置 的 排除模式 为 「」。
    可变 模式已设 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        a 为 参数 的 第 i 项。
        若 a 等于 「-i」：
            配置 的 选项 为 配置 的 选项 位或 GREP_忽略大小写。
        否则若 a 等于 「-n」：
            配置 的 选项 为 配置 的 选项 位或 GREP_行号。
        否则若 a 等于 「-c」：
            配置 的 选项 为 配置 的 选项 位或 GREP_计数。
        否则若 a 等于 「-v」：
            配置 的 选项 为 配置 的 选项 位或 GREP_反转。
        否则若 a 等于 「-r」 或者 a 等于 「-R」：
            配置 的 选项 为 配置 的 选项 位或 GREP_递归。
        否则若 a 等于 「-w」：
            配置 的 选项 为 配置 的 选项 位或 GREP_全词。
        否则若 a 等于 「-l」：
            配置 的 选项 为 配置 的 选项 位或 GREP_仅文件名。
        否则若 a 等于 「-E」：
            配置 的 选项 为 配置 的 选项 位或 GREP_正则。
        否则若 a 等于 「-q」：
            配置 的 选项 为 配置 的 选项 位或 GREP_静默。
        否则若 a 等于 「-A」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 后上下文 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-B」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 前上下文 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-C」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            上下文 为 做 字符串转整数 用 参数 的 第 i 项。
            配置 的 前上下文 为 上下文。
            配置 的 后上下文 为 上下文。
        否则若 a 等于 「-m」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 最大匹配 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「--include」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 包含模式 为 参数 的 第 i 项。
        否则若 a 等于 「--exclude」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 排除模式 为 参数 的 第 i 项。
        否则若 非 模式已设：
            配置 的 模式 为 a。
            模式已设 为 真。
        否则：
            做 追加 用 配置 的 文件列表、a。
        完。
        i 为 i 加 1。
    完。
    若 非 模式已设：
        配置 的 模式 为 「」。
    完。
    返还 配置。
完。

grep_main 接受 参数：
    做 grep统计_初始化。
    配置 为 做 grep_解析参数 用 参数。
    若 配置 的 模式 等于 「」：
        做 打印行 用 「用法: grep [选项] 模式 [文件...]」。
        做 打印行 用 「  -i 忽略大小写  -n 行号  -c 计数  -v 反转匹配」。
        做 打印行 用 「  -r 递归搜索  -w 全词匹配  -l 仅文件名  -E 正则模式」。
        做 打印行 用 「  -A N 后上下文  -B N 前上下文  -C N 上下文」。
        做 打印行 用 「  -m N 最大匹配数  -q 静默模式」。
        做 打印行 用 「  --include 模式  --exclude 模式」。
        返还 1。
    完。
    若 做 计数 用 配置 的 文件列表 等于 0：
        做 追加 用 配置 的 文件列表、「-」。
    完。
    若 配置 的 选项 位与 GREP_递归 不等于 0：
        展开列表 为 一组：（空）。
        可变 k 为 0。
        只要 k 小于 做 计数 用 配置 的 文件列表：
            路径 为 配置 的 文件列表 的 第 k 项。
            若 做 文件_是目录 用 路径：
                全局_grep统计 的 递归目录数 为 全局_grep统计 的 递归目录数 加 1。
                做 grep_展开目录 用 路径、展开列表、配置。
            否则：
                做 追加 用 展开列表、路径。
            完。
            k 为 k 加 1。
        完。
        配置 的 文件列表 为 展开列表。
    完。
    可变 总匹配 为 0。
    多文件 为 做 计数 用 配置 的 文件列表 大于 1。
    可变 k 为 0。
    只要 k 小于 做 计数 用 配置 的 文件列表：
        路径 为 配置 的 文件列表 的 第 k 项。
        若 非 做 grep_文件匹配过滤 用 路径、配置：
            k 为 k 加 1。
            继续。
        完。
        文件匹配数 为 做 grep_搜索文件 用 路径、配置、多文件。
        总匹配 为 总匹配 加 文件匹配数。
        若 文件匹配数 大于 0：
            全局_grep统计 的 匹配文件数 为 全局_grep统计 的 匹配文件数 加 1。
        完。
        k 为 k 加 1。
    完。
    若 总匹配 大于 0：
        返还 0。
    完。
    返还 1。
完。

grep_搜索文件 接受 路径、配置、多文件：
    全局_grep统计 的 搜索文件数 为 全局_grep统计 的 搜索文件数 加 1。
    内容 为 做 文件_读取全部 用 路径。
    若 内容 等于 空：
        返还 0。
    完。
    行列表 为 做 字符串分割 用 内容、「\n」。
    行数 为 做 计数 用 行列表。
    全局_grep统计 的 扫描行数 为 全局_grep统计 的 扫描行数 加 行数。
    可变 文件匹配数 为 0。
    可变 上次打印行 为 负 1。
    可变 j 为 0。
    只要 j 小于 行数：
        若 配置 的 最大匹配 大于 0 并且 文件匹配数 大于或等于 配置 的 最大匹配：
            跳出。
        完。
        行 为 行列表 的 第 j 项。
        匹配 为 做 grep_匹配行 用 行、配置 的 模式、配置 的 选项。
        若 配置 的 选项 位与 GREP_反转 不等于 0：
            匹配 为 非 匹配。
        完。
        若 匹配：
            文件匹配数 为 文件匹配数 加 1。
            全局_grep统计 的 匹配行数 为 全局_grep统计 的 匹配行数 加 1。
            若 配置 的 选项 位与 GREP_静默 不等于 0：
                j 为 j 加 1。
                继续。
            完。
            若 配置 的 选项 位与 GREP_仅文件名 不等于 0：
                做 打印行 用 路径。
                返还 文件匹配数。
            完。
            若 配置 的 选项 位与 GREP_计数 等于 0：
                若 配置 的 前上下文 大于 0：
                    可变 开始 为 j 减 配置 的 前上下文。
                    若 开始 小于 0：
                        开始 为 0。
                    完。
                    若 开始 小于或等于 上次打印行：
                        开始 为 上次打印行 加 1。
                    完。
                    若 开始 小于 j 并且 上次打印行 大于或等于 0 并且 开始 大于 上次打印行 加 1：
                        做 打印行 用 「--」。
                    完。
                    可变 b 为 开始。
                    只要 b 小于 j：
                        做 grep_输出行 用 路径、b、行列表 的 第 b 项、多文件、配置 的 选项、假。
                        b 为 b 加 1。
                    完。
                完。
                做 grep_输出行 用 路径、j、行、多文件、配置 的 选项、真。
                上次打印行 为 j。
                若 配置 的 后上下文 大于 0：
                    可变 结束 为 j 加 配置 的 后上下文。
                    若 结束 大于或等于 行数：
                        结束 为 行数 减 1。
                    完。
                    可变 a 为 j 加 1。
                    只要 a 小于或等于 结束：
                        做 grep_输出行 用 路径、a、行列表 的 第 a 项、多文件、配置 的 选项、假。
                        上次打印行 为 a。
                        a 为 a 加 1。
                    完。
                完。
            完。
        完。
        j 为 j 加 1。
    完。
    若 配置 的 选项 位与 GREP_计数 不等于 0 并且 配置 的 选项 位与 GREP_静默 等于 0：
        若 多文件：
            做 打印 用 做 字符串拼接 用 路径、「:」。
        完。
        做 打印行 用 做 整数转字符串 用 文件匹配数。
    完。
    返还 文件匹配数。
完。

grep_输出行 接受 路径、行号、行内容、多文件、选项、是匹配：
    可变 前缀 为 「」。
    若 多文件：
        前缀 为 做 字符串拼接 用 路径、「:」。
    完。
    若 选项 位与 GREP_行号 不等于 0：
        分隔 为 「:」。
        若 非 是匹配：
            分隔 为 「-」。
        完。
        前缀 为 做 字符串拼接 用 前缀、做 字符串拼接 用 做 整数转字符串 用 行号 加 1、分隔。
    完。
    做 打印行 用 做 字符串拼接 用 前缀、行内容。
完。

grep_匹配行 接受 行、模式、选项：
    可变 搜索行 为 行。
    可变 搜索模式 为 模式。
    若 选项 位与 GREP_忽略大小写 不等于 0：
        搜索行 为 做 字符串转小写 用 行。
        搜索模式 为 做 字符串转小写 用 模式。
    完。
    若 选项 位与 GREP_正则 不等于 0：
        返还 做 正则_包含 用 搜索行、搜索模式。
    完。
    若 选项 位与 GREP_全词 不等于 0：
        返还 做 grep_全词匹配 用 搜索行、搜索模式。
    完。
    返还 做 字符串包含 用 搜索行、搜索模式。
完。

grep_全词匹配 接受 行、模式：
    模式长 为 做 字符串长度 用 模式。
    行长 为 做 字符串长度 用 行。
    若 模式长 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于或等于 行长 减 模式长：
        子串 为 做 字符串子串 用 行、i、i 加 模式长。
        若 子串 等于 模式：
            前OK 为 i 等于 0 或者 非 做 grep_是单词字符 用 做 字符串字符 用 行、i 减 1。
            后OK 为 i 加 模式长 大于或等于 行长 或者 非 做 grep_是单词字符 用 做 字符串字符 用 行、i 加 模式长。
            若 前OK 并且 后OK：
                返还 真。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

grep_是单词字符 接受 ch：
    返还 ch 大于或等于 65 并且 ch 小于或等于 90 或者 ch 大于或等于 97 并且 ch 小于或等于 122 或者 ch 大于或等于 48 并且 ch 小于或等于 57 或者 ch 等于 95。
完。

grep_展开目录 接受 目录、结果列表、配置：
    条目列表 为 做 文件_列出目录 用 目录。
    若 条目列表 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目列表：
        条目 为 条目列表 的 第 i 项。
        完整路径 为 做 字符串拼接 用 目录、做 字符串拼接 用 「/」、条目。
        若 做 文件_是目录 用 完整路径：
            全局_grep统计 的 递归目录数 为 全局_grep统计 的 递归目录数 加 1。
            做 grep_展开目录 用 完整路径、结果列表、配置。
        否则：
            做 追加 用 结果列表、完整路径。
        完。
        i 为 i 加 1。
    完。
完。

grep_文件匹配过滤 接受 路径、配置：
    若 配置 的 包含模式 不等于 「」：
        若 非 做 grep_简单通配符 用 路径、配置 的 包含模式：
            返还 假。
        完。
    完。
    若 配置 的 排除模式 不等于 「」：
        若 做 grep_简单通配符 用 路径、配置 的 排除模式：
            返还 假。
        完。
    完。
    返还 真。
完。

grep_简单通配符 接受 文件名、模式：
    若 做 字符串长度 用 模式 大于 1 并且 做 字符串字符 用 模式、0 等于 42：
        后缀 为 做 字符串子串 用 模式、1、做 字符串长度 用 模式。
        返还 做 字符串结尾 用 文件名、后缀。
    完。
    返还 做 字符串包含 用 文件名、模式。
完。

grep_打印统计 接受 零参：
    做 grep统计_初始化。
    做 打印行 用 「=== grep统计 ===」。
    做 打印行 用 「搜索文件数: 」 加 做 整数转字符串 用 全局_grep统计 的 搜索文件数。
    做 打印行 用 「匹配文件数: 」 加 做 整数转字符串 用 全局_grep统计 的 匹配文件数。
    做 打印行 用 「匹配行数: 」 加 做 整数转字符串 用 全局_grep统计 的 匹配行数。
    做 打印行 用 「扫描行数: 」 加 做 整数转字符串 用 全局_grep统计 的 扫描行数。
    做 打印行 用 「递归目录数: 」 加 做 整数转字符串 用 全局_grep统计 的 递归目录数。
完。

可变 AI_搜索_热文件表 为 一组：（空）。

AI_搜索_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_搜索_热文件表：
        若 AI_搜索_热文件表 的 第 i 项 的 路径 等于 路径：
            AI_搜索_热文件表 的 第 i 项 的 频率 为 AI_搜索_热文件表 的 第 i 项 的 频率 加 1。
            返还 AI_搜索_热文件表 的 第 i 项 的 频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_搜索_热文件表 小于 128：
        做 追加 用 AI_搜索_热文件表、一本字典：路径 对应 路径、频率 对应 1。
    完。
    返还 1。
完。

AI_搜索_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
        返还 0。
    完。
    返还 32768。
完。

本章 收束。
