《节点操作》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

引用《.·标签处理》。

引用《.·文档初始化》。

DOM_创建节点 接受 文档、类型、标签名、标签ID：
    节点 为 新建 DOM节点。
    节点 的 ID 为 文档 的 下一个ID。
    文档 的 下一个ID 为 文档 的 下一个ID 加 1。
    节点 的 类型 为 类型。
    节点 的 标签名 为 标签名。
    节点 的 标签ID 为 标签ID。
    节点 的 文本内容 为 「」。
    节点 的 属性 为 一组：（空）。
    节点 的 子节点 为 一组：（空）。
    节点 的 父节点 为 0。
    节点 的 下一兄弟 为 0。
    节点 的 样式 为 一组：（空）。
    节点 的 事件 为 一组：（空）。
    节点 的 脏 为 真。
    做 追加 用 文档 的 节点池、节点。
    文档 的 总节点数 为 文档 的 总节点数 加 1。
    返还 节点。
完。

DOM_添加子节点 接受 文档、父ID、子节点：
    父 为 做 DOM_查找节点 用 文档、父ID。
    若 父 等于 空：
        返还。
    完。
    子节点 的 父节点 为 父ID。
    若 做 计数 用 父 的 子节点 大于 0：
        最后子ID 为 父 的 子节点 的 第 做 计数 用 父 的 子节点 减 1 项。
        最后子 为 做 DOM_查找节点 用 文档、最后子ID。
        若 最后子 不等于 空：
            最后子 的 下一兄弟 为 子节点 的 ID。
        完。
    完。
    做 追加 用 父 的 子节点、子节点 的 ID。
    父 的 脏 为 真。
完。

DOM_查找节点 接受 文档、ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档 的 节点池：
        若 文档 的 节点池 的 第 i 项 的 ID 等于 ID：
            返还 文档 的 节点池 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

DOM_设置属性 接受 文档、节点ID、键、值：
    节点 为 做 DOM_查找节点 用 文档、节点ID。
    若 节点 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点 的 属性：
        若 节点 的 属性 的 第 i 项 的 第 0 项 等于 键：
            节点 的 属性 的 第 i 项 的 第 1 项 为 值。
            节点 的 脏 为 真。
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 节点 的 属性、一组：键、值。
    节点 的 脏 为 真。
完。

DOM_获取属性 接受 文档、节点ID、键：
    节点 为 做 DOM_查找节点 用 文档、节点ID。
    若 节点 等于 空：
        返还 「」。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点 的 属性：
        若 节点 的 属性 的 第 i 项 的 第 0 项 等于 键：
            返还 节点 的 属性 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

DOM_解析HTML 接受 html、url：
    文档 为 做 DOM_创建文档 用 url。
    可变 状态 为 解析_数据。
    可变 位置 为 0。
    可变 文本缓冲 为 「」。
    可变 标签缓冲 为 「」。
    可变 属性名缓冲 为 「」。
    可变 属性值缓冲 为 「」。
    可变 当前属性 为 一组：（空）。
    可变 是闭合 为 假。
    可变 栈 为 一组：（空）。
    长度 为 做 字符串长度 用 html。
    html节点 为 做 DOM_创建节点 用 文档、节点_元素、「html」、标签_HTML。
    做 DOM_添加子节点 用 文档、文档 的 根节点、html节点。
    文档 的 根节点 为 html节点 的 ID。
    做 追加 用 栈、html节点 的 ID。
    只要 位置 小于 长度：
        c 为 做 字符串子串 用 html、位置、1。
        若 状态 等于 解析_数据：
            若 c 等于 「<」：
                若 做 字符串长度 用 文本缓冲 大于 0：
                    文本节点 为 做 DOM_创建节点 用 文档、节点_文本、「#text」、标签_UNKNOWN。
                    文本节点 的 文本内容 为 文本缓冲。
                    若 做 计数 用 栈 大于 0：
                        做 DOM_添加子节点 用 文档、栈 的 第 做 计数 用 栈 减 1 项、文本节点。
                    完。
                    文本缓冲 为 「」。
                完。
                状态 为 解析_标签开始。
                标签缓冲 为 「」。
                当前属性 为 一组：（空）。
                是闭合 为 假。
            否则：
                文本缓冲 为 文本缓冲 加 c。
            完。
        否则若 状态 等于 解析_标签开始：
            若 c 等于 「/」：
                是闭合 为 真。
                状态 为 解析_标签名。
            否则若 c 等于 「!」：
                状态 为 解析_注释。
            否则：
                标签缓冲 为 c。
                状态 为 解析_标签名。
            完。
        否则若 状态 等于 解析_标签名：
            若 c 等于 「 」 或者 c 等于 「\t」 或者 c 等于 「\n」：
                状态 为 解析_属性名。
                属性名缓冲 为 「」。
            否则若 c 等于 「>」：
                做 DOM_处理标签 用 文档、标签缓冲、当前属性、是闭合、栈。
                状态 为 解析_数据。
                若 非 是闭合 并且 标签缓冲 等于 「script」：
                    状态 为 解析_脚本。
                完。
                若 非 是闭合 并且 标签缓冲 等于 「style」：
                    状态 为 解析_样式。
                完。
                文本缓冲 为 「」。
            否则若 c 等于 「/」：
            否则：
                标签缓冲 为 标签缓冲 加 c。
            完。
        否则若 状态 等于 解析_属性名：
            若 c 等于 「=」：
                状态 为 解析_属性值。
                属性值缓冲 为 「」。
            否则若 c 等于 「 」 或者 c 等于 「\t」：
                若 做 字符串长度 用 属性名缓冲 大于 0：
                    做 追加 用 当前属性、一组：属性名缓冲、「」。
                    属性名缓冲 为 「」。
                完。
            否则若 c 等于 「>」：
                若 做 字符串长度 用 属性名缓冲 大于 0：
                    做 追加 用 当前属性、一组：属性名缓冲、「」。
                完。
                做 DOM_处理标签 用 文档、标签缓冲、当前属性、是闭合、栈。
                状态 为 解析_数据。
                文本缓冲 为 「」。
            否则若 c 等于 「/」：
            否则：
                属性名缓冲 为 属性名缓冲 加 c。
            完。
        否则若 状态 等于 解析_属性值：
            若 c 等于 「\"」 或者 c 等于 「'」：
                引号 为 c。
                位置 为 位置 加 1。
                只要 位置 小于 长度：
                    vc 为 做 字符串子串 用 html、位置、1。
                    若 vc 等于 引号：
                        中断。
                    完。
                    属性值缓冲 为 属性值缓冲 加 vc。
                    位置 为 位置 加 1。
                完。
                做 追加 用 当前属性、一组：属性名缓冲、属性值缓冲。
                属性名缓冲 为 「」。
                状态 为 解析_属性名。
            否则若 c 等于 「 」 或者 c 等于 「>」：
                做 追加 用 当前属性、一组：属性名缓冲、属性值缓冲。
                属性名缓冲 为 「」。
                若 c 等于 「>」：
                    做 DOM_处理标签 用 文档、标签缓冲、当前属性、是闭合、栈。
                    状态 为 解析_数据。
                    文本缓冲 为 「」。
                否则：
                    状态 为 解析_属性名。
                完。
            否则：
                属性值缓冲 为 属性值缓冲 加 c。
            完。
        否则若 状态 等于 解析_注释：
            若 c 等于 「>」：
                状态 为 解析_数据。
                文本缓冲 为 「」。
            完。
        否则若 状态 等于 解析_脚本：
            文本缓冲 为 文本缓冲 加 c。
            若 做 字符串查找 用 文本缓冲、「</script>」 大于或等于 0：
                脚本内容 为 做 字符串子串 用 文本缓冲、0、做 字符串长度 用 文本缓冲 减 9。
                做 追加 用 文档 的 脚本列表、脚本内容。
                文本缓冲 为 「」。
                状态 为 解析_数据。
                若 做 计数 用 栈 大于 0：
                    顶 为 做 DOM_查找节点 用 文档、栈 的 第 做 计数 用 栈 减 1 项。
                    若 顶 不等于 空 并且 顶 的 标签ID 等于 标签_SCRIPT：
                        做 列表_移除 用 栈、做 计数 用 栈 减 1。
                    完。
                完。
            完。
        否则若 状态 等于 解析_样式：
            文本缓冲 为 文本缓冲 加 c。
            若 做 字符串查找 用 文本缓冲、「</style>」 大于或等于 0：
                样式内容 为 做 字符串子串 用 文本缓冲、0、做 字符串长度 用 文本缓冲 减 8。
                做 追加 用 文档 的 样式列表、样式内容。
                文本缓冲 为 「」。
                状态 为 解析_数据。
                若 做 计数 用 栈 大于 0：
                    顶 为 做 DOM_查找节点 用 文档、栈 的 第 做 计数 用 栈 减 1 项。
                    若 顶 不等于 空 并且 顶 的 标签ID 等于 标签_STYLE：
                        做 列表_移除 用 栈、做 计数 用 栈 减 1。
                    完。
                完。
            完。
        完。
        位置 为 位置 加 1。
    完。
    返还 文档。
完。

可变 AI_节点操作_热节点缓存 为 0。

AI_节点操作_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_节点操作_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_节点操作_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

本章 收束。
