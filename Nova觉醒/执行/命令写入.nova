《命令写入》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·感知·内核直连层》。

引用《.·平台时间》。

引用《..·推理·定点Q算术》。

引用《..·中枢·内容可见性》。

图处_写命令 接受 ctx、类型、参数1、参数2、参数3、参数4：
    若 ctx 等于 0：
        返还。
    完。
    若 ctx 的 写位置 加 24 大于 ctx 的 缓冲大小：
        做 图处_提交 用 ctx。
    完。
    偏移 为 ctx 的 命令缓冲 加 ctx 的 写位置。
    做 内存_写32 用 偏移、类型。
    做 内存_写32 用 偏移 加 4、参数1。
    做 内存_写32 用 偏移 加 8、参数2。
    做 内存_写32 用 偏移 加 12、参数3。
    做 内存_写32 用 偏移 加 16、参数4。
    做 内存_写32 用 偏移 加 20、0。
    ctx 的 写位置 为 ctx 的 写位置 加 24。
完。

图处_清屏 接受 ctx、R、G、B、A：
    做 图处_写命令 用 ctx、图处命令_清屏、R、G、B、A。
完。

图处_绘制矩形 接受 ctx、x、y、w、h、颜色RGBA：
    做 图处_写命令 用 ctx、图处命令_绘制矩形、x 位或 y 左移 16、w 位或 h 左移 16、颜色RGBA、0。
完。

图处_绘制纹理矩形 接受 ctx、纹理ID、x、y、w、h：
    做 图处_写命令 用 ctx、图处命令_绘制纹理、纹理ID、x 位或 y 左移 16、w 位或 h 左移 16、0。
完。

图处_设置裁剪 接受 ctx、x、y、w、h：
    做 图处_写命令 用 ctx、图处命令_设置裁剪、x、y、w、h。
完。

图处_设置变换 接受 ctx、a、b、c、d、tx、ty：
    做 图处_写命令 用 ctx、图处命令_设置变换、a 位或 b 左移 16、c 位或 d 左移 16、tx、ty。
完。

图处_设置透明度 接受 ctx、透明度_0_255：
    做 图处_写命令 用 ctx、图处命令_设置透明度、透明度_0_255、0、0、0。
完。

图处_创建纹理 接受 ctx、宽、高、格式：
    若 ctx 等于 0 或者 ctx 的 纹理数 大于或等于 256：
        返还 0。
    完。
    tex_id 为 做 系统调用 用 系统调用_图处_创建纹理、ctx 的 ctx_id、宽、高、格式、0。
    若 tex_id 大于 0：
        ctx 的 纹理表 的 第 ctx 的 纹理数 项 为 tex_id。
        ctx 的 纹理数 为 ctx 的 纹理数 加 1。
    完。
    返还 tex_id。
完。

图处_上传纹理 接受 ctx、纹理ID、数据、字节数：
    若 ctx 等于 0：
        返还 负 1。
    完。
    返还 做 系统调用 用 系统调用_图处_上传纹理、ctx 的 ctx_id、纹理ID、数据、字节数、0。
完。

图处_销毁纹理 接受 ctx、纹理ID：
    若 ctx 等于 0：
        返还。
    完。
    做 系统调用 用 系统调用_图处_销毁纹理、ctx 的 ctx_id、纹理ID、0、0、0。
完。

图处_提交 接受 ctx：
    若 ctx 等于 0 或者 ctx 的 写位置 等于 0：
        返还。
    完。
    做 系统调用 用 系统调用_图处_提交命令、ctx 的 ctx_id、ctx 的 命令缓冲、ctx 的 写位置、0、0。
    ctx 的 写位置 为 0。
    ctx 的 上次提交时间 为 做 平台_获取高精度时间。
完。

图处_帧结束 接受 ctx：
    若 ctx 等于 0：
        返还。
    完。
    做 图处_提交 用 ctx。
    做 系统调用 用 系统调用_图处_交换缓冲、ctx 的 ctx_id、0、0、0、0。
    ctx 的 帧数 为 ctx 的 帧数 加 1。
    ctx 的 图处繁忙度 为 做 系统调用 用 系统调用_图处_查询状态、ctx 的 ctx_id、0、0、0、0。
完。

图处命令_清屏 为 0x01。

图处命令_绘制矩形 为 0x02。

图处命令_绘制纹理 为 0x03。

图处命令_设置裁剪 为 0x04。

图处命令_设置变换 为 0x05。

图处命令_设置透明度 为 0x06。

图处命令_绘制三角 为 0x07。

图处命令_绘制线段 为 0x08。

图处命令_混合模式 为 0x09。

图处命令_模板测试 为 0x0A。

图处命令_深度测试 为 0x0B。

图处命令_栅栏同步 为 0x0C。

图处命令_缓冲屏障 为 0x0D。

图处命令_绑定渲染目标 为 0x0E。

图处命令_解绑渲染目标 为 0x0F。

NGCB_默认缓冲大小 为 65536。

NGCB_最大缓冲大小 为 1048576。

NGCB_命令宽度 为 24。

NGCB_环形缓冲 是 一种 事物，含 数据 为 0、容量 为 0、写头 为 0、读头 为 0、命令数 为 0、已满标志 为 假。

NGCB_环形创建 接受 容量：
    若 容量 小于 NGCB_默认缓冲大小：
        容量 为 NGCB_默认缓冲大小。
    完。
    若 容量 大于 NGCB_最大缓冲大小：
        容量 为 NGCB_最大缓冲大小。
    完。
    rb 为 做 堆_分配 用 NGCB_环形缓冲。
    rb 的 数据 为 做 堆_分配_数组 用 容量。
    rb 的 容量 为 容量。
    rb 的 写头 为 0。
    rb 的 读头 为 0。
    rb 的 命令数 为 0。
    rb 的 已满标志 为 假。
    做 内存_填零 用 rb 的 数据、容量。
    返还 rb。
完。

NGCB_环形可用空间 接受 rb：
    若 rb 的 写头 大于或等于 rb 的 读头：
        返还 rb 的 容量 减 (rb 的 写头 减 rb 的 读头)。
    否则：
        返还 rb 的 读头 减 rb 的 写头。
    完。
完。

NGCB_环形写入 接受 rb、数据、字节数：
    若 做 NGCB_环形可用空间 用 rb 小于 字节数：
        rb 的 已满标志 为 真。
        返还 负 1。
    完。
    尾部剩余 为 rb 的 容量 减 rb 的 写头。
    若 尾部剩余 大于或等于 字节数：
        做 内存_复制 用 rb 的 数据 加 rb 的 写头、数据、字节数。
        rb 的 写头 为 rb 的 写头 加 字节数。
        若 rb 的 写头 等于 rb 的 容量：
            rb 的 写头 为 0。
        完。
    否则：
        做 内存_复制 用 rb 的 数据 加 rb 的 写头、数据、尾部剩余。
        做 内存_复制 用 rb 的 数据、数据 加 尾部剩余、字节数 减 尾部剩余。
        rb 的 写头 为 字节数 减 尾部剩余。
    完。
    rb 的 命令数 为 rb 的 命令数 加 1。
    返还 0。
完。

NGCB_环形重置 接受 rb：
    rb 的 写头 为 0。
    rb 的 读头 为 0。
    rb 的 命令数 为 0。
    rb 的 已满标志 为 假。
完。

NGCB_环形销毁 接受 rb：
    若 rb 等于 0：
        返还。
    完。
    若 rb 的 数据 不等于 0：
        做 堆_释放 用 rb 的 数据。
    完。
    做 堆_释放 用 rb。
完。

NGCB_最大批次 为 256。

NGCB_批次条目 是 一种 事物，含 命令类型 为 0、起始偏移 为 0、命令数量 为 0、纹理ID 为 0。

可变 NGCB_批次表 为 0。

可变 NGCB_批次数 为 0。

NGCB_批次初始化 接受 零参：
    NGCB_批次表 为 做 堆_分配_数组 用 做 大小为 用 NGCB_批次条目 乘 NGCB_最大批次。
    NGCB_批次数 为 0。
完。

NGCB_尝试合并 接受 ctx、类型、纹理：
    若 NGCB_批次数 大于 0：
        上一个 为 NGCB_批次表 的 第 NGCB_批次数 减 1 项。
        若 上一个 的 命令类型 等于 类型 并且 上一个 的 纹理ID 等于 纹理：
            上一个 的 命令数量 为 上一个 的 命令数量 加 1。
            返还 真。
        完。
    完。
    若 NGCB_批次数 大于或等于 NGCB_最大批次：
        返还 假。
    完。
    idx 为 NGCB_批次数。
    NGCB_批次表 的 第 idx 项 的 命令类型 为 类型。
    NGCB_批次表 的 第 idx 项 的 起始偏移 为 ctx 的 写位置。
    NGCB_批次表 的 第 idx 项 的 命令数量 为 1。
    NGCB_批次表 的 第 idx 项 的 纹理ID 为 纹理。
    NGCB_批次数 为 NGCB_批次数 加 1。
    返还 假。
完。

NGCB_获取批次数 接受 零参：
    返还 NGCB_批次数。
完。

NGCB_重置批次 接受 零参：
    NGCB_批次数 为 0。
完。

NGCB_状态缓存 是 一种 事物，含 当前裁剪_x 为 负 1、当前裁剪_y 为 负 1、当前裁剪_w 为 负 1、当前裁剪_h 为 负 1、当前变换_a 为 0、当前变换_b 为 0、当前变换_c 为 0、当前变换_d 为 0、当前变换_tx 为 0、当前变换_ty 为 0、当前透明度 为 255、当前混合模式 为 0、当前渲染目标 为 0、已初始化 为 假。

可变 NGCB_状态 为 0。

可变 NGCB_跳过状态数 为 0。

NGCB_状态初始化 接受 零参：
    NGCB_状态 为 做 堆_分配 用 NGCB_状态缓存。
    NGCB_状态 的 已初始化 为 假。
    NGCB_跳过状态数 为 0。
完。

NGCB_检查裁剪变更 接受 ctx、x、y、w、h：
    若 NGCB_状态 的 已初始化 等于 真：
        若 NGCB_状态 的 当前裁剪_x 等于 x 并且 NGCB_状态 的 当前裁剪_y 等于 y 并且 NGCB_状态 的 当前裁剪_w 等于 w 并且 NGCB_状态 的 当前裁剪_h 等于 h：
            NGCB_跳过状态数 为 NGCB_跳过状态数 加 1。
            返还 假。
        完。
    完。
    NGCB_状态 的 当前裁剪_x 为 x。
    NGCB_状态 的 当前裁剪_y 为 y。
    NGCB_状态 的 当前裁剪_w 为 w。
    NGCB_状态 的 当前裁剪_h 为 h。
    NGCB_状态 的 已初始化 为 真。
    做 图处_设置裁剪 用 ctx、x、y、w、h。
    返还 真。
完。

NGCB_检查透明度变更 接受 ctx、透明度：
    若 NGCB_状态 的 已初始化 等于 真 并且 NGCB_状态 的 当前透明度 等于 透明度：
        NGCB_跳过状态数 为 NGCB_跳过状态数 加 1。
        返还 假。
    完。
    NGCB_状态 的 当前透明度 为 透明度。
    NGCB_状态 的 已初始化 为 真。
    做 图处_设置透明度 用 ctx、透明度。
    返还 真。
完。

NGCB_最大栅栏 为 32。

NGCB_栅栏 是 一种 事物，含 栅栏ID 为 0、帧号 为 0、已信号 为 假、时间戳 为 0。

可变 NGCB_栅栏表 为 0。

可变 NGCB_栅栏数 为 0。

可变 NGCB_下一个栅栏ID 为 1。

NGCB_栅栏初始化 接受 零参：
    NGCB_栅栏表 为 做 堆_分配_数组 用 做 大小为 用 NGCB_栅栏 乘 NGCB_最大栅栏。
    NGCB_栅栏数 为 0。
    NGCB_下一个栅栏ID 为 1。
完。

NGCB_插入栅栏 接受 ctx：
    若 NGCB_栅栏数 大于或等于 NGCB_最大栅栏：
        做 NGCB_回收栅栏。
        若 NGCB_栅栏数 大于或等于 NGCB_最大栅栏：
            返还 负 1。
        完。
    完。
    fid 为 NGCB_下一个栅栏ID。
    NGCB_下一个栅栏ID 为 NGCB_下一个栅栏ID 加 1。
    idx 为 NGCB_栅栏数。
    NGCB_栅栏表 的 第 idx 项 的 栅栏ID 为 fid。
    NGCB_栅栏表 的 第 idx 项 的 帧号 为 ctx 的 帧数。
    NGCB_栅栏表 的 第 idx 项 的 已信号 为 假。
    NGCB_栅栏表 的 第 idx 项 的 时间戳 为 做 平台_获取高精度时间。
    NGCB_栅栏数 为 NGCB_栅栏数 加 1。
    做 图处_写命令 用 ctx、图处命令_栅栏同步、fid、0、0、0。
    返还 fid。
完。

NGCB_等待栅栏 接受 fid：
    可变 i 为 0。
    只要 i 小于 NGCB_栅栏数：
        若 NGCB_栅栏表 的 第 i 项 的 栅栏ID 等于 fid：
            可变 尝试 为 0。
            只要 NGCB_栅栏表 的 第 i 项 的 已信号 等于 假 并且 尝试 小于 100：
                状态 为 做 系统调用 用 系统调用_图处_查询栅栏、fid、0、0、0、0。
                若 状态 等于 1：
                    NGCB_栅栏表 的 第 i 项 的 已信号 为 真。
                完。
                尝试 为 尝试 加 1。
            完。
            返还 NGCB_栅栏表 的 第 i 项 的 已信号。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

NGCB_回收栅栏 接受 零参：
    可变 写位 为 0。
    可变 i 为 0。
    只要 i 小于 NGCB_栅栏数：
        若 NGCB_栅栏表 的 第 i 项 的 已信号 等于 假：
            若 写位 不等于 i：
                NGCB_栅栏表 的 第 写位 项 为 NGCB_栅栏表 的 第 i 项。
            完。
            写位 为 写位 加 1。
        完。
        i 为 i 加 1。
    完。
    NGCB_栅栏数 为 写位。
完。

NGCB_插入屏障 接受 ctx：
    做 图处_写命令 用 ctx、图处命令_缓冲屏障、0、0、0、0。
完。

NGCB_最大渲染目标 为 8。

NGCB_渲染目标 是 一种 事物，含 目标ID 为 0、纹理ID 为 0、宽 为 0、高 为 0、格式 为 0、已绑定 为 假。

可变 NGCB_渲染目标表 为 0。

可变 NGCB_渲染目标数 为 0。

NGCB_渲染目标初始化 接受 零参：
    NGCB_渲染目标表 为 做 堆_分配_数组 用 做 大小为 用 NGCB_渲染目标 乘 NGCB_最大渲染目标。
    NGCB_渲染目标数 为 0。
完。

NGCB_创建渲染目标 接受 ctx、宽、高、格式：
    若 NGCB_渲染目标数 大于或等于 NGCB_最大渲染目标：
        返还 负 1。
    完。
    tex_id 为 做 图处_创建纹理 用 ctx、宽、高、格式。
    若 tex_id 小于或等于 0：
        返还 负 1。
    完。
    idx 为 NGCB_渲染目标数。
    NGCB_渲染目标表 的 第 idx 项 的 目标ID 为 idx 加 1。
    NGCB_渲染目标表 的 第 idx 项 的 纹理ID 为 tex_id。
    NGCB_渲染目标表 的 第 idx 项 的 宽 为 宽。
    NGCB_渲染目标表 的 第 idx 项 的 高 为 高。
    NGCB_渲染目标表 的 第 idx 项 的 格式 为 格式。
    NGCB_渲染目标表 的 第 idx 项 的 已绑定 为 假。
    NGCB_渲染目标数 为 NGCB_渲染目标数 加 1。
    返还 idx 加 1。
完。

NGCB_绑定渲染目标 接受 ctx、目标ID：
    可变 i 为 0。
    只要 i 小于 NGCB_渲染目标数：
        若 NGCB_渲染目标表 的 第 i 项 的 目标ID 等于 目标ID：
            NGCB_渲染目标表 的 第 i 项 的 已绑定 为 真。
            做 图处_写命令 用 ctx、图处命令_绑定渲染目标、NGCB_渲染目标表 的 第 i 项 的 纹理ID、0、0、0。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

NGCB_解绑渲染目标 接受 ctx：
    做 图处_写命令 用 ctx、图处命令_解绑渲染目标、0、0、0、0。
完。

NGCB_录制块 是 一种 事物，含 数据 为 0、字节数 为 0、命令数 为 0、已结束 为 假。

可变 NGCB_当前录制 为 0。

NGCB_开始录制 接受 零参：
    rec 为 做 堆_分配 用 NGCB_录制块。
    rec 的 数据 为 做 堆_分配_数组 用 NGCB_默认缓冲大小。
    rec 的 字节数 为 0。
    rec 的 命令数 为 0。
    rec 的 已结束 为 假。
    NGCB_当前录制 为 rec。
    返还 rec。
完。

NGCB_录制命令 接受 类型、参1、参2、参3、参4：
    若 NGCB_当前录制 等于 0 或者 NGCB_当前录制 的 已结束 等于 真：
        返还。
    完。
    rec 为 NGCB_当前录制。
    若 rec 的 字节数 加 NGCB_命令宽度 大于 NGCB_默认缓冲大小：
        返还。
    完。
    off 为 rec 的 字节数。
    做 内存_写32 用 rec 的 数据 加 off、类型。
    做 内存_写32 用 rec 的 数据 加 off 加 4、参1。
    做 内存_写32 用 rec 的 数据 加 off 加 8、参2。
    做 内存_写32 用 rec 的 数据 加 off 加 12、参3。
    做 内存_写32 用 rec 的 数据 加 off 加 16、参4。
    做 内存_写32 用 rec 的 数据 加 off 加 20、0。
    rec 的 字节数 为 rec 的 字节数 加 NGCB_命令宽度。
    rec 的 命令数 为 rec 的 命令数 加 1。
完。

NGCB_结束录制 接受 零参：
    若 NGCB_当前录制 不等于 0：
        NGCB_当前录制 的 已结束 为 真。
    完。
    result 为 NGCB_当前录制。
    NGCB_当前录制 为 0。
    返还 result。
完。

NGCB_回放 接受 ctx、rec：
    若 ctx 等于 0 或者 rec 等于 0 或者 rec 的 字节数 等于 0：
        返还。
    完。
    若 ctx 的 写位置 加 rec 的 字节数 大于 ctx 的 缓冲大小：
        做 图处_提交 用 ctx。
    完。
    做 内存_复制 用 ctx 的 命令缓冲 加 ctx 的 写位置、rec 的 数据、rec 的 字节数。
    ctx 的 写位置 为 ctx 的 写位置 加 rec 的 字节数。
完。

NGCB_释放录制块 接受 rec：
    若 rec 等于 0：
        返还。
    完。
    若 rec 的 数据 不等于 0：
        做 堆_释放 用 rec 的 数据。
    完。
    做 堆_释放 用 rec。
完。

可变 NGCB_总命令数 为 0。

可变 NGCB_总提交数 为 0。

可变 NGCB_总合并数 为 0。

可变 NGCB_总回放数 为 0。

NGCB_AI提交策略 接受 ctx：
    tensor 为 做 Tensor_创建 用 6。
    占用率 为 ctx 的 写位置 乘 100 除以 ctx 的 缓冲大小。
    做 Tensor_写 用 tensor、0、占用率。
    做 Tensor_写 用 tensor、1、NGCB_批次数。
    做 Tensor_写 用 tensor、2、ctx 的 图处繁忙度。
    做 Tensor_写 用 tensor、3、ctx 的 帧数。
    做 Tensor_写 用 tensor、4、NGCB_总命令数。
    做 Tensor_写 用 tensor、5、NGCB_栅栏数。
    结果 为 做 AI总线_推理 用 tensor、「NGCB_提交策略」。
    做 Tensor_释放 用 tensor。
    若 结果 大于 512：
        做 图处_提交 用 ctx。
        NGCB_总提交数 为 NGCB_总提交数 加 1。
        返还 1。
    完。
    返还 0。
完。

NGCB_AI异常检测 接受 零参：
    tensor 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 tensor、0、NGCB_总命令数。
    做 Tensor_写 用 tensor、1、NGCB_总提交数。
    做 Tensor_写 用 tensor、2、NGCB_总合并数。
    做 Tensor_写 用 tensor、3、NGCB_跳过状态数。
    异常 为 做 AI总线_异常检测 用 tensor、「NGCB_命令异常」。
    做 Tensor_释放 用 tensor。
    返还 异常。
完。

NGCB_全局初始化 接受 零参：
    做 NGCB_批次初始化。
    做 NGCB_状态初始化。
    做 NGCB_栅栏初始化。
    做 NGCB_渲染目标初始化。
    NGCB_总命令数 为 0。
    NGCB_总提交数 为 0。
    NGCB_总合并数 为 0。
    NGCB_总回放数 为 0。
完。

NGCB_获取统计 接受 零参：
    返还 NGCB_总命令数 乘 65536 加 NGCB_总提交数。
完。

NGCB_重置统计 接受 零参：
    NGCB_总命令数 为 0。
    NGCB_总提交数 为 0。
    NGCB_总合并数 为 0。
    NGCB_总回放数 为 0。
    NGCB_跳过状态数 为 0。
完。

本章 收束。
