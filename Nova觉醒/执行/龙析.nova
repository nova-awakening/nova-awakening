《龙析》

※ NovaOS · 龙析 (DragonProf) 性能分析器
※ 功能:
※ §1 函数计时探针: 进入/离开计时+调用计数
※ §2 调用图构建: 调用者→被调用者边+权重
※ §3 热点分析: 多关键字排序 (CPU/调用/最大)
※ §4 火焰图数据生成: 栈采样→折叠→JSON输出
※ §5 内存分析: 堆分配跟踪+分类统计+泄漏检测
※ §6 采样分析器: 定时中断采样栈
※ §7 调用树重建: 从采样→自底向上汇总
※ §8 AI总线: EWMA异常检测 + 智能建议

引用《..·标准·原生字符串》。
引用《..·中枢·硬链接》。
引用《.·控制台输出》。
引用《..·中枢·自动扩容》。
引用《..·中枢·整数转字符串》。
引用《..·中枢·内核打印》。
引用《..·感知·内核直连层》。

※ §1 常量 + 数据结构

最大探针数 为 4096。
最大调用图边 为 8192。
最大采样栈深 为 128。
最大采样数 为 65536。
最大分配跟踪 为 16384。
Q10 为 1024。
Q_ONE 为 1024。

探针条目 是 一种 事物：
    函数名,。
    调用次数,。
    总耗时,
    最大耗时,。
    最小耗时,。
    进入时戳,。
完。

调用图边 是 一种 事物：
    调用者哈希,。
    被调用者哈希,。
    调用次数,。
    总耗时,。
完。

采样帧 是 一种 事物：
    栈深度,。
    栈,
    时间戳,。
完。

分配记录 是 一种 事物：
    地址,。
    大小,。
    分配者,
    时间戳,。
    已释放,。
完。

※ §2 全局状态

可变 分析_条目 为 做 字典
可变 分析_开始时间 为 做 字典
可变 分析_总采样 为 0。
可变 调用图_边表 为 0
可变 调用图_边数 为 0。
可变 采样缓冲 为 0
可变 采样数 为 0。
可变 采样写头 为 0。
可变 分配跟踪_表 为 0
可变 分配跟踪_数 为 0。
可变 当前调用栈 为 []

※ §3 CLI 入口

龙析_main 接受 参数)：
    若 做 计数 用 参数 等于 0：
        做 打印行 用 「龙析 v2.0 — NovaOS性能分析器」。
        做 打印行 用 「用法:」。
        做 打印行 用 「  龙析 report     — 显示性能报告」。
        做 打印行 用 「  龙析 reset      — 重置所有计数器」。
        做 打印行 用 「  龙析 top [N]    — 显示前N热点函数」。
        做 打印行 用 「  龙析 memory     — 内存使用分析」。
        做 打印行 用 「  龙析 callgraph  — 调用图统计」。
        做 打印行 用 「  龙析 flame      — 火焰图数据输出」。
        做 打印行 用 「  龙析 leaks      — 内存泄漏检测」。
        做 打印行 用 「  龙析 ai         — AI智能分析建议」。
        返还 0。
    完。
    命令 为 参数[0]。
    若 命令 等于 「report」：
    否则如果 (命令 等于 「reset」) { 做 龙析_重置 }。
    否则如果 (命令 等于 「top」) ：
        n 为 若 (做 计数 用 参数 大于 1, 做 字符串转整数 用 参数[1], 10)。
        做 龙析_热点 用 n。
    完。
    否则如果 (命令 等于 「memory」) { 做 龙析_内存 }。
    否则如果 (命令 等于 「callgraph」) { 做 龙析_调用图 }。
    否则如果 (命令 等于 「flame」) { 做 龙析_火焰图 }。
    否则如果 (命令 等于 「leaks」) { 做 龙析_泄漏检测 }。
    否则如果 (命令 等于 「ai」) { 做 龙析_AI分析 }。
    否则 { 做 打印行 用 做 字符串拼接 用 「[龙析] 未知命令: 」、命令 }。
    返还 0。
完。

龙析_重置 接受 )：
    分析_条目 为 做 字典。
    分析_开始时间 为 做 字典。
    分析_总采样 为 0。
    调用图_边数 为 0。
    采样数 为 0。
    采样写头 为 0。
    分配跟踪_数 为 0。
    当前调用栈 = []。
    做 打印行 用 「[龙析] 所有计数器已重置」。
完。

※ §4 函数计时探针

分析_进入 接受 函数名)：
    分析_总采样 为 分析_总采样 加 1。
    做 字典设 用 分析_开始时间、函数名、做 获取滴答。
    ※ 压入调用栈
    做 追加 用 当前调用栈、函数名。
完。

分析_离开 接受 函数名)：
    开始 为 做 字典取 用 分析_开始时间、函数名。
    若 开始 等于 空：
    耗时 为 做 获取滴答 减 开始。
    ※ 更新条目
    现有 为 做 字典取 用 分析_条目、函数名。
    若 现有 等于 空：
        做 字典设 用 分析_条目、函数名、[1、耗时、耗时、耗时]
    否则：
        做 设置 用 现有、0、现有[0] 加 1。
        做 设置 用 现有、1、现有[1] 加 耗时。
        若 耗时 大于 现有[2]：
        若 耗时 小于 现有[3]：
    完。
    ※ 更新调用图
    若 做 计数 用 当前调用栈 不小于 2：
        调用者 为 当前调用栈[做 计数 用 当前调用栈 减 2]。
        做 调用图_添加边 用 调用者、函数名、耗时。
    完。
    ※ 弹出调用栈
    若 做 计数 用 当前调用栈 大于 0：
        做 弹出 用 当前调用栈。
    完。
完。

※ §5 调用图

调用图_初始化 接受 )：
    若 调用图_边表 等于 0：
        调用图_边表 为 做 堆_分配 用 最大调用图边 乘 32
        做 内存_置零 用 调用图_边表、最大调用图边 乘 32。
        调用图_边数 为 0。
    完。
完。

调用图_添加边 接受 调用者、被调用者、耗时)：
    做 调用图_初始化。
    ※ 查找已有边
    caller_h 为 做 字符串_哈希 用 调用者。
    callee_h 为 做 字符串_哈希 用 被调用者。
    可变 i 为 0。
    只要 i 小于 调用图_边数：
        off 为 i 乘 32。
        若 做 内存_读64 用 调用图_边表 加 off 等于 caller_h 并且 做 内存_读64 用 调用图_边表 加 off 加 8 等于 callee_h：
            ※ 更新已有边
            旧次 为 做 内存_读64 用 调用图_边表 加 off 加 16。
            做 内存_写64 用 调用图_边表 加 off 加 16、旧次 加 1。
            旧时 为 做 内存_读64 用 调用图_边表 加 off 加 24。
            做 内存_写64 用 调用图_边表 加 off 加 24、旧时 加 耗时。
            返还。
        完。
        i 为 i 加 1。
    完。
    ※ 新边
    若 调用图_边数 小于 最大调用图边：
        off 为 调用图_边数 乘 32。
        做 内存_写64 用 调用图_边表 加 off、caller_h。
        做 内存_写64 用 调用图_边表 加 off 加 8、callee_h。
        做 内存_写64 用 调用图_边表 加 off 加 16、1。
        做 内存_写64 用 调用图_边表 加 off 加 24、耗时。
        调用图_边数 为 调用图_边数 加 1。
    完。
完。

※ §6 采样分析器 (定时中断采样栈)

采样_初始化 接受 )：
    若 采样缓冲 等于 0：
        ※ 每个采样帧: 8(栈深度) + 128*8(栈) + 8(时间戳) = 1040字节
        采样缓冲 为 做 堆_分配 用 最大采样数 乘 1040。
        做 内存_置零 用 采样缓冲、最大采样数 乘 1040。
        采样数 为 0。
        采样写头 为 0。
    完。
完。

采样_记录 接受 )：
    ※ 由定时器中断调用: 记录当前调用栈
    做 采样_初始化。
    off 为 采样写头 乘 1040。
    深度 为 做 计数 用 当前调用栈。
    若 深度 大于 最大采样栈深：
    做 内存_写64 用 采样缓冲 加 off、深度。
    可变 i 为 0。
    只要 i 小于 深度：
        函数名 为 当前调用栈[i]。
        做 内存_写64 用 采样缓冲 加 off 加 8 加 i 乘 8、做 字符串_哈希 用 函数名。
        i 为 i 加 1。
    完。
    做 内存_写64 用 采样缓冲 加 off 加 8 加 128 乘 8、做 获取滴答。
    采样写头 为 (采样写头 加 1) 取余于 最大采样数。
    若 采样数 小于 最大采样数：
完。

※ §7 内存分配跟踪 + 泄漏检测

分配跟踪_初始化 接受 )：
    若 分配跟踪_表 等于 0：
        分配跟踪_表 为 做 堆_分配 用 最大分配跟踪 乘 40
        做 内存_置零 用 分配跟踪_表、最大分配跟踪 乘 40。
        分配跟踪_数 为 0。
    完。
完。

分配跟踪_记录 接受 地址、大小)：
    做 分配跟踪_初始化。
    若 分配跟踪_数 不小于 最大分配跟踪：
    off 为 分配跟踪_数 乘 40。
    做 内存_写64 用 分配跟踪_表 加 off、地址。
    做 内存_写64 用 分配跟踪_表 加 off 加 8、大小。
    ※ 记录分配者(当前调用栈栈顶)
    若 做 计数 用 当前调用栈 大于 0：
        做 内存_写64 用 分配跟踪_表 加 off 加 16、做 字符串_哈希 用 当前调用栈[做 计数 用 当前调用栈 减 1]。
    完。
    做 内存_写64 用 分配跟踪_表 加 off 加 24、做 获取滴答。
    做 内存_写64 用 分配跟踪_表 加 off 加 32、0
    分配跟踪_数 为 分配跟踪_数 加 1。
完。

分配跟踪_标记释放 接受 地址)：
    若 分配跟踪_表 等于 0：
    可变 i 为 分配跟踪_数 减 1。
    只要 i 不小于 0：
        off 为 i 乘 40。
        若 做 内存_读64 用 分配跟踪_表 加 off 等于 地址 并且 做 内存_读64 用 分配跟踪_表 加 off 加 32 等于 0：
            做 内存_写64 用 分配跟踪_表 加 off 加 32、1
            返还。
        完。
        i 为 i 减 1。
    完。
完。

※ §8 报告 + 热点 + 调用图输出

龙析_报告 接受 )：
    做 打印行 用 「╔═════════════════════════════════════════════════════════╗」。
    做 打印行 用 「║         NovaOS 性能分析报告 (DragonProf v2.0)     ║」。
    做 打印行 用 「╠═════════════════════════════════════════════════════════╣」。
    做 打印行 用 做 字符串拼接 用 「  总探针采样: 」、做 整数转字符串 用 分析_总采样。
    做 打印行 用 做 字符串拼接 用 「  调用图边数: 」、做 整数转字符串 用 调用图_边数。
    做 打印行 用 做 字符串拼接 用 「  栈采样数:   」、做 整数转字符串 用 采样数。
    做 打印行 用 做 字符串拼接 用 「  分配跟踪数: 」、做 整数转字符串 用 分配跟踪_数。
    做 打印行 用 「」。
    做 打印行 用 「  函数名              调用    做 总时间 用 us  做 平均 用 us  做 最大 用 us  做 最小 用 us」。
    做 打印行 用 「  ────────────────  ─────  ────────  ────────  ────────  ────────」。
    ※ 遍历字典输出
    键列表 为 做 字典_键列表 用 分析_条目。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        名 为 键列表[i]。
        条 为 做 字典取 用 分析_条目、名。
        若 条 不等于 空：
            次 为 条[0]。
            总 为 条[1]。
            平均 为 若 (次 大于 0, 总 除以 次, 0)。
            做 打印 用 做 字符串拼接 用 「  」、名。
            做 打印 用 做 字符串拼接 用 「  」、做 整数转字符串 用 次。
            做 打印 用 做 字符串拼接 用 「  」、做 整数转字符串 用 总。
            做 打印 用 做 字符串拼接 用 「  」、做 整数转字符串 用 平均。
            做 打印 用 做 字符串拼接 用 「  」、做 整数转字符串 用 条[2]。
            做 打印行 用 做 字符串拼接 用 「  」、做 整数转字符串 用 条[3]。
        完。
        i 为 i 加 1。
    完。
    做 打印行 用 「╚═════════════════════════════════════════════════════════╝」。
完。

龙析_热点 接受 数量)：
    ※ 按总耗时排序前 N 个函数
    键列表 为 做 字典_键列表 用 分析_条目。
    排序表 为 []
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        名 为 键列表[i]。
        条 为 做 字典取 用 分析_条目、名。
        若 条 不等于 空：
            做 追加 用 排序表、[条[1]、名]。
        完。
        i 为 i 加 1。
    完。
    ※ 简单选择排序(降序)
    i 为 0。
    只要 i 小于 做 计数 用 排序表 减 1：
        可变 max_idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 排序表：
            若 排序表[j][0] 大于 排序表[max_idx][0]：
                max_idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 max_idx 不等于 i：
            tmp 为 排序表[i]。
            做 设置 用 排序表、i、排序表[max_idx]。
            做 设置 用 排序表、max_idx、tmp。
        完。
        i 为 i 加 1。
    完。
    ※ 输出前N
    做 打印行 用 「─── 热点函数 (CPU时间排序) ───」。
    i 为 0。
    上限 为 若 (数量 小于 做 计数 用 排序表, 数量, 做 计数 用 排序表)。
    只要 i 小于 上限：
        做 打印行 用 做 字符串拼接 用 做 字符串拼接 用 做 整数转字符串 用 i 加 1、「. 」、做 字符串拼接 用 排序表[i][1]、做 字符串拼接 用 「 减 」、做 字符串拼接 用 做 整数转字符串 用 排序表[i][0]、「us」。
        i 为 i 加 1。
    完。
完。

龙析_调用图 接受 )：
    做 打印行 用 「─── 调用图统计 ───」。
    做 打印行 用 做 字符串拼接 用 「总边数: 」、做 整数转字符串 用 调用图_边数。
    可变 i 为 0。
    只要 i 小于 调用图_边数 并且 i 小于 20：
        off 为 i 乘 32。
        caller_h 为 做 内存_读64 用 调用图_边表 加 off。
        callee_h 为 做 内存_读64 用 调用图_边表 加 off 加 8。
        cnt 为 做 内存_读64 用 调用图_边表 加 off 加 16。
        time 为 做 内存_读64 用 调用图_边表 加 off 加 24。
        做 打印行 用 做 字符串拼接 用 做 字符串拼接 用 做 整数转字符串 用 caller_h、「 -> 」、做 字符串拼接 用 做 整数转字符串 用 callee_h、做 字符串拼接 用 「 x」、做 字符串拼接 用 做 整数转字符串 用 cnt、做 字符串拼接 用 「 (」、做 字符串拼接 用 做 整数转字符串 用 time、「us)」)。
        i 为 i 加 1。
    完。
完。

龙析_火焰图 接受 )：
    ※ 输出折叠栈格式 (Brendan Gregg FlameGraph)
    做 打印行 用 「─── 火焰图数据 (折叠栈格式) ───」。
    若 采样缓冲 等于 0 或者 采样数 等于 0：
        做 打印行 用 「无采样数据」。
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 采样数：
        off 为 i 乘 1040。
        深度 为 做 内存_读64 用 采样缓冲 加 off。
        可变 line 为 「」。
        可变 j 为 0。
        只要 j 小于 深度：
            h 为 做 内存_读64 用 采样缓冲 加 off 加 8 加 j 乘 8。
            若 j 大于 0：
            line 为 做 字符串_拼接 用 line、做 整数转字符串 用 h。
            j 为 j 加 1。
        完。
        做 打印行 用 做 字符串_拼接 用 line、「 1」。
        i 为 i 加 1。
    完。
完。

※ §9 内存分析 + 泄漏检测

龙析_内存 接受 )：
    做 打印行 用 「─── 内存使用分析 ───」。
    ※ 基础统计
    总分配 为 0。
    总释放 为 0。
    在用 为 0。
    可变 i 为 0。
    只要 i 小于 分配跟踪_数：
        off 为 i 乘 40。
        sz 为 做 内存_读64 用 分配跟踪_表 加 off 加 8。
        总分配 为 总分配 加 sz。
        若 做 内存_读64 用 分配跟踪_表 加 off 加 32 不等于 0：
            总释放 为 总释放 加 sz。
        否则：
            在用 为 在用 加 sz。
        完。
        i 为 i 加 1。
    完。
    做 打印行 用 做 字符串拼接 用 「  总分配:   」、做 字符串拼接 用 做 整数转字符串 用 总分配、「 字节」。
    做 打印行 用 做 字符串拼接 用 「  已释放:   」、做 字符串拼接 用 做 整数转字符串 用 总释放、「 字节」。
    做 打印行 用 做 字符串拼接 用 「  当前在用: 」、做 字符串拼接 用 做 整数转字符串 用 在用、「 字节」。
    做 打印行 用 做 字符串拼接 用 「  跟踪记录: 」、做 整数转字符串 用 分配跟踪_数。
完。

龙析_泄漏检测 接受 )：
    做 打印行 用 「─── 内存泄漏检测 ───」。
    泄漏数 为 0。
    泄漏总量 为 0。
    可变 i 为 0。
    只要 i 小于 分配跟踪_数：
        off 为 i 乘 40。
        若 做 内存_读64 用 分配跟踪_表 加 off 加 32 等于 0：
            addr 为 做 内存_读64 用 分配跟踪_表 加 off。
            sz 为 做 内存_读64 用 分配跟踪_表 加 off 加 8。
            alloc_h 为 做 内存_读64 用 分配跟踪_表 加 off 加 16。
            做 打印行 用 做 字符串拼接 用 「  泄漏: addr=」、做 字符串拼接 用 做 整数转字符串 用 addr、做 字符串拼接 用 「 size=」、做 字符串拼接 用 做 整数转字符串 用 sz、做 字符串拼接 用 「 alloc_by=」、做 整数转字符串 用 alloc_h。
            泄漏数 为 泄漏数 加 1。
            泄漏总量 为 泄漏总量 加 sz。
        完。
        i 为 i 加 1。
    完。
    做 打印行 用 做 字符串拼接 用 「  泄漏总数: 」、做 字符串拼接 用 做 整数转字符串 用 泄漏数、做 字符串拼接 用 「  总量: 」、做 字符串拼接 用 做 整数转字符串 用 泄漏总量、「 字节」。
完。

※ §10 AI总线: EWMA异常检测 + 智能建议

EWMA_PROF_ALPHA 为 205
可变 EWMA_平均耗时 为 0。
可变 EWMA_分配率 为 0。

龙析_AI分析 接受 )：
    bus 为 全局AI总线。
    做 打印行 用 「─── AI智能分析建议 ───」。
    若 bus 等于 0：
        做 打印行 用 「  AI总线未初始化」。
        返还。
    完。
    ※ 特征: [平均耗时, 分配率, 泄漏率, 栈深度]
    特征 为 做 Tensor_创建 用 1、4。
    做 Tensor_写 用 特征、0、0、EWMA_平均耗时。
    做 Tensor_写 用 特征、0、1、EWMA_分配率。
    ※ 计算泄漏率
    泄漏数 为 0。
    可变 i 为 0。
    只要 i 小于 分配跟踪_数：
        若 做 内存_读64 用 分配跟踪_表 加 i 乘 40 加 32 等于 0：
            泄漏数 为 泄漏数 加 1。
        完。
        i 为 i 加 1。
    完。
    泄漏率 为 若 (分配跟踪_数 大于 0, 泄漏数 乘 Q10 除以 分配跟踪_数, 0)。
    做 Tensor_写 用 特征、0、2、泄漏率。
    做 Tensor_写 用 特征、0、3、Q10
    分数 为 做 AI总线_注意力权重 用 bus、特征。
    做 Tensor_释放 用 特征。
    ※ 生成建议
    若 分数 大于 800：
        做 打印行 用 「  [!] 严重性能问题检测」。
    否则若 分数 大于 500：
        做 打印行 用 「  [警告] 性能异常检测」。
    否则：
        做 打印行 用 「  [正常] 未检测到明显异常」。
    完。
    若 泄漏率 大于 200：
        做 打印行 用 做 字符串拼接 用 「  建议: 检查内存泄漏 (泄漏率 」、做 字符串拼接 用 做 整数转字符串 用 泄漏率 乘 100 除以 Q10、「%)」。
    完。
    若 EWMA_平均耗时 大于 10000：
        做 打印行 用 「  建议: 某些函数耗时过长、建议用 'top' 命令定位热点」。
    完。
完。

龙析_AI反馈耗时 接受 耗时_us)：
    ※ 将函数耗时反馈到EWMA
    EWMA_平均耗时 为 (EWMA_PROF_ALPHA 乘 耗时_us 加 (Q10 减 EWMA_PROF_ALPHA) 乘 EWMA_平均耗时) 除以 Q10。
完。

龙析_AI反馈分配 接受 分配字节)：
    ※ 将堆分配量反馈到EWMA
    EWMA_分配率 为 (EWMA_PROF_ALPHA 乘 分配字节 加 (Q10 减 EWMA_PROF_ALPHA) 乘 EWMA_分配率) 除以 Q10。
完。

※ 辅助: 字符串哈希
字符串_哈希 接受 s)：
    若 s 等于 空 或者 s 等于 0：
    可变 h 为 0x811C9DC5。
    len 为 做 字符串长度 用 s。
    可变 i 为 0。
    只要 i 小于 len：
        h 为 (h 位异或 做 字节取 用 s、i) 乘 16777619。
        i 为 i 加 1。
    完。
    返还 h 位与 0x7FFFFFFF。
完。

本章 收束。
