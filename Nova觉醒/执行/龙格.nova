《龙格》

引用《..·中枢·类型定义》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《..·标准·原生字符串》。

引用《..·中枢·字符串替换》。

引用《..·中枢·整数转字符串》。

龙格_main 接受 参数：
    若 做 计数 用 参数 等于 0：
        做 打印行 用 「龙格 v0.1 — Nova代码格式化器」。
        做 打印行 用 「用法: 龙格 [文件...]」。
        做 打印行 用 「  龙格 --check [文件...]  — 仅检查不修改」。
        返还 0。
    完。
    可变 仅检查 为 假。
    可变 文件列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        若 参数 的 第 i 项 等于 「--check」：
            仅检查 为 真。
        否则：
            做 追加 用 文件列表、参数 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 已格式化 为 0。
    可变 需修改 为 0。
    可变 k 为 0。
    只要 k 小于 做 计数 用 文件列表：
        路径 为 文件列表 的 第 k 项。
        内容 为 做 文件_读取全部 用 路径。
        若 内容 等于 空：
            做 打印行 用 做 字符串拼接 用 「龙格: 无法读取 」、路径。
            k 为 k 加 1。
            继续。
        完。
        格式化后 为 做 龙格_格式化 用 内容。
        若 格式化后 不等于 内容：
            需修改 为 需修改 加 1。
            若 仅检查：
                做 打印行 用 做 字符串拼接 用 「需格式化: 」、路径。
            否则：
                做 文件_写入全部 用 路径、格式化后。
                做 打印行 用 做 字符串拼接 用 「已格式化: 」、路径。
            完。
        完。
        已格式化 为 已格式化 加 1。
        k 为 k 加 1。
    完。
    做 打印行 用 做 字符串拼接 用 「检查 」、做 字符串拼接 用 做 整数转字符串 用 已格式化、做 字符串拼接 用 「 个文件, 」、做 字符串拼接 用 做 整数转字符串 用 需修改、「 需修改」。
    返还 0。
完。

龙格_格式化 接受 代码：
    行列表 为 做 字符串分割 用 代码、「\n」。
    可变 结果 为 一组：（空）。
    可变 缩进深度 为 0。
    缩进单位 为 「    」。
    可变 上一行空 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去首尾空白 用 行列表 的 第 i 项。
        若 做 字符串长度 用 行 等于 0：
            若 非 上一行空：
                做 追加 用 结果、「」。
                上一行空 为 真。
            完。
            i 为 i 加 1。
            继续。
        完。
        上一行空 为 假。
        若 做 字符串开始于 用 行、「}」：
            缩进深度 为 缩进深度 减 1。
            若 缩进深度 小于 0：
                缩进深度 为 0。
            完。
        完。
        可变 缩进行 为 「」。
        可变 d 为 0。
        只要 d 小于 缩进深度：
            缩进行 为 缩进行 加 缩进单位。
            d 为 d 加 1。
        完。
        缩进行 为 缩进行 加 行。
        做 追加 用 结果、缩进行。
        可变 j 为 0。
        只要 j 小于 做 字符串长度 用 行：
            ch 为 做 字符串字符 用 行、j。
            若 ch 等于 123：
                缩进深度 为 缩进深度 加 1。
            完。
            若 ch 等于 125：
            完。
            j 为 j 加 1。
        完。
        若 i 大于 0 并且 (做 字符串开始于 用 行、「函数 」 或者 做 字符串开始于 用 行、「结构 」)：
            若 做 计数 用 结果 大于 1：
                上行 为 结果 的 第 做 计数 用 结果 减 2 项。
                若 做 字符串长度 用 上行 大于 0 并且 非 做 字符串开始于 用 做 字符串去首尾空白 用 上行、「//」：
                    最后 为 结果 的 第 做 计数 用 结果 减 1 项。
                    做 设置 用 结果、做 计数 用 结果 减 1、「」。
                    做 追加 用 结果、最后。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    只要 做 计数 用 结果 大于 0 并且 做 字符串长度 用 结果 的 第 做 计数 用 结果 减 1 项 等于 0：
        做 列表_移除 用 结果、做 计数 用 结果 减 1。
    完。
    做 追加 用 结果、「」。
    返还 做 字符串连接 用 结果、「\n」。
完。

龙格_差异 接受 原始、格式化后：
    原行 为 做 字符串分割 用 原始、「\n」。
    新行 为 做 字符串分割 用 格式化后、「\n」。
    可变 差异数 为 0。
    最大 为 做 计数 用 原行。
    若 做 计数 用 新行 大于 最大：
        最大 为 做 计数 用 新行。
    完。
    可变 i 为 0。
    只要 i 小于 最大：
        旧 为 「」。
        新 为 「」。
        若 i 小于 做 计数 用 原行：
            旧 为 原行 的 第 i 项。
        完。
        若 i 小于 做 计数 用 新行：
            新 为 新行 的 第 i 项。
        完。
        若 旧 不等于 新：
            差异数 为 差异数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 差异数。
完。

本章 收束。
