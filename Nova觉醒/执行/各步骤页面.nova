《各步骤页面》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·扫描磁盘》。

引用《.·辅助UI绘制》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内存回收》。

绘制_欢迎页 接受 x、y、宽、高：
    中心X 为 x 加 宽 除以 2。
    做 绘制_AI全息人形 用 中心X、y 加 80、120。
    做 绘制文字 用 中心X 减 80、y 加 220、「欢迎来到 NovaOS」、28、0x4FC3F7。
    做 绘制文字 用 中心X 减 160、y 加 270、「AI原生操作系统 — 你的智能伙伴」、16、0xA0A0C0。
    做 绘制文字 用 中心X 减 200、y 加 320、「NovaOS将AI融入操作系统的每一个角落」、14、0x808090。
    做 绘制文字 用 中心X 减 180、y 加 345、「从内核调度到桌面交互，AI让一切更智能」、14、0x808090。
    图标Y 为 y 加 400。
    做 绘制_特性图标 用 中心X 减 220、图标Y、「🧠」、「AI内核」、「29个AI模块深度集成」。
    做 绘制_特性图标 用 中心X 减 40、图标Y、「🔄」、「热更新」、「不重启升级内核」。
    做 绘制_特性图标 用 中心X 加 140、图标Y、「🛡️」、「自动回滚」、「崩溃自动恢复」。
完。

绘制_语言页 接受 x、y、宽、高：
    做 绘制文字 用 x 加 30、y 加 20、「选择语言 / Select Language」、22、0xE0E0F0。
    可变 选项 为 一组：一组：「zh_CN」、「简体中文」、「中华人民共和国」、一组：「zh_TW」、「繁體中文」、「中國台灣」、一组：「en_US」、「English」、「United States」、一组：「ja_JP」、「日本語」、「日本」、一组：「ko_KR」、「한국어」、「대한민국」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 选项：
        项Y 为 y 加 80 加 i 乘 60。
        选中 为 安装 的 语言 等于 选项 的 第 i 项 的 第 0 项。
        若 选中：
            做 绘制_发光边框 用 空、x 加 30、项Y、宽 减 60、50、12。
            做 填充矩形 用 x 加 30、项Y、宽 减 60、50、做 颜色 用 79、195、247、30。
        完。
        做 绘制文字 用 x 加 50、项Y 加 8、选项 的 第 i 项 的 第 1 项、18、做 如果选 用 选中、0x4FC3F7、0xC0C0D0。
        做 绘制文字 用 x 加 50、项Y 加 30、选项 的 第 i 项 的 第 2 项、12、0x808090。
        i 为 i 加 1。
    完。
    做 绘制文字 用 x 加 30、y 加 400、「时区: 」 加 安装 的 时区、14、0xA0A0B0。
完。

绘制_磁盘页 接受 x、y、宽、高：
    做 绘制文字 用 x 加 30、y 加 20、「选择安装磁盘」、22、0xE0E0F0。
    磁盘列表 为 做 扫描磁盘。
    若 做 计数 用 磁盘列表 等于 0：
        做 绘制文字 用 x 加 30、y 加 100、「⚠ 未检测到磁盘」、18、0xEF5350。
        做 绘制文字 用 x 加 30、y 加 130、「请确认磁盘已连接且驱动正常」、14、0x808090。
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 磁盘列表：
        磁盘 为 磁盘列表 的 第 i 项。
        项Y 为 y 加 80 加 i 乘 80。
        选中 为 安装 的 目标磁盘 等于 磁盘 的 第 0 项。
        若 选中：
            做 绘制_发光边框 用 空、x 加 30、项Y、宽 减 60、70、12。
        完。
        做 绘制文字 用 x 加 50、项Y 加 8、「💾 」 加 磁盘 的 第 0 项、16、做 如果选 用 选中、0x4FC3F7、0xC0C0D0。
        做 绘制文字 用 x 加 50、项Y 加 30、磁盘 的 第 1 项 加 「 | 」 加 做 整数转字符串 用 磁盘 的 第 2 项 除以 1073741824 加 「 GB」、13、0x808090。
        已用比 为 磁盘 的 第 3 项 乘 100 除以 磁盘 的 第 2 项。
        做 绘制_进度条 用 x 加 50、项Y 加 50、宽 减 110、8、已用比、0x4FC3F7。
        i 为 i 加 1。
    完。
完。

绘制_分区页 接受 x、y、宽、高：
    做 绘制文字 用 x 加 30、y 加 20、「分区设置」、22、0xE0E0F0。
    自动选中 为 安装 的 分区方案 等于 0。
    手动选中 为 安装 的 分区方案 等于 1。
    Y1 为 y 加 80。
    若 自动选中：
        做 绘制_发光边框 用 空、x 加 30、Y1、宽 减 60、120、12。
    完。
    做 绘制文字 用 x 加 50、Y1 加 10、「⚡ 自动分区 (推荐)」、18、做 如果选 用 自动选中、0x4FC3F7、0xC0C0D0。
    做 绘制文字 用 x 加 50、Y1 加 35、「自动创建EFI(512MB) + 根分区 + 交换分区(4GB)」、13、0x808090。
    做 绘制文字 用 x 加 50、Y1 加 55、「⚠ 将擦除 」 加 安装 的 目标磁盘 加 「 上的所有数据」、13、0xFFA726。
    若 自动选中：
        做 绘制_分区预览 用 x 加 50、Y1 加 80、宽 减 110、30。
    完。
    Y2 为 y 加 220。
    若 手动选中：
        做 绘制_发光边框 用 空、x 加 30、Y2、宽 减 60、80、12。
    完。
    做 绘制文字 用 x 加 50、Y2 加 10、「🔧 手动分区」、18、做 如果选 用 手动选中、0x4FC3F7、0xC0C0D0。
    做 绘制文字 用 x 加 50、Y2 加 35、「自定义分区表(高级用户)」、13、0x808090。
完。

绘制_用户页 接受 x、y、宽、高：
    做 绘制文字 用 x 加 30、y 加 20、「创建用户」、22、0xE0E0F0。
    中心X 为 x 加 宽 除以 2。
    做 绘制_全息头像 用 中心X、y 加 80、60。
    做 绘制_输入框 用 x 加 宽 除以 2 减 200、y 加 180、400、「用户名」、安装 的 用户名。
    做 绘制_输入框 用 x 加 宽 除以 2 减 200、y 加 240、400、「密码」、「••••••••」。
    做 绘制_输入框 用 x 加 宽 除以 2 减 200、y 加 300、400、「确认密码」、「••••••••」。
    做 绘制_输入框 用 x 加 宽 除以 2 减 200、y 加 360、400、「主机名」、安装 的 主机名。
    做 绘制文字 用 x 加 宽 除以 2 减 180、y 加 420、「💡 AI将为你创建个性化的桌面环境」、13、0x66BB6A。
完。

绘制_AI设置页 接受 x、y、宽、高：
    做 绘制文字 用 x 加 30、y 加 20、「AI 智能配置」、22、0xE0E0F0。
    做 绘制文字 用 x 加 30、y 加 50、「NovaOS的灵魂 — 选择AI运行模式」、14、0x808090。
    模式 为 一组：一组：0、「☁️ 云端优先 (推荐新手)」、「通过接口调用云端AI(Nova语言模型/Claude/DeepSeek)」、「本地TNF作为加速缓存，减少接口调用」、「需要网络连接，响应快，能力强」、一组：1、「🧠 本地优先 (推荐高手)」、「本地TNF模型为主力引擎」、「云端仅在本地无法回答时补充」、「离线可用，隐私最强，但需要训练」、一组：2、「🔄 智能混合 (推荐)」、「AI自动判断：简单任务本地处理」、「复杂任务云端协助，本地持续学习」、「兼顾速度、能力、隐私、离线」。
    可变 i 为 0。
    只要 i 小于 3：
        项Y 为 y 加 90 加 i 乘 120。
        选中 为 安装 的 AI模式 等于 模式 的 第 i 项 的 第 0 项。
        若 选中：
            做 绘制_发光边框 用 空、x 加 30、项Y、宽 减 60、110、12。
            做 填充矩形 用 x 加 30、项Y、宽 减 60、110、做 颜色 用 79、195、247、20。
        完。
        做 绘制文字 用 x 加 50、项Y 加 8、模式 的 第 i 项 的 第 1 项、17、做 如果选 用 选中、0x4FC3F7、0xC0C0D0。
        做 绘制文字 用 x 加 50、项Y 加 32、模式 的 第 i 项 的 第 2 项、12、0x909098。
        做 绘制文字 用 x 加 50、项Y 加 50、模式 的 第 i 项 的 第 3 项、12、0x909098。
        做 绘制文字 用 x 加 50、项Y 加 68、模式 的 第 i 项 的 第 4 项、12、0x909098。
        i 为 i 加 1。
    完。
    若 安装 的 AI模式 等于 0 或者 安装 的 AI模式 等于 2：
        做 绘制_输入框 用 x 加 30、y 加 460、宽 减 60、「云端接口密钥 (可选,之后设置也行)」、安装 的 智能_接口密钥。
    完。
完。

绘制_安装进度页 接受 x、y、宽、高：
    中心X 为 x 加 宽 除以 2。
    做 绘制_AI全息人形_工作中 用 中心X、y 加 60、80。
    做 绘制文字 用 中心X 减 60、y 加 170、「正在安装...」、24、0x4FC3F7。
    做 绘制_进度条 用 x 加 60、y 加 220、宽 减 120、16、安装 的 安装进度、0x4FC3F7。
    做 绘制文字 用 中心X 减 15、y 加 245、做 整数转字符串 用 安装 的 安装进度 加 「%」、16、0xE0E0F0。
    操作 为 做 获取安装操作 用 安装 的 安装进度。
    做 绘制文字 用 x 加 60、y 加 280、操作、14、0xA0A0B0。
    步骤列表 为 一组：一组：10、「格式化分区」、一组：20、「复制系统文件」、一组：40、「安装内核」、一组：55、「配置引导加载器」、一组：65、「初始化AI子系统」、一组：75、「下载AI基础权重」、一组：85、「配置网络和驱动」、一组：90、「创建用户账户」、一组：95、「生成个性化桌面」、一组：100、「安装完成」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 步骤列表：
        项Y 为 y 加 320 加 i 乘 18。
        完成 为 安装 的 安装进度 大于或等于 步骤列表 的 第 i 项 的 第 0 项。
        图标 为 做 如果选 用 完成、「✅」、「⬜」。
        颜色 为 做 如果选 用 完成、0x66BB6A、0x505060。
        做 绘制文字 用 x 加 80、项Y、图标 加 「 」 加 步骤列表 的 第 i 项 的 第 1 项、12、颜色。
        i 为 i 加 1。
    完。
完。

绘制_完成页 接受 x、y、宽、高：
    中心X 为 x 加 宽 除以 2。
    做 绘制_AI全息人形 用 中心X、y 加 60、100。
    做 绘制文字 用 中心X 减 100、y 加 200、「🎉 安装完成!」、32、0x66BB6A。
    做 绘制文字 用 中心X 减 160、y 加 260、「NovaOS已就绪，AI正在等待与你相遇」、16、0xA0A0C0。
    做 绘制文字 用 中心X 减 120、y 加 320、「系统信息:」、16、0xC0C0D0。
    做 绘制文字 用 中心X 减 120、y 加 345、「用户: 」 加 安装 的 用户名 加 「@」 加 安装 的 主机名、13、0x909098。
    做 绘制文字 用 中心X 减 120、y 加 365、「AI模式: 」 加 做 AI模式名称 用 安装 的 AI模式、13、0x909098。
    做 绘制文字 用 中心X 减 120、y 加 385、「磁盘: 」 加 安装 的 目标磁盘、13、0x909098。
    按钮X 为 中心X 减 100。
    按钮Y 为 y 加 440。
    做 绘制_发光边框 用 空、按钮X、按钮Y、200、48、24。
    做 填充矩形 用 按钮X、按钮Y、200、48、做 颜色 用 79、195、247、40。
    做 绘制文字 用 按钮X 加 40、按钮Y 加 12、「立即重启」、20、0x4FC3F7。
完。

本章 收束。
