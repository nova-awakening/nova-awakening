《内核总控》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

浏览器版本 为 「2.0.0」。

浏览器名称 为 「NovaOS 终端」。

浏览器代号 为 「NovaBrowser/1.0 (内核原生; NovaOS x86-64)」。

默认首页 为 「nova://终端」。

默认搜索引擎 为 「https://search.novaos.dev/?q=」。

默认视口宽 为 1280。

默认视口高 为 800。

浏览器内核状态 是 一种 事物，含 已初始化、运行中、启动时间、版本、适配层就绪、核心层就绪、AI层就绪、网络增强就绪、反检测就绪、自动化就绪、Web桌面就绪、入口层就绪、总页面加载、总JS执行、总AI推理、总安全拦截、总验证破解、总自动化任务、运行时长秒。

可变 内核状态 为 空。

本章 收束。
