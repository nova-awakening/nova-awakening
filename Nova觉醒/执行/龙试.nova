《龙试》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·标准·文件》。

引用《..·中枢·内核打印》。

引用《..·标准·原生时间》。

引用《..·中枢·字符串替换》。

引用《..·推理·智能体文件操作》。

引用《..·中枢·整数转字符串》。

可变 测试_总数 为 0。

可变 测试_通过 为 0。

可变 测试_失败 为 0。

可变 测试_跳过 为 0。

可变 测试_失败列表 为 一组：（空）。

可变 测试_套件列表 为 一组：（空）。

可变 测试_当前套件 为 「」。

可变 测试_总耗时 为 0。

龙试_main 接受 参数：
    做 打印行 用 「龙试 v0.1 — Nova测试框架」。
    做 打印行 用 「」。
    测试_总数 为 0。
    测试_通过 为 0。
    测试_失败 为 0。
    测试_跳过 为 0。
    测试_失败列表 为 一组：（空）。
    测试_套件列表 为 一组：（空）。
    总计时 为 做 计时器_创建。
    可变 测试目录 为 「tests」。
    若 做 计数 用 参数 大于 0：
        测试目录 为 参数 的 第 0 项。
    完。
    测试文件 为 做 智能体文件_搜索 用 测试目录、「.nova」。
    若 做 计数 用 测试文件 等于 0：
        测试文件 为 做 智能体文件_搜索 用 「.」、「test_」。
    完。
    若 做 计数 用 测试文件 等于 0：
        做 打印行 用 「[龙试] 未找到测试文件」。
        做 打印行 用 「  搜索: 」 加 测试目录 加 「/*.nova 和 ./test_*.nova」。
        返还 0。
    完。
    做 打印行 用 「[龙试] 找到 」 加 做 整数转字符串 用 做 计数 用 测试文件 加 「 个测试文件」。
    做 打印行 用 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 测试文件：
        做 龙试_执行文件 用 测试文件 的 第 i 项。
        i 为 i 加 1。
    完。
    测试_总耗时 为 做 计时器_经过 用 总计时。
    做 打印行 用 「」。
    做 打印行 用 「═══════════════════════════════════════」。
    做 打印行 用 「测试结果:」。
    做 打印行 用 「  ✓ 通过: 」 加 做 整数转字符串 用 测试_通过。
    做 打印行 用 「  ✗ 失败: 」 加 做 整数转字符串 用 测试_失败。
    若 测试_跳过 大于 0：
        做 打印行 用 「  ○ 跳过: 」 加 做 整数转字符串 用 测试_跳过。
    完。
    做 打印行 用 「  总计: 」 加 做 整数转字符串 用 测试_总数 加 「 测试 | 」 加 做 整数转字符串 用 测试_总耗时 加 「ms」。
    做 打印行 用 「═══════════════════════════════════════」。
    若 测试_失败 大于 0：
        做 打印行 用 「」。
        做 打印行 用 「失败详情:」。
        可变 j 为 0。
        只要 j 小于 做 计数 用 测试_失败列表：
            做 打印行 用 「  ✗ 」 加 测试_失败列表 的 第 j 项。
            j 为 j 加 1。
        完。
    完。
    返还 测试_失败。
完。

龙试_执行文件 接受 文件路径：
    源码 为 做 文件_读全部 用 文件路径。
    若 源码 等于 空：
        做 打印行 用 「  ✗ 无法读取: 」 加 文件路径。
        返还。
    完。
    测试_当前套件 为 文件路径。
    做 打印行 用 「━━ 」 加 文件路径 加 「 ━━」。
    计时 为 做 计时器_创建。
    结果 为 做 nova引擎v2_执行 用 源码、一组：（空）。
    耗时 为 做 计时器_经过 用 计时。
    若 结果 等于 空 或者 非 结果 的 成功：
        做 打印行 用 「  ✗ 执行失败 (」 加 做 整数转字符串 用 耗时 加 「ms)」。
        测试_总数 为 测试_总数 加 1。
        测试_失败 为 测试_失败 加 1。
        做 追加 用 测试_失败列表、文件路径 加 「: 执行错误」。
    否则：
        做 打印行 用 「  ✓ (」 加 做 整数转字符串 用 耗时 加 「ms)」。
    完。
    做 追加 用 测试_套件列表、一组：文件路径、耗时。
完。

测试 接受 名称、条件：
    测试_总数 为 测试_总数 加 1。
    若 条件：
        做 打印行 用 「    ✓ 」 加 名称。
        测试_通过 为 测试_通过 加 1。
    否则：
        做 打印行 用 「    ✗ 」 加 名称。
        测试_失败 为 测试_失败 加 1。
        做 追加 用 测试_失败列表、测试_当前套件 加 「: 」 加 名称。
    完。
完。

断言相等 接受 实际、期望、描述：
    测试_总数 为 测试_总数 加 1。
    若 实际 等于 期望：
        做 打印行 用 「    ✓ 」 加 描述。
        测试_通过 为 测试_通过 加 1。
    否则：
        做 打印行 用 「    ✗ 」 加 描述 加 「 (期望=」 加 做 转字符串 用 期望 加 「 实际=」 加 做 转字符串 用 实际 加 「)」。
        测试_失败 为 测试_失败 加 1。
        做 追加 用 测试_失败列表、测试_当前套件 加 「: 」 加 描述。
    完。
完。

断言不等 接受 实际、不期望、描述：
    测试_总数 为 测试_总数 加 1。
    若 实际 不等于 不期望：
        做 打印行 用 「    ✓ 」 加 描述。
        测试_通过 为 测试_通过 加 1。
    否则：
        做 打印行 用 「    ✗ 」 加 描述 加 「 (不应等于 」 加 做 转字符串 用 不期望 加 「)」。
        测试_失败 为 测试_失败 加 1。
        做 追加 用 测试_失败列表、测试_当前套件 加 「: 」 加 描述。
    完。
完。

断言真 接受 条件、描述：
    做 测试 用 描述、条件。
完。

断言假 接受 条件、描述：
    做 测试 用 描述、非 条件。
完。

断言空 接受 值、描述：
    做 测试 用 描述、值 等于 空。
完。

断言非空 接受 值、描述：
    做 测试 用 描述、值 不等于 空。
完。

断言大于 接受 实际、期望、描述：
    做 测试 用 描述 加 「 (」 加 做 转字符串 用 实际 加 「>」 加 做 转字符串 用 期望 加 「)」、实际 大于 期望。
完。

断言范围 接受 值、最小、最大、描述：
    做 测试 用 描述、值 大于或等于 最小 并且 值 小于或等于 最大。
完。

测试_跳过标记 接受 描述：
    测试_总数 为 测试_总数 加 1。
    测试_跳过 为 测试_跳过 加 1。
    做 打印行 用 「    ○ 」 加 描述 加 「 [跳过]」。
完。

测试套件 接受 名称：
    测试_当前套件 为 名称。
    做 打印行 用 「  ┌─ 」 加 名称。
完。

测试套件结束 接受 零参：
    做 打印行 用 「  └─ 完成」。
完。

性能测试 接受 名称、函数、重复次数：
    计时 为 做 计时器_创建。
    可变 i 为 0。
    只要 i 小于 重复次数：
        以 零参 为 引，其做法是：
        i 为 i 加 1。
    完。
    耗时 为 做 计时器_经过 用 计时。
    每次 为 耗时 乘 1000 除以 重复次数。
    做 打印行 用 「    ⏱ 」 加 名称 加 「: 」 加 做 整数转字符串 用 耗时 加 「ms (」 加 做 整数转字符串 用 重复次数 加 「次, 」 加 做 整数转字符串 用 每次 加 「μs/次)」。
完。

本章 收束。
