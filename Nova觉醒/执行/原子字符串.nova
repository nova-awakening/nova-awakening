《原子字符串》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·感知·内核直连层》。

引用《..·感知·原子驻留》。

引用《..·中枢·内存初始化》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·辅助存根》。

原子表 是 一种 事物，含 条目、数量、容量、下一个ID。

可变 全局原子表 为 0。

原子表_初始化 接受 零参：
    若 全局原子表 不等于 0：
        返还。
    完。
    全局原子表 为 做 堆_分配 用 32。
    容量 为 1024。
    做 内存_写64 用 全局原子表、做 堆_分配_数组 用 容量 乘 24。
    做 内存_写32 用 全局原子表 加 8、0。
    做 内存_写32 用 全局原子表 加 12、容量。
    做 内存_写32 用 全局原子表 加 16、1。
完。

原子_驻留 接受 str：
    若 str 等于 0：
        返还 0。
    完。
    若 全局原子表 等于 0：
        做 原子表_初始化。
    完。
    哈希 为 做 字符串_哈希 用 str。
    条目列表 为 做 内存_读64 用 全局原子表。
    数量 为 做 内存_读32 用 全局原子表 加 8。
    可变 i 为 0。
    只要 i 小于 数量：
        偏移 为 条目列表 加 i 乘 24。
        若 做 内存_读32 用 偏移 等于 哈希：
            已有str 为 做 内存_读64 用 偏移 加 8。
            若 做 字符串_相等 用 已有str、str：
                返还 做 内存_读32 用 偏移 加 16。
            完。
        完。
        i 为 i 加 1。
    完。
    容量 为 做 内存_读32 用 全局原子表 加 12。
    若 数量 大于或等于 容量：
        返还 0。
    完。
    ID 为 做 内存_读32 用 全局原子表 加 16。
    偏移 为 条目列表 加 数量 乘 24。
    做 内存_写32 用 偏移、哈希。
    做 内存_写64 用 偏移 加 8、做 字符串_复制 用 str。
    做 内存_写32 用 偏移 加 16、ID。
    做 内存_写32 用 全局原子表 加 8、数量 加 1。
    做 内存_写32 用 全局原子表 加 16、ID 加 1。
    返还 ID。
完。

可变 原子_html 为 0。

可变 原子_head 为 0。

可变 原子_body 为 0。

可变 原子_div 为 0。

可变 原子_span 为 0。

可变 原子_p 为 0。

可变 原子_a 为 0。

可变 原子_h1 为 0。

可变 原子_h2 为 0。

可变 原子_h3 为 0。

可变 原子_ul 为 0。

可变 原子_ol 为 0。

可变 原子_li 为 0。

可变 原子_img 为 0。

可变 原子_br 为 0。

可变 原子_hr 为 0。

可变 原子_input 为 0。

可变 原子_button 为 0。

可变 原子_form 为 0。

可变 原子_textarea 为 0。

可变 原子_select 为 0。

可变 原子_option 为 0。

可变 原子_table 为 0。

可变 原子_tr 为 0。

可变 原子_td 为 0。

可变 原子_th 为 0。

可变 原子_script 为 0。

可变 原子_style 为 0。

可变 原子_link 为 0。

可变 原子_meta 为 0。

可变 原子_title 为 0。

可变 原子_header 为 0。

可变 原子_footer 为 0。

可变 原子_nav 为 0。

可变 原子_section 为 0。

可变 原子_article 为 0。

可变 原子_aside 为 0。

可变 原子_main 为 0。

可变 原子_canvas 为 0。

可变 原子_video 为 0。

可变 原子_audio 为 0。

可变 原子_iframe 为 0。

元素_初始化原子表 接受 零参：
    做 原子表_初始化。
    原子_html 为 做 原子_驻留 用 「html」。
    原子_head 为 做 原子_驻留 用 「head」。
    原子_body 为 做 原子_驻留 用 「body」。
    原子_div 为 做 原子_驻留 用 「div」。
    原子_span 为 做 原子_驻留 用 「span」。
    原子_p 为 做 原子_驻留 用 「p」。
    原子_a 为 做 原子_驻留 用 「a」。
    原子_h1 为 做 原子_驻留 用 「h1」。
    原子_h2 为 做 原子_驻留 用 「h2」。
    原子_h3 为 做 原子_驻留 用 「h3」。
    原子_ul 为 做 原子_驻留 用 「ul」。
    原子_ol 为 做 原子_驻留 用 「ol」。
    原子_li 为 做 原子_驻留 用 「li」。
    原子_img 为 做 原子_驻留 用 「img」。
    原子_br 为 做 原子_驻留 用 「br」。
    原子_hr 为 做 原子_驻留 用 「hr」。
    原子_input 为 做 原子_驻留 用 「input」。
    原子_button 为 做 原子_驻留 用 「button」。
    原子_form 为 做 原子_驻留 用 「form」。
    原子_textarea 为 做 原子_驻留 用 「textarea」。
    原子_select 为 做 原子_驻留 用 「select」。
    原子_option 为 做 原子_驻留 用 「option」。
    原子_table 为 做 原子_驻留 用 「table」。
    原子_tr 为 做 原子_驻留 用 「tr」。
    原子_td 为 做 原子_驻留 用 「td」。
    原子_th 为 做 原子_驻留 用 「th」。
    原子_script 为 做 原子_驻留 用 「script」。
    原子_style 为 做 原子_驻留 用 「style」。
    原子_link 为 做 原子_驻留 用 「link」。
    原子_meta 为 做 原子_驻留 用 「meta」。
    原子_title 为 做 原子_驻留 用 「title」。
    原子_header 为 做 原子_驻留 用 「header」。
    原子_footer 为 做 原子_驻留 用 「footer」。
    原子_nav 为 做 原子_驻留 用 「nav」。
    原子_section 为 做 原子_驻留 用 「section」。
    原子_article 为 做 原子_驻留 用 「article」。
    原子_aside 为 做 原子_驻留 用 「aside」。
    原子_main 为 做 原子_驻留 用 「main」。
    原子_canvas 为 做 原子_驻留 用 「canvas」。
    原子_video 为 做 原子_驻留 用 「video」。
    原子_audio 为 做 原子_驻留 用 「audio」。
    原子_iframe 为 做 原子_驻留 用 「iframe」。
完。

可变 AI_原子字符串_热节点缓存 为 0。

AI_原子字符串_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_原子字符串_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_原子字符串_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

本章 收束。
