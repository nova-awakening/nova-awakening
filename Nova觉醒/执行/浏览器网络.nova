《浏览器网络》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

方法_GET 为 0。

方法_POST 为 1。

方法_PUT 为 2。

方法_DELETE 为 3。

方法_HEAD 为 4。

方法_OPTIONS 为 5。

请求状态_等待 为 0。

请求状态_域名解析中 为 1。

请求状态_连接中 为 2。

请求状态_发送中 为 3。

请求状态_接收中 为 4。

请求状态_完成 为 5。

请求状态_失败 为 6。

请求状态_取消 为 7。

优先级_关键 为 0。

优先级_高 为 1。

优先级_中 为 2。

优先级_低 为 3。

优先级_空闲 为 4。

浏览器网络请求 是 一种 事物，含 ID、方法、URL、主机、端口、路径、请求头、请求体、状态、优先级、响应码、响应头、响应体、响应体大小、Socket_FD、AI预测命中、AI优先级调整。

浏览器连接池 是 一种 事物，含 连接列表、最大连接数、每主机最大、空闲超时。

可变 网络_请求队列 为 一组：（空）。

可变 网络_连接池 为 空。

可变 网络_下一个ID 为 1。

可变 网络_已初始化 为 假。

可变 网络_域名缓存 为 一组：（空）。

可变 网络_总请求数 为 0。

可变 网络_总字节数 为 0。

可变 网络_AI预取命中 为 0。

浏览器网络_初始化 接受 零参：
    网络_连接池 为 新建 浏览器连接池。
    网络_连接池 的 连接列表 为 一组：（空）。
    网络_连接池 的 最大连接数 为 64。
    网络_连接池 的 每主机最大 为 6。
    网络_连接池 的 空闲超时 为 30000。
    网络_请求队列 为 一组：（空）。
    网络_域名缓存 为 一组：（空）。
    网络_已初始化 为 真。
    做 内核信息 用 「浏览器网络适配: 初始化完成, 直连内核L2网络栈」。
完。

解析后URL 是 一种 事物，含 协议、主机、端口、路径、查询、片段。

浏览器网络_解析URL 接受 url：
    结果 为 新建 解析后URL。
    结果 的 端口 为 80。
    结果 的 路径 为 「/」。
    协议结束 为 做 字符串查找 用 url、「://」。
    若 协议结束 大于或等于 0：
        结果 的 协议 为 做 字符串子串 用 url、0、协议结束。
        url 为 做 字符串子串 用 url、协议结束 加 3、做 字符串长度 用 url 减 协议结束 减 3。
    否则：
        结果 的 协议 为 「https」。
    完。
    若 结果 的 协议 等于 「https」：
        结果 的 端口 为 443。
    完。
    若 结果 的 协议 等于 「nova」：
        结果 的 端口 为 0。
    完。
    路径开始 为 做 字符串查找 用 url、「/」。
    若 路径开始 大于或等于 0：
        结果 的 主机 为 做 字符串子串 用 url、0、路径开始。
        结果 的 路径 为 做 字符串子串 用 url、路径开始、做 字符串长度 用 url 减 路径开始。
    否则：
        结果 的 主机 为 url。
    完。
    端口标记 为 做 字符串查找 用 结果 的 主机、「:」。
    若 端口标记 大于或等于 0：
        端口串 为 做 字符串子串 用 结果 的 主机、端口标记 加 1、做 字符串长度 用 结果 的 主机 减 端口标记 减 1。
        结果 的 端口 为 做 字符串转整数 用 端口串。
        结果 的 主机 为 做 字符串子串 用 结果 的 主机、0、端口标记。
    完。
    查询开始 为 做 字符串查找 用 结果 的 路径、「?」。
    若 查询开始 大于或等于 0：
        结果 的 查询 为 做 字符串子串 用 结果 的 路径、查询开始 加 1、做 字符串长度 用 结果 的 路径 减 查询开始 减 1。
        结果 的 路径 为 做 字符串子串 用 结果 的 路径、0、查询开始。
    完。
    返还 结果。
完。

本章 收束。
