《龙境》

引用《..·感知·ReadableStream》。

引用《..·标准·原生字符串》。

引用《..·中枢·硬链接》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·内核打印》。

引用《..·中枢·字符串替换》。

引用《..·标准·标准流》。

引用《..·中枢·高精度定时器hrtimer》。

引用《..·中枢·整数转字符串》。

最大历史数 为 500。

最大变量数 为 256。

最大代码片段数 为 64。

REPL变量 是 一种 事物，含 名称、值、类型、创建行号。

代码片段 是 一种 事物，含 名称、代码、描述。

REPL统计 是 一种 事物，含 总输入行数、总求值次数、成功次数、错误次数、命令次数、定义变量数、总用时_us。

可变 REPL_历史 为 一组：（空）。

可变 REPL_行号 为 1。

可变 REPL_运行中 为 真。

可变 REPL_变量表 为 一组：（空）。

可变 REPL_片段库 为 一组：（空）。

可变 REPL_多行缓冲 为 「」。

可变 REPL_多行模式 为 假。

可变 REPL_缩进层 为 0。

可变 REPL_统计数据 为 空。

可变 REPL_提示符 为 「Nova」。

可变 REPL_显示类型 为 真。

可变 REPL_计时模式 为 假。

REPL_初始化统计 接受 零参：
    若 REPL_统计数据 等于 空：
        REPL_统计数据 为 新建 REPL统计。
        REPL_统计数据 的 总输入行数 为 0。
        REPL_统计数据 的 总求值次数 为 0。
        REPL_统计数据 的 成功次数 为 0。
        REPL_统计数据 的 错误次数 为 0。
        REPL_统计数据 的 命令次数 为 0。
        REPL_统计数据 的 定义变量数 为 0。
        REPL_统计数据 的 总用时_us 为 0。
    完。
完。

龙境_main 接受 参数：
    做 REPL_初始化统计。
    做 打印行 用 「╔═══════════════════════════════════════╗」。
    做 打印行 用 「║  龙境 v1.0 — Nova交互式编程环境       ║」。
    做 打印行 用 「║  输入Nova代码立即执行, help查看帮助    ║」。
    做 打印行 用 「╚═══════════════════════════════════════╝」。
    做 打印行 用 「」。
    REPL_运行中 为 真。
    REPL_行号 为 1。
    做 REPL_注册内置片段。
    若 做 计数 用 参数 大于 1：
        可变 i 为 1。
        只要 i 小于 做 计数 用 参数：
            若 参数 的 第 i 项 等于 「--prompt」：
                若 i 加 1 小于 做 计数 用 参数：
                    REPL_提示符 为 参数 的 第 i 加 1 项。
                    i 为 i 加 1。
                完。
            完。
            若 参数 的 第 i 项 等于 「--no-type」：
                REPL_显示类型 为 假。
            完。
            若 参数 的 第 i 项 等于 「--timer」：
                REPL_计时模式 为 真。
            完。
            i 为 i 加 1。
        完。
    完。
    只要 REPL_运行中：
        若 REPL_多行模式：
            做 标准输出 用 「...   」。
        否则：
            做 标准输出 用 REPL_提示符 加 「[」 加 做 整数转字符串 用 REPL_行号 加 「]> 」。
        完。
        输入 为 做 读取行。
        REPL_统计数据 的 总输入行数 为 REPL_统计数据 的 总输入行数 加 1。
        若 REPL_多行模式：
            若 输入 等于 「」 或者 输入 等于 「;;」：
                REPL_多行模式 为 假。
                REPL_缩进层 为 0。
                完整代码 为 REPL_多行缓冲。
                REPL_多行缓冲 为 「」。
                做 REPL_执行并显示 用 完整代码。
                继续。
            完。
            REPL_多行缓冲 为 REPL_多行缓冲 加 「\n」 加 输入。
            REPL_缩进层 为 做 REPL_计算缩进 用 输入。
            继续。
        完。
        若 输入 等于 「exit」 或者 输入 等于 「quit」 或者 输入 等于 「q」：
            REPL_运行中 为 假。
            做 打印行 用 「龙境会话结束。共执行 」 加 做 整数转字符串 用 REPL_统计数据 的 总求值次数 加 「 次求值。」。
            做 打印行 用 「再见!」。
            继续。
        完。
        若 输入 等于 「」：
            继续。
        完。
        若 做 REPL_处理命令 用 输入：
            REPL_统计数据 的 命令次数 为 REPL_统计数据 的 命令次数 加 1。
            继续。
        完。
        若 做 字符串结尾是 用 输入、「{」 或者 做 字符串结尾是 用 输入、「\\」：
            REPL_多行模式 为 真。
            REPL_多行缓冲 为 输入。
            REPL_缩进层 为 1。
            继续。
        完。
        做 追加 用 REPL_历史、输入。
        若 做 计数 用 REPL_历史 大于 最大历史数：
            可变 新历史 为 一组：（空）。
            可变 j 为 做 计数 用 REPL_历史 减 最大历史数。
            只要 j 小于 做 计数 用 REPL_历史：
                做 追加 用 新历史、REPL_历史 的 第 j 项。
                j 为 j 加 1。
            完。
            REPL_历史 为 新历史。
        完。
        做 REPL_执行并显示 用 输入。
        REPL_行号 为 REPL_行号 加 1。
    完。
完。

REPL_执行并显示 接受 代码：
    做 REPL_初始化统计。
    REPL_统计数据 的 总求值次数 为 REPL_统计数据 的 总求值次数 加 1。
    可变 开始时间 为 0。
    若 REPL_计时模式：
        开始时间 为 做 获取滴答。
    完。
    结果 为 做 REPL_求值 用 代码。
    若 REPL_计时模式：
        用时 为 做 获取滴答 减 开始时间。
        REPL_统计数据 的 总用时_us 为 REPL_统计数据 的 总用时_us 加 用时。
        若 结果 不等于 空：
            类型标记 为 「」。
            若 REPL_显示类型：
                类型标记 为 「 : 」 加 做 REPL_推断类型 用 结果。
            完。
            做 打印行 用 「=> 」 加 做 转字符串 用 结果 加 类型标记 加 「  (」 加 做 整数转字符串 用 用时 加 「 μs)」。
        完。
    否则若 结果 不等于 空：
        类型标记 为 「」。
        若 REPL_显示类型：
            类型标记 为 「 : 」 加 做 REPL_推断类型 用 结果。
        完。
        做 打印行 用 「=> 」 加 做 转字符串 用 结果 加 类型标记。
    完。
    REPL_统计数据 的 成功次数 为 REPL_统计数据 的 成功次数 加 1。
完。

REPL_处理命令 接受 输入：
    若 输入 等于 「help」 或者 输入 等于 「?」：
        做 REPL_帮助。
        返还 真。
    完。
    若 输入 等于 「history」 或者 输入 等于 「h」：
        做 REPL_显示历史。
        返还 真。
    完。
    若 输入 等于 「vars」：
        做 REPL_显示变量。
        返还 真。
    完。
    若 输入 等于 「clear」：
        REPL_历史 为 一组：（空）。
        做 打印行 用 「[历史已清空]」。
        返还 真。
    完。
    若 输入 等于 「reset」：
        REPL_变量表 为 一组：（空）。
        做 打印行 用 「[变量已重置]」。
        返还 真。
    完。
    若 输入 等于 「stats」：
        做 REPL_显示统计。
        返还 真。
    完。
    若 输入 等于 「snippets」：
        做 REPL_显示片段。
        返还 真。
    完。
    若 输入 等于 「timer on」：
        REPL_计时模式 为 真。
        做 打印行 用 「[计时模式开启]」。
        返还 真。
    完。
    若 输入 等于 「timer off」：
        REPL_计时模式 为 假。
        做 打印行 用 「[计时模式关闭]」。
        返还 真。
    完。
    若 输入 等于 「type on」：
        REPL_显示类型 为 真。
        做 打印行 用 「[类型显示开启]」。
        返还 真。
    完。
    若 输入 等于 「type off」：
        REPL_显示类型 为 假。
        做 打印行 用 「[类型显示关闭]」。
        返还 真。
    完。
    若 做 字符串开头是 用 输入、「save 」：
        名称 为 做 字符串截取 用 输入、5、做 字符串长度 用 输入 减 5。
        若 做 计数 用 REPL_历史 大于 0：
            做 REPL_保存片段 用 名称、REPL_历史 的 第 做 计数 用 REPL_历史 减 1 项、「用户保存」。
            做 打印行 用 「[已保存片段: 」 加 名称 加 「]」。
        完。
        返还 真。
    完。
    若 做 字符串开头是 用 输入、「load 」：
        名称 为 做 字符串截取 用 输入、5、做 字符串长度 用 输入 减 5。
        代码 为 做 REPL_加载片段 用 名称。
        若 代码 不等于 空：
            做 打印行 用 「[加载片段: 」 加 名称 加 「]」。
            做 REPL_执行并显示 用 代码。
        否则：
            做 打印行 用 「[片段未找到: 」 加 名称 加 「]」。
        完。
        返还 真。
    完。
    若 做 字符串开头是 用 输入、「!」：
        索引文本 为 做 字符串截取 用 输入、1、做 字符串长度 用 输入 减 1。
        索引 为 做 字符串转整数 用 索引文本 减 1。
        若 索引 大于或等于 0 并且 索引 小于 做 计数 用 REPL_历史：
            做 打印行 用 「[重放: 」 加 REPL_历史 的 第 索引 项 加 「]」。
            做 REPL_执行并显示 用 REPL_历史 的 第 索引 项。
        否则：
            做 打印行 用 「[无效的历史索引]」。
        完。
        返还 真。
    完。
    返还 假。
完。

REPL_求值 接受 代码：
    若 做 字符串开头是 用 代码、「定义 」：
        剩余 为 做 字符串截取 用 代码、3、做 字符串长度 用 代码 减 3。
        等号位 为 做 字符串查找 用 剩余、「=」。
        若 等号位 大于 0：
            名称 为 做 字符串修剪 用 做 字符串截取 用 剩余、0、等号位。
            值文本 为 做 字符串修剪 用 做 字符串截取 用 剩余、等号位 加 1、做 字符串长度 用 剩余 减 等号位 减 1。
            做 REPL_设置变量 用 名称、值文本、「用户定义」。
            做 打印行 用 「[定义 」 加 名称 加 「 = 」 加 值文本 加 「]」。
            返还 空。
        完。
        做 打印行 用 「[定义已记录]」。
        返还 空。
    完。
    若 做 字符串包含 用 代码、「打印」：
        返还 空。
    完。
    变量值 为 做 REPL_获取变量 用 代码。
    若 变量值 不等于 空：
        返还 变量值。
    完。
    返还 代码。
完。

REPL_设置变量 接受 名称、值、类型：
    做 REPL_初始化统计。
    可变 i 为 0。
    只要 i 小于 做 计数 用 REPL_变量表：
        若 REPL_变量表 的 第 i 项 的 名称 等于 名称：
            REPL_变量表 的 第 i 项 的 值 为 值。
            REPL_变量表 的 第 i 项 的 类型 为 类型。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 REPL_变量表 大于或等于 最大变量数：
        做 打印行 用 「[警告: 变量表已满]」。
        返还。
    完。
    v 为 新建 REPL变量。
    v 的 名称 为 名称。
    v 的 值 为 值。
    v 的 类型 为 类型。
    v 的 创建行号 为 REPL_行号。
    做 追加 用 REPL_变量表、v。
    REPL_统计数据 的 定义变量数 为 REPL_统计数据 的 定义变量数 加 1。
完。

REPL_获取变量 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 REPL_变量表：
        若 REPL_变量表 的 第 i 项 的 名称 等于 名称：
            返还 REPL_变量表 的 第 i 项 的 值。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

REPL_推断类型 接受 值：
    若 值 等于 空：
        返还 「空」。
    完。
    若 值 等于 真 或者 值 等于 假：
        返还 「布尔」。
    完。
    返还 「字符串」。
完。

REPL_计算缩进 接受 行：
    可变 层 为 REPL_缩进层。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 行：
        若 做 字符串取字符 用 行、i 等于 「{」：
            层 为 层 加 1。
        完。
        若 做 字符串取字符 用 行、i 等于 「}」：
            层 为 层 减 1。
        完。
        i 为 i 加 1。
    完。
    若 层 小于 0：
        层 为 0。
    完。
    返还 层。
完。

REPL_保存片段 接受 名称、代码、描述：
    若 做 计数 用 REPL_片段库 大于或等于 最大代码片段数：
        做 打印行 用 「[片段库已满]」。
        返还。
    完。
    s 为 新建 代码片段。
    s 的 名称 为 名称。
    s 的 代码 为 代码。
    s 的 描述 为 描述。
    做 追加 用 REPL_片段库、s。
完。

REPL_加载片段 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 REPL_片段库：
        若 REPL_片段库 的 第 i 项 的 名称 等于 名称：
            返还 REPL_片段库 的 第 i 项 的 代码。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

REPL_注册内置片段 接受 零参：
    做 REPL_保存片段 用 「hello」、「打印行(\"你好,Nova世界!\")」、「Hello World示例」。
    做 REPL_保存片段 用 「fib」、「函数 fib(n) ：若 (n <= 1) ：返还 n } 返回 fib(n-1) + fib(n-2) }」、「斐波那契」。
    做 REPL_保存片段 用 「loop」、「定义 可变 i = 0; 当 (i < 10) { 打印行(整数转字符串(i)); i = i + 1 }」、「循环示例」。
完。

REPL_帮助 接受 零参：
    做 打印行 用 「╔═══════════════════════════════════════╗」。
    做 打印行 用 「║          龙境命令参考                  ║」。
    做 打印行 用 「╠═══════════════════════════════════════╣」。
    做 打印行 用 「║  help, ?     — 显示此帮助              ║」。
    做 打印行 用 「║  history, h  — 显示输入历史            ║」。
    做 打印行 用 「║  vars        — 显示所有变量            ║」。
    做 打印行 用 「║  clear       — 清空历史记录            ║」。
    做 打印行 用 「║  reset       — 重置变量环境            ║」。
    做 打印行 用 「║  stats       — 显示会话统计            ║」。
    做 打印行 用 「║  snippets    — 显示代码片段            ║」。
    做 打印行 用 「║  save <名称> — 保存上一条为片段        ║」。
    做 打印行 用 「║  load <名称> — 加载并执行片段          ║」。
    做 打印行 用 「║  !<编号>     — 重放历史命令            ║」。
    做 打印行 用 「║  timer on/off — 计时模式               ║」。
    做 打印行 用 「║  type on/off — 类型显示                ║」。
    做 打印行 用 「║  exit, quit  — 退出龙境                ║」。
    做 打印行 用 「╠═══════════════════════════════════════╣」。
    做 打印行 用 「║  多行输入: 以 { 结尾进入,空行结束      ║」。
    做 打印行 用 「║  直接输入Nova代码即可执行              ║」。
    做 打印行 用 「╚═══════════════════════════════════════╝」。
完。

REPL_显示历史 接受 零参：
    若 做 计数 用 REPL_历史 等于 0：
        做 打印行 用 「[历史为空]」。
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 REPL_历史：
        做 打印行 用 「  」 加 做 整数转字符串 用 i 加 1 加 「: 」 加 REPL_历史 的 第 i 项。
        i 为 i 加 1。
    完。
完。

REPL_显示变量 接受 零参：
    若 做 计数 用 REPL_变量表 等于 0：
        做 打印行 用 「[无变量]」。
        返还。
    完。
    做 打印行 用 「── 变量表 ──」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 REPL_变量表：
        v 为 REPL_变量表 的 第 i 项。
        做 打印行 用 「  」 加 v 的 名称 加 「 = 」 加 做 转字符串 用 v 的 值 加 「  [行 」 加 做 整数转字符串 用 v 的 创建行号 加 「]」。
        i 为 i 加 1。
    完。
完。

REPL_显示片段 接受 零参：
    若 做 计数 用 REPL_片段库 等于 0：
        做 打印行 用 「[无片段]」。
        返还。
    完。
    做 打印行 用 「── 代码片段 ──」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 REPL_片段库：
        s 为 REPL_片段库 的 第 i 项。
        做 打印行 用 「  」 加 s 的 名称 加 「 — 」 加 s 的 描述。
        i 为 i 加 1。
    完。
完。

REPL_显示统计 接受 零参：
    做 REPL_初始化统计。
    做 打印行 用 「=== 龙境会话统计 ===」。
    做 打印行 用 「输入行数: 」 加 做 整数转字符串 用 REPL_统计数据 的 总输入行数。
    做 打印行 用 「求值次数: 」 加 做 整数转字符串 用 REPL_统计数据 的 总求值次数。
    做 打印行 用 「成功: 」 加 做 整数转字符串 用 REPL_统计数据 的 成功次数。
    做 打印行 用 「错误: 」 加 做 整数转字符串 用 REPL_统计数据 的 错误次数。
    做 打印行 用 「命令: 」 加 做 整数转字符串 用 REPL_统计数据 的 命令次数。
    做 打印行 用 「变量: 」 加 做 整数转字符串 用 做 计数 用 REPL_变量表。
    做 打印行 用 「历史: 」 加 做 整数转字符串 用 做 计数 用 REPL_历史 加 「 条」。
    做 打印行 用 「片段: 」 加 做 整数转字符串 用 做 计数 用 REPL_片段库 加 「 个」。
    若 REPL_计时模式 并且 REPL_统计数据 的 总求值次数 大于 0：
        做 打印行 用 「平均用时: 」 加 做 整数转字符串 用 REPL_统计数据 的 总用时_us 除以 REPL_统计数据 的 总求值次数 加 「 μs」。
    完。
完。

REPL_完整统计 接受 零参：
    做 REPL_初始化统计。
    返还 一组：REPL_统计数据 的 总输入行数、REPL_统计数据 的 总求值次数、REPL_统计数据 的 成功次数、REPL_统计数据 的 错误次数、REPL_统计数据 的 命令次数、REPL_统计数据 的 定义变量数、做 计数 用 REPL_历史、做 计数 用 REPL_片段库。
完。

REPL_重置统计 接受 零参：
    REPL_统计数据 的 总输入行数 为 0。
    REPL_统计数据 的 总求值次数 为 0。
    REPL_统计数据 的 成功次数 为 0。
    REPL_统计数据 的 错误次数 为 0。
    REPL_统计数据 的 命令次数 为 0。
    REPL_统计数据 的 定义变量数 为 0。
    REPL_统计数据 的 总用时_us 为 0。
完。

REPL_获取求值数 接受 零参：
    做 REPL_初始化统计。
    返还 REPL_统计数据 的 总求值次数。
完。

REPL_获取错误数 接受 零参：
    做 REPL_初始化统计。
    返还 REPL_统计数据 的 错误次数。
完。

REPL_获取变量数 接受 零参：
    返还 做 计数 用 REPL_变量表。
完。

REPL_获取片段数 接受 零参：
    返还 做 计数 用 REPL_片段库。
完。

REPL_执行历史 接受 编号：
    若 编号 小于 1 或者 编号 大于 做 计数 用 REPL_历史：
        做 打印行 用 「[无效历史编号]」。
        返还 空。
    完。
    返还 REPL_历史 的 第 编号 减 1 项。
完。

REPL_清空 接受 零参：
    REPL_历史 为 一组：（空）。
    REPL_变量表 为 一组：（空）。
    REPL_行号 为 0。
    REPL_缩进层 为 0。
    做 REPL_重置统计。
完。

REPL_分析输入 接受 行：
    若 做 字符串开头是 用 行、「help」 或者 做 字符串开头是 用 行、「?」：
        返还 0。
    完。
    若 做 字符串开头是 用 行、「history」 或者 做 字符串开头是 用 行、「vars」：
        返还 0。
    完。
    若 做 字符串开头是 用 行、「clear」 或者 做 字符串开头是 用 行、「reset」：
        返还 0。
    完。
    若 做 字符串开头是 用 行、「stats」 或者 做 字符串开头是 用 行、「snippets」：
        返还 0。
    完。
    若 做 字符串开头是 用 行、「save」 或者 做 字符串开头是 用 行、「load」：
        返还 0。
    完。
    若 做 字符串开头是 用 行、「exit」 或者 做 字符串开头是 用 行、「quit」：
        返还 0。
    完。
    若 做 字符串开头是 用 行、「定义」：
        返还 1。
    完。
    若 做 字符串开头是 用 行、「函数」：
        返还 2。
    完。
    返还 3。
完。

REPL_打印欢迎 接受 零参：
    做 打印行 用 「╭─────────────────────────────────────╮」。
    做 打印行 用 「│   龙境 — Nova交互式开发环境      │」。
    做 打印行 用 「│   输入 help 查看命令             │」。
    做 打印行 用 「╰─────────────────────────────────────╯」。
完。

REPL_生成提示符 接受 零参：
    可变 结果 为 「龙境」。
    若 REPL_缩进层 大于 0：
        可变 i 为 0。
        只要 i 小于 REPL_缩进层：
            结果 为 做 字符串拼接 用 结果、「..」。
            i 为 i 加 1。
        完。
    完。
    返还 做 字符串拼接 用 结果、「> 」。
完。

REPL_查找变量类型 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 REPL_变量表：
        若 REPL_变量表 的 第 i 项 的 名称 等于 名称：
            返还 REPL_变量表 的 第 i 项 的 类型。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

REPL_获取历史长度 接受 零参：
    返还 做 计数 用 REPL_历史。
完。

REPL_获取输入行数 接受 零参：
    做 REPL_初始化统计。
    返还 REPL_统计数据 的 总输入行数。
完。

REPL_获取命令数 接受 零参：
    做 REPL_初始化统计。
    返还 REPL_统计数据 的 命令次数。
完。

REPL_获取成功率 接受 零参：
    做 REPL_初始化统计。
    若 REPL_统计数据 的 总求值次数 等于 0：
        返还 0。
    完。
    返还 REPL_统计数据 的 成功次数 乘 1000 除以 REPL_统计数据 的 总求值次数。
完。

可变 AI_龙境_调用数 为 0。

可变 AI_龙境_异常数 为 0。

AI_龙境_监控 接受 零参：
    AI_龙境_调用数 为 AI_龙境_调用数 加 1。
    返还 AI_龙境_异常数 等于 0。
完。

AI_龙境_健康检查 接受 零参：
    若 AI_龙境_调用数 大于 0 并且 AI_龙境_异常数 乘 100 除以 AI_龙境_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
