《表达式求值》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

值类型_未定义 为 0。

值类型_空 为 1。

值类型_数字 为 2。

值类型_字符串 为 3。

值类型_布尔 为 4。

值类型_对象 为 5。

最大变量 为 128。

脚本值 是 一种 事物，含 类型、数值、字串哈希。

可变 变量表 为 0。

可变 变量数 为 0。

可变 求值次数 为 0。

表达式_初始化 接受 零参：
    变量表 为 做 堆_分配 用 最大变量 乘 24。
    变量数 为 0。
完。

表达式_创建数字 接受 值：
    结果 为 做 堆_分配 用 脚本值。
    结果 的 类型 为 值类型_数字。
    结果 的 数值 为 值。
    结果 的 字串哈希 为 0。
    返还 结果。
完。

表达式_创建字串 接受 哈希：
    结果 为 做 堆_分配 用 脚本值。
    结果 的 类型 为 值类型_字符串。
    结果 的 数值 为 0。
    结果 的 字串哈希 为 哈希。
    返还 结果。
完。

表达式_创建布尔 接受 值：
    结果 为 做 堆_分配 用 脚本值。
    结果 的 类型 为 值类型_布尔。
    结果 的 数值 为 值。
    结果 的 字串哈希 为 0。
    返还 结果。
完。

表达式_创建未定义 接受 零参：
    结果 为 做 堆_分配 用 脚本值。
    结果 的 类型 为 值类型_未定义。
    结果 的 数值 为 0。
    结果 的 字串哈希 为 0。
    返还 结果。
完。

表达式_创建空 接受 零参：
    结果 为 做 堆_分配 用 脚本值。
    结果 的 类型 为 值类型_空。
    结果 的 数值 为 0。
    结果 的 字串哈希 为 0。
    返还 结果。
完。

表达式_设变量 接受 名称哈希、值：
    可变 序号 为 0。
    只要 序号 小于 变量数：
        若 做 内存_读64 用 变量表 加 序号 乘 24 等于 名称哈希：
            做 内存_写64 用 变量表 加 序号 乘 24 加 8、值。
            返还。
        完。
        序号 为 序号 加 1。
    完。
    若 变量数 大于或等于 最大变量：
        返还。
    完。
    做 内存_写64 用 变量表 加 变量数 乘 24、名称哈希。
    做 内存_写64 用 变量表 加 变量数 乘 24 加 8、值。
    变量数 为 变量数 加 1。
完。

表达式_获取变量 接受 名称哈希：
    可变 序号 为 0。
    只要 序号 小于 变量数：
        若 做 内存_读64 用 变量表 加 序号 乘 24 等于 名称哈希：
            返还 做 内存_读64 用 变量表 加 序号 乘 24 加 8。
        完。
        序号 为 序号 加 1。
    完。
    返还 做 表达式_创建未定义。
完。

表达式_加法 接受 左、右：
    若 左 等于 0 或者 右 等于 0：
        返还 做 表达式_创建未定义。
    完。
    若 左 的 类型 等于 值类型_数字 并且 右 的 类型 等于 值类型_数字：
        返还 做 表达式_创建数字 用 左 的 数值 加 右 的 数值。
    完。
    返还 做 表达式_创建数字 用 0。
完。

表达式_减法 接受 左、右：
    若 左 等于 0 或者 右 等于 0：
        返还 做 表达式_创建未定义。
    完。
    返还 做 表达式_创建数字 用 做 表达式_转数字 用 左 减 做 表达式_转数字 用 右。
完。

表达式_乘法 接受 左、右：
    若 左 等于 0 或者 右 等于 0：
        返还 做 表达式_创建未定义。
    完。
    返还 做 表达式_创建数字 用 做 表达式_转数字 用 左 乘 做 表达式_转数字 用 右。
完。

表达式_除法 接受 左、右：
    若 左 等于 0 或者 右 等于 0：
        返还 做 表达式_创建未定义。
    完。
    除数 为 做 表达式_转数字 用 右。
    若 除数 等于 0：
        返还 做 表达式_创建未定义。
    完。
    返还 做 表达式_创建数字 用 做 表达式_转数字 用 左 除以 除数。
完。

表达式_取余 接受 左、右：
    若 左 等于 0 或者 右 等于 0：
        返还 做 表达式_创建未定义。
    完。
    除数 为 做 表达式_转数字 用 右。
    若 除数 等于 0：
        返还 做 表达式_创建未定义。
    完。
    返还 做 表达式_创建数字 用 做 表达式_转数字 用 左 取余于 除数。
完。

表达式_等于 接受 左、右：
    若 左 等于 0 或者 右 等于 0：
        返还 做 表达式_创建布尔 用 左 等于 右。
    完。
    若 左 的 类型 不等于 右 的 类型：
        返还 做 表达式_创建布尔 用 假。
    完。
    若 左 的 类型 等于 值类型_数字：
        返还 做 表达式_创建布尔 用 左 的 数值 等于 右 的 数值。
    完。
    若 左 的 类型 等于 值类型_字符串：
        返还 做 表达式_创建布尔 用 左 的 字串哈希 等于 右 的 字串哈希。
    完。
    返还 做 表达式_创建布尔 用 假。
完。

表达式_小于 接受 左、右：
    返还 做 表达式_创建布尔 用 做 表达式_转数字 用 左 小于 做 表达式_转数字 用 右。
完。

表达式_大于 接受 左、右：
    返还 做 表达式_创建布尔 用 做 表达式_转数字 用 左 大于 做 表达式_转数字 用 右。
完。

表达式_取反 接受 值：
    若 值 等于 0：
        返还 做 表达式_创建布尔 用 真。
    完。
    返还 做 表达式_创建布尔 用 非 做 表达式_转布尔 用 值。
完。

表达式_负号 接受 值：
    若 值 等于 0：
        返还 做 表达式_创建数字 用 0。
    完。
    返还 做 表达式_创建数字 用 0 减 做 表达式_转数字 用 值。
完。

表达式_转数字 接受 值：
    若 值 等于 0：
        返还 0。
    完。
    若 值 的 类型 等于 值类型_数字：
        返还 值 的 数值。
    完。
    若 值 的 类型 等于 值类型_布尔：
        返还 值 的 数值。
    完。
    返还 0。
完。

表达式_转布尔 接受 值：
    若 值 等于 0：
        返还 假。
    完。
    若 值 的 类型 等于 值类型_未定义 或者 值 的 类型 等于 值类型_空：
        返还 假。
    完。
    若 值 的 类型 等于 值类型_数字：
        返还 值 的 数值 不等于 0。
    完。
    若 值 的 类型 等于 值类型_布尔：
        返还 值 的 数值 不等于 0。
    完。
    若 值 的 类型 等于 值类型_字符串：
        返还 值 的 字串哈希 不等于 0。
    完。
    返还 真。
完。

表达式_类型名 接受 值：
    若 值 等于 0：
        返还 「未定义」。
    完。
    若 值 的 类型 等于 值类型_未定义：
        返还 「未定义」。
    完。
    若 值 的 类型 等于 值类型_空：
        返还 「对象」。
    完。
    若 值 的 类型 等于 值类型_数字：
        返还 「数字」。
    完。
    若 值 的 类型 等于 值类型_字符串：
        返还 「字符串」。
    完。
    若 值 的 类型 等于 值类型_布尔：
        返还 「布尔」。
    完。
    若 值 的 类型 等于 值类型_对象：
        返还 「对象」。
    完。
    返还 「未知」。
完。

表达式_完整统计 接受 零参：
    返还 一组：变量数、求值次数。
完。

可变 AI_表达式求值_调用数 为 0。

可变 AI_表达式求值_异常数 为 0。

AI_表达式求值_监控 接受 零参：
    AI_表达式求值_调用数 为 AI_表达式求值_调用数 加 1。
    返还 AI_表达式求值_异常数 等于 0。
完。

AI_表达式求值_健康检查 接受 零参：
    若 AI_表达式求值_调用数 大于 0 并且 AI_表达式求值_异常数 乘 100 除以 AI_表达式求值_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
