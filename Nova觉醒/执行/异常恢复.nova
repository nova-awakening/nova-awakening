《异常恢复》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·编排初始化》。

引用《.·真实点击》。

引用《..·外联·序列工具》。

引用《..·中枢·内核打印》。

引用《.·视觉识别引擎》。

引用《.·透明代理》。

引用《.·统一破解入》。

引用《..·中枢·高精度定时器hrtimer》。

引用《..·中枢·整数转字符串》。

恢复_最大重试 为 5。

恢复_最大历史 为 64。

恢复_基础延迟_ms 为 1000。

恢复_最大延迟_ms 为 30000。

错误类型_网络 为 1。

错误类型_超时 为 2。

错误类型_验证码 为 3。

错误类型_弹窗 为 4。

错误类型_元素未找到 为 5。

错误类型_页面崩溃 为 6。

错误类型_封禁 为 7。

错误类型_未知 为 99。

断路器_关闭 为 0。

断路器_打开 为 1。

断路器_半开 为 2。

断路器_失败阈值 为 5。

断路器_恢复时间_ms 为 60000。

可变 编排_任务列表 为 一组：（空）。

可变 编排_总任务数 为 0。

可变 编排_总成功数 为 0。

恢复历史项 是 一种 事物，含 任务ID、错误类型、错误描述、恢复策略、成功、时间戳、重试次数。

断路器 是 一种 事物，含 状态、连续失败数、上次失败时间、上次成功时间、总调用数、失败率。

异常恢复统计 是 一种 事物，含 恢复尝试次数、恢复成功次数、恢复失败次数、验证码处理次数、弹窗关闭次数、IP轮换次数、页面刷新次数、超时恢复次数、断路器触发次数、断路器恢复次数、降级次数、AI诊断次数。

可变 全局_恢复统计 为 空。

可变 全局_恢复历史 为 一组：（空）。

可变 全局_恢复历史数 为 0。

可变 全局_断路器 为 空。

恢复统计_初始化 接受 零参：
    若 全局_恢复统计 等于 空：
        全局_恢复统计 为 新建 异常恢复统计。
        全局_恢复统计 的 恢复尝试次数 为 0。
        全局_恢复统计 的 恢复成功次数 为 0。
        全局_恢复统计 的 恢复失败次数 为 0。
        全局_恢复统计 的 验证码处理次数 为 0。
        全局_恢复统计 的 弹窗关闭次数 为 0。
        全局_恢复统计 的 IP轮换次数 为 0。
        全局_恢复统计 的 页面刷新次数 为 0。
        全局_恢复统计 的 超时恢复次数 为 0。
        全局_恢复统计 的 断路器触发次数 为 0。
        全局_恢复统计 的 断路器恢复次数 为 0。
        全局_恢复统计 的 降级次数 为 0。
        全局_恢复统计 的 AI诊断次数 为 0。
        全局_断路器 为 新建 断路器。
        全局_断路器 的 状态 为 断路器_关闭。
        全局_断路器 的 连续失败数 为 0。
        全局_断路器 的 上次失败时间 为 0。
        全局_断路器 的 上次成功时间 为 0。
        全局_断路器 的 总调用数 为 0。
        全局_断路器 的 失败率 为 0。
    完。
完。

恢复_分类错误 接受 任务、步骤：
    若 步骤 等于 空：
        返还 错误类型_未知。
    完。
    若 步骤 的 错误代码 等于 408 或者 步骤 的 错误代码 等于 504：
        返还 错误类型_超时。
    完。
    若 步骤 的 错误代码 等于 403 或者 步骤 的 错误代码 等于 429：
        返还 错误类型_封禁。
    完。
    若 步骤 的 错误代码 大于或等于 500：
        返还 错误类型_页面崩溃。
    完。
    视觉状态 为 做 视觉识别_分析页面。
    若 视觉状态 不等于 空：
        若 视觉状态 的 有验证码：
            返还 错误类型_验证码。
        完。
        若 视觉状态 的 有弹窗：
            返还 错误类型_弹窗。
        完。
    完。
    若 步骤 的 类型 等于 步骤_点击 或者 步骤 的 类型 等于 步骤_输入：
        返还 错误类型_元素未找到。
    完。
    返还 错误类型_网络。
完。

恢复_计算延迟 接受 重试次数：
    可变 延迟 为 恢复_基础延迟_ms。
    可变 i 为 0。
    只要 i 小于 重试次数：
        延迟 为 延迟 乘 2。
        i 为 i 加 1。
    完。
    若 延迟 大于 恢复_最大延迟_ms：
        延迟 为 恢复_最大延迟_ms。
    完。
    抖动 为 做 获取滴答 取余于 500 减 250。
    延迟 为 延迟 加 抖动。
    若 延迟 小于 恢复_基础延迟_ms：
        延迟 为 恢复_基础延迟_ms。
    完。
    返还 延迟。
完。

断路器_检查 接受 零参：
    做 恢复统计_初始化。
    全局_断路器 的 总调用数 为 全局_断路器 的 总调用数 加 1。
    若 全局_断路器 的 状态 等于 断路器_打开：
        已过时间 为 做 获取滴答 减 全局_断路器 的 上次失败时间。
        若 已过时间 大于 断路器_恢复时间_ms 乘 1000：
            全局_断路器 的 状态 为 断路器_半开。
            全局_恢复统计 的 断路器恢复次数 为 全局_恢复统计 的 断路器恢复次数 加 1。
            返还 真。
        完。
        返还 假。
    完。
    返还 真。
完。

断路器_记录成功 接受 零参：
    做 恢复统计_初始化。
    全局_断路器 的 连续失败数 为 0。
    全局_断路器 的 上次成功时间 为 做 获取滴答。
    若 全局_断路器 的 状态 等于 断路器_半开：
        全局_断路器 的 状态 为 断路器_关闭。
    完。
完。

断路器_记录失败 接受 零参：
    做 恢复统计_初始化。
    全局_断路器 的 连续失败数 为 全局_断路器 的 连续失败数 加 1。
    全局_断路器 的 上次失败时间 为 做 获取滴答。
    若 全局_断路器 的 连续失败数 大于或等于 断路器_失败阈值：
        全局_断路器 的 状态 为 断路器_打开。
        全局_恢复统计 的 断路器触发次数 为 全局_恢复统计 的 断路器触发次数 加 1。
    完。
完。

任务编排_异常恢复 接受 任务、步骤：
    做 恢复统计_初始化。
    全局_恢复统计 的 恢复尝试次数 为 全局_恢复统计 的 恢复尝试次数 加 1。
    做 内核信息 用 「任务编排: 异常恢复, 重试#」 加 做 整数转字符串 用 任务 的 重试次数。
    若 非 做 断路器_检查：
        做 内核信息 用 「任务编排: 断路器已打开, 跳过恢复」。
        全局_恢复统计 的 恢复失败次数 为 全局_恢复统计 的 恢复失败次数 加 1。
        做 恢复_记录历史 用 任务、错误类型_未知、「断路器阻止」、「跳过」、假。
        返还。
    完。
    若 任务 的 重试次数 大于或等于 恢复_最大重试：
        做 内核信息 用 「任务编排: 已达最大重试次数, 降级处理」。
        全局_恢复统计 的 降级次数 为 全局_恢复统计 的 降级次数 加 1。
        做 恢复_降级处理 用 任务。
        做 断路器_记录失败。
        返还。
    完。
    错误类型 为 做 恢复_分类错误 用 任务、步骤。
    可变 恢复成功 为 假。
    若 错误类型 等于 错误类型_验证码：
        恢复成功 为 做 恢复_处理验证码。
    否则若 错误类型 等于 错误类型_弹窗：
        恢复成功 为 做 恢复_关闭弹窗。
    否则若 错误类型 等于 错误类型_超时：
        恢复成功 为 做 恢复_处理超时 用 任务。
    否则若 错误类型 等于 错误类型_封禁：
        恢复成功 为 做 恢复_处理封禁 用 任务。
    否则若 错误类型 等于 错误类型_元素未找到：
        恢复成功 为 做 恢复_处理元素未找到 用 任务、步骤。
    否则若 错误类型 等于 错误类型_页面崩溃：
        恢复成功 为 做 恢复_处理页面崩溃 用 任务。
    否则：
        恢复成功 为 做 恢复_通用重试 用 任务。
    完。
    若 恢复成功：
        全局_恢复统计 的 恢复成功次数 为 全局_恢复统计 的 恢复成功次数 加 1。
        做 断路器_记录成功。
    否则：
        全局_恢复统计 的 恢复失败次数 为 全局_恢复统计 的 恢复失败次数 加 1。
        做 断路器_记录失败。
    完。
    策略名 为 做 恢复_错误类型名 用 错误类型。
    做 恢复_记录历史 用 任务、错误类型、「」、策略名、恢复成功。
完。

恢复_处理验证码 接受 零参：
    做 恢复统计_初始化。
    全局_恢复统计 的 验证码处理次数 为 全局_恢复统计 的 验证码处理次数 加 1。
    视觉状态 为 做 视觉识别_分析页面。
    若 视觉状态 的 有验证码 并且 DOM_当前文档 不等于 空：
        做 验证破解_自动处理 用 DOM_当前文档。
        返还 真。
    完。
    返还 假。
完。

恢复_关闭弹窗 接受 零参：
    做 恢复统计_初始化。
    全局_恢复统计 的 弹窗关闭次数 为 全局_恢复统计 的 弹窗关闭次数 加 1。
    按钮文本 为 一组：「关闭」、「Close」、「Accept」、「确定」、「OK」、「Dismiss」、「取消」、「拒绝」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 按钮文本：
        关闭 为 做 视觉识别_查找按钮 用 按钮文本 的 第 i 项。
        若 关闭 不等于 空：
            做 行为仿真_点击 用 关闭 的 x 加 关闭 的 宽 除以 2、关闭 的 y 加 关闭 的 高 除以 2。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    做 行为仿真_按键 用 「Escape」。
    返还 真。
完。

恢复_处理超时 接受 任务：
    做 恢复统计_初始化。
    全局_恢复统计 的 超时恢复次数 为 全局_恢复统计 的 超时恢复次数 加 1。
    延迟 为 做 恢复_计算延迟 用 任务 的 重试次数。
    做 等待_毫秒 用 延迟。
    全局_恢复统计 的 页面刷新次数 为 全局_恢复统计 的 页面刷新次数 加 1。
    做 行为仿真_刷新。
    返还 真。
完。

恢复_处理封禁 接受 任务：
    做 恢复统计_初始化。
    全局_恢复统计 的 IP轮换次数 为 全局_恢复统计 的 IP轮换次数 加 1。
    做 IP隐匿_轮换。
    延迟 为 做 恢复_计算延迟 用 任务 的 重试次数。
    做 等待_毫秒 用 延迟。
    返还 真。
完。

恢复_处理元素未找到 接受 任务、步骤：
    做 等待_毫秒 用 2000。
    视觉状态 为 做 视觉识别_分析页面。
    若 视觉状态 不等于 空 并且 视觉状态 的 页面已加载：
        返还 真。
    完。
    全局_恢复统计 的 页面刷新次数 为 全局_恢复统计 的 页面刷新次数 加 1。
    做 行为仿真_刷新。
    做 等待_毫秒 用 3000。
    返还 真。
完。

恢复_处理页面崩溃 接受 任务：
    全局_恢复统计 的 页面刷新次数 为 全局_恢复统计 的 页面刷新次数 加 1。
    做 行为仿真_刷新。
    做 等待_毫秒 用 5000。
    返还 真。
完。

恢复_通用重试 接受 任务：
    延迟 为 做 恢复_计算延迟 用 任务 的 重试次数。
    做 等待_毫秒 用 延迟。
    返还 真。
完。

恢复_降级处理 接受 任务：
    做 内核信息 用 「任务编排: 降级 - 任务 」 加 做 整数转字符串 用 任务 的 ID 加 「 标记为失败」。
    任务 的 状态 为 「失败」。
完。

恢复_AI诊断 接受 任务、步骤：
    做 恢复统计_初始化。
    全局_恢复统计 的 AI诊断次数 为 全局_恢复统计 的 AI诊断次数 加 1。
    描述 为 「任务编排异常诊断: 任务ID=」 加 做 整数转字符串 用 任务 的 ID 加 「 重试=」 加 做 整数转字符串 用 任务 的 重试次数 加 「 错误类型=」 加 做 整数转字符串 用 做 恢复_分类错误 用 任务、步骤。
    建议 为 做 智能_接口_推理 用 描述。
    返还 建议。
完。

恢复_错误类型名 接受 类型：
    若 类型 等于 错误类型_网络：
        返还 「网络」。
    完。
    若 类型 等于 错误类型_超时：
        返还 「超时」。
    完。
    若 类型 等于 错误类型_验证码：
        返还 「验证码」。
    完。
    若 类型 等于 错误类型_弹窗：
        返还 「弹窗」。
    完。
    若 类型 等于 错误类型_元素未找到：
        返还 「元素未找到」。
    完。
    若 类型 等于 错误类型_页面崩溃：
        返还 「页面崩溃」。
    完。
    若 类型 等于 错误类型_封禁：
        返还 「封禁」。
    完。
    返还 「未知」。
完。

恢复_记录历史 接受 任务、错误类型、错误描述、策略、成功：
    若 全局_恢复历史数 大于或等于 恢复_最大历史：
        可变 i 为 0。
        只要 i 小于 全局_恢复历史数 减 1：
            全局_恢复历史 的 第 i 项 为 全局_恢复历史 的 第 i 加 1 项。
            i 为 i 加 1。
        完。
        全局_恢复历史数 为 全局_恢复历史数 减 1。
    完。
    项 为 新建 恢复历史项。
    项 的 任务ID 为 任务 的 ID。
    项 的 错误类型 为 错误类型。
    项 的 错误描述 为 错误描述。
    项 的 恢复策略 为 策略。
    项 的 成功 为 成功。
    项 的 时间戳 为 做 获取滴答。
    项 的 重试次数 为 任务 的 重试次数。
    若 全局_恢复历史数 小于 做 计数 用 全局_恢复历史：
        全局_恢复历史 的 第 全局_恢复历史数 项 为 项。
    否则：
        做 追加 用 全局_恢复历史、项。
    完。
    全局_恢复历史数 为 全局_恢复历史数 加 1。
完。

任务编排_快速导航 接受 url：
    id 为 做 任务编排_创建任务 用 「导航」、url。
    步骤 为 新建 任务步骤。
    步骤 的 类型 为 步骤_导航。
    步骤 的 URL 为 url。
    做 任务编排_添加步骤 用 id、步骤。
    返还 做 任务编排_执行 用 id。
完。

任务编排_快速登录 接受 url、用户名、密码：
    id 为 做 任务编排_创建任务 用 「自动登录」、url。
    s1 为 新建 任务步骤。
    s1 的 类型 为 步骤_导航。
    s1 的 URL 为 url。
    做 任务编排_添加步骤 用 id、s1。
    s2 为 新建 任务步骤。
    s2 的 类型 为 步骤_等待。
    s2 的 等待时间 为 2000。
    做 任务编排_添加步骤 用 id、s2。
    s3 为 新建 任务步骤。
    s3 的 类型 为 步骤_输入。
    s3 的 文字 为 用户名。
    s3 的 定位方式 为 定位_视觉类型。
    s3 的 选择器 为 「username」。
    做 任务编排_添加步骤 用 id、s3。
    s4 为 新建 任务步骤。
    s4 的 类型 为 步骤_输入。
    s4 的 文字 为 密码。
    s4 的 定位方式 为 定位_视觉类型。
    s4 的 选择器 为 「password」。
    做 任务编排_添加步骤 用 id、s4。
    s5 为 新建 任务步骤。
    s5 的 类型 为 步骤_点击。
    s5 的 定位方式 为 定位_视觉文字。
    s5 的 选择器 为 「登录」。
    做 任务编排_添加步骤 用 id、s5。
    s6 为 新建 任务步骤。
    s6 的 类型 为 步骤_验证处理。
    做 任务编排_添加步骤 用 id、s6。
    返还 做 任务编排_执行 用 id。
完。

任务编排_快速截图 接受 url、保存路径：
    id 为 做 任务编排_创建任务 用 「截图」、url。
    s1 为 新建 任务步骤。
    s1 的 类型 为 步骤_导航。
    s1 的 URL 为 url。
    做 任务编排_添加步骤 用 id、s1。
    s2 为 新建 任务步骤。
    s2 的 类型 为 步骤_等待。
    s2 的 等待时间 为 3000。
    做 任务编排_添加步骤 用 id、s2。
    s3 为 新建 任务步骤。
    s3 的 类型 为 步骤_截图。
    s3 的 保存路径 为 保存路径。
    做 任务编排_添加步骤 用 id、s3。
    返还 做 任务编排_执行 用 id。
完。

任务编排_查找 接受 任务ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 编排_任务列表：
        若 编排_任务列表 的 第 i 项 的 ID 等于 任务ID：
            返还 编排_任务列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

任务编排_统计 接受 零参：
    返还 一组：编排_总任务数、编排_总成功数、做 计数 用 编排_任务列表。
完。

异常恢复_打印统计 接受 零参：
    做 恢复统计_初始化。
    做 打印行 用 「=== 异常恢复统计 ===」。
    做 打印行 用 「恢复尝试: 」 加 做 整数转字符串 用 全局_恢复统计 的 恢复尝试次数。
    做 打印行 用 「恢复成功: 」 加 做 整数转字符串 用 全局_恢复统计 的 恢复成功次数。
    做 打印行 用 「恢复失败: 」 加 做 整数转字符串 用 全局_恢复统计 的 恢复失败次数。
    做 打印行 用 「验证码处理: 」 加 做 整数转字符串 用 全局_恢复统计 的 验证码处理次数。
    做 打印行 用 「弹窗关闭: 」 加 做 整数转字符串 用 全局_恢复统计 的 弹窗关闭次数。
    做 打印行 用 「IP轮换: 」 加 做 整数转字符串 用 全局_恢复统计 的 IP轮换次数。
    做 打印行 用 「页面刷新: 」 加 做 整数转字符串 用 全局_恢复统计 的 页面刷新次数。
    做 打印行 用 「超时恢复: 」 加 做 整数转字符串 用 全局_恢复统计 的 超时恢复次数。
    做 打印行 用 「断路器触发: 」 加 做 整数转字符串 用 全局_恢复统计 的 断路器触发次数。
    做 打印行 用 「断路器恢复: 」 加 做 整数转字符串 用 全局_恢复统计 的 断路器恢复次数。
    做 打印行 用 「降级: 」 加 做 整数转字符串 用 全局_恢复统计 的 降级次数。
    做 打印行 用 「AI诊断: 」 加 做 整数转字符串 用 全局_恢复统计 的 AI诊断次数。
    做 打印行 用 「恢复历史: 」 加 做 整数转字符串 用 全局_恢复历史数 加 「 条」。
    做 打印行 用 「断路器状态: 」 加 做 整数转字符串 用 全局_断路器 的 状态。
完。

恢复_策略评分 接受 任务、步骤：
    错误类型 为 做 恢复_分类错误 用 任务、步骤。
    可变 重试分 为 0。
    可变 刷新分 为 0。
    可变 降级分 为 0。
    若 错误类型 等于 错误类型_网络：
        重试分 为 80。
    完。
    若 错误类型 等于 错误类型_超时：
        重试分 为 70。
    完。
    若 错误类型 等于 错误类型_页面崩溃：
        刷新分 为 90。
    完。
    若 错误类型 等于 错误类型_封禁：
        降级分 为 95。
    完。
    若 错误类型 等于 错误类型_元素未找到：
        刷新分 为 60。
        重试分 为 50。
    完。
    若 任务 的 重试次数 大于 2：
        重试分 为 重试分 除以 2。
    完。
    若 任务 的 重试次数 大于 5：
        降级分 为 100。
    完。
    返还 一组：重试分、刷新分、降级分。
完。

恢复_自适应延迟 接受 重试次数、基础延迟：
    可变 延迟 为 基础延迟。
    可变 i 为 0。
    只要 i 小于 重试次数 并且 i 小于 6：
        延迟 为 延迟 乘 2。
        i 为 i 加 1。
    完。
    若 延迟 大于 60000：
        延迟 为 60000。
    完。
    jitter 为 延迟 乘 (做 获取滴答 取余于 40) 除以 100。
    返还 延迟 加 jitter 减 延迟 除以 5。
完。

恢复_重置统计 接受 零参：
    做 恢复统计_初始化。
    全局_恢复统计 的 恢复尝试次数 为 0。
    全局_恢复统计 的 恢复成功次数 为 0。
    全局_恢复统计 的 恢复失败次数 为 0。
    全局_恢复统计 的 验证码处理次数 为 0。
    全局_恢复统计 的 弹窗关闭次数 为 0。
    全局_恢复统计 的 IP轮换次数 为 0。
    全局_恢复统计 的 页面刷新次数 为 0。
    全局_恢复统计 的 超时恢复次数 为 0。
    全局_恢复统计 的 断路器触发次数 为 0。
    全局_恢复统计 的 断路器恢复次数 为 0。
    全局_恢复统计 的 降级次数 为 0。
    全局_恢复统计 的 AI诊断次数 为 0。
完。

恢复_历史查询 接受 任务ID：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 全局_恢复历史数：
        若 全局_恢复历史 的 第 i 项 的 任务ID 等于 任务ID：
            做 追加 用 结果、全局_恢复历史 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

恢复_成功率 接受 零参：
    做 恢复统计_初始化。
    总 为 全局_恢复统计 的 恢复尝试次数。
    若 总 等于 0：
        返还 0。
    完。
    返还 全局_恢复统计 的 恢复成功次数 乘 100 除以 总。
完。

本章 收束。
