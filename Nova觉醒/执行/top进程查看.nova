《top进程查看》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《..·中枢·整数转字符串》。

TOP_排序_CPU 为 0。

TOP_排序_内存 为 1。

TOP_排序_PID 为 2。

TOP_排序_时间 为 3。

TOP_默认延迟 为 3。

TOP_默认显示数 为 20。

进程信息 是 一种 事物，含 PID、用户、优先级、NICE值、虚拟内存、驻留内存、共享内存、状态、CPU百分比、内存百分比、时间、命令。

系统概要 是 一种 事物，含 运行时间秒、用户数、负载1分钟、负载5分钟、负载15分钟、总进程、运行进程、睡眠进程、停止进程、僵尸进程、CPU用户、CPU系统、CPU空闲、CPU等待、内存总量、内存已用、内存空闲、内存缓存、交换总量、交换已用、交换空闲。

top_main 接受 参数：
    可变 刷新次数 为 0。
    可变 延迟秒 为 TOP_默认延迟。
    可变 排序方式 为 TOP_排序_CPU。
    可变 批处理 为 假。
    可变 显示线程 为 假。
    可变 显示数 为 TOP_默认显示数。
    可变 过滤用户 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        a 为 参数 的 第 i 项。
        若 a 等于 「-n」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            刷新次数 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-d」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            延迟秒 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-b」：
            批处理 为 真。
        否则若 a 等于 「-H」：
            显示线程 为 真。
        否则若 a 等于 「-u」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            过滤用户 为 参数 的 第 i 项。
        否则若 a 等于 「-o」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            若 参数 的 第 i 项 等于 「cpu」 或者 参数 的 第 i 项 等于 「%CPU」：
                排序方式 为 TOP_排序_CPU。
            否则若 参数 的 第 i 项 等于 「mem」 或者 参数 的 第 i 项 等于 「%MEM」：
                排序方式 为 TOP_排序_内存。
            否则若 参数 的 第 i 项 等于 「pid」 或者 参数 的 第 i 项 等于 「PID」：
                排序方式 为 TOP_排序_PID。
            否则若 参数 的 第 i 项 等于 「time」 或者 参数 的 第 i 项 等于 「TIME+」：
                排序方式 为 TOP_排序_时间。
            完。
        否则若 a 等于 「--help」 或者 a 等于 「-h」：
            做 top_显示帮助。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    可变 已刷新 为 0。
    只要 刷新次数 等于 0 或者 已刷新 小于 刷新次数：
        若 非 批处理 并且 已刷新 大于 0：
            做 标准输出 用 「\x1b[H\x1b[2J」。
        完。
        做 top_显示 用 排序方式、显示数、过滤用户。
        已刷新 为 已刷新 加 1。
        若 刷新次数 大于 0 并且 已刷新 大于或等于 刷新次数：
            跳出。
        完。
        跳出。
    完。
    返还 0。
完。

top_显示帮助 接受 零参：
    做 打印行 用 「用法: top [选项]」。
    做 打印行 用 「选项:」。
    做 打印行 用 「  -n N      刷新N次后退出」。
    做 打印行 用 「  -d N      刷新延迟秒数 (默认3)」。
    做 打印行 用 「  -b        批处理模式」。
    做 打印行 用 「  -H        显示线程」。
    做 打印行 用 「  -u 用户   仅显示指定用户进程」。
    做 打印行 用 「  -o 字段   排序字段 (cpu/mem/pid/time)」。
完。

top_显示 接受 排序方式、显示数、过滤用户：
    概要 为 做 top_获取系统概要。
    做 top_显示头部 用 概要。
    做 打印行 用 「」。
    做 top_显示任务概要 用 概要。
    做 top_显示CPU概要 用 概要。
    做 top_显示内存概要 用 概要。
    做 top_显示交换概要 用 概要。
    做 打印行 用 「」。
    做 打印行 用 「  PID USER      PR  NI    VIRT    RES    SHR S  %CPU  %MEM   TIME+ COMMAND」。
    进程列表 为 做 top_获取进程列表。
    进程列表 为 做 top_排序进程 用 进程列表、排序方式。
    可变 已显示 为 0。
    可变 k 为 0。
    只要 k 小于 做 计数 用 进程列表 并且 已显示 小于 显示数：
        p 为 进程列表 的 第 k 项。
        若 过滤用户 不等于 「」 并且 p 的 用户 不等于 过滤用户：
            k 为 k 加 1。
            继续。
        完。
        做 打印行 用 做 top_左填 用 做 整数转字符串 用 p 的 PID、5 加 「 」 加 做 top_右填 用 p 的 用户、9 加 「 」 加 做 top_左填 用 做 整数转字符串 用 p 的 优先级、3 加 「 」 加 做 top_左填 用 做 整数转字符串 用 p 的 NICE值、3 加 「 」 加 做 top_左填 用 做 top_格式化KB 用 p 的 虚拟内存、7 加 「 」 加 做 top_左填 用 做 top_格式化KB 用 p 的 驻留内存、6 加 「 」 加 做 top_左填 用 做 top_格式化KB 用 p 的 共享内存、6 加 「 」 加 p 的 状态 加 「 」 加 做 top_左填 用 做 top_格式化百分比 用 p 的 CPU百分比、5 加 「 」 加 做 top_左填 用 做 top_格式化百分比 用 p 的 内存百分比、5 加 「 」 加 做 top_格式化时间 用 p 的 时间 加 「 」 加 p 的 命令。
        已显示 为 已显示 加 1。
        k 为 k 加 1。
    完。
完。

top_显示头部 接受 概要：
    时 为 概要 的 运行时间秒 除以 3600。
    分 为 概要 的 运行时间秒 取余于 3600 除以 60。
    做 打印行 用 「top - 运行: 」 加 做 整数转字符串 用 时 加 「:」 加 做 top_左填 用 做 整数转字符串 用 分、2 加 「,  」 加 做 整数转字符串 用 概要 的 用户数 加 「 用户」 加 「,  负载: 」 加 做 top_格式化百分比 用 概要 的 负载1分钟 加 「 」 加 做 top_格式化百分比 用 概要 的 负载5分钟 加 「 」 加 做 top_格式化百分比 用 概要 的 负载15分钟。
完。

top_显示任务概要 接受 概要：
    做 打印行 用 「任务: 」 加 做 整数转字符串 用 概要 的 总进程 加 「 总计, 」 加 做 整数转字符串 用 概要 的 运行进程 加 「 运行, 」 加 做 整数转字符串 用 概要 的 睡眠进程 加 「 睡眠, 」 加 做 整数转字符串 用 概要 的 停止进程 加 「 停止, 」 加 做 整数转字符串 用 概要 的 僵尸进程 加 「 僵尸」。
完。

top_显示CPU概要 接受 概要：
    做 打印行 用 「%CPU: 」 加 做 top_格式化百分比 用 概要 的 CPU用户 加 「 用户, 」 加 做 top_格式化百分比 用 概要 的 CPU系统 加 「 系统, 」 加 做 top_格式化百分比 用 概要 的 CPU空闲 加 「 空闲, 」 加 做 top_格式化百分比 用 概要 的 CPU等待 加 「 等待IO」。
完。

top_显示内存概要 接受 概要：
    做 打印行 用 「MiB Mem: 」 加 做 整数转字符串 用 概要 的 内存总量 除以 1024 加 「 总计, 」 加 做 整数转字符串 用 概要 的 内存已用 除以 1024 加 「 已用, 」 加 做 整数转字符串 用 概要 的 内存空闲 除以 1024 加 「 空闲, 」 加 做 整数转字符串 用 概要 的 内存缓存 除以 1024 加 「 缓存」。
完。

top_显示交换概要 接受 概要：
    做 打印行 用 「MiB Swap: 」 加 做 整数转字符串 用 概要 的 交换总量 除以 1024 加 「 总计, 」 加 做 整数转字符串 用 概要 的 交换已用 除以 1024 加 「 已用, 」 加 做 整数转字符串 用 概要 的 交换空闲 除以 1024 加 「 空闲」。
完。

top_排序进程 接受 列表、排序方式：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 列表：
            可变 交换 为 假。
            若 排序方式 等于 TOP_排序_CPU 并且 列表 的 第 j 项 的 CPU百分比 大于 列表 的 第 i 项 的 CPU百分比：
                交换 为 真。
            否则若 排序方式 等于 TOP_排序_内存 并且 列表 的 第 j 项 的 内存百分比 大于 列表 的 第 i 项 的 内存百分比：
                交换 为 真。
            否则若 排序方式 等于 TOP_排序_PID 并且 列表 的 第 j 项 的 PID 小于 列表 的 第 i 项 的 PID：
                交换 为 真。
            否则若 排序方式 等于 TOP_排序_时间 并且 列表 的 第 j 项 的 时间 大于 列表 的 第 i 项 的 时间：
                交换 为 真。
            完。
            若 交换：
                临时 为 列表 的 第 i 项。
                列表 的 第 i 项 为 列表 的 第 j 项。
                列表 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 列表。
完。

top_格式化KB 接受 kb：
    若 kb 大于或等于 1048576：
        返还 做 整数转字符串 用 kb 除以 1048576 加 「g」。
    完。
    若 kb 大于或等于 1024：
        返还 做 整数转字符串 用 kb 除以 1024 加 「m」。
    完。
    返还 做 整数转字符串 用 kb。
完。

top_格式化百分比 接受 值：
    返还 做 整数转字符串 用 值 除以 10 加 「.」 加 做 整数转字符串 用 值 取余于 10。
完。

top_格式化时间 接受 秒：
    分 为 秒 除以 60。
    余秒 为 秒 取余于 60。
    返还 做 整数转字符串 用 分 加 「:」 加 做 top_左填 用 做 整数转字符串 用 余秒、2。
完。

top_左填 接受 文本、宽度：
    可变 结果 为 文本。
    只要 做 字符串长度 用 结果 小于 宽度：
        结果 为 「 」 加 结果。
    完。
    返还 结果。
完。

top_右填 接受 文本、宽度：
    可变 结果 为 文本。
    只要 做 字符串长度 用 结果 小于 宽度：
        结果 为 结果 加 「 」。
    完。
    返还 结果。
完。

top_获取系统概要 接受 零参：
    返还 新建 系统概要：运行时间秒 为 0、用户数 为 1、负载1分钟 为 5、负载5分钟 为 3、负载15分钟 为 1、总进程 为 4、运行进程 为 1、睡眠进程 为 3、停止进程 为 0、僵尸进程 为 0、CPU用户 为 20、CPU系统 为 10、CPU空闲 为 680、CPU等待 为 0、内存总量 为 262144、内存已用 为 131072、内存空闲 为 65536、内存缓存 为 65536、交换总量 为 524288、交换已用 为 0、交换空闲 为 524288。
完。

top_获取进程列表 接受 零参：
    返还 一组：新建 进程信息：PID 为 0、用户 为 「root」、优先级 为 20、NICE值 为 0、虚拟内存 为 0、驻留内存 为 0、共享内存 为 0、状态 为 「S」、CPU百分比 为 0、内存百分比 为 0、时间 为 0、命令 为 「idle」、新建 进程信息：PID 为 1、用户 为 「root」、优先级 为 20、NICE值 为 0、虚拟内存 为 4096、驻留内存 为 2048、共享内存 为 1024、状态 为 「S」、CPU百分比 为 0、内存百分比 为 1、时间 为 0、命令 为 「nova-init」、新建 进程信息：PID 为 2、用户 为 「root」、优先级 为 20、NICE值 为 0、虚拟内存 为 8192、驻留内存 为 4096、共享内存 为 2048、状态 为 「S」、CPU百分比 为 0、内存百分比 为 2、时间 为 0、命令 为 「nova-ai」、新建 进程信息：PID 为 3、用户 为 「root」、优先级 为 20、NICE值 为 0、虚拟内存 为 2048、驻留内存 为 1024、共享内存 为 512、状态 为 「R」、CPU百分比 为 5、内存百分比 为 0、时间 为 0、命令 为 「nova-shell」。
完。

可变 AI_进程查_运行指标 为 0。

可变 AI_进程查_异常指标 为 0。

AI_进程查_自诊断 接受 零参：
    AI_进程查_运行指标 为 0。
    返还 AI_进程查_运行指标。
完。

AI_进程查_优化建议 接受 零参：
    指标 为 做 AI_进程查_自诊断。
    返还 「进程查状态正常」。
完。

本章 收束。
