《平台时间》

引用《..·中枢·类型定义》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内核打印》。

调用号_时间微秒 为 300。

调用号_时间纳秒 为 301。

调用号_墙钟秒 为 302。

调用号_睡眠微秒 为 303。

调用号_性能计数 为 304。

调用号_性能频率 为 305。

调用号_进程时间 为 306。

调用号_设置定时器 为 307。

调用号_取消定时器 为 308。

最大定时器数 为 32。

定时器 是 一种 事物，含 ID、截止时间_us、间隔_us、回调、重复、活跃。

秒表 是 一种 事物，含 开始时间、分段列表、运行中。

时间统计 是 一种 事物，含 调用总数、时间查询数、睡眠总时间_us、睡眠次数、忙等总时间_us、忙等次数、定时器触发次数、定时器超时次数。

可变 启动时刻 为 0。

可变 性能频率 为 0。

可变 全局_时间统计 为 空。

可变 全局_定时器列表 为 一组：（空）。

可变 全局_定时器下个ID 为 1。

可变 时区偏移_秒 为 28800。

时间统计_初始化 接受 零参：
    若 全局_时间统计 等于 空：
        全局_时间统计 为 新建 时间统计。
        全局_时间统计 的 调用总数 为 0。
        全局_时间统计 的 时间查询数 为 0。
        全局_时间统计 的 睡眠总时间_us 为 0。
        全局_时间统计 的 睡眠次数 为 0。
        全局_时间统计 的 忙等总时间_us 为 0。
        全局_时间统计 的 忙等次数 为 0。
        全局_时间统计 的 定时器触发次数 为 0。
        全局_时间统计 的 定时器超时次数 为 0。
    完。
完。

系统调用 接受 号码、参数1、参数2、参数3、参数4、参数5：
    做 时间统计_初始化。
    全局_时间统计 的 调用总数 为 全局_时间统计 的 调用总数 加 1。
    返还 做 __内置_系统调用 用 号码、参数1、参数2、参数3、参数4、参数5。
完。

平台_初始化时间 接受 零参：
    做 时间统计_初始化。
    性能频率 为 做 系统调用 用 调用号_性能频率、0、0、0、0、0。
    若 性能频率 等于 0：
        性能频率 为 1000000。
    完。
    启动时刻 为 做 平台_获取高精度时间。
完。

平台_获取高精度时间 接受 零参：
    做 时间统计_初始化。
    全局_时间统计 的 时间查询数 为 全局_时间统计 的 时间查询数 加 1。
    返还 做 系统调用 用 调用号_时间微秒、0、0、0、0、0。
完。

平台_获取纳秒时间 接受 零参：
    做 时间统计_初始化。
    全局_时间统计 的 时间查询数 为 全局_时间统计 的 时间查询数 加 1。
    返还 做 系统调用 用 调用号_时间纳秒、0、0、0、0、0。
完。

平台_获取墙钟秒 接受 零参：
    返还 做 系统调用 用 调用号_墙钟秒、0、0、0、0、0。
完。

平台_获取单调时间 接受 零参：
    返还 做 平台_获取高精度时间。
完。

平台_运行时间微秒 接受 零参：
    返还 做 平台_获取高精度时间 减 启动时刻。
完。

平台_运行时间毫秒 接受 零参：
    返还 做 平台_运行时间微秒 除以 1000。
完。

平台_运行时间秒 接受 零参：
    返还 做 平台_运行时间微秒 除以 1000000。
完。

平台_睡眠微秒 接受 微秒：
    做 时间统计_初始化。
    若 微秒 小于或等于 0：
        返还。
    完。
    全局_时间统计 的 睡眠次数 为 全局_时间统计 的 睡眠次数 加 1。
    全局_时间统计 的 睡眠总时间_us 为 全局_时间统计 的 睡眠总时间_us 加 微秒。
    做 系统调用 用 调用号_睡眠微秒、微秒、0、0、0、0。
完。

平台_睡眠毫秒 接受 毫秒：
    做 平台_睡眠微秒 用 毫秒 乘 1000。
完。

平台_睡眠秒 接受 秒：
    做 平台_睡眠微秒 用 秒 乘 1000000。
完。

平台_忙等微秒 接受 微秒：
    做 时间统计_初始化。
    全局_时间统计 的 忙等次数 为 全局_时间统计 的 忙等次数 加 1。
    全局_时间统计 的 忙等总时间_us 为 全局_时间统计 的 忙等总时间_us 加 微秒。
    目标 为 做 平台_获取高精度时间 加 微秒。
    只要 做 平台_获取高精度时间 小于 目标：
    完。
完。

平台_忙等纳秒 接受 纳秒：
    目标 为 做 平台_获取纳秒时间 加 纳秒。
    只要 做 平台_获取纳秒时间 小于 目标：
    完。
完。

平台_性能计数 接受 零参：
    返还 做 系统调用 用 调用号_性能计数、0、0、0、0、0。
完。

平台_性能频率 接受 零参：
    返还 性能频率。
完。

平台_计时开始 接受 零参：
    返还 做 平台_性能计数。
完。

平台_计时结束 接受 开始值：
    结束 为 做 平台_性能计数。
    差值 为 结束 减 开始值。
    若 性能频率 大于 0：
        返还 差值 乘 1000000 除以 性能频率。
    完。
    返还 差值。
完。

平台_计时结束_毫秒 接受 开始值：
    返还 做 平台_计时结束 用 开始值 除以 1000。
完。

平台_进程处理器时间 接受 零参：
    返还 做 系统调用 用 调用号_进程时间、0、0、0、0、0。
完。

创建定时器 接受 间隔_us、回调、重复：
    做 时间统计_初始化。
    若 做 计数 用 全局_定时器列表 大于或等于 最大定时器数：
        返还 负 1。
    完。
    t 为 新建 定时器。
    t 的 ID 为 全局_定时器下个ID。
    全局_定时器下个ID 为 全局_定时器下个ID 加 1。
    t 的 截止时间_us 为 做 平台_获取高精度时间 加 间隔_us。
    t 的 间隔_us 为 间隔_us。
    t 的 回调 为 回调。
    t 的 重复 为 重复。
    t 的 活跃 为 真。
    做 追加 用 全局_定时器列表、t。
    返还 t 的 ID。
完。

取消定时器 接受 ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 全局_定时器列表：
        若 全局_定时器列表 的 第 i 项 的 ID 等于 ID：
            全局_定时器列表 的 第 i 项 的 活跃 为 假。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

检查定时器 接受 零参：
    做 时间统计_初始化。
    当前 为 做 平台_获取高精度时间。
    可变 i 为 0。
    只要 i 小于 做 计数 用 全局_定时器列表：
        t 为 全局_定时器列表 的 第 i 项。
        若 t 的 活跃 并且 当前 大于或等于 t 的 截止时间_us：
            全局_时间统计 的 定时器触发次数 为 全局_时间统计 的 定时器触发次数 加 1。
            若 t 的 回调 不等于 空：
                做 t 的 回调。
            完。
            若 t 的 重复：
                t 的 截止时间_us 为 当前 加 t 的 间隔_us。
            否则：
                t 的 活跃 为 假。
            完。
        完。
        i 为 i 加 1。
    完。
完。

清理过期定时器 接受 零参：
    可变 新列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 全局_定时器列表：
        若 全局_定时器列表 的 第 i 项 的 活跃：
            做 追加 用 新列表、全局_定时器列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    全局_定时器列表 为 新列表。
完。

等待截止时间 接受 截止_us：
    做 时间统计_初始化。
    当前 为 做 平台_获取高精度时间。
    若 当前 大于或等于 截止_us：
        全局_时间统计 的 定时器超时次数 为 全局_时间统计 的 定时器超时次数 加 1。
        返还 假。
    完。
    做 平台_睡眠微秒 用 截止_us 减 当前。
    返还 真。
完。

秒表_创建 接受 零参：
    sw 为 新建 秒表。
    sw 的 开始时间 为 0。
    sw 的 分段列表 为 一组：（空）。
    sw 的 运行中 为 假。
    返还 sw。
完。

秒表_启动 接受 sw：
    sw 的 开始时间 为 做 平台_获取高精度时间。
    sw 的 运行中 为 真。
完。

秒表_分段 接受 sw：
    若 非 sw 的 运行中：
        返还 0。
    完。
    当前 为 做 平台_获取高精度时间。
    分段时间 为 当前 减 sw 的 开始时间。
    做 追加 用 sw 的 分段列表、分段时间。
    返还 分段时间。
完。

秒表_停止 接受 sw：
    若 非 sw 的 运行中：
        返还 0。
    完。
    总时间 为 做 平台_获取高精度时间 减 sw 的 开始时间。
    sw 的 运行中 为 假。
    返还 总时间。
完。

秒表_重置 接受 sw：
    sw 的 开始时间 为 0。
    sw 的 分段列表 为 一组：（空）。
    sw 的 运行中 为 假。
完。

设置时区 接受 偏移_秒：
    时区偏移_秒 为 偏移_秒。
完。

墙钟_本地秒 接受 零参：
    返还 做 平台_获取墙钟秒 加 时区偏移_秒。
完。

秒转日期 接受 总秒：
    可变 剩余 为 总秒。
    天 为 剩余 除以 86400。
    剩余 为 剩余 取余于 86400。
    时 为 剩余 除以 3600。
    剩余 为 剩余 取余于 3600。
    分 为 剩余 除以 60。
    秒 为 剩余 取余于 60。
    可变 年 为 1970。
    可变 剩余天 为 天。
    只要 真：
        年天数 为 365。
        若 做 是闰年 用 年：
            年天数 为 366。
        完。
        若 剩余天 小于 年天数：
            跳出。
        完。
        剩余天 为 剩余天 减 年天数。
        年 为 年 加 1。
    完。
    月天数 为 一组：31、28、31、30、31、30、31、31、30、31、30、31。
    若 做 是闰年 用 年：
        月天数 的 第 1 项 为 29。
    完。
    可变 月 为 0。
    只要 月 小于 12 并且 剩余天 大于或等于 月天数 的 第 月 项：
        剩余天 为 剩余天 减 月天数 的 第 月 项。
        月 为 月 加 1。
    完。
    返还 一组：年、月 加 1、剩余天 加 1、时、分、秒。
完。

是闰年 接受 年：
    若 年 取余于 400 等于 0：
        返还 真。
    完。
    若 年 取余于 100 等于 0：
        返还 假。
    完。
    若 年 取余于 4 等于 0：
        返还 真。
    完。
    返还 假。
完。

格式化时间 接受 日期：
    返还 做 整数转字符串 用 日期 的 第 0 项 加 「-」 加 做 补零2 用 日期 的 第 1 项 加 「-」 加 做 补零2 用 日期 的 第 2 项 加 「 」 加 做 补零2 用 日期 的 第 3 项 加 「:」 加 做 补零2 用 日期 的 第 4 项 加 「:」 加 做 补零2 用 日期 的 第 5 项。
完。

补零2 接受 n：
    若 n 小于 10：
        返还 「0」 加 做 整数转字符串 用 n。
    完。
    返还 做 整数转字符串 用 n。
完。

获取当前时间字符串 接受 零参：
    返还 做 格式化时间 用 做 秒转日期 用 做 墙钟_本地秒。
完。

平台_微秒转毫秒 接受 微秒：
    返还 微秒 除以 1000。
完。

平台_微秒转秒 接受 微秒：
    返还 微秒 除以 1000000。
完。

平台_毫秒转微秒 接受 毫秒：
    返还 毫秒 乘 1000。
完。

平台_秒转微秒 接受 秒：
    返还 秒 乘 1000000。
完。

平台_纳秒转微秒 接受 纳秒：
    返还 纳秒 除以 1000。
完。

平台时间_完整统计 接受 零参：
    做 时间统计_初始化。
    返还 一组：全局_时间统计 的 调用总数、全局_时间统计 的 时间查询数、做 平台_运行时间微秒、性能频率、全局_时间统计 的 睡眠次数、全局_时间统计 的 忙等次数、全局_时间统计 的 定时器触发次数、做 计数 用 全局_定时器列表。
完。

平台时间_打印统计 接受 零参：
    做 时间统计_初始化。
    做 打印行 用 「=== 平台时间统计 ===」。
    做 打印行 用 「运行时间: 」 加 做 整数转字符串 用 做 平台_运行时间微秒 除以 1000000 加 「 秒」。
    做 打印行 用 「系统调用: 」 加 做 整数转字符串 用 全局_时间统计 的 调用总数 加 「 次」。
    做 打印行 用 「时间查询: 」 加 做 整数转字符串 用 全局_时间统计 的 时间查询数 加 「 次」。
    做 打印行 用 「睡眠: 」 加 做 整数转字符串 用 全局_时间统计 的 睡眠次数 加 「 次 (」 加 做 整数转字符串 用 全局_时间统计 的 睡眠总时间_us 除以 1000 加 「 ms)」。
    做 打印行 用 「忙等: 」 加 做 整数转字符串 用 全局_时间统计 的 忙等次数 加 「 次」。
    做 打印行 用 「定时器: 」 加 做 整数转字符串 用 做 计数 用 全局_定时器列表 加 「 活跃, 」 加 做 整数转字符串 用 全局_时间统计 的 定时器触发次数 加 「 触发」。
    做 打印行 用 「当前时间: 」 加 做 获取当前时间字符串。
完。

秒表_开始 接受 sw：
    sw 的 开始时间 为 做 平台_获取高精度时间。
    sw 的 运行中 为 真。
    sw 的 分段列表 为 一组：（空）。
完。

秒表_获取分段数 接受 sw：
    返还 做 计数 用 sw 的 分段列表。
完。

秒表_平均分段 接受 sw：
    若 做 计数 用 sw 的 分段列表 等于 0：
        返还 0。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 sw 的 分段列表：
        总 为 总 加 sw 的 分段列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总 除以 做 计数 用 sw 的 分段列表。
完。

平台_时间差_微秒 接受 t1、t2：
    若 t2 大于或等于 t1：
        返还 t2 减 t1。
    完。
    返还 0。
完。

平台_时间差_毫秒 接受 t1、t2：
    返还 做 平台_时间差_微秒 用 t1、t2 除以 1000。
完。

获取定时器数 接受 零参：
    返还 做 计数 用 全局_定时器列表。
完。

获取活跃定时器数 接受 零参：
    可变 活跃 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 全局_定时器列表：
        若 全局_定时器列表 的 第 i 项 的 活跃：
            活跃 为 活跃 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 活跃。
完。

平台_设置时区 接受 偏移_秒：
    时区偏移_秒 为 偏移_秒。
完。

平台_获取时区 接受 零参：
    返还 时区偏移_秒。
完。

壁钟_本地秒 接受 零参：
    返还 做 平台_获取壁钟秒 加 时区偏移_秒。
完。

平台_重置统计 接受 零参：
    若 全局_时间统计 不等于 空：
        全局_时间统计 的 调用总数 为 0。
        全局_时间统计 的 时间查询数 为 0。
        全局_时间统计 的 睡眠总时间_us 为 0。
        全局_时间统计 的 睡眠次数 为 0。
        全局_时间统计 的 忙等总时间_us 为 0。
        全局_时间统计 的 忙等次数 为 0。
        全局_时间统计 的 定时器触发次数 为 0。
        全局_时间统计 的 定时器超时次数 为 0。
    完。
完。

平台_获取调用数 接受 零参：
    做 时间统计_初始化。
    返还 全局_时间统计 的 调用总数。
完。

平台_获取睡眠数 接受 零参：
    做 时间统计_初始化。
    返还 全局_时间统计 的 睡眠次数。
完。

平台_获取定时器触发数 接受 零参：
    做 时间统计_初始化。
    返还 全局_时间统计 的 定时器触发次数。
完。

平台_获取定时器超时数 接受 零参：
    做 时间统计_初始化。
    返还 全局_时间统计 的 定时器超时次数。
完。

平台_时间戳格式化 接受 零参：
    d 为 做 秒转日期 用 做 壁钟_本地秒。
    返还 做 整数转字符串 用 d 的 第 0 项 加 「-」 加 做 整数转字符串 用 d 的 第 1 项 加 「-」 加 做 整数转字符串 用 d 的 第 2 项 加 「 」 加 做 整数转字符串 用 d 的 第 3 项 加 「:」 加 做 整数转字符串 用 d 的 第 4 项 加 「:」 加 做 整数转字符串 用 d 的 第 5 项。
完。

可变 AI_平台时间_负载窗口 为 一组：（空）。

AI_平台时间_负载预测 接受 当前负载：
    做 追加 用 AI_平台时间_负载窗口、当前负载。
    若 做 计数 用 AI_平台时间_负载窗口 大于 20：
        做 列表_移除 用 AI_平台时间_负载窗口、0。
    完。
    若 做 计数 用 AI_平台时间_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_平台时间_负载窗口。
    趋势 为 AI_平台时间_负载窗口 的 第 n 减 1 项 减 AI_平台时间_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_平台时间_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
