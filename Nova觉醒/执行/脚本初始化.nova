《脚本初始化》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·脚本值构造》。

JS类型_未定义 为 0。

JS类型_空 为 1。

JS类型_布尔 为 2。

JS类型_数字 为 3。

JS类型_字符串 为 4。

JS类型_对象 为 5。

JS类型_数组 为 6。

JS类型_函数 为 7。

TK_数字 为 1。

TK_字符串 为 2。

TK_标识符 为 3。

TK_运算符 为 4。

TK_标点 为 5。

TK_关键字 为 6。

TK_EOF 为 0。

JS值 是 一种 事物，含 类型、数字值、字符串值、对象属性、数组元素、函数体、函数参数。

JS变量 是 一种 事物，含 名称、值。

JS作用域 是 一种 事物，含 变量表、父作用域、类型。

JS运行时 是 一种 事物，含 全局作用域、作用域栈、文档、控制台输出、错误、已初始化、执行计数、最大执行步。

可变 JS_运行时 为 空。

JS_初始化 接受 零参：
    JS_运行时 为 新建 JS运行时。
    JS_运行时 的 全局作用域 为 新建 JS作用域。
    JS_运行时 的 全局作用域 的 变量表 为 一组：（空）。
    JS_运行时 的 全局作用域 的 父作用域 为 0。
    JS_运行时 的 全局作用域 的 类型 为 0。
    JS_运行时 的 作用域栈 为 一组：（空）。
    JS_运行时 的 控制台输出 为 一组：（空）。
    JS_运行时 的 错误 为 「」。
    JS_运行时 的 执行计数 为 0。
    JS_运行时 的 最大执行步 为 1000000。
    JS_运行时 的 已初始化 为 真。
    做 JS_注入全局接口。
    做 内核信息 用 「JS引擎核心: 初始化完成, ES6解释执行模式」。
完。

JS_注入全局接口 接受 零参：
    做 JS_设置全局变量 用 「undefined」、做 JS_创建未定义。
    做 JS_设置全局变量 用 「null」、做 JS_创建空。
    做 JS_设置全局变量 用 「true」、做 JS_创建布尔 用 真。
    做 JS_设置全局变量 用 「false」、做 JS_创建布尔 用 假。
    做 JS_设置全局变量 用 「Infinity」、做 JS_创建数字 用 999999999。
    做 JS_设置全局变量 用 「NaN」、做 JS_创建数字 用 0。
    doc 为 做 JS_创建对象。
    做 JS_设置全局变量 用 「document」、doc。
    win 为 做 JS_创建对象。
    做 JS_设置全局变量 用 「window」、win。
    nav 为 做 JS_创建对象。
    做 JS_对象设置 用 nav、「user智能体」、做 JS_创建字符串 用 「NovaOS-Browser/1.0 (内核原生)」。
    做 JS_对象设置 用 nav、「platform」、做 JS_创建字符串 用 「NovaOS x86-64」。
    做 JS_设置全局变量 用 「navigator」、nav。
完。

本章 收束。
