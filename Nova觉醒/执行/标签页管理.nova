《标签页管理》

引用《..·中枢·会话桥一体》。

引用《..·中枢·整数转字符串》。

TAB_最大数量 为 512。

TAB_状态_创建中 为 0。

TAB_状态_加载中 为 1。

TAB_状态_活跃 为 2。

TAB_状态_休眠 为 3。

TAB_状态_冒号 为 4。

TAB_状态_崩溃 为 5。

TAB_状态_关闭中 为 6。

TAB_休眠超时 为 300000。

TAB_内存压缩阈值 为 104857600。

TAB_最大历史 为 50。

TAB_最大前进 为 50。

可变 NTLM_创建总数 为 0。

可变 NTLM_关闭总数 为 0。

可变 NTLM_休眠总数 为 0。

可变 NTLM_唤醒总数 为 0。

可变 NTLM_崩溃总数 为 0。

可变 NTLM_导航总数 为 0。

可变 NTLM_AI预测总数 为 0。

TabEntry 是 一种 事物，含 id、窗口ID、url、标题、图标、状态、进程ID、创建时间、最后活跃时间、内存使用、是置顶、是静音、正在播放音频、历史列表、历史数量、历史位置、父标签ID。

可变 标签页池 为 0。

可变 标签页数量 为 0。

可变 标签页容量 为 0。

可变 当前活跃标签 为 0。

NTLM_初始化标签池 接受 零参：
    标签页容量 为 64。
    标签页池 为 做 堆_分配 用 标签页容量 乘 8。
    标签页数量 为 0。
    当前活跃标签 为 0。
    若 标签页池 等于 0：
        返还 假。
    完。
    返还 真。
完。

NTLM_确保标签容量 接受 零参：
    若 标签页数量 小于 标签页容量：
        返还。
    完。
    新容量 为 标签页容量 乘 2。
    若 新容量 大于 TAB_最大数量：
        新容量 为 TAB_最大数量。
    完。
    若 新容量 小于或等于 标签页容量：
        返还。
    完。
    新池 为 做 堆_分配 用 新容量 乘 8。
    做 内存_拷贝 用 新池、标签页池、标签页容量 乘 8。
    做 堆_释放 用 标签页池。
    标签页池 为 新池。
    标签页容量 为 新容量。
完。

NTLM_查找标签 接受 标签ID：
    可变 i 为 0。
    只要 i 小于 标签页数量：
        t 为 做 内存_读64 用 标签页池 加 i 乘 8。
        若 t 不等于 0 并且 t 的 id 等于 标签ID：
            返还 t。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

NTLM_查找标签索引 接受 标签ID：
    可变 i 为 0。
    只要 i 小于 标签页数量：
        t 为 做 内存_读64 用 标签页池 加 i 乘 8。
        若 t 不等于 0 并且 t 的 id 等于 标签ID：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

NTLM_创建标签 接受 窗口ID、url、父ID：
    若 标签页数量 大于或等于 TAB_最大数量：
        返还 0。
    完。
    做 NTLM_确保标签容量。
    t 为 做 堆_分配 用 TabEntry。
    t 的 id 为 标签_下一个ID。
    标签_下一个ID 为 标签_下一个ID 加 1。
    t 的 窗口ID 为 窗口ID。
    t 的 url 为 url。
    t 的 标题 为 「」。
    t 的 图标 为 0。
    t 的 状态 为 TAB_状态_创建中。
    t 的 进程ID 为 0。
    t 的 创建时间 为 做 时间_当前毫秒。
    t 的 最后活跃时间 为 t 的 创建时间。
    t 的 内存使用 为 0。
    t 的 是置顶 为 假。
    t 的 是静音 为 假。
    t 的 正在播放音频 为 假。
    t 的 历史列表 为 做 堆_分配 用 TAB_最大历史 乘 8。
    t 的 历史数量 为 0。
    t 的 历史位置 为 负 1。
    t 的 父标签ID 为 父ID。
    做 内存_写64 用 标签页池 加 标签页数量 乘 8、t。
    标签页数量 为 标签页数量 加 1。
    t 的 进程ID 为 做 浏览器调度_创建标签进程 用 t 的 id、url。
    做 资源调度_注册标签 用 t 的 id。
    做 合成器对接_添加标签 用 窗口ID、t 的 id。
    NTLM_创建总数 为 NTLM_创建总数 加 1。
    做 内核信息 用 「新标签页 #」 加 做 整数转字符串 用 t 的 id 加 「: 」 加 url。
    返还 t 的 id。
完。

NTLM_关闭标签 接受 标签ID：
    idx 为 做 NTLM_查找标签索引 用 标签ID。
    若 idx 小于 0：
        返还。
    完。
    t 为 做 内存_读64 用 标签页池 加 idx 乘 8。
    若 t 等于 0：
        返还。
    完。
    t 的 状态 为 TAB_状态_关闭中。
    做 浏览器调度_销毁标签进程 用 标签ID。
    做 资源调度_注销标签 用 标签ID。
    若 t 的 历史列表 不等于 0：
        可变 i 为 0。
        只要 i 小于 t 的 历史数量：
            h 为 做 内存_读64 用 t 的 历史列表 加 i 乘 8。
            若 h 不等于 0：
                做 堆_释放 用 h。
            完。
            i 为 i 加 1。
        完。
        做 堆_释放 用 t 的 历史列表。
    完。
    若 当前活跃标签 等于 标签ID：
        做 NTLM_切换到相邻 用 idx。
    完。
    可变 j 为 idx。
    只要 j 小于 标签页数量 减 1：
        做 内存_写64 用 标签页池 加 j 乘 8、做 内存_读64 用 标签页池 加 (j 加 1) 乘 8。
        j 为 j 加 1。
    完。
    标签页数量 为 标签页数量 减 1。
    做 堆_释放 用 t。
    NTLM_关闭总数 为 NTLM_关闭总数 加 1。
    做 内核信息 用 「标签页 #」 加 做 整数转字符串 用 标签ID 加 「 已关闭」。
完。

NTLM_切换到相邻 接受 idx：
    若 标签页数量 小于或等于 1：
        当前活跃标签 为 0。
        返还。
    完。
    可变 新idx 为 idx 减 1。
    若 新idx 小于 0：
        新idx 为 idx 加 1。
    完。
    若 新idx 大于或等于 标签页数量：
        新idx 为 标签页数量 减 1。
    完。
    t 为 做 内存_读64 用 标签页池 加 新idx 乘 8。
    若 t 不等于 0：
        做 NTLM_激活标签 用 t 的 id。
    完。
完。

NTLM_激活标签 接受 标签ID：
    t 为 做 NTLM_查找标签 用 标签ID。
    若 t 等于 0：
        返还。
    完。
    若 t 的 状态 等于 TAB_状态_休眠：
        做 NTLM_唤醒标签 用 t。
    完。
    t 的 最后活跃时间 为 做 时间_当前毫秒。
    t 的 状态 为 TAB_状态_活跃。
    当前活跃标签 为 标签ID。
    做 合成器对接_激活标签 用 t 的 窗口ID、标签ID。
完。

NTLM_下一个标签 接受 零参：
    idx 为 做 NTLM_查找标签索引 用 当前活跃标签。
    若 idx 小于 0 或者 标签页数量 小于或等于 1：
        返还。
    完。
    新idx 为 (idx 加 1) 取余于 标签页数量。
    t 为 做 内存_读64 用 标签页池 加 新idx 乘 8。
    若 t 不等于 0：
        做 NTLM_激活标签 用 t 的 id。
    完。
完。

NTLM_上一个标签 接受 零参：
    idx 为 做 NTLM_查找标签索引 用 当前活跃标签。
    若 idx 小于 0 或者 标签页数量 小于或等于 1：
        返还。
    完。
    新idx 为 (idx 减 1 加 标签页数量) 取余于 标签页数量。
    t 为 做 内存_读64 用 标签页池 加 新idx 乘 8。
    若 t 不等于 0：
        做 NTLM_激活标签 用 t 的 id。
    完。
完。

NTLM_休眠标签 接受 t：
    若 t 等于 0 或者 t 的 状态 不等于 TAB_状态_活跃：
        返还。
    完。
    若 t 的 id 等于 当前活跃标签：
        返还。
    完。
    若 t 的 是置顶：
        返还。
    完。
    若 t 的 正在播放音频：
        返还。
    完。
    t 的 状态 为 TAB_状态_休眠。
    做 浏览器调度_挂起标签进程 用 t 的 id。
    NTLM_休眠总数 为 NTLM_休眠总数 加 1。
    做 内核信息 用 「标签 #」 加 做 整数转字符串 用 t 的 id 加 「 进入休眠」。
完。

NTLM_唤醒标签 接受 t：
    若 t 等于 0 或者 t 的 状态 不等于 TAB_状态_休眠：
        返还。
    完。
    t 的 状态 为 TAB_状态_活跃。
    做 浏览器调度_恢复标签进程 用 t 的 id。
    NTLM_唤醒总数 为 NTLM_唤醒总数 加 1。
    做 内核信息 用 「标签 #」 加 做 整数转字符串 用 t 的 id 加 「 已唤醒」。
完。

NTLM_扫描休眠候选 接受 零参：
    现在 为 做 时间_当前毫秒。
    可变 i 为 0。
    只要 i 小于 标签页数量：
        t 为 做 内存_读64 用 标签页池 加 i 乘 8。
        若 t 不等于 0 并且 t 的 状态 等于 TAB_状态_活跃：
            若 t 的 id 不等于 当前活跃标签：
                空闲 为 现在 减 t 的 最后活跃时间。
                若 空闲 大于 TAB_休眠超时：
                    做 NTLM_休眠标签 用 t。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

NTLM_添加历史 接受 t、url：
    若 t 等于 0 或者 url 等于 0 或者 t 的 历史列表 等于 0：
        返还。
    完。
    若 t 的 历史位置 小于 t 的 历史数量 减 1：
        可变 j 为 t 的 历史位置 加 1。
        只要 j 小于 t 的 历史数量：
            h 为 做 内存_读64 用 t 的 历史列表 加 j 乘 8。
            若 h 不等于 0：
                做 堆_释放 用 h。
            完。
            j 为 j 加 1。
        完。
        t 的 历史数量 为 t 的 历史位置 加 1。
    完。
    若 t 的 历史数量 大于或等于 TAB_最大历史：
        old 为 做 内存_读64 用 t 的 历史列表。
        若 old 不等于 0：
            做 堆_释放 用 old。
        完。
        做 内存_拷贝 用 t 的 历史列表、t 的 历史列表 加 8、(t 的 历史数量 减 1) 乘 8。
        t 的 历史数量 为 t 的 历史数量 减 1。
    完。
    做 内存_写64 用 t 的 历史列表 加 t 的 历史数量 乘 8、url。
    t 的 历史数量 为 t 的 历史数量 加 1。
    t 的 历史位置 为 t 的 历史数量 减 1。
完。

NTLM_后退 接受 t：
    若 t 等于 0 或者 t 的 历史位置 小于或等于 0：
        返还 0。
    完。
    t 的 历史位置 为 t 的 历史位置 减 1。
    url 为 做 内存_读64 用 t 的 历史列表 加 t 的 历史位置 乘 8。
    t 的 url 为 url。
    返还 url。
完。

NTLM_前进 接受 t：
    若 t 等于 0 或者 t 的 历史位置 大于或等于 t 的 历史数量 减 1：
        返还 0。
    完。
    t 的 历史位置 为 t 的 历史位置 加 1。
    url 为 做 内存_读64 用 t 的 历史列表 加 t 的 历史位置 乘 8。
    t 的 url 为 url。
    返还 url。
完。

NTLM_可后退 接受 t：
    若 t 等于 0：
        返还 假。
    完。
    返还 t 的 历史位置 大于 0。
完。

NTLM_可前进 接受 t：
    若 t 等于 0：
        返还 假。
    完。
    返还 t 的 历史位置 小于 t 的 历史数量 减 1。
完。

NTLM_设置标题 接受 标签ID、标题：
    t 为 做 NTLM_查找标签 用 标签ID。
    若 t 等于 0：
        返还。
    完。
    t 的 标题 为 标题。
完。

NTLM_设置图标 接受 标签ID、图标数据：
    t 为 做 NTLM_查找标签 用 标签ID。
    若 t 等于 0：
        返还。
    完。
    t 的 图标 为 图标数据。
完。

NTLM_切换置顶 接受 标签ID：
    t 为 做 NTLM_查找标签 用 标签ID。
    若 t 等于 0：
        返还。
    完。
    t 的 是置顶 为 非 t 的 是置顶。
完。

NTLM_切换静音 接受 标签ID：
    t 为 做 NTLM_查找标签 用 标签ID。
    若 t 等于 0：
        返还。
    完。
    t 的 是静音 为 非 t 的 是静音。
完。

NTLM_更新内存使用 接受 标签ID、字节数：
    t 为 做 NTLM_查找标签 用 标签ID。
    若 t 等于 0：
        返还。
    完。
    t 的 内存使用 为 字节数。
完。

NTLM_处理崩溃 接受 标签ID：
    t 为 做 NTLM_查找标签 用 标签ID。
    若 t 等于 0：
        返还。
    完。
    t 的 状态 为 TAB_状态_崩溃。
    NTLM_崩溃总数 为 NTLM_崩溃总数 加 1。
    做 内核信息 用 「标签 #」 加 做 整数转字符串 用 标签ID 加 「 崩溃」。
完。

NTLM_重新加载 接受 标签ID：
    t 为 做 NTLM_查找标签 用 标签ID。
    若 t 等于 0：
        返还。
    完。
    若 t 的 状态 等于 TAB_状态_崩溃：
        t 的 进程ID 为 做 浏览器调度_创建标签进程 用 t 的 id、t 的 url。
    完。
    t 的 状态 为 TAB_状态_加载中。
    做 浏览器内核_导航 用 t 的 id、t 的 url。
    NTLM_导航总数 为 NTLM_导航总数 加 1。
完。

NTLM_移动标签 接受 标签ID、新位置：
    idx 为 做 NTLM_查找标签索引 用 标签ID。
    若 idx 小于 0 或者 新位置 小于 0 或者 新位置 大于或等于 标签页数量：
        返还。
    完。
    若 idx 等于 新位置：
        返还。
    完。
    t 为 做 内存_读64 用 标签页池 加 idx 乘 8。
    若 idx 小于 新位置：
        可变 k 为 idx。
        只要 k 小于 新位置：
            做 内存_写64 用 标签页池 加 k 乘 8、做 内存_读64 用 标签页池 加 (k 加 1) 乘 8。
            k 为 k 加 1。
        完。
    否则：
        可变 k 为 idx。
        只要 k 大于 新位置：
            做 内存_写64 用 标签页池 加 k 乘 8、做 内存_读64 用 标签页池 加 (k 减 1) 乘 8。
            k 为 k 减 1。
        完。
    完。
    做 内存_写64 用 标签页池 加 新位置 乘 8、t。
完。

NTLM_AI提取特征 接受 零参：
    t 为 做 Tensor_创建 用 12。
    若 t 等于 0：
        返还 0。
    完。
    做 Tensor_写 用 t、0、标签页数量。
    做 Tensor_写 用 t、1、NTLM_创建总数。
    做 Tensor_写 用 t、2、NTLM_关闭总数。
    做 Tensor_写 用 t、3、NTLM_休眠总数。
    做 Tensor_写 用 t、4、NTLM_唤醒总数。
    做 Tensor_写 用 t、5、NTLM_崩溃总数。
    做 Tensor_写 用 t、6、NTLM_导航总数。
    可变 总内存 为 0。
    可变 休眠数 为 0。
    可变 i 为 0。
    只要 i 小于 标签页数量：
        tab 为 做 内存_读64 用 标签页池 加 i 乘 8。
        若 tab 不等于 0：
            总内存 为 总内存 加 tab 的 内存使用。
            若 tab 的 状态 等于 TAB_状态_休眠：
                休眠数 为 休眠数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    做 Tensor_写 用 t、7、总内存。
    做 Tensor_写 用 t、8、休眠数。
    做 Tensor_写 用 t、9、当前活跃标签。
    做 Tensor_写 用 t、10、内核状态 的 总页面加载。
    做 Tensor_写 用 t、11、NTLM_AI预测总数。
    返还 t。
完。

NTLM_AI预测关闭候选 接受 零参：
    feat 为 做 NTLM_AI提取特征。
    若 feat 等于 0：
        返还 负 1。
    完。
    可变 最久未用 为 0。
    可变 最久未用时间 为 0。
    现在 为 做 时间_当前毫秒。
    可变 i 为 0。
    只要 i 小于 标签页数量：
        tab 为 做 内存_读64 用 标签页池 加 i 乘 8。
        若 tab 不等于 0 并且 tab 的 id 不等于 当前活跃标签 并且 非 tab 的 是置顶：
            空闲 为 现在 减 tab 的 最后活跃时间。
            若 空闲 大于 最久未用时间：
                最久未用时间 为 空闲。
                最久未用 为 tab 的 id。
            完。
        完。
        i 为 i 加 1。
    完。
    做 全局AI总线 的 发布 用 「ntlm_tab」、「close_predict」、最久未用。
    做 Tensor_释放 用 feat。
    NTLM_AI预测总数 为 NTLM_AI预测总数 加 1。
    返还 最久未用。
完。

NTLM_AI内存压力检测 接受 零参：
    feat 为 做 NTLM_AI提取特征。
    若 feat 等于 0：
        返还 假。
    完。
    总内存 为 做 Tensor_读 用 feat、7。
    做 Tensor_释放 用 feat。
    返还 总内存 大于 TAB_内存压缩阈值。
完。

NTLM_获取统计 接受 零参：
    返还 一组：标签页数量、NTLM_创建总数、NTLM_关闭总数、NTLM_休眠总数、NTLM_唤醒总数、NTLM_崩溃总数、NTLM_导航总数、NTLM_AI预测总数。
完。

NTLM_重置统计 接受 零参：
    NTLM_创建总数 为 0。
    NTLM_关闭总数 为 0。
    NTLM_休眠总数 为 0。
    NTLM_唤醒总数 为 0。
    NTLM_崩溃总数 为 0。
    NTLM_导航总数 为 0。
    NTLM_AI预测总数 为 0。
完。

NTLM_打印标签列表 接受 零参：
    可变 i 为 0。
    只要 i 小于 标签页数量：
        t 为 做 内存_读64 用 标签页池 加 i 乘 8。
        若 t 不等于 0：
            可变 活标记 为 「 」。
            若 t 的 id 等于 当前活跃标签：
                活标记 为 「*」。
            完。
            做 日志 用 「[」、活标记、「] #」、t 的 id、「 状态=」、t 的 状态、「 内存=」、t 的 内存使用、「 置顶=」、t 的 是置顶、「 url=」、t 的 url。
        完。
        i 为 i 加 1。
    完。
完。

NTLM_自检 接受 零参：
    若 非 做 NTLM_初始化标签池：
        返还 假。
    完。
    id1 为 做 NTLM_创建标签 用 1、「nova://home」、0。
    若 id1 等于 0：
        返还 假。
    完。
    id2 为 做 NTLM_创建标签 用 1、「nova://settings」、id1。
    若 标签页数量 不等于 2：
        返还 假。
    完。
    做 NTLM_激活标签 用 id1。
    若 当前活跃标签 不等于 id1：
        返还 假。
    完。
    做 NTLM_关闭标签 用 id2。
    若 标签页数量 不等于 1：
        返还 假。
    完。
    做 NTLM_关闭标签 用 id1。
    返还 标签页数量 等于 0。
完。

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核界面》。

引用《.·反追踪引擎》。

引用《..·推理·定点Q算术》。

引用《.·资源调度》。

引用《.·透明代理》。

引用《.·泄露防护》。

引用《.·定时器》。

引用《.·安全检测》。

引用《.·用户输入处》。

引用《..·中枢·整数转字符串》。

引用《.·视觉识别引擎》。

引用《.·合成器对接》。

引用《.·算术验证》。

引用《.·异常恢复》。

引用《.·进程调度适配》。

引用《.·真实滚动》。

引用《.·流量伪装》。

引用《..·中枢·反追踪》。

引用《.·页面预测》。

引用《.·AI交互入口》。

引用《.·网桌面引擎》。

可变 标签_下一个ID 为 1。

浏览器内核_新建标签 接受 窗口ID、url：
    标签ID 为 标签_下一个ID。
    标签_下一个ID 为 标签_下一个ID 加 1。
    做 合成器对接_添加标签 用 窗口ID、标签ID。
    做 浏览器调度_创建标签进程 用 标签ID、url。
    做 资源调度_注册标签 用 标签ID。
    做 浏览器内核_导航 用 标签ID、url。
    做 内核信息 用 「新标签页 #」 加 做 整数转字符串 用 标签ID 加 「: 」 加 url。
    返还 标签ID。
完。

浏览器内核_关闭标签 接受 标签ID：
    做 浏览器调度_销毁标签进程 用 标签ID。
    做 资源调度_注销标签 用 标签ID。
    做 内核信息 用 「标签页 #」 加 做 整数转字符串 用 标签ID 加 「 已关闭」。
完。

浏览器内核_导航 接受 标签ID、url：
    内核状态 的 总页面加载 为 内核状态 的 总页面加载 加 1。
    若 做 安全检测_是否拦截URL 用 url：
        做 内核信息 用 「安全拦截: 」 加 url。
        返还。
    完。
    等待 为 做 流量伪装_请求前等待。
    做 流量伪装_更新Referer 用 url。
    做 IP隐匿_检查自动轮换。
    做 页面预测_记录访问 用 url、「」。
    做 事件循环_导航 用 url。
完。

浏览器内核_地址栏输入 接受 输入：
    返还 做 AI交互入口_处理输入 用 输入。
完。

浏览器内核_地址栏建议 接受 部分输入：
    返还 做 AI交互入口_获取建议 用 部分输入。
完。

浏览器内核_关闭 接受 零参：
    做 内核信息 用 「Nova Browser: 正在关闭...」。
    做 事件循环_停止。
    内核状态 的 运行中 为 假。
    做 内核信息 用 「Nova Browser: 已关闭, 运行了 」 加 做 整数转字符串 用 内核状态 的 总页面加载 加 「 次页面加载, 」 加 做 整数转字符串 用 内核状态 的 总AI推理 加 「 次AI推理」。
完。

浏览器内核_全局统计 接受 零参：
    指纹统计 为 做 指纹伪装_统计。
    仿真统计 为 做 行为仿真_统计。
    验证统计 为 做 验证破解_统计。
    IP统计 为 做 IP隐匿_统计。
    反追踪统计 为 做 反追踪_统计。
    流量统计 为 做 流量伪装_统计。
    视觉统计 为 做 视觉识别_统计。
    编排统计 为 做 任务编排_统计。
    桌面统计 为 做 Web桌面_统计。
    返还 一组：内核状态 的 总页面加载、内核状态 的 已初始化、内核状态 的 运行中。
完。

标签页管理_绑定页面会话 接受 页面会话ID、标签ID：
    会话 为 做 一体桥_查页面会话 用 页面会话ID。
    标签 为 做 NTLM_查找标签 用 标签ID。
    若 会话 等于 空 或者 标签 等于 0：
        返还 假。
    完。
    返还 做 一体桥_绑定页面资源 用 页面会话ID、标签 的 窗口ID、标签ID、会话 的 文档指针、会话 的 脚本上下文、会话 的 推理上下文。
完。

标签页管理_生成标签快照 接受 页面会话ID：
    会话 为 做 一体桥_查页面会话 用 页面会话ID。
    若 会话 等于 空 或者 会话 的 标签ID 等于 0：
        返还 空。
    完。
    标签 为 做 NTLM_查找标签 用 会话 的 标签ID。
    若 标签 等于 0：
        返还 空。
    完。
    返还 做 一体桥_生成标签快照 用 页面会话ID、标签 的 id、标签 的 窗口ID、标签 的 url、标签 的 标题、标签 的 状态、标签 的 内存使用。
完。

本章 收束。
