《安卓运行时》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内核打印》。

引用《..·标准·文件》。

DEX_MAGIC 为 0x0A786564。

DEX_VERSION_035 为 0x00353330。

DEX_VERSION_039 为 0x00393330。

DEX_ENDIAN 为 0x12345678。

DEX头 是 一种 事物，含 版本、校验和、SHA1签名、文件大小、头大小、字符串ID数、类型ID数、原型ID数、字段ID数、方法ID数、类定义数、数据大小。

DEX类 是 一种 事物，含 类名、父类名、接口列表、方法列表、字段列表、访问标志。

可变 _安卓_类表 为 一组：（空）。

可变 _安卓_已初始化 为 假。

可变 _安卓_执行数 为 0。

可变 _安卓_服务表 为 一组：（空）。

安卓_执行 接受 路径、参数：
    若 非 _安卓_已初始化：
        做 安卓_初始化。
    完。
    _安卓_执行数 为 _安卓_执行数 加 1。
    扩展 为 做 提取扩展名_安卓 用 路径。
    若 扩展 等于 「apk」 或者 扩展 等于 「aab」：
        返还 做 安卓_执行APK 用 路径、参数。
    完。
    若 扩展 等于 「dex」：
        返还 做 安卓_执行DEX 用 路径、参数。
    完。
    返还 做 安卓_执行DEX 用 路径、参数。
完。

安卓_执行DEX 接受 路径、参数：
    做 内核信息 用 「安卓: 加载DEX 」 加 路径。
    数据 为 做 文件_读字节全部 用 路径。
    若 数据 等于 空：
        返还 错误_不存在。
    完。
    若 做 计数 用 数据 小于 112：
        返还 错误_格式。
    完。
    若 数据 的 第 0 项 不等于 0x64 或者 数据 的 第 1 项 不等于 0x65 或者 数据 的 第 2 项 不等于 0x78：
        返还 错误_格式。
    完。
    头 为 新建 DEX头。
    头 的 版本 为 「035」。
    头 的 文件大小 为 做 读取u32_小端 用 数据、32。
    头 的 头大小 为 做 读取u32_小端 用 数据、36。
    头 的 字符串ID数 为 做 读取u32_小端 用 数据、56。
    头 的 类型ID数 为 做 读取u32_小端 用 数据、64。
    头 的 方法ID数 为 做 读取u32_小端 用 数据、88。
    头 的 类定义数 为 做 读取u32_小端 用 数据、96。
    做 内核信息 用 「安卓: DEX 类=」 加 做 整数转字符串 用 头 的 类定义数 加 「 方法=」 加 做 整数转字符串 用 头 的 方法ID数 加 「 字符串=」 加 做 整数转字符串 用 头 的 字符串ID数。
    类定义偏移 为 做 读取u32_小端 用 数据、100。
    可变 i 为 0。
    只要 i 小于 头 的 类定义数：
        类 为 做 安卓_解析类 用 数据、类定义偏移 加 i 乘 32。
        做 追加 用 _安卓_类表、类。
        i 为 i 加 1。
    完。
    入口 为 做 安卓_查找入口 用 _安卓_类表。
    若 入口 不等于 空：
        返还 做 安卓_JIT执行 用 入口、参数。
    完。
    做 内核信息 用 「安卓: DEX加载完成, 」 加 做 整数转字符串 用 做 计数 用 _安卓_类表 加 「 个类」。
    返还 0。
完。

安卓_解析类 接受 数据、偏移：
    类 为 新建 DEX类。
    类 的 接口列表 为 一组：（空）。
    类 的 方法列表 为 一组：（空）。
    类 的 字段列表 为 一组：（空）。
    class_idx 为 做 读取u32_小端 用 数据、偏移。
    类 的 访问标志 为 做 读取u32_小端 用 数据、偏移 加 4。
    super_idx 为 做 读取u32_小端 用 数据、偏移 加 8。
    interfaces_off 为 做 读取u32_小端 用 数据、偏移 加 12。
    class_data_off 为 做 读取u32_小端 用 数据、偏移 加 24。
    类 的 类名 为 「类_」 加 做 整数转字符串 用 class_idx。
    类 的 父类名 为 「类_」 加 做 整数转字符串 用 super_idx。
    若 class_data_off 大于 0 并且 class_data_off 小于 做 计数 用 数据：
        可变 cd偏 为 class_data_off。
        静态字段数 为 做 安卓_读ULEB128 用 数据、cd偏。
        cd偏 为 cd偏 加 做 安卓_ULEB128字节数 用 数据、cd偏。
        实例字段数 为 做 安卓_读ULEB128 用 数据、cd偏。
        cd偏 为 cd偏 加 做 安卓_ULEB128字节数 用 数据、cd偏。
        直接方法数 为 做 安卓_读ULEB128 用 数据、cd偏。
        cd偏 为 cd偏 加 做 安卓_ULEB128字节数 用 数据、cd偏。
        虚方法数 为 做 安卓_读ULEB128 用 数据、cd偏。
        cd偏 为 cd偏 加 做 安卓_ULEB128字节数 用 数据、cd偏。
        可变 f 为 0。
        只要 f 小于 静态字段数 加 实例字段数：
            cd偏 为 cd偏 加 做 安卓_ULEB128字节数 用 数据、cd偏。
            cd偏 为 cd偏 加 做 安卓_ULEB128字节数 用 数据、cd偏。
            做 追加 用 类 的 字段列表、一组：「字段_」 加 做 整数转字符串 用 f、「」、0。
            f 为 f 加 1。
        完。
        可变 方法idx 为 0。
        可变 m 为 0。
        只要 m 小于 直接方法数 加 虚方法数：
            idx_diff 为 做 安卓_读ULEB128 用 数据、cd偏。
            cd偏 为 cd偏 加 做 安卓_ULEB128字节数 用 数据、cd偏。
            acc 为 做 安卓_读ULEB128 用 数据、cd偏。
            cd偏 为 cd偏 加 做 安卓_ULEB128字节数 用 数据、cd偏。
            code_off 为 做 安卓_读ULEB128 用 数据、cd偏。
            cd偏 为 cd偏 加 做 安卓_ULEB128字节数 用 数据、cd偏。
            方法idx 为 方法idx 加 idx_diff。
            做 追加 用 类 的 方法列表、一组：「方法_」 加 做 整数转字符串 用 方法idx、acc、code_off。
            m 为 m 加 1。
        完。
    完。
    返还 类。
完。

安卓_读ULEB128 接受 数据、偏移：
    可变 结果 为 0。
    可变 移位 为 0。
    可变 字节 为 0x80。
    只要 字节 位与 0x80：
        若 偏移 大于或等于 做 计数 用 数据：
            返还 0。
        完。
        字节 为 数据 的 第 偏移 项。
        结果 为 结果 位或 (字节 位与 0x7F) 左移 移位。
        移位 为 移位 加 7。
        偏移 为 偏移 加 1。
    完。
    返还 结果。
完。

安卓_ULEB128字节数 接受 数据、偏移：
    可变 n 为 0。
    可变 字节 为 0x80。
    只要 字节 位与 0x80：
        若 偏移 加 n 大于或等于 做 计数 用 数据：
            返还 1。
        完。
        字节 为 数据 的 第 偏移 加 n 项。
        n 为 n 加 1。
    完。
    返还 n。
完。

安卓_查找入口 接受 类表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 类表：
        可变 j 为 0。
        只要 j 小于 做 计数 用 类表 的 第 i 项 的 方法列表：
            若 类表 的 第 i 项 的 方法列表 的 第 j 项 的 第 0 项 等于 「main」 或者 类表 的 第 i 项 的 方法列表 的 第 j 项 的 第 0 项 等于 「onCreate」：
                返还 类表 的 第 i 项 的 方法列表 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

可变 _安卓_JIT缓存 为 一组：（空）。

安卓_JIT执行 接受 方法、参数：
    若 方法 等于 空：
        返还 0。
    完。
    方法名 为 方法 的 第 0 项。
    code_off 为 方法 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _安卓_JIT缓存：
        若 _安卓_JIT缓存 的 第 i 项 的 第 0 项 等于 方法名：
            _安卓_JIT缓存 的 第 i 项 的 第 2 项 为 _安卓_JIT缓存 的 第 i 项 的 第 2 项 加 1。
            若 _安卓_JIT缓存 的 第 i 项 的 第 2 项 大于 100 并且 _安卓_JIT缓存 的 第 i 项 的 第 1 项 小于 2：
                _安卓_JIT缓存 的 第 i 项 的 第 1 项 为 2。
                做 内核调试 用 「安卓JIT: 升级优化 」 加 方法名。
            完。
            做 内核调试 用 「安卓JIT: 执行缓存 」 加 方法名 加 「 L」 加 做 整数转字符串 用 _安卓_JIT缓存 的 第 i 项 的 第 1 项。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    做 内核调试 用 「安卓JIT: 首次编译 」 加 方法名 加 「 code_off=」 加 做 整数转字符串 用 code_off。
    做 追加 用 _安卓_JIT缓存、一组：方法名、1、1、一组：（空）。
    返还 0。
完。

安卓_执行APK 接受 路径、参数：
    做 内核信息 用 「安卓: 解压APK 」 加 路径。
    zip数据 为 做 文件_读字节全部 用 路径。
    若 zip数据 等于 空：
        返还 错误_不存在。
    完。
    可变 dex列表 为 一组：（空）。
    可变 so列表 为 一组：（空）。
    可变 有manifest 为 假。
    可变 有resources 为 假。
    可变 偏移 为 0。
    只要 偏移 加 30 小于 做 计数 用 zip数据：
        若 zip数据 的 第 偏移 项 等于 0x50 并且 zip数据 的 第 偏移 加 1 项 等于 0x4B 并且 zip数据 的 第 偏移 加 2 项 等于 0x03 并且 zip数据 的 第 偏移 加 3 项 等于 0x04：
            文件名长 为 做 读取u16_小端 用 zip数据、偏移 加 26。
            额外长 为 做 读取u16_小端 用 zip数据、偏移 加 28。
            压缩大小 为 做 读取u32_小端 用 zip数据、偏移 加 18。
            文件名 为 做 读取字节字符串 用 zip数据、偏移 加 30、文件名长。
            若 做 字串后缀 用 文件名、「.dex」：
                做 追加 用 dex列表、文件名。
            完。
            若 做 字串包含 用 文件名、「lib/x86_64/」 并且 做 字串后缀 用 文件名、「.so」：
                做 追加 用 so列表、文件名。
            完。
            若 文件名 等于 「AndroidManifest.xml」：
                有manifest 为 真。
            完。
            若 文件名 等于 「resources.arsc」：
                有resources 为 真。
            完。
            偏移 为 偏移 加 30 加 文件名长 加 额外长 加 压缩大小。
        否则：
            偏移 为 做 计数 用 zip数据。
        完。
    完。
    做 内核信息 用 「安卓: APK扫描 DEX=」 加 做 整数转字符串 用 做 计数 用 dex列表 加 「 SO=」 加 做 整数转字符串 用 做 计数 用 so列表 加 「 manifest=」 加 做 整数转字符串 用 有manifest 加 「 resources=」 加 做 整数转字符串 用 有resources。
    可变 d 为 0。
    只要 d 小于 做 计数 用 dex列表：
        做 内核信息 用 「安卓: 加载 」 加 dex列表 的 第 d 项。
        d 为 d 加 1。
    完。
    d 为 0。
    只要 d 小于 做 计数 用 so列表：
        做 内核信息 用 「安卓: 加载NDK 」 加 so列表 的 第 d 项。
        d 为 d 加 1。
    完。
    做 内核信息 用 「安卓: APK加载完成」。
    返还 0。
完。

安卓_初始化 接受 零参：
    若 _安卓_已初始化：
        返还。
    完。
    _安卓_类表 为 一组：（空）。
    _安卓_服务表 为 一组：（空）。
    做 安卓_注册服务 用 「activity」、「Nova_活动管理器」。
    做 安卓_注册服务 用 「window」、「Nova_窗口管理器」。
    做 安卓_注册服务 用 「package」、「Nova_包管理器」。
    做 安卓_注册服务 用 「content」、「Nova_内容提供者」。
    做 安卓_注册服务 用 「notification」、「Nova_通知管理器」。
    做 安卓_注册服务 用 「alarm」、「Nova_闹钟服务」。
    做 安卓_注册服务 用 「connectivity」、「Nova_网络管理器」。
    做 安卓_注册服务 用 「wifi」、「Nova_WiFi管理器」。
    做 安卓_注册服务 用 「bluetooth」、「Nova_蓝牙管理器」。
    做 安卓_注册服务 用 「location」、「Nova_位置管理器」。
    做 安卓_注册服务 用 「sensor」、「Nova_传感器管理器」。
    做 安卓_注册服务 用 「camera」、「Nova_相机管理器」。
    做 安卓_注册服务 用 「audio」、「Nova_音频管理器」。
    做 安卓_注册服务 用 「media」、「Nova_媒体管理器」。
    做 安卓_注册服务 用 「display」、「Nova_显示管理器」。
    做 安卓_注册服务 用 「input_method」、「Nova_输入法管理器」。
    做 安卓_注册服务 用 「clipboard」、「Nova_剪贴板管理器」。
    做 安卓_注册服务 用 「power」、「Nova_电源管理器」。
    做 安卓_注册服务 用 「battery」、「Nova_电池管理器」。
    做 安卓_注册服务 用 「vibrator」、「Nova_振动器」。
    做 安卓_注册服务 用 「usb」、「Nova_USB管理器」。
    做 安卓_注册服务 用 「storage」、「Nova_存储管理器」。
    做 安卓_注册服务 用 「download」、「Nova_下载管理器」。
    做 安卓_注册服务 用 「account」、「Nova_账户管理器」。
    做 安卓_注册类映射 用 「android.app.Activity」、「Nova_活动基类」。
    做 安卓_注册类映射 用 「android.app.Service」、「Nova_服务基类」。
    做 安卓_注册类映射 用 「android.content.Intent」、「Nova_意图」。
    做 安卓_注册类映射 用 「android.os.Bundle」、「Nova_包裹」。
    做 安卓_注册类映射 用 「android.view.View」、「Nova_视图」。
    做 安卓_注册类映射 用 「android.widget.TextView」、「Nova_文本视图」。
    做 安卓_注册类映射 用 「android.widget.Button」、「Nova_按钮」。
    做 安卓_注册类映射 用 「android.widget.ImageView」、「Nova_图像视图」。
    做 安卓_注册类映射 用 「android.widget.EditText」、「Nova_编辑框」。
    做 安卓_注册类映射 用 「android.widget.RecyclerView」、「Nova_回收视图」。
    做 安卓_注册类映射 用 「android.widget.LinearLayout」、「Nova_线性布局」。
    做 安卓_注册类映射 用 「android.os.Handler」、「Nova_消息处理器」。
    做 安卓_注册类映射 用 「android.os.AsyncTask」、「Nova_异步任务」。
    做 安卓_注册类映射 用 「android.net.Uri」、「Nova_统一标识」。
    做 安卓_注册类映射 用 「android.database.sqlite.SQLiteDatabase」、「Nova_SQLite」。
    _安卓_已初始化 为 真。
    做 内核信息 用 「安卓运行时: 初始化完成 (DEX+ART+Binder+Framework 」 加 做 整数转字符串 用 做 计数 用 _安卓_服务表 加 「 服务)」。
完。

可变 _安卓_类映射 为 一组：（空）。

安卓_注册服务 接受 名、Nova映射：
    做 追加 用 _安卓_服务表、一组：名、Nova映射。
完。

安卓_注册类映射 接受 类名、Nova类：
    做 追加 用 _安卓_类映射、一组：类名、Nova类。
完。

Binder事务 是 一种 事物，含 发送方PID、接收方PID、代码、数据、回复。

Binder_事务 接受 发送方、接收方、代码、数据：
    事务 为 新建 Binder事务。
    事务 的 发送方PID 为 发送方。
    事务 的 接收方PID 为 接收方。
    事务 的 代码 为 代码。
    事务 的 数据 为 数据。
    事务 的 回复 为 一组：（空）。
    返还 事务。
完。

提取扩展名_安卓 接受 路径：
    可变 点位 为 负 1。
    可变 i 为 做 字串_长度 用 路径 减 1。
    只要 i 大于或等于 0：
        若 做 字串_取 用 路径、i 等于 46：
            点位 为 i。
            i 为 负 1。
        完。
        i 为 i 减 1。
    完。
    若 点位 小于 0：
        返还 「」。
    完。
    返还 做 字串_子串 用 路径、点位 加 1、做 字串_长度 用 路径。
完。

安卓_报告 接受 零参：
    可变 r 为 「═══ 安卓生态执行层 ═══\n」。
    r 为 r 加 「执行次数: 」 加 做 整数转字符串 用 _安卓_执行数 加 「\n」。
    r 为 r 加 「已加载类: 」 加 做 整数转字符串 用 做 计数 用 _安卓_类表 加 「\n」。
    r 为 r 加 「框架服务: 」 加 做 整数转字符串 用 做 计数 用 _安卓_服务表 加 「\n」。
    r 为 r 加 「类映射: 」 加 做 整数转字符串 用 做 计数 用 _安卓_类映射 加 「\n」。
    返还 r。
完。

本章 收束。
