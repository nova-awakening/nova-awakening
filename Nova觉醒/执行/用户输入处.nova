《用户输入处》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《.·协议处理》。

引用《.·布局计算》。

引用《.·绘制命令》。

引用《.·渲染初始化》。

引用《..·中枢·整数转字符串》。

引用《.·节点操作》。

引用《.·浏览器事件循环》。

引用《.·内存适配》。

引用《.·JS执行入口》。

引用《.·样式引擎》。

引用《.·请求接口》。

事件循环_处理用户输入 接受 任务：
    若 任务 等于 0 或者 任务 的 数据 等于 0：
        返还。
    完。
    事件 为 任务 的 数据。
    目标 为 做 DOM_命中测试 用 事件 的 x、事件 的 y。
    若 目标 等于 0：
        返还。
    完。
    若 事件 的 类型 等于 输入_鼠标点击：
        做 DOM_分发事件 用 目标、「click」、事件。
    否则若 事件 的 类型 等于 输入_鼠标移动：
        做 DOM_分发事件 用 目标、「mousemove」、事件。
    否则若 事件 的 类型 等于 输入_键盘按下：
        做 DOM_分发事件 用 做 DOM_获取焦点元素、「keydown」、事件。
    否则若 事件 的 类型 等于 输入_键盘释放：
        做 DOM_分发事件 用 做 DOM_获取焦点元素、「keyup」、事件。
    否则若 事件 的 类型 等于 输入_滚轮：
        做 DOM_分发事件 用 目标、「wheel」、事件。
    完。
完。

事件循环_处理AI回调 接受 任务：
    若 任务 等于 0 或者 任务 的 数据 等于 0：
        返还。
    完。
    结果 为 任务 的 数据。
    若 结果 的 类型 等于 AI回调_翻译完成：
        做 DOM_替换文本 用 结果 的 目标节点、结果 的 翻译文本。
    否则若 结果 的 类型 等于 AI回调_摘要完成：
        做 DOM_插入摘要面板 用 结果 的 目标节点、结果 的 摘要文本。
    否则若 结果 的 类型 等于 AI回调_预测加载：
        做 浏览器_预加载资源 用 结果 的 URL列表。
    否则若 结果 的 类型 等于 AI回调_安全警告：
        做 浏览器_显示安全警告 用 结果 的 警告信息。
    完。
完。

事件循环_处理系统事件 接受 任务：
    若 任务 的 数据 等于 「memory_pressure」：
        做 浏览器内存_主动回收。
    完。
完。

事件循环_检查定时器 接受 零参：
    当前 为 做 高精度定时器_读取。
    可变 i 为 0。
    只要 i 小于 做 计数 用 事件循环 的 定时器列表：
        定时器 为 事件循环 的 定时器列表 的 第 i 项。
        若 定时器 的 执行时间 小于或等于 当前：
            做 事件循环_提交任务 用 任务_定时器、任务优先级_普通、定时器 的 回调、「」。
            若 定时器 的 重复：
                定时器 的 执行时间 为 当前 加 定时器 的 间隔 乘 1000。
                i 为 i 加 1。
            否则：
                做 列表_移除 用 事件循环 的 定时器列表、i。
            完。
        否则：
            i 为 i 加 1。
        完。
    完。
完。

事件循环_导航 接受 url：
    做 内核信息 用 「浏览器导航: 」 加 url。
    预取缓存 为 做 浏览器网络_检查预取缓存 用 url。
    可变 HTML内容 为 「」。
    若 预取缓存 不等于 空：
        HTML内容 为 预取缓存 的 第 0 项。
        做 内核信息 用 「浏览器: AI预取命中!」。
    否则：
        请求 为 做 浏览器网络_发起请求 用 方法_GET、url、一组：（空）、「」。
        若 请求 的 状态 等于 请求状态_完成 并且 做 计数 用 请求 的 响应体 大于 0：
            HTML内容 为 请求 的 响应体 的 第 0 项。
        完。
    完。
    若 做 字符串长度 用 HTML内容 等于 0：
        做 内核信息 用 「浏览器: 页面加载失败」。
        返还。
    完。
    文档 为 做 DOM_解析HTML 用 HTML内容、url。
    可变 si 为 0。
    只要 si 小于 做 计数 用 文档 的 样式列表：
        样式表 为 做 CSS_解析样式表 用 文档 的 样式列表 的 第 si 项、url。
        做 追加 用 CSS_样式表列表、样式表。
        si 为 si 加 1。
    完。
    做 渲染_构建树 用 文档。
    做 渲染_布局。
    做 渲染_绘制 用 文档。
    可变 ji 为 0。
    只要 ji 小于 做 计数 用 文档 的 脚本列表：
        做 JS_执行脚本 用 文档 的 脚本列表 的 第 ji 项。
        ji 为 ji 加 1。
    完。
    做 浏览器网络_AI预取 用 url。
    做 内核信息 用 「浏览器: 页面加载完成, DOM节点:」 加 做 整数转字符串 用 文档 的 总节点数。
完。

事件循环_统计 接受 零参：
    返还 一组：事件循环 的 总任务数、事件循环 的 总帧数、事件循环 的 丢帧数、做 计数 用 事件循环 的 宏任务队列、做 计数 用 事件循环 的 微任务队列。
完。

本章 收束。
