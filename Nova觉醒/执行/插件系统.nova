《插件系统》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·外联·URL解析辅助》。

引用《.·JS执行入口》。

插件类型_Web扩展 为 0。

插件类型_Nova原生 为 1。

插件类型_内容脚本 为 2。

插件类型_主题 为 3。

插件状态_未安装 为 0。

插件状态_已安装 为 1。

插件状态_已启用 为 2。

插件状态_已禁用 为 3。

插件状态_错误 为 4。

插件权限_标签 为 1。

插件权限_书签 为 2。

插件权限_历史 为 4。

插件权限_存储 为 8。

插件权限_网络 为 16。

插件权限_通知 为 32。

插件权限_DOM 为 64。

插件权限_系统 为 128。

浏览器插件 是 一种 事物，含 ID、名称、版本、描述、类型、状态、权限、manifest、背景脚本、内容脚本、弹出页面、图标、沙箱级别。

可变 插件_列表 为 一组：（空）。

可变 插件_已初始化 为 假。

插件系统_初始化 接受 零参：
    插件_列表 为 一组：（空）。
    做 插件系统_注册内置插件。
    插件_已初始化 为 真。
    做 内核信息 用 「插件系统: Nova浏览器 Extension兼容 + Nova原生扩展就绪」。
完。

插件系统_注册内置插件 接受 零参：
    ai插件 为 新建 浏览器插件。
    ai插件 的 ID 为 「nova-ai-assistant」。
    ai插件 的 名称 为 「Nova AI 助手」。
    ai插件 的 版本 为 「1.0」。
    ai插件 的 描述 为 「内核原生AI助手, 自然语言操作系统」。
    ai插件 的 类型 为 插件类型_Nova原生。
    ai插件 的 状态 为 插件状态_已启用。
    ai插件 的 权限 为 插件权限_系统 位或 插件权限_标签 位或 插件权限_DOM 位或 插件权限_网络。
    ai插件 的 沙箱级别 为 沙箱_轻量。
    做 追加 用 插件_列表、ai插件。
    ad插件 为 新建 浏览器插件。
    ad插件 的 ID 为 「nova-ad-blocker」。
    ad插件 的 名称 为 「Nova 广告拦截」。
    ad插件 的 版本 为 「1.0」。
    ad插件 的 描述 为 「AI驱动的智能广告拦截」。
    ad插件 的 类型 为 插件类型_Nova原生。
    ad插件 的 状态 为 插件状态_已启用。
    ad插件 的 权限 为 插件权限_网络 位或 插件权限_DOM。
    ad插件 的 沙箱级别 为 沙箱_标准。
    做 追加 用 插件_列表、ad插件。
    暗色插件 为 新建 浏览器插件。
    暗色插件 的 ID 为 「nova-dark-mode」。
    暗色插件 的 名称 为 「Nova 暗色模式」。
    暗色插件 的 版本 为 「1.0」。
    暗色插件 的 描述 为 「全局网页暗色模式」。
    暗色插件 的 类型 为 插件类型_内容脚本。
    暗色插件 的 状态 为 插件状态_已启用。
    暗色插件 的 权限 为 插件权限_DOM。
    暗色插件 的 沙箱级别 为 沙箱_标准。
    做 追加 用 插件_列表、暗色插件。
完。

插件系统_安装 接受 manifest_json：
    插件 为 新建 浏览器插件。
    插件 的 manifest 为 manifest_json。
    插件 的 状态 为 插件状态_已安装。
    插件 的 沙箱级别 为 沙箱_标准。
    若 插件 的 权限 位与 插件权限_系统 不等于 0：
        插件 的 沙箱级别 为 沙箱_严格。
    完。
    做 追加 用 插件_列表、插件。
    返还 插件 的 ID。
完。

插件系统_卸载 接受 插件ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 插件_列表：
        若 插件_列表 的 第 i 项 的 ID 等于 插件ID：
            做 列表_移除 用 插件_列表、i。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

插件系统_启用 接受 插件ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 插件_列表：
        若 插件_列表 的 第 i 项 的 ID 等于 插件ID：
            插件_列表 的 第 i 项 的 状态 为 插件状态_已启用。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

插件系统_禁用 接受 插件ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 插件_列表：
        若 插件_列表 的 第 i 项 的 ID 等于 插件ID：
            插件_列表 的 第 i 项 的 状态 为 插件状态_已禁用。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

插件系统_注入内容脚本 接受 url：
    可变 i 为 0。
    只要 i 小于 做 计数 用 插件_列表：
        插件 为 插件_列表 的 第 i 项。
        若 插件 的 状态 等于 插件状态_已启用：
            若 插件 的 类型 等于 插件类型_内容脚本 或者 做 计数 用 插件 的 内容脚本 大于 0：
                可变 j 为 0。
                只要 j 小于 做 计数 用 插件 的 内容脚本：
                    匹配模式 为 插件 的 内容脚本 的 第 j 项 的 第 0 项。
                    若 做 插件系统_匹配URL 用 url、匹配模式：
                        做 JS_执行脚本 用 插件 的 内容脚本 的 第 j 项 的 第 1 项。
                    完。
                    j 为 j 加 1。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

插件系统_匹配URL 接受 url、模式：
    若 模式 等于 「*」：
        返还 真。
    完。
    若 模式 等于 「<all_urls>」：
        返还 真。
    完。
    若 做 字符串查找 用 url、模式 大于或等于 0：
        返还 真。
    完。
    返还 假。
完。

插件系统_获取列表 接受 零参：
    返还 插件_列表。
完。

本章 收束。
