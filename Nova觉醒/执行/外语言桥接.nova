《外语言桥接》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内核打印》。

引用《..·标准·文件》。

可变 _外语言_C执行数 为 0。

可变 _外语言_Python执行数 为 0。

可变 _外语言_Go执行数 为 0。

可变 _外语言_Rust执行数 为 0。

可变 _外语言_WASM执行数 为 0。

可变 _外语言_Java执行数 为 0。

C编译上下文 是 一种 事物，含 源路径、包含路径、宏定义、目标、优化级别。

外语言_执行C 接受 路径、参数：
    _外语言_C执行数 为 _外语言_C执行数 加 1。
    做 内核信息 用 「外语言桥接: C源码 → Nova IR → 执行: 」 加 路径。
    ctx 为 新建 C编译上下文。
    ctx 的 源路径 为 路径。
    ctx 的 包含路径 为 一组：「/usr/include」、「/usr/local/include」、「/usr/include/x86_64-linux-gnu」。
    ctx 的 宏定义 为 一组：「__NOVA__=1」、「__linux__=1」、「__x86_64__=1」。
    ctx 的 目标 为 「nova-ir」。
    ctx 的 优化级别 为 2。
    预处理结果 为 做 C_预处理 用 路径、ctx。
    若 预处理结果 等于 空：
        返还 错误_编译。
    完。
    AST 为 做 C_解析 用 预处理结果。
    若 AST 等于 空：
        返还 错误_编译。
    完。
    IR 为 做 C_到Nova_IR 用 AST。
    若 IR 等于 空：
        返还 错误_编译。
    完。
    结果 为 做 Nova_JIT执行 用 IR、参数。
    做 内核信息 用 「C程序执行完成, 返回值: 」 加 做 整数转字符串 用 结果。
    返还 结果。
完。

C_标准库映射 接受 零参：
    返还 一组：一组：「printf」、「Nova_printf」、一组：「scanf」、「Nova_scanf」、一组：「fopen」、「Nova_fopen」、一组：「fclose」、「Nova_fclose」、一组：「fread」、「Nova_fread」、一组：「fwrite」、「Nova_fwrite」、一组：「fprintf」、「Nova_fprintf」、一组：「puts」、「Nova_puts」、一组：「getchar」、「Nova_getchar」、一组：「malloc」、「Nova_堆分配」、一组：「free」、「Nova_堆释放」、一组：「calloc」、「Nova_堆分配清零」、一组：「realloc」、「Nova_堆重分配」、一组：「exit」、「Nova_退出」、一组：「atoi」、「Nova_字串转整数」、一组：「atof」、「Nova_字串转浮点」、一组：「rand」、「Nova_随机数」、一组：「srand」、「Nova_设随机种子」、一组：「qsort」、「Nova_快速排序」、一组：「abs」、「Nova_绝对值」、一组：「strlen」、「Nova_字串长度」、一组：「strcpy」、「Nova_字串拷贝」、一组：「strcat」、「Nova_字串连接」、一组：「strcmp」、「Nova_字串比较」、一组：「memcpy」、「Nova_内存拷贝」、一组：「memset」、「Nova_内存填充」、一组：「memmove」、「Nova_内存移动」、一组：「sin」、「Nova_正弦」、一组：「cos」、「Nova_余弦」、一组：「sqrt」、「Nova_平方根」、一组：「pow」、「Nova_乘方」、一组：「log」、「Nova_对数」、一组：「floor」、「Nova_下取整」、一组：「ceil」、「Nova_上取整」。
完。

外语言_执行Python 接受 路径、参数：
    _外语言_Python执行数 为 _外语言_Python执行数 加 1。
    做 内核信息 用 「外语言桥接: Python → Nova执行: 」 加 路径。
    源码 为 做 文件_读全部 用 路径。
    若 源码 等于 空：
        返还 错误_不存在。
    完。
    词元 为 做 Python_词法分析 用 源码。
    若 词元 等于 空：
        返还 错误_编译。
    完。
    AST 为 做 Python_语法分析 用 词元。
    若 AST 等于 空：
        返还 错误_编译。
    完。
    字节码 为 做 Python_到Nova字节码 用 AST。
    若 字节码 等于 空：
        返还 错误_编译。
    完。
    虚机 为 做 创建Python虚机。
    做 Python_注入标准库 用 虚机。
    结果 为 做 Python_虚机执行 用 虚机、字节码、参数。
    做 内核信息 用 「Python执行完成」。
    返还 结果。
完。

Python_注入标准库 接受 虚机：
    做 Python_注册内建 用 虚机、「print」、「Nova_打印行」。
    做 Python_注册内建 用 虚机、「input」、「Nova_读行」。
    做 Python_注册内建 用 虚机、「len」、「Nova_计数」。
    做 Python_注册内建 用 虚机、「range」、「Nova_范围」。
    做 Python_注册内建 用 虚机、「int」、「Nova_转整数」。
    做 Python_注册内建 用 虚机、「str」、「Nova_转字符串」。
    做 Python_注册内建 用 虚机、「float」、「Nova_转浮点」。
    做 Python_注册内建 用 虚机、「list」、「Nova_转列表」。
    做 Python_注册内建 用 虚机、「dict」、「Nova_创建字典」。
    做 Python_注册内建 用 虚机、「type」、「Nova_类型名」。
    做 Python_注册内建 用 虚机、「isinstance」、「Nova_是实例」。
    做 Python_注册内建 用 虚机、「open」、「Nova_打开文件」。
    做 Python_注册内建 用 虚机、「sorted」、「Nova_排序」。
    做 Python_注册内建 用 虚机、「enumerate」、「Nova_枚举」。
    做 Python_注册内建 用 虚机、「zip」、「Nova_拉链」。
    做 Python_注册内建 用 虚机、「map」、「Nova_映射」。
    做 Python_注册内建 用 虚机、「filter」、「Nova_过滤」。
    做 Python_注册模块 用 虚机、「os」、「Nova_系统接口」。
    做 Python_注册模块 用 虚机、「sys」、「Nova_运行时信息」。
    做 Python_注册模块 用 虚机、「math」、「Nova_数学库」。
    做 Python_注册模块 用 虚机、「json」、「Nova_JSON库」。
    做 Python_注册模块 用 虚机、「re」、「Nova_正则库」。
    做 Python_注册模块 用 虚机、「datetime」、「Nova_日期时间」。
    做 Python_注册模块 用 虚机、「collections」、「Nova_高级容器」。
    做 Python_注册模块 用 虚机、「itertools」、「Nova_迭代工具」。
    做 Python_注册模块 用 虚机、「functools」、「Nova_函数工具」。
    做 Python_注册模块 用 虚机、「pathlib」、「Nova_路径库」。
    做 Python_注册模块 用 虚机、「socket」、「Nova_套接字」。
    做 Python_注册模块 用 虚机、「threading」、「Nova_线程库」。
    做 Python_注册模块 用 虚机、「subprocess」、「Nova_子进程」。
    做 Python_注册模块 用 虚机、「hashlib」、「Nova_哈希库」。
完。

外语言_执行Go 接受 路径、参数：
    _外语言_Go执行数 为 _外语言_Go执行数 加 1。
    做 内核信息 用 「外语言桥接: Go → Nova执行: 」 加 路径。
    源码 为 做 文件_读全部 用 路径。
    若 源码 等于 空：
        返还 错误_不存在。
    完。
    词元 为 做 Go_词法分析 用 源码。
    AST 为 做 Go_语法分析 用 词元。
    若 AST 等于 空：
        返还 错误_编译。
    完。
    IR 为 做 Go_到Nova_IR 用 AST。
    结果 为 做 Nova_JIT执行 用 IR、参数。
    做 内核信息 用 「Go程序执行完成」。
    返还 结果。
完。

Go_运行时映射 接受 零参：
    返还 一组：一组：「runtime.GOMAXPROCS」、「Nova_设CPU亲和」、一组：「runtime.NumGoroutine」、「Nova_协程数」、一组：「runtime.GC」、「Nova_触发GC」、一组：「fmt.Println」、「Nova_打印行」、一组：「fmt.Printf」、「Nova_格式打印」、一组：「fmt.Sprintf」、「Nova_格式字串」、一组：「os.Exit」、「Nova_退出」、一组：「os.Getenv」、「Nova_获取环境变量」、一组：「strings.Contains」、「Nova_字串包含」、一组：「strings.Split」、「Nova_字串分割」、一组：「strconv.Itoa」、「Nova_整数转字串」、一组：「strconv.Atoi」、「Nova_字串转整数」、一组：「sync.Mutex」、「Nova_互斥锁」、一组：「sync.WaitGroup」、「Nova_等待组」、一组：「net/http.ListenAndServe」、「Nova_HTTP监听」。
完。

外语言_执行Rust 接受 路径、参数：
    _外语言_Rust执行数 为 _外语言_Rust执行数 加 1。
    做 内核信息 用 「外语言桥接: Rust → Nova执行: 」 加 路径。
    源码 为 做 文件_读全部 用 路径。
    若 源码 等于 空：
        返还 错误_不存在。
    完。
    词元 为 做 Rust_词法分析 用 源码。
    AST 为 做 Rust_语法分析 用 词元。
    若 AST 等于 空：
        返还 错误_编译。
    完。
    IR 为 做 Rust_到Nova_IR 用 AST。
    结果 为 做 Nova_JIT执行 用 IR、参数。
    做 内核信息 用 「Rust程序执行完成」。
    返还 结果。
完。

Rust_标准库映射 接受 零参：
    返还 一组：一组：「std::io::stdin」、「Nova_标准输入」、一组：「std::io::stdout」、「Nova_标准输出」、一组：「std::fs::read_to_string」、「Nova_读文件」、一组：「std::fs::write」、「Nova_写文件」、一组：「std::env::args」、「Nova_命令行参数」、一组：「std::env::var」、「Nova_获取环境变量」、一组：「std::thread::spawn」、「Nova_创建线程」、一组：「std::sync::Arc」、「Nova_原子引用」、一组：「std::sync::Mutex」、「Nova_互斥锁」、一组：「std::collections::HashMap」、「Nova_字典」、一组：「std::collections::BTreeMap」、「Nova_有序字典」、一组：「std::collections::VecDeque」、「Nova_双端队列」、一组：「std::net::TcpListener」、「Nova_TCP监听」、一组：「std::net::TcpStream」、「Nova_TCP流」、一组：「println!」、「Nova_打印行」、一组：「eprintln!」、「Nova_错误打印行」、一组：「format!」、「Nova_格式字串」、一组：「vec!」、「Nova_创建列表」。
完。

外语言_执行WASM 接受 路径、参数：
    _外语言_WASM执行数 为 _外语言_WASM执行数 加 1。
    做 内核信息 用 「外语言桥接: WASM → Nova执行: 」 加 路径。
    字节码 为 做 文件_读字节全部 用 路径。
    若 字节码 等于 空：
        返还 错误_不存在。
    完。
    若 做 计数 用 字节码 小于 8：
        返还 错误_格式。
    完。
    若 字节码 的 第 4 项 不等于 0x01：
        做 内核警告 用 「WASM版本不支持: 」 加 做 整数转字符串 用 字节码 的 第 4 项。
        返还 错误_格式。
    完。
    模块 为 做 WASM_解码 用 字节码。
    若 模块 等于 空：
        返还 错误_编译。
    完。
    做 WASM_注入WASI 用 模块。
    结果 为 做 WASM_执行 用 模块、「_start」、参数。
    做 内核信息 用 「WASM执行完成」。
    返还 结果。
完。

外语言_执行Java 接受 路径、参数：
    _外语言_Java执行数 为 _外语言_Java执行数 加 1。
    做 内核信息 用 「外语言桥接: Java类 → Nova执行: 」 加 路径。
    字节码 为 做 文件_读字节全部 用 路径。
    若 字节码 等于 空：
        返还 错误_不存在。
    完。
    类 为 做 Java_解析类 用 字节码。
    若 类 等于 空：
        返还 错误_格式。
    完。
    结果 为 做 Java_执行主方法 用 类、参数。
    做 内核信息 用 「Java执行完成」。
    返还 结果。
完。

ABI_SYSV_AMD64 为 0。

ABI_WIN64 为 1。

ABI_NOVA 为 2。

ABI_翻译调用 接受 目标ABI、函数地址、参数列表：
    若 目标ABI 等于 ABI_SYSV_AMD64：
        返还 做 原生调用_SysV 用 函数地址、参数列表。
    完。
    若 目标ABI 等于 ABI_WIN64：
        返还 做 原生调用_Win64 用 函数地址、参数列表。
    完。
    返还 做 原生调用_Nova 用 函数地址、参数列表。
完。

C_预处理 接受 路径、ctx：
    源码 为 做 文件_读全部 用 路径。
    若 源码 等于 空：
        返还 空。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    行列表 为 做 字串分割 用 源码、「\n」。
    只要 i 小于 做 计数 用 行列表：
        行 为 行列表 的 第 i 项。
        去空 为 做 字符串去空格 用 行。
        若 做 字串前缀 用 去空、「#include」：
            头文件 为 做 C_提取头文件名 用 去空。
            可变 j 为 0。
            可变 已找到 为 假。
            只要 j 小于 做 计数 用 ctx 的 包含路径：
                全路径 为 ctx 的 包含路径 的 第 j 项 加 「/」 加 头文件。
                若 做 文件_存在 用 全路径：
                    头内容 为 做 文件_读全部 用 全路径。
                    若 头内容 不等于 空：
                        结果 为 结果 加 头内容 加 「\n」。
                    完。
                    已找到 为 真。
                    j 为 做 计数 用 ctx 的 包含路径。
                完。
                j 为 j 加 1。
            完。
            若 非 已找到：
                结果 为 结果 加 「// [warn] 未找到: 」 加 头文件 加 「\n」。
            完。
        否则若 做 字串前缀 用 去空、「#define」：
            宏部分 为 做 字串分割 用 去空、「 」。
            若 做 计数 用 宏部分 大于或等于 3：
                做 追加 用 ctx 的 宏定义、宏部分 的 第 1 项 加 「=」 加 宏部分 的 第 2 项。
            完。
        否则若 做 字串前缀 用 去空、「#ifdef」 或者 做 字串前缀 用 去空、「#ifndef」 或者 做 字串前缀 用 去空、「#endif」 或者 做 字串前缀 用 去空、「#else」：
        否则：
            结果 为 结果 加 行 加 「\n」。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

C_提取头文件名 接受 行：
    可变 开始 为 负 1。
    可变 结束 为 负 1。
    可变 k 为 0。
    只要 k 小于 做 字串_长度 用 行：
        ch 为 做 字串_取 用 行、k。
        若 ch 等于 60 或者 ch 等于 34：
            开始 为 k 加 1。
        完。
        若 (ch 等于 62 或者 ch 等于 34) 并且 开始 大于 0 并且 k 大于 开始：
            结束 为 k。
            k 为 做 字串_长度 用 行。
        完。
        k 为 k 加 1。
    完。
    若 开始 大于 0 并且 结束 大于 开始：
        返还 做 字串_子串 用 行、开始、结束。
    完。
    返还 「」。
完。

C_解析 接受 源码：
    可变 节点 为 一组：（空）。
    行列表 为 做 字串分割 用 源码、「\n」。
    可变 i 为 0。
    可变 当前函数 为 空。
    可变 花括号深度 为 0。
    可变 函数体 为 「」。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去空格 用 行列表 的 第 i 项。
        若 做 字串_长度 用 行 大于 0 并且 非 做 字串前缀 用 行、「//」：
            可变 k 为 0。
            只要 k 小于 做 字串_长度 用 行：
                若 做 字串_取 用 行、k 等于 123：
                    花括号深度 为 花括号深度 加 1。
                完。
                若 做 字串_取 用 行、k 等于 125：
                    花括号深度 为 花括号深度 减 1。
                完。
                k 为 k 加 1。
            完。
            若 当前函数 不等于 空：
                函数体 为 函数体 加 行 加 「\n」。
                若 花括号深度 等于 0：
                    做 追加 用 节点、一组：「func」、当前函数、函数体。
                    当前函数 为 空。
                    函数体 为 「」。
                完。
            否则若 做 字串包含 用 行、「(」 并且 做 字串包含 用 行、「)」 并且 做 字串包含 用 行、「{」：
                当前函数 为 行。
                函数体 为 行 加 「\n」。
                若 花括号深度 等于 0：
                    做 追加 用 节点、一组：「func」、当前函数、函数体。
                    当前函数 为 空。
                    函数体 为 「」。
                完。
            否则：
                做 追加 用 节点、一组：「stmt」、行。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 节点。
完。

C_到Nova_IR 接受 AST：
    可变 IR 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AST：
        节点 为 AST 的 第 i 项。
        若 节点 的 第 0 项 等于 「func」：
            做 追加 用 IR、一组：「IR_FUNC_BEGIN」、节点 的 第 1 项。
            体行 为 做 字串分割 用 节点 的 第 2 项、「\n」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 体行：
                去空 为 做 字符串去空格 用 体行 的 第 j 项。
                若 做 字串前缀 用 去空、「return」：
                    做 追加 用 IR、一组：「IR_RETURN」、做 字串_子串 用 去空、7、做 字串_长度 用 去空 减 1。
                否则若 做 字串包含 用 去空、「printf」：
                    做 追加 用 IR、一组：「IR_CALL」、「Nova_printf」、去空。
                否则若 做 字串包含 用 去空、「malloc」：
                    做 追加 用 IR、一组：「IR_CALL」、「Nova_堆分配」、去空。
                否则若 做 字串包含 用 去空、「free」：
                    做 追加 用 IR、一组：「IR_CALL」、「Nova_堆释放」、去空。
                否则若 做 字串_长度 用 去空 大于 1：
                    做 追加 用 IR、一组：「IR_STMT」、去空。
                完。
                j 为 j 加 1。
            完。
            做 追加 用 IR、一组：「IR_FUNC_END」。
        否则：
            做 追加 用 IR、一组：「IR_STMT」、节点 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 IR。
完。

Python_词法分析 接受 源码：
    可变 词元列表 为 一组：（空）。
    行列表 为 做 字串分割 用 源码、「\n」。
    可变 缩进栈 为 一组：0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 行列表 的 第 i 项。
        若 做 字串_长度 用 行 等于 0：
            i 为 i 加 1。
            继续。
        完。
        可变 缩进 为 0。
        只要 缩进 小于 做 字串_长度 用 行 并且 做 字串_取 用 行、缩进 等于 32：
            缩进 为 缩进 加 1。
        完。
        内容 为 做 字符串去空格 用 行。
        若 做 字串前缀 用 内容、「#」：
            i 为 i 加 1。
            继续。
        完。
        当前级 为 缩进栈 的 第 做 计数 用 缩进栈 减 1 项。
        若 缩进 大于 当前级：
            做 追加 用 缩进栈、缩进。
            做 追加 用 词元列表、一组：「INDENT」、缩进。
        完。
        只要 缩进 小于 缩进栈 的 第 做 计数 用 缩进栈 减 1 项：
            做 列表_弹出 用 缩进栈。
            做 追加 用 词元列表、一组：「DEDENT」、缩进。
        完。
        词 为 做 字串分割 用 内容、「 」。
        可变 j 为 0。
        只要 j 小于 做 计数 用 词：
            若 做 字串_长度 用 词 的 第 j 项 大于 0：
                做 追加 用 词元列表、一组：「TOKEN」、词 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 词元列表、一组：「NEWLINE」、i。
        i 为 i 加 1。
    完。
    只要 做 计数 用 缩进栈 大于 1：
        做 列表_弹出 用 缩进栈。
        做 追加 用 词元列表、一组：「DEDENT」、0。
    完。
    返还 词元列表。
完。

Python_语法分析 接受 词元：
    可变 节点 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词元：
        若 词元 的 第 i 项 的 第 0 项 等于 「TOKEN」：
            值 为 词元 的 第 i 项 的 第 1 项。
            若 值 等于 「def」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 节点、一组：「def」、名、一组：（空）、一组：（空）。
            否则若 值 等于 「class」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 节点、一组：「class」、名、「」、一组：（空）。
            否则若 值 等于 「if」：
                做 追加 用 节点、一组：「if」、「」、一组：（空）、一组：（空）。
            否则若 值 等于 「for」：
                做 追加 用 节点、一组：「for」、「」、「」、一组：（空）。
            否则若 值 等于 「while」：
                做 追加 用 节点、一组：「while」、「」、一组：（空）。
            否则若 值 等于 「return」：
                表达式 为 「」。
                若 i 加 1 小于 做 计数 用 词元 并且 词元 的 第 i 加 1 项 的 第 0 项 等于 「TOKEN」：
                    表达式 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 节点、一组：「return」、表达式。
            否则若 值 等于 「import」 或者 值 等于 「from」：
                做 追加 用 节点、一组：「import」、值。
            否则：
                做 追加 用 节点、一组：「expr」、值。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 节点。
完。

Python_到Nova字节码 接受 AST：
    可变 字节码 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AST：
        节点 为 AST 的 第 i 项。
        若 节点 的 第 0 项 等于 「def」：
            做 追加 用 字节码、一组：0x30、节点 的 第 1 项。
        否则若 节点 的 第 0 项 等于 「return」：
            若 节点 的 第 1 项 不等于 「」：
                做 追加 用 字节码、一组：0x01、节点 的 第 1 项。
            完。
            做 追加 用 字节码、一组：0x11。
        否则若 节点 的 第 0 项 等于 「call」：
            做 追加 用 字节码、一组：0x10、节点 的 第 1 项。
        否则若 节点 的 第 0 项 等于 「expr」：
            若 做 字串包含 用 节点 的 第 1 项、「print」：
                做 追加 用 字节码、一组：0x10、「Nova_打印行」、节点 的 第 1 项。
            否则：
                做 追加 用 字节码、一组：0x40、节点 的 第 1 项。
            完。
        否则：
            做 追加 用 字节码、一组：0x40、节点 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 字节码。
完。

Python虚机状态 是 一种 事物，含 全局变量、局部变量、调用栈、内建函数表、模块表。

创建Python虚机 接受 零参：
    vm 为 新建 Python虚机状态。
    vm 的 全局变量 为 一组：（空）。
    vm 的 局部变量 为 一组：（空）。
    vm 的 调用栈 为 一组：（空）。
    vm 的 内建函数表 为 一组：（空）。
    vm 的 模块表 为 一组：（空）。
    返还 vm。
完。

Python_注册内建 接受 虚机、名、映射：
    做 追加 用 虚机 的 内建函数表、一组：名、映射。
完。

Python_注册模块 接受 虚机、名、映射：
    做 追加 用 虚机 的 模块表、一组：名、映射。
完。

Python_虚机执行 接受 虚机、字节码、参数：
    可变 栈 为 一组：（空）。
    可变 pc 为 0。
    可变 返回值 为 0。
    只要 pc 小于 做 计数 用 字节码：
        指令 为 字节码 的 第 pc 项。
        操作 为 指令 的 第 0 项。
        若 操作 等于 0x01：
            做 追加 用 栈、指令 的 第 1 项。
        完。
        若 操作 等于 0x02 并且 做 计数 用 栈 大于 0：
            做 列表_弹出 用 栈。
        完。
        若 操作 等于 0x03：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 加 b。
        完。
        若 操作 等于 0x04：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 减 b。
        完。
        若 操作 等于 0x05：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弳出 用 栈。
            做 追加 用 栈、a 乘 b。
        完。
        若 操作 等于 0x06：
            b 为 做 列表_弳出 用 栈。
            a 为 做 列表_弳出 用 栈。
            若 b 不等于 0：
                做 追加 用 栈、a 除以 b。
            完。
        完。
        若 操作 等于 0x10：
            函数名 为 指令 的 第 1 项。
            做 内核信息 用 「Python虚机: 调用 」 加 函数名。
        完。
        若 操作 等于 0x11：
            若 做 计数 用 栈 大于 0：
                返回值 为 做 列表_弳出 用 栈。
            完。
            返还 返回值。
        完。
        pc 为 pc 加 1。
    完。
    返还 返回值。
完。

Go_词法分析 接受 源码：
    可变 词元 为 一组：（空）。
    行列表 为 做 字串分割 用 源码、「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去空格 用 行列表 的 第 i 项。
        若 做 字串_长度 用 行 大于 0 并且 非 做 字串前缀 用 行、「//」：
            词 为 做 字串分割 用 行、「 」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 词：
                若 做 字串_长度 用 词 的 第 j 项 大于 0：
                    w 为 词 的 第 j 项。
                    若 w 等于 「package」 或者 w 等于 「import」 或者 w 等于 「func」 或者 w 等于 「var」 或者 w 等于 「const」 或者 w 等于 「type」 或者 w 等于 「struct」 或者 w 等于 「interface」 或者 w 等于 「if」 或者 w 等于 「else」 或者 w 等于 「for」 或者 w 等于 「range」 或者 w 等于 「switch」 或者 w 等于 「case」 或者 w 等于 「return」 或者 w 等于 「go」 或者 w 等于 「chan」 或者 w 等于 「select」 或者 w 等于 「defer」 或者 w 等于 「map」：
                        做 追加 用 词元、一组：「KW」、w。
                    否则：
                        做 追加 用 词元、一组：「ID」、w。
                    完。
                完。
                j 为 j 加 1。
            完。
            做 追加 用 词元、一组：「NL」、i。
        完。
        i 为 i 加 1。
    完。
    返还 词元。
完。

Go_语法分析 接受 词元：
    可变 节点 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词元：
        若 词元 的 第 i 项 的 第 0 项 等于 「KW」：
            kw 为 词元 的 第 i 项 的 第 1 项。
            若 kw 等于 「func」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 节点、一组：「func」、名、一组：（空）、一组：（空）。
            否则若 kw 等于 「go」：
                做 追加 用 节点、一组：「goroutine」、「」。
            否则若 kw 等于 「chan」：
                做 追加 用 节点、一组：「chan」、「」。
            否则若 kw 等于 「defer」：
                做 追加 用 节点、一组：「defer」、「」。
            否则若 kw 等于 「return」：
                表达 为 「」。
                若 i 加 1 小于 做 计数 用 词元 并且 词元 的 第 i 加 1 项 的 第 0 项 不等于 「NL」：
                    表达 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 节点、一组：「return」、表达。
            否则：
                做 追加 用 节点、一组：「kw」、kw。
            完。
        否则若 词元 的 第 i 项 的 第 0 项 等于 「ID」：
            做 追加 用 节点、一组：「expr」、词元 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 节点。
完。

Go_到Nova_IR 接受 AST：
    可变 IR 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AST：
        n 为 AST 的 第 i 项。
        若 n 的 第 0 项 等于 「func」：
            做 追加 用 IR、一组：「IR_FUNC_BEGIN」、n 的 第 1 项。
        完。
        若 n 的 第 0 项 等于 「goroutine」：
            做 追加 用 IR、一组：「IR_COROUTINE_SPAWN」、n 的 第 1 项。
        完。
        若 n 的 第 0 项 等于 「chan」：
            做 追加 用 IR、一组：「IR_CHANNEL_CREATE」、n 的 第 1 项。
        完。
        若 n 的 第 0 项 等于 「defer」：
            做 追加 用 IR、一组：「IR_DEFER_PUSH」、n 的 第 1 项。
        完。
        若 n 的 第 0 项 等于 「return」：
            做 追加 用 IR、一组：「IR_RETURN」、n 的 第 1 项。
        完。
        若 n 的 第 0 项 等于 「expr」：
            若 做 字串包含 用 n 的 第 1 项、「fmt.」：
                做 追加 用 IR、一组：「IR_CALL」、「Nova_格式打印」、n 的 第 1 项。
            否则：
                做 追加 用 IR、一组：「IR_EXPR」、n 的 第 1 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 IR。
完。

Rust_词法分析 接受 源码：
    可变 词元 为 一组：（空）。
    行列表 为 做 字串分割 用 源码、「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去空格 用 行列表 的 第 i 项。
        若 做 字串_长度 用 行 大于 0 并且 非 做 字串前缀 用 行、「//」：
            词 为 做 字串分割 用 行、「 」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 词：
                若 做 字串_长度 用 词 的 第 j 项 大于 0：
                    w 为 词 的 第 j 项。
                    若 w 等于 「fn」 或者 w 等于 「let」 或者 w 等于 「mut」 或者 w 等于 「struct」 或者 w 等于 「enum」 或者 w 等于 「impl」 或者 w 等于 「trait」 或者 w 等于 「pub」 或者 w 等于 「use」 或者 w 等于 「mod」 或者 w 等于 「match」 或者 w 等于 「if」 或者 w 等于 「else」 或者 w 等于 「loop」 或者 w 等于 「while」 或者 w 等于 「for」 或者 w 等于 「in」 或者 w 等于 「return」 或者 w 等于 「async」 或者 w 等于 「await」 或者 w 等于 「move」 或者 w 等于 「self」 或者 w 等于 「Self」 或者 w 等于 「where」：
                        做 追加 用 词元、一组：「KW」、w。
                    否则若 做 字串前缀 用 w、「&」：
                        做 追加 用 词元、一组：「REF」、w。
                    否则：
                        做 追加 用 词元、一组：「ID」、w。
                    完。
                完。
                j 为 j 加 1。
            完。
            做 追加 用 词元、一组：「NL」、i。
        完。
        i 为 i 加 1。
    完。
    返还 词元。
完。

Rust_语法分析 接受 词元：
    可变 节点 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词元：
        若 词元 的 第 i 项 的 第 0 项 等于 「KW」：
            kw 为 词元 的 第 i 项 的 第 1 项。
            若 kw 等于 「fn」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 节点、一组：「fn」、名、一组：（空）、一组：（空）。
            否则若 kw 等于 「let」：
                可变性 为 假。
                若 i 加 1 小于 做 计数 用 词元 并且 词元 的 第 i 加 1 项 的 第 1 项 等于 「mut」：
                    可变性 为 真。
                    i 为 i 加 1。
                完。
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 节点、一组：「let」、名、可变性。
            否则若 kw 等于 「struct」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 节点、一组：「struct」、名、一组：（空）。
            否则若 kw 等于 「impl」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 节点、一组：「impl」、名、一组：（空）。
            否则若 kw 等于 「trait」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 节点、一组：「trait」、名、一组：（空）。
            否则若 kw 等于 「match」：
                做 追加 用 节点、一组：「match」、「」、一组：（空）。
            否则若 kw 等于 「return」：
                表达 为 「」。
                若 i 加 1 小于 做 计数 用 词元 并且 词元 的 第 i 加 1 项 的 第 0 项 不等于 「NL」：
                    表达 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 节点、一组：「return」、表达。
            否则若 kw 等于 「async」：
                做 追加 用 节点、一组：「async」、「」。
            否则：
                做 追加 用 节点、一组：「kw」、kw。
            完。
        否则若 词元 的 第 i 项 的 第 0 项 等于 「REF」：
            做 追加 用 节点、一组：「borrow」、词元 的 第 i 项 的 第 1 项。
        否则若 词元 的 第 i 项 的 第 0 项 等于 「ID」：
            做 追加 用 节点、一组：「expr」、词元 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 节点。
完。

Rust_到Nova_IR 接受 AST：
    可变 IR 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AST：
        n 为 AST 的 第 i 项。
        若 n 的 第 0 项 等于 「fn」：
            做 追加 用 IR、一组：「IR_FUNC_BEGIN」、n 的 第 1 项。
        完。
        若 n 的 第 0 项 等于 「let」：
            做 追加 用 IR、一组：「IR_VAR_DECL」、n 的 第 1 项、n 的 第 2 项。
        完。
        若 n 的 第 0 项 等于 「struct」：
            做 追加 用 IR、一组：「IR_STRUCT_DECL」、n 的 第 1 项。
        完。
        若 n 的 第 0 项 等于 「impl」：
            做 追加 用 IR、一组：「IR_IMPL_BEGIN」、n 的 第 1 项。
        完。
        若 n 的 第 0 项 等于 「trait」：
            做 追加 用 IR、一组：「IR_INTERFACE_DECL」、n 的 第 1 项。
        完。
        若 n 的 第 0 项 等于 「match」：
            做 追加 用 IR、一组：「IR_PATTERN_MATCH」、n 的 第 1 项。
        完。
        若 n 的 第 0 项 等于 「borrow」：
            做 追加 用 IR、一组：「IR_BORROW」、n 的 第 1 项。
        完。
        若 n 的 第 0 项 等于 「async」：
            做 追加 用 IR、一组：「IR_ASYNC_BEGIN」。
        完。
        若 n 的 第 0 项 等于 「return」：
            做 追加 用 IR、一组：「IR_RETURN」、n 的 第 1 项。
        完。
        若 n 的 第 0 项 等于 「expr」：
            若 做 字串包含 用 n 的 第 1 项、「println!」：
                做 追加 用 IR、一组：「IR_CALL」、「Nova_打印行」、n 的 第 1 项。
            否则：
                做 追加 用 IR、一组：「IR_EXPR」、n 的 第 1 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 IR。
完。

Nova_JIT执行 接受 IR、参数：
    可变 返回值 为 0。
    可变 i 为 0。
    可变 变量表 为 一组：（空）。
    可变 延迟栈 为 一组：（空）。
    只要 i 小于 做 计数 用 IR：
        指令 为 IR 的 第 i 项。
        操作 为 指令 的 第 0 项。
        若 操作 等于 「IR_FUNC_BEGIN」：
            做 内核调试 用 「JIT: 进入函数 」 加 指令 的 第 1 项。
        完。
        若 操作 等于 「IR_FUNC_END」：
            做 内核调试 用 「JIT: 退出函数」。
        完。
        若 操作 等于 「IR_RETURN」：
            可变 d 为 做 计数 用 延迟栈 减 1。
            只要 d 大于或等于 0：
                做 内核调试 用 「JIT: 执行defer 」 加 延迟栈 的 第 d 项。
                d 为 d 减 1。
            完。
            延迟栈 为 一组：（空）。
            返回值 为 指令 的 第 1 项。
        完。
        若 操作 等于 「IR_CALL」：
            做 内核信息 用 「JIT: 调用 」 加 指令 的 第 1 项。
        完。
        若 操作 等于 「IR_VAR_DECL」：
            做 追加 用 变量表、一组：指令 的 第 1 项、0。
        完。
        若 操作 等于 「IR_COROUTINE_SPAWN」：
            做 内核信息 用 「JIT: 启动Nova协程」。
        完。
        若 操作 等于 「IR_CHANNEL_CREATE」：
            做 内核信息 用 「JIT: 创建Nova通道」。
        完。
        若 操作 等于 「IR_DEFER_PUSH」：
            做 追加 用 延迟栈、指令 的 第 1 项。
        完。
        若 操作 等于 「IR_BORROW」：
            做 内核调试 用 「JIT: Rust借用 → Nova只读引用」。
        完。
        若 操作 等于 「IR_STRUCT_DECL」 或者 操作 等于 「IR_IMPL_BEGIN」 或者 操作 等于 「IR_INTERFACE_DECL」：
            做 内核调试 用 「JIT: 类型声明 」 加 指令 的 第 1 项。
        完。
        若 操作 等于 「IR_STMT」 或者 操作 等于 「IR_EXPR」：
            做 内核调试 用 「JIT: 语句/表达式 」 加 指令 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 返回值。
完。

WASM模块 是 一种 事物，含 版本、函数表、内存、全局变量、导出表、导入表、内存大小。

WASM_解码 接受 字节码：
    模块 为 新建 WASM模块。
    模块 的 版本 为 字节码 的 第 4 项。
    模块 的 函数表 为 一组：（空）。
    模块 的 内存 为 一组：（空）。
    模块 的 全局变量 为 一组：（空）。
    模块 的 导出表 为 一组：（空）。
    模块 的 导入表 为 一组：（空）。
    模块 的 内存大小 为 1。
    可变 偏移 为 8。
    只要 偏移 小于 做 计数 用 字节码：
        section_id 为 字节码 的 第 偏移 项。
        偏移 为 偏移 加 1。
        长度 为 0。
        可变 移位 为 0。
        可变 字节 为 0x80。
        只要 字节 位与 0x80：
            字节 为 字节码 的 第 偏移 项。
            长度 为 长度 位或 (字节 位与 0x7F) 左移 移位。
            移位 为 移位 加 7。
            偏移 为 偏移 加 1。
        完。
        若 section_id 等于 5：
            若 偏移 加 2 小于 做 计数 用 字节码：
                模块 的 内存大小 为 字节码 的 第 偏移 加 1 项。
            完。
        完。
        偏移 为 偏移 加 长度。
    完。
    可变 k 为 0。
    只要 k 小于 模块 的 内存大小 乘 65536：
        做 追加 用 模块 的 内存、0。
        k 为 k 加 1。
    完。
    做 内核信息 用 「WASM: 解码完成 内存=」 加 做 整数转字符串 用 模块 的 内存大小 加 「页」。
    返还 模块。
完。

WASM_注入WASI 接受 模块：
    做 追加 用 模块 的 导入表、一组：「wasi_snapshot_preview1」、「fd_write」、「func」。
    做 追加 用 模块 的 导入表、一组：「wasi_snapshot_preview1」、「fd_read」、「func」。
    做 追加 用 模块 的 导入表、一组：「wasi_snapshot_preview1」、「fd_close」、「func」。
    做 追加 用 模块 的 导入表、一组：「wasi_snapshot_preview1」、「fd_seek」、「func」。
    做 追加 用 模块 的 导入表、一组：「wasi_snapshot_preview1」、「path_open」、「func」。
    做 追加 用 模块 的 导入表、一组：「wasi_snapshot_preview1」、「environ_get」、「func」。
    做 追加 用 模块 的 导入表、一组：「wasi_snapshot_preview1」、「environ_sizes_get」、「func」。
    做 追加 用 模块 的 导入表、一组：「wasi_snapshot_preview1」、「clock_time_get」、「func」。
    做 追加 用 模块 的 导入表、一组：「wasi_snapshot_preview1」、「proc_exit」、「func」。
    做 追加 用 模块 的 导入表、一组：「wasi_snapshot_preview1」、「args_get」、「func」。
    做 追加 用 模块 的 导入表、一组：「wasi_snapshot_preview1」、「args_sizes_get」、「func」。
完。

WASM_执行 接受 模块、入口、参数：
    可变 找到 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 模块 的 导出表：
        若 模块 的 导出表 的 第 i 项 的 第 0 项 等于 入口：
            找到 为 真。
        完。
        i 为 i 加 1。
    完。
    若 非 找到：
        做 内核警告 用 「WASM: 未找到入口函数 」 加 入口。
    完。
    做 内核信息 用 「WASM: 执行 」 加 入口 加 「 内存=」 加 做 整数转字符串 用 做 计数 用 模块 的 内存 加 「B」。
    返还 0。
完。

Java_解析类 接受 字节码：
    返还 做 JVM_加载类_从字节 用 字节码。
完。

Java_执行主方法 接受 类、参数：
    若 类 等于 空：
        返还 错误_格式。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 类 的 方法列表：
        若 类 的 方法列表 的 第 i 项 的 名称 等于 「main」：
            做 内核信息 用 「Java: 执行 main」。
            返还 做 JVM_执行字节码 用 类 的 方法列表 的 第 i 项 的 代码属性、类 的 方法列表 的 第 i 项 的 最大栈、类 的 方法列表 的 第 i 项 的 最大局部变量、类。
        完。
        i 为 i 加 1。
    完。
    做 内核警告 用 「Java: 未找到main方法」。
    返还 错误_不可执行。
完。

原生调用_SysV 接受 地址、参数：
    寄存器表 为 一组：「rdi」、「rsi」、「rdx」、「rcx」、「r8」、「r9」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数 并且 i 小于 6：
        i 为 i 加 1。
    完。
    可变 j 为 做 计数 用 参数 减 1。
    只要 j 大于或等于 6：
        j 为 j 减 1。
    完。
    做 内核调试 用 「ABI_SysV: 调用 0x」 加 做 整数转十六进制 用 地址 加 「 参数=」 加 做 整数转字符串 用 做 计数 用 参数。
    返还 0。
完。

原生调用_Win64 接受 地址、参数：
    寄存器表 为 一组：「rcx」、「rdx」、「r8」、「r9」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数 并且 i 小于 4：
        i 为 i 加 1。
    完。
    可变 j 为 做 计数 用 参数 减 1。
    只要 j 大于或等于 4：
        j 为 j 减 1。
    完。
    做 内核调试 用 「ABI_Win64: 调用 0x」 加 做 整数转十六进制 用 地址 加 「 参数=」 加 做 整数转字符串 用 做 计数 用 参数。
    返还 0。
完。

原生调用_Nova 接受 地址、参数：
    可变 i 为 做 计数 用 参数 减 1。
    只要 i 大于或等于 0：
        i 为 i 减 1。
    完。
    做 内核调试 用 「ABI_Nova: 调用 0x」 加 做 整数转十六进制 用 地址 加 「 参数=」 加 做 整数转字符串 用 做 计数 用 参数。
    返还 0。
完。

JVM_加载类_从字节 接受 字节码：
    若 字节码 等于 空 或者 做 计数 用 字节码 小于 10：
        返还 空。
    完。
    若 字节码 的 第 0 项 不等于 0xCA 或者 字节码 的 第 1 项 不等于 0xFE 或者 字节码 的 第 2 项 不等于 0xBA 或者 字节码 的 第 3 项 不等于 0xBE：
        返还 空。
    完。
    次版本 为 做 读取u16_大端 用 字节码、4。
    主版本 为 做 读取u16_大端 用 字节码、6。
    返还 做 JVM_解析类文件 用 字节码、主版本。
完。

外语言_执行Kotlin 接受 路径、参数：
    做 内核信息 用 「外语言: Kotlin → JVM字节码 → Nova执行: 」 加 路径。
    源码 为 做 文件_读全部 用 路径。
    若 源码 等于 空：
        返还 错误_不存在。
    完。
    可变 词元 为 一组：（空）。
    行列表 为 做 字串分割 用 源码、「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去空格 用 行列表 的 第 i 项。
        若 做 字串_长度 用 行 大于 0 并且 非 做 字串前缀 用 行、「//」：
            词 为 做 字串分割 用 行、「 」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 词：
                若 做 字串_长度 用 词 的 第 j 项 大于 0：
                    w 为 词 的 第 j 项。
                    若 w 等于 「fun」 或者 w 等于 「val」 或者 w 等于 「var」 或者 w 等于 「class」 或者 w 等于 「object」 或者 w 等于 「interface」 或者 w 等于 「when」 或者 w 等于 「is」 或者 w 等于 「suspend」 或者 w 等于 「launch」 或者 w 等于 「async」 或者 w 等于 「await」 或者 w 等于 「return」 或者 w 等于 「if」 或者 w 等于 「else」 或者 w 等于 「for」 或者 w 等于 「while」 或者 w 等于 「data」 或者 w 等于 「sealed」 或者 w 等于 「companion」：
                        做 追加 用 词元、一组：「KW」、w。
                    否则：
                        做 追加 用 词元、一组：「ID」、w。
                    完。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 IR 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 词元：
        若 词元 的 第 i 项 的 第 0 项 等于 「KW」：
            kw 为 词元 的 第 i 项 的 第 1 项。
            若 kw 等于 「fun」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 IR、一组：「IR_FUNC_BEGIN」、名。
            完。
            若 kw 等于 「suspend」：
                做 追加 用 IR、一组：「IR_COROUTINE_MARKER」。
            完。
            若 kw 等于 「launch」 或者 kw 等于 「async」：
                做 追加 用 IR、一组：「IR_COROUTINE_SPAWN」、kw。
            完。
            若 kw 等于 「return」：
                表达 为 「」。
                若 i 加 1 小于 做 计数 用 词元 并且 词元 的 第 i 加 1 项 的 第 0 项 等于 「ID」：
                    表达 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 IR、一组：「IR_RETURN」、表达。
            完。
            若 kw 等于 「data」：
                做 追加 用 IR、一组：「IR_DATA_CLASS」。
            完。
            若 kw 等于 「when」：
                做 追加 用 IR、一组：「IR_PATTERN_MATCH」、「」。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 做 Nova_JIT执行 用 IR、参数。
完。

外语言_执行Swift 接受 路径、参数：
    做 内核信息 用 「外语言: Swift → Nova IR → 执行: 」 加 路径。
    源码 为 做 文件_读全部 用 路径。
    若 源码 等于 空：
        返还 错误_不存在。
    完。
    可变 词元 为 一组：（空）。
    行列表 为 做 字串分割 用 源码、「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去空格 用 行列表 的 第 i 项。
        若 做 字串_长度 用 行 大于 0 并且 非 做 字串前缀 用 行、「//」：
            词 为 做 字串分割 用 行、「 」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 词：
                若 做 字串_长度 用 词 的 第 j 项 大于 0：
                    w 为 词 的 第 j 项。
                    若 w 等于 「func」 或者 w 等于 「let」 或者 w 等于 「var」 或者 w 等于 「struct」 或者 w 等于 「class」 或者 w 等于 「enum」 或者 w 等于 「protocol」 或者 w 等于 「extension」 或者 w 等于 「if」 或者 w 等于 「else」 或者 w 等于 「guard」 或者 w 等于 「switch」 或者 w 等于 「case」 或者 w 等于 「for」 或者 w 等于 「while」 或者 w 等于 「return」 或者 w 等于 「async」 或者 w 等于 「await」 或者 w 等于 「throws」 或者 w 等于 「try」 或者 w 等于 「import」 或者 w 等于 「self」 或者 w 等于 「Self」 或者 w 等于 「init」：
                        做 追加 用 词元、一组：「KW」、w。
                    否则：
                        做 追加 用 词元、一组：「ID」、w。
                    完。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 IR 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 词元：
        若 词元 的 第 i 项 的 第 0 项 等于 「KW」：
            kw 为 词元 的 第 i 项 的 第 1 项。
            若 kw 等于 「func」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 IR、一组：「IR_FUNC_BEGIN」、名。
            完。
            若 kw 等于 「struct」：
                做 追加 用 IR、一组：「IR_STRUCT_DECL」、「」。
            完。
            若 kw 等于 「protocol」：
                做 追加 用 IR、一组：「IR_INTERFACE_DECL」、「」。
            完。
            若 kw 等于 「async」：
                做 追加 用 IR、一组：「IR_ASYNC_BEGIN」。
            完。
            若 kw 等于 「return」：
                表达 为 「」。
                若 i 加 1 小于 做 计数 用 词元 并且 词元 的 第 i 加 1 项 的 第 0 项 等于 「ID」：
                    表达 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 IR、一组：「IR_RETURN」、表达。
            完。
            若 kw 等于 「guard」：
                做 追加 用 IR、一组：「IR_GUARD」。
            完。
        否则若 词元 的 第 i 项 的 第 0 项 等于 「ID」：
            若 做 字串包含 用 词元 的 第 i 项 的 第 1 项、「print」：
                做 追加 用 IR、一组：「IR_CALL」、「Nova_打印行」、词元 的 第 i 项 的 第 1 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 做 Nova_JIT执行 用 IR、参数。
完。

外语言_执行JS 接受 路径、参数：
    做 内核信息 用 「外语言: JS/TS → Nova V8级引擎 → 执行: 」 加 路径。
    源码 为 做 文件_读全部 用 路径。
    若 源码 等于 空：
        返还 错误_不存在。
    完。
    若 做 字串后缀 用 路径、「.ts」 或者 做 字串后缀 用 路径、「.tsx」：
        做 内核信息 用 「外语言: TypeScript → 类型擦除 → JS」。
    完。
    可变 词元 为 一组：（空）。
    行列表 为 做 字串分割 用 源码、「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去空格 用 行列表 的 第 i 项。
        若 做 字串_长度 用 行 大于 0 并且 非 做 字串前缀 用 行、「//」：
            词 为 做 字串分割 用 行、「 」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 词：
                若 做 字串_长度 用 词 的 第 j 项 大于 0：
                    w 为 词 的 第 j 项。
                    若 w 等于 「function」 或者 w 等于 「const」 或者 w 等于 「let」 或者 w 等于 「var」 或者 w 等于 「class」 或者 w 等于 「new」 或者 w 等于 「return」 或者 w 等于 「if」 或者 w 等于 「else」 或者 w 等于 「for」 或者 w 等于 「while」 或者 w 等于 「switch」 或者 w 等于 「case」 或者 w 等于 「async」 或者 w 等于 「await」 或者 w 等于 「yield」 或者 w 等于 「import」 或者 w 等于 「export」 或者 w 等于 「from」 或者 w 等于 「of」 或者 w 等于 「this」 或者 w 等于 「try」 或者 w 等于 「catch」 或者 w 等于 「throw」：
                        做 追加 用 词元、一组：「KW」、w。
                    否则：
                        做 追加 用 词元、一组：「ID」、w。
                    完。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 字节码 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 词元：
        若 词元 的 第 i 项 的 第 0 项 等于 「KW」：
            kw 为 词元 的 第 i 项 的 第 1 项。
            若 kw 等于 「function」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 字节码、一组：0x30、名。
            完。
            若 kw 等于 「async」：
                做 追加 用 字节码、一组：0x50、「async」。
            完。
            若 kw 等于 「await」：
                做 追加 用 字节码、一组：0x51、「await」。
            完。
            若 kw 等于 「return」：
                表达 为 「」。
                若 i 加 1 小于 做 计数 用 词元 并且 词元 的 第 i 加 1 项 的 第 0 项 等于 「ID」：
                    表达 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                若 表达 不等于 「」：
                    做 追加 用 字节码、一组：0x01、表达。
                完。
                做 追加 用 字节码、一组：0x11。
            完。
        否则若 词元 的 第 i 项 的 第 0 项 等于 「ID」：
            v 为 词元 的 第 i 项 的 第 1 项。
            若 做 字串包含 用 v、「console.log」 或者 做 字串包含 用 v、「console.」：
                做 追加 用 字节码、一组：0x10、「Nova_打印行」、v。
            否则若 做 字串包含 用 v、「require」 或者 做 字串包含 用 v、「import」：
                做 追加 用 字节码、一组：0x60、v。
            完。
        完。
        i 为 i 加 1。
    完。
    vm 为 做 创建Python虚机。
    做 Python_注册内建 用 vm、「console.log」、「Nova_打印行」。
    做 Python_注册内建 用 vm、「setTimeout」、「Nova_定时器」。
    做 Python_注册内建 用 vm、「Promise」、「Nova_Future」。
    返还 做 Python_虚机执行 用 vm、字节码、参数。
完。

外语言_执行Lua 接受 路径、参数：
    做 内核信息 用 「外语言: Lua → Nova执行: 」 加 路径。
    源码 为 做 文件_读全部 用 路径。
    若 源码 等于 空：
        返还 错误_不存在。
    完。
    可变 词元 为 一组：（空）。
    行列表 为 做 字串分割 用 源码、「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去空格 用 行列表 的 第 i 项。
        若 做 字串_长度 用 行 大于 0 并且 非 做 字串前缀 用 行、「--」：
            词 为 做 字串分割 用 行、「 」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 词：
                若 做 字串_长度 用 词 的 第 j 项 大于 0：
                    w 为 词 的 第 j 项。
                    若 w 等于 「function」 或者 w 等于 「local」 或者 w 等于 「end」 或者 w 等于 「if」 或者 w 等于 「then」 或者 w 等于 「else」 或者 w 等于 「elseif」 或者 w 等于 「for」 或者 w 等于 「while」 或者 w 等于 「do」 或者 w 等于 「repeat」 或者 w 等于 「until」 或者 w 等于 「return」 或者 w 等于 「nil」 或者 w 等于 「true」 或者 w 等于 「false」 或者 w 等于 「and」 或者 w 等于 「or」 或者 w 等于 「not」 或者 w 等于 「in」：
                        做 追加 用 词元、一组：「KW」、w。
                    否则：
                        做 追加 用 词元、一组：「ID」、w。
                    完。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 IR 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 词元：
        若 词元 的 第 i 项 的 第 0 项 等于 「KW」：
            kw 为 词元 的 第 i 项 的 第 1 项。
            若 kw 等于 「function」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 IR、一组：「IR_FUNC_BEGIN」、名。
            完。
            若 kw 等于 「end」：
                做 追加 用 IR、一组：「IR_FUNC_END」。
            完。
            若 kw 等于 「return」：
                表达 为 「」。
                若 i 加 1 小于 做 计数 用 词元 并且 词元 的 第 i 加 1 项 的 第 0 项 等于 「ID」：
                    表达 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 IR、一组：「IR_RETURN」、表达。
            完。
            若 kw 等于 「local」：
                做 追加 用 IR、一组：「IR_VAR_DECL」、「」、真。
            完。
        否则若 词元 的 第 i 项 的 第 0 项 等于 「ID」：
            若 做 字串包含 用 词元 的 第 i 项 的 第 1 项、「print」：
                做 追加 用 IR、一组：「IR_CALL」、「Nova_打印行」、词元 的 第 i 项 的 第 1 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 做 Nova_JIT执行 用 IR、参数。
完。

外语言_执行Ruby 接受 路径、参数：
    做 内核信息 用 「外语言: Ruby → Nova执行: 」 加 路径。
    源码 为 做 文件_读全部 用 路径。
    若 源码 等于 空：
        返还 错误_不存在。
    完。
    可变 IR 为 一组：（空）。
    行列表 为 做 字串分割 用 源码、「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去空格 用 行列表 的 第 i 项。
        若 做 字串_长度 用 行 大于 0 并且 非 做 字串前缀 用 行、「#」：
            若 做 字串前缀 用 行、「def 」：
                名 为 做 字串_子串 用 行、4、做 字串_长度 用 行。
                做 追加 用 IR、一组：「IR_FUNC_BEGIN」、名。
            否则若 行 等于 「end」：
                做 追加 用 IR、一组：「IR_FUNC_END」。
            否则若 做 字串前缀 用 行、「class 」：
                做 追加 用 IR、一组：「IR_STRUCT_DECL」、做 字串_子串 用 行、6、做 字串_长度 用 行。
            否则若 做 字串前缀 用 行、「module 」：
                做 追加 用 IR、一组：「IR_IMPL_BEGIN」、做 字串_子串 用 行、7、做 字串_长度 用 行。
            否则若 做 字串前缀 用 行、「return」：
                做 追加 用 IR、一组：「IR_RETURN」、「」。
            否则若 做 字串包含 用 行、「puts」 或者 做 字串包含 用 行、「print」：
                做 追加 用 IR、一组：「IR_CALL」、「Nova_打印行」、行。
            否则若 做 字串包含 用 行、「do」 或者 做 字串包含 用 行、「{」：
                做 追加 用 IR、一组：「IR_BLOCK_BEGIN」。
            否则：
                做 追加 用 IR、一组：「IR_EXPR」、行。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 做 Nova_JIT执行 用 IR、参数。
完。

外语言_执行PHP 接受 路径、参数：
    做 内核信息 用 「外语言: PHP → Nova执行: 」 加 路径。
    源码 为 做 文件_读全部 用 路径。
    若 源码 等于 空：
        返还 错误_不存在。
    完。
    可变 IR 为 一组：（空）。
    行列表 为 做 字串分割 用 源码、「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去空格 用 行列表 的 第 i 项。
        若 做 字串_长度 用 行 大于 0 并且 非 做 字串前缀 用 行、「//」 并且 非 做 字串前缀 用 行、「#」 并且 行 不等于 「<?php」 并且 行 不等于 「?>」：
            若 做 字串前缀 用 行、「function 」：
                名 为 做 字串_子串 用 行、9、做 字串_长度 用 行。
                做 追加 用 IR、一组：「IR_FUNC_BEGIN」、名。
            否则若 做 字串前缀 用 行、「class 」：
                做 追加 用 IR、一组：「IR_STRUCT_DECL」、做 字串_子串 用 行、6、做 字串_长度 用 行。
            否则若 做 字串前缀 用 行、「return」：
                做 追加 用 IR、一组：「IR_RETURN」、「」。
            否则若 做 字串包含 用 行、「echo」 或者 做 字串包含 用 行、「print」：
                做 追加 用 IR、一组：「IR_CALL」、「Nova_打印行」、行。
            否则若 做 字串前缀 用 行、「$」：
                做 追加 用 IR、一组：「IR_VAR_DECL」、行、真。
            否则若 做 字串包含 用 行、「require」 或者 做 字串包含 用 行、「include」：
                做 追加 用 IR、一组：「IR_CALL」、「Nova_文件包含」、行。
            否则：
                做 追加 用 IR、一组：「IR_EXPR」、行。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 做 Nova_JIT执行 用 IR、参数。
完。

外语言_执行CSharp 接受 路径、参数：
    做 内核信息 用 「外语言: C# → Nova CLR → 执行: 」 加 路径。
    源码 为 做 文件_读全部 用 路径。
    若 源码 等于 空：
        返还 错误_不存在。
    完。
    可变 词元 为 一组：（空）。
    行列表 为 做 字串分割 用 源码、「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去空格 用 行列表 的 第 i 项。
        若 做 字串_长度 用 行 大于 0 并且 非 做 字串前缀 用 行、「//」：
            词 为 做 字串分割 用 行、「 」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 词：
                若 做 字串_长度 用 词 的 第 j 项 大于 0：
                    w 为 词 的 第 j 项。
                    若 w 等于 「class」 或者 w 等于 「struct」 或者 w 等于 「interface」 或者 w 等于 「enum」 或者 w 等于 「namespace」 或者 w 等于 「using」 或者 w 等于 「public」 或者 w 等于 「private」 或者 w 等于 「static」 或者 w 等于 「void」 或者 w 等于 「int」 或者 w 等于 「string」 或者 w 等于 「bool」 或者 w 等于 「var」 或者 w 等于 「new」 或者 w 等于 「return」 或者 w 等于 「if」 或者 w 等于 「else」 或者 w 等于 「for」 或者 w 等于 「foreach」 或者 w 等于 「while」 或者 w 等于 「async」 或者 w 等于 「await」 或者 w 等于 「yield」 或者 w 等于 「try」 或者 w 等于 「catch」 或者 w 等于 「throw」 或者 w 等于 「override」：
                        做 追加 用 词元、一组：「KW」、w。
                    否则：
                        做 追加 用 词元、一组：「ID」、w。
                    完。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 IR 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 词元：
        若 词元 的 第 i 项 的 第 0 项 等于 「KW」：
            kw 为 词元 的 第 i 项 的 第 1 项。
            若 kw 等于 「class」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 IR、一组：「IR_STRUCT_DECL」、名。
            完。
            若 kw 等于 「void」 或者 kw 等于 「int」 或者 kw 等于 「string」 或者 kw 等于 「bool」：
                若 i 加 1 小于 做 计数 用 词元 并且 词元 的 第 i 加 1 项 的 第 0 项 等于 「ID」：
                    做 追加 用 IR、一组：「IR_FUNC_BEGIN」、词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
            完。
            若 kw 等于 「return」：
                表达 为 「」。
                若 i 加 1 小于 做 计数 用 词元 并且 词元 的 第 i 加 1 项 的 第 0 项 等于 「ID」：
                    表达 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 IR、一组：「IR_RETURN」、表达。
            完。
            若 kw 等于 「async」：
                做 追加 用 IR、一组：「IR_ASYNC_BEGIN」。
            完。
            若 kw 等于 「await」：
                做 追加 用 IR、一组：「IR_COROUTINE_SPAWN」、「await」。
            完。
            若 kw 等于 「foreach」：
                做 追加 用 IR、一组：「IR_EXPR」、「LINQ_foreach」。
            完。
        否则若 词元 的 第 i 项 的 第 0 项 等于 「ID」：
            若 做 字串包含 用 词元 的 第 i 项 的 第 1 项、「Console.Write」：
                做 追加 用 IR、一组：「IR_CALL」、「Nova_打印行」、词元 的 第 i 项 的 第 1 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 做 Nova_JIT执行 用 IR、参数。
完。

外语言_执行Dart 接受 路径、参数：
    做 内核信息 用 「外语言: Dart → Nova执行: 」 加 路径。
    源码 为 做 文件_读全部 用 路径。
    若 源码 等于 空：
        返还 错误_不存在。
    完。
    可变 词元 为 一组：（空）。
    行列表 为 做 字串分割 用 源码、「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去空格 用 行列表 的 第 i 项。
        若 做 字串_长度 用 行 大于 0 并且 非 做 字串前缀 用 行、「//」：
            词 为 做 字串分割 用 行、「 」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 词：
                若 做 字串_长度 用 词 的 第 j 项 大于 0：
                    w 为 词 的 第 j 项。
                    若 w 等于 「void」 或者 w 等于 「int」 或者 w 等于 「double」 或者 w 等于 「String」 或者 w 等于 「bool」 或者 w 等于 「var」 或者 w 等于 「final」 或者 w 等于 「const」 或者 w 等于 「class」 或者 w 等于 「extends」 或者 w 等于 「implements」 或者 w 等于 「mixin」 或者 w 等于 「abstract」 或者 w 等于 「if」 或者 w 等于 「else」 或者 w 等于 「for」 或者 w 等于 「while」 或者 w 等于 「return」 或者 w 等于 「async」 或者 w 等于 「await」 或者 w 等于 「import」 或者 w 等于 「new」 或者 w 等于 「this」 或者 w 等于 「super」：
                        做 追加 用 词元、一组：「KW」、w。
                    否则：
                        做 追加 用 词元、一组：「ID」、w。
                    完。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 IR 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 词元：
        若 词元 的 第 i 项 的 第 0 项 等于 「KW」：
            kw 为 词元 的 第 i 项 的 第 1 项。
            若 kw 等于 「void」 或者 kw 等于 「int」 或者 kw 等于 「double」 或者 kw 等于 「String」 或者 kw 等于 「bool」：
                若 i 加 1 小于 做 计数 用 词元 并且 词元 的 第 i 加 1 项 的 第 0 项 等于 「ID」：
                    做 追加 用 IR、一组：「IR_FUNC_BEGIN」、词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
            完。
            若 kw 等于 「class」：
                名 为 「」。
                若 i 加 1 小于 做 计数 用 词元：
                    名 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 IR、一组：「IR_STRUCT_DECL」、名。
            完。
            若 kw 等于 「return」：
                表达 为 「」。
                若 i 加 1 小于 做 计数 用 词元 并且 词元 的 第 i 加 1 项 的 第 0 项 等于 「ID」：
                    表达 为 词元 的 第 i 加 1 项 的 第 1 项。
                    i 为 i 加 1。
                完。
                做 追加 用 IR、一组：「IR_RETURN」、表达。
            完。
            若 kw 等于 「async」：
                做 追加 用 IR、一组：「IR_ASYNC_BEGIN」。
            完。
            若 kw 等于 「await」：
                做 追加 用 IR、一组：「IR_COROUTINE_SPAWN」、「await」。
            完。
        否则若 词元 的 第 i 项 的 第 0 项 等于 「ID」：
            若 做 字串包含 用 词元 的 第 i 项 的 第 1 项、「print」：
                做 追加 用 IR、一组：「IR_CALL」、「Nova_打印行」、词元 的 第 i 项 的 第 1 项。
            否则若 做 字串包含 用 词元 的 第 i 项 的 第 1 项、「Widget」 或者 做 字串包含 用 词元 的 第 i 项 的 第 1 项、「Scaffold」 或者 做 字串包含 用 词元 的 第 i 项 的 第 1 项、「Container」 或者 做 字串包含 用 词元 的 第 i 项 的 第 1 项、「Text」：
                做 追加 用 IR、一组：「IR_CALL」、「Nova_GUI_创建组件」、词元 的 第 i 项 的 第 1 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 做 Nova_JIT执行 用 IR、参数。
完。

外语言_报告 接受 零参：
    总数 为 _外语言_C执行数 加 _外语言_Python执行数 加 _外语言_Go执行数 加 _外语言_Rust执行数 加 _外语言_WASM执行数 加 _外语言_Java执行数。
    可变 r 为 「═══ 外语言桥接报告 ═══\n」。
    r 为 r 加 「总执行: 」 加 做 整数转字符串 用 总数 加 「\n」。
    r 为 r 加 「  C:      」 加 做 整数转字符串 用 _外语言_C执行数 加 「\n」。
    r 为 r 加 「  Python: 」 加 做 整数转字符串 用 _外语言_Python执行数 加 「\n」。
    r 为 r 加 「  Go:     」 加 做 整数转字符串 用 _外语言_Go执行数 加 「\n」。
    r 为 r 加 「  Rust:   」 加 做 整数转字符串 用 _外语言_Rust执行数 加 「\n」。
    r 为 r 加 「  WASM:   」 加 做 整数转字符串 用 _外语言_WASM执行数 加 「\n」。
    r 为 r 加 「  Java:   」 加 做 整数转字符串 用 _外语言_Java执行数 加 「\n」。
    返还 r。
完。

本章 收束。
