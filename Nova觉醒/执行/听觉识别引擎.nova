《听觉识别引擎》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

音频_语音验证码 为 0。

音频_语音指令 为 1。

音频_音乐 为 2。

音频_通知音 为 3。

音频_警告音 为 4。

音频帧 是 一种 事物，含 采样率、通道数、位深、数据、时长毫秒。

语音识别结果 是 一种 事物，含 文字、置信度、语言、时长毫秒。

音频特征 是 一种 事物，含 能量、过零率、频谱、节奏。

可变 听觉_已初始化 为 假。

可变 听觉_总识别次数 为 0。

可变 听觉_语音模型 为 空。

听觉识别_初始化 接受 零参：
    听觉_已初始化 为 真。
    做 内核信息 用 「听觉识别引擎: 音频验证码/语音识别/音频分析 就绪」。
完。

听觉识别_音频验证码 接受 音频数据：
    听觉_总识别次数 为 听觉_总识别次数 加 1。
    干净音频 为 做 听觉识别_降噪 用 音频数据。
    语音段 为 做 听觉识别_端点检测 用 干净音频。
    可变 特征序列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语音段：
        特征 为 做 听觉识别_提取特征 用 语音段 的 第 i 项。
        做 追加 用 特征序列、特征。
        i 为 i 加 1。
    完。
    可变 识别文字 为 「」。
    i 为 0。
    只要 i 小于 做 计数 用 特征序列：
        字符 为 做 听觉识别_TNF匹配 用 特征序列 的 第 i 项。
        识别文字 为 识别文字 加 字符。
        i 为 i 加 1。
    完。
    结果 为 新建 语音识别结果。
    结果 的 文字 为 识别文字。
    结果 的 置信度 为 65。
    结果 的 语言 为 「en」。
    返还 结果。
完。

听觉识别_降噪 接受 音频数据：
    可变 结果 为 一组：（空）。
    窗口 为 5。
    可变 i 为 0。
    只要 i 小于 做 计数 用 音频数据：
        可变 总和 为 0。
        可变 计 为 0。
        可变 j 为 i 减 窗口 除以 2。
        只要 j 小于或等于 i 加 窗口 除以 2：
            若 j 大于或等于 0 并且 j 小于 做 计数 用 音频数据：
                总和 为 总和 加 音频数据 的 第 j 项。
                计 为 计 加 1。
            完。
            j 为 j 加 1。
        完。
        若 计 大于 0：
            做 追加 用 结果、总和 除以 计。
        否则：
            做 追加 用 结果、0。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

听觉识别_端点检测 接受 音频数据：
    可变 段列表 为 一组：（空）。
    可变 当前段 为 一组：（空）。
    可变 在语音中 为 假。
    能量阈值 为 500。
    帧大小 为 320。
    可变 i 为 0。
    只要 i 小于 做 计数 用 音频数据：
        可变 能量 为 0。
        可变 j 为 0。
        只要 j 小于 帧大小 并且 i 加 j 小于 做 计数 用 音频数据：
            能量 为 能量 加 音频数据 的 第 i 加 j 项 乘 音频数据 的 第 i 加 j 项 除以 1000。
            j 为 j 加 1。
        完。
        若 能量 大于 能量阈值：
            若 非 在语音中：
                当前段 为 一组：（空）。
            完。
            在语音中 为 真。
            j 为 0。
            只要 j 小于 帧大小 并且 i 加 j 小于 做 计数 用 音频数据：
                做 追加 用 当前段、音频数据 的 第 i 加 j 项。
                j 为 j 加 1。
            完。
        否则：
            若 在语音中 并且 做 计数 用 当前段 大于 帧大小 乘 3：
                做 追加 用 段列表、当前段。
            完。
            在语音中 为 假。
        完。
        i 为 i 加 帧大小。
    完。
    若 在语音中 并且 做 计数 用 当前段 大于 帧大小 乘 3：
        做 追加 用 段列表、当前段。
    完。
    返还 段列表。
完。

听觉识别_提取特征 接受 语音段：
    特征 为 新建 音频特征。
    特征 的 频谱 为 一组：（空）。
    可变 总能量 为 0。
    可变 过零 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语音段：
        总能量 为 总能量 加 语音段 的 第 i 项 乘 语音段 的 第 i 项 除以 10000。
        若 i 大于 0：
            若 语音段 的 第 i 项 大于 0 并且 语音段 的 第 i 减 1 项 小于或等于 0 或者 语音段 的 第 i 项 小于或等于 0 并且 语音段 的 第 i 减 1 项 大于 0：
                过零 为 过零 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    特征 的 能量 为 总能量。
    特征 的 过零率 为 过零。
    返还 特征。
完。

听觉识别_TNF匹配 接受 特征：
    若 特征 的 过零率 大于 100：
        返还 「s」。
    完。
    若 特征 的 能量 大于 5000：
        返还 「a」。
    完。
    若 特征 的 能量 大于 2000：
        返还 「o」。
    完。
    返还 「e」。
完。

听觉识别_语音转文字 接受 音频帧：
    听觉_总识别次数 为 听觉_总识别次数 加 1。
    干净 为 做 听觉识别_降噪 用 音频帧 的 数据。
    段 为 做 听觉识别_端点检测 用 干净。
    可变 文字 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 段：
        特征 为 做 听觉识别_提取特征 用 段 的 第 i 项。
        字符 为 做 听觉识别_TNF匹配 用 特征。
        文字 为 文字 加 字符。
        i 为 i 加 1。
    完。
    结果 为 新建 语音识别结果。
    结果 的 文字 为 文字。
    结果 的 置信度 为 50。
    返还 结果。
完。

听觉识别_检测事件 接受 音频数据：
    特征 为 做 听觉识别_提取特征 用 音频数据。
    若 特征 的 能量 小于 10：
        返还 「静默」。
    完。
    若 特征 的 过零率 大于 200：
        返还 「噪声」。
    完。
    若 特征 的 能量 大于 8000 并且 特征 的 过零率 小于 50：
        返还 「通知音」。
    完。
    返还 「语音」。
完。

听觉识别_统计 接受 零参：
    返还 一组：听觉_总识别次数。
完。

本章 收束。
