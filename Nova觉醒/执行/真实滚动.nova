《真实滚动》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·时钟中枢》。

滚动类型_鼠标滚轮 为 0。

滚动类型_触控板 为 1。

滚动类型_触摸 为 2。

衰减_鼠标滚轮 为 92。

衰减_触控板 为 95。

衰减_触摸 为 88。

滚动_最小速度 为 5。

滚动_最大抖动 为 3。

滚动_触摸惯性帧 为 30。

滚动事件 是 一种 事物，含 方向、deltaY、deltaX、时间戳、类型。

滚动画像 是 一种 事物，含 平均初速、速度标准差、衰减率、水平偏移倾向、停顿概率、连续滚动概率。

可变 当前画像 为 0。

可变 滚动统计_总次数 为 0。

可变 滚动统计_鼠标次数 为 0。

可变 滚动统计_触控次数 为 0。

可变 滚动统计_触摸次数 为 0。

行为仿真_初始化滚动画像 接受 零参：
    当前画像 为 做 堆_分配 用 滚动画像。
    当前画像 的 平均初速 为 做 仿真_随机范围 用 60、100。
    当前画像 的 速度标准差 为 做 仿真_随机范围 用 15、35。
    当前画像 的 衰减率 为 做 仿真_随机范围 用 88、96。
    当前画像 的 水平偏移倾向 为 做 仿真_随机范围 用 负 1、1。
    当前画像 的 停顿概率 为 做 仿真_随机范围 用 3、12。
    当前画像 的 连续滚动概率 为 做 仿真_随机范围 用 60、85。
完。

行为仿真_滚动 接受 方向、总距离：
    仿真_总操作数 为 仿真_总操作数 加 1。
    滚动统计_总次数 为 滚动统计_总次数 加 1。
    滚动统计_鼠标次数 为 滚动统计_鼠标次数 加 1。
    若 当前画像 等于 0：
        做 行为仿真_初始化滚动画像。
    完。
    可变 事件列表 为 一组：（空）。
    可变 已滚动 为 0。
    可变 速度 为 做 仿真_高斯 用 当前画像 的 平均初速、当前画像 的 速度标准差。
    若 速度 小于 20：
        速度 为 20。
    完。
    可变 时间 为 做 读取毫秒。
    只要 已滚动 小于 总距离：
        若 做 仿真_随机范围 用 0、100 小于 当前画像 的 停顿概率：
            时间 为 时间 加 做 仿真_随机范围 用 30、150。
        完。
        可变 本次 为 速度。
        若 已滚动 加 本次 大于 总距离：
            本次 为 总距离 减 已滚动。
        完。
        水平偏移 为 做 仿真_随机范围 用 负 滚动_最大抖动、滚动_最大抖动 加 当前画像 的 水平偏移倾向。
        事件 为 新建 滚动事件。
        事件 的 方向 为 方向。
        事件 的 deltaY 为 本次。
        事件 的 deltaX 为 水平偏移。
        事件 的 时间戳 为 时间。
        事件 的 类型 为 滚动类型_鼠标滚轮。
        做 追加 用 事件列表、事件。
        已滚动 为 已滚动 加 本次。
        速度 为 速度 乘 当前画像 的 衰减率 除以 100。
        若 速度 小于 滚动_最小速度：
            速度 为 滚动_最小速度。
        完。
        时间 为 时间 加 做 仿真_随机范围 用 12、35。
    完。
    返还 事件列表。
完。

行为仿真_触控板滚动 接受 方向、总距离：
    仿真_总操作数 为 仿真_总操作数 加 1。
    滚动统计_触控次数 为 滚动统计_触控次数 加 1。
    可变 事件列表 为 一组：（空）。
    可变 已滚动 为 0。
    可变 时间 为 做 读取毫秒。
    步数 为 总距离 除以 3 加 做 仿真_随机范围 用 5、15。
    每步 为 总距离 除以 步数。
    可变 i 为 0。
    只要 i 小于 步数 并且 已滚动 小于 总距离：
        可变 本次 为 每步 加 做 仿真_随机范围 用 负 2、2。
        若 已滚动 加 本次 大于 总距离：
            本次 为 总距离 减 已滚动。
        完。
        若 本次 小于或等于 0：
            跳出。
        完。
        事件 为 新建 滚动事件。
        事件 的 方向 为 方向。
        事件 的 deltaY 为 本次。
        事件 的 deltaX 为 做 仿真_随机范围 用 负 1、1。
        事件 的 时间戳 为 时间。
        事件 的 类型 为 滚动类型_触控板。
        做 追加 用 事件列表、事件。
        已滚动 为 已滚动 加 本次。
        时间 为 时间 加 做 仿真_随机范围 用 8、18。
        i 为 i 加 1。
    完。
    可变 惯性速度 为 每步。
    可变 帧 为 0。
    只要 帧 小于 滚动_触摸惯性帧 并且 惯性速度 大于 1：
        事件 为 新建 滚动事件。
        事件 的 方向 为 方向。
        事件 的 deltaY 为 惯性速度。
        事件 的 deltaX 为 0。
        事件 的 时间戳 为 时间。
        事件 的 类型 为 滚动类型_触控板。
        做 追加 用 事件列表、事件。
        惯性速度 为 惯性速度 乘 衰减_触控板 除以 100。
        时间 为 时间 加 16。
        帧 为 帧 加 1。
    完。
    返还 事件列表。
完。

行为仿真_触摸滚动 接受 方向、总距离、起始x、起始y：
    仿真_总操作数 为 仿真_总操作数 加 1。
    滚动统计_触摸次数 为 滚动统计_触摸次数 加 1。
    可变 事件列表 为 一组：（空）。
    可变 已滚动 为 0。
    可变 时间 为 做 读取毫秒。
    可变 当前y 为 起始y。
    加速段 为 总距离 除以 4。
    匀速段 为 总距离 除以 2。
    减速段 为 总距离 减 加速段 减 匀速段。
    可变 速度 为 做 仿真_随机范围 用 5、15。
    最大速度 为 做 仿真_随机范围 用 30、60。
    只要 已滚动 小于 总距离：
        若 已滚动 小于 加速段：
            速度 为 速度 加 做 仿真_随机范围 用 2、5。
            若 速度 大于 最大速度：
                速度 为 最大速度。
            完。
        否则若 已滚动 大于 加速段 加 匀速段：
            速度 为 速度 乘 衰减_触摸 除以 100。
            若 速度 小于 3：
                速度 为 3。
            完。
        完。
        可变 本次 为 速度。
        若 已滚动 加 本次 大于 总距离：
            本次 为 总距离 减 已滚动。
        完。
        若 方向 大于 0：
            当前y 为 当前y 减 本次。
        否则：
            当前y 为 当前y 加 本次。
        完。
        做 追加 用 事件列表、一组：「touchmove」、起始x 加 做 仿真_随机范围 用 负 2、2、当前y、时间。
        已滚动 为 已滚动 加 本次。
        时间 为 时间 加 做 仿真_随机范围 用 10、22。
    完。
    可变 惯性 为 速度。
    可变 帧 为 0。
    只要 帧 小于 滚动_触摸惯性帧 并且 惯性 大于 2：
        若 方向 大于 0：
            当前y 为 当前y 减 惯性。
        否则：
            当前y 为 当前y 加 惯性。
        完。
        做 追加 用 事件列表、一组：「inertia」、起始x、当前y、时间。
        惯性 为 惯性 乘 衰减_触摸 除以 100。
        时间 为 时间 加 16。
        帧 为 帧 加 1。
    完。
    返还 事件列表。
完。

行为仿真_平滑滚动动画 接受 目标偏移、当前偏移、持续时间：
    差值 为 目标偏移 减 当前偏移。
    若 差值 等于 0：
        返还 一组：（空）。
    完。
    帧数 为 持续时间 除以 16。
    若 帧数 小于 1：
        帧数 为 1。
    完。
    可变 帧列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 帧数：
        t 为 (i 加 1) 乘 1000 除以 帧数。
        缓动 为 t 乘 t 乘 (3000 减 2 乘 t) 除以 1000000。
        偏移 为 当前偏移 加 差值 乘 缓动 除以 1000。
        做 追加 用 帧列表、偏移。
        i 为 i 加 1。
    完。
    返还 帧列表。
完。

仿真_高斯 接受 均值、标准差：
    sum 为 做 仿真_随机范围 用 0、1000 加 做 仿真_随机范围 用 0、1000 加 做 仿真_随机范围 用 0、1000。
    归一 为 (sum 减 1500) 乘 标准差 除以 500。
    返还 均值 加 归一。
完。

仿真_随机范围 接受 最小、最大：
    仿真_PRNG种子 为 仿真_PRNG种子 位异或 仿真_PRNG种子 左移 13。
    仿真_PRNG种子 为 仿真_PRNG种子 位异或 仿真_PRNG种子 右移 7。
    仿真_PRNG种子 为 仿真_PRNG种子 位异或 仿真_PRNG种子 左移 17。
    若 仿真_PRNG种子 小于 0：
        仿真_PRNG种子 为 0 减 仿真_PRNG种子。
    完。
    范围 为 最大 减 最小 加 1。
    若 范围 小于或等于 0：
        返还 最小。
    完。
    返还 最小 加 仿真_PRNG种子 取余于 范围。
完。

仿真_整数开方 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

行为仿真_统计 接受 零参：
    返还 一组：仿真_总操作数、做 计数 用 仿真_画像库、滚动统计_总次数、滚动统计_鼠标次数、滚动统计_触控次数、滚动统计_触摸次数。
完。

可变 AI_真实滚动_调用数 为 0。

可变 AI_真实滚动_异常数 为 0。

AI_真实滚动_监控 接受 零参：
    AI_真实滚动_调用数 为 AI_真实滚动_调用数 加 1。
    返还 AI_真实滚动_异常数 等于 0。
完。

AI_真实滚动_健康检查 接受 零参：
    若 AI_真实滚动_调用数 大于 0 并且 AI_真实滚动_异常数 乘 100 除以 AI_真实滚动_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
