《进程调度适配》

引用《..·中枢·进程调度》。

引用《..·中枢·进程任务》。

引用《..·推理·通信渠道》。

引用《..·中枢·日志中枢》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·整数转字符串》。

线程_主线程 为 0。

线程_渲染 为 1。

线程_JS执行 为 2。

线程_合成 为 3。

线程_IO 为 4。

线程_解码 为 5。

线程_AI推理 为 6。

线程_预取 为 7。

线程_工线 为 8。

优先级_实时 为 0。

优先级_交互 为 1。

优先级_普通 为 2。

优先级_后台 为 3。

浏览器线程 是 一种 事物，含 ID、类型、进程ID、名称、优先级、状态、CPU时间、切换次数、AI增强。

浏览器标签进程 是 一种 事物，含 标签ID、URL、线程列表、内存用量、CPU占用、沙箱级别。

可变 调度_线程表 为 一组：（空）。

可变 调度_标签进程表 为 一组：（空）。

可变 调度_下一个ID 为 1。

可变 调度_已初始化 为 假。

可变 调度_帧目标FPS 为 60。

可变 调度_帧预算微秒 为 16666。

浏览器调度_初始化 接受 零参：
    调度_线程表 为 一组：（空）。
    调度_标签进程表 为 一组：（空）。
    做 浏览器调度_创建线程 用 线程_主线程、「Browser-Main」、优先级_交互。
    做 浏览器调度_创建线程 用 线程_合成、「Browser-Compositor」、优先级_实时。
    做 浏览器调度_创建线程 用 线程_IO、「Browser-IO」、优先级_后台。
    做 浏览器调度_创建线程 用 线程_AI推理、「Browser-AI」、优先级_普通。
    做 浏览器调度_创建线程 用 线程_预取、「Browser-Prefetch」、优先级_后台。
    调度_已初始化 为 真。
    做 内核信息 用 「浏览器调度适配: 5个核心线程已创建, 直连内核Nova公平调度器」。
完。

浏览器调度_创建线程 接受 类型、名称、优先级：
    线程 为 新建 浏览器线程。
    线程 的 ID 为 调度_下一个ID。
    调度_下一个ID 为 调度_下一个ID 加 1。
    线程 的 类型 为 类型。
    线程 的 名称 为 名称。
    线程 的 优先级 为 优先级。
    线程 的 状态 为 0。
    线程 的 CPU时间 为 0。
    线程 的 切换次数 为 0。
    线程 的 AI增强 为 真。
    内核优先级 为 做 浏览器调度_映射优先级 用 优先级。
    线程 的 进程ID 为 做 进程_创建内核线程 用 名称、内核优先级。
    若 线程 的 进程ID 大于 0：
        若 优先级 等于 优先级_实时：
            做 调度_设置策略 用 线程 的 进程ID、调度策略_FIFO、50。
        否则若 优先级 等于 优先级_交互：
            做 调度_设置nice 用 线程 的 进程ID、负 5。
        否则若 优先级 等于 优先级_后台：
            做 调度_设置nice 用 线程 的 进程ID、10。
        完。
    完。
    做 追加 用 调度_线程表、线程。
    返还 线程 的 ID。
完。

浏览器调度_映射优先级 接受 浏览器优先级：
    若 浏览器优先级 等于 优先级_实时：
        返还 50。
    完。
    若 浏览器优先级 等于 优先级_交互：
        返还 115。
    完。
    若 浏览器优先级 等于 优先级_普通：
        返还 120。
    完。
    若 浏览器优先级 等于 优先级_后台：
        返还 130。
    完。
    返还 120。
完。

浏览器调度_创建标签进程 接受 标签ID、url：
    标签 为 新建 浏览器标签进程。
    标签 的 标签ID 为 标签ID。
    标签 的 URL 为 url。
    标签 的 线程列表 为 一组：（空）。
    标签 的 内存用量 为 0。
    标签 的 CPU占用 为 0。
    标签 的 沙箱级别 为 2。
    渲染ID 为 做 浏览器调度_创建线程 用 线程_渲染、「Tab-」 加 做 整数转字符串 用 标签ID 加 「-渲染」、优先级_交互。
    JS_ID 为 做 浏览器调度_创建线程 用 线程_JS执行、「Tab-」 加 做 整数转字符串 用 标签ID 加 「-JS」、优先级_普通。
    做 追加 用 标签 的 线程列表、渲染ID。
    做 追加 用 标签 的 线程列表、JS_ID。
    若 做 浏览器调度_AI需要解码线程 用 url：
        解码ID 为 做 浏览器调度_创建线程 用 线程_解码、「Tab-」 加 做 整数转字符串 用 标签ID 加 「-Decode」、优先级_后台。
        做 追加 用 标签 的 线程列表、解码ID。
    完。
    做 追加 用 调度_标签进程表、标签。
    返还 标签ID。
完。

浏览器调度_销毁标签进程 接受 标签ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_标签进程表：
        若 调度_标签进程表 的 第 i 项 的 标签ID 等于 标签ID：
            可变 j 为 0。
            只要 j 小于 做 计数 用 调度_标签进程表 的 第 i 项 的 线程列表：
                做 浏览器调度_终止线程 用 调度_标签进程表 的 第 i 项 的 线程列表 的 第 j 项。
                j 为 j 加 1。
            完。
            做 列表_移除 用 调度_标签进程表、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

浏览器调度_终止线程 接受 线程ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_线程表：
        若 调度_线程表 的 第 i 项 的 ID 等于 线程ID：
            调度_线程表 的 第 i 项 的 状态 为 3。
            做 进程_终止 用 调度_线程表 的 第 i 项 的 进程ID、0。
            做 列表_移除 用 调度_线程表、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

浏览器调度_帧开始 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_线程表：
        若 调度_线程表 的 第 i 项 的 类型 等于 线程_渲染 或者 调度_线程表 的 第 i 项 的 类型 等于 线程_合成：
            做 调度_设置nice 用 调度_线程表 的 第 i 项 的 进程ID、负 10。
        完。
        i 为 i 加 1。
    完。
完。

浏览器调度_帧结束 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_线程表：
        若 调度_线程表 的 第 i 项 的 类型 等于 线程_渲染：
            做 调度_设置nice 用 调度_线程表 的 第 i 项 的 进程ID、负 5。
        完。
        i 为 i 加 1。
    完。
完。

浏览器调度_AI优化 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_标签进程表：
        标签 为 调度_标签进程表 的 第 i 项。
        若 标签 的 CPU占用 大于 8000：
            做 浏览器调度_限制标签CPU 用 标签 的 标签ID、5000。
        完。
        i 为 i 加 1。
    完。
完。

浏览器调度_AI需要解码线程 接受 url：
    若 做 字符串查找 用 url、「youtube」 大于或等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 url、「bilibili」 大于或等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 url、「video」 大于或等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 url、「.mp4」 大于或等于 0：
        返还 真。
    完。
    返还 假。
完。

浏览器调度_限制标签CPU 接受 标签ID、最大占用：
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_标签进程表：
        若 调度_标签进程表 的 第 i 项 的 标签ID 等于 标签ID：
            可变 j 为 0。
            只要 j 小于 做 计数 用 调度_标签进程表 的 第 i 项 的 线程列表：
                线程ID 为 调度_标签进程表 的 第 i 项 的 线程列表 的 第 j 项。
                可变 k 为 0。
                只要 k 小于 做 计数 用 调度_线程表：
                    若 调度_线程表 的 第 k 项 的 ID 等于 线程ID：
                        做 调度_设置nice 用 调度_线程表 的 第 k 项 的 进程ID、15。
                    完。
                    k 为 k 加 1。
                完。
                j 为 j 加 1。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

浏览器调度_创建工线 接受 标签ID、名称：
    workerID 为 做 浏览器调度_创建线程 用 线程_工线、「工线-」 加 名称、优先级_普通。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_标签进程表：
        若 调度_标签进程表 的 第 i 项 的 标签ID 等于 标签ID：
            做 追加 用 调度_标签进程表 的 第 i 项 的 线程列表、workerID。
            返还 workerID。
        完。
        i 为 i 加 1。
    完。
    返还 workerID。
完。

浏览器调度_设置CPU亲和 接受 线程ID、CPU掩码：
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_线程表：
        若 调度_线程表 的 第 i 项 的 ID 等于 线程ID：
            做 调度_设置亲和性 用 调度_线程表 的 第 i 项 的 进程ID、CPU掩码。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

浏览器调度_绑定关键线程到性能核 接受 零参：
    性能核掩码 为 0x0F。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_线程表：
        若 调度_线程表 的 第 i 项 的 类型 等于 线程_合成 或者 调度_线程表 的 第 i 项 的 类型 等于 线程_主线程：
            做 调度_设置亲和性 用 调度_线程表 的 第 i 项 的 进程ID、性能核掩码。
        完。
        i 为 i 加 1。
    完。
完。

内存_压力低 为 0。

内存_压力中 为 1。

内存_压力高 为 2。

可变 当前内存压力 为 0。

浏览器调度_内存压力通知 接受 压力级别：
    当前内存压力 为 压力级别。
    若 压力级别 等于 内存_压力高：
        可变 i 为 0。
        只要 i 小于 做 计数 用 调度_标签进程表：
            可变 j 为 0。
            只要 j 小于 做 计数 用 调度_标签进程表 的 第 i 项 的 线程列表：
                线程ID 为 调度_标签进程表 的 第 i 项 的 线程列表 的 第 j 项。
                做 浏览器调度_挂起线程 用 线程ID。
                j 为 j 加 1。
            完。
            i 为 i 加 1。
        完。
    否则若 压力级别 等于 内存_压力低：
        可变 i 为 0。
        只要 i 小于 做 计数 用 调度_线程表：
            若 调度_线程表 的 第 i 项 的 状态 等于 2：
                做 浏览器调度_恢复线程 用 调度_线程表 的 第 i 项 的 ID。
            完。
            i 为 i 加 1。
        完。
    完。
完。

浏览器调度_挂起线程 接受 线程ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_线程表：
        若 调度_线程表 的 第 i 项 的 ID 等于 线程ID 并且 调度_线程表 的 第 i 项 的 状态 等于 0：
            调度_线程表 的 第 i 项 的 状态 为 2。
            做 调度_设置nice 用 调度_线程表 的 第 i 项 的 进程ID、19。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

浏览器调度_恢复线程 接受 线程ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_线程表：
        若 调度_线程表 的 第 i 项 的 ID 等于 线程ID 并且 调度_线程表 的 第 i 项 的 状态 等于 2：
            调度_线程表 的 第 i 项 的 状态 为 0。
            内核优先级 为 做 浏览器调度_映射优先级 用 调度_线程表 的 第 i 项 的 优先级。
            做 调度_设置nice 用 调度_线程表 的 第 i 项 的 进程ID、0。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

浏览器调度_设置帧率 接受 目标FPS：
    若 目标FPS 小于 15：
        目标FPS 为 15。
    完。
    若 目标FPS 大于 144：
        目标FPS 为 144。
    完。
    调度_帧目标FPS 为 目标FPS。
    调度_帧预算微秒 为 1000000 除以 目标FPS。
完。

浏览器调度_获取线程 接受 线程ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_线程表：
        若 调度_线程表 的 第 i 项 的 ID 等于 线程ID：
            返还 调度_线程表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

浏览器调度_获取运行线程数 接受 零参：
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_线程表：
        若 调度_线程表 的 第 i 项 的 状态 等于 0：
            计数 为 计数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计数。
完。

浏览器调度_统计 接受 零参：
    返还 一组：一组：「线程总数」、做 计数 用 调度_线程表、一组：「标签数」、做 计数 用 调度_标签进程表、一组：「目标FPS」、调度_帧目标FPS、一组：「运行线程」、做 浏览器调度_获取运行线程数、一组：「内存压力」、当前内存压力、一组：「线程池大小」、调度_线程池大小、一组：「池活跃数」、做 浏览器调度_线程池活跃数。
完。

可变 调度_线程池大小 为 8。

可变 调度_线程池最大 为 32。

可变 调度_线程池 为 一组：（空）。

浏览器调度_线程池初始化 接受 初始大小：
    若 初始大小 大于 调度_线程池最大：
        初始大小 为 调度_线程池最大。
    完。
    调度_线程池大小 为 初始大小。
    可变 i 为 0。
    只要 i 小于 初始大小：
        tid 为 做 浏览器调度_创建线程 用 线程_工线、「池线程-」 加 i、优先级_普通。
        做 追加 用 调度_线程池、tid。
        做 浏览器调度_挂起线程 用 tid。
        i 为 i 加 1。
    完。
完。

浏览器调度_线程池获取 接受 零参：
    若 做 计数 用 调度_线程池 等于 0：
        若 调度_线程池大小 小于 调度_线程池最大：
            tid 为 做 浏览器调度_创建线程 用 线程_工线、「池线程-动态」、优先级_普通。
            调度_线程池大小 为 调度_线程池大小 加 1。
            返还 tid。
        完。
        返还 负 1。
    完。
    tid 为 调度_线程池 的 第 做 计数 用 调度_线程池 减 1 项。
    做 删除 用 调度_线程池、做 计数 用 调度_线程池 减 1。
    做 浏览器调度_恢复线程 用 tid。
    返还 tid。
完。

浏览器调度_线程池归还 接受 tid：
    做 浏览器调度_挂起线程 用 tid。
    做 追加 用 调度_线程池、tid。
完。

浏览器调度_线程池活跃数 接受 零参：
    返还 调度_线程池大小 减 做 计数 用 调度_线程池。
完。

内存_标签上限 为 512 乘 1024。

浏览器调度_查询标签RSS 接受 标签ID：
    可变 i 为 0。
    可变 总内存 为 0。
    只要 i 小于 做 计数 用 调度_标签进程表：
        若 调度_标签进程表 的 第 i 项 的 标签ID 等于 标签ID：
            可变 j 为 0。
            只要 j 小于 做 计数 用 调度_标签进程表 的 第 i 项 的 线程列表：
                tid 为 调度_标签进程表 的 第 i 项 的 线程列表 的 第 j 项。
                信息 为 做 浏览器调度_获取线程 用 tid。
                若 信息 不等于 空：
                    总内存 为 总内存 加 做 调度_查询进程内存 用 信息 的 进程ID。
                完。
                j 为 j 加 1。
            完。
            返还 总内存。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

浏览器调度_检查内存超限 接受 零参：
    可变 i 为 0。
    可变 超限列表 为 一组：（空）。
    只要 i 小于 做 计数 用 调度_标签进程表：
        rss 为 做 浏览器调度_查询标签RSS 用 调度_标签进程表 的 第 i 项 的 标签ID。
        若 rss 大于 内存_标签上限：
            做 追加 用 超限列表、一组：调度_标签进程表 的 第 i 项 的 标签ID、rss。
        完。
        i 为 i 加 1。
    完。
    返还 超限列表。
完。

可变 调度_AI历史缓存 为 一组：（空）。

AI历史缓存上限 为 64。

浏览器调度_AI记录帧时间 接受 帧时间微秒：
    做 追加 用 调度_AI历史缓存、帧时间微秒。
    若 做 计数 用 调度_AI历史缓存 大于 AI历史缓存上限：
        做 删除 用 调度_AI历史缓存、0。
    完。
完。

浏览器调度_AI平均帧时间 接受 零参：
    若 做 计数 用 调度_AI历史缓存 等于 0：
        返还 0。
    完。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_AI历史缓存：
        总和 为 总和 加 调度_AI历史缓存 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总和 除以 做 计数 用 调度_AI历史缓存。
完。

浏览器调度_AI自适应帧率 接受 零参：
    平均 为 做 浏览器调度_AI平均帧时间。
    若 平均 等于 0：
        返还。
    完。
    若 平均 大于 33333：
        做 浏览器调度_设置帧率 用 30。
    否则若 平均 小于 8333：
        做 浏览器调度_设置帧率 用 120。
    否则若 平均 小于 16667：
        做 浏览器调度_设置帧率 用 60。
    完。
完。

浏览器调度_检测优先级反转 接受 零参：
    可变 异常列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_线程表：
        若 调度_线程表 的 第 i 项 的 状态 等于 1 并且 调度_线程表 的 第 i 项 的 优先级 大于或等于 优先级_高：
            做 追加 用 异常列表、调度_线程表 的 第 i 项 的 ID。
        完。
        i 为 i 加 1。
    完。
    返还 异常列表。
完。

浏览器调度_修复优先级反转 接受 零参：
    异常 为 做 浏览器调度_检测优先级反转。
    可变 i 为 0。
    只要 i 小于 做 计数 用 异常：
        线程 为 做 浏览器调度_获取线程 用 异常 的 第 i 项。
        若 线程 不等于 空：
            做 调度_设置nice 用 线程 的 进程ID、负 10。
        完。
        i 为 i 加 1。
    完。
完。

浏览器调度_关闭 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度_线程表：
        做 调度_结束进程 用 调度_线程表 的 第 i 项 的 进程ID。
        i 为 i 加 1。
    完。
    调度_线程表 为 一组：（空）。
    调度_标签进程表 为 一组：（空）。
    调度_线程池 为 一组：（空）。
完。

浏览器调度_诊断报告 接受 零参：
    反转 为 做 浏览器调度_检测优先级反转。
    超限 为 做 浏览器调度_检查内存超限。
    平均帧 为 做 浏览器调度_AI平均帧时间。
    返还 一组：一组：「线程总数」、做 计数 用 调度_线程表、一组：「标签数」、做 计数 用 调度_标签进程表、一组：「目标FPS」、调度_帧目标FPS、一组：「平均帧时间微秒」、平均帧、一组：「内存压力」、当前内存压力、一组：「优先级反转数」、做 计数 用 反转、一组：「内存超限标签」、做 计数 用 超限、一组：「线程池大小」、调度_线程池大小、一组：「池活跃数」、做 浏览器调度_线程池活跃数、一组：「运行线程数」、做 浏览器调度_获取运行线程数。
完。

本章 收束。
