《龙使》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·推理·智能体协调器》。

引用《..·中枢·AI代理》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·内核打印》。

引用《..·中枢·时钟中枢》。

引用《..·标准·进程接口》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·通知链》。

龙使_最大队列 为 256。

龙使_最大智能体 为 16。

龙使_最大工具 为 64。

龙使_心跳间隔 为 5000。

龙使_任务超时 为 60000。

任务状态_待执行 为 0。

任务状态_执行中 为 1。

任务状态_完成 为 2。

任务状态_失败 为 3。

任务状态_超时 为 4。

优先级_紧急 为 0。

优先级_高 为 1。

优先级_中 为 2。

优先级_低 为 3。

龙使任务 是 一种 事物，含 ID、类型、类型哈希、优先级、状态、步骤、创建时间、开始时间、完成时间、分配智能体、结果、错误信息、重试次数、最大重试。

智能体信息 是 一种 事物，含 名称、能力列表、状态、当前任务数、最大并发、总完成、总失败、上次心跳。

工具信息 是 一种 事物，含 名称、描述、处理函数、调用次数。

可变 龙使_运行中 为 假。

可变 龙使_总任务 为 0。

可变 龙使_成功数 为 0。

可变 龙使_失败数 为 0。

可变 龙使_任务队列 为 一组：（空）。

可变 龙使_输入队列 为 一组：（空）。

可变 龙使_智能体列表 为 一组：（空）。

可变 龙使_工具列表 为 一组：（空）。

可变 龙使_下一ID 为 1。

龙使_主函数 接受 零参：
    做 打印行 用 「[龙使] 智能体协调守护进程启动」。
    做 代理_初始化。
    做 协调器_初始化。
    做 龙使_注册内置工具。
    做 打印行 用 「[龙使] 智能体框架就绪 工具:」 加 做 整数转字符串 用 做 计数 用 龙使_工具列表。
    做 协调器_注册 用 「文件智能体」、一组：「文件读写」、「代码分析」、「代码生成」。
    做 协调器_注册 用 「终端智能体」、一组：「命令执行」、「系统管理」。
    做 协调器_注册 用 「网络智能体」、一组：「HTTP请求」、「文件下载」。
    做 协调器_注册 用 「AI智能体」、一组：「推理」、「对话」、「翻译」。
    做 龙使_注册智能体 用 「文件智能体」、一组：「文件读写」、「代码分析」、「代码生成」、4。
    做 龙使_注册智能体 用 「终端智能体」、一组：「命令执行」、「系统管理」、2。
    做 龙使_注册智能体 用 「网络智能体」、一组：「HTTP请求」、「文件下载」、8。
    做 龙使_注册智能体 用 「AI智能体」、一组：「推理」、「对话」、「翻译」、4。
    做 打印行 用 「[龙使] 已注册」 加 做 整数转字符串 用 做 计数 用 龙使_智能体列表 加 「个智能体」。
    龙使_运行中 为 真。
    可变 上次心跳 为 0。
    只要 龙使_运行中：
        当前时间 为 做 读取毫秒。
        任务 为 做 龙使_接收任务。
        若 任务 不等于 空：
            做 龙使_入队任务 用 任务。
        完。
        做 龙使_调度循环 用 当前时间。
        做 龙使_检查超时 用 当前时间。
        若 当前时间 减 上次心跳 大于 龙使_心跳间隔：
            做 龙使_健康检查 用 当前时间。
            上次心跳 为 当前时间。
        完。
        做 代理_tick。
        做 进程_睡眠毫秒 用 10。
    完。
    做 打印行 用 「[龙使] 已停止 总任务:」 加 做 整数转字符串 用 龙使_总任务 加 「 成功:」 加 做 整数转字符串 用 龙使_成功数 加 「 失败:」 加 做 整数转字符串 用 龙使_失败数。
完。

龙使_注册智能体 接受 名称、能力、最大并发：
    若 做 计数 用 龙使_智能体列表 大于或等于 龙使_最大智能体：
        返还。
    完。
    智能体 为 新建 智能体信息。
    智能体 的 名称 为 名称。
    智能体 的 能力列表 为 能力。
    智能体 的 状态 为 1。
    智能体 的 当前任务数 为 0。
    智能体 的 最大并发 为 最大并发。
    智能体 的 总完成 为 0。
    智能体 的 总失败 为 0。
    智能体 的 上次心跳 为 做 读取毫秒。
    做 追加 用 龙使_智能体列表、智能体。
完。

龙使_注册工具 接受 名称、描述、处理函数：
    若 做 计数 用 龙使_工具列表 大于或等于 龙使_最大工具：
        返还。
    完。
    工具 为 新建 工具信息。
    工具 的 名称 为 名称。
    工具 的 描述 为 描述。
    工具 的 处理函数 为 处理函数。
    工具 的 调用次数 为 0。
    做 追加 用 龙使_工具列表、工具。
完。

龙使_注册内置工具 接受 零参：
    做 龙使_注册工具 用 「文件读取」、「读取文件内容」、0。
    做 龙使_注册工具 用 「文件写入」、「写入文件内容」、0。
    做 龙使_注册工具 用 「命令执行」、「执行系统命令」、0。
    做 龙使_注册工具 用 「HTTP请求」、「发送HTTP请求」、0。
    做 龙使_注册工具 用 「AI推理」、「调用AI推理引擎」、0。
    做 龙使_注册工具 用 「代码分析」、「分析代码结构」、0。
完。

龙使_入队任务 接受 任务：
    若 做 计数 用 龙使_任务队列 大于或等于 龙使_最大队列：
        做 打印行 用 「[龙使] 任务队列已满, 拒绝任务」。
        返还。
    完。
    任务 的 ID 为 龙使_下一ID。
    龙使_下一ID 为 龙使_下一ID 加 1。
    任务 的 状态 为 任务状态_待执行。
    任务 的 创建时间 为 做 读取毫秒。
    任务 的 重试次数 为 0。
    若 任务 的 最大重试 等于 0：
        任务 的 最大重试 为 3。
    完。
    龙使_总任务 为 龙使_总任务 加 1。
    可变 插入位置 为 做 计数 用 龙使_任务队列。
    可变 i 为 0。
    只要 i 小于 做 计数 用 龙使_任务队列：
        若 龙使_任务队列 的 第 i 项 的 优先级 大于 任务 的 优先级：
            插入位置 为 i。
            跳出。
        完。
        i 为 i 加 1。
    完。
    若 插入位置 等于 做 计数 用 龙使_任务队列：
        做 追加 用 龙使_任务队列、任务。
    否则：
        做 列表_插入 用 龙使_任务队列、插入位置、任务。
    完。
完。

龙使_调度循环 接受 当前时间：
    可变 i 为 0。
    只要 i 小于 做 计数 用 龙使_任务队列：
        任务 为 龙使_任务队列 的 第 i 项。
        若 任务 的 状态 不等于 任务状态_待执行：
            i 为 i 加 1。
            继续。
        完。
        智能体 为 做 龙使_选择智能体 用 任务 的 类型。
        若 智能体 不等于 空 并且 智能体 的 当前任务数 小于 智能体 的 最大并发：
            任务 的 状态 为 任务状态_执行中。
            任务 的 开始时间 为 当前时间。
            任务 的 分配智能体 为 智能体 的 名称。
            智能体 的 当前任务数 为 智能体 的 当前任务数 加 1。
            做 打印行 用 「[龙使] 分派#」 加 做 整数转字符串 用 任务 的 ID 加 「: 」 加 任务 的 类型 加 「 → 」 加 智能体 的 名称。
            做 代理_提交任务 用 任务 的 类型哈希、800、任务 的 步骤。
        完。
        i 为 i 加 1。
    完。
完。

龙使_选择智能体 接受 任务类型：
    可变 最佳 为 空。
    可变 最低负载 为 999999。
    可变 i 为 0。
    只要 i 小于 做 计数 用 龙使_智能体列表：
        智能体 为 龙使_智能体列表 的 第 i 项。
        若 智能体 的 状态 不等于 1：
            i 为 i 加 1。
            继续。
        完。
        可变 j 为 0。
        只要 j 小于 做 计数 用 智能体 的 能力列表：
            若 智能体 的 能力列表 的 第 j 项 等于 任务类型 或者 做 字符串查找 用 任务类型、智能体 的 能力列表 的 第 j 项 大于或等于 0：
                若 智能体 的 当前任务数 小于 最低负载：
                    最佳 为 智能体。
                    最低负载 为 智能体 的 当前任务数。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

龙使_任务完成 接受 任务ID、结果：
    可变 i 为 0。
    只要 i 小于 做 计数 用 龙使_任务队列：
        若 龙使_任务队列 的 第 i 项 的 ID 等于 任务ID：
            龙使_任务队列 的 第 i 项 的 状态 为 任务状态_完成。
            龙使_任务队列 的 第 i 项 的 完成时间 为 做 读取毫秒。
            龙使_任务队列 的 第 i 项 的 结果 为 结果。
            龙使_成功数 为 龙使_成功数 加 1。
            做 龙使_释放智能体 用 龙使_任务队列 的 第 i 项 的 分配智能体、真。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

龙使_任务失败 接受 任务ID、错误：
    可变 i 为 0。
    只要 i 小于 做 计数 用 龙使_任务队列：
        若 龙使_任务队列 的 第 i 项 的 ID 等于 任务ID：
            任务 为 龙使_任务队列 的 第 i 项。
            做 龙使_释放智能体 用 任务 的 分配智能体、假。
            若 任务 的 重试次数 小于 任务 的 最大重试：
                任务 的 重试次数 为 任务 的 重试次数 加 1。
                任务 的 状态 为 任务状态_待执行。
                做 打印行 用 「[龙使] 重试#」 加 做 整数转字符串 用 任务 的 ID 加 「 第」 加 做 整数转字符串 用 任务 的 重试次数 加 「次」。
            否则：
                任务 的 状态 为 任务状态_失败。
                任务 的 完成时间 为 做 读取毫秒。
                任务 的 错误信息 为 错误。
                龙使_失败数 为 龙使_失败数 加 1。
                做 打印行 用 「[龙使] 任务#」 加 做 整数转字符串 用 任务 的 ID 加 「 最终失败: 」 加 错误。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

龙使_释放智能体 接受 智能体名、成功：
    可变 i 为 0。
    只要 i 小于 做 计数 用 龙使_智能体列表：
        若 龙使_智能体列表 的 第 i 项 的 名称 等于 智能体名：
            龙使_智能体列表 的 第 i 项 的 当前任务数 为 龙使_智能体列表 的 第 i 项 的 当前任务数 减 1。
            若 成功：
                龙使_智能体列表 的 第 i 项 的 总完成 为 龙使_智能体列表 的 第 i 项 的 总完成 加 1。
            否则：
                龙使_智能体列表 的 第 i 项 的 总失败 为 龙使_智能体列表 的 第 i 项 的 总失败 加 1。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

龙使_检查超时 接受 当前时间：
    可变 i 为 0。
    只要 i 小于 做 计数 用 龙使_任务队列：
        任务 为 龙使_任务队列 的 第 i 项。
        若 任务 的 状态 等于 任务状态_执行中：
            若 当前时间 减 任务 的 开始时间 大于 龙使_任务超时：
                做 打印行 用 「[龙使] 超时#」 加 做 整数转字符串 用 任务 的 ID。
                做 龙使_任务失败 用 任务 的 ID、「任务超时」。
            完。
        完。
        i 为 i 加 1。
    完。
完。

龙使_健康检查 接受 当前时间：
    可变 i 为 0。
    只要 i 小于 做 计数 用 龙使_智能体列表：
        智能体 为 龙使_智能体列表 的 第 i 项。
        若 智能体 的 状态 等于 1 并且 当前时间 减 智能体 的 上次心跳 大于 龙使_心跳间隔 乘 3：
            做 打印行 用 「[龙使] 智能体无响应: 」 加 智能体 的 名称。
            智能体 的 状态 为 0。
        完。
        i 为 i 加 1。
    完。
完。

龙使_清理完成任务 接受 零参：
    可变 i 为 做 计数 用 龙使_任务队列 减 1。
    只要 i 大于或等于 0：
        若 龙使_任务队列 的 第 i 项 的 状态 等于 任务状态_完成 或者 龙使_任务队列 的 第 i 项 的 状态 等于 任务状态_失败：
            做 列表_移除 用 龙使_任务队列、i。
        完。
        i 为 i 减 1。
    完。
完。

龙使_接收任务 接受 零参：
    若 做 计数 用 龙使_输入队列 等于 0：
        返还 空。
    完。
    任务 为 龙使_输入队列 的 第 0 项。
    做 列表_移除 用 龙使_输入队列、0。
    返还 任务。
完。

龙使_停止 接受 零参：
    龙使_运行中 为 假。
完。

龙使_统计 接受 零参：
    返还 一本字典：总任务 对应 龙使_总任务、成功 对应 龙使_成功数、失败 对应 龙使_失败数、队列 对应 做 计数 用 龙使_任务队列、智能体 对应 做 计数 用 龙使_智能体列表、工具 对应 做 计数 用 龙使_工具列表、成功率 对应 若 龙使_总任务 大于 0 则 龙使_成功数 乘 100 除以 龙使_总任务 否则 0。
完。

龙使_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 龙使_失败数 大于 龙使_总任务 除以 3 并且 龙使_总任务 大于 50：
        做 追加 用 问题、「龙使_失败数过高」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

龙使_综合报告 接受 零参：
    做 打印 用 「[龙使] ═══ 综合报告 ═══」。
    做 打印 用 「  龙使_运行中: 」 加 龙使_运行中。
    做 打印 用 「  龙使_总任务: 」 加 龙使_总任务。
    做 打印 用 「  龙使_成功数: 」 加 龙使_成功数。
    做 打印 用 「  龙使_失败数: 」 加 龙使_失败数。
    做 打印 用 「  健康: 」 加 若 做 龙使_健康评估 则 「正常」 否则 「异常」。
完。

龙使_全局重置 接受 零参：
    龙使_运行中 为 0。
    龙使_总任务 为 0。
    龙使_成功数 为 0。
    龙使_失败数 为 0。
    做 打印 用 「[龙使] 统计已重置」。
    返还 0。
完。

本章 收束。
