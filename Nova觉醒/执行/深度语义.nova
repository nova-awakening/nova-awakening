《深度语义》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

引用《.·用户输入处》。

意图推理_TNF深度分析 接受 输入：
    结果 为 新建 意图结果。
    结果 的 参数 为 一组：（空）。
    结果 的 需要确认 为 假。
    若 做 字符串查找 用 输入、「打开」 大于或等于 0：
        目标 为 做 字符串子串 用 输入、做 字符串查找 用 输入、「打开」 加 2、做 字符串长度 用 输入 减 做 字符串查找 用 输入、「打开」 减 2。
        目标 为 做 字符串修剪 用 目标。
        结果 的 意图类型 为 意图_系统命令。
        结果 的 置信度 为 75。
        做 追加 用 结果 的 参数、一组：「target」、目标。
        结果 的 回复文本 为 「正在打开: 」 加 目标。
        返还 结果。
    完。
    若 做 字符串查找 用 输入、「关闭」 大于或等于 0 或者 做 字符串查找 用 输入、「停止」 大于或等于 0：
        结果 的 意图类型 为 意图_进程管理。
        结果 的 置信度 为 70。
        结果 的 回复文本 为 「正在处理关闭请求」。
        返还 结果。
    完。
    若 做 字符串查找 用 输入、「查看」 大于或等于 0 或者 做 字符串查找 用 输入、「显示」 大于或等于 0 或者 做 字符串查找 用 输入、「看看」 大于或等于 0：
        结果 的 意图类型 为 意图_系统命令。
        结果 的 置信度 为 65。
        结果 的 回复文本 为 「正在获取信息...」。
        返还 结果。
    完。
    结果 的 意图类型 为 意图_AI对话。
    结果 的 置信度 为 40。
    结果 的 回复文本 为 『我是Nova AI助手, 你可以用自然语言操作系统, 例如「打开设置」「清理内存」「查看进程」』。
    返还 结果。
完。

意图推理_是URL 接受 输入：
    若 做 字符串查找 用 输入、「http://」 等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 输入、「https://」 等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 输入、「nova://」 等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 输入、「www.」 等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 输入、「.com」 大于或等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 输入、「.org」 大于或等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 输入、「.net」 大于或等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 输入、「.io」 大于或等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 输入、「.dev」 大于或等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 输入、「.cn」 大于或等于 0：
        返还 真。
    完。
    返还 假。
完。

意图推理_是计算 接受 输入：
    可变 有数字 为 假。
    可变 有运算符 为 假。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 输入：
        c 为 做 字符串字节 用 输入、i。
        若 c 大于或等于 48 并且 c 小于或等于 57：
            有数字 为 真。
        完。
        若 c 等于 43 或者 c 等于 45 或者 c 等于 42 或者 c 等于 47：
            有运算符 为 真。
        完。
        i 为 i 加 1。
    完。
    返还 有数字 并且 有运算符。
完。

意图推理_计算 接受 表达式：
    返还 「计算中...」。
完。

意图推理_执行 接受 结果：
    若 结果 的 需要确认：
        返还 「⚠ 需要确认: 」 加 结果 的 回复文本 加 「 (输入Y确认)」。
    完。
    若 结果 的 意图类型 等于 意图_导航：
        可变 i 为 0。
        只要 i 小于 做 计数 用 结果 的 参数：
            若 结果 的 参数 的 第 i 项 的 第 0 项 等于 「url」：
                做 事件循环_导航 用 结果 的 参数 的 第 i 项 的 第 1 项。
                返还 结果 的 回复文本。
            完。
            i 为 i 加 1。
        完。
    完。
    若 结果 的 意图类型 等于 意图_系统命令：
        返还 「✓ 已执行: 」 加 结果 的 内核操作。
    完。
    若 结果 的 意图类型 等于 意图_AI对话：
        返还 结果 的 回复文本。
    完。
    返还 结果 的 回复文本。
完。

意图推理_统计 接受 零参：
    返还 一组：意图_总推理次数、意图_命中次数、做 计数 用 意图_命令表。
完。

本章 收束。
