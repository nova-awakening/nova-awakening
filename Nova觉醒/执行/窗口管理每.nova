《窗口管理每》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·生成窗口元》。

引用《.·绘制命令》。

引用《..·中枢·硬链接》。

引用《..·外联·URL解析辅助》。

引用《.·渲染初始化》。

引用《.·桌面元素生成》。

引用《.·渲染层管理》。

引用《.·布局计算》。

引用《.·用户输入处》。

Nova原生桌面_同步文档状态 接受 零参：
    若 非 桌面_已初始化：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 桌面 的 窗口列表：
        若 桌面 的 窗口列表 的 第 i 项 的 可见：
            桌面 的 窗口列表 的 第 i 项 的 DOM文档 为 做 Nova原生桌面_构建窗口文档 用 桌面 的 窗口列表 的 第 i 项。
        否则：
            桌面 的 窗口列表 的 第 i 项 的 DOM文档 为 空。
        完。
        i 为 i 加 1。
    完。
    桌面 的 桌面DOM 为 做 Nova原生桌面_构建桌面NML。
完。

Nova原生桌面_查找窗口索引 接受 窗口ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 桌面 的 窗口列表：
        若 桌面 的 窗口列表 的 第 i 项 的 ID 等于 窗口ID：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

Nova原生桌面_查找窗口ID_按URL 接受 url：
    可变 i 为 0。
    只要 i 小于 做 计数 用 桌面 的 窗口列表：
        若 桌面 的 窗口列表 的 第 i 项 的 URL 等于 url：
            返还 桌面 的 窗口列表 的 第 i 项 的 ID。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

Nova原生桌面_设置壳输入缓冲 接受 输入：
    若 非 桌面_已初始化：
        返还。
    完。
    桌面 的 壳输入缓冲 为 输入。
完。

Nova原生桌面_记录壳状态 接受 输入、动作、结果：
    若 非 桌面_已初始化：
        返还。
    完。
    桌面 的 壳最后输入 为 输入。
    桌面 的 壳输入缓冲 为 「」。
    若 做 字符串长度 用 动作 大于 0：
        桌面 的 壳最后动作 为 动作。
    完。
    若 做 字符串长度 用 结果 大于 0：
        桌面 的 壳最后结果 为 结果。
    完。
完。

Nova原生桌面_同步系统状态 接受 核心状态、核心副文本、视觉状态、视觉副文本、声音状态、声音副文本、形象状态、形象副文本：
    若 非 桌面_已初始化：
        返还。
    完。
    桌面 的 核心状态 为 核心状态。
    桌面 的 核心副文本 为 核心副文本。
    桌面 的 视觉状态 为 视觉状态。
    桌面 的 视觉副文本 为 视觉副文本。
    桌面 的 声音状态 为 声音状态。
    桌面 的 声音副文本 为 声音副文本。
    桌面 的 形象状态 为 形象状态。
    桌面 的 形象副文本 为 形象副文本。
完。

Nova原生桌面_刷新前台模块状态 接受 零参：
    若 非 桌面_已初始化：
        返还。
    完。
    可变 已打开语音 为 假。
    可变 已打开形象 为 假。
    可变 焦点URL 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 桌面 的 窗口列表：
        url 为 桌面 的 窗口列表 的 第 i 项 的 URL。
        若 做 字符串查找 用 url、「nova://voice」 等于 0：
            已打开语音 为 真。
        完。
        若 做 字符串查找 用 url、「nova://avatar」 等于 0：
            已打开形象 为 真。
        完。
        若 桌面 的 窗口列表 的 第 i 项 的 ID 等于 桌面 的 焦点窗口：
            焦点URL 为 url。
        完。
        i 为 i 加 1。
    完。
    若 桌面 的 声音状态 等于 空 或者 做 字符串长度 用 桌面 的 声音状态 等于 0：
        桌面 的 声音状态 为 「待机」。
    完。
    若 桌面 的 声音副文本 等于 空 或者 做 字符串长度 用 桌面 的 声音副文本 等于 0：
        桌面 的 声音副文本 为 「语音前台未激活」。
    完。
    若 已打开语音：
        若 桌面 的 声音状态 等于 「待机」：
            桌面 的 声音状态 为 「在线」。
        完。
        若 桌面 的 声音副文本 等于 「语音前台未激活」：
            桌面 的 声音副文本 为 「语音中枢窗口已挂载」。
        完。
    完。
    若 做 字符串查找 用 焦点URL、「nova://voice」 等于 0：
        若 桌面 的 声音状态 等于 「待机」 或者 桌面 的 声音状态 等于 「在线」 或者 桌面 的 声音状态 等于 「前台」：
            桌面 的 声音状态 为 「前台」。
        完。
        若 桌面 的 声音副文本 等于 「语音前台未激活」 或者 桌面 的 声音副文本 等于 「语音中枢窗口已挂载」：
            桌面 的 声音副文本 为 「语音中枢正在接收桌面入口」。
        完。
    完。
    若 桌面 的 形象状态 等于 空 或者 做 字符串长度 用 桌面 的 形象状态 等于 0：
        桌面 的 形象状态 为 「待机」。
    完。
    若 桌面 的 形象副文本 等于 空 或者 做 字符串长度 用 桌面 的 形象副文本 等于 0：
        桌面 的 形象副文本 为 「形象前台未激活」。
    完。
    若 已打开形象：
        若 桌面 的 形象状态 等于 「待机」：
            桌面 的 形象状态 为 「在线」。
        完。
        若 桌面 的 形象副文本 等于 「形象前台未激活」：
            桌面 的 形象副文本 为 「全息形象窗口已挂载」。
        完。
    完。
    若 做 字符串查找 用 焦点URL、「nova://avatar」 等于 0：
        若 桌面 的 形象状态 等于 「待机」 或者 桌面 的 形象状态 等于 「在线」 或者 桌面 的 形象状态 等于 「前台」：
            桌面 的 形象状态 为 「前台」。
        完。
        若 桌面 的 形象副文本 等于 「形象前台未激活」 或者 桌面 的 形象副文本 等于 「全息形象窗口已挂载」：
            桌面 的 形象副文本 为 「全息形象正在投影当前人格状态」。
        完。
    完。
完。

Nova原生桌面_打开或聚焦窗口 接受 标题、url、x、y、宽、高：
    现有窗口ID 为 做 Nova原生桌面_查找窗口ID_按URL 用 url。
    若 现有窗口ID 大于 0：
        做 Nova原生桌面_聚焦窗口 用 现有窗口ID。
        返还 现有窗口ID。
    完。
    返还 做 Nova原生桌面_打开窗口 用 标题、url、x、y、宽、高。
完。

Nova原生桌面_打开窗口 接受 标题、url、x、y、宽、高：
    窗口 为 新建 Nova原生桌面窗口。
    窗口 的 ID 为 桌面 的 下一个窗口ID。
    桌面 的 下一个窗口ID 为 桌面 的 下一个窗口ID 加 1。
    窗口 的 标题 为 标题。
    窗口 的 URL 为 url。
    窗口 的 x 为 x。
    窗口 的 y 为 y。
    窗口 的 宽 为 宽。
    窗口 的 高 为 高。
    窗口 的 模式 为 窗口模式_普通。
    窗口 的 Z序 为 桌面 的 下一个窗口ID。
    窗口 的 可见 为 真。
    窗口 的 活跃 为 真。
    窗口 的 图处层ID 为 做 浏览器图处_创建层 用 x、y 加 桌面 的 系统栏高、宽、高。
    窗口 的 DOM文档 为 做 Nova原生桌面_构建窗口文档 用 窗口。
    做 追加 用 桌面 的 窗口列表、窗口。
    桌面 的 焦点窗口 为 窗口 的 ID。
    桌面 的 壳最后动作 为 「open-window」。
    桌面 的 壳最后结果 为 「窗口已挂载: 」 加 标题。
    做 Nova原生桌面_刷新前台模块状态。
    做 Nova原生桌面_同步文档状态。
    做 事件循环_导航 用 url。
    做 内核信息 用 「Nova原生桌面: 打开窗口 [」 加 标题 加 「] → 」 加 url。
    返还 窗口 的 ID。
完。

Nova原生桌面_关闭窗口 接受 窗口ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 桌面 的 窗口列表：
        若 桌面 的 窗口列表 的 第 i 项 的 ID 等于 窗口ID：
            标题 为 桌面 的 窗口列表 的 第 i 项 的 标题。
            做 浏览器图处_销毁层 用 桌面 的 窗口列表 的 第 i 项 的 图处层ID。
            做 列表_移除 用 桌面 的 窗口列表、i。
            若 做 计数 用 桌面 的 窗口列表 大于 0：
                桌面 的 焦点窗口 为 桌面 的 窗口列表 的 第 做 计数 用 桌面 的 窗口列表 减 1 项 的 ID。
            否则：
                桌面 的 焦点窗口 为 0。
            完。
            桌面 的 壳最后动作 为 「close-window」。
            桌面 的 壳最后结果 为 「窗口已关闭: 」 加 标题。
            做 Nova原生桌面_刷新前台模块状态。
            做 Nova原生桌面_同步文档状态。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

Nova原生桌面_聚焦窗口 接受 窗口ID：
    桌面 的 焦点窗口 为 窗口ID。
    可变 i 为 0。
    只要 i 小于 做 计数 用 桌面 的 窗口列表：
        若 桌面 的 窗口列表 的 第 i 项 的 ID 等于 窗口ID：
            桌面 的 窗口列表 的 第 i 项 的 Z序 为 桌面 的 下一个窗口ID 加 100。
            桌面 的 壳最后动作 为 「focus-window」。
            桌面 的 壳最后结果 为 「焦点切换到: 」 加 桌面 的 窗口列表 的 第 i 项 的 标题。
        完。
        i 为 i 加 1。
    完。
    做 Nova原生桌面_刷新前台模块状态。
    做 Nova原生桌面_同步文档状态。
完。

Nova原生桌面_最大化窗口 接受 窗口ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 桌面 的 窗口列表：
        若 桌面 的 窗口列表 的 第 i 项 的 ID 等于 窗口ID：
            桌面 的 窗口列表 的 第 i 项 的 模式 为 窗口模式_最大化。
            桌面 的 窗口列表 的 第 i 项 的 x 为 0。
            桌面 的 窗口列表 的 第 i 项 的 y 为 0。
            桌面 的 窗口列表 的 第 i 项 的 宽 为 桌面 的 屏幕宽。
            桌面 的 窗口列表 的 第 i 项 的 高 为 做 Nova原生桌面_工作区高度。
            桌面 的 窗口列表 的 第 i 项 的 DOM文档 为 做 Nova原生桌面_构建窗口文档 用 桌面 的 窗口列表 的 第 i 项。
            做 Nova原生桌面_同步文档状态。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

Nova原生桌面_渲染帧 接受 零参：
    若 非 桌面_已初始化：
        返还。
    完。
    做 Nova原生桌面_同步文档状态。
    若 桌面 的 桌面DOM 不等于 空：
        做 渲染_构建树 用 桌面 的 桌面DOM。
        做 渲染_布局。
        做 渲染_绘制 用 桌面 的 桌面DOM。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 桌面 的 窗口列表：
        窗口 为 桌面 的 窗口列表 的 第 i 项。
        若 窗口 的 可见 并且 窗口 的 DOM文档 不等于 空：
        完。
        i 为 i 加 1。
    完。
    做 浏览器图处_合成帧。
完。

可变 AI_窗口管理每_热节点缓存 为 0。

AI_窗口管理每_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_窗口管理每_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_窗口管理每_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

本章 收束。
