《内建测试》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·辅助存根》。

引用《.·文本处理环》。

引用《..·标准·标准流》。

引用《..·标准·原生字符串》。

引用《..·记忆·路径查找》。

引用《.·设置重定向》。

引用《..·中枢·内核打印》。

引用《.·是内建命令》。

引用《..·中枢·虚拟内存区》。

引用《..·中枢·进程fork创建》。

引用《.·hexdump十六转储》。

最大别名数 为 128。

别名条目 是 一种 事物，含 名称、值、有效。

可变 别名表 为 做 数组 用 别名条目、128。

可变 别名数量 为 0。

最大陷阱数 为 32。

信号陷阱 是 一种 事物，含 信号号、命令、有效。

可变 陷阱表 为 做 数组 用 信号陷阱、32。

最大哈希数 为 64。

路径缓存条目 是 一种 事物，含 命令名、完整路径、命中次数、有效。

可变 路径缓存 为 做 数组 用 路径缓存条目、64。

可变 路径缓存数量 为 0。

内建_test 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        返还 1。
    完。
    若 做 计数 用 参数 等于 3：
        返还 做 test_一元 用 参数 的 第 1 项、参数 的 第 2 项。
    完。
    若 做 计数 用 参数 等于 4：
        返还 做 test_二元 用 参数 的 第 1 项、参数 的 第 2 项、参数 的 第 3 项。
    完。
    若 做 计数 用 参数 大于或等于 3 并且 参数 的 第 1 项 等于 「!」：
        子参 为 做 数组_切片 用 参数、1、做 计数 用 参数。
        子参 的 第 0 项 为 「test」。
        结果 为 做 内建_test 用 子参。
        返还 做 如果选字符 用 结果 不等于 0、0、1。
    完。
    若 做 计数 用 参数 等于 2：
        返还 做 如果选字符 用 参数 的 第 1 项 不等于 「」、0、1。
    完。
    返还 1。
完。

test_一元 接受 操作符、操作数：
    若 操作符 等于 「-f」：
        返还 做 如果选字符 用 做 路径_存在 用 操作数 并且 非 做 路径_是目录 用 操作数、0、1。
    完。
    若 操作符 等于 「-d」：
        返还 做 如果选字符 用 做 路径_是目录 用 操作数、0、1。
    完。
    若 操作符 等于 「-e」：
        返还 做 如果选字符 用 做 路径_存在 用 操作数、0、1。
    完。
    若 操作符 等于 「-r」：
        返还 做 如果选字符 用 做 路径_可读 用 操作数、0、1。
    完。
    若 操作符 等于 「-w」：
        返还 做 如果选字符 用 做 路径_可写 用 操作数、0、1。
    完。
    若 操作符 等于 「-x」：
        返还 做 如果选字符 用 做 路径_可执行 用 操作数、0、1。
    完。
    若 操作符 等于 「-s」：
        返还 做 如果选字符 用 做 路径_大小 用 操作数 大于 0、0、1。
    完。
    若 操作符 等于 「-L」：
        返还 做 如果选字符 用 做 路径_是符号链接 用 操作数、0、1。
    完。
    若 操作符 等于 「-p」：
        返还 做 如果选字符 用 做 路径_是管道 用 操作数、0、1。
    完。
    若 操作符 等于 「-S」：
        返还 做 如果选字符 用 做 路径_是套接字 用 操作数、0、1。
    完。
    若 操作符 等于 「-b」：
        返还 做 如果选字符 用 做 路径_是块设备 用 操作数、0、1。
    完。
    若 操作符 等于 「-c」：
        返还 做 如果选字符 用 做 路径_是字符设备 用 操作数、0、1。
    完。
    若 操作符 等于 「-z」：
        返还 做 如果选字符 用 操作数 等于 「」、0、1。
    完。
    若 操作符 等于 「-n」：
        返还 做 如果选字符 用 操作数 不等于 「」、0、1。
    完。
    若 操作符 等于 「!」：
        返还 做 如果选字符 用 操作数 等于 「」、0、1。
    完。
    若 操作符 等于 「-t」：
        返还 做 如果选字符 用 做 FD_是终端 用 做 字符串转整数 用 操作数、0、1。
    完。
    返还 2。
完。

test_二元 接受 左、操作符、右：
    若 操作符 等于 「=」 或者 操作符 等于 「==」：
        返还 做 如果选字符 用 左 等于 右、0、1。
    完。
    若 操作符 等于 「!=」：
        返还 做 如果选字符 用 左 不等于 右、0、1。
    完。
    若 操作符 等于 「-eq」：
        返还 做 如果选字符 用 做 字符串转整数 用 左 等于 做 字符串转整数 用 右、0、1。
    完。
    若 操作符 等于 「-ne」：
        返还 做 如果选字符 用 做 字符串转整数 用 左 不等于 做 字符串转整数 用 右、0、1。
    完。
    若 操作符 等于 「-lt」：
        返还 做 如果选字符 用 做 字符串转整数 用 左 小于 做 字符串转整数 用 右、0、1。
    完。
    若 操作符 等于 「-gt」：
        返还 做 如果选字符 用 做 字符串转整数 用 左 大于 做 字符串转整数 用 右、0、1。
    完。
    若 操作符 等于 「-le」：
        返还 做 如果选字符 用 做 字符串转整数 用 左 小于或等于 做 字符串转整数 用 右、0、1。
    完。
    若 操作符 等于 「-ge」：
        返还 做 如果选字符 用 做 字符串转整数 用 左 大于或等于 做 字符串转整数 用 右、0、1。
    完。
    若 操作符 等于 「-nt」：
        返还 做 如果选字符 用 做 路径_修改时间 用 左 大于 做 路径_修改时间 用 右、0、1。
    完。
    若 操作符 等于 「-ot」：
        返还 做 如果选字符 用 做 路径_修改时间 用 左 小于 做 路径_修改时间 用 右、0、1。
    完。
    若 操作符 等于 「-ef」：
        返还 做 如果选字符 用 做 路径_同一文件 用 左、右、0、1。
    完。
    返还 2。
完。

内建_read 接受 参数：
    可变 提示符 为 「」。
    可变 超时秒 为 0。
    可变 静音 为 假。
    可变 读取N字符 为 0。
    可变 分隔符 为 「\n」。
    可变 变量名开始 为 1。
    可变 i 为 1。
    只要 i 小于 做 计数 用 参数：
        若 参数 的 第 i 项 等于 「-p」 并且 i 加 1 小于 做 计数 用 参数：
            提示符 为 参数 的 第 i 加 1 项。
            i 为 i 加 2。
            变量名开始 为 i。
        完。
        做 否则如果 用 参数 的 第 i 项 等于 「-t」 并且 i 加 1 小于 做 计数 用 参数。
        块：
            超时秒 为 做 字符串转整数 用 参数 的 第 i 加 1 项。
            i 为 i 加 2。
            变量名开始 为 i。
        完。
        做 否则如果 用 参数 的 第 i 项 等于 「-s」。
        块：
            静音 为 真。
            i 为 i 加 1。
            变量名开始 为 i。
        完。
        做 否则如果 用 参数 的 第 i 项 等于 「-n」 并且 i 加 1 小于 做 计数 用 参数。
        块：
            读取N字符 为 做 字符串转整数 用 参数 的 第 i 加 1 项。
            i 为 i 加 2。
            变量名开始 为 i。
        完。
        做 否则如果 用 参数 的 第 i 项 等于 「-d」 并且 i 加 1 小于 做 计数 用 参数。
        块：
            分隔符 为 参数 的 第 i 加 1 项。
            i 为 i 加 2。
            变量名开始 为 i。
        完。
        否则。
        块：
            跳出。
        完。
    完。
    若 变量名开始 大于或等于 做 计数 用 参数：
        做 打印行 用 「用法: read [-p 提示] [-t 秒] [-s] [-n 数] 变量名...」。
        返还 1。
    完。
    若 提示符 不等于 「」：
        做 打印 用 提示符。
    完。
    若 静音：
        做 终端_设置回显 用 假。
    完。
    可变 行 为 「」。
    若 超时秒 大于 0：
        行 为 做 读取行_超时 用 超时秒 乘 1000。
        若 行 等于 空：
            若 静音：
                做 终端_设置回显 用 真。
            完。
            返还 1。
        完。
    完。
    做 否则如果 用 读取N字符 大于 0。
    块：
        行 为 做 读取N字节 用 读取N字符。
    完。
    否则。
    块：
        行 为 做 读取行。
    完。
    若 静音：
        做 终端_设置回显 用 真。
    完。
    若 行 等于 空：
        返还 1。
    完。
    词表 为 做 字符串_分割 用 做 字符串去首尾空白 用 行、分隔符。
    变量数 为 做 计数 用 参数 减 变量名开始。
    i 为 0。
    只要 i 小于 变量数：
        变名 为 参数 的 第 变量名开始 加 i 项。
        若 i 小于 做 计数 用 词表 减 1：
            做 环境_设置 用 变名、词表 的 第 i 项。
        完。
        做 否则如果 用 i 等于 变量数 减 1。
        块：
            可变 剩余 为 「」。
            可变 j 为 i。
            只要 j 小于 做 计数 用 词表：
                若 剩余 不等于 「」：
                    剩余 为 剩余 加 「 」。
                完。
                剩余 为 剩余 加 词表 的 第 j 项。
                j 为 j 加 1。
            完。
            做 环境_设置 用 变名、剩余。
        完。
        否则。
        块：
            做 环境_设置 用 变名、「」。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

内建_source 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        做 打印行 用 「用法: source 文件」。
        返还 1。
    完。
    若 做 路径_存在 用 参数 的 第 1 项 等于 假：
        做 打印行 用 「source: 文件不存在: 」 加 参数 的 第 1 项。
        返还 1。
    完。
    返还 做 龙壳_执行脚本 用 参数 的 第 1 项。
完。

内建_eval 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        返还 0。
    完。
    可变 命令 为 「」。
    可变 i 为 1。
    只要 i 小于 做 计数 用 参数：
        若 命令 不等于 「」：
            命令 为 命令 加 「 」。
        完。
        命令 为 命令 加 参数 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 龙壳_执行命令 用 命令。
完。

内建_exec 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        返还 0。
    完。
    命令 为 做 数组_切片 用 参数、1、做 计数 用 参数。
    结果 为 做 进程_exec 用 命令。
    若 结果 不等于 0：
        做 打印行 用 「exec: 」 加 参数 的 第 1 项 加 「: 无法执行」。
    完。
    返还 结果。
完。

可变 当前umask 为 18。

内建_umask 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        做 打印行 用 做 八进制转字符串 用 当前umask、4。
        返还 0。
    完。
    若 参数 的 第 1 项 等于 「-S」：
        可变 结果 为 「u=」。
        若 当前umask 位与 256 等于 0：
            结果 为 结果 加 「r」。
        完。
        若 当前umask 位与 128 等于 0：
            结果 为 结果 加 「w」。
        完。
        若 当前umask 位与 64 等于 0：
            结果 为 结果 加 「x」。
        完。
        结果 为 结果 加 「,g=」。
        若 当前umask 位与 32 等于 0：
            结果 为 结果 加 「r」。
        完。
        若 当前umask 位与 16 等于 0：
            结果 为 结果 加 「w」。
        完。
        若 当前umask 位与 8 等于 0：
            结果 为 结果 加 「x」。
        完。
        结果 为 结果 加 「,o=」。
        若 当前umask 位与 4 等于 0：
            结果 为 结果 加 「r」。
        完。
        若 当前umask 位与 2 等于 0：
            结果 为 结果 加 「w」。
        完。
        若 当前umask 位与 1 等于 0：
            结果 为 结果 加 「x」。
        完。
        做 打印行 用 结果。
        返还 0。
    完。
    新值 为 做 八进制转整数 用 参数 的 第 1 项。
    若 新值 小于 0 或者 新值 大于 511：
        做 打印行 用 「umask: 无效的掩码: 」 加 参数 的 第 1 项。
        返还 1。
    完。
    当前umask 为 新值。
    返还 0。
完。

内建_set 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        做 环境_显示全部。
        返还 0。
    完。
    若 参数 的 第 1 项 等于 「-e」：
        做 龙壳_设置标志 用 「错误即退」、真。
        返还 0。
    完。
    若 参数 的 第 1 项 等于 「+e」：
        做 龙壳_设置标志 用 「错误即退」、假。
        返还 0。
    完。
    若 参数 的 第 1 项 等于 「-x」：
        做 龙壳_设置标志 用 「执行跟踪」、真。
        返还 0。
    完。
    若 参数 的 第 1 项 等于 「+x」：
        做 龙壳_设置标志 用 「执行跟踪」、假。
        返还 0。
    完。
    若 参数 的 第 1 项 等于 「-u」：
        做 龙壳_设置标志 用 「未定义报错」、真。
        返还 0。
    完。
    若 参数 的 第 1 项 等于 「+u」：
        做 龙壳_设置标志 用 「未定义报错」、假。
        返还 0。
    完。
    若 参数 的 第 1 项 等于 「--」：
        做 龙壳_设置位置参数 用 做 数组_切片 用 参数、2、做 计数 用 参数。
        返还 0。
    完。
    做 打印行 用 「set: 未知选项: 」 加 参数 的 第 1 项。
    返还 1。
完。

内建_shift 接受 参数：
    n 为 1。
    若 做 计数 用 参数 大于或等于 2：
        n 为 做 字符串转整数 用 参数 的 第 1 项。
    完。
    返还 做 龙壳_移位参数 用 n。
完。

内建_trap 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        可变 i 为 0。
        只要 i 小于 最大陷阱数：
            若 陷阱表 的 第 i 项 的 有效：
                做 打印行 用 「trap -- '」 加 陷阱表 的 第 i 项 的 命令 加 「' 」 加 做 信号名称 用 陷阱表 的 第 i 项 的 信号号。
            完。
            i 为 i 加 1。
        完。
        返还 0。
    完。
    若 做 计数 用 参数 小于 3：
        做 打印行 用 「用法: trap [命令] 信号...」。
        返还 1。
    完。
    命令 为 参数 的 第 1 项。
    可变 i 为 2。
    只要 i 小于 做 计数 用 参数：
        信号 为 做 信号名_转编号 用 参数 的 第 i 项。
        若 信号 小于 0：
            做 打印行 用 「trap: 未知信号: 」 加 参数 的 第 i 项。
            i 为 i 加 1。
            继续。
        完。
        若 命令 等于 「-」：
            做 陷阱_移除 用 信号。
        否则：
            做 陷阱_设置 用 信号、命令。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

陷阱_设置 接受 信号、命令：
    可变 i 为 0。
    只要 i 小于 最大陷阱数：
        若 陷阱表 的 第 i 项 的 有效 并且 陷阱表 的 第 i 项 的 信号号 等于 信号：
            陷阱表 的 第 i 项 的 命令 为 命令。
            返还。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 最大陷阱数：
        若 陷阱表 的 第 i 项 的 有效 等于 假：
            陷阱表 的 第 i 项 的 信号号 为 信号。
            陷阱表 的 第 i 项 的 命令 为 命令。
            陷阱表 的 第 i 项 的 有效 为 真。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

陷阱_移除 接受 信号：
    可变 i 为 0。
    只要 i 小于 最大陷阱数：
        若 陷阱表 的 第 i 项 的 有效 并且 陷阱表 的 第 i 项 的 信号号 等于 信号：
            陷阱表 的 第 i 项 的 有效 为 假。
        完。
        i 为 i 加 1。
    完。
完。

内建_type 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        做 打印行 用 「用法: type 命令名...」。
        返还 1。
    完。
    可变 结果 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 参数：
        名 为 参数 的 第 i 项。
        别名值 为 做 别名_查找 用 名。
        若 别名值 不等于 空：
            做 打印行 用 名 加 「 是 '」 加 别名值 加 「' 的别名」。
            i 为 i 加 1。
            继续。
        完。
        若 做 是内建命令 用 名：
            做 打印行 用 名 加 「 是内建命令」。
            i 为 i 加 1。
            继续。
        完。
        路径 为 做 PATH_查找命令 用 名。
        若 路径 不等于 空：
            做 打印行 用 名 加 「 是 」 加 路径。
        否则：
            做 打印行 用 名 加 「: 未找到」。
            结果 为 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

内建_hash 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        若 路径缓存数量 等于 0：
            做 打印行 用 「hash: 缓存为空」。
            返还 0。
        完。
        做 打印行 用 「命中\t命令」。
        可变 i 为 0。
        只要 i 小于 最大哈希数：
            若 路径缓存 的 第 i 项 的 有效：
                做 打印行 用 做 整数转字符串 用 路径缓存 的 第 i 项 的 命中次数 加 「\t」 加 路径缓存 的 第 i 项 的 完整路径。
            完。
            i 为 i 加 1。
        完。
        返还 0。
    完。
    若 参数 的 第 1 项 等于 「-r」：
        可变 i 为 0。
        只要 i 小于 最大哈希数：
            路径缓存 的 第 i 项 的 有效 为 假。
            i 为 i 加 1。
        完。
        路径缓存数量 为 0。
        返还 0。
    完。
    返还 0。
完。

内建_alias 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        可变 i 为 0。
        只要 i 小于 最大别名数：
            若 别名表 的 第 i 项 的 有效：
                做 打印行 用 「alias 」 加 别名表 的 第 i 项 的 名称 加 「='」 加 别名表 的 第 i 项 的 值 加 「'」。
            完。
            i 为 i 加 1。
        完。
        返还 0。
    完。
    可变 i 为 1。
    只要 i 小于 做 计数 用 参数：
        等号位 为 做 字符串_查找 用 参数 的 第 i 项、「=」。
        若 等号位 大于或等于 0：
            名 为 做 字符串_子串 用 参数 的 第 i 项、0、等号位。
            值 为 做 字符串_子串 用 参数 的 第 i 项、等号位 加 1、做 字符串长度 用 参数 的 第 i 项。
            做 别名_设置 用 名、值。
        否则：
            值 为 做 别名_查找 用 参数 的 第 i 项。
            若 值 不等于 空：
                做 打印行 用 「alias 」 加 参数 的 第 i 项 加 「='」 加 值 加 「'」。
            否则：
                做 打印行 用 「alias: 」 加 参数 的 第 i 项 加 「: 未找到」。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

内建_unalias 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        做 打印行 用 「用法: unalias 名称...」。
        返还 1。
    完。
    若 参数 的 第 1 项 等于 「-a」：
        可变 i 为 0。
        只要 i 小于 最大别名数：
            别名表 的 第 i 项 的 有效 为 假。
            i 为 i 加 1。
        完。
        别名数量 为 0。
        返还 0。
    完。
    可变 i 为 1。
    只要 i 小于 做 计数 用 参数：
        做 别名_移除 用 参数 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 0。
完。

别名_设置 接受 名、值：
    可变 i 为 0。
    只要 i 小于 最大别名数：
        若 别名表 的 第 i 项 的 有效 并且 别名表 的 第 i 项 的 名称 等于 名：
            别名表 的 第 i 项 的 值 为 值。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 别名数量 大于或等于 最大别名数：
        返还。
    完。
    i 为 0。
    只要 i 小于 最大别名数：
        若 别名表 的 第 i 项 的 有效 等于 假：
            别名表 的 第 i 项 的 名称 为 名。
            别名表 的 第 i 项 的 值 为 值。
            别名表 的 第 i 项 的 有效 为 真。
            别名数量 为 别名数量 加 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

别名_查找 接受 名：
    可变 i 为 0。
    只要 i 小于 最大别名数：
        若 别名表 的 第 i 项 的 有效 并且 别名表 的 第 i 项 的 名称 等于 名：
            返还 别名表 的 第 i 项 的 值。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

别名_移除 接受 名：
    可变 i 为 0。
    只要 i 小于 最大别名数：
        若 别名表 的 第 i 项 的 有效 并且 别名表 的 第 i 项 的 名称 等于 名：
            别名表 的 第 i 项 的 有效 为 假。
            别名数量 为 别名数量 减 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

内建_wait 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        返还 做 作业_等待全部。
    完。
    可变 i 为 1。
    可变 最后状态 为 0。
    只要 i 小于 做 计数 用 参数：
        pid 为 做 字符串转整数 用 参数 的 第 i 项。
        最后状态 为 做 作业_等待 用 pid。
        i 为 i 加 1。
    完。
    返还 最后状态。
完。

内建_jobs 接受 参数：
    可变 长格式 为 假。
    若 做 计数 用 参数 大于或等于 2 并且 参数 的 第 1 项 等于 「-l」：
        长格式 为 真。
    完。
    作业列表 为 做 作业_获取全部。
    若 做 计数 用 作业列表 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 作业列表：
        j 为 作业列表 的 第 i 项。
        若 长格式：
            做 打印行 用 「[%d] %d %s %s」、j 的 作业号、j 的 pid、j 的 状态、j 的 命令。
        否则：
            做 打印行 用 「[%d] %s %s」、j 的 作业号、j 的 状态、j 的 命令。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

内建_fg 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        返还 做 作业_前台继续 用 负 1。
    完。
    作业号 为 做 字符串转整数 用 参数 的 第 1 项。
    返还 做 作业_前台继续 用 作业号。
完。

内建_bg 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        返还 做 作业_后台继续 用 负 1。
    完。
    作业号 为 做 字符串转整数 用 参数 的 第 1 项。
    返还 做 作业_后台继续 用 作业号。
完。

内建_printf 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        做 打印行 用 「用法: printf 格式串 [参数...]」。
        返还 1。
    完。
    格式 为 参数 的 第 1 项。
    可变 索引 为 2。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 格式：
        ch 为 做 字符串_取字符 用 格式、i。
        若 ch 等于 「\\」：
            i 为 i 加 1。
            若 i 大于或等于 做 字符串长度 用 格式：
                跳出。
            完。
            esc 为 做 字符串_取字符 用 格式、i。
            若 esc 等于 「n」：
                结果 为 结果 加 「\n」。
            完。
            做 否则如果 用 esc 等于 「t」。
            块：
                结果 为 结果 加 「\t」。
            完。
            做 否则如果 用 esc 等于 「\\」。
            块：
                结果 为 结果 加 「\\」。
            完。
            否则。
            块：
                结果 为 结果 加 「\\」 加 做 字符转字符串 用 esc。
            完。
        完。
        做 否则如果 用 ch 等于 「%」。
        块：
            i 为 i 加 1。
            若 i 大于或等于 做 字符串长度 用 格式：
                跳出。
            完。
            fmt 为 做 字符串_取字符 用 格式、i。
            参值 为 「」。
            若 索引 小于 做 计数 用 参数：
                参值 为 参数 的 第 索引 项。
                索引 为 索引 加 1。
            完。
            若 fmt 等于 「s」：
                结果 为 结果 加 参值。
            完。
            做 否则如果 用 fmt 等于 「d」。
            块：
                结果 为 结果 加 做 整数转字符串 用 做 字符串转整数 用 参值。
            完。
            做 否则如果 用 fmt 等于 「x」。
            块：
                结果 为 结果 加 做 十六进制转字符串 用 做 字符串转整数 用 参值。
            完。
            做 否则如果 用 fmt 等于 「o」。
            块：
                结果 为 结果 加 做 八进制转字符串 用 做 字符串转整数 用 参值、0。
            完。
            做 否则如果 用 fmt 等于 「%」。
            块：
                结果 为 结果 加 「%」。
            完。
        完。
        否则。
        块：
            结果 为 结果 加 做 字符转字符串 用 ch。
        完。
        i 为 i 加 1。
    完。
    做 打印 用 结果。
    返还 0。
完。

编辑距离 接受 字符串1、字符串2：
    长1 为 做 字符串长度 用 字符串1。
    长2 为 做 字符串长度 用 字符串2。
    若 长1 等于 0：
        返还 长2。
    完。
    若 长2 等于 0：
        返还 长1。
    完。
    若 字符串1 等于 字符串2：
        返还 0。
    完。
    可变 前一行 为 做 数组 用 u32、64。
    可变 当前行 为 做 数组 用 u32、64。
    若 长2 大于 63：
        返还 99。
    完。
    可变 j 为 0。
    只要 j 小于或等于 长2：
        前一行 的 第 j 项 为 j。
        j 为 j 加 1。
    完。
    可变 i 为 1。
    只要 i 小于或等于 长1：
        当前行 的 第 0 项 为 i。
        j 为 1。
        只要 j 小于或等于 长2：
            代价 为 0。
            若 做 字符串_取字符 用 字符串1、i 减 1 不等于 做 字符串_取字符 用 字符串2、j 减 1：
                代价 为 1。
            完。
            删除 为 前一行 的 第 j 项 加 1。
            插入 为 当前行 的 第 j 减 1 项 加 1。
            替换 为 前一行 的 第 j 减 1 项 加 代价。
            可变 最小 为 删除。
            若 插入 小于 最小：
                最小 为 插入。
            完。
            若 替换 小于 最小：
                最小 为 替换。
            完。
            当前行 的 第 j 项 为 最小。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于或等于 长2：
            前一行 的 第 j 项 为 当前行 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 当前行 的 第 长2 项。
完。

AI_命令纠错 接受 输入命令：
    内建表 为 一组：「test」、「read」、「source」、「eval」、「exec」、「set」、「umask」、「shift」、「trap」、「type」、「hash」、「alias」、「unalias」、「wait」、「jobs」、「fg」、「bg」、「printf」、「cd」、「pwd」、「echo」、「exit」、「export」、「history」、「help」。
    可变 最近命令 为 「」。
    可变 最小距离 为 999。
    可变 i 为 0。
    只要 i 小于 做 计数 用 内建表：
        d 为 做 编辑距离 用 输入命令、内建表 的 第 i 项。
        若 d 小于 最小距离：
            最小距离 为 d。
            最近命令 为 内建表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    外部表 为 做 PATH_获取所有命令。
    i 为 0。
    只要 i 小于 做 计数 用 外部表：
        d 为 做 编辑距离 用 输入命令、外部表 的 第 i 项。
        若 d 小于 最小距离：
            最小距离 为 d。
            最近命令 为 外部表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 最小距离 小于或等于 2 并且 最近命令 不等于 「」：
        做 打印行 用 「命令未找到: 」 加 输入命令 加 「, 您是否想输入: 」 加 最近命令 加 「?」。
    否则：
        做 打印行 用 「命令未找到: 」 加 输入命令。
    完。
完。

本章 收束。
