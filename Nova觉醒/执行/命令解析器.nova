《命令解析器》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·标准·原生字符串》。

解析结果 是 一种 事物，含 参数、重定向。

重定向信息 是 一种 事物，含 输入、输出、错误、追加、合并错误。

解析命令行_完整 接受 行：
    去空白 为 做 字符串去首尾空白 用 行。
    若 去空白 等于 「」：
        返还 空。
    完。
    令牌s 为 做 词法分析 用 去空白。
    若 令牌s 等于 空 或者 做 计数 用 令牌s 等于 0：
        返还 空。
    完。
    可变 参数 为 一组：（空）。
    可变 redir 为 新建 重定向信息。
    可变 i 为 0。
    只要 i 小于 做 计数 用 令牌s：
        t 为 令牌s 的 第 i 项。
        若 t 等于 「>」：
            若 i 加 1 小于 做 计数 用 令牌s：
                redir 的 输出 为 令牌s 的 第 i 加 1 项。
                redir 的 追加 为 假。
                i 为 i 加 2。
                继续。
            完。
        完。
        若 t 等于 「>>」：
            若 i 加 1 小于 做 计数 用 令牌s：
                redir 的 输出 为 令牌s 的 第 i 加 1 项。
                redir 的 追加 为 真。
                i 为 i 加 2。
                继续。
            完。
        完。
        若 t 等于 「<」：
            若 i 加 1 小于 做 计数 用 令牌s：
                redir 的 输入 为 令牌s 的 第 i 加 1 项。
                i 为 i 加 2。
                继续。
            完。
        完。
        若 t 等于 「2>」：
            若 i 加 1 小于 做 计数 用 令牌s：
                redir 的 错误 为 令牌s 的 第 i 加 1 项。
                i 为 i 加 2。
                继续。
            完。
        完。
        若 t 等于 「2>&1」：
            redir 的 合并错误 为 真。
            i 为 i 加 1。
            继续。
        完。
        若 t 等于 「1>&2」 或者 t 等于 「>&2」：
            i 为 i 加 1。
            继续。
        完。
        做 追加 用 参数、t。
        i 为 i 加 1。
    完。
    结果 为 新建 解析结果。
    结果 的 参数 为 参数。
    若 redir 的 输入 不等于 空 或者 redir 的 输出 不等于 空 或者 redir 的 错误 不等于 空 或者 redir 的 合并错误：
        结果 的 重定向 为 redir。
    完。
    返还 结果。
完。

解析命令行 接受 行：
    去空白 为 做 字符串去首尾空白 用 行。
    若 去空白 等于 「」：
        返还 空。
    完。
    返还 做 词法分析 用 去空白。
完。

词法分析 接受 行：
    可变 令牌s 为 一组：（空）。
    可变 当前令牌 为 「」。
    可变 i 为 0。
    长度 为 做 字符串长度 用 行。
    可变 状态 为 0。
    只要 i 小于 长度：
        ch 为 做 字符串取字符 用 行、i。
        若 状态 等于 1：
            若 ch 等于 「'」：
                状态 为 0。
            否则：
                当前令牌 为 当前令牌 加 ch。
            完。
            i 为 i 加 1。
            继续。
        完。
        若 状态 等于 2：
            若 ch 等于 「\"」：
                状态 为 0。
            否则若 ch 等于 「\\」 并且 i 加 1 小于 长度：
                下一个 为 做 字符串取字符 用 行、i 加 1。
                若 下一个 等于 「\"」 或者 下一个 等于 「\\」 或者 下一个 等于 「$」 或者 下一个 等于 「`」：
                    当前令牌 为 当前令牌 加 下一个。
                    i 为 i 加 1。
                否则若 下一个 等于 「n」：
                    当前令牌 为 当前令牌 加 「\n」。
                    i 为 i 加 1。
                否则若 下一个 等于 「t」：
                    当前令牌 为 当前令牌 加 「\t」。
                    i 为 i 加 1。
                否则：
                    当前令牌 为 当前令牌 加 ch。
                完。
            否则：
                当前令牌 为 当前令牌 加 ch。
            完。
            i 为 i 加 1。
            继续。
        完。
        若 ch 等于 「'」：
            状态 为 1。
            i 为 i 加 1。
            继续。
        完。
        若 ch 等于 「\"」：
            状态 为 2。
            i 为 i 加 1。
            继续。
        完。
        若 ch 等于 「\\」 并且 i 加 1 小于 长度：
            当前令牌 为 当前令牌 加 做 字符串取字符 用 行、i 加 1。
            i 为 i 加 2。
            继续。
        完。
        若 ch 等于 「 」 或者 ch 等于 「\t」：
            若 当前令牌 不等于 「」：
                做 追加 用 令牌s、当前令牌。
                当前令牌 为 「」。
            完。
            i 为 i 加 1。
            继续。
        完。
        若 ch 等于 「>」 并且 i 加 1 小于 长度 并且 做 字符串取字符 用 行、i 加 1 等于 「>」：
            若 当前令牌 不等于 「」：
                做 追加 用 令牌s、当前令牌。
                当前令牌 为 「」。
            完。
            做 追加 用 令牌s、「>>」。
            i 为 i 加 2。
            继续。
        完。
        若 ch 等于 「2」 并且 i 加 2 小于 长度 并且 做 字符串取字符 用 行、i 加 1 等于 「>」 并且 做 字符串取字符 用 行、i 加 2 等于 「&」：
            若 i 加 3 小于 长度 并且 做 字符串取字符 用 行、i 加 3 等于 「1」：
                若 当前令牌 不等于 「」：
                    做 追加 用 令牌s、当前令牌。
                    当前令牌 为 「」。
                完。
                做 追加 用 令牌s、「2>&1」。
                i 为 i 加 4。
                继续。
            完。
        完。
        若 ch 等于 「2」 并且 i 加 1 小于 长度 并且 做 字符串取字符 用 行、i 加 1 等于 「>」：
            若 当前令牌 不等于 「」：
                做 追加 用 令牌s、当前令牌。
                当前令牌 为 「」。
            完。
            做 追加 用 令牌s、「2>」。
            i 为 i 加 2。
            继续。
        完。
        若 ch 等于 「>」 或者 ch 等于 「<」：
            若 当前令牌 不等于 「」：
                做 追加 用 令牌s、当前令牌。
                当前令牌 为 「」。
            完。
            做 追加 用 令牌s、ch。
            i 为 i 加 1。
            继续。
        完。
        当前令牌 为 当前令牌 加 ch。
        i 为 i 加 1。
    完。
    若 当前令牌 不等于 「」：
        做 追加 用 令牌s、当前令牌。
    完。
    返还 令牌s。
完。

解析管道 接受 行：
    返还 做 字符串分割 用 行、「 | 」。
完。

解析重定向 接受 参数列表：
    可变 输入文件 为 空。
    可变 输出文件 为 空。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数列表：
        若 参数列表 的 第 i 项 等于 「>」：
            若 i 加 1 小于 做 计数 用 参数列表：
                输出文件 为 参数列表 的 第 i 加 1 项。
            完。
        完。
        若 参数列表 的 第 i 项 等于 「<」：
            若 i 加 1 小于 做 计数 用 参数列表：
                输入文件 为 参数列表 的 第 i 加 1 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 一组：输入文件、输出文件。
完。

可变 AI_命令解析器_热点函数 为 一组：（空）。

AI_命令解析器_JIT候选 接受 函数名、调用次数：
    若 调用次数 大于 100：
        做 追加 用 AI_命令解析器_热点函数、函数名。
        返还 真。
    完。
    返还 假。
完。

AI_命令解析器_内联建议 接受 函数体大小、调用次数：
    若 函数体大小 小于 30 并且 调用次数 大于 50：
        返还 「内联」。
    完。
    返还 「不内联」。
完。

本章 收束。
