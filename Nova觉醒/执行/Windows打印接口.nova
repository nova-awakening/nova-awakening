《Windows打印接口》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·网络系统调用》。

引用《..·引导·端口IO》。

引用《..·记忆·VFS文件读写》。

引用《.·控制台输出》。

引用《..·记忆·VFS读写》。

引用《..·中枢·退出调用》。

引用《..·中枢·块内存分配器》。

引用《..·感知·内核直连层》。

win32_printf 接受 fmt：
    返还 做 vfs_write_fd 用 1、fmt、做 字符串长度 用 fmt。
完。

win32_puts 接受 str：
    做 vfs_write_fd 用 1、str、做 字符串长度 用 str。
    返还 1。
完。

win32_malloc 接受 大小：
    返还 做 kmalloc 用 大小。
完。

win32_free 接受 地址：
    做 kfree 用 地址。
完。

win32_exit 接受 code：
    做 sys_exit 用 code、0、0、0、0、0。
完。

win32_memcpy 接受 dst、src、n：
    做 内存拷贝 用 dst、src、n。
    返还 dst。
完。

win32_memset 接受 dst、val、n：
    做 内存填充 用 dst、val、n。
    返还 dst。
完。

win32_strlen 接受 str：
    返还 做 C字符串长度 用 str。
完。

win32_strcpy 接受 dst、src：
    做 内存拷贝 用 dst、src、做 C字符串长度 用 src 加 1。
    返还 dst。
完。

win32_WSAStartup 接受 version、data：
    主版本 为 做 位与 用 version、0xFF。
    次版本 为 做 位与 用 做 右移 用 version、8、0xFF。
    若 主版本 小于 2：
        返还 0x276B。
    完。
    若 data 不等于 0：
        做 内存写16 用 data、version。
        做 内存写16 用 data 加 2、0x0202。
    完。
    _winsock已初始化 为 真。
    返还 0。
完。

win32_socket 接受 af、type、protocol：
    返还 做 sys_socket 用 af、type、protocol、0、0、0。
完。

win32_connect 接受 s、name、namelen：
    返还 做 sys_connect 用 s、name、namelen、0、0、0。
完。

win32_send 接受 s、buf、len、flags：
    返还 做 sys_sendto 用 s、buf、len、flags、0、0。
完。

win32_recv 接受 s、buf、len、flags：
    返还 做 sys_recvfrom 用 s、buf、len、flags、0、0。
完。

win32_closesocket 接受 s：
    做 vfs_close 用 s。
    返还 0。
完。

win32_stub 接受 零参：
    Win32_接口调用数 为 Win32_接口调用数 加 1。
    返还 0。
完。

Win32_查找接口 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 PE_已加载模块：
        dll 为 PE_已加载模块 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 dll 的 导出表：
            若 dll 的 导出表 的 第 j 项 的 第 0 项 等于 名称：
                Win32_接口调用数 为 Win32_接口调用数 加 1。
                返还 dll 的 导出表 的 第 j 项 的 第 1 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    做 内核调试 用 「Win32: 未找到接口 」 加 名称。
    返还 0。
完。

转换Windows路径 接受 win_path：
    若 win_path 等于 0：
        返还 「/」。
    完。
    返还 「/」。
完。

提取文件名 接受 路径：
    返还 路径。
完。

读取固定字符串 接受 地址、长度：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 长度：
        c 为 做 内存读8 用 地址 加 i。
        若 c 等于 0：
            返还 结果。
        完。
        若 c 大于或等于 0x20 并且 c 小于或等于 0x7E：
            结果 为 结果 加 做 字符 用 c。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

C字符串长度 接受 str：
    若 str 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 65536：
        若 做 内存读8 用 str 加 i 等于 0：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 i。
完。

sys_nanosleep_ms 接受 ms：
    若 ms 小于或等于 0：
        返还。
    完。
    开始 为 做 内联汇编_返回值 用 「rdtsc」。
    目标 为 开始 加 ms 乘 2000000。
    只要 做 内联汇编_返回值 用 「rdtsc」 小于 目标：
        做 内联汇编 用 「pause」。
    完。
完。

释放内存 接受 地址：
    做 kfree 用 地址。
完。

错误_文件不存在 为 负 2。

错误_格式 为 负 8。

Windows兼容_统计 接受 零参：
    返还 一组：Win32_程序数、Win32_接口调用数、做 计数 用 PE_已加载模块。
完。

可变 NW32_API调用计数 为 0。

可变 NW32_路径转换计数 为 0。

可变 NW32_编码转换计数 为 0。

可变 NW32_内存分配计数 为 0。

可变 NW32_文件操作计数 为 0。

可变 NW32_错误计数 为 0。

可变 NW32_AI检测计数 为 0。

可变 Win32_接口调用数 为 0。

可变 Win32_程序数 为 0。

Win32_路径转换 接受 路径：
    NW32_路径转换计数 为 NW32_路径转换计数 加 1。
    若 路径 等于 0 或者 做 字符串长度 用 路径 等于 0：
        返还 「/」。
    完。
    长 为 做 字符串长度 用 路径。
    若 长 大于或等于 2：
        c1 为 做 字节取 用 路径、0。
        c2 为 做 字节取 用 路径、1。
        若 c2 等于 58：
            可变 盘 为 c1。
            若 盘 大于或等于 65 并且 盘 小于或等于 90：
                盘 为 盘 加 32。
            完。
            可变 结果 为 「/mnt/」 加 做 字符 用 盘。
            可变 i 为 2。
            只要 i 小于 长：
                ch 为 做 字节取 用 路径、i。
                若 ch 等于 92：
                    结果 为 结果 加 「/」。
                否则：
                    结果 为 结果 加 做 字符 用 ch。
                完。
                i 为 i 加 1。
            完。
            返还 结果。
        完。
    完。
    可变 结果2 为 「」。
    可变 i 为 0。
    只要 i 小于 长：
        ch 为 做 字节取 用 路径、i。
        若 ch 等于 92：
            结果2 为 结果2 加 「/」。
        否则：
            结果2 为 结果2 加 做 字符 用 ch。
        完。
        i 为 i 加 1。
    完。
    返还 结果2。
完。

WCHAR转UTF8 接受 宽字符串、输出、最大长度：
    NW32_编码转换计数 为 NW32_编码转换计数 加 1。
    若 宽字符串 等于 0 或者 输出 等于 0：
        返还 0。
    完。
    可变 ri 为 0。
    可变 wi 为 0。
    只要 wi 小于 最大长度 减 4：
        lo 为 做 内存读8 用 宽字符串 加 ri。
        hi 为 做 内存读8 用 宽字符串 加 ri 加 1。
        cp 为 lo 位或 hi 左移 8。
        若 cp 等于 0：
            跳出。
        完。
        ri 为 ri 加 2。
        若 cp 小于 0x80：
            做 内存写入 用 输出 加 wi、cp。
            wi 为 wi 加 1。
        否则若 cp 小于 0x800：
            做 内存写入 用 输出 加 wi、0xC0 位或 cp 右移 6。
            wi 为 wi 加 1。
            做 内存写入 用 输出 加 wi、0x80 位或 cp 位与 0x3F。
            wi 为 wi 加 1。
        否则：
            做 内存写入 用 输出 加 wi、0xE0 位或 cp 右移 12。
            wi 为 wi 加 1。
            做 内存写入 用 输出 加 wi、0x80 位或 cp 右移 6 位与 0x3F。
            wi 为 wi 加 1。
            做 内存写入 用 输出 加 wi、0x80 位或 cp 位与 0x3F。
            wi 为 wi 加 1。
        完。
    完。
    做 内存写入 用 输出 加 wi、0。
    返还 wi。
完。

UTF8转WCHAR 接受 utf8、输出、最大字符数：
    NW32_编码转换计数 为 NW32_编码转换计数 加 1。
    若 utf8 等于 0 或者 输出 等于 0：
        返还 0。
    完。
    可变 ri 为 0。
    可变 wi 为 0。
    只要 wi 小于 最大字符数 减 1：
        b 为 做 内存读8 用 utf8 加 ri。
        若 b 等于 0：
            跳出。
        完。
        可变 cp 为 0。
        若 b 小于 0x80：
            cp 为 b。
            ri 为 ri 加 1。
        否则若 b 小于 0xE0：
            cp 为 (b 位与 0x1F) 左移 6 位或 做 内存读8 用 utf8 加 ri 加 1 位与 0x3F。
            ri 为 ri 加 2。
        否则：
            cp 为 (b 位与 0x0F) 左移 12 位或 (做 内存读8 用 utf8 加 ri 加 1 位与 0x3F) 左移 6 位或 做 内存读8 用 utf8 加 ri 加 2 位与 0x3F。
            ri 为 ri 加 3。
        完。
        做 内存写入 用 输出 加 wi 乘 2、cp 位与 0xFF。
        做 内存写入 用 输出 加 wi 乘 2 加 1、cp 右移 8 位与 0xFF。
        wi 为 wi 加 1。
    完。
    做 内存写入 用 输出 加 wi 乘 2、0。
    做 内存写入 用 输出 加 wi 乘 2 加 1、0。
    返还 wi。
完。

win32_CreateFileA 接受 文件名、访问模式、共享模式、安全、创建模式、标志、模板：
    NW32_文件操作计数 为 NW32_文件操作计数 加 1。
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    nova路径 为 做 Win32_路径转换 用 文件名。
    可变 标志位 为 0。
    若 访问模式 位与 0x80000000 不等于 0：
        标志位 为 标志位 位或 0。
    完。
    若 访问模式 位与 0x40000000 不等于 0：
        标志位 为 标志位 位或 1。
    完。
    若 创建模式 等于 1：
        返还 做 vfs_open 用 nova路径、0x41。
    完。
    若 创建模式 等于 2：
        返还 做 vfs_open 用 nova路径、0x42。
    完。
    若 创建模式 等于 3：
        返还 做 vfs_open 用 nova路径、0。
    完。
    若 创建模式 等于 4：
        返还 做 vfs_open 用 nova路径、0x41。
    完。
    返还 做 vfs_open 用 nova路径、标志位。
完。

win32_ReadFile 接受 句柄、缓冲、大小、已读、重叠：
    NW32_文件操作计数 为 NW32_文件操作计数 加 1。
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    n 为 做 vfs_read 用 句柄、缓冲、大小。
    若 已读 不等于 0：
        做 内存_写32 用 已读、n。
    完。
    返还 n 大于或等于 0。
完。

win32_WriteFile 接受 句柄、缓冲、大小、已写、重叠：
    NW32_文件操作计数 为 NW32_文件操作计数 加 1。
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    n 为 做 vfs_write_fd 用 句柄、缓冲、大小。
    若 已写 不等于 0：
        做 内存_写32 用 已写、n。
    完。
    返还 n 大于或等于 0。
完。

win32_CloseHandle 接受 句柄：
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    做 vfs_close 用 句柄。
    返还 1。
完。

win32_GetLastError 接受 零参：
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    返还 0。
完。

win32_GetTickCount 接受 零参：
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    返还 做 内联汇编_返回值 用 「rdtsc」 除以 2000000。
完。

API_哈希表大小 为 256。

API表条目 是 一种 事物，含 名称、函数、下一个。

API_名称哈希 接受 名称：
    可变 h 为 0。
    可变 i 为 0。
    长 为 做 C字符串长度 用 名称。
    只要 i 小于 长：
        h 为 h 乘 31 加 做 内存读8 用 名称 加 i。
        i 为 i 加 1。
    完。
    返还 h 取余于 API_哈希表大小。
完。

API_哈希表_查找 接受 表、名称：
    若 表 等于 0 或者 名称 等于 0：
        返还 0。
    完。
    idx 为 做 API_名称哈希 用 名称。
    可变 条目 为 做 内存_读64 用 表 加 idx 乘 8。
    只要 条目 不等于 0：
        若 做 字符串_等于 用 条目 的 名称、名称：
            返还 条目 的 函数。
        完。
        条目 为 条目 的 下一个。
    完。
    返还 0。
完。

API_哈希表_插入 接受 表、名称、函数：
    若 表 等于 0：
        返还 假。
    完。
    idx 为 做 API_名称哈希 用 名称。
    新 为 做 堆_分配 用 API表条目。
    新 的 名称 为 名称。
    新 的 函数 为 以 零参 为 引，其做法是：
    新 的 下一个 为 做 内存_读64 用 表 加 idx 乘 8。
    做 内存_写64 用 表 加 idx 乘 8、新。
    返还 真。
完。

NW32_AI初始化 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    t 为 做 Tensor_创建 用 7。
    做 Tensor_写 用 t、0、NW32_API调用计数。
    做 Tensor_写 用 t、1、NW32_路径转换计数。
    做 Tensor_写 用 t、2、NW32_编码转换计数。
    做 Tensor_写 用 t、3、NW32_内存分配计数。
    做 Tensor_写 用 t、4、NW32_文件操作计数。
    做 Tensor_写 用 t、5、NW32_错误计数。
    做 Tensor_写 用 t、6、NW32_AI检测计数。
    做 AI总线_推理 用 bus、「NW32_模块注册」、t。
    做 Tensor_释放 用 t。
完。

NW32_AI兼容性检测 接受 程序名：
    NW32_AI检测计数 为 NW32_AI检测计数 加 1。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    t 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 t、0、NW32_API调用计数。
    做 Tensor_写 用 t、1、NW32_文件操作计数。
    做 Tensor_写 用 t、2、NW32_错误计数。
    做 Tensor_写 用 t、3、Win32_程序数。
    兼容 为 做 AI总线_推理 用 bus、「NW32_兼容检测」、t。
    做 Tensor_释放 用 t。
    返还 兼容。
完。

NW32_获取统计 接受 零参：
    s 为 做 堆_分配_数组 用 56。
    做 内存_写64 用 s 加 0、NW32_API调用计数。
    做 内存_写64 用 s 加 8、NW32_路径转换计数。
    做 内存_写64 用 s 加 16、NW32_编码转换计数。
    做 内存_写64 用 s 加 24、NW32_内存分配计数。
    做 内存_写64 用 s 加 32、NW32_文件操作计数。
    做 内存_写64 用 s 加 40、NW32_错误计数。
    做 内存_写64 用 s 加 48、NW32_AI检测计数。
    返还 s。
完。

NW32_诊断打印 接受 零参：
    做 内核打印 用 「[NW32] API调用=」、NW32_API调用计数、「 路径转换=」、NW32_路径转换计数。
    做 内核打印 用 「[NW32] 编码转换=」、NW32_编码转换计数、「 文件操作=」、NW32_文件操作计数。
    做 内核打印 用 「[NW32] 内存分配=」、NW32_内存分配计数、「 错误=」、NW32_错误计数。
    做 内核打印 用 「[NW32] AI检测=」、NW32_AI检测计数。
完。

NW32_初始化 接受 零参：
    做 内核打印 用 「[NW32] Nova Win32兼容层 v1.0 启动」。
    做 NW32_AI初始化。
    做 内核打印 用 「[NW32] AI智能兼容已加载」。
    返还 真。
完。

Win32环境变量 是 一种 事物，含 名称、值、下一个。

可变 Win32_环境变量头 为 0。

win32_GetEnvironmentVariableA 接受 名称、缓冲、大小：
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    可变 env 为 Win32_环境变量头。
    只要 env 不等于 0：
        若 做 字符串_等于 用 env 的 名称、名称：
            长 为 做 字符串长度 用 env 的 值。
            若 缓冲 不等于 0 并且 大小 大于 长：
                做 内存拷贝 用 缓冲、env 的 值、长 加 1。
            完。
            返还 长。
        完。
        env 为 env 的 下一个。
    完。
    若 做 字符串_等于 用 名称、「SystemRoot」：
        v 为 「C:\\Windows」。
        若 缓冲 不等于 0 并且 大小 大于 10：
            做 内存拷贝 用 缓冲、v、11。
        完。
        返还 10。
    完。
    若 做 字符串_等于 用 名称、「TEMP」：
        v 为 「C:\\Temp」。
        若 缓冲 不等于 0 并且 大小 大于 7：
            做 内存拷贝 用 缓冲、v、8。
        完。
        返还 7。
    完。
    返还 0。
完。

win32_SetEnvironmentVariableA 接受 名称、值：
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    可变 env 为 Win32_环境变量头。
    只要 env 不等于 0：
        若 做 字符串_等于 用 env 的 名称、名称：
            env 的 值 为 值。
            返还 1。
        完。
        env 为 env 的 下一个。
    完。
    新 为 做 堆_分配 用 Win32环境变量。
    新 的 名称 为 名称。
    新 的 值 为 值。
    新 的 下一个 为 Win32_环境变量头。
    Win32_环境变量头 为 新。
    返还 1。
完。

HKEY_LOCAL_MACHINE 为 0x80000002。

HKEY_CURRENT_USER 为 0x80000001。

ERROR_SUCCESS 为 0。

ERROR_FILE_NOT_FOUND 为 2。

注册表条目 是 一种 事物，含 键路径、值名、数据、类型、下一个。

可变 注册表头 为 0。

win32_RegOpenKeyExA 接受 根键、子键、保留、权限、输出句柄：
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    若 输出句柄 不等于 0：
        做 内存_写32 用 输出句柄、根键 加 1。
    完。
    返还 ERROR_SUCCESS。
完。

win32_RegQueryValueExA 接受 句柄、值名、保留、类型、数据、大小：
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    可变 entry 为 注册表头。
    只要 entry 不等于 0：
        若 做 字符串_等于 用 entry 的 值名、值名：
            若 类型 不等于 0：
                做 内存_写32 用 类型、entry 的 类型。
            完。
            返还 ERROR_SUCCESS。
        完。
        entry 为 entry 的 下一个。
    完。
    返还 ERROR_FILE_NOT_FOUND。
完。

win32_RegCloseKey 接受 句柄：
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    返还 ERROR_SUCCESS。
完。

win32_GetCurrentProcessId 接受 零参：
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    返还 1000。
完。

win32_GetCurrentThreadId 接受 零参：
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    返还 1001。
完。

win32_Sleep 接受 ms：
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    做 sys_nanosleep_ms 用 ms。
完。

win32_GetSystemInfo 接受 信息：
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    若 信息 不等于 0：
        做 内存_写32 用 信息 加 0、0。
        做 内存_写32 用 信息 加 4、4096。
        做 内存_写32 用 信息 加 20、1。
    完。
完。

win32_VirtualAlloc 接受 地址、大小、类型、保护：
    NW32_内存分配计数 为 NW32_内存分配计数 加 1。
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    返还 做 kmalloc 用 大小。
完。

win32_VirtualFree 接受 地址、大小、类型：
    NW32_内存分配计数 为 NW32_内存分配计数 加 1。
    NW32_API调用计数 为 NW32_API调用计数 加 1。
    做 kfree 用 地址。
    返还 1。
完。

可变 _谐振打印_排序次数 为 0。

可变 _谐振打印_合并次数 为 0。

谐振打印_优先排序 接受 任务列表、任务数：
    可变 i 为 0。
    只要 i 小于 任务数 减 1：
        可变 最小idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 任务数：
            若 任务列表 的 第 j 项 的 页数 小于 任务列表 的 第 最小idx 项 的 页数：
                最小idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最小idx 不等于 i：
            临时 为 任务列表 的 第 i 项。
            任务列表 的 第 i 项 为 任务列表 的 第 最小idx 项。
            任务列表 的 第 最小idx 项 为 临时。
        完。
        i 为 i 加 1。
    完。
    _谐振打印_排序次数 为 _谐振打印_排序次数 加 1。
完。

打印_完整统计 接受 零参：
    返还 一组：「谐振排序次数」、_谐振打印_排序次数、「谐振合并次数」、_谐振打印_合并次数。
完。

本章 收束。
