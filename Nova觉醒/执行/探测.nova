《探测》

引用《..·感知·ReadableStream》。

引用《..·标准·原生字符串》。

引用《..·感知·内核直连层》。

引用《..·中枢·内核打印》。

引用《..·中枢·高精度定时器hrtimer》。

引用《..·中枢·整数转字符串》。

PING_默认次数 为 4。

PING_默认间隔 为 1000。

PING_默认TTL 为 64。

PING_默认大小 为 56。

PING_默认超时 为 5000。

PING_ICMP_回显请求 为 8。

PING_ICMP_回显应答 为 0。

PING_ICMP头大小 为 8。

PING_IP头大小 为 20。

Ping配置 是 一种 事物，含 目标、次数、间隔、TTL值、包大小、超时、洪水模式、静默模式、详细模式、解析地址。

Ping结果 是 一种 事物，含 已发、已收、总往返时间、最小往返时间、最大往返时间、往返时间列表、重复包数、错误数。

ping_解析参数 接受 参数：
    配置 为 新建 Ping配置。
    配置 的 目标 为 「」。
    配置 的 次数 为 PING_默认次数。
    配置 的 间隔 为 PING_默认间隔。
    配置 的 TTL值 为 PING_默认TTL。
    配置 的 包大小 为 PING_默认大小。
    配置 的 超时 为 PING_默认超时。
    配置 的 洪水模式 为 假。
    配置 的 静默模式 为 假。
    配置 的 详细模式 为 假。
    配置 的 解析地址 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        a 为 参数 的 第 i 项。
        若 a 等于 「-c」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 次数 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-i」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 间隔 为 做 字符串转整数 用 参数 的 第 i 项 乘 1000。
        否则若 a 等于 「-t」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 TTL值 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-s」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 包大小 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-W」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 超时 为 做 字符串转整数 用 参数 的 第 i 项 乘 1000。
        否则若 a 等于 「-f」：
            配置 的 洪水模式 为 真。
        否则若 a 等于 「-q」：
            配置 的 静默模式 为 真。
        否则若 a 等于 「-v」：
            配置 的 详细模式 为 真。
        否则：
            配置 的 目标 为 a。
        完。
        i 为 i 加 1。
    完。
    返还 配置。
完。

ping_验证配置 接受 配置：
    若 配置 的 目标 等于 「」：
        做 打印行 用 「用法: ping [-c 次数] [-i 间隔秒] [-t TTL] [-s 大小] [-W 超时秒] [-f] [-q] [-v] 目标地址」。
        返还 假。
    完。
    若 配置 的 次数 小于或等于 0：
        配置 的 次数 为 PING_默认次数。
    完。
    若 配置 的 TTL值 小于或等于 0 或者 配置 的 TTL值 大于 255：
        配置 的 TTL值 为 PING_默认TTL。
    完。
    若 配置 的 包大小 小于 0：
        配置 的 包大小 为 PING_默认大小。
    完。
    若 配置 的 包大小 大于 65500：
        配置 的 包大小 为 65500。
    完。
    若 配置 的 超时 小于或等于 0：
        配置 的 超时 为 PING_默认超时。
    完。
    返还 真。
完。

ping_ICMP校验和 接受 数据、长度：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 加 1 小于 长度：
        和 为 和 加 (做 内存_读8 用 数据 加 i 左移 8) 加 做 内存_读8 用 数据 加 i 加 1。
        i 为 i 加 2。
    完。
    若 i 小于 长度：
        和 为 和 加 (做 内存_读8 用 数据 加 i 左移 8)。
    完。
    只要 和 大于 0xFFFF：
        和 为 (和 位与 0xFFFF) 加 (和 右移 16)。
    完。
    返还 (和 位异或 0xFFFF) 位与 0xFFFF。
完。

ping_构造ICMP包 接受 序号、包大小：
    总大小 为 PING_ICMP头大小 加 包大小。
    包 为 做 堆_分配 用 总大小。
    做 内存_写8 用 包、PING_ICMP_回显请求。
    做 内存_写8 用 包 加 1、0。
    做 内存_写16 用 包 加 2、0。
    做 内存_写16 用 包 加 4、做 获取当前进程ID 位与 0xFFFF。
    做 内存_写16 用 包 加 6、序号。
    可变 i 为 0。
    只要 i 小于 包大小：
        做 内存_写8 用 包 加 PING_ICMP头大小 加 i、(i 加 65) 取余于 256。
        i 为 i 加 1。
    完。
    校验和 为 做 ping_ICMP校验和 用 包、总大小。
    做 内存_写16 用 包 加 2、校验和。
    返还 一组：包、总大小。
完。

ping_DNS解析 接受 主机名：
    ip 为 做 网络_DNS解析 用 主机名。
    若 ip 不等于 空 并且 ip 不等于 「」：
        返还 ip。
    完。
    返还 主机名。
完。

ping_计算抖动 接受 往返列表、数量：
    若 数量 小于 2：
        返还 0。
    完。
    可变 总差 为 0。
    可变 i 为 1。
    只要 i 小于 数量：
        差 为 往返列表 的 第 i 项 减 往返列表 的 第 i 减 1 项。
        若 差 小于 0：
            差 为 0 减 差。
        完。
        总差 为 总差 加 差。
        i 为 i 加 1。
    完。
    返还 总差 除以 (数量 减 1)。
完。

ping_计算标准差 接受 往返列表、数量、平均：
    若 数量 小于 2：
        返还 0。
    完。
    可变 总方差 为 0。
    可变 i 为 0。
    只要 i 小于 数量：
        差 为 往返列表 的 第 i 项 减 平均。
        总方差 为 总方差 加 差 乘 差。
        i 为 i 加 1。
    完。
    返还 做 整数平方根 用 总方差 除以 数量。
完。

整数平方根 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

ping_main 接受 参数：
    配置 为 做 ping_解析参数 用 参数。
    若 非 做 ping_验证配置 用 配置：
        返还 1。
    完。
    解析IP 为 做 ping_DNS解析 用 配置 的 目标。
    配置 的 解析地址 为 解析IP。
    总字节 为 配置 的 包大小 加 PING_ICMP头大小 加 PING_IP头大小。
    若 非 配置 的 静默模式：
        做 打印行 用 做 字符串拼接 用 「PING 」、做 字符串拼接 用 配置 的 目标、做 字符串拼接 用 「 (」、做 字符串拼接 用 解析IP、做 字符串拼接 用 「) 」、做 字符串拼接 用 做 整数转字符串 用 配置 的 包大小、做 字符串拼接 用 「(」、做 字符串拼接 用 做 整数转字符串 用 总字节、「) bytes of data.」。
    完。
    结果 为 新建 Ping结果。
    结果 的 已发 为 0。
    结果 的 已收 为 0。
    结果 的 总往返时间 为 0。
    结果 的 最小往返时间 为 999999。
    结果 的 最大往返时间 为 0。
    结果 的 往返时间列表 为 一组：（空）。
    结果 的 重复包数 为 0。
    结果 的 错误数 为 0。
    可变 序号 为 1。
    只要 序号 小于或等于 配置 的 次数：
        包信息 为 做 ping_构造ICMP包 用 序号、配置 的 包大小。
        开始时间 为 做 获取滴答。
        往返时间 为 做 网络_ping_扩展 用 解析IP、包信息 的 第 0 项、包信息 的 第 1 项、配置 的 TTL值、配置 的 超时。
        结果 的 已发 为 结果 的 已发 加 1。
        若 往返时间 大于或等于 0：
            结果 的 已收 为 结果 的 已收 加 1。
            结果 的 总往返时间 为 结果 的 总往返时间 加 往返时间。
            若 往返时间 小于 结果 的 最小往返时间：
                结果 的 最小往返时间 为 往返时间。
            完。
            若 往返时间 大于 结果 的 最大往返时间：
                结果 的 最大往返时间 为 往返时间。
            完。
            做 追加 用 结果 的 往返时间列表、往返时间。
            若 非 配置 的 静默模式 并且 非 配置 的 洪水模式：
                做 打印行 用 做 字符串拼接 用 做 整数转字符串 用 总字节、做 字符串拼接 用 「 bytes from 」、做 字符串拼接 用 解析IP、做 字符串拼接 用 「: icmp_seq=」、做 字符串拼接 用 做 整数转字符串 用 序号、做 字符串拼接 用 「 ttl=」、做 字符串拼接 用 做 整数转字符串 用 配置 的 TTL值、做 字符串拼接 用 「 time=」、做 字符串拼接 用 做 整数转字符串 用 往返时间、「 ms」。
            完。
            若 配置 的 洪水模式：
                做 打印 用 「.」。
            完。
        否则若 往返时间 等于 负 1：
            若 非 配置 的 静默模式 并且 非 配置 的 洪水模式：
                做 打印行 用 做 字符串拼接 用 「Request timeout for icmp_seq 」、做 整数转字符串 用 序号。
            完。
        否则若 往返时间 等于 负 2：
            结果 的 错误数 为 结果 的 错误数 加 1。
            若 配置 的 详细模式：
                做 打印行 用 做 字符串拼接 用 「From 」、做 字符串拼接 用 解析IP、「: Destination Host Unreachable」。
            完。
        否则若 往返时间 等于 负 3：
            结果 的 错误数 为 结果 的 错误数 加 1。
            若 配置 的 详细模式：
                做 打印行 用 做 字符串拼接 用 「From 」、做 字符串拼接 用 解析IP、「: Time to live exceeded」。
            完。
        完。
        做 堆_释放 用 包信息 的 第 0 项。
        若 非 配置 的 洪水模式 并且 序号 小于 配置 的 次数：
            做 睡眠 用 配置 的 间隔。
        完。
        序号 为 序号 加 1。
    完。
    若 配置 的 洪水模式：
        做 打印行 用 「」。
    完。
    做 ping_打印统计 用 配置、结果。
    返还 0。
完。

ping_打印统计 接受 配置、结果：
    做 打印行 用 「」。
    做 打印行 用 做 字符串拼接 用 「--- 」、做 字符串拼接 用 配置 的 目标、「 ping statistics ---」。
    可变 丢包率 为 0。
    若 结果 的 已发 大于 0：
        丢包率 为 (结果 的 已发 减 结果 的 已收) 乘 100 除以 结果 的 已发。
    完。
    可变 统计行 为 做 字符串拼接 用 做 整数转字符串 用 结果 的 已发、「 packets transmitted, 」。
    统计行 为 做 字符串拼接 用 统计行、做 字符串拼接 用 做 整数转字符串 用 结果 的 已收、「 received, 」。
    若 结果 的 重复包数 大于 0：
        统计行 为 做 字符串拼接 用 统计行、做 字符串拼接 用 「+」、做 字符串拼接 用 做 整数转字符串 用 结果 的 重复包数、「 duplicates, 」。
    完。
    若 结果 的 错误数 大于 0：
        统计行 为 做 字符串拼接 用 统计行、做 字符串拼接 用 「+」、做 字符串拼接 用 做 整数转字符串 用 结果 的 错误数、「 errors, 」。
    完。
    统计行 为 做 字符串拼接 用 统计行、做 字符串拼接 用 做 整数转字符串 用 丢包率、「% packet loss」。
    做 打印行 用 统计行。
    若 结果 的 已收 大于 0：
        平均 为 结果 的 总往返时间 除以 结果 的 已收。
        标准差 为 做 ping_计算标准差 用 结果 的 往返时间列表、结果 的 已收、平均。
        抖动 为 做 ping_计算抖动 用 结果 的 往返时间列表、结果 的 已收。
        做 打印行 用 做 字符串拼接 用 「rtt min/avg/max/mdev = 」、做 字符串拼接 用 做 整数转字符串 用 结果 的 最小往返时间、做 字符串拼接 用 「/」、做 字符串拼接 用 做 整数转字符串 用 平均、做 字符串拼接 用 「/」、做 字符串拼接 用 做 整数转字符串 用 结果 的 最大往返时间、做 字符串拼接 用 「/」、做 字符串拼接 用 做 整数转字符串 用 标准差、「 ms」。
        若 抖动 大于 0：
            做 打印行 用 做 字符串拼接 用 「jitter = 」、做 字符串拼接 用 做 整数转字符串 用 抖动、「 ms」。
        完。
    完。
完。

网络_ping 接受 目标：
    返还 做 网络_ping_扩展 用 目标、0、0、PING_默认TTL、PING_默认超时。
完。

网络_ping_扩展 接受 目标、包数据、包大小、ttl、超时：
    sock 为 做 网络_创建原始套接字 用 PING_ICMP_回显请求。
    若 sock 小于 0：
        返还 负 2。
    完。
    做 网络_设置套接字选项 用 sock、「TTL」、ttl。
    做 网络_设置套接字选项 用 sock、「超时」、超时。
    开始 为 做 获取滴答。
    发送结果 为 做 网络_发送到 用 sock、目标、包数据、包大小。
    若 发送结果 小于 0：
        做 网络_关闭套接字 用 sock。
        返还 负 2。
    完。
    接收缓冲 为 做 堆_分配 用 1024。
    接收长度 为 做 网络_接收从 用 sock、接收缓冲、1024。
    结束 为 做 获取滴答。
    做 网络_关闭套接字 用 sock。
    若 接收长度 小于 0：
        做 堆_释放 用 接收缓冲。
        返还 负 1。
    完。
    若 接收长度 大于或等于 PING_IP头大小 加 PING_ICMP头大小：
        类型 为 做 内存_读8 用 接收缓冲 加 PING_IP头大小。
        做 堆_释放 用 接收缓冲。
        若 类型 等于 PING_ICMP_回显应答：
            返还 结束 减 开始。
        完。
        若 类型 等于 3：
            返还 负 2。
        完。
        若 类型 等于 11：
            返还 负 3。
        完。
    完。
    做 堆_释放 用 接收缓冲。
    返还 负 1。
完。

网络_DNS解析 接受 主机名：
    返还 主机名。
完。

ping_traceroute 接受 目标、最大跳数：
    做 打印行 用 做 字符串拼接 用 「traceroute to 」、做 字符串拼接 用 目标、做 字符串拼接 用 「, 」、做 字符串拼接 用 做 整数转字符串 用 最大跳数、「 hops max」。
    可变 ttl 为 1。
    只要 ttl 小于或等于 最大跳数：
        包信息 为 做 ping_构造ICMP包 用 ttl、PING_默认大小。
        可变 最佳时间 为 999999。
        可变 成功 为 假。
        可变 探测 为 0。
        做 打印 用 做 字符串拼接 用 做 整数转字符串 用 ttl、「  」。
        只要 探测 小于 3：
            开始 为 做 获取滴答。
            时间 为 做 网络_ping_扩展 用 目标、包信息 的 第 0 项、包信息 的 第 1 项、ttl、3000。
            若 时间 大于或等于 0：
                做 打印 用 做 字符串拼接 用 做 整数转字符串 用 时间、「 ms  」。
                成功 为 真。
                若 时间 小于 最佳时间：
                    最佳时间 为 时间。
                完。
            否则：
                做 打印 用 「*  」。
            完。
            探测 为 探测 加 1。
        完。
        做 打印行 用 「」。
        做 堆_释放 用 包信息 的 第 0 项。
        若 成功 并且 最佳时间 小于 999999：
        完。
        ttl 为 ttl 加 1。
    完。
完。

ping_探测MTU 接受 目标：
    可变 上界 为 1500。
    可变 下界 为 68。
    可变 最佳MTU 为 下界。
    只要 下界 小于或等于 上界：
        mid 为 (上界 加 下界) 除以 2。
        负载 为 做 堆_分配 用 mid。
        结果 为 做 网络_ping_扩展 用 目标、负载、mid、PING_默认TTL、2000。
        做 堆_释放 用 负载。
        若 结果 大于或等于 0：
            最佳MTU 为 mid。
            下界 为 mid 加 1。
        否则：
            上界 为 mid 减 1。
        完。
    完。
    返还 最佳MTU。
完。

可变 NNPT_总探测数 为 0。

可变 NNPT_成功数 为 0。

可变 NNPT_总RTT 为 0。

可变 NNPT_最小RTT 为 999999。

可变 NNPT_最大RTT 为 0。

NNPT_记录 接受 往返时间：
    NNPT_总探测数 为 NNPT_总探测数 加 1。
    若 往返时间 大于或等于 0：
        NNPT_成功数 为 NNPT_成功数 加 1。
        NNPT_总RTT 为 NNPT_总RTT 加 往返时间。
        若 往返时间 小于 NNPT_最小RTT：
            NNPT_最小RTT 为 往返时间。
        完。
        若 往返时间 大于 NNPT_最大RTT：
            NNPT_最大RTT 为 往返时间。
        完。
    完。
完。

NNPT_平均RTT 接受 零参：
    若 NNPT_成功数 等于 0：
        返还 0。
    完。
    返还 NNPT_总RTT 除以 NNPT_成功数。
完。

NNPT_丢包率 接受 零参：
    若 NNPT_总探测数 等于 0：
        返还 0。
    完。
    返还 (NNPT_总探测数 减 NNPT_成功数) 乘 1000 除以 NNPT_总探测数。
完。

NNPT_重置 接受 零参：
    NNPT_总探测数 为 0。
    NNPT_成功数 为 0。
    NNPT_总RTT 为 0。
    NNPT_最小RTT 为 999999。
    NNPT_最大RTT 为 0。
完。

NNPT_完整诊断 接受 零参：
    返还 一组：「总探测数」、NNPT_总探测数、「成功数」、NNPT_成功数、「平均RTT」、做 NNPT_平均RTT、「最小RTT」、NNPT_最小RTT、「最大RTT」、NNPT_最大RTT、「丢包率」、做 NNPT_丢包率、「NNPT网络探测」、1。
完。

ping_批量探测 接受 目标列表、超时：
    结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 目标列表：
        rtt 为 做 网络_ping_扩展 用 目标列表 的 第 i 项、0、0、PING_默认TTL、超时。
        做 NNPT_记录 用 rtt。
        做 追加 用 结果、一组：目标列表 的 第 i 项、rtt。
        i 为 i 加 1。
    完。
    返还 结果。
完。

ping_连续探测 接受 目标、次数、间隔ms：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 次数：
        rtt 为 做 网络_ping_扩展 用 目标、0、0、PING_默认TTL、PING_默认超时。
        做 NNPT_记录 用 rtt。
        做 追加 用 结果、rtt。
        若 i 小于 次数 减 1 并且 间隔ms 大于 0：
            做 睡眠 用 间隔ms。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

NNPT_质量评分 接受 零参：
    可变 分 为 100。
    平均 为 做 NNPT_平均RTT。
    若 平均 大于 200：
        分 为 分 减 30。
    否则若 平均 大于 100：
        分 为 分 减 15。
    否则若 平均 大于 50：
        分 为 分 减 5。
    完。
    丢包 为 做 NNPT_丢包率。
    若 丢包 大于 100：
        分 为 分 减 40。
    否则若 丢包 大于 50：
        分 为 分 减 20。
    否则若 丢包 大于 10：
        分 为 分 减 5。
    完。
    若 NNPT_最大RTT 减 NNPT_最小RTT 大于 100：
        分 为 分 减 10。
    完。
    若 分 小于 0：
        分 为 0。
    完。
    返还 分。
完。

NNPT_获取总探测数 接受 零参：
    返还 NNPT_总探测数。
完。

NNPT_获取成功数 接受 零参：
    返还 NNPT_成功数。
完。

NNPT_获取最小RTT 接受 零参：
    返还 NNPT_最小RTT。
完。

NNPT_获取最大RTT 接受 零参：
    返还 NNPT_最大RTT。
完。

ping_获取默认配置 接受 零参：
    返还 一本字典：次数 对应 PING_默认次数、TTL 对应 PING_默认TTL、超时 对应 PING_默认超时、大小 对应 PING_默认大小、间隔 对应 PING_默认间隔。
完。

可变 AI_探测_带宽估计 为 0。

可变 AI_探测_丢包窗口 为 一组：（空）。

AI_探测_带宽预测 接受 发送字节、耗时毫秒：
    若 耗时毫秒 等于 0：
        返还 AI_探测_带宽估计。
    完。
    当前 为 发送字节 乘 1000 除以 耗时毫秒。
    AI_探测_带宽估计 为 AI_探测_带宽估计 乘 700 除以 1000 加 当前 乘 300 除以 1000。
    返还 AI_探测_带宽估计。
完。

AI_探测_拥塞建议 接受 丢包率：
    做 追加 用 AI_探测_丢包窗口、丢包率。
    若 做 计数 用 AI_探测_丢包窗口 大于 10：
        做 列表_移除 用 AI_探测_丢包窗口、0。
    完。
    若 丢包率 大于 50：
        返还 「减半窗口」。
    完。
    若 丢包率 小于 5：
        返还 「增大窗口」。
    完。
    返还 「保持」。
完。

本章 收束。
