《语法高亮》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·标准·原生字符串》。

颜色_重置 为 「\x1b[0m」。

颜色_粗体 为 「\x1b[1m」。

颜色_红 为 「\x1b[0;31m」。

颜色_绿 为 「\x1b[0;32m」。

颜色_黄 为 「\x1b[0;33m」。

颜色_蓝 为 「\x1b[0;34m」。

颜色_品红 为 「\x1b[0;35m」。

颜色_青 为 「\x1b[0;36m」。

颜色_灰 为 「\x1b[0;90m」。

颜色_亮绿 为 「\x1b[1;32m」。

颜色_亮红 为 「\x1b[1;31m」。

颜色_亮蓝 为 「\x1b[1;34m」。

可变 高亮_已启用 为 真。

高亮_命令行 接受 文本：
    若 非 高亮_已启用：
        返还 文本。
    完。
    若 做 字符串长度 用 文本 等于 0：
        返还 文本。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    可变 词索引 为 0。
    可变 在字符串内 为 假。
    可变 字符串引号 为 「」。
    可变 当前词 为 「」。
    只要 i 小于 做 字符串长度 用 文本：
        c 为 做 字符串取字符 用 文本、i。
        若 c 等于 「#」 并且 非 在字符串内：
            结果 为 结果 加 颜色_灰 加 做 字符串_截取 用 文本、i、做 字符串长度 用 文本 加 颜色_重置。
            返还 结果。
        完。
        若 (c 等于 「\"」 或者 c 等于 「'」) 并且 非 在字符串内：
            在字符串内 为 真。
            字符串引号 为 c。
            结果 为 结果 加 颜色_黄 加 c。
            i 为 i 加 1。
            继续。
        完。
        若 在字符串内 并且 c 等于 字符串引号：
            在字符串内 为 假。
            结果 为 结果 加 c 加 颜色_重置。
            i 为 i 加 1。
            继续。
        完。
        若 在字符串内：
            若 c 等于 「$」 并且 字符串引号 等于 「\"」：
                结果 为 结果 加 颜色_亮蓝 加 c。
            否则：
                结果 为 结果 加 c。
            完。
            i 为 i 加 1。
            继续。
        完。
        若 c 等于 「$」：
            结果 为 结果 加 颜色_蓝。
            只要 i 小于 做 字符串长度 用 文本 并且 做 字符串取字符 用 文本、i 不等于 「 」：
                结果 为 结果 加 做 字符串取字符 用 文本、i。
                i 为 i 加 1。
            完。
            结果 为 结果 加 颜色_重置。
            继续。
        完。
        若 c 等于 「|」 或者 c 等于 「>」 或者 c 等于 「<」：
            结果 为 结果 加 颜色_品红 加 c。
            若 c 等于 「>」 并且 i 加 1 小于 做 字符串长度 用 文本 并且 做 字符串取字符 用 文本、i 加 1 等于 「>」：
                结果 为 结果 加 「>」。
                i 为 i 加 1。
            完。
            结果 为 结果 加 颜色_重置。
            i 为 i 加 1。
            继续。
        完。
        若 c 等于 「 」：
            若 做 字符串长度 用 当前词 大于 0：
                结果 为 结果 加 做 高亮_词 用 当前词、词索引。
                当前词 为 「」。
                词索引 为 词索引 加 1。
            完。
            结果 为 结果 加 「 」。
            i 为 i 加 1。
            继续。
        完。
        当前词 为 当前词 加 c。
        i 为 i 加 1。
    完。
    若 做 字符串长度 用 当前词 大于 0：
        结果 为 结果 加 做 高亮_词 用 当前词、词索引。
    完。
    若 在字符串内：
        结果 为 结果 加 颜色_重置。
    完。
    返还 结果。
完。

高亮_词 接受 词、索引：
    若 索引 等于 0：
        若 做 是有效命令 用 词：
            返还 颜色_亮绿 加 词 加 颜色_重置。
        完。
        返还 颜色_亮红 加 词 加 颜色_重置。
    完。
    若 做 字符串_开头是 用 词、「--」 或者 做 字符串_开头是 用 词、「-」：
        返还 颜色_青 加 词 加 颜色_重置。
    完。
    若 做 字符串包含 用 词、「/」：
        返还 颜色_蓝 加 词 加 颜色_重置。
    完。
    返还 词。
完。

是有效命令 接受 名称：
    内建 为 一组：「cd」、「pwd」、「echo」、「exit」、「export」、「history」、「alias」、「type」、「help」、「source」、「jobs」、「fg」、「bg」、「kill」、「set」、「unset」、「env」、「clear」、「read」、「test」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 内建：
        若 内建 的 第 i 项 等于 名称：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    常见 为 一组：「ls」、「cat」、「grep」、「find」、「ps」、「top」、「cp」、「mv」、「rm」、「mkdir」、「touch」、「chmod」、「df」、「du」、「wc」、「head」、「tail」、「sort」、「uniq」、「mount」、「umount」。
    i 为 0。
    只要 i 小于 做 计数 用 常见：
        若 常见 的 第 i 项 等于 名称：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

高亮_命令 接受 文本：
    返还 做 高亮_命令行 用 文本。
完。

高亮_开关 接受 启用：
    高亮_已启用 为 启用。
完。

可变 AI_语法高亮_调用数 为 0。

可变 AI_语法高亮_异常数 为 0。

AI_语法高亮_监控 接受 零参：
    AI_语法高亮_调用数 为 AI_语法高亮_调用数 加 1。
    返还 AI_语法高亮_异常数 等于 0。
完。

AI_语法高亮_健康检查 接受 零参：
    若 AI_语法高亮_调用数 大于 0 并且 AI_语法高亮_异常数 乘 100 除以 AI_语法高亮_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
