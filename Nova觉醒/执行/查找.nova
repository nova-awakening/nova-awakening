《查找》

引用《..·标准·文件》。

引用《..·感知·ReadableStream》。

引用《..·标准·原生字符串》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内核打印》。

Find配置 是 一种 事物，含 路径、名称模式、名称忽略大小写、类型过滤、最小大小、最大大小、修改天数、修改比较、权限掩码、最大深度、仅空、较新参考、较新时间、执行命令、打印路径、删除匹配。

Find统计 是 一种 事物，含 扫描文件数、扫描目录数、匹配数、跳过数、执行命令数、删除数、最大深度达到。

可变 全局_find统计 为 空。

find统计_初始化 接受 零参：
    若 全局_find统计 等于 空：
        全局_find统计 为 新建 Find统计。
        全局_find统计 的 扫描文件数 为 0。
        全局_find统计 的 扫描目录数 为 0。
        全局_find统计 的 匹配数 为 0。
        全局_find统计 的 跳过数 为 0。
        全局_find统计 的 执行命令数 为 0。
        全局_find统计 的 删除数 为 0。
        全局_find统计 的 最大深度达到 为 0。
    完。
完。

find_解析参数 接受 参数：
    配置 为 新建 Find配置。
    配置 的 路径 为 「.」。
    配置 的 名称模式 为 「」。
    配置 的 名称忽略大小写 为 假。
    配置 的 类型过滤 为 「」。
    配置 的 最小大小 为 负 1。
    配置 的 最大大小 为 负 1。
    配置 的 修改天数 为 0。
    配置 的 修改比较 为 0。
    配置 的 权限掩码 为 负 1。
    配置 的 最大深度 为 999。
    配置 的 仅空 为 假。
    配置 的 较新参考 为 「」。
    配置 的 较新时间 为 0。
    配置 的 执行命令 为 「」。
    配置 的 打印路径 为 真。
    配置 的 删除匹配 为 假。
    可变 路径已设 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        a 为 参数 的 第 i 项。
        若 a 等于 「-name」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 名称模式 为 参数 的 第 i 项。
            配置 的 名称忽略大小写 为 假。
        否则若 a 等于 「-iname」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 名称模式 为 参数 的 第 i 项。
            配置 的 名称忽略大小写 为 真。
        否则若 a 等于 「-type」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 类型过滤 为 参数 的 第 i 项。
        否则若 a 等于 「-size」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            做 find_解析大小 用 参数 的 第 i 项、配置。
        否则若 a 等于 「-mtime」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            做 find_解析修改时间 用 参数 的 第 i 项、配置。
        否则若 a 等于 「-perm」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 权限掩码 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-maxdepth」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 最大深度 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-empty」：
            配置 的 仅空 为 真。
        否则若 a 等于 「-newer」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 较新参考 为 参数 的 第 i 项。
            配置 的 较新时间 为 做 文件_修改时间 用 参数 的 第 i 项。
        否则若 a 等于 「-exec」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            配置 的 执行命令 为 参数 的 第 i 项。
        否则若 a 等于 「-delete」：
            配置 的 删除匹配 为 真。
            配置 的 打印路径 为 假。
        否则若 a 等于 「-print」：
            配置 的 打印路径 为 真。
        否则若 非 路径已设：
            配置 的 路径 为 a。
            路径已设 为 真。
        完。
        i 为 i 加 1。
    完。
    返还 配置。
完。

find_解析大小 接受 大小字符串、配置：
    长 为 做 字符串长度 用 大小字符串。
    若 长 等于 0：
        返还。
    完。
    首字符 为 做 字符串取字符 用 大小字符串、0。
    可变 数值串 为 大小字符串。
    可变 乘数 为 512。
    末字符 为 做 字符串取字符 用 大小字符串、长 减 1。
    若 末字符 等于 99：
        乘数 为 1。
        数值串 为 做 字符串子串 用 大小字符串、0、长 减 1。
    否则若 末字符 等于 107：
        乘数 为 1024。
        数值串 为 做 字符串子串 用 大小字符串、0、长 减 1。
    否则若 末字符 等于 77：
        乘数 为 1024 乘 1024。
        数值串 为 做 字符串子串 用 大小字符串、0、长 减 1。
    否则若 末字符 等于 71：
        乘数 为 1024 乘 1024 乘 1024。
        数值串 为 做 字符串子串 用 大小字符串、0、长 减 1。
    完。
    若 首字符 等于 43：
        配置 的 最小大小 为 做 字符串转整数 用 做 字符串子串 用 数值串、1、做 字符串长度 用 数值串 乘 乘数。
    否则若 首字符 等于 45：
        配置 的 最大大小 为 做 字符串转整数 用 做 字符串子串 用 数值串、1、做 字符串长度 用 数值串 乘 乘数。
    否则：
        精确 为 做 字符串转整数 用 数值串 乘 乘数。
        配置 的 最小大小 为 精确。
        配置 的 最大大小 为 精确 加 乘数 减 1。
    完。
完。

find_解析修改时间 接受 时间字符串、配置：
    首字符 为 做 字符串取字符 用 时间字符串、0。
    若 首字符 等于 43：
        配置 的 修改天数 为 做 字符串转整数 用 做 字符串子串 用 时间字符串、1、做 字符串长度 用 时间字符串。
        配置 的 修改比较 为 1。
    否则若 首字符 等于 45：
        配置 的 修改天数 为 做 字符串转整数 用 做 字符串子串 用 时间字符串、1、做 字符串长度 用 时间字符串。
        配置 的 修改比较 为 负 1。
    否则：
        配置 的 修改天数 为 做 字符串转整数 用 时间字符串。
        配置 的 修改比较 为 0。
    完。
完。

find_main 接受 参数：
    做 find统计_初始化。
    配置 为 做 find_解析参数 用 参数。
    若 配置 的 路径 等于 「」 并且 做 计数 用 参数 等于 0：
        做 打印行 用 「用法: find [路径] [选项]」。
        做 打印行 用 「  -name 模式    名称匹配(支持*通配符)」。
        做 打印行 用 「  -iname 模式   忽略大小写名称匹配」。
        做 打印行 用 「  -type f|d     类型过滤(f=文件,d=目录)」。
        做 打印行 用 「  -size [+/-]N[c|k|M|G]  大小过滤」。
        做 打印行 用 「  -mtime [+/-]N 修改时间(天)」。
        做 打印行 用 「  -maxdepth N   最大递归深度」。
        做 打印行 用 「  -empty        仅空文件/目录」。
        做 打印行 用 「  -newer 文件   比文件新」。
        做 打印行 用 「  -exec 命令    对匹配执行命令」。
        做 打印行 用 「  -delete       删除匹配项」。
        返还 1。
    完。
    做 find_递归 用 配置 的 路径、配置、0。
    返还 0。
完。

find_递归 接受 路径、配置、深度：
    若 深度 大于 配置 的 最大深度：
        全局_find统计 的 最大深度达到 为 全局_find统计 的 最大深度达到 加 1。
        返还。
    完。
    条目 为 做 文件_读目录 用 路径。
    若 条目 等于 空：
        返还。
    完。
    全局_find统计 的 扫描目录数 为 全局_find统计 的 扫描目录数 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目：
        名称 为 条目 的 第 i 项 的 第 0 项。
        子路径 为 做 字符串拼接 用 路径、做 字符串拼接 用 「/」、名称。
        是目录 为 做 文件_是目录 用 子路径。
        若 是目录：
            全局_find统计 的 扫描目录数 为 全局_find统计 的 扫描目录数 加 1。
        否则：
            全局_find统计 的 扫描文件数 为 全局_find统计 的 扫描文件数 加 1。
        完。
        匹配 为 做 find_检查匹配 用 名称、子路径、是目录、配置。
        若 匹配：
            全局_find统计 的 匹配数 为 全局_find统计 的 匹配数 加 1。
            若 配置 的 打印路径：
                做 打印行 用 子路径。
            完。
            若 配置 的 执行命令 不等于 「」：
                做 find_执行命令 用 配置 的 执行命令、子路径。
                全局_find统计 的 执行命令数 为 全局_find统计 的 执行命令数 加 1。
            完。
            若 配置 的 删除匹配 并且 非 是目录：
                做 文件_删除 用 子路径。
                全局_find统计 的 删除数 为 全局_find统计 的 删除数 加 1。
            完。
        否则：
            全局_find统计 的 跳过数 为 全局_find统计 的 跳过数 加 1。
        完。
        若 是目录：
            做 find_递归 用 子路径、配置、深度 加 1。
        完。
        i 为 i 加 1。
    完。
完。

find_检查匹配 接受 名称、路径、是目录、配置：
    若 配置 的 类型过滤 等于 「f」 并且 是目录：
        返还 假。
    完。
    若 配置 的 类型过滤 等于 「d」 并且 非 是目录：
        返还 假。
    完。
    若 配置 的 名称模式 不等于 「」：
        可变 检查名 为 名称。
        可变 检查模式 为 配置 的 名称模式。
        若 配置 的 名称忽略大小写：
            检查名 为 做 字符串转小写 用 名称。
            检查模式 为 做 字符串转小写 用 配置 的 名称模式。
        完。
        若 非 做 find_匹配名称 用 检查名、检查模式：
            返还 假。
        完。
    完。
    若 配置 的 最小大小 大于或等于 0 或者 配置 的 最大大小 大于或等于 0：
        若 是目录：
            返还 假。
        完。
        文件大小 为 做 文件_大小 用 路径。
        若 配置 的 最小大小 大于或等于 0 并且 文件大小 小于 配置 的 最小大小：
            返还 假。
        完。
        若 配置 的 最大大小 大于或等于 0 并且 文件大小 大于 配置 的 最大大小：
            返还 假。
        完。
    完。
    若 配置 的 修改天数 大于 0：
        文件时间 为 做 文件_修改时间 用 路径。
        当前时间 为 做 系统_时间。
        天差 为 (当前时间 减 文件时间) 除以 86400。
        若 配置 的 修改比较 等于 1 并且 天差 小于或等于 配置 的 修改天数：
            返还 假。
        完。
        若 配置 的 修改比较 等于 负 1 并且 天差 大于或等于 配置 的 修改天数：
            返还 假。
        完。
        若 配置 的 修改比较 等于 0 并且 天差 不等于 配置 的 修改天数：
            返还 假。
        完。
    完。
    若 配置 的 权限掩码 大于或等于 0：
        文件权限 为 做 文件_权限 用 路径。
        若 文件权限 位与 配置 的 权限掩码 不等于 配置 的 权限掩码：
            返还 假。
        完。
    完。
    若 配置 的 仅空：
        若 是目录：
            子条目 为 做 文件_读目录 用 路径。
            若 子条目 不等于 空 并且 做 计数 用 子条目 大于 0：
                返还 假。
            完。
        否则：
            若 做 文件_大小 用 路径 大于 0：
                返还 假。
            完。
        完。
    完。
    若 配置 的 较新参考 不等于 「」 并且 配置 的 较新时间 大于 0：
        文件时间 为 做 文件_修改时间 用 路径。
        若 文件时间 小于或等于 配置 的 较新时间：
            返还 假。
        完。
    完。
    返还 真。
完。

find_匹配名称 接受 名称、模式：
    模式长 为 做 字符串长度 用 模式。
    若 模式长 等于 0：
        返还 真。
    完。
    若 做 字符串取字符 用 模式、0 等于 42：
        若 模式长 大于 1 并且 做 字符串取字符 用 模式、模式长 减 1 等于 42：
            中间 为 做 字符串子串 用 模式、1、模式长 减 1。
            返还 做 字符串包含 用 名称、中间。
        完。
        后缀 为 做 字符串子串 用 模式、1、模式长。
        返还 做 字符串以结尾 用 名称、后缀。
    完。
    若 做 字符串取字符 用 模式、模式长 减 1 等于 42：
        前缀 为 做 字符串子串 用 模式、0、模式长 减 1。
        返还 做 字符串以开头 用 名称、前缀。
    完。
    返还 名称 等于 模式。
完。

find_执行命令 接受 命令模板、文件路径：
    可变 实际命令 为 命令模板。
    若 做 字符串包含 用 命令模板、「{}」：
        实际命令 为 做 字符串替换 用 命令模板、「{}」、文件路径。
    否则：
        实际命令 为 做 字符串拼接 用 命令模板、做 字符串拼接 用 「 」、文件路径。
    完。
    做 系统_执行 用 实际命令。
完。

字符串以结尾 接受 文本、后缀：
    文长 为 做 字符串长度 用 文本。
    后长 为 做 字符串长度 用 后缀。
    若 后长 大于 文长：
        返还 假。
    完。
    返还 做 字符串子串 用 文本、文长 减 后长、文长 等于 后缀。
完。

字符串以开头 接受 文本、前缀：
    前长 为 做 字符串长度 用 前缀。
    若 前长 大于 做 字符串长度 用 文本：
        返还 假。
    完。
    返还 做 字符串子串 用 文本、0、前长 等于 前缀。
完。

find_打印统计 接受 零参：
    做 find统计_初始化。
    做 打印行 用 「=== find统计 ===」。
    做 打印行 用 「扫描文件: 」 加 做 整数转字符串 用 全局_find统计 的 扫描文件数。
    做 打印行 用 「扫描目录: 」 加 做 整数转字符串 用 全局_find统计 的 扫描目录数。
    做 打印行 用 「匹配数: 」 加 做 整数转字符串 用 全局_find统计 的 匹配数。
    做 打印行 用 「跳过数: 」 加 做 整数转字符串 用 全局_find统计 的 跳过数。
    做 打印行 用 「执行命令: 」 加 做 整数转字符串 用 全局_find统计 的 执行命令数。
    做 打印行 用 「删除数: 」 加 做 整数转字符串 用 全局_find统计 的 删除数。
    若 全局_find统计 的 扫描文件数 加 全局_find统计 的 扫描目录数 大于 0：
        命中率 为 全局_find统计 的 匹配数 乘 1000 除以 (全局_find统计 的 扫描文件数 加 全局_find统计 的 扫描目录数)。
        做 打印行 用 「命中率: 」 加 做 整数转字符串 用 命中率 加 「/1000」。
    完。
完。

可变 全局_grep匹配数 为 0。

find_grep 接受 路径、模式、最大深度：
    全局_grep匹配数 为 0。
    做 find_grep_递归 用 路径、模式、0、最大深度。
    返还 全局_grep匹配数。
完。

find_grep_递归 接受 路径、模式、深度、最大深度：
    若 深度 大于 最大深度：
        返还。
    完。
    条目 为 做 文件_读目录 用 路径。
    若 条目 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目：
        名称 为 条目 的 第 i 项 的 第 0 项。
        子路径 为 做 字符串拼接 用 路径、做 字符串拼接 用 「/」、名称。
        是目录 为 条目 的 第 i 项 的 第 1 项 等于 1。
        若 是目录：
            做 find_grep_递归 用 子路径、模式、深度 加 1、最大深度。
        否则：
            内容 为 做 文件_读全 用 子路径。
            若 内容 不等于 空 并且 做 字符串包含 用 内容、模式：
                做 打印行 用 子路径。
                全局_grep匹配数 为 全局_grep匹配数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

find_高级匹配 接受 文本、模式：
    返还 做 find_通配匹配_递归 用 文本、0、模式、0。
完。

find_通配匹配_递归 接受 文本、ti、模式、pi：
    文长 为 做 字符串长度 用 文本。
    模长 为 做 字符串长度 用 模式。
    若 pi 等于 模长：
        返还 ti 等于 文长。
    完。
    模字符 为 做 字符串取字符 用 模式、pi。
    若 模字符 等于 42：
        可变 k 为 ti。
        只要 k 小于或等于 文长：
            若 做 find_通配匹配_递归 用 文本、k、模式、pi 加 1：
                返还 真。
            完。
            k 为 k 加 1。
        完。
        返还 假。
    完。
    若 ti 大于或等于 文长：
        返还 假。
    完。
    若 模字符 等于 63：
        返还 做 find_通配匹配_递归 用 文本、ti 加 1、模式、pi 加 1。
    完。
    若 做 字符串取字符 用 文本、ti 等于 模字符：
        返还 做 find_通配匹配_递归 用 文本、ti 加 1、模式、pi 加 1。
    完。
    返还 假。
完。

可变 find_结果缓冲 为 一组：（空）。

可变 find_结果数 为 0。

find_最大结果 为 4096。

find_收集结果 接受 路径、配置、最大深度：
    find_结果缓冲 为 一组：（空）。
    find_结果数 为 0。
    做 find_收集递归 用 路径、配置、0、最大深度。
    返还 find_结果数。
完。

find_收集递归 接受 路径、配置、深度、最大深度：
    若 深度 大于 最大深度 或者 find_结果数 大于或等于 find_最大结果：
        返还。
    完。
    条目 为 做 文件_读目录 用 路径。
    若 条目 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目：
        名称 为 条目 的 第 i 项 的 第 0 项。
        子路径 为 做 字符串拼接 用 路径、做 字符串拼接 用 「/」、名称。
        是目录 为 条目 的 第 i 项 的 第 1 项 等于 1。
        若 做 find_检查匹配 用 名称、子路径、是目录、配置：
            做 追加 用 find_结果缓冲、子路径。
            find_结果数 为 find_结果数 加 1。
        完。
        若 是目录：
            做 find_收集递归 用 子路径、配置、深度 加 1、最大深度。
        完。
        i 为 i 加 1。
    完。
完。

find_获取结果 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 find_结果数：
        返还 「」。
    完。
    返还 find_结果缓冲 的 第 索引 项。
完。

find_获取结果数 接受 零参：
    返还 find_结果数。
完。

find_重置统计 接受 零参：
    做 find统计_初始化。
    全局_find统计 的 扫描文件数 为 0。
    全局_find统计 的 扫描目录数 为 0。
    全局_find统计 的 匹配数 为 0。
    全局_find统计 的 跳过数 为 0。
    全局_find统计 的 执行命令数 为 0。
    全局_find统计 的 删除数 为 0。
    全局_find统计 的 最大深度达到 为 0。
    全局_grep匹配数 为 0。
完。

find_获取扫描文件数 接受 零参：
    返还 全局_find统计 的 扫描文件数。
完。

find_获取扫描目录数 接受 零参：
    返还 全局_find统计 的 扫描目录数。
完。

find_获取匹配数 接受 零参：
    返还 全局_find统计 的 匹配数。
完。

find_获取跳过数 接受 零参：
    返还 全局_find统计 的 跳过数。
完。

find_获取执行数 接受 零参：
    返还 全局_find统计 的 执行命令数。
完。

find_获取删除数 接受 零参：
    返还 全局_find统计 的 删除数。
完。

find_获取grep数 接受 零参：
    返还 全局_grep匹配数。
完。

find_完整统计 接受 零参：
    做 find统计_初始化。
    返还 一组：一组：「扫描文件」、全局_find统计 的 扫描文件数、一组：「扫描目录」、全局_find统计 的 扫描目录数、一组：「匹配数」、全局_find统计 的 匹配数、一组：「跳过数」、全局_find统计 的 跳过数、一组：「执行命令」、全局_find统计 的 执行命令数、一组：「删除数」、全局_find统计 的 删除数、一组：「grep匹配」、全局_grep匹配数、一组：「收集结果」、find_结果数。
完。

find_健康检查 接受 零参：
    做 find统计_初始化。
    可变 问题 为 0。
    若 全局_find统计 的 删除数 大于 全局_find统计 的 匹配数：
        问题 为 问题 加 1。
    完。
    若 find_结果数 大于 find_最大结果：
        问题 为 问题 加 1。
    完。
    返还 问题 等于 0。
完。

可变 AI_查找_热文件表 为 一组：（空）。

AI_查找_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_查找_热文件表：
        若 AI_查找_热文件表 的 第 i 项 的 路径 等于 路径：
            AI_查找_热文件表 的 第 i 项 的 频率 为 AI_查找_热文件表 的 第 i 项 的 频率 加 1。
            返还 AI_查找_热文件表 的 第 i 项 的 频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_查找_热文件表 小于 128：
        做 追加 用 AI_查找_热文件表、一本字典：路径 对应 路径、频率 对应 1。
    完。
    返还 1。
完。

AI_查找_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
        返还 0。
    完。
    返还 32768。
完。

本章 收束。
