《下载管理》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·归档解包》。

引用《..·中枢·时钟中枢》。

引用《..·记忆·VFS文件读写》。

引用《..·中枢·读取调用》。

引用《..·感知·内核直连层》。

下载状态_等待 为 0。

下载状态_进行 为 1。

下载状态_完成 为 2。

下载状态_失败 为 3。

下载状态_暂停 为 4。

下载状态_取消 为 5。

下载_最大并发 为 4。

下载_最大重试 为 3。

下载_块大小 为 65536。

下载_速度采样间隔 为 1000。

下载任务 是 一种 事物，含 ID、URL、文件名、保存路径、总大小、已下载、状态、文件FD、开始时间、最近写入时间、重试次数、MIME类型、速度、速度_上次采样字节、速度_上次采样时间、哈希_CRC32、支持续传。

可变 下载_下一个ID 为 1。

可变 下载_总完成数 为 0。

可变 下载_总失败数 为 0。

可变 下载_总字节数 为 0。

浏览器存储_开始下载 接受 url、文件名：
    任务 为 做 堆_分配 用 下载任务。
    任务 的 ID 为 下载_下一个ID。
    下载_下一个ID 为 下载_下一个ID 加 1。
    任务 的 URL 为 url。
    任务 的 文件名 为 文件名。
    任务 的 保存路径 为 做 字符串拼接 用 存储_下载目录、做 字符串拼接 用 「/」、文件名。
    任务 的 总大小 为 0。
    任务 的 已下载 为 0。
    任务 的 状态 为 下载状态_等待。
    任务 的 重试次数 为 0。
    任务 的 速度 为 0。
    任务 的 哈希_CRC32 为 0。
    任务 的 支持续传 为 假。
    任务 的 开始时间 为 做 读取毫秒。
    任务 的 速度_上次采样时间 为 任务 的 开始时间。
    任务 的 速度_上次采样字节 为 0。
    活跃数 为 做 浏览器存储_活跃下载数。
    若 活跃数 大于或等于 下载_最大并发：
        做 追加 用 存储_下载列表、任务。
        返还 任务。
    完。
    任务 的 文件FD 为 做 VFS_打开 用 任务 的 保存路径、O_创建 位或 O_写入、644。
    若 任务 的 文件FD 小于 0：
        任务 的 状态 为 下载状态_失败。
        下载_总失败数 为 下载_总失败数 加 1。
        做 追加 用 存储_下载列表、任务。
        返还 任务。
    完。
    任务 的 状态 为 下载状态_进行。
    做 追加 用 存储_下载列表、任务。
    返还 任务。
完。

浏览器存储_活跃下载数 接受 零参：
    可变 数量 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 存储_下载列表：
        若 存储_下载列表 的 第 i 项 的 状态 等于 下载状态_进行：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

浏览器存储_下载写入块 接受 任务ID、数据、大小：
    任务 为 做 浏览器存储_查找任务 用 任务ID。
    若 任务 等于 0：
        返还 负 1。
    完。
    若 任务 的 状态 不等于 下载状态_进行：
        返还 负 2。
    完。
    写入量 为 做 VFS_写入 用 任务 的 文件FD、数据、大小。
    若 写入量 小于 0：
        任务 的 重试次数 为 任务 的 重试次数 加 1。
        若 任务 的 重试次数 大于或等于 下载_最大重试：
            任务 的 状态 为 下载状态_失败。
            做 VFS_关闭 用 任务 的 文件FD。
            下载_总失败数 为 下载_总失败数 加 1。
        完。
        返还 负 3。
    完。
    任务 的 已下载 为 任务 的 已下载 加 大小。
    任务 的 最近写入时间 为 做 读取毫秒。
    下载_总字节数 为 下载_总字节数 加 大小。
    任务 的 哈希_CRC32 为 做 CRC32_更新 用 任务 的 哈希_CRC32、数据、大小。
    做 浏览器存储_更新速度 用 任务。
    返还 0。
完。

浏览器存储_更新速度 接受 任务：
    现在 为 做 读取毫秒。
    间隔 为 现在 减 任务 的 速度_上次采样时间。
    若 间隔 大于或等于 下载_速度采样间隔：
        字节差 为 任务 的 已下载 减 任务 的 速度_上次采样字节。
        任务 的 速度 为 字节差 乘 1000 除以 间隔。
        任务 的 速度_上次采样字节 为 任务 的 已下载。
        任务 的 速度_上次采样时间 为 现在。
    完。
完。

浏览器存储_下载完成 接受 任务ID：
    任务 为 做 浏览器存储_查找任务 用 任务ID。
    若 任务 等于 0：
        返还。
    完。
    做 VFS_关闭 用 任务 的 文件FD。
    任务 的 状态 为 下载状态_完成。
    下载_总完成数 为 下载_总完成数 加 1。
    做 浏览器存储_启动等待任务。
完。

浏览器存储_暂停下载 接受 任务ID：
    任务 为 做 浏览器存储_查找任务 用 任务ID。
    若 任务 等于 0 或者 任务 的 状态 不等于 下载状态_进行：
        返还。
    完。
    任务 的 状态 为 下载状态_暂停。
    做 VFS_同步 用 任务 的 文件FD。
    做 浏览器存储_启动等待任务。
完。

浏览器存储_恢复下载 接受 任务ID：
    任务 为 做 浏览器存储_查找任务 用 任务ID。
    若 任务 等于 0 或者 任务 的 状态 不等于 下载状态_暂停：
        返还。
    完。
    活跃数 为 做 浏览器存储_活跃下载数。
    若 活跃数 大于或等于 下载_最大并发：
        返还。
    完。
    任务 的 状态 为 下载状态_进行。
    任务 的 速度_上次采样时间 为 做 读取毫秒。
    任务 的 速度_上次采样字节 为 任务 的 已下载。
完。

浏览器存储_取消下载 接受 任务ID：
    任务 为 做 浏览器存储_查找任务 用 任务ID。
    若 任务 等于 0：
        返还。
    完。
    若 任务 的 状态 等于 下载状态_进行 或者 任务 的 状态 等于 下载状态_暂停：
        做 VFS_关闭 用 任务 的 文件FD。
    完。
    任务 的 状态 为 下载状态_取消。
    做 VFS_删除 用 任务 的 保存路径。
    做 浏览器存储_启动等待任务。
完。

浏览器存储_重试下载 接受 任务ID：
    任务 为 做 浏览器存储_查找任务 用 任务ID。
    若 任务 等于 0 或者 任务 的 状态 不等于 下载状态_失败：
        返还。
    完。
    任务 的 重试次数 为 0。
    若 任务 的 支持续传 并且 任务 的 已下载 大于 0：
        任务 的 文件FD 为 做 VFS_打开 用 任务 的 保存路径、O_写入 位或 O_追加、644。
    否则：
        任务 的 已下载 为 0。
        任务 的 哈希_CRC32 为 0。
        任务 的 文件FD 为 做 VFS_打开 用 任务 的 保存路径、O_创建 位或 O_写入 位或 O_截断、644。
    完。
    若 任务 的 文件FD 小于 0：
        任务 的 状态 为 下载状态_失败。
        返还。
    完。
    任务 的 状态 为 下载状态_进行。
    任务 的 速度_上次采样时间 为 做 读取毫秒。
    任务 的 速度_上次采样字节 为 任务 的 已下载。
完。

浏览器存储_查找任务 接受 任务ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 存储_下载列表：
        若 存储_下载列表 的 第 i 项 的 ID 等于 任务ID：
            返还 存储_下载列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

浏览器存储_启动等待任务 接受 零参：
    活跃数 为 做 浏览器存储_活跃下载数。
    若 活跃数 大于或等于 下载_最大并发：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 存储_下载列表 并且 活跃数 小于 下载_最大并发：
        若 存储_下载列表 的 第 i 项 的 状态 等于 下载状态_等待：
            任务 为 存储_下载列表 的 第 i 项。
            任务 的 文件FD 为 做 VFS_打开 用 任务 的 保存路径、O_创建 位或 O_写入、644。
            若 任务 的 文件FD 大于或等于 0：
                任务 的 状态 为 下载状态_进行。
                任务 的 开始时间 为 做 读取毫秒。
                活跃数 为 活跃数 加 1。
            否则：
                任务 的 状态 为 下载状态_失败。
                下载_总失败数 为 下载_总失败数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

浏览器存储_下载进度 接受 任务ID：
    任务 为 做 浏览器存储_查找任务 用 任务ID。
    若 任务 等于 0：
        返还 0。
    完。
    若 任务 的 总大小 小于或等于 0：
        返还 负 1。
    完。
    返还 任务 的 已下载 乘 100 除以 任务 的 总大小。
完。

浏览器存储_下载剩余时间 接受 任务ID：
    任务 为 做 浏览器存储_查找任务 用 任务ID。
    若 任务 等于 0 或者 任务 的 速度 小于或等于 0 或者 任务 的 总大小 小于或等于 0：
        返还 负 1。
    完。
    剩余 为 任务 的 总大小 减 任务 的 已下载。
    返还 剩余 除以 任务 的 速度。
完。

CRC32_更新 接受 crc、数据、大小：
    可变 c 为 crc 位异或 0xFFFFFFFF。
    可变 i 为 0。
    只要 i 小于 大小：
        字节 为 做 内存_读8 用 数据 加 i。
        可变 j 为 0。
        c 为 c 位异或 字节。
        只要 j 小于 8：
            若 c 位与 1：
                c 为 c 右移 1 位异或 0xEDB88320。
            否则：
                c 为 c 右移 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c 位异或 0xFFFFFFFF。
完。

浏览器存储_统计 接受 零参：
    返还 一组：做 计数 用 存储_缓存表、存储_缓存已用、做 计数 用 存储_Cookie表、做 计数 用 存储_本地数据、做 计数 用 存储_下载列表、下载_总完成数、下载_总失败数、下载_总字节数、做 浏览器存储_活跃下载数。
完。

可变 AI_下载管理_负载窗口 为 一组：（空）。

AI_下载管理_负载预测 接受 当前负载：
    做 追加 用 AI_下载管理_负载窗口、当前负载。
    若 做 计数 用 AI_下载管理_负载窗口 大于 20：
        做 列表_移除 用 AI_下载管理_负载窗口、0。
    完。
    若 做 计数 用 AI_下载管理_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_下载管理_负载窗口。
    趋势 为 AI_下载管理_负载窗口 的 第 n 减 1 项 减 AI_下载管理_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_下载管理_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
