《任务控制》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·内核打印》。

引用《..·中枢·线程退出》。

引用《..·中枢·整数转字符串》。

任务状态_运行 为 0。

任务状态_暂停 为 1。

任务状态_完成 为 2。

任务状态_前台 为 3。

任务项 是 一种 事物，含 编号、pid、pgid、状态、退出码、命令、通知。

最大任务数 为 256。

可变 任务表 为 一组：（空）。

可变 下一个任务编号 为 1。

可变 当前任务 为 0。

可变 前一个任务 为 0。

任务_初始化 接受 零参：
    任务表 为 一组：（空）。
    下一个任务编号 为 1。
    当前任务 为 0。
    前一个任务 为 0。
完。

任务_添加 接受 pid、命令、状态：
    若 做 计数 用 任务表 大于或等于 最大任务数：
        做 任务_清理已完成。
    完。
    任务 为 新建 任务项。
    任务 的 编号 为 下一个任务编号。
    任务 的 pid 为 pid。
    任务 的 pgid 为 pid。
    任务 的 状态 为 状态。
    任务 的 退出码 为 负 1。
    任务 的 命令 为 命令。
    任务 的 通知 为 假。
    做 追加 用 任务表、任务。
    前一个任务 为 当前任务。
    当前任务 为 下一个任务编号。
    下一个任务编号 为 下一个任务编号 加 1。
    返还 任务 的 编号。
完。

任务_添加后台 接受 pid、命令：
    返还 做 任务_添加 用 pid、命令、任务状态_运行。
完。

任务_列表 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务表：
        任务 为 任务表 的 第 i 项。
        若 任务 的 状态 不等于 任务状态_完成 或者 任务 的 通知：
            状态文本 为 做 任务_状态文本 用 任务 的 状态。
            标记 为 「 」。
            若 任务 的 编号 等于 当前任务：
                标记 为 「+」。
            否则若 任务 的 编号 等于 前一个任务：
                标记 为 「-」。
            完。
            做 打印行 用 「[」 加 做 整数转字符串 用 任务 的 编号 加 「]」 加 标记 加 「  」 加 状态文本 加 「                    」 加 任务 的 命令。
            若 任务 的 状态 等于 任务状态_完成：
                任务 的 通知 为 假。
            完。
        完。
        i 为 i 加 1。
    完。
完。

任务_状态文本 接受 状态：
    若 状态 等于 任务状态_运行：
        返还 「Running」。
    完。
    若 状态 等于 任务状态_暂停：
        返还 「Stopped」。
    完。
    若 状态 等于 任务状态_完成：
        返还 「Done」。
    完。
    若 状态 等于 任务状态_前台：
        返还 「Running(fg)」。
    完。
    返还 「Unknown」。
完。

任务_查找 接受 编号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务表：
        若 任务表 的 第 i 项 的 编号 等于 编号：
            返还 任务表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

任务_按PID查找 接受 pid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务表：
        若 任务表 的 第 i 项 的 pid 等于 pid：
            返还 任务表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

任务_获取PID 接受 编号：
    若 编号 等于 0：
        编号 为 当前任务。
    完。
    任务 为 做 任务_查找 用 编号。
    若 任务 等于 空：
        返还 负 1。
    完。
    返还 任务 的 pid。
完。

任务_当前编号 接受 零参：
    返还 当前任务。
完。

任务_设置状态 接受 编号、新状态：
    任务 为 做 任务_查找 用 编号。
    若 任务 不等于 空：
        任务 的 状态 为 新状态。
        若 新状态 等于 任务状态_完成：
            任务 的 通知 为 真。
        完。
    完。
完。

任务_设置退出码 接受 编号、退出码：
    任务 为 做 任务_查找 用 编号。
    若 任务 不等于 空：
        任务 的 退出码 为 退出码。
    完。
完。

任务_移除 接受 编号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务表：
        若 任务表 的 第 i 项 的 编号 等于 编号：
            做 列表_移除 用 任务表、i。
            若 当前任务 等于 编号：
                当前任务 为 前一个任务。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

任务_回收后台 接受 零参：
    只要 真：
        状态 为 一组：0。
        pid 为 做 sys_wait4 用 负 1、状态、WNOHANG、空、0、0。
        若 pid 小于或等于 0：
            中断。
        完。
        任务 为 做 任务_按PID查找 用 pid。
        若 任务 不等于 空：
            退出码 为 状态 的 第 0 项 右移 8 位与 0xFF。
            任务 的 状态 为 任务状态_完成。
            任务 的 退出码 为 退出码。
            任务 的 通知 为 真。
            做 打印行 用 「\n[」 加 做 整数转字符串 用 任务 的 编号 加 「]+  Done」 加 「                    」 加 任务 的 命令。
        完。
    完。
完。

任务_等待全部 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务表：
        任务 为 任务表 的 第 i 项。
        若 任务 的 状态 等于 任务状态_运行：
            状态 为 一组：0。
            做 sys_wait4 用 任务 的 pid、状态、0、空、0、0。
            任务 的 状态 为 任务状态_完成。
            任务 的 退出码 为 状态 的 第 0 项 右移 8 位与 0xFF。
        完。
        i 为 i 加 1。
    完。
完。

任务_清理已完成 接受 零参：
    可变 i 为 做 计数 用 任务表 减 1。
    只要 i 大于或等于 0：
        若 任务表 的 第 i 项 的 状态 等于 任务状态_完成 并且 非 任务表 的 第 i 项 的 通知：
            做 列表_移除 用 任务表、i。
        完。
        i 为 i 减 1。
    完。
完。

任务_统计 接受 零参：
    可变 运行中 为 0。
    可变 暂停 为 0。
    可变 完成 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务表：
        若 任务表 的 第 i 项 的 状态 等于 任务状态_运行：
            运行中 为 运行中 加 1。
        完。
        若 任务表 的 第 i 项 的 状态 等于 任务状态_暂停：
            暂停 为 暂停 加 1。
        完。
        若 任务表 的 第 i 项 的 状态 等于 任务状态_完成：
            完成 为 完成 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：运行中、暂停、完成。
完。

WNOHANG 为 0x00000001。

可变 AI_任务控制_运行指标 为 0。

可变 AI_任务控制_异常指标 为 0。

AI_任务控制_自诊断 接受 零参：
    AI_任务控制_运行指标 为 0。
    返还 AI_任务控制_运行指标。
完。

AI_任务控制_优化建议 接受 零参：
    指标 为 做 AI_任务控制_自诊断。
    返还 「任务控制状态正常」。
完。

本章 收束。
