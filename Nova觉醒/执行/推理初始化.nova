《推理初始化》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·整数转字符串》。

意图_导航 为 0。

意图_搜索 为 1。

意图_系统命令 为 2。

意图_AI对话 为 3。

意图_文件操作 为 4。

意图_进程管理 为 5。

意图_网络操作 为 6。

意图_内存管理 为 7。

意图_计算 为 8。

意图_翻译 为 9。

意图_代码 为 10。

意图_未知 为 99。

命令映射 是 一种 事物，含 关键词列表、意图类型、内核操作、描述。

意图结果 是 一种 事物，含 意图类型、置信度、参数、内核操作、回复文本、需要确认。

对话上下文 是 一种 事物，含 会话ID、历史、当前意图、连续对话、上下文参数。

可变 意图_命令表 为 一组：（空）。

可变 意图_当前上下文 为 空。

可变 意图_总推理次数 为 0。

可变 意图_命中次数 为 0。

可变 意图_已初始化 为 假。

意图推理_初始化 接受 零参：
    意图_命令表 为 一组：（空）。
    做 意图推理_注册命令 用 一组：「打开设置」、「系统设置」、「settings」、意图_系统命令、「系统_打开设置」、「打开系统设置」。
    做 意图推理_注册命令 用 一组：「打开终端」、「终端」、「terminal」、「终端」、意图_系统命令、「系统_打开终端」、「打开终端」。
    做 意图推理_注册命令 用 一组：「打开文件管理」、「文件管理器」、「files」、意图_系统命令、「系统_打开文件管理器」、「打开文件管理器」。
    做 意图推理_注册命令 用 一组：「截图」、「截屏」、「screenshot」、意图_系统命令、「系统_截图」、「截取屏幕」。
    做 意图推理_注册命令 用 一组：「锁屏」、「锁定」、「lock」、意图_系统命令、「系统_锁屏」、「锁定屏幕」。
    做 意图推理_注册命令 用 一组：「关机」、「shutdown」、意图_系统命令、「系统_关机」、「关闭系统」。
    做 意图推理_注册命令 用 一组：「重启」、「reboot」、意图_系统命令、「系统_重启」、「重启系统」。
    做 意图推理_注册命令 用 一组：「音量」、「声音」、「volume」、意图_系统命令、「系统_音量调节」、「调节音量」。
    做 意图推理_注册命令 用 一组：「创建文件」、「新建文件」、意图_文件操作、「VFS_创建文件」、「创建新文件」。
    做 意图推理_注册命令 用 一组：「删除文件」、「移除文件」、意图_文件操作、「VFS_删除」、「删除文件」。
    做 意图推理_注册命令 用 一组：「搜索文件」、「查找文件」、「find」、意图_文件操作、「VFS_搜索」、「搜索文件」。
    做 意图推理_注册命令 用 一组：「查看进程」、「任务管理」、「top」、「htop」、意图_进程管理、「进程_列表」、「查看进程列表」。
    做 意图推理_注册命令 用 一组：「杀死进程」、「结束进程」、「kill」、意图_进程管理、「进程_终止」、「终止进程」。
    做 意图推理_注册命令 用 一组：「清理内存」、「释放内存」、「free memory」、意图_内存管理、「内存_回收」、「清理系统内存」。
    做 意图推理_注册命令 用 一组：「内存状态」、「内存使用」、「memory」、意图_内存管理、「内存_统计」、「查看内存状态」。
    做 意图推理_注册命令 用 一组：「网络状态」、「网络连接」、「network」、意图_网络操作、「网络_状态」、「查看网络状态」。
    做 意图推理_注册命令 用 一组：「ping」、「网络测试」、意图_网络操作、「网络_ping」、「网络连通测试」。
    做 意图推理_注册命令 用 一组：「你好」、「hi」、「hello」、「help」、「帮助」、意图_AI对话、「智能_对话」、「AI助手对话」。
    意图_当前上下文 为 新建 对话上下文。
    意图_当前上下文 的 历史 为 一组：（空）。
    意图_当前上下文 的 连续对话 为 假。
    意图_当前上下文 的 上下文参数 为 一组：（空）。
    意图_已初始化 为 真。
    做 内核信息 用 「AI意图推理: 初始化完成, 」 加 做 整数转字符串 用 做 计数 用 意图_命令表 加 「个命令已注册」。
完。

意图推理_注册命令 接受 关键词、意图类型、操作、描述：
    映射 为 新建 命令映射。
    映射 的 关键词列表 为 关键词。
    映射 的 意图类型 为 意图类型。
    映射 的 内核操作 为 操作。
    映射 的 描述 为 描述。
    做 追加 用 意图_命令表、映射。
完。

本章 收束。
