《渲染初始化》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《.·节点操作》。

渲染盒 是 一种 事物，含 节点ID、x、y、宽、高、外边距上、外边距右、外边距下、外边距左、内边距上、内边距右、内边距下、内边距左、边框宽、显示、位置、背景色、前景色、字号、圆角、透明度、Z序、子盒、父盒、图处层ID、需要独立层、脏。

渲染树 是 一种 事物，含 根盒、盒池、下一个ID、视口宽、视口高、滚动X、滚动Y、总盒数、脏。

可变 渲染_树 为 空。

可变 渲染_已初始化 为 假。

渲染_初始化 接受 视口宽、视口高：
    渲染_树 为 新建 渲染树。
    渲染_树 的 盒池 为 一组：（空）。
    渲染_树 的 下一个ID 为 1。
    渲染_树 的 视口宽 为 视口宽。
    渲染_树 的 视口高 为 视口高。
    渲染_树 的 滚动X 为 0。
    渲染_树 的 滚动Y 为 0。
    渲染_树 的 总盒数 为 0。
    渲染_树 的 脏 为 真。
    渲染_已初始化 为 真。
完。

渲染_构建树 接受 文档：
    渲染_树 的 盒池 为 一组：（空）。
    渲染_树 的 下一个ID 为 1。
    渲染_树 的 总盒数 为 0。
    body 为 做 DOM_查找节点 用 文档、文档 的 体节点。
    若 body 等于 空：
        body 为 做 DOM_查找节点 用 文档、文档 的 根节点。
    完。
    若 body 等于 空：
        返还。
    完。
    根盒 为 做 渲染_创建盒 用 body、文档。
    根盒 的 x 为 0。
    根盒 的 y 为 0。
    根盒 的 宽 为 渲染_树 的 视口宽。
    渲染_树 的 根盒 为 根盒 的 节点ID。
    做 渲染_构建子盒 用 文档、body、根盒。
    渲染_树 的 脏 为 真。
完。

渲染_创建盒 接受 节点、文档：
    盒 为 新建 渲染盒。
    盒 的 节点ID 为 节点 的 ID。
    盒 的 子盒 为 一组：（空）。
    盒 的 脏 为 真。
    盒 的 需要独立层 为 假。
    盒 的 透明度 为 255。
    样式 为 做 CSS_计算节点样式 用 文档、节点。
    若 样式 的 有效：
        显示值 为 样式 的 属性值 的 第 CSS_显示 项。
        若 显示值 等于 「none」：
            盒 的 显示 为 显示_无。
        否则若 显示值 等于 「inline」：
            盒 的 显示 为 显示_行。
        否则若 显示值 等于 「inline-block」：
            盒 的 显示 为 显示_行块。
        否则若 显示值 等于 「flex」：
            盒 的 显示 为 显示_弹性。
        否则若 显示值 等于 「grid」：
            盒 的 显示 为 显示_网格。
        否则：
            盒 的 显示 为 显示_块。
        完。
        盒 的 背景色 为 做 CSS_解析颜色值 用 样式 的 属性值 的 第 CSS_背景色 项。
        盒 的 前景色 为 做 CSS_解析颜色值 用 样式 的 属性值 的 第 CSS_前景色 项。
        盒 的 字号 为 做 CSS_解析像素值 用 样式 的 属性值 的 第 CSS_字号 项。
        若 盒 的 字号 等于 0：
            盒 的 字号 为 16。
        完。
        宽值 为 样式 的 属性值 的 第 CSS_宽度 项。
        若 做 字符串长度 用 宽值 大于 0 并且 宽值 不等于 「auto」：
            盒 的 宽 为 做 CSS_解析像素值 用 宽值。
        完。
        高值 为 样式 的 属性值 的 第 CSS_高度 项。
        若 做 字符串长度 用 高值 大于 0 并且 高值 不等于 「auto」：
            盒 的 高 为 做 CSS_解析像素值 用 高值。
        完。
        边距值 为 样式 的 属性值 的 第 CSS_外边距 项。
        若 做 字符串长度 用 边距值 大于 0：
            m 为 做 CSS_解析像素值 用 边距值。
            盒 的 外边距上 为 m。
            盒 的 外边距右 为 m。
            盒 的 外边距下 为 m。
            盒 的 外边距左 为 m。
        完。
        内边距值 为 样式 的 属性值 的 第 CSS_内边距 项。
        若 做 字符串长度 用 内边距值 大于 0：
            p 为 做 CSS_解析像素值 用 内边距值。
            盒 的 内边距上 为 p。
            盒 的 内边距右 为 p。
            盒 的 内边距下 为 p。
            盒 的 内边距左 为 p。
        完。
        盒 的 圆角 为 做 CSS_解析像素值 用 样式 的 属性值 的 第 CSS_圆角 项。
        若 做 字符串长度 用 样式 的 属性值 的 第 CSS_变换 项 大于 0：
            盒 的 需要独立层 为 真。
        完。
        若 盒 的 透明度 小于 255：
            盒 的 需要独立层 为 真。
        完。
    完。
    做 追加 用 渲染_树 的 盒池、盒。
    渲染_树 的 总盒数 为 渲染_树 的 总盒数 加 1。
    返还 盒。
完。

渲染_构建子盒 接受 文档、节点、父盒：
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点 的 子节点：
        子节点 为 做 DOM_查找节点 用 文档、节点 的 子节点 的 第 i 项。
        若 子节点 不等于 空：
            若 子节点 的 类型 等于 节点_元素：
                子盒 为 做 渲染_创建盒 用 子节点、文档。
                若 子盒 的 显示 不等于 显示_无：
                    子盒 的 父盒 为 父盒 的 节点ID。
                    做 追加 用 父盒 的 子盒、子盒 的 节点ID。
                    做 渲染_构建子盒 用 文档、子节点、子盒。
                完。
            否则若 子节点 的 类型 等于 节点_文本：
                若 做 字符串长度 用 子节点 的 文本内容 大于 0：
                    文本盒 为 新建 渲染盒。
                    文本盒 的 节点ID 为 子节点 的 ID。
                    文本盒 的 显示 为 显示_行。
                    文本盒 的 前景色 为 父盒 的 前景色。
                    文本盒 的 字号 为 父盒 的 字号。
                    文本盒 的 子盒 为 一组：（空）。
                    文本盒 的 脏 为 真。
                    文本盒 的 透明度 为 255。
                    文本盒 的 父盒 为 父盒 的 节点ID。
                    做 追加 用 父盒 的 子盒、文本盒 的 节点ID。
                    做 追加 用 渲染_树 的 盒池、文本盒。
                    渲染_树 的 总盒数 为 渲染_树 的 总盒数 加 1。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

本章 收束。
