《浏览器图处》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·每核页释放》。

命令_填充矩形 为 0。

命令_绘制线条 为 1。

命令_绘制文字 为 2。

命令_绘制图像 为 3。

命令_设置裁剪 为 4。

命令_清除裁剪 为 5。

命令_变换矩阵 为 6。

命令_绘制圆角矩形 为 7。

命令_绘制阴影 为 8。

命令_绘制渐变 为 9。

命令_混合模式 为 10。

命令_提交帧 为 11。

混合_正常 为 0。

混合_叠加 为 1。

混合_正片叠底 为 2。

混合_滤色 为 3。

渲染命令 是 一种 事物，含 类型、x、y、宽、高、颜色、透明度、圆角、文字、字号、图像数据、图像宽、图像高。

图处渲染层 是 一种 事物，含 ID、x、y、宽、高、缓冲地址、脏、透明度、混合模式、变换、命令队列。

图处渲染上下文 是 一种 事物，含 屏幕宽、屏幕高、帧缓冲、后缓冲、层列表、下一层ID、帧计数、图处模式、VSync。

可变 图处_上下文 为 空。

可变 图处_已初始化 为 假。

可变 图处_帧率 为 0。

可变 图处_上帧时间 为 0。

浏览器图处_初始化 接受 宽、高：
    图处_上下文 为 新建 图处渲染上下文。
    图处_上下文 的 屏幕宽 为 宽。
    图处_上下文 的 屏幕高 为 高。
    图处_上下文 的 层列表 为 一组：（空）。
    图处_上下文 的 下一层ID 为 1。
    图处_上下文 的 帧计数 为 0。
    图处_上下文 的 VSync 为 真。
    图处_上下文 的 帧缓冲 为 帧缓冲地址。
    图处_上下文 的 后缓冲 为 做 伙伴系统_分配 用 宽 乘 高 乘 4。
    图处能力 为 做 图处_查询能力。
    若 图处能力 大于或等于 2：
        图处_上下文 的 图处模式 为 2。
        做 内核信息 用 「浏览器图处: 3D硬件加速启用」。
    否则若 图处能力 大于或等于 1：
        图处_上下文 的 图处模式 为 1。
        做 内核信息 用 「浏览器图处: 2D硬件加速启用」。
    否则：
        图处_上下文 的 图处模式 为 0。
        做 内核信息 用 「浏览器图处: CPU软渲染模式」。
    完。
    图处_已初始化 为 真。
完。

本章 收束。
