《管道系统》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·命令解析器》。

引用《..·中枢·退出调用》。

引用《..·中枢·线程clone语义》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《..·中枢·字符串替换》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·复制描述》。

引用《..·中枢·读取调用》。

执行管道 接受 命令列表：
    命令数 为 做 计数 用 命令列表。
    若 命令数 等于 0：
        返还 0。
    完。
    若 命令数 等于 1：
        返还 做 执行命令 用 做 解析命令行 用 命令列表 的 第 0 项。
    完。
    可变 管道列表 为 一组：（空）。
    可变 子进程列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 命令数 减 1：
        pipefd 为 做 sys_pipe。
        若 pipefd 等于 空：
            做 打印行 用 「龙壳: 管道创建失败」。
            返还 负 1。
        完。
        做 追加 用 管道列表、pipefd。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 命令数：
        pid 为 做 sys_fork。
        若 pid 等于 0：
            若 i 大于 0：
                做 sys_dup2 用 管道列表 的 第 i 减 1 项 的 第 0 项、0。
            完。
            若 i 小于 命令数 减 1：
                做 sys_dup2 用 管道列表 的 第 i 项 的 第 1 项、1。
            完。
            可变 j 为 0。
            只要 j 小于 做 计数 用 管道列表：
                做 sys_close 用 管道列表 的 第 j 项 的 第 0 项。
                做 sys_close 用 管道列表 的 第 j 项 的 第 1 项。
                j 为 j 加 1。
            完。
            解析结果 为 做 解析命令行 用 命令列表 的 第 i 项。
            做 执行命令 用 解析结果。
            做 sys_exit 用 0。
        完。
        做 追加 用 子进程列表、pid。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 管道列表：
        做 sys_close 用 管道列表 的 第 i 项 的 第 0 项。
        做 sys_close 用 管道列表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    可变 最后退出码 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 子进程列表：
        状态 为 做 sys_waitpid 用 子进程列表 的 第 i 项。
        若 i 等于 做 计数 用 子进程列表 减 1：
            最后退出码 为 状态。
        完。
        i 为 i 加 1。
    完。
    返还 最后退出码。
完。

分割管道 接受 命令行：
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 引号内 为 假。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 命令行：
        c 为 做 字符串取字符 用 命令行、i。
        若 c 等于 「'」 或者 c 等于 「\"」：
            引号内 为 非 引号内。
        否则若 c 等于 「|」 并且 非 引号内：
            做 追加 用 结果、做 字符串_去首尾空白 用 当前。
            当前 为 「」。
            i 为 i 加 1。
            继续。
        完。
        当前 为 当前 加 c。
        i 为 i 加 1。
    完。
    若 做 字符串长度 用 做 字符串_去首尾空白 用 当前 大于 0：
        做 追加 用 结果、做 字符串_去首尾空白 用 当前。
    完。
    返还 结果。
完。

包含管道 接受 命令行：
    可变 i 为 0。
    可变 引号内 为 假。
    只要 i 小于 做 字符串长度 用 命令行：
        c 为 做 字符串取字符 用 命令行、i。
        若 c 等于 「'」 或者 c 等于 「\"」：
            引号内 为 非 引号内。
        完。
        若 c 等于 「|」 并且 非 引号内：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

本章 收束。
