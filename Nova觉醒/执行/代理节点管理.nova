《代理节点管理》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·整数转字符串》。

引用《.·伪装模板》。

引用《..·外联·URL解析辅助》。

IP隐匿_添加代理 接受 类型、地址、端口、用户名、密码、国家、是住宅：
    节点 为 新建 代理节点。
    节点 的 ID 为 做 计数 用 IP_代理池 的 节点列表 加 1。
    节点 的 类型 为 类型。
    节点 的 地址 为 地址。
    节点 的 端口 为 端口。
    节点 的 用户名 为 用户名。
    节点 的 密码 为 密码。
    节点 的 国家 为 国家。
    节点 的 状态 为 代理状态_可用。
    节点 的 延迟 为 0。
    节点 的 成功率 为 10000。
    节点 的 使用次数 为 0。
    节点 的 带宽 为 0。
    节点 的 是否住宅 为 是住宅。
    节点 的 风险评分 为 0。
    做 追加 用 IP_代理池 的 节点列表、节点。
    做 IP隐匿_更新国家索引 用 国家、节点 的 ID。
    返还 节点 的 ID。
完。

IP隐匿_更新国家索引 接受 国家、节点ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 IP_代理池 的 国家索引：
        若 IP_代理池 的 国家索引 的 第 i 项 的 第 0 项 等于 国家：
            做 追加 用 IP_代理池 的 国家索引 的 第 i 项 的 第 1 项、节点ID。
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 IP_代理池 的 国家索引、一组：国家、一组：节点ID。
完。

IP隐匿_批量添加 接受 代理列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 代理列表：
        p 为 代理列表 的 第 i 项。
        做 IP隐匿_添加代理 用 p 的 第 0 项、p 的 第 1 项、p 的 第 2 项、p 的 第 3 项、p 的 第 4 项、p 的 第 5 项、p 的 第 6 项。
        i 为 i 加 1。
    完。
    做 内核信息 用 「IP隐匿: 批量导入 」 加 做 整数转字符串 用 做 计数 用 代理列表 加 「 个代理节点」。
完。

IP隐匿_设置国家 接受 国家代码：
    IP_配置 的 目标国家 为 国家代码。
    做 指纹伪装_设置国家 用 国家代码。
    IP_域名映射 为 一组：（空）。
    做 内核信息 用 「IP隐匿: 目标国家设置为 」 加 国家代码。
完。

IP隐匿_选择代理 接受 目标域名：
    若 非 IP_配置 的 启用代理：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 IP_配置 的 排除域名：
        若 做 字符串查找 用 目标域名、IP_配置 的 排除域名 的 第 i 项 大于或等于 0：
            返还 空。
        完。
        i 为 i 加 1。
    完。
    若 IP_配置 的 同域保持IP：
        i 为 0。
        只要 i 小于 做 计数 用 IP_域名映射：
            若 IP_域名映射 的 第 i 项 的 第 0 项 等于 目标域名：
                节点 为 做 IP隐匿_获取节点 用 IP_域名映射 的 第 i 项 的 第 1 项。
                若 节点 不等于 空 并且 节点 的 状态 等于 代理状态_可用：
                    返还 节点。
                完。
            完。
            i 为 i 加 1。
        完。
    完。
    候选 为 做 IP隐匿_获取国家节点 用 IP_配置 的 目标国家。
    若 做 计数 用 候选 等于 0：
        候选 为 IP_代理池 的 节点列表。
    完。
    可变 最佳 为 空。
    可变 最高分 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 候选：
        节点 为 候选 的 第 i 项。
        若 节点 的 状态 不等于 代理状态_可用：
            i 为 i 加 1。
            继续。
        完。
        若 IP_配置 的 仅住宅IP 并且 非 节点 的 是否住宅：
            i 为 i 加 1。
            继续。
        完。
        分数 为 节点 的 成功率 减 节点 的 延迟 除以 10 减 节点 的 风险评分 乘 50 减 节点 的 使用次数。
        若 分数 大于 最高分 或者 最佳 等于 空：
            最高分 为 分数。
            最佳 为 节点。
        完。
        i 为 i 加 1。
    完。
    若 最佳 不等于 空：
        若 IP_配置 的 同域保持IP：
            做 追加 用 IP_域名映射、一组：目标域名、最佳 的 ID。
        完。
        最佳 的 使用次数 为 最佳 的 使用次数 加 1。
    完。
    IP_总请求数 为 IP_总请求数 加 1。
    返还 最佳。
完。

IP隐匿_获取节点 接受 节点ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 IP_代理池 的 节点列表：
        若 IP_代理池 的 节点列表 的 第 i 项 的 ID 等于 节点ID：
            返还 IP_代理池 的 节点列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

IP隐匿_获取国家节点 接受 国家：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 IP_代理池 的 节点列表：
        若 IP_代理池 的 节点列表 的 第 i 项 的 国家 等于 国家：
            做 追加 用 结果、IP_代理池 的 节点列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

本章 收束。
