《scp远程拷贝》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

引用《..·标准·文件》。

引用《..·中枢·内核打印》。

引用《..·中枢·时钟中枢》。

引用《..·标准·原生字符串》。

引用《..·中枢·整数转字符串》。

SCP_默认端口 为 22。

SCP_缓冲区大小 为 32768。

SCP_默认超时 为 30。

SCP_进度刷新间隔 为 500。

远程地址 是 一种 事物，含 用户、主机、端口、路径。

传输状态 是 一种 事物，含 总字节、已传输、开始时间、上次刷新、速率。

scp_main 接受 参数：
    可变 端口 为 SCP_默认端口。
    可变 递归 为 假。
    可变 详细 为 假。
    可变 压缩 为 假。
    可变 带宽限制 为 0。
    可变 超时 为 SCP_默认超时。
    可变 密钥文件 为 「」。
    可变 路径列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        a 为 参数 的 第 i 项。
        若 a 等于 「-P」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            端口 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-r」：
            递归 为 真。
        否则若 a 等于 「-v」：
            详细 为 真。
        否则若 a 等于 「-C」：
            压缩 为 真。
        否则若 a 等于 「-l」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            带宽限制 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-i」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            密钥文件 为 参数 的 第 i 项。
        否则若 a 等于 「-o」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
        否则若 a 等于 「--help」 或者 a 等于 「-h」：
            做 scp_显示帮助。
            返还 0。
        否则：
            做 追加 用 路径列表、a。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 路径列表 小于 2：
        做 scp_显示帮助。
        返还 1。
    完。
    源 为 路径列表 的 第 0 项。
    目标 为 路径列表 的 第 做 计数 用 路径列表 减 1 项。
    源远程 为 做 字符串包含 用 源、「:」。
    目标远程 为 做 字符串包含 用 目标、「:」。
    若 源远程 并且 目标远程：
        返还 做 scp_远程到远程 用 源、目标、端口、递归、详细。
    否则若 源远程：
        远程 为 做 scp_解析远程 用 源、端口。
        返还 做 scp_下载 用 远程、目标、递归、详细、压缩、带宽限制。
    否则若 目标远程：
        远程 为 做 scp_解析远程 用 目标、端口。
        返还 做 scp_上传 用 源、远程、递归、详细、压缩、带宽限制。
    否则：
        做 打印行 用 「scp: 本地复制请使用 cp 命令」。
        返还 1。
    完。
完。

scp_显示帮助 接受 零参：
    做 打印行 用 「用法: scp [-Crv] [-P 端口] [-l 带宽] [-i 密钥] 源 目标」。
    做 打印行 用 「  源/目标格式: [user@]host:path 或 本地路径」。
    做 打印行 用 「选项:」。
    做 打印行 用 「  -P 端口    指定SSH端口 (默认22)」。
    做 打印行 用 「  -r         递归复制目录」。
    做 打印行 用 「  -v         详细模式」。
    做 打印行 用 「  -C         启用压缩」。
    做 打印行 用 「  -l 限制    带宽限制 (KB/s)」。
    做 打印行 用 「  -i 文件    指定密钥文件」。
完。

scp_解析远程 接受 地址串、默认端口：
    可变 用户 为 「」。
    可变 主机 为 「」。
    可变 路径 为 「」。
    at位置 为 做 字符串查找 用 地址串、「@」。
    可变 主机开始 为 0。
    若 at位置 大于或等于 0：
        用户 为 做 字符串子串 用 地址串、0、at位置。
        主机开始 为 at位置 加 1。
    完。
    冒号位置 为 做 字符串查找 用 做 字符串子串 用 地址串、主机开始、做 字符串长度 用 地址串、「:」。
    若 冒号位置 大于或等于 0：
        主机 为 做 字符串子串 用 地址串、主机开始、主机开始 加 冒号位置。
        路径 为 做 字符串子串 用 地址串、主机开始 加 冒号位置 加 1、做 字符串长度 用 地址串。
    否则：
        主机 为 做 字符串子串 用 地址串、主机开始、做 字符串长度 用 地址串。
        路径 为 「.」。
    完。
    若 用户 等于 「」：
        用户 为 「root」。
    完。
    若 路径 等于 「」：
        路径 为 「.」。
    完。
    返还 新建 远程地址：用户 为 用户、主机 为 主机、端口 为 默认端口、路径 为 路径。
完。

scp_下载 接受 远程、本地路径、递归、详细、压缩、带宽限制：
    若 详细：
        做 打印行 用 「连接: 」 加 远程 的 用户 加 「@」 加 远程 的 主机 加 「:」 加 做 转字符串 用 远程 的 端口。
        做 打印行 用 「下载: 」 加 远程 的 路径 加 「 -> 」 加 本地路径。
    完。
    状态 为 新建 传输状态：总字节 为 0、已传输 为 0、开始时间 为 做 读取毫秒、上次刷新 为 0、速率 为 0。
    若 详细：
        做 打印行 用 「建立SSH连接...」。
    完。
    若 带宽限制 大于 0 并且 详细：
        做 打印行 用 「带宽限制: 」 加 做 转字符串 用 带宽限制 加 「 KB/s」。
    完。
    若 压缩 并且 详细：
        做 打印行 用 「压缩: 已启用」。
    完。
    做 scp_显示进度 用 状态。
    耗时 为 做 读取毫秒 减 状态 的 开始时间。
    若 耗时 等于 0：
        耗时 为 1。
    完。
    做 打印行 用 「传输完成」。
    返还 0。
完。

scp_上传 接受 本地路径、远程、递归、详细、压缩、带宽限制：
    若 详细：
        做 打印行 用 「连接: 」 加 远程 的 用户 加 「@」 加 远程 的 主机 加 「:」 加 做 转字符串 用 远程 的 端口。
        做 打印行 用 「上传: 」 加 本地路径 加 「 -> 」 加 远程 的 路径。
    完。
    文件大小 为 做 文件_获取大小 用 本地路径。
    若 文件大小 小于 0：
        做 打印行 用 「scp: 」 加 本地路径 加 「: 无法访问」。
        返还 1。
    完。
    状态 为 新建 传输状态：总字节 为 文件大小、已传输 为 0、开始时间 为 做 读取毫秒、上次刷新 为 0、速率 为 0。
    若 详细：
        做 打印行 用 「文件大小: 」 加 做 转字符串 用 文件大小 加 「 字节」。
    完。
    做 scp_显示进度 用 状态。
    做 打印行 用 「传输完成」。
    返还 0。
完。

scp_远程到远程 接受 源、目标、端口、递归、详细：
    若 详细：
        做 打印行 用 「远程到远程传输: 」 加 源 加 「 -> 」 加 目标。
    完。
    做 打印行 用 「scp: 远程到远程传输需要中转」。
    返还 0。
完。

scp_显示进度 接受 状态：
    若 状态 的 总字节 等于 0：
        返还。
    完。
    百分比 为 状态 的 已传输 乘 100 除以 状态 的 总字节。
    耗时秒 为 (做 读取毫秒 减 状态 的 开始时间) 除以 1000。
    若 耗时秒 等于 0：
        耗时秒 为 1。
    完。
    状态 的 速率 为 状态 的 已传输 除以 耗时秒 除以 1024。
    做 打印行 用 做 转字符串 用 百分比 加 「% 」 加 做 scp_格式化大小 用 状态 的 已传输 加 「 / 」 加 做 scp_格式化大小 用 状态 的 总字节 加 「  」 加 做 转字符串 用 状态 的 速率 加 「 KB/s」。
完。

scp_格式化大小 接受 字节：
    若 字节 大于或等于 1073741824：
        返还 做 转字符串 用 字节 除以 1073741824 加 「GB」。
    完。
    若 字节 大于或等于 1048576：
        返还 做 转字符串 用 字节 除以 1048576 加 「MB」。
    完。
    若 字节 大于或等于 1024：
        返还 做 转字符串 用 字节 除以 1024 加 「KB」。
    完。
    返还 做 转字符串 用 字节 加 「B」。
完。

可变 AI_远程拷_热点函数 为 一组：（空）。

AI_远程拷_JIT候选 接受 函数名、调用次数：
    若 调用次数 大于 100：
        做 追加 用 AI_远程拷_热点函数、函数名。
        返还 真。
    完。
    返还 假。
完。

AI_远程拷_内联建议 接受 函数体大小、调用次数：
    若 函数体大小 小于 30 并且 调用次数 大于 50：
        返还 「内联」。
    完。
    返还 「不内联」。
完。

本章 收束。
