《伪装模板》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·整数转字符串》。

引用《.·伪装初始化》。

指纹_确定性随机 接受 种子：
    可变 x 为 种子。
    若 x 等于 0：
        x 为 0x9E3779B9。
    完。
    x 为 x 位异或 x 左移 13。
    x 为 x 位异或 x 右移 7。
    x 为 x 位异或 x 左移 17。
    若 x 小于 0：
        x 为 0 减 x。
    完。
    返还 x。
完。

指纹伪装_刷新 接受 零参：
    指纹_PRNG种子 为 做 指纹_确定性随机 用 指纹_PRNG种子。
    指纹_拦截次数 为 0。
    返还 真。
完。

指纹伪装_切换模板 接受 模板名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 指纹_模板库：
        若 指纹_模板库 的 第 i 项 的 名称 等于 模板名：
            指纹_当前配置 为 指纹_模板库 的 第 i 项 的 配置。
            指纹_PRNG种子 为 做 指纹_确定性随机 用 指纹_PRNG种子。
            做 内核信息 用 「指纹伪装: 切换到 」 加 模板名。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

指纹伪装_设置国家 接受 国家代码：
    若 国家代码 等于 「US」：
        指纹_当前配置 的 时区名称 为 「America/New_York」。
        指纹_当前配置 的 时区偏移 为 300。
        指纹_当前配置 的 语言 为 「en-US」。
        指纹_当前配置 的 语言列表 为 一组：「en-US」、「en」。
    否则若 国家代码 等于 「CN」：
        指纹_当前配置 的 时区名称 为 「Asia/Shanghai」。
        指纹_当前配置 的 时区偏移 为 负 480。
        指纹_当前配置 的 语言 为 「zh-CN」。
        指纹_当前配置 的 语言列表 为 一组：「zh-CN」、「zh」、「en-US」、「en」。
    否则若 国家代码 等于 「JP」：
        指纹_当前配置 的 时区名称 为 「Asia/Tokyo」。
        指纹_当前配置 的 时区偏移 为 负 540。
        指纹_当前配置 的 语言 为 「ja」。
        指纹_当前配置 的 语言列表 为 一组：「ja」、「en-US」、「en」。
    否则若 国家代码 等于 「UK」：
        指纹_当前配置 的 时区名称 为 「Europe/London」。
        指纹_当前配置 的 时区偏移 为 0。
        指纹_当前配置 的 语言 为 「en-GB」。
        指纹_当前配置 的 语言列表 为 一组：「en-GB」、「en」。
    否则若 国家代码 等于 「DE」：
        指纹_当前配置 的 时区名称 为 「Europe/Berlin」。
        指纹_当前配置 的 时区偏移 为 负 60。
        指纹_当前配置 的 语言 为 「de」。
        指纹_当前配置 的 语言列表 为 一组：「de」、「en-US」、「en」。
    否则若 国家代码 等于 「KR」：
        指纹_当前配置 的 时区名称 为 「Asia/Seoul」。
        指纹_当前配置 的 时区偏移 为 负 540。
        指纹_当前配置 的 语言 为 「ko」。
        指纹_当前配置 的 语言列表 为 一组：「ko」、「en-US」、「en」。
    完。
    做 内核信息 用 「指纹伪装: 国家切换到 」 加 国家代码。
完。

指纹伪装_设置屏幕分辨率 接受 宽、高：
    指纹_当前配置 的 屏幕宽 为 宽。
    指纹_当前配置 的 屏幕高 为 高。
    指纹_当前配置 的 可用宽 为 宽。
    指纹_当前配置 的 可用高 为 高 减 48。
    做 内核信息 用 「指纹伪装: 分辨率切换到 」 加 做 整数转字符串 用 宽 加 「x」 加 做 整数转字符串 用 高。
    返还 真。
完。

本章 收束。
