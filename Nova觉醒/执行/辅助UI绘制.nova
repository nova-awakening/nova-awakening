《辅助UI绘制》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·扫描磁盘》。

引用《..·中枢·硬链接》。

引用《..·中枢·内存回收》。

绘制_顶部栏 接受 零参：
    做 绘制文字 用 30、20、「🌟 NovaOS」、24、0x4FC3F7。
    做 绘制文字 用 30、50、「AI原生操作系统 安装向导」、12、0x606080。
    步骤名 为 一组：「欢迎」、「语言」、「磁盘」、「分区」、「用户」、「AI」、「安装」、「完成」。
    可变 i 为 0。
    起始X 为 屏幕宽 除以 2 减 350。
    只要 i 小于 8：
        sx 为 起始X 加 i 乘 100。
        活跃 为 i 等于 安装 的 当前步骤。
        完成 为 i 小于 安装 的 当前步骤。
        颜色 为 0x404060。
        若 活跃：
            颜色 为 0x4FC3F7。
        否则若 完成：
            颜色 为 0x66BB6A。
        完。
        做 填充圆 用 sx 加 10、40、8、颜色。
        做 绘制文字 用 sx 减 5、55、步骤名 的 第 i 项、10、颜色。
        若 i 小于 7：
            线色 为 做 如果选 用 完成、0x66BB6A、0x303050。
            做 填充矩形 用 sx 加 20、38、78、2、线色。
        完。
        i 为 i 加 1。
    完。
完。

绘制_底部导航 接受 x、y、宽：
    若 安装 的 当前步骤 大于 0：
        做 绘制_按钮 用 x、y、120、40、「← 上一步」、0x606080。
    完。
    若 安装 的 当前步骤 小于 步骤_安装：
        文字 为 做 如果选 用 安装 的 当前步骤 等于 步骤_AI设置、「开始安装」、「下一步 →」。
        做 绘制_发光按钮 用 x 加 宽 减 140、y、140、40、文字。
    完。
完。

绘制_AI全息人形 接受 中心X、y、大小：
    颜色 为 0x4FC3F7。
    做 绘制_发光圆 用 中心X、y 加 大小 除以 4、大小 除以 4、颜色。
    做 绘制_发光线 用 中心X、y 加 大小 除以 2、中心X 减 大小 除以 3、y 加 大小、颜色。
    做 绘制_发光线 用 中心X、y 加 大小 除以 2、中心X 加 大小 除以 3、y 加 大小、颜色。
    做 绘制_发光椭圆 用 中心X、y 加 大小 加 10、大小 除以 2、大小 除以 6、颜色。
完。

绘制_进度条 接受 x、y、宽、高、百分比、颜色：
    做 填充矩形 用 x、y、宽、高、0x1A1A2E。
    填充宽 为 宽 乘 百分比 除以 100。
    做 填充矩形 用 x、y、填充宽、高、颜色。
    若 填充宽 大于 2：
        做 填充矩形 用 x 加 填充宽 减 2、y、4、高、做 颜色_变亮 用 颜色、80。
    完。
完。

绘制_分区预览 接受 x、y、宽、高：
    EFI宽 为 宽 乘 5 除以 100。
    Swap宽 为 宽 乘 8 除以 100。
    根宽 为 宽 减 EFI宽 减 Swap宽。
    做 填充矩形 用 x、y、EFI宽、高、0xFFA726。
    做 绘制文字 用 x 加 2、y 加 8、「EFI」、10、0x000000。
    做 填充矩形 用 x 加 EFI宽 加 2、y、根宽 减 4、高、0x4FC3F7。
    做 绘制文字 用 x 加 EFI宽 加 10、y 加 8、「/ (根分区)」、10、0x000000。
    做 填充矩形 用 x 加 EFI宽 加 根宽、y、Swap宽、高、0x66BB6A。
    做 绘制文字 用 x 加 EFI宽 加 根宽 加 2、y 加 8、「swap」、10、0x000000。
完。

绘制_输入框 接受 x、y、宽、占位文字、值：
    做 填充矩形 用 x、y、宽、40、做 颜色 用 25、25、40、200。
    做 绘制_发光边框 用 空、x、y、宽、40、8。
    若 做 字符串长度 用 值 大于 0：
        做 绘制文字 用 x 加 12、y 加 12、值、14、0xE0E0F0。
    否则：
        做 绘制文字 用 x 加 12、y 加 12、占位文字、14、0x505070。
    完。
完。

绘制_特性图标 接受 x、y、图标、标题、描述：
    做 绘制文字 用 x 加 20、y、图标、28、0x4FC3F7。
    做 绘制文字 用 x 加 5、y 加 35、标题、14、0xC0C0D0。
    做 绘制文字 用 x 加 5、y 加 55、描述、11、0x808090。
完。

获取安装操作 接受 进度：
    若 进度 小于 10：
        返还 「正在创建分区...」。
    完。
    若 进度 小于 20：
        返还 「正在格式化分区...」。
    完。
    若 进度 小于 40：
        返还 「正在复制系统文件...」。
    完。
    若 进度 小于 55：
        返还 「正在安装内核...」。
    完。
    若 进度 小于 65：
        返还 「正在配置引导加载器...」。
    完。
    若 进度 小于 75：
        返还 「正在初始化AI子系统...」。
    完。
    若 进度 小于 85：
        返还 「正在下载AI基础权重...」。
    完。
    若 进度 小于 90：
        返还 「正在配置网络和驱动...」。
    完。
    若 进度 小于 95：
        返还 「正在创建用户账户...」。
    完。
    若 进度 小于 100：
        返还 「正在生成个性化桌面...」。
    完。
    返还 「安装完成!」。
完。

AI模式名称 接受 模式：
    若 模式 等于 0：
        返还 「云端优先」。
    完。
    若 模式 等于 1：
        返还 「本地优先」。
    完。
    返还 「智能混合」。
完。

更新进度 接受 进度、消息：
    安装 的 安装进度 为 进度。
    做 绘制_安装界面。
完。

本章 收束。
