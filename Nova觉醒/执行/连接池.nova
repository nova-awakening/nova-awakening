《连接池》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内容可见性》。

引用《.·平台时间》。

引用《..·中枢·字符串替换》。

引用《.·网络创建》。

引用《..·外联·TLS创建》。

连接池_默认容量 为 256。

连接池_最大容量 为 2048。

连接池_熔断槽位 为 128。

连接池_预连接队列上限 为 256。

连接状态_空闲 为 0。

连接状态_使用中 为 1。

连接状态_关闭中 为 2。

连接状态_故障 为 3。

协议_TCP 为 1。

协议_TLS 为 2。

协议_QUIC 为 3。

TLS_版本_1_2 为 0x0303。

TLS_版本_1_3 为 0x0304。

TLS_默认最小版本 为 TLS_版本_1_2。

空闲超时_默认毫秒 为 45000。

生命周期_默认毫秒 为 300000。

心跳间隔_默认毫秒 为 15000。

连接重试_最大次数 为 3。

连接重试_基础退避毫秒 为 40。

连接建立超时毫秒 为 4000。

风险阈值_高 为 0.72。

AI预连接_置信阈值 为 0.68。

AI预连接_最多创建 为 3。

淘汰策略_LRU 为 1。

淘汰策略_LFU 为 2。

淘汰策略_风险优先 为 3。

淘汰策略_LCAP综合 为 4。

LCAP_权重_热度 为 0.34。

LCAP_权重_复用 为 0.29。

LCAP_权重_风险 为 0.23。

LCAP_权重_空闲 为 0.14。

连接池统计 是 一种 事物，含 创建总数、复用命中数、复用未命中数、放回总数、主动关闭数、淘汰关闭数、超时关闭数、健康检查失败数、熔断拒绝数、TLS握手成功数、TLS握手失败数、证书验证失败数、会话恢复成功数、AI预连接触发数、AI预连接成功数、AI预连接失败数、当前活跃连接数、当前空闲连接数、总发送字节、总接收字节、总连接时延、最大连接时延。

连接池 是 一种 事物，含 连接、主机、主机哈希、端口、协议、ALPN、状态、活跃、已握手、正在使用、创建时间、上次使用时间、最后心跳时间、空闲超时、最大寿命、往返时延估计、复用计数、连续失败次数、风险评分、热度评分、发送字节、接收字节、TLS版本、会话票据、证书指纹、熔断主机哈希、熔断失败次数、熔断开启、熔断截止时间、预连接主机、预连接端口、预连接协议、预连接优先级、预连接入队时间、预连接大小、大小、容量、自动创建、默认协议、默认空闲超时、默认最大寿命、默认心跳间隔、淘汰策略、最小TLS版本、统计。

连接请求 是 一种 事物，含 主机、端口、协议、ALPN、最小TLS版本、允许自动创建。

可变 全局连接池 为 空。

可变 全局连接池统计 为 新建 连接池统计。

连接池_哈希主机端口协议 接受 主机、端口、协议、ALPN：
    可变 h 为 1469598103934665603。
    可变 i 为 0。
    只要 i 小于 做 字符串_长度 用 主机：
        h 为 h 位异或 做 字符串_读字节 用 主机、i。
        h 为 h 乘 1099511628211。
        i 为 i 加 1。
    完。
    h 为 h 位异或 端口。
    h 为 h 乘 1099511628211。
    h 为 h 位异或 协议。
    h 为 h 乘 1099511628211。
    i 为 0。
    只要 i 小于 做 字符串_长度 用 ALPN：
        h 为 h 位异或 做 字符串_读字节 用 ALPN、i。
        h 为 h 乘 1099511628211。
        i 为 i 加 1。
    完。
    返还 h。
完。

连接池_当前时间 接受 零参：
    返还 做 平台_获取高精度时间。
完。

连接池_是否过期 接受 创建时间、最大寿命、当前时间：
    若 最大寿命 小于或等于 0：
        返还 假。
    完。
    返还 当前时间 减 创建时间 大于或等于 最大寿命。
完。

连接池_是否空闲超时 接受 上次使用时间、空闲超时、当前时间：
    若 空闲超时 小于或等于 0：
        返还 假。
    完。
    返还 当前时间 减 上次使用时间 大于或等于 空闲超时。
完。

连接池_最小值 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

连接池_最大值 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

连接池_字符串相等 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 假。
    完。
    返还 做 字符串_相等 用 a、b。
完。

连接池_创建 接受 容量：
    可变 cap 为 容量。
    若 cap 小于或等于 0：
        cap 为 连接池_默认容量。
    完。
    若 cap 大于 连接池_最大容量：
        cap 为 连接池_最大容量。
    完。
    池 为 新建 连接池。
    池 的 容量 为 cap。
    池 的 大小 为 0。
    池 的 自动创建 为 真。
    池 的 默认协议 为 协议_TLS。
    池 的 默认空闲超时 为 空闲超时_默认毫秒。
    池 的 默认最大寿命 为 生命周期_默认毫秒。
    池 的 默认心跳间隔 为 心跳间隔_默认毫秒。
    池 的 淘汰策略 为 淘汰策略_LCAP综合。
    池 的 最小TLS版本 为 TLS_默认最小版本。
    池 的 预连接大小 为 0。
    池 的 连接 为 做 创建数组 用 cap、0。
    池 的 主机 为 做 创建数组 用 cap、「」。
    池 的 主机哈希 为 做 创建数组 用 cap、0。
    池 的 端口 为 做 创建数组 用 cap、0。
    池 的 协议 为 做 创建数组 用 cap、协议_TLS。
    池 的 ALPN 为 做 创建数组 用 cap、「h2」。
    池 的 状态 为 做 创建数组 用 cap、连接状态_空闲。
    池 的 活跃 为 做 创建数组 用 cap、假。
    池 的 已握手 为 做 创建数组 用 cap、假。
    池 的 正在使用 为 做 创建数组 用 cap、假。
    池 的 创建时间 为 做 创建数组 用 cap、0。
    池 的 上次使用时间 为 做 创建数组 用 cap、0。
    池 的 最后心跳时间 为 做 创建数组 用 cap、0。
    池 的 空闲超时 为 做 创建数组 用 cap、空闲超时_默认毫秒。
    池 的 最大寿命 为 做 创建数组 用 cap、生命周期_默认毫秒。
    池 的 往返时延估计 为 做 创建数组 用 cap、0。
    池 的 复用计数 为 做 创建数组 用 cap、0。
    池 的 连续失败次数 为 做 创建数组 用 cap、0。
    池 的 风险评分 为 做 创建数组 用 cap、0.0。
    池 的 热度评分 为 做 创建数组 用 cap、0.0。
    池 的 发送字节 为 做 创建数组 用 cap、0。
    池 的 接收字节 为 做 创建数组 用 cap、0。
    池 的 TLS版本 为 做 创建数组 用 cap、TLS_版本_1_2。
    池 的 会话票据 为 做 创建数组 用 cap、「」。
    池 的 证书指纹 为 做 创建数组 用 cap、「」。
    池 的 熔断主机哈希 为 做 创建数组 用 连接池_熔断槽位、0。
    池 的 熔断失败次数 为 做 创建数组 用 连接池_熔断槽位、0。
    池 的 熔断开启 为 做 创建数组 用 连接池_熔断槽位、假。
    池 的 熔断截止时间 为 做 创建数组 用 连接池_熔断槽位、0。
    池 的 预连接主机 为 做 创建数组 用 连接池_预连接队列上限、「」。
    池 的 预连接端口 为 做 创建数组 用 连接池_预连接队列上限、0。
    池 的 预连接协议 为 做 创建数组 用 连接池_预连接队列上限、协议_TLS。
    池 的 预连接优先级 为 做 创建数组 用 连接池_预连接队列上限、0.0。
    池 的 预连接入队时间 为 做 创建数组 用 连接池_预连接队列上限、0。
    返还 池。
完。

连接池_初始化全局 接受 容量：
    全局连接池 为 做 连接池_创建 用 容量。
    返还 全局连接池。
完。

连接池_销毁 接受 池：
    若 池 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 池 的 大小：
        若 池 的 连接 的 第 i 项 不等于 0：
            做 安全_关闭 用 池 的 连接 的 第 i 项。
            池 的 连接 的 第 i 项 为 0。
        完。
        池 的 活跃 的 第 i 项 为 假。
        i 为 i 加 1。
    完。
    池 的 大小 为 0。
完。

连接池_是否可复用 接受 池、i、主机、主机哈希、端口、协议、ALPN、最小TLS版本：
    若 非 池 的 活跃 的 第 i 项：
        返还 假。
    完。
    若 池 的 正在使用 的 第 i 项：
        返还 假。
    完。
    若 池 的 状态 的 第 i 项 等于 连接状态_故障 或者 池 的 状态 的 第 i 项 等于 连接状态_关闭中：
        返还 假。
    完。
    若 池 的 主机哈希 的 第 i 项 不等于 主机哈希：
        返还 假。
    完。
    若 池 的 端口 的 第 i 项 不等于 端口：
        返还 假。
    完。
    若 池 的 协议 的 第 i 项 不等于 协议：
        返还 假。
    完。
    若 非 做 连接池_字符串相等 用 池 的 ALPN 的 第 i 项、ALPN：
        返还 假。
    完。
    若 池 的 TLS版本 的 第 i 项 小于 最小TLS版本：
        返还 假。
    完。
    若 非 池 的 已握手 的 第 i 项：
        返还 假。
    完。
    返还 真。
完。

连接池_LCAP评分 接受 池、i、当前时间：
    空闲时长 为 当前时间 减 池 的 上次使用时间 的 第 i 项。
    可变 idle_norm 为 空闲时长 除以 60000.0。
    若 idle_norm 大于 1.0：
        idle_norm 为 1.0。
    完。
    可变 reuse_norm 为 池 的 复用计数 的 第 i 项 除以 64.0。
    若 reuse_norm 大于 1.0：
        reuse_norm 为 1.0。
    完。
    可变 heat_norm 为 池 的 热度评分 的 第 i 项。
    若 heat_norm 小于 0.0：
        heat_norm 为 0.0。
    完。
    若 heat_norm 大于 1.0：
        heat_norm 为 1.0。
    完。
    可变 risk_norm 为 池 的 风险评分 的 第 i 项。
    若 risk_norm 小于 0.0：
        risk_norm 为 0.0。
    完。
    若 risk_norm 大于 1.0：
        risk_norm 为 1.0。
    完。
    返还 LCAP_权重_热度 乘 heat_norm 加 LCAP_权重_复用 乘 reuse_norm 减 LCAP_权重_风险 乘 risk_norm 减 LCAP_权重_空闲 乘 idle_norm。
完。

连接池_健康检查单连接 接受 池、i、当前时间：
    若 非 池 的 活跃 的 第 i 项：
        返还 假。
    完。
    若 池 的 连接 的 第 i 项 等于 0：
        返还 假。
    完。
    若 做 连接池_是否过期 用 池 的 创建时间 的 第 i 项、池 的 最大寿命 的 第 i 项、当前时间：
        返还 假。
    完。
    若 做 连接池_是否空闲超时 用 池 的 上次使用时间 的 第 i 项、池 的 空闲超时 的 第 i 项、当前时间：
        返还 假。
    完。
    若 非 做 传控_已连接 用 池 的 连接 的 第 i 项 的 tcp：
        返还 假。
    完。
    返还 真。
完。

连接池_获取 接受 池、主机、端口：
    req 为 新建 连接请求：主机 为 主机、端口 为 端口、协议 为 协议_TLS、ALPN 为 「h2」、最小TLS版本 为 TLS_默认最小版本、允许自动创建 为 真。
    返还 做 连接池_获取扩展 用 池、req。
完。

连接池_获取扩展 接受 池、req：
    若 池 等于 0：
        返还 0。
    完。
    当前时间 为 做 连接池_当前时间。
    主机哈希 为 做 连接池_哈希主机端口协议 用 req 的 主机、req 的 端口、req 的 协议、req 的 ALPN。
    若 非 做 连接池_熔断允许访问 用 池、主机哈希、当前时间：
        池 的 统计 的 熔断拒绝数 为 池 的 统计 的 熔断拒绝数 加 1。
        返还 0。
    完。
    可变 最佳索引 为 负 1。
    可变 最佳分数 为 负 999999.0。
    可变 i 为 0。
    只要 i 小于 池 的 大小：
        若 做 连接池_是否可复用 用 池、i、req 的 主机、主机哈希、req 的 端口、req 的 协议、req 的 ALPN、req 的 最小TLS版本：
            若 做 连接池_健康检查单连接 用 池、i、当前时间：
                score 为 做 连接池_LCAP评分 用 池、i、当前时间。
                若 score 大于 最佳分数：
                    最佳分数 为 score。
                    最佳索引 为 i。
                完。
            否则：
                做 连接池_关闭索引 用 池、i、真、「健康检查失败」。
                池 的 统计 的 健康检查失败数 为 池 的 统计 的 健康检查失败数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最佳索引 大于或等于 0：
        池 的 正在使用 的 第 最佳索引 项 为 真。
        池 的 状态 的 第 最佳索引 项 为 连接状态_使用中。
        池 的 上次使用时间 的 第 最佳索引 项 为 当前时间。
        池 的 复用计数 的 第 最佳索引 项 为 池 的 复用计数 的 第 最佳索引 项 加 1。
        池 的 热度评分 的 第 最佳索引 项 为 做 连接池_最大值 用 池 的 热度评分 的 第 最佳索引 项、0.6。
        池 的 统计 的 复用命中数 为 池 的 统计 的 复用命中数 加 1。
        池 的 统计 的 当前活跃连接数 为 池 的 统计 的 当前活跃连接数 加 1。
        返还 池 的 连接 的 第 最佳索引 项。
    完。
    池 的 统计 的 复用未命中数 为 池 的 统计 的 复用未命中数 加 1。
    若 req 的 允许自动创建 并且 池 的 自动创建：
        tls 为 做 连接池_创建安全连接 用 池、req。
        若 tls 不等于 0：
            做 连接池_放回扩展 用 池、tls、req 的 主机、req 的 端口、req 的 协议、req 的 ALPN、req 的 最小TLS版本。
            返还 做 连接池_获取扩展 用 池、req。
        完。
    完。
    返还 0。
完。

连接池_放回 接受 池、tls、主机、端口：
    做 连接池_放回扩展 用 池、tls、主机、端口、协议_TLS、「h2」、TLS_默认最小版本。
完。

连接池_放回扩展 接受 池、tls、主机、端口、协议、ALPN、最小TLS版本：
    若 池 等于 0 或者 tls 等于 0：
        返还。
    完。
    当前时间 为 做 连接池_当前时间。
    主机哈希 为 做 连接池_哈希主机端口协议 用 主机、端口、协议、ALPN。
    可变 i 为 0。
    只要 i 小于 池 的 大小：
        若 池 的 连接 的 第 i 项 等于 tls：
            池 的 正在使用 的 第 i 项 为 假。
            池 的 状态 的 第 i 项 为 连接状态_空闲。
            池 的 上次使用时间 的 第 i 项 为 当前时间。
            池 的 活跃 的 第 i 项 为 真。
            池 的 已握手 的 第 i 项 为 真。
            池 的 热度评分 的 第 i 项 为 做 连接池_最大值 用 池 的 热度评分 的 第 i 项 乘 0.95 加 0.05、0.1。
            池 的 统计 的 放回总数 为 池 的 统计 的 放回总数 加 1。
            若 池 的 统计 的 当前活跃连接数 大于 0：
                池 的 统计 的 当前活跃连接数 为 池 的 统计 的 当前活跃连接数 减 1。
            完。
            池 的 统计 的 当前空闲连接数 为 池 的 统计 的 当前空闲连接数 加 1。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 池 的 大小 大于或等于 池 的 容量：
        做 连接池_触发淘汰 用 池、当前时间。
    完。
    若 池 的 大小 大于或等于 池 的 容量：
        做 连接池_淘汰最旧 用 池。
    完。
    若 池 的 大小 大于或等于 池 的 容量：
        做 安全_关闭 用 tls。
        返还。
    完。
    idx 为 池 的 大小。
    池 的 连接 的 第 idx 项 为 tls。
    池 的 主机 的 第 idx 项 为 主机。
    池 的 主机哈希 的 第 idx 项 为 主机哈希。
    池 的 端口 的 第 idx 项 为 端口。
    池 的 协议 的 第 idx 项 为 协议。
    池 的 ALPN 的 第 idx 项 为 ALPN。
    池 的 状态 的 第 idx 项 为 连接状态_空闲。
    池 的 活跃 的 第 idx 项 为 真。
    池 的 已握手 的 第 idx 项 为 真。
    池 的 正在使用 的 第 idx 项 为 假。
    池 的 创建时间 的 第 idx 项 为 当前时间。
    池 的 上次使用时间 的 第 idx 项 为 当前时间。
    池 的 最后心跳时间 的 第 idx 项 为 当前时间。
    池 的 空闲超时 的 第 idx 项 为 池 的 默认空闲超时。
    池 的 最大寿命 的 第 idx 项 为 池 的 默认最大寿命。
    池 的 往返时延估计 的 第 idx 项 为 0。
    池 的 复用计数 的 第 idx 项 为 0。
    池 的 连续失败次数 的 第 idx 项 为 0。
    池 的 风险评分 的 第 idx 项 为 0.0。
    池 的 热度评分 的 第 idx 项 为 0.5。
    池 的 发送字节 的 第 idx 项 为 0。
    池 的 接收字节 的 第 idx 项 为 0。
    池 的 TLS版本 的 第 idx 项 为 最小TLS版本。
    池 的 会话票据 的 第 idx 项 为 「」。
    池 的 证书指纹 的 第 idx 项 为 「」。
    池 的 大小 为 池 的 大小 加 1。
    池 的 统计 的 创建总数 为 池 的 统计 的 创建总数 加 1。
    池 的 统计 的 当前空闲连接数 为 池 的 统计 的 当前空闲连接数 加 1。
完。

连接池_创建安全连接 接受 池、req：
    开始 为 做 连接池_当前时间。
    主机哈希 为 做 连接池_哈希主机端口协议 用 req 的 主机、req 的 端口、req 的 协议、req 的 ALPN。
    可变 尝试 为 0。
    只要 尝试 小于 连接重试_最大次数：
        sock 为 做 传控_创建。
        若 sock 等于 0：
            尝试 为 尝试 加 1。
            继续。
        完。
        做 传控_设置超时 用 sock、连接建立超时毫秒。
        rc 为 做 传控_连接 用 sock、req 的 主机、req 的 端口。
        若 rc 不等于 0：
            做 传控_关闭 用 sock。
            尝试 为 尝试 加 1。
            做 连接池_退避 用 尝试。
            做 连接池_熔断记录失败 用 池、主机哈希。
            继续。
        完。
        tls 为 做 安全_创建 用 sock。
        若 tls 等于 0：
            做 传控_关闭 用 sock。
            尝试 为 尝试 加 1。
            做 连接池_退避 用 尝试。
            做 连接池_熔断记录失败 用 池、主机哈希。
            继续。
        完。
        做 安全_设置最小版本 用 tls、req 的 最小TLS版本。
        做 安全_设置ALPN 用 tls、req 的 ALPN。
        做 安全_启用会话恢复 用 tls、真。
        做 安全_设置主机名 用 tls、req 的 主机。
        握手成功 为 做 安全_握手 用 tls、req 的 主机。
        若 非 握手成功：
            池 的 统计 的 TLS握手失败数 为 池 的 统计 的 TLS握手失败数 加 1。
            做 安全_关闭 用 tls。
            尝试 为 尝试 加 1。
            做 连接池_退避 用 尝试。
            做 连接池_熔断记录失败 用 池、主机哈希。
            继续。
        完。
        池 的 统计 的 TLS握手成功数 为 池 的 统计 的 TLS握手成功数 加 1。
        若 非 做 连接池_验证证书 用 tls、req 的 主机：
            池 的 统计 的 证书验证失败数 为 池 的 统计 的 证书验证失败数 加 1。
            做 安全_关闭 用 tls。
            尝试 为 尝试 加 1。
            做 连接池_熔断记录失败 用 池、主机哈希。
            继续。
        完。
        做 连接池_熔断记录成功 用 池、主机哈希。
        耗时 为 做 连接池_当前时间 减 开始。
        池 的 统计 的 总连接时延 为 池 的 统计 的 总连接时延 加 耗时。
        池 的 统计 的 最大连接时延 为 做 连接池_最大值 用 池 的 统计 的 最大连接时延、耗时。
        返还 tls。
    完。
    返还 0。
完。

连接池_验证证书 接受 tls、主机：
    若 非 做 安全_证书链验证 用 tls：
        返还 假。
    完。
    若 非 做 安全_证书校验主机名 用 tls、主机：
        返还 假。
    完。
    若 做 安全_支持OCSP 用 tls：
        若 非 做 安全_OCSP校验 用 tls：
            返还 假。
        完。
    完。
    返还 真。
完。

连接池_退避 接受 尝试次数：
    backoff 为 连接重试_基础退避毫秒 左移 尝试次数。
    若 backoff 大于 800：
        backoff 为 800。
    完。
    做 平台_睡眠毫秒 用 backoff。
完。

连接池_关闭索引 接受 池、idx、是淘汰、原因：
    若 idx 小于 0 或者 idx 大于或等于 池 的 大小：
        返还。
    完。
    若 池 的 连接 的 第 idx 项 不等于 0：
        做 安全_关闭 用 池 的 连接 的 第 idx 项。
    完。
    若 是淘汰：
        池 的 统计 的 淘汰关闭数 为 池 的 统计 的 淘汰关闭数 加 1。
    否则：
        池 的 统计 的 主动关闭数 为 池 的 统计 的 主动关闭数 加 1。
    完。
    尾 为 池 的 大小 减 1。
    若 idx 不等于 尾：
        池 的 连接 的 第 idx 项 为 池 的 连接 的 第 尾 项。
        池 的 主机 的 第 idx 项 为 池 的 主机 的 第 尾 项。
        池 的 主机哈希 的 第 idx 项 为 池 的 主机哈希 的 第 尾 项。
        池 的 端口 的 第 idx 项 为 池 的 端口 的 第 尾 项。
        池 的 协议 的 第 idx 项 为 池 的 协议 的 第 尾 项。
        池 的 ALPN 的 第 idx 项 为 池 的 ALPN 的 第 尾 项。
        池 的 状态 的 第 idx 项 为 池 的 状态 的 第 尾 项。
        池 的 活跃 的 第 idx 项 为 池 的 活跃 的 第 尾 项。
        池 的 已握手 的 第 idx 项 为 池 的 已握手 的 第 尾 项。
        池 的 正在使用 的 第 idx 项 为 池 的 正在使用 的 第 尾 项。
        池 的 创建时间 的 第 idx 项 为 池 的 创建时间 的 第 尾 项。
        池 的 上次使用时间 的 第 idx 项 为 池 的 上次使用时间 的 第 尾 项。
        池 的 最后心跳时间 的 第 idx 项 为 池 的 最后心跳时间 的 第 尾 项。
        池 的 空闲超时 的 第 idx 项 为 池 的 空闲超时 的 第 尾 项。
        池 的 最大寿命 的 第 idx 项 为 池 的 最大寿命 的 第 尾 项。
        池 的 往返时延估计 的 第 idx 项 为 池 的 往返时延估计 的 第 尾 项。
        池 的 复用计数 的 第 idx 项 为 池 的 复用计数 的 第 尾 项。
        池 的 连续失败次数 的 第 idx 项 为 池 的 连续失败次数 的 第 尾 项。
        池 的 风险评分 的 第 idx 项 为 池 的 风险评分 的 第 尾 项。
        池 的 热度评分 的 第 idx 项 为 池 的 热度评分 的 第 尾 项。
        池 的 发送字节 的 第 idx 项 为 池 的 发送字节 的 第 尾 项。
        池 的 接收字节 的 第 idx 项 为 池 的 接收字节 的 第 尾 项。
        池 的 TLS版本 的 第 idx 项 为 池 的 TLS版本 的 第 尾 项。
        池 的 会话票据 的 第 idx 项 为 池 的 会话票据 的 第 尾 项。
        池 的 证书指纹 的 第 idx 项 为 池 的 证书指纹 的 第 尾 项。
    完。
    池 的 大小 为 池 的 大小 减 1。
    若 池 的 统计 的 当前空闲连接数 大于 0：
        池 的 统计 的 当前空闲连接数 为 池 的 统计 的 当前空闲连接数 减 1。
    完。
完。

安全_关闭 接受 tls：
    若 tls 等于 0：
        返还。
    完。
    做 安全_优雅关闭 用 tls。
    做 传控_关闭 用 tls 的 tcp。
    做 堆_释放 用 tls。
完。

连接池_巡检 接受 池：
    若 池 等于 0：
        返还。
    完。
    当前时间 为 做 连接池_当前时间。
    可变 i 为 0。
    只要 i 小于 池 的 大小：
        若 非 池 的 活跃 的 第 i 项：
            做 连接池_关闭索引 用 池、i、假、「非活跃」。
            继续。
        完。
        若 做 连接池_是否过期 用 池 的 创建时间 的 第 i 项、池 的 最大寿命 的 第 i 项、当前时间：
            池 的 统计 的 超时关闭数 为 池 的 统计 的 超时关闭数 加 1。
            做 连接池_关闭索引 用 池、i、假、「寿命超时」。
            继续。
        完。
        若 非 池 的 正在使用 的 第 i 项 并且 做 连接池_是否空闲超时 用 池 的 上次使用时间 的 第 i 项、池 的 空闲超时 的 第 i 项、当前时间：
            池 的 统计 的 超时关闭数 为 池 的 统计 的 超时关闭数 加 1。
            做 连接池_关闭索引 用 池、i、假、「空闲超时」。
            继续。
        完。
        若 非 池 的 正在使用 的 第 i 项 并且 当前时间 减 池 的 最后心跳时间 的 第 i 项 大于或等于 池 的 默认心跳间隔：
            若 非 做 连接池_发送心跳 用 池、i：
                池 的 连续失败次数 的 第 i 项 为 池 的 连续失败次数 的 第 i 项 加 1。
                池 的 风险评分 的 第 i 项 为 做 连接池_最小值 用 池 的 风险评分 的 第 i 项 加 0.12、1.0。
                若 池 的 连续失败次数 的 第 i 项 大于或等于 2：
                    池 的 统计 的 健康检查失败数 为 池 的 统计 的 健康检查失败数 加 1。
                    做 连接池_关闭索引 用 池、i、假、「心跳失败」。
                    继续。
                完。
            否则：
                池 的 连续失败次数 的 第 i 项 为 0。
                池 的 风险评分 的 第 i 项 为 做 连接池_最大值 用 池 的 风险评分 的 第 i 项 减 0.08、0.0。
            完。
            池 的 最后心跳时间 的 第 i 项 为 当前时间。
        完。
        池 的 热度评分 的 第 i 项 为 池 的 热度评分 的 第 i 项 乘 0.992。
        i 为 i 加 1。
    完。
    做 连接池_熔断巡检 用 池、当前时间。
完。

连接池_发送心跳 接受 池、idx：
    若 idx 小于 0 或者 idx 大于或等于 池 的 大小：
        返还 假。
    完。
    tls 为 池 的 连接 的 第 idx 项。
    若 tls 等于 0：
        返还 假。
    完。
    ok 为 做 安全_心跳 用 tls。
    返还 ok。
完。

连接池_触发淘汰 接受 池、当前时间：
    若 池 的 大小 小于或等于 0：
        返还。
    完。
    做 匹配 用 池 的 淘汰策略。
完。

连接池_淘汰最旧 接受 池：
    若 池 等于 0 或者 池 的 大小 小于或等于 0：
        返还。
    完。
    可变 最旧idx 为 负 1。
    可变 最旧时间 为 9223372036854775807。
    可变 i 为 0。
    只要 i 小于 池 的 大小：
        若 非 池 的 正在使用 的 第 i 项 并且 池 的 上次使用时间 的 第 i 项 小于 最旧时间：
            最旧时间 为 池 的 上次使用时间 的 第 i 项。
            最旧idx 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最旧idx 大于或等于 0：
        做 连接池_关闭索引 用 池、最旧idx、真、「LRU淘汰」。
    完。
完。

连接池_淘汰最低复用 接受 池：
    若 池 等于 0 或者 池 的 大小 小于或等于 0：
        返还。
    完。
    可变 target 为 负 1。
    可变 minReuse 为 2147483647。
    可变 i 为 0。
    只要 i 小于 池 的 大小：
        若 非 池 的 正在使用 的 第 i 项 并且 池 的 复用计数 的 第 i 项 小于 minReuse：
            minReuse 为 池 的 复用计数 的 第 i 项。
            target 为 i。
        完。
        i 为 i 加 1。
    完。
    若 target 大于或等于 0：
        做 连接池_关闭索引 用 池、target、真、「LFU淘汰」。
    完。
完。

连接池_淘汰最高风险 接受 池：
    若 池 等于 0 或者 池 的 大小 小于或等于 0：
        返还。
    完。
    可变 target 为 负 1。
    可变 maxRisk 为 负 1.0。
    可变 i 为 0。
    只要 i 小于 池 的 大小：
        若 非 池 的 正在使用 的 第 i 项 并且 池 的 风险评分 的 第 i 项 大于 maxRisk：
            maxRisk 为 池 的 风险评分 的 第 i 项。
            target 为 i。
        完。
        i 为 i 加 1。
    完。
    若 target 大于或等于 0：
        做 连接池_关闭索引 用 池、target、真、「高风险淘汰」。
    完。
完。

连接池_淘汰_LCAP综合 接受 池、当前时间：
    若 池 等于 0 或者 池 的 大小 小于或等于 0：
        返还。
    完。
    可变 target 为 负 1。
    可变 minScore 为 999999.0。
    可变 i 为 0。
    只要 i 小于 池 的 大小：
        若 非 池 的 正在使用 的 第 i 项：
            score 为 做 连接池_LCAP评分 用 池、i、当前时间。
            若 score 小于 minScore：
                minScore 为 score。
                target 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    若 target 大于或等于 0：
        做 连接池_关闭索引 用 池、target、真、「LCAP综合淘汰」。
    完。
完。

连接池_熔断槽位索引 接受 主机哈希：
    返还 主机哈希 取余于 连接池_熔断槽位。
完。

连接池_熔断允许访问 接受 池、主机哈希、当前时间：
    idx 为 做 连接池_熔断槽位索引 用 主机哈希。
    若 池 的 熔断主机哈希 的 第 idx 项 不等于 主机哈希：
        池 的 熔断主机哈希 的 第 idx 项 为 主机哈希。
        池 的 熔断失败次数 的 第 idx 项 为 0。
        池 的 熔断开启 的 第 idx 项 为 假。
        池 的 熔断截止时间 的 第 idx 项 为 0。
        返还 真。
    完。
    若 非 池 的 熔断开启 的 第 idx 项：
        返还 真。
    完。
    若 当前时间 大于或等于 池 的 熔断截止时间 的 第 idx 项：
        池 的 熔断开启 的 第 idx 项 为 假。
        池 的 熔断失败次数 的 第 idx 项 为 0。
        返还 真。
    完。
    返还 假。
完。

连接池_熔断记录失败 接受 池、主机哈希：
    idx 为 做 连接池_熔断槽位索引 用 主机哈希。
    若 池 的 熔断主机哈希 的 第 idx 项 不等于 主机哈希：
        池 的 熔断主机哈希 的 第 idx 项 为 主机哈希。
        池 的 熔断失败次数 的 第 idx 项 为 0。
        池 的 熔断开启 的 第 idx 项 为 假。
        池 的 熔断截止时间 的 第 idx 项 为 0。
    完。
    池 的 熔断失败次数 的 第 idx 项 为 池 的 熔断失败次数 的 第 idx 项 加 1。
    若 池 的 熔断失败次数 的 第 idx 项 大于或等于 4：
        池 的 熔断开启 的 第 idx 项 为 真。
        池 的 熔断截止时间 的 第 idx 项 为 做 连接池_当前时间 加 10000。
    完。
完。

连接池_熔断记录成功 接受 池、主机哈希：
    idx 为 做 连接池_熔断槽位索引 用 主机哈希。
    若 池 的 熔断主机哈希 的 第 idx 项 等于 主机哈希：
        池 的 熔断失败次数 的 第 idx 项 为 0。
        池 的 熔断开启 的 第 idx 项 为 假。
        池 的 熔断截止时间 的 第 idx 项 为 0。
    完。
完。

连接池_熔断巡检 接受 池、当前时间：
    可变 i 为 0。
    只要 i 小于 连接池_熔断槽位：
        若 池 的 熔断开启 的 第 i 项 并且 当前时间 大于或等于 池 的 熔断截止时间 的 第 i 项：
            池 的 熔断开启 的 第 i 项 为 假。
            池 的 熔断失败次数 的 第 i 项 为 0。
        完。
        i 为 i 加 1。
    完。
完。

AI网络_预连接 接受 主机、端口：
    若 全局连接池 等于 0：
        返还。
    完。
    做 AI网络_预连接扩展 用 全局连接池、主机、端口、协议_TLS、「h2」。
完。

AI网络_预连接扩展 接受 池、主机、端口、协议、ALPN：
    若 池 等于 0：
        返还。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    feature 为 做 Tensor_创建 用 10。
    做 Tensor_写 用 feature、0、端口 除以 65535.0。
    做 Tensor_写 用 feature、1、池 的 大小 除以 池 的 容量。
    做 Tensor_写 用 feature、2、池 的 统计 的 复用命中数 除以 (池 的 统计 的 复用命中数 加 池 的 统计 的 复用未命中数 加 1.0)。
    做 Tensor_写 用 feature、3、池 的 统计 的 TLS握手失败数 除以 (池 的 统计 的 TLS握手成功数 加 池 的 统计 的 TLS握手失败数 加 1.0)。
    做 Tensor_写 用 feature、4、池 的 统计 的 当前活跃连接数 除以 (池 的 容量 加 1.0)。
    做 Tensor_写 用 feature、5、池 的 统计 的 当前空闲连接数 除以 (池 的 容量 加 1.0)。
    做 Tensor_写 用 feature、6、做 连接池_当前时间 取余于 86400000 除以 86400000.0。
    做 Tensor_写 用 feature、7、协议 除以 3.0。
    做 Tensor_写 用 feature、8、做 字符串_长度 用 主机 除以 128.0。
    做 Tensor_写 用 feature、9、0.0。
    预测 为 做 AI总线_推理 用 bus、「LCAP_预连接概率」、feature。
    风险 为 做 AI总线_风险评分 用 bus、「LCAP_预连接风险」、feature。
    做 Tensor_释放 用 feature。
    池 的 统计 的 AI预连接触发数 为 池 的 统计 的 AI预连接触发数 加 1。
    若 预测 小于 AI预连接_置信阈值：
        返还。
    完。
    若 风险 大于 风险阈值_高：
        返还。
    完。
    做 连接池_预连接入队 用 池、主机、端口、协议、预测 减 风险。
    做 连接池_执行预连接队列 用 池、AI预连接_最多创建。
完。

连接池_预连接入队 接受 池、主机、端口、协议、优先级：
    若 池 的 预连接大小 大于或等于 连接池_预连接队列上限：
        做 连接池_预连接淘汰最低优先级 用 池。
    完。
    若 池 的 预连接大小 大于或等于 连接池_预连接队列上限：
        返还。
    完。
    idx 为 池 的 预连接大小。
    池 的 预连接主机 的 第 idx 项 为 主机。
    池 的 预连接端口 的 第 idx 项 为 端口。
    池 的 预连接协议 的 第 idx 项 为 协议。
    池 的 预连接优先级 的 第 idx 项 为 优先级。
    池 的 预连接入队时间 的 第 idx 项 为 做 连接池_当前时间。
    池 的 预连接大小 为 池 的 预连接大小 加 1。
完。

连接池_预连接淘汰最低优先级 接受 池：
    若 池 的 预连接大小 小于或等于 0：
        返还。
    完。
    可变 idx 为 0。
    可变 minP 为 池 的 预连接优先级 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 池 的 预连接大小：
        若 池 的 预连接优先级 的 第 i 项 小于 minP：
            minP 为 池 的 预连接优先级 的 第 i 项。
            idx 为 i。
        完。
        i 为 i 加 1。
    完。
    做 连接池_预连接移除索引 用 池、idx。
完。

连接池_预连接移除索引 接受 池、idx：
    若 idx 小于 0 或者 idx 大于或等于 池 的 预连接大小：
        返还。
    完。
    tail 为 池 的 预连接大小 减 1。
    若 idx 不等于 tail：
        池 的 预连接主机 的 第 idx 项 为 池 的 预连接主机 的 第 tail 项。
        池 的 预连接端口 的 第 idx 项 为 池 的 预连接端口 的 第 tail 项。
        池 的 预连接协议 的 第 idx 项 为 池 的 预连接协议 的 第 tail 项。
        池 的 预连接优先级 的 第 idx 项 为 池 的 预连接优先级 的 第 tail 项。
        池 的 预连接入队时间 的 第 idx 项 为 池 的 预连接入队时间 的 第 tail 项。
    完。
    池 的 预连接大小 为 池 的 预连接大小 减 1。
完。

连接池_执行预连接队列 接受 池、最大处理数：
    若 池 等于 0 或者 池 的 预连接大小 小于或等于 0：
        返还。
    完。
    可变 已处理 为 0。
    只要 池 的 预连接大小 大于 0 并且 已处理 小于 最大处理数：
        idx 为 做 连接池_预连接取最高优先级 用 池。
        若 idx 小于 0：
            跳出。
        完。
        host 为 池 的 预连接主机 的 第 idx 项。
        port 为 池 的 预连接端口 的 第 idx 项。
        proto 为 池 的 预连接协议 的 第 idx 项。
        做 连接池_预连接移除索引 用 池、idx。
        req 为 新建 连接请求：主机 为 host、端口 为 port、协议 为 proto、ALPN 为 「h2」、最小TLS版本 为 池 的 最小TLS版本、允许自动创建 为 真。
        tls 为 做 连接池_创建安全连接 用 池、req。
        若 tls 不等于 0：
            做 连接池_放回扩展 用 池、tls、host、port、proto、「h2」、池 的 最小TLS版本。
            池 的 统计 的 AI预连接成功数 为 池 的 统计 的 AI预连接成功数 加 1。
        否则：
            池 的 统计 的 AI预连接失败数 为 池 的 统计 的 AI预连接失败数 加 1。
        完。
        已处理 为 已处理 加 1。
    完。
完。

连接池_预连接取最高优先级 接受 池：
    若 池 的 预连接大小 小于或等于 0：
        返还 负 1。
    完。
    可变 idx 为 0。
    可变 maxP 为 池 的 预连接优先级 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 池 的 预连接大小：
        若 池 的 预连接优先级 的 第 i 项 大于 maxP：
            maxP 为 池 的 预连接优先级 的 第 i 项。
            idx 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 idx。
完。

连接池_设置最小TLS版本 接受 池、版本：
    若 池 等于 0：
        返还。
    完。
    若 版本 小于 TLS_版本_1_2：
        版本 为 TLS_版本_1_2。
    完。
    若 版本 大于 TLS_版本_1_3：
        版本 为 TLS_版本_1_3。
    完。
    池 的 最小TLS版本 为 版本。
完。

连接池_设置淘汰策略 接受 池、策略：
    若 池 等于 0：
        返还。
    完。
    做 匹配 用 策略。
完。

连接池_设置超时配置 接受 池、空闲毫秒、生命周期毫秒、心跳毫秒：
    若 池 等于 0：
        返还。
    完。
    若 空闲毫秒 大于 0：
        池 的 默认空闲超时 为 空闲毫秒。
    完。
    若 生命周期毫秒 大于 0：
        池 的 默认最大寿命 为 生命周期毫秒。
    完。
    若 心跳毫秒 大于 0：
        池 的 默认心跳间隔 为 心跳毫秒。
    完。
完。

连接池_启用自动创建 接受 池、启用：
    若 池 等于 0：
        返还。
    完。
    池 的 自动创建 为 启用。
完。

连接池_记录传输 接受 池、tls、发送字节、接收字节：
    若 池 等于 0 或者 tls 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 池 的 大小：
        若 池 的 连接 的 第 i 项 等于 tls：
            池 的 发送字节 的 第 i 项 为 池 的 发送字节 的 第 i 项 加 发送字节。
            池 的 接收字节 的 第 i 项 为 池 的 接收字节 的 第 i 项 加 接收字节。
            池 的 统计 的 总发送字节 为 池 的 统计 的 总发送字节 加 发送字节。
            池 的 统计 的 总接收字节 为 池 的 统计 的 总接收字节 加 接收字节。
            池 的 上次使用时间 的 第 i 项 为 做 连接池_当前时间。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

连接池_获取统计 接受 池：
    若 池 等于 0：
        返还 全局连接池统计。
    完。
    返还 一本字典：创建总数 对应 池 的 统计 的 创建总数、复用命中数 对应 池 的 统计 的 复用命中数、复用未命中数 对应 池 的 统计 的 复用未命中数、放回总数 对应 池 的 统计 的 放回总数、主动关闭数 对应 池 的 统计 的 主动关闭数、淘汰关闭数 对应 池 的 统计 的 淘汰关闭数、超时关闭数 对应 池 的 统计 的 超时关闭数、健康检查失败数 对应 池 的 统计 的 健康检查失败数、熔断拒绝数 对应 池 的 统计 的 熔断拒绝数、TLS握手成功数 对应 池 的 统计 的 TLS握手成功数、TLS握手失败数 对应 池 的 统计 的 TLS握手失败数、证书验证失败数 对应 池 的 统计 的 证书验证失败数、会话恢复成功数 对应 池 的 统计 的 会话恢复成功数、AI预连接触发数 对应 池 的 统计 的 AI预连接触发数、AI预连接成功数 对应 池 的 统计 的 AI预连接成功数、AI预连接失败数 对应 池 的 统计 的 AI预连接失败数、当前活跃连接数 对应 池 的 统计 的 当前活跃连接数、当前空闲连接数 对应 池 的 统计 的 当前空闲连接数、总发送字节 对应 池 的 统计 的 总发送字节、总接收字节 对应 池 的 统计 的 总接收字节、总连接时延 对应 池 的 统计 的 总连接时延、最大连接时延 对应 池 的 统计 的 最大连接时延、池大小 对应 池 的 大小、池容量 对应 池 的 容量、预连接队列大小 对应 池 的 预连接大小。
完。

连接池_打印统计 接受 池：
    s 为 做 连接池_获取统计 用 池。
    做 内核打印 用 「═══ 连接池统计 ═══」。
    做 内核打印 用 「创建={} 命中={} 未命中={} 放回={}」、s 的 创建总数、s 的 复用命中数、s 的 复用未命中数、s 的 放回总数。
    做 内核打印 用 「关闭[主动/淘汰/超时]={}/{}/{} 健康失败={}」、s 的 主动关闭数、s 的 淘汰关闭数、s 的 超时关闭数、s 的 健康检查失败数。
    做 内核打印 用 「TLS[成功/失败]={}/{} 证书失败={} 熔断拒绝={}」、s 的 TLS握手成功数、s 的 TLS握手失败数、s 的 证书验证失败数、s 的 熔断拒绝数。
    做 内核打印 用 「AI预连接[触发/成功/失败]={}/{}/{}」、s 的 AI预连接触发数、s 的 AI预连接成功数、s 的 AI预连接失败数。
    做 内核打印 用 「活跃={} 空闲={} 队列={} 容量={}/{}」、s 的 当前活跃连接数、s 的 当前空闲连接数、s 的 预连接队列大小、s 的 池大小、s 的 池容量。
完。

连接池_重置统计 接受 池：
    若 池 等于 0：
        返还。
    完。
    池 的 统计 为 新建 连接池统计。
完。

连接池_后台维护循环 接受 池、运行标志引用：
    只要 运行标志引用 的 值：
        做 连接池_巡检 用 池。
        做 连接池_执行预连接队列 用 池、1。
        做 平台_睡眠毫秒 用 200。
    完。
完。

连接池_诊断连接 接受 池、idx：
    若 池 等于 0 或者 idx 小于 0 或者 idx 大于或等于 池 的 大小：
        返还 空。
    完。
    返还 一本字典：主机 对应 池 的 主机 的 第 idx 项、端口 对应 池 的 端口 的 第 idx 项、协议 对应 池 的 协议 的 第 idx 项、ALPN 对应 池 的 ALPN 的 第 idx 项、状态 对应 池 的 状态 的 第 idx 项、活跃 对应 池 的 活跃 的 第 idx 项、正在使用 对应 池 的 正在使用 的 第 idx 项、已握手 对应 池 的 已握手 的 第 idx 项、复用计数 对应 池 的 复用计数 的 第 idx 项、风险评分 对应 池 的 风险评分 的 第 idx 项、热度评分 对应 池 的 热度评分 的 第 idx 项、空闲时长 对应 做 连接池_当前时间 减 池 的 上次使用时间 的 第 idx 项、生命周期 对应 做 连接池_当前时间 减 池 的 创建时间 的 第 idx 项、往返时延估计 对应 池 的 往返时延估计 的 第 idx 项。
完。

连接池_批量预热 接受 池、目标列表：
    若 池 等于 0 或者 目标列表 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 目标列表 的 长度：
        item 为 目标列表 的 第 i 项。
        做 AI网络_预连接扩展 用 池、item 的 主机、item 的 端口、协议_TLS、「h2」。
        i 为 i 加 1。
    完。
完。

本章 收束。
