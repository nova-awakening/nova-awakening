《Linux兼容初启》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·特文模拟》。

引用《.·ELF解析与进程》。

引用《.·缺调补充》。

引用《..·中枢·整数转字符串》。

可变 兼容_已初始化 为 假。

可变 兼容_Linux程序数 为 0。

可变 兼容_Nova系统调用翻译数 为 0。

可变 兼容_失败Nova系统调用列表 为 一组：（空）。

Linux兼容_初始化 接受 零参：
    做 确保目录存在 用 「/lib」。
    做 确保目录存在 用 「/lib64」。
    做 确保目录存在 用 「/lib/x86_64-linux-gnu」。
    做 确保目录存在 用 「/usr/lib」。
    做 确保目录存在 用 「/usr/lib/x86_64-linux-gnu」。
    做 确保目录存在 用 「/usr/share」。
    做 确保目录存在 用 「/etc/ld.so.conf.d」。
    做 确保目录存在 用 「/dev/shm」。
    做 确保目录存在 用 「/dev/pts」。
    做 确保目录存在 用 「/tmp」。
    做 确保目录存在 用 「/run」。
    做 创建符号链接 用 「/lib64/ld-linux-x86-64.so.2」、「/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2」。
    做 创建_proc_self。
    做 创建_dev_特殊文件。
    做 注册_缺失Nova系统调用。
    兼容_已初始化 为 真。
    做 内核信息 用 「Linux兼容层: 初始化完成 (ELF+Nova系统调用+FS路径)」。
完。

Linux_执行 接受 路径、参数、环境变量：
    若 非 兼容_已初始化：
        做 Linux兼容_初始化。
    完。
    若 非 做 是ELF文件 用 路径：
        若 做 是脚本文件 用 路径：
            返还 做 执行_脚本 用 路径、参数、环境变量。
        完。
        返还 错误_格式。
    完。
    elf 为 做 ELF_解析头 用 路径。
    若 elf 等于 空：
        返还 错误_格式。
    完。
    若 elf 的 机器类型 不等于 0x3E：
        做 内核警告 用 「Linux兼容: 不支持的架构 」 加 做 整数转字符串 用 elf 的 机器类型。
        返还 错误_格式。
    完。
    可变 完整环境 为 环境变量。
    若 完整环境 等于 空：
        完整环境 为 一组：（空）。
    完。
    做 追加 用 完整环境、「PATH=/usr/local/bin:/usr/bin:/bin」。
    做 追加 用 完整环境、「HOME=/home/」 加 做 获取当前用户名。
    做 追加 用 完整环境、「LANG=zh_CN.UTF-8」。
    做 追加 用 完整环境、「LD_LIBRARY_PATH=/lib/x86_64-linux-gnu:/usr/lib/x86_64-linux-gnu」。
    做 追加 用 完整环境、「XDG_RUNTIME_DIR=/run/user/1000」。
    做 追加 用 完整环境、「DISPLAY=:0」。
    做 追加 用 完整环境、「WAYLAND_DISPLAY=wayland-0」。
    pid 为 做 创建进程_Linux 用 路径、参数、完整环境。
    若 pid 大于 0：
        兼容_Linux程序数 为 兼容_Linux程序数 加 1。
        做 内核信息 用 「Linux兼容: 启动 」 加 路径 加 「 PID=」 加 做 整数转字符串 用 pid。
    完。
    返还 pid。
完。

本章 收束。
