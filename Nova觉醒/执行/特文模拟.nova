《特文模拟》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·读取调用》。

引用《.·归档解包》。

引用《..·中枢·硬链接》。

引用《.·ELF解析与进程》。

引用《..·记忆·VFS文件读写》。

创建_proc_self 接受 零参：
    做 确保目录存在 用 「/proc/self」。
    做 确保目录存在 用 「/proc/self/fd」。
完。

创建_dev_特殊文件 接受 零参：
    做 确保目录存在 用 「/dev/shm」。
    做 确保目录存在 用 「/dev/pts」。
    做 确保目录存在 用 「/dev/dri」。
完。

安装_deb包 接受 deb路径：
    做 内核信息 用 「安装deb: 」 加 deb路径。
    数据 为 做 解包_ar 用 deb路径、「data.tar」。
    若 数据 等于 空：
        返还 错误_格式。
    完。
    做 解包_tar 用 数据、「/」。
    做 内核信息 用 「安装deb: 完成 」 加 deb路径。
    返还 成功。
完。

安装_appimage 接受 路径：
    做 设置权限 用 路径、493。
    做 内核信息 用 「App图像就绪: 」 加 路径。
    返还 成功。
完。

安装_flatpak 接受 ref：
    做 内核信息 用 「Flatpak: 」 加 ref 加 「 (需要runtime)」。
    返还 成功。
完。

X11兼容_初始化 接受 零参：
    做 确保目录存在 用 「/tmp/.X11-unix」。
    做 内核信息 用 「X11兼容: XWayland桥接就绪」。
完。

Linux兼容_统计 接受 零参：
    返还 一组：兼容_Linux程序数、兼容_Nova系统调用翻译数、做 计数 用 兼容_失败Nova系统调用列表。
完。

确保目录存在 接受 路径：
    结果 为 做 VFS_查找 用 路径。
    若 结果 等于 空：
        做 VFS_创建目录 用 路径、0x1ED。
    完。
完。

创建符号链接 接受 链接、目标：
    做 VFS_创建符号链接 用 链接、目标。
完。

是ELF文件 接受 路径：
    文件 为 做 VFS_打开 用 路径、0。
    若 文件 等于 空：
        返还 假。
    完。
    头 为 做 VFS_读取 用 文件、4。
    做 VFS_关闭 用 文件。
    若 头 等于 空 或者 做 计数 用 头 小于 4：
        返还 假。
    完。
    返还 头 的 第 0 项 等于 0x7F 并且 头 的 第 1 项 等于 0x45 并且 头 的 第 2 项 等于 0x4C 并且 头 的 第 3 项 等于 0x46。
完。

是脚本文件 接受 路径：
    文件 为 做 VFS_打开 用 路径、0。
    若 文件 等于 空：
        返还 假。
    完。
    头 为 做 VFS_读取 用 文件、2。
    做 VFS_关闭 用 文件。
    若 头 等于 空 或者 做 计数 用 头 小于 2：
        返还 假。
    完。
    返还 头 的 第 0 项 等于 0x23 并且 头 的 第 1 项 等于 0x21。
完。

执行_脚本 接受 路径、参数、环境：
    文件 为 做 VFS_打开 用 路径、0。
    若 文件 等于 空：
        返还 错误_格式。
    完。
    首行 为 做 VFS_读行 用 文件。
    做 VFS_关闭 用 文件。
    若 首行 等于 空 或者 做 字符串长度 用 首行 小于 3：
        返还 错误_格式。
    完。
    解释器 为 做 字符串_去前缀 用 首行、「#!」。
    返还 做 创建进程_Linux 用 解释器、一组：路径 加 参数、环境。
完。

本章 收束。
