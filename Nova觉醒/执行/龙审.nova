《龙审》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《..·标准·原生字符串》。

引用《..·中枢·字符串替换》。

引用《..·记忆·VFS读写同步》。

引用《..·中枢·整数转字符串》。

可变 警告数 为 0。

可变 错误数 为 0。

可变 已检查文件数 为 0。

可变 最长函数行数 为 0。

可变 最长函数起始行 为 0。

可变 最长函数文件 为 「」。

可变 最长函数签名 为 「」。

龙审_包含待收口标记 接受 行：
    标签一 为 「TO」 加 「DO」。
    标签二 为 「待」 加 「实现」。
    标签三 为 「未」 加 「实现」。
    返还 做 字符串包含 用 行、标签一 或者 做 字符串包含 用 行、标签二 或者 做 字符串包含 用 行、标签三。
完。

龙审_main 接受 参数：
    若 做 计数 用 参数 等于 0：
        做 打印行 用 「龙审 v1.0」。
        做 打印行 用 「用法: 龙审 [文件...]」。
        返还 0。
    完。
    做 龙审_全局重置。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        内容 为 做 文件_读取全部 用 参数 的 第 i 项。
        若 内容 不等于 空：
            做 龙审_检查文件 用 参数 的 第 i 项、内容。
        否则：
            做 龙审_错误 用 参数 的 第 i 项、0、「无法读取文件」。
        完。
        i 为 i 加 1。
    完。
    做 打印行 用 「」。
    做 打印行 用 「检查完成: 」 加 做 整数转字符串 用 错误数 加 「 错误, 」 加 做 整数转字符串 用 警告数 加 「 警告」。
    返还 错误数。
完。

龙审_记录函数跨度 接受 路径、起始行、结束行、签名：
    若 起始行 小于或等于 0 或者 结束行 小于 起始行：
        返还。
    完。
    行数 为 结束行 减 起始行 加 1。
    若 行数 大于 最长函数行数：
        最长函数行数 为 行数。
        最长函数起始行 为 起始行。
        最长函数文件 为 路径。
        最长函数签名 为 签名。
    完。
    若 行数 大于 100：
        做 龙审_警告 用 路径、起始行、「函数过长(」 加 做 整数转字符串 用 行数 加 「行), 建议拆分: 」 加 签名。
    完。
完。

龙审_检查文件 接受 路径、内容：
    已检查文件数 为 已检查文件数 加 1。
    行列表 为 做 字符串分割 用 内容、「\n」。
    可变 括号深度 为 0。
    可变 连续空行 为 0。
    可变 函数数 为 0。
    可变 当前函数起始 为 0。
    可变 当前函数签名 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 行列表 的 第 i 项。
        行号 为 i 加 1。
        去空白行 为 做 字符串去首尾空白 用 行。
        若 做 字符串长度 用 行 大于 120：
            做 龙审_警告 用 路径、行号、「行过长(」 加 做 整数转字符串 用 做 字符串长度 用 行 加 「字符>120)」。
        完。
        若 做 龙审_包含待收口标记 用 行：
            做 龙审_警告 用 路径、行号、「包含待收口标记」。
        完。
        若 做 字符串包含 用 去空白行、「{ }」 或者 做 字符串包含 用 去空白行、「：返还 成功 }」 或者 做 字符串包含 用 去空白行、「：返还 0 }」：
            做 龙审_警告 用 路径、行号、「可能存在空实现或占位函数」。
        完。
        若 做 字符串长度 用 行 大于 0：
            最后字符 为 做 字符串字符 用 行、做 字符串长度 用 行 减 1。
            若 最后字符 等于 32 或者 最后字符 等于 9：
                做 龙审_警告 用 路径、行号、「行尾有空白字符」。
            完。
        完。
        若 做 字符串长度 用 去空白行 等于 0：
            连续空行 为 连续空行 加 1。
            若 连续空行 大于 2：
                做 龙审_警告 用 路径、行号、「连续空行过多(>2)」。
            完。
        否则：
            连续空行 为 0。
        完。
        若 做 字符串包含 用 去空白行、「函数 」：
            若 当前函数起始 大于 0：
                做 龙审_记录函数跨度 用 路径、当前函数起始、行号 减 1、当前函数签名。
            完。
            当前函数起始 为 行号。
            当前函数签名 为 去空白行。
            函数数 为 函数数 加 1。
        完。
        可变 j 为 0。
        只要 j 小于 做 字符串长度 用 去空白行：
            ch 为 做 字符串字符 用 去空白行、j。
            若 ch 等于 123：
                括号深度 为 括号深度 加 1。
            完。
            若 ch 等于 125：
                括号深度 为 括号深度 减 1。
            完。
            j 为 j 加 1。
        完。
        若 括号深度 小于 0：
            做 龙审_错误 用 路径、行号、「多余的右花括号」。
            括号深度 为 0。
        完。
        若 非 做 字符串开头于 用 去空白行、「//」 并且 非 做 字符串开头于 用 去空白行、「定义 」：
            若 做 字符串包含 用 去空白行、「999999」 或者 做 字符串包含 用 去空白行、「0xDEAD」 或者 做 字符串包含 用 去空白行、「0xBEEF」：
                做 龙审_警告 用 路径、行号、「可能的预设魔数」。
            完。
        完。
        可变 前导空格 为 0。
        可变 k 为 0。
        只要 k 小于 做 字符串长度 用 行 并且 做 字符串字符 用 行、k 等于 32：
            前导空格 为 前导空格 加 1。
            k 为 k 加 1。
        完。
        若 前导空格 大于 20 并且 做 字符串长度 用 去空白行 大于 0：
            做 龙审_警告 用 路径、行号、「嵌套过深(缩进>」 加 做 整数转字符串 用 前导空格 除以 4 加 「层)」。
        完。
        i 为 i 加 1。
    完。
    若 当前函数起始 大于 0：
        做 龙审_记录函数跨度 用 路径、当前函数起始、做 计数 用 行列表、当前函数签名。
    完。
    若 括号深度 大于 0：
        做 龙审_错误 用 路径、做 计数 用 行列表、「未闭合的花括号(」 加 做 整数转字符串 用 括号深度 加 「个)」。
    完。
    若 做 计数 用 行列表 大于 500 并且 函数数 小于 3：
        做 龙审_警告 用 路径、1、「文件过长(」 加 做 整数转字符串 用 做 计数 用 行列表 加 「行)但函数过少(」 加 做 整数转字符串 用 函数数 加 「), 建议拆分」。
    完。
完。

龙审_警告 接受 文件、行号、消息：
    做 打印行 用 「  WARN 」 加 文件 加 「:」 加 做 整数转字符串 用 行号 加 「: 」 加 消息。
    警告数 为 警告数 加 1。
完。

龙审_错误 接受 文件、行号、消息：
    做 打印行 用 「  ERROR 」 加 文件 加 「:」 加 做 整数转字符串 用 行号 加 「: 」 加 消息。
    错误数 为 错误数 加 1。
完。

龙审_统计 接受 零参：
    返还 一本字典：模块 对应 「龙审」、已检查文件数 对应 已检查文件数、警告数 对应 警告数、错误数 对应 错误数、最长函数行数 对应 最长函数行数、最长函数文件 对应 最长函数文件、最长函数起始行 对应 最长函数起始行、最长函数签名 对应 最长函数签名。
完。

龙审_健康评估 接受 零参：
    若 错误数 大于 0：
        返还 假。
    完。
    若 最长函数行数 大于 200：
        返还 假。
    完。
    若 警告数 大于 500：
        返还 假。
    完。
    返还 真。
完。

龙审_综合报告 接受 零参：
    做 打印 用 「[龙审] 综合报告」。
    做 打印 用 「  已检查文件数: 」 加 做 整数转字符串 用 已检查文件数。
    做 打印 用 「  警告数: 」 加 做 整数转字符串 用 警告数。
    做 打印 用 「  错误数: 」 加 做 整数转字符串 用 错误数。
    做 打印 用 「  最长函数行数: 」 加 做 整数转字符串 用 最长函数行数。
    做 打印 用 「  最长函数位置: 」 加 最长函数文件 加 「:」 加 做 整数转字符串 用 最长函数起始行。
    做 打印 用 「  最长函数签名: 」 加 最长函数签名。
    做 打印 用 「  健康: 」 加 若 做 龙审_健康评估 则 「正常」 否则 「异常」。
完。

龙审_全局重置 接受 零参：
    警告数 为 0。
    错误数 为 0。
    已检查文件数 为 0。
    最长函数行数 为 0。
    最长函数起始行 为 0。
    最长函数文件 为 「」。
    最长函数签名 为 「」。
    返还 0。
完。

本章 收束。
