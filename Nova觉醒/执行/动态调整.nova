《动态调整》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·底层系统调用原语》。

引用《..·中枢·内存初始化》。

引用《..·感知·帧管线获取》。

Config_更新视口 接受 宽、高：
    c 为 做 Config_获取。
    若 c 等于 0：
        返还。
    完。
    c 的 视口宽 为 宽。
    c 的 视口高 为 高。
完。

Config_切换省电模式 接受 启用：
    c 为 做 Config_获取。
    若 c 等于 0：
        返还。
    完。
    c 的 省电模式 为 启用。
    若 启用：
        c 的 目标帧率 为 30。
        c 的 帧预算us 为 33333。
        c 的 图片缓存MB 为 c 的 图片缓存MB 除以 2。
        c 的 AI启用 为 假。
    否则：
        c 的 目标帧率 为 c 的 屏幕刷新率。
        c 的 帧预算us 为 1000000 除以 c 的 目标帧率。
        c 的 AI启用 为 真。
    完。
完。

Config_自适应帧率 接受 连续掉帧数：
    c 为 做 Config_获取。
    若 c 等于 0：
        返还。
    完。
    若 连续掉帧数 大于 5：
        c 的 目标帧率 为 30。
        c 的 帧预算us 为 33333。
    否则若 连续掉帧数 大于 2：
        c 的 目标帧率 为 45。
        c 的 帧预算us 为 22222。
    否则若 连续掉帧数 等于 0 并且 c 的 目标帧率 小于 c 的 屏幕刷新率：
        c 的 目标帧率 为 c 的 目标帧率 加 15。
        若 c 的 目标帧率 大于 c 的 屏幕刷新率：
            c 的 目标帧率 为 c 的 屏幕刷新率。
        完。
        c 的 帧预算us 为 1000000 除以 c 的 目标帧率。
    完。
完。

Config_内存压力响应 接受 压力级别：
    c 为 做 Config_获取。
    若 c 等于 0：
        返还。
    完。
    若 压力级别 等于 1：
        c 的 图片缓存MB 为 c 的 图片缓存MB 乘 3 除以 4。
        c 的 网络缓存MB 为 c 的 网络缓存MB 乘 3 除以 4。
    否则若 压力级别 大于或等于 2：
        c 的 图片缓存MB 为 c 的 图片缓存MB 除以 2。
        c 的 网络缓存MB 为 c 的 网络缓存MB 除以 2。
        c 的 图处纹理缓存MB 为 c 的 图处纹理缓存MB 除以 2。
        若 压力级别 大于或等于 3：
            c 的 AI启用 为 假。
        完。
    完。
完。

Config_评估性能 接受 cfg：
    若 cfg 等于 0：
        返还 50。
    完。
    可变 分数 为 0。
    若 cfg 的 总物理内存MB 大于或等于 8192：
        分数 为 分数 加 25。
    否则若 cfg 的 总物理内存MB 大于或等于 4096：
        分数 为 分数 加 20。
    否则若 cfg 的 总物理内存MB 大于或等于 2048：
        分数 为 分数 加 10。
    否则：
        分数 为 分数 加 5。
    完。
    若 cfg 的 图处加速：
        分数 为 分数 加 25。
    否则：
        分数 为 分数 加 5。
    完。
    若 cfg 的 屏幕刷新率 大于或等于 120：
        分数 为 分数 加 25。
    否则若 cfg 的 屏幕刷新率 大于或等于 60：
        分数 为 分数 加 15。
    否则：
        分数 为 分数 加 5。
    完。
    若 cfg 的 视口宽 大于或等于 3840：
        分数 为 分数 加 25。
    否则若 cfg 的 视口宽 大于或等于 1920：
        分数 为 分数 加 20。
    否则若 cfg 的 视口宽 大于或等于 1280：
        分数 为 分数 加 10。
    否则：
        分数 为 分数 加 5。
    完。
    返还 分数。
完。

平台_检测屏幕宽 接受 零参：
    返还 0。
完。

平台_检测屏幕高 接受 零参：
    fd 为 做 系统调用_open 用 「/dev/dri/card0」、0。
    若 fd 小于 0：
        返还 768。
    完。
    res 为 做 堆_分配 用 64。
    做 系统调用_Nova设备控制 用 fd、DRM_Nova设备控制_MODE_GETRESOURCES、res。
    h 为 做 内存_读32 用 res 加 4。
    做 堆_释放 用 res。
    做 系统调用_close 用 fd。
    若 h 大于 0：
        返还 h。
    完。
    返还 768。
完。

平台_检测DPR 接受 零参：
    返还 1024。
完。

平台_检测刷新率 接受 零参：
    返还 60。
完。

平台_检测音频采样率 接受 零参：
    返还 48000。
完。

平台_检测内存MB 接受 零参：
    返还 0。
完。

平台_检测图处可用 接受 零参：
    fd 为 做 系统调用_打开 用 「/dev/dri/card0」、0、0。
    若 fd 小于 0：
        返还 假。
    完。
    做 系统调用_关闭 用 fd。
    渲染fd 为 做 系统调用_打开 用 「/dev/dri/renderD128」、0、0。
    若 渲染fd 大于或等于 0：
        做 系统调用_关闭 用 渲染fd。
        返还 真。
    完。
    返还 真。
完。

平台_检测图处最大纹理 接受 零参：
    返还 8192。
完。

动态调整_统计 接受 零参：
    返还 一本字典：模块 对应 「动态调整」。
完。

动态调整_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    返还 做 计数 用 问题 等于 0。
完。

动态调整_综合报告 接受 零参：
    做 打印 用 「[动态调整] ═══ 综合报告 ═══」。
    做 打印 用 「  健康: 」 加 若 做 动态调整_健康评估 则 「正常」 否则 「异常」。
完。

动态调整_全局重置 接受 零参：
    做 打印 用 「[动态调整] 全局统计已重置」。
    返还 0。
完。

本章 收束。
