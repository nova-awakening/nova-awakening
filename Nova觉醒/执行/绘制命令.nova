《绘制命令》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·渲染层管理》。

引用《.·节点操作》。

引用《.·布局计算》。

渲染_绘制 接受 文档：
    若 渲染_树 等于 空：
        返还。
    完。
    根 为 做 渲染_查找盒 用 渲染_树 的 根盒。
    若 根 等于 空：
        返还。
    完。
    主层 为 做 浏览器图处_创建层 用 0、0、渲染_树 的 视口宽、渲染_树 的 视口高。
    做 渲染_绘制盒 用 文档、根、主层。
    做 浏览器图处_合成帧。
完。

渲染_绘制盒 接受 文档、盒、层ID：
    若 盒 的 显示 等于 显示_无：
        返还。
    完。
    绘制X 为 盒 的 x 减 渲染_树 的 滚动X。
    绘制Y 为 盒 的 y 减 渲染_树 的 滚动Y。
    若 盒 的 背景色 不等于 0 并且 盒 的 背景色 不等于 0x00000000：
        若 盒 的 圆角 大于 0：
            做 浏览器图处_圆角矩形 用 层ID、绘制X、绘制Y、盒 的 宽、盒 的 高、盒 的 背景色、盒 的 圆角、盒 的 透明度。
        否则：
            做 浏览器图处_填充矩形 用 层ID、绘制X、绘制Y、盒 的 宽、盒 的 高、盒 的 背景色、盒 的 透明度。
        完。
    完。
    若 盒 的 边框宽 大于 0：
        做 浏览器图处_填充矩形 用 层ID、绘制X、绘制Y、盒 的 宽、盒 的 边框宽、盒 的 前景色、255。
    完。
    节点 为 做 DOM_查找节点 用 文档、盒 的 节点ID。
    若 节点 不等于 空 并且 节点 的 类型 等于 节点_文本：
        做 浏览器图处_绘制文字 用 层ID、绘制X 加 盒 的 内边距左、绘制Y 加 盒 的 内边距上、节点 的 文本内容、盒 的 字号、盒 的 前景色。
    完。
    子层 为 层ID。
    若 盒 的 需要独立层：
        子层 为 做 浏览器图处_创建层 用 绘制X、绘制Y、盒 的 宽、盒 的 高。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 盒 的 子盒：
        子 为 做 渲染_查找盒 用 盒 的 子盒 的 第 i 项。
        若 子 不等于 空：
            做 渲染_绘制盒 用 文档、子、子层。
        完。
        i 为 i 加 1。
    完。
完。

渲染_滚动 接受 增量X、增量Y：
    渲染_树 的 滚动X 为 渲染_树 的 滚动X 加 增量X。
    渲染_树 的 滚动Y 为 渲染_树 的 滚动Y 加 增量Y。
    若 渲染_树 的 滚动X 小于 0：
        渲染_树 的 滚动X 为 0。
    完。
    若 渲染_树 的 滚动Y 小于 0：
        渲染_树 的 滚动Y 为 0。
    完。
    渲染_树 的 脏 为 真。
完。

渲染_命中测试 接受 x、y：
    可变 命中 为 0。
    可变 i 为 做 计数 用 渲染_树 的 盒池 减 1。
    只要 i 大于或等于 0：
        盒 为 渲染_树 的 盒池 的 第 i 项。
        若 盒 的 显示 不等于 显示_无：
            bx 为 盒 的 x 减 渲染_树 的 滚动X。
            by 为 盒 的 y 减 渲染_树 的 滚动Y。
            若 x 大于或等于 bx 并且 x 小于 bx 加 盒 的 宽 并且 y 大于或等于 by 并且 y 小于 by 加 盒 的 高：
                命中 为 盒 的 节点ID。
            完。
        完。
        i 为 i 减 1。
    完。
    返还 命中。
完。

渲染_统计 接受 零参：
    返还 一组：渲染_树 的 总盒数、渲染_树 的 视口宽、渲染_树 的 视口高。
完。

本章 收束。
