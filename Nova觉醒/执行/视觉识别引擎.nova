《视觉识别引擎》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·推理·TNF视觉识别》。

引用《..·引导·端口IO》。

引用《..·外联·URL解析辅助》。

视觉_按钮 为 0。

视觉_输入框 为 1。

视觉_链接 为 2。

视觉_图片 为 3。

视觉_文字 为 4。

视觉_验证码 为 5。

视觉_滑块 为 6。

视觉_复选框 为 7。

视觉_下拉菜单 为 8。

视觉_弹窗 为 9。

视觉_关闭按钮 为 10。

视觉_Cookie提示 为 11。

视觉_登录表单 为 12。

视觉_未知 为 99。

视觉元素 是 一种 事物，含 类型、x、y、宽、高、置信度、文字内容、可交互。

页面视觉状态 是 一种 事物，含 元素列表、有弹窗、有验证码、有Cookie提示、有登录表单、页面加载完成、截图哈希。

OCR结果 是 一种 事物，含 文字、x、y、宽、高、置信度。

可变 视觉_引擎 为 空。

可变 视觉_已初始化 为 假。

可变 视觉_总识别次数 为 0。

可变 视觉_总OCR次数 为 0。

视觉识别_初始化 接受 零参：
    视觉_引擎 为 做 TNF视觉_初始化。
    视觉_已初始化 为 真。
    做 内核信息 用 「视觉识别引擎: TNF视觉+像素感知+OCR 就绪」。
完。

视觉识别_分析页面 接受 零参：
    视觉_总识别次数 为 视觉_总识别次数 加 1。
    状态 为 新建 页面视觉状态。
    状态 的 元素列表 为 一组：（空）。
    状态 的 有弹窗 为 假。
    状态 的 有验证码 为 假。
    状态 的 有Cookie提示 为 假。
    状态 的 有登录表单 为 假。
    像素 为 做 视觉识别_获取截图像素。
    网格宽 为 160。
    网格高 为 120。
    屏幕宽 为 图处_上下文 的 屏幕宽。
    屏幕高 为 图处_上下文 的 屏幕高。
    可变 gy 为 0。
    只要 gy 小于 屏幕高：
        可变 gx 为 0。
        只要 gx 小于 屏幕宽：
            区域像素 为 做 视觉识别_提取区域 用 像素、gx、gy、网格宽、网格高、屏幕宽。
            TNF结果 为 做 TNF视觉_识别区域 用 视觉_引擎、区域像素。
            若 TNF结果 不等于 空：
                元素 为 做 视觉识别_结果转元素 用 TNF结果、gx、gy、网格宽、网格高。
                若 元素 的 置信度 大于 50：
                    做 追加 用 状态 的 元素列表、元素。
                    若 元素 的 类型 等于 视觉_弹窗：
                        状态 的 有弹窗 为 真。
                    完。
                    若 元素 的 类型 等于 视觉_验证码 或者 元素 的 类型 等于 视觉_滑块：
                        状态 的 有验证码 为 真。
                    完。
                    若 元素 的 类型 等于 视觉_Cookie提示：
                        状态 的 有Cookie提示 为 真。
                    完。
                    若 元素 的 类型 等于 视觉_登录表单：
                        状态 的 有登录表单 为 真。
                    完。
                完。
            完。
            gx 为 gx 加 网格宽。
        完。
        gy 为 gy 加 网格高。
    完。
    状态 的 截图哈希 为 做 视觉识别_计算哈希 用 像素。
    返还 状态。
完。

视觉识别_获取截图像素 接受 零参：
    若 图处_上下文 不等于 空：
        返还 图处_上下文 的 后缓冲。
    完。
    返还 0。
完。

视觉识别_提取区域 接受 像素基址、x、y、宽、高、步长：
    可变 区域 为 一组：（空）。
    可变 py 为 0。
    只要 py 小于 高：
        行偏移 为 ((y 加 py) 乘 步长 加 x) 乘 4。
        可变 px 为 0。
        只要 px 小于 宽：
            像素偏移 为 行偏移 加 px 乘 4。
            r 为 做 内存读8 用 像素基址 加 像素偏移。
            g 为 做 内存读8 用 像素基址 加 像素偏移 加 1。
            b 为 做 内存读8 用 像素基址 加 像素偏移 加 2。
            做 追加 用 区域、r。
            做 追加 用 区域、g。
            做 追加 用 区域、b。
            px 为 px 加 4。
        完。
        py 为 py 加 4。
    完。
    返还 区域。
完。

视觉识别_结果转元素 接受 TNF结果、x、y、宽、高：
    元素 为 新建 视觉元素。
    元素 的 x 为 x。
    元素 的 y 为 y。
    元素 的 宽 为 宽。
    元素 的 高 为 高。
    元素 的 可交互 为 假。
    若 TNF结果 等于 0：
        元素 的 类型 为 视觉_按钮。
        元素 的 可交互 为 真。
    否则若 TNF结果 等于 1：
        元素 的 类型 为 视觉_输入框。
        元素 的 可交互 为 真。
    否则若 TNF结果 等于 2：
        元素 的 类型 为 视觉_文字。
    否则若 TNF结果 等于 3：
        元素 的 类型 为 视觉_图片。
    否则：
        元素 的 类型 为 视觉_未知。
    完。
    元素 的 置信度 为 70。
    返还 元素。
完。

视觉识别_OCR 接受 x、y、宽、高：
    视觉_总OCR次数 为 视觉_总OCR次数 加 1。
    结果 为 新建 OCR结果。
    结果 的 x 为 x。
    结果 的 y 为 y。
    结果 的 宽 为 宽。
    结果 的 高 为 高。
    像素 为 做 视觉识别_获取截图像素。
    区域 为 做 视觉识别_提取区域 用 像素、x、y、宽、高、图处_上下文 的 屏幕宽。
    结果 的 文字 为 「」。
    结果 的 置信度 为 60。
    返还 结果。
完。

视觉识别_查找按钮 接受 文字：
    状态 为 做 视觉识别_分析页面。
    可变 i 为 0。
    只要 i 小于 做 计数 用 状态 的 元素列表：
        元素 为 状态 的 元素列表 的 第 i 项。
        若 元素 的 类型 等于 视觉_按钮：
            ocr 为 做 视觉识别_OCR 用 元素 的 x、元素 的 y、元素 的 宽、元素 的 高。
            若 做 字符串查找 用 ocr 的 文字、文字 大于或等于 0：
                返还 元素。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

视觉识别_查找输入框 接受 零参：
    状态 为 做 视觉识别_分析页面。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 状态 的 元素列表：
        若 状态 的 元素列表 的 第 i 项 的 类型 等于 视觉_输入框：
            做 追加 用 结果、状态 的 元素列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 视觉_上次哈希 为 0。

视觉识别_页面已变化 接受 零参：
    像素 为 做 视觉识别_获取截图像素。
    当前哈希 为 做 视觉识别_计算哈希 用 像素。
    已变化 为 当前哈希 不等于 视觉_上次哈希。
    视觉_上次哈希 为 当前哈希。
    返还 已变化。
完。

视觉识别_计算哈希 接受 像素基址：
    可变 哈希 为 0。
    若 像素基址 等于 0：
        返还 0。
    完。
    可变 y 为 0。
    只要 y 小于 8：
        可变 x 为 0。
        只要 x 小于 8：
            偏移 为 (y 乘 100 乘 图处_上下文 的 屏幕宽 加 x 乘 160) 乘 4。
            亮度 为 做 内存读8 用 像素基址 加 偏移。
            哈希 为 哈希 乘 31 加 亮度。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 哈希。
完。

视觉识别_等待元素出现 接受 元素类型、超时毫秒：
    可变 已等待 为 0。
    只要 已等待 小于 超时毫秒：
        状态 为 做 视觉识别_分析页面。
        可变 i 为 0。
        只要 i 小于 做 计数 用 状态 的 元素列表：
            若 状态 的 元素列表 的 第 i 项 的 类型 等于 元素类型：
                返还 状态 的 元素列表 的 第 i 项。
            完。
            i 为 i 加 1。
        完。
        已等待 为 已等待 加 200。
    完。
    返还 空。
完。

视觉识别_统计 接受 零参：
    返还 一组：视觉_总识别次数、视觉_总OCR次数。
完。

本章 收束。
