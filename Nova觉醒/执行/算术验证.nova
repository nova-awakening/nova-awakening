《算术验证》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·内核打印》。

引用《.·真实点击》。

引用《..·中枢·字符串替换》。

引用《..·中枢·整数转字符串》。

引用《.·节点操作》。

引用《.·旋转验证》。

算术_最大重试 为 3。

算术_最小置信度 为 60。

OCR_最大字符 为 64。

算术验证统计 是 一种 事物，含 总尝试数、成功数、失败数、OCR失败数、计算错误数、重试次数、平均置信度、置信度总和。

可变 全局_算术统计 为 空。

算术统计_初始化 接受 零参：
    若 全局_算术统计 等于 空：
        全局_算术统计 为 新建 算术验证统计。
        全局_算术统计 的 总尝试数 为 0。
        全局_算术统计 的 成功数 为 0。
        全局_算术统计 的 失败数 为 0。
        全局_算术统计 的 OCR失败数 为 0。
        全局_算术统计 的 计算错误数 为 0。
        全局_算术统计 的 重试次数 为 0。
        全局_算术统计 的 平均置信度 为 0。
        全局_算术统计 的 置信度总和 为 0。
    完。
完。

验证破解_算术 接受 文档：
    做 算术统计_初始化。
    全局_算术统计 的 总尝试数 为 全局_算术统计 的 总尝试数 加 1。
    结果 为 新建 验证结果。
    结果 的 操作序列 为 一组：（空）。
    结果 的 成功 为 假。
    结果 的 置信度 为 0。
    可变 重试 为 0。
    只要 重试 小于 算术_最大重试：
        原始文本 为 做 验证破解_获取提示文字 用 文档。
        若 原始文本 等于 0 或者 做 字符串长度 用 原始文本 等于 0：
            全局_算术统计 的 OCR失败数 为 全局_算术统计 的 OCR失败数 加 1。
            重试 为 重试 加 1。
            全局_算术统计 的 重试次数 为 全局_算术统计 的 重试次数 加 1。
            继续。
        完。
        表达式 为 做 算术_预处理 用 原始文本。
        若 做 字符串长度 用 表达式 等于 0：
            重试 为 重试 加 1。
            全局_算术统计 的 重试次数 为 全局_算术统计 的 重试次数 加 1。
            继续。
        完。
        若 非 做 算术_验证表达式 用 表达式：
            全局_算术统计 的 计算错误数 为 全局_算术统计 的 计算错误数 加 1。
            重试 为 重试 加 1。
            继续。
        完。
        答案 为 做 算术_求值 用 表达式、0、做 字符串长度 用 表达式。
        若 答案 的 有效：
            答案文本 为 做 整数转字符串 用 答案 的 值。
            置信度 为 做 算术_计算置信度 用 原始文本、表达式。
            若 置信度 大于或等于 算术_最小置信度：
                输入事件 为 做 行为仿真_输入文字 用 答案文本。
                做 追加 用 结果 的 操作序列、一组：「仿真输入」、输入事件。
                结果 的 成功 为 真。
                结果 的 置信度 为 置信度。
                全局_算术统计 的 成功数 为 全局_算术统计 的 成功数 加 1。
                全局_算术统计 的 置信度总和 为 全局_算术统计 的 置信度总和 加 置信度。
                返还 结果。
            完。
        完。
        重试 为 重试 加 1。
        全局_算术统计 的 重试次数 为 全局_算术统计 的 重试次数 加 1。
    完。
    全局_算术统计 的 失败数 为 全局_算术统计 的 失败数 加 1。
    返还 结果。
完。

算术_预处理 接受 原始：
    若 原始 等于 0：
        返还 「」。
    完。
    可变 结果 为 「」。
    长度 为 做 字符串长度 用 原始。
    可变 i 为 0。
    只要 i 小于 长度 并且 i 小于 OCR_最大字符：
        c 为 做 字符串取字符 用 原始、i。
        若 c 大于或等于 48 并且 c 小于或等于 57：
            结果 为 做 字符串拼接 用 结果、做 字符转字符串 用 c。
        否则若 c 等于 43 或者 c 等于 45 或者 c 等于 42 或者 c 等于 47：
            结果 为 做 字符串拼接 用 结果、做 字符转字符串 用 c。
        否则若 c 等于 40 或者 c 等于 41：
            结果 为 做 字符串拼接 用 结果、做 字符转字符串 用 c。
        否则若 c 等于 120 或者 c 等于 88：
            结果 为 做 字符串拼接 用 结果、「*」。
        否则若 c 等于 247：
            结果 为 做 字符串拼接 用 结果、「/」。
        否则若 c 等于 215：
            结果 为 做 字符串拼接 用 结果、「*」。
        否则若 c 等于 32 或者 c 等于 61：
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

算术_验证表达式 接受 表达式：
    若 表达式 等于 0 或者 做 字符串长度 用 表达式 等于 0：
        返还 假。
    完。
    长度 为 做 字符串长度 用 表达式。
    可变 括号深度 为 0。
    可变 有数字 为 假。
    可变 i 为 0。
    只要 i 小于 长度：
        c 为 做 字符串取字符 用 表达式、i。
        若 c 大于或等于 48 并且 c 小于或等于 57：
            有数字 为 真。
        否则若 c 等于 40：
            括号深度 为 括号深度 加 1。
        否则若 c 等于 41：
            括号深度 为 括号深度 减 1。
            若 括号深度 小于 0：
                返还 假。
            完。
        否则若 c 等于 43 或者 c 等于 45 或者 c 等于 42 或者 c 等于 47：
        否则：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 有数字 并且 括号深度 等于 0。
完。

求值结果 是 一种 事物，含 值、有效、位置。

算术_求值 接受 表达式、起始、结束：
    返还 做 算术_求值_加减 用 表达式、起始、结束。
完。

算术_求值_加减 接受 表达式、起始、结束：
    左 为 做 算术_求值_乘除 用 表达式、起始、结束。
    若 非 左 的 有效：
        返还 左。
    完。
    可变 结果值 为 左 的 值。
    可变 位 为 左 的 位置。
    只要 位 小于 结束：
        c 为 做 字符串取字符 用 表达式、位。
        若 c 等于 43：
            右 为 做 算术_求值_乘除 用 表达式、位 加 1、结束。
            若 非 右 的 有效：
                返还 右。
            完。
            结果值 为 结果值 加 右 的 值。
            位 为 右 的 位置。
        否则若 c 等于 45：
            右 为 做 算术_求值_乘除 用 表达式、位 加 1、结束。
            若 非 右 的 有效：
                返还 右。
            完。
            结果值 为 结果值 减 右 的 值。
            位 为 右 的 位置。
        否则：
            跳出。
        完。
    完。
    r 为 新建 求值结果。
    r 的 值 为 结果值。
    r 的 有效 为 真。
    r 的 位置 为 位。
    返还 r。
完。

算术_求值_乘除 接受 表达式、起始、结束：
    左 为 做 算术_求值_原子 用 表达式、起始、结束。
    若 非 左 的 有效：
        返还 左。
    完。
    可变 结果值 为 左 的 值。
    可变 位 为 左 的 位置。
    只要 位 小于 结束：
        c 为 做 字符串取字符 用 表达式、位。
        若 c 等于 42：
            右 为 做 算术_求值_原子 用 表达式、位 加 1、结束。
            若 非 右 的 有效：
                返还 右。
            完。
            结果值 为 结果值 乘 右 的 值。
            位 为 右 的 位置。
        否则若 c 等于 47：
            右 为 做 算术_求值_原子 用 表达式、位 加 1、结束。
            若 非 右 的 有效：
                返还 右。
            完。
            若 右 的 值 等于 0：
                err 为 新建 求值结果。
                err 的 有效 为 假。
                返还 err。
            完。
            结果值 为 结果值 除以 右 的 值。
            位 为 右 的 位置。
        否则：
            跳出。
        完。
    完。
    r 为 新建 求值结果。
    r 的 值 为 结果值。
    r 的 有效 为 真。
    r 的 位置 为 位。
    返还 r。
完。

算术_求值_原子 接受 表达式、起始、结束：
    可变 位 为 起始。
    r 为 新建 求值结果。
    若 位 大于或等于 结束：
        r 的 有效 为 假。
        返还 r。
    完。
    c 为 做 字符串取字符 用 表达式、位。
    若 c 等于 40：
        可变 深度 为 1。
        可变 j 为 位 加 1。
        只要 j 小于 结束 并且 深度 大于 0：
            若 做 字符串取字符 用 表达式、j 等于 40：
                深度 为 深度 加 1。
            完。
            若 做 字符串取字符 用 表达式、j 等于 41：
                深度 为 深度 减 1。
            完。
            j 为 j 加 1。
        完。
        内部 为 做 算术_求值 用 表达式、位 加 1、j 减 1。
        r 的 值 为 内部 的 值。
        r 的 有效 为 内部 的 有效。
        r 的 位置 为 j。
        返还 r。
    完。
    可变 符号 为 1。
    若 c 等于 45：
        符号 为 负 1。
        位 为 位 加 1。
    完。
    可变 值 为 0。
    可变 有数字 为 假。
    只要 位 小于 结束：
        d 为 做 字符串取字符 用 表达式、位。
        若 d 大于或等于 48 并且 d 小于或等于 57：
            值 为 值 乘 10 加 (d 减 48)。
            有数字 为 真。
            位 为 位 加 1。
        否则：
            跳出。
        完。
    完。
    r 的 值 为 值 乘 符号。
    r 的 有效 为 有数字。
    r 的 位置 为 位。
    返还 r。
完。

验证破解_计算表达式 接受 表达式：
    若 表达式 等于 0 或者 做 字符串长度 用 表达式 等于 0：
        返还 「」。
    完。
    结果 为 做 算术_求值 用 表达式、0、做 字符串长度 用 表达式。
    若 结果 的 有效：
        返还 做 整数转字符串 用 结果 的 值。
    完。
    返还 「」。
完。

算术_计算置信度 接受 原始、清理后：
    若 原始 等于 0 或者 清理后 等于 0：
        返还 0。
    完。
    原始长 为 做 字符串长度 用 原始。
    清理长 为 做 字符串长度 用 清理后。
    若 原始长 等于 0：
        返还 0。
    完。
    保留率 为 清理长 乘 100 除以 原始长。
    若 保留率 大于 100：
        保留率 为 100。
    完。
    可变 置信度 为 保留率。
    若 清理长 小于或等于 7：
        置信度 为 置信度 加 10。
    完。
    若 置信度 大于 100：
        置信度 为 100。
    完。
    返还 置信度。
完。

验证破解_查找元素 接受 文档、关键词：
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档 的 节点池：
        节点 为 文档 的 节点池 的 第 i 项。
        cls 为 做 DOM_获取属性 用 文档、节点 的 ID、「class」。
        id 为 做 DOM_获取属性 用 文档、节点 的 ID、「id」。
        若 做 字符串查找 用 cls、关键词 大于或等于 0 或者 做 字符串查找 用 id、关键词 大于或等于 0：
            返还 一组：0、0。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

验证破解_查找元素_按标签 接受 文档、标签名、关键词：
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档 的 节点池：
        节点 为 文档 的 节点池 的 第 i 项。
        若 节点 的 标签名 等于 标签名：
            cls 为 做 DOM_获取属性 用 文档、节点 的 ID、「class」。
            若 做 字符串查找 用 cls、关键词 大于或等于 0：
                返还 一组：0、0。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

验证破解_查找输入框 接受 文档：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档 的 节点池：
        节点 为 文档 的 节点池 的 第 i 项。
        若 节点 的 标签名 等于 「input」：
            类型 为 做 DOM_获取属性 用 文档、节点 的 ID、「type」。
            若 类型 等于 「text」 或者 类型 等于 「number」 或者 类型 等于 「」：
                做 追加 用 结果、节点。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

验证破解_统计 接受 零参：
    做 算术统计_初始化。
    成功率 为 0。
    若 全局_算术统计 的 总尝试数 大于 0：
        成功率 为 全局_算术统计 的 成功数 乘 100 除以 全局_算术统计 的 总尝试数。
        全局_算术统计 的 平均置信度 为 全局_算术统计 的 置信度总和 除以 全局_算术统计 的 总尝试数。
    完。
    返还 一组：全局_算术统计 的 总尝试数、全局_算术统计 的 成功数、成功率。
完。

验证破解_打印统计 接受 零参：
    做 算术统计_初始化。
    做 打印行 用 「=== 算术验证统计 ===」。
    做 打印行 用 「总尝试: 」 加 做 整数转字符串 用 全局_算术统计 的 总尝试数。
    做 打印行 用 「成功数: 」 加 做 整数转字符串 用 全局_算术统计 的 成功数。
    做 打印行 用 「失败数: 」 加 做 整数转字符串 用 全局_算术统计 的 失败数。
    做 打印行 用 「OCR失败: 」 加 做 整数转字符串 用 全局_算术统计 的 OCR失败数。
    做 打印行 用 「计算错误: 」 加 做 整数转字符串 用 全局_算术统计 的 计算错误数。
    做 打印行 用 「重试次数: 」 加 做 整数转字符串 用 全局_算术统计 的 重试次数。
    若 全局_算术统计 的 总尝试数 大于 0：
        率 为 全局_算术统计 的 成功数 乘 100 除以 全局_算术统计 的 总尝试数。
        做 打印行 用 「成功率: 」 加 做 整数转字符串 用 率 加 「%」。
        做 打印行 用 「平均置信度: 」 加 做 整数转字符串 用 全局_算术统计 的 平均置信度。
    完。
完。

OCR_纠错_数字 接受 ch：
    若 ch 等于 79 或者 ch 等于 111：
        返还 48。
    完。
    若 ch 等于 108 或者 ch 等于 73：
        返还 49。
    完。
    若 ch 等于 83 或者 ch 等于 115：
        返还 53。
    完。
    若 ch 等于 66：
        返还 56。
    完。
    若 ch 等于 90 或者 ch 等于 122：
        返还 50。
    完。
    若 ch 等于 71：
        返还 54。
    完。
    返还 ch。
完。

OCR_纠错_运算符 接受 ch：
    若 ch 等于 120 或者 ch 等于 88：
        返还 42。
    完。
    若 ch 等于 247：
        返还 47。
    完。
    若 ch 等于 215：
        返还 42。
    完。
    返还 ch。
完。

OCR_全自动纠错 接受 文本：
    若 文本 等于 0：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    len 为 做 字符串长度 用 文本。
    只要 i 小于 len：
        ch 为 做 字符串取字符 用 文本、i。
        若 ch 大于或等于 48 并且 ch 小于或等于 57：
            结果 为 结果 加 做 字符串从字节 用 ch。
        否则若 ch 等于 43 或者 ch 等于 45 或者 ch 等于 42 或者 ch 等于 47：
            结果 为 结果 加 做 字符串从字节 用 ch。
        否则：
            纠正 为 做 OCR_纠错_数字 用 ch。
            若 纠正 不等于 ch：
                结果 为 结果 加 做 字符串从字节 用 纠正。
            否则：
                纠正 为 做 OCR_纠错_运算符 用 ch。
                若 纠正 不等于 ch：
                    结果 为 结果 加 做 字符串从字节 用 纠正。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

表达式_复杂度 接受 表达式：
    若 表达式 等于 0：
        返还 0。
    完。
    可变 ops 为 0。
    可变 depth 为 0。
    可变 max_depth 为 0。
    可变 i 为 0。
    len 为 做 字符串长度 用 表达式。
    只要 i 小于 len：
        ch 为 做 字符串取字符 用 表达式、i。
        若 ch 等于 43 或者 ch 等于 45 或者 ch 等于 42 或者 ch 等于 47：
            ops 为 ops 加 1。
        完。
        若 ch 等于 40：
            depth 为 depth 加 1。
        完。
        若 ch 等于 41：
            depth 为 depth 减 1。
        完。
        若 depth 大于 max_depth：
            max_depth 为 depth。
        完。
        i 为 i 加 1。
    完。
    返还 ops 乘 10 加 max_depth。
完。

验证破解_重置统计 接受 零参：
    做 算术统计_初始化。
    全局_算术统计 的 总尝试数 为 0。
    全局_算术统计 的 成功数 为 0。
    全局_算术统计 的 失败数 为 0。
    全局_算术统计 的 OCR失败数 为 0。
    全局_算术统计 的 计算错误数 为 0。
    全局_算术统计 的 重试次数 为 0。
    全局_算术统计 的 置信度总和 为 0。
    全局_算术统计 的 平均置信度 为 0。
完。

本章 收束。
