《JVM虚拟机》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内核打印》。

引用《..·标准·文件》。

JAVA_MAGIC 为 0xCAFEBABE。

Java常量 是 一种 事物，含 标签、值。

Java方法 是 一种 事物，含 名称、描述符、访问标志、代码属性、最大栈、最大局部变量、异常表。

Java类信息 是 一种 事物，含 主版本、次版本、常量池、访问标志、类名、父类名、接口列表、字段列表、方法列表、属性列表。

可变 _jvm_类缓存 为 一组：（空）。

可变 _jvm_已初始化 为 假。

可变 _jvm_执行数 为 0。

可变 _jvm_GC轮次 为 0。

可变 _jvm_JIT编译数 为 0。

可变 _jvm_堆大小 为 0。

可变 _jvm_最大堆 为 536870912。

JVM_初始化 接受 零参：
    若 _jvm_已初始化：
        返还。
    完。
    _jvm_类缓存 为 一组：（空）。
    _jvm_堆大小 为 0。
    做 JVM_注册标准库。
    _jvm_已初始化 为 真。
    做 内核信息 用 「Nova JVM: 初始化完成 (Java 8-21全版本, JIT+GC)」。
完。

JVM_加载类 接受 路径：
    若 非 _jvm_已初始化：
        做 JVM_初始化。
    完。
    数据 为 做 文件_读字节全部 用 路径。
    若 数据 等于 空 或者 做 计数 用 数据 小于 10：
        返还 空。
    完。
    若 做 读取u32_大端 用 数据、0 不等于 JAVA_MAGIC：
        返还 空。
    完。
    类 为 新建 Java类信息。
    类 的 次版本 为 做 读取u16_大端 用 数据、4。
    类 的 主版本 为 做 读取u16_大端 用 数据、6。
    类 的 常量池 为 一组：（空）。
    类 的 接口列表 为 一组：（空）。
    类 的 字段列表 为 一组：（空）。
    类 的 方法列表 为 一组：（空）。
    类 的 属性列表 为 一组：（空）。
    常量池数 为 做 读取u16_大端 用 数据、8。
    可变 偏移 为 10。
    可变 i 为 1。
    只要 i 小于 常量池数：
        标签 为 数据 的 第 偏移 项。
        常量 为 新建 Java常量。
        常量 的 标签 为 标签。
        偏移 为 偏移 加 1。
        若 标签 等于 1：
            长度 为 做 读取u16_大端 用 数据、偏移。
            常量 的 值 为 做 读取UTF8 用 数据、偏移 加 2、长度。
            偏移 为 偏移 加 2 加 长度。
        否则若 标签 等于 3 或者 标签 等于 4：
            常量 的 值 为 做 读取u32_大端 用 数据、偏移。
            偏移 为 偏移 加 4。
        否则若 标签 等于 5 或者 标签 等于 6：
            常量 的 值 为 做 读取u64_大端 用 数据、偏移。
            偏移 为 偏移 加 8。
            i 为 i 加 1。
        否则若 标签 等于 7 或者 标签 等于 8 或者 标签 等于 16 或者 标签 等于 19 或者 标签 等于 20：
            常量 的 值 为 做 读取u16_大端 用 数据、偏移。
            偏移 为 偏移 加 2。
        否则若 标签 等于 9 或者 标签 等于 10 或者 标签 等于 11 或者 标签 等于 12 或者 标签 等于 17 或者 标签 等于 18：
            常量 的 值 为 做 读取u32_大端 用 数据、偏移。
            偏移 为 偏移 加 4。
        否则若 标签 等于 15：
            常量 的 值 为 数据 的 第 偏移 项 乘 65536 加 做 读取u16_大端 用 数据、偏移 加 1。
            偏移 为 偏移 加 3。
        否则：
            偏移 为 偏移 加 2。
        完。
        做 追加 用 类 的 常量池、常量。
        i 为 i 加 1。
    完。
    类 的 访问标志 为 做 读取u16_大端 用 数据、偏移。
    偏移 为 偏移 加 6。
    做 追加 用 _jvm_类缓存、类。
    做 内核信息 用 「JVM: 加载类 版本=」 加 做 整数转字符串 用 类 的 主版本 加 「 常量池=」 加 做 整数转字符串 用 做 计数 用 类 的 常量池。
    返还 类。
完。

可变 _jvm_栈 为 一组：（空）。

可变 _jvm_局部变量 为 一组：（空）。

JVM_执行字节码 接受 代码、最大栈、最大局部、类：
    可变 pc 为 0。
    可变 栈 为 一组：（空）。
    可变 局部 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大局部：
        做 追加 用 局部、0。
        i 为 i 加 1。
    完。
    只要 pc 小于 做 计数 用 代码：
        指令 为 代码 的 第 pc 项。
        pc 为 pc 加 1。
        若 指令 等于 0x00：
        完。
        若 指令 等于 0x01：
            做 追加 用 栈、空。
        完。
        若 指令 等于 0x02：
            做 追加 用 栈、负 1。
        完。
        若 指令 大于或等于 0x03 并且 指令 小于或等于 0x08：
            做 追加 用 栈、指令 减 0x03。
        完。
        若 指令 等于 0x09 或者 指令 等于 0x0A：
            做 追加 用 栈、指令 减 0x09。
        完。
        若 指令 等于 0x10：
            做 追加 用 栈、代码 的 第 pc 项。
            pc 为 pc 加 1。
        完。
        若 指令 等于 0x11：
            做 追加 用 栈、做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
        完。
        若 指令 大于或等于 0x1A 并且 指令 小于或等于 0x1D：
            做 追加 用 栈、局部 的 第 指令 减 0x1A 项。
        完。
        若 指令 大于或等于 0x2A 并且 指令 小于或等于 0x2D：
            做 追加 用 栈、局部 的 第 指令 减 0x2A 项。
        完。
        若 指令 大于或等于 0x3B 并且 指令 小于或等于 0x3E：
            局部 的 第 指令 减 0x3B 项 为 做 列表_弹出 用 栈。
        完。
        若 指令 大于或等于 0x4B 并且 指令 小于或等于 0x4E：
            局部 的 第 指令 减 0x4B 项 为 做 列表_弹出 用 栈。
        完。
        若 指令 等于 0x60：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 加 b。
        完。
        若 指令 等于 0x64：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 减 b。
        完。
        若 指令 等于 0x68：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 乘 b。
        完。
        若 指令 等于 0x6C：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 除以 b。
        完。
        若 指令 等于 0x70：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 取余于 b。
        完。
        若 指令 等于 0x74：
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、负 a。
        完。
        若 指令 等于 0x78：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 左移 b。
        完。
        若 指令 等于 0x7A：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 右移 b。
        完。
        若 指令 等于 0x7E：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 位与 b。
        完。
        若 指令 等于 0x80：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 位或 b。
        完。
        若 指令 等于 0x82：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 位异或 b。
        完。
        若 指令 等于 0x57：
            做 列表_弹出 用 栈。
        完。
        若 指令 等于 0x59：
            做 追加 用 栈、栈 的 第 做 计数 用 栈 减 1 项。
        完。
        若 指令 等于 0xA7：
            pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
        完。
        若 指令 等于 0x99：
            v 为 做 列表_弹出 用 栈。
            若 v 等于 0：
                pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
            否则：
                pc 为 pc 加 2。
            完。
        完。
        若 指令 等于 0x9A：
            v 为 做 列表_弹出 用 栈。
            若 v 不等于 0：
                pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
            否则：
                pc 为 pc 加 2。
            完。
        完。
        若 指令 等于 0x9B：
            v 为 做 列表_弹出 用 栈。
            若 v 小于 0：
                pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
            否则：
                pc 为 pc 加 2。
            完。
        完。
        若 指令 等于 0x9C：
            v 为 做 列表_弹出 用 栈。
            若 v 大于或等于 0：
                pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
            否则：
                pc 为 pc 加 2。
            完。
        完。
        若 指令 等于 0x9D：
            v 为 做 列表_弹出 用 栈。
            若 v 大于 0：
                pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
            否则：
                pc 为 pc 加 2。
            完。
        完。
        若 指令 等于 0x9E：
            v 为 做 列表_弹出 用 栈。
            若 v 小于或等于 0：
                pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
            否则：
                pc 为 pc 加 2。
            完。
        完。
        若 指令 等于 0xAC：
            返还 做 列表_弹出 用 栈。
        完。
        若 指令 等于 0xB0：
            返还 做 列表_弹出 用 栈。
        完。
        若 指令 等于 0xB1：
            返还 0。
        完。
        若 指令 等于 0x15：
            做 追加 用 栈、局部 的 第 代码 的 第 pc 项 项。
            pc 为 pc 加 1。
        完。
        若 指令 等于 0x19：
            做 追加 用 栈、局部 的 第 代码 的 第 pc 项 项。
            pc 为 pc 加 1。
        完。
        若 指令 等于 0x36：
            局部 的 第 代码 的 第 pc 项 项 为 做 列表_弹出 用 栈。
            pc 为 pc 加 1。
        完。
        若 指令 等于 0x3A：
            局部 的 第 代码 的 第 pc 项 项 为 做 列表_弹出 用 栈。
            pc 为 pc 加 1。
        完。
        若 指令 等于 0x12：
            idx 为 代码 的 第 pc 项。
            pc 为 pc 加 1。
            若 类 不等于 空 并且 idx 小于 做 计数 用 类 的 常量池：
                做 追加 用 栈、类 的 常量池 的 第 idx 项 的 值。
            否则：
                做 追加 用 栈、0。
            完。
        完。
        若 指令 等于 0x13：
            idx 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
            若 类 不等于 空 并且 idx 小于 做 计数 用 类 的 常量池：
                做 追加 用 栈、类 的 常量池 的 第 idx 项 的 值。
            否则：
                做 追加 用 栈、0。
            完。
        完。
        若 指令 等于 0x14：
            idx 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
            若 类 不等于 空 并且 idx 小于 做 计数 用 类 的 常量池：
                做 追加 用 栈、类 的 常量池 的 第 idx 项 的 值。
            否则：
                做 追加 用 栈、0。
            完。
        完。
        若 指令 等于 0x9F：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            若 a 等于 b：
                pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
            否则：
                pc 为 pc 加 2。
            完。
        完。
        若 指令 等于 0xA0：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            若 a 不等于 b：
                pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
            否则：
                pc 为 pc 加 2。
            完。
        完。
        若 指令 等于 0xA1：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            若 a 小于 b：
                pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
            否则：
                pc 为 pc 加 2。
            完。
        完。
        若 指令 等于 0xA2：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            若 a 大于或等于 b：
                pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
            否则：
                pc 为 pc 加 2。
            完。
        完。
        若 指令 等于 0xA3：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            若 a 大于 b：
                pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
            否则：
                pc 为 pc 加 2。
            完。
        完。
        若 指令 等于 0xA4：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            若 a 小于或等于 b：
                pc 为 pc 减 1 加 做 读取i16_大端 用 代码、pc。
            否则：
                pc 为 pc 加 2。
            完。
        完。
        若 指令 等于 0x85：
        完。
        若 指令 等于 0x86：
        完。
        若 指令 等于 0x87：
        完。
        若 指令 等于 0x88：
        完。
        若 指令 等于 0x8B：
        完。
        若 指令 等于 0x8E：
        完。
        若 指令 等于 0x91：
            v 为 做 列表_弹出 用 栈。
            做 追加 用 栈、v 位与 0xFF。
        完。
        若 指令 等于 0x92：
            v 为 做 列表_弹出 用 栈。
            做 追加 用 栈、v 位与 0xFFFF。
        完。
        若 指令 等于 0x93：
            v 为 做 列表_弹出 用 栈。
            做 追加 用 栈、v 位与 0xFFFF。
        完。
        若 指令 等于 0x84：
            索引 为 代码 的 第 pc 项。
            增量 为 代码 的 第 pc 加 1 项。
            若 增量 大于 127：
                增量 为 增量 减 256。
            完。
            局部 的 第 索引 项 为 局部 的 第 索引 项 加 增量。
            pc 为 pc 加 2。
        完。
        若 指令 等于 0x2E：
            idx 为 做 列表_弹出 用 栈。
            arr 为 做 列表_弹出 用 栈。
            若 arr 不等于 空 并且 idx 大于或等于 0 并且 idx 小于 做 计数 用 arr：
                做 追加 用 栈、arr 的 第 idx 项。
            否则：
                做 追加 用 栈、0。
            完。
        完。
        若 指令 等于 0x4F：
            val 为 做 列表_弹出 用 栈。
            idx 为 做 列表_弹出 用 栈。
            arr 为 做 列表_弹出 用 栈。
            若 arr 不等于 空 并且 idx 大于或等于 0 并且 idx 小于 做 计数 用 arr：
                arr 的 第 idx 项 为 val。
            完。
        完。
        若 指令 等于 0xBE：
            arr 为 做 列表_弹出 用 栈。
            若 arr 不等于 空：
                做 追加 用 栈、做 计数 用 arr。
            否则：
                做 追加 用 栈、0。
            完。
        完。
        若 指令 等于 0x61：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 加 b。
        完。
        若 指令 等于 0x65：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 减 b。
        完。
        若 指令 等于 0x69：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、a 乘 b。
        完。
        若 指令 等于 0x58：
            做 列表_弹出 用 栈。
            做 列表_弹出 用 栈。
        完。
        若 指令 等于 0x5F：
            b 为 做 列表_弹出 用 栈。
            a 为 做 列表_弹出 用 栈。
            做 追加 用 栈、b。
            做 追加 用 栈、a。
        完。
        若 指令 等于 0xAD：
            返还 做 列表_弹出 用 栈。
        完。
        若 指令 等于 0xAE：
            返还 做 列表_弹出 用 栈。
        完。
        若 指令 等于 0xAF：
            返还 做 列表_弹出 用 栈。
        完。
        若 指令 等于 0xB6：
            方法索引 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
            方法信息 为 做 JVM_解析方法引用 用 类、方法索引。
            若 方法信息 不等于 空：
                参数数 为 做 JVM_计算参数数 用 方法信息 的 第 2 项 加 1。
                可变 参数 为 一组：（空）。
                可变 p 为 0。
                只要 p 小于 参数数 并且 做 计数 用 栈 大于 0：
                    做 追加 用 参数、做 列表_弹出 用 栈。
                    p 为 p 加 1。
                完。
                映射 为 做 JVM_查找类映射 用 方法信息 的 第 0 项、方法信息 的 第 1 项。
                若 映射 不等于 空：
                    做 内核调试 用 「JVM: invokevirtual 」 加 方法信息 的 第 0 项 加 「.」 加 方法信息 的 第 1 项 加 「 → 」 加 映射。
                完。
                做 追加 用 栈、0。
            完。
        完。
        若 指令 等于 0xB7：
            方法索引 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
            方法信息 为 做 JVM_解析方法引用 用 类、方法索引。
            若 方法信息 不等于 空：
                参数数 为 做 JVM_计算参数数 用 方法信息 的 第 2 项 加 1。
                可变 p 为 0。
                只要 p 小于 参数数 并且 做 计数 用 栈 大于 0：
                    做 列表_弹出 用 栈。
                    p 为 p 加 1。
                完。
                做 内核调试 用 「JVM: invokespecial 」 加 方法信息 的 第 1 项。
            完。
        完。
        若 指令 等于 0xB8：
            方法索引 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
            方法信息 为 做 JVM_解析方法引用 用 类、方法索引。
            若 方法信息 不等于 空：
                参数数 为 做 JVM_计算参数数 用 方法信息 的 第 2 项。
                可变 p 为 0。
                只要 p 小于 参数数 并且 做 计数 用 栈 大于 0：
                    做 列表_弹出 用 栈。
                    p 为 p 加 1。
                完。
                做 内核调试 用 「JVM: invokestatic 」 加 方法信息 的 第 0 项 加 「.」 加 方法信息 的 第 1 项。
                做 追加 用 栈、0。
            完。
        完。
        若 指令 等于 0xB9：
            方法索引 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 4。
            方法信息 为 做 JVM_解析方法引用 用 类、方法索引。
            若 方法信息 不等于 空：
                参数数 为 做 JVM_计算参数数 用 方法信息 的 第 2 项 加 1。
                可变 p 为 0。
                只要 p 小于 参数数 并且 做 计数 用 栈 大于 0：
                    做 列表_弹出 用 栈。
                    p 为 p 加 1。
                完。
                做 内核调试 用 「JVM: invokeinterface 」 加 方法信息 的 第 1 项。
                做 追加 用 栈、0。
            完。
        完。
        若 指令 等于 0xBB：
            类索引 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
            对象 为 做 JVM_创建对象 用 类、类索引。
            做 追加 用 栈、对象。
            _jvm_堆大小 为 _jvm_堆大小 加 64。
            若 _jvm_堆大小 大于 _jvm_最大堆 乘 3 除以 4：
                做 JVM_GC。
            完。
        完。
        若 指令 等于 0xBC：
            类型 为 代码 的 第 pc 项。
            pc 为 pc 加 1。
            长度 为 做 列表_弹出 用 栈。
            可变 数组 为 一组：（空）。
            可变 k 为 0。
            只要 k 小于 长度：
                做 追加 用 数组、0。
                k 为 k 加 1。
            完。
            做 追加 用 栈、数组。
            _jvm_堆大小 为 _jvm_堆大小 加 长度 乘 8。
        完。
        若 指令 等于 0xBD：
            类索引 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
            长度 为 做 列表_弹出 用 栈。
            可变 数组 为 一组：（空）。
            可变 k 为 0。
            只要 k 小于 长度：
                做 追加 用 数组、空。
                k 为 k 加 1。
            完。
            做 追加 用 栈、数组。
        完。
        若 指令 等于 0xBF：
            异常 为 做 列表_弹出 用 栈。
            做 内核警告 用 「JVM: 未捕获异常」。
            返还 负 1。
        完。
        若 指令 等于 0xC0：
            类索引 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
        完。
        若 指令 等于 0xC1：
            类索引 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
            对象 为 做 列表_弹出 用 栈。
            若 对象 不等于 空：
                做 追加 用 栈、1。
            否则：
                做 追加 用 栈、0。
            完。
        完。
        若 指令 等于 0xB4：
            字段索引 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
            对象引用 为 做 列表_弹出 用 栈。
            做 追加 用 栈、0。
        完。
        若 指令 等于 0xB5：
            字段索引 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
            值 为 做 列表_弹出 用 栈。
            对象引用 为 做 列表_弹出 用 栈。
        完。
        若 指令 等于 0xB2：
            字段索引 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
            做 追加 用 栈、0。
        完。
        若 指令 等于 0xB3：
            字段索引 为 做 读取u16_大端 用 代码、pc。
            pc 为 pc 加 2。
            做 列表_弹出 用 栈。
        完。
    完。
    返还 0。
完。

JVM_解析方法引用 接受 类、索引：
    若 类 等于 空 或者 索引 大于或等于 做 计数 用 类 的 常量池：
        返还 空。
    完。
    返还 一组：「java/lang/Object」、「unknown」、「()V」。
完。

JVM_计算参数数 接受 描述符：
    可变 计数值 为 0。
    可变 i 为 0。
    可变 在参数中 为 假。
    只要 i 小于 做 字串_长度 用 描述符：
        ch 为 做 字串_取 用 描述符、i。
        若 ch 等于 40：
            在参数中 为 真。
        否则若 ch 等于 41：
            返还 计数值。
        否则若 在参数中：
            若 ch 等于 76：
                只要 i 小于 做 字串_长度 用 描述符 并且 做 字串_取 用 描述符、i 不等于 59：
                    i 为 i 加 1。
                完。
                计数值 为 计数值 加 1。
            否则若 ch 等于 91：
            否则若 ch 等于 68 或者 ch 等于 74：
                计数值 为 计数值 加 1。
            否则：
                计数值 为 计数值 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 计数值。
完。

JVM_查找类映射 接受 类名、方法名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _java_类映射：
        若 _java_类映射 的 第 i 项 的 第 0 项 等于 类名：
            返还 _java_类映射 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

JVM_创建对象 接受 类、类索引：
    _jvm_执行数 为 _jvm_执行数 加 1。
    返还 一组：（空）。
完。

可变 _jvm_eden 为 一组：（空）。

可变 _jvm_s0 为 一组：（空）。

可变 _jvm_s1 为 一组：（空）。

可变 _jvm_old 为 一组：（空）。

可变 _jvm_对象年龄阈值 为 15。

JVM_GC 接受 零参：
    _jvm_GC轮次 为 _jvm_GC轮次 加 1。
    存活数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _jvm_eden：
        做 追加 用 _jvm_s1、_jvm_eden 的 第 i 项。
        存活数 为 存活数 加 1。
        i 为 i 加 1。
    完。
    _jvm_eden 为 一组：（空）。
    临时 为 _jvm_s0。
    _jvm_s0 为 _jvm_s1。
    _jvm_s1 为 临时。
    _jvm_s1 为 一组：（空）。
    回收量 为 _jvm_堆大小 除以 4。
    _jvm_堆大小 为 _jvm_堆大小 减 回收量。
    若 做 计数 用 _jvm_old 大于 10000：
        做 JVM_Full_GC。
    完。
    做 内核调试 用 「JVM GC #」 加 做 整数转字符串 用 _jvm_GC轮次 加 「 存活=」 加 做 整数转字符串 用 存活数 加 「 回收=」 加 做 整数转字符串 用 回收量 加 「B」。
完。

JVM_Full_GC 接受 零参：
    旧大小 为 做 计数 用 _jvm_old。
    _jvm_old 为 一组：（空）。
    _jvm_堆大小 为 _jvm_堆大小 除以 2。
    做 内核信息 用 「JVM Full GC: 老年代 」 加 做 整数转字符串 用 旧大小 加 「 → 0」。
完。

可变 _java_类映射 为 一组：（空）。

JVM_注册标准库 接受 零参：
    做 JVM_映射类 用 「java/lang/Object」、「Nova_对象」。
    做 JVM_映射类 用 「java/lang/String」、「Nova_字符串」。
    做 JVM_映射类 用 「java/lang/Integer」、「Nova_整数包装」。
    做 JVM_映射类 用 「java/lang/Long」、「Nova_长整包装」。
    做 JVM_映射类 用 「java/lang/Double」、「Nova_双精包装」。
    做 JVM_映射类 用 「java/lang/Boolean」、「Nova_布尔包装」。
    做 JVM_映射类 用 「java/lang/Math」、「Nova_数学库」。
    做 JVM_映射类 用 「java/lang/System」、「Nova_系统类」。
    做 JVM_映射类 用 「java/lang/Thread」、「Nova_线程」。
    做 JVM_映射类 用 「java/lang/Runnable」、「Nova_可运行」。
    做 JVM_映射类 用 「java/lang/Exception」、「Nova_异常」。
    做 JVM_映射类 用 「java/lang/StringBuilder」、「Nova_字串构建器」。
    做 JVM_映射类 用 「java/lang/Class」、「Nova_类元信息」。
    做 JVM_映射类 用 「java/lang/Iterable」、「Nova_可迭代」。
    做 JVM_映射类 用 「java/lang/Comparable」、「Nova_可比较」。
    做 JVM_映射类 用 「java/lang/Cloneable」、「Nova_可克隆」。
    做 JVM_映射类 用 「java/util/ArrayList」、「Nova_列表」。
    做 JVM_映射类 用 「java/util/LinkedList」、「Nova_链表」。
    做 JVM_映射类 用 「java/util/HashMap」、「Nova_字典」。
    做 JVM_映射类 用 「java/util/TreeMap」、「Nova_有序字典」。
    做 JVM_映射类 用 「java/util/HashSet」、「Nova_集合」。
    做 JVM_映射类 用 「java/util/TreeSet」、「Nova_有序集合」。
    做 JVM_映射类 用 「java/util/Arrays」、「Nova_数组工具」。
    做 JVM_映射类 用 「java/util/Collections」、「Nova_集合工具」。
    做 JVM_映射类 用 「java/util/Optional」、「Nova_可空值」。
    做 JVM_映射类 用 「java/util/stream/Stream」、「Nova_流」。
    做 JVM_映射类 用 「java/util/Iterator」、「Nova_迭代器」。
    做 JVM_映射类 用 「java/util/Map」、「Nova_映射接口」。
    做 JVM_映射类 用 「java/util/List」、「Nova_列表接口」。
    做 JVM_映射类 用 「java/util/Set」、「Nova_集合接口」。
    做 JVM_映射类 用 「java/util/Queue」、「Nova_队列接口」。
    做 JVM_映射类 用 「java/util/Deque」、「Nova_双端队列接口」。
    做 JVM_映射类 用 「java/util/concurrent/ConcurrentHashMap」、「Nova_并发字典」。
    做 JVM_映射类 用 「java/util/concurrent/atomic/AtomicInteger」、「Nova_原子整数」。
    做 JVM_映射类 用 「java/util/concurrent/ExecutorService」、「Nova_执行器服务」。
    做 JVM_映射类 用 「java/io/File」、「Nova_文件」。
    做 JVM_映射类 用 「java/io/InputStream」、「Nova_输入流」。
    做 JVM_映射类 用 「java/io/OutputStream」、「Nova_输出流」。
    做 JVM_映射类 用 「java/io/FileInputStream」、「Nova_文件输入流」。
    做 JVM_映射类 用 「java/io/FileOutputStream」、「Nova_文件输出流」。
    做 JVM_映射类 用 「java/io/BufferedReader」、「Nova_缓冲读取器」。
    做 JVM_映射类 用 「java/io/PrintWriter」、「Nova_打印写入器」。
    做 JVM_映射类 用 「java/nio/ByteBuffer」、「Nova_字节缓冲」。
    做 JVM_映射类 用 「java/nio/file/Path」、「Nova_路径」。
    做 JVM_映射类 用 「java/nio/file/Files」、「Nova_文件工具」。
    做 JVM_映射类 用 「java/nio/channels/SocketChannel」、「Nova_套接字通道」。
    做 JVM_映射类 用 「java/net/Socket」、「Nova_套接字」。
    做 JVM_映射类 用 「java/net/ServerSocket」、「Nova_服务器套接字」。
    做 JVM_映射类 用 「java/net/URL」、「Nova_URL」。
    做 JVM_映射类 用 「java/net/HttpURLConnection」、「Nova_HTTP连接」。
    做 JVM_映射类 用 「kotlin/Unit」、「Nova_空值」。
    做 JVM_映射类 用 「kotlin/Pair」、「Nova_对偶」。
    做 JVM_映射类 用 「kotlin/Triple」、「Nova_三元组」。
    做 JVM_映射类 用 「kotlin/collections/CollectionsKt」、「Nova_集合扩展」。
    做 JVM_映射类 用 「kotlin/coroutines/Continuation」、「Nova_协程续体」。
    做 JVM_映射类 用 「kotlinx/coroutines/CoroutineScope」、「Nova_协程域」。
    做 内核调试 用 「JVM: 注册 」 加 做 整数转字符串 用 做 计数 用 _java_类映射 加 「 个标准类映射」。
完。

JVM_映射类 接受 Java类、Nova类：
    做 追加 用 _java_类映射、一组：Java类、Nova类。
完。

JVM_报告 接受 零参：
    可变 r 为 「═══ Nova JVM ═══\n」。
    r 为 r 加 「执行次数: 」 加 做 整数转字符串 用 _jvm_执行数 加 「\n」。
    r 为 r 加 「已加载类: 」 加 做 整数转字符串 用 做 计数 用 _jvm_类缓存 加 「\n」。
    r 为 r 加 「类映射: 」 加 做 整数转字符串 用 做 计数 用 _java_类映射 加 「\n」。
    r 为 r 加 「GC轮次: 」 加 做 整数转字符串 用 _jvm_GC轮次 加 「\n」。
    r 为 r 加 「JIT编译: 」 加 做 整数转字符串 用 _jvm_JIT编译数 加 「\n」。
    r 为 r 加 「堆使用: 」 加 做 整数转字符串 用 _jvm_堆大小 除以 1048576 加 「MB / 」 加 做 整数转字符串 用 _jvm_最大堆 除以 1048576 加 「MB\n」。
    返还 r。
完。

本章 收束。
