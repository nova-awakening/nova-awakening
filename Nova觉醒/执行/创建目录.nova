《创建目录》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·标准·文件》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内核打印》。

引用《..·标准·原生字符串》。

MKDIR_递归 为 0x01。

MKDIR_详细 为 0x02。

MKDIR_上下文 为 0x04。

mkdir_统计 是 一种 事物，含 创建数、跳过数、错误数。

mkdir_main 接受 参数：
    可变 选项 为 0。
    可变 权限 为 493。
    可变 目录列表 为 一组：（空）。
    可变 安全上下文 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        a 为 参数 的 第 i 项。
        若 a 等于 「-p」 或者 a 等于 「--parents」：
            选项 为 选项 位或 MKDIR_递归。
        否则若 a 等于 「-v」 或者 a 等于 「--verbose」：
            选项 为 选项 位或 MKDIR_详细。
        否则若 a 等于 「-m」 或者 a 等于 「--mode」：
            若 i 加 1 小于 做 计数 用 参数：
                i 为 i 加 1。
                权限 为 做 mkdir_解析权限 用 参数 的 第 i 项。
            否则：
                做 打印行 用 「mkdir: 选项 '-m' 需要参数」。
                返还 1。
            完。
        否则若 做 字符串开头是 用 a、「--mode=」：
            权限 为 做 mkdir_解析权限 用 做 字符串子串 用 a、7、做 字符串长度 用 a。
        否则若 a 等于 「-Z」 或者 a 等于 「--context」：
            选项 为 选项 位或 MKDIR_上下文。
            若 i 加 1 小于 做 计数 用 参数 并且 非 做 字符串开头是 用 参数 的 第 i 加 1 项、「-」：
                i 为 i 加 1。
                安全上下文 为 参数 的 第 i 项。
            完。
        否则若 a 等于 「--help」：
            做 mkdir_帮助。
            返还 0。
        否则若 a 等于 「--version」：
            做 打印行 用 「mkdir (NovaOS) 1.0」。
            返还 0。
        否则若 a 等于 「--」：
            i 为 i 加 1。
            跳出。
        否则若 做 字符串开头是 用 a、「-」 并且 做 字符串长度 用 a 大于 1：
            可变 j 为 1。
            只要 j 小于 做 字符串长度 用 a：
                c 为 做 字符串取字符 用 a、j。
                若 c 等于 「p」：
                    选项 为 选项 位或 MKDIR_递归。
                否则若 c 等于 「v」：
                    选项 为 选项 位或 MKDIR_详细。
                否则若 c 等于 「Z」：
                    选项 为 选项 位或 MKDIR_上下文。
                否则若 c 等于 「m」：
                    若 j 加 1 小于 做 字符串长度 用 a：
                        权限 为 做 mkdir_解析权限 用 做 字符串子串 用 a、j 加 1、做 字符串长度 用 a。
                        跳出。
                    否则若 i 加 1 小于 做 计数 用 参数：
                        i 为 i 加 1。
                        权限 为 做 mkdir_解析权限 用 参数 的 第 i 项。
                        跳出。
                    完。
                否则：
                    做 打印行 用 「mkdir: 无效选项 '-」 加 c 加 「'」。
                    返还 1。
                完。
                j 为 j 加 1。
            完。
        否则：
            做 追加 用 目录列表、a。
        完。
        i 为 i 加 1。
    完。
    只要 i 小于 做 计数 用 参数：
        做 追加 用 目录列表、参数 的 第 i 项。
        i 为 i 加 1。
    完。
    若 做 计数 用 目录列表 等于 0：
        做 打印行 用 「mkdir: 缺少操作数」。
        做 打印行 用 「请尝试 'mkdir --help' 以获取更多信息」。
        返还 1。
    完。
    统计 为 做 堆_分配 用 mkdir_统计。
    统计 的 创建数 为 0。
    统计 的 跳过数 为 0。
    统计 的 错误数 为 0。
    可变 k 为 0。
    只要 k 小于 做 计数 用 目录列表：
        路径 为 做 mkdir_规范化路径 用 目录列表 的 第 k 项。
        可变 结果 为 0。
        若 选项 位与 MKDIR_递归 不等于 0：
            结果 为 做 mkdir_递归 用 路径、权限、选项、统计。
        否则：
            若 做 文件_存在 用 路径：
                做 打印行 用 「mkdir: 无法创建目录 '」 加 路径 加 「': 文件已存在」。
                统计 的 错误数 为 统计 的 错误数 加 1。
            否则：
                结果 为 做 文件_创建目录 用 路径、权限。
                若 结果 小于 0：
                    父路径 为 做 mkdir_父目录 用 路径。
                    若 非 做 文件_存在 用 父路径：
                        做 打印行 用 「mkdir: 无法创建目录 '」 加 路径 加 「': 父目录不存在」。
                    否则：
                        做 打印行 用 「mkdir: 无法创建目录 '」 加 路径 加 「': 」 加 做 错误描述 用 结果。
                    完。
                    统计 的 错误数 为 统计 的 错误数 加 1。
                否则：
                    统计 的 创建数 为 统计 的 创建数 加 1。
                    若 选项 位与 MKDIR_详细 不等于 0：
                        做 打印行 用 「mkdir: 已创建目录 '」 加 路径 加 「'」。
                    完。
                    若 选项 位与 MKDIR_上下文 不等于 0 并且 安全上下文 不等于 「」：
                        做 mkdir_设置安全上下文 用 路径、安全上下文。
                    完。
                完。
            完。
        完。
        k 为 k 加 1。
    完。
    返还 若 统计 的 错误数 大于 0 则 1 否则 0。
完。

mkdir_递归 接受 路径、权限、选项、统计：
    是绝对 为 做 字符串长度 用 路径 大于 0 并且 做 字符串取字符 用 路径、0 等于 「/」。
    部分 为 做 字符串分割 用 路径、「/」。
    可变 当前 为 「」。
    若 是绝对：
        当前 为 「」。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 部分：
        若 部分 的 第 i 项 等于 「」 或者 部分 的 第 i 项 等于 「.」：
            i 为 i 加 1。
            继续。
        完。
        若 部分 的 第 i 项 等于 「..」：
            当前 为 做 mkdir_父目录 用 当前。
            i 为 i 加 1。
            继续。
        完。
        若 当前 等于 「」 并且 是绝对：
            当前 为 「/」 加 部分 的 第 i 项。
        否则若 当前 等于 「」：
            当前 为 部分 的 第 i 项。
        否则：
            当前 为 当前 加 「/」 加 部分 的 第 i 项。
        完。
        若 做 文件_存在 用 当前：
            若 非 做 文件_是目录 用 当前：
                做 打印行 用 「mkdir: '」 加 当前 加 「' 不是目录」。
                统计 的 错误数 为 统计 的 错误数 加 1。
                返还 负 1。
            完。
            统计 的 跳过数 为 统计 的 跳过数 加 1。
        否则：
            使用权限 为 权限。
            若 i 小于 做 计数 用 部分 减 1：
                使用权限 为 权限 位或 192。
            完。
            结果 为 做 文件_创建目录 用 当前、使用权限。
            若 结果 小于 0：
                做 打印行 用 「mkdir: 无法创建目录 '」 加 当前 加 「': 」 加 做 错误描述 用 结果。
                统计 的 错误数 为 统计 的 错误数 加 1。
                返还 结果。
            完。
            统计 的 创建数 为 统计 的 创建数 加 1。
            若 选项 位与 MKDIR_详细 不等于 0：
                做 打印行 用 「mkdir: 已创建目录 '」 加 当前 加 「'」。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

mkdir_规范化路径 接受 路径：
    可变 结果 为 路径。
    只要 做 字符串长度 用 结果 大于 1 并且 做 字符串结尾是 用 结果、「/」：
        结果 为 做 字符串子串 用 结果、0、做 字符串长度 用 结果 减 1。
    完。
    返还 结果。
完。

mkdir_父目录 接受 路径：
    可变 最后斜杠 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 路径：
        若 做 字符串取字符 用 路径、i 等于 「/」：
            最后斜杠 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最后斜杠 小于或等于 0：
        返还 「/」。
    完。
    返还 做 字符串子串 用 路径、0、最后斜杠。
完。

mkdir_解析权限 接受 文本：
    若 做 字符串长度 用 文本 等于 0：
        返还 493。
    完。
    首字符 为 做 字符串取字符码 用 文本、0。
    若 首字符 大于或等于 0x30 并且 首字符 小于或等于 0x37：
        返还 做 字符串转八进制 用 文本。
    完。
    可变 权限 为 493。
    部分 为 做 字符串分割 用 文本、「,」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 部分：
        权限 为 做 mkdir_解析符号权限 用 部分 的 第 i 项、权限。
        i 为 i 加 1。
    完。
    返还 权限。
完。

mkdir_解析符号权限 接受 表达式、当前权限：
    可变 谁 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 表达式：
        c 为 做 字符串取字符 用 表达式、i。
        若 c 等于 「u」：
            谁 为 谁 位或 448。
        否则若 c 等于 「g」：
            谁 为 谁 位或 56。
        否则若 c 等于 「o」：
            谁 为 谁 位或 7。
        否则若 c 等于 「a」：
            谁 为 511。
        否则：
            跳出。
        完。
        i 为 i 加 1。
    完。
    若 谁 等于 0：
        谁 为 511。
    完。
    若 i 大于或等于 做 字符串长度 用 表达式：
        返还 当前权限。
    完。
    操作 为 做 字符串取字符 用 表达式、i。
    i 为 i 加 1。
    可变 位 为 0。
    只要 i 小于 做 字符串长度 用 表达式：
        c 为 做 字符串取字符 用 表达式、i。
        若 c 等于 「r」：
            位 为 位 位或 292。
        否则若 c 等于 「w」：
            位 为 位 位或 146。
        否则若 c 等于 「x」：
            位 为 位 位或 73。
        否则若 c 等于 「s」：
            位 为 位 位或 3072。
        否则若 c 等于 「t」：
            位 为 位 位或 512。
        完。
        i 为 i 加 1。
    完。
    位 为 位 位与 谁。
    若 操作 等于 「+」：
        返还 当前权限 位或 位。
    完。
    若 操作 等于 「-」：
        返还 当前权限 位与 按位取反 位。
    完。
    若 操作 等于 「=」：
        返还 当前权限 位与 按位取反 谁 位或 位。
    完。
    返还 当前权限。
完。

mkdir_设置安全上下文 接受 路径、上下文：
    结果 为 做 文件_设置扩展属性 用 路径、「security.selinux」、上下文。
    若 结果 小于 0：
        做 打印行 用 「mkdir: 无法设置安全上下文: 」 加 路径。
    完。
完。

字符串转八进制 接受 文本：
    可变 结果 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        c 为 做 字符串取字符码 用 文本、i。
        若 c 大于或等于 48 并且 c 小于或等于 55：
            结果 为 结果 乘 8 加 (c 减 48)。
        否则：
            跳出。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

mkdir_帮助 接受 零参：
    做 打印行 用 「用法: mkdir [选项] 目录...」。
    做 打印行 用 「创建目录（如果目录不存在）」。
    做 打印行 用 「」。
    做 打印行 用 「  -m, --mode=MODE  设置文件权限（如 'mkdir -m 755'）」。
    做 打印行 用 「  -p, --parents    递归创建父目录」。
    做 打印行 用 「  -v, --verbose    显示每个创建的目录」。
    做 打印行 用 「  -Z, --context    设置 SELinux 安全上下文」。
    做 打印行 用 「  --help           显示帮助信息」。
    做 打印行 用 「  --version        显示版本信息」。
完。

可变 AI_创建目录_热文件表 为 一组：（空）。

AI_创建目录_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_创建目录_热文件表：
        若 AI_创建目录_热文件表 的 第 i 项 的 路径 等于 路径：
            AI_创建目录_热文件表 的 第 i 项 的 频率 为 AI_创建目录_热文件表 的 第 i 项 的 频率 加 1。
            返还 AI_创建目录_热文件表 的 第 i 项 的 频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_创建目录_热文件表 小于 128：
        做 追加 用 AI_创建目录_热文件表、一本字典：路径 对应 路径、频率 对应 1。
    完。
    返还 1。
完。

AI_创建目录_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
        返还 0。
    完。
    返还 32768。
完。

本章 收束。
