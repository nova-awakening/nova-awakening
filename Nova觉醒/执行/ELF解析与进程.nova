《ELF解析与进程》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·读取调用》。

引用《..·中枢·字符串替换》。

引用《.·归档解包》。

引用《..·中枢·链表后继》。

引用《..·记忆·VFS文件读写》。

引用《..·中枢·每处理器》。

引用《..·中枢·进程描述符》。

引用《..·中枢·整数转字符串》。

外部。

ELF加载_解析。

ELF_解析头 接受 路径：
    文件 为 做 VFS_打开 用 路径、0。
    若 文件 等于 空：
        返还 空。
    完。
    头数据 为 做 VFS_读取 用 文件、64。
    做 VFS_关闭 用 文件。
    若 头数据 等于 空 或者 做 计数 用 头数据 小于 64：
        返还 空。
    完。
    类型 为 头数据 的 第 16 项 位或 头数据 的 第 17 项 左移 8。
    机器 为 头数据 的 第 18 项 位或 头数据 的 第 19 项 左移 8。
    入口 为 头数据 的 第 24 项 位或 头数据 的 第 25 项 左移 8 位或 头数据 的 第 26 项 左移 16 位或 头数据 的 第 27 项 左移 24。
    返还 一组：类型、机器、入口。
完。

外部。

下一个PID。

创建进程_Linux 接受 路径、参数、环境：
    pid 为 下一个PID。
    下一个PID 为 下一个PID 加 1。
    头 为 做 ELF_解析头 用 路径。
    若 头 等于 空：
        返还 错误_格式。
    完。
    若 头 的 第 1 项 不等于 0x3E：
        做 内核警告 用 「兼容层: 不支持的架构 」 加 做 整数转十六进制 用 头 的 第 1 项。
        返还 错误_格式。
    完。
    兼容_Linux程序数 为 兼容_Linux程序数 加 1。
    做 内核信息 用 「兼容层: 加载Linux程序 PID=」 加 做 整数转字符串 用 pid 加 「 」 加 路径。
    返还 pid。
完。

获取当前用户名 接受 零参：
    返还 「nova」。
完。

可变 Nova快速锁_等待队列 为 一组：（空）。

Nova快速锁_等待 接受 地址、超时：
    当前 为 做 获取当前进程。
    做 追加 用 Nova快速锁_等待队列、一组：地址、当前。
    做 进程_设置状态_兼容 用 当前、1。
    做 调度_兼容。
完。

Nova快速锁_唤醒 接受 地址、数量：
    可变 唤醒数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 Nova快速锁_等待队列 并且 唤醒数 小于 数量：
        若 Nova快速锁_等待队列 的 第 i 项 的 第 0 项 等于 地址：
            进程 为 Nova快速锁_等待队列 的 第 i 项 的 第 1 项。
            做 进程_设置状态_兼容 用 进程、0。
            做 链表_删除 用 Nova快速锁_等待队列、i。
            唤醒数 为 唤醒数 加 1。
        否则：
            i 为 i 加 1。
        完。
    完。
    返还 唤醒数。
完。

可变 兼容_随机种子 为 0x5DEECE66D。

随机数 接受 零参：
    兼容_随机种子 为 兼容_随机种子 乘 0x5DEECE66D 加 0xB 位与 0xFFFFFFFFFFFF。
    返还 兼容_随机种子 右移 16 位与 0x7FFFFFFF。
完。

读取用户字符串 接受 地址：
    若 地址 等于 0：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 偏移 为 0。
    最大长度 为 4096。
    只要 偏移 小于 最大长度：
        字节 为 做 读取用户字节 用 地址 加 偏移。
        若 字节 等于 0：
            返还 结果。
        完。
        结果 为 结果 加 做 字符 用 字节。
        偏移 为 偏移 加 1。
    完。
    返还 结果。
完。

兼容_进程_设名称 接受 进程、名称：
    做 进程_设名称 用 进程、名称。
完。

设置权限 接受 路径、模式：
    做 VFS_设置属性 用 路径、模式。
完。

本章 收束。
