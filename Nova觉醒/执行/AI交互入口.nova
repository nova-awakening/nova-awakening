《AI交互入口》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·推理·总线悬浮球》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·字符串替换》。

引用《.·深度语义》。

引用《.·用户输入处》。

引用《..·中枢·整数转字符串》。

引用《.·核心推理》。

模式_统一 为 0。

模式_URL 为 1。

模式_搜索 为 2。

模式_命令 为 3。

模式_AI对话 为 4。

模式_计算 为 5。

建议_URL 为 0。

建议_搜索 为 1。

建议_命令 为 2。

建议_历史 为 3。

建议_书签 为 4。

建议_AI 为 5。

输入建议 是 一种 事物，含 类型、文本、描述、图标、优先级。

AI交互会话 是 一种 事物，含 ID、历史、上下文、模式、活跃。

可变 AI入口_当前模式 为 模式_统一。

可变 AI入口_会话 为 空。

可变 AI入口_建议缓存 为 一组：（空）。

可变 AI入口_输入历史 为 一组：（空）。

可变 AI入口_已初始化 为 假。

可变 AI入口_总交互数 为 0。

AI交互入口_初始化 接受 零参：
    AI入口_会话 为 新建 AI交互会话。
    AI入口_会话 的 历史 为 一组：（空）。
    AI入口_会话 的 上下文 为 「」。
    AI入口_会话 的 模式 为 模式_统一。
    AI入口_会话 的 活跃 为 真。
    AI入口_建议缓存 为 一组：（空）。
    AI入口_输入历史 为 一组：（空）。
    AI入口_已初始化 为 真。
    做 内核信息 用 「AI交互入口: 地址栏=系统入口=AI助手, 统一模式就绪」。
完。

AI交互入口_匹配系统页 接受 输入：
    若 做 字符串查找 用 输入、「设置」 大于或等于 0：
        返还 「nova://settings」。
    完。
    若 做 字符串查找 用 输入、「回滚」 大于或等于 0 或者 做 字符串查找 用 输入、「恢复」 大于或等于 0 或者 做 字符串查找 用 输入、「热更新」 大于或等于 0 或者 做 字符串查找 用 输入、「自修复」 大于或等于 0：
        返还 「nova://recovery」。
    完。
    若 做 字符串查找 用 输入、「语音」 大于或等于 0 或者 做 字符串查找 用 输入、「声音」 大于或等于 0 或者 做 字符串查找 用 输入、「麦克风」 大于或等于 0 或者 做 字符串查找 用 输入、「听觉」 大于或等于 0：
        返还 「nova://voice」。
    完。
    若 做 字符串查找 用 输入、「形象」 大于或等于 0 或者 做 字符串查找 用 输入、「虚拟人」 大于或等于 0 或者 做 字符串查找 用 输入、「化身」 大于或等于 0 或者 做 字符串查找 用 输入、「avatar」 大于或等于 0 或者 做 字符串查找 用 输入、「全息」 大于或等于 0：
        返还 「nova://avatar」。
    完。
    若 做 字符串查找 用 输入、「多模态」 大于或等于 0 或者 做 字符串查找 用 输入、「视觉」 大于或等于 0：
        返还 「nova://multimodal」。
    完。
    若 做 字符串查找 用 输入、「自动化」 大于或等于 0 或者 做 字符串查找 用 输入、「工作流」 大于或等于 0 或者 做 字符串查找 用 输入、「任务编排」 大于或等于 0 或者 做 字符串查找 用 输入、「发布」 大于或等于 0：
        返还 「nova://automation」。
    完。
    若 做 字符串查找 用 输入、「系统总线」 大于或等于 0 或者 做 字符串查找 用 输入、「系统状态」 大于或等于 0 或者 做 字符串查找 用 输入、「硬件」 大于或等于 0 或者 做 字符串查找 用 输入、「进程」 大于或等于 0 或者 做 字符串查找 用 输入、「内存」 大于或等于 0 或者 做 字符串查找 用 输入、「gpu」 大于或等于 0 或者 做 字符串查找 用 输入、「cpu」 大于或等于 0：
        返还 「nova://system」。
    完。
    若 做 字符串查找 用 输入、「ai工作台」 大于或等于 0 或者 做 字符串查找 用 输入、「ai助手」 大于或等于 0 或者 做 字符串查找 用 输入、「模型」 大于或等于 0 或者 做 字符串查找 用 输入、「对话」 大于或等于 0：
        返还 「nova://ai」。
    完。
    若 做 字符串查找 用 输入、「系统主壳」 大于或等于 0 或者 做 字符串查找 用 输入、「系统主页」 大于或等于 0 或者 做 字符串查找 用 输入、「browser-终端」 大于或等于 0 或者 做 字符串查找 用 输入、「主壳」 大于或等于 0 或者 做 字符串查找 用 输入、「终端」 大于或等于 0：
        返还 「nova://终端」。
    完。
    返还 「」。
完。

AI交互入口_处理输入 接受 输入：
    AI入口_总交互数 为 AI入口_总交互数 加 1。
    做 追加 用 AI入口_输入历史、输入。
    修剪输入 为 做 字符串修剪 用 输入。
    若 做 字符串长度 用 修剪输入 等于 0：
        返还 「」。
    完。
    首字 为 做 字符串子串 用 修剪输入、0、1。
    若 首字 等于 「>」：
        AI入口_当前模式 为 模式_命令。
        命令 为 做 字符串修剪 用 做 字符串子串 用 修剪输入、1、做 字符串长度 用 修剪输入 减 1。
        返还 做 AI交互入口_执行系统命令 用 命令。
    完。
    若 首字 等于 「@」：
        AI入口_当前模式 为 模式_AI对话。
        对话 为 做 字符串修剪 用 做 字符串子串 用 修剪输入、1、做 字符串长度 用 修剪输入 减 1。
        返还 做 AI交互入口_AI对话 用 对话。
    完。
    若 首字 等于 「=」：
        AI入口_当前模式 为 模式_计算。
        表达式 为 做 字符串修剪 用 做 字符串子串 用 修剪输入、1、做 字符串长度 用 修剪输入 减 1。
        返还 做 AI交互入口_计算 用 表达式。
    完。
    系统页 为 做 AI交互入口_匹配系统页 用 修剪输入。
    若 做 字符串长度 用 系统页 大于 0：
        做 事件循环_导航 用 系统页。
        返还 「打开系统页: 」 加 系统页。
    完。
    AI入口_当前模式 为 模式_统一。
    意图 为 做 意图推理_分析 用 修剪输入。
    若 意图 的 意图类型 等于 意图_导航：
        可变 url 为 「」。
        可变 i 为 0。
        只要 i 小于 做 计数 用 意图 的 参数：
            若 意图 的 参数 的 第 i 项 的 第 0 项 等于 「url」：
                url 为 意图 的 参数 的 第 i 项 的 第 1 项。
            完。
            i 为 i 加 1。
        完。
        若 做 字符串长度 用 url 大于 0：
            若 做 字符串查找 用 url、「://」 小于 0 并且 做 字符串查找 用 url、「nova:」 小于 0：
                url 为 「https://」 加 url。
            完。
            做 事件循环_导航 用 url。
            返还 「导航中: 」 加 url。
        完。
    完。
    若 意图 的 意图类型 等于 意图_搜索：
        可变 searchURL 为 「」。
        可变 i 为 0。
        只要 i 小于 做 计数 用 意图 的 参数：
            若 意图 的 参数 的 第 i 项 的 第 0 项 等于 「url」：
                searchURL 为 意图 的 参数 的 第 i 项 的 第 1 项。
            完。
            i 为 i 加 1。
        完。
        若 做 字符串长度 用 searchURL 大于 0：
            做 事件循环_导航 用 searchURL。
        完。
        返还 「搜索: 」 加 修剪输入。
    完。
    若 意图 的 意图类型 等于 意图_系统命令：
        返还 做 意图推理_执行 用 意图。
    完。
    若 意图 的 意图类型 等于 意图_AI对话：
        返还 做 AI交互入口_AI对话 用 修剪输入。
    完。
    做 事件循环_导航 用 「https://search.novaos.dev/?q=」 加 修剪输入。
    返还 「搜索: 」 加 修剪输入。
完。

AI交互入口_执行系统命令 接受 命令：
    系统页 为 做 AI交互入口_匹配系统页 用 命令。
    若 做 字符串长度 用 系统页 大于 0：
        做 事件循环_导航 用 系统页。
        返还 「打开系统页: 」 加 系统页。
    完。
    意图 为 做 意图推理_分析 用 命令。
    若 意图 的 置信度 大于 50：
        返还 做 意图推理_执行 用 意图。
    完。
    返还 「未识别的命令: 」 加 命令 加 『\n提示: 试试「>系统主壳」「>AI工作台」「>查看进程」「>多模态」「>回滚恢复」』。
完。

AI交互入口_AI对话 接受 输入：
    做 追加 用 AI入口_会话 的 历史、一组：输入、「」。
    意图 为 做 意图推理_分析 用 输入。
    若 意图 的 置信度 大于 70：
        回复 为 做 意图推理_执行 用 意图。
        若 做 计数 用 AI入口_会话 的 历史 大于 0：
            AI入口_会话 的 历史 的 第 做 计数 用 AI入口_会话 的 历史 减 1 项 的 第 1 项 为 回复。
        完。
        返还 回复。
    完。
    回复 为 『Nova AI: 我理解你在说「』 加 输入 加 『」\n』。
    回复 为 回复 加 「你可以:\n」。
    回复 为 回复 加 「• 输入URL直接访问网页\n」。
    回复 为 回复 加 「• 用 > 前缀执行系统命令 (如 >系统主壳、>查看进程)\n」。
    回复 为 回复 加 「• 直接说自然语言 (如 打开多模态、查看系统总线)\n」。
    回复 为 回复 加 「• 输入关键词搜索网页」。
    若 做 计数 用 AI入口_会话 的 历史 大于 0：
        AI入口_会话 的 历史 的 第 做 计数 用 AI入口_会话 的 历史 减 1 项 的 第 1 项 为 回复。
    完。
    返还 回复。
完。

AI交互入口_计算 接受 表达式：
    加号 为 做 字符串查找 用 表达式、「+」。
    若 加号 大于 0：
        左 为 做 字符串转整数 用 做 字符串修剪 用 做 字符串子串 用 表达式、0、加号。
        右 为 做 字符串转整数 用 做 字符串修剪 用 做 字符串子串 用 表达式、加号 加 1、做 字符串长度 用 表达式 减 加号 减 1。
        返还 表达式 加 「 = 」 加 做 整数转字符串 用 左 加 右。
    完。
    减号 为 做 字符串查找 用 表达式、「-」。
    若 减号 大于 0：
        左 为 做 字符串转整数 用 做 字符串修剪 用 做 字符串子串 用 表达式、0、减号。
        右 为 做 字符串转整数 用 做 字符串修剪 用 做 字符串子串 用 表达式、减号 加 1、做 字符串长度 用 表达式 减 减号 减 1。
        返还 表达式 加 「 = 」 加 做 整数转字符串 用 左 减 右。
    完。
    乘号 为 做 字符串查找 用 表达式、「*」。
    若 乘号 大于 0：
        左 为 做 字符串转整数 用 做 字符串修剪 用 做 字符串子串 用 表达式、0、乘号。
        右 为 做 字符串转整数 用 做 字符串修剪 用 做 字符串子串 用 表达式、乘号 加 1、做 字符串长度 用 表达式 减 乘号 减 1。
        返还 表达式 加 「 = 」 加 做 整数转字符串 用 左 乘 右。
    完。
    返还 「计算: 」 加 表达式。
完。

AI交互入口_获取建议 接受 部分输入：
    可变 建议 为 一组：（空）。
    若 做 字符串长度 用 部分输入 小于 2：
        返还 建议。
    完。
    可变 i 为 做 计数 用 AI入口_输入历史 减 1。
    只要 i 大于或等于 0 并且 做 计数 用 建议 小于 3：
        若 做 字符串查找 用 AI入口_输入历史 的 第 i 项、部分输入 等于 0：
            s 为 新建 输入建议。
            s 的 类型 为 建议_历史。
            s 的 文本 为 AI入口_输入历史 的 第 i 项。
            s 的 描述 为 「历史记录」。
            s 的 优先级 为 90。
            做 追加 用 建议、s。
        完。
        i 为 i 减 1。
    完。
    若 做 字符串子串 用 部分输入、0、1 等于 「>」 或者 做 字符串查找 用 部分输入、「打开」 大于或等于 0 或者 做 字符串查找 用 部分输入、「查看」 大于或等于 0 或者 做 字符串查找 用 部分输入、「清理」 大于或等于 0：
        命令建议 为 新建 输入建议。
        命令建议 的 类型 为 建议_命令。
        命令建议 的 文本 为 部分输入。
        命令建议 的 描述 为 「系统命令」。
        命令建议 的 优先级 为 85。
        做 追加 用 建议、命令建议。
    完。
    系统页 为 做 AI交互入口_匹配系统页 用 部分输入。
    若 做 字符串长度 用 系统页 大于 0 并且 做 计数 用 建议 小于 5：
        系统页建议 为 新建 输入建议。
        系统页建议 的 类型 为 建议_AI。
        系统页建议 的 文本 为 系统页。
        系统页建议 的 描述 为 「打开系统页」。
        系统页建议 的 优先级 为 88。
        做 追加 用 建议、系统页建议。
    完。
    若 做 字符串查找 用 部分输入、「.」 大于或等于 0：
        url建议 为 新建 输入建议。
        url建议 的 类型 为 建议_URL。
        url建议 的 文本 为 「https://」 加 部分输入。
        url建议 的 描述 为 「访问网站」。
        url建议 的 优先级 为 80。
        做 追加 用 建议、url建议。
    完。
    搜索建议 为 新建 输入建议。
    搜索建议 的 类型 为 建议_搜索。
    搜索建议 的 文本 为 部分输入。
    搜索建议 的 描述 为 「搜索 \"」 加 部分输入 加 「\"」。
    搜索建议 的 优先级 为 50。
    做 追加 用 建议、搜索建议。
    返还 建议。
完。

AI交互入口_统计 接受 零参：
    返还 一组：AI入口_总交互数、AI入口_当前模式、做 计数 用 AI入口_输入历史。
完。

本章 收束。
