《请求接口》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·外联·套接字API》。

引用《.·协议增强》。

引用《.·协议处理》。

引用《.·浏览器网络》。

引用《..·外联·域名初始化》。

浏览器网络_发起请求 接受 方法、url、请求头、请求体：
    请求 为 新建 浏览器网络请求。
    请求 的 ID 为 网络_下一个ID。
    网络_下一个ID 为 网络_下一个ID 加 1。
    请求 的 方法 为 方法。
    请求 的 URL 为 url。
    请求 的 请求头 为 请求头。
    请求 的 请求体 为 请求体。
    请求 的 状态 为 请求状态_等待。
    请求 的 响应体 为 一组：（空）。
    请求 的 AI预测命中 为 假。
    请求 的 AI优先级调整 为 假。
    请求 的 优先级 为 做 浏览器网络_AI评估优先级 用 url。
    parsed 为 做 浏览器网络_解析URL 用 url。
    请求 的 主机 为 parsed 的 主机。
    请求 的 端口 为 parsed 的 端口。
    请求 的 路径 为 parsed 的 路径。
    若 parsed 的 协议 等于 「nova」：
        返还 做 浏览器网络_处理内部协议 用 请求。
    完。
    请求 的 状态 为 请求状态_域名解析中。
    ip 为 做 浏览器网络_域名查询 用 parsed 的 主机。
    若 ip 等于 0：
        请求 的 状态 为 请求状态_失败。
        返还 请求。
    完。
    请求 的 状态 为 请求状态_连接中。
    fd 为 做 浏览器网络_获取连接 用 parsed 的 主机、ip、parsed 的 端口。
    若 fd 小于 0：
        请求 的 状态 为 请求状态_失败。
        返还 请求。
    完。
    请求 的 Socket_FD 为 fd。
    请求 的 状态 为 请求状态_发送中。
    http请求 为 做 浏览器网络_构造HTTP请求 用 请求、parsed。
    做 Socket_发送 用 fd、http请求、做 字符串长度 用 http请求、0。
    请求 的 状态 为 请求状态_接收中。
    做 浏览器网络_接收响应 用 请求。
    网络_总请求数 为 网络_总请求数 加 1。
    网络_总字节数 为 网络_总字节数 加 请求 的 响应体大小。
    返还 请求。
完。

浏览器网络_域名查询 接受 主机名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 网络_域名缓存：
        若 网络_域名缓存 的 第 i 项 的 第 0 项 等于 主机名：
            返还 网络_域名缓存 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    ip 为 做 域名_解析 用 主机名。
    若 ip 不等于 0：
        做 追加 用 网络_域名缓存、一组：主机名、ip、0。
    完。
    返还 ip。
完。

浏览器网络_获取连接 接受 主机名、ip、端口：
    可变 i 为 0。
    只要 i 小于 做 计数 用 网络_连接池 的 连接列表：
        连接 为 网络_连接池 的 连接列表 的 第 i 项。
        若 连接 的 第 0 项 等于 主机名 并且 连接 的 第 2 项 等于 假：
            连接 的 第 2 项 为 真。
            返还 连接 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    fd 为 做 Socket_创建 用 AF_INET、SOCK_STREAM、0。
    若 fd 小于 0：
        返还 负 1。
    完。
    结果 为 做 Socket_连接 用 fd、ip、端口。
    若 结果 小于 0：
        做 Socket_关闭 用 fd。
        返还 负 1。
    完。
    做 追加 用 网络_连接池 的 连接列表、一组：主机名、fd、真。
    返还 fd。
完。

浏览器网络_归还连接 接受 主机名、fd：
    可变 i 为 0。
    只要 i 小于 做 计数 用 网络_连接池 的 连接列表：
        若 网络_连接池 的 连接列表 的 第 i 项 的 第 1 项 等于 fd：
            网络_连接池 的 连接列表 的 第 i 项 的 第 2 项 为 假。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

本章 收束。
