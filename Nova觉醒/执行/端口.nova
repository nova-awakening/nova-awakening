《端口》

引用《..·感知·ReadableStream》。

引用《..·标准·原生字符串》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内核打印》。

引用《..·记忆·VFS读写同步》。

引用《..·中枢·整数转字符串》。

PS_全部进程 为 0x01。

PS_完整格式 为 0x02。

PS_用户格式 为 0x04。

PS_树形 为 0x08。

PS_线程 为 0x10。

PS_长格式 为 0x20。

PS_排序CPU 为 0x40。

PS_排序内存 为 0x80。

PS_排序PID 为 0x100。

进程信息 是 一种 事物，含 pid、ppid、uid、用户名、名称、命令行、状态、状态字符、cpu使用率、内存使用率、虚拟内存、常驻内存、线程数、cpu时间、启动时间、优先级、nice值、终端。

ps_main 接受 参数：
    可变 选项 为 0。
    可变 过滤用户 为 「」。
    可变 过滤pid 为 负 1。
    可变 过滤名称 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        a 为 参数 的 第 i 项。
        若 a 等于 「-e」 或者 a 等于 「-A」：
            选项 为 选项 位或 PS_全部进程。
        否则若 a 等于 「-f」：
            选项 为 选项 位或 PS_完整格式。
        否则若 a 等于 「-l」：
            选项 为 选项 位或 PS_长格式。
        否则若 a 等于 「-H」 或者 a 等于 「--forest」：
            选项 为 选项 位或 PS_树形。
        否则若 a 等于 「-T」 或者 a 等于 「-L」：
            选项 为 选项 位或 PS_线程。
        否则若 a 等于 「aux」 或者 a 等于 「-aux」：
            选项 为 选项 位或 PS_全部进程 位或 PS_用户格式。
        否则若 a 等于 「-u」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            过滤用户 为 参数 的 第 i 项。
            选项 为 选项 位或 PS_用户格式。
        否则若 a 等于 「-p」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            过滤pid 为 做 字符串转整数 用 参数 的 第 i 项。
        否则若 a 等于 「-C」 并且 i 加 1 小于 做 计数 用 参数：
            i 为 i 加 1。
            过滤名称 为 参数 的 第 i 项。
        否则若 a 等于 「--sort=cpu」：
            选项 为 选项 位或 PS_排序CPU。
        否则若 a 等于 「--sort=mem」：
            选项 为 选项 位或 PS_排序内存。
        否则若 a 等于 「--sort=pid」：
            选项 为 选项 位或 PS_排序PID。
        否则若 a 等于 「--help」：
            做 ps_帮助。
            返还 0。
        否则若 a 等于 「--version」：
            做 打印行 用 「ps (NovaOS) 1.0」。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    若 选项 等于 0：
        选项 为 PS_全部进程。
    完。
    进程列表 为 做 获取进程列表详细。
    若 进程列表 等于 空 或者 做 计数 用 进程列表 等于 0：
        做 打印行 用 「ps: 无法获取进程信息」。
        返还 1。
    完。
    可变 过滤后 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 做 计数 用 进程列表：
        p 为 进程列表 的 第 k 项。
        可变 通过 为 真。
        若 过滤用户 不等于 「」 并且 p 的 用户名 不等于 过滤用户：
            通过 为 假。
        完。
        若 过滤pid 大于或等于 0 并且 p 的 pid 不等于 过滤pid：
            通过 为 假。
        完。
        若 过滤名称 不等于 「」 并且 非 做 字符串包含 用 p 的 名称、过滤名称：
            通过 为 假。
        完。
        若 通过：
            做 追加 用 过滤后、p。
        完。
        k 为 k 加 1。
    完。
    若 选项 位与 PS_排序CPU 不等于 0：
        做 ps_排序 用 过滤后、「cpu」。
    否则若 选项 位与 PS_排序内存 不等于 0：
        做 ps_排序 用 过滤后、「mem」。
    否则若 选项 位与 PS_排序PID 不等于 0：
        做 ps_排序 用 过滤后、「pid」。
    完。
    若 选项 位与 PS_树形 不等于 0：
        做 ps_打印树 用 过滤后、选项。
    否则：
        做 ps_打印表头 用 选项。
        k 为 0。
        只要 k 小于 做 计数 用 过滤后：
            做 ps_打印进程 用 过滤后 的 第 k 项、选项、「」。
            k 为 k 加 1。
        完。
    完。
    返还 0。
完。

ps_打印表头 接受 选项：
    若 选项 位与 PS_用户格式 不等于 0：
        做 打印行 用 「USER       PID %CPU %MEM    VSZ   RSS TTY      STAT  TIME COMMAND」。
    否则若 选项 位与 PS_完整格式 不等于 0：
        做 打印行 用 「UID        PID  PPID  C STIME TTY       TIME CMD」。
    否则若 选项 位与 PS_长格式 不等于 0：
        做 打印行 用 「F S   UID   PID  PPID  C PRI  NI   VSZ   RSS TTY      TIME CMD」。
    否则：
        做 打印行 用 「  PID TTY       TIME CMD」。
    完。
完。

ps_打印进程 接受 p、选项、前缀：
    若 选项 位与 PS_用户格式 不等于 0：
        做 打印行 用 做 ps_右填充 用 p 的 用户名、8 加 「 」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 pid、5 加 「 」 加 做 ps_左填充 用 做 ps_格式化百分比 用 p 的 cpu使用率、4 加 「 」 加 做 ps_左填充 用 做 ps_格式化百分比 用 p 的 内存使用率、4 加 「 」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 虚拟内存、6 加 「 」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 常驻内存、5 加 「 」 加 做 ps_右填充 用 p 的 终端、8 加 「 」 加 做 ps_右填充 用 p 的 状态字符、4 加 「 」 加 做 ps_格式化时间 用 p 的 cpu时间 加 「 」 加 前缀 加 p 的 命令行。
    否则若 选项 位与 PS_完整格式 不等于 0：
        做 打印行 用 做 ps_右填充 用 p 的 用户名、8 加 「 」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 pid、5 加 「 」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 ppid、5 加 「  」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 cpu使用率 除以 10、1 加 「 」 加 做 ps_右填充 用 做 ps_格式化启动时间 用 p 的 启动时间、5 加 「 」 加 做 ps_右填充 用 p 的 终端、8 加 「 」 加 做 ps_格式化时间 用 p 的 cpu时间 加 「 」 加 前缀 加 p 的 命令行。
    否则若 选项 位与 PS_长格式 不等于 0：
        做 打印行 用 做 ps_左填充 用 「0」、1 加 「 」 加 p 的 状态字符 加 「 」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 uid、5 加 「 」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 pid、5 加 「 」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 ppid、5 加 「  」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 cpu使用率 除以 10、1 加 「 」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 优先级、3 加 「 」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 nice值、3 加 「 」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 虚拟内存、5 加 「 」 加 做 ps_左填充 用 做 整数转字符串 用 p 的 常驻内存、5 加 「 」 加 做 ps_右填充 用 p 的 终端、8 加 「 」 加 做 ps_格式化时间 用 p 的 cpu时间 加 「 」 加 前缀 加 p 的 名称。
    否则：
        做 打印行 用 做 ps_左填充 用 做 整数转字符串 用 p 的 pid、5 加 「 」 加 做 ps_右填充 用 p 的 终端、8 加 「 」 加 做 ps_格式化时间 用 p 的 cpu时间 加 「 」 加 前缀 加 p 的 名称。
    完。
完。

ps_打印树 接受 列表、选项：
    做 ps_打印表头 用 选项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 的 ppid 等于 0 或者 非 做 ps_父在列表中 用 列表、列表 的 第 i 项 的 ppid：
            做 ps_打印树节点 用 列表、列表 的 第 i 项、选项、「」、真。
        完。
        i 为 i 加 1。
    完。
完。

ps_打印树节点 接受 列表、进程、选项、缩进、是末尾：
    前缀 为 「」。
    若 做 字符串长度 用 缩进 大于 0：
        若 是末尾：
            前缀 为 缩进 加 「└─ 」。
        否则：
            前缀 为 缩进 加 「├─ 」。
        完。
    完。
    做 ps_打印进程 用 进程、选项、前缀。
    子进程 为 做 ps_获取子进程 用 列表、进程 的 pid。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子进程：
        新缩进 为 缩进。
        若 做 字符串长度 用 缩进 大于 0：
            若 是末尾：
                新缩进 为 缩进 加 「   」。
            否则：
                新缩进 为 缩进 加 「│  」。
            完。
        否则：
            新缩进 为 「」。
        完。
        做 ps_打印树节点 用 列表、子进程 的 第 i 项、选项、新缩进、i 等于 做 计数 用 子进程 减 1。
        i 为 i 加 1。
    完。
完。

ps_获取子进程 接受 列表、父pid：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 的 ppid 等于 父pid 并且 列表 的 第 i 项 的 pid 不等于 父pid：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

ps_父在列表中 接受 列表、ppid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 的 pid 等于 ppid：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

ps_排序 接受 列表、字段：
    n 为 做 计数 用 列表。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 0。
        只要 j 小于 n 减 1 减 i：
            可变 交换 为 假。
            若 字段 等于 「cpu」：
                若 列表 的 第 j 项 的 cpu使用率 小于 列表 的 第 j 加 1 项 的 cpu使用率：
                    交换 为 真。
                完。
            否则若 字段 等于 「mem」：
                若 列表 的 第 j 项 的 内存使用率 小于 列表 的 第 j 加 1 项 的 内存使用率：
                    交换 为 真。
                完。
            否则：
                若 列表 的 第 j 项 的 pid 大于 列表 的 第 j 加 1 项 的 pid：
                    交换 为 真。
                完。
            完。
            若 交换：
                临时 为 列表 的 第 j 项。
                列表 的 第 j 项 为 列表 的 第 j 加 1 项。
                列表 的 第 j 加 1 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

ps_格式化时间 接受 秒：
    小时 为 秒 除以 3600。
    分 为 秒 取余于 3600 除以 60。
    秒余 为 秒 取余于 60。
    返还 做 ps_左填充 用 做 整数转字符串 用 小时、2 加 「:」 加 做 ps_零填充 用 分、2 加 「:」 加 做 ps_零填充 用 秒余、2。
完。

ps_格式化启动时间 接受 时间戳：
    小时 为 时间戳 除以 3600 取余于 24。
    分 为 时间戳 除以 60 取余于 60。
    返还 做 ps_零填充 用 小时、2 加 「:」 加 做 ps_零填充 用 分、2。
完。

ps_格式化百分比 接受 千分值：
    整数部分 为 千分值 除以 10。
    小数部分 为 千分值 取余于 10。
    返还 做 整数转字符串 用 整数部分 加 「.」 加 做 整数转字符串 用 小数部分。
完。

ps_左填充 接受 文本、宽度：
    可变 结果 为 文本。
    只要 做 字符串长度 用 结果 小于 宽度：
        结果 为 「 」 加 结果。
    完。
    返还 结果。
完。

ps_右填充 接受 文本、宽度：
    可变 结果 为 文本。
    只要 做 字符串长度 用 结果 小于 宽度：
        结果 为 结果 加 「 」。
    完。
    返还 结果。
完。

ps_零填充 接受 数值、宽度：
    可变 结果 为 做 整数转字符串 用 数值。
    只要 做 字符串长度 用 结果 小于 宽度：
        结果 为 「0」 加 结果。
    完。
    返还 结果。
完。

获取进程列表详细 接受 零参：
    可变 结果 为 一组：（空）。
    proc目录 为 做 文件_读目录 用 「/proc」。
    若 proc目录 等于 空：
        idle 为 做 堆_分配 用 进程信息。
        idle 的 pid 为 0。
        idle 的 ppid 为 0。
        idle 的 uid 为 0。
        idle 的 用户名 为 「root」。
        idle 的 名称 为 「idle」。
        idle 的 命令行 为 「[idle]」。
        idle 的 状态 为 0。
        idle 的 状态字符 为 「S」。
        idle 的 cpu使用率 为 0。
        idle 的 内存使用率 为 0。
        idle 的 虚拟内存 为 0。
        idle 的 常驻内存 为 0。
        idle 的 线程数 为 1。
        idle 的 cpu时间 为 0。
        idle 的 启动时间 为 0。
        idle 的 优先级 为 0。
        idle 的 nice值 为 0。
        idle 的 终端 为 「?」。
        做 追加 用 结果、idle。
        init 为 做 堆_分配 用 进程信息。
        init 的 pid 为 1。
        init 的 ppid 为 0。
        init 的 uid 为 0。
        init 的 用户名 为 「root」。
        init 的 名称 为 「nova-init」。
        init 的 命令行 为 「/sbin/nova-init」。
        init 的 状态 为 0。
        init 的 状态字符 为 「Ss」。
        init 的 cpu使用率 为 0。
        init 的 内存使用率 为 5。
        init 的 虚拟内存 为 2048。
        init 的 常驻内存 为 512。
        init 的 线程数 为 1。
        init 的 cpu时间 为 0。
        init 的 启动时间 为 0。
        init 的 优先级 为 20。
        init 的 nice值 为 0。
        init 的 终端 为 「?」。
        做 追加 用 结果、init。
        返还 结果。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 proc目录：
        名称 为 proc目录 的 第 i 项 的 第 0 项。
        若 做 ps_是数字字符串 用 名称：
            p 为 做 ps_读取进程信息 用 做 字符串转整数 用 名称。
            若 p 不等于 空：
                做 追加 用 结果、p。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

ps_读取进程信息 接受 pid：
    状态路径 为 「/proc/」 加 做 整数转字符串 用 pid 加 「/status」。
    内容 为 做 文件_读取全部 用 状态路径。
    若 内容 等于 空：
        返还 空。
    完。
    p 为 做 堆_分配 用 进程信息。
    p 的 pid 为 pid。
    p 的 ppid 为 0。
    p 的 uid 为 0。
    p 的 用户名 为 「root」。
    p 的 名称 为 「」。
    p 的 命令行 为 「」。
    p 的 状态 为 0。
    p 的 状态字符 为 「S」。
    p 的 cpu使用率 为 0。
    p 的 内存使用率 为 0。
    p 的 虚拟内存 为 0。
    p 的 常驻内存 为 0。
    p 的 线程数 为 1。
    p 的 cpu时间 为 0。
    p 的 启动时间 为 0。
    p 的 优先级 为 20。
    p 的 nice值 为 0。
    p 的 终端 为 「?」。
    行列表 为 做 字符串分割 用 内容、「\n」。
    可变 j 为 0。
    只要 j 小于 做 计数 用 行列表：
        行 为 行列表 的 第 j 项。
        若 做 字符串开头是 用 行、「Name:」：
            p 的 名称 为 做 字符串去首尾空白 用 做 字符串子串 用 行、5、做 字符串长度 用 行。
        否则若 做 字符串开头是 用 行、「PPid:」：
            p 的 ppid 为 做 字符串转整数 用 做 字符串去首尾空白 用 做 字符串子串 用 行、5、做 字符串长度 用 行。
        否则若 做 字符串开头是 用 行、「Uid:」：
            p 的 uid 为 做 字符串转整数 用 做 字符串去首尾空白 用 做 字符串子串 用 行、4、做 字符串长度 用 行。
        否则若 做 字符串开头是 用 行、「Threads:」：
            p 的 线程数 为 做 字符串转整数 用 做 字符串去首尾空白 用 做 字符串子串 用 行、8、做 字符串长度 用 行。
        否则若 做 字符串开头是 用 行、「VmSize:」：
            p 的 虚拟内存 为 做 字符串转整数 用 做 字符串去首尾空白 用 做 字符串子串 用 行、7、做 字符串长度 用 行。
        否则若 做 字符串开头是 用 行、「VmRSS:」：
            p 的 常驻内存 为 做 字符串转整数 用 做 字符串去首尾空白 用 做 字符串子串 用 行、6、做 字符串长度 用 行。
        完。
        j 为 j 加 1。
    完。
    若 p 的 命令行 等于 「」：
        cmd路径 为 「/proc/」 加 做 整数转字符串 用 pid 加 「/cmdline」。
        cmd 为 做 文件_读取全部 用 cmd路径。
        若 cmd 不等于 空 并且 做 字符串长度 用 cmd 大于 0：
            p 的 命令行 为 cmd。
        否则：
            p 的 命令行 为 「[」 加 p 的 名称 加 「]」。
        完。
    完。
    返还 p。
完。

ps_是数字字符串 接受 文本：
    若 做 字符串长度 用 文本 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        c 为 做 字符串取字符码 用 文本、i。
        若 c 小于 0x30 或者 c 大于 0x39：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

ps_帮助 接受 零参：
    做 打印行 用 「用法: ps [选项]」。
    做 打印行 用 「显示当前进程的快照」。
    做 打印行 用 「」。
    做 打印行 用 「  -e, -A           显示所有进程」。
    做 打印行 用 「  -f               完整格式输出」。
    做 打印行 用 「  -l               长格式输出」。
    做 打印行 用 「  -u USER          按用户过滤」。
    做 打印行 用 「  -p PID           按进程ID过滤」。
    做 打印行 用 「  -C NAME          按命令名过滤」。
    做 打印行 用 「  -H, --forest     树形显示」。
    做 打印行 用 「  -T, -L           显示线程」。
    做 打印行 用 「  --sort=cpu|mem|pid  排序方式」。
    做 打印行 用 「  aux              BSD风格完整输出」。
    做 打印行 用 「  --help           显示帮助」。
    做 打印行 用 「  --version        显示版本」。
完。

ps_统计 是 一种 事物，含 查询次数、进程总数、线程总数、过滤次数、排序次数、树形次数、失败次数。

可变 全局_ps统计 为 空。

ps统计_初始化 接受 零参：
    若 全局_ps统计 等于 空：
        全局_ps统计 为 新建 ps_统计。
        全局_ps统计 的 查询次数 为 0。
        全局_ps统计 的 进程总数 为 0。
        全局_ps统计 的 线程总数 为 0。
        全局_ps统计 的 过滤次数 为 0。
        全局_ps统计 的 排序次数 为 0。
        全局_ps统计 的 树形次数 为 0。
        全局_ps统计 的 失败次数 为 0。
    完。
完。

ps_打印统计 接受 零参：
    做 ps统计_初始化。
    做 打印行 用 「=== NPMI进程统计 ===」。
    做 打印行 用 「查询: 」 加 做 整数转字符串 用 全局_ps统计 的 查询次数。
    做 打印行 用 「进程: 」 加 做 整数转字符串 用 全局_ps统计 的 进程总数。
    做 打印行 用 「线程: 」 加 做 整数转字符串 用 全局_ps统计 的 线程总数。
    做 打印行 用 「过滤: 」 加 做 整数转字符串 用 全局_ps统计 的 过滤次数。
    做 打印行 用 「排序: 」 加 做 整数转字符串 用 全局_ps统计 的 排序次数。
    做 打印行 用 「树形: 」 加 做 整数转字符串 用 全局_ps统计 的 树形次数。
    做 打印行 用 「失败: 」 加 做 整数转字符串 用 全局_ps统计 的 失败次数。
完。

ps_完整统计 接受 零参：
    做 ps统计_初始化。
    返还 一组：全局_ps统计 的 查询次数、全局_ps统计 的 进程总数、全局_ps统计 的 线程总数、全局_ps统计 的 过滤次数、全局_ps统计 的 排序次数、全局_ps统计 的 树形次数、全局_ps统计 的 失败次数。
完。

ps_重置统计 接受 零参：
    若 全局_ps统计 不等于 空：
        全局_ps统计 的 查询次数 为 0。
        全局_ps统计 的 进程总数 为 0。
        全局_ps统计 的 线程总数 为 0。
        全局_ps统计 的 过滤次数 为 0。
        全局_ps统计 的 排序次数 为 0。
        全局_ps统计 的 树形次数 为 0。
        全局_ps统计 的 失败次数 为 0。
    完。
完。

ps_获取查询数 接受 零参：
    做 ps统计_初始化。
    返还 全局_ps统计 的 查询次数。
完。

ps_获取进程数 接受 零参：
    做 ps统计_初始化。
    返还 全局_ps统计 的 进程总数。
完。

ps_获取线程数 接受 零参：
    做 ps统计_初始化。
    返还 全局_ps统计 的 线程总数。
完。

ps_获取过滤数 接受 零参：
    做 ps统计_初始化。
    返还 全局_ps统计 的 过滤次数。
完。

ps_获取树形数 接受 零参：
    做 ps统计_初始化。
    返还 全局_ps统计 的 树形次数。
完。

ps_获取失败数 接受 零参：
    做 ps统计_初始化。
    返还 全局_ps统计 的 失败次数。
完。

ps_CPU占用排序 接受 进程列表、数量：
    做 ps统计_初始化。
    全局_ps统计 的 排序次数 为 全局_ps统计 的 排序次数 加 1。
    可变 i 为 0。
    只要 i 小于 数量 减 1：
        可变 max_idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 数量：
            若 进程列表 的 第 j 项 的 CPU占用 大于 进程列表 的 第 max_idx 项 的 CPU占用：
                max_idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 max_idx 不等于 i：
            tmp 为 进程列表 的 第 i 项。
            进程列表 的 第 i 项 为 进程列表 的 第 max_idx 项。
            进程列表 的 第 max_idx 项 为 tmp。
        完。
        i 为 i 加 1。
    完。
完。

ps_内存占用排序 接受 进程列表、数量：
    做 ps统计_初始化。
    全局_ps统计 的 排序次数 为 全局_ps统计 的 排序次数 加 1。
    可变 i 为 0。
    只要 i 小于 数量 减 1：
        可变 max_idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 数量：
            若 进程列表 的 第 j 项 的 内存占用 大于 进程列表 的 第 max_idx 项 的 内存占用：
                max_idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 max_idx 不等于 i：
            tmp 为 进程列表 的 第 i 项。
            进程列表 的 第 i 项 为 进程列表 的 第 max_idx 项。
            进程列表 的 第 max_idx 项 为 tmp。
        完。
        i 为 i 加 1。
    完。
完。

ps_完整诊断 接受 零参：
    做 ps统计_初始化。
    返还 一组：「查询次数」、全局_ps统计 的 查询次数、「进程总数」、全局_ps统计 的 进程总数、「线程总数」、全局_ps统计 的 线程总数、「过滤次数」、全局_ps统计 的 过滤次数、「排序次数」、全局_ps统计 的 排序次数、「树形次数」、全局_ps统计 的 树形次数、「失败次数」、全局_ps统计 的 失败次数、「NPMI进程管理」、1。
完。

ps_排序_按CPU 接受 列表：
    n 为 做 计数 用 列表。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 0。
        只要 j 小于 n 减 1 减 i：
            若 列表 的 第 j 项 的 cpu使用率 小于 列表 的 第 j 加 1 项 的 cpu使用率：
                临时 为 列表 的 第 j 项。
                列表 的 第 j 项 为 列表 的 第 j 加 1 项。
                列表 的 第 j 加 1 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    做 ps统计_初始化。
    全局_ps统计 的 排序次数 为 全局_ps统计 的 排序次数 加 1。
    返还 列表。
完。

ps_过滤_按用户 接受 列表、目标用户：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 的 用户名 等于 目标用户：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    做 ps统计_初始化。
    全局_ps统计 的 过滤次数 为 全局_ps统计 的 过滤次数 加 1。
    返还 结果。
完。

可变 AI_端口_运行指标 为 0。

可变 AI_端口_异常指标 为 0。

AI_端口_自诊断 接受 零参：
    AI_端口_运行指标 为 0。
    返还 AI_端口_运行指标。
完。

AI_端口_优化建议 接受 零参：
    指标 为 做 AI_端口_自诊断。
    返还 「端口状态正常」。
完。

本章 收束。
