《改权限》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《..·中枢·字符串替换》。

引用《..·标准·文件》。

权限_读 为 4。

权限_写 为 2。

权限_执行 为 1。

可变 chmod_成功数 为 0。

可变 chmod_失败数 为 0。

chmod_main 接受 参数：
    可变 递归 为 假。
    可变 详细 为 假。
    可变 安静 为 假。
    可变 仅变化 为 假。
    可变 模式文本 为 「」。
    可变 文件列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        a 为 参数 的 第 i 项。
        若 a 等于 「-R」：
            递归 为 真。
        否则若 a 等于 「-v」：
            详细 为 真。
        否则若 a 等于 「-f」：
            安静 为 真。
        否则若 a 等于 「-c」：
            仅变化 为 真。
            详细 为 真。
        否则若 a 等于 「--help」 或者 a 等于 「-h」：
            做 chmod_显示帮助。
            返还 0。
        否则若 模式文本 等于 「」：
            模式文本 为 a。
        否则：
            做 追加 用 文件列表、a。
        完。
        i 为 i 加 1。
    完。
    若 模式文本 等于 「」 或者 做 计数 用 文件列表 等于 0：
        做 chmod_显示帮助。
        返还 1。
    完。
    chmod_成功数 为 0。
    chmod_失败数 为 0。
    是八进制 为 做 chmod_是八进制模式 用 模式文本。
    可变 k 为 0。
    只要 k 小于 做 计数 用 文件列表：
        若 递归：
            做 chmod_递归处理 用 文件列表 的 第 k 项、模式文本、是八进制、详细、安静、仅变化。
        否则：
            做 chmod_处理文件 用 文件列表 的 第 k 项、模式文本、是八进制、详细、安静、仅变化。
        完。
        k 为 k 加 1。
    完。
    若 chmod_失败数 大于 0 并且 非 安静：
        做 打印行 用 「chmod: 」 加 做 转字符串 用 chmod_失败数 加 「 个文件处理失败」。
    完。
    若 chmod_失败数 大于 0：
        返还 1。
    完。
    返还 0。
完。

chmod_显示帮助 接受 零参：
    做 打印行 用 「用法: chmod [-Rvcf] 模式 文件...」。
    做 打印行 用 「模式格式:」。
    做 打印行 用 「  八进制: 755, 644, 600 等」。
    做 打印行 用 「  符号: u+x, g-w, o=r, a+rw 等」。
    做 打印行 用 「选项:」。
    做 打印行 用 「  -R    递归处理目录」。
    做 打印行 用 「  -v    详细输出」。
    做 打印行 用 「  -c    仅显示变化的文件」。
    做 打印行 用 「  -f    静默模式,忽略错误」。
完。

chmod_处理文件 接受 路径、模式文本、是八进制、详细、安静、仅变化：
    旧权限 为 做 文件_获取权限 用 路径。
    若 旧权限 小于 0：
        若 非 安静：
            做 打印行 用 「chmod: 无法访问 '」 加 路径 加 「'」。
        完。
        chmod_失败数 为 chmod_失败数 加 1。
        返还。
    完。
    可变 新权限 为 0。
    若 是八进制：
        新权限 为 做 字符串转八进制 用 模式文本。
    否则：
        新权限 为 做 chmod_解析符号模式 用 模式文本、旧权限。
    完。
    若 新权限 等于 旧权限 并且 仅变化：
        返还。
    完。
    结果 为 做 文件_设置权限 用 路径、新权限。
    若 结果 小于 0：
        若 非 安静：
            做 打印行 用 「chmod: 设置权限失败 '」 加 路径 加 「'」。
        完。
        chmod_失败数 为 chmod_失败数 加 1。
    否则：
        chmod_成功数 为 chmod_成功数 加 1。
        若 详细：
            做 打印行 用 「'」 加 路径 加 「': 」 加 做 chmod_权限字符串 用 旧权限 加 「 -> 」 加 做 chmod_权限字符串 用 新权限。
        完。
    完。
完。

chmod_递归处理 接受 路径、模式文本、是八进制、详细、安静、仅变化：
    做 chmod_处理文件 用 路径、模式文本、是八进制、详细、安静、仅变化。
    子列表 为 做 目录_列出 用 路径。
    若 子列表 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子列表：
        子路径 为 路径 加 「/」 加 子列表 的 第 i 项。
        若 做 文件_是目录 用 子路径：
            做 chmod_递归处理 用 子路径、模式文本、是八进制、详细、安静、仅变化。
        否则：
            做 chmod_处理文件 用 子路径、模式文本、是八进制、详细、安静、仅变化。
        完。
        i 为 i 加 1。
    完。
完。

chmod_是八进制模式 接受 文本：
    若 做 字符串长度 用 文本 等于 0：
        返还 假。
    完。
    第一字符 为 做 编码 用 做 字符串取字符 用 文本、0。
    返还 第一字符 大于或等于 48 并且 第一字符 小于或等于 55。
完。

chmod_解析符号模式 接受 模式、旧权限：
    可变 结果 为 旧权限。
    可变 主体 为 0。
    可变 操作 为 「」。
    可变 权限值 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 模式：
        c 为 做 字符串取字符 用 模式、i。
        若 c 等于 「u」：
            主体 为 主体 位或 448。
        否则若 c 等于 「g」：
            主体 为 主体 位或 56。
        否则若 c 等于 「o」：
            主体 为 主体 位或 7。
        否则若 c 等于 「a」：
            主体 为 511。
        否则若 c 等于 「+」 或者 c 等于 「-」 或者 c 等于 「=」：
            操作 为 c。
            若 主体 等于 0：
                主体 为 511。
            完。
        否则若 c 等于 「r」：
            权限值 为 权限值 位或 4。
        否则若 c 等于 「w」：
            权限值 为 权限值 位或 2。
        否则若 c 等于 「x」：
            权限值 为 权限值 位或 1。
        完。
        i 为 i 加 1。
    完。
    可变 掩码 为 0。
    若 主体 位与 448：
        掩码 为 掩码 位或 权限值 乘 64。
    完。
    若 主体 位与 56：
        掩码 为 掩码 位或 权限值 乘 8。
    完。
    若 主体 位与 7：
        掩码 为 掩码 位或 权限值。
    完。
    若 操作 等于 「+」：
        结果 为 结果 位或 掩码。
    否则若 操作 等于 「-」：
        结果 为 结果 位与 按位取反 掩码。
    否则若 操作 等于 「=」：
        结果 为 结果 位与 按位取反 主体。
        结果 为 结果 位或 掩码。
    完。
    返还 结果。
完。

chmod_权限字符串 接受 模式：
    可变 结果 为 「」。
    结果 为 结果 加 若 模式 位与 256 则 「r」 否则 「-」。
    结果 为 结果 加 若 模式 位与 128 则 「w」 否则 「-」。
    结果 为 结果 加 若 模式 位与 64 则 「x」 否则 「-」。
    结果 为 结果 加 若 模式 位与 32 则 「r」 否则 「-」。
    结果 为 结果 加 若 模式 位与 16 则 「w」 否则 「-」。
    结果 为 结果 加 若 模式 位与 8 则 「x」 否则 「-」。
    结果 为 结果 加 若 模式 位与 4 则 「r」 否则 「-」。
    结果 为 结果 加 若 模式 位与 2 则 「w」 否则 「-」。
    结果 为 结果 加 若 模式 位与 1 则 「x」 否则 「-」。
    返还 结果。
完。

字符串转八进制 接受 文本：
    可变 结果 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        c 为 做 编码 用 做 字符串取字符 用 文本、i。
        若 c 大于或等于 48 并且 c 小于或等于 55：
            结果 为 结果 乘 8 加 (c 减 48)。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 AI_改权限_热文件表 为 一组：（空）。

AI_改权限_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_改权限_热文件表：
        若 AI_改权限_热文件表 的 第 i 项 的 路径 等于 路径：
            AI_改权限_热文件表 的 第 i 项 的 频率 为 AI_改权限_热文件表 的 第 i 项 的 频率 加 1。
            返还 AI_改权限_热文件表 的 第 i 项 的 频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_改权限_热文件表 小于 128：
        做 追加 用 AI_改权限_热文件表、一本字典：路径 对应 路径、频率 对应 1。
    完。
    返还 1。
完。

AI_改权限_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
        返还 0。
    完。
    返还 32768。
完。

本章 收束。
