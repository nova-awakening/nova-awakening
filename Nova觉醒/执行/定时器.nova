《定时器》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·绘制命令》。

引用《.·用户输入处》。

引用《.·JS执行入口》。

引用《.·布局计算》。

引用《.·进程调度适配》。

事件循环_setTimeout 接受 回调、延迟毫秒：
    任务 为 新建 浏览器任务。
    任务 的 ID 为 事件循环 的 下一个ID。
    事件循环 的 下一个ID 为 事件循环 的 下一个ID 加 1。
    任务 的 类型 为 任务_定时器。
    任务 的 回调 为 回调。
    任务 的 延迟 为 延迟毫秒。
    任务 的 重复 为 假。
    做 追加 用 事件循环 的 定时器列表、任务。
    返还 任务 的 ID。
完。

事件循环_setInterval 接受 回调、间隔毫秒：
    任务 为 新建 浏览器任务。
    任务 的 ID 为 事件循环 的 下一个ID。
    事件循环 的 下一个ID 为 事件循环 的 下一个ID 加 1。
    任务 的 类型 为 任务_定时器。
    任务 的 回调 为 回调。
    任务 的 间隔 为 间隔毫秒。
    任务 的 重复 为 真。
    做 追加 用 事件循环 的 定时器列表、任务。
    返还 任务 的 ID。
完。

事件循环_clearTimer 接受 定时器ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 事件循环 的 定时器列表：
        若 事件循环 的 定时器列表 的 第 i 项 的 ID 等于 定时器ID：
            做 列表_移除 用 事件循环 的 定时器列表、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

事件循环_requestAnimationFrame 接受 回调：
    任务 为 新建 浏览器任务。
    任务 的 ID 为 事件循环 的 下一个ID。
    事件循环 的 下一个ID 为 事件循环 的 下一个ID 加 1。
    任务 的 类型 为 任务_渲染帧。
    任务 的 回调 为 回调。
    做 追加 用 事件循环 的 帧回调队列、任务。
    返还 任务 的 ID。
完。

事件循环_启动 接受 零参：
    事件循环 的 运行中 为 真。
    做 内核信息 用 「浏览器事件循环: 启动」。
    只要 事件循环 的 运行中：
        做 事件循环_单次迭代。
    完。
完。

事件循环_停止 接受 零参：
    事件循环 的 运行中 为 假。
完。

事件循环_单次迭代 接受 零参：
    做 事件循环_检查定时器。
    若 做 计数 用 事件循环 的 宏任务队列 大于 0：
        任务 为 事件循环 的 宏任务队列 的 第 0 项。
        做 列表_移除 用 事件循环 的 宏任务队列、0。
        做 事件循环_执行任务 用 任务。
    完。
    只要 做 计数 用 事件循环 的 微任务队列 大于 0：
        微任务 为 事件循环 的 微任务队列 的 第 0 项。
        做 列表_移除 用 事件循环 的 微任务队列、0。
        做 事件循环_执行任务 用 微任务。
    完。
    当前时间 为 做 高精度定时器_读取。
    若 当前时间 减 事件循环 的 上帧时间 大于或等于 事件循环 的 帧间隔：
        做 浏览器调度_帧开始。
        可变 帧回调 为 事件循环 的 帧回调队列。
        事件循环 的 帧回调队列 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 帧回调：
            做 事件循环_执行任务 用 帧回调 的 第 i 项。
            i 为 i 加 1。
        完。
        做 渲染_布局。
        若 DOM_当前文档 不等于 空：
            做 渲染_绘制 用 DOM_当前文档。
        完。
        做 浏览器调度_帧结束。
        帧耗时 为 做 高精度定时器_读取 减 当前时间。
        若 帧耗时 大于 事件循环 的 帧间隔：
            事件循环 的 丢帧数 为 事件循环 的 丢帧数 加 1。
        完。
        事件循环 的 上帧时间 为 当前时间。
        事件循环 的 帧计数 为 事件循环 的 帧计数 加 1。
        事件循环 的 总帧数 为 事件循环 的 总帧数 加 1。
    完。
    若 做 计数 用 事件循环 的 宏任务队列 等于 0 并且 做 计数 用 事件循环 的 空闲回调 大于 0：
        空闲任务 为 事件循环 的 空闲回调 的 第 0 项。
        做 列表_移除 用 事件循环 的 空闲回调、0。
        做 事件循环_执行任务 用 空闲任务。
    完。
完。

事件循环_执行任务 接受 任务：
    若 任务 的 类型 等于 任务_用户输入：
        做 事件循环_处理用户输入 用 任务。
    否则若 任务 的 类型 等于 任务_网络回调：
        做 JS_执行脚本 用 任务 的 回调。
    否则若 任务 的 类型 等于 任务_定时器：
        做 JS_执行脚本 用 任务 的 回调。
    否则若 任务 的 类型 等于 任务_渲染帧：
        做 JS_执行脚本 用 任务 的 回调。
    否则若 任务 的 类型 等于 任务_微任务：
        做 JS_执行脚本 用 任务 的 回调。
    否则若 任务 的 类型 等于 任务_AI推理：
        做 事件循环_处理AI回调 用 任务。
    否则若 任务 的 类型 等于 任务_系统事件：
        做 事件循环_处理系统事件 用 任务。
    完。
完。

本章 收束。
