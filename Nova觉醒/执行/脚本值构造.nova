《脚本值构造》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

JS_创建未定义 接受 零参：
    v 为 新建 JS值。
    v 的 类型 为 JS类型_未定义。
    返还 v。
完。

JS_创建空 接受 零参：
    v 为 新建 JS值。
    v 的 类型 为 JS类型_空。
    返还 v。
完。

JS_创建布尔 接受 b：
    v 为 新建 JS值。
    v 的 类型 为 JS类型_布尔。
    v 的 数字值 为 0。
    若 b：
        v 的 数字值 为 1。
    完。
    返还 v。
完。

JS_创建数字 接受 n：
    v 为 新建 JS值。
    v 的 类型 为 JS类型_数字。
    v 的 数字值 为 n。
    返还 v。
完。

JS_创建字符串 接受 s：
    v 为 新建 JS值。
    v 的 类型 为 JS类型_字符串。
    v 的 字符串值 为 s。
    返还 v。
完。

JS_创建对象 接受 零参：
    v 为 新建 JS值。
    v 的 类型 为 JS类型_对象。
    v 的 对象属性 为 一组：（空）。
    返还 v。
完。

JS_创建数组 接受 零参：
    v 为 新建 JS值。
    v 的 类型 为 JS类型_数组。
    v 的 数组元素 为 一组：（空）。
    返还 v。
完。

JS_对象设置 接受 obj、键、值：
    若 obj 的 类型 不等于 JS类型_对象：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 obj 的 对象属性：
        若 obj 的 对象属性 的 第 i 项 的 第 0 项 等于 键：
            obj 的 对象属性 的 第 i 项 的 第 1 项 为 值。
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 obj 的 对象属性、一组：键、值。
完。

JS_对象获取 接受 obj、键：
    若 obj 的 类型 不等于 JS类型_对象：
        返还 做 JS_创建未定义。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 obj 的 对象属性：
        若 obj 的 对象属性 的 第 i 项 的 第 0 项 等于 键：
            返还 obj 的 对象属性 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 做 JS_创建未定义。
完。

JS_设置全局变量 接受 名称、值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 JS_运行时 的 全局作用域 的 变量表：
        若 JS_运行时 的 全局作用域 的 变量表 的 第 i 项 的 名称 等于 名称：
            JS_运行时 的 全局作用域 的 变量表 的 第 i 项 的 值 为 值。
            返还。
        完。
        i 为 i 加 1。
    完。
    变量 为 新建 JS变量。
    变量 的 名称 为 名称。
    变量 的 值 为 值。
    做 追加 用 JS_运行时 的 全局作用域 的 变量表、变量。
完。

JS_查找变量 接受 名称：
    可变 si 为 做 计数 用 JS_运行时 的 作用域栈 减 1。
    只要 si 大于或等于 0：
        作用域 为 JS_运行时 的 作用域栈 的 第 si 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 作用域 的 变量表：
            若 作用域 的 变量表 的 第 i 项 的 名称 等于 名称：
                返还 作用域 的 变量表 的 第 i 项 的 值。
            完。
            i 为 i 加 1。
        完。
        si 为 si 减 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 JS_运行时 的 全局作用域 的 变量表：
        若 JS_运行时 的 全局作用域 的 变量表 的 第 i 项 的 名称 等于 名称：
            返还 JS_运行时 的 全局作用域 的 变量表 的 第 i 项 的 值。
        完。
        i 为 i 加 1。
    完。
    返还 做 JS_创建未定义。
完。

本章 收束。
