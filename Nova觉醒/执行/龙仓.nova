《龙仓》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·标准·文件》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《..·中枢·时钟中枢》。

引用《..·中枢·字符串替换》。

引用《..·推理·智能体文件操作》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·应用Bundle》。

引用《..·中枢·能力声明》。

引用《..·中枢·设备绑定》。

引用《..·中枢·加密框架》。

引用《.·Bundle加载器》。

包信息 是 一种 事物，含 名称、版本、描述、入口、依赖、安装时间。

包目录 为 「/var/nova/packages」。

清单文件 为 「/var/nova/packages/清单.json」。

可变 已安装包 为 一组：（空）。

可变 龙仓_已初始化 为 假。

龙仓_初始化 接受 零参：
    若 龙仓_已初始化：
        返还。
    完。
    做 目录_创建 用 包目录。
    做 龙仓_加载清单。
    龙仓_已初始化 为 真。
完。

龙仓_main 接受 参数：
    做 龙仓_初始化。
    若 做 计数 用 参数 等于 0：
        做 龙仓_帮助。
        返还 0。
    完。
    命令 为 参数 的 第 0 项。
    若 命令 等于 「install」 并且 做 计数 用 参数 大于 1：
        返还 做 龙仓_安装 用 参数 的 第 1 项。
    完。
    若 命令 等于 「remove」 并且 做 计数 用 参数 大于 1：
        返还 做 龙仓_卸载 用 参数 的 第 1 项。
    完。
    若 命令 等于 「list」：
        返还 做 龙仓_列表。
    完。
    若 命令 等于 「info」 并且 做 计数 用 参数 大于 1：
        返还 做 龙仓_信息 用 参数 的 第 1 项。
    完。
    若 命令 等于 「update」：
        返还 做 龙仓_更新。
    完。
    若 命令 等于 「init」：
        返还 做 龙仓_初始化包 用 参数。
    完。
    若 命令 等于 「pack」 并且 做 计数 用 参数 大于 1：
        返还 做 龙仓_打包 用 参数 的 第 1 项。
    完。
    若 命令 等于 「verify」 并且 做 计数 用 参数 大于 1：
        返还 做 龙仓_校验Bundle 用 参数 的 第 1 项。
    完。
    若 命令 等于 「inspect」 并且 做 计数 用 参数 大于 1：
        返还 做 龙仓_查看Bundle 用 参数 的 第 1 项。
    完。
    做 打印行 用 「[龙仓] 未知命令: 」 加 命令。
    返还 1。
完。

龙仓_安装 接受 包名：
    若 做 龙仓_查找 用 包名 大于或等于 0：
        做 打印行 用 「[龙仓] 已安装: 」 加 包名。
        返还 0。
    完。
    做 打印行 用 「[龙仓] 安装: 」 加 包名。
    包路径 为 包目录 加 「/」 加 包名。
    包配置路径 为 包路径 加 「/包.json」。
    配置 为 做 文件_读全部 用 包配置路径。
    若 配置 不等于 空：
        做 打印行 用 「[龙仓] 从本地安装: 」 加 包路径。
    否则：
        做 目录_创建 用 包路径。
        做 打印行 用 「[龙仓] 创建包目录: 」 加 包路径。
    完。
    pkg 为 新建 包信息。
    pkg 的 名称 为 包名。
    pkg 的 版本 为 「0.1.0」。
    pkg 的 描述 为 「」。
    pkg 的 入口 为 「主函数.nova」。
    pkg 的 依赖 为 一组：（空）。
    pkg 的 安装时间 为 做 读取毫秒。
    做 追加 用 已安装包、pkg。
    做 龙仓_保存清单。
    做 打印行 用 「[龙仓] ✓ 安装完成: 」 加 包名 加 「 v」 加 pkg 的 版本。
    返还 0。
完。

龙仓_卸载 接受 包名：
    索引 为 做 龙仓_查找 用 包名。
    若 索引 小于 0：
        做 打印行 用 「[龙仓] 未安装: 」 加 包名。
        返还 1。
    完。
    做 打印行 用 「[龙仓] 卸载: 」 加 包名。
    包路径 为 包目录 加 「/」 加 包名。
    文件 为 做 智能体文件_搜索 用 包路径、「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文件：
        做 智能体文件_删除 用 文件 的 第 i 项。
        i 为 i 加 1。
    完。
    做 列表_移除 用 已安装包、索引。
    做 龙仓_保存清单。
    做 打印行 用 「[龙仓] ✓ 卸载完成: 」 加 包名。
    返还 0。
完。

龙仓_列表 接受 零参：
    做 打印行 用 「已安装的包 (」 加 做 整数转字符串 用 做 计数 用 已安装包 加 「 个):」。
    若 做 计数 用 已安装包 等于 0：
        做 打印行 用 「  (无)」。
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已安装包：
        pkg 为 已安装包 的 第 i 项。
        做 打印行 用 「  」 加 pkg 的 名称 加 「 v」 加 pkg 的 版本 加 「  」 加 pkg 的 描述。
        i 为 i 加 1。
    完。
    返还 0。
完。

龙仓_信息 接受 包名：
    索引 为 做 龙仓_查找 用 包名。
    若 索引 小于 0：
        做 打印行 用 「[龙仓] 未安装: 」 加 包名。
        返还 1。
    完。
    pkg 为 已安装包 的 第 索引 项。
    做 打印行 用 「包名: 」 加 pkg 的 名称。
    做 打印行 用 「版本: 」 加 pkg 的 版本。
    做 打印行 用 「描述: 」 加 pkg 的 描述。
    做 打印行 用 「入口: 」 加 pkg 的 入口。
    做 打印行 用 「依赖: 」 加 做 整数转字符串 用 做 计数 用 pkg 的 依赖 加 「 个」。
    返还 0。
完。

龙仓_更新 接受 零参：
    做 打印行 用 「[龙仓] 检查更新...」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已安装包：
        做 打印行 用 「  检查: 」 加 已安装包 的 第 i 项 的 名称 加 「 v」 加 已安装包 的 第 i 项 的 版本 加 「 → 最新」。
        i 为 i 加 1。
    完。
    做 打印行 用 「[龙仓] ✓ 所有包已是最新」。
    返还 0。
完。

龙仓_初始化包 接受 参数：
    可变 包名 为 「my_package」。
    若 做 计数 用 参数 大于 1：
        包名 为 参数 的 第 1 项。
    完。
    做 打印行 用 「[龙仓] 初始化包: 」 加 包名。
    配置 为 「{\"名称\":\"」 加 包名 加 「\",\"版本\":\"0.1.0\",\"描述\":\"\",\"入口\":\"主函数.nova\",\"依赖\":[]}」。
    做 文件_写全部 用 「包.json」、配置。
    做 打印行 用 「  创建 包.json」。
    返还 0。
完。

龙仓_加载清单 接受 零参：
    数据 为 做 文件_读全部 用 清单文件。
    若 数据 等于 空：
        已安装包 为 一组：（空）。
        返还。
    完。
    行 为 做 字符串分割 用 数据、「\n」。
    已安装包 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行：
        若 做 字符串长度 用 行 的 第 i 项 大于 0：
            部分 为 做 字符串分割 用 行 的 第 i 项、「@」。
            pkg 为 新建 包信息。
            pkg 的 名称 为 部分 的 第 0 项。
            若 做 计数 用 部分 大于 1：
                pkg 的 版本 为 部分 的 第 1 项。
            否则：
                pkg 的 版本 为 「0.0.0」。
            完。
            做 追加 用 已安装包、pkg。
        完。
        i 为 i 加 1。
    完。
完。

龙仓_保存清单 接受 零参：
    可变 内容 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已安装包：
        内容 为 内容 加 已安装包 的 第 i 项 的 名称 加 「@」 加 已安装包 的 第 i 项 的 版本 加 「\n」。
        i 为 i 加 1。
    完。
    做 文件_写全部 用 清单文件、内容。
完。

龙仓_查找 接受 包名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 已安装包：
        若 已安装包 的 第 i 项 的 名称 等于 包名：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

龙仓_帮助 接受 零参：
    做 打印行 用 「龙仓 v0.1 — Nova包管理器」。
    做 打印行 用 「」。
    做 打印行 用 「用法:」。
    做 打印行 用 「  龙仓 install <包名>  安装包」。
    做 打印行 用 「  龙仓 remove <包名>   卸载包」。
    做 打印行 用 「  龙仓 list            列出已安装」。
    做 打印行 用 「  龙仓 info <包名>     查看包详情」。
    做 打印行 用 「  龙仓 update          更新全部」。
    做 打印行 用 「  龙仓 init [包名]     初始化新包」。
    做 打印行 用 「  龙仓 pack <包名>     打包为签名Bundle(.napp)」。
    做 打印行 用 「  龙仓 verify <文件>   校验Bundle签名」。
    做 打印行 用 「  龙仓 inspect <文件>  查看Bundle信息」。
完。

可变 龙仓_签名密钥 为 一组：（空）。

龙仓_设签名密钥 接受 密钥：
    龙仓_签名密钥 为 密钥。
完。

龙仓_打包 接受 包名：
    索引 为 做 龙仓_查找 用 包名。
    若 索引 小于 0：
        做 打印行 用 「[龙仓] 未安装: 」 加 包名 加 「, 无法打包」。
        返还 1。
    完。
    pkg 为 已安装包 的 第 索引 项。
    做 打印行 用 「[龙仓] 打包: 」 加 包名 加 「 v」 加 pkg 的 版本 加 「 → .napp」。
    清单 为 做 Bundle_创建清单 用 包名、pkg 的 版本、BUNDLE_类型_应用、pkg 的 入口。
    做 Bundle_设作者 用 清单、「NovaOS开发者」。
    做 Bundle_设描述 用 清单、pkg 的 描述。
    做 Bundle_设签名方式 用 清单、BUNDLE_签名_HMAC256。
    做 Bundle_设加密 用 清单、BUNDLE_加密_AES256。
    包路径 为 包目录 加 「/」 加 包名。
    代码段 为 做 龙仓_收集代码 用 包路径。
    数据段 为 一组：（空）。
    水印值 为 做 水印_生成 用 水印层_包、「龙仓」、0。
    做 Bundle_设水印 用 清单、水印值。
    bundle数据 为 做 Bundle_打包 用 清单、代码段、数据段、龙仓_签名密钥。
    输出路径 为 包路径 加 「.napp」。
    做 文件_写字节 用 输出路径、bundle数据。
    做 打印行 用 「[龙仓] ✓ Bundle已生成: 」 加 输出路径。
    做 打印行 用 「  大小: 」 加 做 整数转字符串 用 做 计数 用 bundle数据 加 「 字节」。
    做 打印行 用 「  加密: AES-256-CBC」。
    做 打印行 用 「  签名: HMAC-SHA256」。
    做 打印行 用 「  水印: 」 加 做 整数转字符串 用 水印值。
    返还 0。
完。

龙仓_校验Bundle 接受 文件路径：
    做 打印行 用 「[龙仓] 校验: 」 加 文件路径。
    数据 为 做 文件_读字节 用 文件路径。
    若 数据 等于 空 或者 做 计数 用 数据 等于 0：
        做 打印行 用 「[龙仓] 无法读取文件」。
        返还 1。
    完。
    当前时间 为 做 读取毫秒 除以 1000。
    结果 为 做 Bundle_校验 用 数据、龙仓_签名密钥、「」、当前时间。
    若 结果 的 第 0 项 等于 BUNDLE_状态_签名有效：
        做 打印行 用 「[龙仓] ✓ 签名有效」。
        返还 0。
    否则：
        做 打印行 用 「[龙仓] ✗ 校验失败: 」 加 结果 的 第 1 项。
        返还 1。
    完。
完。

龙仓_查看Bundle 接受 文件路径：
    做 打印行 用 「[龙仓] 查看: 」 加 文件路径。
    数据 为 做 文件_读字节 用 文件路径。
    若 数据 等于 空 或者 做 计数 用 数据 小于 BUNDLE_头长度：
        做 打印行 用 「[龙仓] 无法读取或文件太短」。
        返还 1。
    完。
    魔数 为 做 Bundle_读u32 用 数据、0。
    若 魔数 不等于 BUNDLE_魔数：
        做 打印行 用 「[龙仓] 不是有效的Bundle文件」。
        返还 1。
    完。
    做 打印行 用 「  格式版本: 」 加 做 整数转字符串 用 做 Bundle_读u32 用 数据、4。
    做 打印行 用 「  包类型: 」 加 做 Bundle_类型名 用 做 Bundle_读u32 用 数据、8。
    做 打印行 用 「  加密: 」 加 做 Bundle_加密名 用 做 Bundle_读u32 用 数据、12。
    做 打印行 用 「  代码段: 」 加 做 整数转字符串 用 做 Bundle_读u64 用 数据、36 加 「 字节」。
    做 打印行 用 「  数据段: 」 加 做 整数转字符串 用 做 Bundle_读u64 用 数据、44 加 「 字节」。
    做 打印行 用 「  水印: 」 加 做 整数转字符串 用 做 Bundle_读u64 用 数据、56。
    做 打印行 用 「  总大小: 」 加 做 整数转字符串 用 做 计数 用 数据 加 「 字节」。
    返还 0。
完。

龙仓_收集代码 接受 包路径：
    可变 代码 为 一组：（空）。
    文件列表 为 做 智能体文件_搜索 用 包路径、「.nova」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文件列表：
        内容 为 做 文件_读字节 用 文件列表 的 第 i 项。
        若 内容 不等于 空：
            可变 j 为 0。
            只要 j 小于 做 计数 用 内容：
                做 追加 用 代码、内容 的 第 j 项。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 代码。
完。

本章 收束。
