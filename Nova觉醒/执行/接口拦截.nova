《接口拦截》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·推理·定点Q算术》。

引用《..·中枢·字符串替换》。

引用《..·标准·原生字符串》。

引用《..·中枢·整数转字符串》。

指纹伪装_拦截Navigator 接受 零参：
    指纹_拦截次数 为 指纹_拦截次数 加 1。
    返还 一组：一组：「user智能体」、指纹_当前配置 的 user智能体、一组：「platform」、指纹_当前配置 的 platform、一组：「language」、指纹_当前配置 的 语言、一组：「languages」、指纹_当前配置 的 语言列表、一组：「hardwareConcurrency」、做 整数转字符串 用 指纹_当前配置 的 硬件并发数、一组：「deviceMemory」、做 整数转字符串 用 指纹_当前配置 的 设备内存、一组：「maxTouchPoints」、做 整数转字符串 用 指纹_当前配置 的 最大触摸点、一组：「vendor」、「行业 Inc.」、一组：「appVersion」、做 字符串子串 用 指纹_当前配置 的 user智能体、8、做 字符串长度 用 指纹_当前配置 的 user智能体 减 8、一组：「cookieEnabled」、「true」、一组：「doNotTrack」、「null」、一组：「webdriver」、「false」。
完。

指纹伪装_拦截Screen 接受 零参：
    指纹_拦截次数 为 指纹_拦截次数 加 1。
    返还 一组：一组：「width」、指纹_当前配置 的 屏幕宽、一组：「height」、指纹_当前配置 的 屏幕高、一组：「availWidth」、指纹_当前配置 的 可用宽、一组：「availHeight」、指纹_当前配置 的 可用高、一组：「colorDepth」、指纹_当前配置 的 色深、一组：「pixelDepth」、指纹_当前配置 的 色深、一组：「devicePixelRatio」、指纹_当前配置 的 像素比。
完。

指纹伪装_画布噪声 接受 像素数据、长度：
    可变 种子 为 指纹_当前配置 的 画布噪声种子。
    强度 为 指纹_当前配置 的 画布噪声强度。
    可变 i 为 0。
    只要 i 小于 长度：
        种子 为 做 指纹_确定性随机 用 种子。
        噪声 为 种子 取余于 (强度 乘 2 加 1) 减 强度。
        原值 为 像素数据 的 第 i 项。
        新值 为 原值 加 噪声。
        若 新值 小于 0：
            新值 为 0。
        完。
        若 新值 大于 255：
            新值 为 255。
        完。
        若 i 取余于 4 不等于 3：
            像素数据 的 第 i 项 为 新值。
        完。
        i 为 i 加 1。
    完。
    指纹_拦截次数 为 指纹_拦截次数 加 1。
完。

指纹伪装_拦截图绘 接受 零参：
    指纹_拦截次数 为 指纹_拦截次数 加 1。
    返还 一组：一组：「UNMASKED_VENDOR_WEBGL」、指纹_当前配置 的 图绘厂商、一组：「UNMASKED_RENDERER_WEBGL」、指纹_当前配置 的 图绘渲染器、一组：「extensions」、指纹_当前配置 的 图绘扩展。
完。

指纹伪装_音频噪声 接受 音频数据、长度：
    可变 种子 为 指纹_当前配置 的 音频噪声种子。
    可变 i 为 0。
    只要 i 小于 长度：
        种子 为 做 指纹_确定性随机 用 种子。
        噪声 为 种子 取余于 100 减 50。
        音频数据 的 第 i 项 为 音频数据 的 第 i 项 加 噪声。
        i 为 i 加 1。
    完。
    指纹_拦截次数 为 指纹_拦截次数 加 1。
完。

指纹伪装_拦截字体枚举 接受 零参：
    指纹_拦截次数 为 指纹_拦截次数 加 1。
    返还 指纹_当前配置 的 安装字体。
完。

NKSI_版本 为 1。

NKSI_最大拦截钩子 为 128。

NKSI_最大属性代理 为 256。

NKSI_时序噪声范围 为 50。

NKSI_调用链深度限制 为 32。

NKSI_检测分数阈值 为 300。

NKSI_审计日志容量 为 512。

NKSI_AI特征维度 为 20。

拦截类型_属性读取 为 0x01。

拦截类型_属性写入 为 0x02。

拦截类型_函数调用 为 0x04。

拦截类型_原型访问 为 0x08。

拦截类型_枚举 为 0x10。

拦截类型_类型检查 为 0x20。

拦截类型_序列化 为 0x40。

可变 NKSI_拦截总数 为 0。

可变 NKSI_属性代理数 为 0。

可变 NKSI_调用链伪装数 为 0。

可变 NKSI_时序归一化数 为 0。

可变 NKSI_检测尝试数 为 0。

可变 NKSI_检测阻止数 为 0。

可变 NKSI_AI推理次数 为 0。

拦截钩子 是 一种 事物，含 对象路径、属性名、拦截类型、原始值缓存、伪装值、调用计数、最后调用时间、启用。

审计日志条目 是 一种 事物，含 时间戳、拦截钩子索引、调用者信息、拦截类型、结果。

可变 NKSI_钩子表 为 一组：（空）。

可变 NKSI_钩子数量 为 0。

可变 NKSI_审计日志 为 一组：（空）。

可变 NKSI_审计索引 为 0。

可变 NKSI_审计计数 为 0。

NKSI_初始化 接受 零参：
    NKSI_钩子表 为 做 数组_创建 用 NKSI_最大拦截钩子。
    可变 i 为 0。
    只要 i 小于 NKSI_最大拦截钩子：
        NKSI_钩子表 的 第 i 项 为 一本字典：对象路径 对应 「」、属性名 对应 「」、拦截类型 对应 0、原始值缓存 对应 空、伪装值 对应 空、调用计数 对应 0、最后调用时间 对应 0、启用 对应 假。
        i 为 i 加 1。
    完。
    NKSI_审计日志 为 做 数组_创建 用 NKSI_审计日志容量。
    可变 j 为 0。
    只要 j 小于 NKSI_审计日志容量：
        NKSI_审计日志 的 第 j 项 为 一本字典：时间戳 对应 0、拦截钩子索引 对应 0、调用者信息 对应 0、拦截类型 对应 0、结果 对应 0。
        j 为 j 加 1。
    完。
    做 NKSI_注册Navigator钩子。
    做 NKSI_注册Screen钩子。
    做 NKSI_注册画布钩子。
    做 NKSI_注册图绘钩子。
    做 NKSI_注册音频钩子。
    做 NKSI_注册WebRTC钩子。
    做 NKSI_注册电池钩子。
    做 NKSI_注册蓝牙钩子。
    返还 NKSI_钩子数量。
完。

NKSI_注册钩子 接受 对象路径、属性名、拦截类型、伪装值：
    若 NKSI_钩子数量 大于或等于 NKSI_最大拦截钩子：
        返还 负 1。
    完。
    NKSI_钩子表 的 第 NKSI_钩子数量 项 为 一本字典：对象路径 对应 对象路径、属性名 对应 属性名、拦截类型 对应 拦截类型、原始值缓存 对应 空、伪装值 对应 伪装值、调用计数 对应 0、最后调用时间 对应 0、启用 对应 真。
    NKSI_钩子数量 为 NKSI_钩子数量 加 1。
    返还 NKSI_钩子数量 减 1。
完。

NKSI_构造属性描述符 接受 钩子索引：
    若 钩子索引 小于 0 或者 钩子索引 大于或等于 NKSI_钩子数量：
        返还 空。
    完。
    钩子 为 NKSI_钩子表 的 第 钩子索引 项。
    描述符 为 一本字典：值 对应 钩子 的 伪装值、可写 对应 假、可枚举 对应 真、可配置 对应 假、getter标记 对应 「function get 」 加 钩子 的 属性名 加 「() { [native code] }」、setter标记 对应 空。
    若 钩子 的 拦截类型 位与 拦截类型_函数调用：
        描述符 的 toString伪装 为 「function 」 加 钩子 的 属性名 加 「() { [native code] }」。
    完。
    返还 描述符。
完。

NKSI_伪装调用栈 接受 原始栈、深度限制：
    可变 清洁栈 为 一组：（空）。
    可变 写入位 为 0。
    可变 读取位 为 0。
    只要 读取位 小于 做 数组长度 用 原始栈 并且 写入位 小于 深度限制：
        帧 为 原始栈 的 第 读取位 项。
        若 非 做 NKSI_是内核帧 用 帧：
            清洁栈 的 第 写入位 项 为 帧。
            写入位 为 写入位 加 1。
        完。
        读取位 为 读取位 加 1。
    完。
    NKSI_调用链伪装数 为 NKSI_调用链伪装数 加 1。
    返还 清洁栈。
完。

NKSI_是内核帧 接受 帧：
    若 做 字符串包含 用 帧、「NKSI_」：
        返还 真。
    完。
    若 做 字符串包含 用 帧、「指纹伪装_」：
        返还 真。
    完。
    若 做 字符串包含 用 帧、「内核拦截_」：
        返还 真。
    完。
    若 做 字符串包含 用 帧、「__nova_hook__」：
        返还 真。
    完。
    返还 假。
完。

NKSI_伪装函数toString 接受 函数对象、原生名称：
    返还 「function 」 加 原生名称 加 「() { [native code] }」。
完。

NKSI_拦截属性描述符查询 接受 对象、属性名：
    可变 i 为 0。
    只要 i 小于 NKSI_钩子数量：
        钩子 为 NKSI_钩子表 的 第 i 项。
        若 钩子 的 启用 并且 钩子 的 属性名 等于 属性名：
            NKSI_拦截总数 为 NKSI_拦截总数 加 1。
            钩子 的 调用计数 为 钩子 的 调用计数 加 1。
            钩子 的 最后调用时间 为 做 内核_读取时间戳。
            做 NKSI_写审计日志 用 i、0、拦截类型_类型检查、2。
            返还 做 NKSI_构造属性描述符 用 i。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

可变 NKSI_时序偏移 为 0。

可变 NKSI_时序种子 为 12345。

NKSI_时序归一化 接受 真实时间：
    NKSI_时序种子 为 做 指纹_确定性随机 用 NKSI_时序种子。
    噪声 为 NKSI_时序种子 取余于 (NKSI_时序噪声范围 乘 2 加 1) 减 NKSI_时序噪声范围。
    归一化时间 为 真实时间 加 噪声 减 NKSI_时序偏移。
    NKSI_时序归一化数 为 NKSI_时序归一化数 加 1。
    返还 归一化时间。
完。

NKSI_拦截性能计时 接受 零参：
    真实时间 为 做 内核_读取高精度时间。
    返还 做 NKSI_时序归一化 用 真实时间。
完。

NKSI_拦截日期时间 接受 零参：
    真实时间 为 做 内核_读取系统时间。
    归一化 为 真实时间 除以 1000 乘 1000。
    返还 归一化。
完。

NKSI_校准时序偏移 接受 零参：
    可变 总偏移 为 0。
    采样数 为 100。
    可变 i 为 0。
    只要 i 小于 采样数：
        开始 为 做 内核_读取TSC。
        做 NKSI_拦截属性描述符查询 用 空、「__测试__」。
        结束 为 做 内核_读取TSC。
        总偏移 为 总偏移 加 (结束 减 开始)。
        i 为 i 加 1。
    完。
    NKSI_时序偏移 为 总偏移 除以 采样数。
完。

NKSI_注册WebRTC钩子 接受 零参：
    做 NKSI_注册钩子 用 「window」、「RTCPeerConnection」、拦截类型_函数调用 位或 拦截类型_属性读取、NKSI_WebRTC代理构造器。
    做 NKSI_注册钩子 用 「RTCPeerConnection.prototype」、「createDataChannel」、拦截类型_函数调用、NKSI_WebRTC代理数据通道。
    做 NKSI_注册钩子 用 「RTCPeerConnection.prototype」、「createOffer」、拦截类型_函数调用、NKSI_WebRTC代理创建Offer。
完。

NKSI_WebRTC代理构造器 接受 配置：
    安全配置 为 一本字典：iceServers 对应 配置 的 iceServers、iceCandidatePoolSize 对应 0、iceTransportPolicy 对应 「relay」。
    返还 安全配置。
完。

NKSI_过滤ICE候选 接受 候选字符串：
    若 做 NKSI_包含私有IP 用 候选字符串：
        返还 做 NKSI_替换私有IP 用 候选字符串、指纹_当前配置 的 伪装本地IP。
    完。
    返还 候选字符串。
完。

NKSI_包含私有IP 接受 文本：
    若 做 字符串包含 用 文本、「192.168.」：
        返还 真。
    完。
    若 做 字符串包含 用 文本、「10.」：
        返还 真。
    完。
    若 做 字符串包含 用 文本、「172.16.」：
        返还 真。
    完。
    若 做 字符串包含 用 文本、「172.17.」：
        返还 真。
    完。
    若 做 字符串包含 用 文本、「172.18.」：
        返还 真。
    完。
    若 做 字符串包含 用 文本、「172.19.」：
        返还 真。
    完。
    若 做 字符串包含 用 文本、「172.2」：
        返还 真。
    完。
    若 做 字符串包含 用 文本、「172.30.」：
        返还 真。
    完。
    若 做 字符串包含 用 文本、「172.31.」：
        返还 真。
    完。
    返还 假。
完。

NKSI_替换私有IP 接受 文本、替换IP：
    可变 结果 为 文本。
    结果 为 做 字符串替换 用 结果、「192.168.」、替换IP。
    结果 为 做 字符串替换 用 结果、「10.」、替换IP。
    返还 结果。
完。

NKSI_WebRTC代理数据通道 接受 标签、选项：
    NKSI_拦截总数 为 NKSI_拦截总数 加 1。
    返还 一本字典：标签 对应 标签、选项 对应 选项、已代理 对应 真。
完。

NKSI_WebRTC代理创建Offer 接受 选项：
    NKSI_拦截总数 为 NKSI_拦截总数 加 1。
    返还 一本字典：选项 对应 选项、已代理 对应 真。
完。

NKSI_注册Navigator钩子 接受 零参：
    做 NKSI_注册钩子 用 「navigator」、「userAgent」、拦截类型_属性读取、指纹_当前配置 的 user智能体。
    做 NKSI_注册钩子 用 「navigator」、「platform」、拦截类型_属性读取、指纹_当前配置 的 platform。
    做 NKSI_注册钩子 用 「navigator」、「language」、拦截类型_属性读取、指纹_当前配置 的 语言。
    做 NKSI_注册钩子 用 「navigator」、「languages」、拦截类型_属性读取、指纹_当前配置 的 语言列表。
    做 NKSI_注册钩子 用 「navigator」、「hardwareConcurrency」、拦截类型_属性读取、指纹_当前配置 的 硬件并发数。
    做 NKSI_注册钩子 用 「navigator」、「deviceMemory」、拦截类型_属性读取、指纹_当前配置 的 设备内存。
    做 NKSI_注册钩子 用 「navigator」、「maxTouchPoints」、拦截类型_属性读取、指纹_当前配置 的 最大触摸点。
    做 NKSI_注册钩子 用 「navigator」、「webdriver」、拦截类型_属性读取、假。
    做 NKSI_注册钩子 用 「navigator」、「plugins」、拦截类型_属性读取 位或 拦截类型_枚举、做 NKSI_生成伪装插件列表。
    做 NKSI_注册钩子 用 「navigator」、「mimeTypes」、拦截类型_属性读取 位或 拦截类型_枚举、做 NKSI_生成伪装MIME列表。
    做 NKSI_注册钩子 用 「navigator」、「connection」、拦截类型_属性读取、做 NKSI_生成伪装网络信息。
完。

NKSI_注册Screen钩子 接受 零参：
    做 NKSI_注册钩子 用 「screen」、「width」、拦截类型_属性读取、指纹_当前配置 的 屏幕宽。
    做 NKSI_注册钩子 用 「screen」、「height」、拦截类型_属性读取、指纹_当前配置 的 屏幕高。
    做 NKSI_注册钩子 用 「screen」、「availWidth」、拦截类型_属性读取、指纹_当前配置 的 可用宽。
    做 NKSI_注册钩子 用 「screen」、「availHeight」、拦截类型_属性读取、指纹_当前配置 的 可用高。
    做 NKSI_注册钩子 用 「screen」、「colorDepth」、拦截类型_属性读取、指纹_当前配置 的 色深。
    做 NKSI_注册钩子 用 「screen」、「pixelDepth」、拦截类型_属性读取、指纹_当前配置 的 色深。
完。

NKSI_注册画布钩子 接受 零参：
    做 NKSI_注册钩子 用 「HTMLCanvasElement.prototype」、「toDataURL」、拦截类型_函数调用、NKSI_画布toDataURL代理。
    做 NKSI_注册钩子 用 「HTMLCanvasElement.prototype」、「toBlob」、拦截类型_函数调用、NKSI_画布toBlob代理。
    做 NKSI_注册钩子 用 「CanvasRenderingContext2D.prototype」、「getImageData」、拦截类型_函数调用、NKSI_画布getImageData代理。
完。

NKSI_注册图绘钩子 接受 零参：
    做 NKSI_注册钩子 用 「WebGLRenderingContext.prototype」、「getParameter」、拦截类型_函数调用、NKSI_图绘参数代理。
    做 NKSI_注册钩子 用 「WebGLRenderingContext.prototype」、「getExtension」、拦截类型_函数调用、NKSI_图绘扩展代理。
    做 NKSI_注册钩子 用 「WebGL2RenderingContext.prototype」、「getParameter」、拦截类型_函数调用、NKSI_图绘参数代理。
完。

NKSI_注册音频钩子 接受 零参：
    做 NKSI_注册钩子 用 「AudioContext.prototype」、「createAnalyser」、拦截类型_函数调用、NKSI_音频分析器代理。
    做 NKSI_注册钩子 用 「OfflineAudioContext.prototype」、「startRendering」、拦截类型_函数调用、NKSI_离线音频代理。
完。

NKSI_注册电池钩子 接受 零参：
    做 NKSI_注册钩子 用 「navigator」、「getBattery」、拦截类型_函数调用、NKSI_电池API代理。
完。

NKSI_注册蓝牙钩子 接受 零参：
    做 NKSI_注册钩子 用 「navigator」、「bluetooth」、拦截类型_属性读取、空。
完。

NKSI_生成伪装插件列表 接受 零参：
    返还 一组：一本字典：名称 对应 「PDF Viewer」、文件名 对应 「internal-pdf-viewer」、描述 对应 「Portable Document Format」、一本字典：名称 对应 「Nova PDF Viewer」、文件名 对应 「internal-pdf-viewer」、描述 对应 「Portable Document Format」。
完。

NKSI_生成伪装MIME列表 接受 零参：
    返还 一组：一本字典：类型 对应 「application/pdf」、描述 对应 「Portable Document Format」、后缀 对应 「pdf」。
完。

NKSI_生成伪装网络信息 接受 零参：
    返还 一本字典：有效类型 对应 「4g」、下行 对应 10、往返 对应 50、保存数据 对应 假。
完。

NKSI_画布toDataURL代理 接受 画布、类型、质量：
    上下文 为 做 画布 的 getContext 用 「2d」。
    图像数据 为 做 上下文 的 getImageData 用 0、0、画布 的 宽、画布 的 高。
    做 指纹伪装_画布噪声 用 图像数据 的 数据、图像数据 的 数据 的 长度。
    做 上下文 的 putImageData 用 图像数据、0、0。
    NKSI_拦截总数 为 NKSI_拦截总数 加 1。
    返还 做 画布 的 __原始toDataURL 用 类型、质量。
完。

NKSI_画布toBlob代理 接受 画布、回调、类型、质量：
    上下文 为 做 画布 的 getContext 用 「2d」。
    图像数据 为 做 上下文 的 getImageData 用 0、0、画布 的 宽、画布 的 高。
    做 指纹伪装_画布噪声 用 图像数据 的 数据、图像数据 的 数据 的 长度。
    做 上下文 的 putImageData 用 图像数据、0、0。
    NKSI_拦截总数 为 NKSI_拦截总数 加 1。
    返还 做 画布 的 __原始toBlob 用 回调、类型、质量。
完。

NKSI_画布getImageData代理 接受 上下文、x、y、宽、高：
    数据 为 做 上下文 的 __原始getImageData 用 x、y、宽、高。
    做 指纹伪装_画布噪声 用 数据 的 数据、数据 的 数据 的 长度。
    NKSI_拦截总数 为 NKSI_拦截总数 加 1。
    返还 数据。
完。

NKSI_图绘参数代理 接受 上下文、参数名：
    若 参数名 等于 0x9245：
        NKSI_拦截总数 为 NKSI_拦截总数 加 1。
        返还 指纹_当前配置 的 图绘厂商。
    完。
    若 参数名 等于 0x9246：
        NKSI_拦截总数 为 NKSI_拦截总数 加 1。
        返还 指纹_当前配置 的 图绘渲染器。
    完。
    返还 做 上下文 的 __原始getParameter 用 参数名。
完。

NKSI_图绘扩展代理 接受 上下文、扩展名：
    NKSI_拦截总数 为 NKSI_拦截总数 加 1。
    返还 做 上下文 的 __原始getExtension 用 扩展名。
完。

NKSI_音频分析器代理 接受 上下文：
    分析器 为 做 上下文 的 __原始createAnalyser。
    NKSI_拦截总数 为 NKSI_拦截总数 加 1。
    返还 分析器。
完。

NKSI_离线音频代理 接受 上下文：
    NKSI_拦截总数 为 NKSI_拦截总数 加 1。
    返还 做 上下文 的 __原始startRendering。
完。

NKSI_电池API代理 接受 零参：
    NKSI_拦截总数 为 NKSI_拦截总数 加 1。
    返还 一本字典：充电中 对应 真、充电时间 对应 0、放电时间 对应 999999、电量 对应 1.0。
完。

NKSI_计算检测分数 接受 零参：
    可变 总分 为 0。
    可变 总权重 为 0。
    UA一致性 为 做 NKSI_检查UA一致性。
    总分 为 总分 加 UA一致性 乘 30。
    总权重 为 总权重 加 30。
    画布稳定性 为 做 NKSI_检查画布稳定性。
    总分 为 总分 加 画布稳定性 乘 20。
    总权重 为 总权重 加 20。
    图绘一致性 为 做 NKSI_检查图绘一致性。
    总分 为 总分 加 图绘一致性 乘 15。
    总权重 为 总权重 加 15。
    时序正常 为 做 NKSI_检查时序正常性。
    总分 为 总分 加 时序正常 乘 20。
    总权重 为 总权重 加 20。
    toString一致性 为 做 NKSI_检查toString一致性。
    总分 为 总分 加 toString一致性 乘 15。
    总权重 为 总权重 加 15。
    若 总权重 等于 0：
        返还 0。
    完。
    返还 总分 乘 1000 除以 (总权重 乘 100)。
完。

NKSI_检查UA一致性 接受 零参：
    UA 为 指纹_当前配置 的 user智能体。
    平台 为 指纹_当前配置 的 platform。
    若 做 字符串包含 用 UA、「Windows」 并且 平台 不等于 「Win32」：
        返还 80。
    完。
    若 做 字符串包含 用 UA、「Mac」 并且 非 做 字符串包含 用 平台、「Mac」：
        返还 80。
    完。
    若 做 字符串包含 用 UA、「Linux」 并且 非 做 字符串包含 用 平台、「Linux」：
        返还 80。
    完。
    若 指纹_当前配置 的 硬件并发数 小于 2 或者 指纹_当前配置 的 硬件并发数 大于 128：
        返还 60。
    完。
    返还 10。
完。

NKSI_检查画布稳定性 接受 零参：
    返还 5。
完。

NKSI_检查图绘一致性 接受 零参：
    厂商 为 指纹_当前配置 的 图绘厂商。
    渲染器 为 指纹_当前配置 的 图绘渲染器。
    若 厂商 等于 「」 或者 渲染器 等于 「」：
        返还 70。
    完。
    返还 10。
完。

NKSI_检查时序正常性 接受 零参：
    若 NKSI_时序偏移 大于 100：
        返还 60。
    完。
    若 NKSI_时序偏移 大于 50：
        返还 30。
    完。
    返还 5。
完。

NKSI_检查toString一致性 接受 零参：
    返还 5。
完。

NKSI_AI预测检测风险 接受 零参：
    特征 为 做 Tensor_创建 用 一组：NKSI_AI特征维度。
    做 Tensor_写 用 特征、一组：0、NKSI_拦截总数。
    做 Tensor_写 用 特征、一组：1、NKSI_属性代理数。
    做 Tensor_写 用 特征、一组：2、NKSI_调用链伪装数。
    做 Tensor_写 用 特征、一组：3、NKSI_时序归一化数。
    做 Tensor_写 用 特征、一组：4、NKSI_检测尝试数。
    做 Tensor_写 用 特征、一组：5、NKSI_时序偏移。
    做 Tensor_写 用 特征、一组：6、NKSI_钩子数量。
    做 Tensor_写 用 特征、一组：7、做 NKSI_计算检测分数。
    可变 nav计数 为 0。
    可变 canvas计数 为 0。
    可变 webgl计数 为 0。
    可变 audio计数 为 0。
    可变 其他计数 为 0。
    可变 i 为 0。
    只要 i 小于 NKSI_钩子数量：
        钩子 为 NKSI_钩子表 的 第 i 项。
        若 做 字符串包含 用 钩子 的 对象路径、「navigator」：
            nav计数 为 nav计数 加 钩子 的 调用计数。
        否则若 做 字符串包含 用 钩子 的 对象路径、「Canvas」：
            canvas计数 为 canvas计数 加 钩子 的 调用计数。
        否则若 做 字符串包含 用 钩子 的 对象路径、「WebGL」：
            webgl计数 为 webgl计数 加 钩子 的 调用计数。
        否则若 做 字符串包含 用 钩子 的 对象路径、「Audio」：
            audio计数 为 audio计数 加 钩子 的 调用计数。
        否则：
            其他计数 为 其他计数 加 钩子 的 调用计数。
        完。
        i 为 i 加 1。
    完。
    做 Tensor_写 用 特征、一组：8、nav计数。
    做 Tensor_写 用 特征、一组：9、canvas计数。
    做 Tensor_写 用 特征、一组：10、webgl计数。
    做 Tensor_写 用 特征、一组：11、audio计数。
    做 Tensor_写 用 特征、一组：12、其他计数。
    做 Tensor_写 用 特征、一组：13、做 NKSI_检查UA一致性。
    近期计数 为 做 NKSI_计算近期拦截 用 1000。
    做 Tensor_写 用 特征、一组：14、近期计数。
    做 Tensor_写 用 特征、一组：15、做 NKSI_计算近期拦截 用 5000。
    做 Tensor_写 用 特征、一组：16、0。
    做 Tensor_写 用 特征、一组：17、0。
    做 Tensor_写 用 特征、一组：18、0。
    做 Tensor_写 用 特征、一组：19、0。
    预测 为 做 AI总线_推理 用 「NKSI检测预测」、特征。
    风险值 为 做 Tensor_读 用 预测、一组：0。
    NKSI_AI推理次数 为 NKSI_AI推理次数 加 1。
    做 Tensor_释放 用 特征。
    做 Tensor_释放 用 预测。
    若 风险值 大于 800：
        做 NKSI_降低拦截频率。
    完。
    返还 风险值。
完。

NKSI_计算近期拦截 接受 时间窗口：
    当前时间 为 做 内核_读取时间戳。
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 NKSI_审计计数：
        索引 为 (NKSI_审计索引 减 1 减 i 加 NKSI_审计日志容量) 取余于 NKSI_审计日志容量。
        若 当前时间 减 NKSI_审计日志 的 第 索引 项 的 时间戳 小于 时间窗口：
            计数 为 计数 加 1。
        否则：
            断开。
        完。
        i 为 i 加 1。
    完。
    返还 计数。
完。

NKSI_降低拦截频率 接受 零参：
    可变 i 为 0。
    只要 i 小于 NKSI_钩子数量：
        钩子 为 NKSI_钩子表 的 第 i 项。
        若 钩子 的 属性名 不等于 「webdriver」 并且 非 做 字符串包含 用 钩子 的 对象路径、「Canvas」 并且 非 做 字符串包含 用 钩子 的 对象路径、「WebGL」：
            若 钩子 的 调用计数 大于 100：
                钩子 的 启用 为 假。
            完。
        完。
        i 为 i 加 1。
    完。
完。

NKSI_AI异常检测 接受 零参：
    特征 为 做 Tensor_创建 用 一组：8。
    做 Tensor_写 用 特征、一组：0、做 NKSI_计算近期拦截 用 500。
    做 Tensor_写 用 特征、一组：1、NKSI_检测尝试数。
    做 Tensor_写 用 特征、一组：2、NKSI_拦截总数。
    做 Tensor_写 用 特征、一组：3、NKSI_调用链伪装数。
    做 Tensor_写 用 特征、一组：4、NKSI_时序归一化数。
    做 Tensor_写 用 特征、一组：5、NKSI_钩子数量。
    做 Tensor_写 用 特征、一组：6、NKSI_审计计数。
    做 Tensor_写 用 特征、一组：7、NKSI_检测阻止数。
    分数 为 做 AI总线_异常检测 用 「NKSI异常」、特征。
    做 Tensor_释放 用 特征。
    若 分数 大于 750：
        NKSI_检测尝试数 为 NKSI_检测尝试数 加 1。
        返还 真。
    完。
    返还 假。
完。

NKSI_写审计日志 接受 钩子索引、调用者信息、拦截类型、结果：
    NKSI_审计日志 的 第 NKSI_审计索引 项 为 一本字典：时间戳 对应 做 内核_读取时间戳、拦截钩子索引 对应 钩子索引、调用者信息 对应 调用者信息、拦截类型 对应 拦截类型、结果 对应 结果。
    NKSI_审计索引 为 (NKSI_审计索引 加 1) 取余于 NKSI_审计日志容量。
    若 NKSI_审计计数 小于 NKSI_审计日志容量：
        NKSI_审计计数 为 NKSI_审计计数 加 1。
    完。
完。

NKSI_获取统计 接受 零参：
    返还 一本字典：版本 对应 NKSI_版本、拦截总数 对应 NKSI_拦截总数、属性代理数 对应 NKSI_属性代理数、调用链伪装数 对应 NKSI_调用链伪装数、时序归一化数 对应 NKSI_时序归一化数、检测尝试数 对应 NKSI_检测尝试数、检测阻止数 对应 NKSI_检测阻止数、钩子数量 对应 NKSI_钩子数量、审计日志数 对应 NKSI_审计计数、AI推理次数 对应 NKSI_AI推理次数、时序偏移 对应 NKSI_时序偏移、当前检测分数 对应 做 NKSI_计算检测分数。
完。

NKSI_获取钩子状态 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 NKSI_钩子数量：
        钩子 为 NKSI_钩子表 的 第 i 项。
        结果 的 第 i 项 为 一本字典：对象路径 对应 钩子 的 对象路径、属性名 对应 钩子 的 属性名、拦截类型 对应 钩子 的 拦截类型、调用计数 对应 钩子 的 调用计数、启用 对应 钩子 的 启用。
        i 为 i 加 1。
    完。
    返还 结果。
完。

NKSI_获取审计日志 接受 最大条目：
    可变 结果 为 一组：（空）。
    数量 为 NKSI_审计计数。
    若 数量 大于 最大条目：
        数量 为 最大条目。
    完。
    可变 i 为 0。
    只要 i 小于 数量：
        索引 为 (NKSI_审计索引 减 1 减 i 加 NKSI_审计日志容量) 取余于 NKSI_审计日志容量。
        结果 的 第 i 项 为 NKSI_审计日志 的 第 索引 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

NKSI_重置统计 接受 零参：
    NKSI_拦截总数 为 0。
    NKSI_属性代理数 为 0。
    NKSI_调用链伪装数 为 0。
    NKSI_时序归一化数 为 0。
    NKSI_检测尝试数 为 0。
    NKSI_检测阻止数 为 0。
    NKSI_AI推理次数 为 0。
    NKSI_审计计数 为 0。
    NKSI_审计索引 为 0。
    可变 i 为 0。
    只要 i 小于 NKSI_钩子数量：
        NKSI_钩子表 的 第 i 项 的 调用计数 为 0。
        NKSI_钩子表 的 第 i 项 的 最后调用时间 为 0。
        i 为 i 加 1。
    完。
完。

本章 收束。
