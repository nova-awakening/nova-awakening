《内建取消》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《.·任务控制》。

引用《..·中枢·线程退出》。

引用《.·文本处理环》。

引用《..·中枢·自动扩容》。

引用《..·中枢·字符串替换》。

引用《.·设置重定向》。

引用《.·是内建命令》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内核打印》。

引用《.·历史管理》。

引用《..·中枢·退出调用》。

内建_unset 接受 参数：
    可变 i 为 1。
    只要 i 小于 做 计数 用 参数：
        做 环境_删除 用 参数 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 0。
完。

内建_help 接受 参数：
    若 做 计数 用 参数 大于 1：
        cmd 为 参数 的 第 1 项。
        若 cmd 等于 「cd」：
            做 打印行 用 「cd [-] [目录]  — 改变当前目录」。
            返还 0。
        完。
        若 cmd 等于 「echo」：
            做 打印行 用 「echo [-n] [-e] [文本...]  — 输出文本」。
            返还 0。
        完。
        若 cmd 等于 「export」：
            做 打印行 用 「export [变量=值...]  — 设置/导出环境变量」。
            返还 0。
        完。
        若 cmd 等于 「history」：
            做 打印行 用 「history [-c] [n]  — 显示/清除命令历史」。
            返还 0。
        完。
        做 打印行 用 「龙壳: help: 」 加 cmd 加 「: 无帮助信息」。
        返还 1。
    完。
    做 打印行 用 「龙壳 (Dragon终端) — NovaOS原生终端」。
    做 打印行 用 「内建命令:」。
    做 打印行 用 「  cd [目录]         改变工作目录」。
    做 打印行 用 「  pwd               显示当前目录」。
    做 打印行 用 「  echo [-n] [文本]  输出文本」。
    做 打印行 用 「  export [变量=值]  导出环境变量」。
    做 打印行 用 「  unset 变量        删除环境变量」。
    做 打印行 用 「  history           命令历史」。
    做 打印行 用 「  type 命令         显示命令类型」。
    做 打印行 用 「  alias 名=命令     定义别名」。
    做 打印行 用 「  jobs              显示后台任务」。
    做 打印行 用 「  fg [%n]           将任务放到前台」。
    做 打印行 用 「  bg [%n]           在后台继续任务」。
    做 打印行 用 「  kill [-信号] pid  发送信号」。
    做 打印行 用 「  read 变量         读取用户输入」。
    做 打印行 用 「  test 表达式       条件测试」。
    做 打印行 用 「  source 文件       执行脚本文件」。
    做 打印行 用 「  exit [码]         退出终端」。
    返还 0。
完。

内建_history 接受 参数：
    若 做 计数 用 参数 大于 1 并且 参数 的 第 1 项 等于 「-c」：
        做 历史_清除。
        返还 0。
    完。
    做 历史_显示。
    返还 0。
完。

内建_type 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        做 打印行 用 「用法: type 命令名」。
        返还 1。
    完。
    可变 i 为 1。
    只要 i 小于 做 计数 用 参数：
        名称 为 参数 的 第 i 项。
        若 做 别名_查找 用 名称 不等于 空：
            做 打印行 用 名称 加 「 是 `」 加 做 别名_查找 用 名称 加 「` 的别名」。
        否则若 做 是内建命令 用 名称：
            做 打印行 用 名称 加 「 是终端内建命令」。
        否则：
            路径 为 做 查找可执行文件 用 名称。
            若 路径 不等于 空：
                做 打印行 用 名称 加 「 是 」 加 路径。
            否则：
                做 打印行 用 「龙壳: type: 」 加 名称 加 「: 未找到」。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

可变 别名表 为 做 字典。

内建_alias 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        做 别名_显示全部。
        返还 0。
    完。
    可变 i 为 1。
    只要 i 小于 做 计数 用 参数：
        等号位 为 做 字符串索引 用 参数 的 第 i 项、「=」。
        若 等号位 大于或等于 0：
            名 为 做 字符串子串 用 参数 的 第 i 项、0、等号位。
            值 为 做 字符串子串 用 参数 的 第 i 项、等号位 加 1、做 字符串长度 用 参数 的 第 i 项。
            做 字典设 用 别名表、名、值。
        否则：
            值 为 做 别名_查找 用 参数 的 第 i 项。
            若 值 不等于 空：
                做 打印行 用 「alias 」 加 参数 的 第 i 项 加 「='」 加 值 加 「'」。
            否则：
                做 打印行 用 「龙壳: alias: 」 加 参数 的 第 i 项 加 「: 未找到」。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

内建_unalias 接受 参数：
    可变 i 为 1。
    只要 i 小于 做 计数 用 参数：
        做 字典设 用 别名表、参数 的 第 i 项、空。
        i 为 i 加 1。
    完。
    返还 0。
完。

别名_查找 接受 名称：
    返还 做 字典_取或默认 用 别名表、名称、空。
完。

别名_显示全部 接受 零参：
    做 字典_遍历 用 别名表、别名_打印项。
完。

别名_打印项 接受 键、值：
    做 打印行 用 「alias 」 加 键 加 「='」 加 值 加 「'」。
完。

内建_jobs 接受 参数：
    做 任务_列表。
    返还 0。
完。

内建_fg 接受 参数：
    可变 任务号 为 0。
    若 做 计数 用 参数 大于 1：
        arg 为 参数 的 第 1 项。
        若 做 字符串取字符 用 arg、0 等于 「%」：
            任务号 为 做 字符串转整数 用 做 字符串子串 用 arg、1、做 字符串长度 用 arg。
        否则：
            任务号 为 做 字符串转整数 用 arg。
        完。
    完。
    pid 为 做 任务_获取PID 用 任务号。
    若 pid 小于或等于 0：
        做 打印行 用 「龙壳: fg: 无此任务」。
        返还 1。
    完。
    做 sys_kill 用 pid、18、0、0、0、0。
    做 任务_设置状态 用 任务号、任务状态_前台。
    状态 为 一组：0。
    做 sys_wait4 用 pid、状态、0、空、0、0。
    做 任务_移除 用 任务号。
    返还 状态 的 第 0 项 右移 8 位与 0xFF。
完。

内建_bg 接受 参数：
    可变 任务号 为 0。
    若 做 计数 用 参数 大于 1：
        arg 为 参数 的 第 1 项。
        若 做 字符串取字符 用 arg、0 等于 「%」：
            任务号 为 做 字符串转整数 用 做 字符串子串 用 arg、1、做 字符串长度 用 arg。
        否则：
            任务号 为 做 字符串转整数 用 arg。
        完。
    完。
    pid 为 做 任务_获取PID 用 任务号。
    若 pid 小于或等于 0：
        做 打印行 用 「龙壳: bg: 无此任务」。
        返还 1。
    完。
    做 sys_kill 用 pid、18、0、0、0、0。
    做 任务_设置状态 用 任务号、任务状态_运行。
    返还 0。
完。

内建_wait 接受 参数：
    若 做 计数 用 参数 大于 1：
        pid 为 做 字符串转整数 用 参数 的 第 1 项。
        状态 为 一组：0。
        做 sys_wait4 用 pid、状态、0、空、0、0。
        返还 状态 的 第 0 项 右移 8 位与 0xFF。
    完。
    做 任务_等待全部。
    返还 0。
完。

内建_kill 接受 参数：
    若 做 计数 用 参数 小于或等于 1：
        做 打印行 用 「用法: kill [-信号] pid」。
        返还 1。
    完。
    可变 信号 为 15。
    可变 pid_idx 为 1。
    若 做 字符串取字符 用 参数 的 第 1 项、0 等于 「-」：
        sig_str 为 做 字符串子串 用 参数 的 第 1 项、1、做 字符串长度 用 参数 的 第 1 项。
        信号 为 做 字符串转整数 用 sig_str。
        pid_idx 为 2。
    完。
    若 pid_idx 大于或等于 做 计数 用 参数：
        做 打印行 用 「龙壳: kill: 缺少pid」。
        返还 1。
    完。
    pid 为 做 字符串转整数 用 参数 的 第 pid_idx 项。
    返还 做 sys_kill 用 pid、信号、0、0、0、0。
完。

可变 AI_内建取消_调用数 为 0。

可变 AI_内建取消_异常数 为 0。

AI_内建取消_监控 接受 零参：
    AI_内建取消_调用数 为 AI_内建取消_调用数 加 1。
    返还 AI_内建取消_异常数 等于 0。
完。

AI_内建取消_健康检查 接受 零参：
    若 AI_内建取消_调用数 大于 0 并且 AI_内建取消_异常数 乘 100 除以 AI_内建取消_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
