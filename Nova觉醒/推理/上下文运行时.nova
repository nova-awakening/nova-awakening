《上下文运行时》

引用《..·中枢·系统调用表》。

引用《..·推理·通信渠道》。

引用《.·JS作用域信息》。

引用《..·中枢·内核打印》。

引用《.·定点Q算术》。

引用《..·中枢·内容可见性》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《..·感知·内核直连层》。

引用《.·错误栈捕获》。

引用《.·JSGenerator创建》。

引用《.·JS对象解构》。

作用域_全局 为 0。

作用域_函数 为 1。

作用域_块 为 2。

作用域_With 为 3。

作用域_Catch 为 4。

作用域_模块 为 5。

作用域_Eval 为 6。

作用域_类体 为 7。

作用域_箭头函数 为 8。

声明_var 为 0。

声明_let 为 1。

声明_const 为 2。

声明_函数声明 为 3。

声明_形参 为 4。

声明_类名 为 5。

声明_导入 为 6。

位置_栈 为 0。

位置_上下文 为 1。

位置_全局 为 2。

位置_动态 为 3。

位置_未解析 为 4。

标记_未初始化 为 0。

标记_已初始化 为 1。

标记_TDZ处罚 为 2。

最大槽数 为 4096。

最大作用域链深度 为 512。

查找缓存容量 为 64。

变量记录大小 为 48。

AI_作用域优化阈值 为 0.65。

ScopeContext 是 一种 事物，含 外部上下文、作用域信息、槽、槽数、扩展、类型、深度、创建时间、访问计数、初始化标志、变量名哈希、变量声明类型、是严格模式、是模块作用域、已销毁。

VariableLookupResult 是 一种 事物，含 找到、位置、深度、槽索引、声明类型、是可变的、已初始化、是TDZ、上下文引用。

查找缓存项 是 一种 事物，含 名称哈希、深度、槽索引、有效。

作用域统计 是 一种 事物，含 Context创建总数、Context销毁总数、槽读取总数、槽写入总数、查找总数、查找命中数、查找未命中数、缓存命中数、TDZ违规数、闭包捐获数、提升操作数、最大链深度、当前活跃上下文数、AI优化触发数。

可变 全局作用域统计 为 新建 作用域统计。

可变 全局查找缓存 为 做 创建数组 用 查找缓存容量、新建 查找缓存项。

作用域_哈希名称 接受 名：
    可变 h 为 2166136261。
    可变 i 为 0。
    只要 i 小于 做 字符串_长度 用 名：
        h 为 h 位异或 做 字符串_读字节 用 名、i。
        h 为 h 乘 16777619。
        i 为 i 加 1。
    完。
    返还 h。
完。

作用域_当前时间 接受 零参：
    返还 做 平台_获取高精度时间。
完。

作用域_是严格声明 接受 类型：
    返还 类型 等于 声明_let 或者 类型 等于 声明_const 或者 类型 等于 声明_类名 或者 类型 等于 声明_导入。
完。

作用域_是可变声明 接受 类型：
    返还 类型 等于 声明_var 或者 类型 等于 声明_let 或者 类型 等于 声明_形参。
完。

ScopeContext_创建 接受 外部、scope_info：
    ctx 为 做 堆_分配 用 ScopeContext。
    若 ctx 等于 0：
        返还 0。
    完。
    ctx 的 外部上下文 为 外部。
    ctx 的 作用域信息 为 scope_info。
    ctx 的 槽数 为 scope_info 的 上下文槽数。
    若 ctx 的 槽数 大于 最大槽数：
        ctx 的 槽数 为 最大槽数。
    完。
    ctx 的 槽 为 做 堆_分配_数组 用 ctx 的 槽数 乘 8。
    ctx 的 初始化标志 为 做 堆_分配_数组 用 ctx 的 槽数。
    ctx 的 变量名哈希 为 做 堆_分配_数组 用 ctx 的 槽数 乘 4。
    ctx 的 变量声明类型 为 做 堆_分配_数组 用 ctx 的 槽数。
    可变 i 为 0。
    只要 i 小于 ctx 的 槽数：
        做 内存_分64 用 ctx 的 槽 加 i 乘 8、0。
        做 内存_分8 用 ctx 的 初始化标志 加 i、标记_未初始化。
        做 内存_分32 用 ctx 的 变量名哈希 加 i 乘 4、0。
        做 内存_分8 用 ctx 的 变量声明类型 加 i、声明_var。
        i 为 i 加 1。
    完。
    ctx 的 扩展 为 0。
    ctx 的 类型 为 作用域_块。
    ctx 的 深度 为 0。
    若 外部 不等于 0：
        ctx 的 深度 为 外部 的 深度 加 1。
    完。
    ctx 的 创建时间 为 做 作用域_当前时间。
    ctx 的 访问计数 为 0。
    ctx 的 是严格模式 为 假。
    ctx 的 是模块作用域 为 假。
    ctx 的 已销毁 为 假。
    全局作用域统计 的 Context创建总数 为 全局作用域统计 的 Context创建总数 加 1。
    全局作用域统计 的 当前活跃上下文数 为 全局作用域统计 的 当前活跃上下文数 加 1。
    若 ctx 的 深度 大于 全局作用域统计 的 最大链深度：
        全局作用域统计 的 最大链深度 为 ctx 的 深度。
    完。
    返还 ctx。
完。

ScopeContext_创建函数作用域 接受 外部、scope_info、严格模式：
    ctx 为 做 ScopeContext_创建 用 外部、scope_info。
    若 ctx 等于 0：
        返还 0。
    完。
    ctx 的 类型 为 作用域_函数。
    ctx 的 是严格模式 为 严格模式。
    返还 ctx。
完。

ScopeContext_创建With作用域 接受 外部、scope_info、with对象：
    ctx 为 做 ScopeContext_创建 用 外部、scope_info。
    若 ctx 等于 0：
        返还 0。
    完。
    ctx 的 类型 为 作用域_With。
    ctx 的 扩展 为 with对象。
    返还 ctx。
完。

ScopeContext_创建Catch作用域 接受 外部、scope_info：
    ctx 为 做 ScopeContext_创建 用 外部、scope_info。
    若 ctx 等于 0：
        返还 0。
    完。
    ctx 的 类型 为 作用域_Catch。
    返还 ctx。
完。

ScopeContext_创建模块作用域 接受 外部、scope_info：
    ctx 为 做 ScopeContext_创建 用 外部、scope_info。
    若 ctx 等于 0：
        返还 0。
    完。
    ctx 的 类型 为 作用域_模块。
    ctx 的 是模块作用域 为 真。
    ctx 的 是严格模式 为 真。
    返还 ctx。
完。

ScopeContext_创建Eval作用域 接受 外部、scope_info、严格模式：
    ctx 为 做 ScopeContext_创建 用 外部、scope_info。
    若 ctx 等于 0：
        返还 0。
    完。
    ctx 的 类型 为 作用域_Eval。
    ctx 的 是严格模式 为 严格模式。
    返还 ctx。
完。

ScopeContext_销毁 接受 ctx：
    若 ctx 等于 0 或者 ctx 的 已销毁：
        返还。
    完。
    ctx 的 已销毁 为 真。
    若 ctx 的 槽 不等于 0：
        做 堆_释放 用 ctx 的 槽。
    完。
    若 ctx 的 初始化标志 不等于 0：
        做 堆_释放 用 ctx 的 初始化标志。
    完。
    若 ctx 的 变量名哈希 不等于 0：
        做 堆_释放 用 ctx 的 变量名哈希。
    完。
    若 ctx 的 变量声明类型 不等于 0：
        做 堆_释放 用 ctx 的 变量声明类型。
    完。
    做 堆_释放 用 ctx。
    全局作用域统计 的 Context销毁总数 为 全局作用域统计 的 Context销毁总数 加 1。
    若 全局作用域统计 的 当前活跃上下文数 大于 0：
        全局作用域统计 的 当前活跃上下文数 为 全局作用域统计 的 当前活跃上下文数 减 1。
    完。
完。

ScopeContext_读取 接受 ctx、slot：
    若 ctx 等于 0 或者 ctx 的 已销毁：
        返还 0。
    完。
    若 slot 大于或等于 ctx 的 槽数：
        返还 0。
    完。
    ctx 的 访问计数 为 ctx 的 访问计数 加 1。
    全局作用域统计 的 槽读取总数 为 全局作用域统计 的 槽读取总数 加 1。
    返还 做 内存_读64 用 ctx 的 槽 加 slot 乘 8。
完。

ScopeContext_写入 接受 ctx、slot、值：
    若 ctx 等于 0 或者 ctx 的 已销毁：
        返还。
    完。
    若 slot 大于或等于 ctx 的 槽数：
        返还。
    完。
    做 内存_分64 用 ctx 的 槽 加 slot 乘 8、值。
    ctx 的 访问计数 为 ctx 的 访问计数 加 1。
    全局作用域统计 的 槽写入总数 为 全局作用域统计 的 槽写入总数 加 1。
完。

ScopeContext_安全读取 接受 ctx、slot：
    若 ctx 等于 0 或者 ctx 的 已销毁：
        返还 0。
    完。
    若 slot 大于或等于 ctx 的 槽数：
        返还 0。
    完。
    标志 为 做 内存_读8 用 ctx 的 初始化标志 加 slot。
    若 标志 等于 标记_未初始化：
        decl_type 为 做 内存_读8 用 ctx 的 变量声明类型 加 slot。
        若 做 作用域_是严格声明 用 decl_type：
            全局作用域统计 的 TDZ违规数 为 全局作用域统计 的 TDZ违规数 加 1。
            做 内存_分8 用 ctx 的 初始化标志 加 slot、标记_TDZ处罚。
            返还 0。
        完。
    完。
    ctx 的 访问计数 为 ctx 的 访问计数 加 1。
    全局作用域统计 的 槽读取总数 为 全局作用域统计 的 槽读取总数 加 1。
    返还 做 内存_读64 用 ctx 的 槽 加 slot 乘 8。
完。

ScopeContext_安全写入 接受 ctx、slot、值：
    若 ctx 等于 0 或者 ctx 的 已销毁：
        返还 假。
    完。
    若 slot 大于或等于 ctx 的 槽数：
        返还 假。
    完。
    decl_type 为 做 内存_读8 用 ctx 的 变量声明类型 加 slot。
    标志 为 做 内存_读8 用 ctx 的 初始化标志 加 slot。
    若 decl_type 等于 声明_const 并且 标志 等于 标记_已初始化：
        返还 假。
    完。
    做 内存_分64 用 ctx 的 槽 加 slot 乘 8、值。
    做 内存_分8 用 ctx 的 初始化标志 加 slot、标记_已初始化。
    ctx 的 访问计数 为 ctx 的 访问计数 加 1。
    全局作用域统计 的 槽写入总数 为 全局作用域统计 的 槽写入总数 加 1。
    返还 真。
完。

ScopeContext_设置槽元数据 接受 ctx、slot、名称哈希、声明类型：
    若 ctx 等于 0 或者 slot 大于或等于 ctx 的 槽数：
        返还。
    完。
    做 内存_分32 用 ctx 的 变量名哈希 加 slot 乘 4、名称哈希。
    做 内存_分8 用 ctx 的 变量声明类型 加 slot、声明类型。
完。

ScopeContext_检查TDZ 接受 ctx、slot：
    若 ctx 等于 0 或者 slot 大于或等于 ctx 的 槽数：
        返还 假。
    完。
    标志 为 做 内存_读8 用 ctx 的 初始化标志 加 slot。
    若 标志 不等于 标记_未初始化：
        返还 假。
    完。
    decl_type 为 做 内存_读8 用 ctx 的 变量声明类型 加 slot。
    返还 做 作用域_是严格声明 用 decl_type。
完。

Scope_查找变量 接受 scope、名：
    结果 为 新建 VariableLookupResult。
    结果 的 找到 为 假。
    结果 的 深度 为 0。
    结果 的 是TDZ 为 假。
    结果 的 上下文引用 为 0。
    全局作用域统计 的 查找总数 为 全局作用域统计 的 查找总数 加 1。
    名哈希 为 做 作用域_哈希名称 用 名。
    缓存结果 为 做 查找缓存_获取 用 名哈希。
    若 缓存结果 的 有效：
        全局作用域统计 的 缓存命中数 为 全局作用域统计 的 缓存命中数 加 1。
        结果 的 找到 为 真。
        结果 的 深度 为 缓存结果 的 深度。
        结果 的 槽索引 为 缓存结果 的 槽索引。
        结果 的 位置 为 位置_上下文。
        全局作用域统计 的 查找命中数 为 全局作用域统计 的 查找命中数 加 1。
        返还 结果。
    完。
    可变 当前scope 为 scope。
    可变 深度 为 0。
    只要 当前scope 不等于 0 并且 深度 小于 最大作用域链深度：
        idx 为 做 ScopeInfo_查找本地 用 当前scope、名。
        若 idx 大于或等于 0：
            偏移 为 当前scope 的 变量列表 加 idx 乘 变量记录大小。
            结果 的 找到 为 真。
            结果 的 位置 为 做 内存_读32 用 偏移 加 12。
            结果 的 深度 为 深度。
            结果 的 槽索引 为 做 内存_读32 用 偏移 加 16。
            结果 的 声明类型 为 做 内存_读32 用 偏移 加 8。
            结果 的 是可变的 为 做 内存_读8 用 偏移 加 20。
            结果 的 已初始化 为 做 内存_读8 用 偏移 加 21。
            结果 的 是TDZ 为 做 作用域_是严格声明 用 结果 的 声明类型 并且 非 结果 的 已初始化。
            做 查找缓存_设置 用 名哈希、深度、结果 的 槽索引。
            全局作用域统计 的 查找命中数 为 全局作用域统计 的 查找命中数 加 1。
            返还 结果。
        完。
        若 当前scope 的 类型 等于 作用域_With：
            结果 的 找到 为 假。
            结果 的 位置 为 位置_动态。
            结果 的 深度 为 深度。
            全局作用域统计 的 查找未命中数 为 全局作用域统计 的 查找未命中数 加 1。
            返还 结果。
        完。
        当前scope 为 当前scope 的 外部作用域。
        深度 为 深度 加 1。
    完。
    结果 的 找到 为 假。
    结果 的 位置 为 位置_全局。
    全局作用域统计 的 查找未命中数 为 全局作用域统计 的 查找未命中数 加 1。
    返还 结果。
完。

ContextChain_查找 接受 ctx、名哈希、槽索引：
    若 ctx 等于 0：
        返还 0。
    完。
    可变 当前 为 ctx。
    只要 当前 不等于 0：
        若 槽索引 小于 当前 的 槽数：
            stored_hash 为 做 内存_读32 用 当前 的 变量名哈希 加 槽索引 乘 4。
            若 stored_hash 等于 名哈希：
                返还 当前。
            完。
        完。
        当前 为 当前 的 外部上下文。
    完。
    返还 0。
完。

ContextChain_深度查找 接受 ctx、目标深度、槽索引：
    若 ctx 等于 0：
        返还 0。
    完。
    可变 当前 为 ctx。
    可变 d 为 0。
    只要 d 小于 目标深度 并且 当前 不等于 0：
        当前 为 当前 的 外部上下文。
        d 为 d 加 1。
    完。
    若 当前 等于 0：
        返还 0。
    完。
    返还 做 ScopeContext_读取 用 当前、槽索引。
完。

ContextChain_深度写入 接受 ctx、目标深度、槽索引、值：
    若 ctx 等于 0：
        返还。
    完。
    可变 当前 为 ctx。
    可变 d 为 0。
    只要 d 小于 目标深度 并且 当前 不等于 0：
        当前 为 当前 的 外部上下文。
        d 为 d 加 1。
    完。
    若 当前 不等于 0：
        做 ScopeContext_写入 用 当前、槽索引、值。
    完。
完。

ContextChain_安全深度读取 接受 ctx、目标深度、槽索引：
    若 ctx 等于 0：
        返还 0。
    完。
    可变 当前 为 ctx。
    可变 d 为 0。
    只要 d 小于 目标深度 并且 当前 不等于 0：
        当前 为 当前 的 外部上下文。
        d 为 d 加 1。
    完。
    若 当前 等于 0：
        返还 0。
    完。
    返还 做 ScopeContext_安全读取 用 当前、槽索引。
完。

ContextChain_获取深度 接受 ctx：
    若 ctx 等于 0：
        返还 0。
    完。
    返还 ctx 的 深度。
完。

With_动态查找 接受 ctx、名：
    若 ctx 等于 0 或者 ctx 的 类型 不等于 作用域_With：
        返还 0。
    完。
    若 ctx 的 扩展 等于 0：
        返还 0。
    完。
    返还 做 对象_获取属性 用 ctx 的 扩展、名。
完。

With_动态写入 接受 ctx、名、值：
    若 ctx 等于 0 或者 ctx 的 类型 不等于 作用域_With：
        返还 假。
    完。
    若 ctx 的 扩展 等于 0：
        返还 假。
    完。
    做 对象_设置属性 用 ctx 的 扩展、名、值。
    返还 真。
完。

Eval_动态查找 接受 ctx、名：
    若 ctx 等于 0：
        返还 0。
    完。
    若 ctx 的 类型 等于 作用域_Eval 并且 ctx 的 是严格模式：
        可变 i 为 0。
        只要 i 小于 ctx 的 槽数：
            h 为 做 内存_读32 用 ctx 的 变量名哈希 加 i 乘 4。
            若 h 等于 做 作用域_哈希名称 用 名：
                返还 做 ScopeContext_读取 用 ctx、i。
            完。
            i 为 i 加 1。
        完。
        返还 0。
    完。
    可变 当前 为 ctx 的 外部上下文。
    只要 当前 不等于 0：
        可变 i 为 0。
        只要 i 小于 当前 的 槽数：
            h 为 做 内存_读32 用 当前 的 变量名哈希 加 i 乘 4。
            若 h 等于 做 作用域_哈希名称 用 名：
                返还 做 ScopeContext_读取 用 当前、i。
            完。
            i 为 i 加 1。
        完。
        当前 为 当前 的 外部上下文。
    完。
    返还 0。
完。

查找缓存_获取 接受 名哈希：
    idx 为 名哈希 取余于 查找缓存容量。
    若 全局查找缓存 的 第 idx 项 的 有效 并且 全局查找缓存 的 第 idx 项 的 名称哈希 等于 名哈希：
        返还 全局查找缓存 的 第 idx 项。
    完。
    返还 新建 查找缓存项：有效 为 假。
完。

查找缓存_设置 接受 名哈希、深度、槽索引：
    idx 为 名哈希 取余于 查找缓存容量。
    全局查找缓存 的 第 idx 项 的 名称哈希 为 名哈希。
    全局查找缓存 的 第 idx 项 的 深度 为 深度。
    全局查找缓存 的 第 idx 项 的 槽索引 为 槽索引。
    全局查找缓存 的 第 idx 项 的 有效 为 真。
完。

查找缓存_无效化 接受 零参：
    可变 i 为 0。
    只要 i 小于 查找缓存容量：
        全局查找缓存 的 第 i 项 的 有效 为 假。
        i 为 i 加 1。
    完。
完。

闭包分析_标记引用 接受 外部scope、名、内部深度：
    若 外部scope 等于 0：
        返还 假。
    完。
    idx 为 做 ScopeInfo_查找本地 用 外部scope、名。
    若 idx 大于或等于 0：
        偏移 为 外部scope 的 变量列表 加 idx 乘 变量记录大小。
        做 内存_分8 用 偏移 加 24、1。
        若 做 内存_读32 用 偏移 加 12 等于 位置_栈：
            做 内存_分32 用 偏移 加 12、位置_上下文。
            全局作用域统计 的 提升操作数 为 全局作用域统计 的 提升操作数 加 1。
        完。
        全局作用域统计 的 闭包捐获数 为 全局作用域统计 的 闭包捐获数 加 1。
        返还 真。
    完。
    返还 做 闭包分析_标记引用 用 外部scope 的 外部作用域、名、内部深度 加 1。
完。

变量提升_执行 接受 scope：
    若 scope 等于 0：
        返还。
    完。
    可变 func_scope 为 scope。
    只要 func_scope 不等于 0：
        若 func_scope 的 类型 等于 作用域_函数 或者 func_scope 的 类型 等于 作用域_全局 或者 func_scope 的 类型 等于 作用域_模块：
            跳出。
        完。
        func_scope 为 func_scope 的 外部作用域。
    完。
    若 func_scope 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 scope 的 变量数量：
        偏移 为 scope 的 变量列表 加 i 乘 变量记录大小。
        decl_type 为 做 内存_读32 用 偏移 加 8。
        做 匹配 用 decl_type。
        i 为 i 加 1。
    完。
完。

ScopeContext_遍历链 接受 ctx、回调：
    若 ctx 等于 0：
        返还。
    完。
    可变 当前 为 ctx。
    只要 当前 不等于 0：
        做 回调 用 当前。
        当前 为 当前 的 外部上下文。
    完。
完。

ScopeContext_获取诊断信息 接受 ctx：
    若 ctx 等于 0：
        返还 空。
    完。
    可变 使用中槽数 为 0。
    可变 tdz槽数 为 0。
    可变 i 为 0。
    只要 i 小于 ctx 的 槽数：
        标志 为 做 内存_读8 用 ctx 的 初始化标志 加 i。
        若 标志 等于 标记_已初始化：
            使用中槽数 为 使用中槽数 加 1。
        完。
        若 标志 等于 标记_未初始化 并且 做 作用域_是严格声明 用 做 内存_读8 用 ctx 的 变量声明类型 加 i：
            tdz槽数 为 tdz槽数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一本字典：类型 对应 ctx 的 类型、深度 对应 ctx 的 深度、总槽数 对应 ctx 的 槽数、使用中槽数 对应 使用中槽数、TDZ槽数 对应 tdz槽数、访问计数 对应 ctx 的 访问计数、是严格模式 对应 ctx 的 是严格模式、是模块 对应 ctx 的 是模块作用域、已销毁 对应 ctx 的 已销毁。
完。

AI_作用域优化分析 接受 ctx：
    若 ctx 等于 0：
        返还。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    feature 为 做 Tensor_创建 用 8。
    做 Tensor_写 用 feature、0、ctx 的 深度 除以 最大作用域链深度。
    做 Tensor_写 用 feature、1、ctx 的 槽数 除以 最大槽数。
    做 Tensor_写 用 feature、2、ctx 的 访问计数 除以 10000.0。
    做 Tensor_写 用 feature、3、ctx 的 类型 除以 8.0。
    做 Tensor_写 用 feature、4、全局作用域统计 的 查找命中数 除以 (全局作用域统计 的 查找总数 加 1.0)。
    做 Tensor_写 用 feature、5、全局作用域统计 的 缓存命中数 除以 (全局作用域统计 的 查找总数 加 1.0)。
    做 Tensor_写 用 feature、6、全局作用域统计 的 TDZ违规数 除以 100.0。
    做 Tensor_写 用 feature、7、全局作用域统计 的 闭包捐获数 除以 1000.0。
    优化建议 为 做 AI总线_推理 用 bus、「SCED_作用域优化」、feature。
    风险 为 做 AI总线_风险评分 用 bus、「SCED_作用域风险」、feature。
    做 Tensor_释放 用 feature。
    全局作用域统计 的 AI优化触发数 为 全局作用域统计 的 AI优化触发数 加 1。
    若 优化建议 大于 AI_作用域优化阈值 并且 风险 小于 0.3：
        若 全局作用域统计 的 缓存命中数 小于 全局作用域统计 的 查找总数 除以 4：
            做 查找缓存_无效化。
        完。
        若 ctx 的 深度 大于 32：
            做 内核打印 用 「SCED警告: 作用域链深度={}, 可能影响性能」、ctx 的 深度。
        完。
    完。
完。

作用域_获取统计 接受 零参：
    返还 一本字典：Context创建总数 对应 全局作用域统计 的 Context创建总数、Context销毁总数 对应 全局作用域统计 的 Context销毁总数、槽读取总数 对应 全局作用域统计 的 槽读取总数、槽写入总数 对应 全局作用域统计 的 槽写入总数、查找总数 对应 全局作用域统计 的 查找总数、查找命中数 对应 全局作用域统计 的 查找命中数、查找未命中数 对应 全局作用域统计 的 查找未命中数、缓存命中数 对应 全局作用域统计 的 缓存命中数、TDZ违规数 对应 全局作用域统计 的 TDZ违规数、闭包捐获数 对应 全局作用域统计 的 闭包捐获数、提升操作数 对应 全局作用域统计 的 提升操作数、最大链深度 对应 全局作用域统计 的 最大链深度、当前活跃上下文数 对应 全局作用域统计 的 当前活跃上下文数、AI优化触发数 对应 全局作用域统计 的 AI优化触发数。
完。

作用域_打印统计 接受 零参：
    s 为 做 作用域_获取统计。
    做 内核打印 用 「═══ SCED作用域统计 ═══」。
    做 内核打印 用 「Context[创建/销毁/活跃]={}/{}/{}」、s 的 Context创建总数、s 的 Context销毁总数、s 的 当前活跃上下文数。
    做 内核打印 用 「槽[读/写]={}/{}」、s 的 槽读取总数、s 的 槽写入总数。
    做 内核打印 用 「查找[总/命中/未命中/缓存]={}/{}/{}/{}」、s 的 查找总数、s 的 查找命中数、s 的 查找未命中数、s 的 缓存命中数。
    做 内核打印 用 「TDZ违规={} 闭包捐获={} 提升={} 最大深度={}」、s 的 TDZ违规数、s 的 闭包捐获数、s 的 提升操作数、s 的 最大链深度。
    做 内核打印 用 「AI优化触发={}」、s 的 AI优化触发数。
完。

上下文运行时_附加错误栈 接受 Nova运行时实例、目标对象、构造函数过滤：
    若 Nova运行时实例 等于 0 或者 目标对象 等于 0：
        返还 0。
    完。
    做 错误_captureStackTrace 用 Nova运行时实例、目标对象、构造函数过滤。
    返还 目标对象。
完。

上下文运行时_创建脚本器 接受 Nova运行时实例、函数、字节码、字节码长度、上下文、receiver：
    返还 做 JSGenerator_创建 用 Nova运行时实例、以 零参 为 引，其做法是：、字节码、字节码长度、上下文、receiver。
完。

上下文运行时_创建异步脚本器 接受 Nova运行时实例、函数、字节码、字节码长度、上下文、receiver：
    返还 做 AsyncGenerator_创建 用 Nova运行时实例、以 零参 为 引，其做法是：、字节码、字节码长度、上下文、receiver。
完。

上下文运行时_获取迭代器 接受 对象：
    返还 做 获取迭代器 用 对象。
完。

上下文运行时_执行ForOf 接受 可迭代对象、回调函数：
    返还 做 ForOf_执行 用 可迭代对象、回调函数。
完。

上下文运行时_对象解构 接受 Nova运行时实例、源、绑定列表、绑定数、有rest：
    做 NJSD_初始化。
    返还 做 NJSD_增强对象解构 用 Nova运行时实例、源、绑定列表、绑定数、有rest。
完。

上下文运行时_数组解构 接受 Nova运行时实例、源、索引数、有rest：
    做 NJSD_初始化。
    返还 做 Destructure_数组 用 Nova运行时实例、源、索引数、有rest。
完。

作用域_重置统计 接受 零参：
    全局作用域统计 为 新建 作用域统计。
完。

本章 收束。
