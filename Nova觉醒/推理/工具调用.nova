《工具调用》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《.·通信渠道》。

引用《..·中枢·内核打印》。

引用《..·中枢·时钟中枢》。

引用《.·定点Q算术》。

引用《..·感知·内核直连层》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

ATCI_最大重试 为 3。

ATCI_基础退避_ms 为 100。

ATCI_最大退避_ms 为 5000。

ATCI_缓存容量 为 64。

ATCI_缓存TTL 为 300000。

ATCI_最大并行 为 8。

ATCI_FNV_OFFSET 为 0x811C9DC5。

ATCI_FNV_PRIME 为 0x01000193。

工具定义 是 一种 事物，含 名称、描述、参数Schema、执行函数、超时毫秒、调用次数、成功次数、失败次数、平均耗时、权限级别、可缓存、可重试。

ATCI统计 是 一种 事物，含 总调用数、总成功数、总失败数、重试次数、重试成功数、缓存命中数、缓存未中数、并行调用数、链式调用数、权限拒绝数、AI选择数。

可变 工具注册表 为 一组：（空）。

可变 全局ATCI统计 为 新建 ATCI统计。

工具_注册 接受 名称、描述、参数Schema、执行函数、超时：
    t 为 新建 工具定义：名称 为 名称、描述 为 描述、参数Schema 为 参数Schema、执行函数 为 执行函数、超时毫秒 为 超时、调用次数 为 0、成功次数 为 0、失败次数 为 0、平均耗时 为 0、权限级别 为 0、可缓存 为 假、可重试 为 真。
    做 追加 用 工具注册表、t。
完。

工具_注册高级 接受 名称、描述、schema、执行、超时、权限、可缓存、可重试：
    t 为 新建 工具定义：名称 为 名称、描述 为 描述、参数Schema 为 schema、执行函数 为 执行、超时毫秒 为 超时、调用次数 为 0、成功次数 为 0、失败次数 为 0、平均耗时 为 0、权限级别 为 权限、可缓存 为 可缓存、可重试 为 可重试。
    做 追加 用 工具注册表、t。
完。

工具_查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 工具注册表：
        若 工具注册表 的 第 i 项 的 名称 等于 名称：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

工具_删除 接受 名称：
    idx 为 做 工具_查找 用 名称。
    若 idx 小于 0：
        返还 假。
    完。
    做 删除 用 工具注册表、idx。
    返还 真。
完。

工具_列表 接受 零参：
    返还 工具注册表。
完。

工具_数量 接受 零参：
    返还 做 计数 用 工具注册表。
完。

ATCI_任务相关度 接受 任务描述、tool：
    任务 为 做 字符串_转小写 用 任务描述。
    名称 为 做 字符串_转小写 用 tool 的 名称。
    描述 为 做 字符串_转小写 用 tool 的 描述。
    若 任务 等于 「」：
        若 名称 等于 「ai_query」：
            返还 700。
        完。
        返还 400。
    完。
    分 为 400。
    若 做 字符串_包含 用 任务、「读取」 或者 做 字符串_包含 用 任务、「read」：
        若 名称 等于 「file_read」 或者 做 字符串_包含 用 名称、「read」 或者 做 字符串_包含 用 描述、「读取」：
            分 为 1000。
        完。
        做 否则如果 用 名称 等于 「file_edit」 或者 做 字符串_包含 用 名称、「write」。
        块：
            分 为 650。
        完。
        做 否则如果 用 名称 等于 「code_search」。
        块：
            分 为 350。
        完。
        做 否则如果 用 名称 等于 「ai_query」。
        块：
            分 为 250。
        完。
    完。
    做 否则如果 用 做 字符串_包含 用 任务、「写入」 或者 做 字符串_包含 用 任务、「write」 或者 做 字符串_包含 用 任务、「保存」。
    块：
        若 名称 等于 「file_write」：
            分 为 1000。
        完。
        做 否则如果 用 名称 等于 「file_edit」。
        块：
            分 为 900。
        完。
        做 否则如果 用 名称 等于 「file_read」。
        块：
            分 为 150。
        完。
        做 否则如果 用 名称 等于 「ai_query」。
        块：
            分 为 300。
        完。
    完。
    做 否则如果 用 做 字符串_包含 用 任务、「编辑」 或者 做 字符串_包含 用 任务、「edit」 或者 做 字符串_包含 用 任务、「修改」。
    块：
        若 名称 等于 「file_edit」：
            分 为 1000。
        完。
        做 否则如果 用 名称 等于 「file_write」。
        块：
            分 为 850。
        完。
        做 否则如果 用 名称 等于 「file_read」。
        块：
            分 为 250。
        完。
        做 否则如果 用 名称 等于 「code_search」。
        块：
            分 为 350。
        完。
        做 否则如果 用 名称 等于 「ai_query」。
        块：
            分 为 300。
        完。
    完。
    做 否则如果 用 做 字符串_包含 用 任务、「命令」 或者 做 字符串_包含 用 任务、「exec」 或者 做 字符串_包含 用 任务、「终端」。
    块：
        若 名称 等于 「终端_exec」：
            分 为 1000。
        完。
        做 否则如果 用 名称 等于 「ai_query」。
        块：
            分 为 350。
        完。
        做 否则如果 用 名称 等于 「web_search」。
        块：
            分 为 250。
        完。
        做 否则如果 用 名称 等于 「code_search」。
        块：
            分 为 250。
        完。
    完。
    做 否则如果 用 做 字符串_包含 用 任务、「搜索」 或者 做 字符串_包含 用 任务、「search」 或者 做 字符串_包含 用 任务、「查找」 或者 做 字符串_包含 用 任务、「查询」。
    块：
        若 做 字符串_包含 用 任务、「代码」 或者 做 字符串_包含 用 任务、「源码」 或者 做 字符串_包含 用 任务、「path」：
            若 名称 等于 「code_search」：
                分 为 1000。
            完。
            做 否则如果 用 名称 等于 「web_search」。
            块：
                分 为 700。
            完。
            做 否则如果 用 名称 等于 「ai_query」。
            块：
                分 为 500。
            完。
        否则：
            若 名称 等于 「web_search」：
                分 为 1000。
            完。
            做 否则如果 用 名称 等于 「code_search」。
            块：
                分 为 650。
            完。
            做 否则如果 用 名称 等于 「ai_query」。
            块：
                分 为 550。
            完。
        完。
    完。
    做 否则如果 用 做 字符串_包含 用 任务、「ai」 或者 做 字符串_包含 用 任务、「推理」 或者 做 字符串_包含 用 任务、「分析」 或者 做 字符串_包含 用 任务、「总结」 或者 做 字符串_包含 用 任务、「解释」。
    块：
        若 名称 等于 「ai_query」：
            分 为 1000。
        完。
        做 否则如果 用 名称 等于 「web_search」。
        块：
            分 为 350。
        完。
        做 否则如果 用 名称 等于 「code_search」。
        块：
            分 为 250。
        完。
    完。
    做 否则如果 用 名称 等于 「ai_query」。
    块：
        分 为 700。
    完。
    若 分 小于 0：
        分 为 0。
    完。
    若 分 大于 1000：
        分 为 1000。
    完。
    返还 分。
完。

工具_调用 接受 名称、参数：
    全局ATCI统计 的 总调用数 为 全局ATCI统计 的 总调用数 加 1。
    idx 为 做 工具_查找 用 名称。
    若 idx 小于 0：
        全局ATCI统计 的 总失败数 为 全局ATCI统计 的 总失败数 加 1。
        返还 一本字典：成功 对应 假、错误 对应 「工具不存在: 」 加 名称。
    完。
    返还 做 工具_执行单次 用 工具注册表 的 第 idx 项、参数。
完。

工具_执行单次 接受 tool、参数：
    tool 的 调用次数 为 tool 的 调用次数 加 1。
    开始 为 做 读取毫秒。
    结果 为 做 tool 的 执行函数 用 参数。
    耗时 为 做 读取毫秒 减 开始。
    若 耗时 大于 tool 的 超时毫秒：
        tool 的 失败次数 为 tool 的 失败次数 加 1。
        全局ATCI统计 的 总失败数 为 全局ATCI统计 的 总失败数 加 1。
        返还 一本字典：成功 对应 假、错误 对应 「超时(」 加 做 整数转字符串 用 耗时 加 「ms)」、耗时 对应 耗时。
    完。
    tool 的 平均耗时 为 (tool 的 平均耗时 乘 922 加 耗时 乘 1024 乘 102) 除以 (1024 乘 1024)。
    若 结果 不等于 空：
        tool 的 成功次数 为 tool 的 成功次数 加 1。
        全局ATCI统计 的 总成功数 为 全局ATCI统计 的 总成功数 加 1。
        返还 一本字典：成功 对应 真、结果 对应 结果、耗时 对应 耗时。
    完。
    tool 的 失败次数 为 tool 的 失败次数 加 1。
    全局ATCI统计 的 总失败数 为 全局ATCI统计 的 总失败数 加 1。
    返还 一本字典：成功 对应 假、错误 对应 「执行失败」、耗时 对应 耗时。
完。

ATCI_计算退避延迟 接受 重试次数：
    延迟 为 ATCI_基础退避_ms。
    可变 n 为 0。
    只要 n 小于 重试次数：
        延迟 为 延迟 乘 2。
        n 为 n 加 1。
    完。
    若 延迟 大于 ATCI_最大退避_ms：
        延迟 为 ATCI_最大退避_ms。
    完。
    返还 延迟。
完。

工具_调用_带重试 接受 名称、参数：
    idx 为 做 工具_查找 用 名称。
    若 idx 小于 0：
        全局ATCI统计 的 总失败数 为 全局ATCI统计 的 总失败数 加 1。
        返还 一本字典：成功 对应 假、错误 对应 「工具不存在: 」 加 名称。
    完。
    tool 为 工具注册表 的 第 idx 项。
    最大 为 ATCI_最大重试。
    若 非 tool 的 可重试：
        最大 为 0。
    完。
    可变 尝试 为 0。
    只要 尝试 小于或等于 最大：
        全局ATCI统计 的 总调用数 为 全局ATCI统计 的 总调用数 加 1。
        结果 为 做 工具_执行单次 用 tool、参数。
        若 结果 的 成功：
            若 尝试 大于 0：
                全局ATCI统计 的 重试成功数 为 全局ATCI统计 的 重试成功数 加 1。
            完。
            返还 结果。
        完。
        若 尝试 小于 最大：
            全局ATCI统计 的 重试次数 为 全局ATCI统计 的 重试次数 加 1。
            延迟 为 做 ATCI_计算退避延迟 用 尝试。
            做 睡眠_毫秒 用 延迟。
        完。
        尝试 为 尝试 加 1。
    完。
    返还 一本字典：成功 对应 假、错误 对应 「重试耗尽(」 加 做 整数转字符串 用 最大 加 「次)」。
完。

缓存条目 是 一种 事物，含 键、结果、时间戳、命中数。

可变 工具缓存 为 一组：（空）。

ATCI_缓存键 接受 名称、参数串：
    组合 为 名称 加 「|」 加 参数串。
    hash 为 ATCI_FNV_OFFSET。
    长度 为 做 字符串_长度 用 组合。
    可变 i 为 0。
    只要 i 小于 长度：
        hash 为 (hash 位异或 做 内存_读8 用 组合 加 i) 乘 ATCI_FNV_PRIME。
        i 为 i 加 1。
    完。
    返还 hash。
完。

ATCI_缓存查找 接受 键：
    now 为 做 读取毫秒。
    可变 i 为 0。
    只要 i 小于 做 计数 用 工具缓存：
        若 工具缓存 的 第 i 项 的 键 等于 键：
            若 now 减 工具缓存 的 第 i 项 的 时间戳 大于 ATCI_缓存TTL：
                做 删除 用 工具缓存、i。
                返还 空。
            完。
            工具缓存 的 第 i 项 的 命中数 为 工具缓存 的 第 i 项 的 命中数 加 1。
            全局ATCI统计 的 缓存命中数 为 全局ATCI统计 的 缓存命中数 加 1。
            返还 工具缓存 的 第 i 项 的 结果。
        完。
        i 为 i 加 1。
    完。
    全局ATCI统计 的 缓存未中数 为 全局ATCI统计 的 缓存未中数 加 1。
    返还 空。
完。

ATCI_缓存写入 接受 键、结果：
    若 做 计数 用 工具缓存 大于或等于 ATCI_缓存容量：
        做 删除 用 工具缓存、0。
    完。
    做 追加 用 工具缓存、新建 缓存条目：键 为 键、结果 为 结果、时间戳 为 做 读取毫秒、命中数 为 0。
完。

工具_调用_带缓存 接受 名称、参数、参数串：
    idx 为 做 工具_查找 用 名称。
    若 idx 小于 0：
        返还 一本字典：成功 对应 假、错误 对应 「工具不存在」。
    完。
    tool 为 工具注册表 的 第 idx 项。
    若 tool 的 可缓存：
        键 为 做 ATCI_缓存键 用 名称、参数串。
        cached 为 做 ATCI_缓存查找 用 键。
        若 cached 不等于 空：
            返还 一本字典：成功 对应 真、结果 对应 cached、耗时 对应 0、缓存 对应 真。
        完。
        结果 为 做 工具_调用 用 名称、参数。
        若 结果 的 成功：
            做 ATCI_缓存写入 用 键、结果 的 结果。
        完。
        返还 结果。
    完。
    返还 做 工具_调用 用 名称、参数。
完。

工具链步骤 是 一种 事物，含 工具名、参数生成器、错误处理。

工具链_执行 接受 步骤列表：
    全局ATCI统计 的 链式调用数 为 全局ATCI统计 的 链式调用数 加 1。
    可变 上一结果 为 空。
    可变 全部结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 步骤列表：
        步 为 步骤列表 的 第 i 项。
        参数 为 做 步 的 参数生成器 用 上一结果。
        结果 为 做 工具_调用_带重试 用 步 的 工具名、参数。
        做 追加 用 全部结果、结果。
        若 非 结果 的 成功：
            若 步 的 错误处理 不等于 空：
                继续 为 做 步 的 错误处理 用 结果。
                若 非 继续：
                    返还 一本字典：成功 对应 假、步骤 对应 i、错误 对应 结果 的 错误、全部 对应 全部结果。
                完。
            否则：
                返还 一本字典：成功 对应 假、步骤 对应 i、错误 对应 结果 的 错误、全部 对应 全部结果。
            完。
        完。
        上一结果 为 结果 的 结果。
        i 为 i 加 1。
    完。
    返还 一本字典：成功 对应 真、结果 对应 上一结果、全部 对应 全部结果。
完。

工具_并行调用 接受 调用列表：
    全局ATCI统计 的 并行调用数 为 全局ATCI统计 的 并行调用数 加 1。
    总数 为 做 计数 用 调用列表。
    若 总数 等于 0：
        返还 一组：（空）。
    完。
    若 总数 大于 ATCI_最大并行：
        总数 为 ATCI_最大并行。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 总数：
        c 为 调用列表 的 第 i 项。
        做 追加 用 结果、做 工具_调用 用 c 的 名称、c 的 参数。
        i 为 i 加 1。
    完。
    返还 结果。
完。

工具_调用_带权限 接受 名称、参数、调用者权限：
    idx 为 做 工具_查找 用 名称。
    若 idx 小于 0：
        返还 一本字典：成功 对应 假、错误 对应 「工具不存在」。
    完。
    tool 为 工具注册表 的 第 idx 项。
    若 调用者权限 小于 tool 的 权限级别：
        全局ATCI统计 的 权限拒绝数 为 全局ATCI统计 的 权限拒绝数 加 1。
        返还 一本字典：成功 对应 假、错误 对应 「权限不足:需要级别」 加 做 整数转字符串 用 tool 的 权限级别。
    完。
    返还 做 工具_调用 用 名称、参数。
完。

工具_生成Schema 接受 零参：
    可变 json 为 「[」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 工具注册表：
        若 i 大于 0：
            json 为 json 加 「,」。
        完。
        json 为 json 加 「{\"type\":\"function\",\"function\":{\"name\":\"」。
        json 为 json 加 工具注册表 的 第 i 项 的 名称 加 「\",\"description\":\"」。
        json 为 json 加 工具注册表 的 第 i 项 的 描述 加 「\",\"parameters\":」。
        json 为 json 加 工具注册表 的 第 i 项 的 参数Schema 加 「}}」。
        i 为 i 加 1。
    完。
    json 为 json 加 「]」。
    返还 json。
完。

工具_注册内置 接受 零参：
    做 工具_注册高级 用 「file_read」、「读取文件内容」、「{\"type\":\"object\",\"properties\":{\"path\":{\"type\":\"string\"}}}」、智能体文件_读取、5000、0、真、真。
    做 工具_注册高级 用 「file_write」、「写入文件」、「{\"type\":\"object\",\"properties\":{\"path\":{\"type\":\"string\"},\"content\":{\"type\":\"string\"}}}」、智能体文件_写入、5000、1、假、真。
    做 工具_注册高级 用 「file_edit」、「编辑文件」、「{\"type\":\"object\",\"properties\":{\"path\":{\"type\":\"string\"},\"old\":{\"type\":\"string\"},\"new\":{\"type\":\"string\"}}}」、智能体文件_编辑、5000、1、假、真。
    做 工具_注册高级 用 「终端_exec」、「执行终端命令」、「{\"type\":\"object\",\"properties\":{\"command\":{\"type\":\"string\"}}}」、智能体终端_执行、30000、2、假、假。
    做 工具_注册高级 用 「ai_query」、「查询AI」、「{\"type\":\"object\",\"properties\":{\"question\":{\"type\":\"string\"}}}」、智能_问、60000、0、真、真。
    做 工具_注册高级 用 「web_search」、「搜索网络」、「{\"type\":\"object\",\"properties\":{\"query\":{\"type\":\"string\"}}}」、智能体网络_搜索、15000、0、真、真。
    做 工具_注册高级 用 「code_search」、「代码搜索」、「{\"type\":\"object\",\"properties\":{\"query\":{\"type\":\"string\"},\"path\":{\"type\":\"string\"}}}」、智能体代码_搜索、10000、0、真、真。
完。

AI_ATCI_推荐工具 接受 任务描述：
    全局ATCI统计 的 AI选择数 为 全局ATCI统计 的 AI选择数 加 1。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 做 ATCI_关键词匹配 用 任务描述。
    完。
    可变 最佳工具 为 「」。
    可变 最高分 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 工具注册表：
        tool 为 工具注册表 的 第 i 项。
        t 为 做 Tensor_创建 用 4。
        成功率 为 500。
        若 tool 的 调用次数 大于 0：
            成功率 为 tool 的 成功次数 乘 1000 除以 tool 的 调用次数。
        完。
        做 Tensor_写 用 t、0、成功率。
        速度分 为 500。
        若 tool 的 平均耗时 大于 0：
            速度分 为 1000 减 tool 的 平均耗时 除以 tool 的 超时毫秒 乘 1000。
            若 速度分 小于 0：
                速度分 为 0。
            完。
        完。
        做 Tensor_写 用 t、1、速度分。
        做 Tensor_写 用 t、2、i 乘 100。
        做 Tensor_写 用 t、3、做 ATCI_任务相关度 用 任务描述、tool。
        分数 为 做 AI总线_推理 用 bus、「atci_tool_select」、t。
        做 Tensor_释放 用 t。
        若 分数 大于 最高分：
            最高分 为 分数。
            最佳工具 为 tool 的 名称。
        完。
        i 为 i 加 1。
    完。
    返还 最佳工具。
完。

ATCI_关键词匹配 接受 任务：
    若 做 字符串_包含 用 任务、「读取」 或者 做 字符串_包含 用 任务、「read」：
        返还 「file_read」。
    完。
    若 做 字符串_包含 用 任务、「写入」 或者 做 字符串_包含 用 任务、「write」：
        返还 「file_write」。
    完。
    若 做 字符串_包含 用 任务、「编辑」 或者 做 字符串_包含 用 任务、「edit」：
        返还 「file_edit」。
    完。
    若 做 字符串_包含 用 任务、「命令」 或者 做 字符串_包含 用 任务、「exec」：
        返还 「终端_exec」。
    完。
    若 做 字符串_包含 用 任务、「搜索」 或者 做 字符串_包含 用 任务、「search」：
        返还 「web_search」。
    完。
    若 做 字符串_包含 用 任务、「AI」 或者 做 字符串_包含 用 任务、「推理」：
        返还 「ai_query」。
    完。
    返还 「ai_query」。
完。

工具_统计 接受 零参：
    返还 全局ATCI统计。
完。

ATCI_重置统计 接受 零参：
    全局ATCI统计 为 新建 ATCI统计。
完。

ATCI_诊断报告 接受 零参：
    s 为 全局ATCI统计。
    做 内核打印 用 「╔═══════════════════════════════════════╗」。
    做 内核打印 用 「║   ATCI 工具调用子系统诊断报告       ║」。
    做 内核打印 用 「╠═══════════════════════════════════════╣」。
    做 内核打印 用 「║ 注册工具: {}  总调用: {}」、做 计数 用 工具注册表、s 的 总调用数。
    成功率 为 0。
    若 s 的 总调用数 大于 0：
        成功率 为 s 的 总成功数 乘 100 除以 s 的 总调用数。
    完。
    做 内核打印 用 「║ 成功/失败: {}/{}  成功率: {}%」、s 的 总成功数、s 的 总失败数、成功率。
    做 内核打印 用 「║ 重试: {} (成功: {})」、s 的 重试次数、s 的 重试成功数。
    做 内核打印 用 「║ 缓存命中/未中: {}/{}」、s 的 缓存命中数、s 的 缓存未中数。
    做 内核打印 用 「║ 并行: {}  链式: {}  权限拒绝: {}」、s 的 并行调用数、s 的 链式调用数、s 的 权限拒绝数。
    做 内核打印 用 「║ AI工具选择: {}次」、s 的 AI选择数。
    做 内核打印 用 「╠─── 各工具明细 ────────────────────────╣」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 工具注册表：
        t 为 工具注册表 的 第 i 项。
        做 内核打印 用 「║ [{}] 调用:{} 成功:{} 失败:{} 平均:{}ms」、t 的 名称、t 的 调用次数、t 的 成功次数、t 的 失败次数、t 的 平均耗时 除以 1024。
        i 为 i 加 1。
    完。
    做 内核打印 用 「╚═══════════════════════════════════════╝」。
完。

ATCI_清理过期缓存 接受 零参：
    now 为 做 读取毫秒。
    可变 i 为 做 计数 用 工具缓存 减 1。
    只要 i 大于或等于 0：
        若 now 减 工具缓存 的 第 i 项 的 时间戳 大于 ATCI_缓存TTL：
            做 删除 用 工具缓存、i。
        完。
        i 为 i 减 1。
    完。
完。

ATCI_工具健康检查 接受 零参：
    可变 不健康 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 工具注册表：
        t 为 工具注册表 的 第 i 项。
        若 t 的 调用次数 大于 5：
            失败率 为 t 的 失败次数 乘 100 除以 t 的 调用次数。
            若 失败率 大于 50：
                做 内核打印 用 「[ATCI警告] 工具 {} 失败率 {}%」、t 的 名称、失败率。
                不健康 为 不健康 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 不健康。
完。

ATCI_初始化 接受 零参：
    全局ATCI统计 为 新建 ATCI统计。
    工具注册表 为 一组：（空）。
    工具缓存 为 一组：（空）。
    做 工具_注册内置。
    做 内核打印 用 「[ATCI] 工具调用子系统已初始化，注册{}个工具」、做 计数 用 工具注册表。
完。

本章 收束。
