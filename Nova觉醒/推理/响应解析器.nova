《响应解析器》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·时钟中枢》。

AI响应结果 是 一种 事物，含 成功、内容、输入令牌、输出令牌、总令牌、结束原因、模型名、延迟毫秒、错误码、错误信息。

响应_解析 接受 格式版本、原始响应、开始时间：
    结果 为 新建 AI响应结果。
    结果 的 延迟毫秒 为 做 读取毫秒 减 开始时间。
    若 原始响应 等于 空 或者 做 字符串长度 用 原始响应 等于 0：
        结果 的 成功 为 0。
        结果 的 错误码 为 1。
        结果 的 错误信息 为 「空响应」。
        返还 结果。
    完。
    若 做 响应_是错误 用 原始响应：
        结果 的 成功 为 0。
        结果 的 错误码 为 做 响应_提取错误码 用 原始响应。
        结果 的 错误信息 为 做 响应_提取错误信息 用 原始响应。
        返还 结果。
    完。
    若 格式版本 等于 0：
        返还 做 响应_解析_Nova智能 用 原始响应、结果。
    完。
    若 格式版本 等于 1：
        返还 做 响应_解析_Anthropic 用 原始响应、结果。
    完。
    若 格式版本 等于 2：
        返还 做 响应_解析_GLM 用 原始响应、结果。
    完。
    若 格式版本 等于 3：
        返还 做 响应_解析_MiniMax 用 原始响应、结果。
    完。
    若 格式版本 等于 4：
        返还 做 响应_解析_Nova智能 用 原始响应、结果。
    完。
    返还 做 响应_解析_Nova智能 用 原始响应、结果。
完。

响应_解析_Nova智能 接受 json、结果：
    content 为 做 序列_提取字符串 用 json、「content」。
    若 content 不等于 空：
        结果 的 内容 为 content。
        结果 的 成功 为 1。
    否则：
        结果 的 成功 为 0。
        结果 的 错误信息 为 「无法提取content」。
    完。
    结果 的 输入令牌 为 做 序列_提取整数 用 json、「prompt_令牌s」。
    结果 的 输出令牌 为 做 序列_提取整数 用 json、「completion_令牌s」。
    结果 的 总令牌 为 做 序列_提取整数 用 json、「total_令牌s」。
    若 结果 的 总令牌 等于 0：
        结果 的 总令牌 为 结果 的 输入令牌 加 结果 的 输出令牌。
    完。
    结果 的 结束原因 为 做 序列_提取字符串 用 json、「finish_reason」。
    若 结果 的 结束原因 等于 空：
        结果 的 结束原因 为 「unknown」。
    完。
    结果 的 模型名 为 做 序列_提取字符串 用 json、「model」。
    返还 结果。
完。

响应_解析_Anthropic 接受 json、结果：
    text 为 做 序列_提取字符串 用 json、「text」。
    若 text 不等于 空：
        结果 的 内容 为 text。
        结果 的 成功 为 1。
    否则：
        结果 的 成功 为 0。
        结果 的 错误信息 为 「无法提取text」。
    完。
    结果 的 输入令牌 为 做 序列_提取整数 用 json、「input_令牌s」。
    结果 的 输出令牌 为 做 序列_提取整数 用 json、「output_令牌s」。
    结果 的 总令牌 为 结果 的 输入令牌 加 结果 的 输出令牌。
    结果 的 结束原因 为 做 序列_提取字符串 用 json、「stop_reason」。
    结果 的 模型名 为 做 序列_提取字符串 用 json、「model」。
    返还 结果。
完。

响应_解析_GLM 接受 json、结果：
    返还 做 响应_解析_Nova智能 用 json、结果。
完。

响应_解析_MiniMax 接受 json、结果：
    r 为 做 响应_解析_Nova智能 用 json、结果。
    若 r 的 输入令牌 等于 0 并且 r 的 总令牌 大于 0：
        r 的 输入令牌 为 r 的 总令牌 除以 3。
        r 的 输出令牌 为 r 的 总令牌 减 r 的 输入令牌。
    完。
    返还 r。
完。

可变 流式_缓冲 为 「」。

可变 流式_总内容 为 「」。

可变 流式_完成 为 假。

流式_重置 接受 零参：
    流式_缓冲 为 「」。
    流式_总内容 为 「」。
    流式_完成 为 假。
完。

流式_接收块 接受 数据块：
    流式_缓冲 为 流式_缓冲 加 数据块。
    可变 处理位置 为 0。
    只要 真：
        分隔位 为 做 字符串查找 用 做 字符串子串 用 流式_缓冲、处理位置、做 字符串长度 用 流式_缓冲、「\n\n」。
        若 分隔位 小于 0：
            跳出。
        完。
        事件 为 做 字符串子串 用 流式_缓冲、处理位置、处理位置 加 分隔位。
        处理位置 为 处理位置 加 分隔位 加 2。
        data位 为 做 字符串查找 用 事件、「data: 」。
        若 data位 大于或等于 0：
            data内容 为 做 字符串子串 用 事件、data位 加 6、做 字符串长度 用 事件。
            若 data内容 等于 「[DONE]」：
                流式_完成 为 真。
            否则：
                delta 为 做 序列_提取字符串 用 data内容、「content」。
                若 delta 不等于 空：
                    流式_总内容 为 流式_总内容 加 delta。
                完。
            完。
        完。
    完。
    若 处理位置 大于 0：
        流式_缓冲 为 做 字符串子串 用 流式_缓冲、处理位置、做 字符串长度 用 流式_缓冲。
    完。
完。

流式_获取增量 接受 零参：
    内容 为 流式_总内容。
    返还 内容。
完。

流式_是否完成 接受 零参：
    返还 流式_完成。
完。

响应_是错误 接受 json：
    返还 做 字符串查找 用 json、「\"error\"」 大于或等于 0。
完。

响应_提取错误码 接受 json：
    code 为 做 序列_提取整数 用 json、「status_code」。
    若 code 等于 0：
        code 为 做 序列_提取整数 用 json、「code」。
    完。
    若 code 等于 0：
        code 为 500。
    完。
    返还 code。
完。

响应_提取错误信息 接受 json：
    msg 为 做 序列_提取字符串 用 json、「message」。
    若 msg 等于 空：
        msg 为 做 序列_提取字符串 用 json、「error」。
    完。
    若 msg 等于 空：
        msg 为 「未知错误」。
    完。
    返还 msg。
完。

序列_提取字符串 接受 json、键：
    搜索 为 「\"」 加 键 加 「\"」。
    位置 为 做 字符串查找 用 json、搜索。
    若 位置 小于 0：
        返还 空。
    完。
    可变 i 为 位置 加 做 字符串长度 用 搜索。
    只要 i 小于 做 字符串长度 用 json：
        ch 为 做 字符串字符 用 json、i。
        若 ch 等于 58 或者 ch 等于 32 或者 ch 等于 9 或者 ch 等于 10：
            i 为 i 加 1。
        否则：
            跳出。
        完。
    完。
    若 i 小于 做 字符串长度 用 json 并且 做 字符串字符 用 json、i 等于 34：
        i 为 i 加 1。
        可变 结果 为 「」。
        只要 i 小于 做 字符串长度 用 json：
            ch 为 做 字符串字符 用 json、i。
            若 ch 等于 34：
                返还 结果。
            完。
            若 ch 等于 92 并且 i 加 1 小于 做 字符串长度 用 json：
                i 为 i 加 1。
                ech 为 做 字符串字符 用 json、i。
                若 ech 等于 110：
                    结果 为 结果 加 「\n」。
                否则若 ech 等于 116：
                    结果 为 结果 加 「\t」。
                否则若 ech 等于 34：
                    结果 为 结果 加 「\"」。
                否则若 ech 等于 92：
                    结果 为 结果 加 「\\」。
                否则：
                    结果 为 结果 加 做 字符串从字符 用 ech。
                完。
            否则：
                结果 为 结果 加 做 字符串从字符 用 ch。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 空。
完。

序列_提取整数 接受 json、键：
    搜索 为 「\"」 加 键 加 「\"」。
    位置 为 做 字符串查找 用 json、搜索。
    若 位置 小于 0：
        返还 0。
    完。
    可变 i 为 位置 加 做 字符串长度 用 搜索。
    只要 i 小于 做 字符串长度 用 json：
        ch 为 做 字符串字符 用 json、i。
        若 ch 等于 58 或者 ch 等于 32：
            i 为 i 加 1。
        否则：
            跳出。
        完。
    完。
    可变 数值 为 0。
    只要 i 小于 做 字符串长度 用 json：
        ch 为 做 字符串字符 用 json、i。
        若 ch 大于或等于 48 并且 ch 小于或等于 57：
            数值 为 数值 乘 10 加 (ch 减 48)。
        否则：
            跳出。
        完。
        i 为 i 加 1。
    完。
    返还 数值。
完。

本章 收束。
