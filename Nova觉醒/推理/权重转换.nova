《权重转换》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《..·中枢·时钟中枢》。

量化_三态 为 3。

量化_七态 为 7。

量化_十五态 为 15。

剪枝_阈值 为 100。

校准_迭代 为 16。

量化配置 是 一种 事物，含 模式、对称、逐通道、剪枝启用、剪枝率、校准数据。

量化结果 是 一种 事物，含 量化权重、缩放因子、零点、误差、稀疏度、原始大小、压缩大小、位宽估计、压缩质量。

转换统计 是 一种 事物，含 总层数、已转换层数、总权重数、总误差、最大层误差、总耗时、剪枝权重数、压缩比、总质量、最近质量、质量样本数。

可变 全局转换统计 为 0。

权重转换_初始化 接受 零参：
    全局转换统计 为 做 堆_分配 用 转换统计。
    全局转换统计 的 总层数 为 0。
    全局转换统计 的 已转换层数 为 0。
    全局转换统计 的 总权重数 为 0。
    全局转换统计 的 总误差 为 0。
    全局转换统计 的 最大层误差 为 0。
    全局转换统计 的 总耗时 为 0。
    全局转换统计 的 剪枝权重数 为 0。
    全局转换统计 的 压缩比 为 0。
    全局转换统计 的 总质量 为 0。
    全局转换统计 的 最近质量 为 0。
    全局转换统计 的 质量样本数 为 0。
完。

权重_估计位宽 接受 模式：
    若 模式 等于 量化_三态：
        返还 2。
    完。
    若 模式 等于 量化_十五态：
        返还 4。
    完。
    返还 3。
完。

权重_压缩质量 接受 原始、量化后、缩放：
    反量化 为 做 权重_反量化 用 量化后、缩放。
    可变 i 为 0。
    可变 原能量 为 0。
    可变 压能量 为 0。
    可变 差和 为 0。
    可变 基准和 为 0。
    可变 非零 为 0。
    只要 i 小于 做 计数 用 原始 并且 i 小于 做 计数 用 反量化：
        原值 为 原始 的 第 i 项。
        压值 为 反量化 的 第 i 项。
        原绝对 为 原值。
        压绝对 为 压值。
        若 原绝对 小于 0：
            原绝对 为 0 减 原绝对。
        完。
        若 压绝对 小于 0：
            压绝对 为 0 减 压绝对。
        完。
        若 压值 不等于 0：
            非零 为 非零 加 1。
        完。
        原能量 为 原能量 加 原绝对。
        压能量 为 压能量 加 压绝对。
        差 为 原值 减 压值。
        若 差 小于 0：
            差 为 0 减 差。
        完。
        差和 为 差和 加 差。
        基准和 为 基准和 加 原绝对 加 压绝对 加 1。
        i 为 i 加 1。
    完。
    若 i 等于 0：
        返还 0。
    完。
    比例分 为 0。
    若 原能量 大于 0 并且 压能量 大于 0：
        小值 为 原能量。
        大值 为 压能量。
        若 压能量 小于 原能量：
            小值 为 压能量。
            大值 为 原能量。
        完。
        比例分 为 小值 乘 1000 除以 大值。
    否则若 原能量 等于 0 并且 压能量 等于 0：
        比例分 为 1000。
    完。
    误差分 为 1000 减 差和 乘 1000 除以 基准和。
    若 误差分 小于 0：
        误差分 为 0。
    完。
    覆盖分 为 非零 乘 1000 除以 做 计数 用 反量化。
    若 覆盖分 大于 1000：
        覆盖分 为 1000。
    完。
    质量 为 比例分 乘 0.35 加 误差分 乘 0.45 加 覆盖分 乘 0.20。
    若 质量 小于 0：
        质量 为 0。
    完。
    若 质量 大于 1000：
        质量 为 1000。
    完。
    返还 质量。
完。

权重_量化_三态 接受 浮点权重、缩放因子：
    可变 i 为 0。
    结果 为 一组：（空）。
    只要 i 小于 做 计数 用 浮点权重：
        v 为 浮点权重 的 第 i 项 乘 缩放因子 除以 1000。
        若 v 大于 500：
            做 追加 用 结果、1。
        否则若 v 小于 0 减 500：
            做 追加 用 结果、0 减 1。
        否则：
            做 追加 用 结果、0。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

权重_量化_十五态 接受 浮点权重、缩放因子：
    可变 i 为 0。
    结果 为 一组：（空）。
    只要 i 小于 做 计数 用 浮点权重：
        v 为 浮点权重 的 第 i 项 乘 缩放因子 除以 1000。
        量化值 为 (v 加 7500) 乘 15 除以 15000。
        若 量化值 小于 0：
            量化值 为 0。
        完。
        若 量化值 大于 15：
            量化值 为 15。
        完。
        做 追加 用 结果、量化值 减 7。
        i 为 i 加 1。
    完。
    返还 结果。
完。

权重_反量化 接受 量化权重、缩放因子：
    结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 量化权重：
        做 追加 用 结果、量化权重 的 第 i 项 乘 1000 除以 缩放因子。
        i 为 i 加 1。
    完。
    返还 结果。
完。

权重_量化误差 接受 原始、量化后、缩放：
    反量化 为 做 权重_反量化 用 量化后、缩放。
    可变 误差和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 原始 并且 i 小于 做 计数 用 反量化：
        差 为 原始 的 第 i 项 减 反量化 的 第 i 项。
        误差和 为 误差和 加 差 乘 差。
        i 为 i 加 1。
    完。
    若 i 等于 0：
        返还 0。
    完。
    返还 误差和 除以 i。
完。

权重_最大绝对误差 接受 原始、量化后、缩放：
    反量化 为 做 权重_反量化 用 量化后、缩放。
    可变 最大误差 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 原始 并且 i 小于 做 计数 用 反量化：
        差 为 原始 的 第 i 项 减 反量化 的 第 i 项。
        若 差 小于 0：
            差 为 0 减 差。
        完。
        若 差 大于 最大误差：
            最大误差 为 差。
        完。
        i 为 i 加 1。
    完。
    返还 最大误差。
完。

权重_自动缩放 接受 浮点权重：
    可变 最大 为 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 浮点权重：
        绝对 为 浮点权重 的 第 i 项。
        若 绝对 小于 0：
            绝对 为 0 减 绝对。
        完。
        若 绝对 大于 最大：
            最大 为 绝对。
        完。
        i 为 i 加 1。
    完。
    若 最大 等于 0：
        返还 1000。
    完。
    返还 3000 乘 1000 除以 最大。
完。

权重_百分位缩放 接受 浮点权重、百分位：
    排序 为 做 权重_排序绝对值 用 浮点权重。
    索引 为 做 计数 用 排序 乘 百分位 除以 100。
    若 索引 大于或等于 做 计数 用 排序：
        索引 为 做 计数 用 排序 减 1。
    完。
    若 索引 小于 0：
        索引 为 0。
    完。
    截断值 为 排序 的 第 索引 项。
    若 截断值 等于 0：
        返还 1000。
    完。
    返还 3000 乘 1000 除以 截断值。
完。

权重_排序绝对值 接受 权重：
    绝对值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 权重：
        v 为 权重 的 第 i 项。
        若 v 小于 0：
            v 为 0 减 v。
        完。
        做 追加 用 绝对值、v。
        i 为 i 加 1。
    完。
    可变 j 为 0。
    只要 j 小于 做 计数 用 绝对值 减 1：
        可变 k 为 j 加 1。
        只要 k 小于 做 计数 用 绝对值：
            若 绝对值 的 第 k 项 小于 绝对值 的 第 j 项：
                临时 为 绝对值 的 第 j 项。
                绝对值 的 第 j 项 为 绝对值 的 第 k 项。
                绝对值 的 第 k 项 为 临时。
            完。
            k 为 k 加 1。
        完。
        j 为 j 加 1。
    完。
    返还 绝对值。
完。

权重_剪枝 接受 浮点权重、剪枝率：
    排序 为 做 权重_排序绝对值 用 浮点权重。
    阈值索引 为 做 计数 用 排序 乘 剪枝率 除以 100。
    若 阈值索引 大于或等于 做 计数 用 排序：
        阈值索引 为 做 计数 用 排序 减 1。
    完。
    阈值 为 排序 的 第 阈值索引 项。
    结果 为 一组：（空）。
    可变 剪枝数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 浮点权重：
        绝对 为 浮点权重 的 第 i 项。
        若 绝对 小于 0：
            绝对 为 0 减 绝对。
        完。
        若 绝对 小于或等于 阈值：
            做 追加 用 结果、0。
            剪枝数 为 剪枝数 加 1。
        否则：
            做 追加 用 结果、浮点权重 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 全局转换统计 不等于 0：
        全局转换统计 的 剪枝权重数 为 全局转换统计 的 剪枝权重数 加 剪枝数。
    完。
    返还 结果。
完。

权重_计算稀疏度 接受 量化权重：
    可变 零数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 量化权重：
        若 量化权重 的 第 i 项 等于 0：
            零数 为 零数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 量化权重 等于 0：
        返还 0。
    完。
    返还 零数 乘 100 除以 做 计数 用 量化权重。
完。

权重_打包_七态 接受 量化权重：
    结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 2 小于 做 计数 用 量化权重：
        a 为 量化权重 的 第 i 项 加 3。
        b 为 量化权重 的 第 i 加 1 项 加 3。
        c 为 量化权重 的 第 i 加 2 项 加 3。
        做 追加 用 结果、a 乘 49 加 b 乘 7 加 c。
        i 为 i 加 3。
    完。
    只要 i 小于 做 计数 用 量化权重：
        做 追加 用 结果、量化权重 的 第 i 项 加 3。
        i 为 i 加 1。
    完。
    返还 结果。
完。

权重_解包_七态 接受 打包数据、权重数：
    结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 打包数据 并且 做 计数 用 结果 小于 权重数：
        v 为 打包数据 的 第 i 项。
        若 做 计数 用 结果 加 3 小于或等于 权重数：
            a 为 v 除以 49。
            b 为 v 取余于 49 除以 7。
            c 为 v 取余于 7。
            做 追加 用 结果、a 减 3。
            做 追加 用 结果、b 减 3。
            做 追加 用 结果、c 减 3。
        否则：
            做 追加 用 结果、v 减 3。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

权重_云端到TNF 接受 浮点权重：
    缩放 为 做 权重_自动缩放 用 浮点权重。
    返还 一组：做 权重_量化 用 浮点权重、缩放、缩放。
完。

权重_云端到TNF_高级 接受 浮点权重、配置：
    开始 为 做 读取毫秒。
    可变 处理后 为 浮点权重。
    若 配置 的 剪枝启用：
        处理后 为 做 权重_剪枝 用 处理后、配置 的 剪枝率。
    完。
    可变 缩放 为 0。
    若 配置 的 校准数据 不等于 0 并且 做 计数 用 配置 的 校准数据 大于 0：
        缩放 为 做 权重_校准缩放 用 处理后、配置 的 校准数据。
    否则：
        缩放 为 做 权重_百分位缩放 用 处理后、99。
    完。
    可变 量化后 为 一组：（空）。
    做 匹配 用 配置 的 模式。
    误差 为 做 权重_量化误差 用 浮点权重、量化后、缩放。
    稀疏度 为 做 权重_计算稀疏度 用 量化后。
    位宽估计 为 做 权重_估计位宽 用 配置 的 模式。
    压缩大小 为 0。
    若 做 计数 用 量化后 大于 0：
        压缩大小 为 (做 计数 用 量化后 乘 位宽估计 加 7) 除以 8。
    完。
    若 压缩大小 等于 0 并且 做 计数 用 量化后 大于 0：
        压缩大小 为 1。
    完。
    压缩质量 为 做 权重_压缩质量 用 浮点权重、量化后、缩放。
    结果 为 做 堆_分配 用 量化结果。
    结果 的 量化权重 为 量化后。
    结果 的 缩放因子 为 缩放。
    结果 的 零点 为 0。
    结果 的 误差 为 误差。
    结果 的 稀疏度 为 稀疏度。
    结果 的 原始大小 为 做 计数 用 浮点权重 乘 4。
    结果 的 压缩大小 为 压缩大小。
    结果 的 位宽估计 为 位宽估计。
    结果 的 压缩质量 为 压缩质量。
    若 全局转换统计 不等于 0：
        全局转换统计 的 已转换层数 为 全局转换统计 的 已转换层数 加 1。
        全局转换统计 的 总权重数 为 全局转换统计 的 总权重数 加 做 计数 用 浮点权重。
        全局转换统计 的 总误差 为 全局转换统计 的 总误差 加 误差。
        若 误差 大于 全局转换统计 的 最大层误差：
            全局转换统计 的 最大层误差 为 误差。
        完。
        全局转换统计 的 总耗时 为 全局转换统计 的 总耗时 加 (做 读取毫秒 减 开始)。
        全局转换统计 的 总质量 为 全局转换统计 的 总质量 加 压缩质量。
        全局转换统计 的 最近质量 为 压缩质量。
        全局转换统计 的 质量样本数 为 全局转换统计 的 质量样本数 加 1。
    完。
    返还 结果。
完。

权重_校准缩放 接受 权重、校准数据：
    可变 最佳缩放 为 做 权重_自动缩放 用 权重。
    可变 最小误差 为 999999999。
    基础缩放 为 最佳缩放。
    可变 试 为 0。
    只要 试 小于 校准_迭代：
        候选缩放 为 基础缩放 乘 (800 加 试 乘 30) 除以 1000。
        量化后 为 做 权重_量化 用 权重、候选缩放。
        误差 为 做 权重_量化误差 用 权重、量化后、候选缩放。
        若 校准数据 不等于 0 并且 做 计数 用 校准数据 大于 0：
            校准量化 为 做 权重_量化 用 校准数据、候选缩放。
            校准误差 为 做 权重_量化误差 用 校准数据、校准量化、候选缩放。
            误差 为 (误差 乘 3 加 校准误差) 除以 4。
        完。
        若 误差 小于 最小误差：
            最小误差 为 误差。
            最佳缩放 为 候选缩放。
        完。
        试 为 试 加 1。
    完。
    返还 最佳缩放。
完。

QAT_INT8_最大 为 127。

QAT_INT4_最大 为 7。

QAT_混合精度_临界层类型 为 0。

QAT_STE前向量化 接受 权重、缩放因子、位宽：
    最大整数 为 若 位宽 等于 8 则 QAT_INT8_最大 否则 QAT_INT4_最大。
    总 为 权重 的 行数 乘 权重 的 列数。
    模拟量化 为 做 Tensor_创建 用 权重 的 行数、权重 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        w 为 做 Tensor_读1D 用 权重、i。
        w_整数 为 w 乘 最大整数 除以 缩放因子。
        若 w_整数 大于 最大整数：
            w_整数 为 最大整数。
        完。
        若 w_整数 小于 0 减 最大整数：
            w_整数 为 0 减 最大整数。
        完。
        w_模拟 为 w_整数 乘 缩放因子 除以 最大整数。
        做 Tensor_写1D 用 模拟量化、i、w_模拟。
        i 为 i 加 1。
    完。
    返还 模拟量化。
完。

QAT_混合精度位宽 接受 层类型：
    若 层类型 等于 0 或者 层类型 等于 1 或者 层类型 等于 4：
        返还 8。
    完。
    返还 4。
完。

权重_批量转换 接受 层列表、配置：
    若 全局转换统计 不等于 0：
        全局转换统计 的 总层数 为 做 计数 用 层列表。
    完。
    结果列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 层列表：
        结果 为 做 权重_云端到TNF_高级 用 层列表 的 第 i 项、配置。
        做 追加 用 结果列表、结果。
        i 为 i 加 1。
    完。
    若 全局转换统计 不等于 0 并且 全局转换统计 的 总权重数 大于 0：
        原始总 为 全局转换统计 的 总权重数 乘 4。
        可变 压缩总 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 结果列表：
            压缩总 为 压缩总 加 结果列表 的 第 j 项 的 压缩大小。
            j 为 j 加 1。
        完。
        若 压缩总 大于 0：
            全局转换统计 的 压缩比 为 原始总 乘 100 除以 压缩总。
        完。
    完。
    返还 结果列表。
完。

本章 收束。
