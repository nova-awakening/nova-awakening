《数字人渲染》

引用《..·中枢·类型定义》。

引用《..·感知·图绘渲染上下》。

引用《.·全息虚拟人》。

引用《..·中枢·赫布学习更新》。

向量3 是 一种 事物，含 x、y、z。

颜色4 是 一种 事物，含 r、g、b、a。

三角面 是 一种 事物，含 v0、v1、v2、法线、uv0、uv1、uv2。

PI_1000 为 3142。

向量3_创建 接受 x、y、z：
    返还 新建 向量3：x 为 x、y 为 y、z 为 z。
完。

向量3_加 接受 a、b：
    返还 新建 向量3：x 为 a 的 x 加 b 的 x、y 为 a 的 y 加 b 的 y、z 为 a 的 z 加 b 的 z。
完。

向量3_缩放 接受 v、s：
    返还 新建 向量3：x 为 v 的 x 乘 s 除以 1000、y 为 v 的 y 乘 s 除以 1000、z 为 v 的 z 乘 s 除以 1000。
完。

简易sin 接受 角度：
    x 为 角度 乘 PI_1000 除以 180000。
    返还 x 减 x 乘 x 乘 x 除以 6000 加 x 乘 x 乘 x 乘 x 乘 x 除以 120000000。
完。

简易cos 接受 角度：
    返还 做 简易sin 用 角度 加 90000。
完。

可变 人体顶点 为 一组：（空）。

可变 人体面 为 一组：（空）。

可变 人体颜色 为 一组：（空）。

可变 人体生成计数 为 0。

生成圆柱 接受 中心X、中心Y、中心Z、半径、高度、分段数、颜色：
    可变 底圈 为 一组：（空）。
    可变 顶圈 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 分段数：
        角度 为 i 乘 360000 除以 分段数。
        cx 为 中心X 加 半径 乘 做 简易cos 用 角度 除以 1000。
        cz 为 中心Z 加 半径 乘 做 简易sin 用 角度 除以 1000。
        做 追加 用 底圈、做 向量3_创建 用 cx、中心Y、cz。
        做 追加 用 顶圈、做 向量3_创建 用 cx、中心Y 加 高度、cz。
        做 追加 用 人体颜色、颜色。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 分段数：
        下一个 为 (i 加 1) 取余于 分段数。
        做 追加 用 人体顶点、底圈 的 第 i 项。
        做 追加 用 人体顶点、顶圈 的 第 i 项。
        做 追加 用 人体顶点、底圈 的 第 下一个 项。
        做 追加 用 人体顶点、顶圈 的 第 下一个 项。
        i 为 i 加 1。
    完。
    人体生成计数 为 人体生成计数 加 分段数 乘 4。
完。

生成球体 接受 中心X、中心Y、中心Z、半径、环数、段数、颜色：
    可变 j 为 0。
    只要 j 小于 环数：
        纬度1 为 j 乘 180000 除以 环数 减 90000。
        纬度2 为 (j 加 1) 乘 180000 除以 环数 减 90000。
        可变 i 为 0。
        只要 i 小于 段数：
            经度 为 i 乘 360000 除以 段数。
            x 为 中心X 加 半径 乘 做 简易cos 用 纬度1 乘 做 简易cos 用 经度 除以 1000000。
            y 为 中心Y 加 半径 乘 做 简易sin 用 纬度1 除以 1000。
            z 为 中心Z 加 半径 乘 做 简易cos 用 纬度1 乘 做 简易sin 用 经度 除以 1000000。
            做 追加 用 人体顶点、做 向量3_创建 用 x、y、z。
            做 追加 用 人体颜色、颜色。
            i 为 i 加 1。
        完。
        j 为 j 加 1。
    完。
    人体生成计数 为 人体生成计数 加 环数 乘 段数。
完。

肤色 为 新建 颜色4：r 为 245、g 为 228、b 为 218、a 为 255。

发色 为 新建 颜色4：r 为 20、g 为 10、b 为 35、a 为 255。

衣色 为 新建 颜色4：r 为 220、g 为 215、b 为 240、a 为 200。

金饰色 为 新建 颜色4：r 为 255、g 为 215、b 为 0、a 为 255。

紫纱色 为 新建 颜色4：r 为 168、g 为 85、b 为 247、a 为 160。

眼色 为 新建 颜色4：r 为 168、g 为 0、b 为 255、a 为 255。

纹理数据 是 一种 事物，含 宽、高、像素缓冲、路径。

可变 纹理缓存 为 一组：（空）。

可变 纹理缓存数 为 0。

数字人渲染_加载纹理 接受 路径：
    可变 i 为 0。
    只要 i 小于 纹理缓存数：
        若 纹理缓存 的 第 i 项 的 路径 等于 路径：
            返还 纹理缓存 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    纹理 为 新建 纹理数据：宽 为 0、高 为 0、像素缓冲 为 0、路径 为 路径。
    结果 为 做 系统调用 用 「加载图片」、路径。
    若 结果 不等于 0：
        纹理 的 宽 为 结果 的 第 0 项。
        纹理 的 高 为 结果 的 第 1 项。
        纹理 的 像素缓冲 为 结果 的 第 2 项。
    完。
    做 追加 用 纹理缓存、纹理。
    纹理缓存数 为 纹理缓存数 加 1。
    返还 纹理。
完。

数字人渲染_2D精灵绘制 接受 帧缓冲、宽、高、纹理、目标X、目标Y、目标宽、目标高：
    若 纹理 的 像素缓冲 等于 0：
        返还。
    完。
    可变 y 为 0。
    只要 y 小于 目标高：
        源Y 为 y 乘 纹理 的 高 除以 目标高。
        屏幕Y 为 目标Y 加 y。
        若 屏幕Y 大于或等于 0 并且 屏幕Y 小于 高：
            可变 x 为 0。
            只要 x 小于 目标宽：
                源X 为 x 乘 纹理 的 宽 除以 目标宽。
                屏幕X 为 目标X 加 x。
                若 屏幕X 大于或等于 0 并且 屏幕X 小于 宽：
                    源偏移 为 (源Y 乘 纹理 的 宽 加 源X) 乘 4。
                    像素 为 做 内存_读32 用 纹理 的 像素缓冲 加 源偏移。
                    alpha 为 像素 右移 24 位与 0xFF。
                    若 alpha 大于 128：
                        目标偏移 为 (屏幕Y 乘 宽 加 屏幕X) 乘 4。
                        做 内存_写32 用 帧缓冲 加 目标偏移、像素 位与 0x00FFFFFF。
                    完。
                完。
                x 为 x 加 1。
            完。
        完。
        y 为 y 加 1。
    完。
完。

数字人渲染_绘制表情精灵 接受 帧缓冲、宽、高、表情ID、目标X、目标Y、目标宽、目标高：
    可变 路径 为 「../资源/数字人/表情_平静.png」。
    若 表情ID 等于 1：
        路径 为 「../资源/数字人/表情_微笑.png」。
    完。
    若 表情ID 等于 2：
        路径 为 「../资源/数字人/表情_思考.png」。
    完。
    若 表情ID 等于 3：
        路径 为 「../资源/数字人/表情_惊讶.png」。
    完。
    若 表情ID 等于 4：
        路径 为 「../资源/数字人/表情_专注.png」。
    完。
    若 表情ID 等于 5：
        路径 为 「../资源/数字人/表情_说话.png」。
    完。
    纹理 为 做 数字人渲染_加载纹理 用 路径。
    做 数字人渲染_2D精灵绘制 用 帧缓冲、宽、高、纹理、目标X、目标Y、目标宽、目标高。
完。

数字人渲染_绘制姿势精灵 接受 帧缓冲、宽、高、姿势、目标X、目标Y、目标宽、目标高：
    可变 路径 为 「../资源/数字人/站姿_000度_正面.png」。
    若 姿势 等于 「行走」：
        路径 为 「../资源/数字人/姿势_行走.png」。
    完。
    若 姿势 等于 「打坐」：
        路径 为 「../资源/数字人/姿势_打坐.png」。
    完。
    若 姿势 等于 「挥手」：
        路径 为 「../资源/数字人/姿势_挥手.png」。
    完。
    若 姿势 等于 「战斗」：
        路径 为 「../资源/数字人/姿势_战斗.png」。
    完。
    若 姿势 等于 「漂浮」：
        路径 为 「../资源/数字人/姿势_漂浮.png」。
    完。
    若 姿势 等于 「说话」：
        路径 为 「../资源/数字人/站姿_000度_正面.png」。
    完。
    纹理 为 做 数字人渲染_加载纹理 用 路径。
    做 数字人渲染_2D精灵绘制 用 帧缓冲、宽、高、纹理、目标X、目标Y、目标宽、目标高。
完。

数字人渲染_绘制朝向精灵 接受 帧缓冲、宽、高、朝向、目标X、目标Y、目标宽、目标高：
    可变 路径 为 「../资源/数字人/站姿_000度_正面.png」。
    若 朝向 大于或等于 23 并且 朝向 小于 68：
        路径 为 「../资源/数字人/站姿_045度_左前.png」。
    完。
    若 朝向 大于或等于 68 并且 朝向 小于 113：
        路径 为 「../资源/数字人/站姿_090度_左侧.png」。
    完。
    若 朝向 大于或等于 113 并且 朝向 小于 158：
        路径 为 「../资源/数字人/站姿_135度_左后.png」。
    完。
    若 朝向 大于或等于 158 并且 朝向 小于 203：
        路径 为 「../资源/数字人/站姿_180度_背面.png」。
    完。
    若 朝向 大于或等于 203 并且 朝向 小于 248：
        路径 为 「../资源/数字人/站姿_225度_右后.png」。
    完。
    若 朝向 大于或等于 248 并且 朝向 小于 293：
        路径 为 「../资源/数字人/站姿_270度_右侧.png」。
    完。
    若 朝向 大于或等于 293 并且 朝向 小于 338：
        路径 为 「../资源/数字人/站姿_315度_右前.png」。
    完。
    纹理 为 做 数字人渲染_加载纹理 用 路径。
    做 数字人渲染_2D精灵绘制 用 帧缓冲、宽、高、纹理、目标X、目标Y、目标宽、目标高。
完。

数字人渲染_构建人体 接受 零参：
    人体顶点 为 一组：（空）。
    人体颜色 为 一组：（空）。
    人体生成计数 为 0。
    做 生成球体 用 0、1600、0、120、8、12、肤色。
    做 生成球体 用 0、1700、0、125、4、12、发色。
    做 生成圆柱 用 0、900、0、150、700、12、衣色。
    做 生成圆柱 用 负 200、1200、0、40、350、8、肤色。
    做 生成圆柱 用 负 200、850、0、35、350、8、肤色。
    做 生成圆柱 用 200、1200、0、40、350、8、肤色。
    做 生成圆柱 用 200、850、0、35、350、8、肤色。
    做 生成圆柱 用 负 80、450、0、50、450、8、衣色。
    做 生成圆柱 用 负 80、0、0、45、450、8、肤色。
    做 生成圆柱 用 80、450、0、50、450、8、衣色。
    做 生成圆柱 用 80、0、0、45、450、8、肤色。
    做 打印 用 「[数字人渲染] 人体构建完成: 」 加 人体生成计数 加 「 顶点」。
完。

光源方向 为 新建 向量3：x 为 负 500、y 为 1000、z 为 负 300。

环境光 为 40。

数字人渲染_着色 接受 顶点索引：
    若 顶点索引 大于或等于 做 计数 用 人体颜色：
        返还 0。
    完。
    基色 为 人体颜色 的 第 顶点索引 项。
    漫反射 为 180。
    最终r 为 基色 的 r 乘 (环境光 加 漫反射) 除以 255。
    最终g 为 基色 的 g 乘 (环境光 加 漫反射) 除以 255。
    最终b 为 基色 的 b 乘 (环境光 加 漫反射) 除以 255。
    若 最终r 大于 255：
        最终r 为 255。
    完。
    若 最终g 大于 255：
        最终g 为 255。
    完。
    若 最终b 大于 255：
        最终b 为 255。
    完。
    返还 最终r 左移 16 位或 最终g 左移 8 位或 最终b。
完。

可变 渲染_帧数 为 0。

数字人渲染_绘制帧 接受 帧缓冲、宽、高：
    渲染_帧数 为 渲染_帧数 加 1。
    若 做 计数 用 人体顶点 等于 0：
        做 数字人渲染_构建人体。
    完。
    焦距 为 500。
    可变 i 为 0。
    只要 i 小于 做 计数 用 人体顶点：
        v 为 人体顶点 的 第 i 项。
        z 为 v 的 z 加 2000。
        若 z 小于或等于 0：
            z 为 1。
        完。
        屏幕x 为 宽 除以 2 加 v 的 x 乘 焦距 除以 z。
        屏幕y 为 高 除以 2 减 v 的 y 乘 焦距 除以 z。
        若 屏幕x 大于或等于 0 并且 屏幕x 小于 宽 并且 屏幕y 大于或等于 0 并且 屏幕y 小于 高：
            颜色 为 做 数字人渲染_着色 用 i。
            偏移 为 (屏幕y 乘 宽 加 屏幕x) 乘 4。
            做 内存_写32 用 帧缓冲 加 偏移、颜色。
        完。
        i 为 i 加 1。
    完。
完。

数字人渲染_应用姿势 接受 姿势：
    若 姿势 等于 「行走」：
        摆角 为 做 简易sin 用 渲染_帧数 乘 5000 乘 30 除以 1000。
    完。
    若 姿势 等于 「挥手」：
    完。
    若 姿势 等于 「思考」：
    完。
完。

PBR材质 是 一种 事物，含 基础色、金属度、粗糙度、自发光、透明度、次表面散射、法线扰动。

材质_皮肤 为 新建 PBR材质：基础色 为 新建 颜色4：r 为 245、g 为 228、b 为 218、a 为 255、金属度 为 0、粗糙度 为 350、自发光 为 0、透明度 为 0、次表面散射 为 700、法线扰动 为 150。

材质_头发 为 新建 PBR材质：基础色 为 新建 颜色4：r 为 20、g 为 10、b 为 35、a 为 255、金属度 为 80、粗糙度 为 280、自发光 为 30、透明度 为 0、次表面散射 为 0、法线扰动 为 450。

材质_纱衣 为 新建 PBR材质：基础色 为 新建 颜色4：r 为 220、g 为 215、b 为 240、a 为 200、金属度 为 0、粗糙度 为 80、自发光 为 80、透明度 为 750、次表面散射 为 0、法线扰动 为 80。

材质_金饰 为 新建 PBR材质：基础色 为 新建 颜色4：r 为 255、g 为 215、b 为 0、a 为 255、金属度 为 950、粗糙度 为 180、自发光 为 250、透明度 为 0、次表面散射 为 0、法线扰动 为 250。

材质_眼睛 为 新建 PBR材质：基础色 为 新建 颜色4：r 为 168、g 为 0、b 为 255、a 为 255、金属度 为 400、粗糙度 为 30、自发光 为 950、透明度 为 0、次表面散射 为 0、法线扰动 为 0。

材质_紫纱 为 新建 PBR材质：基础色 为 新建 颜色4：r 为 168、g 为 85、b 为 247、a 为 160、金属度 为 0、粗糙度 为 60、自发光 为 120、透明度 为 800、次表面散射 为 0、法线扰动 为 60。

PBR_着色 接受 法线、视线、光线、材质：
    NdotL 为 法线 的 x 乘 光线 的 x 加 法线 的 y 乘 光线 的 y 加 法线 的 z 乘 光线 的 z。
    若 NdotL 小于 0：
        NdotL 为 0。
    完。
    漫反射 为 (1000 减 材质 的 金属度) 乘 NdotL 除以 1000。
    半角x 为 (视线 的 x 加 光线 的 x) 除以 2。
    半角y 为 (视线 的 y 加 光线 的 y) 除以 2。
    NdotH 为 法线 的 x 乘 半角x 加 法线 的 y 乘 半角y。
    若 NdotH 小于 0：
        NdotH 为 0。
    完。
    镜面 为 材质 的 金属度 乘 NdotH 乘 NdotH 除以 1000000。
    sss 为 材质 的 次表面散射 乘 (500 加 NdotL 除以 2) 除以 1000。
    发光 为 材质 的 自发光。
    强度 为 环境光 加 漫反射 乘 150 除以 1000 加 镜面 乘 200 加 sss 乘 100 除以 1000 加 发光 乘 255 除以 1000。
    若 强度 大于 255：
        强度 为 255。
    完。
    返还 强度。
完。

辉光_后处理 接受 帧缓冲、宽、高：
    可变 x 为 1。
    只要 x 小于 宽 减 1：
        可变 y 为 1。
        只要 y 小于 高 减 1：
            偏移 为 (y 乘 宽 加 x) 乘 4。
            像素 为 做 内存_读32 用 帧缓冲 加 偏移。
            亮度 为 像素 右移 16 位与 0xFF。
            若 亮度 大于 200：
                扩散 为 亮度 除以 4。
                做 内存_写32 用 帧缓冲 加 ((y 减 1) 乘 宽 加 x) 乘 4、做 内存_读32 用 帧缓冲 加 ((y 减 1) 乘 宽 加 x) 乘 4 加 扩散。
                做 内存_写32 用 帧缓冲 加 ((y 加 1) 乘 宽 加 x) 乘 4、做 内存_读32 用 帧缓冲 加 ((y 加 1) 乘 宽 加 x) 乘 4 加 扩散。
                做 内存_写32 用 帧缓冲 加 (y 乘 宽 加 x 减 1) 乘 4、做 内存_读32 用 帧缓冲 加 (y 乘 宽 加 x 减 1) 乘 4 加 扩散。
                做 内存_写32 用 帧缓冲 加 (y 乘 宽 加 x 加 1) 乘 4、做 内存_读32 用 帧缓冲 加 (y 乘 宽 加 x 加 1) 乘 4 加 扩散。
            完。
            y 为 y 加 1。
        完。
        x 为 x 加 1。
    完。
完。

数字人渲染_构建女性高精度 接受 零参：
    人体顶点 为 一组：（空）。
    人体颜色 为 一组：（空）。
    人体生成计数 为 0。
    做 生成球体 用 0、1650、0、110、16、24、肤色。
    做 生成球体 用 0、1750、0、120、8、24、发色。
    做 生成球体 用 负 30、1700、50、80、6、16、发色。
    做 生成球体 用 30、1700、50、80、6、16、发色。
    做 生成球体 用 负 35、1680、90、12、6、8、新建 颜色4：r 为 180、g 为 0、b 为 255、a 为 255。
    做 生成球体 用 35、1680、90、12、6、8、新建 颜色4：r 为 180、g 为 0、b 为 255、a 为 255。
    做 生成圆柱 用 0、1100、0、130、250、16、肤色。
    做 生成圆柱 用 0、900、0、110、200、16、衣色。
    做 生成圆柱 用 0、700、0、140、200、16、衣色。
    做 生成圆柱 用 0、1200、80、5、400、6、新建 颜色4：r 为 255、g 为 215、b 为 0、a 为 255。
    做 生成圆柱 用 0、500、0、160、700、16、新建 颜色4：r 为 200、g 为 200、b 为 230、a 为 128。
    做 生成圆柱 用 负 170、1200、0、35、300、10、肤色。
    做 生成圆柱 用 负 170、900、0、30、300、10、肤色。
    做 生成圆柱 用 170、1200、0、35、300、10、肤色。
    做 生成圆柱 用 170、900、0、30、300、10、肤色。
    做 生成圆柱 用 负 70、400、0、50、400、10、肤色。
    做 生成圆柱 用 负 70、0、0、40、400、10、肤色。
    做 生成圆柱 用 70、400、0、50、400、10、肤色。
    做 生成圆柱 用 70、0、0、40、400、10、肤色。
    做 打印 用 「[数字人] 高精度女性人体: 」 加 人体生成计数 加 「 顶点」。
完。

本章 收束。
