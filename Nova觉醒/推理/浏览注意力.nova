《浏览注意力》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·定点Q算术》。

引用《..·中枢·内核打印》。

引用《..·中枢·高精度定时器hrtimer》。

引用《..·中枢·整数转字符串》。

引用《.·注意力编码》。

注意力_DOM_维度 为 32。

注意力_行为_维度 为 16。

注意力_页面_维度 为 24。

注意力_文本_维度 为 64。

注意力_表单_维度 为 20。

注意力_媒体_维度 为 28。

注意力_无障碍_维度 为 16。

注意力_最大节点 为 256。

注意力_最大序列 为 64。

注意力_最大区域 为 128。

注意力_最大文本 为 512。

浏览器注意力统计 是 一种 事物，含 DOM模型创建次数、行为模型创建次数、页面模型创建次数、文本模型创建次数、表单模型创建次数、媒体模型创建次数、无障碍模型创建次数、GQA模型创建次数、因果掩码创建次数、填充掩码创建次数、组合掩码创建次数、推理次数、GQA增量推理次数、GQA缓存重置次数、批量推理次数、总推理耗时。

可变 全局_浏览器注意力统计 为 空。

浏览器注意力统计_初始化 接受 零参：
    若 全局_浏览器注意力统计 等于 空：
        全局_浏览器注意力统计 为 新建 浏览器注意力统计。
        全局_浏览器注意力统计 的 DOM模型创建次数 为 0。
        全局_浏览器注意力统计 的 行为模型创建次数 为 0。
        全局_浏览器注意力统计 的 页面模型创建次数 为 0。
        全局_浏览器注意力统计 的 文本模型创建次数 为 0。
        全局_浏览器注意力统计 的 表单模型创建次数 为 0。
        全局_浏览器注意力统计 的 媒体模型创建次数 为 0。
        全局_浏览器注意力统计 的 无障碍模型创建次数 为 0。
        全局_浏览器注意力统计 的 GQA模型创建次数 为 0。
        全局_浏览器注意力统计 的 因果掩码创建次数 为 0。
        全局_浏览器注意力统计 的 填充掩码创建次数 为 0。
        全局_浏览器注意力统计 的 组合掩码创建次数 为 0。
        全局_浏览器注意力统计 的 推理次数 为 0。
        全局_浏览器注意力统计 的 GQA增量推理次数 为 0。
        全局_浏览器注意力统计 的 GQA缓存重置次数 为 0。
        全局_浏览器注意力统计 的 批量推理次数 为 0。
        全局_浏览器注意力统计 的 总推理耗时 为 0。
    完。
完。

创建_DOM注意力Nova注意力架构 接受 零参：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 DOM模型创建次数 为 全局_浏览器注意力统计 的 DOM模型创建次数 加 1。
    返还 做 Nova注意力架构Encoder_创建 用 注意力_DOM_维度、2、2、注意力_最大节点。
完。

创建_行为序列Nova注意力架构 接受 零参：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 行为模型创建次数 为 全局_浏览器注意力统计 的 行为模型创建次数 加 1。
    返还 做 Nova注意力架构Encoder_创建 用 注意力_行为_维度、2、2、注意力_最大序列。
完。

创建_页面结构Nova注意力架构 接受 零参：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 页面模型创建次数 为 全局_浏览器注意力统计 的 页面模型创建次数 加 1。
    返还 做 Nova注意力架构Encoder_创建 用 注意力_页面_维度、3、2、注意力_最大区域。
完。

创建_文本注意力Nova注意力架构 接受 零参：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 文本模型创建次数 为 全局_浏览器注意力统计 的 文本模型创建次数 加 1。
    返还 做 Nova注意力架构Encoder_创建 用 注意力_文本_维度、4、3、注意力_最大文本。
完。

创建_表单注意力Nova注意力架构 接受 零参：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 表单模型创建次数 为 全局_浏览器注意力统计 的 表单模型创建次数 加 1。
    返还 做 Nova注意力架构Encoder_创建 用 注意力_表单_维度、2、2、64。
完。

创建_媒体注意力Nova注意力架构 接受 零参：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 媒体模型创建次数 为 全局_浏览器注意力统计 的 媒体模型创建次数 加 1。
    返还 做 Nova注意力架构Encoder_创建 用 注意力_媒体_维度、2、2、128。
完。

创建_无障碍注意力Nova注意力架构 接受 零参：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 无障碍模型创建次数 为 全局_浏览器注意力统计 的 无障碍模型创建次数 加 1。
    返还 做 Nova注意力架构Encoder_创建 用 注意力_无障碍_维度、2、1、注意力_最大节点。
完。

创建_GQA分页注意力架构 接受 模型维度、Q头数、KV头数、层数、分页分配器：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 GQA模型创建次数 为 全局_浏览器注意力统计 的 GQA模型创建次数 加 1。
    返还 做 GQA注意力架构Encoder_创建 用 模型维度、Q头数、KV头数、层数、分页分配器。
完。

选择_性能感知模型 接受 节点数、可用内存_KB：
    若 可用内存_KB 小于 256：
        返还 做 Nova注意力架构Encoder_创建 用 8、1、1、64。
    否则若 可用内存_KB 小于 1024：
        返还 做 Nova注意力架构Encoder_创建 用 16、2、1、128。
    否则若 节点数 大于 128：
        返还 做 Nova注意力架构Encoder_创建 用 24、2、2、注意力_最大节点。
    完。
    返还 做 创建_DOM注意力Nova注意力架构。
完。

选择_性能感知GQA模型 接受 节点数、可用内存_KB、分页分配器：
    若 可用内存_KB 小于 256：
        返还 做 创建_GQA分页注意力架构 用 8、1、1、1、分页分配器。
    否则若 可用内存_KB 小于 1024：
        返还 做 创建_GQA分页注意力架构 用 16、2、1、1、分页分配器。
    否则若 节点数 大于 128：
        返还 做 创建_GQA分页注意力架构 用 24、4、2、2、分页分配器。
    完。
    返还 做 创建_GQA分页注意力架构 用 注意力_DOM_维度、2、1、2、分页分配器。
完。

注意力_推理 接受 模型、输入、序列长度、掩码：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 推理次数 为 全局_浏览器注意力统计 的 推理次数 加 1。
    开始 为 做 获取滴答。
    结果 为 做 Nova注意力架构Encoder_前向 用 模型、输入、掩码。
    耗时 为 做 获取滴答 减 开始。
    全局_浏览器注意力统计 的 总推理耗时 为 全局_浏览器注意力统计 的 总推理耗时 加 耗时。
    返还 结果。
完。

注意力_增量推理_GQA 接受 模型、输入、位置：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 GQA增量推理次数 为 全局_浏览器注意力统计 的 GQA增量推理次数 加 1。
    开始 为 做 获取滴答。
    结果 为 做 GQA注意力架构Encoder_增量前向 用 模型、输入、位置。
    耗时 为 做 获取滴答 减 开始。
    全局_浏览器注意力统计 的 总推理耗时 为 全局_浏览器注意力统计 的 总推理耗时 加 耗时。
    返还 结果。
完。

注意力_GQA重置缓存 接受 模型：
    做 浏览器注意力统计_初始化。
    若 模型 等于 空：
        返还 0。
    完。
    做 GQA注意力架构Encoder_重置缓存 用 模型。
    全局_浏览器注意力统计 的 GQA缓存重置次数 为 全局_浏览器注意力统计 的 GQA缓存重置次数 加 1。
    返还 0。
完。

注意力_批量推理 接受 模型、输入列表、序列长度列表、掩码列表：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 批量推理次数 为 全局_浏览器注意力统计 的 批量推理次数 加 1。
    批大小 为 做 计数 用 输入列表。
    可变 结果列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 批大小：
        掩码 为 空。
        若 i 小于 做 计数 用 掩码列表：
            掩码 为 掩码列表 的 第 i 项。
        完。
        序列长度 为 0。
        若 i 小于 做 计数 用 序列长度列表：
            序列长度 为 序列长度列表 的 第 i 项。
        完。
        做 追加 用 结果列表、做 注意力_推理 用 模型、输入列表 的 第 i 项、序列长度、掩码。
        i 为 i 加 1。
    完。
    返还 结果列表。
完。

创建_因果掩码 接受 序列长度：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 因果掩码创建次数 为 全局_浏览器注意力统计 的 因果掩码创建次数 加 1。
    掩码 为 做 Tensor_创建 用 序列长度、序列长度。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 j 为 0。
        只要 j 小于 序列长度：
            若 j 小于或等于 i：
                做 Tensor_写 用 掩码、i、j、Q_ONE。
            否则：
                做 Tensor_写 用 掩码、i、j、0。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 掩码。
完。

创建_填充掩码 接受 序列长度、有效长度：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 填充掩码创建次数 为 全局_浏览器注意力统计 的 填充掩码创建次数 加 1。
    掩码 为 做 Tensor_创建 用 序列长度、序列长度。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 j 为 0。
        只要 j 小于 序列长度：
            若 i 小于 有效长度 并且 j 小于 有效长度：
                做 Tensor_写 用 掩码、i、j、Q_ONE。
            否则：
                做 Tensor_写 用 掩码、i、j、0。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 掩码。
完。

创建_组合掩码 接受 序列长度、有效长度：
    做 浏览器注意力统计_初始化。
    全局_浏览器注意力统计 的 组合掩码创建次数 为 全局_浏览器注意力统计 的 组合掩码创建次数 加 1。
    掩码 为 做 Tensor_创建 用 序列长度、序列长度。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 j 为 0。
        只要 j 小于 序列长度：
            若 j 小于或等于 i 并且 i 小于 有效长度 并且 j 小于 有效长度：
                做 Tensor_写 用 掩码、i、j、Q_ONE。
            否则：
                做 Tensor_写 用 掩码、i、j、0。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 掩码。
完。

创建_滑动窗口掩码 接受 序列长度、窗口大小：
    掩码 为 做 Tensor_创建 用 序列长度、序列长度。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 j 为 0。
        只要 j 小于 序列长度：
            距离 为 i 减 j。
            若 距离 小于 0：
                距离 为 0 减 距离。
            完。
            若 距离 小于或等于 窗口大小：
                做 Tensor_写 用 掩码、i、j、Q_ONE。
            否则：
                做 Tensor_写 用 掩码、i、j、0。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 掩码。
完。

提取注意力权重 接受 模型、层索引：
    返还 做 Nova注意力架构_获取注意力权重 用 模型、层索引。
完。

注意力_Top_K 接受 权重、K：
    行数 为 做 Tensor_行数 用 权重。
    列数 为 做 Tensor_列数 用 权重。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 行数：
        可变 行TopK 为 一组：（空）。
        可变 k 为 0。
        只要 k 小于 K 并且 k 小于 列数：
            可变 最大值 为 0 减 0x7FFFFFFF。
            可变 最大索引 为 负 1。
            可变 j 为 0。
            只要 j 小于 列数：
                v 为 做 Tensor_读 用 权重、i、j。
                可变 已选 为 假。
                可变 c 为 0。
                只要 c 小于 做 计数 用 行TopK：
                    若 行TopK 的 第 c 项 等于 j：
                        已选 为 真。
                    完。
                    c 为 c 加 1。
                完。
                若 非 已选 并且 v 大于 最大值：
                    最大值 为 v。
                    最大索引 为 j。
                完。
                j 为 j 加 1。
            完。
            若 最大索引 大于或等于 0：
                做 追加 用 行TopK、最大索引。
            完。
            k 为 k 加 1。
        完。
        做 追加 用 结果、行TopK。
        i 为 i 加 1。
    完。
    返还 结果。
完。

浏览器注意力_打印统计 接受 零参：
    做 浏览器注意力统计_初始化。
    做 打印行 用 「=== 浏览器注意力统计 ===」。
    做 打印行 用 「DOM模型: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 DOM模型创建次数。
    做 打印行 用 「行为模型: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 行为模型创建次数。
    做 打印行 用 「页面模型: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 页面模型创建次数。
    做 打印行 用 「文本模型: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 文本模型创建次数。
    做 打印行 用 「表单模型: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 表单模型创建次数。
    做 打印行 用 「媒体模型: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 媒体模型创建次数。
    做 打印行 用 「无障碍模型: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 无障碍模型创建次数。
    做 打印行 用 「GQA模型: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 GQA模型创建次数。
    做 打印行 用 「因果掩码: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 因果掩码创建次数。
    做 打印行 用 「填充掩码: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 填充掩码创建次数。
    做 打印行 用 「组合掩码: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 组合掩码创建次数。
    做 打印行 用 「推理次数: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 推理次数。
    做 打印行 用 「GQA增量推理: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 GQA增量推理次数。
    做 打印行 用 「GQA缓存重置: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 GQA缓存重置次数。
    做 打印行 用 「批量推理: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 批量推理次数。
    做 打印行 用 「总推理耗时: 」 加 做 整数转字符串 用 全局_浏览器注意力统计 的 总推理耗时 除以 1000 加 「 ms」。
完。

本章 收束。
