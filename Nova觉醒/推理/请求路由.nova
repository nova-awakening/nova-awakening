《请求路由》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·哈希整数》。

引用《..·中枢·内容可见性》。

最大路由 为 64。

最大中间件 为 16。

路由项 是 一种 事物，含 模式哈希、处理回调、优先级、调用次数、总耗时、最近调用。

可变 路由表 为 一组：（空）。

可变 路由数 为 0。

可变 中间件表 为 0。

可变 中间件数 为 0。

可变 分发总数 为 0。

可变 匹配成功 为 0。

可变 匹配失败 为 0。

可变 默认处理 为 0。

路由_初始化 接受 零参：
    路由表 为 一组：（空）。
    路由数 为 0。
    中间件表 为 做 堆_分配 用 最大中间件 乘 8。
    中间件数 为 0。
    默认处理 为 0。
完。

路由_注册 接受 模式、回调、优先级：
    若 路由数 大于或等于 最大路由：
        返还 负 1。
    完。
    路由 为 新建 路由项。
    路由 的 模式哈希 为 做 哈希_字符串 用 模式。
    路由 的 处理回调 为 回调。
    路由 的 优先级 为 优先级。
    路由 的 调用次数 为 0。
    路由 的 总耗时 为 0。
    路由 的 最近调用 为 0。
    做 追加 用 路由表、路由。
    索引 为 路由数。
    路由数 为 路由数 加 1。
    返还 索引。
完。

路由_注册中间件 接受 回调：
    若 中间件数 大于或等于 最大中间件：
        返还。
    完。
    做 内存_写64 用 中间件表 加 中间件数 乘 8、回调。
    中间件数 为 中间件数 加 1。
完。

路由_设默认 接受 回调：
    默认处理 为 回调。
完。

路由_分发 接受 意图、会话哈希、消息哈希：
    分发总数 为 分发总数 加 1。
    可变 序号 为 0。
    只要 序号 小于 中间件数：
        中间件 为 做 内存_读64 用 中间件表 加 序号 乘 8。
        若 中间件 不等于 0：
            结果 为 做 中间件 用 意图、会话哈希、消息哈希。
            若 结果 不等于 0：
                返还 结果。
            完。
        完。
        序号 为 序号 加 1。
    完。
    意图哈希 为 做 哈希_字符串 用 意图。
    可变 最佳 为 负 1。
    可变 最高优先 为 负 1。
    序号 为 0。
    只要 序号 小于 路由数：
        若 路由表 的 第 序号 项 的 模式哈希 等于 意图哈希 并且 路由表 的 第 序号 项 的 优先级 大于 最高优先：
            最高优先 为 路由表 的 第 序号 项 的 优先级。
            最佳 为 序号。
        完。
        序号 为 序号 加 1。
    完。
    若 最佳 大于或等于 0：
        匹配成功 为 匹配成功 加 1。
        开始 为 做 平台_获取高精度时间。
        结果 为 做 路由表 的 第 最佳 项 的 处理回调 用 会话哈希、消息哈希。
        耗时 为 做 平台_获取高精度时间 减 开始。
        路由表 的 第 最佳 项 的 调用次数 为 路由表 的 第 最佳 项 的 调用次数 加 1。
        路由表 的 第 最佳 项 的 总耗时 为 路由表 的 第 最佳 项 的 总耗时 加 耗时。
        路由表 的 第 最佳 项 的 最近调用 为 做 平台_获取高精度时间。
        返还 结果。
    完。
    匹配失败 为 匹配失败 加 1。
    若 默认处理 不等于 0：
        返还 做 默认处理 用 会话哈希、消息哈希。
    完。
    返还 0。
完。

路由_删除 接受 模式：
    目标 为 做 哈希_字符串 用 模式。
    可变 序号 为 0。
    只要 序号 小于 路由数：
        若 路由表 的 第 序号 项 的 模式哈希 等于 目标：
            路由表 的 第 序号 项 为 路由表 的 第 路由数 减 1 项。
            路由数 为 路由数 减 1。
            返还 真。
        完。
        序号 为 序号 加 1。
    完。
    返还 假。
完。

路由_获取统计 接受 索引：
    若 索引 大于或等于 路由数：
        返还 一组：0、0。
    完。
    返还 一组：路由表 的 第 索引 项 的 调用次数、路由表 的 第 索引 项 的 总耗时。
完。

路由_最热 接受 零参：
    若 路由数 等于 0：
        返还 负 1。
    完。
    可变 最多 为 0。
    可变 最多索引 为 0。
    可变 序号 为 0。
    只要 序号 小于 路由数：
        若 路由表 的 第 序号 项 的 调用次数 大于 最多：
            最多 为 路由表 的 第 序号 项 的 调用次数。
            最多索引 为 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 最多索引。
完。

路由_匹配率 接受 零参：
    若 分发总数 等于 0：
        返还 0。
    完。
    返还 匹配成功 乘 100 除以 分发总数。
完。

路由_完整统计 接受 零参：
    返还 一组：路由数、中间件数、分发总数、匹配成功、匹配失败、做 路由_匹配率。
完。

本章 收束。
