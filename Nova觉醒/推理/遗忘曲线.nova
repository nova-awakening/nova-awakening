《遗忘曲线》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·激活导数》。

引用《.·定点精度》。

引用《.·色彩空间转换》。

引用《.·视觉对比敏感度》。

引用《.·定点Q算术》。

遗忘曲线 接受 时间、稳定性：
    若 稳定性 小于或等于 0：
        返还 Q_ZERO。
    完。
    指数 为 负 做 Q除 用 时间、稳定性。
    返还 做 Q_exp 用 指数。
完。

相对预测误差 接受 实际、预测：
    若 实际 等于 0：
        返还 Q_ONE。
    完。
    差 为 实际 减 预测。
    若 差 小于 0：
        差 为 负 差。
    完。
    返还 做 Q除 用 差、实际。
完。

Weber_JND 接受 亮度_q10、Weber_k：
    返还 做 Q乘 用 Weber_k、亮度_q10。
完。

Fitts预测时间 接受 距离、目标宽度：
    若 目标宽度 小于或等于 0：
        返还 10 乘 Q_ONE。
    完。
    难度 为 做 Q除 用 距离、目标宽度 加 Q_ONE。
    log2_难度 为 做 Q除 用 做 Q_log 用 难度、710。
    返还 50 乘 Q_ONE 加 做 Q乘 用 150 乘 Q_ONE、log2_难度。
完。

估计VC维度 接受 总权重数：
    log2_w 为 做 Q除 用 做 Q_log 用 总权重数 乘 Q_ONE、710。
    返还 做 Q乘 用 总权重数 乘 Q_ONE、log2_w 右移 Q_SHIFT。
完。

泛化误差上界 接受 VC维、样本数：
    若 样本数 小于或等于 0：
        返还 Q_ONE。
    完。
    返还 做 Q开方 用 做 Q除 用 VC维 乘 Q_ONE、样本数 乘 Q_ONE。
完。

最大记忆卡 为 256。

记忆卡片 是 一种 事物，含 内容哈希、稳定性、下次复习时间、复习次数、成功次数、失败次数。

可变 记忆卡库 为 一组：（空）。

可变 记忆卡数 为 0。

稳定性_成功倍率 为 2500。

稳定性_失败倍率 为 500。

记忆_添加 接受 内容哈希、当前时间：
    若 记忆卡数 大于或等于 最大记忆卡：
        返还 负 1。
    完。
    卡 为 新建 记忆卡片。
    卡 的 内容哈希 为 内容哈希。
    卡 的 稳定性 为 1000。
    卡 的 下次复习时间 为 当前时间 加 60。
    卡 的 复习次数 为 0。
    卡 的 成功次数 为 0。
    卡 的 失败次数 为 0。
    做 追加 用 记忆卡库、卡。
    索引 为 记忆卡数。
    记忆卡数 为 记忆卡数 加 1。
    返还 索引。
完。

记忆_复习 接受 索引、成功、当前时间：
    若 索引 大于或等于 记忆卡数：
        返还。
    完。
    记忆卡库 的 第 索引 项 的 复习次数 为 记忆卡库 的 第 索引 项 的 复习次数 加 1。
    若 成功：
        记忆卡库 的 第 索引 项 的 成功次数 为 记忆卡库 的 第 索引 项 的 成功次数 加 1。
        记忆卡库 的 第 索引 项 的 稳定性 为 记忆卡库 的 第 索引 项 的 稳定性 乘 稳定性_成功倍率 除以 1000。
        若 记忆卡库 的 第 索引 项 的 稳定性 大于 100000：
            记忆卡库 的 第 索引 项 的 稳定性 为 100000。
        完。
    否则：
        记忆卡库 的 第 索引 项 的 失败次数 为 记忆卡库 的 第 索引 项 的 失败次数 加 1。
        记忆卡库 的 第 索引 项 的 稳定性 为 记忆卡库 的 第 索引 项 的 稳定性 乘 稳定性_失败倍率 除以 1000。
        若 记忆卡库 的 第 索引 项 的 稳定性 小于 100：
            记忆卡库 的 第 索引 项 的 稳定性 为 100。
        完。
    完。
    记忆卡库 的 第 索引 项 的 下次复习时间 为 当前时间 加 记忆卡库 的 第 索引 项 的 稳定性。
完。

记忆_获取到期卡 接受 当前时间：
    可变 最佳 为 负 1。
    可变 最早时间 为 999999999。
    可变 i 为 0。
    只要 i 小于 记忆卡数：
        若 记忆卡库 的 第 i 项 的 下次复习时间 小于或等于 当前时间 并且 记忆卡库 的 第 i 项 的 下次复习时间 小于 最早时间：
            最早时间 为 记忆卡库 的 第 i 项 的 下次复习时间。
            最佳 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

晶化_阈值 为 800。

记忆_晶化度 接受 索引：
    若 索引 大于或等于 记忆卡数：
        返还 0。
    完。
    卡 为 记忆卡库 的 第 索引 项。
    若 卡 的 复习次数 等于 0：
        返还 0。
    完。
    成功率 为 卡 的 成功次数 乘 1000 除以 卡 的 复习次数。
    可变 对数 为 0。
    可变 tmp 为 卡 的 复习次数 加 1。
    只要 tmp 大于 1：
        tmp 为 tmp 除以 2。
        对数 为 对数 加 1。
    完。
    返还 成功率 乘 对数 除以 10。
完。

记忆_已结晶 接受 索引：
    返还 做 记忆_晶化度 用 索引 大于 晶化_阈值。
完。

情绪_编码修正 接受 基础学习率、情绪值：
    若 情绪值 大于 300：
        增强 为 (情绪值 减 300) 乘 500 除以 700。
        返还 基础学习率 乘 (1000 加 增强) 除以 1000。
    完。
    若 情绪值 小于 负 300：
        抑制 为 (0 减 情绪值 减 300) 乘 400 除以 700。
        返还 基础学习率 乘 (1000 减 抑制) 除以 1000。
    完。
    返还 基础学习率。
完。

情绪_提取修正 接受 基础稳定性、情绪值：
    情绪强度 为 情绪值。
    若 情绪强度 小于 0：
        情绪强度 为 0 减 情绪强度。
    完。
    修正 为 (1000 减 情绪强度 除以 3) 乘 基础稳定性 除以 1000。
    返还 修正。
完。

理论基础_全面自检 接受 零参：
    做 断言 用 做 理论验证_定点精度、「定点数论验证失败」。
    做 断言 用 做 Q_log 用 Q_ONE 等于 0 或者 做 Q_log 用 Q_ONE 等于 1、「ln(1)应为0」。
    亮度 为 做 计算亮度 用 512、512、512。
    做 断言 用 亮度 大于 0、「亮度计算异常」。
    衰减 为 做 注视点衰减 用 0、300 乘 Q_ONE。
    做 断言 用 衰减 大于或等于 Q_ONE 减 10、「注视点中心衰减应≈1.0」。
    远衰减 为 做 注视点衰减 用 1000 乘 Q_ONE、300 乘 Q_ONE。
    做 断言 用 远衰减 小于 Q_HALF、「远离注视点衰减应<0.5」。
    静止遮蔽 为 做 运动遮蔽系数 用 0。
    做 断言 用 静止遮蔽 等于 0、「静止时不应有运动遮蔽」。
    返还 真。
完。

遗忘曲线_完整统计 接受 零参：
    可变 结晶数 为 0。
    可变 i 为 0。
    只要 i 小于 记忆卡数：
        若 做 记忆_已结晶 用 i：
            结晶数 为 结晶数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：一组：「模块」、「遗忘曲线」、一组：「记忆卡数」、记忆卡数、一组：「已结晶数」、结晶数、一组：「功能」、「遗忘曲线+间隔重复+记忆晶化+情绪调制」、一组：「创新」、「记忆晶化理论+情绪调制记忆」。
完。

本章 收束。
