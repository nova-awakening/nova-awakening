《内联缓存》

引用《..·中枢·系统调用表》。

引用《..·推理·通信渠道》。

引用《..·中枢·整数转字符串》。

缓存_单态 为 0。

缓存_多态 为 1。

缓存_超态 为 2。

最大多态 为 4。

最大缓存点 为 512。

衰减基数 为 1024。

内联缓存项 是 一种 事物，含 形状哈希、属性偏移、命中次数。

内联缓存点 是 一种 事物，含 代码位置、状态、项表、项数、总命中、总未中、转换次数。

可变 缓存点表 为 一组：（空）。

可变 缓存点数 为 0。

可变 总命中 为 0。

可变 总未中 为 0。

可变 预热次数 为 0。

可变 超态转换 为 0。

内联缓存_创建 接受 代码位置：
    若 缓存点数 大于或等于 最大缓存点：
        返还 负 1。
    完。
    点 为 新建 内联缓存点。
    点 的 代码位置 为 代码位置。
    点 的 状态 为 缓存_单态。
    点 的 项表 为 做 堆_分配 用 最大多态 乘 24。
    点 的 项数 为 0。
    点 的 总命中 为 0。
    点 的 总未中 为 0。
    点 的 转换次数 为 0。
    做 追加 用 缓存点表、点。
    索引 为 缓存点数。
    缓存点数 为 缓存点数 加 1。
    返还 索引。
完。

内联缓存_查找 接受 缓存索引、形状哈希：
    若 缓存索引 大于或等于 缓存点数：
        返还 负 1。
    完。
    点 为 缓存点表 的 第 缓存索引 项。
    若 点 的 状态 等于 缓存_超态：
        点 的 总未中 为 点 的 总未中 加 1。
        总未中 为 总未中 加 1。
        返还 负 1。
    完。
    可变 序号 为 0。
    只要 序号 小于 点 的 项数：
        偏移 为 点 的 项表 加 序号 乘 24。
        若 做 内存_读64 用 偏移 等于 形状哈希：
            属性偏移 为 做 内存_读64 用 偏移 加 8。
            做 内存_写64 用 偏移 加 16、做 内存_读64 用 偏移 加 16 加 1。
            点 的 总命中 为 点 的 总命中 加 1。
            总命中 为 总命中 加 1。
            返还 属性偏移。
        完。
        序号 为 序号 加 1。
    完。
    点 的 总未中 为 点 的 总未中 加 1。
    总未中 为 总未中 加 1。
    返还 负 1。
完。

内联缓存_更新 接受 缓存索引、形状哈希、属性偏移：
    若 缓存索引 大于或等于 缓存点数：
        返还。
    完。
    点 为 缓存点表 的 第 缓存索引 项。
    若 点 的 状态 等于 缓存_超态：
        返还。
    完。
    若 点 的 项数 大于或等于 最大多态：
        点 的 状态 为 缓存_超态。
        点 的 转换次数 为 点 的 转换次数 加 1。
        超态转换 为 超态转换 加 1。
        返还。
    完。
    偏移 为 点 的 项表 加 点 的 项数 乘 24。
    做 内存_写64 用 偏移、形状哈希。
    做 内存_写64 用 偏移 加 8、属性偏移。
    做 内存_写64 用 偏移 加 16、1。
    点 的 项数 为 点 的 项数 加 1。
    若 点 的 项数 等于 1：
        点 的 状态 为 缓存_单态。
    否则：
        点 的 状态 为 缓存_多态。
        点 的 转换次数 为 点 的 转换次数 加 1。
    完。
完。

内联缓存_预热 接受 缓存索引、形状哈希、属性偏移：
    预热次数 为 预热次数 加 1。
    做 内联缓存_更新 用 缓存索引、形状哈希、属性偏移。
完。

内联缓存_智能预测 接受 缓存索引、当前形状、属性名哈希：
    若 缓存索引 大于或等于 缓存点数：
        返还。
    完。
    点 为 缓存点表 的 第 缓存索引 项。
    若 点 的 项数 等于 0：
        返还。
    完。
    最热偏移 为 点 的 项表。
    最热命中 为 做 内存_读64 用 最热偏移 加 16。
    可变 序号 为 1。
    只要 序号 小于 点 的 项数：
        偏移 为 点 的 项表 加 序号 乘 24。
        若 做 内存_读64 用 偏移 加 16 大于 最热命中：
            最热偏移 为 偏移。
            最热命中 为 做 内存_读64 用 偏移 加 16。
        完。
        序号 为 序号 加 1。
    完。
完。

内联缓存_获取状态名 接受 缓存索引：
    若 缓存索引 大于或等于 缓存点数：
        返还 「无效」。
    完。
    状态 为 缓存点表 的 第 缓存索引 项 的 状态。
    若 状态 等于 缓存_单态：
        返还 「单态」。
    完。
    若 状态 等于 缓存_多态：
        返还 「多态」。
    完。
    若 状态 等于 缓存_超态：
        返还 「超态」。
    完。
    返还 「未知」。
完。

内联缓存_命中率 接受 零参：
    总 为 总命中 加 总未中。
    若 总 等于 0：
        返还 0。
    完。
    返还 总命中 乘 100 除以 总。
完。

内联缓存_单态比例 接受 零参：
    若 缓存点数 等于 0：
        返还 0。
    完。
    可变 单态数 为 0。
    可变 序号 为 0。
    只要 序号 小于 缓存点数：
        若 缓存点表 的 第 序号 项 的 状态 等于 缓存_单态：
            单态数 为 单态数 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 单态数 乘 100 除以 缓存点数。
完。

内联缓存_完整统计 接受 零参：
    返还 一组：缓存点数、总命中、总未中、做 内联缓存_命中率、预热次数、超态转换、做 内联缓存_单态比例。
完。

内联缓存_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 做 内联缓存_命中率 小于 50 并且 总命中 加 总未中 大于 1000：
        做 追加 用 问题、「IC命中率过低: 」 加 做 整数转字符串 用 做 内联缓存_命中率 加 「%」。
    完。
    若 超态转换 大于 缓存点数 除以 3 并且 缓存点数 大于 50：
        做 追加 用 问题、「超态转换过多: 」 加 做 整数转字符串 用 超态转换 加 「/」 加 做 整数转字符串 用 缓存点数。
    完。
    若 做 内联缓存_单态比例 小于 30 并且 缓存点数 大于 100：
        做 追加 用 问题、「单态比例低: 」 加 做 整数转字符串 用 做 内联缓存_单态比例 加 「%, 代码多态性高」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「内联缓存健康」。
    完。
    返还 问题。
完。

内联缓存_详细统计 接受 零参：
    返还 一本字典：缓存点总数 对应 缓存点数、总命中 对应 总命中、总未中 对应 总未中、命中率 对应 做 内联缓存_命中率、预热次数 对应 预热次数、超态转换 对应 超态转换、单态比例 对应 做 内联缓存_单态比例、多态比例 对应 做 内联缓存_多态比例、超态比例 对应 做 内联缓存_超态比例。
完。

内联缓存_多态比例 接受 零参：
    若 缓存点数 等于 0：
        返还 0。
    完。
    可变 多态数 为 0。
    可变 i 为 0。
    只要 i 小于 缓存点数：
        若 缓存点表 的 第 i 项 的 状态 等于 缓存_多态：
            多态数 为 多态数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 多态数 乘 100 除以 缓存点数。
完。

内联缓存_超态比例 接受 零参：
    若 缓存点数 等于 0：
        返还 0。
    完。
    可变 超态数 为 0。
    可变 i 为 0。
    只要 i 小于 缓存点数：
        若 缓存点表 的 第 i 项 的 状态 等于 缓存_超态：
            超态数 为 超态数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 超态数 乘 100 除以 缓存点数。
完。

内联缓存_重置 接受 缓存索引：
    若 缓存索引 大于或等于 缓存点数：
        返还。
    完。
    点 为 缓存点表 的 第 缓存索引 项。
    点 的 状态 为 缓存_单态。
    点 的 项数 为 0。
    点 的 总命中 为 0。
    点 的 总未中 为 0。
    点 的 转换次数 为 0。
完。

内联缓存_全部重置 接受 零参：
    可变 i 为 0。
    只要 i 小于 缓存点数：
        做 内联缓存_重置 用 i。
        i 为 i 加 1。
    完。
    总命中 为 0。
    总未中 为 0。
    预热次数 为 0。
    超态转换 为 0。
完。

内联缓存_清除超态 接受 零参：
    可变 i 为 0。
    只要 i 小于 缓存点数：
        若 缓存点表 的 第 i 项 的 状态 等于 缓存_超态：
            做 内联缓存_重置 用 i。
        完。
        i 为 i 加 1。
    完。
完。

内联缓存_获取点 接受 缓存索引：
    若 缓存索引 大于或等于 缓存点数：
        返还 空。
    完。
    返还 缓存点表 的 第 缓存索引 项。
完。

内联缓存_点诊断 接受 缓存索引：
    若 缓存索引 大于或等于 缓存点数：
        返还 「无效索引」。
    完。
    点 为 缓存点表 的 第 缓存索引 项。
    返还 一本字典：代码位置 对应 点 的 代码位置、状态 对应 做 内联缓存_获取状态名 用 缓存索引、项数 对应 点 的 项数、命中 对应 点 的 总命中、未中 对应 点 的 总未中、转换 对应 点 的 转换次数。
完。

对象形状 是 一种 事物，含 哈希、属性表、属性数、原型形状、转换表、转换数。

可变 形状表 为 一组：（空）。

可变 形状总数 为 0。

最大形状数 为 4096。

形状_创建 接受 零参：
    若 形状总数 大于或等于 最大形状数：
        返还 0。
    完。
    形状 为 新建 对象形状。
    形状 的 哈希 为 形状总数 乘 2654435761。
    形状 的 属性表 为 一组：（空）。
    形状 的 属性数 为 0。
    形状 的 原型形状 为 0。
    形状 的 转换表 为 一组：（空）。
    形状 的 转换数 为 0。
    做 追加 用 形状表、形状。
    形状总数 为 形状总数 加 1。
    返还 形状。
完。

形状_添加属性 接受 形状、属性名：
    若 形状 等于 0：
        返还 0。
    完。
    已有 为 做 形状_查找转换 用 形状、属性名。
    若 已有 不等于 0：
        返还 已有。
    完。
    新形状 为 做 形状_创建。
    若 新形状 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 形状 的 属性数：
        做 追加 用 新形状 的 属性表、形状 的 属性表 的 第 i 项。
        i 为 i 加 1。
    完。
    做 追加 用 新形状 的 属性表、属性名。
    新形状 的 属性数 为 形状 的 属性数 加 1。
    新形状 的 原型形状 为 形状。
    做 追加 用 形状 的 转换表、一本字典：属性名 对应 属性名、目标形状 对应 新形状。
    形状 的 转换数 为 形状 的 转换数 加 1。
    返还 新形状。
完。

形状_查找转换 接受 形状、属性名：
    若 形状 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 形状 的 转换数：
        若 做 字符串_相等 用 形状 的 转换表 的 第 i 项 的 属性名、属性名：
            返还 形状 的 转换表 的 第 i 项 的 目标形状。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

形状_查找属性偏移 接受 形状、属性名：
    若 形状 等于 0：
        返还 负 1。
    完。
    可变 i 为 0。
    只要 i 小于 形状 的 属性数：
        若 做 字符串_相等 用 形状 的 属性表 的 第 i 项、属性名：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

形状_统计 接受 零参：
    返还 一本字典：形状总数 对应 形状总数、最大形状 对应 最大形状数。
完。

可变 IC_衰减周期 为 10000。

可变 IC_自适应阈值 为 100。

内联缓存_衰减 接受 零参：
    可变 i 为 0。
    只要 i 小于 缓存点数：
        点 为 缓存点表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 点 的 项数：
            偏移 为 点 的 项表 加 j 乘 24。
            命中 为 做 内存_读64 用 偏移 加 16。
            做 内存_写64 用 偏移 加 16、命中 乘 衰减基数 除以 (衰减基数 加 1)。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

内联缓存_建议优化 接受 缓存索引：
    若 缓存索引 大于或等于 缓存点数：
        返还 「无效」。
    完。
    点 为 缓存点表 的 第 缓存索引 项。
    若 点 的 状态 等于 缓存_超态：
        返还 「建议重构:超态IC,属性访问多态性过高」。
    完。
    若 点 的 状态 等于 缓存_多态 并且 点 的 转换次数 大于 5：
        返还 「建议review:多态IC频繁转换」。
    完。
    若 点 的 总未中 大于 点 的 总命中 乘 2 并且 点 的 总命中 大于 10：
        返还 「建议预热:未命中率过高」。
    完。
    返还 「正常」。
完。

内联缓存_热点列表 接受 前N：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 缓存点数 并且 做 计数 用 结果 小于 前N：
        点 为 缓存点表 的 第 i 项。
        若 点 的 总命中 加 点 的 总未中 大于 IC_自适应阈值：
            做 追加 用 结果、一本字典：索引 对应 i、命中 对应 点 的 总命中、未中 对应 点 的 总未中、状态 对应 点 的 状态。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

内联缓存_设置衰减周期 接受 周期：
    若 周期 大于 0：
        IC_衰减周期 为 周期。
    完。
完。

内联缓存_设置自适应阈值 接受 阈值：
    若 阈值 大于 0：
        IC_自适应阈值 为 阈值。
    完。
完。

IC_完整报告 接受 零参：
    返还 一本字典：缓存点 对应 缓存点数、命中率 对应 做 内联缓存_命中率、单态 对应 做 内联缓存_单态比例、多态 对应 做 内联缓存_多态比例、超态 对应 做 内联缓存_超态比例、预热 对应 预热次数、形状 对应 形状总数。
完。

本章 收束。
