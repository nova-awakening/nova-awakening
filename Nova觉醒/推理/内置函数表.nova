《内置函数表》

引用《..·中枢·系统调用表》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《.·定点Q算术》。

引用《..·中枢·整数转字符串》。

引用《.·小整数SMI判定》。

BUILTIN_MAX 为 512。

BuiltinEntry 是 一种 事物，含 名称、函数指针、参数数。

BuiltinTable 是 一种 事物，含 条目、数量。

可变 全局内置表 为 0。

Builtins_初始化 接受 零参：
    若 全局内置表 不等于 0：
        返还。
    完。
    全局内置表 为 做 堆_分配 用 BuiltinTable。
    全局内置表 的 数量 为 0。
    做 Builtins_注册 用 「Object.keys」、Builtin_Object_keys、1。
    做 Builtins_注册 用 「Object.values」、Builtin_Object_values、1。
    做 Builtins_注册 用 「Object.entries」、Builtin_Object_entries、1。
    做 Builtins_注册 用 「Object.assign」、Builtin_Object_assign、2。
    做 Builtins_注册 用 「Object.freeze」、Builtin_Object_freeze、1。
    做 Builtins_注册 用 「Object.create」、Builtin_Object_create、1。
    做 Builtins_注册 用 「Object.defineProperty」、Builtin_Object_defineProperty、3。
    做 Builtins_注册 用 「数组.is数组」、Builtin_数组_is数组、1。
    做 Builtins_注册 用 「数组.from」、Builtin_数组_from、1。
    做 Builtins_注册 用 「序列.parse」、Builtin_序列_parse、1。
    做 Builtins_注册 用 「序列.stringify」、Builtin_序列_stringify、1。
    做 Builtins_注册 用 「数学.random」、Builtin_数学_random、0。
    做 Builtins_注册 用 「数学.floor」、Builtin_数学_floor、1。
    做 Builtins_注册 用 「数学.ceil」、Builtin_数学_ceil、1。
    做 Builtins_注册 用 「数学.round」、Builtin_数学_round、1。
    做 Builtins_注册 用 「数学.abs」、Builtin_数学_abs、1。
    做 Builtins_注册 用 「数学.max」、Builtin_数学_max、2。
    做 Builtins_注册 用 「数学.min」、Builtin_数学_min、2。
    做 Builtins_注册 用 「数学.pow」、Builtin_数学_pow、2。
    做 Builtins_注册 用 「数学.sqrt」、Builtin_数学_sqrt、1。
    做 Builtins_注册 用 「parseInt」、Builtin_parseInt、1。
    做 Builtins_注册 用 「parse浮动」、Builtin_parse浮动、1。
    做 Builtins_注册 用 「isNaN」、Builtin_isNaN、1。
    做 Builtins_注册 用 「isFinite」、Builtin_isFinite、1。
    做 Builtins_注册 用 「typeof」、Builtin_typeof、1。
完。

Builtins_注册 接受 名称、函数、参数数：
    若 全局内置表 等于 0 或者 全局内置表 的 数量 大于或等于 BUILTIN_MAX：
        返还。
    完。
    idx 为 全局内置表 的 数量。
    全局内置表 的 条目 的 第 idx 项 的 名称 为 名称。
    全局内置表 的 条目 的 第 idx 项 的 函数指针 为 以 零参 为 引，其做法是：
    全局内置表 的 条目 的 第 idx 项 的 参数数 为 参数数。
    全局内置表 的 数量 为 全局内置表 的 数量 加 1。
完。

Builtins_查找 接受 名称：
    若 全局内置表 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 全局内置表 的 数量：
        若 做 字符串_相等 用 全局内置表 的 条目 的 第 i 项 的 名称、名称：
            返还 全局内置表 的 条目 的 第 i 项 的 函数指针。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

Builtin_typeof 接受 参数：
    返还 Nova_未定义。
完。

Builtin_parseInt 接受 参数：
    返还 做 Nova小整数_编码 用 0。
完。

Builtin_parse浮动 接受 参数：
    返还 做 Nova小整数_编码 用 0。
完。

Builtin_isNaN 接受 参数：
    返还 Nova_假。
完。

Builtin_isFinite 接受 参数：
    返还 Nova_真。
完。

Builtin_Object_keys 接受 参数：
    返还 Nova_未定义。
完。

Builtin_Object_values 接受 参数：
    返还 Nova_未定义。
完。

Builtin_Object_entries 接受 参数：
    返还 Nova_未定义。
完。

Builtin_Object_assign 接受 参数：
    返还 Nova_未定义。
完。

Builtin_Object_freeze 接受 参数：
    返还 Nova_未定义。
完。

Builtin_Object_create 接受 参数：
    返还 Nova_未定义。
完。

Builtin_Object_defineProperty 接受 参数：
    返还 Nova_未定义。
完。

Builtin_数组_is数组 接受 参数：
    返还 Nova_假。
完。

Builtin_数组_from 接受 参数：
    返还 Nova_未定义。
完。

Builtin_序列_parse 接受 参数：
    返还 Nova_未定义。
完。

Builtin_序列_stringify 接受 参数：
    返还 Nova_未定义。
完。

Builtin_数学_random 接受 参数：
    返还 做 Nova小整数_编码 用 0。
完。

Builtin_数学_floor 接受 参数：
    返还 做 Nova小整数_编码 用 0。
完。

Builtin_数学_ceil 接受 参数：
    返还 做 Nova小整数_编码 用 0。
完。

Builtin_数学_round 接受 参数：
    返还 做 Nova小整数_编码 用 0。
完。

Builtin_数学_abs 接受 参数：
    返还 做 Nova小整数_编码 用 0。
完。

Builtin_数学_max 接受 参数：
    返还 做 Nova小整数_编码 用 0。
完。

Builtin_数学_min 接受 参数：
    返还 做 Nova小整数_编码 用 0。
完。

Builtin_数学_pow 接受 参数：
    返还 做 Nova小整数_编码 用 0。
完。

Builtin_数学_sqrt 接受 参数：
    返还 做 Nova小整数_编码 用 0。
完。

NBFT_版本 为 1。

NBFT_哈希表大小 为 1024。

NBFT_AI特征维度 为 12。

可变 NBFT_查找总数 为 0。

可变 NBFT_命中总数 为 0。

可变 NBFT_未命中总数 为 0。

可变 NBFT_调用总数 为 0。

可变 NBFT_AI推理次数 为 0。

可变 NBFT_哈希表 为 一组：（空）。

哈希条目 是 一种 事物，含 名称、函数指针、参数数、调用计数、下一个。

NBFT_哈希 接受 名称：
    可变 哈希 为 5381。
    可变 i 为 0。
    长度 为 做 字符串长度 用 名称。
    只要 i 小于 长度：
        哈希 为 (哈希 左移 5) 加 哈希 加 做 字符串_取字符 用 名称、i 位与 0xFFFFFFFF。
        i 为 i 加 1。
    完。
    返还 哈希 取余于 NBFT_哈希表大小。
完。

NBFT_初始化哈希表 接受 零参：
    NBFT_哈希表 为 做 数组_创建 用 NBFT_哈希表大小。
    可变 i 为 0。
    只要 i 小于 NBFT_哈希表大小：
        NBFT_哈希表 的 第 i 项 为 空。
        i 为 i 加 1。
    完。
    若 全局内置表 不等于 0：
        i 为 0。
        只要 i 小于 全局内置表 的 数量：
            名称 为 全局内置表 的 条目 的 第 i 项 的 名称。
            哈希值 为 做 NBFT_哈希 用 名称。
            条目 为 一本字典：名称 对应 名称、函数指针 对应 全局内置表 的 条目 的 第 i 项 的 函数指针、参数数 对应 全局内置表 的 条目 的 第 i 项 的 参数数、调用计数 对应 0、下一个 对应 NBFT_哈希表 的 第 哈希值 项。
            NBFT_哈希表 的 第 哈希值 项 为 条目。
            i 为 i 加 1。
        完。
    完。
完。

NBFT_快速查找 接受 名称：
    NBFT_查找总数 为 NBFT_查找总数 加 1。
    哈希值 为 做 NBFT_哈希 用 名称。
    可变 条目 为 NBFT_哈希表 的 第 哈希值 项。
    只要 条目 不等于 空：
        若 做 字符串_相等 用 条目 的 名称、名称：
            条目 的 调用计数 为 条目 的 调用计数 加 1。
            NBFT_命中总数 为 NBFT_命中总数 加 1。
            返还 条目 的 函数指针。
        完。
        条目 为 条目 的 下一个。
    完。
    NBFT_未命中总数 为 NBFT_未命中总数 加 1。
    返还 0。
完。

NBFT_数学_floor 接受 值：
    整数部分 为 值 位或 0。
    若 值 小于 0 并且 值 不等于 整数部分：
        返还 整数部分 减 1。
    完。
    返还 整数部分。
完。

NBFT_数学_ceil 接受 值：
    整数部分 为 值 位或 0。
    若 值 大于 0 并且 值 不等于 整数部分：
        返还 整数部分 加 1。
    完。
    返还 整数部分。
完。

NBFT_数学_round 接受 值：
    返还 做 NBFT_数学_floor 用 值 加 0.5。
完。

NBFT_数学_abs 接受 值：
    若 值 小于 0：
        返还 负 值。
    完。
    返还 值。
完。

NBFT_数学_max 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

NBFT_数学_min 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

NBFT_数学_sqrt 接受 值：
    若 值 小于 0：
        返还 负 1。
    完。
    若 值 等于 0：
        返还 0。
    完。
    可变 猜 为 值 除以 2。
    可变 迭代 为 0。
    只要 迭代 小于 50：
        新猜 为 (猜 加 值 除以 猜) 除以 2。
        差 为 新猜 减 猜。
        若 差 小于 0：
            差 为 负 差。
        完。
        若 差 小于 0.0000001：
            返还 新猜。
        完。
        猜 为 新猜。
        迭代 为 迭代 加 1。
    完。
    返还 猜。
完。

NBFT_数学_pow 接受 底、指数：
    若 指数 等于 0：
        返还 1。
    完。
    若 指数 小于 0：
        返还 1 除以 做 NBFT_数学_pow 用 底、负 指数。
    完。
    可变 结果 为 1。
    可变 基 为 底。
    可变 幂 为 指数。
    只要 幂 大于 0：
        若 幂 位与 1：
            结果 为 结果 乘 基。
        完。
        基 为 基 乘 基。
        幂 为 幂 右移 1。
    完。
    返还 结果。
完。

可变 NBFT_随机种子 为 2463534242。

NBFT_数学_random 接受 零参：
    NBFT_随机种子 为 NBFT_随机种子 位异或 NBFT_随机种子 左移 13。
    NBFT_随机种子 为 NBFT_随机种子 位异或 NBFT_随机种子 右移 17。
    NBFT_随机种子 为 NBFT_随机种子 位异或 NBFT_随机种子 左移 5。
    返还 (NBFT_随机种子 位与 0x7FFFFFFF) 除以 2147483647。
完。

NBFT_数学_clamp 接受 值、最小、最大：
    若 值 小于 最小：
        返还 最小。
    完。
    若 值 大于 最大：
        返还 最大。
    完。
    返还 值。
完。

NBFT_数学_lerp 接受 a、b、t：
    返还 a 加 (b 减 a) 乘 t。
完。

NBFT_字符串_charAt 接受 字符串、索引：
    若 索引 小于 0 或者 索引 大于或等于 做 字符串长度 用 字符串：
        返还 「」。
    完。
    返还 做 字符串_取子串 用 字符串、索引、1。
完。

NBFT_字符串_indexOf 接受 字符串、搜索、起始位：
    源长 为 做 字符串长度 用 字符串。
    搜长 为 做 字符串长度 用 搜索。
    若 搜长 等于 0：
        返还 起始位。
    完。
    若 搜长 大于 源长：
        返还 负 1。
    完。
    可变 i 为 起始位。
    只要 i 小于或等于 源长 减 搜长：
        子串 为 做 字符串_取子串 用 字符串、i、搜长。
        若 做 字符串_相等 用 子串、搜索：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

NBFT_字符串_slice 接受 字符串、开始、结束：
    长度 为 做 字符串长度 用 字符串。
    若 开始 小于 0：
        开始 为 长度 加 开始。
    完。
    若 结束 小于 0：
        结束 为 长度 加 结束。
    完。
    若 开始 小于 0：
        开始 为 0。
    完。
    若 结束 大于 长度：
        结束 为 长度。
    完。
    若 开始 大于或等于 结束：
        返还 「」。
    完。
    返还 做 字符串_取子串 用 字符串、开始、结束 减 开始。
完。

NBFT_字符串_toUpperCase 接受 字符串：
    可变 结果 为 「」。
    可变 i 为 0。
    长度 为 做 字符串长度 用 字符串。
    只要 i 小于 长度：
        字符 为 做 字符串_取字符 用 字符串、i。
        若 字符 大于或等于 97 并且 字符 小于或等于 122：
            结果 为 结果 加 做 字符_从编码 用 字符 减 32。
        否则：
            结果 为 结果 加 做 字符_从编码 用 字符。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

NBFT_字符串_toLowerCase 接受 字符串：
    可变 结果 为 「」。
    可变 i 为 0。
    长度 为 做 字符串长度 用 字符串。
    只要 i 小于 长度：
        字符 为 做 字符串_取字符 用 字符串、i。
        若 字符 大于或等于 65 并且 字符 小于或等于 90：
            结果 为 结果 加 做 字符_从编码 用 字符 加 32。
        否则：
            结果 为 结果 加 做 字符_从编码 用 字符。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

NBFT_字符串_trim 接受 字符串：
    长度 为 做 字符串长度 用 字符串。
    可变 开始 为 0。
    可变 结束 为 长度 减 1。
    只要 开始 小于 长度：
        c 为 做 字符串_取字符 用 字符串、开始。
        若 c 不等于 32 并且 c 不等于 9 并且 c 不等于 10 并且 c 不等于 13：
            断开。
        完。
        开始 为 开始 加 1。
    完。
    只要 结束 大于或等于 开始：
        c 为 做 字符串_取字符 用 字符串、结束。
        若 c 不等于 32 并且 c 不等于 9 并且 c 不等于 10 并且 c 不等于 13：
            断开。
        完。
        结束 为 结束 减 1。
    完。
    若 开始 大于 结束：
        返还 「」。
    完。
    返还 做 字符串_取子串 用 字符串、开始、结束 减 开始 加 1。
完。

NBFT_数组_push 接受 数组、值：
    长度 为 做 数组长度 用 数组。
    数组 的 第 长度 项 为 值。
    返还 长度 加 1。
完。

NBFT_数组_pop 接受 数组：
    长度 为 做 数组长度 用 数组。
    若 长度 等于 0：
        返还 空。
    完。
    值 为 数组 的 第 长度 减 1 项。
    做 数组_设置长度 用 数组、长度 减 1。
    返还 值。
完。

NBFT_数组_isArray 接受 值：
    返还 做 Nova_获取类型 用 值 等于 Nova类型_数组。
完。

NBFT_数组_indexOf 接受 数组、搜索值：
    可变 i 为 0。
    长度 为 做 数组长度 用 数组。
    只要 i 小于 长度：
        若 数组 的 第 i 项 等于 搜索值：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

NBFT_数组_reverse 接受 数组：
    长度 为 做 数组长度 用 数组。
    可变 左 为 0。
    可变 右 为 长度 减 1。
    只要 左 小于 右：
        临时 为 数组 的 第 左 项。
        数组 的 第 左 项 为 数组 的 第 右 项。
        数组 的 第 右 项 为 临时。
        左 为 左 加 1。
        右 为 右 减 1。
    完。
    返还 数组。
完。

NBFT_typeof 接受 值：
    类型 为 做 Nova_获取类型 用 值。
    若 类型 等于 Nova类型_未定义：
        返还 「undefined」。
    完。
    若 类型 等于 Nova类型_空：
        返还 「object」。
    完。
    若 类型 等于 Nova类型_布尔：
        返还 「boolean」。
    完。
    若 类型 等于 Nova类型_数字：
        返还 「number」。
    完。
    若 类型 等于 Nova类型_字符串：
        返还 「string」。
    完。
    若 类型 等于 Nova类型_函数：
        返还 「function」。
    完。
    若 类型 等于 Nova类型_符号：
        返还 「symbol」。
    完。
    返还 「object」。
完。

NBFT_parseInt 接受 字符串、基数：
    可变 结果 为 0。
    可变 符号 为 1。
    可变 i 为 0。
    长度 为 做 字符串长度 用 字符串。
    若 基数 等于 0：
        基数 为 10。
    完。
    只要 i 小于 长度：
        c 为 做 字符串_取字符 用 字符串、i。
        若 c 不等于 32 并且 c 不等于 9：
            断开。
        完。
        i 为 i 加 1。
    完。
    若 i 小于 长度：
        c 为 做 字符串_取字符 用 字符串、i。
        若 c 等于 45：
            符号 为 负 1。
            i 为 i 加 1。
        否则若 c 等于 43：
            i 为 i 加 1。
        完。
    完。
    只要 i 小于 长度：
        c 为 做 字符串_取字符 用 字符串、i。
        可变 数位 为 负 1。
        若 c 大于或等于 48 并且 c 小于或等于 57：
            数位 为 c 减 48。
        否则若 c 大于或等于 65 并且 c 小于或等于 90：
            数位 为 c 减 55。
        否则若 c 大于或等于 97 并且 c 小于或等于 122：
            数位 为 c 减 87。
        完。
        若 数位 小于 0 或者 数位 大于或等于 基数：
            断开。
        完。
        结果 为 结果 乘 基数 加 数位。
        i 为 i 加 1。
    完。
    返还 结果 乘 符号。
完。

NBFT_isNaN 接受 值：
    返还 值 不等于 值。
完。

NBFT_isFinite 接受 值：
    返还 非 做 NBFT_isNaN 用 值 并且 值 不等于 999999999 并且 值 不等于 负 999999999。
完。

NBFT_注册扩展内置 接受 零参：
    做 Builtins_注册 用 「字符串.charAt」、NBFT_字符串_charAt、2。
    做 Builtins_注册 用 「字符串.indexOf」、NBFT_字符串_indexOf、3。
    做 Builtins_注册 用 「字符串.slice」、NBFT_字符串_slice、3。
    做 Builtins_注册 用 「字符串.toUpperCase」、NBFT_字符串_toUpperCase、1。
    做 Builtins_注册 用 「字符串.toLowerCase」、NBFT_字符串_toLowerCase、1。
    做 Builtins_注册 用 「字符串.trim」、NBFT_字符串_trim、1。
    做 Builtins_注册 用 「数组.push」、NBFT_数组_push、2。
    做 Builtins_注册 用 「数组.pop」、NBFT_数组_pop、1。
    做 Builtins_注册 用 「数组.indexOf」、NBFT_数组_indexOf、2。
    做 Builtins_注册 用 「数组.reverse」、NBFT_数组_reverse、1。
    做 Builtins_注册 用 「数学.clamp」、NBFT_数学_clamp、3。
    做 Builtins_注册 用 「数学.lerp」、NBFT_数学_lerp、3。
    做 NBFT_初始化哈希表。
完。

NBFT_AI预测热函数 接受 零参：
    特征 为 做 Tensor_创建 用 一组：NBFT_AI特征维度。
    做 Tensor_写 用 特征、一组：0、NBFT_查找总数。
    做 Tensor_写 用 特征、一组：1、NBFT_命中总数。
    做 Tensor_写 用 特征、一组：2、NBFT_未命中总数。
    做 Tensor_写 用 特征、一组：3、NBFT_调用总数。
    做 Tensor_写 用 特征、一组：4、全局内置表 的 数量。
    若 NBFT_查找总数 大于 0：
        做 Tensor_写 用 特征、一组：5、NBFT_命中总数 乘 1000 除以 NBFT_查找总数。
    否则：
        做 Tensor_写 用 特征、一组：5、0。
    完。
    做 Tensor_写 用 特征、一组：6、0。
    做 Tensor_写 用 特征、一组：7、0。
    做 Tensor_写 用 特征、一组：8、0。
    做 Tensor_写 用 特征、一组：9、0。
    做 Tensor_写 用 特征、一组：10、0。
    做 Tensor_写 用 特征、一组：11、0。
    预测 为 做 AI总线_推理 用 「NBFT热函数」、特征。
    NBFT_AI推理次数 为 NBFT_AI推理次数 加 1。
    做 Tensor_释放 用 特征。
    做 Tensor_释放 用 预测。
完。

NBFT_获取统计 接受 零参：
    返还 一本字典：版本 对应 NBFT_版本、查找总数 对应 NBFT_查找总数、命中总数 对应 NBFT_命中总数、未命中总数 对应 NBFT_未命中总数、调用总数 对应 NBFT_调用总数、内置函数数 对应 全局内置表 的 数量、命中率 对应 若 NBFT_查找总数 大于 0 则 NBFT_命中总数 乘 1000 除以 NBFT_查找总数 否则 0、AI推理次数 对应 NBFT_AI推理次数。
完。

NBFT_重置统计 接受 零参：
    NBFT_查找总数 为 0。
    NBFT_命中总数 为 0。
    NBFT_未命中总数 为 0。
    NBFT_调用总数 为 0。
    NBFT_AI推理次数 为 0。
完。

可变 _谐振热路径_调整次数 为 0。

谐振热路径_分析 接受 零参：
    可变 最热索引 为 负 1。
    可变 最热调用 为 0。
    可变 i 为 0。
    只要 i 小于 全局内置表 的 数量：
        若 全局内置表 的 条目 的 第 i 项 的 调用次数 大于 最热调用：
            最热调用 为 全局内置表 的 条目 的 第 i 项 的 调用次数。
            最热索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最热索引。
完。

谐振热路径_优化 接受 零参：
    热索引 为 做 谐振热路径_分析。
    若 热索引 小于 0：
        返还。
    完。
    _谐振热路径_调整次数 为 _谐振热路径_调整次数 加 1。
完。

内置函数表_完整统计 接受 零参：
    返还 一组：「NBFT版本」、NBFT_版本、「内置函数数」、全局内置表 的 数量、「查找总数」、NBFT_查找总数、「命中总数」、NBFT_命中总数、「调用总数」、NBFT_调用总数、「AI推理次数」、NBFT_AI推理次数、「热路径调整」、_谐振热路径_调整次数。
完。

内置函数表_统计 接受 零参：
    返还 一本字典：NBFT查找总数 对应 NBFT_查找总数、NBFT命中总数 对应 NBFT_命中总数、NBFT未命中总数 对应 NBFT_未命中总数、NBFT调用总数 对应 NBFT_调用总数、NBFTAI推理次数 对应 NBFT_AI推理次数、谐振热路径调整次数 对应 _谐振热路径_调整次数。
完。

内置函数表_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 NBFT_查找总数 大于 100000：
        做 追加 用 问题、「NBFT_查找总数异常增长」。
    完。
    若 NBFT_命中总数 大于 100000：
        做 追加 用 问题、「NBFT_命中总数异常增长」。
    完。
    若 NBFT_未命中总数 大于 100000：
        做 追加 用 问题、「NBFT_未命中总数异常增长」。
    完。
    若 NBFT_调用总数 大于 100000：
        做 追加 用 问题、「NBFT_调用总数异常增长」。
    完。
    若 NBFT_AI推理次数 大于 100000：
        做 追加 用 问题、「NBFT_AI推理次数异常增长」。
    完。
    若 做 计数 用 NBFT_哈希表 大于 10000：
        做 追加 用 问题、「NBFT_哈希表膨胀」。
    完。
    若 _谐振热路径_调整次数 大于 100000：
        做 追加 用 问题、「_谐振热路径_调整次数异常增长」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

内置函数表_综合报告 接受 零参：
    做 打印 用 「[内置函数表] ═══ 综合报告 ═══」。
    做 打印 用 「  NBFT_查找总数: 」 加 NBFT_查找总数。
    做 打印 用 「  NBFT_命中总数: 」 加 NBFT_命中总数。
    做 打印 用 「  NBFT_未命中总数: 」 加 NBFT_未命中总数。
    做 打印 用 「  NBFT_调用总数: 」 加 NBFT_调用总数。
    做 打印 用 「  NBFT_AI推理次数: 」 加 NBFT_AI推理次数。
    做 打印 用 「  _谐振热路径_调整次数: 」 加 _谐振热路径_调整次数。
    做 打印 用 「  健康: 」 加 若 做 内置函数表_健康评估 则 「正常」 否则 「异常」。
完。

内置函数表_全局重置 接受 零参：
    NBFT_查找总数 为 0。
    NBFT_命中总数 为 0。
    NBFT_未命中总数 为 0。
    NBFT_调用总数 为 0。
    NBFT_AI推理次数 为 0。
    _谐振热路径_调整次数 为 0。
    做 打印 用 「[内置函数表] 统计已重置」。
    返还 0。
完。

本章 收束。
