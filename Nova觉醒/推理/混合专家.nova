《混合专家》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《.·AI鼠标事件处理》。

引用《..·中枢·字符串替换》。

引用《..·中枢·张量数学引擎》。

引用《..·标准·数学幂根对数》。

MOE_默认专家数 为 8。

MOE_默认TopK 为 2。

MOE_负载均衡系数 为 0.01。

MOE_容量因子 为 1.25。

MOE_噪声系数 为 1.0。

MOE_辅助损失_重要性 为 0.01。

MoE层 是 一种 事物，含 专家数、topk、隐藏维度、中间维度、门控权重、专家列表、负载计数、是训练。

混合专家层_创建 接受 隐藏维度、中间维度、专家数、topk：
    可变 层 为 新建 MoE层。
    层 的 专家数 为 专家数。
    层 的 topk 为 topk。
    层 的 隐藏维度 为 隐藏维度。
    层 的 中间维度 为 中间维度。
    层 的 门控权重 为 做 张量_随机正态 用 隐藏维度、专家数、0.0、0.02。
    层 的 负载计数 为 做 张量_零 用 专家数。
    层 的 是训练 为 真。
    层 的 专家列表 为 做 数组_创建 用 专家数。
    可变 i 为 0。
    只要 i 小于 专家数：
        专家 为 做 FFN_创建_门控线性 用 隐藏维度、中间维度。
        做 数组_设置 用 层 的 专家列表、i、专家。
        i 为 i 加 1。
    完。
    返还 层。
完。

混合专家层_前向 接受 层、输入：
    B 为 输入 的 批大小。
    N 为 输入 的 序列长度。
    D 为 层 的 隐藏维度。
    总令牌s 为 B 乘 N。
    路由logits 为 做 矩阵乘 用 输入、层 的 门控权重。
    若 层 的 是训练：
        噪声 为 做 张量_随机正态同形 用 路由logits。
        路由logits 为 做 张量_加 用 路由logits、做 张量_标量乘 用 噪声、MOE_噪声系数。
    完。
    选择结果 为 做 张量_topk 用 路由logits、层 的 topk。
    选中索引 为 选择结果 的 索引。
    选中logits 为 选择结果 的 值。
    门控权重 为 做 张量_Nova归一化指数 用 选中logits、负 1。
    可变 输出 为 做 张量_零同形 用 输入。
    可变 k 为 0。
    只要 k 小于 层 的 topk：
        可变 t 为 0。
        只要 t 小于 总令牌s：
            专家idx 为 做 张量_取整数 用 选中索引、t、k。
            权重 为 做 张量_取浮点 用 门控权重、t、k。
            令牌向量 为 做 张量_取行 用 输入、t。
            专家 为 做 数组_取 用 层 的 专家列表、专家idx。
            专家输出 为 做 FFN_前向 用 专家、令牌向量。
            做 张量_累加行 用 输出、t、做 张量_标量乘 用 专家输出、权重。
            做 张量_累加 用 层 的 负载计数、专家idx、1.0。
            t 为 t 加 1。
        完。
        k 为 k 加 1。
    完。
    返还 输出。
完。

混合专家_负载均衡损失 接受 层、路由logits：
    E 为 层 的 专家数。
    N 为 路由logits 的 行数。
    可变 f 为 做 张量_零 用 E。
    可变 P 为 做 张量_Nova归一化指数_列 用 路由logits。
    P_均值 为 做 张量_列均值 用 P。
    选择结果 为 做 张量_topk 用 路由logits、层 的 topk。
    可变 i 为 0。
    只要 i 小于 N：
        可变 k 为 0。
        只要 k 小于 层 的 topk：
            idx 为 做 张量_取整数 用 选择结果 的 索引、i、k。
            做 张量_累加 用 f、idx、1.0 除以 N。
            k 为 k 加 1。
        完。
        i 为 i 加 1。
    完。
    损失 为 MOE_辅助损失_重要性 乘 E 乘 做 张量_点积 用 f、P_均值。
    返还 损失。
完。

混合专家_ExpertChoice_前向 接受 层、输入：
    N 为 输入 的 行数。
    E 为 层 的 专家数。
    容量 为 N 乘 MOE_容量因子 除以 E。
    分数 为 做 矩阵乘 用 输入、层 的 门控权重。
    分数_转置 为 做 张量_转置 用 分数。
    可变 输出 为 做 张量_零同形 用 输入。
    可变 e 为 0。
    只要 e 小于 E：
        专家分数 为 做 张量_取行 用 分数_转置、e。
        选中 为 做 张量_topk_1d 用 专家分数、容量。
        专家 为 做 数组_取 用 层 的 专家列表、e。
        可变 c 为 0。
        只要 c 小于 容量：
            令牌_idx 为 做 张量_取整数_1d 用 选中 的 索引、c。
            权重 为 做 张量_Nova归一化指数_1d_取 用 专家分数、令牌_idx。
            令牌向量 为 做 张量_取行 用 输入、令牌_idx。
            专家输出 为 做 FFN_前向 用 专家、令牌向量。
            做 张量_累加行 用 输出、令牌_idx、做 张量_标量乘 用 专家输出、权重。
            c 为 c 加 1。
        完。
        e 为 e 加 1。
    完。
    返还 输出。
完。

FFN_SwiGLU 是 一种 事物，含 W_gate、W_up、W_down、隐藏维度、中间维度。

FFN_创建_门控线性 接受 隐藏维度、中间维度：
    ffn 为 新建 FFN_SwiGLU。
    ffn 的 W_gate 为 做 张量_随机正态 用 隐藏维度、中间维度、0.0、0.02。
    ffn 的 W_up 为 做 张量_随机正态 用 隐藏维度、中间维度、0.0、0.02。
    ffn 的 W_down 为 做 张量_随机正态 用 中间维度、隐藏维度、0.0、0.02。
    ffn 的 隐藏维度 为 隐藏维度。
    ffn 的 中间维度 为 中间维度。
    返还 ffn。
完。

FFN_前向 接受 ffn、x：
    若 ffn 等于 0 或者 x 等于 0：
        返还 空。
    完。
    gate 为 做 矩阵乘 用 x、ffn 的 W_gate。
    up 为 做 矩阵乘 用 x、ffn 的 W_up。
    gate_激活 为 做 向量_silu 用 gate。
    中间 为 做 张量_逐元素乘 用 gate_激活、up。
    返还 做 矩阵乘 用 中间、ffn 的 W_down。
完。

FFN_销毁 接受 ffn：
    若 ffn 等于 0：
        返还。
    完。
    若 ffn 的 W_gate 不等于 0：
        做 张量_释放 用 ffn 的 W_gate。
    完。
    若 ffn 的 W_up 不等于 0：
        做 张量_释放 用 ffn 的 W_up。
    完。
    若 ffn 的 W_down 不等于 0：
        做 张量_释放 用 ffn 的 W_down。
    完。
完。

向量_silu 接受 x：
    若 x 等于 0：
        返还 空。
    完。
    N 为 x 的 长度。
    可变 结果 为 做 张量_零 用 N。
    可变 i 为 0。
    只要 i 小于 N：
        val 为 做 张量_取 用 x、i。
        sigmoid 为 1.0 除以 (1.0 加 做 数学_exp 用 0.0 减 val)。
        做 张量_设 用 结果、i、val 乘 sigmoid。
        i 为 i 加 1。
    完。
    返还 结果。
完。

混合专家层_销毁 接受 层：
    若 层 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 层 的 专家数：
        做 FFN_销毁 用 做 数组_取 用 层 的 专家列表、i。
        i 为 i 加 1。
    完。
    若 层 的 门控权重 不等于 0：
        做 张量_释放 用 层 的 门控权重。
    完。
    若 层 的 负载计数 不等于 0：
        做 张量_释放 用 层 的 负载计数。
    完。
完。

混合专家层_重置负载 接受 层：
    若 层 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 层 的 专家数：
        做 张量_设 用 层 的 负载计数、i、0.0。
        i 为 i 加 1。
    完。
完。

MoE统计 是 一种 事物，含 总前向次数、总令牌处理数、总专家调用次数、专家负载方差、最热专家ID、最冷专家ID、最热专家负载、最冷专家负载、平均路由熵、辅助损失累计、负载均衡损失历史、历史长度、总推理耗时、路由耗时、专家计算耗时、丢弃令牌数。

可变 全局MoE统计 为 0。

混合专家统计_初始化 接受 零参：
    s 为 新建 MoE统计。
    s 的 总前向次数 为 0。
    s 的 总令牌处理数 为 0。
    s 的 总专家调用次数 为 0。
    s 的 专家负载方差 为 0。
    s 的 最热专家ID 为 0。
    s 的 最冷专家ID 为 0。
    s 的 最热专家负载 为 0。
    s 的 最冷专家负载 为 999999。
    s 的 平均路由熵 为 0。
    s 的 辅助损失累计 为 0。
    s 的 负载均衡损失历史 为 一组：（空）。
    s 的 历史长度 为 0。
    s 的 总推理耗时 为 0。
    s 的 路由耗时 为 0。
    s 的 专家计算耗时 为 0。
    s 的 丢弃令牌数 为 0。
    全局MoE统计 为 s。
    返还 s。
完。

混合专家统计_更新负载 接受 层：
    若 全局MoE统计 等于 0：
        做 混合专家统计_初始化。
    完。
    若 层 等于 0：
        返还。
    完。
    s 为 全局MoE统计。
    可变 最大 为 0。
    可变 最小 为 999999。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 层 的 专家数：
        负载 为 做 张量_取 用 层 的 负载计数、i。
        总和 为 总和 加 负载。
        若 负载 大于 最大：
            最大 为 负载。
            s 的 最热专家ID 为 i。
        完。
        若 负载 小于 最小：
            最小 为 负载。
            s 的 最冷专家ID 为 i。
        完。
        i 为 i 加 1。
    完。
    s 的 最热专家负载 为 最大。
    s 的 最冷专家负载 为 最小。
    均值 为 总和 除以 层 的 专家数。
    可变 方差和 为 0。
    i 为 0。
    只要 i 小于 层 的 专家数：
        差 为 做 张量_取 用 层 的 负载计数、i 减 均值。
        方差和 为 方差和 加 差 乘 差。
        i 为 i 加 1。
    完。
    s 的 专家负载方差 为 方差和 乘 1024 除以 层 的 专家数。
完。

混合专家统计_打印 接受 零参：
    若 全局MoE统计 等于 0：
        做 打印 用 「[MoE] 统计未初始化」。
        返还。
    完。
    s 为 全局MoE统计。
    做 打印 用 「[MoE] ═══ 混合专家统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  总前向次数: 」、做 转字符串 用 s 的 总前向次数。
    做 打印 用 做 字符串拼接 用 「  总令牌处理数: 」、做 转字符串 用 s 的 总令牌处理数。
    做 打印 用 做 字符串拼接 用 「  总专家调用次数: 」、做 转字符串 用 s 的 总专家调用次数。
    做 打印 用 做 字符串拼接 用 「  最热专家: #」、做 字符串拼接 用 做 转字符串 用 s 的 最热专家ID、做 字符串拼接 用 「 负载=」、做 转字符串 用 s 的 最热专家负载。
    做 打印 用 做 字符串拼接 用 「  最冷专家: #」、做 字符串拼接 用 做 转字符串 用 s 的 最冷专家ID、做 字符串拼接 用 「 负载=」、做 转字符串 用 s 的 最冷专家负载。
    做 打印 用 做 字符串拼接 用 「  负载方差(Q10): 」、做 转字符串 用 s 的 专家负载方差。
    做 打印 用 做 字符串拼接 用 「  丢弃令牌数: 」、做 转字符串 用 s 的 丢弃令牌数。
    做 打印 用 做 字符串拼接 用 「  辅助损失累计: 」、做 转字符串 用 s 的 辅助损失累计。
    做 打印 用 做 字符串拼接 用 「  总耗时: 」、做 字符串拼接 用 做 转字符串 用 s 的 总推理耗时、「μs」。
    做 打印 用 「[MoE] ═══════════════════════」。
完。

混合专家_AI自适应TopK 接受 bus、输入嵌入、默认topk：
    若 bus 等于 0：
        返还 默认topk。
    完。
    复杂度 为 做 AI总线_语义分类 用 bus、输入嵌入。
    若 复杂度 大于 800：
        返还 做 最小值 用 默认topk 加 2、4。
    完。
    若 复杂度 小于 200：
        返还 1。
    完。
    返还 默认topk。
完。

混合专家_AI自适应容量 接受 bus、默认容量因子：
    若 bus 等于 0：
        返还 默认容量因子。
    完。
    若 bus 的 图处负载 小于 50：
        返还 默认容量因子 加 50。
    完。
    若 bus 的 图处负载 大于 80：
        返还 默认容量因子 减 25。
        若 默认容量因子 减 25 小于 100：
            返还 100。
        完。
    完。
    返还 默认容量因子。
完。

最大GPU数 为 8。

专家并行上下文 是 一种 事物，含 GPU数量、本地专家起始、本地专家结束、通信缓冲、发送计数、接收计数。

专家并行_创建 接受 GPU数量、总专家数：
    ctx 为 新建 专家并行上下文。
    ctx 的 GPU数量 为 GPU数量。
    每GPU专家 为 总专家数 除以 GPU数量。
    ctx 的 本地专家起始 为 0。
    ctx 的 本地专家结束 为 每GPU专家。
    ctx 的 通信缓冲 为 一组：（空）。
    ctx 的 发送计数 为 0。
    ctx 的 接收计数 为 0。
    返还 ctx。
完。

专家并行_全到全 接受 ctx、本地令牌、目标专家列表：
    若 ctx 等于 0：
        返还 本地令牌。
    完。
    可变 发送列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 本地令牌：
        目标GPU 为 目标专家列表 的 第 i 项 除以 (ctx 的 本地专家结束 减 ctx 的 本地专家起始)。
        若 目标GPU 大于或等于 ctx 的 GPU数量：
            目标GPU 为 ctx 的 GPU数量 减 1。
        完。
        做 追加 用 发送列表、目标GPU。
        ctx 的 发送计数 为 ctx 的 发送计数 加 1。
        i 为 i 加 1。
    完。
    ctx 的 接收计数 为 ctx 的 接收计数 加 做 计数 用 本地令牌。
    返还 本地令牌。
完。

专家并行_销毁 接受 ctx：
    若 ctx 等于 0：
        返还。
    完。
    ctx 的 通信缓冲 为 一组：（空）。
    ctx 的 发送计数 为 0。
    ctx 的 接收计数 为 0。
完。

专家并行_打印统计 接受 ctx：
    若 ctx 等于 0：
        做 打印 用 「[专家并行] 上下文未初始化」。
        返还。
    完。
    做 打印 用 「[专家并行] ═══ 通信统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  GPU数量: 」、做 转字符串 用 ctx 的 GPU数量。
    做 打印 用 做 字符串拼接 用 「  本地专家范围: [」、做 字符串拼接 用 做 转字符串 用 ctx 的 本地专家起始、做 字符串拼接 用 「, 」、做 字符串拼接 用 做 转字符串 用 ctx 的 本地专家结束、「)」。
    做 打印 用 做 字符串拼接 用 「  发送令牌数: 」、做 转字符串 用 ctx 的 发送计数。
    做 打印 用 做 字符串拼接 用 「  接收令牌数: 」、做 转字符串 用 ctx 的 接收计数。
    做 打印 用 「[专家并行] ═══════════════════」。
完。

混合专家_计算辅助损失 接受 层、路由概率：
    若 层 等于 0 或者 路由概率 等于 0：
        返还 0。
    完。
    N 为 路由概率 的 行数。
    E 为 层 的 专家数。
    可变 损失 为 0。
    可变 e 为 0。
    只要 e 小于 E：
        选中比例 为 做 张量_取 用 层 的 负载计数、e 除以 N。
        可变 概率和 为 0。
        可变 i 为 0。
        只要 i 小于 N：
            概率和 为 概率和 加 做 张量_取 用 路由概率、i 乘 E 加 e。
            i 为 i 加 1。
        完。
        平均概率 为 概率和 除以 N。
        损失 为 损失 加 选中比例 乘 平均概率。
        e 为 e 加 1。
    完。
    损失 为 损失 乘 E。
    若 全局MoE统计 不等于 0：
        全局MoE统计 的 辅助损失累计 为 全局MoE统计 的 辅助损失累计 加 损失 乘 1024。
    完。
    返还 损失。
完。

混合专家_计算路由熵 接受 路由概率、N、E：
    可变 总熵 为 0。
    可变 i 为 0。
    只要 i 小于 N：
        可变 熵 为 0。
        可变 e 为 0。
        只要 e 小于 E：
            p 为 做 张量_取 用 路由概率、i 乘 E 加 e。
            若 p 大于 0.0001：
                熵 为 熵 减 p 乘 做 数学_log 用 p。
            完。
            e 为 e 加 1。
        完。
        总熵 为 总熵 加 熵。
        i 为 i 加 1。
    完。
    返还 总熵 乘 1024 除以 N。
完。

混合专家_检测危险专家 接受 层：
    若 层 等于 0 或者 层 的 负载计数 等于 0：
        返还 0。
    完。
    E 为 层 的 专家数。
    可变 总负载 为 0。
    可变 e 为 0。
    只要 e 小于 E：
        总负载 为 总负载 加 做 张量_取 用 层 的 负载计数、e。
        e 为 e 加 1。
    完。
    若 总负载 等于 0：
        返还 0。
    完。
    均值负载 为 总负载 除以 E。
    危险阈值 为 均值负载 乘 2。
    可变 危险数 为 0。
    e 为 0。
    只要 e 小于 E：
        若 做 张量_取 用 层 的 负载计数、e 大于 危险阈值：
            危险数 为 危险数 加 1。
        完。
        e 为 e 加 1。
    完。
    返还 危险数。
完。

混合专家_路由健康分 接受 层、路由概率、N：
    若 层 等于 0：
        返还 0。
    完。
    E 为 层 的 专家数。
    熵分 为 做 混合专家_计算路由熵 用 路由概率、N、E。
    最大熵 为 做 数学_log 用 E 乘 1024。
    熵归一 为 若 最大熵 大于 0 则 熵分 乘 1000 除以 最大熵 否则 0。
    危险数 为 做 混合专家_检测危险专家 用 层。
    危险惩罚 为 危险数 乘 200 除以 E。
    健康分 为 熵归一 减 危险惩罚。
    若 健康分 小于 0：
        健康分 为 0。
    完。
    若 健康分 大于 1000：
        健康分 为 1000。
    完。
    返还 健康分。
完。

混合专家_中文感知健康分 接受 层、路由概率、N、中文密度_千分：
    基础分 为 做 混合专家_路由健康分 用 层、路由概率、N。
    若 中文密度_千分 小于或等于 0：
        返还 基础分。
    完。
    中文加成 为 中文密度_千分 乘 100 除以 1000。
    若 基础分 大于 500：
        调整分 为 基础分 加 中文加成。
        若 调整分 大于 1000：
            调整分 为 1000。
        完。
        返还 调整分。
    完。
    返还 基础分。
完。

本章 收束。
