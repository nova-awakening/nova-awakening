《告警通知》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·哈希整数》。

引用《..·中枢·内容可见性》。

级别_信息 为 0。

级别_警告 为 1。

级别_严重 为 2。

级别_紧急 为 3。

最大规则 为 32。

最大历史 为 512。

节流间隔 为 60000000。

告警规则 是 一种 事物，含 名称哈希、级别、检查回调、启用、触发次数、上次触发、节流间隔。

告警记录 是 一种 事物，含 时间戳、级别、规则哈希、内容哈希、已确认。

可变 规则表 为 一组：（空）。

可变 规则数 为 0。

可变 历史表 为 一组：（空）。

可变 历史数 为 0。

可变 通知回调表 为 0。

可变 通知回调数 为 0。

可变 总触发 为 0。

可变 总抑制 为 0。

告警_初始化 接受 零参：
    规则表 为 一组：（空）。
    规则数 为 0。
    历史表 为 一组：（空）。
    历史数 为 0。
    通知回调表 为 做 堆_分配 用 8 乘 8。
    通知回调数 为 0。
完。

告警_注册规则 接受 名称、级别、检查回调：
    若 规则数 大于或等于 最大规则：
        返还 负 1。
    完。
    规则 为 新建 告警规则。
    规则 的 名称哈希 为 做 哈希_字符串 用 名称。
    规则 的 级别 为 级别。
    规则 的 检查回调 为 检查回调。
    规则 的 启用 为 真。
    规则 的 触发次数 为 0。
    规则 的 上次触发 为 0。
    规则 的 节流间隔 为 节流间隔。
    做 追加 用 规则表、规则。
    索引 为 规则数。
    规则数 为 规则数 加 1。
    返还 索引。
完。

告警_注册通知 接受 回调：
    若 通知回调数 大于或等于 8：
        返还。
    完。
    做 内存_写64 用 通知回调表 加 通知回调数 乘 8、回调。
    通知回调数 为 通知回调数 加 1。
完。

告警_触发 接受 规则索引、内容哈希：
    若 规则索引 大于或等于 规则数：
        返还。
    完。
    规则 为 规则表 的 第 规则索引 项。
    若 非 规则 的 启用：
        返还。
    完。
    现在 为 做 平台_获取高精度时间。
    若 现在 减 规则 的 上次触发 小于 规则 的 节流间隔：
        总抑制 为 总抑制 加 1。
        返还。
    完。
    规则 的 触发次数 为 规则 的 触发次数 加 1。
    规则 的 上次触发 为 现在。
    总触发 为 总触发 加 1。
    若 历史数 大于或等于 最大历史：
        历史表 的 第 0 项 为 历史表 的 第 历史数 减 1 项。
        历史数 为 历史数 减 1。
    完。
    记录 为 新建 告警记录。
    记录 的 时间戳 为 现在。
    记录 的 级别 为 规则 的 级别。
    记录 的 规则哈希 为 规则 的 名称哈希。
    记录 的 内容哈希 为 内容哈希。
    记录 的 已确认 为 假。
    做 追加 用 历史表、记录。
    历史数 为 历史数 加 1。
    做 告警_分发通知 用 规则 的 级别、规则 的 名称哈希、内容哈希。
完。

告警_分发通知 接受 级别、规则哈希、内容哈希：
    可变 序号 为 0。
    只要 序号 小于 通知回调数：
        回调 为 做 内存_读64 用 通知回调表 加 序号 乘 8。
        若 回调 不等于 0：
            做 回调 用 级别、规则哈希、内容哈希。
        完。
        序号 为 序号 加 1。
    完。
完。

告警_检查全部 接受 零参：
    可变 序号 为 0。
    只要 序号 小于 规则数：
        若 规则表 的 第 序号 项 的 启用 并且 规则表 的 第 序号 项 的 检查回调 不等于 0：
            若 做 规则表 的 第 序号 项 的 检查回调：
                做 告警_触发 用 序号、0。
            完。
        完。
        序号 为 序号 加 1。
    完。
完。

告警_确认 接受 历史索引：
    若 历史索引 大于或等于 历史数：
        返还。
    完。
    历史表 的 第 历史索引 项 的 已确认 为 真。
完。

告警_启用规则 接受 规则索引、启用：
    若 规则索引 大于或等于 规则数：
        返还。
    完。
    规则表 的 第 规则索引 项 的 启用 为 启用。
完。

告警_设节流 接受 规则索引、间隔：
    若 规则索引 大于或等于 规则数：
        返还。
    完。
    规则表 的 第 规则索引 项 的 节流间隔 为 间隔。
完。

告警_未确认数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 历史数：
        若 非 历史表 的 第 序号 项 的 已确认：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

告警_按级别统计 接受 级别：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 历史数：
        若 历史表 的 第 序号 项 的 级别 等于 级别：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

告警_级别名 接受 级别：
    若 级别 等于 级别_信息：
        返还 「信息」。
    完。
    若 级别 等于 级别_警告：
        返还 「警告」。
    完。
    若 级别 等于 级别_严重：
        返还 「严重」。
    完。
    若 级别 等于 级别_紧急：
        返还 「紧急」。
    完。
    返还 「未知」。
完。

告警_完整统计 接受 零参：
    返还 一组：规则数、历史数、总触发、总抑制、做 告警_未确认数、做 告警_按级别统计 用 级别_严重。
完。

可变 AI_告警通知_事件频率 为 0。

可变 AI_告警通知_节流阈值 为 16。

AI_告警通知_节流判断 接受 事件类型、间隔毫秒：
    AI_告警通知_事件频率 为 AI_告警通知_事件频率 加 1。
    若 事件类型 等于 「滚动」 并且 间隔毫秒 小于 AI_告警通知_节流阈值：
        返还 真。
    完。
    若 事件类型 等于 「移动」 并且 间隔毫秒 小于 AI_告警通知_节流阈值：
        返还 真。
    完。
    返还 假。
完。

AI_告警通知_优先级排序 接受 事件类型：
    若 事件类型 等于 「点击」：
        返还 100。
    完。
    若 事件类型 等于 「输入」：
        返还 90。
    完。
    若 事件类型 等于 「滚动」：
        返还 30。
    完。
    返还 50。
完。

本章 收束。
