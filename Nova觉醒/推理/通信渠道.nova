《通信渠道》

引用《..·外联·路由查找》。

引用《.·模板系统》。

引用《..·执行·内核适配》。

引用《.·消息总线》。

引用《.·渠道接口》。

引用《.·网关框架》。

引用《.·上下文追踪》。

引用《.·提示模板》。

引用《.·会话管理》。

引用《.·回复核心》。

引用《..·中枢·哈希整数》。

引用《..·中枢·硬链接》。

引用《.·配置管理》。

引用《.·结果聚合》。

引用《.·任务队列》。

引用《..·中枢·整数转字符串》。

引用《.·告警通知》。

引用《.·接口适配》。

引用《.·消息存储》。

引用《..·中枢·自动扩容》。

引用《.·知识库》。

引用《.·意图识别》。

引用《.·健康检查》。

引用《.·指标收集》。

引用《.·请求路由》。

引用《.·终端渠道》。

引用《.·网聊适配》。

引用《.·工具调用》。

渠道档 是 一种 事物，含 名称、渠道表、最近会话、最近请求、最近回证、最近意图、最近回复、最近错误、初始化完成、渠道数、请求数、回复数、命中数、失败数、轮询数、异步数、健康。

渠道请求 是 一种 事物，含 渠道名、渠道号、用户ID、文本、请求ID、会话ID、意图、实体、标签、优先级。

渠道回证 是 一种 事物，含 已收、已成、会话ID、请求ID、意图、置信度、回复、来源、路由、错因。

渠道摘要 是 一种 事物，含 名称、渠道数、请求数、回复数、命中数、失败数、异步数、健康。

可变 渠道账本 为 一组：（空）。

可变 渠道建数 为 0。

可变 渠道总请求 为 0。

可变 渠道总回复 为 0。

可变 渠道总命中 为 0。

可变 渠道总失败 为 0。

可变 默认智能渠道档 为 0。

智能渠道_布串 接受 值：
    若 值：
        返还 「true」。
    完。
    返还 「false」。
完。

智能渠道_建档 接受 名称：
    档 为 做 堆_分配 用 渠道档。
    档 的 名称 为 名称。
    档 的 渠道表 为 做 字典。
    档 的 最近会话 为 0。
    档 的 最近请求 为 0。
    档 的 最近回证 为 0。
    档 的 最近意图 为 0。
    档 的 最近回复 为 「」。
    档 的 最近错误 为 「」。
    档 的 初始化完成 为 假。
    档 的 渠道数 为 0。
    档 的 请求数 为 0。
    档 的 回复数 为 0。
    档 的 命中数 为 0。
    档 的 失败数 为 0。
    档 的 轮询数 为 0。
    档 的 异步数 为 0。
    档 的 健康 为 「未初始化」。
    返还 档。
完。

智能渠道_登记 接受 档：
    做 追加 用 渠道账本、档。
    渠道建数 为 渠道建数 加 1。
    返还 档。
完。

智能渠道_创建 接受 名称：
    档 为 做 智能渠道_建档 用 名称。
    做 智能渠道_初始化 用 档。
    默认智能渠道档 为 档。
    返还 做 智能渠道_登记 用 档。
完。

智能渠道_获取默认档 接受 零参：
    返还 默认智能渠道档。
完。

智能渠道_初始化 接受 档：
    做 配置_初始化。
    做 消息总线_初始化。
    做 会话管理_初始化。
    做 任务队列_初始化。
    做 结果聚合_初始化。
    做 路由_初始化。
    做 上下文追踪_初始化。
    做 回复核心_初始化。
    做 意图识别_初始化。
    做 模板_初始化。
    做 知识库_初始化。
    做 网关框架_初始化。
    做 接口适配_初始化。
    做 终端_初始化。
    做 网聊_初始化。
    做 健康_初始化。
    做 指标_初始化。
    做 告警_初始化。
    做 ATCI_初始化。
    做 智能渠道_初始化指标。
    档 的 初始化完成 为 真。
    档 的 健康 为 「运行」。
    返还 档。
完。

智能渠道_初始化指标 接受 零参：
    做 指标_注册 用 「渠道请求」、「counter」。
    做 指标_注册 用 「渠道回复」、「counter」。
    做 指标_注册 用 「渠道失败」、「counter」。
    做 指标_注册 用 「渠道耗时」、「histogram」。
完。

智能渠道_注册终端 接受 档、名称：
    渠道 为 做 渠道_创建 用 名称、渠道类型_终端、终端_接收、终端_发送。
    做 渠道_启动 用 渠道。
    做 渠道注册表_注册 用 渠道。
    做 网关_注册渠道 用 名称、终端_接收、终端_发送。
    做 字典设 用 档 的 渠道表、名称、渠道。
    档 的 渠道数 为 档 的 渠道数 加 1。
    返还 渠道。
完。

智能渠道_注册网聊 接受 档、名称：
    渠道 为 做 渠道_创建 用 名称、渠道类型_WebChat、网聊_取接收、网聊_发送消息。
    做 渠道_启动 用 渠道。
    做 渠道注册表_注册 用 渠道。
    做 网关_注册渠道 用 名称、网聊_取接收、网聊_发送消息。
    做 字典设 用 档 的 渠道表、名称、渠道。
    档 的 渠道数 为 档 的 渠道数 加 1。
    返还 渠道。
完。

智能渠道_注册接口 接受 档、名称：
    渠道 为 做 渠道_创建 用 名称、渠道类型_接口、接口_接收、接口_发送。
    做 渠道_启动 用 渠道。
    做 渠道注册表_注册 用 渠道。
    做 网关_注册渠道 用 名称、接口_接收、接口_发送。
    做 字典设 用 档 的 渠道表、名称、渠道。
    档 的 渠道数 为 档 的 渠道数 加 1。
    返还 渠道。
完。

智能渠道_取渠道 接受 档、名称：
    返还 做 字典取 用 档 的 渠道表、名称。
完。

智能渠道_建请求 接受 渠道名、渠道号、用户ID、文本：
    请求 为 做 堆_分配 用 渠道请求。
    请求 的 渠道名 为 渠道名。
    请求 的 渠道号 为 渠道号。
    请求 的 用户ID 为 用户ID。
    请求 的 文本 为 文本。
    请求 的 请求ID 为 做 会话_生成ID。
    请求 的 会话ID 为 0。
    请求 的 意图 为 意图_未知。
    请求 的 实体 为 一组：（空）。
    请求 的 标签 为 一组：（空）。
    请求 的 优先级 为 优先级_普通。
    返还 请求。
完。

智能渠道_准备会话 接受 档、请求：
    会话 为 做 会话_查找或创建 用 请求 的 渠道号、请求 的 用户ID。
    请求 的 会话ID 为 会话 的 ID。
    档 的 最近会话 为 会话。
    返还 会话。
完。

智能渠道_识别意图 接受 档、请求：
    结果 为 做 意图_识别详细 用 请求 的 文本。
    请求 的 意图 为 结果 的 意图。
    请求 的 实体 为 结果 的 实体列表。
    档 的 最近意图 为 结果。
    返还 结果。
完。

智能渠道_推进会话 接受 档、会话、请求、意图结果：
    做 会话_添加消息 用 会话、「用户」、请求 的 文本。
    做 会话_设上下文 用 会话、「意图」、意图结果 的 意图。
    做 会话_设上下文 用 会话、「置信度」、意图结果 的 置信度。
    做 会话_合并上下文 用 会话、做 字典。
    做 会话_添加标签 用 会话、请求 的 渠道名。
    档 的 最近会话 为 会话。
    返还 会话。
完。

智能渠道_推进上下文 接受 档、会话、请求、意图结果：
    上下文 为 做 上下文_获取会话 用 会话 的 ID。
    若 上下文 等于 空 或者 上下文 等于 0：
        上下文 为 做 上下文_创建带ID 用 会话 的 ID、请求 的 用户ID。
    完。
    做 上下文_更新 用 上下文、意图结果 的 意图、请求 的 文本。
    档 的 最近会话 为 上下文。
    返还 上下文。
完。

智能渠道_路由 接受 档、请求、意图结果：
    路由结果 为 做 路由_分发 用 意图结果 的 意图、请求 的 会话ID、请求 的 文本。
    档 的 最近请求 为 请求。
    返还 路由结果。
完。

智能渠道_模板回退 接受 请求、意图结果：
    若 意图结果 的 意图 等于 意图_问候：
        返还 「你好，我已接入智能渠道。」。
    完。
    若 意图结果 的 意图 等于 意图_求助：
        返还 「我会先拆解问题，再给出可执行步骤。」。
    完。
    若 意图结果 的 意图 等于 意图_命令：
        返还 「命令已接收，我会回证执行结果。」。
    完。
    若 意图结果 的 意图 等于 意图_查询：
        返还 「我正在整理查询结果，请稍候。」。
    完。
    若 意图结果 的 意图 等于 意图_投诉：
        返还 「我已记录问题，会优先检查失败链路。」。
    完。
    返还 「我已收到消息，并将继续处理。」。
完。

智能渠道_构造结果 接受 文本、来源、置信度、耗时：
    返还 新建 聚合结果：文本 为 文本、来源 为 来源、置信度 为 置信度、耗时ms 为 耗时、质量分 为 做 结果_评估质量 用 文本。
完。

智能渠道_工具候选 接受 档、请求：
    若 请求 等于 空 或者 请求 的 文本 等于 空 或者 请求 的 文本 等于 「」：
        返还 空。
    完。
    若 请求 的 渠道名 不等于 「接口」：
        返还 空。
    完。
    若 做 工具_数量 等于 0：
        做 ATCI_初始化。
    完。
    工具名 为 做 AI_ATCI_推荐工具 用 请求 的 文本。
    若 工具名 等于 空 或者 工具名 等于 「」：
        返还 空。
    完。
    参数 为 一本字典：task 对应 请求 的 文本、query 对应 请求 的 文本、question 对应 请求 的 文本、command 对应 请求 的 文本、path 对应 请求 的 文本、content 对应 请求 的 文本。
    结果 为 做 工具_调用_带重试 用 工具名、参数。
    若 非 结果 的 成功：
        档 的 最近错误 为 结果 的 错误。
        返还 空。
    完。
    文本 为 「已通过工具链处理: 」 加 工具名。
    若 结果 的 耗时 大于 0：
        文本 为 文本 加 「，耗时 」 加 做 整数转字符串 用 结果 的 耗时 加 「ms」。
    完。
    返还 做 智能渠道_构造结果 用 文本、「工具调用」、62、结果 的 耗时。
完。

智能渠道_候选回复 接受 档、会话、请求、意图结果：
    候选 为 一组：（空）。
    回复 为 做 回复_生成 用 会话、请求 的 文本。
    若 回复 不等于 空 并且 回复 不等于 「」：
        做 追加 用 候选、做 智能渠道_构造结果 用 回复、来源_TNF、85、1。
    完。
    知识回复 为 做 知识库_查询 用 请求 的 文本。
    若 知识回复 不等于 空 并且 知识回复 不等于 「」：
        做 追加 用 候选、做 智能渠道_构造结果 用 知识回复、来源_知识库、70、1。
        档 的 命中数 为 档 的 命中数 加 1。
        渠道总命中 为 渠道总命中 加 1。
    完。
    工具回复 为 做 智能渠道_工具候选 用 档、请求。
    若 工具回复 不等于 空：
        做 追加 用 候选、工具回复。
    完。
    模板回复 为 做 智能渠道_模板回退 用 请求、意图结果。
    若 模板回复 不等于 空 并且 模板回复 不等于 「」：
        做 追加 用 候选、做 智能渠道_构造结果 用 模板回复、来源_模板、55、0。
    完。
    返还 候选。
完。

智能渠道_聚合回复 接受 档、候选：
    结果 为 做 结果_聚合 用 候选。
    若 结果 等于 空 或者 结果 等于 「」：
        结果 为 「智能渠道未产出有效回复。」。
    完。
    档 的 最近回复 为 结果。
    返还 结果。
完。

智能渠道_出站 接受 档、请求、文本：
    做 网关_发送到渠道 用 请求 的 渠道名、请求 的 用户ID、文本。
    做 消息总线_发布 用 「渠道.出站」、文本。
    返还 文本。
完。

智能渠道_建回证 接受 请求、意图结果、回复、来源、路由、错因：
    回证 为 做 堆_分配 用 渠道回证。
    回证 的 已收 为 真。
    回证 的 已成 为 错因 等于 「」。
    回证 的 会话ID 为 请求 的 会话ID。
    回证 的 请求ID 为 请求 的 请求ID。
    回证 的 意图 为 意图结果 的 意图。
    回证 的 置信度 为 意图结果 的 置信度。
    回证 的 回复 为 回复。
    回证 的 来源 为 来源。
    回证 的 路由 为 路由。
    回证 的 错因 为 错因。
    返还 回证。
完。

智能渠道_处理消息 接受 档、渠道名、渠道号、用户ID、文本：
    若 非 档 的 初始化完成：
        做 智能渠道_初始化 用 档。
    完。
    开始 为 做 当前时间_毫秒。
    请求 为 做 智能渠道_建请求 用 渠道名、渠道号、用户ID、文本。
    会话 为 做 智能渠道_准备会话 用 档、请求。
    做 消息总线_发布 用 「渠道.入站」、请求。
    做 消息存储_保存 用 渠道名、做 转字符串 用 用户ID、角色_用户、做 哈希_字符串 用 文本、做 字符串长度 用 文本。
    意图结果 为 做 智能渠道_识别意图 用 档、请求。
    做 智能渠道_推进会话 用 档、会话、请求、意图结果。
    做 智能渠道_推进上下文 用 档、会话、请求、意图结果。
    路由结果 为 做 智能渠道_路由 用 档、请求、意图结果。
    候选 为 做 智能渠道_候选回复 用 档、会话、请求、意图结果。
    回复 为 做 智能渠道_聚合回复 用 档、候选。
    做 智能渠道_出站 用 档、请求、回复。
    做 会话_添加消息 用 会话、「助手」、回复。
    做 消息存储_保存 用 渠道名、做 转字符串 用 用户ID、角色_助手、做 哈希_字符串 用 回复、做 字符串长度 用 回复。
    做 指标_递增 用 「渠道请求」。
    做 指标_递增 用 「渠道回复」。
    做 指标_记录直方图 用 「渠道耗时」、做 当前时间_毫秒 减 开始。
    档 的 请求数 为 档 的 请求数 加 1。
    档 的 回复数 为 档 的 回复数 加 1。
    渠道总请求 为 渠道总请求 加 1。
    渠道总回复 为 渠道总回复 加 1。
    回证 为 做 智能渠道_建回证 用 请求、意图结果、回复、来源_TNF、路由结果、「」。
    档 的 最近请求 为 请求。
    档 的 最近回证 为 回证。
    返还 回证。
完。

智能渠道_处理消息安全 接受 档、渠道名、渠道号、用户ID、文本：
    回证 为 做 智能渠道_处理消息 用 档、渠道名、渠道号、用户ID、文本。
    若 回证 等于 空 或者 回证 等于 0：
        档 的 失败数 为 档 的 失败数 加 1。
        渠道总失败 为 渠道总失败 加 1。
        做 指标_递增 用 「渠道失败」。
        档 的 最近错误 为 「处理失败」。
        返还 做 智能渠道_建回证 用 做 智能渠道_建请求 用 渠道名、渠道号、用户ID、文本、新建 意图结果：意图 为 意图_未知、置信度 为 0、次要意图 为 意图_未知、次要置信度 为 0、实体列表 为 一组：（空）、「」、来源_模板、「」、「处理失败」。
    完。
    返还 回证。
完。

智能渠道_任务入口 接受 参数：
    返还 做 智能渠道_处理消息 用 参数 的 第 0 项、参数 的 第 1 项、参数 的 第 2 项、参数 的 第 3 项、参数 的 第 4 项。
完。

智能渠道_提交异步 接受 档、渠道名、渠道号、用户ID、文本：
    参数 为 一组：档、渠道名、渠道号、用户ID、文本。
    档 的 异步数 为 档 的 异步数 加 1。
    做 任务_提交带回调 用 「智能渠道」、智能渠道_任务入口、参数、优先级_高、0、默认超时ms。
完。

智能渠道_轮询 接受 档：
    做 网关_轮询。
    做 任务_检查延迟。
    做 任务_处理批量 用 16。
    做 会话_定时检查。
    做 上下文_清理过期会话。
    做 健康_执行全部。
    做 告警_检查全部。
    档 的 轮询数 为 档 的 轮询数 加 1。
    返还 档 的 轮询数。
完。

智能渠道_暂停渠道 接受 档、名称：
    渠道 为 做 智能渠道_取渠道 用 档、名称。
    若 渠道 不等于 空 并且 渠道 不等于 0：
        做 渠道_暂停 用 渠道。
    完。
完。

智能渠道_恢复渠道 接受 档、名称：
    渠道 为 做 智能渠道_取渠道 用 档、名称。
    若 渠道 不等于 空 并且 渠道 不等于 0：
        做 渠道_恢复 用 渠道。
    完。
完。

智能渠道_关闭渠道 接受 档、名称：
    渠道 为 做 智能渠道_取渠道 用 档、名称。
    若 渠道 不等于 空 并且 渠道 不等于 0：
        做 渠道_停止 用 渠道。
    完。
完。

智能渠道_配置布尔 接受 键、值：
    做 配置_设布尔 用 键、值。
完。

智能渠道_配置整数 接受 键、值：
    做 配置_设整数 用 键、值。
完。

智能渠道_配置取值 接受 键：
    返还 做 配置_取值 用 键。
完。

智能渠道_状态串 接受 档：
    返还 做 字符串拼接 用 「渠道[」、做 字符串拼接 用 档 的 名称、做 字符串拼接 用 「] 请求=」、做 字符串拼接 用 做 整数转字符串 用 档 的 请求数、做 字符串拼接 用 「 回复=」、做 字符串拼接 用 做 整数转字符串 用 档 的 回复数、做 字符串拼接 用 「 失败=」、做 整数转字符串 用 档 的 失败数。
完。

智能渠道_摘要 接受 档：
    返还 新建 渠道摘要：名称 为 档 的 名称、渠道数 为 档 的 渠道数、请求数 为 档 的 请求数、回复数 为 档 的 回复数、命中数 为 档 的 命中数、失败数 为 档 的 失败数、异步数 为 档 的 异步数、健康 为 做 智能渠道_健康 用 档。
完。

智能渠道_健康 接受 档：
    若 非 档 的 初始化完成：
        返还 「待初始化」。
    完。
    若 做 健康_故障组件数 大于 0：
        返还 「故障」。
    完。
    若 档 的 失败数 大于 0：
        返还 「退化」。
    完。
    若 非 做 健康_是就绪：
        返还 「预热」。
    完。
    返还 「运行」。
完。

智能渠道_统计 接受 档：
    返还 一组：档 的 渠道数、档 的 请求数、档 的 回复数、档 的 命中数、档 的 失败数、档 的 轮询数、档 的 异步数、做 渠道注册表_数量、做 网关_渠道数。
完。

智能渠道_监控快照 接受 零参：
    返还 一组：做 健康_完整统计、做 指标_完整统计、做 告警_完整统计、做 任务队列_统计、做 结果聚合_统计、做 路由_完整统计、做 上下文追踪_统计、做 回复核心_统计、做 意图识别_统计、做 模板_获取统计摘要、做 知识库_统计、做 会话_定时检查、做 消息总线_统计、做 工具调用_统计。
完。

智能渠道_自检 接受 档：
    做 智能渠道_初始化 用 档。
    做 智能渠道_注册终端 用 档、「终端」。
    做 智能渠道_注册接口 用 档、「接口」。
    做 智能渠道_注册网聊 用 档、「网聊」。
    回证 为 做 智能渠道_处理消息 用 档、「终端」、渠道_终端、「用户」、「你好，帮我查看状态」。
    档 的 健康 为 做 智能渠道_健康 用 档。
    档 的 最近回证 为 回证。
    返还 档 的 健康。
完。

智能渠道_按名查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 渠道账本：
        若 渠道账本 的 第 i 项 的 名称 等于 名称：
            返还 渠道账本 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

智能渠道_最近回证 接受 档：
    返还 档 的 最近回证。
完。

智能渠道_最近请求 接受 档：
    返还 档 的 最近请求。
完。

智能渠道_最近回复 接受 档：
    返还 档 的 最近回复。
完。

智能渠道_最近错误 接受 档：
    返还 档 的 最近错误。
完。

智能渠道_账本数 接受 零参：
    返还 做 计数 用 渠道账本。
完。

本章 收束。
