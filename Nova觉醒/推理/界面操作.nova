《界面操作》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·执行·VSync帧调度》。

引用《..·中枢·内容可见性》。

操作_点击 为 1。

操作_双击 为 2。

操作_输入 为 3。

操作_滚动 为 4。

操作_拖拽 为 5。

操作_按键 为 6。

操作_截图 为 7。

最大操作历史 为 128。

界面操作记录 是 一种 事物，含 类型、目标哈希、横坐标、纵坐标、参数、时间戳、结果。

可变 操作历史 为 一组：（空）。

可变 历史数 为 0。

可变 总操作 为 0。

可变 成功操作 为 0。

可变 失败操作 为 0。

可变 超时操作 为 0。

界面_初始化 接受 零参：
    操作历史 为 一组：（空）。
    历史数 为 0。
完。

界面_记录操作 接受 类型、目标哈希、横、纵、参数、结果：
    总操作 为 总操作 加 1。
    若 结果 等于 0：
        成功操作 为 成功操作 加 1。
    否则：
        失败操作 为 失败操作 加 1。
    完。
    若 历史数 大于或等于 最大操作历史：
        可变 序号 为 0。
        只要 序号 小于 历史数 减 1：
            操作历史 的 第 序号 项 为 操作历史 的 第 序号 加 1 项。
            序号 为 序号 加 1。
        完。
        历史数 为 历史数 减 1。
    完。
    记录 为 新建 界面操作记录。
    记录 的 类型 为 类型。
    记录 的 目标哈希 为 目标哈希。
    记录 的 横坐标 为 横。
    记录 的 纵坐标 为 纵。
    记录 的 参数 为 参数。
    记录 的 时间戳 为 做 平台_获取高精度时间。
    记录 的 结果 为 结果。
    做 追加 用 操作历史、记录。
    历史数 为 历史数 加 1。
完。

界面_点击 接受 目标哈希、横、纵：
    结果 为 做 界面_模拟点击 用 横、纵。
    做 界面_记录操作 用 操作_点击、目标哈希、横、纵、0、结果。
    返还 结果。
完。

界面_双击 接受 目标哈希、横、纵：
    做 界面_模拟点击 用 横、纵。
    结果 为 做 界面_模拟点击 用 横、纵。
    做 界面_记录操作 用 操作_双击、目标哈希、横、纵、0、结果。
    返还 结果。
完。

界面_输入文本 接受 目标哈希、横、纵、文本哈希：
    做 界面_模拟点击 用 横、纵。
    结果 为 做 界面_模拟输入 用 文本哈希。
    做 界面_记录操作 用 操作_输入、目标哈希、横、纵、文本哈希、结果。
    返还 结果。
完。

界面_滚动 接受 横、纵、方向、距离：
    结果 为 做 界面_模拟滚动 用 横、纵、方向、距离。
    做 界面_记录操作 用 操作_滚动、0、横、纵、距离、结果。
    返还 结果。
完。

界面_拖拽 接受 起横、起纵、终横、终纵：
    结果 为 做 界面_模拟拖拽 用 起横、起纵、终横、终纵。
    做 界面_记录操作 用 操作_拖拽、0、起横、起纵、终横 左移 16 位或 终纵、结果。
    返还 结果。
完。

界面_按键 接受 键码：
    结果 为 做 界面_模拟按键 用 键码。
    做 界面_记录操作 用 操作_按键、0、0、0、键码、结果。
    返还 结果。
完。

界面_截图 接受 零参：
    帧 为 做 视觉设备_捕获帧 用 0。
    做 界面_记录操作 用 操作_截图、0、0、0、0、帧 不等于 0。
    返还 帧。
完。

界面_等待元素 接受 目标哈希、超时微秒：
    可变 已等待 为 0。
    可变 间隔 为 50000。
    最大间隔 为 500000。
    只要 已等待 小于 超时微秒：
        做 平台_睡眠微秒 用 间隔。
        已等待 为 已等待 加 间隔。
        若 间隔 小于 最大间隔：
            间隔 为 间隔 乘 2。
        完。
        若 间隔 大于 最大间隔：
            间隔 为 最大间隔。
        完。
    完。
    超时操作 为 超时操作 加 1。
    返还 错误_超时。
完。

界面_模拟点击 接受 横、纵：
    总操作 为 总操作 加 1。
    目标 为 做 命中测试 用 横、纵。
    若 目标 等于 空：
        失败操作 为 失败操作 加 1。
        返还 负 1。
    完。
    做 派发鼠标事件 用 目标、「mousedown」、横、纵。
    做 派发鼠标事件 用 目标、「mouseup」、横、纵。
    做 派发鼠标事件 用 目标、「click」、横、纵。
    成功操作 为 成功操作 加 1。
    返还 0。
完。

界面_模拟输入 接受 文本哈希：
    总操作 为 总操作 加 1。
    焦点 为 做 获取当前焦点。
    若 焦点 等于 空：
        失败操作 为 失败操作 加 1。
        返还 负 1。
    完。
    做 派发输入事件 用 焦点、文本哈希。
    成功操作 为 成功操作 加 1。
    返还 0。
完。

界面_模拟滚动 接受 横、纵、方向、距离：
    总操作 为 总操作 加 1。
    目标 为 做 命中测试 用 横、纵。
    若 目标 等于 空：
        失败操作 为 失败操作 加 1。
        返还 负 1。
    完。
    做 派发滚轮事件 用 目标、方向、距离。
    成功操作 为 成功操作 加 1。
    返还 0。
完。

界面_模拟拖拽 接受 起横、起纵、终横、终纵：
    总操作 为 总操作 加 1。
    起点 为 做 命中测试 用 起横、起纵。
    终点 为 做 命中测试 用 终横、终纵。
    若 起点 等于 空：
        失败操作 为 失败操作 加 1。
        返还 负 1。
    完。
    做 派发拖拽事件 用 起点、「dragstart」、起横、起纵。
    做 派发拖拽事件 用 起点、「drag」、终横、终纵。
    若 终点 不等于 空：
        做 派发拖拽事件 用 终点、「drop」、终横、终纵。
    完。
    做 派发拖拽事件 用 起点、「dragend」、终横、终纵。
    成功操作 为 成功操作 加 1。
    返还 0。
完。

界面_模拟按键 接受 键码：
    总操作 为 总操作 加 1。
    焦点 为 做 获取当前焦点。
    若 焦点 等于 空：
        失败操作 为 失败操作 加 1。
        返还 负 1。
    完。
    做 派发键盘事件 用 焦点、「keydown」、键码。
    做 派发键盘事件 用 焦点、「keypress」、键码。
    做 派发键盘事件 用 焦点、「keyup」、键码。
    成功操作 为 成功操作 加 1。
    返还 0。
完。

界面_成功率 接受 零参：
    若 总操作 等于 0：
        返还 0。
    完。
    返还 成功操作 乘 100 除以 总操作。
完。

界面_完整统计 接受 零参：
    返还 一组：总操作、成功操作、失败操作、超时操作、做 界面_成功率。
完。

本章 收束。
