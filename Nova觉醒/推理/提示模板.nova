《提示模板》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·自动扩容》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

引用《..·标准·原生字符串》。

模板_最大嵌套 为 5。

模板_最大变量替换 为 100。

模板_默认令牌限制 为 4096。

模板_字符令牌比 为 4。

模板条目 是 一种 事物，含 名称、文本、父模板、标签列表、令牌限制、创建时间。

可变 模板库 为 做 字典。

可变 模板_渲染次数 为 0。

可变 模板_渲染失败 为 0。

模板_注册 接受 名称、模板文本：
    条目 为 新建 模板条目：名称 为 名称、文本 为 模板文本、父模板 为 「」、标签列表 为 一组：（空）、令牌限制 为 模板_默认令牌限制、创建时间 为 做 当前时间_毫秒。
    做 字典设 用 模板库、名称、条目。
完。

模板_注册带父 接受 名称、模板文本、父名称：
    条目 为 新建 模板条目：名称 为 名称、文本 为 模板文本、父模板 为 父名称、标签列表 为 一组：（空）、令牌限制 为 模板_默认令牌限制、创建时间 为 做 当前时间_毫秒。
    做 字典设 用 模板库、名称、条目。
完。

模板_设标签 接受 名称、标签：
    条目 为 做 字典取 用 模板库、名称。
    若 条目 不等于 空：
        条目 的 标签列表 为 标签。
    完。
完。

模板_设令牌限制 接受 名称、限制：
    条目 为 做 字典取 用 模板库、名称。
    若 条目 不等于 空：
        条目 的 令牌限制 为 限制。
    完。
完。

模板_删除 接受 名称：
    做 字典删 用 模板库、名称。
完。

模板_渲染 接受 名称、变量表：
    模板_渲染次数 为 模板_渲染次数 加 1。
    条目 为 做 字典取 用 模板库、名称。
    若 条目 等于 空：
        模板_渲染失败 为 模板_渲染失败 加 1。
        返还 「」。
    完。
    可变 基础文本 为 「」。
    若 条目 的 父模板 不等于 「」 并且 条目 的 父模板 不等于 名称：
        父条目 为 做 字典取 用 模板库、条目 的 父模板。
        若 父条目 不等于 空：
            基础文本 为 父条目 的 文本 加 「\n」。
        完。
    完。
    基础文本 为 基础文本 加 条目 的 文本。
    可变 结果 为 做 模板_替换变量 用 基础文本、变量表。
    结果 为 做 模板_处理条件段 用 结果、变量表。
    结果 为 做 模板_处理循环段 用 结果、变量表。
    结果 为 做 模板_令牌截断 用 结果、条目 的 令牌限制。
    返还 结果。
完。

模板_替换变量 接受 模板、变量表：
    可变 结果 为 模板。
    可变 安全计数 为 0。
    只要 安全计数 小于 模板_最大变量替换：
        开始 为 做 字符串查找 用 结果、「{{」。
        若 开始 小于 0：
            跳出。
        完。
        结束 为 做 字符串查找 用 做 字符串子串 用 结果、开始 加 2、做 字符串长度 用 结果、「}}」。
        若 结束 小于 0：
            跳出。
        完。
        变量名 为 做 字符串子串 用 结果、开始 加 2、开始 加 2 加 结束。
        若 做 字符串包含 用 变量名、「|」：
            分隔位 为 做 字符串查找 用 变量名、「|」。
            真名 为 做 字符串子串 用 变量名、0、分隔位。
            默认值 为 做 字符串子串 用 变量名、分隔位 加 1、做 字符串长度 用 变量名。
            值 为 做 字典取 用 变量表、真名。
            若 值 等于 空：
                值 为 默认值。
            完。
            结果 为 做 字符串子串 用 结果、0、开始 加 做 转字符串 用 值 加 做 字符串子串 用 结果、开始 加 4 加 结束、做 字符串长度 用 结果。
        否则：
            值 为 做 字典取 用 变量表、变量名。
            若 值 等于 空：
                值 为 「」。
            完。
            结果 为 做 字符串子串 用 结果、0、开始 加 做 转字符串 用 值 加 做 字符串子串 用 结果、开始 加 4 加 结束、做 字符串长度 用 结果。
        完。
        安全计数 为 安全计数 加 1。
    完。
    返还 结果。
完。

模板_处理条件段 接受 文本、变量表：
    可变 结果 为 文本。
    可变 安全计数 为 0。
    只要 安全计数 小于 20：
        开始 为 做 字符串查找 用 结果、「{%如果 」。
        若 开始 小于 0：
            跳出。
        完。
        条件结束 为 做 字符串查找 用 做 字符串子串 用 结果、开始、做 字符串长度 用 结果、「%}」。
        若 条件结束 小于 0：
            跳出。
        完。
        块结束 为 做 字符串查找 用 做 字符串子串 用 结果、开始、做 字符串长度 用 结果、「{%结束%}」。
        若 块结束 小于 0：
            跳出。
        完。
        条件名 为 做 字符串子串 用 结果、开始 加 5、开始 加 条件结束。
        条件值 为 做 字典取 用 变量表、条件名。
        块内容 为 做 字符串子串 用 结果、开始 加 条件结束 加 2、开始 加 块结束。
        若 条件值 不等于 空 并且 条件值 不等于 「」 并且 条件值 不等于 假：
            结果 为 做 字符串子串 用 结果、0、开始 加 块内容 加 做 字符串子串 用 结果、开始 加 块结束 加 8、做 字符串长度 用 结果。
        否则：
            结果 为 做 字符串子串 用 结果、0、开始 加 做 字符串子串 用 结果、开始 加 块结束 加 8、做 字符串长度 用 结果。
        完。
        安全计数 为 安全计数 加 1。
    完。
    返还 结果。
完。

模板_处理循环段 接受 文本、变量表：
    可变 结果 为 文本。
    可变 安全计数 为 0。
    只要 安全计数 小于 10：
        开始 为 做 字符串查找 用 结果、「{%遍历 」。
        若 开始 小于 0：
            跳出。
        完。
        头结束 为 做 字符串查找 用 做 字符串子串 用 结果、开始、做 字符串长度 用 结果、「%}」。
        若 头结束 小于 0：
            跳出。
        完。
        块结束 为 做 字符串查找 用 做 字符串子串 用 结果、开始、做 字符串长度 用 结果、「{%结束遍历%}」。
        若 块结束 小于 0：
            跳出。
        完。
        列表名 为 做 字符串子串 用 结果、开始 加 5、开始 加 头结束。
        块模板 为 做 字符串子串 用 结果、开始 加 头结束 加 2、开始 加 块结束。
        列表值 为 做 字典取 用 变量表、列表名。
        可变 展开 为 「」。
        若 列表值 不等于 空 并且 做 是列表 用 列表值：
            可变 j 为 0。
            只要 j 小于 做 计数 用 列表值：
                项变量 为 做 字典。
                做 字典设 用 项变量、「项」、列表值 的 第 j 项。
                做 字典设 用 项变量、「索引」、j。
                展开 为 展开 加 做 模板_替换变量 用 块模板、项变量。
                j 为 j 加 1。
            完。
        完。
        结果 为 做 字符串子串 用 结果、0、开始 加 展开 加 做 字符串子串 用 结果、开始 加 块结束 加 12、做 字符串长度 用 结果。
        安全计数 为 安全计数 加 1。
    完。
    返还 结果。
完。

模板_估算令牌 接受 文本：
    若 文本 等于 空：
        返还 0。
    完。
    总长 为 做 字符串长度 用 文本。
    若 总长 等于 0：
        返还 0。
    完。
    扫描长 为 做 最小值 用 总长、200。
    可变 中文数 为 0。
    可变 i 为 0。
    只要 i 小于 扫描长：
        c 为 做 字符串字符码 用 文本、i。
        若 c 大于或等于 0x4E00 并且 c 小于或等于 0x9FFF：
            中文数 为 中文数 加 1。
        完。
        i 为 i 加 1。
    完。
    中文比 为 中文数 乘 1000 除以 扫描长。
    中文字数 为 总长 乘 中文比 除以 1000。
    非中文字数 为 总长 减 中文字数。
    返还 中文字数 加 非中文字数 除以 模板_字符令牌比。
完。

模板_令牌截断 接受 文本、令牌限制：
    估算 为 做 模板_估算令牌 用 文本。
    若 估算 小于或等于 令牌限制：
        返还 文本。
    完。
    最大字符 为 令牌限制 乘 模板_字符令牌比。
    返还 做 字符串子串 用 文本、0、最大字符 加 「\n...[已截断]」。
完。

模板_验证 接受 名称：
    条目 为 做 字典取 用 模板库、名称。
    若 条目 等于 空：
        返还 「模板不存在」。
    完。
    文本 为 条目 的 文本。
    可变 打开数 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本 减 1：
        若 做 字符串子串 用 文本、i、i 加 2 等于 「{{」：
            打开数 为 打开数 加 1。
        完。
        若 做 字符串子串 用 文本、i、i 加 2 等于 「}}」：
            打开数 为 打开数 减 1。
        完。
        i 为 i 加 1。
    完。
    若 打开数 不等于 0：
        返还 「变量标记不匹配」。
    完。
    返还 「有效」。
完。

模板_按标签查找 接受 标签：
    可变 结果 为 一组：（空）。
    返还 结果。
完。

模板_初始化 接受 零参：
    做 模板_注册 用 「系统」、「你是NovaOS的AI助手。纯Nova语言(中文关键字)操作系统。用中文回答。简洁直接。」。
    做 模板_注册 用 「代码分析」、「分析以下Nova代码:\n```\n{{代码}}\n```\n指出问题和改进建议。」。
    做 模板_注册 用 「代码生成」、「用Nova语言(关键字: 函数/定义/可变/如果/否则/当/返回/结构)编写:\n{{描述}}\n直接输出代码。」。
    做 模板_注册 用 「错误分析」、「程序出错:\n错误: {{错误信息}}\n上下文: {{上下文}}\n分析原因并给出修复。」。
    做 模板_注册 用 「文件编辑」、「文件: {{路径}}\n当前内容:\n{{内容}}\n请求: {{请求}}\n输出修改后的完整内容。」。
    做 模板_注册 用 「任务分解」、「任务: {{任务}}\n将其分解为可执行的子步骤(每步一行)。」。
    做 模板_注册 用 「终端命令」、「需要在NovaOS上执行: {{目标}}\n给出终端命令(一行一个)。注意安全。」。
    做 模板_注册 用 「摘要」、「将以下内容压缩为不超过{{长度}}字的摘要:\n{{内容}}」。
    做 模板_注册 用 「翻译」、「将以下文本翻译为{{目标语言}}:\n{{文本}}」。
    做 模板_注册 用 「对话摘要」、「[对话历史摘要]\n{{摘要}}\n\n基于以上背景继续对话。」。
    做 模板_注册 用 「安全审查」、「审查以下操作的安全性:\n操作: {{操作}}\n用户: {{用户}}\n权限: {{权限|未知}}\n评估风险等级(低/中/高/极高)并给出建议。」。
    做 模板_注册 用 「性能分析」、「分析以下代码的性能瓶颈:\n```\n{{代码}}\n```\n时间复杂度: {{复杂度|未知}}\n给出优化建议。」。
    做 模板_注册 用 「API文档」、「为以下函数生成API文档:\n```\n{{函数签名}}\n```\n包含: 参数说明、返回值、示例、注意事项。」。
    做 模板_设标签 用 「系统」、一组：「核心」、「系统」。
    做 模板_设标签 用 「代码分析」、一组：「开发」、「分析」。
    做 模板_设标签 用 「代码生成」、一组：「开发」、「生成」。
完。

模板_列表 接受 零参：
    返还 模板库。
完。

模板_存在 接受 名称：
    返还 做 字典取 用 模板库、名称 不等于 空。
完。

模板_统计 接受 零参：
    返还 一组：模板_渲染次数、模板_渲染失败。
完。

本章 收束。
