《模块元信息》

引用《..·中枢·系统调用表》。

引用《..·推理·通信渠道》。

引用《.·Promise辅助》。

引用《..·感知·兼容桥》。

引用《..·中枢·内核打印》。

引用《..·外联·TLS数据收发》。

引用《.·定点Q算术》。

引用《..·中枢·辅助存根》。

引用《.·小整数SMI判定》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

引用《.·AI鼠标事件处理》。

引用《.·栈帧创建》。

引用《..·中枢·内容可见性》。

引用《..·外联·HTTP请求构造》。

引用《..·外联·TLS创建》。

引用《..·外联·域名初始化》。

模块_获取ImportMeta 接受 Nova运行时实例、模块：
    若 模块 等于 0：
        返还 0。
    完。
    meta 为 做 Nova对象_创建 用 Nova运行时实例、0。
    做 Nova对象_设置属性 用 meta、「url」、做 PropDesc_创建_数据 用 模块 的 URL、假、真、假。
    做 Nova对象_设置属性 用 meta、「resolve」、做 PropDesc_创建_数据 用 做 JSNative_创建_ImportMetaResolve 用 Nova运行时实例、模块、假、真、假。
    返还 meta。
完。

URL_合并 接受 基、相对：
    返还 相对。
完。

字符串_结尾是 接受 str、后缀：
    若 str 等于 0 或者 后缀 等于 0：
        返还 假。
    完。
    slen 为 做 字节长度 用 str。
    plen 为 做 字节长度 用 后缀。
    若 plen 大于 slen：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 plen：
        若 做 字节取 用 str、slen 减 plen 加 i 不等于 做 字节取 用 后缀、i：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

网络_获取模块 接受 url：
    若 url 等于 0：
        返还 0。
    完。
    host 为 做 URL_提取主机名 用 url。
    ip 为 做 域名_解析 用 host。
    若 ip 等于 0：
        返还 0。
    完。
    fd 为 做 平台_tcp_连接 用 ip、443。
    若 fd 小于 0：
        返还 0。
    完。
    tls 为 做 安全_创建会话 用 fd、host。
    做 安全_握手 用 tls。
    req 为 做 HTTP_创建请求 用 「GET」、url。
    做 HTTP_设置头 用 req、「Accept」、「application/javascript」。
    resp 为 做 HTTP_发送并接收 用 tls、req。
    做 安全_关闭 用 tls。
    做 平台_close 用 fd。
    若 resp 等于 0 或者 resp 的 状态码 不等于 200：
        返还 0。
    完。
    返还 resp 的 body。
完。

Bytecode_执行模块 接受 i、bc、len、env：
    若 bc 等于 0 或者 i 等于 0：
        返还 Nova_未定义。
    完。
    frame 为 做 StackFrame_创建 用 FRAME_JS、bc、i 的 当前栈帧。
    frame 的 上下文 为 env。
    保存帧 为 i 的 当前栈帧。
    i 的 当前栈帧 为 frame。
    result 为 做 Bytecode_执行 用 i、frame。
    i 的 当前栈帧 为 保存帧。
    返还 result。
完。

Nova承诺_创建_内部 接受 i：
    p 为 做 堆_分配 用 Nova承诺。
    p 的 状态 为 0。
    p 的 值 为 Nova_未定义。
    p 的 then回调数 为 0。
    p 的 then回调 为 0。
    p 的 catch回调数 为 0。
    p 的 catch回调 为 0。
    返还 做 Nova值_创建_Object 用 i、p。
完。

Nova承诺_resolve_内部 接受 i、p、v：
    若 p 不等于 0 并且 p 的 状态 等于 0：
        p 的 状态 为 1。
        p 的 值 为 v。
        做 Nova承诺_触发回调 用 i、p。
    完。
完。

Nova承诺_reject_内部 接受 i、p、e：
    若 p 不等于 0 并且 p 的 状态 等于 0：
        p 的 状态 为 2。
        p 的 值 为 e。
        做 Nova承诺_触发回调 用 i、p。
    完。
完。

智能_JS模块System_AI初始化 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
完。

智能_JS模块System_AI热路径反馈 接受 函数哈希、执行时间：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
完。

智能_JS模块System_AI应该优化 接受 函数哈希、执行次数、平均时间：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 假。
    完。
    特征 为 做 Tensor_创建 用 1、8。
    做 Tensor_写 用 特征、0、0、若 执行次数 大于 2000 则 Q_ONE 否则 执行次数 乘 Q_ONE 除以 2000。
    做 Tensor_写 用 特征、0、1、若 平均时间 大于 1000 则 Q_ONE 否则 平均时间 乘 Q_ONE 除以 1000。
    分数 为 做 AI总线_注意力权重 用 bus、特征。
    做 Tensor_释放 用 特征。
    返还 分数 大于 512。
完。

可变 全局IMEI统计 为 新建 IMEI统计结构。

IMEI统计结构 是 一种 事物，含 模块加载次数、动态导入次数、解析调用次数、热更新次数、依赖图节点数、依赖图边数、AI预加载次数。

模块状态_未加载 为 0。

模块状态_加载中 为 1。

模块状态_已加载 为 2。

模块状态_已评估 为 3。

模块状态_错误 为 4。

模块注册表_最大 为 1024。

模块记录 是 一种 事物，含 URL、状态、字节码、字节码长度、命名空间、依赖表、依赖数、加载时间、执行时间、哈希值。

模块注册表 是 一种 事物，含 模块列表、模块数、依赖边、依赖边数。

可变 全局模块注册表 为 空。

模块注册表_初始化 接受 零参：
    reg 为 做 堆_分配 用 模块注册表。
    若 reg 等于 0：
        返还 空。
    完。
    reg 的 模块列表 为 做 堆_分配 用 模块注册表_最大 乘 8。
    reg 的 模块数 为 0。
    reg 的 依赖边 为 做 堆_分配 用 模块注册表_最大 乘 4 乘 8。
    reg 的 依赖边数 为 0。
    全局模块注册表 为 reg。
    返还 reg。
完。

模块注册表_查找 接受 reg、url：
    若 reg 等于 空 或者 url 等于 0：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 reg 的 模块数：
        mod 为 做 内存_读64 用 reg 的 模块列表 加 i 乘 8。
        若 mod 不等于 0 并且 做 字符串_相等 用 mod 的 URL、url：
            返还 mod。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

模块注册表_注册 接受 reg、url：
    若 reg 等于 空 或者 url 等于 0：
        返还 空。
    完。
    已有 为 做 模块注册表_查找 用 reg、url。
    若 已有 不等于 空：
        返还 已有。
    完。
    若 reg 的 模块数 大于或等于 模块注册表_最大：
        返还 空。
    完。
    mod 为 做 堆_分配 用 模块记录。
    若 mod 等于 0：
        返还 空。
    完。
    mod 的 URL 为 url。
    mod 的 状态 为 模块状态_未加载。
    mod 的 字节码 为 0。
    mod 的 命名空间 为 0。
    mod 的 依赖数 为 0。
    mod 的 加载时间 为 0。
    mod 的 哈希值 为 0。
    做 内存_写64 用 reg 的 模块列表 加 reg 的 模块数 乘 8、mod。
    reg 的 模块数 为 reg 的 模块数 加 1。
    全局IMEI统计 的 依赖图节点数 为 reg 的 模块数。
    返还 mod。
完。

ImportMeta_resolve 接受 基础URL、说明符：
    若 说明符 等于 0：
        返还 0。
    完。
    若 做 URL_是绝对 用 说明符：
        返还 说明符。
    完。
    若 做 URL_是相对 用 说明符：
        返还 做 URL_合并 用 基础URL、说明符。
    完。
    映射结果 为 做 ImportMap_解析 用 说明符。
    若 映射结果 不等于 0：
        返还 映射结果。
    完。
    若 做 是内置模块 用 说明符：
        返还 做 字符串_拼接 用 「nova:builtin/」、说明符。
    完。
    全局IMEI统计 的 解析调用次数 为 全局IMEI统计 的 解析调用次数 加 1。
    返还 0。
完。

URL_是绝对 接受 url：
    若 url 等于 0：
        返还 假。
    完。
    c0 为 做 字符取 用 url、0。
    c1 为 做 字符取 用 url、1。
    若 c0 等于 104 并且 c1 等于 116：
        返还 真。
    完。
    若 c0 等于 110 并且 c1 等于 111：
        返还 真。
    完。
    返还 假。
完。

URL_是相对 接受 url：
    若 url 等于 0：
        返还 假。
    完。
    c0 为 做 字符取 用 url、0。
    返还 c0 等于 46。
完。

IMPORT_MAP_最大条目 为 256。

ImportMap条目 是 一种 事物，含 说明符、映射URL。

ImportMap 是 一种 事物，含 条目列表、条目数、作用域。

可变 全局ImportMap 为 空。

ImportMap_初始化 接受 零参：
    map 为 做 堆_分配 用 ImportMap。
    若 map 等于 0：
        返还 空。
    完。
    map 的 条目列表 为 做 堆_分配 用 IMPORT_MAP_最大条目 乘 16。
    map 的 条目数 为 0。
    全局ImportMap 为 map。
    返还 map。
完。

ImportMap_添加 接受 说明符、目标URL：
    若 全局ImportMap 等于 空：
        做 ImportMap_初始化。
    完。
    若 全局ImportMap 的 条目数 大于或等于 IMPORT_MAP_最大条目：
        返还。
    完。
    idx 为 全局ImportMap 的 条目数。
    做 内存_写64 用 全局ImportMap 的 条目列表 加 idx 乘 16、说明符。
    做 内存_写64 用 全局ImportMap 的 条目列表 加 idx 乘 16 加 8、目标URL。
    全局ImportMap 的 条目数 为 全局ImportMap 的 条目数 加 1。
完。

ImportMap_解析 接受 说明符：
    若 全局ImportMap 等于 空 或者 说明符 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 全局ImportMap 的 条目数：
        key 为 做 内存_读64 用 全局ImportMap 的 条目列表 加 i 乘 16。
        若 做 字符串_相等 用 key、说明符：
            返还 做 内存_读64 用 全局ImportMap 的 条目列表 加 i 乘 16 加 8。
        完。
        若 做 字符串_前缀匹配 用 说明符、key：
            基 为 做 内存_读64 用 全局ImportMap 的 条目列表 加 i 乘 16 加 8。
            后缀 为 做 字符串_子串 用 说明符、做 字符串_长度 用 key、负 1。
            返还 做 字符串_拼接 用 基、后缀。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

模块_动态导入 接受 Nova运行时实例、当前模块、说明符：
    promise 为 做 Nova承诺_创建_内部 用 Nova运行时实例。
    基础 为 0。
    若 当前模块 不等于 0：
        基础 为 当前模块 的 URL。
    完。
    url 为 做 ImportMeta_resolve 用 基础、说明符。
    若 url 等于 0：
        做 Nova承诺_reject_内部 用 Nova运行时实例、promise、做 字符串_创建 用 「Module not found」。
        返还 promise。
    完。
    若 全局模块注册表 不等于 空：
        缓存 为 做 模块注册表_查找 用 全局模块注册表、url。
        若 缓存 不等于 空 并且 缓存 的 状态 等于 模块状态_已评估：
            做 Nova承诺_resolve_内部 用 Nova运行时实例、promise、缓存 的 命名空间。
            返还 promise。
        完。
    完。
    source 为 做 网络_获取模块 用 url。
    若 source 等于 0：
        做 Nova承诺_reject_内部 用 Nova运行时实例、promise、做 字符串_创建 用 「Fetch failed」。
        返还 promise。
    完。
    开始 为 做 平台_获取高精度时间。
    bc 为 做 编译_模块 用 Nova运行时实例、source、url。
    ns 为 做 Bytecode_执行模块 用 Nova运行时实例、bc、做 字符串_长度 用 source、0。
    结束 为 做 平台_获取高精度时间。
    若 全局模块注册表 不等于 空：
        mod 为 做 模块注册表_注册 用 全局模块注册表、url。
        若 mod 不等于 空：
            mod 的 状态 为 模块状态_已评估。
            mod 的 命名空间 为 ns。
            mod 的 执行时间 为 结束 减 开始。
        完。
    完。
    全局IMEI统计 的 动态导入次数 为 全局IMEI统计 的 动态导入次数 加 1。
    做 Nova承诺_resolve_内部 用 Nova运行时实例、promise、ns。
    返还 promise。
完。

HMR状态 是 一种 事物，含 接受回调、处理回调、已废弃。

模块_热更新_初始化 接受 mod：
    若 mod 等于 空：
        返还 空。
    完。
    hmr 为 做 堆_分配 用 HMR状态。
    若 hmr 等于 0：
        返还 空。
    完。
    hmr 的 接受回调 为 0。
    hmr 的 处理回调 为 0。
    hmr 的 已废弃 为 0。
    返还 hmr。
完。

模块_热更新_应用 接受 Nova运行时实例、url、新源码：
    若 全局模块注册表 等于 空：
        返还 假。
    完。
    mod 为 做 模块注册表_查找 用 全局模块注册表、url。
    若 mod 等于 空：
        返还 假。
    完。
    bc 为 做 编译_模块 用 Nova运行时实例、新源码、url。
    若 bc 等于 0：
        返还 假。
    完。
    旧bc 为 mod 的 字节码。
    mod 的 字节码 为 bc。
    ns 为 做 Bytecode_执行模块 用 Nova运行时实例、bc、做 字符串_长度 用 新源码、0。
    mod 的 命名空间 为 ns。
    若 旧bc 不等于 0：
        做 堆_释放 用 旧bc。
    完。
    全局IMEI统计 的 热更新次数 为 全局IMEI统计 的 热更新次数 加 1。
    做 内核打印 用 「[IMEI] 模块热更新: {}」、url。
    返还 真。
完。

ENV_最大条目 为 128。

环境变量表 是 一种 事物，含 键列表、值列表、条目数。

可变 全局环境变量表 为 空。

环境变量_初始化 接受 零参：
    env 为 做 堆_分配 用 环境变量表。
    若 env 等于 0：
        返还 空。
    完。
    env 的 键列表 为 做 堆_分配 用 ENV_最大条目 乘 8。
    env 的 值列表 为 做 堆_分配 用 ENV_最大条目 乘 8。
    env 的 条目数 为 0。
    做 环境变量_设置_内部 用 env、「MODE」、「production」。
    做 环境变量_设置_内部 用 env、「DEV」、「false」。
    做 环境变量_设置_内部 用 env、「PROD」、「true」。
    做 环境变量_设置_内部 用 env、「BASE_URL」、「/」。
    全局环境变量表 为 env。
    返还 env。
完。

环境变量_设置_内部 接受 env、键、值：
    若 env 等于 空 或者 env 的 条目数 大于或等于 ENV_最大条目：
        返还。
    完。
    idx 为 env 的 条目数。
    做 内存_写64 用 env 的 键列表 加 idx 乘 8、键。
    做 内存_写64 用 env 的 值列表 加 idx 乘 8、值。
    env 的 条目数 为 env 的 条目数 加 1。
完。

环境变量_获取 接受 键：
    若 全局环境变量表 等于 空：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 全局环境变量表 的 条目数：
        k 为 做 内存_读64 用 全局环境变量表 的 键列表 加 i 乘 8。
        若 做 字符串_相等 用 k、键：
            返还 做 内存_读64 用 全局环境变量表 的 值列表 加 i 乘 8。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

模块_获取完整ImportMeta 接受 Nova运行时实例、模块、包含Env：
    若 模块 等于 0：
        返还 0。
    完。
    meta 为 做 Nova对象_创建 用 Nova运行时实例、0。
    做 Nova对象_设置属性 用 meta、「url」、做 PropDesc_创建_数据 用 模块 的 URL、假、真、假。
    做 Nova对象_设置属性 用 meta、「resolve」、做 PropDesc_创建_数据 用 做 JSNative_创建_ImportMetaResolve 用 Nova运行时实例、模块、假、真、假。
    若 包含Env：
        envObj 为 做 Nova对象_创建 用 Nova运行时实例、0。
        若 全局环境变量表 不等于 空：
            可变 i 为 0。
            只要 i 小于 全局环境变量表 的 条目数：
                k 为 做 内存_读64 用 全局环境变量表 的 键列表 加 i 乘 8。
                v 为 做 内存_读64 用 全局环境变量表 的 值列表 加 i 乘 8。
                做 Nova对象_设置属性 用 envObj、k、做 PropDesc_创建_数据 用 v、假、真、假。
                i 为 i 加 1。
            完。
        完。
        做 Nova对象_设置属性 用 meta、「env」、做 PropDesc_创建_数据 用 envObj、假、真、假。
    完。
    返还 meta。
完。

IMEI_打印诊断 接受 零参：
    做 内核打印 用 「╔════════════════════════════════════════════╗」。
    做 内核打印 用 「║  IMEI 智能模块元信息引擎 诊断报告       ║」。
    做 内核打印 用 「╠════════════════════════════════════════════╣」。
    做 内核打印 用 「║ 模块加载次数:  {}」、全局IMEI统计 的 模块加载次数。
    做 内核打印 用 「║ 动态导入次数:  {}」、全局IMEI统计 的 动态导入次数。
    做 内核打印 用 「║ 解析调用:      {}」、全局IMEI统计 的 解析调用次数。
    做 内核打印 用 「║ 热更新:        {}」、全局IMEI统计 的 热更新次数。
    若 全局模块注册表 不等于 空：
        做 内核打印 用 「║ 依赖图节点:    {}」、全局模块注册表 的 模块数。
        做 内核打印 用 「║ 依赖图边:      {}」、全局模块注册表 的 依赖边数。
    完。
    做 内核打印 用 「╚════════════════════════════════════════════╝」。
完。

IMEI_重置统计 接受 零参：
    全局IMEI统计 的 模块加载次数 为 0。
    全局IMEI统计 的 动态导入次数 为 0。
    全局IMEI统计 的 解析调用次数 为 0。
    全局IMEI统计 的 热更新次数 为 0。
完。

IMEI_获取模块数 接受 零参：
    若 全局模块注册表 等于 空：
        返还 0。
    完。
    返还 全局模块注册表 的 模块数。
完。

IMEI_获取依赖边数 接受 零参：
    若 全局模块注册表 等于 空：
        返还 0。
    完。
    返还 全局模块注册表 的 依赖边数。
完。

IMEI_模块是否加载 接受 模块名：
    若 全局模块注册表 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 全局模块注册表 的 模块数：
        若 全局模块注册表 的 模块 的 第 i 项 的 名称 等于 模块名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

本章 收束。
