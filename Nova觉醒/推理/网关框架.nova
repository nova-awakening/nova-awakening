《网关框架》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·消息总线》。

引用《.·会话管理》。

网关_停止 为 0。

网关_运行 为 1。

网关_错误 为 2。

可变 网关_状态值 为 网关_停止。

可变 网关_渠道列表 为 一组：（空）。

可变 网关_总消息 为 0。

可变 网关_总回复 为 0。

可变 网关_总错误 为 0。

网关框架_初始化 接受 零参：
    做 消息总线_初始化。
    网关_状态值 为 网关_运行。
    网关_渠道列表 为 一组：（空）。
    网关_总消息 为 0。
    网关_总回复 为 0。
    网关_总错误 为 0。
    返还 成功。
完。

网关_注册渠道 接受 渠道名、接收函数、发送函数：
    做 追加 用 网关_渠道列表、一组：渠道名、接收函数、发送函数、真。
完。

网关_禁用渠道 接受 渠道名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 网关_渠道列表：
        若 网关_渠道列表 的 第 i 项 的 第 0 项 等于 渠道名：
            做 设置 用 网关_渠道列表 的 第 i 项、3、假。
        完。
        i 为 i 加 1。
    完。
完。

网关_处理消息 接受 渠道名、用户ID、消息文本：
    网关_总消息 为 网关_总消息 加 1。
    会话 为 做 会话_查找或创建 用 渠道名、用户ID。
    做 会话_添加消息 用 会话、「用户」、消息文本。
    做 消息总线_发布 用 「收到消息」、一组：渠道名、用户ID、消息文本、会话。
    回复 为 做 网关_生成回复 用 会话、消息文本。
    若 回复 不等于 「」 并且 回复 不等于 空：
        做 网关_发送到渠道 用 渠道名、用户ID、回复。
        做 会话_添加消息 用 会话、「AI」、回复。
        网关_总回复 为 网关_总回复 加 1。
    完。
    返还 回复。
完。

网关_生成回复 接受 会话、消息：
    返还 做 字符串拼接 用 「收到: 」、消息。
完。

网关_发送到渠道 接受 渠道名、用户ID、回复：
    可变 i 为 0。
    只要 i 小于 做 计数 用 网关_渠道列表：
        若 网关_渠道列表 的 第 i 项 的 第 0 项 等于 渠道名 并且 网关_渠道列表 的 第 i 项 的 第 3 项：
            发送函数 为 网关_渠道列表 的 第 i 项 的 第 2 项。
            若 发送函数 不等于 空：
                做 发送函数 用 用户ID、回复。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
    网关_总错误 为 网关_总错误 加 1。
完。

网关_轮询 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 网关_渠道列表：
        渠道 为 网关_渠道列表 的 第 i 项。
        若 渠道 的 第 3 项：
            接收函数 为 渠道 的 第 1 项。
            若 接收函数 不等于 空：
                消息 为 做 接收函数。
                若 消息 不等于 空：
                    做 网关_处理消息 用 渠道 的 第 0 项、消息 的 第 0 项、消息 的 第 1 项。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

网关框架_状态 接受 零参：
    若 网关_状态值 等于 网关_运行：
        返还 「运行」。
    完。
    若 网关_状态值 等于 网关_错误：
        返还 「错误」。
    完。
    返还 「停止」。
完。

网关_统计 接受 零参：
    返还 一组：网关_总消息、网关_总回复、网关_总错误、做 计数 用 网关_渠道列表。
完。

网关_渠道数 接受 零参：
    返还 做 计数 用 网关_渠道列表。
完。

本章 收束。
