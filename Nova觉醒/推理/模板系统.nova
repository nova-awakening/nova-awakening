《模板系统》

引用《..·中枢·类型定义》。

引用《..·中枢·哈希整数》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·高精度定时器hrtimer》。

引用《..·中枢·内核打印》。

最大模板 为 64。

最大变体 为 8。

最大变量 为 16。

最大缓存 为 128。

最大条件模板 为 32。

最大继承深度 为 4。

模板项 是 一种 事物，含 意图哈希、变体表、变体数、调用次数、优先级、父模板索引、已启用。

条件模板项 是 一种 事物，含 意图哈希、条件键、条件值、模板索引。

模板缓存项 是 一种 事物，含 键哈希、结果哈希、时间戳、命中次数。

模板统计 是 一种 事物，含 注册次数、查找次数、命中次数、未命中次数、替换次数、缓存命中次数、缓存未命中次数、条件匹配次数、继承解析次数、批量渲染次数、删除次数、变体添加次数。

可变 模板表 为 一组：（空）。

可变 模板数 为 0。

可变 随机种子 为 42。

可变 匹配总数 为 0。

可变 未匹配数 为 0。

可变 条件模板表 为 一组：（空）。

可变 条件模板数 为 0。

可变 模板缓存 为 一组：（空）。

可变 模板缓存数 为 0。

可变 全局_模板统计 为 空。

模板统计_初始化 接受 零参：
    若 全局_模板统计 等于 空：
        全局_模板统计 为 新建 模板统计。
        全局_模板统计 的 注册次数 为 0。
        全局_模板统计 的 查找次数 为 0。
        全局_模板统计 的 命中次数 为 0。
        全局_模板统计 的 未命中次数 为 0。
        全局_模板统计 的 替换次数 为 0。
        全局_模板统计 的 缓存命中次数 为 0。
        全局_模板统计 的 缓存未命中次数 为 0。
        全局_模板统计 的 条件匹配次数 为 0。
        全局_模板统计 的 继承解析次数 为 0。
        全局_模板统计 的 批量渲染次数 为 0。
        全局_模板统计 的 删除次数 为 0。
        全局_模板统计 的 变体添加次数 为 0。
    完。
完。

模板_初始化 接受 零参：
    模板表 为 一组：（空）。
    模板数 为 0。
    条件模板表 为 一组：（空）。
    条件模板数 为 0。
    模板缓存 为 一组：（空）。
    模板缓存数 为 0。
完。

模板_注册 接受 意图、变体哈希表、变体数量：
    做 模板统计_初始化。
    全局_模板统计 的 注册次数 为 全局_模板统计 的 注册次数 加 1。
    若 模板数 大于或等于 最大模板：
        返还 负 1。
    完。
    项 为 新建 模板项。
    项 的 意图哈希 为 做 哈希_字符串 用 意图。
    项 的 变体表 为 做 堆_分配 用 最大变体 乘 8。
    项 的 变体数 为 0。
    项 的 优先级 为 0。
    项 的 父模板索引 为 负 1。
    项 的 已启用 为 真。
    可变 序号 为 0。
    只要 序号 小于 变体数量 并且 序号 小于 最大变体：
        做 内存_写64 用 项 的 变体表 加 序号 乘 8、做 内存_读64 用 变体哈希表 加 序号 乘 8。
        项 的 变体数 为 项 的 变体数 加 1。
        序号 为 序号 加 1。
    完。
    项 的 调用次数 为 0。
    做 追加 用 模板表、项。
    索引 为 模板数。
    模板数 为 模板数 加 1。
    返还 索引。
完。

模板_注册简单 接受 意图：
    做 模板统计_初始化。
    全局_模板统计 的 注册次数 为 全局_模板统计 的 注册次数 加 1。
    若 模板数 大于或等于 最大模板：
        返还 负 1。
    完。
    项 为 新建 模板项。
    项 的 意图哈希 为 做 哈希_字符串 用 意图。
    项 的 变体表 为 做 堆_分配 用 最大变体 乘 8。
    项 的 变体数 为 0。
    项 的 优先级 为 0。
    项 的 父模板索引 为 负 1。
    项 的 已启用 为 真。
    项 的 调用次数 为 0。
    做 追加 用 模板表、项。
    索引 为 模板数。
    模板数 为 模板数 加 1。
    返还 索引。
完。

模板_删除 接受 意图：
    做 模板统计_初始化。
    全局_模板统计 的 删除次数 为 全局_模板统计 的 删除次数 加 1。
    目标 为 做 哈希_字符串 用 意图。
    可变 序号 为 0。
    只要 序号 小于 模板数：
        若 模板表 的 第 序号 项 的 意图哈希 等于 目标：
            模板表 的 第 序号 项 的 已启用 为 假。
            返还 真。
        完。
        序号 为 序号 加 1。
    完。
    返还 假。
完。

模板_设置优先级 接受 意图、优先级：
    目标 为 做 哈希_字符串 用 意图。
    可变 序号 为 0。
    只要 序号 小于 模板数：
        若 模板表 的 第 序号 项 的 意图哈希 等于 目标：
            模板表 的 第 序号 项 的 优先级 为 优先级。
            返还。
        完。
        序号 为 序号 加 1。
    完。
完。

模板_设置继承 接受 子意图、父意图：
    做 模板统计_初始化。
    全局_模板统计 的 继承解析次数 为 全局_模板统计 的 继承解析次数 加 1。
    子哈希 为 做 哈希_字符串 用 子意图。
    父哈希 为 做 哈希_字符串 用 父意图。
    可变 子索引 为 负 1。
    可变 父索引 为 负 1。
    可变 序号 为 0。
    只要 序号 小于 模板数：
        若 模板表 的 第 序号 项 的 意图哈希 等于 子哈希：
            子索引 为 序号。
        完。
        若 模板表 的 第 序号 项 的 意图哈希 等于 父哈希：
            父索引 为 序号。
        完。
        序号 为 序号 加 1。
    完。
    若 子索引 大于或等于 0 并且 父索引 大于或等于 0：
        模板表 的 第 子索引 项 的 父模板索引 为 父索引。
    完。
完。

模板_添加变体 接受 意图、变体哈希：
    做 模板统计_初始化。
    全局_模板统计 的 变体添加次数 为 全局_模板统计 的 变体添加次数 加 1。
    目标 为 做 哈希_字符串 用 意图。
    可变 序号 为 0。
    只要 序号 小于 模板数：
        若 模板表 的 第 序号 项 的 意图哈希 等于 目标：
            若 模板表 的 第 序号 项 的 变体数 大于或等于 最大变体：
                返还。
            完。
            做 内存_写64 用 模板表 的 第 序号 项 的 变体表 加 模板表 的 第 序号 项 的 变体数 乘 8、变体哈希。
            模板表 的 第 序号 项 的 变体数 为 模板表 的 第 序号 项 的 变体数 加 1。
            返还。
        完。
        序号 为 序号 加 1。
    完。
完。

模板_注册条件 接受 意图、条件键、条件值、模板索引：
    若 条件模板数 大于或等于 最大条件模板：
        返还 负 1。
    完。
    项 为 新建 条件模板项。
    项 的 意图哈希 为 做 哈希_字符串 用 意图。
    项 的 条件键 为 条件键。
    项 的 条件值 为 条件值。
    项 的 模板索引 为 模板索引。
    做 追加 用 条件模板表、项。
    条件模板数 为 条件模板数 加 1。
    返还 条件模板数 减 1。
完。

模板_条件查找 接受 意图、上下文键、上下文值：
    做 模板统计_初始化。
    全局_模板统计 的 条件匹配次数 为 全局_模板统计 的 条件匹配次数 加 1。
    目标 为 做 哈希_字符串 用 意图。
    可变 序号 为 0。
    只要 序号 小于 条件模板数：
        若 条件模板表 的 第 序号 项 的 意图哈希 等于 目标：
            若 条件模板表 的 第 序号 项 的 条件键 等于 上下文键 并且 条件模板表 的 第 序号 项 的 条件值 等于 上下文值：
                idx 为 条件模板表 的 第 序号 项 的 模板索引。
                若 idx 大于或等于 0 并且 idx 小于 模板数 并且 模板表 的 第 idx 项 的 已启用：
                    返还 做 模板_从索引选择变体 用 idx。
                完。
            完。
        完。
        序号 为 序号 加 1。
    完。
    返还 做 模板_查找 用 意图。
完。

模板_从索引选择变体 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 模板数：
        返还 0。
    完。
    模板表 的 第 索引 项 的 调用次数 为 模板表 的 第 索引 项 的 调用次数 加 1。
    若 模板表 的 第 索引 项 的 变体数 等于 0：
        若 模板表 的 第 索引 项 的 父模板索引 大于或等于 0：
            返还 做 模板_从索引选择变体 用 模板表 的 第 索引 项 的 父模板索引。
        完。
        返还 0。
    完。
    随机索引 为 做 模板_伪随机 取余于 模板表 的 第 索引 项 的 变体数。
    返还 做 内存_读64 用 模板表 的 第 索引 项 的 变体表 加 随机索引 乘 8。
完。

模板_伪随机 接受 零参：
    随机种子 为 随机种子 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    返还 随机种子。
完。

模板_缓存查找 接受 键哈希：
    可变 i 为 0。
    只要 i 小于 模板缓存数：
        若 模板缓存 的 第 i 项 的 键哈希 等于 键哈希：
            模板缓存 的 第 i 项 的 命中次数 为 模板缓存 的 第 i 项 的 命中次数 加 1。
            做 模板统计_初始化。
            全局_模板统计 的 缓存命中次数 为 全局_模板统计 的 缓存命中次数 加 1。
            返还 模板缓存 的 第 i 项 的 结果哈希。
        完。
        i 为 i 加 1。
    完。
    做 模板统计_初始化。
    全局_模板统计 的 缓存未命中次数 为 全局_模板统计 的 缓存未命中次数 加 1。
    返还 负 1。
完。

模板_缓存存储 接受 键哈希、结果哈希：
    若 模板缓存数 大于或等于 最大缓存：
        可变 最旧索引 为 0。
        可变 最旧时间 为 模板缓存 的 第 0 项 的 时间戳。
        可变 i 为 1。
        只要 i 小于 模板缓存数：
            若 模板缓存 的 第 i 项 的 时间戳 小于 最旧时间：
                最旧时间 为 模板缓存 的 第 i 项 的 时间戳。
                最旧索引 为 i。
            完。
            i 为 i 加 1。
        完。
        模板缓存 的 第 最旧索引 项 的 键哈希 为 键哈希。
        模板缓存 的 第 最旧索引 项 的 结果哈希 为 结果哈希。
        模板缓存 的 第 最旧索引 项 的 时间戳 为 做 获取滴答。
        模板缓存 的 第 最旧索引 项 的 命中次数 为 0。
        返还。
    完。
    项 为 新建 模板缓存项。
    项 的 键哈希 为 键哈希。
    项 的 结果哈希 为 结果哈希。
    项 的 时间戳 为 做 获取滴答。
    项 的 命中次数 为 0。
    做 追加 用 模板缓存、项。
    模板缓存数 为 模板缓存数 加 1。
完。

模板_清空缓存 接受 零参：
    模板缓存 为 一组：（空）。
    模板缓存数 为 0。
完。

模板_查找 接受 意图：
    做 模板统计_初始化。
    全局_模板统计 的 查找次数 为 全局_模板统计 的 查找次数 加 1。
    匹配总数 为 匹配总数 加 1。
    目标 为 做 哈希_字符串 用 意图。
    缓存结果 为 做 模板_缓存查找 用 目标。
    若 缓存结果 不等于 负 1：
        全局_模板统计 的 命中次数 为 全局_模板统计 的 命中次数 加 1。
        返还 缓存结果。
    完。
    可变 最佳索引 为 负 1。
    可变 最佳优先级 为 负 1。
    可变 序号 为 0。
    只要 序号 小于 模板数：
        若 模板表 的 第 序号 项 的 意图哈希 等于 目标 并且 模板表 的 第 序号 项 的 已启用：
            若 模板表 的 第 序号 项 的 优先级 大于 最佳优先级：
                最佳优先级 为 模板表 的 第 序号 项 的 优先级。
                最佳索引 为 序号。
            完。
        完。
        序号 为 序号 加 1。
    完。
    若 最佳索引 大于或等于 0：
        全局_模板统计 的 命中次数 为 全局_模板统计 的 命中次数 加 1。
        结果 为 做 模板_从索引选择变体 用 最佳索引。
        做 模板_缓存存储 用 目标、结果。
        返还 结果。
    完。
    未匹配数 为 未匹配数 加 1。
    全局_模板统计 的 未命中次数 为 全局_模板统计 的 未命中次数 加 1。
    返还 0。
完。

模板_替换变量 接受 模板哈希、变量键表、变量值表、变量数：
    做 模板统计_初始化。
    全局_模板统计 的 替换次数 为 全局_模板统计 的 替换次数 加 1。
    若 变量数 小于或等于 0 或者 变量数 大于 最大变量：
        返还 模板哈希。
    完。
    可变 结果 为 模板哈希。
    可变 i 为 0。
    只要 i 小于 变量数：
        结果 为 结果 位异或 变量键表 的 第 i 项 乘 31 加 变量值表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模板_批量渲染 接受 意图列表、数量：
    做 模板统计_初始化。
    全局_模板统计 的 批量渲染次数 为 全局_模板统计 的 批量渲染次数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 数量：
        r 为 做 模板_查找 用 意图列表 的 第 i 项。
        做 追加 用 结果、r。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模板_获取变体数 接受 意图：
    目标 为 做 哈希_字符串 用 意图。
    可变 序号 为 0。
    只要 序号 小于 模板数：
        若 模板表 的 第 序号 项 的 意图哈希 等于 目标：
            返还 模板表 的 第 序号 项 的 变体数。
        完。
        序号 为 序号 加 1。
    完。
    返还 0。
完。

模板_获取调用次数 接受 意图：
    目标 为 做 哈希_字符串 用 意图。
    可变 序号 为 0。
    只要 序号 小于 模板数：
        若 模板表 的 第 序号 项 的 意图哈希 等于 目标：
            返还 模板表 的 第 序号 项 的 调用次数。
        完。
        序号 为 序号 加 1。
    完。
    返还 0。
完。

模板_匹配率 接受 零参：
    若 匹配总数 等于 0：
        返还 0。
    完。
    返还 (匹配总数 减 未匹配数) 乘 100 除以 匹配总数。
完。

模板_活跃数 接受 零参：
    可变 活跃 为 0。
    可变 i 为 0。
    只要 i 小于 模板数：
        若 模板表 的 第 i 项 的 已启用：
            活跃 为 活跃 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 活跃。
完。

模板_完整统计 接受 零参：
    返还 一组：模板数、匹配总数、未匹配数、做 模板_匹配率。
完。

模板_打印统计 接受 零参：
    做 模板统计_初始化。
    做 打印行 用 「=== 模板系统统计 ===」。
    做 打印行 用 「模板总数: 」 加 做 整数转字符串 用 模板数。
    做 打印行 用 「活跃模板: 」 加 做 整数转字符串 用 做 模板_活跃数。
    做 打印行 用 「注册: 」 加 做 整数转字符串 用 全局_模板统计 的 注册次数。
    做 打印行 用 「查找: 」 加 做 整数转字符串 用 全局_模板统计 的 查找次数。
    做 打印行 用 「命中: 」 加 做 整数转字符串 用 全局_模板统计 的 命中次数。
    做 打印行 用 「未命中: 」 加 做 整数转字符串 用 全局_模板统计 的 未命中次数。
    做 打印行 用 「替换: 」 加 做 整数转字符串 用 全局_模板统计 的 替换次数。
    做 打印行 用 「缓存命中: 」 加 做 整数转字符串 用 全局_模板统计 的 缓存命中次数。
    做 打印行 用 「缓存未命中: 」 加 做 整数转字符串 用 全局_模板统计 的 缓存未命中次数。
    做 打印行 用 「条件匹配: 」 加 做 整数转字符串 用 全局_模板统计 的 条件匹配次数。
    做 打印行 用 「继承解析: 」 加 做 整数转字符串 用 全局_模板统计 的 继承解析次数。
    做 打印行 用 「批量渲染: 」 加 做 整数转字符串 用 全局_模板统计 的 批量渲染次数。
    做 打印行 用 「删除: 」 加 做 整数转字符串 用 全局_模板统计 的 删除次数。
    做 打印行 用 「变体添加: 」 加 做 整数转字符串 用 全局_模板统计 的 变体添加次数。
    做 打印行 用 「缓存条目: 」 加 做 整数转字符串 用 模板缓存数。
    做 打印行 用 「条件模板: 」 加 做 整数转字符串 用 条件模板数。
    做 打印行 用 「匹配率: 」 加 做 整数转字符串 用 做 模板_匹配率 加 「%」。
完。

模板_继承解析 接受 子模板哈希、父模板哈希：
    做 模板统计_初始化。
    全局_模板统计 的 继承解析次数 为 全局_模板统计 的 继承解析次数 加 1。
    若 子模板哈希 不等于 0：
        返还 子模板哈希。
    完。
    返还 父模板哈希。
完。

模板_条件匹配 接受 意图、上下文：
    做 模板统计_初始化。
    全局_模板统计 的 条件匹配次数 为 全局_模板统计 的 条件匹配次数 加 1。
    若 上下文 等于 空 或者 上下文 等于 「」：
        返还 做 模板_查找 用 意图。
    完。
    目标 为 做 哈希_字符串 用 意图。
    可变 i 为 0。
    只要 i 小于 条件模板数：
        ct 为 条件模板表 的 第 i 项。
        若 ct 的 意图哈希 等于 目标：
            若 ct 的 条件键 等于 上下文 或者 ct 的 条件值 等于 上下文：
                idx 为 ct 的 模板索引。
                若 idx 大于或等于 0 并且 idx 小于 模板数 并且 模板表 的 第 idx 项 的 已启用：
                    返还 做 模板_从索引选择变体 用 idx。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 做 模板_查找 用 意图。
完。

模板_缓存命中率 接受 零参：
    做 模板统计_初始化。
    总 为 全局_模板统计 的 缓存命中次数 加 全局_模板统计 的 缓存未命中次数。
    若 总 等于 0：
        返还 0。
    完。
    返还 全局_模板统计 的 缓存命中次数 乘 100 除以 总。
完。

模板_重置统计 接受 零参：
    做 模板统计_初始化。
    全局_模板统计 的 注册次数 为 0。
    全局_模板统计 的 查找次数 为 0。
    全局_模板统计 的 命中次数 为 0。
    全局_模板统计 的 未命中次数 为 0。
    全局_模板统计 的 替换次数 为 0。
    全局_模板统计 的 缓存命中次数 为 0。
    全局_模板统计 的 缓存未命中次数 为 0。
    全局_模板统计 的 条件匹配次数 为 0。
    全局_模板统计 的 继承解析次数 为 0。
    全局_模板统计 的 批量渲染次数 为 0。
    全局_模板统计 的 删除次数 为 0。
    全局_模板统计 的 变体添加次数 为 0。
完。

模板_获取统计摘要 接受 零参：
    做 模板统计_初始化。
    返还 一组：模板数、做 模板_活跃数、全局_模板统计 的 查找次数、全局_模板统计 的 命中次数、全局_模板统计 的 未命中次数、做 模板_匹配率、做 模板_缓存命中率、模板缓存数、条件模板数。
完。

模板_启用 接受 意图：
    目标 为 做 哈希_字符串 用 意图。
    可变 i 为 0。
    只要 i 小于 模板数：
        若 模板表 的 第 i 项 的 意图哈希 等于 目标：
            模板表 的 第 i 项 的 已启用 为 真。
        完。
        i 为 i 加 1。
    完。
完。

模板_禁用 接受 意图：
    目标 为 做 哈希_字符串 用 意图。
    可变 i 为 0。
    只要 i 小于 模板数：
        若 模板表 的 第 i 项 的 意图哈希 等于 目标：
            模板表 的 第 i 项 的 已启用 为 假。
        完。
        i 为 i 加 1。
    完。
完。

可变 AI_模板系统_热点函数 为 一组：（空）。

AI_模板系统_JIT候选 接受 函数名、调用次数：
    若 调用次数 大于 100：
        做 追加 用 AI_模板系统_热点函数、函数名。
        返还 真。
    完。
    返还 假。
完。

AI_模板系统_内联建议 接受 函数体大小、调用次数：
    若 函数体大小 小于 30 并且 调用次数 大于 50：
        返还 「内联」。
    完。
    返还 「不内联」。
完。

本章 收束。
