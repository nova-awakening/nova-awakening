《总线悬浮球》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·AI总线初始化》。

AI悬浮球_命中测试 接受 参数1、参数2、参数3：
    返还 做 AI总线_分发 用 「AI悬浮球_命中测试」、一组：参数1、参数2、参数3。
完。

AI总线_发送事件 接受 参数1、参数2、参数3：
    返还 做 AI总线_分发 用 「AI总线_发送事件」、一组：参数1、参数2、参数3。
完。

AI控制层就绪 接受 参数1：
    返还 做 AI总线_分发 用 「AI控制层就绪」、一组：参数1。
完。

AI配置面板优先处理 接受 参数1：
    返还 做 AI总线_分发 用 「AI配置面板优先处理」、一组：参数1。
完。

AI控制层渲染 接受 参数1：
    返还 做 AI总线_分发 用 「AI控制层渲染」、一组：参数1。
完。

AI配置面板渲染 接受 参数1：
    返还 做 AI总线_分发 用 「AI配置面板渲染」、一组：参数1。
完。

AI设置 接受 参数1：
    返还 做 AI总线_分发 用 「AI设置」、一组：参数1。
完。

AI拦截 接受 参数1：
    返还 做 AI总线_分发 用 「AI拦截」、一组：参数1。
完。

AI命令 接受 参数1：
    返还 做 AI总线_分发 用 「AI命令」、一组：参数1。
完。

AI搜索快捷键 接受 参数1：
    返还 做 AI总线_分发 用 「AI搜索快捷键」、一组：参数1。
完。

AI助手面板 接受 参数1：
    返还 做 AI总线_分发 用 「AI助手面板」、一组：参数1。
完。

AI搜索框 接受 参数1：
    返还 做 AI总线_分发 用 「AI搜索框」、一组：参数1。
完。

AI状态指示器 接受 参数1：
    返还 做 AI总线_分发 用 「AI状态指示器」、一组：参数1。
完。

智能_构建点击上下文 接受 参数1、参数2：
    返还 做 AI总线_分发 用 「智能_构建点击上下文」、一组：参数1、参数2。
完。

AI全局查找 接受 参数1：
    返还 做 AI总线_分发 用 「AI全局查找」、一组：参数1。
完。

AI事件注入 接受 参数1：
    返还 做 AI总线_分发 用 「AI事件注入」、一组：参数1。
完。

AI注入点击 接受 参数1：
    返还 做 AI总线_分发 用 「AI注入点击」、一组：参数1。
完。

AI注入文字 接受 参数1、参数2：
    返还 做 AI总线_分发 用 「AI注入文字」、一组：参数1、参数2。
完。

AI注入滚动 接受 参数1、参数2、参数3：
    返还 做 AI总线_分发 用 「AI注入滚动」、一组：参数1、参数2、参数3。
完。

AI操作接口 接受 参数1、参数2：
    返还 做 AI总线_分发 用 「AI操作接口」、一组：参数1、参数2。
完。

AI点击元素 接受 参数1：
    返还 做 AI总线_分发 用 「AI点击元素」、一组：参数1。
完。

AI读取 接受 参数1：
    返还 做 AI总线_分发 用 「AI读取」、一组：参数1。
完。

AI获取信息 接受 参数1：
    返还 做 AI总线_分发 用 「AI获取信息」、一组：参数1。
完。

AI总线_是否启用 接受 零参：
    返还 做 AI总线_分发 用 「AI总线_是否启用」、一组：（空）。
完。

AI增强 接受 参数1：
    返还 做 AI总线_分发 用 「AI增强」、一组：参数1。
完。

AI安全评估 接受 参数1：
    返还 做 AI总线_分发 用 「AI安全评估」、一组：参数1。
完。

AI模型评估 接受 参数1：
    返还 做 AI总线_分发 用 「AI模型评估」、一组：参数1。
完。

AI翻译 接受 参数1：
    返还 做 AI总线_分发 用 「AI翻译」、一组：参数1。
完。

AI朗读 接受 零参：
    返还 做 AI总线_分发 用 「AI朗读」、一组：（空）。
完。

AI侧边栏 接受 参数1：
    返还 做 AI总线_分发 用 「AI侧边栏」、一组：参数1。
完。

AI聚焦到某个元素 接受 参数1：
    返还 做 AI总线_分发 用 「AI聚焦到某个元素」、一组：参数1。
完。

AI生成UI 接受 参数1：
    返还 做 AI总线_分发 用 「AI生成UI」、一组：参数1。
完。

AI服务 接受 参数1：
    返还 做 AI总线_分发 用 「AI服务」、一组：参数1。
完。

AI基础权重 接受 参数1：
    返还 做 AI总线_分发 用 「AI基础权重」、一组：参数1。
完。

AI子系统初始化完成 接受 参数1：
    返还 做 AI总线_分发 用 「AI子系统初始化完成」、一组：参数1。
完。

AI经络 接受 参数1：
    返还 做 AI总线_分发 用 「AI经络」、一组：参数1。
完。

AI视觉识别 接受 参数1：
    返还 做 AI总线_分发 用 「AI视觉识别」、一组：参数1。
完。

AI优化 接受 零参：
    返还 做 AI总线_分发 用 「AI优化」、一组：（空）。
完。

AI安全_检查内容 接受 参数1：
    返还 做 AI总线_分发 用 「AI安全_检查内容」、一组：参数1。
完。

AI安全_评估域名 接受 参数1：
    返还 做 AI总线_分发 用 「AI安全_评估域名」、一组：参数1。
完。

AI对话模式 接受 参数1：
    返还 做 AI总线_分发 用 「AI对话模式」、一组：参数1。
完。

AI记忆_获取浏览历史 接受 参数1、参数2：
    返还 做 AI总线_分发 用 「AI记忆_获取浏览历史」、一组：参数1、参数2。
完。

AI原生融合层 接受 参数1：
    返还 做 AI总线_分发 用 「AI原生融合层」、一组：参数1。
完。

AI加载 接受 参数1：
    返还 做 AI总线_分发 用 「AI加载」、一组：参数1。
完。

AI推理引擎_初始化 接受 零参：
    返还 做 AI总线_分发 用 「AI推理引擎_初始化」、一组：（空）。
完。

AI语义分析_初始化 接受 零参：
    返还 做 AI总线_分发 用 「AI语义分析_初始化」、一组：（空）。
完。

AI推理tick 接受 参数1：
    返还 做 AI总线_分发 用 「AI推理tick」、一组：参数1。
完。

AI学习反馈 接受 参数1：
    返还 做 AI总线_分发 用 「AI学习反馈」、一组：参数1。
完。

AI总线_反馈 接受 参数1：
    返还 做 AI总线_分发 用 「AI总线_反馈」、一组：参数1。
完。

AI启用 接受 零参：
    返还 做 AI总线_分发 用 「AI启用」、一组：（空）。
完。

AI提供者 接受 参数1：
    返还 做 AI总线_分发 用 「AI提供者」、一组：参数1。
完。

AI权重快照 接受 参数1：
    返还 做 AI总线_分发 用 「AI权重快照」、一组：参数1。
完。

AI质量监控 接受 参数1：
    返还 做 AI总线_分发 用 「AI质量监控」、一组：参数1。
完。

AI钩子必须O 接受 参数1：
    返还 做 AI总线_分发 用 「AI钩子必须O」、一组：参数1。
完。

本章 收束。
