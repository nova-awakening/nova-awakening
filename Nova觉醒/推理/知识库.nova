《知识库》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·自动扩容》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·标准·原生列表》。

引用《..·标准·原生字符串》。

引用《..·中枢·高精度定时器hrtimer》。

最大知识条目 为 1024。

最大搜索结果 为 10。

最小匹配分数 为 1。

知识条目结构 是 一种 事物，含 ID、关键词列表、回答、分类、权重、访问次数、创建时间、更新时间、标签列表。

可变 知识条目 为 一组：（空）。

可变 知识分类表 为 做 字典。

可变 知识ID计数 为 0。

可变 知识_总查询 为 0。

可变 知识_总命中 为 0。

可变 知识_总未命中 为 0。

知识库_初始化 接受 零参：
    知识条目 为 一组：（空）。
    知识分类表 为 做 字典。
    知识ID计数 为 0。
    知识_总查询 为 0。
    知识_总命中 为 0。
    知识_总未命中 为 0。
    做 知识库_添加分类 用 「NovaOS」、「系统核心」。
    做 知识库_添加分类 用 「语言」、「编程语言」。
    做 知识库_添加分类 用 「AI」、「人工智能」。
    做 知识库_添加分类 用 「工具」、「开发工具」。
    做 知识库_添加带分类 用 一组：「NovaOS」、「操作系统」、「什么是」、「NovaOS是用Nova语言从零构建的AI原生操作系统。」、「NovaOS」、10。
    做 知识库_添加带分类 用 一组：「Nova语言」、「编程语言」、「Nova是NovaOS的原生编程语言，支持中文标识符、内联汇编和AI集成。」、「语言」、10。
    做 知识库_添加带分类 用 一组：「龙编译器」、「编译」、「龙编译器是Nova语言的编译器，可生成x86-64原生代码。」、「工具」、8。
    做 知识库_添加带分类 用 一组：「AI」、「人工智能」、「TNF」、「NovaOS内嵌TNF三态神经网络，支持内核级AI推理。」、「AI」、10。
    做 知识库_添加带分类 用 一组：「内核」、「架构」、「设计」、「NovaOS内核采用混合架构，结合微内核安全性和宏内核性能。」、「NovaOS」、8。
    做 知识库_添加带分类 用 一组：「文件系统」、「ext4」、「FAT32」、「NovaOS支持ext4和FAT32文件系统，提供VFS虚拟文件系统抽象层。」、「NovaOS」、7。
    做 知识库_添加带分类 用 一组：「网络」、「TCP」、「IP」、「协议栈」、「NovaOS实现了完整的TCP/IP协议栈，支持以太网和环回接口。」、「NovaOS」、7。
    做 知识库_添加带分类 用 一组：「脚本引擎」、「JavaScript」、「JS」、「NovaOS内置JavaScript脚本引擎，支持ES6+语法和Web API绑定。」、「语言」、8。
    返还 成功。
完。

知识库_添加 接受 关键词列表、回答：
    做 知识库_添加带分类 用 关键词列表、回答、「通用」、5。
完。

知识库_添加分类 接受 分类名、描述：
    做 字典设 用 知识分类表、分类名、描述。
完。

知识库_添加带分类 接受 关键词列表、回答、分类、权重：
    若 做 计数 用 知识条目 大于或等于 最大知识条目：
        返还 负 1。
    完。
    知识ID计数 为 知识ID计数 加 1。
    条目 为 新建 知识条目结构：ID 为 知识ID计数、关键词列表 为 关键词列表、回答 为 回答、分类 为 分类、权重 为 权重、访问次数 为 0、创建时间 为 做 获取滴答、更新时间 为 做 获取滴答、标签列表 为 一组：（空）。
    做 追加 用 知识条目、条目。
    返还 知识ID计数。
完。

知识库_添加标签 接受 条目ID、标签：
    可变 i 为 0。
    只要 i 小于 做 计数 用 知识条目：
        若 知识条目 的 第 i 项 的 ID 等于 条目ID：
            做 追加 用 知识条目 的 第 i 项 的 标签列表、标签。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

知识库_更新回答 接受 条目ID、新回答：
    可变 i 为 0。
    只要 i 小于 做 计数 用 知识条目：
        若 知识条目 的 第 i 项 的 ID 等于 条目ID：
            知识条目 的 第 i 项 的 回答 为 新回答。
            知识条目 的 第 i 项 的 更新时间 为 做 获取滴答。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

知识库_删除 接受 条目ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 知识条目：
        若 知识条目 的 第 i 项 的 ID 等于 条目ID：
            知识条目 为 做 列表_删除 用 知识条目、i。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

知识库_计算匹配分 接受 问题、条目：
    关键词 为 条目 的 关键词列表。
    可变 分数 为 0。
    可变 j 为 0。
    只要 j 小于 做 计数 用 关键词：
        若 做 字符串包含 用 问题、关键词 的 第 j 项：
            分数 为 分数 加 条目 的 权重。
        否则：
            部分匹配 为 做 知识库_模糊匹配 用 问题、关键词 的 第 j 项。
            若 部分匹配 大于 50：
                分数 为 分数 加 条目 的 权重 乘 部分匹配 除以 100。
            完。
        完。
        j 为 j 加 1。
    完。
    返还 分数。
完。

知识库_模糊匹配 接受 文本、关键词：
    若 做 字符串长度 用 关键词 等于 0：
        返还 0。
    完。
    可变 匹配字符 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 关键词：
        若 做 字符串包含 用 文本、做 字符串子串 用 关键词、i、i 加 1：
            匹配字符 为 匹配字符 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 匹配字符 乘 100 除以 做 字符串长度 用 关键词。
完。

知识库_查询 接受 问题：
    知识_总查询 为 知识_总查询 加 1。
    可变 最佳匹配 为 空。
    可变 最高分 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 知识条目：
        分数 为 做 知识库_计算匹配分 用 问题、知识条目 的 第 i 项。
        若 分数 大于 最高分：
            最高分 为 分数。
            最佳匹配 为 知识条目 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 最高分 大于或等于 最小匹配分数 并且 最佳匹配 不等于 空：
        最佳匹配 的 访问次数 为 最佳匹配 的 访问次数 加 1。
        知识_总命中 为 知识_总命中 加 1。
        返还 最佳匹配 的 回答。
    完。
    知识_总未命中 为 知识_总未命中 加 1。
    返还 空。
完。

知识库_多结果查询 接受 问题、最大结果：
    知识_总查询 为 知识_总查询 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 知识条目：
        分数 为 做 知识库_计算匹配分 用 问题、知识条目 的 第 i 项。
        若 分数 大于或等于 最小匹配分数：
            做 追加 用 结果、一组：知识条目 的 第 i 项 的 回答、分数、知识条目 的 第 i 项 的 分类。
            知识条目 的 第 i 项 的 访问次数 为 知识条目 的 第 i 项 的 访问次数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 结果 大于 0：
        知识_总命中 为 知识_总命中 加 1。
    否则：
        知识_总未命中 为 知识_总未命中 加 1。
    完。
    返还 结果。
完。

知识库_按分类查询 接受 分类名：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 知识条目：
        若 知识条目 的 第 i 项 的 分类 等于 分类名：
            做 追加 用 结果、知识条目 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

知识库_热门条目 接受 数量：
    可变 排序 为 知识条目。
    可变 i 为 0。
    只要 i 小于 做 计数 用 排序 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 排序：
            若 排序 的 第 j 项 的 访问次数 大于 排序 的 第 i 项 的 访问次数：
                临时 为 排序 的 第 i 项。
                排序 的 第 i 项 为 排序 的 第 j 项。
                排序 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 数量 大于 做 计数 用 排序：
        数量 为 做 计数 用 排序。
    完。
    返还 做 列表_切片 用 排序、0、数量。
完。

知识库_条目数 接受 零参：
    返还 做 计数 用 知识条目。
完。

知识库_分类数 接受 零参：
    返还 做 字典_大小 用 知识分类表。
完。

知识库_统计 接受 零参：
    返还 一组：做 计数 用 知识条目、知识_总查询、知识_总命中、知识_总未命中。
完。

知识库_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 做 计数 用 知识条目 大于 10000：
        做 追加 用 问题、「知识条目膨胀」。
    完。
    若 知识ID计数 大于 100000：
        做 追加 用 问题、「知识ID计数异常增长」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

知识库_综合报告 接受 零参：
    做 打印 用 「[知识库] ═══ 综合报告 ═══」。
    做 打印 用 「  知识ID计数: 」 加 知识ID计数。
    做 打印 用 「  知识_总查询: 」 加 知识_总查询。
    做 打印 用 「  知识_总命中: 」 加 知识_总命中。
    做 打印 用 「  知识_总未命中: 」 加 知识_总未命中。
    做 打印 用 「  健康: 」 加 若 做 知识库_健康评估 则 「正常」 否则 「异常」。
完。

知识库_全局重置 接受 零参：
    知识ID计数 为 0。
    知识_总查询 为 0。
    知识_总命中 为 0。
    知识_总未命中 为 0。
    做 打印 用 「[知识库] 统计已重置」。
    返还 0。
完。

本章 收束。
