《网络操作》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《.·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《.·定点Q算术》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·辅助存根》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

引用《..·执行·探测》。

引用《..·标准·文件》。

引用《..·外联·WebSocket_IO缓冲》。

引用《..·标准·原生路径》。

引用《..·感知·历史与位置接口》。

智能体网络_默认超时毫秒 为 30000。

智能体网络_默认连接超时毫秒 为 8000。

智能体网络_默认读取超时毫秒 为 20000。

智能体网络_默认重试次数 为 3。

智能体网络_默认重试基准延迟 为 250。

智能体网络_最大重试次数 为 8。

智能体网络_最大重定向次数 为 5。

智能体网络_连接池最大连接数 为 128。

智能体网络_每主机最大连接 为 8。

智能体网络_连接空闲回收秒 为 60。

智能体网络_请求头最大数 为 64。

智能体网络_默认端口_HTTP 为 80。

智能体网络_默认端口_HTTPS 为 443。

智能体网络_默认端口_WS 为 80。

智能体网络_默认端口_WSS 为 443。

智能体网络_流块大小 为 16384。

智能体网络_下载缓冲大小 为 65536。

智能体网络_上传缓冲大小 为 65536。

智能体网络_DNS缓存上限 为 512。

智能体网络_DNS缓存TTL秒 为 300。

智能体网络_白名单最大项 为 1024。

智能体网络_黑名单最大项 为 1024。

智能体网络_熔断窗口 为 30。

智能体网络_熔断失败阈值 为 0.5。

智能体网络_熔断最小样本 为 20。

智能体网络_熔断半开探测数 为 3。

智能体网络_限流窗口毫秒 为 1000。

智能体网络_限流默认QPS 为 100。

智能体网络_统计环大小 为 2048。

NAPT_特征维度 为 14。

NAPT_历史窗口 为 64。

NAPT_置信阈值 为 0.70。

NAPT_退避衰减 为 0.85。

NAPT_并发上限预测阈值 为 0.75。

协议_HTTP 为 0。

协议_HTTPS 为 1。

协议_WS 为 2。

协议_WSS 为 3。

方法_GET 为 「GET」。

方法_POST 为 「POST」。

方法_PUT 为 「PUT」。

方法_DELETE 为 「DELETE」。

方法_PATCH 为 「PATCH」。

方法_HEAD 为 「HEAD」。

方法_OPTIONS 为 「OPTIONS」。

熔断_关闭 为 0。

熔断_打开 为 1。

熔断_半开 为 2。

URL解析结果 是 一种 事物，含 协议类型、协议字符串、主机、端口、路径、查询串、片段、用户名、密码、原始URL、归一化URL。

网络头 是 一种 事物，含 键、值。

网络请求配置 是 一种 事物，含 方法、URL、头数组、头数量、请求体、连接超时毫秒、读取超时毫秒、总超时毫秒、最大重试、允许重定向、最大重定向、TLS校验、TLS主机名校验、TLS证书固定指纹、使用连接池、使用压缩、使用代理、代理主机、代理端口、代理用户名、代理密码、追踪ID。

网络响应 是 一种 事物，含 成功、状态码、状态文本、头数组、头数量、数据、数据长度、错误、耗时毫秒、重试次数、来自缓存、最终URL。

网络连接 是 一种 事物，含 连接ID、套接字、TLS会话、主机、端口、协议类型、已连接、可复用、正在使用、创建时间戳、最后活跃时间戳、失败次数、RTT毫秒。

主机连接池 是 一种 事物，含 主机、端口、协议类型、连接数组、连接数量、最大连接、熔断状态、熔断开启时间、窗口请求数、窗口失败数、半开探测剩余、限流窗口起始、限流计数、限流QPS。

DNS缓存项 是 一种 事物，含 主机、IP、过期时间戳。

TLS验证结果 是 一种 事物，含 有效、错误、证书摘要、证书颁发者、证书主题、到期时间。

流式响应句柄 是 一种 事物，含 请求ID、连接、已结束、状态码、头数组、头数量、已接收字节、回调_数据块、回调_结束、回调_错误。

网络统计 是 一种 事物，含 总请求数、总成功数、总失败数、总重试数、总超时数、总重定向数、总TLS握手数、总TLS失败数、总连接创建数、总连接复用数、总连接回收数、总下载字节、总上传字节、平均耗时毫秒、最近错误。

可变 全局主机池列表 为 做 数组_创建 用 256、0。

可变 全局主机池数量 为 0。

可变 全局DNS缓存 为 做 数组_创建 用 智能体网络_DNS缓存上限、0。

可变 全局DNS缓存数量 为 0。

可变 全局白名单 为 做 数组_创建 用 智能体网络_白名单最大项、0。

可变 全局白名单数量 为 0。

可变 全局黑名单 为 做 数组_创建 用 智能体网络_黑名单最大项、0。

可变 全局黑名单数量 为 0。

可变 全局网络统计 为 新建 网络统计。

可变 全局请求序号 为 1。

可变 全局连接序号 为 1。

可变 NAPT_耗时历史 为 做 数组_创建 用 NAPT_历史窗口、0.0。

可变 NAPT_失败历史 为 做 数组_创建 用 NAPT_历史窗口、0.0。

可变 NAPT_吞吐历史 为 做 数组_创建 用 NAPT_历史窗口、0.0。

可变 NAPT_历史写指针 为 0。

智能体网络_初始化 接受 零参：
    全局网络统计 为 新建 网络统计。
    全局网络统计 的 总请求数 为 0。
    全局网络统计 的 总成功数 为 0。
    全局网络统计 的 总失败数 为 0。
    全局网络统计 的 总重试数 为 0。
    全局网络统计 的 总超时数 为 0。
    全局网络统计 的 总重定向数 为 0。
    全局网络统计 的 总TLS握手数 为 0。
    全局网络统计 的 总TLS失败数 为 0。
    全局网络统计 的 总连接创建数 为 0。
    全局网络统计 的 总连接复用数 为 0。
    全局网络统计 的 总连接回收数 为 0。
    全局网络统计 的 总下载字节 为 0。
    全局网络统计 的 总上传字节 为 0。
    全局网络统计 的 平均耗时毫秒 为 0。
    全局网络统计 的 最近错误 为 「」。
    做 内核打印 用 「[网络操作] 智能体网络子系统已初始化」。
完。

智能体网络_添加白名单 接受 域名：
    若 全局白名单数量 大于或等于 智能体网络_白名单最大项：
        返还 假。
    完。
    全局白名单 的 第 全局白名单数量 项 为 做 字符串_复制 用 域名。
    全局白名单数量 为 全局白名单数量 加 1。
    返还 真。
完。

智能体网络_添加黑名单 接受 域名：
    若 全局黑名单数量 大于或等于 智能体网络_黑名单最大项：
        返还 假。
    完。
    全局黑名单 的 第 全局黑名单数量 项 为 做 字符串_复制 用 域名。
    全局黑名单数量 为 全局黑名单数量 加 1。
    返还 真。
完。

智能体网络_清空白名单 接受 零参：
    可变 i 为 0。
    只要 i 小于 全局白名单数量：
        若 全局白名单 的 第 i 项 不等于 0：
            做 字符串_释放 用 全局白名单 的 第 i 项。
        完。
        全局白名单 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    全局白名单数量 为 0。
完。

智能体网络_清空黑名单 接受 零参：
    可变 i 为 0。
    只要 i 小于 全局黑名单数量：
        若 全局黑名单 的 第 i 项 不等于 0：
            做 字符串_释放 用 全局黑名单 的 第 i 项。
        完。
        全局黑名单 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    全局黑名单数量 为 0。
完。

智能体网络_策略允许主机 接受 主机：
    可变 i 为 0。
    只要 i 小于 全局黑名单数量：
        若 做 字符串_域名匹配 用 主机、全局黑名单 的 第 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    若 全局白名单数量 等于 0：
        返还 真。
    完。
    i 为 0。
    只要 i 小于 全局白名单数量：
        若 做 字符串_域名匹配 用 主机、全局白名单 的 第 i 项：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

智能体网络_解析URL 接受 url：
    若 url 等于 空 或者 做 字符串长度 用 url 等于 0：
        返还 空。
    完。
    结果 为 新建 URL解析结果。
    结果 的 原始URL 为 做 字符串_复制 用 url。
    协议分隔 为 做 字符串查找 用 url、「://」。
    若 协议分隔 小于 0：
        返还 空。
    完。
    协议串 为 做 字符串小写 用 做 字符串子串 用 url、0、协议分隔。
    若 协议串 等于 「http」：
        结果 的 协议类型 为 协议_HTTP。
        结果 的 端口 为 智能体网络_默认端口_HTTP。
    否则若 协议串 等于 「https」：
        结果 的 协议类型 为 协议_HTTPS。
        结果 的 端口 为 智能体网络_默认端口_HTTPS。
    否则若 协议串 等于 「ws」：
        结果 的 协议类型 为 协议_WS。
        结果 的 端口 为 智能体网络_默认端口_WS。
    否则若 协议串 等于 「wss」：
        结果 的 协议类型 为 协议_WSS。
        结果 的 端口 为 智能体网络_默认端口_WSS。
    否则：
        返还 空。
    完。
    结果 的 协议字符串 为 协议串。
    authority开始 为 协议分隔 加 3。
    路径起点 为 做 字符串查找_从 用 url、「/」、authority开始。
    authority 为 「」。
    若 路径起点 小于 0：
        authority 为 做 字符串子串 用 url、authority开始、做 字符串长度 用 url。
        结果 的 路径 为 「/」。
    否则：
        authority 为 做 字符串子串 用 url、authority开始、路径起点。
        结果 的 路径 为 做 字符串子串 用 url、路径起点、做 字符串长度 用 url。
    完。
    at位 为 做 字符串查找 用 authority、「@」。
    主机端口 为 authority。
    若 at位 大于或等于 0：
        userinfo 为 做 字符串子串 用 authority、0、at位。
        主机端口 为 做 字符串子串 用 authority、at位 加 1、做 字符串长度 用 authority。
        冒号位_user 为 做 字符串查找 用 userinfo、「:」。
        若 冒号位_user 大于或等于 0：
            结果 的 用户名 为 做 字符串URL解码 用 做 字符串子串 用 userinfo、0、冒号位_user。
            结果 的 密码 为 做 字符串URL解码 用 做 字符串子串 用 userinfo、冒号位_user 加 1、做 字符串长度 用 userinfo。
        否则：
            结果 的 用户名 为 做 字符串URL解码 用 userinfo。
            结果 的 密码 为 「」。
        完。
    否则：
        结果 的 用户名 为 「」。
        结果 的 密码 为 「」。
    完。
    若 做 字符串开始于 用 主机端口、「[」：
        右括号 为 做 字符串查找 用 主机端口、「]」。
        若 右括号 小于 0：
            返还 空。
        完。
        结果 的 主机 为 做 字符串子串 用 主机端口、1、右括号。
        若 右括号 加 1 小于 做 字符串长度 用 主机端口 并且 做 字符串子串 用 主机端口、右括号 加 1、右括号 加 2 等于 「:」：
            端口串 为 做 字符串子串 用 主机端口、右括号 加 2、做 字符串长度 用 主机端口。
            结果 的 端口 为 做 字符串转整数 用 端口串。
        完。
    否则：
        冒号位 为 做 字符串查找 用 主机端口、「:」。
        若 冒号位 大于 0：
            结果 的 主机 为 做 字符串小写 用 做 字符串子串 用 主机端口、0、冒号位。
            端口串 为 做 字符串子串 用 主机端口、冒号位 加 1、做 字符串长度 用 主机端口。
            结果 的 端口 为 做 字符串转整数 用 端口串。
        否则：
            结果 的 主机 为 做 字符串小写 用 主机端口。
        完。
    完。
    问号位 为 做 字符串查找 用 结果 的 路径、「?」。
    井号位 为 做 字符串查找 用 结果 的 路径、「#」。
    结果 的 查询串 为 「」。
    结果 的 片段 为 「」。
    若 问号位 大于或等于 0：
        若 井号位 大于或等于 0 并且 井号位 大于 问号位：
            结果 的 查询串 为 做 字符串子串 用 结果 的 路径、问号位 加 1、井号位。
            结果 的 片段 为 做 字符串子串 用 结果 的 路径、井号位 加 1、做 字符串长度 用 结果 的 路径。
            结果 的 路径 为 做 字符串子串 用 结果 的 路径、0、问号位。
        否则：
            结果 的 查询串 为 做 字符串子串 用 结果 的 路径、问号位 加 1、做 字符串长度 用 结果 的 路径。
            结果 的 路径 为 做 字符串子串 用 结果 的 路径、0、问号位。
        完。
    否则若 井号位 大于或等于 0：
        结果 的 片段 为 做 字符串子串 用 结果 的 路径、井号位 加 1、做 字符串长度 用 结果 的 路径。
        结果 的 路径 为 做 字符串子串 用 结果 的 路径、0、井号位。
    完。
    若 结果 的 路径 等于 「」：
        结果 的 路径 为 「/」。
    完。
    结果 的 路径 为 做 路径_规范化 用 结果 的 路径。
    结果 的 归一化URL 为 做 智能体网络_URL归一化 用 结果。
    返还 结果。
完。

智能体网络_URL归一化 接受 解析：
    url 为 解析 的 协议字符串 加 「://」 加 解析 的 主机。
    若 解析 的 协议类型 等于 协议_HTTP 并且 解析 的 端口 不等于 80 或者 解析 的 协议类型 等于 协议_HTTPS 并且 解析 的 端口 不等于 443 或者 解析 的 协议类型 等于 协议_WS 并且 解析 的 端口 不等于 80 或者 解析 的 协议类型 等于 协议_WSS 并且 解析 的 端口 不等于 443：
        url 为 url 加 「:」 加 做 整数转字符串 用 解析 的 端口。
    完。
    url 为 url 加 解析 的 路径。
    若 解析 的 查询串 不等于 「」：
        url 为 url 加 「?」 加 解析 的 查询串。
    完。
    若 解析 的 片段 不等于 「」：
        url 为 url 加 「#」 加 解析 的 片段。
    完。
    返还 url。
完。

网络_DNS缓存_查询 接受 主机：
    当前时间 为 做 系统_获取时间戳。
    可变 i 为 0。
    只要 i 小于 全局DNS缓存数量：
        项 为 全局DNS缓存 的 第 i 项。
        若 项 不等于 0 并且 项 的 主机 等于 主机：
            若 项 的 过期时间戳 大于 当前时间：
                返还 项 的 IP。
            否则：
                全局DNS缓存数量 为 全局DNS缓存数量 减 1。
                全局DNS缓存 的 第 i 项 为 全局DNS缓存 的 第 全局DNS缓存数量 项。
                全局DNS缓存 的 第 全局DNS缓存数量 项 为 0。
                继续。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

网络_DNS缓存_写入 接受 主机、IP：
    若 全局DNS缓存数量 大于或等于 智能体网络_DNS缓存上限：
        全局DNS缓存数量 为 智能体网络_DNS缓存上限 减 1。
    完。
    项 为 新建 DNS缓存项。
    项 的 主机 为 做 字符串_复制 用 主机。
    项 的 IP 为 做 字符串_复制 用 IP。
    项 的 过期时间戳 为 做 系统_获取时间戳 加 智能体网络_DNS缓存TTL秒 乘 1000。
    全局DNS缓存 的 第 全局DNS缓存数量 项 为 项。
    全局DNS缓存数量 为 全局DNS缓存数量 加 1。
完。

网络_DNS解析_带缓存 接受 主机：
    缓存命中 为 做 网络_DNS缓存_查询 用 主机。
    若 缓存命中 不等于 空：
        返还 缓存命中。
    完。
    IP 为 做 网络_DNS解析 用 主机。
    若 IP 不等于 空：
        做 网络_DNS缓存_写入 用 主机、IP。
    完。
    返还 IP。
完。

网络_查找主机池 接受 主机、端口、协议类型：
    可变 i 为 0。
    只要 i 小于 全局主机池数量：
        池 为 全局主机池列表 的 第 i 项。
        若 池 不等于 0 并且 池 的 主机 等于 主机 并且 池 的 端口 等于 端口 并且 池 的 协议类型 等于 协议类型：
            返还 池。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

网络_获取或创建主机池 接受 主机、端口、协议类型：
    池 为 做 网络_查找主机池 用 主机、端口、协议类型。
    若 池 不等于 空：
        返还 池。
    完。
    若 全局主机池数量 大于或等于 256：
        返还 空。
    完。
    池 为 新建 主机连接池。
    池 的 主机 为 做 字符串_复制 用 主机。
    池 的 端口 为 端口。
    池 的 协议类型 为 协议类型。
    池 的 连接数组 为 做 数组_创建 用 智能体网络_每主机最大连接、0。
    池 的 连接数量 为 0。
    池 的 最大连接 为 智能体网络_每主机最大连接。
    池 的 熔断状态 为 熔断_关闭。
    池 的 熔断开启时间 为 0。
    池 的 窗口请求数 为 0。
    池 的 窗口失败数 为 0。
    池 的 半开探测剩余 为 智能体网络_熔断半开探测数。
    池 的 限流窗口起始 为 做 系统_获取时间戳。
    池 的 限流计数 为 0。
    池 的 限流QPS 为 智能体网络_限流默认QPS。
    全局主机池列表 的 第 全局主机池数量 项 为 池。
    全局主机池数量 为 全局主机池数量 加 1。
    返还 池。
完。

网络_创建连接 接受 解析、配置：
    conn 为 新建 网络连接。
    conn 的 连接ID 为 全局连接序号。
    全局连接序号 为 全局连接序号 加 1。
    conn 的 主机 为 做 字符串_复制 用 解析 的 主机。
    conn 的 端口 为 解析 的 端口。
    conn 的 协议类型 为 解析 的 协议类型。
    conn 的 已连接 为 假。
    conn 的 可复用 为 真。
    conn 的 正在使用 为 真。
    conn 的 创建时间戳 为 做 系统_获取时间戳。
    conn 的 最后活跃时间戳 为 conn 的 创建时间戳。
    conn 的 失败次数 为 0。
    conn 的 RTT毫秒 为 0。
    IP 为 做 网络_DNS解析_带缓存 用 解析 的 主机。
    若 IP 等于 空：
        返还 空。
    完。
    开始 为 做 系统_获取时间戳。
    conn 的 套接字 为 做 网络_TCP_连接 用 IP、解析 的 端口、配置 的 连接超时毫秒。
    若 conn 的 套接字 等于 0：
        返还 空。
    完。
    conn 的 RTT毫秒 为 做 系统_获取时间戳 减 开始。
    conn 的 已连接 为 真。
    若 解析 的 协议类型 等于 协议_HTTPS 或者 解析 的 协议类型 等于 协议_WSS：
        全局网络统计 的 总TLS握手数 为 全局网络统计 的 总TLS握手数 加 1。
        conn 的 TLS会话 为 做 网络_TLS_握手 用 conn 的 套接字、解析 的 主机。
        若 conn 的 TLS会话 等于 0：
            全局网络统计 的 总TLS失败数 为 全局网络统计 的 总TLS失败数 加 1。
            做 网络_TCP_关闭 用 conn 的 套接字。
            返还 空。
        完。
        若 配置 的 TLS校验：
            校验 为 做 网络_TLS_验证证书 用 conn 的 TLS会话、解析 的 主机、配置 的 TLS主机名校验。
            若 非 校验 的 有效：
                全局网络统计 的 总TLS失败数 为 全局网络统计 的 总TLS失败数 加 1。
                做 网络_TLS_关闭 用 conn 的 TLS会话。
                做 网络_TCP_关闭 用 conn 的 套接字。
                返还 空。
            完。
            若 配置 的 TLS证书固定指纹 不等于 「」 并且 校验 的 证书摘要 不等于 配置 的 TLS证书固定指纹：
                全局网络统计 的 总TLS失败数 为 全局网络统计 的 总TLS失败数 加 1。
                做 网络_TLS_关闭 用 conn 的 TLS会话。
                做 网络_TCP_关闭 用 conn 的 套接字。
                返还 空。
            完。
        完。
    完。
    全局网络统计 的 总连接创建数 为 全局网络统计 的 总连接创建数 加 1。
    返还 conn。
完。

网络_从池获取连接 接受 解析、配置：
    池 为 做 网络_获取或创建主机池 用 解析 的 主机、解析 的 端口、解析 的 协议类型。
    若 池 等于 空：
        返还 空。
    完。
    若 池 的 熔断状态 等于 熔断_打开：
        过去 为 做 系统_获取时间戳 减 池 的 熔断开启时间。
        若 过去 大于 智能体网络_熔断窗口 乘 1000：
            池 的 熔断状态 为 熔断_半开。
            池 的 半开探测剩余 为 智能体网络_熔断半开探测数。
        否则：
            返还 空。
        完。
    完。
    可变 i 为 0。
    只要 i 小于 池 的 连接数量：
        conn 为 池 的 连接数组 的 第 i 项。
        若 conn 不等于 0 并且 非 conn 的 正在使用 并且 conn 的 已连接 并且 conn 的 可复用：
            若 做 系统_获取时间戳 减 conn 的 最后活跃时间戳 大于 智能体网络_连接空闲回收秒 乘 1000：
                做 网络_销毁连接 用 conn。
                池 的 连接数量 为 池 的 连接数量 减 1。
                池 的 连接数组 的 第 i 项 为 池 的 连接数组 的 第 池 的 连接数量 项。
                池 的 连接数组 的 第 池 的 连接数量 项 为 0。
                继续。
            完。
            conn 的 正在使用 为 真。
            conn 的 最后活跃时间戳 为 做 系统_获取时间戳。
            全局网络统计 的 总连接复用数 为 全局网络统计 的 总连接复用数 加 1。
            返还 conn。
        完。
        i 为 i 加 1。
    完。
    若 池 的 连接数量 小于 池 的 最大连接：
        conn 为 做 网络_创建连接 用 解析、配置。
        若 conn 等于 空：
            返还 空。
        完。
        池 的 连接数组 的 第 池 的 连接数量 项 为 conn。
        池 的 连接数量 为 池 的 连接数量 加 1。
        返还 conn。
    完。
    返还 空。
完。

网络_归还连接 接受 conn：
    若 conn 等于 空：
        返还。
    完。
    conn 的 正在使用 为 假。
    conn 的 最后活跃时间戳 为 做 系统_获取时间戳。
完。

网络_销毁连接 接受 conn：
    若 conn 等于 空：
        返还。
    完。
    conn 的 已连接 为 假。
    conn 的 正在使用 为 假。
    conn 的 可复用 为 假。
    若 conn 的 TLS会话 不等于 0：
        做 网络_TLS_关闭 用 conn 的 TLS会话。
        conn 的 TLS会话 为 0。
    完。
    若 conn 的 套接字 不等于 0：
        做 网络_TCP_关闭 用 conn 的 套接字。
        conn 的 套接字 为 0。
    完。
    全局网络统计 的 总连接回收数 为 全局网络统计 的 总连接回收数 加 1。
完。

网络_连接池维护 接受 零参：
    当前 为 做 系统_获取时间戳。
    可变 pi 为 0。
    只要 pi 小于 全局主机池数量：
        池 为 全局主机池列表 的 第 pi 项。
        若 池 不等于 0：
            若 当前 减 池 的 限流窗口起始 大于或等于 智能体网络_限流窗口毫秒：
                池 的 限流窗口起始 为 当前。
                池 的 限流计数 为 0。
            完。
            可变 ci 为 0。
            只要 ci 小于 池 的 连接数量：
                conn 为 池 的 连接数组 的 第 ci 项。
                若 conn 不等于 0 并且 非 conn 的 正在使用 并且 当前 减 conn 的 最后活跃时间戳 大于 智能体网络_连接空闲回收秒 乘 1000：
                    做 网络_销毁连接 用 conn。
                    池 的 连接数量 为 池 的 连接数量 减 1。
                    池 的 连接数组 的 第 ci 项 为 池 的 连接数组 的 第 池 的 连接数量 项。
                    池 的 连接数组 的 第 池 的 连接数量 项 为 0。
                    继续。
                完。
                ci 为 ci 加 1。
            完。
        完。
        pi 为 pi 加 1。
    完。
完。

网络_限流检查 接受 池：
    当前 为 做 系统_获取时间戳。
    若 当前 减 池 的 限流窗口起始 大于或等于 智能体网络_限流窗口毫秒：
        池 的 限流窗口起始 为 当前。
        池 的 限流计数 为 0。
    完。
    若 池 的 限流计数 大于或等于 池 的 限流QPS：
        返还 假。
    完。
    池 的 限流计数 为 池 的 限流计数 加 1。
    返还 真。
完。

网络_熔断记录结果 接受 池、成功：
    池 的 窗口请求数 为 池 的 窗口请求数 加 1。
    若 非 成功：
        池 的 窗口失败数 为 池 的 窗口失败数 加 1。
    完。
    若 池 的 窗口请求数 大于或等于 智能体网络_熔断最小样本：
        失败率 为 池 的 窗口失败数 乘 1.0 除以 池 的 窗口请求数。
        若 失败率 大于或等于 智能体网络_熔断失败阈值：
            池 的 熔断状态 为 熔断_打开。
            池 的 熔断开启时间 为 做 系统_获取时间戳。
            池 的 半开探测剩余 为 智能体网络_熔断半开探测数。
        否则：
            池 的 窗口请求数 为 池 的 窗口请求数 除以 2。
            池 的 窗口失败数 为 池 的 窗口失败数 除以 2。
        完。
    完。
    若 池 的 熔断状态 等于 熔断_半开：
        若 成功：
            池 的 半开探测剩余 为 池 的 半开探测剩余 减 1。
            若 池 的 半开探测剩余 小于或等于 0：
                池 的 熔断状态 为 熔断_关闭。
                池 的 窗口请求数 为 0。
                池 的 窗口失败数 为 0。
            完。
        否则：
            池 的 熔断状态 为 熔断_打开。
            池 的 熔断开启时间 为 做 系统_获取时间戳。
            池 的 半开探测剩余 为 智能体网络_熔断半开探测数。
        完。
    完。
完。

NAPT_记录样本 接受 耗时毫秒、成功、吞吐KB每秒：
    idx 为 NAPT_历史写指针 取余于 NAPT_历史窗口。
    NAPT_耗时历史 的 第 idx 项 为 耗时毫秒。
    NAPT_失败历史 的 第 idx 项 为 若 成功 则 0.0 否则 1.0。
    NAPT_吞吐历史 的 第 idx 项 为 吞吐KB每秒。
    NAPT_历史写指针 为 NAPT_历史写指针 加 1。
完。

NAPT_构建特征 接受 解析、配置：
    可变 特征 为 做 数组_创建 用 NAPT_特征维度、0.0。
    特征 的 第 0 项 为 配置 的 连接超时毫秒。
    特征 的 第 1 项 为 配置 的 读取超时毫秒。
    特征 的 第 2 项 为 配置 的 最大重试。
    特征 的 第 3 项 为 解析 的 端口。
    特征 的 第 4 项 为 若 解析 的 协议类型 等于 协议_HTTPS 或者 解析 的 协议类型 等于 协议_WSS 则 1 否则 0。
    可变 样本数 为 若 NAPT_历史写指针 小于 NAPT_历史窗口 则 NAPT_历史写指针 否则 NAPT_历史窗口。
    若 样本数 大于 0：
        可变 耗时和 为 0.0。
        可变 失败和 为 0.0。
        可变 吞吐和 为 0.0。
        可变 i 为 0。
        只要 i 小于 样本数：
            耗时和 为 耗时和 加 NAPT_耗时历史 的 第 i 项。
            失败和 为 失败和 加 NAPT_失败历史 的 第 i 项。
            吞吐和 为 吞吐和 加 NAPT_吞吐历史 的 第 i 项。
            i 为 i 加 1。
        完。
        特征 的 第 5 项 为 耗时和 除以 样本数。
        特征 的 第 6 项 为 失败和 除以 样本数。
        特征 的 第 7 项 为 吞吐和 除以 样本数。
    完。
    特征 的 第 8 项 为 全局网络统计 的 总请求数。
    特征 的 第 9 项 为 全局网络统计 的 总失败数。
    特征 的 第 10 项 为 全局网络统计 的 平均耗时毫秒。
    特征 的 第 11 项 为 全局网络统计 的 总连接复用数。
    特征 的 第 12 项 为 全局网络统计 的 总TLS失败数。
    特征 的 第 13 项 为 全局主机池数量。
    返还 特征。
完。

NAPT_优化配置 接受 解析、配置：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 配置。
    完。
    特征 为 做 NAPT_构建特征 用 解析、配置。
    张量 为 做 Tensor_创建 用 1、NAPT_特征维度。
    可变 i 为 0。
    只要 i 小于 NAPT_特征维度：
        做 Tensor_写 用 张量、0、i、特征 的 第 i 项。
        i 为 i 加 1。
    完。
    预测 为 做 AI总线_推理 用 「NAPT_网络策略优化」、张量。
    做 Tensor_释放 用 张量。
    若 预测 的 置信度 小于 NAPT_置信阈值：
        返还 配置。
    完。
    超时倍率 为 预测 的 输出向量 的 第 0 项。
    重试增量 为 预测 的 输出向量 的 第 1 项。
    池开关 为 预测 的 输出向量 的 第 2 项。
    压缩开关 为 预测 的 输出向量 的 第 3 项。
    若 超时倍率 小于 0.5：
        超时倍率 为 0.5。
    完。
    若 超时倍率 大于 2.5：
        超时倍率 为 2.5。
    完。
    配置 的 连接超时毫秒 为 配置 的 连接超时毫秒 乘 超时倍率。
    配置 的 读取超时毫秒 为 配置 的 读取超时毫秒 乘 超时倍率。
    配置 的 总超时毫秒 为 配置 的 总超时毫秒 乘 超时倍率。
    新重试 为 配置 的 最大重试 加 重试增量。
    若 新重试 小于 0：
        新重试 为 0。
    完。
    若 新重试 大于 智能体网络_最大重试次数：
        新重试 为 智能体网络_最大重试次数。
    完。
    配置 的 最大重试 为 新重试。
    配置 的 使用连接池 为 池开关 大于或等于 0.5。
    配置 的 使用压缩 为 压缩开关 大于或等于 0.5。
    返还 配置。
完。

网络_构建HTTP请求文本 接受 配置、解析、重定向URL：
    path 为 若 重定向URL 不等于 「」 则 重定向URL 的 路径 否则 解析 的 路径。
    query 为 若 重定向URL 不等于 「」 并且 重定向URL 的 查询串 不等于 「」 则 重定向URL 的 查询串 否则 解析 的 查询串。
    请求行 为 配置 的 方法 加 「 」 加 path 加 若 query 不等于 「」 则 「?」 加 query 否则 「」 加 「 HTTP/1.1\r\n」。
    头 为 「」。
    头 为 头 加 「Host: 」 加 解析 的 主机 加 「\r\n」。
    头 为 头 加 「Connection: keep-alive\r\n」。
    头 为 头 加 「User-Agent: NovaOS-Agent/1.0\r\n」。
    头 为 头 加 「Accept: */*\r\n」。
    若 配置 的 使用压缩：
        头 为 头 加 「Accept-Encoding: gzip, deflate, br\r\n」。
    完。
    若 配置 的 追踪ID 不等于 「」：
        头 为 头 加 「X-Nova-Trace-ID: 」 加 配置 的 追踪ID 加 「\r\n」。
    完。
    可变 i 为 0。
    只要 i 小于 配置 的 头数量：
        h 为 配置 的 头数组 的 第 i 项。
        头 为 头 加 h 的 键 加 「: 」 加 h 的 值 加 「\r\n」。
        i 为 i 加 1。
    完。
    body 为 若 配置 的 请求体 不等于 空 则 配置 的 请求体 否则 「」。
    若 做 字符串长度 用 body 大于 0：
        头 为 头 加 「Content-Length: 」 加 做 整数转字符串 用 做 字符串长度 用 body 加 「\r\n」。
    完。
    头 为 头 加 「\r\n」。
    返还 请求行 加 头 加 body。
完。

网络_解析HTTP响应 接受 原始响应文本：
    resp 为 新建 网络响应。
    resp 的 成功 为 假。
    resp 的 状态码 为 0。
    resp 的 状态文本 为 「」。
    resp 的 头数组 为 做 数组_创建 用 智能体网络_请求头最大数、0。
    resp 的 头数量 为 0。
    resp 的 数据 为 「」。
    resp 的 数据长度 为 0。
    resp 的 错误 为 「」。
    resp 的 来自缓存 为 假。
    若 原始响应文本 等于 空 或者 做 字符串长度 用 原始响应文本 等于 0：
        resp 的 错误 为 「空响应」。
        返还 resp。
    完。
    头体分界 为 做 字符串查找 用 原始响应文本、「\r\n\r\n」。
    若 头体分界 小于 0：
        resp 的 错误 为 「响应格式错误」。
        返还 resp。
    完。
    头文本 为 做 字符串子串 用 原始响应文本、0、头体分界。
    resp 的 数据 为 做 字符串子串 用 原始响应文本、头体分界 加 4、做 字符串长度 用 原始响应文本。
    resp 的 数据长度 为 做 字符串长度 用 resp 的 数据。
    行数组 为 做 字符串_分割 用 头文本、「\r\n」。
    若 行数组 的 长度 小于或等于 0：
        resp 的 错误 为 「无状态行」。
        返还 resp。
    完。
    状态行 为 行数组 的 第 0 项。
    状态段 为 做 字符串_分割 用 状态行、「 」。
    若 状态段 的 长度 小于 2：
        resp 的 错误 为 「状态行格式错误」。
        返还 resp。
    完。
    resp 的 状态码 为 做 字符串转整数 用 状态段 的 第 1 项。
    resp 的 状态文本 为 若 状态段 的 长度 大于或等于 3 则 状态段 的 第 2 项 否则 「」。
    可变 i 为 1。
    只要 i 小于 行数组 的 长度 并且 resp 的 头数量 小于 智能体网络_请求头最大数：
        行 为 行数组 的 第 i 项。
        冒号位 为 做 字符串查找 用 行、「:」。
        若 冒号位 大于 0：
            h 为 新建 网络头。
            h 的 键 为 做 字符串_trim 用 做 字符串子串 用 行、0、冒号位。
            h 的 值 为 做 字符串_trim 用 做 字符串子串 用 行、冒号位 加 1、做 字符串长度 用 行。
            resp 的 头数组 的 第 resp 的 头数量 项 为 h。
            resp 的 头数量 为 resp 的 头数量 加 1。
        完。
        i 为 i 加 1。
    完。
    编码 为 做 网络_响应头取 用 resp、「Content-Encoding」。
    若 编码 等于 「gzip」：
        resp 的 数据 为 做 压缩_gzip_解压 用 resp 的 数据。
        resp 的 数据长度 为 做 字符串长度 用 resp 的 数据。
    否则若 编码 等于 「deflate」：
        resp 的 数据 为 做 压缩_deflate_解压 用 resp 的 数据。
        resp 的 数据长度 为 做 字符串长度 用 resp 的 数据。
    完。
    resp 的 成功 为 resp 的 状态码 大于或等于 200 并且 resp 的 状态码 小于 300。
    返还 resp。
完。

网络_响应头取 接受 resp、键名：
    可变 i 为 0。
    只要 i 小于 resp 的 头数量：
        若 做 字符串_相等_忽略大小写 用 resp 的 头数组 的 第 i 项 的 键、键名：
            返还 resp 的 头数组 的 第 i 项 的 值。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

智能体网络_默认配置 接受 方法、url：
    cfg 为 新建 网络请求配置。
    cfg 的 方法 为 方法。
    cfg 的 URL 为 做 字符串_复制 用 url。
    cfg 的 头数组 为 做 数组_创建 用 智能体网络_请求头最大数、0。
    cfg 的 头数量 为 0。
    cfg 的 请求体 为 「」。
    cfg 的 连接超时毫秒 为 智能体网络_默认连接超时毫秒。
    cfg 的 读取超时毫秒 为 智能体网络_默认读取超时毫秒。
    cfg 的 总超时毫秒 为 智能体网络_默认超时毫秒。
    cfg 的 最大重试 为 智能体网络_默认重试次数。
    cfg 的 允许重定向 为 真。
    cfg 的 最大重定向 为 智能体网络_最大重定向次数。
    cfg 的 TLS校验 为 真。
    cfg 的 TLS主机名校验 为 真。
    cfg 的 TLS证书固定指纹 为 「」。
    cfg 的 使用连接池 为 真。
    cfg 的 使用压缩 为 真。
    cfg 的 使用代理 为 假。
    cfg 的 代理主机 为 「」。
    cfg 的 代理端口 为 0。
    cfg 的 代理用户名 为 「」。
    cfg 的 代理密码 为 「」。
    cfg 的 追踪ID 为 做 智能体网络_生成追踪ID。
    返还 cfg。
完。

智能体网络_请求 接受 配置：
    总开始 为 做 系统_获取时间戳。
    resp 为 新建 网络响应。
    resp 的 成功 为 假。
    resp 的 状态码 为 0。
    resp 的 错误 为 「」。
    resp 的 重试次数 为 0。
    resp 的 最终URL 为 配置 的 URL。
    全局网络统计 的 总请求数 为 全局网络统计 的 总请求数 加 1。
    解析 为 做 智能体网络_解析URL 用 配置 的 URL。
    若 解析 等于 空：
        resp 的 错误 为 「无效URL」。
        全局网络统计 的 总失败数 为 全局网络统计 的 总失败数 加 1。
        返还 resp。
    完。
    若 非 做 智能体网络_策略允许主机 用 解析 的 主机：
        resp 的 错误 为 「主机不在策略允许范围」。
        全局网络统计 的 总失败数 为 全局网络统计 的 总失败数 加 1。
        返还 resp。
    完。
    配置 为 做 NAPT_优化配置 用 解析、配置。
    可变 当前URL解析 为 解析。
    可变 重定向次数 为 0。
    可变 尝试 为 0。
    可变 最后错误 为 「」。
    只要 尝试 小于或等于 配置 的 最大重试：
        若 做 系统_获取时间戳 减 总开始 大于 配置 的 总超时毫秒：
            resp 的 错误 为 「总超时」。
            全局网络统计 的 总超时数 为 全局网络统计 的 总超时数 加 1。
            跳出。
        完。
        池 为 做 网络_获取或创建主机池 用 当前URL解析 的 主机、当前URL解析 的 端口、当前URL解析 的 协议类型。
        若 池 等于 空：
            最后错误 为 「连接池创建失败」。
            尝试 为 尝试 加 1。
            继续。
        完。
        若 非 做 网络_限流检查 用 池：
            最后错误 为 「限流触发」。
            做 睡眠 用 20。
            尝试 为 尝试 加 1。
            继续。
        完。
        conn 为 若 配置 的 使用连接池 则 做 网络_从池获取连接 用 当前URL解析、配置 否则 做 网络_创建连接 用 当前URL解析、配置。
        若 conn 等于 空：
            最后错误 为 「连接建立失败/熔断中」。
            做 网络_熔断记录结果 用 池、假。
            尝试 为 尝试 加 1。
            延迟 为 做 智能体网络_计算退避延迟 用 尝试。
            做 睡眠 用 延迟。
            继续。
        完。
        请求文本 为 做 网络_构建HTTP请求文本 用 配置、当前URL解析、「」。
        发送成功 为 做 网络_连接发送 用 conn、请求文本、配置 的 读取超时毫秒。
        若 非 发送成功：
            最后错误 为 「发送失败」。
            conn 的 失败次数 为 conn 的 失败次数 加 1。
            做 网络_熔断记录结果 用 池、假。
            做 网络_销毁连接 用 conn。
            尝试 为 尝试 加 1。
            resp 的 重试次数 为 尝试。
            全局网络统计 的 总重试数 为 全局网络统计 的 总重试数 加 1。
            做 睡眠 用 做 智能体网络_计算退避延迟 用 尝试。
            继续。
        完。
        原始响应 为 做 网络_连接接收完整 用 conn、配置 的 读取超时毫秒。
        若 原始响应 等于 空：
            最后错误 为 「读取响应失败」。
            conn 的 失败次数 为 conn 的 失败次数 加 1。
            做 网络_熔断记录结果 用 池、假。
            做 网络_销毁连接 用 conn。
            尝试 为 尝试 加 1。
            resp 的 重试次数 为 尝试。
            全局网络统计 的 总重试数 为 全局网络统计 的 总重试数 加 1。
            做 睡眠 用 做 智能体网络_计算退避延迟 用 尝试。
            继续。
        完。
        resp 为 做 网络_解析HTTP响应 用 原始响应。
        resp 的 耗时毫秒 为 做 系统_获取时间戳 减 总开始。
        resp 的 重试次数 为 尝试。
        resp 的 最终URL 为 当前URL解析 的 归一化URL。
        做 网络_熔断记录结果 用 池、resp 的 状态码 小于 500 并且 resp 的 状态码 不等于 0。
        连接头 为 做 网络_响应头取 用 resp、「Connection」。
        若 做 字符串_相等_忽略大小写 用 连接头、「close」：
            做 网络_销毁连接 用 conn。
        否则：
            做 网络_归还连接 用 conn。
        完。
        若 配置 的 允许重定向 并且 (resp 的 状态码 等于 301 或者 resp 的 状态码 等于 302 或者 resp 的 状态码 等于 307 或者 resp 的 状态码 等于 308) 并且 重定向次数 小于 配置 的 最大重定向：
            loc 为 做 网络_响应头取 用 resp、「Location」。
            若 loc 不等于 「」：
                当前URL解析 为 若 做 字符串开始于 用 loc、「http://」 或者 做 字符串开始于 用 loc、「https://」 则 做 智能体网络_解析URL 用 loc 否则 做 智能体网络_解析URL 用 当前URL解析 的 协议字符串 加 「://」 加 当前URL解析 的 主机 加 loc。
                若 当前URL解析 不等于 空：
                    重定向次数 为 重定向次数 加 1。
                    全局网络统计 的 总重定向数 为 全局网络统计 的 总重定向数 加 1。
                    继续。
                完。
            完。
        完。
        若 resp 的 成功：
            全局网络统计 的 总成功数 为 全局网络统计 的 总成功数 加 1。
            全局网络统计 的 总下载字节 为 全局网络统计 的 总下载字节 加 resp 的 数据长度。
            做 NAPT_记录样本 用 resp 的 耗时毫秒、真、若 resp 的 耗时毫秒 大于 0 则 resp 的 数据长度 除以 resp 的 耗时毫秒 否则 0。
            做 智能体网络_更新平均耗时 用 resp 的 耗时毫秒。
            返还 resp。
        完。
        若 resp 的 状态码 大于或等于 400 并且 resp 的 状态码 小于 500 并且 resp 的 状态码 不等于 429：
            全局网络统计 的 总失败数 为 全局网络统计 的 总失败数 加 1。
            全局网络统计 的 最近错误 为 「HTTP 」 加 做 整数转字符串 用 resp 的 状态码。
            做 NAPT_记录样本 用 resp 的 耗时毫秒、假、0。
            返还 resp。
        完。
        最后错误 为 「HTTP 」 加 做 整数转字符串 用 resp 的 状态码。
        尝试 为 尝试 加 1。
        resp 的 重试次数 为 尝试。
        全局网络统计 的 总重试数 为 全局网络统计 的 总重试数 加 1。
        做 睡眠 用 做 智能体网络_计算退避延迟 用 尝试。
    完。
    resp 的 成功 为 假。
    resp 的 错误 为 若 最后错误 不等于 「」 则 最后错误 否则 「请求失败」。
    resp 的 耗时毫秒 为 做 系统_获取时间戳 减 总开始。
    全局网络统计 的 总失败数 为 全局网络统计 的 总失败数 加 1。
    全局网络统计 的 最近错误 为 resp 的 错误。
    做 NAPT_记录样本 用 resp 的 耗时毫秒、假、0。
    做 智能体网络_更新平均耗时 用 resp 的 耗时毫秒。
    返还 resp。
完。

智能体网络_计算退避延迟 接受 尝试次数：
    基础 为 智能体网络_默认重试基准延迟。
    延迟 为 基础 乘 (1 左移 尝试次数)。
    若 延迟 大于 8000：
        延迟 为 8000。
    完。
    抖动 为 做 随机_范围 用 0、200。
    返还 延迟 加 抖动。
完。

智能体网络_更新平均耗时 接受 耗时：
    n 为 全局网络统计 的 总请求数。
    若 n 小于或等于 0：
        全局网络统计 的 平均耗时毫秒 为 耗时。
    否则：
        全局网络统计 的 平均耗时毫秒 为 (全局网络统计 的 平均耗时毫秒 乘 (n 减 1) 加 耗时) 除以 n。
    完。
完。

智能体网络_生成追踪ID 接受 零参：
    seq 为 全局请求序号。
    全局请求序号 为 全局请求序号 加 1。
    返还 「nova-net-」 加 做 整数转字符串 用 做 系统_获取时间戳 加 「-」 加 做 整数转字符串 用 seq。
完。

智能体网络_GET 接受 url：
    cfg 为 做 智能体网络_默认配置 用 方法_GET、url。
    返还 做 智能体网络_请求 用 cfg。
完。

智能体网络_GET_带头 接受 url、头数组、头数量：
    cfg 为 做 智能体网络_默认配置 用 方法_GET、url。
    cfg 的 头数组 为 头数组。
    cfg 的 头数量 为 头数量。
    返还 做 智能体网络_请求 用 cfg。
完。

智能体网络_POST 接受 url、数据：
    cfg 为 做 智能体网络_默认配置 用 方法_POST、url。
    cfg 的 请求体 为 数据。
    cfg 的 头数组 的 第 cfg 的 头数量 项 为 新建 网络头：键 为 「Content-Type」、值 为 「application/json」。
    cfg 的 头数量 为 cfg 的 头数量 加 1。
    返还 做 智能体网络_请求 用 cfg。
完。

智能体网络_POST_表单 接受 url、表单体：
    cfg 为 做 智能体网络_默认配置 用 方法_POST、url。
    cfg 的 请求体 为 表单体。
    cfg 的 头数组 的 第 cfg 的 头数量 项 为 新建 网络头：键 为 「Content-Type」、值 为 「application/x-www-form-urlencoded」。
    cfg 的 头数量 为 cfg 的 头数量 加 1。
    返还 做 智能体网络_请求 用 cfg。
完。

智能体网络_PUT 接受 url、数据：
    cfg 为 做 智能体网络_默认配置 用 方法_PUT、url。
    cfg 的 请求体 为 数据。
    返还 做 智能体网络_请求 用 cfg。
完。

智能体网络_DELETE 接受 url：
    cfg 为 做 智能体网络_默认配置 用 方法_DELETE、url。
    返还 做 智能体网络_请求 用 cfg。
完。

智能体网络_PATCH 接受 url、数据：
    cfg 为 做 智能体网络_默认配置 用 方法_PATCH、url。
    cfg 的 请求体 为 数据。
    return。
    做 智能体网络_请求 用 cfg。
完。

智能体网络_HEAD 接受 url：
    cfg 为 做 智能体网络_默认配置 用 方法_HEAD、url。
    返还 做 智能体网络_请求 用 cfg。
完。

智能体网络_流式GET 接受 url、回调_数据块、回调_结束、回调_错误：
    cfg 为 做 智能体网络_默认配置 用 方法_GET、url。
    cfg 的 使用连接池 为 真。
    解析 为 做 智能体网络_解析URL 用 url。
    若 解析 等于 空：
        若 回调_错误 不等于 0：
            做 回调_错误 用 「无效URL」。
        完。
        返还 空。
    完。
    conn 为 做 网络_从池获取连接 用 解析、cfg。
    若 conn 等于 空：
        若 回调_错误 不等于 0：
            做 回调_错误 用 「连接失败」。
        完。
        返还 空。
    完。
    请求文本 为 做 网络_构建HTTP请求文本 用 cfg、解析、「」。
    若 非 做 网络_连接发送 用 conn、请求文本、cfg 的 读取超时毫秒：
        做 网络_销毁连接 用 conn。
        若 回调_错误 不等于 0：
            做 回调_错误 用 「发送失败」。
        完。
        返还 空。
    完。
    h 为 新建 流式响应句柄。
    h 的 请求ID 为 全局请求序号。
    全局请求序号 为 全局请求序号 加 1。
    h 的 连接 为 conn。
    h 的 已结束 为 假。
    h 的 状态码 为 0。
    h 的 头数组 为 做 数组_创建 用 智能体网络_请求头最大数、0。
    h 的 头数量 为 0。
    h 的 已接收字节 为 0。
    h 的 回调_数据块 为 回调_数据块。
    h 的 回调_结束 为 回调_结束。
    h 的 回调_错误 为 回调_错误。
    做 网络_流读取循环 用 h。
    返还 h。
完。

网络_流读取循环 接受 句柄：
    conn 为 句柄 的 连接。
    头已解析 为 假。
    剩余缓冲 为 「」。
    只要 非 句柄 的 已结束：
        块 为 做 网络_连接接收块 用 conn、智能体网络_流块大小、智能体网络_默认读取超时毫秒。
        若 块 等于 空：
            句柄 的 已结束 为 真。
            若 句柄 的 回调_结束 不等于 0：
                做 句柄 的 回调_结束 用 句柄 的 已接收字节。
            完。
            做 网络_归还连接 用 conn。
            跳出。
        完。
        句柄 的 已接收字节 为 句柄 的 已接收字节 加 做 字符串长度 用 块。
        若 非 头已解析：
            剩余缓冲 为 剩余缓冲 加 块。
            idx 为 做 字符串查找 用 剩余缓冲、「\r\n\r\n」。
            若 idx 大于或等于 0：
                头文本 为 做 字符串子串 用 剩余缓冲、0、idx 加 4。
                tmpResp 为 做 网络_解析HTTP响应 用 头文本 加 「」。
                句柄 的 状态码 为 tmpResp 的 状态码。
                句柄 的 头数组 为 tmpResp 的 头数组。
                句柄 的 头数量 为 tmpResp 的 头数量。
                头已解析 为 真。
                body 为 做 字符串子串 用 剩余缓冲、idx 加 4、做 字符串长度 用 剩余缓冲。
                若 做 字符串长度 用 body 大于 0 并且 句柄 的 回调_数据块 不等于 0：
                    做 句柄 的 回调_数据块 用 body。
                完。
                剩余缓冲 为 「」。
            完。
        否则：
            若 句柄 的 回调_数据块 不等于 0：
                做 句柄 的 回调_数据块 用 块。
            完。
        完。
    完。
完。

智能体网络_流式上传 接受 url、读取块回调、总长度、头数组、头数量：
    cfg 为 做 智能体网络_默认配置 用 方法_POST、url。
    cfg 的 头数组 为 头数组。
    cfg 的 头数量 为 头数量。
    cfg 的 头数组 的 第 cfg 的 头数量 项 为 新建 网络头：键 为 「Transfer-Encoding」、值 为 「chunked」。
    cfg 的 头数量 为 cfg 的 头数量 加 1。
    解析 为 做 智能体网络_解析URL 用 url。
    若 解析 等于 空：
        返还 一本字典：成功 对应 假、错误 对应 「无效URL」。
    完。
    conn 为 做 网络_从池获取连接 用 解析、cfg。
    若 conn 等于 空：
        返还 一本字典：成功 对应 假、错误 对应 「连接失败」。
    完。
    请求行 为 cfg 的 方法 加 「 」 加 解析 的 路径 加 「 HTTP/1.1\r\n」。
    头 为 「Host: 」 加 解析 的 主机 加 「\r\nConnection: keep-alive\r\n」。
    可变 i 为 0。
    只要 i 小于 cfg 的 头数量：
        头 为 头 加 cfg 的 头数组 的 第 i 项 的 键 加 「: 」 加 cfg 的 头数组 的 第 i 项 的 值 加 「\r\n」。
        i 为 i 加 1。
    完。
    头 为 头 加 「\r\n」。
    若 非 做 网络_连接发送 用 conn、请求行 加 头、cfg 的 读取超时毫秒：
        做 网络_销毁连接 用 conn。
        返还 一本字典：成功 对应 假、错误 对应 「发送请求头失败」。
    完。
    可变 已发 为 0。
    只要 已发 小于 总长度：
        块 为 做 读取块回调 用 已发、智能体网络_上传缓冲大小。
        若 块 等于 空 或者 做 字符串长度 用 块 等于 0：
            跳出。
        完。
        块长 为 做 字符串长度 用 块。
        chunk 为 做 整数转十六进制 用 块长 加 「\r\n」 加 块 加 「\r\n」。
        若 非 做 网络_连接发送 用 conn、chunk、cfg 的 读取超时毫秒：
            做 网络_销毁连接 用 conn。
            返还 一本字典：成功 对应 假、错误 对应 「上传中断」、已发送 对应 已发。
        完。
        已发 为 已发 加 块长。
        全局网络统计 的 总上传字节 为 全局网络统计 的 总上传字节 加 块长。
    完。
    做 网络_连接发送 用 conn、「0\r\n\r\n」、cfg 的 读取超时毫秒。
    响应文本 为 做 网络_连接接收完整 用 conn、cfg 的 读取超时毫秒。
    resp 为 做 网络_解析HTTP响应 用 响应文本。
    做 网络_归还连接 用 conn。
    返还 resp。
完。

智能体网络_下载 接受 url、保存路径：
    返还 做 智能体网络_下载_高级 用 url、保存路径、假、「」、0。
完。

智能体网络_下载_高级 接受 url、保存路径、断点续传、期望哈希、限速KB每秒：
    已有大小 为 0。
    若 断点续传 并且 做 文件_存在 用 保存路径：
        已有大小 为 做 文件_大小 用 保存路径。
    完。
    cfg 为 做 智能体网络_默认配置 用 方法_GET、url。
    若 已有大小 大于 0：
        cfg 的 头数组 的 第 cfg 的 头数量 项 为 新建 网络头：键 为 「Range」、值 为 「bytes=」 加 做 整数转字符串 用 已有大小 加 「-」。
        cfg 的 头数量 为 cfg 的 头数量 加 1。
    完。
    resp 为 做 智能体网络_请求 用 cfg。
    若 非 resp 的 成功 并且 resp 的 状态码 不等于 206：
        返还 一本字典：成功 对应 假、错误 对应 resp 的 错误、状态码 对应 resp 的 状态码。
    完。
    若 已有大小 大于 0 并且 resp 的 状态码 等于 206：
        做 文件_追加 用 保存路径、resp 的 数据。
    否则：
        做 文件_写全部 用 保存路径、resp 的 数据。
    完。
    可变 校验通过 为 真。
    若 期望哈希 不等于 「」：
        实际哈希 为 做 哈希_SHA256_文件 用 保存路径。
        校验通过 为 做 字符串_相等_忽略大小写 用 实际哈希、期望哈希。
    完。
    返还 一本字典：成功 对应 校验通过、路径 对应 保存路径、大小 对应 做 文件_大小 用 保存路径、状态码 对应 resp 的 状态码、校验通过 对应 校验通过。
完。

智能体网络_WS连接 接受 url、子协议：
    解析 为 做 智能体网络_解析URL 用 url。
    若 解析 等于 空 或者 解析 的 协议类型 不等于 协议_WS 并且 解析 的 协议类型 不等于 协议_WSS：
        返还 一本字典：成功 对应 假、错误 对应 「无效WS URL」。
    完。
    cfg 为 做 智能体网络_默认配置 用 方法_GET、url。
    cfg 的 头数组 的 第 cfg 的 头数量 项 为 新建 网络头：键 为 「Upgrade」、值 为 「websocket」。
    cfg 的 头数量 为 cfg 的 头数量 加 1。
    cfg 的 头数组 的 第 cfg 的 头数量 项 为 新建 网络头：键 为 「Connection」、值 为 「Upgrade」。
    cfg 的 头数量 为 cfg 的 头数量 加 1。
    cfg 的 头数组 的 第 cfg 的 头数量 项 为 新建 网络头：键 为 「Sec-WebSocket-Version」、值 为 「13」。
    cfg 的 头数量 为 cfg 的 头数量 加 1。
    cfg 的 头数组 的 第 cfg 的 头数量 项 为 新建 网络头：键 为 「Sec-WebSocket-Key」、值 为 做 Base64_编码 用 做 随机_字节串 用 16。
    cfg 的 头数量 为 cfg 的 头数量 加 1。
    若 子协议 不等于 「」：
        cfg 的 头数组 的 第 cfg 的 头数量 项 为 新建 网络头：键 为 「Sec-WebSocket-Protocol」、值 为 子协议。
        cfg 的 头数量 为 cfg 的 头数量 加 1。
    完。
    resp 为 做 智能体网络_请求 用 cfg。
    若 resp 的 状态码 不等于 101：
        返还 一本字典：成功 对应 假、错误 对应 「WS握手失败」、状态码 对应 resp 的 状态码。
    完。
    返还 一本字典：成功 对应 真、状态码 对应 101。
完。

智能体网络_是否可达 接受 主机：
    测试URL 为 「http://」 加 主机 加 「/」。
    cfg 为 做 智能体网络_默认配置 用 方法_HEAD、测试URL。
    cfg 的 最大重试 为 1。
    cfg 的 总超时毫秒 为 5000。
    resp 为 做 智能体网络_请求 用 cfg。
    返还 resp 的 状态码 大于 0。
完。

智能体网络_探测 接受 url：
    开始 为 做 系统_获取时间戳。
    resp 为 做 智能体网络_HEAD 用 url。
    耗时 为 做 系统_获取时间戳 减 开始。
    返还 一本字典：可达 对应 resp 的 状态码 大于 0、状态码 对应 resp 的 状态码、耗时毫秒 对应 耗时、错误 对应 resp 的 错误。
完。

智能体网络_获取统计 接受 零参：
    返还 一本字典：总请求 对应 全局网络统计 的 总请求数、总成功 对应 全局网络统计 的 总成功数、总失败 对应 全局网络统计 的 总失败数、总重试 对应 全局网络统计 的 总重试数、总超时 对应 全局网络统计 的 总超时数、总重定向 对应 全局网络统计 的 总重定向数、TLS握手 对应 全局网络统计 的 总TLS握手数、TLS失败 对应 全局网络统计 的 总TLS失败数、连接创建 对应 全局网络统计 的 总连接创建数、连接复用 对应 全局网络统计 的 总连接复用数、连接回收 对应 全局网络统计 的 总连接回收数、下载字节 对应 全局网络统计 的 总下载字节、上传字节 对应 全局网络统计 的 总上传字节、平均耗时 对应 全局网络统计 的 平均耗时毫秒、最近错误 对应 全局网络统计 的 最近错误、主机池数量 对应 全局主机池数量、DNS缓存数量 对应 全局DNS缓存数量。
完。

智能体网络_打印统计 接受 零参：
    做 内核打印 用 「═══ 智能体网络统计 ═══」。
    做 内核打印 用 「请求: 总={} 成功={} 失败={} 重试={} 超时={}」、全局网络统计 的 总请求数、全局网络统计 的 总成功数、全局网络统计 的 总失败数、全局网络统计 的 总重试数、全局网络统计 的 总超时数。
    做 内核打印 用 「连接: 创建={} 复用={} 回收={} 主机池={}」、全局网络统计 的 总连接创建数、全局网络统计 的 总连接复用数、全局网络统计 的 总连接回收数、全局主机池数量。
    做 内核打印 用 「TLS: 握手={} 失败={}」、全局网络统计 的 总TLS握手数、全局网络统计 的 总TLS失败数。
    做 内核打印 用 「流量: 下载={}B 上传={}B 平均耗时={}ms」、全局网络统计 的 总下载字节、全局网络统计 的 总上传字节、全局网络统计 的 平均耗时毫秒。
    做 内核打印 用 「最近错误: {}」、全局网络统计 的 最近错误。
完。

智能体网络_重置统计 接受 零参：
    做 智能体网络_初始化。
完。

本章 收束。
