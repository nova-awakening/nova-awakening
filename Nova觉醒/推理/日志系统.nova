《日志系统》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·感知·内核直连层》。

引用《..·中枢·哈希整数》。

引用《..·中枢·内存初始化》。

引用《..·中枢·内容可见性》。

引用《..·执行·底层内存操》。

级别_调试 为 0。

级别_信息 为 1。

级别_警告 为 2。

级别_错误 为 3。

级别_致命 为 4。

最大日志 为 2048。

限速窗口 为 1000000。

限速上限 为 200。

日志条目 是 一种 事物，含 时间戳、级别、模块哈希、消息哈希。

可变 日志环 为 0。

可变 日志写位 为 0。

可变 日志总数 为 0。

可变 最低级别 为 级别_信息。

可变 按级别计数 为 0。

可变 丢弃总数 为 0。

可变 限速计数 为 0。

可变 限速窗口起 为 0。

可变 输出回调 为 0。

日志_初始化 接受 零参：
    日志环 为 做 堆_分配 用 最大日志 乘 24。
    做 内存_清零 用 日志环、最大日志 乘 24。
    日志写位 为 0。
    日志总数 为 0。
    按级别计数 为 做 堆_分配 用 5 乘 8。
    做 内存_清零 用 按级别计数、5 乘 8。
完。

日志_设级别 接受 级别：
    最低级别 为 级别。
完。

日志_设输出 接受 回调：
    输出回调 为 回调。
完。

日志_记录 接受 级别、模块、消息：
    若 级别 小于 最低级别：
        返还。
    完。
    若 非 做 日志_限速检查：
        丢弃总数 为 丢弃总数 加 1。
        返还。
    完。
    日志总数 为 日志总数 加 1。
    做 内存_写64 用 按级别计数 加 级别 乘 8、做 内存_读64 用 按级别计数 加 级别 乘 8 加 1。
    偏移 为 日志环 加 日志写位 乘 24。
    做 内存_写64 用 偏移、做 平台_获取高精度时间。
    做 内存_写32 用 偏移 加 8、级别。
    做 内存_写64 用 偏移 加 12、做 哈希_字符串 用 模块。
    做 内存_写64 用 偏移 加 20、做 哈希_字符串 用 消息。
    日志写位 为 (日志写位 加 1) 取余于 最大日志。
    若 输出回调 不等于 0：
        做 输出回调 用 级别、模块、消息。
    完。
完。

日志_限速检查 接受 零参：
    现在 为 做 平台_获取高精度时间。
    若 现在 减 限速窗口起 大于 限速窗口：
        限速窗口起 为 现在。
        限速计数 为 0。
    完。
    限速计数 为 限速计数 加 1。
    返还 限速计数 小于或等于 限速上限。
完。

日志_调试 接受 模块、消息：
    做 日志_记录 用 级别_调试、模块、消息。
完。

日志_信息 接受 模块、消息：
    做 日志_记录 用 级别_信息、模块、消息。
完。

日志_警告 接受 模块、消息：
    做 日志_记录 用 级别_警告、模块、消息。
完。

日志_错误 接受 模块、消息：
    做 日志_记录 用 级别_错误、模块、消息。
完。

日志_致命 接受 模块、消息：
    做 日志_记录 用 级别_致命、模块、消息。
完。

日志_获取 接受 索引：
    若 索引 大于或等于 最大日志：
        返还 0。
    完。
    返还 日志环 加 索引 乘 24。
完。

日志_按级别查询 接受 级别、最大数：
    可变 找到 为 0。
    可变 位置 为 日志写位 减 1。
    若 位置 小于 0：
        位置 为 最大日志 减 1。
    完。
    可变 已检查 为 0。
    只要 已检查 小于 日志总数 并且 已检查 小于 最大日志 并且 找到 小于 最大数：
        偏移 为 日志环 加 位置 乘 24。
        若 做 内存_读32 用 偏移 加 8 等于 级别：
            找到 为 找到 加 1。
        完。
        位置 为 位置 减 1。
        若 位置 小于 0：
            位置 为 最大日志 减 1。
        完。
        已检查 为 已检查 加 1。
    完。
    返还 找到。
完。

日志_按模块查询 接受 模块、最大数：
    模块哈希 为 做 哈希_字符串 用 模块。
    可变 找到 为 0。
    可变 位置 为 日志写位 减 1。
    若 位置 小于 0：
        位置 为 最大日志 减 1。
    完。
    可变 已检查 为 0。
    只要 已检查 小于 日志总数 并且 已检查 小于 最大日志 并且 找到 小于 最大数：
        偏移 为 日志环 加 位置 乘 24。
        若 做 内存_读64 用 偏移 加 12 等于 模块哈希：
            找到 为 找到 加 1。
        完。
        位置 为 位置 减 1。
        若 位置 小于 0：
            位置 为 最大日志 减 1。
        完。
        已检查 为 已检查 加 1。
    完。
    返还 找到。
完。

日志_清空 接受 零参：
    做 内存_清零 用 日志环、最大日志 乘 24。
    日志写位 为 0。
    日志总数 为 0。
    做 内存_清零 用 按级别计数、5 乘 8。
完。

日志_级别名 接受 级别：
    若 级别 等于 级别_调试：
        返还 「调试」。
    完。
    若 级别 等于 级别_信息：
        返还 「信息」。
    完。
    若 级别 等于 级别_警告：
        返还 「警告」。
    完。
    若 级别 等于 级别_错误：
        返还 「错误」。
    完。
    若 级别 等于 级别_致命：
        返还 「致命」。
    完。
    返还 「未知」。
完。

日志_错误率 接受 零参：
    若 日志总数 等于 0：
        返还 0。
    完。
    错误数 为 做 内存_读64 用 按级别计数 加 级别_错误 乘 8 加 做 内存_读64 用 按级别计数 加 级别_致命 乘 8。
    返还 错误数 乘 100 除以 日志总数。
完。

日志_完整统计 接受 零参：
    返还 一组：日志总数、做 内存_读64 用 按级别计数 加 级别_错误 乘 8、做 内存_读64 用 按级别计数 加 级别_警告 乘 8、丢弃总数、做 日志_错误率。
完。

本章 收束。
