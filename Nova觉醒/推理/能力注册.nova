《能力注册》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内核打印》。

能力_最大熟练度 为 1000。

能力_最小熟练度 为 50。

能力_默认熟练度 为 500。

能力_成功提升 为 10。

能力_失败降低 为 20。

能力_衰减率 为 1。

能力_最大类别 为 32。

能力_健康_正常 为 0。

能力_健康_降级 为 1。

能力_健康_故障 为 2。

智能体能力 是 一种 事物，含 智能体名、能力名、熟练度、调用次数、成功次数、失败次数、版本、类别、依赖列表、依赖数、健康状态、最后调用时间、平均耗时、当前负载、最大并发、已启用。

能力类别 是 一种 事物，含 名称、描述、能力数。

能力统计 是 一种 事物，含 注册总数、查询次数、匹配次数、调度次数、衰减次数、注销次数、协商次数。

可变 能力列表 为 一组：（空）。

可变 类别列表 为 一组：（空）。

可变 类别数量 为 0。

可变 全局_能力统计 为 空。

能力统计_初始化 接受 零参：
    若 全局_能力统计 等于 空：
        全局_能力统计 为 新建 能力统计。
        全局_能力统计 的 注册总数 为 0。
        全局_能力统计 的 查询次数 为 0。
        全局_能力统计 的 匹配次数 为 0。
        全局_能力统计 的 调度次数 为 0。
        全局_能力统计 的 衰减次数 为 0。
        全局_能力统计 的 注销次数 为 0。
        全局_能力统计 的 协商次数 为 0。
    完。
完。

能力_注册 接受 智能体名、能力名、熟练度：
    做 能力统计_初始化。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 智能体名 等于 智能体名 并且 能力列表 的 第 i 项 的 能力名 等于 能力名：
            能力列表 的 第 i 项 的 熟练度 为 熟练度。
            能力列表 的 第 i 项 的 已启用 为 真。
            返还。
        完。
        i 为 i 加 1。
    完。
    cap 为 新建 智能体能力。
    cap 的 智能体名 为 智能体名。
    cap 的 能力名 为 能力名。
    cap 的 熟练度 为 熟练度。
    cap 的 调用次数 为 0。
    cap 的 成功次数 为 0。
    cap 的 失败次数 为 0。
    cap 的 版本 为 1。
    cap 的 类别 为 「」。
    cap 的 依赖列表 为 一组：（空）。
    cap 的 依赖数 为 0。
    cap 的 健康状态 为 能力_健康_正常。
    cap 的 最后调用时间 为 0。
    cap 的 平均耗时 为 0。
    cap 的 当前负载 为 0。
    cap 的 最大并发 为 10。
    cap 的 已启用 为 真。
    做 追加 用 能力列表、cap。
    全局_能力统计 的 注册总数 为 全局_能力统计 的 注册总数 加 1。
完。

能力_注册_完整 接受 智能体名、能力名、熟练度、版本、类别、最大并发：
    做 能力_注册 用 智能体名、能力名、熟练度。
    idx 为 做 能力_查找索引 用 智能体名、能力名。
    若 idx 大于或等于 0：
        能力列表 的 第 idx 项 的 版本 为 版本。
        能力列表 的 第 idx 项 的 类别 为 类别。
        能力列表 的 第 idx 项 的 最大并发 为 最大并发。
        做 能力_确保类别 用 类别。
    完。
完。

能力_批量注册 接受 智能体名、能力列表输入：
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表输入：
        做 能力_注册 用 智能体名、能力列表输入 的 第 i 项、能力_默认熟练度。
        i 为 i 加 1。
    完。
完。

能力_注销 接受 智能体名、能力名：
    做 能力统计_初始化。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 智能体名 等于 智能体名 并且 能力列表 的 第 i 项 的 能力名 等于 能力名：
            能力列表 的 第 i 项 的 已启用 为 假。
            全局_能力统计 的 注销次数 为 全局_能力统计 的 注销次数 加 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

能力_注销全部 接受 智能体名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 智能体名 等于 智能体名：
            能力列表 的 第 i 项 的 已启用 为 假。
        完。
        i 为 i 加 1。
    完。
完。

能力_添加依赖 接受 智能体名、能力名、依赖能力名：
    idx 为 做 能力_查找索引 用 智能体名、能力名。
    若 idx 大于或等于 0：
        做 追加 用 能力列表 的 第 idx 项 的 依赖列表、依赖能力名。
        能力列表 的 第 idx 项 的 依赖数 为 能力列表 的 第 idx 项 的 依赖数 加 1。
    完。
完。

能力_检查依赖 接受 智能体名、能力名：
    idx 为 做 能力_查找索引 用 智能体名、能力名。
    若 idx 小于 0：
        返还 假。
    完。
    cap 为 能力列表 的 第 idx 项。
    可变 i 为 0。
    只要 i 小于 cap 的 依赖数：
        dep 为 cap 的 依赖列表 的 第 i 项。
        候选 为 做 能力_查询 用 dep。
        若 做 计数 用 候选 等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

能力_查找索引 接受 智能体名、能力名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 智能体名 等于 智能体名 并且 能力列表 的 第 i 项 的 能力名 等于 能力名：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

能力_查询 接受 能力名：
    做 能力统计_初始化。
    全局_能力统计 的 查询次数 为 全局_能力统计 的 查询次数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 能力名 等于 能力名 并且 能力列表 的 第 i 项 的 已启用：
            若 能力列表 的 第 i 项 的 健康状态 不等于 能力_健康_故障：
                做 追加 用 结果、能力列表 的 第 i 项。
                全局_能力统计 的 匹配次数 为 全局_能力统计 的 匹配次数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 j 为 0。
    只要 j 小于 做 计数 用 结果 减 1：
        可变 k 为 0。
        只要 k 小于 做 计数 用 结果 减 1 减 j：
            若 结果 的 第 k 项 的 熟练度 小于 结果 的 第 k 加 1 项 的 熟练度：
                tmp 为 结果 的 第 k 项。
                结果 的 第 k 项 为 结果 的 第 k 加 1 项。
                结果 的 第 k 加 1 项 为 tmp。
            完。
            k 为 k 加 1。
        完。
        j 为 j 加 1。
    完。
    返还 结果。
完。

能力_查询_按类别 接受 类别名：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 类别 等于 类别名 并且 能力列表 的 第 i 项 的 已启用：
            做 追加 用 结果、能力列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

能力_最佳智能体 接受 能力名：
    候选 为 做 能力_查询 用 能力名。
    若 做 计数 用 候选 大于 0：
        返还 候选 的 第 0 项 的 智能体名。
    完。
    返还 空。
完。

能力_负载均衡选择 接受 能力名：
    做 能力统计_初始化。
    全局_能力统计 的 调度次数 为 全局_能力统计 的 调度次数 加 1。
    候选 为 做 能力_查询 用 能力名。
    若 做 计数 用 候选 等于 0：
        返还 空。
    完。
    可变 最佳 为 0。
    可变 最佳分数 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 候选：
        负载比 为 0。
        若 候选 的 第 i 项 的 最大并发 大于 0：
            负载比 为 (候选 的 第 i 项 的 最大并发 减 候选 的 第 i 项 的 当前负载) 乘 100 除以 候选 的 第 i 项 的 最大并发。
        完。
        分数 为 候选 的 第 i 项 的 熟练度 加 负载比 乘 2。
        若 候选 的 第 i 项 的 健康状态 等于 能力_健康_降级：
            分数 为 分数 除以 2。
        完。
        若 分数 大于 最佳分数：
            最佳分数 为 分数。
            最佳 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 候选 的 第 最佳 项 的 智能体名。
完。

能力_记录成功 接受 智能体名、能力名：
    idx 为 做 能力_查找索引 用 智能体名、能力名。
    若 idx 小于 0：
        返还。
    完。
    能力列表 的 第 idx 项 的 调用次数 为 能力列表 的 第 idx 项 的 调用次数 加 1。
    能力列表 的 第 idx 项 的 成功次数 为 能力列表 的 第 idx 项 的 成功次数 加 1。
    能力列表 的 第 idx 项 的 熟练度 为 能力列表 的 第 idx 项 的 熟练度 加 能力_成功提升。
    若 能力列表 的 第 idx 项 的 熟练度 大于 能力_最大熟练度：
        能力列表 的 第 idx 项 的 熟练度 为 能力_最大熟练度。
    完。
    能力列表 的 第 idx 项 的 最后调用时间 为 做 系统_时间。
    做 能力_更新健康 用 idx。
完。

能力_记录失败 接受 智能体名、能力名：
    idx 为 做 能力_查找索引 用 智能体名、能力名。
    若 idx 小于 0：
        返还。
    完。
    能力列表 的 第 idx 项 的 调用次数 为 能力列表 的 第 idx 项 的 调用次数 加 1。
    能力列表 的 第 idx 项 的 失败次数 为 能力列表 的 第 idx 项 的 失败次数 加 1。
    能力列表 的 第 idx 项 的 熟练度 为 能力列表 的 第 idx 项 的 熟练度 减 能力_失败降低。
    若 能力列表 的 第 idx 项 的 熟练度 小于 能力_最小熟练度：
        能力列表 的 第 idx 项 的 熟练度 为 能力_最小熟练度。
    完。
    能力列表 的 第 idx 项 的 最后调用时间 为 做 系统_时间。
    做 能力_更新健康 用 idx。
完。

能力_记录耗时 接受 智能体名、能力名、耗时：
    idx 为 做 能力_查找索引 用 智能体名、能力名。
    若 idx 小于 0：
        返还。
    完。
    若 能力列表 的 第 idx 项 的 平均耗时 等于 0：
        能力列表 的 第 idx 项 的 平均耗时 为 耗时。
    否则：
        能力列表 的 第 idx 项 的 平均耗时 为 (能力列表 的 第 idx 项 的 平均耗时 乘 7 加 耗时) 除以 8。
    完。
完。

能力_更新健康 接受 idx：
    若 idx 小于 0 或者 idx 大于或等于 做 计数 用 能力列表：
        返还。
    完。
    cap 为 能力列表 的 第 idx 项。
    若 cap 的 调用次数 小于 5：
        返还。
    完。
    成功率 为 cap 的 成功次数 乘 100 除以 cap 的 调用次数。
    若 成功率 大于或等于 90：
        能力列表 的 第 idx 项 的 健康状态 为 能力_健康_正常。
    否则若 成功率 大于或等于 50：
        能力列表 的 第 idx 项 的 健康状态 为 能力_健康_降级。
    否则：
        能力列表 的 第 idx 项 的 健康状态 为 能力_健康_故障。
    完。
完。

能力_熟练度衰减 接受 零参：
    做 能力统计_初始化。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 已启用 并且 能力列表 的 第 i 项 的 熟练度 大于 能力_最小熟练度：
            能力列表 的 第 i 项 的 熟练度 为 能力列表 的 第 i 项 的 熟练度 减 能力_衰减率。
            全局_能力统计 的 衰减次数 为 全局_能力统计 的 衰减次数 加 1。
        完。
        i 为 i 加 1。
    完。
完。

能力_协商 接受 请求方、能力名、最低熟练度：
    做 能力统计_初始化。
    全局_能力统计 的 协商次数 为 全局_能力统计 的 协商次数 加 1。
    候选 为 做 能力_查询 用 能力名。
    可变 i 为 0。
    只要 i 小于 做 计数 用 候选：
        若 候选 的 第 i 项 的 熟练度 大于或等于 最低熟练度 并且 候选 的 第 i 项 的 当前负载 小于 候选 的 第 i 项 的 最大并发：
            返还 候选 的 第 i 项 的 智能体名。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

能力_确保类别 接受 类别名：
    若 类别名 等于 「」：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 类别数量：
        若 类别列表 的 第 i 项 的 名称 等于 类别名：
            返还。
        完。
        i 为 i 加 1。
    完。
    若 类别数量 大于或等于 能力_最大类别：
        返还。
    完。
    cat 为 新建 能力类别。
    cat 的 名称 为 类别名。
    cat 的 描述 为 「」。
    cat 的 能力数 为 0。
    做 追加 用 类别列表、cat。
    类别数量 为 类别数量 加 1。
完。

能力_获取智能体列表 接受 智能体名：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 智能体名 等于 智能体名 并且 能力列表 的 第 i 项 的 已启用：
            做 追加 用 结果、能力列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

能力_总数 接受 零参：
    返还 做 计数 用 能力列表。
完。

能力_活跃总数 接受 零参：
    可变 计 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 已启用：
            计 为 计 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计。
完。

能力_打印统计 接受 零参：
    做 能力统计_初始化。
    做 打印行 用 「=== 能力注册统计 ===」。
    做 打印行 用 「注册总数: 」 加 做 整数转字符串 用 全局_能力统计 的 注册总数。
    做 打印行 用 「活跃数: 」 加 做 整数转字符串 用 做 能力_活跃总数。
    做 打印行 用 「查询次数: 」 加 做 整数转字符串 用 全局_能力统计 的 查询次数。
    做 打印行 用 「匹配次数: 」 加 做 整数转字符串 用 全局_能力统计 的 匹配次数。
    做 打印行 用 「调度次数: 」 加 做 整数转字符串 用 全局_能力统计 的 调度次数。
    做 打印行 用 「衰减次数: 」 加 做 整数转字符串 用 全局_能力统计 的 衰减次数。
    做 打印行 用 「注销次数: 」 加 做 整数转字符串 用 全局_能力统计 的 注销次数。
    做 打印行 用 「协商次数: 」 加 做 整数转字符串 用 全局_能力统计 的 协商次数。
    做 打印行 用 「类别数: 」 加 做 整数转字符串 用 类别数量。
完。

能力_批量启用 接受 智能体名：
    可变 操作数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 智能体名 等于 智能体名 并且 非 能力列表 的 第 i 项 的 已启用：
            能力列表 的 第 i 项 的 已启用 为 真。
            操作数 为 操作数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 操作数。
完。

能力_批量禁用 接受 智能体名：
    可变 操作数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 智能体名 等于 智能体名 并且 能力列表 的 第 i 项 的 已启用：
            能力列表 的 第 i 项 的 已启用 为 假。
            操作数 为 操作数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 操作数。
完。

能力_导出快照 接受 零参：
    可变 快照 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        cap 为 能力列表 的 第 i 项。
        做 追加 用 快照、一本字典：名称 对应 cap 的 能力名、智能体 对应 cap 的 智能体名、熟练度 对应 cap 的 熟练度、调用 对应 cap 的 调用次数、成功 对应 cap 的 成功次数、失败 对应 cap 的 失败次数、健康 对应 cap 的 健康状态、启用 对应 cap 的 已启用。
        i 为 i 加 1。
    完。
    返还 快照。
完。

能力_健康报告 接受 零参：
    可变 正常数 为 0。
    可变 降级数 为 0。
    可变 故障数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 非 能力列表 的 第 i 项 的 已启用：
            i 为 i 加 1。
            继续。
        完。
        若 能力列表 的 第 i 项 的 健康状态 等于 能力_健康_正常：
            正常数 为 正常数 加 1。
        否则若 能力列表 的 第 i 项 的 健康状态 等于 能力_健康_降级：
            降级数 为 降级数 加 1。
        否则：
            故障数 为 故障数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：正常数、降级数、故障数。
完。

能力_重置统计 接受 零参：
    做 能力统计_初始化。
    全局_能力统计 的 注册总数 为 0。
    全局_能力统计 的 查询次数 为 0。
    全局_能力统计 的 匹配次数 为 0。
    全局_能力统计 的 调度次数 为 0。
    全局_能力统计 的 衰减次数 为 0。
    全局_能力统计 的 注销次数 为 0。
    全局_能力统计 的 协商次数 为 0。
完。

能力_获取类别数 接受 零参：
    返还 类别数量。
完。

能力_获取注册数 接受 零参：
    返还 全局_能力统计 的 注册总数。
完。

能力_获取查询数 接受 零参：
    返还 全局_能力统计 的 查询次数。
完。

能力_获取调度数 接受 零参：
    返还 全局_能力统计 的 调度次数。
完。

能力_获取协商数 接受 零参：
    返还 全局_能力统计 的 协商次数。
完。

能力_平均熟练度 接受 零参：
    可变 总和 为 0。
    可变 计 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 已启用：
            总和 为 总和 加 能力列表 的 第 i 项 的 熟练度。
            计 为 计 加 1。
        完。
        i 为 i 加 1。
    完。
    若 计 等于 0：
        返还 0。
    完。
    返还 总和 除以 计。
完。

能力_最高熟练度 接受 零参：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 能力列表 的 第 i 项 的 已启用 并且 能力列表 的 第 i 项 的 熟练度 大于 最大：
            最大 为 能力列表 的 第 i 项 的 熟练度。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

本章 收束。
