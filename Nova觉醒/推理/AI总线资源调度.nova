《AI总线资源调度》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《..·中枢·字符串替换》。

引用《.·定点Q算术》。

引用《.·节点聚类》。

引用《.·权重持久》。

引用《.·马尔可夫链》。

引用《.·AI总线初始化》。

引用《.·行为预测器》。

引用《.·注视点模型》。

引用《.·滚动预测》。

引用《..·中枢·激活函数层》。

Q_SHIFT 为 10。

Q_ONE 为 1024。

Q_HALF 为 512。

动作_空闲 为 0。

动作_点击 为 1。

动作_滚动 为 2。

学习率_默认 为 10。

学习率_最大 为 50。

学习率_最小 为 1。

动量_默认 为 9。

预取_最大队列 为 64。

预取_状态_等待 为 0。

预取_状态_加载中 为 1。

预取_状态_完成 为 2。

预取_状态_失败 为 3。

资源类型_脚本 为 1。

资源类型_样式表 为 2。

资源类型_图片 为 3。

资源类型_字体 为 4。

资源类型_数据 为 5。

资源类型_视频 为 6。

错误_无效参数 为 负 1。

错误_未初始化 为 负 2。

错误_队列已满 为 负 7。

预取请求 是 一种 事物，含 url、资源类型、优先级分数、状态、请求时间、完成时间、重试次数、大小估计。

预取调度器 是 一种 事物，含 请求队列、队列长度、已完成数、已失败数、命中次数、未命中次数、总节省时间、并发限制、当前并发数。

在线学习状态 是 一种 事物，含 累计损失、损失历史、历史长度、最佳损失、无改善步数、早停阈值、学习率衰减因子、梯度裁剪阈值、权重衰减。

AI总线_资源优先级 接受 bus、url：
    若 bus 等于 0 或者 url 等于 0：
        返还 Q_HALF。
    完。
    若 bus 的 降级模式 大于或等于 2：
        返还 做 AI总线_资源优先级_规则 用 url。
    完。
    url嵌入 为 做 语义嵌入_计算URL嵌入 用 bus 的 语义引擎、url。
    若 url嵌入 等于 0：
        返还 Q_HALF。
    完。
    输入 为 做 Tensor_创建 用 1、10。
    可变 i 为 0。
    只要 i 小于 10 并且 i 小于 url嵌入 的 列数：
        做 Tensor_写 用 输入、0、i、做 Tensor_读 用 url嵌入、0、i。
        i 为 i 加 1。
    完。
    做 Tensor_释放 用 url嵌入。
    输出 为 做 Nova多层感知器_前向 用 bus 的 资源优先级网络、输入。
    优先级 为 做 Tensor_读 用 输出、0、0。
    做 Tensor_释放 用 输入。
    做 Tensor_释放 用 输出。
    bus 的 推理次数 为 bus 的 推理次数 加 1。
    若 优先级 小于 0：
        优先级 为 0。
    完。
    若 优先级 大于 Q_ONE：
        优先级 为 Q_ONE。
    完。
    返还 优先级。
完。

AI总线_资源优先级_规则 接受 url：
    若 url 等于 0：
        返还 Q_HALF。
    完。
    若 做 以结尾 用 url、「.js」 等于 真 或者 做 以结尾 用 url、「.nova」 等于 真：
        返还 Q_ONE 减 100。
    完。
    若 做 以结尾 用 url、「.css」 等于 真：
        返还 Q_ONE 减 200。
    完。
    若 做 以结尾 用 url、「.woff2」 等于 真 或者 做 以结尾 用 url、「.ttf」 等于 真：
        返还 Q_HALF 加 100。
    完。
    若 做 以结尾 用 url、「.png」 等于 真 或者 做 以结尾 用 url、「.jpg」 等于 真 或者 做 以结尾 用 url、「.webp」 等于 真：
        返还 Q_HALF 减 200。
    完。
    若 做 以结尾 用 url、「.mp4」 等于 真 或者 做 以结尾 用 url、「.webm」 等于 真：
        返还 100。
    完。
    返还 Q_HALF。
完。

AI总线_批量资源排序 接受 bus、url列表、数量：
    若 bus 等于 0 或者 url列表 等于 0 或者 数量 小于或等于 0：
        返还 错误_无效参数。
    完。
    可变 优先级列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 数量：
        优先级 为 做 AI总线_资源优先级 用 bus、url列表 的 第 i 项。
        做 追加 用 优先级列表、优先级。
        i 为 i 加 1。
    完。
    可变 j 为 0。
    只要 j 小于 数量 减 1：
        可变 k 为 0。
        只要 k 小于 数量 减 j 减 1：
            若 优先级列表 的 第 k 项 小于 优先级列表 的 第 k 加 1 项：
                临时优先级 为 优先级列表 的 第 k 项。
                做 设置 用 优先级列表、k、优先级列表 的 第 k 加 1 项。
                做 设置 用 优先级列表、k 加 1、临时优先级。
                临时url 为 url列表 的 第 k 项。
                做 设置 用 url列表、k、url列表 的 第 k 加 1 项。
                做 设置 用 url列表、k 加 1、临时url。
            完。
            k 为 k 加 1。
        完。
        j 为 j 加 1。
    完。
    返还 0。
完。

AI总线_布局预测 接受 bus、元素特征：
    若 bus 等于 0 或者 bus 的 布局预测网络 等于 0：
        返还 0。
    完。
    若 bus 的 降级模式 大于或等于 2：
        返还 0。
    完。
    预测 为 做 Nova多层感知器_前向 用 bus 的 布局预测网络、元素特征。
    bus 的 推理次数 为 bus 的 推理次数 加 1。
    返还 预测。
完。

AI总线_预测下一步 接受 bus：
    若 bus 等于 0 或者 bus 的 行为预测器 等于 0：
        返还 动作_空闲。
    完。
    mk1_预测 为 做 MarkovChain_预测 用 bus 的 行为预测器 的 马尔可夫1阶。
    hom_预测 为 做 HighOrderMarkov_预测 用 bus 的 行为预测器 的 马尔可夫3阶。
    可变 序列预测 为 bus 的 行为预测器 的 上次预测。
    上下文隐藏 为 做 行为预测器_当前上下文隐藏 用 bus 的 行为预测器。
    若 上下文隐藏 不等于 0：
        序列预测 为 做 行为预测器_输出头预测 用 bus 的 行为预测器、上下文隐藏。
        做 Tensor_释放 用 上下文隐藏。
    完。
    若 序列预测 小于 0：
        返还 mk1_预测。
    完。
    投票 为 做 Tensor_创建 用 1、动作数量。
    做 Tensor_写 用 投票、0、mk1_预测、做 Tensor_读 用 投票、0、mk1_预测 加 307。
    做 Tensor_写 用 投票、0、hom_预测、做 Tensor_读 用 投票、0、hom_预测 加 205。
    做 Tensor_写 用 投票、0、序列预测、做 Tensor_读 用 投票、0、序列预测 加 512。
    可变 最终最大 为 0。
    可变 最终预测 为 0。
    可变 j 为 0。
    只要 j 小于 动作数量：
        v 为 做 Tensor_读 用 投票、0、j。
        若 v 大于 最终最大：
            最终最大 为 v。
            最终预测 为 j。
        完。
        j 为 j 加 1。
    完。
    做 Tensor_释放 用 投票。
    bus 的 行为预测器 的 上次预测 为 最终预测。
    返还 最终预测。
完。

AI总线_预测滚动位置 接受 bus、毫秒后：
    若 bus 等于 0：
        返还 0。
    完。
    返还 做 滚动预测器_预测位置 用 bus 的 滚动预测器、毫秒后。
完。

AI总线_预测下一页面 接受 bus：
    若 bus 等于 0 或者 bus 的 行为预测器 等于 0：
        返还 空。
    完。
    预测动作 为 做 AI总线_预测下一步 用 bus。
    若 预测动作 等于 动作_点击：
        返还 做 行为预测器_获取热门目标 用 bus 的 行为预测器。
    完。
    返还 空。
完。

可变 全局预取调度器 为 0。

预取调度器_创建 接受 零参：
    调度器 为 新建 预取调度器。
    调度器 的 请求队列 为 一组：（空）。
    调度器 的 队列长度 为 0。
    调度器 的 已完成数 为 0。
    调度器 的 已失败数 为 0。
    调度器 的 命中次数 为 0。
    调度器 的 未命中次数 为 0。
    调度器 的 总节省时间 为 0。
    调度器 的 并发限制 为 4。
    调度器 的 当前并发数 为 0。
    全局预取调度器 为 调度器。
    返还 调度器。
完。

预取调度器_销毁 接受 零参：
    若 全局预取调度器 等于 0：
        返还。
    完。
    全局预取调度器 为 0。
完。

预取调度器_提交 接受 bus、url、资源类型：
    若 全局预取调度器 等于 0：
        返还 错误_未初始化。
    完。
    调度器 为 全局预取调度器。
    若 调度器 的 队列长度 大于或等于 预取_最大队列：
        返还 错误_队列已满。
    完。
    可变 i 为 0。
    只要 i 小于 调度器 的 队列长度：
        若 调度器 的 请求队列 的 第 i 项 的 url 等于 url：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    优先级 为 做 AI总线_资源优先级 用 bus、url。
    请求 为 新建 预取请求。
    请求 的 url 为 url。
    请求 的 资源类型 为 资源类型。
    请求 的 优先级分数 为 优先级。
    请求 的 状态 为 预取_状态_等待。
    请求 的 请求时间 为 bus 的 帧计数。
    请求 的 完成时间 为 0。
    请求 的 重试次数 为 0。
    请求 的 大小估计 为 0。
    做 追加 用 调度器 的 请求队列、请求。
    调度器 的 队列长度 为 调度器 的 队列长度 加 1。
    返还 0。
完。

预取调度器_处理队列 接受 bus：
    若 全局预取调度器 等于 0 或者 bus 等于 0：
        返还。
    完。
    调度器 为 全局预取调度器。
    可变 i 为 0。
    只要 i 小于 调度器 的 队列长度 并且 调度器 的 当前并发数 小于 调度器 的 并发限制：
        若 调度器 的 请求队列 的 第 i 项 的 状态 等于 预取_状态_等待：
            调度器 的 请求队列 的 第 i 项 的 状态 为 预取_状态_加载中。
            调度器 的 当前并发数 为 调度器 的 当前并发数 加 1。
            做 打印 用 做 字符串拼接 用 「[AI预取] 开始加载: 」、调度器 的 请求队列 的 第 i 项 的 url。
        完。
        i 为 i 加 1。
    完。
完。

预取调度器_标记完成 接受 url、耗时微秒：
    若 全局预取调度器 等于 0：
        返还。
    完。
    调度器 为 全局预取调度器。
    可变 i 为 0。
    只要 i 小于 调度器 的 队列长度：
        若 调度器 的 请求队列 的 第 i 项 的 url 等于 url：
            调度器 的 请求队列 的 第 i 项 的 状态 为 预取_状态_完成。
            调度器 的 已完成数 为 调度器 的 已完成数 加 1。
            调度器 的 当前并发数 为 调度器 的 当前并发数 减 1。
            调度器 的 总节省时间 为 调度器 的 总节省时间 加 耗时微秒。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

预取调度器_记录命中 接受 url：
    若 全局预取调度器 等于 0：
        返还。
    完。
    调度器 为 全局预取调度器。
    可变 i 为 0。
    只要 i 小于 调度器 的 队列长度：
        若 调度器 的 请求队列 的 第 i 项 的 url 等于 url 并且 调度器 的 请求队列 的 第 i 项 的 状态 等于 预取_状态_完成：
            调度器 的 命中次数 为 调度器 的 命中次数 加 1。
            返还。
        完。
        i 为 i 加 1。
    完。
    调度器 的 未命中次数 为 调度器 的 未命中次数 加 1。
完。

AI总线_帧开始 接受 bus：
    若 bus 等于 0：
        返还。
    完。
    bus 的 推理次数 为 0。
    bus 的 AI实际耗时 为 0。
完。

AI总线_帧结束 接受 bus、帧耗时：
    若 bus 等于 0：
        返还。
    完。
    bus 的 上帧耗时 为 帧耗时。
    bus 的 帧计数 为 bus 的 帧计数 加 1。
    若 帧耗时 大于 0：
        bus 的 当前FPS 为 1000000 除以 帧耗时。
    完。
    bus 的 累计推理次数 为 bus 的 累计推理次数 加 bus 的 推理次数。
    若 bus 的 统计 不等于 0：
        bus 的 统计 的 总推理调用次数 为 bus 的 统计 的 总推理调用次数 加 bus 的 推理次数。
        bus 的 统计 的 总推理耗时微秒 为 bus 的 统计 的 总推理耗时微秒 加 bus 的 AI实际耗时。
        若 bus 的 AI实际耗时 大于 bus 的 统计 的 最大推理耗时：
            bus 的 统计 的 最大推理耗时 为 bus 的 AI实际耗时。
        完。
        若 bus 的 AI实际耗时 小于 bus 的 统计 的 最小推理耗时：
            bus 的 统计 的 最小推理耗时 为 bus 的 AI实际耗时。
        完。
        若 bus 的 统计 的 总推理调用次数 大于 0：
            bus 的 统计 的 平均推理耗时 为 bus 的 统计 的 总推理耗时微秒 除以 bus 的 统计 的 总推理调用次数。
        完。
    完。
    做 质量分配器_帧反馈 用 bus 的 质量分配器、帧耗时。
    做 AI总线_调整推理预算 用 bus、帧耗时。
    若 bus 的 帧计数 取余于 60 等于 0：
        做 AI总线_心跳检测 用 bus。
    完。
    若 bus 的 帧计数 取余于 100 等于 0：
        做 AI总线_在线学习步 用 bus。
    完。
    若 bus 的 帧计数 取余于 1000 等于 0：
        做 AI总线_保存模型 用 bus。
    完。
    做 预取调度器_处理队列 用 bus。
完。

AI总线_调整推理预算 接受 bus、帧耗时：
    若 bus 等于 0 或者 帧耗时 等于 0：
        返还。
    完。
    AI占比 为 bus 的 AI实际耗时 乘 100 除以 帧耗时。
    若 AI占比 大于 12：
        若 bus 的 AI推理预算 大于 500：
            bus 的 AI推理预算 为 bus 的 AI推理预算 减 100。
        完。
        bus 的 推理超时次数 为 bus 的 推理超时次数 加 1。
        若 bus 的 统计 不等于 0：
            bus 的 统计 的 推理超时次数 为 bus 的 统计 的 推理超时次数 加 1。
        完。
    否则若 AI占比 小于 6：
        若 bus 的 AI推理预算 小于 8000：
            bus 的 AI推理预算 为 bus 的 AI推理预算 加 50。
        完。
    完。
    若 bus 的 当前FPS 小于 30 并且 bus 的 AI推理预算 大于 1000：
        bus 的 AI推理预算 为 bus 的 AI推理预算 减 200。
        做 打印 用 「[AI总线] 低FPS紧急降低推理预算」。
    完。
完。

可变 全局学习状态 为 0。

在线学习状态_创建 接受 零参：
    状态 为 新建 在线学习状态。
    状态 的 累计损失 为 0。
    状态 的 损失历史 为 一组：（空）。
    状态 的 历史长度 为 0。
    状态 的 最佳损失 为 999999。
    状态 的 无改善步数 为 0。
    状态 的 早停阈值 为 50。
    状态 的 学习率衰减因子 为 950。
    状态 的 梯度裁剪阈值 为 Q_ONE 乘 5。
    状态 的 权重衰减 为 1。
    全局学习状态 为 状态。
    返还 状态。
完。

AI总线_在线学习步 接受 bus：
    若 bus 等于 0：
        返还。
    完。
    若 全局学习状态 等于 0：
        做 在线学习状态_创建。
    完。
    学习 为 全局学习状态。
    bus 的 学习步数 为 bus 的 学习步数 加 1。
    准确率 为 做 行为预测器_准确率 用 bus 的 行为预测器。
    损失 为 Q_ONE 减 准确率。
    学习 的 累计损失 为 学习 的 累计损失 加 损失。
    做 追加 用 学习 的 损失历史、损失。
    学习 的 历史长度 为 学习 的 历史长度 加 1。
    若 损失 小于 学习 的 最佳损失：
        学习 的 最佳损失 为 损失。
        学习 的 无改善步数 为 0。
    否则：
        学习 的 无改善步数 为 学习 的 无改善步数 加 1。
    完。
    若 学习 的 无改善步数 大于 20：
        bus 的 优化器 的 学习率 为 bus 的 优化器 的 学习率 乘 学习 的 学习率衰减因子 除以 Q_ONE。
        若 bus 的 优化器 的 学习率 小于 学习率_最小：
            bus 的 优化器 的 学习率 为 学习率_最小。
        完。
    否则若 准确率 小于 Q_HALF：
        bus 的 优化器 的 学习率 为 学习率_默认 乘 2。
        若 bus 的 优化器 的 学习率 大于 学习率_最大：
            bus 的 优化器 的 学习率 为 学习率_最大。
        完。
    否则：
        bus 的 优化器 的 学习率 为 学习率_默认。
    完。
    若 bus 的 统计 不等于 0：
        bus 的 统计 的 在线学习次数 为 bus 的 统计 的 在线学习次数 加 1。
    完。
完。

AI总线_保存模型 接受 bus：
    若 bus 等于 0：
        返还 错误_未初始化。
    完。
    目录 为 bus 的 模型目录。
    做 打印 用 「[AI总线] 正在保存模型...」。
    若 bus 的 布局预测网络 不等于 0：
        做 Nova多层感知器_保存 用 bus 的 布局预测网络、做 字符串拼接 用 目录、「布局预测.bin」。
    完。
    若 bus 的 注意力网络 不等于 0：
        做 Nova多层感知器_保存 用 bus 的 注意力网络、做 字符串拼接 用 目录、「注意力.bin」。
    完。
    若 bus 的 语义分类网络 不等于 0：
        做 Nova多层感知器_保存 用 bus 的 语义分类网络、做 字符串拼接 用 目录、「语义分类.bin」。
    完。
    若 bus 的 渲染质量网络 不等于 0：
        做 Nova多层感知器_保存 用 bus 的 渲染质量网络、做 字符串拼接 用 目录、「渲染质量.bin」。
    完。
    若 bus 的 资源优先级网络 不等于 0：
        做 Nova多层感知器_保存 用 bus 的 资源优先级网络、做 字符串拼接 用 目录、「资源优先级.bin」。
    完。
    若 bus 的 统计 不等于 0：
        bus 的 统计 的 模型保存次数 为 bus 的 统计 的 模型保存次数 加 1。
    完。
    做 打印 用 「[AI总线] ✓ 模型保存完成」。
    返还 0。
完。

AI总线_加载模型 接受 bus：
    若 bus 等于 0：
        返还 错误_未初始化。
    完。
    目录 为 bus 的 模型目录。
    做 打印 用 「[AI总线] 正在加载模型...」。
    可变 成功数 为 0。
    若 bus 的 布局预测网络 不等于 0：
        r 为 做 Nova多层感知器_加载 用 bus 的 布局预测网络、做 字符串拼接 用 目录、「布局预测.bin」。
        若 r 等于 0：
            成功数 为 成功数 加 1。
        完。
    完。
    若 bus 的 注意力网络 不等于 0：
        r 为 做 Nova多层感知器_加载 用 bus 的 注意力网络、做 字符串拼接 用 目录、「注意力.bin」。
        若 r 等于 0：
            成功数 为 成功数 加 1。
        完。
    完。
    若 bus 的 语义分类网络 不等于 0：
        r 为 做 Nova多层感知器_加载 用 bus 的 语义分类网络、做 字符串拼接 用 目录、「语义分类.bin」。
        若 r 等于 0：
            成功数 为 成功数 加 1。
        完。
    完。
    若 bus 的 渲染质量网络 不等于 0：
        r 为 做 Nova多层感知器_加载 用 bus 的 渲染质量网络、做 字符串拼接 用 目录、「渲染质量.bin」。
        若 r 等于 0：
            成功数 为 成功数 加 1。
        完。
    完。
    若 bus 的 资源优先级网络 不等于 0：
        r 为 做 Nova多层感知器_加载 用 bus 的 资源优先级网络、做 字符串拼接 用 目录、「资源优先级.bin」。
        若 r 等于 0：
            成功数 为 成功数 加 1。
        完。
    完。
    做 打印 用 做 字符串拼接 用 「[AI总线] 模型加载: 」、做 字符串拼接 用 做 转字符串 用 成功数、「/5」。
    若 成功数 等于 0：
        返还 错误_无效参数。
    完。
    返还 0。
完。

预取调度器_打印统计 接受 零参：
    若 全局预取调度器 等于 0：
        做 打印 用 「[AI预取] 调度器未初始化」。
        返还。
    完。
    调度器 为 全局预取调度器。
    做 打印 用 「[AI预取] ═══ 预取统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  队列长度: 」、做 转字符串 用 调度器 的 队列长度。
    做 打印 用 做 字符串拼接 用 「  已完成: 」、做 转字符串 用 调度器 的 已完成数。
    做 打印 用 做 字符串拼接 用 「  已失败: 」、做 转字符串 用 调度器 的 已失败数。
    做 打印 用 做 字符串拼接 用 「  命中次数: 」、做 转字符串 用 调度器 的 命中次数。
    做 打印 用 做 字符串拼接 用 「  未命中次数: 」、做 转字符串 用 调度器 的 未命中次数。
    可变 命中率 为 0。
    若 调度器 的 命中次数 加 调度器 的 未命中次数 大于 0：
        命中率 为 调度器 的 命中次数 乘 100 除以 (调度器 的 命中次数 加 调度器 的 未命中次数)。
    完。
    做 打印 用 做 字符串拼接 用 「  预取命中率: 」、做 字符串拼接 用 做 转字符串 用 命中率、「%」。
    做 打印 用 做 字符串拼接 用 「  节省总时间: 」、做 字符串拼接 用 做 转字符串 用 调度器 的 总节省时间、「μs」。
    做 打印 用 做 字符串拼接 用 「  当前并发数: 」、做 字符串拼接 用 做 转字符串 用 调度器 的 当前并发数、「/」、做 转字符串 用 调度器 的 并发限制。
    做 打印 用 「[AI预取] ═══════════════════」。
完。

在线学习_打印统计 接受 零参：
    若 全局学习状态 等于 0：
        做 打印 用 「[AI学习] 学习状态未初始化」。
        返还。
    完。
    学习 为 全局学习状态。
    做 打印 用 「[AI学习] ═══ 在线学习统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  训练步数: 」、做 转字符串 用 学习 的 历史长度。
    做 打印 用 做 字符串拼接 用 「  累计损失: 」、做 转字符串 用 学习 的 累计损失。
    做 打印 用 做 字符串拼接 用 「  最佳损失: 」、做 转字符串 用 学习 的 最佳损失。
    做 打印 用 做 字符串拼接 用 「  无改善步数: 」、做 转字符串 用 学习 的 无改善步数。
    做 打印 用 「[AI学习] ════════════════════」。
完。

本章 收束。
