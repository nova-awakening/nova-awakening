《滚动预测》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《.·通信渠道》。

引用《..·中枢·内核打印》。

引用《..·中枢·内存初始化》。

引用《.·定点Q算术》。

引用《.·权重持久》。

引用《..·中枢·整数转字符串》。

Q_SHIFT 为 10。

Q_ONE 为 1024。

Q_HALF 为 512。

Q_ZERO 为 0。

Q转整数 接受 v：
    返还 v 右移 Q_SHIFT。
完。

整数转Q 接受 v：
    返还 v 左移 Q_SHIFT。
完。

isqrt 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

绝对值 接受 x：
    若 x 小于 0：
        返还 负 x。
    完。
    返还 x。
完。

滚动窗口大小 为 16。

默认摩擦系数_Q10 为 51。

滚动预测器 是 一种 事物，含 历史位置、历史时间、窗口大小、有效长度、当前速度、当前加速度、摩擦系数、方向、kalman_x、kalman_v、kalman_P00、kalman_P01、kalman_P10、kalman_P11、kalman_Q_pos、kalman_Q_vel、kalman_R、模式、模式置信度、方向变换数、速度历史、速度历史索引。

滚动预测器_创建 接受 零参：
    sp 为 做 堆_分配 用 滚动预测器。
    sp 的 窗口大小 为 滚动窗口大小。
    sp 的 历史位置 为 做 堆_分配_数组 用 sp 的 窗口大小 乘 4。
    sp 的 历史时间 为 做 堆_分配_数组 用 sp 的 窗口大小 乘 8。
    sp 的 有效长度 为 0。
    sp 的 当前速度 为 0。
    sp 的 当前加速度 为 0。
    sp 的 摩擦系数 为 默认摩擦系数_Q10。
    sp 的 方向 为 0。
    sp 的 kalman_x 为 0。
    sp 的 kalman_v 为 0。
    sp 的 kalman_P00 为 Q_ONE。
    sp 的 kalman_P01 为 0。
    sp 的 kalman_P10 为 0。
    sp 的 kalman_P11 为 Q_ONE。
    sp 的 kalman_Q_pos 为 10。
    sp 的 kalman_Q_vel 为 100。
    sp 的 kalman_R 为 200。
    sp 的 模式 为 滚动模式_停止。
    sp 的 模式置信度 为 0。
    sp 的 方向变换数 为 0。
    sp 的 速度历史 为 做 堆_分配_数组 用 8 乘 4。
    sp 的 速度历史索引 为 0。
    返还 sp。
完。

滚动预测器_更新 接受 sp、当前Y、当前时间：
    若 sp 等于 0：
        返还。
    完。
    若 sp 的 有效长度 小于 sp 的 窗口大小：
        做 内存_则32 用 sp 的 历史位置 加 sp 的 有效长度 乘 4、当前Y。
        做 内存_则64 用 sp 的 历史时间 加 sp 的 有效长度 乘 8、当前时间。
        sp 的 有效长度 为 sp 的 有效长度 加 1。
    否则：
        可变 i 为 0。
        只要 i 小于 sp 的 窗口大小 减 1：
            做 内存_则32 用 sp 的 历史位置 加 i 乘 4、做 内存_读32 用 sp 的 历史位置 加 (i 加 1) 乘 4。
            做 内存_则64 用 sp 的 历史时间 加 i 乘 8、做 内存_读64 用 sp 的 历史时间 加 (i 加 1) 乘 8。
            i 为 i 加 1。
        完。
        做 内存_则32 用 sp 的 历史位置 加 (sp 的 窗口大小 减 1) 乘 4、当前Y。
        做 内存_则64 用 sp 的 历史时间 加 (sp 的 窗口大小 减 1) 乘 8、当前时间。
    完。
    旧方向 为 sp 的 方向。
    若 sp 的 有效长度 大于或等于 3：
        n 为 sp 的 有效长度。
        y2 为 做 内存_读32有符号 用 sp 的 历史位置 加 (n 减 1) 乘 4。
        y1 为 做 内存_读32有符号 用 sp 的 历史位置 加 (n 减 2) 乘 4。
        y0 为 做 内存_读32有符号 用 sp 的 历史位置 加 (n 减 3) 乘 4。
        t2 为 做 内存_读64 用 sp 的 历史时间 加 (n 减 1) 乘 8。
        t1 为 做 内存_读64 用 sp 的 历史时间 加 (n 减 2) 乘 8。
        t0 为 做 内存_读64 用 sp 的 历史时间 加 (n 减 3) 乘 8。
        dt1 为 t2 减 t1。
        dt0 为 t1 减 t0。
        若 dt1 小于或等于 0：
            dt1 为 1。
        完。
        若 dt0 小于或等于 0：
            dt0 为 1。
        完。
        v1 为 做 Q除 用 (y2 减 y1) 乘 Q_ONE、dt1 乘 Q_ONE 除以 1000。
        v0 为 做 Q除 用 (y1 减 y0) 乘 Q_ONE、dt0 乘 Q_ONE 除以 1000。
        sp 的 当前速度 为 v1。
        sp 的 当前加速度 为 做 Q除 用 (v1 减 v0) 乘 Q_ONE、(dt1 加 dt0) 除以 2 乘 Q_ONE 除以 1000。
        若 v1 大于 50：
            sp 的 方向 为 1。
        完。
        做 否则如果 用 v1 小于 负 50。
        块：
            sp 的 方向 为 负 1。
        完。
        否则。
        块：
            sp 的 方向 为 0。
        完。
        若 sp 的 方向 不等于 0 并且 sp 的 方向 不等于 旧方向 并且 旧方向 不等于 0：
            sp 的 方向变换数 为 sp 的 方向变换数 加 1。
        完。
        vidx 为 sp 的 速度历史索引 取余于 8。
        做 内存_则32 用 sp 的 速度历史 加 vidx 乘 4、v1。
        sp 的 速度历史索引 为 sp 的 速度历史索引 加 1。
    完。
    做 滚动预测器_Kalman更新 用 sp、当前Y。
    做 滚动预测器_AI检测模式 用 sp。
完。

滚动预测器_Kalman更新 接受 sp、观测位置：
    dt 为 16。
    x_pred 为 sp 的 kalman_x 加 做 Q乘 用 sp 的 kalman_v、dt。
    v_pred 为 sp 的 kalman_v。
    P00_pred 为 sp 的 kalman_P00 加 做 Q乘 用 dt、sp 的 kalman_P10 加 做 Q乘 用 dt、sp 的 kalman_P01 加 做 Q乘 用 做 Q乘 用 dt、dt、sp 的 kalman_P11 加 sp 的 kalman_Q_pos。
    P01_pred 为 sp 的 kalman_P01 加 做 Q乘 用 dt、sp 的 kalman_P11。
    P10_pred 为 sp 的 kalman_P10 加 做 Q乘 用 dt、sp 的 kalman_P11。
    P11_pred 为 sp 的 kalman_P11 加 sp 的 kalman_Q_vel。
    残差 为 做 整数转Q 用 观测位置 减 x_pred。
    S 为 P00_pred 加 sp 的 kalman_R。
    若 S 等于 0：
        S 为 1。
    完。
    K0 为 做 Q除 用 P00_pred、S。
    K1 为 做 Q除 用 P10_pred、S。
    sp 的 kalman_x 为 x_pred 加 做 Q乘 用 K0、残差。
    sp 的 kalman_v 为 v_pred 加 做 Q乘 用 K1、残差。
    sp 的 kalman_P00 为 做 Q乘 用 Q_ONE 减 K0、P00_pred。
    sp 的 kalman_P01 为 做 Q乘 用 Q_ONE 减 K0、P01_pred。
    sp 的 kalman_P10 为 P10_pred 减 做 Q乘 用 K1、P00_pred。
    sp 的 kalman_P11 为 P11_pred 减 做 Q乘 用 K1、P01_pred。
完。

滚动预测器_Kalman预测 接受 sp、毫秒后：
    dt 为 毫秒后 乘 Q_ONE 除以 1000。
    返还 做 Q转整数 用 sp 的 kalman_x 加 做 Q乘 用 sp 的 kalman_v、dt。
完。

滚动预测器_物理预测 接受 sp、毫秒后：
    若 sp 等于 0 或者 sp 的 有效长度 等于 0：
        返还 0。
    完。
    当前Y 为 做 内存_读32有符号 用 sp 的 历史位置 加 (sp 的 有效长度 减 1) 乘 4。
    t 为 毫秒后 乘 Q_ONE 除以 1000。
    位移_速度 为 做 Q乘 用 sp 的 当前速度、t。
    位移_加速 为 做 Q乘 用 Q_HALF、做 Q乘 用 sp 的 当前加速度、做 Q乘 用 t、t。
    摩擦力 为 做 Q乘 用 sp 的 摩擦系数、做 Q乘 用 做 绝对值 用 sp 的 当前速度、t。
    可变 摩擦修正 为 0。
    若 sp 的 当前速度 大于 0：
        摩擦修正 为 负 摩擦力。
    完。
    做 否则如果 用 sp 的 当前速度 小于 0。
    块：
        摩擦修正 为 摩擦力。
    完。
    返还 当前Y 加 做 Q转整数 用 位移_速度 加 位移_加速 加 摩擦修正。
完。

滚动模式_停止 为 0。

滚动模式_快速 为 1。

滚动模式_慢速 为 2。

滚动模式_往复 为 3。

滚动模式_惯性 为 4。

滚动预测器_AI检测模式 接受 sp：
    若 sp 的 速度历史索引 小于 4：
        sp 的 模式 为 滚动模式_停止。
        sp 的 模式置信度 为 0。
        返还。
    完。
    可变 速度绝对和 为 0。
    可变 单调递减数 为 0。
    可变 样本数 为 0。
    起始 为 0。
    若 sp 的 速度历史索引 大于 8：
        起始 为 sp 的 速度历史索引 减 8。
    完。
    可变 i 为 起始。
    可变 上一绝对 为 0。
    只要 i 小于 sp 的 速度历史索引：
        idx 为 i 取余于 8。
        v 为 做 内存_读32有符号 用 sp 的 速度历史 加 idx 乘 4。
        abs_v 为 做 绝对值 用 v。
        速度绝对和 为 速度绝对和 加 abs_v。
        若 样本数 大于 0 并且 abs_v 小于 上一绝对：
            单调递减数 为 单调递减数 加 1。
        完。
        上一绝对 为 abs_v。
        样本数 为 样本数 加 1。
        i 为 i 加 1。
    完。
    若 样本数 等于 0：
        返还。
    完。
    平均速度 为 速度绝对和 除以 样本数。
    速度阈值_快 为 500 乘 Q_ONE 除以 1000。
    速度阈值_慢 为 100 乘 Q_ONE 除以 1000。
    若 平均速度 小于 速度阈值_慢 除以 2：
        sp 的 模式 为 滚动模式_停止。
        sp 的 模式置信度 为 240。
    完。
    做 否则如果 用 sp 的 方向变换数 大于 3 并且 样本数 大于或等于 4。
    块：
        sp 的 模式 为 滚动模式_往复。
        sp 的 模式置信度 为 200。
    完。
    做 否则如果 用 单调递减数 大于或等于 样本数 乘 3 除以 4 并且 平均速度 大于 速度阈值_慢。
    块：
        sp 的 模式 为 滚动模式_惯性。
        sp 的 模式置信度 为 220。
    完。
    做 否则如果 用 平均速度 大于 速度阈值_快。
    块：
        sp 的 模式 为 滚动模式_快速。
        sp 的 模式置信度 为 200。
    完。
    否则。
    块：
        sp 的 模式 为 滚动模式_慢速。
        sp 的 模式置信度 为 180。
    完。
    若 sp 的 速度历史索引 取余于 20 等于 0：
        sp 的 方向变换数 为 sp 的 方向变换数 除以 2。
    完。
完。

滚动预测器_预测位置 接受 sp、毫秒后：
    若 sp 等于 0 或者 sp 的 有效长度 等于 0：
        返还 0。
    完。
    P_物理 为 做 滚动预测器_物理预测 用 sp、毫秒后。
    P_Kalman 为 做 滚动预测器_Kalman预测 用 sp、毫秒后。
    可变 α 为 128。
    可变 β 为 128。
    若 sp 的 模式 等于 滚动模式_惯性：
        α 为 192。
        β 为 64。
    完。
    做 否则如果 用 sp 的 模式 等于 滚动模式_往复。
    块：
        α 为 64。
        β 为 192。
    完。
    做 否则如果 用 sp 的 模式 等于 滚动模式_快速。
    块：
        α 为 128。
        β 为 128。
    完。
    做 否则如果 用 sp 的 模式 等于 滚动模式_慢速。
    块：
        α 为 96。
        β 为 160。
    完。
    返还 α 乘 P_物理 加 β 乘 P_Kalman 右移 8。
完。

预加载区域 是 一种 事物，含 开始_y、结束_y、方向、优先级、置信度。

滚动预测器_计算预加载 接受 sp、视口高度、当前滚动Y：
    结果 为 新建 预加载区域。
    结果 的 方向 为 sp 的 方向。
    结果 的 置信度 为 sp 的 模式置信度。
    预测位置 为 做 滚动预测器_预测位置 用 sp、200。
    偏移 为 预测位置 减 当前滚动Y。
    若 sp 的 方向 等于 1：
        结果 的 开始_y 为 当前滚动Y 加 视口高度。
        结果 的 结束_y 为 当前滚动Y 加 视口高度 加 做 绝对值 用 偏移 加 视口高度。
    完。
    做 否则如果 用 sp 的 方向 等于 负 1。
    块：
        结果 的 结束_y 为 当前滚动Y。
        结果 的 开始_y 为 当前滚动Y 减 做 绝对值 用 偏移 减 视口高度。
    完。
    否则。
    块：
        结果 的 开始_y 为 当前滚动Y 减 视口高度 除以 2。
        结果 的 结束_y 为 当前滚动Y 加 视口高度 加 视口高度 除以 2。
    完。
    若 sp 的 模式 等于 滚动模式_快速 或者 sp 的 模式 等于 滚动模式_惯性：
        结果 的 优先级 为 2。
    完。
    做 否则如果 用 sp 的 模式 等于 滚动模式_慢速。
    块：
        结果 的 优先级 为 1。
    完。
    否则。
    块：
        结果 的 优先级 为 0。
    完。
    返还 结果。
完。

惯性停止速度阈值 为 50。

滚动预测器_预测停止时间 接受 sp：
    abs_v 为 做 绝对值 用 sp 的 当前速度。
    若 abs_v 小于或等于 惯性停止速度阈值：
        返还 0。
    完。
    若 sp 的 摩擦系数 小于或等于 0：
        返还 999999。
    完。
    停止时间 为 做 Q除 用 abs_v、做 Q乘 用 sp 的 摩擦系数 乘 2、惯性停止速度阈值。
    返还 做 Q转整数 用 停止时间 乘 1000。
完。

滚动预测器_预测停止位置 接受 sp：
    停止时间 为 做 滚动预测器_预测停止时间 用 sp。
    若 停止时间 小于或等于 0：
        若 sp 的 有效长度 大于 0：
            返还 做 内存_读32有符号 用 sp 的 历史位置 加 (sp 的 有效长度 减 1) 乘 4。
        完。
        返还 0。
    完。
    返还 做 滚动预测器_物理预测 用 sp、停止时间。
完。

滚动预测器_获取速度 接受 sp：
    返还 sp 的 当前速度。
完。

滚动预测器_获取加速度 接受 sp：
    返还 sp 的 当前加速度。
完。

滚动预测器_获取方向 接受 sp：
    返还 sp 的 方向。
完。

滚动预测器_获取模式 接受 sp：
    返还 sp 的 模式。
完。

滚动预测器_是否滚动中 接受 sp：
    返还 sp 的 方向 不等于 0。
完。

滚动预测器_设置摩擦 接受 sp、摩擦系数_Q10：
    sp 的 摩擦系数 为 摩擦系数_Q10。
完。

滚动预测器_发送特征 接受 sp：
    特征 为 做 Tensor_创建 用 8。
    做 Tensor_写 用 特征、0、sp 的 当前速度。
    做 Tensor_写 用 特征、1、sp 的 当前加速度。
    做 Tensor_写 用 特征、2、sp 的 方向。
    做 Tensor_写 用 特征、3、sp 的 模式。
    做 Tensor_写 用 特征、4、sp 的 模式置信度。
    做 Tensor_写 用 特征、5、做 Q转整数 用 sp 的 kalman_v。
    做 Tensor_写 用 特征、6、sp 的 方向变换数。
    做 Tensor_写 用 特征、7、sp 的 有效长度。
    做 全局AI总线 的 发送 用 「预测.滚动方向」、特征。
    做 Tensor_释放 用 特征。
完。

滚动预测器_重置 接受 sp：
    sp 的 有效长度 为 0。
    sp 的 当前速度 为 0。
    sp 的 当前加速度 为 0。
    sp 的 方向 为 0。
    sp 的 kalman_x 为 0。
    sp 的 kalman_v 为 0。
    sp 的 kalman_P00 为 Q_ONE。
    sp 的 kalman_P01 为 0。
    sp 的 kalman_P10 为 0。
    sp 的 kalman_P11 为 Q_ONE。
    sp 的 模式 为 滚动模式_停止。
    sp 的 模式置信度 为 0。
    sp 的 方向变换数 为 0。
    sp 的 速度历史索引 为 0。
完。

滚动预测器_销毁 接受 sp：
    若 sp 等于 0：
        返还。
    完。
    做 堆_释放 用 sp 的 历史位置。
    做 堆_释放 用 sp 的 历史时间。
    做 堆_释放 用 sp 的 速度历史。
    做 堆_释放 用 sp。
完。

预测评估_窗口 为 16。

可变 预测误差历史 为 一组：（空）。

可变 预测误差索引 为 0。

可变 预测评估_总次数 为 0。

可变 预测评估_MAE 为 0。

滚动预测器_记录预测误差 接受 sp、预测位置、实际位置：
    误差 为 预测位置 减 实际位置。
    若 误差 小于 0：
        误差 为 0 减 误差。
    完。
    若 预测评估_总次数 小于 预测评估_窗口：
        做 追加 用 预测误差历史、误差。
    否则：
        idx 为 预测误差索引 取余于 预测评估_窗口。
        做 设置 用 预测误差历史、idx、误差。
    完。
    预测误差索引 为 预测误差索引 加 1。
    预测评估_总次数 为 预测评估_总次数 加 1。
    可变 总误差 为 0。
    有效数 为 做 计数 用 预测误差历史。
    可变 i 为 0。
    只要 i 小于 有效数：
        总误差 为 总误差 加 预测误差历史 的 第 i 项。
        i 为 i 加 1。
    完。
    若 有效数 大于 0：
        预测评估_MAE 为 总误差 除以 有效数。
    完。
    若 预测评估_MAE 大于 100 并且 sp 不等于 0：
        sp 的 摩擦系数 为 sp 的 摩擦系数 加 5。
        若 sp 的 摩擦系数 大于 200：
            sp 的 摩擦系数 为 200。
        完。
    完。
    做 否则如果 用 预测评估_MAE 小于 30 并且 sp 不等于 0。
    块：
        sp 的 摩擦系数 为 sp 的 摩擦系数 减 2。
        若 sp 的 摩擦系数 小于 10：
            sp 的 摩擦系数 为 10。
        完。
    完。
完。

滚动预测器_获取MAE 接受 零参：
    返还 预测评估_MAE。
完。

滚动预测器_获取预测次数 接受 零参：
    返还 预测评估_总次数。
完。

滚动预测器_打印统计 接受 sp：
    若 sp 等于 0：
        做 打印行 用 「滚动预测器: 未初始化」。
        返还。
    完。
    做 打印行 用 「=== 滚动预测器统计 ===」。
    做 打印行 用 「有效样本: 」 加 做 整数转字符串 用 sp 的 有效长度。
    做 打印行 用 「当前速度(Q10): 」 加 做 整数转字符串 用 sp 的 当前速度。
    做 打印行 用 「当前加速度(Q10): 」 加 做 整数转字符串 用 sp 的 当前加速度。
    做 打印行 用 「方向: 」 加 做 整数转字符串 用 sp 的 方向。
    做 打印行 用 「模式: 」 加 做 整数转字符串 用 sp 的 模式。
    做 打印行 用 「模式置信度(Q8): 」 加 做 整数转字符串 用 sp 的 模式置信度。
    做 打印行 用 「方向变换累计: 」 加 做 整数转字符串 用 sp 的 方向变换数。
    做 打印行 用 「摩擦系数(Q10): 」 加 做 整数转字符串 用 sp 的 摩擦系数。
    做 打印行 用 「Kalman位置(Q10): 」 加 做 整数转字符串 用 sp 的 kalman_x。
    做 打印行 用 「Kalman速度(Q10): 」 加 做 整数转字符串 用 sp 的 kalman_v。
    做 打印行 用 「预测评估次数: 」 加 做 整数转字符串 用 预测评估_总次数。
    做 打印行 用 「平均绝对误差: 」 加 做 整数转字符串 用 预测评估_MAE。
完。

可变 _谐振滚动_速度历史 为 一组：（空）。

可变 _谐振滚动_预测次数 为 0。

谐振滚动_记录速度 接受 速度：
    做 追加 用 _谐振滚动_速度历史、速度。
    若 做 计数 用 _谐振滚动_速度历史 大于 16：
        可变 新表 为 一组：（空）。
        可变 i 为 做 计数 用 _谐振滚动_速度历史 减 16。
        只要 i 小于 做 计数 用 _谐振滚动_速度历史：
            做 追加 用 新表、_谐振滚动_速度历史 的 第 i 项。
            i 为 i 加 1。
        完。
        _谐振滚动_速度历史 为 新表。
    完。
完。

谐振滚动_预测停止偏移 接受 零参：
    若 做 计数 用 _谐振滚动_速度历史 小于 2：
        返还 0。
    完。
    最近 为 _谐振滚动_速度历史 的 第 做 计数 用 _谐振滚动_速度历史 减 1 项。
    前一 为 _谐振滚动_速度历史 的 第 做 计数 用 _谐振滚动_速度历史 减 2 项。
    减速率 为 前一 减 最近。
    若 减速率 小于或等于 0：
        返还 最近 乘 5。
    完。
    预测 为 最近 乘 最近 除以 (2 乘 减速率 加 1)。
    _谐振滚动_预测次数 为 _谐振滚动_预测次数 加 1。
    返还 预测。
完。

滚动预测_完整统计 接受 零参：
    返还 一组：「谐振预测次数」、_谐振滚动_预测次数、「速度历史数」、做 计数 用 _谐振滚动_速度历史。
完。

本章 收束。
