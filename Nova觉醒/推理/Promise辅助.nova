《Promise辅助》

引用《..·中枢·系统调用表》。

引用《..·推理·通信渠道》。

引用《..·感知·内核直连层》。

引用《..·中枢·字符串替换》。

引用《.·定点Q算术》。

引用《..·中枢·内存初始化》。

引用《.·AI鼠标事件处理》。

引用《..·中枢·内容可见性》。

引用《..·执行·底层内存操》。

承诺状态_待定 为 0。

承诺状态_已完成 为 1。

承诺状态_已拒绝 为 2。

承诺_魔数 为 0x50524F4D。

回调类型_resolve 为 0。

回调类型_reject 为 1。

回调类型_then 为 2。

回调类型_finally 为 3。

组合器_all 为 0。

组合器_allSettled 为 1。

组合器_any 为 2。

组合器_race 为 3。

微任务_最大容量 为 4096。

微任务_结构大小 为 24。

AI_承诺链_短阈值 为 3。

AI_承诺链_长阈值 为 10。

AI_批量微任务阈值 为 16。

承诺回调绑定 是 一种 事物，含 目标承诺、操作类型、上下文数据、下一个。

组合器上下文 是 一种 事物，含 结果数组、索引、计数器地址、结果承诺、运行时实例、组合器类型、是成功回调、总数。

微任务队列 是 一种 事物，含 基址、容量、头部、尾部、当前大小、AI批量激活、AI累积数、统计_入队总数、统计_执行总数、统计_溢出次数、统计_批量执行次数。

微任务队列_创建 接受 零参：
    可变 队列 为 做 堆_分配 用 微任务队列。
    若 队列 等于 0：
        做 打印 用 「[微任务] 错误: 队列分配失败」。
        返还 空。
    完。
    队列 的 容量 为 微任务_最大容量。
    队列 的 基址 为 做 平台_申请内存 用 队列 的 容量 乘 微任务_结构大小。
    若 队列 的 基址 等于 0：
        做 堆_释放 用 队列。
        返还 空。
    完。
    做 内存_清零 用 队列 的 基址、队列 的 容量 乘 微任务_结构大小。
    队列 的 头部 为 0。
    队列 的 尾部 为 0。
    队列 的 当前大小 为 0。
    队列 的 AI批量激活 为 假。
    队列 的 AI累积数 为 0。
    队列 的 统计_入队总数 为 0。
    队列 的 统计_执行总数 为 0。
    队列 的 统计_溢出次数 为 0。
    队列 的 统计_批量执行次数 为 0。
    返还 队列。
完。

微任务队列_销毁 接受 队列：
    若 队列 等于 空：
        返还。
    完。
    若 队列 的 基址 不等于 0：
        做 平台_释放内存 用 队列 的 基址、队列 的 容量 乘 微任务_结构大小。
    完。
    做 堆_释放 用 队列。
完。

微任务队列_入队 接受 队列、回调、参数、类型：
    若 队列 等于 空 或者 队列 的 基址 等于 0：
        返还。
    完。
    若 队列 的 当前大小 大于或等于 队列 的 容量：
        队列 的 统计_溢出次数 为 队列 的 统计_溢出次数 加 1。
        做 打印 用 「[微任务] 警告: 队列已满」。
        返还。
    完。
    偏移 为 队列 的 基址 加 队列 的 尾部 乘 微任务_结构大小。
    做 内存_写64 用 偏移、回调。
    做 内存_写64 用 偏移 加 8、参数。
    做 内存_写32 用 偏移 加 16、类型。
    做 内存_写32 用 偏移 加 20、0。
    队列 的 尾部 为 (队列 的 尾部 加 1) 取余于 队列 的 容量。
    队列 的 当前大小 为 队列 的 当前大小 加 1。
    队列 的 统计_入队总数 为 队列 的 统计_入队总数 加 1。
    队列 的 AI累积数 为 队列 的 AI累积数 加 1。
完。

微任务队列_执行全部 接受 队列、Nova运行时实例：
    若 队列 等于 空 或者 Nova运行时实例 等于 0：
        返还。
    完。
    可变 执行数 为 0。
    只要 队列 的 当前大小 大于 0：
        偏移 为 队列 的 基址 加 队列 的 头部 乘 微任务_结构大小。
        回调 为 做 内存_读64 用 偏移。
        参数 为 做 内存_读64 用 偏移 加 8。
        队列 的 头部 为 (队列 的 头部 加 1) 取余于 队列 的 容量。
        队列 的 当前大小 为 队列 的 当前大小 减 1。
        若 回调 不等于 0：
            做 Nova函数_Call 用 Nova运行时实例、回调、参数。
        完。
        执行数 为 执行数 加 1。
        队列 的 统计_执行总数 为 队列 的 统计_执行总数 加 1。
    完。
    若 执行数 大于 0：
        队列 的 AI累积数 为 0。
    完。
完。

微任务队列_为空 接受 队列：
    若 队列 等于 空：
        返还 真。
    完。
    返还 队列 的 当前大小 等于 0。
完。

Nova承诺_是承诺 接受 值：
    若 值 等于 0：
        返还 假。
    完。
    返还 做 内存_读32 用 值 等于 0x50524F4D。
完。

Nova承诺_创建resolve绑定 接受 Nova运行时实例、目标promise：
    ctx 为 做 堆_分配 用 16。
    做 内存_写64 用 ctx、目标promise。
    做 内存_写32 用 ctx 加 8、0。
    返还 ctx。
完。

Nova承诺_创建reject绑定 接受 Nova运行时实例、目标promise：
    ctx 为 做 堆_分配 用 16。
    做 内存_写64 用 ctx、目标promise。
    做 内存_写32 用 ctx 加 8、1。
    返还 ctx。
完。

Nova承诺_创建all回调 接受 Nova运行时实例、结果数组、索引、计数器、结果promise：
    ctx 为 做 堆_分配 用 40。
    做 内存_写64 用 ctx、结果数组。
    做 内存_写32 用 ctx 加 8、索引。
    做 内存_写64 用 ctx 加 12、计数器。
    做 内存_写64 用 ctx 加 20、结果promise。
    做 内存_写64 用 ctx 加 28、Nova运行时实例。
    返还 ctx。
完。

Nova承诺_创建allSettled回调 接受 Nova运行时实例、结果数组、索引、计数器、结果promise、是成功：
    ctx 为 做 堆_分配 用 48。
    做 内存_写64 用 ctx、结果数组。
    做 内存_写32 用 ctx 加 8、索引。
    做 内存_写64 用 ctx 加 12、计数器。
    做 内存_写64 用 ctx 加 20、结果promise。
    做 内存_写64 用 ctx 加 28、Nova运行时实例。
    做 内存_写8 用 ctx 加 36、若 是成功 则 1 否则 0。
    返还 ctx。
完。

Nova承诺_创建any拒绝回调 接受 Nova运行时实例、错误数组、索引、计数器、结果promise：
    ctx 为 做 堆_分配 用 40。
    做 内存_写64 用 ctx、错误数组。
    做 内存_写32 用 ctx 加 8、索引。
    做 内存_写64 用 ctx 加 12、计数器。
    做 内存_写64 用 ctx 加 20、结果promise。
    做 内存_写64 用 ctx 加 28、Nova运行时实例。
    返还 ctx。
完。

异步_创建恢复回调 接受 ctx、是成功：
    cb 为 做 堆_分配 用 16。
    做 内存_写64 用 cb、ctx。
    做 内存_写8 用 cb 加 8、若 是成功 则 1 否则 0。
    返还 cb。
完。

Nova函数_Call 接受 Nova运行时实例、函数、参数：
    若 以 零参 为 引，其做法是： 等于 0：
        返还 0。
    完。
    返还 做 Nova调用 用 以 零参 为 引，其做法是：、Nova运行时实例 的 undefined值、参数、1。
完。

智能_承诺Complete_AI初始化 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
完。

智能_承诺Complete_AI热路径反馈 接受 函数哈希、执行时间：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
完。

智能_承诺Complete_AI应该优化 接受 函数哈希、执行次数、平均时间：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 假。
    完。
    特征 为 做 Tensor_创建 用 1、8。
    做 Tensor_写 用 特征、0、0、若 执行次数 大于 2000 则 Q_ONE 否则 执行次数 乘 Q_ONE 除以 2000。
    做 Tensor_写 用 特征、0、1、若 平均时间 大于 1000 则 Q_ONE 否则 平均时间 乘 Q_ONE 除以 1000。
    分数 为 做 AI总线_注意力权重 用 bus、特征。
    做 Tensor_释放 用 特征。
    返还 分数 大于 512。
完。

Nova承诺 是 一种 事物，含 魔数、状态、值、完成回调链、拒绝回调链、已处理、引用计数、链长度、创建时间。

Nova承诺_创建 接受 Nova运行时实例：
    若 Nova运行时实例 等于 0：
        返还 0。
    完。
    可变 p 为 做 堆_分配 用 Nova承诺。
    若 p 等于 0：
        返还 0。
    完。
    p 的 魔数 为 承诺_魔数。
    p 的 状态 为 承诺状态_待定。
    p 的 值 为 0。
    p 的 完成回调链 为 0。
    p 的 拒绝回调链 为 0。
    p 的 已处理 为 假。
    p 的 引用计数 为 1。
    p 的 链长度 为 0。
    p 的 创建时间 为 做 平台_获取高精度时间。
    返还 p。
完。

Nova承诺_销毁 接受 p：
    若 p 等于 0：
        返还。
    完。
    做 Nova承诺_释放回调链 用 p 的 完成回调链。
    做 Nova承诺_释放回调链 用 p 的 拒绝回调链。
    做 堆_释放 用 p。
完。

Nova承诺_释放回调链 接受 链头：
    可变 当前 为 链头。
    只要 当前 不等于 0：
        下一个 为 做 内存_读64 用 当前 加 24。
        做 堆_释放 用 当前。
        当前 为 下一个。
    完。
完。

Nova承诺_增引用 接受 p：
    若 p 等于 0：
        返还。
    完。
    p 的 引用计数 为 p 的 引用计数 加 1。
完。

Nova承诺_减引用 接受 p：
    若 p 等于 0：
        返还。
    完。
    p 的 引用计数 为 p 的 引用计数 减 1。
    若 p 的 引用计数 小于或等于 0：
        做 Nova承诺_销毁 用 p。
    完。
完。

Nova承诺_解决 接受 Nova运行时实例、p、值：
    若 p 等于 0 或者 p 的 状态 不等于 承诺状态_待定：
        返还。
    完。
    若 值 等于 p：
        做 打印 用 「[承诺] TypeError: 承诺不能resolve自身」。
        做 Nova承诺_拒绝 用 Nova运行时实例、p、0。
        返还。
    完。
    若 做 Nova承诺_是承诺 用 值：
        内部承诺 为 值。
        若 内部承诺 的 状态 等于 承诺状态_已完成：
            做 Nova承诺_完成 用 Nova运行时实例、p、内部承诺 的 值。
        否则若 内部承诺 的 状态 等于 承诺状态_已拒绝：
            做 Nova承诺_拒绝 用 Nova运行时实例、p、内部承诺 的 值。
        否则：
            做 Nova承诺_链接 用 Nova运行时实例、内部承诺、p。
        完。
        返还。
    完。
    做 Nova承诺_完成 用 Nova运行时实例、p、值。
完。

Nova承诺_完成 接受 Nova运行时实例、p、值：
    若 p 等于 0 或者 p 的 状态 不等于 承诺状态_待定：
        返还。
    完。
    p 的 状态 为 承诺状态_已完成。
    p 的 值 为 值。
    做 Nova承诺_触发回调 用 Nova运行时实例、p 的 完成回调链、值。
完。

Nova承诺_拒绝 接受 Nova运行时实例、p、原因：
    若 p 等于 0 或者 p 的 状态 不等于 承诺状态_待定：
        返还。
    完。
    p 的 状态 为 承诺状态_已拒绝。
    p 的 值 为 原因。
    做 Nova承诺_触发回调 用 Nova运行时实例、p 的 拒绝回调链、原因。
    若 p 的 已处理 等于 假 并且 p 的 拒绝回调链 等于 0：
        做 打印 用 「[承诺] 警告: 未处理的Promise rejection」。
    完。
完。

Nova承诺_触发回调 接受 Nova运行时实例、回调链、值：
    可变 当前 为 回调链。
    只要 当前 不等于 0：
        回调 为 做 内存_读64 用 当前。
        若 回调 不等于 0：
            做 Nova函数_Call 用 Nova运行时实例、回调、值。
        完。
        当前 为 做 内存_读64 用 当前 加 24。
    完。
完。

Nova承诺_链接 接受 Nova运行时实例、源承诺、目标承诺：
    解决绑定 为 做 Nova承诺_创建resolve绑定 用 Nova运行时实例、目标承诺。
    拒绝绑定 为 做 Nova承诺_创建reject绑定 用 Nova运行时实例、目标承诺。
    做 Nova承诺_添加回调 用 源承诺、解决绑定、拒绝绑定。
完。

Nova承诺_添加回调 接受 p、完成回调、拒绝回调：
    若 p 等于 0：
        返还。
    完。
    p 的 已处理 为 真。
    若 p 的 状态 等于 承诺状态_待定：
        若 完成回调 不等于 0：
            节点 为 做 堆_分配 用 承诺回调绑定。
            若 节点 不等于 0：
                做 内存_写64 用 节点、完成回调。
                做 内存_写64 用 节点 加 24、p 的 完成回调链。
                p 的 完成回调链 为 节点。
            完。
        完。
        若 拒绝回调 不等于 0：
            节点 为 做 堆_分配 用 承诺回调绑定。
            若 节点 不等于 0：
                做 内存_写64 用 节点、拒绝回调。
                做 内存_写64 用 节点 加 24、p 的 拒绝回调链。
                p 的 拒绝回调链 为 节点。
            完。
        完。
        p 的 链长度 为 p 的 链长度 加 1。
    否则若 p 的 状态 等于 承诺状态_已完成 并且 完成回调 不等于 0：
        做 Nova函数_Call 用 0、完成回调、p 的 值。
    否则若 p 的 状态 等于 承诺状态_已拒绝 并且 拒绝回调 不等于 0：
        做 Nova函数_Call 用 0、拒绝回调、p 的 值。
    完。
完。

Nova承诺_then 接受 Nova运行时实例、p、完成处理、拒绝处理：
    若 p 等于 0 或者 非 做 Nova承诺_是承诺 用 p：
        返还 0。
    完。
    新承诺 为 做 Nova承诺_创建 用 Nova运行时实例。
    若 新承诺 等于 0：
        返还 0。
    完。
    包装完成 为 做 Nova承诺_包装then回调 用 Nova运行时实例、新承诺、完成处理、回调类型_resolve。
    包装拒绝 为 做 Nova承诺_包装then回调 用 Nova运行时实例、新承诺、拒绝处理、回调类型_reject。
    做 Nova承诺_添加回调 用 p、包装完成、包装拒绝。
    返还 新承诺。
完。

Nova承诺_catch 接受 Nova运行时实例、p、拒绝处理：
    返还 做 Nova承诺_then 用 Nova运行时实例、p、0、拒绝处理。
完。

Nova承诺_finally 接受 Nova运行时实例、p、处理函数：
    若 p 等于 0：
        返还 0。
    完。
    新承诺 为 做 Nova承诺_创建 用 Nova运行时实例。
    若 新承诺 等于 0：
        返还 0。
    完。
    包装 为 做 Nova承诺_包装finally回调 用 Nova运行时实例、新承诺、处理函数。
    做 Nova承诺_添加回调 用 p、包装、包装。
    返还 新承诺。
完。

Nova承诺_包装then回调 接受 Nova运行时实例、新承诺、处理函数、类型：
    ctx 为 做 堆_分配 用 32。
    若 ctx 等于 0：
        返还 0。
    完。
    做 内存_写64 用 ctx、新承诺。
    做 内存_写64 用 ctx 加 8、处理函数。
    做 内存_写64 用 ctx 加 16、Nova运行时实例。
    做 内存_写32 用 ctx 加 24、类型。
    返还 ctx。
完。

Nova承诺_包装finally回调 接受 Nova运行时实例、新承诺、处理函数：
    ctx 为 做 堆_分配 用 24。
    若 ctx 等于 0：
        返还 0。
    完。
    做 内存_写64 用 ctx、新承诺。
    做 内存_写64 用 ctx 加 8、处理函数。
    做 内存_写64 用 ctx 加 16、Nova运行时实例。
    返还 ctx。
完。

Nova承诺_静态resolve 接受 Nova运行时实例、值：
    若 做 Nova承诺_是承诺 用 值：
        返还 值。
    完。
    p 为 做 Nova承诺_创建 用 Nova运行时实例。
    若 p 不等于 0：
        做 Nova承诺_完成 用 Nova运行时实例、p、值。
    完。
    返还 p。
完。

Nova承诺_静态reject 接受 Nova运行时实例、原因：
    p 为 做 Nova承诺_创建 用 Nova运行时实例。
    若 p 不等于 0：
        做 Nova承诺_拒绝 用 Nova运行时实例、p、原因。
    完。
    返还 p。
完。

智能_承诺_预测链长度 接受 Nova运行时实例、p：
    若 p 等于 0：
        返还 0。
    完。
    可变 长度 为 0。
    可变 当前 为 p 的 完成回调链。
    只要 当前 不等于 0：
        长度 为 长度 加 1。
        当前 为 做 内存_读64 用 当前 加 24。
    完。
    返还 长度。
完。

智能_承诺_应该内联 接受 链长度：
    返还 链长度 小于或等于 AI_承诺链_短阈值。
完。

可变 全局_承诺创建数 为 0。

可变 全局_承诺resolve数 为 0。

可变 全局_承诺reject数 为 0。

可变 全局_承诺then数 为 0。

可变 全局_未处理rejection数 为 0。

承诺系统_打印统计 接受 零参：
    做 打印 用 「[承诺] ═══════════ 承诺系统统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  承诺创建数:       」、做 转字符串 用 全局_承诺创建数。
    做 打印 用 做 字符串拼接 用 「  承诺resolve数:    」、做 转字符串 用 全局_承诺resolve数。
    做 打印 用 做 字符串拼接 用 「  承诺reject数:     」、做 转字符串 用 全局_承诺reject数。
    做 打印 用 做 字符串拼接 用 「  承诺then数:       」、做 转字符串 用 全局_承诺then数。
    做 打印 用 做 字符串拼接 用 「  未处理rejection:  」、做 转字符串 用 全局_未处理rejection数。
    做 打印 用 「[承诺] ═══════════════════════════════════════」。
完。

微任务队列_打印统计 接受 队列：
    若 队列 等于 空：
        返还。
    完。
    做 打印 用 「[微任务] ═══════════ 微任务队列统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  当前大小:     」、做 转字符串 用 队列 的 当前大小。
    做 打印 用 做 字符串拼接 用 「  容量:         」、做 转字符串 用 队列 的 容量。
    做 打印 用 做 字符串拼接 用 「  入队总数:     」、做 转字符串 用 队列 的 统计_入队总数。
    做 打印 用 做 字符串拼接 用 「  执行总数:     」、做 转字符串 用 队列 的 统计_执行总数。
    做 打印 用 做 字符串拼接 用 「  溢出次数:     」、做 转字符串 用 队列 的 统计_溢出次数。
    做 打印 用 做 字符串拼接 用 「  AI批量激活:   」、做 转字符串 用 队列 的 AI批量激活。
    做 打印 用 「[微任务] ═══════════════════════════════════════」。
完。

Nova承诺_打印信息 接受 p：
    若 p 等于 0：
        做 打印 用 「[承诺] 空承诺」。
        返还。
    完。
    做 打印 用 「[承诺] ── 承诺信息 ──」。
    若 p 的 状态 等于 承诺状态_待定：
        做 打印 用 「  状态: pending」。
    否则若 p 的 状态 等于 承诺状态_已完成：
        做 打印 用 「  状态: fulfilled」。
    否则若 p 的 状态 等于 承诺状态_已拒绝：
        做 打印 用 「  状态: rejected」。
    完。
    做 打印 用 做 字符串拼接 用 「  值:       」、做 转字符串 用 p 的 值。
    做 打印 用 做 字符串拼接 用 「  已处理:   」、做 转字符串 用 p 的 已处理。
    做 打印 用 做 字符串拼接 用 「  链长度:   」、做 转字符串 用 p 的 链长度。
    做 打印 用 做 字符串拼接 用 「  引用计数: 」、做 转字符串 用 p 的 引用计数。
完。

本章 收束。
