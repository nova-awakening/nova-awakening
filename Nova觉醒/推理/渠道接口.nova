《渠道接口》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·自动扩容》。

引用《..·中枢·整数转字符串》。

引用《..·标准·原生列表》。

渠道类型_终端 为 「terminal」。

渠道类型_WebChat 为 「webchat」。

渠道类型_Telegram 为 「telegram」。

渠道类型_接口 为 「api」。

渠道类型_微信 为 「wechat」。

渠道类型_钉钉 为 「dingtalk」。

渠道状态_停止 为 「停止」。

渠道状态_运行 为 「运行」。

渠道状态_暂停 为 「暂停」。

渠道状态_错误 为 「错误」。

渠道_默认速率限制 为 60。

渠道_默认队列容量 为 256。

渠道_健康检查间隔 为 30000。

渠道适配器 是 一种 事物，含 名称、类型、接收函数、发送函数、状态、配置、发送队列、接收队列、速率限制_每分钟、速率_当前计数、速率_重置时间、总接收数、总发送数、总失败数、最后活跃时间、错误信息、健康回调。

可变 渠道注册表 为 做 字典。

可变 渠道注册表_计数 为 0。

可变 渠道接口_广播数 为 0。

可变 渠道接口_健康巡检数 为 0。

可变 渠道接口_最后错误 为 「」。

渠道_创建 接受 名称、类型、接收函数、发送函数：
    返还 新建 渠道适配器：名称 为 名称、类型 为 类型、接收函数 为 接收函数、发送函数 为 发送函数、状态 为 渠道状态_停止、配置 为 做 字典、发送队列 为 一组：（空）、接收队列 为 一组：（空）、速率限制_每分钟 为 渠道_默认速率限制、速率_当前计数 为 0、速率_重置时间 为 0、总接收数 为 0、总发送数 为 0、总失败数 为 0、最后活跃时间 为 0、错误信息 为 「」、健康回调 为 空。
完。

渠道_启动 接受 渠道：
    若 渠道 的 状态 等于 渠道状态_运行：
        返还 成功。
    完。
    渠道 的 状态 为 渠道状态_运行。
    渠道 的 最后活跃时间 为 做 当前时间_毫秒。
    渠道 的 速率_当前计数 为 0。
    渠道 的 速率_重置时间 为 做 当前时间_毫秒 加 60000。
    渠道 的 错误信息 为 「」。
    返还 成功。
完。

渠道_停止 接受 渠道：
    渠道 的 状态 为 渠道状态_停止。
    渠道 的 最后活跃时间 为 做 当前时间_毫秒。
完。

渠道_暂停 接受 渠道：
    若 渠道 的 状态 等于 渠道状态_运行：
        渠道 的 状态 为 渠道状态_暂停。
    完。
完。

渠道_恢复 接受 渠道：
    若 渠道 的 状态 等于 渠道状态_暂停：
        渠道 的 状态 为 渠道状态_运行。
        渠道 的 最后活跃时间 为 做 当前时间_毫秒。
    完。
完。

渠道_是运行 接受 渠道：
    返还 渠道 的 状态 等于 渠道状态_运行。
完。

渠道_名称 接受 渠道：
    返还 渠道 的 名称。
完。

渠道_类型 接受 渠道：
    返还 渠道 的 类型。
完。

渠道_状态 接受 渠道：
    返还 渠道 的 状态。
完。

渠道_检查速率 接受 渠道：
    现在 为 做 当前时间_毫秒。
    若 现在 大于或等于 渠道 的 速率_重置时间：
        渠道 的 速率_当前计数 为 0。
        渠道 的 速率_重置时间 为 现在 加 60000。
    完。
    若 渠道 的 速率_当前计数 大于或等于 渠道 的 速率限制_每分钟：
        返还 假。
    完。
    渠道 的 速率_当前计数 为 渠道 的 速率_当前计数 加 1。
    返还 真。
完。

渠道_默认目标 接受 渠道：
    目标 为 做 字典取 用 渠道 的 配置、「广播目标」。
    若 目标 等于 空 或者 目标 等于 「」：
        目标 为 做 字典取 用 渠道 的 配置、「默认用户ID」。
    完。
    若 目标 等于 空 或者 目标 等于 「」：
        目标 为 「broadcast」。
    完。
    返还 目标。
完。

渠道_放入发送队列 接受 渠道、用户ID、消息：
    若 做 计数 用 渠道 的 发送队列 大于或等于 渠道_默认队列容量：
        渠道 的 总失败数 为 渠道 的 总失败数 加 1。
        渠道 的 错误信息 为 「发送队列已满」。
        渠道接口_最后错误 为 渠道 的 名称 加 「: 发送队列已满」。
        返还 错误_资源不足。
    完。
    做 追加 用 渠道 的 发送队列、一组：用户ID、消息。
    渠道 的 最后活跃时间 为 做 当前时间_毫秒。
    返还 成功。
完。

渠道_接收消息 接受 渠道：
    若 非 做 渠道_是运行 用 渠道：
        返还 空。
    完。
    若 做 计数 用 渠道 的 接收队列 大于 0：
        消息 为 渠道 的 接收队列 的 第 0 项。
        渠道 的 接收队列 为 做 列表_切片 用 渠道 的 接收队列、1、做 计数 用 渠道 的 接收队列。
        渠道 的 总接收数 为 渠道 的 总接收数 加 1。
        渠道 的 最后活跃时间 为 做 当前时间_毫秒。
        返还 消息。
    完。
    若 渠道 的 接收函数 不等于 空：
        消息 为 做 渠道 的 接收函数。
        若 消息 不等于 空：
            渠道 的 总接收数 为 渠道 的 总接收数 加 1。
            渠道 的 最后活跃时间 为 做 当前时间_毫秒。
        完。
        返还 消息。
    完。
    返还 空。
完。

渠道_发送消息 接受 渠道、用户ID、消息：
    若 非 做 渠道_是运行 用 渠道：
        返还 错误_设备忙。
    完。
    若 非 做 渠道_检查速率 用 渠道：
        返还 做 渠道_放入发送队列 用 渠道、用户ID、消息。
    完。
    若 渠道 的 发送函数 等于 空：
        返还 做 渠道_放入发送队列 用 渠道、用户ID、消息。
    完。
    结果 为 做 渠道 的 发送函数 用 用户ID、消息。
    若 结果 等于 成功：
        渠道 的 总发送数 为 渠道 的 总发送数 加 1。
        渠道 的 最后活跃时间 为 做 当前时间_毫秒。
        渠道 的 错误信息 为 「」。
    否则：
        渠道 的 总失败数 为 渠道 的 总失败数 加 1。
        渠道 的 错误信息 为 「发送失败」。
        渠道接口_最后错误 为 渠道 的 名称 加 「: 发送失败」。
        返还 结果。
    完。
    做 渠道_刷新发送队列 用 渠道。
    返还 结果。
完。

渠道_推入接收 接受 渠道、消息：
    若 做 计数 用 渠道 的 接收队列 大于或等于 渠道_默认队列容量：
        返还 假。
    完。
    做 追加 用 渠道 的 接收队列、消息。
    返还 真。
完。

渠道_刷新发送队列 接受 渠道：
    若 非 做 渠道_是运行 用 渠道：
        返还。
    完。
    只要 做 计数 用 渠道 的 发送队列 大于 0 并且 渠道 的 发送函数 不等于 空：
        若 非 做 渠道_检查速率 用 渠道：
            跳出。
        完。
        项 为 渠道 的 发送队列 的 第 0 项。
        结果 为 做 渠道 的 发送函数 用 项 的 第 0 项、项 的 第 1 项。
        若 结果 不等于 成功：
            渠道 的 总失败数 为 渠道 的 总失败数 加 1。
            渠道 的 错误信息 为 「队列发送失败」。
            渠道接口_最后错误 为 渠道 的 名称 加 「: 队列发送失败」。
            跳出。
        完。
        渠道 的 发送队列 为 做 列表_切片 用 渠道 的 发送队列、1、做 计数 用 渠道 的 发送队列。
        渠道 的 总发送数 为 渠道 的 总发送数 加 1。
        渠道 的 最后活跃时间 为 做 当前时间_毫秒。
    完。
完。

渠道_设配置 接受 渠道、键、值：
    做 字典设 用 渠道 的 配置、键、值。
完。

渠道_取配置 接受 渠道、键：
    返还 做 字典取 用 渠道 的 配置、键。
完。

渠道_设速率限制 接受 渠道、每分钟：
    渠道 的 速率限制_每分钟 为 每分钟。
完。

渠道_设健康回调 接受 渠道、回调：
    渠道 的 健康回调 为 回调。
完。

渠道_健康检查 接受 渠道：
    现在 为 做 当前时间_毫秒。
    空闲时间 为 现在 减 渠道 的 最后活跃时间。
    若 渠道 的 状态 等于 渠道状态_停止 或者 渠道 的 状态 等于 渠道状态_暂停：
        返还 渠道 的 状态。
    完。
    若 渠道 的 状态 等于 渠道状态_错误：
        返还 「错误」。
    完。
    若 空闲时间 大于 渠道_健康检查间隔 乘 10：
        返还 「超时」。
    完。
    若 渠道 的 健康回调 不等于 空：
        返还 做 渠道 的 健康回调。
    完。
    若 渠道 的 状态 等于 渠道状态_运行：
        返还 「健康」。
    完。
    返还 渠道 的 状态。
完。

渠道_统计 接受 渠道：
    返还 一组：渠道 的 总接收数、渠道 的 总发送数、渠道 的 总失败数、做 计数 用 渠道 的 发送队列、做 计数 用 渠道 的 接收队列。
完。

渠道注册表_条目列表 接受 零参：
    结果 为 一组：（空）。
    若 渠道注册表 等于 空：
        返还 结果。
    完。
    桶 为 渠道注册表 的 第 0 项。
    容量 为 渠道注册表 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 容量：
        槽 为 桶 的 第 i 项。
        若 槽 的 第 2 项 等于 真：
            做 追加 用 结果、一组：槽 的 第 1 项 的 第 0 项、槽 的 第 1 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

渠道注册表_注册 接受 渠道：
    若 做 字典取 用 渠道注册表、渠道 的 名称 等于 空：
        渠道注册表_计数 为 渠道注册表_计数 加 1。
    完。
    做 字典设 用 渠道注册表、渠道 的 名称、渠道。
完。

渠道注册表_注销 接受 名称：
    渠道 为 做 字典取 用 渠道注册表、名称。
    若 渠道 不等于 空：
        做 渠道_停止 用 渠道。
        做 字典删 用 渠道注册表、名称。
        若 渠道注册表_计数 大于 0：
            渠道注册表_计数 为 渠道注册表_计数 减 1。
        完。
    完。
完。

渠道注册表_获取 接受 名称：
    返还 做 字典取 用 渠道注册表、名称。
完。

渠道注册表_数量 接受 零参：
    返还 渠道注册表_计数。
完。

渠道注册表_广播 接受 消息：
    可变 成功数 为 0。
    条目列表 为 做 渠道注册表_条目列表。
    可变 i 为 0。
    渠道接口_广播数 为 渠道接口_广播数 加 1。
    只要 i 小于 做 计数 用 条目列表：
        渠道 为 条目列表 的 第 i 项 的 第 1 项。
        若 做 渠道_是运行 用 渠道：
            结果 为 做 渠道_发送消息 用 渠道、做 渠道_默认目标 用 渠道、消息。
            若 结果 等于 成功：
                成功数 为 成功数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 成功数。
完。

渠道注册表_全部健康 接受 零参：
    返还 做 渠道注册表_健康摘要 用 真。
完。

渠道注册表_健康摘要 接受 计入巡检：
    条目列表 为 做 渠道注册表_条目列表。
    可变 健康数 为 0。
    可变 异常数 为 0。
    可变 i 为 0。
    若 计入巡检：
        渠道接口_健康巡检数 为 渠道接口_健康巡检数 加 1。
    完。
    只要 i 小于 做 计数 用 条目列表：
        状态 为 做 渠道_健康检查 用 条目列表 的 第 i 项 的 第 1 项。
        若 状态 等于 「健康」 或者 状态 等于 渠道状态_运行：
            健康数 为 健康数 加 1。
        否则：
            异常数 为 异常数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一本字典：总数 对应 做 计数 用 条目列表、健康 对应 健康数、异常 对应 异常数。
完。

可变 渠道接口_带宽估计 为 0。

可变 渠道接口_丢包窗口 为 一组：（空）。

渠道接口_更新带宽估计 接受 发送字节、耗时毫秒：
    若 耗时毫秒 等于 0：
        返还 渠道接口_带宽估计。
    完。
    当前 为 发送字节 乘 1000 除以 耗时毫秒。
    渠道接口_带宽估计 为 渠道接口_带宽估计 乘 700 除以 1000 加 当前 乘 300 除以 1000。
    返还 渠道接口_带宽估计。
完。

渠道接口_生成拥塞建议 接受 丢包率：
    做 追加 用 渠道接口_丢包窗口、丢包率。
    若 做 计数 用 渠道接口_丢包窗口 大于 10：
        渠道接口_丢包窗口 为 做 列表_切片 用 渠道接口_丢包窗口、1、做 计数 用 渠道接口_丢包窗口。
    完。
    若 丢包率 大于 50：
        返还 「减半窗口」。
    完。
    若 丢包率 小于 5：
        返还 「增大窗口」。
    完。
    返还 「保持」。
完。

渠道接口_统计 接受 零参：
    健康摘要 为 做 渠道注册表_健康摘要 用 假。
    返还 一本字典：模块 对应 「渠道接口」、渠道数 对应 渠道注册表_计数、健康渠道数 对应 健康摘要 的 健康、异常渠道数 对应 健康摘要 的 异常、广播次数 对应 渠道接口_广播数、健康巡检数 对应 渠道接口_健康巡检数、带宽估计 对应 渠道接口_带宽估计、丢包窗口长度 对应 做 计数 用 渠道接口_丢包窗口、最后错误 对应 渠道接口_最后错误。
完。

渠道接口_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    健康摘要 为 做 渠道注册表_健康摘要 用 假。
    若 渠道注册表_计数 大于 100000：
        做 追加 用 问题、「渠道注册数量异常增长」。
    完。
    若 健康摘要 的 异常 大于 健康摘要 的 健康 并且 健康摘要 的 总数 大于 0：
        做 追加 用 问题、「异常渠道多于健康渠道」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

渠道接口_综合报告 接受 零参：
    做 打印 用 「[渠道接口] ═══ 综合报告 ═══」。
    健康摘要 为 做 渠道注册表_健康摘要 用 假。
    做 打印 用 「  渠道数: 」 加 做 整数转字符串 用 渠道注册表_计数。
    做 打印 用 「  健康/异常: 」 加 做 整数转字符串 用 健康摘要 的 健康 加 「/」 加 做 整数转字符串 用 健康摘要 的 异常。
    做 打印 用 「  广播次数: 」 加 做 整数转字符串 用 渠道接口_广播数。
    做 打印 用 「  健康巡检数: 」 加 做 整数转字符串 用 渠道接口_健康巡检数。
    做 打印 用 「  带宽估计: 」 加 做 整数转字符串 用 渠道接口_带宽估计。
    做 打印 用 「  丢包窗口长度: 」 加 做 整数转字符串 用 做 计数 用 渠道接口_丢包窗口。
    做 打印 用 「  最后错误: 」 加 渠道接口_最后错误。
    做 打印 用 「  健康: 」 加 若 做 渠道接口_健康评估 则 「正常」 否则 「异常」。
完。

渠道接口_全局重置 接受 零参：
    渠道注册表 为 做 字典。
    渠道注册表_计数 为 0。
    渠道接口_广播数 为 0。
    渠道接口_健康巡检数 为 0。
    渠道接口_最后错误 为 「」。
    渠道接口_带宽估计 为 0。
    渠道接口_丢包窗口 为 一组：（空）。
    做 打印 用 「[渠道接口] 全局统计已重置」。
    返还 0。
完。

本章 收束。
