《思考链》

引用《..·中枢·类型定义》。
引用《..·中枢·系统调用表》。
引用《..·中枢·整数转字符串》。
引用《..·中枢·内核打印》。

※ NovaOS 思考链推理 (Nova思维链)
※ §1 线性思考链: 步骤1→2→3→结论
※ §2 分支思考: 多个方案并行评估→选最优
※ §3 自我反思: 检查推理是否自洽
※ §4 Nova感知-推理-执行循环: 思考→行动→观察→思考...

步骤类型_思考 为 0。
步骤类型_行动 为 1。
步骤类型_观察 为 2。
步骤类型_反思 为 3。
步骤类型_结论 为 4。

思考步骤 是 一种 事物：
    类型,。
    描述,。
    结果,。
    置信度,
    耗时毫秒,。
完。

思考链 是 一种 事物：
    步骤列表,
    最终结论,。
    总置信度,。
    已完成,。
    Nova推理执行轮数,。
完。

思考链_创建 接受 )：
    链 为 思考链 {}。
    链.步骤列表 为 []。
    链.最终结论 为 「」。
    链.总置信度 为 0。
    链.已完成 为 0。
    链.Nova推理执行轮数 为 0。
    返还 链。
完。

思考链_添加步骤 接受 链、类型、描述、结果、置信度)：
    步 为 思考步骤 {}。
    步.类型 为 类型; 步.描述 = 描述; 步.结果 = 结果; 步.置信度 = 置信度。
    步.耗时毫秒 为 0。
    做 追加 用 链.步骤列表、步。
完。

思考链_思考 接受 链、描述、结果、置信度)：
    做 思考链_添加步骤 用 链、步骤类型_思考、描述、结果、置信度。
完。

思考链_行动 接受 链、行动描述、行动结果)：
    做 思考链_添加步骤 用 链、步骤类型_行动、行动描述、行动结果、800。
完。

思考链_观察 接受 链、观察内容)：
    做 思考链_添加步骤 用 链、步骤类型_观察、「观察」、观察内容、900。
完。

思考链_反思 接受 链)：
    ※ 检查推理链自洽性: 各步骤置信度是否递增/稳定
    步数 为 做 计数 用 链.步骤列表。
    若 步数 小于 2：
    可变 下降次数 为 0。
    可变 i 为 1。
    只要 i 小于 步数：
        若 链.步骤列表[i].置信度 小于 链.步骤列表[i-1].置信度 减 200：
            下降次数 为 下降次数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 下降次数 大于 步数 除以 3：
        做 思考链_添加步骤 用 链、步骤类型_反思、「置信度下降过多、推理可能有误」、「需要重新审视」、400。
    否则：
        做 思考链_添加步骤 用 链、步骤类型_反思、「推理链自洽」、「可以继续」、800。
    完。
完。

思考链_设置结论 接受 链、结论)：
    链.最终结论 为 结论。
    链.已完成 为 1。
    ※ 计算总置信度: 所有步骤置信度的加权平均
    可变 总 为 0。
    步数 为 做 计数 用 链.步骤列表。
    可变 i 为 0。
    只要 i 小于 步数：
        总 为 总 加 链.步骤列表[i].置信度。
        i 为 i 加 1。
    完。
    若 步数 大于 0：
    做 思考链_添加步骤 用 链、步骤类型_结论、「结论」、结论、链.总置信度。
完。

※ Nova感知-推理-执行循环: 思考→行动→观察 (最多N轮)
思考链_Nova感知-推理-执行循环 接受 链、问题、最大轮数)：
    可变 轮 为 0。
    只要 轮 小于 最大轮数 并且 链.已完成 等于 0：
        链.Nova推理执行轮数 为 链.Nova推理执行轮数 加 1。
        ※ 1. 思考: 分析当前状态
        做 思考链_思考 用 链、「分析问题第」 加 做 整数转字符串 用 轮+1 加 「轮」、问题、600。
        ※ 2. 决定是否需要行动
        若 链.总置信度 大于 800：
            做 思考链_设置结论 用 链、「置信度足够、得出结论」。
            跳出。
        完。
        轮 为 轮 加 1。
    完。
完。

思考链_输出 接受 链)：
    步骤名 为 [「思考」, 「行动」, 「观察」, 「反思」, 「结论」]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 链.步骤列表：
        步 为 链.步骤列表[i]。
        类型名 为 步骤名[步.类型]。
        做 打印行 用 「[」 加 类型名 加 「] 」 加 步.描述 加 「 → 」 加 步.结果 加 「 (置信度:」 加 做 整数转字符串 用 步.置信度 加 「)」。
        i 为 i 加 1。
    完。
    若 链.已完成 等于 1：
        做 打印行 用 「═══ 结论: 」 加 链.最终结论 加 「 (总置信度:」 加 做 整数转字符串 用 链.总置信度 加 「) ═══」。
    完。
完。

思考链_步骤数 接受 链) ：返还 做 计数 用 链.步骤列表 }：
思考链_置信度 接受 链) ：返还 链.总置信度 }：

本章 收束。
