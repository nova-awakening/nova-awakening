《网聊适配》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·内容可见性》。

最大连接 为 128。

最大消息队列 为 256。

最大发送队列 为 256。

限速窗口 为 60000000。

限速上限 为 30。

网聊连接 是 一种 事物，含 用户哈希、连接标识、活跃、最近心跳、消息数、创建时刻。

网聊消息 是 一种 事物，含 用户哈希、内容哈希、时间戳。

可变 连接表 为 一组：（空）。

可变 连接数 为 0。

可变 接收队列 为 一组：（空）。

可变 接收数 为 0。

可变 发送队列 为 一组：（空）。

可变 发送数 为 0。

可变 总接收 为 0。

可变 总发送 为 0。

可变 限速拒绝 为 0。

可变 下一连接标识 为 1。

网聊_初始化 接受 零参：
    连接表 为 一组：（空）。
    连接数 为 0。
    接收队列 为 一组：（空）。
    接收数 为 0。
    发送队列 为 一组：（空）。
    发送数 为 0。
完。

网聊_新连接 接受 用户哈希：
    若 连接数 大于或等于 最大连接：
        做 网聊_清理断开。
    完。
    若 连接数 大于或等于 最大连接：
        返还 负 1。
    完。
    连接 为 新建 网聊连接。
    连接 的 用户哈希 为 用户哈希。
    连接 的 连接标识 为 下一连接标识。
    下一连接标识 为 下一连接标识 加 1。
    连接 的 活跃 为 真。
    连接 的 最近心跳 为 做 平台_获取高精度时间。
    连接 的 消息数 为 0。
    连接 的 创建时刻 为 连接 的 最近心跳。
    做 追加 用 连接表、连接。
    索引 为 连接数。
    连接数 为 连接数 加 1。
    返还 索引。
完。

网聊_断开 接受 连接索引：
    若 连接索引 大于或等于 连接数：
        返还。
    完。
    连接表 的 第 连接索引 项 的 活跃 为 假。
完。

网聊_心跳 接受 连接索引：
    若 连接索引 大于或等于 连接数：
        返还。
    完。
    连接表 的 第 连接索引 项 的 最近心跳 为 做 平台_获取高精度时间。
完。

网聊_接收消息 接受 用户哈希、内容哈希：
    若 非 做 网聊_检查限速 用 用户哈希：
        限速拒绝 为 限速拒绝 加 1。
        返还 错误_限速。
    完。
    若 接收数 大于或等于 最大消息队列：
        返还 错误_队列满。
    完。
    消息 为 新建 网聊消息。
    消息 的 用户哈希 为 用户哈希。
    消息 的 内容哈希 为 内容哈希。
    消息 的 时间戳 为 做 平台_获取高精度时间。
    做 追加 用 接收队列、消息。
    接收数 为 接收数 加 1。
    总接收 为 总接收 加 1。
    做 网聊_更新连接消息数 用 用户哈希。
    返还 0。
完。

网聊_取接收 接受 零参：
    若 接收数 等于 0：
        返还 0。
    完。
    消息 为 接收队列 的 第 0 项。
    可变 序号 为 0。
    只要 序号 小于 接收数 减 1：
        接收队列 的 第 序号 项 为 接收队列 的 第 序号 加 1 项。
        序号 为 序号 加 1。
    完。
    接收数 为 接收数 减 1。
    返还 消息。
完。

网聊_发送消息 接受 用户哈希、内容哈希：
    若 发送数 大于或等于 最大发送队列：
        返还 错误_队列满。
    完。
    消息 为 新建 网聊消息。
    消息 的 用户哈希 为 用户哈希。
    消息 的 内容哈希 为 内容哈希。
    消息 的 时间戳 为 做 平台_获取高精度时间。
    做 追加 用 发送队列、消息。
    发送数 为 发送数 加 1。
    总发送 为 总发送 加 1。
    返还 0。
完。

网聊_取发送 接受 用户哈希：
    可变 序号 为 0。
    只要 序号 小于 发送数：
        若 发送队列 的 第 序号 项 的 用户哈希 等于 用户哈希：
            消息 为 发送队列 的 第 序号 项。
            可变 移 为 序号。
            只要 移 小于 发送数 减 1：
                发送队列 的 第 移 项 为 发送队列 的 第 移 加 1 项。
                移 为 移 加 1。
            完。
            发送数 为 发送数 减 1。
            返还 消息。
        完。
        序号 为 序号 加 1。
    完。
    返还 0。
完。

网聊_检查限速 接受 用户哈希：
    现在 为 做 平台_获取高精度时间。
    窗口起始 为 现在 减 限速窗口。
    可变 计数 为 0。
    可变 序号 为 接收数 减 1。
    只要 序号 大于或等于 0 并且 计数 小于 限速上限 加 1：
        若 接收队列 的 第 序号 项 的 用户哈希 等于 用户哈希 并且 接收队列 的 第 序号 项 的 时间戳 大于 窗口起始：
            计数 为 计数 加 1。
        完。
        序号 为 序号 减 1。
    完。
    返还 计数 小于 限速上限。
完。

网聊_更新连接消息数 接受 用户哈希：
    可变 序号 为 0。
    只要 序号 小于 连接数：
        若 连接表 的 第 序号 项 的 用户哈希 等于 用户哈希 并且 连接表 的 第 序号 项 的 活跃：
            连接表 的 第 序号 项 的 消息数 为 连接表 的 第 序号 项 的 消息数 加 1。
        完。
        序号 为 序号 加 1。
    完。
完。

网聊_清理断开 接受 零参：
    现在 为 做 平台_获取高精度时间。
    超时 为 300000000。
    可变 序号 为 0。
    只要 序号 小于 连接数：
        若 连接表 的 第 序号 项 的 活跃 并且 现在 减 连接表 的 第 序号 项 的 最近心跳 大于 超时：
            连接表 的 第 序号 项 的 活跃 为 假。
        完。
        序号 为 序号 加 1。
    完。
完。

网聊_活跃连接数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 连接数：
        若 连接表 的 第 序号 项 的 活跃：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

网聊_完整统计 接受 零参：
    返还 一组：连接数、做 网聊_活跃连接数、接收数、发送数、总接收、总发送、限速拒绝。
完。

可变 AI_网聊适配_带宽估计 为 0。

可变 AI_网聊适配_丢包窗口 为 一组：（空）。

AI_网聊适配_带宽预测 接受 发送字节、耗时毫秒：
    若 耗时毫秒 等于 0：
        返还 AI_网聊适配_带宽估计。
    完。
    当前 为 发送字节 乘 1000 除以 耗时毫秒。
    AI_网聊适配_带宽估计 为 AI_网聊适配_带宽估计 乘 700 除以 1000 加 当前 乘 300 除以 1000。
    返还 AI_网聊适配_带宽估计。
完。

AI_网聊适配_拥塞建议 接受 丢包率：
    做 追加 用 AI_网聊适配_丢包窗口、丢包率。
    若 做 计数 用 AI_网聊适配_丢包窗口 大于 10：
        做 列表_移除 用 AI_网聊适配_丢包窗口、0。
    完。
    若 丢包率 大于 50：
        返还 「减半窗口」。
    完。
    若 丢包率 小于 5：
        返还 「增大窗口」。
    完。
    返还 「保持」。
完。

本章 收束。
