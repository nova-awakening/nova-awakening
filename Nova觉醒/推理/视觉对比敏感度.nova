《视觉对比敏感度》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《..·中枢·内核打印》。

引用《.·定点Q算术》。

CSF_灵敏度 接受 频率_q10：
    负指数 为 做 Q乘 用 307、频率_q10。
    exp项 为 做 Q_exp 用 负 负指数。
    返还 做 Q乘 用 频率_q10、exp项。
完。

运动掩蔽系数 接受 滚动速度：
    v2 为 做 Q乘 用 滚动速度、滚动速度。
    sigma2 为 256 乘 Q_ONE。
    返还 做 Q除 用 v2、v2 加 sigma2。
完。

注视点衰减 接受 距离、sigma：
    若 sigma 小于或等于 0：
        返还 Q_ZERO。
    完。
    归一化距离 为 做 Q除 用 距离、sigma。
    d2 为 做 Q乘 用 归一化距离、归一化距离。
    指数 为 负 (d2 右移 1)。
    返还 做 Q_exp 用 指数。
完。

可变 全局PCSO统计 为 新建 PCSO统计结构。

PCSO统计结构 是 一种 事物，含 CSF计算次数、运动掩蔽次数、注视衰减次数、亮度适应次数、时域CSF次数、感知质量评估次数、AI优化次数。

亮度_暂经最小 为 1。

亮度_室内典型 为 300。

亮度_室外典型 为 5000。

亮度适应_因子 接受 环境亮度：
    若 环境亮度 小于或等于 0：
        环境亮度 为 亮度_暂经最小。
    完。
    可变 log_val 为 0。
    可变 temp 为 环境亮度。
    只要 temp 大于 1：
        temp 为 temp 右移 1。
        log_val 为 log_val 加 1。
    完。
    最大log 为 13。
    若 log_val 大于 最大log：
        log_val 为 最大log。
    完。
    灵敏度 为 log_val 乘 Q_ONE 除以 最大log。
    全局PCSO统计 的 亮度适应次数 为 全局PCSO统计 的 亮度适应次数 加 1。
    返还 灵敏度。
完。

亮度加权CSF 接受 频率_q10、环境亮度：
    基SF 为 做 CSF_灵敏度 用 频率_q10。
    适应因子 为 做 亮度适应_因子 用 环境亮度。
    返还 做 Q乘 用 基SF、适应因子。
完。

时域_峰值频率 为 8192。

时域_临界融合频率 为 61440。

时域_衰减系数 为 102。

时域CSF 接受 时间频率_q10：
    若 时间频率_q10 小于或等于 0：
        返还 0。
    完。
    指数 为 做 Q乘 用 时域_衰减系数、时间频率_q10。
    exp项 为 做 Q_exp 用 负 指数。
    比值 为 做 Q除 用 时间频率_q10、时域_峰值频率。
    比值平方 为 做 Q乘 用 比值、比值。
    分母 为 Q_ONE 加 比值平方。
    若 分母 等于 0：
        分母 为 1。
    完。
    分子 为 做 Q乘 用 时间频率_q10、exp项。
    全局PCSO统计 的 时域CSF次数 为 全局PCSO统计 的 时域CSF次数 加 1。
    返还 做 Q除 用 分子、分母。
完。

帧率感知质量 接受 当前帧率、目标帧率：
    若 当前帧率 大于或等于 时域_临界融合频率：
        返还 Q_ONE。
    完。
    当前感知 为 做 时域CSF 用 当前帧率。
    目标感知 为 做 时域CSF 用 目标帧率。
    若 目标感知 等于 0：
        返还 Q_ONE。
    完。
    返还 做 Q除 用 当前感知、目标感知。
完。

对比掩蔽_阈值 接受 基础阈值、掩蔽对比度、基础对比度：
    若 基础对比度 小于或等于 0：
        返还 基础阈值。
    完。
    比率 为 做 Q除 用 掩蔽对比度、基础对比度。
    掩蔽因子 为 Q_ONE 加 做 Q乘 用 比率、666。
    返还 做 Q乘 用 基础阈值、掩蔽因子。
完。

纹理掩蔽系数 接受 区域方差、均值：
    若 均值 小于或等于 0：
        均值 为 1。
    完。
    变异系数 为 做 Q除 用 区域方差、均值。
    cv2 为 做 Q乘 用 变异系数、变异系数。
    阈值平方 为 做 Q乘 用 512、512。
    返还 做 Q除 用 cv2、cv2 加 阈值平方。
完。

中央凹_半角 为 2。

像素每度 为 60。

中央凹区域 是 一种 事物，含 注视点X、注视点Y、精度等级。

中央凹_精度等级 接受 注视X、注视Y、目标X、目标Y：
    dx 为 目标X 减 注视X。
    dy 为 目标Y 减 注视Y。
    若 dx 小于 0：
        dx 为 负 dx。
    完。
    若 dy 小于 0：
        dy 为 负 dy。
    完。
    可变 最大 为 dx。
    可变 最小 为 dy。
    若 dy 大于 dx：
        最大 为 dy。
        最小 为 dx。
    完。
    距离 为 最大 加 最小 乘 420 除以 1024。
    角度 为 距离 除以 像素每度。
    若 角度 小于或等于 2：
        返还 0。
    完。
    若 角度 小于或等于 5：
        返还 1。
    完。
    若 角度 小于或等于 15：
        返还 2。
    完。
    返还 3。
完。

中央凹_缩放因子 接受 精度等级：
    若 精度等级 等于 0：
        返还 Q_ONE。
    完。
    若 精度等级 等于 1：
        返还 Q_ONE 右移 1。
    完。
    若 精度等级 等于 2：
        返还 Q_ONE 右移 2。
    完。
    返还 Q_ONE 右移 3。
完。

中央凹_节省率估算 接受 屏宽、屏高、注视X、注视Y：
    可变 全精度像素 为 0。
    可变 实际像素 为 0。
    可变 y 为 0。
    只要 y 小于 屏高：
        可变 x 为 0。
        只要 x 小于 屏宽：
            等级 为 做 中央凹_精度等级 用 注视X、注视Y、x、y。
            因子 为 做 中央凹_缩放因子 用 等级。
            全精度像素 为 全精度像素 加 Q_ONE。
            实际像素 为 实际像素 加 因子。
            x 为 x 加 16。
        完。
        y 为 y 加 16。
    完。
    若 全精度像素 等于 0：
        返还 0。
    完。
    返还 Q_ONE 减 做 Q除 用 实际像素、全精度像素。
完。

色彩通道_L 为 0。

色彩通道_a 为 1。

色彩通道_b 为 2。

色彩通道CSF 接受 频率_q10、通道：
    基础 为 做 CSF_灵敏度 用 频率_q10。
    若 通道 等于 色彩通道_L：
        返还 基础。
    完。
    若 通道 等于 色彩通道_a：
        衰减 为 做 Q乘 用 基础、717。
        若 频率_q10 大于 8192：
            衰减 为 衰减 右移 1。
        完。
        返还 衰减。
    完。
    衰减 为 做 Q乘 用 基础、512。
    若 频率_q10 大于 4096：
        衰减 为 衰减 右移 2。
    完。
    返还 衰减。
完。

视觉显著性映射 是 一种 事物，含 网格宽、网格高、网格数据、显著阈值。

显著性_创建 接受 屏宽、屏高、格子大小：
    map 为 做 堆_分配 用 视觉显著性映射。
    若 map 等于 0：
        返还 空。
    完。
    map 的 网格宽 为 (屏宽 加 格子大小 减 1) 除以 格子大小。
    map 的 网格高 为 (屏高 加 格子大小 减 1) 除以 格子大小。
    map 的 网格数据 为 做 Tensor_创建 用 map 的 网格宽 乘 map 的 网格高。
    map 的 显著阈值 为 512。
    返还 map。
完。

显著性_计算网格 接受 亮度差、色彩差、方向差：
    wL 为 512。
    wC 为 307。
    wO 为 205。
    若 亮度差 小于 0：
        亮度差 为 负 亮度差。
    完。
    若 色彩差 小于 0：
        色彩差 为 负 色彩差。
    完。
    若 方向差 小于 0：
        方向差 为 负 方向差。
    完。
    结果 为 做 Q乘 用 wL、亮度差 加 做 Q乘 用 wC、色彩差 加 做 Q乘 用 wO、方向差。
    若 结果 大于 Q_ONE：
        结果 为 Q_ONE。
    完。
    返还 结果。
完。

感知质量结果 是 一种 事物，含 空间质量、时域质量、色彩质量、综合得分、可降级比例。

感知质量_评估 接受 频率、亮度、帧率、注视距离、滚动速度：
    结果 为 做 堆_分配 用 感知质量结果。
    若 结果 等于 0：
        返还 空。
    完。
    结果 的 空间质量 为 做 亮度加权CSF 用 频率、亮度。
    结果 的 时域质量 为 做 帧率感知质量 用 帧率、时域_临界融合频率。
    结果 的 色彩质量 为 做 色彩通道CSF 用 频率、色彩通道_L。
    掩蔽 为 做 运动掩蔽系数 用 滚动速度。
    衰减 为 做 注视点衰减 用 注视距离、307。
    加权空间 为 做 Q乘 用 结果 的 空间质量、512。
    加权时域 为 做 Q乘 用 结果 的 时域质量、307。
    加权色彩 为 做 Q乘 用 结果 的 色彩质量、205。
    结果 的 综合得分 为 加权空间 加 加权时域 加 加权色彩。
    可感知 为 做 Q乘 用 Q_ONE 减 掩蔽、衰减。
    结果 的 可降级比例 为 Q_ONE 减 可感知。
    全局PCSO统计 的 感知质量评估次数 为 全局PCSO统计 的 感知质量评估次数 加 1。
    返还 结果。
完。

AI_感知优化_帧质量 接受 频率、亮度、帧率、注视距离、滚动速度：
    质量 为 做 感知质量_评估 用 频率、亮度、帧率、注视距离、滚动速度。
    若 质量 等于 空：
        返还 Q_ONE。
    完。
    特征 为 做 Tensor_创建 用 6。
    做 Tensor_写 用 特征、0、质量 的 空间质量。
    做 Tensor_写 用 特征、1、质量 的 时域质量。
    做 Tensor_写 用 特征、2、质量 的 色彩质量。
    做 Tensor_写 用 特征、3、质量 的 综合得分。
    做 Tensor_写 用 特征、4、质量 的 可降级比例。
    做 Tensor_写 用 特征、5、滚动速度。
    AI结果 为 做 AI总线_推理 用 全局AI总线、特征。
    做 Tensor_释放 用 特征。
    全局PCSO统计 的 AI优化次数 为 全局PCSO统计 的 AI优化次数 加 1。
    若 AI结果 大于 0：
        返还 AI结果。
    完。
    返还 质量 的 综合得分。
完。

AI_自适应渲染策略 接受 屏宽、屏高、注视X、注视Y、帧率、滚动速度：
    中央凹节省 为 做 中央凹_节省率估算 用 屏宽、屏高、注视X、注视Y。
    运动节省 为 做 运动掩蔽系数 用 滚动速度。
    时域节省 为 Q_ONE 减 做 帧率感知质量 用 帧率、时域_临界融合频率。
    总节省 为 中央凹节省。
    若 运动节省 大于 总节省：
        总节省 为 运动节省。
    完。
    总节省 为 总节省 加 做 Q乘 用 时域节省、205。
    若 总节省 大于 870：
        总节省 为 870。
    完。
    返还 总节省。
完。

扫视跳_最小持续 为 20。

扫视跳_最大持续 为 70。

固视_典型持续 为 250。

眼动状态 是 一种 事物，含 当前注视X、当前注视Y、上次注视X、上次注视Y、是否扫视跳、扫视跳进度、扫视跳总时长、固视进度。

眼动_是否扫视跳中 接受 状态：
    若 状态 等于 空：
        返还 假。
    完。
    返还 状态 的 是否扫视跳 等于 1。
完。

眼动_扫视跳抑制因子 接受 状态：
    若 状态 等于 空：
        返还 Q_ONE。
    完。
    若 状态 的 是否扫视跳 等于 0：
        返还 Q_ONE。
    完。
    进度 为 做 Q除 用 状态 的 扫视跳进度 乘 Q_ONE、状态 的 扫视跳总时长。
    可变 抑制 为 进度。
    若 进度 大于 Q_HALF：
        抑制 为 Q_ONE 减 进度。
    完。
    抑制 为 抑制 左移 1。
    若 抑制 大于 Q_ONE：
        抑制 为 Q_ONE。
    完。
    返还 Q_ONE 减 抑制。
完。

多带CSF 接受 频率_q10、带索引：
    基础 为 做 CSF_灵敏度 用 频率_q10。
    若 带索引 等于 0：
        返还 做 Q乘 用 基础、717。
    完。
    若 带索引 等于 1：
        返还 基础。
    完。
    返还 做 Q乘 用 基础、410。
完。

PCSO_打印诊断 接受 零参：
    做 内核打印 用 「╔════════════════════════════════════════════╗」。
    做 内核打印 用 「║  PCSO 感知对比灵敏度优化 诊断报告       ║」。
    做 内核打印 用 「╠════════════════════════════════════════════╣」。
    做 内核打印 用 「║ CSF计算次数:    {}」、全局PCSO统计 的 CSF计算次数。
    做 内核打印 用 「║ 运动掩蔽次数:  {}」、全局PCSO统计 的 运动掩蔽次数。
    做 内核打印 用 「║ 注视衰减次数:  {}」、全局PCSO统计 的 注视衰减次数。
    做 内核打印 用 「║ 亮度适应次数:  {}」、全局PCSO统计 的 亮度适应次数。
    做 内核打印 用 「║ 时域CSF次数:    {}」、全局PCSO统计 的 时域CSF次数。
    做 内核打印 用 「║ 感知质量评估:  {}」、全局PCSO统计 的 感知质量评估次数。
    做 内核打印 用 「║ AI优化次数:    {}」、全局PCSO统计 的 AI优化次数。
    做 内核打印 用 「╚════════════════════════════════════════════╝」。
完。

本章 收束。
