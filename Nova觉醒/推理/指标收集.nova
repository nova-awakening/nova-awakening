《指标收集》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·哈希整数》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

最大指标 为 128。

直方图桶数 为 8。

类型_计数器 为 0。

类型_仪表 为 1。

类型_直方图 为 2。

指标项 是 一种 事物，含 名称哈希、类型、值、最小值、最大值、样本数、桶数据。

可变 指标表 为 一组：（空）。

可变 指标数 为 0。

可变 采集次数 为 0。

指标_初始化 接受 零参：
    指标表 为 一组：（空）。
    指标数 为 0。
完。

指标_注册 接受 名称、类型：
    若 指标数 大于或等于 最大指标：
        返还 负 1。
    完。
    项 为 新建 指标项。
    项 的 名称哈希 为 做 哈希_字符串 用 名称。
    项 的 类型 为 类型。
    项 的 值 为 0。
    项 的 最小值 为 0x7FFFFFFFFFFFFFFF。
    项 的 最大值 为 0。
    项 的 样本数 为 0。
    项 的 桶数据 为 0。
    若 类型 等于 类型_直方图：
        项 的 桶数据 为 做 堆_分配 用 直方图桶数 乘 4。
        可变 序号 为 0。
        只要 序号 小于 直方图桶数：
            做 内存_写32 用 项 的 桶数据 加 序号 乘 4、0。
            序号 为 序号 加 1。
        完。
    完。
    做 追加 用 指标表、项。
    索引 为 指标数。
    指标数 为 指标数 加 1。
    返还 索引。
完。

指标_查找 接受 名称哈希：
    可变 序号 为 0。
    只要 序号 小于 指标数：
        若 指标表 的 第 序号 项 的 名称哈希 等于 名称哈希：
            返还 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 负 1。
完。

指标_递增 接受 名称：
    采集次数 为 采集次数 加 1。
    索引 为 做 指标_查找 用 做 哈希_字符串 用 名称。
    若 索引 小于 0：
        索引 为 做 指标_注册 用 名称、类型_计数器。
    完。
    若 索引 大于或等于 0：
        指标表 的 第 索引 项 的 值 为 指标表 的 第 索引 项 的 值 加 1。
    完。
完。

指标_递增量 接受 名称、增量：
    采集次数 为 采集次数 加 1。
    索引 为 做 指标_查找 用 做 哈希_字符串 用 名称。
    若 索引 小于 0：
        索引 为 做 指标_注册 用 名称、类型_计数器。
    完。
    若 索引 大于或等于 0：
        指标表 的 第 索引 项 的 值 为 指标表 的 第 索引 项 的 值 加 增量。
    完。
完。

指标_设仪表 接受 名称、值：
    采集次数 为 采集次数 加 1。
    索引 为 做 指标_查找 用 做 哈希_字符串 用 名称。
    若 索引 小于 0：
        索引 为 做 指标_注册 用 名称、类型_仪表。
    完。
    若 索引 大于或等于 0：
        指标表 的 第 索引 项 的 值 为 值。
    完。
完。

指标_记录直方图 接受 名称、值：
    采集次数 为 采集次数 加 1。
    索引 为 做 指标_查找 用 做 哈希_字符串 用 名称。
    若 索引 小于 0：
        索引 为 做 指标_注册 用 名称、类型_直方图。
    完。
    若 索引 小于 0：
        返还。
    完。
    项 为 指标表 的 第 索引 项。
    项 的 样本数 为 项 的 样本数 加 1。
    项 的 值 为 项 的 值 加 值。
    若 值 小于 项 的 最小值：
        项 的 最小值 为 值。
    完。
    若 值 大于 项 的 最大值：
        项 的 最大值 为 值。
    完。
    若 项 的 桶数据 不等于 0：
        可变 桶号 为 0。
        若 值 小于 1000：
            桶号 为 0。
        否则若 值 小于 5000：
            桶号 为 1。
        否则若 值 小于 10000：
            桶号 为 2。
        否则若 值 小于 50000：
            桶号 为 3。
        否则若 值 小于 100000：
            桶号 为 4。
        否则若 值 小于 500000：
            桶号 为 5。
        否则若 值 小于 1000000：
            桶号 为 6。
        否则：
            桶号 为 7。
        完。
        做 内存_写32 用 项 的 桶数据 加 桶号 乘 4、做 内存_读32 用 项 的 桶数据 加 桶号 乘 4 加 1。
    完。
完。

指标_获取值 接受 名称：
    索引 为 做 指标_查找 用 做 哈希_字符串 用 名称。
    若 索引 小于 0：
        返还 0。
    完。
    返还 指标表 的 第 索引 项 的 值。
完。

指标_获取平均 接受 名称：
    索引 为 做 指标_查找 用 做 哈希_字符串 用 名称。
    若 索引 小于 0 或者 指标表 的 第 索引 项 的 样本数 等于 0：
        返还 0。
    完。
    返还 指标表 的 第 索引 项 的 值 除以 指标表 的 第 索引 项 的 样本数。
完。

指标_获取最大 接受 名称：
    索引 为 做 指标_查找 用 做 哈希_字符串 用 名称。
    若 索引 小于 0：
        返还 0。
    完。
    返还 指标表 的 第 索引 项 的 最大值。
完。

指标_获取最小 接受 名称：
    索引 为 做 指标_查找 用 做 哈希_字符串 用 名称。
    若 索引 小于 0：
        返还 0。
    完。
    返还 指标表 的 第 索引 项 的 最小值。
完。

指标_重置 接受 名称：
    索引 为 做 指标_查找 用 做 哈希_字符串 用 名称。
    若 索引 小于 0：
        返还。
    完。
    指标表 的 第 索引 项 的 值 为 0。
    指标表 的 第 索引 项 的 样本数 为 0。
    指标表 的 第 索引 项 的 最小值 为 0x7FFFFFFFFFFFFFFF。
    指标表 的 第 索引 项 的 最大值 为 0。
完。

指标_完整统计 接受 零参：
    返还 一组：指标数、采集次数。
完。

本章 收束。
