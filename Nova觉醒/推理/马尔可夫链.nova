《马尔可夫链》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·定点Q算术》。

引用《..·感知·内核直连层》。

动作数量 为 11。

动作_空闲 为 0。

动作_滚动 为 1。

动作_点击 为 2。

动作_输入 为 3。

动作_悬停 为 4。

动作_后退 为 5。

动作_前进 为 6。

动作_切换标签 为 7。

动作_关闭标签 为 8。

动作_搜索 为 9。

动作_缩放 为 10。

MarkovChain 是 一种 事物，含 转移计数、转移概率、总次数、上一动作、总转移数、衰减因子。

MarkovChain_创建 接受 零参：
    mc 为 做 堆_分配 用 MarkovChain。
    mc 的 转移计数 为 做 Tensor_创建 用 动作数量、动作数量。
    mc 的 转移概率 为 做 Tensor_创建 用 动作数量、动作数量。
    mc 的 总次数 为 做 Tensor_创建 用 1、动作数量。
    mc 的 上一动作 为 负 1。
    mc 的 总转移数 为 0。
    mc 的 衰减因子 为 1014。
    初始计数 为 Q_ONE 除以 动作数量。
    可变 i 为 0。
    只要 i 小于 动作数量：
        可变 j 为 0。
        只要 j 小于 动作数量：
            做 Tensor_写 用 mc 的 转移计数、i、j、初始计数。
            j 为 j 加 1。
        完。
        做 Tensor_写 用 mc 的 总次数、0、i、初始计数 乘 动作数量。
        i 为 i 加 1。
    完。
    做 MarkovChain_重算概率 用 mc。
    返还 mc。
完。

MarkovChain_观察 接受 mc、当前动作：
    若 mc 的 上一动作 大于或等于 0 并且 mc 的 上一动作 小于 动作数量：
        prev 为 mc 的 上一动作。
        若 mc 的 总转移数 大于 100 并且 mc 的 总转移数 取余于 50 等于 0：
            可变 i 为 0。
            只要 i 小于 动作数量：
                可变 j 为 0。
                可变 行总 为 0。
                只要 j 小于 动作数量：
                    旧 为 做 Tensor_读 用 mc 的 转移计数、i、j。
                    新 为 做 Q乘 用 旧、mc 的 衰减因子。
                    做 Tensor_写 用 mc 的 转移计数、i、j、新。
                    行总 为 行总 加 新。
                    j 为 j 加 1。
                完。
                做 Tensor_写 用 mc 的 总次数、0、i、行总。
                i 为 i 加 1。
            完。
        完。
        旧计数 为 做 Tensor_读 用 mc 的 转移计数、prev、当前动作。
        做 Tensor_写 用 mc 的 转移计数、prev、当前动作、旧计数 加 Q_ONE。
        旧总 为 做 Tensor_读 用 mc 的 总次数、0、prev。
        做 Tensor_写 用 mc 的 总次数、0、prev、旧总 加 Q_ONE。
        做 MarkovChain_重算概率 用 mc。
    完。
    mc 的 上一动作 为 当前动作。
    mc 的 总转移数 为 mc 的 总转移数 加 1。
完。

MarkovChain_预测 接受 mc：
    若 mc 的 上一动作 小于 0：
        返还 动作_空闲。
    完。
    可变 最大概率 为 0。
    可变 最佳动作 为 0。
    可变 j 为 0。
    只要 j 小于 动作数量：
        p 为 做 Tensor_读 用 mc 的 转移概率、mc 的 上一动作、j。
        若 p 大于 最大概率：
            最大概率 为 p。
            最佳动作 为 j。
        完。
        j 为 j 加 1。
    完。
    返还 最佳动作。
完。

MarkovChain_预测TopK 接受 mc、K、结果动作、结果概率：
    若 mc 的 上一动作 小于 0 或者 K 小于或等于 0：
        返还 0。
    完。
    已选 为 做 Tensor_创建 用 1、动作数量。
    可变 选了 为 0。
    只要 选了 小于 K 并且 选了 小于 动作数量：
        可变 最大 为 负 1。
        可变 最大j 为 0。
        可变 j 为 0。
        只要 j 小于 动作数量：
            若 做 Tensor_读 用 已选、0、j 等于 0：
                p 为 做 Tensor_读 用 mc 的 转移概率、mc 的 上一动作、j。
                若 p 大于 最大：
                    最大 为 p。
                    最大j 为 j。
                完。
            完。
            j 为 j 加 1。
        完。
        做 内存_写32 用 结果动作 加 选了 乘 4、最大j。
        做 内存_写32 用 结果概率 加 选了 乘 4、最大。
        做 Tensor_写 用 已选、0、最大j、1。
        选了 为 选了 加 1。
    完。
    做 Tensor_释放 用 已选。
    返还 选了。
完。

本章 收束。
