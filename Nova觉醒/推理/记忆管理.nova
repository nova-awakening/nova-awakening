《记忆管理》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《.·通信总线》。

引用《..·中枢·自动扩容》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《..·中枢·字符串替换》。

引用《.·定点Q算术》。

引用《..·中枢·键值缓存》。

引用《..·中枢·巩固管线》。

引用《..·标准·数学幂根对数》。

引用《.·语义嵌入》。

引用《..·中枢·工作记忆记》。

引用《..·中枢·时钟中枢》。

STM_最大容量 为 128。

EM_最大容量 为 256。

LTM_最大容量 为 4096。

语义索引容量 为 512。

嵌入维度 为 嵌入维度_节点。

衰减_λ 为 0.01。

情感加成 为 1.5。

巩固_基准间隔 为 60000。

组装_最大片段 为 20。

记忆类型_短期 为 1。

记忆类型_情景 为 2。

记忆类型_长期 为 3。

记忆类型_语义 为 4。

短期记忆条目 是 一种 事物，含 内容、时间戳、重要度、情感值、访问次数。

情景记忆条目 是 一种 事物，含 时间、事件、上下文、重要度、情感标签、巩固次数、下次巩固时间。

长期记忆条目 是 一种 事物，含 键、值、创建时间、最后访问、访问次数、重要度。

语义索引条目 是 一种 事物，含 概念、嵌入向量、关联记忆、访问次数、最近匹配时间。

AMMS统计 是 一种 事物，含 STM添加数、STM淘汰数、STM查询数、EM添加数、EM查询数、LTM写入数、LTM读取数、LTM未命中数、组装次数、巩固次数、AI预测次数、语义搜索数。

可变 短期记忆 为 一组：（空）。

可变 情景记忆库 为 一组：（空）。

可变 长期记忆 为 做 字典。

可变 语义索引 为 一组：（空）。

可变 语义索引草图 为 一组：（空）。

可变 语义索引质量 为 一组：（空）。

可变 智能体记忆_时钟 为 0。

可变 AMMS全局统计 为 新建 AMMS统计。

语义索引_释放 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 语义索引：
        若 语义索引 的 第 i 项 的 嵌入向量 不等于 0：
            做 Tensor_释放 用 语义索引 的 第 i 项 的 嵌入向量。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 语义索引草图：
        若 语义索引草图 的 第 i 项 不等于 0：
            做 Tensor_释放 用 语义索引草图 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    语义索引 为 一组：（空）。
    语义索引草图 为 一组：（空）。
    语义索引质量 为 一组：（空）。
完。

智能体记忆_添加短期 接受 内容、重要度：
    智能体记忆_时钟 为 智能体记忆_时钟 加 1。
    条目 为 新建 短期记忆条目：内容 为 内容、时间戳 为 智能体记忆_时钟、重要度 为 重要度、情感值 为 500、访问次数 为 0。
    做 追加 用 短期记忆、条目。
    AMMS全局统计 的 STM添加数 为 AMMS全局统计 的 STM添加数 加 1。
    若 做 计数 用 短期记忆 大于 STM_最大容量：
        做 STM_淘汰最弱。
    完。
    做 工作写入_文本 用 内容。
完。

STM_淘汰最弱 接受 零参：
    可变 最弱 为 0。
    可变 最弱分 为 999999.0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 短期记忆：
        m 为 短期记忆 的 第 i 项。
        年龄 为 智能体记忆_时钟 减 m 的 时间戳。
        λ有效 为 衰减_λ 乘 (1000 减 m 的 重要度 乘 9 除以 10) 除以 1000。
        若 λ有效 小于或等于 0：
            λ有效 为 衰减_λ 除以 10。
        完。
        衰减因子 为 做 数学_exp 用 0 减 λ有效 乘 年龄。
        情感加成值 为 1.0。
        若 m 的 情感值 大于 500：
            情感加成值 为 1.0 加 (m 的 情感值 减 500) 乘 (情感加成 减 1.0) 除以 500。
        完。
        分 为 m 的 重要度 乘 衰减因子 乘 情感加成值 加 m 的 访问次数 乘 10。
        若 分 小于 最弱分：
            最弱分 为 分。
            最弱 为 i。
        完。
        i 为 i 加 1。
    完。
    做 列表_移除 用 短期记忆、最弱。
    AMMS全局统计 的 STM淘汰数 为 AMMS全局统计 的 STM淘汰数 加 1。
完。

智能体记忆_最近 接受 数量：
    AMMS全局统计 的 STM查询数 为 AMMS全局统计 的 STM查询数 加 1。
    n 为 做 计数 用 短期记忆。
    若 n 小于或等于 数量：
        返还 短期记忆。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 n 减 数量。
    只要 i 小于 n：
        做 追加 用 结果、短期记忆 的 第 i 项。
        短期记忆 的 第 i 项 的 访问次数 为 短期记忆 的 第 i 项 的 访问次数 加 1。
        i 为 i 加 1。
    完。
    返还 结果。
完。

智能体记忆_添加短期_带情感 接受 内容、重要度、情感值：
    智能体记忆_时钟 为 智能体记忆_时钟 加 1。
    条目 为 新建 短期记忆条目：内容 为 内容、时间戳 为 智能体记忆_时钟、重要度 为 重要度、情感值 为 情感值、访问次数 为 0。
    做 追加 用 短期记忆、条目。
    AMMS全局统计 的 STM添加数 为 AMMS全局统计 的 STM添加数 加 1。
    若 做 计数 用 短期记忆 大于 STM_最大容量：
        做 STM_淘汰最弱。
    完。
    做 工作写入_文本 用 内容。
完。

情景记忆_记录 接受 事件、上下文、重要度、情感标签：
    条目 为 新建 情景记忆条目：时间 为 做 读取毫秒、事件 为 事件、上下文 为 上下文、重要度 为 重要度、情感标签 为 情感标签、巩固次数 为 0、下次巩固时间 为 做 读取毫秒 加 巩固_基准间隔。
    做 追加 用 情景记忆库、条目。
    AMMS全局统计 的 EM添加数 为 AMMS全局统计 的 EM添加数 加 1。
    若 做 计数 用 情景记忆库 大于 EM_最大容量：
        做 情景记忆_淘汰。
    完。
完。

情景记忆_淘汰 接受 零参：
    可变 最弱 为 0。
    可变 最弱分 为 999999.0。
    now 为 做 读取毫秒。
    可变 i 为 0。
    只要 i 小于 做 计数 用 情景记忆库：
        m 为 情景记忆库 的 第 i 项。
        年龄 为 (now 减 m 的 时间) 除以 1000。
        分 为 m 的 重要度 乘 做 数学_exp 用 0 减 衰减_λ 乘 年龄 加 m 的 巩固次数 乘 50。
        若 分 小于 最弱分：
            最弱分 为 分。
            最弱 为 i。
        完。
        i 为 i 加 1。
    完。
    做 列表_移除 用 情景记忆库、最弱。
完。

情景记忆_搜索 接受 查询、最大数：
    AMMS全局统计 的 EM查询数 为 AMMS全局统计 的 EM查询数 加 1。
    可变 结果 为 一组：（空）。
    查询哈希 为 做 字符串哈希 用 查询。
    可变 i 为 0。
    只要 i 小于 做 计数 用 情景记忆库：
        m 为 情景记忆库 的 第 i 项。
        相关 为 0。
        若 做 字符串_包含 用 m 的 事件、查询：
            相关 为 相关 加 500。
        完。
        若 做 字符串_包含 用 m 的 上下文、查询：
            相关 为 相关 加 300。
        完。
        相关 为 相关 加 m 的 重要度 除以 10。
        若 相关 大于 100：
            做 追加 用 结果、一本字典：条目 对应 m、分数 对应 相关。
        完。
        i 为 i 加 1。
    完。
    结果 为 做 列表_排序 用 结果、以 a、b 为 引，其做法是：返还 b 的 分数 减 a 的 分数。
    若 做 计数 用 结果 大于 最大数：
        结果 为 做 列表_截取 用 结果、0、最大数。
    完。
    返还 结果。
完。

智能体记忆_保存长期 接受 键、值：
    做 字典设 用 长期记忆、键、一本字典：值 对应 值、创建时间 对应 做 读取毫秒、最后访问 对应 做 读取毫秒、访问次数 对应 0、重要度 对应 500。
    AMMS全局统计 的 LTM写入数 为 AMMS全局统计 的 LTM写入数 加 1。
    做 KV_写入 用 做 字符串哈希 用 键、做 字符串哈希 用 值、0。
完。

智能体记忆_保存长期_带重要度 接受 键、值、重要度：
    做 字典设 用 长期记忆、键、一本字典：值 对应 值、创建时间 对应 做 读取毫秒、最后访问 对应 做 读取毫秒、访问次数 对应 0、重要度 对应 重要度。
    AMMS全局统计 的 LTM写入数 为 AMMS全局统计 的 LTM写入数 加 1。
    做 KV_写入 用 做 字符串哈希 用 键、做 字符串哈希 用 值、0。
完。

智能体记忆_检索 接受 键：
    AMMS全局统计 的 LTM读取数 为 AMMS全局统计 的 LTM读取数 加 1。
    本地 为 做 字典取 用 长期记忆、键。
    若 本地 不等于 空：
        本地 的 最后访问 为 做 读取毫秒。
        本地 的 访问次数 为 本地 的 访问次数 加 1。
        返还 本地 的 值。
    完。
    缓存 为 做 KV_读取 用 做 字符串哈希 用 键。
    若 缓存 不等于 负 1：
        返还 缓存。
    完。
    AMMS全局统计 的 LTM未命中数 为 AMMS全局统计 的 LTM未命中数 加 1。
    返还 空。
完。

智能体记忆_批量检索 接受 键列表：
    可变 结果 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        v 为 做 智能体记忆_检索 用 键列表 的 第 i 项。
        若 v 不等于 空：
            做 字典设 用 结果、键列表 的 第 i 项、v。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

智能体记忆_删除长期 接受 键：
    做 字典删 用 长期记忆、键。
    做 KV_删除 用 做 字符串哈希 用 键。
完。

语义索引_添加 接受 概念、关联记忆ID：
    若 做 计数 用 语义索引 大于或等于 语义索引容量：
        返还。
    完。
    新向量 为 做 语义嵌入_编码 用 概念。
    新草图 为 做 语义嵌入_压缩草图 用 新向量、嵌入维度_节点。
    新质量 为 做 语义嵌入_草图质量 用 新向量、新草图。
    若 新质量 小于 650：
        若 新向量 不等于 0：
            做 Tensor_释放 用 新向量。
        完。
        若 新草图 不等于 0：
            做 Tensor_释放 用 新草图。
        完。
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语义索引：
        若 语义索引 的 第 i 项 的 概念 等于 概念：
            可变 接纳向量 为 假。
            可变 接纳草图 为 假。
            做 追加 用 语义索引 的 第 i 项 的 关联记忆、关联记忆ID。
            若 语义索引 的 第 i 项 的 嵌入向量 等于 0：
                语义索引 的 第 i 项 的 嵌入向量 为 新向量。
                接纳向量 为 真。
            完。
            若 i 小于 做 计数 用 语义索引草图 并且 语义索引草图 的 第 i 项 等于 0 并且 新草图 不等于 0：
                语义索引草图 的 第 i 项 为 新草图。
                接纳草图 为 真。
            完。
            若 i 小于 做 计数 用 语义索引质量 并且 语义索引质量 的 第 i 项 小于 新质量：
                语义索引质量 的 第 i 项 为 新质量。
            完。
            若 i 大于或等于 做 计数 用 语义索引质量：
                可变 k 为 做 计数 用 语义索引质量。
                只要 k 小于 i：
                    做 追加 用 语义索引质量、0。
                    k 为 k 加 1。
                完。
                做 追加 用 语义索引质量、新质量。
            完。
            若 接纳向量 等于 假 并且 新向量 不等于 0：
                做 Tensor_释放 用 新向量。
            完。
            若 接纳草图 等于 假 并且 新草图 不等于 0：
                做 Tensor_释放 用 新草图。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
    条目 为 新建 语义索引条目：概念 为 概念、嵌入向量 为 新向量、关联记忆 为 一组：关联记忆ID、访问次数 为 0、最近匹配时间 为 0。
    做 追加 用 语义索引、条目。
    做 追加 用 语义索引草图、新草图。
    做 追加 用 语义索引质量、新质量。
完。

语义索引_限制值 接受 值、最大值：
    若 值 大于 最大值：
        返还 最大值。
    完。
    返还 值。
完。

语义索引_综合评分 接受 基础分、访问次数、关联数、最近匹配时间、当前时间：
    访问奖励 为 做 语义索引_限制值 用 访问次数 乘 12、96。
    关联奖励 为 做 语义索引_限制值 用 关联数 乘 24、120。
    可变 新鲜度奖励 为 0。
    若 最近匹配时间 大于 0：
        年龄毫秒 为 当前时间 减 最近匹配时间。
        若 年龄毫秒 小于 60000：
            新鲜度奖励 为 48。
        否则若 年龄毫秒 小于 300000：
            新鲜度奖励 为 24。
        否则若 年龄毫秒 小于 3600000：
            新鲜度奖励 为 12。
        完。
    完。
    返还 基础分 加 访问奖励 加 关联奖励 加 新鲜度奖励。
完。

语义索引_搜索 接受 查询、最大数：
    AMMS全局统计 的 语义搜索数 为 AMMS全局统计 的 语义搜索数 加 1。
    可变 结果 为 一组：（空）。
    查询向量 为 做 语义嵌入_编码 用 查询。
    查询草图 为 做 语义嵌入_压缩草图 用 查询向量、嵌入维度_节点。
    当前时间 为 做 读取毫秒。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语义索引：
        基础分 为 0。
        若 查询向量 不等于 0 并且 语义索引 的 第 i 项 的 嵌入向量 不等于 0：
            基础分 为 做 余弦相似度 用 查询向量、语义索引 的 第 i 项 的 嵌入向量、嵌入维度。
        完。
        若 查询草图 不等于 0 并且 i 小于 做 计数 用 语义索引草图 并且 语义索引草图 的 第 i 项 不等于 0：
            基础分 为 基础分 加 做 语义嵌入_草图相似度 用 查询草图、语义索引草图 的 第 i 项 除以 4。
        完。
        若 做 字符串_包含 用 语义索引 的 第 i 项 的 概念、查询：
            基础分 为 基础分 加 320。
        否则若 做 字符串_包含 用 查询、语义索引 的 第 i 项 的 概念：
            基础分 为 基础分 加 192。
        完。
        新访问 为 语义索引 的 第 i 项 的 访问次数 加 1。
        分数 为 做 语义索引_综合评分 用 基础分、新访问、做 计数 用 语义索引 的 第 i 项 的 关联记忆、语义索引 的 第 i 项 的 最近匹配时间、当前时间。
        质量 为 0。
        若 i 小于 做 计数 用 语义索引质量：
            质量 为 语义索引质量 的 第 i 项。
        完。
        质量加权分 为 分数 乘 质量 除以 1000。
        若 质量加权分 大于 256：
            语义索引 的 第 i 项 的 访问次数 为 新访问。
            语义索引 的 第 i 项 的 最近匹配时间 为 当前时间。
            做 追加 用 结果、一本字典：概念 对应 语义索引 的 第 i 项 的 概念、关联 对应 语义索引 的 第 i 项 的 关联记忆、分数 对应 质量加权分。
        完。
        i 为 i 加 1。
    完。
    结果 为 做 列表_排序 用 结果、以 a、b 为 引，其做法是：返还 b 的 分数 减 a 的 分数。
    若 做 计数 用 结果 大于 最大数：
        结果 为 做 列表_截取 用 结果、0、最大数。
    完。
    若 查询向量 不等于 0：
        做 Tensor_释放 用 查询向量。
    完。
    若 查询草图 不等于 0：
        做 Tensor_释放 用 查询草图。
    完。
    返还 结果。
完。

智能体记忆_组装上下文 接受 查询、最大令牌：
    AMMS全局统计 的 组装次数 为 AMMS全局统计 的 组装次数 加 1。
    可变 上下文 为 「」。
    可变 已用令牌 为 0。
    令牌极限 为 最大令牌 乘 3。
    最近 为 做 智能体记忆_最近 用 组装_最大片段 除以 2。
    可变 i 为 0。
    只要 i 小于 做 计数 用 最近：
        片段 为 最近 的 第 i 项 的 内容。
        片段长 为 做 字符串长度 用 片段。
        若 已用令牌 加 片段长 小于 令牌极限：
            上下文 为 上下文 加 「[STM] 」 加 片段 加 「\n」。
            已用令牌 为 已用令牌 加 片段长。
        完。
        i 为 i 加 1。
    完。
    工作上下文 为 做 工作记忆_语义上下文 用 查询、3。
    若 工作上下文 不等于 0 并且 工作上下文 不等于 「」：
        工作上下文长 为 做 字符串长度 用 工作上下文。
        若 已用令牌 加 工作上下文长 小于 令牌极限：
            上下文 为 上下文 加 工作上下文。
            已用令牌 为 已用令牌 加 工作上下文长。
        完。
    完。
    情景 为 做 情景记忆_搜索 用 查询、5。
    i 为 0。
    只要 i 小于 做 计数 用 情景：
        em 为 情景 的 第 i 项 的 条目。
        片 为 「[EM] 」 加 em 的 事件 加 「 | 」 加 em 的 上下文。
        若 已用令牌 加 做 字符串长度 用 片 小于 令牌极限：
            上下文 为 上下文 加 片 加 「\n」。
            已用令牌 为 已用令牌 加 做 字符串长度 用 片。
        完。
        i 为 i 加 1。
    完。
    相关 为 做 智能体记忆_检索 用 查询。
    若 相关 不等于 空 并且 已用令牌 加 做 字符串长度 用 做 转字符串 用 相关 小于 令牌极限：
        上下文 为 上下文 加 「[LTM] 」 加 做 转字符串 用 相关 加 「\n」。
    完。
    语义 为 做 语义索引_搜索 用 查询、3。
    i 为 0。
    只要 i 小于 做 计数 用 语义：
        片 为 「[SEM] 」 加 语义 的 第 i 项 的 概念。
        若 已用令牌 加 做 字符串长度 用 片 小于 令牌极限：
            上下文 为 上下文 加 片 加 「\n」。
        完。
        i 为 i 加 1。
    完。
    返还 上下文。
完。

智能体记忆_巩固任务 接受 任务描述、结果：
    AMMS全局统计 的 巩固次数 为 AMMS全局统计 的 巩固次数 加 1。
    做 智能体记忆_保存长期_带重要度 用 任务描述、结果、900。
    做 情景记忆_记录 用 「任务完成: 」 加 任务描述、结果、900、「positive」。
    做 智能体记忆_添加短期 用 「任务完成: 」 加 任务描述、900。
    做 语义索引_添加 用 任务描述、任务描述。
    做 记忆_巩固周期。
完。

巩固引擎_执行周期 接受 零参：
    now 为 做 读取毫秒。
    可变 i 为 0。
    只要 i 小于 做 计数 用 情景记忆库：
        m 为 情景记忆库 的 第 i 项。
        若 now 大于或等于 m 的 下次巩固时间 并且 m 的 重要度 大于 300：
            做 智能体记忆_保存长期 用 m 的 事件、m 的 上下文。
            m 的 巩固次数 为 m 的 巩固次数 加 1。
            EF_x10 为 13 加 (m 的 重要度 减 300) 乘 15 除以 600。
            若 EF_x10 小于 13：
                EF_x10 为 13。
            完。
            若 EF_x10 大于 28：
                EF_x10 为 28。
            完。
            下次间隔 为 0。
            若 m 的 巩固次数 等于 1：
                下次间隔 为 巩固_基准间隔。
            否则若 m 的 巩固次数 等于 2：
                下次间隔 为 巩固_基准间隔 乘 6。
            否则：
                前一间隔 为 now 减 m 的 下次巩固时间 加 巩固_基准间隔。
                下次间隔 为 前一间隔 乘 EF_x10 除以 10。
            完。
            若 下次间隔 小于 巩固_基准间隔：
                下次间隔 为 巩固_基准间隔。
            完。
            m 的 下次巩固时间 为 now 加 下次间隔。
            情景记忆库 的 第 i 项 为 m。
        完。
        i 为 i 加 1。
    完。
完。

AI_AMMS记忆相关性推理 接受 查询、记忆内容：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 500。
    完。
    feature 为 做 Tensor_创建 用 8。
    做 Tensor_写 用 feature、0、做 字符串哈希 用 查询 除以 4294967296.0。
    做 Tensor_写 用 feature、1、做 字符串哈希 用 记忆内容 除以 4294967296.0。
    做 Tensor_写 用 feature、2、做 字符串长度 用 查询 除以 1000.0。
    做 Tensor_写 用 feature、3、做 字符串长度 用 记忆内容 除以 1000.0。
    做 Tensor_写 用 feature、4、做 计数 用 短期记忆 除以 STM_最大容量。
    做 Tensor_写 用 feature、5、做 计数 用 情景记忆库 除以 EM_最大容量。
    做 Tensor_写 用 feature、6、AMMS全局统计 的 组装次数 除以 100.0。
    做 Tensor_写 用 feature、7、0.0。
    结果 为 做 AI总线_推理 用 bus、「AMMS_记忆相关性」、feature。
    做 Tensor_释放 用 feature。
    AMMS全局统计 的 AI预测次数 为 AMMS全局统计 的 AI预测次数 加 1。
    若 结果 不等于 0：
        返还 做 Tensor_读 用 结果、0 乘 1000。
    完。
    返还 500。
完。

智能体记忆_统计 接受 零参：
    返还 一本字典：短期 对应 做 计数 用 短期记忆、情景 对应 做 计数 用 情景记忆库、长期 对应 做 计数 用 长期记忆、语义 对应 做 计数 用 语义索引、语义质量 对应 做 计数 用 语义索引质量、STM添加 对应 AMMS全局统计 的 STM添加数、STM淘汰 对应 AMMS全局统计 的 STM淘汰数、LTM写入 对应 AMMS全局统计 的 LTM写入数、LTM未命中 对应 AMMS全局统计 的 LTM未命中数、巩固 对应 AMMS全局统计 的 巩固次数、AI预测 对应 AMMS全局统计 的 AI预测次数。
完。

智能体记忆_打印统计 接受 零参：
    s 为 做 智能体记忆_统计。
    做 内核打印 用 「═══ AMMS智能体记忆统计 ═══」。
    做 内核打印 用 「STM={}/{} EM={}/{} LTM={} SEM={}」、s 的 短期、STM_最大容量、s 的 情景、EM_最大容量、s 的 长期、s 的 语义。
    做 内核打印 用 「STM[添加/淘汰]={}/{} LTM[写/未命中]={}/{}」、s 的 STM添加、s 的 STM淘汰、s 的 LTM写入、s 的 LTM未命中。
    做 内核打印 用 「巩固={} AI预测={}」、s 的 巩固、s 的 AI预测。
完。

智能体记忆_重置 接受 零参：
    做 工作记忆记_全局重置。
    短期记忆 为 一组：（空）。
    情景记忆库 为 一组：（空）。
    长期记忆 为 做 字典。
    做 语义索引_释放。
    智能体记忆_时钟 为 0。
    AMMS全局统计 为 新建 AMMS统计。
完。

智能体记忆_初始化 接受 零参：
    做 智能体记忆_重置。
    做 内核打印 用 「AMMS智能体记忆系统已初始化」。
完。

智能体记忆_全量导出 接受 零参：
    返还 一本字典：短期 对应 短期记忆、情景 对应 情景记忆库、语义 对应 语义索引、语义草图 对应 语义索引草图、语义质量 对应 语义索引质量、时钟 对应 智能体记忆_时钟、统计 对应 AMMS全局统计。
完。

本章 收束。
