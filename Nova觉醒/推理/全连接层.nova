《全连接层》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·定点Q算术》。

引用《..·感知·内核直连层》。

引用《.·激活函数非线》。

引用《.·权重持久》。

引用《.·矩阵运算》。

激活类型_无 为 0。

激活类型_ReLU 为 1。

激活类型_Nova激活函数 为 2。

激活类型_Tanh 为 3。

激活类型_Nova归一化指数 为 4。

激活类型_GELU 为 5。

可变 全连接层_量化次数 为 0。

可变 全连接层_量化累计误差 为 0。

可变 全连接层_量化最大误差 为 0。

DenseLayer 是 一种 事物，含 权重、偏置、激活、输入维度、输出维度。

DenseLayer_创建 接受 输入维度、输出维度、激活类型：
    若 输入维度 小于或等于 0 或者 输出维度 小于或等于 0：
        返还 0。
    完。
    层 为 做 堆_分配 用 DenseLayer。
    若 层 等于 0：
        返还 0。
    完。
    层 的 输入维度 为 输入维度。
    层 的 输出维度 为 输出维度。
    层 的 激活 为 激活类型。
    层 的 权重 为 做 Tensor_创建 用 输入维度、输出维度。
    层 的 偏置 为 做 Tensor_创建 用 1、输出维度。
    若 层 的 权重 等于 0 或者 层 的 偏置 等于 0：
        若 层 的 权重 不等于 0：
            做 Tensor_释放 用 层 的 权重。
        完。
        若 层 的 偏置 不等于 0：
            做 Tensor_释放 用 层 的 偏置。
        完。
        做 堆_释放 用 层。
        返还 0。
    完。
    范围 为 做 Q开方 用 做 Q除 用 6 乘 Q_ONE、(输入维度 加 输出维度) 乘 Q_ONE。
    可变 i 为 0。
    只要 i 小于 输入维度 乘 输出维度：
        种子 为 i 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        随机值 为 种子 取余于 (2 乘 范围 加 1) 减 范围。
        做 内存_写32 用 层 的 权重 的 数据 加 i 乘 4、随机值。
        i 为 i 加 1。
    完。
    返还 层。
完。

DenseLayer_销毁 接受 层：
    若 层 等于 0：
        返还。
    完。
    若 层 的 权重 不等于 0：
        做 Tensor_释放 用 层 的 权重。
    完。
    若 层 的 偏置 不等于 0：
        做 Tensor_释放 用 层 的 偏置。
    完。
    层 的 权重 为 0。
    层 的 偏置 为 0。
    层 的 输入维度 为 0。
    层 的 输出维度 为 0。
    做 堆_释放 用 层。
完。

DenseLayer_前向 接受 层、输入：
    若 层 等于 0 或者 输入 等于 0：
        返还 0。
    完。
    线性输出 为 做 Tensor_矩阵乘 用 输入、层 的 权重。
    若 线性输出 等于 0：
        返还 0。
    完。
    做 Tensor_加偏置 用 线性输出、层 的 偏置。
    若 层 的 激活 等于 激活类型_ReLU：
        做 激活_修正线性 用 线性输出。
    否则若 层 的 激活 等于 激活类型_Nova激活函数：
        做 激活_Nova激活函数 用 线性输出。
    否则若 层 的 激活 等于 激活类型_Tanh：
        做 激活_Tanh 用 线性输出。
    否则若 层 的 激活 等于 激活类型_Nova归一化指数：
        做 激活_Nova归一化指数 用 线性输出。
    否则若 层 的 激活 等于 激活类型_GELU：
        做 激活_GELU 用 线性输出。
    完。
    返还 线性输出。
完。

Nova多层感知器 是 一种 事物，含 层列表、层数量、层容量。

Nova多层感知器_创建 接受 容量：
    若 容量 小于或等于 0：
        返还 0。
    完。
    net 为 做 堆_分配 用 Nova多层感知器。
    若 net 等于 0：
        返还 0。
    完。
    net 的 层容量 为 容量。
    net 的 层数量 为 0。
    net 的 层列表 为 做 堆_分配_数组 用 容量 乘 8。
    若 net 的 层列表 等于 0：
        做 堆_释放 用 net。
        返还 0。
    完。
    返还 net。
完。

Nova多层感知器_添加层 接受 net、输入维度、输出维度、激活：
    若 net 等于 0 或者 net 的 层数量 大于或等于 net 的 层容量：
        返还。
    完。
    层 为 做 DenseLayer_创建 用 输入维度、输出维度、激活。
    若 层 等于 0：
        返还。
    完。
    做 内存_写64 用 net 的 层列表 加 net 的 层数量 乘 8、层。
    net 的 层数量 为 net 的 层数量 加 1。
完。

Nova多层感知器_前向 接受 net、输入：
    若 net 等于 0 或者 输入 等于 0：
        返还 0。
    完。
    可变 当前 为 输入。
    可变 i 为 0。
    只要 i 小于 net 的 层数量：
        层 为 做 内存_读64 用 net 的 层列表 加 i 乘 8。
        若 层 等于 0：
            返还 0。
        完。
        输出 为 做 DenseLayer_前向 用 层、当前。
        若 i 大于 0：
            做 Tensor_释放 用 当前。
        完。
        当前 为 输出。
        i 为 i 加 1。
    完。
    返还 当前。
完。

Nova多层感知器_销毁 接受 net：
    若 net 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 net 的 层数量：
        层 为 做 内存_读64 用 net 的 层列表 加 i 乘 8。
        若 层 不等于 0：
            做 DenseLayer_销毁 用 层。
        完。
        i 为 i 加 1。
    完。
    若 net 的 层列表 不等于 0：
        做 堆_释放 用 net 的 层列表。
    完。
    net 的 层列表 为 0。
    net 的 层数量 为 0。
    net 的 层容量 为 0。
    做 堆_释放 用 net。
完。

QuantizedLayer 是 一种 事物，含 权重_i8、偏置_i32、缩放因子、零点、输入维度、输出维度。

七态向量量化_内积误差估计 接受 a_模长、b_模长、维度：
    误差_Q10 为 Q_ONE 乘 10 除以 做 Q开方 用 维度 乘 Q_ONE。
    返还 误差_Q10。
完。

PRUNE_通用安全阈值 为 500。

PRUNE_中文安全阈值 为 600。

PRUNE_最大阶段数 为 5。

DenseLayer_渐进剪枝_阶段 接受 层、目标稀疏度_千分、当前阶段、总阶段数：
    若 层 等于 0 或者 层 的 权重 等于 0：
        返还。
    完。
    阶段稀疏度 为 目标稀疏度_千分 乘 当前阶段 除以 总阶段数。
    N 为 层 的 输入维度 乘 层 的 输出维度。
    可变 阈值 为 0。
    可变 排序值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        做 追加 用 排序值、做 绝对值 用 做 Tensor_读1D 用 层 的 权重、i。
        i 为 i 加 1。
    完。
    排序值 为 做 快速排序 用 排序值。
    阈值idx 为 N 乘 阶段稀疏度 除以 1000。
    若 阈值idx 大于或等于 N：
        阈值idx 为 N 减 1。
    完。
    阈值 为 排序值 的 第 阈值idx 项。
    i 为 0。
    只要 i 小于 N：
        若 做 绝对值 用 做 Tensor_读1D 用 层 的 权重、i 小于 阈值：
            做 Tensor_写1D 用 层 的 权重、i、0。
        完。
        i 为 i 加 1。
    完。
完。

DenseLayer_当前稀疏度 接受 层：
    若 层 等于 0 或者 层 的 权重 等于 0：
        返还 0。
    完。
    N 为 层 的 输入维度 乘 层 的 输出维度。
    可变 零数 为 0。
    可变 i 为 0。
    只要 i 小于 N：
        若 做 Tensor_读1D 用 层 的 权重、i 等于 0：
            零数 为 零数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 零数 乘 1000 除以 N。
完。

QuantizedLayer_前向 接受 ql、输入：
    若 ql 等于 0 或者 输入 等于 0 或者 ql 的 输入维度 小于或等于 0 或者 ql 的 输出维度 小于或等于 0：
        返还 0。
    完。
    若 ql 的 权重_i8 等于 0 或者 ql 的 偏置_i32 等于 0：
        返还 0。
    完。
    batch 为 输入 的 行数。
    out 为 做 Tensor_创建 用 batch、ql 的 输出维度。
    若 out 等于 0：
        返还 0。
    完。
    可变 b 为 0。
    只要 b 小于 batch：
        可变 j 为 0。
        只要 j 小于 ql 的 输出维度：
            可变 累加 为 做 内存_读32有符号 用 ql 的 偏置_i32 加 j 乘 4。
            可变 k 为 0。
            只要 k 小于 ql 的 输入维度：
                x 为 做 Tensor_读 用 输入、b、k。
                w 为 做 内存_读8 用 ql 的 权重_i8 加 k 乘 ql 的 输出维度 加 j。
                真实w 为 做 Q乘 用 w、ql 的 缩放因子 加 ql 的 零点。
                累加 为 累加 加 做 Q乘 用 x、真实w。
                k 为 k 加 1。
            完。
            做 Tensor_写 用 out、b、j、累加。
            j 为 j 加 1。
        完。
        b 为 b 加 1。
    完。
    返还 out。
完。

本章 收束。
