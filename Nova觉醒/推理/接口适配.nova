《接口适配》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·自动扩容》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

引用《..·标准·原生列表》。

引用《.·渠道接口》。

引用《..·中枢·墙钟设置》。

接口_版本 为 「v1」。

接口_最大请求体 为 65536。

接口_速率窗口秒 为 60。

接口_默认速率限制 为 120。

HTTP_200 为 200。

HTTP_400 为 400。

HTTP_401 为 401。

HTTP_403 为 403。

HTTP_429 为 429。

HTTP_500 为 500。

接口配置 是 一种 事物，含 密钥、允许跨域、允许来源、速率限制、需要认证、版本。

接口速率 是 一种 事物，含 请求计数表、窗口开始。

可变 接口_消息队列 为 一组：（空）。

可变 接口_响应队列 为 一组：（空）。

可变 接口_配置 为 空。

可变 接口_请求计数 为 0。

可变 接口_错误计数 为 0。

可变 接口_拒绝计数 为 0。

可变 接口_速率 为 空。

可变 接口_webhook列表 为 一组：（空）。

接口适配_创建 接受 密钥：
    接口_配置 为 新建 接口配置：密钥 为 密钥、允许跨域 为 真、允许来源 为 「*」、速率限制 为 接口_默认速率限制、需要认证 为 密钥 不等于 「」、版本 为 接口_版本。
    接口_速率 为 新建 接口速率：请求计数表 为 做 字典、窗口开始 为 做 当前时间_秒。
    返还 做 渠道_创建 用 「接口」、渠道类型_接口、接口_接收、接口_发送。
完。

接口_接收 接受 零参：
    若 做 计数 用 接口_消息队列 大于 0：
        消息 为 接口_消息队列 的 第 0 项。
        接口_消息队列 为 做 列表_切片 用 接口_消息队列、1、做 计数 用 接口_消息队列。
        返还 消息。
    完。
    返还 空。
完。

接口_发送 接受 请求ID、回复：
    做 追加 用 接口_响应队列、一组：请求ID、回复、做 当前时间_毫秒。
    做 接口_触发webhook 用 「回复」、请求ID、回复。
    返还 成功。
完。

接口_获取响应 接受 请求ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 接口_响应队列：
        若 接口_响应队列 的 第 i 项 的 第 0 项 等于 请求ID：
            响应 为 接口_响应队列 的 第 i 项。
            接口_响应队列 为 做 列表_拼接 用 做 列表_切片 用 接口_响应队列、0、i、做 列表_切片 用 接口_响应队列、i 加 1、做 计数 用 接口_响应队列。
            返还 响应 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

接口_HTTP处理 接受 请求体：
    返还 做 接口_HTTP处理带头 用 请求体、「」、「」。
完。

接口_HTTP处理带头 接受 请求体、来源IP、请求头：
    接口_请求计数 为 接口_请求计数 加 1。
    若 做 字符串长度 用 请求体 大于 接口_最大请求体：
        接口_错误计数 为 接口_错误计数 加 1。
        返还 做 接口_错误响应 用 HTTP_400、「请求体过大」。
    完。
    若 来源IP 不等于 「」 并且 非 做 接口_检查速率 用 来源IP：
        接口_拒绝计数 为 接口_拒绝计数 加 1。
        返还 做 接口_错误响应 用 HTTP_429、「请求过于频繁」。
    完。
    消息 为 做 接口_提取字段 用 请求体、「message」。
    用户 为 做 接口_提取字段 用 请求体、「user_id」。
    密钥 为 做 接口_提取字段 用 请求体、「api_key」。
    若 接口_配置 的 需要认证：
        若 密钥 等于 「」：
            密钥 为 做 接口_从头提取Bearer 用 请求头。
        完。
        若 密钥 不等于 接口_配置 的 密钥：
            接口_拒绝计数 为 接口_拒绝计数 加 1。
            返还 做 接口_错误响应 用 HTTP_401、「未授权」。
        完。
    完。
    若 消息 等于 「」：
        接口_错误计数 为 接口_错误计数 加 1。
        返还 做 接口_错误响应 用 HTTP_400、「消息不能为空」。
    完。
    若 用户 等于 「」：
        用户 为 「api_user」。
    完。
    请求ID 为 做 接口_生成请求ID。
    做 追加 用 接口_消息队列、一组：用户、消息、请求ID。
    做 接口_触发webhook 用 「接收」、请求ID、消息。
    返还 做 接口_成功响应 用 请求ID。
完。

接口_检查速率 接受 来源IP：
    若 接口_速率 等于 空：
        返还 真。
    完。
    现在 为 做 当前时间_秒。
    若 现在 减 接口_速率 的 窗口开始 大于 接口_速率窗口秒：
        接口_速率 的 请求计数表 为 做 字典。
        接口_速率 的 窗口开始 为 现在。
    完。
    当前计数 为 做 字典取 用 接口_速率 的 请求计数表、来源IP。
    若 当前计数 等于 空：
        当前计数 为 0。
    完。
    若 当前计数 大于或等于 接口_配置 的 速率限制：
        返还 假。
    完。
    做 字典设 用 接口_速率 的 请求计数表、来源IP、当前计数 加 1。
    返还 真。
完。

接口_从头提取Bearer 接受 请求头：
    位置 为 做 字符串查找 用 请求头、「Bearer 」。
    若 位置 小于 0：
        返还 「」。
    完。
    返还 做 字符串子串 用 请求头、位置 加 7、做 字符串长度 用 请求头。
完。

接口_生成请求ID 接受 零参：
    返还 做 字符串拼接 用 「req_」、做 转字符串 用 接口_请求计数。
完。

接口_提取字段 接受 json、字段名：
    查找 为 做 字符串拼接 用 「\"」、做 字符串拼接 用 字段名、「\":\"」。
    位置 为 做 字符串查找 用 json、查找。
    若 位置 小于 0：
        返还 「」。
    完。
    起始 为 位置 加 做 字符串长度 用 查找。
    结束 为 做 字符串查找 用 做 字符串子串 用 json、起始、做 字符串长度 用 json、「\"」。
    若 结束 小于 0：
        返还 「」。
    完。
    返还 做 字符串子串 用 json、起始、起始 加 结束。
完。

接口_成功响应 接受 请求ID：
    返还 「{\"status\":\"accepted\",\"request_id\":\"」 加 请求ID 加 「\",\"version\":\"」 加 接口_版本 加 「\"}」。
完。

接口_错误响应 接受 状态码、消息：
    返还 「{\"error\":\"」 加 消息 加 「\",\"code\":」 加 做 转字符串 用 状态码 加 「}」。
完。

接口_CORS头 接受 零参：
    若 非 接口_配置 的 允许跨域：
        返还 「」。
    完。
    返还 「Access-Control-Allow-Origin: 」 加 接口_配置 的 允许来源。
完。

接口_注册webhook 接受 URL、事件类型：
    做 追加 用 接口_webhook列表、一组：URL、事件类型。
完。

接口_触发webhook 接受 事件、请求ID、数据：
    可变 i 为 0。
    只要 i 小于 做 计数 用 接口_webhook列表：
        wh 为 接口_webhook列表 的 第 i 项。
        若 wh 的 第 1 项 等于 事件 或者 wh 的 第 1 项 等于 「全部」：
        完。
        i 为 i 加 1。
    完。
完。

接口适配_初始化 接受 零参：
    返还 成功。
完。

接口适配_统计 接受 零参：
    返还 一组：接口_请求计数、接口_错误计数、接口_拒绝计数、做 计数 用 接口_消息队列、做 计数 用 接口_响应队列。
完。

接口适配_设速率 接受 限制：
    若 接口_配置 不等于 空：
        接口_配置 的 速率限制 为 限制。
    完。
完。

接口适配_设跨域 接受 允许、来源：
    若 接口_配置 不等于 空：
        接口_配置 的 允许跨域 为 允许。
        接口_配置 的 允许来源 为 来源。
    完。
完。

接口适配_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 接口_错误计数 大于 100：
        做 追加 用 问题、「接口_错误计数=」 加 接口_错误计数。
    完。
    返还 做 计数 用 问题 等于 0。
完。

接口适配_综合报告 接受 零参：
    做 打印 用 「[接口适配] ═══ 综合报告 ═══」。
    做 打印 用 「  接口_请求计数: 」 加 接口_请求计数。
    做 打印 用 「  接口_错误计数: 」 加 接口_错误计数。
    做 打印 用 「  接口_拒绝计数: 」 加 接口_拒绝计数。
    做 打印 用 「  接口_速率: 」 加 接口_速率。
    做 打印 用 「  健康: 」 加 若 做 接口适配_健康评估 则 「正常」 否则 「异常」。
完。

接口适配_全局重置 接受 零参：
    接口_请求计数 为 0。
    接口_错误计数 为 0。
    接口_拒绝计数 为 0。
    接口_速率 为 0。
    做 打印 用 「[接口适配] 统计已重置」。
    返还 0。
完。

本章 收束。
