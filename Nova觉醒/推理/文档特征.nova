《文档特征》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《.·定点Q算术》。

引用《..·执行·控制台输出》。

引用《..·感知·原子驻留》。

提取_类名特征 接受 节点：
    特征 为 做 Tensor_创建 用 1、8。
    若 节点 等于 0：
        返还 特征。
    完。
    类名 为 做 节点_获取class 用 节点。
    若 类名 不等于 0：
        哈希 为 做 字符串_哈希 用 类名。
        可变 i 为 0。
        只要 i 小于 8：
            做 Tensor_写 用 特征、0、i、(哈希 右移 i 乘 4 位与 0xF) 乘 64。
            i 为 i 加 1。
        完。
    完。
    返还 特征。
完。

提取_结构特征 接受 节点：
    特征 为 做 Tensor_创建 用 1、8。
    若 节点 等于 0：
        返还 特征。
    完。
    深度 为 做 节点_获取深度 用 节点。
    做 Tensor_写 用 特征、0、0、若 深度 大于 20 则 Q_ONE 否则 深度 乘 Q_ONE 除以 20。
    子数 为 做 节点_获取子节点数 用 节点。
    做 Tensor_写 用 特征、0、1、若 子数 大于 50 则 Q_ONE 否则 子数 乘 Q_ONE 除以 50。
    文本长 为 做 节点_获取文本长度 用 节点。
    做 Tensor_写 用 特征、0、2、若 文本长 大于 1000 则 Q_ONE 否则 文本长 乘 Q_ONE 除以 1000。
    做 Tensor_写 用 特征、0、3、若 做 节点_有ID 用 节点 则 Q_ONE 否则 0。
    做 Tensor_写 用 特征、0、4、Q_HALF。
    做 Tensor_写 用 特征、0、5、若 做 节点_包含链接 用 节点 则 Q_ONE 否则 0。
    做 Tensor_写 用 特征、0、6、若 做 节点_包含图片 用 节点 则 Q_ONE 否则 0。
    做 Tensor_写 用 特征、0、7、若 做 节点_包含表单 用 节点 则 Q_ONE 否则 0。
    返还 特征。
完。

标签_DIV 为 1。

标签_P 为 2。

标签_SPAN 为 3。

标签_A 为 4。

标签_IMG 为 5。

标签_INPUT 为 6。

标签_BUTTON 为 7。

标签_H1 为 8。

标签_H2 为 9。

标签_UL 为 10。

标签_LI 为 11。

标签_TABLE 为 12。

标签_FORM 为 13。

标签_NAV 为 14。

标签_ARTICLE 为 15。

标签_SECTION 为 16。

标签_HEADER 为 17。

标签_FOOTER 为 18。

标签_VIDEO 为 19。

标签_CANVAS 为 20。

特征_结构维数 为 8。

特征_类名维数 为 8。

特征_样式维数 为 8。

特征_交互维数 为 8。

特征_总维数 为 32。

特征缓存_容量 为 256。

可变 特征缓存_键 为 空。

可变 特征缓存_版本 为 空。

可变 特征缓存_值 为 空。

可变 特征缓存_已初始化 为 假。

可变 特征_提取总次数 为 0。

可变 特征_缓存命中 为 0。

可变 特征_缓存未中 为 0。

可变 特征_AI分类次数 为 0。

特征缓存_初始化 接受 零参：
    若 特征缓存_已初始化：
        返还。
    完。
    特征缓存_键 为 做 数组_创建 用 特征缓存_容量、0。
    特征缓存_版本 为 做 数组_创建 用 特征缓存_容量、0。
    特征缓存_值 为 做 数组_创建 用 特征缓存_容量、空。
    特征缓存_已初始化 为 真。
完。

特征缓存_查找 接受 节点：
    若 非 特征缓存_已初始化：
        返还 空。
    完。
    slot 为 (节点 右移 4) 取余于 特征缓存_容量。
    若 特征缓存_键 的 第 slot 项 等于 节点 并且 特征缓存_版本 的 第 slot 项 等于 做 节点_获取版本 用 节点：
        特征_缓存命中 为 特征_缓存命中 加 1。
        返还 特征缓存_值 的 第 slot 项。
    完。
    特征_缓存未中 为 特征_缓存未中 加 1。
    返还 空。
完。

特征缓存_写入 接受 节点、tensor：
    若 非 特征缓存_已初始化：
        做 特征缓存_初始化。
    完。
    slot 为 (节点 右移 4) 取余于 特征缓存_容量。
    特征缓存_键 的 第 slot 项 为 节点。
    特征缓存_版本 的 第 slot 项 为 做 节点_获取版本 用 节点。
    特征缓存_值 的 第 slot 项 为 tensor。
完。

特征缓存_清空 接受 零参：
    可变。
    i 为 0。
    只要 i 小于 特征缓存_容量：
        若 特征缓存_值 的 第 i 项 不等于 空：
            做 Tensor_释放 用 特征缓存_值 的 第 i 项。
            特征缓存_值 的 第 i 项 为 空。
        完。
        特征缓存_键 的 第 i 项 为 0。
        特征缓存_版本 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
完。

提取_样式特征 接受 节点：
    特征 为 做 Tensor_创建 用 1、特征_样式维数。
    若 节点 等于 0：
        返还 特征。
    完。
    disp 为 做 节点_获取样式 用 节点、「display」。
    若 disp 等于 「none」：
        做 Tensor_写 用 特征、0、0、0。
    否则若 disp 等于 「inline」：
        做 Tensor_写 用 特征、0、0、Q_ONE 除以 4。
    否则若 disp 等于 「block」：
        做 Tensor_写 用 特征、0、0、Q_HALF。
    否则若 disp 等于 「flex」：
        做 Tensor_写 用 特征、0、0、Q_ONE 乘 3 除以 4。
    否则：
        做 Tensor_写 用 特征、0、0、Q_ONE。
    完。
    pos 为 做 节点_获取样式 用 节点、「position」。
    若 pos 等于 「static」：
        做 Tensor_写 用 特征、0、1、0。
    否则若 pos 等于 「relative」：
        做 Tensor_写 用 特征、0、1、Q_ONE 除以 3。
    否则若 pos 等于 「absolute」：
        做 Tensor_写 用 特征、0、1、Q_ONE 乘 2 除以 3。
    否则：
        做 Tensor_写 用 特征、0、1、Q_ONE。
    完。
    透明度 为 做 节点_获取样式数值 用 节点、「opacity」。
    做 Tensor_写 用 特征、0、2、做 Q乘 用 透明度、Q_ONE。
    字号 为 做 节点_获取样式数值 用 节点、「font-size」。
    若 字号 大于 48：
        字号 为 48。
    完。
    做 Tensor_写 用 特征、0、3、字号 乘 Q_ONE 除以 48。
    bgL 为 做 节点_获取背景亮度 用 节点。
    做 Tensor_写 用 特征、0、4、bgL。
    tf 为 做 节点_获取样式 用 节点、「transform」。
    做 Tensor_写 用 特征、0、5、若 tf 不等于 「none」 并且 tf 不等于 「」 则 Q_ONE 否则 0。
    ov 为 做 节点_获取样式 用 节点、「overflow」。
    若 ov 等于 「hidden」 或者 ov 等于 「scroll」 或者 ov 等于 「auto」：
        做 Tensor_写 用 特征、0、6、Q_ONE。
    否则：
        做 Tensor_写 用 特征、0、6、0。
    完。
    zi 为 做 节点_获取样式数值 用 节点、「z-index」。
    若 zi 大于 100：
        zi 为 100。
    完。
    做 Tensor_写 用 特征、0、7、zi 乘 Q_ONE 除以 100。
    返还 特征。
完。

提取_交互特征 接受 节点：
    特征 为 做 Tensor_创建 用 1、特征_交互维数。
    若 节点 等于 0：
        返还 特征。
    完。
    tag 为 做 节点_获取标签ID 用 节点。
    可变。
    可点击 为 假。
    若 tag 等于 标签_A 或者 tag 等于 标签_BUTTON 或者 tag 等于 标签_INPUT：
        可点击 为 真。
    完。
    若 做 节点_有事件监听 用 节点、「click」：
        可点击 为 真。
    完。
    做 Tensor_写 用 特征、0、0、若 可点击 则 Q_ONE 否则 0。
    可变。
    可输入 为 tag 等于 标签_INPUT 或者 做 节点_是可编辑 用 节点。
    做 Tensor_写 用 特征、0、1、若 可输入 则 Q_ONE 否则 0。
    ov 为 做 节点_获取样式 用 节点、「overflow」。
    做 Tensor_写 用 特征、0、2、若 ov 等于 「scroll」 或者 ov 等于 「auto」 则 Q_ONE 否则 0。
    evtCount 为 做 节点_获取事件数 用 节点。
    若 evtCount 大于 10：
        evtCount 为 10。
    完。
    做 Tensor_写 用 特征、0、3、evtCount 乘 Q_ONE 除以 10。
    tabIdx 为 做 节点_获取属性 用 节点、「tabindex」。
    做 Tensor_写 用 特征、0、4、若 tabIdx 不等于 空 则 Q_ONE 否则 0。
    role 为 做 节点_获取属性 用 节点、「role」。
    做 Tensor_写 用 特征、0、5、若 role 不等于 空 则 Q_ONE 否则 0。
    draggable 为 做 节点_获取属性 用 节点、「draggable」。
    做 Tensor_写 用 特征、0、6、若 draggable 等于 「真」 则 Q_ONE 否则 0。
    在视口 为 做 节点_是在视口内 用 节点。
    做 Tensor_写 用 特征、0、7、若 在视口 则 Q_ONE 否则 0。
    返还 特征。
完。

提取_合并特征 接受 节点：
    特征_提取总次数 为 特征_提取总次数 加 1。
    cached 为 做 特征缓存_查找 用 节点。
    若 cached 不等于 空：
        返还 cached。
    完。
    合并 为 做 Tensor_创建 用 1、特征_总维数。
    结构 为 做 提取_结构特征 用 节点。
    可变。
    i 为 0。
    只要 i 小于 特征_结构维数：
        做 Tensor_写 用 合并、0、i、做 Tensor_读 用 结构、0、i。
        i 为 i 加 1。
    完。
    做 Tensor_释放 用 结构。
    类名 为 做 提取_类名特征 用 节点。
    i 为 0。
    只要 i 小于 特征_类名维数：
        做 Tensor_写 用 合并、0、特征_结构维数 加 i、做 Tensor_读 用 类名、0、i。
        i 为 i 加 1。
    完。
    做 Tensor_释放 用 类名。
    样式 为 做 提取_样式特征 用 节点。
    i 为 0。
    只要 i 小于 特征_样式维数：
        做 Tensor_写 用 合并、0、特征_结构维数 加 特征_类名维数 加 i、做 Tensor_读 用 样式、0、i。
        i 为 i 加 1。
    完。
    做 Tensor_释放 用 样式。
    交互 为 做 提取_交互特征 用 节点。
    i 为 0。
    只要 i 小于 特征_交互维数：
        做 Tensor_写 用 合并、0、特征_结构维数 加 特征_类名维数 加 特征_样式维数 加 i、做 Tensor_读 用 交互、0、i。
        i 为 i 加 1。
    完。
    做 Tensor_释放 用 交互。
    做 特征缓存_写入 用 节点、合并。
    返还 合并。
完。

AI_语义丰富度 接受 节点：
    特征_AI分类次数 为 特征_AI分类次数 加 1。
    特征 为 做 提取_合并特征 用 节点。
    结果 为 做 AI总线_推理 用 「semantic_richness」、特征。
    可变。
    分数 为 Q_HALF。
    若 结果 不等于 空：
        分数 为 做 Tensor_读 用 结果、0、0。
        做 Tensor_释放 用 结果。
    完。
    返还 分数。
完。

AI_节点重要度排序 接受 节点列表：
    可变。
    i 为 1。
    只要 i 小于 做 数组_长度 用 节点列表：
        key 为 节点列表 的 第 i 项。
        keyScore 为 做 AI_语义丰富度 用 key。
        可变。
        j 为 i 减 1。
        只要 j 大于或等于 0 并且 做 AI_语义丰富度 用 节点列表 的 第 j 项 小于 keyScore：
            节点列表 的 第 j 加 1 项 为 节点列表 的 第 j 项。
            j 为 j 减 1。
        完。
        节点列表 的 第 j 加 1 项 为 key。
        i 为 i 加 1。
    完。
完。

特征提取_诊断报告 接受 零参：
    可变。
    报告 为 「[NDFE诊断报告]\n」。
    报告 为 报告 加 「提取总次数: 」 加 特征_提取总次数 加 「\n」。
    报告 为 报告 加 「缓存命中: 」 加 特征_缓存命中 加 「\n」。
    报告 为 报告 加 「缓存未中: 」 加 特征_缓存未中 加 「\n」。
    报告 为 报告 加 「AI分类次数: 」 加 特征_AI分类次数 加 「\n」。
    若 特征_提取总次数 大于 0：
        命中率 为 特征_缓存命中 乘 100 除以 特征_提取总次数。
        报告 为 报告 加 「缓存命中率: 」 加 命中率 加 「%\n」。
    完。
    返还 报告。
完。

节点_获取标签ID 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    返还 节点 的 标签原子。
完。

节点_获取class 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    返还 做 元素_getAttribute 用 节点、「class」。
完。

节点_获取深度 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变。
    depth 为 0。
    可变。
    p 为 节点 的 父节点。
    只要 p 不等于 0：
        depth 为 depth 加 1。
        p 为 p 的 父节点。
    完。
    返还 depth。
完。

节点_获取子节点数 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    返还 节点 的 子节点数。
完。

节点_获取文本长度 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    若 节点 的 类型 等于 节点类型_文本：
        返还 做 字节长度 用 节点 的 数据。
    完。
    可变。
    total 为 0。
    可变。
    child 为 节点 的 第一个子节点。
    只要 child 不等于 0：
        total 为 total 加 做 节点_获取文本长度 用 child。
        child 为 child 的 下一个兄弟。
    完。
    返还 total。
完。

节点_有ID 接受 节点：
    若 节点 等于 0：
        返还 假。
    完。
    返还 做 节点_获取属性 用 节点、「id」 不等于 空。
完。

节点_包含链接 接受 节点：
    若 节点 等于 0：
        返还 假。
    完。
    返还 做 节点_查找后代标签 用 节点、标签_A 不等于 空。
完。

节点_包含图片 接受 节点：
    若 节点 等于 0：
        返还 假。
    完。
    返还 做 节点_查找后代标签 用 节点、标签_IMG 不等于 空。
完。

节点_包含表单 接受 节点：
    若 节点 等于 0：
        返还 假。
    完。
    返还 做 节点_查找后代标签 用 节点、标签_FORM 不等于 空。
完。

节点_获取版本 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    返还 节点 的 版本。
完。

节点_获取样式 接受 节点、属性名：
    返还 「」。
完。

节点_获取样式数值 接受 节点、属性名：
    若 节点 等于 0：
        返还 0。
    完。
    样式 为 节点 的 计算样式。
    若 样式 等于 空：
        返还 0。
    完。
    值 为 做 样式表_获取 用 样式、属性名。
    若 值 等于 空：
        返还 0。
    完。
    返还 值。
完。

节点_获取背景亮度 接受 节点：
    返还 Q_HALF。
完。

节点_获取属性 接受 节点、名称：
    若 节点 等于 0 或者 名称 等于 空：
        返还 空。
    完。
    attrs 为 节点 的 属性列表。
    若 attrs 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 attrs：
        若 attrs 的 第 i 项 的 名称 等于 名称：
            返还 attrs 的 第 i 项 的 值。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

节点_有事件监听 接受 节点、事件名：
    若 节点 等于 0 或者 事件名 等于 空：
        返还 假。
    完。
    监听器 为 节点 的 事件监听器。
    若 监听器 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 监听器：
        若 监听器 的 第 i 项 的 事件名 等于 事件名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

节点_是可编辑 接受 节点：
    若 节点 等于 0：
        返还 假。
    完。
    若 节点 的 标签ID 等于 标签_INPUT 或者 节点 的 标签ID 等于 标签_TEXTAREA：
        返还 真。
    完。
    ce 为 做 节点_获取属性 用 节点、「contenteditable」。
    若 ce 不等于 空 并且 ce 等于 「真」：
        返还 真。
    完。
    返还 假。
完。

节点_获取事件数 接受 节点：
    若 节点 等于 0 或者 节点 的 事件监听器 等于 空：
        返还 0。
    完。
    返还 做 计数 用 节点 的 事件监听器。
完。

节点_是在视口内 接受 节点：
    若 节点 等于 0：
        返还 假。
    完。
    bbox 为 节点 的 边界框。
    若 bbox 等于 空：
        返还 真。
    完。
    vw 为 做 视口_宽度。
    vh 为 做 视口_高度。
    若 bbox 的 x 加 bbox 的 宽 大于 0 并且 bbox 的 x 小于 vw 并且 bbox 的 y 加 bbox 的 高 大于 0 并且 bbox 的 y 小于 vh：
        返还 真。
    完。
    返还 假。
完。

节点_查找后代标签 接受 节点、标签ID：
    若 节点 等于 0：
        返还 空。
    完。
    子节点 为 节点 的 子节点。
    若 子节点 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子节点：
        若 子节点 的 第 i 项 的 标签ID 等于 标签ID：
            返还 子节点 的 第 i 项。
        完。
        找到 为 做 节点_查找后代标签 用 子节点 的 第 i 项、标签ID。
        若 找到 不等于 空：
            返还 找到。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

批量提取特征 接受 节点列表：
    若 做 数组_长度 用 节点列表 等于 0：
        返还 一组：（空）。
    完。
    可变。
    结果 为 一组：（空）。
    可变。
    i 为 0。
    只要 i 小于 做 数组_长度 用 节点列表：
        特征 为 做 提取_合并特征 用 节点列表 的 第 i 项。
        做 追加 用 结果、特征。
        i 为 i 加 1。
    完。
    返还 结果。
完。

特征_Min_Max归一化 接受 tensor、维度：
    若 tensor 等于 0 或者 维度 小于或等于 0：
        返还。
    完。
    可变。
    min_v 为 做 Tensor_读 用 tensor、0、0。
    可变。
    max_v 为 min_v。
    可变。
    i 为 1。
    只要 i 小于 维度：
        v 为 做 Tensor_读 用 tensor、0、i。
        若 v 小于 min_v：
            min_v 为 v。
        完。
        若 v 大于 max_v：
            max_v 为 v。
        完。
        i 为 i 加 1。
    完。
    范围 为 max_v 减 min_v。
    若 范围 小于或等于 0：
        返还。
    完。
    i 为 0。
    只要 i 小于 维度：
        v 为 做 Tensor_读 用 tensor、0、i。
        做 Tensor_写 用 tensor、0、i、(v 减 min_v) 乘 Q10 除以 范围。
        i 为 i 加 1。
    完。
完。

特征_Z_Score归一化 接受 tensor、维度：
    若 tensor 等于 0 或者 维度 小于或等于 0：
        返还。
    完。
    可变。
    总和 为 0。
    可变。
    i 为 0。
    只要 i 小于 维度：
        总和 为 总和 加 做 Tensor_读 用 tensor、0、i。
        i 为 i 加 1。
    完。
    均值 为 总和 除以 维度。
    可变。
    方差和 为 0。
    i 为 0。
    只要 i 小于 维度：
        d 为 做 Tensor_读 用 tensor、0、i 减 均值。
        方差和 为 方差和 加 d 乘 d 除以 Q10。
        i 为 i 加 1。
    完。
    标准差 为 方差和 除以 维度。
    可变。
    s 为 Q10。
    可变。
    iter 为 0。
    只要 iter 小于 10 并且 s 大于 0：
        s 为 (s 加 标准差 乘 Q10 除以 s) 除以 2。
        iter 为 iter 加 1。
    完。
    若 s 小于或等于 0：
        s 为 1。
    完。
    i 为 0。
    只要 i 小于 维度：
        v 为 做 Tensor_读 用 tensor、0、i。
        做 Tensor_写 用 tensor、0、i、(v 减 均值) 乘 Q10 除以 s。
        i 为 i 加 1。
    完。
完。

特征提取_重置统计 接受 零参：
    特征_提取总次数 为 0。
    特征_缓存命中 为 0。
    特征_缓存未中 为 0。
    特征_AI分类次数 为 0。
完。

本章 收束。
