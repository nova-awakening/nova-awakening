《会话存储》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·哈希整数》。

引用《..·感知·内核直连层》。

引用《..·中枢·内容可见性》。

最大会话 为 256。

默认过期 为 3600000000。

校验种子 为 2654435761。

会话记录 是 一种 事物，含 键哈希、渠道哈希、用户哈希、数据指针、数据长度、创建时刻、最近访问、过期时刻、校验值、活跃。

可变 会话表 为 一组：（空）。

可变 会话数 为 0。

可变 保存次数 为 0。

可变 加载次数 为 0。

可变 淘汰次数 为 0。

可变 过期次数 为 0。

会话存储_初始化 接受 零参：
    会话表 为 一组：（空）。
    会话数 为 0。
完。

会话存储_创建 接受 渠道、用户、数据、数据长、过期秒：
    若 会话数 大于或等于 最大会话：
        做 会话存储_淘汰。
    完。
    记录 为 新建 会话记录。
    现在 为 做 平台_获取高精度时间。
    记录 的 键哈希 为 做 会话存储_生成键 用 渠道、用户、现在。
    记录 的 渠道哈希 为 做 哈希_字符串 用 渠道。
    记录 的 用户哈希 为 做 哈希_字符串 用 用户。
    记录 的 数据指针 为 做 堆_分配 用 数据长。
    做 内存_复制 用 记录 的 数据指针、数据、数据长。
    记录 的 数据长度 为 数据长。
    记录 的 创建时刻 为 现在。
    记录 的 最近访问 为 现在。
    若 过期秒 大于 0：
        记录 的 过期时刻 为 现在 加 过期秒 乘 1000000。
    否则：
        记录 的 过期时刻 为 现在 加 默认过期。
    完。
    记录 的 校验值 为 做 会话存储_计算校验 用 数据、数据长。
    记录 的 活跃 为 真。
    做 追加 用 会话表、记录。
    索引 为 会话数。
    会话数 为 会话数 加 1。
    保存次数 为 保存次数 加 1。
    返还 记录 的 键哈希。
完。

会话存储_查找 接受 键哈希：
    加载次数 为 加载次数 加 1。
    现在 为 做 平台_获取高精度时间。
    可变 序号 为 0。
    只要 序号 小于 会话数：
        若 会话表 的 第 序号 项 的 键哈希 等于 键哈希 并且 会话表 的 第 序号 项 的 活跃：
            若 现在 大于 会话表 的 第 序号 项 的 过期时刻：
                会话表 的 第 序号 项 的 活跃 为 假。
                过期次数 为 过期次数 加 1。
                返还 0。
            完。
            会话表 的 第 序号 项 的 最近访问 为 现在。
            返还 会话表 的 第 序号 项 的 数据指针。
        完。
        序号 为 序号 加 1。
    完。
    返还 0。
完。

会话存储_更新 接受 键哈希、新数据、新长度：
    可变 序号 为 0。
    只要 序号 小于 会话数：
        若 会话表 的 第 序号 项 的 键哈希 等于 键哈希 并且 会话表 的 第 序号 项 的 活跃：
            若 会话表 的 第 序号 项 的 数据指针 不等于 0：
                做 堆_释放 用 会话表 的 第 序号 项 的 数据指针。
            完。
            会话表 的 第 序号 项 的 数据指针 为 做 堆_分配 用 新长度。
            做 内存_复制 用 会话表 的 第 序号 项 的 数据指针、新数据、新长度。
            会话表 的 第 序号 项 的 数据长度 为 新长度。
            会话表 的 第 序号 项 的 最近访问 为 做 平台_获取高精度时间。
            会话表 的 第 序号 项 的 校验值 为 做 会话存储_计算校验 用 新数据、新长度。
            保存次数 为 保存次数 加 1。
            返还 0。
        完。
        序号 为 序号 加 1。
    完。
    返还 错误_不存在。
完。

会话存储_删除 接受 键哈希：
    可变 序号 为 0。
    只要 序号 小于 会话数：
        若 会话表 的 第 序号 项 的 键哈希 等于 键哈希 并且 会话表 的 第 序号 项 的 活跃：
            会话表 的 第 序号 项 的 活跃 为 假。
            若 会话表 的 第 序号 项 的 数据指针 不等于 0：
                做 堆_释放 用 会话表 的 第 序号 项 的 数据指针。
                会话表 的 第 序号 项 的 数据指针 为 0。
            完。
            返还 0。
        完。
        序号 为 序号 加 1。
    完。
    返还 错误_不存在。
完。

会话存储_续期 接受 键哈希、额外秒：
    可变 序号 为 0。
    只要 序号 小于 会话数：
        若 会话表 的 第 序号 项 的 键哈希 等于 键哈希 并且 会话表 的 第 序号 项 的 活跃：
            会话表 的 第 序号 项 的 过期时刻 为 会话表 的 第 序号 项 的 过期时刻 加 额外秒 乘 1000000。
            返还 0。
        完。
        序号 为 序号 加 1。
    完。
    返还 错误_不存在。
完。

会话存储_清理过期 接受 零参：
    现在 为 做 平台_获取高精度时间。
    可变 序号 为 0。
    只要 序号 小于 会话数：
        若 会话表 的 第 序号 项 的 活跃 并且 现在 大于 会话表 的 第 序号 项 的 过期时刻：
            会话表 的 第 序号 项 的 活跃 为 假。
            若 会话表 的 第 序号 项 的 数据指针 不等于 0：
                做 堆_释放 用 会话表 的 第 序号 项 的 数据指针。
                会话表 的 第 序号 项 的 数据指针 为 0。
            完。
            过期次数 为 过期次数 加 1。
        完。
        序号 为 序号 加 1。
    完。
完。

会话存储_淘汰 接受 零参：
    可变 最旧 为 负 1。
    可变 最旧时间 为 0x7FFFFFFFFFFFFFFF。
    可变 序号 为 0。
    只要 序号 小于 会话数：
        若 会话表 的 第 序号 项 的 活跃 并且 会话表 的 第 序号 项 的 最近访问 小于 最旧时间：
            最旧时间 为 会话表 的 第 序号 项 的 最近访问。
            最旧 为 序号。
        完。
        序号 为 序号 加 1。
    完。
    若 最旧 大于或等于 0：
        会话表 的 第 最旧 项 的 活跃 为 假。
        若 会话表 的 第 最旧 项 的 数据指针 不等于 0：
            做 堆_释放 用 会话表 的 第 最旧 项 的 数据指针。
        完。
        淘汰次数 为 淘汰次数 加 1。
    完。
完。

会话存储_生成键 接受 渠道、用户、时间：
    返还 (做 哈希_字符串 用 渠道 位异或 做 哈希_字符串 用 用户 位异或 时间 位与 0xFFFFFFFF) 位与 0x7FFFFFFFFFFFFFFF。
完。

会话存储_计算校验 接受 数据、长度：
    可变 校验 为 校验种子。
    可变 位 为 0。
    只要 位 小于 长度：
        校验 为 校验 位异或 做 内存_读8 用 数据 加 位 乘 (位 加 1)。
        校验 为 校验 左移 5 位或 校验 右移 27。
        位 为 位 加 1。
    完。
    返还 校验。
完。

会话存储_验证 接受 键哈希：
    可变 序号 为 0。
    只要 序号 小于 会话数：
        若 会话表 的 第 序号 项 的 键哈希 等于 键哈希 并且 会话表 的 第 序号 项 的 活跃：
            实际校验 为 做 会话存储_计算校验 用 会话表 的 第 序号 项 的 数据指针、会话表 的 第 序号 项 的 数据长度。
            返还 实际校验 等于 会话表 的 第 序号 项 的 校验值。
        完。
        序号 为 序号 加 1。
    完。
    返还 假。
完。

会话存储_活跃数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 会话数：
        若 会话表 的 第 序号 项 的 活跃：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

会话存储_完整统计 接受 零参：
    返还 一组：会话数、做 会话存储_活跃数、保存次数、加载次数、淘汰次数、过期次数。
完。

可变 AI_会话存储_威胁计数 为 0。

AI_会话存储_风险评分 接受 操作类型、权限级别、来源可信度：
    可变 风险 为 0。
    若 权限级别 大于 500：
        风险 为 风险 加 300。
    完。
    若 来源可信度 小于 200：
        风险 为 风险 加 400。
    完。
    若 风险 大于 500：
        AI_会话存储_威胁计数 为 AI_会话存储_威胁计数 加 1。
    完。
    返还 风险。
完。

AI_会话存储_异常行为检测 接受 操作频率、基线频率：
    若 基线频率 大于 0 并且 操作频率 大于 基线频率 乘 5：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
