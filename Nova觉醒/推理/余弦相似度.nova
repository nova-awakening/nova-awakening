《余弦相似度》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·定点Q算术》。

引用《.·文档特征》。

引用《..·中枢·激活函数层》。

引用《.·权重持久》。

引用《.·注意力编码》。

Tensor行_余弦相似度 接受 A、行A、B、行B：
    返还 做 余弦相似度 用 A 的 数据、行A 乘 A 的 列数、B 的 数据、行B 乘 B 的 列数、A 的 列数。
完。

语义嵌入_计算节点嵌入 接受 引擎、节点：
    若 引擎 等于 0 或者 节点 等于 0：
        返还 0。
    完。
    标签ID 为 做 节点_获取标签ID 用 节点。
    标签向量 为 做 Tensor_切片行 用 引擎 的 标签嵌入表、标签ID、1。
    类名特征 为 做 提取_类名特征 用 节点。
    类名向量 为 做 Nova多层感知器_前向 用 引擎 的 类名嵌入网络、类名特征。
    做 Tensor_释放 用 类名特征。
    结构特征 为 做 提取_结构特征 用 节点。
    拼接输入 为 做 Tensor_创建 用 1、72。
    可变 i 为 0。
    只要 i 小于 32：
        做 Tensor_写 用 拼接输入、0、i、做 Tensor_读 用 标签向量、0、i。
        做 Tensor_写 用 拼接输入、0、32 加 i、做 Tensor_读 用 类名向量、0、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 8：
        做 Tensor_写 用 拼接输入、0、64 加 i、做 Tensor_读 用 结构特征、0、i。
        i 为 i 加 1。
    完。
    节点嵌入 为 做 Nova多层感知器_前向 用 引擎 的 节点嵌入网络、拼接输入。
    做 Tensor_释放 用 标签向量。
    做 Tensor_释放 用 类名向量。
    做 Tensor_释放 用 结构特征。
    做 Tensor_释放 用 拼接输入。
    返还 节点嵌入。
完。

语义嵌入_分类节点角色 接受 引擎、节点嵌入：
    若 引擎 等于 0 或者 节点嵌入 等于 0：
        返还 0。
    完。
    可变 最大相似度 为 负 Q_ONE。
    可变 最佳角色 为 0。
    可变 r 为 0。
    只要 r 小于 引擎 的 角色数量：
        相似度 为 做 Tensor行_余弦相似度 用 节点嵌入、0、引擎 的 语义原型、r。
        若 相似度 大于 最大相似度：
            最大相似度 为 相似度。
            最佳角色 为 r。
        完。
        r 为 r 加 1。
    完。
    返还 最佳角色。
完。

语义嵌入_批量分类 接受 引擎、嵌入矩阵：
    N 为 嵌入矩阵 的 行数。
    结果 为 做 Tensor_创建 用 1、N。
    可变 i 为 0。
    只要 i 小于 N：
        节点嵌入 为 做 Tensor_切片行 用 嵌入矩阵、i、1。
        角色 为 做 语义嵌入_分类节点角色 用 引擎、节点嵌入。
        做 Tensor_写 用 结果、0、i、角色。
        做 Tensor_释放 用 节点嵌入。
        i 为 i 加 1。
    完。
    返还 结果。
完。

本章 收束。
