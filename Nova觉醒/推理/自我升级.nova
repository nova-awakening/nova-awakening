《自我升级》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《..·标准·文件》。

引用《..·中枢·内核打印》。

引用《.·智能体文件操作》。

引用《.·网络操作》。

引用《..·标准·原生路径》。

引用《..·感知·错误创建》。

引用《..·中枢·高精度定时器hrtimer》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

升级_最大历史 为 32。

升级_最大依赖 为 64。

升级_最大关键文件 为 16。

升级_哈希_种子 为 0x4E564155。

可变 升级_当前版本 为 「0.1.0」。

可变 升级_最新版本 为 「」。

可变 升级_状态 为 「空闲」。

可变 升级_进度 为 0。

可变 升级_备份路径 为 「/var/nova/backup/」。

可变 升级_下载路径 为 「/var/nova/download/」。

可变 升级_暂存路径 为 「/var/nova/staging/」。

升级历史记录 是 一种 事物，含 版本、时间戳、成功、变更摘要。

升级统计 是 一种 事物，含 检查次数、有更新次数、下载次数、下载失败次数、安装次数、安装失败次数、回滚次数、备份次数、验证次数、验证失败次数、依赖检查次数、依赖冲突次数、总下载字节。

可变 全局_升级统计 为 空。

可变 全局_升级历史 为 一组：（空）。

可变 全局_升级历史数 为 0。

升级统计_初始化 接受 零参：
    若 全局_升级统计 等于 空：
        全局_升级统计 为 新建 升级统计。
        全局_升级统计 的 检查次数 为 0。
        全局_升级统计 的 有更新次数 为 0。
        全局_升级统计 的 下载次数 为 0。
        全局_升级统计 的 下载失败次数 为 0。
        全局_升级统计 的 安装次数 为 0。
        全局_升级统计 的 安装失败次数 为 0。
        全局_升级统计 的 回滚次数 为 0。
        全局_升级统计 的 备份次数 为 0。
        全局_升级统计 的 验证次数 为 0。
        全局_升级统计 的 验证失败次数 为 0。
        全局_升级统计 的 依赖检查次数 为 0。
        全局_升级统计 的 依赖冲突次数 为 0。
        全局_升级统计 的 总下载字节 为 0。
    完。
完。

版本_比较 接受 a、b：
    a主 为 做 版本_提取段 用 a、0。
    a次 为 做 版本_提取段 用 a、1。
    a补 为 做 版本_提取段 用 a、2。
    b主 为 做 版本_提取段 用 b、0。
    b次 为 做 版本_提取段 用 b、1。
    b补 为 做 版本_提取段 用 b、2。
    若 a主 不等于 b主：
        返还 a主 减 b主。
    完。
    若 a次 不等于 b次：
        返还 a次 减 b次。
    完。
    返还 a补 减 b补。
完。

版本_提取段 接受 版本字符串、段索引：
    可变 当前段 为 0。
    可变 值 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串_长度 用 版本字符串：
        ch 为 做 字符串_字符 用 版本字符串、i。
        若 ch 等于 46：
            若 当前段 等于 段索引：
                返还 值。
            完。
            当前段 为 当前段 加 1。
            值 为 0。
        否则若 ch 大于或等于 48 并且 ch 小于或等于 57：
            值 为 值 乘 10 加 (ch 减 48)。
        完。
        i 为 i 加 1。
    完。
    若 当前段 等于 段索引：
        返还 值。
    完。
    返还 0。
完。

升级_计算哈希 接受 数据、长度：
    可变 哈希 为 升级_哈希_种子。
    可变 i 为 0。
    只要 i 小于 长度：
        哈希 为 哈希 位异或 数据 的 第 i 项。
        哈希 为 哈希 乘 16777619。
        哈希 为 哈希 位与 0xFFFFFFFF。
        i 为 i 加 1。
    完。
    返还 哈希。
完。

自我升级_检查 接受 零参：
    做 升级统计_初始化。
    全局_升级统计 的 检查次数 为 全局_升级统计 的 检查次数 加 1。
    升级_状态 为 「检查中」。
    升级_进度 为 0。
    结果 为 做 智能体网络_GET 用 「https://api.novaos.dev/version」。
    若 结果 的 成功：
        升级_最新版本 为 结果 的 数据。
        若 做 版本_比较 用 升级_最新版本、升级_当前版本 大于 0：
            全局_升级统计 的 有更新次数 为 全局_升级统计 的 有更新次数 加 1。
            升级_状态 为 「空闲」。
            返还 真。
        完。
    完。
    升级_状态 为 「空闲」。
    返还 假。
完。

自我升级_获取变更日志 接受 版本：
    url 为 「https://api.novaos.dev/changelog/」 加 版本。
    结果 为 做 智能体网络_GET 用 url。
    若 结果 的 成功：
        返还 结果 的 数据。
    完。
    返还 「」。
完。

自我升级_下载 接受 版本：
    做 升级统计_初始化。
    全局_升级统计 的 下载次数 为 全局_升级统计 的 下载次数 加 1。
    升级_状态 为 「下载中」。
    升级_进度 为 0。
    做 目录_创建 用 升级_下载路径。
    url 为 「https://api.novaos.dev/download/」 加 版本。
    结果 为 做 智能体网络_GET 用 url。
    若 非 结果 的 成功：
        全局_升级统计 的 下载失败次数 为 全局_升级统计 的 下载失败次数 加 1。
        升级_状态 为 「失败」。
        返还 假。
    完。
    文件路径 为 升级_下载路径 加 「nova-」 加 版本 加 「.pkg」。
    做 文件_写全部 用 文件路径、结果 的 数据。
    全局_升级统计 的 总下载字节 为 全局_升级统计 的 总下载字节 加 做 字符串_长度 用 结果 的 数据。
    升级_进度 为 100。
    升级_状态 为 「空闲」。
    返还 真。
完。

自我升级_验证完整性 接受 文件路径、期望哈希：
    做 升级统计_初始化。
    全局_升级统计 的 验证次数 为 全局_升级统计 的 验证次数 加 1。
    若 非 做 智能体文件_存在 用 文件路径：
        全局_升级统计 的 验证失败次数 为 全局_升级统计 的 验证失败次数 加 1。
        返还 假。
    完。
    内容 为 做 文件_读全部 用 文件路径。
    若 内容 等于 空：
        全局_升级统计 的 验证失败次数 为 全局_升级统计 的 验证失败次数 加 1。
        返还 假。
    完。
    实际哈希 为 做 升级_计算哈希 用 内容、做 字符串_长度 用 内容。
    若 实际哈希 不等于 期望哈希：
        全局_升级统计 的 验证失败次数 为 全局_升级统计 的 验证失败次数 加 1。
        返还 假。
    完。
    返还 真。
完。

自我升级_检查依赖 接受 版本：
    做 升级统计_初始化。
    全局_升级统计 的 依赖检查次数 为 全局_升级统计 的 依赖检查次数 加 1。
    url 为 「https://api.novaos.dev/deps/」 加 版本。
    结果 为 做 智能体网络_GET 用 url。
    若 非 结果 的 成功：
        返还 真。
    完。
    依赖列表 为 结果 的 数据。
    可变 i 为 0。
    只要 i 小于 做 计数 用 依赖列表 并且 i 小于 升级_最大依赖：
        dep 为 依赖列表 的 第 i 项。
        若 非 做 自我升级_依赖已满足 用 dep：
            全局_升级统计 的 依赖冲突次数 为 全局_升级统计 的 依赖冲突次数 加 1。
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

自我升级_依赖已满足 接受 dep：
    若 dep 等于 空：
        返还 真。
    完。
    若 dep 的 名称 等于 空 或者 dep 的 最低版本 等于 空：
        返还 真。
    完。
    已安装版本 为 做 智能体_获取包版本 用 dep 的 名称。
    若 已安装版本 等于 空：
        返还 假。
    完。
    返还 做 版本_比较 用 已安装版本、dep 的 最低版本 大于或等于 0。
完。

自我升级_备份 接受 零参：
    做 升级统计_初始化。
    全局_升级统计 的 备份次数 为 全局_升级统计 的 备份次数 加 1。
    做 目录_创建 用 升级_备份路径。
    关键文件 为 一组：「/var/nova/记忆.dat」、「/var/nova/packages/清单.json」、「/var/nova/config/系统配置.json」、「/var/nova/data/突触网格.dat」。
    可变 成功数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 关键文件 并且 i 小于 升级_最大关键文件：
        内容 为 做 文件_读全部 用 关键文件 的 第 i 项。
        若 内容 不等于 空：
            做 文件_写全部 用 升级_备份路径 加 做 路径_文件名 用 关键文件 的 第 i 项、内容。
            成功数 为 成功数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 成功数 大于 0。
完。

自我升级_暂存 接受 新版本路径：
    做 目录_创建 用 升级_暂存路径。
    若 非 做 智能体文件_存在 用 新版本路径：
        返还 假。
    完。
    内容 为 做 文件_读全部 用 新版本路径。
    若 内容 等于 空：
        返还 假。
    完。
    做 文件_写全部 用 升级_暂存路径 加 「staged.pkg」、内容。
    返还 真。
完。

自我升级_应用 接受 新版本路径：
    做 升级统计_初始化。
    全局_升级统计 的 安装次数 为 全局_升级统计 的 安装次数 加 1。
    升级_状态 为 「安装中」。
    升级_进度 为 0。
    若 非 做 自我升级_检查依赖 用 升级_最新版本：
        升级_状态 为 「失败」。
        全局_升级统计 的 安装失败次数 为 全局_升级统计 的 安装失败次数 加 1。
        返还 做 错误_创建 用 「升级」、「依赖检查失败」。
    完。
    升级_进度 为 10。
    备份结果 为 做 自我升级_备份。
    若 非 备份结果：
        升级_状态 为 「失败」。
        全局_升级统计 的 安装失败次数 为 全局_升级统计 的 安装失败次数 加 1。
        返还 做 错误_创建 用 「升级」、「备份失败」。
    完。
    升级_进度 为 30。
    若 非 做 智能体文件_存在 用 新版本路径：
        升级_状态 为 「失败」。
        全局_升级统计 的 安装失败次数 为 全局_升级统计 的 安装失败次数 加 1。
        返还 做 错误_创建 用 「升级」、「新版本文件不存在」。
    完。
    升级_进度 为 40。
    若 非 做 自我升级_暂存 用 新版本路径：
        升级_状态 为 「失败」。
        全局_升级统计 的 安装失败次数 为 全局_升级统计 的 安装失败次数 加 1。
        返还 做 错误_创建 用 「升级」、「暂存失败」。
    完。
    升级_进度 为 60。
    升级_进度 为 80。
    旧版本 为 升级_当前版本。
    升级_当前版本 为 升级_最新版本。
    升级_进度 为 90。
    做 升级_记录历史 用 升级_最新版本、真、「从 」 加 旧版本 加 「 升级」。
    升级_进度 为 100。
    升级_状态 为 「完成」。
    返还 成功。
完。

自我升级_回滚 接受 零参：
    做 升级统计_初始化。
    全局_升级统计 的 回滚次数 为 全局_升级统计 的 回滚次数 加 1。
    升级_状态 为 「回滚中」。
    备份文件 为 做 智能体文件_搜索 用 升级_备份路径、「」。
    若 备份文件 等于 空 或者 做 计数 用 备份文件 等于 0：
        升级_状态 为 「失败」。
        返还 假。
    完。
    可变 恢复数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 备份文件：
        原路径 为 「/var/nova/」 加 做 路径_文件名 用 备份文件 的 第 i 项。
        内容 为 做 文件_读全部 用 备份文件 的 第 i 项。
        若 内容 不等于 空：
            做 文件_写全部 用 原路径、内容。
            恢复数 为 恢复数 加 1。
        完。
        i 为 i 加 1。
    完。
    做 升级_记录历史 用 升级_当前版本、假、「回滚」。
    升级_状态 为 「空闲」。
    返还 恢复数 大于 0。
完。

升级_记录历史 接受 版本、成功、摘要：
    若 全局_升级历史数 大于或等于 升级_最大历史：
        可变 i 为 0。
        只要 i 小于 全局_升级历史数 减 1：
            全局_升级历史 的 第 i 项 为 全局_升级历史 的 第 i 加 1 项。
            i 为 i 加 1。
        完。
        全局_升级历史数 为 全局_升级历史数 减 1。
    完。
    记录 为 新建 升级历史记录。
    记录 的 版本 为 版本。
    记录 的 时间戳 为 做 获取滴答。
    记录 的 成功 为 成功。
    记录 的 变更摘要 为 摘要。
    若 全局_升级历史数 小于 做 计数 用 全局_升级历史：
        全局_升级历史 的 第 全局_升级历史数 项 为 记录。
    否则：
        做 追加 用 全局_升级历史、记录。
    完。
    全局_升级历史数 为 全局_升级历史数 加 1。
完。

自我升级_全自动 接受 零参：
    若 非 做 自我升级_检查：
        返还 假。
    完。
    版本 为 升级_最新版本。
    若 非 做 自我升级_下载 用 版本：
        返还 假。
    完。
    pkg路径 为 升级_下载路径 加 「nova-」 加 版本 加 「.pkg」。
    结果 为 做 自我升级_应用 用 pkg路径。
    若 结果 不等于 成功：
        做 自我升级_回滚。
        返还 假。
    完。
    返还 真。
完。

自我升级_状态 接受 零参：
    返还 升级_状态。
完。

自我升级_进度 接受 零参：
    返还 升级_进度。
完。

自我升级_版本 接受 零参：
    返还 升级_当前版本。
完。

自我升级_历史数 接受 零参：
    返还 全局_升级历史数。
完。

自我升级_获取历史 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 全局_升级历史数：
        返还 空。
    完。
    返还 全局_升级历史 的 第 索引 项。
完。

自我升级_打印统计 接受 零参：
    做 升级统计_初始化。
    做 打印行 用 「=== 自我升级统计 ===」。
    做 打印行 用 「当前版本: 」 加 升级_当前版本。
    做 打印行 用 「检查次数: 」 加 做 整数转字符串 用 全局_升级统计 的 检查次数。
    做 打印行 用 「有更新: 」 加 做 整数转字符串 用 全局_升级统计 的 有更新次数。
    做 打印行 用 「下载: 」 加 做 整数转字符串 用 全局_升级统计 的 下载次数。
    做 打印行 用 「下载失败: 」 加 做 整数转字符串 用 全局_升级统计 的 下载失败次数。
    做 打印行 用 「安装: 」 加 做 整数转字符串 用 全局_升级统计 的 安装次数。
    做 打印行 用 「安装失败: 」 加 做 整数转字符串 用 全局_升级统计 的 安装失败次数。
    做 打印行 用 「回滚: 」 加 做 整数转字符串 用 全局_升级统计 的 回滚次数。
    做 打印行 用 「备份: 」 加 做 整数转字符串 用 全局_升级统计 的 备份次数。
    做 打印行 用 「验证: 」 加 做 整数转字符串 用 全局_升级统计 的 验证次数。
    做 打印行 用 「验证失败: 」 加 做 整数转字符串 用 全局_升级统计 的 验证失败次数。
    做 打印行 用 「依赖检查: 」 加 做 整数转字符串 用 全局_升级统计 的 依赖检查次数。
    做 打印行 用 「依赖冲突: 」 加 做 整数转字符串 用 全局_升级统计 的 依赖冲突次数。
    做 打印行 用 「下载总量: 」 加 做 整数转字符串 用 全局_升级统计 的 总下载字节 加 「 字节」。
    做 打印行 用 「升级历史: 」 加 做 整数转字符串 用 全局_升级历史数 加 「 条」。
完。

升级_自动间隔 为 3600000。

可变 升级_上次自动检查 为 0。

可变 升级_自动升级开关 为 假。

自我升级_启用自动 接受 零参：
    升级_自动升级开关 为 真。
完。

自我升级_禁用自动 接受 零参：
    升级_自动升级开关 为 假。
完。

自我升级_自动开关 接受 零参：
    返还 升级_自动升级开关。
完。

自我升级_定时检查 接受 零参：
    若 非 升级_自动升级开关：
        返还。
    完。
    当前 为 做 获取滴答。
    若 当前 减 升级_上次自动检查 小于 升级_自动间隔：
        返还。
    完。
    升级_上次自动检查 为 当前。
    做 自我升级_全自动。
完。

自我升级_健康检查 接受 零参：
    做 升级统计_初始化。
    可变 问题 为 0。
    若 升级_状态 不等于 「空闲」 并且 升级_状态 不等于 「完成」 并且 升级_状态 不等于 「失败」 并且 升级_状态 不等于 「检查中」 并且 升级_状态 不等于 「下载中」 并且 升级_状态 不等于 「安装中」 并且 升级_状态 不等于 「回滚中」：
        问题 为 问题 加 1。
    完。
    若 升级_进度 小于 0 或者 升级_进度 大于 100：
        问题 为 问题 加 1。
    完。
    若 全局_升级统计 的 安装次数 大于 0：
        失败率 为 全局_升级统计 的 安装失败次数 乘 100 除以 全局_升级统计 的 安装次数。
        若 失败率 大于 50：
            问题 为 问题 加 1。
        完。
    完。
    返还 问题 等于 0。
完。

自我升级_重置统计 接受 零参：
    做 升级统计_初始化。
    全局_升级统计 的 检查次数 为 0。
    全局_升级统计 的 有更新次数 为 0。
    全局_升级统计 的 下载次数 为 0。
    全局_升级统计 的 下载失败次数 为 0。
    全局_升级统计 的 安装次数 为 0。
    全局_升级统计 的 安装失败次数 为 0。
    全局_升级统计 的 回滚次数 为 0。
    全局_升级统计 的 备份次数 为 0。
    全局_升级统计 的 验证次数 为 0。
    全局_升级统计 的 验证失败次数 为 0。
    全局_升级统计 的 依赖检查次数 为 0。
    全局_升级统计 的 依赖冲突次数 为 0。
    全局_升级统计 的 总下载字节 为 0。
    全局_升级历史数 为 0。
完。

自我升级_获取检查数 接受 零参：
    返还 全局_升级统计 的 检查次数。
完。

自我升级_获取下载数 接受 零参：
    返还 全局_升级统计 的 下载次数。
完。

自我升级_获取安装数 接受 零参：
    返还 全局_升级统计 的 安装次数。
完。

自我升级_获取回滚数 接受 零参：
    返还 全局_升级统计 的 回滚次数。
完。

自我升级_获取备份数 接受 零参：
    返还 全局_升级统计 的 备份次数。
完。

自我升级_获取总字节 接受 零参：
    返还 全局_升级统计 的 总下载字节。
完。

自我升级_完整统计 接受 零参：
    做 升级统计_初始化。
    返还 一组：全局_升级统计 的 检查次数、全局_升级统计 的 有更新次数、全局_升级统计 的 下载次数、全局_升级统计 的 安装次数、全局_升级统计 的 安装失败次数、全局_升级统计 的 回滚次数、全局_升级统计 的 备份次数、全局_升级统计 的 总下载字节、全局_升级历史数。
完。

自我升级_完整历史 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 全局_升级历史数：
        做 追加 用 结果、一本字典：版本 对应 全局_升级历史 的 第 i 项 的 版本、时间 对应 全局_升级历史 的 第 i 项 的 时间戳、成功 对应 全局_升级历史 的 第 i 项 的 成功、摘要 对应 全局_升级历史 的 第 i 项 的 变更摘要。
        i 为 i 加 1。
    完。
    返还 结果。
完。

自我升级_最近升级是否成功 接受 零参：
    若 全局_升级历史数 等于 0：
        返还 假。
    完。
    返还 全局_升级历史 的 第 全局_升级历史数 减 1 项 的 成功。
完。

自我升级_平均下载字节 接受 零参：
    做 升级统计_初始化。
    若 全局_升级统计 的 下载次数 等于 0：
        返还 0。
    完。
    返还 全局_升级统计 的 总下载字节 除以 全局_升级统计 的 下载次数。
完。

自我升级_成功率 接受 零参：
    做 升级统计_初始化。
    若 全局_升级统计 的 安装次数 等于 0：
        返还 1000。
    完。
    返还 (全局_升级统计 的 安装次数 减 全局_升级统计 的 安装失败次数) 乘 1000 除以 全局_升级统计 的 安装次数。
完。

自我升级_下载成功率 接受 零参：
    做 升级统计_初始化。
    若 全局_升级统计 的 下载次数 等于 0：
        返还 1000。
    完。
    返还 (全局_升级统计 的 下载次数 减 全局_升级统计 的 下载失败次数) 乘 1000 除以 全局_升级统计 的 下载次数。
完。

自我升级_验证成功率 接受 零参：
    做 升级统计_初始化。
    若 全局_升级统计 的 验证次数 等于 0：
        返还 1000。
    完。
    返还 (全局_升级统计 的 验证次数 减 全局_升级统计 的 验证失败次数) 乘 1000 除以 全局_升级统计 的 验证次数。
完。

自我升级_综合健康度 接受 零参：
    s1 为 做 自我升级_成功率。
    s2 为 做 自我升级_下载成功率。
    s3 为 做 自我升级_验证成功率。
    返还 (s1 加 s2 加 s3) 除以 3。
完。

本章 收束。
