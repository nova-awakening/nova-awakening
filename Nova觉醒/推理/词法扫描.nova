《词法扫描》

引用《..·中枢·系统调用表》。

引用《..·推理·通信渠道》。

引用《..·感知·内核直连层》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

TOK_EOF 为 0。

TOK_标识符 为 1。

TOK_数字 为 2。

TOK_字符串 为 3。

TOK_模板头 为 4。

TOK_模板中 为 5。

TOK_模板尾 为 6。

TOK_正则 为 7。

TOK_左括号 为 10。

TOK_右括号 为 11。

TOK_左方括号 为 12。

TOK_右方括号 为 13。

TOK_左花括号 为 14。

TOK_右花括号 为 15。

TOK_分号 为 16。

TOK_逗号 为 17。

TOK_点 为 18。

TOK_展开 为 19。

TOK_可选链 为 20。

TOK_加 为 30。

TOK_减 为 31。

TOK_乘 为 32。

TOK_除 为 33。

TOK_取模 为 34。

TOK_幂 为 35。

TOK_赋值 为 40。

TOK_加等 为 41。

TOK_减等 为 42。

TOK_乘等 为 43。

TOK_除等 为 44。

TOK_模等 为 45。

TOK_幂等 为 46。

TOK_等于 为 50。

TOK_不等 为 51。

TOK_严格等 为 52。

TOK_严格不等 为 53。

TOK_小于 为 54。

TOK_大于 为 55。

TOK_小于等 为 56。

TOK_大于等 为 57。

TOK_与 为 60。

TOK_或 为 61。

TOK_非 为 62。

TOK_空合 为 63。

TOK_位与 为 64。

TOK_位或 为 65。

TOK_位异或 为 66。

TOK_位非 为 67。

TOK_左移 为 68。

TOK_右移 为 69。

TOK_无符号右移 为 70。

TOK_自增 为 71。

TOK_自减 为 72。

TOK_箭头 为 73。

TOK_问号 为 74。

TOK_冒号 为 75。

TOK_错误 为 99。

KW_var 为 100。

KW_let 为 101。

KW_const 为 102。

KW_function 为 103。

KW_return 为 104。

KW_if 为 105。

KW_else 为 106。

KW_while 为 107。

KW_for 为 108。

KW_do 为 109。

KW_break 为 110。

KW_continue 为 111。

KW_switch 为 112。

KW_case 为 113。

KW_default 为 114。

KW_new 为 115。

KW_delete 为 116。

KW_typeof 为 117。

KW_instanceof 为 118。

KW_in 为 119。

KW_this 为 120。

KW_class 为 121。

KW_extends 为 122。

KW_super 为 123。

KW_import 为 124。

KW_export 为 125。

KW_try 为 126。

KW_catch 为 127。

KW_finally 为 128。

KW_throw 为 129。

KW_void 为 130。

KW_yield 为 131。

KW_await 为 132。

KW_async 为 133。

KW_of 为 134。

KW_true 为 135。

KW_false 为 136。

KW_null 为 137。

KW_undefined 为 138。

令牌 是 一种 事物，含 类型、值、值长度、行、列。

Scanner 是 一种 事物，含 源码、源码长度、位置、行、列、当前令牌、前瞻令牌、模板深度、上一个令牌类型、扫描令牌数、错误数。

Scanner_创建 接受 源码：
    s 为 做 堆_分配 用 Scanner。
    s 的 源码 为 源码。
    s 的 源码长度 为 做 字符串_长度 用 源码。
    s 的 位置 为 0。
    s 的 行 为 1。
    s 的 列 为 0。
    s 的 模板深度 为 0。
    s 的 上一个令牌类型 为 0。
    s 的 扫描令牌数 为 0。
    s 的 错误数 为 0。
    做 Scanner_扫描 用 s。
    返还 s。
完。

Scanner_字符 接受 s：
    若 s 的 位置 大于或等于 s 的 源码长度：
        返还 0。
    完。
    返还 做 内存_读8 用 s 的 源码 加 s 的 位置。
完。

Scanner_前看 接受 s：
    若 s 的 位置 加 1 大于或等于 s 的 源码长度：
        返还 0。
    完。
    返还 做 内存_读8 用 s 的 源码 加 s 的 位置 加 1。
完。

Scanner_前看2 接受 s：
    若 s 的 位置 加 2 大于或等于 s 的 源码长度：
        返还 0。
    完。
    返还 做 内存_读8 用 s 的 源码 加 s 的 位置 加 2。
完。

Scanner_前进 接受 s：
    若 s 的 位置 小于 s 的 源码长度：
        若 做 Scanner_字符 用 s 等于 0x0A：
            s 的 行 为 s 的 行 加 1。
            s 的 列 为 0。
        否则：
            s 的 列 为 s 的 列 加 1。
        完。
        s 的 位置 为 s 的 位置 加 1。
    完。
完。

Scanner_跳过空白和注释 接受 s：
    只要 s 的 位置 小于 s 的 源码长度：
        c 为 做 Scanner_字符 用 s。
        若 c 等于 0x20 或者 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0D 或者 c 等于 0x0C 或者 c 等于 0xFEFF：
            做 Scanner_前进 用 s。
            继续。
        完。
        若 c 等于 0x2F 并且 做 Scanner_前看 用 s 等于 0x2F：
            只要 s 的 位置 小于 s 的 源码长度 并且 做 Scanner_字符 用 s 不等于 0x0A：
                做 Scanner_前进 用 s。
            完。
            继续。
        完。
        若 c 等于 0x2F 并且 做 Scanner_前看 用 s 等于 0x2A：
            做 Scanner_前进 用 s。
            做 Scanner_前进 用 s。
            只要 s 的 位置 小于 s 的 源码长度 减 1：
                若 做 Scanner_字符 用 s 等于 0x2A 并且 做 Scanner_前看 用 s 等于 0x2F：
                    做 Scanner_前进 用 s。
                    做 Scanner_前进 用 s。
                    跳出。
                完。
                做 Scanner_前进 用 s。
            完。
            继续。
        完。
        跳出。
    完。
完。

Scanner_是标识符开始 接受 c：
    若 c 大于或等于 0x41 并且 c 小于或等于 0x5A：
        返还 真。
    完。
    若 c 大于或等于 0x61 并且 c 小于或等于 0x7A：
        返还 真。
    完。
    若 c 等于 0x5F 或者 c 等于 0x24：
        返还 真。
    完。
    若 c 大于 127：
        返还 真。
    完。
    返还 假。
完。

Scanner_是标识符续 接受 c：
    若 做 Scanner_是标识符开始 用 c：
        返还 真。
    完。
    若 c 大于或等于 0x30 并且 c 小于或等于 0x39：
        返还 真。
    完。
    返还 假。
完。

Scanner_关键字查找 接受 文本、长度：
    若 长度 等于 2：
        若 做 字符串_相等 用 文本、「if」、长度：
            返还 KW_if。
        完。
        若 做 字符串_相等 用 文本、「in」、长度：
            返还 KW_in。
        完。
        若 做 字符串_相等 用 文本、「do」、长度：
            返还 KW_do。
        完。
        若 做 字符串_相等 用 文本、「of」、长度：
            返还 KW_of。
        完。
    完。
    若 长度 等于 3：
        若 做 字符串_相等 用 文本、「var」、长度：
            返还 KW_var。
        完。
        若 做 字符串_相等 用 文本、「let」、长度：
            返还 KW_let。
        完。
        若 做 字符串_相等 用 文本、「for」、长度：
            返还 KW_for。
        完。
        若 做 字符串_相等 用 文本、「new」、长度：
            返还 KW_new。
        完。
        若 做 字符串_相等 用 文本、「try」、长度：
            返还 KW_try。
        完。
    完。
    若 长度 等于 4：
        若 做 字符串_相等 用 文本、「this」、长度：
            返还 KW_this。
        完。
        若 做 字符串_相等 用 文本、「else」、长度：
            返还 KW_else。
        完。
        若 做 字符串_相等 用 文本、「case」、长度：
            返还 KW_case。
        完。
        若 做 字符串_相等 用 文本、「void」、长度：
            返还 KW_void。
        完。
        若 做 字符串_相等 用 文本、「true」、长度：
            返还 KW_true。
        完。
        若 做 字符串_相等 用 文本、「null」、长度：
            返还 KW_null。
        完。
    完。
    若 长度 等于 5：
        若 做 字符串_相等 用 文本、「const」、长度：
            返还 KW_const。
        完。
        若 做 字符串_相等 用 文本、「while」、长度：
            返还 KW_while。
        完。
        若 做 字符串_相等 用 文本、「break」、长度：
            返还 KW_break。
        完。
        若 做 字符串_相等 用 文本、「class」、长度：
            返还 KW_class。
        完。
        若 做 字符串_相等 用 文本、「super」、长度：
            返还 KW_super。
        完。
        若 做 字符串_相等 用 文本、「throw」、长度：
            返还 KW_throw。
        完。
        若 做 字符串_相等 用 文本、「yield」、长度：
            返还 KW_yield。
        完。
        若 做 字符串_相等 用 文本、「await」、长度：
            返还 KW_await。
        完。
        若 做 字符串_相等 用 文本、「async」、长度：
            返还 KW_async。
        完。
        若 做 字符串_相等 用 文本、「false」、长度：
            返还 KW_false。
        完。
        若 做 字符串_相等 用 文本、「catch」、长度：
            返还 KW_catch。
        完。
    完。
    若 长度 等于 6：
        若 做 字符串_相等 用 文本、「return」、长度：
            返还 KW_return。
        完。
        若 做 字符串_相等 用 文本、「switch」、长度：
            返还 KW_switch。
        完。
        若 做 字符串_相等 用 文本、「delete」、长度：
            返还 KW_delete。
        完。
        若 做 字符串_相等 用 文本、「typeof」、长度：
            返还 KW_typeof。
        完。
        若 做 字符串_相等 用 文本、「import」、长度：
            返还 KW_import。
        完。
        若 做 字符串_相等 用 文本、「export」、长度：
            返还 KW_export。
        完。
    完。
    若 长度 等于 7：
        若 做 字符串_相等 用 文本、「default」、长度：
            返还 KW_default。
        完。
        若 做 字符串_相等 用 文本、「extends」、长度：
            返还 KW_extends。
        完。
        若 做 字符串_相等 用 文本、「finally」、长度：
            返还 KW_finally。
        完。
    完。
    若 长度 等于 8：
        若 做 字符串_相等 用 文本、「function」、长度：
            返还 KW_function。
        完。
        若 做 字符串_相等 用 文本、「continue」、长度：
            返还 KW_continue。
        完。
    完。
    若 长度 等于 9：
        若 做 字符串_相等 用 文本、「undefined」、长度：
            返还 KW_undefined。
        完。
    完。
    若 长度 等于 10：
        若 做 字符串_相等 用 文本、「instanceof」、长度：
            返还 KW_instanceof。
        完。
    完。
    返还 0。
完。

Scanner_扫描标识符 接受 s、tok：
    开始 为 s 的 位置。
    只要 s 的 位置 小于 s 的 源码长度 并且 做 Scanner_是标识符续 用 做 Scanner_字符 用 s：
        做 Scanner_前进 用 s。
    完。
    长度 为 s 的 位置 减 开始。
    关键字 为 做 Scanner_关键字查找 用 s 的 源码 加 开始、长度。
    若 关键字 大于 0：
        tok 的 类型 为 关键字。
    否则：
        tok 的 类型 为 TOK_标识符。
    完。
    tok 的 值 为 s 的 源码 加 开始。
    tok 的 值长度 为 长度。
完。

Scanner_扫描数字 接受 s、tok：
    开始 为 s 的 位置。
    c 为 做 Scanner_字符 用 s。
    若 c 等于 0x30：
        下一个 为 做 Scanner_前看 用 s。
        若 下一个 等于 0x78 或者 下一个 等于 0x58：
            做 Scanner_前进 用 s。
            做 Scanner_前进 用 s。
            只要 s 的 位置 小于 s 的 源码长度：
                h 为 做 Scanner_字符 用 s。
                若 h 大于或等于 0x30 并且 h 小于或等于 0x39 或者 h 大于或等于 0x41 并且 h 小于或等于 0x46 或者 h 大于或等于 0x61 并且 h 小于或等于 0x66：
                    做 Scanner_前进 用 s。
                否则：
                    跳出。
                完。
            完。
            tok 的 类型 为 TOK_数字。
            tok 的 值 为 s 的 源码 加 开始。
            tok 的 值长度 为 s 的 位置 减 开始。
            返还。
        完。
        若 下一个 等于 0x6F 或者 下一个 等于 0x4F：
            做 Scanner_前进 用 s。
            做 Scanner_前进 用 s。
            只要 s 的 位置 小于 s 的 源码长度 并且 做 Scanner_字符 用 s 大于或等于 0x30 并且 做 Scanner_字符 用 s 小于或等于 0x37：
                做 Scanner_前进 用 s。
            完。
            tok 的 类型 为 TOK_数字。
            tok 的 值 为 s 的 源码 加 开始。
            tok 的 值长度 为 s 的 位置 减 开始。
            返还。
        完。
        若 下一个 等于 0x62 或者 下一个 等于 0x42：
            做 Scanner_前进 用 s。
            做 Scanner_前进 用 s。
            只要 s 的 位置 小于 s 的 源码长度 并且 (做 Scanner_字符 用 s 等于 0x30 或者 做 Scanner_字符 用 s 等于 0x31)：
                做 Scanner_前进 用 s。
            完。
            tok 的 类型 为 TOK_数字。
            tok 的 值 为 s 的 源码 加 开始。
            tok 的 值长度 为 s 的 位置 减 开始。
            返还。
        完。
    完。
    只要 s 的 位置 小于 s 的 源码长度 并且 做 Scanner_字符 用 s 大于或等于 0x30 并且 做 Scanner_字符 用 s 小于或等于 0x39：
        做 Scanner_前进 用 s。
    完。
    若 s 的 位置 小于 s 的 源码长度 并且 做 Scanner_字符 用 s 等于 0x2E：
        做 Scanner_前进 用 s。
        只要 s 的 位置 小于 s 的 源码长度 并且 做 Scanner_字符 用 s 大于或等于 0x30 并且 做 Scanner_字符 用 s 小于或等于 0x39：
            做 Scanner_前进 用 s。
        完。
    完。
    若 s 的 位置 小于 s 的 源码长度 并且 (做 Scanner_字符 用 s 等于 0x65 或者 做 Scanner_字符 用 s 等于 0x45)：
        做 Scanner_前进 用 s。
        若 s 的 位置 小于 s 的 源码长度 并且 (做 Scanner_字符 用 s 等于 0x2B 或者 做 Scanner_字符 用 s 等于 0x2D)：
            做 Scanner_前进 用 s。
        完。
        只要 s 的 位置 小于 s 的 源码长度 并且 做 Scanner_字符 用 s 大于或等于 0x30 并且 做 Scanner_字符 用 s 小于或等于 0x39：
            做 Scanner_前进 用 s。
        完。
    完。
    若 s 的 位置 小于 s 的 源码长度 并且 做 Scanner_字符 用 s 等于 0x6E：
        做 Scanner_前进 用 s。
    完。
    tok 的 类型 为 TOK_数字。
    tok 的 值 为 s 的 源码 加 开始。
    tok 的 值长度 为 s 的 位置 减 开始。
完。

Scanner_扫描字符串 接受 s、tok、引号：
    做 Scanner_前进 用 s。
    开始 为 s 的 位置。
    只要 s 的 位置 小于 s 的 源码长度：
        c 为 做 Scanner_字符 用 s。
        若 c 等于 引号：
            跳出。
        完。
        若 c 等于 0x5C：
            做 Scanner_前进 用 s。
        完。
        若 c 等于 0x0A：
            s 的 错误数 为 s 的 错误数 加 1。
            跳出。
        完。
        做 Scanner_前进 用 s。
    完。
    tok 的 类型 为 TOK_字符串。
    tok 的 值 为 s 的 源码 加 开始。
    tok 的 值长度 为 s 的 位置 减 开始。
    若 s 的 位置 小于 s 的 源码长度：
        做 Scanner_前进 用 s。
    完。
完。

Scanner_扫描模板 接受 s、tok：
    做 Scanner_前进 用 s。
    开始 为 s 的 位置。
    只要 s 的 位置 小于 s 的 源码长度：
        c 为 做 Scanner_字符 用 s。
        若 c 等于 0x60：
            tok 的 类型 为 TOK_模板尾。
            tok 的 值 为 s 的 源码 加 开始。
            tok 的 值长度 为 s 的 位置 减 开始。
            做 Scanner_前进 用 s。
            返还。
        完。
        若 c 等于 0x24 并且 做 Scanner_前看 用 s 等于 0x7B：
            tok 的 类型 为 TOK_模板头。
            tok 的 值 为 s 的 源码 加 开始。
            tok 的 值长度 为 s 的 位置 减 开始。
            做 Scanner_前进 用 s。
            做 Scanner_前进 用 s。
            s 的 模板深度 为 s 的 模板深度 加 1。
            返还。
        完。
        若 c 等于 0x5C：
            做 Scanner_前进 用 s。
        完。
        做 Scanner_前进 用 s。
    完。
    tok 的 类型 为 TOK_错误。
    s 的 错误数 为 s 的 错误数 加 1。
完。

Scanner_可能是正则 接受 上一个类型：
    若 上一个类型 等于 TOK_标识符：
        返还 假。
    完。
    若 上一个类型 等于 TOK_数字：
        返还 假。
    完。
    若 上一个类型 等于 TOK_字符串：
        返还 假。
    完。
    若 上一个类型 等于 TOK_右括号：
        返还 假。
    完。
    若 上一个类型 等于 TOK_右方括号：
        返还 假。
    完。
    若 上一个类型 等于 KW_this：
        返还 假。
    完。
    若 上一个类型 等于 KW_true：
        返还 假。
    完。
    若 上一个类型 等于 KW_false：
        返还 假。
    完。
    若 上一个类型 等于 KW_null：
        返还 假。
    完。
    返还 真。
完。

Scanner_扫描正则 接受 s、tok：
    做 Scanner_前进 用 s。
    开始 为 s 的 位置。
    可变 在字符类中 为 假。
    只要 s 的 位置 小于 s 的 源码长度：
        c 为 做 Scanner_字符 用 s。
        若 c 等于 0x0A：
            s 的 错误数 为 s 的 错误数 加 1。
            跳出。
        完。
        若 c 等于 0x5C：
            做 Scanner_前进 用 s。
            做 Scanner_前进 用 s。
            继续。
        完。
        若 c 等于 0x5B：
            在字符类中 为 真。
        完。
        若 c 等于 0x5D：
            在字符类中 为 假。
        完。
        若 c 等于 0x2F 并且 非 在字符类中：
            跳出。
        完。
        做 Scanner_前进 用 s。
    完。
    模式结束 为 s 的 位置。
    若 s 的 位置 小于 s 的 源码长度：
        做 Scanner_前进 用 s。
    完。
    只要 s 的 位置 小于 s 的 源码长度 并且 做 Scanner_是标识符续 用 做 Scanner_字符 用 s：
        做 Scanner_前进 用 s。
    完。
    tok 的 类型 为 TOK_正则。
    tok 的 值 为 s 的 源码 加 开始。
    tok 的 值长度 为 s 的 位置 减 开始。
完。

Scanner_扫描 接受 s：
    做 Scanner_跳过空白和注释 用 s。
    tok 为 新建 令牌。
    tok 的 行 为 s 的 行。
    tok 的 列 为 s 的 列。
    若 s 的 位置 大于或等于 s 的 源码长度：
        tok 的 类型 为 TOK_EOF。
        s 的 当前令牌 为 tok。
        返还。
    完。
    s 的 扫描令牌数 为 s 的 扫描令牌数 加 1。
    c 为 做 Scanner_字符 用 s。
    c2 为 做 Scanner_前看 用 s。
    若 做 Scanner_是标识符开始 用 c：
        做 Scanner_扫描标识符 用 s、tok。
        s 的 上一个令牌类型 为 tok 的 类型。
        s 的 当前令牌 为 tok。
        返还。
    完。
    若 c 大于或等于 0x30 并且 c 小于或等于 0x39：
        做 Scanner_扫描数字 用 s、tok。
        s 的 上一个令牌类型 为 tok 的 类型。
        s 的 当前令牌 为 tok。
        返还。
    完。
    若 c 等于 0x22 或者 c 等于 0x27：
        做 Scanner_扫描字符串 用 s、tok、c。
        s 的 上一个令牌类型 为 tok 的 类型。
        s 的 当前令牌 为 tok。
        返还。
    完。
    若 c 等于 0x60：
        做 Scanner_扫描模板 用 s、tok。
        s 的 上一个令牌类型 为 tok 的 类型。
        s 的 当前令牌 为 tok。
        返还。
    完。
    做 匹配 用 c。
    s 的 上一个令牌类型 为 tok 的 类型。
    s 的 当前令牌 为 tok。
完。

Scanner_下一个 接受 s：
    做 Scanner_扫描 用 s。
    返还 s 的 当前令牌。
完。

Scanner_期望 接受 s、期望类型：
    若 s 的 当前令牌 的 类型 不等于 期望类型：
        s 的 错误数 为 s 的 错误数 加 1。
        返还 假。
    完。
    做 Scanner_扫描 用 s。
    返还 真。
完。

Scanner_统计 接受 s：
    返还 一本字典：令牌数 对应 s 的 扫描令牌数、错误数 对应 s 的 错误数、行 对应 s 的 行、位置 对应 s 的 位置、模板深度 对应 s 的 模板深度。
完。

本章 收束。
