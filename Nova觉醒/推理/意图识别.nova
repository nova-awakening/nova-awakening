《意图识别》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·标准·原生列表》。

引用《..·标准·原生字符串》。

引用《.·语义嵌入》。

意图_问候 为 「问候」。

意图_告别 为 「告别」。

意图_提问 为 「提问」。

意图_命令 为 「命令」。

意图_闲聊 为 「闲聊」。

意图_求助 为 「求助」。

意图_反馈 为 「反馈」。

意图_投诉 为 「投诉」。

意图_查询 为 「查询」。

意图_确认 为 「确认」。

意图_否定 为 「否定」。

意图_未知 为 「未知」。

意图_最小置信度 为 30。

意图_历史最大 为 32。

意图规则 是 一种 事物，含 意图类型、关键词列表、权重。

意图结果 是 一种 事物，含 意图、置信度、次要意图、次要置信度、实体列表。

问候词 为 一组：「你好」、「hello」、「hi」、「嘘」、「早上好」、「晚上好」、「下午好」、「您好」、「你好啊」、「小年好」、「节日快乐」。

告别词 为 一组：「再见」、「拜拜」、「bye」、「晚安」、「下次见」、「告辞」、「我走了」、「我先走」。

求助词 为 一组：「帮忙」、「怎么」、「如何」、「帮我」、「help」、「能不能」、「请问」、「求助」、「教我」、「能帮」、「请帮」、「我不会」。

命令词 为 一组：「打开」、「关闭」、「运行」、「执行」、「创建」、「删除」、「搜索」、「启动」、「停止」、「安装」、「卸载」、「请你」、「麻烦你」、「帮我做」。

反馈词 为 一组：「好的」、「可以」、「不错」、「谢谢」、「感谢」、「很好」、「棒」、「赞」、「太好了」、「幾了几了」。

投诉词 为 一组：「不好」、「太慢」、「有问题」、「bug」、「出错」、「故障」、「失败」、「差」、「没法用」、「怎回事」。

查询词 为 一组：「查看」、「查询」、「显示」、「列出」、「状态」、「多少」、「什么是」、「哪个」、「能看看」、「告诉我」。

确认词 为 一组：「是」、「对」、「好」、「确认」、「同意」、「yes」、「ok」、「没问题」、「对的」、「就这么办」。

否定词 为 一组：「不」、「否」、「不要」、「取消」、「no」、「算了」、「不用」、「不行」、「不是的」。

可变 意图规则表 为 一组：（空）。

可变 意图识别_已初始化 为 假。

可变 意图_总识别数 为 0。

可变 意图_历史 为 一组：（空）。

意图识别_初始化 接受 零参：
    意图规则表 为 一组：（空）。
    做 意图_添加规则 用 意图_问候、问候词、80。
    做 意图_添加规则 用 意图_告别、告别词、80。
    做 意图_添加规则 用 意图_求助、求助词、70。
    做 意图_添加规则 用 意图_命令、命令词、75。
    做 意图_添加规则 用 意图_反馈、反馈词、60。
    做 意图_添加规则 用 意图_投诉、投诉词、65。
    做 意图_添加规则 用 意图_查询、查询词、70。
    做 意图_添加规则 用 意图_确认、确认词、85。
    做 意图_添加规则 用 意图_否定、否定词、85。
    意图识别_已初始化 为 真。
    返还 成功。
完。

意图_添加规则 接受 意图类型、关键词、权重：
    做 追加 用 意图规则表、新建 意图规则：意图类型 为 意图类型、关键词列表 为 关键词、权重 为 权重。
完。

意图_识别 接受 消息：
    结果 为 做 意图_识别详细 用 消息。
    返还 结果 的 意图。
完。

意图_识别详细 接受 消息：
    意图_总识别数 为 意图_总识别数 加 1。
    可变 最佳意图 为 意图_闲聊。
    可变 最佳分 为 0。
    可变 次佳意图 为 意图_未知。
    可变 次佳分 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 意图规则表：
        规则 为 意图规则表 的 第 i 项。
        分数 为 做 意图_计算匹配分 用 消息、规则。
        若 分数 大于 最佳分：
            次佳意图 为 最佳意图。
            次佳分 为 最佳分。
            最佳意图 为 规则 的 意图类型。
            最佳分 为 分数。
        否则若 分数 大于 次佳分：
            次佳意图 为 规则 的 意图类型。
            次佳分 为 分数。
        完。
        i 为 i 加 1。
    完。
    长度 为 做 字符串长度 用 消息。
    若 长度 大于 0：
        最后 为 做 字符串取字符 用 消息、长度 减 1。
        若 最后 等于 「?」 或者 最后 等于 「？」 或者 最后 等于 「吗」 或者 最后 等于 「呢」 或者 最后 等于 「吧」：
            若 最佳分 小于 60：
                最佳意图 为 意图_提问。
                最佳分 为 65。
            完。
        完。
        若 最后 等于 「!」 或者 最后 等于 「！」：
            若 最佳分 小于 70 并且 (最佳意图 等于 意图_闲聊 或者 最佳意图 等于 意图_未知)：
                最佳意图 为 意图_命令。
                最佳分 为 60。
            完。
        完。
    完。
    若 最佳分 小于 60 并且 做 语义嵌入_已初始化：
        消息向量 为 做 语义嵌入_文本草图 用 消息。
        若 消息向量 不等于 0：
            语义意图 为 做 意图_语义匹配 用 消息向量。
            若 语义意图 的 置信度 大于 最佳分：
                最佳意图 为 语义意图 的 意图。
                最佳分 为 语义意图 的 置信度。
            完。
        完。
    完。
    若 最佳分 小于 意图_最小置信度：
        最佳意图 为 意图_闲聊。
        最佳分 为 20。
    完。
    结果 为 新建 意图结果：意图 为 最佳意图、置信度 为 最佳分、次要意图 为 次佳意图、次要置信度 为 次佳分、实体列表 为 做 意图_提取实体 用 消息。
    做 意图_记录历史 用 结果。
    返还 结果。
完。

意图_计算匹配分 接受 消息、规则：
    可变 命中数 为 0。
    可变 总词数 为 做 计数 用 规则 的 关键词列表。
    若 总词数 等于 0：
        返还 0。
    完。
    可变 j 为 0。
    只要 j 小于 总词数：
        若 做 字符串包含 用 消息、规则 的 关键词列表 的 第 j 项：
            命中数 为 命中数 加 1。
        完。
        j 为 j 加 1。
    完。
    若 命中数 等于 0：
        返还 0。
    完。
    命中率 为 命中数 乘 100 除以 总词数。
    基础分 为 规则 的 权重 乘 命中数 除以 总词数。
    若 命中数 大于 1：
        返还 基础分 加 命中数 乘 5。
    完。
    返还 基础分。
完。

意图_提取实体 接受 消息：
    可变 实体 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 消息：
        字符 为 做 字符串取字符 用 消息、i。
        若 字符 大于或等于 「0」 并且 字符 小于或等于 「9」：
            可变 数字开始 为 i。
            只要 i 小于 做 字符串长度 用 消息 并且 做 字符串取字符 用 消息、i 大于或等于 「0」 并且 做 字符串取字符 用 消息、i 小于或等于 「9」：
                i 为 i 加 1。
            完。
            做 追加 用 实体、一组：「数字」、做 字符串子串 用 消息、数字开始、i。
            继续。
        完。
        i 为 i 加 1。
    完。
    返还 实体。
完。

意图_上下文修正 接受 结果、上一意图：
    若 上一意图 等于 意图_提问 并且 结果 的 意图 等于 意图_闲聊：
        若 结果 的 置信度 小于 40：
            返还 新建 意图结果：意图 为 意图_提问、置信度 为 45、次要意图 为 结果 的 意图、次要置信度 为 结果 的 置信度、实体列表 为 结果 的 实体列表。
        完。
    完。
    若 上一意图 等于 意图_命令 并且 (结果 的 意图 等于 意图_确认 或者 结果 的 意图 等于 意图_否定)：
        返还 新建 意图结果：意图 为 结果 的 意图、置信度 为 90、次要意图 为 上一意图、次要置信度 为 50、实体列表 为 结果 的 实体列表。
    完。
    返还 结果。
完。

意图_记录历史 接受 结果：
    做 追加 用 意图_历史、一组：结果 的 意图、结果 的 置信度。
    若 做 计数 用 意图_历史 大于 意图_历史最大：
        意图_历史 为 做 列表_切片 用 意图_历史、意图_历史最大 除以 2、做 计数 用 意图_历史。
    完。
完。

意图_获取上一个 接受 零参：
    若 做 计数 用 意图_历史 等于 0：
        返还 意图_未知。
    完。
    返还 意图_历史 的 第 做 计数 用 意图_历史 减 1 项 的 第 0 项。
完。

意图识别_状态 接受 零参：
    若 意图识别_已初始化：
        返还 「运行」。
    完。
    返还 「停止」。
完。

意图识别_统计 接受 零参：
    返还 一组：意图_总识别数、做 计数 用 意图_历史、做 计数 用 意图规则表。
完。

意图识别_重置历史 接受 零参：
    意图_历史 为 一组：（空）。
完。

可变 意图原型表 为 一组：（空）。

可变 语义嵌入_就绪 为 假。

语义嵌入_已初始化 接受 零参：
    返还 语义嵌入_就绪。
完。

意图_初始化语义原型 接受 零参：
    意图原型表 为 一组：（空）。
    意图列表 为 一组：意图_问候、意图_告别、意图_提问、意图_命令、意图_闲聊、意图_求助、意图_反馈、意图_投诉、意图_查询、意图_确认、意图_否定。
    关键词组 为 一组：问候词、告别词、求助词、命令词、一组：（空）、求助词、反馈词、投诉词、查询词、确认词、否定词。
    可变 i 为 0。
    只要 i 小于 做 计数 用 意图列表：
        词组 为 关键词组 的 第 i 项。
        若 做 计数 用 词组 等于 0：
            做 追加 用 意图原型表、一组：意图列表 的 第 i 项、0。
            i 为 i 加 1。
            继续。
        完。
        可变 原型 为 做 语义嵌入_文本草图 用 词组 的 第 0 项。
        可变 j 为 1。
        只要 j 小于 做 计数 用 词组：
            词向量 为 做 语义嵌入_文本草图 用 词组 的 第 j 项。
            若 词向量 不等于 0 并且 原型 不等于 0：
                原型 为 做 向量_加 用 原型、词向量。
            完。
            j 为 j 加 1。
        完。
        若 原型 不等于 0：
            原型 为 做 向量_归一化 用 原型。
        完。
        做 追加 用 意图原型表、一组：意图列表 的 第 i 项、原型。
        i 为 i 加 1。
    完。
    语义嵌入_就绪 为 真。
完。

意图_语义匹配 接受 消息向量：
    可变 最佳意图 为 意图_闲聊。
    可变 最佳分 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 意图原型表：
        原型 为 意图原型表 的 第 i 项 的 第 1 项。
        若 原型 不等于 0：
            相似度 为 做 语义嵌入_草图相似度 用 消息向量、原型。
            置信度 为 相似度 除以 10。
            若 置信度 大于 最佳分：
                最佳意图 为 意图原型表 的 第 i 项 的 第 0 项。
                最佳分 为 置信度。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 新建 意图结果：意图 为 最佳意图、置信度 为 最佳分、次要意图 为 意图_未知、次要置信度 为 0、实体列表 为 一组：（空）。
完。

本章 收束。
