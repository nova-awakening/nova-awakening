《模型持久化》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《.·定点Q算术》。

引用《..·标准·文件》。

引用《.·权重持久》。

引用《..·感知·内核直连层》。

引用《.·行为预测器》。

引用《..·中枢·内存初始化》。

AI总线_保存模型 接受 bus：
    若 bus 等于 0 或者 bus 的 模型目录 等于 0：
        返还。
    完。
    做 Nova多层感知器_保存 用 bus 的 布局预测网络、做 字符串_拼接 用 bus 的 模型目录、「layout.novamodel」。
    做 Nova多层感知器_保存 用 bus 的 注意力网络、做 字符串_拼接 用 bus 的 模型目录、「Nova注意力.novamodel」。
    做 Nova多层感知器_保存 用 bus 的 语义分类网络、做 字符串_拼接 用 bus 的 模型目录、「semantic.novamodel」。
    做 Nova多层感知器_保存 用 bus 的 渲染质量网络、做 字符串_拼接 用 bus 的 模型目录、「quality.novamodel」。
    做 Nova多层感知器_保存 用 bus 的 资源优先级网络、做 字符串_拼接 用 bus 的 模型目录、「priority.novamodel」。
完。

AI总线_加载模型 接受 bus：
    若 bus 等于 0 或者 bus 的 模型目录 等于 0：
        返还。
    完。
    布局模型 为 做 Nova多层感知器_加载 用 做 字符串_拼接 用 bus 的 模型目录、「layout.novamodel」。
    若 布局模型 不等于 0：
        bus 的 布局预测网络 为 布局模型。
    完。
    注意力模型 为 做 Nova多层感知器_加载 用 做 字符串_拼接 用 bus 的 模型目录、「Nova注意力.novamodel」。
    若 注意力模型 不等于 0：
        bus 的 注意力网络 为 注意力模型。
    完。
    语义模型 为 做 Nova多层感知器_加载 用 做 字符串_拼接 用 bus 的 模型目录、「semantic.novamodel」。
    若 语义模型 不等于 0：
        bus 的 语义分类网络 为 语义模型。
    完。
    质量模型 为 做 Nova多层感知器_加载 用 做 字符串_拼接 用 bus 的 模型目录、「quality.novamodel」。
    若 质量模型 不等于 0：
        bus 的 渲染质量网络 为 质量模型。
    完。
    优先级模型 为 做 Nova多层感知器_加载 用 做 字符串_拼接 用 bus 的 模型目录、「priority.novamodel」。
    若 优先级模型 不等于 0：
        bus 的 资源优先级网络 为 优先级模型。
    完。
完。

AI总线_更新带宽 接受 bus、带宽KB：
    若 bus 等于 0：
        返还。
    完。
    bus 的 网络带宽 为 带宽KB。
完。

AI总线_更新图处负载 接受 bus、负载百分比：
    若 bus 等于 0：
        返还。
    完。
    bus 的 图处负载 为 负载百分比。
完。

AI总线_更新电池 接受 bus、电量：
    若 bus 等于 0：
        返还。
    完。
    bus 的 电池电量 为 电量。
    若 电量 小于 20：
        bus 的 AI推理预算 为 500。
    否则若 电量 小于 40：
        bus 的 AI推理预算 为 1000。
    完。
完。

AI诊断信息 是 一种 事物，含 FPS、帧耗时、AI推理耗时、AI推理次数、行为预测准确率、滚动速度、注视点x、注视点y、质量偏移、学习步数、模型数量。

AI总线_获取诊断 接受 bus：
    info 为 做 堆_分配 用 AI诊断信息。
    若 bus 等于 0：
        返还 info。
    完。
    info 的 FPS 为 bus 的 当前FPS。
    info 的 帧耗时 为 bus 的 上帧耗时。
    info 的 AI推理耗时 为 bus 的 AI实际耗时。
    info 的 AI推理次数 为 bus 的 推理次数。
    info 的 行为预测准确率 为 做 行为预测器_准确率 用 bus 的 行为预测器。
    info 的 滚动速度 为 bus 的 滚动速度。
    info 的 注视点x 为 bus 的 质量分配器 的 注视点 的 注视x 右移 Q_SHIFT。
    info 的 注视点y 为 bus 的 质量分配器 的 注视点 的 注视y 右移 Q_SHIFT。
    info 的 质量偏移 为 bus 的 质量分配器 的 质量偏移。
    info 的 学习步数 为 bus 的 学习步数。
    info 的 模型数量 为 5。
    返还 info。
完。

NOVA_MODEL_MAGIC 为 0x4e4f5641。

NOVA_MODEL_VERSION 为 3。

NOVA_MODEL_HEADER_SIZE 为 64。

NOVA_MODEL_MAX_LAYERS 为 32。

NOVA_MODEL_MAX_SIZE 为 1048576。

NOVA_DELTA_THRESHOLD 为 5。

保存模式_全量 为 0。

保存模式_增量 为 1。

保存模式_快照 为 2。

可变 持久化_保存次数 为 0。

可变 持久化_加载次数 为 0。

可变 持久化_增量保存次数 为 0。

可变 持久化_校验失败数 为 0。

可变 持久化_总字节写入 为 0。

可变 持久化_总字节读取 为 0。

可变 持久化_AI优化命中 为 0。

可变 持久化_AI优化未中 为 0。

模型文件头 是 一种 事物，含 魔数、版本、层数、参数总量、数据大小、CRC32、时间戳、保存模式、推理次数、训练步数、损失值、网络名称。

层头 是 一种 事物，含 行数、列数、偏置大小、激活类型。

增量记录 是 一种 事物，含 层索引、行、列、新值。

可变 CRC32_表已初始化 为 假。

可变 CRC32_表 为 空。

CRC32_初始化表 接受 零参：
    若 CRC32_表已初始化：
        返还。
    完。
    CRC32_表 为 做 数组_创建 用 256、0。
    可变。
    i 为 0。
    只要 i 小于 256：
        可变。
        crc 为 i。
        可变。
        j 为 0。
        只要 j 小于 8：
            若 crc 位与 1：
                crc 为 crc 右移 1 位异或 0xEDB88320。
            否则：
                crc 为 crc 右移 1。
            完。
            j 为 j 加 1。
        完。
        CRC32_表 的 第 i 项 为 crc。
        i 为 i 加 1。
    完。
    CRC32_表已初始化 为 真。
完。

CRC32_计算 接受 数据、长度：
    做 CRC32_初始化表。
    可变。
    crc 为 0xFFFFFFFF。
    可变。
    i 为 0。
    只要 i 小于 长度：
        byte 为 做 内存_读8 用 数据 加 i。
        crc 为 CRC32_表 的 第 (crc 位异或 byte) 位与 0xFF 项 位异或 crc 右移 8。
        i 为 i 加 1。
    完。
    返还 crc 位异或 0xFFFFFFFF。
完。

模型_计算参数量 接受 网络：
    若 网络 等于 0：
        返还 0。
    完。
    可变。
    总量 为 0。
    可变。
    i 为 0。
    只要 i 小于 网络 的 层数：
        层 为 网络 的 层 的 第 i 项。
        总量 为 总量 加 层 的 权重 的 行数 乘 层 的 权重 的 列数。
        总量 为 总量 加 层 的 偏置 的 列数。
        i 为 i 加 1。
    完。
    返还 总量。
完。

模型_计算数据大小 接受 网络：
    返还 做 模型_计算参数量 用 网络 乘 4。
完。

模型_序列化到缓冲区 接受 网络、缓冲区：
    若 网络 等于 0 或者 缓冲区 等于 0：
        返还 0。
    完。
    可变。
    偏移 为 0。
    可变。
    i 为 0。
    只要 i 小于 网络 的 层数：
        层 为 网络 的 层 的 第 i 项。
        做 内存_写32 用 缓冲区 加 偏移、层 的 权重 的 行数。
        偏移 为 偏移 加 4。
        做 内存_写32 用 缓冲区 加 偏移、层 的 权重 的 列数。
        偏移 为 偏移 加 4。
        做 内存_写32 用 缓冲区 加 偏移、层 的 偏置 的 列数。
        偏移 为 偏移 加 4。
        做 内存_写32 用 缓冲区 加 偏移、层 的 激活类型。
        偏移 为 偏移 加 4。
        可变。
        r 为 0。
        只要 r 小于 层 的 权重 的 行数：
            可变。
            c 为 0。
            只要 c 小于 层 的 权重 的 列数：
                做 内存_写32 用 缓冲区 加 偏移、做 Tensor_读 用 层 的 权重、r、c。
                偏移 为 偏移 加 4。
                c 为 c 加 1。
            完。
            r 为 r 加 1。
        完。
        可变。
        b 为 0。
        只要 b 小于 层 的 偏置 的 列数：
            做 内存_写32 用 缓冲区 加 偏移、做 Tensor_读 用 层 的 偏置、0、b。
            偏移 为 偏移 加 4。
            b 为 b 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 偏移。
完。

模型_全量保存 接受 网络、文件路径：
    若 网络 等于 0：
        返还 假。
    完。
    持久化_保存次数 为 持久化_保存次数 加 1。
    数据大小 为 做 模型_计算数据大小 用 网络 加 网络 的 层数 乘 16。
    若 数据大小 大于 NOVA_MODEL_MAX_SIZE：
        返还 假。
    完。
    总大小 为 NOVA_MODEL_HEADER_SIZE 加 数据大小。
    buf 为 做 堆_分配 用 总大小。
    若 buf 等于 0：
        返还 假。
    完。
    做 内存_写32 用 buf 加 0、NOVA_MODEL_MAGIC。
    做 内存_写32 用 buf 加 4、NOVA_MODEL_VERSION。
    做 内存_写32 用 buf 加 8、网络 的 层数。
    做 内存_写32 用 buf 加 12、做 模型_计算参数量 用 网络。
    做 内存_写32 用 buf 加 16、数据大小。
    做 内存_写32 用 buf 加 24、做 时间_获取微秒。
    做 内存_写8 用 buf 加 32、保存模式_全量。
    写入量 为 做 模型_序列化到缓冲区 用 网络、buf 加 NOVA_MODEL_HEADER_SIZE。
    crc 为 做 CRC32_计算 用 buf 加 NOVA_MODEL_HEADER_SIZE、写入量。
    做 内存_写32 用 buf 加 20、crc。
    fd 为 做 文件_打开 用 文件路径、文件_创建 位或 文件_写。
    若 fd 小于 0：
        做 堆_释放 用 buf。
        返还 假。
    完。
    做 文件_写 用 fd、buf、总大小。
    做 文件_关闭 用 fd。
    做 堆_释放 用 buf。
    持久化_总字节写入 为 持久化_总字节写入 加 总大小。
    返还 真。
完。

模型_从缓冲区反序列化 接受 网络、缓冲区、层数：
    若 网络 等于 0 或者 缓冲区 等于 0：
        返还 假。
    完。
    可变。
    偏移 为 0。
    可变。
    i 为 0。
    只要 i 小于 层数：
        rows 为 做 内存_读32 用 缓冲区 加 偏移。
        偏移 为 偏移 加 4。
        cols 为 做 内存_读32 用 缓冲区 加 偏移。
        偏移 为 偏移 加 4。
        bias_sz 为 做 内存_读32 用 缓冲区 加 偏移。
        偏移 为 偏移 加 4。
        act 为 做 内存_读32 用 缓冲区 加 偏移。
        偏移 为 偏移 加 4。
        可变。
        r 为 0。
        只要 r 小于 rows：
            可变。
            c 为 0。
            只要 c 小于 cols：
                v 为 做 内存_读32 用 缓冲区 加 偏移。
                做 Tensor_写 用 网络 的 层 的 第 i 项 的 权重、r、c、v。
                偏移 为 偏移 加 4。
                c 为 c 加 1。
            完。
            r 为 r 加 1。
        完。
        可变。
        b 为 0。
        只要 b 小于 bias_sz：
            v 为 做 内存_读32 用 缓冲区 加 偏移。
            做 Tensor_写 用 网络 的 层 的 第 i 项 的 偏置、0、b、v。
            偏移 为 偏移 加 4。
            b 为 b 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

模型_全量加载 接受 网络、文件路径：
    若 网络 等于 0：
        返还 假。
    完。
    持久化_加载次数 为 持久化_加载次数 加 1。
    fd 为 做 文件_打开 用 文件路径、文件_读。
    若 fd 小于 0：
        返还 假。
    完。
    文件大小 为 做 文件_获取大小 用 fd。
    若 文件大小 小于 NOVA_MODEL_HEADER_SIZE 或者 文件大小 大于 NOVA_MODEL_MAX_SIZE：
        做 文件_关闭 用 fd。
        返还 假。
    完。
    buf 为 做 堆_分配 用 文件大小。
    若 buf 等于 0：
        做 文件_关闭 用 fd。
        返还 假。
    完。
    做 文件_读 用 fd、buf、文件大小。
    做 文件_关闭 用 fd。
    若 做 内存_读32 用 buf 不等于 NOVA_MODEL_MAGIC：
        做 堆_释放 用 buf。
        返还 假。
    完。
    ver 为 做 内存_读32 用 buf 加 4。
    若 ver 大于 NOVA_MODEL_VERSION：
        做 堆_释放 用 buf。
        返还 假。
    完。
    层数 为 做 内存_读32 用 buf 加 8。
    数据大小 为 做 内存_读32 用 buf 加 16。
    存储CRC 为 做 内存_读32 用 buf 加 20。
    计算CRC 为 做 CRC32_计算 用 buf 加 NOVA_MODEL_HEADER_SIZE、数据大小。
    若 计算CRC 不等于 存储CRC：
        持久化_校验失败数 为 持久化_校验失败数 加 1。
        做 堆_释放 用 buf。
        返还 假。
    完。
    做 模型_从缓冲区反序列化 用 网络、buf 加 NOVA_MODEL_HEADER_SIZE、层数。
    持久化_总字节读取 为 持久化_总字节读取 加 文件大小。
    做 堆_释放 用 buf。
    返还 真。
完。

可变 快照_权重 为 空。

模型_创建快照 接受 网络：
    若 网络 等于 0：
        返还。
    完。
    sz 为 做 模型_计算数据大小 用 网络。
    若 快照_权重 不等于 空：
        做 堆_释放 用 快照_权重。
    完。
    快照_权重 为 做 堆_分配 用 sz。
    若 快照_权重 等于 空：
        返还。
    完。
    做 模型_序列化到缓冲区 用 网络、快照_权重。
完。

模型_增量保存 接受 网络、文件路径：
    若 网络 等于 0 或者 快照_权重 等于 空：
        返还 做 模型_全量保存 用 网络、文件路径。
    完。
    持久化_增量保存次数 为 持久化_增量保存次数 加 1。
    可变。
    delta列表 为 一组：（空）。
    可变。
    快照偏移 为 0。
    可变。
    i 为 0。
    只要 i 小于 网络 的 层数：
        层 为 网络 的 层 的 第 i 项。
        快照偏移 为 快照偏移 加 16。
        可变。
        r 为 0。
        只要 r 小于 层 的 权重 的 行数：
            可变。
            c 为 0。
            只要 c 小于 层 的 权重 的 列数：
                新值 为 做 Tensor_读 用 层 的 权重、r、c。
                旧值 为 做 内存_读32 用 快照_权重 加 快照偏移。
                diff 为 新值 减 旧值。
                若 diff 小于 0：
                    diff 为 负 diff。
                完。
                若 diff 大于 NOVA_DELTA_THRESHOLD：
                    做 数组_推入 用 delta列表、新建 增量记录：层索引 为 i、行 为 r、列 为 c、新值 为 新值。
                完。
                快照偏移 为 快照偏移 加 4。
                c 为 c 加 1。
            完。
            r 为 r 加 1。
        完。
        可变。
        b 为 0。
        只要 b 小于 层 的 偏置 的 列数：
            快照偏移 为 快照偏移 加 4。
            b 为 b 加 1。
        完。
        i 为 i 加 1。
    完。
    若 做 数组_长度 用 delta列表 大于 做 模型_计算参数量 用 网络 除以 2：
        做 模型_创建快照 用 网络。
        返还 做 模型_全量保存 用 网络、文件路径。
    完。
    delta数 为 做 数组_长度 用 delta列表。
    总大小 为 NOVA_MODEL_HEADER_SIZE 加 delta数 乘 16。
    buf 为 做 堆_分配 用 总大小。
    若 buf 等于 0：
        返还 假。
    完。
    做 内存_写32 用 buf 加 0、NOVA_MODEL_MAGIC。
    做 内存_写32 用 buf 加 4、NOVA_MODEL_VERSION。
    做 内存_写32 用 buf 加 8、网络 的 层数。
    做 内存_写32 用 buf 加 12、delta数。
    做 内存_写32 用 buf 加 16、delta数 乘 16。
    做 内存_写8 用 buf 加 32、保存模式_增量。
    可变。
    off 为 NOVA_MODEL_HEADER_SIZE。
    可变。
    di 为 0。
    只要 di 小于 delta数：
        rec 为 delta列表 的 第 di 项。
        做 内存_写32 用 buf 加 off、rec 的 层索引。
        做 内存_写32 用 buf 加 off 加 4、rec 的 行。
        做 内存_写32 用 buf 加 off 加 8、rec 的 列。
        做 内存_写32 用 buf 加 off 加 12、rec 的 新值。
        off 为 off 加 16。
        di 为 di 加 1。
    完。
    crc 为 做 CRC32_计算 用 buf 加 NOVA_MODEL_HEADER_SIZE、delta数 乘 16。
    做 内存_写32 用 buf 加 20、crc。
    fd 为 做 文件_打开 用 文件路径 加 「.delta」、文件_创建 位或 文件_写。
    若 fd 大于或等于 0：
        做 文件_写 用 fd、buf、总大小。
        做 文件_关闭 用 fd。
    完。
    做 堆_释放 用 buf。
    做 模型_创建快照 用 网络。
    持久化_总字节写入 为 持久化_总字节写入 加 总大小。
    返还 真。
完。

模型_AI_应否保存 接受 bus、网络名称：
    特征 为 做 Tensor_创建 用 1、4。
    做 Tensor_写 用 特征、0、0、bus 的 推理次数 乘 1.0。
    做 Tensor_写 用 特征、0、1、bus 的 学习步数 乘 1.0。
    做 Tensor_写 用 特征、0、2、持久化_总字节写入 乘 1.0。
    做 Tensor_写 用 特征、0、3、做 模型_计算参数量 用 网络名称 乘 1.0。
    结果 为 做 AI总线_推理 用 「model_save_strategy」、特征。
    可变。
    应保存 为 假。
    若 结果 不等于 空：
        urgency 为 做 Tensor_读 用 结果、0、0。
        若 urgency 大于 0.6：
            应保存 为 真。
            持久化_AI优化命中 为 持久化_AI优化命中 加 1。
        否则：
            持久化_AI优化未中 为 持久化_AI优化未中 加 1。
        完。
        做 Tensor_释放 用 结果。
    完。
    做 Tensor_释放 用 特征。
    返还 应保存。
完。

持久化_诊断报告 接受 零参：
    可变。
    报告 为 「[NAMP诊断报告]\n」。
    报告 为 报告 加 「保存次数: 」 加 持久化_保存次数 加 「\n」。
    报告 为 报告 加 「加载次数: 」 加 持久化_加载次数 加 「\n」。
    报告 为 报告 加 「增量保存: 」 加 持久化_增量保存次数 加 「\n」。
    报告 为 报告 加 「校验失败: 」 加 持久化_校验失败数 加 「\n」。
    报告 为 报告 加 「总写入: 」 加 持久化_总字节写入 加 「 字节\n」。
    报告 为 报告 加 「总读取: 」 加 持久化_总字节读取 加 「 字节\n」。
    报告 为 报告 加 「AI命中: 」 加 持久化_AI优化命中 加 「\n」。
    报告 为 报告 加 「AI未中: 」 加 持久化_AI优化未中 加 「\n」。
    返还 报告。
完。

字符串_拼接 接受 a、b：
    返还 a 加 b。
完。

可变 _谐振检查点_间隔 为 100。

可变 _谐振检查点_保存次数 为 0。

可变 _谐振检查点_上次损失 为 0。

谐振检查点_应该保存 接受 当前步、当前损失：
    若 当前步 取余于 _谐振检查点_间隔 不等于 0：
        返还 假。
    完。
    若 _谐振检查点_上次损失 大于 0：
        下降 为 _谐振检查点_上次损失 减 当前损失。
        若 下降 大于 _谐振检查点_上次损失 除以 10：
            _谐振检查点_保存次数 为 _谐振检查点_保存次数 加 1。
            _谐振检查点_上次损失 为 当前损失。
            返还 真。
        完。
    完。
    _谐振检查点_保存次数 为 _谐振检查点_保存次数 加 1。
    _谐振检查点_上次损失 为 当前损失。
    返还 真。
完。

模型持久化_完整统计 接受 零参：
    返还 一组：「谐振保存间隔」、_谐振检查点_间隔、「谐振保存次数」、_谐振检查点_保存次数。
完。

本章 收束。
