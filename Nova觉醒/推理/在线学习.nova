《在线学习》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《.·定点Q算术》。

引用《..·感知·内核直连层》。

引用《.·权重持久》。

学习率_默认 为 10。

动量_默认 为 922。

Nova梯度下降优化器 是 一种 事物，含 学习率、动量、速度列表、速度数量。

Nova梯度下降_创建 接受 学习率、动量：
    opt 为 做 堆_分配 用 Nova梯度下降优化器。
    opt 的 学习率 为 学习率。
    opt 的 动量 为 动量。
    opt 的 速度列表 为 做 堆_分配_数组 用 16 乘 8。
    opt 的 速度数量 为 0。
    返还 opt。
完。

Nova梯度下降_注册层 接受 opt、层：
    若 opt 的 速度数量 大于或等于 16：
        返还。
    完。
    v 为 做 Tensor_创建 用 层 的 输入维度、层 的 输出维度。
    做 内存_写64 用 opt 的 速度列表 加 opt 的 速度数量 乘 8、v。
    opt 的 速度数量 为 opt 的 速度数量 加 1。
完。

损失_MSE 接受 预测、目标：
    总 为 预测 的 行数 乘 预测 的 列数。
    可变 累加 为 0。
    可变 i 为 0。
    只要 i 小于 总：
        p 为 做 内存_读32有符号 用 预测 的 数据 加 i 乘 4。
        t 为 做 内存_读32有符号 用 目标 的 数据 加 i 乘 4。
        差 为 p 减 t。
        累加 为 累加 加 做 Q乘 用 差、差。
        i 为 i 加 1。
    完。
    返还 做 Q除 用 累加、总 乘 Q_ONE。
完。

损失_MSE_梯度 接受 预测、目标：
    总 为 预测 的 行数 乘 预测 的 列数。
    梯度 为 做 Tensor_创建 用 预测 的 行数、预测 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        p 为 做 内存_读32有符号 用 预测 的 数据 加 i 乘 4。
        t 为 做 内存_读32有符号 用 目标 的 数据 加 i 乘 4。
        g 为 做 Q除 用 2 乘 (p 减 t)、总 乘 Q_ONE。
        做 内存_写32 用 梯度 的 数据 加 i 乘 4、g。
        i 为 i 加 1。
    完。
    返还 梯度。
完。

损失_交叉熔 接受 预测、目标：
    总 为 预测 的 行数 乘 预测 的 列数。
    可变 累加 为 0。
    可变 i 为 0。
    只要 i 小于 总：
        p 为 做 内存_读32有符号 用 预测 的 数据 加 i 乘 4。
        t 为 做 内存_读32有符号 用 目标 的 数据 加 i 乘 4。
        若 p 小于 1：
            p 为 1。
        完。
        x1 为 p 减 Q_ONE。
        对数 为 x1 减 (做 Q乘 用 x1、x1 右移 1)。
        累加 为 累加 减 做 Q乘 用 t、对数。
        i 为 i 加 1。
    完。
    返还 做 Q除 用 累加、预测 的 行数 乘 Q_ONE。
完。

可变 NOGL_更新次数 为 0。

可变 NOGL_Adam次数 为 0。

可变 NOGL_RMSProp次数 为 0。

可变 NOGL_调度次数 为 0。

可变 NOGL_正则化次数 为 0。

可变 NOGL_随机丢弃次数 为 0。

可变 NOGL_AI元学习次数 为 0。

Q_ONE 为 1024。

Q_HALF 为 512。

Q_EPSILON 为 1。

Q乘 接受 a、b：
    返还 a 乘 b 右移 10。
完。

Q平方根 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 r 为 x。
    可变 i 为 0。
    只要 i 小于 16：
        r 为 r 加 做 Q除 用 x、r 右移 1。
        i 为 i 加 1。
    完。
    返还 r。
完。

Nova自适应矩估计优化器 是 一种 事物，含 学习率、beta1、beta2、m列表、v列表、层数、步数。

自适应矩估计_创建 接受 lr、b1、b2：
    opt 为 做 堆_分配 用 Nova自适应矩估计优化器。
    opt 的 学习率 为 lr。
    opt 的 beta1 为 b1。
    opt 的 beta2 为 b2。
    opt 的 m列表 为 做 堆_分配_数组 用 32 乘 8。
    opt 的 v列表 为 做 堆_分配_数组 用 32 乘 8。
    opt 的 层数 为 0。
    opt 的 步数 为 0。
    返还 opt。
完。

自适应矩估计_注册层 接受 opt、层：
    若 opt 的 层数 大于或等于 32：
        返还。
    完。
    m 为 做 Tensor_创建 用 层 的 输入维度、层 的 输出维度。
    v 为 做 Tensor_创建 用 层 的 输入维度、层 的 输出维度。
    做 内存_写64 用 opt 的 m列表 加 opt 的 层数 乘 8、m。
    做 内存_写64 用 opt 的 v列表 加 opt 的 层数 乘 8、v。
    opt 的 层数 为 opt 的 层数 加 1。
完。

自适应矩估计_更新 接受 opt、层索引、权重、梯度：
    NOGL_Adam次数 为 NOGL_Adam次数 加 1。
    opt 的 步数 为 opt 的 步数 加 1。
    若 层索引 大于或等于 opt 的 层数：
        返还。
    完。
    m_t 为 做 内存_读64 用 opt 的 m列表 加 层索引 乘 8。
    v_t 为 做 内存_读64 用 opt 的 v列表 加 层索引 乘 8。
    总 为 权重 的 行数 乘 权重 的 列数。
    b1 为 opt 的 beta1。
    b2 为 opt 的 beta2。
    b1_t 为 做 Q快速幂整数 用 b1、opt 的 步数。
    b2_t 为 做 Q快速幂整数 用 b2、opt 的 步数。
    m_校正 为 做 Q除 用 Q_ONE、Q_ONE 减 b1_t。
    v_校正 为 做 Q除 用 Q_ONE、Q_ONE 减 b2_t。
    可变 i 为 0。
    只要 i 小于 总：
        g 为 做 内存_读32有符号 用 梯度 的 数据 加 i 乘 4。
        m_old 为 做 内存_读32有符号 用 m_t 的 数据 加 i 乘 4。
        m_new 为 做 Q乘 用 b1、m_old 加 做 Q乘 用 Q_ONE 减 b1、g。
        做 内存_写32 用 m_t 的 数据 加 i 乘 4、m_new。
        v_old 为 做 内存_读32有符号 用 v_t 的 数据 加 i 乘 4。
        v_new 为 做 Q乘 用 b2、v_old 加 做 Q乘 用 Q_ONE 减 b2、做 Q乘 用 g、g。
        做 内存_写32 用 v_t 的 数据 加 i 乘 4、v_new。
        m_hat 为 做 Q乘 用 m_new、m_校正。
        v_hat 为 做 Q乘 用 v_new、v_校正。
        denom 为 做 Q平方根 用 v_hat 加 Q_EPSILON。
        delta 为 做 Q乘 用 opt 的 学习率、做 Q除 用 m_hat、denom。
        w 为 做 内存_读32有符号 用 权重 的 数据 加 i 乘 4。
        做 内存_写32 用 权重 的 数据 加 i 乘 4、w 减 delta。
        i 为 i 加 1。
    完。
完。

NovaRMSProp优化器 是 一种 事物，含 学习率、衰减率、sq_列表、层数。

均方根传播_创建 接受 lr、decay：
    opt 为 做 堆_分配 用 NovaRMSProp优化器。
    opt 的 学习率 为 lr。
    opt 的 衰减率 为 decay。
    opt 的 sq_列表 为 做 堆_分配_数组 用 32 乘 8。
    opt 的 层数 为 0。
    返还 opt。
完。

均方根传播_注册层 接受 opt、层：
    若 opt 的 层数 大于或等于 32：
        返还。
    完。
    sq 为 做 Tensor_创建 用 层 的 输入维度、层 的 输出维度。
    做 内存_写64 用 opt 的 sq_列表 加 opt 的 层数 乘 8、sq。
    opt 的 层数 为 opt 的 层数 加 1。
完。

均方根传播_更新 接受 opt、层索引、权重、梯度：
    NOGL_RMSProp次数 为 NOGL_RMSProp次数 加 1。
    若 层索引 大于或等于 opt 的 层数：
        返还。
    完。
    sq 为 做 内存_读64 用 opt 的 sq_列表 加 层索引 乘 8。
    总 为 权重 的 行数 乘 权重 的 列数。
    rho 为 opt 的 衰减率。
    可变 i 为 0。
    只要 i 小于 总：
        g 为 做 内存_读32有符号 用 梯度 的 数据 加 i 乘 4。
        sq_old 为 做 内存_读32有符号 用 sq 的 数据 加 i 乘 4。
        sq_new 为 做 Q乘 用 rho、sq_old 加 做 Q乘 用 Q_ONE 减 rho、做 Q乘 用 g、g。
        做 内存_写32 用 sq 的 数据 加 i 乘 4、sq_new。
        denom 为 做 Q平方根 用 sq_new 加 Q_EPSILON。
        delta 为 做 Q乘 用 opt 的 学习率、做 Q除 用 g、denom。
        w 为 做 内存_读32有符号 用 权重 的 数据 加 i 乘 4。
        做 内存_写32 用 权重 的 数据 加 i 乘 4、w 减 delta。
        i 为 i 加 1。
    完。
完。

NOGL_调度_固定 为 0。

NOGL_调度_衰减 为 1。

NOGL_调度_余弦退火 为 2。

NOGL_调度_周期 为 3。

学习率调度器 是 一种 事物，含 策略、初始LR、最小LR、衰减因子、周期步数、当前步。

调度器_创建 接受 策略、初始LR、最小LR：
    s 为 做 堆_分配 用 学习率调度器。
    s 的 策略 为 策略。
    s 的 初始LR 为 初始LR。
    s 的 最小LR 为 最小LR。
    s 的 衰减因子 为 1013。
    s 的 周期步数 为 100。
    s 的 当前步 为 0。
    返还 s。
完。

调度器_步进_v2 接受 s：
    NOGL_调度次数 为 NOGL_调度次数 加 1。
    s 的 当前步 为 s 的 当前步 加 1。
    若 s 的 策略 等于 NOGL_调度_固定：
        返还 s 的 初始LR。
    完。
    若 s 的 策略 等于 NOGL_调度_衰减：
        衰减_t 为 做 Q快速幂整数 用 s 的 衰减因子、s 的 当前步。
        lr 为 做 Q乘 用 s 的 初始LR、衰减_t。
        若 lr 小于 s 的 最小LR：
            lr 为 s 的 最小LR。
        完。
        返还 lr。
    完。
    若 s 的 策略 等于 NOGL_调度_余弦退火：
        t 为 s 的 当前步 取余于 s 的 周期步数 乘 Q_ONE 除以 s 的 周期步数。
        FP 为 10000。
        FP_PI 为 31416。
        pt 为 t 乘 FP_PI 除以 FP。
        pt2 为 pt 乘 pt 除以 FP。
        pt4 为 pt2 乘 pt2 除以 FP。
        cos_val 为 (FP 减 pt2 除以 2 加 pt4 除以 24) 乘 Q_ONE 除以 FP。
        range 为 s 的 初始LR 减 s 的 最小LR。
        返还 s 的 最小LR 加 做 Q乘 用 range、(Q_ONE 加 cos_val) 除以 2。
    完。
    若 s 的 策略 等于 NOGL_调度_周期：
        half 为 s 的 周期步数 除以 2。
        pos 为 s 的 当前步 取余于 s 的 周期步数。
        若 pos 小于 half：
            返还 s 的 最小LR 加 (s 的 初始LR 减 s 的 最小LR) 乘 pos 除以 half。
        完。
        返还 s 的 初始LR 减 (s 的 初始LR 减 s 的 最小LR) 乘 (pos 减 half) 除以 half。
    完。
    返还 s 的 初始LR。
完。

NOGL_L2正则化 接受 权重、lambda：
    NOGL_正则化次数 为 NOGL_正则化次数 加 1。
    总 为 权重 的 行数 乘 权重 的 列数。
    可变 penalty 为 0。
    可变 i 为 0。
    只要 i 小于 总：
        w 为 做 内存_读32有符号 用 权重 的 数据 加 i 乘 4。
        penalty 为 penalty 加 做 Q乘 用 w、w。
        i 为 i 加 1。
    完。
    返还 做 Q乘 用 lambda、penalty。
完。

NOGL_L1正则化 接受 权重、lambda：
    NOGL_正则化次数 为 NOGL_正则化次数 加 1。
    总 为 权重 的 行数 乘 权重 的 列数。
    可变 penalty 为 0。
    可变 i 为 0。
    只要 i 小于 总：
        w 为 做 内存_读32有符号 用 权重 的 数据 加 i 乘 4。
        若 w 小于 0：
            penalty 为 penalty 减 w。
        否则：
            penalty 为 penalty 加 w。
        完。
        i 为 i 加 1。
    完。
    返还 做 Q乘 用 lambda、penalty。
完。

NOGL_L2梯度 接受 权重、梯度、lambda：
    总 为 权重 的 行数 乘 权重 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        w 为 做 内存_读32有符号 用 权重 的 数据 加 i 乘 4。
        g 为 做 内存_读32有符号 用 梯度 的 数据 加 i 乘 4。
        做 内存_写32 用 梯度 的 数据 加 i 乘 4、g 加 做 Q乘 用 2 乘 lambda、w。
        i 为 i 加 1。
    完。
完。

可变 NOGL_随机种子 为 12345。

NOGL_随机 接受 零参：
    NOGL_随机种子 为 NOGL_随机种子 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    返还 NOGL_随机种子。
完。

NOGL_随机丢弃前向 接受 输入、概率Q10、训练中：
    NOGL_随机丢弃次数 为 NOGL_随机丢弃次数 加 1。
    总 为 输入 的 行数 乘 输入 的 列数。
    若 非 训练中：
        返还 输入。
    完。
    输出 为 做 Tensor_创建 用 输入 的 行数、输入 的 列数。
    scale 为 做 Q除 用 Q_ONE、Q_ONE 减 概率Q10。
    可变 i 为 0。
    只要 i 小于 总：
        r 为 做 NOGL_随机 取余于 Q_ONE。
        若 r 小于 概率Q10：
            做 内存_写32 用 输出 的 数据 加 i 乘 4、0。
        否则：
            v 为 做 内存_读32有符号 用 输入 的 数据 加 i 乘 4。
            做 内存_写32 用 输出 的 数据 加 i 乘 4、做 Q乘 用 v、scale。
        完。
        i 为 i 加 1。
    完。
    返还 输出。
完。

NOGL_梯度范数 接受 梯度：
    总 为 梯度 的 行数 乘 梯度 的 列数。
    可变 sum_sq 为 0。
    可变 i 为 0。
    只要 i 小于 总：
        g 为 做 内存_读32有符号 用 梯度 的 数据 加 i 乘 4。
        sum_sq 为 sum_sq 加 做 Q乘 用 g、g。
        i 为 i 加 1。
    完。
    返还 做 Q平方根 用 sum_sq。
完。

NOGL_梯度裁剪 接受 梯度、最大范数：
    norm 为 做 NOGL_梯度范数 用 梯度。
    若 norm 小于或等于 最大范数：
        返还。
    完。
    scale 为 做 Q除 用 最大范数、norm。
    总 为 梯度 的 行数 乘 梯度 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        g 为 做 内存_读32有符号 用 梯度 的 数据 加 i 乘 4。
        做 内存_写32 用 梯度 的 数据 加 i 乘 4、做 Q乘 用 g、scale。
        i 为 i 加 1。
    完。
完。

NOGL_AI_选择优化器 接受 损失历史、历史长度：
    若 全局AI总线 等于 0：
        返还 0。
    完。
    NOGL_AI元学习次数 为 NOGL_AI元学习次数 加 1。
    tensor 为 做 Tensor_创建 用 6。
    可变 sum 为 0。
    可变 i 为 0。
    只要 i 小于 历史长度：
        sum 为 sum 加 损失历史 的 第 i 项。
        i 为 i 加 1。
    完。
    avg_loss 为 若 历史长度 大于 0 则 sum 除以 历史长度 否则 0。
    做 Tensor_写 用 tensor、0、avg_loss 除以 10240.0。
    trend 为 0。
    若 历史长度 大于或等于 2：
        trend 为 损失历史 的 第 历史长度 减 1 项 减 损失历史 的 第 历史长度 减 2 项。
    完。
    做 Tensor_写 用 tensor、1、trend 除以 10240.0。
    可变 var_sum 为 0。
    i 为 0。
    只要 i 小于 历史长度：
        diff 为 损失历史 的 第 i 项 减 avg_loss。
        var_sum 为 var_sum 加 做 Q乘 用 diff、diff。
        i 为 i 加 1。
    完。
    做 Tensor_写 用 tensor、2、若 历史长度 大于 0 则 var_sum 除以 历史长度 否则 0 除以 10240.0。
    做 Tensor_写 用 tensor、3、历史长度 除以 1000.0。
    做 Tensor_写 用 tensor、4、NOGL_更新次数 除以 10000.0。
    可变 活跃度 为 0。
    若 NOGL_更新次数 大于 0：
        活跃度 为 NOGL_正则化次数 加 NOGL_随机丢弃次数 加 NOGL_调度次数。
        活跃度 为 活跃度 除以 (NOGL_更新次数 乘 3)。
    完。
    若 活跃度 小于 0：
        活跃度 为 0。
    完。
    若 活跃度 大于 1：
        活跃度 为 1。
    完。
    做 Tensor_写 用 tensor、5、活跃度。
    out 为 做 AI总线_推理 用 全局AI总线、「NOGL_元学习」、tensor、6。
    可变 choice 为 0。
    若 out 不等于 0：
        score 为 做 Tensor_读 用 out、0。
        若 score 大于 0.66：
            choice 为 2。
        否则若 score 大于 0.33：
            choice 为 1。
        否则：
            choice 为 0。
        完。
        做 Tensor_释放 用 out。
    完。
    做 Tensor_释放 用 tensor。
    返还 choice。
完。

NOGL_运行时统计 接受 零参：
    做 打印 用 「[NOGL] updates=」 加 做 整数转字符串 用 NOGL_更新次数 加 「 adam=」 加 做 整数转字符串 用 NOGL_Adam次数 加 「 rmsprop=」 加 做 整数转字符串 用 NOGL_RMSProp次数 加 「 schedule=」 加 做 整数转字符串 用 NOGL_调度次数 加 「 reg=」 加 做 整数转字符串 用 NOGL_正则化次数 加 「 dropout=」 加 做 整数转字符串 用 NOGL_随机丢弃次数 加 「 meta=」 加 做 整数转字符串 用 NOGL_AI元学习次数。
完。

MAML_内循环学习率 为 102。

MAML_外循环学习率 为 10。

MAML_内循环步数 为 1。

可变 MAML_任务完成数 为 0。

MAML_内循环适应 接受 网络、支撑样本、支撑标签、k步数：
    适应网络 为 做 网络克隆 用 网络。
    可变 step 为 0。
    只要 step 小于 k步数：
        预测 为 做 网络_前向 用 适应网络、支撑样本。
        梯度 为 做 网络_反向梯度 用 适应网络、预测、支撑标签。
        可变 层 为 0。
        只要 层 小于 适应网络 的 层数：
            w 为 做 网络_获取权重 用 适应网络、层。
            g 为 梯度 的 第 层 项。
            做 Tensor_减去缩放 用 w、g、MAML_内循环学习率。
            层 为 层 加 1。
        完。
        step 为 step 加 1。
    完。
    返还 适应网络。
完。

MAML_外循环更新 接受 元网络、适应网络、查询样本、查询标签：
    预测 为 做 网络_前向 用 适应网络、查询样本。
    梯度 为 做 网络_反向梯度 用 适应网络、预测、查询标签。
    可变 层 为 0。
    只要 层 小于 元网络 的 层数：
        w 为 做 网络_获取权重 用 元网络、层。
        g 为 梯度 的 第 层 项。
        做 Tensor_减去缩放 用 w、g、MAML_外循环学习率。
        层 为 层 加 1。
    完。
    NOGL_AI元学习次数 为 NOGL_AI元学习次数 加 1。
    MAML_任务完成数 为 MAML_任务完成数 加 1。
完。

MAML_涌现状态 接受 零参：
    若 MAML_任务完成数 大于或等于 50：
        返还 「已涌现」。
    完。
    若 MAML_任务完成数 大于或等于 20：
        返还 「涌现中」。
    完。
    返还 「未涌现(」 加 做 整数转字符串 用 50 减 MAML_任务完成数 加 「步后)」。
完。

EWC_中文_LAMBDA 为 100。

EWC_英文_LAMBDA 为 100。

EWC_Fisher估计_样本数 为 50。

EWC参考点 是 一种 事物，含 权重快照、Fisher对角、lambda。

弹性权重_创建参考点 接受 网络、旧任务样本、旧任务标签、是否中文：
    ref 为 做 堆_分配 用 EWC参考点。
    ref 的 权重快照 为 做 网络_克隆权重 用 网络。
    ref 的 Fisher对角 为 做 网络_估计Fisher 用 网络、旧任务样本、旧任务标签、EWC_Fisher估计_样本数。
    ref 的 lambda 为 若 是否中文 则 EWC_中文_LAMBDA 否则 EWC_英文_LAMBDA。
    返还 ref。
完。

EWC_安全DIFF_MAX 为 3783。
