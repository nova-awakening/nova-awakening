《语义嵌入》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·定点Q算术》。

引用《..·中枢·内核打印》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

引用《.·定点精度》。

嵌入维度_标签 为 32。

嵌入维度_类名 为 32。

嵌入维度_URL 为 32。

嵌入维度_节点 为 32。

语义草图维度 为 8。

标签数量 为 48。

标签ID_div 为 0。

标签ID_span 为 1。

标签ID_p 为 2。

标签ID_a 为 3。

标签ID_h1 为 4。

标签ID_h2 为 5。

标签ID_h3 为 6。

标签ID_h4 为 7。

标签ID_img 为 8。

标签ID_nav 为 9。

标签ID_header 为 10。

标签ID_footer 为 11。

标签ID_main 为 12。

标签ID_article 为 13。

标签ID_section 为 14。

标签ID_aside 为 15。

标签ID_form 为 16。

标签ID_input 为 17。

标签ID_button 为 18。

标签ID_ul 为 19。

标签ID_ol 为 20。

标签ID_li 为 21。

标签ID_table 为 22。

标签ID_video 为 23。

标签ID_audio 为 24。

标签ID_canvas 为 25。

标签ID_script 为 26。

标签ID_style 为 27。

标签ID_iframe 为 28。

标签ID_strong 为 29。

标签ID_em 为 30。

标签ID_br 为 31。

标签ID_hr 为 32。

标签ID_select 为 33。

标签ID_textarea 为 34。

标签ID_label 为 35。

标签ID_figure 为 36。

标签ID_figcaption 为 37。

标签ID_blockquote 为 38。

标签ID_code 为 39。

标签ID_pre 为 40。

标签ID_details 为 41。

标签ID_dialog 为 42。

标签ID_svg 为 43。

标签ID_meta 为 44。

标签ID_link 为 45。

标签ID_head 为 46。

标签ID_body 为 47。

语义嵌入引擎 是 一种 事物，含 标签嵌入表、类名嵌入网络、URL嵌入网络、节点嵌入网络、语义原型、角色数量。

Q_ONE 为 1024。

Q_ZERO 为 0。

Q_HALF 为 512。

可变 全局语义引擎 为 空。

Q乘 接受 a、b：
    返还 a 乘 b 右移 10。
完。

向量_模长 接受 向量、维度：
    可变 平方和 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        v 为 做 Tensor_读 用 向量、i。
        平方和 为 平方和 加 做 Q乘 用 v、v。
        i 为 i 加 1。
    完。
    若 平方和 小于或等于 0：
        返还 0。
    完。
    可变 x 为 平方和。
    可变 迭代 为 0。
    只要 迭代 小于 8：
        x 为 x 加 做 Q除 用 平方和、x 右移 1。
        迭代 为 迭代 加 1。
    完。
    返还 x。
完。

向量_加 接受 结果、向量A、向量B、维度：
    可变 i 为 0。
    只要 i 小于 维度：
        做 Tensor_写 用 结果、i、做 Tensor_读 用 向量A、i 加 做 Tensor_读 用 向量B、i。
        i 为 i 加 1。
    完。
完。

向量_缩放 接受 结果、向量、因子、维度：
    可变 i 为 0。
    只要 i 小于 维度：
        做 Tensor_写 用 结果、i、做 Q乘 用 做 Tensor_读 用 向量、i、因子。
        i 为 i 加 1。
    完。
完。

向量_归一化 接受 向量、维度：
    模 为 做 向量_模长 用 向量、维度。
    若 模 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 维度：
        做 Tensor_写 用 向量、i、做 Q除 用 做 Tensor_读 用 向量、i、模。
        i 为 i 加 1。
    完。
完。

极坐标_编码单维 接受 值_Q10、模长_Q10：
    若 模长_Q10 等于 0：
        返还 0。
    完。
    方向_Q10 为 做 Q除 用 值_Q10、模长_Q10。
    方向4 为 方向_Q10 乘 7 除以 1024。
    幅值15 为 模长_Q10 乘 15 除以 4096。
    若 幅值15 大于 15：
        幅值15 为 15。
    完。
    返还 方向4 加 7 左移 4 位或 幅值15。
完。

极坐标_解码单维 接受 编码_8bit、模长_Q10：
    方向4 为 (编码_8bit 右移 4 位与 0xF) 减 7。
    幅值15 为 编码_8bit 位与 0xF。
    值 为 方向4 乘 模长_Q10 除以 7 乘 幅值15 除以 15。
    返还 值。
完。

JL_种子 为 42。

JL_缩放 为 362。

JL投影_32到8 接受 向量32、草图8、源维度、目标维度：
    可变 k 为 0。
    只要 k 小于 目标维度：
        可变 累加 为 0。
        可变 j 为 0。
        只要 j 小于 源维度：
            h 为 (k 乘 137 加 j 乘 31 加 JL_种子) 乘 1664525 加 1013904223。
            符号 为 若 h 位与 0x80000000 不等于 0 则 1 否则 负 1。
            累加 为 累加 加 做 Tensor_读 用 向量32、j 乘 符号。
            j 为 j 加 1。
        完。
        做 Tensor_写 用 草图8、k、累加 乘 JL_缩放 除以 1024。
        k 为 k 加 1。
    完。
完。

语义嵌入_初始化 接受 零参：
    若 全局语义引擎 不等于 空：
        做 语义嵌入_释放引擎 用 全局语义引擎。
        全局语义引擎 为 空。
    完。
    全局NSEI统计 为 新建 NSEI统计。
    engine 为 做 堆_分配 用 语义嵌入引擎。
    若 engine 等于 0：
        返还 空。
    完。
    engine 的 标签嵌入表 为 做 Tensor_创建 用 标签数量 乘 嵌入维度_标签。
    若 engine 的 标签嵌入表 等于 空：
        做 语义嵌入_创建失败清理 用 engine。
        返还 空。
    完。
    做 标签嵌入_初始化 用 engine。
    engine 的 角色数量 为 8。
    engine 的 语义原型 为 做 Tensor_创建 用 engine 的 角色数量 乘 嵌入维度_节点。
    若 engine 的 语义原型 等于 空：
        做 语义嵌入_创建失败清理 用 engine。
        返还 空。
    完。
    做 语义原型_初始化 用 engine。
    全局语义引擎 为 engine。
    做 内核打印 用 「[NSEI] 语义嵌入引擎已初始化 标签数={} 角色数={}」、标签数量、engine 的 角色数量。
    返还 engine。
完。

语义嵌入_释放引擎 接受 engine：
    若 engine 等于 空：
        返还。
    完。
    若 engine 的 标签嵌入表 不等于 空：
        做 Tensor_释放 用 engine 的 标签嵌入表。
        engine 的 标签嵌入表 为 空。
    完。
    若 engine 的 语义原型 不等于 空：
        做 Tensor_释放 用 engine 的 语义原型。
        engine 的 语义原型 为 空。
    完。
    engine 的 类名嵌入网络 为 空。
    engine 的 URL嵌入网络 为 空。
    engine 的 节点嵌入网络 为 空。
    若 engine 等于 全局语义引擎：
        全局语义引擎 为 空。
    完。
    做 堆_释放 用 engine。
完。

语义嵌入_创建失败清理 接受 engine：
    若 engine 等于 空：
        返还。
    完。
    若 engine 的 标签嵌入表 不等于 空：
        做 Tensor_释放 用 engine 的 标签嵌入表。
        engine 的 标签嵌入表 为 空。
    完。
    若 engine 的 语义原型 不等于 空：
        做 Tensor_释放 用 engine 的 语义原型。
        engine 的 语义原型 为 空。
    完。
    engine 的 类名嵌入网络 为 空。
    engine 的 URL嵌入网络 为 空。
    engine 的 节点嵌入网络 为 空。
    做 堆_释放 用 engine。
完。

标签嵌入_初始化 接受 engine：
    结构标签 为 一组：标签ID_nav、标签ID_header、标签ID_footer、标签ID_main、标签ID_article、标签ID_section、标签ID_aside。
    可变 i 为 0。
    只要 i 小于 7：
        基址 为 结构标签 的 第 i 项 乘 嵌入维度_标签。
        做 Tensor_写 用 engine 的 标签嵌入表、基址 加 0、900。
        做 Tensor_写 用 engine 的 标签嵌入表、基址 加 1、100 加 i 乘 80。
        做 Tensor_写 用 engine 的 标签嵌入表、基址 加 2、200。
        i 为 i 加 1。
    完。
    文本标签 为 一组：标签ID_p、标签ID_h1、标签ID_h2、标签ID_h3、标签ID_span、标签ID_strong。
    i 为 0。
    只要 i 小于 6：
        基址 为 文本标签 的 第 i 项 乘 嵌入维度_标签。
        做 Tensor_写 用 engine 的 标签嵌入表、基址 加 0、200。
        做 Tensor_写 用 engine 的 标签嵌入表、基址 加 1、900。
        做 Tensor_写 用 engine 的 标签嵌入表、基址 加 2、500 加 i 乘 50。
        i 为 i 加 1。
    完。
完。

语义原型_写入 接受 engine、角色、布局、内容、结构、交互、媒体、代码、广告、头部：
    基址 为 角色 乘 嵌入维度_节点。
    可变 d 为 0。
    只要 d 小于 嵌入维度_节点：
        做 Tensor_写 用 engine 的 语义原型、基址 加 d、120。
        d 为 d 加 1。
    完。
    做 Tensor_写 用 engine 的 语义原型、基址 加 0、布局。
    做 Tensor_写 用 engine 的 语义原型、基址 加 1、内容。
    做 Tensor_写 用 engine 的 语义原型、基址 加 2、结构。
    做 Tensor_写 用 engine 的 语义原型、基址 加 3、交互。
    做 Tensor_写 用 engine 的 语义原型、基址 加 4、媒体。
    做 Tensor_写 用 engine 的 语义原型、基址 加 5、代码。
    做 Tensor_写 用 engine 的 语义原型、基址 加 6、广告。
    做 Tensor_写 用 engine 的 语义原型、基址 加 7、头部。
完。

语义原型_初始化 接受 engine：
    做 语义原型_写入 用 engine、0、900、220、840、160、120、120、80、960。
    做 语义原型_写入 用 engine、1、220、960、240、140、120、120、80、180。
    做 语义原型_写入 用 engine、2、820、260、780、180、120、120、180、220。
    做 语义原型_写入 用 engine、3、300、520、260、940、100、100、80、180。
    做 语义原型_写入 用 engine、4、240、360、260、220、960、140、80、160。
    做 语义原型_写入 用 engine、5、220、420、220、160、140、960、80、180。
    做 语义原型_写入 用 engine、6、760、200、680、720、180、120、960、180。
    做 语义原型_写入 用 engine、7、920、320、840、140、120、120、80、980。
完。

标签名_到ID 接受 标签名：
    若 做 字符串_相等 用 标签名、「div」：
        返还 标签ID_div。
    完。
    若 做 字符串_相等 用 标签名、「span」：
        返还 标签ID_span。
    完。
    若 做 字符串_相等 用 标签名、「p」：
        返还 标签ID_p。
    完。
    若 做 字符串_相等 用 标签名、「a」：
        返还 标签ID_a。
    完。
    若 做 字符串_相等 用 标签名、「h1」：
        返还 标签ID_h1。
    完。
    若 做 字符串_相等 用 标签名、「h2」：
        返还 标签ID_h2。
    完。
    若 做 字符串_相等 用 标签名、「nav」：
        返还 标签ID_nav。
    完。
    若 做 字符串_相等 用 标签名、「header」：
        返还 标签ID_header。
    完。
    若 做 字符串_相等 用 标签名、「footer」：
        返还 标签ID_footer。
    完。
    若 做 字符串_相等 用 标签名、「main」：
        返还 标签ID_main。
    完。
    若 做 字符串_相等 用 标签名、「article」：
        返还 标签ID_article。
    完。
    若 做 字符串_相等 用 标签名、「section」：
        返还 标签ID_section。
    完。
    若 做 字符串_相等 用 标签名、「form」：
        返还 标签ID_form。
    完。
    若 做 字符串_相等 用 标签名、「img」：
        返还 标签ID_img。
    完。
    若 做 字符串_相等 用 标签名、「video」：
        返还 标签ID_video。
    完。
    若 做 字符串_相等 用 标签名、「script」：
        返还 标签ID_script。
    完。
    返还 标签ID_div。
完。

获取标签嵌入 接受 标签ID、结果向量：
    若 全局语义引擎 等于 空：
        返还。
    完。
    若 标签ID 大于或等于 标签数量：
        标签ID 为 0。
    完。
    基址 为 标签ID 乘 嵌入维度_标签。
    可变 i 为 0。
    只要 i 小于 嵌入维度_标签：
        做 Tensor_写 用 结果向量、i、做 Tensor_读 用 全局语义引擎 的 标签嵌入表、基址 加 i。
        i 为 i 加 1。
    完。
完。

类名哈希_桶数 为 128。

类名_哈希嵌入 接受 类名字符串、结果向量：
    若 类名字符串 等于 0：
        返还。
    完。
    长度 为 做 字符串_长度 用 类名字符串。
    可变 哈希特征 为 做 Tensor_创建 用 类名哈希_桶数。
    可变 i 为 0。
    只要 i 加 2 小于 长度：
        c0 为 做 字符取 用 类名字符串、i。
        c1 为 做 字符取 用 类名字符串、i 加 1。
        c2 为 做 字符取 用 类名字符串、i 加 2。
        h 为 ((c0 乘 31 加 c1) 乘 31 加 c2) 取余于 类名哈希_桶数。
        做 Tensor_写 用 哈希特征、h、做 Tensor_读 用 哈希特征、h 加 Q_ONE。
        i 为 i 加 1。
    完。
    可变 d 为 0。
    只要 d 小于 嵌入维度_类名：
        可变 总和 为 0。
        可变 j 为 0。
        只要 j 小于 类名哈希_桶数：
            权重 为 (j 乘 73 加 d 乘 37) 取余于 2048 减 1024。
            总和 为 总和 加 做 Q乘 用 做 Tensor_读 用 哈希特征、j、权重。
            j 为 j 加 1。
        完。
        做 Tensor_写 用 结果向量、d、总和。
        d 为 d 加 1。
    完。
    做 Tensor_释放 用 哈希特征。
    做 向量_归一化 用 结果向量、嵌入维度_类名。
完。

角色_导航 为 0。

角色_正文 为 1。

角色_侧边栏 为 2。

角色_表单 为 3。

角色_媒体 为 4。

角色_代码 为 5。

角色_广告 为 6。

角色_头部 为 7。

节点_语义分类 接受 标签名、类名字符串：
    若 全局语义引擎 等于 空：
        返还 角色_正文。
    完。
    标签向量 为 做 Tensor_创建 用 嵌入维度_标签。
    id 为 做 标签名_到ID 用 标签名。
    做 获取标签嵌入 用 id、标签向量。
    类名向量 为 做 Tensor_创建 用 嵌入维度_类名。
    做 类名_哈希嵌入 用 类名字符串、类名向量。
    节点向量 为 做 Tensor_创建 用 嵌入维度_节点。
    做 向量_加 用 节点向量、标签向量、类名向量、嵌入维度_节点。
    做 向量_归一化 用 节点向量、嵌入维度_节点。
    可变 最佳角色 为 0。
    可变 最高相似度 为 负 9999。
    可变 r 为 0。
    只要 r 小于 全局语义引擎 的 角色数量：
        原型 为 做 Tensor_创建 用 嵌入维度_节点。
        可变 d 为 0。
        只要 d 小于 嵌入维度_节点：
            做 Tensor_写 用 原型、d、做 Tensor_读 用 全局语义引擎 的 语义原型、r 乘 嵌入维度_节点 加 d。
            d 为 d 加 1。
        完。
        相似度 为 做 余弦相似度 用 节点向量、原型、嵌入维度_节点。
        若 相似度 大于 最高相似度：
            最高相似度 为 相似度。
            最佳角色 为 r。
        完。
        做 Tensor_释放 用 原型。
        r 为 r 加 1。
    完。
    做 Tensor_释放 用 标签向量。
    做 Tensor_释放 用 类名向量。
    做 Tensor_释放 用 节点向量。
    全局NSEI统计 的 分类次数 为 全局NSEI统计 的 分类次数 加 1。
    返还 最佳角色。
完。

KMeans_聚类 接受 节点嵌入列表、节点数、K、维度、最大迭代：
    若 节点数 小于 K 或者 K 等于 0：
        返还 空。
    完。
    中心 为 做 Tensor_创建 用 K 乘 维度。
    可变 k 为 0。
    只要 k 小于 K：
        可变 d 为 0。
        只要 d 小于 维度：
            做 Tensor_写 用 中心、k 乘 维度 加 d、做 Tensor_读 用 节点嵌入列表、k 乘 维度 加 d。
            d 为 d 加 1。
        完。
        k 为 k 加 1。
    完。
    标签 为 做 Tensor_创建 用 节点数。
    可变 迭代 为 0。
    只要 迭代 小于 最大迭代：
        可变 n 为 0。
        只要 n 小于 节点数：
            可变 最佳 为 0。
            可变 最高似 为 负 9999。
            k 为 0。
            只要 k 小于 K：
                可变 点积 为 0。
                可变 d 为 0。
                只要 d 小于 维度：
                    点积 为 点积 加 做 Q乘 用 做 Tensor_读 用 节点嵌入列表、n 乘 维度 加 d、做 Tensor_读 用 中心、k 乘 维度 加 d。
                    d 为 d 加 1。
                完。
                若 点积 大于 最高似：
                    最高似 为 点积。
                    最佳 为 k。
                完。
                k 为 k 加 1。
            完。
            做 Tensor_写 用 标签、n、最佳。
            n 为 n 加 1。
        完。
        k 为 0。
        只要 k 小于 K 乘 维度：
            做 Tensor_写 用 中心、k、0。
            k 为 k 加 1。
        完。
        计数器 为 做 Tensor_创建 用 K。
        n 为 0。
        只要 n 小于 节点数：
            所属类 为 做 Tensor_读 用 标签、n。
            做 Tensor_写 用 计数器、所属类、做 Tensor_读 用 计数器、所属类 加 1。
            可变 d 为 0。
            只要 d 小于 维度：
                旧值 为 做 Tensor_读 用 中心、所属类 乘 维度 加 d。
                做 Tensor_写 用 中心、所属类 乘 维度 加 d、旧值 加 做 Tensor_读 用 节点嵌入列表、n 乘 维度 加 d。
                d 为 d 加 1。
            完。
            n 为 n 加 1。
        完。
        k 为 0。
        只要 k 小于 K：
            cnt 为 做 Tensor_读 用 计数器、k。
            若 cnt 大于 0：
                可变 d 为 0。
                只要 d 小于 维度：
                    做 Tensor_写 用 中心、k 乘 维度 加 d、做 Tensor_读 用 中心、k 乘 维度 加 d 除以 cnt。
                    d 为 d 加 1。
                完。
            完。
            k 为 k 加 1。
        完。
        做 Tensor_释放 用 计数器。
        迭代 为 迭代 加 1。
    完。
    做 Tensor_释放 用 中心。
    返还 标签。
完。

NSEI统计 是 一种 事物，含 分类次数、聚类次数、嵌入查询次数、在线更新次数。

可变 全局NSEI统计 为 新建 NSEI统计。

语义嵌入_编码 接受 文本：
    若 文本 等于 0：
        返还 0。
    完。
    若 全局语义引擎 等于 空：
        做 语义嵌入_初始化。
    完。
    向量 为 做 Tensor_创建 用 嵌入维度_节点。
    若 向量 等于 0：
        返还 0。
    完。
    做 Tensor_清零 用 向量、嵌入维度_节点。
    长度 为 做 字符串_长度 用 文本。
    若 长度 等于 0：
        全局NSEI统计 的 嵌入查询次数 为 全局NSEI统计 的 嵌入查询次数 加 1。
        返还 向量。
    完。
    可变 i 为 0。
    只要 i 小于 长度：
        c 为 做 字符取 用 文本、i。
        主桶 为 (c 乘 131 加 i 乘 17) 取余于 嵌入维度_节点。
        次桶 为 (c 乘 17 加 i 乘 131) 取余于 嵌入维度_节点。
        做 Tensor_写 用 向量、主桶、做 Tensor_读 用 向量、主桶 加 Q_ONE。
        若 i 加 1 小于 长度：
            c2 为 做 字符取 用 文本、i 加 1。
            双桶 为 ((c 乘 31 加 c2) 乘 7 加 i 乘 13) 取余于 嵌入维度_节点。
            做 Tensor_写 用 向量、双桶、做 Tensor_读 用 向量、双桶 加 Q_HALF。
        完。
        若 c 大于或等于 0x4E00 并且 c 小于或等于 0x9FFF：
            部首桶 为 (c 减 0x4E00) 除以 100 取余于 嵌入维度_节点。
            做 Tensor_写 用 向量、次桶、做 Tensor_读 用 向量、次桶 加 1800。
            做 Tensor_写 用 向量、部首桶、做 Tensor_读 用 向量、部首桶 加 900。
        否则若 c 大于或等于 65 并且 c 小于或等于 90 或者 c 大于或等于 97 并且 c 小于或等于 122：
            做 Tensor_写 用 向量、次桶、做 Tensor_读 用 向量、次桶 加 1000。
        否则若 c 大于或等于 48 并且 c 小于或等于 57：
            做 Tensor_写 用 向量、次桶、做 Tensor_读 用 向量、次桶 加 512。
        否则：
            做 Tensor_写 用 向量、次桶、做 Tensor_读 用 向量、次桶 加 256。
        完。
        i 为 i 加 1。
    完。
    做 向量_归一化 用 向量、嵌入维度_节点。
    全局NSEI统计 的 嵌入查询次数 为 全局NSEI统计 的 嵌入查询次数 加 1。
    返还 向量。
完。

语义嵌入_压缩草图 接受 向量、维度：
    若 向量 等于 0：
        返还 0。
    完。
    若 维度 小于或等于 0：
        维度 为 嵌入维度_节点。
    完。
    草图 为 做 Tensor_创建 用 语义草图维度。
    若 草图 等于 0：
        返还 0。
    完。
    做 Tensor_清零 用 草图、语义草图维度。
    做 JL投影_32到8 用 向量、草图、维度、语义草图维度。
    返还 草图。
完。

语义嵌入_文本草图 接受 文本：
    若 文本 等于 0：
        返还 0。
    完。
    向量 为 做 语义嵌入_编码 用 文本。
    若 向量 等于 0：
        返还 0。
    完。
    草图 为 做 语义嵌入_压缩草图 用 向量、嵌入维度_节点。
    做 Tensor_释放 用 向量。
    返还 草图。
完。

语义嵌入_草图相似度 接受 草图A、草图B：
    若 草图A 等于 0 或者 草图B 等于 0：
        返还 0。
    完。
    返还 做 余弦相似度 用 草图A、草图B、语义草图维度。
完。

语义嵌入_草图质量 接受 向量、草图：
    若 向量 等于 0 或者 草图 等于 0：
        返还 0。
    完。
    质量 为 做 定点_压缩验证 用 向量、草图、「语义草图」。
    若 质量 小于 0：
        质量 为 0。
    完。
    若 质量 大于 1000：
        质量 为 1000。
    完。
    返还 质量。
完。

NSEI_打印诊断 接受 零参：
    做 内核打印 用 「╔════════════════════════════════════════════╗」。
    做 内核打印 用 「║  NSEI 语义嵌入智能引擎 诊断报告           ║」。
    做 内核打印 用 「╠════════════════════════════════════════════╣」。
    做 内核打印 用 「║ 标签数量:       {}」、标签数量。
    做 内核打印 用 「║ 嵌入维度:       {}」、嵌入维度_标签。
    若 全局语义引擎 不等于 空：
        做 内核打印 用 「║ 语义角色数:     {}」、全局语义引擎 的 角色数量。
    完。
    做 内核打印 用 「║ 分类次数:       {}」、全局NSEI统计 的 分类次数。
    做 内核打印 用 「║ 聚类次数:       {}」、全局NSEI统计 的 聚类次数。
    做 内核打印 用 「║ 嵌入查询:       {}」、全局NSEI统计 的 嵌入查询次数。
    做 内核打印 用 「╚════════════════════════════════════════════╝」。
完。

本章 收束。
