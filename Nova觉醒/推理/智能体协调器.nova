《智能体协调器》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·哈希整数》。

引用《..·中枢·内容可见性》。

最大智能体 为 32。

最大任务 为 128。

最大能力 为 16。

状态_空闲 为 0。

状态_忙碌 为 1。

状态_挂起 为 2。

优先_低 为 0。

优先_中 为 1。

优先_高 为 2。

优先_紧急 为 3。

智能体 是 一种 事物，含 名称哈希、能力掩码、状态、当前任务、完成任务数、失败任务数、总耗时、最近活跃、评分。

协调任务 是 一种 事物，含 标识、类型哈希、优先级、数据指针、分配智能体、提交时刻、开始时刻、完成标记。

可变 智能体表 为 一组：（空）。

可变 智能体数 为 0。

可变 任务队列 为 一组：（空）。

可变 任务数 为 0。

可变 下一任务标识 为 1。

可变 分派总数 为 0。

可变 完成总数 为 0。

可变 失败总数 为 0。

协调器_初始化 接受 零参：
    智能体表 为 一组：（空）。
    智能体数 为 0。
    任务队列 为 一组：（空）。
    任务数 为 0。
完。

协调器_注册 接受 名称、能力掩码：
    若 智能体数 大于或等于 最大智能体：
        返还 负 1。
    完。
    体 为 新建 智能体。
    体 的 名称哈希 为 做 哈希_字符串 用 名称。
    体 的 能力掩码 为 能力掩码。
    体 的 状态 为 状态_空闲。
    体 的 当前任务 为 负 1。
    体 的 完成任务数 为 0。
    体 的 失败任务数 为 0。
    体 的 总耗时 为 0。
    体 的 最近活跃 为 做 平台_获取高精度时间。
    体 的 评分 为 500。
    做 追加 用 智能体表、体。
    索引 为 智能体数。
    智能体数 为 智能体数 加 1。
    返还 索引。
完。

协调器_提交任务 接受 类型、优先级、数据：
    若 任务数 大于或等于 最大任务：
        返还 负 1。
    完。
    任务 为 新建 协调任务。
    任务 的 标识 为 下一任务标识。
    下一任务标识 为 下一任务标识 加 1。
    任务 的 类型哈希 为 做 哈希_字符串 用 类型。
    任务 的 优先级 为 优先级。
    任务 的 数据指针 为 数据。
    任务 的 分配智能体 为 负 1。
    任务 的 提交时刻 为 做 平台_获取高精度时间。
    任务 的 开始时刻 为 0。
    任务 的 完成标记 为 假。
    做 追加 用 任务队列、任务。
    任务数 为 任务数 加 1。
    做 协调器_尝试分派。
    返还 任务 的 标识。
完。

协调器_尝试分派 接受 零参：
    可变 任务序 为 0。
    只要 任务序 小于 任务数：
        任务 为 任务队列 的 第 任务序 项。
        若 任务 的 分配智能体 大于或等于 0 或者 任务 的 完成标记：
            任务序 为 任务序 加 1。
            继续。
        完。
        最佳 为 做 协调器_选择最佳 用 任务 的 类型哈希、任务 的 优先级。
        若 最佳 大于或等于 0：
            任务 的 分配智能体 为 最佳。
            任务 的 开始时刻 为 做 平台_获取高精度时间。
            智能体表 的 第 最佳 项 的 状态 为 状态_忙碌。
            智能体表 的 第 最佳 项 的 当前任务 为 任务 的 标识。
            分派总数 为 分派总数 加 1。
        完。
        任务序 为 任务序 加 1。
    完。
完。

协调器_选择最佳 接受 类型哈希、优先级：
    可变 最佳 为 负 1。
    可变 最高分 为 负 1。
    可变 序号 为 0。
    只要 序号 小于 智能体数：
        体 为 智能体表 的 第 序号 项。
        若 体 的 状态 等于 状态_空闲：
            分数 为 体 的 评分。
            若 分数 大于 最高分：
                最高分 为 分数。
                最佳 为 序号。
            完。
        完。
        序号 为 序号 加 1。
    完。
    返还 最佳。
完。

协调器_完成任务 接受 任务标识、成功：
    可变 任务序 为 0。
    只要 任务序 小于 任务数：
        任务 为 任务队列 的 第 任务序 项。
        若 任务 的 标识 等于 任务标识：
            任务 的 完成标记 为 真。
            智能体索引 为 任务 的 分配智能体。
            若 智能体索引 大于或等于 0 并且 智能体索引 小于 智能体数：
                耗时 为 做 平台_获取高精度时间 减 任务 的 开始时刻。
                智能体表 的 第 智能体索引 项 的 状态 为 状态_空闲。
                智能体表 的 第 智能体索引 项 的 当前任务 为 负 1。
                智能体表 的 第 智能体索引 项 的 总耗时 为 智能体表 的 第 智能体索引 项 的 总耗时 加 耗时。
                智能体表 的 第 智能体索引 项 的 最近活跃 为 做 平台_获取高精度时间。
                若 成功：
                    智能体表 的 第 智能体索引 项 的 完成任务数 为 智能体表 的 第 智能体索引 项 的 完成任务数 加 1。
                    智能体表 的 第 智能体索引 项 的 评分 为 智能体表 的 第 智能体索引 项 的 评分 加 10。
                    若 智能体表 的 第 智能体索引 项 的 评分 大于 1000：
                        智能体表 的 第 智能体索引 项 的 评分 为 1000。
                    完。
                    完成总数 为 完成总数 加 1。
                否则：
                    智能体表 的 第 智能体索引 项 的 失败任务数 为 智能体表 的 第 智能体索引 项 的 失败任务数 加 1。
                    智能体表 的 第 智能体索引 项 的 评分 为 智能体表 的 第 智能体索引 项 的 评分 减 20。
                    若 智能体表 的 第 智能体索引 项 的 评分 小于 0：
                        智能体表 的 第 智能体索引 项 的 评分 为 0。
                    完。
                    失败总数 为 失败总数 加 1。
                完。
            完。
            做 协调器_尝试分派。
            返还。
        完。
        任务序 为 任务序 加 1。
    完。
完。

协调器_释放 接受 名称：
    目标 为 做 哈希_字符串 用 名称。
    可变 序号 为 0。
    只要 序号 小于 智能体数：
        若 智能体表 的 第 序号 项 的 名称哈希 等于 目标：
            智能体表 的 第 序号 项 的 状态 为 状态_空闲。
            智能体表 的 第 序号 项 的 当前任务 为 负 1。
        完。
        序号 为 序号 加 1。
    完。
完。

协调器_空闲数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 智能体数：
        若 智能体表 的 第 序号 项 的 状态 等于 状态_空闲：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

协调器_待处理任务数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 任务数：
        若 非 任务队列 的 第 序号 项 的 完成标记 并且 任务队列 的 第 序号 项 的 分配智能体 小于 0：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

协调器_完整统计 接受 零参：
    返还 一组：智能体数、做 协调器_空闲数、任务数、做 协调器_待处理任务数、分派总数、完成总数、失败总数。
完。

本章 收束。
