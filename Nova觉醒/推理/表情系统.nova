《表情系统》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

引用《..·中枢·内容可见性》。

表情_中立 为 0。

表情_微笑 为 1。

表情_思考 为 2。

表情_惊讶 为 3。

表情_专注 为 4。

表情_悲伤 为 5。

表情_愤怒 为 6。

表情_恐惧 为 7。

表情_厌恶 为 8。

表情数 为 9。

混合维度 为 16。

过渡基数 为 1024。

默认过渡速 为 80。

表情状态 是 一种 事物，含 当前表情、目标表情、混合权重、过渡进度、过渡速度、情绪值、唤醒度、效价、微表情标记、微表情强度、上次更新。

可变 全局表情 为 0。

可变 切换次数 为 0。

可变 微表情检测 为 0。

可变 表情名表 为 0。

表情_初始化 接受 零参：
    全局表情 为 做 堆_分配 用 表情状态。
    全局表情 的 当前表情 为 表情_中立。
    全局表情 的 目标表情 为 表情_中立。
    全局表情 的 混合权重 为 做 堆_分配 用 混合维度 乘 4。
    可变 序号 为 0。
    只要 序号 小于 混合维度：
        做 内存_写32 用 全局表情 的 混合权重 加 序号 乘 4、0。
        序号 为 序号 加 1。
    完。
    全局表情 的 过渡进度 为 过渡基数。
    全局表情 的 过渡速度 为 默认过渡速。
    全局表情 的 情绪值 为 0。
    全局表情 的 唤醒度 为 512。
    全局表情 的 效价 为 512。
    全局表情 的 微表情标记 为 假。
    全局表情 的 微表情强度 为 0。
    全局表情 的 上次更新 为 做 平台_获取高精度时间。
    表情名表 为 做 堆_分配 用 表情数 乘 8。
完。

表情_设情绪 接受 情绪、唤醒、效价：
    若 全局表情 等于 0：
        返还。
    完。
    全局表情 的 情绪值 为 情绪。
    全局表情 的 唤醒度 为 唤醒。
    全局表情 的 效价 为 效价。
    新表情 为 做 表情_从情绪矩阵 用 情绪、唤醒、效价。
    若 新表情 不等于 全局表情 的 目标表情：
        全局表情 的 目标表情 为 新表情。
        全局表情 的 过渡进度 为 0。
        切换次数 为 切换次数 加 1。
    完。
完。

表情_从情绪矩阵 接受 情绪、唤醒、效价：
    若 效价 大于 700 并且 唤醒 大于 500：
        返还 表情_微笑。
    完。
    若 效价 大于 600 并且 唤醒 小于 400：
        返还 表情_专注。
    完。
    若 效价 小于 300 并且 唤醒 大于 700：
        返还 表情_愤怒。
    完。
    若 效价 小于 300 并且 唤醒 小于 300：
        返还 表情_悲伤。
    完。
    若 唤醒 大于 800：
        返还 表情_惊讶。
    完。
    若 效价 小于 200：
        返还 表情_恐惧。
    完。
    若 情绪 大于 50：
        返还 表情_微笑。
    完。
    若 情绪 小于 负 30：
        返还 表情_思考。
    完。
    返还 表情_中立。
完。

表情_更新 接受 零参：
    若 全局表情 等于 0：
        返还。
    完。
    现在 为 做 平台_获取高精度时间。
    间隔 为 现在 减 全局表情 的 上次更新。
    全局表情 的 上次更新 为 现在。
    若 全局表情 的 过渡进度 小于 过渡基数：
        全局表情 的 过渡进度 为 全局表情 的 过渡进度 加 全局表情 的 过渡速度。
        若 全局表情 的 过渡进度 大于或等于 过渡基数：
            全局表情 的 过渡进度 为 过渡基数。
            全局表情 的 当前表情 为 全局表情 的 目标表情。
        完。
    完。
    做 表情_更新混合权重。
    做 表情_检测微表情。
完。

表情_更新混合权重 接受 零参：
    进度 为 全局表情 的 过渡进度。
    反进度 为 过渡基数 减 进度。
    可变 序号 为 0。
    只要 序号 小于 混合维度：
        当前值 为 做 表情_获取基准权重 用 全局表情 的 当前表情、序号。
        目标值 为 做 表情_获取基准权重 用 全局表情 的 目标表情、序号。
        混合值 为 (当前值 乘 反进度 加 目标值 乘 进度) 除以 过渡基数。
        做 内存_写32 用 全局表情 的 混合权重 加 序号 乘 4、混合值。
        序号 为 序号 加 1。
    完。
完。

表情_获取基准权重 接受 表情、维度：
    若 表情 等于 表情_微笑：
        若 维度 等于 0：
            返还 800。
        完。
        若 维度 等于 1：
            返还 600。
        完。
        若 维度 等于 4：
            返还 300。
        完。
    完。
    若 表情 等于 表情_惊讶：
        若 维度 等于 2：
            返还 900。
        完。
        若 维度 等于 3：
            返还 700。
        完。
        若 维度 等于 5：
            返还 500。
        完。
    完。
    若 表情 等于 表情_悲伤：
        若 维度 等于 6：
            返还 700。
        完。
        若 维度 等于 7：
            返还 400。
        完。
    完。
    若 表情 等于 表情_愤怒：
        若 维度 等于 8：
            返还 800。
        完。
        若 维度 等于 9：
            返还 600。
        完。
    完。
    若 表情 等于 表情_专注：
        若 维度 等于 10：
            返还 500。
        完。
        若 维度 等于 11：
            返还 700。
        完。
    完。
    若 表情 等于 表情_思考：
        若 维度 等于 10：
            返还 400。
        完。
        若 维度 等于 12：
            返还 600。
        完。
    完。
    返还 0。
完。

表情_检测微表情 接受 零参：
    情绪变化 为 全局表情 的 情绪值。
    若 情绪变化 小于 0：
        情绪变化 为 0 减 情绪变化。
    完。
    若 情绪变化 大于 30 并且 全局表情 的 过渡进度 大于 过渡基数 除以 2：
        全局表情 的 微表情标记 为 真。
        全局表情 的 微表情强度 为 情绪变化。
        微表情检测 为 微表情检测 加 1。
    否则：
        全局表情 的 微表情标记 为 假。
        全局表情 的 微表情强度 为 0。
    完。
完。

表情_设过渡速度 接受 速度：
    若 全局表情 等于 0：
        返还。
    完。
    若 速度 小于 10：
        速度 为 10。
    完。
    若 速度 大于 500：
        速度 为 500。
    完。
    全局表情 的 过渡速度 为 速度。
完。

表情_获取当前 接受 零参：
    若 全局表情 等于 0：
        返还 表情_中立。
    完。
    返还 全局表情 的 当前表情。
完。

表情_获取混合权重 接受 维度：
    若 全局表情 等于 0 或者 维度 大于或等于 混合维度：
        返还 0。
    完。
    返还 做 内存_读32 用 全局表情 的 混合权重 加 维度 乘 4。
完。

表情_获取名称 接受 表情：
    若 表情 等于 表情_中立：
        返还 「中立」。
    完。
    若 表情 等于 表情_微笑：
        返还 「微笑」。
    完。
    若 表情 等于 表情_思考：
        返还 「思考」。
    完。
    若 表情 等于 表情_惊讶：
        返还 「惊讶」。
    完。
    若 表情 等于 表情_专注：
        返还 「专注」。
    完。
    若 表情 等于 表情_悲伤：
        返还 「悲伤」。
    完。
    若 表情 等于 表情_愤怒：
        返还 「愤怒」。
    完。
    若 表情 等于 表情_恐惧：
        返还 「恐惧」。
    完。
    若 表情 等于 表情_厌恶：
        返还 「厌恶」。
    完。
    返还 「未知」。
完。

表情_是否过渡中 接受 零参：
    若 全局表情 等于 0：
        返还 假。
    完。
    返还 全局表情 的 过渡进度 小于 过渡基数。
完。

表情_完整统计 接受 零参：
    若 全局表情 等于 0：
        返还 一组：0、0、0、0、0。
    完。
    返还 一组：全局表情 的 当前表情、全局表情 的 目标表情、全局表情 的 过渡进度、切换次数、微表情检测。
完。

可变 AI_表情系统_调用数 为 0。

可变 AI_表情系统_异常数 为 0。

AI_表情系统_监控 接受 零参：
    AI_表情系统_调用数 为 AI_表情系统_调用数 加 1。
    返还 AI_表情系统_异常数 等于 0。
完。

AI_表情系统_健康检查 接受 零参：
    若 AI_表情系统_调用数 大于 0 并且 AI_表情系统_异常数 乘 100 除以 AI_表情系统_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
