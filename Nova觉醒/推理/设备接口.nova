《设备接口》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

引用《..·中枢·内容可见性》。

引用《..·稳态·帧缓冲》。

最大设备 为 8。

设备_帧缓冲 为 0。

设备_摄像头 为 1。

设备_屏幕捕获 为 2。

格式_灰度 为 0。

格式_三通道 为 1。

格式_四通道 为 2。

视觉设备 是 一种 事物，含 标识、类型、宽度、高度、帧率、格式、缓冲地址、缓冲大小、活跃、捕获帧数、最近捕获。

可变 设备表 为 一组：（空）。

可变 设备数 为 0。

可变 总捕获帧 为 0。

视觉_初始化 接受 零参：
    设备表 为 一组：（空）。
    设备数 为 0。
    做 视觉_注册帧缓冲。
完。

视觉_注册帧缓冲 接受 零参：
    若 设备数 大于或等于 最大设备：
        返还 负 1。
    完。
    设备 为 新建 视觉设备。
    设备 的 标识 为 设备数。
    设备 的 类型 为 设备_帧缓冲。
    设备 的 宽度 为 做 帧缓冲_获取信息 的 第 0 项。
    设备 的 高度 为 做 帧缓冲_获取信息 的 第 1 项。
    设备 的 帧率 为 60。
    设备 的 格式 为 格式_四通道。
    设备 的 缓冲地址 为 0。
    设备 的 缓冲大小 为 设备 的 宽度 乘 设备 的 高度 乘 4。
    设备 的 活跃 为 真。
    设备 的 捕获帧数 为 0。
    设备 的 最近捕获 为 0。
    做 追加 用 设备表、设备。
    索引 为 设备数。
    设备数 为 设备数 加 1。
    返还 索引。
完。

视觉_注册摄像头 接受 宽、高、帧率：
    若 设备数 大于或等于 最大设备：
        返还 负 1。
    完。
    设备 为 新建 视觉设备。
    设备 的 标识 为 设备数。
    设备 的 类型 为 设备_摄像头。
    设备 的 宽度 为 宽。
    设备 的 高度 为 高。
    设备 的 帧率 为 帧率。
    设备 的 格式 为 格式_三通道。
    设备 的 缓冲大小 为 宽 乘 高 乘 3。
    设备 的 缓冲地址 为 做 堆_分配 用 设备 的 缓冲大小。
    设备 的 活跃 为 假。
    设备 的 捕获帧数 为 0。
    设备 的 最近捕获 为 0。
    做 追加 用 设备表、设备。
    索引 为 设备数。
    设备数 为 设备数 加 1。
    返还 索引。
完。

视觉_捕获帧 接受 设备索引：
    若 设备索引 大于或等于 设备数：
        返还 0。
    完。
    设备 为 设备表 的 第 设备索引 项。
    若 非 设备 的 活跃 并且 设备 的 类型 不等于 设备_帧缓冲：
        返还 0。
    完。
    帧大小 为 设备 的 宽度 乘 设备 的 高度 乘 4。
    帧缓冲 为 做 堆_分配 用 帧大小。
    若 帧缓冲 等于 0：
        返还 0。
    完。
    若 设备 的 类型 等于 设备_帧缓冲：
        源 为 做 帧缓冲_获取绘制目标。
        做 内存_复制 用 帧缓冲、源、帧大小。
    否则若 设备 的 类型 等于 设备_摄像头 并且 设备 的 缓冲地址 不等于 0：
        做 视觉_转换格式 用 设备 的 缓冲地址、帧缓冲、设备 的 宽度、设备 的 高度、设备 的 格式、格式_四通道。
    完。
    设备 的 捕获帧数 为 设备 的 捕获帧数 加 1。
    设备 的 最近捕获 为 做 平台_获取高精度时间。
    总捕获帧 为 总捕获帧 加 1。
    返还 帧缓冲。
完。

视觉_转换格式 接受 源、目标、宽、高、源格式、目标格式：
    像素数 为 宽 乘 高。
    若 源格式 等于 格式_三通道 并且 目标格式 等于 格式_四通道：
        可变 序号 为 0。
        只要 序号 小于 像素数：
            红 为 做 内存_读8 用 源 加 序号 乘 3。
            绿 为 做 内存_读8 用 源 加 序号 乘 3 加 1。
            蓝 为 做 内存_读8 用 源 加 序号 乘 3 加 2。
            做 内存_写32 用 目标 加 序号 乘 4、255 左移 24 位或 红 左移 16 位或 绿 左移 8 位或 蓝。
            序号 为 序号 加 1。
        完。
    否则若 源格式 等于 格式_灰度 并且 目标格式 等于 格式_四通道：
        可变 序号 为 0。
        只要 序号 小于 像素数：
            灰 为 做 内存_读8 用 源 加 序号。
            做 内存_写32 用 目标 加 序号 乘 4、255 左移 24 位或 灰 左移 16 位或 灰 左移 8 位或 灰。
            序号 为 序号 加 1。
        完。
    完。
完。

视觉_缩放帧 接受 源、源宽、源高、目标、目标宽、目标高：
    横比 为 源宽 乘 1024 除以 目标宽。
    纵比 为 源高 乘 1024 除以 目标高。
    可变 纵 为 0。
    只要 纵 小于 目标高：
        源纵 为 纵 乘 纵比 除以 1024。
        若 源纵 大于或等于 源高：
            源纵 为 源高 减 1。
        完。
        可变 横 为 0。
        只要 横 小于 目标宽：
            源横 为 横 乘 横比 除以 1024。
            若 源横 大于或等于 源宽：
                源横 为 源宽 减 1。
            完。
            像素 为 做 内存_读32 用 源 加 (源纵 乘 源宽 加 源横) 乘 4。
            做 内存_写32 用 目标 加 (纵 乘 目标宽 加 横) 乘 4、像素。
            横 为 横 加 1。
        完。
        纵 为 纵 加 1。
    完。
完。

视觉_灰度化 接受 源、目标、宽、高：
    像素数 为 宽 乘 高。
    可变 序号 为 0。
    只要 序号 小于 像素数：
        像素 为 做 内存_读32 用 源 加 序号 乘 4。
        红 为 像素 右移 16 位与 0xFF。
        绿 为 像素 右移 8 位与 0xFF。
        蓝 为 像素 位与 0xFF。
        灰 为 红 乘 77 加 绿 乘 150 加 蓝 乘 29 右移 8。
        做 内存_写8 用 目标 加 序号、灰。
        序号 为 序号 加 1。
    完。
完。

视觉_边缘检测 接受 灰度、输出、宽、高：
    可变 纵 为 1。
    只要 纵 小于 高 减 1：
        可变 横 为 1。
        只要 横 小于 宽 减 1：
            梯度横 为 做 内存_读8 用 灰度 加 (纵 乘 宽 加 横 加 1) 减 做 内存_读8 用 灰度 加 (纵 乘 宽 加 横 减 1)。
            梯度纵 为 做 内存_读8 用 灰度 加 ((纵 加 1) 乘 宽 加 横) 减 做 内存_读8 用 灰度 加 ((纵 减 1) 乘 宽 加 横)。
            若 梯度横 小于 0：
                梯度横 为 0 减 梯度横。
            完。
            若 梯度纵 小于 0：
                梯度纵 为 0 减 梯度纵。
            完。
            强度 为 梯度横 加 梯度纵。
            若 强度 大于 255：
                强度 为 255。
            完。
            做 内存_写8 用 输出 加 纵 乘 宽 加 横、强度。
            横 为 横 加 1。
        完。
        纵 为 纵 加 1。
    完。
完。

视觉_启动设备 接受 索引：
    若 索引 大于或等于 设备数：
        返还。
    完。
    设备表 的 第 索引 项 的 活跃 为 真。
完。

视觉_停止设备 接受 索引：
    若 索引 大于或等于 设备数：
        返还。
    完。
    设备表 的 第 索引 项 的 活跃 为 假。
完。

视觉_获取分辨率 接受 索引：
    若 索引 大于或等于 设备数：
        返还 一组：0、0。
    完。
    返还 一组：设备表 的 第 索引 项 的 宽度、设备表 的 第 索引 项 的 高度。
完。

视觉_获取帧率 接受 索引：
    若 索引 大于或等于 设备数：
        返还 0。
    完。
    返还 设备表 的 第 索引 项 的 帧率。
完。

视觉_完整统计 接受 零参：
    返还 一组：设备数、总捕获帧。
完。

可变 AI_设备接口_故障预警数 为 0。

AI_设备接口_设备健康 接受 错误率、响应时间、温度：
    可变 分 为 1000。
    若 错误率 大于 10：
        分 为 分 减 错误率 乘 5。
    完。
    若 响应时间 大于 1000：
        分 为 分 减 200。
    完。
    若 温度 大于 80：
        分 为 分 减 300。
    完。
    若 分 小于 300：
        AI_设备接口_故障预警数 为 AI_设备接口_故障预警数 加 1。
    完。
    返还 分。
完。

本章 收束。
