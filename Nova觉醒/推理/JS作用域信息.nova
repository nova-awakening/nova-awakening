《JS作用域信息》

引用《.·上下文运行时》。

引用《.·字节码操作码》。

引用《..·中枢·系统调用表》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·辅助存根》。

作用域_全局 为 0。

作用域_函数 为 1。

作用域_块 为 2。

作用域_模块 为 3。

作用域_With 为 4。

作用域_Catch 为 5。

作用域_Eval 为 6。

变量_var 为 0。

变量_let 为 1。

变量_const 为 2。

变量_function 为 3。

变量_parameter 为 4。

变量_class 为 5。

位置_栈 为 0。

位置_上下文 为 1。

位置_全局 为 2。

ScopeVariable 是 一种 事物，含 名、声明类型、位置、槽索引、是可变的、已初始化、被内部函数引用。

ScopeInfo 是 一种 事物，含 类型、是严格模式、外部作用域、变量列表、变量数量、变量容量、函数名、参数数量、有arguments、有rest参数、有new_target、上下文槽数、上下文变量起始。

ScopeInfo_创建 接受 类型、外部：
    scope 为 做 堆_分配 用 ScopeInfo。
    scope 的 类型 为 类型。
    scope 的 是严格模式 为 假。
    scope 的 外部作用域 为 外部。
    scope 的 变量容量 为 32。
    scope 的 变量列表 为 做 堆_分配_数组 用 scope 的 变量容量 乘 48。
    scope 的 变量数量 为 0。
    scope 的 函数名 为 0。
    scope 的 参数数量 为 0。
    scope 的 有arguments 为 假。
    scope 的 有rest参数 为 假。
    scope 的 有new_target 为 假。
    scope 的 上下文槽数 为 0。
    scope 的 上下文变量起始 为 0。
    返还 scope。
完。

ScopeInfo_声明变量 接受 scope、名、声明类型：
    若 声明类型 等于 变量_let 或者 声明类型 等于 变量_const：
        已有 为 做 ScopeInfo_查找本地 用 scope、名。
        若 已有 大于或等于 0：
            返还 负 1。
        完。
    完。
    若 scope 的 变量数量 大于或等于 scope 的 变量容量：
        返还 负 1。
    完。
    偏移 为 scope 的 变量列表 加 scope 的 变量数量 乘 48。
    做 内存_写64 用 偏移、做 字符串_复制 用 名。
    做 内存_写32 用 偏移 加 8、声明类型。
    做 内存_写32 用 偏移 加 12、位置_栈。
    做 内存_写32 用 偏移 加 16、scope 的 变量数量。
    做 内存_写8 用 偏移 加 20、声明类型 不等于 变量_const。
    做 内存_写8 用 偏移 加 21、声明类型 等于 变量_var 或者 声明类型 等于 变量_function 或者 声明类型 等于 变量_parameter。
    做 内存_写8 用 偏移 加 22、0。
    索引 为 scope 的 变量数量。
    scope 的 变量数量 为 scope 的 变量数量 加 1。
    返还 索引。
完。

ScopeInfo_查找本地 接受 scope、名：
    可变 i 为 0。
    只要 i 小于 scope 的 变量数量：
        偏移 为 scope 的 变量列表 加 i 乘 48。
        若 做 字符串_相等 用 做 内存_读64 用 偏移、名：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

ScopeInfo_标记被引用 接受 scope、变量索引：
    偏移 为 scope 的 变量列表 加 变量索引 乘 48。
    做 内存_写8 用 偏移 加 22、1。
    做 内存_写32 用 偏移 加 12、位置_上下文。
    slot 为 scope 的 上下文槽数。
    做 内存_写32 用 偏移 加 16、slot。
    scope 的 上下文槽数 为 scope 的 上下文槽数 加 1。
完。

ScopeInfo_查找 接受 scope、名：
    若 scope 等于 0 或者 名 等于 0：
        返还 空。
    完。
    本地 为 做 ScopeInfo_查找本地 用 scope、名。
    若 本地 大于或等于 0：
        返还 一本字典：作用域 对应 scope、索引 对应 本地、深度 对应 0。
    完。
    若 scope 的 外部作用域 不等于 0：
        结果 为 做 ScopeInfo_查找 用 scope 的 外部作用域、名。
        若 结果 不等于 空：
            结果 的 深度 为 结果 的 深度 加 1。
            返还 结果。
        完。
    完。
    返还 空。
完。

ScopeInfo_查找并标记 接受 scope、名：
    结果 为 做 ScopeInfo_查找 用 scope、名。
    若 结果 等于 空：
        返还 空。
    完。
    若 结果 的 深度 大于 0：
        做 ScopeInfo_标记被引用 用 结果 的 作用域、结果 的 索引。
    完。
    返还 结果。
完。

ScopeInfo_作用域深度 接受 scope：
    可变 深度 为 0。
    可变 当前 为 scope。
    只要 当前 不等于 0：
        深度 为 深度 加 1。
        当前 为 当前 的 外部作用域。
    完。
    返还 深度。
完。

Context 是 一种 事物，含 外部上下文、作用域信息、槽、槽数。

Context_创建 接受 外部、作用域信息：
    ctx 为 做 堆_分配 用 Context。
    ctx 的 外部上下文 为 外部。
    ctx 的 作用域信息 为 作用域信息。
    ctx 的 槽数 为 作用域信息 的 上下文槽数。
    若 ctx 的 槽数 大于 0：
        ctx 的 槽 为 做 堆_分配_数组 用 ctx 的 槽数 乘 8。
        可变 i 为 0。
        只要 i 小于 ctx 的 槽数：
            做 内存_写64 用 ctx 的 槽 加 i 乘 8、0。
            i 为 i 加 1。
        完。
    否则：
        ctx 的 槽 为 0。
    完。
    返还 ctx。
完。

Context_获取 接受 ctx、槽索引：
    若 ctx 等于 0 或者 槽索引 大于或等于 ctx 的 槽数：
        返还 0。
    完。
    返还 做 内存_读64 用 ctx 的 槽 加 槽索引 乘 8。
完。

Context_设置 接受 ctx、槽索引、值：
    若 ctx 等于 0 或者 槽索引 大于或等于 ctx 的 槽数：
        返还。
    完。
    做 内存_写64 用 ctx 的 槽 加 槽索引 乘 8、值。
完。

Context_查找深度 接受 ctx、深度：
    可变 当前 为 ctx。
    可变 i 为 0。
    只要 i 小于 深度 并且 当前 不等于 0：
        当前 为 当前 的 外部上下文。
        i 为 i 加 1。
    完。
    返还 当前。
完。

Context_获取跨域 接受 ctx、深度、槽索引：
    目标 为 做 Context_查找深度 用 ctx、深度。
    若 目标 等于 0：
        返还 0。
    完。
    返还 做 Context_获取 用 目标、槽索引。
完。

Context_设置跨域 接受 ctx、深度、槽索引、值：
    目标 为 做 Context_查找深度 用 ctx、深度。
    若 目标 等于 0：
        返还。
    完。
    做 Context_设置 用 目标、槽索引、值。
完。

ScopeInfo_检查TDZ 接受 scope、变量索引：
    若 scope 等于 0 或者 变量索引 小于 0 或者 变量索引 大于或等于 scope 的 变量数量：
        返还 假。
    完。
    偏移 为 scope 的 变量列表 加 变量索引 乘 48。
    声明类型 为 做 内存_读32 用 偏移 加 8。
    已初始化 为 做 内存_读8 用 偏移 加 21。
    若 (声明类型 等于 变量_let 或者 声明类型 等于 变量_const) 并且 非 已初始化：
        返还 真。
    完。
    返还 假。
完。

ScopeInfo_标记已初始化 接受 scope、变量索引：
    若 scope 等于 0 或者 变量索引 小于 0：
        返还。
    完。
    偏移 为 scope 的 变量列表 加 变量索引 乘 48。
    做 内存_写8 用 偏移 加 21、1。
完。

ScopeInfo_是常量 接受 scope、变量索引：
    若 scope 等于 0 或者 变量索引 小于 0：
        返还 假。
    完。
    偏移 为 scope 的 变量列表 加 变量索引 乘 48。
    返还 做 内存_读32 用 偏移 加 8 等于 变量_const。
完。

ScopeInfo_是可变 接受 scope、变量索引：
    若 scope 等于 0 或者 变量索引 小于 0：
        返还 假。
    完。
    偏移 为 scope 的 变量列表 加 变量索引 乘 48。
    返还 做 内存_读8 用 偏移 加 20 不等于 0。
完。

ScopeInfo_收集var声明 接受 scope：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 scope 的 变量数量：
        偏移 为 scope 的 变量列表 加 i 乘 48。
        声明类型 为 做 内存_读32 用 偏移 加 8。
        若 声明类型 等于 变量_var 或者 声明类型 等于 变量_function：
            做 追加 用 结果、一本字典：名 对应 做 内存_读64 用 偏移、索引 对应 i、类型 对应 声明类型。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

ScopeInfo_收集let_const声明 接受 scope：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 scope 的 变量数量：
        偏移 为 scope 的 变量列表 加 i 乘 48。
        声明类型 为 做 内存_读32 用 偏移 加 8。
        若 声明类型 等于 变量_let 或者 声明类型 等于 变量_const：
            做 追加 用 结果、一本字典：名 对应 做 内存_读64 用 偏移、索引 对应 i、类型 对应 声明类型。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

ScopeInfo_创建arguments 接受 scope、参数值、参数数：
    若 scope 等于 0 或者 非 scope 的 有arguments：
        返还 0。
    完。
    args_obj 为 做 堆_分配 用 32。
    做 内存_写32 用 args_obj、参数数。
    可变 i 为 0。
    只要 i 小于 参数数：
        做 内存_写64 用 args_obj 加 8 加 i 乘 8、参数值 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 args_obj。
完。

Arguments_获取 接受 args、索引：
    若 args 等于 0：
        返还 0。
    完。
    长度 为 做 内存_读32 用 args。
    若 索引 大于或等于 长度：
        返还 0。
    完。
    返还 做 内存_读64 用 args 加 8 加 索引 乘 8。
完。

Arguments_长度 接受 args：
    若 args 等于 0：
        返还 0。
    完。
    返还 做 内存_读32 用 args。
完。

ScopeInfo_类型名 接受 类型：
    若 类型 等于 作用域_全局：
        返还 「global」。
    完。
    若 类型 等于 作用域_函数：
        返还 「function」。
    完。
    若 类型 等于 作用域_块：
        返还 「block」。
    完。
    若 类型 等于 作用域_模块：
        返还 「module」。
    完。
    若 类型 等于 作用域_With：
        返还 「with」。
    完。
    若 类型 等于 作用域_Catch：
        返还 「catch」。
    完。
    若 类型 等于 作用域_Eval：
        返还 「eval」。
    完。
    返还 「unknown」。
完。

ScopeInfo_变量声明类型名 接受 类型：
    若 类型 等于 变量_var：
        返还 「var」。
    完。
    若 类型 等于 变量_let：
        返还 「let」。
    完。
    若 类型 等于 变量_const：
        返还 「const」。
    完。
    若 类型 等于 变量_function：
        返还 「function」。
    完。
    若 类型 等于 变量_parameter：
        返还 「parameter」。
    完。
    若 类型 等于 变量_class：
        返还 「class」。
    完。
    返还 「unknown」。
完。

ScopeInfo_获取变量名 接受 scope、索引：
    若 scope 等于 0 或者 索引 小于 0 或者 索引 大于或等于 scope 的 变量数量：
        返还 「」。
    完。
    返还 做 内存_读64 用 scope 的 变量列表 加 索引 乘 48。
完。

ScopeInfo_获取变量声明类型 接受 scope、索引：
    若 scope 等于 0 或者 索引 小于 0 或者 索引 大于或等于 scope 的 变量数量：
        返还 负 1。
    完。
    返还 做 内存_读32 用 scope 的 变量列表 加 索引 乘 48 加 8。
完。

ScopeInfo_是严格 接受 scope：
    若 scope 等于 0：
        返还 假。
    完。
    返还 scope 的 是严格模式。
完。

ScopeInfo_设置严格 接受 scope：
    若 scope 等于 0：
        返还。
    完。
    scope 的 是严格模式 为 真。
完。

可变 作用域_创建总数 为 0。

可变 作用域_变量查找总数 为 0。

可变 作用域_TDZ违规数 为 0。

可变 Context_创建总数 为 0。

作用域_统计 接受 零参：
    返还 一本字典：作用域创建 对应 作用域_创建总数、变量查找 对应 作用域_变量查找总数、TDZ违规 对应 作用域_TDZ违规数、Context创建 对应 Context_创建总数。
完。

作用域_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 作用域_TDZ违规数 大于 100：
        做 追加 用 问题、「TDZ违规过多: 」 加 做 整数转字符串 用 作用域_TDZ违规数。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「作用域链健康」。
    完。
    返还 问题。
完。

作用域_重置统计 接受 零参：
    作用域_创建总数 为 0。
    作用域_变量查找总数 为 0。
    作用域_TDZ违规数 为 0。
    Context_创建总数 为 0。
完。

ScopeInfo_诊断 接受 scope：
    若 scope 等于 0：
        返还 「作用域为空」。
    完。
    返还 一本字典：类型 对应 做 ScopeInfo_类型名 用 scope 的 类型、严格模式 对应 scope 的 是严格模式、变量数 对应 scope 的 变量数量、上下文槽 对应 scope 的 上下文槽数、深度 对应 做 ScopeInfo_作用域深度 用 scope、有arguments 对应 scope 的 有arguments、函数名 对应 scope 的 函数名。
完。

作用域_完整统计 接受 零参：
    返还 一组：作用域_创建总数、作用域_变量查找总数、作用域_TDZ违规数、Context_创建总数。
完。

可变 AI_域信息创建_调用数 为 0。

可变 AI_域信息创建_异常数 为 0。

AI_域信息创建_监控 接受 零参：
    AI_域信息创建_调用数 为 AI_域信息创建_调用数 加 1。
    返还 AI_域信息创建_异常数 等于 0。
完。

AI_域信息创建_健康检查 接受 零参：
    若 AI_域信息创建_调用数 大于 0 并且 AI_域信息创建_异常数 乘 100 除以 AI_域信息创建_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
