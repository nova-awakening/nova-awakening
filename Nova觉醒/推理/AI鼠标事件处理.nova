《AI鼠标事件处理》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《.·AI总线初始化》。

引用《.·余弦相似度》。

引用《.·定点Q算术》。

引用《.·滚动预测》。

引用《..·中枢·字符串替换》。

引用《..·中枢·激活函数层》。

引用《.·注视点模型》。

引用《.·运动掩蔽》。

引用《.·行为预测器》。

Q_SHIFT 为 10。

Q_ONE 为 1024。

Q_HALF 为 512。

动作_空闲 为 0。

动作_点击 为 1。

动作_滚动 为 2。

动作_输入 为 3。

动作_悬停 为 4。

动作_拖拽 为 5。

动作_缩放 为 6。

动作_双击 为 7。

动作_右键 为 8。

动作_返回 为 9。

渲染级别_高 为 3。

渲染级别_中 为 2。

渲染级别_低 为 1。

渲染级别_极低 为 0。

触摸_最大点数 为 10。

手势_无 为 0。

手势_捏合 为 1。

手势_旋转 为 2。

手势_滑动 为 3。

手势_长按 为 4。

鼠标轨迹_最大长度 为 64。

鼠标速度_快阈值 为 50。

鼠标速度_慢阈值 为 5。

错误_无效参数 为 负 1。

错误_未初始化 为 负 2。

鼠标轨迹点 是 一种 事物，含 x、y、时间戳、速度、方向角。

鼠标轨迹分析器 是 一种 事物，含 轨迹缓冲、缓冲长度、上次x、上次y、上次时间戳、累计距离、平均速度、方向稳定性、是否拖拽中、停留计数、热区列表、热区数量。

热区 是 一种 事物，含 中心x、中心y、半径、访问次数、上次访问帧、权重。

触摸事件 是 一种 事物，含 触摸ID、x、y、压力、时间戳。

手势状态 是 一种 事物，含 当前手势、手势进度、起始距离、当前距离、起始角度、当前角度、触摸点数、触摸点列表。

可变 全局轨迹分析器 为 0。

鼠标轨迹分析器_创建 接受 零参：
    分析器 为 新建 鼠标轨迹分析器。
    分析器 的 轨迹缓冲 为 一组：（空）。
    分析器 的 缓冲长度 为 0。
    分析器 的 上次x 为 0。
    分析器 的 上次y 为 0。
    分析器 的 上次时间戳 为 0。
    分析器 的 累计距离 为 0。
    分析器 的 平均速度 为 0。
    分析器 的 方向稳定性 为 Q_ONE。
    分析器 的 是否拖拽中 为 假。
    分析器 的 停留计数 为 0。
    分析器 的 热区列表 为 一组：（空）。
    分析器 的 热区数量 为 0。
    全局轨迹分析器 为 分析器。
    返还 分析器。
完。

鼠标轨迹分析器_销毁 接受 分析器：
    若 分析器 等于 0：
        返还。
    完。
    全局轨迹分析器 为 0。
完。

鼠标轨迹分析器_重置 接受 分析器：
    若 分析器 等于 0：
        返还。
    完。
    分析器 的 轨迹缓冲 为 一组：（空）。
    分析器 的 缓冲长度 为 0。
    分析器 的 累计距离 为 0。
    分析器 的 平均速度 为 0。
    分析器 的 方向稳定性 为 Q_ONE。
    分析器 的 停留计数 为 0。
完。

AI总线_鼠标移动 接受 bus、x、y：
    若 bus 等于 0：
        返还。
    完。
    做 注视点模型_更新 用 bus 的 质量分配器 的 注视点、x、y。
    若 全局轨迹分析器 不等于 0：
        做 鼠标轨迹_记录点 用 全局轨迹分析器、x、y、bus 的 帧计数。
    完。
    若 全局轨迹分析器 不等于 0：
        若 全局轨迹分析器 的 平均速度 大于 鼠标速度_快阈值：
            若 bus 的 AI推理预算 大于 1000：
                bus 的 AI推理预算 为 bus 的 AI推理预算 减 50。
            完。
        否则若 全局轨迹分析器 的 平均速度 小于 鼠标速度_慢阈值：
            若 bus 的 AI推理预算 小于 4000：
                bus 的 AI推理预算 为 bus 的 AI推理预算 加 25。
            完。
        完。
    完。
    做 AI总线_子系统心跳 用 bus、2。
完。

鼠标轨迹_记录点 接受 分析器、x、y、时间戳：
    若 分析器 等于 0：
        返还。
    完。
    dx 为 x 减 分析器 的 上次x。
    dy 为 y 减 分析器 的 上次y。
    距离平方 为 dx 乘 dx 加 dy 乘 dy。
    距离 为 做 整数平方根 用 距离平方。
    dt 为 时间戳 减 分析器 的 上次时间戳。
    可变 速度 为 0。
    若 dt 大于 0：
        速度 为 距离 乘 Q_ONE 除以 dt。
    完。
    方向角 为 做 快速反正切 用 dy、dx。
    点 为 新建 鼠标轨迹点。
    点 的 x 为 x。
    点 的 y 为 y。
    点 的 时间戳 为 时间戳。
    点 的 速度 为 速度。
    点 的 方向角 为 方向角。
    若 分析器 的 缓冲长度 小于 鼠标轨迹_最大长度：
        做 追加 用 分析器 的 轨迹缓冲、点。
        分析器 的 缓冲长度 为 分析器 的 缓冲长度 加 1。
    否则：
        做 删除 用 分析器 的 轨迹缓冲、0。
        做 追加 用 分析器 的 轨迹缓冲、点。
    完。
    分析器 的 平均速度 为 (分析器 的 平均速度 乘 819 加 速度 乘 205) 除以 Q_ONE。
    若 分析器 的 缓冲长度 大于 2：
        上个点 为 分析器 的 轨迹缓冲 的 第 分析器 的 缓冲长度 减 2 项。
        角度差 为 方向角 减 上个点 的 方向角。
        若 角度差 小于 0：
            角度差 为 0 减 角度差。
        完。
        若 角度差 大于 Q_HALF：
            分析器 的 方向稳定性 为 分析器 的 方向稳定性 乘 900 除以 Q_ONE。
        否则：
            分析器 的 方向稳定性 为 (分析器 的 方向稳定性 乘 900 加 Q_ONE 乘 124) 除以 Q_ONE。
        完。
    完。
    分析器 的 累计距离 为 分析器 的 累计距离 加 距离。
    若 距离 小于 3：
        分析器 的 停留计数 为 分析器 的 停留计数 加 1。
    否则：
        分析器 的 停留计数 为 0。
    完。
    做 鼠标轨迹_更新热区 用 分析器、x、y、时间戳。
    分析器 的 上次x 为 x。
    分析器 的 上次y 为 y。
    分析器 的 上次时间戳 为 时间戳。
完。

鼠标轨迹_更新热区 接受 分析器、x、y、帧计数：
    若 分析器 等于 0：
        返还。
    完。
    可变 i 为 0。
    可变 找到 为 假。
    只要 i 小于 分析器 的 热区数量：
        区 为 分析器 的 热区列表 的 第 i 项。
        dx 为 x 减 区 的 中心x。
        dy 为 y 减 区 的 中心y。
        若 dx 乘 dx 加 dy 乘 dy 小于 区 的 半径 乘 区 的 半径：
            区 的 访问次数 为 区 的 访问次数 加 1。
            区 的 上次访问帧 为 帧计数。
            区 的 权重 为 (区 的 权重 乘 900 加 Q_ONE 乘 124) 除以 Q_ONE。
            若 区 的 权重 大于 Q_ONE：
                区 的 权重 为 Q_ONE。
            完。
            找到 为 真。
        完。
        i 为 i 加 1。
    完。
    若 找到 等于 假 并且 分析器 的 热区数量 小于 32：
        新区 为 新建 热区。
        新区 的 中心x 为 x。
        新区 的 中心y 为 y。
        新区 的 半径 为 48。
        新区 的 访问次数 为 1。
        新区 的 上次访问帧 为 帧计数。
        新区 的 权重 为 Q_HALF。
        做 追加 用 分析器 的 热区列表、新区。
        分析器 的 热区数量 为 分析器 的 热区数量 加 1。
    完。
完。

热区分析器_获取最热区域 接受 分析器：
    若 分析器 等于 0 或者 分析器 的 热区数量 等于 0：
        返还 0。
    完。
    可变 最热索引 为 0。
    可变 最大权重 为 0。
    可变 i 为 0。
    只要 i 小于 分析器 的 热区数量：
        若 分析器 的 热区列表 的 第 i 项 的 权重 大于 最大权重：
            最大权重 为 分析器 的 热区列表 的 第 i 项 的 权重。
            最热索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 分析器 的 热区列表 的 第 最热索引 项。
完。

AI总线_记录行为动作 接受 bus、当前动作：
    若 bus 等于 0 或者 bus 的 行为预测器 等于 0：
        返还 动作_空闲。
    完。
    预测 为 做 行为预测器_观察并预测 用 bus 的 行为预测器、当前动作。
    隐藏 为 做 行为预测器_当前隐藏 用 bus 的 行为预测器。
    若 隐藏 等于 0：
        返还 预测。
    完。
    若 bus 的 行为预测器 的 序列位置 大于 注意力_最大序列：
        做 AI总线_重置行为序列缓存 用 bus。
        bus 的 行为预测器 的 序列位置 为 1。
    完。
    可变 位置 为 0。
    若 bus 的 行为预测器 的 序列位置 大于 0：
        位置 为 bus 的 行为预测器 的 序列位置 减 1。
    完。
    上下文隐藏 为 0。
    若 bus 的 行为序列模型 不等于 0 并且 bus 的 降级模式 小于 3：
        上下文隐藏 为 做 AI总线_行为序列增量推理 用 bus、隐藏、位置。
    完。
    若 上下文隐藏 不等于 0：
        做 行为预测器_写入上下文隐藏 用 bus 的 行为预测器、上下文隐藏。
        预测 为 做 行为预测器_输出头预测 用 bus 的 行为预测器、上下文隐藏。
        做 Tensor_释放 用 上下文隐藏。
    否则：
        做 行为预测器_写入上下文隐藏 用 bus 的 行为预测器、隐藏。
    完。
    做 Tensor_释放 用 隐藏。
    bus 的 行为预测器 的 上次预测 为 预测。
    返还 预测。
完。

可变 上次点击时间 为 0。

可变 上次点击x 为 0。

可变 上次点击y 为 0。

AI总线_点击 接受 bus、x、y：
    若 bus 等于 0：
        返还。
    完。
    做 注视点模型_更新 用 bus 的 质量分配器 的 注视点、x、y。
    做 AI总线_记录行为动作 用 bus、动作_点击。
    dt 为 bus 的 帧计数 减 上次点击时间。
    dx 为 x 减 上次点击x。
    dy 为 y 减 上次点击y。
    若 dt 小于 20 并且 dx 乘 dx 加 dy 乘 dy 小于 100：
        做 AI总线_记录行为动作 用 bus、动作_双击。
        做 打印 用 「[AI感知] 检测到双击事件」。
    完。
    上次点击时间 为 bus 的 帧计数。
    上次点击x 为 x。
    上次点击y 为 y。
完。

AI总线_右键 接受 bus、x、y：
    若 bus 等于 0：
        返还。
    完。
    做 注视点模型_更新 用 bus 的 质量分配器 的 注视点、x、y。
    做 AI总线_记录行为动作 用 bus、动作_右键。
完。

AI总线_滚动 接受 bus、滚动Y、时间戳：
    若 bus 等于 0：
        返还。
    完。
    做 滚动预测器_更新 用 bus 的 滚动预测器、滚动Y、时间戳。
    做 AI总线_记录行为动作 用 bus、动作_滚动。
    bus 的 滚动速度 为 bus 的 滚动预测器 的 当前速度 右移 Q_SHIFT。
    若 bus 的 滚动速度 大于 30：
        若 bus 的 降级模式 小于 1：
            bus 的 降级模式 为 1。
        完。
    否则若 bus 的 滚动速度 小于 5 并且 bus 的 降级模式 大于 0：
        bus 的 降级模式 为 bus 的 降级模式 减 1。
    完。
完。

AI总线_键盘输入 接受 bus：
    若 bus 等于 0：
        返还。
    完。
    做 AI总线_记录行为动作 用 bus、动作_输入。
完。

AI总线_悬停 接受 bus、x、y：
    若 bus 等于 0：
        返还。
    完。
    做 注视点模型_更新 用 bus 的 质量分配器 的 注视点、x、y。
    做 AI总线_记录行为动作 用 bus、动作_悬停。
    若 全局轨迹分析器 不等于 0：
        全局轨迹分析器 的 停留计数 为 全局轨迹分析器 的 停留计数 加 1。
    完。
完。

AI总线_拖拽开始 接受 bus、x、y：
    若 bus 等于 0：
        返还。
    完。
    若 全局轨迹分析器 不等于 0：
        全局轨迹分析器 的 是否拖拽中 为 真。
    完。
    做 AI总线_记录行为动作 用 bus、动作_拖拽。
完。

AI总线_拖拽结束 接受 bus、x、y：
    若 bus 等于 0：
        返还。
    完。
    若 全局轨迹分析器 不等于 0：
        全局轨迹分析器 的 是否拖拽中 为 假。
    完。
完。

可变 全局手势状态 为 0。

手势状态_创建 接受 零参：
    状态 为 新建 手势状态。
    状态 的 当前手势 为 手势_无。
    状态 的 手势进度 为 0。
    状态 的 起始距离 为 0。
    状态 的 当前距离 为 0。
    状态 的 起始角度 为 0。
    状态 的 当前角度 为 0。
    状态 的 触摸点数 为 0。
    状态 的 触摸点列表 为 一组：（空）。
    全局手势状态 为 状态。
    返还 状态。
完。

AI总线_触摸开始 接受 bus、触摸ID、x、y、压力：
    若 bus 等于 0：
        返还。
    完。
    若 全局手势状态 等于 0：
        做 手势状态_创建。
    完。
    状态 为 全局手势状态。
    若 状态 的 触摸点数 大于或等于 触摸_最大点数：
        返还。
    完。
    触摸 为 新建 触摸事件。
    触摸 的 触摸ID 为 触摸ID。
    触摸 的 x 为 x。
    触摸 的 y 为 y。
    触摸 的 压力 为 压力。
    触摸 的 时间戳 为 bus 的 帧计数。
    做 追加 用 状态 的 触摸点列表、触摸。
    状态 的 触摸点数 为 状态 的 触摸点数 加 1。
    若 状态 的 触摸点数 等于 2：
        p0 为 状态 的 触摸点列表 的 第 0 项。
        p1 为 状态 的 触摸点列表 的 第 1 项。
        dx 为 p1 的 x 减 p0 的 x。
        dy 为 p1 的 y 减 p0 的 y。
        状态 的 起始距离 为 做 整数平方根 用 dx 乘 dx 加 dy 乘 dy。
        状态 的 起始角度 为 做 快速反正切 用 dy、dx。
    完。
完。

AI总线_触摸移动 接受 bus、触摸ID、x、y：
    若 bus 等于 0 或者 全局手势状态 等于 0：
        返还。
    完。
    状态 为 全局手势状态。
    可变 i 为 0。
    只要 i 小于 状态 的 触摸点数：
        若 状态 的 触摸点列表 的 第 i 项 的 触摸ID 等于 触摸ID：
            状态 的 触摸点列表 的 第 i 项 的 x 为 x。
            状态 的 触摸点列表 的 第 i 项 的 y 为 y。
        完。
        i 为 i 加 1。
    完。
    若 状态 的 触摸点数 等于 2：
        p0 为 状态 的 触摸点列表 的 第 0 项。
        p1 为 状态 的 触摸点列表 的 第 1 项。
        dx 为 p1 的 x 减 p0 的 x。
        dy 为 p1 的 y 减 p0 的 y。
        状态 的 当前距离 为 做 整数平方根 用 dx 乘 dx 加 dy 乘 dy。
        状态 的 当前角度 为 做 快速反正切 用 dy、dx。
        距离变化 为 状态 的 当前距离 减 状态 的 起始距离。
        若 距离变化 大于 20 或者 距离变化 小于 负 20：
            状态 的 当前手势 为 手势_捏合。
            做 AI总线_记录行为动作 用 bus、动作_缩放。
        完。
    完。
完。

AI总线_触摸结束 接受 bus、触摸ID：
    若 bus 等于 0 或者 全局手势状态 等于 0：
        返还。
    完。
    状态 为 全局手势状态。
    可变 i 为 0。
    只要 i 小于 状态 的 触摸点数：
        若 状态 的 触摸点列表 的 第 i 项 的 触摸ID 等于 触摸ID：
            做 删除 用 状态 的 触摸点列表、i。
            状态 的 触摸点数 为 状态 的 触摸点数 减 1。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 状态 的 触摸点数 等于 0：
        状态 的 当前手势 为 手势_无。
    完。
完。

AI总线_语义分类 接受 bus、节点：
    若 bus 等于 0 或者 节点 等于 0：
        返还 0。
    完。
    若 bus 的 降级模式 大于或等于 3：
        返还 0。
    完。
    嵌入 为 做 语义嵌入_计算节点嵌入 用 bus 的 语义引擎、节点。
    若 嵌入 等于 0：
        返还 0。
    完。
    角色 为 做 语义嵌入_分类节点角色 用 bus 的 语义引擎、嵌入。
    做 Tensor_释放 用 嵌入。
    bus 的 推理次数 为 bus 的 推理次数 加 1。
    bus 的 累计推理次数 为 bus 的 累计推理次数 加 1。
    返还 角色。
完。

AI总线_DOM语义分类_GQA 接受 bus、节点嵌入、位置：
    若 bus 等于 0 或者 节点嵌入 等于 0：
        返还 0。
    完。
    若 bus 的 降级模式 大于或等于 3：
        返还 0。
    完。
    上下文嵌入 为 做 AI总线_DOM注意力增量推理 用 bus、节点嵌入、位置。
    可变 角色 为 0。
    若 上下文嵌入 不等于 0：
        角色 为 做 语义嵌入_分类节点角色 用 bus 的 语义引擎、上下文嵌入。
        做 Tensor_释放 用 上下文嵌入。
    否则：
        角色 为 做 语义嵌入_分类节点角色 用 bus 的 语义引擎、节点嵌入。
    完。
    bus 的 推理次数 为 bus 的 推理次数 加 1。
    bus 的 累计推理次数 为 bus 的 累计推理次数 加 1。
    返还 角色。
完。

AI总线_注意力权重 接受 bus、节点特征：
    若 bus 等于 0 或者 bus 的 注意力网络 等于 0：
        返还 Q_HALF。
    完。
    若 bus 的 降级模式 大于或等于 2：
        返还 Q_HALF。
    完。
    输出 为 做 Nova多层感知器_前向 用 bus 的 注意力网络、节点特征。
    分数 为 做 Tensor_读 用 输出、0、0。
    做 Tensor_释放 用 输出。
    bus 的 推理次数 为 bus 的 推理次数 加 1。
    bus 的 累计推理次数 为 bus 的 累计推理次数 加 1。
    返还 分数。
完。

AI总线_渲染质量 接受 bus、区域x、区域y、区域w、区域h、语义重要性、是否可见：
    若 bus 等于 0：
        返还 渲染级别_中。
    完。
    若 bus 的 降级模式 大于或等于 3：
        若 是否可见 等于 真：
            返还 渲染级别_中。
        完。
        返还 渲染级别_低。
    完。
    运动降级 为 做 运动掩蔽_质量降级 用 bus 的 滚动速度。
    可变 目标质量 为 做 质量分配器_决定质量 用 bus 的 质量分配器、区域x、区域y、区域w、区域h、语义重要性、是否可见。
    目标质量 为 目标质量 减 运动降级。
    若 目标质量 小于 0：
        目标质量 为 0。
    完。
    若 全局轨迹分析器 不等于 0：
        可变 j 为 0。
        只要 j 小于 全局轨迹分析器 的 热区数量：
            区 为 全局轨迹分析器 的 热区列表 的 第 j 项。
            若 区域x 小于 区 的 中心x 加 区 的 半径 并且 区域x 加 区域w 大于 区 的 中心x 减 区 的 半径：
                若 区域y 小于 区 的 中心y 加 区 的 半径 并且 区域y 加 区域h 大于 区 的 中心y 减 区 的 半径：
                    加成 为 区 的 权重 乘 渲染级别_高 除以 Q_ONE。
                    目标质量 为 目标质量 加 加成。
                完。
            完。
            j 为 j 加 1。
        完。
    完。
    若 目标质量 大于 渲染级别_高：
        目标质量 为 渲染级别_高。
    完。
    区域索引 为 区域y 除以 64 乘 16 加 区域x 除以 64。
    若 区域索引 大于或等于 256：
        区域索引 为 255。
    完。
    最终质量 为 做 质量平滑器_平滑 用 bus 的 质量平滑器、区域索引、目标质量。
    bus 的 推理次数 为 bus 的 推理次数 加 1。
    bus 的 累计推理次数 为 bus 的 累计推理次数 加 1。
    返还 最终质量。
完。

快速反正切 接受 y、x：
    若 x 等于 0 并且 y 等于 0：
        返还 0。
    完。
    若 x 等于 0：
        若 y 大于 0：
            返还 Q_HALF。
        完。
        返还 0 减 Q_HALF。
    完。
    可变 角度 为 y 乘 Q_ONE 除以 x。
    若 角度 大于 Q_ONE：
        角度 为 Q_ONE。
    完。
    若 角度 小于 0 减 Q_ONE：
        角度 为 0 减 Q_ONE。
    完。
    返还 角度。
完。

AI感知层_打印统计 接受 零参：
    若 全局轨迹分析器 等于 0：
        做 打印 用 「[AI感知] 轨迹分析器未初始化」。
        返还。
    完。
    分析器 为 全局轨迹分析器。
    做 打印 用 「[AI感知] ═══ 感知层统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  轨迹缓冲点数: 」、做 转字符串 用 分析器 的 缓冲长度。
    做 打印 用 做 字符串拼接 用 「  累计移动距离: 」、做 字符串拼接 用 做 转字符串 用 分析器 的 累计距离、「px」。
    做 打印 用 做 字符串拼接 用 「  平均鼠标速度: 」、做 转字符串 用 分析器 的 平均速度。
    做 打印 用 做 字符串拼接 用 「  方向稳定性: 」、做 转字符串 用 分析器 的 方向稳定性。
    做 打印 用 做 字符串拼接 用 「  热区数量: 」、做 转字符串 用 分析器 的 热区数量。
    做 打印 用 做 字符串拼接 用 「  是否拖拽中: 」、做 转字符串 用 分析器 的 是否拖拽中。
    做 打印 用 做 字符串拼接 用 「  鼠标停留帧数: 」、做 转字符串 用 分析器 的 停留计数。
    做 打印 用 「[AI感知] ═══════════════════」。
完。

本章 收束。
