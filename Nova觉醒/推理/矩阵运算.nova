《矩阵运算》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《.·定点Q算术》。

引用《..·感知·内核直连层》。

引用《..·中枢·内核打印》。

引用《.·权重持久》。

引用《..·中枢·整数转字符串》。

矩阵运算统计 是 一种 事物，含 矩阵乘次数、加法次数、转置次数、激活次数、归一化次数、卷积次数、池化次数、总乘加运算、总元素处理。

可变 全局_矩阵统计 为 空。

矩阵统计_初始化 接受 零参：
    若 全局_矩阵统计 等于 空：
        全局_矩阵统计 为 新建 矩阵运算统计。
        全局_矩阵统计 的 矩阵乘次数 为 0。
        全局_矩阵统计 的 加法次数 为 0。
        全局_矩阵统计 的 转置次数 为 0。
        全局_矩阵统计 的 激活次数 为 0。
        全局_矩阵统计 的 归一化次数 为 0。
        全局_矩阵统计 的 卷积次数 为 0。
        全局_矩阵统计 的 池化次数 为 0。
        全局_矩阵统计 的 总乘加运算 为 0。
        全局_矩阵统计 的 总元素处理 为 0。
    完。
完。

Tensor_矩阵乘 接受 A、B：
    做 矩阵统计_初始化。
    M 为 A 的 行数。
    K 为 A 的 列数。
    N 为 B 的 列数。
    C 为 做 Tensor_创建 用 M、N。
    可变 i 为 0。
    只要 i 小于 M：
        可变 j 为 0。
        只要 j 小于 N：
            可变 累加 为 0。
            可变 k 为 0。
            只要 k 小于 K：
                a 为 做 Tensor_读 用 A、i、k。
                b 为 做 Tensor_读 用 B、k、j。
                累加 为 累加 加 a 乘 b。
                k 为 k 加 1。
            完。
            做 Tensor_写 用 C、i、j、累加 右移 Q_SHIFT。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    全局_矩阵统计 的 矩阵乘次数 为 全局_矩阵统计 的 矩阵乘次数 加 1。
    全局_矩阵统计 的 总乘加运算 为 全局_矩阵统计 的 总乘加运算 加 M 乘 K 乘 N。
    返还 C。
完。

Tensor_矩阵向量乘 接受 A、x：
    做 矩阵统计_初始化。
    M 为 A 的 行数。
    K 为 A 的 列数。
    y 为 做 Tensor_创建 用 M、1。
    可变 i 为 0。
    只要 i 小于 M：
        可变 累加 为 0。
        可变 k 为 0。
        只要 k 小于 K：
            累加 为 累加 加 做 Tensor_读 用 A、i、k 乘 做 Tensor_读 用 x、k、0。
            k 为 k 加 1。
        完。
        做 Tensor_写 用 y、i、0、累加 右移 Q_SHIFT。
        i 为 i 加 1。
    完。
    全局_矩阵统计 的 矩阵乘次数 为 全局_矩阵统计 的 矩阵乘次数 加 1。
    全局_矩阵统计 的 总乘加运算 为 全局_矩阵统计 的 总乘加运算 加 M 乘 K。
    返还 y。
完。

Tensor_加偏置 接受 C、bias：
    可变 i 为 0。
    只要 i 小于 C 的 行数：
        可变 j 为 0。
        只要 j 小于 C 的 列数：
            旧 为 做 Tensor_读 用 C、i、j。
            b 为 做 Tensor_读 用 bias、0、j。
            做 Tensor_写 用 C、i、j、旧 加 b。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

Tensor_加 接受 A、B：
    做 矩阵统计_初始化。
    C 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        a 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        b 为 做 内存_读32有符号 用 B 的 数据 加 i 乘 4。
        做 内存_写32 用 C 的 数据 加 i 乘 4、a 加 b。
        i 为 i 加 1。
    完。
    全局_矩阵统计 的 加法次数 为 全局_矩阵统计 的 加法次数 加 1。
    全局_矩阵统计 的 总元素处理 为 全局_矩阵统计 的 总元素处理 加 总。
    返还 C。
完。

Tensor_减 接受 A、B：
    做 矩阵统计_初始化。
    C 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        a 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        b 为 做 内存_读32有符号 用 B 的 数据 加 i 乘 4。
        做 内存_写32 用 C 的 数据 加 i 乘 4、a 减 b。
        i 为 i 加 1。
    完。
    全局_矩阵统计 的 加法次数 为 全局_矩阵统计 的 加法次数 加 1。
    返还 C。
完。

Tensor_逐元素乘 接受 A、B：
    C 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        a 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        b 为 做 内存_读32有符号 用 B 的 数据 加 i 乘 4。
        做 内存_写32 用 C 的 数据 加 i 乘 4、做 Q乘 用 a、b。
        i 为 i 加 1。
    完。
    返还 C。
完。

Tensor_转置 接受 A：
    做 矩阵统计_初始化。
    B 为 做 Tensor_创建 用 A 的 列数、A 的 行数。
    可变 i 为 0。
    只要 i 小于 A 的 行数：
        可变 j 为 0。
        只要 j 小于 A 的 列数：
            做 Tensor_写 用 B、j、i、做 Tensor_读 用 A、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    全局_矩阵统计 的 转置次数 为 全局_矩阵统计 的 转置次数 加 1。
    返还 B。
完。

Tensor_缩放 接受 A、scalar：
    B 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        a 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        做 内存_写32 用 B 的 数据 加 i 乘 4、做 Q乘 用 a、scalar。
        i 为 i 加 1。
    完。
    返还 B。
完。

Tensor_取反 接受 A：
    B 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        a 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        做 内存_写32 用 B 的 数据 加 i 乘 4、负 a。
        i 为 i 加 1。
    完。
    返还 B。
完。

张量_修正线性 接受 A：
    做 矩阵统计_初始化。
    B 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        v 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        若 v 小于 0：
            v 为 0。
        完。
        做 内存_写32 用 B 的 数据 加 i 乘 4、v。
        i 为 i 加 1。
    完。
    全局_矩阵统计 的 激活次数 为 全局_矩阵统计 的 激活次数 加 1。
    全局_矩阵统计 的 总元素处理 为 全局_矩阵统计 的 总元素处理 加 总。
    返还 B。
完。

张量_泄漏修正线性 接受 A、alpha：
    做 矩阵统计_初始化。
    B 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        v 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        若 v 小于 0：
            v 为 做 Q乘 用 v、alpha。
        完。
        做 内存_写32 用 B 的 数据 加 i 乘 4、v。
        i 为 i 加 1。
    完。
    全局_矩阵统计 的 激活次数 为 全局_矩阵统计 的 激活次数 加 1。
    返还 B。
完。

张量_S形 接受 A：
    做 矩阵统计_初始化。
    B 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    Q_ONE 为 1 左移 Q_SHIFT。
    Q_HALF 为 Q_ONE 除以 2。
    可变 i 为 0。
    只要 i 小于 总：
        x 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        可变 y 为 0。
        若 x 大于或等于 4 乘 Q_ONE：
            y 为 Q_ONE。
        否则若 x 小于或等于 负 4 乘 Q_ONE：
            y 为 0。
        否则：
            y 为 Q_HALF 加 x 除以 8。
            若 y 小于 0：
                y 为 0。
            完。
            若 y 大于 Q_ONE：
                y 为 Q_ONE。
            完。
        完。
        做 内存_写32 用 B 的 数据 加 i 乘 4、y。
        i 为 i 加 1。
    完。
    全局_矩阵统计 的 激活次数 为 全局_矩阵统计 的 激活次数 加 1。
    返还 B。
完。

Tensor_Tanh 接受 A：
    做 矩阵统计_初始化。
    B 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    Q_ONE 为 1 左移 Q_SHIFT。
    可变 i 为 0。
    只要 i 小于 总：
        x 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        可变 y 为 0。
        若 x 大于或等于 2 乘 Q_ONE：
            y 为 Q_ONE。
        否则若 x 小于或等于 负 2 乘 Q_ONE：
            y 为 负 Q_ONE。
        否则：
            y 为 x 除以 2。
            若 y 大于 Q_ONE：
                y 为 Q_ONE。
            完。
            若 y 小于 负 Q_ONE：
                y 为 负 Q_ONE。
            完。
        完。
        做 内存_写32 用 B 的 数据 加 i 乘 4、y。
        i 为 i 加 1。
    完。
    全局_矩阵统计 的 激活次数 为 全局_矩阵统计 的 激活次数 加 1。
    返还 B。
完。

张量_归一化指数 接受 A：
    做 矩阵统计_初始化。
    B 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    Q_ONE 为 1 左移 Q_SHIFT。
    可变 r 为 0。
    只要 r 小于 A 的 行数：
        可变 最大值 为 做 Tensor_读 用 A、r、0。
        可变 j 为 1。
        只要 j 小于 A 的 列数：
            v 为 做 Tensor_读 用 A、r、j。
            若 v 大于 最大值：
                最大值 为 v。
            完。
            j 为 j 加 1。
        完。
        可变 总和 为 0。
        j 为 0。
        只要 j 小于 A 的 列数：
            x 为 做 Tensor_读 用 A、r、j 减 最大值。
            可变 e 为 Q_ONE 加 x。
            若 e 小于 1：
                e 为 1。
            完。
            做 Tensor_写 用 B、r、j、e。
            总和 为 总和 加 e。
            j 为 j 加 1。
        完。
        若 总和 大于 0：
            j 为 0。
            只要 j 小于 A 的 列数：
                e 为 做 Tensor_读 用 B、r、j。
                做 Tensor_写 用 B、r、j、e 乘 Q_ONE 除以 总和。
                j 为 j 加 1。
            完。
        完。
        r 为 r 加 1。
    完。
    全局_矩阵统计 的 激活次数 为 全局_矩阵统计 的 激活次数 加 1。
    返还 B。
完。

Tensor_批归一化 接受 A、gamma、beta：
    做 矩阵统计_初始化。
    B 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    eps 为 1。
    可变 j 为 0。
    只要 j 小于 A 的 列数：
        可变 和 为 0。
        可变 i 为 0。
        只要 i 小于 A 的 行数：
            和 为 和 加 做 Tensor_读 用 A、i、j。
            i 为 i 加 1。
        完。
        均值 为 和 除以 A 的 行数。
        可变 方差和 为 0。
        i 为 0。
        只要 i 小于 A 的 行数：
            diff 为 做 Tensor_读 用 A、i、j 减 均值。
            方差和 为 方差和 加 做 Q乘 用 diff、diff。
            i 为 i 加 1。
        完。
        方差 为 方差和 除以 A 的 行数 加 eps。
        标准差 为 做 Q_sqrt 用 方差。
        若 标准差 等于 0：
            标准差 为 1。
        完。
        g 为 做 Tensor_读 用 gamma、0、j。
        b 为 做 Tensor_读 用 beta、0、j。
        i 为 0。
        只要 i 小于 A 的 行数：
            归一 为 (做 Tensor_读 用 A、i、j 减 均值 左移 Q_SHIFT) 除以 标准差。
            输出 为 做 Q乘 用 归一、g 加 b。
            做 Tensor_写 用 B、i、j、输出。
            i 为 i 加 1。
        完。
        j 为 j 加 1。
    完。
    全局_矩阵统计 的 归一化次数 为 全局_矩阵统计 的 归一化次数 加 1。
    返还 B。
完。

Tensor_层归一化 接受 A：
    做 矩阵统计_初始化。
    B 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    可变 i 为 0。
    只要 i 小于 A 的 行数：
        可变 和 为 0。
        可变 j 为 0。
        只要 j 小于 A 的 列数：
            和 为 和 加 做 Tensor_读 用 A、i、j。
            j 为 j 加 1。
        完。
        均值 为 和 除以 A 的 列数。
        可变 方差和 为 0。
        j 为 0。
        只要 j 小于 A 的 列数：
            diff 为 做 Tensor_读 用 A、i、j 减 均值。
            方差和 为 方差和 加 做 Q乘 用 diff、diff。
            j 为 j 加 1。
        完。
        标准差 为 做 Q_sqrt 用 方差和 除以 A 的 列数 加 1。
        若 标准差 等于 0：
            标准差 为 1。
        完。
        j 为 0。
        只要 j 小于 A 的 列数：
            做 Tensor_写 用 B、i、j、(做 Tensor_读 用 A、i、j 减 均值 左移 Q_SHIFT) 除以 标准差。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    全局_矩阵统计 的 归一化次数 为 全局_矩阵统计 的 归一化次数 加 1。
    返还 B。
完。

Tensor_最大池化2x2 接受 A：
    做 矩阵统计_初始化。
    新行 为 A 的 行数 除以 2。
    新列 为 A 的 列数 除以 2。
    B 为 做 Tensor_创建 用 新行、新列。
    可变 i 为 0。
    只要 i 小于 新行：
        可变 j 为 0。
        只要 j 小于 新列：
            a 为 做 Tensor_读 用 A、i 乘 2、j 乘 2。
            b 为 做 Tensor_读 用 A、i 乘 2、j 乘 2 加 1。
            c 为 做 Tensor_读 用 A、i 乘 2 加 1、j 乘 2。
            d 为 做 Tensor_读 用 A、i 乘 2 加 1、j 乘 2 加 1。
            可变 最大 为 a。
            若 b 大于 最大：
                最大 为 b。
            完。
            若 c 大于 最大：
                最大 为 c。
            完。
            若 d 大于 最大：
                最大 为 d。
            完。
            做 Tensor_写 用 B、i、j、最大。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    全局_矩阵统计 的 池化次数 为 全局_矩阵统计 的 池化次数 加 1。
    返还 B。
完。

Tensor_平均池化2x2 接受 A：
    做 矩阵统计_初始化。
    新行 为 A 的 行数 除以 2。
    新列 为 A 的 列数 除以 2。
    B 为 做 Tensor_创建 用 新行、新列。
    可变 i 为 0。
    只要 i 小于 新行：
        可变 j 为 0。
        只要 j 小于 新列：
            a 为 做 Tensor_读 用 A、i 乘 2、j 乘 2。
            b 为 做 Tensor_读 用 A、i 乘 2、j 乘 2 加 1。
            c 为 做 Tensor_读 用 A、i 乘 2 加 1、j 乘 2。
            d 为 做 Tensor_读 用 A、i 乘 2 加 1、j 乘 2 加 1。
            做 Tensor_写 用 B、i、j、(a 加 b 加 c 加 d) 除以 4。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    全局_矩阵统计 的 池化次数 为 全局_矩阵统计 的 池化次数 加 1。
    返还 B。
完。

Tensor_范数 接受 A：
    总 为 A 的 行数 乘 A 的 列数。
    可变 平方和 为 0。
    可变 i 为 0。
    只要 i 小于 总：
        v 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        平方和 为 平方和 加 做 Q乘 用 v、v。
        i 为 i 加 1。
    完。
    返还 做 Q_sqrt 用 平方和。
完。

Tensor_求和 接受 A：
    总 为 A 的 行数 乘 A 的 列数。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 总：
        和 为 和 加 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        i 为 i 加 1。
    完。
    返还 和。
完。

Tensor_最大值 接受 A：
    总 为 A 的 行数 乘 A 的 列数。
    若 总 等于 0：
        返还 0。
    完。
    可变 最大 为 做 内存_读32有符号 用 A 的 数据。
    可变 i 为 1。
    只要 i 小于 总：
        v 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        若 v 大于 最大：
            最大 为 v。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

Tensor_argmax 接受 A：
    总 为 A 的 行数 乘 A 的 列数。
    若 总 等于 0：
        返还 负 1。
    完。
    可变 最大 为 做 内存_读32有符号 用 A 的 数据。
    可变 最大索引 为 0。
    可变 i 为 1。
    只要 i 小于 总：
        v 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        若 v 大于 最大：
            最大 为 v。
            最大索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最大索引。
完。

Tensor_置零 接受 A：
    总 为 A 的 行数 乘 A 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        做 内存_写32 用 A 的 数据 加 i 乘 4、0。
        i 为 i 加 1。
    完。
完。

Tensor_单位 接受 n：
    I 为 做 Tensor_创建 用 n、n。
    做 Tensor_置零 用 I。
    Q_ONE 为 1 左移 Q_SHIFT。
    可变 i 为 0。
    只要 i 小于 n：
        做 Tensor_写 用 I、i、i、Q_ONE。
        i 为 i 加 1。
    完。
    返还 I。
完。

Tensor_克隆 接受 A：
    B 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        做 内存_写32 用 B 的 数据 加 i 乘 4、做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        i 为 i 加 1。
    完。
    返还 B。
完。

矩阵运算_打印统计 接受 零参：
    做 矩阵统计_初始化。
    做 打印行 用 「=== 矩阵运算统计 ===」。
    做 打印行 用 「矩阵乘: 」 加 做 整数转字符串 用 全局_矩阵统计 的 矩阵乘次数。
    做 打印行 用 「加法: 」 加 做 整数转字符串 用 全局_矩阵统计 的 加法次数。
    做 打印行 用 「转置: 」 加 做 整数转字符串 用 全局_矩阵统计 的 转置次数。
    做 打印行 用 「激活: 」 加 做 整数转字符串 用 全局_矩阵统计 的 激活次数。
    做 打印行 用 「归一化: 」 加 做 整数转字符串 用 全局_矩阵统计 的 归一化次数。
    做 打印行 用 「卷积: 」 加 做 整数转字符串 用 全局_矩阵统计 的 卷积次数。
    做 打印行 用 「池化: 」 加 做 整数转字符串 用 全局_矩阵统计 的 池化次数。
    做 打印行 用 「总乘加: 」 加 做 整数转字符串 用 全局_矩阵统计 的 总乘加运算。
    做 打印行 用 「总元素: 」 加 做 整数转字符串 用 全局_矩阵统计 的 总元素处理。
完。

可变 AI_矩阵运算_热节点缓存 为 0。

AI_矩阵运算_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_矩阵运算_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_矩阵运算_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

本章 收束。
