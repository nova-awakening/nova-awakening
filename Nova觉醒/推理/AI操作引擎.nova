《AI操作引擎》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《.·定点Q算术》。

引用《..·中枢·整数转字符串》。

引用《..·感知·原子驻留》。

引用《..·中枢·时钟中枢》。

引用《..·标准·进程接口》。

操作_点击 为 0。

操作_双击 为 1。

操作_输入 为 2。

操作_滚动 为 3。

操作_拖拽 为 4。

操作_按键 为 5。

操作_等待 为 6。

AI操作 是 一种 事物，含 类型、参数、时间戳、结果。

可变 操作历史 为 一组：（空）。

可变 操作_总执行 为 0。

可变 操作_总成功 为 0。

AI操作_点击 接受 x、y：
    op 为 新建 AI操作。
    op 的 类型 为 操作_点击。
    op 的 参数 为 一组：x、y。
    op 的 时间戳 为 做 读取毫秒。
    op 的 结果 为 0。
    返还 op。
完。

AI操作_双击 接受 x、y：
    op 为 新建 AI操作。
    op 的 类型 为 操作_双击。
    op 的 参数 为 一组：x、y。
    op 的 时间戳 为 做 读取毫秒。
    返还 op。
完。

AI操作_输入 接受 文本：
    op 为 新建 AI操作。
    op 的 类型 为 操作_输入。
    op 的 参数 为 一组：文本。
    op 的 时间戳 为 做 读取毫秒。
    返还 op。
完。

AI操作_滚动 接受 方向、量：
    op 为 新建 AI操作。
    op 的 类型 为 操作_滚动。
    op 的 参数 为 一组：方向、量。
    op 的 时间戳 为 做 读取毫秒。
    返还 op。
完。

AI操作_拖拽 接受 x1、y1、x2、y2：
    op 为 新建 AI操作。
    op 的 类型 为 操作_拖拽。
    op 的 参数 为 一组：x1、y1、x2、y2。
    op 的 时间戳 为 做 读取毫秒。
    返还 op。
完。

AI操作_按键 接受 键码：
    op 为 新建 AI操作。
    op 的 类型 为 操作_按键。
    op 的 参数 为 一组：键码。
    op 的 时间戳 为 做 读取毫秒。
    返还 op。
完。

AI操作_等待 接受 毫秒：
    op 为 新建 AI操作。
    op 的 类型 为 操作_等待。
    op 的 参数 为 一组：毫秒。
    op 的 时间戳 为 做 读取毫秒。
    返还 op。
完。

AI操作_执行 接受 操作：
    操作_总执行 为 操作_总执行 加 1。
    若 操作 的 类型 等于 操作_点击：
        做 事件_发送_鼠标点击 用 操作 的 参数 的 第 0 项、操作 的 参数 的 第 1 项、1。
        操作 的 结果 为 1。
        操作_总成功 为 操作_总成功 加 1。
    否则若 操作 的 类型 等于 操作_双击：
        做 事件_发送_鼠标点击 用 操作 的 参数 的 第 0 项、操作 的 参数 的 第 1 项、1。
        做 事件_发送_鼠标点击 用 操作 的 参数 的 第 0 项、操作 的 参数 的 第 1 项、1。
        操作 的 结果 为 1。
        操作_总成功 为 操作_总成功 加 1。
    否则若 操作 的 类型 等于 操作_输入：
        文本 为 操作 的 参数 的 第 0 项。
        可变 i 为 0。
        只要 i 小于 做 字符串长度 用 文本：
            做 事件_发送_按键 用 做 字符串字符 用 文本、i。
            i 为 i 加 1。
        完。
        操作 的 结果 为 1。
        操作_总成功 为 操作_总成功 加 1。
    否则若 操作 的 类型 等于 操作_等待：
        做 进程_睡眠毫秒 用 操作 的 参数 的 第 0 项。
        操作 的 结果 为 1。
        操作_总成功 为 操作_总成功 加 1。
    否则：
        操作 的 结果 为 2。
    完。
    做 追加 用 操作历史、操作。
    返还 操作 的 结果。
完。

AI操作_执行序列 接受 操作列表：
    可变 i 为 0。
    可变 失败 为 0。
    只要 i 小于 做 计数 用 操作列表：
        结果 为 做 AI操作_执行 用 操作列表 的 第 i 项。
        若 结果 等于 2：
            失败 为 失败 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 失败 等于 0。
完。

AI操作_录制开始 接受 零参：
    操作历史 为 一组：（空）。
完。

AI操作_录制结束 接受 零参：
    返还 操作历史。
完。

AI操作_回放 接受 录制：
    返还 做 AI操作_执行序列 用 录制。
完。

AI操作_统计 接受 零参：
    返还 一本字典：总执行 对应 操作_总执行、成功 对应 操作_总成功。
完。

可变 全局AIOE统计 为 新建 AIOE统计结构。

AIOE统计结构 是 一种 事物，含 元素定位次数、智能点击次数、智能输入次数、操作序列次数、重试次数、自愈次数、AI推理次数。

定位策略_坐标 为 0。

定位策略_选择器 为 1。

定位策略_文本 为 2。

定位策略_图像 为 3。

定位策略_AI 为 4。

元素定位器 是 一种 事物，含 策略、选择器、文本内容、目标X、目标Y、置信度、重试次数。

元素定位_通过选择器 接受 选择器字符串：
    loc 为 做 堆_分配 用 元素定位器。
    若 loc 等于 0：
        返还 空。
    完。
    loc 的 策略 为 定位策略_选择器。
    loc 的 选择器 为 选择器字符串。
    loc 的 置信度 为 0。
    loc 的 重试次数 为 0。
    元素 为 做 DOM_查询选择器 用 选择器字符串。
    若 元素 不等于 0：
        矩形 为 做 DOM_获取边界矩形 用 元素。
        loc 的 目标X 为 矩形 的 x 加 矩形 的 宽 除以 2。
        loc 的 目标Y 为 矩形 的 y 加 矩形 的 高 除以 2。
        loc 的 置信度 为 900。
    完。
    全局AIOE统计 的 元素定位次数 为 全局AIOE统计 的 元素定位次数 加 1。
    返还 loc。
完。

元素定位_通过文本 接受 目标文本：
    loc 为 做 堆_分配 用 元素定位器。
    若 loc 等于 0：
        返还 空。
    完。
    loc 的 策略 为 定位策略_文本。
    loc 的 文本内容 为 目标文本。
    loc 的 置信度 为 0。
    元素 为 做 DOM_搜索文本 用 目标文本。
    若 元素 不等于 0：
        矩形 为 做 DOM_获取边界矩形 用 元素。
        loc 的 目标X 为 矩形 的 x 加 矩形 的 宽 除以 2。
        loc 的 目标Y 为 矩形 的 y 加 矩形 的 高 除以 2。
        loc 的 置信度 为 800。
    完。
    全局AIOE统计 的 元素定位次数 为 全局AIOE统计 的 元素定位次数 加 1。
    返还 loc。
完。

元素定位_AI视觉 接受 描述文本：
    loc 为 做 堆_分配 用 元素定位器。
    若 loc 等于 0：
        返还 空。
    完。
    loc 的 策略 为 定位策略_AI。
    loc 的 文本内容 为 描述文本。
    特征 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 特征、0、做 字符串_哈希 用 描述文本。
    做 Tensor_写 用 特征、1、做 读取毫秒。
    做 Tensor_写 用 特征、2、0。
    做 Tensor_写 用 特征、3、0。
    结果 为 做 AI总线_推理 用 全局AI总线、特征。
    做 Tensor_释放 用 特征。
    若 结果 大于 0：
        loc 的 目标X 为 结果 右移 16。
        loc 的 目标Y 为 结果 位与 0xFFFF。
        loc 的 置信度 为 700。
    完。
    全局AIOE统计 的 AI推理次数 为 全局AIOE统计 的 AI推理次数 加 1。
    返还 loc。
完。

最大重试 为 3。

重试间隔_ms 为 500。

智能操作_执行带重试 接受 操作：
    可变 尝试 为 0。
    可变 结果 为 2。
    只要 尝试 小于 最大重试：
        结果 为 做 AI操作_执行 用 操作。
        若 结果 等于 1：
            返还 1。
        完。
        做 进程_睡眠毫秒 用 重试间隔_ms 乘 (尝试 加 1)。
        全局AIOE统计 的 重试次数 为 全局AIOE统计 的 重试次数 加 1。
        尝试 为 尝试 加 1。
    完。
    返还 结果。
完。

智能操作_点击元素 接受 定位器：
    若 定位器 等于 空：
        返还 2。
    完。
    若 定位器 的 置信度 小于 300：
        若 定位器 的 策略 等于 定位策略_选择器：
            新定位 为 做 元素定位_通过选择器 用 定位器 的 选择器。
            若 新定位 不等于 空 并且 新定位 的 置信度 大于 定位器 的 置信度：
                定位器 的 目标X 为 新定位 的 目标X。
                定位器 的 目标Y 为 新定位 的 目标Y。
                定位器 的 置信度 为 新定位 的 置信度。
            完。
        完。
    完。
    op 为 做 AI操作_点击 用 定位器 的 目标X、定位器 的 目标Y。
    结果 为 做 智能操作_执行带重试 用 op。
    全局AIOE统计 的 智能点击次数 为 全局AIOE统计 的 智能点击次数 加 1。
    返还 结果。
完。

智能操作_输入到元素 接受 定位器、文本：
    若 定位器 等于 空 或者 文本 等于 0：
        返还 2。
    完。
    点击结果 为 做 智能操作_点击元素 用 定位器。
    若 点击结果 不等于 1：
        返还 点击结果。
    完。
    做 事件_发送_按键 用 65 加 0x10000。
    做 事件_发送_按键 用 46。
    op 为 做 AI操作_输入 用 文本。
    结果 为 做 智能操作_执行带重试 用 op。
    全局AIOE统计 的 智能输入次数 为 全局AIOE统计 的 智能输入次数 加 1。
    返还 结果。
完。

规划最大步骤 为 64。

操作规划 是 一种 事物，含 步骤列表、步骤数、当前步骤、失败策略、总成功、总失败。

操作规划_创建 接受 失败策略：
    plan 为 做 堆_分配 用 操作规划。
    若 plan 等于 0：
        返还 空。
    完。
    plan 的 步骤列表 为 做 堆_分配 用 规划最大步骤 乘 8。
    plan 的 步骤数 为 0。
    plan 的 当前步骤 为 0。
    plan 的 失败策略 为 失败策略。
    plan 的 总成功 为 0。
    plan 的 总失败 为 0。
    返还 plan。
完。

操作规划_添加步骤 接受 plan、操作：
    若 plan 等于 空 或者 plan 的 步骤数 大于或等于 规划最大步骤：
        返还。
    完。
    做 内存_写64 用 plan 的 步骤列表 加 plan 的 步骤数 乘 8、操作。
    plan 的 步骤数 为 plan 的 步骤数 加 1。
完。

操作规划_执行 接受 plan：
    若 plan 等于 空：
        返还 假。
    完。
    plan 的 当前步骤 为 0。
    plan 的 总成功 为 0。
    plan 的 总失败 为 0。
    只要 plan 的 当前步骤 小于 plan 的 步骤数：
        op 为 做 内存_读64 用 plan 的 步骤列表 加 plan 的 当前步骤 乘 8。
        若 op 等于 0：
            plan 的 当前步骤 为 plan 的 当前步骤 加 1。
            继续。
        完。
        结果 为 做 智能操作_执行带重试 用 op。
        若 结果 等于 1：
            plan 的 总成功 为 plan 的 总成功 加 1。
        否则：
            plan 的 总失败 为 plan 的 总失败 加 1。
            若 plan 的 失败策略 等于 0：
                返还 假。
            完。
        完。
        plan 的 当前步骤 为 plan 的 当前步骤 加 1。
    完。
    全局AIOE统计 的 操作序列次数 为 全局AIOE统计 的 操作序列次数 加 1。
    返还 plan 的 总失败 等于 0。
完。

宏最大数 为 32。

操作宏 是 一种 事物，含 名称、操作列表、操作数、创建时间、执行次数、成功率。

可变 宏列表 为 空。

可变 宏数量 为 0。

操作宏_保存 接受 名称、操作历史列表：
    若 宏列表 等于 空：
        宏列表 为 做 堆_分配 用 宏最大数 乘 8。
    完。
    若 宏数量 大于或等于 宏最大数：
        返还 假。
    完。
    macro 为 做 堆_分配 用 操作宏。
    若 macro 等于 0：
        返还 假。
    完。
    macro 的 名称 为 名称。
    macro 的 操作列表 为 操作历史列表。
    macro 的 操作数 为 做 计数 用 操作历史列表。
    macro 的 创建时间 为 做 读取毫秒。
    macro 的 执行次数 为 0。
    macro 的 成功率 为 0。
    做 内存_写64 用 宏列表 加 宏数量 乘 8、macro。
    宏数量 为 宏数量 加 1。
    返还 真。
完。

操作宏_执行 接受 名称：
    若 宏列表 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 宏数量：
        macro 为 做 内存_读64 用 宏列表 加 i 乘 8。
        若 macro 不等于 0 并且 做 字符串_相等 用 macro 的 名称、名称：
            macro 的 执行次数 为 macro 的 执行次数 加 1。
            结果 为 做 AI操作_执行序列 用 macro 的 操作列表。
            本次 为 0。
            若 结果：
                本次 为 1024。
            完。
            macro 的 成功率 为 (macro 的 成功率 乘 921 加 本次 乘 103) 除以 1024。
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

AIOE_打印诊断 接受 零参：
    做 内核打印 用 「╔════════════════════════════════════════════╗」。
    做 内核打印 用 「║  AIOE AI操作执行智能 诊断报告           ║」。
    做 内核打印 用 「╠════════════════════════════════════════════╣」。
    做 内核打印 用 「║ 基础执行:     {} ({}成功)」、操作_总执行、操作_总成功。
    做 内核打印 用 「║ 元素定位:     {}」、全局AIOE统计 的 元素定位次数。
    做 内核打印 用 「║ 智能点击:     {}」、全局AIOE统计 的 智能点击次数。
    做 内核打印 用 「║ 智能输入:     {}」、全局AIOE统计 的 智能输入次数。
    做 内核打印 用 「║ 操作序列:     {}」、全局AIOE统计 的 操作序列次数。
    做 内核打印 用 「║ 重试次数:     {}」、全局AIOE统计 的 重试次数。
    做 内核打印 用 「║ AI推理:       {}」、全局AIOE统计 的 AI推理次数。
    做 内核打印 用 「║ 已存宏数:     {}」、宏数量。
    做 内核打印 用 「║ 模式学习:     {}」、全局模式数量。
    做 内核打印 用 「║ 自愈次数:     {}」、全局AIOE统计 的 自愈次数。
    做 内核打印 用 「╚════════════════════════════════════════════╝」。
完。

等待类型_元素可见 为 0。

等待类型_元素可点击 为 1。

等待类型_文本出现 为 2。

等待类型_网络空闲 为 3。

等待类型_动画结束 为 4。

智能等待配置 是 一种 事物，含 类型、超时_ms、轮询间隔_ms、选择器、目标文本。

智能等待_元素可见 接受 选择器字符串、超时_ms：
    可变 已用 为 0。
    可变 间隔 为 50。
    只要 已用 小于 超时_ms：
        元素 为 做 DOM_查询选择器 用 选择器字符串。
        若 元素 不等于 0：
            可见 为 做 DOM_检查可见性 用 元素。
            若 可见：
                返还 真。
            完。
        完。
        做 进程_睡眠毫秒 用 间隔。
        已用 为 已用 加 间隔。
        若 间隔 小于 500：
            间隔 为 间隔 乘 3 除以 2。
        完。
    完。
    返还 假。
完。

智能等待_文本出现 接受 目标文本、超时_ms：
    可变 已用 为 0。
    可变 间隔 为 80。
    只要 已用 小于 超时_ms：
        元素 为 做 DOM_搜索文本 用 目标文本。
        若 元素 不等于 0：
            返还 真。
        完。
        做 进程_睡眠毫秒 用 间隔。
        已用 为 已用 加 间隔。
        若 间隔 小于 500：
            间隔 为 间隔 乘 3 除以 2。
        完。
    完。
    返还 假。
完。

智能等待_网络空闲 接受 超时_ms：
    可变 已用 为 0。
    可变 空闲计数 为 0。
    只要 已用 小于 超时_ms：
        活跃 为 做 网络_获取活跃请求数。
        若 活跃 等于 0：
            空闲计数 为 空闲计数 加 1。
            若 空闲计数 大于或等于 3：
                返还 真。
            完。
        否则：
            空闲计数 为 0。
        完。
        做 进程_睡眠毫秒 用 200。
        已用 为 已用 加 200。
    完。
    返还 假。
完。

智能等待_动画结束 接受 超时_ms：
    可变 已用 为 0。
    可变 上次帧号 为 0。
    可变 稳定计数 为 0。
    只要 已用 小于 超时_ms：
        当前帧 为 做 渲染_获取帧号。
        有动画 为 做 动画_检查运行中。
        若 有动画 等于 假：
            稳定计数 为 稳定计数 加 1。
            若 稳定计数 大于或等于 2：
                返还 真。
            完。
        否则：
            稳定计数 为 0。
        完。
        上次帧号 为 当前帧。
        做 进程_睡眠毫秒 用 100。
        已用 为 已用 加 100。
    完。
    返还 假。
完。

条件_元素存在 为 0。

条件_文本匹配 为 1。

条件_屏幕像素 为 2。

条件_AI判断 为 3。

条件操作 是 一种 事物，含 条件类型、条件参数、成功操作、失败操作。

条件操作_创建 接受 条件类型、条件参数、成功操作、失败操作：
    cond 为 做 堆_分配 用 条件操作。
    若 cond 等于 0：
        返还 空。
    完。
    cond 的 条件类型 为 条件类型。
    cond 的 条件参数 为 条件参数。
    cond 的 成功操作 为 成功操作。
    cond 的 失败操作 为 失败操作。
    返还 cond。
完。

条件操作_求值 接受 条件类型、条件参数：
    若 条件类型 等于 条件_元素存在：
        elem 为 做 DOM_查询选择器 用 条件参数。
        返还 elem 不等于 0。
    完。
    若 条件类型 等于 条件_文本匹配：
        elem 为 做 DOM_搜索文本 用 条件参数。
        返还 elem 不等于 0。
    完。
    若 条件类型 等于 条件_屏幕像素：
        x 为 条件参数 右移 16。
        y 为 条件参数 位与 0xFFFF。
        像素 为 做 屏幕_读取像素 用 x、y。
        返还 像素 不等于 0。
    完。
    若 条件类型 等于 条件_AI判断：
        特征 为 做 Tensor_创建 用 2。
        做 Tensor_写 用 特征、0、做 字符串_哈希 用 条件参数。
        做 Tensor_写 用 特征、1、做 读取毫秒。
        分数 为 做 AI总线_推理 用 全局AI总线、特征。
        做 Tensor_释放 用 特征。
        全局AIOE统计 的 AI推理次数 为 全局AIOE统计 的 AI推理次数 加 1。
        返还 分数 大于 512。
    完。
    返还 假。
完。

条件操作_执行 接受 cond：
    若 cond 等于 空：
        返还 2。
    完。
    结果 为 做 条件操作_求值 用 cond 的 条件类型、cond 的 条件参数。
    若 结果：
        若 cond 的 成功操作 不等于 0：
            返还 做 智能操作_执行带重试 用 cond 的 成功操作。
        完。
        返还 1。
    否则：
        若 cond 的 失败操作 不等于 0：
            返还 做 智能操作_执行带重试 用 cond 的 失败操作。
        完。
        返还 2。
    完。
完。

模式最大数 为 128。

操作模式 是 一种 事物，含 前置操作哈希、后续操作哈希、置信度、观察次数、成功次数。

可变 全局模式库 为 空。

可变 全局模式数量 为 0。

操作模式_学习 接受 前操作哈希、后操作哈希、是否成功：
    若 全局模式库 等于 空：
        全局模式库 为 做 堆_分配 用 模式最大数 乘 8。
        若 全局模式库 等于 0：
            返还。
        完。
    完。
    可变 i 为 0。
    只要 i 小于 全局模式数量：
        p 为 做 内存_读64 用 全局模式库 加 i 乘 8。
        若 p 不等于 0 并且 p 的 前置操作哈希 等于 前操作哈希 并且 p 的 后续操作哈希 等于 后操作哈希：
            p 的 观察次数 为 p 的 观察次数 加 1。
            若 是否成功：
                p 的 成功次数 为 p 的 成功次数 加 1。
            完。
            比率 为 p 的 成功次数 乘 1024 除以 p 的 观察次数。
            观察权 为 p 的 观察次数。
            若 观察权 大于 100：
                观察权 为 100。
            完。
            p 的 置信度 为 比率 乘 观察权 除以 100。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 全局模式数量 大于或等于 模式最大数：
        返还。
    完。
    np 为 做 堆_分配 用 操作模式。
    若 np 等于 0：
        返还。
    完。
    np 的 前置操作哈希 为 前操作哈希。
    np 的 后续操作哈希 为 后操作哈希。
    np 的 观察次数 为 1。
    np 的 成功次数 为 0。
    若 是否成功：
        np 的 成功次数 为 1。
    完。
    np 的 置信度 为 100。
    做 内存_写64 用 全局模式库 加 全局模式数量 乘 8、np。
    全局模式数量 为 全局模式数量 加 1。
完。

操作模式_预测 接受 当前操作哈希：
    若 全局模式库 等于 空：
        返还 0。
    完。
    可变 最佳 为 空。
    可变 最佳置信 为 0。
    可变 i 为 0。
    只要 i 小于 全局模式数量：
        p 为 做 内存_读64 用 全局模式库 加 i 乘 8。
        若 p 不等于 0 并且 p 的 前置操作哈希 等于 当前操作哈希：
            若 p 的 置信度 大于 最佳置信：
                最佳 为 p。
                最佳置信 为 p 的 置信度。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最佳 不等于 空：
        返还 最佳 的 后续操作哈希。
    完。
    返还 0。
完。

自愈策略_重定位 为 0。

自愈策略_替代选择器 为 1。

自愈策略_AI视觉 为 2。

自愈策略_刷新页面 为 3。

自愈_尝试恢复 接受 失败的操作、定位器：
    全局AIOE统计 的 自愈次数 为 全局AIOE统计 的 自愈次数 加 1。
    若 定位器 不等于 空 并且 定位器 的 选择器 不等于 0：
        新loc 为 做 元素定位_通过选择器 用 定位器 的 选择器。
        若 新loc 不等于 空 并且 新loc 的 置信度 大于 500：
            op 为 做 AI操作_点击 用 新loc 的 目标X、新loc 的 目标Y。
            r 为 做 AI操作_执行 用 op。
            若 r 等于 1：
                返还 真。
            完。
        完。
    完。
    若 定位器 不等于 空 并且 定位器 的 文本内容 不等于 0：
        ai_loc 为 做 元素定位_AI视觉 用 定位器 的 文本内容。
        若 ai_loc 不等于 空 并且 ai_loc 的 置信度 大于 400：
            op 为 做 AI操作_点击 用 ai_loc 的 目标X、ai_loc 的 目标Y。
            r 为 做 AI操作_执行 用 op。
            若 r 等于 1：
                返还 真。
            完。
        完。
    完。
    做 事件_发送_按键 用 116 加 0x10000。
    做 智能等待_网络空闲 用 5000。
    若 失败的操作 不等于 0：
        r 为 做 AI操作_执行 用 失败的操作。
        若 r 等于 1：
            返还 真。
        完。
    完。
    返还 假。
完。

AIOE_初始化 接受 零参：
    做 内核打印 用 「[AIOE] AI操作执行智能引擎 启动」。
    全局AIOE统计 的 元素定位次数 为 0。
    全局AIOE统计 的 智能点击次数 为 0。
    全局AIOE统计 的 智能输入次数 为 0。
    全局AIOE统计 的 操作序列次数 为 0。
    全局AIOE统计 的 重试次数 为 0。
    全局AIOE统计 的 自愈次数 为 0。
    全局AIOE统计 的 AI推理次数 为 0。
    全局模式库 为 空。
    全局模式数量 为 0。
    宏列表 为 空。
    宏数量 为 0。
    做 内核打印 用 「[AIOE] 初始化完成 — 元素定位/智能等待/条件分支/模式学习/自愈 就绪」。
完。

AIOE_全自动操作 接受 操作序列、数量：
    plan 为 做 操作规划_创建 用 1。
    可变 i 为 0。
    只要 i 小于 数量：
        op 为 做 内存_读64 用 操作序列 加 i 乘 8。
        做 操作规划_添加步骤 用 plan、op。
        i 为 i 加 1。
    完。
    结果 为 做 操作规划_执行 用 plan。
    可变 j 为 0。
    只要 j 小于 数量 减 1：
        h1 为 做 内存_读64 用 操作序列 加 j 乘 8。
        h2 为 做 内存_读64 用 操作序列 加 (j 加 1) 乘 8。
        做 操作模式_学习 用 做 字符串_哈希 用 h1、做 字符串_哈希 用 h2、结果。
        j 为 j 加 1。
    完。
    返还 结果。
完。

本章 收束。
