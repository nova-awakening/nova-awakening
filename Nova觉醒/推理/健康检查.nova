《健康检查》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·哈希整数》。

引用《..·感知·内核直连层》。

引用《..·中枢·内容可见性》。

最大检查项 为 32。

最大历史 为 64。

状态_健康 为 0。

状态_降级 为 1。

状态_故障 为 2。

检查间隔 为 10000000。

健康检查项 是 一种 事物，含 名称哈希、检查回调、当前状态、连续失败、检查次数、失败次数、最近检查、超时阈值。

可变 检查表 为 一组：（空）。

可变 检查数 为 0。

可变 历史环 为 0。

可变 历史写位 为 0。

可变 总检查 为 0。

可变 总故障 为 0。

可变 就绪 为 假。

健康_初始化 接受 零参：
    检查表 为 一组：（空）。
    检查数 为 0。
    历史环 为 做 堆_分配 用 最大历史 乘 16。
    历史写位 为 0。
    就绪 为 假。
完。

健康_注册 接受 名称、回调、超时：
    若 检查数 大于或等于 最大检查项：
        返还 负 1。
    完。
    项 为 新建 健康检查项。
    项 的 名称哈希 为 做 哈希_字符串 用 名称。
    项 的 检查回调 为 回调。
    项 的 当前状态 为 状态_健康。
    项 的 连续失败 为 0。
    项 的 检查次数 为 0。
    项 的 失败次数 为 0。
    项 的 最近检查 为 0。
    项 的 超时阈值 为 超时。
    做 追加 用 检查表、项。
    索引 为 检查数。
    检查数 为 检查数 加 1。
    返还 索引。
完。

健康_执行全部 接受 零参：
    总检查 为 总检查 加 1。
    可变 全部健康 为 真。
    可变 序号 为 0。
    只要 序号 小于 检查数：
        项 为 检查表 的 第 序号 项。
        项 的 检查次数 为 项 的 检查次数 加 1。
        项 的 最近检查 为 做 平台_获取高精度时间。
        可变 结果 为 假。
        若 项 的 检查回调 不等于 0：
            结果 为 做 项 的 检查回调。
        完。
        若 结果：
            项 的 连续失败 为 0。
            项 的 当前状态 为 状态_健康。
        否则：
            项 的 连续失败 为 项 的 连续失败 加 1。
            项 的 失败次数 为 项 的 失败次数 加 1。
            若 项 的 连续失败 大于或等于 3：
                项 的 当前状态 为 状态_故障。
                总故障 为 总故障 加 1。
            否则：
                项 的 当前状态 为 状态_降级。
            完。
            全部健康 为 假。
        完。
        做 健康_记录历史 用 项 的 名称哈希、项 的 当前状态。
        序号 为 序号 加 1。
    完。
    就绪 为 全部健康。
    返还 全部健康。
完。

健康_记录历史 接受 名称哈希、状态：
    偏移 为 历史环 加 历史写位 乘 16。
    做 内存_写64 用 偏移、名称哈希。
    做 内存_写32 用 偏移 加 8、状态。
    做 内存_写32 用 偏移 加 12、做 平台_获取高精度时间 位与 0xFFFFFFFF。
    历史写位 为 (历史写位 加 1) 取余于 最大历史。
完。

健康_是活跃 接受 零参：
    返还 检查数 大于 0。
完。

健康_是就绪 接受 零参：
    返还 就绪。
完。

健康_获取状态 接受 索引：
    若 索引 大于或等于 检查数：
        返还 状态_故障。
    完。
    返还 检查表 的 第 索引 项 的 当前状态。
完。

健康_故障组件数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 检查数：
        若 检查表 的 第 序号 项 的 当前状态 等于 状态_故障：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

健康_降级组件数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 检查数：
        若 检查表 的 第 序号 项 的 当前状态 等于 状态_降级：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

健康_状态名 接受 状态：
    若 状态 等于 状态_健康：
        返还 「健康」。
    完。
    若 状态 等于 状态_降级：
        返还 「降级」。
    完。
    若 状态 等于 状态_故障：
        返还 「故障」。
    完。
    返还 「未知」。
完。

健康_完整统计 接受 零参：
    返还 一组：检查数、总检查、总故障、做 健康_故障组件数、做 健康_降级组件数、就绪。
完。

本章 收束。
