《配置管理》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·哈希整数》。

最大配置项 为 128。

最大监听器 为 16。

类型_字符串 为 0。

类型_整数 为 1。

类型_布尔 为 2。

配置项 是 一种 事物，含 键哈希、值哈希、整数值、布尔值、类型、只读、修改次数。

可变 配置表 为 一组：（空）。

可变 配置数 为 0。

可变 监听表 为 0。

可变 监听数 为 0。

可变 读取次数 为 0。

可变 写入次数 为 0。

配置_初始化 接受 零参：
    配置表 为 一组：（空）。
    配置数 为 0。
    监听表 为 做 堆_分配 用 最大监听器 乘 8。
    监听数 为 0。
    做 配置_设默认。
完。

配置_设默认 接受 零参：
    做 配置_设整数 用 「网关端口」、8080。
    做 配置_设整数 用 「最大连接」、1000。
    做 配置_设整数 用 「超时秒」、30。
    做 配置_设整数 用 「最大令牌」、4096。
    做 配置_设整数 用 「温度」、70。
    做 配置_设整数 用 「日志级别」、2。
    做 配置_设整数 用 「日志最大条目」、1000。
    做 配置_设布尔 用 「调试模式」、假。
    做 配置_设布尔 用 「安全模式」、真。
完。

配置_设值 接受 键、值哈希：
    写入次数 为 写入次数 加 1。
    键哈希 为 做 哈希_字符串 用 键。
    可变 序号 为 0。
    只要 序号 小于 配置数：
        若 配置表 的 第 序号 项 的 键哈希 等于 键哈希：
            若 配置表 的 第 序号 项 的 只读：
                返还 错误_只读。
            完。
            旧值 为 配置表 的 第 序号 项 的 值哈希。
            配置表 的 第 序号 项 的 值哈希 为 值哈希。
            配置表 的 第 序号 项 的 修改次数 为 配置表 的 第 序号 项 的 修改次数 加 1。
            做 配置_通知变更 用 键哈希、旧值、值哈希。
            返还 0。
        完。
        序号 为 序号 加 1。
    完。
    若 配置数 大于或等于 最大配置项：
        返还 错误_无资源。
    完。
    项 为 新建 配置项。
    项 的 键哈希 为 键哈希。
    项 的 值哈希 为 值哈希。
    项 的 整数值 为 0。
    项 的 布尔值 为 假。
    项 的 类型 为 类型_字符串。
    项 的 只读 为 假。
    项 的 修改次数 为 0。
    做 追加 用 配置表、项。
    配置数 为 配置数 加 1。
    返还 0。
完。

配置_设整数 接受 键、值：
    写入次数 为 写入次数 加 1。
    键哈希 为 做 哈希_字符串 用 键。
    可变 序号 为 0。
    只要 序号 小于 配置数：
        若 配置表 的 第 序号 项 的 键哈希 等于 键哈希：
            若 配置表 的 第 序号 项 的 只读：
                返还。
            完。
            配置表 的 第 序号 项 的 整数值 为 值。
            配置表 的 第 序号 项 的 类型 为 类型_整数。
            配置表 的 第 序号 项 的 修改次数 为 配置表 的 第 序号 项 的 修改次数 加 1。
            返还。
        完。
        序号 为 序号 加 1。
    完。
    若 配置数 大于或等于 最大配置项：
        返还。
    完。
    项 为 新建 配置项。
    项 的 键哈希 为 键哈希。
    项 的 整数值 为 值。
    项 的 类型 为 类型_整数。
    项 的 只读 为 假。
    项 的 修改次数 为 0。
    做 追加 用 配置表、项。
    配置数 为 配置数 加 1。
完。

配置_设布尔 接受 键、值：
    写入次数 为 写入次数 加 1。
    键哈希 为 做 哈希_字符串 用 键。
    可变 序号 为 0。
    只要 序号 小于 配置数：
        若 配置表 的 第 序号 项 的 键哈希 等于 键哈希：
            若 配置表 的 第 序号 项 的 只读：
                返还。
            完。
            配置表 的 第 序号 项 的 布尔值 为 值。
            配置表 的 第 序号 项 的 类型 为 类型_布尔。
            配置表 的 第 序号 项 的 修改次数 为 配置表 的 第 序号 项 的 修改次数 加 1。
            返还。
        完。
        序号 为 序号 加 1。
    完。
    若 配置数 大于或等于 最大配置项：
        返还。
    完。
    项 为 新建 配置项。
    项 的 键哈希 为 键哈希。
    项 的 布尔值 为 值。
    项 的 类型 为 类型_布尔。
    项 的 只读 为 假。
    项 的 修改次数 为 0。
    做 追加 用 配置表、项。
    配置数 为 配置数 加 1。
完。

配置_取值 接受 键：
    读取次数 为 读取次数 加 1。
    键哈希 为 做 哈希_字符串 用 键。
    可变 序号 为 0。
    只要 序号 小于 配置数：
        若 配置表 的 第 序号 项 的 键哈希 等于 键哈希：
            返还 配置表 的 第 序号 项 的 值哈希。
        完。
        序号 为 序号 加 1。
    完。
    返还 0。
完。

配置_取整数 接受 键、默认值：
    读取次数 为 读取次数 加 1。
    键哈希 为 做 哈希_字符串 用 键。
    可变 序号 为 0。
    只要 序号 小于 配置数：
        若 配置表 的 第 序号 项 的 键哈希 等于 键哈希 并且 配置表 的 第 序号 项 的 类型 等于 类型_整数：
            返还 配置表 的 第 序号 项 的 整数值。
        完。
        序号 为 序号 加 1。
    完。
    返还 默认值。
完。

配置_取布尔 接受 键、默认值：
    读取次数 为 读取次数 加 1。
    键哈希 为 做 哈希_字符串 用 键。
    可变 序号 为 0。
    只要 序号 小于 配置数：
        若 配置表 的 第 序号 项 的 键哈希 等于 键哈希 并且 配置表 的 第 序号 项 的 类型 等于 类型_布尔：
            返还 配置表 的 第 序号 项 的 布尔值。
        完。
        序号 为 序号 加 1。
    完。
    返还 默认值。
完。

配置_设只读 接受 键：
    键哈希 为 做 哈希_字符串 用 键。
    可变 序号 为 0。
    只要 序号 小于 配置数：
        若 配置表 的 第 序号 项 的 键哈希 等于 键哈希：
            配置表 的 第 序号 项 的 只读 为 真。
            返还。
        完。
        序号 为 序号 加 1。
    完。
完。

配置_注册监听 接受 回调：
    若 监听数 大于或等于 最大监听器：
        返还。
    完。
    做 内存_写64 用 监听表 加 监听数 乘 8、回调。
    监听数 为 监听数 加 1。
完。

配置_通知变更 接受 键哈希、旧值、新值：
    可变 序号 为 0。
    只要 序号 小于 监听数：
        回调 为 做 内存_读64 用 监听表 加 序号 乘 8。
        若 回调 不等于 0：
            做 回调 用 键哈希、旧值、新值。
        完。
        序号 为 序号 加 1。
    完。
完。

配置_包含 接受 键：
    键哈希 为 做 哈希_字符串 用 键。
    可变 序号 为 0。
    只要 序号 小于 配置数：
        若 配置表 的 第 序号 项 的 键哈希 等于 键哈希：
            返还 真。
        完。
        序号 为 序号 加 1。
    完。
    返还 假。
完。

配置_完整统计 接受 零参：
    返还 一组：配置数、读取次数、写入次数、监听数。
完。

可变 AI_配置管理_事件频率 为 0。

可变 AI_配置管理_节流阈值 为 16。

AI_配置管理_节流判断 接受 事件类型、间隔毫秒：
    AI_配置管理_事件频率 为 AI_配置管理_事件频率 加 1。
    若 事件类型 等于 「滚动」 并且 间隔毫秒 小于 AI_配置管理_节流阈值：
        返还 真。
    完。
    若 事件类型 等于 「移动」 并且 间隔毫秒 小于 AI_配置管理_节流阈值：
        返还 真。
    完。
    返还 假。
完。

AI_配置管理_优先级排序 接受 事件类型：
    若 事件类型 等于 「点击」：
        返还 100。
    完。
    若 事件类型 等于 「输入」：
        返还 90。
    完。
    若 事件类型 等于 「滚动」：
        返还 30。
    完。
    返还 50。
完。

本章 收束。
