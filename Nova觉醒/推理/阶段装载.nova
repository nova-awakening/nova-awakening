《阶段装载》

引用《..·中枢·阶段契约》。

引用《.·AI总线初始化》。

引用《.·AI操作引擎》。

引用《.·感知循环》。

引用《.·通信渠道》。

引用《.·通信总线》。

引用《.·能力注册》。

引用《.·自我升级》。

引用《.·记忆管理》。

引用《.·语义嵌入》。

引用《.·上下文运行时》。

引用《.·值型系统》。

引用《.·会话管理》。

引用《.·模型持久化》。

引用《.·渠道接口》。

引用《.·脚本运行时装载》。

引用《.·模型推演装载》。

引用《.·交互协同装载》。

可变 推理阶段装载_已装载 为 一组：（空）。

推理阶段装载_清单 接受 阶段：
    若 阶段 等于 阶段_boot：
        返还 一组：「./AI总线初始化.nova」、「./能力注册.nova」。
    完。
    若 阶段 等于 阶段_内核核心：
        返还 一组：「./AI操作引擎.nova」、「./通信总线.nova」、「./记忆管理.nova」、「./脚本运行时装载.nova」。
    完。
    若 阶段 等于 阶段_浏览器外衣：
        返还 一组：「./感知循环.nova」、「./语义嵌入.nova」、「./会话管理.nova」、「./交互协同装载.nova」。
    完。
    若 阶段 等于 阶段_AI运行时：
        返还 一组：「./上下文运行时.nova」、「./值型系统.nova」、「./模型持久化.nova」、「./模型推演装载.nova」。
    完。
    若 阶段 等于 阶段_外部桥接：
        返还 一组：「./通信渠道.nova」、「./渠道接口.nova」、「./通信总线.nova」、「./交互协同装载.nova」。
    完。
    返还 一组：（空）。
完。

推理阶段装载_登记 接受 阶段：
    可变 i 为 0。
    只要 i 小于 做 计数 用 推理阶段装载_已装载：
        若 推理阶段装载_已装载 的 第 i 项 等于 阶段：
            返还 做 计数 用 做 推理阶段装载_清单 用 阶段。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 推理阶段装载_已装载、阶段。
    返还 做 计数 用 做 推理阶段装载_清单 用 阶段。
完。

本章 收束。
