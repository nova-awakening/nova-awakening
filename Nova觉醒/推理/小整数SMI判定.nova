《小整数SMI判定》

引用《..·中枢·系统调用表》。

引用《..·推理·通信总线》。

引用《.·定点Q算术》。

引用《..·中枢·字符串替换》。

TAG_SMI 为 0。

TAG_HEAP 为 1。

TAG_MASK 为 1。

Nova_未定义 为 0x0A。

Nova_空值 为 0x02。

Nova_真 为 0x12。

Nova_假 为 0x22。

Nova_空洞 为 0x32。

Nova_是小整数 接受 v：
    返还 v 位与 TAG_MASK 等于 TAG_SMI 并且 v 不等于 0。
完。

Nova_是堆对象 接受 v：
    返还 v 位与 TAG_MASK 等于 TAG_HEAP 并且 v 大于 0x40。
完。

Nova_是未定义 接受 v：
    返还 v 等于 Nova_未定义。
完。

Nova_是空值 接受 v：
    返还 v 等于 Nova_空值。
完。

Nova_是布尔 接受 v：
    返还 v 等于 Nova_真 或者 v 等于 Nova_假。
完。

Nova_是空洞 接受 v：
    返还 v 等于 Nova_空洞。
完。

Nova_是空或未定义 接受 v：
    返还 v 等于 Nova_空值 或者 v 等于 Nova_未定义。
完。

Nova小整数_编码 接受 整数：
    返还 整数 左移 1。
完。

Nova小整数_解码 接受 tagged：
    返还 tagged 右移 1。
完。

Nova堆_编码 接受 指针：
    返还 指针 位或 TAG_HEAP。
完。

Nova堆_解码 接受 tagged：
    返还 tagged 位与 按位取反 TAG_MASK。
完。

Nova布尔_编码 接受 b：
    若 b：
        返还 Nova_真。
    完。
    返还 Nova_假。
完。

Nova布尔_解码 接受 v：
    返还 v 等于 Nova_真。
完。

Nova类型_形状 为 0。

Nova类型_字符串 为 1。

Nova类型_堆数值 为 2。

Nova类型_对象 为 3。

Nova类型_数组 为 4。

Nova类型_函数 为 5。

Nova类型_正则 为 6。

Nova类型_承诺 为 7。

Nova类型_日期 为 8。

Nova类型_错误 为 9。

Nova类型_映射 为 10。

Nova类型_集合 为 11。

Nova类型_弱映射 为 12。

Nova类型_弱集合 为 13。

Nova类型_数组BUFFER 为 14。

Nova类型_类型数组 为 15。

Nova类型_数据视图 为 16。

Nova类型_代理 为 17。

Nova类型_符号 为 18。

Nova类型_大整数 为 19。

Nova类型_共享函数信息 为 20。

Nova类型_字节码数组 为 21。

Nova类型_固定数组 为 22。

Nova类型_属性数组 为 23。

Nova类型_哈希表 为 24。

Nova类型_作用域信息 为 25。

Nova类型_反馈向量 为 26。

Nova类型_机器码 为 27。

Nova堆对象Header 是 一种 事物，含 map指针、gc标记、gc代。

可变 全局_类型检查次数 为 0。

可变 全局_SMI快速路径 为 0。

可变 全局_堆对象访问 为 0。

可变 全局_形状缓存命中 为 0。

可变 全局_形状缓存未中 为 0。

可变 全局_形状转换次数 为 0。

可变 全局_AI预测命中 为 0。

可变 全局_AI预测未中 为 0。

Nova_获取堆类型 接受 tagged：
    全局_类型检查次数 为 全局_类型检查次数 加 1。
    若 非 做 Nova_是堆对象 用 tagged：
        返还 负 1。
    完。
    全局_堆对象访问 为 全局_堆对象访问 加 1。
    指针 为 做 Nova堆_解码 用 tagged。
    map 为 做 内存_读字 用 指针、0。
    若 map 等于 0：
        返还 负 1。
    完。
    返还 做 内存_读半字 用 做 内存_读字 用 map、0、8。
完。

Nova_是字符串 接受 v：
    若 非 做 Nova_是堆对象 用 v：
        返还 假。
    完。
    返还 做 Nova_获取堆类型 用 v 等于 Nova类型_字符串。
完。

Nova_是数值 接受 v：
    若 做 Nova_是小整数 用 v：
        返还 真。
    完。
    若 非 做 Nova_是堆对象 用 v：
        返还 假。
    完。
    返还 做 Nova_获取堆类型 用 v 等于 Nova类型_堆数值。
完。

Nova_是对象 接受 v：
    若 非 做 Nova_是堆对象 用 v：
        返还 假。
    完。
    t 为 做 Nova_获取堆类型 用 v。
    返还 t 等于 Nova类型_对象 或者 t 等于 Nova类型_数组 或者 t 等于 Nova类型_函数。
完。

Nova_是函数 接受 v：
    若 非 做 Nova_是堆对象 用 v：
        返还 假。
    完。
    返还 做 Nova_获取堆类型 用 v 等于 Nova类型_函数。
完。

Nova_是数组 接受 v：
    若 非 做 Nova_是堆对象 用 v：
        返还 假。
    完。
    返还 做 Nova_获取堆类型 用 v 等于 Nova类型_数组。
完。

Nova_是符号 接受 v：
    若 非 做 Nova_是堆对象 用 v：
        返还 假。
    完。
    返还 做 Nova_获取堆类型 用 v 等于 Nova类型_符号。
完。

最大属性槽 为 32。

形状转换缓存大小 为 256。

属性描述符 是 一种 事物，含 属性名、偏移、属性类型、可写、可枚举、可配置。

Nova形状 是 一种 事物，含 实例类型、属性数量、属性描述符组、父形状、转换键、原型、使用计数。

可变 形状转换缓存 为 空。

形状系统_初始化 接受 零参：
    形状转换缓存 为 做 数组_创建 用 形状转换缓存大小、空。
    全局_类型检查次数 为 0。
    全局_SMI快速路径 为 0。
    全局_堆对象访问 为 0。
    全局_形状缓存命中 为 0。
    全局_形状缓存未中 为 0。
    全局_形状转换次数 为 0。
    全局_AI预测命中 为 0。
    全局_AI预测未中 为 0。
    返还 真。
完。

创建空形状 接受 实例类型：
    返还 新建 Nova形状：实例类型 为 实例类型、属性数量 为 0、属性描述符组 为 做 数组_创建 用 最大属性槽、空、父形状 为 空、转换键 为 空、原型 为 空、使用计数 为 0。
完。

形状转换_哈希 接受 形状、属性名：
    可变。
    h 为 0x1505。
    可变。
    i 为 0。
    只要 i 小于 做 字符串_长度 用 属性名：
        h 为 h 乘 33 加 做 字符串_字符码 用 属性名、i。
        i 为 i 加 1。
    完。
    h 为 h 乘 31 加 形状 的 属性数量。
    返还 h 位与 0xFFFFFFFF。
完。

形状_查找转换 接受 当前形状、属性名：
    若 形状转换缓存 不等于 空：
        h 为 做 形状转换_哈希 用 当前形状、属性名。
        idx 为 h 取余于 形状转换缓存大小。
        缓存项 为 形状转换缓存 的 第 idx 项。
        若 缓存项 不等于 空 并且 缓存项 的 转换键 等于 属性名 并且 缓存项 的 父形状 等于 当前形状：
            全局_形状缓存命中 为 全局_形状缓存命中 加 1。
            返还 缓存项。
        完。
    完。
    全局_形状缓存未中 为 全局_形状缓存未中 加 1。
    返还 空。
完。

形状_执行转换 接受 当前形状、属性名：
    已有 为 做 形状_查找转换 用 当前形状、属性名。
    若 已有 不等于 空：
        返还 已有。
    完。
    若 当前形状 的 属性数量 大于或等于 最大属性槽：
        返还 空。
    完。
    可变。
    新形状 为 做 创建空形状 用 当前形状 的 实例类型。
    可变。
    i 为 0。
    只要 i 小于 当前形状 的 属性数量：
        新形状 的 属性描述符组 的 第 i 项 为 当前形状 的 属性描述符组 的 第 i 项。
        i 为 i 加 1。
    完。
    新形状 的 属性描述符组 的 第 当前形状 的 属性数量 项 为 新建 属性描述符：属性名 为 属性名、偏移 为 当前形状 的 属性数量、属性类型 为 0、可写 为 真、可枚举 为 真、可配置 为 真。
    新形状 的 属性数量 为 当前形状 的 属性数量 加 1。
    新形状 的 父形状 为 当前形状。
    新形状 的 转换键 为 属性名。
    新形状 的 原型 为 当前形状 的 原型。
    若 形状转换缓存 不等于 空：
        h 为 做 形状转换_哈希 用 当前形状、属性名。
        idx 为 h 取余于 形状转换缓存大小。
        形状转换缓存 的 第 idx 项 为 新形状。
    完。
    全局_形状转换次数 为 全局_形状转换次数 加 1。
    返还 新形状。
完。

形状_查找属性 接受 形状、属性名：
    若 形状 等于 空：
        返还 负 1。
    完。
    形状 的 使用计数 为 形状 的 使用计数 加 1。
    可变。
    i 为 0。
    只要 i 小于 形状 的 属性数量：
        若 形状 的 属性描述符组 的 第 i 项 不等于 空 并且 形状 的 属性描述符组 的 第 i 项 的 属性名 等于 属性名：
            返还 形状 的 属性描述符组 的 第 i 项 的 偏移。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

Nova对象 是 一种 事物，含 头、形状、命名属性、命名属性容量、索引属性、索引属性长度。

Nova对象_创建 接受 初始形状：
    可变。
    obj 为 新建 Nova对象。
    obj 的 头 为 新建 Nova堆对象Header：map指针 为 0、gc标记 为 0、gc代 为 0。
    obj 的 形状 为 初始形状。
    obj 的 命名属性容量 为 8。
    obj 的 命名属性 为 做 数组_创建 用 8、Nova_未定义。
    obj 的 索引属性 为 空。
    obj 的 索引属性长度 为 0。
    返还 obj。
完。

Nova对象_获取属性 接受 obj、属性名：
    若 obj 等于 空 或者 obj 的 形状 等于 空：
        返还 Nova_未定义。
    完。
    偏移 为 做 形状_查找属性 用 obj 的 形状、属性名。
    若 偏移 大于或等于 0 并且 偏移 小于 obj 的 命名属性容量：
        返还 obj 的 命名属性 的 第 偏移 项。
    完。
    若 obj 的 形状 的 原型 不等于 空：
        返还 做 Nova对象_获取属性 用 obj 的 形状 的 原型、属性名。
    完。
    返还 Nova_未定义。
完。

Nova对象_设置属性 接受 obj、属性名、值：
    若 obj 等于 空：
        返还 假。
    完。
    偏移 为 做 形状_查找属性 用 obj 的 形状、属性名。
    若 偏移 大于或等于 0 并且 偏移 小于 obj 的 命名属性容量：
        obj 的 命名属性 的 第 偏移 项 为 值。
        返还 真。
    完。
    新形状 为 做 形状_执行转换 用 obj 的 形状、属性名。
    若 新形状 等于 空：
        返还 假。
    完。
    obj 的 形状 为 新形状。
    若 新形状 的 属性数量 大于 obj 的 命名属性容量：
        新容量 为 obj 的 命名属性容量 乘 2。
        可变。
        新存储 为 做 数组_创建 用 新容量、Nova_未定义。
        可变。
        ci 为 0。
        只要 ci 小于 obj 的 命名属性容量：
            新存储 的 第 ci 项 为 obj 的 命名属性 的 第 ci 项。
            ci 为 ci 加 1。
        完。
        obj 的 命名属性 为 新存储。
        obj 的 命名属性容量 为 新容量。
    完。
    obj 的 命名属性 的 第 新形状 的 属性数量 减 1 项 为 值。
    返还 真。
完。

SMI_加法 接受 a、b：
    全局_SMI快速路径 为 全局_SMI快速路径 加 1。
    结果 为 a 加 b。
    若 结果 大于 0x7FFFFFFE 或者 结果 小于 负 0x80000000：
        返还 做 创建堆数值 用 做 Nova小整数_解码 用 a 加 做 Nova小整数_解码 用 b。
    完。
    返还 结果。
完。

SMI_减法 接受 a、b：
    全局_SMI快速路径 为 全局_SMI快速路径 加 1。
    结果 为 a 减 b。
    若 结果 大于 0x7FFFFFFE 或者 结果 小于 负 0x80000000：
        返还 做 创建堆数值 用 做 Nova小整数_解码 用 a 减 做 Nova小整数_解码 用 b。
    完。
    返还 结果。
完。

SMI_乘法 接受 a、b：
    全局_SMI快速路径 为 全局_SMI快速路径 加 1。
    va 为 做 Nova小整数_解码 用 a。
    vb 为 做 Nova小整数_解码 用 b。
    结果 为 va 乘 vb。
    若 结果 大于 0x3FFFFFFF 或者 结果 小于 负 0x40000000：
        返还 做 创建堆数值 用 结果 乘 1.0。
    完。
    返还 做 Nova小整数_编码 用 结果。
完。

SMI_位与 接受 a、b：
    全局_SMI快速路径 为 全局_SMI快速路径 加 1。
    返还 a 位与 b。
完。

SMI_位或 接受 a、b：
    全局_SMI快速路径 为 全局_SMI快速路径 加 1。
    返还 a 位或 b。
完。

SMI_左移 接受 a、n：
    全局_SMI快速路径 为 全局_SMI快速路径 加 1。
    va 为 做 Nova小整数_解码 用 a。
    结果 为 va 左移 n。
    返还 做 Nova小整数_编码 用 结果。
完。

SMI_比较 接受 a、b：
    全局_SMI快速路径 为 全局_SMI快速路径 加 1。
    若 a 小于 b：
        返还 负 1。
    完。
    若 a 大于 b：
        返还 1。
    完。
    返还 0。
完。

创建堆数值 接受 浮点值：
    指针 为 做 堆_分配 用 16。
    做 内存_写双字 用 指针、8、浮点值。
    返还 做 Nova堆_编码 用 指针。
完。

AI_属性访问预测 接受 形状、访问历史：
    若 形状 等于 空：
        返还。
    完。
    特征 为 做 Tensor_创建 用 1、4。
    做 Tensor_写 用 特征、0、0、形状 的 属性数量 乘 1.0。
    做 Tensor_写 用 特征、0、1、形状 的 使用计数 乘 1.0。
    做 Tensor_写 用 特征、0、2、全局_形状缓存命中 乘 1.0。
    做 Tensor_写 用 特征、0、3、访问历史 乘 1.0。
    结果 为 做 AI总线_推理 用 「property_access_predictor」、特征。
    若 结果 不等于 空：
        conf 为 做 Tensor_读 用 结果、0、0。
        若 conf 大于 0.75：
            全局_AI预测命中 为 全局_AI预测命中 加 1。
        否则：
            全局_AI预测未中 为 全局_AI预测未中 加 1。
        完。
        做 Tensor_释放 用 结果。
    完。
    做 Tensor_释放 用 特征。
完。

NTVM_诊断输出 接受 零参：
    可变。
    报告 为 「[NTVM诊断报告]\n」。
    报告 为 报告 加 「类型检查总次数: 」 加 全局_类型检查次数 加 「\n」。
    报告 为 报告 加 「SMI快速路径: 」 加 全局_SMI快速路径 加 「\n」。
    报告 为 报告 加 「堆对象访问: 」 加 全局_堆对象访问 加 「\n」。
    报告 为 报告 加 「形状缓存命中: 」 加 全局_形状缓存命中 加 「\n」。
    报告 为 报告 加 「形状缓存未中: 」 加 全局_形状缓存未中 加 「\n」。
    报告 为 报告 加 「形状转换次数: 」 加 全局_形状转换次数 加 「\n」。
    报告 为 报告 加 「AI预测命中: 」 加 全局_AI预测命中 加 「\n」。
    报告 为 报告 加 「AI预测未中: 」 加 全局_AI预测未中 加 「\n」。
    总查 为 全局_形状缓存命中 加 全局_形状缓存未中。
    若 总查 大于 0：
        率 为 全局_形状缓存命中 乘 100 除以 总查。
        报告 为 报告 加 「形状缓存命中率: 」 加 率 加 「%\n」。
    完。
    返还 报告。
完。

Nova_typeof 接受 v：
    全局_类型检查次数 为 全局_类型检查次数 加 1。
    若 做 Nova_是未定义 用 v：
        返还 「undefined」。
    完。
    若 做 Nova_是空值 用 v：
        返还 「object」。
    完。
    若 做 Nova_是布尔 用 v：
        返还 「boolean」。
    完。
    若 做 Nova_是小整数 用 v：
        全局_SMI快速路径 为 全局_SMI快速路径 加 1。
        返还 「number」。
    完。
    若 非 做 Nova_是堆对象 用 v：
        返还 「undefined」。
    完。
    t 为 做 Nova_获取堆类型 用 v。
    若 t 等于 Nova类型_字符串：
        返还 「string」。
    完。
    若 t 等于 Nova类型_堆数值：
        返还 「number」。
    完。
    若 t 等于 Nova类型_大整数：
        返还 「bigint」。
    完。
    若 t 等于 Nova类型_符号：
        返还 「symbol」。
    完。
    若 t 等于 Nova类型_函数：
        返还 「function」。
    完。
    返还 「object」。
完。

本章 收束。
