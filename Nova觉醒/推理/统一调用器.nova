《统一调用器》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·统一Provider框架》。

引用《..·中枢·硬链接》。

引用《..·中枢·时钟中枢》。

引用《.·令牌优化器》。

引用《..·中枢·AI安全策略》。

引用《.·响应解析器》。

引用《..·中枢·键值缓存》。

引用《.·请求构造器》。

引用《..·中枢·AI对话》。

引用《..·中枢·工作记忆记》。

引用《..·标准·原生网络》。

智能_调用 接受 系统提示、用户消息：
    返还 做 智能_调用_完整 用 系统提示、一组：一组：「user」、用户消息、空。
完。

智能_调用_多轮 接受 系统提示、消息列表：
    返还 做 智能_调用_完整 用 系统提示、消息列表、空。
完。

智能_调用_完整 接受 系统提示、消息列表、指定Provider：
    cfg 为 空。
    若 指定Provider 不等于 空：
        cfg 为 指定Provider。
    否则：
        cfg 为 做 Provider_当前。
    完。
    若 cfg 等于 空：
        返还 做 AI调用_创建错误 用 「无可用Provider, 请先配置接口密钥和端点」。
    完。
    api密钥 为 做 安全_读取密钥 用 cfg 的 接口密钥名、0。
    若 api密钥 等于 空 或者 做 计数 用 api密钥 等于 0：
        若 cfg 的 类型 不等于 提供者_本地Ollama：
            返还 做 AI调用_创建错误 用 「接口密钥未配置: 」 加 cfg 的 名称。
        完。
        api密钥 为 「」。
    完。
    原始令牌 为 做 令牌_估算消息列表 用 消息列表。
    优化消息 为 做 令牌_智能优化 用 消息列表、系统提示、cfg 的 最大令牌、cfg 的 最大输出令牌。
    请求体 为 做 请求_构造 用 cfg、系统提示、优化消息。
    头列表 为 做 请求_构造头 用 cfg、api密钥。
    可变 结果 为 空。
    可变 重试 为 0。
    可变 延迟 为 1000。
    只要 重试 小于或等于 cfg 的 重试次数：
        开始时间 为 做 读取毫秒。
        原始响应 为 做 HTTP_POST 用 cfg 的 接口端点、请求体、头列表、cfg 的 超时毫秒。
        结果 为 做 响应_解析 用 cfg 的 消息格式版本、原始响应、开始时间。
        若 结果 的 成功 等于 1：
            cfg 的 总请求数 为 cfg 的 总请求数 加 1。
            cfg 的 总成功数 为 cfg 的 总成功数 加 1。
            cfg 的 总令牌消耗 为 cfg 的 总令牌消耗 加 结果 的 总令牌。
            cfg 的 平均延迟EMA 为 (cfg 的 平均延迟EMA 乘 922 加 结果 的 延迟毫秒 乘 1024 乘 102) 除以 (1024 乘 1024)。
            做 令牌_记录使用 用 结果 的 输入令牌、结果 的 输出令牌、原始令牌。
            做 AI调用_记入记忆 用 优化消息、结果 的 内容。
            api密钥 为 「」。
            返还 结果。
        完。
        cfg 的 总请求数 为 cfg 的 总请求数 加 1。
        cfg 的 总失败数 为 cfg 的 总失败数 加 1。
        重试 为 重试 加 1。
        若 重试 小于或等于 cfg 的 重试次数：
            做 睡眠 用 延迟。
            延迟 为 延迟 乘 2。
            若 延迟 大于 30000：
                延迟 为 30000。
            完。
        完。
    完。
    降级结果 为 做 AI调用_降级 用 系统提示、优化消息、cfg。
    若 降级结果 不等于 空 并且 降级结果 的 成功 等于 1：
        返还 降级结果。
    完。
    api密钥 为 「」。
    返还 结果。
完。

AI调用_降级 接受 系统提示、消息列表、失败cfg：
    可变 i 为 0。
    只要 i 小于 做 Provider_数量：
        备选 为 Provider列表 的 第 i 项。
        若 备选 的 已启用 等于 1 并且 备选 的 名称 不等于 失败cfg 的 名称：
            做 内核警告 用 「[AI] 降级到备用: 」 加 备选 的 名称。
            返还 做 智能_调用_完整 用 系统提示、消息列表、备选。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

AI调用_记入记忆 接受 消息列表、AI回复：
    可变 i 为 0。
    只要 i 小于 做 计数 用 消息列表：
        角色 为 消息列表 的 第 i 项 的 第 0 项。
        内容哈希 为 做 字符串哈希 用 消息列表 的 第 i 项 的 第 1 项。
        若 角色 等于 「user」：
            做 对话_添加 用 0、内容哈希、800。
        否则若 角色 等于 「assistant」：
            做 对话_添加 用 1、内容哈希、600。
        否则：
            做 对话_添加 用 2、内容哈希、400。
        完。
        i 为 i 加 1。
    完。
    做 对话_添加 用 1、做 字符串哈希 用 AI回复、700。
    做 工作写入_文本 用 AI回复。
    最后用户 为 「」。
    i 为 做 计数 用 消息列表 减 1。
    只要 i 大于或等于 0：
        若 消息列表 的 第 i 项 的 第 0 项 等于 「user」：
            最后用户 为 消息列表 的 第 i 项 的 第 1 项。
            跳出。
        完。
        i 为 i 减 1。
    完。
    若 做 字符串长度 用 最后用户 大于 0：
        做 KV_写入 用 做 字符串哈希 用 最后用户、做 字符串哈希 用 AI回复、3600。
    完。
完。

AI调用_创建错误 接受 信息：
    结果 为 新建 AI响应结果。
    结果 的 成功 为 0。
    结果 的 错误码 为 999。
    结果 的 错误信息 为 信息。
    返还 结果。
完。

字符串哈希 接受 s：
    可变 h 为 5381。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 s：
        h 为 h 乘 33 加 做 字符串字符 用 s、i。
        i 为 i 加 1。
    完。
    返还 h。
完。

HTTP_POST 接受 端点、请求体、头列表、超时：
    返还 做 网络_HTTP_POST 用 端点、请求体、头列表。
完。

睡眠 接受 毫秒：
    做 定时器_等待 用 毫秒。
完。

智能_问 接受 问题：
    结果 为 做 智能_调用 用 「你是NovaOS的AI助手。」、问题。
    若 结果 的 成功 等于 1：
        返还 结果 的 内容。
    完。
    返还 「错误: 」 加 结果 的 错误信息。
完。

智能_代码 接受 编程请求：
    返还 做 智能_调用 用 「你是一个编程助手，使用Nova语言。直接输出代码，不要解释。」、编程请求。
完。

智能_翻译 接受 文本、目标语言：
    返还 做 智能_调用 用 「将文本翻译为」 加 目标语言 加 「。只输出翻译结果。」、文本。
完。

本章 收束。
