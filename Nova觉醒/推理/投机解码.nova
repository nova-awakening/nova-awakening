《投机解码》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《.·强化对齐》。

引用《..·中枢·字符串替换》。

引用《..·标准·数学幂根对数》。

SPEC_默认草稿长度 为 5。

SPEC_最小接受率 为 0.3。

SPEC_最大草稿长度 为 8。

SPEC_自适应_启用 为 真。

SPEC_历史窗口大小 为 10。

SPEC_草稿长度_中文 为 3。

SPEC_草稿长度_混合 为 4。

SPEC_中文密度_阈值 为 600。

SPEC_中文感知草稿长度 接受 最近令牌s：
    若 非 SPEC_自适应_启用 或者 最近令牌s 等于 0：
        返还 SPEC_默认草稿长度。
    完。
    检测数 为 若 做 计数 用 最近令牌s 小于 16 则 做 计数 用 最近令牌s 否则 16。
    若 检测数 等于 0：
        返还 SPEC_默认草稿长度。
    完。
    可变 中文数 为 0。
    可变 i 为 0。
    只要 i 小于 检测数：
        令牌 为 最近令牌s 的 第 做 计数 用 最近令牌s 减 检测数 加 i 项。
        若 令牌 大于或等于 0x4E00 并且 令牌 小于或等于 0x9FFF：
            中文数 为 中文数 加 1。
        完。
        i 为 i 加 1。
    完。
    密度 为 中文数 乘 1000 除以 检测数。
    若 密度 大于 SPEC_中文密度_阈值：
        返还 SPEC_草稿长度_中文。
    完。
    若 密度 大于 300：
        返还 SPEC_草稿长度_混合。
    完。
    返还 SPEC_默认草稿长度。
完。

可变 SPEC_接受率历史 为 一组：（空）。

可变 SPEC_历史写指针 为 0。

投机解码_生成 接受 大模型、小模型、提示令牌s、最大长度、温度：
    可变 输出 为 做 数组_复制 用 提示令牌s。
    可变 草稿长度 为 SPEC_默认草稿长度。
    可变 总接受 为 0。
    可变 总尝试 为 0。
    只要 做 计数 用 输出 小于 最大长度：
        草稿令牌s 为 做 数组_创建 用 草稿长度。
        草稿概率 为 做 数组_创建 用 草稿长度。
        可变 当前 为 输出。
        可变 k 为 0。
        只要 k 小于 草稿长度：
            小logits 为 做 模型_前向 用 小模型、当前。
            小p 为 做 Nova归一化指数_温度 用 小logits、温度。
            采样令牌 为 做 分类采样 用 小p。
            做 数组_设置 用 草稿令牌s、k、采样令牌。
            做 数组_设置 用 草稿概率、k、小p。
            当前 为 做 数组_追加复制 用 当前、采样令牌。
            k 为 k 加 1。
        完。
        验证输入 为 做 数组_拼接 用 输出、草稿令牌s。
        大logits_序列 为 做 模型_前向_批量 用 大模型、验证输入、做 计数 用 输出、草稿长度 加 1。
        可变 接受数 为 0。
        可变 i 为 0。
        只要 i 小于 草稿长度：
            大p 为 做 Nova归一化指数_温度 用 大logits_序列 的 第 i 项、温度。
            小p 为 草稿概率 的 第 i 项。
            草稿令牌 为 草稿令牌s 的 第 i 项。
            大概率 为 做 张量_取 用 大p、草稿令牌。
            小概率 为 做 张量_取 用 小p、草稿令牌。
            若 小概率 小于或等于 0：
                跳出。
            完。
            接受率 为 做 最小值_浮点 用 1.0、大概率 除以 小概率。
            随机 为 做 随机_均匀。
            若 随机 小于 接受率：
                做 追加 用 输出、草稿令牌。
                接受数 为 接受数 加 1。
                i 为 i 加 1。
            否则：
                修正分布 为 做 投机_修正分布 用 大p、小p。
                修正令牌 为 做 分类采样 用 修正分布。
                做 追加 用 输出、修正令牌。
                跳出。
            完。
        完。
        若 接受数 等于 草稿长度：
            额外p 为 做 Nova归一化指数_温度 用 大logits_序列 的 第 草稿长度 项、温度。
            额外令牌 为 做 分类采样 用 额外p。
            做 追加 用 输出、额外令牌。
        完。
        总接受 为 总接受 加 接受数。
        总尝试 为 总尝试 加 草稿长度。
        若 SPEC_自适应_启用：
            草稿长度 为 做 投机_自适应长度 用 草稿长度、接受数。
        完。
        若 输出 的 第 做 计数 用 输出 减 1 项 等于 令牌_EOS：
            跳出。
        完。
    完。
    返还 输出。
完。

投机_修正分布 接受 大p、小p：
    词汇量 为 大p 的 长度。
    可变 修正 为 做 张量_零 用 词汇量。
    可变 总和 为 0.0。
    可变 i 为 0。
    只要 i 小于 词汇量：
        差 为 做 张量_取 用 大p、i 减 做 张量_取 用 小p、i。
        值 为 做 最大值_浮点 用 0.0、差。
        做 张量_设 用 修正、i、值。
        总和 为 总和 加 值。
        i 为 i 加 1。
    完。
    若 总和 大于 1e-10：
        i 为 0。
        只要 i 小于 词汇量：
            做 张量_设 用 修正、i、做 张量_取 用 修正、i 除以 总和。
            i 为 i 加 1。
        完。
    否则：
        返还 大p。
    完。
    返还 修正。
完。

投机_自适应长度 接受 当前长度、接受数：
    单次接受率 为 接受数 乘 1000 除以 当前长度。
    若 做 计数 用 SPEC_接受率历史 小于 SPEC_历史窗口大小：
        做 追加 用 SPEC_接受率历史、单次接受率。
    否则：
        SPEC_接受率历史 的 第 SPEC_历史写指针 项 为 单次接受率。
        SPEC_历史写指针 为 (SPEC_历史写指针 加 1) 取余于 SPEC_历史窗口大小。
    完。
    可变 总和 为 0。
    可变 i 为 0。
    n 为 做 计数 用 SPEC_接受率历史。
    只要 i 小于 n：
        总和 为 总和 加 SPEC_接受率历史 的 第 i 项。
        i 为 i 加 1。
    完。
    均值接受率 为 总和 除以 n。
    若 均值接受率 大于 800 并且 当前长度 小于 SPEC_最大草稿长度：
        返还 当前长度 加 1。
    完。
    若 均值接受率 小于 SPEC_最小接受率 乘 1000 并且 当前长度 大于 1：
        返还 当前长度 减 1。
    完。
    返还 当前长度。
完。

Medusa_前向 接受 模型、隐藏状态、头数：
    可变 候选 为 做 数组_创建 用 头数。
    可变 h 为 0。
    只要 h 小于 头数：
        头输出 为 做 Medusa头_前向 用 模型 的 medusa头 的 第 h 项、隐藏状态。
        topk 为 做 张量_topk_1d 用 头输出、5。
        做 数组_设置 用 候选、h、topk。
        h 为 h 加 1。
    完。
    候选树 为 做 Medusa_构建树 用 候选。
    验证结果 为 做 模型_前向_树 用 模型、隐藏状态、候选树。
    返还 做 Medusa_选择路径 用 候选树、验证结果。
完。

MEDUSA_最大树节点 为 64。

MEDUSA_TopK 为 5。

树节点 是 一种 事物，含 令牌ID、深度、父节点索引、概率、是否验证通过。

Medusa_构建树 接受 候选：
    头数 为 做 计数 用 候选。
    可变 树 为 一组：（空）。
    可变 节点数 为 0。
    可变 k 为 0。
    只要 k 小于 MEDUSA_TopK 并且 节点数 小于 MEDUSA_最大树节点：
        节点 为 新建 树节点。
        节点 的 令牌ID 为 候选 的 第 0 项 的 第 k 项 的 令牌。
        节点 的 深度 为 0。
        节点 的 父节点索引 为 负 1。
        节点 的 概率 为 候选 的 第 0 项 的 第 k 项 的 概率。
        节点 的 是否验证通过 为 假。
        做 追加 用 树、节点。
        节点数 为 节点数 加 1。
        k 为 k 加 1。
    完。
    可变 层 为 1。
    只要 层 小于 头数 并且 节点数 小于 MEDUSA_最大树节点：
        上层起始 为 节点数 减 MEDUSA_TopK。
        若 上层起始 小于 0：
            上层起始 为 0。
        完。
        可变 p 为 上层起始。
        只要 p 小于 节点数 并且 节点数 小于 MEDUSA_最大树节点：
            可变 c 为 0。
            只要 c 小于 2 并且 c 小于 MEDUSA_TopK 并且 节点数 小于 MEDUSA_最大树节点：
                子节点 为 新建 树节点。
                子节点 的 令牌ID 为 候选 的 第 层 项 的 第 c 项 的 令牌。
                子节点 的 深度 为 层。
                子节点 的 父节点索引 为 p。
                子节点 的 概率 为 候选 的 第 层 项 的 第 c 项 的 概率。
                子节点 的 是否验证通过 为 假。
                做 追加 用 树、子节点。
                节点数 为 节点数 加 1。
                c 为 c 加 1。
            完。
            p 为 p 加 1。
        完。
        层 为 层 加 1。
    完。
    返还 树。
完。

Medusa_选择路径 接受 树、验证结果：
    若 树 等于 0 或者 验证结果 等于 0：
        返还 一组：（空）。
    完。
    节点数 为 做 计数 用 树。
    可变 i 为 0。
    只要 i 小于 节点数：
        若 验证结果 的 第 i 项 等于 真：
            树 的 第 i 项 的 是否验证通过 为 真。
        完。
        i 为 i 加 1。
    完。
    可变 最深节点 为 负 1。
    可变 最大深度 为 负 1。
    i 为 0。
    只要 i 小于 节点数：
        若 树 的 第 i 项 的 是否验证通过 等于 真 并且 树 的 第 i 项 的 深度 大于 最大深度：
            最大深度 为 树 的 第 i 项 的 深度。
            最深节点 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最深节点 小于 0：
        返还 一组：（空）。
    完。
    可变 路径 为 一组：（空）。
    可变 当前 为 最深节点。
    只要 当前 大于或等于 0：
        做 追加 用 路径、树 的 第 当前 项 的 令牌ID。
        当前 为 树 的 第 当前 项 的 父节点索引。
    完。
    可变 反转 为 一组：（空）。
    可变 j 为 做 计数 用 路径 减 1。
    只要 j 大于或等于 0：
        做 追加 用 反转、路径 的 第 j 项。
        j 为 j 减 1。
    完。
    返还 反转。
完。

投机解码统计 是 一种 事物，含 总生成轮次、总草稿令牌数、总接受令牌数、总拒绝令牌数、平均接受率、平均草稿长度、最大连续接受、Medusa使用次数、Medusa平均路径长、总节省推理次数、总生成耗时、平均每令牌耗时、草稿模型耗时、验证模型耗时、自适应调整次数、当前草稿长度。

可变 全局投机统计 为 0。

投机统计_初始化 接受 零参：
    统计 为 新建 投机解码统计。
    统计 的 总生成轮次 为 0。
    统计 的 总草稿令牌数 为 0。
    统计 的 总接受令牌数 为 0。
    统计 的 总拒绝令牌数 为 0。
    统计 的 平均接受率 为 0。
    统计 的 平均草稿长度 为 0。
    统计 的 最大连续接受 为 0。
    统计 的 Medusa使用次数 为 0。
    统计 的 Medusa平均路径长 为 0。
    统计 的 总节省推理次数 为 0。
    统计 的 总生成耗时 为 0。
    统计 的 平均每令牌耗时 为 0。
    统计 的 草稿模型耗时 为 0。
    统计 的 验证模型耗时 为 0。
    统计 的 自适应调整次数 为 0。
    统计 的 当前草稿长度 为 SPEC_默认草稿长度。
    全局投机统计 为 统计。
    返还 统计。
完。

投机统计_记录轮次 接受 草稿长度、接受数：
    若 全局投机统计 等于 0：
        做 投机统计_初始化。
    完。
    s 为 全局投机统计。
    s 的 总生成轮次 为 s 的 总生成轮次 加 1。
    s 的 总草稿令牌数 为 s 的 总草稿令牌数 加 草稿长度。
    s 的 总接受令牌数 为 s 的 总接受令牌数 加 接受数。
    s 的 总拒绝令牌数 为 s 的 总拒绝令牌数 加 (草稿长度 减 接受数)。
    本次接受率 为 接受数 乘 1024 除以 草稿长度。
    s 的 平均接受率 为 (s 的 平均接受率 乘 900 加 本次接受率 乘 124) 除以 1024。
    若 接受数 大于 s 的 最大连续接受：
        s 的 最大连续接受 为 接受数。
    完。
    s 的 当前草稿长度 为 草稿长度。
    若 接受数 大于 0：
        s 的 总节省推理次数 为 s 的 总节省推理次数 加 (接受数 减 1)。
    完。
完。

投机统计_打印 接受 零参：
    若 全局投机统计 等于 0：
        做 打印 用 「[投机解码] 统计未初始化」。
        返还。
    完。
    s 为 全局投机统计。
    做 打印 用 「[投机解码] ═══ 统计信息 ═══」。
    做 打印 用 做 字符串拼接 用 「  总生成轮次: 」、做 转字符串 用 s 的 总生成轮次。
    做 打印 用 做 字符串拼接 用 「  总草稿令牌数: 」、做 转字符串 用 s 的 总草稿令牌数。
    做 打印 用 做 字符串拼接 用 「  总接受令牌数: 」、做 转字符串 用 s 的 总接受令牌数。
    做 打印 用 做 字符串拼接 用 「  总拒绝令牌数: 」、做 转字符串 用 s 的 总拒绝令牌数。
    可变 接受百分比 为 0。
    若 s 的 总草稿令牌数 大于 0：
        接受百分比 为 s 的 总接受令牌数 乘 100 除以 s 的 总草稿令牌数。
    完。
    做 打印 用 做 字符串拼接 用 「  总接受率: 」、做 字符串拼接 用 做 转字符串 用 接受百分比、「%」。
    做 打印 用 做 字符串拼接 用 「  最大连续接受: 」、做 转字符串 用 s 的 最大连续接受。
    做 打印 用 做 字符串拼接 用 「  节省推理次数: 」、做 转字符串 用 s 的 总节省推理次数。
    做 打印 用 做 字符串拼接 用 「  当前草稿长度: 」、做 转字符串 用 s 的 当前草稿长度。
    做 打印 用 做 字符串拼接 用 「  Medusa使用次数: 」、做 转字符串 用 s 的 Medusa使用次数。
    可变 加速比 为 0。
    若 s 的 总生成轮次 大于 0 并且 s 的 总草稿令牌数 大于 0：
        加速比 为 (s 的 总接受令牌数 加 s 的 总生成轮次) 乘 10 除以 s 的 总生成轮次。
    完。
    做 打印 用 做 字符串拼接 用 「  估算加速比: 」、做 字符串拼接 用 做 转字符串 用 加速比 除以 10、「.」、做 转字符串 用 加速比 取余于 10、「x」。
    做 打印 用 「[投机解码] ═══════════════════」。
完。

策略_标准投机 为 0。

策略_Medusa 为 1。

策略_无投机 为 2。

投机_AI选择策略 接受 bus、上下文令牌数、接受率历史：
    若 bus 等于 0：
        返还 策略_标准投机。
    完。
    若 上下文令牌数 大于 4096：
        返还 策略_Medusa。
    完。
    若 接受率历史 小于 300：
        返还 策略_无投机。
    完。
    返还 策略_标准投机。
完。

Nova归一化指数_温度 接受 logits、温度：
    若 logits 等于 0：
        返还 空。
    完。
    若 温度 小于或等于 0：
        温度 为 1.0。
    完。
    N 为 logits 的 长度。
    可变 结果 为 做 张量_零 用 N。
    可变 最大值 为 负 1e30。
    可变 i 为 0。
    只要 i 小于 N：
        val 为 做 张量_取 用 logits、i 除以 温度。
        若 val 大于 最大值：
            最大值 为 val。
        完。
        i 为 i 加 1。
    完。
    可变 总和 为 0.0。
    i 为 0。
    只要 i 小于 N：
        val 为 做 数学_exp 用 做 张量_取 用 logits、i 除以 温度 减 最大值。
        做 张量_设 用 结果、i、val。
        总和 为 总和 加 val。
        i 为 i 加 1。
    完。
    若 总和 大于 1e-10：
        i 为 0。
        只要 i 小于 N：
            做 张量_设 用 结果、i、做 张量_取 用 结果、i 除以 总和。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

分类采样 接受 概率分布：
    若 概率分布 等于 0：
        返还 0。
    完。
    N 为 概率分布 的 长度。
    随机值 为 做 随机_均匀。
    可变 累积 为 0.0。
    可变 i 为 0。
    只要 i 小于 N：
        累积 为 累积 加 做 张量_取 用 概率分布、i。
        若 累积 大于或等于 随机值：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 N 减 1。
完。

TopP采样 接受 概率分布、p阈值：
    若 概率分布 等于 0：
        返还 0。
    完。
    N 为 概率分布 的 长度。
    可变 索引列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        做 追加 用 索引列表、i。
        i 为 i 加 1。
    完。
    可变 累积概率 为 0.0。
    可变 截止 为 0。
    可变 j 为 0。
    只要 j 小于 N 并且 累积概率 小于 p阈值：
        可变 最大idx 为 j。
        可变 k 为 j 加 1。
        只要 k 小于 N：
            若 做 张量_取 用 概率分布、索引列表 的 第 k 项 大于 做 张量_取 用 概率分布、索引列表 的 第 最大idx 项：
                最大idx 为 k。
            完。
            k 为 k 加 1。
        完。
        若 最大idx 不等于 j：
            临时 为 索引列表 的 第 j 项。
            做 设置 用 索引列表、j、索引列表 的 第 最大idx 项。
            做 设置 用 索引列表、最大idx、临时。
        完。
        累积概率 为 累积概率 加 做 张量_取 用 概率分布、索引列表 的 第 j 项。
        截止 为 j 加 1。
        j 为 j 加 1。
    完。
    随机值 为 做 随机_均匀 乘 累积概率。
    可变 累积2 为 0.0。
    i 为 0。
    只要 i 小于 截止：
        累积2 为 累积2 加 做 张量_取 用 概率分布、索引列表 的 第 i 项。
        若 累积2 大于或等于 随机值：
            返还 索引列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 索引列表 的 第 0 项。
完。

本章 收束。
