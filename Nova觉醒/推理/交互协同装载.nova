《交互协同装载》

引用《.·分组查询》。

引用《.·浏览连接》。

引用《.·推理后援》。

引用《.·会话存储》。

引用《.·设备接口》。

引用《.·像素感知引擎》。

引用《.·智能体状态机》。

引用《.·用户管理》。

引用《.·消息存储》。

引用《.·界面操作》。

引用《.·表情系统》。

引用《.·自适应矩估计优化器》。

引用《.·模型导出》。

推理交互协同装载_模块清单 接受 零参：
    返还 一组：「./分组查询.nova」、「./浏览连接.nova」、「./推理后援.nova」、「./会话存储.nova」、「./设备接口.nova」、「./像素感知引擎.nova」、「./智能体状态机.nova」、「./用户管理.nova」、「./消息存储.nova」、「./界面操作.nova」、「./表情系统.nova」、「./自适应矩估计优化器.nova」、「./模型导出.nova」。
完。

推理交互协同装载_统计 接受 零参：
    返还 一本字典：模块数 对应 做 计数 用 做 推理交互协同装载_模块清单。
完。

本章 收束。
