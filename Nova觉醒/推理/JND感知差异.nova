《JND感知差异》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《..·感知·内核直连层》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《..·执行·底层内存操》。

WEBER_K 为 20。

JND_像素 接受 背景亮度、纹理复杂度：
    基础JND 为 做 Q乘 用 WEBER_K、背景亮度。
    可变 亮度因子 为 Q_ONE。
    若 背景亮度 小于 30 乘 Q_ONE 除以 255：
        亮度因子 为 Q_ONE 加 做 Q除 用 Q_ONE、背景亮度 加 1。
        若 亮度因子 大于 3 乘 Q_ONE：
            亮度因子 为 3 乘 Q_ONE。
        完。
    否则若 背景亮度 大于 220 乘 Q_ONE 除以 255：
        亮度因子 为 Q_ONE 加 做 Q除 用 背景亮度 减 220 乘 Q_ONE 除以 255、35 乘 Q_ONE 除以 255。
    完。
    纹理因子 为 Q_ONE 加 做 Q乘 用 Q_HALF、纹理复杂度。
    若 纹理因子 大于 4 乘 Q_ONE：
        纹理因子 为 4 乘 Q_ONE。
    完。
    返还 做 Q乘 用 做 Q乘 用 基础JND、亮度因子、纹理因子。
完。

JND_块 接受 像素块、步长：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 8：
        可变 j 为 0。
        只要 j 小于 8：
            总 为 总 加 做 内存_读8 用 像素块 加 i 乘 步长 加 j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    均值 为 总 除以 64。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 8：
        可变 j 为 0。
        只要 j 小于 8：
            差 为 做 内存_读8 用 像素块 加 i 乘 步长 加 j 减 均值。
            方差 为 方差 加 差 乘 差。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    方差 为 方差 除以 64。
    亮度 为 均值 乘 Q_ONE 除以 255。
    纹理 为 做 Q除 用 方差 乘 Q_ONE、6400 乘 Q_ONE。
    若 纹理 大于 Q_ONE：
        纹理 为 Q_ONE。
    完。
    返还 做 JND_像素 用 亮度、纹理。
完。

显著性图 是 一种 事物，含 数据、行数、列数、块大小。

显著性图_创建 接受 图像宽、图像高、块大小：
    sm 为 做 堆_分配 用 显著性图。
    sm 的 块大小 为 块大小。
    sm 的 行数 为 图像高 除以 块大小。
    sm 的 列数 为 图像宽 除以 块大小。
    若 sm 的 行数 等于 0：
        sm 的 行数 为 1。
    完。
    若 sm 的 列数 等于 0：
        sm 的 列数 为 1。
    完。
    sm 的 数据 为 做 堆_分配_数组 用 sm 的 行数 乘 sm 的 列数 乘 4。
    做 内存_清零 用 sm 的 数据、sm 的 行数 乘 sm 的 列数 乘 4。
    返还 sm。
完。

显著性图_计算 接受 sm、灰度图、图像步长：
    若 sm 等于 0 或者 灰度图 等于 0：
        返还。
    完。
    块 为 sm 的 块大小。
    总块数 为 sm 的 行数 乘 sm 的 列数。
    块均值 为 做 堆_分配_数组 用 总块数 乘 4。
    块方差 为 做 堆_分配_数组 用 总块数 乘 4。
    可变 by 为 0。
    只要 by 小于 sm 的 行数：
        可变 bx 为 0。
        只要 bx 小于 sm 的 列数：
            块索引 为 by 乘 sm 的 列数 加 bx。
            可变 总 为 0。
            可变 py 为 0。
            只要 py 小于 块：
                可变 px 为 0。
                只要 px 小于 块：
                    总 为 总 加 做 内存_读8 用 灰度图 加 (by 乘 块 加 py) 乘 图像步长 加 bx 乘 块 加 px。
                    px 为 px 加 1。
                完。
                py 为 py 加 1。
            完。
            均值 为 总 除以 (块 乘 块)。
            做 内存_写32 用 块均值 加 块索引 乘 4、均值。
            可变 方差和 为 0。
            py 为 0。
            只要 py 小于 块：
                可变 px 为 0。
                只要 px 小于 块：
                    差 为 做 内存_读8 用 灰度图 加 (by 乘 块 加 py) 乘 图像步长 加 bx 乘 块 加 px 减 均值。
                    方差和 为 方差和 加 差 乘 差。
                    px 为 px 加 1。
                完。
                py 为 py 加 1。
            完。
            做 内存_写32 用 块方差 加 块索引 乘 4、方差和 除以 (块 乘 块)。
            bx 为 bx 加 1。
        完。
        by 为 by 加 1。
    完。
    可变 全局均值 为 0。
    可变 idx 为 0。
    只要 idx 小于 总块数：
        全局均值 为 全局均值 加 做 内存_读32 用 块均值 加 idx 乘 4。
        idx 为 idx 加 1。
    完。
    全局均值 为 全局均值 除以 总块数。
    idx 为 0。
    只要 idx 小于 总块数：
        局部均值 为 做 内存_读32 用 块均值 加 idx 乘 4。
        局部方差 为 做 内存_读32 用 块方差 加 idx 乘 4。
        对比度 为 局部均值 减 全局均值。
        若 对比度 小于 0：
            对比度 为 负 对比度。
        完。
        对比度归一 为 做 Q除 用 对比度 乘 Q_ONE、128 乘 Q_ONE。
        若 对比度归一 大于 Q_ONE：
            对比度归一 为 Q_ONE。
        完。
        纹理 为 做 Q除 用 做 Q开方 用 局部方差 乘 Q_ONE、80 乘 Q_ONE。
        若 纹理 大于 Q_ONE：
            纹理 为 Q_ONE。
        完。
        显著性 为 做 Q乘 用 614、对比度归一 加 做 Q乘 用 410、纹理。
        若 显著性 大于 Q_ONE：
            显著性 为 Q_ONE。
        完。
        做 内存_写32 用 sm 的 数据 加 idx 乘 4、显著性。
        idx 为 idx 加 1。
    完。
    做 堆_释放 用 块均值。
    做 堆_释放 用 块方差。
完。

JND_图像 接受 灰度图、步长、宽、高：
    块数W 为 宽 除以 8。
    块数H 为 高 除以 8。
    若 块数W 等于 0 或者 块数H 等于 0：
        返还 空。
    完。
    JND图 为 做 堆_分配_数组 用 块数W 乘 块数H 乘 4。
    若 JND图 等于 0：
        返还 空。
    完。
    可变 by 为 0。
    只要 by 小于 块数H：
        可变 bx 为 0。
        只要 bx 小于 块数W：
            偏移 为 灰度图 加 by 乘 8 乘 步长 加 bx 乘 8。
            jnd 为 做 JND_块 用 偏移、步长。
            做 内存_写32 用 JND图 加 (by 乘 块数W 加 bx) 乘 4、jnd。
            bx 为 bx 加 1。
        完。
        by 为 by 加 1。
    完。
    返还 JND图。
完。

色度_WEBER_K 为 50。

JND_色度 接受 L通道、a通道、b通道、纹理复杂度：
    亮度JND 为 做 JND_像素 用 L通道、纹理复杂度。
    色度a_JND 为 做 Q乘 用 色度_WEBER_K、a通道。
    色度b_JND 为 做 Q乘 用 色度_WEBER_K、b通道。
    色度JND 为 色度a_JND。
    若 色度b_JND 小于 色度JND：
        色度JND 为 色度b_JND。
    完。
    返还 做 Q乘 用 717、亮度JND 加 做 Q乘 用 307、色度JND。
完。

时间JND 是 一种 事物，含 上帧亮度图、块数W、块数H、帧间隔、闪烁敏感频率。

时间JND_创建 接受 宽、高：
    tjnd 为 新建 时间JND。
    tjnd 的 块数W 为 宽 除以 8。
    tjnd 的 块数H 为 高 除以 8。
    若 tjnd 的 块数W 等于 0：
        tjnd 的 块数W 为 1。
    完。
    若 tjnd 的 块数H 等于 0：
        tjnd 的 块数H 为 1。
    完。
    tjnd 的 上帧亮度图 为 做 堆_分配_数组 用 tjnd 的 块数W 乘 tjnd 的 块数H 乘 4。
    做 内存_清零 用 tjnd 的 上帧亮度图、tjnd 的 块数W 乘 tjnd 的 块数H 乘 4。
    tjnd 的 帧间隔 为 16666。
    tjnd 的 闪烁敏感频率 为 10。
    返还 tjnd。
完。

时间JND_销毁 接受 tjnd：
    若 tjnd 等于 0：
        返还。
    完。
    若 tjnd 的 上帧亮度图 不等于 0：
        做 堆_释放 用 tjnd 的 上帧亮度图。
    完。
完。

时间JND_计算块 接受 tjnd、块索引、当前亮度：
    若 tjnd 等于 0：
        返还 Q_ONE。
    完。
    上帧亮度 为 做 内存_读32 用 tjnd 的 上帧亮度图 加 块索引 乘 4。
    变化 为 当前亮度 减 上帧亮度。
    若 变化 小于 0：
        变化 为 0 减 变化。
    完。
    做 内存_写32 用 tjnd 的 上帧亮度图 加 块索引 乘 4、当前亮度。
    变化率 为 变化 乘 1000000 除以 tjnd 的 帧间隔。
    若 变化率 大于 500：
        返还 WEBER_K 除以 2。
    否则若 变化率 小于 50：
        返还 WEBER_K 乘 3。
    完。
    返还 WEBER_K。
完。

显著性图_销毁 接受 sm：
    若 sm 等于 0：
        返还。
    完。
    若 sm 的 数据 不等于 0：
        做 堆_释放 用 sm 的 数据。
    完。
完。

显著性图_查询 接受 sm、块x、块y：
    若 sm 等于 0：
        返还 Q_HALF。
    完。
    若 块x 大于或等于 sm 的 列数 或者 块y 大于或等于 sm 的 行数：
        返还 Q_HALF。
    完。
    索引 为 块y 乘 sm 的 列数 加 块x。
    返还 做 内存_读32 用 sm 的 数据 加 索引 乘 4。
完。

显著性图_查询像素 接受 sm、像素x、像素y：
    若 sm 等于 0：
        返还 Q_HALF。
    完。
    块x 为 像素x 除以 sm 的 块大小。
    块y 为 像素y 除以 sm 的 块大小。
    返还 做 显著性图_查询 用 sm、块x、块y。
完。

显著性图_获取最显著区域 接受 sm：
    若 sm 等于 0：
        返还 一组：0、0。
    完。
    总块数 为 sm 的 行数 乘 sm 的 列数。
    可变 最大值 为 0。
    可变 最大索引 为 0。
    可变 i 为 0。
    只要 i 小于 总块数：
        值 为 做 内存_读32 用 sm 的 数据 加 i 乘 4。
        若 值 大于 最大值：
            最大值 为 值。
            最大索引 为 i。
        完。
        i 为 i 加 1。
    完。
    块y 为 最大索引 除以 sm 的 列数。
    块x 为 最大索引 取余于 sm 的 列数。
    返还 一组：块x 乘 sm 的 块大小、块y 乘 sm 的 块大小。
完。

JND统计 是 一种 事物，含 总像素计算次数、总块计算次数、总图像计算次数、平均JND阈值、最小JND阈值、最大JND阈值、高纹理区块数、低纹理区块数、暗区块数、亮区块数、显著性计算次数、总耗时微秒。

可变 全局JND统计 为 0。

JND统计_初始化 接受 零参：
    s 为 新建 JND统计。
    s 的 总像素计算次数 为 0。
    s 的 总块计算次数 为 0。
    s 的 总图像计算次数 为 0。
    s 的 平均JND阈值 为 0。
    s 的 最小JND阈值 为 Q_ONE。
    s 的 最大JND阈值 为 0。
    s 的 高纹理区块数 为 0。
    s 的 低纹理区块数 为 0。
    s 的 暗区块数 为 0。
    s 的 亮区块数 为 0。
    s 的 显著性计算次数 为 0。
    s 的 总耗时微秒 为 0。
    全局JND统计 为 s。
    返还 s。
完。

JND统计_记录 接受 jnd值：
    若 全局JND统计 等于 0：
        做 JND统计_初始化。
    完。
    s 为 全局JND统计。
    s 的 总块计算次数 为 s 的 总块计算次数 加 1。
    若 jnd值 小于 s 的 最小JND阈值：
        s 的 最小JND阈值 为 jnd值。
    完。
    若 jnd值 大于 s 的 最大JND阈值：
        s 的 最大JND阈值 为 jnd值。
    完。
完。

JND统计_打印 接受 零参：
    若 全局JND统计 等于 0：
        做 打印 用 「[JND] 统计未初始化」。
        返还。
    完。
    s 为 全局JND统计。
    做 打印 用 「[JND] ═══ 感知差异统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  总块计算次数: 」、做 转字符串 用 s 的 总块计算次数。
    做 打印 用 做 字符串拼接 用 「  总图像计算次数: 」、做 转字符串 用 s 的 总图像计算次数。
    做 打印 用 做 字符串拼接 用 「  最小JND阈值(Q10): 」、做 转字符串 用 s 的 最小JND阈值。
    做 打印 用 做 字符串拼接 用 「  最大JND阈值(Q10): 」、做 转字符串 用 s 的 最大JND阈值。
    做 打印 用 做 字符串拼接 用 「  高纹理区块数: 」、做 转字符串 用 s 的 高纹理区块数。
    做 打印 用 做 字符串拼接 用 「  暗区块数: 」、做 转字符串 用 s 的 暗区块数。
    做 打印 用 做 字符串拼接 用 「  显著性计算: 」、做 转字符串 用 s 的 显著性计算次数。
    做 打印 用 做 字符串拼接 用 「  总耗时: 」、做 字符串拼接 用 做 转字符串 用 s 的 总耗时微秒、「μs」。
    做 打印 用 「[JND] ═══════════════════════」。
完。

JND_AI自适应Weber 接受 bus、场景类型、运动速度：
    若 bus 等于 0：
        返还 WEBER_K。
    完。
    若 场景类型 等于 1：
        返还 WEBER_K 除以 2。
    完。
    若 运动速度 大于 30：
        返还 WEBER_K 乘 3。
    完。
    若 场景类型 等于 3：
        返还 WEBER_K 乘 2。
    完。
    返还 WEBER_K。
完。

Q_ONE 为 1024。

Q_HALF 为 512。

Q乘 接受 a、b：
    返还 a 乘 b 右移 10。
完。

Q除 接受 a、b：
    若 b 等于 0：
        返还 0。
    完。
    返还 (a 左移 10) 除以 b。
完。

Q开方 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 r 为 x。
    可变 s 为 (r 加 1) 除以 2。
    只要 s 小于 r：
        r 为 s。
        s 为 (r 加 x 除以 r) 除以 2。
    完。
    返还 r。
完。

综合感知评估 接受 灰度块、步长、显著性值、时间变化率：
    空间jnd 为 做 JND_块 用 灰度块、步长。
    可变 时间因子 为 Q_ONE。
    若 时间变化率 大于 500：
        时间因子 为 Q_HALF。
    否则若 时间变化率 小于 50：
        时间因子 为 Q_ONE 加 Q_HALF。
    完。
    可变 显著因子 为 Q_ONE。
    若 显著性值 大于 800：
        显著因子 为 Q_HALF。
    否则若 显著性值 小于 200：
        显著因子 为 Q_ONE 加 300。
    完。
    返还 做 Q乘 用 做 Q乘 用 空间jnd、时间因子、显著因子。
完。

综合感知_决定降级 接受 灰度块、步长、降级块、显著性值、时间变化率：
    jnd阈值 为 做 综合感知评估 用 灰度块、步长、显著性值、时间变化率。
    可变 误差和 为 0。
    可变 i 为 0。
    只要 i 小于 8：
        可变 j 为 0。
        只要 j 小于 8：
            原始 为 做 内存_读8 用 灰度块 加 i 乘 步长 加 j。
            降级 为 做 内存_读8 用 降级块 加 i 乘 步长 加 j。
            差 为 原始 减 降级。
            若 差 小于 0：
                差 为 0 减 差。
            完。
            误差和 为 误差和 加 差。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    平均误差 为 误差和 乘 Q_ONE 除以 (64 乘 255)。
    若 平均误差 小于 jnd阈值：
        返还 真。
    完。
    返还 假。
完。

CSF_敏感度 接受 空间频率：
    若 空间频率 小于或等于 0：
        返还 0。
    完。
    f 为 空间频率。
    项1 为 20 加 做 Q乘 用 117、f。
    指数项 为 0 减 做 Q乘 用 117、f。
    可变 exp值 为 Q_ONE。
    若 指数项 小于 0 减 3 乘 Q_ONE：
        exp值 为 0。
    否则：
        exp值 为 Q_ONE 加 指数项 加 做 Q乘 用 指数项、指数项 除以 2。
    完。
    返还 做 Q乘 用 做 Q乘 用 2662、项1、exp值。
完。

本章 收束。
