《智能体文件操作》

引用《..·中枢·类型定义》。

引用《..·中枢·内存物理》。

引用《..·中枢·硬链接》。

引用《..·标准·原生字符串》。

引用《..·标准·文件》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内核打印》。

可变 安全目录 为 一组：「/home」、「/tmp」、「/var/nova」、「/opt」。

最大搜索深度 为 10。

最大搜索结果 为 256。

最大撤销历史 为 32。

最大监视数 为 16。

文件操作记录 是 一种 事物，含 路径、操作、原内容、时间戳。

可变 撤销历史 为 一组：（空）。

文件监视器 是 一种 事物，含 路径、回调、最后修改时间、活跃。

可变 监视列表 为 一组：（空）。

文件操作统计 是 一种 事物，含 读取次数、写入次数、编辑次数、删除次数、搜索次数、权限拒绝次数、备份次数、撤销次数、复制次数、目录创建次数、读取字节数、写入字节数。

可变 全局_文件统计 为 空。

文件统计_初始化 接受 零参：
    若 全局_文件统计 等于 空：
        全局_文件统计 为 新建 文件操作统计。
        全局_文件统计 的 读取次数 为 0。
        全局_文件统计 的 写入次数 为 0。
        全局_文件统计 的 编辑次数 为 0。
        全局_文件统计 的 删除次数 为 0。
        全局_文件统计 的 搜索次数 为 0。
        全局_文件统计 的 权限拒绝次数 为 0。
        全局_文件统计 的 备份次数 为 0。
        全局_文件统计 的 撤销次数 为 0。
        全局_文件统计 的 复制次数 为 0。
        全局_文件统计 的 目录创建次数 为 0。
        全局_文件统计 的 读取字节数 为 0。
        全局_文件统计 的 写入字节数 为 0。
    完。
完。

智能体文件_检查权限 接受 路径：
    做 文件统计_初始化。
    可变 i 为 0。
    只要 i 小于 做 计数 用 安全目录：
        若 做 字符串开始于 用 路径、安全目录 的 第 i 项：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    全局_文件统计 的 权限拒绝次数 为 全局_文件统计 的 权限拒绝次数 加 1。
    返还 假。
完。

智能体文件_添加安全目录 接受 目录：
    可变 i 为 0。
    只要 i 小于 做 计数 用 安全目录：
        若 安全目录 的 第 i 项 等于 目录：
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 安全目录、目录。
完。

智能体文件_移除安全目录 接受 目录：
    可变 i 为 0。
    只要 i 小于 做 计数 用 安全目录：
        若 安全目录 的 第 i 项 等于 目录：
            做 删除 用 安全目录、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

智能体文件_读取 接受 路径：
    做 文件统计_初始化。
    若 非 做 智能体文件_检查权限 用 路径：
        返还 空。
    完。
    内容 为 做 文件_读全部 用 路径。
    若 内容 不等于 空：
        全局_文件统计 的 读取次数 为 全局_文件统计 的 读取次数 加 1。
        全局_文件统计 的 读取字节数 为 全局_文件统计 的 读取字节数 加 做 字符串长度 用 内容。
    完。
    返还 内容。
完。

智能体文件_读取行 接受 路径、起始行、行数：
    内容 为 做 智能体文件_读取 用 路径。
    若 内容 等于 空：
        返还 空。
    完。
    所有行 为 做 字符串分割 用 内容、「\n」。
    若 起始行 小于 0：
        起始行 为 0。
    完。
    若 起始行 大于或等于 做 计数 用 所有行：
        返还 「」。
    完。
    可变 结果 为 「」。
    结束行 为 起始行 加 行数。
    若 结束行 大于 做 计数 用 所有行：
        结束行 为 做 计数 用 所有行。
    完。
    可变 i 为 起始行。
    只要 i 小于 结束行：
        若 i 大于 起始行：
            结果 为 结果 加 「\n」。
        完。
        结果 为 结果 加 所有行 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

智能体文件_读取二进制 接受 路径、偏移、长度：
    若 非 做 智能体文件_检查权限 用 路径：
        返还 空。
    完。
    返还 做 文件_读二进制 用 路径、偏移、长度。
完。

智能体文件_写入 接受 路径、内容：
    做 文件统计_初始化。
    若 非 做 智能体文件_检查权限 用 路径：
        返还 错误_权限。
    完。
    已存在 为 做 文件_存在 用 路径。
    若 已存在：
        原内容 为 做 文件_读全部 用 路径。
        若 原内容 不等于 空：
            备份路径 为 路径 加 「.bak」。
            做 文件_写全部 用 备份路径、原内容。
            全局_文件统计 的 备份次数 为 全局_文件统计 的 备份次数 加 1。
            做 智能体文件_记录撤销 用 路径、「写入」、原内容。
        完。
    完。
    结果 为 做 文件_写全部 用 路径、内容。
    若 结果 等于 成功：
        全局_文件统计 的 写入次数 为 全局_文件统计 的 写入次数 加 1。
        全局_文件统计 的 写入字节数 为 全局_文件统计 的 写入字节数 加 做 字符串长度 用 内容。
    完。
    返还 结果。
完。

智能体文件_原子写入 接受 路径、内容：
    做 文件统计_初始化。
    若 非 做 智能体文件_检查权限 用 路径：
        返还 错误_权限。
    完。
    临时路径 为 路径 加 「.tmp.」 加 做 整数转字符串 用 做 系统时间_毫秒。
    结果 为 做 文件_写全部 用 临时路径、内容。
    若 结果 不等于 成功：
        做 文件_删除 用 临时路径。
        返还 结果。
    完。
    若 做 文件_存在 用 路径：
        原内容 为 做 文件_读全部 用 路径。
        若 原内容 不等于 空：
            做 智能体文件_记录撤销 用 路径、「写入」、原内容。
        完。
    完。
    结果 为 做 文件_重命名 用 临时路径、路径。
    若 结果 等于 成功：
        全局_文件统计 的 写入次数 为 全局_文件统计 的 写入次数 加 1。
        全局_文件统计 的 写入字节数 为 全局_文件统计 的 写入字节数 加 做 字符串长度 用 内容。
    完。
    返还 结果。
完。

智能体文件_追加 接受 路径、内容：
    做 文件统计_初始化。
    若 非 做 智能体文件_检查权限 用 路径：
        返还 错误_权限。
    完。
    结果 为 做 文件_追加 用 路径、内容。
    若 结果 等于 成功：
        全局_文件统计 的 写入次数 为 全局_文件统计 的 写入次数 加 1。
        全局_文件统计 的 写入字节数 为 全局_文件统计 的 写入字节数 加 做 字符串长度 用 内容。
    完。
    返还 结果。
完。

智能体文件_搜索 接受 目录、模式：
    做 文件统计_初始化。
    全局_文件统计 的 搜索次数 为 全局_文件统计 的 搜索次数 加 1。
    若 非 做 智能体文件_检查权限 用 目录：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    做 智能体文件_搜索_递归 用 目录、模式、结果、0。
    返还 结果。
完。

智能体文件_搜索_递归 接受 目录、模式、结果、深度：
    若 深度 大于或等于 最大搜索深度：
        返还。
    完。
    若 做 计数 用 结果 大于或等于 最大搜索结果：
        返还。
    完。
    文件列表 为 做 目录_列出 用 目录。
    若 文件列表 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文件列表：
        若 做 计数 用 结果 大于或等于 最大搜索结果：
            返还。
        完。
        名称 为 文件列表 的 第 i 项。
        完整路径 为 目录 加 「/」 加 名称。
        若 做 文件_是目录 用 完整路径：
            做 智能体文件_搜索_递归 用 完整路径、模式、结果、深度 加 1。
        否则：
            若 做 字符串包含 用 名称、模式：
                做 追加 用 结果、完整路径。
            完。
        完。
        i 为 i 加 1。
    完。
完。

智能体文件_搜索内容 接受 目录、文本模式：
    做 文件统计_初始化。
    全局_文件统计 的 搜索次数 为 全局_文件统计 的 搜索次数 加 1。
    若 非 做 智能体文件_检查权限 用 目录：
        返还 一组：（空）。
    完。
    所有文件 为 做 智能体文件_搜索 用 目录、「」。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有文件：
        内容 为 做 文件_读全部 用 所有文件 的 第 i 项。
        若 内容 不等于 空 并且 做 字符串包含 用 内容、文本模式：
            做 追加 用 结果、所有文件 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

智能体文件_编辑 接受 路径、旧内容、新内容：
    做 文件统计_初始化。
    全文 为 做 智能体文件_读取 用 路径。
    若 全文 等于 空：
        返还 错误_不存在。
    完。
    若 非 做 字符串包含 用 全文、旧内容：
        返还 错误_未找到。
    完。
    做 智能体文件_记录撤销 用 路径、「编辑」、全文。
    修改后 为 做 字符串替换 用 全文、旧内容、新内容。
    结果 为 做 智能体文件_写入 用 路径、修改后。
    若 结果 等于 成功：
        全局_文件统计 的 编辑次数 为 全局_文件统计 的 编辑次数 加 1。
    完。
    返还 结果。
完。

智能体文件_插入行 接受 路径、行号、新行：
    内容 为 做 智能体文件_读取 用 路径。
    若 内容 等于 空：
        返还 错误_不存在。
    完。
    所有行 为 做 字符串分割 用 内容、「\n」。
    若 行号 小于 0：
        行号 为 0。
    完。
    若 行号 大于 做 计数 用 所有行：
        行号 为 做 计数 用 所有行。
    完。
    做 智能体文件_记录撤销 用 路径、「编辑」、内容。
    可变 新内容 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有行：
        若 i 等于 行号：
            新内容 为 新内容 加 新行 加 「\n」。
        完。
        新内容 为 新内容 加 所有行 的 第 i 项。
        若 i 小于 做 计数 用 所有行 减 1：
            新内容 为 新内容 加 「\n」。
        完。
        i 为 i 加 1。
    完。
    若 行号 大于或等于 做 计数 用 所有行：
        新内容 为 新内容 加 「\n」 加 新行。
    完。
    返还 做 智能体文件_写入 用 路径、新内容。
完。

智能体文件_删除行 接受 路径、行号：
    内容 为 做 智能体文件_读取 用 路径。
    若 内容 等于 空：
        返还 错误_不存在。
    完。
    所有行 为 做 字符串分割 用 内容、「\n」。
    若 行号 小于 0 或者 行号 大于或等于 做 计数 用 所有行：
        返还 错误_越界。
    完。
    做 智能体文件_记录撤销 用 路径、「编辑」、内容。
    可变 新内容 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有行：
        若 i 不等于 行号：
            若 做 字符串长度 用 新内容 大于 0：
                新内容 为 新内容 加 「\n」。
            完。
            新内容 为 新内容 加 所有行 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 做 智能体文件_写入 用 路径、新内容。
完。

智能体文件_行数 接受 路径：
    内容 为 做 智能体文件_读取 用 路径。
    若 内容 等于 空：
        返还 0。
    完。
    可变 行数 为 1。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 内容：
        若 做 字符串字符 用 内容、i 等于 10：
            行数 为 行数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 行数。
完。

智能体文件_大小 接受 路径：
    返还 做 文件_大小 用 路径。
完。

智能体文件_存在 接受 路径：
    返还 做 文件_存在 用 路径。
完。

智能体文件_信息 接受 路径：
    若 非 做 智能体文件_检查权限 用 路径：
        返还 空。
    完。
    返还 一本字典：大小 对应 做 文件_大小 用 路径、行数 对应 做 智能体文件_行数 用 路径、存在 对应 做 文件_存在 用 路径、是目录 对应 做 文件_是目录 用 路径、修改时间 对应 做 文件_修改时间 用 路径。
完。

智能体文件_校验和 接受 路径：
    内容 为 做 智能体文件_读取 用 路径。
    若 内容 等于 空：
        返还 0。
    完。
    可变 哈希 为 2166136261。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 内容：
        哈希 为 (哈希 位异或 做 字符串字符 用 内容、i) 乘 16777619。
        i 为 i 加 1。
    完。
    返还 哈希。
完。

智能体文件_删除 接受 路径：
    做 文件统计_初始化。
    若 非 做 智能体文件_检查权限 用 路径：
        返还 错误_权限。
    完。
    内容 为 做 文件_读全部 用 路径。
    若 内容 不等于 空：
        做 智能体文件_记录撤销 用 路径、「删除」、内容。
    完。
    结果 为 做 文件_删除 用 路径。
    若 结果 等于 成功：
        全局_文件统计 的 删除次数 为 全局_文件统计 的 删除次数 加 1。
    完。
    返还 结果。
完。

智能体文件_复制 接受 源路径、目标路径：
    做 文件统计_初始化。
    若 非 做 智能体文件_检查权限 用 源路径：
        返还 错误_权限。
    完。
    若 非 做 智能体文件_检查权限 用 目标路径：
        返还 错误_权限。
    完。
    内容 为 做 文件_读全部 用 源路径。
    若 内容 等于 空：
        返还 错误_不存在。
    完。
    结果 为 做 文件_写全部 用 目标路径、内容。
    若 结果 等于 成功：
        全局_文件统计 的 复制次数 为 全局_文件统计 的 复制次数 加 1。
    完。
    返还 结果。
完。

智能体文件_重命名 接受 旧路径、新路径：
    若 非 做 智能体文件_检查权限 用 旧路径：
        返还 错误_权限。
    完。
    若 非 做 智能体文件_检查权限 用 新路径：
        返还 错误_权限。
    完。
    返还 做 文件_重命名 用 旧路径、新路径。
完。

智能体文件_创建目录 接受 路径：
    做 文件统计_初始化。
    若 非 做 智能体文件_检查权限 用 路径：
        返还 错误_权限。
    完。
    结果 为 做 目录_创建 用 路径。
    若 结果 等于 成功：
        全局_文件统计 的 目录创建次数 为 全局_文件统计 的 目录创建次数 加 1。
    完。
    返还 结果。
完。

智能体文件_列出目录 接受 路径：
    若 非 做 智能体文件_检查权限 用 路径：
        返还 一组：（空）。
    完。
    返还 做 目录_列出 用 路径。
完。

智能体文件_记录撤销 接受 路径、操作、原内容：
    若 做 计数 用 撤销历史 大于或等于 最大撤销历史：
        做 删除 用 撤销历史、0。
    完。
    记录 为 新建 文件操作记录。
    记录 的 路径 为 路径。
    记录 的 操作 为 操作。
    记录 的 原内容 为 原内容。
    记录 的 时间戳 为 做 系统时间_毫秒。
    做 追加 用 撤销历史、记录。
完。

智能体文件_撤销 接受 零参：
    做 文件统计_初始化。
    若 做 计数 用 撤销历史 等于 0：
        返还 错误_未找到。
    完。
    最后 为 撤销历史 的 第 做 计数 用 撤销历史 减 1 项。
    做 删除 用 撤销历史、做 计数 用 撤销历史 减 1。
    若 最后 的 操作 等于 「删除」：
        做 文件_写全部 用 最后 的 路径、最后 的 原内容。
    否则：
        做 文件_写全部 用 最后 的 路径、最后 的 原内容。
    完。
    全局_文件统计 的 撤销次数 为 全局_文件统计 的 撤销次数 加 1。
    返还 成功。
完。

智能体文件_撤销历史数 接受 零参：
    返还 做 计数 用 撤销历史。
完。

智能体文件_监视 接受 路径、回调：
    若 做 计数 用 监视列表 大于或等于 最大监视数：
        返还 错误_资源不足。
    完。
    若 非 做 智能体文件_检查权限 用 路径：
        返还 错误_权限。
    完。
    监视 为 新建 文件监视器。
    监视 的 路径 为 路径。
    监视 的 回调 为 回调。
    监视 的 最后修改时间 为 做 文件_修改时间 用 路径。
    监视 的 活跃 为 真。
    做 追加 用 监视列表、监视。
    返还 成功。
完。

智能体文件_取消监视 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 监视列表：
        若 监视列表 的 第 i 项 的 路径 等于 路径：
            监视列表 的 第 i 项 的 活跃 为 假。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    返还 错误_未找到。
完。

智能体文件_检查监视 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 监视列表：
        若 监视列表 的 第 i 项 的 活跃：
            当前修改时间 为 做 文件_修改时间 用 监视列表 的 第 i 项 的 路径。
            若 当前修改时间 不等于 监视列表 的 第 i 项 的 最后修改时间：
                监视列表 的 第 i 项 的 最后修改时间 为 当前修改时间。
                若 监视列表 的 第 i 项 的 回调 不等于 空：
                    做 监视列表 的 第 i 项 的 回调 用 监视列表 的 第 i 项 的 路径。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

智能体文件_打印统计 接受 零参：
    做 文件统计_初始化。
    做 打印行 用 「=== 智能体文件操作统计 ===」。
    做 打印行 用 「读取次数: 」 加 做 整数转字符串 用 全局_文件统计 的 读取次数。
    做 打印行 用 「写入次数: 」 加 做 整数转字符串 用 全局_文件统计 的 写入次数。
    做 打印行 用 「编辑次数: 」 加 做 整数转字符串 用 全局_文件统计 的 编辑次数。
    做 打印行 用 「删除次数: 」 加 做 整数转字符串 用 全局_文件统计 的 删除次数。
    做 打印行 用 「复制次数: 」 加 做 整数转字符串 用 全局_文件统计 的 复制次数。
    做 打印行 用 「搜索次数: 」 加 做 整数转字符串 用 全局_文件统计 的 搜索次数。
    做 打印行 用 「权限拒绝: 」 加 做 整数转字符串 用 全局_文件统计 的 权限拒绝次数。
    做 打印行 用 「备份次数: 」 加 做 整数转字符串 用 全局_文件统计 的 备份次数。
    做 打印行 用 「撤销次数: 」 加 做 整数转字符串 用 全局_文件统计 的 撤销次数。
    做 打印行 用 「目录创建: 」 加 做 整数转字符串 用 全局_文件统计 的 目录创建次数。
    做 打印行 用 「读取字节: 」 加 做 整数转字符串 用 全局_文件统计 的 读取字节数。
    做 打印行 用 「写入字节: 」 加 做 整数转字符串 用 全局_文件统计 的 写入字节数。
    做 打印行 用 「撤销历史: 」 加 做 整数转字符串 用 做 计数 用 撤销历史。
    做 打印行 用 「活跃监视: 」 加 做 整数转字符串 用 做 计数 用 监视列表。
完。

本章 收束。
