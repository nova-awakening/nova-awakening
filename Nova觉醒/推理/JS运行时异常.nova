《JS运行时异常》

引用《..·中枢·系统调用表》。

引用《..·推理·通信渠道》。

引用《..·感知·内核直连层》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《..·标准·加法运算符》。

引用《..·中枢·内容可见性》。

引用《.·小整数SMI判定》。

引用《..·执行·底层内存操》。

TryCatch 是 一种 事物，含 上一个、异常、有异常、是详细的。

TryCatch_创建 接受 Nova运行时实例：
    tc 为 做 堆_分配 用 TryCatch。
    tc 的 上一个 为 Nova运行时实例 的 异常处理器栈。
    tc 的 异常 为 0。
    tc 的 有异常 为 假。
    tc 的 是详细的 为 假。
    Nova运行时实例 的 异常处理器栈 为 tc。
    返还 tc。
完。

TryCatch_销毁 接受 Nova运行时实例、tc：
    Nova运行时实例 的 异常处理器栈 为 tc 的 上一个。
完。

Nova运行时_抛出异常 接受 Nova运行时实例、异常值：
    Nova运行时实例 的 当前异常 为 异常值。
    Nova运行时实例 的 有待处理异常 为 真。
    若 Nova运行时实例 的 异常处理器栈 不等于 0：
        tc 为 Nova运行时实例 的 异常处理器栈。
        tc 的 异常 为 异常值。
        tc 的 有异常 为 真。
    完。
完。

Context 是 一种 事物，含 Nova运行时实例、全局对象、全局代理、作用域链。

Context_创建 接受 Nova运行时实例：
    ctx 为 做 堆_分配 用 Context。
    ctx 的 Nova运行时实例 为 Nova运行时实例。
    ctx 的 全局对象 为 做 Nova对象_创建 用 Nova运行时实例、Nova运行时实例 的 Object原型。
    ctx 的 全局代理 为 ctx 的 全局对象。
    ctx 的 作用域链 为 0。
    做 Context_安装内置 用 Nova运行时实例、ctx。
    Nova运行时实例 的 当前上下文 为 ctx。
    返还 ctx。
完。

Context_安装内置 接受 Nova运行时实例、ctx：
    console 为 做 Nova对象_创建 用 Nova运行时实例、Nova运行时实例 的 Object原型。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「console」、console。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「undefined」、Nova运行时实例 的 undefined值。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「NaN」、做 Nova值_创建_Number 用 Nova运行时实例、0x7FF8000000000000。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「Infinity」、做 Nova值_创建_Number 用 Nova运行时实例、0x7FF0000000000000。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「Object」、做 Nova函数_创建 用 Nova运行时实例、「Object」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「数组」、做 Nova函数_创建 用 Nova运行时实例、「数组」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「Function」、做 Nova函数_创建 用 Nova运行时实例、「Function」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「String」、做 Nova函数_创建 用 Nova运行时实例、「String」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「Number」、做 Nova函数_创建 用 Nova运行时实例、「Number」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「Boolean」、做 Nova函数_创建 用 Nova运行时实例、「Boolean」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「错误」、做 Nova函数_创建 用 Nova运行时实例、「错误」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「Type错误」、做 Nova函数_创建 用 Nova运行时实例、「Type错误」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「Range错误」、做 Nova函数_创建 用 Nova运行时实例、「Range错误」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「承诺」、做 Nova函数_创建 用 Nova运行时实例、「承诺」、0、1。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「Map」、做 Nova函数_创建 用 Nova运行时实例、「Map」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「Set」、做 Nova函数_创建 用 Nova运行时实例、「Set」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「WeakMap」、做 Nova函数_创建 用 Nova运行时实例、「WeakMap」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「WeakSet」、做 Nova函数_创建 用 Nova运行时实例、「WeakSet」、0、0。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「序列」、做 Nova对象_创建 用 Nova运行时实例、Nova运行时实例 的 Object原型。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「数学」、做 Nova对象_创建 用 Nova运行时实例、Nova运行时实例 的 Object原型。
完。

热事件_函数调用 为 1。

热事件_循环迭代 为 2。

热事件_属性访问 为 3。

热事件_定时器回调 为 4。

热事件_事件处理 为 5。

错误类型_Error 为 0。

错误类型_TypeError 为 1。

错误类型_RangeError 为 2。

错误类型_ReferenceError 为 3。

错误类型_SyntaxError 为 4。

错误类型_URIError 为 5。

错误类型_EvalError 为 6。

错误类型_InternalError 为 7。

异常信息 是 一种 事物，含 类型、消息、栈跟踪、文件名、行号、列号、原始异常。

异常_创建 接受 Nova运行时实例、类型、消息：
    若 Nova运行时实例 等于 0：
        返还 0。
    完。
    可变 异常 为 做 堆_分配 用 异常信息。
    若 异常 等于 0：
        返还 0。
    完。
    异常 的 类型 为 类型。
    异常 的 消息 为 消息。
    异常 的 栈跟踪 为 0。
    异常 的 文件名 为 0。
    异常 的 行号 为 0。
    异常 的 列号 为 0。
    异常 的 原始异常 为 0。
    做 异常_捕获栈跟踪 用 Nova运行时实例、异常。
    返还 异常。
完。

异常_创建_TypeError 接受 Nova运行时实例、消息：
    返还 做 异常_创建 用 Nova运行时实例、错误类型_TypeError、消息。
完。

异常_创建_RangeError 接受 Nova运行时实例、消息：
    返还 做 异常_创建 用 Nova运行时实例、错误类型_RangeError、消息。
完。

异常_创建_ReferenceError 接受 Nova运行时实例、消息：
    返还 做 异常_创建 用 Nova运行时实例、错误类型_ReferenceError、消息。
完。

异常_创建_SyntaxError 接受 Nova运行时实例、消息：
    返还 做 异常_创建 用 Nova运行时实例、错误类型_SyntaxError、消息。
完。

栈跟踪_最大深度 为 32。

异常_捕获栈跟踪 接受 Nova运行时实例、异常：
    若 Nova运行时实例 等于 0 或者 异常 等于 0：
        返还。
    完。
    可变 帧 为 Nova运行时实例 的 当前帧。
    可变 深度 为 0。
    可变 跟踪缓冲 为 做 平台_申请内存 用 栈跟踪_最大深度 乘 32。
    若 跟踪缓冲 等于 0：
        返还。
    完。
    做 内存_清零 用 跟踪缓冲、栈跟踪_最大深度 乘 32。
    只要 帧 不等于 0 并且 深度 小于 栈跟踪_最大深度：
        帧基址 为 跟踪缓冲 加 深度 乘 32。
        做 内存_写64 用 帧基址、帧 的 字节码。
        做 内存_写32 用 帧基址 加 8、帧 的 PC。
        做 内存_写32 用 帧基址 加 12、0。
        做 内存_写32 用 帧基址 加 16、0。
        做 内存_写64 用 帧基址 加 20、0。
        做 内存_写32 用 帧基址 加 28、帧 的 类型。
        深度 为 深度 加 1。
        帧 为 帧 的 父帧。
    完。
    异常 的 栈跟踪 为 跟踪缓冲。
完。

异常_格式化栈跟踪 接受 异常：
    若 异常 等于 0 或者 异常 的 栈跟踪 等于 0：
        返还。
    完。
    类型名 为 做 异常_类型名 用 异常 的 类型。
    做 打印 用 做 字符串拼接 用 类型名、做 字符串拼接 用 「: 」、做 转字符串 用 异常 的 消息。
    可变 i 为 0。
    只要 i 小于 栈跟踪_最大深度：
        帧基址 为 异常 的 栈跟踪 加 i 乘 32。
        字节码 为 做 内存_读64 用 帧基址。
        若 字节码 等于 0：
            跳出。
        完。
        PC偏移 为 做 内存_读32 用 帧基址 加 8。
        帧类型 为 做 内存_读32 用 帧基址 加 28。
        做 打印 用 做 字符串拼接 用 「    at <」、做 字符串拼接 用 做 转字符串 用 帧类型、做 字符串拼接 用 「> PC:」、做 转字符串 用 PC偏移。
        i 为 i 加 1。
    完。
完。

异常_类型名 接受 类型：
    若 类型 等于 错误类型_Error：
        返还 「Error」。
    完。
    若 类型 等于 错误类型_TypeError：
        返还 「TypeError」。
    完。
    若 类型 等于 错误类型_RangeError：
        返还 「RangeError」。
    完。
    若 类型 等于 错误类型_ReferenceError：
        返还 「ReferenceError」。
    完。
    若 类型 等于 错误类型_SyntaxError：
        返还 「SyntaxError」。
    完。
    若 类型 等于 错误类型_URIError：
        返还 「URIError」。
    完。
    若 类型 等于 错误类型_EvalError：
        返还 「EvalError」。
    完。
    若 类型 等于 错误类型_InternalError：
        返还 「InternalError」。
    完。
    返还 「UnknownError」。
完。

Nova运行时_检查异常 接受 Nova运行时实例：
    若 Nova运行时实例 等于 0：
        返还 假。
    完。
    返还 Nova运行时实例 的 有待处理异常 等于 真。
完。

Nova运行时_获取异常 接受 Nova运行时实例：
    若 Nova运行时实例 等于 0：
        返还 0。
    完。
    返还 Nova运行时实例 的 当前异常。
完。

Nova运行时_清除异常 接受 Nova运行时实例：
    若 Nova运行时实例 等于 0：
        返还。
    完。
    Nova运行时实例 的 当前异常 为 0。
    Nova运行时实例 的 有待处理异常 为 假。
完。

Nova运行时_传播异常 接受 Nova运行时实例：
    若 Nova运行时实例 等于 0：
        返还。
    完。
    若 Nova运行时实例 的 有待处理异常 等于 假：
        返还。
    完。
    可变 帧 为 Nova运行时实例 的 当前帧。
    只要 帧 不等于 0：
        若 帧 的 异常处理链 不等于 0：
            处理器 为 帧 的 异常处理链。
            catch_PC 为 做 内存_读32 用 处理器。
            若 catch_PC 大于 0：
                帧 的 PC 为 catch_PC。
                帧 的 累加器 为 Nova运行时实例 的 当前异常。
                做 Nova运行时_清除异常 用 Nova运行时实例。
                返还。
            完。
        完。
        帧 为 帧 的 父帧。
    完。
    做 Nova运行时_未捕获异常 用 Nova运行时实例。
完。

Nova运行时_未捕获异常 接受 Nova运行时实例：
    若 Nova运行时实例 等于 0：
        返还。
    完。
    做 打印 用 「[异常] ═══ 未捕获的异常 ═══」。
    异常 为 Nova运行时实例 的 当前异常。
    若 异常 不等于 0：
        做 异常_格式化栈跟踪 用 异常。
    否则：
        做 打印 用 「[异常] (无异常对象)」。
    完。
    做 打印 用 「[异常] ═══════════════════」。
    做 Nova运行时_AI记录异常 用 Nova运行时实例、异常。
完。

异常处理器 是 一种 事物，含 catch_PC、finally_PC、上一个、栈深度。

异常处理器_进入 接受 Nova运行时实例、frame、catch偏移、finally偏移：
    若 Nova运行时实例 等于 0 或者 frame 等于 0：
        返还。
    完。
    处理器 为 做 堆_分配 用 异常处理器。
    若 处理器 等于 0：
        返还。
    完。
    处理器 的 catch_PC 为 catch偏移。
    处理器 的 finally_PC 为 finally偏移。
    处理器 的 上一个 为 frame 的 异常处理链。
    处理器 的 栈深度 为 0。
    frame 的 异常处理链 为 处理器。
完。

异常处理器_离开 接受 Nova运行时实例、frame：
    若 Nova运行时实例 等于 0 或者 frame 等于 0：
        返还。
    完。
    若 frame 的 异常处理链 等于 0：
        返还。
    完。
    处理器 为 frame 的 异常处理链。
    frame 的 异常处理链 为 处理器 的 上一个。
    做 堆_释放 用 处理器。
完。

异常处理器_查找 接受 frame：
    若 frame 等于 0：
        返还 0。
    完。
    返还 frame 的 异常处理链。
完。

AI异常分析器 是 一种 事物，含 异常历史、历史容量、历史数量、历史游标、TypeError次数、RangeError次数、ReferenceError次数、SyntaxError次数、总异常次数、未捕获次数、AI预测下次类型、AI预测置信度。

AI异常分析器_创建 接受 零参：
    可变 分析器 为 做 堆_分配 用 AI异常分析器。
    若 分析器 等于 0：
        返还 空。
    完。
    分析器 的 历史容量 为 64。
    分析器 的 异常历史 为 做 平台_申请内存 用 分析器 的 历史容量 乘 16。
    若 分析器 的 异常历史 不等于 0：
        做 内存_清零 用 分析器 的 异常历史、分析器 的 历史容量 乘 16。
    完。
    分析器 的 历史数量 为 0。
    分析器 的 历史游标 为 0。
    分析器 的 TypeError次数 为 0。
    分析器 的 RangeError次数 为 0。
    分析器 的 ReferenceError次数 为 0。
    分析器 的 SyntaxError次数 为 0。
    分析器 的 总异常次数 为 0。
    分析器 的 未捕获次数 为 0。
    分析器 的 AI预测下次类型 为 0。
    分析器 的 AI预测置信度 为 0。
    返还 分析器。
完。

AI异常分析器_销毁 接受 分析器：
    若 分析器 等于 空：
        返还。
    完。
    若 分析器 的 异常历史 不等于 0：
        做 平台_释放内存 用 分析器 的 异常历史、分析器 的 历史容量 乘 16。
    完。
    做 堆_释放 用 分析器。
完。

Nova运行时_AI记录异常 接受 Nova运行时实例、异常：
    若 Nova运行时实例 等于 0 或者 异常 等于 0：
        返还。
    完。
    若 异常 的 类型 等于 错误类型_TypeError：
        做 打印 用 「[AI异常] TypeError: 考虑添加空值检查」。
    否则若 异常 的 类型 等于 错误类型_RangeError：
        做 打印 用 「[AI异常] RangeError: 检查数组/递归边界」。
    否则若 异常 的 类型 等于 错误类型_ReferenceError：
        做 打印 用 「[AI异常] ReferenceError: 检查变量声明」。
    完。
完。

Context_安装全局函数 接受 Nova运行时实例、ctx：
    若 Nova运行时实例 等于 0 或者 ctx 等于 0：
        返还。
    完。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「parseInt」、做 Nova函数_创建 用 Nova运行时实例、「parseInt」、内建_parseInt、1。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「parseFloat」、做 Nova函数_创建 用 Nova运行时实例、「parseFloat」、内建_parseFloat、1。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「isNaN」、做 Nova函数_创建 用 Nova运行时实例、「isNaN」、内建_isNaN、1。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「isFinite」、做 Nova函数_创建 用 Nova运行时实例、「isFinite」、内建_isFinite、1。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「encodeURI」、做 Nova函数_创建 用 Nova运行时实例、「encodeURI」、内建_encodeURI、1。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「decodeURI」、做 Nova函数_创建 用 Nova运行时实例、「decodeURI」、内建_decodeURI、1。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「setTimeout」、做 Nova函数_创建 用 Nova运行时实例、「setTimeout」、内建_setTimeout、2。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「setInterval」、做 Nova函数_创建 用 Nova运行时实例、「setInterval」、内建_setInterval、2。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「clearTimeout」、做 Nova函数_创建 用 Nova运行时实例、「clearTimeout」、内建_clearTimeout、1。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「queueMicrotask」、做 Nova函数_创建 用 Nova运行时实例、「queueMicrotask」、内建_queueMicrotask、1。
    做 Nova对象_设置属性 用 Nova运行时实例、ctx 的 全局对象、「structuredClone」、做 Nova函数_创建 用 Nova运行时实例、「structuredClone」、内建_structuredClone、1。
完。

可变 全局_异常创建数 为 0。

可变 全局_异常捕获数 为 0。

可变 全局_异常未捕获数 为 0。

可变 全局_TryCatch创建数 为 0。

异常系统_打印统计 接受 零参：
    做 打印 用 「[异常] ═══════════ 异常处理统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  异常创建数:     」、做 转字符串 用 全局_异常创建数。
    做 打印 用 做 字符串拼接 用 「  异常捕获数:     」、做 转字符串 用 全局_异常捕获数。
    做 打印 用 做 字符串拼接 用 「  未捕获异常数:   」、做 转字符串 用 全局_异常未捕获数。
    做 打印 用 做 字符串拼接 用 「  TryCatch创建数: 」、做 转字符串 用 全局_TryCatch创建数。
    做 打印 用 「[异常] ═══════════════════════════════════════」。
完。

异常_打印信息 接受 异常：
    若 异常 等于 0：
        做 打印 用 「[异常] 空异常」。
        返还。
    完。
    做 打印 用 「[异常] ── 异常信息 ──」。
    做 打印 用 做 字符串拼接 用 「  类型: 」、做 异常_类型名 用 异常 的 类型。
    做 打印 用 做 字符串拼接 用 「  消息: 」、做 转字符串 用 异常 的 消息。
    若 异常 的 行号 大于 0：
        做 打印 用 做 字符串拼接 用 「  行号: 」、做 转字符串 用 异常 的 行号。
        做 打印 用 做 字符串拼接 用 「  列号: 」、做 转字符串 用 异常 的 列号。
    完。
完。

异常处理器链_打印 接受 frame：
    若 frame 等于 0：
        返还。
    完。
    可变 处理器 为 frame 的 异常处理链。
    可变 深度 为 0。
    做 打印 用 「[异常] ── 处理器链 ──」。
    只要 处理器 不等于 0：
        做 打印 用 做 字符串拼接 用 「  [」、做 字符串拼接 用 做 转字符串 用 深度、做 字符串拼接 用 「] catch=」、做 字符串拼接 用 做 转字符串 用 处理器 的 catch_PC、做 字符串拼接 用 「 finally=」、做 转字符串 用 处理器 的 finally_PC。
        处理器 为 处理器 的 上一个。
        深度 为 深度 加 1。
    完。
    若 深度 等于 0：
        做 打印 用 「  (无处理器)」。
    完。
完。

异常处理器链_深度 接受 frame：
    若 frame 等于 0：
        返还 0。
    完。
    可变 处理器 为 frame 的 异常处理链。
    可变 深度 为 0。
    只要 处理器 不等于 0：
        深度 为 深度 加 1。
        处理器 为 处理器 的 上一个。
    完。
    返还 深度。
完。

异常_序列化为文本 接受 异常：
    若 异常 等于 0：
        返还 「null」。
    完。
    可变 结果 为 做 异常_类型名 用 异常 的 类型。
    结果 为 做 字符串拼接 用 结果、「: 」。
    若 异常 的 消息 不等于 0：
        结果 为 做 字符串拼接 用 结果、做 转字符串 用 异常 的 消息。
    否则：
        结果 为 做 字符串拼接 用 结果、「(无消息)」。
    完。
    若 异常 的 文件名 不等于 0 并且 异常 的 行号 大于 0：
        结果 为 做 字符串拼接 用 结果、「 at 」。
        结果 为 做 字符串拼接 用 结果、做 转字符串 用 异常 的 文件名。
        结果 为 做 字符串拼接 用 结果、「:」。
        结果 为 做 字符串拼接 用 结果、做 转字符串 用 异常 的 行号。
        结果 为 做 字符串拼接 用 结果、「:」。
        结果 为 做 字符串拼接 用 结果、做 转字符串 用 异常 的 列号。
    完。
    返还 结果。
完。

异常_设置原因 接受 异常、原因：
    若 异常 等于 0：
        返还。
    完。
    异常 的 原始异常 为 原因。
完。

异常_获取原因 接受 异常：
    若 异常 等于 0：
        返还 0。
    完。
    返还 异常 的 原始异常。
完。

异常_创建_带原因 接受 Nova运行时实例、类型、消息、原因：
    异常 为 做 异常_创建 用 Nova运行时实例、类型、消息。
    若 异常 不等于 0：
        异常 的 原始异常 为 原因。
    完。
    返还 异常。
完。

AI异常分析器_记录 接受 分析器、异常：
    若 分析器 等于 空 或者 异常 等于 0：
        返还。
    完。
    分析器 的 总异常次数 为 分析器 的 总异常次数 加 1。
    若 异常 的 类型 等于 错误类型_TypeError：
        分析器 的 TypeError次数 为 分析器 的 TypeError次数 加 1。
    否则若 异常 的 类型 等于 错误类型_RangeError：
        分析器 的 RangeError次数 为 分析器 的 RangeError次数 加 1。
    否则若 异常 的 类型 等于 错误类型_ReferenceError：
        分析器 的 ReferenceError次数 为 分析器 的 ReferenceError次数 加 1。
    否则若 异常 的 类型 等于 错误类型_SyntaxError：
        分析器 的 SyntaxError次数 为 分析器 的 SyntaxError次数 加 1。
    完。
    若 分析器 的 异常历史 不等于 0：
        位置 为 分析器 的 历史游标 取余于 分析器 的 历史容量。
        基址 为 分析器 的 异常历史 加 位置 乘 16。
        做 内存_写32 用 基址、异常 的 类型。
        做 内存_写64 用 基址 加 4、异常 的 消息。
        做 内存_写32 用 基址 加 12、做 平台_获取高精度时间 位与 0xFFFFFFFF。
        分析器 的 历史游标 为 分析器 的 历史游标 加 1。
        若 分析器 的 历史数量 小于 分析器 的 历史容量：
            分析器 的 历史数量 为 分析器 的 历史数量 加 1。
        完。
    完。
    可变 最大次数 为 分析器 的 TypeError次数。
    分析器 的 AI预测下次类型 为 错误类型_TypeError。
    若 分析器 的 RangeError次数 大于 最大次数：
        最大次数 为 分析器 的 RangeError次数。
        分析器 的 AI预测下次类型 为 错误类型_RangeError。
    完。
    若 分析器 的 ReferenceError次数 大于 最大次数：
        最大次数 为 分析器 的 ReferenceError次数。
        分析器 的 AI预测下次类型 为 错误类型_ReferenceError。
    完。
    若 分析器 的 总异常次数 大于 0：
        分析器 的 AI预测置信度 为 最大次数 乘 10000 除以 分析器 的 总异常次数。
    完。
完。

AI异常分析器_打印统计 接受 分析器：
    若 分析器 等于 空：
        返还。
    完。
    做 打印 用 「[AI异常] ═══════════ 异常模式分析 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  总异常次数:       」、做 转字符串 用 分析器 的 总异常次数。
    做 打印 用 做 字符串拼接 用 「  TypeError次数:    」、做 转字符串 用 分析器 的 TypeError次数。
    做 打印 用 做 字符串拼接 用 「  RangeError次数:   」、做 转字符串 用 分析器 的 RangeError次数。
    做 打印 用 做 字符串拼接 用 「  ReferenceError次: 」、做 转字符串 用 分析器 的 ReferenceError次数。
    做 打印 用 做 字符串拼接 用 「  SyntaxError次数:  」、做 转字符串 用 分析器 的 SyntaxError次数。
    做 打印 用 做 字符串拼接 用 「  未捕获次数:       」、做 转字符串 用 分析器 的 未捕获次数。
    做 打印 用 做 字符串拼接 用 「  AI预测下次类型:   」、做 异常_类型名 用 分析器 的 AI预测下次类型。
    做 打印 用 做 字符串拼接 用 「  AI预测置信度‱:   」、做 转字符串 用 分析器 的 AI预测置信度。
    做 打印 用 「[AI异常] ═══════════════════════════════════════」。
完。

本章 收束。
