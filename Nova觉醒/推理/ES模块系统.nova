《ES模块系统》

引用《.·上下文运行时》。

引用《.·值型系统》。

引用《..·中枢·系统调用表》。

引用《..·感知·内核直连层》。

引用《.·Promise辅助》。

引用《..·执行·控制台输出》。

引用《..·感知·取数网络请》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《..·执行·底层内存操》。

MODULE_UNLINKED 为 0。

MODULE_LINKING 为 1。

MODULE_LINKED 为 2。

MODULE_EVALUATING 为 3。

MODULE_EVALUATED 为 4。

MODULE_ERRORED 为 5。

EXPORT_LOCAL 为 0。

EXPORT_DEFAULT 为 1。

EXPORT_INDIRECT 为 2。

EXPORT_STAR 为 3。

IMPORT_NAMED 为 0。

IMPORT_DEFAULT 为 1。

IMPORT_NAMESPACE 为 2。

ImportEntry 是 一种 事物，含 模块请求、导入名、本地名、类型。

ExportEntry 是 一种 事物，含 导出名、本地名、模块请求、导入名、类型。

模块Record 是 一种 事物，含 URL、说明符、状态、错误、源代码、源长度、字节码、字节码长度、导入列表、导入数量、导出列表、导出数量、依赖列表、依赖数量、命名空间、环境、已求值、求值结果、dfs索引、dfs祖先索引、有顶层await、异步求值中、等待的异步依赖、在栈中、AI热度、AI预编译。

模块Record_创建 接受 URL、说明符：
    可变 模块 为 做 堆_分配 用 模块Record。
    若 模块 等于 0：
        做 打印 用 「[ESM] 错误: 模块Record分配失败」。
        返还 空。
    完。
    模块 的 URL 为 URL。
    模块 的 说明符 为 说明符。
    模块 的 状态 为 MODULE_UNLINKED。
    模块 的 错误 为 0。
    模块 的 源代码 为 0。
    模块 的 源长度 为 0。
    模块 的 字节码 为 0。
    模块 的 字节码长度 为 0。
    模块 的 导入数量 为 0。
    模块 的 导入列表 为 做 平台_申请内存 用 64 乘 32。
    模块 的 导出数量 为 0。
    模块 的 导出列表 为 做 平台_申请内存 用 64 乘 32。
    模块 的 依赖数量 为 0。
    模块 的 依赖列表 为 做 平台_申请内存 用 32 乘 8。
    模块 的 命名空间 为 0。
    模块 的 环境 为 0。
    模块 的 已求值 为 假。
    模块 的 求值结果 为 0。
    模块 的 dfs索引 为 负 1。
    模块 的 dfs祖先索引 为 负 1。
    模块 的 有顶层await 为 假。
    模块 的 异步求值中 为 假。
    模块 的 等待的异步依赖 为 0。
    模块 的 在栈中 为 假。
    模块 的 AI热度 为 0。
    模块 的 AI预编译 为 假。
    返还 模块。
完。

模块Record_销毁 接受 模块：
    若 模块 等于 空：
        返还。
    完。
    若 模块 的 导入列表 不等于 0：
        做 平台_释放内存 用 模块 的 导入列表、64 乘 32。
    完。
    若 模块 的 导出列表 不等于 0：
        做 平台_释放内存 用 模块 的 导出列表、64 乘 32。
    完。
    若 模块 的 依赖列表 不等于 0：
        做 平台_释放内存 用 模块 的 依赖列表、32 乘 8。
    完。
    做 堆_释放 用 模块。
完。

模块映射_桶数 为 128。

可变 全局模块映射 为 0。

模块映射节点 是 一种 事物，含 URL哈希、模块引用、下一个。

模块映射 是 一种 事物，含 桶数组、总数、统计_查找次数、统计_命中次数、统计_注册次数。

模块映射_创建 接受 零参：
    可变 映射 为 做 堆_分配 用 模块映射。
    若 映射 等于 0：
        返还 空。
    完。
    映射 的 桶数组 为 做 平台_申请内存 用 模块映射_桶数 乘 8。
    若 映射 的 桶数组 等于 0：
        做 堆_释放 用 映射。
        返还 空。
    完。
    做 内存_清零 用 映射 的 桶数组、模块映射_桶数 乘 8。
    映射 的 总数 为 0。
    映射 的 统计_查找次数 为 0。
    映射 的 统计_命中次数 为 0。
    映射 的 统计_注册次数 为 0。
    返还 映射。
完。

模块映射_销毁 接受 映射：
    若 映射 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 模块映射_桶数：
        可变 节点 为 做 内存_读64 用 映射 的 桶数组 加 i 乘 8。
        只要 节点 不等于 0：
            下一个 为 做 内存_读64 用 节点 加 16。
            模块 为 做 内存_读64 用 节点 加 8。
            若 模块 不等于 0：
                做 模块Record_销毁 用 模块。
            完。
            做 堆_释放 用 节点。
            节点 为 下一个。
        完。
        i 为 i 加 1。
    完。
    做 平台_释放内存 用 映射 的 桶数组、模块映射_桶数 乘 8。
    做 堆_释放 用 映射。
完。

模块映射_URL哈希 接受 URL：
    若 URL 等于 0：
        返还 0。
    完。
    可变 哈希 为 5381。
    长度 为 做 字节长度 用 URL。
    可变 i 为 0。
    只要 i 小于 长度：
        哈希 为 哈希 乘 33 加 做 字节取 用 URL、i。
        i 为 i 加 1。
    完。
    若 哈希 小于 0：
        哈希 为 0 减 哈希。
    完。
    返还 哈希。
完。

模块映射_查找 接受 映射、URL：
    若 映射 等于 空 或者 URL 等于 0：
        返还 空。
    完。
    映射 的 统计_查找次数 为 映射 的 统计_查找次数 加 1。
    哈希 为 做 模块映射_URL哈希 用 URL。
    桶索引 为 哈希 取余于 模块映射_桶数。
    可变 节点 为 做 内存_读64 用 映射 的 桶数组 加 桶索引 乘 8。
    只要 节点 不等于 0：
        若 做 内存_读32 用 节点 等于 哈希：
            映射 的 统计_命中次数 为 映射 的 统计_命中次数 加 1。
            返还 做 内存_读64 用 节点 加 8。
        完。
        节点 为 做 内存_读64 用 节点 加 16。
    完。
    返还 空。
完。

模块映射_注册 接受 映射、URL、模块：
    若 映射 等于 空 或者 URL 等于 0 或者 模块 等于 空：
        返还。
    完。
    已有 为 做 模块映射_查找 用 映射、URL。
    若 已有 不等于 空：
        返还。
    完。
    哈希 为 做 模块映射_URL哈希 用 URL。
    桶索引 为 哈希 取余于 模块映射_桶数。
    节点 为 做 堆_分配 用 模块映射节点。
    若 节点 等于 0：
        返还。
    完。
    做 内存_写32 用 节点、哈希。
    做 内存_写64 用 节点 加 8、模块。
    做 内存_写64 用 节点 加 16、做 内存_读64 用 映射 的 桶数组 加 桶索引 乘 8。
    做 内存_写64 用 映射 的 桶数组 加 桶索引 乘 8、节点。
    映射 的 总数 为 映射 的 总数 加 1。
    映射 的 统计_注册次数 为 映射 的 统计_注册次数 加 1。
完。

模块_Link 接受 模块、映射：
    若 模块 等于 空：
        返还 负 1。
    完。
    若 模块 的 状态 不等于 MODULE_UNLINKED 并且 模块 的 状态 不等于 MODULE_LINKING：
        若 模块 的 状态 等于 MODULE_ERRORED：
            返还 负 1。
        完。
        返还 0。
    完。
    模块 的 状态 为 MODULE_LINKING。
    做 打印 用 做 字符串拼接 用 「[ESM] Linking模块: 」、做 转字符串 用 模块 的 URL。
    可变 i 为 0。
    只要 i 小于 模块 的 导入数量：
        导入偏移 为 模块 的 导入列表 加 i 乘 32。
        模块请求 为 做 内存_读64 用 导入偏移。
        若 模块请求 不等于 0：
            解析后URL 为 做 模块_解析说明符 用 模块 的 URL、模块请求。
            依赖模块 为 做 模块映射_查找 用 映射、解析后URL。
            若 依赖模块 等于 空：
                做 打印 用 做 字符串拼接 用 「[ESM] 错误: 模块未找到 」、做 转字符串 用 模块请求。
                模块 的 状态 为 MODULE_ERRORED。
                返还 负 1。
            完。
            结果 为 做 模块_Link 用 依赖模块、映射。
            若 结果 小于 0：
                模块 的 状态 为 MODULE_ERRORED。
                返还 负 1。
            完。
            若 模块 的 依赖数量 小于 32：
                做 内存_写64 用 模块 的 依赖列表 加 模块 的 依赖数量 乘 8、依赖模块。
                模块 的 依赖数量 为 模块 的 依赖数量 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    做 模块_绑定导入 用 模块、映射。
    模块 的 状态 为 MODULE_LINKED。
    返还 0。
完。

模块_绑定导入 接受 模块、映射：
    若 模块 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 模块 的 导入数量：
        导入偏移 为 模块 的 导入列表 加 i 乘 32。
        模块请求 为 做 内存_读64 用 导入偏移。
        导入名 为 做 内存_读64 用 导入偏移 加 8。
        类型 为 做 内存_读32 用 导入偏移 加 24。
        若 模块请求 不等于 0：
            解析后URL 为 做 模块_解析说明符 用 模块 的 URL、模块请求。
            依赖 为 做 模块映射_查找 用 映射、解析后URL。
            若 依赖 不等于 空：
                若 类型 等于 IMPORT_NAMED 或者 类型 等于 IMPORT_DEFAULT：
                    导出存在 为 做 模块_查找导出 用 依赖、导入名。
                    若 导出存在 等于 假：
                        做 打印 用 做 字符串拼接 用 「[ESM] SyntaxError: 导入不存在 」、做 转字符串 用 导入名。
                    完。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

模块_查找导出 接受 模块、导出名：
    若 模块 等于 空 或者 导出名 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 模块 的 导出数量：
        导出偏移 为 模块 的 导出列表 加 i 乘 32。
        名称 为 做 内存_读64 用 导出偏移。
        若 名称 等于 导出名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

模块_Evaluate 接受 Nova运行时实例、模块、映射：
    若 模块 等于 空 或者 Nova运行时实例 等于 0：
        返还 负 1。
    完。
    若 模块 的 状态 等于 MODULE_EVALUATED：
        返还 0。
    完。
    若 模块 的 状态 等于 MODULE_ERRORED：
        返还 负 1。
    完。
    若 模块 的 状态 不等于 MODULE_LINKED：
        做 打印 用 「[ESM] 错误: 模块未Link, 无法Evaluate」。
        返还 负 1。
    完。
    模块 的 状态 为 MODULE_EVALUATING。
    模块 的 AI热度 为 模块 的 AI热度 加 1。
    可变 i 为 0。
    只要 i 小于 模块 的 依赖数量：
        依赖 为 做 内存_读64 用 模块 的 依赖列表 加 i 乘 8。
        若 依赖 不等于 0：
            结果 为 做 模块_Evaluate 用 Nova运行时实例、依赖、映射。
            若 结果 小于 0：
                模块 的 状态 为 MODULE_ERRORED。
                返还 负 1。
            完。
        完。
        i 为 i 加 1。
    完。
    做 打印 用 做 字符串拼接 用 「[ESM] Evaluating模块: 」、做 转字符串 用 模块 的 URL。
    若 模块 的 字节码 不等于 0 并且 模块 的 字节码长度 大于 0：
        环境 为 做 模块_创建环境 用 Nova运行时实例、模块。
        模块 的 环境 为 环境。
    完。
    模块 的 状态 为 MODULE_EVALUATED。
    模块 的 已求值 为 真。
    返还 0。
完。

模块_创建环境 接受 Nova运行时实例、模块：
    若 Nova运行时实例 等于 0 或者 模块 等于 空：
        返还 0。
    完。
    环境 为 做 平台_申请内存 用 256。
    若 环境 等于 0：
        返还 0。
    完。
    做 内存_清零 用 环境、256。
    做 内存_写64 用 环境、Nova运行时实例 的 当前上下文。
    返还 环境。
完。

模块_解析说明符 接受 基础URL、说明符：
    若 说明符 等于 0：
        返还 0。
    完。
    返还 说明符。
完。

模块_是相对路径 接受 说明符：
    若 说明符 等于 0：
        返还 假。
    完。
    c0 为 做 字节取 用 说明符、0。
    c1 为 做 字节取 用 说明符、1。
    若 c0 等于 46 并且 c1 等于 47：
        返还 真。
    完。
    若 c0 等于 46 并且 c1 等于 46：
        返还 真。
    完。
    若 c0 等于 47：
        返还 真。
    完。
    返还 假。
完。

模块_动态导入 接受 Nova运行时实例、说明符、引用脚本URL：
    若 Nova运行时实例 等于 0 或者 说明符 等于 0：
        返还 0。
    完。
    承诺 为 做 Nova承诺_创建 用 Nova运行时实例。
    若 承诺 等于 0：
        返还 0。
    完。
    URL 为 做 模块_解析说明符 用 引用脚本URL、说明符。
    若 全局模块映射 等于 0：
        全局模块映射 为 做 模块映射_创建。
    完。
    已有模块 为 做 模块映射_查找 用 全局模块映射、URL。
    若 已有模块 不等于 空 并且 已有模块 的 状态 等于 MODULE_EVALUATED：
        做 Nova承诺_解决 用 Nova运行时实例、承诺、已有模块 的 命名空间。
        返还 承诺。
    完。
    做 打印 用 做 字符串拼接 用 「[ESM] 动态import: 」、做 转字符串 用 说明符。
    返还 承诺。
完。

模块_创建ImportMeta 接受 模块：
    若 模块 等于 空：
        返还 0。
    完。
    元对象 为 做 平台_申请内存 用 64。
    若 元对象 等于 0：
        返还 0。
    完。
    做 内存_写64 用 元对象、模块 的 URL。
    返还 元对象。
完。

智能_模块_应该预编译 接受 模块、映射：
    若 模块 等于 空：
        返还 假。
    完。
    若 模块 的 AI热度 大于 3：
        返还 真。
    完。
    若 模块 的 依赖数量 小于或等于 2：
        返还 真。
    完。
    返还 假。
完。

智能_模块_预编译 接受 Nova运行时实例、模块：
    若 Nova运行时实例 等于 0 或者 模块 等于 空：
        返还。
    完。
    若 模块 的 AI预编译 等于 真：
        返还。
    完。
    若 模块 的 字节码 不等于 0：
        返还。
    完。
    若 模块 的 源代码 等于 0：
        返还。
    完。
    做 打印 用 做 字符串拼接 用 「[ESM-AI] 预编译模块: 」、做 转字符串 用 模块 的 URL。
    模块 的 AI预编译 为 真。
完。

模块映射_打印统计 接受 映射：
    若 映射 等于 空：
        返还。
    完。
    做 打印 用 「[ESM] ═══════════ 模块系统统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  注册模块数:   」、做 转字符串 用 映射 的 总数。
    做 打印 用 做 字符串拼接 用 「  查找次数:     」、做 转字符串 用 映射 的 统计_查找次数。
    做 打印 用 做 字符串拼接 用 「  命中次数:     」、做 转字符串 用 映射 的 统计_命中次数。
    做 打印 用 做 字符串拼接 用 「  注册次数:     」、做 转字符串 用 映射 的 统计_注册次数。
    若 映射 的 统计_查找次数 大于 0：
        命中率 为 映射 的 统计_命中次数 乘 10000 除以 映射 的 统计_查找次数。
        做 打印 用 做 字符串拼接 用 「  命中率‱:     」、做 转字符串 用 命中率。
    完。
    做 打印 用 「[ESM] ═══════════════════════════════════════」。
完。

模块Record_打印 接受 模块：
    若 模块 等于 空：
        做 打印 用 「[ESM] 空模块」。
        返还。
    完。
    做 打印 用 「[ESM] ── 模块信息 ──」。
    做 打印 用 做 字符串拼接 用 「  URL:         」、做 转字符串 用 模块 的 URL。
    做 打印 用 做 字符串拼接 用 「  状态:        」、做 转字符串 用 模块 的 状态。
    做 打印 用 做 字符串拼接 用 「  导入数:      」、做 转字符串 用 模块 的 导入数量。
    做 打印 用 做 字符串拼接 用 「  导出数:      」、做 转字符串 用 模块 的 导出数量。
    做 打印 用 做 字符串拼接 用 「  依赖数:      」、做 转字符串 用 模块 的 依赖数量。
    做 打印 用 做 字符串拼接 用 「  已求值:      」、做 转字符串 用 模块 的 已求值。
    做 打印 用 做 字符串拼接 用 「  有顶层await: 」、做 转字符串 用 模块 的 有顶层await。
    做 打印 用 做 字符串拼接 用 「  AI热度:      」、做 转字符串 用 模块 的 AI热度。
    做 打印 用 做 字符串拼接 用 「  AI预编译:    」、做 转字符串 用 模块 的 AI预编译。
完。

本章 收束。
