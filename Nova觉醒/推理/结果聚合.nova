《结果聚合》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·自动扩容》。

引用《..·中枢·硬链接》。

引用《..·标准·原生字符串》。

聚合策略_最佳 为 0。

聚合策略_合并 为 1。

聚合策略_投票 为 2。

聚合策略_加权 为 3。

聚合策略_回退链 为 4。

来源_TNF 为 「TNF」。

来源_模板 为 「模板」。

来源_知识库 为 「知识库」。

来源_云端 为 「云端」。

聚合结果 是 一种 事物，含 文本、来源、置信度、耗时ms、质量分。

可变 结果聚合_已初始化 为 假。

可变 当前策略 为 聚合策略_最佳。

可变 来源权重 为 做 字典。

可变 回退链顺序 为 一组：（空）。

可变 聚合_总次数 为 0。

可变 聚合_总成功 为 0。

可变 聚合_总空结果 为 0。

可变 最小置信度 为 30。

结果聚合_初始化 接受 零参：
    结果聚合_已初始化 为 真。
    当前策略 为 聚合策略_最佳。
    聚合_总次数 为 0。
    聚合_总成功 为 0。
    聚合_总空结果 为 0。
    来源权重 为 做 字典。
    做 字典设 用 来源权重、来源_TNF、90。
    做 字典设 用 来源权重、来源_云端、85。
    做 字典设 用 来源权重、来源_知识库、70。
    做 字典设 用 来源权重、来源_模板、50。
    回退链顺序 为 一组：来源_TNF、来源_知识库、来源_云端、来源_模板。
    返还 成功。
完。

结果聚合_设置策略 接受 策略：
    当前策略 为 策略。
完。

结果聚合_设置来源权重 接受 来源、新权重：
    做 字典设 用 来源权重、来源、新权重。
完。

结果聚合_设置最小置信度 接受 值：
    最小置信度 为 值。
完。

结果聚合_状态 接受 零参：
    若 结果聚合_已初始化：
        返还 「运行」。
    完。
    返还 「停止」。
完。

结果_评估质量 接受 文本：
    若 文本 等于 空 或者 做 字符串长度 用 文本 等于 0：
        返还 0。
    完。
    可变 分数 为 30。
    长度 为 做 字符串长度 用 文本。
    若 长度 大于 10：
        分数 为 分数 加 10。
    完。
    若 长度 大于 50：
        分数 为 分数 加 15。
    完。
    若 长度 大于 200：
        分数 为 分数 加 10。
    完。
    若 长度 大于 1000：
        分数 为 分数 减 5。
    完。
    若 做 字符串包含 用 文本、「。」：
        分数 为 分数 加 10。
    完。
    若 做 字符串包含 用 文本、「，」：
        分数 为 分数 加 5。
    完。
    若 分数 大于 100：
        分数 为 100。
    完。
    返还 分数。
完。

结果_获取来源权重 接受 来源：
    权重 为 做 字典取 用 来源权重、来源。
    若 权重 等于 空：
        返还 50。
    完。
    返还 权重。
完。

结果_计算综合分 接受 结果：
    若 结果 等于 空：
        返还 0。
    完。
    质量 为 做 结果_评估质量 用 结果 的 文本。
    权重 为 做 结果_获取来源权重 用 结果 的 来源。
    置信 为 结果 的 置信度。
    返还 (质量 乘 30 加 权重 乘 40 加 置信 乘 30) 除以 100。
完。

结果_选择最佳 接受 结果列表：
    若 做 计数 用 结果列表 等于 0：
        返还 空。
    完。
    可变 i 为 0。
    可变 最佳 为 空。
    可变 最高分 为 0。
    只要 i 小于 做 计数 用 结果列表：
        结果 为 结果列表 的 第 i 项。
        若 结果 不等于 空 并且 结果 不等于 「」 并且 结果 的 文本 不等于 空 并且 结果 的 置信度 大于或等于 最小置信度：
            分数 为 做 结果_计算综合分 用 结果。
            若 分数 大于 最高分：
                最高分 为 分数。
                最佳 为 结果。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最佳 不等于 空：
        返还 最佳。
    完。
    返还 空。
完。

结果_合并 接受 结果列表、分隔符：
    可变 合并 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 结果列表：
        若 结果列表 的 第 i 项 不等于 空 并且 结果列表 的 第 i 项 不等于 「」：
            若 合并 不等于 「」：
                合并 为 合并 加 分隔符。
            完。
            合并 为 合并 加 结果列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 合并。
完。

结果_按置信度选择 接受 结果列表：
    若 做 计数 用 结果列表 等于 0：
        返还 空。
    完。
    可变 最佳 为 结果列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 结果列表：
        若 结果列表 的 第 i 项 的 第 1 项 大于 最佳 的 第 1 项：
            最佳 为 结果列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最佳 的 第 0 项。
完。

结果_加权选择 接受 聚合结果列表：
    若 做 计数 用 聚合结果列表 等于 0：
        返还 空。
    完。
    可变 最佳 为 空。
    可变 最高分 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 聚合结果列表：
        结果 为 聚合结果列表 的 第 i 项。
        若 结果 等于 空 或者 结果 的 文本 等于 空：
            i 为 i 加 1。
            继续。
        完。
        若 结果 的 置信度 小于 最小置信度：
            i 为 i 加 1。
            继续。
        完。
        综合分 为 做 结果_计算综合分 用 结果。
        若 综合分 大于 最高分：
            最高分 为 综合分。
            最佳 为 结果。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

结果_投票选择 接受 聚合结果列表：
    若 做 计数 用 聚合结果列表 等于 0：
        返还 空。
    完。
    投票箱 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 聚合结果列表：
        结果 为 聚合结果列表 的 第 i 项。
        若 结果 不等于 空 并且 结果 的 文本 不等于 空：
            当前票数 为 做 字典取 用 投票箱、结果 的 文本。
            若 当前票数 等于 空：
                当前票数 为 0。
            完。
            做 字典设 用 投票箱、结果 的 文本、当前票数 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 最多票文本 为 空。
    可变 最多票数 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 聚合结果列表：
        结果 为 聚合结果列表 的 第 i 项。
        若 结果 不等于 空 并且 结果 的 文本 不等于 空：
            票数 为 做 字典取 用 投票箱、结果 的 文本。
            若 票数 大于 最多票数：
                最多票数 为 票数。
                最多票文本 为 结果 的 文本。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 最多票文本。
完。

结果_回退链选择 接受 聚合结果列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 回退链顺序：
        目标来源 为 回退链顺序 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 聚合结果列表：
            结果 为 聚合结果列表 的 第 j 项。
            若 结果 不等于 空 并且 结果 的 来源 等于 目标来源 并且 结果 的 文本 不等于 空 并且 结果 的 置信度 大于或等于 最小置信度：
                返还 结果。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

结果_聚合 接受 聚合结果列表：
    聚合_总次数 为 聚合_总次数 加 1。
    可变 最终结果 为 空。
    若 当前策略 等于 聚合策略_加权：
        最终结果 为 做 结果_加权选择 用 聚合结果列表。
    完。
    做 否则如果 用 当前策略 等于 聚合策略_投票。
    块：
        文本 为 做 结果_投票选择 用 聚合结果列表。
        若 文本 不等于 空：
            最终结果 为 新建 聚合结果：文本 为 文本、来源 为 「投票」、置信度 为 80、耗时ms 为 0、质量分 为 0。
        完。
    完。
    做 否则如果 用 当前策略 等于 聚合策略_回退链。
    块：
        最终结果 为 做 结果_回退链选择 用 聚合结果列表。
    完。
    否则。
    块：
        最终结果 为 做 结果_加权选择 用 聚合结果列表。
    完。
    若 最终结果 不等于 空：
        最终结果 的 质量分 为 做 结果_评估质量 用 最终结果 的 文本。
        聚合_总成功 为 聚合_总成功 加 1。
    否则：
        聚合_总空结果 为 聚合_总空结果 加 1。
    完。
    返还 最终结果。
完。

结果聚合_统计 接受 零参：
    返还 一组：聚合_总次数、聚合_总成功、聚合_总空结果。
完。

结果聚合_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 结果聚合_已初始化 等于 假：
        做 追加 用 问题、「结果聚合_已初始化未就绪」。
    完。
    若 当前策略 小于 聚合策略_最佳 或者 当前策略 大于 聚合策略_回退链：
        做 追加 用 问题、「当前策略越界」。
    完。
    若 做 计数 用 回退链顺序 等于 0：
        做 追加 用 问题、「回退链顺序为空」。
    完。
    若 做 计数 用 回退链顺序 大于 10000：
        做 追加 用 问题、「回退链顺序膨胀」。
    完。
    若 聚合_总次数 大于 100000：
        做 追加 用 问题、「聚合_总次数异常增长」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

结果聚合_综合报告 接受 零参：
    做 打印 用 「[结果聚合] ═══ 综合报告 ═══」。
    做 打印 用 「  结果聚合_已初始化: 」 加 结果聚合_已初始化。
    做 打印 用 「  聚合_总次数: 」 加 聚合_总次数。
    做 打印 用 「  聚合_总成功: 」 加 聚合_总成功。
    做 打印 用 「  聚合_总空结果: 」 加 聚合_总空结果。
    做 打印 用 「  健康: 」 加 若 做 结果聚合_健康评估 则 「正常」 否则 「异常」。
完。

结果聚合_全局重置 接受 零参：
    结果聚合_已初始化 为 假。
    当前策略 为 聚合策略_最佳。
    来源权重 为 做 字典。
    回退链顺序 为 一组：（空）。
    聚合_总次数 为 0。
    聚合_总成功 为 0。
    聚合_总空结果 为 0。
    最小置信度 为 30。
    做 打印 用 「[结果聚合] 统计已重置」。
    返还 0。
完。

本章 收束。
