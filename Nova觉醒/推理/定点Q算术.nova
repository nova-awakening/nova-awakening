《定点Q算术》

引用《..·执行·内核适配》。

引用《..·感知·内核直连层》。

引用《..·中枢·字符串替换》。

引用《.·权重持久》。

引用《..·执行·底层内存操》。

Q_SHIFT 为 10。

Q_ONE 为 1024。

Q_HALF 为 512。

Q_ZERO 为 0。

Q_NEG_ONE 为 负 1024。

Q乘 接受 a、b：
    返还 a 乘 b 右移 Q_SHIFT。
完。

Q除 接受 a、b：
    若 b 等于 0：
        返还 0。
    完。
    返还 (a 左移 Q_SHIFT) 除以 b。
完。

Q开方 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 猜 为 x 右移 1。
    若 猜 等于 0：
        猜 为 1。
    完。
    可变 i 为 0。
    只要 i 小于 4：
        猜 为 猜 加 做 Q除 用 x、猜 右移 1。
        i 为 i 加 1。
    完。
    返还 猜。
完。

Tensor 是 一种 事物，含 数据、行数、列数、步长、拥有内存。

Tensor_创建 接受 行数、列数：
    t 为 做 堆_分配 用 Tensor。
    t 的 行数 为 行数。
    t 的 列数 为 列数。
    t 的 步长 为 列数 乘 4。
    t 的 数据 为 做 堆_分配_数组 用 行数 乘 列数 乘 4。
    t 的 拥有内存 为 1。
    做 内存_清零 用 t 的 数据、行数 乘 列数 乘 4。
    返还 t。
完。

Tensor_释放 接受 t：
    若 t 等于 0：
        返还。
    完。
    若 t 的 拥有内存 等于 1 并且 t 的 数据 不等于 0：
        做 堆_释放 用 t 的 数据。
    完。
    做 堆_释放 用 t。
完。

Tensor_读 接受 t、行、列：
    返还 做 内存_读32有符号 用 t 的 数据 加 (行 乘 t 的 列数 加 列) 乘 4。
完。

Tensor_写 接受 t、行、列、值：
    做 内存_写32 用 t 的 数据 加 (行 乘 t 的 列数 加 列) 乘 4、值。
完。

Tensor_创建_从数据 接受 行数、列数、数据指针：
    t 为 做 堆_分配 用 Tensor。
    t 的 行数 为 行数。
    t 的 列数 为 列数。
    t 的 步长 为 列数 乘 4。
    t 的 数据 为 数据指针。
    t 的 拥有内存 为 0。
    返还 t。
完。

Tensor_克隆 接受 源：
    若 源 等于 0：
        返还 0。
    完。
    t 为 做 Tensor_创建 用 源 的 行数、源 的 列数。
    做 内存_复制 用 t 的 数据、源 的 数据、源 的 行数 乘 源 的 列数 乘 4。
    返还 t。
完。

Tensor_填充 接受 t、值：
    若 t 等于 0：
        返还。
    完。
    总 为 t 的 行数 乘 t 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        做 内存_写32 用 t 的 数据 加 i 乘 4、值。
        i 为 i 加 1。
    完。
完。

Tensor_零化 接受 t：
    若 t 等于 0：
        返还。
    完。
    做 内存_清零 用 t 的 数据、t 的 行数 乘 t 的 列数 乘 4。
完。

Tensor_获取行数 接受 t：
    若 t 等于 0：
        返还 0。
    完。
    返还 t 的 行数。
完。

Tensor_获取列数 接受 t：
    若 t 等于 0：
        返还 0。
    完。
    返还 t 的 列数。
完。

Tensor_元素数 接受 t：
    若 t 等于 0：
        返还 0。
    完。
    返还 t 的 行数 乘 t 的 列数。
完。

Tensor_矩阵乘 接受 A、B：
    若 A 等于 0 或者 B 等于 0：
        返还 0。
    完。
    若 A 的 列数 不等于 B 的 行数：
        做 打印 用 「[张量] 错误: 矩阵乘维度不匹配」。
        返还 0。
    完。
    M 为 A 的 行数。
    K 为 A 的 列数。
    N 为 B 的 列数。
    C 为 做 Tensor_创建 用 M、N。
    可变 i 为 0。
    只要 i 小于 M：
        可变 j 为 0。
        只要 j 小于 N：
            可变 和 为 0。
            可变 k 为 0。
            只要 k 小于 K：
                a 为 做 Tensor_读 用 A、i、k。
                b 为 做 Tensor_读 用 B、k、j。
                和 为 和 加 做 Q乘 用 a、b。
                k 为 k 加 1。
            完。
            做 Tensor_写 用 C、i、j、和。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 C。
完。

Tensor_矩阵乘加 接受 A、B、偏置：
    C 为 做 Tensor_矩阵乘 用 A、B。
    若 C 等于 0 或者 偏置 等于 0：
        返还 C。
    完。
    可变 i 为 0。
    只要 i 小于 C 的 行数：
        可变 j 为 0。
        只要 j 小于 C 的 列数：
            原值 为 做 Tensor_读 用 C、i、j。
            偏 为 做 Tensor_读 用 偏置、0、j。
            做 Tensor_写 用 C、i、j、原值 加 偏。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 C。
完。

Tensor_加 接受 A、B：
    若 A 等于 0 或者 B 等于 0：
        返还 0。
    完。
    C 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        a 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        b 为 做 内存_读32有符号 用 B 的 数据 加 i 乘 4。
        做 内存_写32 用 C 的 数据 加 i 乘 4、a 加 b。
        i 为 i 加 1。
    完。
    返还 C。
完。

Tensor_减 接受 A、B：
    若 A 等于 0 或者 B 等于 0：
        返还 0。
    完。
    C 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        a 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        b 为 做 内存_读32有符号 用 B 的 数据 加 i 乘 4。
        做 内存_写32 用 C 的 数据 加 i 乘 4、a 减 b。
        i 为 i 加 1。
    完。
    返还 C。
完。

Tensor_逐元素乘 接受 A、B：
    若 A 等于 0 或者 B 等于 0：
        返还 0。
    完。
    C 为 做 Tensor_创建 用 A 的 行数、A 的 列数。
    总 为 A 的 行数 乘 A 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        a 为 做 内存_读32有符号 用 A 的 数据 加 i 乘 4。
        b 为 做 内存_读32有符号 用 B 的 数据 加 i 乘 4。
        做 内存_写32 用 C 的 数据 加 i 乘 4、做 Q乘 用 a、b。
        i 为 i 加 1。
    完。
    返还 C。
完。

Tensor_缩放 接受 t、缩放因子：
    若 t 等于 0：
        返还 0。
    完。
    C 为 做 Tensor_创建 用 t 的 行数、t 的 列数。
    总 为 t 的 行数 乘 t 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        v 为 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        做 内存_写32 用 C 的 数据 加 i 乘 4、做 Q乘 用 v、缩放因子。
        i 为 i 加 1。
    完。
    返还 C。
完。

Tensor_转置 接受 A：
    若 A 等于 0：
        返还 0。
    完。
    B 为 做 Tensor_创建 用 A 的 列数、A 的 行数。
    可变 i 为 0。
    只要 i 小于 A 的 行数：
        可变 j 为 0。
        只要 j 小于 A 的 列数：
            做 Tensor_写 用 B、j、i、做 Tensor_读 用 A、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 B。
完。

Tensor_求和 接受 t：
    若 t 等于 0：
        返还 0。
    完。
    总 为 t 的 行数 乘 t 的 列数。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 总：
        和 为 和 加 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        i 为 i 加 1。
    完。
    返还 和。
完。

Tensor_最大值 接受 t：
    若 t 等于 0：
        返还 0。
    完。
    总 为 t 的 行数 乘 t 的 列数。
    可变 最大 为 做 内存_读32有符号 用 t 的 数据。
    可变 i 为 1。
    只要 i 小于 总：
        v 为 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        若 v 大于 最大：
            最大 为 v。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

Tensor_最小值 接受 t：
    若 t 等于 0：
        返还 0。
    完。
    总 为 t 的 行数 乘 t 的 列数。
    可变 最小 为 做 内存_读32有符号 用 t 的 数据。
    可变 i 为 1。
    只要 i 小于 总：
        v 为 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        若 v 小于 最小：
            最小 为 v。
        完。
        i 为 i 加 1。
    完。
    返还 最小。
完。

Tensor_均值 接受 t：
    若 t 等于 0：
        返还 0。
    完。
    总元素 为 t 的 行数 乘 t 的 列数。
    若 总元素 等于 0：
        返还 0。
    完。
    返还 做 Tensor_求和 用 t 除以 总元素。
完。

Tensor_取行 接受 t、行索引：
    若 t 等于 0 或者 行索引 大于或等于 t 的 行数：
        返还 0。
    完。
    行 为 做 Tensor_创建 用 1、t 的 列数。
    做 内存_复制 用 行 的 数据、t 的 数据 加 行索引 乘 t 的 列数 乘 4、t 的 列数 乘 4。
    返还 行。
完。

Tensor_行求和 接受 t、行索引：
    若 t 等于 0 或者 行索引 大于或等于 t 的 行数：
        返还 0。
    完。
    可变 和 为 0。
    可变 j 为 0。
    只要 j 小于 t 的 列数：
        和 为 和 加 做 Tensor_读 用 t、行索引、j。
        j 为 j 加 1。
    完。
    返还 和。
完。

Tensor_行最大值 接受 t、行索引：
    若 t 等于 0 或者 行索引 大于或等于 t 的 行数：
        返还 0。
    完。
    可变 最大 为 做 Tensor_读 用 t、行索引、0。
    可变 j 为 1。
    只要 j 小于 t 的 列数：
        v 为 做 Tensor_读 用 t、行索引、j。
        若 v 大于 最大：
            最大 为 v。
        完。
        j 为 j 加 1。
    完。
    返还 最大。
完。

可变 随机种子 为 12345。

随机_下一个 接受 零参：
    随机种子 为 随机种子 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    返还 随机种子。
完。

随机_范围 接受 最小、最大：
    若 最大 小于或等于 最小：
        返还 最小。
    完。
    返还 最小 加 做 随机_下一个 取余于 (最大 减 最小)。
完。

Tensor_随机初始化 接受 t、范围：
    若 t 等于 0：
        返还。
    完。
    总 为 t 的 行数 乘 t 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        值 为 做 随机_范围 用 0 减 范围、范围。
        做 内存_写32 用 t 的 数据 加 i 乘 4、值。
        i 为 i 加 1。
    完。
完。

张量_增益初始化 接受 t：
    若 t 等于 0：
        返还。
    完。
    扇入 为 t 的 列数。
    扇出 为 t 的 行数。
    范围 为 做 Q开方 用 做 Q除 用 6 乘 Q_ONE、(扇入 加 扇出) 乘 Q_ONE。
    做 Tensor_随机初始化 用 t、范围。
完。

Tensor_方差缩放初始化 接受 t：
    若 t 等于 0：
        返还。
    完。
    扇入 为 t 的 列数。
    范围 为 做 Q开方 用 做 Q除 用 6 乘 Q_ONE、扇入 乘 Q_ONE。
    做 Tensor_随机初始化 用 t、范围。
完。

Tensor_打印 接受 t、名称：
    若 t 等于 0：
        做 打印 用 做 字符串拼接 用 「[张量] 」、做 字符串拼接 用 名称、「 = NULL」。
        返还。
    完。
    做 打印 用 做 字符串拼接 用 「[张量] 」、做 字符串拼接 用 名称、做 字符串拼接 用 「 [」、做 字符串拼接 用 做 转字符串 用 t 的 行数、做 字符串拼接 用 「×」、做 字符串拼接 用 做 转字符串 用 t 的 列数、「]」。
    可变 i 为 0。
    显示行 为 t 的 行数。
    若 显示行 大于 3：
        显示行 为 3。
    完。
    只要 i 小于 显示行：
        可变 行文本 为 「  [」。
        可变 j 为 0。
        显示列 为 t 的 列数。
        若 显示列 大于 5：
            显示列 为 5。
        完。
        只要 j 小于 显示列：
            行文本 为 做 字符串拼接 用 行文本、做 字符串拼接 用 做 转字符串 用 做 Tensor_读 用 t、i、j、「 」。
            j 为 j 加 1。
        完。
        若 t 的 列数 大于 5：
            行文本 为 做 字符串拼接 用 行文本、「...」。
        完。
        行文本 为 做 字符串拼接 用 行文本、「]」。
        做 打印 用 行文本。
        i 为 i 加 1。
    完。
    若 t 的 行数 大于 3：
        做 打印 用 「  ...」。
    完。
完。

张量统计 是 一种 事物，含 总创建数、总释放数、总矩阵乘次数、总矩阵乘元素数、总内存分配字节、总内存释放字节、峰值内存使用、当前内存使用。

可变 全局张量统计 为 0。

张量统计_初始化 接受 零参：
    s 为 新建 张量统计。
    s 的 总创建数 为 0。
    s 的 总释放数 为 0。
    s 的 总矩阵乘次数 为 0。
    s 的 总矩阵乘元素数 为 0。
    s 的 总内存分配字节 为 0。
    s 的 总内存释放字节 为 0。
    s 的 峰值内存使用 为 0。
    s 的 当前内存使用 为 0。
    全局张量统计 为 s。
    返还 s。
完。

张量统计_打印 接受 零参：
    若 全局张量统计 等于 0：
        做 打印 用 「[张量] 统计未初始化」。
        返还。
    完。
    s 为 全局张量统计。
    做 打印 用 「[张量] ═══ 张量运算统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  总创建数: 」、做 转字符串 用 s 的 总创建数。
    做 打印 用 做 字符串拼接 用 「  总释放数: 」、做 转字符串 用 s 的 总释放数。
    做 打印 用 做 字符串拼接 用 「  总矩阵乘次数: 」、做 转字符串 用 s 的 总矩阵乘次数。
    做 打印 用 做 字符串拼接 用 「  总矩阵乘元素: 」、做 转字符串 用 s 的 总矩阵乘元素数。
    做 打印 用 做 字符串拼接 用 「  当前内存: 」、做 字符串拼接 用 做 转字符串 用 s 的 当前内存使用、「B」。
    做 打印 用 做 字符串拼接 用 「  峰值内存: 」、做 字符串拼接 用 做 转字符串 用 s 的 峰值内存使用、「B」。
    做 打印 用 「[张量] ═══════════════════════」。
完。

ShiftNorm 接受 t：
    若 t 等于 0：
        返还。
    完。
    n 为 t 的 行数 乘 t 的 列数。
    若 n 等于 0：
        返还。
    完。
    可变 sum_sq 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        x 为 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        sum_sq 为 sum_sq 加 做 Q乘 用 x、x 除以 n。
        i 为 i 加 1。
    完。
    rms 为 做 Q开方 用 sum_sq。
    若 rms 等于 0：
        返还。
    完。
    i 为 0。
    只要 i 小于 n：
        x 为 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        做 内存_写32 用 t 的 数据 加 i 乘 4、做 Q除 用 x、rms。
        i 为 i 加 1。
    完。
完。

nova_ln_v2 接受 x：
    若 x 小于或等于 0：
        返还 负 10 乘 Q_ONE。
    完。
    LN2_Q 为 709。
    可变 k 为 0。
    可变 m 为 x。
    只要 m 大于或等于 2 乘 Q_ONE：
        m 为 m 除以 2。
        k 为 k 加 1。
    完。
    只要 m 小于 Q_ONE：
        m 为 m 乘 2。
        k 为 k 减 1。
    完。
    u 为 m 减 Q_ONE。
    two_plus_u 为 2 乘 Q_ONE 加 u。
    pade_main 为 做 Q除 用 2 乘 u、two_plus_u。
    u2 为 做 Q乘 用 u、u。
    denom2 为 3 乘 做 Q乘 用 two_plus_u、two_plus_u 除以 Q_ONE。
    correction 为 做 Q乘 用 pade_main、做 Q除 用 u2、denom2。
    ln_m 为 pade_main 加 correction。
    返还 ln_m 加 k 乘 LN2_Q。
完。

归一化指数_数值稳定 接受 scores、n：
    若 scores 等于 0 或者 n 等于 0：
        返还。
    完。
    可变 max_s 为 做 内存_读32有符号 用 scores。
    可变 i 为 1。
    只要 i 小于 n：
        v 为 做 内存_读32有符号 用 scores 加 i 乘 4。
        若 v 大于 max_s：
            max_s 为 v。
        完。
        i 为 i 加 1。
    完。
    可变 sum_exp 为 0。
    i 为 0。
    只要 i 小于 n：
        v 为 做 内存_读32有符号 用 scores 加 i 乘 4。
        e 为 做 Q_exp 用 v 减 max_s。
        做 内存_写32 用 scores 加 i 乘 4、e。
        sum_exp 为 sum_exp 加 e。
        i 为 i 加 1。
    完。
    若 sum_exp 等于 0：
        返还。
    完。
    i 为 0。
    只要 i 小于 n：
        e 为 做 内存_读32有符号 用 scores 加 i 乘 4。
        做 内存_写32 用 scores 加 i 乘 4、做 Q除 用 e、sum_exp 乘 Q_ONE 除以 Q_ONE。
        i 为 i 加 1。
    完。
完。

本章 收束。
