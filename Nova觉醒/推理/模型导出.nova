《模型导出》

引用《..·中枢·类型定义》。

引用《..·记忆·接口》。

引用《.·数字人渲染》。

可变 导出_顶点数 为 0。

可变 导出_面数 为 0。

模型导出_OBJ 接受 输出路径：
    若 做 计数 用 人体顶点 等于 0：
        做 数字人渲染_构建女性高精度。
    完。
    可变 内容 为 「# NovaOS 3D数字人模型\n」。
    内容 为 内容 加 「# 程序化生成 — 纯Nova语言\n」。
    内容 为 内容 加 「# 顶点数: 」 加 做 计数 用 人体顶点 加 「\n\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 人体顶点：
        v 为 人体顶点 的 第 i 项。
        内容 为 内容 加 「v 」 加 v 的 x 除以 1000 加 「 」 加 v 的 y 除以 1000 加 「 」 加 v 的 z 除以 1000 加 「\n」。
        导出_顶点数 为 导出_顶点数 加 1。
        i 为 i 加 1。
    完。
    内容 为 内容 加 「\n# UV坐标\n」。
    i 为 0。
    只要 i 小于 做 计数 用 人体顶点：
        v 为 人体顶点 的 第 i 项。
        u 为 (v 的 x 加 200) 乘 1000 除以 400。
        vt 为 v 的 y 乘 1000 除以 1800。
        若 u 小于 0：
            u 为 0。
        完。
        若 u 大于 1000：
            u 为 1000。
        完。
        若 vt 小于 0：
            vt 为 0。
        完。
        若 vt 大于 1000：
            vt 为 1000。
        完。
        内容 为 内容 加 「vt 」 加 u 除以 1000 加 「 」 加 vt 除以 1000 加 「\n」。
        i 为 i 加 1。
    完。
    内容 为 内容 加 「\n# 面(每4个顶点组成2个三角形)\n」。
    i 为 0。
    只要 i 小于 做 计数 用 人体顶点 减 3：
        i1 为 i 加 1。
        i2 为 i 加 2。
        i3 为 i 加 3。
        内容 为 内容 加 「f 」 加 i1 加 「/」 加 i1 加 「 」 加 i2 加 「/」 加 i2 加 「 」 加 i3 加 「/」 加 i3 加 「\n」。
        导出_面数 为 导出_面数 加 1。
        i 为 i 加 4。
    完。
    做 记忆_写文件 用 输出路径、内容。
    做 打印 用 「[模型导出] OBJ已导出: 」 加 输出路径 加 「 (」 加 做 整数转字符串 用 导出_顶点数 加 「顶点 」 加 做 整数转字符串 用 导出_面数 加 「面)」。
    返还 0。
完。

模型导出_MTL 接受 输出路径：
    可变 内容 为 「# NovaOS 数字人材质\n\n」。
    内容 为 内容 加 「newmtl 皮肤\n」。
    内容 为 内容 加 「Kd 1.0 0.855 0.725\n」。
    内容 为 内容 加 「Ks 0.3 0.3 0.3\n」。
    内容 为 内容 加 「Ns 30\n」。
    内容 为 内容 加 「map_Kd 资源/数字人/正面.png\n\n」。
    内容 为 内容 加 「newmtl 头发\n」。
    内容 为 内容 加 「Kd 0.08 0.06 0.15\n」。
    内容 为 内容 加 「Ks 0.5 0.5 0.5\n」。
    内容 为 内容 加 「Ns 80\n\n」。
    内容 为 内容 加 「newmtl 纱衣\n」。
    内容 为 内容 加 「Kd 0.8 0.8 0.9\n」。
    内容 为 内容 加 「d 0.3\n」。
    内容 为 内容 加 「Ks 0.8 0.8 0.8\n\n」。
    内容 为 内容 加 「newmtl 金饰\n」。
    内容 为 内容 加 「Kd 1.0 0.843 0.0\n」。
    内容 为 内容 加 「Ks 1.0 1.0 0.8\n」。
    内容 为 内容 加 「Ns 200\n\n」。
    内容 为 内容 加 「newmtl 眼睛\n」。
    内容 为 内容 加 「Kd 0.7 0.0 1.0\n」。
    内容 为 内容 加 「Ke 0.7 0.0 1.0\n」。
    内容 为 内容 加 「Ns 500\n\n」。
    做 记忆_写文件 用 输出路径、内容。
    做 打印 用 「[模型导出] MTL已导出: 」 加 输出路径。
完。

模型导出_完整 接受 目录：
    做 模型导出_OBJ 用 目录 加 「数字人.obj」。
    做 模型导出_MTL 用 目录 加 「数字人.mtl」。
    做 打印 用 「[模型导出] 完整模型已导出到 」 加 目录。
    做 打印 用 「[模型导出] 可用Blender打开.obj文件查看和编辑」。
完。

本章 收束。
