《检索增强推理》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《.·语义嵌入》。

引用《.·记忆管理》。

引用《..·记忆·接口》。

引用《..·中枢·接口》。

引用《..·中枢·硬链接》。

引用《.·定点Q算术》。

引用《.·AI总线桥接》。

引用《..·中枢·辅助存根》。

知识块大小 为 512。

最大检索数 为 10。

相似度阈值 为 600。

上下文窗口 为 4096。

RAG_近似重复阈值 为 940。

知识块 是 一种 事物，含 内容、嵌入向量、来源、时间戳、访问次数、相关度。

知识库 是 一种 事物，含 块列表、块数、容量、总访问、最近检索时间。

可变 全局知识库 为 0。

可变 检索_总次数 为 0。

可变 检索_命中数 为 0。

可变 检索_空结果数 为 0。

可变 索引_总块数 为 0。

可变 索引_总更新数 为 0。

可变 检索_累计综合分 为 0。

可变 检索_最高综合分 为 0。

可变 检索_最近命中块数 为 0。

可变 知识块内容哈希 为 一组：（空）。

可变 知识块草图 为 一组：（空）。

可变 知识块质量 为 一组：（空）。

可变 知识块综合分 为 一组：（空）。

可变 索引_重复跳过数 为 0。

可变 索引_无效块跳过数 为 0。

可变 RAG_近似重复阈值 为 940。

RAG_创建失败清理 接受 库：
    若 库 等于 0：
        返还。
    完。
    若 库 的 块列表 不等于 0：
        做 堆_释放 用 库 的 块列表。
    完。
    做 堆_释放 用 库。
完。

知识库_创建 接受 容量：
    若 容量 小于或等于 0：
        返还 0。
    完。
    若 全局知识库 不等于 0：
        做 RAG_释放知识库 用 全局知识库。
        全局知识库 为 0。
    完。
    检索_总次数 为 0。
    检索_命中数 为 0。
    检索_空结果数 为 0。
    检索_累计综合分 为 0。
    检索_最高综合分 为 0。
    检索_最近命中块数 为 0。
    库 为 做 堆_分配 用 知识库。
    若 库 等于 0：
        返还 0。
    完。
    库 的 块列表 为 做 堆_分配_数组 用 知识块、容量。
    若 库 的 块列表 等于 0：
        做 RAG_创建失败清理 用 库。
        返还 0。
    完。
    库 的 块数 为 0。
    库 的 容量 为 容量。
    库 的 总访问 为 0。
    库 的 最近检索时间 为 0。
    知识块内容哈希 为 一组：（空）。
    知识块草图 为 一组：（空）。
    知识块质量 为 一组：（空）。
    知识块综合分 为 一组：（空）。
    索引_总块数 为 0。
    索引_总更新数 为 0。
    索引_重复跳过数 为 0。
    索引_无效块跳过数 为 0。
    全局知识库 为 库。
    返还 库。
完。

RAG_释放知识块草图 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 知识块草图：
        若 知识块草图 的 第 i 项 不等于 0：
            做 Tensor_释放 用 知识块草图 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    知识块草图 为 一组：（空）。
    知识块内容哈希 为 一组：（空）。
    知识块质量 为 一组：（空）。
    知识块综合分 为 一组：（空）。
完。

RAG_释放知识库 接受 库：
    若 库 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 库 的 块数：
        块 为 库 的 块列表 加 i 乘 48。
        块向量 为 做 内存_读64 用 块 加 8。
        若 块向量 不等于 0：
            做 Tensor_释放 用 块向量。
        完。
        i 为 i 加 1。
    完。
    若 库 的 块列表 不等于 0：
        做 堆_释放 用 库 的 块列表。
    完。
    若 库 等于 全局知识库：
        全局知识库 为 0。
    完。
    做 堆_释放 用 库。
    做 RAG_释放知识块草图。
完。

RAG_知识块是否重复 接受 内容、内容哈希、草图：
    可变 i 为 0。
    只要 i 小于 做 计数 用 知识块内容哈希：
        若 知识块内容哈希 的 第 i 项 等于 内容哈希：
            返还 真。
        完。
        若 草图 不等于 0 并且 i 小于 做 计数 用 知识块草图 并且 知识块草图 的 第 i 项 不等于 0：
            相似 为 做 语义嵌入_草图相似度 用 草图、知识块草图 的 第 i 项。
            若 相似 大于或等于 RAG_近似重复阈值：
                返还 真。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

知识库_添加 接受 库、内容、来源：
    若 库 等于 0 或者 内容 等于 0：
        返还 负 1。
    完。
    若 库 的 块数 大于或等于 库 的 容量：
        返还 负 1。
    完。
    内容哈希 为 做 字符串哈希 用 内容。
    内容向量 为 做 语义嵌入_编码 用 内容。
    内容草图 为 做 语义嵌入_压缩草图 用 内容向量、嵌入维度_节点。
    内容质量 为 做 语义嵌入_草图质量 用 内容向量、内容草图。
    若 内容向量 等于 0 或者 内容草图 等于 0：
        索引_无效块跳过数 为 索引_无效块跳过数 加 1。
        若 内容向量 不等于 0：
            做 Tensor_释放 用 内容向量。
        完。
        若 内容草图 不等于 0：
            做 Tensor_释放 用 内容草图。
        完。
        返还 负 3。
    完。
    若 内容质量 小于 300：
        索引_无效块跳过数 为 索引_无效块跳过数 加 1。
        做 Tensor_释放 用 内容向量。
        做 Tensor_释放 用 内容草图。
        返还 负 4。
    完。
    若 做 RAG_知识块是否重复 用 内容、内容哈希、内容草图：
        索引_重复跳过数 为 索引_重复跳过数 加 1。
        若 内容向量 不等于 0：
            做 Tensor_释放 用 内容向量。
        完。
        若 内容草图 不等于 0：
            做 Tensor_释放 用 内容草图。
        完。
        返还 负 2。
    完。
    块 为 库 的 块列表 加 库 的 块数 乘 48。
    做 内存_写64 用 块、内容。
    做 内存_写64 用 块 加 8、内容向量。
    做 内存_写64 用 块 加 16、来源。
    做 内存_写64 用 块 加 24、做 中枢_当前时间纳秒。
    做 内存_写64 用 块 加 32、0。
    做 内存_写64 用 块 加 40、内容质量。
    做 追加 用 知识块内容哈希、内容哈希。
    做 追加 用 知识块草图、内容草图。
    做 追加 用 知识块质量、内容质量。
    做 追加 用 知识块综合分、内容质量。
    库 的 块数 为 库 的 块数 加 1。
    索引_总块数 为 索引_总块数 加 1。
    索引_总更新数 为 索引_总更新数 加 1。
    返还 库 的 块数 减 1。
完。

RAG_限制值 接受 值、最大值：
    若 值 大于 最大值：
        返还 最大值。
    完。
    返还 值。
完。

RAG_检测中文密度 接受 内容指针：
    若 内容指针 等于 0：
        返还 0。
    完。
    总长 为 做 字符串_长度_ptr 用 内容指针。
    若 总长 等于 0：
        返还 0。
    完。
    扫描长 为 做 最小值 用 总长、100。
    可变 中文数 为 0。
    可变 i 为 0。
    只要 i 小于 扫描长：
        c 为 做 字符串_字符码_ptr 用 内容指针、i。
        若 c 大于或等于 0x4E00 并且 c 小于或等于 0x9FFF：
            中文数 为 中文数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 中文数 乘 1000 除以 扫描长。
完。

RAG_综合评分 接受 相似度、访问次数、时间戳、当前时间：
    访问奖励 为 做 RAG_限制值 用 访问次数 乘 16、128。
    可变 新鲜度奖励 为 0。
    可变 年龄秒 为 0。
    若 当前时间 大于 时间戳：
        年龄秒 为 (当前时间 减 时间戳) 除以 1000000000。
    完。
    若 年龄秒 小于 60：
        新鲜度奖励 为 64。
    否则若 年龄秒 小于 300：
        新鲜度奖励 为 32。
    否则若 年龄秒 小于 3600：
        新鲜度奖励 为 16。
    完。
    返还 相似度 加 访问奖励 加 新鲜度奖励。
完。

知识库_检索 接受 库、查询、数量：
    若 库 等于 0 或者 库 的 块数 等于 0 或者 数量 小于或等于 0 或者 查询 等于 0：
        检索_空结果数 为 检索_空结果数 加 1。
        返还 一组：（空）。
    完。
    检索_总次数 为 检索_总次数 加 1。
    查询向量 为 做 语义嵌入_编码 用 查询。
    若 查询向量 等于 0：
        检索_空结果数 为 检索_空结果数 加 1。
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    查询时间 为 做 中枢_当前时间纳秒。
    查询草图 为 做 语义嵌入_压缩草图 用 查询向量、嵌入维度_节点。
    可变 i 为 0。
    只要 i 小于 库 的 块数：
        块 为 库 的 块列表 加 i 乘 48。
        块向量 为 做 内存_读64 用 块 加 8。
        若 块向量 等于 0：
            i 为 i 加 1。
            继续。
        完。
        相似度 为 做 余弦相似度 用 查询向量、块向量、嵌入维度_节点。
        若 查询草图 不等于 0 并且 i 小于 做 计数 用 知识块草图 并且 知识块草图 的 第 i 项 不等于 0：
            相似度 为 相似度 加 做 语义嵌入_草图相似度 用 查询草图、知识块草图 的 第 i 项 除以 4。
        完。
        块内容指针 为 做 内存_读64 用 块。
        若 块内容指针 不等于 0：
            块中文密度 为 做 RAG_检测中文密度 用 块内容指针。
            若 块中文密度 大于 500：
                相似度 为 相似度 加 块中文密度 乘 80 除以 1000。
            完。
        完。
        若 相似度 大于或等于 相似度阈值：
            新访问 为 做 内存_读64 用 块 加 32 加 1。
            时间戳 为 做 内存_读64 用 块 加 24。
            块质量 为 0。
            若 i 小于 做 计数 用 知识块质量：
                块质量 为 知识块质量 的 第 i 项。
            否则：
                块质量 为 做 内存_读64 用 块 加 40。
            完。
            综合分 为 做 RAG_综合评分 用 相似度、新访问、时间戳、查询时间。
            综合分 为 综合分 加 块质量 除以 10。
            做 内存_写64 用 块 加 32、新访问。
            若 i 小于 做 计数 用 知识块综合分：
                知识块综合分 的 第 i 项 为 综合分。
            完。
            做 追加 用 结果、一本字典：索引 对应 i、相似度 对应 相似度、综合分 对应 综合分、内容 对应 做 内存_读64 用 块、来源 对应 做 内存_读64 用 块 加 16、访问次数 对应 新访问。
        完。
        i 为 i 加 1。
    完。
    结果 为 做 排序_按字段 用 结果、「综合分」、真。
    若 做 计数 用 结果 大于 数量：
        结果 为 做 列表_截取 用 结果、0、数量。
    完。
    若 查询向量 不等于 0：
        做 Tensor_释放 用 查询向量。
    完。
    若 查询草图 不等于 0：
        做 Tensor_释放 用 查询草图。
    完。
    若 做 计数 用 结果 大于 0：
        检索_命中数 为 检索_命中数 加 1。
        检索_最近命中块数 为 做 计数 用 结果。
        检索_累计综合分 为 检索_累计综合分 加 结果 的 第 0 项 的 综合分。
        若 结果 的 第 0 项 的 综合分 大于 检索_最高综合分：
            检索_最高综合分 为 结果 的 第 0 项 的 综合分。
        完。
    否则：
        检索_空结果数 为 检索_空结果数 加 1。
        检索_最近命中块数 为 0。
    完。
    库 的 总访问 为 库 的 总访问 加 1。
    库 的 最近检索时间 为 做 中枢_当前时间纳秒。
    返还 结果。
完。

RAG_推理 接受 查询：
    若 全局知识库 等于 0：
        返还 0。
    完。
    相关块 为 做 知识库_检索 用 全局知识库、查询、最大检索数。
    记忆上下文 为 做 智能体记忆_组装上下文 用 查询、上下文窗口 除以 3。
    可变 上下文 为 「相关知识:\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 相关块：
        上下文 为 上下文 加 「- 」 加 相关块 的 第 i 项 的 内容。
        若 相关块 的 第 i 项 的 来源 不等于 0：
            上下文 为 上下文 加 「 [来源: 」 加 相关块 的 第 i 项 的 来源 加 「]」。
        完。
        上下文 为 上下文 加 「\n」。
        i 为 i 加 1。
    完。
    若 记忆上下文 不等于 0 并且 记忆上下文 不等于 「」：
        上下文 为 上下文 加 「\n记忆上下文:\n」 加 记忆上下文。
    完。
    上下文 为 上下文 加 「\n问题: 」 加 查询。
    上下文向量 为 做 语义嵌入_编码 用 上下文。
    若 上下文向量 等于 0：
        返还 0。
    完。
    推理结果 为 做 AI总线_tick 用 上下文向量。
    若 推理结果 等于 0：
        推理结果 为 做 Tensor_克隆 用 上下文向量。
    完。
    做 Tensor_释放 用 上下文向量。
    返还 推理结果。
完。

RAG_跳过空白 接受 内容、起始、总长：
    可变 pos 为 起始。
    只要 pos 小于 总长：
        c 为 做 字符取 用 内容、pos。
        若 c 不等于 10 并且 c 不等于 13 并且 c 不等于 9 并且 c 不等于 32：
            返还 pos。
        完。
        pos 为 pos 加 1。
    完。
    返还 pos。
完。

RAG_定位分块终点 接受 内容、起始、最大长度、总长：
    硬终点 为 起始 加 最大长度。
    若 硬终点 大于或等于 总长：
        返还 总长。
    完。
    可变 搜索终点 为 硬终点 加 128。
    若 搜索终点 大于 总长：
        搜索终点 为 总长。
    完。
    可变 i 为 硬终点。
    只要 i 小于 搜索终点：
        c 为 做 字符取 用 内容、i。
        若 c 等于 10 或者 c 等于 13 或者 c 等于 44 或者 c 等于 59 或者 c 等于 46 或者 c 等于 33 或者 c 等于 63 或者 c 等于 12290 或者 c 等于 65292 或者 c 等于 65307 或者 c 等于 65311 或者 c 等于 65281：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 硬终点。
完。

RAG_加载知识 接受 文件路径：
    若 全局知识库 等于 0：
        若 做 知识库_创建 用 1024 等于 0：
            返还 0。
        完。
    完。
    内容 为 做 记忆_读文件 用 文件路径。
    若 内容 等于 0：
        返还 0。
    完。
    可变 已加载 为 0。
    可变 偏移 为 0。
    总长 为 做 字符串长度 用 内容。
    只要 偏移 小于 总长：
        偏移 为 做 RAG_跳过空白 用 内容、偏移、总长。
        若 偏移 大于或等于 总长：
            跳出。
        完。
        终点 为 做 RAG_定位分块终点 用 内容、偏移、知识块大小、总长。
        块长 为 终点 减 偏移。
        若 块长 小于或等于 0：
            若 终点 大于或等于 总长：
                跳出。
            完。
            偏移 为 终点 加 1。
            继续。
        完。
        块 为 做 字符串_子串 用 内容、偏移、终点。
        若 块 不等于 0 并且 做 字符串长度 用 块 大于 16：
            结果 为 做 知识库_添加 用 全局知识库、块、文件路径。
            若 结果 等于 负 1：
                跳出。
            完。
            若 结果 大于或等于 0：
                已加载 为 已加载 加 1。
            完。
        完。
        若 终点 大于或等于 总长：
            偏移 为 总长。
        否则：
            偏移 为 终点 加 1。
        完。
    完。
    返还 已加载。
完。

排序_字段值 接受 项、字段：
    若 字段 等于 「综合分」：
        返还 项 的 综合分。
    完。
    若 字段 等于 「相似度」：
        返还 项 的 相似度。
    完。
    若 字段 等于 「访问次数」：
        返还 项 的 访问次数。
    完。
    若 字段 等于 「索引」：
        返还 项 的 索引。
    完。
    返还 0。
完。

TIMSORT_MIN_MERGE 为 32。

TimSort_归并 接受 arr、左、中、右、缓冲：
    left_len 为 中 减 左 加 1。
    可变 i 为 0。
    只要 i 小于 left_len：
        做 内存_写32 用 缓冲 加 i 乘 4、做 内存_读32有符号 用 arr 加 (左 加 i) 乘 4。
        i 为 i 加 1。
    完。
    可变 li 为 0。
    可变 ri 为 中 加 1。
    可变 k 为 左。
    只要 li 小于 left_len 并且 ri 小于或等于 右：
        若 做 内存_读32有符号 用 缓冲 加 li 乘 4 小于或等于 做 内存_读32有符号 用 arr 加 ri 乘 4：
            做 内存_写32 用 arr 加 k 乘 4、做 内存_读32有符号 用 缓冲 加 li 乘 4。
            li 为 li 加 1。
        否则：
            做 内存_写32 用 arr 加 k 乘 4、做 内存_读32有符号 用 arr 加 ri 乘 4。
            ri 为 ri 加 1。
        完。
        k 为 k 加 1。
    完。
    只要 li 小于 left_len：
        做 内存_写32 用 arr 加 k 乘 4、做 内存_读32有符号 用 缓冲 加 li 乘 4。
        li 为 li 加 1。
        k 为 k 加 1。
    完。
完。

本章 收束。
