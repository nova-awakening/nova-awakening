《用户管理》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·哈希整数》。

引用《..·中枢·内容可见性》。

最大用户 为 512。

角色_访客 为 0。

角色_普通 为 1。

角色_高级 为 2。

角色_管理员 为 3。

权限_读 为 1。

权限_写 为 2。

权限_执行 为 4。

权限_管理 为 8。

用户记录 是 一种 事物，含 标识哈希、名称哈希、密码哈希、角色、权限掩码、注册时刻、最近活跃、登录次数、消息数、绑定渠道、已禁用。

可变 用户表 为 一组：（空）。

可变 用户数 为 0。

可变 注册总数 为 0。

可变 认证总数 为 0。

可变 认证失败 为 0。

用户_初始化 接受 零参：
    用户表 为 一组：（空）。
    用户数 为 0。
    做 用户_注册 用 「管理员」、「管理员」、0、角色_管理员。
完。

用户_注册 接受 名称、标识、密码哈希、角色：
    若 用户数 大于或等于 最大用户：
        返还 负 1。
    完。
    注册总数 为 注册总数 加 1。
    用户 为 新建 用户记录。
    用户 的 标识哈希 为 做 哈希_字符串 用 标识。
    用户 的 名称哈希 为 做 哈希_字符串 用 名称。
    用户 的 密码哈希 为 密码哈希。
    用户 的 角色 为 角色。
    若 角色 等于 角色_管理员：
        用户 的 权限掩码 为 权限_读 位或 权限_写 位或 权限_执行 位或 权限_管理。
    否则若 角色 等于 角色_高级：
        用户 的 权限掩码 为 权限_读 位或 权限_写 位或 权限_执行。
    否则若 角色 等于 角色_普通：
        用户 的 权限掩码 为 权限_读 位或 权限_写。
    否则：
        用户 的 权限掩码 为 权限_读。
    完。
    用户 的 注册时刻 为 做 平台_获取高精度时间。
    用户 的 最近活跃 为 用户 的 注册时刻。
    用户 的 登录次数 为 0。
    用户 的 消息数 为 0。
    用户 的 绑定渠道 为 0。
    用户 的 已禁用 为 假。
    做 追加 用 用户表、用户。
    索引 为 用户数。
    用户数 为 用户数 加 1。
    返还 索引。
完。

用户_认证 接受 标识、密码哈希：
    认证总数 为 认证总数 加 1。
    目标 为 做 哈希_字符串 用 标识。
    可变 序号 为 0。
    只要 序号 小于 用户数：
        若 用户表 的 第 序号 项 的 标识哈希 等于 目标 并且 非 用户表 的 第 序号 项 的 已禁用：
            若 用户表 的 第 序号 项 的 密码哈希 等于 密码哈希 或者 密码哈希 等于 0：
                用户表 的 第 序号 项 的 登录次数 为 用户表 的 第 序号 项 的 登录次数 加 1。
                用户表 的 第 序号 项 的 最近活跃 为 做 平台_获取高精度时间。
                返还 序号。
            完。
            认证失败 为 认证失败 加 1。
            返还 负 1。
        完。
        序号 为 序号 加 1。
    完。
    认证失败 为 认证失败 加 1。
    返还 负 1。
完。

用户_查找 接受 标识：
    目标 为 做 哈希_字符串 用 标识。
    可变 序号 为 0。
    只要 序号 小于 用户数：
        若 用户表 的 第 序号 项 的 标识哈希 等于 目标：
            返还 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 负 1。
完。

用户_自动注册 接受 渠道、标识：
    索引 为 做 用户_查找 用 标识。
    若 索引 大于或等于 0：
        用户表 的 第 索引 项 的 最近活跃 为 做 平台_获取高精度时间。
        返还 索引。
    完。
    返还 做 用户_注册 用 标识、标识、0、角色_普通。
完。

用户_检查权限 接受 索引、权限：
    若 索引 小于 0 或者 索引 大于或等于 用户数：
        返还 假。
    完。
    若 用户表 的 第 索引 项 的 已禁用：
        返还 假。
    完。
    返还 用户表 的 第 索引 项 的 权限掩码 位与 权限 等于 权限。
完。

用户_是管理员 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 用户数：
        返还 假。
    完。
    返还 用户表 的 第 索引 项 的 角色 等于 角色_管理员。
完。

用户_设角色 接受 索引、角色：
    若 索引 小于 0 或者 索引 大于或等于 用户数：
        返还。
    完。
    用户表 的 第 索引 项 的 角色 为 角色。
完。

用户_禁用 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 用户数：
        返还。
    完。
    用户表 的 第 索引 项 的 已禁用 为 真。
完。

用户_启用 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 用户数：
        返还。
    完。
    用户表 的 第 索引 项 的 已禁用 为 假。
完。

用户_记录消息 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 用户数：
        返还。
    完。
    用户表 的 第 索引 项 的 消息数 为 用户表 的 第 索引 项 的 消息数 加 1。
    用户表 的 第 索引 项 的 最近活跃 为 做 平台_获取高精度时间。
完。

用户_绑定渠道 接受 索引、渠道哈希：
    若 索引 小于 0 或者 索引 大于或等于 用户数：
        返还。
    完。
    用户表 的 第 索引 项 的 绑定渠道 为 渠道哈希。
完。

用户_活跃数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 用户数：
        若 非 用户表 的 第 序号 项 的 已禁用：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

用户_完整统计 接受 零参：
    返还 一组：用户数、做 用户_活跃数、注册总数、认证总数、认证失败。
完。

本章 收束。
