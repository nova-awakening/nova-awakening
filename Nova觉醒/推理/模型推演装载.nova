《模型推演装载》

引用《.·模型同步》。

引用《.·Flash注意力》。

引用《.·投机解码》。

引用《.·混合专家》。

引用《.·分页注意力》。

引用《.·结构相似块》。

引用《.·JND感知差异》。

引用《.·权重转换》。

推理模型推演装载_模块清单 接受 零参：
    返还 一组：「./模型同步.nova」、「./Flash注意力.nova」、「./投机解码.nova」、「./混合专家.nova」、「./分页注意力.nova」、「./结构相似块.nova」、「./JND感知差异.nova」、「./权重转换.nova」。
完。

推理模型推演装载_统计 接受 零参：
    返还 一本字典：模块数 对应 做 计数 用 做 推理模型推演装载_模块清单。
完。

本章 收束。
