《节点聚类》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·定点Q算术》。

引用《..·感知·原子驻留》。

引用《..·中枢·激活函数层》。

引用《..·感知·内核直连层》。

引用《..·中枢·字符串替换》。

语义嵌入_KMeans聚类 接受 嵌入矩阵、K、迭代次数：
    N 为 嵌入矩阵 的 行数。
    D 为 嵌入矩阵 的 列数。
    若 N 等于 0 或者 K 等于 0：
        返还 0。
    完。
    中心 为 做 Tensor_创建 用 K、D。
    可变 k 为 0。
    只要 k 小于 K 并且 k 小于 N：
        可变 d 为 0。
        只要 d 小于 D：
            做 Tensor_写 用 中心、k、d、做 Tensor_读 用 嵌入矩阵、k、d。
            d 为 d 加 1。
        完。
        k 为 k 加 1。
    完。
    分配 为 做 Tensor_创建 用 1、N。
    可变 iter 为 0。
    只要 iter 小于 迭代次数：
        可变 i 为 0。
        只要 i 小于 N：
            可变 最近距离 为 0x7FFFFFFF。
            可变 最近中心 为 0。
            k 为 0。
            只要 k 小于 K：
                可变 距离 为 0。
                可变 d 为 0。
                只要 d 小于 D：
                    差 为 做 Tensor_读 用 嵌入矩阵、i、d 减 做 Tensor_读 用 中心、k、d。
                    距离 为 距离 加 做 Q乘 用 差、差。
                    d 为 d 加 1。
                完。
                若 距离 小于 最近距离：
                    最近距离 为 距离。
                    最近中心 为 k。
                完。
                k 为 k 加 1。
            完。
            做 Tensor_写 用 分配、0、i、最近中心。
            i 为 i 加 1。
        完。
        计数 为 做 Tensor_创建 用 1、K。
        新中心 为 做 Tensor_创建 用 K、D。
        i 为 0。
        只要 i 小于 N：
            簇 为 做 Tensor_读 用 分配、0、i。
            旧计数 为 做 Tensor_读 用 计数、0、簇。
            做 Tensor_写 用 计数、0、簇、旧计数 加 Q_ONE。
            可变 d 为 0。
            只要 d 小于 D：
                旧 为 做 Tensor_读 用 新中心、簇、d。
                做 Tensor_写 用 新中心、簇、d、旧 加 做 Tensor_读 用 嵌入矩阵、i、d。
                d 为 d 加 1。
            完。
            i 为 i 加 1。
        完。
        k 为 0。
        只要 k 小于 K：
            c 为 做 Tensor_读 用 计数、0、k。
            若 c 大于 0：
                可变 d 为 0。
                只要 d 小于 D：
                    做 Tensor_写 用 中心、k、d、做 Q除 用 做 Tensor_读 用 新中心、k、d、c。
                    d 为 d 加 1。
                完。
            完。
            k 为 k 加 1。
        完。
        做 Tensor_释放 用 计数。
        做 Tensor_释放 用 新中心。
        iter 为 iter 加 1。
    完。
    做 Tensor_释放 用 中心。
    返还 分配。
完。

提取_URL特征 接受 url：
    特征 为 做 Tensor_创建 用 1、12。
    若 url 等于 0：
        返还 特征。
    完。
    长度 为 做 字符串_长度 用 url。
    做 Tensor_写 用 特征、0、0、若 长度 大于 200 则 Q_ONE 否则 长度 乘 Q_ONE 除以 200。
    可变 斜杠数 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        若 做 内存_读8 用 url 加 i 等于 0x2F：
            斜杠数 为 斜杠数 加 1。
        完。
        i 为 i 加 1。
    完。
    做 Tensor_写 用 特征、0、1、若 斜杠数 大于 10 则 Q_ONE 否则 斜杠数 乘 Q_ONE 除以 10。
    可变 参数数 为 0。
    i 为 0。
    只要 i 小于 长度：
        若 做 内存_读8 用 url 加 i 等于 0x26：
            参数数 为 参数数 加 1。
        完。
        i 为 i 加 1。
    完。
    做 Tensor_写 用 特征、0、2、若 参数数 大于 10 则 Q_ONE 否则 参数数 乘 Q_ONE 除以 10。
    做 Tensor_写 用 特征、0、3、若 做 字符串_包含 用 url、「ad」 或者 做 字符串_包含 用 url、「ads」 则 Q_ONE 否则 0。
    做 Tensor_写 用 特征、0、4、若 做 字符串_包含 用 url、「track」 或者 做 字符串_包含 用 url、「analytics」 或者 做 字符串_包含 用 url、「beacon」 则 Q_ONE 否则 0。
    做 Tensor_写 用 特征、0、5、若 做 字符串_包含 用 url、「cdn」 或者 做 字符串_包含 用 url、「static」 则 Q_ONE 否则 0。
    可变 扩展类型 为 0。
    若 做 字符串_以结尾 用 url、「.js」：
        扩展类型 为 Q_ONE 除以 4。
    否则若 做 字符串_以结尾 用 url、「.css」：
        扩展类型 为 Q_ONE 除以 2。
    否则若 做 字符串_以结尾 用 url、「.png」 或者 做 字符串_以结尾 用 url、「.jpg」 或者 做 字符串_以结尾 用 url、「.webp」：
        扩展类型 为 3 乘 Q_ONE 除以 4。
    否则若 做 字符串_以结尾 用 url、「.woff2」 或者 做 字符串_以结尾 用 url、「.woff」：
        扩展类型 为 Q_ONE。
    完。
    做 Tensor_写 用 特征、0、6、扩展类型。
    域名哈希 为 做 字符串_哈希 用 url 位与 0xFFFF。
    做 Tensor_写 用 特征、0、7、域名哈希 乘 Q_ONE 除以 65536。
    全哈希 为 做 字符串_哈希 用 url。
    做 Tensor_写 用 特征、0、8、(全哈希 右移 24 位与 0xFF) 乘 4。
    做 Tensor_写 用 特征、0、9、(全哈希 右移 16 位与 0xFF) 乘 4。
    做 Tensor_写 用 特征、0、10、(全哈希 右移 8 位与 0xFF) 乘 4。
    做 Tensor_写 用 特征、0、11、(全哈希 位与 0xFF) 乘 4。
    返还 特征。
完。

语义嵌入_计算URL嵌入 接受 引擎、url：
    若 引擎 等于 0 或者 url 等于 0：
        返还 0。
    完。
    特征 为 做 提取_URL特征 用 url。
    嵌入 为 做 Nova多层感知器_前向 用 引擎 的 URL嵌入网络、特征。
    做 Tensor_释放 用 特征。
    返还 嵌入。
完。

本章 收束。
