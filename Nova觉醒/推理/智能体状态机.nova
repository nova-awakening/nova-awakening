《智能体状态机》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·思考链》。

引用《..·标准·原生列表》。

引用《..·中枢·高精度定时器hrtimer》。

智能体_空闲 为 0。

智能体_感知 为 1。

智能体_思考 为 2。

智能体_决策 为 3。

智能体_执行 为 4。

智能体_观察 为 5。

智能体_完成 为 6。

智能体_错误 为 7。

智能体_等待 为 8。

AG_状态 为 0。

AG_任务队列 为 1。

AG_当前任务 为 2。

AG_步数 为 3。

AG_最大步数 为 4。

AG_思考链 为 5。

AG_记忆 为 6。

AG_历史 为 7。

AG_错误计数 为 8。

AG_总任务数 为 9。

AG_成功任务 为 10。

AG_字段数 为 11。

智能体状态机_创建 接受 零参：
    可变 机 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AG_字段数：
        做 追加 用 机、0。
        i 为 i 加 1。
    完。
    做 设置 用 机、AG_状态、智能体_空闲。
    做 设置 用 机、AG_任务队列、一组：（空）。
    做 设置 用 机、AG_当前任务、空。
    做 设置 用 机、AG_步数、0。
    做 设置 用 机、AG_最大步数、20。
    做 设置 用 机、AG_思考链、做 思考链_创建。
    做 设置 用 机、AG_记忆、做 记忆_创建。
    做 设置 用 机、AG_历史、一组：（空）。
    做 设置 用 机、AG_错误计数、0。
    做 设置 用 机、AG_总任务数、0。
    做 设置 用 机、AG_成功任务、0。
    返还 机。
完。

智能体状态机_转移 接受 机、新状态：
    旧 为 机 的 第 AG_状态 项。
    做 设置 用 机、AG_状态、新状态。
    做 设置 用 机、AG_步数、机 的 第 AG_步数 项 加 1。
    做 追加 用 机 的 第 AG_历史 项、一组：新状态、做 获取滴答、做 智能体状态名 用 新状态。
    若 做 计数 用 机 的 第 AG_历史 项 大于 200：
        做 设置 用 机、AG_历史、做 列表_切片 用 机 的 第 AG_历史 项、1、做 计数 用 机 的 第 AG_历史 项。
    完。
完。

智能体状态名 接受 状态：
    若 状态 等于 智能体_空闲：
        返还 「空闲」。
    完。
    若 状态 等于 智能体_感知：
        返还 「感知」。
    完。
    若 状态 等于 智能体_思考：
        返还 「思考」。
    完。
    若 状态 等于 智能体_决策：
        返还 「决策」。
    完。
    若 状态 等于 智能体_执行：
        返还 「执行」。
    完。
    若 状态 等于 智能体_观察：
        返还 「观察」。
    完。
    若 状态 等于 智能体_完成：
        返还 「完成」。
    完。
    若 状态 等于 智能体_错误：
        返还 「错误」。
    完。
    返还 「未知」。
完。

智能体状态机_提交任务 接受 机、任务：
    做 追加 用 机 的 第 AG_任务队列 项、任务。
    做 设置 用 机、AG_总任务数、机 的 第 AG_总任务数 项 加 1。
完。

智能体状态机_下一个任务 接受 机：
    队列 为 机 的 第 AG_任务队列 项。
    若 做 计数 用 队列 等于 0：
        返还 空。
    完。
    任务 为 队列 的 第 0 项。
    做 设置 用 机、AG_任务队列、做 列表_切片 用 队列、1、做 计数 用 队列。
    做 设置 用 机、AG_当前任务、任务。
    做 设置 用 机、AG_步数、0。
    做 设置 用 机、AG_思考链、做 思考链_创建。
    做 智能体状态机_转移 用 机、智能体_感知。
    返还 任务。
完。

智能体状态机_tick 接受 机：
    状态 为 机 的 第 AG_状态 项。
    若 状态 等于 智能体_空闲：
        若 做 计数 用 机 的 第 AG_任务队列 项 大于 0：
            做 智能体状态机_下一个任务 用 机。
        完。
        返还。
    完。
    若 机 的 第 AG_步数 项 大于 机 的 第 AG_最大步数 项：
        做 智能体状态机_转移 用 机、智能体_错误。
        做 设置 用 机、AG_错误计数、机 的 第 AG_错误计数 项 加 1。
        返还。
    完。
    若 状态 等于 智能体_感知：
        做 思考链_添加步骤 用 机 的 第 AG_思考链 项、「感知环境」、「已获取当前界面状态」。
        做 智能体状态机_转移 用 机、智能体_思考。
        返还。
    完。
    若 状态 等于 智能体_思考：
        做 思考链_添加步骤 用 机 的 第 AG_思考链 项、「分析任务」、「已分析任务需求」。
        做 智能体状态机_转移 用 机、智能体_决策。
        返还。
    完。
    若 状态 等于 智能体_决策：
        做 思考链_添加步骤 用 机 的 第 AG_思考链 项、「选择操作」、「已决定下一步」。
        做 智能体状态机_转移 用 机、智能体_执行。
        返还。
    完。
    若 状态 等于 智能体_执行：
        做 智能体状态机_转移 用 机、智能体_观察。
        返还。
    完。
    若 状态 等于 智能体_观察：
        任务完成 为 假。
        若 任务完成：
            做 思考链_设置结论 用 机 的 第 AG_思考链 项、「任务完成」。
            做 智能体状态机_转移 用 机、智能体_完成。
            做 设置 用 机、AG_成功任务、机 的 第 AG_成功任务 项 加 1。
        否则：
            做 智能体状态机_转移 用 机、智能体_感知。
        完。
        返还。
    完。
    若 状态 等于 智能体_完成 或者 状态 等于 智能体_错误：
        做 设置 用 机、AG_当前任务、空。
        做 智能体状态机_转移 用 机、智能体_空闲。
    完。
完。

智能体状态机_是空闲 接受 机：
    返还 机 的 第 AG_状态 项 等于 智能体_空闲。
完。

智能体状态机_状态 接受 机：
    返还 做 智能体状态名 用 机 的 第 AG_状态 项。
完。

智能体状态机_步数 接受 机：
    返还 机 的 第 AG_步数 项。
完。

智能体状态机_队列长度 接受 机：
    返还 做 计数 用 机 的 第 AG_任务队列 项。
完。

智能体状态机_统计 接受 机：
    返还 一组：机 的 第 AG_总任务数 项、机 的 第 AG_成功任务 项、机 的 第 AG_错误计数 项、机 的 第 AG_步数 项。
完。

本章 收束。
