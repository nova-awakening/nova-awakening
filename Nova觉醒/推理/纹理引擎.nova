《纹理引擎》

引用《..·中枢·类型定义》。

引用《..·感知·图片解码器》。

引用《..·记忆·接口》。

纹理 是 一种 事物，含 数据、宽、高、通道数、名称。

UV坐标 是 一种 事物，含 u、v。

可变 纹理表 为 一组：（空）。

可变 纹理总数 为 0。

可变 纹理_采样次数 为 0。

可变 纹理_加载次数 为 0。

纹理_从文件加载 接受 文件路径、名称：
    纹理_加载次数 为 纹理_加载次数 加 1。
    文件数据 为 做 记忆_读文件 用 文件路径。
    若 文件数据 等于 0：
        做 打印 用 「[纹理] 加载失败: 」 加 文件路径。
        返还 空。
    完。
    头字节 为 做 内存_读8 用 文件数据。
    tex 为 做 堆_分配 用 纹理。
    tex 的 名称 为 名称。
    若 头字节 等于 0x89：
        tex 为 做 纹理_解码PNG 用 文件数据、tex。
    否则若 头字节 等于 0x42：
        tex 为 做 纹理_解码BMP 用 文件数据、tex。
    否则若 头字节 等于 0xFF：
        tex 为 做 纹理_解码JPEG 用 文件数据、tex。
    完。
    若 tex 的 数据 不等于 0：
        做 追加 用 纹理表、tex。
        纹理总数 为 纹理总数 加 1。
        做 打印 用 「[纹理] 加载成功: 」 加 名称 加 「 」 加 tex 的 宽 加 「x」 加 tex 的 高。
    完。
    返还 tex。
完。

纹理_解码BMP 接受 数据、tex：
    数据起始 为 做 内存_读32 用 数据 加 10。
    tex 的 宽 为 做 内存_读32 用 数据 加 18。
    tex 的 高 为 做 内存_读32 用 数据 加 22。
    tex 的 通道数 为 3。
    tex 的 数据 为 数据 加 数据起始。
    返还 tex。
完。

纹理_解码PNG 接受 数据、tex：
    tex 的 宽 为 0。
    tex 的 高 为 0。
    tex 的 数据 为 0。
    返还 tex。
完。

纹理_解码JPEG 接受 数据、tex：
    tex 的 宽 为 0。
    tex 的 高 为 0。
    tex 的 数据 为 0。
    返还 tex。
完。

纹理_采样 接受 tex、u、v：
    若 tex 等于 空 或者 tex 的 数据 等于 0：
        返还 0xFFCCCCCC。
    完。
    纹理_采样次数 为 纹理_采样次数 加 1。
    px 为 u 乘 tex 的 宽 除以 1000。
    py 为 v 乘 tex 的 高 除以 1000。
    若 px 小于 0：
        px 为 0。
    完。
    若 py 小于 0：
        py 为 0。
    完。
    若 px 大于或等于 tex 的 宽：
        px 为 tex 的 宽 减 1。
    完。
    若 py 大于或等于 tex 的 高：
        py 为 tex 的 高 减 1。
    完。
    翻转y 为 tex 的 高 减 1 减 py。
    偏移 为 (翻转y 乘 tex 的 宽 加 px) 乘 tex 的 通道数。
    b 为 做 内存_读8 用 tex 的 数据 加 偏移。
    g 为 做 内存_读8 用 tex 的 数据 加 偏移 加 1。
    r 为 做 内存_读8 用 tex 的 数据 加 偏移 加 2。
    a 为 255。
    若 tex 的 通道数 等于 4：
        a 为 做 内存_读8 用 tex 的 数据 加 偏移 加 3。
    完。
    返还 a 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
完。

纹理_双线性采样 接受 tex、u、v：
    若 tex 等于 空 或者 tex 的 数据 等于 0：
        返还 0xFFCCCCCC。
    完。
    fx 为 u 乘 tex 的 宽 除以 1000。
    fy 为 v 乘 tex 的 高 除以 1000。
    x0 为 fx。
    y0 为 fy。
    x1 为 x0 加 1。
    y1 为 y0 加 1。
    若 x1 大于或等于 tex 的 宽：
        x1 为 tex 的 宽 减 1。
    完。
    若 y1 大于或等于 tex 的 高：
        y1 为 tex 的 高 减 1。
    完。
    c00 为 做 纹理_采样 用 tex、x0 乘 1000 除以 tex 的 宽、y0 乘 1000 除以 tex 的 高。
    c10 为 做 纹理_采样 用 tex、x1 乘 1000 除以 tex 的 宽、y0 乘 1000 除以 tex 的 高。
    c01 为 做 纹理_采样 用 tex、x0 乘 1000 除以 tex 的 宽、y1 乘 1000 除以 tex 的 高。
    c11 为 做 纹理_采样 用 tex、x1 乘 1000 除以 tex 的 宽、y1 乘 1000 除以 tex 的 高。
    r 为 (c00 右移 16 位与 0xFF 加 (c10 右移 16) 位与 0xFF 加 (c01 右移 16) 位与 0xFF 加 (c11 右移 16) 位与 0xFF) 除以 4。
    g 为 (c00 右移 8 位与 0xFF 加 (c10 右移 8) 位与 0xFF 加 (c01 右移 8) 位与 0xFF 加 (c11 右移 8) 位与 0xFF) 除以 4。
    b 为 (c00 位与 0xFF 加 c10 位与 0xFF 加 c01 位与 0xFF 加 c11 位与 0xFF) 除以 4。
    返还 255 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
完。

UV映射段 是 一种 事物，含 部位名、纹理索引、u起始、v起始、u结束、v结束。

可变 人体UV映射 为 一组：（空）。

UV映射_初始化 接受 零参：
    人体UV映射 为 一组：（空）。
    做 追加 用 人体UV映射、新建 UV映射段：部位名 为 「头部」、纹理索引 为 0、u起始 为 300、v起始 为 0、u结束 为 700、v结束 为 200。
    做 追加 用 人体UV映射、新建 UV映射段：部位名 为 「躯干」、纹理索引 为 0、u起始 为 200、v起始 为 200、u结束 为 800、v结束 为 600。
    做 追加 用 人体UV映射、新建 UV映射段：部位名 为 「左臂」、纹理索引 为 0、u起始 为 0、v起始 为 200、u结束 为 200、v结束 为 500。
    做 追加 用 人体UV映射、新建 UV映射段：部位名 为 「右臂」、纹理索引 为 0、u起始 为 800、v起始 为 200、u结束 为 1000、v结束 为 500。
    做 追加 用 人体UV映射、新建 UV映射段：部位名 为 「左腿」、纹理索引 为 0、u起始 为 200、v起始 为 600、u结束 为 500、v结束 为 1000。
    做 追加 用 人体UV映射、新建 UV映射段：部位名 为 「右腿」、纹理索引 为 0、u起始 为 500、v起始 为 600、u结束 为 800、v结束 为 1000。
完。

UV映射_获取颜色 接受 部位名、局部u、局部v：
    可变 i 为 0。
    只要 i 小于 做 计数 用 人体UV映射：
        若 人体UV映射 的 第 i 项 的 部位名 等于 部位名：
            段 为 人体UV映射 的 第 i 项。
            全局u 为 段 的 u起始 加 局部u 乘 (段 的 u结束 减 段 的 u起始) 除以 1000。
            全局v 为 段 的 v起始 加 局部v 乘 (段 的 v结束 减 段 的 v起始) 除以 1000。
            若 段 的 纹理索引 小于 纹理总数：
                返还 做 纹理_双线性采样 用 纹理表 的 第 段 的 纹理索引 项、全局u、全局v。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 0xFFCCCCCC。
完。

纹理数字人_加载皮肤 接受 图片路径：
    tex 为 做 纹理_从文件加载 用 图片路径、「角色皮肤」。
    若 tex 不等于 空：
        做 UV映射_初始化。
        做 打印 用 「[纹理数字人] 皮肤纹理已加载: 」 加 图片路径。
    完。
    返还 tex。
完。

可变 纹理_正面 为 0。

可变 纹理_坐姿 为 0。

可变 纹理_侧面 为 0。

可变 纹理_特写 为 0。

纹理数字人_加载全部 接受 零参：
    纹理_正面 为 做 纹理_从文件加载 用 「资源/数字人/正面.png」、「正面」。
    纹理_坐姿 为 做 纹理_从文件加载 用 「资源/数字人/坐姿.png」、「坐姿」。
    纹理_侧面 为 做 纹理_从文件加载 用 「资源/数字人/侧面.png」、「侧面」。
    纹理_特写 为 做 纹理_从文件加载 用 「资源/数字人/特写.png」、「特写」。
    做 UV映射_初始化。
    做 打印 用 「[纹理数字人] 4张多角度纹理已加载」。
完。

纹理数字人_选纹理 接受 朝向角度：
    若 朝向角度 大于或等于 315 或者 朝向角度 小于 45：
        返还 纹理_正面。
    完。
    若 朝向角度 大于或等于 45 并且 朝向角度 小于 135：
        返还 纹理_侧面。
    完。
    若 朝向角度 大于或等于 135 并且 朝向角度 小于 225：
        返还 纹理_正面。
    完。
    返还 纹理_侧面。
完。

纹理数字人_获取特写 接受 零参：
    返还 纹理_特写。
完。

纹理数字人_获取坐姿 接受 零参：
    返还 纹理_坐姿。
完。

纹理数字人_渲染帧 接受 帧缓冲、宽、高：
    若 纹理总数 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 人体顶点：
        v 为 人体顶点 的 第 i 项。
        z 为 v 的 z 加 2000。
        若 z 小于或等于 0：
            z 为 1。
        完。
        屏幕x 为 宽 除以 2 加 v 的 x 乘 500 除以 z。
        屏幕y 为 高 除以 2 减 v 的 y 乘 500 除以 z。
        若 屏幕x 大于或等于 0 并且 屏幕x 小于 宽 并且 屏幕y 大于或等于 0 并且 屏幕y 小于 高：
            可变 部位 为 「躯干」。
            若 v 的 y 大于 1500：
                部位 为 「头部」。
            否则若 v 的 y 大于 900：
                部位 为 「躯干」。
            否则若 v 的 y 大于 400：
                部位 为 「左腿」。
            否则：
                部位 为 「右腿」。
            完。
            局部u 为 (v 的 x 加 200) 乘 1000 除以 400。
            局部v 为 (1800 减 v 的 y) 乘 1000 除以 1800。
            颜色 为 做 UV映射_获取颜色 用 部位、局部u、局部v。
            做 内存_写32 用 帧缓冲 加 (屏幕y 乘 宽 加 屏幕x) 乘 4、颜色。
        完。
        i 为 i 加 1。
    完。
完。

本章 收束。
