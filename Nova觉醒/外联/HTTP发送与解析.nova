《HTTP发送与解析》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·URL解析辅助》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《.·TCP校验和》。

引用《..·执行·网络创建》。

引用《..·中枢·整数转字符串》。

HTTP_发送 接受 req：
    HTTP_请求总数 为 HTTP_请求总数 加 1。
    方法名 为 做 HTTP_方法名 用 req 的 方法。
    可变 原始请求 为 方法名 加 「 」 加 req 的 路径 加 「 」 加 HTTP_版本 加 「\r\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 req 的 头部列表：
        原始请求 为 原始请求 加 req 的 头部列表 的 第 i 项 的 名称 加 「: 」 加 req 的 头部列表 的 第 i 项 的 值 加 「\r\n」。
        i 为 i 加 1。
    完。
    原始请求 为 原始请求 加 「\r\n」。
    若 req 的 正文 不等于 「」：
        原始请求 为 原始请求 加 req 的 正文。
    完。
    连接 为 做 传控_连接 用 req 的 主机、req 的 端口、req 的 超时_ms。
    若 连接 等于 空：
        HTTP_失败总数 为 HTTP_失败总数 加 1。
        失败响应 为 新建 HTTP响应。
        失败响应 的 状态码 为 0。
        失败响应 的 成功 为 假。
        失败响应 的 状态文本 为 「连接失败」。
        返还 失败响应。
    完。
    做 传控_发送数据 用 连接、原始请求。
    原始响应 为 做 传控_接收数据 用 连接、req 的 超时_ms。
    做 传控_关闭 用 连接。
    响应 为 做 HTTP_解析响应 用 原始响应。
    若 响应 的 状态码 大于或等于 200 并且 响应 的 状态码 小于 300：
        HTTP_成功总数 为 HTTP_成功总数 加 1。
        响应 的 成功 为 真。
    否则：
        HTTP_失败总数 为 HTTP_失败总数 加 1。
        响应 的 成功 为 假。
    完。
    返还 响应。
完。

HTTP_发送_带重试 接受 req：
    可变 尝试 为 0。
    只要 尝试 小于 HTTP_最大重试：
        响应 为 做 HTTP_发送 用 req。
        若 响应 的 成功：
            返还 响应。
        完。
        若 响应 的 状态码 大于或等于 400 并且 响应 的 状态码 小于 500：
            返还 响应。
        完。
        尝试 为 尝试 加 1。
        做 内核调试 用 「HTTP: 重试 」 加 做 整数转字符串 用 尝试 加 「/」 加 做 整数转字符串 用 HTTP_最大重试。
    完。
    失败 为 新建 HTTP响应。
    失败 的 状态码 为 0。
    失败 的 成功 为 假。
    失败 的 状态文本 为 「重试耗尽」。
    返还 失败。
完。

HTTP_解析响应 接受 原始数据：
    响应 为 新建 HTTP响应。
    若 原始数据 等于 「」 或者 原始数据 等于 空：
        响应 的 状态码 为 0。
        响应 的 成功 为 假。
        返还 响应。
    完。
    状态行 为 做 提取行 用 原始数据、0。
    响应 的 状态码 为 做 解析状态码 用 状态行。
    响应 的 状态文本 为 状态行。
    响应 的 头部列表 为 一组：（空）。
    可变 行号 为 1。
    可变 正文开始 为 0。
    可变 解析头部中 为 真。
    只要 解析头部中：
        行 为 做 提取行 用 原始数据、行号。
        若 行 等于 「」 或者 行 等于 「\r」：
            解析头部中 为 假。
            正文开始 为 行号 加 1。
        否则：
            h 为 新建 HTTP头部。
            冒号位置 为 做 查找字符 用 行、58。
            若 冒号位置 大于 0：
                h 的 名称 为 做 子字符串 用 行、0、冒号位置。
                h 的 值 为 做 子字符串 用 行、冒号位置 加 2、做 字符串长度 用 行。
            完。
            做 追加 用 响应 的 头部列表、h。
        完。
        行号 为 行号 加 1。
        若 行号 大于 100：
            解析头部中 为 假。
        完。
    完。
    响应 的 正文 为 做 提取正文 用 原始数据、正文开始。
    响应 的 正文长度 为 做 字符串长度 用 响应 的 正文。
    返还 响应。
完。

HTTP_方法名 接受 方法：
    若 方法 等于 HTTP_GET：
        返还 「GET」。
    完。
    若 方法 等于 HTTP_POST：
        返还 「POST」。
    完。
    若 方法 等于 HTTP_PUT：
        返还 「PUT」。
    完。
    若 方法 等于 HTTP_DELETE：
        返还 「DELETE」。
    完。
    若 方法 等于 HTTP_PATCH：
        返还 「PATCH」。
    完。
    返还 「GET」。
完。

HTTP_获取响应头 接受 响应、名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 响应 的 头部列表：
        若 响应 的 头部列表 的 第 i 项 的 名称 等于 名称：
            返还 响应 的 头部列表 的 第 i 项 的 值。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

本章 收束。
