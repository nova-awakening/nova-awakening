《HTTP客户端》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·套接字层》。

引用《..·中枢·哈希整数》。

引用《..·中枢·字符串工具》。

引用《..·感知·内核直连层》。

引用《..·中枢·内容可见性》。

方法_获取 为 0。

方法_提交 为 1。

方法_放置 为 2。

方法_删除 为 3。

方法_头部 为 4。

最大头部 为 32。

最大连接池 为 16。

默认超时 为 30000000。

最大重试 为 3。

超传请求 是 一种 事物，含 方法、地址哈希、端口、路径、路径长度、头部表、头部数、正文、正文长度、超时。

超传响应 是 一种 事物，含 状态码、头部表、头部数、正文、正文长度、接收时刻、耗时。

连接项 是 一种 事物，含 地址哈希、端口、套接描述、最近使用、活跃。

可变 连接池 为 一组：（空）。

可变 连接池数 为 0。

可变 请求总数 为 0。

可变 成功总数 为 0。

可变 失败总数 为 0。

可变 重试总数 为 0。

可变 总发送字节 为 0。

可变 总接收字节 为 0。

超传_初始化 接受 零参：
    连接池 为 一组：（空）。
    连接池数 为 0。
完。

超传_请求 接受 方法、地址、端口、路径、正文、正文长：
    请求总数 为 请求总数 加 1。
    请求 为 新建 超传请求。
    请求 的 方法 为 方法。
    请求 的 地址哈希 为 做 哈希_字符串 用 地址。
    请求 的 端口 为 端口。
    请求 的 路径 为 路径。
    请求 的 路径长度 为 做 字串_长度 用 路径。
    请求 的 头部表 为 做 堆_分配 用 最大头部 乘 16。
    请求 的 头部数 为 0。
    请求 的 正文 为 正文。
    请求 的 正文长度 为 正文长。
    请求 的 超时 为 默认超时。
    可变 重试 为 0。
    可变 响应 为 0。
    只要 重试 小于 最大重试：
        响应 为 做 超传_执行请求 用 请求。
        若 响应 不等于 0 并且 响应 的 状态码 大于 0：
            成功总数 为 成功总数 加 1。
            返还 响应。
        完。
        重试 为 重试 加 1。
        重试总数 为 重试总数 加 1。
    完。
    失败总数 为 失败总数 加 1。
    返还 0。
完。

超传_执行请求 接受 请求：
    套接 为 做 超传_获取连接 用 请求 的 地址哈希、请求 的 端口。
    若 套接 小于 0：
        返还 0。
    完。
    起始 为 做 平台_获取高精度时间。
    发送缓冲 为 做 堆_分配 用 4096。
    可变 写位 为 0。
    若 请求 的 方法 等于 方法_获取：
        写位 为 写位 加 做 格式化写 用 发送缓冲、「GET 」。
    否则若 请求 的 方法 等于 方法_提交：
        写位 为 写位 加 做 格式化写 用 发送缓冲、「POST 」。
    否则若 请求 的 方法 等于 方法_放置：
        写位 为 写位 加 做 格式化写 用 发送缓冲、「PUT 」。
    否则若 请求 的 方法 等于 方法_删除：
        写位 为 写位 加 做 格式化写 用 发送缓冲、「DELETE 」。
    完。
    做 内存_复制 用 发送缓冲 加 写位、请求 的 路径、请求 的 路径长度。
    写位 为 写位 加 请求 的 路径长度。
    写位 为 写位 加 做 格式化写 用 发送缓冲 加 写位、「 HTTP/1.1\r\n」。
    若 请求 的 正文长度 大于 0：
        写位 为 写位 加 做 格式化写 用 发送缓冲 加 写位、「Content-Length: 」。
        写位 为 写位 加 做 整数写 用 发送缓冲 加 写位、请求 的 正文长度。
        写位 为 写位 加 做 格式化写 用 发送缓冲 加 写位、「\r\n」。
    完。
    写位 为 写位 加 做 格式化写 用 发送缓冲 加 写位、「\r\n」。
    做 套接_发送 用 套接、发送缓冲、写位。
    总发送字节 为 总发送字节 加 写位。
    若 请求 的 正文长度 大于 0：
        做 套接_发送 用 套接、请求 的 正文、请求 的 正文长度。
        总发送字节 为 总发送字节 加 请求 的 正文长度。
    完。
    接收缓冲 为 做 堆_分配 用 65536。
    接收长 为 做 套接_接收 用 套接、接收缓冲、65536。
    总接收字节 为 总接收字节 加 接收长。
    响应 为 做 超传_解析响应 用 接收缓冲、接收长。
    若 响应 不等于 0：
        响应 的 耗时 为 做 平台_获取高精度时间 减 起始。
        响应 的 接收时刻 为 做 平台_获取高精度时间。
    完。
    做 堆_释放 用 发送缓冲。
    返还 响应。
完。

超传_解析响应 接受 数据、长度：
    若 长度 小于 12：
        返还 0。
    完。
    响应 为 新建 超传响应。
    可变 状态码 为 0。
    可变 位 为 9。
    只要 位 小于 长度 并且 位 小于 12：
        字节 为 做 内存_读8 用 数据 加 位。
        若 字节 大于或等于 48 并且 字节 小于或等于 57：
            状态码 为 状态码 乘 10 加 (字节 减 48)。
        完。
        位 为 位 加 1。
    完。
    响应 的 状态码 为 状态码。
    正文起始 为 做 超传_查找正文 用 数据、长度。
    若 正文起始 大于 0：
        响应 的 正文 为 数据 加 正文起始。
        响应 的 正文长度 为 长度 减 正文起始。
    否则：
        响应 的 正文 为 0。
        响应 的 正文长度 为 0。
    完。
    响应 的 头部表 为 0。
    响应 的 头部数 为 0。
    返还 响应。
完。

超传_查找正文 接受 数据、长度：
    可变 位 为 0。
    只要 位 加 3 小于 长度：
        若 做 内存_读8 用 数据 加 位 等于 13 并且 做 内存_读8 用 数据 加 位 加 1 等于 10 并且 做 内存_读8 用 数据 加 位 加 2 等于 13 并且 做 内存_读8 用 数据 加 位 加 3 等于 10：
            返还 位 加 4。
        完。
        位 为 位 加 1。
    完。
    返还 负 1。
完。

超传_获取连接 接受 地址哈希、端口：
    可变 序号 为 0。
    只要 序号 小于 连接池数：
        若 连接池 的 第 序号 项 的 地址哈希 等于 地址哈希 并且 连接池 的 第 序号 项 的 端口 等于 端口 并且 连接池 的 第 序号 项 的 活跃：
            连接池 的 第 序号 项 的 最近使用 为 做 平台_获取高精度时间。
            返还 连接池 的 第 序号 项 的 套接描述。
        完。
        序号 为 序号 加 1。
    完。
    新套接 为 做 套接_创建 用 1、0。
    若 新套接 小于 0：
        返还 负 1。
    完。
    做 套接_连接 用 新套接、地址哈希、端口。
    若 连接池数 小于 最大连接池：
        连接 为 新建 连接项。
        连接 的 地址哈希 为 地址哈希。
        连接 的 端口 为 端口。
        连接 的 套接描述 为 新套接。
        连接 的 最近使用 为 做 平台_获取高精度时间。
        连接 的 活跃 为 真。
        做 追加 用 连接池、连接。
        连接池数 为 连接池数 加 1。
    完。
    返还 新套接。
完。

超传_获取 接受 地址、端口、路径：
    返还 做 超传_请求 用 方法_获取、地址、端口、路径、0、0。
完。

超传_提交 接受 地址、端口、路径、正文、正文长：
    返还 做 超传_请求 用 方法_提交、地址、端口、路径、正文、正文长。
完。

超传客户端_完整统计 接受 零参：
    返还 一组：请求总数、成功总数、失败总数、重试总数、总发送字节、总接收字节、连接池数。
完。

可变 AI_超传客户端_热点函数 为 一组：（空）。

AI_超传客户端_JIT候选 接受 函数名、调用次数：
    若 调用次数 大于 100：
        做 追加 用 AI_超传客户端_热点函数、函数名。
        返还 真。
    完。
    返还 假。
完。

AI_超传客户端_内联建议 接受 函数体大小、调用次数：
    若 函数体大小 小于 30 并且 调用次数 大于 50：
        返还 「内联」。
    完。
    返还 「不内联」。
完。

本章 收束。
