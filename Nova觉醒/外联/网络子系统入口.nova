《网络子系统入口》

引用《.·TCP会话记录》。

引用《.·路由查找》。

引用《.·网络分片》。

引用《.·IP接收》。

引用《.·ICMP控制报文》。

引用《.·IP前缀掩码转换》。

引用《.·ping应答接收》。

引用《.·网络路由初始化》。

引用《..·推理·请求路由》。

引用《.·ICMP处理》。

引用《..·中枢·整数转字符串》。

引用《.·IP发送》。

引用《.·IP统一收发》。

引用《.·路由表初始化》。

引用《.·ICMP接收》。

引用《.·IP分片发送》。

网络档 是 一种 事物，含 名称、本机IP、最近目标、最近探测、最近路由、最近分片、最近接收、最近发送、最近错误、初始化完成、探测数、路由数、发送数、接收数、分片数、命中数、失败数、健康。

网络摘要 是 一种 事物，含 名称、探测数、路由数、发送数、接收数、分片数、命中数、失败数、健康。

网络任务 是 一种 事物，含 目标IP、源IP、协议、TTL、端口、数据、数据长。

可变 网络账本 为 一组：（空）。

可变 网络建数 为 0。

可变 网络总动作 为 0。

可变 网络总命中 为 0。

可变 网络总失败 为 0。

网络网络_建档 接受 名称、本机IP：
    档 为 做 堆_分配 用 网络档。
    档 的 名称 为 名称。
    档 的 本机IP 为 本机IP。
    档 的 最近目标 为 0。
    档 的 最近探测 为 0。
    档 的 最近路由 为 0。
    档 的 最近分片 为 0。
    档 的 最近接收 为 0。
    档 的 最近发送 为 0。
    档 的 最近错误 为 「」。
    档 的 初始化完成 为 假。
    档 的 探测数 为 0。
    档 的 路由数 为 0。
    档 的 发送数 为 0。
    档 的 接收数 为 0。
    档 的 分片数 为 0。
    档 的 命中数 为 0。
    档 的 失败数 为 0。
    档 的 健康 为 「未初始化」。
    返还 档。
完。

网络网络_登记 接受 档：
    做 追加 用 网络账本、档。
    网络建数 为 网络建数 加 1。
    返还 档。
完。

网络网络_创建 接受 名称、本机IP：
    档 为 做 网络网络_建档 用 名称、本机IP。
    做 网络网络_初始化 用 档。
    返还 做 网络网络_登记 用 档。
完。

网络网络_初始化 接受 档：
    做 探测统计_初始化。
    做 网络_初始化。
    做 路由表_初始化。
    做 路由_初始化。
    做 IP_设置本机地址 用 档 的 本机IP。
    做 IP_添加本机地址 用 档 的 本机IP。
    档 的 初始化完成 为 真。
    档 的 健康 为 「运行」。
    返还 档。
完。

网络网络_建任务 接受 源IP、目标IP、协议、TTL、端口、数据、数据长：
    任务 为 做 堆_分配 用 网络任务。
    任务 的 目标IP 为 目标IP。
    任务 的 源IP 为 源IP。
    任务 的 协议 为 协议。
    任务 的 TTL 为 TTL。
    任务 的 端口 为 端口。
    任务 的 数据 为 数据。
    任务 的 数据长 为 数据长。
    返还 任务。
完。

网络网络_设置本机 接受 档、IP：
    档 的 本机IP 为 IP。
    做 IP_设置本机地址 用 IP。
    做 IP_添加本机地址 用 IP。
    返还 IP。
完。

网络网络_获取本机 接受 档：
    IP 为 做 IP_获取本机地址。
    档 的 本机IP 为 IP。
    返还 IP。
完。

网络网络_本机判定 接受 档、IP：
    结果 为 做 IP_是本机地址 用 IP。
    若 结果：
        档 的 命中数 为 档 的 命中数 加 1。
        网络总命中 为 网络总命中 加 1。
    完。
    返还 结果。
完。

网络网络_前缀转掩码 接受 前缀长度：
    返还 做 前缀长度_转掩码 用 前缀长度。
完。

网络网络_掩码转前缀 接受 掩码：
    返还 做 掩码_转前缀长度 用 掩码。
完。

网络网络_路由表初始化 接受 档：
    做 路由表_初始化。
    做 路由_初始化。
    档 的 路由数 为 档 的 路由数 加 1。
完。

网络网络_添加路由 接受 档、目标、前缀长度、网关、设备、度量：
    掩码 为 做 前缀长度_转掩码 用 前缀长度。
    做 路由_添加_快速 用 目标、掩码、网关、设备、度量、RTF_UP。
    做 网络_添加路由 用 目标、掩码、网关、设备。
    档 的 路由数 为 档 的 路由数 加 1。
    档 的 最近路由 为 一组：目标、前缀长度、网关、设备。
完。

网络网络_添加默认路由 接受 档、网关、设备：
    做 路由_添加 用 0、0、网关、设备、0、RTF_UP 位或 RTF_DEFAULT。
    档 的 路由数 为 档 的 路由数 加 1。
    档 的 最近路由 为 一组：0、0、网关、设备。
完。

网络网络_查找路由 接受 档、目标IP：
    结果 为 做 路由_查找_快速 用 目标IP。
    若 结果 等于 空 或者 结果 等于 0：
        结果 为 做 路由_查找 用 目标IP。
    完。
    若 结果 不等于 空 并且 结果 不等于 0：
        档 的 命中数 为 档 的 命中数 加 1。
        网络总命中 为 网络总命中 加 1。
    完。
    档 的 路由数 为 档 的 路由数 加 1。
    档 的 最近目标 为 目标IP。
    档 的 最近路由 为 结果。
    返还 结果。
完。

网络网络_缓存命中率 接受 档：
    比率 为 做 路由_缓存命中率。
    档 的 最近路由 为 比率。
    返还 比率。
完。

网络网络_路由统计 接受 档：
    统计 为 做 路由_完整统计。
    档 的 最近路由 为 统计。
    返还 统计。
完。

网络网络_发送统一 接受 档、Nova缓冲、源IP、目标IP、协议、TTL：
    结果 为 做 IP_发送_统一 用 Nova缓冲、源IP、目标IP、协议、TTL。
    档 的 发送数 为 档 的 发送数 加 1。
    档 的 最近发送 为 结果。
    网络总动作 为 网络总动作 加 1。
    返还 结果。
完。

网络网络_发送基础 接受 档、Nova缓冲、源IP、目标IP、协议、TTL：
    结果 为 做 IP_发送 用 Nova缓冲、源IP、目标IP、协议、TTL。
    档 的 发送数 为 档 的 发送数 加 1。
    档 的 最近发送 为 结果。
    网络总动作 为 网络总动作 加 1。
    返还 结果。
完。

网络网络_协议发送 接受 档、Nova缓冲、协议、目标IP、源IP：
    结果 为 做 IP_发送_协议 用 Nova缓冲、协议、目标IP、源IP。
    档 的 发送数 为 档 的 发送数 加 1。
    档 的 最近发送 为 结果。
    返还 结果。
完。

网络网络_输入输出发送 接受 档、源地址、目标地址、协议、数据、数据长：
    结果 为 做 网络_发送包 用 源地址、目标地址、协议、数据、数据长。
    档 的 发送数 为 档 的 发送数 加 1。
    档 的 最近发送 为 结果。
    返还 结果。
完。

网络网络_接收统一 接受 档、Nova缓冲：
    结果 为 做 IP_接收_统一 用 Nova缓冲。
    档 的 接收数 为 档 的 接收数 加 1。
    档 的 最近接收 为 结果。
    网络总动作 为 网络总动作 加 1。
    返还 结果。
完。

网络网络_接收基础 接受 档、Nova缓冲：
    结果 为 做 IP_接收 用 Nova缓冲。
    档 的 接收数 为 档 的 接收数 加 1。
    档 的 最近接收 为 结果。
    网络总动作 为 网络总动作 加 1。
    返还 结果。
完。

网络网络_输入输出接收 接受 档、缓冲、长度：
    结果 为 做 网络_接收包 用 缓冲、长度。
    档 的 接收数 为 档 的 接收数 加 1。
    档 的 最近接收 为 结果。
    返还 结果。
完。

网络网络_转发 接受 档、Nova缓冲：
    结果 为 做 IP_输入输出_转发 用 Nova缓冲。
    档 的 接收数 为 档 的 接收数 加 1。
    档 的 最近接收 为 结果。
    返还 结果。
完。

网络网络_分片 接受 档、数据、总长度、MTU：
    结果 为 做 IP_分片 用 数据、总长度、MTU。
    档 的 分片数 为 档 的 分片数 加 1。
    档 的 最近分片 为 结果。
    返还 结果。
完。

网络网络_重组 接受 档、源IP、目标IP、ID、协议、偏移、MF、数据、长度：
    结果 为 做 IP_重组 用 源IP、目标IP、ID、协议、偏移、MF、数据、长度。
    档 的 分片数 为 档 的 分片数 加 1。
    档 的 最近分片 为 结果。
    返还 结果。
完。

网络网络_统一分片发送 接受 档、Nova缓冲、设备、MTU：
    结果 为 做 IP_分片发送_统一 用 Nova缓冲、设备、MTU。
    档 的 分片数 为 档 的 分片数 加 1。
    档 的 最近分片 为 结果。
    返还 结果。
完。

网络网络_清理分片 接受 档：
    做 分片_清理超时。
    档 的 分片数 为 档 的 分片数 加 1。
完。

网络网络_分片统计 接受 档：
    统计 为 做 分片_完整统计。
    档 的 最近分片 为 统计。
    返还 统计。
完。

网络网络_回显请求 接受 档、Nova缓冲：
    结果 为 做 icmp_处理回显请求 用 Nova缓冲。
    档 的 探测数 为 档 的 探测数 加 1。
    档 的 最近探测 为 结果。
    返还 结果。
完。

网络网络_回显接收 接受 档、源IP、标识符、序列号：
    结果 为 做 ping_接收应答 用 源IP、标识符、序列号。
    档 的 探测数 为 档 的 探测数 加 1。
    档 的 最近探测 为 结果。
    返还 结果。
完。

网络网络_探测 接受 档、目标IP、超时_毫秒：
    结果 为 做 ping 用 目标IP、超时_毫秒。
    档 的 探测数 为 档 的 探测数 加 1。
    档 的 最近目标 为 目标IP。
    档 的 最近探测 为 结果。
    网络总动作 为 网络总动作 加 1。
    返还 结果。
完。

网络网络_多次探测 接受 档、目标IP、次数、间隔_毫秒、超时_毫秒：
    结果 为 做 ping_多次 用 目标IP、次数、间隔_毫秒、超时_毫秒。
    档 的 探测数 为 档 的 探测数 加 次数。
    档 的 最近目标 为 目标IP。
    档 的 最近探测 为 结果。
    返还 结果。
完。

网络网络_路追 接受 档、目标IP、最大跳数、超时_毫秒：
    结果 为 做 traceroute 用 目标IP、最大跳数、超时_毫秒。
    档 的 探测数 为 档 的 探测数 加 1。
    档 的 最近目标 为 目标IP。
    档 的 最近探测 为 结果。
    返还 结果。
完。

网络网络_不可达 接受 档、Nova缓冲、代码：
    做 icmp_处理不可达 用 Nova缓冲、代码。
    档 的 失败数 为 档 的 失败数 加 1。
    网络总失败 为 网络总失败 加 1。
    档 的 最近错误 为 「不可达」。
完。

网络网络_超时 接受 档、Nova缓冲、代码：
    做 icmp_处理超时 用 Nova缓冲、代码。
    档 的 失败数 为 档 的 失败数 加 1。
    网络总失败 为 网络总失败 加 1。
    档 的 最近错误 为 「超时」。
完。

网络网络_重定向 接受 档、Nova缓冲、代码：
    做 icmp_处理重定向 用 Nova缓冲、代码。
    档 的 接收数 为 档 的 接收数 加 1。
完。

网络网络_回显发送 接受 档、目标IP、标识符、数据、数据长度：
    结果 为 做 icmp_发送回显 用 目标IP、标识符、数据、数据长度。
    档 的 探测数 为 档 的 探测数 加 1。
    档 的 最近探测 为 结果。
    返还 结果。
完。

网络网络_端口不可达 接受 档、Nova缓冲：
    做 icmp_发送_端口不可达 用 Nova缓冲。
    档 的 失败数 为 档 的 失败数 加 1。
完。

网络网络_主机不可达 接受 档、Nova缓冲：
    做 icmp_发送_主机不可达 用 Nova缓冲。
    档 的 失败数 为 档 的 失败数 加 1。
完。

网络网络_需要分片 接受 档、Nova缓冲、mtu：
    做 icmp_发送_需要分片 用 Nova缓冲、mtu。
    档 的 分片数 为 档 的 分片数 加 1。
完。

网络网络_TTL超时 接受 档、Nova缓冲：
    做 icmp_发送_TTL超时 用 Nova缓冲。
    档 的 失败数 为 档 的 失败数 加 1。
完。

网络网络_接收控制报文 接受 档、Nova缓冲：
    结果 为 做 icmp_接收 用 Nova缓冲。
    档 的 接收数 为 档 的 接收数 加 1。
    档 的 最近接收 为 结果。
    返还 结果。
完。

网络网络_探测统计 接受 档：
    统计 为 做 ICMP_完整统计。
    档 的 最近探测 为 统计。
    返还 统计。
完。

网络网络_输入输出统计 接受 档：
    统计 为 做 网络_完整统计。
    档 的 最近接收 为 统计。
    返还 统计。
完。

网络网络_发送统计 接受 档：
    统计 为 做 IP发送_完整统计。
    档 的 最近发送 为 统计。
    返还 统计。
完。

网络网络_接收统计 接受 档：
    统计 为 做 IP接收_完整统计。
    档 的 最近接收 为 统计。
    返还 统计。
完。

网络网络_丢包率 接受 档：
    比率 为 做 网络_丢包率。
    档 的 最近探测 为 比率。
    返还 比率。
完。

网络网络_状态串 接受 档：
    返还 做 字符串拼接 用 「网络[」、做 字符串拼接 用 档 的 名称、做 字符串拼接 用 「] 发=」、做 字符串拼接 用 做 整数转字符串 用 档 的 发送数、做 字符串拼接 用 「 收=」、做 字符串拼接 用 做 整数转字符串 用 档 的 接收数、做 字符串拼接 用 「 路由=」、做 整数转字符串 用 档 的 路由数。
完。

网络网络_摘要 接受 档：
    返还 新建 网络摘要：名称 为 档 的 名称、探测数 为 档 的 探测数、路由数 为 档 的 路由数、发送数 为 档 的 发送数、接收数 为 档 的 接收数、分片数 为 档 的 分片数、命中数 为 档 的 命中数、失败数 为 档 的 失败数、健康 为 做 网络网络_健康 用 档。
完。

网络网络_健康 接受 档：
    若 非 档 的 初始化完成：
        返还 「待初始化」。
    完。
    若 档 的 失败数 大于 0：
        返还 「退化」。
    完。
    若 做 网络网络_丢包率 用 档 大于 50：
        返还 「拥塞」。
    完。
    返还 「运行」。
完。

网络网络_自检 接受 档：
    做 网络网络_初始化 用 档。
    做 网络网络_设置本机 用 档、档 的 本机IP。
    做 网络网络_添加路由 用 档、0x0A000000、24、0、「eth0」、10。
    做 网络网络_添加默认路由 用 档、0x0A000001、「eth0」。
    做 网络网络_查找路由 用 档、0x0A000002。
    做 网络网络_探测 用 档、0x7F000001、1000。
    做 网络网络_多次探测 用 档、0x7F000001、2、50、500。
    档 的 健康 为 做 网络网络_健康 用 档。
    返还 档 的 健康。
完。

网络网络_按名查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 网络账本：
        若 网络账本 的 第 i 项 的 名称 等于 名称：
            返还 网络账本 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

网络网络_最近探测 接受 档：
    返还 档 的 最近探测。
完。

网络网络_最近路由 接受 档：
    返还 档 的 最近路由。
完。

网络网络_最近发送 接受 档：
    返还 档 的 最近发送。
完。

网络网络_最近接收 接受 档：
    返还 档 的 最近接收。
完。

网络网络_账本数 接受 零参：
    返还 做 计数 用 网络账本。
完。

网络网络_全局统计 接受 零参：
    返还 一组：网络建数、网络总动作、网络总命中、网络总失败。
完。

网络网络_示例流程 接受 零参：
    档 为 做 网络网络_创建 用 「网络网络」、0x0A000002。
    任务 为 做 网络网络_建任务 用 档 的 本机IP、0x0A000001、1、64、0、0、0。
    做 网络网络_添加路由 用 档、0x0A000000、24、0、「eth0」、10。
    做 网络网络_查找路由 用 档、任务 的 目标IP。
    做 网络网络_探测 用 档、任务 的 目标IP、800。
    做 网络网络_路追 用 档、任务 的 目标IP、4、600。
    做 网络网络_发送统计 用 档。
    做 网络网络_接收统计 用 档。
    做 网络网络_分片统计 用 档。
    返还 做 网络网络_摘要 用 档。
完。

可变 AI_网络网络_负载窗口 为 一组：（空）。

AI_网络网络_负载预测 接受 当前负载：
    做 追加 用 AI_网络网络_负载窗口、当前负载。
    若 做 计数 用 AI_网络网络_负载窗口 大于 20：
        做 列表_移除 用 AI_网络网络_负载窗口、0。
    完。
    若 做 计数 用 AI_网络网络_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_网络网络_负载窗口。
    趋势 为 AI_网络网络_负载窗口 的 第 n 减 1 项 减 AI_网络网络_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_网络网络_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
