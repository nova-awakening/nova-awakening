《VLAN虚拟局域网》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·IP接收》。

引用《.·帧填充校验》。

引用《.·ARP查询》。

引用《.·以太网封装》。

引用《..·中枢·字符串替换》。

引用《.·网缓冲分配》。

引用《.·网络缓冲读》。

VLAN_头长度 为 4。

可变 以太网_VLAN帧数 为 0。

以太网_封装_VLAN 接受 Nova缓冲、目标MAC、源MAC、VLAN_ID、优先级、内部类型：
    做 Nova缓冲_前推 用 Nova缓冲、以太网_头长度 加 VLAN_头长度。
    头 为 做 Nova缓冲_数据 用 Nova缓冲。
    可变 i 为 0。
    只要 i 小于 以太网_MAC长度：
        做 缓冲写 用 头、i、做 MAC_字节 用 目标MAC、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 以太网_MAC长度：
        做 缓冲写 用 头、6 加 i、做 MAC_字节 用 源MAC、i。
        i 为 i 加 1。
    完。
    做 缓冲写 用 头、12、0x81。
    做 缓冲写 用 头、13、0x00。
    TCI 为 (优先级 位与 0x07) 左移 13 位或 VLAN_ID 位与 0x0FFF。
    做 缓冲写 用 头、14、TCI 右移 8 位与 0xFF。
    做 缓冲写 用 头、15、TCI 位与 0xFF。
    做 缓冲写 用 头、16、内部类型 右移 8 位与 0xFF。
    做 缓冲写 用 头、17、内部类型 位与 0xFF。
    以太网_VLAN帧数 为 以太网_VLAN帧数 加 1。
    返还 成功。
完。

以太网_解析VLAN 接受 Nova缓冲：
    数据 为 做 Nova缓冲_数据 用 Nova缓冲。
    TCI 为 做 缓冲读 用 数据、0 左移 8 位或 做 缓冲读 用 数据、1。
    VLAN_ID 为 TCI 位与 0x0FFF。
    优先级 为 TCI 右移 13 位与 0x07。
    内部类型 为 做 缓冲读 用 数据、2 左移 8 位或 做 缓冲读 用 数据、3。
    做 Nova缓冲_拉取 用 Nova缓冲、VLAN_头长度。
    以太网_VLAN帧数 为 以太网_VLAN帧数 加 1。
    返还 一组：VLAN_ID、优先级、内部类型。
完。

以太网_接收_完整 接受 Nova缓冲、设备：
    数据 为 做 Nova缓冲_数据 用 Nova缓冲。
    长度 为 做 Nova缓冲_长度 用 Nova缓冲。
    若 长度 小于 以太网_头长度：
        以太网_接收丢弃 为 以太网_接收丢弃 加 1。
        返还 错误_无效参数。
    完。
    目标 为 做 MAC_从缓冲 用 数据、ETH_目标MAC。
    本机MAC 为 做 设备_获取MAC 用 设备。
    可变 包类型 为 0。
    若 做 MAC_是广播 用 目标：
        包类型 为 1。
    否则若 做 MAC_是多播 用 目标：
        包类型 为 2。
    否则若 非 做 MAC_相等 用 目标、本机MAC：
        若 非 做 设备_混杂模式 用 设备：
            以太网_接收丢弃 为 以太网_接收丢弃 加 1。
            返还 错误_目标不匹配。
        完。
        包类型 为 3。
    完。
    类型 为 做 缓冲读 用 数据、ETH_类型 左移 8 位或 做 缓冲读 用 数据、ETH_类型 加 1。
    做 Nova缓冲_拉取 用 Nova缓冲、以太网_头长度。
    做 Nova缓冲_设置协议 用 Nova缓冲、类型。
    以太网_接收计数 为 以太网_接收计数 加 1。
    若 类型 等于 以太网类型_VLAN：
        VLAN信息 为 做 以太网_解析VLAN 用 Nova缓冲。
        类型 为 VLAN信息 的 第 2 项。
    完。
    若 类型 等于 以太网类型_IP：
        返还 做 IP_接收 用 Nova缓冲。
    完。
    若 类型 等于 以太网类型_ARP：
        返还 做 ARP_接收 用 Nova缓冲。
    完。
    若 类型 等于 以太网类型_IPV6：
        返还 做 IPv6_接收 用 Nova缓冲。
    完。
    以太网_接收丢弃 为 以太网_接收丢弃 加 1。
    返还 错误_无效参数。
完。

虚网支持_统计 接受 零参：
    返还 一本字典：模块 对应 「虚网支持」、以太网_VLAN帧数 对应 以太网_VLAN帧数。
完。

虚网支持_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    返还 做 计数 用 问题 等于 0。
完。

虚网支持_综合报告 接受 零参：
    做 打印 用 「[虚网支持] ═══ 综合报告 ═══」。
    做 打印 用 「  以太网_VLAN帧数: 」 加 做 整数转字符串 用 以太网_VLAN帧数。
    做 打印 用 「  健康: 」 加 若 做 虚网支持_健康评估 则 「正常」 否则 「异常」。
完。

虚网支持_全局重置 接受 零参：
    以太网_VLAN帧数 为 0。
    做 打印 用 「[虚网支持] 全局统计已重置」。
    返还 0。
完。

本章 收束。
