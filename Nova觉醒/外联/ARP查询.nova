《ARP查询》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·感知·内核直连层》。

引用《.·网缓冲分配》。

引用《.·网络设备启停》。

引用《..·中枢·时钟中枢》。

引用《..·中枢·内核打印》。

引用《..·执行·域名解析》。

引用《..·中枢·整数转字符串》。

ARP_操作_请求 为 1。

ARP_操作_应答 为 2。

ARP_硬件类型_以太网 为 1。

ARP_协议类型_IPv4 为 0x0800。

ARP_硬件地址长度 为 6。

ARP_协议地址长度 为 4。

ARP_包长度 为 28。

ARP_缓存超时 为 1200000。

ARP_最大重试 为 3。

ARP_重试间隔 为 1000。

ARP_缓存最大条目 为 256。

ARP状态_不完整 为 0。

ARP状态_可达 为 1。

ARP状态_过期 为 2。

ARP状态_永久 为 3。

ARP缓存条目 是 一种 事物，含 IP地址、MAC地址、状态、创建时间、最后使用时间、重试次数、接口。

外部。

ARP缓存列表。

可变 ARP_请求计数 为 0。

可变 ARP_应答计数 为 0。

可变 ARP_缓存命中 为 0。

可变 ARP_缓存未命中 为 0。

ARP_查询 接受 IP：
    若 ARP缓存列表 等于 空：
        ARP_缓存未命中 为 ARP_缓存未命中 加 1。
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 ARP缓存列表：
        条目 为 ARP缓存列表 的 第 i 项。
        若 条目 的 IP地址 等于 IP：
            若 条目 的 状态 等于 ARP状态_可达 或者 条目 的 状态 等于 ARP状态_永久：
                条目 的 最后使用时间 为 做 读取毫秒。
                ARP_缓存命中 为 ARP_缓存命中 加 1。
                返还 条目 的 MAC地址。
            完。
            若 条目 的 状态 等于 ARP状态_过期：
                ARP_缓存未命中 为 ARP_缓存未命中 加 1。
                返还 0。
            完。
        完。
        i 为 i 加 1。
    完。
    ARP_缓存未命中 为 ARP_缓存未命中 加 1。
    返还 0。
完。

ARP_解析 接受 IP、设备：
    MAC 为 做 ARP_查询 用 IP。
    若 MAC 不等于 0：
        返还 MAC。
    完。
    做 ARP_添加不完整条目 用 IP、设备。
    做 ARP_发送请求 用 IP、设备。
    返还 0。
完。

ARP_添加不完整条目 接受 IP、设备：
    可变 i 为 0。
    只要 i 小于 做 计数 用 ARP缓存列表：
        若 ARP缓存列表 的 第 i 项 的 IP地址 等于 IP：
            返还。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 ARP缓存列表 大于或等于 ARP_缓存最大条目：
        做 ARP_淘汰最旧条目。
    完。
    条目 为 新建 ARP缓存条目：IP地址 为 IP、MAC地址 为 0、状态 为 ARP状态_不完整、创建时间 为 做 读取毫秒、最后使用时间 为 做 读取毫秒、重试次数 为 0、接口 为 设备。
    做 追加 用 ARP缓存列表、条目。
完。

ARP_更新缓存 接受 IP、MAC：
    可变 i 为 0。
    只要 i 小于 做 计数 用 ARP缓存列表：
        若 ARP缓存列表 的 第 i 项 的 IP地址 等于 IP：
            ARP缓存列表 的 第 i 项 的 MAC地址 为 MAC。
            ARP缓存列表 的 第 i 项 的 状态 为 ARP状态_可达。
            ARP缓存列表 的 第 i 项 的 最后使用时间 为 做 读取毫秒。
            ARP缓存列表 的 第 i 项 的 重试次数 为 0。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 ARP缓存列表 大于或等于 ARP_缓存最大条目：
        做 ARP_淘汰最旧条目。
    完。
    条目 为 新建 ARP缓存条目：IP地址 为 IP、MAC地址 为 MAC、状态 为 ARP状态_可达、创建时间 为 做 读取毫秒、最后使用时间 为 做 读取毫秒、重试次数 为 0、接口 为 空。
    做 追加 用 ARP缓存列表、条目。
完。

ARP_设置永久 接受 IP、MAC：
    做 ARP_更新缓存 用 IP、MAC。
    可变 i 为 0。
    只要 i 小于 做 计数 用 ARP缓存列表：
        若 ARP缓存列表 的 第 i 项 的 IP地址 等于 IP：
            ARP缓存列表 的 第 i 项 的 状态 为 ARP状态_永久。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

ARP_删除条目 接受 IP：
    可变 i 为 0。
    只要 i 小于 做 计数 用 ARP缓存列表：
        若 ARP缓存列表 的 第 i 项 的 IP地址 等于 IP：
            做 列表_移除 用 ARP缓存列表、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

ARP_淘汰最旧条目 接受 零参：
    若 做 计数 用 ARP缓存列表 等于 0：
        返还。
    完。
    可变 最旧索引 为 0。
    可变 最旧时间 为 ARP缓存列表 的 第 0 项 的 最后使用时间。
    可变 i 为 1。
    只要 i 小于 做 计数 用 ARP缓存列表：
        若 ARP缓存列表 的 第 i 项 的 状态 不等于 ARP状态_永久 并且 ARP缓存列表 的 第 i 项 的 最后使用时间 小于 最旧时间：
            最旧索引 为 i。
            最旧时间 为 ARP缓存列表 的 第 i 项 的 最后使用时间。
        完。
        i 为 i 加 1。
    完。
    若 ARP缓存列表 的 第 最旧索引 项 的 状态 不等于 ARP状态_永久：
        做 列表_移除 用 ARP缓存列表、最旧索引。
    完。
完。

ARP_老化检查 接受 零参：
    当前 为 做 读取毫秒。
    可变 i 为 做 计数 用 ARP缓存列表 减 1。
    只要 i 大于或等于 0：
        条目 为 ARP缓存列表 的 第 i 项。
        若 条目 的 状态 等于 ARP状态_永久：
            i 为 i 减 1。
            继续。
        完。
        若 条目 的 状态 等于 ARP状态_可达 并且 当前 减 条目 的 创建时间 大于 ARP_缓存超时：
            ARP缓存列表 的 第 i 项 的 状态 为 ARP状态_过期。
        完。
        若 条目 的 状态 等于 ARP状态_不完整 并且 条目 的 重试次数 大于或等于 ARP_最大重试：
            做 列表_移除 用 ARP缓存列表、i。
        完。
        i 为 i 减 1。
    完。
完。

ARP_发送请求 接受 IP、设备：
    ARP_请求计数 为 ARP_请求计数 加 1。
    做 内核调试 用 「以太网: ARP请求 」 加 做 IP转字符串_以太网 用 IP。
    Nova缓冲 为 做 Nova缓冲_分配 用 ARP_包长度 加 14。
    若 Nova缓冲 等于 0：
        返还。
    完。
    arp头 为 做 Nova缓冲_前推 用 Nova缓冲、ARP_包长度。
    做 内存_写16大端 用 arp头、ARP_硬件类型_以太网。
    做 内存_写16大端 用 arp头 加 2、ARP_协议类型_IPv4。
    做 内存_写8 用 arp头 加 4、ARP_硬件地址长度。
    做 内存_写8 用 arp头 加 5、ARP_协议地址长度。
    做 内存_写16大端 用 arp头 加 6、ARP_操作_请求。
    做 网络设备_发送 用 设备、Nova缓冲。
完。

ARP_处理接收 接受 Nova缓冲、设备：
    arp头 为 做 Nova缓冲_拉取 用 Nova缓冲、ARP_包长度。
    若 arp头 等于 0：
        返还。
    完。
    操作 为 做 内存_读16大端 用 arp头 加 6。
    发送方IP 为 做 内存_读32大端 用 arp头 加 14。
    发送方MAC 为 做 读取MAC 用 arp头 加 8。
    做 ARP_更新缓存 用 发送方IP、发送方MAC。
    若 操作 等于 ARP_操作_请求：
        目标IP 为 做 内存_读32大端 用 arp头 加 24。
        若 做 ARP_是本地IP 用 目标IP、设备：
            做 ARP_发送应答 用 发送方IP、发送方MAC、设备。
        完。
    完。
    ARP_应答计数 为 ARP_应答计数 加 1。
完。

ARP_发送应答 接受 目标IP、目标MAC、设备：
    做 内核调试 用 「以太网: ARP应答 → 」 加 做 IP转字符串_以太网 用 目标IP。
    Nova缓冲 为 做 Nova缓冲_分配 用 ARP_包长度 加 14。
    若 Nova缓冲 等于 0：
        返还。
    完。
    arp头 为 做 Nova缓冲_前推 用 Nova缓冲、ARP_包长度。
    做 内存_写16大端 用 arp头、ARP_硬件类型_以太网。
    做 内存_写16大端 用 arp头 加 2、ARP_协议类型_IPv4。
    做 内存_写8 用 arp头 加 4、ARP_硬件地址长度。
    做 内存_写8 用 arp头 加 5、ARP_协议地址长度。
    做 内存_写16大端 用 arp头 加 6、ARP_操作_应答。
    做 网络设备_发送 用 设备、Nova缓冲。
完。

ARP_免费公告 接受 IP、设备：
    做 内核调试 用 「以太网: 免费ARP 」 加 做 IP转字符串_以太网 用 IP。
    做 ARP_发送请求 用 IP、设备。
完。

ARP_是本地IP 接受 IP、设备：
    若 设备 等于 0：
        返还 假。
    完。
    若 设备 的 IP 等于 IP：
        返还 真。
    完。
    若 设备 的 IP地址 等于 IP：
        返还 真。
    完。
    若 设备 的 本地IP 等于 IP：
        返还 真。
    完。
    返还 假。
完。

读取MAC 接受 地址：
    返还 做 内存_读48 用 地址。
完。

IP转字符串_以太网 接受 ip：
    返还 做 整数转字符串 用 ip 右移 24 位与 0xFF 加 「.」 加 做 整数转字符串 用 ip 右移 16 位与 0xFF 加 「.」 加 做 整数转字符串 用 ip 右移 8 位与 0xFF 加 「.」 加 做 整数转字符串 用 ip 位与 0xFF。
完。

Nova缓冲_前推 接受 Nova缓冲、长度：
    返还 做 Nova缓冲_push 用 Nova缓冲、长度。
完。

Nova缓冲_拉取 接受 Nova缓冲、长度：
    返还 做 Nova缓冲_pull 用 Nova缓冲、长度。
完。

Nova缓冲_后推 接受 Nova缓冲、长度：
    返还 做 Nova缓冲_put 用 Nova缓冲、长度。
完。

随机数64 接受 零参：
    返还 做 读取毫秒 乘 6364136223846793005 加 1。
完。

外部。

网络设备_发送。

以太网_完整统计 接受 零参：
    返还 一组：以太网_发送帧数、以太网_接收帧数、以太网_丢弃帧数、ARP_请求计数、ARP_应答计数、ARP_缓存命中、ARP_缓存未命中、做 计数 用 ARP缓存列表。
完。

可变 AI_地址发现查_带宽估计 为 0。

可变 AI_地址发现查_丢包窗口 为 一组：（空）。

AI_地址发现查_带宽预测 接受 发送字节、耗时毫秒：
    若 耗时毫秒 等于 0：
        返还 AI_地址发现查_带宽估计。
    完。
    当前 为 发送字节 乘 1000 除以 耗时毫秒。
    AI_地址发现查_带宽估计 为 AI_地址发现查_带宽估计 乘 700 除以 1000 加 当前 乘 300 除以 1000。
    返还 AI_地址发现查_带宽估计。
完。

AI_地址发现查_拥塞建议 接受 丢包率：
    做 追加 用 AI_地址发现查_丢包窗口、丢包率。
    若 做 计数 用 AI_地址发现查_丢包窗口 大于 10：
        做 列表_移除 用 AI_地址发现查_丢包窗口、0。
    完。
    若 丢包率 大于 50：
        返还 「减半窗口」。
    完。
    若 丢包率 小于 5：
        返还 「增大窗口」。
    完。
    返还 「保持」。
完。

地址发现查_统计 接受 零参：
    返还 一本字典：ARP请求计数 对应 ARP_请求计数、ARP应答计数 对应 ARP_应答计数、ARP缓存命中 对应 ARP_缓存命中、ARP缓存未命中 对应 ARP_缓存未命中。
完。

地址发现查_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 ARP_请求计数 大于 100000：
        做 追加 用 问题、「ARP_请求计数异常增长」。
    完。
    若 ARP_应答计数 大于 100000：
        做 追加 用 问题、「ARP_应答计数异常增长」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

地址发现查_综合报告 接受 零参：
    做 打印 用 「[地址发现查] ═══ 综合报告 ═══」。
    做 打印 用 「  ARP_请求计数: 」 加 ARP_请求计数。
    做 打印 用 「  ARP_应答计数: 」 加 ARP_应答计数。
    做 打印 用 「  ARP_缓存命中: 」 加 ARP_缓存命中。
    做 打印 用 「  ARP_缓存未命中: 」 加 ARP_缓存未命中。
    做 打印 用 「  健康: 」 加 若 做 地址发现查_健康评估 则 「正常」 否则 「异常」。
完。

地址发现查_全局重置 接受 零参：
    ARP_请求计数 为 0。
    ARP_应答计数 为 0。
    ARP_缓存命中 为 0。
    ARP_缓存未命中 为 0。
    做 打印 用 「[地址发现查] 统计已重置」。
    返还 0。
完。

本章 收束。
