《自适应预取引擎》

预取_最大历史 为 64。

预取_最大并发 为 8。

预取_最大列表 为 128。

预取_超时毫秒 为 10000。

预取_最小置信 为 3000。

预取_缓存最大字节 为 52428800。

可变 NAIPE_预取总数 为 0。

可变 NAIPE_命中总数 为 0。

可变 NAIPE_取消总数 为 0。

可变 NAIPE_超时总数 为 0。

可变 NAIPE_缓存字节 为 0。

可变 NAIPE_AI预测总数 为 0。

AI预取_已存在 接受 引擎、url哈希：
    若 引擎 等于 0 或者 引擎 的 预取列表 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 引擎 的 预取数量：
        条目 为 做 NAIPE_获取条目 用 引擎、i。
        若 条目 不等于 0 并且 条目 的 URL哈希 等于 url哈希：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

NAIPE_获取条目 接受 引擎、idx：
    若 引擎 等于 0 或者 idx 小于 0 或者 idx 大于或等于 引擎 的 预取数量：
        返还 0。
    完。
    返还 做 内存_读64 用 引擎 的 预取列表 加 idx 乘 8。
完。

NAIPE_确保容量 接受 引擎：
    若 引擎 的 预取数量 小于 引擎 的 预取容量：
        返还。
    完。
    新容量 为 引擎 的 预取容量 乘 2。
    若 新容量 大于 预取_最大列表：
        新容量 为 预取_最大列表。
    完。
    若 新容量 小于或等于 引擎 的 预取容量：
        返还。
    完。
    新列表 为 做 堆_分配 用 新容量 乘 8。
    做 内存_拷贝 用 新列表、引擎 的 预取列表、引擎 的 预取容量 乘 8。
    做 堆_释放 用 引擎 的 预取列表。
    引擎 的 预取列表 为 新列表。
    引擎 的 预取容量 为 新容量。
完。

AI预取_添加 接受 引擎、url、哈希、类型、置信度、优先级：
    若 引擎 等于 0：
        返还。
    完。
    若 置信度 小于 预取_最小置信：
        返还。
    完。
    做 NAIPE_确保容量 用 引擎。
    entry 为 做 堆_分配 用 AI预取条目。
    entry 的 URL 为 url。
    entry 的 URL哈希 为 哈希。
    entry 的 类型 为 类型。
    entry 的 置信度 为 置信度。
    entry 的 状态 为 预取状态_等待。
    entry 的 优先级 为 优先级。
    entry 的 创建时间 为 做 时间_当前毫秒。
    做 内存_写64 用 引擎 的 预取列表 加 引擎 的 预取数量 乘 8、entry。
    引擎 的 预取数量 为 引擎 的 预取数量 加 1。
    引擎 的 总预取次数 为 引擎 的 总预取次数 加 1。
    NAIPE_预取总数 为 NAIPE_预取总数 加 1。
完。

NAIPE_排序置信度 接受 引擎：
    若 引擎 等于 0 或者 引擎 的 预取数量 小于或等于 1：
        返还。
    完。
    可变 i 为 1。
    只要 i 小于 引擎 的 预取数量：
        key 为 做 NAIPE_获取条目 用 引擎、i。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0：
            a 为 做 NAIPE_获取条目 用 引擎、j。
            若 a 等于 0 或者 key 等于 0：
                断开。
            完。
            若 a 的 置信度 大于或等于 key 的 置信度：
                断开。
            完。
            做 内存_写64 用 引擎 的 预取列表 加 (j 加 1) 乘 8、a。
            j 为 j 减 1。
        完。
        做 内存_写64 用 引擎 的 预取列表 加 (j 加 1) 乘 8、key。
        i 为 i 加 1。
    完。
完。

NAIPE_调度执行 接受 引擎：
    若 引擎 等于 0 或者 引擎 的 预取数量 等于 0：
        返还。
    完。
    做 NAIPE_排序置信度 用 引擎。
    最大并发 为 做 AI预取_最大并发 用 引擎。
    可变 当前并发 为 0。
    可变 i 为 0。
    只要 i 小于 引擎 的 预取数量 并且 当前并发 小于 最大并发：
        entry 为 做 NAIPE_获取条目 用 引擎、i。
        若 entry 不等于 0 并且 entry 的 状态 等于 预取状态_等待：
            entry 的 状态 为 预取状态_加载中。
            做 NAIPE_发起请求 用 引擎、entry。
            当前并发 为 当前并发 加 1。
        完。
        i 为 i 加 1。
    完。
完。

NAIPE_发起请求 接受 引擎、entry：
    若 引擎 等于 0 或者 entry 等于 0：
        返还。
    完。
    req 为 做 堆_分配 用 40。
    做 内存_写64 用 req、entry 的 URL。
    做 内存_写32 用 req 加 8、entry 的 优先级。
    做 内存_写32 用 req 加 12、1。
    做 内存_写64 用 req 加 16、entry。
    做 HTTP2_提交请求 用 req。
完。

NAIPE_检查命中 接受 引擎、url：
    若 引擎 等于 0 或者 url 等于 0：
        返还 假。
    完。
    h 为 做 字符串_哈希 用 url。
    可变 i 为 0。
    只要 i 小于 引擎 的 预取数量：
        entry 为 做 NAIPE_获取条目 用 引擎、i。
        若 entry 不等于 0 并且 entry 的 URL哈希 等于 h 并且 entry 的 状态 等于 预取状态_完成：
            引擎 的 总命中次数 为 引擎 的 总命中次数 加 1。
            NAIPE_命中总数 为 NAIPE_命中总数 加 1。
            若 引擎 的 总预取次数 大于 0：
                引擎 的 预取命中率 为 引擎 的 总命中次数 乘 10000 除以 引擎 的 总预取次数。
            完。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

NAIPE_标记完成 接受 引擎、entry、字节数：
    若 entry 等于 0：
        返还。
    完。
    entry 的 状态 为 预取状态_完成。
    NAIPE_缓存字节 为 NAIPE_缓存字节 加 字节数。
完。

NAIPE_标记失败 接受 引擎、entry：
    若 entry 等于 0：
        返还。
    完。
    entry 的 状态 为 预取状态_失败。
完。

NAIPE_清理超时 接受 引擎：
    若 引擎 等于 0：
        返还。
    完。
    现在 为 做 时间_当前毫秒。
    可变 i 为 0。
    只要 i 小于 引擎 的 预取数量：
        entry 为 做 NAIPE_获取条目 用 引擎、i。
        若 entry 不等于 0 并且 entry 的 状态 等于 预取状态_加载中：
            若 现在 减 entry 的 创建时间 大于 预取_超时毫秒：
                entry 的 状态 为 预取状态_失败。
                NAIPE_超时总数 为 NAIPE_超时总数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

NAIPE_取消全部 接受 引擎：
    若 引擎 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 引擎 的 预取数量：
        entry 为 做 NAIPE_获取条目 用 引擎、i。
        若 entry 不等于 0 并且 (entry 的 状态 等于 预取状态_等待 或者 entry 的 状态 等于 预取状态_加载中)：
            entry 的 状态 为 预取状态_失败。
            NAIPE_取消总数 为 NAIPE_取消总数 加 1。
        完。
        i 为 i 加 1。
    完。
完。

NAIPE_清理已完成 接受 引擎：
    若 引擎 等于 0：
        返还。
    完。
    可变 写 为 0。
    可变 i 为 0。
    只要 i 小于 引擎 的 预取数量：
        entry 为 做 NAIPE_获取条目 用 引擎、i。
        若 entry 不等于 0 并且 (entry 的 状态 等于 预取状态_等待 或者 entry 的 状态 等于 预取状态_加载中)：
            做 内存_写64 用 引擎 的 预取列表 加 写 乘 8、entry。
            写 为 写 加 1。
        否则：
            若 entry 不等于 0：
                做 堆_释放 用 entry。
            完。
        完。
        i 为 i 加 1。
    完。
    引擎 的 预取数量 为 写。
完。

NAIPE_历史预取 接受 引擎、当前URL：
    若 引擎 等于 0 或者 当前URL 等于 0：
        返还。
    完。
    若 引擎 的 历史数量 小于 2：
        返还。
    完。
    当前哈希 为 做 字符串_哈希 用 当前URL。
    可变 i 为 0。
    只要 i 小于 引擎 的 历史数量 减 1：
        h 为 做 内存_读64 用 引擎 的 历史URL表 加 i 乘 8。
        若 h 不等于 0 并且 做 字符串_哈希 用 h 等于 当前哈希：
            下一 为 做 内存_读64 用 引擎 的 历史URL表 加 (i 加 1) 乘 8。
            若 下一 不等于 0：
                nh 为 做 字符串_哈希 用 下一。
                若 非 做 AI预取_已存在 用 引擎、nh：
                    做 AI预取_添加 用 引擎、下一、nh、AI预取_历史模式、8000、100。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

NAIPE_语义预取 接受 引擎、链接列表、数量、页面类型：
    若 引擎 等于 0 或者 链接列表 等于 0：
        返还。
    完。
    可变 基准置信 为 5000。
    若 页面类型 等于 1：
        基准置信 为 7000。
    完。
    若 页面类型 等于 2：
        基准置信 为 6000。
    完。
    若 页面类型 等于 3：
        基准置信 为 4000。
    完。
    可变 i 为 0。
    可变 已添加 为 0。
    只要 i 小于 数量 并且 已添加 小于 6：
        url 为 做 内存_读64 用 链接列表 加 i 乘 8。
        若 url 不等于 0：
            h 为 做 字符串_哈希 用 url。
            若 非 做 AI预取_已存在 用 引擎、h：
                c 为 基准置信 减 i 乘 500。
                若 c 小于 预取_最小置信：
                    c 为 预取_最小置信。
                完。
                做 AI预取_添加 用 引擎、url、h、AI预取_语义关联、c、80。
                已添加 为 已添加 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

AI预取_释放引擎 接受 引擎：
    若 引擎 等于 0：
        返还。
    完。
    若 引擎 的 预取列表 不等于 0：
        可变 i 为 0。
        只要 i 小于 引擎 的 预取数量：
            e 为 做 NAIPE_获取条目 用 引擎、i。
            若 e 不等于 0：
                做 堆_释放 用 e。
            完。
            i 为 i 加 1。
        完。
        做 堆_释放 用 引擎 的 预取列表。
    完。
    若 引擎 的 历史URL表 不等于 0：
        做 堆_释放 用 引擎 的 历史URL表。
    完。
    做 堆_释放 用 引擎。
完。

NAIPE_AI提取特征 接受 引擎：
    t 为 做 Tensor_创建 用 10。
    若 t 等于 0：
        返还 0。
    完。
    做 Tensor_写 用 t、0、引擎 的 预取数量。
    做 Tensor_写 用 t、1、引擎 的 总预取次数。
    做 Tensor_写 用 t、2、引擎 的 总命中次数。
    做 Tensor_写 用 t、3、引擎 的 网络带宽估计。
    做 Tensor_写 用 t、4、引擎 的 预取命中率。
    做 Tensor_写 用 t、5、引擎 的 历史数量。
    做 Tensor_写 用 t、6、NAIPE_预取总数。
    做 Tensor_写 用 t、7、NAIPE_命中总数。
    做 Tensor_写 用 t、8、NAIPE_缓存字节。
    做 Tensor_写 用 t、9、NAIPE_AI预测总数。
    返还 t。
完。

NAIPE_AI预测下一预取 接受 引擎、当前URL：
    若 引擎 等于 0：
        返还 0。
    完。
    feat 为 做 NAIPE_AI提取特征 用 引擎。
    若 feat 等于 0：
        返还 0。
    完。
    结果 为 做 AI总线_推理 用 「naipe_prefetch」、feat。
    做 全局AI总线 的 发布 用 「naipe_predict」、「next」、结果。
    做 Tensor_释放 用 feat。
    NAIPE_AI预测总数 为 NAIPE_AI预测总数 加 1。
    返还 结果。
完。

NAIPE_AI异常检测 接受 引擎：
    feat 为 做 NAIPE_AI提取特征 用 引擎。
    若 feat 等于 0：
        返还 0。
    完。
    异常 为 做 AI总线_异常检测 用 「naipe_engine」、feat。
    做 Tensor_释放 用 feat。
    返还 异常。
完。

NAIPE_获取统计 接受 零参：
    返还 一组：NAIPE_预取总数、NAIPE_命中总数、NAIPE_取消总数、NAIPE_超时总数、NAIPE_缓存字节、NAIPE_AI预测总数。
完。

NAIPE_重置统计 接受 零参：
    NAIPE_预取总数 为 0。
    NAIPE_命中总数 为 0。
    NAIPE_取消总数 为 0。
    NAIPE_超时总数 为 0。
    NAIPE_缓存字节 为 0。
    NAIPE_AI预测总数 为 0。
完。

NAIPE_自检 接受 零参：
    e 为 做 AI预取_创建引擎。
    若 e 等于 0：
        返还 假。
    完。
    做 AI预取_链接悬停 用 e、「nova://test/page1」。
    若 e 的 预取数量 不等于 1：
        做 AI预取_释放引擎 用 e。
        返还 假。
    完。
    做 AI预取_链接悬停 用 e、「nova://test/page1」。
    若 e 的 预取数量 不等于 1：
        做 AI预取_释放引擎 用 e。
        返还 假。
    完。
    若 做 AI预取_最大并发 用 e 不等于 4：
        做 AI预取_释放引擎 用 e。
        返还 假。
    完。
    做 AI预取_更新带宽 用 e、6000。
    若 做 AI预取_最大并发 用 e 不等于 6：
        做 AI预取_释放引擎 用 e。
        返还 假。
    完。
    做 AI预取_释放引擎 用 e。
    返还 真。
完。

NAIPE_缓存压力检查 接受 引擎：
    若 引擎 等于 0：
        返还 假。
    完。
    返还 NAIPE_缓存字节 大于 预取_缓存最大字节。
完。

NAIPE_驱逐低优先级 接受 引擎：
    若 引擎 等于 0：
        返还。
    完。
    做 NAIPE_排序置信度 用 引擎。
    可变 释放数 为 0。
    可变 i 为 引擎 的 预取数量 减 1。
    只要 i 大于或等于 0 并且 NAIPE_缓存字节 大于 预取_缓存最大字节 乘 3 除以 4：
        entry 为 做 NAIPE_获取条目 用 引擎、i。
        若 entry 不等于 0 并且 entry 的 状态 等于 预取状态_完成：
            entry 的 状态 为 预取状态_失败。
            NAIPE_缓存字节 为 NAIPE_缓存字节 减 8192。
            释放数 为 释放数 加 1。
        完。
        i 为 i 减 1。
    完。
完。

NAIPE_全局净化 接受 引擎：
    若 引擎 等于 0：
        返还。
    完。
    做 NAIPE_清理超时 用 引擎。
    做 NAIPE_清理已完成 用 引擎。
    若 做 NAIPE_缓存压力检查 用 引擎：
        做 NAIPE_驱逐低优先级 用 引擎。
    完。
完。

NAIPE_打印状态 接受 引擎：
    若 引擎 等于 0：
        返还。
    完。
    做 日志 用 「[NAIPE] 预取数=」、引擎 的 预取数量、「 总预取=」、引擎 的 总预取次数、「 命中=」、引擎 的 总命中次数、「 命中率=」、引擎 的 预取命中率、「 带宽=」、引擎 的 网络带宽估计、「 缓存=」、NAIPE_缓存字节。
完。

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·同步原语》。

引用《..·感知·内核直连层》。

引用《..·感知·原子驻留》。

引用《..·推理·定点Q算术》。

AI预取_链接悬停 为 1。

AI预取_滚动方向 为 2。

AI预取_历史模式 为 3。

AI预取_语义关联 为 4。

预取状态_等待 为 0。

预取状态_加载中 为 1。

预取状态_完成 为 2。

预取状态_失败 为 3。

AI预取条目 是 一种 事物，含 URL、URL哈希、类型、置信度、状态、优先级、创建时间。

AI预取引擎 是 一种 事物，含 预取列表、预取数量、预取容量、历史URL表、历史数量、网络带宽估计、预取命中率、总预取次数、总命中次数。

AI预取_创建引擎 接受 零参：
    e 为 做 堆_分配 用 AI预取引擎。
    e 的 预取容量 为 32。
    e 的 预取列表 为 做 堆_分配_数组 用 e 的 预取容量 乘 40。
    e 的 预取数量 为 0。
    e 的 历史URL表 为 做 堆_分配_数组 用 64 乘 8。
    e 的 历史数量 为 0。
    e 的 网络带宽估计 为 1000。
    e 的 预取命中率 为 0。
    e 的 总预取次数 为 0。
    e 的 总命中次数 为 0。
    返还 e。
完。

AI预取_链接悬停 接受 引擎、链接URL：
    若 引擎 等于 0 或者 链接URL 等于 0：
        返还。
    完。
    url哈希 为 做 字符串_哈希 用 链接URL。
    若 做 AI预取_已存在 用 引擎、url哈希：
        返还。
    完。
    做 AI预取_添加 用 引擎、链接URL、url哈希、AI预取_链接悬停、6000、128。
完。

AI预取_滚动方向 接受 引擎、资源URL列表、数量：
    若 引擎 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 数量 并且 i 小于 8：
        url 为 做 内存_读64 用 资源URL列表 加 i 乘 8。
        若 url 不等于 0：
            h 为 做 字符串_哈希 用 url。
            若 非 做 AI预取_已存在 用 引擎、h：
                做 AI预取_添加 用 引擎、url、h、AI预取_滚动方向、7500、160。
            完。
        完。
        i 为 i 加 1。
    完。
完。

AI预取_记录访问 接受 引擎、当前URL：
    若 引擎 等于 0 或者 当前URL 等于 0：
        返还。
    完。
    若 引擎 的 历史数量 小于 64：
        做 内存_写64 用 引擎 的 历史URL表 加 引擎 的 历史数量 乘 8、当前URL。
        引擎 的 历史数量 为 引擎 的 历史数量 加 1。
    完。
完。

AI预取_更新带宽 接受 引擎、实测带宽：
    若 引擎 等于 0：
        返还。
    完。
    引擎 的 网络带宽估计 为 实测带宽。
完。

AI预取_最大并发 接受 引擎：
    若 引擎 等于 0：
        返还 2。
    完。
    若 引擎 的 网络带宽估计 大于 5000：
        返还 6。
    完。
    若 引擎 的 网络带宽估计 大于 1000：
        返还 4。
    完。
    若 引擎 的 网络带宽估计 大于 200：
        返还 2。
    完。
    返还 1。
完。

本章 收束。
