《UDP校验和》

UDP_最大包长 为 65535。

UDP_头长 为 8。

UDP_默认接收缓冲 为 212992。

UDP_最大接收缓冲 为 16777216。

UDP_校验和_无 为 0。

UDP_校验和_完整 为 1。

UDP_校验和_硬件 为 2。

MSG_PEEK 为 0x02。

MSG_DONTWAIT 为 0x40。

MSG_TRUNC 为 0x20。

MSG_WAITALL 为 0x100。

MSG_ERRQUEUE 为 0x2000。

RECV_超时默认 为 30000。

RECV_水位低 为 1。

RECV_水位高 为 0。

MCAST_最大组 为 256。

MCAST_加入 为 1。

MCAST_离开 为 2。

MCAST_阻塞源 为 3。

MCAST_解除阻塞 为 4。

可变 NURP_接收总数 为 0。

可变 NURP_字节总数 为 0。

可变 NURP_丢包总数 为 0。

可变 NURP_校验和错误 为 0。

可变 NURP_超时总数 为 0。

可变 NURP_组播接收 为 0。

可变 NURP_广播接收 为 0。

可变 NURP_AI预测总数 为 0。

NURP_伪头校验和 接受 源IP、目标IP、协议、长度：
    可变 和 为 0。
    和 为 和 加 (源IP 右移 16)。
    和 为 和 加 (源IP 位与 0xFFFF)。
    和 为 和 加 (目标IP 右移 16)。
    和 为 和 加 (目标IP 位与 0xFFFF)。
    和 为 和 加 协议。
    和 为 和 加 长度。
    返还 和。
完。

NURP_数据校验和 接受 数据、长度：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 加 1 小于 长度：
        字 为 做 内存_读8 用 数据 加 i 左移 8 位或 做 内存_读8 用 数据 加 i 加 1。
        和 为 和 加 字。
        i 为 i 加 2。
    完。
    若 i 小于 长度：
        和 为 和 加 (做 内存_读8 用 数据 加 i 左移 8)。
    完。
    只要 和 大于 0xFFFF：
        和 为 (和 右移 16) 加 (和 位与 0xFFFF)。
    完。
    返还 和 位与 0xFFFF。
完。

NURP_验证校验和 接受 Nova缓冲、源IP、目标IP：
    若 Nova缓冲 等于 空：
        返还 假。
    完。
    头校验 为 做 内存_读16大端 用 Nova缓冲 的 数据 加 6。
    若 头校验 等于 0：
        返还 真。
    完。
    若 Nova缓冲 的 校验状态 等于 UDP_校验和_硬件：
        返还 真。
    完。
    总长 为 Nova缓冲 的 数据长度。
    伪 为 做 NURP_伪头校验和 用 源IP、目标IP、17、总长。
    数据 为 做 NURP_数据校验和 用 Nova缓冲 的 数据、总长。
    可变 total 为 伪 加 数据。
    只要 total 大于 0xFFFF：
        total 为 (total 右移 16) 加 (total 位与 0xFFFF)。
    完。
    结果 为 按位取反 total 位与 0xFFFF。
    若 结果 不等于 0：
        NURP_校验和错误 为 NURP_校验和错误 加 1。
        返还 假。
    完。
    返还 真。
完。

RecvBufferCtrl 是 一种 事物，含 当前大小、最大大小、低水位、高水位、队列长度、锁。

NURP_创建缓冲控制 接受 零参：
    b 为 做 堆_分配 用 RecvBufferCtrl。
    若 b 等于 0：
        返还 0。
    完。
    b 的 当前大小 为 0。
    b 的 最大大小 为 UDP_默认接收缓冲。
    b 的 低水位 为 RECV_水位低。
    b 的 高水位 为 0。
    b 的 队列长度 为 0。
    b 的 锁 为 0。
    返还 b。
完。

NURP_缓冲可接收 接受 ctrl、数据长度：
    若 ctrl 等于 0：
        返还 假。
    完。
    返还 ctrl 的 当前大小 加 数据长度 小于或等于 ctrl 的 最大大小。
完。

NURP_缓冲入队 接受 ctrl、大小：
    若 ctrl 等于 0：
        返还。
    完。
    ctrl 的 当前大小 为 ctrl 的 当前大小 加 大小。
    ctrl 的 队列长度 为 ctrl 的 队列长度 加 1。
完。

NURP_缓冲出队 接受 ctrl、大小：
    若 ctrl 等于 0：
        返还。
    完。
    ctrl 的 当前大小 为 ctrl 的 当前大小 减 大小。
    若 ctrl 的 当前大小 小于 0：
        ctrl 的 当前大小 为 0。
    完。
    ctrl 的 队列长度 为 ctrl 的 队列长度 减 1。
    若 ctrl 的 队列长度 小于 0：
        ctrl 的 队列长度 为 0。
    完。
完。

NURP_设置接收缓冲大小 接受 sock、新大小：
    若 sock 等于 空 或者 sock 的 接收缓冲控制 等于 空：
        返还 错误码_参数无效。
    完。
    若 新大小 小于 1024：
        新大小 为 1024。
    完。
    若 新大小 大于 UDP_最大接收缓冲：
        新大小 为 UDP_最大接收缓冲。
    完。
    sock 的 接收缓冲控制 的 最大大小 为 新大小。
    返还 0。
完。

NURP_等待数据_超时 接受 sock、超时毫秒、标志：
    若 sock 等于 空：
        返还 错误码_参数无效。
    完。
    若 标志 位与 MSG_DONTWAIT：
        若 做 Nova网络缓冲_队列_为空 用 sock 的 接收缓冲：
            返还 错误码_将阻塞。
        完。
        返还 0。
    完。
    截止 为 做 时间_当前毫秒 加 超时毫秒。
    只要 做 Nova网络缓冲_队列_为空 用 sock 的 接收缓冲：
        剩余 为 截止 减 做 时间_当前毫秒。
        若 剩余 小于或等于 0：
            NURP_超时总数 为 NURP_超时总数 加 1。
            返还 错误码_超时。
        完。
        可变 等待项 为 做 等待项_创建 用 做 获取当前进程、真。
        做 等待队列_加入 用 sock 的 接收等待队列、等待项。
        做 进程_设置状态 用 做 获取当前进程、进程状态_可中断睡眠。
        做 调度_超时 用 剩余。
        做 等待队列_移除 用 sock 的 接收等待队列、等待项。
        若 做 当前进程_有待处理信号：
            返还 错误码_被中断。
        完。
    完。
    返还 0。
完。

NURP_peek接收 接受 sock、缓冲、最大长度、源地址：
    若 sock 等于 空：
        返还 错误码_参数无效。
    完。
    做 自旋锁_获取 用 sock 的 锁。
    Nova缓冲 为 做 Nova网络缓冲_队列_查看头 用 sock 的 接收缓冲。
    做 自旋锁_释放 用 sock 的 锁。
    若 Nova缓冲 等于 空：
        返还 0。
    完。
    复制长度 为 做 取小 用 Nova缓冲 的 数据长度、最大长度。
    做 内存复制 用 缓冲、Nova缓冲 的 数据、复制长度。
    若 源地址 不等于 空：
        源地址 的 IP 为 Nova缓冲 的 源IP。
        源地址 的 端口 为 Nova缓冲 的 源端口。
    完。
    返还 复制长度。
完。

udp_recvmsg 接受 sock、缓冲、最大长度、标志、源地址：
    若 sock 等于 空 或者 缓冲 等于 空：
        返还 错误码_参数无效。
    完。
    超时 为 sock 的 接收超时。
    若 超时 小于或等于 0：
        超时 为 RECV_超时默认。
    完。
    等待结果 为 做 NURP_等待数据_超时 用 sock、超时、标志。
    若 等待结果 不等于 0：
        返还 等待结果。
    完。
    若 标志 位与 MSG_PEEK：
        返还 做 NURP_peek接收 用 sock、缓冲、最大长度、源地址。
    完。
    做 自旋锁_获取 用 sock 的 锁。
    Nova缓冲 为 做 Nova网络缓冲_队列_出队 用 sock 的 接收缓冲。
    做 自旋锁_释放 用 sock 的 锁。
    若 Nova缓冲 等于 空：
        返还 0。
    完。
    实际长度 为 Nova缓冲 的 数据长度。
    复制长度 为 做 取小 用 实际长度、最大长度。
    做 内存复制 用 缓冲、Nova缓冲 的 数据、复制长度。
    若 源地址 不等于 空：
        源地址 的 IP 为 Nova缓冲 的 源IP。
        源地址 的 端口 为 Nova缓冲 的 源端口。
    完。
    若 sock 的 接收缓冲控制 不等于 空：
        做 NURP_缓冲出队 用 sock 的 接收缓冲控制、实际长度。
    完。
    NURP_接收总数 为 NURP_接收总数 加 1。
    NURP_字节总数 为 NURP_字节总数 加 复制长度。
    做 Nova缓冲_释放 用 Nova缓冲。
    若 标志 位与 MSG_TRUNC：
        返还 实际长度。
    完。
    返还 复制长度。
完。

MulticastGroup 是 一种 事物，含 组地址、接口索引、源过滤列表、源过滤数量、模式。

NURP_是组播地址 接受 IP：
    首字节 为 IP 右移 24 位与 0xFF。
    返还 首字节 大于或等于 224 并且 首字节 小于或等于 239。
完。

NURP_是广播地址 接受 IP、子网掩码：
    主机位 为 IP 位或 子网掩码。
    返还 主机位 等于 0xFFFFFFFF。
完。

NURP_检查组播成员 接受 sock、组地址：
    若 sock 等于 空 或者 sock 的 组播列表 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 sock 的 组播数量：
        g 为 做 内存_读64 用 sock 的 组播列表 加 i 乘 8。
        若 g 不等于 0 并且 g 的 组地址 等于 组地址：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

NURP_检查源过滤 接受 组、源IP：
    若 组 等于 0 或者 组 的 源过滤数量 等于 0：
        返还 真。
    完。
    可变 i 为 0。
    可变 找到 为 假。
    只要 i 小于 组 的 源过滤数量：
        若 做 内存_读32 用 组 的 源过滤列表 加 i 乘 4 等于 源IP：
            找到 为 真。
            断开。
        完。
        i 为 i 加 1。
    完。
    若 组 的 模式 等于 1：
        返还 找到。
    完。
    返还 非 找到。
完。

NURP_组播入队 接受 Nova缓冲、组地址：
    可变 分发数 为 0。
    可变 桶 为 0。
    只要 桶 小于 全局数报哈希表 的 桶数：
        可变 当前 为 全局数报哈希表 的 桶 的 第 桶 项。
        只要 当前 不等于 空：
            若 当前 的 本地端口 等于 Nova缓冲 的 目标端口：
                若 做 NURP_检查组播成员 用 当前、组地址：
                    克隆 为 做 Nova缓冲_克隆 用 Nova缓冲。
                    若 克隆 不等于 空：
                        做 自旋锁_获取 用 当前 的 锁。
                        做 Nova网络缓冲_队列_入队 用 当前 的 接收缓冲、克隆。
                        做 自旋锁_释放 用 当前 的 锁。
                        做 等待队列_唤醒 用 当前 的 接收等待队列。
                        分发数 为 分发数 加 1。
                    完。
                完。
            完。
            当前 为 当前 的 下一个。
        完。
        桶 为 桶 加 1。
    完。
    NURP_组播接收 为 NURP_组播接收 加 分发数。
    返还 分发数。
完。

udp_加入组播 接受 sock、组地址、接口索引：
    若 sock 等于 空：
        返还 错误码_参数无效。
    完。
    若 非 做 NURP_是组播地址 用 组地址：
        返还 错误码_参数无效。
    完。
    若 sock 的 组播数量 大于或等于 MCAST_最大组：
        返还 错误码_无空间。
    完。
    g 为 做 堆_分配 用 MulticastGroup。
    g 的 组地址 为 组地址。
    g 的 接口索引 为 接口索引。
    g 的 源过滤列表 为 0。
    g 的 源过滤数量 为 0。
    g 的 模式 为 0。
    若 sock 的 组播列表 等于 空：
        sock 的 组播列表 为 做 堆_分配 用 MCAST_最大组 乘 8。
    完。
    做 内存_写64 用 sock 的 组播列表 加 sock 的 组播数量 乘 8、g。
    sock 的 组播数量 为 sock 的 组播数量 加 1。
    返还 0。
完。

udp_离开组播 接受 sock、组地址：
    若 sock 等于 空 或者 sock 的 组播列表 等于 空：
        返还 错误码_参数无效。
    完。
    可变 i 为 0。
    只要 i 小于 sock 的 组播数量：
        g 为 做 内存_读64 用 sock 的 组播列表 加 i 乘 8。
        若 g 不等于 0 并且 g 的 组地址 等于 组地址：
            若 g 的 源过滤列表 不等于 0：
                做 堆_释放 用 g 的 源过滤列表。
            完。
            做 堆_释放 用 g。
            可变 j 为 i。
            只要 j 小于 sock 的 组播数量 减 1：
                做 内存_写64 用 sock 的 组播列表 加 j 乘 8、做 内存_读64 用 sock 的 组播列表 加 (j 加 1) 乘 8。
                j 为 j 加 1。
            完。
            sock 的 组播数量 为 sock 的 组播数量 减 1。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 错误码_未找到。
完。

udp_接收入站 接受 Nova缓冲、源IP、目标IP：
    若 Nova缓冲 等于 空：
        返还 错误码_参数无效。
    完。
    若 非 做 NURP_验证校验和 用 Nova缓冲、源IP、目标IP：
        做 Nova缓冲_释放 用 Nova缓冲。
        返还 错误码_校验和。
    完。
    源端口 为 做 内存_读16大端 用 Nova缓冲 的 数据。
    目标端口 为 做 内存_读16大端 用 Nova缓冲 的 数据 加 2。
    长度 为 做 内存_读16大端 用 Nova缓冲 的 数据 加 4。
    若 长度 小于 UDP_头长 或者 长度 大于 Nova缓冲 的 数据长度：
        NURP_丢包总数 为 NURP_丢包总数 加 1。
        做 Nova缓冲_释放 用 Nova缓冲。
        返还 错误码_消息太长。
    完。
    Nova缓冲 的 数据 为 Nova缓冲 的 数据 加 UDP_头长。
    Nova缓冲 的 数据长度 为 长度 减 UDP_头长。
    Nova缓冲 的 源IP 为 源IP。
    Nova缓冲 的 源端口 为 源端口。
    Nova缓冲 的 目标端口 为 目标端口。
    若 做 NURP_是组播地址 用 目标IP：
        做 NURP_组播入队 用 Nova缓冲、目标IP。
        做 Nova缓冲_释放 用 Nova缓冲。
        返还 0。
    完。
    若 目标IP 等于 0xFFFFFFFF：
        NURP_广播接收 为 NURP_广播接收 加 1。
    完。
    sock 为 做 udp_查找socket 用 目标IP、目标端口、源IP、源端口。
    若 sock 等于 空：
        NURP_丢包总数 为 NURP_丢包总数 加 1。
        做 Nova缓冲_释放 用 Nova缓冲。
        返还 错误码_连接拒绝。
    完。
    若 sock 的 接收缓冲控制 不等于 空：
        若 非 做 NURP_缓冲可接收 用 sock 的 接收缓冲控制、Nova缓冲 的 数据长度：
            NURP_丢包总数 为 NURP_丢包总数 加 1。
            做 Nova缓冲_释放 用 Nova缓冲。
            返还 错误码_无缓冲。
        完。
        做 NURP_缓冲入队 用 sock 的 接收缓冲控制、Nova缓冲 的 数据长度。
    完。
    做 自旋锁_获取 用 sock 的 锁。
    做 Nova网络缓冲_队列_入队 用 sock 的 接收缓冲、Nova缓冲。
    做 自旋锁_释放 用 sock 的 锁。
    做 等待队列_唤醒 用 sock 的 接收等待队列。
    返还 0。
完。

UDP_哈希桶数 为 512。

udp_哈希 接受 端口：
    可变 h 为 端口。
    h 为 h 加 (h 左移 10)。
    h 为 h 位异或 h 右移 6。
    h 为 h 加 (h 左移 3)。
    h 为 h 位异或 h 右移 11。
    h 为 h 加 (h 左移 15)。
    返还 (h 位与 0x7FFFFFFF) 取余于 UDP_哈希桶数。
完。

udp_注册socket 接受 sock：
    若 sock 等于 空：
        返还 错误码_参数无效。
    完。
    桶号 为 做 udp_哈希 用 sock 的 本地端口。
    sock 的 下一个 为 全局数报哈希表 的 桶 的 第 桶号 项。
    全局数报哈希表 的 桶 的 第 桶号 项 为 sock。
    全局数报哈希表 的 总数 为 全局数报哈希表 的 总数 加 1。
    返还 0。
完。

udp_注销socket 接受 sock：
    若 sock 等于 空：
        返还 错误码_参数无效。
    完。
    桶号 为 做 udp_哈希 用 sock 的 本地端口。
    可变 前 为 空。
    可变 当前 为 全局数报哈希表 的 桶 的 第 桶号 项。
    只要 当前 不等于 空：
        若 当前 等于 sock：
            若 前 等于 空：
                全局数报哈希表 的 桶 的 第 桶号 项 为 当前 的 下一个。
            否则：
                前 的 下一个 为 当前 的 下一个。
            完。
            全局数报哈希表 的 总数 为 全局数报哈希表 的 总数 减 1。
            返还 0。
        完。
        前 为 当前。
        当前 为 当前 的 下一个。
    完。
    返还 错误码_未找到。
完。

udp_recvmmsg 接受 sock、缓冲数组、长度数组、数量、标志、源地址数组：
    若 sock 等于 空 或者 数量 小于或等于 0：
        返还 错误码_参数无效。
    完。
    可变 已接收 为 0。
    只要 已接收 小于 数量：
        buf 为 做 内存_读64 用 缓冲数组 加 已接收 乘 8。
        最大长 为 做 内存_读32 用 长度数组 加 已接收 乘 4。
        可变 src 为 空。
        若 源地址数组 不等于 空：
            src 为 做 内存_读64 用 源地址数组 加 已接收 乘 8。
        完。
        可变 此标志 为 标志。
        若 已接收 大于 0：
            此标志 为 此标志 位或 MSG_DONTWAIT。
        完。
        r 为 做 udp_recvmsg 用 sock、buf、最大长、此标志、src。
        若 r 小于 0：
            若 已接收 大于 0：
                断开。
            完。
            返还 r。
        完。
        做 内存_写32 用 长度数组 加 已接收 乘 4、r。
        已接收 为 已接收 加 1。
    完。
    返还 已接收。
完。

NURP_入队错误 接受 sock、类型、代码、原始数据、长度：
    若 sock 等于 空 或者 sock 的 错误队列 等于 空：
        返还。
    完。
    err 为 做 堆_分配 用 20 加 长度。
    若 err 等于 0：
        返还。
    完。
    做 内存_写32 用 err、类型。
    做 内存_写32 用 err 加 4、代码。
    做 内存_写32 用 err 加 8、做 时间_当前毫秒。
    做 内存_写32 用 err 加 12、长度。
    若 长度 大于 0 并且 原始数据 不等于 空：
        做 内存复制 用 err 加 20、原始数据、长度。
    完。
    做 自旋锁_获取 用 sock 的 锁。
    做 Nova网络缓冲_队列_入队 用 sock 的 错误队列、err。
    做 自旋锁_释放 用 sock 的 锁。
完。

NURP_接收错误 接受 sock、缓冲、最大长度：
    若 sock 等于 空 或者 sock 的 错误队列 等于 空：
        返还 错误码_参数无效。
    完。
    做 自旋锁_获取 用 sock 的 锁。
    err 为 做 Nova网络缓冲_队列_出队 用 sock 的 错误队列。
    做 自旋锁_释放 用 sock 的 锁。
    若 err 等于 空：
        返还 错误码_无消息。
    完。
    长度 为 做 内存_读32 用 err 加 12。
    复制 为 做 取小 用 长度、最大长度。
    做 内存复制 用 缓冲、err 加 20、复制。
    做 堆_释放 用 err。
    返还 复制。
完。

NURP_AI提取特征 接受 sock：
    t 为 做 Tensor_创建 用 10。
    若 t 等于 0：
        返还 0。
    完。
    做 Tensor_写 用 t、0、NURP_接收总数。
    做 Tensor_写 用 t、1、NURP_字节总数。
    做 Tensor_写 用 t、2、NURP_丢包总数。
    做 Tensor_写 用 t、3、NURP_校验和错误。
    做 Tensor_写 用 t、4、NURP_超时总数。
    若 sock 不等于 空 并且 sock 的 接收缓冲控制 不等于 空：
        做 Tensor_写 用 t、5、sock 的 接收缓冲控制 的 当前大小。
        做 Tensor_写 用 t、6、sock 的 接收缓冲控制 的 最大大小。
        做 Tensor_写 用 t、7、sock 的 接收缓冲控制 的 队列长度。
    完。
    做 Tensor_写 用 t、8、NURP_组播接收。
    做 Tensor_写 用 t、9、NURP_广播接收。
    返还 t。
完。

NURP_AI预测缓冲大小 接受 sock：
    t 为 做 NURP_AI提取特征 用 sock。
    若 t 等于 0：
        返还 UDP_默认接收缓冲。
    完。
    总包 为 做 Tensor_读 用 t、0。
    丢包 为 做 Tensor_读 用 t、2。
    当前buf 为 做 Tensor_读 用 t、5。
    最大buf 为 做 Tensor_读 用 t、6。
    可变 建议 为 最大buf。
    若 总包 大于 100 并且 丢包 乘 100 除以 总包 大于 5：
        建议 为 最大buf 乘 3 除以 2。
    完。
    若 总包 大于 100 并且 当前buf 乘 100 除以 最大buf 小于 20：
        建议 为 最大buf 乘 4 除以 5。
    完。
    建议 为 做 限制范围_NURP 用 建议、65536、UDP_最大接收缓冲。
    做 全局AI总线 的 发布 用 「nurp_buffer」、「predict」、建议。
    做 Tensor_释放 用 t。
    NURP_AI预测总数 为 NURP_AI预测总数 加 1。
    返还 建议。
完。

限制范围_NURP 接受 v、最小、最大：
    若 v 小于 最小：
        返还 最小。
    完。
    若 v 大于 最大：
        返还 最大。
    完。
    返还 v。
完。

NURP_AI异常检测 接受 sock：
    t 为 做 NURP_AI提取特征 用 sock。
    若 t 等于 0：
        返还 0。
    完。
    异常 为 做 AI总线_异常检测 用 「nurp_recv」、t。
    做 Tensor_释放 用 t。
    返还 异常。
完。

NURP_获取统计 接受 零参：
    返还 一组：NURP_接收总数、NURP_字节总数、NURP_丢包总数、NURP_校验和错误、NURP_超时总数、NURP_组播接收、NURP_广播接收、NURP_AI预测总数。
完。

NURP_重置统计 接受 零参：
    NURP_接收总数 为 0。
    NURP_字节总数 为 0。
    NURP_丢包总数 为 0。
    NURP_校验和错误 为 0。
    NURP_超时总数 为 0。
    NURP_组播接收 为 0。
    NURP_广播接收 为 0。
    NURP_AI预测总数 为 0。
完。

NURP_打印socket状态 接受 sock：
    若 sock 等于 空：
        返还。
    完。
    做 日志 用 「[NURP] 端口=」、sock 的 本地端口、「 接收缓冲=」、sock 的 接收缓冲控制 的 当前大小、「/」、sock 的 接收缓冲控制 的 最大大小、「 队列=」、sock 的 接收缓冲控制 的 队列长度、「 组播组数=」、sock 的 组播数量。
完。

NURP_自检 接受 零参：
    伪 为 做 NURP_伪头校验和 用 0x0A000001、0x0A000002、17、16。
    若 伪 等于 0：
        返还 假。
    完。
    h1 为 做 udp_哈希 用 80。
    h2 为 做 udp_哈希 用 443。
    若 h1 等于 h2：
        返还 假。
    完。
    若 非 做 NURP_是组播地址 用 0xE0000001：
        返还 假。
    完。
    若 做 NURP_是组播地址 用 0x0A000001：
        返还 假。
    完。
    返还 真。
完。

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·同步原语》。

引用《..·中枢·进程信号》。

引用《..·推理·定点Q算术》。

引用《..·中枢·进程描述符》。

引用《..·中枢·字符串替换》。

引用《.·网络缓冲读》。

引用《.·网缓冲分配》。

引用《..·中枢·每处理器》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

udp_recv 接受 sock、缓冲、最大长度：
    返还 做 udp_recvfrom 用 sock、缓冲、最大长度、空。
完。

udp_recvfrom 接受 sock、缓冲、最大长度、源地址：
    只要 做 Nova网络缓冲_队列_为空 用 sock 的 接收缓冲：
        可变 等待项 为 做 等待项_创建 用 做 获取当前进程、真。
        做 等待队列_加入 用 sock 的 接收等待队列、等待项。
        做 进程_设置状态 用 做 获取当前进程、进程状态_可中断睡眠。
        做 调度。
        做 等待队列_移除 用 sock 的 接收等待队列、等待项。
        若 做 当前进程_有待处理信号：
            返还 错误码_被中断。
        完。
    完。
    做 自旋锁_获取 用 sock 的 锁。
    Nova缓冲 为 做 Nova网络缓冲_队列_出队 用 sock 的 接收缓冲。
    做 自旋锁_释放 用 sock 的 锁。
    若 Nova缓冲 等于 空：
        返还 0。
    完。
    复制长度 为 做 取小 用 Nova缓冲 的 数据长度、最大长度。
    做 内存复制 用 缓冲、Nova缓冲 的 数据、复制长度。
    若 源地址 不等于 空：
        源地址 的 IP 为 Nova缓冲 的 源IP。
        源地址 的 端口 为 Nova缓冲 的 源端口。
    完。
    做 Nova缓冲_释放 用 Nova缓冲。
    返还 复制长度。
完。

udp_查找socket 接受 本地IP、本地端口、远程IP、远程端口：
    桶号 为 做 udp_哈希 用 本地端口。
    可变 当前 为 全局数报哈希表 的 桶 的 第 桶号 项。
    可变 最佳 为 空。
    可变 最佳分数 为 负 1。
    只要 当前 不等于 空：
        若 当前 的 本地端口 等于 本地端口：
            可变 分数 为 0。
            若 当前 的 本地IP 不等于 0 并且 当前 的 本地IP 等于 本地IP：
                分数 为 分数 加 2。
            否则若 当前 的 本地IP 等于 0：
                分数 为 分数 加 1。
            否则：
                当前 为 当前 的 下一个。
                继续。
            完。
            若 当前 的 已连接：
                若 当前 的 远程IP 等于 远程IP 并且 当前 的 远程端口 等于 远程端口：
                    分数 为 分数 加 4。
                否则：
                    当前 为 当前 的 下一个。
                    继续。
                完。
            完。
            若 分数 大于 最佳分数：
                最佳分数 为 分数。
                最佳 为 当前。
            完。
        完。
        当前 为 当前 的 下一个。
    完。
    返还 最佳。
完。

udp_查找_本地 接受 端口、IP：
    桶号 为 做 udp_哈希 用 端口。
    可变 当前 为 全局数报哈希表 的 桶 的 第 桶号 项。
    只要 当前 不等于 空：
        若 当前 的 本地端口 等于 端口：
            若 IP 等于 0 或者 当前 的 本地IP 等于 0 或者 当前 的 本地IP 等于 IP：
                返还 当前。
            完。
        完。
        当前 为 当前 的 下一个。
    完。
    返还 空。
完。

本章 收束。
