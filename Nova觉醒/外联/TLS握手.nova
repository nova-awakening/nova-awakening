《TLS握手》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·加密框架》。

引用《..·执行·底层系统调用原语》。

引用《..·中枢·硬链接》。

引用《..·感知·内核直连层》。

引用《..·中枢·内核打印》。

引用《..·执行·内存管理封装》。

引用《..·执行·平台时间》。

引用《..·中枢·整数转字符串》。

引用《.·TLS数据收发》。

TLS12_VERIFY_DATA_LEN 为 12。

TLS12_MASTER_SECRET_LEN 为 48。

TLS12_记录类型_握手 为 22。

TLS12_记录类型_变更密码规范 为 20。

TLS12_握手类型_Finished 为 20。

TLS12_握手类型_NewSessionTicket 为 4。

TLS12_密码套件_AES128_GCM_SHA256 为 0x009C。

TLS12_密码套件_AES256_GCM_SHA384 为 0x009D。

TLS12_密码套件_CHACHA20_POLY1305 为 0xCCA8。

TLS12_密码套件_AES128_CBC_SHA256 为 0x003C。

TLS12_握手统计 是 一种 事物，含 发送Finished次数、验证Finished次数、验证失败次数、重协商次数、会话恢复次数、票据颁发次数。

可变 全局_握手统计 为 空。

TLS12_握手统计_初始化 接受 零参：
    若 全局_握手统计 等于 空：
        全局_握手统计 为 做 堆_分配 用 TLS12_握手统计。
        全局_握手统计 的 发送Finished次数 为 0。
        全局_握手统计 的 验证Finished次数 为 0。
        全局_握手统计 的 验证失败次数 为 0。
        全局_握手统计 的 重协商次数 为 0。
        全局_握手统计 的 会话恢复次数 为 0。
        全局_握手统计 的 票据颁发次数 为 0。
    完。
完。

安全12_发送Finished 接受 conn：
    做 TLS12_握手统计_初始化。
    verify_data 为 做 堆_分配 用 TLS12_VERIFY_DATA_LEN。
    若 verify_data 等于 0：
        返还 0。
    完。
    hs_hash 为 做 堆_分配 用 32。
    若 hs_hash 等于 0：
        返还 0。
    完。
    做 SHA256_哈希 用 conn 的 握手消息、conn 的 握手消息长度、hs_hash。
    标签 为 「client finished」。
    若 非 conn 的 是客户端：
        标签 为 「server finished」。
    完。
    做 TLS12_PRF_SHA256 用 conn 的 主密钥、TLS12_MASTER_SECRET_LEN、标签、hs_hash、32、verify_data、TLS12_VERIFY_DATA_LEN。
    buf 为 做 堆_分配 用 4 加 TLS12_VERIFY_DATA_LEN。
    若 buf 等于 0：
        返还 0。
    完。
    做 内存_写8 用 buf、TLS12_握手类型_Finished。
    做 内存_写8 用 buf 加 1、0。
    做 内存_写8 用 buf 加 2、0。
    做 内存_写8 用 buf 加 3、TLS12_VERIFY_DATA_LEN。
    做 内存_复制 用 buf 加 4、verify_data、TLS12_VERIFY_DATA_LEN。
    做 安全12_发送变更密码规范 用 conn。
    若 做 安全_发送记录 用 conn、TLS12_记录类型_握手、buf、4 加 TLS12_VERIFY_DATA_LEN 小于 0：
        做 堆_释放 用 buf。
        做 堆_释放 用 verify_data。
        做 堆_释放 用 hs_hash。
        返还 负 1。
    完。
    做 内存_复制 用 conn 的 本方verify_data、verify_data、TLS12_VERIFY_DATA_LEN。
    全局_握手统计 的 发送Finished次数 为 全局_握手统计 的 发送Finished次数 加 1。
    做 堆_释放 用 buf。
    做 堆_释放 用 verify_data。
    做 堆_释放 用 hs_hash。
完。

安全12_发送变更密码规范 接受 conn：
    ccs 为 做 堆_分配 用 1。
    若 ccs 等于 0：
        返还 0。
    完。
    做 内存_写8 用 ccs、1。
    做 安全_发送记录 用 conn、TLS12_记录类型_变更密码规范、ccs、1。
    做 堆_释放 用 ccs。
    conn 的 发送加密已启用 为 真。
完。

安全12_验证Finished 接受 conn、数据、长度：
    做 TLS12_握手统计_初始化。
    全局_握手统计 的 验证Finished次数 为 全局_握手统计 的 验证Finished次数 加 1。
    若 长度 小于 4 加 TLS12_VERIFY_DATA_LEN：
        conn 的 握手完成 为 假。
        全局_握手统计 的 验证失败次数 为 全局_握手统计 的 验证失败次数 加 1。
        返还 假。
    完。
    类型 为 做 内存_读8 用 数据。
    若 类型 不等于 TLS12_握手类型_Finished：
        conn 的 握手完成 为 假。
        全局_握手统计 的 验证失败次数 为 全局_握手统计 的 验证失败次数 加 1。
        返还 假。
    完。
    expected 为 做 堆_分配 用 TLS12_VERIFY_DATA_LEN。
    若 expected 等于 0：
        返还 0。
    完。
    hs_hash 为 做 堆_分配 用 32。
    若 hs_hash 等于 0：
        返还 0。
    完。
    做 SHA256_哈希 用 conn 的 握手消息、conn 的 握手消息长度、hs_hash。
    标签 为 「server finished」。
    若 非 conn 的 是客户端：
        标签 为 「client finished」。
    完。
    做 TLS12_PRF_SHA256 用 conn 的 主密钥、TLS12_MASTER_SECRET_LEN、标签、hs_hash、32、expected、TLS12_VERIFY_DATA_LEN。
    匹配 为 做 TLS12_常量时间比较 用 数据 加 4、expected、TLS12_VERIFY_DATA_LEN。
    若 非 匹配：
        conn 的 握手完成 为 假。
        全局_握手统计 的 验证失败次数 为 全局_握手统计 的 验证失败次数 加 1。
    否则：
        conn 的 握手完成 为 真。
        做 内存_复制 用 conn 的 对方verify_data、数据 加 4、TLS12_VERIFY_DATA_LEN。
    完。
    做 堆_释放 用 expected。
    做 堆_释放 用 hs_hash。
    返还 匹配。
完。

TLS12_常量时间比较 接受 a、b、长度：
    可变 差异 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        差异 为 差异 位或 做 内存_读8 用 a 加 i 位异或 做 内存_读8 用 b 加 i。
        i 为 i 加 1。
    完。
    返还 差异 等于 0。
完。

TLS12_PRF_SHA256 接受 密钥、密钥长度、标签、种子、种子长度、输出、输出长度：
    标签长度 为 做 字符串长度 用 标签。
    标签种子长度 为 标签长度 加 种子长度。
    标签种子 为 做 堆_分配 用 标签种子长度。
    若 标签种子 等于 0：
        返还 0。
    完。
    做 内存_复制 用 标签种子、标签、标签长度。
    做 内存_复制 用 标签种子 加 标签长度、种子、种子长度。
    A 为 做 堆_分配 用 32。
    若 A 等于 0：
        返还 0。
    完。
    临时 为 做 堆_分配 用 32 加 标签种子长度。
    若 临时 等于 0：
        返还 0。
    完。
    做 HMAC_SHA256 用 密钥、密钥长度、标签种子、标签种子长度、A。
    可变 已生成 为 0。
    只要 已生成 小于 输出长度：
        做 内存_复制 用 临时、A、32。
        做 内存_复制 用 临时 加 32、标签种子、标签种子长度。
        P 为 做 堆_分配 用 32。
        若 P 等于 0：
            返还 0。
        完。
        做 HMAC_SHA256 用 密钥、密钥长度、临时、32 加 标签种子长度、P。
        可复制 为 输出长度 减 已生成。
        若 可复制 大于 32：
            可复制 为 32。
        完。
        做 内存_复制 用 输出 加 已生成、P、可复制。
        已生成 为 已生成 加 可复制。
        做 HMAC_SHA256 用 密钥、密钥长度、A、32、A。
        做 堆_释放 用 P。
    完。
    做 堆_释放 用 A。
    做 堆_释放 用 临时。
    做 堆_释放 用 标签种子。
完。

TLS12_派生密钥材料 接受 conn：
    种子长度 为 64。
    种子 为 做 堆_分配 用 种子长度。
    若 种子 等于 0：
        返还 0。
    完。
    做 内存_复制 用 种子、conn 的 服务器随机数、32。
    做 内存_复制 用 种子 加 32、conn 的 客户端随机数、32。
    密钥块长度 为 做 TLS12_密钥块大小 用 conn 的 密码套件。
    密钥块 为 做 堆_分配 用 密钥块长度。
    若 密钥块 等于 0：
        返还 0。
    完。
    做 TLS12_PRF_SHA256 用 conn 的 主密钥、TLS12_MASTER_SECRET_LEN、「key expansion」、种子、种子长度、密钥块、密钥块长度。
    可变 偏移 为 0。
    mac大小 为 做 TLS12_MAC大小 用 conn 的 密码套件。
    密钥大小 为 做 TLS12_密钥大小 用 conn 的 密码套件。
    IV大小 为 做 TLS12_IV大小 用 conn 的 密码套件。
    若 mac大小 大于 0：
        做 内存_复制 用 conn 的 客户端MAC密钥、密钥块 加 偏移、mac大小。
        偏移 为 偏移 加 mac大小。
        做 内存_复制 用 conn 的 服务器MAC密钥、密钥块 加 偏移、mac大小。
        偏移 为 偏移 加 mac大小。
    完。
    做 内存_复制 用 conn 的 客户端加密密钥、密钥块 加 偏移、密钥大小。
    偏移 为 偏移 加 密钥大小。
    做 内存_复制 用 conn 的 服务器加密密钥、密钥块 加 偏移、密钥大小。
    偏移 为 偏移 加 密钥大小。
    做 内存_复制 用 conn 的 客户端IV、密钥块 加 偏移、IV大小。
    偏移 为 偏移 加 IV大小。
    做 内存_复制 用 conn 的 服务器IV、密钥块 加 偏移、IV大小。
    做 堆_释放 用 种子。
    做 内存_清零 用 密钥块、32。
    做 堆_释放 用 密钥块。
完。

TLS12_密钥块大小 接受 密码套件：
    若 密码套件 等于 TLS12_密码套件_AES128_GCM_SHA256：
        返还 72。
    完。
    若 密码套件 等于 TLS12_密码套件_AES256_GCM_SHA384：
        返还 104。
    完。
    若 密码套件 等于 TLS12_密码套件_CHACHA20_POLY1305：
        返还 88。
    完。
    若 密码套件 等于 TLS12_密码套件_AES128_CBC_SHA256：
        返还 128。
    完。
    返还 72。
完。

TLS12_密钥大小 接受 密码套件：
    若 密码套件 等于 TLS12_密码套件_AES128_GCM_SHA256：
        返还 16。
    完。
    若 密码套件 等于 TLS12_密码套件_AES256_GCM_SHA384：
        返还 32。
    完。
    若 密码套件 等于 TLS12_密码套件_CHACHA20_POLY1305：
        返还 32。
    完。
    若 密码套件 等于 TLS12_密码套件_AES128_CBC_SHA256：
        返还 16。
    完。
    返还 16。
完。

TLS12_MAC大小 接受 密码套件：
    若 密码套件 等于 TLS12_密码套件_AES128_GCM_SHA256：
        返还 0。
    完。
    若 密码套件 等于 TLS12_密码套件_AES256_GCM_SHA384：
        返还 0。
    完。
    若 密码套件 等于 TLS12_密码套件_CHACHA20_POLY1305：
        返还 0。
    完。
    若 密码套件 等于 TLS12_密码套件_AES128_CBC_SHA256：
        返还 32。
    完。
    返还 0。
完。

TLS12_IV大小 接受 密码套件：
    若 密码套件 等于 TLS12_密码套件_AES128_GCM_SHA256：
        返还 4。
    完。
    若 密码套件 等于 TLS12_密码套件_AES256_GCM_SHA384：
        返还 4。
    完。
    若 密码套件 等于 TLS12_密码套件_CHACHA20_POLY1305：
        返还 12。
    完。
    若 密码套件 等于 TLS12_密码套件_AES128_CBC_SHA256：
        返还 16。
    完。
    返还 4。
完。

安全12_处理会话票据 接受 conn、数据、长度：
    做 TLS12_握手统计_初始化。
    若 长度 小于 6：
        返还。
    完。
    生存时间 为 做 内存_读8 用 数据 左移 24 位或 做 内存_读8 用 数据 加 1 左移 16 位或 做 内存_读8 用 数据 加 2 左移 8 位或 做 内存_读8 用 数据 加 3。
    票据长度 为 做 内存_读8 用 数据 加 4 左移 8 位或 做 内存_读8 用 数据 加 5。
    若 票据长度 大于 0 并且 票据长度 加 6 小于或等于 长度：
        若 conn 的 会话票据 不等于 空：
            做 堆_释放 用 conn 的 会话票据。
        完。
        conn 的 会话票据 为 做 堆_分配 用 票据长度。
        做 内存_复制 用 conn 的 会话票据、数据 加 6、票据长度。
        conn 的 会话票据长度 为 票据长度。
        conn 的 票据生存时间 为 生存时间。
        全局_握手统计 的 票据颁发次数 为 全局_握手统计 的 票据颁发次数 加 1。
    完。
完。

安全12_检测重协商 接受 conn：
    做 TLS12_握手统计_初始化。
    若 conn 的 握手完成 并且 conn 的 收到ClientHello：
        全局_握手统计 的 重协商次数 为 全局_握手统计 的 重协商次数 加 1。
        若 conn 的 安全重协商扩展：
            返还 真。
        完。
        返还 假。
    完。
    返还 真。
完。

安全12_握手统计打印 接受 零参：
    做 TLS12_握手统计_初始化。
    做 打印行 用 「=== TLS 1.2 握手统计 ===」。
    做 打印行 用 「发送Finished: 」 加 做 整数转字符串 用 全局_握手统计 的 发送Finished次数。
    做 打印行 用 「验证Finished: 」 加 做 整数转字符串 用 全局_握手统计 的 验证Finished次数。
    做 打印行 用 「验证失败: 」 加 做 整数转字符串 用 全局_握手统计 的 验证失败次数。
    做 打印行 用 「重协商: 」 加 做 整数转字符串 用 全局_握手统计 的 重协商次数。
    做 打印行 用 「会话恢复: 」 加 做 整数转字符串 用 全局_握手统计 的 会话恢复次数。
    做 打印行 用 「票据颁发: 」 加 做 整数转字符串 用 全局_握手统计 的 票据颁发次数。
完。

平台_随机字节 接受 buf、len：
    SYS_getrandom 为 278。
    可变 已读 为 0。
    只要 已读 小于 len：
        ret 为 做 系统调用 用 SYS_getrandom、buf 加 已读、len 减 已读、0、0、0、0。
        若 ret 小于或等于 0：
            fd 为 做 系统调用_open 用 「/dev/urandom」、0。
            若 fd 大于或等于 0：
                rd 为 做 系统调用_read 用 fd、buf 加 已读、len 减 已读。
                做 系统调用_close 用 fd。
                若 rd 大于 0：
                    已读 为 已读 加 rd。
                完。
            完。
            返还。
        完。
        已读 为 已读 加 ret。
    完。
完。

平台_socket_发送 接受 fd、数据、长度：
    返还 做 系统调用_sendto 用 fd、数据、长度、0、0、0。
完。

平台_socket_接收 接受 fd、buf、最大：
    返还 做 系统调用_recvfrom 用 fd、buf、最大、0、0、0。
完。

可变 AI_安全发送完_带宽估计 为 0。

可变 AI_安全发送完_丢包窗口 为 一组：（空）。

AI_安全发送完_带宽预测 接受 发送字节、耗时毫秒：
    若 耗时毫秒 等于 0：
        返还 AI_安全发送完_带宽估计。
    完。
    当前 为 发送字节 乘 1000 除以 耗时毫秒。
    AI_安全发送完_带宽估计 为 AI_安全发送完_带宽估计 乘 700 除以 1000 加 当前 乘 300 除以 1000。
    返还 AI_安全发送完_带宽估计。
完。

AI_安全发送完_拥塞建议 接受 丢包率：
    做 追加 用 AI_安全发送完_丢包窗口、丢包率。
    若 做 计数 用 AI_安全发送完_丢包窗口 大于 10：
        做 列表_移除 用 AI_安全发送完_丢包窗口、0。
    完。
    若 丢包率 大于 50：
        返还 「减半窗口」。
    完。
    若 丢包率 小于 5：
        返还 「增大窗口」。
    完。
    返还 「保持」。
完。

本章 收束。
