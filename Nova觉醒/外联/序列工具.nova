《序列工具》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·硬链接》。

引用《.·URL解析辅助》。

引用《.·HTTP请求构造》。

引用《.·HTTP发送与解析》。

引用《..·中枢·内核打印》。

引用《..·中枢·数学工具》。

引用《..·中枢·整数转字符串》。

序列_构造对象 接受 键值对：
    可变 结果 为 「{」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键值对：
        若 i 大于 0：
            结果 为 结果 加 「,」。
        完。
        结果 为 结果 加 「\"」 加 键值对 的 第 i 项 的 第 0 项 加 「\":」 加 做 序列_编码值 用 键值对 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 结果 加 「}」。
完。

序列_编码值 接受 值：
    若 值 等于 空：
        返还 「null」。
    完。
    若 值 等于 真：
        返还 「true」。
    完。
    若 值 等于 假：
        返还 「false」。
    完。
    返还 「\"」 加 值 加 「\"」。
完。

序列_提取字段 接受 json、字段名：
    搜索 为 「\"」 加 字段名 加 「\":」。
    位置 为 做 字符串查找 用 json、搜索。
    若 位置 小于 0：
        返还 「」。
    完。
    值开始 为 位置 加 做 字符串长度 用 搜索。
    只要 值开始 小于 做 字符串长度 用 json 并且 做 字符串取字节 用 json、值开始 等于 32：
        值开始 为 值开始 加 1。
    完。
    若 值开始 大于或等于 做 字符串长度 用 json：
        返还 「」。
    完。
    若 做 字符串取字节 用 json、值开始 等于 34：
        值结束 为 值开始 加 1。
        只要 值结束 小于 做 字符串长度 用 json 并且 做 字符串取字节 用 json、值结束 不等于 34：
            值结束 为 值结束 加 1。
        完。
        返还 做 子字符串 用 json、值开始 加 1、值结束。
    否则：
        值结束 为 值开始。
        只要 值结束 小于 做 字符串长度 用 json 并且 做 字符串取字节 用 json、值结束 不等于 44 并且 做 字符串取字节 用 json、值结束 不等于 125：
            值结束 为 值结束 加 1。
        完。
        返还 做 子字符串 用 json、值开始、值结束。
    完。
完。

接口类型_Nova智能 为 0。

接口类型_Anthropic 为 1。

接口类型_本地 为 2。

智能_接口配置 是 一种 事物，含 类型、基础URL、接口密钥、模型名、最大令牌、温度。

可变 智能_接口_默认配置 为 空。

可变 智能_接口_调用总数 为 0。

可变 智能_接口_成功总数 为 0。

智能_接口_初始化 接受 类型、基础URL、接口密钥、模型名：
    config 为 新建 智能_接口配置。
    config 的 类型 为 类型。
    config 的 基础URL 为 基础URL。
    config 的 接口密钥 为 接口密钥。
    config 的 模型名 为 模型名。
    config 的 最大令牌 为 4096。
    config 的 温度 为 700。
    智能_接口_默认配置 为 config。
    做 内核信息 用 「AI-接口: 初始化 类型=」 加 做 整数转字符串 用 类型 加 「 模型=」 加 模型名 加 「 URL=」 加 基础URL。
    返还 config。
完。

智能_接口_聊天 接受 消息列表：
    若 智能_接口_默认配置 等于 空：
        做 内核错误 用 「AI-接口: 未初始化配置」。
        返还 空。
    完。
    config 为 智能_接口_默认配置。
    智能_接口_调用总数 为 智能_接口_调用总数 加 1。
    可变 消息json 为 「[」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 消息列表：
        若 i 大于 0：
            消息json 为 消息json 加 「,」。
        完。
        消息json 为 消息json 加 「{\"role\":\"」 加 消息列表 的 第 i 项 的 第 0 项 加 「\",\"content\":\"」 加 消息列表 的 第 i 项 的 第 1 项 加 「\"}」。
        i 为 i 加 1。
    完。
    消息json 为 消息json 加 「]」。
    正文 为 「{\"model\":\"」 加 config 的 模型名 加 「\",\"messages\":」 加 消息json 加 「,\"max_令牌s\":」 加 做 整数转字符串 用 config 的 最大令牌 加 「,\"temperature\":」 加 做 数学_千分位转小数字符串 用 config 的 温度 加 「}」。
    端点 为 config 的 基础URL 加 「/v1/chat/completions」。
    若 config 的 类型 等于 接口类型_Anthropic：
        端点 为 config 的 基础URL 加 「/v1/messages」。
    完。
    req 为 做 HTTP_创建请求 用 HTTP_POST、端点。
    做 HTTP_设序列正文 用 req、正文。
    做 HTTP_设授权 用 req、config 的 接口密钥。
    响应 为 做 HTTP_发送_带重试 用 req。
    若 响应 的 成功：
        智能_接口_成功总数 为 智能_接口_成功总数 加 1。
        内容 为 做 序列_提取字段 用 响应 的 正文、「content」。
        返还 内容。
    完。
    做 内核调试 用 「AI-接口: 请求失败 状态=」 加 做 整数转字符串 用 响应 的 状态码。
    返还 空。
完。

智能_接口_推理 接受 输入文本：
    消息 为 一组：一组：「user」、输入文本。
    返还 做 智能_接口_聊天 用 消息。
完。

智能_接口_流式推理 接受 输入文本、回调函数：
    若 智能_接口_默认配置 等于 空：
        返还 假。
    完。
    config 为 智能_接口_默认配置。
    智能_接口_调用总数 为 智能_接口_调用总数 加 1。
    正文 为 「{\"model\":\"」 加 config 的 模型名 加 「\",\"messages\":[{\"role\":\"user\",\"content\":\"」 加 输入文本 加 「\"}]」 加 「,\"max_令牌s\":」 加 做 整数转字符串 用 config 的 最大令牌 加 「,\"stream\":true}」。
    req 为 做 HTTP_创建请求 用 HTTP_POST、config 的 基础URL 加 「/v1/chat/completions」。
    做 HTTP_设序列正文 用 req、正文。
    做 HTTP_设授权 用 req、config 的 接口密钥。
    做 HTTP_添加头部 用 req、「Accept」、「text/事件-stream」。
    响应 为 做 HTTP_发送 用 req。
    若 响应 的 成功：
        智能_接口_成功总数 为 智能_接口_成功总数 加 1。
    完。
    返还 响应 的 成功。
完。

本章 收束。
