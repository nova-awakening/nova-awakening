《应用超传》

引用《.·TCP会话记录》。

引用《.·网络子系统入口》。

引用《..·标准·原生字符串》。

引用《..·中枢·硬链接》。

引用《..·中枢·整数转字符串》。

超传档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、头数、帧数、流数、发送数、推送数、预取数、解码数、成功数、失败数、最近流号、最近路径、最近主机。

超传请求 是 一种 事物，含 动作、目标、参数、选项。

超传回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

超传摘要 是 一种 事物，含 名称、头数、帧数、流数、发送数、推送数、预取数、解码数、成功数、失败数、健康。

超传头 是 一种 事物，含 名称、值。

超传帧 是 一种 事物，含 类型、流号、标志、载荷。

可变 超传账本 为 一组：（空）。

可变 超传构建数 为 0。

可变 超传总动作 为 0。

可变 超传总成功 为 0。

可变 超传总失败 为 0。

应用超传_建档 接受 名称：
    档 为 新建 超传档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 头数 为 0。
    档 的 帧数 为 0。
    档 的 流数 为 0。
    档 的 发送数 为 0。
    档 的 推送数 为 0。
    档 的 预取数 为 0。
    档 的 解码数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    档 的 最近流号 为 1。
    档 的 最近路径 为 「」。
    档 的 最近主机 为 「」。
    返还 档。
完。

应用超传_登记 接受 档：
    做 追加 用 超传账本、档。
    超传构建数 为 超传构建数 加 1。
    返还 档。
完。

应用超传_完整统计 接受 零参：
    返还 一组：超传构建数、超传总动作、超传总成功、超传总失败。
完。

应用超传_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 超传回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

应用超传_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 帧数 大于 0 或者 档 的 发送数 大于 0 或者 档 的 解码数 大于 0：
        档 的 最近健康 为 「运行」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「空载」。
    返还 档 的 最近健康。
完。

应用超传_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    档 的 最近错误 为 「」。
    档 的 最近统计 为 做 应用超传_完整统计。
完。

应用超传_成功 接受 档、来源、数据：
    超传总动作 为 超传总动作 加 1。
    超传总成功 为 超传总成功 加 1。
    若 档 等于 空：
        返还 做 应用超传_建回证 用 真、真、来源、「」、数据、做 应用超传_完整统计。
    完。
    档 的 最近来源 为 来源。
    档 的 最近结果 为 数据。
    档 的 最近错误 为 「」。
    档 的 成功数 为 档 的 成功数 加 1。
    档 的 最近统计 为 做 应用超传_完整统计。
    做 应用超传_刷新健康 用 档。
    返还 做 应用超传_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

应用超传_失败 接受 档、来源、错因：
    超传总动作 为 超传总动作 加 1。
    超传总失败 为 超传总失败 加 1。
    若 档 等于 空：
        返还 做 应用超传_建回证 用 真、假、来源、错因、0、做 应用超传_完整统计。
    完。
    档 的 最近来源 为 来源。
    档 的 最近错误 为 错因。
    档 的 失败数 为 档 的 失败数 加 1。
    档 的 最近统计 为 做 应用超传_完整统计。
    做 应用超传_刷新健康 用 档。
    返还 做 应用超传_建回证 用 真、假、来源、错因、0、档 的 最近统计。
完。

应用超传_初始化 接受 档：
    做 网络网络_创建 用 「超传网络」、0。
    做 传输传输_创建 用 「超传传输」。
    档 的 是否初始化 为 真。
    档 的 最近动作 为 「初始化」。
    档 的 最近来源 为 「应用超传_初始化」。
    档 的 最近统计 为 做 应用超传_完整统计。
    做 应用超传_刷新健康 用 档。
    返还 档。
完。

应用超传_创建 接受 名称：
    档 为 做 应用超传_建档 用 名称。
    做 应用超传_初始化 用 档。
    返还 做 应用超传_登记 用 档。
完。

应用超传_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 非 档 的 是否初始化：
        返还 假。
    完。
    返还 真。
完。

应用超传_建头 接受 名称、值：
    头 为 新建 超传头。
    头 的 名称 为 名称。
    头 的 值 为 值。
    返还 头。
完。

应用超传_建帧 接受 类型、流号、标志、载荷：
    帧 为 新建 超传帧。
    帧 的 类型 为 类型。
    帧 的 流号 为 流号。
    帧 的 标志 为 标志。
    帧 的 载荷 为 载荷。
    返还 帧。
完。

应用超传_静态表 接受 零参：
    返还 一组：做 应用超传_建头 用 「:method」、「GET」、做 应用超传_建头 用 「:method」、「POST」、做 应用超传_建头 用 「:path」、「/」、做 应用超传_建头 用 「:scheme」、「https」、做 应用超传_建头 用 「:authority」、「」、做 应用超传_建头 用 「content-type」、「text/html; charset=utf-8」、做 应用超传_建头 用 「accept」、「*/*」、做 应用超传_建头 用 「user-agent」、「AwakePure/1.0」。
完。

应用超传_头串 接受 头列：
    建 为 做 字符串构建器_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 头列：
        若 i 大于 0：
            做 字符串构建器_追加 用 建、「\n」。
        完。
        做 字符串构建器_追加 用 建、头列 的 第 i 项 的 名称。
        做 字符串构建器_追加 用 建、「: 」。
        做 字符串构建器_追加 用 建、头列 的 第 i 项 的 值。
        i 为 i 加 1。
    完。
    返还 做 字符串构建器_构建 用 建。
完。

应用超传_解码整数 接受 前缀值、续值列：
    结果 为 前缀值。
    可变 i 为 0。
    可变 乘 为 1。
    只要 i 小于 做 计数 用 续值列：
        结果 为 结果 加 续值列 的 第 i 项 乘 乘。
        乘 为 乘 乘 128。
        i 为 i 加 1。
    完。
    返还 结果。
完。

应用超传_头部解码 接受 档、名称列、值列：
    若 非 做 应用超传_检查档 用 档：
        返还 做 应用超传_失败 用 档、「头部解码」、「超传档不可用」。
    完。
    做 应用超传_记录 用 档、「头部解码」、一组：名称列、值列、「头压缩」。
    结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 名称列：
        值 为 「」。
        若 i 小于 做 计数 用 值列：
            值 为 值列 的 第 i 项。
        完。
        做 追加 用 结果、做 应用超传_建头 用 名称列 的 第 i 项、值。
        i 为 i 加 1。
    完。
    档 的 解码数 为 档 的 解码数 加 1。
    档 的 头数 为 档 的 头数 加 做 计数 用 结果。
    返还 做 应用超传_成功 用 档、「头部解码」、结果。
完。

应用超传_头块 接受 档、方法、路径、主机、类型：
    若 非 做 应用超传_检查档 用 档：
        返还 做 应用超传_失败 用 档、「头块」、「超传档不可用」。
    完。
    做 应用超传_记录 用 档、「头块」、一组：方法、路径、主机、类型、「头压缩」。
    头列 为 一组：做 应用超传_建头 用 「:method」、方法、做 应用超传_建头 用 「:path」、路径、做 应用超传_建头 用 「:scheme」、「https」、做 应用超传_建头 用 「:authority」、主机、做 应用超传_建头 用 「content-type」、类型、做 应用超传_建头 用 「accept」、「*/*」。
    档 的 头数 为 档 的 头数 加 做 计数 用 头列。
    档 的 最近路径 为 路径。
    档 的 最近主机 为 主机。
    返还 做 应用超传_成功 用 档、「头块」、头列。
完。

应用超传_帧编码 接受 档、类型、流号、标志、载荷：
    若 非 做 应用超传_检查档 用 档：
        返还 做 应用超传_失败 用 档、「帧编码」、「超传档不可用」。
    完。
    做 应用超传_记录 用 档、「帧编码」、一组：类型、流号、标志、载荷、「帧处理」。
    帧 为 做 应用超传_建帧 用 类型、流号、标志、载荷。
    档 的 帧数 为 档 的 帧数 加 1。
    档 的 最近流号 为 流号。
    返还 做 应用超传_成功 用 档、「帧编码」、帧。
完。

应用超传_头帧 接受 档、流号、头列、结束流：
    若 非 做 应用超传_检查档 用 档：
        返还 做 应用超传_失败 用 档、「头帧」、「超传档不可用」。
    完。
    做 应用超传_记录 用 档、「头帧」、一组：流号、头列、结束流、「帧处理」。
    标志 为 「END_HEADERS」。
    若 结束流：
        标志 为 标志 加 「|END_STREAM」。
    完。
    载荷 为 做 应用超传_头串 用 头列。
    档 的 帧数 为 档 的 帧数 加 1。
    档 的 头数 为 档 的 头数 加 做 计数 用 头列。
    档 的 最近流号 为 流号。
    返还 做 应用超传_成功 用 档、「头帧」、做 应用超传_建帧 用 「HEADERS」、流号、标志、载荷。
完。

应用超传_数据帧 接受 档、流号、数据、结束流：
    若 非 做 应用超传_检查档 用 档：
        返还 做 应用超传_失败 用 档、「数据帧」、「超传档不可用」。
    完。
    做 应用超传_记录 用 档、「数据帧」、一组：流号、数据、结束流、「帧处理」。
    标志 为 「」。
    若 结束流：
        标志 为 「END_STREAM」。
    完。
    档 的 帧数 为 档 的 帧数 加 1。
    档 的 最近流号 为 流号。
    返还 做 应用超传_成功 用 档、「数据帧」、做 应用超传_建帧 用 「DATA」、流号、标志、数据。
完。

应用超传_推送承诺 接受 档、流号、承诺流号、头列：
    若 非 做 应用超传_检查档 用 档：
        返还 做 应用超传_失败 用 档、「推送承诺」、「超传档不可用」。
    完。
    做 应用超传_记录 用 档、「推送承诺」、一组：流号、承诺流号、头列、「帧处理」。
    载荷 为 「promise=」 加 做 整数转字符串 用 承诺流号 加 「\n」 加 做 应用超传_头串 用 头列。
    档 的 推送数 为 档 的 推送数 加 1。
    档 的 帧数 为 档 的 帧数 加 1。
    档 的 流数 为 档 的 流数 加 1。
    档 的 最近流号 为 承诺流号。
    返还 做 应用超传_成功 用 档、「推送承诺」、做 应用超传_建帧 用 「PUSH_PROMISE」、流号、「END_HEADERS」、载荷。
完。

应用超传_发请求 接受 档、方法、路径、主机、体：
    若 非 做 应用超传_检查档 用 档：
        返还 做 应用超传_失败 用 档、「发请求」、「超传档不可用」。
    完。
    做 应用超传_记录 用 档、「发请求」、一组：方法、路径、主机、体、「超传二」。
    流号 为 档 的 最近流号。
    头列 为 做 应用超传_头块 用 档、方法、路径、主机、「application/json」 的 数据。
    头帧 为 做 应用超传_头帧 用 档、流号、头列、做 字符串长度 用 体 等于 0 的 数据。
    数据帧 为 0。
    若 做 字符串长度 用 体 大于 0：
        数据帧 为 做 应用超传_数据帧 用 档、流号、体、真 的 数据。
    完。
    档 的 发送数 为 档 的 发送数 加 1。
    档 的 流数 为 档 的 流数 加 1。
    档 的 最近路径 为 路径。
    档 的 最近主机 为 主机。
    档 的 最近流号 为 流号 加 2。
    返还 做 应用超传_成功 用 档、「发请求」、一组：头帧、数据帧。
完。

应用超传_预取命中 接受 档、路径、权重、命中：
    若 非 做 应用超传_检查档 用 档：
        返还 做 应用超传_失败 用 档、「预取命中」、「超传档不可用」。
    完。
    做 应用超传_记录 用 档、「预取命中」、一组：路径、权重、命中、「智能预取」。
    档 的 预取数 为 档 的 预取数 加 1。
    档 的 最近路径 为 路径。
    返还 做 应用超传_成功 用 档、「预取命中」、一组：路径、权重、命中。
完。

应用超传_静态表项 接受 档、索引：
    若 非 做 应用超传_检查档 用 档：
        返还 做 应用超传_失败 用 档、「静态表项」、「超传档不可用」。
    完。
    做 应用超传_记录 用 档、「静态表项」、一组：索引、「头压缩」。
    表 为 做 应用超传_静态表。
    若 索引 小于 0 或者 索引 大于或等于 做 计数 用 表：
        返还 做 应用超传_失败 用 档、「静态表项」、「索引越界」。
    完。
    档 的 解码数 为 档 的 解码数 加 1。
    返还 做 应用超传_成功 用 档、「静态表项」、表 的 第 索引 项。
完。

应用超传_摘要 接受 档：
    摘要 为 新建 超传摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 头数 为 档 的 头数。
    摘要 的 帧数 为 档 的 帧数。
    摘要 的 流数 为 档 的 流数。
    摘要 的 发送数 为 档 的 发送数。
    摘要 的 推送数 为 档 的 推送数。
    摘要 的 预取数 为 档 的 预取数。
    摘要 的 解码数 为 档 的 解码数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 应用超传_刷新健康 用 档。
    返还 摘要。
完。

应用超传_自检 接受 档：
    若 非 做 应用超传_检查档 用 档：
        返还 假。
    完。
    若 做 应用超传_静态表项 用 档、0 的 已成 等于 假：
        返还 假。
    完。
    若 做 应用超传_头部解码 用 档、一组：「:method」、「:path」、一组：「GET」、「/」 的 已成 等于 假：
        返还 假。
    完。
    若 做 应用超传_帧编码 用 档、「SETTINGS」、0、「ACK」、「」 的 已成 等于 假：
        返还 假。
    完。
    若 做 应用超传_发请求 用 档、「GET」、「/pure」、「pure.local」、「」 的 已成 等于 假：
        返还 假。
    完。
    若 做 应用超传_推送承诺 用 档、1、2、一组：做 应用超传_建头 用 「:path」、「/style.css」 的 已成 等于 假：
        返还 假。
    完。
    若 做 应用超传_预取命中 用 档、「/script.js」、92、真 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

应用超传_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 应用超传_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        做 应用超传_初始化 用 档。
        返还 做 应用超传_成功 用 档、「处理」、「已初始化」。
    完。
    若 请求 的 动作 等于 「静态表项」：
        返还 做 应用超传_静态表项 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「解码整数」：
        返还 做 应用超传_成功 用 档、「解码整数」、做 应用超传_解码整数 用 请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「头部解码」：
        返还 做 应用超传_头部解码 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「头块」：
        返还 做 应用超传_头块 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「帧编码」：
        返还 做 应用超传_帧编码 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「头帧」：
        返还 做 应用超传_头帧 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「数据帧」：
        返还 做 应用超传_数据帧 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「推送承诺」：
        返还 做 应用超传_推送承诺 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「发请求」：
        返还 做 应用超传_发请求 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「预取命中」：
        返还 做 应用超传_预取命中 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「摘要」：
        返还 做 应用超传_成功 用 档、「摘要」、做 应用超传_摘要 用 档。
    完。
    返还 做 应用超传_失败 用 档、「处理」、「未知动作」。
完。

可变 AI_应用超传_调用数 为 0。

可变 AI_应用超传_异常数 为 0。

AI_应用超传_监控 接受 零参：
    AI_应用超传_调用数 为 AI_应用超传_调用数 加 1。
    返还 AI_应用超传_异常数 等于 0。
完。

AI_应用超传_健康检查 接受 零参：
    若 AI_应用超传_调用数 大于 0 并且 AI_应用超传_异常数 乘 100 除以 AI_应用超传_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
