《TLS创建》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

安全_版本_12 为 0x0303。

安全_版本_13 为 0x0304。

安全_改变密码规格 为 20。

安全_警报 为 21。

安全_握手 为 22。

安全_应用数据 为 23。

HS_ClientHello 为 1。

HS_ServerHello 为 2。

HS_证书 为 11。

HS_密钥交换 为 12。

HS_证书验证 为 15。

HS_完成 为 20。

安全_AES_128_GCM_SHA256 为 0x1301。

安全_AES_256_GCM_SHA384 为 0x1302。

安全_CHACHA20_POLY1305 为 0x1303。

X25519_组 为 0x001D。

安全_状态_初始 为 0。

安全_状态_握手中 为 1。

安全_状态_等待服务器 为 2。

安全_状态_已建立 为 3。

安全_状态_关闭中 为 4。

安全_状态_错误 为 5。

安全连接 是 一种 事物，含 socket_fd、状态、版本、密码套件、客户端随机数、服务端随机数、预主密钥、主密钥、客户端写密钥、服务端写密钥、客户端写IV、服务端写IV、发送序号、接收序号、私钥、公钥、对方公钥、服务端证书、已验证、主机名。

安全_创建 接受 socket_fd、主机名：
    tls 为 新建 安全连接。
    tls 的 socket_fd 为 socket_fd。
    tls 的 状态 为 安全_状态_初始。
    tls 的 版本 为 安全_版本_13。
    tls 的 密码套件 为 安全_AES_128_GCM_SHA256。
    tls 的 主机名 为 主机名。
    tls 的 发送序号 为 0。
    tls 的 接收序号 为 0。
    tls 的 已验证 为 假。
    tls 的 客户端随机数 为 做 生成随机字节 用 32。
    tls 的 私钥 为 做 生成随机字节 用 32。
    tls 的 公钥 为 做 X25519_基点乘 用 tls 的 私钥。
    返还 tls。
完。

安全_握手 接受 tls：
    若 tls 等于 空：
        返还 假。
    完。
    tls 的 状态 为 安全_状态_握手中。
完。

本章 收束。
