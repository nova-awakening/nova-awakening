《网络设备分配》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·网络缓冲读》。

引用《..·中枢·哈希函数》。

引用《.·网络设备启停》。

引用《..·中枢·字符串替换》。

网络设备_最大数量 为 32。

网络设备_名称最大长度 为 16。

网络设备_MAC长度 为 6。

网络设备_MTU_默认 为 1500。

网络设备_MTU_最小 为 68。

网络设备_MTU_最大 为 65535。

网络设备_发送队列长度 为 1000。

IFF_UP 为 0x0001。

IFF_BROADCAST 为 0x0002。

IFF_LOOPBACK 为 0x0008。

IFF_POINTOPOINT 为 0x0010。

IFF_RUNNING 为 0x0040。

IFF_NOARP 为 0x0080。

IFF_PROMISC 为 0x0100。

IFF_MULTICAST 为 0x1000。

网络设备_状态_已注册 为 0x01。

网络设备_状态_已启动 为 0x02。

网络设备_状态_已连接 为 0x04。

网络设备操作 是 一种 事物，含 打开、停止、发送、设置MAC、设置MTU、Nova设备控制、获取统计、设置混杂模式。

网络设备统计 是 一种 事物，含 接收字节、发送字节、接收包数、发送包数、接收错误、发送错误、接收丢弃、发送丢弃、多播包数、碰撞次数。

网络设备 是 一种 事物，含 名称、索引、mac地址、ip地址、子网掩码、网关、mtu、标志、状态、操作、统计、发送队列、发送队列长度、私有数据、锁。

可变 网络设备表 为 做 数组 用 做 指针 用 网络设备、32。

可变 网络设备列表 为 一组：（空）。

可变 网络设备数量 为 0。

网络设备_分配 接受 名称、操作表、私有数据大小：
    可变 dev 为 做 分配内存 用 做 大小 用 网络设备。
    若 dev 等于 空：
        返还 空。
    完。
    dev 的 名称 为 名称。
    dev 的 索引 为 0。
    dev 的 mtu 为 网络设备_MTU_默认。
    dev 的 标志 为 IFF_BROADCAST 位或 IFF_MULTICAST。
    dev 的 状态 为 0。
    dev 的 操作 为 操作表。
    dev 的 统计 为 新建 网络设备统计。
    dev 的 发送队列 为 做 Nova网络缓冲_队列_创建。
    dev 的 发送队列长度 为 网络设备_发送队列长度。
    若 私有数据大小 大于 0：
        dev 的 私有数据 为 做 分配内存 用 私有数据大小。
    完。
    返还 dev。
完。

网络设备_注册 接受 dev：
    若 网络设备数量 大于或等于 网络设备_最大数量：
        返还 错误码_无空间。
    完。
    dev 的 索引 为 网络设备数量 加 1。
    dev 的 状态 为 dev 的 状态 位或 网络设备_状态_已注册。
    网络设备表 的 第 网络设备数量 项 为 dev。
    网络设备数量 为 网络设备数量 加 1。
    做 内核信息 用 「网络设备注册: %s (索引=%d MTU=%d)\n」、dev 的 名称、dev 的 索引、dev 的 mtu。
    返还 0。
完。

网络设备_注册_简单 接受 名称、发送函数、MAC地址：
    可变 操作 为 新建 网络设备操作。
    操作 的 发送 为 发送函数。
    dev 为 做 网络设备_分配 用 名称、操作、0。
    若 dev 等于 空：
        返还 错误码_内存不足。
    完。
    做 内存复制 用 dev 的 mac地址、MAC地址、网络设备_MAC长度。
    返还 做 网络设备_注册 用 dev。
完。

网络设备_注销 接受 dev：
    若 dev 的 状态 位与 网络设备_状态_已启动：
        做 网络设备_停止 用 dev。
    完。
    可变 i 为 0。
    只要 i 小于 网络设备数量：
        若 网络设备表 的 第 i 项 等于 dev：
            网络设备表 的 第 i 项 为 空。
            跳出。
        完。
        i 为 i 加 1。
    完。
    dev 的 状态 为 dev 的 状态 位与 按位取反 网络设备_状态_已注册。
    做 内核信息 用 「网络设备注销: %s\n」、dev 的 名称。
完。

可变 AI_网络设备分_运行指标 为 0。

可变 AI_网络设备分_异常指标 为 0。

AI_网络设备分_自诊断 接受 零参：
    AI_网络设备分_运行指标 为 网络设备数量。
    返还 AI_网络设备分_运行指标。
完。

AI_网络设备分_优化建议 接受 零参：
    指标 为 做 AI_网络设备分_自诊断。
    若 AI_网络设备分_运行指标 大于 10000：
        返还 「网络设备分负载较高」。
    完。
    返还 「网络设备分状态正常」。
完。

本章 收束。
