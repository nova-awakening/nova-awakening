《ARP地址解析》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·墙钟设置》。

ARP_硬件类型_以太网 为 1。

ARP_协议类型_IPv4 为 0x0800。

ARP_操作_请求 为 1。

ARP_操作_应答 为 2。

ARP_硬件地址长度 为 6。

ARP_协议地址长度 为 4。

ARP_包_大小 为 28。

以太网类型_ARP 为 0x0806。

ARP_状态_无效 为 0。

ARP_状态_不完整 为 1。

ARP_状态_可达 为 2。

ARP_状态_过期 为 3。

ARP_状态_探测 为 4。

ARP_状态_失败 为 5。

ARP_状态_永久 为 6。

ARP_缓存_超时 为 300000。

ARP_请求_超时 为 1000。

ARP_请求_最大重试 为 3。

ARP_缓存_桶数 为 256。

ARP包 是 一种 事物，含 硬件类型、协议类型、硬件地址长度、协议地址长度、操作、发送方MAC、发送方IP、目标MAC、目标IP。

ARP缓存条目 是 一种 事物，含 IP地址、MAC地址、状态、创建时间、最后确认时间、重试次数、等待队列、下一个、设备。

ARP缓存表 是 一种 事物，含 桶、条目数、锁。

可变 全局ARP缓存 为 新建 ARP缓存表。

可变 ARP缓存列表 为 一组：（空）。

arp_哈希 接受 ip：
    返还 (ip 位异或 ip 右移 16 位异或 ip 右移 8) 取余于 ARP_缓存_桶数。
完。

arp_查找 接受 IP地址：
    桶号 为 做 arp_哈希 用 IP地址。
    可变 当前 为 全局ARP缓存 的 桶 的 第 桶号 项。
    只要 当前 不等于 空：
        若 当前 的 IP地址 等于 IP地址：
            若 当前 的 状态 等于 ARP_状态_可达 或者 当前 的 状态 等于 ARP_状态_永久：
                若 当前 的 状态 不等于 ARP_状态_永久：
                    年龄 为 做 当前时间_秒 减 当前 的 最后确认时间。
                    若 年龄 大于 ARP_缓存_超时：
                        当前 的 状态 为 ARP_状态_过期。
                        返还 空。
                    完。
                完。
                返还 当前 的 MAC地址。
            完。
            返还 空。
        完。
        当前 为 当前 的 下一个。
    完。
    返还 空。
完。

arp_查找条目 接受 IP地址：
    桶号 为 做 arp_哈希 用 IP地址。
    可变 当前 为 全局ARP缓存 的 桶 的 第 桶号 项。
    只要 当前 不等于 空：
        若 当前 的 IP地址 等于 IP地址：
            返还 当前。
        完。
        当前 为 当前 的 下一个。
    完。
    返还 空。
完。

本章 收束。
