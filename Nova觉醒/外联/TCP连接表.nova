《TCP连接表》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·TCP拥塞控制》。

引用《.·TCP定时器》。

引用《.·网络缓冲读》。

引用《.·TCP关闭》。

引用《.·TCP四元组》。

引用《.·网缓冲分配》。

引用《..·中枢·同步原语》。

tcp_连接表_插入_已建立 接受 tcb：
    桶号 为 做 tcp_已建立_哈希桶 用 tcb 的 四元组。
    做 自旋锁_获取 用 全局传控连接表 的 锁。
    tcb 的 下一个 为 全局传控连接表 的 已建立 的 第 桶号 项 的 头。
    全局传控连接表 的 已建立 的 第 桶号 项 的 头 为 tcb。
    全局传控连接表 的 已建立 的 第 桶号 项 的 数量 为 全局传控连接表 的 已建立 的 第 桶号 项 的 数量 加 1。
    全局传控连接表 的 连接总数 为 全局传控连接表 的 连接总数 加 1。
    做 自旋锁_释放 用 全局传控连接表 的 锁。
完。

tcp_连接表_查找 接受 四元组：
    桶号 为 做 tcp_已建立_哈希桶 用 四元组。
    可变 当前 为 全局传控连接表 的 已建立 的 第 桶号 项 的 头。
    只要 当前 不等于 空：
        若 当前 的 四元组 的 源IP 等于 四元组 的 源IP 并且 当前 的 四元组 的 源端口 等于 四元组 的 源端口 并且 当前 的 四元组 的 目标IP 等于 四元组 的 目标IP 并且 当前 的 四元组 的 目标端口 等于 四元组 的 目标端口：
            返还 当前。
        完。
        当前 为 当前 的 下一个。
    完。
    返还 空。
完。

tcp_连接表_查找监听 接受 端口：
    桶号 为 做 tcp_监听_哈希桶 用 端口。
    可变 当前 为 全局传控连接表 的 监听 的 第 桶号 项 的 头。
    只要 当前 不等于 空：
        若 当前 的 四元组 的 源端口 等于 端口：
            返还 当前。
        完。
        当前 为 当前 的 下一个。
    完。
    返还 空。
完。

tcp_连接表_移除 接受 tcb：
    桶号 为 做 tcp_已建立_哈希桶 用 tcb 的 四元组。
    做 自旋锁_获取 用 全局传控连接表 的 锁。
    可变 前一个 为 空。
    可变 当前 为 全局传控连接表 的 已建立 的 第 桶号 项 的 头。
    只要 当前 不等于 空：
        若 当前 等于 tcb：
            若 前一个 等于 空：
                全局传控连接表 的 已建立 的 第 桶号 项 的 头 为 当前 的 下一个。
            否则：
                前一个 的 下一个 为 当前 的 下一个。
            完。
            全局传控连接表 的 已建立 的 第 桶号 项 的 数量 为 全局传控连接表 的 已建立 的 第 桶号 项 的 数量 减 1。
            全局传控连接表 的 连接总数 为 全局传控连接表 的 连接总数 减 1。
            跳出。
        完。
        前一个 为 当前。
        当前 为 当前 的 下一个。
    完。
    做 自旋锁_释放 用 全局传控连接表 的 锁。
完。

tcp_连接 接受 sk、目标IP、目标端口：
    可变 tcb 为 sk 的 tcp控制块。
    若 tcb 的 状态 不等于 传控_状态_关闭：
        返还 错误码_已连接。
    完。
    若 tcb 的 四元组 的 源端口 等于 0：
        tcb 的 四元组 的 源端口 为 做 tcp_分配临时端口。
    完。
    tcb 的 四元组 的 目标IP 为 目标IP。
    tcb 的 四元组 的 目标端口 为 目标端口。
    可变 Nova缓冲 为 做 tcp_构建SYN段 用 tcb。
    若 Nova缓冲 等于 空：
        返还 错误码_内存不足。
    完。
    tcb 的 状态 为 传控_状态_SYN已发送。
    tcb 的 发送序号 为 tcb 的 发送序号 加 1。
    做 Nova网络缓冲_队列_入队 用 tcb 的 重传队列、Nova缓冲。
    做 tcp_启动重传定时器 用 tcb。
    结果 为 做 tcp_发送段 用 tcb、Nova缓冲。
    若 结果 不等于 0：
        tcb 的 状态 为 传控_状态_关闭。
        返还 结果。
    完。
    返还 0。
完。

tcp_构建SYN段 接受 tcb：
    可变 Nova缓冲 为 做 Nova缓冲_分配 用 传控_头部大小 加 传控_选项最大大小。
    若 Nova缓冲 等于 空：
        返还 空。
    完。
    可变 头 为 Nova缓冲 的 数据。
    做 tcp_设置源端口 用 头、tcb 的 四元组 的 源端口。
    做 tcp_设置目标端口 用 头、tcb 的 四元组 的 目标端口。
    做 tcp_设置序号 用 头、tcb 的 发送序号。
    做 tcp_设置确认号 用 头、0。
    做 tcp_设置标志 用 头、传控_标志_SYN。
    做 tcp_设置窗口 用 头、tcb 的 接收窗口。
    做 tcp_添加MSS选项 用 头、tcb 的 mss。
    做 tcp_添加窗口缩放选项 用 头、传控_默认窗口缩放。
    返还 Nova缓冲。
完。

本章 收束。
