《路由添加》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·路由查找》。

引用《.·IP前缀掩码转换》。

引用《..·中枢·哈希函数》。

引用《..·中枢·同步原语》。

路由_添加 接受 目标网络、前缀长度、网关、设备、度量、标志：
    返还 做 路由_添加_完整 用 目标网络、前缀长度、网关、设备、路由类型_单播、路由范围_全局、路由协议_静态、度量。
完。

路由_添加_完整 接受 目标网络、前缀长度、网关、设备、类型、范围、协议、度量：
    可变 条目 为 做 分配内存 用 做 大小 用 路由条目。
    若 条目 等于 空：
        返还 错误码_内存不足。
    完。
    条目 的 目标网络 为 目标网络。
    条目 的 前缀长度 为 前缀长度。
    条目 的 子网掩码 为 做 前缀长度_转掩码 用 前缀长度。
    条目 的 网关 为 网关。
    条目 的 设备 为 设备。
    条目 的 类型 为 类型。
    条目 的 范围 为 范围。
    条目 的 协议 为 协议。
    条目 的 度量 为 度量。
    条目 的 优先级 为 度量。
    条目 的 标志 为 0。
    条目 的 引用计数 为 1。
    条目 的 使用计数 为 0。
    条目 的 最后使用时间 为 0。
    若 网关 不等于 0：
        条目 的 标志 为 条目 的 标志 位或 路由标志_网关。
    完。
    若 前缀长度 等于 32：
        条目 的 标志 为 条目 的 标志 位或 路由标志_主机。
    完。
    若 前缀长度 等于 0：
        条目 的 标志 为 条目 的 标志 位或 路由标志_默认。
    完。
    可变 表 为 主路由表。
    若 类型 等于 路由类型_本地：
        表 为 本地路由表。
    完。
    做 写锁_获取 用 表 的 锁。
    若 前缀长度 等于 0：
        表 的 默认路由 为 条目。
    完。
    可变 前一个 为 空。
    可变 当前 为 表 的 条目链表。
    只要 当前 不等于 空：
        若 条目 的 前缀长度 大于 当前 的 前缀长度：
            跳出。
        完。
        若 条目 的 前缀长度 等于 当前 的 前缀长度 并且 条目 的 度量 小于 当前 的 度量：
            跳出。
        完。
        前一个 为 当前。
        当前 为 当前 的 下一个。
    完。
    若 前一个 等于 空：
        条目 的 下一个 为 表 的 条目链表。
        表 的 条目链表 为 条目。
    否则：
        条目 的 下一个 为 前一个 的 下一个。
        前一个 的 下一个 为 条目。
    完。
    表 的 条目数 为 表 的 条目数 加 1。
    做 写锁_释放 用 表 的 锁。
    做 路由缓存_清除。
    返还 0。
完。

路由_删除 接受 目标网络、前缀长度：
    做 写锁_获取 用 主路由表 的 锁。
    掩码 为 做 前缀长度_转掩码 用 前缀长度。
    可变 前一个 为 空。
    可变 当前 为 主路由表 的 条目链表。
    只要 当前 不等于 空：
        若 当前 的 目标网络 等于 目标网络 并且 当前 的 前缀长度 等于 前缀长度：
            若 前一个 等于 空：
                主路由表 的 条目链表 为 当前 的 下一个。
            否则：
                前一个 的 下一个 为 当前 的 下一个。
            完。
            主路由表 的 条目数 为 主路由表 的 条目数 减 1。
            若 主路由表 的 默认路由 等于 当前：
                主路由表 的 默认路由 为 空。
            完。
            做 写锁_释放 用 主路由表 的 锁。
            做 释放内存 用 当前。
            做 路由缓存_清除。
            返还 0。
        完。
        前一个 为 当前。
        当前 为 当前 的 下一个。
    完。
    做 写锁_释放 用 主路由表 的 锁。
    返还 错误码_不存在。
完。

本章 收束。
