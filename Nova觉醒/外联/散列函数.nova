《散列函数》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

SHA256_H0 为 0x6a09e667。

SHA256_H1 为 0xbb67ae85。

SHA256_H2 为 0x3c6ef372。

SHA256_H3 为 0xa54ff53a。

SHA256_H4 为 0x510e527f。

SHA256_H5 为 0x9b05688c。

SHA256_H6 为 0x1f83d9ab。

SHA256_H7 为 0x5be0cd19。

可变 SHA256_K 为 一组：0x428a2f98、0x71374491、0xb5c0fbcf、0xe9b5dba5、0x3956c25b、0x59f111f1、0x923f82a4、0xab1c5ed5、0xd807aa98、0x12835b01、0x243185be、0x550c7dc3、0x72be5d74、0x80deb1fe、0x9bdc06a7、0xc19bf174、0xe49b69c1、0xefbe4786、0x0fc19dc6、0x240ca1cc、0x2de92c6f、0x4a7484aa、0x5cb0a9dc、0x76f988da、0x983e5152、0xa831c66d、0xb00327c8、0xbf597fc7、0xc6e00bf3、0xd5a79147、0x06ca6351、0x14292967、0x27b70a85、0x2e1b2138、0x4d2c6dfc、0x53380d13、0x650a7354、0x766a0abb、0x81c2c92e、0x92722c85、0xa2bfe8a1、0xa81a664b、0xc24b8b70、0xc76c51a3、0xd192e819、0xd6990624、0xf40e3585、0x106aa070、0x19a4c116、0x1e376c08、0x2748774c、0x34b0bcb5、0x391c0cb3、0x4ed8aa4a、0x5b9cca4f、0x682e6ff3、0x748f82ee、0x78a5636f、0x84c87814、0x8cc70208、0x90befffa、0xa4506ceb、0xbef9a3f7、0xc67178f2。

SHA256_CTX 是 一种 事物，含 状态、缓冲、缓冲长度、总长度。

SHA256_初始化 接受 ctx：
    ctx 的 状态 的 第 0 项 为 SHA256_H0。
    ctx 的 状态 的 第 1 项 为 SHA256_H1。
    ctx 的 状态 的 第 2 项 为 SHA256_H2。
    ctx 的 状态 的 第 3 项 为 SHA256_H3。
    ctx 的 状态 的 第 4 项 为 SHA256_H4。
    ctx 的 状态 的 第 5 项 为 SHA256_H5。
    ctx 的 状态 的 第 6 项 为 SHA256_H6。
    ctx 的 状态 的 第 7 项 为 SHA256_H7。
    ctx 的 缓冲长度 为 0。
    ctx 的 总长度 为 0。
完。

SHA256_ROTR 接受 x、n：
    返还 (x 右移 n 位或 x 左移 32 减 n) 位与 0xFFFFFFFF。
完。

SHA256_处理块 接受 ctx、块：
    W 为 做 堆_分配 用 64 乘 4。
    可变 i 为 0。
    只要 i 小于 16：
        offset 为 i 乘 4。
        做 内存_写32 用 W 加 i 乘 4、做 内存_读8 用 块 加 offset 左移 24 位或 做 内存_读8 用 块 加 offset 加 1 左移 16 位或 做 内存_读8 用 块 加 offset 加 2 左移 8 位或 做 内存_读8 用 块 加 offset 加 3。
        i 为 i 加 1。
    完。
    只要 i 小于 64：
        w15 为 做 内存_读32 用 W 加 (i 减 15) 乘 4。
        w2 为 做 内存_读32 用 W 加 (i 减 2) 乘 4。
        s0 为 做 SHA256_ROTR 用 w15、7 位异或 做 SHA256_ROTR 用 w15、18 位异或 w15 右移 3。
        s1 为 做 SHA256_ROTR 用 w2、17 位异或 做 SHA256_ROTR 用 w2、19 位异或 w2 右移 10。
        新w 为 做 内存_读32 用 W 加 (i 减 16) 乘 4 加 s0 加 做 内存_读32 用 W 加 (i 减 7) 乘 4 加 s1 位与 0xFFFFFFFF。
        做 内存_写32 用 W 加 i 乘 4、新w。
        i 为 i 加 1。
    完。
    可变 a 为 ctx 的 状态 的 第 0 项。
    可变 b 为 ctx 的 状态 的 第 1 项。
    可变 c 为 ctx 的 状态 的 第 2 项。
    可变 d 为 ctx 的 状态 的 第 3 项。
    可变 e 为 ctx 的 状态 的 第 4 项。
    可变 f 为 ctx 的 状态 的 第 5 项。
    可变 g 为 ctx 的 状态 的 第 6 项。
    可变 h 为 ctx 的 状态 的 第 7 项。
    i 为 0。
    只要 i 小于 64：
        S1 为 做 SHA256_ROTR 用 e、6 位异或 做 SHA256_ROTR 用 e、11 位异或 做 SHA256_ROTR 用 e、25。
        ch 为 e 位与 f 位异或 按位取反 e 位与 g。
        temp1 为 h 加 S1 加 ch 加 SHA256_K 的 第 i 项 加 做 内存_读32 用 W 加 i 乘 4 位与 0xFFFFFFFF。
        S0 为 做 SHA256_ROTR 用 a、2 位异或 做 SHA256_ROTR 用 a、13 位异或 做 SHA256_ROTR 用 a、22。
        maj 为 a 位与 b 位异或 a 位与 c 位异或 b 位与 c。
        temp2 为 S0 加 maj 位与 0xFFFFFFFF。
        h 为 g。
        g 为 f。
        f 为 e。
        e 为 d 加 temp1 位与 0xFFFFFFFF。
        d 为 c。
        c 为 b。
        b 为 a。
        a 为 temp1 加 temp2 位与 0xFFFFFFFF。
        i 为 i 加 1。
    完。
    ctx 的 状态 的 第 0 项 为 ctx 的 状态 的 第 0 项 加 a 位与 0xFFFFFFFF。
    ctx 的 状态 的 第 1 项 为 ctx 的 状态 的 第 1 项 加 b 位与 0xFFFFFFFF。
    ctx 的 状态 的 第 2 项 为 ctx 的 状态 的 第 2 项 加 c 位与 0xFFFFFFFF。
    ctx 的 状态 的 第 3 项 为 ctx 的 状态 的 第 3 项 加 d 位与 0xFFFFFFFF。
    ctx 的 状态 的 第 4 项 为 ctx 的 状态 的 第 4 项 加 e 位与 0xFFFFFFFF。
    ctx 的 状态 的 第 5 项 为 ctx 的 状态 的 第 5 项 加 f 位与 0xFFFFFFFF。
    ctx 的 状态 的 第 6 项 为 ctx 的 状态 的 第 6 项 加 g 位与 0xFFFFFFFF。
    ctx 的 状态 的 第 7 项 为 ctx 的 状态 的 第 7 项 加 h 位与 0xFFFFFFFF。
    做 堆_释放 用 W。
完。

SHA256_更新 接受 ctx、数据、长度：
    可变 offset 为 0。
    ctx 的 总长度 为 ctx 的 总长度 加 长度。
    若 ctx 的 缓冲长度 大于 0：
        空间 为 64 减 ctx 的 缓冲长度。
        复制量 为 若 长度 小于 空间 则 长度 否则 空间。
        做 内存_复制 用 ctx 的 缓冲 加 ctx 的 缓冲长度、数据、复制量。
        ctx 的 缓冲长度 为 ctx 的 缓冲长度 加 复制量。
        offset 为 复制量。
        若 ctx 的 缓冲长度 等于 64：
            做 SHA256_处理块 用 ctx、ctx 的 缓冲。
            ctx 的 缓冲长度 为 0。
        完。
    完。
    只要 offset 加 64 小于或等于 长度：
        做 SHA256_处理块 用 ctx、数据 加 offset。
        offset 为 offset 加 64。
    完。
    若 offset 小于 长度：
        剩余 为 长度 减 offset。
        做 内存_复制 用 ctx 的 缓冲、数据 加 offset、剩余。
        ctx 的 缓冲长度 为 剩余。
    完。
完。

SHA256_最终 接受 ctx、摘要：
    位长 为 ctx 的 总长度 乘 8。
    做 内存_写8 用 ctx 的 缓冲 加 ctx 的 缓冲长度、0x80。
    ctx 的 缓冲长度 为 ctx 的 缓冲长度 加 1。
    若 ctx 的 缓冲长度 大于 56：
        做 内存_置零 用 ctx 的 缓冲 加 ctx 的 缓冲长度、64 减 ctx 的 缓冲长度。
        做 SHA256_处理块 用 ctx、ctx 的 缓冲。
        ctx 的 缓冲长度 为 0。
    完。
    做 内存_置零 用 ctx 的 缓冲 加 ctx 的 缓冲长度、56 减 ctx 的 缓冲长度。
    做 内存_写8 用 ctx 的 缓冲 加 56、位长 右移 56 位与 0xFF。
    做 内存_写8 用 ctx 的 缓冲 加 57、位长 右移 48 位与 0xFF。
    做 内存_写8 用 ctx 的 缓冲 加 58、位长 右移 40 位与 0xFF。
    做 内存_写8 用 ctx 的 缓冲 加 59、位长 右移 32 位与 0xFF。
    做 内存_写8 用 ctx 的 缓冲 加 60、位长 右移 24 位与 0xFF。
    做 内存_写8 用 ctx 的 缓冲 加 61、位长 右移 16 位与 0xFF。
    做 内存_写8 用 ctx 的 缓冲 加 62、位长 右移 8 位与 0xFF。
    做 内存_写8 用 ctx 的 缓冲 加 63、位长 位与 0xFF。
    做 SHA256_处理块 用 ctx、ctx 的 缓冲。
    可变 j 为 0。
    只要 j 小于 8：
        v 为 ctx 的 状态 的 第 j 项。
        做 内存_写8 用 摘要 加 j 乘 4、v 右移 24 位与 0xFF。
        做 内存_写8 用 摘要 加 j 乘 4 加 1、v 右移 16 位与 0xFF。
        做 内存_写8 用 摘要 加 j 乘 4 加 2、v 右移 8 位与 0xFF。
        做 内存_写8 用 摘要 加 j 乘 4 加 3、v 位与 0xFF。
        j 为 j 加 1。
    完。
完。

SHA256 接受 数据、长度、摘要：
    ctx 为 做 堆_分配 用 SHA256_CTX。
    做 SHA256_初始化 用 ctx。
    做 SHA256_更新 用 ctx、数据、长度。
    做 SHA256_最终 用 ctx、摘要。
    做 堆_释放 用 ctx。
完。

可变 AI_散列函数_调用数 为 0。

可变 AI_散列函数_异常数 为 0。

AI_散列函数_监控 接受 零参：
    AI_散列函数_调用数 为 AI_散列函数_调用数 加 1。
    返还 AI_散列函数_异常数 等于 0。
完。

AI_散列函数_健康检查 接受 零参：
    若 AI_散列函数_调用数 大于 0 并且 AI_散列函数_异常数 乘 100 除以 AI_散列函数_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
