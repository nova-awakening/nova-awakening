《协议族注册》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·内核打印》。

引用《..·中枢·链表后继》。

引用《..·中枢·整数转字符串》。

NPROTO 为 46。

协议族 是 一种 事物，含 family、create、owner。

传输协议 是 一种 事物，含 名称、close、connect、accept、sendmsg、recvmsg、bind、listen、shutdown、setsockopt、getsockopt、obj_size、sockets_allocated。

可变 协议族表 为 一组：（空）。

可变 传输协议表 为 一组：（空）。

协议族_注册 接受 域、创建函数：
    若 做 协议族_查找 用 域 不等于 空：
        返还 错误_已存在。
    完。
    pf 为 新建 协议族。
    pf 的 family 为 域。
    pf 的 create 为 创建函数。
    pf 的 owner 为 0。
    做 追加 用 协议族表、pf。
    做 内核信息 用 「协议族注册: AF_」 加 做 AF_名称 用 域。
    返还 成功。
完。

协议族_注销 接受 域：
    可变 i 为 0。
    只要 i 小于 做 计数 用 协议族表：
        若 协议族表 的 第 i 项 的 family 等于 域：
            做 链表_删除 用 协议族表、i。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    返还 错误_不存在。
完。

协议族_查找 接受 域：
    可变 i 为 0。
    只要 i 小于 做 计数 用 协议族表：
        若 协议族表 的 第 i 项 的 family 等于 域：
            返还 协议族表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

传输协议_注册 接受 协议：
    做 追加 用 传输协议表、协议。
    做 内核信息 用 「传输协议注册: 」 加 协议 的 名称。
    返还 成功。
完。

传输协议_查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 传输协议表：
        若 传输协议表 的 第 i 项 的 名称 等于 名称：
            返还 传输协议表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

sock_创建 接受 域、类型、协议：
    pf 为 做 协议族_查找 用 域。
    若 pf 等于 空：
        返还 空。
    完。
    若 pf 的 create 等于 空：
        返还 空。
    完。
    返还 做 pf 的 create 用 域、类型、协议。
完。

网络协议_初始化全部 接受 零参：
    做 协议族_注册 用 AF_INET、inet_创建socket。
    做 协议族_注册 用 AF_UNIX、unix_创建socket。
    做 内核信息数值 用 「协议族初始化完成, 已注册」、做 计数 用 协议族表。
完。

AF_名称 接受 域：
    若 域 等于 AF_UNSPEC：
        返还 「UNSPEC」。
    完。
    若 域 等于 AF_UNIX：
        返还 「UNIX」。
    完。
    若 域 等于 AF_INET：
        返还 「INET」。
    完。
    若 域 等于 AF_INET6：
        返还 「INET6」。
    完。
    若 域 等于 AF_NETLINK：
        返还 「NETLINK」。
    完。
    若 域 等于 AF_PACKET：
        返还 「PACKET」。
    完。
    返还 做 整数转字符串 用 域。
完。

socket_域 接受 sock：
    若 sock 等于 空：
        返还 0。
    完。
    返还 sock 的 域。
完。

socket_类型 接受 sock：
    若 sock 等于 空：
        返还 0。
    完。
    返还 sock 的 类型。
完。

socket_操作表 接受 sock：
    若 sock 等于 空：
        返还 空。
    完。
    返还 sock 的 操作表。
完。

socket_文件_创建 接受 sock、标志：
    若 sock 等于 空：
        返还 空。
    完。
    返还 一本字典：类型 对应 「套接字」、套接字 对应 sock、标志 对应 标志、错误码 对应 0、已关闭 对应 假。
完。

SOCK_类型名 接受 类型：
    若 类型 等于 SOCK_STREAM：
        返还 「STREAM」。
    完。
    若 类型 等于 SOCK_DGRAM：
        返还 「DGRAM」。
    完。
    若 类型 等于 SOCK_RAW：
        返还 「RAW」。
    完。
    若 类型 等于 SOCK_SEQPACKET：
        返还 「SEQPACKET」。
    完。
    返还 做 整数转字符串 用 类型。
完。

协议族_列举 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 协议族表：
        做 内核信息 用 「  AF_」 加 做 AF_名称 用 协议族表 的 第 i 项 的 family。
        i 为 i 加 1。
    完。
完。

传输协议_列举 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 传输协议表：
        做 内核信息 用 「  」 加 传输协议表 的 第 i 项 的 名称 加 「 sockets=」 加 做 整数转字符串 用 传输协议表 的 第 i 项 的 sockets_allocated。
        i 为 i 加 1。
    完。
完。

协议族_统计 接受 零参：
    返还 一组：做 计数 用 协议族表、做 计数 用 传输协议表。
完。

协议族_完整统计 接受 零参：
    返还 一组：做 计数 用 协议族表、做 计数 用 传输协议表。
完。

可变 AI_协议族注册_热文件表 为 一组：（空）。

AI_协议族注册_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_协议族注册_热文件表：
        若 AI_协议族注册_热文件表 的 第 i 项 的 路径 等于 路径：
            AI_协议族注册_热文件表 的 第 i 项 的 频率 为 AI_协议族注册_热文件表 的 第 i 项 的 频率 加 1。
            返还 AI_协议族注册_热文件表 的 第 i 项 的 频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_协议族注册_热文件表 小于 128：
        做 追加 用 AI_协议族注册_热文件表、一本字典：路径 对应 路径、频率 对应 1。
    完。
    返还 1。
完。

AI_协议族注册_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
        返还 0。
    完。
    返还 32768。
完。

本章 收束。
