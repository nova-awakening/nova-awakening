《网络设备启停》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·ARP接收》。

引用《.·IP接收》。

引用《.·网络设备诊断》。

引用《.·网缓冲分配》。

引用《.·网络缓冲读》。

网络设备_启动 接受 dev：
    若 dev 的 状态 位与 网络设备_状态_已启动：
        返还 0。
    完。
    若 dev 的 操作 的 打开 不等于 空：
        结果 为 做 dev 的 操作 的 打开 用 dev。
        若 结果 不等于 0：
            返还 结果。
        完。
    完。
    dev 的 标志 为 dev 的 标志 位或 IFF_UP 位或 IFF_RUNNING。
    dev 的 状态 为 dev 的 状态 位或 网络设备_状态_已启动 位或 网络设备_状态_已连接。
    做 内核信息 用 「网络设备 %s: 已启动\n」、dev 的 名称。
    返还 0。
完。

网络设备_停止 接受 dev：
    若 非 (dev 的 状态 位与 网络设备_状态_已启动)：
        返还 0。
    完。
    dev 的 标志 为 dev 的 标志 位与 按位取反 (IFF_UP 位或 IFF_RUNNING)。
    dev 的 状态 为 dev 的 状态 位与 按位取反 (网络设备_状态_已启动 位或 网络设备_状态_已连接)。
    做 Nova网络缓冲_队列_清空 用 dev 的 发送队列。
    若 dev 的 操作 的 停止 不等于 空：
        做 dev 的 操作 的 停止 用 dev。
    完。
    做 内核信息 用 「网络设备 %s: 已停止\n」、dev 的 名称。
    返还 0。
完。

网络设备_发送 接受 dev、Nova缓冲：
    若 非 (dev 的 标志 位与 IFF_UP)：
        做 Nova缓冲_释放 用 Nova缓冲。
        返还 错误码_网络不可达。
    完。
    若 dev 的 操作 的 发送 等于 空：
        做 Nova缓冲_释放 用 Nova缓冲。
        返还 错误码_不支持。
    完。
    Nova缓冲 的 设备 为 dev。
    dev 的 统计 的 发送包数 为 dev 的 统计 的 发送包数 加 1。
    dev 的 统计 的 发送字节 为 dev 的 统计 的 发送字节 加 Nova缓冲 的 数据长度。
    结果 为 做 dev 的 操作 的 发送 用 Nova缓冲。
    若 结果 不等于 0：
        dev 的 统计 的 发送错误 为 dev 的 统计 的 发送错误 加 1。
    完。
    返还 结果。
完。

网络设备_接收 接受 Nova缓冲：
    dev 为 Nova缓冲 的 设备。
    若 dev 等于 空：
        做 Nova缓冲_释放 用 Nova缓冲。
        返还。
    完。
    dev 的 统计 的 接收包数 为 dev 的 统计 的 接收包数 加 1。
    dev 的 统计 的 接收字节 为 dev 的 统计 的 接收字节 加 Nova缓冲 的 数据长度。
    以太网类型 为 做 网络序_读u16 用 Nova缓冲 的 数据、12。
    Nova缓冲 的 网络层偏移 为 14。
    做 匹配 用 以太网类型。
完。

网络设备启_统计 接受 零参：
    返还 一本字典：模块 对应 「网络设备启」。
完。

网络设备启_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    返还 做 计数 用 问题 等于 0。
完。

网络设备启_综合报告 接受 零参：
    做 打印 用 「[网络设备启] ═══ 综合报告 ═══」。
    做 打印 用 「  健康: 」 加 若 做 网络设备启_健康评估 则 「正常」 否则 「异常」。
完。

网络设备启_全局重置 接受 零参：
    做 打印 用 「[网络设备启] 全局统计已重置」。
    返还 0。
完。

本章 收束。
