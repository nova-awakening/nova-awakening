《HPACK整数编解码》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·感知·内核直连层》。

HPACK_解码整数 接受 数据、pos、前缀位数：
    掩码 为 (1 左移 前缀位数) 减 1。
    可变 值 为 做 内存_读8 用 数据 加 pos 位与 掩码。
    pos 为 pos 加 1。
    若 值 小于 掩码：
        返还 值。
    完。
    可变 M 为 0。
    可变 继续 为 真。
    只要 继续：
        b 为 做 内存_读8 用 数据 加 pos。
        pos 为 pos 加 1。
        值 为 值 加 (b 位与 127) 乘 (1 左移 M)。
        M 为 M 加 7。
        继续 为 b 位与 128 不等于 0。
    完。
    返还 值。
完。

HPACK_编码整数 接受 输出、pos、值、前缀位数、前缀字节：
    掩码 为 (1 左移 前缀位数) 减 1。
    若 值 小于 掩码：
        做 内存_写8 用 输出 加 pos、前缀字节 位或 值。
        返还 pos 加 1。
    完。
    做 内存_写8 用 输出 加 pos、前缀字节 位或 掩码。
    pos 为 pos 加 1。
    值 为 值 减 掩码。
    只要 值 大于或等于 128：
        做 内存_写8 用 输出 加 pos、值 位与 127 位或 128。
        pos 为 pos 加 1。
        值 为 值 右移 7。
    完。
    做 内存_写8 用 输出 加 pos、值。
    返还 pos 加 1。
完。

HPACK_解码字符串 接受 数据、pos、输出、输出长度：
    首字节 为 做 内存_读8 用 数据 加 pos。
    是频率编码 为 首字节 位与 0x80 不等于 0。
    长度 为 做 HPACK_解码整数 用 数据、pos、7。
    若 首字节 位与 0x7F 小于 127：
        pos 为 pos 加 1。
    否则：
        pos 为 pos 加 1。
        只要 做 内存_读8 用 数据 加 pos 减 1 位与 128：
            pos 为 pos 加 1。
        完。
    完。
    若 是频率编码：
        做 HPACK_频率编码_解码 用 数据 加 pos、长度、输出、输出长度。
    否则：
        做 内存_复制 用 输出、数据 加 pos、长度。
        输出长度 为 长度。
    完。
    返还 pos 加 长度。
完。

HPACK_编码字符串 接受 输出、pos、字符串、长度、使用频率编码：
    若 使用频率编码：
        huf_buf 为 做 堆_分配 用 长度 乘 2。
        huf_len 为 做 HPACK_频率编码_编码 用 字符串、长度、huf_buf。
        若 huf_len 小于 长度：
            pos 为 做 HPACK_编码整数 用 输出、pos、huf_len、7、0x80。
            做 内存_复制 用 输出 加 pos、huf_buf、huf_len。
            做 堆_释放 用 huf_buf。
            返还 pos 加 huf_len。
        完。
        做 堆_释放 用 huf_buf。
    完。
    pos 为 做 HPACK_编码整数 用 输出、pos、长度、7、0x00。
    做 内存_复制 用 输出 加 pos、字符串、长度。
    返还 pos 加 长度。
完。

HPACK_频率编码_编码 接受 输入、输入长度、输出：
    可变 bit_buf 为 0。
    可变 bit_count 为 0。
    可变 out_pos 为 0。
    可变 i 为 0。
    只要 i 小于 输入长度：
        ch 为 做 内存_读8 用 输入 加 i。
        code 为 做 HPACK_频率编码_码表 用 ch。
        code_len 为 做 HPACK_频率编码_码长 用 ch。
        bit_buf 为 bit_buf 左移 code_len 位或 code。
        bit_count 为 bit_count 加 code_len。
        只要 bit_count 大于或等于 8：
            bit_count 为 bit_count 减 8。
            做 内存_写8 用 输出 加 out_pos、bit_buf 右移 bit_count 位与 0xFF。
            out_pos 为 out_pos 加 1。
        完。
        i 为 i 加 1。
    完。
    若 bit_count 大于 0：
        做 内存_写8 用 输出 加 out_pos、bit_buf 左移 8 减 bit_count 位或 (1 左移 8 减 bit_count) 减 1。
        out_pos 为 out_pos 加 1。
    完。
    返还 out_pos。
完。

HPACK_频率编码_解码 接受 输入、输入长度、输出、输出长度：
    可变 out_pos 为 0。
    可变 bit_buf 为 0。
    可变 bit_count 为 0。
    可变 i 为 0。
    只要 i 小于 输入长度：
        bit_buf 为 bit_buf 左移 8 位或 做 内存_读8 用 输入 加 i。
        bit_count 为 bit_count 加 8。
        i 为 i 加 1。
        只要 bit_count 大于或等于 5：
            matched 为 做 HPACK_频率编码_匹配 用 bit_buf、bit_count。
            若 matched 大于或等于 0：
                码长 为 做 HPACK_频率编码_码长 用 matched。
                bit_count 为 bit_count 减 码长。
                bit_buf 为 bit_buf 位与 (1 左移 bit_count) 减 1。
                若 matched 小于 256：
                    做 内存_写8 用 输出 加 out_pos、matched。
                    out_pos 为 out_pos 加 1。
                完。
            否则：
                跳出。
            完。
        完。
    完。
    输出长度 为 out_pos。
完。

HPACK_频率编码_码表 接受 ch：
    若 ch 等于 0x30：
        返还 0x00。
    完。
    若 ch 等于 0x31：
        返还 0x01。
    完。
    若 ch 等于 0x32：
        返还 0x02。
    完。
    若 ch 等于 0x61：
        返还 0x03。
    完。
    若 ch 等于 0x63：
        返还 0x04。
    完。
    若 ch 等于 0x65：
        返还 0x05。
    完。
    若 ch 等于 0x69：
        返还 0x06。
    完。
    若 ch 等于 0x6F：
        返还 0x07。
    完。
    若 ch 等于 0x73：
        返还 0x08。
    完。
    若 ch 等于 0x74：
        返还 0x09。
    完。
    返还 ch。
完。

HPACK_频率编码_码长 接受 ch：
    若 ch 大于或等于 0x30 并且 ch 小于或等于 0x39：
        返还 5。
    完。
    若 ch 大于或等于 0x61 并且 ch 小于或等于 0x7A：
        返还 5。
    完。
    若 ch 大于或等于 0x41 并且 ch 小于或等于 0x5A：
        返还 6。
    完。
    若 ch 等于 0x20：
        返还 6。
    完。
    若 ch 等于 0x2F：
        返还 6。
    完。
    若 ch 等于 0x3A：
        返还 7。
    完。
    若 ch 等于 0x2D：
        返还 6。
    完。
    若 ch 等于 0x2E：
        返还 6。
    完。
    若 ch 等于 0x2C：
        返还 7。
    完。
    返还 8。
完。

HPACK_频率编码_匹配 接受 bit_buf、bit_count：
    可变 ch 为 0。
    只要 ch 小于 256：
        码长 为 做 HPACK_频率编码_码长 用 ch。
        若 码长 小于或等于 bit_count：
            码 为 做 HPACK_频率编码_码表 用 ch。
            提取 为 bit_buf 右移 bit_count 减 码长 位与 (1 左移 码长) 减 1。
            若 提取 等于 码：
                返还 ch。
            完。
        完。
        ch 为 ch 加 1。
    完。
    返还 负 1。
完。

可变 AI_确认解码整_操作热度 为 0。

AI_确认解码整_最优算法 接受 数据量、已排序：
    AI_确认解码整_操作热度 为 AI_确认解码整_操作热度 加 1。
    若 数据量 小于 16：
        返还 「插入排序」。
    完。
    若 已排序：
        返还 「二分查找」。
    完。
    若 数据量 小于 10000：
        返还 「快速排序」。
    完。
    返还 「归并排序」。
完。

本章 收束。
