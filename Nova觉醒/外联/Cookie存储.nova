《Cookie存储》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

Cookie_最大 为 256。

Cookie 是 一种 事物，含 名称、值、域、路径、过期时间、安全、HttpOnly、SameSite。

可变 Cookie_存储 为 一组：（空）。

Cookie_设置 接受 域、名称、值、过期、安全：
    可变 i 为 0。
    只要 i 小于 做 计数 用 Cookie_存储：
        若 Cookie_存储 的 第 i 项 的 域 等于 域 并且 Cookie_存储 的 第 i 项 的 名称 等于 名称：
            Cookie_存储 的 第 i 项 的 值 为 值。
            Cookie_存储 的 第 i 项 的 过期时间 为 过期。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 Cookie_存储 大于或等于 Cookie_最大：
        返还。
    完。
    c 为 新建 Cookie。
    c 的 名称 为 名称。
    c 的 值 为 值。
    c 的 域 为 域。
    c 的 路径 为 「/」。
    c 的 过期时间 为 过期。
    c 的 安全 为 安全。
    c 的 HttpOnly 为 假。
    c 的 SameSite 为 1。
    做 追加 用 Cookie_存储、c。
完。

Cookie_获取 接受 域、名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 Cookie_存储：
        若 Cookie_存储 的 第 i 项 的 域 等于 域 并且 Cookie_存储 的 第 i 项 的 名称 等于 名称：
            返还 Cookie_存储 的 第 i 项 的 值。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

Cookie_域所有 接受 域：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 Cookie_存储：
        若 Cookie_存储 的 第 i 项 的 域 等于 域：
            做 追加 用 结果、Cookie_存储 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 本地存储 为 一组：（空）。

Local存储_设置 接受 键、值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 本地存储：
        若 本地存储 的 第 i 项 的 第 0 项 等于 键：
            本地存储 的 第 i 项 的 第 1 项 为 值。
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 本地存储、一组：键、值。
完。

Local存储_获取 接受 键：
    可变 i 为 0。
    只要 i 小于 做 计数 用 本地存储：
        若 本地存储 的 第 i 项 的 第 0 项 等于 键：
            返还 本地存储 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

书签 是 一种 事物，含 标题、URL、文件夹、添加时间。

书签_添加 接受 标题、URL、文件夹：
    若 做 计数 用 全局书签 大于或等于 最大书签：
        返还。
    完。
    bm 为 新建 书签。
    bm 的 标题 为 标题。
    bm 的 URL 为 URL。
    bm 的 文件夹 为 文件夹。
    bm 的 添加时间 为 浏览器_时钟。
    做 追加 用 全局书签、bm。
完。

书签_删除 接受 URL：
    可变 i 为 0。
    只要 i 小于 做 计数 用 全局书签：
        若 全局书签 的 第 i 项 的 URL 等于 URL：
            做 删除 用 全局书签、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

本章 收束。
