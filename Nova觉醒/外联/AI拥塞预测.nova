《AI拥塞预测》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·时钟中枢》。

引用《..·中枢·链表后继》。

可变 Nova往返时间历史 为 一组：（空）。

Nova往返时间_窗口 为 32。

可变 Nova往返时间_EMA 为 0。

可变 Nova往返时间_方差EMA 为 0。

可变 Nova往返时间_斜率 为 0。

可变 拥塞概率 为 0。

可变 丢包率EMA 为 0。

可变 AI拥塞_已初始化 为 假。

AI拥塞_初始化 接受 零参：
    AI拥塞_已初始化 为 真。
    做 内核信息 用 「AI拥塞预测: Nova往返时间线性回归+方差检测+联合概率窗口调整」。
完。

AI拥塞_记录Nova往返时间 接受 Nova往返时间：
    现在 为 做 读取毫秒。
    做 追加 用 Nova往返时间历史、一组：现在、Nova往返时间。
    若 做 计数 用 Nova往返时间历史 大于 Nova往返时间_窗口：
        做 链表_删除 用 Nova往返时间历史、0。
    完。
    Nova往返时间_EMA 为 (Nova往返时间_EMA 乘 922 加 Nova往返时间 乘 1024 乘 102) 除以 (1024 乘 1024)。
    偏差 为 Nova往返时间 乘 1024 减 Nova往返时间_EMA。
    若 偏差 小于 0：
        偏差 为 0 减 偏差。
    完。
    Nova往返时间_方差EMA 为 (Nova往返时间_方差EMA 乘 922 加 偏差 乘 102) 除以 1024。
    Nova往返时间_斜率 为 做 AI拥塞_计算斜率。
    可变 概率 为 0。
    若 Nova往返时间_斜率 大于 0：
        概率 为 概率 加 Nova往返时间_斜率 乘 2。
        若 概率 大于 500：
            概率 为 500。
        完。
    完。
    若 Nova往返时间_方差EMA 大于 Nova往返时间_EMA 除以 2：
        概率 为 概率 加 300。
    完。
    若 丢包率EMA 大于 50：
        概率 为 概率 加 200。
    完。
    若 概率 大于 1000：
        概率 为 1000。
    完。
    拥塞概率 为 概率。
完。

AI拥塞_记录丢包 接受 零参：
    丢包率EMA 为 (丢包率EMA 乘 922 加 1024 乘 102) 除以 1024。
完。

AI拥塞_计算斜率 接受 零参：
    n 为 做 计数 用 Nova往返时间历史。
    若 n 小于 3：
        返还 0。
    完。
    可变 sum_x 为 0。
    可变 sum_y 为 0。
    可变 sum_xy 为 0。
    可变 sum_x2 为 0。
    基准 为 Nova往返时间历史 的 第 0 项 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 n：
        x 为 Nova往返时间历史 的 第 i 项 的 第 0 项 减 基准。
        y 为 Nova往返时间历史 的 第 i 项 的 第 1 项。
        sum_x 为 sum_x 加 x。
        sum_y 为 sum_y 加 y。
        sum_xy 为 sum_xy 加 x 乘 y。
        sum_x2 为 sum_x2 加 x 乘 x。
        i 为 i 加 1。
    完。
    分母 为 n 乘 sum_x2 减 sum_x 乘 sum_x。
    若 分母 等于 0：
        返还 0。
    完。
    返还 (n 乘 sum_xy 减 sum_x 乘 sum_y) 除以 分母。
完。

AI拥塞_预测 接受 零参：
    返还 拥塞概率 大于 500。
完。

AI拥塞_概率 接受 零参：
    返还 拥塞概率。
完。

AI拥塞_建议窗口 接受 当前窗口：
    若 拥塞概率 大于 800：
        返还 当前窗口 除以 2。
    完。
    若 拥塞概率 大于 500：
        返还 当前窗口 乘 3 除以 4。
    完。
    若 拥塞概率 小于 200：
        返还 当前窗口 乘 5 除以 4。
    完。
    返还 当前窗口。
完。

AI拥塞_Nova往返时间平滑 接受 零参：
    返还 Nova往返时间_EMA 除以 1024。
完。

AI拥塞_Nova往返时间方差 接受 零参：
    返还 Nova往返时间_方差EMA 除以 1024。
完。

可变 Nova拥塞窗口_当前 为 10。

可变 Nova拥塞窗口_慢启动阈值 为 64。

可变 Nova拥塞窗口_模式 为 0。

AI拥塞_更新窗口 接受 是否丢包：
    若 是否丢包：
        做 AI拥塞_记录丢包。
        Nova拥塞窗口_慢启动阈值 为 Nova拥塞窗口_当前 除以 2。
        若 Nova拥塞窗口_慢启动阈值 小于 2：
            Nova拥塞窗口_慢启动阈值 为 2。
        完。
        Nova拥塞窗口_当前 为 Nova拥塞窗口_慢启动阈值。
        Nova拥塞窗口_模式 为 2。
        返还。
    完。
    AI建议 为 做 AI拥塞_建议窗口 用 Nova拥塞窗口_当前。
    若 Nova拥塞窗口_模式 等于 0：
        Nova拥塞窗口_当前 为 Nova拥塞窗口_当前 加 1。
        若 Nova拥塞窗口_当前 大于或等于 Nova拥塞窗口_慢启动阈值：
            Nova拥塞窗口_模式 为 1。
        完。
    否则若 Nova拥塞窗口_模式 等于 1：
        Nova拥塞窗口_当前 为 Nova拥塞窗口_当前 加 1。
    否则：
        Nova拥塞窗口_模式 为 1。
    完。
    若 AI建议 小于 Nova拥塞窗口_当前：
        Nova拥塞窗口_当前 为 AI建议。
    完。
    若 Nova拥塞窗口_当前 小于 1：
        Nova拥塞窗口_当前 为 1。
    完。
    若 Nova拥塞窗口_当前 大于 65535：
        Nova拥塞窗口_当前 为 65535。
    完。
完。

AI拥塞_获取窗口 接受 零参：
    返还 Nova拥塞窗口_当前。
完。

AI拥塞_获取模式 接受 零参：
    返还 Nova拥塞窗口_模式。
完。

可变 带宽_EMA 为 0。

可变 上次ACK时间 为 0。

可变 上次ACK字节 为 0。

AI拥塞_记录ACK 接受 确认字节数：
    现在 为 做 读取毫秒。
    若 上次ACK时间 大于 0 并且 现在 大于 上次ACK时间：
        间隔 为 现在 减 上次ACK时间。
        瞬时带宽 为 确认字节数 乘 1000 除以 间隔。
        带宽_EMA 为 (带宽_EMA 乘 922 加 瞬时带宽 乘 1024 乘 102) 除以 (1024 乘 1024)。
    完。
    上次ACK时间 为 现在。
    上次ACK字节 为 确认字节数。
完。

AI拥塞_估算带宽 接受 零参：
    返还 带宽_EMA 除以 1024。
完。

AI拥塞_统计 接受 零参：
    返还 一组：拥塞概率、Nova往返时间_EMA 除以 1024、Nova往返时间_方差EMA 除以 1024、Nova往返时间_斜率、Nova拥塞窗口_当前、Nova拥塞窗口_模式、带宽_EMA 除以 1024、丢包率EMA 除以 1024。
完。

AI拥塞_模式名 接受 模式：
    若 模式 等于 0：
        返还 「slow_start」。
    完。
    若 模式 等于 1：
        返还 「congestion_avoidance」。
    完。
    若 模式 等于 2：
        返还 「fast_recovery」。
    完。
    返还 「unknown」。
完。

AI拥塞_级别名 接受 零参：
    若 拥塞概率 大于 800：
        返还 「critical」。
    完。
    若 拥塞概率 大于 500：
        返还 「warning」。
    完。
    若 拥塞概率 大于 200：
        返还 「mild」。
    完。
    返还 「clear」。
完。

AI拥塞_完整统计 接受 零参：
    返还 一组：拥塞概率、Nova往返时间_EMA 除以 1024、Nova往返时间_方差EMA 除以 1024、Nova往返时间_斜率、Nova拥塞窗口_当前、Nova拥塞窗口_模式、带宽_EMA 除以 1024、丢包率EMA 除以 1024、做 计数 用 Nova往返时间历史。
完。

可变 拥塞_斜率权重 为 500。

可变 拥塞_方差权重 为 300。

可变 拥塞_丢包权重 为 200。

拥塞_赫布学习率 为 50。

AI拥塞_赫布更新 接受 真实发生拥塞：
    斜率命中 为 若 Nova往返时间_斜率 大于 0 并且 真实发生拥塞 则 1 否则 0。
    方差命中 为 若 Nova往返时间_方差EMA 大于 Nova往返时间_EMA 除以 2 并且 真实发生拥塞 则 1 否则 0。
    丢包命中 为 若 丢包率EMA 大于 50 并且 真实发生拥塞 则 1 否则 0。
    若 斜率命中 等于 1：
        拥塞_斜率权重 为 拥塞_斜率权重 加 拥塞_赫布学习率。
    完。
    做 否则如果 用 非 真实发生拥塞 并且 Nova往返时间_斜率 大于 0。
    块：
        拥塞_斜率权重 为 拥塞_斜率权重 减 拥塞_赫布学习率 除以 2。
    完。
    若 方差命中 等于 1：
        拥塞_方差权重 为 拥塞_方差权重 加 拥塞_赫布学习率。
    完。
    若 丢包命中 等于 1：
        拥塞_丢包权重 为 拥塞_丢包权重 加 拥塞_赫布学习率。
    完。
    总和 为 拥塞_斜率权重 加 拥塞_方差权重 加 拥塞_丢包权重。
    若 总和 大于 0：
        拥塞_斜率权重 为 拥塞_斜率权重 乘 1000 除以 总和。
        拥塞_方差权重 为 拥塞_方差权重 乘 1000 除以 总和。
        拥塞_丢包权重 为 1000 减 拥塞_斜率权重 减 拥塞_方差权重。
    完。
    若 拥塞_斜率权重 小于 100：
        拥塞_斜率权重 为 100。
    完。
    若 拥塞_方差权重 小于 100：
        拥塞_方差权重 为 100。
    完。
    若 拥塞_丢包权重 小于 100：
        拥塞_丢包权重 为 100。
    完。
完。

AI拥塞_概率_自适应 接受 零参：
    可变 概率 为 0。
    若 Nova往返时间_斜率 大于 0：
        概率 为 概率 加 Nova往返时间_斜率 乘 2 乘 拥塞_斜率权重 除以 1000。
        若 概率 大于 500：
            概率 为 500。
        完。
    完。
    若 Nova往返时间_方差EMA 大于 Nova往返时间_EMA 除以 2：
        概率 为 概率 加 300 乘 拥塞_方差权重 除以 1000。
    完。
    若 丢包率EMA 大于 50：
        概率 为 概率 加 200 乘 拥塞_丢包权重 除以 1000。
    完。
    若 概率 大于 1000：
        概率 为 1000。
    完。
    返还 概率。
完。

本章 收束。
