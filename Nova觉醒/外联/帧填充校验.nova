《帧填充校验》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·ARP查询》。

引用《.·以太网封装》。

引用《..·中枢·整数转字符串》。

引用《.·网络设备启停》。

引用《..·中枢·内核打印》。

引用《.·网络缓冲读》。

引用《..·中枢·字符串替换》。

以太网_填充 接受 Nova缓冲：
    长度 为 做 Nova缓冲_长度 用 Nova缓冲。
    若 长度 小于 以太网_最小帧：
        填充量 为 以太网_最小帧 减 长度。
        可变 i 为 0。
        只要 i 小于 填充量：
            做 Nova缓冲_后推 用 Nova缓冲、1。
            i 为 i 加 1。
        完。
    完。
完。

MAC_格式化 接受 mac：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 6：
        若 i 大于 0：
            结果 为 结果 加 「:」。
        完。
        字节 为 做 MAC_字节 用 mac、i。
        高 为 字节 右移 4 位与 0xF。
        低 为 字节 位与 0xF。
        若 高 小于 10：
            结果 为 结果 加 做 字符 用 48 加 高。
        否则：
            结果 为 结果 加 做 字符 用 55 加 高。
        完。
        若 低 小于 10：
            结果 为 结果 加 做 字符 用 48 加 低。
        否则：
            结果 为 结果 加 做 字符 用 55 加 低。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

MAC_从字符串 接受 字符串：
    部分 为 做 字符串_分割 用 字符串、「:」。
    若 做 计数 用 部分 不等于 6：
        返还 0。
    完。
    可变 mac 为 0。
    可变 i 为 0。
    只要 i 小于 6：
        mac 为 mac 左移 8 位或 做 十六进制转整数 用 部分 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 mac。
完。

MAC_从缓冲 接受 缓冲、偏移：
    返还 做 MAC_从字节 用 做 缓冲读 用 缓冲、偏移、做 缓冲读 用 缓冲、偏移 加 1、做 缓冲读 用 缓冲、偏移 加 2、做 缓冲读 用 缓冲、偏移 加 3、做 缓冲读 用 缓冲、偏移 加 4、做 缓冲读 用 缓冲、偏移 加 5。
完。

MAC_是本地管理 接受 mac：
    返还 做 MAC_字节 用 mac、0 位与 2 不等于 0。
完。

MAC_生成随机 接受 零参：
    可变 mac 为 做 随机数64。
    mac 为 mac 位与 0xFEFFFFFFFFFF 位或 0x020000000000。
    返还 mac。
完。

链路层_发送 接受 Nova缓冲、下一跳IP、设备：
    目标MAC 为 做 ARP_查询 用 下一跳IP。
    若 目标MAC 等于 0：
        做 ARP_发送请求 用 下一跳IP、设备。
        返还 错误_重试。
    完。
    源MAC 为 做 设备_获取MAC 用 设备。
    做 以太网_封装 用 Nova缓冲、目标MAC、源MAC、以太网类型_IP。
    做 以太网_填充 用 Nova缓冲。
    返还 做 设备_发送 用 设备、Nova缓冲。
完。

外部。

网络设备列表。

设备_获取MAC 接受 设备：
    若 设备 等于 空：
        返还 0x525400000001。
    完。
    若 网络设备列表 不等于 空 并且 做 计数 用 网络设备列表 大于 0：
        返还 网络设备列表 的 第 0 项。
    完。
    返还 0x525400000001。
完。

设备_混杂模式 接受 设备：
    返还 假。
完。

设备_发送 接受 设备、Nova缓冲：
    返还 做 网络设备_发送 用 设备、Nova缓冲。
完。

IPv6_接收 接受 Nova缓冲：
    做 内核调试 用 「以太网: IPv6帧(暂不处理)」。
    返还 0。
完。

本章 收束。
