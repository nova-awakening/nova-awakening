《套接字层》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·感知·内核直连层》。

引用《..·中枢·内容可见性》。

最大套接字 为 1024。

套接_流 为 1。

套接_数据报 为 2。

套接_原始 为 3。

状态_未连接 为 0。

状态_监听 为 1。

状态_已连接 为 2。

状态_关闭中 为 3。

状态_已关闭 为 4。

套接字 是 一种 事物，含 描述符、类型、协议、状态、本地地址、本地端口、远程地址、远程端口、发送缓冲、接收缓冲、发送字节、接收字节、积压队列、积压最大、引用计数、创建时刻。

可变 套接字表 为 0。

可变 套接字数 为 0。

可变 创建数 为 0。

可变 关闭数 为 0。

可变 总发送字节 为 0。

可变 总接收字节 为 0。

可变 连接数 为 0。

可变 监听数 为 0。

套接_初始化 接受 零参：
    套接字表 为 做 堆_分配 用 最大套接字 乘 8。
    可变 序号 为 0。
    只要 序号 小于 最大套接字：
        做 内存_写64 用 套接字表 加 序号 乘 8、0。
        序号 为 序号 加 1。
    完。
    套接字数 为 0。
完。

套接_创建 接受 类型、协议：
    若 套接字数 大于或等于 最大套接字：
        返还 负 1。
    完。
    创建数 为 创建数 加 1。
    套接 为 做 堆_分配 用 套接字。
    套接 的 描述符 为 套接字数。
    套接 的 类型 为 类型。
    套接 的 协议 为 协议。
    套接 的 状态 为 状态_未连接。
    套接 的 本地地址 为 0。
    套接 的 本地端口 为 0。
    套接 的 远程地址 为 0。
    套接 的 远程端口 为 0。
    套接 的 发送缓冲 为 做 堆_分配 用 65536。
    套接 的 接收缓冲 为 做 堆_分配 用 65536。
    套接 的 发送字节 为 0。
    套接 的 接收字节 为 0。
    套接 的 积压队列 为 0。
    套接 的 积压最大 为 128。
    套接 的 引用计数 为 1。
    套接 的 创建时刻 为 做 平台_获取高精度时间。
    做 内存_写64 用 套接字表 加 套接字数 乘 8、套接。
    索引 为 套接字数。
    套接字数 为 套接字数 加 1。
    返还 索引。
完。

套接_绑定 接受 描述符、地址、端口：
    套接 为 做 套接_获取 用 描述符。
    若 套接 等于 0：
        返还 错误_无效参数。
    完。
    套接 的 本地地址 为 地址。
    套接 的 本地端口 为 端口。
    返还 0。
完。

套接_监听 接受 描述符、积压：
    套接 为 做 套接_获取 用 描述符。
    若 套接 等于 0：
        返还 错误_无效参数。
    完。
    若 套接 的 类型 不等于 套接_流：
        返还 错误_不支持。
    完。
    套接 的 状态 为 状态_监听。
    套接 的 积压最大 为 积压。
    监听数 为 监听数 加 1。
    返还 0。
完。

套接_连接 接受 描述符、远程地址、远程端口：
    套接 为 做 套接_获取 用 描述符。
    若 套接 等于 0：
        返还 错误_无效参数。
    完。
    套接 的 远程地址 为 远程地址。
    套接 的 远程端口 为 远程端口。
    套接 的 状态 为 状态_已连接。
    连接数 为 连接数 加 1。
    返还 0。
完。

套接_发送 接受 描述符、数据、长度：
    套接 为 做 套接_获取 用 描述符。
    若 套接 等于 0：
        返还 错误_无效参数。
    完。
    若 套接 的 状态 不等于 状态_已连接 并且 套接 的 类型 等于 套接_流：
        返还 错误_未连接。
    完。
    做 内存_复制 用 套接 的 发送缓冲 加 套接 的 发送字节、数据、长度。
    套接 的 发送字节 为 套接 的 发送字节 加 长度。
    总发送字节 为 总发送字节 加 长度。
    返还 长度。
完。

套接_接收 接受 描述符、缓冲、最大长：
    套接 为 做 套接_获取 用 描述符。
    若 套接 等于 0：
        返还 错误_无效参数。
    完。
    若 套接 的 接收字节 等于 0：
        返还 0。
    完。
    可读 为 套接 的 接收字节。
    若 可读 大于 最大长：
        可读 为 最大长。
    完。
    做 内存_复制 用 缓冲、套接 的 接收缓冲、可读。
    若 可读 小于 套接 的 接收字节：
        做 内存_复制 用 套接 的 接收缓冲、套接 的 接收缓冲 加 可读、套接 的 接收字节 减 可读。
    完。
    套接 的 接收字节 为 套接 的 接收字节 减 可读。
    总接收字节 为 总接收字节 加 可读。
    返还 可读。
完。

套接_关闭 接受 描述符：
    套接 为 做 套接_获取 用 描述符。
    若 套接 等于 0：
        返还 错误_无效参数。
    完。
    套接 的 引用计数 为 套接 的 引用计数 减 1。
    若 套接 的 引用计数 小于或等于 0：
        套接 的 状态 为 状态_已关闭。
        若 套接 的 发送缓冲 不等于 0：
            做 堆_释放 用 套接 的 发送缓冲。
        完。
        若 套接 的 接收缓冲 不等于 0：
            做 堆_释放 用 套接 的 接收缓冲。
        完。
        做 内存_写64 用 套接字表 加 描述符 乘 8、0。
        做 堆_释放 用 套接。
    完。
    关闭数 为 关闭数 加 1。
    返还 0。
完。

套接_获取 接受 描述符：
    若 描述符 大于或等于 套接字数：
        返还 0。
    完。
    返还 做 内存_读64 用 套接字表 加 描述符 乘 8。
完。

套接_投递数据 接受 描述符、数据、长度：
    套接 为 做 套接_获取 用 描述符。
    若 套接 等于 0：
        返还。
    完。
    若 套接 的 接收字节 加 长度 小于或等于 65536：
        做 内存_复制 用 套接 的 接收缓冲 加 套接 的 接收字节、数据、长度。
        套接 的 接收字节 为 套接 的 接收字节 加 长度。
    完。
完。

套接_活跃数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 套接字数：
        若 做 内存_读64 用 套接字表 加 序号 乘 8 不等于 0：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

套接层_完整统计 接受 零参：
    返还 一组：创建数、关闭数、总发送字节、总接收字节、连接数、监听数、做 套接_活跃数。
完。

可变 AI_套接层完整_带宽估计 为 0。

可变 AI_套接层完整_丢包窗口 为 一组：（空）。

AI_套接层完整_带宽预测 接受 发送字节、耗时毫秒：
    若 耗时毫秒 等于 0：
        返还 AI_套接层完整_带宽估计。
    完。
    当前 为 发送字节 乘 1000 除以 耗时毫秒。
    AI_套接层完整_带宽估计 为 AI_套接层完整_带宽估计 乘 700 除以 1000 加 当前 乘 300 除以 1000。
    返还 AI_套接层完整_带宽估计。
完。

AI_套接层完整_拥塞建议 接受 丢包率：
    做 追加 用 AI_套接层完整_丢包窗口、丢包率。
    若 做 计数 用 AI_套接层完整_丢包窗口 大于 10：
        做 列表_移除 用 AI_套接层完整_丢包窗口、0。
    完。
    若 丢包率 大于 50：
        返还 「减半窗口」。
    完。
    若 丢包率 小于 5：
        返还 「增大窗口」。
    完。
    返还 「保持」。
完。

本章 收束。
