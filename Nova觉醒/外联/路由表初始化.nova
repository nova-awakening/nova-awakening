《路由表初始化》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·同步原语》。

路由类型_单播 为 1。

路由类型_本地 为 2。

路由类型_广播 为 3。

路由类型_丢弃 为 8。

路由类型_不可达 为 7。

路由类型_禁止 为 6。

路由范围_全局 为 0。

路由范围_站点 为 200。

路由范围_链路 为 253。

路由范围_主机 为 254。

路由范围_无处 为 255。

路由协议_内核 为 2。

路由协议_启动 为 3。

路由协议_静态 为 4。

路由标志_网关 为 0x0002。

路由标志_主机 为 0x0004。

路由标志_拒绝 为 0x0200。

路由标志_动态 为 0x0010。

路由标志_已修改 为 0x0020。

路由标志_默认 为 0x8000。

路由表_本地 为 255。

路由表_主 为 254。

路由表_默认 为 253。

路由条目 是 一种 事物，含 目标网络、子网掩码、前缀长度、网关、设备、类型、范围、协议、标志、度量、优先级、引用计数、使用计数、最后使用时间、下一个。

FIB路由表 是 一种 事物，含 表ID、条目链表、条目数、默认路由、锁。

可变 主路由表 为 新建 FIB路由表。

可变 本地路由表 为 新建 FIB路由表。

路由缓存条目 是 一种 事物，含 目标IP、源IP、网关、设备、类型、有效、过期时间。

路由缓存_桶数 为 256。

路由缓存_超时 为 300000。

可变 路由缓存表 为 做 数组 用 路由缓存条目、256。

路由表_初始化 接受 零参：
    主路由表 的 表ID 为 路由表_主。
    主路由表 的 条目链表 为 空。
    主路由表 的 条目数 为 0。
    主路由表 的 默认路由 为 空。
    主路由表 的 锁 为 做 读写自旋锁_创建。
    本地路由表 的 表ID 为 路由表_本地。
    本地路由表 的 条目链表 为 空。
    本地路由表 的 条目数 为 0。
    本地路由表 的 锁 为 做 读写自旋锁_创建。
    可变 i 为 0。
    只要 i 小于 路由缓存_桶数：
        路由缓存表 的 第 i 项 的 有效 为 假。
        i 为 i 加 1。
    完。
    做 内核信息 用 「路由表初始化完成\n」。
完。

本章 收束。
