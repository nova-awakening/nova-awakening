《UDP临时端口》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·同步原语》。

引用《.·UDP校验和》。

引用《.·网络缓冲读》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《.·网络设备诊断》。

引用《..·中枢·哈希函数》。

引用《.·IP发送》。

数报_临时端口起始 为 32768。

数报_临时端口结束 为 60999。

可变 udp_下一个端口 为 32768。

udp_分配临时端口 接受 零参：
    可变 尝试 为 0。
    只要 尝试 小于 数报_临时端口结束 减 数报_临时端口起始：
        端口 为 udp_下一个端口。
        udp_下一个端口 为 udp_下一个端口 加 1。
        若 udp_下一个端口 大于 数报_临时端口结束：
            udp_下一个端口 为 数报_临时端口起始。
        完。
        若 做 udp_查找_本地 用 端口、0 等于 空：
            返还 端口。
        完。
        尝试 为 尝试 加 1。
    完。
    返还 0。
完。

udp_计算校验和 接受 udp头、长度、源IP、目标IP：
    做 网络序_写u16 用 udp头、数报_偏移_校验和、0。
    可变 和 为 0。
    和 为 和 加 (源IP 右移 16) 加 (源IP 位与 0xFFFF)。
    和 为 和 加 (目标IP 右移 16) 加 (目标IP 位与 0xFFFF)。
    和 为 和 加 IP协议_数报。
    和 为 和 加 长度。
    可变 i 为 0。
    只要 i 小于 长度 减 1：
        和 为 和 加 (udp头 的 第 i 项 左移 8) 加 udp头 的 第 i 加 1 项。
        i 为 i 加 2。
    完。
    若 长度 取余于 2 不等于 0：
        和 为 和 加 (udp头 的 第 长度 减 1 项 左移 8)。
    完。
    只要 和 大于 0xFFFF：
        和 为 (和 右移 16) 加 (和 位与 0xFFFF)。
    完。
    结果 为 按位取反 和 位与 0xFFFF。
    若 结果 等于 0：
        结果 为 0xFFFF。
    完。
    做 网络序_写u16 用 udp头、数报_偏移_校验和、结果。
完。

udp_验证校验和 接受 udp头、长度、源IP、目标IP：
    可变 和 为 0。
    和 为 和 加 (源IP 右移 16) 加 (源IP 位与 0xFFFF)。
    和 为 和 加 (目标IP 右移 16) 加 (目标IP 位与 0xFFFF)。
    和 为 和 加 IP协议_数报。
    和 为 和 加 长度。
    可变 i 为 0。
    只要 i 小于 长度 减 1：
        和 为 和 加 (udp头 的 第 i 项 左移 8) 加 udp头 的 第 i 加 1 项。
        i 为 i 加 2。
    完。
    若 长度 取余于 2 不等于 0：
        和 为 和 加 (udp头 的 第 长度 减 1 项 左移 8)。
    完。
    只要 和 大于 0xFFFF：
        和 为 (和 右移 16) 加 (和 位与 0xFFFF)。
    完。
    返还 和 位与 0xFFFF 等于 0xFFFF。
完。

udp_poll 接受 sock：
    可变 事件 为 Nova事件轮询OUT。
    若 非 做 Nova网络缓冲_队列_为空 用 sock 的 接收缓冲：
        事件 为 事件 位或 Nova事件轮询IN 位或 Nova事件轮询RDNORM。
    完。
    返还 事件。
完。

数报_完整统计 接受 零参：
    返还 一组：数报_发送包数、数报_接收包数、数报_丢弃包数。
完。

NUDP_端口哈希大小 为 256。

NUDP_哈希系数 为 2654435761。

NUDP_最大分片 为 64。

NUDP_分片超时 为 30000。

NUDP_多播最大组 为 32。

AI_NUDP_特征维度 为 8。

可变 NUDP_发送包 为 0。

可变 NUDP_接收包 为 0。

可变 NUDP_丢弃包 为 0。

可变 NUDP_校验和失败 为 0。

可变 NUDP_分片重组数 为 0。

可变 NUDP_多播数 为 0。

UDP端口节点 是 一种 事物，含 本地端口 为 0、本地IP 为 0、远端端口 为 0、远端IP 为 0、接收队列 为 空、发送队列 为 空、回调 为 空、下一个 为 空。

可变 NUDP_端口表 为 一组：（空）。

NUDP_端口哈希 接受 端口：
    返还 端口 乘 NUDP_哈希系数 右移 24 位与 NUDP_端口哈希大小 减 1。
完。

NUDP_初始化端口表 接受 零参：
    可变 i 为 0。
    只要 i 小于 NUDP_端口哈希大小：
        NUDP_端口表 的 第 i 项 为 空。
        i 为 i 加 1。
    完。
完。

NUDP_查找端口 接受 端口、ip：
    索引 为 做 NUDP_端口哈希 用 端口。
    可变 节点 为 NUDP_端口表 的 第 索引 项。
    只要 节点 不等于 空：
        若 节点 的 本地端口 等于 端口：
            若 ip 等于 0 或者 节点 的 本地IP 等于 0 或者 节点 的 本地IP 等于 ip：
                返还 节点。
            完。
        完。
        节点 为 节点 的 下一个。
    完。
    返还 空。
完。

NUDP_绑定端口 接受 端口、ip、回调：
    若 做 NUDP_查找端口 用 端口、ip 不等于 空：
        返还 错误_地址已使用。
    完。
    节点 为 新建 UDP端口节点：本地端口 为 端口、本地IP 为 ip、远端端口 为 0、远端IP 为 0、接收队列 为 空、发送队列 为 空、回调 为 回调、下一个 为 空。
    索引 为 做 NUDP_端口哈希 用 端口。
    节点 的 下一个 为 NUDP_端口表 的 第 索引 项。
    NUDP_端口表 的 第 索引 项 为 节点。
    返还 0。
完。

NUDP_释放端口 接受 端口、ip：
    索引 为 做 NUDP_端口哈希 用 端口。
    可变 前 为 空。
    可变 当前 为 NUDP_端口表 的 第 索引 项。
    只要 当前 不等于 空：
        若 当前 的 本地端口 等于 端口 并且 当前 的 本地IP 等于 ip：
            若 前 等于 空：
                NUDP_端口表 的 第 索引 项 为 当前 的 下一个。
            否则：
                前 的 下一个 为 当前 的 下一个。
            完。
            返还 0。
        完。
        前 为 当前。
        当前 为 当前 的 下一个。
    完。
    返还 错误_未找到。
完。

NUDP_构建报文 接受 源端口、目标端口、数据、数据长：
    总长 为 8 加 数据长。
    报文 为 做 分配内存 用 总长。
    若 报文 等于 空：
        返还 空。
    完。
    做 网络序_写u16 用 报文、0、源端口。
    做 网络序_写u16 用 报文、2、目标端口。
    做 网络序_写u16 用 报文、4、总长。
    做 网络序_写u16 用 报文、6、0。
    做 内存复制 用 报文 加 8、数据、数据长。
    返还 报文。
完。

NUDP_发送 接受 源IP、目标IP、源端口、目标端口、数据、数据长：
    报文 为 做 NUDP_构建报文 用 源端口、目标端口、数据、数据长。
    若 报文 等于 空：
        返还 错误_内存不足。
    完。
    总长 为 8 加 数据长。
    做 udp_计算校验和 用 报文、总长、源IP、目标IP。
    结果 为 做 IP_发送 用 源IP、目标IP、IP协议_数报、报文、总长。
    做 释放内存 用 报文。
    NUDP_发送包 为 NUDP_发送包 加 1。
    返还 结果。
完。

NUDP_接收 接受 源IP、目标IP、报文、长度：
    校验 为 做 网络序_读u16 用 报文、6。
    若 校验 不等于 0：
        若 做 udp_验证校验和 用 报文、长度、源IP、目标IP 等于 假：
            NUDP_校验和失败 为 NUDP_校验和失败 加 1。
            返还。
        完。
    完。
    目标端口 为 做 网络序_读u16 用 报文、2。
    源端口 为 做 网络序_读u16 用 报文、0。
    数据长 为 做 网络序_读u16 用 报文、4 减 8。
    节点 为 做 NUDP_查找端口 用 目标端口、目标IP。
    若 节点 等于 空：
        节点 为 做 NUDP_查找端口 用 目标端口、0。
    完。
    若 节点 等于 空：
        NUDP_丢弃包 为 NUDP_丢弃包 加 1。
        做 NUDP_发送ICMP不可达 用 源IP、报文、长度。
        返还。
    完。
    若 节点 的 回调 不等于 空：
        做 节点 的 回调 用 源IP、源端口、报文 加 8、数据长。
    完。
    NUDP_接收包 为 NUDP_接收包 加 1。
完。

NUDP_发送ICMP不可达 接受 目标IP、原始报文、长度：
    复制长 为 8。
    若 长度 小于 复制长：
        复制长 为 长度。
    完。
    做 ICMP_发送_目标不可达 用 目标IP、3、3、原始报文、复制长。
完。

UDP分片记录 是 一种 事物，含 标识 为 0、源IP 为 0、已收位图 为 0、总分片数 为 0、缓冲区 为 空、到期时间 为 0、有效 为 假。

可变 NUDP_分片表 为 一组：（空）。

可变 NUDP_分片表数 为 0。

NUDP_接收分片 接受 源IP、标识、偏移、更多分片、数据、数据长：
    可变 记录 为 空。
    可变 i 为 0。
    只要 i 小于 NUDP_分片表数：
        若 NUDP_分片表 的 第 i 项 的 有效 等于 真 并且 NUDP_分片表 的 第 i 项 的 标识 等于 标识 并且 NUDP_分片表 的 第 i 项 的 源IP 等于 源IP：
            记录 为 NUDP_分片表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 记录 等于 空：
        若 NUDP_分片表数 大于或等于 NUDP_最大分片：
            NUDP_丢弃包 为 NUDP_丢弃包 加 1。
            返还。
        完。
        记录 为 新建 UDP分片记录：标识 为 标识、源IP 为 源IP、已收位图 为 0、总分片数 为 0、缓冲区 为 做 分配内存 用 65536、到期时间 为 做 获取时间戳 加 NUDP_分片超时、有效 为 真。
        NUDP_分片表 的 第 NUDP_分片表数 项 为 记录。
        NUDP_分片表数 为 NUDP_分片表数 加 1。
    完。
    做 内存复制 用 记录 的 缓冲区 加 偏移、数据、数据长。
    分片号 为 偏移 除以 8。
    记录 的 已收位图 为 记录 的 已收位图 位或 1 左移 分片号。
    若 更多分片 等于 假：
        记录 的 总分片数 为 分片号 加 1。
    完。
    若 记录 的 总分片数 大于 0：
        期望 为 (1 左移 记录 的 总分片数) 减 1。
        若 记录 的 已收位图 等于 期望：
            NUDP_分片重组数 为 NUDP_分片重组数 加 1。
            做 NUDP_接收 用 源IP、0、记录 的 缓冲区、偏移 加 数据长。
            做 释放内存 用 记录 的 缓冲区。
            记录 的 有效 为 假。
        完。
    完。
完。

NUDP_清理过期分片 接受 零参：
    现在 为 做 获取时间戳。
    可变 i 为 0。
    只要 i 小于 NUDP_分片表数：
        若 NUDP_分片表 的 第 i 项 的 有效 等于 真 并且 现在 大于 NUDP_分片表 的 第 i 项 的 到期时间：
            做 释放内存 用 NUDP_分片表 的 第 i 项 的 缓冲区。
            NUDP_分片表 的 第 i 项 的 有效 为 假。
            NUDP_丢弃包 为 NUDP_丢弃包 加 1。
        完。
        i 为 i 加 1。
    完。
完。

多播组 是 一种 事物，含 组IP 为 0、端口 为 0、接口索引 为 0、有效 为 假。

可变 NUDP_多播组表 为 一组：（空）。

可变 NUDP_多播组数 为 0。

NUDP_加入多播组 接受 组IP、端口、接口：
    若 NUDP_多播组数 大于或等于 NUDP_多播最大组：
        返还 错误_资源耗尽。
    完。
    可变 i 为 0。
    只要 i 小于 NUDP_多播组数：
        若 NUDP_多播组表 的 第 i 项 的 组IP 等于 组IP 并且 NUDP_多播组表 的 第 i 项 的 端口 等于 端口：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    NUDP_多播组表 的 第 NUDP_多播组数 项 为 新建 多播组：组IP 为 组IP、端口 为 端口、接口索引 为 接口、有效 为 真。
    NUDP_多播组数 为 NUDP_多播组数 加 1。
    返还 0。
完。

NUDP_离开多播组 接受 组IP、端口：
    可变 i 为 0。
    只要 i 小于 NUDP_多播组数：
        若 NUDP_多播组表 的 第 i 项 的 组IP 等于 组IP 并且 NUDP_多播组表 的 第 i 项 的 端口 等于 端口：
            NUDP_多播组表 的 第 i 项 的 有效 为 假。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 错误_未找到。
完。

NUDP_多播发送 接受 组IP、源端口、目标端口、数据、长度：
    NUDP_多播数 为 NUDP_多播数 加 1。
    返还 做 NUDP_发送 用 0、组IP、源端口、目标端口、数据、长度。
完。

NUDP_AI分析 接受 零参：
    特征 为 做 Tensor_创建 用 AI_NUDP_特征维度。
    若 特征 等于 空：
        返还。
    完。
    做 Tensor_写 用 特征、0、NUDP_发送包。
    做 Tensor_写 用 特征、1、NUDP_接收包。
    做 Tensor_写 用 特征、2、NUDP_丢弃包。
    做 Tensor_写 用 特征、3、NUDP_校验和失败。
    做 Tensor_写 用 特征、4、NUDP_分片重组数。
    做 Tensor_写 用 特征、5、NUDP_多播数。
    做 Tensor_写 用 特征、6、NUDP_分片表数。
    做 Tensor_写 用 特征、7、NUDP_多播组数。
    结果 为 做 AI总线_推理 用 「UDP流量预测」、特征。
    若 结果 大于 80：
        做 内核警告 用 「NUDP-AI: 流量异常分值=%d」、结果。
    完。
    做 Tensor_释放 用 特征。
完。

NUDP_诊断报告 接受 零参：
    做 内核打印 用 「═══ NUDP 数据报协议统计 ═══\n」。
    做 内核打印 用 「发送: %d  接收: %d  丢弃: %d\n」、NUDP_发送包、NUDP_接收包、NUDP_丢弃包。
    做 内核打印 用 「校验和失败: %d  分片重组: %d  多播: %d\n」、NUDP_校验和失败、NUDP_分片重组数、NUDP_多播数。
完。

NUDP_全局初始化 接受 零参：
    做 NUDP_初始化端口表。
    NUDP_发送包 为 0。
    NUDP_接收包 为 0。
    NUDP_丢弃包 为 0。
    NUDP_校验和失败 为 0。
    NUDP_分片重组数 为 0。
    NUDP_多播数 为 0。
    NUDP_分片表数 为 0。
    NUDP_多播组数 为 0。
    NUDP_速率计数 为 0。
    NUDP_速率窗口开始 为 做 获取时间戳。
    做 内核信息 用 「NUDP: 数据报协议引擎已初始化」。
    做 NUDP_AI分析。
完。

NUDP_最大速率 为 10000。

NUDP_速率窗口 为 1000。

可变 NUDP_速率计数 为 0。

可变 NUDP_速率窗口开始 为 0。

可变 NUDP_被限速包 为 0。

NUDP_检查速率 接受 零参：
    现在 为 做 获取时间戳。
    若 现在 减 NUDP_速率窗口开始 大于或等于 NUDP_速率窗口：
        NUDP_速率计数 为 0。
        NUDP_速率窗口开始 为 现在。
    完。
    NUDP_速率计数 为 NUDP_速率计数 加 1。
    若 NUDP_速率计数 大于 NUDP_最大速率：
        NUDP_被限速包 为 NUDP_被限速包 加 1。
        返还 假。
    完。
    返还 真。
完。

NUDP_广播发送 接受 源端口、目标端口、数据、长度：
    返还 做 NUDP_发送 用 0、0xFFFFFFFF、源端口、目标端口、数据、长度。
完。

NUDP_子网广播发送 接受 源IP、子网掩码、源端口、目标端口、数据、长度：
    广播IP 为 源IP 位与 子网掩码 位或 按位取反 子网掩码 位与 0xFFFFFFFF。
    返还 做 NUDP_发送 用 源IP、广播IP、源端口、目标端口、数据、长度。
完。

NUDP_socket创建 接受 零参：
    端口 为 做 udp_分配临时端口。
    若 端口 等于 0：
        返还 空。
    完。
    结果 为 做 NUDP_绑定端口 用 端口、0、空。
    若 结果 不等于 0：
        返还 空。
    完。
    返还 端口。
完。

NUDP_socket绑定 接受 sock、端口、ip：
    做 NUDP_释放端口 用 sock、0。
    返还 做 NUDP_绑定端口 用 端口、ip、空。
完。

NUDP_socket发送到 接受 sock、目标IP、目标端口、数据、长度：
    若 做 NUDP_检查速率 等于 假：
        返还 错误_资源耗尽。
    完。
    返还 做 NUDP_发送 用 0、目标IP、sock、目标端口、数据、长度。
完。

NUDP_socket关闭 接受 sock：
    返还 做 NUDP_释放端口 用 sock、0。
完。

本章 收束。
