《导航系统》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·HTTP请求构造》。

引用《.·智能浏览功能》。

引用《.·HTTP发送与解析》。

引用《..·中枢·内核打印》。

引用《..·稳态·原生样式系统》。

标签页_导航 接受 标签ID、URL：
    tab 为 做 标签页_查找 用 标签ID。
    若 tab 等于 空：
        返还。
    完。
    tab 的 URL 为 URL。
    tab 的 状态 为 标签状态_加载中。
    tab 的 加载进度 为 0。
    tab 的 安全 为 做 字符串前缀_浏览器 用 URL、「https://」。
    若 tab 的 历史位置 小于 做 计数 用 tab 的 历史 减 1：
        只要 做 计数 用 tab 的 历史 大于 tab 的 历史位置 加 1：
            做 删除 用 tab 的 历史、做 计数 用 tab 的 历史 减 1。
        完。
    完。
    做 追加 用 tab 的 历史、URL。
    tab 的 历史位置 为 做 计数 用 tab 的 历史 减 1。
    若 做 计数 用 全局历史 大于或等于 最大历史：
        做 删除 用 全局历史、0。
    完。
    做 追加 用 全局历史、一组：URL、浏览器_时钟。
    做 页面_加载 用 tab、URL。
完。

标签页_后退 接受 标签ID：
    tab 为 做 标签页_查找 用 标签ID。
    若 tab 等于 空 或者 tab 的 历史位置 小于或等于 0：
        返还。
    完。
    tab 的 历史位置 为 tab 的 历史位置 减 1。
    做 页面_加载 用 tab、tab 的 历史 的 第 tab 的 历史位置 项。
完。

标签页_前进 接受 标签ID：
    tab 为 做 标签页_查找 用 标签ID。
    若 tab 等于 空 或者 tab 的 历史位置 大于或等于 做 计数 用 tab 的 历史 减 1：
        返还。
    完。
    tab 的 历史位置 为 tab 的 历史位置 加 1。
    做 页面_加载 用 tab、tab 的 历史 的 第 tab 的 历史位置 项。
完。

标签页_刷新 接受 标签ID：
    tab 为 做 标签页_查找 用 标签ID。
    若 tab 等于 空：
        返还。
    完。
    做 页面_加载 用 tab、tab 的 URL。
完。

页面_加载 接受 tab、URL：
    tab 的 加载进度 为 5。
    做 内核调试 用 「浏览器: 加载 」 加 URL。
    tab 的 加载进度 为 10。
    IP 为 做 浏览器_域名查询 用 URL。
    tab 的 加载进度 为 20。
    req 为 做 HTTP_创建请求 用 0、URL。
    响应 为 做 HTTP_发送 用 req。
    若 非 响应 的 成功：
        tab 的 状态 为 标签状态_错误。
        tab 的 标题 为 「加载失败」。
        tab 的 加载进度 为 100。
        返还。
    完。
    tab 的 加载进度 为 50。
    tab 的 DOM根节点 为 做 HTML_解析 用 响应 的 正文。
    tab 的 加载进度 为 70。
    tab 的 标题 为 做 页面_提取标题 用 tab 的 DOM根节点。
    做 页面_应用样式 用 tab 的 DOM根节点。
    tab 的 加载进度 为 85。
    做 布局_计算 用 tab 的 DOM根节点、0、0、渲染_宽、渲染_高。
    tab 的 加载进度 为 100。
    tab 的 状态 为 标签状态_完成。
    做 智能_分析页面 用 tab。
    做 内核调试 用 「浏览器: 加载完成 '」 加 tab 的 标题 加 「'」。
完。

页面_提取标题 接受 dom：
    若 dom 等于 空：
        返还 「无标题」。
    完。
    标题列表 为 做 HTML_get元素sByTag 用 dom、10。
    若 做 计数 用 标题列表 大于 0：
        返还 做 DOM_提取文本 用 标题列表 的 第 0 项。
    完。
    返还 「无标题」。
完。

页面_应用样式 接受 节点：
    若 节点 等于 空：
        返还。
    完。
    节点 的 样式 为 做 CSS_默认样式 用 节点 的 标签。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点 的 子节点：
        做 页面_应用样式 用 节点 的 子节点 的 第 i 项。
        i 为 i 加 1。
    完。
完。

浏览器_域名查询 接受 URL：
    返还 「0.0.0.0」。
完。

本章 收束。
