《TCP会话记录》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·TCP校验和》。

引用《.·TCP重传RTT》。

引用《.·套接缓冲管理》。

引用《.·TCP连接分配》。

引用《.·TCP拥塞控制》。

引用《.·TCP接收》。

引用《.·传控核心》。

引用《.·UDP校验和》。

引用《.·TCP监听》。

引用《.·传控拥塞控制》。

引用《.·传控状态机》。

引用《.·UDP端口管理》。

引用《.·TCP连接表》。

引用《.·UDP临时端口》。

引用《.·AI拥塞预测》。

引用《.·UDP哈希》。

引用《.·TCP发送数据》。

引用《.·UDP发送》。

引用《.·TCP定时器》。

引用《.·TCP关闭》。

传输档 是 一种 事物，含 名称、当前控制块、当前套接、当前缓冲、当前数报、最近任务、最近输入、最近输出、最近预测、最近统计、最近错误、初始化完成、传控数、套接数、数据报数、定时数、拥塞数、缓冲数、命中数、失败数、健康。

传输请求 是 一种 事物，含 本地IP、本地端口、远端IP、远端端口、数据、长度、标志、超时。

传输回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

传输摘要 是 一种 事物，含 名称、传控数、套接数、数据报数、定时数、拥塞数、缓冲数、命中数、失败数、健康。

传输套接 是 一种 事物，含 名称、本地IP、本地端口、远端IP、远端端口、tcp控制块、缓冲。

可变 传输账本 为 一组：（空）。

可变 传输建数 为 0。

可变 传输总动作 为 0。

可变 传输总命中 为 0。

可变 传输总失败 为 0。

可变 传输已初始化 为 假。

可变 传输定时轮 为 0。

传输传输_建档 接受 名称：
    档 为 做 堆_分配 用 传输档。
    档 的 名称 为 名称。
    档 的 当前控制块 为 0。
    档 的 当前套接 为 0。
    档 的 当前缓冲 为 0。
    档 的 当前数报 为 0。
    档 的 最近任务 为 0。
    档 的 最近输入 为 0。
    档 的 最近输出 为 0。
    档 的 最近预测 为 0。
    档 的 最近统计 为 0。
    档 的 最近错误 为 「」。
    档 的 初始化完成 为 假。
    档 的 传控数 为 0。
    档 的 套接数 为 0。
    档 的 数据报数 为 0。
    档 的 定时数 为 0。
    档 的 拥塞数 为 0。
    档 的 缓冲数 为 0。
    档 的 命中数 为 0。
    档 的 失败数 为 0。
    档 的 健康 为 「未初始化」。
    返还 档。
完。

传输传输_登记 接受 档：
    做 追加 用 传输账本、档。
    传输建数 为 传输建数 加 1。
    返还 档。
完。

传输传输_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 做 堆_分配 用 传输回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

传输传输_成功 接受 档、来源、数据：
    档 的 命中数 为 档 的 命中数 加 1。
    档 的 最近错误 为 「」。
    档 的 最近输出 为 数据。
    档 的 健康 为 「运行」。
    传输总动作 为 传输总动作 加 1。
    传输总命中 为 传输总命中 加 1。
    返还 做 传输传输_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

传输传输_失败 接受 档、来源、错因：
    档 的 失败数 为 档 的 失败数 加 1。
    档 的 最近错误 为 错因。
    档 的 健康 为 「降级」。
    传输总动作 为 传输总动作 加 1。
    传输总失败 为 传输总失败 加 1。
    返还 做 传输传输_建回证 用 真、假、来源、错因、0、档 的 最近统计。
完。

传输传输_初始化 接受 档：
    若 非 传输已初始化：
        做 传控连接_初始化。
        做 拥塞控制_初始化。
        做 AI拥塞_初始化。
        做 定时器统计_初始化。
        做 NUDP_全局初始化。
        传输定时轮 为 做 定时器轮_创建 用 256、10。
        传输已初始化 为 真。
    完。
    档 的 初始化完成 为 真。
    档 的 健康 为 「运行」。
    档 的 最近统计 为 做 传输传输_完整统计。
    返还 档。
完。

传输传输_创建 接受 名称：
    档 为 做 传输传输_建档 用 名称。
    做 传输传输_初始化 用 档。
    返还 做 传输传输_登记 用 档。
完。

传输传输_建请求 接受 本地IP、本地端口、远端IP、远端端口、数据、长度、标志、超时：
    请求 为 做 堆_分配 用 传输请求。
    请求 的 本地IP 为 本地IP。
    请求 的 本地端口 为 本地端口。
    请求 的 远端IP 为 远端IP。
    请求 的 远端端口 为 远端端口。
    请求 的 数据 为 数据。
    请求 的 长度 为 长度。
    请求 的 标志 为 标志。
    请求 的 超时 为 超时。
    返还 请求。
完。

传输传输_建套接 接受 名称、本地IP、本地端口、远端IP、远端端口：
    套接 为 做 堆_分配 用 传输套接。
    可变 源端口 为 本地端口。
    若 源端口 等于 0：
        源端口 为 做 传控_分配临时端口。
    完。
    套接 的 名称 为 名称。
    套接 的 本地IP 为 本地IP。
    套接 的 本地端口 为 源端口。
    套接 的 远端IP 为 远端IP。
    套接 的 远端端口 为 远端端口。
    套接 的 tcp控制块 为 做 传控_创建TCB 用 源端口、远端IP、远端端口。
    套接 的 缓冲 为 做 socket内存_创建。
    若 套接 的 tcp控制块 不等于 空：
        做 tcp_初始化定时器 用 套接 的 tcp控制块。
    完。
    返还 套接。
完。

传输传输_登记套接 接受 档、套接：
    档 的 当前套接 为 套接。
    档 的 当前控制块 为 套接 的 tcp控制块。
    档 的 当前缓冲 为 套接 的 缓冲。
    档 的 套接数 为 档 的 套接数 加 1。
    档 的 缓冲数 为 档 的 缓冲数 加 1。
    档 的 传控数 为 档 的 传控数 加 1。
    档 的 最近统计 为 做 传输传输_完整统计。
    返还 套接。
完。

传输传输_创建套接 接受 档、名称、本地IP、本地端口、远端IP、远端端口：
    若 非 档 的 初始化完成：
        做 传输传输_初始化 用 档。
    完。
    套接 为 做 传输传输_建套接 用 名称、本地IP、本地端口、远端IP、远端端口。
    若 套接 等于 空：
        返还 做 传输传输_失败 用 档、「创建套接」、「套接创建失败」。
    完。
    档 的 最近任务 为 做 传输传输_建请求 用 本地IP、套接 的 本地端口、远端IP、远端端口、0、0、0、0。
    做 传输传输_登记套接 用 档、套接。
    返还 做 传输传输_成功 用 档、「创建套接」、套接。
完。

传输传输_创建缓冲 接受 档：
    缓冲 为 做 socket内存_创建。
    若 缓冲 等于 空：
        返还 做 传输传输_失败 用 档、「创建缓冲」、「缓冲创建失败」。
    完。
    档 的 当前缓冲 为 缓冲。
    档 的 缓冲数 为 档 的 缓冲数 加 1。
    档 的 最近统计 为 做 socket_缓冲统计 用 缓冲。
    返还 做 传输传输_成功 用 档、「创建缓冲」、缓冲。
完。

传输传输_调缓冲 接受 档、缓冲、发送大小、接收大小：
    做 socket_设置发送缓冲 用 缓冲、发送大小。
    做 socket_设置接收缓冲 用 缓冲、接收大小。
    做 socket_自动调整缓冲 用 缓冲、「双向」。
    档 的 当前缓冲 为 缓冲。
    档 的 最近统计 为 做 socket_缓冲统计 用 缓冲。
    返还 做 传输传输_成功 用 档、「调缓冲」、档 的 最近统计。
完。

传输传输_缓冲发送 接受 档、缓冲、数据、长度：
    结果 为 做 socket_阻塞发送 用 缓冲、数据、长度、100。
    档 的 最近输出 为 数据。
    档 的 最近统计 为 做 socket_缓冲统计 用 缓冲。
    若 结果 小于 0：
        返还 做 传输传输_失败 用 档、「缓冲发送」、「发送缓冲写入失败」。
    完。
    返还 做 传输传输_成功 用 档、「缓冲发送」、结果。
完。

传输传输_缓冲接收 接受 档、缓冲、超时：
    数据 为 做 socket_阻塞接收 用 缓冲、超时。
    档 的 最近输入 为 数据。
    档 的 最近统计 为 做 socket_缓冲统计 用 缓冲。
    若 数据 等于 空：
        返还 做 传输传输_失败 用 档、「缓冲接收」、「接收缓冲为空」。
    完。
    返还 做 传输传输_成功 用 档、「缓冲接收」、数据。
完。

传输传输_保存缓冲 接受 档、缓冲：
    快照 为 做 socket_保存快照 用 缓冲。
    档 的 最近统计 为 做 socket_缓冲统计 用 缓冲。
    返还 做 传输传输_成功 用 档、「保存缓冲」、快照。
完。

传输传输_恢复缓冲 接受 档、缓冲、快照：
    做 socket_恢复快照 用 缓冲、快照。
    档 的 最近统计 为 做 socket_缓冲统计 用 缓冲。
    返还 做 传输传输_成功 用 档、「恢复缓冲」、档 的 最近统计。
完。

传输传输_监听 接受 档、套接、积压数：
    结果 为 做 tcp_监听 用 套接、积压数。
    档 的 当前套接 为 套接。
    档 的 当前控制块 为 套接 的 tcp控制块。
    档 的 最近统计 为 做 传控连接_完整统计。
    若 结果 不等于 0：
        返还 做 传输传输_失败 用 档、「监听」、「监听失败」。
    完。
    返还 做 传输传输_成功 用 档、「监听」、套接 的 tcp控制块。
完。

传输传输_接受 接受 档、监听控制块：
    结果 为 做 tcp_接受 用 监听控制块。
    档 的 最近统计 为 做 传控连接_完整统计。
    若 结果 等于 空 或者 结果 小于 0：
        返还 做 传输传输_失败 用 档、「接受」、「没有待接入连接」。
    完。
    档 的 当前控制块 为 结果。
    返还 做 传输传输_成功 用 档、「接受」、结果。
完。

传输传输_连接 接受 档、套接、目标IP、目标端口：
    结果 为 做 tcp_连接 用 套接、目标IP、目标端口。
    档 的 当前套接 为 套接。
    档 的 当前控制块 为 套接 的 tcp控制块。
    档 的 最近统计 为 做 传控连接_完整统计。
    若 结果 不等于 0：
        返还 做 传输传输_失败 用 档、「连接」、「主动连接失败」。
    完。
    返还 做 传输传输_成功 用 档、「连接」、套接 的 tcp控制块。
完。

传输传输_查连接 接受 档、本地端口、远端地址、远端端口：
    索引 为 做 传控_查找连接 用 本地端口、远端地址、远端端口。
    档 的 最近统计 为 做 传控连接_完整统计。
    若 索引 小于 0：
        返还 做 传输传输_失败 用 档、「查连接」、「连接不存在」。
    完。
    返还 做 传输传输_成功 用 档、「查连接」、索引。
完。

传输传输_关连接 接受 档、控制块：
    结果 为 做 tcp_关闭 用 控制块。
    档 的 当前控制块 为 控制块。
    档 的 最近统计 为 做 传控连接_完整统计。
    若 结果 不等于 0：
        返还 做 传输传输_失败 用 档、「关连接」、「关闭失败」。
    完。
    返还 做 传输传输_成功 用 档、「关连接」、控制块。
完。

传输传输_输入处理 接受 档、Nova缓冲：
    结果 为 做 tcp_输入处理 用 Nova缓冲。
    档 的 最近输入 为 Nova缓冲。
    档 的 最近统计 为 做 传控状态机_完整统计。
    若 结果 不等于 0：
        返还 做 传输传输_失败 用 档、「输入处理」、「传控输入处理失败」。
    完。
    返还 做 传输传输_成功 用 档、「输入处理」、结果。
完。

传输传输_发送数据 接受 档、控制块、数据、长度：
    结果 为 做 tcp_发送数据 用 控制块、数据、长度。
    档 的 当前控制块 为 控制块。
    档 的 最近输出 为 数据。
    档 的 最近统计 为 做 传控数据_完整统计。
    若 结果 小于 0：
        返还 做 传输传输_失败 用 档、「发送数据」、「传控发送失败」。
    完。
    返还 做 传输传输_成功 用 档、「发送数据」、结果。
完。

传输传输_接收数据 接受 档、控制块、缓冲、最大长度：
    结果 为 做 tcp_接收数据 用 控制块、缓冲、最大长度。
    档 的 当前控制块 为 控制块。
    档 的 最近输入 为 结果。
    档 的 最近统计 为 做 传控数据_完整统计。
    若 结果 小于 0：
        返还 做 传输传输_失败 用 档、「接收数据」、「传控接收失败」。
    完。
    返还 做 传输传输_成功 用 档、「接收数据」、结果。
完。

传输传输_处理数据 接受 档、控制块、Nova缓冲、序号：
    结果 为 做 tcp_处理数据 用 控制块、Nova缓冲、序号。
    档 的 最近输入 为 Nova缓冲。
    档 的 最近统计 为 做 传控数据_完整统计。
    若 结果 不等于 0：
        返还 做 传输传输_失败 用 档、「处理数据」、「数据重组失败」。
    完。
    返还 做 传输传输_成功 用 档、「处理数据」、结果。
完。

传输传输_校验段 接受 档、数据、长度、源地址、目标地址：
    结果 为 做 传控_验证校验和 用 数据、长度、源地址、目标地址。
    档 的 最近统计 为 做 传控数据_完整统计。
    若 非 结果：
        返还 做 传输传输_失败 用 档、「校验段」、「校验和失败」。
    完。
    返还 做 传输传输_成功 用 档、「校验段」、真。
完。

传输传输_构段 接受 档、缓冲、源端口、目标端口、序号、确认号、标志、窗口、载荷、载荷长：
    长度 为 做 传控_构建段 用 缓冲、源端口、目标端口、序号、确认号、标志、窗口、载荷、载荷长。
    档 的 最近统计 为 做 传控数据_完整统计。
    若 长度 小于或等于 0：
        返还 做 传输传输_失败 用 档、「构段」、「构段失败」。
    完。
    返还 做 传输传输_成功 用 档、「构段」、长度。
完。

传输传输_解析段 接受 档、数据、长度：
    结果 为 做 传控_解析段 用 数据、长度。
    档 的 最近统计 为 做 传控数据_完整统计。
    若 结果 等于 0：
        返还 做 传输传输_失败 用 档、「解析段」、「段解析失败」。
    完。
    返还 做 传输传输_成功 用 档、「解析段」、结果。
完。

传输传输_发确认 接受 档、缓冲、源端口、目标端口、序号、确认号、窗口、源地址、目标地址：
    结果 为 做 传控_发送确认 用 缓冲、源端口、目标端口、序号、确认号、窗口、源地址、目标地址。
    档 的 最近统计 为 做 传控数据_完整统计。
    返还 做 传输传输_成功 用 档、「发确认」、结果。
完。

传输传输_发同步 接受 档、缓冲、源端口、目标端口、初始序号、源地址、目标地址：
    结果 为 做 传控_发送同步 用 缓冲、源端口、目标端口、初始序号、源地址、目标地址。
    档 的 最近统计 为 做 传控数据_完整统计。
    返还 做 传输传输_成功 用 档、「发同步」、结果。
完。

传输传输_发结束 接受 档、缓冲、源端口、目标端口、序号、确认号、源地址、目标地址：
    结果 为 做 传控_发送结束 用 缓冲、源端口、目标端口、序号、确认号、源地址、目标地址。
    档 的 最近统计 为 做 传控数据_完整统计。
    返还 做 传输传输_成功 用 档、「发结束」、结果。
完。

传输传输_记录拥塞确认 接受 档、控制块、已确认字节：
    做 tcp_拥塞_ACK 用 控制块。
    做 AI拥塞_记录ACK 用 已确认字节。
    档 的 拥塞数 为 档 的 拥塞数 加 1。
    档 的 最近预测 为 做 AI拥塞_统计。
    档 的 最近统计 为 做 传控拥塞_完整统计。
    返还 做 传输传输_成功 用 档、「拥塞确认」、档 的 最近预测。
完。

传输传输_记录丢包 接受 档、控制块：
    做 tcp_拥塞_丢包 用 控制块。
    做 AI拥塞_记录丢包。
    档 的 拥塞数 为 档 的 拥塞数 加 1。
    档 的 最近预测 为 做 AI拥塞_统计。
    档 的 最近统计 为 做 传控拥塞_完整统计。
    返还 做 传输传输_成功 用 档、「记录丢包」、档 的 最近预测。
完。

传输传输_记录时延 接受 档、控制块、Nova往返时间：
    做 tcp_更新Nova往返时间 用 控制块、Nova往返时间。
    做 AI拥塞_记录Nova往返时间 用 Nova往返时间。
    档 的 拥塞数 为 档 的 拥塞数 加 1。
    档 的 最近预测 为 做 AI拥塞_统计。
    档 的 最近统计 为 做 传控拥塞_完整统计。
    返还 做 传输传输_成功 用 档、「记录时延」、档 的 最近预测。
完。

传输传输_预测拥塞 接受 档、Nova往返时间、是否丢包：
    做 AI拥塞_记录Nova往返时间 用 Nova往返时间。
    若 是否丢包：
        做 AI拥塞_记录丢包。
    完。
    做 AI拥塞_更新窗口 用 是否丢包。
    档 的 最近预测 为 做 AI拥塞_完整统计。
    档 的 最近统计 为 做 传控拥塞_完整统计。
    返还 做 传输传输_成功 用 档、「预测拥塞」、档 的 最近预测。
完。

传输传输_推荐算法 接受 档、丢包率、延迟、带宽：
    推荐 为 做 拥塞_AI推荐算法 用 丢包率、延迟、带宽。
    档 的 最近预测 为 一组：推荐、做 列出拥塞算法。
    档 的 最近统计 为 做 传控拥塞_完整统计。
    返还 做 传输传输_成功 用 档、「推荐算法」、档 的 最近预测。
完。

传输传输_拥塞摘要 接受 档：
    摘要 为 一组：做 传控拥塞_完整统计、做 AI拥塞_完整统计、做 AI拥塞_获取窗口、做 AI拥塞_获取模式、做 传控_重传率、做 传控_丢包率。
    档 的 最近预测 为 摘要。
    档 的 最近统计 为 摘要。
    返还 做 传输传输_成功 用 档、「拥塞摘要」、摘要。
完。

传输传输_启动延确认 接受 档、控制块：
    做 tcp_启动延迟ACK 用 控制块。
    档 的 定时数 为 档 的 定时数 加 1。
    档 的 最近统计 为 做 tcp定时器_完整统计。
    返还 做 传输传输_成功 用 档、「启动延确认」、档 的 最近统计。
完。

传输传输_启动保活 接受 档、控制块：
    做 tcp_启动保活定时器 用 控制块。
    档 的 定时数 为 档 的 定时数 加 1。
    档 的 最近统计 为 做 tcp定时器_完整统计。
    返还 做 传输传输_成功 用 档、「启动保活」、档 的 最近统计。
完。

传输传输_重置保活 接受 档、控制块：
    做 tcp_重置保活定时器 用 控制块。
    档 的 定时数 为 档 的 定时数 加 1。
    档 的 最近统计 为 做 tcp定时器_完整统计。
    返还 做 传输传输_成功 用 档、「重置保活」、档 的 最近统计。
完。

传输传输_启动时等待 接受 档、控制块：
    做 tcp_启动TIME_WAIT定时器 用 控制块。
    档 的 定时数 为 档 的 定时数 加 1。
    档 的 最近统计 为 做 tcp定时器_完整统计。
    返还 做 传输传输_成功 用 档、「启动时等待」、档 的 最近统计。
完。

传输传输_启动零探 接受 档、控制块：
    做 tcp_启动零窗口探测 用 控制块。
    档 的 定时数 为 档 的 定时数 加 1。
    档 的 最近统计 为 做 tcp定时器_完整统计。
    返还 做 传输传输_成功 用 档、「启动零探」、档 的 最近统计。
完。

传输传输_停止零探 接受 档、控制块：
    做 tcp_停止零窗口探测 用 控制块。
    档 的 定时数 为 档 的 定时数 加 1。
    档 的 最近统计 为 做 tcp定时器_完整统计。
    返还 做 传输传输_成功 用 档、「停止零探」、档 的 最近统计。
完。

传输传输_推进定时 接受 档、当前时间：
    做 定时器轮_推进 用 当前时间。
    档 的 定时数 为 档 的 定时数 加 1。
    档 的 最近统计 为 做 tcp定时器_完整统计。
    返还 做 传输传输_成功 用 档、「推进定时」、档 的 最近统计。
完。

传输传输_重传超时 接受 档、控制块：
    做 tcp_重传超时 用 控制块。
    做 传控_记录重传。
    档 的 最近统计 为 一组：做 传控状态机_完整统计、做 传控数据_完整统计。
    返还 做 传输传输_成功 用 档、「重传超时」、档 的 最近统计。
完。

传输传输_建数报 接受 档：
    套接 为 做 udp_创建socket。
    若 套接 等于 空：
        返还 做 传输传输_失败 用 档、「建数报」、「数报套接创建失败」。
    完。
    档 的 当前数报 为 套接。
    档 的 数据报数 为 档 的 数据报数 加 1。
    档 的 最近统计 为 做 数报扩展_完整统计。
    返还 做 传输传输_成功 用 档、「建数报」、套接。
完。

传输传输_绑数报 接受 档、套接、IP、端口：
    可变 结果 为 做 数报_绑定 用 套接、IP、端口。
    若 结果 不等于 0：
        结果 为 做 udp_绑定 用 套接、IP、端口。
    完。
    档 的 当前数报 为 套接。
    档 的 最近统计 为 一组：做 数报扩展_完整统计、做 数报_完整统计。
    若 结果 不等于 0：
        返还 做 传输传输_失败 用 档、「绑数报」、「数报绑定失败」。
    完。
    返还 做 传输传输_成功 用 档、「绑数报」、端口。
完。

传输传输_发数报 接受 档、套接、数据、长度、目标IP、目标端口：
    结果 为 做 udp_发送 用 套接、数据、长度、目标IP、目标端口。
    档 的 最近输出 为 数据。
    档 的 最近统计 为 一组：做 数报扩展_完整统计、做 数报_完整统计。
    若 结果 小于 0：
        返还 做 传输传输_失败 用 档、「发数报」、「数报发送失败」。
    完。
    返还 做 传输传输_成功 用 档、「发数报」、结果。
完。

传输传输_收数报 接受 档、套接、缓冲、最大长度、源地址：
    结果 为 做 udp_recvmsg 用 套接、缓冲、最大长度、0、源地址。
    档 的 最近输入 为 结果。
    档 的 最近统计 为 一组：做 NURP_获取统计、做 数报_完整统计。
    若 结果 小于 0：
        返还 做 传输传输_失败 用 档、「收数报」、「数报接收失败」。
    完。
    返还 做 传输传输_成功 用 档、「收数报」、结果。
完。

传输传输_加组播 接受 档、套接、组IP、接口索引：
    做 udp_加入组播 用 套接、组IP、接口索引。
    档 的 最近统计 为 一组：做 NURP_获取统计、做 NUDP_诊断报告。
    返还 做 传输传输_成功 用 档、「加组播」、组IP。
完。

传输传输_离组播 接受 档、套接、组IP：
    做 udp_离开组播 用 套接、组IP。
    档 的 最近统计 为 一组：做 NURP_获取统计、做 NUDP_诊断报告。
    返还 做 传输传输_成功 用 档、「离组播」、组IP。
完。

传输传输_数报诊断 接受 档：
    诊断 为 一组：做 数报扩展_完整统计、做 数报_完整统计、做 NURP_获取统计、做 NUDP_诊断报告。
    档 的 最近统计 为 诊断。
    返还 做 传输传输_成功 用 档、「数报诊断」、诊断。
完。

传输传输_连接摘要 接受 档、控制块：
    摘要 为 一组：做 传控_TCB摘要 用 控制块、做 传控_连接摘要 用 控制块、做 传控_状态名 用 控制块 的 状态。
    档 的 当前控制块 为 控制块。
    档 的 最近统计 为 摘要。
    返还 做 传输传输_成功 用 档、「连接摘要」、摘要。
完。

传输传输_摘要 接受 档：
    摘要 为 做 堆_分配 用 传输摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 传控数 为 档 的 传控数。
    摘要 的 套接数 为 档 的 套接数。
    摘要 的 数据报数 为 档 的 数据报数。
    摘要 的 定时数 为 档 的 定时数。
    摘要 的 拥塞数 为 档 的 拥塞数。
    摘要 的 缓冲数 为 档 的 缓冲数。
    摘要 的 命中数 为 档 的 命中数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 档 的 健康。
    返还 摘要。
完。

传输传输_完整统计 接受 零参：
    返还 一组：传输建数、传输总动作、传输总命中、传输总失败、做 传控_完整统计、做 传控状态机_完整统计、做 传控连接_完整统计、做 传控拥塞_完整统计、做 AI拥塞_完整统计、做 传控数据_完整统计、做 socket内存_完整统计、做 数报扩展_完整统计、做 数报_完整统计。
完。

传输传输_健康 接受 档：
    若 非 档 的 初始化完成：
        返还 「未初始化」。
    完。
    若 档 的 失败数 大于 档 的 命中数：
        返还 「降级」。
    完。
    若 档 的 失败数 大于 0：
        返还 「监视」。
    完。
    返还 「运行」。
完。

传输传输_自检 接受 零参：
    档 为 做 传输传输_创建 用 「传输传输」。
    套接回证 为 做 传输传输_创建套接 用 档、「内联传输」、0x7F000001、0、0x7F000001、8080。
    缓冲回证 为 做 传输传输_创建缓冲 用 档。
    可变 缓冲 为 0。
    若 缓冲回证 的 已成：
        缓冲 为 缓冲回证 的 数据。
        做 传输传输_调缓冲 用 档、缓冲、4096、4096。
        做 传输传输_保存缓冲 用 档、缓冲。
    完。
    做 传输传输_预测拥塞 用 档、12000、假。
    数报回证 为 做 传输传输_建数报 用 档。
    档 的 健康 为 做 传输传输_健康 用 档。
    档 的 最近统计 为 做 传输传输_完整统计。
    返还 一组：档 的 初始化完成、套接回证 的 已成、缓冲回证 的 已收、数报回证 的 已收、做 AI拥塞_获取窗口 大于 0、做 计数 用 传输账本 大于 0、档 的 健康。
完。

本章 收束。
