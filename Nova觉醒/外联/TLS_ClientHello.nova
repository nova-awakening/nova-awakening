《TLS_ClientHello》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·加密框架》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

构建_ClientHello 接受 tls：
    可变 msg 为 一组：（空）。
    做 追加 用 msg、HS_ClientHello。
    做 追加 用 msg、0。
    做 追加 用 msg、0。
    做 追加 用 msg、0。
    做 追加 用 msg、0x03。
    做 追加 用 msg、0x03。
    可变 i 为 0。
    只要 i 小于 32：
        做 追加 用 msg、tls 的 客户端随机数 的 第 i 项。
        i 为 i 加 1。
    完。
    做 追加 用 msg、0。
    做 追加 用 msg、0。
    做 追加 用 msg、2。
    做 追加 用 msg、0x13。
    做 追加 用 msg、0x01。
    做 追加 用 msg、1。
    做 追加 用 msg、0。
    扩展 为 做 构建_ClientHello扩展 用 tls。
    做 追加 用 msg、做 计数 用 扩展 右移 8 位与 0xFF。
    做 追加 用 msg、做 计数 用 扩展 位与 0xFF。
    i 为 0。
    只要 i 小于 做 计数 用 扩展：
        做 追加 用 msg、扩展 的 第 i 项。
        i 为 i 加 1。
    完。
    载荷长度 为 做 计数 用 msg 减 4。
    msg 的 第 1 项 为 载荷长度 右移 16 位与 0xFF。
    msg 的 第 2 项 为 载荷长度 右移 8 位与 0xFF。
    msg 的 第 3 项 为 载荷长度 位与 0xFF。
    返还 msg。
完。

构建_ClientHello扩展 接受 tls：
    可变 ext 为 一组：（空）。
    若 tls 的 主机名 不等于 「」 并且 tls 的 主机名 不等于 空：
        名长 为 做 字符串长度 用 tls 的 主机名。
        做 追加 用 ext、0。
        做 追加 用 ext、0。
        sni_data_len 为 名长 加 5。
        做 追加 用 ext、sni_data_len 右移 8 位与 0xFF。
        做 追加 用 ext、sni_data_len 位与 0xFF。
        sni_list_len 为 名长 加 3。
        做 追加 用 ext、sni_list_len 右移 8 位与 0xFF。
        做 追加 用 ext、sni_list_len 位与 0xFF。
        做 追加 用 ext、0。
        做 追加 用 ext、名长 右移 8 位与 0xFF。
        做 追加 用 ext、名长 位与 0xFF。
        可变 j 为 0。
        只要 j 小于 名长：
            做 追加 用 ext、做 字符编码 用 做 字符串子串 用 tls 的 主机名、j、j 加 1。
            j 为 j 加 1。
        完。
    完。
    做 追加 用 ext、0。
    做 追加 用 ext、43。
    做 追加 用 ext、0。
    做 追加 用 ext、3。
    做 追加 用 ext、2。
    做 追加 用 ext、0x03。
    做 追加 用 ext、0x04。
    做 追加 用 ext、0。
    做 追加 用 ext、51。
    做 追加 用 ext、0。
    做 追加 用 ext、36。
    做 追加 用 ext、0。
    做 追加 用 ext、34。
    做 追加 用 ext、0。
    做 追加 用 ext、0x1D。
    做 追加 用 ext、0。
    做 追加 用 ext、32。
    可变 k 为 0。
    只要 k 小于 32：
        做 追加 用 ext、tls 的 公钥 的 第 k 项。
        k 为 k 加 1。
    完。
    返还 ext。
完。

解析_ServerHello 接受 tls、数据：
    若 数据 等于 空 或者 做 计数 用 数据 小于 38：
        返还。
    完。
    可变 偏移 为 4。
    偏移 为 偏移 加 2。
    tls 的 服务端随机数 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 32：
        做 追加 用 tls 的 服务端随机数、数据 的 第 偏移 加 i 项。
        i 为 i 加 1。
    完。
    偏移 为 偏移 加 32。
    会话ID长 为 数据 的 第 偏移 项。
    偏移 为 偏移 加 1 加 会话ID长。
    若 偏移 加 2 小于或等于 做 计数 用 数据：
        tls 的 密码套件 为 数据 的 第 偏移 项 乘 256 加 数据 的 第 偏移 加 1 项。
    完。
完。

构建_Finished 接受 tls：
    verify_data 为 做 HMAC_SHA256 用 tls 的 主密钥、「client finished」。
    可变 msg 为 一组：HS_完成、0、0、32。
    可变 i 为 0。
    只要 i 小于 做 计数 用 verify_data 并且 i 小于 32：
        做 追加 用 msg、verify_data 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 msg。
完。

本章 收束。
