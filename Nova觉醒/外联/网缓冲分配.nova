《网缓冲分配》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·字符串替换》。

引用《..·引导·端口IO》。

Nova缓冲_缓冲 为 0。

Nova缓冲_头偏移 为 1。

Nova缓冲_数据偏移 为 2。

Nova缓冲_尾偏移 为 3。

Nova缓冲_尾端偏移 为 4。

Nova缓冲_长度 为 5。

Nova缓冲_协议 为 6。

Nova缓冲_传输层头 为 7。

Nova缓冲_网络层头 为 8。

Nova缓冲_MAC头 为 9。

Nova缓冲_设备 为 10。

Nova缓冲_引用计数 为 11。

Nova缓冲_优先级 为 12。

Nova缓冲_标记 为 13。

Nova缓冲_时间戳 为 14。

ETH_P_IP 为 0x0800。

ETH_P_ARP 为 0x0806。

ETH_P_IPV6 为 0x86DD。

Nova缓冲_F_校验和已验证 为 1 左移 0。

Nova缓冲_F_本地发送 为 1 左移 1。

Nova缓冲_F_广播 为 1 左移 2。

Nova缓冲_F_多播 为 1 左移 3。

Nova缓冲_默认头部预留 为 128。

Nova缓冲_默认缓冲大小 为 2048。

可变 Nova缓冲_分配次数 为 0。

可变 Nova缓冲_释放次数 为 0。

可变 Nova缓冲_克隆次数 为 0。

Nova缓冲_分配 接受 大小：
    若 大小 小于 Nova缓冲_默认缓冲大小：
        大小 为 Nova缓冲_默认缓冲大小。
    完。
    缓冲 为 做 内核_分配字节 用 大小。
    若 缓冲 等于 空：
        返还 空。
    完。
    Nova缓冲 为 一组：缓冲、0、Nova缓冲_默认头部预留、Nova缓冲_默认头部预留、大小、0、0、0、0、0、空、1、0、0、0。
    Nova缓冲_分配次数 为 Nova缓冲_分配次数 加 1。
    返还 Nova缓冲。
完。

Nova缓冲_分配_带预留 接受 大小、头部预留：
    Nova缓冲 为 做 Nova缓冲_分配 用 大小 加 头部预留。
    若 Nova缓冲 等于 空：
        返还 空。
    完。
    做 设置 用 Nova缓冲、Nova缓冲_数据偏移、头部预留。
    做 设置 用 Nova缓冲、Nova缓冲_尾偏移、头部预留。
    返还 Nova缓冲。
完。

Nova缓冲_释放 接受 Nova缓冲：
    若 Nova缓冲 等于 空：
        返还。
    完。
    做 设置 用 Nova缓冲、Nova缓冲_引用计数、Nova缓冲 的 第 Nova缓冲_引用计数 项 减 1。
    若 Nova缓冲 的 第 Nova缓冲_引用计数 项 小于或等于 0：
        Nova缓冲_释放次数 为 Nova缓冲_释放次数 加 1。
    完。
完。

Nova缓冲_增引用 接受 Nova缓冲：
    做 设置 用 Nova缓冲、Nova缓冲_引用计数、Nova缓冲 的 第 Nova缓冲_引用计数 项 加 1。
完。

Nova缓冲_push 接受 Nova缓冲、长度：
    新数据偏移 为 Nova缓冲 的 第 Nova缓冲_数据偏移 项 减 长度。
    若 新数据偏移 小于 Nova缓冲 的 第 Nova缓冲_头偏移 项：
        做 内核警告 用 「Nova缓冲_push: 超出headroom!」。
        返还 0。
    完。
    做 设置 用 Nova缓冲、Nova缓冲_数据偏移、新数据偏移。
    做 设置 用 Nova缓冲、Nova缓冲_长度、Nova缓冲 的 第 Nova缓冲_长度 项 加 长度。
    返还 新数据偏移。
完。

Nova缓冲_pull 接受 Nova缓冲、长度：
    若 长度 大于 Nova缓冲 的 第 Nova缓冲_长度 项：
        返还 0。
    完。
    做 设置 用 Nova缓冲、Nova缓冲_数据偏移、Nova缓冲 的 第 Nova缓冲_数据偏移 项 加 长度。
    做 设置 用 Nova缓冲、Nova缓冲_长度、Nova缓冲 的 第 Nova缓冲_长度 项 减 长度。
    返还 Nova缓冲 的 第 Nova缓冲_数据偏移 项。
完。

Nova缓冲_put 接受 Nova缓冲、长度：
    新尾偏移 为 Nova缓冲 的 第 Nova缓冲_尾偏移 项 加 长度。
    若 新尾偏移 大于 Nova缓冲 的 第 Nova缓冲_尾端偏移 项：
        做 内核警告 用 「Nova缓冲_put: 超出tailroom!」。
        返还 0。
    完。
    旧尾 为 Nova缓冲 的 第 Nova缓冲_尾偏移 项。
    做 设置 用 Nova缓冲、Nova缓冲_尾偏移、新尾偏移。
    做 设置 用 Nova缓冲、Nova缓冲_长度、Nova缓冲 的 第 Nova缓冲_长度 项 加 长度。
    返还 旧尾。
完。

Nova缓冲_trim 接受 Nova缓冲、新长度：
    若 新长度 大于或等于 Nova缓冲 的 第 Nova缓冲_长度 项：
        返还。
    完。
    做 设置 用 Nova缓冲、Nova缓冲_尾偏移、Nova缓冲 的 第 Nova缓冲_数据偏移 项 加 新长度。
    做 设置 用 Nova缓冲、Nova缓冲_长度、新长度。
完。

Nova缓冲_数据 接受 Nova缓冲：
    返还 Nova缓冲 的 第 Nova缓冲_缓冲 项 加 Nova缓冲 的 第 Nova缓冲_数据偏移 项。
完。

Nova缓冲_写数据 接受 Nova缓冲、偏移、数据、长度：
    目标 为 Nova缓冲 的 第 Nova缓冲_缓冲 项 加 Nova缓冲 的 第 Nova缓冲_数据偏移 项 加 偏移。
    做 内存复制 用 目标、数据、长度。
完。

Nova缓冲_读数据 接受 Nova缓冲、偏移、缓冲、长度：
    源 为 Nova缓冲 的 第 Nova缓冲_缓冲 项 加 Nova缓冲 的 第 Nova缓冲_数据偏移 项 加 偏移。
    做 内存复制 用 缓冲、源、长度。
完。

Nova缓冲_写8 接受 Nova缓冲、偏移、值：
    做 内存写8 用 Nova缓冲 的 第 Nova缓冲_缓冲 项 加 Nova缓冲 的 第 Nova缓冲_数据偏移 项 加 偏移、值。
完。

Nova缓冲_写16 接受 Nova缓冲、偏移、值：
    地址 为 Nova缓冲 的 第 Nova缓冲_缓冲 项 加 Nova缓冲 的 第 Nova缓冲_数据偏移 项 加 偏移。
    做 内存写8 用 地址、值 右移 8 位与 0xFF。
    做 内存写8 用 地址 加 1、值 位与 0xFF。
完。

Nova缓冲_写32 接受 Nova缓冲、偏移、值：
    地址 为 Nova缓冲 的 第 Nova缓冲_缓冲 项 加 Nova缓冲 的 第 Nova缓冲_数据偏移 项 加 偏移。
    做 内存写8 用 地址、值 右移 24 位与 0xFF。
    做 内存写8 用 地址 加 1、值 右移 16 位与 0xFF。
    做 内存写8 用 地址 加 2、值 右移 8 位与 0xFF。
    做 内存写8 用 地址 加 3、值 位与 0xFF。
完。

Nova缓冲_读8 接受 Nova缓冲、偏移：
    返还 做 内存读8 用 Nova缓冲 的 第 Nova缓冲_缓冲 项 加 Nova缓冲 的 第 Nova缓冲_数据偏移 项 加 偏移。
完。

Nova缓冲_读16 接受 Nova缓冲、偏移：
    地址 为 Nova缓冲 的 第 Nova缓冲_缓冲 项 加 Nova缓冲 的 第 Nova缓冲_数据偏移 项 加 偏移。
    返还 做 内存读8 用 地址 左移 8 位或 做 内存读8 用 地址 加 1。
完。

可变 AI_网缓分配_运行指标 为 0。

可变 AI_网缓分配_异常指标 为 0。

AI_网缓分配_自诊断 接受 零参：
    AI_网缓分配_运行指标 为 Nova缓冲_分配次数。
    返还 AI_网缓分配_运行指标。
完。

AI_网缓分配_优化建议 接受 零参：
    指标 为 做 AI_网缓分配_自诊断。
    若 AI_网缓分配_运行指标 大于 10000：
        返还 「网缓分配负载较高」。
    完。
    返还 「网缓分配状态正常」。
完。

本章 收束。
