《IP统一收发》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·IP接收》。

引用《.·网络缓冲读》。

引用《.·网缓冲分配》。

引用《.·UDP端口管理》。

引用《.·帧填充校验》。

引用《.·IP分片发送》。

引用《.·IP前缀掩码转换》。

引用《.·传控核心》。

引用《.·IP发送》。

IP头_版本IHL 为 0。

IP头_TOS 为 1。

IP头_总长度 为 2。

IP头_标识 为 4。

IP头_标志偏移 为 6。

IP头_TTL 为 8。

IP头_协议 为 9。

IP头_校验和 为 10。

IP头_源地址 为 12。

IP头_目标地址 为 16。

IP头_长度 为 20。

IPPROTO_ICMP 为 1。

IPPROTO_传控 为 6。

IPPROTO_数报 为 17。

IP_DF 为 0x4000。

IP_MF 为 0x2000。

IP_默认TTL 为 64。

IP_最大包长 为 65535。

可变 IP_接收包数 为 0。

可变 IP_发送包数 为 0。

可变 IP_转发包数 为 0。

可变 IP_丢弃包数 为 0。

可变 IP_校验和错误 为 0。

可变 IP_分片标识 为 0。

IP_接收_统一 接受 Nova缓冲：
    若 Nova缓冲 等于 空 或者 做 Nova缓冲_长度 用 Nova缓冲 小于 IP头_长度：
        IP_丢弃包数 为 IP_丢弃包数 加 1。
        返还 错误_无效参数。
    完。
    做 Nova缓冲_设置网络头 用 Nova缓冲。
    版本IHL 为 做 Nova缓冲_读8 用 Nova缓冲、IP头_版本IHL。
    版本 为 版本IHL 右移 4 位与 0x0F。
    头长度 为 (版本IHL 位与 0x0F) 乘 4。
    若 版本 不等于 4：
        IP_丢弃包数 为 IP_丢弃包数 加 1。
        返还 错误_无效参数。
    完。
    若 头长度 小于 20：
        IP_丢弃包数 为 IP_丢弃包数 加 1。
        返还 错误_无效参数。
    完。
    校验 为 做 IP_计算校验和 用 Nova缓冲、头长度。
    若 校验 不等于 0：
        IP_校验和错误 为 IP_校验和错误 加 1。
        IP_丢弃包数 为 IP_丢弃包数 加 1。
        返还 错误_校验和。
    完。
    总长 为 做 Nova缓冲_读16 用 Nova缓冲、IP头_总长度。
    协议 为 做 Nova缓冲_读8 用 Nova缓冲、IP头_协议。
    目标IP 为 做 Nova缓冲_读32 用 Nova缓冲、IP头_目标地址。
    源IP 为 做 Nova缓冲_读32 用 Nova缓冲、IP头_源地址。
    TTL 为 做 Nova缓冲_读8 用 Nova缓冲、IP头_TTL。
    若 做 Nova缓冲_长度 用 Nova缓冲 大于 总长：
        做 Nova缓冲_trim 用 Nova缓冲、总长。
    完。
    IP_接收包数 为 IP_接收包数 加 1。
    若 做 IP_是本机地址 用 目标IP：
        做 Nova缓冲_pull 用 Nova缓冲、头长度。
        做 Nova缓冲_设置传输头 用 Nova缓冲。
        若 协议 等于 IPPROTO_传控：
            返还 做 传控_接收 用 Nova缓冲、源IP、目标IP。
        否则若 协议 等于 IPPROTO_数报：
            返还 做 数报_接收 用 Nova缓冲、源IP、目标IP。
        否则若 协议 等于 IPPROTO_ICMP：
            返还 做 ICMP_接收 用 Nova缓冲、源IP。
        完。
        IP_丢弃包数 为 IP_丢弃包数 加 1。
        返还 错误_协议不支持。
    完。
    若 IP转发已启用：
        返还 做 IP_转发 用 Nova缓冲。
    完。
    IP_丢弃包数 为 IP_丢弃包数 加 1。
    返还 错误_目标不可达。
完。

IP_发送_统一 接受 Nova缓冲、源IP、目标IP、协议、TTL：
    若 TTL 等于 0：
        TTL 为 IP_默认TTL。
    完。
    头偏移 为 做 Nova缓冲_push 用 Nova缓冲、IP头_长度。
    若 头偏移 等于 0：
        返还 错误_内存不足。
    完。
    做 Nova缓冲_设置网络头 用 Nova缓冲。
    做 Nova缓冲_写8 用 Nova缓冲、IP头_版本IHL、0x45。
    做 Nova缓冲_写8 用 Nova缓冲、IP头_TOS、0。
    做 Nova缓冲_写16 用 Nova缓冲、IP头_总长度、做 Nova缓冲_长度 用 Nova缓冲。
    IP_分片标识 为 IP_分片标识 加 1。
    做 Nova缓冲_写16 用 Nova缓冲、IP头_标识、IP_分片标识 位与 0xFFFF。
    做 Nova缓冲_写16 用 Nova缓冲、IP头_标志偏移、IP_DF。
    做 Nova缓冲_写8 用 Nova缓冲、IP头_TTL、TTL。
    做 Nova缓冲_写8 用 Nova缓冲、IP头_协议、协议。
    做 Nova缓冲_写16 用 Nova缓冲、IP头_校验和、0。
    做 Nova缓冲_写32 用 Nova缓冲、IP头_源地址、源IP。
    做 Nova缓冲_写32 用 Nova缓冲、IP头_目标地址、目标IP。
    校验和 为 做 IP_计算校验和 用 Nova缓冲、IP头_长度。
    做 Nova缓冲_写16 用 Nova缓冲、IP头_校验和、按位取反 校验和 位与 0xFFFF。
    路由 为 做 路由_查找 用 目标IP。
    若 路由 等于 空：
        IP_丢弃包数 为 IP_丢弃包数 加 1。
        返还 错误_网络不可达。
    完。
    设备 为 路由 的 出口设备。
    MTU 为 做 网络设备_MTU 用 设备。
    若 做 Nova缓冲_长度 用 Nova缓冲 大于 MTU：
        返还 做 IP_分片发送 用 Nova缓冲、设备、MTU、0。
    完。
    做 Nova缓冲_设置设备 用 Nova缓冲、设备。
    做 Nova缓冲_设置协议 用 Nova缓冲、ETH_P_IP。
    IP_发送包数 为 IP_发送包数 加 1。
    返还 做 链路层_发送 用 Nova缓冲、路由 的 下一跳、设备。
完。

可变 IP转发已启用 为 假。

IP_输入输出_转发 接受 Nova缓冲：
    TTL 为 做 Nova缓冲_读8 用 Nova缓冲、IP头_TTL。
    若 TTL 小于或等于 1：
        做 ICMP_发送_超时 用 Nova缓冲。
        IP_丢弃包数 为 IP_丢弃包数 加 1。
        返还 错误_TTL耗尽。
    完。
    做 Nova缓冲_写8 用 Nova缓冲、IP头_TTL、TTL 减 1。
    做 Nova缓冲_写16 用 Nova缓冲、IP头_校验和、0。
    头长度 为 (做 Nova缓冲_读8 用 Nova缓冲、IP头_版本IHL 位与 0x0F) 乘 4。
    校验和 为 做 IP_计算校验和 用 Nova缓冲、头长度。
    做 Nova缓冲_写16 用 Nova缓冲、IP头_校验和、按位取反 校验和 位与 0xFFFF。
    目标IP 为 做 Nova缓冲_读32 用 Nova缓冲、IP头_目标地址。
    路由 为 做 路由_查找 用 目标IP。
    若 路由 等于 空：
        做 ICMP_发送_不可达 用 Nova缓冲。
        返还 错误_网络不可达。
    完。
    做 Nova缓冲_设置设备 用 Nova缓冲、路由 的 出口设备。
    IP_转发包数 为 IP_转发包数 加 1。
    返还 做 链路层_发送 用 Nova缓冲、路由 的 下一跳、路由 的 出口设备。
完。

可变 AI_网络接收统_带宽估计 为 0。

可变 AI_网络接收统_丢包窗口 为 一组：（空）。

AI_网络接收统_带宽预测 接受 发送字节、耗时毫秒：
    若 耗时毫秒 等于 0：
        返还 AI_网络接收统_带宽估计。
    完。
    当前 为 发送字节 乘 1000 除以 耗时毫秒。
    AI_网络接收统_带宽估计 为 AI_网络接收统_带宽估计 乘 700 除以 1000 加 当前 乘 300 除以 1000。
    返还 AI_网络接收统_带宽估计。
完。

AI_网络接收统_拥塞建议 接受 丢包率：
    做 追加 用 AI_网络接收统_丢包窗口、丢包率。
    若 做 计数 用 AI_网络接收统_丢包窗口 大于 10：
        做 列表_移除 用 AI_网络接收统_丢包窗口、0。
    完。
    若 丢包率 大于 50：
        返还 「减半窗口」。
    完。
    若 丢包率 小于 5：
        返还 「增大窗口」。
    完。
    返还 「保持」。
完。

本章 收束。
