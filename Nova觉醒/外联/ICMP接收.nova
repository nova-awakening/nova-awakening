《ICMP接收》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·ICMP处理》。

引用《.·ICMP控制报文》。

ICMP_回显应答 为 0。

ICMP_目标不可达 为 3。

ICMP_源抑制 为 4。

ICMP_重定向 为 5。

ICMP_回显请求 为 8。

ICMP_路由通告 为 9。

ICMP_路由请求 为 10。

ICMP_超时 为 11。

ICMP_参数问题 为 12。

ICMP_时间戳请求 为 13。

ICMP_时间戳应答 为 14。

ICMP_网络不可达 为 0。

ICMP_主机不可达 为 1。

ICMP_协议不可达 为 2。

ICMP_端口不可达 为 3。

ICMP_需要分片 为 4。

ICMP_源路由失败 为 5。

ICMP_目标网络未知 为 6。

ICMP_目标主机未知 为 7。

ICMP_管理性禁止 为 13。

ICMP_TTL超时 为 0。

ICMP_分片重组超时 为 1。

ICMP_头部大小 为 8。

ICMP_最小MTU 为 576。

IP协议_ICMP 为 1。

ICMP_限速_间隔 为 1000。

ICMP_限速_突发 为 50。

ICMP头部 是 一种 事物，含 类型、代码、校验和、标识符、序列号。

ICMP_偏移_类型 为 0。

ICMP_偏移_代码 为 1。

ICMP_偏移_校验和 为 2。

ICMP_偏移_标识符 为 4。

ICMP_偏移_序列号 为 6。

ICMP统计 是 一种 事物，含 接收总数、发送总数、接收错误、发送错误、回显请求收、回显应答收、回显请求发、回显应答发、不可达收、不可达发、超时收、限速丢弃。

可变 icmp统计 为 新建 ICMP统计。

可变 icmp_上次发送时间 为 0。

可变 icmp_突发计数 为 0。

icmp_接收 接受 Nova缓冲：
    icmp头 为 Nova缓冲 的 数据 加 Nova缓冲 的 传输层偏移。
    icmp长度 为 Nova缓冲 的 数据长度 减 Nova缓冲 的 传输层偏移。
    若 icmp长度 小于 ICMP_头部大小：
        icmp统计 的 接收错误 为 icmp统计 的 接收错误 加 1。
        返还 错误码_无效参数。
    完。
    若 非 做 icmp_验证校验和 用 icmp头、icmp长度：
        icmp统计 的 接收错误 为 icmp统计 的 接收错误 加 1。
        返还 错误码_校验和错误。
    完。
    icmp统计 的 接收总数 为 icmp统计 的 接收总数 加 1。
    类型 为 icmp头 的 第 ICMP_偏移_类型 项。
    代码 为 icmp头 的 第 ICMP_偏移_代码 项。
    做 匹配 用 类型。
    返还 0。
完。

本章 收束。
