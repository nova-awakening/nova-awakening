《TCP四元组》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《.·TCP连接表》。

引用《.·网络缓冲读》。

引用《..·中枢·墙钟设置》。

传控_连接表_桶数 为 1024。

传控_监听表_桶数 为 256。

传控_TIME_WAIT表_桶数 为 512。

传控连接表 是 一种 事物，含 已建立、监听、time_wait、连接总数、监听总数、锁。

传控连接链表 是 一种 事物，含 头、数量。

可变 全局传控连接表 为 新建 传控连接表。

传控四元组 是 一种 事物，含 源IP、源端口、目标IP、目标端口。

tcp_四元组_哈希 接受 四元组：
    可变 h 为 四元组 的 源IP。
    h 为 h 位异或 四元组 的 目标IP 左移 16。
    h 为 h 位异或 (四元组 的 源端口 位或 四元组 的 目标端口 左移 16)。
    h 为 h 位异或 h 右移 16。
    h 为 h 乘 0x85EBCA6B。
    h 为 h 位异或 h 右移 13。
    返还 h。
完。

tcp_已建立_哈希桶 接受 四元组：
    返还 做 tcp_四元组_哈希 用 四元组 取余于 传控_连接表_桶数。
完。

tcp_监听_哈希桶 接受 端口：
    返还 端口 取余于 传控_监听表_桶数。
完。

tcp_创建连接 接受 源IP、源端口、目标IP、目标端口：
    可变 tcb 为 新建 传控控制块。
    tcb 的 四元组 的 源IP 为 源IP。
    tcb 的 四元组 的 源端口 为 源端口。
    tcb 的 四元组 的 目标IP 为 目标IP。
    tcb 的 四元组 的 目标端口 为 目标端口。
    tcb 的 状态 为 传控_状态_关闭。
    tcb 的 发送序号 为 做 tcp_生成初始序号。
    tcb 的 接收序号 为 0。
    tcb 的 发送窗口 为 传控_默认窗口大小。
    tcb 的 接收窗口 为 传控_默认窗口大小。
    tcb 的 mss 为 传控_默认MSS。
    tcb 的 重传超时 为 传控_初始RTO。
    tcb 的 sNova往返时间 为 0。
    tcb 的 Nova往返时间var 为 0。
    tcb 的 拥塞窗口 为 传控_默认MSS。
    tcb 的 慢启动阈值 为 65535。
    tcb 的 发送缓冲 为 做 Nova网络缓冲_队列_创建。
    tcb 的 接收缓冲 为 做 Nova网络缓冲_队列_创建。
    tcb 的 重传队列 为 做 Nova网络缓冲_队列_创建。
    tcb 的 乱序队列 为 做 Nova网络缓冲_队列_创建。
    返还 tcb。
完。

tcp_销毁连接 接受 tcb：
    做 Nova网络缓冲_队列_清空 用 tcb 的 发送缓冲。
    做 Nova网络缓冲_队列_清空 用 tcb 的 接收缓冲。
    做 Nova网络缓冲_队列_清空 用 tcb 的 重传队列。
    做 Nova网络缓冲_队列_清空 用 tcb 的 乱序队列。
    做 tcp_连接表_移除 用 tcb。
完。

tcp_生成初始序号 接受 零参：
    时间 为 做 当前时间_秒。
    随机 为 做 内核随机数。
    返还 时间 乘 250000 加 随机 位与 0xFFFFFFFF。
完。

本章 收束。
