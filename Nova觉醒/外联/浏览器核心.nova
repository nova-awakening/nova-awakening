《浏览器核心》

引用《..·中枢·类型定义》。

引用《..·中枢·系统调用表》。

引用《..·中枢·哈希整数》。

引用《..·中枢·内容可见性》。

最大标签 为 64。

最大书签 为 256。

最大历史 为 1024。

最大缓存 为 512。

标签页 是 一种 事物，含 标识、地址哈希、标题哈希、活跃、加载状态、创建时刻、最近访问、导航次数。

书签项 是 一种 事物，含 地址哈希、标题哈希、文件夹哈希、创建时刻。

历史项 是 一种 事物，含 地址哈希、标题哈希、访问时刻、访问次数。

缓存项 是 一种 事物，含 键哈希、值哈希、过期时刻、安全标记。

可变 标签表 为 一组：（空）。

可变 标签数 为 0。

可变 活跃标签 为 负 1。

可变 下一标签标识 为 1。

可变 书签表 为 一组：（空）。

可变 书签数 为 0。

可变 历史表 为 一组：（空）。

可变 历史数 为 0。

可变 缓存表 为 一组：（空）。

可变 缓存数 为 0。

浏览器_初始化 接受 零参：
    标签表 为 一组：（空）。
    标签数 为 0。
    活跃标签 为 负 1。
    书签表 为 一组：（空）。
    书签数 为 0。
    历史表 为 一组：（空）。
    历史数 为 0。
    缓存表 为 一组：（空）。
    缓存数 为 0。
完。

标签_创建 接受 地址、标题：
    若 标签数 大于或等于 最大标签：
        返还 负 1。
    完。
    标签 为 新建 标签页。
    标签 的 标识 为 下一标签标识。
    下一标签标识 为 下一标签标识 加 1。
    标签 的 地址哈希 为 做 哈希_字符串 用 地址。
    标签 的 标题哈希 为 做 哈希_字符串 用 标题。
    标签 的 活跃 为 真。
    标签 的 加载状态 为 0。
    标签 的 创建时刻 为 做 平台_获取高精度时间。
    标签 的 最近访问 为 标签 的 创建时刻。
    标签 的 导航次数 为 1。
    做 追加 用 标签表、标签。
    索引 为 标签数。
    标签数 为 标签数 加 1。
    活跃标签 为 索引。
    做 历史_添加 用 地址、标题。
    返还 索引。
完。

标签_关闭 接受 索引：
    若 索引 大于或等于 标签数：
        返还。
    完。
    标签表 的 第 索引 项 的 活跃 为 假。
    若 活跃标签 等于 索引：
        活跃标签 为 负 1。
        可变 搜索 为 标签数 减 1。
        只要 搜索 大于或等于 0：
            若 标签表 的 第 搜索 项 的 活跃：
                活跃标签 为 搜索。
                跳出。
            完。
            搜索 为 搜索 减 1。
        完。
    完。
完。

标签_切换 接受 索引：
    若 索引 大于或等于 标签数 或者 非 标签表 的 第 索引 项 的 活跃：
        返还。
    完。
    活跃标签 为 索引。
    标签表 的 第 索引 项 的 最近访问 为 做 平台_获取高精度时间。
完。

标签_导航 接受 索引、地址、标题：
    若 索引 大于或等于 标签数：
        返还。
    完。
    标签表 的 第 索引 项 的 地址哈希 为 做 哈希_字符串 用 地址。
    标签表 的 第 索引 项 的 标题哈希 为 做 哈希_字符串 用 标题。
    标签表 的 第 索引 项 的 导航次数 为 标签表 的 第 索引 项 的 导航次数 加 1。
    标签表 的 第 索引 项 的 最近访问 为 做 平台_获取高精度时间。
    做 历史_添加 用 地址、标题。
完。

标签_活跃数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 标签数：
        若 标签表 的 第 序号 项 的 活跃：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

书签_添加 接受 地址、标题、文件夹：
    若 书签数 大于或等于 最大书签：
        返还 负 1。
    完。
    书签 为 新建 书签项。
    书签 的 地址哈希 为 做 哈希_字符串 用 地址。
    书签 的 标题哈希 为 做 哈希_字符串 用 标题。
    书签 的 文件夹哈希 为 做 哈希_字符串 用 文件夹。
    书签 的 创建时刻 为 做 平台_获取高精度时间。
    做 追加 用 书签表、书签。
    书签数 为 书签数 加 1。
    返还 书签数 减 1。
完。

历史_添加 接受 地址、标题：
    地址哈希 为 做 哈希_字符串 用 地址。
    可变 序号 为 历史数 减 1。
    只要 序号 大于或等于 0：
        若 历史表 的 第 序号 项 的 地址哈希 等于 地址哈希：
            历史表 的 第 序号 项 的 访问次数 为 历史表 的 第 序号 项 的 访问次数 加 1。
            历史表 的 第 序号 项 的 访问时刻 为 做 平台_获取高精度时间。
            返还。
        完。
        序号 为 序号 减 1。
    完。
    若 历史数 大于或等于 最大历史：
        历史表 的 第 0 项 为 历史表 的 第 历史数 减 1 项。
        历史数 为 历史数 减 1。
    完。
    历史 为 新建 历史项。
    历史 的 地址哈希 为 地址哈希。
    历史 的 标题哈希 为 做 哈希_字符串 用 标题。
    历史 的 访问时刻 为 做 平台_获取高精度时间。
    历史 的 访问次数 为 1。
    做 追加 用 历史表、历史。
    历史数 为 历史数 加 1。
完。

缓存_设置 接受 键、值、过期秒、安全：
    若 缓存数 大于或等于 最大缓存：
        做 缓存_淘汰。
    完。
    项 为 新建 缓存项。
    项 的 键哈希 为 做 哈希_字符串 用 键。
    项 的 值哈希 为 做 哈希_字符串 用 值。
    项 的 过期时刻 为 做 平台_获取高精度时间 加 过期秒 乘 1000000。
    项 的 安全标记 为 安全。
    做 追加 用 缓存表、项。
    缓存数 为 缓存数 加 1。
完。

缓存_获取 接受 键：
    目标 为 做 哈希_字符串 用 键。
    现在 为 做 平台_获取高精度时间。
    可变 序号 为 0。
    只要 序号 小于 缓存数：
        若 缓存表 的 第 序号 项 的 键哈希 等于 目标：
            若 缓存表 的 第 序号 项 的 过期时刻 大于 现在：
                返还 缓存表 的 第 序号 项 的 值哈希。
            完。
            返还 0。
        完。
        序号 为 序号 加 1。
    完。
    返还 0。
完。

缓存_淘汰 接受 零参：
    现在 为 做 平台_获取高精度时间。
    可变 序号 为 0。
    只要 序号 小于 缓存数：
        若 缓存表 的 第 序号 项 的 过期时刻 小于或等于 现在：
            缓存表 的 第 序号 项 为 缓存表 的 第 缓存数 减 1 项。
            缓存数 为 缓存数 减 1。
            继续。
        完。
        序号 为 序号 加 1。
    完。
完。

浏览器_完整统计 接受 零参：
    返还 一组：做 标签_活跃数、活跃标签、书签数、历史数、缓存数。
完。

本章 收束。
