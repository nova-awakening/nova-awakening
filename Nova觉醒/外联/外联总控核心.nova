《外联总控核心》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·日志中枢》。

引用《..·感知·全景照》。

引用《..·标准·脑区名称常量》。

引用《..·执行·内核界面》。

引用《.·TCP会话记录》。

引用《..·感知·文档节点》。

引用《..·中枢·硬链接》。

引用《..·标准·原生字符串》。

引用《..·中枢·工具协议工》。

引用《..·执行·浏览器系统》。

引用《.·URL解析辅助》。

引用《..·中枢·界面元素发现》。

引用《..·中枢·元素定位策》。

引用《..·中枢·整数转字符串》。

法心跳 为 「心跳」。

法取照 为 「取照」。

法取树 为 「取树」。

法找点 为 「找点」。

法点选 为 「点选」。

法输文 为 「输文」。

法滚动 为 「滚动」。

法等待 为 「等待」。

法转页 为 「转页」。

法读文 为 「读文」。

法跑脚 为 「跑脚」。

法下令 为 「下令」。

法订流 为 「订流」。

控信封 是 一种 事物，含 编号、方法、参数、原文、新版。

动作单 是 一种 事物，含 方法、参数、前照。

回证单 是 一种 事物，含 编号、已收、已成、结果、错因、耗时、证据、载荷。

可变 控序 为 1。

可变 控收数 为 0。

可变 控成数 为 0。

可变 控错数 为 0。

可变 控初态 为 假。

可变 上照 为 空。

可变 模列 为 一组：（空）。

可变 脑列 为 一组：（空）。

真串 接受 值：
    若 值：
        返还 「true」。
    完。
    返还 「false」。
完。

串义 接受 文本：
    若 文本 等于 空：
        返还 「」。
    完。
    建 为 做 字符串构建器_创建。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        c 为 做 字符串取字符码 用 文本、i。
        若 c 等于 34：
            做 字符串构建器_追加 用 建、「\\\"」。
        否则若 c 等于 92：
            做 字符串构建器_追加 用 建、「\\\\」。
        否则若 c 等于 10：
            做 字符串构建器_追加 用 建、「\\n」。
        否则若 c 等于 13：
            做 字符串构建器_追加 用 建、「\\r」。
        否则若 c 等于 9：
            做 字符串构建器_追加 用 建、「\\t」。
        否则：
            做 字符串构建器_追加字符 用 建、c。
        完。
        i 为 i 加 1。
    完。
    返还 做 字符串构建器_构建 用 建。
完。

控初 接受 零参：
    若 控初态：
        返还。
    完。
    模列 为 一组：做 模档样控、做 模档样照、做 模档样档。
    外脑 为 做 脑档建 用 「外联」。
    做 脑档记 用 外脑、「控核」、真。
    做 脑档链 用 外脑、「感知」、「取照」。
    做 脑档链 用 外脑、「感知」、「取树」。
    做 脑档链 用 外脑、「执行」、「点选」。
    做 脑档链 用 外脑、「执行」、「输文」。
    做 脑档链 用 外脑、「外联」、「心跳」。
    做 脑档预算 用 外脑、「本地串口」。
    做 脑档预算 用 外脑、「QMP协同」。
    做 脑档态 用 外脑、「轮询接入」。
    脑列 为 一组：外脑。
    控初态 为 真。
完。

控封 接受 编号、方法、参数、原文、新版：
    封 为 新建 控信封。
    封 的 编号 为 编号。
    封 的 方法 为 方法。
    封 的 参数 为 参数。
    封 的 原文 为 原文。
    封 的 新版 为 新版。
    返还 封。
完。

动单 接受 方法、参数：
    动 为 新建 动作单。
    动 的 方法 为 方法。
    动 的 参数 为 参数。
    动 的 前照 为 做 照采。
    返还 动。
完。

回建 接受 编号、已成、结果、错因、证据、载荷：
    回 为 新建 回证单。
    回 的 编号 为 编号。
    回 的 已收 为 真。
    回 的 已成 为 已成。
    回 的 结果 为 结果。
    回 的 错因 为 错因。
    回 的 耗时 为 0。
    回 的 证据 为 证据。
    回 的 载荷 为 载荷。
    若 已成：
        控成数 为 控成数 加 1。
    否则：
        控错数 为 控错数 加 1。
    完。
    返还 回。
完。

控统 接受 零参：
    返还 「{\"收数\":」 加 做 整数转字符串 用 控收数 加 「,\"成数\":」 加 做 整数转字符串 用 控成数 加 「,\"错数\":」 加 做 整数转字符串 用 控错数 加 「,\"模列\":」 加 做 模档摘要 用 模列 加 「,\"脑列\":」 加 做 脑档摘要 用 脑列 加 「,\"照统\":」 加 做 照统 加 「}」。
完。

回串 接受 回：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"编号\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 回 的 编号。
    做 字符串构建器_追加 用 建、「,\"已收\":」。
    做 字符串构建器_追加 用 建、做 真串 用 回 的 已收。
    做 字符串构建器_追加 用 建、「,\"已成\":」。
    做 字符串构建器_追加 用 建、做 真串 用 回 的 已成。
    做 字符串构建器_追加 用 建、「,\"结果\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 回 的 结果。
    做 字符串构建器_追加 用 建、「\",\"错因\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 回 的 错因。
    做 字符串构建器_追加 用 建、「\",\"耗时\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 回 的 耗时。
    做 字符串构建器_追加 用 建、「,\"证据\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 回 的 证据。
    做 字符串构建器_追加 用 建、「\"」。
    若 做 字符串长度 用 回 的 载荷 大于 0：
        做 字符串构建器_追加 用 建、「,\"载荷\":」。
        做 字符串构建器_追加 用 建、回 的 载荷。
    完。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

串键 接受 文本、键：
    标 为 「\"」 加 键 加 「\":\"」。
    起 为 做 字符串查找 用 文本、标。
    若 起 小于 0：
        返还 「」。
    完。
    起 为 起 加 做 字符串长度 用 标。
    建 为 做 字符串构建器_创建。
    可变 i 为 起。
    只要 i 小于 做 字符串长度 用 文本：
        c 为 做 字符串取字符码 用 文本、i。
        若 c 等于 34：
            返还 做 字符串构建器_构建 用 建。
        完。
        做 字符串构建器_追加字符 用 建、c。
        i 为 i 加 1。
    完。
    返还 做 字符串构建器_构建 用 建。
完。

数键 接受 文本、键：
    标 为 「\"」 加 键 加 「\":」。
    起 为 做 字符串查找 用 文本、标。
    若 起 小于 0：
        返还 0。
    完。
    起 为 起 加 做 字符串长度 用 标。
    建 为 做 字符串构建器_创建。
    可变 i 为 起。
    只要 i 小于 做 字符串长度 用 文本：
        c 为 做 字符串取字符码 用 文本、i。
        若 c 大于或等于 48 并且 c 小于或等于 57：
            做 字符串构建器_追加字符 用 建、c。
        否则若 做 字符串长度 用 做 字符串构建器_构建 用 建 大于 0：
            返还 做 字符串转整数 用 做 字符串构建器_构建 用 建。
        完。
        i 为 i 加 1。
    完。
    若 做 字符串长度 用 做 字符串构建器_构建 用 建 等于 0：
        返还 0。
    完。
    返还 做 字符串转整数 用 做 字符串构建器_构建 用 建。
完。

新封 接受 文本：
    编 为 做 数键 用 文本、「编号」。
    若 编 小于或等于 0：
        编 为 控序。
    完。
    法 为 做 串键 用 文本、「方法」。
    参 为 做 串键 用 文本、「参数」。
    返还 做 控封 用 编、法、参、文本、真。
完。

旧法映 接受 命：
    若 命 等于 「PING」：
        返还 法心跳。
    完。
    若 命 等于 「BROWSE」：
        返还 法取树。
    完。
    若 命 等于 「CLICK_ID」：
        返还 法点选。
    完。
    若 命 等于 「INPUT」：
        返还 法输文。
    完。
    若 命 等于 「INPUT_ID」：
        返还 法输文。
    完。
    若 命 等于 「WAIT」：
        返还 法等待。
    完。
    若 命 等于 「NAVIGATE」：
        返还 法转页。
    完。
    若 命 等于 「EXEC_JS」：
        返还 法跑脚。
    完。
    若 命 等于 「READ」：
        返还 法读文。
    完。
    返还 命。
完。

旧封 接受 文本：
    段 为 做 字符串分割N 用 文本、「 」、4。
    若 做 计数 用 段 小于 2：
        返还 做 控封 用 控序、「」、「」、文本、假。
    完。
    编 为 做 字符串转整数 用 段 的 第 1 项。
    法 为 「」。
    参 为 「」。
    若 做 计数 用 段 大于或等于 3：
        法 为 做 旧法映 用 段 的 第 2 项。
    完。
    若 做 计数 用 段 大于或等于 4：
        参 为 段 的 第 3 项。
    完。
    返还 做 控封 用 编、法、参、文本、假。
完。

控析 接受 文本：
    若 做 字符串长度 用 文本 等于 0：
        返还 做 控封 用 控序、「」、「」、文本、假。
    完。
    若 做 字符串取字符码 用 文本、0 等于 123：
        返还 做 新封 用 文本。
    完。
    若 做 字符串查找 用 文本、「REQ 」 等于 0：
        返还 做 旧封 用 文本。
    完。
    返还 做 控封 用 控序、文本、「」、文本、真。
完。

整串 接受 文本：
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        c 为 做 字符串取字符码 用 文本、i。
        若 c 小于 48 或者 c 大于 57：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 做 字符串长度 用 文本 大于 0。
完。

输拆 接受 参数：
    结果 为 一组：「」、「」。
    位 为 做 字符串查找 用 参数、「|」。
    若 位 大于或等于 0：
        结果 的 第 0 项 为 做 子字符串 用 参数、0、位。
        结果 的 第 1 项 为 做 子字符串 用 参数、位 加 1、做 字符串长度 用 参数 减 位 减 1。
        返还 结果。
    完。
    段 为 做 字符串分割N 用 参数、「 」、2。
    若 做 计数 用 段 大于或等于 2：
        结果 的 第 0 项 为 段 的 第 0 项。
        结果 的 第 1 项 为 段 的 第 1 项。
    否则：
        结果 的 第 1 项 为 参数。
    完。
    返还 结果。
完。

点执 接受 参数：
    前 为 做 照采。
    结果 为 做 浏览器系统会话_执行页面动作 用 「click-id」、参数、「」。
    若 做 字符串查找 用 结果、「click-id:」 等于 0：
        后 为 做 照采。
        上照 为 后。
        返还 做 回建 用 控序、真、结果、「」、做 照差 用 前、后、「」。
    完。
    若 做 整串 用 参数：
        做 UI_扫描全部元素。
        目标 为 做 UI_按ID获取 用 做 字符串转整数 用 参数。
        若 目标 不等于 空 并且 做 UI_点击 用 目标：
            后 为 做 照采。
            上照 为 后。
            返还 做 回建 用 控序、真、「点选:」 加 参数、「」、做 照差 用 前、后、「」。
        完。
    完。
    返还 做 回建 用 控序、假、「」、「点选失败:」 加 参数、「」、「」。
完。

输执 接受 参数：
    拆 为 做 输拆 用 参数。
    标识 为 拆 的 第 0 项。
    文本 为 拆 的 第 1 项。
    前 为 做 照采。
    若 做 字符串长度 用 标识 大于 0：
        结果 为 做 浏览器系统会话_执行页面动作 用 「set-input-id」、标识、文本。
        若 做 字符串查找 用 结果、「set-input-id:」 等于 0：
            后 为 做 照采。
            上照 为 后。
            返还 做 回建 用 控序、真、结果、「」、做 照差 用 前、后、「」。
        完。
    完。
    做 UI_扫描全部元素。
    框列 为 做 UI_按角色查找 用 「textbox」。
    若 做 计数 用 框列 大于 0 并且 做 UI_输入文本 用 框列 的 第 0 项、文本：
        后 为 做 照采。
        上照 为 后。
        返还 做 回建 用 控序、真、「输文」、「」、做 照差 用 前、后、「」。
    完。
    返还 做 回建 用 控序、假、「」、「输文失败」、「」、「」。
完。

等执 接受 参数：
    请 为 新建 MCP请求。
    请 的 请求ID 为 控序。
    请 的 工具类型 为 MCP_工具_等待。
    请 的 参数 为 一组：参数。
    请 的 超时_ms 为 5000。
    应 为 做 MCP_处理请求 用 请。
    若 应 的 成功：
        返还 做 回建 用 控序、真、应 的 结果、「」、「」、「」。
    完。
    返还 做 回建 用 控序、假、「」、应 的 错误信息、「」、「」。
完。

页执 接受 参数：
    前 为 做 照采。
    结果 为 做 浏览器系统会话_执行页面动作 用 「navigate-current」、参数、「」。
    若 做 字符串查找 用 结果、「navigate-current:」 不等于 0：
        结果 为 做 浏览器系统会话_执行页面动作 用 「open-url」、参数、「」。
    完。
    若 做 字符串查找 用 结果、「navigate-current:」 等于 0 或者 做 字符串查找 用 结果、「open-url:」 等于 0：
        后 为 做 照采。
        上照 为 后。
        返还 做 回建 用 控序、真、结果、「」、做 照差 用 前、后、「」。
    完。
    返还 做 回建 用 控序、假、「」、「转页失败:」 加 参数、「」、「」。
完。

文执 接受 零参：
    页 为 做 浏览器系统会话_获取当前页面上下文。
    若 页 等于 空 或者 做 计数 用 页 小于 7：
        返还 做 回建 用 控序、假、「」、「页态不可用」、「」、「」。
    完。
    返还 做 回建 用 控序、真、「读文」、「」、「」、「{\"页摘\":\"」 加 做 串义 用 页 的 第 6 项 加 「\"}」。
完。

滚执 接受 参数：
    返还 做 回建 用 控序、假、「」、「当前页核未开放滚动动作:」 加 参数、「」、「」。
完。

脚执 接受 参数：
    结果 为 做 浏览器系统会话_执行页面动作 用 「run-js」、参数、「」。
    若 做 字符串查找 用 结果、「unsupported-page-action:」 等于 0：
        返还 做 回建 用 控序、假、「」、「当前页核未开放跑脚」、「」、「」。
    完。
    返还 做 回建 用 控序、真、结果、「」、「」、「」。
完。

令执 接受 参数：
    若 做 字符串查找 用 参数、「nova://」 等于 0：
        返还 做 页执 用 参数。
    完。
    返还 做 回建 用 控序、假、「」、「下令暂只接受页面地址」、「」、「」。
完。

流执 接受 零参：
    返还 做 回建 用 控序、真、「订流已接收」、「」、「当前版采用轮询订流」、「{\"轮询\":true}」。
完。

控执 接受 封：
    若 封 的 方法 等于 法心跳：
        返还 做 回建 用 封 的 编号、真、「心跳正常」、「」、「」、做 控统。
    完。
    若 封 的 方法 等于 法取照：
        照 为 做 照采。
        上照 为 照。
        返还 做 回建 用 封 的 编号、真、做 照摘要 用 照、「」、「」、做 照串 用 照。
    完。
    若 封 的 方法 等于 法取树：
        返还 做 回建 用 封 的 编号、真、「界树已返回」、「」、「」、做 树串。
    完。
    若 封 的 方法 等于 法找点：
        返还 做 回建 用 封 的 编号、真、「找点完成」、「」、「」、做 点找串 用 封 的 参数。
    完。
    若 封 的 方法 等于 法点选：
        控序 为 封 的 编号。
        返还 做 点执 用 封 的 参数。
    完。
    若 封 的 方法 等于 法输文：
        控序 为 封 的 编号。
        返还 做 输执 用 封 的 参数。
    完。
    若 封 的 方法 等于 法滚动：
        控序 为 封 的 编号。
        返还 做 滚执 用 封 的 参数。
    完。
    若 封 的 方法 等于 法等待：
        控序 为 封 的 编号。
        返还 做 等执 用 封 的 参数。
    完。
    若 封 的 方法 等于 法转页：
        控序 为 封 的 编号。
        返还 做 页执 用 封 的 参数。
    完。
    若 封 的 方法 等于 法读文：
        控序 为 封 的 编号。
        返还 做 文执。
    完。
    若 封 的 方法 等于 法跑脚：
        控序 为 封 的 编号。
        返还 做 脚执 用 封 的 参数。
    完。
    若 封 的 方法 等于 法下令：
        控序 为 封 的 编号。
        返还 做 令执 用 封 的 参数。
    完。
    若 封 的 方法 等于 法订流：
        控序 为 封 的 编号。
        返还 做 流执。
    完。
    返还 做 回建 用 封 的 编号、假、「」、「未知方法:」 加 封 的 方法、「」、「」。
完。

控串 接受 文本：
    做 控初。
    行 为 做 字符串去首尾空白 用 文本。
    若 做 字符串长度 用 行 等于 0：
        返还 「」。
    完。
    控收数 为 控收数 加 1。
    封 为 做 控析 用 行。
    若 封 的 编号 小于或等于 0：
        封 的 编号 为 控序。
    完。
    控序 为 封 的 编号 加 1。
    回 为 做 控执 用 封。
    返还 做 回串 用 回。
完。

可变 AI_控核_运行指标 为 0。

可变 AI_控核_异常指标 为 0。

AI_控核_自诊断 接受 零参：
    AI_控核_运行指标 为 0。
    返还 AI_控核_运行指标。
完。

AI_控核_优化建议 接受 零参：
    指标 为 做 AI_控核_自诊断。
    返还 「控核状态正常」。
完。

本章 收束。
