《界面元素渲染》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《.·帧缓冲》。

引用《.·写寄存器》。

引用《.·帧缓冲复制区》。

最大元素 为 512。

元素_矩形 为 1。

元素_文本 为 2。

元素_图像 为 3。

元素_按钮 为 4。

元素_输入框 为 5。

元素_滚动区 为 6。

界面元素 是 一种 事物，含 标识、类型、横坐标、纵坐标、宽度、高度、背景色、前景色、边框色、边框宽、可见、可点击、父元素、子元素数、数据指针、脏标记。

可变 元素表 为 一组：（空）。

可变 元素数 为 0。

可变 下一标识 为 1。

可变 渲染帧数 为 0。

可变 脏元素数 为 0。

可变 点击事件数 为 0。

界面_初始化 接受 零参：
    元素表 为 一组：（空）。
    元素数 为 0。
    下一标识 为 1。
完。

界面_创建元素 接受 类型、横、纵、宽、高、背景、父：
    若 元素数 大于或等于 最大元素：
        返还 负 1。
    完。
    元素 为 新建 界面元素。
    元素 的 标识 为 下一标识。
    下一标识 为 下一标识 加 1。
    元素 的 类型 为 类型。
    元素 的 横坐标 为 横。
    元素 的 纵坐标 为 纵。
    元素 的 宽度 为 宽。
    元素 的 高度 为 高。
    元素 的 背景色 为 背景。
    元素 的 前景色 为 0xFFFFFF。
    元素 的 边框色 为 0x888888。
    元素 的 边框宽 为 1。
    元素 的 可见 为 真。
    元素 的 可点击 为 类型 等于 元素_按钮。
    元素 的 父元素 为 父。
    元素 的 子元素数 为 0。
    元素 的 数据指针 为 0。
    元素 的 脏标记 为 真。
    做 追加 用 元素表、元素。
    脏元素数 为 脏元素数 加 1。
    索引 为 元素数。
    元素数 为 元素数 加 1。
    返还 索引。
完。

界面_设位置 接受 索引、横、纵：
    若 索引 大于或等于 元素数：
        返还。
    完。
    元素表 的 第 索引 项 的 横坐标 为 横。
    元素表 的 第 索引 项 的 纵坐标 为 纵。
    元素表 的 第 索引 项 的 脏标记 为 真。
    脏元素数 为 脏元素数 加 1。
完。

界面_设大小 接受 索引、宽、高：
    若 索引 大于或等于 元素数：
        返还。
    完。
    元素表 的 第 索引 项 的 宽度 为 宽。
    元素表 的 第 索引 项 的 高度 为 高。
    元素表 的 第 索引 项 的 脏标记 为 真。
    脏元素数 为 脏元素数 加 1。
完。

界面_设颜色 接受 索引、背景、前景：
    若 索引 大于或等于 元素数：
        返还。
    完。
    元素表 的 第 索引 项 的 背景色 为 背景。
    元素表 的 第 索引 项 的 前景色 为 前景。
    元素表 的 第 索引 项 的 脏标记 为 真。
完。

界面_设可见 接受 索引、可见：
    若 索引 大于或等于 元素数：
        返还。
    完。
    元素表 的 第 索引 项 的 可见 为 可见。
    元素表 的 第 索引 项 的 脏标记 为 真。
完。

界面_点击测试 接受 横、纵：
    可变 序号 为 元素数 减 1。
    只要 序号 大于或等于 0：
        元素 为 元素表 的 第 序号 项。
        若 元素 的 可见 并且 元素 的 可点击：
            若 横 大于或等于 元素 的 横坐标 并且 横 小于 元素 的 横坐标 加 元素 的 宽度 并且 纵 大于或等于 元素 的 纵坐标 并且 纵 小于 元素 的 纵坐标 加 元素 的 高度：
                点击事件数 为 点击事件数 加 1。
                返还 序号。
            完。
        完。
        序号 为 序号 减 1。
    完。
    返还 负 1。
完。

界面_渲染帧 接受 零参：
    渲染帧数 为 渲染帧数 加 1。
    可变 已渲染 为 0。
    可变 序号 为 0。
    只要 序号 小于 元素数：
        元素 为 元素表 的 第 序号 项。
        若 元素 的 可见 并且 元素 的 脏标记：
            做 界面_渲染元素 用 元素。
            元素 的 脏标记 为 假。
            已渲染 为 已渲染 加 1。
        完。
        序号 为 序号 加 1。
    完。
    脏元素数 为 0。
    返还 已渲染。
完。

界面_渲染元素 接受 元素：
    做 帧缓冲_填充矩形 用 元素 的 横坐标、元素 的 纵坐标、元素 的 宽度、元素 的 高度、元素 的 背景色。
    若 元素 的 边框宽 大于 0：
        做 帧缓冲_水平线 用 元素 的 横坐标、元素 的 纵坐标、元素 的 宽度、元素 的 边框色。
        做 帧缓冲_水平线 用 元素 的 横坐标、元素 的 纵坐标 加 元素 的 高度 减 1、元素 的 宽度、元素 的 边框色。
        做 帧缓冲_垂直线 用 元素 的 横坐标、元素 的 纵坐标、元素 的 高度、元素 的 边框色。
        做 帧缓冲_垂直线 用 元素 的 横坐标 加 元素 的 宽度 减 1、元素 的 纵坐标、元素 的 高度、元素 的 边框色。
    完。
完。

界面_标记全脏 接受 零参：
    可变 序号 为 0。
    只要 序号 小于 元素数：
        元素表 的 第 序号 项 的 脏标记 为 真。
        序号 为 序号 加 1。
    完。
    脏元素数 为 元素数。
完。

界面_删除元素 接受 索引：
    若 索引 大于或等于 元素数：
        返还。
    完。
    元素表 的 第 索引 项 的 可见 为 假。
    元素表 的 第 索引 项 的 可点击 为 假。
完。

界面渲染_完整统计 接受 零参：
    返还 一组：元素数、下一标识 减 1、渲染帧数、脏元素数、点击事件数。
完。

可变 AI_界面渲染完_热节点缓存 为 0。

AI_界面渲染完_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_界面渲染完_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_界面渲染完_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

本章 收束。
