《设备节点》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《.·PCM音频流》。

引用《..·中枢·字符串替换》。

引用《..·引导·端口IO》。

引用《.·软件混音器》。

引用《..·中枢·内核打印》。

引用《..·中枢·整数转字符串》。

引用《..·执行·特文模拟》。

音频_创建设备节点 接受 零参：
    做 确保目录存在 用 「/dev/snd」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 声卡列表：
        card 为 声卡列表 的 第 i 项。
        做 devtmpfs_创建 用 「/dev/snd/controlC」 加 做 整数转字符串 用 i。
        做 devtmpfs_创建 用 「/dev/snd/pcmC」 加 做 整数转字符串 用 i 加 「D0p」。
        做 devtmpfs_创建 用 「/dev/snd/pcmC」 加 做 整数转字符串 用 i 加 「D0c」。
        若 i 等于 0：
            做 devtmpfs_创建 用 「/dev/snd/timer」。
        完。
        i 为 i 加 1。
    完。
完。

音频_Nova设备控制 接受 fd、cmd、参数：
    若 cmd 等于 SNDRV_PCM_Nova设备控制_HW_PARAMS：
        返还 做 音频_Nova设备控制_hw_params 用 fd、参数。
    完。
    若 cmd 等于 SNDRV_PCM_Nova设备控制_PREPARE：
        流 为 做 音频_获取流 用 fd。
        若 流 等于 空：
            返还 错误_无效FD。
        完。
        返还 做 PCM_准备 用 流。
    完。
    若 cmd 等于 SNDRV_PCM_Nova设备控制_START：
        流 为 做 音频_获取流 用 fd。
        若 流 等于 空：
            返还 错误_无效FD。
        完。
        返还 做 PCM_启动 用 流。
    完。
    若 cmd 等于 SNDRV_PCM_Nova设备控制_DROP：
        流 为 做 音频_获取流 用 fd。
        若 流 等于 空：
            返还 错误_无效FD。
        完。
        返还 做 PCM_停止 用 流。
    完。
    若 cmd 等于 SNDRV_PCM_Nova设备控制_WRITEI_FRAMES：
        流 为 做 音频_获取流 用 fd。
        若 流 等于 空：
            返还 错误_无效FD。
        完。
        返还 做 PCM_写帧 用 流、参数、参数。
    完。
    若 cmd 等于 SNDRV_CTL_Nova设备控制_ELEM_READ：
        返还 做 音频_读混音器 用 参数。
    完。
    若 cmd 等于 SNDRV_CTL_Nova设备控制_ELEM_WRITE：
        返还 做 音频_写混音器 用 参数。
    完。
    返还 错误_无效参数。
完。

音频_Nova设备控制_hw_params 接受 fd、参数：
    流 为 做 音频_获取流 用 fd。
    若 流 等于 空：
        返还 错误_无效FD。
    完。
    若 参数 等于 0：
        返还 错误_无效参数。
    完。
    格式掩码 为 做 内存读32 用 参数 加 12。
    可变 格式 为 SND_PCM_FORMAT_S16_LE。
    若 格式掩码 位与 1 左移 SND_PCM_FORMAT_S32_LE 不等于 0：
        格式 为 SND_PCM_FORMAT_S32_LE。
    否则若 格式掩码 位与 1 左移 SND_PCM_FORMAT_S24_LE 不等于 0：
        格式 为 SND_PCM_FORMAT_S24_LE。
    否则若 格式掩码 位与 1 左移 SND_PCM_FORMAT_S16_LE 不等于 0：
        格式 为 SND_PCM_FORMAT_S16_LE。
    完。
    采样率_min 为 做 内存读32 用 参数 加 128。
    采样率_max 为 做 内存读32 用 参数 加 132。
    可变 采样率 为 48000。
    若 采样率_min 大于 0 并且 采样率_min 小于或等于 192000：
        采样率 为 采样率_min。
    完。
    若 采样率 小于 8000：
        采样率 为 8000。
    完。
    若 采样率 大于 192000：
        采样率 为 192000。
    完。
    声道_min 为 做 内存读32 用 参数 加 68。
    可变 声道数 为 2。
    若 声道_min 大于或等于 1 并且 声道_min 小于或等于 8：
        声道数 为 声道_min。
    完。
    周期帧_min 为 做 内存读32 用 参数 加 152。
    若 周期帧_min 大于 0 并且 周期帧_min 小于或等于 16384：
        流 的 周期帧数 为 周期帧_min。
    完。
    缓冲帧_min 为 做 内存读32 用 参数 加 164。
    若 缓冲帧_min 大于 0 并且 缓冲帧_min 小于或等于 65536：
        流 的 缓冲帧数 为 缓冲帧_min。
    完。
    做 内核信息 用 「音频: hw_params 格式=」 加 做 整数转字符串 用 格式 加 「 采样率=」 加 做 整数转字符串 用 采样率 加 「 声道=」 加 做 整数转字符串 用 声道数 加 「 周期=」 加 做 整数转字符串 用 流 的 周期帧数 加 「 缓冲=」 加 做 整数转字符串 用 流 的 缓冲帧数。
    返还 做 PCM_设置参数 用 流、格式、采样率、声道数。
完。

可变 fd_流映射 为 一组：（空）。

音频_注册fd映射 接受 fd、声卡索引、pcm索引、方向：
    做 追加 用 fd_流映射、一组：fd、声卡索引、pcm索引、方向。
完。

音频_获取流 接受 fd：
    可变 i 为 0。
    只要 i 小于 做 计数 用 fd_流映射：
        若 fd_流映射 的 第 i 项 的 第 0 项 等于 fd：
            ci 为 fd_流映射 的 第 i 项 的 第 1 项。
            pi 为 fd_流映射 的 第 i 项 的 第 2 项。
            dir 为 fd_流映射 的 第 i 项 的 第 3 项。
            若 ci 小于 做 计数 用 声卡列表 并且 pi 小于 做 计数 用 声卡列表 的 第 ci 项 的 PCM列表：
                若 dir 等于 SND_PCM_STREAM_PLAYBACK：
                    返还 声卡列表 的 第 ci 项 的 PCM列表 的 第 pi 项 的 播放。
                否则：
                    返还 声卡列表 的 第 ci 项 的 PCM列表 的 第 pi 项 的 录音。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 声卡列表 等于 0：
        返还 空。
    完。
    若 做 计数 用 声卡列表 的 第 0 项 的 PCM列表 等于 0：
        返还 空。
    完。
    返还 声卡列表 的 第 0 项 的 PCM列表 的 第 0 项 的 播放。
完。

音频_读混音器 接受 参数：
    若 参数 等于 0：
        返还 错误_无效参数。
    完。
    若 做 计数 用 声卡列表 等于 0：
        返还 错误_设备忙。
    完。
    card 为 声卡列表 的 第 0 项。
    控件索引 为 做 内存读32 用 参数 加 4。
    若 控件索引 等于 0 或者 控件索引 大于 做 计数 用 card 的 混音器列表：
        返还 错误_无效参数。
    完。
    ctrl 为 card 的 混音器列表 的 第 控件索引 减 1 项。
    若 ctrl 的 类型 等于 0：
        做 内存写32 用 参数 加 64、ctrl 的 当前值。
    否则若 ctrl 的 类型 等于 1：
        可变 ch 为 0。
        只要 ch 小于 ctrl 的 声道数：
            做 内存写32 用 参数 加 64 加 ch 乘 4、ctrl 的 当前值。
            ch 为 ch 加 1。
        完。
    完。
    返还 0。
完。

音频_写混音器 接受 参数：
    若 参数 等于 0：
        返还 错误_无效参数。
    完。
    若 做 计数 用 声卡列表 等于 0：
        返还 错误_设备忙。
    完。
    card 为 声卡列表 的 第 0 项。
    控件索引 为 做 内存读32 用 参数 加 4。
    若 控件索引 等于 0 或者 控件索引 大于 做 计数 用 card 的 混音器列表：
        返还 错误_无效参数。
    完。
    ctrl 为 card 的 混音器列表 的 第 控件索引 减 1 项。
    新值 为 做 内存读32 用 参数 加 64。
    若 新值 小于 ctrl 的 最小值：
        新值 为 ctrl 的 最小值。
    完。
    若 新值 大于 ctrl 的 最大值：
        新值 为 ctrl 的 最大值。
    完。
    ctrl 的 当前值 为 新值。
    若 ctrl 的 名称 等于 「Master Playback Volume」：
        音频_主音量 为 新值。
        hda_gain 为 新值 乘 0x7F 除以 100。
        amp_verb 为 声驱_VERB_SET_AMP_GAIN 位或 0xB000 位或 hda_gain 位与 0x7F。
        做 声驱_发送命令 用 card、0、2、amp_verb。
        做 内核调试 用 「音频: 主音量=」 加 做 整数转字符串 用 新值 加 「 声驱增益=」 加 做 整数转十六进制 用 hda_gain。
    完。
    若 ctrl 的 名称 等于 「Master Playback Switch」：
        音频_静音 为 新值 等于 0。
        mute_bit 为 0。
        若 音频_静音：
            mute_bit 为 0x80。
        完。
        做 声驱_发送命令 用 card、0、2、声驱_VERB_SET_AMP_GAIN 位或 0xB000 位或 mute_bit。
    完。
    返还 0。
完。

设备节点_统计 接受 零参：
    返还 一本字典：模块 对应 「设备节点」、fd_流映射 对应 做 计数 用 fd_流映射。
完。

设备节点_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    返还 做 计数 用 问题 等于 0。
完。

设备节点_综合报告 接受 零参：
    做 打印 用 「[设备节点] ═══ 综合报告 ═══」。
    做 打印 用 「  fd_流映射: 」 加 做 整数转字符串 用 做 计数 用 fd_流映射。
    做 打印 用 「  健康: 」 加 若 做 设备节点_健康评估 则 「正常」 否则 「异常」。
完。

设备节点_全局重置 接受 零参：
    做 打印 用 「[设备节点] 全局统计已重置」。
    返还 0。
完。

本章 收束。
