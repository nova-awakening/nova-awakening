《样式内联样》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·中枢·硬链接》。

引用《.·NML标记语解析》。

引用《..·感知·表单数据编程》。

引用《..·执行·组件网化》。

样式_解析内联 接受 样式文本：
    s 为 新建 原生计算样式。
    颜色 为 做 样式_提取属性 用 样式文本、「color」。
    若 颜色 不等于 「」：
        s 的 前景色 为 做 样式_解析颜色 用 颜色。
    完。
    背景 为 做 样式_提取属性 用 样式文本、「background」。
    若 背景 等于 「」：
        背景 为 做 样式_提取属性 用 样式文本、「background-color」。
    完。
    若 背景 不等于 「」：
        s 的 背景色 为 做 样式_解析颜色 用 背景。
    完。
    字号 为 做 样式_提取属性 用 样式文本、「font-size」。
    若 字号 不等于 「」：
        s 的 字号 为 做 样式_解析像素值 用 字号。
    完。
    显示 为 做 样式_提取属性 用 样式文本、「display」。
    若 显示 等于 「none」：
        s 的 显示 为 3。
    否则若 显示 等于 「flex」：
        s 的 显示 为 2。
    否则若 显示 等于 「inline」：
        s 的 显示 为 1。
    否则若 显示 等于 「block」：
        s 的 显示 为 0。
    完。
    返还 s。
完。

样式_提取属性 接受 样式、属性名：
    搜索 为 属性名 加 「:」。
    位置 为 做 NML_查找子串 用 样式、搜索。
    若 位置 小于 0：
        返还 「」。
    完。
    值开始 为 位置 加 做 字符串长度 用 搜索。
    只要 值开始 小于 做 字符串长度 用 样式 并且 做 字符串取字节 用 样式、值开始 等于 32：
        值开始 为 值开始 加 1。
    完。
    可变 值结束 为 值开始。
    只要 值结束 小于 做 字符串长度 用 样式 并且 做 字符串取字节 用 样式、值结束 不等于 59：
        值结束 为 值结束 加 1。
    完。
    返还 做 NML_修剪空白 用 做 NML_子串 用 样式、值开始、值结束。
完。

样式_解析颜色 接受 颜色文本：
    若 做 字符串长度 用 颜色文本 大于 0 并且 做 字符串取字节 用 颜色文本、0 等于 35：
        若 做 字符串长度 用 颜色文本 等于 7：
            返还 做 十六进制解析 用 颜色文本、1、7。
        完。
        若 做 字符串长度 用 颜色文本 等于 4：
            r 为 做 十六进制字符值 用 做 字符串取字节 用 颜色文本、1。
            g 为 做 十六进制字符值 用 做 字符串取字节 用 颜色文本、2。
            b 为 做 十六进制字符值 用 做 字符串取字节 用 颜色文本、3。
            返还 r 乘 17 左移 16 位或 g 乘 17 左移 8 位或 b 乘 17。
        完。
    完。
    若 颜色文本 等于 「red」：
        返还 0xFF0000。
    完。
    若 颜色文本 等于 「green」：
        返还 0x00FF00。
    完。
    若 颜色文本 等于 「blue」：
        返还 0x0000FF。
    完。
    若 颜色文本 等于 「white」：
        返还 0xFFFFFF。
    完。
    若 颜色文本 等于 「black」：
        返还 0x000000。
    完。
    若 颜色文本 等于 「gray」 或者 颜色文本 等于 「grey」：
        返还 0x808080。
    完。
    若 颜色文本 等于 「yellow」：
        返还 0xFFFF00。
    完。
    若 颜色文本 等于 「orange」：
        返还 0xFFA500。
    完。
    若 颜色文本 等于 「purple」：
        返还 0x800080。
    完。
    若 颜色文本 等于 「transparent」：
        返还 0x00000000。
    完。
    返还 0x000000。
完。

样式_解析像素值 接受 文本：
    可变 值 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        ch 为 做 字符串取字节 用 文本、i。
        若 ch 大于或等于 48 并且 ch 小于或等于 57：
            值 为 值 乘 10 加 (ch 减 48)。
        否则：
            i 为 做 字符串长度 用 文本。
        完。
        i 为 i 加 1。
    完。
    返还 值。
完。

NML_按ID查找 接受 根、id值：
    若 根 等于 空：
        返还 空。
    完。
    当前id 为 做 元素_获取属性 用 根、做 字符串哈希 用 「id」。
    若 当前id 等于 id值：
        返还 根。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 根 的 子节点：
        结果 为 做 NML_按ID查找 用 根 的 子节点 的 第 i 项、id值。
        若 结果 不等于 空：
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

NML_get元素sByTag 接受 根、标签ID：
    可变 结果 为 一组：（空）。
    做 NML_收集标签 用 根、标签ID、结果。
    返还 结果。
完。

NML_收集标签 接受 节点、标签ID、结果：
    若 节点 等于 空：
        返还。
    完。
    若 节点 的 标签 等于 标签ID：
        做 追加 用 结果、节点。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点 的 子节点：
        做 NML_收集标签 用 节点 的 子节点 的 第 i 项、标签ID、结果。
        i 为 i 加 1。
    完。
完。

本章 收束。
