《图处统一操作》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《.·NVIDIA推送缓冲》。

引用《.·显示模式设》。

引用《.·帧提交双缓冲》。

引用《.·AMD_PM4命令提交》。

引用《..·执行·图处上下文》。

引用《.·渲染管线》。

引用《.·AMD中断处理》。

引用《.·NVIDIA引擎初始化》。

图处_填充矩形 接受 x、y、宽、高、颜色：
    若 当前驱动类型 等于 DRV_NVIDIA：
        做 NV_2D_矩形填充 用 x、y、宽、高、颜色。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        做 AMD_2D_矩形填充 用 x、y、宽、高、颜色。
    完。
    否则。
    块：
        做 渲染_矩形填充 用 x、y、宽、高、颜色。
    完。
完。

图处_位块传输 接受 源x、源y、目x、目y、宽、高：
    若 当前驱动类型 等于 DRV_NVIDIA：
        做 NV_2D_位块传输 用 源x、源y、目x、目y、宽、高。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        做 AMD_直传_复制 用 0、0、宽 乘 高 乘 4。
    完。
    否则。
    块：
        做 渲染_位块传输 用 源x、源y、目x、目y、宽、高。
    完。
完。

图处_计算分发 接受 核函数、网格X、网格Y、网格Z、块X、块Y、块Z：
    若 当前驱动类型 等于 DRV_NVIDIA：
        返还 做 NV_计算分发 用 核函数、网格X、网格Y、网格Z、块X、块Y、块Z。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        返还 做 AMD_计算分发 用 核函数、网格X、网格Y、网格Z、块X、块Y、块Z。
    完。
    返还 错误_不支持。
完。

图处_设置显示模式 接受 头索引、宽、高、刷新率：
    若 当前驱动类型 等于 DRV_NVIDIA：
        做 NV_设置显示模式 用 头索引、宽、高、刷新率。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        做 AMD_设置显示模式 用 头索引、宽、高、刷新率。
    完。
    否则。
    块：
        若 当前图处 不等于 空：
            做 图处_设置显示 用 当前图处、当前图处 的 BAR0_地址、宽、高、刷新率。
        完。
    完。
完。

图处_提交帧 接受 零参：
    做 渲染_提交帧。
完。

图处_AI推理_统一 接受 输入、输入大小、权重、权重大小：
    若 当前驱动类型 等于 DRV_NVIDIA 或者 当前驱动类型 等于 DRV_AMD：
        返还 做 图处_AI推理 用 输入、输入大小、权重、权重大小。
    完。
    返还 空。
完。

图处_直传_复制 接受 源、目标、大小：
    若 当前驱动类型 等于 DRV_AMD：
        做 AMD_直传_复制 用 源、目标、大小。
    否则：
        若 做 计数 用 图处_命令队列列表 大于 0：
            命令 为 新建 图处命令。
            命令 的 类型 为 CMD_直传_COPY。
            命令 的 参数0 为 源。
            命令 的 参数1 为 目标。
            命令 的 参数2 为 大小。
            做 图处_提交命令 用 图处_命令队列列表 的 第 0 项、命令。
        完。
    完。
完。

图处_读取温度 接受 零参：
    若 当前驱动类型 等于 DRV_AMD：
        返还 做 AMD_读取温度。
    完。
    返还 0。
完。

图处_读取功耗 接受 零参：
    若 当前驱动类型 等于 DRV_AMD：
        返还 做 AMD_读取功耗。
    完。
    返还 0。
完。

图处_有光追 接受 零参：
    若 当前图处 等于 空：
        返还 假。
    完。
    返还 当前图处 的 能力 位与 图处_CAP_RAY_TRACE 不等于 0。
完。

图处_有AI加速器 接受 零参：
    若 当前图处 等于 空：
        返还 假。
    完。
    返还 当前图处 的 能力 位与 图处_CAP_智能_ACCEL 不等于 0。
完。

图处_有计算能力 接受 零参：
    若 当前图处 等于 空：
        返还 假。
    完。
    返还 当前图处 的 能力 位与 图处_CAP_COMPUTE 不等于 0。
完。

图处_计算单元数 接受 零参：
    若 当前图处 等于 空：
        返还 0。
    完。
    返还 当前图处 的 计算单元数。
完。

图处_厂商名称 接受 零参：
    若 当前图处 等于 空：
        返还 「无」。
    完。
    返还 当前图处 的 厂商名。
完。

图处_型号名称 接受 零参：
    若 当前图处 等于 空：
        返还 「无」。
    完。
    返还 当前图处 的 型号名。
完。

图处_驱动类型名 接受 零参：
    若 当前驱动类型 等于 DRV_NVIDIA：
        返还 「nova-nv」。
    完。
    若 当前驱动类型 等于 DRV_AMD：
        返还 「nova-amd」。
    完。
    若 当前驱动类型 等于 DRV_INTEL：
        返还 「nova-intel」。
    完。
    若 当前驱动类型 等于 DRV_VIRTIO：
        返还 「virtio-gpu」。
    完。
    若 当前驱动类型 等于 DRV_GENERIC：
        返还 「generic-fb」。
    完。
    返还 「none」。
完。

本章 收束。
