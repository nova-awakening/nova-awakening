《字体写字形》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·引导·端口IO》。

引用《..·中枢·硬链接》。

字体_写字形 接受 字符码、行数据：
    若 字符码 小于 字体_首字符 或者 字符码 大于 字体_末字符：
        返还。
    完。
    偏移 为 (字符码 减 字体_首字符) 乘 字体_高。
    可变 i 为 0。
    只要 i 小于 字体_高 并且 i 小于 做 计数 用 行数据：
        字体数据 的 第 偏移 加 i 项 为 行数据 的 第 i 项。
        i 为 i 加 1。
    完。
完。

外部。

屏幕宽。

外部。

屏幕位深。

字体_渲染字符 接受 fb、x、y、字符码、前景色、背景色、缩放：
    若 字符码 小于 字体_首字符 或者 字符码 大于 字体_末字符：
        字符码 为 0x3F。
        若 字符码 大于 字体_末字符：
            返还。
        完。
    完。
    偏移 为 (字符码 减 字体_首字符) 乘 字体_高。
    bpp 为 屏幕位深 除以 8。
    若 bpp 小于 3：
        bpp 为 4。
    完。
    可变 行 为 0。
    只要 行 小于 字体_高：
        行数据 为 字体数据 的 第 偏移 加 行 项。
        可变 列 为 0。
        只要 列 小于 字体_宽：
            像素亮 为 行数据 右移 7 减 列 位与 1。
            颜色 为 背景色。
            若 像素亮 等于 1：
                颜色 为 前景色。
            完。
            可变 sy 为 0。
            只要 sy 小于 缩放：
                可变 sx 为 0。
                只要 sx 小于 缩放：
                    px 为 x 加 列 乘 缩放 加 sx。
                    py 为 y 加 行 乘 缩放 加 sy。
                    像素偏移 为 (py 乘 屏幕宽 加 px) 乘 bpp。
                    做 内存写8 用 fb 加 像素偏移、颜色 位与 0xFF。
                    做 内存写8 用 fb 加 像素偏移 加 1、颜色 右移 8 位与 0xFF。
                    做 内存写8 用 fb 加 像素偏移 加 2、颜色 右移 16 位与 0xFF。
                    做 内存写8 用 fb 加 像素偏移 加 3、0xFF。
                    sx 为 sx 加 1。
                完。
                sy 为 sy 加 1。
            完。
            列 为 列 加 1。
        完。
        行 为 行 加 1。
    完。
完。

字体_渲染字符_透明 接受 fb、x、y、字符码、前景色、缩放：
    若 字符码 小于 字体_首字符 或者 字符码 大于 字体_末字符：
        返还。
    完。
    偏移 为 (字符码 减 字体_首字符) 乘 字体_高。
    bpp 为 屏幕位深 除以 8。
    若 bpp 小于 3：
        bpp 为 4。
    完。
    可变 行 为 0。
    只要 行 小于 字体_高：
        行数据 为 字体数据 的 第 偏移 加 行 项。
        可变 列 为 0。
        只要 列 小于 字体_宽：
            若 行数据 右移 7 减 列 位与 1 等于 1：
                可变 sy 为 0。
                只要 sy 小于 缩放：
                    可变 sx 为 0。
                    只要 sx 小于 缩放：
                        px 为 x 加 列 乘 缩放 加 sx。
                        py 为 y 加 行 乘 缩放 加 sy。
                        像素偏移 为 (py 乘 屏幕宽 加 px) 乘 bpp。
                        做 内存写8 用 fb 加 像素偏移、前景色 位与 0xFF。
                        做 内存写8 用 fb 加 像素偏移 加 1、前景色 右移 8 位与 0xFF。
                        做 内存写8 用 fb 加 像素偏移 加 2、前景色 右移 16 位与 0xFF。
                        做 内存写8 用 fb 加 像素偏移 加 3、0xFF。
                        sx 为 sx 加 1。
                    完。
                    sy 为 sy 加 1。
                完。
            完。
            列 为 列 加 1。
        完。
        行 为 行 加 1。
    完。
完。

字体_渲染文本 接受 fb、x、y、文本、前景色、背景色、缩放：
    字符宽 为 字体_宽 乘 缩放。
    字符高 为 字体_高 乘 缩放。
    可变 cx 为 x。
    可变 cy 为 y。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        ch 为 做 字符串取字节 用 文本、i。
        若 ch 等于 10：
            cx 为 x。
            cy 为 cy 加 字符高。
        完。
        做 否则如果 用 ch 等于 13。
        块：
            cx 为 x。
        完。
        做 否则如果 用 ch 等于 9。
        块：
            cx 为 cx 加 字符宽 乘 4。
        完。
        否则。
        块：
            做 字体_渲染字符 用 fb、cx、cy、ch、前景色、背景色、缩放。
            cx 为 cx 加 字符宽。
        完。
        i 为 i 加 1。
    完。
完。

字体_渲染文本_透明 接受 fb、x、y、文本、前景色、缩放：
    字符宽 为 字体_宽 乘 缩放。
    字符高 为 字体_高 乘 缩放。
    可变 cx 为 x。
    可变 cy 为 y。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        ch 为 做 字符串取字节 用 文本、i。
        若 ch 等于 10：
            cx 为 x。
            cy 为 cy 加 字符高。
        完。
        做 否则如果 用 ch 等于 13。
        块：
            cx 为 x。
        完。
        做 否则如果 用 ch 等于 9。
        块：
            cx 为 cx 加 字符宽 乘 4。
        完。
        否则。
        块：
            做 字体_渲染字符_透明 用 fb、cx、cy、ch、前景色、缩放。
            cx 为 cx 加 字符宽。
        完。
        i 为 i 加 1。
    完。
完。

字体_文本宽度 接受 文本、缩放：
    字符宽 为 字体_宽 乘 缩放。
    可变 最大宽 为 0。
    可变 当前宽 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        ch 为 做 字符串取字节 用 文本、i。
        若 ch 等于 10：
            若 当前宽 大于 最大宽：
                最大宽 为 当前宽。
            完。
            当前宽 为 0。
        完。
        做 否则如果 用 ch 等于 9。
        块：
            当前宽 为 当前宽 加 字符宽 乘 4。
        完。
        否则。
        块：
            当前宽 为 当前宽 加 字符宽。
        完。
        i 为 i 加 1。
    完。
    若 当前宽 大于 最大宽：
        最大宽 为 当前宽。
    完。
    返还 最大宽。
完。

字体_文本高度 接受 文本、缩放：
    字符高 为 字体_高 乘 缩放。
    可变 行数 为 1。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        若 做 字符串取字节 用 文本、i 等于 10：
            行数 为 行数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 行数 乘 字符高。
完。

可变 AI_字体写字形_帧率窗口 为 一组：（空）。

AI_字体写字形_帧率预测 接受 当前帧耗时：
    做 追加 用 AI_字体写字形_帧率窗口、当前帧耗时。
    若 做 计数 用 AI_字体写字形_帧率窗口 大于 30：
        做 列表_移除 用 AI_字体写字形_帧率窗口、0。
    完。
    若 做 计数 用 AI_字体写字形_帧率窗口 小于 3：
        返还 60。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_字体写字形_帧率窗口：
        总 为 总 加 AI_字体写字形_帧率窗口 的 第 i 项。
        i 为 i 加 1。
    完。
    平均帧耗时 为 总 除以 做 计数 用 AI_字体写字形_帧率窗口。
    若 平均帧耗时 等于 0：
        返还 60。
    完。
    返还 1000 除以 平均帧耗时。
完。

AI_字体写字形_质量降级建议 接受 帧率：
    若 帧率 小于 30：
        返还 「降低分辨率」。
    完。
    若 帧率 小于 45：
        返还 「跳过阴影」。
    完。
    返还 「全质量」。
完。

本章 收束。
