《总线解析》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·中断表注册》。

引用《.·PCI配置读》。

引用《..·感知·内核直连层》。

引用《..·推理·定点Q算术》。

PCI_REG_厂商ID 为 0x00。

PCI_REG_设备ID 为 0x02。

PCI_REG_命令 为 0x04。

PCI_REG_状态 为 0x06。

PCI_REG_修订ID 为 0x08。

PCI_REG_类代码 为 0x09。

PCI_REG_缓存行 为 0x0C。

PCI_REG_延迟定时器 为 0x0D。

PCI_REG_头类型 为 0x0E。

PCI_REG_BIST 为 0x0F。

PCI_REG_BAR0 为 0x10。

PCI_REG_BAR1 为 0x14。

PCI_REG_BAR2 为 0x18。

PCI_REG_BAR3 为 0x1C。

PCI_REG_BAR4 为 0x20。

PCI_REG_BAR5 为 0x24。

PCI_REG_子系统厂商 为 0x2C。

PCI_REG_子系统ID 为 0x2E。

PCI_REG_扩展ROM 为 0x30。

PCI_REG_能力指针 为 0x34。

PCI_REG_中断线 为 0x3C。

PCI_REG_中断引脚 为 0x3D。

PCI_CMD_IO使能 为 0x0001。

PCI_CMD_内存使能 为 0x0002。

PCI_CMD_总线主控 为 0x0004。

PCI_CMD_特殊周期 为 0x0008。

PCI_CMD_写无效使能 为 0x0010。

PCI_CMD_VGA调色板 为 0x0020。

PCI_CMD_奇偶校验响应 为 0x0040。

PCI_CMD_SERR使能 为 0x0100。

PCI_CMD_快速背靠背 为 0x0200。

PCI_CMD_中断禁用 为 0x0400。

PCI_STS_能力列表 为 0x0010。

PCI_STS_66MHz 为 0x0020。

PCI_STS_快速背靠背 为 0x0080。

PCI_STS_主数据奇偶错 为 0x0100。

PCI_STS_信号目标中止 为 0x0800。

PCI_STS_接收目标中止 为 0x1000。

PCI_STS_接收主中止 为 0x2000。

PCI_STS_信号系统错误 为 0x4000。

PCI_STS_检测奇偶错 为 0x8000。

PCI_BAR_类型_IO 为 1。

PCI_BAR_类型_内存 为 0。

PCI_BAR_内存_32位 为 0x00。

PCI_BAR_内存_1M以下 为 0x02。

PCI_BAR_内存_64位 为 0x04。

PCI_BAR_内存_预取 为 0x08。

PCI_CAP_电源管理 为 0x01。

PCI_CAP_AGP 为 0x02。

PCI_CAP_VPD 为 0x03。

PCI_CAP_槽标识 为 0x04。

PCI_CAP_MSI 为 0x05。

PCI_CAP_热交换 为 0x06。

PCI_CAP_PCIX 为 0x07。

PCI_CAP_厂商专用 为 0x09。

PCI_CAP_调试端口 为 0x0A。

PCI_CAP_MSIX 为 0x11。

PCI_CAP_PCIE 为 0x10。

PCI_CAP_SATA 为 0x12。

PCI_CAP_高级特性 为 0x13。

PCIE_ECAP_AER 为 0x0001。

PCIE_ECAP_VC 为 0x0002。

PCIE_ECAP_序列号 为 0x0003。

PCIE_ECAP_电源预算 为 0x0004。

PCIE_ECAP_ACS 为 0x000D。

PCIE_ECAP_ARI 为 0x000E。

PCIE_ECAP_SRIOV 为 0x0010。

PCIE_ECAP_LTR 为 0x0018。

PCIE_ECAP_L1PM 为 0x001E。

PCI_PM_状态_D0 为 0x00。

PCI_PM_状态_D1 为 0x01。

PCI_PM_状态_D2 为 0x02。

PCI_PM_状态_D3热 为 0x03。

PCI_PM_掩码 为 0x03。

MSI_CTRL_使能 为 0x0001。

MSI_CTRL_多消息能力 为 0x000E。

MSI_CTRL_多消息使能 为 0x0070。

MSI_CTRL_64位地址 为 0x0080。

MSI_CTRL_每向量掩码 为 0x0100。

MSIX_CTRL_使能 为 0x8000。

MSIX_CTRL_函数掩码 为 0x4000。

MSIX_CTRL_表大小掩码 为 0x07FF。

可变 全局_BAR解析总数 为 0。

可变 全局_IO_BAR计数 为 0。

可变 全局_内存BAR计数 为 0。

可变 全局_64位BAR计数 为 0。

可变 全局_预取BAR计数 为 0。

可变 全局_能力扫描总数 为 0。

可变 全局_MSI配置计数 为 0。

可变 全局_MSIX配置计数 为 0。

可变 全局_电源转换计数 为 0。

可变 全局_PCIe设备计数 为 0。

可变 全局_MMIO映射总字节 为 0。

可变 全局_IO空间总字节 为 0。

可变 全局_BAR冲突计数 为 0。

可变 全局_设备使能计数 为 0。

可变 全局_设备禁用计数 为 0。

可变 全局_AI识别计数 为 0。

可变 全局_扩展能力扫描计数 为 0。

可变 全局_SRIOV检测计数 为 0。

可变 全局_BAR解析错误计数 为 0。

可变 全局_资源分配最大延迟 为 0。

BAR冲突表容量 为 256。

可变 BAR冲突哈希表 为 做 创建数组 用 BAR冲突表容量、空。

PCI_解析BAR 接受 dev：
    最大BAR 为 6。
    若 dev 的 头类型 等于 1：
        最大BAR 为 2。
    完。
    若 dev 的 头类型 等于 2：
        最大BAR 为 1。
    完。
    解析开始时间 为 做 系统_读取时间戳。
    可变 i 为 0。
    可变 有效BAR数 为 0。
    只要 i 小于 最大BAR：
        偏移 为 PCI_REG_BAR0 加 i 乘 4。
        原值 为 做 PCI设备_读32 用 dev、偏移。
        若 原值 等于 0：
            dev 的 BAR 的 第 i 项 的 类型 为 PCI_BAR_类型_内存。
            dev 的 BAR 的 第 i 项 的 基地址 为 0。
            dev 的 BAR 的 第 i 项 的 大小 为 0。
            dev 的 BAR 的 第 i 项 的 已映射 为 假。
            i 为 i 加 1。
            继续。
        完。
        原命令 为 做 PCI设备_读16 用 dev、PCI_REG_命令。
        做 PCI设备_写16 用 dev、PCI_REG_命令、原命令 位与 按位取反 (PCI_CMD_IO使能 位或 PCI_CMD_内存使能)。
        做 PCI设备_写32 用 dev、偏移、0xFFFFFFFF。
        大小值 为 做 PCI设备_读32 用 dev、偏移。
        做 PCI设备_写32 用 dev、偏移、原值。
        做 PCI设备_写16 用 dev、PCI_REG_命令、原命令。
        若 大小值 等于 0 或者 大小值 等于 0xFFFFFFFF：
            全局_BAR解析错误计数 为 全局_BAR解析错误计数 加 1。
            i 为 i 加 1。
            继续。
        完。
        若 原值 位与 1：
            做 BAR_解析IO空间 用 dev、i、原值、大小值。
            全局_IO_BAR计数 为 全局_IO_BAR计数 加 1。
        否则：
            类型位 为 原值 右移 1 位与 0x03。
            是预取 为 原值 位与 PCI_BAR_内存_预取 不等于 0。
            若 类型位 等于 0x02：
                做 BAR_解析64位内存 用 dev、i、偏移、原值、大小值、是预取。
                全局_64位BAR计数 为 全局_64位BAR计数 加 1。
                i 为 i 加 1。
            否则若 类型位 等于 0x01：
                做 BAR_解析低端内存 用 dev、i、原值、大小值、是预取。
            否则：
                做 BAR_解析32位内存 用 dev、i、原值、大小值、是预取。
            完。
            全局_内存BAR计数 为 全局_内存BAR计数 加 1。
            若 是预取：
                全局_预取BAR计数 为 全局_预取BAR计数 加 1。
            完。
        完。
        做 BAR_检测资源冲突 用 dev、i。
        有效BAR数 为 有效BAR数 加 1。
        i 为 i 加 1。
    完。
    dev 的 BAR数量 为 有效BAR数。
    全局_BAR解析总数 为 全局_BAR解析总数 加 有效BAR数。
    解析耗时 为 做 系统_读取时间戳 减 解析开始时间。
    若 解析耗时 大于 全局_资源分配最大延迟：
        全局_资源分配最大延迟 为 解析耗时。
    完。
完。

BAR_解析IO空间 接受 dev、索引、原值、大小值：
    基地址 为 原值 位与 0xFFFC。
    掩码值 为 大小值 位与 0xFFFC。
    空间大小 为 按位取反 掩码值 加 1 位与 0xFFFF。
    若 基地址 加 空间大小 大于 0x10000：
        空间大小 为 0x10000 减 基地址。
        全局_BAR解析错误计数 为 全局_BAR解析错误计数 加 1。
    完。
    dev 的 BAR 的 第 索引 项 的 基地址 为 基地址。
    dev 的 BAR 的 第 索引 项 的 大小 为 空间大小。
    dev 的 BAR 的 第 索引 项 的 类型 为 PCI_BAR_类型_IO。
    dev 的 BAR 的 第 索引 项 的 是64位 为 假。
    dev 的 BAR 的 第 索引 项 的 是预取 为 假。
    dev 的 BAR 的 第 索引 项 的 已映射 为 假。
    dev 的 BAR 的 第 索引 项 的 虚拟地址 为 0。
    全局_IO空间总字节 为 全局_IO空间总字节 加 空间大小。
完。

BAR_解析32位内存 接受 dev、索引、原值、大小值、是预取：
    基地址 为 原值 位与 0xFFFFFFF0。
    掩码值 为 大小值 位与 0xFFFFFFF0。
    空间大小 为 按位取反 掩码值 加 1 位与 0xFFFFFFFF。
    若 空间大小 不等于 0 并且 空间大小 位与 空间大小 减 1 不等于 0：
        空间大小 为 做 BAR_向上对齐2幂 用 空间大小。
        全局_BAR解析错误计数 为 全局_BAR解析错误计数 加 1。
    完。
    若 空间大小 大于 0 并且 基地址 位与 空间大小 减 1 不等于 0：
        全局_BAR解析错误计数 为 全局_BAR解析错误计数 加 1。
    完。
    dev 的 BAR 的 第 索引 项 的 基地址 为 基地址。
    dev 的 BAR 的 第 索引 项 的 大小 为 空间大小。
    dev 的 BAR 的 第 索引 项 的 类型 为 PCI_BAR_类型_内存。
    dev 的 BAR 的 第 索引 项 的 是64位 为 假。
    dev 的 BAR 的 第 索引 项 的 是预取 为 是预取。
    dev 的 BAR 的 第 索引 项 的 已映射 为 假。
    dev 的 BAR 的 第 索引 项 的 虚拟地址 为 0。
    全局_MMIO映射总字节 为 全局_MMIO映射总字节 加 空间大小。
完。

BAR_解析64位内存 接受 dev、索引、偏移、原值、大小值低、是预取：
    高32 为 做 PCI设备_读32 用 dev、偏移 加 4。
    基地址 为 原值 位与 0xFFFFFFF0 位或 高32 左移 32。
    原命令 为 做 PCI设备_读16 用 dev、PCI_REG_命令。
    做 PCI设备_写16 用 dev、PCI_REG_命令、原命令 位与 按位取反 PCI_CMD_内存使能。
    做 PCI设备_写32 用 dev、偏移 加 4、0xFFFFFFFF。
    高大小 为 做 PCI设备_读32 用 dev、偏移 加 4。
    做 PCI设备_写32 用 dev、偏移 加 4、高32。
    做 PCI设备_写16 用 dev、PCI_REG_命令、原命令。
    完整大小掩码 为 大小值低 位与 0xFFFFFFF0 位或 高大小 左移 32。
    空间大小 为 按位取反 完整大小掩码 加 1。
    若 空间大小 大于 0x4000000000：
        全局_BAR解析错误计数 为 全局_BAR解析错误计数 加 1。
    完。
    dev 的 BAR 的 第 索引 项 的 基地址 为 基地址。
    dev 的 BAR 的 第 索引 项 的 大小 为 空间大小。
    dev 的 BAR 的 第 索引 项 的 类型 为 PCI_BAR_类型_内存。
    dev 的 BAR 的 第 索引 项 的 是64位 为 真。
    dev 的 BAR 的 第 索引 项 的 是预取 为 是预取。
    dev 的 BAR 的 第 索引 项 的 已映射 为 假。
    dev 的 BAR 的 第 索引 项 的 虚拟地址 为 0。
    dev 的 BAR 的 第 索引 项 的 高32位原值 为 高32。
    若 索引 加 1 小于 6：
        dev 的 BAR 的 第 索引 加 1 项 的 类型 为 PCI_BAR_类型_内存。
        dev 的 BAR 的 第 索引 加 1 项 的 基地址 为 0。
        dev 的 BAR 的 第 索引 加 1 项 的 大小 为 0。
        dev 的 BAR 的 第 索引 加 1 项 的 是64位上半 为 真。
    完。
    全局_MMIO映射总字节 为 全局_MMIO映射总字节 加 空间大小。
完。

BAR_解析低端内存 接受 dev、索引、原值、大小值、是预取：
    基地址 为 原值 位与 0xFFFFFFF0。
    空间大小 为 按位取反 (大小值 位与 0xFFFFFFF0) 加 1 位与 0x000FFFFF。
    若 基地址 大于或等于 0x100000：
        全局_BAR解析错误计数 为 全局_BAR解析错误计数 加 1。
    完。
    dev 的 BAR 的 第 索引 项 的 基地址 为 基地址。
    dev 的 BAR 的 第 索引 项 的 大小 为 空间大小。
    dev 的 BAR 的 第 索引 项 的 类型 为 PCI_BAR_类型_内存。
    dev 的 BAR 的 第 索引 项 的 是64位 为 假。
    dev 的 BAR 的 第 索引 项 的 是预取 为 是预取。
    dev 的 BAR 的 第 索引 项 的 是低端 为 真。
    dev 的 BAR 的 第 索引 项 的 已映射 为 假。
    dev 的 BAR 的 第 索引 项 的 虚拟地址 为 0。
    全局_MMIO映射总字节 为 全局_MMIO映射总字节 加 空间大小。
完。

BAR_MMIO映射 接受 dev、BAR索引：
    bar 为 dev 的 BAR 的 第 BAR索引 项。
    若 bar 的 已映射：
        返还 bar 的 虚拟地址。
    完。
    若 bar 的 类型 等于 PCI_BAR_类型_IO：
        返还 错误_无效参数。
    完。
    若 bar 的 大小 等于 0：
        返还 错误_无效参数。
    完。
    缓存标志 为 0。
    若 bar 的 是预取：
        缓存标志 为 页面_写合并。
    否则：
        缓存标志 为 页面_不缓存。
    完。
    虚拟地址 为 做 内核_ioremap 用 bar 的 基地址、bar 的 大小、缓存标志。
    若 虚拟地址 等于 0：
        全局_BAR解析错误计数 为 全局_BAR解析错误计数 加 1。
        返还 错误_内存不足。
    完。
    dev 的 BAR 的 第 BAR索引 项 的 虚拟地址 为 虚拟地址。
    dev 的 BAR 的 第 BAR索引 项 的 已映射 为 真。
    返还 虚拟地址。
完。

BAR_MMIO取消映射 接受 dev、BAR索引：
    bar 为 dev 的 BAR 的 第 BAR索引 项。
    若 非 bar 的 已映射：
        返还。
    完。
    做 内核_iounmap 用 bar 的 虚拟地址、bar 的 大小。
    dev 的 BAR 的 第 BAR索引 项 的 虚拟地址 为 0。
    dev 的 BAR 的 第 BAR索引 项 的 已映射 为 假。
完。

BAR_检测资源冲突 接受 dev、BAR索引：
    bar 为 dev 的 BAR 的 第 BAR索引 项。
    若 bar 的 大小 等于 0：
        返还。
    完。
    起始 为 bar 的 基地址。
    结束 为 起始 加 bar 的 大小 减 1。
    桶索引 为 起始 右移 20 位与 BAR冲突表容量 减 1。
    可变 条目 为 BAR冲突哈希表 的 第 桶索引 项。
    只要 条目 不等于 空：
        若 起始 小于或等于 条目 的 结束 并且 结束 大于或等于 条目 的 起始：
            若 条目 的 设备BDF 不等于 dev 的 BDF：
                全局_BAR冲突计数 为 全局_BAR冲突计数 加 1。
                dev 的 BAR 的 第 BAR索引 项 的 有冲突 为 真。
                dev 的 BAR 的 第 BAR索引 项 的 冲突设备 为 条目 的 设备BDF。
            完。
        完。
        条目 为 条目 的 下一个。
    完。
    新条目 为 一本字典：起始 对应 起始、结束 对应 结束、设备BDF 对应 dev 的 BDF、BAR索引 对应 BAR索引、类型 对应 bar 的 类型、下一个 对应 BAR冲突哈希表 的 第 桶索引 项。
    BAR冲突哈希表 的 第 桶索引 项 为 新条目。
完。

BAR_向上对齐2幂 接受 值：
    可变 v 为 值 减 1。
    v 为 v 位或 v 右移 1。
    v 为 v 位或 v 右移 2。
    v 为 v 位或 v 右移 4。
    v 为 v 位或 v 右移 8。
    v 为 v 位或 v 右移 16。
    v 为 v 位或 v 右移 32。
    返还 v 加 1。
完。

PCI_扫描能力 接受 dev：
    状态 为 做 PCI设备_读16 用 dev、PCI_REG_状态。
    若 状态 位与 PCI_STS_能力列表 等于 0：
        dev 的 能力数量 为 0。
        返还。
    完。
    可变 偏移 为 做 PCI设备_读8 用 dev、PCI_REG_能力指针 位与 0xFC。
    可变 安全计数 为 48。
    可变 能力索引 为 0。
    只要 偏移 不等于 0 并且 偏移 大于或等于 0x40 并且 安全计数 大于 0：
        能力头 为 做 PCI设备_读16 用 dev、偏移。
        能力ID 为 能力头 位与 0xFF。
        下一个 为 能力头 右移 8 位与 0xFC。
        若 能力索引 小于 32：
            dev 的 能力列表 的 第 能力索引 项 的 ID 为 能力ID。
            dev 的 能力列表 的 第 能力索引 项 的 偏移 为 偏移。
        完。
        做 匹配 用 能力ID。
        能力索引 为 能力索引 加 1。
        偏移 为 下一个。
        安全计数 为 安全计数 减 1。
    完。
    dev 的 能力数量 为 能力索引。
    全局_能力扫描总数 为 全局_能力扫描总数 加 能力索引。
完。

PCI_解析MSI能力 接受 dev、偏移：
    控制 为 做 PCI设备_读16 用 dev、偏移 加 2。
    dev 的 MSI_64位 为 控制 位与 MSI_CTRL_64位地址 不等于 0。
    dev 的 MSI_支持掩码 为 控制 位与 MSI_CTRL_每向量掩码 不等于 0。
    dev 的 MSI_最大向量 为 1 左移 ((控制 位与 MSI_CTRL_多消息能力) 右移 1)。
    若 dev 的 MSI_最大向量 大于 32：
        dev 的 MSI_最大向量 为 32。
    完。
完。

PCI_配置MSI 接受 dev、目标地址、数据值、向量数：
    若 非 dev 的 支持MSI：
        返还 错误_不支持。
    完。
    偏移 为 dev 的 MSI_偏移。
    控制 为 做 PCI设备_读16 用 dev、偏移 加 2。
    做 PCI设备_写16 用 dev、偏移 加 2、控制 位与 按位取反 MSI_CTRL_使能。
    可变 实际向量 为 1。
    可变 移位 为 0。
    只要 实际向量 小于 向量数 并且 实际向量 小于 dev 的 MSI_最大向量：
        实际向量 为 实际向量 左移 1。
        移位 为 移位 加 1。
    完。
    做 PCI设备_写32 用 dev、偏移 加 4、目标地址 位与 0xFFFFFFFF。
    可变 数据偏移 为 偏移 加 8。
    若 dev 的 MSI_64位：
        做 PCI设备_写32 用 dev、偏移 加 8、目标地址 右移 32 位与 0xFFFFFFFF。
        数据偏移 为 偏移 加 12。
    完。
    做 PCI设备_写16 用 dev、数据偏移、数据值 位与 0xFFFF。
    新控制 为 控制 位与 按位取反 MSI_CTRL_多消息使能 位或 移位 左移 4 位或 MSI_CTRL_使能。
    做 PCI设备_写16 用 dev、偏移 加 2、新控制。
    dev 的 MSI_已使能 为 真。
    dev 的 MSI_当前向量数 为 实际向量。
    全局_MSI配置计数 为 全局_MSI配置计数 加 1。
    返还 实际向量。
完。

PCI_解析MSIX能力 接受 dev、偏移：
    控制 为 做 PCI设备_读16 用 dev、偏移 加 2。
    dev 的 MSIX_表大小 为 (控制 位与 MSIX_CTRL_表大小掩码) 加 1。
    表偏移值 为 做 PCI设备_读32 用 dev、偏移 加 4。
    dev 的 MSIX_表BAR 为 表偏移值 位与 0x07。
    dev 的 MSIX_表偏移 为 表偏移值 位与 0xFFFFFFF8。
    PBA偏移值 为 做 PCI设备_读32 用 dev、偏移 加 8。
    dev 的 MSIX_PBA_BAR 为 PBA偏移值 位与 0x07。
    dev 的 MSIX_PBA偏移 为 PBA偏移值 位与 0xFFFFFFF8。
完。

PCI_配置MSIX 接受 dev、向量表、向量数：
    若 非 dev 的 支持MSIX：
        返还 错误_不支持。
    完。
    偏移 为 dev 的 MSIX_偏移。
    控制 为 做 PCI设备_读16 用 dev、偏移 加 2。
    做 PCI设备_写16 用 dev、偏移 加 2、控制 位或 MSIX_CTRL_函数掩码。
    表虚拟地址 为 做 BAR_MMIO映射 用 dev、dev 的 MSIX_表BAR。
    若 表虚拟地址 小于 0：
        返还 错误_内存不足。
    完。
    表基 为 表虚拟地址 加 dev 的 MSIX_表偏移。
    可变 配置数 为 向量数。
    若 配置数 大于 dev 的 MSIX_表大小：
        配置数 为 dev 的 MSIX_表大小。
    完。
    可变 j 为 0。
    只要 j 小于 配置数：
        条目基 为 表基 加 j 乘 16。
        做 内存_写32 用 条目基 加 0、向量表 的 第 j 项 的 地址低32。
        做 内存_写32 用 条目基 加 4、向量表 的 第 j 项 的 地址高32。
        做 内存_写32 用 条目基 加 8、向量表 的 第 j 项 的 数据。
        做 内存_写32 用 条目基 加 12、0。
        j 为 j 加 1。
    完。
    只要 j 小于 dev 的 MSIX_表大小：
        做 内存_写32 用 表基 加 j 乘 16 加 12、1。
        j 为 j 加 1。
    完。
    做 PCI设备_写16 用 dev、偏移 加 2、(控制 位或 MSIX_CTRL_使能) 位与 按位取反 MSIX_CTRL_函数掩码。
    dev 的 MSIX_已使能 为 真。
    dev 的 MSIX_当前向量数 为 配置数。
    全局_MSIX配置计数 为 全局_MSIX配置计数 加 1。
    返还 配置数。
完。

PCI_解析电源管理能力 接受 dev、偏移：
    PMC 为 做 PCI设备_读16 用 dev、偏移 加 2。
    dev 的 PM_版本 为 PMC 位与 0x07。
    dev 的 PM_支持D1 为 PMC 位与 0x0200 不等于 0。
    dev 的 PM_支持D2 为 PMC 位与 0x0400 不等于 0。
    dev 的 PM_支持PME 为 PMC 右移 11 位与 0x1F。
    PMCSR 为 做 PCI设备_读16 用 dev、偏移 加 4。
    dev 的 PM_当前状态 为 PMCSR 位与 PCI_PM_掩码。
完。

PCI_设置电源状态 接受 dev、目标状态：
    若 非 dev 的 支持电源管理：
        返还 错误_不支持。
    完。
    若 目标状态 大于 PCI_PM_状态_D3热：
        返还 错误_无效参数。
    完。
    若 目标状态 等于 PCI_PM_状态_D1 并且 非 dev 的 PM_支持D1：
        返还 错误_不支持。
    完。
    若 目标状态 等于 PCI_PM_状态_D2 并且 非 dev 的 PM_支持D2：
        返还 错误_不支持。
    完。
    偏移 为 dev 的 PM_偏移。
    当前PMCSR 为 做 PCI设备_读16 用 dev、偏移 加 4。
    当前状态 为 当前PMCSR 位与 PCI_PM_掩码。
    若 当前状态 等于 目标状态：
        返还 0。
    完。
    新PMCSR 为 当前PMCSR 位与 按位取反 PCI_PM_掩码 位或 目标状态。
    做 PCI设备_写16 用 dev、偏移 加 4、新PMCSR。
    若 当前状态 等于 PCI_PM_状态_D3热 并且 目标状态 等于 PCI_PM_状态_D0：
        做 系统_延迟微秒 用 10000。
        做 PCI_恢复配置空间 用 dev。
    否则若 当前状态 大于或等于 PCI_PM_状态_D2：
        做 系统_延迟微秒 用 200。
    完。
    dev 的 PM_当前状态 为 目标状态。
    全局_电源转换计数 为 全局_电源转换计数 加 1。
    返还 0。
完。

PCI_恢复配置空间 接受 dev：
    可变 i 为 0。
    只要 i 小于 dev 的 BAR数量：
        偏移 为 PCI_REG_BAR0 加 i 乘 4。
        做 PCI设备_写32 用 dev、偏移、dev 的 BAR 的 第 i 项 的 基地址。
        若 dev 的 BAR 的 第 i 项 的 是64位 并且 i 加 1 小于 6：
            做 PCI设备_写32 用 dev、偏移 加 4、dev 的 BAR 的 第 i 项 的 高32位原值。
            i 为 i 加 1。
        完。
        i 为 i 加 1。
    完。
    若 dev 的 已使能：
        做 PCI_使能设备 用 dev。
    完。
完。

PCI_解析PCIe能力 接受 dev、偏移：
    PCIe标志 为 做 PCI设备_读16 用 dev、偏移 加 2。
    dev 的 PCIe_版本 为 PCIe标志 位与 0x0F。
    dev 的 PCIe_设备类型 为 PCIe标志 右移 4 位与 0x0F。
    dev 的 PCIe_有槽 为 PCIe标志 位与 0x0100 不等于 0。
    设备能力 为 做 PCI设备_读32 用 dev、偏移 加 4。
    dev 的 PCIe_最大负载 为 128 左移 (设备能力 位与 0x07)。
    dev 的 PCIe_支持扩展标签 为 设备能力 位与 0x0020 不等于 0。
    dev 的 PCIe_支持FLR 为 设备能力 位与 0x10000000 不等于 0。
    链路能力 为 做 PCI设备_读32 用 dev、偏移 加 12。
    dev 的 PCIe_最大链路速度 为 链路能力 位与 0x0F。
    dev 的 PCIe_最大链路宽度 为 链路能力 右移 4 位与 0x3F。
    链路状态 为 做 PCI设备_读16 用 dev、偏移 加 18。
    dev 的 PCIe_当前速度 为 链路状态 位与 0x0F。
    dev 的 PCIe_当前宽度 为 链路状态 右移 4 位与 0x3F。
完。

PCI_扫描扩展能力 接受 dev：
    若 非 dev 的 是PCIe设备：
        返还。
    完。
    可变 偏移 为 0x100。
    可变 安全计数 为 96。
    可变 扩展能力数 为 0。
    只要 偏移 大于或等于 0x100 并且 偏移 小于 0x1000 并且 安全计数 大于 0：
        头 为 做 PCI设备_读32 用 dev、偏移。
        若 头 等于 0 或者 头 等于 0xFFFFFFFF：
            跳出。
        完。
        能力ID 为 头 位与 0xFFFF。
        版本 为 头 右移 16 位与 0x0F。
        下一个 为 头 右移 20 位与 0xFFC。
        做 匹配 用 能力ID。
        扩展能力数 为 扩展能力数 加 1。
        若 下一个 等于 0：
            跳出。
        完。
        偏移 为 下一个。
        安全计数 为 安全计数 减 1。
    完。
    dev 的 扩展能力数量 为 扩展能力数。
    全局_扩展能力扫描计数 为 全局_扩展能力扫描计数 加 扩展能力数。
完。

PCI_解析AER 接受 dev、偏移：
    dev 的 AER_不可纠正状态 为 做 PCI设备_读32 用 dev、偏移 加 4。
    dev 的 AER_不可纠正掩码 为 做 PCI设备_读32 用 dev、偏移 加 8。
    dev 的 AER_可纠正状态 为 做 PCI设备_读32 用 dev、偏移 加 16。
    dev 的 AER_可纠正掩码 为 做 PCI设备_读32 用 dev、偏移 加 20。
完。

PCI_解析SRIOV 接受 dev、偏移：
    控制 为 做 PCI设备_读16 用 dev、偏移 加 8。
    dev 的 SRIOV_总VF数 为 做 PCI设备_读16 用 dev、偏移 加 14。
    dev 的 SRIOV_当前VF数 为 做 PCI设备_读16 用 dev、偏移 加 16。
    dev 的 SRIOV_VF偏移 为 做 PCI设备_读16 用 dev、偏移 加 20。
    dev 的 SRIOV_VF步长 为 做 PCI设备_读16 用 dev、偏移 加 22。
完。

PCI_使能设备 接受 dev：
    cmd 为 做 PCI设备_读16 用 dev、PCI_REG_命令。
    新cmd 为 cmd 位或 PCI_CMD_IO使能 位或 PCI_CMD_内存使能。
    新cmd 为 新cmd 位与 按位取反 PCI_CMD_SERR使能。
    做 PCI设备_写16 用 dev、PCI_REG_命令、新cmd。
    验证 为 做 PCI设备_读16 用 dev、PCI_REG_命令。
    若 验证 位与 (PCI_CMD_IO使能 位或 PCI_CMD_内存使能) 不等于 PCI_CMD_IO使能 位或 PCI_CMD_内存使能：
        全局_BAR解析错误计数 为 全局_BAR解析错误计数 加 1。
        返还 错误_设备故障。
    完。
    dev 的 已使能 为 真。
    全局_设备使能计数 为 全局_设备使能计数 加 1。
    返还 0。
完。

PCI_禁用设备 接受 dev：
    若 dev 的 MSI_已使能：
        做 PCI_禁用MSI 用 dev。
    完。
    若 dev 的 MSIX_已使能：
        做 PCI_禁用MSIX 用 dev。
    完。
    cmd 为 做 PCI设备_读16 用 dev、PCI_REG_命令。
    cmd 为 cmd 位与 按位取反 (PCI_CMD_IO使能 位或 PCI_CMD_内存使能 位或 PCI_CMD_总线主控)。
    做 PCI设备_写16 用 dev、PCI_REG_命令、cmd。
    可变 i 为 0。
    只要 i 小于 6：
        若 dev 的 BAR 的 第 i 项 的 已映射：
            做 BAR_MMIO取消映射 用 dev、i。
        完。
        i 为 i 加 1。
    完。
    dev 的 已使能 为 假。
    全局_设备禁用计数 为 全局_设备禁用计数 加 1。
完。

PCI_使能总线主控 接受 dev：
    cmd 为 做 PCI设备_读16 用 dev、PCI_REG_命令。
    若 cmd 位与 PCI_CMD_总线主控：
        返还。
    完。
    做 PCI设备_写16 用 dev、PCI_REG_命令、cmd 位或 PCI_CMD_总线主控。
    延迟 为 做 PCI设备_读8 用 dev、PCI_REG_延迟定时器。
    若 延迟 小于 64：
        做 PCI设备_写8 用 dev、PCI_REG_延迟定时器、64。
    完。
完。

PCI_禁用MSI 接受 dev：
    若 非 dev 的 支持MSI：
        返还。
    完。
    控制 为 做 PCI设备_读16 用 dev、dev 的 MSI_偏移 加 2。
    做 PCI设备_写16 用 dev、dev 的 MSI_偏移 加 2、控制 位与 按位取反 MSI_CTRL_使能。
    dev 的 MSI_已使能 为 假。
完。

PCI_禁用MSIX 接受 dev：
    若 非 dev 的 支持MSIX：
        返还。
    完。
    控制 为 做 PCI设备_读16 用 dev、dev 的 MSIX_偏移 加 2。
    做 PCI设备_写16 用 dev、dev 的 MSIX_偏移 加 2、控制 位与 按位取反 MSIX_CTRL_使能 位或 MSIX_CTRL_函数掩码。
    dev 的 MSIX_已使能 为 假。
完。

BAR_AI分析设备 接受 dev：
    张量 为 做 Tensor_创建 用 10。
    做 Tensor_写 用 张量、0、dev 的 BAR数量 除以 6.0。
    可变 MMIO总量 为 0。
    可变 IO总量 为 0。
    可变 BAR64比 为 0.0。
    可变 预取比 为 0.0。
    可变 k 为 0。
    只要 k 小于 dev 的 BAR数量：
        若 dev 的 BAR 的 第 k 项 的 类型 等于 PCI_BAR_类型_内存：
            MMIO总量 为 MMIO总量 加 dev 的 BAR 的 第 k 项 的 大小。
            若 dev 的 BAR 的 第 k 项 的 是64位：
                BAR64比 为 BAR64比 加 1.0。
            完。
            若 dev 的 BAR 的 第 k 项 的 是预取：
                预取比 为 预取比 加 1.0。
            完。
        否则：
            IO总量 为 IO总量 加 dev 的 BAR 的 第 k 项 的 大小。
        完。
        k 为 k 加 1。
    完。
    若 dev 的 BAR数量 大于 0：
        BAR64比 为 BAR64比 除以 dev 的 BAR数量。
        预取比 为 预取比 除以 dev 的 BAR数量。
    完。
    做 Tensor_写 用 张量、1、做 BRAT_对数归一 用 MMIO总量、0x100000000。
    做 Tensor_写 用 张量、2、做 BRAT_对数归一 用 IO总量、0x10000。
    做 Tensor_写 用 张量、3、BAR64比。
    做 Tensor_写 用 张量、4、预取比。
    若 dev 的 是PCIe设备：
        做 Tensor_写 用 张量、5、dev 的 PCIe_当前速度 除以 5.0。
        做 Tensor_写 用 张量、6、dev 的 PCIe_当前宽度 除以 16.0。
    否则：
        做 Tensor_写 用 张量、5、0.0。
        做 Tensor_写 用 张量、6、0.0。
    完。
    做 Tensor_写 用 张量、7、dev 的 能力数量 除以 32.0。
    若 dev 的 支持SRIOV：
        做 Tensor_写 用 张量、8、dev 的 SRIOV_总VF数 除以 256.0。
    否则：
        做 Tensor_写 用 张量、8、0.0。
    完。
    做 Tensor_写 用 张量、9、dev 的 类代码 除以 255.0。
    设备分类 为 做 AI总线_推理 用 全局AI总线、「BRAT_设备分类」、张量。
    冲突风险 为 做 AI总线_风险评分 用 全局AI总线、「BRAT_冲突预测」、张量。
    资源效率 为 做 AI总线_推理 用 全局AI总线、「BRAT_资源效率」、张量。
    dev 的 AI_设备分类 为 设备分类。
    dev 的 AI_冲突风险 为 冲突风险。
    dev 的 AI_资源效率 为 资源效率。
    若 冲突风险 大于 0.7：
        异常 为 做 AI总线_异常检测 用 全局AI总线、「BRAT_资源冲突」、张量。
        dev 的 AI_需要重新分配 为 异常 大于 0.5。
    完。
    全局_AI识别计数 为 全局_AI识别计数 加 1。
    做 Tensor_释放 用 张量。
完。

BRAT_对数归一 接受 值、最大值：
    若 值 小于或等于 0：
        返还 0.0。
    完。
    若 值 大于或等于 最大值：
        返还 1.0。
    完。
    可变 近似对数值 为 0.0。
    可变 当前 为 值 加 1。
    只要 当前 大于 2：
        当前 为 当前 除以 2。
        近似对数值 为 近似对数值 加 1.0。
    完。
    可变 近似对数最大 为 0.0。
    当前 为 最大值 加 1。
    只要 当前 大于 2：
        当前 为 当前 除以 2。
        近似对数最大 为 近似对数最大 加 1.0。
    完。
    若 近似对数最大 等于 0.0：
        返还 0.0。
    完。
    返还 近似对数值 除以 近似对数最大。
完。

BRAT_计算资源分配分数 接受 dev：
    可变 总分 为 0.0。
    可变 i 为 0。
    只要 i 小于 dev 的 BAR数量：
        bar 为 dev 的 BAR 的 第 i 项。
        可变 类型权重 为 1.0。
        若 bar 的 类型 等于 PCI_BAR_类型_内存：
            类型权重 为 2.0。
        完。
        若 bar 的 是预取：
            类型权重 为 类型权重 乘 1.5。
        完。
        若 bar 的 是64位：
            类型权重 为 类型权重 乘 1.2。
        完。
        可变 对齐因子 为 1.0。
        若 bar 的 大小 大于 0 并且 bar 的 基地址 位与 bar 的 大小 减 1 不等于 0：
            对齐因子 为 0.5。
        完。
        总分 为 总分 加 bar 的 大小 乘 类型权重 乘 对齐因子。
        i 为 i 加 1。
    完。
    可变 冲突度 为 1.0。
    若 dev 的 AI_冲突风险 大于 0：
        冲突度 为 1.0 加 dev 的 AI_冲突风险 乘 2.0。
    完。
    返还 总分 除以 冲突度。
完。

BAR_获取统计信息 接受 零参：
    返还 一本字典：BAR解析总数 对应 全局_BAR解析总数、IO_BAR计数 对应 全局_IO_BAR计数、内存BAR计数 对应 全局_内存BAR计数、六十四位BAR计数 对应 全局_64位BAR计数、预取BAR计数 对应 全局_预取BAR计数、能力扫描总数 对应 全局_能力扫描总数、MSI配置计数 对应 全局_MSI配置计数、MSIX配置计数 对应 全局_MSIX配置计数、电源转换计数 对应 全局_电源转换计数、PCIe设备计数 对应 全局_PCIe设备计数、MMIO映射总字节 对应 全局_MMIO映射总字节、IO空间总字节 对应 全局_IO空间总字节、BAR冲突计数 对应 全局_BAR冲突计数、设备使能计数 对应 全局_设备使能计数、设备禁用计数 对应 全局_设备禁用计数、AI识别计数 对应 全局_AI识别计数、扩展能力扫描 对应 全局_扩展能力扫描计数、SRIOV检测数 对应 全局_SRIOV检测计数、错误计数 对应 全局_BAR解析错误计数、最大解析延迟 对应 全局_资源分配最大延迟。
完。

BAR_诊断设备 接受 dev：
    可变 问题列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 dev 的 BAR数量：
        若 dev 的 BAR 的 第 i 项 的 有冲突：
            问题列表 为 问题列表 加 一组：「BAR」 加 i 加 「与设备」 加 dev 的 BAR 的 第 i 项 的 冲突设备 加 「地址冲突」。
        完。
        若 dev 的 BAR 的 第 i 项 的 大小 等于 0 并且 dev 的 BAR 的 第 i 项 的 基地址 不等于 0：
            问题列表 为 问题列表 加 一组：「BAR」 加 i 加 「大小探测异常」。
        完。
        i 为 i 加 1。
    完。
    若 dev 的 是PCIe设备：
        若 dev 的 PCIe_当前速度 小于 dev 的 PCIe_最大链路速度：
            问题列表 为 问题列表 加 一组：「PCIe链路速度降级: 当前Gen」 加 dev 的 PCIe_当前速度 加 「 最大Gen」 加 dev 的 PCIe_最大链路速度。
        完。
        若 dev 的 PCIe_当前宽度 小于 dev 的 PCIe_最大链路宽度：
            问题列表 为 问题列表 加 一组：「PCIe链路宽度降级: 当前x」 加 dev 的 PCIe_当前宽度 加 「 最大x」 加 dev 的 PCIe_最大链路宽度。
        完。
    完。
    若 dev 的 支持AER 并且 dev 的 AER_不可纠正状态 不等于 0：
        问题列表 为 问题列表 加 一组：「存在不可纠正AER错误: 0x」 加 dev 的 AER_不可纠正状态。
    完。
    若 dev 的 AI_冲突风险 大于 0.5：
        问题列表 为 问题列表 加 一组：「AI检测高资源冲突风险: 」 加 dev 的 AI_冲突风险。
    完。
    返还 一本字典：设备BDF 对应 dev 的 BDF、BAR数量 对应 dev 的 BAR数量、能力数量 对应 dev 的 能力数量、是PCIe 对应 dev 的 是PCIe设备、电源状态 对应 dev 的 PM_当前状态、问题数量 对应 问题列表 的 长度、问题列表 对应 问题列表、资源分配分数 对应 做 BRAT_计算资源分配分数 用 dev、AI分类 对应 dev 的 AI_设备分类。
完。

PCI_完整设备初始化 接受 dev：
    做 PCI_解析BAR 用 dev。
    做 PCI_扫描能力 用 dev。
    做 PCI_扫描扩展能力 用 dev。
    做 BAR_AI分析设备 用 dev。
    做 PCI_使能设备 用 dev。
    做 PCI_使能总线主控 用 dev。
    若 dev 的 支持电源管理 并且 dev 的 PM_当前状态 不等于 PCI_PM_状态_D0：
        做 PCI_设置电源状态 用 dev、PCI_PM_状态_D0。
    完。
    可变 首个内存BAR 为 负 1。
    可变 m 为 0。
    只要 m 小于 dev 的 BAR数量：
        若 dev 的 BAR 的 第 m 项 的 类型 等于 PCI_BAR_类型_内存 并且 dev 的 BAR 的 第 m 项 的 大小 大于 0：
            首个内存BAR 为 m。
            跳出。
        完。
        m 为 m 加 1。
    完。
    若 首个内存BAR 大于或等于 0：
        做 BAR_MMIO映射 用 dev、首个内存BAR。
    完。
    返还 0。
完。

BAR_重置统计信息 接受 零参：
    全局_BAR解析总数 为 0。
    全局_IO_BAR计数 为 0。
    全局_内存BAR计数 为 0。
    全局_64位BAR计数 为 0。
    全局_预取BAR计数 为 0。
    全局_能力扫描总数 为 0。
    全局_MSI配置计数 为 0。
    全局_MSIX配置计数 为 0。
    全局_电源转换计数 为 0。
    全局_PCIe设备计数 为 0。
    全局_MMIO映射总字节 为 0。
    全局_IO空间总字节 为 0。
    全局_BAR冲突计数 为 0。
    全局_设备使能计数 为 0。
    全局_设备禁用计数 为 0。
    全局_AI识别计数 为 0。
    全局_扩展能力扫描计数 为 0。
    全局_SRIOV检测计数 为 0。
    全局_BAR解析错误计数 为 0。
    全局_资源分配最大延迟 为 0。
完。

本章 收束。
