《Nova音频流控制》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《.·软件混音器》。

引用《..·中枢·字符串替换》。

引用《.·E1000收发初始化》。

引用《..·引导·端口IO》。

引用《.·NVMe驱动辅助》。

引用《.·中断处理》。

引用《..·中枢·整数转字符串》。

BDL条目 是 一种 事物，含 地址低、地址高、长度、IOC。

可变 声驱_当前流索引 为 0。

可变 声驱_BDL缓冲列表 为 一组：（空）。

声驱_编码流格式 接受 采样率、位深、声道数：
    可变 fmt 为 0。
    若 采样率 等于 48000：
        fmt 为 0。
    否则若 采样率 等于 96000：
        fmt 为 1 左移 11。
    否则若 采样率 等于 192000：
        fmt 为 3 左移 11。
    否则若 采样率 等于 44100：
        fmt 为 1 左移 14。
    否则若 采样率 等于 88200：
        fmt 为 1 左移 14 位或 1 左移 11。
    否则若 采样率 等于 16000：
        fmt 为 2 左移 8。
    否则若 采样率 等于 8000：
        fmt 为 5 左移 8。
    否则：
        fmt 为 0。
    完。
    若 位深 等于 16：
        fmt 为 fmt 位或 1 左移 4。
    否则若 位深 等于 20：
        fmt 为 fmt 位或 2 左移 4。
    否则若 位深 等于 24：
        fmt 为 fmt 位或 3 左移 4。
    否则若 位深 等于 32：
        fmt 为 fmt 位或 4 左移 4。
    否则：
        fmt 为 fmt 位或 1 左移 4。
    完。
    fmt 为 fmt 位或 声道数 减 1 位与 0xF。
    返还 fmt。
完。

声驱_启动流 接受 流：
    若 做 计数 用 声卡列表 等于 0：
        返还。
    完。
    card 为 声卡列表 的 第 0 项。
    base 为 card 的 MMIO基址。
    若 base 等于 0：
        返还。
    完。
    gcap 为 做 声驱_读16 用 base、声驱_GCAP。
    输入流数 为 gcap 右移 8 位与 0xF。
    流索引 为 输入流数。
    若 流 的 方向 等于 SND_PCM_STREAM_CAPTURE：
        流索引 为 0。
    完。
    sd_base 为 base 加 0x80 加 流索引 乘 0x20。
    ctl 为 做 声驱_读8 用 base、sd_base 减 base 加 声驱_SD_CTL。
    做 声驱_写8 用 base、sd_base 减 base 加 声驱_SD_CTL、ctl 位与 按位取反 0x01。
    可变 等待停 为 0。
    只要 等待停 小于 100：
        若 做 声驱_读8 用 base、sd_base 减 base 加 声驱_SD_CTL 位与 0x01 等于 0：
            跳出。
        完。
        做 延迟_us 用 10。
        等待停 为 等待停 加 1。
    完。
    做 声驱_写8 用 base、sd_base 减 base 加 声驱_SD_CTL、0x01。
    做 延迟_us 用 100。
    做 声驱_写8 用 base、sd_base 减 base 加 声驱_SD_CTL、0x00。
    做 延迟_us 用 100。
    bdl_addr 为 做 直传_分配 用 4096。
    做 内存填零 用 bdl_addr、4096。
    做 追加 用 声驱_BDL缓冲列表、bdl_addr。
    周期字节 为 流 的 周期帧数 乘 流 的 帧大小。
    周期数 为 流 的 缓冲帧数 除以 流 的 周期帧数。
    可变 bi 为 0。
    只要 bi 小于 周期数 并且 bi 小于 256：
        entry 为 bdl_addr 加 bi 乘 16。
        buf_offset 为 bi 乘 周期字节。
        做 内存写32 用 entry、流 的 缓冲区 加 buf_offset 位与 0xFFFFFFFF。
        做 内存写32 用 entry 加 4、流 的 缓冲区 加 buf_offset 右移 32 位与 0xFFFFFFFF。
        做 内存写32 用 entry 加 8、周期字节。
        做 内存写32 用 entry 加 12、1。
        bi 为 bi 加 1。
    完。
    做 声驱_写32 用 base、sd_base 减 base 加 声驱_SD_BDLPL、bdl_addr 位与 0xFFFFFFFF。
    做 声驱_写32 用 base、sd_base 减 base 加 声驱_SD_BDLPU、bdl_addr 右移 32 位与 0xFFFFFFFF。
    总字节 为 流 的 缓冲帧数 乘 流 的 帧大小。
    做 声驱_写32 用 base、sd_base 减 base 加 声驱_SD_CBL、总字节。
    做 声驱_写16 用 base、sd_base 减 base 加 声驱_SD_LVI、周期数 减 1。
    位深 为 16。
    若 流 的 格式 等于 SND_PCM_FORMAT_S24_LE 或者 流 的 格式 等于 SND_PCM_FORMAT_S32_LE：
        位深 为 32。
    完。
    若 流 的 格式 等于 SND_PCM_FORMAT_S24_3LE：
        位深 为 24。
    完。
    fmt 为 做 声驱_编码流格式 用 流 的 采样率、位深、流 的 声道数。
    做 声驱_写16 用 base、sd_base 减 base 加 声驱_SD_FORMAT、fmt。
    stream_id 为 流索引 加 1。
    若 做 计数 用 card 的 PCM列表 大于 0：
        做 声驱_发送命令 用 card、0、2、声驱_VERB_SET_STREAM 位或 stream_id 左移 4。
        做 声驱_发送命令 用 card、0、2、声驱_VERB_SET_FORMAT 位或 fmt。
    完。
    new_ctl 为 0x02 位或 0x04 位或 stream_id 左移 20。
    做 声驱_写8 用 base、sd_base 减 base 加 声驱_SD_CTL 加 2、stream_id 左移 4 位与 0xFF。
    做 声驱_写8 用 base、sd_base 减 base 加 声驱_SD_CTL、0x02 位或 0x01。
    声驱_当前流索引 为 流索引。
    做 内核信息 用 「声驱: 启动流 索引=」 加 做 整数转字符串 用 流索引 加 「 方向=」 加 若 流 的 方向 等于 0 则 「播放」 否则 「录音」 加 「 格式=」 加 做 整数转十六进制 用 fmt 加 「 缓冲=」 加 做 整数转字符串 用 总字节 加 「字节」 加 「 周期=」 加 做 整数转字符串 用 周期数。
完。

声驱_停止流 接受 流：
    若 做 计数 用 声卡列表 等于 0：
        返还。
    完。
    card 为 声卡列表 的 第 0 项。
    base 为 card 的 MMIO基址。
    若 base 等于 0：
        返还。
    完。
    gcap 为 做 声驱_读16 用 base、声驱_GCAP。
    输入流数 为 gcap 右移 8 位与 0xF。
    流索引 为 输入流数。
    若 流 的 方向 等于 SND_PCM_STREAM_CAPTURE：
        流索引 为 0。
    完。
    sd_base 为 base 加 0x80 加 流索引 乘 0x20。
    ctl 为 做 声驱_读8 用 base、sd_base 减 base 加 声驱_SD_CTL。
    做 声驱_写8 用 base、sd_base 减 base 加 声驱_SD_CTL、ctl 位与 按位取反 0x03。
    可变 等待 为 0。
    只要 等待 小于 300：
        若 做 声驱_读8 用 base、sd_base 减 base 加 声驱_SD_CTL 位与 0x01 等于 0：
            跳出。
        完。
        做 延迟_us 用 10。
        等待 为 等待 加 1。
    完。
    做 内核信息 用 「声驱: 停止流 索引=」 加 做 整数转字符串 用 流索引。
完。

声驱_读8 接受 base、offset：
    返还 做 内存读8 用 base 加 offset。
完。

可变 AI_直传流控制_调用数 为 0。

可变 AI_直传流控制_异常数 为 0。

AI_直传流控制_监控 接受 零参：
    AI_直传流控制_调用数 为 AI_直传流控制_调用数 加 1。
    返还 AI_直传流控制_异常数 等于 0。
完。

AI_直传流控制_健康检查 接受 零参：
    若 AI_直传流控制_调用数 大于 0 并且 AI_直传流控制_异常数 乘 100 除以 AI_直传流控制_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
