《窗口管理》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·中断表注册》。

引用《..·中枢·内核打印》。

引用《.·图层合成Alpha混合》。

引用《.·事件分发》。

引用《..·中枢·整数转字符串》。

窗口状态_正常 为 0。

窗口状态_最大化 为 1。

窗口状态_最小化 为 2。

窗口状态_全屏 为 3。

窗口状态_贴靠左 为 4。

窗口状态_贴靠右 为 5。

窗口状态_贴靠上 为 6。

窗口状态_贴靠下 为 7。

装饰_标题栏高 为 30。

装饰_边框宽 为 2。

装饰_按钮宽 为 28。

装饰_圆角半径 为 6。

装饰_阴影大小 为 4。

窗口管理统计 是 一种 事物，含 创建数、销毁数、移动数、调整数、最大化数、最小化数、还原数、贴靠数、焦点切换数、分组操作数。

可变 全局_窗口统计 为 空。

窗口统计_初始化 接受 零参：
    若 全局_窗口统计 等于 空：
        全局_窗口统计 为 新建 窗口管理统计。
        全局_窗口统计 的 创建数 为 0。
        全局_窗口统计 的 销毁数 为 0。
        全局_窗口统计 的 移动数 为 0。
        全局_窗口统计 的 调整数 为 0。
        全局_窗口统计 的 最大化数 为 0。
        全局_窗口统计 的 最小化数 为 0。
        全局_窗口统计 的 还原数 为 0。
        全局_窗口统计 的 贴靠数 为 0。
        全局_窗口统计 的 焦点切换数 为 0。
        全局_窗口统计 的 分组操作数 为 0。
    完。
完。

窗口_创建 接受 进程ID、x、y、宽、高、图层：
    做 窗口统计_初始化。
    若 做 计数 用 所有窗口 大于或等于 最大窗口数：
        返还 负 1。
    完。
    win 为 新建 窗口。
    win 的 窗口ID 为 下一个窗口ID。
    下一个窗口ID 为 下一个窗口ID 加 1。
    win 的 进程ID 为 进程ID。
    win 的 x 为 x。
    win 的 y 为 y。
    win 的 宽 为 宽。
    win 的 高 为 高。
    win 的 最小宽 为 100。
    win 的 最小高 为 60。
    win 的 最大宽 为 屏幕宽。
    win 的 最大高 为 屏幕高。
    win 的 状态 为 窗口状态_正常。
    win 的 图层 为 图层。
    win 的 Z序 为 做 计数 用 所有窗口。
    win 的 透明度 为 255。
    win 的 可见 为 真。
    win 的 有焦点 为 假。
    win 的 缓冲大小 为 宽 乘 高 乘 (屏幕位深 除以 8)。
    win 的 缓冲地址 为 做 分配窗口缓冲 用 win 的 缓冲大小。
    win 的 脏 为 真。
    win 的 装饰_启用 为 真。
    win 的 标题 为 「」。
    win 的 分组ID 为 0。
    win 的 还原x 为 x。
    win 的 还原y 为 y。
    win 的 还原宽 为 宽。
    win 的 还原高 为 高。
    做 追加 用 所有窗口、win。
    做 窗口_设焦点 用 win 的 窗口ID。
    做 合成器_标记脏 用 x、y、宽、高。
    全局_窗口统计 的 创建数 为 全局_窗口统计 的 创建数 加 1。
    返还 win 的 窗口ID。
完。

窗口_创建带标题 接受 进程ID、x、y、宽、高、图层、标题：
    wid 为 做 窗口_创建 用 进程ID、x、y、宽、高、图层。
    若 wid 大于或等于 0：
        win 为 做 窗口_查找 用 wid。
        若 win 不等于 空：
            win 的 标题 为 标题。
        完。
    完。
    返还 wid。
完。

窗口_销毁 接受 窗口ID：
    做 窗口统计_初始化。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有窗口：
        若 所有窗口 的 第 i 项 的 窗口ID 等于 窗口ID：
            做 合成器_标记脏 用 所有窗口 的 第 i 项 的 x、所有窗口 的 第 i 项 的 y、所有窗口 的 第 i 项 的 宽、所有窗口 的 第 i 项 的 高。
            做 释放窗口缓冲 用 所有窗口 的 第 i 项 的 缓冲地址。
            做 删除 用 所有窗口、i。
            若 焦点窗口ID 等于 窗口ID 并且 做 计数 用 所有窗口 大于 0：
                做 窗口_设焦点 用 所有窗口 的 第 做 计数 用 所有窗口 减 1 项 的 窗口ID。
            完。
            全局_窗口统计 的 销毁数 为 全局_窗口统计 的 销毁数 加 1。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

窗口_移动 接受 窗口ID、新x、新y：
    做 窗口统计_初始化。
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    若 新x 小于 负 win 的 宽 加 20：
        新x 为 负 win 的 宽 加 20。
    完。
    若 新y 小于 0：
        新y 为 0。
    完。
    若 新x 大于 屏幕宽 减 20：
        新x 为 屏幕宽 减 20。
    完。
    若 新y 大于 屏幕高 减 20：
        新y 为 屏幕高 减 20。
    完。
    做 合成器_标记脏 用 win 的 x、win 的 y、win 的 宽、win 的 高。
    win 的 x 为 新x。
    win 的 y 为 新y。
    win 的 脏 为 真。
    做 合成器_标记脏 用 新x、新y、win 的 宽、win 的 高。
    全局_窗口统计 的 移动数 为 全局_窗口统计 的 移动数 加 1。
完。

窗口_调整大小 接受 窗口ID、新宽、新高：
    做 窗口统计_初始化。
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    若 新宽 小于 win 的 最小宽：
        新宽 为 win 的 最小宽。
    完。
    若 新高 小于 win 的 最小高：
        新高 为 win 的 最小高。
    完。
    若 新宽 大于 win 的 最大宽：
        新宽 为 win 的 最大宽。
    完。
    若 新高 大于 win 的 最大高：
        新高 为 win 的 最大高。
    完。
    做 合成器_标记脏 用 win 的 x、win 的 y、win 的 宽、win 的 高。
    做 释放窗口缓冲 用 win 的 缓冲地址。
    win 的 宽 为 新宽。
    win 的 高 为 新高。
    win 的 缓冲大小 为 新宽 乘 新高 乘 (屏幕位深 除以 8)。
    win 的 缓冲地址 为 做 分配窗口缓冲 用 win 的 缓冲大小。
    win 的 脏 为 真。
    做 合成器_标记脏 用 win 的 x、win 的 y、新宽、新高。
    全局_窗口统计 的 调整数 为 全局_窗口统计 的 调整数 加 1。
完。

窗口_设置最小大小 接受 窗口ID、最小宽、最小高：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    win 的 最小宽 为 最小宽。
    win 的 最小高 为 最小高。
完。

窗口_设置最大大小 接受 窗口ID、最大宽、最大高：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    win 的 最大宽 为 最大宽。
    win 的 最大高 为 最大高。
完。

窗口_保存还原位置 接受 win：
    若 win 的 状态 等于 窗口状态_正常：
        win 的 还原x 为 win 的 x。
        win 的 还原y 为 win 的 y。
        win 的 还原宽 为 win 的 宽。
        win 的 还原高 为 win 的 高。
    完。
完。

窗口_最大化 接受 窗口ID：
    做 窗口统计_初始化。
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    做 窗口_保存还原位置 用 win。
    win 的 状态 为 窗口状态_最大化。
    做 窗口_移动 用 窗口ID、0、0。
    做 窗口_调整大小 用 窗口ID、屏幕宽、屏幕高。
    全局_窗口统计 的 最大化数 为 全局_窗口统计 的 最大化数 加 1。
完。

窗口_最小化 接受 窗口ID：
    做 窗口统计_初始化。
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    做 窗口_保存还原位置 用 win。
    win 的 状态 为 窗口状态_最小化。
    win 的 可见 为 假。
    做 合成器_标记脏 用 win 的 x、win 的 y、win 的 宽、win 的 高。
    全局_窗口统计 的 最小化数 为 全局_窗口统计 的 最小化数 加 1。
    若 焦点窗口ID 等于 窗口ID：
        做 窗口_焦点下一个。
    完。
完。

窗口_还原 接受 窗口ID：
    做 窗口统计_初始化。
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    做 合成器_标记脏 用 win 的 x、win 的 y、win 的 宽、win 的 高。
    win 的 状态 为 窗口状态_正常。
    win 的 可见 为 真。
    做 窗口_移动 用 窗口ID、win 的 还原x、win 的 还原y。
    做 窗口_调整大小 用 窗口ID、win 的 还原宽、win 的 还原高。
    全局_窗口统计 的 还原数 为 全局_窗口统计 的 还原数 加 1。
完。

窗口_全屏 接受 窗口ID：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    做 窗口_保存还原位置 用 win。
    win 的 状态 为 窗口状态_全屏。
    win 的 装饰_启用 为 假。
    做 窗口_移动 用 窗口ID、0、0。
    做 窗口_调整大小 用 窗口ID、屏幕宽、屏幕高。
完。

窗口_切换最大化 接受 窗口ID：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    若 win 的 状态 等于 窗口状态_最大化 或者 win 的 状态 等于 窗口状态_全屏：
        做 窗口_还原 用 窗口ID。
    否则：
        做 窗口_最大化 用 窗口ID。
    完。
完。

窗口_贴靠左 接受 窗口ID：
    做 窗口统计_初始化。
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    做 窗口_保存还原位置 用 win。
    win 的 状态 为 窗口状态_贴靠左。
    做 窗口_移动 用 窗口ID、0、0。
    做 窗口_调整大小 用 窗口ID、屏幕宽 除以 2、屏幕高。
    全局_窗口统计 的 贴靠数 为 全局_窗口统计 的 贴靠数 加 1。
完。

窗口_贴靠右 接受 窗口ID：
    做 窗口统计_初始化。
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    做 窗口_保存还原位置 用 win。
    win 的 状态 为 窗口状态_贴靠右。
    做 窗口_移动 用 窗口ID、屏幕宽 除以 2、0。
    做 窗口_调整大小 用 窗口ID、屏幕宽 除以 2、屏幕高。
    全局_窗口统计 的 贴靠数 为 全局_窗口统计 的 贴靠数 加 1。
完。

窗口_贴靠上半 接受 窗口ID：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    做 窗口_保存还原位置 用 win。
    win 的 状态 为 窗口状态_贴靠上。
    做 窗口_移动 用 窗口ID、0、0。
    做 窗口_调整大小 用 窗口ID、屏幕宽、屏幕高 除以 2。
完。

窗口_贴靠下半 接受 窗口ID：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    做 窗口_保存还原位置 用 win。
    win 的 状态 为 窗口状态_贴靠下。
    做 窗口_移动 用 窗口ID、0、屏幕高 除以 2。
    做 窗口_调整大小 用 窗口ID、屏幕宽、屏幕高 除以 2。
完。

窗口_平铺全部 接受 零参：
    可变 可见窗口 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有窗口：
        若 所有窗口 的 第 i 项 的 可见：
            做 追加 用 可见窗口、所有窗口 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    数量 为 做 计数 用 可见窗口。
    若 数量 等于 0：
        返还。
    完。
    列数 为 1。
    行数 为 1。
    若 数量 大于 1：
        列数 为 2。
    完。
    若 数量 大于 2：
        行数 为 2。
    完。
    若 数量 大于 4：
        列数 为 3。
    完。
    格宽 为 屏幕宽 除以 列数。
    格高 为 屏幕高 除以 行数。
    i 为 0。
    只要 i 小于 数量：
        列 为 i 取余于 列数。
        行 为 i 除以 列数。
        做 窗口_移动 用 可见窗口 的 第 i 项 的 窗口ID、列 乘 格宽、行 乘 格高。
        做 窗口_调整大小 用 可见窗口 的 第 i 项 的 窗口ID、格宽、格高。
        i 为 i 加 1。
    完。
完。

窗口_设焦点 接受 窗口ID：
    做 窗口统计_初始化。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有窗口：
        所有窗口 的 第 i 项 的 有焦点 为 所有窗口 的 第 i 项 的 窗口ID 等于 窗口ID。
        i 为 i 加 1。
    完。
    焦点窗口ID 为 窗口ID。
    全局_窗口统计 的 焦点切换数 为 全局_窗口统计 的 焦点切换数 加 1。
    做 窗口_提升到顶 用 窗口ID。
完。

窗口_焦点下一个 接受 零参：
    可变 最高Z 为 负 1。
    可变 最佳ID 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有窗口：
        若 所有窗口 的 第 i 项 的 可见 并且 所有窗口 的 第 i 项 的 窗口ID 不等于 焦点窗口ID：
            若 所有窗口 的 第 i 项 的 Z序 大于 最高Z：
                最高Z 为 所有窗口 的 第 i 项 的 Z序。
                最佳ID 为 所有窗口 的 第 i 项 的 窗口ID。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最佳ID 大于或等于 0：
        做 窗口_设焦点 用 最佳ID。
    完。
完。

窗口_提升到顶 接受 窗口ID：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    可变 最大Z 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有窗口：
        若 所有窗口 的 第 i 项 的 Z序 大于 最大Z：
            最大Z 为 所有窗口 的 第 i 项 的 Z序。
        完。
        i 为 i 加 1。
    完。
    win 的 Z序 为 最大Z 加 1。
完。

窗口_设分组 接受 窗口ID、分组ID：
    做 窗口统计_初始化。
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    win 的 分组ID 为 分组ID。
    全局_窗口统计 的 分组操作数 为 全局_窗口统计 的 分组操作数 加 1。
完。

窗口_最小化分组 接受 分组ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有窗口：
        若 所有窗口 的 第 i 项 的 分组ID 等于 分组ID：
            做 窗口_最小化 用 所有窗口 的 第 i 项 的 窗口ID。
        完。
        i 为 i 加 1。
    完。
完。

窗口_还原分组 接受 分组ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有窗口：
        若 所有窗口 的 第 i 项 的 分组ID 等于 分组ID 并且 所有窗口 的 第 i 项 的 状态 等于 窗口状态_最小化：
            做 窗口_还原 用 所有窗口 的 第 i 项 的 窗口ID。
        完。
        i 为 i 加 1。
    完。
完。

窗口_设透明度 接受 窗口ID、透明度：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    若 透明度 小于 0：
        透明度 为 0。
    完。
    若 透明度 大于 255：
        透明度 为 255。
    完。
    win 的 透明度 为 透明度。
    win 的 脏 为 真。
    做 合成器_标记脏 用 win 的 x、win 的 y、win 的 宽、win 的 高。
完。

窗口_显示 接受 窗口ID：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    win 的 可见 为 真。
    win 的 脏 为 真。
    做 合成器_标记脏 用 win 的 x、win 的 y、win 的 宽、win 的 高。
完。

窗口_隐藏 接受 窗口ID：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    win 的 可见 为 假。
    做 合成器_标记脏 用 win 的 x、win 的 y、win 的 宽、win 的 高。
完。

窗口_设标题 接受 窗口ID、标题：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    win 的 标题 为 标题。
    win 的 脏 为 真。
完。

窗口_查找 接受 窗口ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有窗口：
        若 所有窗口 的 第 i 项 的 窗口ID 等于 窗口ID：
            返还 所有窗口 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

窗口_查找按进程 接受 进程ID：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有窗口：
        若 所有窗口 的 第 i 项 的 进程ID 等于 进程ID：
            做 追加 用 结果、所有窗口 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

窗口_获取焦点窗口 接受 零参：
    返还 做 窗口_查找 用 焦点窗口ID。
完。

窗口_总数 接受 零参：
    返还 做 计数 用 所有窗口。
完。

窗口_可见数 接受 零参：
    可变 计 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有窗口：
        若 所有窗口 的 第 i 项 的 可见：
            计 为 计 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计。
完。

窗口_点击测试 接受 x、y：
    可变 最佳win 为 空。
    可变 最高Z 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有窗口：
        win 为 所有窗口 的 第 i 项。
        若 win 的 可见 并且 x 大于或等于 win 的 x 并且 x 小于 win 的 x 加 win 的 宽 并且 y 大于或等于 win 的 y 并且 y 小于 win 的 y 加 win 的 高：
            若 win 的 Z序 大于 最高Z：
                最高Z 为 win 的 Z序。
                最佳win 为 win。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 最佳win。
完。

窗口管理_打印统计 接受 零参：
    做 窗口统计_初始化。
    做 打印行 用 「=== 窗口管理统计 ===」。
    做 打印行 用 「创建数: 」 加 做 整数转字符串 用 全局_窗口统计 的 创建数。
    做 打印行 用 「销毁数: 」 加 做 整数转字符串 用 全局_窗口统计 的 销毁数。
    做 打印行 用 「移动数: 」 加 做 整数转字符串 用 全局_窗口统计 的 移动数。
    做 打印行 用 「调整数: 」 加 做 整数转字符串 用 全局_窗口统计 的 调整数。
    做 打印行 用 「最大化: 」 加 做 整数转字符串 用 全局_窗口统计 的 最大化数。
    做 打印行 用 「最小化: 」 加 做 整数转字符串 用 全局_窗口统计 的 最小化数。
    做 打印行 用 「还原数: 」 加 做 整数转字符串 用 全局_窗口统计 的 还原数。
    做 打印行 用 「贴靠数: 」 加 做 整数转字符串 用 全局_窗口统计 的 贴靠数。
    做 打印行 用 「焦点切换: 」 加 做 整数转字符串 用 全局_窗口统计 的 焦点切换数。
    做 打印行 用 「分组操作: 」 加 做 整数转字符串 用 全局_窗口统计 的 分组操作数。
    做 打印行 用 「当前窗口: 」 加 做 整数转字符串 用 做 计数 用 所有窗口。
    做 打印行 用 「可见窗口: 」 加 做 整数转字符串 用 做 窗口_可见数。
完。

可变 AI_窗口管理_负载窗口 为 一组：（空）。

AI_窗口管理_负载预测 接受 当前负载：
    做 追加 用 AI_窗口管理_负载窗口、当前负载。
    若 做 计数 用 AI_窗口管理_负载窗口 大于 20：
        做 列表_移除 用 AI_窗口管理_负载窗口、0。
    完。
    若 做 计数 用 AI_窗口管理_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_窗口管理_负载窗口。
    趋势 为 AI_窗口管理_负载窗口 的 第 n 减 1 项 减 AI_窗口管理_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_窗口管理_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
