《USB控制传输》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

最大端口 为 16。

最大设备 为 127。

最大端点 为 32。

传输_控制 为 0。

传输_批量 为 1。

传输_中断 为 2。

传输_等时 为 3。

速度_低 为 0。

速度_全 为 1。

速度_高 为 2。

速度_超 为 3。

通串设备 是 一种 事物，含 地址、速度、端口号、厂商标识、产品标识、设备类、配置数、当前配置、端点数、已枚举。

通串端点 是 一种 事物，含 设备地址、端点号、方向、传输类型、最大包大小、间隔、活跃、传输字节、错误数。

可变 控制器基址 为 0。

可变 设备表 为 一组：（空）。

可变 设备数 为 0。

可变 端点表 为 一组：（空）。

可变 端点数 为 0。

可变 下一地址 为 1。

可变 控制传输数 为 0。

可变 批量传输数 为 0。

可变 中断传输数 为 0。

可变 总传输字节 为 0。

可变 错误总数 为 0。

通串_初始化 接受 基址：
    控制器基址 为 基址。
    设备表 为 一组：（空）。
    设备数 为 0。
    端点表 为 一组：（空）。
    端点数 为 0。
    下一地址 为 1。
    做 通串_写寄存器 用 0x00、做 通串_读寄存器 用 0x00 位或 2。
    可变 等待 为 0。
    只要 等待 小于 100000 并且 做 通串_读寄存器 用 0x00 位与 2 不等于 0：
        等待 为 等待 加 1。
    完。
    做 通串_写寄存器 用 0x38、做 通串_读寄存器 用 0x38 位或 1。
完。

通串_读寄存器 接受 偏移：
    返还 做 内存_读32 用 控制器基址 加 偏移。
完。

通串_写寄存器 接受 偏移、值：
    做 内存_写32 用 控制器基址 加 偏移、值。
完。

通串_枚举端口 接受 端口号：
    若 端口号 大于或等于 最大端口：
        返还 负 1。
    完。
    端口偏移 为 0x400 加 端口号 乘 0x10。
    端口状态 为 做 通串_读寄存器 用 端口偏移。
    若 端口状态 位与 1 等于 0：
        返还 负 1。
    完。
    做 通串_写寄存器 用 端口偏移、端口状态 位或 1 左移 4。
    做 通串_延迟 用 50。
    做 通串_写寄存器 用 端口偏移、端口状态 位与 按位取反 (1 左移 4)。
    做 通串_延迟 用 100。
    若 设备数 大于或等于 最大设备：
        返还 错误_无资源。
    完。
    设备 为 新建 通串设备。
    设备 的 地址 为 下一地址。
    下一地址 为 下一地址 加 1。
    设备 的 速度 为 端口状态 右移 10 位与 3。
    设备 的 端口号 为 端口号。
    设备 的 厂商标识 为 0。
    设备 的 产品标识 为 0。
    设备 的 设备类 为 0。
    设备 的 配置数 为 0。
    设备 的 当前配置 为 0。
    设备 的 端点数 为 0。
    设备 的 已枚举 为 假。
    做 追加 用 设备表、设备。
    索引 为 设备数。
    设备数 为 设备数 加 1。
    做 通串_获取设备描述 用 索引。
    设备 的 已枚举 为 真。
    返还 索引。
完。

通串_控制传输 接受 设备索引、请求类型、请求、值、索引、长度、数据：
    若 设备索引 大于或等于 设备数：
        返还 错误_无效参数。
    完。
    控制传输数 为 控制传输数 加 1。
    设置包 为 做 堆_分配 用 8。
    做 内存_写8 用 设置包、请求类型。
    做 内存_写8 用 设置包 加 1、请求。
    做 内存_写16 用 设置包 加 2、值。
    做 内存_写16 用 设置包 加 4、索引。
    做 内存_写16 用 设置包 加 6、长度。
    结果 为 做 通串_提交传输 用 设备表 的 第 设备索引 项 的 地址、0、传输_控制、设置包、8、数据、长度。
    做 堆_释放 用 设置包。
    若 结果 小于 0：
        错误总数 为 错误总数 加 1。
    否则：
        总传输字节 为 总传输字节 加 长度。
    完。
    返还 结果。
完。

通串_批量传输 接受 设备索引、端点号、方向、数据、长度：
    若 设备索引 大于或等于 设备数：
        返还 错误_无效参数。
    完。
    批量传输数 为 批量传输数 加 1。
    结果 为 做 通串_提交传输 用 设备表 的 第 设备索引 项 的 地址、端点号、传输_批量、0、0、数据、长度。
    若 结果 大于或等于 0：
        总传输字节 为 总传输字节 加 长度。
    否则：
        错误总数 为 错误总数 加 1。
    完。
    返还 结果。
完。

通串_中断传输 接受 设备索引、端点号、数据、长度：
    若 设备索引 大于或等于 设备数：
        返还 错误_无效参数。
    完。
    中断传输数 为 中断传输数 加 1。
    返还 做 通串_提交传输 用 设备表 的 第 设备索引 项 的 地址、端点号、传输_中断、0、0、数据、长度。
完。

通串_提交传输 接受 地址、端点、类型、设置、设置长、数据、数据长：
    返还 数据长。
完。

通串_获取设备描述 接受 设备索引：
    缓冲 为 做 堆_分配 用 18。
    结果 为 做 通串_控制传输 用 设备索引、0x80、6、0x0100、0、18、缓冲。
    若 结果 大于或等于 0：
        设备表 的 第 设备索引 项 的 厂商标识 为 做 内存_读16 用 缓冲 加 8。
        设备表 的 第 设备索引 项 的 产品标识 为 做 内存_读16 用 缓冲 加 10。
        设备表 的 第 设备索引 项 的 设备类 为 做 内存_读8 用 缓冲 加 4。
        设备表 的 第 设备索引 项 的 配置数 为 做 内存_读8 用 缓冲 加 17。
    完。
    做 堆_释放 用 缓冲。
完。

通串_延迟 接受 毫秒：
    可变 计 为 0。
    只要 计 小于 毫秒 乘 1000：
        计 为 计 加 1。
    完。
完。

通串_获取设备数 接受 零参：
    返还 设备数。
完。

通串_获取设备信息 接受 索引：
    若 索引 大于或等于 设备数：
        返还 一组：0、0、0、0。
    完。
    返还 一组：设备表 的 第 索引 项 的 地址、设备表 的 第 索引 项 的 厂商标识、设备表 的 第 索引 项 的 产品标识、设备表 的 第 索引 项 的 设备类。
完。

通串_完整统计 接受 零参：
    返还 一组：设备数、控制传输数、批量传输数、中断传输数、总传输字节、错误总数。
完。

可变 AI_通串控制传_风暴阈值 为 10000。

可变 AI_通串控制传_预测IRQ 为 0。

AI_通串控制传_风暴检测 接受 触发频率：
    若 触发频率 大于 AI_通串控制传_风暴阈值：
        AI_通串控制传_风暴阈值 为 AI_通串控制传_风暴阈值 乘 3 除以 2。
        返还 真。
    完。
    返还 假。
完。

AI_通串控制传_自适应合并 接受 当前延迟：
    若 当前延迟 小于 100：
        返还 16。
    完。
    若 当前延迟 小于 500：
        返还 8。
    完。
    返还 1。
完。

本章 收束。
