《AMD图形驱动》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·中枢·块内存分配器》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

mmGNova受限能量网络_STATUS 为 0x8010。

mmGNova受限能量网络_STATUS2 为 0x8014。

mmGNova受限能量网络_SOFT_RESET 为 0x8020。

mmGNova受限能量网络_GFX_INDEX 为 0x802C。

GNova受限能量网络_STATUS_GUI_ACTIVE 为 1 左移 31。

GNova受限能量网络_STATUS_CP_BUSY 为 1 左移 29。

GNova受限能量网络_STATUS_GFX_BUSY 为 1 左移 26。

GNova受限能量网络_STATUS_TA_BUSY 为 1 左移 14。

GNova受限能量网络_STATUS_SX_BUSY 为 1 左移 20。

GNova受限能量网络_STATUS_DB_BUSY 为 1 左移 26。

mmCP_RB_BASE 为 0x8040。

mmCP_RB_CNTL 为 0x8044。

mmCP_RB_RPTR 为 0x8700。

mmCP_RB_WPTR 为 0x8704。

mmCP_RB_RPTR_ADDR 为 0x8060。

mmCP_ME_CNTL 为 0x86D4。

mmCP_PFP_UCODE_ADDR 为 0xC150。

mmCP_PFP_UCODE_DATA 为 0xC154。

mmCP_ME_UCODE_ADDR 为 0xC158。

mmCP_ME_UCODE_DATA 为 0xC15C。

mmCP_CE_UCODE_ADDR 为 0xC168。

mmCP_CE_UCODE_DATA 为 0xC16C。

mmCP_COMPUTE_RB_BASE 为 0x8E04。

mmCP_COMPUTE_RB_CNTL 为 0x8E08。

mmCP_COMPUTE_RB_RPTR 为 0x8E14。

mmCP_COMPUTE_RB_WPTR 为 0x8E18。

mmCP_MEC_UCODE_ADDR 为 0xC170。

mmCP_MEC_UCODE_DATA 为 0xC174。

mmS直传0_RB_BASE 为 0xD000。

mmS直传0_RB_CNTL 为 0xD004。

mmS直传0_RB_RPTR 为 0xD008。

mmS直传0_RB_WPTR 为 0xD00C。

mmS直传0_CNTL 为 0xD010。

mmS直传1_RB_BASE 为 0xD800。

mmS直传1_RB_CNTL 为 0xD804。

mmS直传1_RB_RPTR 为 0xD808。

mmS直传1_RB_WPTR 为 0xD80C。

mmIH_RB_BASE 为 0xE000。

mmIH_RB_CNTL 为 0xE004。

mmIH_RB_RPTR 为 0xE008。

mmIH_RB_WPTR 为 0xE00C。

mmIH_CNTL 为 0xE010。

mmDCN_HUBP0_SURFACE_ADDR 为 0x3A00。

mmDCN_HUBP0_SIZE 为 0x3A04。

mmDCN_OTG0_CONTROL 为 0x3C00。

mmDCN_OTG0_H_TOTAL 为 0x3C04。

mmDCN_OTG0_V_TOTAL 为 0x3C08。

mmDCN_OTG0_H_ACTIVE 为 0x3C0C。

mmDCN_OTG0_V_ACTIVE 为 0x3C10。

mmDCN_OTG_OFFSET 为 0x80。

mmMC_VM_FB_LOCATION_BASE 为 0x2024。

mmMC_VM_FB_LOCATION_TOP 为 0x2028。

mmMC_VM_AGP_BASE 为 0x2030。

mmMC_VM_AGP_TOP 为 0x2034。

mmMC_VM_SYSTEM_APERTURE_LOW 为 0x2040。

mmMC_VM_SYSTEM_APERTURE_HIGH 为 0x2044。

mmVM_CONTEXT0_CNTL 为 0x2080。

mmVM_L2_CNTL 为 0x2090。

mmSMU_TEMPERATURE 为 0xE308。

mmSMU_FAN_SPEED 为 0xE310。

mmSMU_POWER 为 0xE314。

图处命令_分配 接受 大小：
    cmd 为 做 kmalloc 用 大小 加 16。
    若 cmd 等于 空：
        返还 空。
    完。
    做 内存_写32 用 cmd、大小。
    做 内存_写32 用 cmd 加 4、0。
    返还 cmd。
完。

图处命令_提交 接受 cmd：
    若 cmd 等于 空：
        返还 错误_参数。
    完。
    返还 做 图处驱动_提交命令缓冲 用 cmd、做 内存_读32 用 cmd。
完。

本章 收束。
