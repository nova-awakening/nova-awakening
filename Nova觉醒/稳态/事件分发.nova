《事件分发》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·中断表注册》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·同步原语》。

引用《..·中枢·数学工具》。

引用《.·窗口管理》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·块内存分配器》。

合成器_鼠标点击 接受 x、y、按钮：
    可变 i 为 做 计数 用 所有窗口 减 1。
    只要 i 大于或等于 0：
        win 为 所有窗口 的 第 i 项。
        若 win 的 可见 并且 win 的 状态 不等于 窗口状态_最小化：
            若 x 大于或等于 win 的 x 并且 x 小于 win 的 x 加 win 的 宽 并且 y 大于或等于 win 的 y 并且 y 小于 win 的 y 加 win 的 高：
                做 窗口_设焦点 用 win 的 窗口ID。
                返还 win 的 窗口ID。
            完。
        完。
        i 为 i 减 1。
    完。
    返还 0。
完。

合成器_坐标到窗口 接受 全局x、全局y：
    win 为 做 窗口_查找 用 焦点窗口ID。
    若 win 等于 空：
        返还 一组：全局x、全局y、0。
    完。
    返还 一组：全局x 减 win 的 x、全局y 减 win 的 y、win 的 窗口ID。
完。

智能_智能布局_自动分屏 接受 窗口数：
    若 窗口数 小于或等于 0：
        返还。
    完。
    若 窗口数 等于 1：
        w 为 屏幕宽 乘 80 除以 100。
        h 为 屏幕高 乘 80 除以 100。
        x 为 (屏幕宽 减 w) 除以 2。
        y 为 (屏幕高 减 h) 除以 2。
        若 做 计数 用 所有窗口 大于 0：
            做 窗口_移动 用 所有窗口 的 第 0 项 的 窗口ID、x、y。
            做 窗口_调整大小 用 所有窗口 的 第 0 项 的 窗口ID、w、h。
        完。
    否则若 窗口数 等于 2：
        半宽 为 屏幕宽 除以 2。
        若 做 计数 用 所有窗口 大于或等于 2：
            做 窗口_移动 用 所有窗口 的 第 0 项 的 窗口ID、0、0。
            做 窗口_调整大小 用 所有窗口 的 第 0 项 的 窗口ID、半宽、屏幕高。
            做 窗口_移动 用 所有窗口 的 第 1 项 的 窗口ID、半宽、0。
            做 窗口_调整大小 用 所有窗口 的 第 1 项 的 窗口ID、半宽、屏幕高。
        完。
    否则：
        列数 为 做 整数开方_简 用 窗口数 加 1。
        行数 为 (窗口数 加 列数 减 1) 除以 列数。
        格宽 为 屏幕宽 除以 列数。
        格高 为 屏幕高 除以 行数。
        可变 i 为 0。
        只要 i 小于 窗口数 并且 i 小于 做 计数 用 所有窗口：
            列 为 i 取余于 列数。
            行 为 i 除以 列数。
            做 窗口_移动 用 所有窗口 的 第 i 项 的 窗口ID、列 乘 格宽、行 乘 格高。
            做 窗口_调整大小 用 所有窗口 的 第 i 项 的 窗口ID、格宽、格高。
            i 为 i 加 1。
        完。
    完。
完。

分配窗口缓冲 接受 大小：
    地址 为 做 kmalloc 用 大小、0。
    若 地址 等于 0：
        做 内核警告 用 「窗口缓冲分配失败: 」 加 做 整数转字符串 用 大小 加 「 字节」。
    完。
    返还 地址。
完。

释放窗口缓冲 接受 地址：
    若 地址 等于 0：
        返还。
    完。
    做 kfree 用 地址。
完。

窗口合成器_完整统计 接受 零参：
    返还 一组：做 计数 用 所有窗口、焦点窗口ID、合成_帧数、屏幕宽、屏幕高、屏幕位深。
完。

按键_按下 为 1。

按键_释放 为 2。

按键_重复 为 3。

键盘事件 是 一种 事物，含 类型、键码、扫描码、修饰键、字符、时间戳、目标窗口。

合成器_键盘按下 接受 键码、扫描码、修饰键：
    快捷结果 为 做 检查全局快捷键 用 键码、修饰键。
    若 快捷结果 不等于 0：
        返还。
    完。
    若 焦点窗口ID 等于 0：
        返还。
    完。
    evt 为 新建 键盘事件。
    evt 的 类型 为 按键_按下。
    evt 的 键码 为 键码。
    evt 的 扫描码 为 扫描码。
    evt 的 修饰键 为 修饰键。
    evt 的 字符 为 做 键码_转Unicode 用 键码、修饰键。
    evt 的 时间戳 为 做 读取时钟。
    evt 的 目标窗口 为 焦点窗口ID。
    做 事件队列_入队 用 evt。
    全局WCED统计 的 键盘事件数 为 全局WCED统计 的 键盘事件数 加 1。
完。

合成器_键盘释放 接受 键码、扫描码、修饰键：
    若 焦点窗口ID 等于 0：
        返还。
    完。
    evt 为 新建 键盘事件。
    evt 的 类型 为 按键_释放。
    evt 的 键码 为 键码。
    evt 的 扫描码 为 扫描码。
    evt 的 修饰键 为 修饰键。
    evt 的 时间戳 为 做 读取时钟。
    evt 的 目标窗口 为 焦点窗口ID。
    做 事件队列_入队 用 evt。
完。

快捷键_最大数 为 64。

快捷键绑定 是 一种 事物，含 键码、修饰键、回调、描述、启用。

可变 快捷键表 为 一组：（空）。

可变 快捷键数 为 0。

注册全局快捷键 接受 键码、修饰键、回调、描述：
    若 快捷键数 大于或等于 快捷键_最大数：
        返还 负 1。
    完。
    idx 为 快捷键数。
    快捷键表 的 第 idx 项 的 键码 为 键码。
    快捷键表 的 第 idx 项 的 修饰键 为 修饰键。
    快捷键表 的 第 idx 项 的 回调 为 回调。
    快捷键表 的 第 idx 项 的 描述 为 描述。
    快捷键表 的 第 idx 项 的 启用 为 真。
    快捷键数 为 快捷键数 加 1。
    返还 0。
完。

检查全局快捷键 接受 键码、修饰键：
    可变 i 为 0。
    只要 i 小于 快捷键数：
        若 快捷键表 的 第 i 项 的 启用 并且 快捷键表 的 第 i 项 的 键码 等于 键码 并且 快捷键表 的 第 i 项 的 修饰键 等于 修饰键：
            做 快捷键表 的 第 i 项 的 回调。
            返还 1。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

触摸_最大触点 为 10。

触摸_开始 为 1。

触摸_移动 为 2。

触摸_结束 为 3。

触摸_取消 为 4。

触摸点 是 一种 事物，含 ID、x、y、压力、半径、时间戳。

触摸事件 是 一种 事物，含 类型、触点列表、触点数、目标窗口。

合成器_触摸事件 接受 类型、触点列表、触点数：
    目标窗口 为 0。
    若 触点数 大于 0：
        目标窗口 为 做 合成器_鼠标点击 用 触点列表 的 第 0 项 的 x、触点列表 的 第 0 项 的 y、0。
    完。
    evt 为 新建 触摸事件。
    evt 的 类型 为 类型。
    evt 的 触点数 为 触点数。
    evt 的 目标窗口 为 目标窗口。
    可变 i 为 0。
    只要 i 小于 触点数 并且 i 小于 触摸_最大触点：
        evt 的 触点列表 的 第 i 项 为 触点列表 的 第 i 项。
        i 为 i 加 1。
    完。
    做 事件队列_入队_触摸 用 evt。
    全局WCED统计 的 触摸事件数 为 全局WCED统计 的 触摸事件数 加 1。
    若 类型 等于 触摸_结束：
        做 手势_识别 用 evt。
    完。
完。

手势_点击 为 1。

手势_滑动 为 2。

手势_缩放 为 3。

手势_旋转 为 4。

手势_长按 为 5。

手势_双击 为 6。

滑动阈值 为 20。

长按时间 为 500。

双击间隔 为 300。

手势状态 是 一种 事物，含 上次触摸时间、上次触摸X、上次触摸Y、初始触点距离、初始触点角度。

可变 全局手势 为 新建 手势状态。

手势_识别 接受 evt：
    若 evt 的 触点数 等于 1：
        p 为 evt 的 触点列表 的 第 0 项。
        dx 为 p 的 x 减 全局手势 的 上次触摸X。
        dy 为 p 的 y 减 全局手势 的 上次触摸Y。
        距离平方 为 dx 乘 dx 加 dy 乘 dy。
        时间差 为 (p 的 时间戳 减 全局手势 的 上次触摸时间) 除以 1000000。
        若 距离平方 小于 滑动阈值 乘 滑动阈值：
            若 时间差 小于 双击间隔：
                做 合成器_分发手势 用 手势_双击、evt 的 目标窗口、p 的 x、p 的 y。
            否则若 时间差 大于或等于 长按时间：
                做 合成器_分发手势 用 手势_长按、evt 的 目标窗口、p 的 x、p 的 y。
            否则：
                做 合成器_分发手势 用 手势_点击、evt 的 目标窗口、p 的 x、p 的 y。
            完。
        否则：
            做 合成器_分发手势 用 手势_滑动、evt 的 目标窗口、p 的 x、p 的 y。
        完。
        全局手势 的 上次触摸时间 为 p 的 时间戳。
        全局手势 的 上次触摸X 为 p 的 x。
        全局手势 的 上次触摸Y 为 p 的 y。
    否则若 evt 的 触点数 等于 2：
        p0 为 evt 的 触点列表 的 第 0 项。
        p1 为 evt 的 触点列表 的 第 1 项。
        dx 为 p1 的 x 减 p0 的 x。
        dy 为 p1 的 y 减 p0 的 y。
        当前距离 为 做 整数开方 用 dx 乘 dx 加 dy 乘 dy。
        若 全局手势 的 初始触点距离 大于 0：
            缩放比 为 当前距离 乘 1024 除以 全局手势 的 初始触点距离。
            若 缩放比 小于 900 或者 缩放比 大于 1100：
                做 合成器_分发手势 用 手势_缩放、evt 的 目标窗口、缩放比、0。
            完。
        否则：
            全局手势 的 初始触点距离 为 当前距离。
        完。
    完。
完。

合成器_分发手势 接受 手势类型、窗口ID、x、y：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    若 win 的 手势回调 不等于 空：
        做 win 的 手势回调 用 手势类型、x、y。
    完。
    全局WCED统计 的 手势识别数 为 全局WCED统计 的 手势识别数 加 1。
完。

事件队列_容量 为 512。

输入类型_鼠标 为 0。

输入类型_键盘 为 1。

输入类型_触摸 为 2。

输入类型_窗口 为 3。

输入类型_系统 为 4。

统一输入事件 是 一种 事物，含 类型、时间戳、目标窗口、数据。

事件队列 是 一种 事物，含 缓冲区、头、尾、大小、锁。

可变 全局事件队列 为 空。

事件队列_创建 接受 零参：
    q 为 做 堆_分配 用 事件队列。
    若 q 等于 0：
        返还 空。
    完。
    q 的 头 为 0。
    q 的 尾 为 0。
    q 的 大小 为 0。
    q 的 锁 为 做 自旋锁_创建。
    返还 q。
完。

事件队列_入队 接受 evt：
    若 全局事件队列 等于 空：
        返还 负 1。
    完。
    做 自旋锁_获取 用 全局事件队列 的 锁。
    若 全局事件队列 的 大小 大于或等于 事件队列_容量：
        做 自旋锁_释放 用 全局事件队列 的 锁。
        全局WCED统计 的 事件丢弃数 为 全局WCED统计 的 事件丢弃数 加 1。
        返还 负 1。
    完。
    全局事件队列 的 缓冲区 的 第 全局事件队列 的 尾 项 为 evt。
    全局事件队列 的 尾 为 (全局事件队列 的 尾 加 1) 取余于 事件队列_容量。
    全局事件队列 的 大小 为 全局事件队列 的 大小 加 1。
    做 自旋锁_释放 用 全局事件队列 的 锁。
    返还 0。
完。

事件队列_出队 接受 零参：
    若 全局事件队列 等于 空 或者 全局事件队列 的 大小 等于 0：
        返还 空。
    完。
    做 自旋锁_获取 用 全局事件队列 的 锁。
    evt 为 全局事件队列 的 缓冲区 的 第 全局事件队列 的 头 项。
    全局事件队列 的 头 为 (全局事件队列 的 头 加 1) 取余于 事件队列_容量。
    全局事件队列 的 大小 为 全局事件队列 的 大小 减 1。
    做 自旋锁_释放 用 全局事件队列 的 锁。
    返还 evt。
完。

事件队列_入队_触摸 接受 touch_evt：
    evt 为 新建 统一输入事件。
    evt 的 类型 为 输入类型_触摸。
    evt 的 时间戳 为 做 读取时钟。
    evt 的 目标窗口 为 touch_evt 的 目标窗口。
    evt 的 数据 的 第 0 项 为 touch_evt 的 类型。
    evt 的 数据 的 第 1 项 为 touch_evt 的 触点数。
    返还 做 事件队列_入队 用 evt。
完。

窗口事件_移动 为 1。

窗口事件_调整大小 为 2。

窗口事件_关闭 为 3。

窗口事件_最小化 为 4。

窗口事件_最大化 为 5。

窗口事件_获得焦点 为 6。

窗口事件_失去焦点 为 7。

合成器_窗口调整大小 接受 窗口ID、新宽、新高：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    若 新宽 小于 100：
        新宽 为 100。
    完。
    若 新高 小于 50：
        新高 为 50。
    完。
    若 新宽 大于 屏幕宽 乘 2：
        新宽 为 屏幕宽 乘 2。
    完。
    若 新高 大于 屏幕高 乘 2：
        新高 为 屏幕高 乘 2。
    完。
    旧宽 为 win 的 宽。
    旧高 为 win 的 高。
    win 的 宽 为 新宽。
    win 的 高 为 新高。
    若 win 的 帧缓冲 不等于 0：
        做 释放窗口缓冲 用 win 的 帧缓冲。
    完。
    win 的 帧缓冲 为 做 分配窗口缓冲 用 新宽 乘 新高 乘 (屏幕位深 除以 8)。
    若 win 的 调整大小回调 不等于 空：
        做 win 的 调整大小回调 用 窗口ID、新宽、新高、旧宽、旧高。
    完。
    全局WCED统计 的 窗口事件数 为 全局WCED统计 的 窗口事件数 加 1。
完。

合成器_窗口移动 接受 窗口ID、新x、新y：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    若 新x 加 win 的 宽 小于 50：
        新x 为 50 减 win 的 宽。
    完。
    若 新y 小于 0：
        新y 为 0。
    完。
    若 新x 大于 屏幕宽 减 50：
        新x 为 屏幕宽 减 50。
    完。
    若 新y 大于 屏幕高 减 30：
        新y 为 屏幕高 减 30。
    完。
    win 的 x 为 新x。
    win 的 y 为 新y。
完。

合成器_窗口关闭请求 接受 窗口ID：
    win 为 做 窗口_查找 用 窗口ID。
    若 win 等于 空：
        返还。
    完。
    若 win 的 关闭回调 不等于 空：
        允许关闭 为 做 win 的 关闭回调 用 窗口ID。
        若 允许关闭 等于 假：
            返还。
        完。
    完。
    若 win 的 帧缓冲 不等于 0：
        做 释放窗口缓冲 用 win 的 帧缓冲。
    完。
    若 焦点窗口ID 等于 窗口ID：
        焦点窗口ID 为 0。
        可变 i 为 做 计数 用 所有窗口 减 1。
        只要 i 大于或等于 0：
            若 所有窗口 的 第 i 项 的 窗口ID 不等于 窗口ID 并且 所有窗口 的 第 i 项 的 可见：
                做 窗口_设焦点 用 所有窗口 的 第 i 项 的 窗口ID。
                返还。
            完。
            i 为 i 减 1。
        完。
    完。
完。

AI_事件预测 接受 事件历史、历史数：
    bus 为 全局AI总线。
    若 bus 等于 空 或者 历史数 小于 3：
        返还 负 1。
    完。
    特征 为 做 Tensor_创建 用 5。
    做 Tensor_写 用 特征、0、事件历史 的 第 历史数 减 1 项 的 类型。
    做 Tensor_写 用 特征、1、事件历史 的 第 历史数 减 2 项 的 类型。
    做 Tensor_写 用 特征、2、事件历史 的 第 历史数 减 3 项 的 类型。
    dt1 为 事件历史 的 第 历史数 减 1 项 的 时间戳 减 事件历史 的 第 历史数 减 2 项 的 时间戳。
    dt2 为 事件历史 的 第 历史数 减 2 项 的 时间戳 减 事件历史 的 第 历史数 减 3 项 的 时间戳。
    做 Tensor_写 用 特征、3、dt1 除以 1000000。
    做 Tensor_写 用 特征、4、dt2 除以 1000000。
    结果 为 做 AI总线_推理 用 bus、「事件意图预测」、特征。
    预测类型 为 做 Tensor_读 用 结果、0。
    做 Tensor_释放 用 特征。
    做 Tensor_释放 用 结果。
    返还 预测类型。
完。

WCED统计 是 一种 事物，含 键盘事件数、鼠标事件数、触摸事件数、窗口事件数、手势识别数、事件丢弃数、AI预测次数。

可变 全局WCED统计 为 新建 WCED统计。

WCED_打印诊断 接受 零参：
    做 内核打印 用 「╔═══════════════════════════════════════╗」。
    做 内核打印 用 「║  WCED 窗口合成器事件分发 诊断       ║」。
    做 内核打印 用 「╠═══════════════════════════════════════╣」。
    做 内核打印 用 「║ 键盘事件:     {}」、全局WCED统计 的 键盘事件数。
    做 内核打印 用 「║ 鼠标事件:     {}」、全局WCED统计 的 鼠标事件数。
    做 内核打印 用 「║ 触摸事件:     {}」、全局WCED统计 的 触摸事件数。
    做 内核打印 用 「║ 窗口事件:     {}」、全局WCED统计 的 窗口事件数。
    做 内核打印 用 「║ 手势识别:     {}」、全局WCED统计 的 手势识别数。
    做 内核打印 用 「║ 事件丢弃:     {}」、全局WCED统计 的 事件丢弃数。
    若 全局事件队列 不等于 空：
        做 内核打印 用 「║ 队列待处理:   {}」、全局事件队列 的 大小。
    完。
    做 内核打印 用 「║ 快捷键注册:   {}」、快捷键数。
    做 内核打印 用 「╚═══════════════════════════════════════╝」。
完。

WCED_初始化 接受 零参：
    全局WCED统计 为 新建 WCED统计。
    全局事件队列 为 做 事件队列_创建。
    全局手势 为 新建 手势状态。
    快捷键数 为 0。
    做 内核打印 用 「[WCED] 窗口合成器事件分发系统已初始化 v1.0」。
完。

可变 _谐振事件_高优先次数 为 0。

可变 _谐振事件_低优先次数 为 0。

谐振事件_优先级 接受 事件类型、窗口是否聚焦：
    若 窗口是否聚焦 并且 (事件类型 等于 1 或者 事件类型 等于 2)：
        _谐振事件_高优先次数 为 _谐振事件_高优先次数 加 1。
        返还 0。
    完。
    若 事件类型 等于 3：
        返还 1。
    完。
    _谐振事件_低优先次数 为 _谐振事件_低优先次数 加 1。
    返还 2。
完。

事件分发_完整统计 接受 零参：
    返还 一组：「谐振高优先」、_谐振事件_高优先次数、「谐振低优先」、_谐振事件_低优先次数。
完。

本章 收束。
