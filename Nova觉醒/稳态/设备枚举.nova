《设备枚举》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《.·信息打印》。

引用《.·通串存储》。

引用《.·USB控制传输》。

通串_枚举全部 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 通串控制器列表：
        做 通串_枚举控制器 用 通串控制器列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

通串_枚举控制器 接受 ctl：
    可变 端口 为 0。
    只要 端口 小于 ctl 的 端口数：
        若 做 通串_端口有设备 用 ctl、端口：
            dev 为 做 通串_枚举设备 用 ctl、端口。
            若 dev 不等于 空：
                做 追加 用 通串设备列表、dev。
                做 追加 用 ctl 的 设备列表、dev。
                做 通串_匹配驱动 用 dev。
                做 通串_打印设备 用 dev。
            完。
        完。
        端口 为 端口 加 1。
    完。
完。

通串_枚举设备 接受 ctl、端口：
    做 通串_端口复位 用 ctl、端口。
    描述符 为 做 通串_获取设备描述符 用 ctl、0。
    若 描述符 等于 空：
        返还 空。
    完。
    地址 为 通串_下一个地址。
    通串_下一个地址 为 通串_下一个地址 加 1。
    做 通串_设置地址 用 ctl、0、地址。
    dev 为 新建 通串设备。
    dev 的 地址 为 地址。
    dev 的 端口 为 端口。
    dev 的 厂商ID 为 描述符 的 第 8 项 位或 描述符 的 第 9 项 左移 8。
    dev 的 产品ID 为 描述符 的 第 10 项 位或 描述符 的 第 11 项 左移 8。
    dev 的 类代码 为 描述符 的 第 4 项。
    dev 的 子类代码 为 描述符 的 第 5 项。
    dev 的 协议 为 描述符 的 第 6 项。
    dev 的 已配置 为 假。
    dev 的 端点列表 为 一组：（空）。
    dev 的 厂商名 为 做 通串_获取字符串 用 ctl、地址、描述符 的 第 14 项。
    dev 的 产品名 为 做 通串_获取字符串 用 ctl、地址、描述符 的 第 15 项。
    dev 的 序列号 为 做 通串_获取字符串 用 ctl、地址、描述符 的 第 16 项。
    做 通串_设置配置 用 ctl、地址、1。
    dev 的 已配置 为 真。
    返还 dev。
完。

通串_匹配驱动 接受 dev：
    若 dev 的 类代码 等于 通串_CLASS_HID：
        dev 的 驱动 为 「usb-hid」。
        做 通串_HID_初始化 用 dev。
    否则若 dev 的 类代码 等于 通串_CLASS_MASS_STORAGE：
        dev 的 驱动 为 「usb-storage」。
        做 通串_存储_初始化 用 dev。
    否则若 dev 的 类代码 等于 通串_CLASS_VIDEO：
        dev 的 驱动 为 「uvc」。
        做 通串_摄像头_初始化 用 dev。
    否则若 dev 的 类代码 等于 通串_CLASS_AUDIO：
        dev 的 驱动 为 「usb-audio」。
    否则若 dev 的 类代码 等于 通串_CLASS_HUB：
        dev 的 驱动 为 「usb-hub」。
    否则：
        dev 的 驱动 为 「generic」。
    完。
完。

通串_HID_初始化 接受 dev：
    做 内核信息 用 「通串-HID: 」 加 dev 的 产品名 加 「 (键盘/鼠标)」。
完。

通串_HID_轮询 接受 dev：
    可变 报告 为 一组：0、0、0、0、0、0、0、0。
    结果 为 做 通串_中断传输 用 dev、dev 的 中断IN端点、报告、8。
    若 结果 小于或等于 0：
        返还。
    完。
    若 dev 的 子类 等于 通串_子类_键盘：
        修饰 为 报告 的 第 0 项。
        可变 i 为 2。
        只要 i 小于 8：
            若 报告 的 第 i 项 不等于 0：
                做 键盘_处理按键 用 报告 的 第 i 项、修饰。
            完。
            i 为 i 加 1。
        完。
    完。
    做 否则如果 用 dev 的 子类 等于 通串_子类_鼠标。
    块：
        按钮 为 报告 的 第 0 项。
        dx 为 报告 的 第 1 项。
        dy 为 报告 的 第 2 项。
        若 dx 大于 127：
            dx 为 dx 减 256。
        完。
        若 dy 大于 127：
            dy 为 dy 减 256。
        完。
        做 鼠标_处理移动 用 dx、dy、按钮。
    完。
完。

本章 收束。
