《Intel图形寄存器》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

寄存器_温度 为 0xC0300004。

寄存器_功耗 为 0xC0300008。

寄存器_时钟 为 0xC030000C。

寄存器_栅栏 为 0xC0300020。

寄存器_电源 为 0xC0300030。

温度_警戒 为 85。

温度_临界 为 100。

最大栅栏 为 256。

电源_满速 为 0。

电源_平衡 为 1。

电源_节能 为 2。

图形处理状态 是 一种 事物，含 已初始化、基地址、计算单元数、着色引擎数、架构代号、显存类型、显存大小、显存已用、当前温度、当前功耗、当前时钟、最大时钟、电源模式、栅栏值、栅栏等待数、栅栏超时数、命令提交数、中断次数、温度历史、历史写位。

可变 全局图形 为 0。

温度历史深 为 16。

图形_初始化 接受 基地址：
    全局图形 为 做 堆_分配 用 图形处理状态。
    全局图形 的 基地址 为 基地址。
    全局图形 的 已初始化 为 假。
    全局图形 的 计算单元数 为 0。
    全局图形 的 着色引擎数 为 0。
    全局图形 的 架构代号 为 0。
    全局图形 的 显存类型 为 0。
    全局图形 的 显存大小 为 0。
    全局图形 的 显存已用 为 0。
    全局图形 的 当前温度 为 0。
    全局图形 的 当前功耗 为 0。
    全局图形 的 当前时钟 为 0。
    全局图形 的 最大时钟 为 0。
    全局图形 的 电源模式 为 电源_平衡。
    全局图形 的 栅栏值 为 0。
    全局图形 的 栅栏等待数 为 0。
    全局图形 的 栅栏超时数 为 0。
    全局图形 的 命令提交数 为 0。
    全局图形 的 中断次数 为 0。
    全局图形 的 温度历史 为 做 堆_分配 用 温度历史深 乘 4。
    全局图形 的 历史写位 为 0。
    做 图形_检测硬件。
    全局图形 的 已初始化 为 真。
完。

图形_读寄存器 接受 偏移：
    若 全局图形 等于 0：
        返还 0。
    完。
    返还 做 内存_读32 用 全局图形 的 基地址 加 偏移。
完。

图形_写寄存器 接受 偏移、值：
    若 全局图形 等于 0：
        返还。
    完。
    做 内存_写32 用 全局图形 的 基地址 加 偏移、值。
完。

图形_检测硬件 接受 零参：
    标识 为 做 图形_读寄存器 用 0。
    全局图形 的 架构代号 为 标识 右移 20 位与 0xFF。
    全局图形 的 着色引擎数 为 (标识 右移 12 位与 0xF) 加 1。
    全局图形 的 计算单元数 为 全局图形 的 着色引擎数 乘 16。
    全局图形 的 显存大小 为 做 图形_读寄存器 用 0x100 乘 1024 乘 1024。
    全局图形 的 最大时钟 为 做 图形_读寄存器 用 寄存器_时钟 加 4。
完。

图形_等待栅栏 接受 目标值：
    若 全局图形 等于 0：
        返还 错误_未初始化。
    完。
    全局图形 的 栅栏等待数 为 全局图形 的 栅栏等待数 加 1。
    可变 等待 为 0。
    只要 等待 小于 1000000：
        当前 为 做 图形_读寄存器 用 寄存器_栅栏。
        若 当前 大于或等于 目标值：
            全局图形 的 栅栏值 为 当前。
            返还 0。
        完。
        等待 为 等待 加 1。
    完。
    全局图形 的 栅栏超时数 为 全局图形 的 栅栏超时数 加 1。
    返还 错误_超时。
完。

图形_推进栅栏 接受 零参：
    若 全局图形 等于 0：
        返还 0。
    完。
    全局图形 的 栅栏值 为 全局图形 的 栅栏值 加 1。
    做 图形_写寄存器 用 寄存器_栅栏、全局图形 的 栅栏值。
    返还 全局图形 的 栅栏值。
完。

图形_读温度 接受 零参：
    若 全局图形 等于 0：
        返还 0。
    完。
    原始 为 做 图形_读寄存器 用 寄存器_温度。
    全局图形 的 当前温度 为 原始 右移 8 位与 0xFFF。
    做 内存_写32 用 全局图形 的 温度历史 加 全局图形 的 历史写位 乘 4、全局图形 的 当前温度。
    全局图形 的 历史写位 为 (全局图形 的 历史写位 加 1) 取余于 温度历史深。
    若 全局图形 的 当前温度 大于或等于 温度_临界：
        做 图形_紧急降频。
    否则若 全局图形 的 当前温度 大于或等于 温度_警戒：
        做 图形_渐进降频。
    完。
    返还 全局图形 的 当前温度。
完。

图形_读功耗 接受 零参：
    若 全局图形 等于 0：
        返还 0。
    完。
    全局图形 的 当前功耗 为 做 图形_读寄存器 用 寄存器_功耗。
    返还 全局图形 的 当前功耗。
完。

图形_读时钟 接受 零参：
    若 全局图形 等于 0：
        返还 0。
    完。
    全局图形 的 当前时钟 为 做 图形_读寄存器 用 寄存器_时钟。
    返还 全局图形 的 当前时钟。
完。

图形_设电源模式 接受 模式：
    若 全局图形 等于 0：
        返还。
    完。
    全局图形 的 电源模式 为 模式。
    若 模式 等于 电源_满速：
        做 图形_写寄存器 用 寄存器_电源、0。
    否则若 模式 等于 电源_平衡：
        做 图形_写寄存器 用 寄存器_电源、1。
    否则：
        做 图形_写寄存器 用 寄存器_电源、2。
    完。
完。

图形_紧急降频 接受 零参：
    若 全局图形 等于 0：
        返还。
    完。
    做 图形_写寄存器 用 寄存器_时钟、全局图形 的 最大时钟 除以 4。
    全局图形 的 当前时钟 为 全局图形 的 最大时钟 除以 4。
完。

图形_渐进降频 接受 零参：
    若 全局图形 等于 0：
        返还。
    完。
    新时钟 为 全局图形 的 当前时钟 乘 90 除以 100。
    若 新时钟 小于 全局图形 的 最大时钟 除以 4：
        新时钟 为 全局图形 的 最大时钟 除以 4。
    完。
    做 图形_写寄存器 用 寄存器_时钟、新时钟。
    全局图形 的 当前时钟 为 新时钟。
完。

图形_分配显存 接受 字节数：
    若 全局图形 等于 0：
        返还 0。
    完。
    若 全局图形 的 显存已用 加 字节数 大于 全局图形 的 显存大小：
        返还 0。
    完。
    偏移 为 全局图形 的 显存已用。
    全局图形 的 显存已用 为 全局图形 的 显存已用 加 (字节数 加 4095 位与 按位取反 4095)。
    返还 偏移。
完。

图形_温度趋势 接受 零参：
    若 全局图形 等于 0：
        返还 0。
    完。
    可变 前半 为 0。
    可变 后半 为 0。
    半深 为 温度历史深 除以 2。
    可变 序号 为 0。
    只要 序号 小于 半深：
        前半 为 前半 加 做 内存_读32 用 全局图形 的 温度历史 加 序号 乘 4。
        序号 为 序号 加 1。
    完。
    只要 序号 小于 温度历史深：
        后半 为 后半 加 做 内存_读32 用 全局图形 的 温度历史 加 序号 乘 4。
        序号 为 序号 加 1。
    完。
    若 前半 等于 0：
        返还 0。
    完。
    返还 (后半 减 前半) 乘 1024 除以 前半。
完。

图形_等待微秒 接受 微秒：
    可变 计 为 0。
    只要 计 小于 微秒 乘 100：
        计 为 计 加 1。
    完。
完。

图形_中断处理 接受 零参：
    若 全局图形 等于 0：
        返还。
    完。
    全局图形 的 中断次数 为 全局图形 的 中断次数 加 1。
    当前栅栏 为 做 图形_读寄存器 用 寄存器_栅栏。
    若 当前栅栏 大于 全局图形 的 栅栏值：
        全局图形 的 栅栏值 为 当前栅栏。
    完。
完。

图形_完整统计 接受 零参：
    若 全局图形 等于 0：
        返还 一组：0、0、0、0、0、0、0、0、0。
    完。
    返还 一组：全局图形 的 已初始化、全局图形 的 计算单元数、全局图形 的 着色引擎数、全局图形 的 显存大小、全局图形 的 显存已用、全局图形 的 当前温度、全局图形 的 当前功耗、全局图形 的 栅栏超时数、做 图形_温度趋势。
完。

可变 AI_等待栅栏温_风暴阈值 为 10000。

可变 AI_等待栅栏温_预测IRQ 为 0。

AI_等待栅栏温_风暴检测 接受 触发频率：
    若 触发频率 大于 AI_等待栅栏温_风暴阈值：
        AI_等待栅栏温_风暴阈值 为 AI_等待栅栏温_风暴阈值 乘 3 除以 2。
        返还 真。
    完。
    返还 假。
完。

AI_等待栅栏温_自适应合并 接受 当前延迟：
    若 当前延迟 小于 100：
        返还 16。
    完。
    若 当前延迟 小于 500：
        返还 8。
    完。
    返还 1。
完。

本章 收束。
