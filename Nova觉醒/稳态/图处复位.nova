《图处复位》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

引用《..·中枢·整数转字符串》。

最大命令队列 为 8。

显存_最大块 为 256。

图处设备 是 一种 事物，含 总线号、设备号、功能号、基地址、显存大小、显存已用、已初始化、复位次数、命令提交数、栅栏值。

显存块 是 一种 事物，含 偏移、大小、已分配。

可变 全局图处 为 0。

可变 显存表 为 0。

可变 显存块数 为 0。

图处_初始化 接受 总线、设备、功能：
    全局图处 为 做 堆_分配 用 图处设备。
    全局图处 的 总线号 为 总线。
    全局图处 的 设备号 为 设备。
    全局图处 的 功能号 为 功能。
    全局图处 的 基地址 为 做 图处_读配置 用 总线、设备、功能、0x10 位与 0xFFFFFFF0。
    全局图处 的 显存大小 为 做 图处_检测显存 用 总线、设备、功能。
    全局图处 的 显存已用 为 0。
    全局图处 的 已初始化 为 假。
    全局图处 的 复位次数 为 0。
    全局图处 的 命令提交数 为 0。
    全局图处 的 栅栏值 为 0。
    显存表 为 做 堆_分配 用 显存_最大块 乘 16。
    显存块数 为 0。
    做 图处_复位。
    做 图处_配置中断 用 总线、设备、功能。
    全局图处 的 已初始化 为 真。
完。

图处_读配置 接受 总线、设备、功能、偏移：
    地址 为 0x80000000 位或 总线 左移 16 位或 设备 左移 11 位或 功能 左移 8 位或 偏移 位与 0xFC。
    做 端口_写32 用 0xCF8、地址。
    返还 做 端口_读32 用 0xCFC。
完。

图处_写配置 接受 总线、设备、功能、偏移、值：
    地址 为 0x80000000 位或 总线 左移 16 位或 设备 左移 11 位或 功能 左移 8 位或 偏移 位与 0xFC。
    做 端口_写32 用 0xCF8、地址。
    做 端口_写32 用 0xCFC、值。
完。

图处_检测显存 接受 总线、设备、功能：
    原始 为 做 图处_读配置 用 总线、设备、功能、0x10。
    做 图处_写配置 用 总线、设备、功能、0x10、0xFFFFFFFF。
    大小掩码 为 做 图处_读配置 用 总线、设备、功能、0x10。
    做 图处_写配置 用 总线、设备、功能、0x10、原始。
    大小 为 按位取反 (大小掩码 位与 0xFFFFFFF0) 加 1。
    返还 大小。
完。

图处_复位 接受 零参：
    若 全局图处 等于 0：
        返还。
    完。
    全局图处 的 复位次数 为 全局图处 的 复位次数 加 1。
    命令 为 做 图处_读配置 用 全局图处 的 总线号、全局图处 的 设备号、全局图处 的 功能号、0x04。
    做 图处_写配置 用 全局图处 的 总线号、全局图处 的 设备号、全局图处 的 功能号、0x04、命令 位与 按位取反 0x07。
    可变 等待 为 0。
    只要 等待 小于 100000：
        等待 为 等待 加 1。
    完。
    做 图处_写配置 用 全局图处 的 总线号、全局图处 的 设备号、全局图处 的 功能号、0x04、命令 位或 0x07。
完。

图处_配置中断 接受 总线、设备、功能：
    命令 为 做 图处_读配置 用 总线、设备、功能、0x04。
    做 图处_写配置 用 总线、设备、功能、0x04、命令 位与 按位取反 (1 左移 10)。
完。

图处_分配显存 接受 大小：
    若 全局图处 等于 0：
        返还 0。
    完。
    对齐大小 为 大小 加 4095 位与 按位取反 4095。
    若 全局图处 的 显存已用 加 对齐大小 大于 全局图处 的 显存大小：
        返还 0。
    完。
    若 显存块数 大于或等于 显存_最大块：
        返还 0。
    完。
    偏移 为 全局图处 的 显存已用。
    做 内存_写64 用 显存表 加 显存块数 乘 16、偏移。
    做 内存_写64 用 显存表 加 显存块数 乘 16 加 8、对齐大小。
    显存块数 为 显存块数 加 1。
    全局图处 的 显存已用 为 全局图处 的 显存已用 加 对齐大小。
    返还 全局图处 的 基地址 加 偏移。
完。

图处_释放显存 接受 地址：
    若 全局图处 等于 0：
        返还。
    完。
    偏移 为 地址 减 全局图处 的 基地址。
    可变 序号 为 0。
    只要 序号 小于 显存块数：
        若 做 内存_读64 用 显存表 加 序号 乘 16 等于 偏移：
            大小 为 做 内存_读64 用 显存表 加 序号 乘 16 加 8。
            全局图处 的 显存已用 为 全局图处 的 显存已用 减 大小。
            返还。
        完。
        序号 为 序号 加 1。
    完。
完。

图处_提交命令 接受 命令指针、命令大小：
    若 全局图处 等于 0 或者 非 全局图处 的 已初始化：
        返还 错误_未初始化。
    完。
    全局图处 的 命令提交数 为 全局图处 的 命令提交数 加 1。
    全局图处 的 栅栏值 为 全局图处 的 栅栏值 加 1。
    返还 全局图处 的 栅栏值。
完。

图处_等待栅栏 接受 目标值：
    若 全局图处 等于 0：
        返还 错误_未初始化。
    完。
    可变 等待 为 0。
    只要 等待 小于 1000000：
        若 全局图处 的 栅栏值 大于或等于 目标值：
            返还 0。
        完。
        等待 为 等待 加 1。
    完。
    返还 错误_超时。
完。

图处_读寄存器 接受 偏移：
    若 全局图处 等于 0：
        返还 0。
    完。
    返还 做 内存_读32 用 全局图处 的 基地址 加 偏移。
完。

图处_写寄存器 接受 偏移、值：
    若 全局图处 等于 0：
        返还。
    完。
    做 内存_写32 用 全局图处 的 基地址 加 偏移、值。
完。

图处_显存使用率 接受 零参：
    若 全局图处 等于 0 或者 全局图处 的 显存大小 等于 0：
        返还 0。
    完。
    返还 全局图处 的 显存已用 乘 100 除以 全局图处 的 显存大小。
完。

图处_完整统计 接受 零参：
    若 全局图处 等于 0：
        返还 一组：0、0、0、0、0、0。
    完。
    返还 一组：全局图处 的 已初始化、全局图处 的 显存大小、全局图处 的 显存已用、做 图处_显存使用率、全局图处 的 命令提交数、全局图处 的 复位次数。
完。

可变 图处_错误总数 为 0。

可变 图处_挂起次数 为 0。

可变 图处_超时次数 为 0。

可变 图处_中断次数 为 0。

图处_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 全局图处 等于 0：
        做 追加 用 问题、「图处设备未初始化」。
        返还 问题。
    完。
    若 非 全局图处 的 已初始化：
        做 追加 用 问题、「图处设备初始化未完成」。
    完。
    若 全局图处 的 复位次数 大于 5：
        做 追加 用 问题、「图处复位次数过多: 」 加 做 整数转字符串 用 全局图处 的 复位次数。
    完。
    若 做 图处_显存使用率 大于 95：
        做 追加 用 问题、「显存即将耗尽: 」 加 做 整数转字符串 用 做 图处_显存使用率 加 「%」。
    完。
    若 图处_错误总数 大于 10：
        做 追加 用 问题、「图处错误过多: 」 加 做 整数转字符串 用 图处_错误总数。
    完。
    若 图处_挂起次数 大于 3：
        做 追加 用 问题、「图处频繁挂起: 」 加 做 整数转字符串 用 图处_挂起次数 加 「次」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「图处设备健康」。
    完。
    返还 问题。
完。

图处_详细统计 接受 零参：
    若 全局图处 等于 0：
        返还 空。
    完。
    返还 一本字典：已初始化 对应 全局图处 的 已初始化、总线 对应 全局图处 的 总线号、设备 对应 全局图处 的 设备号、功能 对应 全局图处 的 功能号、基地址 对应 全局图处 的 基地址、显存总量 对应 全局图处 的 显存大小、显存已用 对应 全局图处 的 显存已用、显存使用率 对应 做 图处_显存使用率、显存剩余 对应 全局图处 的 显存大小 减 全局图处 的 显存已用、命令提交 对应 全局图处 的 命令提交数、复位次数 对应 全局图处 的 复位次数、栅栏值 对应 全局图处 的 栅栏值、错误数 对应 图处_错误总数、挂起数 对应 图处_挂起次数、超时数 对应 图处_超时次数、中断数 对应 图处_中断次数、显存块数 对应 显存块数。
完。

图处_显存剩余 接受 零参：
    若 全局图处 等于 0：
        返还 0。
    完。
    返还 全局图处 的 显存大小 减 全局图处 的 显存已用。
完。

图处_显存剩余MB 接受 零参：
    返还 做 图处_显存剩余 除以 1024 除以 1024。
完。

图处_显存总MB 接受 零参：
    若 全局图处 等于 0：
        返还 0。
    完。
    返还 全局图处 的 显存大小 除以 1024 除以 1024。
完。

图处_显存已用MB 接受 零参：
    若 全局图处 等于 0：
        返还 0。
    完。
    返还 全局图处 的 显存已用 除以 1024 除以 1024。
完。

图处_显存不足 接受 零参：
    返还 做 图处_显存使用率 大于 90。
完。

图处_显存块信息 接受 序号：
    若 序号 大于或等于 显存块数：
        返还 空。
    完。
    返还 一本字典：偏移 对应 做 内存_读64 用 显存表 加 序号 乘 16、大小 对应 做 内存_读64 用 显存表 加 序号 乘 16 加 8。
完。

图处_显存碎片数 接受 零参：
    返还 显存块数。
完。

图处_最大连续显存 接受 零参：
    若 全局图处 等于 0：
        返还 0。
    完。
    返还 全局图处 的 显存大小 减 全局图处 的 显存已用。
完。

图处电源_D0 为 0。

图处电源_D1 为 1。

图处电源_D2 为 2。

图处电源_D3冷 为 3。

图处电源_D3热 为 4。

可变 图处_当前电源状态 为 0。

图处_设置电源状态 接受 状态：
    若 全局图处 等于 0：
        返还 错误_未初始化。
    完。
    若 状态 小于 图处电源_D0 或者 状态 大于 图处电源_D3热：
        返还 错误_无效参数。
    完。
    图处_当前电源状态 为 状态。
    若 状态 等于 图处电源_D0：
        做 图处_复位。
    完。
    返还 0。
完。

图处_获取电源状态 接受 零参：
    返还 图处_当前电源状态。
完。

图处_电源状态名 接受 状态：
    若 状态 等于 图处电源_D0：
        返还 「D0(全功率)」。
    完。
    若 状态 等于 图处电源_D1：
        返还 「D1(低功耗)」。
    完。
    若 状态 等于 图处电源_D2：
        返还 「D2(更低功耗)」。
    完。
    若 状态 等于 图处电源_D3冷：
        返还 「D3cold(关闭)」。
    完。
    若 状态 等于 图处电源_D3热：
        返还 「D3hot(待机)」。
    完。
    返还 「未知」。
完。

图处_挂起 接受 零参：
    图处_挂起次数 为 图处_挂起次数 加 1。
    返还 做 图处_设置电源状态 用 图处电源_D3热。
完。

图处_恢复 接受 零参：
    结果 为 做 图处_设置电源状态 用 图处电源_D0。
    若 结果 等于 0 并且 全局图处 不等于 0：
        全局图处 的 已初始化 为 真。
    完。
    返还 结果。
完。

PCI_VENDOR_ID 为 0x00。

PCI_DEVICE_ID 为 0x02。

PCI_COMMAND 为 0x04。

PCI_STATUS 为 0x06。

PCI_REVISION 为 0x08。

PCI_CLASS 为 0x0B。

PCI_SUBCLASS 为 0x0A。

PCI_BAR0 为 0x10。

PCI_BAR1 为 0x14。

PCI_BAR2 为 0x18。

PCI_INT_LINE 为 0x3C。

PCI_INT_PIN 为 0x3D。

PCI_CMD_IO 为 0x01。

PCI_CMD_MEMORY 为 0x02。

PCI_CMD_BUSMASTER 为 0x04。

PCI_CMD_INT_DISABLE 为 0x400。

图处_获取厂商ID 接受 零参：
    若 全局图处 等于 0：
        返还 0。
    完。
    返还 做 图处_读配置 用 全局图处 的 总线号、全局图处 的 设备号、全局图处 的 功能号、PCI_VENDOR_ID 位与 0xFFFF。
完。

图处_获取设备ID 接受 零参：
    若 全局图处 等于 0：
        返还 0。
    完。
    返还 做 图处_读配置 用 全局图处 的 总线号、全局图处 的 设备号、全局图处 的 功能号、PCI_DEVICE_ID 右移 16 位与 0xFFFF。
完。

图处_获取类别 接受 零参：
    若 全局图处 等于 0：
        返还 0。
    完。
    返还 做 图处_读配置 用 全局图处 的 总线号、全局图处 的 设备号、全局图处 的 功能号、PCI_CLASS 位与 0xFF。
完。

图处_是显示控制器 接受 零参：
    返还 做 图处_获取类别 等于 0x03。
完。

可变 命令队列_头 为 0。

可变 命令队列_尾 为 0。

可变 命令队列_大小 为 4096。

图处_命令队列满 接受 零参：
    返还 (命令队列_尾 加 1) 取余于 命令队列_大小 等于 命令队列_头。
完。

图处_命令队列空 接受 零参：
    返还 命令队列_头 等于 命令队列_尾。
完。

图处_命令队列深度 接受 零参：
    若 命令队列_尾 大于或等于 命令队列_头：
        返还 命令队列_尾 减 命令队列_头。
    完。
    返还 命令队列_大小 减 命令队列_头 加 命令队列_尾。
完。

图处_等待空闲 接受 零参：
    若 全局图处 等于 0：
        返还 错误_未初始化。
    完。
    可变 等待 为 0。
    只要 非 做 图处_命令队列空 并且 等待 小于 10000000：
        等待 为 等待 加 1。
    完。
    若 等待 大于或等于 10000000：
        图处_超时次数 为 图处_超时次数 加 1。
        返还 错误_超时。
    完。
    返还 0。
完。

图处_栅栏同步 接受 零参：
    若 全局图处 等于 0：
        返还 错误_未初始化。
    完。
    目标 为 全局图处 的 栅栏值。
    返还 做 图处_等待栅栏 用 目标。
完。

图处_重置统计 接受 零参：
    图处_错误总数 为 0。
    图处_挂起次数 为 0。
    图处_超时次数 为 0。
    图处_中断次数 为 0。
    若 全局图处 不等于 0：
        全局图处 的 命令提交数 为 0。
        全局图处 的 复位次数 为 0。
    完。
完。

图处_打印信息 接受 零参：
    若 全局图处 等于 0：
        做 内核信息 用 「图处: 未初始化」。
        返还。
    完。
    做 内核信息 用 「图处: 总线=」 加 做 整数转字符串 用 全局图处 的 总线号 加 「 设备=」 加 做 整数转字符串 用 全局图处 的 设备号 加 「 功能=」 加 做 整数转字符串 用 全局图处 的 功能号。
    做 内核信息 用 「  基地址=」 加 做 十六进制 用 全局图处 的 基地址 加 「 显存=」 加 做 整数转字符串 用 做 图处_显存总MB 加 「MB」 加 「 已用=」 加 做 整数转字符串 用 做 图处_显存已用MB 加 「MB」 加 「 (」 加 做 整数转字符串 用 做 图处_显存使用率 加 「%)」。
    做 内核信息 用 「  命令=」 加 做 整数转字符串 用 全局图处 的 命令提交数 加 「 复位=」 加 做 整数转字符串 用 全局图处 的 复位次数 加 「 电源=」 加 做 图处_电源状态名 用 图处_当前电源状态。
完。

图处_是否就绪 接受 零参：
    返还 全局图处 不等于 0 并且 全局图处 的 已初始化 并且 图处_当前电源状态 等于 图处电源_D0。
完。

图处_错误率 接受 零参：
    若 全局图处 等于 0 或者 全局图处 的 命令提交数 等于 0：
        返还 0。
    完。
    返还 图处_错误总数 乘 100 除以 全局图处 的 命令提交数。
完。

GPU_VENDOR_INTEL 为 0x8086。

GPU_VENDOR_AMD 为 0x1002。

GPU_VENDOR_NVIDIA 为 0x10DE。

GPU_VENDOR_VMWARE 为 0x15AD。

GPU_VENDOR_VIRTIO 为 0x1AF4。

GPU_VENDOR_BOCHS 为 0x1234。

图处_厂商名 接受 零参：
    vid 为 做 图处_获取厂商ID。
    若 vid 等于 GPU_VENDOR_INTEL：
        返还 「Intel」。
    完。
    若 vid 等于 GPU_VENDOR_AMD：
        返还 「AMD」。
    完。
    若 vid 等于 GPU_VENDOR_NVIDIA：
        返还 「NVIDIA」。
    完。
    若 vid 等于 GPU_VENDOR_VMWARE：
        返还 「VMware」。
    完。
    若 vid 等于 GPU_VENDOR_VIRTIO：
        返还 「VirtIO」。
    完。
    若 vid 等于 GPU_VENDOR_BOCHS：
        返还 「Bochs/QEMU」。
    完。
    返还 「未知(0x」 加 做 十六进制 用 vid 加 「)」。
完。

可变 AI_图处复位_风暴阈值 为 10000。

可变 AI_图处复位_预测IRQ 为 0。

AI_图处复位_风暴检测 接受 触发频率：
    若 触发频率 大于 AI_图处复位_风暴阈值：
        AI_图处复位_风暴阈值 为 AI_图处复位_风暴阈值 乘 3 除以 2。
        返还 真。
    完。
    返还 假。
完。

AI_图处复位_自适应合并 接受 当前延迟：
    若 当前延迟 小于 100：
        返还 16。
    完。
    若 当前延迟 小于 500：
        返还 8。
    完。
    返还 1。
完。

本章 收束。
