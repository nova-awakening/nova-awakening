《设备驱动模型》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·中枢·哈希整数》。

引用《..·中枢·内容可见性》。

最大总线 为 16。

最大驱动 为 64。

最大设备 为 256。

总线描述 是 一种 事物，含 名称哈希、匹配回调、设备数。

驱动描述 是 一种 事物，含 名称哈希、总线索引、探测回调、移除回调、挂起回调、恢复回调、绑定设备数。

设备描述 是 一种 事物，含 标识、总线索引、驱动索引、状态、电源状态、中断号、基地址、绑定时刻。

可变 总线表 为 一组：（空）。

可变 总线数 为 0。

可变 驱动表 为 一组：（空）。

可变 驱动数 为 0。

可变 设备表 为 一组：（空）。

可变 设备数 为 0。

可变 探测成功 为 0。

可变 探测失败 为 0。

总线_注册 接受 名称、匹配回调：
    若 总线数 大于或等于 最大总线：
        返还 负 1。
    完。
    总线 为 新建 总线描述。
    总线 的 名称哈希 为 做 哈希_字符串 用 名称。
    总线 的 匹配回调 为 匹配回调。
    总线 的 设备数 为 0。
    做 追加 用 总线表、总线。
    索引 为 总线数。
    总线数 为 总线数 加 1。
    返还 索引。
完。

驱动_注册 接受 名称、总线索引、探测、移除：
    若 驱动数 大于或等于 最大驱动：
        返还 负 1。
    完。
    若 总线索引 大于或等于 总线数：
        返还 错误_无效参数。
    完。
    驱动 为 新建 驱动描述。
    驱动 的 名称哈希 为 做 哈希_字符串 用 名称。
    驱动 的 总线索引 为 总线索引。
    驱动 的 探测回调 为 探测。
    驱动 的 移除回调 为 移除。
    驱动 的 挂起回调 为 0。
    驱动 的 恢复回调 为 0。
    驱动 的 绑定设备数 为 0。
    做 追加 用 驱动表、驱动。
    索引 为 驱动数。
    驱动数 为 驱动数 加 1。
    做 驱动_尝试绑定 用 索引。
    返还 索引。
完。

设备_注册 接受 总线索引、中断号、基地址：
    若 设备数 大于或等于 最大设备：
        返还 负 1。
    完。
    若 总线索引 大于或等于 总线数：
        返还 错误_无效参数。
    完。
    设备 为 新建 设备描述。
    设备 的 标识 为 设备数。
    设备 的 总线索引 为 总线索引。
    设备 的 驱动索引 为 负 1。
    设备 的 状态 为 0。
    设备 的 电源状态 为 0。
    设备 的 中断号 为 中断号。
    设备 的 基地址 为 基地址。
    设备 的 绑定时刻 为 0。
    做 追加 用 设备表、设备。
    总线表 的 第 总线索引 项 的 设备数 为 总线表 的 第 总线索引 项 的 设备数 加 1。
    索引 为 设备数。
    设备数 为 设备数 加 1。
    做 设备_尝试匹配 用 索引。
    返还 索引。
完。

驱动_尝试绑定 接受 驱动索引：
    驱动 为 驱动表 的 第 驱动索引 项。
    可变 序号 为 0。
    只要 序号 小于 设备数：
        若 设备表 的 第 序号 项 的 总线索引 等于 驱动 的 总线索引 并且 设备表 的 第 序号 项 的 驱动索引 等于 负 1：
            做 设备_绑定驱动 用 序号、驱动索引。
        完。
        序号 为 序号 加 1。
    完。
完。

设备_尝试匹配 接受 设备索引：
    设备 为 设备表 的 第 设备索引 项。
    可变 序号 为 0。
    只要 序号 小于 驱动数：
        若 驱动表 的 第 序号 项 的 总线索引 等于 设备 的 总线索引：
            总线 为 总线表 的 第 设备 的 总线索引 项。
            匹配 为 真。
            若 总线 的 匹配回调 不等于 0：
                匹配 为 做 总线 的 匹配回调 用 设备索引、序号。
            完。
            若 匹配：
                做 设备_绑定驱动 用 设备索引、序号。
                返还。
            完。
        完。
        序号 为 序号 加 1。
    完。
完。

设备_绑定驱动 接受 设备索引、驱动索引：
    设备 为 设备表 的 第 设备索引 项。
    驱动 为 驱动表 的 第 驱动索引 项。
    若 驱动 的 探测回调 不等于 0：
        结果 为 做 驱动 的 探测回调 用 设备索引。
        若 结果 不等于 0：
            探测失败 为 探测失败 加 1。
            返还。
        完。
    完。
    设备 的 驱动索引 为 驱动索引。
    设备 的 状态 为 1。
    设备 的 绑定时刻 为 做 平台_获取高精度时间。
    驱动 的 绑定设备数 为 驱动 的 绑定设备数 加 1。
    探测成功 为 探测成功 加 1。
完。

设备_解绑驱动 接受 设备索引：
    若 设备索引 大于或等于 设备数：
        返还。
    完。
    设备 为 设备表 的 第 设备索引 项。
    若 设备 的 驱动索引 大于或等于 0 并且 设备 的 驱动索引 小于 驱动数：
        驱动 为 驱动表 的 第 设备 的 驱动索引 项。
        若 驱动 的 移除回调 不等于 0：
            做 驱动 的 移除回调 用 设备索引。
        完。
        驱动 的 绑定设备数 为 驱动 的 绑定设备数 减 1。
    完。
    设备 的 驱动索引 为 负 1。
    设备 的 状态 为 0。
完。

设备_挂起全部 接受 零参：
    可变 序号 为 设备数 减 1。
    只要 序号 大于或等于 0：
        设备 为 设备表 的 第 序号 项。
        若 设备 的 驱动索引 大于或等于 0：
            驱动 为 驱动表 的 第 设备 的 驱动索引 项。
            若 驱动 的 挂起回调 不等于 0：
                做 驱动 的 挂起回调 用 序号。
            完。
            设备 的 电源状态 为 1。
        完。
        序号 为 序号 减 1。
    完。
完。

设备_恢复全部 接受 零参：
    可变 序号 为 0。
    只要 序号 小于 设备数：
        设备 为 设备表 的 第 序号 项。
        若 设备 的 驱动索引 大于或等于 0 并且 设备 的 电源状态 等于 1：
            驱动 为 驱动表 的 第 设备 的 驱动索引 项。
            若 驱动 的 恢复回调 不等于 0：
                做 驱动 的 恢复回调 用 序号。
            完。
            设备 的 电源状态 为 0。
        完。
        序号 为 序号 加 1。
    完。
完。

驱动模型_完整统计 接受 零参：
    返还 一组：总线数、驱动数、设备数、探测成功、探测失败。
完。

本章 收束。
