《GPU驱动辅助》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

寄存器_控制 为 0x00000。

寄存器_中断状态 为 0x00100。

寄存器_中断掩码 为 0x00140。

寄存器_显存控制 为 0x10000。

寄存器_显存大小 为 0x10020。

寄存器_引擎状态 为 0x20000。

寄存器_命令入口 为 0x40000。

寄存器_温度 为 0x20400。

电源_满速 为 0。

电源_节能 为 1。

电源_休眠 为 2。

显卡状态 是 一种 事物，含 已初始化、基地址、显存大小、显存已用、流处理器数、图形处理簇数、通道数、架构代、温度、电源状态、中断次数、命令提交数、命令完成数、错误数。

可变 全局显卡 为 0。

显卡_初始化 接受 基地址：
    全局显卡 为 做 堆_分配 用 显卡状态。
    全局显卡 的 基地址 为 基地址。
    全局显卡 的 显存大小 为 做 显卡_读寄存器 用 寄存器_显存大小 乘 1024 乘 1024。
    全局显卡 的 显存已用 为 0。
    全局显卡 的 流处理器数 为 0。
    全局显卡 的 图形处理簇数 为 0。
    全局显卡 的 通道数 为 0。
    全局显卡 的 架构代 为 0。
    全局显卡 的 温度 为 0。
    全局显卡 的 电源状态 为 电源_满速。
    全局显卡 的 中断次数 为 0。
    全局显卡 的 命令提交数 为 0。
    全局显卡 的 命令完成数 为 0。
    全局显卡 的 错误数 为 0。
    做 显卡_检测硬件。
    做 显卡_写寄存器 用 寄存器_中断掩码、0xFFFFFFFF。
    全局显卡 的 已初始化 为 真。
完。

显卡_读寄存器 接受 偏移：
    若 全局显卡 等于 0：
        返还 0。
    完。
    返还 做 内存_读32 用 全局显卡 的 基地址 加 偏移。
完。

显卡_写寄存器 接受 偏移、值：
    若 全局显卡 等于 0：
        返还。
    完。
    做 内存_写32 用 全局显卡 的 基地址 加 偏移、值。
完。

显卡_检测硬件 接受 零参：
    控制 为 做 显卡_读寄存器 用 寄存器_控制。
    全局显卡 的 架构代 为 控制 右移 20 位与 0xFF。
    全局显卡 的 图形处理簇数 为 (控制 右移 12 位与 0xF) 加 1。
    全局显卡 的 流处理器数 为 全局显卡 的 图形处理簇数 乘 128。
    全局显卡 的 通道数 为 (控制 右移 8 位与 0xF) 加 1。
完。

显卡_等待微秒 接受 微秒：
    可变 计 为 0。
    只要 计 小于 微秒 乘 100：
        计 为 计 加 1。
    完。
完。

显卡_分配显存 接受 字节数：
    若 全局显卡 等于 0：
        返还 0。
    完。
    若 全局显卡 的 显存已用 加 字节数 大于 全局显卡 的 显存大小：
        返还 0。
    完。
    偏移 为 全局显卡 的 显存已用。
    全局显卡 的 显存已用 为 全局显卡 的 显存已用 加 (字节数 加 4095 位与 按位取反 4095)。
    返还 偏移。
完。

显卡_释放显存 接受 偏移、字节数：
    若 全局显卡 等于 0：
        返还。
    完。
    若 偏移 加 字节数 等于 全局显卡 的 显存已用：
        全局显卡 的 显存已用 为 偏移。
    完。
完。

显卡_提交命令 接受 命令数据、命令长度：
    若 全局显卡 等于 0：
        返还 错误_未初始化。
    完。
    引擎状态 为 做 显卡_读寄存器 用 寄存器_引擎状态。
    若 引擎状态 位与 1 不等于 0：
        可变 等待 为 0。
        只要 等待 小于 100000：
            若 做 显卡_读寄存器 用 寄存器_引擎状态 位与 1 等于 0：
                跳出。
            完。
            等待 为 等待 加 1。
        完。
        若 等待 大于或等于 100000：
            全局显卡 的 错误数 为 全局显卡 的 错误数 加 1。
            返还 错误_超时。
        完。
    完。
    可变 位 为 0。
    只要 位 小于 命令长度：
        做 显卡_写寄存器 用 寄存器_命令入口 加 位、做 内存_读32 用 命令数据 加 位。
        位 为 位 加 4。
    完。
    做 显卡_写寄存器 用 寄存器_引擎状态、1。
    全局显卡 的 命令提交数 为 全局显卡 的 命令提交数 加 1。
    返还 0。
完。

显卡_等待完成 接受 零参：
    若 全局显卡 等于 0：
        返还 错误_未初始化。
    完。
    可变 等待 为 0。
    只要 等待 小于 1000000：
        若 做 显卡_读寄存器 用 寄存器_引擎状态 位与 1 等于 0：
            全局显卡 的 命令完成数 为 全局显卡 的 命令完成数 加 1。
            返还 0。
        完。
        等待 为 等待 加 1。
    完。
    全局显卡 的 错误数 为 全局显卡 的 错误数 加 1。
    返还 错误_超时。
完。

显卡_中断处理 接受 零参：
    若 全局显卡 等于 0：
        返还。
    完。
    状态 为 做 显卡_读寄存器 用 寄存器_中断状态。
    做 显卡_写寄存器 用 寄存器_中断状态、状态。
    全局显卡 的 中断次数 为 全局显卡 的 中断次数 加 1。
    若 状态 位与 1 不等于 0：
        全局显卡 的 命令完成数 为 全局显卡 的 命令完成数 加 1。
    完。
    若 状态 位与 1 左移 8 不等于 0：
        全局显卡 的 错误数 为 全局显卡 的 错误数 加 1。
    完。
完。

显卡_读温度 接受 零参：
    若 全局显卡 等于 0：
        返还 0。
    完。
    原始 为 做 显卡_读寄存器 用 寄存器_温度。
    全局显卡 的 温度 为 原始 右移 8 位与 0xFF。
    返还 全局显卡 的 温度。
完。

显卡_设电源状态 接受 状态：
    若 全局显卡 等于 0：
        返还。
    完。
    全局显卡 的 电源状态 为 状态。
    若 状态 等于 电源_节能：
        做 显卡_写寄存器 用 寄存器_控制、做 显卡_读寄存器 用 寄存器_控制 位或 1 左移 4。
    否则若 状态 等于 电源_满速：
        做 显卡_写寄存器 用 寄存器_控制、做 显卡_读寄存器 用 寄存器_控制 位与 按位取反 (1 左移 4)。
    完。
完。

显卡_获取状态 接受 零参：
    返还 全局显卡。
完。

显卡_完整统计 接受 零参：
    若 全局显卡 等于 0：
        返还 一组：0、0、0、0、0、0、0、0。
    完。
    返还 一组：全局显卡 的 已初始化、全局显卡 的 流处理器数、全局显卡 的 图形处理簇数、全局显卡 的 显存大小、全局显卡 的 显存已用、全局显卡 的 命令提交数、全局显卡 的 温度、全局显卡 的 错误数。
完。

本章 收束。
