《音频统计》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·中枢·哈希整数》。

引用《..·感知·内核直连层》。

最大声卡 为 8。

最大流 为 16。

采样率_44100 为 44100。

采样率_48000 为 48000。

格式_16位 为 16。

格式_24位 为 24。

方向_播放 为 0。

方向_录音 为 1。

声卡 是 一种 事物，含 标识、名称哈希、流数、主音量、静音、采样率、位深度、通道数。

音频流 是 一种 事物，含 声卡索引、方向、缓冲地址、缓冲大小、写位置、读位置、帧大小、运行中、总帧数、欠载次数。

可变 声卡表 为 一组：（空）。

可变 声卡数 为 0。

可变 流表 为 一组：（空）。

可变 流数 为 0。

可变 播放总帧 为 0。

可变 录音总帧 为 0。

可变 总欠载 为 0。

音频_初始化 接受 零参：
    声卡表 为 一组：（空）。
    声卡数 为 0。
    流表 为 一组：（空）。
    流数 为 0。
完。

音频_注册声卡 接受 名称：
    若 声卡数 大于或等于 最大声卡：
        返还 负 1。
    完。
    卡 为 新建 声卡。
    卡 的 标识 为 声卡数。
    卡 的 名称哈希 为 做 哈希_字符串 用 名称。
    卡 的 流数 为 0。
    卡 的 主音量 为 80。
    卡 的 静音 为 假。
    卡 的 采样率 为 采样率_48000。
    卡 的 位深度 为 格式_16位。
    卡 的 通道数 为 2。
    做 追加 用 声卡表、卡。
    索引 为 声卡数。
    声卡数 为 声卡数 加 1。
    返还 索引。
完。

音频_创建流 接受 声卡索引、方向、缓冲大小：
    若 流数 大于或等于 最大流 或者 声卡索引 大于或等于 声卡数：
        返还 负 1。
    完。
    流 为 新建 音频流。
    流 的 声卡索引 为 声卡索引。
    流 的 方向 为 方向。
    流 的 缓冲大小 为 缓冲大小。
    流 的 缓冲地址 为 做 堆_分配 用 缓冲大小。
    流 的 写位置 为 0。
    流 的 读位置 为 0。
    卡 为 声卡表 的 第 声卡索引 项。
    流 的 帧大小 为 卡 的 通道数 乘 (卡 的 位深度 除以 8)。
    流 的 运行中 为 假。
    流 的 总帧数 为 0。
    流 的 欠载次数 为 0。
    做 追加 用 流表、流。
    卡 的 流数 为 卡 的 流数 加 1。
    索引 为 流数。
    流数 为 流数 加 1。
    返还 索引。
完。

音频_写入 接受 流索引、数据、帧数：
    若 流索引 大于或等于 流数：
        返还 0。
    完。
    流 为 流表 的 第 流索引 项。
    若 流 的 方向 不等于 方向_播放：
        返还 错误_无效参数。
    完。
    字节数 为 帧数 乘 流 的 帧大小。
    可用 为 流 的 缓冲大小 减 (流 的 写位置 减 流 的 读位置 加 流 的 缓冲大小) 取余于 流 的 缓冲大小。
    若 字节数 大于 可用：
        字节数 为 可用。
    完。
    可变 已写 为 0。
    只要 已写 小于 字节数：
        写偏移 为 流 的 写位置 取余于 流 的 缓冲大小。
        做 内存_写8 用 流 的 缓冲地址 加 写偏移、做 内存_读8 用 数据 加 已写。
        流 的 写位置 为 流 的 写位置 加 1。
        已写 为 已写 加 1。
    完。
    写帧 为 已写 除以 流 的 帧大小。
    流 的 总帧数 为 流 的 总帧数 加 写帧。
    播放总帧 为 播放总帧 加 写帧。
    返还 写帧。
完。

音频_读取 接受 流索引、缓冲、帧数：
    若 流索引 大于或等于 流数：
        返还 0。
    完。
    流 为 流表 的 第 流索引 项。
    若 流 的 方向 不等于 方向_录音：
        返还 错误_无效参数。
    完。
    字节数 为 帧数 乘 流 的 帧大小。
    可用 为 (流 的 写位置 减 流 的 读位置 加 流 的 缓冲大小) 取余于 流 的 缓冲大小。
    若 字节数 大于 可用：
        字节数 为 可用。
        流 的 欠载次数 为 流 的 欠载次数 加 1。
        总欠载 为 总欠载 加 1。
    完。
    可变 已读 为 0。
    只要 已读 小于 字节数：
        读偏移 为 流 的 读位置 取余于 流 的 缓冲大小。
        做 内存_写8 用 缓冲 加 已读、做 内存_读8 用 流 的 缓冲地址 加 读偏移。
        流 的 读位置 为 流 的 读位置 加 1。
        已读 为 已读 加 1。
    完。
    读帧 为 已读 除以 流 的 帧大小。
    流 的 总帧数 为 流 的 总帧数 加 读帧。
    录音总帧 为 录音总帧 加 读帧。
    返还 读帧。
完。

音频_设音量 接受 声卡索引、音量：
    若 声卡索引 大于或等于 声卡数：
        返还。
    完。
    若 音量 大于 100：
        音量 为 100。
    完。
    若 音量 小于 0：
        音量 为 0。
    完。
    声卡表 的 第 声卡索引 项 的 主音量 为 音量。
完。

音频_设静音 接受 声卡索引、静音：
    若 声卡索引 大于或等于 声卡数：
        返还。
    完。
    声卡表 的 第 声卡索引 项 的 静音 为 静音。
完。

音频_获取音量 接受 声卡索引：
    若 声卡索引 大于或等于 声卡数：
        返还 0。
    完。
    返还 声卡表 的 第 声卡索引 项 的 主音量。
完。

音频_启动流 接受 流索引：
    若 流索引 大于或等于 流数：
        返还。
    完。
    流表 的 第 流索引 项 的 运行中 为 真。
完。

音频_停止流 接受 流索引：
    若 流索引 大于或等于 流数：
        返还。
    完。
    流表 的 第 流索引 项 的 运行中 为 假。
完。

音频_统计 接受 零参：
    可变 主音量 为 0。
    若 声卡数 大于 0：
        主音量 为 声卡表 的 第 0 项 的 主音量。
    完。
    返还 一组：声卡数、流数、播放总帧、录音总帧、主音量、总欠载。
完。

可变 AI_音频统计_缓冲水位 为 0。

AI_音频统计_缓冲策略 接受 带宽、播放速率：
    若 带宽 等于 0：
        返还 「暂停缓冲」。
    完。
    AI_音频统计_缓冲水位 为 带宽 乘 1000 除以 播放速率。
    若 AI_音频统计_缓冲水位 大于 3000：
        返还 「充足」。
    完。
    若 AI_音频统计_缓冲水位 大于 1000：
        返还 「正常」。
    完。
    返还 「紧张」。
完。

AI_音频统计_码率自适应 接受 缓冲水位、可用码率列表：
    若 缓冲水位 大于 5000：
        返还 做 计数 用 可用码率列表 减 1。
    完。
    若 缓冲水位 小于 1000：
        返还 0。
    完。
    返还 做 计数 用 可用码率列表 除以 2。
完。

本章 收束。
