《E1000全局状态》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·中枢·字符串替换》。

引用《..·引导·端口IO》。

引用《.·E1000收发初始化》。

引用《.·图处枚举》。

引用《.·输入原语》。

引用《..·中枢·整数转字符串》。

RX_DESC_COUNT 为 256。

TX_DESC_COUNT 为 256。

RX_BUF_SIZE 为 2048。

e1000_初始化 接受 零参：
    e1000设备列表 为 一组：（空）。
    做 内核信息 用 「e1000: 扫描Intel以太网控制器...」。
    可变 总线 为 0。
    只要 总线 小于 256：
        可变 设备号 为 0。
        只要 设备号 小于 32：
            可变 功能号 为 0。
            只要 功能号 小于 8：
                vid_did 为 做 PCI_配置读32 用 总线、设备号、功能号、0x00。
                若 vid_did 等于 0xFFFFFFFF 或者 vid_did 等于 0：
                    功能号 为 功能号 加 1。
                    继续。
                完。
                vid 为 vid_did 位与 0xFFFF。
                did 为 vid_did 右移 16 位与 0xFFFF。
                若 vid 等于 0x8086 并且 做 e1000_是支持设备 用 did：
                    做 内核信息 用 「e1000: 发现 」 加 做 e1000_设备名 用 did 加 「 @PCI 」 加 做 整数转字符串 用 总线 加 「:」 加 做 整数转字符串 用 设备号。
                    cmd 为 做 PCI_配置读32 用 总线、设备号、功能号、0x04。
                    做 PCI_配置写32 用 总线、设备号、功能号、0x04、cmd 位或 0x07。
                    irq_info 为 做 PCI_配置读32 用 总线、设备号、功能号、0x3C。
                    irq 为 irq_info 位与 0xFF。
                    bar0 为 做 PCI_读BAR 用 总线、设备号、功能号、0。
                    做 e1000_初始化设备 用 bar0、irq。
                完。
                功能号 为 功能号 加 1。
            完。
            设备号 为 设备号 加 1。
        完。
        总线 为 总线 加 1。
    完。
    做 内核信息 用 「e1000: 初始化完成 网卡=」 加 做 整数转字符串 用 做 计数 用 e1000设备列表。
完。

e1000_是支持设备 接受 did：
    返还 did 等于 0x100E 或者 did 等于 0x100F 或者 did 等于 0x10D3 或者 did 等于 0x153A 或者 did 等于 0x15A1 或者 did 等于 0x15B8 或者 did 等于 0x15BE 或者 did 等于 0x15D7 或者 did 等于 0x0D4F 或者 did 等于 0x1A1E。
完。

e1000_设备名 接受 did：
    若 did 等于 0x100E：
        返还 「82540EM (QEMU)」。
    完。
    若 did 等于 0x10D3：
        返还 「82574L」。
    完。
    若 did 等于 0x153A：
        返还 「I217-LM」。
    完。
    若 did 等于 0x15B8：
        返还 「I219-V」。
    完。
    返还 「e1000e 」 加 做 整数转十六进制 用 did。
完。

e1000_初始化设备 接受 bar0、irq：
    dev 为 新建 e1000设备。
    dev 的 MMIO基址 为 bar0。
    dev 的 IRQ 为 irq。
    dev 的 RX环大小 为 RX_DESC_COUNT。
    dev 的 TX环大小 为 TX_DESC_COUNT。
    dev 的 RX缓冲列表 为 一组：（空）。
    dev 的 TX缓冲列表 为 一组：（空）。
    做 e1000_写 用 dev、E1000_CTRL、E1000_CTRL_RST。
    做 延迟_ms 用 10。
    做 e1000_写 用 dev、E1000_IMC、0xFFFFFFFF。
    dev 的 MAC地址 为 做 e1000_读MAC 用 dev。
    做 内核信息 用 「e1000: MAC=」 加 做 MAC转字符串 用 dev 的 MAC地址。
    ctrl 为 做 e1000_读 用 dev、E1000_CTRL。
    ctrl 为 ctrl 位或 E1000_CTRL_SLU 位或 E1000_CTRL_ASDE。
    ctrl 为 ctrl 位与 按位取反 E1000_CTRL_RST。
    做 e1000_写 用 dev、E1000_CTRL、ctrl。
    可变 i 为 0。
    只要 i 小于 128：
        做 e1000_写 用 dev、E1000_MTA 加 i 乘 4、0。
        i 为 i 加 1。
    完。
    做 e1000_初始化RX 用 dev。
    做 e1000_初始化TX 用 dev。
    做 e1000_写 用 dev、E1000_IMS、E1000_ICR_RXT0 位或 E1000_ICR_LSC 位或 E1000_ICR_TXDW。
    status 为 做 e1000_读 用 dev、E1000_STATUS。
    dev 的 链路状态 为 status 位与 0x02 不等于 0。
    若 status 位与 0xC0 等于 0xC0：
        dev 的 链路速度 为 1000。
    否则若 status 位与 0xC0 等于 0x80：
        dev 的 链路速度 为 100。
    否则：
        dev 的 链路速度 为 10。
    完。
    做 内核信息 用 「e1000: 链路=」 加 若 dev 的 链路状态 则 「UP」 否则 「DOWN」 加 「 速度=」 加 做 整数转字符串 用 dev 的 链路速度 加 「Mbps」。
    做 网络设备_注册 用 「eth0」、dev。
    做 追加 用 e1000设备列表、dev。
完。

e1000_读MAC 接受 dev：
    可变 mac 为 一组：0、0、0、0、0、0。
    低 为 做 e1000_读 用 dev、0x5400。
    高 为 做 e1000_读 用 dev、0x5404。
    mac 的 第 0 项 为 低 位与 0xFF。
    mac 的 第 1 项 为 低 右移 8 位与 0xFF。
    mac 的 第 2 项 为 低 右移 16 位与 0xFF。
    mac 的 第 3 项 为 低 右移 24 位与 0xFF。
    mac 的 第 4 项 为 高 位与 0xFF。
    mac 的 第 5 项 为 高 右移 8 位与 0xFF。
    返还 mac。
完。

MAC转字符串 接受 mac：
    返还 做 十六进制2 用 mac 的 第 0 项 加 「:」 加 做 十六进制2 用 mac 的 第 1 项 加 「:」 加 做 十六进制2 用 mac 的 第 2 项 加 「:」 加 做 十六进制2 用 mac 的 第 3 项 加 「:」 加 做 十六进制2 用 mac 的 第 4 项 加 「:」 加 做 十六进制2 用 mac 的 第 5 项。
完。

十六进制2 接受 值：
    高 为 值 右移 4 位与 0xF。
    低 为 值 位与 0xF。
    字符表 为 「0123456789abcdef」。
    返还 做 字符 用 字符表 的 第 高 项 加 做 字符 用 字符表 的 第 低 项。
完。

e1000_检查链路 接受 dev：
    状态 为 做 e1000_读 用 dev、E1000_STATUS。
    链路 为 状态 位与 0x02 不等于 0。
    若 链路 不等于 dev 的 链路状态：
        dev 的 链路状态 为 链路。
        若 链路：
            速度位 为 状态 右移 6 位与 0x03。
            若 速度位 等于 0：
                dev 的 链路速度 为 10。
            否则若 速度位 等于 1：
                dev 的 链路速度 为 100。
            否则：
                dev 的 链路速度 为 1000。
            完。
            做 内核信息 用 「e1000: 链路UP 」 加 做 整数转字符串 用 dev 的 链路速度 加 「Mbps」。
        否则：
            做 内核信息 用 「e1000: 链路DOWN」。
        完。
    完。
完。

e1000_EEPROM读 接受 dev、偏移：
    做 e1000_写 用 dev、E1000_EERD、偏移 左移 8 位或 1。
    可变 等待 为 0。
    只要 等待 小于 10000：
        值 为 做 e1000_读 用 dev、E1000_EERD。
        若 值 位与 0x10 不等于 0：
            返还 值 右移 16 位与 0xFFFF。
        完。
        等待 为 等待 加 1。
    完。
    返还 0。
完。

e1000_清除多播表 接受 dev：
    可变 i 为 0。
    只要 i 小于 128：
        做 e1000_写 用 dev、0x5200 加 i 乘 4、0。
        i 为 i 加 1。
    完。
完。

e1000_设置混杂模式 接受 dev、启用：
    rctl 为 做 e1000_读 用 dev、E1000_RCTL。
    若 启用：
        rctl 为 rctl 位或 1 左移 3 位或 1 左移 4。
    否则：
        rctl 为 rctl 位与 按位取反 (1 左移 3 位或 1 左移 4)。
    完。
    做 e1000_写 用 dev、E1000_RCTL、rctl。
完。

E1000_CTRL_SLU 为 0x40。

E1000_CTRL_ASDE 为 0x20。

E1000_CTRL_RST 为 0x04000000。

E1000_STATUS 为 0x0008。

E1000_CTRL 为 0x0000。

E1000_RCTL 为 0x0100。

E1000_EERD 为 0x0014。

可变 e1000设备列表 为 一组：（空）。

e1000_读 接受 dev、寄存器：
    返还 做 内存读32 用 dev 的 MMIO基址 加 寄存器。
完。

e1000_写 接受 dev、寄存器、值：
    做 内存写32 用 dev 的 MMIO基址 加 寄存器、值。
完。

网络设备_注册 接受 名称、dev：
    若 dev 等于 空：
        返还 负 1。
    完。
    dev 的 名称 为 名称。
    做 内核信息 用 「网络设备: 注册 」 加 名称。
    返还 0。
完。

e1000_全局统计 接受 零参：
    返还 一本字典：设备数 对应 做 计数 用 e1000设备列表。
完。

e1000_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 做 计数 用 e1000设备列表 等于 0：
        做 追加 用 问题、「无e1000网卡」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「e1000健康」。
    完。
    返还 问题。
完。

e1000_获取设备 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 做 计数 用 e1000设备列表：
        返还 空。
    完。
    返还 e1000设备列表 的 第 索引 项。
完。

e1000_设备数 接受 零参：
    返还 做 计数 用 e1000设备列表。
完。

e1000_链接状态 接受 dev：
    若 dev 等于 空：
        返还 假。
    完。
    返还 dev 的 链路状态。
完。

e1000_MAC地址字符串 接受 dev：
    若 dev 等于 空：
        返还 「00:00:00:00:00:00」。
    完。
    返还 做 MAC转字符串 用 dev 的 MAC地址。
完。

E1000_REG_STATUS 为 0x0008。

E1000_REG_CTRL 为 0x0000。

E1000_STATUS_LU 为 0x02。

E1000_REG_RCTL 为 0x0100。

E1000_REG_TCTL 为 0x0400。

E1000_REG_RDBAL 为 0x2800。

E1000_REG_TDBAL 为 0x3800。

e1000_重置统计 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 e1000设备列表：
        e1000设备列表 的 第 i 项 的 接收包数 为 0。
        e1000设备列表 的 第 i 项 的 发送包数 为 0。
        i 为 i 加 1。
    完。
完。

可变 AI_全局_带宽估计 为 0。

可变 AI_全局_丢包窗口 为 一组：（空）。

可变 AI_全局_链路质量 为 1000。

AI_全局_带宽预测 接受 发送字节、耗时毫秒：
    若 耗时毫秒 等于 0：
        返还 AI_全局_带宽估计。
    完。
    当前 为 发送字节 乘 1000 除以 耗时毫秒。
    AI_全局_带宽估计 为 AI_全局_带宽估计 乘 700 除以 1000 加 当前 乘 300 除以 1000。
    返还 AI_全局_带宽估计。
完。

AI_全局_拥塞建议 接受 丢包率：
    做 追加 用 AI_全局_丢包窗口、丢包率。
    若 做 计数 用 AI_全局_丢包窗口 大于 10：
        做 列表_移除 用 AI_全局_丢包窗口、0。
    完。
    若 丢包率 大于 50：
        返还 「减半窗口」。
    完。
    若 丢包率 小于 5：
        返还 「增大窗口」。
    完。
    返还 「保持」。
完。

AI_全局_链路质量评估 接受 零参：
    可变 总收 为 0。
    可变 总发 为 0。
    可变 链路异常数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 e1000设备列表：
        dev 为 e1000设备列表 的 第 i 项。
        总收 为 总收 加 dev 的 接收包数。
        总发 为 总发 加 dev 的 发送包数。
        若 非 dev 的 链路状态：
            链路异常数 为 链路异常数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 e1000设备列表 等于 0：
        AI_全局_链路质量 为 0。
        返还 0。
    完。
    AI_全局_链路质量 为 (做 计数 用 e1000设备列表 减 链路异常数) 乘 1000 除以 做 计数 用 e1000设备列表。
    返还 AI_全局_链路质量。
完。

AI_全局_网络诊断 接受 零参：
    质量 为 做 AI_全局_链路质量评估。
    若 做 计数 用 e1000设备列表 等于 0：
        返还 「无网卡设备」。
    完。
    若 质量 等于 0：
        返还 「所有链路断开，检查物理连接」。
    完。
    若 质量 小于 500：
        返还 「多数链路DOWN，建议重置网卡」。
    完。
    若 AI_全局_带宽估计 大于 0 并且 AI_全局_带宽估计 小于 1000：
        返还 「带宽低于预期，可能拥塞」。
    完。
    返还 「网络状态良好」。
完。

本章 收束。
