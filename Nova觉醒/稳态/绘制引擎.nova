《绘制引擎》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《.·字体写字形》。

引用《.·原生样式系统》。

引用《.·图层合成Alpha混合》。

绘制_元素树 接受 元素、fb：
    若 元素 等于 空 或者 非 元素 的 可见：
        返还。
    完。
    s 为 元素 的 样式。
    若 s 等于 空：
        s 为 做 样式_默认 用 元素 的 控件类型。
    完。
    若 s 的 背景色 不等于 0x00000000：
        做 像素_填充矩形 用 fb、元素 的 布局_x、元素 的 布局_y、元素 的 布局_宽、元素 的 布局_高、s 的 背景色。
    完。
    若 s 的 边框宽 大于 0：
        做 绘制_边框 用 fb、元素 的 布局_x、元素 的 布局_y、元素 的 布局_宽、元素 的 布局_高、s 的 边框色、s 的 边框宽。
    完。
    若 元素 的 类型 等于 元素类型_文本 并且 元素 的 文本内容 不等于 「」：
        做 绘制_文本 用 fb、元素 的 布局_x、元素 的 布局_y、元素 的 文本内容、s 的 前景色、s 的 字号。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 元素 的 子元素：
        做 绘制_元素树 用 元素 的 子元素 的 第 i 项、fb。
        i 为 i 加 1。
    完。
完。

绘制_边框 接受 fb、x、y、w、h、颜色、宽度：
    做 像素_填充矩形 用 fb、x、y、w、宽度、颜色。
    做 像素_填充矩形 用 fb、x、y 加 h 减 宽度、w、宽度、颜色。
    做 像素_填充矩形 用 fb、x、y、宽度、h、颜色。
    做 像素_填充矩形 用 fb、x 加 w 减 宽度、y、宽度、h、颜色。
完。

绘制_文本 接受 fb、x、y、文本、颜色、字号：
    缩放 为 字号 除以 16。
    若 缩放 小于 1：
        缩放 为 1。
    完。
    做 字体_渲染文本_透明 用 fb、x、y、文本、颜色、缩放。
完。

绘制_字符 接受 fb、x、y、字符码、颜色、字号：
    缩放 为 字号 除以 16。
    若 缩放 小于 1：
        缩放 为 1。
    完。
    做 字体_渲染字符_透明 用 fb、x、y、字符码、颜色、缩放。
完。

无障碍节点 是 一种 事物，含 节点ID、角色、名称、值、x、y、宽、高、可聚焦、已禁用、子节点。

构建无障碍树 接受 元素：
    若 元素 等于 空 或者 非 元素 的 可见：
        返还 空。
    完。
    a11y 为 新建 无障碍节点。
    a11y 的 节点ID 为 元素 的 元素ID。
    a11y 的 角色 为 元素 的 语义角色。
    a11y 的 x 为 元素 的 布局_x。
    a11y 的 y 为 元素 的 布局_y。
    a11y 的 宽 为 元素 的 布局_宽。
    a11y 的 高 为 元素 的 布局_高。
    a11y 的 可聚焦 为 元素 的 可交互。
    a11y 的 已禁用 为 假。
    a11y 的 子节点 为 一组：（空）。
    若 元素 的 类型 等于 元素类型_文本：
        a11y 的 名称 为 元素 的 文本内容。
    否则：
        a11y 的 名称 为 做 元素_提取文本 用 元素。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 元素 的 子元素：
        子a11y 为 做 构建无障碍树 用 元素 的 子元素 的 第 i 项。
        若 子a11y 不等于 空：
            做 追加 用 a11y 的 子节点、子a11y。
        完。
        i 为 i 加 1。
    完。
    返还 a11y。
完。

元素_提取文本 接受 元素：
    若 元素 等于 空：
        返还 「」。
    完。
    若 元素 的 类型 等于 元素类型_文本：
        返还 元素 的 文本内容。
    完。
    可变 文本 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 元素 的 子元素：
        文本 为 文本 加 做 元素_提取文本 用 元素 的 子元素 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 文本。
完。

无障碍_查找可交互元素 接受 a11y：
    可变 结果 为 一组：（空）。
    做 无障碍_收集交互元素 用 a11y、结果。
    返还 结果。
完。

无障碍_收集交互元素 接受 节点、结果：
    若 节点 等于 空：
        返还。
    完。
    若 节点 的 可聚焦 并且 非 节点 的 已禁用：
        做 追加 用 结果、节点。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点 的 子节点：
        做 无障碍_收集交互元素 用 节点 的 子节点 的 第 i 项、结果。
        i 为 i 加 1。
    完。
完。

无障碍_按坐标查找 接受 a11y、x、y：
    若 a11y 等于 空：
        返还 空。
    完。
    若 x 小于 a11y 的 x 或者 x 大于或等于 a11y 的 x 加 a11y 的 宽 或者 y 小于 a11y 的 y 或者 y 大于或等于 a11y 的 y 加 a11y 的 高：
        返还 空。
    完。
    可变 i 为 做 计数 用 a11y 的 子节点 减 1。
    只要 i 大于或等于 0：
        命中 为 做 无障碍_按坐标查找 用 a11y 的 子节点 的 第 i 项、x、y。
        若 命中 不等于 空：
            返还 命中。
        完。
        i 为 i 减 1。
    完。
    若 a11y 的 可聚焦：
        返还 a11y。
    完。
    返还 空。
完。

本章 收束。
