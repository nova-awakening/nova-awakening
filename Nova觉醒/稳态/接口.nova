《接口》

引用《.·窗口管理》。

引用《.·键盘》。

引用《.·AMD_GPU驱动》。

引用《.·音频常量》。

引用《.·图处显示》。

引用《.·驱动注册》。

稳态_初始化 接受 零参：
    做 驱动_全局初始化。
    做 显示_初始化。
    返还 0。
完。

稳态_创建窗口 接受 宽、高、标题：
    返还 做 窗口_创建 用 宽、高、标题。
完。

稳态_刷新显示 接受 零参：
    做 显示_刷新帧缓冲。
完。

稳态_设置像素 接受 横、纵、颜色：
    做 帧缓冲_写像素 用 横、纵、颜色。
完。

稳态_读取键盘 接受 零参：
    返还 做 键盘_获取按键。
完。

稳态_读取鼠标 接受 零参：
    返还 做 鼠标_获取状态。
完。

稳态_播放音频 接受 数据、采样率：
    做 音频_播放缓冲 用 数据、采样率。
完。

稳态_注册驱动 接受 驱动描述：
    返还 做 驱动_注册 用 驱动描述。
完。

可变 稳态_调用计数 为 0。

可变 稳态_最近总线消息 为 空。

稳态_安全入口 接受 调用者、操作码：
    稳态_调用计数 为 稳态_调用计数 加 1。
    若 调用者 小于 0 或者 调用者 大于 8：
        返还 负 1。
    完。
    返还 0。
完。

稳态_处理总线消息 接受 调用者、操作码、数据：
    稳态_最近总线消息 为 一本字典：调用者 对应 调用者、操作码 对应 操作码、数据 对应 数据。
    返还 0。
完。

引用《.·音频编解码枚举》。

引用《.·标记语言解析器》。

引用《.·NML标记语解析》。

引用《.·GPU驱动辅助》。

引用《.·NVIDIA_MMIO寄存器》。

引用《.·TTY终端层》。

引用《.·常量》。

引用《.·NVIDIA引擎初始化》。

引用《.·串口》。

引用《.·窗口合成器》。

引用《.·NVMe_IO队列》。

引用《.·NML词法分析》。

引用《.·AHCI寄存器》。

引用《.·AHCI驱动辅助》。

引用《.·AHCI驱动全局》。

引用《.·AHCI统计》。

引用《.·Intel图形寄存器》。

引用《.·ACPI表签名》。

引用《.·ACPI子系统》。

引用《.·AHCI端口初始化》。

引用《.·分区表》。

引用《.·矢量读取辅助》。

引用《.·管理命令》。

引用《.·光栅化扫描》。

引用《.·绘制引擎》。

引用《.·AHCI集成驱动》。

引用《.·E1000E寄存器》。

引用《.·E1000收发初始化》。

引用《.·ACPI电源管理》。

引用《.·界面元素渲染》。

引用《.·NVMe寄存器》。

引用《.·NVMe驱动辅助》。

引用《.·NVMe驱动》。

引用《.·蓝牙框架》。

引用《.·粒子系统》。

引用《.·WiFi连接》。

引用《.·PCM音频流》。

引用《.·AMD_PM4命令格式》。

引用《.·AMD_PM4命令提交》。

引用《.·位图字体ASCII》。

引用《.·图处能力探测》。

引用《.·设备驱动模型》。

引用《.·驱动匹配设备》。

引用《.·驱动探测全部》。

引用《.·E1000全局状态》。

引用《.·软件混音器》。

引用《.·散热管理》。

引用《.·设备节点》。

引用《.·设备枚举》。

引用《.·ACPI设备树》。

引用《.·事件分发》。

引用《.·输入原语》。

引用《.·鼠标》。

引用《.·数学基础向量》。

引用《.·AMD初始化》。

引用《.·随机数》。

引用《.·通串初始化》。

引用《.·通串存储》。

引用《.·USB控制传输》。

引用《.·图处统一操作》。

引用《.·图层合成Alpha混合》。

引用《.·图处复位》。

引用《.·图处枚举》。

引用《.·图处内存管理》。

引用《.·GPU驱动初始化》。

引用《.·图处设备创建》。

引用《.·图处识别查询》。

引用《.·图处统一初始化》。

引用《.·NVIDIA推送缓冲》。

引用《.·图处型号表》。

引用《.·网格与骨骼》。

引用《.·位图字体》。

引用《.·AMD中断处理》。

引用《.·特殊设备文件》。

引用《.·PTY伪终端》。

引用《.·WiFi辅助》。

引用《.·WiFi全局状态》。

引用《.·显基设置颜》。

引用《.·显基写十六进》。

引用《.·DRM显示管理》。

引用《.·显示管理控》。

引用《.·显示管理模》。

引用《.·显示管理项控》。

引用《.·显示模式设》。

引用《.·写寄存器》。

引用《.·信息打印》。

引用《.·virtio块初始化》。

引用《.·virtio块读写》。

引用《.·virtio块设备》。

引用《.·virtio块中断》。

引用《.·virtio通用》。

引用《.·virtio网络》。

引用《.·渲染管线》。

引用《.·渲染管线双》。

引用《.·样式内联样》。

引用《.·音频框架全局》。

引用《.·音频统计》。

引用《.·音频音频》。

引用《.·NVIDIA引擎复位》。

引用《.·NML语法分析》。

引用《.·AMD图形驱动》。

引用《.·GPU命令队列驱动》。

引用《.·PCI总线驱动》。

引用《.·原生样式系统》。

引用《.·原生元素树》。

引用《.·帧缓冲复制区》。

引用《.·帧缓冲》。

引用《.·帧率统计》。

引用《.·帧收发》。

引用《.·帧提交双缓冲》。

引用《.·TTF文件解析》。

引用《.·Nova音频流控制》。

引用《.·AI磁盘预取》。

引用《.·AI设备感知》。

引用《.·中断处理》。

引用《.·中文支持框》。

引用《.·状态打印》。

引用《.·字体写字形》。

引用《.·TTF字形缓存LRU》。

引用《.·PCI查找设备》。

引用《.·PCI配置读》。

引用《.·总线解析》。

引用《.·总线注册》。

引用《..·中枢·接口》。

稳态接口_统计 接受 零参：
    返还 一本字典：模块 对应 「稳态接口」、脑区 对应 「稳态」。
完。

稳态接口_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 稳态_调用计数 大于 100000：
        做 追加 用 问题、「稳态_调用计数异常增长」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

稳态接口_综合报告 接受 零参：
    做 打印 用 「[稳态接口] 报告」。
    做 打印 用 「  脑区: 稳态」。
    做 打印 用 「  健康: 」 加 若 做 稳态接口_健康评估 则 「正常」 否则 「异常」。
完。

稳态接口_全局重置 接受 零参：
    稳态_调用计数 为 0。
    稳态_最近总线消息 为 空。
    做 打印 用 「[稳态接口] 全局统计已重置」。
    返还 0。
完。

本章 收束。
