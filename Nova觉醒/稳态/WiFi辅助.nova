《WiFi辅助》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·中断表注册》。

引用《..·中枢·块内存分配器》。

引用《..·引导·端口IO》。

引用《..·外联·IP接收》。

引用《..·中枢·内核打印》。

引用《..·外联·帧填充校验》。

引用《..·中枢·整数转字符串》。

WIFI_BAND_2G 为 0。

WIFI_BAND_5G 为 1。

WIFI_BAND_6G 为 2。

WIFI_BAND_未知 为 负 1。

WIFI_漫游阈值 为 负 70。

WIFI_漫游增益最小 为 8。

BSS_老化超时 为 30000。

可变 wifi_设备查找数 为 0。

可变 wifi_BSS查找数 为 0。

可变 wifi_漫游评估数 为 0。

可变 wifi_漫游触发数 为 0。

可变 wifi_帧接收数 为 0。

可变 wifi_帧丢弃数 为 0。

WiFi_查找设备 接受 名称：
    wifi_设备查找数 为 wifi_设备查找数 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 WiFi设备列表：
        若 WiFi设备列表 的 第 i 项 的 名称 等于 名称：
            返还 WiFi设备列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

WiFi_设备数 接受 零参：
    返还 做 计数 用 WiFi设备列表。
完。

WiFi_获取设备 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 做 计数 用 WiFi设备列表：
        返还 空。
    完。
    返还 WiFi设备列表 的 第 索引 项。
完。

WiFi_查找BSS 接受 dev、ssid：
    wifi_BSS查找数 为 wifi_BSS查找数 加 1。
    可变 最强 为 空。
    可变 最强信号 为 负 200。
    可变 i 为 0。
    只要 i 小于 做 计数 用 dev 的 扫描结果：
        若 dev 的 扫描结果 的 第 i 项 的 SSID 等于 ssid：
            若 dev 的 扫描结果 的 第 i 项 的 信号强度 大于 最强信号：
                最强 为 dev 的 扫描结果 的 第 i 项。
                最强信号 为 dev 的 扫描结果 的 第 i 项 的 信号强度。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 最强。
完。

WiFi_查找BSS_BSSID 接受 dev、bssid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 dev 的 扫描结果：
        若 做 MAC相同 用 dev 的 扫描结果 的 第 i 项 的 BSSID、bssid：
            返还 dev 的 扫描结果 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

WiFi_BSS数量 接受 dev、ssid：
    可变 数量 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 dev 的 扫描结果：
        若 dev 的 扫描结果 的 第 i 项 的 SSID 等于 ssid：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

WiFi_清理过期BSS 接受 dev、当前时间：
    可变 i 为 做 计数 用 dev 的 扫描结果 减 1。
    只要 i 大于或等于 0：
        若 当前时间 减 dev 的 扫描结果 的 第 i 项 的 最后更新时间 大于 BSS_老化超时：
            做 删除 用 dev 的 扫描结果、i。
        完。
        i 为 i 减 1。
    完。
完。

WiFi_频率转频道 接受 频率：
    若 频率 大于或等于 2412 并且 频率 小于或等于 2472：
        返还 (频率 减 2407) 除以 5。
    完。
    若 频率 等于 2484：
        返还 14。
    完。
    若 频率 大于或等于 5180 并且 频率 小于或等于 5825：
        返还 (频率 减 5000) 除以 5。
    完。
    若 频率 大于或等于 5955 并且 频率 小于或等于 7115：
        返还 (频率 减 5950) 除以 5。
    完。
    返还 0。
完。

WiFi_频道转频率 接受 频道、频段：
    若 频段 等于 WIFI_BAND_2G：
        若 频道 等于 14：
            返还 2484。
        完。
        若 频道 大于或等于 1 并且 频道 小于或等于 13：
            返还 2407 加 频道 乘 5。
        完。
    完。
    若 频段 等于 WIFI_BAND_5G：
        返还 5000 加 频道 乘 5。
    完。
    若 频段 等于 WIFI_BAND_6G：
        返还 5950 加 频道 乘 5。
    完。
    返还 0。
完。

WiFi_频率转频段 接受 频率：
    若 频率 大于或等于 2400 并且 频率 小于或等于 2500：
        返还 WIFI_BAND_2G。
    完。
    若 频率 大于或等于 5150 并且 频率 小于或等于 5850：
        返还 WIFI_BAND_5G。
    完。
    若 频率 大于或等于 5925 并且 频率 小于或等于 7125：
        返还 WIFI_BAND_6G。
    完。
    返还 WIFI_BAND_未知。
完。

WiFi_频段名 接受 频段：
    若 频段 等于 WIFI_BAND_2G：
        返还 「2.4GHz」。
    完。
    若 频段 等于 WIFI_BAND_5G：
        返还 「5GHz」。
    完。
    若 频段 等于 WIFI_BAND_6G：
        返还 「6GHz」。
    完。
    返还 「未知」。
完。

WiFi_信号质量 接受 rssi：
    若 rssi 大于或等于 负 50：
        返还 100。
    完。
    若 rssi 小于或等于 负 100：
        返还 0。
    完。
    返还 (rssi 加 100) 乘 2。
完。

WiFi_信号等级 接受 rssi：
    若 rssi 大于或等于 负 55：
        返还 4。
    完。
    若 rssi 大于或等于 负 65：
        返还 3。
    完。
    若 rssi 大于或等于 负 75：
        返还 2。
    完。
    若 rssi 大于或等于 负 85：
        返还 1。
    完。
    返还 0。
完。

WiFi_信号描述 接受 rssi：
    若 rssi 大于或等于 负 50：
        返还 「极好」。
    完。
    若 rssi 大于或等于 负 60：
        返还 「很好」。
    完。
    若 rssi 大于或等于 负 70：
        返还 「良好」。
    完。
    若 rssi 大于或等于 负 80：
        返还 「一般」。
    完。
    若 rssi 大于或等于 负 90：
        返还 「较弱」。
    完。
    返还 「极弱」。
完。

WiFi_加密名 接受 类型：
    若 类型 等于 WIFI_CIPHER_NONE：
        返还 「Open」。
    完。
    若 类型 等于 WIFI_CIPHER_WEP：
        返还 「WEP」。
    完。
    若 类型 等于 WIFI_CIPHER_TKIP：
        返还 「WPA-TKIP」。
    完。
    若 类型 等于 WIFI_CIPHER_CCMP：
        返还 「WPA2-AES」。
    完。
    若 类型 等于 WIFI_CIPHER_GCMP_256：
        返还 「WPA3」。
    完。
    若 类型 等于 WIFI_CIPHER_CCMP_256：
        返还 「WPA3-AES256」。
    完。
    返还 「Unknown」。
完。

WiFi_加密安全等级 接受 类型：
    若 类型 等于 WIFI_CIPHER_NONE：
        返还 0。
    完。
    若 类型 等于 WIFI_CIPHER_WEP：
        返还 1。
    完。
    若 类型 等于 WIFI_CIPHER_TKIP：
        返还 2。
    完。
    若 类型 等于 WIFI_CIPHER_CCMP：
        返还 3。
    完。
    若 类型 等于 WIFI_CIPHER_GCMP_256：
        返还 3。
    完。
    返还 0。
完。

WiFi_漫游评分 接受 bss：
    可变 分数 为 做 WiFi_信号质量 用 bss 的 信号强度。
    频段 为 做 WiFi_频率转频段 用 bss 的 频率。
    若 频段 等于 WIFI_BAND_5G：
        分数 为 分数 加 10。
    完。
    若 频段 等于 WIFI_BAND_6G：
        分数 为 分数 加 15。
    完。
    分数 为 分数 加 做 WiFi_加密安全等级 用 bss 的 加密类型 乘 3。
    若 分数 大于 100：
        分数 为 100。
    完。
    返还 分数。
完。

WiFi_应该漫游 接受 dev、ssid：
    wifi_漫游评估数 为 wifi_漫游评估数 加 1。
    若 dev 的 当前信号 大于 WIFI_漫游阈值：
        返还 假。
    完。
    可变 最佳 为 空。
    可变 最佳分数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 dev 的 扫描结果：
        若 dev 的 扫描结果 的 第 i 项 的 SSID 等于 ssid 并且 非 做 MAC相同 用 dev 的 扫描结果 的 第 i 项 的 BSSID、dev 的 当前BSSID：
            分数 为 做 WiFi_漫游评分 用 dev 的 扫描结果 的 第 i 项。
            若 分数 大于 最佳分数：
                最佳 为 dev 的 扫描结果 的 第 i 项。
                最佳分数 为 分数。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最佳 等于 空：
        返还 假。
    完。
    若 最佳 的 信号强度 减 dev 的 当前信号 大于或等于 WIFI_漫游增益最小：
        wifi_漫游触发数 为 wifi_漫游触发数 加 1。
        返还 真。
    完。
    返还 假。
完。

MAC相同 接受 a、b：
    若 做 计数 用 a 小于 6 或者 做 计数 用 b 小于 6：
        返还 假。
    完。
    返还 a 的 第 0 项 等于 b 的 第 0 项 并且 a 的 第 1 项 等于 b 的 第 1 项 并且 a 的 第 2 项 等于 b 的 第 2 项 并且 a 的 第 3 项 等于 b 的 第 3 项 并且 a 的 第 4 项 等于 b 的 第 4 项 并且 a 的 第 5 项 等于 b 的 第 5 项。
完。

MAC转字符串 接受 mac：
    若 做 计数 用 mac 小于 6：
        返还 「??:??:??:??:??:??」。
    完。
    返还 做 字节转十六进制 用 mac 的 第 0 项 加 「:」 加 做 字节转十六进制 用 mac 的 第 1 项 加 「:」 加 做 字节转十六进制 用 mac 的 第 2 项 加 「:」 加 做 字节转十六进制 用 mac 的 第 3 项 加 「:」 加 做 字节转十六进制 用 mac 的 第 4 项 加 「:」 加 做 字节转十六进制 用 mac 的 第 5 项。
完。

MAC_是广播 接受 mac：
    返还 mac 的 第 0 项 等于 0xFF 并且 mac 的 第 1 项 等于 0xFF 并且 mac 的 第 2 项 等于 0xFF 并且 mac 的 第 3 项 等于 0xFF 并且 mac 的 第 4 项 等于 0xFF 并且 mac 的 第 5 项 等于 0xFF。
完。

MAC_是组播 接受 mac：
    返还 mac 的 第 0 项 位与 0x01 不等于 0。
完。

MAC_是零 接受 mac：
    返还 mac 的 第 0 项 等于 0 并且 mac 的 第 1 项 等于 0 并且 mac 的 第 2 项 等于 0 并且 mac 的 第 3 项 等于 0 并且 mac 的 第 4 项 等于 0 并且 mac 的 第 5 项 等于 0。
完。

字节转十六进制 接受 b：
    高 为 「0123456789abcdef」。
    返还 做 字符串_取字符 用 高、b 右移 4 位与 0xF 加 做 字符串_取字符 用 高、b 位与 0xF。
完。

释放内存 接受 地址：
    做 kfree 用 地址。
完。

网络_接收帧 接受 数据、长度：
    wifi_帧接收数 为 wifi_帧接收数 加 1。
    若 数据 等于 0 或者 长度 小于 14：
        wifi_帧丢弃数 为 wifi_帧丢弃数 加 1。
        返还。
    完。
    类型 为 做 内存读8 用 数据 加 12 左移 8 位或 做 内存读8 用 数据 加 13。
    若 类型 等于 0x0800：
        做 IP_接收 用 数据 加 14、长度 减 14。
    否则若 类型 等于 0x0806：
        做 ARP_接收 用 数据、长度。
    否则若 类型 等于 0x86DD：
        做 IPv6_接收 用 数据 加 14、长度 减 14。
    否则若 类型 等于 0x888E：
        做 EAPOL_接收 用 数据 加 14、长度 减 14。
    否则：
        wifi_帧丢弃数 为 wifi_帧丢弃数 加 1。
    完。
完。

DHCP_请求 接受 dev_name：
    做 内核信息 用 「WiFi: 请求DHCP 」 加 dev_name。
完。

错误_设备不存在 为 负 19。

错误_设备忙 为 负 16。

错误_网络不存在 为 负 113。

错误_认证失败 为 负 111。

错误_未连接 为 负 107。

错误_ENOSYS 为 负 38。

错误_超时 为 负 110。

错误_信道忙 为 负 114。

WiFi_错误描述 接受 错误码：
    若 错误码 等于 0：
        返还 「成功」。
    完。
    若 错误码 等于 错误_设备不存在：
        返还 「设备不存在」。
    完。
    若 错误码 等于 错误_设备忙：
        返还 「设备忙」。
    完。
    若 错误码 等于 错误_网络不存在：
        返还 「网络不存在」。
    完。
    若 错误码 等于 错误_认证失败：
        返还 「认证失败」。
    完。
    若 错误码 等于 错误_未连接：
        返还 「未连接」。
    完。
    若 错误码 等于 错误_超时：
        返还 「超时」。
    完。
    若 错误码 等于 错误_信道忙：
        返还 「信道忙」。
    完。
    返还 「未知错误(」 加 做 整数转字符串 用 错误码 加 「)」。
完。

WiFi_打印BSS 接受 bss：
    若 bss 等于 空：
        返还。
    完。
    做 打印行 用 「SSID: 」 加 bss 的 SSID。
    做 打印行 用 「BSSID: 」 加 做 MAC转字符串 用 bss 的 BSSID。
    做 打印行 用 「频率: 」 加 做 整数转字符串 用 bss 的 频率 加 「 MHz (CH 」 加 做 整数转字符串 用 做 WiFi_频率转频道 用 bss 的 频率 加 「)」。
    做 打印行 用 「频段: 」 加 做 WiFi_频段名 用 做 WiFi_频率转频段 用 bss 的 频率。
    做 打印行 用 「信号: 」 加 做 整数转字符串 用 bss 的 信号强度 加 「 dBm (」 加 做 WiFi_信号描述 用 bss 的 信号强度 加 「)」。
    做 打印行 用 「加密: 」 加 做 WiFi_加密名 用 bss 的 加密类型。
    做 打印行 用 「质量: 」 加 做 整数转字符串 用 做 WiFi_信号质量 用 bss 的 信号强度 加 「%」。
    做 打印行 用 「漫游评分: 」 加 做 整数转字符串 用 做 WiFi_漫游评分 用 bss。
完。

WiFi_统计 接受 零参：
    若 做 计数 用 WiFi设备列表 等于 0：
        返还 一组：0、0、0、0。
    完。
    dev 为 WiFi设备列表 的 第 0 项。
    返还 一组：dev 的 状态、dev 的 发送包数、dev 的 接收包数、做 计数 用 dev 的 扫描结果。
完。

WiFi_打印统计 接受 零参：
    做 打印行 用 「=== WiFi辅助统计 ===」。
    做 打印行 用 「设备数: 」 加 做 整数转字符串 用 做 计数 用 WiFi设备列表。
    做 打印行 用 「设备查找: 」 加 做 整数转字符串 用 wifi_设备查找数。
    做 打印行 用 「BSS查找: 」 加 做 整数转字符串 用 wifi_BSS查找数。
    做 打印行 用 「漫游评估: 」 加 做 整数转字符串 用 wifi_漫游评估数。
    做 打印行 用 「漫游触发: 」 加 做 整数转字符串 用 wifi_漫游触发数。
    做 打印行 用 「帧接收: 」 加 做 整数转字符串 用 wifi_帧接收数。
    做 打印行 用 「帧丢弃: 」 加 做 整数转字符串 用 wifi_帧丢弃数。
    可变 i 为 0。
    只要 i 小于 做 计数 用 WiFi设备列表：
        dev 为 WiFi设备列表 的 第 i 项。
        做 打印行 用 「  设备[」 加 做 整数转字符串 用 i 加 「] 」 加 dev 的 名称 加 「 状态=」 加 做 整数转字符串 用 dev 的 状态 加 「 TX=」 加 做 整数转字符串 用 dev 的 发送包数 加 「 RX=」 加 做 整数转字符串 用 dev 的 接收包数 加 「 BSS=」 加 做 整数转字符串 用 做 计数 用 dev 的 扫描结果。
        i 为 i 加 1。
    完。
完。

WiFi_信号均值 接受 列表、数量：
    若 数量 等于 0：
        返还 0。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 数量：
        总 为 总 加 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总 除以 数量。
完。

WiFi_信号方差 接受 列表、数量：
    若 数量 小于 2：
        返还 0。
    完。
    均值 为 做 WiFi_信号均值 用 列表、数量。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 数量：
        差 为 列表 的 第 i 项 减 均值。
        总 为 总 加 差 乘 差。
        i 为 i 加 1。
    完。
    返还 总 除以 数量。
完。

WiFi_最佳BSS 接受 设备：
    若 设备 等于 空 或者 做 计数 用 设备 的 扫描结果 等于 0：
        返还 空。
    完。
    可变 最佳 为 设备 的 扫描结果 的 第 0 项。
    可变 最高分 为 做 WiFi_漫游评分 用 最佳。
    可变 i 为 1。
    只要 i 小于 做 计数 用 设备 的 扫描结果：
        分数 为 做 WiFi_漫游评分 用 设备 的 扫描结果 的 第 i 项。
        若 分数 大于 最高分：
            最高分 为 分数。
            最佳 为 设备 的 扫描结果 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

WiFi_频道数_2G 为 14。

WiFi_频道数_5G 为 25。

WiFi_信道占用率 接受 设备、信道：
    若 设备 等于 空：
        返还 0。
    完。
    可变 占用 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备 的 扫描结果：
        若 做 WiFi_频率转频道 用 设备 的 扫描结果 的 第 i 项 的 频率 等于 信道：
            占用 为 占用 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 占用。
完。

WiFi_最佳信道 接受 设备：
    若 设备 等于 空：
        返还 1。
    完。
    可变 最佳 为 1。
    可变 最低 为 9999。
    可变 ch 为 1。
    只要 ch 小于或等于 WiFi_频道数_2G：
        占用 为 做 WiFi_信道占用率 用 设备、ch。
        若 占用 小于 最低：
            最低 为 占用。
            最佳 为 ch。
        完。
        ch 为 ch 加 1。
    完。
    返还 最佳。
完。

WiFi_完整统计 接受 零参：
    返还 一组：wifi_设备查找数、wifi_BSS查找数、wifi_漫游评估数、wifi_漫游触发数、wifi_帧接收数、wifi_帧丢弃数、做 计数 用 WiFi设备列表。
完。

WiFi_重置统计 接受 零参：
    wifi_设备查找数 为 0。
    wifi_BSS查找数 为 0。
    wifi_漫游评估数 为 0。
    wifi_漫游触发数 为 0。
    wifi_帧接收数 为 0。
    wifi_帧丢弃数 为 0。
完。

WiFi_获取设备数 接受 零参：
    返还 做 计数 用 WiFi设备列表。
完。

WiFi_获取帧接收数 接受 零参：
    返还 wifi_帧接收数。
完。

WiFi_获取漫游数 接受 零参：
    返还 wifi_漫游触发数。
完。

WiFi_连接质量 接受 设备：
    若 设备 等于 空 或者 设备 的 当前BSS 等于 空：
        返还 0。
    完。
    信号分 为 做 WiFi_信号质量 用 设备 的 当前BSS 的 信号强度 乘 10。
    可变 丢包分 为 0。
    若 设备 的 发送包数 大于 0：
        丢包分 为 1000 减 (设备 的 发送包数 减 设备 的 接收包数) 乘 1000 除以 设备 的 发送包数。
        若 丢包分 小于 0：
            丢包分 为 0。
        完。
    否则：
        丢包分 为 500。
    完。
    返还 (信号分 乘 6 加 丢包分 乘 4) 除以 10。
完。

WiFi_建议频段 接受 设备：
    若 设备 等于 空：
        返还 0。
    完。
    质量 为 做 WiFi_连接质量 用 设备。
    若 质量 大于 700：
        返还 0。
    完。
    若 设备 的 当前BSS 不等于 空：
        当前频段 为 做 WiFi_频率转频段 用 设备 的 当前BSS 的 频率。
        若 当前频段 等于 1：
            返还 2。
        完。
        返还 1。
    完。
    返还 0。
完。

WiFi_信噪比估算 接受 RSSI、噪底：
    若 噪底 等于 0：
        噪底 为 负 95。
    完。
    SNR 为 RSSI 减 噪底。
    若 SNR 小于 0：
        SNR 为 0。
    完。
    返还 SNR。
完。

WiFi_频谱效率 接受 调制阶数、编码率_千分：
    可变 比特 为 0。
    可变 m 为 调制阶数。
    只要 m 大于 1：
        m 为 m 除以 2。
        比特 为 比特 加 1。
    完。
    返还 比特 乘 编码率_千分 除以 1000。
完。

WiFi_获取BSS数 接受 零参：
    返还 wifi_BSS查找数。
完。

WiFi_获取丢弃率 接受 零参：
    若 wifi_帧接收数 等于 0：
        返还 0。
    完。
    返还 wifi_帧丢弃数 乘 1000 除以 wifi_帧接收数。
完。

可变 AI_无线框架辅_运行指标 为 0。

可变 AI_无线框架辅_异常指标 为 0。

AI_无线框架辅_自诊断 接受 零参：
    AI_无线框架辅_运行指标 为 0。
    返还 AI_无线框架辅_运行指标。
完。

AI_无线框架辅_优化建议 接受 零参：
    指标 为 做 AI_无线框架辅_自诊断。
    返还 「无线框架辅助状态正常」。
完。

无线框架辅助_统计 接受 零参：
    返还 一本字典：模块 对应 「无线框架辅助」、wifi_设备查找数 对应 wifi_设备查找数、wifi_BSS查找数 对应 wifi_BSS查找数、wifi_漫游评估数 对应 wifi_漫游评估数、wifi_漫游触发数 对应 wifi_漫游触发数、wifi_帧接收数 对应 wifi_帧接收数、wifi_帧丢弃数 对应 wifi_帧丢弃数、AI_无线框架辅_运行指标 对应 AI_无线框架辅_运行指标、AI_无线框架辅_异常指标 对应 AI_无线框架辅_异常指标。
完。

无线框架辅助_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    返还 做 计数 用 问题 等于 0。
完。

无线框架辅助_综合报告 接受 零参：
    做 打印 用 「[无线框架辅助] ═══ 综合报告 ═══」。
    做 打印 用 「  wifi_设备查找数: 」 加 做 整数转字符串 用 wifi_设备查找数。
    做 打印 用 「  wifi_BSS查找数: 」 加 做 整数转字符串 用 wifi_BSS查找数。
    做 打印 用 「  wifi_漫游评估数: 」 加 做 整数转字符串 用 wifi_漫游评估数。
    做 打印 用 「  wifi_漫游触发数: 」 加 做 整数转字符串 用 wifi_漫游触发数。
    做 打印 用 「  wifi_帧接收数: 」 加 做 整数转字符串 用 wifi_帧接收数。
    做 打印 用 「  wifi_帧丢弃数: 」 加 做 整数转字符串 用 wifi_帧丢弃数。
    做 打印 用 「  AI_无线框架辅_运行指标: 」 加 做 整数转字符串 用 AI_无线框架辅_运行指标。
    做 打印 用 「  AI_无线框架辅_异常指标: 」 加 做 整数转字符串 用 AI_无线框架辅_异常指标。
    做 打印 用 「  健康: 」 加 若 做 无线框架辅助_健康评估 则 「正常」 否则 「异常」。
完。

无线框架辅助_全局重置 接受 零参：
    wifi_设备查找数 为 0。
    wifi_BSS查找数 为 0。
    wifi_漫游评估数 为 0。
    wifi_漫游触发数 为 0。
    wifi_帧接收数 为 0。
    wifi_帧丢弃数 为 0。
    AI_无线框架辅_运行指标 为 0。
    AI_无线框架辅_异常指标 为 0。
    做 打印 用 「[无线框架辅助] 全局统计已重置」。
    返还 0。
完。

本章 收束。
