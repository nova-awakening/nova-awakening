《窗口合成器》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·中枢·整数转字符串》。

外部。

VESA_查询最佳模式。

外部。

GOP_获取模式信息。

外部。

kmalloc。

外部。

kfree。

最大窗口数 为 128。

最大图层数 为 8。

窗口状态_正常 为 0。

窗口状态_最小化 为 1。

窗口状态_最大化 为 2。

窗口状态_全屏 为 3。

窗口状态_隐藏 为 4。

图层_桌面 为 0。

图层_普通 为 1。

图层_浮动 为 2。

图层_通知 为 3。

图层_顶层 为 4。

图层_全屏 为 5。

图层_覆盖 为 6。

图层_鼠标 为 7。

窗口 是 一种 事物，含 窗口ID、进程ID、标题哈希、x、y、宽、高、最小宽、最小高、状态、图层、Z序、透明度、可见、有焦点、缓冲地址、缓冲大小、脏、装饰_启用。

可变 所有窗口 为 一组：（空）。

可变 焦点窗口ID 为 0。

可变 下一个窗口ID 为 1。

可变 合成器_已初始化 为 假。

可变 屏幕宽 为 1920。

可变 屏幕高 为 1080。

屏幕_设置分辨率 接受 宽、高、位深：
    若 宽 小于 640 或者 高 小于 480：
        做 内核警告 用 「屏幕: 分辨率过低 」 加 做 整数转字符串 用 宽 加 「x」 加 做 整数转字符串 用 高。
        返还 假。
    完。
    若 宽 大于 7680 或者 高 大于 4320：
        做 内核警告 用 「屏幕: 分辨率超出支持范围」。
        返还 假。
    完。
    屏幕宽 为 宽。
    屏幕高 为 高。
    屏幕位深 为 位深。
    做 内核信息 用 「屏幕: 分辨率设置 」 加 做 整数转字符串 用 宽 加 「x」 加 做 整数转字符串 用 高 加 「@」 加 做 整数转字符串 用 位深 加 「bpp」。
    返还 真。
完。

屏幕_自动检测分辨率 接受 零参：
    gop 为 做 GOP_获取模式信息。
    若 gop 不等于 空：
        返还 做 屏幕_设置分辨率 用 gop 的 水平分辨率、gop 的 垂直分辨率、32。
    完。
    vesa 为 做 VESA_查询最佳模式。
    若 vesa 不等于 空：
        返还 做 屏幕_设置分辨率 用 vesa 的 宽、vesa 的 高、vesa 的 位深。
    完。
    做 内核信息 用 「屏幕: 使用默认 1920x1080@32bpp」。
    返还 真。
完。

可变 屏幕位深 为 32。

可变 前缓冲 为 0。

可变 后缓冲 为 0。

可变 合成_帧数 为 0。

可变 合成_脏区域 为 一组：（空）。

合成器_初始化 接受 宽、高、位深、fb地址：
    屏幕宽 为 宽。
    屏幕高 为 高。
    屏幕位深 为 位深。
    前缓冲 为 fb地址。
    后缓冲 为 fb地址 加 宽 乘 高 乘 (位深 除以 8)。
    所有窗口 为 一组：（空）。
    焦点窗口ID 为 0。
    下一个窗口ID 为 1。
    合成器_已初始化 为 真。
    合成_帧数 为 0。
    合成_脏区域 为 一组：（空）。
    做 内核信息 用 「合成器: 初始化 」 加 做 整数转字符串 用 宽 加 「×」 加 做 整数转字符串 用 高 加 「 」 加 做 整数转字符串 用 位深 加 「bpp」。
完。

本章 收束。
