《状态打印》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·中断表注册》。

引用《..·推理·通信渠道》。

引用《.·图处统一操作》。

引用《..·推理·定点Q算术》。

引用《.·图处枚举》。

引用《.·图处复位》。

引用《..·中枢·整数转字符串》。

GPU热管理状态 是 一种 事物，含 核心温度、显存温度、热点温度、风扇转速_RPM、风扇占空比、正在降频、温度阈值_降频、温度阈值_关机。

GPU电源状态 是 一种 事物，含 总功耗_mW、TDP_mW、板载功耗_mW、核心电压_mV、显存电压_mV、电源限制激活、过流保护触发次数。

GPU显存状态 是 一种 事物，含 总容量_MB、已使用_MB、空闲_MB、BAR_大小_MB、ECC_单位纠正、ECC_双位不可纠正、显存时钟_MHz、显存效率、页面退休数。

GPU显存带宽采样 是 一种 事物，含 读带宽_MBps、写带宽_MBps、时间戳。

PCIe链路状态 是 一种 事物，含 当前代、最大代、当前宽度、最大宽度、重训练次数、CRC错误、编码错误、ASPM状态。

GPU性能计数 是 一种 事物，含 GPU利用率、显存利用率、编码器利用率、解码器利用率、核心时钟_MHz、显存时钟_MHz、Shader活跃数、当前提交队列深度。

GPU工作负载统计 是 一种 事物，含 总提交命令数、总绘制调用、总计算调度、总DMA传输字节、帧缓冲翻转次数、帧延迟_累计us、帧延迟_最大us、超时重置次数。

最大显示输出 为 8。

显示输出状态 是 一种 事物，含 接口类型、已连接、宽度、高度、刷新率_mHz、色深、HDR激活、自适应同步。

GPU错误历史_容量 为 32。

GPU错误条目 是 一种 事物，含 时间戳、错误类型、严重级、描述、恢复已尝试、已恢复。

GPU错误历史 是 一种 事物，含 条目、写索引、总错误数、致命错误数。

健康权重_温度 为 20。

健康权重_显存 为 25。

健康权重_PCIe 为 15。

健康权重_功耗 为 15。

健康权重_错误 为 25。

健康窗口_大小 为 64。

GPU健康跟踪器 是 一种 事物，含 分数历史、写索引、当前分数、最低分数、AI预警已发、上次计算时间。

可变 GPU健康 为 新建 GPU健康跟踪器。

可变 GPU错误记录 为 新建 GPU错误历史。

NV_架构名称 接受 代：
    若 代 大于或等于 0x1A0：
        返还 「Blackwell」。
    完。
    若 代 大于或等于 0x190：
        返还 「Ada Lovelace」。
    完。
    若 代 大于或等于 0x170：
        返还 「Ampere」。
    完。
    若 代 大于或等于 0x160：
        返还 「Turing」。
    完。
    若 代 大于或等于 0x140：
        返还 「Volta」。
    完。
    若 代 大于或等于 0x130：
        返还 「Pascal」。
    完。
    若 代 大于或等于 0x110：
        返还 「Maxwell」。
    完。
    若 代 大于或等于 0xE0：
        返还 「Kepler」。
    完。
    若 代 大于或等于 0xC0：
        返还 「Fermi」。
    完。
    返还 「Tesla」。
完。

AMD_架构名称 接受 代：
    若 代 大于或等于 3000：
        返还 「CDNA 3」。
    完。
    若 代 大于或等于 2000：
        返还 「CDNA 2」。
    完。
    若 代 大于或等于 1000：
        返还 「CDNA 1」。
    完。
    若 代 大于或等于 400：
        返还 「RDNA 4」。
    完。
    若 代 大于或等于 300：
        返还 「RDNA 3」。
    完。
    若 代 大于或等于 200：
        返还 「RDNA 2」。
    完。
    若 代 大于或等于 100：
        返还 「RDNA 1」。
    完。
    若 代 大于或等于 50：
        返还 「GCN 5 (Vega)」。
    完。
    若 代 大于或等于 40：
        返还 「GCN 4 (Polaris)」。
    完。
    若 代 大于或等于 30：
        返还 「GCN 3」。
    完。
    若 代 大于或等于 20：
        返还 「GCN 2」。
    完。
    返还 「GCN 1」。
完。

显示接口名称 接受 类型：
    若 类型 等于 1：
        返还 「HDMI」。
    完。
    若 类型 等于 2：
        返还 「DisplayPort」。
    完。
    若 类型 等于 3：
        返还 「DVI」。
    完。
    若 类型 等于 4：
        返还 「VGA」。
    完。
    若 类型 等于 5：
        返还 「USB-C/DP」。
    完。
    返还 「未知」。
完。

PCIe代号名称 接受 代：
    若 代 等于 1：
        返还 「Gen1 2.5GT/s」。
    完。
    若 代 等于 2：
        返还 「Gen2 5GT/s」。
    完。
    若 代 等于 3：
        返还 「Gen3 8GT/s」。
    完。
    若 代 等于 4：
        返还 「Gen4 16GT/s」。
    完。
    若 代 等于 5：
        返还 「Gen5 32GT/s」。
    完。
    返还 「未知」。
完。

图处_统一打印状态 接受 零参：
    若 当前图处 等于 空：
        做 内核信息 用 「图处统一接口: 无图处」。
        返还。
    完。
    做 内核信息 用 「══════════════ GPU综合状态 ══════════════」。
    做 内核信息 用 「  图处: 」 加 当前图处 的 厂商名 加 「 」 加 当前图处 的 型号名。
    做 内核信息 用 「  驱动: 」 加 做 图处_驱动类型名。
    做 内核信息 用 「  显存: 」 加 做 整数转字符串 用 当前图处 的 显存大小_MB 加 「MB」。
    若 当前驱动类型 等于 DRV_NVIDIA：
        做 打印_NV状态。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        做 打印_AMD状态。
    完。
    可变 能力 为 「」。
    若 做 图处_有光追：
        能力 为 能力 加 「光追 」。
    完。
    若 做 图处_有AI加速器：
        能力 为 能力 加 「AI加速 」。
    完。
    若 做 图处_有计算能力：
        能力 为 能力 加 「计算 」。
    完。
    若 做 图处_有编码器：
        能力 为 能力 加 「视频编码 」。
    完。
    做 内核信息 用 「  能力: 」 加 能力。
    做 内核信息 用 「══════════════════════════════════════════」。
完。

打印_NV状态 接受 零参：
    nv 为 做 NV_获取状态。
    做 内核信息 用 「  架构: 」 加 做 NV_架构名称 用 nv 的 架构代。
    做 内核信息 用 「  SM: %d  GPC: %d  TPC/GPC: %d」、nv 的 SM数、nv 的 GPC数、nv 的 TPC每_GPC。
    做 内核信息 用 「  CUDA核心: %d  Tensor核心: %d  RT核心: %d」、nv 的 CUDA核心数、nv 的 Tensor核心数、nv 的 RT核心数。
    做 内核信息 用 「  显存类型: 」 加 nv 的 显存类型 加 「 」 加 做 整数转字符串 用 nv 的 显存总线宽 加 「bit」。
    做 内核信息 用 「  通道: %d  NVLink: %d」、nv 的 通道数、nv 的 NVLink连接数。
完。

打印_AMD状态 接受 零参：
    amd 为 做 AMD_获取状态。
    做 内核信息 用 「  架构: 」 加 做 AMD_架构名称 用 amd 的 架构代。
    做 内核信息 用 「  CU: %d  SE: %d  WGP/SA: %d」、amd 的 CU总数、amd 的 SE数、amd 的 WGP每_SA。
    做 内核信息 用 「  流处理器: %d  TMU: %d  ROP: %d」、amd 的 流处理器数、amd 的 TMU数、amd 的 ROP数。
    做 内核信息 用 「  显存类型: 」 加 amd 的 显存类型 加 「 」 加 做 整数转字符串 用 amd 的 显存总线宽 加 「bit」。
    做 内核信息 用 「  DCN: v%d  Infinity Cache: %dMB」、amd 的 DCN版本、amd 的 无限缓存_MB。
完。

图处_打印热状态 接受 零参：
    若 当前图处 等于 空：
        返还。
    完。
    热 为 做 图处_获取热管理状态。
    做 内核信息 用 「── 热管理 ──」。
    做 内核信息 用 「  核心: %d°C  显存: %d°C  热点: %d°C」、热 的 核心温度、热 的 显存温度、热 的 热点温度。
    做 内核信息 用 「  风扇: %dRPM (%d%%)」、热 的 风扇转速_RPM、热 的 风扇占空比。
    若 热 的 正在降频：
        做 内核信息 用 「  ⚠ 正在热降频! 阈值: %d°C」、热 的 温度阈值_降频。
    完。
    若 热 的 核心温度 大于或等于 热 的 温度阈值_降频 减 5：
        做 内核信息 用 「  ⚠ 温度接近降频阈值!」。
    完。
完。

图处_打印显存状态 接受 零参：
    若 当前图处 等于 空：
        返还。
    完。
    显存 为 做 图处_获取显存状态。
    做 内核信息 用 「── 显存 ──」。
    做 内核信息 用 「  总量: %dMB  已用: %dMB  空闲: %dMB  利用率: %d%%」、显存 的 总容量_MB、显存 的 已使用_MB、显存 的 空闲_MB、显存 的 已使用_MB 乘 100 除以 显存 的 总容量_MB。
    做 内核信息 用 「  时钟: %dMHz  BAR: %dMB」、显存 的 显存时钟_MHz、显存 的 BAR_大小_MB。
    做 内核信息 用 「  效率: %d%%  页退休: %d页」、显存 的 显存效率、显存 的 页面退休数。
    若 显存 的 ECC_单位纠正 大于 0 或者 显存 的 ECC_双位不可纠正 大于 0：
        做 内核信息 用 「  ECC: 可纠正=%d  不可纠正=%d」、显存 的 ECC_单位纠正、显存 的 ECC_双位不可纠正。
        若 显存 的 ECC_双位不可纠正 大于 0：
            做 内核信息 用 「  ⚠ 存在不可纠正显存错误! 建议RMA」。
        完。
    否则：
        做 内核信息 用 「  ECC: 无错误」。
    完。
完。

图处_打印PCIe状态 接受 零参：
    若 当前图处 等于 空：
        返还。
    完。
    pcie 为 做 图处_获取PCIe状态。
    做 内核信息 用 「── PCIe ──」。
    做 内核信息 用 「  当前: %s x%d  最大: %s x%d」、做 PCIe代号名称 用 pcie 的 当前代、pcie 的 当前宽度、做 PCIe代号名称 用 pcie 的 最大代、pcie 的 最大宽度。
    若 pcie 的 当前代 小于 pcie 的 最大代 或者 pcie 的 当前宽度 小于 pcie 的 最大宽度：
        做 内核信息 用 「  ⚠ PCIe链路未以最大速度运行!」。
    完。
    做 内核信息 用 「  重训练: %d次  CRC错误: %d  编码错误: %d」、pcie 的 重训练次数、pcie 的 CRC错误、pcie 的 编码错误。
    若 pcie 的 重训练次数 大于 10：
        做 内核信息 用 「  ⚠ 重训练次数过高, 可能存在硬件问题」。
    完。
    aspm_名 为 「禁用」。
    若 pcie 的 ASPM状态 等于 1：
        aspm_名 为 「L0s」。
    完。
    做 否则如果 用 pcie 的 ASPM状态 等于 2。
    块：
        aspm_名 为 「L1」。
    完。
    做 否则如果 用 pcie 的 ASPM状态 等于 3。
    块：
        aspm_名 为 「L0s+L1」。
    完。
    做 内核信息 用 「  ASPM: 」 加 aspm_名。
完。

图处_打印电源状态 接受 零参：
    若 当前图处 等于 空：
        返还。
    完。
    电 为 做 图处_获取电源状态。
    做 内核信息 用 「── 电源 ──」。
    做 内核信息 用 「  总功耗: %d.%dW  TDP: %d.%dW  (%d%%)」、电 的 总功耗_mW 除以 1000、电 的 总功耗_mW 取余于 1000 除以 100、电 的 TDP_mW 除以 1000、电 的 TDP_mW 取余于 1000 除以 100、电 的 总功耗_mW 乘 100 除以 电 的 TDP_mW。
    做 内核信息 用 「  板载: %d.%dW」、电 的 板载功耗_mW 除以 1000、电 的 板载功耗_mW 取余于 1000 除以 100。
    做 内核信息 用 「  核心电压: %dmV  显存电压: %dmV」、电 的 核心电压_mV、电 的 显存电压_mV。
    若 电 的 电源限制激活：
        做 内核信息 用 「  ⚠ 电源限制已激活! 可能影响性能」。
    完。
    若 电 的 过流保护触发次数 大于 0：
        做 内核信息 用 「  ⚠ 过流保护触发: %d次」、电 的 过流保护触发次数。
    完。
完。

图处_打印性能计数 接受 零参：
    若 当前图处 等于 空：
        返还。
    完。
    性能 为 做 图处_获取性能计数。
    负载 为 做 图处_获取工作负载统计。
    做 内核信息 用 「── 性能 ──」。
    做 内核信息 用 「  GPU利用率: %d%%  显存利用率: %d%%」、性能 的 GPU利用率、性能 的 显存利用率。
    做 内核信息 用 「  核心: %dMHz  显存: %dMHz」、性能 的 核心时钟_MHz、性能 的 显存时钟_MHz。
    做 内核信息 用 「  编码器: %d%%  解码器: %d%%」、性能 的 编码器利用率、性能 的 解码器利用率。
    做 内核信息 用 「  Shader活跃: %d  提交队列: %d」、性能 的 Shader活跃数、性能 的 当前提交队列深度。
    做 内核信息 用 「── 工作负载统计 ──」。
    做 内核信息 用 「  总提交: %d  绘制: %d  计算: %d」、负载 的 总提交命令数、负载 的 总绘制调用、负载 的 总计算调度。
    做 内核信息 用 「  DMA传输: %dMB  帧翻转: %d」、负载 的 总DMA传输字节 除以 (1024 乘 1024)、负载 的 帧缓冲翻转次数。
    若 负载 的 帧缓冲翻转次数 大于 0：
        平均帧时 为 负载 的 帧延迟_累计us 除以 负载 的 帧缓冲翻转次数。
        做 内核信息 用 「  帧延迟: 平均%dus  最大%dus」、平均帧时、负载 的 帧延迟_最大us。
    完。
    若 负载 的 超时重置次数 大于 0：
        做 内核信息 用 「  ⚠ GPU超时重置: %d次」、负载 的 超时重置次数。
    完。
完。

图处_打印显示输出 接受 零参：
    若 当前图处 等于 空：
        返还。
    完。
    做 内核信息 用 「── 显示输出 ──」。
    可变 已连接数 为 0。
    可变 i 为 0。
    只要 i 小于 最大显示输出：
        输出 为 做 图处_获取显示输出 用 i。
        若 输出 的 已连接：
            刷新率_Hz 为 输出 的 刷新率_mHz 除以 1000。
            刷新率_小数 为 输出 的 刷新率_mHz 取余于 1000 除以 10。
            可变 标志 为 「」。
            若 输出 的 HDR激活：
                标志 为 标志 加 「 HDR」。
            完。
            若 输出 的 自适应同步：
                标志 为 标志 加 「 VRR」。
            完。
            做 内核信息 用 「  [%d] %s: %dx%d@%d.%02dHz %dbit%s」、i、做 显示接口名称 用 输出 的 接口类型、输出 的 宽度、输出 的 高度、刷新率_Hz、刷新率_小数、输出 的 色深、标志。
            已连接数 为 已连接数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 已连接数 等于 0：
        做 内核信息 用 「  无已连接显示器」。
    否则：
        做 内核信息 用 「  共%d个显示器已连接」、已连接数。
    完。
完。

图处_记录错误 接受 类型、严重级、描述：
    可变 条目 为 新建 GPU错误条目。
    条目 的 时间戳 为 做 读取纳秒时钟。
    条目 的 错误类型 为 类型。
    条目 的 严重级 为 严重级。
    条目 的 描述 为 描述。
    条目 的 恢复已尝试 为 假。
    条目 的 已恢复 为 假。
    GPU错误记录 的 条目 的 第 GPU错误记录 的 写索引 取余于 GPU错误历史_容量 项 为 条目。
    GPU错误记录 的 写索引 为 GPU错误记录 的 写索引 加 1。
    GPU错误记录 的 总错误数 为 GPU错误记录 的 总错误数 加 1。
    若 严重级 大于或等于 3：
        GPU错误记录 的 致命错误数 为 GPU错误记录 的 致命错误数 加 1。
    完。
完。

GPU错误类型名 接受 类型：
    若 类型 等于 0：
        返还 「总线错误」。
    完。
    若 类型 等于 1：
        返还 「显存ECC」。
    完。
    若 类型 等于 2：
        返还 「过热」。
    完。
    若 类型 等于 3：
        返还 「挂起超时」。
    完。
    若 类型 等于 4：
        返还 「驱动异常」。
    完。
    返还 「未知」。
完。

GPU严重级名 接受 级别：
    若 级别 等于 0：
        返还 「信息」。
    完。
    若 级别 等于 1：
        返还 「警告」。
    完。
    若 级别 等于 2：
        返还 「错误」。
    完。
    若 级别 等于 3：
        返还 「致命」。
    完。
    返还 「?」。
完。

图处_打印错误历史 接受 零参：
    做 内核信息 用 「── GPU错误历史 (总计%d, 致命%d) ──」、GPU错误记录 的 总错误数、GPU错误记录 的 致命错误数。
    可变 数量 为 GPU错误记录 的 写索引。
    若 数量 大于 GPU错误历史_容量：
        数量 为 GPU错误历史_容量。
    完。
    可变 i 为 0。
    只要 i 小于 数量：
        idx 为 (GPU错误记录 的 写索引 减 数量 加 i) 取余于 GPU错误历史_容量。
        e 为 GPU错误记录 的 条目 的 第 idx 项。
        恢复状 为 「未尝试」。
        若 e 的 已恢复：
            恢复状 为 「已恢复」。
        完。
        做 否则如果 用 e 的 恢复已尝试。
        块：
            恢复状 为 「恢复失败」。
        完。
        做 内核信息 用 「  [%d] [%s] %s: %s (%s)」、i、做 GPU严重级名 用 e 的 严重级、做 GPU错误类型名 用 e 的 错误类型、e 的 描述、恢复状。
        i 为 i 加 1。
    完。
    若 数量 等于 0：
        做 内核信息 用 「  无错误记录」。
    完。
完。

图处_计算健康分 接受 零参：
    若 当前图处 等于 空：
        返还 0。
    完。
    热 为 做 图处_获取热管理状态。
    可变 温度分 为 100。
    若 热 的 核心温度 大于或等于 100：
        温度分 为 0。
    完。
    做 否则如果 用 热 的 核心温度 大于或等于 60。
    块：
        温度分 为 (100 减 热 的 核心温度) 乘 100 除以 40。
    完。
    显存 为 做 图处_获取显存状态。
    可变 显存分 为 100。
    若 显存 的 ECC_双位不可纠正 大于 0：
        显存分 为 0。
    完。
    做 否则如果 用 显存 的 ECC_单位纠正 大于 100。
    块：
        显存分 为 50。
    完。
    做 否则如果 用 显存 的 ECC_单位纠正 大于 10。
    块：
        显存分 为 80。
    完。
    pcie 为 做 图处_获取PCIe状态。
    可变 PCIe分 为 100。
    若 pcie 的 重训练次数 大于 100：
        PCIe分 为 30。
    完。
    做 否则如果 用 pcie 的 重训练次数 大于 10。
    块：
        PCIe分 为 70。
    完。
    若 pcie 的 当前代 小于 pcie 的 最大代：
        PCIe分 为 PCIe分 减 20。
    完。
    若 PCIe分 小于 0：
        PCIe分 为 0。
    完。
    电 为 做 图处_获取电源状态。
    可变 功耗分 为 100。
    若 电 的 TDP_mW 大于 0：
        比率 为 电 的 总功耗_mW 乘 100 除以 电 的 TDP_mW。
        若 比率 大于 120：
            功耗分 为 30。
        完。
        做 否则如果 用 比率 大于 100。
        块：
            功耗分 为 70。
        完。
    完。
    若 电 的 电源限制激活：
        功耗分 为 功耗分 减 15。
    完。
    若 功耗分 小于 0：
        功耗分 为 0。
    完。
    可变 错误分 为 100。
    若 GPU错误记录 的 致命错误数 大于 0：
        错误分 为 10。
    完。
    做 否则如果 用 GPU错误记录 的 总错误数 大于 50。
    块：
        错误分 为 40。
    完。
    做 否则如果 用 GPU错误记录 的 总错误数 大于 10。
    块：
        错误分 为 70。
    完。
    总分 为 (温度分 乘 健康权重_温度 加 显存分 乘 健康权重_显存 加 PCIe分 乘 健康权重_PCIe 加 功耗分 乘 健康权重_功耗 加 错误分 乘 健康权重_错误) 除以 100。
    GPU健康 的 分数历史 的 第 GPU健康 的 写索引 取余于 健康窗口_大小 项 为 总分。
    GPU健康 的 写索引 为 GPU健康 的 写索引 加 1。
    GPU健康 的 当前分数 为 总分。
    若 总分 小于 GPU健康 的 最低分数 或者 GPU健康 的 最低分数 等于 0：
        GPU健康 的 最低分数 为 总分。
    完。
    GPU健康 的 上次计算时间 为 做 读取纳秒时钟。
    返还 总分。
完。

图处_打印健康分 接受 零参：
    分 为 做 图处_计算健康分。
    等级 为 「优秀」。
    若 分 小于 30：
        等级 为 「危险」。
    完。
    做 否则如果 用 分 小于 50。
    块：
        等级 为 「警告」。
    完。
    做 否则如果 用 分 小于 70。
    块：
        等级 为 「一般」。
    完。
    做 否则如果 用 分 小于 85。
    块：
        等级 为 「良好」。
    完。
    做 内核信息 用 「── GPU健康评分 ──」。
    做 内核信息 用 「  当前: %d/100 (%s)  历史最低: %d/100」、分、等级、GPU健康 的 最低分数。
完。

图处_AI健康检查 接受 零参：
    若 GPU健康 的 写索引 小于 8：
        返还。
    完。
    可变 近期总 为 0。
    可变 i 为 0。
    只要 i 小于 8：
        idx 为 (GPU健康 的 写索引 减 8 加 i) 取余于 健康窗口_大小。
        近期总 为 近期总 加 GPU健康 的 分数历史 的 第 idx 项。
        i 为 i 加 1。
    完。
    近期均 为 近期总 除以 8。
    若 GPU健康 的 写索引 大于或等于 16：
        可变 早期总 为 0。
        i 为 0。
        只要 i 小于 8：
            idx 为 (GPU健康 的 写索引 减 16 加 i) 取余于 健康窗口_大小。
            早期总 为 早期总 加 GPU健康 的 分数历史 的 第 idx 项。
            i 为 i 加 1。
        完。
        早期均 为 早期总 除以 8。
        若 早期均 减 近期均 大于 15 并且 GPU健康 的 AI预警已发 等于 假：
            做 内核信息 用 「⚠ AI检测: GPU健康分持续下降 (%d → %d), 建议检查」、早期均、近期均。
            GPU健康 的 AI预警已发 为 真。
            做 图处_AI总线_报告健康下降 用 早期均、近期均。
        完。
    完。
    若 近期均 大于或等于 85 并且 GPU健康 的 AI预警已发：
        GPU健康 的 AI预警已发 为 假。
    完。
完。

图处_AI总线_报告健康下降 接受 早期、近期：
    bus 为 全局AI总线。
    若 bus 等于 空：
        返还。
    完。
    特征 为 做 Tensor_创建 用 1、4。
    做 Tensor_写 用 特征、0、0、早期 乘 Q_ONE 除以 100。
    做 Tensor_写 用 特征、0、1、近期 乘 Q_ONE 除以 100。
    做 Tensor_写 用 特征、0、2、(早期 减 近期) 乘 Q_ONE 除以 100。
    致命比 为 0。
    若 GPU错误记录 的 总错误数 大于 0：
        致命比 为 GPU错误记录 的 致命错误数 乘 Q_ONE 除以 GPU错误记录 的 总错误数。
    完。
    做 Tensor_写 用 特征、0、3、致命比。
    做 AI总线_GPU健康分析 用 bus、特征。
    做 Tensor_释放 用 特征。
完。

图处_全量诊断 接受 零参：
    做 图处_统一打印状态。
    做 图处_打印热状态。
    做 图处_打印显存状态。
    做 图处_打印PCIe状态。
    做 图处_打印电源状态。
    做 图处_打印性能计数。
    做 图处_打印显示输出。
    做 图处_打印错误历史。
    做 图处_打印健康分。
    做 图处_AI健康检查。
完。

图处统一_完整统计 接受 零参：
    返还 一组：当前驱动类型、做 图处_厂商名称、做 图处_型号名称、做 图处_计算单元数、做 图处_有光追、做 图处_有AI加速器、GPU健康 的 当前分数、GPU健康 的 最低分数、GPU错误记录 的 总错误数、GPU错误记录 的 致命错误数。
完。

本章 收束。
