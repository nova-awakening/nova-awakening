《位图字体》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·感知·内核直连层》。

引用《..·执行·控制台输出》。

默认字宽 为 8。

默认字高 为 16。

首字符 为 32。

末字符 为 126。

最大字库 为 65536。

字体信息 是 一种 事物，含 宽、高、基线位图、字符数、每字符字节、中文库、中文库大小、中文已加载。

可变 当前字体 为 0。

可变 渲染字符数 为 0。

可变 渲染中文数 为 0。

字体_初始化 接受 宽、高、位图数据：
    当前字体 为 做 堆_分配 用 字体信息。
    当前字体 的 宽 为 宽。
    当前字体 的 高 为 高。
    当前字体 的 基线位图 为 位图数据。
    当前字体 的 字符数 为 末字符 减 首字符 加 1。
    当前字体 的 每字符字节 为 (宽 加 7) 除以 8 乘 高。
    当前字体 的 中文库 为 0。
    当前字体 的 中文库大小 为 0。
    当前字体 的 中文已加载 为 假。
完。

字体_加载中文库 接受 数据、大小：
    若 当前字体 等于 0：
        返还。
    完。
    当前字体 的 中文库 为 数据。
    当前字体 的 中文库大小 为 大小。
    当前字体 的 中文已加载 为 真。
完。

字体_获取字形 接受 字符码：
    若 当前字体 等于 0：
        返还 0。
    完。
    若 字符码 大于或等于 首字符 并且 字符码 小于或等于 末字符：
        索引 为 字符码 减 首字符。
        返还 当前字体 的 基线位图 加 索引 乘 当前字体 的 每字符字节。
    完。
    返还 0。
完。

字体_渲染字符 接受 帧缓冲、横、纵、字符码、前景、背景：
    若 当前字体 等于 0：
        返还。
    完。
    渲染字符数 为 渲染字符数 加 1。
    字形 为 做 字体_获取字形 用 字符码。
    若 字形 等于 0：
        返还。
    完。
    字宽 为 当前字体 的 宽。
    字高 为 当前字体 的 高。
    行字节 为 (字宽 加 7) 除以 8。
    可变 行 为 0。
    只要 行 小于 字高：
        可变 列 为 0。
        只要 列 小于 字宽：
            字节索引 为 列 除以 8。
            位索引 为 7 减 列 取余于 8。
            像素 为 做 内存_读8 用 字形 加 行 乘 行字节 加 字节索引 右移 位索引 位与 1。
            若 像素：
                做 帧缓冲_写像素 用 横 加 列、纵 加 行、前景。
            否则若 背景 不等于 0：
                做 帧缓冲_写像素 用 横 加 列、纵 加 行、背景。
            完。
            列 为 列 加 1。
        完。
        行 为 行 加 1。
    完。
完。

字体_渲染中文 接受 帧缓冲、横、纵、区码、位码、前景、背景：
    若 当前字体 等于 0 或者 非 当前字体 的 中文已加载：
        返还。
    完。
    渲染中文数 为 渲染中文数 加 1。
    索引 为 (区码 减 1) 乘 94 加 (位码 减 1)。
    每字节 为 32。
    偏移 为 当前字体 的 中文库 加 索引 乘 每字节。
    若 偏移 加 每字节 大于 当前字体 的 中文库 加 当前字体 的 中文库大小：
        返还。
    完。
    可变 行 为 0。
    只要 行 小于 16：
        字节一 为 做 内存_读8 用 偏移 加 行 乘 2。
        字节二 为 做 内存_读8 用 偏移 加 行 乘 2 加 1。
        可变 列 为 0。
        只要 列 小于 8：
            若 字节一 右移 7 减 列 位与 1：
                做 帧缓冲_写像素 用 横 加 列、纵 加 行、前景。
            否则若 背景 不等于 0：
                做 帧缓冲_写像素 用 横 加 列、纵 加 行、背景。
            完。
            列 为 列 加 1。
        完。
        只要 列 小于 16：
            若 字节二 右移 15 减 列 位与 1：
                做 帧缓冲_写像素 用 横 加 列、纵 加 行、前景。
            否则若 背景 不等于 0：
                做 帧缓冲_写像素 用 横 加 列、纵 加 行、背景。
            完。
            列 为 列 加 1。
        完。
        行 为 行 加 1。
    完。
完。

字体_渲染字符串 接受 帧缓冲、横、纵、文本、长度、前景、背景：
    可变 当前横 为 横。
    可变 位 为 0。
    只要 位 小于 长度：
        字节 为 做 内存_读8 用 文本 加 位。
        若 字节 小于 128：
            做 字体_渲染字符 用 帧缓冲、当前横、纵、字节、前景、背景。
            当前横 为 当前横 加 当前字体 的 宽。
            位 为 位 加 1。
        否则若 位 加 1 小于 长度：
            字节二 为 做 内存_读8 用 文本 加 位 加 1。
            区码 为 字节 减 0xA0。
            位码 为 字节二 减 0xA0。
            做 字体_渲染中文 用 帧缓冲、当前横、纵、区码、位码、前景、背景。
            当前横 为 当前横 加 16。
            位 为 位 加 2。
        否则：
            位 为 位 加 1。
        完。
    完。
完。

字体_获取宽 接受 零参：
    若 当前字体 等于 0：
        返还 默认字宽。
    完。
    返还 当前字体 的 宽。
完。

字体_获取高 接受 零参：
    若 当前字体 等于 0：
        返还 默认字高。
    完。
    返还 当前字体 的 高。
完。

位图字体_完整统计 接受 零参：
    若 当前字体 等于 0：
        返还 一组：默认字宽、默认字高、0、假、0、0、0。
    完。
    返还 一组：当前字体 的 宽、当前字体 的 高、当前字体 的 字符数、当前字体 的 中文已加载、当前字体 的 中文库大小、渲染字符数、渲染中文数。
完。

字体_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 当前字体 等于 0：
        做 追加 用 问题、「字体未初始化」。
        返还 问题。
    完。
    若 当前字体 的 基线位图 等于 0：
        做 追加 用 问题、「字体位图数据为空」。
    完。
    若 非 当前字体 的 中文已加载：
        做 追加 用 问题、「中文字库未加载」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「位图字体健康」。
    完。
    返还 问题。
完。

字体_详细统计 接受 零参：
    若 当前字体 等于 0：
        返还 空。
    完。
    返还 一本字典：宽 对应 当前字体 的 宽、高 对应 当前字体 的 高、字符数 对应 当前字体 的 字符数、每字符字节 对应 当前字体 的 每字符字节、中文已加载 对应 当前字体 的 中文已加载、中文库大小 对应 当前字体 的 中文库大小、渲染ASCII 对应 渲染字符数、渲染中文 对应 渲染中文数、总渲染 对应 渲染字符数 加 渲染中文数。
完。

字体_重置统计 接受 零参：
    渲染字符数 为 0。
    渲染中文数 为 0。
完。

字体_文本宽度 接受 文本、长度：
    若 当前字体 等于 0：
        返还 长度 乘 默认字宽。
    完。
    可变 宽度 为 0。
    可变 位 为 0。
    只要 位 小于 长度：
        字节 为 做 内存_读8 用 文本 加 位。
        若 字节 小于 128：
            宽度 为 宽度 加 当前字体 的 宽。
            位 为 位 加 1。
        否则：
            宽度 为 宽度 加 16。
            位 为 位 加 2。
        完。
    完。
    返还 宽度。
完。

字体_文本高度 接受 零参：
    若 当前字体 等于 0：
        返还 默认字高。
    完。
    返还 当前字体 的 高。
完。

字体_字符数_从文本 接受 文本、长度：
    可变 字符数 为 0。
    可变 位 为 0。
    只要 位 小于 长度：
        字节 为 做 内存_读8 用 文本 加 位。
        若 字节 小于 128：
            位 为 位 加 1。
        否则：
            位 为 位 加 2。
        完。
        字符数 为 字符数 加 1。
    完。
    返还 字符数。
完。

字体_计算行数 接受 文本、长度、最大宽度：
    若 最大宽度 小于或等于 0：
        返还 1。
    完。
    文本宽 为 做 字体_文本宽度 用 文本、长度。
    行数 为 (文本宽 加 最大宽度 减 1) 除以 最大宽度。
    若 行数 小于 1：
        返还 1。
    完。
    返还 行数。
完。

字体_每行字符数 接受 最大宽度：
    若 当前字体 等于 0：
        返还 最大宽度 除以 默认字宽。
    完。
    返还 最大宽度 除以 当前字体 的 宽。
完。

可变 控制台_光标X 为 0。

可变 控制台_光标Y 为 0。

可变 控制台_前景色 为 0xFFFFFF。

可变 控制台_背景色 为 0x000000。

可变 控制台_屏幕宽 为 1920。

可变 控制台_屏幕高 为 1080。

可变 控制台_帧缓冲 为 0。

控制台_初始化 接受 帧缓冲、宽、高：
    控制台_帧缓冲 为 帧缓冲。
    控制台_屏幕宽 为 宽。
    控制台_屏幕高 为 高。
    控制台_光标X 为 0。
    控制台_光标Y 为 0。
完。

控制台_打印字符 接受 ch：
    若 控制台_帧缓冲 等于 0 或者 当前字体 等于 0：
        返还。
    完。
    若 ch 等于 10：
        控制台_光标X 为 0。
        控制台_光标Y 为 控制台_光标Y 加 当前字体 的 高。
        若 控制台_光标Y 加 当前字体 的 高 大于 控制台_屏幕高：
            做 控制台_滚动。
        完。
        返还。
    完。
    若 ch 等于 13：
        控制台_光标X 为 0。
        返还。
    完。
    若 ch 等于 9：
        控制台_光标X 为 (控制台_光标X 除以 (当前字体 的 宽 乘 8) 加 1) 乘 当前字体 的 宽 乘 8。
        返还。
    完。
    做 字体_渲染字符 用 控制台_帧缓冲、控制台_光标X、控制台_光标Y、ch、控制台_前景色、控制台_背景色。
    控制台_光标X 为 控制台_光标X 加 当前字体 的 宽。
    若 控制台_光标X 加 当前字体 的 宽 大于 控制台_屏幕宽：
        控制台_光标X 为 0。
        控制台_光标Y 为 控制台_光标Y 加 当前字体 的 高。
        若 控制台_光标Y 加 当前字体 的 高 大于 控制台_屏幕高：
            做 控制台_滚动。
        完。
    完。
完。

控制台_打印 接受 文本：
    若 文本 等于 0：
        返还。
    完。
    长度 为 做 字节长度 用 文本。
    可变 i 为 0。
    只要 i 小于 长度：
        做 控制台_打印字符 用 做 内存_读8 用 文本 加 i。
        i 为 i 加 1。
    完。
完。

控制台_换行 接受 零参：
    做 控制台_打印字符 用 10。
完。

控制台_滚动 接受 零参：
    若 控制台_帧缓冲 等于 0 或者 当前字体 等于 0：
        返还。
    完。
    行字节 为 控制台_屏幕宽 乘 4。
    滚动高度 为 当前字体 的 高。
    做 内存_复制 用 控制台_帧缓冲、控制台_帧缓冲 加 滚动高度 乘 行字节、(控制台_屏幕高 减 滚动高度) 乘 行字节。
    可变 i 为 0。
    只要 i 小于 滚动高度 乘 行字节：
        做 内存_写8 用 控制台_帧缓冲 加 (控制台_屏幕高 减 滚动高度) 乘 行字节 加 i、0。
        i 为 i 加 1。
    完。
    控制台_光标Y 为 控制台_光标Y 减 滚动高度。
完。

控制台_设置颜色 接受 前景、背景：
    控制台_前景色 为 前景。
    控制台_背景色 为 背景。
完。

控制台_清屏 接受 零参：
    若 控制台_帧缓冲 等于 0：
        返还。
    完。
    总字节 为 控制台_屏幕宽 乘 控制台_屏幕高 乘 4。
    可变 i 为 0。
    只要 i 小于 总字节：
        做 内存_写8 用 控制台_帧缓冲 加 i、0。
        i 为 i 加 1。
    完。
    控制台_光标X 为 0。
    控制台_光标Y 为 0。
完。

控制台_设置光标 接受 x、y：
    控制台_光标X 为 x。
    控制台_光标Y 为 y。
完。

控制台_获取列数 接受 零参：
    若 当前字体 等于 0：
        返还 控制台_屏幕宽 除以 默认字宽。
    完。
    返还 控制台_屏幕宽 除以 当前字体 的 宽。
完。

控制台_获取行数 接受 零参：
    若 当前字体 等于 0：
        返还 控制台_屏幕高 除以 默认字高。
    完。
    返还 控制台_屏幕高 除以 当前字体 的 高。
完。

UTF8_字符长度 接受 首字节：
    若 首字节 小于 0x80：
        返还 1。
    完。
    若 首字节 小于 0xC0：
        返还 1。
    完。
    若 首字节 小于 0xE0：
        返还 2。
    完。
    若 首字节 小于 0xF0：
        返还 3。
    完。
    返还 4。
完。

UTF8_解码 接受 数据、位置：
    首字节 为 做 内存_读8 用 数据 加 位置。
    若 首字节 小于 0x80：
        返还 首字节。
    完。
    若 首字节 小于 0xE0：
        返还 (首字节 位与 0x1F) 左移 6 位或 做 内存_读8 用 数据 加 位置 加 1 位与 0x3F。
    完。
    若 首字节 小于 0xF0：
        返还 (首字节 位与 0x0F) 左移 12 位或 (做 内存_读8 用 数据 加 位置 加 1 位与 0x3F) 左移 6 位或 做 内存_读8 用 数据 加 位置 加 2 位与 0x3F。
    完。
    返还 (首字节 位与 0x07) 左移 18 位或 (做 内存_读8 用 数据 加 位置 加 1 位与 0x3F) 左移 12 位或 (做 内存_读8 用 数据 加 位置 加 2 位与 0x3F) 左移 6 位或 做 内存_读8 用 数据 加 位置 加 3 位与 0x3F。
完。

UTF8_是ASCII 接受 字节：
    返还 字节 小于 0x80。
完。

UTF8_是中文 接受 码点：
    返还 码点 大于或等于 0x4E00 并且 码点 小于或等于 0x9FFF。
完。

UTF8_字符串长度 接受 数据、字节长度：
    可变 字符数 为 0。
    可变 位 为 0。
    只要 位 小于 字节长度：
        位 为 位 加 做 UTF8_字符长度 用 做 内存_读8 用 数据 加 位。
        字符数 为 字符数 加 1。
    完。
    返还 字符数。
完。

帧缓冲_写像素 接受 x、y、颜色：
    若 控制台_帧缓冲 等于 0：
        返还。
    完。
    若 x 小于 0 或者 x 大于或等于 控制台_屏幕宽 或者 y 小于 0 或者 y 大于或等于 控制台_屏幕高：
        返还。
    完。
    偏移 为 (y 乘 控制台_屏幕宽 加 x) 乘 4。
    做 内存_写32 用 控制台_帧缓冲 加 偏移、颜色。
完。

字体_控制台统计 接受 零参：
    返还 一本字典：列数 对应 做 控制台_获取列数、行数 对应 做 控制台_获取行数、光标X 对应 控制台_光标X、光标Y 对应 控制台_光标Y、前景色 对应 控制台_前景色、渲染总数 对应 渲染字符数 加 渲染中文数。
完。

可变 AI_位图字体完_操作热度 为 0。

AI_位图字体完_最优算法 接受 数据量、已排序：
    AI_位图字体完_操作热度 为 AI_位图字体完_操作热度 加 1。
    若 数据量 小于 16：
        返还 「插入排序」。
    完。
    若 已排序：
        返还 「二分查找」。
    完。
    若 数据量 小于 10000：
        返还 「快速排序」。
    完。
    返还 「归并排序」。
完。

位图字体完整_统计 接受 零参：
    返还 一本字典：模块 对应 「位图字体完整」、当前字体 对应 当前字体、渲染字符数 对应 渲染字符数、渲染中文数 对应 渲染中文数、控制台_光标X 对应 控制台_光标X、控制台_光标Y 对应 控制台_光标Y、控制台_前景色 对应 控制台_前景色、控制台_背景色 对应 控制台_背景色、控制台_屏幕宽 对应 控制台_屏幕宽、控制台_屏幕高 对应 控制台_屏幕高、控制台_帧缓冲 对应 控制台_帧缓冲、AI_位图字体完_操作热度 对应 AI_位图字体完_操作热度。
完。

位图字体完整_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    返还 做 计数 用 问题 等于 0。
完。

位图字体完整_综合报告 接受 零参：
    做 打印 用 「[位图字体完整] ═══ 综合报告 ═══」。
    做 打印 用 「  当前字体: 」 加 做 整数转字符串 用 当前字体。
    做 打印 用 「  渲染字符数: 」 加 做 整数转字符串 用 渲染字符数。
    做 打印 用 「  渲染中文数: 」 加 做 整数转字符串 用 渲染中文数。
    做 打印 用 「  控制台_光标X: 」 加 做 整数转字符串 用 控制台_光标X。
    做 打印 用 「  控制台_光标Y: 」 加 做 整数转字符串 用 控制台_光标Y。
    做 打印 用 「  控制台_前景色: 」 加 做 整数转字符串 用 控制台_前景色。
    做 打印 用 「  控制台_背景色: 」 加 做 整数转字符串 用 控制台_背景色。
    做 打印 用 「  控制台_屏幕宽: 」 加 做 整数转字符串 用 控制台_屏幕宽。
    做 打印 用 「  控制台_屏幕高: 」 加 做 整数转字符串 用 控制台_屏幕高。
    做 打印 用 「  控制台_帧缓冲: 」 加 做 整数转字符串 用 控制台_帧缓冲。
    做 打印 用 「  AI_位图字体完_操作热度: 」 加 做 整数转字符串 用 AI_位图字体完_操作热度。
    做 打印 用 「  健康: 」 加 若 做 位图字体完整_健康评估 则 「正常」 否则 「异常」。
完。

位图字体完整_全局重置 接受 零参：
    当前字体 为 0。
    渲染字符数 为 0。
    渲染中文数 为 0。
    控制台_光标X 为 0。
    控制台_光标Y 为 0。
    控制台_屏幕宽 为 1920。
    控制台_屏幕高 为 1080。
    控制台_帧缓冲 为 0。
    AI_位图字体完_操作热度 为 0。
    做 打印 用 「[位图字体完整] 全局统计已重置」。
    返还 0。
完。

本章 收束。
