《AHCI驱动全局》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·日志中枢》。

引用《..·中枢·中断管理系统》。

引用《..·中枢·同步原语》。

引用《..·中枢·进程调度》。

引用《..·中枢·内存虚拟》。

引用《.·总线解析》。

引用《..·中枢·进程信号》。

引用《..·中枢·公共智能》。

引用《..·引导·引导加载器》。

引用《..·中枢·字符串替换》。

引用《..·感知·内核直连层》。

引用《..·引导·端口IO》。

引用《.·图处枚举》。

引用《.·输入原语》。

引用《..·中枢·内存初始化》。

引用《..·中枢·哈希函数》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·每核页释放》。

AHCI_CAP 为 0x00。

AHCI_GHC 为 0x04。

AHCI_IS 为 0x08。

AHCI_PI 为 0x0C。

AHCI_VS 为 0x10。

AHCI_CAP2 为 0x24。

AHCI_PORT_BASE 为 0x100。

AHCI_PORT_SIZE 为 0x80。

AHCI_PxCLB 为 0x00。

AHCI_PxCLBU 为 0x04。

AHCI_PxFB 为 0x08。

AHCI_PxFBU 为 0x0C。

AHCI_PxIS 为 0x10。

AHCI_PxIE 为 0x14。

AHCI_PxCMD 为 0x18。

AHCI_PxTFD 为 0x20。

AHCI_PxSIG 为 0x24。

AHCI_PxSSTS 为 0x28。

AHCI_PxSCTL 为 0x2C。

AHCI_PxSERR 为 0x30。

AHCI_PxSACT 为 0x34。

AHCI_PxCI 为 0x38。

SATA_SIG_ATA 为 0x00000101。

SATA_SIG_ATAPI 为 0xEB140101。

SATA_SIG_PM 为 0x96690101。

SATA_SIG_SEMB 为 0xC33C0101。

ATA_CMD_IDENTIFY 为 0xEC。

ATA_CMD_READ_DMA_EX 为 0x25。

ATA_CMD_WRITE_DMA_EX 为 0x35。

ATA_CMD_SMART 为 0xB0。

ATA_CMD_FLUSH 为 0xE7。

扇区大小 为 512。

命令列表_大小 为 1024。

FIS_大小 为 256。

PRDT_最大项 为 8。

AHCI控制器 是 一种 事物，含 MMIO基址、端口数、命令槽数、支持64位、支持NCQ、版本、端口实现位图、设备列表、IRQ号、总线号、设备号、功能号。

AHCI端口 是 一种 事物，含 控制器、端口号、端口基址、类型、命令列表_地址、FIS_地址、已连接、型号、序列号、固件版本、总扇区数、容量_MB、支持NCQ、NCQ深度、支持SMART、读扇区数、写扇区数、读命令数、写命令数、错误数、上次LBA、连续读计数、预取窗口、锁。

可变 AHCI控制器列表 为 一组：（空）。

可变 AHCI端口列表 为 一组：（空）。

可变 AHCI_读扇区总数 为 0。

可变 AHCI_写扇区总数 为 0。

可变 AHCI_已初始化 为 假。

可变 AHCI_错误总数 为 0。

可变 AHCI_中断总数 为 0。

可变 AHCI_AI预取命中 为 0。

AHCI_初始化 接受 零参：
    AHCI控制器列表 为 一组：（空）。
    AHCI端口列表 为 一组：（空）。
    做 内核信息 用 「AHCI: 扫描SATA控制器...」。
    可变 总线 为 0。
    只要 总线 小于 256：
        可变 设备号 为 0。
        只要 设备号 小于 32：
            可变 功能号 为 0。
            只要 功能号 小于 8：
                vid_did 为 做 PCI_配置读32 用 总线、设备号、功能号、0x00。
                若 vid_did 等于 0xFFFFFFFF 或者 vid_did 等于 0：
                    功能号 为 功能号 加 1。
                    继续。
                完。
                类信息 为 做 PCI_配置读32 用 总线、设备号、功能号、0x08。
                类代码 为 类信息 右移 24 位与 0xFF。
                子类 为 类信息 右移 16 位与 0xFF。
                接口 为 类信息 右移 8 位与 0xFF。
                若 类代码 等于 0x01 并且 子类 等于 0x06 并且 接口 等于 0x01：
                    vid 为 vid_did 位与 0xFFFF。
                    did 为 vid_did 右移 16 位与 0xFFFF。
                    做 内核信息 用 「AHCI: 发现控制器 VID=」 加 做 整数转十六进制 用 vid 加 「 DID=」 加 做 整数转十六进制 用 did 加 「 @PCI 」 加 做 整数转字符串 用 总线 加 「:」 加 做 整数转字符串 用 设备号 加 「.」 加 做 整数转字符串 用 功能号。
                    cmd 为 做 PCI_配置读32 用 总线、设备号、功能号、0x04。
                    做 PCI_配置写32 用 总线、设备号、功能号、0x04、cmd 位或 0x06。
                    abar 为 做 PCI_读BAR 用 总线、设备号、功能号、5。
                    做 AHCI_初始化控制器 用 abar。
                完。
                功能号 为 功能号 加 1。
            完。
            设备号 为 设备号 加 1。
        完。
        总线 为 总线 加 1。
    完。
    AHCI_已初始化 为 真。
    做 内核信息 用 「AHCI: 初始化完成 控制器=」 加 做 整数转字符串 用 做 计数 用 AHCI控制器列表 加 「 磁盘=」 加 做 整数转字符串 用 做 计数 用 AHCI端口列表。
完。

AHCI_初始化控制器 接受 abar：
    若 abar 等于 0：
        返还。
    完。
    ctl 为 新建 AHCI控制器。
    ctl 的 MMIO基址 为 abar。
    ghc 为 做 MMIO读32 用 abar 加 AHCI_GHC。
    做 MMIO写32 用 abar 加 AHCI_GHC、ghc 位或 0x80000000。
    cap 为 做 MMIO读32 用 abar 加 AHCI_CAP。
    ctl 的 端口数 为 (cap 位与 0x1F) 加 1。
    ctl 的 命令槽数 为 (cap 右移 8 位与 0x1F) 加 1。
    ctl 的 支持64位 为 cap 右移 31 位与 1 不等于 0。
    ctl 的 支持NCQ 为 cap 右移 30 位与 1 不等于 0。
    vs 为 做 MMIO读32 用 abar 加 AHCI_VS。
    ctl 的 版本 为 vs。
    ctl 的 端口实现位图 为 做 MMIO读32 用 abar 加 AHCI_PI。
    ctl 的 设备列表 为 一组：（空）。
    做 内核信息 用 「AHCI: 版本=」 加 做 整数转十六进制 用 vs 加 「 端口=」 加 做 整数转字符串 用 ctl 的 端口数 加 「 命令槽=」 加 做 整数转字符串 用 ctl 的 命令槽数 加 「 64位=」 加 若 ctl 的 支持64位 则 「是」 否则 「否」 加 「 NCQ=」 加 若 ctl 的 支持NCQ 则 「是」 否则 「否」。
    做 追加 用 AHCI控制器列表、ctl。
    可变 端口号 为 0。
    只要 端口号 小于 32：
        若 ctl 的 端口实现位图 位与 1 左移 端口号 不等于 0：
            做 AHCI_初始化端口 用 ctl、端口号。
        完。
        端口号 为 端口号 加 1。
    完。
完。

AHCI_初始化端口 接受 ctl、端口号：
    端口基址 为 ctl 的 MMIO基址 加 AHCI_PORT_BASE 加 端口号 乘 AHCI_PORT_SIZE。
    ssts 为 做 MMIO读32 用 端口基址 加 AHCI_PxSSTS。
    det 为 ssts 位与 0x0F。
    ipm 为 ssts 右移 8 位与 0x0F。
    若 det 不等于 3 或者 ipm 不等于 1：
        返还。
    完。
    sig 为 做 MMIO读32 用 端口基址 加 AHCI_PxSIG。
    可变 类型名 为 「未知」。
    若 sig 等于 SATA_SIG_ATA：
        类型名 为 「SATA硬盘」。
    否则若 sig 等于 SATA_SIG_ATAPI：
        类型名 为 「ATAPI光驱」。
    否则若 sig 等于 SATA_SIG_PM：
        类型名 为 「端口复用器」。
    否则：
        返还。
    完。
    做 AHCI_端口_停止命令 用 端口基址。
    命令列表 为 做 直传_分配 用 命令列表_大小。
    FIS接收 为 做 直传_分配 用 FIS_大小。
    若 命令列表 等于 0 或者 FIS接收 等于 0：
        返还。
    完。
    做 内存_置零 用 命令列表、命令列表_大小。
    做 内存_置零 用 FIS接收、FIS_大小。
    做 MMIO写32 用 端口基址 加 AHCI_PxCLB、命令列表 位与 0xFFFFFFFF。
    做 MMIO写32 用 端口基址 加 AHCI_PxCLBU、0。
    做 MMIO写32 用 端口基址 加 AHCI_PxFB、FIS接收 位与 0xFFFFFFFF。
    做 MMIO写32 用 端口基址 加 AHCI_PxFBU、0。
    做 MMIO写32 用 端口基址 加 AHCI_PxSERR、0xFFFFFFFF。
    做 MMIO写32 用 端口基址 加 AHCI_PxIS、0xFFFFFFFF。
    做 MMIO写32 用 端口基址 加 AHCI_PxIE、0x7DC000FF。
    做 AHCI_端口_启动命令 用 端口基址。
    端口 为 新建 AHCI端口：控制器 为 ctl、端口号 为 端口号、端口基址 为 端口基址、类型 为 sig、命令列表_地址 为 命令列表、FIS_地址 为 FIS接收、已连接 为 真、型号 为 「」、序列号 为 「」、固件版本 为 「」、总扇区数 为 0、容量_MB 为 0、支持NCQ 为 假、NCQ深度 为 0、支持SMART 为 假、读扇区数 为 0、写扇区数 为 0、读命令数 为 0、写命令数 为 0、错误数 为 0、上次LBA 为 0、连续读计数 为 0、预取窗口 为 8、锁 为 做 自旋锁_创建。
    做 追加 用 AHCI端口列表、端口。
    做 追加 用 ctl 的 设备列表、端口。
    做 AHCI_识别设备 用 端口。
    做 内核信息 用 「AHCI: 端口」 加 做 整数转字符串 用 端口号 加 「 」 加 类型名 加 「 」 加 端口 的 型号 加 「 」 加 做 整数转字符串 用 端口 的 容量_MB 加 「MB」。
完。

AHCI_端口_停止命令 接受 端口基址：
    cmd 为 做 MMIO读32 用 端口基址 加 AHCI_PxCMD。
    cmd 为 cmd 位与 按位取反 0x01。
    cmd 为 cmd 位与 按位取反 0x10。
    做 MMIO写32 用 端口基址 加 AHCI_PxCMD、cmd。
    可变 等待 为 0。
    只要 等待 小于 1000：
        当前cmd 为 做 MMIO读32 用 端口基址 加 AHCI_PxCMD。
        若 当前cmd 位与 0x4000 等于 0 并且 当前cmd 位与 0x8000 等于 0：
            返还。
        完。
        等待 为 等待 加 1。
    完。
完。

AHCI_端口_启动命令 接受 端口基址：
    可变 等待 为 0。
    只要 等待 小于 1000：
        若 做 MMIO读32 用 端口基址 加 AHCI_PxCMD 位与 0x8000 等于 0：
            跳出。
        完。
        等待 为 等待 加 1。
    完。
    cmd 为 做 MMIO读32 用 端口基址 加 AHCI_PxCMD。
    cmd 为 cmd 位或 0x10。
    cmd 为 cmd 位或 0x01。
    做 MMIO写32 用 端口基址 加 AHCI_PxCMD、cmd。
完。

AHCI_识别设备 接受 端口：
    数据缓冲 为 做 直传_分配 用 扇区大小。
    若 数据缓冲 等于 0：
        返还。
    完。
    做 内存_置零 用 数据缓冲、扇区大小。
    结果 为 做 AHCI_发送命令 用 端口、ATA_CMD_IDENTIFY、0、1、数据缓冲、扇区大小。
    若 结果 不等于 成功：
        做 释放内存 用 数据缓冲。
        返还。
    完。
    可变 型号 为 「」。
    可变 i 为 0。
    只要 i 小于 20：
        w 为 做 内存_读16 用 数据缓冲 加 (27 加 i) 乘 2。
        高字节 为 w 右移 8 位与 0xFF。
        低字节 为 w 位与 0xFF。
        若 高字节 大于或等于 0x20：
            型号 为 型号 加 做 字符 用 高字节。
        完。
        若 低字节 大于或等于 0x20：
            型号 为 型号 加 做 字符 用 低字节。
        完。
        i 为 i 加 1。
    完。
    端口 的 型号 为 做 字符串_去尾空 用 型号。
    可变 序列 为 「」。
    i 为 0。
    只要 i 小于 10：
        w 为 做 内存_读16 用 数据缓冲 加 (10 加 i) 乘 2。
        序列 为 序列 加 做 字符 用 w 右移 8 位与 0xFF 加 做 字符 用 w 位与 0xFF。
        i 为 i 加 1。
    完。
    端口 的 序列号 为 做 字符串_去尾空 用 序列。
    扇区低 为 做 内存_读32 用 数据缓冲 加 100 乘 2。
    扇区高 为 做 内存_读32 用 数据缓冲 加 102 乘 2。
    端口 的 总扇区数 为 扇区低。
    端口 的 容量_MB 为 扇区低 除以 2048。
    ncq_word 为 做 内存_读16 用 数据缓冲 加 75 乘 2。
    端口 的 NCQ深度 为 (ncq_word 位与 0x1F) 加 1。
    端口 的 支持NCQ 为 端口 的 NCQ深度 大于 1。
    feat82 为 做 内存_读16 用 数据缓冲 加 82 乘 2。
    端口 的 支持SMART 为 feat82 位与 0x01 不等于 0。
    做 释放内存 用 数据缓冲。
完。

AHCI_读扇区 接受 端口、LBA、扇区数、缓冲：
    若 非 端口 的 已连接：
        返还 错误_设备不存在。
    完。
    做 自旋锁_获取 用 端口 的 锁。
    若 LBA 等于 端口 的 上次LBA 加 扇区数 或者 LBA 等于 端口 的 上次LBA 加 1：
        端口 的 连续读计数 为 端口 的 连续读计数 加 1。
        若 端口 的 连续读计数 大于 4：
            端口 的 预取窗口 为 端口 的 预取窗口 乘 2。
            若 端口 的 预取窗口 大于 256：
                端口 的 预取窗口 为 256。
            完。
            AHCI_AI预取命中 为 AHCI_AI预取命中 加 1。
        完。
    否则：
        端口 的 连续读计数 为 0。
        端口 的 预取窗口 为 8。
    完。
    端口 的 上次LBA 为 LBA。
    结果 为 做 AHCI_发送命令 用 端口、ATA_CMD_READ_DMA_EX、LBA、扇区数、缓冲、扇区数 乘 扇区大小。
    若 结果 等于 成功：
        端口 的 读扇区数 为 端口 的 读扇区数 加 扇区数。
        端口 的 读命令数 为 端口 的 读命令数 加 1。
        AHCI_读扇区总数 为 AHCI_读扇区总数 加 扇区数。
    否则：
        端口 的 错误数 为 端口 的 错误数 加 1。
        AHCI_错误总数 为 AHCI_错误总数 加 1。
    完。
    做 自旋锁_释放 用 端口 的 锁。
    返还 结果。
完。

AHCI_写扇区 接受 端口、LBA、扇区数、缓冲：
    若 非 端口 的 已连接：
        返还 错误_设备不存在。
    完。
    做 自旋锁_获取 用 端口 的 锁。
    结果 为 做 AHCI_发送命令 用 端口、ATA_CMD_WRITE_DMA_EX、LBA、扇区数、缓冲、扇区数 乘 扇区大小。
    若 结果 等于 成功：
        端口 的 写扇区数 为 端口 的 写扇区数 加 扇区数。
        端口 的 写命令数 为 端口 的 写命令数 加 1。
        AHCI_写扇区总数 为 AHCI_写扇区总数 加 扇区数。
    否则：
        端口 的 错误数 为 端口 的 错误数 加 1。
        AHCI_错误总数 为 AHCI_错误总数 加 1。
    完。
    做 自旋锁_释放 用 端口 的 锁。
    返还 结果。
完。

AHCI_刷新缓存 接受 端口：
    返还 做 AHCI_发送命令 用 端口、ATA_CMD_FLUSH、0、0、0、0。
完。

AHCI_发送命令 接受 端口、命令、LBA、扇区数、缓冲、缓冲大小：
    槽 为 做 AHCI_查找空闲槽 用 端口。
    若 槽 小于 0：
        返还 错误_资源忙。
    完。
    头基址 为 端口 的 命令列表_地址 加 槽 乘 32。
    可变 头DW0 为 5。
    若 命令 等于 ATA_CMD_WRITE_DMA_EX：
        头DW0 为 头DW0 位或 0x40。
    完。
    若 缓冲大小 大于 0：
        头DW0 为 头DW0 位或 1 左移 16。
    完。
    做 内存_写32 用 头基址、头DW0。
    做 内存_写32 用 头基址 加 4、0。
    表地址 为 做 直传_分配 用 256。
    做 内存_置零 用 表地址、256。
    做 内存_写32 用 头基址 加 8、表地址 位与 0xFFFFFFFF。
    做 内存_写32 用 头基址 加 12、0。
    做 内存_写8 用 表地址、0x27。
    做 内存_写8 用 表地址 加 1、0x80。
    做 内存_写8 用 表地址 加 2、命令。
    做 内存_写8 用 表地址 加 3、0。
    做 内存_写8 用 表地址 加 4、LBA 位与 0xFF。
    做 内存_写8 用 表地址 加 5、LBA 右移 8 位与 0xFF。
    做 内存_写8 用 表地址 加 6、LBA 右移 16 位与 0xFF。
    做 内存_写8 用 表地址 加 7、0x40。
    做 内存_写8 用 表地址 加 8、LBA 右移 24 位与 0xFF。
    做 内存_写8 用 表地址 加 12、扇区数 位与 0xFF。
    做 内存_写8 用 表地址 加 13、扇区数 右移 8 位与 0xFF。
    若 缓冲大小 大于 0 并且 缓冲 不等于 0：
        prdt基址 为 表地址 加 0x80。
        做 内存_写32 用 prdt基址、缓冲 位与 0xFFFFFFFF。
        做 内存_写32 用 prdt基址 加 4、0。
        做 内存_写32 用 prdt基址 加 8、0。
        做 内存_写32 用 prdt基址 加 12、缓冲大小 减 1 位或 0x80000000。
    完。
    做 MMIO写32 用 端口 的 端口基址 加 AHCI_PxCI、1 左移 槽。
    可变 超时 为 0。
    只要 超时 小于 500000：
        ci 为 做 MMIO读32 用 端口 的 端口基址 加 AHCI_PxCI。
        若 ci 位与 1 左移 槽 等于 0：
            tfd 为 做 MMIO读32 用 端口 的 端口基址 加 AHCI_PxTFD。
            若 tfd 位与 0x01：
                做 释放内存 用 表地址。
                返还 错误_IO。
            完。
            做 释放内存 用 表地址。
            返还 成功。
        完。
        超时 为 超时 加 1。
    完。
    做 释放内存 用 表地址。
    返还 错误_超时。
完。

AHCI_查找空闲槽 接受 端口：
    ci 为 做 MMIO读32 用 端口 的 端口基址 加 AHCI_PxCI。
    sact 为 做 MMIO读32 用 端口 的 端口基址 加 AHCI_PxSACT。
    已用 为 ci 位或 sact。
    可变 i 为 0。
    只要 i 小于 端口 的 控制器 的 命令槽数：
        若 已用 位与 1 左移 i 等于 0：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

AHCI_中断处理 接受 ctl：
    AHCI_中断总数 为 AHCI_中断总数 加 1。
    全局IS 为 做 MMIO读32 用 ctl 的 MMIO基址 加 AHCI_IS。
    可变 端口号 为 0。
    只要 端口号 小于 32：
        若 全局IS 位与 1 左移 端口号 不等于 0：
            做 AHCI_端口中断 用 ctl、端口号。
        完。
        端口号 为 端口号 加 1。
    完。
    做 MMIO写32 用 ctl 的 MMIO基址 加 AHCI_IS、全局IS。
完。

AHCI_端口中断 接受 ctl、端口号：
    端口基址 为 ctl 的 MMIO基址 加 AHCI_PORT_BASE 加 端口号 乘 AHCI_PORT_SIZE。
    端口IS 为 做 MMIO读32 用 端口基址 加 AHCI_PxIS。
    做 MMIO写32 用 端口基址 加 AHCI_PxIS、端口IS。
    若 端口IS 位与 0x40000000：
        serr 为 做 MMIO读32 用 端口基址 加 AHCI_PxSERR。
        做 MMIO写32 用 端口基址 加 AHCI_PxSERR、serr。
        做 内核警告 用 「[AHCI] 端口」 加 做 整数转字符串 用 端口号 加 「 错误: SERR=0x」 加 做 整数转十六进制 用 serr。
    完。
完。

块设备_读 接受 设备、偏移、大小：
    若 做 计数 用 AHCI端口列表 等于 0：
        返还 空。
    完。
    端口 为 AHCI端口列表 的 第 0 项。
    LBA 为 偏移 除以 扇区大小。
    扇区数 为 (大小 加 扇区大小 减 1) 除以 扇区大小。
    缓冲 为 做 分配内存 用 扇区数 乘 扇区大小。
    若 缓冲 等于 空：
        返还 空。
    完。
    结果 为 做 AHCI_读扇区 用 端口、LBA、扇区数、缓冲。
    若 结果 不等于 成功：
        做 释放内存 用 缓冲。
        返还 空。
    完。
    返还 缓冲。
完。

块设备_写 接受 设备、偏移、数据、大小：
    若 做 计数 用 AHCI端口列表 等于 0：
        返还 错误_设备不存在。
    完。
    端口 为 AHCI端口列表 的 第 0 项。
    LBA 为 偏移 除以 扇区大小。
    扇区数 为 (大小 加 扇区大小 减 1) 除以 扇区大小。
    返还 做 AHCI_写扇区 用 端口、LBA、扇区数、数据。
完。

存储_初始化 接受 零参：
    做 AHCI_初始化。
    返还 做 计数 用 AHCI端口列表。
完。

获取存储驱动句柄 接受 零参：
    返还 AHCI端口列表。
完。

AHCI_统计 接受 零参：
    返还 一本字典：控制器数 对应 做 计数 用 AHCI控制器列表、磁盘数 对应 做 计数 用 AHCI端口列表、读扇区总数 对应 AHCI_读扇区总数、写扇区总数 对应 AHCI_写扇区总数、错误总数 对应 AHCI_错误总数、中断总数 对应 AHCI_中断总数、AI预取命中 对应 AHCI_AI预取命中。
完。

AHCI_磁盘信息 接受 索引：
    若 索引 大于或等于 做 计数 用 AHCI端口列表：
        返还 空。
    完。
    p 为 AHCI端口列表 的 第 索引 项。
    返还 一本字典：型号 对应 p 的 型号、序列号 对应 p 的 序列号、容量MB 对应 p 的 容量_MB、支持NCQ 对应 p 的 支持NCQ、NCQ深度 对应 p 的 NCQ深度、读命令 对应 p 的 读命令数、写命令 对应 p 的 写命令数、错误 对应 p 的 错误数。
完。

AHCI_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 做 计数 用 AHCI端口列表 等于 0：
        做 追加 用 问题、「无SATA磁盘」。
    完。
    若 AHCI_错误总数 大于 10：
        做 追加 用 问题、「磁盘错误过多: 」 加 做 整数转字符串 用 AHCI_错误总数。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AHCI端口列表：
        若 AHCI端口列表 的 第 i 项 的 错误数 大于 5：
            做 追加 用 问题、「磁盘」 加 做 整数转字符串 用 i 加 「错误: 」 加 做 整数转字符串 用 AHCI端口列表 的 第 i 项 的 错误数。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「存储健康」。
    完。
    返还 问题。
完。

直传_分配 接受 大小：
    返还 做 伙伴系统_分配 用 (大小 加 4095) 除以 4096。
完。

字符串_去尾空 接受 s：
    返还 s。
完。

磁控驱动全局_统计 接受 零参：
    返还 一本字典：AHCI读扇区总数 对应 AHCI_读扇区总数、AHCI写扇区总数 对应 AHCI_写扇区总数、AHCI已初始化 对应 AHCI_已初始化、AHCI错误总数 对应 AHCI_错误总数、AHCI中断总数 对应 AHCI_中断总数。
完。

磁控驱动全局_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 AHCI_错误总数 大于 100：
        做 追加 用 问题、「AHCI_错误总数=」 加 AHCI_错误总数。
    完。
    返还 做 计数 用 问题 等于 0。
完。

磁控驱动全局_综合报告 接受 零参：
    做 打印 用 「[磁控驱动全局] ═══ 综合报告 ═══」。
    做 打印 用 「  AHCI_读扇区总数: 」 加 AHCI_读扇区总数。
    做 打印 用 「  AHCI_写扇区总数: 」 加 AHCI_写扇区总数。
    做 打印 用 「  AHCI_已初始化: 」 加 AHCI_已初始化。
    做 打印 用 「  AHCI_错误总数: 」 加 AHCI_错误总数。
    做 打印 用 「  AHCI_中断总数: 」 加 AHCI_中断总数。
    做 打印 用 「  健康: 」 加 若 做 磁控驱动全局_健康评估 则 「正常」 否则 「异常」。
完。

磁控驱动全局_全局重置 接受 零参：
    AHCI_读扇区总数 为 0。
    AHCI_写扇区总数 为 0。
    AHCI_已初始化 为 0。
    AHCI_错误总数 为 0。
    AHCI_中断总数 为 0。
    做 打印 用 「[磁控驱动全局] 统计已重置」。
    返还 0。
完。

本章 收束。
