《输入原语》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·引导·端口IO》。

引用《..·中枢·虚拟内存管理》。

外部。

映射页面。

外部。

取消映射页面。

MMIO读8 接受 地址：
    值 为 做 内存读8 用 地址。
    做 内存屏障_读。
    返还 值。
完。

MMIO读16 接受 地址：
    值 为 做 内存读16 用 地址。
    做 内存屏障_读。
    返还 值。
完。

MMIO读32 接受 地址：
    值 为 做 内存读32 用 地址。
    做 内存屏障_读。
    返还 值。
完。

MMIO读64 接受 地址：
    值 为 做 内存读64 用 地址。
    做 内存屏障_读。
    返还 值。
完。

MMIO写8 接受 地址、值：
    做 内存屏障_写。
    做 内存写8 用 地址、值。
完。

MMIO写16 接受 地址、值：
    做 内存屏障_写。
    做 内存写16 用 地址、值。
完。

MMIO写32 接受 地址、值：
    做 内存屏障_写。
    做 内存写32 用 地址、值。
完。

MMIO写64 接受 地址、值：
    做 内存屏障_写。
    做 内存写64 用 地址、值。
完。

MMIO_raw读32 接受 地址：
    返还 做 内存读32 用 地址。
完。

MMIO_raw写32 接受 地址、值：
    做 内存写32 用 地址、值。
完。

IO_SPACE_LIMIT 为 0xFFFF。

ioread8 接受 地址：
    若 地址 小于或等于 IO_SPACE_LIMIT：
        返还 做 端口读8 用 地址。
    完。
    返还 做 MMIO读8 用 地址。
完。

ioread16 接受 地址：
    若 地址 小于或等于 IO_SPACE_LIMIT：
        返还 做 端口读16 用 地址。
    完。
    返还 做 MMIO读16 用 地址。
完。

ioread32 接受 地址：
    若 地址 小于或等于 IO_SPACE_LIMIT：
        返还 做 端口读32 用 地址。
    完。
    返还 做 MMIO读32 用 地址。
完。

iowrite8 接受 地址、值：
    若 地址 小于或等于 IO_SPACE_LIMIT：
        做 端口写8 用 地址、值。
        返还。
    完。
    做 MMIO写8 用 地址、值。
完。

iowrite16 接受 地址、值：
    若 地址 小于或等于 IO_SPACE_LIMIT：
        做 端口写16 用 地址、值。
        返还。
    完。
    做 MMIO写16 用 地址、值。
完。

iowrite32 接受 地址、值：
    若 地址 小于或等于 IO_SPACE_LIMIT：
        做 端口写32 用 地址、值。
        返还。
    完。
    做 MMIO写32 用 地址、值。
完。

内存屏障_读 接受 零参：
    做 内联汇编 用 「lfence」。
完。

内存屏障_写 接受 零参：
    做 内联汇编 用 「sfence」。
完。

内存屏障_全 接受 零参：
    做 内联汇编 用 「mfence」。
完。

IO原语_暂停 接受 零参：
    做 内联汇编 用 「pause」。
完。

IO原语_读取时间戳计数 接受 零参：
    返还 做 内联汇编_返回值 用 「rdtsc」。
完。

IO原语_延迟微秒 接受 微秒：
    若 微秒 小于或等于 0：
        返还。
    完。
    目标滴答 为 微秒 乘 2000。
    开始 为 做 IO原语_读取时间戳计数。
    只要 做 IO原语_读取时间戳计数 减 开始 小于 目标滴答：
        做 IO原语_暂停。
    完。
完。

IO原语_延迟毫秒 接受 毫秒：
    若 毫秒 小于或等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 毫秒：
        做 IO原语_延迟微秒 用 1000。
        i 为 i 加 1。
    完。
完。

memcpy_fromio 接受 目标、IO地址、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        做 内存写8 用 目标 加 i、做 MMIO读8 用 IO地址 加 i。
        i 为 i 加 1。
    完。
完。

memcpy_toio 接受 IO地址、源、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        做 MMIO写8 用 IO地址 加 i、做 内存读8 用 源 加 i。
        i 为 i 加 1。
    完。
完。

memset_io 接受 IO地址、值、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        做 MMIO写8 用 IO地址 加 i、值。
        i 为 i 加 1。
    完。
完。

可变 ioremap_下一个虚拟地址 为 0xFFFF_8000_0000_0000。

IO原语_ioremap 接受 物理地址、大小：
    虚拟地址 为 ioremap_下一个虚拟地址。
    页数 为 (大小 加 4095) 除以 4096。
    ioremap_下一个虚拟地址 为 ioremap_下一个虚拟地址 加 页数 乘 4096。
    可变 i 为 0。
    只要 i 小于 页数：
        做 映射页面 用 虚拟地址 加 i 乘 4096、物理地址 加 i 乘 4096、0x13。
        i 为 i 加 1。
    完。
    返还 虚拟地址。
完。

IO原语_iounmap 接受 虚拟地址、大小：
    页数 为 (大小 加 4095) 除以 4096。
    可变 i 为 0。
    只要 i 小于 页数：
        做 取消映射页面 用 虚拟地址 加 i 乘 4096。
        i 为 i 加 1。
    完。
完。

ioremap_wc 接受 物理地址、大小：
    虚拟地址 为 ioremap_下一个虚拟地址。
    页数 为 (大小 加 4095) 除以 4096。
    ioremap_下一个虚拟地址 为 ioremap_下一个虚拟地址 加 页数 乘 4096。
    可变 i 为 0。
    只要 i 小于 页数：
        做 映射页面 用 虚拟地址 加 i 乘 4096、物理地址 加 i 乘 4096、0x1B。
        i 为 i 加 1。
    完。
    返还 虚拟地址。
完。

ioread32_rep 接受 地址、目标、数量：
    可变 i 为 0。
    只要 i 小于 数量：
        做 内存写32 用 目标 加 i 乘 4、做 ioread32 用 地址。
        i 为 i 加 1。
    完。
完。

iowrite32_rep 接受 地址、源、数量：
    可变 i 为 0。
    只要 i 小于 数量：
        做 iowrite32 用 地址、做 内存读32 用 源 加 i 乘 4。
        i 为 i 加 1。
    完。
完。

可变 ioremap_映射数 为 0。

IO_类型名 接受 地址：
    若 地址 小于或等于 IO_SPACE_LIMIT：
        返还 「PIO」。
    完。
    返还 「MMIO」。
完。

ioremap_统计 接受 零参：
    返还 一组：ioremap_映射数、ioremap_下一个虚拟地址。
完。

IO原语_完整统计 接受 零参：
    返还 一组：IO_读次数、IO_写次数。
完。

本章 收束。
