《图处显示》

引用《..·感知·样式解析》。

引用《..·感知·文档节点》。

引用《..·执行·内核界面》。

引用《.·图层合成Alpha混合》。

引用《..·标准·原生字符串》。

引用《.·显基设置颜》。

引用《.·显示管理控》。

引用《.·绘制引擎》。

引用《.·写寄存器》。

引用《.·DRM显示管理》。

引用《..·感知·DRM内核显示》。

引用《.·原生样式系统》。

引用《..·中枢·整数转字符串》。

引用《.·窗口合成器》。

引用《..·执行·扫描磁盘》。

显示档 是 一种 事物，含 名称、宽、高、位深、帧缓、设备、模式、最近树、最近障树、模式切换数、绘制数、文本数、控制数、翻页数。

显示摘要 是 一种 事物，含 名称、宽、高、位深、模式切换数、绘制数、文本数、控制数、翻页数、节点数、交互数、健康。

可变 显示账本 为 一组：（空）。

可变 显示建数 为 0。

显示布尔串 接受 值：
    若 值：
        返还 「true」。
    完。
    返还 「false」。
完。

图处显示_建档 接受 名称、宽、高、位深：
    档 为 做 堆_分配 用 显示档。
    档 的 名称 为 名称。
    档 的 宽 为 宽。
    档 的 高 为 高。
    档 的 位深 为 位深。
    档 的 帧缓 为 0。
    档 的 设备 为 0。
    档 的 模式 为 0。
    档 的 最近树 为 0。
    档 的 最近障树 为 0。
    档 的 模式切换数 为 0。
    档 的 绘制数 为 0。
    档 的 文本数 为 0。
    档 的 控制数 为 0。
    档 的 翻页数 为 0。
    返还 档。
完。

图处显示_创建 接受 名称、宽、高、位深：
    做 DRM_初始化。
    做 BGA_初始化模式 用 宽、高、位深。
    做 帧缓冲_初始化。
    做 合成器_初始化 用 宽、高、位深、0。
    档 为 做 图处显示_建档 用 名称、宽、高、位深。
    做 追加 用 显示账本、档。
    显示建数 为 显示建数 加 1。
    返还 档。
完。

图处显示_自动创建 接受 名称：
    模式 为 做 屏幕_自动检测分辨率。
    宽 为 模式 的 第 0 项。
    高 为 模式 的 第 1 项。
    位深 为 模式 的 第 2 项。
    返还 做 图处显示_创建 用 名称、宽、高、位深。
完。

图处显示_名称 接受 档：
    返还 档 的 名称。
完。

图处显示_宽 接受 档：
    返还 档 的 宽。
完。

图处显示_高 接受 档：
    返还 档 的 高。
完。

图处显示_位深 接受 档：
    返还 档 的 位深。
完。

图处显示_设分辨率 接受 档、宽、高、位深：
    做 屏幕_设置分辨率 用 宽、高、位深。
    做 BGA_初始化模式 用 宽、高、位深。
    档 的 宽 为 宽。
    档 的 高 为 高。
    档 的 位深 为 位深。
    档 的 模式切换数 为 档 的 模式切换数 加 1。
完。

图处显示_注册设备 接受 档、vid、did、总线、设备号、功能号：
    档 的 设备 为 做 DRM_注册图处 用 vid、did、总线、设备号、功能号。
    做 DRM_创建默认对象 用 档 的 设备。
    档 的 控制数 为 档 的 控制数 加 1。
    返还 档 的 设备。
完。

图处显示_创建模式 接受 档、宽、高、hz、名称：
    档 的 模式 为 做 DRM_创建模式 用 宽、高、hz、名称。
    档 的 模式切换数 为 档 的 模式切换数 加 1。
    返还 档 的 模式。
完。

图处显示_设备控制 接受 档、fd、cmd、参数：
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制 用 fd、cmd、参数。
完。

图处显示_设备版本 接受 档、dev、参数：
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制_version 用 dev、参数。
完。

图处显示_设备能力 接受 档、dev、参数：
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制_get_cap 用 dev、参数。
完。

图处显示_资源 接受 档、dev、参数：
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制_getresources 用 dev、参数。
完。

图处显示_连接器 接受 档、dev、参数：
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制_getconnector 用 dev、参数。
完。

图处显示_建哑缓冲 接受 档、dev、参数：
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制_create_dumb 用 dev、参数。
完。

图处显示_映哑缓冲 接受 档、dev、参数：
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制_map_dumb 用 dev、参数。
完。

图处显示_加帧缓冲 接受 档、dev、参数：
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制_addfb 用 dev、参数。
完。

图处显示_设CRTC 接受 档、dev、参数：
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制_setcrtc 用 dev、参数。
完。

图处显示_翻页 接受 档、dev、参数：
    档 的 翻页数 为 档 的 翻页数 加 1。
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制_pageflip 用 dev、参数。
完。

图处显示_删帧缓冲 接受 档、dev、参数：
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制_rmfb 用 dev、参数。
完。

图处显示_销哑缓冲 接受 档、dev、参数：
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制_destroy_dumb 用 dev、参数。
完。

图处显示_原子提交 接受 档、dev、参数：
    档 的 控制数 为 档 的 控制数 加 1。
    返还 做 DRM_Nova设备控制_atomic 用 dev、参数。
完。

图处显示_画像素 接受 档、x、y、颜色：
    做 帧缓冲_画像素 用 x、y、颜色。
    档 的 绘制数 为 档 的 绘制数 加 1。
完。

图处显示_读像素 接受 档、x、y：
    返还 做 帧缓冲_读像素 用 x、y。
完。

图处显示_填矩形 接受 档、x、y、宽、高、颜色：
    做 帧缓冲_填充矩形 用 x、y、宽、高、颜色。
    档 的 绘制数 为 档 的 绘制数 加 1。
完。

图处显示_清屏 接受 档：
    做 显基_清屏。
    档 的 文本数 为 档 的 文本数 加 1。
完。

图处显示_设颜色 接受 档、前景、背景：
    做 显基_设置颜色 用 前景、背景。
    档 的 文本数 为 档 的 文本数 加 1。
完。

图处显示_写字符 接受 档、字符码：
    做 显基_写字符 用 字符码。
    档 的 文本数 为 档 的 文本数 加 1。
完。

图处显示_写文本 接受 档、文本：
    做 显基_写字符串 用 文本。
    档 的 文本数 为 档 的 文本数 加 1。
完。

图处显示_打印行 接受 档、文本：
    做 显基_打印行 用 文本。
    档 的 文本数 为 档 的 文本数 加 1。
完。

图处显示_彩色打印 接受 档、文本、前景、背景：
    做 显基_彩色打印 用 文本、前景、背景。
    档 的 文本数 为 档 的 文本数 加 1。
完。

图处显示_错误 接受 档、文本：
    做 显基_错误 用 文本。
    档 的 文本数 为 档 的 文本数 加 1。
完。

图处显示_警告 接受 档、文本：
    做 显基_警告 用 文本。
    档 的 文本数 为 档 的 文本数 加 1。
完。

图处显示_成功 接受 档、文本：
    做 显基_成功 用 文本。
    档 的 文本数 为 档 的 文本数 加 1。
完。

图处显示_信息 接受 档、文本：
    做 显基_信息 用 文本。
    档 的 文本数 为 档 的 文本数 加 1。
完。

图处显示_设光标 接受 档、行、列：
    做 显基_设置光标位置 用 行、列。
    档 的 文本数 为 档 的 文本数 加 1。
完。

图处显示_显光标 接受 档、起始行、结束行：
    做 显基_显示光标 用 起始行、结束行。
完。

图处显示_隐光标 接受 档：
    做 显基_隐藏光标。
完。

图处显示_块光标 接受 档：
    做 显基_块状光标。
完。

图处显示_下划线光标 接受 档：
    做 显基_下划线光标。
完。

图处显示_样式默认 接受 控件类型：
    返还 做 样式_默认 用 控件类型。
完。

图处显示_布局计算 接受 元素、容器x、容器y、容器宽、容器高：
    做 布局_计算 用 元素、容器x、容器y、容器宽、容器高。
完。

图处显示_布局块 接受 元素：
    做 布局_块 用 元素。
完。

图处显示_布局弹性 接受 元素：
    做 布局_弹性 用 元素。
完。

图处显示_绘制树 接受 档、元素、fb：
    做 绘制_元素树 用 元素、fb。
    档 的 最近树 为 元素。
    档 的 绘制数 为 档 的 绘制数 加 1。
完。

图处显示_绘边框 接受 档、fb、x、y、w、h、颜色、宽度：
    做 绘制_边框 用 fb、x、y、w、h、颜色、宽度。
    档 的 绘制数 为 档 的 绘制数 加 1。
完。

图处显示_绘文本 接受 档、fb、x、y、文本、颜色、字号：
    做 绘制_文本 用 fb、x、y、文本、颜色、字号。
    档 的 绘制数 为 档 的 绘制数 加 1。
    档 的 文本数 为 档 的 文本数 加 1。
完。

图处显示_绘字符 接受 档、fb、x、y、字符码、颜色、字号：
    做 绘制_字符 用 fb、x、y、字符码、颜色、字号。
    档 的 绘制数 为 档 的 绘制数 加 1。
完。

图处显示_构障树 接受 档、元素：
    档 的 最近障树 为 做 构建无障碍树 用 元素。
    返还 档 的 最近障树。
完。

图处显示_交互元素 接受 档、障树：
    返还 做 无障碍_查找可交互元素 用 障树。
完。

图处显示_按坐标找 接受 档、障树、x、y：
    返还 做 无障碍_按坐标查找 用 障树、x、y。
完。

图处显示_文本摘取 接受 元素：
    返还 做 元素_提取文本 用 元素。
完。

图处显示_切换模式 接受 档、宽、高、位深、hz：
    做 图处显示_设分辨率 用 档、宽、高、位深。
    做 图处显示_创建模式 用 档、宽、高、hz、「主模」。
完。

图处显示_刷新 接受 档、dev、参数：
    做 图处显示_翻页 用 档、dev、参数。
完。

图处显示_摘要 接受 档：
    摘 为 做 堆_分配 用 显示摘要。
    摘 的 名称 为 档 的 名称。
    摘 的 宽 为 档 的 宽。
    摘 的 高 为 档 的 高。
    摘 的 位深 为 档 的 位深。
    摘 的 模式切换数 为 档 的 模式切换数。
    摘 的 绘制数 为 档 的 绘制数。
    摘 的 文本数 为 档 的 文本数。
    摘 的 控制数 为 档 的 控制数。
    摘 的 翻页数 为 档 的 翻页数。
    摘 的 节点数 为 0。
    摘 的 交互数 为 0。
    若 档 的 最近树 不等于 0：
        摘 的 节点数 为 1。
    完。
    若 档 的 最近障树 不等于 0：
        交互 为 做 图处显示_交互元素 用 档、档 的 最近障树。
        摘 的 交互数 为 做 计数 用 交互。
    完。
    摘 的 健康 为 做 图处显示_健康检查 用 档。
    返还 摘。
完。

图处显示_摘要串 接受 档：
    摘 为 做 图处显示_摘要 用 档。
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"名称\":\"」。
    做 字符串构建器_追加 用 建、摘 的 名称。
    做 字符串构建器_追加 用 建、「\",\"宽\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 宽。
    做 字符串构建器_追加 用 建、「,\"高\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 高。
    做 字符串构建器_追加 用 建、「,\"位深\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 位深。
    做 字符串构建器_追加 用 建、「,\"模式切换数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 模式切换数。
    做 字符串构建器_追加 用 建、「,\"绘制数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 绘制数。
    做 字符串构建器_追加 用 建、「,\"文本数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 文本数。
    做 字符串构建器_追加 用 建、「,\"控制数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 控制数。
    做 字符串构建器_追加 用 建、「,\"翻页数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 翻页数。
    做 字符串构建器_追加 用 建、「,\"交互数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 交互数。
    做 字符串构建器_追加 用 建、「,\"健康\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 健康。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

图处显示_批量摘要串 接受 列表：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「[」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 i 大于 0：
            做 字符串构建器_追加 用 建、「,」。
        完。
        做 字符串构建器_追加 用 建、做 图处显示_摘要串 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    做 字符串构建器_追加 用 建、「]」。
    返还 做 字符串构建器_构建 用 建。
完。

图处显示_统计串 接受 档：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"控制\":\"」。
    做 字符串构建器_追加 用 建、做 ioctl_统计。
    做 字符串构建器_追加 用 建、「\",\"控制健康\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 ioctl_健康检查。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

图处显示_健康检查 接受 档：
    可变 错数 为 0。
    若 档 等于 0：
        返还 1。
    完。
    若 档 的 宽 小于或等于 0 或者 档 的 高 小于或等于 0：
        错数 为 错数 加 1。
    完。
    若 档 的 位深 小于或等于 0：
        错数 为 错数 加 1。
    完。
    若 做 ioctl_健康检查 不等于 0：
        错数 为 错数 加 1。
    完。
    返还 错数。
完。

图处显示_账本健康检查 接受 零参：
    可变 错数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 显示账本：
        错数 为 错数 加 做 图处显示_健康检查 用 显示账本 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 错数。
完。

图处显示_总览 接受 零参：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"建数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 显示建数。
    做 字符串构建器_追加 用 建、「,\"账本数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 计数 用 显示账本。
    做 字符串构建器_追加 用 建、「,\"健康错误\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 图处显示_账本健康检查。
    做 字符串构建器_追加 用 建、「,\"明细\":」。
    做 字符串构建器_追加 用 建、做 图处显示_批量摘要串 用 显示账本。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

图处显示_示例流程 接受 零参：
    档 为 做 图处显示_创建 用 「主显示」、1024、768、32。
    做 图处显示_设颜色 用 档、显基_亮青、显基_黑。
    做 图处显示_清屏 用 档。
    做 图处显示_打印行 用 档、「觉醒显示管线已启动」。
    做 图处显示_填矩形 用 档、10、10、120、40、0x00FFAA。
    做 图处显示_绘文本 用 档、0、24、24、「Awake」、0xFFFFFF、16。
    做 图处显示_块光标 用 档。
    做 图处显示_切换模式 用 档、1280、720、32、60。
    返还 做 图处显示_总览。
完。

可变 AI_图处显示_运行指标 为 0。

可变 AI_图处显示_异常指标 为 0。

AI_图处显示_自诊断 接受 零参：
    AI_图处显示_运行指标 为 0。
    返还 AI_图处显示_运行指标。
完。

AI_图处显示_优化建议 接受 零参：
    指标 为 做 AI_图处显示_自诊断。
    返还 「图处显示状态正常」。
完。

本章 收束。
