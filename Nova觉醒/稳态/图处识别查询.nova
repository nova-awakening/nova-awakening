《图处识别查询》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《.·GPU命令队列驱动》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内核打印》。

引用《..·引导·端口IO》。

引用《..·中枢·整数转字符串》。

图处_CAP_3D渲染 为 0x01。

图处_CAP_视频解码 为 0x02。

图处_CAP_视频编码 为 0x04。

图处_CAP_计算 为 0x08。

图处_CAP_智能_ACCEL 为 0x10。

图处_CAP_光线追踪 为 0x20。

图处_CAP_张量核心 为 0x40。

图处_CAP_显示输出 为 0x80。

图处_供应商_NVIDIA 为 0x10DE。

图处_供应商_AMD 为 0x1002。

图处_供应商_INTEL 为 0x8086。

图处_供应商_VIRTIO 为 0x1AF4。

图处信息 是 一种 事物，含 供应商ID、设备ID、修订版本、显存大小_MB、计算单元数、核心频率_MHz、显存频率_MHz、能力、总线号、设备号、功能号、MMIO基址、MMIO大小、温度、功耗_mW、驱动版本、名称。

可变 图处列表 为 一组：（空）。

可变 主图处 为 空。

可变 计算图处 为 空。

可变 图处_已检测 为 0。

可变 图处_供应商ID 为 0。

可变 图处_设备ID 为 0。

图处_数量 接受 零参：
    返还 做 计数 用 图处列表。
完。

图处_获取主设备 接受 零参：
    返还 主图处。
完。

图处_获取计算设备 接受 零参：
    返还 计算图处。
完。

图处_有AI加速 接受 零参：
    若 计算图处 等于 空：
        返还 假。
    完。
    返还 计算图处 的 能力 位与 图处_CAP_智能_ACCEL 不等于 0。
完。

图处_有光线追踪 接受 零参：
    若 主图处 等于 空：
        返还 假。
    完。
    返还 主图处 的 能力 位与 图处_CAP_光线追踪 不等于 0。
完。

图处_有张量核心 接受 零参：
    若 计算图处 等于 空：
        返还 假。
    完。
    返还 计算图处 的 能力 位与 图处_CAP_张量核心 不等于 0。
完。

图处_获取列表 接受 零参：
    返还 图处列表。
完。

图处_按索引获取 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 做 计数 用 图处列表：
        返还 空。
    完。
    返还 图处列表 的 第 索引 项。
完。

图处_按供应商查找 接受 供应商ID：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 图处列表：
        若 图处列表 的 第 i 项 的 供应商ID 等于 供应商ID：
            做 追加 用 结果、图处列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

图处_获取最强计算 接受 零参：
    可变 最强 为 空。
    可变 最大计算力 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 图处列表：
        g 为 图处列表 的 第 i 项。
        若 g 的 能力 位与 图处_CAP_计算 不等于 0：
            计算力 为 g 的 计算单元数 乘 g 的 核心频率_MHz。
            若 计算力 大于 最大计算力：
                最大计算力 为 计算力。
                最强 为 g。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 最强。
完。

图处_总显存_MB 接受 零参：
    可变 总计 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 图处列表：
        总计 为 总计 加 图处列表 的 第 i 项 的 显存大小_MB。
        i 为 i 加 1。
    完。
    返还 总计。
完。

图处_统计 接受 零参：
    返还 一组：做 计数 用 图处列表、若 主图处 不等于 空 则 主图处 的 显存大小_MB 否则 0、若 计算图处 不等于 空 则 计算图处 的 计算单元数 否则 0。
完。

图处_供应商名称 接受 供应商ID：
    若 供应商ID 等于 图处_供应商_NVIDIA：
        返还 「NVIDIA」。
    完。
    若 供应商ID 等于 图处_供应商_AMD：
        返还 「AMD」。
    完。
    若 供应商ID 等于 图处_供应商_INTEL：
        返还 「Intel」。
    完。
    若 供应商ID 等于 图处_供应商_VIRTIO：
        返还 「VirtIO」。
    完。
    返还 「未知」。
完。

图处_能力字符串 接受 能力：
    可变 结果 为 「」。
    若 能力 位与 图处_CAP_3D渲染 不等于 0：
        结果 为 结果 加 「3D 」。
    完。
    若 能力 位与 图处_CAP_视频解码 不等于 0：
        结果 为 结果 加 「解码 」。
    完。
    若 能力 位与 图处_CAP_视频编码 不等于 0：
        结果 为 结果 加 「编码 」。
    完。
    若 能力 位与 图处_CAP_计算 不等于 0：
        结果 为 结果 加 「计算 」。
    完。
    若 能力 位与 图处_CAP_智能_ACCEL 不等于 0：
        结果 为 结果 加 「AI 」。
    完。
    若 能力 位与 图处_CAP_光线追踪 不等于 0：
        结果 为 结果 加 「光追 」。
    完。
    若 能力 位与 图处_CAP_张量核心 不等于 0：
        结果 为 结果 加 「张量 」。
    完。
    若 能力 位与 图处_CAP_显示输出 不等于 0：
        结果 为 结果 加 「显示 」。
    完。
    返还 结果。
完。

图处_读温度 接受 图处：
    若 图处 等于 空 或者 图处 的 MMIO基址 等于 0：
        返还 负 1。
    完。
    若 图处 的 供应商ID 等于 图处_供应商_NVIDIA：
        返还 做 MMIO_读32 用 图处 的 MMIO基址 加 0x20400 位与 0x1FF。
    完。
    若 图处 的 供应商ID 等于 图处_供应商_AMD：
        原始 为 做 MMIO_读32 用 图处 的 MMIO基址 加 0xC0300E0C。
        返还 原始 右移 21 位与 0x7FF。
    完。
    返还 负 1。
完。

图处_读功耗 接受 图处：
    若 图处 等于 空 或者 图处 的 MMIO基址 等于 0：
        返还 负 1。
    完。
    若 图处 的 供应商ID 等于 图处_供应商_NVIDIA：
        返还 做 MMIO_读32 用 图处 的 MMIO基址 加 0x20800 位与 0xFFFF。
    完。
    返还 负 1。
完。

图处_更新传感器 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 图处列表：
        g 为 图处列表 的 第 i 项。
        g 的 温度 为 做 图处_读温度 用 g。
        g 的 功耗_mW 为 做 图处_读功耗 用 g。
        i 为 i 加 1。
    完。
完。

图处_打印信息 接受 图处：
    若 图处 等于 空：
        返还。
    完。
    做 打印行 用 「=== GPU: 」 加 图处 的 名称 加 「 ===」。
    做 打印行 用 「供应商: 」 加 做 图处_供应商名称 用 图处 的 供应商ID 加 「 (0x」 加 做 整数转十六进制 用 图处 的 供应商ID 加 「)」。
    做 打印行 用 「设备ID: 0x」 加 做 整数转十六进制 用 图处 的 设备ID。
    做 打印行 用 「显存: 」 加 做 整数转字符串 用 图处 的 显存大小_MB 加 「 MB」。
    做 打印行 用 「计算单元: 」 加 做 整数转字符串 用 图处 的 计算单元数。
    做 打印行 用 「核心频率: 」 加 做 整数转字符串 用 图处 的 核心频率_MHz 加 「 MHz」。
    做 打印行 用 「显存频率: 」 加 做 整数转字符串 用 图处 的 显存频率_MHz 加 「 MHz」。
    做 打印行 用 「能力: 」 加 做 图处_能力字符串 用 图处 的 能力。
    做 打印行 用 「PCI: 」 加 做 整数转字符串 用 图处 的 总线号 加 「:」 加 做 整数转字符串 用 图处 的 设备号 加 「.」 加 做 整数转字符串 用 图处 的 功能号。
    若 图处 的 温度 大于或等于 0：
        做 打印行 用 「温度: 」 加 做 整数转字符串 用 图处 的 温度 加 「°C」。
    完。
    若 图处 的 功耗_mW 大于 0：
        做 打印行 用 「功耗: 」 加 做 整数转字符串 用 图处 的 功耗_mW 除以 1000 加 「.」 加 做 整数转字符串 用 图处 的 功耗_mW 取余于 1000 除以 100 加 「W」。
    完。
完。

图处_打印全部 接受 零参：
    做 打印行 用 「检测到 」 加 做 整数转字符串 用 做 计数 用 图处列表 加 「 个GPU」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 图处列表：
        做 图处_打印信息 用 图处列表 的 第 i 项。
        i 为 i 加 1。
    完。
    做 打印行 用 「总显存: 」 加 做 整数转字符串 用 做 图处_总显存_MB 加 「 MB」。
完。

PCI_配置读32 接受 总线、设备、功能、偏移：
    地址 为 0x80000000 位或 总线 左移 16 位或 设备 左移 11 位或 功能 左移 8 位或 偏移 位与 0xFC。
    做 端口写32 用 0xCF8、地址。
    返还 做 端口读32 用 0xCFC。
完。

PCI_配置写32 接受 总线、设备、功能、偏移、值：
    地址 为 0x80000000 位或 总线 左移 16 位或 设备 左移 11 位或 功能 左移 8 位或 偏移 位与 0xFC。
    做 端口写32 用 0xCF8、地址。
    做 端口写32 用 0xCFC、值。
完。

PCI_读BAR 接受 总线、设备、功能、BAR索引：
    偏移 为 0x10 加 BAR索引 乘 4。
    值 为 做 PCI_配置读32 用 总线、设备、功能、偏移。
    若 值 位与 1 等于 0：
        返还 值 位与 0xFFFFFFF0。
    完。
    返还 值 位与 0xFFFFFFFC。
完。

PCI_BAR大小 接受 总线、设备、功能、BAR索引：
    偏移 为 0x10 加 BAR索引 乘 4。
    原值 为 做 PCI_配置读32 用 总线、设备、功能、偏移。
    做 PCI_配置写32 用 总线、设备、功能、偏移、0xFFFFFFFF。
    掩码 为 做 PCI_配置读32 用 总线、设备、功能、偏移。
    做 PCI_配置写32 用 总线、设备、功能、偏移、原值。
    若 掩码 等于 0 或者 掩码 等于 0xFFFFFFFF：
        返还 0。
    完。
    掩码 为 掩码 位与 0xFFFFFFF0。
    返还 按位取反 掩码 加 1。
完。

PCI_读BAR64 接受 总线、设备、功能、BAR索引：
    低 为 做 PCI_配置读32 用 总线、设备、功能、0x10 加 BAR索引 乘 4。
    高 为 做 PCI_配置读32 用 总线、设备、功能、0x10 加 (BAR索引 加 1) 乘 4。
    返还 (高 位与 0xFFFFFFFF) 左移 32 位或 低 位与 0xFFFFFFF0。
完。

图处识别_完整统计 接受 零参：
    做 图处_更新传感器。
    返还 一组：图处_已检测、图处_供应商ID、图处_设备ID、做 图处_总显存_MB、做 计数 用 图处列表。
完。

本章 收束。
