《AHCI统计》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

最大端口 为 32。

最大命令槽 为 32。

磁控端口 是 一种 事物，含 端口号、基地址、已连接、设备类型、命令表、命令槽数、活跃命令、读扇区数、写扇区数、错误数、总命令数。

可变 控制器基址 为 0。

可变 端口表 为 一组：（空）。

可变 端口数 为 0。

可变 总读扇区 为 0。

可变 总写扇区 为 0。

可变 总错误 为 0。

可变 总命令 为 0。

磁控_初始化 接受 基址：
    控制器基址 为 基址。
    端口表 为 一组：（空）。
    端口数 为 0。
    端口实现 为 做 内存_读32 用 基址 加 0x0C。
    可变 位 为 0。
    只要 位 小于 最大端口：
        若 端口实现 位与 1 左移 位 不等于 0：
            端口基 为 基址 加 0x100 加 位 乘 0x80。
            端口 为 新建 磁控端口。
            端口 的 端口号 为 位。
            端口 的 基地址 为 端口基。
            状态 为 做 内存_读32 用 端口基 加 0x28。
            端口 的 已连接 为 状态 位与 0xF 等于 3。
            端口 的 设备类型 为 状态 右移 4 位与 0xF。
            端口 的 命令表 为 做 堆_分配 用 最大命令槽 乘 32。
            端口 的 命令槽数 为 最大命令槽。
            端口 的 活跃命令 为 0。
            端口 的 读扇区数 为 0。
            端口 的 写扇区数 为 0。
            端口 的 错误数 为 0。
            端口 的 总命令数 为 0。
            做 追加 用 端口表、端口。
            端口数 为 端口数 加 1。
        完。
        位 为 位 加 1。
    完。
完。

磁控_读扇区 接受 端口索引、起始扇区、扇区数、缓冲：
    若 端口索引 大于或等于 端口数：
        返还 错误_无效参数。
    完。
    端口 为 端口表 的 第 端口索引 项。
    若 非 端口 的 已连接：
        返还 错误_设备离线。
    完。
    槽 为 做 磁控_分配命令槽 用 端口。
    若 槽 小于 0：
        返还 错误_无资源。
    完。
    做 磁控_构建读命令 用 端口、槽、起始扇区、扇区数、缓冲。
    做 磁控_发射命令 用 端口、槽。
    结果 为 做 磁控_等待完成 用 端口、槽。
    做 磁控_释放命令槽 用 端口、槽。
    若 结果 等于 0：
        端口 的 读扇区数 为 端口 的 读扇区数 加 扇区数。
        总读扇区 为 总读扇区 加 扇区数。
    否则：
        端口 的 错误数 为 端口 的 错误数 加 1。
        总错误 为 总错误 加 1。
    完。
    端口 的 总命令数 为 端口 的 总命令数 加 1。
    总命令 为 总命令 加 1。
    返还 结果。
完。

磁控_写扇区 接受 端口索引、起始扇区、扇区数、缓冲：
    若 端口索引 大于或等于 端口数：
        返还 错误_无效参数。
    完。
    端口 为 端口表 的 第 端口索引 项。
    若 非 端口 的 已连接：
        返还 错误_设备离线。
    完。
    槽 为 做 磁控_分配命令槽 用 端口。
    若 槽 小于 0：
        返还 错误_无资源。
    完。
    做 磁控_构建写命令 用 端口、槽、起始扇区、扇区数、缓冲。
    做 磁控_发射命令 用 端口、槽。
    结果 为 做 磁控_等待完成 用 端口、槽。
    做 磁控_释放命令槽 用 端口、槽。
    若 结果 等于 0：
        端口 的 写扇区数 为 端口 的 写扇区数 加 扇区数。
        总写扇区 为 总写扇区 加 扇区数。
    否则：
        端口 的 错误数 为 端口 的 错误数 加 1。
        总错误 为 总错误 加 1。
    完。
    端口 的 总命令数 为 端口 的 总命令数 加 1。
    总命令 为 总命令 加 1。
    返还 结果。
完。

磁控_分配命令槽 接受 端口：
    可变 槽 为 0。
    只要 槽 小于 端口 的 命令槽数：
        若 端口 的 活跃命令 位与 1 左移 槽 等于 0：
            端口 的 活跃命令 为 端口 的 活跃命令 位或 1 左移 槽。
            返还 槽。
        完。
        槽 为 槽 加 1。
    完。
    返还 负 1。
完。

磁控_释放命令槽 接受 端口、槽：
    端口 的 活跃命令 为 端口 的 活跃命令 位与 按位取反 (1 左移 槽)。
完。

磁控_构建读命令 接受 端口、槽、扇区、数量、缓冲：
    偏移 为 端口 的 命令表 加 槽 乘 32。
    做 内存_写8 用 偏移、0x25。
    做 内存_写64 用 偏移 加 8、扇区。
    做 内存_写32 用 偏移 加 16、数量。
    做 内存_写64 用 偏移 加 20、缓冲。
完。

磁控_构建写命令 接受 端口、槽、扇区、数量、缓冲：
    偏移 为 端口 的 命令表 加 槽 乘 32。
    做 内存_写8 用 偏移、0x35。
    做 内存_写64 用 偏移 加 8、扇区。
    做 内存_写32 用 偏移 加 16、数量。
    做 内存_写64 用 偏移 加 20、缓冲。
完。

磁控_发射命令 接受 端口、槽：
    做 内存_写32 用 端口 的 基地址 加 0x38、1 左移 槽。
完。

磁控_等待完成 接受 端口、槽：
    可变 等待 为 0。
    只要 等待 小于 1000000：
        已发射 为 做 内存_读32 用 端口 的 基地址 加 0x38。
        若 已发射 位与 1 左移 槽 等于 0：
            返还 0。
        完。
        中断状态 为 做 内存_读32 用 端口 的 基地址 加 0x10。
        若 中断状态 位与 1 左移 30 不等于 0：
            返还 错误_设备错误。
        完。
        等待 为 等待 加 1。
    完。
    返还 错误_超时。
完。

磁控_获取端口数 接受 零参：
    返还 端口数。
完。

磁控_获取端口信息 接受 索引：
    若 索引 大于或等于 端口数：
        返还 一组：0、0、0、0。
    完。
    端口 为 端口表 的 第 索引 项。
    返还 一组：端口 的 端口号、端口 的 已连接、端口 的 读扇区数、端口 的 写扇区数。
完。

磁控_统计 接受 零参：
    返还 一组：端口数、总读扇区、总写扇区、总错误、总命令。
完。

可变 AI_磁控统计_运行指标 为 0。

可变 AI_磁控统计_异常指标 为 0。

AI_磁控统计_自诊断 接受 零参：
    AI_磁控统计_运行指标 为 0。
    返还 AI_磁控统计_运行指标。
完。

AI_磁控统计_优化建议 接受 零参：
    指标 为 做 AI_磁控统计_自诊断。
    返还 「磁控统计状态正常」。
完。

本章 收束。
