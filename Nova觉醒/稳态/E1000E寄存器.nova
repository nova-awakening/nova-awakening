《E1000E寄存器》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

E1000_CTRL 为 0x0000。

E1000_STATUS 为 0x0008。

E1000_EECD 为 0x0010。

E1000_EERD 为 0x0014。

E1000_ICR 为 0x00C0。

E1000_IMS 为 0x00D0。

E1000_IMC 为 0x00D8。

E1000_RCTL 为 0x0100。

E1000_TCTL 为 0x0400。

E1000_RDBAL 为 0x2800。

E1000_RDBAH 为 0x2804。

E1000_RDLEN 为 0x2808。

E1000_RDH 为 0x2810。

E1000_RDT 为 0x2818。

E1000_TDBAL 为 0x3800。

E1000_TDBAH 为 0x3804。

E1000_TDLEN 为 0x3808。

E1000_TDH 为 0x3810。

E1000_TDT 为 0x3818。

E1000_RAL 为 0x5400。

E1000_RAH 为 0x5404。

E1000_MTA 为 0x5200。

E1000_TIPG 为 0x0410。

E1000_CTRL_SLU 为 0x40。

E1000_CTRL_RST 为 0x04000000。

E1000_CTRL_ASDE 为 0x20。

E1000_RCTL_EN 为 0x02。

E1000_RCTL_BAM 为 0x8000。

E1000_RCTL_BSIZE_2048 为 0x00。

E1000_RCTL_BSIZE_4096 为 0x30000。

E1000_RCTL_SECRC 为 0x04000000。

E1000_TCTL_EN 为 0x02。

E1000_TCTL_PSP 为 0x08。

E1000_ICR_TXDW 为 0x01。

E1000_ICR_TXQE 为 0x02。

E1000_ICR_LSC 为 0x04。

E1000_ICR_RXO 为 0x40。

E1000_ICR_RXT0 为 0x80。

E1000_RXD_STAT_DD 为 0x01。

E1000_RXD_STAT_EOP 为 0x02。

E1000_TXD_CMD_EOP 为 0x01。

E1000_TXD_CMD_IFCS 为 0x02。

E1000_TXD_CMD_RS 为 0x08。

E1000_TXD_STAT_DD 为 0x01。

e1000_rx_desc 是 一种 事物，含 缓冲地址、长度、校验和、状态、错误、特殊。

e1000_tx_desc 是 一种 事物，含 缓冲地址、长度、CSO、命令、状态、CSS、特殊。

e1000设备 是 一种 事物，含 MMIO基址、MAC地址、IRQ、RX描述符环、TX描述符环、RX缓冲列表、TX缓冲列表、RX环大小、TX环大小、RX尾、TX尾、链路速度、链路状态、发送字节、接收字节、发送包数、接收包数。

本章 收束。
