《键盘》

引用《..·中枢·日志中枢》。

引用《..·中枢·时钟中枢》。

引用《..·引导·端口IO》。

PS2_数据端口 为 0x60。

PS2_状态端口 为 0x64。

PS2_命令端口 为 0x64。

PS2_CMD_读配置 为 0x20。

PS2_CMD_写配置 为 0x60。

PS2_CMD_禁用端口2 为 0xA7。

PS2_CMD_启用端口2 为 0xA8。

PS2_CMD_自检 为 0xAA。

PS2_CMD_禁用端口1 为 0xAD。

PS2_CMD_启用端口1 为 0xAE。

SC_ESC 为 0x01。

SC_BACKSPACE 为 0x0E。

SC_TAB 为 0x0F。

SC_ENTER 为 0x1C。

SC_LCTRL 为 0x1D。

SC_LSHIFT 为 0x2A。

SC_RSHIFT 为 0x36。

SC_LALT 为 0x38。

SC_CAPSLOCK 为 0x3A。

SC_F1 为 0x3B。

SC_F12 为 0x58。

SC_NUMLOCK 为 0x45。

SC_SCROLLLOCK 为 0x46。

SC_EXTENDED 为 0xE0。

可变 键盘_shift 为 假。

可变 键盘_ctrl 为 假。

可变 键盘_alt 为 假。

可变 键盘_capslock 为 假。

键盘缓冲大小 为 256。

可变 键盘缓冲 为 一组：（空）。

可变 键盘缓冲_头 为 0。

可变 键盘缓冲_尾 为 0。

可变 键盘_扩展标志 为 假。

可变 键盘_中断计数 为 0。

可变 键盘_按键总数 为 0。

可变 键盘_numlock 为 假。

可变 键盘_scrolllock 为 假。

扫描码表_小写 为 一组：0、27、49、50、51、52、53、54、55、56、57、48、45、61、8、9、113、119、101、114、116、121、117、105、111、112、91、93、10、0、97、115、100、102、103、104、106、107、108、59、39、96、0、92、122、120、99、118、98、110、109、44、46、47、0、42、0、32。

扫描码表_大写 为 一组：0、27、33、64、35、36、37、94、38、42、40、41、95、43、8、9、81、87、69、82、84、89、85、73、79、80、123、125、10、0、65、83、68、70、71、72、74、75、76、58、34、126、0、124、90、88、67、86、66、78、77、60、62、63、0、42、0、32。

键盘_初始化 接受 零参：
    键盘缓冲 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 键盘缓冲大小：
        做 追加 用 键盘缓冲、0。
        i 为 i 加 1。
    完。
    键盘缓冲_头 为 0。
    键盘缓冲_尾 为 0。
    键盘_扩展标志 为 假。
    键盘_shift 为 假。
    键盘_ctrl 为 假。
    键盘_alt 为 假。
    键盘_capslock 为 假。
    只要 做 端口读8 用 PS2_状态端口 位与 0x01 不等于 0：
        做 端口读8 用 PS2_数据端口。
    完。
    做 ps2_发命令 用 PS2_CMD_启用端口1。
    做 ps2_发命令 用 PS2_CMD_读配置。
    配置 为 做 ps2_等待读数据。
    做 ps2_发命令 用 PS2_CMD_写配置。
    做 ps2_写数据 用 配置 位或 0x01。
    做 内核信息 用 「PS/2键盘: 初始化完成 (IRQ1)」。
    返还 成功。
完。

键盘_中断处理 接受 IRQ号：
    扫描码 为 做 端口读8 用 PS2_数据端口。
    键盘_中断计数 为 键盘_中断计数 加 1。
    若 扫描码 等于 SC_EXTENDED：
        键盘_扩展标志 为 真。
        返还。
    完。
    按下 为 扫描码 位与 0x80 等于 0。
    码 为 扫描码 位与 0x7F。
    若 码 等于 SC_LSHIFT 或者 码 等于 SC_RSHIFT：
        键盘_shift 为 按下。
        键盘_扩展标志 为 假。
        返还。
    完。
    若 码 等于 SC_LCTRL：
        键盘_ctrl 为 按下。
        键盘_扩展标志 为 假。
        返还。
    完。
    若 码 等于 SC_LALT：
        键盘_alt 为 按下。
        键盘_扩展标志 为 假。
        返还。
    完。
    若 码 等于 SC_CAPSLOCK 并且 按下：
        键盘_capslock 为 非 键盘_capslock。
        键盘_扩展标志 为 假。
        返还。
    完。
    若 非 按下：
        键盘_扩展标志 为 假。
        返还。
    完。
    若 键盘_ctrl 并且 码 等于 0x2E：
        键盘_扩展标志 为 假。
        返还。
    完。
    若 键盘_扩展标志：
        键盘_扩展标志 为 假。
        若 码 等于 0x48：
            做 缓冲_写入3 用 27、91、65。
            返还。
        完。
        若 码 等于 0x50：
            做 缓冲_写入3 用 27、91、66。
            返还。
        完。
        若 码 等于 0x4D：
            做 缓冲_写入3 用 27、91、67。
            返还。
        完。
        若 码 等于 0x4B：
            做 缓冲_写入3 用 27、91、68。
            返还。
        完。
        若 码 等于 0x47：
            做 缓冲_写入3 用 27、91、72。
            返还。
        完。
        若 码 等于 0x4F：
            做 缓冲_写入3 用 27、91、70。
            返还。
        完。
        若 码 等于 0x53：
            做 缓冲_写入4 用 27、91、51、126。
            返还。
        完。
        返还。
    完。
    ascii 为 做 扫描码_转ASCII 用 码。
    若 ascii 大于 0：
        做 缓冲_写入 用 ascii。
    完。
完。

扫描码_转ASCII 接受 码：
    若 码 大于或等于 做 计数 用 扫描码表_小写：
        返还 0。
    完。
    可变 ascii 为 0。
    若 键盘_shift：
        若 码 小于 做 计数 用 扫描码表_大写：
            ascii 为 扫描码表_大写 的 第 码 项。
        完。
    否则：
        ascii 为 扫描码表_小写 的 第 码 项。
    完。
    若 键盘_capslock 并且 ascii 大于或等于 97 并且 ascii 小于或等于 122：
        ascii 为 ascii 减 32。
    否则若 键盘_capslock 并且 ascii 大于或等于 65 并且 ascii 小于或等于 90：
        ascii 为 ascii 加 32。
    完。
    若 键盘_ctrl 并且 ascii 大于或等于 97 并且 ascii 小于或等于 122：
        ascii 为 ascii 减 96。
    完。
    返还 ascii。
完。

缓冲_写入 接受 字节：
    下一头 为 (键盘缓冲_头 加 1) 取余于 键盘缓冲大小。
    若 下一头 等于 键盘缓冲_尾：
        返还。
    完。
    做 设置 用 键盘缓冲、键盘缓冲_头、字节。
    键盘缓冲_头 为 下一头。
完。

缓冲_写入3 接受 a、b、c：
    做 缓冲_写入 用 a。
    做 缓冲_写入 用 b。
    做 缓冲_写入 用 c。
完。

缓冲_写入4 接受 a、b、c、d：
    做 缓冲_写入 用 a。
    做 缓冲_写入 用 b。
    做 缓冲_写入 用 c。
    做 缓冲_写入 用 d。
完。

键盘_读取 接受 零参：
    若 键盘缓冲_头 等于 键盘缓冲_尾：
        返还 负 1。
    完。
    字节 为 键盘缓冲 的 第 键盘缓冲_尾 项。
    键盘缓冲_尾 为 (键盘缓冲_尾 加 1) 取余于 键盘缓冲大小。
    返还 字节。
完。

键盘_可读 接受 零参：
    返还 键盘缓冲_头 不等于 键盘缓冲_尾。
完。

键盘_读取扫描码 接受 零参：
    返还 做 键盘_读取。
完。

ps2_等待输入空 接受 零参：
    可变 超时 为 100000。
    只要 超时 大于 0 并且 做 端口读8 用 PS2_状态端口 位与 0x02 不等于 0：
        超时 为 超时 减 1。
    完。
完。

ps2_等待输出满 接受 零参：
    可变 超时 为 100000。
    只要 超时 大于 0 并且 做 端口读8 用 PS2_状态端口 位与 0x01 等于 0：
        超时 为 超时 减 1。
    完。
完。

ps2_发命令 接受 命令：
    做 ps2_等待输入空。
    做 端口写8 用 PS2_命令端口、命令。
完。

ps2_写数据 接受 数据：
    做 ps2_等待输入空。
    做 端口写8 用 PS2_数据端口、数据。
完。

ps2_等待读数据 接受 零参：
    做 ps2_等待输出满。
    返还 做 端口读8 用 PS2_数据端口。
完。

控制台_读取 接受 缓冲、大小：
    可变 已读 为 0。
    只要 已读 小于 大小：
        只要 非 做 键盘_可读：
            做 暂停。
        完。
        ch 为 做 键盘_读取。
        若 ch 小于 0：
            中断。
        完。
        做 设置字节 用 缓冲、已读、ch。
        已读 为 已读 加 1。
        若 ch 等于 10：
            中断。
        完。
    完。
    返还 已读。
完。

PS2_CMD_设置LED 为 0xED。

键盘_更新LED 接受 零参：
    可变 led状态 为 0。
    若 键盘_scrolllock：
        led状态 为 led状态 位或 0x01。
    完。
    若 键盘_numlock：
        led状态 为 led状态 位或 0x02。
    完。
    若 键盘_capslock：
        led状态 为 led状态 位或 0x04。
    完。
    做 ps2_写数据 用 PS2_CMD_设置LED。
    做 ps2_等待读数据。
    做 ps2_写数据 用 led状态。
    做 ps2_等待读数据。
完。

可变 typematic_当前键 为 0。

可变 typematic_开始时间 为 0。

可变 typematic_上次重复 为 0。

可变 typematic_延迟 为 500。

可变 typematic_间隔 为 33。

typematic_按下 接受 按键ASCII：
    typematic_当前键 为 按键ASCII。
    typematic_开始时间 为 做 读取毫秒。
    typematic_上次重复 为 0。
完。

typematic_释放 接受 按键ASCII：
    若 typematic_当前键 等于 按键ASCII：
        typematic_当前键 为 0。
    完。
完。

typematic_检查 接受 零参：
    若 typematic_当前键 等于 0：
        返还 0。
    完。
    现在 为 做 读取毫秒。
    经过 为 现在 减 typematic_开始时间。
    若 经过 小于 typematic_延迟：
        返还 0。
    完。
    若 typematic_上次重复 等于 0：
        typematic_上次重复 为 现在。
        返还 typematic_当前键。
    完。
    若 现在 减 typematic_上次重复 大于或等于 typematic_间隔：
        typematic_上次重复 为 现在。
        返还 typematic_当前键。
    完。
    返还 0。
完。

键盘_设置重复速率 接受 延迟ms、间隔ms：
    若 延迟ms 大于或等于 250 并且 延迟ms 小于或等于 1000：
        typematic_延迟 为 延迟ms。
    完。
    若 间隔ms 大于或等于 33 并且 间隔ms 小于或等于 500：
        typematic_间隔 为 间隔ms。
    完。
完。

最大热键 为 32。

可变 热键表_键码 为 一组：（空）。

可变 热键表_修饰符 为 一组：（空）。

可变 热键表_处理 为 一组：（空）。

可变 热键_已注册 为 0。

键盘_注册热键 接受 键码、修饰符、处理函数：
    若 热键_已注册 大于或等于 最大热键：
        返还 0 减 1。
    完。
    做 追加 用 热键表_键码、键码。
    做 追加 用 热键表_修饰符、修饰符。
    做 追加 用 热键表_处理、处理函数。
    热键_已注册 为 热键_已注册 加 1。
    返还 0。
完。

热键_检查 接受 键码：
    可变 当前修饰 为 0。
    若 键盘_ctrl：
        当前修饰 为 当前修饰 位或 1。
    完。
    若 键盘_alt：
        当前修饰 为 当前修饰 位或 2。
    完。
    可变 i 为 0。
    只要 i 小于 热键_已注册：
        若 热键表_键码 的 第 i 项 等于 键码 并且 热键表_修饰符 的 第 i 项 等于 当前修饰：
            做 热键表_处理 的 第 i 项。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

事件_按下 为 1。

事件_释放 为 0。

最大事件 为 64。

可变 事件队列_数据 为 一组：（空）。

可变 事件队列_头 为 0。

可变 事件队列_尾 为 0。

可变 事件队列_满次数 为 0。

事件队列_初始化 接受 零参：
    事件队列_数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大事件 乘 4：
        做 追加 用 事件队列_数据、0。
        i 为 i 加 1。
    完。
    事件队列_头 为 0。
    事件队列_尾 为 0。
完。

事件队列_推入 接受 扫描码、状态：
    下一头 为 (事件队列_头 加 1) 取余于 最大事件。
    若 下一头 等于 事件队列_尾：
        事件队列_满次数 为 事件队列_满次数 加 1。
        返还。
    完。
    base 为 事件队列_头 乘 4。
    可变 修饰状态 为 0。
    若 键盘_shift：
        修饰状态 为 修饰状态 位或 1。
    完。
    若 键盘_ctrl：
        修饰状态 为 修饰状态 位或 2。
    完。
    若 键盘_alt：
        修饰状态 为 修饰状态 位或 4。
    完。
    做 设置 用 事件队列_数据、base、扫描码。
    做 设置 用 事件队列_数据、base 加 1、状态。
    做 设置 用 事件队列_数据、base 加 2、修饰状态。
    做 设置 用 事件队列_数据、base 加 3、键盘_中断计数。
    事件队列_头 为 下一头。
完。

事件队列_弹出 接受 零参：
    若 事件队列_头 等于 事件队列_尾：
        返还 空。
    完。
    base 为 事件队列_尾 乘 4。
    事件 为 一组：事件队列_数据 的 第 base 项、事件队列_数据 的 第 base 加 1 项、事件队列_数据 的 第 base 加 2 项、事件队列_数据 的 第 base 加 3 项。
    事件队列_尾 为 (事件队列_尾 加 1) 取余于 最大事件。
    返还 事件。
完。

事件队列_可读 接受 零参：
    返还 事件队列_头 不等于 事件队列_尾。
完。

键盘_增强中断 接受 扫描码_原始：
    按下 为 扫描码_原始 位与 0x80 等于 0。
    码 为 扫描码_原始 位与 0x7F。
    若 码 等于 SC_NUMLOCK 并且 按下：
        键盘_numlock 为 非 键盘_numlock。
        做 键盘_更新LED。
    完。
    若 码 等于 SC_SCROLLLOCK 并且 按下：
        键盘_scrolllock 为 非 键盘_scrolllock。
        做 键盘_更新LED。
    完。
    若 按下：
        键盘_按键总数 为 键盘_按键总数 加 1。
        做 事件队列_推入 用 码、事件_按下。
        若 做 热键_检查 用 码：
            返还 真。
        完。
    否则：
        做 事件队列_推入 用 码、事件_释放。
    完。
    返还 假。
完。

键盘_完整统计 接受 零参：
    返还 一本字典：中断计数 对应 键盘_中断计数、按键总数 对应 键盘_按键总数、shift 对应 键盘_shift、ctrl 对应 键盘_ctrl、alt 对应 键盘_alt、capslock 对应 键盘_capslock、numlock 对应 键盘_numlock、scrolllock 对应 键盘_scrolllock、热键数 对应 热键_已注册、事件满次数 对应 事件队列_满次数。
完。

可变 AI_键盘_风暴阈值 为 10000。

可变 AI_键盘_预测IRQ 为 0。

AI_键盘_风暴检测 接受 触发频率：
    若 触发频率 大于 AI_键盘_风暴阈值：
        AI_键盘_风暴阈值 为 AI_键盘_风暴阈值 乘 3 除以 2。
        返还 真。
    完。
    返还 假。
完。

AI_键盘_自适应合并 接受 当前延迟：
    若 当前延迟 小于 100：
        返还 16。
    完。
    若 当前延迟 小于 500：
        返还 8。
    完。
    返还 1。
完。

本章 收束。
