《PCI查找设备》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《.·PCI配置读》。

引用《..·标准·原生字符串》。

引用《..·中枢·内核打印》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·APIC寄存器读》。

引用《.·图处枚举》。

PCI_REG_厂商 为 0x00。

PCI_REG_设备 为 0x02。

PCI_REG_命令 为 0x04。

PCI_REG_状态 为 0x06。

PCI_REG_修订 为 0x08。

PCI_REG_类代码 为 0x0B。

PCI_REG_BAR0 为 0x10。

PCI_REG_能力指针 为 0x34。

PCI_CMD_IO使能 为 0x01。

PCI_CMD_内存使能 为 0x02。

PCI_CMD_总线主控 为 0x04。

PCI_CMD_中断禁用 为 0x400。

PCI_CAP_电源管理 为 0x01。

PCI_CAP_MSI 为 0x05。

PCI_CAP_MSIX 为 0x11。

PCI_CAP_PCIE 为 0x10。

PCI_电源_D0 为 0。

PCI_电源_D1 为 1。

PCI_电源_D2 为 2。

PCI_电源_D3 为 3。

外部。

PCI设备列表。

外部。

PCI设备数量。

可变 PCI_查找次数 为 0。

可变 PCI_MSI配置数 为 0。

可变 PCI_BAR映射数 为 0。

可变 PCI_热插拔事件 为 0。

PCI_查找设备 接受 厂商ID、设备ID：
    PCI_查找次数 为 PCI_查找次数 加 1。
    可变 i 为 0。
    只要 i 小于 PCI设备数量：
        dev 为 PCI设备列表 的 第 i 项。
        若 dev 的 厂商ID 等于 厂商ID 并且 dev 的 设备ID 等于 设备ID：
            返还 dev。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

PCI_查找类 接受 类代码、子类代码：
    PCI_查找次数 为 PCI_查找次数 加 1。
    可变 i 为 0。
    只要 i 小于 PCI设备数量：
        dev 为 PCI设备列表 的 第 i 项。
        若 dev 的 类代码 等于 类代码 并且 dev 的 子类代码 等于 子类代码：
            返还 dev。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

PCI_查找所有类 接受 类代码、子类代码：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 PCI设备数量：
        dev 为 PCI设备列表 的 第 i 项。
        若 dev 的 类代码 等于 类代码 并且 dev 的 子类代码 等于 子类代码：
            做 追加 用 结果、dev。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

PCI_查找厂商 接受 厂商ID：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 PCI设备数量：
        dev 为 PCI设备列表 的 第 i 项。
        若 dev 的 厂商ID 等于 厂商ID：
            做 追加 用 结果、dev。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

PCI_读BAR 接受 dev、BAR索引：
    若 dev 等于 空 或者 BAR索引 大于 5：
        返还 0。
    完。
    偏移 为 PCI_REG_BAR0 加 BAR索引 乘 4。
    原始值 为 做 PCI设备_读32 用 dev、偏移。
    若 原始值 位与 1：
        返还 原始值 位与 0xFFFFFFFC。
    否则：
        返还 原始值 位与 0xFFFFFFF0。
    完。
完。

PCI_BAR大小 接受 dev、BAR索引：
    若 dev 等于 空 或者 BAR索引 大于 5：
        返还 0。
    完。
    偏移 为 PCI_REG_BAR0 加 BAR索引 乘 4。
    原始值 为 做 PCI设备_读32 用 dev、偏移。
    做 PCI设备_写32 用 dev、偏移、0xFFFFFFFF。
    大小值 为 做 PCI设备_读32 用 dev、偏移。
    做 PCI设备_写32 用 dev、偏移、原始值。
    若 原始值 位与 1：
        大小值 为 大小值 位与 0xFFFFFFFC。
    否则：
        大小值 为 大小值 位与 0xFFFFFFF0。
    完。
    若 大小值 等于 0：
        返还 0。
    完。
    返还 按位取反 大小值 加 1。
完。

PCI_BAR是内存 接受 dev、BAR索引：
    若 dev 等于 空 或者 BAR索引 大于 5：
        返还 假。
    完。
    偏移 为 PCI_REG_BAR0 加 BAR索引 乘 4。
    返还 做 PCI设备_读32 用 dev、偏移 位与 1 等于 0。
完。

PCI_使能设备 接受 dev：
    若 dev 等于 空：
        返还 错误码_无效参数。
    完。
    cmd 为 做 PCI设备_读16 用 dev、PCI_REG_命令。
    cmd 为 cmd 位或 PCI_CMD_IO使能 位或 PCI_CMD_内存使能 位或 PCI_CMD_总线主控。
    做 PCI设备_写16 用 dev、PCI_REG_命令、cmd。
    返还 0。
完。

PCI_禁用设备 接受 dev：
    若 dev 等于 空：
        返还 错误码_无效参数。
    完。
    cmd 为 做 PCI设备_读16 用 dev、PCI_REG_命令。
    cmd 为 cmd 位与 按位取反 (PCI_CMD_IO使能 位或 PCI_CMD_内存使能 位或 PCI_CMD_总线主控)。
    做 PCI设备_写16 用 dev、PCI_REG_命令、cmd。
    返还 0。
完。

PCI_查找能力 接受 dev、能力ID：
    若 dev 等于 空：
        返还 0。
    完。
    状态 为 做 PCI设备_读16 用 dev、PCI_REG_状态。
    若 状态 位与 0x10 等于 0：
        返还 0。
    完。
    可变 偏移 为 做 PCI设备_读8 用 dev、PCI_REG_能力指针 位与 0xFC。
    可变 遍历 为 0。
    只要 偏移 不等于 0 并且 遍历 小于 48：
        id 为 做 PCI设备_读8 用 dev、偏移。
        若 id 等于 能力ID：
            返还 偏移。
        完。
        偏移 为 做 PCI设备_读8 用 dev、偏移 加 1 位与 0xFC。
        遍历 为 遍历 加 1。
    完。
    返还 0。
完。

PCI_使能MSI 接受 dev、向量号：
    若 dev 等于 空：
        返还 错误码_无效参数。
    完。
    偏移 为 做 PCI_查找能力 用 dev、PCI_CAP_MSI。
    若 偏移 等于 0：
        返还 错误码_不支持。
    完。
    控制 为 做 PCI设备_读16 用 dev、偏移 加 2。
    地址 为 0xFEE00000 位或 做 接口C_获取ID 左移 12。
    做 PCI设备_写32 用 dev、偏移 加 4、地址。
    若 控制 位与 0x80：
        做 PCI设备_写32 用 dev、偏移 加 8、0。
        做 PCI设备_写16 用 dev、偏移 加 12、向量号。
    否则：
        做 PCI设备_写16 用 dev、偏移 加 8、向量号。
    完。
    做 PCI设备_写16 用 dev、偏移 加 2、控制 位或 0x01。
    cmd 为 做 PCI设备_读16 用 dev、PCI_REG_命令。
    做 PCI设备_写16 用 dev、PCI_REG_命令、cmd 位或 PCI_CMD_中断禁用。
    PCI_MSI配置数 为 PCI_MSI配置数 加 1。
    返还 0。
完。

PCI_使能MSIX 接受 dev、表项数：
    若 dev 等于 空：
        返还 错误码_无效参数。
    完。
    偏移 为 做 PCI_查找能力 用 dev、PCI_CAP_MSIX。
    若 偏移 等于 0：
        返还 错误码_不支持。
    完。
    控制 为 做 PCI设备_读16 用 dev、偏移 加 2。
    表大小 为 (控制 位与 0x7FF) 加 1。
    若 表项数 大于 表大小：
        表项数 为 表大小。
    完。
    做 PCI设备_写16 用 dev、偏移 加 2、控制 位或 0x8000。
    cmd 为 做 PCI设备_读16 用 dev、PCI_REG_命令。
    做 PCI设备_写16 用 dev、PCI_REG_命令、cmd 位或 PCI_CMD_中断禁用。
    PCI_MSI配置数 为 PCI_MSI配置数 加 1。
    返还 0。
完。

PCI_设置电源状态 接受 dev、状态：
    若 dev 等于 空：
        返还 错误码_无效参数。
    完。
    偏移 为 做 PCI_查找能力 用 dev、PCI_CAP_电源管理。
    若 偏移 等于 0：
        返还 错误码_不支持。
    完。
    pmcsr 为 做 PCI设备_读16 用 dev、偏移 加 4。
    pmcsr 为 pmcsr 位与 0xFFFC 位或 状态 位与 0x03。
    做 PCI设备_写16 用 dev、偏移 加 4、pmcsr。
    若 状态 等于 PCI_电源_D0：
        做 延迟微秒 用 10000。
    完。
    返还 0。
完。

PCI_获取电源状态 接受 dev：
    若 dev 等于 空：
        返还 负 1。
    完。
    偏移 为 做 PCI_查找能力 用 dev、PCI_CAP_电源管理。
    若 偏移 等于 0：
        返还 负 1。
    完。
    返还 做 PCI设备_读16 用 dev、偏移 加 4 位与 0x03。
完。

PCI_功能级复位 接受 dev：
    若 dev 等于 空：
        返还 错误码_无效参数。
    完。
    偏移 为 做 PCI_查找能力 用 dev、PCI_CAP_PCIE。
    若 偏移 等于 0：
        返还 错误码_不支持。
    完。
    devctl 为 做 PCI设备_读16 用 dev、偏移 加 8。
    做 PCI设备_写16 用 dev、偏移 加 8、devctl 位或 0x8000。
    做 延迟微秒 用 100000。
    返还 0。
完。

PCI_热插拔检测 接受 零参：
    可变 i 为 0。
    只要 i 小于 256：
        可变 dev 为 0。
        只要 dev 小于 32：
            厂商 为 做 PCI_配置读16 用 i、dev、0、PCI_REG_厂商。
            若 厂商 不等于 0xFFFF 并且 厂商 不等于 0：
                已知 为 做 PCI_查找设备 用 厂商、做 PCI_配置读16 用 i、dev、0、PCI_REG_设备。
                若 已知 等于 空：
                    PCI_热插拔事件 为 PCI_热插拔事件 加 1。
                    做 内核调试 用 「PCI: 检测到新设备 总线=」 加 做 整数转字符串 用 i 加 「 设备=」 加 做 整数转字符串 用 dev。
                完。
            完。
            dev 为 dev 加 1。
        完。
        i 为 i 加 1。
    完。
完。

PCI_打印设备信息 接受 dev：
    若 dev 等于 空：
        返还。
    完。
    做 内核调试 用 「PCI设备: 」 加 做 整数转16进制 用 dev 的 厂商ID 加 「:」 加 做 整数转16进制 用 dev 的 设备ID。
    做 内核调试 用 「  类: 」 加 做 整数转16进制 用 dev 的 类代码 加 「:」 加 做 整数转16进制 用 dev 的 子类代码。
    做 内核调试 用 「  中断: IRQ 」 加 做 整数转字符串 用 dev 的 中断线。
    可变 bar 为 0。
    只要 bar 小于 6：
        基址 为 做 PCI_读BAR 用 dev、bar。
        若 基址 不等于 0：
            大小 为 做 PCI_BAR大小 用 dev、bar。
            做 内核调试 用 「  BAR」 加 做 整数转字符串 用 bar 加 「: 」 加 做 整数转16进制 用 基址 加 「 大小=」 加 做 整数转字符串 用 大小。
        完。
        bar 为 bar 加 1。
    完。
完。

PCI_完整统计 接受 零参：
    返还 一本字典：设备总数 对应 PCI设备数量、查找次数 对应 PCI_查找次数、MSI配置 对应 PCI_MSI配置数、BAR映射 对应 PCI_BAR映射数、热插拔事件 对应 PCI_热插拔事件。
完。

PCI_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 PCI设备数量 等于 0：
        做 追加 用 问题、「未发现PCI设备」。
    完。
    若 PCI_热插拔事件 大于 100：
        做 追加 用 问题、「热插拔事件过多: 」 加 做 整数转字符串 用 PCI_热插拔事件。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「PCI总线健康」。
    完。
    返还 问题。
完。

PCI_设备存在 接受 总线、设备、功能：
    地址 为 总线 左移 16 位或 设备 左移 11 位或 功能 左移 8。
    值 为 做 PCI_配置读32 用 地址。
    若 值 等于 0xFFFFFFFF：
        返还 假。
    完。
    返还 真。
完。

PCI_读取厂商ID 接受 总线、设备、功能：
    地址 为 总线 左移 16 位或 设备 左移 11 位或 功能 左移 8。
    返还 做 PCI_配置读32 用 地址 位与 0xFFFF。
完。

可变 AI_总线查找设_运行指标 为 0。

可变 AI_总线查找设_异常指标 为 0。

AI_总线查找设_自诊断 接受 零参：
    AI_总线查找设_运行指标 为 PCI_查找次数。
    返还 AI_总线查找设_运行指标。
完。

AI_总线查找设_优化建议 接受 零参：
    指标 为 做 AI_总线查找设_自诊断。
    若 AI_总线查找设_运行指标 大于 10000：
        返还 「总线查找设负载较高」。
    完。
    返还 「总线查找设状态正常」。
完。

本章 收束。
