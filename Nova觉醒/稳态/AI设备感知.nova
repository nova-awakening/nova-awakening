《AI设备感知》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·时钟中枢》。

引用《..·中枢·链表后继》。

设备画像 是 一种 事物，含 名称、错误率EMA、延迟EMA、总错误数、总IO数、健康评分、上次错误时间、连续错误、已降级。

可变 设备画像表 为 一组：（空）。

AI设备_初始化 接受 零参：
    做 内核信息 用 「AI设备感知: 错误率EMA+故障预测+健康评分+自动降级」。
完。

AI设备_记录错误 接受 设备名、错误类型：
    画像 为 做 AI设备_获取或创建 用 设备名。
    画像 的 总错误数 为 画像 的 总错误数 加 1。
    画像 的 连续错误 为 画像 的 连续错误 加 1。
    画像 的 上次错误时间 为 做 读取毫秒。
    画像 的 错误率EMA 为 (画像 的 错误率EMA 乘 922 加 1024 乘 102) 除以 1024。
    做 AI设备_更新健康 用 画像。
完。

AI设备_记录成功IO 接受 设备名、延迟us：
    画像 为 做 AI设备_获取或创建 用 设备名。
    画像 的 总IO数 为 画像 的 总IO数 加 1。
    画像 的 连续错误 为 0。
    画像 的 延迟EMA 为 (画像 的 延迟EMA 乘 922 加 延迟us 乘 1024 乘 102) 除以 (1024 乘 1024)。
    画像 的 错误率EMA 为 画像 的 错误率EMA 乘 922 除以 1024。
    做 AI设备_更新健康 用 画像。
完。

AI设备_更新健康 接受 画像：
    可变 分数 为 100。
    若 画像 的 错误率EMA 大于 500：
        分数 为 分数 减 50。
    否则若 画像 的 错误率EMA 大于 100：
        分数 为 分数 减 20。
    否则若 画像 的 错误率EMA 大于 10：
        分数 为 分数 减 5。
    完。
    若 画像 的 连续错误 大于 10：
        分数 为 分数 减 30。
    否则若 画像 的 连续错误 大于 5：
        分数 为 分数 减 15。
    完。
    若 画像 的 延迟EMA 大于 10000 乘 1024：
        分数 为 分数 减 10。
    完。
    若 分数 小于 0：
        分数 为 0。
    完。
    画像 的 健康评分 为 分数。
    若 分数 小于 30 并且 画像 的 已降级 等于 0：
        画像 的 已降级 为 1。
        做 内核警告 用 「AI设备: 」 加 画像 的 名称 加 「 健康评分=」 加 做 整数转字符串 用 分数 加 「, 建议降级」。
    完。
完。

AI设备_预测故障 接受 设备名：
    画像 为 做 AI设备_查找 用 设备名。
    若 画像 等于 空：
        返还 假。
    完。
    返还 画像 的 健康评分 小于 40。
完。

AI设备_健康评分 接受 设备名：
    画像 为 做 AI设备_查找 用 设备名。
    若 画像 等于 空：
        返还 100。
    完。
    返还 画像 的 健康评分。
完。

AI设备_查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备画像表：
        若 设备画像表 的 第 i 项 的 名称 等于 名称：
            返还 设备画像表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

AI设备_获取或创建 接受 名称：
    已有 为 做 AI设备_查找 用 名称。
    若 已有 不等于 空：
        返还 已有。
    完。
    新画像 为 新建 设备画像。
    新画像 的 名称 为 名称。
    新画像 的 健康评分 为 100。
    做 追加 用 设备画像表、新画像。
    返还 新画像。
完。

可变 错误率历史 为 一组：（空）。

ERROR_HISTORY_MAX 为 64。

AI设备_记录错误率样本 接受 设备名、错误率：
    可变 历史 为 空。
    可变 idx 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 错误率历史：
        若 错误率历史 的 第 i 项 的 第 0 项 等于 设备名：
            历史 为 错误率历史 的 第 i 项 的 第 1 项。
            idx 为 i。
            中断。
        完。
        i 为 i 加 1。
    完。
    若 历史 等于 空：
        历史 为 一组：（空）。
        做 追加 用 错误率历史、一组：设备名、历史。
        idx 为 做 计数 用 错误率历史 减 1。
    完。
    做 追加 用 历史、一组：做 读取毫秒、错误率。
    若 做 计数 用 历史 大于 ERROR_HISTORY_MAX：
        做 链表_删除 用 历史、0。
    完。
    错误率历史 的 第 idx 项 的 第 1 项 为 历史。
完。

AI设备_预测故障时间 接受 设备名：
    可变 历史 为 空。
    可变 i 为 0。
    只要 i 小于 做 计数 用 错误率历史：
        若 错误率历史 的 第 i 项 的 第 0 项 等于 设备名：
            历史 为 错误率历史 的 第 i 项 的 第 1 项。
            中断。
        完。
        i 为 i 加 1。
    完。
    若 历史 等于 空 或者 做 计数 用 历史 小于 5：
        返还 负 1。
    完。
    n 为 做 计数 用 历史。
    可变 sx 为 0。
    可变 sy 为 0。
    可变 sxy 为 0。
    可变 sx2 为 0。
    基准 为 历史 的 第 0 项 的 第 0 项。
    i 为 0。
    只要 i 小于 n：
        x 为 历史 的 第 i 项 的 第 0 项 减 基准。
        y 为 历史 的 第 i 项 的 第 1 项。
        sx 为 sx 加 x。
        sy 为 sy 加 y。
        sxy 为 sxy 加 x 乘 y。
        sx2 为 sx2 加 x 乘 x。
        i 为 i 加 1。
    完。
    分母 为 n 乘 sx2 减 sx 乘 sx。
    若 分母 等于 0：
        返还 负 1。
    完。
    斜率 为 (n 乘 sxy 减 sx 乘 sy) 除以 分母。
    若 斜率 小于或等于 0：
        返还 负 1。
    完。
    当前错误率 为 历史 的 第 n 减 1 项 的 第 1 项。
    临界错误率 为 800。
    若 当前错误率 大于或等于 临界错误率：
        返还 0。
    完。
    剩余增长 为 临界错误率 减 当前错误率。
    预计毫秒 为 剩余增长 乘 1000 除以 斜率。
    返还 预计毫秒 除以 1000。
完。

可变 设备冗余表 为 一组：（空）。

AI设备_注册冗余 接受 主设备、备设备：
    做 追加 用 设备冗余表、一组：主设备、备设备、假。
    做 内核信息 用 「AI设备: 注册冗余对 」 加 主设备 加 「 ↔ 」 加 备设备。
完。

AI设备_检查切换 接受 设备名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备冗余表：
        项 为 设备冗余表 的 第 i 项。
        若 项 的 第 0 项 等于 设备名 并且 非 项 的 第 2 项：
            主健康 为 做 AI设备_健康评分 用 项 的 第 0 项。
            备健康 为 做 AI设备_健康评分 用 项 的 第 1 项。
            若 主健康 小于 30 并且 备健康 大于 60：
                设备冗余表 的 第 i 项 的 第 2 项 为 真。
                做 内核警告 用 「AI设备: 自动切换 」 加 项 的 第 0 项 加 「 → 」 加 项 的 第 1 项 加 「 (主健康=」 加 做 整数转字符串 用 主健康 加 「, 备健康=」 加 做 整数转字符串 用 备健康 加 「)」。
                返还 项 的 第 1 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

AI设备_报告 接受 零参：
    可变 输出 为 「设备健康报告:\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备画像表：
        p 为 设备画像表 的 第 i 项。
        输出 为 输出 加 「  」 加 p 的 名称 加 「: 健康=」 加 做 整数转字符串 用 p 的 健康评分 加 「 错误=」 加 做 整数转字符串 用 p 的 总错误数 加 「 IO=」 加 做 整数转字符串 用 p 的 总IO数。
        若 p 的 已降级 等于 1：
            输出 为 输出 加 「 [已降级]」。
        完。
        输出 为 输出 加 「\n」。
        i 为 i 加 1。
    完。
    返还 输出。
完。

AI设备_统计 接受 零参：
    返还 一组：做 计数 用 设备画像表、做 计数 用 设备冗余表。
完。

AI设备_降级数 接受 零参：
    可变 计 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备画像表：
        若 设备画像表 的 第 i 项 的 已降级 等于 1：
            计 为 计 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计。
完。

AI设备_平均健康 接受 零参：
    若 做 计数 用 设备画像表 等于 0：
        返还 100。
    完。
    可变 总分 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备画像表：
        总分 为 总分 加 设备画像表 的 第 i 项 的 健康评分。
        i 为 i 加 1。
    完。
    返还 总分 除以 做 计数 用 设备画像表。
完。

AI设备_完整统计 接受 零参：
    返还 一组：做 计数 用 设备画像表、做 AI设备_降级数、做 AI设备_平均健康。
完。

AI设备_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 做 计数 用 设备画像表 等于 0：
        做 追加 用 问题、「无设备画像」。
    完。
    降级率 为 做 AI设备_降级数 乘 100。
    若 做 计数 用 设备画像表 大于 0：
        降级率 为 降级率 除以 做 计数 用 设备画像表。
    完。
    若 降级率 大于 30：
        做 追加 用 问题、「设备降级率过高: 」 加 做 整数转字符串 用 降级率 加 「%」。
    完。
    若 做 AI设备_平均健康 小于 50：
        做 追加 用 问题、「平均设备健康度过低」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「智能设备感知健康」。
    完。
    返还 问题。
完。

AI设备_查找画像 接受 设备ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备画像表：
        若 设备画像表 的 第 i 项 的 设备ID 等于 设备ID：
            返还 设备画像表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

AI设备_画像总数 接受 零参：
    返还 做 计数 用 设备画像表。
完。

AI设备_重置 接受 零参：
    设备画像表 为 一组：（空）。
完。

本章 收束。
