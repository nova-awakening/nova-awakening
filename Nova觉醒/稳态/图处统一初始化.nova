《图处统一初始化》

引用《.·图处显示》。

引用《..·中枢·日志中枢》。

引用《..·推理·定点Q算术》。

引用《.·图处型号表》。

引用《.·GPU驱动初始化》。

引用《.·AMD_PM4命令格式》。

引用《.·AMD初始化》。

引用《.·图处枚举》。

引用《.·NVIDIA引擎初始化》。

引用《.·图处内存管理》。

引用《.·帧率统计》。

引用《..·中枢·内存初始化》。

引用《..·中枢·字符串替换》。

引用《.·AMD中断处理》。

引用《..·感知·内核直连层》。

NGUI_最大图处数 为 4。

NGUI_最大命令池 为 16。

NGUI_命令缓冲大小 为 65536。

NGUI_最大围栏数 为 256。

可变 NGUI_命令提交总数 为 0。

可变 NGUI_围栏总数 为 0。

可变 NGUI_显存分配总数 为 0。

可变 NGUI_错误总数 为 0。

可变 NGUI_恢复总数 为 0。

图处_统一打印状态 接受 零参：
    若 当前图处 等于 空：
        做 内核信息 用 「  图处状态: 无图处 (纯CPU渲染模式)」。
        返还。
    完。
    做 内核信息 用 「  图处状态: 驱动类型=」 加 做 NGUI_驱动类型名 用 当前驱动类型。
    做 内核信息 用 「  厂商ID=0x」 加 做 整数转十六进制 用 当前图处 的 厂商ID。
    做 内核信息 用 「  设备ID=0x」 加 做 整数转十六进制 用 当前图处 的 设备ID。
    若 当前图处 的 显存大小 大于 0：
        做 内核信息 用 「  显存=」 加 当前图处 的 显存大小 除以 1048576 加 「MB」。
    完。
完。

NGUI_驱动类型名 接受 类型：
    若 类型 等于 DRV_NVIDIA：
        返还 「NVIDIA」。
    完。
    若 类型 等于 DRV_AMD：
        返还 「AMD」。
    完。
    若 类型 等于 DRV_INTEL：
        返还 「Intel」。
    完。
    若 类型 等于 DRV_VIRTIO：
        返还 「VirtIO」。
    完。
    若 类型 等于 DRV_GENERIC：
        返还 「Generic」。
    完。
    返还 「None」。
完。

NGUI_显示模式 是 一种 事物，含 宽、高、深度、刷新率。

NGUI_设置显示模式 接受 宽、高、深度、刷新率：
    若 当前图处 等于 空：
        返还 假。
    完。
    若 宽 小于 320 或者 宽 大于 7680：
        返还 假。
    完。
    若 高 小于 240 或者 高 大于 4320：
        返还 假。
    完。
    若 深度 不等于 24 并且 深度 不等于 32：
        深度 为 32。
    完。
    若 刷新率 小于 30 或者 刷新率 大于 360：
        刷新率 为 60。
    完。
    可变 结果 为 假。
    若 当前驱动类型 等于 DRV_NVIDIA：
        结果 为 做 NV_设置显示模式 用 当前图处、宽、高、深度、刷新率。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        结果 为 做 AMD_设置显示模式 用 当前图处、宽、高、深度、刷新率。
    完。
    否则。
    块：
        结果 为 做 图处驱动_设置模式 用 当前图处、宽、高、深度。
    完。
    若 结果：
        做 渲染管线_更新大小 用 宽、高。
        做 内核信息 用 「  显示模式: 」 加 宽 加 「x」 加 高 加 「@」 加 刷新率 加 「Hz」。
    完。
    返还 结果。
完。

NGUI_获取显示模式 接受 输出：
    若 当前图处 等于 空 或者 输出 等于 0：
        返还 假。
    完。
    若 当前驱动类型 等于 DRV_NVIDIA：
        返还 做 NV_获取显示模式 用 当前图处、输出。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        返还 做 AMD_获取显示模式 用 当前图处、输出。
    完。
    返还 做 图处驱动_获取模式 用 当前图处、输出。
完。

NGUI_显存分配 接受 大小、对齐、标志：
    若 当前图处 等于 空 或者 大小 等于 0：
        返还 0。
    完。
    可变 地址 为 0。
    若 当前驱动类型 等于 DRV_NVIDIA：
        地址 为 做 NV_显存分配 用 当前图处、大小、对齐。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        地址 为 做 AMD_显存分配 用 当前图处、大小、对齐。
    完。
    否则。
    块：
        地址 为 做 图处驱动_分配显存 用 当前图处、大小。
    完。
    若 地址 不等于 0：
        NGUI_显存分配总数 为 NGUI_显存分配总数 加 1。
    完。
    返还 地址。
完。

NGUI_显存释放 接受 地址：
    若 当前图处 等于 空 或者 地址 等于 0：
        返还。
    完。
    若 当前驱动类型 等于 DRV_NVIDIA：
        做 NV_显存释放 用 当前图处、地址。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        做 AMD_显存释放 用 当前图处、地址。
    完。
    否则。
    块：
        做 图处驱动_释放显存 用 当前图处、地址。
    完。
完。

NGUI_获取显存总量 接受 零参：
    若 当前图处 等于 空：
        返还 0。
    完。
    返还 当前图处 的 显存大小。
完。

NGUI_获取显存已用 接受 零参：
    若 当前图处 等于 空：
        返还 0。
    完。
    若 当前驱动类型 等于 DRV_NVIDIA：
        返还 做 NV_获取显存已用 用 当前图处。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        返还 做 AMD_获取显存已用 用 当前图处。
    完。
    返还 做 图处驱动_获取显存已用 用 当前图处。
完。

NGUI_命令缓冲 是 一种 事物，含 数据、大小、写位置、已提交。

可变 NGUI_命令池 为 0。

可变 NGUI_命令池数量 为 0。

NGUI_初始化命令池 接受 零参：
    若 NGUI_命令池 不等于 0：
        返还。
    完。
    NGUI_命令池 为 做 堆_分配 用 NGUI_最大命令池 乘 24。
    可变 i 为 0。
    只要 i 小于 NGUI_最大命令池：
        偏移 为 NGUI_命令池 加 i 乘 24。
        数据 为 做 堆_分配 用 NGUI_命令缓冲大小。
        做 内存_写64 用 偏移、数据。
        做 内存_写32 用 偏移 加 8、NGUI_命令缓冲大小。
        做 内存_写32 用 偏移 加 12、0。
        做 内存_写32 用 偏移 加 16、0。
        i 为 i 加 1。
    完。
    NGUI_命令池数量 为 NGUI_最大命令池。
完。

NGUI_获取命令缓冲 接受 零参：
    若 NGUI_命令池 等于 0：
        做 NGUI_初始化命令池。
    完。
    可变 i 为 0。
    只要 i 小于 NGUI_命令池数量：
        偏移 为 NGUI_命令池 加 i 乘 24。
        若 做 内存_读32 用 偏移 加 16 等于 0：
            做 内存_写32 用 偏移 加 12、0。
            返还 偏移。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

NGUI_命令写入32 接受 cmd、值：
    若 cmd 等于 0：
        返还。
    完。
    写位置 为 做 内存_读32 用 cmd 加 12。
    大小 为 做 内存_读32 用 cmd 加 8。
    若 写位置 加 4 大于 大小：
        返还。
    完。
    数据 为 做 内存_读64 用 cmd。
    做 内存_写32 用 数据 加 写位置、值。
    做 内存_写32 用 cmd 加 12、写位置 加 4。
完。

NGUI_提交命令 接受 cmd：
    若 cmd 等于 0 或者 当前图处 等于 空：
        返还 假。
    完。
    数据 为 做 内存_读64 用 cmd。
    大小 为 做 内存_读32 用 cmd 加 12。
    可变 结果 为 假。
    若 当前驱动类型 等于 DRV_NVIDIA：
        结果 为 做 NV_提交命令 用 当前图处、数据、大小。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        结果 为 做 AMD_提交命令 用 当前图处、数据、大小。
    完。
    否则。
    块：
        结果 为 做 图处驱动_提交命令 用 当前图处、数据、大小。
    完。
    若 结果：
        做 内存_写32 用 cmd 加 16、1。
        NGUI_命令提交总数 为 NGUI_命令提交总数 加 1。
    完。
    返还 结果。
完。

NGUI_回收命令缓冲 接受 cmd：
    若 cmd 等于 0：
        返还。
    完。
    做 内存_写32 用 cmd 加 12、0。
    做 内存_写32 用 cmd 加 16、0。
完。

可变 NGUI_下一个围栏ID 为 1。

NGUI_创建围栏 接受 零参：
    若 当前图处 等于 空：
        返还 0。
    完。
    id 为 NGUI_下一个围栏ID。
    NGUI_下一个围栏ID 为 NGUI_下一个围栏ID 加 1。
    可变 句柄 为 0。
    若 当前驱动类型 等于 DRV_NVIDIA：
        句柄 为 做 NV_创建围栏 用 当前图处、id。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        句柄 为 做 AMD_创建围栏 用 当前图处、id。
    完。
    否则。
    块：
        句柄 为 做 图处驱动_创建围栏 用 当前图处、id。
    完。
    NGUI_围栏总数 为 NGUI_围栏总数 加 1。
    返还 句柄。
完。

NGUI_等待围栏 接受 句柄、超时毫秒：
    若 句柄 等于 0：
        返还 假。
    完。
    若 当前驱动类型 等于 DRV_NVIDIA：
        返还 做 NV_等待围栏 用 当前图处、句柄、超时毫秒。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        返还 做 AMD_等待围栏 用 当前图处、句柄、超时毫秒。
    完。
    返还 做 图处驱动_等待围栏 用 当前图处、句柄、超时毫秒。
完。

NGUI_释放围栏 接受 句柄：
    若 句柄 等于 0：
        返还。
    完。
    若 当前驱动类型 等于 DRV_NVIDIA：
        做 NV_释放围栏 用 当前图处、句柄。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        做 AMD_释放围栏 用 当前图处、句柄。
    完。
    否则。
    块：
        做 图处驱动_释放围栏 用 当前图处、句柄。
    完。
完。

NGUI_电源_性能 为 0。

NGUI_电源_平衡 为 1。

NGUI_电源_节能 为 2。

可变 NGUI_当前电源模式 为 1。

NGUI_设置电源模式 接受 模式：
    若 当前图处 等于 空：
        返还。
    完。
    若 模式 小于 0 或者 模式 大于 2：
        模式 为 NGUI_电源_平衡。
    完。
    NGUI_当前电源模式 为 模式。
    若 当前驱动类型 等于 DRV_NVIDIA：
        做 NV_设置电源模式 用 当前图处、模式。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        做 AMD_设置电源模式 用 当前图处、模式。
    完。
    做 内核信息 用 「  图处电源模式: 」 加 做 NGUI_电源模式名 用 模式。
完。

NGUI_电源模式名 接受 模式：
    若 模式 等于 NGUI_电源_性能：
        返还 「性能」。
    完。
    若 模式 等于 NGUI_电源_平衡：
        返还 「平衡」。
    完。
    若 模式 等于 NGUI_电源_节能：
        返还 「节能」。
    完。
    返还 「未知」。
完。

NGUI_获取温度 接受 零参：
    若 当前图处 等于 空：
        返还 负 1。
    完。
    若 当前驱动类型 等于 DRV_NVIDIA：
        返还 做 NV_获取温度 用 当前图处。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        返还 做 AMD_获取温度 用 当前图处。
    完。
    返还 负 1。
完。

NGUI_获取时钟频率 接受 零参：
    若 当前图处 等于 空：
        返还 0。
    完。
    若 当前驱动类型 等于 DRV_NVIDIA：
        返还 做 NV_获取时钟频率 用 当前图处。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        返还 做 AMD_获取时钟频率 用 当前图处。
    完。
    返还 0。
完。

可变 NGUI_图处列表 为 0。

可变 NGUI_图处数量 为 0。

NGUI_枚举所有图处 接受 零参：
    若 NGUI_图处列表 等于 0：
        NGUI_图处列表 为 做 堆_分配 用 NGUI_最大图处数 乘 8。
    完。
    NGUI_图处数量 为 做 图处_获取设备数。
    若 NGUI_图处数量 大于 NGUI_最大图处数：
        NGUI_图处数量 为 NGUI_最大图处数。
    完。
    可变 i 为 0。
    只要 i 小于 NGUI_图处数量：
        做 内存_写64 用 NGUI_图处列表 加 i 乘 8、做 图处_获取设备 用 i。
        i 为 i 加 1。
    完。
    返还 NGUI_图处数量。
完。

NGUI_切换主图处 接受 索引：
    若 索引 大于或等于 NGUI_图处数量：
        返还 假。
    完。
    gpu 为 做 内存_读64 用 NGUI_图处列表 加 索引 乘 8。
    若 gpu 等于 0：
        返还 假。
    完。
    当前图处 为 gpu。
    若 gpu 的 厂商ID 等于 图处_VENDOR_NVIDIA：
        当前驱动类型 为 DRV_NVIDIA。
    完。
    做 否则如果 用 gpu 的 厂商ID 等于 图处_VENDOR_AMD。
    块：
        当前驱动类型 为 DRV_AMD。
    完。
    做 否则如果 用 gpu 的 厂商ID 等于 图处_VENDOR_INTEL。
    块：
        当前驱动类型 为 DRV_INTEL。
    完。
    否则。
    块：
        当前驱动类型 为 DRV_GENERIC。
    完。
    做 内核信息 用 「图处统一接口: 切换到图处#」 加 索引。
    返还 真。
完。

NGUI_检测故障 接受 零参：
    若 当前图处 等于 空：
        返还 假。
    完。
    可变 故障 为 假。
    若 当前驱动类型 等于 DRV_NVIDIA：
        故障 为 做 NV_检测故障 用 当前图处。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        故障 为 做 AMD_检测故障 用 当前图处。
    完。
    否则。
    块：
        故障 为 做 图处驱动_检测故障 用 当前图处。
    完。
    若 故障：
        NGUI_错误总数 为 NGUI_错误总数 加 1。
        做 内核警告 用 「图处统一接口: 检测到GPU故障!」。
    完。
    返还 故障。
完。

NGUI_故障恢复 接受 零参：
    若 当前图处 等于 空：
        返还 假。
    完。
    做 内核信息 用 「图处统一接口: 尝试GPU故障恢复...」。
    可变 结果 为 假。
    若 当前驱动类型 等于 DRV_NVIDIA：
        结果 为 做 NV_重置 用 当前图处。
    完。
    做 否则如果 用 当前驱动类型 等于 DRV_AMD。
    块：
        结果 为 做 AMD_重置 用 当前图处。
    完。
    否则。
    块：
        结果 为 做 图处驱动_重置 用 当前图处。
    完。
    若 结果：
        NGUI_恢复总数 为 NGUI_恢复总数 加 1。
        做 内核信息 用 「图处统一接口: GPU故障恢复成功」。
        做 渲染管线_重置。
    否则：
        做 内核警告 用 「图处统一接口: GPU故障恢复失败, 回退CPU渲染」。
        当前驱动类型 为 DRV_NONE。
    完。
    返还 结果。
完。

可变 NGUI_AI预测次数 为 0。

NGUI_AI提取特征 接受 零参：
    t 为 做 Tensor_创建 用 10。
    若 t 等于 0：
        返还 0。
    完。
    做 Tensor_写 用 t、0、当前驱动类型。
    显存总 为 做 NGUI_获取显存总量。
    显存用 为 做 NGUI_获取显存已用。
    做 Tensor_写 用 t、1、显存总 除以 1048576。
    使用率 为 0。
    若 显存总 大于 0：
        使用率 为 显存用 乘 100 除以 显存总。
    完。
    做 Tensor_写 用 t、2、使用率。
    做 Tensor_写 用 t、3、做 NGUI_获取温度。
    做 Tensor_写 用 t、4、做 NGUI_获取时钟频率。
    做 Tensor_写 用 t、5、NGUI_当前电源模式。
    做 Tensor_写 用 t、6、NGUI_命令提交总数。
    做 Tensor_写 用 t、7、NGUI_围栏总数。
    做 Tensor_写 用 t、8、NGUI_错误总数。
    做 Tensor_写 用 t、9、NGUI_图处数量。
    返还 t。
完。

NGUI_AI预测GPU性能 接受 零参：
    特征 为 做 NGUI_AI提取特征。
    若 特征 等于 0：
        返还。
    完。
    显存使用率 为 做 Tensor_读 用 特征、2。
    温度 为 做 Tensor_读 用 特征、3。
    错误数 为 做 Tensor_读 用 特征、8。
    若 显存使用率 大于 90：
        做 全局AI总线 的 发布 用 「ngui_显存警告」、「显存使用率过高」、显存使用率。
    完。
    若 温度 大于 85：
        做 全局AI总线 的 发布 用 「ngui_温度警告」、「图处过热」、温度。
        若 NGUI_当前电源模式 等于 NGUI_电源_性能：
            做 NGUI_设置电源模式 用 NGUI_电源_平衡。
        完。
    完。
    若 错误数 大于 10：
        做 AI总线_异常检测 用 「NGUI_GPU错误率异常」、错误数。
    完。
    做 Tensor_释放 用 特征。
    NGUI_AI预测次数 为 NGUI_AI预测次数 加 1。
完。

图处_统一关机 接受 零参：
    做 内核信息 用 「图处统一接口: 开始关机...」。
    若 NGUI_命令池 不等于 0：
        可变 i 为 0。
        只要 i 小于 NGUI_命令池数量：
            偏移 为 NGUI_命令池 加 i 乘 24。
            数据 为 做 内存_读64 用 偏移。
            若 数据 不等于 0：
                做 堆_释放 用 数据。
            完。
            i 为 i 加 1。
        完。
        做 堆_释放 用 NGUI_命令池。
        NGUI_命令池 为 0。
    完。
    若 当前图处 不等于 空：
        若 当前驱动类型 等于 DRV_NVIDIA：
            做 NV_关机 用 当前图处。
        完。
        做 否则如果 用 当前驱动类型 等于 DRV_AMD。
        块：
            做 AMD_关机 用 当前图处。
        完。
        否则。
        块：
            做 图处驱动_关机 用 当前图处。
        完。
    完。
    当前图处 为 空。
    当前驱动类型 为 DRV_NONE。
    做 内核信息 用 「图处统一接口: 关机完成」。
完。

NGUI_获取统计 接受 零参：
    返还 一组：NGUI_命令提交总数、NGUI_围栏总数、NGUI_显存分配总数、NGUI_错误总数、NGUI_恢复总数、NGUI_AI预测次数。
完。

NGUI_重置统计 接受 零参：
    NGUI_命令提交总数 为 0。
    NGUI_围栏总数 为 0。
    NGUI_显存分配总数 为 0。
    NGUI_错误总数 为 0。
    NGUI_恢复总数 为 0。
    NGUI_AI预测次数 为 0。
完。

DRV_NONE 为 0。

DRV_NVIDIA 为 1。

DRV_AMD 为 2。

DRV_INTEL 为 3。

DRV_VIRTIO 为 4。

DRV_GENERIC 为 5。

可变 当前驱动类型 为 DRV_NONE。

可变 当前图处 为 空。

图处_统一初始化 接受 零参：
    做 内核信息 用 「图处统一接口: 开始图处探测...」。
    做 图处_枚举。
    gpu 为 做 图处_获取主设备。
    若 gpu 等于 空：
        做 内核警告 用 「图处统一接口: 未发现图处, 使用纯CPU渲染」。
        当前驱动类型 为 DRV_NONE。
        返还。
    完。
    当前图处 为 gpu。
    若 gpu 的 厂商ID 等于 图处_VENDOR_NVIDIA：
        做 内核信息 用 「图处统一接口: 检测到NVIDIA 图处 → 加载nova-nv驱动」。
        做 NV_识别完整型号 用 gpu。
        结果 为 做 NV_初始化 用 gpu。
        若 结果 等于 成功：
            当前驱动类型 为 DRV_NVIDIA。
        否则：
            做 内核警告 用 「图处统一接口: NVIDIA驱动初始化失败, 回退通用驱动」。
            当前驱动类型 为 DRV_GENERIC。
            做 图处驱动_初始化 用 gpu。
        完。
    完。
    做 否则如果 用 gpu 的 厂商ID 等于 图处_VENDOR_AMD。
    块：
        做 内核信息 用 「图处统一接口: 检测到AMD 图处 → 加载nova-amd驱动」。
        做 AMD_识别完整型号 用 gpu。
        结果 为 做 AMD_初始化 用 gpu。
        若 结果 等于 成功：
            当前驱动类型 为 DRV_AMD。
        否则：
            做 内核警告 用 「图处统一接口: AMD驱动初始化失败, 回退通用驱动」。
            当前驱动类型 为 DRV_GENERIC。
            做 图处驱动_初始化 用 gpu。
        完。
    完。
    做 否则如果 用 gpu 的 厂商ID 等于 图处_VENDOR_INTEL。
    块：
        做 内核信息 用 「图处统一接口: 检测到Intel 图处 → 加载通用驱动(Intel专用驱动待扩展)」。
        当前驱动类型 为 DRV_INTEL。
        做 图处驱动_初始化 用 gpu。
    完。
    做 否则如果 用 gpu 的 厂商ID 等于 图处_VENDOR_VIRTIO 或者 gpu 的 厂商ID 等于 图处_VENDOR_QEMU 或者 gpu 的 厂商ID 等于 图处_VENDOR_VMWARE。
    块：
        做 内核信息 用 「图处统一接口: 检测到虚拟图处 → 加载通用驱动」。
        当前驱动类型 为 DRV_VIRTIO。
        做 图处驱动_初始化 用 gpu。
    完。
    否则。
    块：
        做 内核信息 用 「图处统一接口: 未知厂商 0x」 加 做 整数转十六进制 用 gpu 的 厂商ID 加 「 → 通用驱动」。
        当前驱动类型 为 DRV_GENERIC。
        做 图处驱动_初始化 用 gpu。
    完。
    可变 宽 为 1920。
    可变 高 为 1080。
    做 渲染管线_初始化 用 宽、高、32。
    做 图处_统一打印状态。
完。

本章 收束。
