《显示管理模》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·感知·内核直连层》。

引用《..·中枢·内存初始化》。

最大连接器 为 8。

最大编码器 为 8。

最大扫描控 为 4。

最大模式 为 32。

最大图形缓冲 为 16。

显示模式 是 一种 事物，含 宽度、高度、刷新率、像素时钟、标志。

连接器 是 一种 事物，含 标识、类型、已连接、编码器索引、首选模式、模式数、模式表。

扫描控制器 是 一种 事物，含 标识、活跃、当前模式索引、帧缓冲索引、横偏移、纵偏移、翻转挂起。

图形缓冲 是 一种 事物，含 标识、宽度、高度、步长、格式、缓冲地址、缓冲大小、引用计数。

可变 连接器表 为 一组：（空）。

可变 连接器数 为 0。

可变 扫描控表 为 一组：（空）。

可变 扫描控数 为 0。

可变 图形缓冲表 为 一组：（空）。

可变 图形缓冲数 为 0。

可变 下一对象标识 为 1。

可变 模式设置次数 为 0。

可变 翻页次数 为 0。

显示_初始化 接受 零参：
    连接器表 为 一组：（空）。
    连接器数 为 0。
    扫描控表 为 一组：（空）。
    扫描控数 为 0。
    图形缓冲表 为 一组：（空）。
    图形缓冲数 为 0。
完。

显示_分配标识 接受 零参：
    标识 为 下一对象标识。
    下一对象标识 为 下一对象标识 加 1。
    返还 标识。
完。

显示_添加连接器 接受 类型：
    若 连接器数 大于或等于 最大连接器：
        返还 负 1。
    完。
    连接 为 新建 连接器。
    连接 的 标识 为 做 显示_分配标识。
    连接 的 类型 为 类型。
    连接 的 已连接 为 假。
    连接 的 编码器索引 为 负 1。
    连接 的 首选模式 为 负 1。
    连接 的 模式数 为 0。
    连接 的 模式表 为 做 堆_分配 用 最大模式 乘 20。
    做 追加 用 连接器表、连接。
    索引 为 连接器数。
    连接器数 为 连接器数 加 1。
    返还 索引。
完。

显示_添加模式 接受 连接器索引、宽、高、刷新率：
    若 连接器索引 大于或等于 连接器数：
        返还 负 1。
    完。
    连接 为 连接器表 的 第 连接器索引 项。
    若 连接 的 模式数 大于或等于 最大模式：
        返还 负 1。
    完。
    偏移 为 连接 的 模式表 加 连接 的 模式数 乘 20。
    做 内存_写32 用 偏移、宽。
    做 内存_写32 用 偏移 加 4、高。
    做 内存_写32 用 偏移 加 8、刷新率。
    做 内存_写32 用 偏移 加 12、宽 乘 高 乘 刷新率。
    做 内存_写32 用 偏移 加 16、0。
    模式索引 为 连接 的 模式数。
    连接 的 模式数 为 连接 的 模式数 加 1。
    若 连接 的 首选模式 小于 0：
        连接 的 首选模式 为 模式索引。
    完。
    返还 模式索引。
完。

显示_添加扫描控 接受 零参：
    若 扫描控数 大于或等于 最大扫描控：
        返还 负 1。
    完。
    控 为 新建 扫描控制器。
    控 的 标识 为 做 显示_分配标识。
    控 的 活跃 为 假。
    控 的 当前模式索引 为 负 1。
    控 的 帧缓冲索引 为 负 1。
    控 的 横偏移 为 0。
    控 的 纵偏移 为 0。
    控 的 翻转挂起 为 假。
    做 追加 用 扫描控表、控。
    索引 为 扫描控数。
    扫描控数 为 扫描控数 加 1。
    返还 索引。
完。

显示_创建图形缓冲 接受 宽、高、格式：
    若 图形缓冲数 大于或等于 最大图形缓冲：
        返还 负 1。
    完。
    步长 为 宽 乘 4。
    大小 为 步长 乘 高。
    缓冲 为 新建 图形缓冲。
    缓冲 的 标识 为 做 显示_分配标识。
    缓冲 的 宽度 为 宽。
    缓冲 的 高度 为 高。
    缓冲 的 步长 为 步长。
    缓冲 的 格式 为 格式。
    缓冲 的 缓冲地址 为 做 堆_分配 用 大小。
    缓冲 的 缓冲大小 为 大小。
    缓冲 的 引用计数 为 1。
    做 追加 用 图形缓冲表、缓冲。
    索引 为 图形缓冲数。
    图形缓冲数 为 图形缓冲数 加 1。
    返还 索引。
完。

显示_设置模式 接受 扫描控索引、连接器索引、模式索引、缓冲索引：
    若 扫描控索引 大于或等于 扫描控数：
        返还 错误_无效参数。
    完。
    若 连接器索引 大于或等于 连接器数：
        返还 错误_无效参数。
    完。
    若 缓冲索引 大于或等于 图形缓冲数：
        返还 错误_无效参数。
    完。
    控 为 扫描控表 的 第 扫描控索引 项。
    控 的 当前模式索引 为 模式索引。
    控 的 帧缓冲索引 为 缓冲索引。
    控 的 活跃 为 真。
    连接器表 的 第 连接器索引 项 的 已连接 为 真。
    模式设置次数 为 模式设置次数 加 1。
    返还 0。
完。

显示_翻页 接受 扫描控索引、新缓冲索引：
    若 扫描控索引 大于或等于 扫描控数：
        返还 错误_无效参数。
    完。
    若 新缓冲索引 大于或等于 图形缓冲数：
        返还 错误_无效参数。
    完。
    控 为 扫描控表 的 第 扫描控索引 项。
    若 控 的 翻转挂起：
        返还 错误_忙。
    完。
    控 的 帧缓冲索引 为 新缓冲索引。
    控 的 翻转挂起 为 真。
    翻页次数 为 翻页次数 加 1。
    返还 0。
完。

显示_完成翻页 接受 扫描控索引：
    若 扫描控索引 大于或等于 扫描控数：
        返还。
    完。
    扫描控表 的 第 扫描控索引 项 的 翻转挂起 为 假。
完。

显示_获取模式 接受 连接器索引、模式索引：
    若 连接器索引 大于或等于 连接器数：
        返还 一组：0、0、0。
    完。
    连接 为 连接器表 的 第 连接器索引 项。
    若 模式索引 大于或等于 连接 的 模式数：
        返还 一组：0、0、0。
    完。
    偏移 为 连接 的 模式表 加 模式索引 乘 20。
    返还 一组：做 内存_读32 用 偏移、做 内存_读32 用 偏移 加 4、做 内存_读32 用 偏移 加 8。
完。

显示_释放图形缓冲 接受 索引：
    若 索引 大于或等于 图形缓冲数：
        返还。
    完。
    图形缓冲表 的 第 索引 项 的 引用计数 为 图形缓冲表 的 第 索引 项 的 引用计数 减 1。
    若 图形缓冲表 的 第 索引 项 的 引用计数 小于或等于 0 并且 图形缓冲表 的 第 索引 项 的 缓冲地址 不等于 0：
        做 堆_释放 用 图形缓冲表 的 第 索引 项 的 缓冲地址。
        图形缓冲表 的 第 索引 项 的 缓冲地址 为 0。
    完。
完。

显示管理_完整统计 接受 零参：
    返还 一组：连接器数、扫描控数、图形缓冲数、模式设置次数、翻页次数。
完。

可变 AI_显示管理模_运行指标 为 0。

可变 AI_显示管理模_异常指标 为 0。

AI_显示管理模_自诊断 接受 零参：
    AI_显示管理模_运行指标 为 模式设置次数。
    返还 AI_显示管理模_运行指标。
完。

AI_显示管理模_优化建议 接受 零参：
    指标 为 做 AI_显示管理模_自诊断。
    若 AI_显示管理模_运行指标 大于 10000：
        返还 「显示管理模负载较高」。
    完。
    返还 「显示管理模状态正常」。
完。

本章 收束。
