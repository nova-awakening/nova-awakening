《粒子系统》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·中枢·数学工具》。

引用《.·数学基础向量》。

引用《..·中枢·内核打印》。

引用《..·中枢·整数转字符串》。

引用《.·渲染管线双》。

粒子_最大 为 1024。

发射器_点 为 0。

发射器_圆 为 1。

发射器_矩形 为 2。

发射器_球 为 3。

发射器_锥 为 4。

粒子类型_光点 为 0。

粒子类型_火花 为 1。

粒子类型_烟雾 为 2。

粒子类型_光环 为 3。

粒子类型_拖尾 为 4。

粒子类型_星尘 为 5。

粒子 是 一种 事物，含 位置、速度、加速度、颜色、结束颜色、生命、最大生命、大小、结束大小、alpha、类型、旋转、旋转速度。

粒子发射器 是 一种 事物，含 位置、方向、类型、粒子类型、速率、初始速度_最小、初始速度_最大、初始大小_最小、初始大小_最大、生命_最小、生命_最大、颜色_开始、颜色_结束、扩散角度、重力、风力、湍流强度、已发射总数、活跃。

粒子统计 是 一种 事物，含 总发射数、总消亡数、当前活跃数、峰值活跃数、总帧数、总渲染像素数。

可变 粒子池 为 一组：（空）。

可变 粒子_活跃数 为 0。

可变 发射器列表 为 一组：（空）。

可变 全局_粒子统计 为 空。

可变 随机种子 为 12345。

粒子_初始化 接受 零参：
    若 全局_粒子统计 等于 空：
        全局_粒子统计 为 新建 粒子统计。
        全局_粒子统计 的 总发射数 为 0。
        全局_粒子统计 的 总消亡数 为 0。
        全局_粒子统计 的 当前活跃数 为 0。
        全局_粒子统计 的 峰值活跃数 为 0。
        全局_粒子统计 的 总帧数 为 0。
        全局_粒子统计 的 总渲染像素数 为 0。
    完。
完。

粒子_伪随机 接受 零参：
    随机种子 为 随机种子 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    返还 随机种子。
完。

粒子_随机范围 接受 最小、最大：
    若 最大 小于或等于 最小：
        返还 最小。
    完。
    返还 最小 加 做 粒子_伪随机 取余于 (最大 减 最小 加 1)。
完。

粒子_颜色插值 接受 c1、c2、t：
    若 t 小于或等于 0：
        返还 c1。
    完。
    若 t 大于或等于 255：
        返还 c2。
    完。
    r1 为 c1 右移 16 位与 0xFF。
    g1 为 c1 右移 8 位与 0xFF。
    b1 为 c1 位与 0xFF。
    r2 为 c2 右移 16 位与 0xFF。
    g2 为 c2 右移 8 位与 0xFF。
    b2 为 c2 位与 0xFF。
    r 为 r1 加 (r2 减 r1) 乘 t 除以 255。
    g 为 g1 加 (g2 减 g1) 乘 t 除以 255。
    b 为 b1 加 (b2 减 b1) 乘 t 除以 255。
    返还 r 左移 16 位或 g 左移 8 位或 b。
完。

粒子_发射 接受 位置、速度、颜色、生命：
    做 粒子_初始化。
    若 粒子_活跃数 大于或等于 粒子_最大：
        返还。
    完。
    p 为 新建 粒子。
    p 的 位置 为 位置。
    p 的 速度 为 速度。
    p 的 加速度 为 做 向量3_新 用 0、0、0。
    p 的 颜色 为 颜色。
    p 的 结束颜色 为 颜色。
    p 的 生命 为 生命。
    p 的 最大生命 为 生命。
    p 的 大小 为 2。
    p 的 结束大小 为 1。
    p 的 alpha 为 200。
    p 的 类型 为 粒子类型_光点。
    p 的 旋转 为 0。
    p 的 旋转速度 为 0。
    做 追加 用 粒子池、p。
    粒子_活跃数 为 粒子_活跃数 加 1。
    全局_粒子统计 的 总发射数 为 全局_粒子统计 的 总发射数 加 1。
    若 粒子_活跃数 大于 全局_粒子统计 的 峰值活跃数：
        全局_粒子统计 的 峰值活跃数 为 粒子_活跃数。
    完。
完。

粒子_发射高级 接受 位置、速度、加速度、颜色始、颜色终、生命、大小始、大小终、类型：
    做 粒子_初始化。
    若 粒子_活跃数 大于或等于 粒子_最大：
        返还。
    完。
    p 为 新建 粒子。
    p 的 位置 为 位置。
    p 的 速度 为 速度。
    p 的 加速度 为 加速度。
    p 的 颜色 为 颜色始。
    p 的 结束颜色 为 颜色终。
    p 的 生命 为 生命。
    p 的 最大生命 为 生命。
    p 的 大小 为 大小始。
    p 的 结束大小 为 大小终。
    p 的 alpha 为 255。
    p 的 类型 为 类型。
    p 的 旋转 为 做 粒子_随机范围 用 0、360。
    p 的 旋转速度 为 做 粒子_随机范围 用 负 5、5。
    做 追加 用 粒子池、p。
    粒子_活跃数 为 粒子_活跃数 加 1。
    全局_粒子统计 的 总发射数 为 全局_粒子统计 的 总发射数 加 1。
    若 粒子_活跃数 大于 全局_粒子统计 的 峰值活跃数：
        全局_粒子统计 的 峰值活跃数 为 粒子_活跃数。
    完。
完。

粒子_爆发 接受 位置、数量、颜色、速度范围、生命：
    可变 i 为 0。
    只要 i 小于 数量 并且 粒子_活跃数 小于 粒子_最大：
        角度 为 i 乘 360 除以 数量。
        速 为 做 粒子_随机范围 用 速度范围 除以 2、速度范围。
        vx 为 速 乘 做 数学_余弦 用 角度 除以 1000。
        vy 为 速 乘 做 数学_正弦 用 角度 除以 1000。
        做 粒子_发射 用 位置、做 向量3_新 用 vx、vy、0、颜色、生命。
        i 为 i 加 1。
    完。
完。

创建发射器 接受 位置、方向、类型、速率：
    e 为 新建 粒子发射器。
    e 的 位置 为 位置。
    e 的 方向 为 方向。
    e 的 类型 为 类型。
    e 的 粒子类型 为 粒子类型_光点。
    e 的 速率 为 速率。
    e 的 初始速度_最小 为 5。
    e 的 初始速度_最大 为 15。
    e 的 初始大小_最小 为 1。
    e 的 初始大小_最大 为 3。
    e 的 生命_最小 为 30。
    e 的 生命_最大 为 90。
    e 的 颜色_开始 为 0x66EEFF。
    e 的 颜色_结束 为 0x003366。
    e 的 扩散角度 为 30。
    e 的 重力 为 0。
    e 的 风力 为 0。
    e 的 湍流强度 为 0。
    e 的 已发射总数 为 0。
    e 的 活跃 为 真。
    做 追加 用 发射器列表、e。
    返还 e。
完。

发射器_更新 接受 e、帧：
    若 非 e 的 活跃：
        返还。
    完。
    若 帧 取余于 e 的 速率 不等于 0：
        返还。
    完。
    可变 发射位置 为 e 的 位置。
    若 e 的 类型 等于 发射器_圆：
        角 为 做 粒子_随机范围 用 0、360。
        半径 为 做 粒子_随机范围 用 0、50。
        发射位置 为 做 向量3_新 用 e 的 位置 的 x 加 半径 乘 做 数学_余弦 用 角 除以 1000、e 的 位置 的 y 加 半径 乘 做 数学_正弦 用 角 除以 1000、e 的 位置 的 z。
    否则若 e 的 类型 等于 发射器_矩形：
        发射位置 为 做 向量3_新 用 e 的 位置 的 x 加 做 粒子_随机范围 用 负 100、100、e 的 位置 的 y 加 做 粒子_随机范围 用 负 100、100、e 的 位置 的 z。
    完。
    速 为 做 粒子_随机范围 用 e 的 初始速度_最小、e 的 初始速度_最大。
    扩散 为 做 粒子_随机范围 用 负 e 的 扩散角度、e 的 扩散角度。
    vx 为 e 的 方向 的 x 乘 速 除以 10 加 扩散 除以 10。
    vy 为 e 的 方向 的 y 乘 速 除以 10。
    加速 为 做 向量3_新 用 e 的 风力、e 的 重力、0。
    生命 为 做 粒子_随机范围 用 e 的 生命_最小、e 的 生命_最大。
    大小 为 做 粒子_随机范围 用 e 的 初始大小_最小、e 的 初始大小_最大。
    做 粒子_发射高级 用 发射位置、做 向量3_新 用 vx、vy、0、加速、e 的 颜色_开始、e 的 颜色_结束、生命、大小、1、e 的 粒子类型。
    e 的 已发射总数 为 e 的 已发射总数 加 1。
完。

粒子_更新并渲染 接受 fb：
    做 粒子_初始化。
    全局_粒子统计 的 总帧数 为 全局_粒子统计 的 总帧数 加 1。
    可变 e 为 0。
    只要 e 小于 做 计数 用 发射器列表：
        做 发射器_更新 用 发射器列表 的 第 e 项、全局_粒子统计 的 总帧数。
        e 为 e 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 粒子池：
        若 粒子池 的 第 i 项 的 生命 小于或等于 0：
            做 删除 用 粒子池、i。
            粒子_活跃数 为 粒子_活跃数 减 1。
            全局_粒子统计 的 总消亡数 为 全局_粒子统计 的 总消亡数 加 1。
            继续。
        完。
        p 为 粒子池 的 第 i 项。
        p 的 速度 为 做 向量3_加 用 p 的 速度、p 的 加速度。
        p 的 位置 为 做 向量3_加 用 p 的 位置、p 的 速度。
        p 的 生命 为 p 的 生命 减 1。
        p 的 旋转 为 p 的 旋转 加 p 的 旋转速度。
        进度 为 255 减 p 的 生命 乘 255 除以 p 的 最大生命。
        p 的 alpha 为 255 乘 p 的 生命 除以 p 的 最大生命。
        当前颜色 为 做 粒子_颜色插值 用 p 的 颜色、p 的 结束颜色、进度。
        当前大小 为 p 的 大小 加 (p 的 结束大小 减 p 的 大小) 乘 进度 除以 255。
        sx 为 渲染_宽 除以 2 加 p 的 位置 的 x 乘 渲染_宽 除以 2000。
        sy 为 渲染_高 除以 2 减 p 的 位置 的 y 乘 渲染_高 除以 2000。
        若 sx 大于或等于 0 并且 sx 小于 渲染_宽 并且 sy 大于或等于 0 并且 sy 小于 渲染_高：
            若 当前大小 小于或等于 1：
                做 全息_混合像素 用 fb、sx、sy、当前颜色、p 的 alpha。
                全局_粒子统计 的 总渲染像素数 为 全局_粒子统计 的 总渲染像素数 加 1。
            否则：
                可变 dy 为 负 当前大小。
                只要 dy 小于或等于 当前大小：
                    可变 dx 为 负 当前大小。
                    只要 dx 小于或等于 当前大小：
                        px 为 sx 加 dx。
                        py 为 sy 加 dy。
                        若 px 大于或等于 0 并且 px 小于 渲染_宽 并且 py 大于或等于 0 并且 py 小于 渲染_高：
                            距离 为 dx 乘 dx 加 dy 乘 dy。
                            若 距离 小于或等于 当前大小 乘 当前大小：
                                衰减alpha 为 p 的 alpha 乘 (当前大小 乘 当前大小 减 距离) 除以 (当前大小 乘 当前大小)。
                                做 全息_混合像素 用 fb、px、py、当前颜色、衰减alpha。
                                全局_粒子统计 的 总渲染像素数 为 全局_粒子统计 的 总渲染像素数 加 1。
                            完。
                        完。
                        dx 为 dx 加 1。
                    完。
                    dy 为 dy 加 1。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    全局_粒子统计 的 当前活跃数 为 粒子_活跃数。
完。

全息_环境粒子 接受 帧：
    若 帧 取余于 3 等于 0：
        随机x 为 做 粒子_伪随机 取余于 400 减 200。
        做 粒子_发射高级 用 做 向量3_新 用 随机x、负 500、0、做 向量3_新 用 0、15、0、做 向量3_新 用 0、0、0、0x66EEFF、0x003366、60、2、0、粒子类型_星尘。
    完。
    若 帧 取余于 10 等于 0：
        rx 为 做 粒子_伪随机 取余于 600 减 300。
        做 粒子_发射高级 用 做 向量3_新 用 rx、负 600、0、做 向量3_新 用 做 粒子_随机范围 用 负 3、3、8、0、做 向量3_新 用 0、0、0、0xFFAA44、0xFF2200、90、3、1、粒子类型_火花。
    完。
完。

粒子_清除全部 接受 零参：
    粒子池 为 一组：（空）。
    粒子_活跃数 为 0。
完。

粒子_打印统计 接受 零参：
    做 粒子_初始化。
    做 打印行 用 「=== 粒子系统统计 ===」。
    做 打印行 用 「总发射: 」 加 做 整数转字符串 用 全局_粒子统计 的 总发射数。
    做 打印行 用 「总消亡: 」 加 做 整数转字符串 用 全局_粒子统计 的 总消亡数。
    做 打印行 用 「当前活跃: 」 加 做 整数转字符串 用 全局_粒子统计 的 当前活跃数。
    做 打印行 用 「峰值活跃: 」 加 做 整数转字符串 用 全局_粒子统计 的 峰值活跃数。
    做 打印行 用 「总帧数: 」 加 做 整数转字符串 用 全局_粒子统计 的 总帧数。
    做 打印行 用 「总渲染像素: 」 加 做 整数转字符串 用 全局_粒子统计 的 总渲染像素数。
    做 打印行 用 「发射器数: 」 加 做 整数转字符串 用 做 计数 用 发射器列表。
完。

正弦近似 接受 角：
    返还 做 数学_正弦 用 角。
完。

余弦近似 接受 角：
    返还 做 数学_余弦 用 角。
完。

全息渲染_完整统计 接受 零参：
    做 粒子_初始化。
    返还 一组：全局_粒子统计 的 总帧数、粒子_活跃数、渲染_宽、渲染_高、全局_粒子统计 的 峰值活跃数、全局_粒子统计 的 总渲染像素数。
完。

可变 AI_粒子系统全_帧率窗口 为 一组：（空）。

AI_粒子系统全_帧率预测 接受 当前帧耗时：
    做 追加 用 AI_粒子系统全_帧率窗口、当前帧耗时。
    若 做 计数 用 AI_粒子系统全_帧率窗口 大于 30：
        做 列表_移除 用 AI_粒子系统全_帧率窗口、0。
    完。
    若 做 计数 用 AI_粒子系统全_帧率窗口 小于 3：
        返还 60。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_粒子系统全_帧率窗口：
        总 为 总 加 AI_粒子系统全_帧率窗口 的 第 i 项。
        i 为 i 加 1。
    完。
    平均帧耗时 为 总 除以 做 计数 用 AI_粒子系统全_帧率窗口。
    若 平均帧耗时 等于 0：
        返还 60。
    完。
    返还 1000 除以 平均帧耗时。
完。

AI_粒子系统全_质量降级建议 接受 帧率：
    若 帧率 小于 30：
        返还 「降低分辨率」。
    完。
    若 帧率 小于 45：
        返还 「跳过阴影」。
    完。
    返还 「全质量」。
完。

本章 收束。
