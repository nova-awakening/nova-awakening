《GPU命令队列驱动》

引用《..·中枢·日志中枢》。

引用《.·图处显示》。

引用《..·推理·通信总线》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·内容可见性》。

CMD_NOP 为 0x00。

CMD_DRAW 为 0x10。

CMD_BLIT 为 0x11。

CMD_FILL 为 0x12。

CMD_COMPUTE 为 0x20。

CMD_直传_COPY 为 0x30。

CMD_SET_MODE 为 0x40。

CMD_FLIP 为 0x41。

CMD_FENCE 为 0x50。

CMD_智能_DISPATCH 为 0x60。

VRAM 为 0。

GTT 为 1。

用途_帧缓冲 为 0。

用途_命令缓冲 为 1。

用途_计算缓冲 为 2。

用途_AI权重 为 3。

FENCE_UNSIGNALED 为 0。

FENCE_SIGNALED 为 1。

FENCE_TIMEOUT 为 2。

默认命令缓冲大小 为 262144。

命令大小 为 48。

最大队列数 为 8。

栅栏超时_默认 为 5000。

MMIO_FENCE_ADDR 为 0x6030。

AI_调度阈值 为 0.5。

图处命令队列 是 一种 事物，含 名称、缓冲地址、缓冲大小、写指针、读指针、MMIO_WPTR、MMIO_RPTR、挂起、已提交命令数、已完成命令数、优先级。

图处命令 是 一种 事物，含 类型、标志、参数0、参数1、参数2、参数3、栅栏值。

图处内存块 是 一种 事物，含 图处地址、CPU地址、物理地址、大小、类型、用途、引用计数、下一个。

图处栅栏 是 一种 事物，含 值、状态、队列索引、提交时间、完成时间。

GRCM统计 是 一种 事物，含 命令提交总数、命令完成总数、MMIO读次数、MMIO写次数、VRAM分配总量、GTT分配总量、栅栏等待次数、栅栏超时次数、DMA传输总量、AI调度次数。

可变 全局GRCM统计 为 新建 GRCM统计。

可变 图处_BAR0基址 为 0。

可变 图处_BAR0大小 为 0。

可变 图处_队列数组 为 0。

可变 图处_队列数量 为 0。

可变 图处_内存链表 为 0。

可变 图处_全局栅栏值 为 0。

可变 图处_VRAM已用 为 0。

可变 图处_GTT已用 为 0。

可变 图处_VRAM总量 为 0。

可变 图处_GTT总量 为 0。

MMIO_读32 接受 偏移：
    若 图处_BAR0基址 等于 0：
        返还 0。
    完。
    若 偏移 大于或等于 图处_BAR0大小：
        返还 0。
    完。
    全局GRCM统计 的 MMIO读次数 为 全局GRCM统计 的 MMIO读次数 加 1。
    返还 做 内存_读32_volatile 用 图处_BAR0基址 加 偏移。
完。

MMIO_分32 接受 偏移、值：
    若 图处_BAR0基址 等于 0：
        返还。
    完。
    若 偏移 大于或等于 图处_BAR0大小：
        返还。
    完。
    做 内存_分32_volatile 用 图处_BAR0基址 加 偏移、值。
    全局GRCM统计 的 MMIO写次数 为 全局GRCM统计 的 MMIO写次数 加 1。
完。

MMIO_读64 接受 偏移：
    低 为 做 MMIO_读32 用 偏移。
    高 为 做 MMIO_读32 用 偏移 加 4。
    返还 高 左移 32 位或 低。
完。

MMIO_分64 接受 偏移、值：
    做 MMIO_分32 用 偏移、值 位与 0xFFFFFFFF。
    做 MMIO_分32 用 偏移 加 4、值 右移 32 位与 0xFFFFFFFF。
完。

队列_初始化 接受 名称、优先级：
    若 图处_队列数量 大于或等于 最大队列数：
        返还 0。
    完。
    q 为 做 堆_分配 用 图处命令队列。
    若 q 等于 0：
        返还 0。
    完。
    buf 为 做 图处内存_分配 用 GTT、用途_命令缓冲、默认命令缓冲大小。
    若 buf 等于 0：
        做 堆_释放 用 q。
        返还 0。
    完。
    q 的 名称 为 名称。
    q 的 缓冲地址 为 buf 的 物理地址。
    q 的 缓冲大小 为 默认命令缓冲大小。
    q 的 写指针 为 0。
    q 的 读指针 为 0。
    q 的 挂起 为 假。
    q 的 已提交命令数 为 0。
    q 的 已完成命令数 为 0。
    q 的 优先级 为 优先级。
    若 图处_队列数组 等于 0：
        图处_队列数组 为 做 堆_分配 用 最大队列数 乘 8。
    完。
    做 内存_分64 用 图处_队列数组 加 图处_队列数量 乘 8、q。
    图处_队列数量 为 图处_队列数量 加 1。
    做 内核打印 用 「GRCM: 队列'{}'初始化 大小={}KB 优先级={}」、名称、默认命令缓冲大小 除以 1024、优先级。
    返还 q。
完。

队列_可用空间 接受 q：
    若 q 等于 0：
        返还 0。
    完。
    若 q 的 MMIO_RPTR 不等于 0：
        q 的 读指针 为 做 MMIO_读32 用 q 的 MMIO_RPTR。
    完。
    若 q 的 写指针 大于或等于 q 的 读指针：
        返还 q 的 缓冲大小 减 q 的 写指针 加 q 的 读指针 减 命令大小。
    完。
    返还 q 的 读指针 减 q 的 写指针 减 命令大小。
完。

队列_是否已满 接受 q：
    返还 做 队列_可用空间 用 q 小于 命令大小。
完。

队列_提交命令 接受 q、cmd：
    若 q 等于 0 或者 cmd 等于 0：
        返还 假。
    完。
    若 q 的 挂起：
        返还 假。
    完。
    若 做 队列_是否已满 用 q：
        可变 重试 为 0。
        只要 做 队列_是否已满 用 q 并且 重试 小于 1000：
            做 平台_微延迟 用 1。
            重试 为 重试 加 1。
        完。
        若 做 队列_是否已满 用 q：
            做 内核打印 用 「GRCM: 队列'{}'已满，命令丢弃」、q 的 名称。
            返还 假。
        完。
    完。
    写位置 为 q 的 缓冲地址 加 q 的 写指针。
    做 内存_分32 用 写位置、cmd 的 类型。
    做 内存_分32 用 写位置 加 4、cmd 的 标志。
    做 内存_分64 用 写位置 加 8、cmd 的 参数0。
    做 内存_分64 用 写位置 加 16、cmd 的 参数1。
    做 内存_分64 用 写位置 加 24、cmd 的 参数2。
    做 内存_分64 用 写位置 加 32、cmd 的 参数3。
    做 内存_分64 用 写位置 加 40、cmd 的 栅栏值。
    q 的 写指针 为 (q 的 写指针 加 命令大小) 取余于 q 的 缓冲大小。
    若 q 的 MMIO_WPTR 不等于 0：
        做 MMIO_分32 用 q 的 MMIO_WPTR、q 的 写指针。
    完。
    q 的 已提交命令数 为 q 的 已提交命令数 加 1。
    全局GRCM统计 的 命令提交总数 为 全局GRCM统计 的 命令提交总数 加 1。
    返还 真。
完。

队列_提交带栅栏 接受 q、cmd：
    图处_全局栅栏值 为 图处_全局栅栏值 加 1。
    cmd 的 栅栏值 为 图处_全局栅栏值。
    成功 为 做 队列_提交命令 用 q、cmd。
    若 成功：
        返还 图处_全局栅栏值。
    完。
    返还 0。
完。

图处内存_分配 接受 类型、用途、大小：
    若 大小 等于 0：
        返还 0。
    完。
    若 类型 等于 VRAM 并且 图处_VRAM已用 加 大小 大于 图处_VRAM总量：
        做 内核打印 用 「GRCM: VRAM分配失败 请求={}KB 剩余={}KB」、大小 除以 1024、(图处_VRAM总量 减 图处_VRAM已用) 除以 1024。
        返还 0。
    完。
    若 类型 等于 GTT 并且 图处_GTT已用 加 大小 大于 图处_GTT总量：
        做 内核打印 用 「GRCM: GTT分配失败 请求={}KB」、大小 除以 1024。
        返还 0。
    完。
    block 为 做 堆_分配 用 图处内存块。
    若 block 等于 0：
        返还 0。
    完。
    phys 为 做 平台_分配DMA内存 用 大小。
    若 phys 等于 0：
        做 堆_释放 用 block。
        返还 0。
    完。
    block 的 物理地址 为 phys。
    block 的 CPU地址 为 做 平台_ioremap 用 phys、大小。
    block 的 图处地址 为 phys。
    block 的 大小 为 大小。
    block 的 类型 为 类型。
    block 的 用途 为 用途。
    block 的 引用计数 为 1。
    block 的 下一个 为 图处_内存链表。
    图处_内存链表 为 block。
    若 类型 等于 VRAM：
        图处_VRAM已用 为 图处_VRAM已用 加 大小。
        全局GRCM统计 的 VRAM分配总量 为 全局GRCM统计 的 VRAM分配总量 加 大小。
    否则：
        图处_GTT已用 为 图处_GTT已用 加 大小。
        全局GRCM统计 的 GTT分配总量 为 全局GRCM统计 的 GTT分配总量 加 大小。
    完。
    返还 block。
完。

图处内存_释放 接受 block：
    若 block 等于 0：
        返还。
    完。
    block 的 引用计数 为 block 的 引用计数 减 1。
    若 block 的 引用计数 大于 0：
        返还。
    完。
    若 block 的 CPU地址 不等于 0：
        做 平台_iounmap 用 block 的 CPU地址、block 的 大小。
    完。
    做 平台_释放DMA内存 用 block 的 物理地址、block 的 大小。
    若 block 的 类型 等于 VRAM：
        图处_VRAM已用 为 图处_VRAM已用 减 block 的 大小。
    否则：
        图处_GTT已用 为 图处_GTT已用 减 block 的 大小。
    完。
    做 堆_释放 用 block。
完。

图处内存_增引用 接受 block：
    若 block 不等于 0：
        block 的 引用计数 为 block 的 引用计数 加 1。
    完。
完。

栅栏_提交 接受 q：
    cmd 为 新建 图处命令：类型 为 CMD_FENCE、标志 为 0、参数0 为 MMIO_FENCE_ADDR、参数1 为 0、参数2 为 0、参数3 为 0、栅栏值 为 0。
    返还 做 队列_提交带栅栏 用 q、cmd。
完。

栅栏_等待 接受 目标值、超时ms：
    全局GRCM统计 的 栅栏等待次数 为 全局GRCM统计 的 栅栏等待次数 加 1。
    开始 为 做 平台_获取高精度时间。
    可变 当前 为 做 MMIO_读64 用 MMIO_FENCE_ADDR。
    只要 当前 小于 目标值：
        已过 为 做 平台_获取高精度时间 减 开始。
        若 已过 大于 超时ms：
            全局GRCM统计 的 栅栏超时次数 为 全局GRCM统计 的 栅栏超时次数 加 1。
            做 内核打印 用 「GRCM: 栅栏超时 目标={} 当前={}」、目标值、当前。
            返还 FENCE_TIMEOUT。
        完。
        做 平台_微延迟 用 1。
        当前 为 做 MMIO_读64 用 MMIO_FENCE_ADDR。
    完。
    返还 FENCE_SIGNALED。
完。

栅栏_提交并等待 接受 q：
    fence_val 为 做 栅栏_提交 用 q。
    若 fence_val 等于 0：
        返还 FENCE_UNSIGNALED。
    完。
    返还 做 栅栏_等待 用 fence_val、栅栏超时_默认。
完。

DMA_复制 接受 q、源地址、目标地址、大小：
    cmd 为 新建 图处命令：类型 为 CMD_直传_COPY、标志 为 0、参数0 为 源地址、参数1 为 目标地址、参数2 为 大小、参数3 为 0、栅栏值 为 0。
    fence 为 做 队列_提交带栅栏 用 q、cmd。
    全局GRCM统计 的 DMA传输总量 为 全局GRCM统计 的 DMA传输总量 加 大小。
    返还 fence。
完。

DMA_复制并等待 接受 q、源地址、目标地址、大小：
    fence 为 做 DMA_复制 用 q、源地址、目标地址、大小。
    若 fence 等于 0：
        返还 FENCE_UNSIGNALED。
    完。
    返还 做 栅栏_等待 用 fence、栅栏超时_默认。
完。

图处_中断处理 接受 零参：
    irq_status 为 做 MMIO_读32 用 0x44。
    若 irq_status 等于 0：
        返还。
    完。
    若 irq_status 位与 0x01 不等于 0：
        可变 i 为 0。
        只要 i 小于 图处_队列数量：
            q 为 做 内存_读64 用 图处_队列数组 加 i 乘 8。
            若 q 不等于 0 并且 q 的 MMIO_RPTR 不等于 0：
                q 的 读指针 为 做 MMIO_读32 用 q 的 MMIO_RPTR。
            完。
            i 为 i 加 1。
        完。
        全局GRCM统计 的 命令完成总数 为 全局GRCM统计 的 命令完成总数 加 1。
    完。
    若 irq_status 位与 0x02 不等于 0：
    完。
    若 irq_status 位与 0x80 不等于 0：
        做 内核打印 用 「GRCM: 图处故障中断! status=0x{:x}」、irq_status。
        做 图处_故障恢复。
    完。
    做 MMIO_分32 用 0x44、irq_status。
完。

图处_故障恢复 接受 零参：
    做 MMIO_分32 用 0x7E、0x01。
    做 平台_微延迟 用 10。
    做 MMIO_分32 用 0x7E、0x00。
    可变 i 为 0。
    只要 i 小于 图处_队列数量：
        q 为 做 内存_读64 用 图处_队列数组 加 i 乘 8。
        若 q 不等于 0：
            q 的 写指针 为 0。
            q 的 读指针 为 0。
            q 的 挂起 为 假。
            若 q 的 MMIO_WPTR 不等于 0：
                做 MMIO_分32 用 q 的 MMIO_WPTR、0。
            完。
        完。
        i 为 i 加 1。
    完。
    做 内核打印 用 「GRCM: 图处故障恢复完成」。
完。

AI_GRCM调度分析 接受 cmd：
    若 cmd 等于 0：
        返还 0。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    feature 为 做 Tensor_创建 用 8。
    做 Tensor_写 用 feature、0、cmd 的 类型 除以 0x60。
    做 Tensor_写 用 feature、1、图处_VRAM已用 除以 图处_VRAM总量。
    做 Tensor_写 用 feature、2、图处_GTT已用 除以 图处_GTT总量。
    做 Tensor_写 用 feature、3、全局GRCM统计 的 命令提交总数 除以 100000.0。
    做 Tensor_写 用 feature、4、全局GRCM统计 的 栅栏超时次数 除以 100.0。
    做 Tensor_写 用 feature、5、图处_队列数量 除以 最大队列数。
    做 Tensor_写 用 feature、6、0.0。
    做 Tensor_写 用 feature、7、0.0。
    最佳队列 为 做 AI总线_推理 用 bus、「GRCM_队列选择」、feature。
    做 Tensor_释放 用 feature。
    全局GRCM统计 的 AI调度次数 为 全局GRCM统计 的 AI调度次数 加 1。
    idx 为 最佳队列 乘 图处_队列数量。
    若 idx 小于 0：
        idx 为 0。
    完。
    若 idx 大于或等于 图处_队列数量：
        idx 为 0。
    完。
    返还 idx。
完。

GRCM_获取统计 接受 零参：
    返还 一本字典：命令提交总数 对应 全局GRCM统计 的 命令提交总数、命令完成总数 对应 全局GRCM统计 的 命令完成总数、MMIO读 对应 全局GRCM统计 的 MMIO读次数、MMIO写 对应 全局GRCM统计 的 MMIO写次数、VRAM分配 对应 全局GRCM统计 的 VRAM分配总量、GTT分配 对应 全局GRCM统计 的 GTT分配总量、栅栏等待 对应 全局GRCM统计 的 栅栏等待次数、栅栏超时 对应 全局GRCM统计 的 栅栏超时次数、DMA传输 对应 全局GRCM统计 的 DMA传输总量、AI调度 对应 全局GRCM统计 的 AI调度次数。
完。

GRCM_打印统计 接受 零参：
    s 为 做 GRCM_获取统计。
    做 内核打印 用 「═══ GRCM图处统计 ═══」。
    做 内核打印 用 「命令[提交/完成]={}/{}」、s 的 命令提交总数、s 的 命令完成总数。
    做 内核打印 用 「MMIO[读/写]={}/{}」、s 的 MMIO读、s 的 MMIO写。
    做 内核打印 用 「内存[VRAM={}KB GTT={}KB]」、图处_VRAM已用 除以 1024、图处_GTT已用 除以 1024。
    做 内核打印 用 「栅栏[等待/超时]={}/{}」、s 的 栅栏等待、s 的 栅栏超时。
    做 内核打印 用 「DMA={}KB AI调度={}」、s 的 DMA传输 除以 1024、s 的 AI调度。
完。

GRCM_重置统计 接受 零参：
    全局GRCM统计 为 新建 GRCM统计。
完。

GRCM_初始化 接受 bar0基址、bar0大小、vram总量、gtt总量：
    图处_BAR0基址 为 bar0基址。
    图处_BAR0大小 为 bar0大小。
    图处_VRAM总量 为 vram总量。
    图处_GTT总量 为 gtt总量。
    图处_队列数量 为 0。
    图处_全局栅栏值 为 0。
    全局GRCM统计 为 新建 GRCM统计。
    做 内核打印 用 「GRCM: 初始化 BAR0=0x{:x} VRAM={}MB GTT={}MB」、bar0基址、vram总量 除以 1048576、gtt总量 除以 1048576。
完。

可变 _谐振GPU_批量阈值 为 8。

可变 _谐振GPU_调度次数 为 0。

谐振GPU_自适应批量 接受 队列深度、平均延迟us：
    若 队列深度 大于 32 并且 平均延迟us 大于 1000：
        _谐振GPU_批量阈值 为 4。
    完。
    做 否则如果 用 队列深度 小于 8 并且 平均延迟us 小于 200。
    块：
        _谐振GPU_批量阈值 为 16。
    完。
    否则。
    块：
        _谐振GPU_批量阈值 为 8。
    完。
    _谐振GPU_调度次数 为 _谐振GPU_调度次数 加 1。
    返还 _谐振GPU_批量阈值。
完。

GPU命令队列_完整统计 接受 零参：
    做 GRCM_打印统计。
    返还 一组：「队列数量」、图处_队列数量、「栅栏值」、图处_全局栅栏值、「VRAM总量」、图处_VRAM总量、「谐振批量阈值」、_谐振GPU_批量阈值、「谐振调度次数」、_谐振GPU_调度次数。
完。

本章 收束。
