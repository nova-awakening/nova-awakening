《音频音频》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

音频档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、设备数、编解数、流数、直传数、混音数、采样数、音量数、路由数、缓冲数、成功数、失败数、最近设备、最近流、最近格式、设备表、流表、混音表。

音频请求 是 一种 事物，含 动作、目标、参数、选项。

音频回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

音频摘要 是 一种 事物，含 名称、设备数、编解数、流数、直传数、混音数、采样数、音量数、路由数、缓冲数、成功数、失败数、健康。

音频设备项 是 一种 事物，含 标识、名称、类型、编解、声道、采样率、音量、静音。

音频流项 是 一种 事物，含 标识、设备、格式、采样率、声道、缓冲、状态、帧数、直传。

音频混音项 是 一种 事物，含 名称、输入、输出、增益、平衡。

可变 音频账本 为 一组：（空）。

可变 音频构建数 为 0。

可变 音频总动作 为 0。

可变 音频总成功 为 0。

可变 音频总失败 为 0。

音频音频_建档 接受 名称：
    档 为 新建 音频档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 设备数 为 0。
    档 的 编解数 为 0。
    档 的 流数 为 0。
    档 的 直传数 为 0。
    档 的 混音数 为 0。
    档 的 采样数 为 0。
    档 的 音量数 为 0。
    档 的 路由数 为 0。
    档 的 缓冲数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    档 的 最近设备 为 「」。
    档 的 最近流 为 「」。
    档 的 最近格式 为 「」。
    档 的 设备表 为 一组：（空）。
    档 的 流表 为 一组：（空）。
    档 的 混音表 为 一组：（空）。
    返还 档。
完。

音频音频_登记 接受 档：
    做 追加 用 音频账本、档。
    音频构建数 为 音频构建数 加 1。
    返还 档。
完。

音频音频_完整统计 接受 零参：
    返还 一组：音频构建数、音频总动作、音频总成功、音频总失败。
完。

音频音频_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 音频回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

音频音频_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    音频总动作 为 音频总动作 加 1。
完。

音频音频_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 设备数 等于 0 或者 档 的 流数 等于 0：
        档 的 最近健康 为 「预热」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 0：
        档 的 最近健康 为 「监视」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「运行」。
    返还 档 的 最近健康。
完。

音频音频_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 档 的 名称 等于 「」：
        返还 假。
    完。
    返还 真。
完。

音频音频_成功 接受 档、来源、数据：
    档 的 最近结果 为 数据。
    档 的 成功数 为 档 的 成功数 加 1。
    音频总成功 为 音频总成功 加 1。
    档 的 最近统计 为 做 音频音频_完整统计。
    做 音频音频_刷新健康 用 档。
    返还 做 音频音频_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

音频音频_失败 接受 档、来源、错因：
    若 档 不等于 空：
        档 的 最近错误 为 错因。
        档 的 失败数 为 档 的 失败数 加 1。
        档 的 最近统计 为 做 音频音频_完整统计。
        做 音频音频_刷新健康 用 档。
    完。
    音频总失败 为 音频总失败 加 1。
    返还 做 音频音频_建回证 用 真、假、来源、错因、空、做 音频音频_完整统计。
完。

音频音频_建设备 接受 标识、名称、类型、编解、声道、采样率：
    设备 为 新建 音频设备项。
    设备 的 标识 为 标识。
    设备 的 名称 为 名称。
    设备 的 类型 为 类型。
    设备 的 编解 为 编解。
    设备 的 声道 为 声道。
    设备 的 采样率 为 采样率。
    设备 的 音量 为 80。
    设备 的 静音 为 假。
    返还 设备。
完。

音频音频_建流 接受 标识、设备、格式、采样率、声道、缓冲、直传：
    流 为 新建 音频流项。
    流 的 标识 为 标识。
    流 的 设备 为 设备。
    流 的 格式 为 格式。
    流 的 采样率 为 采样率。
    流 的 声道 为 声道。
    流 的 缓冲 为 缓冲。
    流 的 状态 为 「待机」。
    流 的 帧数 为 0。
    流 的 直传 为 直传。
    返还 流。
完。

音频音频_找设备 接受 档、标识：
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 设备表：
        项 为 档 的 设备表 的 第 索引 项。
        若 项 的 标识 等于 标识：
            返还 项。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

音频音频_找流 接受 档、标识：
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 流表：
        项 为 档 的 流表 的 第 索引 项。
        若 项 的 标识 等于 标识：
            返还 项。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

音频音频_登记设备 接受 档、标识、名称、类型、编解、声道、采样率：
    若 非 做 音频音频_检查档 用 档：
        返还 做 音频音频_失败 用 档、「登记设备」、「音频档不可用」。
    完。
    做 音频音频_记录 用 档、「登记设备」、一组：标识、名称、类型、编解、声道、采样率、「音频音频」。
    若 做 音频音频_找设备 用 档、标识 不等于 空：
        返还 做 音频音频_失败 用 档、「登记设备」、「音频设备已存在」。
    完。
    设备 为 做 音频音频_建设备 用 标识、名称、类型、编解、声道、采样率。
    做 追加 用 档 的 设备表、设备。
    档 的 设备数 为 档 的 设备数 加 1。
    档 的 编解数 为 档 的 编解数 加 1。
    档 的 最近设备 为 标识。
    档 的 最近格式 为 编解。
    返还 做 音频音频_成功 用 档、「登记设备」、设备。
完。

音频音频_枚举编解 接受 档：
    若 非 做 音频音频_检查档 用 档：
        返还 做 音频音频_失败 用 档、「枚举编解」、「音频档不可用」。
    完。
    做 音频音频_记录 用 档、「枚举编解」、一组：（空）、「音频音频」。
    结果 为 一组：「脉码」、「浮码」、「白璃声码」、「通道编组」。
    档 的 编解数 为 做 计数 用 结果。
    返还 做 音频音频_成功 用 档、「枚举编解」、结果。
完。

音频音频_开流 接受 档、标识、设备标识、格式、采样率、声道、缓冲、直传：
    若 非 做 音频音频_检查档 用 档：
        返还 做 音频音频_失败 用 档、「开流」、「音频档不可用」。
    完。
    做 音频音频_记录 用 档、「开流」、一组：标识、设备标识、格式、采样率、声道、缓冲、直传、「音频音频」。
    若 做 音频音频_找设备 用 档、设备标识 等于 空：
        返还 做 音频音频_失败 用 档、「开流」、「音频设备不存在」。
    完。
    流 为 做 音频音频_建流 用 标识、设备标识、格式、采样率、声道、缓冲、直传。
    流 的 状态 为 「运行」。
    做 追加 用 档 的 流表、流。
    档 的 流数 为 档 的 流数 加 1。
    档 的 缓冲数 为 档 的 缓冲数 加 缓冲。
    档 的 最近流 为 标识。
    档 的 最近设备 为 设备标识。
    档 的 最近格式 为 格式。
    若 直传：
        档 的 直传数 为 档 的 直传数 加 1。
    完。
    返还 做 音频音频_成功 用 档、「开流」、流。
完。

音频音频_写流 接受 档、流标识、帧数：
    若 非 做 音频音频_检查档 用 档：
        返还 做 音频音频_失败 用 档、「写流」、「音频档不可用」。
    完。
    做 音频音频_记录 用 档、「写流」、一组：流标识、帧数、「音频音频」。
    流 为 做 音频音频_找流 用 档、流标识。
    若 流 等于 空：
        返还 做 音频音频_失败 用 档、「写流」、「音频流不存在」。
    完。
    流 的 帧数 为 流 的 帧数 加 帧数。
    流 的 状态 为 「播放」。
    档 的 采样数 为 档 的 采样数 加 帧数。
    档 的 最近流 为 流标识。
    返还 做 音频音频_成功 用 档、「写流」、一组：流标识、流 的 帧数。
完。

音频音频_停流 接受 档、流标识：
    若 非 做 音频音频_检查档 用 档：
        返还 做 音频音频_失败 用 档、「停流」、「音频档不可用」。
    完。
    做 音频音频_记录 用 档、「停流」、一组：流标识、「音频音频」。
    流 为 做 音频音频_找流 用 档、流标识。
    若 流 等于 空：
        返还 做 音频音频_失败 用 档、「停流」、「音频流不存在」。
    完。
    流 的 状态 为 「停止」。
    档 的 最近流 为 流标识。
    返还 做 音频音频_成功 用 档、「停流」、流标识。
完。

音频音频_音量 接受 档、设备标识、音量：
    若 非 做 音频音频_检查档 用 档：
        返还 做 音频音频_失败 用 档、「音量」、「音频档不可用」。
    完。
    做 音频音频_记录 用 档、「音量」、一组：设备标识、音量、「音频音频」。
    设备 为 做 音频音频_找设备 用 档、设备标识。
    若 设备 等于 空：
        返还 做 音频音频_失败 用 档、「音量」、「音频设备不存在」。
    完。
    设备 的 音量 为 音量。
    档 的 音量数 为 档 的 音量数 加 1。
    档 的 最近设备 为 设备标识。
    返还 做 音频音频_成功 用 档、「音量」、一组：设备标识、音量。
完。

音频音频_静音 接受 档、设备标识、是否静音：
    若 非 做 音频音频_检查档 用 档：
        返还 做 音频音频_失败 用 档、「静音」、「音频档不可用」。
    完。
    做 音频音频_记录 用 档、「静音」、一组：设备标识、是否静音、「音频音频」。
    设备 为 做 音频音频_找设备 用 档、设备标识。
    若 设备 等于 空：
        返还 做 音频音频_失败 用 档、「静音」、「音频设备不存在」。
    完。
    设备 的 静音 为 是否静音。
    档 的 音量数 为 档 的 音量数 加 1。
    档 的 最近设备 为 设备标识。
    返还 做 音频音频_成功 用 档、「静音」、一组：设备标识、是否静音。
完。

音频音频_路由 接受 档、流标识、新设备：
    若 非 做 音频音频_检查档 用 档：
        返还 做 音频音频_失败 用 档、「路由」、「音频档不可用」。
    完。
    做 音频音频_记录 用 档、「路由」、一组：流标识、新设备、「音频音频」。
    流 为 做 音频音频_找流 用 档、流标识。
    若 流 等于 空：
        返还 做 音频音频_失败 用 档、「路由」、「音频流不存在」。
    完。
    若 做 音频音频_找设备 用 档、新设备 等于 空：
        返还 做 音频音频_失败 用 档、「路由」、「目标设备不存在」。
    完。
    流 的 设备 为 新设备。
    档 的 路由数 为 档 的 路由数 加 1。
    档 的 最近流 为 流标识。
    档 的 最近设备 为 新设备。
    返还 做 音频音频_成功 用 档、「路由」、一组：流标识、新设备。
完。

音频音频_建混音 接受 档、名称、输入、输出、增益、平衡：
    若 非 做 音频音频_检查档 用 档：
        返还 做 音频音频_失败 用 档、「建混音」、「音频档不可用」。
    完。
    做 音频音频_记录 用 档、「建混音」、一组：名称、输入、输出、增益、平衡、「音频音频」。
    项 为 新建 音频混音项。
    项 的 名称 为 名称。
    项 的 输入 为 输入。
    项 的 输出 为 输出。
    项 的 增益 为 增益。
    项 的 平衡 为 平衡。
    做 追加 用 档 的 混音表、项。
    档 的 混音数 为 档 的 混音数 加 1。
    返还 做 音频音频_成功 用 档、「建混音」、项。
完。

音频音频_软件混音 接受 档、名称：
    若 非 做 音频音频_检查档 用 档：
        返还 做 音频音频_失败 用 档、「软件混音」、「音频档不可用」。
    完。
    做 音频音频_记录 用 档、「软件混音」、一组：名称、「音频音频」。
    索引 为 0。
    总帧 为 0。
    只要 索引 小于 做 计数 用 档 的 流表：
        总帧 为 总帧 加 档 的 流表 的 第 索引 项 的 帧数。
        索引 为 索引 加 1。
    完。
    档 的 混音数 为 档 的 混音数 加 1。
    返还 做 音频音频_成功 用 档、「软件混音」、一组：名称、总帧、做 计数 用 档 的 流表。
完。

音频音频_直传统计 接受 档：
    若 非 做 音频音频_检查档 用 档：
        返还 做 音频音频_失败 用 档、「直传统计」、「音频档不可用」。
    完。
    做 音频音频_记录 用 档、「直传统计」、一组：（空）、「音频音频」。
    结果 为 一组：（空）。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 流表：
        流 为 档 的 流表 的 第 索引 项。
        若 流 的 直传：
            做 追加 用 结果、一组：流 的 标识、流 的 设备、流 的 帧数。
        完。
        索引 为 索引 加 1。
    完。
    返还 做 音频音频_成功 用 档、「直传统计」、结果。
完。

音频音频_摘要 接受 档：
    摘要 为 新建 音频摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 设备数 为 档 的 设备数。
    摘要 的 编解数 为 档 的 编解数。
    摘要 的 流数 为 档 的 流数。
    摘要 的 直传数 为 档 的 直传数。
    摘要 的 混音数 为 档 的 混音数。
    摘要 的 采样数 为 档 的 采样数。
    摘要 的 音量数 为 档 的 音量数。
    摘要 的 路由数 为 档 的 路由数。
    摘要 的 缓冲数 为 档 的 缓冲数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 音频音频_刷新健康 用 档。
    返还 摘要。
完。

音频音频_初始化 接受 档：
    若 非 做 音频音频_检查档 用 档：
        返还 做 音频音频_失败 用 档、「初始化」、「音频档不可用」。
    完。
    做 音频音频_记录 用 档、「初始化」、一组：（空）、「音频音频」。
    档 的 设备表 为 一组：（空）。
    档 的 流表 为 一组：（空）。
    档 的 混音表 为 一组：（空）。
    档 的 设备数 为 0。
    档 的 流数 为 0。
    档 的 混音数 为 0。
    档 的 是否初始化 为 真。
    做 音频音频_枚举编解 用 档。
    做 音频音频_登记设备 用 档、「播一」、「白璃扬声器」、「输出」、「脉码」、2、48000。
    做 音频音频_登记设备 用 档、「录一」、「白璃麦克风」、「输入」、「脉码」、2、48000。
    做 音频音频_开流 用 档、「流一」、「播一」、「脉码」、48000、2、4096、真。
    做 音频音频_建混音 用 档、「主混」、一组：「流一」、「播一」、1.0、0。
    返还 做 音频音频_成功 用 档、「初始化」、「已初始化」。
完。

音频音频_自检 接受 档：
    若 非 做 音频音频_检查档 用 档：
        返还 假。
    完。
    若 做 音频音频_初始化 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 音频音频_写流 用 档、「流一」、1024 的 已成 等于 假：
        返还 假。
    完。
    若 做 音频音频_音量 用 档、「播一」、86 的 已成 等于 假：
        返还 假。
    完。
    若 做 音频音频_静音 用 档、「录一」、假 的 已成 等于 假：
        返还 假。
    完。
    若 做 音频音频_开流 用 档、「流二」、「播一」、「浮码」、48000、2、2048、假 的 已成 等于 假：
        返还 假。
    完。
    若 做 音频音频_写流 用 档、「流二」、768 的 已成 等于 假：
        返还 假。
    完。
    若 做 音频音频_路由 用 档、「流二」、「播一」 的 已成 等于 假：
        返还 假。
    完。
    若 做 音频音频_软件混音 用 档、「主混」 的 已成 等于 假：
        返还 假。
    完。
    若 做 音频音频_直传统计 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 音频音频_停流 用 档、「流二」 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

音频音频_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 音频音频_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        返还 做 音频音频_初始化 用 档。
    完。
    若 请求 的 动作 等于 「登记设备」：
        返还 做 音频音频_登记设备 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项、请求 的 参数 的 第 4 项、请求 的 参数 的 第 5 项。
    完。
    若 请求 的 动作 等于 「枚举编解」：
        返还 做 音频音频_枚举编解 用 档。
    完。
    若 请求 的 动作 等于 「开流」：
        返还 做 音频音频_开流 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项、请求 的 参数 的 第 4 项、请求 的 参数 的 第 5 项、请求 的 参数 的 第 6 项。
    完。
    若 请求 的 动作 等于 「写流」：
        返还 做 音频音频_写流 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「停流」：
        返还 做 音频音频_停流 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「音量」：
        返还 做 音频音频_音量 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「静音」：
        返还 做 音频音频_静音 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「路由」：
        返还 做 音频音频_路由 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「建混音」：
        返还 做 音频音频_建混音 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项、请求 的 参数 的 第 4 项。
    完。
    若 请求 的 动作 等于 「软件混音」：
        返还 做 音频音频_软件混音 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「直传统计」：
        返还 做 音频音频_直传统计 用 档。
    完。
    若 请求 的 动作 等于 「摘要」：
        返还 做 音频音频_成功 用 档、「摘要」、做 音频音频_摘要 用 档。
    完。
    返还 做 音频音频_失败 用 档、「处理」、「未知动作」。
完。

可变 AI_音频音频_故障预警数 为 0。

AI_音频音频_设备健康 接受 错误率、响应时间、温度：
    可变 分 为 1000。
    若 错误率 大于 10：
        分 为 分 减 错误率 乘 5。
    完。
    若 响应时间 大于 1000：
        分 为 分 减 200。
    完。
    若 温度 大于 80：
        分 为 分 减 300。
    完。
    若 分 小于 300：
        AI_音频音频_故障预警数 为 AI_音频音频_故障预警数 加 1。
    完。
    返还 分。
完。

本章 收束。
