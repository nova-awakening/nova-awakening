《通串存储》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·中断表注册》。

引用《..·感知·内核直连层》。

引用《.·信息打印》。

引用《..·中枢·链表后继》。

引用《.·设备枚举》。

引用《..·中枢·整数转字符串》。

通串_存储_初始化 接受 dev：
    做 内核信息 用 「通串-存储: 」 加 dev 的 产品名 加 「 (U盘/移动硬盘)」。
    dev 的 下一个标签 为 1。
    可变 cbw 为 做 通串_CBW_创建。
    cbw 的 标签 为 dev 的 下一个标签。
    dev 的 下一个标签 为 dev 的 下一个标签 加 1。
    cbw 的 传输长度 为 0。
    cbw 的 标志 为 0。
    cbw 的 LUN 为 0。
    cbw 的 命令长度 为 6。
    cbw 的 命令 的 第 0 项 为 0x00。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量OUT端点、cbw、31。
    若 结果 小于 0：
        做 内核信息 用 「通串-存储: TEST UNIT READY 失败」。
        返还 结果。
    完。
    可变 csw 为 做 通串_CSW_创建。
    做 通串_批量传输 用 dev、dev 的 批量IN端点、csw、13。
    cbw 为 做 通串_CBW_创建。
    cbw 的 标签 为 dev 的 下一个标签。
    dev 的 下一个标签 为 dev 的 下一个标签 加 1。
    cbw 的 传输长度 为 36。
    cbw 的 标志 为 0x80。
    cbw 的 LUN 为 0。
    cbw 的 命令长度 为 6。
    cbw 的 命令 的 第 0 项 为 0x12。
    cbw 的 命令 的 第 4 项 为 36。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量OUT端点、cbw、31。
    若 结果 大于或等于 0：
        inquiry缓冲 为 做 堆_分配 用 36。
        做 通串_批量传输 用 dev、dev 的 批量IN端点、inquiry缓冲、36。
        csw 为 做 通串_CSW_创建。
        做 通串_批量传输 用 dev、dev 的 批量IN端点、csw、13。
        dev 的 设备类型 为 做 内存_读8 用 inquiry缓冲 位与 0x1F。
    完。
    cbw 为 做 通串_CBW_创建。
    cbw 的 标签 为 dev 的 下一个标签。
    dev 的 下一个标签 为 dev 的 下一个标签 加 1。
    cbw 的 传输长度 为 8。
    cbw 的 标志 为 0x80。
    cbw 的 LUN 为 0。
    cbw 的 命令长度 为 10。
    cbw 的 命令 的 第 0 项 为 0x25。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量OUT端点、cbw、31。
    若 结果 大于或等于 0：
        cap缓冲 为 做 堆_分配 用 8。
        做 通串_批量传输 用 dev、dev 的 批量IN端点、cap缓冲、8。
        csw 为 做 通串_CSW_创建。
        做 通串_批量传输 用 dev、dev 的 批量IN端点、csw、13。
        dev 的 最大LBA 为 做 内存_读8 用 cap缓冲 左移 24 位或 做 内存_读8 用 cap缓冲 加 1 左移 16 位或 做 内存_读8 用 cap缓冲 加 2 左移 8 位或 做 内存_读8 用 cap缓冲 加 3。
        dev 的 扇区大小 为 做 内存_读8 用 cap缓冲 加 4 左移 24 位或 做 内存_读8 用 cap缓冲 加 5 左移 16 位或 做 内存_读8 用 cap缓冲 加 6 左移 8 位或 做 内存_读8 用 cap缓冲 加 7。
        若 dev 的 扇区大小 等于 0：
            dev 的 扇区大小 为 512。
        完。
        容量MB 为 dev 的 最大LBA 除以 1024 乘 dev 的 扇区大小 除以 1024。
        做 内核信息 用 「通串-存储: 容量=」 加 做 整数转字符串 用 容量MB 加 「MB, 扇区=」 加 做 整数转字符串 用 dev 的 扇区大小 加 「B」。
    完。
    dev 的 已初始化 为 真。
    做 devtmpfs_创建_设备 用 「/dev/sda」。
    做 内核信息 用 「通串-存储: 已注册 /dev/sda」。
    返还 0。
完。

通串_存储_读扇区 接受 dev、LBA、扇区数、缓冲：
    可变 cbw 为 做 通串_CBW_创建。
    cbw 的 标签 为 dev 的 下一个标签。
    dev 的 下一个标签 为 dev 的 下一个标签 加 1。
    cbw 的 传输长度 为 扇区数 乘 512。
    cbw 的 标志 为 0x80。
    cbw 的 LUN 为 0。
    cbw 的 命令长度 为 10。
    cbw 的 命令 的 第 0 项 为 0x28。
    cbw 的 命令 的 第 2 项 为 LBA 右移 24 位与 0xFF。
    cbw 的 命令 的 第 3 项 为 LBA 右移 16 位与 0xFF。
    cbw 的 命令 的 第 4 项 为 LBA 右移 8 位与 0xFF。
    cbw 的 命令 的 第 5 项 为 LBA 位与 0xFF。
    cbw 的 命令 的 第 7 项 为 扇区数 右移 8 位与 0xFF。
    cbw 的 命令 的 第 8 项 为 扇区数 位与 0xFF。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量OUT端点、cbw、31。
    若 结果 小于 0：
        返还 结果。
    完。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量IN端点、缓冲、扇区数 乘 512。
    若 结果 小于 0：
        返还 结果。
    完。
    可变 csw 为 做 通串_CSW_创建。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量IN端点、csw、13。
    若 结果 小于 0：
        返还 结果。
    完。
    若 csw 的 状态 不等于 0：
        返还 错误_IO。
    完。
    返还 0。
完。

通串_存储_写扇区 接受 dev、LBA、扇区数、数据：
    可变 cbw 为 做 通串_CBW_创建。
    cbw 的 标签 为 dev 的 下一个标签。
    dev 的 下一个标签 为 dev 的 下一个标签 加 1。
    cbw 的 传输长度 为 扇区数 乘 512。
    cbw 的 标志 为 0x00。
    cbw 的 LUN 为 0。
    cbw 的 命令长度 为 10。
    cbw 的 命令 的 第 0 项 为 0x2A。
    cbw 的 命令 的 第 2 项 为 LBA 右移 24 位与 0xFF。
    cbw 的 命令 的 第 3 项 为 LBA 右移 16 位与 0xFF。
    cbw 的 命令 的 第 4 项 为 LBA 右移 8 位与 0xFF。
    cbw 的 命令 的 第 5 项 为 LBA 位与 0xFF。
    cbw 的 命令 的 第 7 项 为 扇区数 右移 8 位与 0xFF。
    cbw 的 命令 的 第 8 项 为 扇区数 位与 0xFF。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量OUT端点、cbw、31。
    若 结果 小于 0：
        返还 结果。
    完。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量OUT端点、数据、扇区数 乘 512。
    若 结果 小于 0：
        返还 结果。
    完。
    可变 csw 为 做 通串_CSW_创建。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量IN端点、csw、13。
    若 结果 小于 0：
        返还 结果。
    完。
    若 csw 的 状态 不等于 0：
        返还 错误_IO。
    完。
    返还 0。
完。

通串_摄像头_初始化 接受 dev：
    做 内核信息 用 「UVC: 」 加 dev 的 产品名 加 「 (摄像头)」。
    做 devtmpfs_创建_设备 用 「/dev/video0」。
完。

通串_摄像头_获取帧 接受 dev、缓冲、宽、高：
    若 dev 等于 0 或者 缓冲 等于 0：
        返还 负 1。
    完。
    若 非 dev 的 流激活：
        返还 负 2。
    完。
    帧大小 为 宽 乘 高 乘 2。
    已读 为 做 通串_同步传输_读 用 dev 的 端点、缓冲、帧大小。
    若 已读 大于 0：
        dev 的 帧计数 为 dev 的 帧计数 加 1。
    完。
    返还 已读。
完。

通串_热插拔_检测 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 通串控制器列表：
        ctl 为 通串控制器列表 的 第 i 项。
        可变 端口 为 0。
        只要 端口 小于 ctl 的 端口数：
            有设备 为 做 通串_端口有设备 用 ctl、端口。
            已知 为 做 通串_端口已枚举 用 ctl、端口。
            若 有设备 并且 非 已知：
                做 内核信息 用 「通串: 新设备插入 端口」 加 做 整数转字符串 用 端口。
                dev 为 做 通串_枚举设备 用 ctl、端口。
                若 dev 不等于 空：
                    做 追加 用 通串设备列表、dev。
                    做 通串_匹配驱动 用 dev。
                    做 通串_打印设备 用 dev。
                完。
            完。
            若 非 有设备 并且 已知：
                做 内核信息 用 「通串: 设备拔出 端口」 加 做 整数转字符串 用 端口。
                做 通串_移除设备 用 ctl、端口。
            完。
            端口 为 端口 加 1。
        完。
        i 为 i 加 1。
    完。
完。

通串_移除设备 接受 ctl、端口：
    可变 i 为 0。
    只要 i 小于 做 计数 用 通串设备列表：
        若 通串设备列表 的 第 i 项 的 端口 等于 端口：
            做 内核信息 用 「通串: 移除 」 加 通串设备列表 的 第 i 项 的 产品名。
            做 链表_删除 用 通串设备列表、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

CBW_签名 为 0x43425355。

CSW_签名 为 0x53425355。

CBW_大小 为 31。

CSW_大小 为 13。

通串_CBW 是 一种 事物，含 签名、标签、传输长度、标志、LUN、命令长度、命令。

通串_CSW 是 一种 事物，含 签名、标签、数据残余、状态。

通串_CBW_创建 接受 零参：
    cbw 为 新建 通串_CBW。
    cbw 的 签名 为 CBW_签名。
    cbw 的 标签 为 0。
    cbw 的 传输长度 为 0。
    cbw 的 标志 为 0。
    cbw 的 LUN 为 0。
    cbw 的 命令长度 为 0。
    cbw 的 命令 为 做 堆_分配 用 16。
    可变 i 为 0。
    只要 i 小于 16：
        做 内存_写8 用 cbw 的 命令 加 i、0。
        i 为 i 加 1。
    完。
    返还 cbw。
完。

通串_CSW_创建 接受 零参：
    csw 为 新建 通串_CSW。
    csw 的 签名 为 0。
    csw 的 标签 为 0。
    csw 的 数据残余 为 0。
    csw 的 状态 为 0。
    返还 csw。
完。

通串_CSW_是成功 接受 csw：
    若 csw 等于 0：
        返还 假。
    完。
    返还 csw 的 签名 等于 CSW_签名 并且 csw 的 状态 等于 0。
完。

SCSI_TEST_UNIT_READY 为 0x00。

SCSI_REQUEST_SENSE 为 0x03。

SCSI_INQUIRY 为 0x12。

SCSI_READ_CAPACITY 为 0x25。

SCSI_READ_10 为 0x28。

SCSI_WRITE_10 为 0x2A。

SCSI_MODE_SENSE 为 0x5A。

SCSI_START_STOP 为 0x1B。

SCSI_命令名 接受 opcode：
    若 opcode 等于 SCSI_TEST_UNIT_READY：
        返还 「TEST UNIT READY」。
    完。
    若 opcode 等于 SCSI_REQUEST_SENSE：
        返还 「REQUEST SENSE」。
    完。
    若 opcode 等于 SCSI_INQUIRY：
        返还 「INQUIRY」。
    完。
    若 opcode 等于 SCSI_READ_CAPACITY：
        返还 「READ CAPACITY」。
    完。
    若 opcode 等于 SCSI_READ_10：
        返还 「READ(10)」。
    完。
    若 opcode 等于 SCSI_WRITE_10：
        返还 「WRITE(10)」。
    完。
    若 opcode 等于 SCSI_MODE_SENSE：
        返还 「MODE SENSE」。
    完。
    若 opcode 等于 SCSI_START_STOP：
        返还 「START/STOP」。
    完。
    返还 「UNKNOWN(0x」 加 做 十六进制 用 opcode 加 「)」。
完。

通串_存储_查询容量 接受 dev：
    可变 cbw 为 做 通串_CBW_创建。
    cbw 的 标签 为 dev 的 下一个标签。
    dev 的 下一个标签 为 dev 的 下一个标签 加 1。
    cbw 的 传输长度 为 8。
    cbw 的 标志 为 0x80。
    cbw 的 命令长度 为 10。
    cbw 的 命令 的 第 0 项 为 SCSI_READ_CAPACITY。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量OUT端点、cbw、CBW_大小。
    若 结果 小于 0：
        返还 0。
    完。
    数据 为 做 堆_分配 用 8。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量IN端点、数据、8。
    若 结果 小于 0：
        返还 0。
    完。
    最大LBA 为 做 内存_读8 用 数据 左移 24 位或 做 内存_读8 用 数据 加 1 左移 16 位或 做 内存_读8 用 数据 加 2 左移 8 位或 做 内存_读8 用 数据 加 3。
    块大小 为 做 内存_读8 用 数据 加 4 左移 24 位或 做 内存_读8 用 数据 加 5 左移 16 位或 做 内存_读8 用 数据 加 6 左移 8 位或 做 内存_读8 用 数据 加 7。
    返还 一本字典：最大LBA 对应 最大LBA、块大小 对应 块大小、总字节 对应 (最大LBA 加 1) 乘 块大小。
完。

通串_存储_查询信息 接受 dev：
    可变 cbw 为 做 通串_CBW_创建。
    cbw 的 标签 为 dev 的 下一个标签。
    dev 的 下一个标签 为 dev 的 下一个标签 加 1。
    cbw 的 传输长度 为 36。
    cbw 的 标志 为 0x80。
    cbw 的 命令长度 为 6。
    cbw 的 命令 的 第 0 项 为 SCSI_INQUIRY。
    cbw 的 命令 的 第 4 项 为 36。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量OUT端点、cbw、CBW_大小。
    若 结果 小于 0：
        返还 空。
    完。
    数据 为 做 堆_分配 用 36。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量IN端点、数据、36。
    若 结果 小于 0：
        返还 空。
    完。
    返还 一本字典：类型 对应 做 内存_读8 用 数据 位与 0x1F、可移除 对应 做 内存_读8 用 数据 加 1 位与 0x80 不等于 0。
完。

通串_存储_测试就绪 接受 dev：
    可变 cbw 为 做 通串_CBW_创建。
    cbw 的 标签 为 dev 的 下一个标签。
    dev 的 下一个标签 为 dev 的 下一个标签 加 1。
    cbw 的 传输长度 为 0。
    cbw 的 标志 为 0。
    cbw 的 命令长度 为 6。
    cbw 的 命令 的 第 0 项 为 SCSI_TEST_UNIT_READY。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量OUT端点、cbw、CBW_大小。
    若 结果 小于 0：
        返还 假。
    完。
    csw 为 做 通串_CSW_创建。
    结果 为 做 通串_批量传输 用 dev、dev 的 批量IN端点、csw、CSW_大小。
    返还 做 通串_CSW_是成功 用 csw。
完。

通串_存储_弹出 接受 dev：
    可变 cbw 为 做 通串_CBW_创建。
    cbw 的 标签 为 dev 的 下一个标签。
    dev 的 下一个标签 为 dev 的 下一个标签 加 1。
    cbw 的 传输长度 为 0。
    cbw 的 标志 为 0。
    cbw 的 命令长度 为 6。
    cbw 的 命令 的 第 0 项 为 SCSI_START_STOP。
    cbw 的 命令 的 第 4 项 为 0x02。
    返还 做 通串_批量传输 用 dev、dev 的 批量OUT端点、cbw、CBW_大小。
完。

通串_批量传输 接受 dev、端点、数据、长度：
    若 dev 等于 0 或者 端点 等于 0：
        返还 错误_无效参数。
    完。
    通串存储_传输总数 为 通串存储_传输总数 加 1。
    返还 长度。
完。

通串_端口已枚举 接受 ctl、端口：
    可变 i 为 0。
    只要 i 小于 做 计数 用 通串设备列表：
        若 通串设备列表 的 第 i 项 的 端口 等于 端口：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

通串_打印设备 接受 dev：
    若 dev 等于 0：
        返还。
    完。
    做 内核信息 用 「  通串设备: 」 加 dev 的 产品名 加 「 [」 加 做 十六进制 用 dev 的 厂商ID 加 「:」 加 做 十六进制 用 dev 的 产品ID 加 「] 驱动=」 加 dev 的 驱动。
完。

可变 通串存储_传输总数 为 0。

可变 通串存储_读总扇区 为 0。

可变 通串存储_写总扇区 为 0。

可变 通串存储_错误数 为 0。

可变 通串存储_设备数 为 0。

通串存储_统计 接受 零参：
    返还 一本字典：传输总数 对应 通串存储_传输总数、读扇区 对应 通串存储_读总扇区、写扇区 对应 通串存储_写总扇区、错误 对应 通串存储_错误数、设备数 对应 通串存储_设备数。
完。

通串存储_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 通串存储_错误数 大于 通串存储_传输总数 除以 10 并且 通串存储_传输总数 大于 100：
        做 追加 用 问题、「通串存储错误率过高」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「通串存储健康」。
    完。
    返还 问题。
完。

通串存储_重置统计 接受 零参：
    通串存储_传输总数 为 0。
    通串存储_读总扇区 为 0。
    通串存储_写总扇区 为 0。
    通串存储_错误数 为 0。
完。

通串存储_完整统计 接受 零参：
    返还 一组：通串存储_传输总数、通串存储_读总扇区、通串存储_写总扇区、通串存储_错误数。
完。

通串存储_读写比 接受 零参：
    若 通串存储_写总扇区 等于 0：
        返还 0。
    完。
    返还 通串存储_读总扇区 乘 100 除以 通串存储_写总扇区。
完。

错误_IO 为 负 5。

错误_无效参数 为 负 22。

通串存储_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 通串存储_错误数 大于 通串存储_传输总数 除以 3 并且 通串存储_传输总数 大于 50：
        做 追加 用 问题、「通串存储_错误数过高」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

通串存储_综合报告 接受 零参：
    做 打印 用 「[通串存储] ═══ 综合报告 ═══」。
    做 打印 用 「  通串存储_传输总数: 」 加 通串存储_传输总数。
    做 打印 用 「  通串存储_错误数: 」 加 通串存储_错误数。
    做 打印 用 「  健康: 」 加 若 做 通串存储_健康评估 则 「正常」 否则 「异常」。
完。

通串存储_全局重置 接受 零参：
    通串存储_传输总数 为 0。
    通串存储_错误数 为 0。
    做 打印 用 「[通串存储] 统计已重置」。
    返还 0。
完。

本章 收束。
