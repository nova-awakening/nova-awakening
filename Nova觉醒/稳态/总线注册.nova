《总线注册》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·中断表注册》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·同步原语》。

引用《..·中枢·整数转字符串》。

驱动_最大数量 为 128。

总线_最大数量 为 16。

驱动状态_未绑定 为 0。

驱动状态_已绑定 为 1。

驱动状态_已暂停 为 2。

驱动状态_错误 为 3。

驱动 是 一种 事物，含 名称、总线、探测、移除、暂停、恢复、关闭、状态、设备列表、设备数、模块、私有数据。

总线类型 是 一种 事物，含 名称、匹配、探测、移除、驱动列表、驱动数、设备列表、设备数、锁。

设备 是 一种 事物，含 名称、类型、总线、驱动、父设备、子设备链表、兄弟链接、总线链接、驱动链接、已初始化、私有数据、引用计数。

可变 全局驱动表 为 做 数组 用 做 指针 用 驱动、128。

可变 全局驱动数 为 0。

可变 全局总线表 为 做 数组 用 做 指针 用 总线类型、16。

可变 全局总线数 为 0。

可变 全局设备根 为 空。

总线_注册 接受 总线：
    若 全局总线数 大于或等于 总线_最大数量：
        返还 错误码_无空间。
    完。
    总线 的 驱动列表 为 空。
    总线 的 驱动数 为 0。
    总线 的 设备列表 为 空。
    总线 的 设备数 为 0。
    总线 的 锁 为 做 互斥锁_创建。
    全局总线表 的 第 全局总线数 项 为 总线。
    全局总线数 为 全局总线数 加 1。
    做 内核信息 用 「总线注册: %s\n」、总线 的 名称。
    返还 0。
完。

总线_按名称查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 全局总线数：
        若 全局总线表 的 第 i 项 不等于 空 并且 全局总线表 的 第 i 项 的 名称 等于 名称：
            返还 全局总线表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

可变 NDM_驱动注册总数 为 0。

可变 NDM_设备注册总数 为 0。

可变 NDM_探测成功数 为 0。

可变 NDM_探测失败数 为 0。

可变 NDM_热插拔事件数 为 0。

驱动_注册 接受 驱动：
    若 驱动 等于 空：
        返还 错误码_无效参数。
    完。
    若 全局驱动数 大于或等于 驱动_最大数量：
        返还 错误码_无空间。
    完。
    可变 i 为 0。
    只要 i 小于 全局驱动数：
        若 全局驱动表 的 第 i 项 不等于 空 并且 全局驱动表 的 第 i 项 的 名称 等于 驱动 的 名称：
            做 内核警告 用 「驱动重复注册: %s\n」、驱动 的 名称。
            返还 错误码_已存在。
        完。
        i 为 i 加 1。
    完。
    驱动 的 状态 为 驱动状态_未绑定。
    驱动 的 设备列表 为 空。
    驱动 的 设备数 为 0。
    全局驱动表 的 第 全局驱动数 项 为 驱动。
    全局驱动数 为 全局驱动数 加 1。
    NDM_驱动注册总数 为 NDM_驱动注册总数 加 1。
    做 内核信息 用 「驱动注册: %s (bus=%s)\n」、驱动 的 名称、若 驱动 的 总线 不等于 空 则 驱动 的 总线 的 名称 否则 「none」。
    若 驱动 的 总线 不等于 空：
        做 总线_尝试匹配驱动 用 驱动 的 总线、驱动。
    完。
    返还 0。
完。

驱动_注销 接受 驱动：
    若 驱动 等于 空：
        返还 错误码_无效参数。
    完。
    若 驱动 的 移除 不等于 空 并且 驱动 的 设备列表 不等于 空：
        可变 dev 为 驱动 的 设备列表。
        只要 dev 不等于 空：
            下一个 为 dev 的 驱动链接。
            做 驱动 的 移除 用 dev。
            dev 的 驱动 为 空。
            dev 为 下一个。
        完。
    完。
    可变 i 为 0。
    只要 i 小于 全局驱动数：
        若 全局驱动表 的 第 i 项 等于 驱动：
            全局驱动表 的 第 i 项 为 全局驱动表 的 第 全局驱动数 减 1 项。
            全局驱动表 的 第 全局驱动数 减 1 项 为 空。
            全局驱动数 为 全局驱动数 减 1。
            做 内核信息 用 「驱动注销: %s\n」、驱动 的 名称。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 错误码_不存在。
完。

设备_注册 接受 设备、总线：
    若 设备 等于 空：
        返还 错误码_无效参数。
    完。
    设备 的 总线 为 总线。
    设备 的 驱动 为 空。
    设备 的 已初始化 为 假。
    设备 的 引用计数 为 1。
    若 总线 不等于 空：
        做 互斥锁_获取 用 总线 的 锁。
        设备 的 总线链接 为 总线 的 设备列表。
        总线 的 设备列表 为 设备。
        总线 的 设备数 为 总线 的 设备数 加 1。
        做 互斥锁_释放 用 总线 的 锁。
    完。
    若 设备 的 父设备 不等于 空：
        设备 的 兄弟链接 为 设备 的 父设备 的 子设备链表。
        设备 的 父设备 的 子设备链表 为 设备。
    完。
    NDM_设备注册总数 为 NDM_设备注册总数 加 1。
    做 内核信息 用 「设备注册: %s (bus=%s)\n」、设备 的 名称、若 总线 不等于 空 则 总线 的 名称 否则 「platform」。
    若 总线 不等于 空：
        做 总线_尝试匹配设备 用 总线、设备。
    完。
    返还 0。
完。

设备_注销 接受 设备：
    若 设备 等于 空：
        返还 错误码_无效参数。
    完。
    若 设备 的 驱动 不等于 空 并且 设备 的 驱动 的 移除 不等于 空：
        做 设备 的 驱动 的 移除 用 设备。
    完。
    若 设备 的 总线 不等于 空：
        做 互斥锁_获取 用 设备 的 总线 的 锁。
        设备 的 总线 的 设备数 为 设备 的 总线 的 设备数 减 1。
        做 总线_从设备链表移除 用 设备 的 总线、设备。
        做 互斥锁_释放 用 设备 的 总线 的 锁。
    完。
    做 内核信息 用 「设备注销: %s\n」、设备 的 名称。
    返还 0。
完。

总线_从设备链表移除 接受 总线、设备：
    若 总线 的 设备列表 等于 设备：
        总线 的 设备列表 为 设备 的 总线链接。
        返还。
    完。
    可变 当前 为 总线 的 设备列表。
    只要 当前 不等于 空：
        若 当前 的 总线链接 等于 设备：
            当前 的 总线链接 为 设备 的 总线链接。
            返还。
        完。
        当前 为 当前 的 总线链接。
    完。
完。

总线_尝试匹配驱动 接受 总线、驱动：
    做 互斥锁_获取 用 总线 的 锁。
    可变 dev 为 总线 的 设备列表。
    只要 dev 不等于 空：
        若 dev 的 驱动 等于 空：
            匹配 为 假。
            若 总线 的 匹配 不等于 空：
                匹配 为 做 总线 的 匹配 用 dev、驱动。
            否则：
                匹配 为 真。
            完。
            若 匹配：
                做 互斥锁_释放 用 总线 的 锁。
                做 设备_绑定驱动 用 dev、驱动。
                做 互斥锁_获取 用 总线 的 锁。
            完。
        完。
        dev 为 dev 的 总线链接。
    完。
    做 互斥锁_释放 用 总线 的 锁。
完。

总线_尝试匹配设备 接受 总线、设备：
    可变 i 为 0。
    只要 i 小于 全局驱动数：
        drv 为 全局驱动表 的 第 i 项。
        若 drv 不等于 空 并且 drv 的 总线 等于 总线：
            匹配 为 假。
            若 总线 的 匹配 不等于 空：
                匹配 为 做 总线 的 匹配 用 设备、drv。
            否则：
                匹配 为 真。
            完。
            若 匹配：
                做 设备_绑定驱动 用 设备、drv。
                返还。
            完。
        完。
        i 为 i 加 1。
    完。
完。

设备_绑定驱动 接受 设备、驱动：
    做 内核信息 用 「尝试探测: %s <-> %s\n」、设备 的 名称、驱动 的 名称。
    ret 为 0。
    若 驱动 的 探测 不等于 空：
        ret 为 做 驱动 的 探测 用 设备。
    否则若 设备 的 总线 不等于 空 并且 设备 的 总线 的 探测 不等于 空：
        ret 为 做 设备 的 总线 的 探测 用 设备。
    完。
    若 ret 等于 0：
        设备 的 驱动 为 驱动。
        设备 的 已初始化 为 真。
        驱动 的 状态 为 驱动状态_已绑定。
        设备 的 驱动链接 为 驱动 的 设备列表。
        驱动 的 设备列表 为 设备。
        驱动 的 设备数 为 驱动 的 设备数 加 1。
        NDM_探测成功数 为 NDM_探测成功数 加 1。
        做 内核信息 用 「探测成功: %s <-> %s\n」、设备 的 名称、驱动 的 名称。
    否则：
        NDM_探测失败数 为 NDM_探测失败数 加 1。
        做 内核警告 用 「探测失败: %s <-> %s (ret=%d)\n」、设备 的 名称、驱动 的 名称、ret。
    完。
完。

热插拔_插入 为 0。

热插拔_拔出 为 1。

热插拔事件 是 一种 事物，含 类型、设备名、总线名、时间戳。

可变 热插拔队列 为 一组：（空）。

热插拔_最大队列 为 64。

热插拔_通知 接受 类型、设备名、总线名：
    NDM_热插拔事件数 为 NDM_热插拔事件数 加 1。
    evt 为 新建 热插拔事件。
    evt 的 类型 为 类型。
    evt 的 设备名 为 设备名。
    evt 的 总线名 为 总线名。
    evt 的 时间戳 为 做 获取时间戳。
    若 做 计数 用 热插拔队列 小于 热插拔_最大队列：
        做 追加 用 热插拔队列、evt。
    完。
    若 类型 等于 热插拔_插入：
        做 内核信息 用 「热插拔: 插入 %s (bus=%s)\n」、设备名、总线名。
    否则：
        做 内核信息 用 「热插拔: 拔出 %s (bus=%s)\n」、设备名、总线名。
    完。
完。

驱动_暂停 接受 设备：
    若 设备 等于 空 或者 设备 的 驱动 等于 空：
        返还 错误码_无效参数。
    完。
    若 设备 的 驱动 的 暂停 不等于 空：
        ret 为 做 设备 的 驱动 的 暂停 用 设备。
        若 ret 等于 0：
            设备 的 驱动 的 状态 为 驱动状态_已暂停。
            做 内核信息 用 「驱动暂停: %s\n」、设备 的 名称。
        完。
        返还 ret。
    完。
    返还 0。
完。

驱动_恢复 接受 设备：
    若 设备 等于 空 或者 设备 的 驱动 等于 空：
        返还 错误码_无效参数。
    完。
    若 设备 的 驱动 的 恢复 不等于 空：
        ret 为 做 设备 的 驱动 的 恢复 用 设备。
        若 ret 等于 0：
            设备 的 驱动 的 状态 为 驱动状态_已绑定。
            做 内核信息 用 「驱动恢复: %s\n」、设备 的 名称。
        完。
        返还 ret。
    完。
    返还 0。
完。

驱动_关闭 接受 设备：
    若 设备 等于 空 或者 设备 的 驱动 等于 空：
        返还 错误码_无效参数。
    完。
    若 设备 的 驱动 的 关闭 不等于 空：
        做 设备 的 驱动 的 关闭 用 设备。
    完。
    做 内核信息 用 「驱动关闭: %s\n」、设备 的 名称。
    返还 0。
完。

外部。

全局AI总线。

NDM_AI初始化 接受 零参：
    若 全局AI总线 等于 空：
        返还。
    完。
    做 内核信息 用 「[NDM] AI驱动匹配引擎已加载」。
完。

NDM_AI驱动推荐 接受 设备：
    若 全局AI总线 等于 空 或者 设备 等于 空：
        返还 空。
    完。
    t 为 做 Tensor_创建 用 6。
    做 Tensor_写 用 t、0、设备 的 类型。
    做 Tensor_写 用 t、1、若 设备 的 总线 不等于 空 则 1 否则 0。
    做 Tensor_写 用 t、2、全局驱动数。
    做 Tensor_写 用 t、3、NDM_设备注册总数。
    做 Tensor_写 用 t、4、NDM_探测成功数。
    做 Tensor_写 用 t、5、NDM_热插拔事件数。
    推荐分 为 做 AI总线_推理 用 全局AI总线、「driver_match」、t。
    做 Tensor_释放 用 t。
    可变 最佳 为 空。
    可变 最佳分 为 0。
    可变 i 为 0。
    只要 i 小于 全局驱动数：
        drv 为 全局驱动表 的 第 i 项。
        若 drv 不等于 空 并且 drv 的 总线 等于 设备 的 总线：
            td 为 做 Tensor_创建 用 4。
            做 Tensor_写 用 td、0、设备 的 类型。
            做 Tensor_写 用 td、1、drv 的 设备数。
            做 Tensor_写 用 td、2、drv 的 状态。
            做 Tensor_写 用 td、3、推荐分。
            s 为 做 AI总线_推理 用 全局AI总线、「driver_score」、td。
            做 Tensor_释放 用 td。
            若 s 大于 最佳分：
                最佳分 为 s。
                最佳 为 drv。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最佳 不等于 空：
        做 内核信息 用 「[NDM] AI推荐驱动: 」 加 最佳 的 名称 加 「 分数=」 加 做 整数转字符串 用 最佳分。
    完。
    返还 最佳。
完。

NDM_AI健康检测 接受 零参：
    若 全局AI总线 等于 空：
        返还 0。
    完。
    t 为 做 Tensor_创建 用 5。
    做 Tensor_写 用 t、0、全局驱动数。
    做 Tensor_写 用 t、1、NDM_设备注册总数。
    做 Tensor_写 用 t、2、NDM_探测成功数。
    做 Tensor_写 用 t、3、NDM_探测失败数。
    做 Tensor_写 用 t、4、NDM_热插拔事件数。
    健康分 为 做 AI总线_异常检测 用 全局AI总线、「driver_health」、t。
    做 Tensor_释放 用 t。
    若 健康分 大于 700：
        做 内核警告 用 「[NDM] AI检测驱动子系统异常 分数=」 加 做 整数转字符串 用 健康分。
    完。
    返还 健康分。
完。

NDM_统计 接受 零参：
    做 内核打印 用 「=== NDM驱动模型统计 ===」。
    做 内核打印 用 「注册总线: 」 加 做 整数转字符串 用 全局总线数。
    做 内核打印 用 「注册驱动: 」 加 做 整数转字符串 用 全局驱动数。
    做 内核打印 用 「设备总数: 」 加 做 整数转字符串 用 NDM_设备注册总数。
    做 内核打印 用 「探测成功: 」 加 做 整数转字符串 用 NDM_探测成功数。
    做 内核打印 用 「探测失败: 」 加 做 整数转字符串 用 NDM_探测失败数。
    做 内核打印 用 「热插拔事件: 」 加 做 整数转字符串 用 NDM_热插拔事件数。
完。

NDM_列举总线 接受 零参：
    可变 i 为 0。
    只要 i 小于 全局总线数：
        bus 为 全局总线表 的 第 i 项。
        若 bus 不等于 空：
            做 内核打印 用 「总线[」 加 做 整数转字符串 用 i 加 「]: 」 加 bus 的 名称 加 「 驱动=」 加 做 整数转字符串 用 bus 的 驱动数 加 「 设备=」 加 做 整数转字符串 用 bus 的 设备数。
        完。
        i 为 i 加 1。
    完。
完。

NDM_列举驱动 接受 零参：
    可变 i 为 0。
    只要 i 小于 全局驱动数：
        drv 为 全局驱动表 的 第 i 项。
        若 drv 不等于 空：
            总线名 为 若 drv 的 总线 不等于 空 则 drv 的 总线 的 名称 否则 「none」。
            做 内核打印 用 「驱动[」 加 做 整数转字符串 用 i 加 「]: 」 加 drv 的 名称 加 「 bus=」 加 总线名 加 「 状态=」 加 做 NDM_状态名称 用 drv 的 状态 加 「 设备数=」 加 做 整数转字符串 用 drv 的 设备数。
        完。
        i 为 i 加 1。
    完。
完。

NDM_状态名称 接受 状态：
    若 状态 等于 驱动状态_未绑定：
        返还 「未绑定」。
    完。
    若 状态 等于 驱动状态_已绑定：
        返还 「已绑定」。
    完。
    若 状态 等于 驱动状态_已暂停：
        返还 「已暂停」。
    完。
    若 状态 等于 驱动状态_错误：
        返还 「错误」。
    完。
    返还 「未知」。
完。

设备_获取 接受 设备：
    若 设备 不等于 空：
        设备 的 引用计数 为 设备 的 引用计数 加 1。
    完。
    返还 设备。
完。

设备_释放 接受 设备：
    若 设备 等于 空：
        返还。
    完。
    设备 的 引用计数 为 设备 的 引用计数 减 1。
    若 设备 的 引用计数 小于或等于 0：
        做 内核信息 用 「设备释放: %s 引用=0\n」、设备 的 名称。
    完。
完。

NDM_初始化 接受 零参：
    全局驱动数 为 0。
    全局总线数 为 0。
    全局设备根 为 空。
    NDM_驱动注册总数 为 0。
    NDM_设备注册总数 为 0。
    NDM_探测成功数 为 0。
    NDM_探测失败数 为 0。
    NDM_热插拔事件数 为 0。
    热插拔队列 为 一组：（空）。
    做 NDM_AI初始化。
    做 内核信息 用 「[NDM] Nova驱动模型 v1.0 初始化完成」。
    返还 0。
完。

NDM_驱动按名称查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 全局驱动数：
        若 全局驱动表 的 第 i 项 不等于 空 并且 全局驱动表 的 第 i 项 的 名称 等于 名称：
            返还 全局驱动表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

本章 收束。
