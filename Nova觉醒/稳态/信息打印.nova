《信息打印》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《.·GPU命令队列驱动》。

引用《..·中枢·字符串替换》。

引用《.·USB控制传输》。

引用《..·中枢·内核打印》。

引用《.·中断处理》。

引用《..·中枢·整数转字符串》。

通串_打印设备 接受 dev：
    速度名 为 「?」。
    若 dev 的 速度 等于 通串_SPEED_LOW：
        速度名 为 「1.5Mbps」。
    完。
    若 dev 的 速度 等于 通串_SPEED_FULL：
        速度名 为 「12Mbps」。
    完。
    若 dev 的 速度 等于 通串_SPEED_HIGH：
        速度名 为 「480Mbps」。
    完。
    若 dev 的 速度 等于 通串_SPEED_SUPER：
        速度名 为 「5Gbps」。
    完。
    做 内核信息 用 「通串: [」 加 做 整数转字符串 用 dev 的 地址 加 「] 」 加 dev 的 产品名 加 「 (」 加 dev 的 厂商名 加 「)」 加 「 VID:PID=」 加 做 整数转十六进制 用 dev 的 厂商ID 加 「:」 加 做 整数转十六进制 用 dev 的 产品ID 加 「 类=」 加 做 整数转十六进制 用 dev 的 类代码 加 「 速度=」 加 速度名 加 「 驱动=」 加 dev 的 驱动。
完。

通串_统计 接受 零参：
    返还 一组：做 计数 用 通串控制器列表、做 计数 用 通串设备列表。
完。

通串_列出设备 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 通串设备列表：
        做 通串_打印设备 用 通串设备列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

通串_端口有设备 接受 ctl、端口：
    若 ctl 等于 空：
        返还 假。
    完。
    PORTSC基址 为 ctl 加 0x400 加 端口 乘 0x10。
    状态 为 做 MMIO_读32 用 PORTSC基址。
    返还 状态 位与 1 不等于 0。
完。

通串_端口已枚举 接受 ctl、端口：
    可变 i 为 0。
    只要 i 小于 做 计数 用 通串设备列表：
        若 通串设备列表 的 第 i 项 的 控制器 等于 ctl 并且 通串设备列表 的 第 i 项 的 端口 等于 端口：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

通串_端口复位 接受 ctl、端口：
    PORTSC基址 为 ctl 加 0x400 加 端口 乘 0x10。
    状态 为 做 MMIO_读32 用 PORTSC基址。
    做 MMIO_写32 用 PORTSC基址、状态 位或 1 左移 4。
    做 延迟_ms 用 50。
    做 内核信息 用 「通串: 端口」 加 做 整数转字符串 用 端口 加 「 复位完成」。
完。

通串_获取设备描述符 接受 ctl、地址：
    描述符 为 做 通串_控制传输 用 ctl、地址、0x80、6、0x0100、0、18。
    若 描述符 等于 空 或者 做 计数 用 描述符 小于 18：
        返还 空。
    完。
    返还 一组：描述符 的 第 8 项 位或 描述符 的 第 9 项 左移 8、描述符 的 第 10 项 位或 描述符 的 第 11 项 左移 8、描述符 的 第 4 项、描述符 的 第 14 项、描述符 的 第 15 项、描述符 的 第 7 项。
完。

通串_设置地址 接受 ctl、旧地址、新地址：
    做 通串_控制传输 用 ctl、旧地址、0x00、5、新地址、0、0。
    做 延迟_ms 用 2。
    做 内核调试 用 「通串: 设置地址 」 加 做 整数转字符串 用 旧地址 加 「 → 」 加 做 整数转字符串 用 新地址。
完。

通串_设置配置 接受 ctl、地址、配置号：
    做 通串_控制传输 用 ctl、地址、0x00、9、配置号、0、0。
    做 内核调试 用 「通串: 设置配置 #」 加 做 整数转字符串 用 配置号 加 「 地址=」 加 做 整数转字符串 用 地址。
完。

通串_获取字符串 接受 ctl、地址、索引：
    若 索引 等于 0：
        返还 「」。
    完。
    数据 为 做 通串_控制传输 用 ctl、地址、0x80、6、0x0300 位或 索引、0x0409、255。
    若 数据 等于 空 或者 做 计数 用 数据 小于 2：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 i 为 2。
    只要 i 小于 做 计数 用 数据 减 1：
        结果 为 结果 加 做 字符 用 数据 的 第 i 项。
        i 为 i 加 2。
    完。
    返还 结果。
完。

devtmpfs_创建_设备 接受 路径：
    做 内核信息 用 「通串: 创建设备节点 」 加 路径。
完。

本章 收束。
