《原生元素树》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·中枢·块内存分配器》。

引用《..·中枢·同步原语》。

元素类型_容器 为 1。

元素类型_文本 为 2。

元素类型_根 为 3。

控件_容器 为 0。

控件_文本标签 为 1。

控件_按钮 为 2。

控件_输入框 为 3。

控件_图片 为 4。

控件_列表 为 5。

控件_列表项 为 6。

控件_导航栏 为 7。

控件_区域 为 8。

控件_表格 为 9。

控件_表行 为 10。

控件_表格 为 11。

控件_表单 为 12。

控件_多行输入 为 13。

控件_下拉选择 为 14。

控件_标题 为 15。

控件_头部 为 16。

控件_底部 为 17。

元素属性 是 一种 事物，含 键哈希、值哈希、值字符串。

原生元素 是 一种 事物，含 元素ID、类型、控件类型、文本内容、属性列表、子元素、父元素ID、布局_x、布局_y、布局_宽、布局_高、样式、可见、可交互、语义角色。

可变 元素_根节点 为 空。

可变 元素_下一个ID 为 1。

可变 元素_总数 为 0。

可变 元素树_锁 为 0。

元素_创建 接受 控件类型：
    元素 为 做 kmalloc 用 原生元素。
    元素 的 元素ID 为 元素_下一个ID。
    元素_下一个ID 为 元素_下一个ID 加 1。
    元素_总数 为 元素_总数 加 1。
    元素 的 类型 为 元素类型_容器。
    元素 的 控件类型 为 控件类型。
    元素 的 属性列表 为 一组：（空）。
    元素 的 子元素 为 一组：（空）。
    元素 的 可见 为 真。
    元素 的 可交互 为 控件类型 等于 控件_按钮 或者 控件类型 等于 控件_输入框 或者 控件类型 等于 控件_多行输入 或者 控件类型 等于 控件_下拉选择。
    元素 的 语义角色 为 做 元素_推断角色 用 控件类型。
    返还 元素。
完。

元素_创建文本 接受 内容：
    元素 为 做 kmalloc 用 原生元素。
    元素 的 元素ID 为 元素_下一个ID。
    元素_下一个ID 为 元素_下一个ID 加 1。
    元素_总数 为 元素_总数 加 1。
    元素 的 类型 为 元素类型_文本。
    元素 的 文本内容 为 内容。
    元素 的 子元素 为 一组：（空）。
    元素 的 属性列表 为 一组：（空）。
    元素 的 可见 为 真。
    元素 的 可交互 为 假。
    元素 的 语义角色 为 「text」。
    返还 元素。
完。

元素_添加子节点 接受 父、子：
    若 父 等于 空 或者 子 等于 空：
        返还。
    完。
    做 自旋锁_获取 用 元素树_锁。
    子 的 父元素ID 为 父 的 元素ID。
    做 追加 用 父 的 子元素、子。
    做 自旋锁_释放 用 元素树_锁。
完。

元素_设属性 接受 元素、键哈希、值：
    若 元素 等于 空：
        返还。
    完。
    attr 为 新建 元素属性。
    attr 的 键哈希 为 键哈希。
    attr 的 值字符串 为 值。
    做 追加 用 元素 的 属性列表、attr。
完。

元素_获取属性 接受 元素、键哈希：
    若 元素 等于 空：
        返还 「」。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 元素 的 属性列表：
        若 元素 的 属性列表 的 第 i 项 的 键哈希 等于 键哈希：
            返还 元素 的 属性列表 的 第 i 项 的 值字符串。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

元素_推断角色 接受 控件类型：
    若 控件类型 等于 控件_按钮：
        返还 「button」。
    完。
    若 控件类型 等于 控件_输入框：
        返还 「textbox」。
    完。
    若 控件类型 等于 控件_多行输入：
        返还 「textbox」。
    完。
    若 控件类型 等于 控件_下拉选择：
        返还 「combobox」。
    完。
    若 控件类型 等于 控件_图片：
        返还 「img」。
    完。
    若 控件类型 等于 控件_导航栏：
        返还 「navigation」。
    完。
    若 控件类型 等于 控件_标题：
        返还 「heading」。
    完。
    若 控件类型 等于 控件_表格：
        返还 「table」。
    完。
    若 控件类型 等于 控件_表单：
        返还 「form」。
    完。
    若 控件类型 等于 控件_列表：
        返还 「list」。
    完。
    若 控件类型 等于 控件_列表项：
        返还 「listitem」。
    完。
    返还 「generic」。
完。

元素_释放 接受 元素：
    若 元素 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 元素 的 子元素：
        做 元素_释放 用 元素 的 子元素 的 第 i 项。
        i 为 i 加 1。
    完。
    做 kfree 用 元素。
    元素_总数 为 元素_总数 减 1。
完。

本章 收束。
