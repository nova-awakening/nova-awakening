《ACPI电源管理》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·中断表注册》。

引用《..·推理·通信渠道》。

引用《..·推理·定点Q算术》。

引用《..·引导·端口IO》。

引用《..·中枢·整数转字符串》。

引用《.·中断处理》。

引用《..·中枢·字符串替换》。

ACPI_S0 为 0。

ACPI_S1 为 1。

ACPI_S2 为 2。

ACPI_S3 为 3。

ACPI_S4 为 4。

ACPI_S5 为 5。

ACPI_D0 为 0。

ACPI_D1 为 1。

ACPI_D2 为 2。

ACPI_D3_HOT 为 3。

ACPI_D3_COLD 为 4。

ACPI_C0 为 0。

ACPI_C1 为 1。

ACPI_C2 为 2。

ACPI_C3 为 3。

ACPI_FADT_WBINVD 为 0x0001。

ACPI_FADT_PROC_C1 为 0x0004。

ACPI_FADT_SLP_BUTTON 为 0x0020。

ACPI_FADT_RESET_REG 为 0x0400。

ACPI_FADT_HW_REDUCED 为 0x1000。

ACPI_FADT_LOW_POWER_S0 为 0x2000。

ACPI_SIG_FADT 为 0x50434146。

ACPI_SIG_HPET 为 0x54455048。

ACPI_SIG_MCFG 为 0x4746434D。

ACPI_SIG_MADT 为 0x43495041。

ACPI_SIG_BGRT 为 0x54524742。

NPTM_最大电源域 为 32。

NPTM_最大设备每域 为 64。

可变 NPTM_解析总数 为 0。

可变 NPTM_状态切换总数 为 0。

可变 NPTM_AI预测总数 为 0。

可变 NPTM_省电总毫瓦时 为 0。

可变 NPTM_当前S状态 为 0。

可变 NPTM_PM定时器满溢 为 0。

可变 ACPI_FADT 为 空。

ACPI_FADT信息 是 一种 事物，含 SCI中断 为 0、SMI命令端口 为 0、ACPI使能值 为 0、ACPI禁用值 为 0、PM1a事件块 为 0、PM1b事件块 为 0、PM1a控制块 为 0、PM1b控制块 为 0、PM定时器块 为 0、PM定时器长度 为 0、世纪寄存器 为 0、标志 为 0、DSDT地址 为 0、复位寄存器 为 0、复位值 为 0。

ACPI_解析FADT 接受 零参：
    fadt 为 做 ACPI_查找表 用 ACPI_SIG_FADT。
    若 fadt 等于 0：
        返还。
    完。
    info 为 新建 ACPI_FADT信息。
    info 的 SCI中断 为 做 内存读16 用 fadt 加 46。
    info 的 SMI命令端口 为 做 内存读32 用 fadt 加 48。
    info 的 ACPI使能值 为 做 内存读8 用 fadt 加 52。
    info 的 ACPI禁用值 为 做 内存读8 用 fadt 加 53。
    info 的 PM1a事件块 为 做 内存读32 用 fadt 加 56。
    info 的 PM1b事件块 为 做 内存读32 用 fadt 加 60。
    info 的 PM1a控制块 为 做 内存读32 用 fadt 加 64。
    info 的 PM1b控制块 为 做 内存读32 用 fadt 加 68。
    info 的 PM定时器块 为 做 内存读32 用 fadt 加 76。
    info 的 PM定时器长度 为 做 内存读8 用 fadt 加 91。
    info 的 世纪寄存器 为 做 内存读8 用 fadt 加 108。
    info 的 标志 为 做 内存读32 用 fadt 加 112。
    长度 为 做 内存读32 用 fadt 加 4。
    若 长度 大于或等于 148：
        info 的 DSDT地址 为 做 内存读64 用 fadt 加 140。
    完。
    若 info 的 DSDT地址 等于 0：
        info 的 DSDT地址 为 做 内存读32 用 fadt 加 40。
    完。
    若 长度 大于或等于 129 并且 info 的 标志 位与 ACPI_FADT_RESET_REG 不等于 0：
        info 的 复位寄存器 为 做 内存读64 用 fadt 加 116。
        info 的 复位值 为 做 内存读8 用 fadt 加 128。
    完。
    ACPI_FADT 为 info。
    做 内核信息 用 「ACPI: FADT SCI=」 加 做 整数转字符串 用 info 的 SCI中断 加 「 PM1a=」 加 做 整数转十六进制 用 info 的 PM1a控制块。
完。

ACPI_解析HPET 接受 零参：
    hpet 为 做 ACPI_查找表 用 ACPI_SIG_HPET。
    若 hpet 等于 0：
        返还。
    完。
    地址 为 做 内存读64 用 hpet 加 44。
    做 内核信息 用 「ACPI: HPET @ 」 加 做 整数转十六进制 用 地址。
完。

ACPI_解析MCFG 接受 零参：
    mcfg 为 做 ACPI_查找表 用 ACPI_SIG_MCFG。
    若 mcfg 等于 0：
        返还。
    完。
    基地址 为 做 内存读64 用 mcfg 加 44。
    起始总线 为 做 内存读8 用 mcfg 加 54。
    结束总线 为 做 内存读8 用 mcfg 加 55。
    做 内核信息 用 「ACPI: PCIe MCFG @ 」 加 做 整数转十六进制 用 基地址 加 「 总线」 加 做 整数转字符串 用 起始总线 加 「-」 加 做 整数转字符串 用 结束总线。
完。

ACPI_关机 接受 零参：
    若 ACPI_FADT 等于 空：
        做 内核警告 用 「ACPI: 无法关机(未初始化)」。
        返还。
    完。
    做 内核信息 用 「ACPI: 系统关机...」。
    slp_typ 为 5 左移 10。
    slp_en 为 1 左移 13。
    做 端口写16 用 ACPI_FADT 的 PM1a控制块、slp_typ 位或 slp_en。
    若 ACPI_FADT 的 PM1b控制块 不等于 0：
        做 端口写16 用 ACPI_FADT 的 PM1b控制块、slp_typ 位或 slp_en。
    完。
完。

ACPI_重启 接受 零参：
    若 ACPI_FADT 不等于 空 并且 ACPI_FADT 的 复位寄存器 不等于 0：
        做 内核信息 用 「ACPI: 系统重启...」。
        做 内存写8 用 ACPI_FADT 的 复位寄存器、ACPI_FADT 的 复位值。
        做 延迟_ms 用 100。
    完。
    做 端口写8 用 0x64、0xFE。
完。

ACPI_挂起到RAM 接受 零参：
    若 ACPI_FADT 等于 空：
        返还。
    完。
    NPTM_状态切换总数 为 NPTM_状态切换总数 加 1。
    做 内核信息 用 「ACPI: 挂起到RAM(S3)...」。
    做 NPTM_保存设备电源状态。
    slp_typ 为 3 左移 10。
    slp_en 为 1 左移 13。
    做 端口写16 用 ACPI_FADT 的 PM1a控制块、slp_typ 位或 slp_en。
    NPTM_当前S状态 为 ACPI_S3。
完。

ACPI_休眠到磁盘 接受 零参：
    若 ACPI_FADT 等于 空：
        返还。
    完。
    NPTM_状态切换总数 为 NPTM_状态切换总数 加 1。
    做 内核信息 用 「ACPI: 休眠到磁盘(S4)...」。
    做 NPTM_保存设备电源状态。
    slp_typ 为 4 左移 10。
    slp_en 为 1 左移 13。
    做 端口写16 用 ACPI_FADT 的 PM1a控制块、slp_typ 位或 slp_en。
    若 ACPI_FADT 的 PM1b控制块 不等于 0：
        做 端口写16 用 ACPI_FADT 的 PM1b控制块、slp_typ 位或 slp_en。
    完。
    NPTM_当前S状态 为 ACPI_S4。
完。

NPTM_读取PM定时器 接受 零参：
    若 ACPI_FADT 等于 空 或者 ACPI_FADT 的 PM定时器块 等于 0：
        返还 0。
    完。
    返还 做 端口读32 用 ACPI_FADT 的 PM定时器块。
完。

NPTM_微秒延迟 接受 微秒数：
    若 ACPI_FADT 等于 空 或者 ACPI_FADT 的 PM定时器块 等于 0：
        可变 n 为 微秒数 乘 10。
        只要 n 大于 0：
            n 为 n 减 1。
        完。
        返还。
    完。
    目标计数 为 微秒数 乘 358 除以 100。
    起始 为 做 NPTM_读取PM定时器。
    可变 已过 为 0。
    只要 已过 小于 目标计数：
        当前 为 做 NPTM_读取PM定时器。
        若 当前 大于或等于 起始：
            已过 为 当前 减 起始。
        否则：
            已过 为 0xFFFFFF 减 起始 加 当前。
            NPTM_PM定时器满溢 为 NPTM_PM定时器满溢 加 1。
        完。
    完。
完。

NPTM_电源域 是 一种 事物，含 域名 为 0、域ID 为 0、父域ID 为 0、当前D状态 为 0、目标D状态 为 0、设备数 为 0、设备列表 为 一组：（空）、功耗毫瓦 为 0、空闲计时 为 0、最大功耗毫瓦 为 0、空闲阈值毫秒 为 5000。

可变 NPTM_电源域表 为 一组：（空）。

可变 NPTM_电源域数 为 0。

NPTM_注册电源域 接受 域名、父域ID、最大功耗：
    若 NPTM_电源域数 大于或等于 NPTM_最大电源域：
        返还 负 1。
    完。
    域 为 新建 NPTM_电源域：域名 为 域名、域ID 为 NPTM_电源域数、父域ID 为 父域ID、当前D状态 为 ACPI_D0、目标D状态 为 ACPI_D0、设备数 为 0、设备列表 为 一组：（空）、功耗毫瓦 为 0、空闲计时 为 0、最大功耗毫瓦 为 最大功耗、空闲阈值毫秒 为 5000。
    NPTM_电源域表 的 第 NPTM_电源域数 项 为 域。
    NPTM_电源域数 为 NPTM_电源域数 加 1。
    返还 域 的 域ID。
完。

NPTM_域添加设备 接受 域ID、设备指针：
    若 域ID 大于或等于 NPTM_电源域数：
        返还 假。
    完。
    域 为 NPTM_电源域表 的 第 域ID 项。
    若 域 的 设备数 大于或等于 NPTM_最大设备每域：
        返还 假。
    完。
    域 的 设备列表 的 第 域 的 设备数 项 为 设备指针。
    域 的 设备数 为 域 的 设备数 加 1。
    返还 真。
完。

NPTM_设置域电源状态 接受 域ID、目标D状态：
    若 域ID 大于或等于 NPTM_电源域数：
        返还。
    完。
    域 为 NPTM_电源域表 的 第 域ID 项。
    若 域 的 当前D状态 等于 目标D状态：
        返还。
    完。
    NPTM_状态切换总数 为 NPTM_状态切换总数 加 1。
    域 的 目标D状态 为 目标D状态。
    做 内核信息 用 「NPTM: 域[」 加 域 的 域名 加 「] D」 加 域 的 当前D状态 加 「 → D」 加 目标D状态。
    可变 i 为 0。
    只要 i 小于 域 的 设备数：
        设备 为 域 的 设备列表 的 第 i 项。
        若 设备 不等于 0：
            若 目标D状态 大于或等于 ACPI_D3_HOT：
                做 NPTM_设备保存状态 用 设备。
            否则若 域 的 当前D状态 大于或等于 ACPI_D3_HOT 并且 目标D状态 等于 ACPI_D0：
                做 NPTM_设备恢复状态 用 设备。
            完。
        完。
        i 为 i 加 1。
    完。
    域 的 当前D状态 为 目标D状态。
    域 的 目标D状态 为 目标D状态。
    若 目标D状态 等于 ACPI_D0：
        域 的 空闲计时 为 0。
    完。
    做 NPTM_更新域功耗 用 域ID。
完。

NPTM_设备保存状态 接受 设备：
    若 设备 不等于 0 并且 设备 的 挂起回调 不等于 0：
        做 设备 的 挂起回调 用 设备。
    完。
完。

NPTM_设备恢复状态 接受 设备：
    若 设备 不等于 0 并且 设备 的 恢复回调 不等于 0：
        做 设备 的 恢复回调 用 设备。
    完。
完。

NPTM_更新域功耗 接受 域ID：
    若 域ID 大于或等于 NPTM_电源域数：
        返还。
    完。
    域 为 NPTM_电源域表 的 第 域ID 项。
    可变 系数 为 100。
    若 域 的 当前D状态 等于 ACPI_D1：
        系数 为 30。
    否则若 域 的 当前D状态 等于 ACPI_D2：
        系数 为 10。
    否则若 域 的 当前D状态 大于或等于 ACPI_D3_HOT：
        系数 为 1。
    完。
    域 的 功耗毫瓦 为 域 的 最大功耗毫瓦 乘 系数 除以 100。
完。

NPTM_保存设备电源状态 接受 零参：
    可变 i 为 0。
    只要 i 小于 NPTM_电源域数：
        域 为 NPTM_电源域表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 域 的 设备数：
            做 NPTM_设备保存状态 用 域 的 设备列表 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

NPTM_恢复设备电源状态 接受 零参：
    可变 i 为 0。
    只要 i 小于 NPTM_电源域数：
        域 为 NPTM_电源域表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 域 的 设备数：
            做 NPTM_设备恢复状态 用 域 的 设备列表 的 第 j 项。
            j 为 j 加 1。
        完。
        域 的 当前D状态 为 ACPI_D0。
        做 NPTM_更新域功耗 用 i。
        i 为 i 加 1。
    完。
    NPTM_当前S状态 为 ACPI_S0。
完。

NPTM_空闲检测周期 接受 经过毫秒：
    可变 i 为 0。
    只要 i 小于 NPTM_电源域数：
        域 为 NPTM_电源域表 的 第 i 项。
        若 域 的 当前D状态 等于 ACPI_D0：
            域 的 空闲计时 为 域 的 空闲计时 加 经过毫秒。
            若 域 的 空闲计时 大于或等于 域 的 空闲阈值毫秒：
                可变 目标 为 ACPI_D1。
                若 域 的 空闲计时 大于 域 的 空闲阈值毫秒 乘 3：
                    目标 为 ACPI_D2。
                完。
                若 域 的 空闲计时 大于 域 的 空闲阈值毫秒 乘 10：
                    目标 为 ACPI_D3_HOT。
                完。
                做 NPTM_设置域电源状态 用 i、目标。
            完。
        完。
        i 为 i 加 1。
    完。
完。

NPTM_标记域活跃 接受 域ID：
    若 域ID 大于或等于 NPTM_电源域数：
        返还。
    完。
    域 为 NPTM_电源域表 的 第 域ID 项。
    域 的 空闲计时 为 0。
    若 域 的 当前D状态 不等于 ACPI_D0：
        做 NPTM_设置域电源状态 用 域ID、ACPI_D0。
    完。
完。

NPTM_计算总功耗 接受 零参：
    可变 总功耗 为 0。
    可变 i 为 0。
    只要 i 小于 NPTM_电源域数：
        总功耗 为 总功耗 加 NPTM_电源域表 的 第 i 项 的 功耗毫瓦。
        i 为 i 加 1。
    完。
    返还 总功耗。
完。

NPTM_AI_电源优化 接受 零参：
    NPTM_AI预测总数 为 NPTM_AI预测总数 加 1。
    特征大小 为 3 加 NPTM_电源域数 乘 2。
    tensor 为 做 Tensor_创建 用 特征大小。
    做 Tensor_写 用 tensor、0、NPTM_电源域数。
    做 Tensor_写 用 tensor、1、做 NPTM_计算总功耗。
    做 Tensor_写 用 tensor、2、NPTM_当前S状态。
    可变 i 为 0。
    只要 i 小于 NPTM_电源域数：
        做 Tensor_写 用 tensor、3 加 i、NPTM_电源域表 的 第 i 项 的 空闲计时。
        做 Tensor_写 用 tensor、3 加 NPTM_电源域数 加 i、NPTM_电源域表 的 第 i 项 的 当前D状态。
        i 为 i 加 1。
    完。
    结果 为 做 AI总线_推理 用 全局AI总线、「NPTM_电源优化」、tensor、特征大小。
    若 结果 不等于 0：
        可变 j 为 0。
        只要 j 小于 NPTM_电源域数：
            建议D 为 做 Tensor_读 用 结果、j。
            若 建议D 大于或等于 ACPI_D0 并且 建议D 小于或等于 ACPI_D3_COLD：
                若 建议D 不等于 NPTM_电源域表 的 第 j 项 的 当前D状态：
                    做 NPTM_设置域电源状态 用 j、建议D。
                完。
            完。
            j 为 j 加 1。
        完。
        做 Tensor_释放 用 结果。
    完。
    做 Tensor_释放 用 tensor。
完。

NPTM_SCI中断处理 接受 零参：
    若 ACPI_FADT 等于 空：
        返还。
    完。
    事件 为 做 端口读16 用 ACPI_FADT 的 PM1a事件块。
    若 事件 位与 0x0100：
        做 内核信息 用 「NPTM: 电源按键事件」。
        做 端口写16 用 ACPI_FADT 的 PM1a事件块、0x0100。
        做 ACPI_关机。
    完。
    若 事件 位与 0x0001：
        做 端口写16 用 ACPI_FADT 的 PM1a事件块、0x0001。
        NPTM_PM定时器满溢 为 NPTM_PM定时器满溢 加 1。
    完。
    若 事件 位与 0x0200：
        做 内核信息 用 「NPTM: 睡眠按键事件」。
        做 端口写16 用 ACPI_FADT 的 PM1a事件块、0x0200。
    完。
完。

NPTM_诊断打印 接受 零参：
    做 内核信息 用 「=== NPTM 电源管理诊断报告 ===」。
    做 内核信息 用 「  当前S状态:     S」 加 NPTM_当前S状态。
    做 内核信息 用 「  电源域数:     」 加 NPTM_电源域数。
    做 内核信息 用 「  解析总数:     」 加 NPTM_解析总数。
    做 内核信息 用 「  状态切换总数: 」 加 NPTM_状态切换总数。
    做 内核信息 用 「  AI预测总数:   」 加 NPTM_AI预测总数。
    做 内核信息 用 「  总功耗(mW):    」 加 做 NPTM_计算总功耗。
    做 内核信息 用 「  PM定时器溢出: 」 加 NPTM_PM定时器满溢。
    可变 i 为 0。
    只要 i 小于 NPTM_电源域数：
        域 为 NPTM_电源域表 的 第 i 项。
        做 内核信息 用 「  域[」 加 域 的 域名 加 「] D」 加 域 的 当前D状态 加 「 | 」 加 域 的 功耗毫瓦 加 「mW | 设备」 加 域 的 设备数 加 「 | 空闲」 加 域 的 空闲计时 加 「ms」。
        i 为 i 加 1。
    完。
    做 内核信息 用 「=========================」。
完。

NPTM_使能ACPI 接受 零参：
    若 ACPI_FADT 等于 空：
        返还 假。
    完。
    若 ACPI_FADT 的 SMI命令端口 等于 0：
        做 内核信息 用 「NPTM: HW-Reduced ACPI，跳过SMI使能」。
        返还 真。
    完。
    pm1a 为 做 端口读16 用 ACPI_FADT 的 PM1a控制块。
    若 pm1a 位与 0x0001：
        做 内核信息 用 「NPTM: 已在ACPI模式」。
        返还 真。
    完。
    做 端口写8 用 ACPI_FADT 的 SMI命令端口、ACPI_FADT 的 ACPI使能值。
    可变 等待 为 0。
    只要 等待 小于 3000：
        val 为 做 端口读16 用 ACPI_FADT 的 PM1a控制块。
        若 val 位与 0x0001：
            做 内核信息 用 「NPTM: ACPI使能成功 (耗时」 加 等待 加 「ms)」。
            返还 真。
        完。
        做 NPTM_微秒延迟 用 1000。
        等待 为 等待 加 1。
    完。
    做 内核警告 用 「NPTM: ACPI使能超时」。
    返还 假。
完。

NPTM_策略_性能 为 0。

NPTM_策略_平衡 为 1。

NPTM_策略_省电 为 2。

可变 NPTM_当前策略 为 1。

NPTM_设置电源策略 接受 策略：
    若 策略 小于 0 或者 策略 大于 2：
        返还。
    完。
    NPTM_当前策略 为 策略。
    可变 i 为 0。
    只要 i 小于 NPTM_电源域数：
        域 为 NPTM_电源域表 的 第 i 项。
        若 策略 等于 NPTM_策略_性能：
            域 的 空闲阈值毫秒 为 60000。
        否则若 策略 等于 NPTM_策略_平衡：
            域 的 空闲阈值毫秒 为 5000。
        否则：
            域 的 空闲阈值毫秒 为 1000。
        完。
        i 为 i 加 1。
    完。
    做 内核信息 用 「NPTM: 电源策略切换为 」 加 做 NPTM_策略名称 用 策略。
完。

NPTM_策略名称 接受 策略：
    若 策略 等于 NPTM_策略_性能：
        返还 「性能优先」。
    完。
    若 策略 等于 NPTM_策略_平衡：
        返还 「平衡模式」。
    完。
    若 策略 等于 NPTM_策略_省电：
        返还 「省电模式」。
    完。
    返还 「未知」。
完。

NPTM_初始化 接受 零参：
    NPTM_解析总数 为 NPTM_解析总数 加 1。
    做 内核信息 用 「NPTM: 电源管理子系统初始化...」。
    做 ACPI_解析FADT。
    做 ACPI_解析HPET。
    做 ACPI_解析MCFG。
    若 ACPI_FADT 不等于 空：
        做 NPTM_使能ACPI。
        若 ACPI_FADT 的 SCI中断 大于 0：
            做 中断_注册 用 ACPI_FADT 的 SCI中断、NPTM_SCI中断处理。
        完。
        做 NPTM_注册电源域 用 「CPU」、0、15000。
        做 NPTM_注册电源域 用 「内存」、0、5000。
        做 NPTM_注册电源域 用 「显示」、0、8000。
        做 NPTM_注册电源域 用 「磁盘」、0、3000。
        做 NPTM_注册电源域 用 「网络」、0、2000。
        做 NPTM_注册电源域 用 「USB」、0、1000。
    完。
    做 NPTM_设置电源策略 用 NPTM_策略_平衡。
    NPTM_当前S状态 为 ACPI_S0。
    做 内核信息 用 「NPTM: 初始化完成, 」 加 NPTM_电源域数 加 「 个电源域已注册」。
完。

可变 _谐振电源_切换次数 为 0。

谐振电源_自适应 接受 CPU负载、内存负载：
    综合负载 为 CPU负载 乘 60 除以 100 加 内存负载 乘 40 除以 100。
    若 综合负载 大于 800 并且 NPTM_当前策略 不等于 NPTM_策略_性能：
        做 NPTM_设置电源策略 用 NPTM_策略_性能。
        _谐振电源_切换次数 为 _谐振电源_切换次数 加 1。
    完。
    做 否则如果 用 综合负载 小于 200 并且 NPTM_当前策略 不等于 NPTM_策略_省电。
    块：
        做 NPTM_设置电源策略 用 NPTM_策略_省电。
        _谐振电源_切换次数 为 _谐振电源_切换次数 加 1。
    完。
    做 否则如果 用 综合负载 大于或等于 200 并且 综合负载 小于或等于 800 并且 NPTM_当前策略 不等于 NPTM_策略_平衡。
    块：
        做 NPTM_设置电源策略 用 NPTM_策略_平衡。
        _谐振电源_切换次数 为 _谐振电源_切换次数 加 1。
    完。
完。

电源管理_完整统计 接受 零参：
    返还 一组：「电源域数」、NPTM_电源域数、「当前S状态」、NPTM_当前S状态、「当前策略」、NPTM_当前策略、「谐振策略切换」、_谐振电源_切换次数。
完。

解析电源表电_统计 接受 零参：
    返还 一本字典：NPTM解析总数 对应 NPTM_解析总数、NPTM状态切换总数 对应 NPTM_状态切换总数、NPTMAI预测总数 对应 NPTM_AI预测总数、谐振电源切换次数 对应 _谐振电源_切换次数。
完。

解析电源表电_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 NPTM_解析总数 大于 100000：
        做 追加 用 问题、「NPTM_解析总数异常增长」。
    完。
    若 NPTM_状态切换总数 大于 100000：
        做 追加 用 问题、「NPTM_状态切换总数异常增长」。
    完。
    若 NPTM_AI预测总数 大于 100000：
        做 追加 用 问题、「NPTM_AI预测总数异常增长」。
    完。
    若 做 计数 用 NPTM_电源域表 大于 10000：
        做 追加 用 问题、「NPTM_电源域表膨胀」。
    完。
    若 _谐振电源_切换次数 大于 100000：
        做 追加 用 问题、「_谐振电源_切换次数异常增长」。
    完。
    若 NPTM_当前策略 小于 NPTM_策略_性能 或者 NPTM_当前策略 大于 NPTM_策略_省电：
        做 追加 用 问题、「NPTM_当前策略越界」。
    完。
    若 NPTM_电源域数 大于 NPTM_最大电源域：
        做 追加 用 问题、「NPTM_电源域数越界」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

解析电源表电_综合报告 接受 零参：
    做 打印 用 「[解析电源表电] ═══ 综合报告 ═══」。
    做 打印 用 「  NPTM_解析总数: 」 加 NPTM_解析总数。
    做 打印 用 「  NPTM_状态切换总数: 」 加 NPTM_状态切换总数。
    做 打印 用 「  NPTM_AI预测总数: 」 加 NPTM_AI预测总数。
    做 打印 用 「  _谐振电源_切换次数: 」 加 _谐振电源_切换次数。
    做 打印 用 「  健康: 」 加 若 做 解析电源表电_健康评估 则 「正常」 否则 「异常」。
完。

解析电源表电_全局重置 接受 零参：
    NPTM_解析总数 为 0。
    NPTM_状态切换总数 为 0。
    NPTM_AI预测总数 为 0。
    _谐振电源_切换次数 为 0。
    做 打印 用 「[解析电源表电] 统计已重置」。
    返还 0。
完。

本章 收束。
