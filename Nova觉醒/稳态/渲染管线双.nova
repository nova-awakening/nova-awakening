《渲染管线双》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·中枢·骨骼动画》。

引用《.·数学基础向量》。

引用《.·粒子系统》。

引用《..·引导·端口IO》。

可变 渲染_深度缓冲 为 一组：（空）。

可变 渲染_宽 为 1920。

可变 渲染_高 为 1080。

可变 渲染_帧数 为 0。

摄像机 是 一种 事物，含 位置、目标、上方向、视角、近平面、远平面、视图矩阵、投影矩阵。

可变 主摄像机 为 空。

摄像机_创建 接受 位置、目标：
    cam 为 新建 摄像机。
    cam 的 位置 为 位置。
    cam 的 目标 为 目标。
    cam 的 上方向 为 做 向量3_新 用 0、1000、0。
    cam 的 视角 为 600。
    cam 的 近平面 为 100。
    cam 的 远平面 为 100000。
    cam 的 投影矩阵 为 做 矩阵_透视 用 cam 的 视角、渲染_宽 乘 1000 除以 渲染_高、cam 的 近平面、cam 的 远平面。
    主摄像机 为 cam。
    返还 cam。
完。

渲染_网格 接受 fb、mesh、cam、全息模式：
    若 mesh 等于 空 或者 cam 等于 空：
        返还。
    完。
    渲染_帧数 为 渲染_帧数 加 1。
    可变 i 为 0。
    只要 i 小于 mesh 的 三角面数：
        面 为 mesh 的 三角面列表 的 第 i 项。
        v0 为 mesh 的 顶点列表 的 第 面 的 v0 项。
        v1 为 mesh 的 顶点列表 的 第 面 的 v1 项。
        v2 为 mesh 的 顶点列表 的 第 面 的 v2 项。
        p0 为 做 投影到屏幕 用 v0 的 位置、cam。
        p1 为 做 投影到屏幕 用 v1 的 位置、cam。
        p2 为 做 投影到屏幕 用 v2 的 位置、cam。
        若 全息模式：
            做 全息_画线 用 fb、p0 的 x、p0 的 y、p1 的 x、p1 的 y、v0 的 颜色、180。
            做 全息_画线 用 fb、p1 的 x、p1 的 y、p2 的 x、p2 的 y、v1 的 颜色、180。
            做 全息_画线 用 fb、p2 的 x、p2 的 y、p0 的 x、p0 的 y、v2 的 颜色、180。
        否则：
            做 渲染_三角形 用 fb、p0、p1、p2、v0 的 颜色。
        完。
        i 为 i 加 1。
    完。
    若 全息模式：
        做 全息_画光环 用 fb、渲染_宽 除以 2、渲染_高 乘 85 除以 100、200、0x44CCFF、渲染_帧数。
        做 全息_扫描线 用 fb、渲染_帧数。
    完。
完。

投影到屏幕 接受 世界位置、cam：
    相对 为 做 向量3_减 用 世界位置、cam 的 位置。
    屏幕x 为 渲染_宽 除以 2 加 相对 的 x 乘 渲染_宽 除以 2000。
    屏幕y 为 渲染_高 除以 2 减 相对 的 y 乘 渲染_高 除以 2000。
    返还 做 向量3_新 用 屏幕x、屏幕y、相对 的 z。
完。

全息_画线 接受 fb、x0、y0、x1、y1、颜色、alpha：
    可变 dx 为 x1 减 x0。
    可变 dy 为 y1 减 y0。
    若 dx 小于 0：
        dx 为 0 减 dx。
    完。
    若 dy 小于 0：
        dy 为 0 减 dy。
    完。
    sx 为 1。
    若 x0 大于 x1：
        sx 为 负 1。
    完。
    sy 为 1。
    若 y0 大于 y1：
        sy 为 负 1。
    完。
    可变 err 为 dx 减 dy。
    可变 cx 为 x0。
    可变 cy 为 y0。
    只要 真：
        若 cx 大于或等于 0 并且 cx 小于 渲染_宽 并且 cy 大于或等于 0 并且 cy 小于 渲染_高：
            做 全息_混合像素 用 fb、cx、cy、颜色、alpha。
            做 全息_混合像素 用 fb、cx 减 1、cy、颜色、alpha 除以 3。
            做 全息_混合像素 用 fb、cx 加 1、cy、颜色、alpha 除以 3。
            做 全息_混合像素 用 fb、cx、cy 减 1、颜色、alpha 除以 3。
            做 全息_混合像素 用 fb、cx、cy 加 1、颜色、alpha 除以 3。
        完。
        若 cx 等于 x1 并且 cy 等于 y1：
            中断。
        完。
        e2 为 err 乘 2。
        若 e2 大于 0 减 dy：
            err 为 err 减 dy。
            cx 为 cx 加 sx。
        完。
        若 e2 小于 dx：
            err 为 err 加 dx。
            cy 为 cy 加 sy。
        完。
    完。
完。

全息_混合像素 接受 fb、x、y、颜色、alpha：
    若 x 小于 0 或者 x 大于或等于 渲染_宽 或者 y 小于 0 或者 y 大于或等于 渲染_高：
        返还。
    完。
    偏移 为 (y 乘 渲染_宽 加 x) 乘 4。
    底色 为 做 内存读32 用 fb 加 偏移。
    混合 为 做 颜色_alpha混合_3d 用 底色、颜色、alpha。
    做 内存写32 用 fb 加 偏移、混合。
完。

颜色_alpha混合_3d 接受 底、顶、a：
    r 为 (底 右移 16 位与 0xFF) 乘 (255 减 a) 除以 255 加 (顶 右移 16 位与 0xFF) 乘 a 除以 255。
    g 为 (底 右移 8 位与 0xFF) 乘 (255 减 a) 除以 255 加 (顶 右移 8 位与 0xFF) 乘 a 除以 255。
    b 为 (底 位与 0xFF) 乘 (255 减 a) 除以 255 加 (顶 位与 0xFF) 乘 a 除以 255。
    返还 r 左移 16 位或 g 左移 8 位或 b。
完。

全息_画光环 接受 fb、cx、cy、半径、颜色、帧：
    可变 角 为 0。
    只要 角 小于 6283：
        动态角 为 角 加 帧 乘 50。
        rx 为 半径 乘 做 余弦近似 用 动态角 除以 1000。
        ry 为 半径 乘 做 正弦近似 用 动态角 除以 3000。
        脉冲 为 128 加 做 正弦近似 用 角 乘 3 加 帧 乘 200 乘 127 除以 1000。
        做 全息_混合像素 用 fb、cx 加 rx、cy 加 ry、颜色、脉冲。
        角 为 角 加 100。
    完。
完。

全息_扫描线 接受 fb、帧：
    扫描y 为 帧 乘 3 取余于 渲染_高。
    可变 x 为 0。
    只要 x 小于 渲染_宽：
        做 全息_混合像素 用 fb、x、扫描y、0x44CCFF、40。
        若 扫描y 大于 0：
            做 全息_混合像素 用 fb、x、扫描y 减 1、0x44CCFF、20。
        完。
        若 扫描y 小于 渲染_高 减 1：
            做 全息_混合像素 用 fb、x、扫描y 加 1、0x44CCFF、20。
        完。
        x 为 x 加 1。
    完。
完。

渲染_三角形 接受 fb、p0、p1、p2、颜色：
    可变 minX 为 p0 的 x。
    若 p1 的 x 小于 minX：
        minX 为 p1 的 x。
    完。
    若 p2 的 x 小于 minX：
        minX 为 p2 的 x。
    完。
    可变 maxX 为 p0 的 x。
    若 p1 的 x 大于 maxX：
        maxX 为 p1 的 x。
    完。
    若 p2 的 x 大于 maxX：
        maxX 为 p2 的 x。
    完。
    可变 minY 为 p0 的 y。
    若 p1 的 y 小于 minY：
        minY 为 p1 的 y。
    完。
    若 p2 的 y 小于 minY：
        minY 为 p2 的 y。
    完。
    可变 maxY 为 p0 的 y。
    若 p1 的 y 大于 maxY：
        maxY 为 p1 的 y。
    完。
    若 p2 的 y 大于 maxY：
        maxY 为 p2 的 y。
    完。
    若 minX 小于 0：
        minX 为 0。
    完。
    若 maxX 大于或等于 渲染_宽：
        maxX 为 渲染_宽 减 1。
    完。
    若 minY 小于 0：
        minY 为 0。
    完。
    若 maxY 大于或等于 渲染_高：
        maxY 为 渲染_高 减 1。
    完。
    可变 y 为 minY。
    只要 y 小于或等于 maxY：
        可变 x 为 minX。
        只要 x 小于或等于 maxX：
            若 做 点在三角形内 用 x、y、p0、p1、p2：
                做 全息_混合像素 用 fb、x、y、颜色、255。
            完。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
完。

点在三角形内 接受 px、py、v0、v1、v2：
    d1 为 (px 减 v1 的 x) 乘 (v0 的 y 减 v1 的 y) 减 (v0 的 x 减 v1 的 x) 乘 (py 减 v1 的 y)。
    d2 为 (px 减 v2 的 x) 乘 (v1 的 y 减 v2 的 y) 减 (v1 的 x 减 v2 的 x) 乘 (py 减 v2 的 y)。
    d3 为 (px 减 v0 的 x) 乘 (v2 的 y 减 v0 的 y) 减 (v2 的 x 减 v0 的 x) 乘 (py 减 v0 的 y)。
    有负 为 d1 小于 0 或者 d2 小于 0 或者 d3 小于 0。
    有正 为 d1 大于 0 或者 d2 大于 0 或者 d3 大于 0。
    返还 非 (有负 并且 有正)。
完。

可变 AI_渲染管线双_帧率窗口 为 一组：（空）。

AI_渲染管线双_帧率预测 接受 当前帧耗时：
    做 追加 用 AI_渲染管线双_帧率窗口、当前帧耗时。
    若 做 计数 用 AI_渲染管线双_帧率窗口 大于 30：
        做 列表_移除 用 AI_渲染管线双_帧率窗口、0。
    完。
    若 做 计数 用 AI_渲染管线双_帧率窗口 小于 3：
        返还 60。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_渲染管线双_帧率窗口：
        总 为 总 加 AI_渲染管线双_帧率窗口 的 第 i 项。
        i 为 i 加 1。
    完。
    平均帧耗时 为 总 除以 做 计数 用 AI_渲染管线双_帧率窗口。
    若 平均帧耗时 等于 0：
        返还 60。
    完。
    返还 1000 除以 平均帧耗时。
完。

AI_渲染管线双_质量降级建议 接受 帧率：
    若 帧率 小于 30：
        返还 「降低分辨率」。
    完。
    若 帧率 小于 45：
        返还 「跳过阴影」。
    完。
    返还 「全质量」。
完。

本章 收束。
