《中文支持框》

引用《..·中枢·类型定义》。

引用《..·中枢·中断表注册》。

引用《..·引导·端口IO》。

引用《..·中枢·整数转字符串》。

可变 中文字库_已加载 为 假。

可变 中文字库_地址 为 0。

可变 中文字库_大小 为 0。

中文字库_加载 接受 数据地址、大小：
    中文字库_地址 为 数据地址。
    中文字库_大小 为 大小。
    中文字库_已加载 为 真。
    做 内核信息 用 「中文字库: HZK16加载完成 (」 加 做 整数转字符串 用 大小 加 「 字节)」。
完。

字体_渲染中文字符 接受 fb、x、y、区码、位码、前景色、缩放：
    若 非 中文字库_已加载：
        返还。
    完。
    偏移 为 ((区码 减 1) 乘 94 加 (位码 减 1)) 乘 32。
    若 偏移 小于 0 或者 偏移 加 32 大于 中文字库_大小：
        返还。
    完。
    可变 行 为 0。
    只要 行 小于 16：
        高字节 为 做 内存读8 用 中文字库_地址 加 偏移 加 行 乘 2。
        低字节 为 做 内存读8 用 中文字库_地址 加 偏移 加 行 乘 2 加 1。
        行数据 为 高字节 左移 8 位或 低字节。
        可变 列 为 0。
        只要 列 小于 16：
            若 行数据 右移 15 减 列 位与 1 等于 1：
                可变 sy 为 0。
                只要 sy 小于 缩放：
                    可变 sx 为 0。
                    只要 sx 小于 缩放：
                        px 为 x 加 列 乘 缩放 加 sx。
                        py 为 y 加 行 乘 缩放 加 sy。
                        bpp 为 屏幕位深 除以 8。
                        若 bpp 小于 3：
                            bpp 为 4。
                        完。
                        像素偏移 为 (py 乘 屏幕宽 加 px) 乘 bpp。
                        做 内存写8 用 fb 加 像素偏移、前景色 位与 0xFF。
                        做 内存写8 用 fb 加 像素偏移 加 1、前景色 右移 8 位与 0xFF。
                        做 内存写8 用 fb 加 像素偏移 加 2、前景色 右移 16 位与 0xFF。
                        做 内存写8 用 fb 加 像素偏移 加 3、0xFF。
                        sx 为 sx 加 1。
                    完。
                    sy 为 sy 加 1。
                完。
            完。
            列 为 列 加 1。
        完。
        行 为 行 加 1。
    完。
完。

字体_渲染字符_AA 接受 fb、x、y、字符码、前景色、背景色：
    若 字符码 小于 字体_首字符 或者 字符码 大于 字体_末字符：
        返还。
    完。
    偏移 为 (字符码 减 字体_首字符) 乘 字体_高。
    bpp 为 屏幕位深 除以 8。
    若 bpp 小于 3：
        bpp 为 4。
    完。
    可变 行 为 0。
    只要 行 小于 字体_高：
        行数据 为 字体数据 的 第 偏移 加 行 项。
        上行 为 0。
        若 行 大于 0：
            上行 为 字体数据 的 第 偏移 加 行 减 1 项。
        完。
        下行 为 0。
        若 行 小于 字体_高 减 1：
            下行 为 字体数据 的 第 偏移 加 行 加 1 项。
        完。
        可变 列 为 0。
        只要 列 小于 字体_宽：
            位 为 7 减 列。
            中心 为 行数据 右移 位 位与 1。
            上 为 上行 右移 位 位与 1。
            下 为 下行 右移 位 位与 1。
            左 为 0。
            若 列 大于 0：
                左 为 行数据 右移 位 加 1 位与 1。
            完。
            右 为 0。
            若 列 小于 7：
                右 为 行数据 右移 位 减 1 位与 1。
            完。
            采样 为 中心 乘 5 加 上 加 下 加 左 加 右。
            前R 为 前景色 右移 16 位与 0xFF。
            前G 为 前景色 右移 8 位与 0xFF。
            前B 为 前景色 位与 0xFF。
            背R 为 背景色 右移 16 位与 0xFF。
            背G 为 背景色 右移 8 位与 0xFF。
            背B 为 背景色 位与 0xFF。
            混R 为 (前R 乘 采样 加 背R 乘 (9 减 采样)) 除以 9。
            混G 为 (前G 乘 采样 加 背G 乘 (9 减 采样)) 除以 9。
            混B 为 (前B 乘 采样 加 背B 乘 (9 减 采样)) 除以 9。
            px 为 x 加 列。
            py 为 y 加 行。
            像素偏移 为 (py 乘 屏幕宽 加 px) 乘 bpp。
            做 内存写8 用 fb 加 像素偏移、混B 位与 0xFF。
            做 内存写8 用 fb 加 像素偏移 加 1、混G 位与 0xFF。
            做 内存写8 用 fb 加 像素偏移 加 2、混R 位与 0xFF。
            做 内存写8 用 fb 加 像素偏移 加 3、0xFF。
            列 为 列 加 1。
        完。
        行 为 行 加 1。
    完。
完。

本章 收束。
