《鼠标光标》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《.·端口IO》。

引用《..·中枢·整数转字符串》。

光标图片_默认 为 「../资源/光标/光标_默认.png」。

光标图片_手指 为 「../资源/光标/光标_手指.png」。

光标图片_等待 为 「../资源/光标/光标_等待.png」。

光标图片_文本 为 「../资源/光标/光标_文本.png」。

光标形状_箭头 为 0。

光标形状_手指 为 1。

光标形状_等待 为 2。

光标形状_文本 为 3。

可变 当前光标形状 为 0。

可变 光标纹理缓存 为 一组：0、0、0、0。

光标_切换形状 接受 形状：
    若 形状 大于或等于 0 并且 形状 小于或等于 3：
        当前光标形状 为 形状。
    完。
完。

光标_获取图片路径 接受 形状：
    若 形状 等于 光标形状_手指：
        返还 光标图片_手指。
    完。
    若 形状 等于 光标形状_等待：
        返还 光标图片_等待。
    完。
    若 形状 等于 光标形状_文本：
        返还 光标图片_文本。
    完。
    返还 光标图片_默认。
完。

可变 cl 为 0。

只要 cl 小于 14：
    可变 cw 为 0。
    只要 cw 小于 cl 加 1 并且 cw 小于 10：
        做 内存写32 用 LFB 加 (384 加 cl) 乘 行字节 加 (512 加 cw) 乘 4、0x00FFFFFF。
        cw 为 cw 加 1。
    完。
    cl 为 cl 加 1。
完。

做 打印 用 「」。

做 打印 用 「═══════════════════════════════════════════════════」。

做 打印 用 「  NovaOS v0.3.0 — 全息AI桌面 (纯Nova语言)」。

做 打印 用 「  渲染: 内存写32() → BGA LFB 0xFD000000」。

做 打印 用 「  全息AI人物: 3D线框+粒子+眼睛+嘴+身体+气泡」。

做 打印 用 「  云端接口: 配置面板(Key/URL/Port/Model)」。

做 打印 用 「  本地模型: TNF推理引擎直连」。

做 打印 用 「  零assembly桌面代码, 纯Nova原生渲染」。

做 打印 用 「═══════════════════════════════════════════════════」。

做 端口写8 用 0x2F9、0x00。

做 端口写8 用 0x2FB、0x80。

做 端口写8 用 0x2F8、0x01。

做 端口写8 用 0x2F9、0x00。

做 端口写8 用 0x2FB、0x03。

做 端口写8 用 0x2FA、0xC7。

做 端口写8 用 0x2FC、0x0B。

做 打印 用 「[NovaOS] COM2 UART 已初始化 (命令通道 0x2F8)」。

做 打印 用 「[NovaOS] 进入真实PS/2交互主循环...」。

可变 鼠x 为 512。

可变 鼠y 为 384。

可变 鼠旧x 为 512。

可变 鼠旧y 为 384。

可变 鼠字节 为 0。

可变 鼠b1 为 0。

可变 鼠b2 为 0。

可变 鼠b3 为 0。

可变 输入缓冲 为 「」。

可变 帧计数 为 0。

可变 脉动相位 为 0。

可变 全息人物x 为 680。

可变 全息人物方向 为 1。

可变 空闲计数 为 0。

可变 人物动作 为 0。

扫描码ASCII 为 一组：0、0、49、50、51、52、53、54、55、56、57、48、45、61、8、9、113、119、101、114、116、121、117、105、111、112、91、93、10、0、97、115、100、102、103、104、106、107、108、59、39、96、0、92、122、120、99、118、98、110、109、44、46、47、0、42、0、32。

只要 帧计数 小于 2000000000：
    若 帧计数 取余于 5000000 等于 0：
        做 打印 用 「[心跳] 帧=」 加 做 整数转字符串 用 帧计数 加 「 鼠标=(」 加 做 整数转字符串 用 鼠x 加 「,」 加 做 整数转字符串 用 鼠y 加 「) 动作=」 加 做 整数转字符串 用 人物动作。
    完。
    状态 为 做 端口读8 用 0x64。
    若 状态 位与 0x01 不等于 0：
        数据 为 做 端口读8 用 0x60。
        若 状态 位与 0x20 不等于 0：
            若 鼠字节 等于 0：
                鼠b1 为 数据。
                若 数据 位与 0x08 不等于 0：
                    鼠字节 为 1。
                完。
            否则若 鼠字节 等于 1：
                鼠b2 为 数据。
                鼠字节 为 2。
            否则若 鼠字节 等于 2：
                鼠b3 为 数据。
                鼠字节 为 0。
                可变 dx 为 鼠b2。
                可变 dy 为 鼠b3。
                若 鼠b1 位与 0x10 不等于 0：
                    dx 为 dx 减 256。
                完。
                若 鼠b1 位与 0x20 不等于 0：
                    dy 为 dy 减 256。
                完。
                dy 为 0 减 dy。
                鼠旧x 为 鼠x。
                鼠旧y 为 鼠y。
                鼠x 为 鼠x 加 dx。
                鼠y 为 鼠y 加 dy。
                若 鼠x 小于 0：
                    鼠x 为 0。
                完。
                若 鼠y 小于 28：
                    鼠y 为 28。
                完。
                若 鼠x 大于 屏宽 减 2：
                    鼠x 为 屏宽 减 2。
                完。
                若 鼠y 大于 屏高 减 34：
                    鼠y 为 屏高 减 34。
                完。
                可变 ecy 为 0。
                只要 ecy 小于 14：
                    可变 ecx 为 0。
                    只要 ecx 小于 10：
                        ey 为 鼠旧y 加 ecy。
                        ex 为 鼠旧x 加 ecx。
                        若 ex 大于 0 并且 ex 小于 屏宽 并且 ey 大于 28 并且 ey 小于 屏高 减 30：
                            壁t 为 ey 乘 256 除以 屏高。
                            壁色 为 (8 加 12 乘 壁t 除以 256) 乘 65536 加 (18 减 10 乘 壁t 除以 256) 乘 256 加 (30 加 14 乘 壁t 除以 256)。
                            做 内存写32 用 LFB 加 ey 乘 行字节 加 ex 乘 4、壁色。
                        完。
                        ecx 为 ecx 加 1。
                    完。
                    ecy 为 ecy 加 1。
                完。
                可变 ncy 为 0。
                只要 ncy 小于 12：
                    可变 ncx 为 0。
                    只要 ncx 小于 ncy 加 1 并且 ncx 小于 10：
                        若 鼠x 加 ncx 小于 屏宽 并且 鼠y 加 ncy 小于 屏高 减 30：
                            做 内存写32 用 LFB 加 (鼠y 加 ncy) 乘 行字节 加 (鼠x 加 ncx) 乘 4、0x00FFFFFF。
                        完。
                        ncx 为 ncx 加 1。
                    完。
                    ncy 为 ncy 加 1。
                完。
                空闲计数 为 0。
                若 鼠b1 位与 0x01 不等于 0：
                    若 鼠x 大于 全息人物x 减 60 并且 鼠x 小于 全息人物x 加 60 并且 鼠y 大于 160 并且 鼠y 小于 450：
                        人物动作 为 2。
                        做 打印 用 「[交互] 点击全息人物 → 挥手」。
                    完。
                    若 鼠x 大于 对话x 加 对话w 减 65 并且 鼠x 小于 对话x 加 对话w 减 15 并且 鼠y 大于 对话y 加 对话h 减 42 并且 鼠y 小于 对话y 加 对话h 减 14：
                        做 打印 用 「[交互] 点击发送: 」 加 输入缓冲。
                        输入缓冲 为 「」。
                    完。
                完。
            完。
        否则：
            做 打印 用 「[KB] scan=」 加 做 整数转字符串 用 数据。
            若 数据 小于 128：
                空闲计数 为 0。
                若 数据 等于 0x1C：
                    做 打印 用 「[输入] 发送: 」 加 输入缓冲。
                    输入缓冲 为 「」。
                    人物动作 为 3。
                否则若 数据 等于 0x0E：
                    若 做 字符串长度 用 输入缓冲 大于 0：
                        输入缓冲 为 做 字符串子串 用 输入缓冲、0、做 字符串长度 用 输入缓冲 减 1。
                    完。
                否则若 数据 小于 58：
                    ch 为 扫描码ASCII 的 第 数据 项。
                    若 ch 大于 0：
                        输入缓冲 为 输入缓冲 加 做 字符转字符串 用 ch。
                        光标绘x 为 对话x 加 16 加 做 字符串长度 用 输入缓冲 乘 8。
                        若 光标绘x 小于 对话x 加 对话w 减 80：
                            做 内存写32 用 LFB 加 (对话y 加 对话h 减 34) 乘 行字节 加 光标绘x 乘 4、0x003B82F6。
                            做 内存写32 用 LFB 加 (对话y 加 对话h 减 33) 乘 行字节 加 光标绘x 乘 4、0x003B82F6。
                            做 内存写32 用 LFB 加 (对话y 加 对话h 减 32) 乘 行字节 加 光标绘x 乘 4、0x003B82F6。
                        完。
                    完。
                完。
                若 数据 等于 0x57：
                    做 打印 用 「[F11] 帧=」 加 做 整数转字符串 用 帧计数 加 「 鼠标=」 加 做 整数转字符串 用 鼠x 加 「,」 加 做 整数转字符串 用 鼠y 加 「 动作=」 加 做 整数转字符串 用 人物动作。
                完。
                若 数据 等于 0x3B：
                    可变 f1y 为 0。
                    只要 f1y 小于 50：
                        可变 f1x 为 0。
                        只要 f1x 小于 100：
                            做 内存写32 用 LFB 加 (300 加 f1y) 乘 行字节 加 (500 加 f1x) 乘 4、0x00FF4444。
                            f1x 为 f1x 加 1。
                        完。
                        f1y 为 f1y 加 1。
                    完。
                    做 打印 用 「[F1] 画红色矩形 (500,300) 100x50」。
                完。
                若 数据 等于 0x3C：
                    可变 f2y 为 0。
                    只要 f2y 小于 50：
                        可变 f2x 为 0。
                        只要 f2x 小于 100：
                            做 内存写32 用 LFB 加 (300 加 f2y) 乘 行字节 加 (620 加 f2x) 乘 4、0x0022C55E。
                            f2x 为 f2x 加 1。
                        完。
                        f2y 为 f2y 加 1。
                    完。
                    做 打印 用 「[F2] 画绿色矩形 (620,300) 100x50」。
                完。
                若 数据 等于 0x3D：
                    f3文字 为 一组：76、73、86、69、32、67、79、78、84、82、79、76。
                    可变 f3i 为 0。
                    只要 f3i 小于 12：
                        f3idx 为 (f3文字 的 第 f3i 项 减 32) 乘 8。
                        若 f3idx 大于或等于 0 并且 f3idx 小于 760：
                            可变 f3r 为 0。
                            只要 f3r 小于 8：
                                f3rd 为 字体 的 第 f3idx 加 f3r 项。
                                可变 f3b 为 0。
                                只要 f3b 小于 8：
                                    若 f3rd 右移 7 减 f3b 位与 1：
                                        做 内存写32 用 LFB 加 (360 加 f3r) 乘 行字节 加 (510 加 f3i 乘 9 加 f3b) 乘 4、0x00FFFFFF。
                                    完。
                                    f3b 为 f3b 加 1。
                                完。
                                f3r 为 f3r 加 1。
                            完。
                        完。
                        f3i 为 f3i 加 1。
                    完。
                    做 打印 用 「[F3] 画文字 LIVE CONTROL」。
                完。
                若 数据 等于 0x3E：
                    可变 f4y 为 0。
                    只要 f4y 小于 50：
                        可变 f4x 为 0。
                        只要 f4x 小于 100：
                            做 内存写32 用 LFB 加 (300 加 f4y) 乘 行字节 加 (740 加 f4x) 乘 4、0x004FC3F7。
                            f4x 为 f4x 加 1。
                        完。
                        f4y 为 f4y 加 1。
                    完。
                    做 打印 用 「[F4] 画蓝色矩形 (740,300) 100x50」。
                完。
            完。
        完。
    完。
    若 帧计数 取余于 4096 等于 0：
        脉动相位 为 (脉动相位 加 1) 取余于 4。
        脉动色列 为 一组：0x0000EEFF、0x0044FFFF、0x0088FFFF、0x0044FFFF。
        脉色 为 脉动色列 的 第 脉动相位 项。
        可变 脉i 为 0。
        只要 脉i 小于 6：
            若 全息人物x 加 脉i 大于 100 并且 全息人物x 加 脉i 小于 屏宽：
                做 内存写32 用 LFB 加 155 乘 行字节 加 (全息人物x 减 3 加 脉i) 乘 4、脉色。
            完。
            脉i 为 脉i 加 1。
        完。
    完。
    空闲计数 为 空闲计数 加 1。
    若 空闲计数 大于 100000 并且 人物动作 等于 0：
        人物动作 为 1。
    完。
    若 人物动作 等于 1 并且 帧计数 取余于 2048 等于 0：
        全息人物x 为 全息人物x 加 全息人物方向 乘 2。
        若 全息人物x 大于 900：
            全息人物方向 为 负 1。
            人物动作 为 0。
            空闲计数 为 0。
        完。
        若 全息人物x 小于 550：
            全息人物方向 为 1。
            人物动作 为 0。
            空闲计数 为 0。
        完。
    完。
    若 人物动作 等于 2 并且 空闲计数 大于 30000：
        人物动作 为 0。
    完。
    若 人物动作 等于 3 并且 空闲计数 大于 50000：
        人物动作 为 0。
    完。
    帧计数 为 帧计数 加 1。
完。

做 打印 用 「[NovaOS] 交互主循环结束 — 帧数=」 加 做 整数转字符串 用 帧计数。

做 打印 用 「[NovaOS] 全息AI桌面运行结束 — 停机」。

光标_默认宽 为 12。

光标_默认高 为 19。

光标_热点X 为 0。

光标_热点Y 为 0。

光标_获取热点 接受 形状：
    若 形状 等于 0：
        返还 一组：0、0。
    完。
    若 形状 等于 1：
        返还 一组：6、9。
    完。
    若 形状 等于 2：
        返还 一组：5、0。
    完。
    返还 一组：0、0。
完。

光标_边界检查 接受 x、y、屏宽、屏高：
    若 x 小于 0：
        x 为 0。
    完。
    若 y 小于 0：
        y 为 0。
    完。
    若 x 大于 屏宽 减 光标_默认宽：
        x 为 屏宽 减 光标_默认宽。
    完。
    若 y 大于 屏高 减 光标_默认高：
        y 为 屏高 减 光标_默认高。
    完。
    返还 一组：x、y。
完。

本章 收束。
