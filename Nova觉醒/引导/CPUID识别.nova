《CPUID识别》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·整数转字符串》。

CPUID 接受 叶：
    可变 eax 为 0。
    可变 ebx 为 0。
    可变 ecx 为 0。
    可变 edx 为 0。
    汇编。
    块：
        「mov eax, [rbp+16]」。
        「xor ecx, ecx」。
        「cpuid」。
        「mov [rbp-8], rax」。
        「mov [rbp-16], rbx」。
        「mov [rbp-24], rcx」。
        「mov [rbp-32], rdx」。
    完。
    返还 一组：eax、ebx、ecx、edx。
完。

CPUID_子叶 接受 叶、子叶：
    可变 eax 为 0。
    可变 ebx 为 0。
    可变 ecx 为 0。
    可变 edx 为 0。
    汇编。
    块：
        「mov eax, [rbp+24]」。
        「mov ecx, [rbp+16]」。
        「cpuid」。
        「mov [rbp-8], rax」。
        「mov [rbp-16], rbx」。
        「mov [rbp-24], rcx」。
        「mov [rbp-32], rdx」。
    完。
    返还 一组：eax、ebx、ecx、edx。
完。

CPU最大叶 接受 零参：
    结果 为 做 CPUID 用 0。
    返还 结果 的 第 0 项。
完。

CPU厂商 接受 零参：
    结果 为 做 CPUID 用 0。
    若 结果 的 第 1 项 等于 0x756E6547：
        返还 「Intel」。
    完。
    若 结果 的 第 1 项 等于 0x68747541：
        返还 「AMD」。
    完。
    返还 「Unknown」。
完。

CPU是Intel 接受 零参：
    返还 做 CPU厂商 等于 「Intel」。
完。

CPU是AMD 接受 零参：
    返还 做 CPU厂商 等于 「AMD」。
完。

CPU型号信息 接受 零参：
    结果 为 做 CPUID 用 1。
    eax 为 结果 的 第 0 项。
    步进 为 eax 位与 0xF。
    型号 为 eax 右移 4 位与 0xF。
    族 为 eax 右移 8 位与 0xF。
    扩展型号 为 eax 右移 16 位与 0xF。
    扩展族 为 eax 右移 20 位与 0xFF。
    返还 一组：族 加 扩展族、扩展型号 左移 4 位或 型号、步进。
完。

CPU支持FPU 接受 零参：
    返还 做 CPUID 用 1 的 第 3 项 右移 0 位与 1。
完。

CPU支持TSC 接受 零参：
    返还 做 CPUID 用 1 的 第 3 项 右移 4 位与 1。
完。

CPU支持MSR 接受 零参：
    返还 做 CPUID 用 1 的 第 3 项 右移 5 位与 1。
完。

CPU支持PAE 接受 零参：
    返还 做 CPUID 用 1 的 第 3 项 右移 6 位与 1。
完。

CPU支持接口C 接受 零参：
    返还 做 CPUID 用 1 的 第 3 项 右移 9 位与 1。
完。

CPU支持MTRR 接受 零参：
    返还 做 CPUID 用 1 的 第 3 项 右移 12 位与 1。
完。

CPU支持PGE 接受 零参：
    返还 做 CPUID 用 1 的 第 3 项 右移 13 位与 1。
完。

CPU支持PAT 接受 零参：
    返还 做 CPUID 用 1 的 第 3 项 右移 16 位与 1。
完。

CPU支持PSE36 接受 零参：
    返还 做 CPUID 用 1 的 第 3 项 右移 17 位与 1。
完。

CPU支持FXSR 接受 零参：
    返还 做 CPUID 用 1 的 第 3 项 右移 24 位与 1。
完。

CPU支持SSE 接受 零参：
    返还 做 CPUID 用 1 的 第 3 项 右移 25 位与 1。
完。

CPU支持SSE2 接受 零参：
    返还 做 CPUID 用 1 的 第 3 项 右移 26 位与 1。
完。

CPU支持SSE3 接受 零参：
    返还 做 CPUID 用 1 的 第 2 项 右移 0 位与 1。
完。

CPU支持SSSE3 接受 零参：
    返还 做 CPUID 用 1 的 第 2 项 右移 9 位与 1。
完。

CPU支持SSE4_1 接受 零参：
    返还 做 CPUID 用 1 的 第 2 项 右移 19 位与 1。
完。

CPU支持SSE4_2 接受 零参：
    返还 做 CPUID 用 1 的 第 2 项 右移 20 位与 1。
完。

CPU支持x2接口C 接受 零参：
    返还 做 CPUID 用 1 的 第 2 项 右移 21 位与 1。
完。

CPU支持POPCNT 接受 零参：
    返还 做 CPUID 用 1 的 第 2 项 右移 23 位与 1。
完。

CPU支持XSAVE 接受 零参：
    返还 做 CPUID 用 1 的 第 2 项 右移 26 位与 1。
完。

CPU支持AVX 接受 零参：
    返还 做 CPUID 用 1 的 第 2 项 右移 28 位与 1。
完。

CPU支持RDRAND 接受 零参：
    返还 做 CPUID 用 1 的 第 2 项 右移 30 位与 1。
完。

CPU支持FSGSBASE 接受 零参：
    返还 做 CPUID_子叶 用 7、0 的 第 1 项 右移 0 位与 1。
完。

CPU支持SMEP 接受 零参：
    返还 做 CPUID_子叶 用 7、0 的 第 1 项 右移 7 位与 1。
完。

CPU支持SMAP 接受 零参：
    返还 做 CPUID_子叶 用 7、0 的 第 1 项 右移 20 位与 1。
完。

CPU支持长模式 接受 零参：
    返还 做 CPUID 用 0x80000001 的 第 3 项 右移 29 位与 1。
完。

CPU支持NX 接受 零参：
    返还 做 CPUID 用 0x80000001 的 第 3 项 右移 20 位与 1。
完。

CPU支持1GB页 接受 零参：
    返还 做 CPUID 用 0x80000001 的 第 3 项 右移 26 位与 1。
完。

CPU支持RDTSCP 接受 零参：
    返还 做 CPUID 用 0x80000001 的 第 3 项 右移 27 位与 1。
完。

CPU支持Nova系统调用 接受 零参：
    返还 做 CPUID 用 0x80000001 的 第 3 项 右移 11 位与 1。
完。

CPU信息_打印 接受 零参：
    型号 为 做 CPU型号信息。
    做 内核信息 用 「CPU: 」 加 做 CPU厂商 加 「 Family 」 加 做 整数转字符串 用 型号 的 第 0 项 加 「 Model 」 加 做 整数转字符串 用 型号 的 第 1 项 加 「 Stepping 」 加 做 整数转字符串 用 型号 的 第 2 项。
    做 内核信息 用 「  特性: 」 加 若 做 CPU支持SSE2 则 「SSE2 」 否则 「」 加 若 做 CPU支持AVX 则 「AVX 」 否则 「」 加 若 做 CPU支持NX 则 「NX 」 否则 「」 加 若 做 CPU支持1GB页 则 「1GB-pages 」 否则 「」 加 若 做 CPU支持RDRAND 则 「RDRAND 」 否则 「」 加 若 做 CPU支持SMEP 则 「SMEP 」 否则 「」 加 若 做 CPU支持SMAP 则 「SMAP 」 否则 「」。
完。

CPU缓存信息 接受 零参：
    可变 缓存列表 为 一组：（空）。
    可变 子叶 为 0。
    只要 子叶 小于 8：
        结果 为 做 CPUID_子叶 用 4、子叶。
        类型 为 结果 的 第 0 项 位与 0x1F。
        若 类型 等于 0：
            返还 缓存列表。
        完。
        级别 为 结果 的 第 0 项 右移 5 位与 0x7。
        行大小 为 (结果 的 第 1 项 位与 0xFFF) 加 1。
        分区 为 (结果 的 第 1 项 右移 12 位与 0x3FF) 加 1。
        路数 为 (结果 的 第 1 项 右移 22 位与 0x3FF) 加 1。
        组数 为 结果 的 第 2 项 加 1。
        大小KB 为 行大小 乘 分区 乘 路数 乘 组数 除以 1024。
        做 追加 用 缓存列表、一组：级别、类型、大小KB、路数、行大小。
        子叶 为 子叶 加 1。
    完。
    返还 缓存列表。
完。

CPU品牌字符串 接受 零参：
    最大扩展叶 为 做 CPUID 用 0x80000000 的 第 0 项。
    若 最大扩展叶 小于 0x80000004：
        返还 「Unknown CPU」。
    完。
    可变 品牌 为 「」。
    可变 叶 为 0x80000002。
    只要 叶 小于或等于 0x80000004：
        结果 为 做 CPUID 用 叶。
        可变 reg 为 0。
        只要 reg 小于 4：
            值 为 结果 的 第 reg 项。
            可变 b 为 0。
            只要 b 小于 4：
                字符 为 值 右移 b 乘 8 位与 0xFF。
                若 字符 不等于 0：
                    品牌 为 品牌 加 做 字符转字符串 用 字符。
                完。
                b 为 b 加 1。
            完。
            reg 为 reg 加 1。
        完。
        叶 为 叶 加 1。
    完。
    返还 品牌。
完。

CPU支持AVX2 接受 零参：
    返还 做 CPUID_子叶 用 7、0 的 第 1 项 右移 5 位与 1。
完。

CPU支持BMI1 接受 零参：
    返还 做 CPUID_子叶 用 7、0 的 第 1 项 右移 3 位与 1。
完。

CPU支持BMI2 接受 零参：
    返还 做 CPUID_子叶 用 7、0 的 第 1 项 右移 8 位与 1。
完。

CPU支持INVPCID 接受 零参：
    返还 做 CPUID_子叶 用 7、0 的 第 1 项 右移 10 位与 1。
完。

CPU支持RDSEED 接受 零参：
    返还 做 CPUID_子叶 用 7、0 的 第 1 项 右移 18 位与 1。
完。

CPU支持CLFLUSHOPT 接受 零参：
    返还 做 CPUID_子叶 用 7、0 的 第 1 项 右移 23 位与 1。
完。

CPUID查询 接受 叶、子叶：
    返还 做 CPUID_子叶 用 叶、子叶。
完。

CPU_特性计数 接受 零参：
    可变 计 为 0。
    计 为 计 加 做 CPU支持FPU 加 做 CPU支持TSC 加 做 CPU支持MSR。
    计 为 计 加 做 CPU支持接口C 加 做 CPU支持SSE 加 做 CPU支持SSE2。
    计 为 计 加 做 CPU支持AVX 加 做 CPU支持NX 加 做 CPU支持RDRAND。
    计 为 计 加 做 CPU支持SMEP 加 做 CPU支持SMAP。
    返还 计。
完。

CPUID_完整统计 接受 零参：
    返还 一组：做 CPU厂商、做 CPU最大叶、做 CPU_特性计数。
完。

可变 AI_处理标识查_运行指标 为 0。

可变 AI_处理标识查_异常指标 为 0。

AI_处理标识查_自诊断 接受 零参：
    AI_处理标识查_运行指标 为 0。
    返还 AI_处理标识查_运行指标。
完。

AI_处理标识查_优化建议 接受 零参：
    指标 为 做 AI_处理标识查_自诊断。
    返还 「处理标识查状态正常」。
完。

本章 收束。
