《安装状态机》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·整数转字符串》。

引用《.·安装流程》。

引用《..·稳态·PCI配置读》。

外部。

VFS_挂载。

UI_背景色 为 0x0A0E1A。

UI_标题栏色 为 0x141E30。

UI_内容区背景 为 0x121828。

UI_边框色 为 0x1A2A44。

UI_日志背景色 为 0x0D1520。

UI_进度条填充 为 0x0088CC。

UI_强调色 为 0x00CCFF。

UI_成功色 为 0x00AA44。

UI_未达到色 为 0x2A3A5A。

UI_正文色 为 0xCCDDEE。

UI_标签色 为 0x8899AA。

UI_日志标题色 为 0x5588AA。

UI_提示色 为 0x44FF88。

UI_副标题色 为 0x8899BB。

UI_白色 为 0xFFFFFF。

安装阶段_欢迎 为 0。

安装阶段_硬件探测 为 1。

安装阶段_AI配置 为 2。

安装阶段_磁盘分区 为 3。

安装阶段_系统部署 为 4。

安装阶段_AI模型部署 为 5。

安装阶段_首次配置 为 6。

安装阶段_完成 为 7。

安装上下文 是 一种 事物，含 当前阶段、总进度、阶段进度、开始时间、阶段消息、详细日志、错误数、警告数、CPU_型号、CPU_核心数、CPU_频率MHz、内存_总MB、磁盘列表、图处_型号、图处_显存MB、网卡列表、UEFI模式、目标磁盘、分区方案、安装AI模型、语言、时区、用户名、推荐_内存分配、推荐_交换分区、推荐_AI模型、推荐_分辨率。

可变 安装 为 新建 安装上下文。

外部。

CPUID_读取。

外部。

内存_探测总量。

外部。

PCI_枚举设备。

外部。

ACPI_获取信息。

外部。

磁盘_枚举。

外部。

读取毫秒。

安装_硬件探测 接受 零参：
    安装 的 阶段消息 为 「正在探测硬件...」。
    安装 的 阶段进度 为 0。
    做 安装_日志 用 「═══ 硬件探测开始 ═══」。
    安装 的 阶段进度 为 100。
    安装 的 阶段消息 为 「探测CPU...」。
    _cpu信息 为 做 CPUID_读取 用 0。
    安装 的 CPU_核心数 为 做 CPUID_读取 用 1 位与 0xFF。
    若 安装 的 CPU_核心数 等于 0：
        安装 的 CPU_核心数 为 1。
    完。
    安装 的 CPU_频率MHz 为 做 CPUID_读取 用 0x16 位与 0xFFFF。
    若 安装 的 CPU_频率MHz 等于 0：
        安装 的 CPU_频率MHz 为 2000。
    完。
    安装 的 CPU_型号 为 「x86-64 」 加 做 整数转字符串 用 安装 的 CPU_核心数 加 「核 @」 加 做 整数转字符串 用 安装 的 CPU_频率MHz 加 「MHz」。
    做 安装_日志 用 「CPU: 」 加 安装 的 CPU_型号。
    安装 的 阶段进度 为 300。
    安装 的 阶段消息 为 「探测内存...」。
    安装 的 内存_总MB 为 做 内存_探测总量 除以 1048576。
    做 安装_日志 用 「内存: 」 加 做 整数转字符串 用 安装 的 内存_总MB 加 「 MB」。
    安装 的 阶段进度 为 500。
    安装 的 阶段消息 为 「扫描磁盘...」。
    安装 的 磁盘列表 为 做 磁盘_枚举。
    做 安装_日志 用 「磁盘: 」 加 做 整数转字符串 用 做 计数 用 安装 的 磁盘列表 加 「 个」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 安装 的 磁盘列表：
        d 为 安装 的 磁盘列表 的 第 i 项。
        做 安装_日志 用 「  [」 加 做 整数转字符串 用 i 加 「] 」 加 d 的 名称 加 「 」 加 做 整数转字符串 用 d 的 大小MB 加 「MB 」 加 d 的 类型。
        i 为 i 加 1。
    完。
    安装 的 阶段进度 为 700。
    安装 的 阶段消息 为 「检测图处...」。
    pci设备 为 做 PCI_枚举设备。
    可变 j 为 0。
    只要 j 小于 做 计数 用 pci设备：
        若 pci设备 的 第 j 项 的 类别 等于 0x03：
            安装 的 图处_型号 为 pci设备 的 第 j 项 的 名称。
            安装 的 图处_显存MB 为 pci设备 的 第 j 项 的 BAR大小 除以 1048576。
            做 安装_日志 用 「图处: 」 加 安装 的 图处_型号 加 「 」 加 做 整数转字符串 用 安装 的 图处_显存MB 加 「MB」。
        完。
        j 为 j 加 1。
    完。
    若 安装 的 图处_型号 等于 「」：
        安装 的 图处_型号 为 「软渲染(无独立图处)」。
    完。
    安装 的 阶段进度 为 850。
    安装 的 阶段消息 为 「检测网络...」。
    可变 k 为 0。
    只要 k 小于 做 计数 用 pci设备：
        若 pci设备 的 第 k 项 的 类别 等于 0x02：
            做 追加 用 安装 的 网卡列表、pci设备 的 第 k 项 的 名称。
            做 安装_日志 用 「网卡: 」 加 pci设备 的 第 k 项 的 名称。
        完。
        k 为 k 加 1。
    完。
    安装 的 UEFI模式 为 做 ACPI_获取信息 不等于 空。
    安装 的 阶段进度 为 1000。
    安装 的 阶段消息 为 「硬件探测完成」。
    做 安装_日志 用 「═══ 硬件探测完成 ═══」。
完。

本章 收束。
