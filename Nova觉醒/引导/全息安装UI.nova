《全息安装UI》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·整数转字符串》。

引用《..·稳态·字体写字形》。

引用《..·稳态·图层合成Alpha混合》。

外部。

像素_填充矩形。

外部。

屏幕宽。

外部。

屏幕高。

外部。

字体_渲染文本。

外部。

字体_渲染文本_透明。

安装UI_渲染 接受 fb：
    做 像素_填充矩形 用 fb、0、0、屏幕宽、屏幕高、UI_背景色。
    做 像素_填充矩形 用 fb、0、0、屏幕宽、60、UI_标题栏色。
    做 字体_渲染文本_透明 用 fb、32、20、「NovaOS Installer」、UI_强调色、2。
    做 安装UI_渲染阶段指示 用 fb、屏幕宽 除以 2 减 200、80。
    做 安装UI_渲染主内容 用 fb。
    做 安装UI_渲染进度条 用 fb。
    做 安装UI_渲染日志 用 fb。
完。

安装UI_渲染阶段指示 接受 fb、x、y：
    阶段名 为 一组：「Welcome」、「Hardware」、「AI Config」、「Partition」、「Deploy」、「AI Model」、「Setup」、「Done!」。
    可变 i 为 0。
    只要 i 小于 8：
        cx 为 x 加 i 乘 58。
        颜色 为 UI_未达到色。
        若 i 小于 安装 的 当前阶段：
            颜色 为 UI_成功色。
        完。
        若 i 等于 安装 的 当前阶段：
            颜色 为 UI_强调色。
        完。
        做 像素_填充矩形 用 fb、cx、y、12、12、颜色。
        做 字体_渲染文本_透明 用 fb、cx 减 8、y 加 16、阶段名 的 第 i 项、UI_标签色、1。
        i 为 i 加 1。
    完。
完。

安装UI_渲染主内容 接受 fb：
    内容x 为 40。
    内容y 为 160。
    内容宽 为 屏幕宽 乘 60 除以 100。
    内容高 为 屏幕高 减 260。
    做 像素_填充矩形 用 fb、内容x、内容y、内容宽、内容高、UI_内容区背景。
    做 像素_填充矩形 用 fb、内容x、内容y、内容宽、2、UI_边框色。
    做 字体_渲染文本_透明 用 fb、内容x 加 20、内容y 加 16、安装 的 阶段消息、UI_强调色、2。
    若 安装 的 当前阶段 等于 安装阶段_欢迎：
        做 字体_渲染文本_透明 用 fb、内容x 加 20、内容y 加 80、「Welcome to NovaOS」、UI_白色、3。
        做 字体_渲染文本_透明 用 fb、内容x 加 20、内容y 加 140、「AI-Native Operating System」、UI_副标题色、2。
        做 字体_渲染文本_透明 用 fb、内容x 加 20、内容y 加 200、「Press ENTER to begin installation」、UI_提示色、1。
    完。
    做 否则如果 用 安装 的 当前阶段 等于 安装阶段_硬件探测。
    块：
        做 字体_渲染文本_透明 用 fb、内容x 加 20、内容y 加 70、「CPU: 」 加 安装 的 CPU_型号、UI_正文色、1。
        做 字体_渲染文本_透明 用 fb、内容x 加 20、内容y 加 95、「RAM: 」 加 做 整数转字符串 用 安装 的 内存_总MB 加 「 MB」、UI_正文色、1。
        做 字体_渲染文本_透明 用 fb、内容x 加 20、内容y 加 120、「图处: 」 加 安装 的 图处_型号、UI_正文色、1。
        做 字体_渲染文本_透明 用 fb、内容x 加 20、内容y 加 145、「Disks: 」 加 做 整数转字符串 用 做 计数 用 安装 的 磁盘列表、UI_正文色、1。
        做 字体_渲染文本_透明 用 fb、内容x 加 20、内容y 加 170、「NICs: 」 加 做 整数转字符串 用 做 计数 用 安装 的 网卡列表、UI_正文色、1。
    完。
    做 否则如果 用 安装 的 当前阶段 等于 安装阶段_AI配置。
    块：
        做 字体_渲染文本_透明 用 fb、内容x 加 20、内容y 加 70、「AI Recommended Configuration:」、UI_强调色、1。
        做 字体_渲染文本_透明 用 fb、内容x 加 40、内容y 加 100、「Kernel Memory: 」 加 做 整数转字符串 用 安装 的 推荐_内存分配 加 「 MB」、UI_正文色、1。
        做 字体_渲染文本_透明 用 fb、内容x 加 40、内容y 加 125、「Swap: 」 加 做 整数转字符串 用 安装 的 推荐_交换分区 加 「 MB」、UI_正文色、1。
        做 字体_渲染文本_透明 用 fb、内容x 加 40、内容y 加 150、「AI Model: 」 加 安装 的 推荐_AI模型、UI_正文色、1。
        做 字体_渲染文本_透明 用 fb、内容x 加 40、内容y 加 175、「Resolution: 」 加 安装 的 推荐_分辨率、UI_正文色、1。
    完。
完。

安装UI_渲染进度条 接受 fb：
    条x 为 40。
    条y 为 屏幕高 减 70。
    条宽 为 屏幕宽 减 80。
    条高 为 24。
    做 像素_填充矩形 用 fb、条x、条y、条宽、条高、UI_边框色。
    总进度 为 (安装 的 当前阶段 乘 1000 加 安装 的 阶段进度) 除以 8。
    填充宽 为 条宽 乘 总进度 除以 1000。
    若 填充宽 大于 0：
        做 像素_填充矩形 用 fb、条x、条y、填充宽、条高、UI_进度条填充。
    完。
    百分比 为 总进度 除以 10。
    做 字体_渲染文本_透明 用 fb、条x 加 条宽 除以 2 减 20、条y 加 4、做 整数转字符串 用 百分比 加 「%」、UI_白色、1。
完。

安装UI_渲染日志 接受 fb：
    日志x 为 屏幕宽 乘 65 除以 100。
    日志y 为 160。
    日志宽 为 屏幕宽 乘 33 除以 100。
    日志高 为 屏幕高 减 260。
    做 像素_填充矩形 用 fb、日志x、日志y、日志宽、日志高、UI_日志背景色。
    做 像素_填充矩形 用 fb、日志x、日志y、日志宽、2、UI_边框色。
    做 字体_渲染文本_透明 用 fb、日志x 加 8、日志y 加 6、「Install Log」、UI_日志标题色、1。
    最大行 为 (日志高 减 30) 除以 18。
    开始 为 做 计数 用 安装 的 详细日志 减 最大行。
    若 开始 小于 0：
        开始 为 0。
    完。
    可变 i 为 开始。
    可变 行y 为 日志y 加 28。
    只要 i 小于 做 计数 用 安装 的 详细日志：
        做 字体_渲染文本_透明 用 fb、日志x 加 8、行y、安装 的 详细日志 的 第 i 项、0x6688AA、1。
        行y 为 行y 加 18。
        i 为 i 加 1。
    完。
完。

UI_背景色 为 0x0A0A1A。

UI_标题栏色 为 0x141428。

UI_强调色 为 0x00AAFF。

UI_成功色 为 0x00FF88。

UI_未达到色 为 0x333355。

UI_标签色 为 0x8888AA。

UI_内容区背景 为 0x12122A。

UI_边框色 为 0x333366。

UI_白色 为 0xFFFFFF。

UI_副标题色 为 0xAABBDD。

UI_提示色 为 0x6688AA。

UI_正文色 为 0xCCDDEE。

UI_进度条填充 为 0x0088CC。

UI_日志背景色 为 0x0E0E22。

UI_日志标题色 为 0x88AACC。

UI_错误色 为 0xFF4444。

UI_警告色 为 0xFFAA00。

安装阶段_欢迎 为 0。

安装阶段_硬件探测 为 1。

安装阶段_AI配置 为 2。

安装阶段_分区 为 3。

安装阶段_部署 为 4。

安装阶段_AI模型 为 5。

安装阶段_设置 为 6。

安装阶段_完成 为 7。

安装状态 是 一种 事物，含 当前阶段、阶段进度、阶段消息、CPU_型号、内存_总MB、图处_型号、磁盘列表、网卡列表、推荐_内存分配、推荐_交换分区、推荐_AI模型、推荐_分辨率、详细日志、是否完成、错误信息。

可变 安装 为 新建 安装状态。

安装_初始化 接受 零参：
    安装 的 当前阶段 为 安装阶段_欢迎。
    安装 的 阶段进度 为 0。
    安装 的 阶段消息 为 「Welcome to NovaOS」。
    安装 的 CPU_型号 为 「Unknown」。
    安装 的 内存_总MB 为 0。
    安装 的 图处_型号 为 「Unknown」。
    安装 的 磁盘列表 为 一组：（空）。
    安装 的 网卡列表 为 一组：（空）。
    安装 的 推荐_内存分配 为 512。
    安装 的 推荐_交换分区 为 1024。
    安装 的 推荐_AI模型 为 「nova-tiny」。
    安装 的 推荐_分辨率 为 「1920x1080」。
    安装 的 详细日志 为 一组：（空）。
    安装 的 是否完成 为 假。
    安装 的 错误信息 为 0。
完。

安装_下一阶段 接受 零参：
    若 安装 的 当前阶段 小于 安装阶段_完成：
        安装 的 当前阶段 为 安装 的 当前阶段 加 1。
        安装 的 阶段进度 为 0。
        做 安装_更新消息。
    完。
完。

安装_更新消息 接受 零参：
    若 安装 的 当前阶段 等于 安装阶段_欢迎：
        安装 的 阶段消息 为 「Welcome to NovaOS」。
    否则若 安装 的 当前阶段 等于 安装阶段_硬件探测：
        安装 的 阶段消息 为 「Detecting Hardware...」。
    否则若 安装 的 当前阶段 等于 安装阶段_AI配置：
        安装 的 阶段消息 为 「AI Configuration」。
    否则若 安装 的 当前阶段 等于 安装阶段_分区：
        安装 的 阶段消息 为 「Disk Partitioning」。
    否则若 安装 的 当前阶段 等于 安装阶段_部署：
        安装 的 阶段消息 为 「Deploying System...」。
    否则若 安装 的 当前阶段 等于 安装阶段_AI模型：
        安装 的 阶段消息 为 「Installing AI Models...」。
    否则若 安装 的 当前阶段 等于 安装阶段_设置：
        安装 的 阶段消息 为 「Final Setup」。
    否则若 安装 的 当前阶段 等于 安装阶段_完成：
        安装 的 阶段消息 为 「Installation Complete!」。
    完。
完。

安装_添加日志 接受 消息：
    做 追加 用 安装 的 详细日志、消息。
完。

安装_设置进度 接受 进度：
    安装 的 阶段进度 为 进度。
    若 安装 的 阶段进度 大于 1000：
        安装 的 阶段进度 为 1000。
    完。
完。

安装_统计 接受 零参：
    返还 一本字典：阶段 对应 安装 的 当前阶段、进度 对应 安装 的 阶段进度、消息 对应 安装 的 阶段消息、日志数 对应 做 计数 用 安装 的 详细日志、完成 对应 安装 的 是否完成。
完。

安装_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 安装 的 错误信息 不等于 0：
        做 追加 用 问题、安装 的 错误信息。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「安装程序健康」。
    完。
    返还 问题。
完。

安装_完整统计 接受 零参：
    返还 一组：安装 的 当前阶段、安装 的 阶段进度、做 计数 用 安装 的 详细日志。
完。

安装_阶段名 接受 阶段：
    若 阶段 等于 安装阶段_欢迎：
        返还 「Welcome」。
    完。
    若 阶段 等于 安装阶段_硬件探测：
        返还 「Hardware」。
    完。
    若 阶段 等于 安装阶段_AI配置：
        返还 「AI Config」。
    完。
    若 阶段 等于 安装阶段_分区：
        返还 「Partition」。
    完。
    若 阶段 等于 安装阶段_部署：
        返还 「Deploy」。
    完。
    若 阶段 等于 安装阶段_AI模型：
        返还 「AI Model」。
    完。
    若 阶段 等于 安装阶段_设置：
        返还 「Setup」。
    完。
    若 阶段 等于 安装阶段_完成：
        返还 「Done」。
    完。
    返还 「Unknown」。
完。

本章 收束。
