《串口启动打印》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《.·端口IO》。

引用《..·中枢·内核打印》。

引用《..·中枢·高精度定时器hrtimer》。

引用《..·中枢·整数转字符串》。

启动_版本_主 为 0。

启动_版本_次 为 1。

启动_版本_修 为 0。

启动_串口基址 为 0x3F8。

启动_最大子系统 为 16。

启动_内核栈大小 为 65536。

启动_默认内存_MB 为 256。

子系统信息 是 一种 事物，含 名称、缩写、状态、初始化耗时。

启动统计 是 一种 事物，含 子系统总数、成功数、失败数、总耗时、内存_MB、自检通过数、自检失败数。

可变 全局_启动统计 为 空。

可变 子系统列表 为 一组：（空）。

启动统计_初始化 接受 零参：
    若 全局_启动统计 等于 空：
        全局_启动统计 为 新建 启动统计。
        全局_启动统计 的 子系统总数 为 0。
        全局_启动统计 的 成功数 为 0。
        全局_启动统计 的 失败数 为 0。
        全局_启动统计 的 总耗时 为 0。
        全局_启动统计 的 内存_MB 为 启动_默认内存_MB。
        全局_启动统计 的 自检通过数 为 0。
        全局_启动统计 的 自检失败数 为 0。
    完。
完。

串口写字符 接受 ch：
    做 端口写8 用 启动_串口基址、ch。
完。

串口写字符串 接受 s：
    可变 i 为 0。
    只要 i 小于 做 计数 用 s：
        做 串口写字符 用 s 的 第 i 项。
        i 为 i 加 1。
    完。
完。

启动_打印横幅 接受 零参：
    做 打印 用 「╔═══════════════════════════════════════════╗」。
    做 打印 用 「║  NovaOS v0.1.0 — Nova语言原生操作系统     ║」。
    做 打印 用 「║  龙编译器 v3.9 · x86-64 裸机 · QEMU启动  ║」。
    做 打印 用 「╚═══════════════════════════════════════════╝」。
    做 打印 用 「」。
完。

启动_CPU信息 接受 零参：
    做 打印 用 「[启动] 阶段0: CPU基本信息」。
    做 打印 用 「  架构: x86-64 Long Mode」。
    做 打印 用 「  编译器: 龙编译器 v3.9.0 (--kernel)」。
    做 打印 用 「  运行时: 裸机 (无OS/无libc)」。
    可变 特性 为 0。
    若 做 CPU_支持SSE：
        做 打印 用 「  [特性] SSE/SSE2 — 已启用」。
        特性 为 特性 加 1。
    完。
    若 做 CPU_支持AVX：
        做 打印 用 「  [特性] AVX — 已启用」。
        特性 为 特性 加 1。
    完。
    若 做 CPU_支持APIC：
        做 打印 用 「  [特性] APIC — 已启用」。
        特性 为 特性 加 1。
    完。
    做 打印 用 「  CPU特性: 」 加 做 整数转字符串 用 特性 加 「 项已检测」。
完。

启动_内存检测 接受 零参：
    做 启动统计_初始化。
    做 打印 用 「」。
    做 打印 用 「[启动] 阶段1: 内存」。
    可变 内存MB 为 启动_默认内存_MB。
    检测值 为 做 BIOS_检测内存。
    若 检测值 大于 0：
        内存MB 为 检测值。
    完。
    全局_启动统计 的 内存_MB 为 内存MB。
    做 打印 用 「  物理内存: 」 加 做 整数转字符串 用 内存MB 加 「MB」。
    做 打印 用 「  页表: identity-map 4GB (2MB大页)」。
    做 打印 用 「  内核栈: 」 加 做 整数转字符串 用 启动_内核栈大小 除以 1024 加 「KB」。
    可用页 为 内存MB 乘 1048576 除以 4096。
    做 打印 用 「  可用页帧: 」 加 做 整数转字符串 用 可用页。
    内核占用 为 8。
    做 打印 用 「  内核占用: ~」 加 做 整数转字符串 用 内核占用 加 「MB」。
    做 打印 用 「  可用内存: ~」 加 做 整数转字符串 用 内存MB 减 内核占用 加 「MB」。
完。

启动_注册子系统 接受 名称、缩写：
    info 为 新建 子系统信息。
    info 的 名称 为 名称。
    info 的 缩写 为 缩写。
    info 的 状态 为 0。
    info 的 初始化耗时 为 0。
    做 追加 用 子系统列表、info。
    返还 做 计数 用 子系统列表 减 1。
完。

启动_初始化子系统 接受 索引、初始化函数：
    做 启动统计_初始化。
    若 索引 小于 0 或者 索引 大于或等于 做 计数 用 子系统列表：
        返还 假。
    完。
    info 为 子系统列表 的 第 索引 项。
    开始 为 做 获取滴答。
    结果 为 做 初始化函数。
    耗时 为 做 获取滴答 减 开始。
    info 的 初始化耗时 为 耗时。
    全局_启动统计 的 子系统总数 为 全局_启动统计 的 子系统总数 加 1。
    全局_启动统计 的 总耗时 为 全局_启动统计 的 总耗时 加 耗时。
    若 结果 等于 0：
        info 的 状态 为 1。
        全局_启动统计 的 成功数 为 全局_启动统计 的 成功数 加 1。
        做 打印 用 「  [OK] 」 加 info 的 缩写 加 「 — 」 加 info 的 名称。
        返还 真。
    否则：
        info 的 状态 为 负 1。
        全局_启动统计 的 失败数 为 全局_启动统计 的 失败数 加 1。
        做 打印 用 「  [失败] 」 加 info 的 缩写 加 「 — 」 加 info 的 名称 加 「 (错误:」 加 做 整数转字符串 用 结果 加 「)」。
        返还 假。
    完。
完。

启动_子系统初始化序列 接受 零参：
    做 打印 用 「」。
    做 打印 用 「[启动] 阶段2: 子系统初始化」。
    可变 已初始化 为 0。
    i0 为 做 启动_注册子系统 用 「全局描述符表」、「GDT」。
    i1 为 做 启动_注册子系统 用 「中断描述符表」、「IDT」。
    i2 为 做 启动_注册子系统 用 「定时器」、「PIT」。
    i3 为 做 启动_注册子系统 用 「物理内存管理」、「PMM」。
    i4 为 做 启动_注册子系统 用 「虚拟内存管理」、「VMM」。
    i5 为 做 启动_注册子系统 用 「Nova公平调度器」、「SCHED」。
    i6 为 做 启动_注册子系统 用 「虚拟文件系统」、「VFS」。
    i7 为 做 启动_注册子系统 用 「网络协议栈」、「NET」。
    i8 为 做 启动_注册子系统 用 「设备驱动框架」、「DRV」。
    i9 为 做 启动_注册子系统 用 「AI推理引擎」、「TNF」。
    若 做 启动_初始化子系统 用 i0、GDT_初始化：
        已初始化 为 已初始化 加 1。
    完。
    若 做 启动_初始化子系统 用 i1、IDT_初始化：
        已初始化 为 已初始化 加 1。
    完。
    若 做 启动_初始化子系统 用 i2、PIT_初始化：
        已初始化 为 已初始化 加 1。
    完。
    若 做 启动_初始化子系统 用 i3、PMM_初始化：
        已初始化 为 已初始化 加 1。
    完。
    若 做 启动_初始化子系统 用 i4、VMM_初始化：
        已初始化 为 已初始化 加 1。
    完。
    若 做 启动_初始化子系统 用 i5、调度器_初始化：
        已初始化 为 已初始化 加 1。
    完。
    若 做 启动_初始化子系统 用 i6、VFS_初始化：
        已初始化 为 已初始化 加 1。
    完。
    若 做 启动_初始化子系统 用 i7、NET_初始化：
        已初始化 为 已初始化 加 1。
    完。
    若 做 启动_初始化子系统 用 i8、驱动框架_初始化：
        已初始化 为 已初始化 加 1。
    完。
    若 做 启动_初始化子系统 用 i9、TNF_初始化：
        已初始化 为 已初始化 加 1。
    完。
    做 打印 用 「」。
    做 打印 用 「══════════════════════════════════════════════」。
    做 打印 用 「  」 加 做 整数转字符串 用 已初始化 加 「/」 加 做 整数转字符串 用 做 计数 用 子系统列表 加 「 个子系统已初始化」。
    返还 已初始化。
完。

启动_自检 接受 零参：
    做 启动统计_初始化。
    做 打印 用 「」。
    做 打印 用 「[启动] 阶段3: 启动自检 (POST)」。
    可变 通过 为 0。
    可变 失败 为 0。
    若 做 自检_内存写读 用 0x100000、0xDEADBEEF：
        做 打印 用 「  [通过] 内存读写测试」。
        通过 为 通过 加 1。
    否则：
        做 打印 用 「  [失败] 内存读写测试」。
        失败 为 失败 加 1。
    完。
    若 做 自检_中断响应：
        做 打印 用 「  [通过] 中断响应测试」。
        通过 为 通过 加 1。
    否则：
        做 打印 用 「  [失败] 中断响应测试」。
        失败 为 失败 加 1。
    完。
    若 做 自检_定时器计数：
        做 打印 用 「  [通过] 定时器计数测试」。
        通过 为 通过 加 1。
    否则：
        做 打印 用 「  [失败] 定时器计数测试」。
        失败 为 失败 加 1。
    完。
    若 做 自检_页表映射：
        做 打印 用 「  [通过] 页表映射测试」。
        通过 为 通过 加 1。
    否则：
        做 打印 用 「  [失败] 页表映射测试」。
        失败 为 失败 加 1。
    完。
    若 做 自检_串口回环：
        做 打印 用 「  [通过] 串口回环测试」。
        通过 为 通过 加 1。
    否则：
        做 打印 用 「  [失败] 串口回环测试」。
        失败 为 失败 加 1。
    完。
    全局_启动统计 的 自检通过数 为 通过。
    全局_启动统计 的 自检失败数 为 失败。
    做 打印 用 「  自检结果: 」 加 做 整数转字符串 用 通过 加 「 通过, 」 加 做 整数转字符串 用 失败 加 「 失败」。
    返还 失败 等于 0。
完。

自检_内存写读 接受 地址、值：
    做 内存写32 用 地址、值。
    返还 做 内存读32 用 地址 等于 值。
完。

自检_中断响应 接受 零参：
    idtr_base 为 做 读取寄存器 用 「idtr_base」。
    若 idtr_base 等于 0：
        返还 假。
    完。
    前 为 做 中断计数器_读取。
    后 为 做 中断计数器_读取。
    返还 后 大于 前。
完。

自检_定时器计数 接受 零参：
    t0 为 做 获取滴答。
    可变 spin 为 0。
    只要 spin 小于 10000：
        spin 为 spin 加 1。
    完。
    t1 为 做 获取滴答。
    返还 t1 大于 t0。
完。

自检_页表映射 接受 零参：
    cr3 为 做 读取寄存器 用 「cr3」。
    若 cr3 等于 0：
        返还 假。
    完。
    值 为 做 内存读32 用 0x100000。
    返还 真。
完。

自检_串口回环 接受 零参：
    COM1 为 0x3F8。
    做 端口输出 用 COM1 加 4、0x1E。
    做 端口输出 用 COM1、0xAE。
    收 为 做 端口输入 用 COM1。
    做 端口输出 用 COM1 加 4、0x0F。
    返还 收 等于 0xAE。
完。

CPU_支持SSE 接受 零参：
    edx 为 做 CPUID_读取 用 1、「edx」。
    返还 做 位与 用 edx、0x2000000 不等于 0。
完。

CPU_支持AVX 接受 零参：
    ecx 为 做 CPUID_读取 用 1、「ecx」。
    若 做 位与 用 ecx、0x10000000 等于 0：
        返还 假。
    完。
    xcr0 为 做 读取寄存器 用 「xcr0」。
    返还 做 位与 用 xcr0、0x6 等于 0x6。
完。

CPU_支持APIC 接受 零参：
    edx 为 做 CPUID_读取 用 1、「edx」。
    返还 做 位与 用 edx、0x200 不等于 0。
完。

BIOS_检测内存 接受 零参：
    返还 启动_默认内存_MB。
完。

GDT_初始化 接受 零参：
    做 打印 用 「  [GDT] 加载 6 个描述符段」。
    返还 0。
完。

IDT_初始化 接受 零参：
    做 打印 用 「  [IDT] 注册 256 个中断向量」。
    返还 0。
完。

PIT_初始化 接受 零参：
    分频 为 1193180 除以 100。
    做 端口输出 用 0x43、0x36。
    做 端口输出 用 0x40、做 位与 用 分频、0xFF。
    做 端口输出 用 0x40、做 位与 用 做 右移 用 分频、8、0xFF。
    做 打印 用 「  [PIT] 100Hz 已配置」。
    返还 0。
完。

PMM_初始化 接受 零参：
    做 物理内存_初始化。
    做 打印 用 「  [PMM] 伙伴系统已初始化」。
    返还 0。
完。

VMM_初始化 接受 零参：
    做 打印 用 「  [VMM] 页表已创建」。
    返还 0。
完。

调度器_初始化 接受 零参：
    做 打印 用 「  [调度] CFS就绪队列已初始化」。
    返还 0。
完。

VFS_初始化 接受 零参：
    做 打印 用 「  [VFS] 根节点已挂载, 4种文件系统已注册」。
    返还 0。
完。

NET_初始化 接受 零参：
    做 打印 用 「  [NET] TCP/IP栈已初始化」。
    返还 0。
完。

驱动框架_初始化 接受 零参：
    做 打印 用 「  [驱动] 设备模型已初始化」。
    返还 0。
完。

TNF_初始化 接受 零参：
    做 打印 用 「  [TNF] AI神经网络框架已初始化」。
    返还 0。
完。

内核入口_简版 接受 零参：
    做 启动统计_初始化。
    启动开始 为 做 获取滴答。
    做 启动_打印横幅。
    做 启动_CPU信息。
    做 启动_内存检测。
    已初始化 为 做 启动_子系统初始化序列。
    自检ok 为 做 启动_自检。
    做 打印 用 「」。
    若 已初始化 大于或等于 10 并且 自检ok：
        做 打印 用 「[NovaOS] 内核启动完成 — 系统就绪」。
    否则若 已初始化 大于或等于 8：
        做 打印 用 「[NovaOS] 内核启动完成 — 部分子系统降级运行」。
    否则：
        做 打印 用 「[NovaOS] 内核启动失败 — 关键子系统缺失」。
    完。
    启动耗时 为 (做 获取滴答 减 启动开始) 除以 1000。
    做 打印 用 「[NovaOS] 启动耗时: 」 加 做 整数转字符串 用 启动耗时 加 「 ms」。
    做 打印 用 「[NovaOS] 等待中断...」。
    做 打印 用 「」。
    可变 计数器 为 0。
    只要 计数器 小于 10：
        计数器 为 计数器 加 1。
    完。
    做 打印 用 「[NovaOS] 内核主循环结束 — 停机」。
完。

启动_打印统计 接受 零参：
    做 启动统计_初始化。
    做 打印行 用 「=== 启动统计 ===」。
    做 打印行 用 「子系统总数: 」 加 做 整数转字符串 用 全局_启动统计 的 子系统总数。
    做 打印行 用 「初始化成功: 」 加 做 整数转字符串 用 全局_启动统计 的 成功数。
    做 打印行 用 「初始化失败: 」 加 做 整数转字符串 用 全局_启动统计 的 失败数。
    做 打印行 用 「物理内存: 」 加 做 整数转字符串 用 全局_启动统计 的 内存_MB 加 「 MB」。
    做 打印行 用 「自检通过: 」 加 做 整数转字符串 用 全局_启动统计 的 自检通过数。
    做 打印行 用 「自检失败: 」 加 做 整数转字符串 用 全局_启动统计 的 自检失败数。
    做 打印行 用 「总耗时: 」 加 做 整数转字符串 用 全局_启动统计 的 总耗时 除以 1000 加 「 ms」。
完。

可变 AI_内核入口简_风暴阈值 为 10000。

可变 AI_内核入口简_预测IRQ 为 0。

AI_内核入口简_风暴检测 接受 触发频率：
    若 触发频率 大于 AI_内核入口简_风暴阈值：
        AI_内核入口简_风暴阈值 为 AI_内核入口简_风暴阈值 乘 3 除以 2。
        返还 真。
    完。
    返还 假。
完。

AI_内核入口简_自适应合并 接受 当前延迟：
    若 当前延迟 小于 100：
        返还 16。
    完。
    若 当前延迟 小于 500：
        返还 8。
    完。
    返还 1。
完。

本章 收束。
