《内核启动阶段》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·日志中枢》。

引用《..·中枢·同步原语》。

引用《..·中枢·进程调度》。

引用《..·中枢·域内均衡》。

引用《..·中枢·中断管理系统》。

引用《..·中枢·内存虚拟》。

引用《..·中枢·安全防护初始化》。

引用《..·感知·样式规则》。

引用《..·外联·网络子系统入口》。

引用《..·稳态·图处显示》。

引用《..·中枢·公共智能》。

引用《..·执行·系统启动》。

引用《.·引导加载器》。

引用《..·执行·内核界面》。

引用《.·子系统注册表》。

引用《.·串口启动打印》。

引用《..·中枢·整数转字符串》。

引用《..·推理·AI总线初始化》。

引用《..·中枢·进程标识分》。

引用《..·中枢·块分配初始化》。

引用《..·中枢·系统调用入口》。

引用《..·感知·Storage存储接口》。

引用《..·中枢·中断请求管理》。

引用《..·稳态·图处识别查询》。

引用《..·稳态·AHCI驱动全局》。

引用《..·感知·事件循环主体》。

引用《..·稳态·音频常量》。

引用《..·中枢·规则匹配》。

引用《..·中枢·软中断初始化》。

引用《..·中枢·虚分配》。

引用《..·中枢·时钟中枢》。

引用《..·记忆·devtmpfs设备》。

引用《..·稳态·AMD_GPU驱动》。

引用《..·中枢·APIC任务优先级》。

引用《..·中枢·内存初始化》。

引用《..·稳态·键盘》。

引用《..·稳态·图处枚举》。

引用《..·稳态·GPU驱动初始化》。

引用《..·中枢·8259PIC中断》。

引用《..·中枢·加密框架》。

引用《..·推理·AI总线桥接》。

引用《.·特权指令》。

引用《..·推理·云端桥接》。

引用《..·稳态·virtio通用》。

引用《..·记忆·sysfs框架》。

引用《..·中枢·APIC初始化》。

引用《..·中枢·墙钟设置》。

引用《..·感知·统一码解码》。

引用《..·稳态·PCI配置读》。

引用《..·稳态·图处内存管理》。

引用《..·中枢·IOAPIC注册》。

引用《..·中枢·运行队列更新》。

引用《.·注册表获取》。

引用《..·中枢·页表重建》。

引用《..·中枢·处理异常处理》。

引用《.·串口早期输》。

阶段_内存 为 2。

阶段_中断 为 3。

阶段_调度 为 4。

阶段_系统调用 为 5。

阶段_文件系统 为 6。

阶段_驱动 为 7。

阶段_网络 为 8。

阶段_安全 为 9。

阶段_AI 为 10。

阶段_渲染 为 11。

阶段_桌面 为 12。

阶段_完成 为 13。

可变 当前启动阶段 为 0。

可变 启动起始时间 为 0。

可变 各阶段耗时 为 一组：（空）。

可变 启动错误数 为 0。

可变 启动警告数 为 0。

阶段耗时记录 是 一种 事物，含 阶段号、名称、开始时间、结束时间、耗时_纳秒、子步骤数、成功。

启动_记录阶段开始 接受 阶段号、名称：
    记录 为 新建 阶段耗时记录：阶段号 为 阶段号、名称 为 名称、开始时间 为 做 当前时间_纳秒、结束时间 为 0、耗时_纳秒 为 0、子步骤数 为 0、成功 为 假。
    做 追加 用 各阶段耗时、记录。
    当前启动阶段 为 阶段号。
    做 早期打印 用 「[启动] ══ 阶段」 加 做 整数转字符串 用 阶段号 加 「: 」 加 名称 加 「 ══」。
完。

启动_记录阶段完成 接受 阶段号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 各阶段耗时：
        若 各阶段耗时 的 第 i 项 的 阶段号 等于 阶段号：
            各阶段耗时 的 第 i 项 的 结束时间 为 做 当前时间_纳秒。
            各阶段耗时 的 第 i 项 的 耗时_纳秒 为 各阶段耗时 的 第 i 项 的 结束时间 减 各阶段耗时 的 第 i 项 的 开始时间。
            各阶段耗时 的 第 i 项 的 成功 为 真。
            做 早期打印OK 用 各阶段耗时 的 第 i 项 的 名称 加 「 (」 加 做 整数转字符串 用 各阶段耗时 的 第 i 项 的 耗时_纳秒 除以 1000000 加 「ms)」。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

启动_检查结果 接受 结果、步骤名：
    若 结果 不等于 成功 并且 结果 不等于 0：
        启动错误数 为 启动错误数 加 1。
        做 早期打印错误 用 「[启动] 」 加 步骤名 加 「 失败! 错误码:」 加 做 整数转字符串 用 结果。
        返还 假。
    完。
    返还 真。
完。

内核启动_阶段2_内存 接受 零参：
    做 启动_记录阶段开始 用 阶段_内存、「内存子系统」。
    启动起始时间 为 做 当前时间_纳秒。
    物理页数 为 做 物理内存_初始化。
    做 早期打印OK 用 「物理内存: 」 加 做 整数转字符串 用 物理页数 乘 4 除以 1024 加 「MB」。
    做 页表_内核重建。
    做 早期打印OK 用 「内核页表重建完成」。
    做 SLUB_初始化。
    做 早期打印OK 用 「SLUB分配器」。
    做 vmalloc_初始化。
    做 早期打印OK 用 「vmalloc区域」。
    做 页缓存_初始化。
    可用MB 为 物理页数 乘 4 除以 1024。
    内核占用MB 为 做 内核_占用页数 乘 4 除以 1024。
    做 早期打印OK 用 「可用内存: 」 加 做 整数转字符串 用 可用MB 加 「MB, 内核: 」 加 做 整数转字符串 用 内核占用MB 加 「MB」。
    做 注册_内存管理器 用 做 获取内存管理器句柄。
    做 启动_记录阶段完成 用 阶段_内存。
完。

内核启动_阶段3_中断 接受 零参：
    做 启动_记录阶段开始 用 阶段_中断、「中断与定时器」。
    做 PIC_禁用。
    做 早期打印OK 用 「8259A PIC已禁用」。
    做 接口C_初始化。
    做 早期打印OK 用 「Local APIC」。
    做 IO接口C_初始化。
    做 早期打印OK 用 「IO APIC」。
    做 IDT_注册异常。
    做 早期打印OK 用 「异常处理 (0-31)」。
    做 接口C_定时器_初始化 用 10。
    做 早期打印OK 用 「LAPIC定时器 (10ms)」。
    做 开中断。
    做 早期打印OK 用 「中断已启用」。
    做 注册_中断控制器 用 做 获取中断控制器句柄。
    做 注册_时间管理器 用 做 获取时间管理器句柄。
    做 启动_记录阶段完成 用 阶段_中断。
完。

内核启动_阶段4_调度 接受 零参：
    做 启动_记录阶段开始 用 阶段_调度、「进程与调度」。
    做 进程表_初始化。
    做 早期打印OK 用 「进程表 (PID 0: idle)」。
    做 全部运行队列_初始化。
    做 早期打印OK 用 「运行队列 (」 加 做 整数转字符串 用 在线CPU数 加 「 CPU)」。
    做 调度域_构建拓扑 用 在线CPU数、2、4。
    做 早期打印OK 用 「调度域拓扑 (SMT→MC→NUMA)」。
    做 调度器_初始化。
    做 早期打印OK 用 「调度器 (Nova公平调度 + RT + DL)」。
    做 软中断_初始化。
    做 早期打印OK 用 「软中断 (10类型)」。
    做 IRQ_初始化。
    做 早期打印OK 用 「IRQ管理 (256向量)」。
    做 时钟源_初始化。
    做 定时器系统_初始化。
    做 高精度定时器_初始化。
    做 早期打印OK 用 「时间子系统 (TSC + hrtimer)」。
    做 负载统计_初始化 用 1。
    做 早期打印OK 用 「负载统计」。
    做 注册_进程调度器 用 做 获取调度器句柄。
    做 启动_记录阶段完成 用 阶段_调度。
完。

内核启动_阶段5_系统调用 接受 零参：
    做 启动_记录阶段开始 用 阶段_系统调用、「系统调用」。
    做 系统调用_注册全部。
    做 早期打印OK 用 「系统调用表 (512项)」。
    做 系统调用入口_初始化。
    做 早期打印OK 用 「syscall入口」。
    做 管道_初始化。
    做 注册_IPC管理器 用 做 获取IPC句柄。
    做 早期打印OK 用 「IPC管道」。
    做 启动_记录阶段完成 用 阶段_系统调用。
完。

内核启动_阶段6_文件系统 接受 零参：
    做 启动_记录阶段开始 用 阶段_文件系统、「文件系统」。
    做 VFS_初始化。
    做 注册_VFS 用 做 获取VFS句柄。
    做 早期打印OK 用 「VFS框架」。
    做 devtmpfs_初始化。
    做 procfs_初始化。
    做 sysfs_初始化。
    做 tmpfs_初始化。
    做 早期打印OK 用 「devtmpfs/procfs/sysfs/tmpfs」。
    根挂载结果 为 做 ext4_挂载根 用 「/dev/sda1」、「/」。
    若 根挂载结果 等于 成功：
        做 注册_根文件系统 用 做 获取根文件系统句柄。
        做 早期打印OK 用 「根文件系统 ext4 (/dev/sda1)」。
    否则：
        做 早期打印警告 用 「根文件系统挂载失败, 使用initramfs」。
        启动警告数 为 启动警告数 加 1。
    完。
    做 键盘_初始化。
    做 IRQ_注册 用 1、「PS/2键盘」、键盘_中断处理。
    做 注册_输入驱动 用 做 获取输入驱动句柄。
    做 早期打印OK 用 「PS/2键盘 (IRQ1)」。
    做 启动_记录阶段完成 用 阶段_文件系统。
完。

内核启动_阶段7_驱动 接受 零参：
    做 启动_记录阶段开始 用 阶段_驱动、「设备驱动」。
    PCI设备数 为 做 PCI_枚举。
    做 早期打印OK 用 「PCI枚举 (」 加 做 整数转字符串 用 PCI设备数 加 「个设备)」。
    磁盘数 为 做 存储_初始化。
    做 注册_存储驱动 用 做 获取存储驱动句柄。
    做 早期打印OK 用 「存储驱动 (」 加 做 整数转字符串 用 磁盘数 加 「个磁盘)」。
    网卡数 为 做 网卡_初始化。
    做 注册_网卡驱动 用 做 获取网卡驱动句柄。
    做 早期打印OK 用 「网卡驱动 (」 加 做 整数转字符串 用 网卡数 加 「个)」。
    做 virtio_初始化 用 0。
    做 早期打印OK 用 「virtio设备」。
    做 图处_枚举。
    若 做 图处_获取主设备 不等于 空：
        做 图处驱动_初始化 用 做 图处_获取主设备。
        做 注册_图处驱动 用 做 获取图处驱动句柄。
    完。
    做 早期打印OK 用 「GPU (」 加 做 整数转字符串 用 做 图处_数量 加 「个)」。
    做 音频_初始化。
    做 早期打印OK 用 「音频驱动」。
    做 启动_记录阶段完成 用 阶段_驱动。
完。

内核启动_阶段8_网络 接受 零参：
    做 启动_记录阶段开始 用 阶段_网络、「网络协议栈」。
    做 套接字_初始化。
    做 早期打印OK 用 「套接字层」。
    做 IP_初始化。
    做 早期打印OK 用 「IPv4/IPv6」。
    做 传控_初始化。
    做 数报_初始化。
    做 注册_传控栈 用 做 获取传控栈句柄。
    做 早期打印OK 用 「TCP/UDP」。
    做 DNS_初始化。
    做 注册_域名解析器 用 做 获取DNS句柄。
    做 早期打印OK 用 「DNS解析器」。
    做 TLS_初始化。
    做 注册_安全引擎 用 做 获取TLS句柄。
    做 早期打印OK 用 「TLS 1.3」。
    做 HTTP2_初始化。
    做 注册_HTTP2引擎 用 做 获取HTTP2句柄。
    做 早期打印OK 用 「HTTP/2」。
    做 防火墙_初始化。
    做 早期打印OK 用 「防火墙」。
    做 启动_记录阶段完成 用 阶段_网络。
完。

内核启动_阶段9_安全 接受 零参：
    做 启动_记录阶段开始 用 阶段_安全、「安全子系统」。
    做 安全防护_初始化。
    做 注册_安全管理器 用 做 获取安全管理器句柄。
    做 早期打印OK 用 「安全防护引擎 (三层防御)」。
    做 加密_初始化。
    做 早期打印OK 用 「加密引擎 (SHA256/AES/RSA)」。
    做 随机数_初始化。
    做 早期打印OK 用 「随机数生成器 (硬件+软件)」。
    做 启动_记录阶段完成 用 阶段_安全。
完。

内核启动_阶段10_AI 接受 零参：
    做 启动_记录阶段开始 用 阶段_AI、「AI智能大脑」。
    做 推理引擎_初始化。
    做 注册_推理引擎 用 做 获取推理引擎句柄。
    做 早期打印OK 用 「推理引擎 (张量/激活/损失/优化器)」。
    做 AI总线_初始化。
    做 注册_AI总线 用 做 获取AI总线句柄。
    做 早期打印OK 用 「AI总线 (模块间智能通信)」。
    做 智能体_初始化。
    做 注册_智能体引擎 用 做 获取智能体句柄。
    做 早期打印OK 用 「智能体引擎 (任务规划/执行)」。
    做 语义分析_初始化。
    做 注册_语义分析器 用 做 获取语义分析句柄。
    做 早期打印OK 用 「语义分析器 (NLU)」。
    做 云端_初始化。
    做 注册_云端桥接 用 做 获取云端句柄。
    做 早期打印OK 用 「云端桥接 (远程模型/联邦学习)」。
    做 启动_记录阶段完成 用 阶段_AI。
完。

内核启动_阶段11_渲染 接受 零参：
    做 启动_记录阶段开始 用 阶段_渲染、「渲染引擎」。
    做 文档解析器_初始化。
    做 注册_文档解析器 用 做 获取文档解析句柄。
    做 早期打印OK 用 「HTML5解析器」。
    做 样式引擎_初始化。
    做 注册_样式引擎 用 做 获取样式引擎句柄。
    做 早期打印OK 用 「CSS3样式引擎」。
    做 布局引擎_初始化。
    做 注册_布局引擎 用 做 获取布局引擎句柄。
    做 早期打印OK 用 「布局引擎 (块/弹性/网格)」。
    做 绘制合成器_初始化。
    做 注册_绘制合成器 用 做 获取绘制合成句柄。
    做 早期打印OK 用 「绘制合成器 (GPU加速)」。
    做 事件循环_初始化。
    做 注册_事件循环 用 做 获取事件循环句柄。
    做 早期打印OK 用 「事件循环」。
    做 Nova画布_初始化。
    做 注册_Nova画布 用 做 获取Nova画布句柄。
    做 字体引擎_初始化。
    做 注册_字体引擎 用 做 获取字体引擎句柄。
    做 早期打印OK 用 「2D画布 + 字体引擎」。
    做 Nova运行时_初始化。
    做 注册_Nova运行时 用 做 获取运行时句柄。
    做 GC_初始化。
    做 注册_GC 用 做 获取GC句柄。
    做 早期打印OK 用 「Nova运行时 + GC」。
    做 启动_记录阶段完成 用 阶段_渲染。
完。

内核启动_阶段12_桌面 接受 零参：
    做 启动_记录阶段开始 用 阶段_桌面、「桌面环境」。
    做 桌面管理器_初始化。
    做 注册_桌面管理器 用 做 获取桌面管理句柄。
    做 早期打印OK 用 「桌面管理器」。
    做 窗口管理器_初始化。
    做 注册_窗口管理器 用 做 获取窗口管理句柄。
    做 早期打印OK 用 「窗口管理器 (合成器)」。
    做 任务栏_初始化。
    做 注册_任务栏 用 做 获取任务栏句柄。
    做 早期打印OK 用 「任务栏」。
    做 通知中心_初始化。
    做 注册_通知中心 用 做 获取通知中心句柄。
    做 早期打印OK 用 「通知中心」。
    做 主题引擎_初始化。
    做 注册_主题引擎 用 做 获取主题引擎句柄。
    做 早期打印OK 用 「主题引擎 (玻璃态)」。
    做 启动_记录阶段完成 用 阶段_桌面。
完。

内核主启动序列 接受 零参：
    启动起始时间 为 做 当前时间_纳秒。
    注册 为 做 注册表_获取。
    注册 的 启动时间 为 启动起始时间。
    做 早期打印 用 「╔══════════════════════════════════════════╗」。
    做 早期打印 用 「║     NovaOS 觉醒系统 — 神经启动序列     ║」。
    做 早期打印 用 「╚══════════════════════════════════════════╝」。
    做 内核启动_阶段2_内存。
    做 内核启动_阶段3_中断。
    做 内核启动_阶段4_调度。
    做 内核启动_阶段5_系统调用。
    做 内核启动_阶段6_文件系统。
    做 内核启动_阶段7_驱动。
    做 内核启动_阶段8_网络。
    做 内核启动_阶段9_安全。
    做 内核启动_阶段10_AI。
    做 内核启动_阶段11_渲染。
    做 内核启动_阶段12_桌面。
    当前启动阶段 为 阶段_完成。
    注册 的 已初始化 为 真。
    注册 的 启动阶段 为 阶段_完成。
    总耗时 为 (做 当前时间_纳秒 减 启动起始时间) 除以 1000000。
    做 早期打印 用 「╔══════════════════════════════════════════╗」。
    做 早期打印 用 「║  觉醒完成! 总耗时: 」 加 做 整数转字符串 用 总耗时 加 「ms」。
    做 早期打印 用 「║  错误: 」 加 做 整数转字符串 用 启动错误数 加 「 警告: 」 加 做 整数转字符串 用 启动警告数。
    做 早期打印 用 「╚══════════════════════════════════════════╝」。
完。

启动_诊断报告 接受 零参：
    返还 一本字典：当前阶段 对应 当前启动阶段、总耗时_纳秒 对应 做 当前时间_纳秒 减 启动起始时间、阶段详情 对应 各阶段耗时、错误数 对应 启动错误数、警告数 对应 启动警告数、注册表完整性 对应 做 启动_验证注册表。
完。

启动_验证注册表 接受 零参：
    注册 为 做 注册表_获取。
    可变 缺失 为 一组：（空）。
    若 注册 的 进程调度器 等于 0：
        做 追加 用 缺失、「进程调度器」。
    完。
    若 注册 的 内存管理器 等于 0：
        做 追加 用 缺失、「内存管理器」。
    完。
    若 注册 的 中断控制器 等于 0：
        做 追加 用 缺失、「中断控制器」。
    完。
    若 注册 的 VFS 等于 0：
        做 追加 用 缺失、「VFS」。
    完。
    若 注册 的 推理引擎 等于 0：
        做 追加 用 缺失、「推理引擎」。
    完。
    若 注册 的 Nova运行时 等于 0：
        做 追加 用 缺失、「Nova运行时」。
    完。
    若 注册 的 桌面管理器 等于 0：
        做 追加 用 缺失、「桌面管理器」。
    完。
    若 做 计数 用 缺失 等于 0：
        返还 「完整」。
    完。
    返还 缺失。
完。

本章 收束。
