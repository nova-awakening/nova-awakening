《玻璃系统栏》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《.·端口IO》。

可变 bar_y 为 0。

只要 bar_y 小于 32：
    可变 bar_c 为 0。
    若 bar_y 等于 0：
        bar_c 为 0x002D3352。
    否则若 bar_y 等于 1：
        bar_c 为 0x00282E48。
    否则若 bar_y 等于 30：
        bar_c 为 0x00121628。
    否则若 bar_y 等于 31：
        bar_c 为 0x001B3060。
    否则：
        bt 为 bar_y 乘 256 除以 32。
        bar_c 为 (24 减 bt 乘 10 除以 256) 乘 65536 加 (28 减 bt 乘 12 除以 256) 乘 256 加 (52 减 bt 乘 20 除以 256)。
    完。
    可变 bar_x 为 0。
    只要 bar_x 小于 屏宽：
        做 内存写32 用 LFB 加 bar_y 乘 行字节 加 bar_x 乘 4、bar_c。
        bar_x 为 bar_x 加 1。
    完。
    bar_y 为 bar_y 加 1。
完。

标题 为 一组：78、111、118、97、32、79、83。

可变 ti 为 0。

只要 ti 小于 7：
    ch_idx 为 (标题 的 第 ti 项 减 32) 乘 8。
    若 ch_idx 大于或等于 0 并且 ch_idx 小于 760：
        可变 行r 为 0。
        只要 行r 小于 8：
            行数据 为 字体 的 第 ch_idx 加 行r 项。
            可变 位r 为 0。
            只要 位r 小于 8：
                若 行数据 右移 7 减 位r 位与 1：
                    做 内存写32 用 LFB 加 (12 加 行r) 乘 行字节 加 (14 加 ti 乘 9 加 位r) 乘 4、0x004FC3F7。
                完。
                位r 为 位r 加 1。
            完。
            行r 为 行r 加 1。
        完。
    完。
    ti 为 ti 加 1。
完。

可变 dot_y 为 0。

只要 dot_y 小于 6：
    可变 dot_x 为 0。
    只要 dot_x 小于 6：
        做 内存写32 用 LFB 加 (13 加 dot_y) 乘 行字节 加 (屏宽 减 178 加 dot_x) 乘 4、0x0022C55E。
        dot_x 为 dot_x 加 1。
    完。
    dot_y 为 dot_y 加 1。
完。

ai文字 为 一组：65、73、32、82、101、97、100、121。

可变 ai_i 为 0。

只要 ai_i 小于 8：
    ai_idx 为 (ai文字 的 第 ai_i 项 减 32) 乘 8。
    若 ai_idx 大于或等于 0 并且 ai_idx 小于 760：
        可变 ai_r 为 0。
        只要 ai_r 小于 8：
            ai_rd 为 字体 的 第 ai_idx 加 ai_r 项。
            可变 ai_b 为 0。
            只要 ai_b 小于 8：
                若 ai_rd 右移 7 减 ai_b 位与 1：
                    做 内存写32 用 LFB 加 (12 加 ai_r) 乘 行字节 加 (屏宽 减 168 加 ai_i 乘 9 加 ai_b) 乘 4、0x0022C55E。
                完。
                ai_b 为 ai_b 加 1。
            完。
            ai_r 为 ai_r 加 1。
        完。
    完。
    ai_i 为 ai_i 加 1。
完。

时间文字 为 一组：48、48、58、53、49。

可变 tm_i 为 0。

只要 tm_i 小于 5：
    tm_idx 为 (时间文字 的 第 tm_i 项 减 32) 乘 8。
    若 tm_idx 大于或等于 0 并且 tm_idx 小于 760：
        可变 tm_r 为 0。
        只要 tm_r 小于 8：
            tm_rd 为 字体 的 第 tm_idx 加 tm_r 项。
            可变 tm_b 为 0。
            只要 tm_b 小于 8：
                若 tm_rd 右移 7 减 tm_b 位与 1：
                    做 内存写32 用 LFB 加 (12 加 tm_r) 乘 行字节 加 (屏宽 减 48 加 tm_i 乘 9 加 tm_b) 乘 4、0x00C8C8D8。
                完。
                tm_b 为 tm_b 加 1。
            完。
            tm_r 为 tm_r 加 1。
        完。
    完。
    tm_i 为 tm_i 加 1。
完。

做 打印 用 「[渲染] 玻璃系统栏 OK」。

任务y 为 屏高 减 32。

可变 tb_y 为 0。

只要 tb_y 小于 32：
    可变 tb_c 为 0。
    若 tb_y 等于 0：
        tb_c 为 0x001B3060。
    否则若 tb_y 等于 1：
        tb_c 为 0x00182844。
    否则：
        tt 为 tb_y 乘 256 除以 32。
        tb_c 为 (14 加 tt 乘 4 除以 256) 乘 65536 加 (18 加 tt 乘 2 除以 256) 乘 256 加 (34 减 tt 乘 6 除以 256)。
    完。
    可变 tb_x 为 0。
    只要 tb_x 小于 屏宽：
        做 内存写32 用 LFB 加 (任务y 加 tb_y) 乘 行字节 加 tb_x 乘 4、tb_c。
        tb_x 为 tb_x 加 1。
    完。
    tb_y 为 tb_y 加 1。
完。

可变 ni 为 0。

只要 ni 小于 28：
    可变 nj 为 2。
    只要 nj 小于 28：
        nb_t 为 nj 乘 256 除以 28。
        nb_c 为 (30 加 (256 减 nb_t) 乘 20 除以 256) 乘 65536 加 (80 加 (256 减 nb_t) 乘 30 除以 256) 乘 256 加 (200 加 (256 减 nb_t) 乘 40 除以 256)。
        做 内存写32 用 LFB 加 (任务y 加 nj) 乘 行字节 加 (4 加 ni) 乘 4、nb_c。
        nj 为 nj 加 1。
    完。
    ni 为 ni 加 1。
完。

n_idx 为 (78 减 32) 乘 8。

可变 n_r 为 0。

只要 n_r 小于 8：
    n_rd 为 字体 的 第 n_idx 加 n_r 项。
    可变 n_b 为 0。
    只要 n_b 小于 8：
        若 n_rd 右移 7 减 n_b 位与 1：
            做 内存写32 用 LFB 加 (任务y 加 10 加 n_r) 乘 行字节 加 (10 加 n_b) 乘 4、0x00FFFFFF。
        完。
        n_b 为 n_b 加 1。
    完。
    n_r 为 n_r 加 1。
完。

本章 收束。
