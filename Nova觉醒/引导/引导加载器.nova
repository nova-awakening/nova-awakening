《引导加载器》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引导档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、描述数、任务数、头项数、镜像数、校验数、加载数、入口数、交接数、日志数、成功数、失败数、最近镜像、最近入口、最近阶段、描述表、任务表、头项表、日志表。

引导请求 是 一种 事物，含 动作、目标、参数、选项。

引导回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

引导摘要 是 一种 事物，含 名称、描述数、任务数、头项数、镜像数、校验数、加载数、入口数、交接数、日志数、成功数、失败数、健康。

描述项 是 一种 事物，含 基址、上限、类型、权限、备注。

启动任务 是 一种 事物，含 名称、阶段、状态、入口、参数。

多引导项 是 一种 事物，含 键、值、来源。

可变 引导账本 为 一组：（空）。

可变 引导构建数 为 0。

可变 引导总动作 为 0。

可变 引导总成功 为 0。

可变 引导总失败 为 0。

引导加载器_建档 接受 名称：
    档 为 新建 引导档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 描述数 为 0。
    档 的 任务数 为 0。
    档 的 头项数 为 0。
    档 的 镜像数 为 0。
    档 的 校验数 为 0。
    档 的 加载数 为 0。
    档 的 入口数 为 0。
    档 的 交接数 为 0。
    档 的 日志数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    档 的 最近镜像 为 「」。
    档 的 最近入口 为 「」。
    档 的 最近阶段 为 「」。
    档 的 描述表 为 一组：（空）。
    档 的 任务表 为 一组：（空）。
    档 的 头项表 为 一组：（空）。
    档 的 日志表 为 一组：（空）。
    返还 档。
完。

引导加载器_登记 接受 档：
    做 追加 用 引导账本、档。
    引导构建数 为 引导构建数 加 1。
    返还 档。
完。

引导加载器_完整统计 接受 零参：
    返还 一组：引导构建数、引导总动作、引导总成功、引导总失败。
完。

引导加载器_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 引导回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

引导加载器_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    引导总动作 为 引导总动作 加 1。
完。

引导加载器_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 入口数 等于 0 或者 档 的 交接数 等于 0：
        档 的 最近健康 为 「预热」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 0：
        档 的 最近健康 为 「监视」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「运行」。
    返还 档 的 最近健康。
完。

引导加载器_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 档 的 名称 等于 「」：
        返还 假。
    完。
    返还 真。
完。

引导加载器_成功 接受 档、来源、数据：
    档 的 最近结果 为 数据。
    档 的 成功数 为 档 的 成功数 加 1。
    引导总成功 为 引导总成功 加 1。
    档 的 最近统计 为 做 引导加载器_完整统计。
    做 引导加载器_刷新健康 用 档。
    返还 做 引导加载器_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

引导加载器_失败 接受 档、来源、错因：
    若 档 不等于 空：
        档 的 最近错误 为 错因。
        档 的 失败数 为 档 的 失败数 加 1。
        档 的 最近统计 为 做 引导加载器_完整统计。
        做 引导加载器_刷新健康 用 档。
    完。
    引导总失败 为 引导总失败 加 1。
    返还 做 引导加载器_建回证 用 真、假、来源、错因、空、做 引导加载器_完整统计。
完。

引导加载器_串口日志 接受 档、文本：
    若 非 做 引导加载器_检查档 用 档：
        返还 做 引导加载器_失败 用 档、「串口日志」、「引导档不可用」。
    完。
    做 引导加载器_记录 用 档、「串口日志」、一组：文本、「引导加载器」。
    做 追加 用 档 的 日志表、文本。
    档 的 日志数 为 档 的 日志数 加 1。
    返还 做 引导加载器_成功 用 档、「串口日志」、文本。
完。

引导加载器_加描述 接受 档、基址、上限、类型、权限、备注：
    若 非 做 引导加载器_检查档 用 档：
        返还 做 引导加载器_失败 用 档、「加描述」、「引导档不可用」。
    完。
    做 引导加载器_记录 用 档、「加描述」、一组：基址、上限、类型、权限、备注、「引导加载器」。
    项 为 新建 描述项。
    项 的 基址 为 基址。
    项 的 上限 为 上限。
    项 的 类型 为 类型。
    项 的 权限 为 权限。
    项 的 备注 为 备注。
    做 追加 用 档 的 描述表、项。
    档 的 描述数 为 档 的 描述数 加 1。
    返还 做 引导加载器_成功 用 档、「加描述」、项。
完。

引导加载器_列描述 接受 档：
    若 非 做 引导加载器_检查档 用 档：
        返还 做 引导加载器_失败 用 档、「列描述」、「引导档不可用」。
    完。
    做 引导加载器_记录 用 档、「列描述」、一组：（空）、「引导加载器」。
    结果 为 一组：（空）。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 描述表：
        项 为 档 的 描述表 的 第 索引 项。
        做 追加 用 结果、一组：项 的 基址、项 的 上限、项 的 类型、项 的 权限、项 的 备注。
        索引 为 索引 加 1。
    完。
    返还 做 引导加载器_成功 用 档、「列描述」、结果。
完。

引导加载器_建任务 接受 档、名称、阶段、入口、参数：
    若 非 做 引导加载器_检查档 用 档：
        返还 做 引导加载器_失败 用 档、「建任务」、「引导档不可用」。
    完。
    做 引导加载器_记录 用 档、「建任务」、一组：名称、阶段、入口、参数、「引导加载器」。
    项 为 新建 启动任务。
    项 的 名称 为 名称。
    项 的 阶段 为 阶段。
    项 的 状态 为 「待运行」。
    项 的 入口 为 入口。
    项 的 参数 为 参数。
    做 追加 用 档 的 任务表、项。
    档 的 任务数 为 档 的 任务数 加 1。
    档 的 最近阶段 为 阶段。
    返还 做 引导加载器_成功 用 档、「建任务」、项。
完。

引导加载器_置任务状态 接受 档、名称、状态：
    若 非 做 引导加载器_检查档 用 档：
        返还 做 引导加载器_失败 用 档、「置任务状态」、「引导档不可用」。
    完。
    做 引导加载器_记录 用 档、「置任务状态」、一组：名称、状态、「引导加载器」。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 任务表：
        项 为 档 的 任务表 的 第 索引 项。
        若 项 的 名称 等于 名称：
            项 的 状态 为 状态。
            档 的 最近阶段 为 项 的 阶段。
            返还 做 引导加载器_成功 用 档、「置任务状态」、项。
        完。
        索引 为 索引 加 1。
    完。
    返还 做 引导加载器_失败 用 档、「置任务状态」、「启动任务不存在」。
完。

引导加载器_加头项 接受 档、键、值、来源：
    若 非 做 引导加载器_检查档 用 档：
        返还 做 引导加载器_失败 用 档、「加头项」、「引导档不可用」。
    完。
    做 引导加载器_记录 用 档、「加头项」、一组：键、值、来源、「引导加载器」。
    项 为 新建 多引导项。
    项 的 键 为 键。
    项 的 值 为 值。
    项 的 来源 为 来源。
    做 追加 用 档 的 头项表、项。
    档 的 头项数 为 档 的 头项数 加 1。
    返还 做 引导加载器_成功 用 档、「加头项」、项。
完。

引导加载器_引导头 接受 档：
    若 非 做 引导加载器_检查档 用 档：
        返还 做 引导加载器_失败 用 档、「引导头」、「引导档不可用」。
    完。
    做 引导加载器_记录 用 档、「引导头」、一组：（空）、「引导加载器」。
    结果 为 一组：（空）。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 头项表：
        项 为 档 的 头项表 的 第 索引 项。
        做 追加 用 结果、一组：项 的 键、项 的 值、项 的 来源。
        索引 为 索引 加 1。
    完。
    返还 做 引导加载器_成功 用 档、「引导头」、结果。
完。

引导加载器_校验镜像 接受 档、镜像名、大小、校验和：
    若 非 做 引导加载器_检查档 用 档：
        返还 做 引导加载器_失败 用 档、「校验镜像」、「引导档不可用」。
    完。
    做 引导加载器_记录 用 档、「校验镜像」、一组：镜像名、大小、校验和、「引导加载器」。
    档 的 镜像数 为 档 的 镜像数 加 1。
    档 的 校验数 为 档 的 校验数 加 1。
    档 的 最近镜像 为 镜像名。
    若 大小 小于或等于 0：
        返还 做 引导加载器_失败 用 档、「校验镜像」、「镜像大小非法」。
    完。
    若 校验和 等于 「」：
        返还 做 引导加载器_失败 用 档、「校验镜像」、「镜像校验为空」。
    完。
    返还 做 引导加载器_成功 用 档、「校验镜像」、一组：镜像名、大小、校验和。
完。

引导加载器_加载镜像 接受 档、镜像名、基址、大小：
    若 非 做 引导加载器_检查档 用 档：
        返还 做 引导加载器_失败 用 档、「加载镜像」、「引导档不可用」。
    完。
    做 引导加载器_记录 用 档、「加载镜像」、一组：镜像名、基址、大小、「引导加载器」。
    档 的 加载数 为 档 的 加载数 加 1。
    档 的 最近镜像 为 镜像名。
    返还 做 引导加载器_成功 用 档、「加载镜像」、一组：镜像名、基址、大小。
完。

引导加载器_置入口 接受 档、入口地址、名称：
    若 非 做 引导加载器_检查档 用 档：
        返还 做 引导加载器_失败 用 档、「置入口」、「引导档不可用」。
    完。
    做 引导加载器_记录 用 档、「置入口」、一组：入口地址、名称、「引导加载器」。
    档 的 入口数 为 档 的 入口数 加 1。
    档 的 最近入口 为 入口地址。
    返还 做 引导加载器_成功 用 档、「置入口」、一组：入口地址、名称。
完。

引导加载器_交接 接受 档、阶段、入口地址、上下文：
    若 非 做 引导加载器_检查档 用 档：
        返还 做 引导加载器_失败 用 档、「交接」、「引导档不可用」。
    完。
    做 引导加载器_记录 用 档、「交接」、一组：阶段、入口地址、上下文、「引导加载器」。
    档 的 交接数 为 档 的 交接数 加 1。
    档 的 最近阶段 为 阶段。
    档 的 最近入口 为 入口地址。
    返还 做 引导加载器_成功 用 档、「交接」、一组：阶段、入口地址、上下文。
完。

引导加载器_初始化 接受 档：
    若 非 做 引导加载器_检查档 用 档：
        返还 做 引导加载器_失败 用 档、「初始化」、「引导档不可用」。
    完。
    做 引导加载器_记录 用 档、「初始化」、一组：（空）、「引导加载器」。
    档 的 描述表 为 一组：（空）。
    档 的 任务表 为 一组：（空）。
    档 的 头项表 为 一组：（空）。
    档 的 日志表 为 一组：（空）。
    档 的 描述数 为 0。
    档 的 任务数 为 0。
    档 的 头项数 为 0。
    档 的 是否初始化 为 真。
    做 引导加载器_串口日志 用 档、「引导加载器开始」。
    做 引导加载器_加描述 用 档、「0x00000000」、「0x000FFFFF」、「代码」、「执行只读」、「实模式与过渡段」。
    做 引导加载器_加描述 用 档、「0x00100000」、「0x00FFFFFF」、「数据」、「读写」、「内核映像窗」。
    做 引导加载器_加头项 用 档、「魔数」、「0x1BADB002」、「初始化」。
    做 引导加载器_加头项 用 档、「页对齐」、「启用」、「初始化」。
    做 引导加载器_加头项 用 档、「内存图」、「启用」、「初始化」。
    做 引导加载器_建任务 用 档、「探测内存」、「早期」、「mem_probe」、一组：「e820」。
    做 引导加载器_建任务 用 档、「加载内核」、「镜像」、「load_kernel」、一组：「kernel.elf」。
    做 引导加载器_建任务 用 档、「切换长模」、「交接」、「jump_long」、一组：「64位」。
    返还 做 引导加载器_成功 用 档、「初始化」、「已初始化」。
完。

引导加载器_摘要 接受 档：
    摘要 为 新建 引导摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 描述数 为 档 的 描述数。
    摘要 的 任务数 为 档 的 任务数。
    摘要 的 头项数 为 档 的 头项数。
    摘要 的 镜像数 为 档 的 镜像数。
    摘要 的 校验数 为 档 的 校验数。
    摘要 的 加载数 为 档 的 加载数。
    摘要 的 入口数 为 档 的 入口数。
    摘要 的 交接数 为 档 的 交接数。
    摘要 的 日志数 为 档 的 日志数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 引导加载器_刷新健康 用 档。
    返还 摘要。
完。

引导加载器_自检 接受 档：
    若 非 做 引导加载器_检查档 用 档：
        返还 假。
    完。
    若 做 引导加载器_初始化 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 引导加载器_校验镜像 用 档、「kernel.elf」、8388608、「sha256:awake」 的 已成 等于 假：
        返还 假。
    完。
    若 做 引导加载器_加载镜像 用 档、「kernel.elf」、「0x00100000」、8388608 的 已成 等于 假：
        返还 假。
    完。
    若 做 引导加载器_置任务状态 用 档、「探测内存」、「完成」 的 已成 等于 假：
        返还 假。
    完。
    若 做 引导加载器_置任务状态 用 档、「加载内核」、「完成」 的 已成 等于 假：
        返还 假。
    完。
    若 做 引导加载器_置入口 用 档、「0x00101000」、「觉醒入口」 的 已成 等于 假：
        返还 假。
    完。
    若 做 引导加载器_交接 用 档、「长模」、「0x00101000」、一组：「页表稳定」、「图形就绪」、「控制面就绪」 的 已成 等于 假：
        返还 假。
    完。
    若 做 引导加载器_串口日志 用 档、「交接完成」 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

引导加载器_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 引导加载器_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        返还 做 引导加载器_初始化 用 档。
    完。
    若 请求 的 动作 等于 「串口日志」：
        返还 做 引导加载器_串口日志 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「加描述」：
        返还 做 引导加载器_加描述 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项、请求 的 参数 的 第 4 项。
    完。
    若 请求 的 动作 等于 「列描述」：
        返还 做 引导加载器_列描述 用 档。
    完。
    若 请求 的 动作 等于 「建任务」：
        返还 做 引导加载器_建任务 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「置任务状态」：
        返还 做 引导加载器_置任务状态 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「加头项」：
        返还 做 引导加载器_加头项 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「引导头」：
        返还 做 引导加载器_引导头 用 档。
    完。
    若 请求 的 动作 等于 「校验镜像」：
        返还 做 引导加载器_校验镜像 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「加载镜像」：
        返还 做 引导加载器_加载镜像 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「置入口」：
        返还 做 引导加载器_置入口 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「交接」：
        返还 做 引导加载器_交接 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「摘要」：
        返还 做 引导加载器_成功 用 档、「摘要」、做 引导加载器_摘要 用 档。
    完。
    返还 做 引导加载器_失败 用 档、「处理」、「未知动作」。
完。

可变 AI_引导加载器_负载窗口 为 一组：（空）。

AI_引导加载器_负载预测 接受 当前负载：
    做 追加 用 AI_引导加载器_负载窗口、当前负载。
    若 做 计数 用 AI_引导加载器_负载窗口 大于 20：
        做 列表_移除 用 AI_引导加载器_负载窗口、0。
    完。
    若 做 计数 用 AI_引导加载器_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_引导加载器_负载窗口。
    趋势 为 AI_引导加载器_负载窗口 的 第 n 减 1 项 减 AI_引导加载器_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_引导加载器_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
