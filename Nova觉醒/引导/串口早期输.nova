《串口早期输》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·字符串替换》。

引用《..·中枢·硬链接》。

引用《.·端口IO》。

引用《..·稳态·串口》。

引用《..·中枢·时钟中枢》。

引用《.·特权指令》。

引用《..·中枢·整数转字符串》。

早期输出_初始化 接受 零参：
    结果 为 做 串口_初始化 用 COM1。
    若 做 是错误 用 结果：
        做 串口_初始化 用 COM2。
    完。
完。

早期打印 接受 消息：
    做 串口_打印行 用 消息。
完。

早期打印数值 接受 标签、数值：
    做 串口_发送字符串 用 标签。
    做 串口_发送字符串 用 「: 」。
    做 串口_打印行 用 做 整数转字符串 用 数值。
完。

早期打印十六进制 接受 标签、数值：
    做 串口_发送字符串 用 标签。
    做 串口_发送字符串 用 「: 」。
    做 串口_打印行 用 做 整数转十六进制 用 数值。
完。

早期打印OK 接受 模块名：
    做 串口_发送字符串 用 「[  OK  ] 」。
    做 串口_打印行 用 模块名。
完。

早期打印失败 接受 模块名：
    做 串口_发送字符串 用 「[失败] 」。
    做 串口_打印行 用 模块名。
完。

早期打印警告 接受 消息：
    做 串口_发送字符串 用 「[警告] 」。
    做 串口_打印行 用 消息。
完。

早期格式化 接受 格式、参数列表：
    可变 结果 为 「」。
    可变 i 为 0。
    可变 参数索引 为 0。
    格式长度 为 做 字符串长度 用 格式。
    只要 i 小于 格式长度：
        ch 为 做 字符串取字符 用 格式、i。
        若 ch 等于 37：
            i 为 i 加 1。
            若 i 大于或等于 格式长度：
                中断。
            完。
            fmt 为 做 字符串取字符 用 格式、i。
            若 fmt 等于 100 并且 参数索引 小于 做 计数 用 参数列表：
                结果 为 结果 加 做 整数转字符串 用 参数列表 的 第 参数索引 项。
                参数索引 为 参数索引 加 1。
            否则若 fmt 等于 120 并且 参数索引 小于 做 计数 用 参数列表：
                结果 为 结果 加 做 整数转十六进制 用 参数列表 的 第 参数索引 项。
                参数索引 为 参数索引 加 1。
            否则若 fmt 等于 115 并且 参数索引 小于 做 计数 用 参数列表：
                结果 为 结果 加 参数列表 的 第 参数索引 项。
                参数索引 为 参数索引 加 1。
            否则若 fmt 等于 37：
                结果 为 结果 加 「%」。
            完。
        否则：
            结果 为 结果 加 做 字符 用 ch。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

早期printf 接受 格式、参数列表：
    做 串口_发送字符串 用 做 早期格式化 用 格式、参数列表。
完。

可变 启动步骤_总数 为 20。

可变 启动步骤_当前 为 0。

可变 启动开始时间 为 0。

早期_启动开始 接受 零参：
    启动步骤_当前 为 0。
    启动开始时间 为 做 读取毫秒。
    做 串口_打印行 用 「NovaOS 启动中...」。
完。

早期_启动步骤 接受 描述：
    启动步骤_当前 为 启动步骤_当前 加 1。
    耗时 为 做 读取毫秒 减 启动开始时间。
    做 串口_发送字符串 用 「[」。
    做 串口_发送字符串 用 做 整数转字符串 用 耗时。
    做 串口_发送字符串 用 「ms] (」。
    做 串口_发送字符串 用 做 整数转字符串 用 启动步骤_当前。
    做 串口_发送字符串 用 「/」。
    做 串口_发送字符串 用 做 整数转字符串 用 启动步骤_总数。
    做 串口_发送字符串 用 「) 」。
    做 串口_打印行 用 描述。
完。

早期_启动完成 接受 零参：
    总耗时 为 做 读取毫秒 减 启动开始时间。
    做 串口_发送字符串 用 「NovaOS 启动完成! 总耗时: 」。
    做 串口_发送字符串 用 做 整数转字符串 用 总耗时。
    做 串口_打印行 用 「ms」。
完。

早期_panic 接受 消息：
    做 串口_打印行 用 「」。
    做 串口_打印行 用 「!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!」。
    做 串口_发送字符串 用 「KERNEL PANIC: 」。
    做 串口_打印行 用 消息。
    做 串口_打印行 用 「!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!」。
    做 串口_打印行 用 「」。
    做 早期打印十六进制 用 「  RSP」、做 读取RSP。
    做 早期打印十六进制 用 「  RIP」、0。
    做 早期打印十六进制 用 「  CR3」、做 读取CR3。
    做 串口_打印行 用 「系统已停机。」。
完。

早期_hex_dump 接受 地址、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        若 i 取余于 16 等于 0：
            若 i 大于 0：
                做 串口_打印行 用 「」。
            完。
            做 串口_发送字符串 用 做 整数转十六进制 用 地址 加 i。
            做 串口_发送字符串 用 「: 」。
        完。
        字节 为 做 内存读8 用 地址 加 i。
        高 为 字节 右移 4 位与 0xF。
        低 为 字节 位与 0xF。
        做 串口_发送字符串 用 做 hex_nibble 用 高。
        做 串口_发送字符串 用 做 hex_nibble 用 低。
        做 串口_发送字符串 用 「 」。
        i 为 i 加 1。
    完。
    做 串口_打印行 用 「」。
完。

hex_nibble 接受 值：
    若 值 小于 10：
        返还 做 整数转字符串 用 值。
    完。
    若 值 等于 10：
        返还 「a」。
    完。
    若 值 等于 11：
        返还 「b」。
    完。
    若 值 等于 12：
        返还 「c」。
    完。
    若 值 等于 13：
        返还 「d」。
    完。
    若 值 等于 14：
        返还 「e」。
    完。
    返还 「f」。
完。

显基_基地址 为 0xB8000。

显基_宽度 为 80。

显基_高度 为 25。

可变 显基_光标_行 为 0。

可变 显基_光标_列 为 0。

显基_白底黑字 为 0x0F。

显基_红底白字 为 0x4F。

早期_显基_清屏 接受 零参：
    可变 i 为 0。
    只要 i 小于 显基_宽度 乘 显基_高度：
        做 内存写16 用 显基_基地址 加 i 乘 2、0x0F20。
        i 为 i 加 1。
    完。
    显基_光标_行 为 0。
    显基_光标_列 为 0。
完。

早期_显基_写字符 接受 字符、颜色：
    若 字符 等于 10：
        显基_光标_列 为 0。
        显基_光标_行 为 显基_光标_行 加 1。
        若 显基_光标_行 大于或等于 显基_高度：
            做 早期_显基_滚屏。
        完。
        返还。
    完。
    偏移 为 (显基_光标_行 乘 显基_宽度 加 显基_光标_列) 乘 2。
    做 内存写8 用 显基_基地址 加 偏移、字符。
    做 内存写8 用 显基_基地址 加 偏移 加 1、颜色。
    显基_光标_列 为 显基_光标_列 加 1。
    若 显基_光标_列 大于或等于 显基_宽度：
        显基_光标_列 为 0。
        显基_光标_行 为 显基_光标_行 加 1。
        若 显基_光标_行 大于或等于 显基_高度：
            做 早期_显基_滚屏。
        完。
    完。
完。

早期_显基_写字符串 接受 字符串、颜色：
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 字符串：
        做 早期_显基_写字符 用 做 字符串取字符 用 字符串、i、颜色。
        i 为 i 加 1。
    完。
完。

早期_显基_滚屏 接受 零参：
    做 内存拷贝 用 显基_基地址、显基_基地址 加 显基_宽度 乘 2、显基_宽度 乘 (显基_高度 减 1) 乘 2。
    可变 i 为 0。
    只要 i 小于 显基_宽度：
        做 内存写16 用 显基_基地址 加 (显基_高度 减 1) 乘 显基_宽度 乘 2 加 i 乘 2、0x0F20。
        i 为 i 加 1。
    完。
    显基_光标_行 为 显基_高度 减 1。
完。

启动_进度百分比 接受 零参：
    若 启动步骤_总数 等于 0：
        返还 0。
    完。
    返还 启动步骤_当前 乘 100 除以 启动步骤_总数。
完。

显基_光标位置 接受 零参：
    返还 一组：显基_光标_行、显基_光标_列。
完。

串口早期_完整统计 接受 零参：
    返还 一组：串口_输出字节数。
完。

可变 AI_串口早期输_操作热度 为 0。

AI_串口早期输_最优算法 接受 数据量、已排序：
    AI_串口早期输_操作热度 为 AI_串口早期输_操作热度 加 1。
    若 数据量 小于 16：
        返还 「插入排序」。
    完。
    若 已排序：
        返还 「二分查找」。
    完。
    若 数据量 小于 10000：
        返还 「快速排序」。
    完。
    返还 「归并排序」。
完。

本章 收束。
