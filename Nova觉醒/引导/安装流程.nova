《安装流程》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·时钟中枢》。

安装阶段_欢迎 为 0。

安装阶段_硬件探测 为 1。

安装阶段_AI配置 为 2。

安装阶段_磁盘分区 为 3。

安装阶段_系统部署 为 4。

安装阶段_引导安装 为 5。

安装阶段_用户配置 为 6。

安装阶段_完成 为 7。

日志_信息 为 0。

日志_警告 为 1。

日志_错误 为 2。

安装状态 是 一种 事物，含 当前阶段、总进度、阶段进度、详细日志、错误数、警告数、开始时间、语言、时区、用户名、主机名、阶段消息、检测到硬件、分区方案、已安装包数、总包数、可回滚。

可变 安装 为 空。

安装_开始 接受 零参：
    安装 为 新建 安装状态：当前阶段 为 安装阶段_欢迎、总进度 为 0、阶段进度 为 0、详细日志 为 一组：（空）、错误数 为 0、警告数 为 0、开始时间 为 做 读取毫秒、语言 为 「zh-CN」、时区 为 「Asia/Shanghai」、用户名 为 「nova」、主机名 为 「novaos」、阶段消息 为 「欢迎安装 NovaOS」、检测到硬件 为 一组：（空）、分区方案 为 一组：（空）、已安装包数 为 0、总包数 为 0、可回滚 为 真。
    做 安装_日志 用 「NovaOS AI-Native OS Installer v1.0」、日志_信息。
    做 安装_日志 用 「Starting installation at 」 加 做 整数转字符串 用 安装 的 开始时间、日志_信息。
    做 内核信息 用 「安装器: 启动」。
完。

安装_下一阶段 接受 零参：
    若 安装 的 当前阶段 大于或等于 安装阶段_完成：
        返还。
    完。
    安装 的 当前阶段 为 安装 的 当前阶段 加 1。
    安装 的 阶段进度 为 0。
    若 安装 的 当前阶段 等于 安装阶段_硬件探测：
        做 安装_硬件探测。
    完。
    做 否则如果 用 安装 的 当前阶段 等于 安装阶段_AI配置。
    块：
        做 安装_AI智能配置。
    完。
    做 否则如果 用 安装 的 当前阶段 等于 安装阶段_磁盘分区。
    块：
        做 安装_自动分区。
    完。
    做 否则如果 用 安装 的 当前阶段 等于 安装阶段_系统部署。
    块：
        做 安装_部署系统。
    完。
    做 否则如果 用 安装 的 当前阶段 等于 安装阶段_引导安装。
    块：
        做 安装_安装引导。
    完。
    做 否则如果 用 安装 的 当前阶段 等于 安装阶段_用户配置。
    块：
        做 安装_配置用户。
    完。
    做 否则如果 用 安装 的 当前阶段 等于 安装阶段_完成。
    块：
        做 安装_完成。
    完。
    做 安装_更新总进度。
完。

安装_硬件探测 接受 零参：
    安装 的 阶段消息 为 「正在探测硬件...」。
    做 安装_日志 用 「开始硬件探测」、日志_信息。
    做 追加 用 安装 的 检测到硬件、一组：「CPU」、「检测中...」。
    安装 的 阶段进度 为 20。
    做 追加 用 安装 的 检测到硬件、一组：「内存」、「检测中...」。
    安装 的 阶段进度 为 40。
    做 追加 用 安装 的 检测到硬件、一组：「磁盘」、「检测中...」。
    安装 的 阶段进度 为 60。
    做 追加 用 安装 的 检测到硬件、一组：「显卡」、「检测中...」。
    安装 的 阶段进度 为 80。
    做 追加 用 安装 的 检测到硬件、一组：「网络」、「检测中...」。
    安装 的 阶段进度 为 100。
    做 安装_日志 用 「硬件探测完成, 发现 」 加 做 整数转字符串 用 做 计数 用 安装 的 检测到硬件 加 「 类设备」、日志_信息。
完。

安装_AI智能配置 接受 零参：
    安装 的 阶段消息 为 「AI智能配置中...」。
    做 安装_日志 用 「AI引擎分析硬件环境」、日志_信息。
    安装 的 阶段进度 为 30。
    做 安装_日志 用 「推荐分区方案: GPT + EFI」、日志_信息。
    安装 的 阶段进度 为 60。
    做 安装_日志 用 「推荐桌面环境: NovaDE」、日志_信息。
    安装 的 阶段进度 为 100。
完。

安装_自动分区 接受 零参：
    安装 的 阶段消息 为 「正在分区...」。
    做 安装_日志 用 「开始自动分区」、日志_信息。
    安装 的 分区方案 为 一组：（空）。
    做 追加 用 安装 的 分区方案、一组：「EFI」、512、「FAT32」。
    安装 的 阶段进度 为 25。
    做 追加 用 安装 的 分区方案、一组：「/」、30720、「ext4」。
    安装 的 阶段进度 为 50。
    做 追加 用 安装 的 分区方案、一组：「/home」、0、「ext4」。
    安装 的 阶段进度 为 75。
    做 追加 用 安装 的 分区方案、一组：「swap」、4096、「swap」。
    安装 的 阶段进度 为 100。
    做 安装_日志 用 「分区完成: 」 加 做 整数转字符串 用 做 计数 用 安装 的 分区方案 加 「 个分区」、日志_信息。
完。

安装_部署系统 接受 零参：
    安装 的 阶段消息 为 「正在部署系统文件...」。
    做 安装_日志 用 「开始系统部署」、日志_信息。
    安装 的 总包数 为 500。
    安装 的 已安装包数 为 0。
    做 安装_安装包组 用 「核心系统」、50。
    做 安装_安装包组 用 「标准库」、80。
    做 安装_安装包组 用 「驱动程序」、100。
    做 安装_安装包组 用 「AI引擎」、120。
    做 安装_安装包组 用 「桌面环境」、100。
    做 安装_安装包组 用 「系统工具」、50。
    安装 的 阶段进度 为 100。
    做 安装_日志 用 「系统部署完成: 」 加 做 整数转字符串 用 安装 的 已安装包数 加 「 个包」、日志_信息。
完。

安装_安装包组 接受 组名、包数：
    做 安装_日志 用 「安装包组: 」 加 组名 加 「 (」 加 做 整数转字符串 用 包数 加 「个包)」、日志_信息。
    安装 的 已安装包数 为 安装 的 已安装包数 加 包数。
    安装 的 阶段进度 为 安装 的 已安装包数 乘 100 除以 安装 的 总包数。
完。

安装_安装引导 接受 零参：
    安装 的 阶段消息 为 「正在安装引导加载器...」。
    做 安装_日志 用 「安装GRUB/Nova引导器」、日志_信息。
    安装 的 阶段进度 为 50。
    做 安装_日志 用 「生成引导配置」、日志_信息。
    安装 的 阶段进度 为 100。
完。

安装_配置用户 接受 零参：
    安装 的 阶段消息 为 「正在配置用户...」。
    做 安装_日志 用 「创建用户: 」 加 安装 的 用户名、日志_信息。
    安装 的 阶段进度 为 30。
    做 安装_日志 用 「设置主机名: 」 加 安装 的 主机名、日志_信息。
    安装 的 阶段进度 为 50。
    做 安装_日志 用 「配置时区: 」 加 安装 的 时区、日志_信息。
    安装 的 阶段进度 为 70。
    做 安装_日志 用 「配置语言: 」 加 安装 的 语言、日志_信息。
    安装 的 阶段进度 为 100。
完。

安装_完成 接受 零参：
    耗时 为 (做 读取毫秒 减 安装 的 开始时间) 除以 1000。
    安装 的 阶段消息 为 「安装完成! 耗时」 加 做 整数转字符串 用 耗时 加 「秒」。
    做 安装_日志 用 「═══ 安装完成! ═══」、日志_信息。
    做 安装_日志 用 「耗时: 」 加 做 整数转字符串 用 耗时 加 「 秒」、日志_信息。
    做 安装_日志 用 「已安装: 」 加 做 整数转字符串 用 安装 的 已安装包数 加 「 个包」、日志_信息。
    做 安装_日志 用 「错误: 」 加 做 整数转字符串 用 安装 的 错误数、日志_信息。
    做 安装_日志 用 「警告: 」 加 做 整数转字符串 用 安装 的 警告数、日志_信息。
    做 安装_日志 用 「请重启进入NovaOS」、日志_信息。
    安装 的 总进度 为 100。
完。

安装_更新总进度 接受 零参：
    安装 的 总进度 为 安装 的 当前阶段 乘 100 除以 安装阶段_完成。
完。

安装_日志 接受 消息、级别：
    前缀 为 「INFO」。
    若 级别 等于 日志_警告：
        前缀 为 「WARN」。
        安装 的 警告数 为 安装 的 警告数 加 1。
    完。
    若 级别 等于 日志_错误：
        前缀 为 「ERROR」。
        安装 的 错误数 为 安装 的 错误数 加 1。
    完。
    做 追加 用 安装 的 详细日志、「[」 加 前缀 加 「] 」 加 消息。
    做 内核信息 用 「安装器: 」 加 消息。
完。

安装_回滚 接受 零参：
    若 非 安装 的 可回滚：
        做 安装_日志 用 「无法回滚」、日志_错误。
        返还。
    完。
    做 安装_日志 用 「开始回滚安装」、日志_警告。
    安装 的 当前阶段 为 安装阶段_欢迎。
    安装 的 阶段进度 为 0。
    安装 的 总进度 为 0。
完。

安装_设置语言 接受 语言：
    安装 的 语言 为 语言。
完。

安装_设置时区 接受 时区：
    安装 的 时区 为 时区。
完。

安装_设置用户名 接受 名：
    安装 的 用户名 为 名。
完。

安装_设置主机名 接受 名：
    安装 的 主机名 为 名。
完。

安装_阶段名称 接受 阶段：
    若 阶段 等于 安装阶段_欢迎：
        返还 「欢迎」。
    完。
    若 阶段 等于 安装阶段_硬件探测：
        返还 「硬件探测」。
    完。
    若 阶段 等于 安装阶段_AI配置：
        返还 「AI配置」。
    完。
    若 阶段 等于 安装阶段_磁盘分区：
        返还 「磁盘分区」。
    完。
    若 阶段 等于 安装阶段_系统部署：
        返还 「系统部署」。
    完。
    若 阶段 等于 安装阶段_引导安装：
        返还 「引导安装」。
    完。
    若 阶段 等于 安装阶段_用户配置：
        返还 「用户配置」。
    完。
    若 阶段 等于 安装阶段_完成：
        返还 「完成」。
    完。
    返还 「未知」。
完。

安装_验证 接受 零参：
    可变 问题 为 一组：（空）。
    若 安装 的 用户名 等于 「」：
        做 追加 用 问题、「未设置用户名」。
    完。
    若 做 计数 用 安装 的 分区方案 等于 0：
        做 追加 用 问题、「未设置分区」。
    完。
    若 安装 的 已安装包数 等于 0：
        做 追加 用 问题、「未安装系统包」。
    完。
    返还 问题。
完。

安装器_完整统计 接受 零参：
    返还 一组：安装 的 当前阶段、安装 的 总进度、安装 的 错误数、安装 的 警告数、做 计数 用 安装 的 详细日志、安装 的 已安装包数。
完。

本章 收束。
