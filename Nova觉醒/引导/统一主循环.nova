《统一主循环》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·神经中枢》。

引用《..·执行·内核界面》。

引用《..·中枢·检查点系统》。

引用《..·推理·TNF视觉识别》。

引用《..·中枢·语音识别》。

引用《..·外联·外联总控核心》。

引用《..·稳态·音频常量》。

引用《..·稳态·图处显示》。

引用《..·中枢·主循环》。

引用《..·感知·样式动画》。

引用《..·中枢·中断管理系统》。

引用《..·执行·内核适配》。

引用《..·稳态·PCM音频流》。

引用《..·中枢·控制通道》。

引用《..·中枢·硬链接》。

引用《..·中枢·三级回滚执》。

引用《..·中枢·内核打印》。

引用《..·执行·浏览器系统》。

引用《..·推理·全息虚拟人》。

引用《..·中枢·全息AI助手状态机》。

引用《..·中枢·模块热加载》。

引用《..·执行·视觉识别引擎》。

引用《..·中枢·整数转字符串》。

引用《..·推理·AI总线桥接》。

引用《..·中枢·骨骼动画》。

引用《..·执行·IO多路复用》。

引用《.·端口IO》。

引用《.·注册表获取》。

引用《..·执行·听觉识别引擎》。

引用《..·中枢·创建检查点》。

引用《..·中枢·字符串替换》。

引用《..·推理·知识蒸馏》。

引用《..·推理·模型同步》。

引用《..·推理·云端桥接》。

引用《..·推理·通信渠道》。

引用《..·推理·会话存储》。

可变 _多模态_帧计数 为 0。

可变 _系统健康_状态 为 「正常」。

可变 _watchdog_下次tick_ns 为 0。

可变 _听觉_录音流就绪 为 假。

可变 _听觉_采样缓冲 为 0。

_听觉_采样帧数 为 256。

可变 _听觉_最近事件 为 「待机」。

可变 _视觉_最近截图哈希 为 0。

可变 _视觉_最近元素数 为 0。

NovaOS_进入统一主循环 接受 reg：
    做 NovaOS_主循环 用 reg。
完。

系统_watchdog_推进 接受 当前时间_ns：
    若 非 watchdog_已启用：
        返还。
    完。
    若 接口C_定时器_已启动：
        返还。
    完。
    若 _watchdog_下次tick_ns 小于或等于 0：
        _watchdog_下次tick_ns 为 当前时间_ns 加 10000000。
        返还。
    完。
    只要 当前时间_ns 大于或等于 _watchdog_下次tick_ns：
        做 watchdog_tick。
        _watchdog_下次tick_ns 为 _watchdog_下次tick_ns 加 10000000。
    完。
完。

多模态_获取默认录音流 接受 零参：
    若 做 计数 用 声卡列表 等于 0：
        返还 空。
    完。
    若 做 计数 用 声卡列表 的 第 0 项 的 PCM列表 等于 0：
        返还 空。
    完。
    返还 声卡列表 的 第 0 项 的 PCM列表 的 第 0 项 的 录音。
完。

多模态_初始化默认录音流 接受 零参：
    若 非 音频_已初始化：
        做 音频_初始化。
    完。
    录音流 为 做 多模态_获取默认录音流。
    若 录音流 等于 空：
        返还 假。
    完。
    若 录音流 的 状态 不等于 SND_PCM_STATE_RUNNING：
        若 做 PCM_设置参数 用 录音流、SND_PCM_FORMAT_S16_LE、SND_RATE_44100、1 小于 0：
            返还 假。
        完。
        若 做 PCM_准备 用 录音流 小于 0：
            返还 假。
        完。
        若 做 PCM_启动 用 录音流 小于 0：
            返还 假。
        完。
    完。
    若 _听觉_采样缓冲 等于 0：
        _听觉_采样缓冲 为 做 堆_分配 用 4096。
    完。
    _听觉_录音流就绪 为 _听觉_采样缓冲 不等于 0。
    返还 _听觉_录音流就绪。
完。

多模态_截断文本 接受 文本、最大长度：
    若 文本 等于 空：
        返还 「」。
    完。
    若 做 字符串长度 用 文本 小于或等于 最大长度：
        返还 文本。
    完。
    若 最大长度 小于或等于 3：
        返还 做 字符串子串 用 文本、0、最大长度。
    完。
    返还 做 字符串子串 用 文本、0、最大长度 减 3 加 「...」。
完。

多模态_图处模式文本 接受 零参：
    若 非 图处_已初始化 或者 图处_上下文 等于 空：
        返还 「图处-offline」。
    完。
    若 图处_上下文 的 图处模式 大于或等于 2：
        返还 「图处-3D」。
    完。
    若 图处_上下文 的 图处模式 大于或等于 1：
        返还 「图处-2D」。
    完。
    返还 「CPU-composite」。
完。

多模态_助手状态文本 接受 状态：
    若 状态 等于 助手状态_待命：
        返还 「待命」。
    完。
    若 状态 等于 助手状态_聆听：
        返还 「聆听中」。
    完。
    若 状态 等于 助手状态_思考：
        返还 「思考中」。
    完。
    若 状态 等于 助手状态_说话：
        返还 「说话中」。
    完。
    若 状态 等于 助手状态_执行：
        返还 「执行中」。
    完。
    若 状态 等于 助手状态_休眠：
        返还 「休眠」。
    完。
    若 状态 等于 助手状态_视频：
        返还 「视频中」。
    完。
    返还 「在线」。
完。

多模态_同步桌面状态 接受 零参：
    可变 核心状态 为 「在线」。
    可变 核心副文本 为 「browser-终端 单路径运行 · 」 加 做 多模态_图处模式文本。
    若 非 图处_已初始化 或者 图处_上下文 等于 空：
        核心状态 为 「预热」。
        核心副文本 为 「图处 图形前台尚未绑定到 browser-终端」。
    否则若 _系统健康_状态 不等于 「正常」：
        核心状态 为 _系统健康_状态。
        核心副文本 为 「统一主循环仍在线 · 默认录音流暂未就绪」。
    完。
    可变 视觉状态 为 「待机」。
    可变 视觉副文本 为 「等待视觉初始化」。
    若 _视觉_已初始化：
        若 非 图处_已初始化 或者 图处_上下文 等于 空 或者 图处_上下文 的 后缓冲 等于 0：
            视觉状态 为 「预热」。
            视觉副文本 为 「图处 截图上下文未绑定」。
        否则若 图处_上下文 的 帧计数 小于或等于 0：
            视觉状态 为 「预热」。
            视觉副文本 为 「等待首帧合成后进入视觉扫描」。
        否则若 _视觉_最近截图哈希 不等于 0：
            视觉状态 为 「在线」。
            视觉副文本 为 「最近识别 」 加 做 整数转字符串 用 _视觉_最近元素数 加 「 个区域 · 总扫描 」 加 做 整数转字符串 用 视觉_总识别次数 加 「 次」。
        否则：
            视觉状态 为 「在线」。
            视觉副文本 为 「图处 后缓冲已接入 · 等待首轮视觉结果」。
        完。
    完。
    可变 声音状态 为 「待机」。
    可变 声音副文本 为 「语音前台未激活」。
    若 _听觉_已初始化：
        若 非 _听觉_录音流就绪：
            声音状态 为 「降级」。
            声音副文本 为 「默认录音流未就绪」。
        否则若 全局TTS 不等于 空 并且 全局TTS 的 正在播放：
            声音状态 为 「说话中」。
            声音副文本 为 做 多模态_截断文本 用 全局TTS 的 当前文本、28。
        否则若 全局ASR 不等于 空 并且 全局ASR 的 正在录音：
            声音状态 为 「聆听中」。
            声音副文本 为 「录音流在线 · 最近事件 」 加 _听觉_最近事件。
        否则：
            声音状态 为 「在线」。
            声音副文本 为 「最近听觉事件 」 加 _听觉_最近事件。
        完。
    完。
    可变 形象状态 为 「待机」。
    可变 形象副文本 为 「形象前台未激活」。
    若 主助手 不等于 空：
        形象状态 为 做 多模态_助手状态文本 用 主助手 的 状态。
        形象副文本 为 「交互 」 加 做 整数转字符串 用 主助手 的 交互次数 加 「 次 · 表情 」 加 做 整数转字符串 用 主助手 的 当前表情。
        若 主助手 的 状态 等于 助手状态_说话：
            形象副文本 为 做 多模态_截断文本 用 主助手 的 说话文本、28。
        否则若 主助手 的 状态 等于 助手状态_聆听：
            形象副文本 为 「唤醒词 」 加 主助手 的 唤醒词 加 「 已接管前台」。
        完。
    完。
    做 浏览器系统会话_同步系统状态 用 核心状态、核心副文本、视觉状态、视觉副文本、声音状态、声音副文本、形象状态、形象副文本。
完。

NovaOS_主循环 接受 reg：
    帧间隔 为 1000000000 除以 60。
    做 AI控制_初始化。
    只要 真：
        帧开始 为 做 平台_获取单调时间_ns。
        做 系统_watchdog_推进 用 帧开始。
        _多模态_帧计数 为 _多模态_帧计数 加 1。
        做 中断系统_处理 用 reg 的 中断控制器。
        做 浏览器系统会话_轮询输入。
        做 AI控制_tick。
        做 事件循环_tick 用 reg 的 事件循环。
        做 AI总线_tick 用 reg 的 AI总线。
        做 云端桥接_tick 用 reg 的 云端桥接。
        若 _多模态_帧计数 取余于 3 等于 0：
            默认渠道档 为 做 智能渠道_获取默认档。
            若 默认渠道档 不等于 0：
                做 智能渠道_轮询 用 默认渠道档。
            完。
        完。
        若 _多模态_帧计数 取余于 30 等于 0：
            若 非 做 训练统计_检查收敛：
                做 蒸馏_批量 用 空、4。
            完。
        完。
        若 _多模态_帧计数 取余于 18000 等于 0 并且 _多模态_帧计数 大于 0：
            做 会话存储_清理过期。
            若 做 训练统计_检查收敛 并且 做 智能_进化阶段 小于 3：
                做 智能_推进进化 用 1000。
            完。
        完。
        若 _多模态_帧计数 取余于 6 等于 0：
            做 多模态_感知_tick。
        完。
        若 _多模态_帧计数 取余于 10 等于 0：
            人 为 做 获取全局虚拟人。
            若 人 不等于 空：
                做 虚拟人_更新表情 用 人。
            完。
        完。
        做 突触_传播 用 0、500。
        若 _多模态_帧计数 取余于 2 等于 0：
            做 NE_更新 用 0、500。
            做 ACh_更新 用 0。
        完。
        若 _多模态_帧计数 取余于 3 等于 0：
            做 认知_周期 用 0、0。
        完。
        若 _多模态_帧计数 取余于 5 等于 0：
            做 稳态_tick。
        完。
        若 _多模态_帧计数 取余于 10 等于 0：
            做 突触_衰减。
            做 STDP_更新 用 500、_多模态_帧计数 减 1、_多模态_帧计数、10、12、20、1。
        完。
        若 _多模态_帧计数 取余于 15 等于 0：
            做 DA_更新 用 累计奖励 减 累计惩罚。
            做 血清素_更新 用 累计奖励 减 累计惩罚、_多模态_帧计数 取余于 100。
        完。
        做 自学习_tick。
        做 睡眠_tick。
        做 防阻塞_tick。
        做 watchdog_喂狗。
        若 _多模态_帧计数 取余于 300 等于 0：
            做 热更新_检查待更新。
            做 模型同步_定期。
        完。
        若 _多模态_帧计数 取余于 1800 等于 0 并且 _多模态_帧计数 大于 0：
            做 自动化_tick。
            做 策略梯度_更新 用 0。
            做 递质_衰减。
            做 神经中枢_脑区健康检查。
        完。
        若 _多模态_帧计数 取余于 3600 等于 0 并且 _多模态_帧计数 大于 0：
            做 系统_自动检查点。
        完。
        做 多模态_同步桌面状态。
        做 浏览器系统会话_渲染帧。
        帧耗时 为 做 平台_获取单调时间_ns 减 帧开始。
        若 帧耗时 小于 帧间隔：
            做 平台_睡眠_ns 用 帧间隔 减 帧耗时。
        完。
    完。
完。

多模态_感知_tick 接受 零参：
    做 视觉_轮询。
    做 听觉_轮询。
    做 语音合成_轮询。
完。

可变 _待更新模块队列 为 一组：（空）。

热更新_提交 接受 模块名、新版本、新代码哈希：
    做 追加 用 _待更新模块队列、一组：模块名、新版本、新代码哈希。
完。

热更新_检查待更新 接受 零参：
    只要 做 计数 用 _待更新模块队列 大于 0：
        请求 为 _待更新模块队列 的 第 0 项。
        做 删除 用 _待更新模块队列、0。
        模块名 为 请求 的 第 0 项。
        新版本 为 请求 的 第 1 项。
        新代码哈希 为 请求 的 第 2 项。
        cp 为 做 检查点_创建 用 「热更新前_」 加 模块名。
        若 cp 不等于 空：
            做 回滚_注册检查点 用 cp 的 检查点ID。
        完。
        模块 为 做 热更新_按名查找 用 模块名。
        若 模块 等于 空：
            做 内核警告 用 「热更新失败: 未注册模块 」 加 模块名。
        否则：
            结果 为 做 热更新_替换 用 模块 的 模块ID、新版本、新代码哈希。
            若 结果 等于 假：
                做 内核警告 用 「热更新元数据失败: 」 加 模块名 加 「 → 已回滚」。
            否则：
                做 内核信息 用 「热更新元数据完成: 」 加 模块名 加 「 (版本/哈希已更新, 未执行代码载荷替换)」。
            完。
        完。
    完。
完。

系统_自动检查点 接受 零参：
    若 _系统健康_状态 不等于 「正常」：
        返还。
    完。
    名称 为 「自动_帧」 加 做 整数转字符串 用 _多模态_帧计数。
    做 检查点_创建 用 名称。
    做 内核调试 用 「自动检查点: 」 加 名称。
完。

可变 _视觉_已初始化 为 假。

可变 _听觉_已初始化 为 假。

可变 _语音合成_已初始化 为 假。

视觉_轮询 接受 零参：
    若 非 _视觉_已初始化：
        返还。
    完。
    若 非 图处_已初始化 或者 图处_上下文 等于 空 或者 图处_上下文 的 后缓冲 等于 0：
        返还。
    完。
    若 图处_上下文 的 帧计数 小于或等于 0：
        返还。
    完。
    像素基址 为 做 视觉识别_获取截图像素。
    若 像素基址 等于 0：
        返还。
    完。
    视觉结果 为 做 视觉识别_分析页面。
    _视觉_最近截图哈希 为 视觉结果 的 截图哈希。
    _视觉_最近元素数 为 做 计数 用 视觉结果 的 元素列表。
完。

听觉_轮询 接受 零参：
    若 非 _听觉_已初始化：
        返还。
    完。
    若 非 _听觉_录音流就绪 或者 _听觉_采样缓冲 等于 0：
        若 非 做 多模态_初始化默认录音流：
            返还。
        完。
        若 _系统健康_状态 等于 「降级」：
            _系统健康_状态 为 「正常」。
        完。
    完。
    录音流 为 做 多模态_获取默认录音流。
    若 录音流 等于 空 或者 录音流 的 状态 不等于 SND_PCM_STATE_RUNNING：
        _听觉_录音流就绪 为 假。
        _听觉_最近事件 为 「录音流中断」。
        _系统健康_状态 为 「降级」。
        返还。
    完。
    读取结果 为 做 PCM_读帧 用 录音流、_听觉_采样缓冲、_听觉_采样帧数。
    若 读取结果 小于或等于 0：
        返还。
    完。
    可变 样本序列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 读取结果：
        可变 样本 为 做 内存读16 用 _听觉_采样缓冲 加 i 乘 录音流 的 帧大小。
        若 样本 大于 32767：
            样本 为 样本 减 65536。
        完。
        做 追加 用 样本序列、样本。
        若 全局ASR 不等于 空 并且 全局ASR 的 正在录音：
            做 ASR_输入样本 用 样本。
        完。
        i 为 i 加 1。
    完。
    _听觉_最近事件 为 做 听觉识别_检测事件 用 样本序列。
完。

语音合成_轮询 接受 零参：
    若 非 _语音合成_已初始化：
        返还。
    完。
    若 主助手 等于 空：
        返还。
    完。
    做 全息助手_tick 用 主助手。
    若 全局ASR 不等于 空 并且 非 全局ASR 的 正在录音 并且 (主助手 的 状态 等于 助手状态_待命 或者 主助手 的 状态 等于 助手状态_休眠)：
        做 ASR_开始录音。
    完。
完。

多模态_初始化 接受 零参：
    做 视觉识别_初始化。
    _视觉_已初始化 为 真。
    做 听觉识别_初始化。
    _听觉_已初始化 为 真。
    若 非 做 多模态_初始化默认录音流：
        _系统健康_状态 为 「降级」。
        做 内核警告 用 「多模态子系统: 默认录音流未就绪, 听觉前台降级运行」。
    完。
    做 ASR_初始化。
    做 TTS_初始化。
    若 主助手 等于 空：
        做 全息助手_创建 用 「Nova」、「Nova」。
    完。
    若 全局ASR 不等于 空 并且 非 全局ASR 的 正在录音：
        做 ASR_开始录音。
    完。
    _语音合成_已初始化 为 真。
    做 内核信息 用 「多模态子系统: 视觉+听觉+语音前台已接入统一主循环」。
完。

注册表_验证 接受 零参：
    reg 为 做 注册表_获取。
    可变 缺失 为 0。
    若 reg 的 进程调度器 等于 0：
        做 内核日志 用 「❌ 缺失: 进程调度器」。
        缺失 为 缺失 加 1。
    完。
    若 reg 的 内存管理器 等于 0：
        做 内核日志 用 「❌ 缺失: 内存管理器」。
        缺失 为 缺失 加 1。
    完。
    若 reg 的 VFS 等于 0：
        做 内核日志 用 「❌ 缺失: VFS」。
        缺失 为 缺失 加 1。
    完。
    若 reg 的 图处驱动 等于 0：
        做 内核日志 用 「❌ 缺失: 图处驱动」。
        缺失 为 缺失 加 1。
    完。
    若 reg 的 传控栈 等于 0：
        做 内核日志 用 「❌ 缺失: 传控栈」。
        缺失 为 缺失 加 1。
    完。
    若 reg 的 文档解析器 等于 0：
        做 内核日志 用 「❌ 缺失: 文档解析器」。
        缺失 为 缺失 加 1。
    完。
    若 reg 的 样式引擎 等于 0：
        做 内核日志 用 「❌ 缺失: 样式引擎」。
        缺失 为 缺失 加 1。
    完。
    若 reg 的 元素树 等于 0：
        做 内核日志 用 「❌ 缺失: 元素树」。
        缺失 为 缺失 加 1。
    完。
    若 reg 的 Nova运行时 等于 0：
        做 内核日志 用 「❌ 缺失: Nova运行时」。
        缺失 为 缺失 加 1。
    完。
    若 reg 的 Nova画布 等于 0：
        做 内核日志 用 「❌ 缺失: Nova画布」。
        缺失 为 缺失 加 1。
    完。
    若 reg 的 推理引擎 等于 0：
        做 内核日志 用 「❌ 缺失: AI推理引擎」。
        缺失 为 缺失 加 1。
    完。
    若 reg 的 AI总线 等于 0：
        做 内核日志 用 「❌ 缺失: AI总线」。
        缺失 为 缺失 加 1。
    完。
    若 reg 的 桌面管理器 等于 0：
        做 内核日志 用 「❌ 缺失: 桌面管理器」。
        缺失 为 缺失 加 1。
    完。
    若 缺失 等于 0：
        做 内核日志 用 「✅ 系统完整性验证通过 — 所有子系统已注册」。
    否则：
        做 内核日志 用 「❌ 系统完整性验证失败 — %d个子系统缺失」、缺失。
    完。
    返还 缺失。
完。

循环统计 是 一种 事物，含 总轮次、总处理时间_us、内核调度次数、浏览器调度次数、AI调度次数、桌面调度次数、空闲轮次、超时轮次、最大轮次耗时_us、最小轮次耗时_us。

可变 全局_循环统计 为 空。

可变 循环_帧预算_us 为 16000。

循环统计_初始化 接受 零参：
    若 全局_循环统计 等于 空：
        全局_循环统计 为 新建 循环统计。
        全局_循环统计 的 总轮次 为 0。
        全局_循环统计 的 总处理时间_us 为 0。
        全局_循环统计 的 内核调度次数 为 0。
        全局_循环统计 的 浏览器调度次数 为 0。
        全局_循环统计 的 AI调度次数 为 0。
        全局_循环统计 的 桌面调度次数 为 0。
        全局_循环统计 的 空闲轮次 为 0。
        全局_循环统计 的 超时轮次 为 0。
        全局_循环统计 的 最大轮次耗时_us 为 0。
        全局_循环统计 的 最小轮次耗时_us 为 0x7FFFFFFF。
    完。
完。

循环_记录轮次 接受 耗时_us、内核、浏览器、AI、桌面：
    做 循环统计_初始化。
    全局_循环统计 的 总轮次 为 全局_循环统计 的 总轮次 加 1。
    全局_循环统计 的 总处理时间_us 为 全局_循环统计 的 总处理时间_us 加 耗时_us。
    若 内核：
        全局_循环统计 的 内核调度次数 为 全局_循环统计 的 内核调度次数 加 1。
    完。
    若 浏览器：
        全局_循环统计 的 浏览器调度次数 为 全局_循环统计 的 浏览器调度次数 加 1。
    完。
    若 AI：
        全局_循环统计 的 AI调度次数 为 全局_循环统计 的 AI调度次数 加 1。
    完。
    若 桌面：
        全局_循环统计 的 桌面调度次数 为 全局_循环统计 的 桌面调度次数 加 1。
    完。
    若 耗时_us 大于 全局_循环统计 的 最大轮次耗时_us：
        全局_循环统计 的 最大轮次耗时_us 为 耗时_us。
    完。
    若 耗时_us 小于 全局_循环统计 的 最小轮次耗时_us：
        全局_循环统计 的 最小轮次耗时_us 为 耗时_us。
    完。
    若 耗时_us 大于 循环_帧预算_us：
        全局_循环统计 的 超时轮次 为 全局_循环统计 的 超时轮次 加 1。
    完。
    若 非 内核 并且 非 浏览器 并且 非 AI 并且 非 桌面：
        全局_循环统计 的 空闲轮次 为 全局_循环统计 的 空闲轮次 加 1。
    完。
完。

循环_平均耗时 接受 零参：
    做 循环统计_初始化。
    若 全局_循环统计 的 总轮次 等于 0：
        返还 0。
    完。
    返还 全局_循环统计 的 总处理时间_us 除以 全局_循环统计 的 总轮次。
完。

循环_超时率 接受 零参：
    做 循环统计_初始化。
    若 全局_循环统计 的 总轮次 等于 0：
        返还 0。
    完。
    返还 全局_循环统计 的 超时轮次 乘 1000 除以 全局_循环统计 的 总轮次。
完。

循环_空闲率 接受 零参：
    做 循环统计_初始化。
    若 全局_循环统计 的 总轮次 等于 0：
        返还 0。
    完。
    返还 全局_循环统计 的 空闲轮次 乘 1000 除以 全局_循环统计 的 总轮次。
完。

循环_设置帧预算 接受 fps：
    若 fps 小于或等于 0：
        fps 为 60。
    完。
    循环_帧预算_us 为 1000000 除以 fps。
完。

循环_打印统计 接受 零参：
    做 循环统计_初始化。
    做 内核日志 用 「═══ NUML统一主循环统计 ═══」。
    做 内核日志 用 「  总轮次: 」 加 做 整数转字符串 用 全局_循环统计 的 总轮次。
    做 内核日志 用 「  平均耗时: 」 加 做 整数转字符串 用 做 循环_平均耗时 加 「 us」。
    做 内核日志 用 「  最大耗时: 」 加 做 整数转字符串 用 全局_循环统计 的 最大轮次耗时_us 加 「 us」。
    做 内核日志 用 「  超时率: 」 加 做 整数转字符串 用 做 循环_超时率 加 「/1000」。
    做 内核日志 用 「  空闲率: 」 加 做 整数转字符串 用 做 循环_空闲率 加 「/1000」。
    做 内核日志 用 「  内核调度: 」 加 做 整数转字符串 用 全局_循环统计 的 内核调度次数。
    做 内核日志 用 「  浏览器调度: 」 加 做 整数转字符串 用 全局_循环统计 的 浏览器调度次数。
    做 内核日志 用 「  AI调度: 」 加 做 整数转字符串 用 全局_循环统计 的 AI调度次数。
    做 内核日志 用 「  桌面调度: 」 加 做 整数转字符串 用 全局_循环统计 的 桌面调度次数。
完。

循环_重置统计 接受 零参：
    若 全局_循环统计 不等于 空：
        全局_循环统计 的 总轮次 为 0。
        全局_循环统计 的 总处理时间_us 为 0。
        全局_循环统计 的 内核调度次数 为 0。
        全局_循环统计 的 浏览器调度次数 为 0。
        全局_循环统计 的 AI调度次数 为 0。
        全局_循环统计 的 桌面调度次数 为 0。
        全局_循环统计 的 空闲轮次 为 0。
        全局_循环统计 的 超时轮次 为 0。
        全局_循环统计 的 最大轮次耗时_us 为 0。
        全局_循环统计 的 最小轮次耗时_us 为 0x7FFFFFFF。
    完。
完。

循环_完整统计 接受 零参：
    做 循环统计_初始化。
    返还 一组：「总轮次」、全局_循环统计 的 总轮次、「平均耗时」、做 循环_平均耗时、「超时率」、做 循环_超时率、「空闲率」、做 循环_空闲率、「内核调度」、全局_循环统计 的 内核调度次数、「浏览器调度」、全局_循环统计 的 浏览器调度次数、「AI调度」、全局_循环统计 的 AI调度次数、「桌面调度」、全局_循环统计 的 桌面调度次数、「NUML主循环」、1。
完。

循环_获取总轮次 接受 零参：
    做 循环统计_初始化。
    返还 全局_循环统计 的 总轮次。
完。

循环_获取超时次数 接受 零参：
    做 循环统计_初始化。
    返还 全局_循环统计 的 超时轮次。
完。

循环_获取帧预算 接受 零参：
    返还 循环_帧预算_us。
完。

主循环统计 是 一种 事物，含 总帧数、总调度时间_us、最大帧时间_us、最小帧时间_us、健康检查次数、热更新次数、检查点次数、事件处理数。

可变 全局_主循环统计 为 空。

主循环统计_初始化 接受 零参：
    若 全局_主循环统计 等于 空：
        全局_主循环统计 为 新建 主循环统计。
        全局_主循环统计 的 总帧数 为 0。
        全局_主循环统计 的 总调度时间_us 为 0。
        全局_主循环统计 的 最大帧时间_us 为 0。
        全局_主循环统计 的 最小帧时间_us 为 999999。
        全局_主循环统计 的 健康检查次数 为 0。
        全局_主循环统计 的 热更新次数 为 0。
        全局_主循环统计 的 检查点次数 为 0。
        全局_主循环统计 的 事件处理数 为 0。
    完。
完。

主循环_记录帧时间 接受 时间_us：
    做 主循环统计_初始化。
    全局_主循环统计 的 总帧数 为 全局_主循环统计 的 总帧数 加 1。
    全局_主循环统计 的 总调度时间_us 为 全局_主循环统计 的 总调度时间_us 加 时间_us。
    若 时间_us 大于 全局_主循环统计 的 最大帧时间_us：
        全局_主循环统计 的 最大帧时间_us 为 时间_us。
    完。
    若 时间_us 小于 全局_主循环统计 的 最小帧时间_us：
        全局_主循环统计 的 最小帧时间_us 为 时间_us。
    完。
完。

主循环_打印统计 接受 零参：
    做 主循环统计_初始化。
    做 打印行 用 「=== 统一主循环统计 ===」。
    做 打印行 用 「总帧: 」 加 做 整数转字符串 用 全局_主循环统计 的 总帧数。
    平均 为 0。
    若 全局_主循环统计 的 总帧数 大于 0：
        平均 为 全局_主循环统计 的 总调度时间_us 除以 全局_主循环统计 的 总帧数。
    完。
    做 打印行 用 「平均帧时间: 」 加 做 整数转字符串 用 平均 加 「 μs」。
    做 打印行 用 「最大: 」 加 做 整数转字符串 用 全局_主循环统计 的 最大帧时间_us 加 「 μs」。
    做 打印行 用 「最小: 」 加 做 整数转字符串 用 全局_主循环统计 的 最小帧时间_us 加 「 μs」。
    做 打印行 用 「健康检查: 」 加 做 整数转字符串 用 全局_主循环统计 的 健康检查次数。
    做 打印行 用 「热更新: 」 加 做 整数转字符串 用 全局_主循环统计 的 热更新次数。
完。

主循环_重置统计 接受 零参：
    若 全局_主循环统计 不等于 空：
        全局_主循环统计 的 总帧数 为 0。
        全局_主循环统计 的 总调度时间_us 为 0。
        全局_主循环统计 的 最大帧时间_us 为 0。
        全局_主循环统计 的 最小帧时间_us 为 999999。
        全局_主循环统计 的 健康检查次数 为 0。
        全局_主循环统计 的 热更新次数 为 0。
    完。
完。

主循环_获取帧数 接受 零参：
    做 主循环统计_初始化。
    返还 全局_主循环统计 的 总帧数。
完。

主循环_获取热更新数 接受 零参：
    做 主循环统计_初始化。
    返还 全局_主循环统计 的 热更新次数。
完。

主循环_完整统计 接受 零参：
    做 主循环统计_初始化。
    返还 一组：全局_主循环统计 的 总帧数、全局_主循环统计 的 总调度时间_us、全局_主循环统计 的 最大帧时间_us、全局_主循环统计 的 最小帧时间_us、全局_主循环统计 的 健康检查次数、全局_主循环统计 的 热更新次数、全局_主循环统计 的 检查点次数、全局_主循环统计 的 事件处理数。
完。

系统_健康报告 接受 零参：
    做 打印行 用 「=== Nova系统健康 ===」。
    做 打印行 用 「状态: 」 加 _系统健康_状态。
    做 打印行 用 「帧: 」 加 做 整数转字符串 用 _多模态_帧计数。
    做 打印行 用 「视觉: 」 加 做 整数转字符串 用 _视觉_已初始化。
    做 打印行 用 「听觉: 」 加 做 整数转字符串 用 _听觉_已初始化。
    做 打印行 用 「语音合成: 」 加 做 整数转字符串 用 _语音合成_已初始化。
完。

本章 收束。
