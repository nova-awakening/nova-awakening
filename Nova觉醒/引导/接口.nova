《接口》

引用《.·64位长模式入口》。

引用《.·寄存器定义》。

引用《.·引导加载器》。

引用《.·统一主循环》。

引导_启动序列 接受 零参：
    做 打印 用 「[引导] 启动序列开始」。
    做 引导_阶段0_硬件。
    做 引导_阶段1_内核。
    做 引导_阶段2_服务。
    做 引导_阶段3_智能。
    做 引导_阶段4_桌面。
    做 打印 用 「[引导] 启动序列完成, 移交神经中枢」。
完。

引导_阶段0_硬件 接受 零参：
    做 中枢_日志信息 用 「硬件初始化: CPU+内存+中断」。
完。

引导_阶段1_内核 接受 零参：
    做 中枢_日志信息 用 「内核启动: 调度器+文件系统+驱动」。
完。

引导_阶段2_服务 接受 零参：
    做 中枢_日志信息 用 「系统服务: init+终端+包管理」。
完。

引导_阶段3_智能 接受 零参：
    做 中枢_日志信息 用 「AI加载: 推理引擎+模型+语义分析」。
完。

引导_阶段4_桌面 接受 零参：
    做 中枢_日志信息 用 「桌面: 渲染引擎+HTML5桌面」。
完。

引导_进入主循环 接受 零参：
    做 NovaOS_进入统一主循环 用 0。
完。

引导_安全入口 接受 调用者、操作码：
    若 调用者 小于 0 或者 调用者 大于 8：
        返还 负 1。
    完。
    返还 0。
完。

引用《.·安装流程》。

引用《.·安装状态机》。

引用《.·玻璃系统栏》。

引用《.·玻璃桌面图标》。

引用《.·CPUID识别》。

引用《.·串口早期输》。

引用《.·端口IO》。

引用《.·MB2头校验》。

引用《.·子系统注册表》。

引用《.·画矩形》。

引用《.·内核启动阶段》。

引用《.·科幻全息AI》。

引用《.·MSR模型寄存器》。

引用《.·串口启动打印》。

引用《.·内核主函数》。

引用《.·内核停机》。

引用《.·GDT全局描述符表》。

引用《.·全息安装UI》。

引用《.·TSS任务状态段》。

引用《.·鼠标光标》。

引用《.·双通道输出》。

引用《.·特权指令》。

引用《.·统一启动》。

引用《.·系统部署》。

引用《.·桌面对话框阴影》。

引用《.·引导存根》。

引用《.·内核初始化调用》。

引用《.·云端接口UI》。

引用《.·智能安装配置》。

引用《.·MB2标签读取》。

引用《.·MB2命令行解析》。

引用《.·multiboot解析》。

引用《.·注册表获取》。

引用《..·中枢·接口》。

本章 收束。
