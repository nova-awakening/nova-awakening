《原子》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

内存序_松散 为 0。

内存序_获取 为 1。

内存序_释放 为 2。

内存序_获取释放 为 3。

内存序_顺序一致 为 4。

原子整数_创建 接受 值：
    返还 一组：值。
完。

原子整数_读 接受 a：
    返还 a 的 第 0 项。
完。

原子整数_写 接受 a、v：
    做 设置 用 a、0、v。
完。

原子整数_加 接受 a、v：
    做 设置 用 a、0、a 的 第 0 项 加 v。
    返还 a 的 第 0 项。
完。

原子整数_减 接受 a、v：
    做 设置 用 a、0、a 的 第 0 项 减 v。
    返还 a 的 第 0 项。
完。

原子整数_自增 接受 a：
    返还 做 原子整数_加 用 a、1。
完。

原子整数_自减 接受 a：
    返还 做 原子整数_减 用 a、1。
完。

原子整数_CAS 接受 a、期望值、新值：
    若 a 的 第 0 项 等于 期望值：
        做 设置 用 a、0、新值。
        返还 真。
    完。
    返还 假。
完。

原子整数_交换 接受 a、新值：
    旧 为 a 的 第 0 项。
    做 设置 用 a、0、新值。
    返还 旧。
完。

原子整数_取加 接受 a、v：
    旧 为 a 的 第 0 项。
    做 设置 用 a、0、a 的 第 0 项 加 v。
    返还 旧。
完。

原子整数_取减 接受 a、v：
    旧 为 a 的 第 0 项。
    做 设置 用 a、0、a 的 第 0 项 减 v。
    返还 旧。
完。

原子整数_取与 接受 a、v：
    旧 为 a 的 第 0 项。
    做 设置 用 a、0、a 的 第 0 项 位与 v。
    返还 旧。
完。

原子整数_取或 接受 a、v：
    旧 为 a 的 第 0 项。
    做 设置 用 a、0、a 的 第 0 项 位或 v。
    返还 旧。
完。

原子整数_取异或 接受 a、v：
    旧 为 a 的 第 0 项。
    做 设置 用 a、0、a 的 第 0 项 位异或 v。
    返还 旧。
完。

原子整数_最大 接受 a、v：
    若 a 的 第 0 项 小于 v：
        做 设置 用 a、0、v。
    完。
    返还 a 的 第 0 项。
完。

原子整数_最小 接受 a、v：
    若 a 的 第 0 项 大于 v：
        做 设置 用 a、0、v。
    完。
    返还 a 的 第 0 项。
完。

原子布尔_创建 接受 值：
    返还 一组：值。
完。

原子布尔_读 接受 a：
    返还 a 的 第 0 项。
完。

原子布尔_写 接受 a、v：
    做 设置 用 a、0、v。
完。

原子布尔_取反 接受 a：
    旧 为 a 的 第 0 项。
    做 设置 用 a、0、非 旧。
    返还 旧。
完。

原子布尔_CAS 接受 a、期望、新值：
    若 a 的 第 0 项 等于 期望：
        做 设置 用 a、0、新值。
        返还 真。
    完。
    返还 假。
完。

原子指针_创建 接受 指针：
    返还 一组：指针。
完。

原子指针_读 接受 a：
    返还 a 的 第 0 项。
完。

原子指针_写 接受 a、指针：
    做 设置 用 a、0、指针。
完。

原子指针_CAS 接受 a、期望、新指针：
    若 a 的 第 0 项 等于 期望：
        做 设置 用 a、0、新指针。
        返还 真。
    完。
    返还 假。
完。

原子指针_交换 接受 a、新指针：
    旧 为 a 的 第 0 项。
    做 设置 用 a、0、新指针。
    返还 旧。
完。

原子指针_是空 接受 a：
    返还 a 的 第 0 项 等于 0 或者 a 的 第 0 项 等于 空。
完。

原子标志_创建 接受 零参：
    返还 一组：假。
完。

原子标志_测试并设置 接受 f：
    旧 为 f 的 第 0 项。
    做 设置 用 f、0、真。
    返还 旧。
完。

原子标志_清除 接受 f：
    做 设置 用 f、0、假。
完。

自旋等待_CAS 接受 a、期望值、新值：
    可变 重试 为 0。
    可变 最大重试 为 1000。
    只要 重试 小于 最大重试：
        若 做 原子整数_CAS 用 a、期望值、新值：
            返还 真。
        完。
        做 自旋等待_暂停 用 重试。
        重试 为 重试 加 1。
    完。
    返还 假。
完。

自旋等待_暂停 接受 重试次数：
    若 重试次数 小于 4：
        返还。
    完。
    若 重试次数 小于 16：
        可变 k 为 0。
        只要 k 小于 重试次数：
            k 为 k 加 1。
        完。
        返还。
    完。
完。

原子计数器 是 一种 事物，含 值、最大值、最小值。

原子计数器_创建 接受 初始、最小、最大：
    返还 新建 原子计数器：值 为 做 原子整数_创建 用 初始、最大值 为 最大、最小值 为 最小。
完。

原子计数器_增 接受 c：
    当前 为 做 原子整数_读 用 c 的 值。
    若 当前 大于或等于 c 的 最大值：
        返还 假。
    完。
    做 原子整数_自增 用 c 的 值。
    返还 真。
完。

原子计数器_减 接受 c：
    当前 为 做 原子整数_读 用 c 的 值。
    若 当前 小于或等于 c 的 最小值：
        返还 假。
    完。
    做 原子整数_自减 用 c 的 值。
    返还 真。
完。

原子计数器_读 接受 c：
    返还 做 原子整数_读 用 c 的 值。
完。

原子计数器_重置 接受 c：
    做 原子整数_写 用 c 的 值、c 的 最小值。
完。

一次标志 是 一种 事物，含 状态。

一次_未执行 为 0。

一次_执行中 为 1。

一次_已完成 为 2。

一次标志_创建 接受 零参：
    返还 新建 一次标志：状态 为 做 原子整数_创建 用 一次_未执行。
完。

一次_执行 接受 标志、函数：
    若 做 原子整数_读 用 标志 的 状态 等于 一次_已完成：
        返还。
    完。
    若 做 原子整数_CAS 用 标志 的 状态、一次_未执行、一次_执行中：
        以 零参 为 引，其做法是：
        做 原子整数_写 用 标志 的 状态、一次_已完成。
    完。
完。

原子屏障 是 一种 事物，含 计数、目标数、轮次。

原子屏障_创建 接受 参与者数：
    返还 新建 原子屏障：计数 为 做 原子整数_创建 用 0、目标数 为 参与者数、轮次 为 做 原子整数_创建 用 0。
完。

原子屏障_等待 接受 屏障：
    当前轮 为 做 原子整数_读 用 屏障 的 轮次。
    到达数 为 做 原子整数_自增 用 屏障 的 计数。
    若 到达数 等于 屏障 的 目标数：
        做 原子整数_写 用 屏障 的 计数、0。
        做 原子整数_自增 用 屏障 的 轮次。
    否则：
        可变 等待 为 0。
        只要 做 原子整数_读 用 屏障 的 轮次 等于 当前轮 并且 等待 小于 100000：
            等待 为 等待 加 1。
        完。
    完。
完。

可变 AI_原子_调用数 为 0。

可变 AI_原子_异常数 为 0。

AI_原子_监控 接受 零参：
    AI_原子_调用数 为 AI_原子_调用数 加 1。
    返还 AI_原子_异常数 等于 0。
完。

AI_原子_健康检查 接受 零参：
    若 AI_原子_调用数 大于 0 并且 AI_原子_异常数 乘 100 除以 AI_原子_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
