《原生通道》

引用《..·中枢·类型定义》。

引用《..·中枢·内核打印》。

引用《..·中枢·高精度定时器hrtimer》。

引用《..·中枢·整数转字符串》。

通道_打开 为 0。

通道_已关 为 1。

默认容量 为 16。

最大订阅者 为 32。

最大管道 为 16。

通道 是 一种 事物，含 缓冲、容量、读位、写位、数量、状态、发送数、接收数、丢弃数、阻塞发送数、阻塞接收数、名称、创建时间。

广播通道 是 一种 事物，含 订阅者、订阅者数、发送数、名称。

管道连接 是 一种 事物，含 源通道、目标通道、过滤器、已转发、已启用。

通道统计 是 一种 事物，含 创建数、关闭数、发送总数、接收总数、阻塞发送总数、阻塞接收总数、丢弃总数、广播发送数、管道转发数、选择调用数、批量发送数、批量接收数、排空次数、释放数。

可变 通道总数 为 0。

可变 全局_通道统计 为 空。

可变 管道列表 为 一组：（空）。

可变 管道数 为 0。

通道统计_初始化 接受 零参：
    若 全局_通道统计 等于 空：
        全局_通道统计 为 新建 通道统计。
        全局_通道统计 的 创建数 为 0。
        全局_通道统计 的 关闭数 为 0。
        全局_通道统计 的 发送总数 为 0。
        全局_通道统计 的 接收总数 为 0。
        全局_通道统计 的 阻塞发送总数 为 0。
        全局_通道统计 的 阻塞接收总数 为 0。
        全局_通道统计 的 丢弃总数 为 0。
        全局_通道统计 的 广播发送数 为 0。
        全局_通道统计 的 管道转发数 为 0。
        全局_通道统计 的 选择调用数 为 0。
        全局_通道统计 的 批量发送数 为 0。
        全局_通道统计 的 批量接收数 为 0。
        全局_通道统计 的 排空次数 为 0。
        全局_通道统计 的 释放数 为 0。
    完。
完。

通道_创建 接受 容量：
    做 通道统计_初始化。
    全局_通道统计 的 创建数 为 全局_通道统计 的 创建数 加 1。
    通道总数 为 通道总数 加 1。
    若 容量 小于或等于 0：
        容量 为 默认容量。
    完。
    通 为 做 堆_分配 用 通道。
    通 的 缓冲 为 做 堆_分配 用 容量 乘 8。
    通 的 容量 为 容量。
    通 的 读位 为 0。
    通 的 写位 为 0。
    通 的 数量 为 0。
    通 的 状态 为 通道_打开。
    通 的 发送数 为 0。
    通 的 接收数 为 0。
    通 的 丢弃数 为 0。
    通 的 阻塞发送数 为 0。
    通 的 阻塞接收数 为 0。
    通 的 名称 为 「」。
    通 的 创建时间 为 做 获取滴答。
    返还 通。
完。

通道_创建_命名 接受 容量、名称：
    通 为 做 通道_创建 用 容量。
    若 通 不等于 0：
        通 的 名称 为 名称。
    完。
    返还 通。
完。

通道_发送 接受 通、值：
    若 通 等于 0：
        返还 错误_无效参数。
    完。
    若 通 的 状态 等于 通道_已关：
        返还 错误_已关闭。
    完。
    若 通 的 数量 大于或等于 通 的 容量：
        通 的 阻塞发送数 为 通 的 阻塞发送数 加 1。
        做 通道统计_初始化。
        全局_通道统计 的 阻塞发送总数 为 全局_通道统计 的 阻塞发送总数 加 1。
        返还 错误_队列满。
    完。
    做 内存_写64 用 通 的 缓冲 加 通 的 写位 乘 8、值。
    通 的 写位 为 (通 的 写位 加 1) 取余于 通 的 容量。
    通 的 数量 为 通 的 数量 加 1。
    通 的 发送数 为 通 的 发送数 加 1。
    做 通道统计_初始化。
    全局_通道统计 的 发送总数 为 全局_通道统计 的 发送总数 加 1。
    返还 0。
完。

通道_接收 接受 通：
    若 通 等于 0：
        返还 0。
    完。
    若 通 的 数量 等于 0：
        若 通 的 状态 等于 通道_已关：
            返还 0。
        完。
        通 的 阻塞接收数 为 通 的 阻塞接收数 加 1。
        做 通道统计_初始化。
        全局_通道统计 的 阻塞接收总数 为 全局_通道统计 的 阻塞接收总数 加 1。
        返还 0。
    完。
    值 为 做 内存_读64 用 通 的 缓冲 加 通 的 读位 乘 8。
    通 的 读位 为 (通 的 读位 加 1) 取余于 通 的 容量。
    通 的 数量 为 通 的 数量 减 1。
    通 的 接收数 为 通 的 接收数 加 1。
    做 通道统计_初始化。
    全局_通道统计 的 接收总数 为 全局_通道统计 的 接收总数 加 1。
    返还 值。
完。

通道_尝试发送 接受 通、值：
    若 通 等于 0 或者 通 的 状态 等于 通道_已关：
        返还 假。
    完。
    若 通 的 数量 大于或等于 通 的 容量：
        返还 假。
    完。
    做 通道_发送 用 通、值。
    返还 真。
完。

通道_尝试接收 接受 通：
    若 通 等于 0 或者 通 的 数量 等于 0：
        返还 一组：假、0。
    完。
    返还 一组：真、做 通道_接收 用 通。
完。

通道_批量发送 接受 通、值数组、数量：
    做 通道统计_初始化。
    全局_通道统计 的 批量发送数 为 全局_通道统计 的 批量发送数 加 1。
    可变 已发送 为 0。
    可变 i 为 0。
    只要 i 小于 数量：
        结果 为 做 通道_发送 用 通、值数组 的 第 i 项。
        若 结果 不等于 0：
            返还 已发送。
        完。
        已发送 为 已发送 加 1。
        i 为 i 加 1。
    完。
    返还 已发送。
完。

通道_批量接收 接受 通、最大数量：
    做 通道统计_初始化。
    全局_通道统计 的 批量接收数 为 全局_通道统计 的 批量接收数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大数量 并且 通 的 数量 大于 0：
        做 追加 用 结果、做 通道_接收 用 通。
        i 为 i 加 1。
    完。
    返还 结果。
完。

通道_关闭 接受 通：
    若 通 等于 0：
        返还。
    完。
    若 通 的 状态 不等于 通道_已关：
        做 通道统计_初始化。
        全局_通道统计 的 关闭数 为 全局_通道统计 的 关闭数 加 1。
    完。
    通 的 状态 为 通道_已关。
完。

通道_为空 接受 通：
    若 通 等于 0：
        返还 真。
    完。
    返还 通 的 数量 等于 0。
完。

通道_已满 接受 通：
    若 通 等于 0：
        返还 真。
    完。
    返还 通 的 数量 大于或等于 通 的 容量。
完。

通道_长度 接受 通：
    若 通 等于 0：
        返还 0。
    完。
    返还 通 的 数量。
完。

通道_是否关闭 接受 通：
    若 通 等于 0：
        返还 真。
    完。
    返还 通 的 状态 等于 通道_已关。
完。

通道_使用率 接受 通：
    若 通 等于 0 或者 通 的 容量 等于 0：
        返还 0。
    完。
    返还 通 的 数量 乘 100 除以 通 的 容量。
完。

通道_选择 接受 通道列表、数量：
    做 通道统计_初始化。
    全局_通道统计 的 选择调用数 为 全局_通道统计 的 选择调用数 加 1。
    可变 序号 为 0。
    只要 序号 小于 数量：
        通 为 通道列表 的 第 序号 项。
        若 通 不等于 0 并且 通 的 数量 大于 0：
            返还 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 负 1。
完。

通道_优先选择 接受 通道列表、优先级列表、数量：
    做 通道统计_初始化。
    全局_通道统计 的 选择调用数 为 全局_通道统计 的 选择调用数 加 1。
    可变 最佳索引 为 负 1。
    可变 最高优先级 为 负 1。
    可变 序号 为 0。
    只要 序号 小于 数量：
        通 为 通道列表 的 第 序号 项。
        若 通 不等于 0 并且 通 的 数量 大于 0：
            若 优先级列表 的 第 序号 项 大于 最高优先级：
                最高优先级 为 优先级列表 的 第 序号 项。
                最佳索引 为 序号。
            完。
        完。
        序号 为 序号 加 1。
    完。
    返还 最佳索引。
完。

通道_排空 接受 通：
    若 通 等于 0：
        返还。
    完。
    做 通道统计_初始化。
    全局_通道统计 的 排空次数 为 全局_通道统计 的 排空次数 加 1。
    可变 丢弃 为 0。
    只要 通 的 数量 大于 0：
        做 通道_接收 用 通。
        丢弃 为 丢弃 加 1。
    完。
    通 的 丢弃数 为 通 的 丢弃数 加 丢弃。
    全局_通道统计 的 丢弃总数 为 全局_通道统计 的 丢弃总数 加 丢弃。
完。

通道_释放 接受 通：
    若 通 等于 0：
        返还。
    完。
    做 通道统计_初始化。
    全局_通道统计 的 释放数 为 全局_通道统计 的 释放数 加 1。
    做 通道_关闭 用 通。
    若 通 的 缓冲 不等于 0：
        做 堆_释放 用 通 的 缓冲。
    完。
    做 堆_释放 用 通。
完。

广播通道_创建 接受 名称：
    广播 为 新建 广播通道。
    广播 的 订阅者 为 一组：（空）。
    广播 的 订阅者数 为 0。
    广播 的 发送数 为 0。
    广播 的 名称 为 名称。
    返还 广播。
完。

广播通道_订阅 接受 广播、通：
    若 广播 等于 空 或者 通 等于 0：
        返还 假。
    完。
    若 广播 的 订阅者数 大于或等于 最大订阅者：
        返还 假。
    完。
    做 追加 用 广播 的 订阅者、通。
    广播 的 订阅者数 为 广播 的 订阅者数 加 1。
    返还 真。
完。

广播通道_取消订阅 接受 广播、通：
    若 广播 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 广播 的 订阅者数：
        若 广播 的 订阅者 的 第 i 项 等于 通：
            做 删除 用 广播 的 订阅者、i。
            广播 的 订阅者数 为 广播 的 订阅者数 减 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

广播通道_发送 接受 广播、值：
    若 广播 等于 空：
        返还 0。
    完。
    做 通道统计_初始化。
    全局_通道统计 的 广播发送数 为 全局_通道统计 的 广播发送数 加 1。
    广播 的 发送数 为 广播 的 发送数 加 1。
    可变 成功数 为 0。
    可变 i 为 0。
    只要 i 小于 广播 的 订阅者数：
        结果 为 做 通道_尝试发送 用 广播 的 订阅者 的 第 i 项、值。
        若 结果：
            成功数 为 成功数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 成功数。
完。

管道_创建 接受 源、目标、过滤器：
    若 管道数 大于或等于 最大管道：
        返还 负 1。
    完。
    p 为 新建 管道连接。
    p 的 源通道 为 源。
    p 的 目标通道 为 目标。
    p 的 过滤器 为 过滤器。
    p 的 已转发 为 0。
    p 的 已启用 为 真。
    做 追加 用 管道列表、p。
    管道数 为 管道数 加 1。
    返还 管道数 减 1。
完。

管道_启用 接受 索引、启用：
    若 索引 小于 0 或者 索引 大于或等于 管道数：
        返还。
    完。
    管道列表 的 第 索引 项 的 已启用 为 启用。
完。

管道_处理 接受 零参：
    做 通道统计_初始化。
    可变 i 为 0。
    只要 i 小于 管道数：
        若 管道列表 的 第 i 项 的 已启用：
            源 为 管道列表 的 第 i 项 的 源通道。
            目标 为 管道列表 的 第 i 项 的 目标通道。
            只要 源 不等于 0 并且 源 的 数量 大于 0 并且 目标 不等于 0 并且 目标 的 数量 小于 目标 的 容量：
                值 为 做 通道_接收 用 源。
                可变 通过 为 真。
                若 管道列表 的 第 i 项 的 过滤器 不等于 空：
                    通过 为 做 管道列表 的 第 i 项 的 过滤器 用 值。
                完。
                若 通过：
                    做 通道_发送 用 目标、值。
                    管道列表 的 第 i 项 的 已转发 为 管道列表 的 第 i 项 的 已转发 加 1。
                    全局_通道统计 的 管道转发数 为 全局_通道统计 的 管道转发数 加 1。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

通道_扇入 接受 源列表、源数量、目标：
    可变 总接收 为 0。
    可变 i 为 0。
    只要 i 小于 源数量：
        只要 源列表 的 第 i 项 不等于 0 并且 源列表 的 第 i 项 的 数量 大于 0 并且 目标 的 数量 小于 目标 的 容量：
            值 为 做 通道_接收 用 源列表 的 第 i 项。
            做 通道_发送 用 目标、值。
            总接收 为 总接收 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 总接收。
完。

通道_扇出 接受 源、目标列表、目标数量：
    可变 已分发 为 0。
    可变 目标索引 为 0。
    只要 源 不等于 0 并且 源 的 数量 大于 0 并且 目标数量 大于 0：
        值 为 做 通道_接收 用 源。
        目标 为 目标列表 的 第 目标索引 取余于 目标数量 项。
        做 通道_发送 用 目标、值。
        已分发 为 已分发 加 1。
        目标索引 为 目标索引 加 1。
    完。
    返还 已分发。
完。

通道_完整统计 接受 通：
    若 通 等于 0：
        返还 一组：0、0、0、0、0、0。
    完。
    返还 一组：通 的 数量、通 的 容量、通 的 发送数、通 的 接收数、通 的 阻塞发送数、通 的 阻塞接收数。
完。

通道_全局统计 接受 零参：
    返还 一组：通道总数。
完。

通道_打印统计 接受 零参：
    做 通道统计_初始化。
    做 打印行 用 「=== 通道统计 ===」。
    做 打印行 用 「总通道数: 」 加 做 整数转字符串 用 通道总数。
    做 打印行 用 「创建: 」 加 做 整数转字符串 用 全局_通道统计 的 创建数。
    做 打印行 用 「关闭: 」 加 做 整数转字符串 用 全局_通道统计 的 关闭数。
    做 打印行 用 「发送总数: 」 加 做 整数转字符串 用 全局_通道统计 的 发送总数。
    做 打印行 用 「接收总数: 」 加 做 整数转字符串 用 全局_通道统计 的 接收总数。
    做 打印行 用 「阻塞发送: 」 加 做 整数转字符串 用 全局_通道统计 的 阻塞发送总数。
    做 打印行 用 「阻塞接收: 」 加 做 整数转字符串 用 全局_通道统计 的 阻塞接收总数。
    做 打印行 用 「丢弃: 」 加 做 整数转字符串 用 全局_通道统计 的 丢弃总数。
    做 打印行 用 「广播: 」 加 做 整数转字符串 用 全局_通道统计 的 广播发送数。
    做 打印行 用 「管道转发: 」 加 做 整数转字符串 用 全局_通道统计 的 管道转发数。
    做 打印行 用 「选择调用: 」 加 做 整数转字符串 用 全局_通道统计 的 选择调用数。
    做 打印行 用 「批量发送: 」 加 做 整数转字符串 用 全局_通道统计 的 批量发送数。
    做 打印行 用 「批量接收: 」 加 做 整数转字符串 用 全局_通道统计 的 批量接收数。
    做 打印行 用 「排空次数: 」 加 做 整数转字符串 用 全局_通道统计 的 排空次数。
    做 打印行 用 「释放: 」 加 做 整数转字符串 用 全局_通道统计 的 释放数。
    做 打印行 用 「管道数: 」 加 做 整数转字符串 用 管道数。
完。

通道_超时发送 接受 通、值、最大重试：
    若 通 等于 0 或者 通 的 状态 等于 通道_已关：
        返还 假。
    完。
    可变 重试 为 0。
    只要 重试 小于 最大重试：
        若 做 通道_尝试发送 用 通、值：
            返还 真。
        完。
        重试 为 重试 加 1。
    完。
    返还 假。
完。

通道_超时接收 接受 通、最大重试：
    若 通 等于 0：
        返还 一组：假、0。
    完。
    可变 重试 为 0。
    只要 重试 小于 最大重试：
        结果 为 做 通道_尝试接收 用 通。
        若 结果 的 第 0 项：
            返还 结果。
        完。
        重试 为 重试 加 1。
    完。
    返还 一组：假、0。
完。

通道_超时选择 接受 通道列表、数量、最大重试：
    可变 重试 为 0。
    只要 重试 小于 最大重试：
        索引 为 做 通道_选择 用 通道列表、数量。
        若 索引 大于或等于 0：
            返还 索引。
        完。
        重试 为 重试 加 1。
    完。
    返还 负 1。
完。

通道池_创建 接受 通道容量、池大小：
    可变 空闲 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 池大小：
        做 追加 用 空闲、做 通道_创建 用 通道容量。
        i 为 i 加 1。
    完。
    返还 一组：空闲、一组：（空）、通道容量、池大小、0、0。
完。

通道池_借出 接受 池：
    若 做 计数 用 池 的 第 0 项 等于 0：
        新通道 为 做 通道_创建 用 池 的 第 2 项。
        做 追加 用 池 的 第 1 项、新通道。
        池 的 第 4 项 为 池 的 第 4 项 加 1。
        返还 新通道。
    完。
    末尾 为 做 计数 用 池 的 第 0 项 减 1。
    通 为 池 的 第 0 项 的 第 末尾 项。
    做 列表_移除 用 池 的 第 0 项、末尾。
    做 追加 用 池 的 第 1 项、通。
    池 的 第 4 项 为 池 的 第 4 项 加 1。
    返还 通。
完。

通道池_归还 接受 池、通：
    若 通 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 池 的 第 1 项：
        若 池 的 第 1 项 的 第 i 项 等于 通：
            做 列表_移除 用 池 的 第 1 项、i。
            跳出。
        完。
        i 为 i 加 1。
    完。
    做 通道_排空 用 通。
    做 追加 用 池 的 第 0 项、通。
    池 的 第 5 项 为 池 的 第 5 项 加 1。
    返还 真。
完。

通道池_空闲数 接受 池：
    返还 做 计数 用 池 的 第 0 项。
完。

通道池_活跃数 接受 池：
    返还 做 计数 用 池 的 第 1 项。
完。

通道池_收缩 接受 池、目标大小：
    可变 释放数 为 0。
    只要 做 计数 用 池 的 第 0 项 大于 目标大小：
        末尾 为 做 计数 用 池 的 第 0 项 减 1。
        做 通道_释放 用 池 的 第 0 项 的 第 末尾 项。
        做 列表_移除 用 池 的 第 0 项、末尾。
        释放数 为 释放数 加 1。
    完。
    返还 释放数。
完。

通道池_释放全部 接受 池：
    可变 i 为 0。
    只要 i 小于 做 计数 用 池 的 第 0 项：
        做 通道_释放 用 池 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 池 的 第 1 项：
        做 通道_释放 用 池 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    做 设置 用 池、0、一组：（空）。
    做 设置 用 池、1、一组：（空）。
完。

通道路由_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、0、空、0。
完。

通道路由_注册 接受 路由器、键、通道：
    可变 i 为 0。
    只要 i 小于 路由器 的 第 2 项：
        若 路由器 的 第 0 项 的 第 i 项 等于 键：
            路由器 的 第 1 项 的 第 i 项 为 通道。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 路由器 的 第 0 项、键。
    做 追加 用 路由器 的 第 1 项、通道。
    路由器 的 第 2 项 为 路由器 的 第 2 项 加 1。
    返还 真。
完。

通道路由_取消 接受 路由器、键：
    可变 i 为 0。
    只要 i 小于 路由器 的 第 2 项：
        若 路由器 的 第 0 项 的 第 i 项 等于 键：
            做 列表_移除 用 路由器 的 第 0 项、i。
            做 列表_移除 用 路由器 的 第 1 项、i。
            路由器 的 第 2 项 为 路由器 的 第 2 项 减 1。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

通道路由_设置默认 接受 路由器、通道：
    路由器 的 第 3 项 为 通道。
完。

通道路由_发送 接受 路由器、键、值：
    可变 i 为 0。
    只要 i 小于 路由器 的 第 2 项：
        若 路由器 的 第 0 项 的 第 i 项 等于 键：
            结果 为 做 通道_尝试发送 用 路由器 的 第 1 项 的 第 i 项、值。
            若 结果：
                路由器 的 第 4 项 为 路由器 的 第 4 项 加 1。
            完。
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    若 路由器 的 第 3 项 不等于 空：
        结果 为 做 通道_尝试发送 用 路由器 的 第 3 项、值。
        若 结果：
            路由器 的 第 4 项 为 路由器 的 第 4 项 加 1。
        完。
        返还 结果。
    完。
    返还 假。
完。

通道路由_广播发送 接受 路由器、值：
    可变 成功 为 0。
    可变 i 为 0。
    只要 i 小于 路由器 的 第 2 项：
        若 做 通道_尝试发送 用 路由器 的 第 1 项 的 第 i 项、值：
            成功 为 成功 加 1。
        完。
        i 为 i 加 1。
    完。
    路由器 的 第 4 项 为 路由器 的 第 4 项 加 成功。
    返还 成功。
完。

通道路由_路由数 接受 路由器：
    返还 路由器 的 第 2 项。
完。

纠缠_初始强度 为 1000。

纠缠_衰减率 为 990。

纠缠_最低强度 为 100。

纠缠通道_创建 接受 容量A、容量B：
    通道A 为 做 通道_创建 用 容量A。
    通道B 为 做 通道_创建 用 容量B。
    返还 一组：通道A、通道B、纠缠_初始强度、纠缠_衰减率、0、0。
完。

纠缠通道_自定义创建 接受 容量A、容量B、初始强度、衰减率：
    通道A 为 做 通道_创建 用 容量A。
    通道B 为 做 通道_创建 用 容量B。
    返还 一组：通道A、通道B、初始强度、衰减率、0、0。
完。

纠缠通道_发送A 接受 纠缠对、值：
    结果A 为 做 通道_尝试发送 用 纠缠对 的 第 0 项、值。
    若 非 结果A：
        纠缠对 的 第 5 项 为 纠缠对 的 第 5 项 加 1。
        返还 假。
    完。
    若 纠缠对 的 第 2 项 大于或等于 500：
        做 通道_尝试发送 用 纠缠对 的 第 1 项、值。
    完。
    纠缠对 的 第 2 项 为 纠缠对 的 第 2 项 乘 纠缠对 的 第 3 项 除以 1000。
    若 纠缠对 的 第 2 项 小于 纠缠_最低强度：
        纠缠对 的 第 2 项 为 纠缠_最低强度。
    完。
    纠缠对 的 第 4 项 为 纠缠对 的 第 4 项 加 1。
    返还 真。
完。

纠缠通道_发送B 接受 纠缠对、值：
    结果B 为 做 通道_尝试发送 用 纠缠对 的 第 1 项、值。
    若 非 结果B：
        纠缠对 的 第 5 项 为 纠缠对 的 第 5 项 加 1。
        返还 假。
    完。
    若 纠缠对 的 第 2 项 大于或等于 500：
        做 通道_尝试发送 用 纠缠对 的 第 0 项、值。
    完。
    纠缠对 的 第 2 项 为 纠缠对 的 第 2 项 乘 纠缠对 的 第 3 项 除以 1000。
    若 纠缠对 的 第 2 项 小于 纠缠_最低强度：
        纠缠对 的 第 2 项 为 纠缠_最低强度。
    完。
    纠缠对 的 第 4 项 为 纠缠对 的 第 4 项 加 1。
    返还 真。
完。

纠缠通道_接收A 接受 纠缠对：
    返还 做 通道_尝试接收 用 纠缠对 的 第 0 项。
完。

纠缠通道_接收B 接受 纠缠对：
    返还 做 通道_尝试接收 用 纠缠对 的 第 1 项。
完。

纠缠通道_强化 接受 纠缠对：
    纠缠对 的 第 2 项 为 纠缠_初始强度。
完。

纠缠通道_强度 接受 纠缠对：
    返还 纠缠对 的 第 2 项。
完。

纠缠通道_同步次数 接受 纠缠对：
    返还 纠缠对 的 第 4 项。
完。

纠缠通道_失谐次数 接受 纠缠对：
    返还 纠缠对 的 第 5 项。
完。

纠缠通道_释放 接受 纠缠对：
    做 通道_释放 用 纠缠对 的 第 0 项。
    做 通道_释放 用 纠缠对 的 第 1 项。
完。

通道_模块完整统计 接受 零参：
    做 通道统计_初始化。
    做 打印行 用 「╔══════════════════════════════════════════╗」。
    做 打印行 用 「║       原生通道模块 · 完整运行统计        ║」。
    做 打印行 用 「╠══════════════════════════════════════════╣」。
    做 打印行 用 「║ [生命周期]                               ║」。
    做 打印行 用 「║   总通道数:   」 加 做 整数转字符串 用 通道总数。
    做 打印行 用 「║   创建次数:   」 加 做 整数转字符串 用 全局_通道统计 的 创建数。
    做 打印行 用 「║   关闭次数:   」 加 做 整数转字符串 用 全局_通道统计 的 关闭数。
    做 打印行 用 「║   释放次数:   」 加 做 整数转字符串 用 全局_通道统计 的 释放数。
    做 打印行 用 「╠══════════════════════════════════════════╣」。
    做 打印行 用 「║ [数据流转]                               ║」。
    做 打印行 用 「║   发送总数:   」 加 做 整数转字符串 用 全局_通道统计 的 发送总数。
    做 打印行 用 「║   接收总数:   」 加 做 整数转字符串 用 全局_通道统计 的 接收总数。
    做 打印行 用 「║   丢弃总数:   」 加 做 整数转字符串 用 全局_通道统计 的 丢弃总数。
    做 打印行 用 「╠══════════════════════════════════════════╣」。
    做 打印行 用 「║ [阻塞状况]                               ║」。
    做 打印行 用 「║   阻塞发送:   」 加 做 整数转字符串 用 全局_通道统计 的 阻塞发送总数。
    做 打印行 用 「║   阻塞接收:   」 加 做 整数转字符串 用 全局_通道统计 的 阻塞接收总数。
    做 打印行 用 「╠══════════════════════════════════════════╣」。
    做 打印行 用 「║ [高级功能]                               ║」。
    做 打印行 用 「║   广播发送:   」 加 做 整数转字符串 用 全局_通道统计 的 广播发送数。
    做 打印行 用 「║   管道转发:   」 加 做 整数转字符串 用 全局_通道统计 的 管道转发数。
    做 打印行 用 「║   选择调用:   」 加 做 整数转字符串 用 全局_通道统计 的 选择调用数。
    做 打印行 用 「║   管道数:     」 加 做 整数转字符串 用 管道数。
    做 打印行 用 「╠══════════════════════════════════════════╣」。
    做 打印行 用 「║ [吞吐量]                                 ║」。
    总流转 为 全局_通道统计 的 发送总数 加 全局_通道统计 的 接收总数。
    做 打印行 用 「║   总流转数:   」 加 做 整数转字符串 用 总流转。
    阻塞率 为 0。
    若 全局_通道统计 的 发送总数 大于 0：
        阻塞率 为 全局_通道统计 的 阻塞发送总数 乘 1000 除以 全局_通道统计 的 发送总数。
    完。
    做 打印行 用 「║   阻塞率‰:    」 加 做 整数转字符串 用 阻塞率。
    做 打印行 用 「╚══════════════════════════════════════════╝」。
完。

可变 AI_原生通道_带宽估计 为 0。

可变 AI_原生通道_丢包窗口 为 一组：（空）。

AI_原生通道_带宽预测 接受 发送字节、耗时毫秒：
    若 耗时毫秒 等于 0：
        返还 AI_原生通道_带宽估计。
    完。
    当前 为 发送字节 乘 1000 除以 耗时毫秒。
    AI_原生通道_带宽估计 为 AI_原生通道_带宽估计 乘 700 除以 1000 加 当前 乘 300 除以 1000。
    返还 AI_原生通道_带宽估计。
完。

AI_原生通道_拥塞建议 接受 丢包率：
    做 追加 用 AI_原生通道_丢包窗口、丢包率。
    若 做 计数 用 AI_原生通道_丢包窗口 大于 10：
        做 列表_移除 用 AI_原生通道_丢包窗口、0。
    完。
    若 丢包率 大于 50：
        返还 「减半窗口」。
    完。
    若 丢包率 小于 5：
        返还 「增大窗口」。
    完。
    返还 「保持」。
完。

本章 收束。
