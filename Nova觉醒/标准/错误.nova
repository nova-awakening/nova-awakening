《错误》

引用《..·中枢·类型定义》。
引用《..·中枢·自动扩容》。
引用《..·中枢·字符串替换》。
引用《.·原生列表》。

错误类型_IO 为 「IO错误」。
错误类型_参数 为 「参数错误」。
错误类型_溢出 为 「溢出错误」。
错误类型_越界 为 「越界错误」。
错误类型_类型 为 「类型错误」。
错误类型_空值 为 「空值错误」。
错误类型_超时 为 「超时错误」。
错误类型_权限 为 「权限错误」。
错误类型_未找到 为 「未找到」。
错误类型_已存在 为 「已存在」。
错误类型_功能缺失 为 「功能未接入」。
错误类型_网络 为 「网络错误」。
错误类型_解析 为 「解析错误」。
错误类型_内存 为 「内存错误」。
错误类型_并发 为 「并发错误」。
错误类型_状态 为 「状态错误」。
错误类型_配置 为 「配置错误」。

错误类别_系统 为 0。
错误类别_用户 为 1。
错误类别_网络 为 2。
错误类别_IO 为 3。
错误类别_逻辑 为 4。

错误_最大栈深度 为 16。
错误_最大历史 为 64。

增强错误 是 一种 事物：
    类型,。
    消息,。
    类别,。
    原因,。
    栈帧列表,。
    时间戳,。
    上下文,。
完。

可变 全局错误处理器 为 空。
可变 错误_总数 为 0。
可变 错误_历史 为 []。
可变 最后错误 为 空。

错误_创建 接受 类型、消息)：
    返还 [类型, 消息]。
完。

错误_创建增强 接受 类型、消息、类别)：
    错误_总数 为 错误_总数 加 1。
    错误 为 增强错误 ：
        类型。
        消息。
        类别。
        原因。
        栈帧列表。
        时间戳。
        上下文。
    完。
    最后错误 为 错误。
    做 后援_记录错误 用 错误。
    返还 错误。
完。

错误_包装 接受 外部类型、外部消息、内部错误)：
    错误 为 做 错误_创建增强 用 外部类型、外部消息、错误类别_逻辑。
    错误.原因 为 内部错误。
    返还 错误。
完。

错误_添加栈帧 接受 错误、函数名、文件、行号)：
    若 错误 等于 空：
    若 做 是列表 用 错误：
    若 做 计数 用 错误.栈帧列表 小于 错误_最大栈深度：
        做 追加 用 错误.栈帧列表、[函数名、文件、行号]。
    完。
完。

错误_添加上下文 接受 错误、键、值)：
    若 错误 等于 空：
    若 做 是列表 用 错误：
    做 字典设 用 错误.上下文、键、值。
完。

错误_类型 接受 错误)：
    若 做 是列表 用 错误：
    返还 错误.类型。
完。

错误_消息 接受 错误)：
    若 做 是列表 用 错误：
    返还 错误.消息。
完。

错误_原因 接受 错误)：
    若 做 是列表 用 错误：
    返还 错误.原因。
完。

错误_根原因 接受 错误)：
    若 错误 等于 空：
    若 做 是列表 用 错误：
    可变 当前 为 错误。
    可变 深度 为 0。
    只要 当前.原因 不等于 空 并且 深度 小于 错误_最大栈深度：
        当前 = 当前.原因。
        深度 为 深度 加 1。
    完。
    返还 当前。
完。

IO错误 接受 消息) ：返还 做 错误_创建 用 错误类型_IO、消息 }：
参数错误 接受 消息) ：返还 做 错误_创建 用 错误类型_参数、消息 }：
溢出错误 接受 消息) ：返还 做 错误_创建 用 错误类型_溢出、消息 }：
越界错误 接受 消息) ：返还 做 错误_创建 用 错误类型_越界、消息 }：
类型错误 接受 消息) ：返还 做 错误_创建 用 错误类型_类型、消息 }：
空值错误 接受 消息) ：返还 做 错误_创建 用 错误类型_空值、消息 }：
超时错误 接受 消息) ：返还 做 错误_创建 用 错误类型_超时、消息 }：
权限错误 接受 消息) ：返还 做 错误_创建 用 错误类型_权限、消息 }：
未找到错误 接受 消息) ：返还 做 错误_创建 用 错误类型_未找到、消息 }：
网络错误 接受 消息) ：返还 做 错误_创建 用 错误类型_网络、消息 }：
解析错误 接受 消息) ：返还 做 错误_创建 用 错误类型_解析、消息 }：
内存错误 接受 消息) ：返还 做 错误_创建 用 错误类型_内存、消息 }：
并发错误 接受 消息) ：返还 做 错误_创建 用 错误类型_并发、消息 }：
状态错误 接受 消息) ：返还 做 错误_创建 用 错误类型_状态、消息 }：
配置错误 接受 消息) ：返还 做 错误_创建 用 错误类型_配置、消息 }：

是错误 接受 值)：
    若 值 等于 空：
    若 做 是列表 用 值 并且 做 计数 用 值 等于 2 并且 做 是字符串 用 值[0]：
    返还 假。
完。

是增强错误 接受 值)：
    若 值 等于 空：
    返还 !做 是列表 用 值。
完。

错误_显示 接受 错误)：
    若 做 是列表 用 错误：
    返还 错误.类型 加 「: 」 加 错误.消息。
完。

错误_完整显示 接受 错误)：
    若 错误 等于 空：
    可变 结果 为 做 错误_显示 用 错误。
    若 !做 是列表 用 错误：
        若 做 计数 用 错误.栈帧列表 大于 0：
            结果 为 结果 加 「\n调用栈:」。
            可变 i 为 0。
            只要 i 小于 做 计数 用 错误.栈帧列表：
                帧 为 错误.栈帧列表[i]。
                结果 为 结果 加 「\n  」 加 帧[0] 加 「 (」 加 帧[1] 加 「:」 加 做 转字符串 用 帧[2] 加 「)」。
                i 为 i 加 1。
            完。
        完。
        若 错误.原因 不等于 空：
            结果 为 结果 加 「\n原因: 」 加 做 错误_显示 用 错误.原因。
        完。
    完。
    返还 结果。
完。

错误_类别名 接受 类别)：
    若 类别 等于 错误类别_系统：
    若 类别 等于 错误类别_用户：
    若 类别 等于 错误类别_网络：
    若 类别 等于 错误类别_IO：
    若 类别 等于 错误类别_逻辑：
    返还 「未知」。
完。

成功结果 接受 值) ：返还 [真、值] }：
失败结果 接受 错误) ：返还 [假、错误] }：
结果_是成功 接受 r) ：返还 r[0]：
结果_取值 接受 r) ：返还 r[1] }：
结果_取错误 接受 r) ：返还 r[1] }：

结果_映射 接受 r、函数)：
    若 做 结果_是成功 用 r：
    返还 r。
完。

结果_映射错误 接受 r、函数)：
    若 !做 结果_是成功 用 r：
    返还 r。
完。

结果_取值或默认 接受 r、默认值)：
    若 做 结果_是成功 用 r：
    返还 默认值。
完。

结果_取值或执行 接受 r、回退函数)：
    若 做 结果_是成功 用 r：
    返还 做 回退函数。
完。

结果_链 接受 r、函数)：
    若 做 结果_是成功 用 r：
    返还 r。
完。

错误_设全局处理器 接受 处理器) { 全局错误处理器：

后援_记录错误 接受 错误)：
    做 追加 用 错误_历史、错误。
    若 做 计数 用 错误_历史 大于 错误_最大历史：
        错误_历史 为 做 列表_切片 用 错误_历史、错误_最大历史 除以 2、做 计数 用 错误_历史。
    完。
    若 全局错误处理器 不等于 空：
        做 全局错误处理器 用 错误。
    完。
完。

错误_获取历史 接受 ) ：返还 错误_历史 }：
错误_获取最后 接受 ) ：返还 最后错误 }：
错误_统计 接受 ) ：返还 错误_总数 }：
错误_清空历史 接受 ) { 错误_历史：

断言 接受 条件、消息)：
    若 !条件：
完。
断言相等 接受 实际、期望、消息)：
    若 实际 不等于 期望：
完。
断言非空 接受 值、消息)：
    若 值 等于 空：
完。
断言类型 接受 值、期望类型、消息)：
    若 做 类型 用 值 不等于 期望类型：
完。

错误_健康评估 接受 )：
    可变 问题 为 []。
    若 错误_总数 大于 100：
    若 最后错误 大于 100：
    返还 做 计数 用 问题 等于 0。
完。

错误_综合报告 接受 )：
    做 打印 用 「[错误] ═══ 综合报告 ═══」。
    做 打印 用 「  错误_总数: 」 加 错误_总数。
    做 打印 用 「  最后错误: 」 加 最后错误。
    做 打印 用 「  健康: 」 加 若 (做 错误_健康评估) { 「正常」 否则： 「异常」 }。
完。

错误_全局重置 接受 )：
    错误_总数 为 0。
    最后错误 为 0。
    做 打印 用 「[错误] 统计已重置」。
    返还 0。
完。

本章 收束。
