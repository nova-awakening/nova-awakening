《原生时间》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·执行·VSync帧调度》。

引用《..·中枢·内容可见性》。

引用《..·执行·平台时间》。

每日秒 为 86400。

每时秒 为 3600。

每分秒 为 60。

月天数表 为 一组：31、28、31、30、31、30、31、31、30、31、30、31。

时间_现在秒 接受 零参：
    返还 做 平台_获取墙钟秒。
完。

时间_现在微秒 接受 零参：
    返还 做 平台_获取墙钟微秒。
完。

时间_单调微秒 接受 零参：
    返还 做 平台_获取高精度时间。
完。

时间_是闰年 接受 年：
    若 年 取余于 400 等于 0：
        返还 真。
    完。
    若 年 取余于 100 等于 0：
        返还 假。
    完。
    若 年 取余于 4 等于 0：
        返还 真。
    完。
    返还 假。
完。

时间_年天数 接受 年：
    若 做 时间_是闰年 用 年：
        返还 366。
    完。
    返还 365。
完。

时间_月天数 接受 年、月：
    若 月 小于 1 或者 月 大于 12：
        返还 30。
    完。
    天 为 月天数表 的 第 月 减 1 项。
    若 月 等于 2 并且 做 时间_是闰年 用 年：
        天 为 天 加 1。
    完。
    返还 天。
完。

时间_秒转日期 接受 秒：
    可变 剩余天 为 秒 除以 每日秒。
    可变 年 为 1970。
    只要 剩余天 大于或等于 做 时间_年天数 用 年：
        剩余天 为 剩余天 减 做 时间_年天数 用 年。
        年 为 年 加 1。
    完。
    可变 月 为 1。
    只要 月 小于或等于 12 并且 剩余天 大于或等于 做 时间_月天数 用 年、月：
        剩余天 为 剩余天 减 做 时间_月天数 用 年、月。
        月 为 月 加 1。
    完。
    日 为 剩余天 加 1。
    时 为 秒 取余于 每日秒 除以 每时秒。
    分 为 秒 取余于 每时秒 除以 每分秒。
    秒余 为 秒 取余于 每分秒。
    返还 一组：年、月、日、时、分、秒余。
完。

时间_日期转秒 接受 年、月、日、时、分、秒：
    可变 总天 为 0。
    可变 当年 为 1970。
    只要 当年 小于 年：
        总天 为 总天 加 做 时间_年天数 用 当年。
        当年 为 当年 加 1。
    完。
    可变 当月 为 1。
    只要 当月 小于 月：
        总天 为 总天 加 做 时间_月天数 用 年、当月。
        当月 为 当月 加 1。
    完。
    总天 为 总天 加 日 减 1。
    返还 总天 乘 每日秒 加 时 乘 每时秒 加 分 乘 每分秒 加 秒。
完。

时间_星期几 接受 秒：
    天 为 秒 除以 每日秒。
    返还 (天 加 4) 取余于 7。
完。

时间_补零 接受 数值：
    若 数值 小于 10：
        返还 「0」。
    完。
    返还 「」。
完。

时间_格式化时分秒 接受 秒：
    时 为 秒 取余于 每日秒 除以 每时秒。
    分 为 秒 取余于 每时秒 除以 每分秒。
    秒余 为 秒 取余于 每分秒。
    返还 一组：时、分、秒余。
完。

时间_差值秒 接受 起始秒、结束秒：
    返还 结束秒 减 起始秒。
完。

时间_增加天 接受 秒、天数：
    返还 秒 加 天数 乘 每日秒。
完。

时间_增加时 接受 秒、小时数：
    返还 秒 加 小时数 乘 每时秒。
完。

计时器 是 一种 事物，含 起始微秒、暂停微秒、已暂停、累计微秒。

计时器_创建 接受 零参：
    计 为 做 堆_分配 用 计时器。
    计 的 起始微秒 为 做 平台_获取高精度时间。
    计 的 暂停微秒 为 0。
    计 的 已暂停 为 假。
    计 的 累计微秒 为 0。
    返还 计。
完。

计时器_经过 接受 计：
    若 计 等于 0：
        返还 0。
    完。
    若 计 的 已暂停：
        返还 计 的 累计微秒。
    完。
    返还 计 的 累计微秒 加 (做 平台_获取高精度时间 减 计 的 起始微秒)。
完。

计时器_暂停 接受 计：
    若 计 等于 0 或者 计 的 已暂停：
        返还。
    完。
    计 的 累计微秒 为 计 的 累计微秒 加 (做 平台_获取高精度时间 减 计 的 起始微秒)。
    计 的 已暂停 为 真。
完。

计时器_恢复 接受 计：
    若 计 等于 0 或者 非 计 的 已暂停：
        返还。
    完。
    计 的 起始微秒 为 做 平台_获取高精度时间。
    计 的 已暂停 为 假。
完。

计时器_重置 接受 计：
    若 计 等于 0：
        返还。
    完。
    计 的 起始微秒 为 做 平台_获取高精度时间。
    计 的 累计微秒 为 0。
    计 的 已暂停 为 假。
完。

计时器_经过毫秒 接受 计：
    返还 做 计时器_经过 用 计 除以 1000。
完。

计时器_经过秒 接受 计：
    返还 做 计时器_经过 用 计 除以 1000000。
完。

时间_度量 接受 回调：
    开始 为 做 平台_获取高精度时间。
    若 回调 不等于 0：
        做 回调。
    完。
    返还 做 平台_获取高精度时间 减 开始。
完。

时间_人类可读 接受 微秒：
    若 微秒 小于 1000：
        返还 「微秒」。
    完。
    若 微秒 小于 1000000：
        返还 「毫秒」。
    完。
    若 微秒 小于 60000000：
        返还 「秒」。
    完。
    若 微秒 小于 3600000000：
        返还 「分钟」。
    完。
    返还 「小时」。
完。

时间_延迟微秒 接受 微秒：
    做 平台_睡眠微秒 用 微秒。
完。

时间_延迟毫秒 接受 毫秒：
    做 平台_睡眠微秒 用 毫秒 乘 1000。
完。

时间_延迟秒 接受 秒：
    做 平台_睡眠微秒 用 秒 乘 1000000。
完。

可变 AI_原生时间_调用数 为 0。

可变 AI_原生时间_异常数 为 0。

AI_原生时间_监控 接受 零参：
    AI_原生时间_调用数 为 AI_原生时间_调用数 加 1。
    返还 AI_原生时间_异常数 等于 0。
完。

AI_原生时间_健康检查 接受 零参：
    若 AI_原生时间_调用数 大于 0 并且 AI_原生时间_异常数 乘 100 除以 AI_原生时间_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
