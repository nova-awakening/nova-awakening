《原生数学》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

定点基 为 1024。

圆周率定点 为 3217。

取大 接受 甲、乙：
    若 甲 大于 乙：
        返还 甲。
    完。
    返还 乙。
完。

取小 接受 甲、乙：
    若 甲 小于 乙：
        返还 甲。
    完。
    返还 乙。
完。

钳制 接受 值、下限、上限：
    若 值 小于 下限：
        返还 下限。
    完。
    若 值 大于 上限：
        返还 上限。
    完。
    返还 值。
完。

符号 接受 x：
    若 x 大于 0：
        返还 1。
    完。
    若 x 小于 0：
        返还 负 1。
    完。
    返还 0。
完。

幂 接受 底数、指数：
    若 指数 等于 0：
        返还 1。
    完。
    若 指数 小于 0：
        返还 0。
    完。
    可变 结果 为 1。
    可变 基 为 底数。
    可变 余指 为 指数。
    只要 余指 大于 0：
        若 余指 位与 1：
            结果 为 结果 乘 基。
        完。
        基 为 基 乘 基。
        余指 为 余指 右移 1。
    完。
    返还 结果。
完。

开方 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

立方根 接受 n：
    若 n 等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 迭代 为 0。
    只要 迭代 小于 40：
        x 为 (2 乘 x 加 n 除以 (x 乘 x)) 除以 3。
        迭代 为 迭代 加 1。
    完。
    返还 x。
完。

最大公约数 接受 甲、乙：
    可变 x 为 做 绝对值 用 甲。
    可变 y 为 做 绝对值 用 乙。
    只要 y 不等于 0：
        临 为 y。
        y 为 x 取余于 y。
        x 为 临。
    完。
    返还 x。
完。

最小公倍数 接受 甲、乙：
    若 甲 等于 0 或者 乙 等于 0：
        返还 0。
    完。
    返还 做 绝对值 用 甲 除以 做 最大公约数 用 甲、乙 乘 做 绝对值 用 乙。
完。

阶乘 接受 n：
    若 n 小于或等于 1：
        返还 1。
    完。
    可变 结果 为 1。
    可变 序号 为 2。
    只要 序号 小于或等于 n：
        结果 为 结果 乘 序号。
        序号 为 序号 加 1。
    完。
    返还 结果。
完。

组合数 接受 n、k：
    若 k 大于 n：
        返还 0。
    完。
    若 k 等于 0 或者 k 等于 n：
        返还 1。
    完。
    若 k 大于 n 减 k：
        k 为 n 减 k。
    完。
    可变 结果 为 1。
    可变 序号 为 0。
    只要 序号 小于 k：
        结果 为 结果 乘 (n 减 序号) 除以 (序号 加 1)。
        序号 为 序号 加 1。
    完。
    返还 结果。
完。

对数2 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 结果 为 0。
    可变 值 为 n。
    只要 值 大于 1：
        值 为 值 右移 1。
        结果 为 结果 加 1。
    完。
    返还 结果。
完。

对数10 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 结果 为 0。
    可变 值 为 n。
    只要 值 大于或等于 10：
        值 为 值 除以 10。
        结果 为 结果 加 1。
    完。
    返还 结果。
完。

正弦定点 接受 角度：
    可变 x 为 角度 取余于 360。
    若 x 小于 0：
        x 为 x 加 360。
    完。
    可变 负 为 假。
    若 x 大于或等于 180：
        x 为 x 减 180。
        负 为 真。
    完。
    若 x 大于 90：
        x 为 180 减 x。
    完。
    可变 结果 为 x 乘 定点基 乘 71 除以 (4068 加 x 乘 x 乘 10 除以 1000)。
    若 负：
        结果 为 0 减 结果。
    完。
    返还 结果。
完。

余弦定点 接受 角度：
    返还 做 正弦定点 用 角度 加 90。
完。

线性插值 接受 甲、乙、比例：
    返还 甲 加 (乙 减 甲) 乘 比例 除以 定点基。
完。

映射范围 接受 值、源起、源终、目起、目终：
    若 源终 等于 源起：
        返还 目起。
    完。
    返还 目起 加 (值 减 源起) 乘 (目终 减 目起) 除以 (源终 减 源起)。
完。

是素数 接受 n：
    若 n 小于 2：
        返还 假。
    完。
    若 n 小于 4：
        返还 真。
    完。
    若 n 取余于 2 等于 0 或者 n 取余于 3 等于 0：
        返还 假。
    完。
    可变 除数 为 5。
    只要 除数 乘 除数 小于或等于 n：
        若 n 取余于 除数 等于 0 或者 n 取余于 (除数 加 2) 等于 0：
            返还 假。
        完。
        除数 为 除数 加 6。
    完。
    返还 真。
完。

斐波那契 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    若 n 等于 1：
        返还 1。
    完。
    可变 前前 为 0。
    可变 前 为 1。
    可变 序号 为 2。
    只要 序号 小于或等于 n：
        临 为 前前 加 前。
        前前 为 前。
        前 为 临。
        序号 为 序号 加 1。
    完。
    返还 前。
完。

位数 接受 n：
    若 n 等于 0：
        返还 1。
    完。
    可变 值 为 做 绝对值 用 n。
    可变 计 为 0。
    只要 值 大于 0：
        值 为 值 除以 10。
        计 为 计 加 1。
    完。
    返还 计。
完。

取整上 接受 甲、乙：
    若 乙 等于 0：
        返还 0。
    完。
    返还 (甲 加 乙 减 1) 除以 乙。
完。

对齐上 接受 值、对齐：
    若 对齐 小于或等于 0：
        返还 值。
    完。
    返还 值 加 对齐 减 1 位与 按位取反 (对齐 减 1)。
完。

可变 AI_原生数学_调用数 为 0。

可变 AI_原生数学_异常数 为 0。

AI_原生数学_监控 接受 零参：
    AI_原生数学_调用数 为 AI_原生数学_调用数 加 1。
    返还 AI_原生数学_异常数 等于 0。
完。

AI_原生数学_健康检查 接受 零参：
    若 AI_原生数学_调用数 大于 0 并且 AI_原生数学_异常数 乘 100 除以 AI_原生数学_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
