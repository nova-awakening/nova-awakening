《类型判断》

引用《..·中枢·系统调用表》。

引用《..·推理·通信渠道》。

引用《..·感知·内核直连层》。

引用《..·执行·控制台输出》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《..·执行·底层内存操》。

JS类型_Undefined 为 0。

JS类型_Null 为 1。

JS类型_Boolean 为 2。

JS类型_Number 为 3。

JS类型_String 为 4。

JS类型_符号 为 5。

JS类型_BigInt 为 6。

JS类型_Object 为 7。

JS类型_Function 为 8。

JS类型_异步Function 为 9。

JS类型_Array 为 10。

JS类型_RegExp 为 11。

JS类型_Date 为 12。

JS类型_Error 为 13。

JS类型_Promise 为 14。

JS类型_Map 为 15。

JS类型_Set 为 16。

JS类型_WeakMap 为 17。

JS类型_WeakSet 为 18。

JS类型_TypedArray 为 19。

JS类型_ArrayBuffer 为 20。

JS类型_Generator 为 21。

JS类型_Proxy 为 22。

TYPEOF_STR_undefined 为 0。

TYPEOF_STR_boolean 为 1。

TYPEOF_STR_number 为 2。

TYPEOF_STR_string 为 3。

TYPEOF_STR_symbol 为 4。

TYPEOF_STR_bigint 为 5。

TYPEOF_STR_object 为 6。

TYPEOF_STR_function 为 7。

属性_可写 为 1。

属性_可枚举 为 2。

属性_可配置 为 4。

数组_初始容量 为 8。

数组_增长因子 为 2。

数组_最大长度 为 4294967295。

AI_类型缓存大小 为 32。

AI类型预测缓存 是 一种 事物，含 基址、容量、命中数、未命中数、总查询数。

可变 缓存_undefined 为 0。

可变 缓存_boolean 为 0。

可变 缓存_number 为 0。

可变 缓存_string 为 0。

可变 缓存_symbol 为 0。

可变 缓存_bigint 为 0。

可变 缓存_object 为 0。

可变 缓存_function 为 0。

TypeOf_初始化缓存 接受 Nova运行时实例：
    若 Nova运行时实例 等于 0：
        返还。
    完。
    缓存_undefined 为 做 Nova值_创建_String 用 Nova运行时实例、「undefined」。
    缓存_boolean 为 做 Nova值_创建_String 用 Nova运行时实例、「boolean」。
    缓存_number 为 做 Nova值_创建_String 用 Nova运行时实例、「number」。
    缓存_string 为 做 Nova值_创建_String 用 Nova运行时实例、「string」。
    缓存_symbol 为 做 Nova值_创建_String 用 Nova运行时实例、「symbol」。
    缓存_bigint 为 做 Nova值_创建_String 用 Nova运行时实例、「bigint」。
    缓存_object 为 做 Nova值_创建_String 用 Nova运行时实例、「object」。
    缓存_function 为 做 Nova值_创建_String 用 Nova运行时实例、「function」。
完。

JSValue_TypeOf 接受 Nova运行时实例、值：
    若 值 等于 0：
        返还 做 Nova值_创建_String 用 Nova运行时实例、「undefined」。
    完。
    类型 为 做 内存_读32 用 值。
    若 类型 等于 JS类型_Undefined：
        返还 做 Nova值_创建_String 用 Nova运行时实例、「undefined」。
    完。
    若 类型 等于 JS类型_Null：
        返还 做 Nova值_创建_String 用 Nova运行时实例、「object」。
    完。
    若 类型 等于 JS类型_Boolean：
        返还 做 Nova值_创建_String 用 Nova运行时实例、「boolean」。
    完。
    若 类型 等于 JS类型_Number：
        返还 做 Nova值_创建_String 用 Nova运行时实例、「number」。
    完。
    若 类型 等于 JS类型_String：
        返还 做 Nova值_创建_String 用 Nova运行时实例、「string」。
    完。
    若 类型 等于 JS类型_符号：
        返还 做 Nova值_创建_String 用 Nova运行时实例、「symbol」。
    完。
    若 类型 等于 JS类型_BigInt：
        返还 做 Nova值_创建_String 用 Nova运行时实例、「bigint」。
    完。
    若 类型 等于 JS类型_Function 或者 类型 等于 JS类型_异步Function：
        返还 做 Nova值_创建_String 用 Nova运行时实例、「function」。
    完。
    返还 做 Nova值_创建_String 用 Nova运行时实例、「object」。
完。

Nova数组_获取 接受 Nova运行时实例、arr、索引：
    若 arr 等于 0：
        返还 Nova运行时实例 的 undefined值。
    完。
    obj数据 为 做 内存_读64 用 arr 加 40。
    若 obj数据 等于 0：
        返还 Nova运行时实例 的 undefined值。
    完。
    长度 为 做 内存_读32 用 obj数据 加 40。
    若 索引 小于 0 或者 索引 大于或等于 长度：
        返还 Nova运行时实例 的 undefined值。
    完。
    元素 为 做 内存_读64 用 obj数据 加 32。
    返还 做 内存_读64 用 元素 加 索引 乘 8。
完。

Nova数组_设置 接受 Nova运行时实例、arr、索引、值：
    若 arr 等于 0：
        返还。
    完。
    obj数据 为 做 内存_读64 用 arr 加 40。
    若 obj数据 等于 0：
        返还。
    完。
    容量 为 做 内存_读32 用 obj数据 加 44。
    若 索引 大于或等于 容量：
        新容量 为 容量。
        只要 新容量 小于或等于 索引：
            新容量 为 新容量 乘 2。
        完。
        旧元素 为 做 内存_读64 用 obj数据 加 32。
        新元素 为 做 堆_分配_数组 用 新容量 乘 8。
        做 内存_复制 用 新元素、旧元素、容量 乘 8。
        做 堆_释放 用 旧元素。
        做 内存_写64 用 obj数据 加 32、新元素。
        做 内存_写32 用 obj数据 加 44、新容量。
    完。
    元素 为 做 内存_读64 用 obj数据 加 32。
    做 内存_写64 用 元素 加 索引 乘 8、值。
    长度 为 做 内存_读32 用 obj数据 加 40。
    若 索引 大于或等于 长度：
        做 内存_写32 用 obj数据 加 40、索引 加 1。
    完。
完。

Nova数组_Push 接受 Nova运行时实例、arr、值：
    obj数据 为 做 内存_读64 用 arr 加 40。
    若 obj数据 等于 0：
        返还。
    完。
    长度 为 做 内存_读32 用 obj数据 加 40。
    做 Nova数组_设置 用 Nova运行时实例、arr、长度、值。
完。

Nova数组_Pop 接受 Nova运行时实例、arr：
    obj数据 为 做 内存_读64 用 arr 加 40。
    若 obj数据 等于 0：
        返还 Nova运行时实例 的 undefined值。
    完。
    长度 为 做 内存_读32 用 obj数据 加 40。
    若 长度 等于 0：
        返还 Nova运行时实例 的 undefined值。
    完。
    元素 为 做 内存_读64 用 obj数据 加 32。
    值 为 做 内存_读64 用 元素 加 (长度 减 1) 乘 8。
    做 内存_写32 用 obj数据 加 40、长度 减 1。
    返还 值。
完。

Nova数组_Length 接受 Nova运行时实例、arr：
    obj数据 为 做 内存_读64 用 arr 加 40。
    若 obj数据 等于 0：
        返还 0。
    完。
    返还 做 内存_读32 用 obj数据 加 40。
完。

Nova对象_获取所有键 接受 Nova运行时实例、obj：
    若 obj 等于 0：
        返还 做 Nova数组_创建 用 Nova运行时实例、0。
    完。
    结果 为 做 Nova数组_创建 用 Nova运行时实例、8。
    obj数据 为 做 内存_读64 用 obj 加 40。
    若 obj数据 等于 0：
        返还 结果。
    完。
    属性数 为 做 内存_读32 用 obj数据 加 24。
    属性存储 为 做 内存_读64 用 obj数据 加 16。
    可变 i 为 0。
    只要 i 小于 属性数：
        偏移 为 属性存储 加 i 乘 32。
        特性 为 做 内存_读32 用 偏移 加 24。
        若 特性 位与 属性_可枚举：
            键 为 做 内存_读64 用 偏移。
            做 Nova数组_Push 用 Nova运行时实例、结果、键。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

JSValue_ToBoolean 接受 Nova运行时实例、值：
    若 值 等于 0：
        返还 假。
    完。
    类型 为 做 内存_读32 用 值。
    若 类型 等于 JS类型_Undefined：
        返还 假。
    完。
    若 类型 等于 JS类型_Null：
        返还 假。
    完。
    若 类型 等于 JS类型_Boolean：
        返还 做 内存_读32 用 值 加 8 不等于 0。
    完。
    若 类型 等于 JS类型_Number：
        数值 为 做 内存_读64 用 值 加 8。
        返还 数值 不等于 0。
    完。
    若 类型 等于 JS类型_String：
        长度 为 做 内存_读32 用 值 加 8。
        返还 长度 大于 0。
    完。
    若 类型 等于 JS类型_BigInt：
        返还 做 内存_读64 用 值 加 8 不等于 0。
    完。
    若 类型 等于 JS类型_符号：
        返还 真。
    完。
    返还 真。
完。

JSValue_ToNumber 接受 Nova运行时实例、值：
    若 值 等于 0 或者 Nova运行时实例 等于 0：
        返还 0。
    完。
    类型 为 做 内存_读32 用 值。
    若 类型 等于 JS类型_Undefined：
        返还 0。
    完。
    若 类型 等于 JS类型_Null：
        返还 0。
    完。
    若 类型 等于 JS类型_Boolean：
        返还 做 内存_读32 用 值 加 8。
    完。
    若 类型 等于 JS类型_Number：
        返还 做 内存_读64 用 值 加 8。
    完。
    若 类型 等于 JS类型_String：
        字符串指针 为 做 内存_读64 用 值 加 16。
        长度 为 做 内存_读32 用 值 加 8。
        返还 做 字符串_解析数字 用 字符串指针、长度。
    完。
    若 类型 等于 JS类型_BigInt：
        返还 做 内存_读64 用 值 加 8。
    完。
    返还 0。
完。

JSValue_ToInt32 接受 Nova运行时实例、值：
    数值 为 做 JSValue_ToNumber 用 Nova运行时实例、值。
    返还 数值 位与 0xFFFFFFFF。
完。

JSValue_ToUint32 接受 Nova运行时实例、值：
    数值 为 做 JSValue_ToNumber 用 Nova运行时实例、值。
    返还 数值 位与 0xFFFFFFFF。
完。

JSValue_ToString 接受 Nova运行时实例、值：
    若 值 等于 0 或者 Nova运行时实例 等于 0：
        返还 「undefined」。
    完。
    类型 为 做 内存_读32 用 值。
    若 类型 等于 JS类型_Undefined：
        返还 「undefined」。
    完。
    若 类型 等于 JS类型_Null：
        返还 「null」。
    完。
    若 类型 等于 JS类型_Boolean：
        若 做 内存_读32 用 值 加 8 不等于 0：
            返还 「true」。
        完。
        返还 「false」。
    完。
    若 类型 等于 JS类型_Number：
        返还 做 转字符串 用 做 内存_读64 用 值 加 8。
    完。
    若 类型 等于 JS类型_String：
        返还 做 内存_读64 用 值 加 16。
    完。
    若 类型 等于 JS类型_符号：
        返还 「Symbol()」。
    完。
    若 类型 等于 JS类型_BigInt：
        返还 做 转字符串 用 做 内存_读64 用 值 加 8。
    完。
    返还 「[object Object]」。
完。

Nova值_创建_Undefined 接受 Nova运行时实例：
    若 Nova运行时实例 等于 0：
        返还 0。
    完。
    返还 Nova运行时实例 的 undefined值。
完。

Nova值_创建_Null 接受 Nova运行时实例：
    若 Nova运行时实例 等于 0：
        返还 0。
    完。
    返还 Nova运行时实例 的 null值。
完。

Nova值_创建_Boolean 接受 Nova运行时实例、值：
    若 Nova运行时实例 等于 0：
        返还 0。
    完。
    若 值 等于 真：
        返还 Nova运行时实例 的 true值。
    完。
    返还 Nova运行时实例 的 false值。
完。

Nova值_创建_Int 接受 Nova运行时实例、整数值：
    返还 整数值 乘 2。
完。

Nova值_创建_Number 接受 Nova运行时实例、数值：
    若 Nova运行时实例 等于 0：
        返还 0。
    完。
    若 数值 大于或等于 负 1073741824 并且 数值 小于或等于 1073741823：
        返还 数值 乘 2。
    完。
    obj 为 做 堆_分配 用 16。
    若 obj 等于 0：
        返还 0。
    完。
    做 内存_写32 用 obj、JS类型_Number。
    做 内存_写64 用 obj 加 8、数值。
    返还 obj。
完。

Nova值_创建_String 接受 Nova运行时实例、字符串：
    若 Nova运行时实例 等于 0：
        返还 0。
    完。
    长度 为 做 字节长度 用 字符串。
    obj 为 做 堆_分配 用 24 加 长度 加 1。
    若 obj 等于 0：
        返还 0。
    完。
    做 内存_写32 用 obj、JS类型_String。
    做 内存_写32 用 obj 加 8、长度。
    做 内存_写64 用 obj 加 16、obj 加 24。
    可变 i 为 0。
    只要 i 小于 长度：
        做 内存_写8 用 obj 加 24 加 i、做 字节取 用 字符串、i。
        i 为 i 加 1。
    完。
    做 内存_写8 用 obj 加 24 加 长度、0。
    返还 obj。
完。

JSValue_IsSmi 接受 值：
    返还 值 位与 1 等于 0。
完。

JSValue_GetSmi 接受 值：
    返还 值 除以 2。
完。

JSValue_IsNumber 接受 值：
    若 做 JSValue_IsSmi 用 值：
        返还 真。
    完。
    若 值 等于 0：
        返还 假。
    完。
    返还 做 内存_读32 用 值 等于 JS类型_Number。
完。

JSValue_IsObject 接受 值：
    若 值 等于 0：
        返还 假。
    完。
    类型 为 做 内存_读32 用 值。
    返还 类型 大于或等于 JS类型_Object。
完。

JSValue_IsFunction 接受 值：
    若 值 等于 0：
        返还 假。
    完。
    类型 为 做 内存_读32 用 值。
    返还 类型 等于 JS类型_Function 或者 类型 等于 JS类型_异步Function。
完。

JSValue_IsArray 接受 值：
    若 值 等于 0：
        返还 假。
    完。
    返还 做 内存_读32 用 值 等于 JS类型_Array。
完。

字符串_解析数字 接受 指针、长度：
    若 指针 等于 0 或者 长度 等于 0：
        返还 0。
    完。
    可变 结果 为 0。
    可变 符号 为 1。
    可变 位置 为 0。
    只要 位置 小于 长度：
        c 为 做 内存_读8 用 指针 加 位置。
        若 c 不等于 32 并且 c 不等于 9 并且 c 不等于 10 并且 c 不等于 13：
            跳出。
        完。
        位置 为 位置 加 1。
    完。
    若 位置 小于 长度：
        c 为 做 内存_读8 用 指针 加 位置。
        若 c 等于 45：
            符号 为 负 1。
            位置 为 位置 加 1。
        否则若 c 等于 43：
            位置 为 位置 加 1。
        完。
    完。
    只要 位置 小于 长度：
        c 为 做 内存_读8 用 指针 加 位置。
        若 c 大于或等于 48 并且 c 小于或等于 57：
            结果 为 结果 乘 10 加 (c 减 48)。
        否则：
            跳出。
        完。
        位置 为 位置 加 1。
    完。
    返还 结果 乘 符号。
完。

AI类型缓存_创建 接受 零参：
    可变 缓存 为 做 堆_分配 用 AI类型预测缓存。
    若 缓存 等于 0：
        返还 空。
    完。
    缓存 的 容量 为 AI_类型缓存大小。
    缓存 的 基址 为 做 平台_申请内存 用 缓存 的 容量 乘 8。
    若 缓存 的 基址 不等于 0：
        做 内存_清零 用 缓存 的 基址、缓存 的 容量 乘 8。
    完。
    缓存 的 命中数 为 0。
    缓存 的 未命中数 为 0。
    缓存 的 总查询数 为 0。
    返还 缓存。
完。

AI类型缓存_销毁 接受 缓存：
    若 缓存 等于 空：
        返还。
    完。
    若 缓存 的 基址 不等于 0：
        做 平台_释放内存 用 缓存 的 基址、缓存 的 容量 乘 8。
    完。
    做 堆_释放 用 缓存。
完。

AI类型缓存_查询 接受 缓存、对象地址：
    若 缓存 等于 空 或者 缓存 的 基址 等于 0：
        返还 负 1。
    完。
    缓存 的 总查询数 为 缓存 的 总查询数 加 1。
    哈希 为 (对象地址 右移 4) 取余于 缓存 的 容量。
    偏移 为 缓存 的 基址 加 哈希 乘 8。
    存储地址 为 做 内存_读32 用 偏移。
    若 存储地址 等于 对象地址 位与 0xFFFFFFFF：
        缓存 的 命中数 为 缓存 的 命中数 加 1。
        返还 做 内存_读32 用 偏移 加 4。
    完。
    缓存 的 未命中数 为 缓存 的 未命中数 加 1。
    返还 负 1。
完。

AI类型缓存_记录 接受 缓存、对象地址、类型：
    若 缓存 等于 空 或者 缓存 的 基址 等于 0：
        返还。
    完。
    哈希 为 (对象地址 右移 4) 取余于 缓存 的 容量。
    偏移 为 缓存 的 基址 加 哈希 乘 8。
    做 内存_写32 用 偏移、对象地址 位与 0xFFFFFFFF。
    做 内存_写32 用 偏移 加 4、类型。
完。

JSValue_StrictEqual 接受 Nova运行时实例、a、b：
    若 a 等于 b：
        返还 真。
    完。
    若 a 等于 0 或者 b 等于 0：
        返还 假。
    完。
    类型a 为 做 内存_读32 用 a。
    类型b 为 做 内存_读32 用 b。
    若 类型a 不等于 类型b：
        返还 假。
    完。
    若 类型a 等于 JS类型_Number：
        返还 做 内存_读64 用 a 加 8 等于 做 内存_读64 用 b 加 8。
    完。
    若 类型a 等于 JS类型_String：
        长度a 为 做 内存_读32 用 a 加 8。
        长度b 为 做 内存_读32 用 b 加 8。
        若 长度a 不等于 长度b：
            返还 假。
        完。
        数据a 为 做 内存_读64 用 a 加 16。
        数据b 为 做 内存_读64 用 b 加 16。
        可变 i 为 0。
        只要 i 小于 长度a：
            若 做 内存_读8 用 数据a 加 i 不等于 做 内存_读8 用 数据b 加 i：
                返还 假。
            完。
            i 为 i 加 1。
        完。
        返还 真。
    完。
    若 类型a 等于 JS类型_Boolean：
        返还 做 内存_读32 用 a 加 8 等于 做 内存_读32 用 b 加 8。
    完。
    返还 假。
完。

可变 统计_typeof调用数 为 0。

可变 统计_ToBoolean调用数 为 0。

可变 统计_ToNumber调用数 为 0。

可变 统计_ToString调用数 为 0。

可变 统计_StrictEqual调用数 为 0。

类型操作_打印统计 接受 零参：
    做 打印 用 「[类型] ═══════════ 类型操作统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  typeof调用数:      」、做 转字符串 用 统计_typeof调用数。
    做 打印 用 做 字符串拼接 用 「  ToBoolean调用数:   」、做 转字符串 用 统计_ToBoolean调用数。
    做 打印 用 做 字符串拼接 用 「  ToNumber调用数:    」、做 转字符串 用 统计_ToNumber调用数。
    做 打印 用 做 字符串拼接 用 「  ToString调用数:    」、做 转字符串 用 统计_ToString调用数。
    做 打印 用 做 字符串拼接 用 「  StrictEqual调用数: 」、做 转字符串 用 统计_StrictEqual调用数。
    做 打印 用 「[类型] ═══════════════════════════════════════」。
完。

AI类型缓存_打印统计 接受 缓存：
    若 缓存 等于 空：
        返还。
    完。
    做 打印 用 「[AI类型] ── 类型预测缓存 ──」。
    做 打印 用 做 字符串拼接 用 「  总查询数:   」、做 转字符串 用 缓存 的 总查询数。
    做 打印 用 做 字符串拼接 用 「  命中数:     」、做 转字符串 用 缓存 的 命中数。
    做 打印 用 做 字符串拼接 用 「  未命中数:   」、做 转字符串 用 缓存 的 未命中数。
    若 缓存 的 总查询数 大于 0：
        命中率 为 缓存 的 命中数 乘 10000 除以 缓存 的 总查询数。
        做 打印 用 做 字符串拼接 用 「  命中率‱:   」、做 转字符串 用 命中率。
    完。
完。

JSValue_类型名称 接受 值：
    若 值 等于 0：
        返还 「undefined」。
    完。
    类型 为 做 内存_读32 用 值。
    若 类型 等于 JS类型_Undefined：
        返还 「Undefined」。
    完。
    若 类型 等于 JS类型_Null：
        返还 「Null」。
    完。
    若 类型 等于 JS类型_Boolean：
        返还 「Boolean」。
    完。
    若 类型 等于 JS类型_Number：
        返还 「Number」。
    完。
    若 类型 等于 JS类型_String：
        返还 「String」。
    完。
    若 类型 等于 JS类型_符号：
        返还 「Symbol」。
    完。
    若 类型 等于 JS类型_BigInt：
        返还 「BigInt」。
    完。
    若 类型 等于 JS类型_Object：
        返还 「Object」。
    完。
    若 类型 等于 JS类型_Function：
        返还 「Function」。
    完。
    若 类型 等于 JS类型_Array：
        返还 「Array」。
    完。
    若 类型 等于 JS类型_RegExp：
        返还 「RegExp」。
    完。
    若 类型 等于 JS类型_Date：
        返还 「Date」。
    完。
    若 类型 等于 JS类型_Promise：
        返还 「Promise」。
    完。
    若 类型 等于 JS类型_Map：
        返还 「Map」。
    完。
    若 类型 等于 JS类型_Set：
        返还 「Set」。
    完。
    返还 「Unknown」。
完。

本章 收束。
