《进程接口》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·系统调用入口》。

进程_获取PID 接受 零参：
    返还 做 系统调用_分发 用 SYS_getpid、0、0、0、0、0。
完。

进程_获取PPID 接受 零参：
    返还 做 系统调用_分发 用 SYS_getppid、0、0、0、0、0。
完。

进程_获取UID 接受 零参：
    返还 做 系统调用_分发 用 SYS_getuid、0、0、0、0、0。
完。

进程_获取GID 接受 零参：
    返还 做 系统调用_分发 用 SYS_getgid、0、0、0、0、0。
完。

进程_获取EUID 接受 零参：
    返还 做 系统调用_分发 用 SYS_geteuid、0、0、0、0、0。
完。

进程_获取EGID 接受 零参：
    返还 做 系统调用_分发 用 SYS_getegid、0、0、0、0、0。
完。

进程_设UID 接受 uid：
    返还 做 系统调用_分发 用 SYS_setuid、uid、0、0、0、0。
完。

进程_设GID 接受 gid：
    返还 做 系统调用_分发 用 SYS_setgid、gid、0、0、0、0。
完。

进程_fork 接受 零参：
    返还 做 系统调用_分发 用 SYS_fork、0、0、0、0、0。
完。

进程_exit 接受 码：
    做 系统调用_分发 用 SYS_exit、码、0、0、0、0。
完。

进程_exec 接受 路径、参数：
    返还 做 系统调用_分发 用 SYS_execve、路径、参数、0、0、0。
完。

进程_execve 接受 路径、参数、环境：
    返还 做 系统调用_分发 用 SYS_execve、路径、参数、环境、0、0。
完。

进程_wait 接受 零参：
    返还 做 系统调用_分发 用 SYS_wait4、负 1、0、0、0、0。
完。

进程_wait_pid 接受 pid：
    返还 做 系统调用_分发 用 SYS_wait4、pid、0、0、0、0。
完。

进程_wait_nohang 接受 pid：
    返还 做 系统调用_分发 用 SYS_wait4、pid、0、WNOHANG、0、0。
完。

WNOHANG 为 1。

WUNTRACED 为 2。

进程_退出码 接受 状态：
    返还 状态 右移 8 位与 0xFF。
完。

进程_是正常退出 接受 状态：
    返还 状态 位与 0x7F 等于 0。
完。

进程_是信号终止 接受 状态：
    返还 状态 位与 0x7F 不等于 0 并且 状态 位与 0x7F 不等于 0x7F。
完。

进程_终止信号 接受 状态：
    返还 状态 位与 0x7F。
完。

进程_kill 接受 pid、sig：
    返还 做 系统调用_分发 用 SYS_kill、pid、sig、0、0、0。
完。

进程_注册信号 接受 信号号、处理函数：
    返还 做 系统调用_分发 用 SYS_sigaction、信号号、处理函数、0、0、0。
完。

进程_信号屏蔽 接受 如何、信号集：
    返还 做 系统调用_分发 用 SYS_sigprocmask、如何、信号集、0、0、0。
完。

进程_暂停 接受 零参：
    返还 做 系统调用_分发 用 SYS_pause、0、0、0、0、0。
完。

进程_发送信号组 接受 pgid、sig：
    返还 做 系统调用_分发 用 SYS_kill、负 pgid、sig、0、0、0。
完。

SIG_BLOCK 为 0。

SIG_UNBLOCK 为 1。

SIG_SETMASK 为 2。

进程_获取环境 接受 名：
    返还 做 系统调用_分发 用 SYS_getenv、名、0、0、0、0。
完。

进程_设置环境 接受 名、值：
    返还 做 系统调用_分发 用 SYS_setenv、名、值、0、0、0。
完。

进程_删除环境 接受 名：
    返还 做 系统调用_分发 用 SYS_unsetenv、名、0、0、0、0。
完。

进程_当前目录 接受 零参：
    返还 做 系统调用_分发 用 SYS_getcwd、0、256、0、0、0。
完。

进程_切换目录 接受 路径：
    返还 做 系统调用_分发 用 SYS_chdir、路径、0、0、0、0。
完。

进程_切换根目录 接受 路径：
    返还 做 系统调用_分发 用 SYS_chroot、路径、0、0、0、0。
完。

进程_创建管道 接受 零参：
    管道fd 为 一组：0、0。
    结果 为 做 系统调用_分发 用 SYS_pipe2、管道fd、0、0、0、0。
    若 结果 小于 0：
        返还 空。
    完。
    返还 管道fd。
完。

进程_创建管道_非阻塞 接受 零参：
    管道fd 为 一组：0、0。
    结果 为 做 系统调用_分发 用 SYS_pipe2、管道fd、O_NONBLOCK、0、0、0。
    若 结果 小于 0：
        返还 空。
    完。
    返还 管道fd。
完。

O_NONBLOCK 为 0x800。

O_CLOEXEC 为 0x80000。

进程_关闭fd 接受 fd：
    返还 做 系统调用_分发 用 SYS_close、fd、0、0、0、0。
完。

进程_复制fd 接受 旧fd：
    返还 做 系统调用_分发 用 SYS_dup、旧fd、0、0、0、0。
完。

进程_复制fd2 接受 旧fd、新fd：
    返还 做 系统调用_分发 用 SYS_dup2、旧fd、新fd、0、0、0。
完。

进程_获取PGID 接受 pid：
    返还 做 系统调用_分发 用 SYS_getpgid、pid、0、0、0、0。
完。

进程_设PGID 接受 pid、pgid：
    返还 做 系统调用_分发 用 SYS_setpgid、pid、pgid、0、0、0。
完。

进程_获取SID 接受 零参：
    返还 做 系统调用_分发 用 SYS_getsid、0、0、0、0、0。
完。

进程_新会话 接受 零参：
    返还 做 系统调用_分发 用 SYS_setsid、0、0、0、0、0。
完。

进程_获取优先级 接受 pid：
    返还 做 系统调用_分发 用 SYS_getpriority、0、pid、0、0、0。
完。

进程_设优先级 接受 pid、优先级：
    返还 做 系统调用_分发 用 SYS_setpriority、0、pid、优先级、0、0。
完。

进程_获取资源限制 接受 资源：
    返还 做 系统调用_分发 用 SYS_getrlimit、资源、0、0、0、0。
完。

进程_设资源限制 接受 资源、软限、硬限：
    返还 做 系统调用_分发 用 SYS_setrlimit、资源、软限、硬限、0、0。
完。

RLIMIT_CPU 为 0。

RLIMIT_FSIZE 为 1。

RLIMIT_DATA 为 2。

RLIMIT_STACK 为 3。

RLIMIT_CORE 为 4。

RLIMIT_NOFILE 为 7。

RLIMIT_NPROC 为 6。

进程_启动 接受 路径、参数：
    pid 为 做 进程_fork。
    若 pid 等于 0：
        做 进程_exec 用 路径、参数。
        做 进程_exit 用 127。
    完。
    返还 pid。
完。

进程_启动并等待 接受 路径、参数：
    pid 为 做 进程_启动 用 路径、参数。
    若 pid 小于 0：
        返还 pid。
    完。
    返还 做 进程_wait_pid 用 pid。
完。

进程_启动管道 接受 路径、参数：
    管道 为 做 进程_创建管道。
    若 管道 等于 空：
        返还 空。
    完。
    pid 为 做 进程_fork。
    若 pid 等于 0：
        做 进程_关闭fd 用 管道 的 第 0 项。
        做 进程_复制fd2 用 管道 的 第 1 项、1。
        做 进程_关闭fd 用 管道 的 第 1 项。
        做 进程_exec 用 路径、参数。
        做 进程_exit 用 127。
    完。
    做 进程_关闭fd 用 管道 的 第 1 项。
    返还 一组：pid、管道 的 第 0 项。
完。

进程_守护化 接受 零参：
    pid 为 做 进程_fork。
    若 pid 小于 0：
        返还 pid。
    完。
    若 pid 大于 0：
        做 进程_exit 用 0。
    完。
    做 进程_新会话。
    做 进程_切换目录 用 「/」。
    做 进程_关闭fd 用 0。
    做 进程_关闭fd 用 1。
    做 进程_关闭fd 用 2。
    返还 0。
完。

进程_睡眠 接受 秒：
    返还 做 系统调用_分发 用 SYS_nanosleep、秒 乘 1000000000、0、0、0、0。
完。

进程_睡眠毫秒 接受 毫秒：
    返还 做 系统调用_分发 用 SYS_nanosleep、毫秒 乘 1000000、0、0、0、0。
完。

进程_睡眠微秒 接受 微秒：
    返还 做 系统调用_分发 用 SYS_nanosleep、微秒 乘 1000、0、0、0、0。
完。

进程_时间 接受 零参：
    返还 做 系统调用_分发 用 SYS_time、0、0、0、0、0。
完。

进程_时钟 接受 零参：
    返还 做 系统调用_分发 用 SYS_clock_gettime、0、0、0、0、0。
完。

进程_mmap 接受 地址、长度、保护、标志、fd、偏移：
    返还 做 系统调用_分发 用 SYS_mmap、地址、长度、保护、标志、fd。
完。

进程_munmap 接受 地址、长度：
    返还 做 系统调用_分发 用 SYS_munmap、地址、长度、0、0、0。
完。

PROT_READ 为 1。

PROT_WRITE 为 2。

PROT_EXEC 为 4。

MAP_PRIVATE 为 2。

MAP_ANONYMOUS 为 0x20。

进程_是root 接受 零参：
    返还 做 进程_获取EUID 等于 0。
完。

可变 AI_进程接口_负载窗口 为 一组：（空）。

AI_进程接口_负载预测 接受 当前负载：
    做 追加 用 AI_进程接口_负载窗口、当前负载。
    若 做 计数 用 AI_进程接口_负载窗口 大于 20：
        做 列表_移除 用 AI_进程接口_负载窗口、0。
    完。
    若 做 计数 用 AI_进程接口_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_进程接口_负载窗口。
    趋势 为 AI_进程接口_负载窗口 的 第 n 减 1 项 减 AI_进程接口_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_进程接口_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
