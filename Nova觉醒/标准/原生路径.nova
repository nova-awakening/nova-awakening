《原生路径》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·感知·内核直连层》。

引用《..·中枢·字符串工具》。

分隔符 为 47。

最大路径 为 4096。

最大组件 为 128。

路径_拼接 接受 甲、乙：
    甲长 为 做 字串_长度 用 甲。
    乙长 为 做 字串_长度 用 乙。
    若 甲长 等于 0：
        返还 乙。
    完。
    若 乙长 等于 0：
        返还 甲。
    完。
    结果 为 做 堆_分配 用 甲长 加 乙长 加 2。
    做 内存_复制 用 结果、甲、甲长。
    若 做 内存_读8 用 甲 加 甲长 减 1 不等于 分隔符：
        做 内存_写8 用 结果 加 甲长、分隔符。
        做 内存_复制 用 结果 加 甲长 加 1、乙、乙长。
        做 内存_写8 用 结果 加 甲长 加 1 加 乙长、0。
    否则：
        做 内存_复制 用 结果 加 甲长、乙、乙长。
        做 内存_写8 用 结果 加 甲长 加 乙长、0。
    完。
    返还 结果。
完。

路径_最后分隔 接受 路径、长度：
    可变 位 为 长度 减 1。
    只要 位 大于或等于 0：
        若 做 内存_读8 用 路径 加 位 等于 分隔符：
            返还 位。
        完。
        位 为 位 减 1。
    完。
    返还 负 1。
完。

路径_目录名 接受 路径：
    长度 为 做 字串_长度 用 路径。
    最后 为 做 路径_最后分隔 用 路径、长度。
    若 最后 小于或等于 0：
        返还 「/」。
    完。
    结果 为 做 堆_分配 用 最后 加 1。
    做 内存_复制 用 结果、路径、最后。
    做 内存_写8 用 结果 加 最后、0。
    返还 结果。
完。

路径_文件名 接受 路径：
    长度 为 做 字串_长度 用 路径。
    最后 为 做 路径_最后分隔 用 路径、长度。
    若 最后 小于 0：
        返还 路径。
    完。
    名长 为 长度 减 最后 减 1。
    结果 为 做 堆_分配 用 名长 加 1。
    做 内存_复制 用 结果、路径 加 最后 加 1、名长。
    做 内存_写8 用 结果 加 名长、0。
    返还 结果。
完。

路径_扩展名 接受 路径：
    名 为 做 路径_文件名 用 路径。
    名长 为 做 字串_长度 用 名。
    可变 点位 为 名长 减 1。
    只要 点位 大于或等于 0：
        若 做 内存_读8 用 名 加 点位 等于 46：
            扩展长 为 名长 减 点位 减 1。
            结果 为 做 堆_分配 用 扩展长 加 1。
            做 内存_复制 用 结果、名 加 点位 加 1、扩展长。
            做 内存_写8 用 结果 加 扩展长、0。
            返还 结果。
        完。
        点位 为 点位 减 1。
    完。
    返还 「」。
完。

路径_无扩展名 接受 路径：
    长度 为 做 字串_长度 用 路径。
    可变 点位 为 长度 减 1。
    只要 点位 大于或等于 0：
        字节 为 做 内存_读8 用 路径 加 点位。
        若 字节 等于 46：
            结果 为 做 堆_分配 用 点位 加 1。
            做 内存_复制 用 结果、路径、点位。
            做 内存_写8 用 结果 加 点位、0。
            返还 结果。
        完。
        若 字节 等于 分隔符：
            跳出。
        完。
        点位 为 点位 减 1。
    完。
    返还 路径。
完。

路径_是绝对 接受 路径：
    若 做 字串_长度 用 路径 等于 0：
        返还 假。
    完。
    返还 做 内存_读8 用 路径 等于 分隔符。
完。

路径_规范化 接受 路径：
    长度 为 做 字串_长度 用 路径。
    结果 为 做 堆_分配 用 长度 加 1。
    可变 读位 为 0。
    可变 写位 为 0。
    只要 读位 小于 长度：
        字节 为 做 内存_读8 用 路径 加 读位。
        若 字节 等于 分隔符：
            若 写位 等于 0 或者 做 内存_读8 用 结果 加 写位 减 1 不等于 分隔符：
                做 内存_写8 用 结果 加 写位、分隔符。
                写位 为 写位 加 1。
            完。
        否则：
            做 内存_写8 用 结果 加 写位、字节。
            写位 为 写位 加 1。
        完。
        读位 为 读位 加 1。
    完。
    若 写位 大于 1 并且 做 内存_读8 用 结果 加 写位 减 1 等于 分隔符：
        写位 为 写位 减 1。
    完。
    做 内存_写8 用 结果 加 写位、0。
    返还 结果。
完。

路径_深度 接受 路径：
    长度 为 做 字串_长度 用 路径。
    可变 深度 为 0。
    可变 位 为 0。
    只要 位 小于 长度：
        若 做 内存_读8 用 路径 加 位 等于 分隔符：
            深度 为 深度 加 1。
        完。
        位 为 位 加 1。
    完。
    返还 深度。
完。

路径_匹配扩展 接受 路径、扩展：
    实际扩展 为 做 路径_扩展名 用 路径。
    返还 做 字串_比较 用 实际扩展、扩展 等于 0。
完。

路径_是隐藏 接受 路径：
    名 为 做 路径_文件名 用 路径。
    若 做 字串_长度 用 名 等于 0：
        返还 假。
    完。
    返还 做 内存_读8 用 名 等于 46。
完。

路径_父级 接受 路径、级数：
    可变 当前 为 路径。
    可变 层 为 0。
    只要 层 小于 级数：
        当前 为 做 路径_目录名 用 当前。
        层 为 层 加 1。
    完。
    返还 当前。
完。

路径_相等 接受 甲、乙：
    返还 做 字串_比较 用 甲、乙 等于 0。
完。

本章 收束。
