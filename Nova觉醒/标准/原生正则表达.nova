《原生正则表达》

引用《..·中枢·类型定义》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内核打印》。

引用《..·中枢·整数转字符串》。

正则_最大匹配 为 256。

正则_最大组 为 16。

正则匹配结果 是 一种 事物，含 起始、长度、文本。

正则统计 是 一种 事物，含 匹配调用数、匹配成功数、匹配失败数、包含调用数、查找调用数、查找全部调用数、替换调用数、分割调用数、回溯次数。

可变 全局_正则统计 为 空。

正则统计_初始化 接受 零参：
    若 全局_正则统计 等于 空：
        全局_正则统计 为 新建 正则统计。
        全局_正则统计 的 匹配调用数 为 0。
        全局_正则统计 的 匹配成功数 为 0。
        全局_正则统计 的 匹配失败数 为 0。
        全局_正则统计 的 包含调用数 为 0。
        全局_正则统计 的 查找调用数 为 0。
        全局_正则统计 的 查找全部调用数 为 0。
        全局_正则统计 的 替换调用数 为 0。
        全局_正则统计 的 分割调用数 为 0。
        全局_正则统计 的 回溯次数 为 0。
    完。
完。

正则_匹配 接受 模式、文本：
    做 正则统计_初始化。
    全局_正则统计 的 匹配调用数 为 全局_正则统计 的 匹配调用数 加 1。
    结果 为 做 正则_匹配_内部 用 模式、0、文本、0。
    若 结果：
        全局_正则统计 的 匹配成功数 为 全局_正则统计 的 匹配成功数 加 1。
    否则：
        全局_正则统计 的 匹配失败数 为 全局_正则统计 的 匹配失败数 加 1。
    完。
    返还 结果。
完。

正则_模式步长 接受 模式、pi：
    ch 为 做 字符串字符 用 模式、pi。
    若 ch 等于 92 并且 pi 加 1 小于 做 字符串长度 用 模式：
        返还 2。
    完。
    若 ch 等于 91：
        可变 j 为 pi 加 1。
        若 j 小于 做 字符串长度 用 模式 并且 做 字符串字符 用 模式、j 等于 93：
            j 为 j 加 1。
        完。
        只要 j 小于 做 字符串长度 用 模式 并且 做 字符串字符 用 模式、j 不等于 93：
            j 为 j 加 1。
        完。
        若 j 小于 做 字符串长度 用 模式：
            返还 j 减 pi 加 1。
        完。
    完。
    返还 1。
完。

可变 正则_递归深度 为 0。

正则_最大递归 为 10000。

正则_匹配_内部 接受 模式、pi、文本、ti：
    正则_递归深度 为 正则_递归深度 加 1。
    若 正则_递归深度 大于 正则_最大递归：
        正则_递归深度 为 正则_递归深度 减 1。
        返还 假。
    完。
    若 pi 大于或等于 做 字符串长度 用 模式：
        正则_递归深度 为 正则_递归深度 减 1。
        返还 ti 大于或等于 做 字符串长度 用 文本。
    完。
    模式字符 为 做 字符串字符 用 模式、pi。
    若 模式字符 等于 94 并且 pi 等于 0：
        返还 做 正则_匹配_内部 用 模式、pi 加 1、文本、ti。
    完。
    若 模式字符 等于 36 并且 pi 加 1 大于或等于 做 字符串长度 用 模式：
        返还 ti 大于或等于 做 字符串长度 用 文本。
    完。
    步长 为 做 正则_模式步长 用 模式、pi。
    可变 下一位 为 pi 加 步长。
    可变 有星号 为 假。
    可变 有加号 为 假。
    可变 有问号 为 假。
    若 下一位 小于 做 字符串长度 用 模式：
        q 为 做 字符串字符 用 模式、下一位。
        若 q 等于 42：
            有星号 为 真。
        完。
        若 q 等于 43：
            有加号 为 真。
        完。
        若 q 等于 63：
            有问号 为 真。
        完。
    完。
    若 有星号：
        若 做 正则_匹配_内部 用 模式、下一位 加 1、文本、ti：
            返还 真。
        完。
        可变 j 为 ti。
        只要 j 小于 做 字符串长度 用 文本：
            若 非 做 正则_字符匹配_扩展 用 模式、pi、步长、做 字符串字符 用 文本、j：
                跳出。
            完。
            j 为 j 加 1。
            若 做 正则_匹配_内部 用 模式、下一位 加 1、文本、j：
                返还 真。
            完。
            做 正则统计_初始化。
            全局_正则统计 的 回溯次数 为 全局_正则统计 的 回溯次数 加 1。
        完。
        返还 假。
    完。
    若 有加号：
        可变 j 为 ti。
        只要 j 小于 做 字符串长度 用 文本：
            若 非 做 正则_字符匹配_扩展 用 模式、pi、步长、做 字符串字符 用 文本、j：
                跳出。
            完。
            j 为 j 加 1。
            若 做 正则_匹配_内部 用 模式、下一位 加 1、文本、j：
                返还 真。
            完。
            做 正则统计_初始化。
            全局_正则统计 的 回溯次数 为 全局_正则统计 的 回溯次数 加 1。
        完。
        返还 假。
    完。
    若 有问号：
        若 做 正则_匹配_内部 用 模式、下一位 加 1、文本、ti：
            返还 真。
        完。
        若 ti 小于 做 字符串长度 用 文本 并且 做 正则_字符匹配_扩展 用 模式、pi、步长、做 字符串字符 用 文本、ti：
            返还 做 正则_匹配_内部 用 模式、下一位 加 1、文本、ti 加 1。
        完。
        返还 假。
    完。
    若 ti 大于或等于 做 字符串长度 用 文本：
        返还 假。
    完。
    若 做 正则_字符匹配_扩展 用 模式、pi、步长、做 字符串字符 用 文本、ti：
        返还 做 正则_匹配_内部 用 模式、pi 加 步长、文本、ti 加 1。
    完。
    返还 假。
完。

正则_字符匹配_扩展 接受 模式、pi、步长、文本字符：
    模式字符 为 做 字符串字符 用 模式、pi。
    若 模式字符 等于 92 并且 步长 等于 2：
        转义 为 做 字符串字符 用 模式、pi 加 1。
        若 转义 等于 100：
            返还 做 正则_是数字 用 文本字符。
        完。
        若 转义 等于 68：
            返还 非 做 正则_是数字 用 文本字符。
        完。
        若 转义 等于 119：
            返还 做 正则_是单词字符 用 文本字符。
        完。
        若 转义 等于 87：
            返还 非 做 正则_是单词字符 用 文本字符。
        完。
        若 转义 等于 115：
            返还 做 正则_是空白 用 文本字符。
        完。
        若 转义 等于 83：
            返还 非 做 正则_是空白 用 文本字符。
        完。
        若 转义 等于 110：
            返还 文本字符 等于 10。
        完。
        若 转义 等于 116：
            返还 文本字符 等于 9。
        完。
        若 转义 等于 114：
            返还 文本字符 等于 13。
        完。
        返还 转义 等于 文本字符。
    完。
    若 模式字符 等于 91 并且 步长 大于 1：
        返还 做 正则_字符集匹配 用 模式、pi、步长、文本字符。
    完。
    若 模式字符 等于 46：
        返还 文本字符 不等于 10。
    完。
    返还 模式字符 等于 文本字符。
完。

正则_字符集匹配 接受 模式、pi、步长、文本字符：
    可变 取反 为 假。
    可变 起始 为 pi 加 1。
    若 起始 小于 pi 加 步长 并且 做 字符串字符 用 模式、起始 等于 94：
        取反 为 真。
        起始 为 起始 加 1。
    完。
    结束 为 pi 加 步长 减 1。
    可变 匹配 为 假。
    可变 j 为 起始。
    只要 j 小于 结束：
        若 j 加 2 小于 结束 并且 做 字符串字符 用 模式、j 加 1 等于 45：
            低 为 做 字符串字符 用 模式、j。
            高 为 做 字符串字符 用 模式、j 加 2。
            若 文本字符 大于或等于 低 并且 文本字符 小于或等于 高：
                匹配 为 真。
            完。
            j 为 j 加 3。
        否则：
            若 做 字符串字符 用 模式、j 等于 文本字符：
                匹配 为 真。
            完。
            j 为 j 加 1。
        完。
    完。
    若 取反：
        返还 非 匹配。
    完。
    返还 匹配。
完。

正则_字符匹配 接受 模式字符、文本字符：
    若 模式字符 等于 46：
        返还 文本字符 不等于 10。
    完。
    返还 模式字符 等于 文本字符。
完。

正则_包含 接受 文本、模式：
    做 正则统计_初始化。
    全局_正则统计 的 包含调用数 为 全局_正则统计 的 包含调用数 加 1。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        若 做 正则_匹配_内部 用 模式、0、文本、i：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

正则_查找 接受 文本、模式：
    做 正则统计_初始化。
    全局_正则统计 的 查找调用数 为 全局_正则统计 的 查找调用数 加 1。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        若 做 正则_匹配_内部 用 模式、0、文本、i：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

正则_查找_匹配长度 接受 文本、模式、起始：
    可变 长度 为 0。
    可变 j 为 起始。
    只要 j 小于或等于 做 字符串长度 用 文本：
        子串 为 做 字符串子串 用 文本、起始、j。
        若 做 正则_匹配 用 模式、子串：
            长度 为 j 减 起始。
        完。
        j 为 j 加 1。
    完。
    返还 长度。
完。

正则_查找全部 接受 文本、模式：
    做 正则统计_初始化。
    全局_正则统计 的 查找全部调用数 为 全局_正则统计 的 查找全部调用数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    可变 找到数 为 0。
    只要 i 小于 做 字符串长度 用 文本 并且 找到数 小于 正则_最大匹配：
        若 做 正则_匹配_内部 用 模式、0、文本、i：
            长度 为 做 正则_查找_匹配长度 用 文本、模式、i。
            若 长度 小于或等于 0：
                长度 为 1。
            完。
            项 为 新建 正则匹配结果。
            项 的 起始 为 i。
            项 的 长度 为 长度。
            项 的 文本 为 做 字符串子串 用 文本、i、i 加 长度。
            做 追加 用 结果、项。
            找到数 为 找到数 加 1。
            i 为 i 加 长度。
        否则：
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

正则_替换 接受 文本、模式、替换文本：
    做 正则统计_初始化。
    全局_正则统计 的 替换调用数 为 全局_正则统计 的 替换调用数 加 1。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        若 做 正则_匹配_内部 用 模式、0、文本、i：
            长度 为 做 正则_查找_匹配长度 用 文本、模式、i。
            若 长度 小于或等于 0：
                长度 为 1。
            完。
            结果 为 做 字符串拼接 用 结果、替换文本。
            i 为 i 加 长度。
        否则：
            结果 为 做 字符串拼接 用 结果、做 字符串子串 用 文本、i、i 加 1。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

正则_替换首个 接受 文本、模式、替换文本：
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        若 做 正则_匹配_内部 用 模式、0、文本、i：
            长度 为 做 正则_查找_匹配长度 用 文本、模式、i。
            若 长度 小于或等于 0：
                长度 为 1。
            完。
            前 为 做 字符串子串 用 文本、0、i。
            后 为 做 字符串子串 用 文本、i 加 长度、做 字符串长度 用 文本。
            返还 做 字符串拼接 用 前、做 字符串拼接 用 替换文本、后。
        完。
        i 为 i 加 1。
    完。
    返还 文本。
完。

正则_分割 接受 文本、模式：
    做 正则统计_初始化。
    全局_正则统计 的 分割调用数 为 全局_正则统计 的 分割调用数 加 1。
    可变 结果 为 一组：（空）。
    可变 上次 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        若 做 正则_匹配_内部 用 模式、0、文本、i：
            长度 为 做 正则_查找_匹配长度 用 文本、模式、i。
            若 长度 小于或等于 0：
                长度 为 1。
            完。
            做 追加 用 结果、做 字符串子串 用 文本、上次、i。
            上次 为 i 加 长度。
            i 为 上次。
        否则：
            i 为 i 加 1。
        完。
    完。
    做 追加 用 结果、做 字符串子串 用 文本、上次、做 字符串长度 用 文本。
    返还 结果。
完。

正则_计数匹配 接受 文本、模式：
    匹配列表 为 做 正则_查找全部 用 文本、模式。
    返还 做 计数 用 匹配列表。
完。

正则_是数字 接受 ch：
    返还 ch 大于或等于 48 并且 ch 小于或等于 57。
完。

正则_是字母 接受 ch：
    返还 ch 大于或等于 65 并且 ch 小于或等于 90 或者 ch 大于或等于 97 并且 ch 小于或等于 122。
完。

正则_是空白 接受 ch：
    返还 ch 等于 32 或者 ch 等于 9 或者 ch 等于 10 或者 ch 等于 13。
完。

正则_是单词字符 接受 ch：
    返还 做 正则_是字母 用 ch 或者 做 正则_是数字 用 ch 或者 ch 等于 95。
完。

正则_是十六进制 接受 ch：
    返还 做 正则_是数字 用 ch 或者 ch 大于或等于 65 并且 ch 小于或等于 70 或者 ch 大于或等于 97 并且 ch 小于或等于 102。
完。

正则_简单查找 接受 模式、文本：
    若 做 正则_匹配 用 模式、文本：
        返还 0。
    完。
    返还 0 减 1。
完。

正则_打印统计 接受 零参：
    做 正则统计_初始化。
    做 打印行 用 「=== 正则表达式统计 ===」。
    做 打印行 用 「匹配调用: 」 加 做 整数转字符串 用 全局_正则统计 的 匹配调用数。
    做 打印行 用 「匹配成功: 」 加 做 整数转字符串 用 全局_正则统计 的 匹配成功数。
    做 打印行 用 「匹配失败: 」 加 做 整数转字符串 用 全局_正则统计 的 匹配失败数。
    做 打印行 用 「包含调用: 」 加 做 整数转字符串 用 全局_正则统计 的 包含调用数。
    做 打印行 用 「查找调用: 」 加 做 整数转字符串 用 全局_正则统计 的 查找调用数。
    做 打印行 用 「查找全部: 」 加 做 整数转字符串 用 全局_正则统计 的 查找全部调用数。
    做 打印行 用 「替换调用: 」 加 做 整数转字符串 用 全局_正则统计 的 替换调用数。
    做 打印行 用 「分割调用: 」 加 做 整数转字符串 用 全局_正则统计 的 分割调用数。
    做 打印行 用 「回溯次数: 」 加 做 整数转字符串 用 全局_正则统计 的 回溯次数。
完。

正则_默认缓存大小 为 32。

正则缓存_创建 接受 最大大小：
    若 最大大小 小于或等于 0：
        最大大小 为 正则_默认缓存大小。
    完。
    返还 一组：一组：（空）、一组：（空）、0、最大大小。
完。

正则缓存_查找 接受 缓存、模式：
    可变 i 为 0。
    只要 i 小于 缓存 的 第 2 项：
        若 缓存 的 第 0 项 的 第 i 项 等于 模式：
            缓存 的 第 1 项 的 第 i 项 为 缓存 的 第 1 项 的 第 i 项 加 1。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

正则缓存_添加 接受 缓存、模式：
    若 做 正则缓存_查找 用 缓存、模式：
        返还 真。
    完。
    若 缓存 的 第 2 项 大于或等于 缓存 的 第 3 项：
        可变 最小命中 为 999999999。
        可变 最小索引 为 0。
        可变 j 为 0。
        只要 j 小于 缓存 的 第 2 项：
            若 缓存 的 第 1 项 的 第 j 项 小于 最小命中：
                最小命中 为 缓存 的 第 1 项 的 第 j 项。
                最小索引 为 j。
            完。
            j 为 j 加 1。
        完。
        做 列表_移除 用 缓存 的 第 0 项、最小索引。
        做 列表_移除 用 缓存 的 第 1 项、最小索引。
        缓存 的 第 2 项 为 缓存 的 第 2 项 减 1。
    完。
    做 追加 用 缓存 的 第 0 项、模式。
    做 追加 用 缓存 的 第 1 项、1。
    缓存 的 第 2 项 为 缓存 的 第 2 项 加 1。
    返还 真。
完。

正则缓存_匹配 接受 缓存、模式、文本：
    做 正则缓存_添加 用 缓存、模式。
    返还 做 正则_匹配 用 模式、文本。
完。

正则缓存_包含 接受 缓存、文本、模式：
    做 正则缓存_添加 用 缓存、模式。
    返还 做 正则_包含 用 文本、模式。
完。

正则缓存_查找全部 接受 缓存、文本、模式：
    做 正则缓存_添加 用 缓存、模式。
    返还 做 正则_查找全部 用 文本、模式。
完。

正则缓存_替换 接受 缓存、文本、模式、替换文本：
    做 正则缓存_添加 用 缓存、模式。
    返还 做 正则_替换 用 文本、模式、替换文本。
完。

正则缓存_大小 接受 缓存：
    返还 缓存 的 第 2 项。
完。

正则缓存_清空 接受 缓存：
    做 设置 用 缓存、0、一组：（空）。
    做 设置 用 缓存、1、一组：（空）。
    缓存 的 第 2 项 为 0。
完。

正则缓存_热门 接受 缓存、数量：
    可变 结果 为 一组：（空）。
    可变 已用 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 数量 并且 k 小于 缓存 的 第 2 项：
        可变 最大 为 负 1。
        可变 最大索引 为 0。
        可变 j 为 0。
        只要 j 小于 缓存 的 第 2 项：
            可变 已使用 为 假。
            可变 m 为 0。
            只要 m 小于 做 计数 用 已用：
                若 已用 的 第 m 项 等于 j：
                    已使用 为 真。
                完。
                m 为 m 加 1。
            完。
            若 非 已使用 并且 缓存 的 第 1 项 的 第 j 项 大于 最大：
                最大 为 缓存 的 第 1 项 的 第 j 项。
                最大索引 为 j。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、一组：缓存 的 第 0 项 的 第 最大索引 项、最大。
        做 追加 用 已用、最大索引。
        k 为 k 加 1。
    完。
    返还 结果。
完。

正则模式_整数 接受 零参：
    返还 「^\\d+$」。
完。

正则模式_小数 接受 零参：
    返还 「^\\d+\\.\\d+$」。
完。

正则模式_标识符 接受 零参：
    返还 「^[a-zA-Z_]\\w*$」。
完。

正则模式_十六进制 接受 零参：
    返还 「^0[xX][0-9a-fA-F]+$」。
完。

正则模式_空白行 接受 零参：
    返还 「^\\s*$」。
完。

正则模式_非空 接受 零参：
    返还 「\\S」。
完。

正则_是整数 接受 文本：
    返还 做 正则_匹配 用 做 正则模式_整数、文本。
完。

正则_是小数 接受 文本：
    返还 做 正则_匹配 用 做 正则模式_小数、文本。
完。

正则_是标识符 接受 文本：
    返还 做 正则_匹配 用 做 正则模式_标识符、文本。
完。

正则_去除数字 接受 文本：
    返还 做 正则_替换 用 文本、「\\d」、「」。
完。

正则_去除空白 接受 文本：
    返还 做 正则_替换 用 文本、「\\s」、「」。
完。

正则_仅保留数字 接受 文本：
    返还 做 正则_替换 用 文本、「\\D」、「」。
完。

正则_仅保留字母 接受 文本：
    返还 做 正则_替换 用 文本、「[^a-zA-Z]」、「」。
完。

正则_仅保留单词 接受 文本：
    返还 做 正则_替换 用 文本、「\\W」、「」。
完。

捨入匹配器_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、0。
完。

捨入匹配器_添加 接受 匹配器、模式、标签：
    做 追加 用 匹配器 的 第 0 项、模式。
    做 追加 用 匹配器 的 第 1 项、标签。
    匹配器 的 第 2 项 为 匹配器 的 第 2 项 加 1。
完。

捨入匹配器_匹配 接受 匹配器、文本：
    可变 i 为 0。
    只要 i 小于 匹配器 的 第 2 项：
        若 做 正则_包含 用 文本、匹配器 的 第 0 项 的 第 i 项：
            返还 匹配器 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

捨入匹配器_全部匹配 接受 匹配器、文本：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 匹配器 的 第 2 项：
        若 做 正则_包含 用 文本、匹配器 的 第 0 项 的 第 i 项：
            做 追加 用 结果、匹配器 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

捨入匹配器_模式数 接受 匹配器：
    返还 匹配器 的 第 2 项。
完。

谐振匹配_完全匹配 为 1000。

谐振匹配_位置衰减 为 950。

谐振匹配_字符距离 接受 字符A、字符B：
    若 字符A 等于 字符B：
        返还 谐振匹配_完全匹配。
    完。
    差值 为 字符A 减 字符B。
    若 差值 小于 0：
        差值 为 0 减 差值。
    完。
    若 差值 等于 32：
        若 字符A 大于或等于 65 并且 字符A 小于或等于 90 或者 字符A 大于或等于 97 并且 字符A 小于或等于 122：
            若 字符B 大于或等于 65 并且 字符B 小于或等于 90 或者 字符B 大于或等于 97 并且 字符B 小于或等于 122：
                返还 900。
            完。
        完。
    完。
    若 差值 大于或等于 128：
        返还 0。
    完。
    返还 1000 减 差值 乘 1000 除以 128。
完。

谐振匹配_计算相似度 接受 模式、文本：
    模式长 为 做 字符串长度 用 模式。
    文本长 为 做 字符串长度 用 文本。
    若 模式长 等于 0 并且 文本长 等于 0：
        返还 谐振匹配_完全匹配。
    完。
    若 模式长 等于 0 或者 文本长 等于 0：
        返还 0。
    完。
    比较长 为 模式长。
    若 文本长 小于 比较长：
        比较长 为 文本长。
    完。
    可变 总分 为 0。
    可变 总权重 为 0。
    可变 位置权重 为 1000。
    可变 i 为 0。
    只要 i 小于 比较长：
        字符分 为 做 谐振匹配_字符距离 用 做 字符串字符 用 模式、i、做 字符串字符 用 文本、i。
        总分 为 总分 加 字符分 乘 位置权重 除以 1000。
        总权重 为 总权重 加 位置权重。
        位置权重 为 位置权重 乘 谐振匹配_位置衰减 除以 1000。
        i 为 i 加 1。
    完。
    最大长 为 模式长。
    若 文本长 大于 最大长：
        最大长 为 文本长。
    完。
    长度因子 为 比较长 乘 1000 除以 最大长。
    若 总权重 等于 0：
        返还 0。
    完。
    返还 总分 乘 长度因子 除以 总权重。
完。

谐振匹配_批量 接受 模式、文本列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本列表：
        分数 为 做 谐振匹配_计算相似度 用 模式、文本列表 的 第 i 项。
        做 追加 用 结果、一组：文本列表 的 第 i 项、分数。
        i 为 i 加 1。
    完。
    返还 结果。
完。

谐振匹配_最佳匹配 接受 模式、文本列表：
    若 做 计数 用 文本列表 等于 0：
        返还 一组：空、0。
    完。
    可变 最佳 为 文本列表 的 第 0 项。
    可变 最佳分 为 做 谐振匹配_计算相似度 用 模式、文本列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 文本列表：
        分数 为 做 谐振匹配_计算相似度 用 模式、文本列表 的 第 i 项。
        若 分数 大于 最佳分：
            最佳分 为 分数。
            最佳 为 文本列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：最佳、最佳分。
完。

谐振匹配_阈值过滤 接受 模式、文本列表、阈值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本列表：
        分数 为 做 谐振匹配_计算相似度 用 模式、文本列表 的 第 i 项。
        若 分数 大于或等于 阈值：
            做 追加 用 结果、一组：文本列表 的 第 i 项、分数。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

正则_完整统计 接受 零参：
    做 正则统计_初始化。
    做 打印行 用 「╔══════════════════════════════════════════╗」。
    做 打印行 用 「║     原生正则表达式模块 · 完整统计      ║」。
    做 打印行 用 「╠══════════════════════════════════════════╣」。
    做 打印行 用 「║ [匹配引擎]                               ║」。
    做 打印行 用 「║   匹配调用:   」 加 做 整数转字符串 用 全局_正则统计 的 匹配调用数。
    做 打印行 用 「║   匹配成功:   」 加 做 整数转字符串 用 全局_正则统计 的 匹配成功数。
    做 打印行 用 「║   匹配失败:   」 加 做 整数转字符串 用 全局_正则统计 的 匹配失败数。
    成功率 为 0。
    若 全局_正则统计 的 匹配调用数 大于 0：
        成功率 为 全局_正则统计 的 匹配成功数 乘 1000 除以 全局_正则统计 的 匹配调用数。
    完。
    做 打印行 用 「║   成功率‰:    」 加 做 整数转字符串 用 成功率。
    做 打印行 用 「╠══════════════════════════════════════════╣」。
    做 打印行 用 「║ [查找与替换]                             ║」。
    做 打印行 用 「║   包含调用:   」 加 做 整数转字符串 用 全局_正则统计 的 包含调用数。
    做 打印行 用 「║   查找调用:   」 加 做 整数转字符串 用 全局_正则统计 的 查找调用数。
    做 打印行 用 「║   查找全部:   」 加 做 整数转字符串 用 全局_正则统计 的 查找全部调用数。
    做 打印行 用 「║   替换调用:   」 加 做 整数转字符串 用 全局_正则统计 的 替换调用数。
    做 打印行 用 「║   分割调用:   」 加 做 整数转字符串 用 全局_正则统计 的 分割调用数。
    做 打印行 用 「╠══════════════════════════════════════════╣」。
    做 打印行 用 「║ [性能]                                   ║」。
    做 打印行 用 「║   回溯次数:   」 加 做 整数转字符串 用 全局_正则统计 的 回溯次数。
    总调用 为 全局_正则统计 的 匹配调用数 加 全局_正则统计 的 包含调用数 加 全局_正则统计 的 查找调用数 加 全局_正则统计 的 替换调用数 加 全局_正则统计 的 分割调用数。
    做 打印行 用 「║   总调用数:   」 加 做 整数转字符串 用 总调用。
    平均回溯 为 0。
    若 总调用 大于 0：
        平均回溯 为 全局_正则统计 的 回溯次数 除以 总调用。
    完。
    做 打印行 用 「║   平均回溯:   」 加 做 整数转字符串 用 平均回溯。
    做 打印行 用 「╚══════════════════════════════════════════╝」。
完。

可变 AI_原生正则表_调用数 为 0。

可变 AI_原生正则表_异常数 为 0。

AI_原生正则表_监控 接受 零参：
    AI_原生正则表_调用数 为 AI_原生正则表_调用数 加 1。
    返还 AI_原生正则表_异常数 等于 0。
完。

AI_原生正则表_健康检查 接受 零参：
    若 AI_原生正则表_调用数 大于 0 并且 AI_原生正则表_异常数 乘 100 除以 AI_原生正则表_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
