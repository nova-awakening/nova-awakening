《位图集合》

引用《..·中枢·类型定义》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内核打印》。

位图_创建 接受 大小：
    字数 为 (大小 加 63) 除以 64。
    数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 字数：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 一组：数据、大小。
完。

位图_创建_从数组 接受 数组、大小：
    若 数组 等于 0：
        返还 做 位图_创建 用 大小。
    完。
    返还 一组：数组、大小。
完。

位图_设置 接受 位图、位：
    若 位 小于 0 或者 位 大于或等于 位图 的 第 1 项：
        返还。
    完。
    w 为 位 除以 64。
    b 为 位 取余于 64。
    做 设置 用 位图 的 第 0 项、w、位图 的 第 0 项 的 第 w 项 位或 1 左移 b。
完。

位图_清除 接受 位图、位：
    若 位 小于 0 或者 位 大于或等于 位图 的 第 1 项：
        返还。
    完。
    w 为 位 除以 64。
    b 为 位 取余于 64。
    做 设置 用 位图 的 第 0 项、w、位图 的 第 0 项 的 第 w 项 位与 按位取反 (1 左移 b)。
完。

位图_测试 接受 位图、位：
    若 位 小于 0 或者 位 大于或等于 位图 的 第 1 项：
        返还 0。
    完。
    w 为 位 除以 64。
    b 为 位 取余于 64。
    返还 位图 的 第 0 项 的 第 w 项 右移 b 位与 1。
完。

位图_翻转 接受 位图、位：
    若 位 小于 0 或者 位 大于或等于 位图 的 第 1 项：
        返还。
    完。
    w 为 位 除以 64。
    b 为 位 取余于 64。
    做 设置 用 位图 的 第 0 项、w、位图 的 第 0 项 的 第 w 项 位异或 1 左移 b。
完。

位图_范围设置 接受 位图、起始、结束：
    若 起始 小于 0：
        起始 为 0。
    完。
    若 结束 大于 位图 的 第 1 项：
        结束 为 位图 的 第 1 项。
    完。
    可变 i 为 起始。
    只要 i 小于 结束：
        w 为 i 除以 64。
        b 为 i 取余于 64。
        若 b 等于 0 并且 i 加 64 小于或等于 结束：
            做 设置 用 位图 的 第 0 项、w、负 1。
            i 为 i 加 64。
        否则：
            做 设置 用 位图 的 第 0 项、w、位图 的 第 0 项 的 第 w 项 位或 1 左移 b。
            i 为 i 加 1。
        完。
    完。
完。

位图_范围清除 接受 位图、起始、结束：
    若 起始 小于 0：
        起始 为 0。
    完。
    若 结束 大于 位图 的 第 1 项：
        结束 为 位图 的 第 1 项。
    完。
    可变 i 为 起始。
    只要 i 小于 结束：
        w 为 i 除以 64。
        b 为 i 取余于 64。
        若 b 等于 0 并且 i 加 64 小于或等于 结束：
            做 设置 用 位图 的 第 0 项、w、0。
            i 为 i 加 64。
        否则：
            做 设置 用 位图 的 第 0 项、w、位图 的 第 0 项 的 第 w 项 位与 按位取反 (1 左移 b)。
            i 为 i 加 1。
        完。
    完。
完。

位图_范围测试全一 接受 位图、起始、结束：
    可变 i 为 起始。
    只要 i 小于 结束：
        若 做 位图_测试 用 位图、i 等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

位图_范围测试全零 接受 位图、起始、结束：
    可变 i 为 起始。
    只要 i 小于 结束：
        若 做 位图_测试 用 位图、i 不等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

位图_找第一个零 接受 位图：
    可变 wi 为 0。
    只要 wi 小于 做 计数 用 位图 的 第 0 项：
        若 位图 的 第 0 项 的 第 wi 项 不等于 负 1：
            可变 b 为 0。
            只要 b 小于 64：
                位 为 wi 乘 64 加 b。
                若 位 大于或等于 位图 的 第 1 项：
                    返还 0 减 1。
                完。
                若 位图 的 第 0 项 的 第 wi 项 右移 b 位与 1 等于 0：
                    返还 位。
                完。
                b 为 b 加 1。
            完。
        完。
        wi 为 wi 加 1。
    完。
    返还 0 减 1。
完。

位图_找第一个一 接受 位图：
    可变 wi 为 0。
    只要 wi 小于 做 计数 用 位图 的 第 0 项：
        若 位图 的 第 0 项 的 第 wi 项 不等于 0：
            可变 b 为 0。
            只要 b 小于 64：
                位 为 wi 乘 64 加 b。
                若 位 大于或等于 位图 的 第 1 项：
                    返还 0 减 1。
                完。
                若 位图 的 第 0 项 的 第 wi 项 右移 b 位与 1 等于 1：
                    返还 位。
                完。
                b 为 b 加 1。
            完。
        完。
        wi 为 wi 加 1。
    完。
    返还 0 减 1。
完。

位图_找下一个零 接受 位图、从：
    可变 i 为 从。
    只要 i 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、i 等于 0：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 0 减 1。
完。

位图_找下一个一 接受 位图、从：
    可变 i 为 从。
    只要 i 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、i 等于 1：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 0 减 1。
完。

位图_popcount64 接受 v：
    可变 x 为 v。
    x 为 x 减 (x 右移 1 位与 0x5555555555555555)。
    x 为 (x 位与 0x3333333333333333) 加 (x 右移 2 位与 0x3333333333333333)。
    x 为 x 加 (x 右移 4) 位与 0x0F0F0F0F0F0F0F0F。
    返还 x 乘 0x0101010101010101 右移 56。
完。

位图_统计一 接受 位图：
    可变 计数值 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 位图 的 第 0 项：
        计数值 为 计数值 加 做 位图_popcount64 用 位图 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 计数值。
完。

位图_统计零 接受 位图：
    返还 位图 的 第 1 项 减 做 位图_统计一 用 位图。
完。

位图_大小 接受 位图：
    返还 位图 的 第 1 项。
完。

位图_为空 接受 位图：
    返还 做 位图_统计一 用 位图 等于 0。
完。

位图_已满 接受 位图：
    返还 做 位图_统计一 用 位图 等于 位图 的 第 1 项。
完。

位图_全部设置 接受 位图：
    可变 i 为 0。
    只要 i 小于 做 计数 用 位图 的 第 0 项：
        做 设置 用 位图 的 第 0 项、i、负 1。
        i 为 i 加 1。
    完。
完。

位图_全部清除 接受 位图：
    可变 i 为 0。
    只要 i 小于 做 计数 用 位图 的 第 0 项：
        做 设置 用 位图 的 第 0 项、i、0。
        i 为 i 加 1。
    完。
完。

位图_与 接受 a、b：
    大小 为 a 的 第 1 项。
    若 b 的 第 1 项 小于 大小：
        大小 为 b 的 第 1 项。
    完。
    结果 为 做 位图_创建 用 大小。
    可变 i 为 0。
    只要 i 小于 做 计数 用 结果 的 第 0 项：
        做 设置 用 结果 的 第 0 项、i、a 的 第 0 项 的 第 i 项 位与 b 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位图_或 接受 a、b：
    大小 为 a 的 第 1 项。
    若 b 的 第 1 项 大于 大小：
        大小 为 b 的 第 1 项。
    完。
    结果 为 做 位图_创建 用 大小。
    可变 i 为 0。
    只要 i 小于 做 计数 用 结果 的 第 0 项：
        va 为 0。
        vb 为 0。
        若 i 小于 做 计数 用 a 的 第 0 项：
            va 为 a 的 第 0 项 的 第 i 项。
        完。
        若 i 小于 做 计数 用 b 的 第 0 项：
            vb 为 b 的 第 0 项 的 第 i 项。
        完。
        做 设置 用 结果 的 第 0 项、i、va 位或 vb。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位图_异或 接受 a、b：
    大小 为 a 的 第 1 项。
    若 b 的 第 1 项 大于 大小：
        大小 为 b 的 第 1 项。
    完。
    结果 为 做 位图_创建 用 大小。
    可变 i 为 0。
    只要 i 小于 做 计数 用 结果 的 第 0 项：
        va 为 0。
        vb 为 0。
        若 i 小于 做 计数 用 a 的 第 0 项：
            va 为 a 的 第 0 项 的 第 i 项。
        完。
        若 i 小于 做 计数 用 b 的 第 0 项：
            vb 为 b 的 第 0 项 的 第 i 项。
        完。
        做 设置 用 结果 的 第 0 项、i、va 位异或 vb。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位图_非 接受 位图：
    结果 为 做 位图_创建 用 位图 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 位图 的 第 0 项：
        做 设置 用 结果 的 第 0 项、i、按位取反 位图 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位图_相等 接受 a、b：
    若 a 的 第 1 项 不等于 b 的 第 1 项：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 0 项：
        若 a 的 第 0 项 的 第 i 项 不等于 b 的 第 0 项 的 第 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

位图_子集 接受 子、父：
    可变 i 为 0。
    只要 i 小于 做 计数 用 子 的 第 0 项：
        若 i 小于 做 计数 用 父 的 第 0 项：
            若 子 的 第 0 项 的 第 i 项 位与 按位取反 父 的 第 0 项 的 第 i 项 不等于 0：
                返还 假。
            完。
        否则：
            若 子 的 第 0 项 的 第 i 项 不等于 0：
                返还 假。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

位图_找连续零 接受 位图、需要数：
    可变 起始 为 负 1。
    可变 连续 为 0。
    可变 i 为 0。
    只要 i 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、i 等于 0：
            若 连续 等于 0：
                起始 为 i。
            完。
            连续 为 连续 加 1。
            若 连续 大于或等于 需要数：
                返还 起始。
            完。
        否则：
            连续 为 0。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

位图_找连续一 接受 位图、需要数：
    可变 起始 为 负 1。
    可变 连续 为 0。
    可变 i 为 0。
    只要 i 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、i 等于 1：
            若 连续 等于 0：
                起始 为 i。
            完。
            连续 为 连续 加 1。
            若 连续 大于或等于 需要数：
                返还 起始。
            完。
        否则：
            连续 为 0。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

位图_复制 接受 位图：
    结果 为 做 位图_创建 用 位图 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 位图 的 第 0 项：
        做 设置 用 结果 的 第 0 项、i、位图 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位图_调整大小 接受 位图、新大小：
    新字数 为 (新大小 加 63) 除以 64。
    旧字数 为 做 计数 用 位图 的 第 0 项。
    若 新字数 大于 旧字数：
        可变 i 为 旧字数。
        只要 i 小于 新字数：
            做 追加 用 位图 的 第 0 项、0。
            i 为 i 加 1。
        完。
    完。
    位图 的 第 1 项 为 新大小。
完。

位图_遍历一 接受 位图、回调：
    可变 i 为 0。
    只要 i 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、i 等于 1：
            做 回调 用 i。
        完。
        i 为 i 加 1。
    完。
完。

位图_转列表 接受 位图：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、i 等于 1：
            做 追加 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位图_从列表 接受 列表、大小：
    位图 为 做 位图_创建 用 大小。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 位图_设置 用 位图、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 位图。
完。

位图_转字符串 接受 位图：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、i 等于 1：
            结果 为 做 字符串拼接 用 结果、「1」。
        否则：
            结果 为 做 字符串拼接 用 结果、「0」。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位图_左移 接受 位图、位数：
    新大小 为 位图 的 第 1 项 加 位数。
    结果 为 做 位图_创建 用 新大小。
    可变 i 为 0。
    只要 i 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、i 等于 1：
            做 位图_设置 用 结果、i 加 位数。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位图_右移 接受 位图、位数：
    若 位数 大于或等于 位图 的 第 1 项：
        返还 做 位图_创建 用 位图 的 第 1 项。
    完。
    结果 为 做 位图_创建 用 位图 的 第 1 项。
    可变 i 为 位数。
    只要 i 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、i 等于 1：
            做 位图_设置 用 结果、i 减 位数。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位图_密度 接受 位图：
    若 位图 的 第 1 项 等于 0：
        返还 0。
    完。
    返还 做 位图_统计一 用 位图 乘 1000 除以 位图 的 第 1 项。
完。

位图_汉明距离 接受 a、b：
    x 为 做 位图_异或 用 a、b。
    返还 做 位图_统计一 用 x。
完。

位图统计 是 一种 事物，含 创建次数、设置次数、清除次数、测试次数、运算次数。

可变 全局_位图统计 为 空。

位图统计_初始化 接受 零参：
    若 全局_位图统计 等于 空：
        全局_位图统计 为 新建 位图统计。
        全局_位图统计 的 创建次数 为 0。
        全局_位图统计 的 设置次数 为 0。
        全局_位图统计 的 清除次数 为 0。
        全局_位图统计 的 测试次数 为 0。
        全局_位图统计 的 运算次数 为 0。
    完。
完。

位图_打印统计 接受 零参：
    做 位图统计_初始化。
    做 打印行 用 「=== NBMS位图统计 ===」。
    做 打印行 用 「创建: 」 加 做 整数转字符串 用 全局_位图统计 的 创建次数。
    做 打印行 用 「设置: 」 加 做 整数转字符串 用 全局_位图统计 的 设置次数。
    做 打印行 用 「清除: 」 加 做 整数转字符串 用 全局_位图统计 的 清除次数。
    做 打印行 用 「测试: 」 加 做 整数转字符串 用 全局_位图统计 的 测试次数。
    做 打印行 用 「运算: 」 加 做 整数转字符串 用 全局_位图统计 的 运算次数。
完。

位图_完整统计 接受 零参：
    做 位图统计_初始化。
    返还 一组：全局_位图统计 的 创建次数、全局_位图统计 的 设置次数、全局_位图统计 的 清除次数、全局_位图统计 的 测试次数、全局_位图统计 的 运算次数。
完。

位图_重置统计 接受 零参：
    若 全局_位图统计 不等于 空：
        全局_位图统计 的 创建次数 为 0。
        全局_位图统计 的 设置次数 为 0。
        全局_位图统计 的 清除次数 为 0。
        全局_位图统计 的 测试次数 为 0。
        全局_位图统计 的 运算次数 为 0。
    完。
完。

位图_旋转左 接受 位图、位数：
    n 为 位图 的 第 1 项。
    若 n 等于 0：
        返还 做 位图_复制 用 位图。
    完。
    实际 为 位数 取余于 n。
    结果 为 做 位图_创建 用 n。
    可变 i 为 0。
    只要 i 小于 n：
        源 为 (i 减 实际 加 n) 取余于 n。
        若 做 位图_测试 用 位图、源 等于 1：
            做 位图_设置 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位图_Jaccard相似度 接受 位图A、位图B：
    交 为 做 位图_统计一 用 做 位图_与 用 位图A、位图B。
    并 为 做 位图_统计一 用 做 位图_或 用 位图A、位图B。
    若 并 等于 0：
        返还 1000。
    完。
    返还 交 乘 1000 除以 并。
完。

位图_Hamming距离 接受 位图A、位图B：
    返还 做 位图_统计一 用 做 位图_异或 用 位图A、位图B。
完。

位图_游程编码 接受 位图：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 位图 的 第 1 项：
        当前值 为 做 位图_测试 用 位图、i。
        可变 运行长 为 0。
        只要 i 小于 位图 的 第 1 项 并且 做 位图_测试 用 位图、i 等于 当前值：
            运行长 为 运行长 加 1。
            i 为 i 加 1。
        完。
        做 追加 用 结果、一组：当前值、运行长。
    完。
    返还 结果。
完。

位图_游程解码 接受 游程列表、大小：
    位图 为 做 位图_创建 用 大小。
    可变 位置 为 0。
    可变 r 为 0。
    只要 r 小于 做 计数 用 游程列表：
        值 为 游程列表 的 第 r 项 的 第 0 项。
        长 为 游程列表 的 第 r 项 的 第 1 项。
        若 值 等于 1：
            做 位图_范围设置 用 位图、位置、位置 加 长。
        完。
        位置 为 位置 加 长。
        r 为 r 加 1。
    完。
    返还 位图。
完。

位图_游程压缩比 接受 位图：
    游程 为 做 位图_游程编码 用 位图。
    原始字数 为 做 计数 用 位图 的 第 0 项。
    若 原始字数 等于 0：
        返还 0。
    完。
    返还 做 计数 用 游程 乘 1000 除以 原始字数。
完。

稀疏位图_创建 接受 大小：
    桶数 为 64。
    可变 桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 桶数：
        做 追加 用 桶、一组：（空）。
        i 为 i 加 1。
    完。
    返还 一组：桶、大小、0、桶数。
完。

_稀疏位图_桶索引 接受 稀疏、位：
    返还 位 取余于 稀疏 的 第 3 项。
完。

稀疏位图_设置 接受 稀疏、位：
    若 位 小于 0 或者 位 大于或等于 稀疏 的 第 1 项：
        返还。
    完。
    bi 为 做 _稀疏位图_桶索引 用 稀疏、位。
    可变 j 为 0。
    只要 j 小于 做 计数 用 稀疏 的 第 0 项 的 第 bi 项：
        若 稀疏 的 第 0 项 的 第 bi 项 的 第 j 项 等于 位：
            返还。
        完。
        j 为 j 加 1。
    完。
    做 追加 用 稀疏 的 第 0 项 的 第 bi 项、位。
    稀疏 的 第 2 项 为 稀疏 的 第 2 项 加 1。
完。

稀疏位图_清除 接受 稀疏、位：
    若 位 小于 0 或者 位 大于或等于 稀疏 的 第 1 项：
        返还。
    完。
    bi 为 做 _稀疏位图_桶索引 用 稀疏、位。
    可变 j 为 0。
    只要 j 小于 做 计数 用 稀疏 的 第 0 项 的 第 bi 项：
        若 稀疏 的 第 0 项 的 第 bi 项 的 第 j 项 等于 位：
            末 为 做 计数 用 稀疏 的 第 0 项 的 第 bi 项 减 1。
            做 设置 用 稀疏 的 第 0 项 的 第 bi 项、j、稀疏 的 第 0 项 的 第 bi 项 的 第 末 项。
            稀疏 的 第 2 项 为 稀疏 的 第 2 项 减 1。
            返还。
        完。
        j 为 j 加 1。
    完。
完。

稀疏位图_测试 接受 稀疏、位：
    若 位 小于 0 或者 位 大于或等于 稀疏 的 第 1 项：
        返还 0。
    完。
    bi 为 做 _稀疏位图_桶索引 用 稀疏、位。
    可变 j 为 0。
    只要 j 小于 做 计数 用 稀疏 的 第 0 项 的 第 bi 项：
        若 稀疏 的 第 0 项 的 第 bi 项 的 第 j 项 等于 位：
            返还 1。
        完。
        j 为 j 加 1。
    完。
    返还 0。
完。

稀疏位图_统计 接受 稀疏：
    返还 稀疏 的 第 2 项。
完。

稀疏位图_转密集 接受 稀疏：
    位图 为 做 位图_创建 用 稀疏 的 第 1 项。
    可变 b 为 0。
    只要 b 小于 稀疏 的 第 3 项：
        可变 j 为 0。
        只要 j 小于 做 计数 用 稀疏 的 第 0 项 的 第 b 项：
            做 位图_设置 用 位图、稀疏 的 第 0 项 的 第 b 项 的 第 j 项。
            j 为 j 加 1。
        完。
        b 为 b 加 1。
    完。
    返还 位图。
完。

位图迭代器_创建 接受 位图：
    返还 一组：位图、负 1。
完。

位图迭代器_下一个 接受 迭代器：
    位图 为 迭代器 的 第 0 项。
    可变 位 为 迭代器 的 第 1 项 加 1。
    只要 位 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、位 等于 1：
            迭代器 的 第 1 项 为 位。
            返还 位。
        完。
        位 为 位 加 1。
    完。
    迭代器 的 第 1 项 为 位图 的 第 1 项。
    返还 负 1。
完。

位图迭代器_重置 接受 迭代器：
    迭代器 的 第 1 项 为 负 1。
完。

位图迭代器_有下一个 接受 迭代器：
    位图 为 迭代器 的 第 0 项。
    可变 位 为 迭代器 的 第 1 项 加 1。
    只要 位 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、位 等于 1：
            返还 真。
        完。
        位 为 位 加 1。
    完。
    返还 假。
完。

位图迭代器_收集全部 接受 位图：
    可变 结果 为 一组：（空）。
    迭代 为 做 位图迭代器_创建 用 位图。
    可变 值 为 做 位图迭代器_下一个 用 迭代。
    只要 值 大于或等于 0：
        做 追加 用 结果、值。
        值 为 做 位图迭代器_下一个 用 迭代。
    完。
    返还 结果。
完。

_谐振位图_密度阈值 为 100。

谐振位图_创建 接受 大小：
    返还 一组：做 位图_创建 用 大小、大小、0、_谐振位图_密度阈值。
完。

谐振位图_设置 接受 谐振、位：
    若 谐振 的 第 2 项 等于 0：
        做 位图_设置 用 谐振 的 第 0 项、位。
    否则：
        做 稀疏位图_设置 用 谐振 的 第 0 项、位。
    完。
    做 谐振位图_检查切换 用 谐振。
完。

谐振位图_测试 接受 谐振、位：
    若 谐振 的 第 2 项 等于 0：
        返还 做 位图_测试 用 谐振 的 第 0 项、位。
    否则：
        返还 做 稀疏位图_测试 用 谐振 的 第 0 项、位。
    完。
完。

谐振位图_检查切换 接受 谐振：
    可变 当前密度 为 0。
    若 谐振 的 第 2 项 等于 0：
        当前密度 为 做 位图_密度 用 谐振 的 第 0 项。
    否则：
        若 谐振 的 第 1 项 大于 0：
            当前密度 为 做 稀疏位图_统计 用 谐振 的 第 0 项 乘 1000 除以 谐振 的 第 1 项。
        完。
    完。
    若 谐振 的 第 2 项 等于 0 并且 当前密度 小于 谐振 的 第 3 项 除以 2：
        新 为 做 稀疏位图_创建 用 谐振 的 第 1 项。
        可变 i 为 0。
        只要 i 小于 谐振 的 第 1 项：
            若 做 位图_测试 用 谐振 的 第 0 项、i 等于 1：
                做 稀疏位图_设置 用 新、i。
            完。
            i 为 i 加 1。
        完。
        谐振 的 第 0 项 为 新。
        谐振 的 第 2 项 为 1。
    完。
    若 谐振 的 第 2 项 等于 1 并且 当前密度 大于 谐振 的 第 3 项：
        谐振 的 第 0 项 为 做 稀疏位图_转密集 用 谐振 的 第 0 项。
        谐振 的 第 2 项 为 0。
    完。
完。

谐振位图_当前模式 接受 谐振：
    若 谐振 的 第 2 项 等于 0：
        返还 「密集」。
    完。
    返还 「稀疏」。
完。

集合位图_完整统计 接受 零参：
    返还 一组：「基础位操作」、6、「范围操作」、4、「布尔运算」、5、「搜索操作」、6、「密度与移位」、6、「游程编码RLE」、3、「稀疏位图」、6、「位图迭代器」、5、「谐振自适应位图」、5。
完。

本章 收束。
