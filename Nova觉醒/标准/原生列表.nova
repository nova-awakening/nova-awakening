《原生列表》

引用《..·中枢·类型定义》。

列表_查找 接受 列表、值)：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表[i] 等于 值：
        i 为 i 加 1。
    完。
    返还 0 减 1。
完。

列表_包含 接受 列表、值)：
    返还 做 列表_查找 用 列表、值 不小于 0。
完。

列表_最后查找 接受 列表、值)：
    可变 i 为 做 计数 用 列表 减 1。
    只要 i 不小于 0：
        若 列表[i] 等于 值：
        i 为 i 减 1。
    完。
    返还 0 减 1。
完。

列表_统计 接受 列表、值)：
    可变 次数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表[i] 等于 值：
        i 为 i 加 1。
    完。
    返还 次数。
完。

列表_反转 接受 列表)：
    结果 为 []。
    可变 i 为 做 计数 用 列表 减 1。
    只要 i 不小于 0：
        做 追加 用 结果、列表[i]。
        i 为 i 减 1。
    完。
    返还 结果。
完。

列表_原地反转 接受 列表)：
    可变 左 为 0。
    可变 右 为 做 计数 用 列表 减 1。
    只要 左 小于 右：
        临时 为 列表[左]。
        做 设置 用 列表、左、列表[右]。
        做 设置 用 列表、右、临时。
        左 为 左 加 1。
        右 为 右 减 1。
    完。
完。

列表_切片 接受 列表、开始、结束)：
    结果 为 []。
    若 开始 小于 0：
    若 结束 大于 做 计数 用 列表：
    可变 i 为 开始。
    只要 i 小于 结束：
        做 追加 用 结果、列表[i]。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_复制 接受 列表)：
    结果 为 []。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表[i]。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_连接 接受 列表a、列表b)：
    结果 为 做 列表_复制 用 列表a。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表b：
        做 追加 用 结果、列表b[i]。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_最大 接受 列表)：
    若 做 计数 用 列表 等于 0：
    可变 最大 为 列表[0]。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表[i] 大于 最大：
        i 为 i 加 1。
    完。
    返还 最大。
完。

列表_最小 接受 列表)：
    若 做 计数 用 列表 等于 0：
    可变 最小 为 列表[0]。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表[i] 小于 最小：
        i 为 i 加 1。
    完。
    返还 最小。
完。

列表_求和 接受 列表)：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        和 为 和 加 列表[i]。
        i 为 i 加 1。
    完。
    返还 和。
完。

列表_平均 接受 列表)：
    若 做 计数 用 列表 等于 0：
    返还 做 列表_求和 用 列表 除以 做 计数 用 列表。
完。

列表_最大索引 接受 列表)：
    若 做 计数 用 列表 等于 0：
    可变 最大索引 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表[i] 大于 列表[最大索引]：
        i 为 i 加 1。
    完。
    返还 最大索引。
完。

列表_最小索引 接受 列表)：
    若 做 计数 用 列表 等于 0：
    可变 最小索引 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表[i] 小于 列表[最小索引]：
        i 为 i 加 1。
    完。
    返还 最小索引。
完。

列表_插入排序 接受 列表)：
    n 为 做 计数 用 列表。
    可变 i 为 1。
    只要 i 小于 n：
        键 为 列表[i]。
        可变 j 为 i 减 1。
        只要 j 不小于 0 并且 列表[j] 大于 键：
            做 设置 用 列表、j 加 1、列表[j]。
            j 为 j 减 1。
        完。
        做 设置 用 列表、j 加 1、键。
        i 为 i 加 1。
    完。
完。

列表_快速排序 接受 列表)：
    若 做 计数 用 列表 不大于 1：
    若 做 计数 用 列表 不大于 16：
        副本 为 做 列表_复制 用 列表。
        做 列表_插入排序 用 副本。
        返还 副本。
    完。
    基准 为 列表[做 计数 用 列表 除以 2]。
    小于 为 []。
    等于 为 []。
    大于 为 []。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表[i] 小于 基准：
        否则 若 (列表[i] 等于 基准) { 做 追加 用 等于、列表[i] }。
        否则 { 做 追加 用 大于、列表[i] }。
        i 为 i 加 1。
    完。
    返还 做 列表_连接 用 做 列表_连接 用 做 列表_快速排序 用 小于、等于、做 列表_快速排序 用 大于。
完。

列表_已排序 接受 列表)：
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表[i] 小于 列表[i 减 1]：
        i 为 i 加 1。
    完。
    返还 真。
完。

列表_二分查找 接受 列表、值)：
    可变 低 为 0。
    可变 高 为 做 计数 用 列表 减 1。
    只要 低 不大于 高：
        中 为 (低 加 高) 除以 2。
        若 列表[中] 等于 值：
        若 列表[中] 小于 值：
        否则 { 高 = 中 减 1 }。
    完。
    返还 -1。
完。

列表_去重 接受 列表)：
    结果 为 []。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 !做 列表_包含 用 结果、列表[i]：
            做 追加 用 结果、列表[i]。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_扁平化 接受 列表)：
    结果 为 []。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        元素 为 列表[i]。
        若 做 是列表 用 元素：
            子结果 为 做 列表_扁平化 用 元素。
            可变 j 为 0。
            只要 j 小于 做 计数 用 子结果：
                做 追加 用 结果、子结果[j]。
                j 为 j 加 1。
            完。
        否则：
            做 追加 用 结果、元素。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_压缩 接受 列表a、列表b)：
    结果 为 []。
    长度 为 做 计数 用 列表a。
    若 做 计数 用 列表b 小于 长度：
    可变 i 为 0。
    只要 i 小于 长度：
        做 追加 用 结果、[列表a[i]、列表b[i]]。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_填充 接受 大小、值)：
    结果 为 []。
    可变 i 为 0。
    只要 i 小于 大小：
        做 追加 用 结果、值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_范围 接受 开始、结束、步长)：
    若 步长 等于 0：
    结果 为 []。
    若 步长 大于 0：
        可变 i 为 开始。
        只要 i 小于 结束：
    否则：
        可变 i 为 开始。
        只要 i 大于 结束：
    完。
    返还 结果。
完。

列表_旋转左 接受 列表、k)：
    n 为 做 计数 用 列表。
    若 n 等于 0：
    k 为 k 取余于 n。
    若 k 小于 0：
    返还 做 列表_连接 用 做 列表_切片 用 列表、k、n、做 列表_切片 用 列表、0、k。
完。

列表_旋转右 接受 列表、k)：
    n 为 做 计数 用 列表。
    若 n 等于 0：
    k 为 k 取余于 n。
    返还 做 列表_旋转左 用 列表、n 减 k。
完。

列表_交集 接受 列表a、列表b)：
    结果 为 []。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表a：
        若 做 列表_包含 用 列表b、列表a[i] 并且 !做 列表_包含 用 结果、列表a[i]：
            做 追加 用 结果、列表a[i]。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_并集 接受 列表a、列表b)：
    结果 为 做 列表_去重 用 列表a。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表b：
        若 !做 列表_包含 用 结果、列表b[i]：
            做 追加 用 结果、列表b[i]。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_差集 接受 列表a、列表b)：
    结果 为 []。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表a：
        若 !做 列表_包含 用 列表b、列表a[i]：
            做 追加 用 结果、列表a[i]。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_相等 接受 列表a、列表b)：
    若 做 计数 用 列表a 不等于 做 计数 用 列表b：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表a：
        若 列表a[i] 不等于 列表b[i]：
        i 为 i 加 1。
    完。
    返还 真。
完。

列表_分块 接受 列表、块大小)：
    若 块大小 不大于 0：
    结果 为 []。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        结束 为 i 加 块大小。
        若 结束 大于 做 计数 用 列表：
        做 追加 用 结果、做 列表_切片 用 列表、i、结束。
        i 为 结束。
    完。
    返还 结果。
完。

列表_交换 接受 列表、索引a、索引b)：
    若 索引a 小于 0 或者 索引a 不小于 做 计数 用 列表：
    若 索引b 小于 0 或者 索引b 不小于 做 计数 用 列表：
    临时 为 列表[索引a]。
    做 设置 用 列表、索引a、列表[索引b]。
    做 设置 用 列表、索引b、临时。
完。

列表_洗牌 接受 列表、种子)：
    n 为 做 计数 用 列表。
    副本 为 做 列表_复制 用 列表。
    可变 状态 为 种子。
    可变 i 为 n 减 1。
    只要 i 大于 0：
        状态 为 (状态 乘 1103515245 加 12345) 位与 0x7FFFFFFF。
        j 为 状态 取余于 (i 加 1)。
        做 列表_交换 用 副本、i、j。
        i 为 i 减 1。
    完。
    返还 副本。
完。

※ 堆排序 (HeapSort)

_堆_下沉 接受 列表、n、i)：
    可变 最大 为 i。
    左 为 2 乘 i 加 1。
    右 为 2 乘 i 加 2。
    若 左 小于 n 并且 列表[左] 大于 列表[最大]：
    若 右 小于 n 并且 列表[右] 大于 列表[最大]：
    若 最大 不等于 i：
        做 列表_交换 用 列表、i、最大。
        做 _堆_下沉 用 列表、n、最大。
    完。
完。

列表_堆排序 接受 列表)：
    副本 为 做 列表_复制 用 列表。
    n 为 做 计数 用 副本。
    ※ 构建最大堆
    可变 i 为 n 除以 2 减 1。
    只要 i 不小于 0：
    ※ 逐步取出堆顶
    i 为 n 减 1。
    只要 i 大于 0：
        做 列表_交换 用 副本、0、i。
        做 _堆_下沉 用 副本、i、0。
        i 为 i 减 1。
    完。
    返还 副本。
完。

※ 归并排序 (MergeSort)

_归并_合并 接受 左、右)：
    结果 为 []。
    可变 i 为 0。
    可变 j 为 0。
    只要 i 小于 做 计数 用 左 并且 j 小于 做 计数 用 右：
        若 左[i] 不大于 右[j]：
            做 追加 用 结果、左[i]。
            i 为 i 加 1。
        否则：
            做 追加 用 结果、右[j]。
            j 为 j 加 1。
        完。
    完。
    只要 i 小于 做 计数 用 左：
    只要 j 小于 做 计数 用 右：
    返还 结果。
完。

列表_归并排序 接受 列表)：
    n 为 做 计数 用 列表。
    若 n 不大于 1：
    中 为 n 除以 2。
    左 为 做 列表_归并排序 用 做 列表_切片 用 列表、0、中。
    右 为 做 列表_归并排序 用 做 列表_切片 用 列表、中、n。
    返还 做 _归并_合并 用 左、右。
完。

※ 滑动窗口算法

列表_滑动窗口 接受 列表、窗口大小)：
    n 为 做 计数 用 列表。
    结果 为 []。
    若 窗口大小 不大于 0 或者 窗口大小 大于 n：
    可变 i 为 0。
    只要 i 加 窗口大小 不大于 n：
        做 追加 用 结果、做 列表_切片 用 列表、i、i 加 窗口大小。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_滑动窗口求和 接受 列表、窗口大小)：
    n 为 做 计数 用 列表。
    结果 为 []。
    若 窗口大小 不大于 0 或者 窗口大小 大于 n：
    ※ 计算初始窗口和
    可变 窗口和 为 0。
    可变 k 为 0。
    只要 k 小于 窗口大小：
    做 追加 用 结果、窗口和。
    ※ 滑动
    可变 i 为 1。
    只要 i 加 窗口大小 不大于 n：
        窗口和 为 窗口和 减 列表[i 减 1] 加 列表[i 加 窗口大小 减 1]。
        做 追加 用 结果、窗口和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_滑动最大值 接受 列表、窗口大小)：
    和列表 为 做 列表_滑动窗口求和 用 列表、窗口大小。
    若 做 计数 用 和列表 等于 0：
    返还 做 列表_最大 用 和列表。
完。

※ 前缀和 / 后缀和

列表_前缀和 接受 列表)：
    n 为 做 计数 用 列表。
    结果 为 [0]。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、结果[i] 加 列表[i]。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_后缀和 接受 列表)：
    n 为 做 计数 用 列表。
    结果 为 做 列表_填充 用 n 加 1、0。
    可变 i 为 n 减 1。
    只要 i 不小于 0：
        结果[i] 为 结果[i 加 1] 加 列表[i]。
        i 为 i 减 1。
    完。
    返还 结果。
完。

列表_区间和 接受 前缀、左、右)：
    ※ 基于前缀和数组的 O(1) 区间求和
    若 左 小于 0：
    若 右 不小于 做 计数 用 前缀：
    返还 前缀[右] 减 前缀[左]。
完。

※ 中位数选择 (QuickSelect)

列表_第K小 接受 列表、k)：
    ※ QuickSelect: 平均 O(n) 找第k小元素 (k从0开始)
    n 为 做 计数 用 列表。
    若 k 小于 0 或者 k 不小于 n：
    副本 为 做 列表_复制 用 列表。
    可变 低 为 0。
    可变 高 为 n 减 1。
    只要 低 小于 高：
        基准 为 副本[高]。
        可变 分割点 为 低。
        可变 j 为 低。
        只要 j 小于 高：
            若 副本[j] 小于 基准：
                做 列表_交换 用 副本、分割点、j。
                分割点 为 分割点 加 1。
            完。
            j 为 j 加 1。
        完。
        做 列表_交换 用 副本、分割点、高。
        若 分割点 等于 k：
        若 分割点 小于 k：
        否则 { 高 = 分割点 减 1 }。
    完。
    返还 副本[低]。
完。

列表_中位数 接受 列表)：
    n 为 做 计数 用 列表。
    若 n 等于 0：
    返还 做 列表_第K小 用 列表、n 除以 2。
完。

列表_百分位 接受 列表、百分比)：
    ※ 百分比: ×1000 定点数 (500 = 50%)
    n 为 做 计数 用 列表。
    若 n 等于 0：
    k 为 (百分比 乘 n) 除以 1000。
    若 k 不小于 n：
    若 k 小于 0：
    返还 做 列表_第K小 用 列表、k。
完。

※ 惯性自适应排序 — 利用序列势能加速排序
※ 检测已有有序子序列(“惯性跑道”)，合并时复用它们的势能

列表_惯性排序 接受 列表)：
    n 为 做 计数 用 列表。
    若 n 不大于 1：
    ※ 第一步: 检测自然跑道
    跑道列表 为 []。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 i 加 1。
        ※ 检测升序跑道
        若 j 小于 n 并且 列表[j] 不小于 列表[j 减 1]：
            只要 j 小于 n 并且 列表[j] 不小于 列表[j 减 1]：
            做 追加 用 跑道列表、做 列表_切片 用 列表、i、j。
        否则若 j 小于 n 并且 列表[j] 小于 列表[j 减 1]：
            ※ 检测降序跑道，反转为升序
            只要 j 小于 n 并且 列表[j] 小于 列表[j 减 1]：
            做 追加 用 跑道列表、做 列表_反转 用 做 列表_切片 用 列表、i、j。
        否则：
            做 追加 用 跑道列表、[列表[i]]。
        完。
        i 为 j。
    完。
    ※ 第二步: 反复合并相邻跑道
    只要 做 计数 用 跑道列表 大于 1：
        新跑道 为 []。
        可变 k 为 0。
        只要 k 加 1 小于 做 计数 用 跑道列表：
            做 追加 用 新跑道、做 _归并_合并 用 跑道列表[k]、跑道列表[k 加 1]。
            k 为 k 加 2。
        完。
        若 k 小于 做 计数 用 跑道列表：
            做 追加 用 新跑道、跑道列表[k]。
        完。
        跑道列表 为 新跑道。
    完。
    返还 跑道列表[0]。
完。

※ 高级列表操作

列表_枝叉分离 接受 列表、谓词)：
    ※ 将列表分离为满足和不满足谓词的两部分
    真部 为 []。
    假部 为 []。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 谓词 用 列表[i]：
            做 追加 用 真部、列表[i]。
        否则：
            做 追加 用 假部、列表[i]。
        完。
        i 为 i 加 1。
    完。
    返还 [真部, 假部]。
完。

列表_累积 接受 列表、初始值、累积函数)：
    可变 累加器 为 初始值。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        累加器 为 做 累积函数 用 累加器、列表[i]。
        i 为 i 加 1。
    完。
    返还 累加器。
完。

列表_扫描 接受 列表、初始值、扫描函数)：
    ※ 类似前缀和的通用版本
    结果 为 [初始值]。
    可变 累加器 为 初始值。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        累加器 为 做 扫描函数 用 累加器、列表[i]。
        做 追加 用 结果、累加器。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_去连续重复 接受 列表)：
    n 为 做 计数 用 列表。
    若 n 等于 0：
    结果 为 [列表[0]]。
    可变 i 为 1。
    只要 i 小于 n：
        若 列表[i] 不等于 列表[i 减 1]：
            做 追加 用 结果、列表[i]。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_频率统计 接受 列表)：
    ※ 返回 [[元素, 次数], ...]
    键 为 []。
    值 为 []。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        可变 找到 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 键：
            若 键[j] 等于 列表[i]：
                值[j] 为 值[j] 加 1。
                找到 为 真。
                跳出。
            完。
            j 为 j 加 1。
        完。
        若 !找到：
            做 追加 用 键、列表[i]。
            做 追加 用 值、1。
        完。
        i 为 i 加 1。
    完。
    结果 为 []。
    i 为 0。
    只要 i 小于 做 计数 用 键：
        做 追加 用 结果、[键[i]、值[i]]。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_连续子序列最大和 接受 列表)：
    ※ Kadane算法: O(n) 求最大子数组和
    n 为 做 计数 用 列表。
    若 n 等于 0：
    可变 当前和 为 列表[0]。
    可变 最大和 为 列表[0]。
    可变 i 为 1。
    只要 i 小于 n：
        若 当前和 加 列表[i] 大于 列表[i]：
            当前和 = 当前和 加 列表[i]。
        否则：
            当前和 = 列表[i]。
        完。
        若 当前和 大于 最大和：
        i 为 i 加 1。
    完。
    返还 最大和。
完。

※ 完整统计

列表_完整统计 接受 )：
    返还 [。
        「模块」, 「原生列表」,。
        「基础查找函数」, 6,。
        「切片复制函数」, 4,。
        「统计聚合函数」, 6,。
        「插入排序函数」, 2,。
        「快速排序函数」, 2,。
        「堆排序函数」, 2,。
        「归并排序函数」, 2,。
        「惯性排序函数」, 1,。
        「二分查找函数」, 1,。
        「集合操作函数」, 4,。
        「滑动窗口函数」, 3,。
        「前缀和函数」, 3,。
        「中位数百分位函数」, 3,。
        「高级操作函数」, 7,。
        「总函数数」, 58。
    ]。
完。

可变 AI_原生列表_操作热度 为 0。

AI_原生列表_最优算法 接受 数据量、已排序)：
    AI_原生列表_操作热度 为 AI_原生列表_操作热度 加 1。
    若 数据量 小于 16：
    若 已排序：
    若 数据量 小于 10000：
    返还 「归并排序」。
完。

本章 收束。
