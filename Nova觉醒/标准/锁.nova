《锁》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·自动扩容》。

引用《..·中枢·进程描述符》。

引用《..·中枢·字符串替换》。

引用《..·中枢·整数转字符串》。

互斥锁 是 一种 事物，含 已锁、持有者、等待数、获取次数、争用次数。

锁_创建 接受 零参：
    返还 新建 互斥锁：已锁 为 假、持有者 为 0、等待数 为 0、获取次数 为 0、争用次数 为 0。
完。

锁_获取 接受 锁：
    可变 自旋 为 0。
    只要 锁 的 已锁 等于 真：
        锁 的 等待数 为 锁 的 等待数 加 1。
        锁 的 争用次数 为 锁 的 争用次数 加 1。
        自旋 为 自旋 加 1。
        若 自旋 大于 10000：
            返还。
        完。
    完。
    锁 的 已锁 为 真。
    锁 的 持有者 为 做 进程_获取PID。
    锁 的 等待数 为 0。
    锁 的 获取次数 为 锁 的 获取次数 加 1。
完。

锁_尝试获取 接受 锁：
    若 锁 的 已锁 等于 真：
        返还 假。
    完。
    锁 的 已锁 为 真。
    锁 的 持有者 为 做 进程_获取PID。
    锁 的 获取次数 为 锁 的 获取次数 加 1。
    返还 真。
完。

锁_带超时获取 接受 锁、超时毫秒：
    开始 为 做 当前时间_毫秒。
    只要 锁 的 已锁 等于 真：
        若 做 当前时间_毫秒 减 开始 大于 超时毫秒：
            返还 假。
        完。
    完。
    锁 的 已锁 为 真。
    锁 的 持有者 为 做 进程_获取PID。
    锁 的 获取次数 为 锁 的 获取次数 加 1。
    返还 真。
完。

锁_释放 接受 锁：
    锁 的 已锁 为 假。
    锁 的 持有者 为 0。
完。

锁_已锁 接受 锁：
    返还 锁 的 已锁。
完。

锁_持有者 接受 锁：
    返还 锁 的 持有者。
完。

锁_统计 接受 锁：
    返还 一组：锁 的 获取次数、锁 的 争用次数。
完。

递归锁 是 一种 事物，含 已锁、持有者、重入计数。

递归锁_创建 接受 零参：
    返还 新建 递归锁：已锁 为 假、持有者 为 0、重入计数 为 0。
完。

递归锁_获取 接受 锁：
    当前PID 为 做 进程_获取PID。
    若 锁 的 已锁 并且 锁 的 持有者 等于 当前PID：
        锁 的 重入计数 为 锁 的 重入计数 加 1。
        返还。
    完。
    只要 锁 的 已锁：
    完。
    锁 的 已锁 为 真。
    锁 的 持有者 为 当前PID。
    锁 的 重入计数 为 1。
完。

递归锁_释放 接受 锁：
    当前PID 为 做 进程_获取PID。
    若 锁 的 持有者 不等于 当前PID：
        返还。
    完。
    锁 的 重入计数 为 锁 的 重入计数 减 1。
    若 锁 的 重入计数 等于 0：
        锁 的 已锁 为 假。
        锁 的 持有者 为 0。
    完。
完。

递归锁_深度 接受 锁：
    返还 锁 的 重入计数。
完。

读写锁结构 是 一种 事物，含 读者数、写锁、写等待数、读等待数、总读获取、总写获取。

读写锁_创建 接受 零参：
    返还 新建 读写锁结构：读者数 为 0、写锁 为 假、写等待数 为 0、读等待数 为 0、总读获取 为 0、总写获取 为 0。
完。

读写锁_读锁 接受 锁：
    只要 锁 的 写锁 或者 锁 的 写等待数 大于 0：
        锁 的 读等待数 为 锁 的 读等待数 加 1。
    完。
    锁 的 读者数 为 锁 的 读者数 加 1。
    锁 的 读等待数 为 0。
    锁 的 总读获取 为 锁 的 总读获取 加 1。
完。

读写锁_尝试读锁 接受 锁：
    若 锁 的 写锁 或者 锁 的 写等待数 大于 0：
        返还 假。
    完。
    锁 的 读者数 为 锁 的 读者数 加 1。
    锁 的 总读获取 为 锁 的 总读获取 加 1。
    返还 真。
完。

读写锁_读解锁 接受 锁：
    若 锁 的 读者数 大于 0：
        锁 的 读者数 为 锁 的 读者数 减 1。
    完。
完。

读写锁_写锁 接受 锁：
    锁 的 写等待数 为 锁 的 写等待数 加 1。
    只要 锁 的 读者数 大于 0 或者 锁 的 写锁：
    完。
    锁 的 写锁 为 真。
    锁 的 写等待数 为 锁 的 写等待数 减 1。
    锁 的 总写获取 为 锁 的 总写获取 加 1。
完。

读写锁_尝试写锁 接受 锁：
    若 锁 的 读者数 大于 0 或者 锁 的 写锁：
        返还 假。
    完。
    锁 的 写锁 为 真。
    锁 的 总写获取 为 锁 的 总写获取 加 1。
    返还 真。
完。

读写锁_写解锁 接受 锁：
    锁 的 写锁 为 假。
完。

读写锁_读者数 接受 锁：
    返还 锁 的 读者数。
完。

读写锁_是写锁 接受 锁：
    返还 锁 的 写锁。
完。

读写锁_统计 接受 锁：
    返还 一组：锁 的 总读获取、锁 的 总写获取。
完。

条件变量结构 是 一种 事物，含 等待计数、通知计数。

条件变量_创建 接受 零参：
    返还 新建 条件变量结构：等待计数 为 0、通知计数 为 0。
完。

条件变量_等待 接受 条件、锁：
    条件 的 等待计数 为 条件 的 等待计数 加 1。
    做 锁_释放 用 锁。
    只要 条件 的 等待计数 大于 0 并且 条件 的 通知计数 等于 0：
    完。
    若 条件 的 通知计数 大于 0：
        条件 的 通知计数 为 条件 的 通知计数 减 1。
    完。
    做 锁_获取 用 锁。
完。

条件变量_带超时等待 接受 条件、锁、超时毫秒：
    条件 的 等待计数 为 条件 的 等待计数 加 1。
    做 锁_释放 用 锁。
    开始 为 做 当前时间_毫秒。
    只要 条件 的 等待计数 大于 0 并且 条件 的 通知计数 等于 0：
        若 做 当前时间_毫秒 减 开始 大于 超时毫秒：
            条件 的 等待计数 为 条件 的 等待计数 减 1。
            做 锁_获取 用 锁。
            返还 假。
        完。
    完。
    若 条件 的 通知计数 大于 0：
        条件 的 通知计数 为 条件 的 通知计数 减 1。
    完。
    做 锁_获取 用 锁。
    返还 真。
完。

条件变量_通知一个 接受 条件：
    若 条件 的 等待计数 大于 0：
        条件 的 等待计数 为 条件 的 等待计数 减 1。
        条件 的 通知计数 为 条件 的 通知计数 加 1。
    完。
完。

条件变量_通知全部 接受 条件：
    条件 的 通知计数 为 条件 的 通知计数 加 条件 的 等待计数。
    条件 的 等待计数 为 0。
完。

信号量结构 是 一种 事物，含 值、最大值。

信号量_创建 接受 初始值：
    返还 新建 信号量结构：值 为 初始值、最大值 为 2147483647。
完。

有界信号量_创建 接受 初始值、最大值：
    返还 新建 信号量结构：值 为 初始值、最大值 为 最大值。
完。

信号量_等待 接受 s：
    只要 s 的 值 小于或等于 0：
    完。
    s 的 值 为 s 的 值 减 1。
完。

信号量_尝试等待 接受 s：
    若 s 的 值 小于或等于 0：
        返还 假。
    完。
    s 的 值 为 s 的 值 减 1。
    返还 真。
完。

信号量_带超时等待 接受 s、超时毫秒：
    开始 为 做 当前时间_毫秒。
    只要 s 的 值 小于或等于 0：
        若 做 当前时间_毫秒 减 开始 大于 超时毫秒：
            返还 假。
        完。
    完。
    s 的 值 为 s 的 值 减 1。
    返还 真。
完。

信号量_信号 接受 s：
    若 s 的 值 小于 s 的 最大值：
        s 的 值 为 s 的 值 加 1。
    完。
完。

信号量_值 接受 s：
    返还 s 的 值。
完。

锁守卫 是 一种 事物，含 锁引用、已释放。

锁守卫_创建 接受 锁：
    做 锁_获取 用 锁。
    返还 新建 锁守卫：锁引用 为 锁、已释放 为 假。
完。

锁守卫_释放 接受 守卫：
    若 非 守卫 的 已释放：
        做 锁_释放 用 守卫 的 锁引用。
        守卫 的 已释放 为 真。
    完。
完。

可变 死锁_锁持有表 为 做 字典。

死锁_注册获取 接受 锁ID、线程ID：
    做 字典设 用 死锁_锁持有表、做 转字符串 用 锁ID、线程ID。
完。

死锁_注册释放 接受 锁ID：
    做 字典删 用 死锁_锁持有表、做 转字符串 用 锁ID。
完。

死锁_检查 接受 锁ID、线程ID：
    持有者 为 做 字典取 用 死锁_锁持有表、做 转字符串 用 锁ID。
    若 持有者 等于 线程ID：
        返还 真。
    完。
    返还 假。
完。

可变 锁_获取总数 为 0。

可变 锁_释放总数 为 0。

可变 锁_竞争次数 为 0。

可变 锁_自旋等待总 为 0。

锁_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    持有中 为 锁_获取总数 减 锁_释放总数。
    若 持有中 大于 100：
        做 追加 用 问题、「锁泄漏: 」 加 做 整数转字符串 用 持有中 加 「个未释放」。
    完。
    若 锁_竞争次数 大于 锁_获取总数 除以 2 并且 锁_获取总数 大于 100：
        做 追加 用 问题、「锁竞争率过高」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「锁系统健康」。
    完。
    返还 问题。
完。

锁_健康评估 接受 零参：
    返还 真。
完。

锁_综合报告 接受 零参：
    做 打印 用 「[锁] ═══ 综合报告 ═══」。
    做 打印 用 「  锁_获取总数: 」 加 锁_获取总数。
    做 打印 用 「  锁_释放总数: 」 加 锁_释放总数。
    做 打印 用 「  锁_竞争次数: 」 加 锁_竞争次数。
    做 打印 用 「  健康: 」 加 若 做 锁_健康评估 则 「正常」 否则 「异常」。
完。

锁_全局重置 接受 零参：
    锁_获取总数 为 0。
    锁_释放总数 为 0。
    锁_竞争次数 为 0。
    做 打印 用 「[锁] 统计已重置」。
    返还 0。
完。

本章 收束。
