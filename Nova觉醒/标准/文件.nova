《文件》

引用《..·中枢·类型定义》。

引用《..·感知·内核直连层》。

引用《..·执行·平台时间》。

引用《..·中枢·字符串工具》。

引用《..·中枢·内存初始化》。

引用《..·执行·控制台输出》。

引用《..·中枢·整数转字符串》。

文件_只读 为 0。

文件_只写 为 1。

文件_读写 为 2。

文件_创建 为 0x40。

文件_截断 为 0x200。

文件_追加 为 0x400。

定位_起始 为 0。

定位_当前 为 1。

定位_末尾 为 2。

调用号_打开 为 2。

调用号_关闭 为 3。

调用号_读 为 0。

调用号_写 为 1。

调用号_定位 为 8。

调用号_状态 为 4。

调用号_删除 为 87。

调用号_重命名 为 82。

缓冲大小 为 4096。

可变 打开文件数 为 0。

可变 读总字节 为 0。

可变 写总字节 为 0。

文件_打开 接受 路径、标志：
    描述 为 做 系统调用 用 调用号_打开、路径、标志、0x1B6、0、0。
    若 描述 大于或等于 0：
        打开文件数 为 打开文件数 加 1。
    完。
    返还 描述。
完。

文件_关闭 接受 描述：
    若 描述 小于 0：
        返还 错误_无效参数。
    完。
    打开文件数 为 打开文件数 减 1。
    返还 做 系统调用 用 调用号_关闭、描述、0、0、0、0。
完。

文件_读 接受 描述、缓冲、大小：
    若 描述 小于 0：
        返还 错误_无效参数。
    完。
    已读 为 做 系统调用 用 调用号_读、描述、缓冲、大小、0、0。
    若 已读 大于 0：
        读总字节 为 读总字节 加 已读。
    完。
    返还 已读。
完。

文件_写 接受 描述、数据、大小：
    若 描述 小于 0：
        返还 错误_无效参数。
    完。
    已写 为 做 系统调用 用 调用号_写、描述、数据、大小、0、0。
    若 已写 大于 0：
        写总字节 为 写总字节 加 已写。
    完。
    返还 已写。
完。

文件_定位 接受 描述、偏移、起点：
    若 描述 小于 0：
        返还 错误_无效参数。
    完。
    返还 做 系统调用 用 调用号_定位、描述、偏移、起点、0、0。
完。

文件_获取大小 接受 描述：
    当前 为 做 文件_定位 用 描述、0、定位_当前。
    末尾 为 做 文件_定位 用 描述、0、定位_末尾。
    做 文件_定位 用 描述、当前、定位_起始。
    返还 末尾。
完。

文件_存在 接受 路径：
    描述 为 做 文件_打开 用 路径、文件_只读。
    若 描述 大于或等于 0：
        做 文件_关闭 用 描述。
        返还 真。
    完。
    返还 假。
完。

文件_读全部 接受 路径：
    描述 为 做 文件_打开 用 路径、文件_只读。
    若 描述 小于 0：
        返还 0。
    完。
    大小 为 做 文件_获取大小 用 描述。
    若 大小 小于或等于 0：
        做 文件_关闭 用 描述。
        返还 0。
    完。
    缓冲 为 做 堆_分配 用 大小 加 1。
    若 缓冲 等于 0：
        做 文件_关闭 用 描述。
        返还 0。
    完。
    做 文件_定位 用 描述、0、定位_起始。
    可变 已读总 为 0。
    只要 已读总 小于 大小：
        本次 为 做 文件_读 用 描述、缓冲 加 已读总、大小 减 已读总。
        若 本次 小于或等于 0：
            跳出。
        完。
        已读总 为 已读总 加 本次。
    完。
    做 内存_写8 用 缓冲 加 已读总、0。
    做 文件_关闭 用 描述。
    返还 缓冲。
完。

文件_写全部 接受 路径、数据、大小：
    描述 为 做 文件_打开 用 路径、文件_只写 位或 文件_创建 位或 文件_截断。
    若 描述 小于 0：
        返还 描述。
    完。
    可变 已写总 为 0。
    只要 已写总 小于 大小：
        本次 为 做 文件_写 用 描述、数据 加 已写总、大小 减 已写总。
        若 本次 小于或等于 0：
            跳出。
        完。
        已写总 为 已写总 加 本次。
    完。
    做 文件_关闭 用 描述。
    返还 已写总。
完。

文件_追加写 接受 路径、数据、大小：
    描述 为 做 文件_打开 用 路径、文件_只写 位或 文件_创建 位或 文件_追加。
    若 描述 小于 0：
        返还 描述。
    完。
    已写 为 做 文件_写 用 描述、数据、大小。
    做 文件_关闭 用 描述。
    返还 已写。
完。

文件_删除 接受 路径：
    返还 做 系统调用 用 调用号_删除、路径、0、0、0、0。
完。

文件_重命名 接受 旧路径、新路径：
    返还 做 系统调用 用 调用号_重命名、旧路径、新路径、0、0、0。
完。

文件_复制 接受 源路径、目标路径：
    数据 为 做 文件_读全部 用 源路径。
    若 数据 等于 0：
        返还 错误_不存在。
    完。
    大小 为 做 字串_长度 用 数据。
    返还 做 文件_写全部 用 目标路径、数据、大小。
完。

文件_读行 接受 描述：
    行缓冲 为 做 堆_分配 用 1024。
    可变 行长 为 0。
    可变 完成 为 假。
    只要 非 完成 并且 行长 小于 1023：
        字节缓冲 为 做 堆_分配 用 1。
        已读 为 做 文件_读 用 描述、字节缓冲、1。
        若 已读 小于或等于 0：
            完成 为 真。
            继续。
        完。
        字节 为 做 内存_读8 用 字节缓冲。
        做 堆_释放 用 字节缓冲。
        若 字节 等于 10：
            完成 为 真。
        否则若 字节 不等于 13：
            做 内存_写8 用 行缓冲 加 行长、字节。
            行长 为 行长 加 1。
        完。
    完。
    做 内存_写8 用 行缓冲 加 行长、0。
    若 行长 等于 0：
        做 堆_释放 用 行缓冲。
        返还 0。
    完。
    返还 行缓冲。
完。

文件_完整统计 接受 零参：
    返还 一组：打开文件数、读总字节、写总字节。
完。

调用号_创建目录 为 83。

调用号_删除目录 为 84。

调用号_获取目录项 为 78。

调用号_获取工作目录 为 79。

调用号_切换工作目录 为 80。

目录_创建 接受 路径、模式：
    返还 做 系统调用 用 调用号_创建目录、路径、模式、0、0、0。
完。

目录_创建默认 接受 路径：
    返还 做 目录_创建 用 路径、493。
完。

目录_删除 接受 路径：
    返还 做 系统调用 用 调用号_删除目录、路径、0、0、0、0。
完。

目录_获取工作目录 接受 零参：
    缓冲 为 做 堆_分配 用 4096。
    若 缓冲 等于 0：
        返还 0。
    完。
    结果 为 做 系统调用 用 调用号_获取工作目录、缓冲、4096、0、0、0。
    若 结果 小于 0：
        做 堆_释放 用 缓冲。
        返还 0。
    完。
    返还 缓冲。
完。

目录_切换工作目录 接受 路径：
    返还 做 系统调用 用 调用号_切换工作目录、路径、0、0、0、0。
完。

目录_存在 接受 路径：
    状态 为 做 文件_状态 用 路径。
    若 状态 等于 0：
        返还 假。
    完。
    返还 状态 的 模式 位与 61440 等于 16384。
完。

文件状态 是 一种 事物，含 设备号、节点号、链接数、模式、用户ID、组ID、大小、块数、块大小、访问时间、修改时间、创建时间。

文件_状态 接受 路径：
    缓冲 为 做 堆_分配 用 144。
    若 缓冲 等于 0：
        返还 0。
    完。
    结果 为 做 系统调用 用 调用号_状态、路径、缓冲、0、0、0。
    若 结果 小于 0：
        做 堆_释放 用 缓冲。
        返还 0。
    完。
    状态 为 新建 文件状态。
    状态 的 设备号 为 做 内存_读64 用 缓冲。
    状态 的 节点号 为 做 内存_读64 用 缓冲 加 8。
    状态 的 链接数 为 做 内存_读64 用 缓冲 加 16。
    状态 的 模式 为 做 内存_读32 用 缓冲 加 24。
    状态 的 用户ID 为 做 内存_读32 用 缓冲 加 28。
    状态 的 组ID 为 做 内存_读32 用 缓冲 加 32。
    状态 的 大小 为 做 内存_读64 用 缓冲 加 48。
    状态 的 块数 为 做 内存_读64 用 缓冲 加 56。
    状态 的 块大小 为 做 内存_读64 用 缓冲 加 64。
    做 堆_释放 用 缓冲。
    返还 状态。
完。

文件_大小 接受 路径：
    状态 为 做 文件_状态 用 路径。
    若 状态 等于 0：
        返还 负 1。
    完。
    返还 状态 的 大小。
完。

文件_是普通文件 接受 路径：
    状态 为 做 文件_状态 用 路径。
    若 状态 等于 0：
        返还 假。
    完。
    返还 状态 的 模式 位与 61440 等于 32768。
完。

文件_是目录 接受 路径：
    返还 做 目录_存在 用 路径。
完。

文件_是符号链接 接受 路径：
    状态 为 做 文件_状态 用 路径。
    若 状态 等于 0：
        返还 假。
    完。
    返还 状态 的 模式 位与 61440 等于 40960。
完。

文件_权限 接受 路径：
    状态 为 做 文件_状态 用 路径。
    若 状态 等于 0：
        返还 0。
    完。
    返还 状态 的 模式 位与 511。
完。

缓冲文件 是 一种 事物，含 描述符、缓冲、缓冲大小、已用、模式、已关闭。

缓冲文件_打开 接受 路径、标志：
    fd 为 做 文件_打开 用 路径、标志。
    若 fd 小于 0：
        返还 0。
    完。
    bf 为 新建 缓冲文件。
    bf 的 描述符 为 fd。
    bf 的 缓冲 为 做 堆_分配 用 缓冲大小。
    bf 的 缓冲大小 为 缓冲大小。
    bf 的 已用 为 0。
    bf 的 模式 为 标志。
    bf 的 已关闭 为 假。
    返还 bf。
完。

缓冲文件_写 接受 bf、数据、大小：
    若 bf 等于 0 或者 bf 的 已关闭：
        返还 错误_无效参数。
    完。
    若 bf 的 已用 加 大小 大于 bf 的 缓冲大小：
        做 缓冲文件_刷新 用 bf。
    完。
    若 大小 大于或等于 bf 的 缓冲大小：
        返还 做 文件_写 用 bf 的 描述符、数据、大小。
    完。
    做 内存_复制 用 bf 的 缓冲 加 bf 的 已用、数据、大小。
    bf 的 已用 为 bf 的 已用 加 大小。
    返还 大小。
完。

缓冲文件_刷新 接受 bf：
    若 bf 等于 0 或者 bf 的 已用 等于 0：
        返还。
    完。
    做 文件_写 用 bf 的 描述符、bf 的 缓冲、bf 的 已用。
    bf 的 已用 为 0。
完。

缓冲文件_关闭 接受 bf：
    若 bf 等于 0 或者 bf 的 已关闭：
        返还。
    完。
    做 缓冲文件_刷新 用 bf。
    做 文件_关闭 用 bf 的 描述符。
    做 堆_释放 用 bf 的 缓冲。
    bf 的 已关闭 为 真。
完。

路径_扩展名 接受 路径：
    若 路径 等于 0：
        返还 0。
    完。
    长度 为 做 字串_长度 用 路径。
    可变 i 为 长度 减 1。
    只要 i 大于或等于 0：
        ch 为 做 字节取 用 路径、i。
        若 ch 等于 46：
            返还 路径 加 i 加 1。
        完。
        若 ch 等于 47：
            返还 0。
        完。
        i 为 i 减 1。
    完。
    返还 0。
完。

路径_文件名 接受 路径：
    若 路径 等于 0：
        返还 0。
    完。
    长度 为 做 字串_长度 用 路径。
    可变 最后斜杠 为 负 1。
    可变 i 为 0。
    只要 i 小于 长度：
        若 做 字节取 用 路径、i 等于 47：
            最后斜杠 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最后斜杠 小于 0：
        返还 路径。
    完。
    返还 路径 加 最后斜杠 加 1。
完。

路径_目录名 接受 路径：
    若 路径 等于 0：
        返还 0。
    完。
    长度 为 做 字串_长度 用 路径。
    可变 最后斜杠 为 负 1。
    可变 i 为 0。
    只要 i 小于 长度：
        若 做 字节取 用 路径、i 等于 47：
            最后斜杠 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最后斜杠 小于或等于 0：
        返还 「/」。
    完。
    结果 为 做 堆_分配 用 最后斜杠 加 1。
    做 内存_复制 用 结果、路径、最后斜杠。
    做 内存_写8 用 结果 加 最后斜杠、0。
    返还 结果。
完。

路径_拼接 接受 基路径、子路径：
    若 基路径 等于 0：
        返还 子路径。
    完。
    若 子路径 等于 0：
        返还 基路径。
    完。
    基长 为 做 字串_长度 用 基路径。
    子长 为 做 字串_长度 用 子路径。
    需要斜杠 为 基长 大于 0 并且 做 字节取 用 基路径、基长 减 1 不等于 47。
    总长 为 基长 加 子长 加 若 需要斜杠 则 1 否则 0 加 1。
    结果 为 做 堆_分配 用 总长。
    做 内存_复制 用 结果、基路径、基长。
    若 需要斜杠：
        做 内存_写8 用 结果 加 基长、47。
        基长 为 基长 加 1。
    完。
    做 内存_复制 用 结果 加 基长、子路径、子长。
    做 内存_写8 用 结果 加 基长 加 子长、0。
    返还 结果。
完。

路径_是绝对 接受 路径：
    若 路径 等于 0：
        返还 假。
    完。
    返还 做 字节取 用 路径、0 等于 47。
完。

文件_读字符串 接受 路径：
    返还 做 文件_读全部 用 路径。
完。

文件_写字符串 接受 路径、文本：
    若 文本 等于 0：
        返还 0。
    完。
    返还 做 文件_写全部 用 路径、文本、做 字串_长度 用 文本。
完。

文件_追加字符串 接受 路径、文本：
    若 文本 等于 0：
        返还 0。
    完。
    返还 做 文件_追加写 用 路径、文本、做 字串_长度 用 文本。
完。

文件_读行列表 接受 路径：
    描述 为 做 文件_打开 用 路径、文件_只读。
    若 描述 小于 0：
        返还 一组：（空）。
    完。
    可变 行列表 为 一组：（空）。
    可变 行 为 做 文件_读行 用 描述。
    只要 行 不等于 0：
        做 追加 用 行列表、行。
        行 为 做 文件_读行 用 描述。
    完。
    做 文件_关闭 用 描述。
    返还 行列表。
完。

文件_写行列表 接受 路径、行列表：
    描述 为 做 文件_打开 用 路径、文件_只写 位或 文件_创建 位或 文件_截断。
    若 描述 小于 0：
        返还 描述。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 行列表 的 第 i 项。
        做 文件_写 用 描述、行、做 字串_长度 用 行。
        做 文件_写 用 描述、「\n」、1。
        i 为 i 加 1。
    完。
    做 文件_关闭 用 描述。
    返还 0。
完。

权限_所有者读 为 256。

权限_所有者写 为 128。

权限_所有者执 为 64。

权限_组读 为 32。

权限_组写 为 16。

权限_组执 为 8。

权限_其他读 为 4。

权限_其他写 为 2。

权限_其他执 为 1。

权限_全部读写 为 438。

权限_全部读写执 为 511。

权限_默认文件 为 420。

权限_默认目录 为 493。

可变 文件_打开总次数 为 0。

可变 文件_关闭总次数 为 0。

可变 文件_读总次数 为 0。

可变 文件_写总次数 为 0。

可变 文件_打开失败数 为 0。

可变 文件_读失败数 为 0。

可变 文件_写失败数 为 0。

文件IO_统计 接受 零参：
    返还 一本字典：打开文件 对应 打开文件数、读字节 对应 读总字节、写字节 对应 写总字节、打开次数 对应 文件_打开总次数、关闭次数 对应 文件_关闭总次数、读次数 对应 文件_读总次数、写次数 对应 文件_写总次数、打开失败 对应 文件_打开失败数、读失败 对应 文件_读失败数、写失败 对应 文件_写失败数。
完。

文件IO_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 打开文件数 大于 1024：
        做 追加 用 问题、「打开文件数过多: 」 加 做 整数转字符串 用 打开文件数。
    完。
    若 文件_打开失败数 大于 文件_打开总次数 除以 3 并且 文件_打开总次数 大于 100：
        做 追加 用 问题、「文件打开失败率过高」。
    完。
    若 文件_读失败数 大于 文件_读总次数 除以 5 并且 文件_读总次数 大于 100：
        做 追加 用 问题、「文件读取失败率过高」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「文件IO健康」。
    完。
    返还 问题。
完。

文件IO_重置统计 接受 零参：
    文件_打开总次数 为 0。
    文件_关闭总次数 为 0。
    文件_读总次数 为 0。
    文件_写总次数 为 0。
    文件_打开失败数 为 0。
    文件_读失败数 为 0。
    文件_写失败数 为 0。
    读总字节 为 0。
    写总字节 为 0。
完。

文件IO_读写比 接受 零参：
    若 写总字节 等于 0：
        返还 0。
    完。
    返还 读总字节 乘 100 除以 写总字节。
完。

文件IO_泄漏数 接受 零参：
    返还 文件_打开总次数 减 文件_关闭总次数。
完。

可变 临时文件_计数器 为 0。

临时文件_创建 接受 前缀：
    临时文件_计数器 为 临时文件_计数器 加 1。
    路径 为 「/tmp/」 加 前缀 加 「_」 加 做 整数转字符串 用 临时文件_计数器。
    fd 为 做 文件_打开 用 路径、文件_读写 位或 文件_创建 位或 文件_截断。
    若 fd 小于 0：
        返还 0。
    完。
    返还 一本字典：描述符 对应 fd、路径 对应 路径。
完。

临时文件_删除 接受 临时：
    若 临时 等于 0：
        返还。
    完。
    做 文件_关闭 用 临时 的 描述符。
    做 文件_删除 用 临时 的 路径。
完。

可变 AI_文件_热文件表 为 一组：（空）。

AI_文件_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_文件_热文件表：
        若 AI_文件_热文件表 的 第 i 项 的 路径 等于 路径：
            AI_文件_热文件表 的 第 i 项 的 频率 为 AI_文件_热文件表 的 第 i 项 的 频率 加 1。
            返还 AI_文件_热文件表 的 第 i 项 的 频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_文件_热文件表 小于 128：
        做 追加 用 AI_文件_热文件表、一本字典：路径 对应 路径、频率 对应 1。
    完。
    返还 1。
完。

AI_文件_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
        返还 0。
    完。
    返还 32768。
完。

本章 收束。
