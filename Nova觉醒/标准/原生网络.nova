《原生网络》

引用《..·中枢·类型定义》。

引用《..·感知·内核直连层》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·内存初始化》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·系统调用入口》。

引用《..·推理·网络操作》。

AF_INET 为 2。

SOCK_STREAM 为 1。

SOCK_DGRAM 为 2。

传控客户端_创建 接受 零参：
    返还 做 系统调用_分发 用 SYS_socket、AF_INET、SOCK_STREAM、0、0、0。
完。

传控客户端_连接 接受 fd、地址、端口：
    返还 做 系统调用_分发 用 SYS_connect、fd、地址、端口、0、0。
完。

传控_发送 接受 fd、数据：
    返还 做 系统调用_分发 用 SYS_write、fd、数据、做 字符串长度 用 数据、0、0。
完。

传控_接收 接受 fd、大小：
    返还 做 系统调用_分发 用 SYS_read、fd、0、大小、0、0。
完。

传控_关闭 接受 fd：
    返还 做 系统调用_分发 用 SYS_close、fd、0、0、0、0。
完。

传控服务器_创建 接受 端口：
    fd 为 做 系统调用_分发 用 SYS_socket、AF_INET、SOCK_STREAM、0、0、0。
    若 fd 小于 0：
        返还 fd。
    完。
    做 系统调用_分发 用 SYS_bind、fd、0、端口、0、0。
    做 系统调用_分发 用 SYS_listen、fd、128、0、0、0。
    返还 fd。
完。

传控服务器_接受 接受 fd：
    返还 做 系统调用_分发 用 SYS_accept、fd、0、0、0、0。
完。

数报_创建 接受 零参：
    返还 做 系统调用_分发 用 SYS_socket、AF_INET、SOCK_DGRAM、0、0、0。
完。

数报_绑定 接受 fd、端口：
    返还 做 系统调用_分发 用 SYS_bind、fd、0、端口、0、0。
完。

数报_发送到 接受 fd、数据、地址、端口：
    返还 做 系统调用_分发 用 SYS_sendto、fd、数据、地址、端口、0。
完。

数报_接收从 接受 fd、大小：
    返还 做 系统调用_分发 用 SYS_recvfrom、fd、0、大小、0、0。
完。

网络_HTTP_GET 接受 主机、端口、路径：
    fd 为 做 传控客户端_创建。
    若 fd 小于 0：
        返还 空。
    完。
    连接结果 为 做 传控客户端_连接 用 fd、主机、端口。
    若 连接结果 小于 0：
        做 传控_关闭 用 fd。
        返还 空。
    完。
    请求 为 「GET 」 加 路径 加 「 HTTP/1.1\r\nHost: 」 加 主机 加 「\r\nConnection: close\r\n\r\n」。
    做 传控_发送 用 fd、请求。
    可变 响应 为 「」。
    可变 块 为 做 传控_接收 用 fd、65536。
    只要 块 不等于 空 并且 做 字符串长度 用 块 大于 0：
        响应 为 响应 加 块。
        块 为 做 传控_接收 用 fd、65536。
    完。
    做 传控_关闭 用 fd。
    头结束 为 做 字符串查找 用 响应、「\r\n\r\n」。
    若 头结束 大于 0：
        返还 做 字符串子串 用 响应、头结束 加 4、做 字符串长度 用 响应。
    完。
    返还 响应。
完。

网络_HTTP_POST 接受 端点、请求体、头列表：
    解析 为 做 智能体网络_解析URL 用 端点。
    若 解析 等于 空：
        返还 空。
    完。
    fd 为 做 传控客户端_创建。
    若 fd 小于 0：
        返还 空。
    完。
    连接结果 为 做 传控客户端_连接 用 fd、解析 的 主机、解析 的 端口。
    若 连接结果 小于 0：
        做 传控_关闭 用 fd。
        返还 空。
    完。
    可变 请求 为 「POST 」 加 解析 的 路径 加 「 HTTP/1.1\r\n」。
    请求 为 请求 加 「Host: 」 加 解析 的 主机 加 「\r\n」。
    请求 为 请求 加 「Content-Length: 」 加 做 整数转字符串 用 做 字符串长度 用 请求体 加 「\r\n」。
    若 做 是列表 用 头列表：
        可变 i 为 0。
        只要 i 小于 做 计数 用 头列表：
            请求 为 请求 加 头列表 的 第 i 项 的 第 0 项 加 「: 」 加 头列表 的 第 i 项 的 第 1 项 加 「\r\n」。
            i 为 i 加 1。
        完。
    完。
    请求 为 请求 加 「Connection: close\r\n\r\n」。
    请求 为 请求 加 请求体。
    做 传控_发送 用 fd、请求。
    可变 响应 为 「」。
    可变 块 为 做 传控_接收 用 fd、65536。
    只要 块 不等于 空 并且 做 字符串长度 用 块 大于 0：
        响应 为 响应 加 块。
        块 为 做 传控_接收 用 fd、65536。
    完。
    做 传控_关闭 用 fd。
    头结束 为 做 字符串查找 用 响应、「\r\n\r\n」。
    若 头结束 大于 0：
        返还 做 字符串子串 用 响应、头结束 加 4、做 字符串长度 用 响应。
    完。
    返还 响应。
完。

NNIO_超时_连接 为 30000。

NNIO_超时_读 为 60000。

NNIO_超时_写 为 30000。

NNIO_最大重试 为 3。

NNIO_缓冲区大小 为 65536。

NNIO_最大连接数 为 1024。

可变 NNIO_发送字节数 为 0。

可变 NNIO_接收字节数 为 0。

可变 NNIO_连接成功数 为 0。

可变 NNIO_连接失败数 为 0。

可变 NNIO_重试次数 为 0。

可变 NNIO_错误次数 为 0。

可变 NNIO_AI检测次数 为 0。

URL解析结果 是 一种 事物，含 协议、主机、端口、路径、查询。

URL_解析 接受 文本：
    若 文本 等于 空 或者 做 字符串长度 用 文本 等于 0：
        返还 空。
    完。
    结果 为 做 堆_分配 用 URL解析结果。
    结果 的 协议 为 「http」。
    结果 的 端口 为 80。
    可变 位置 为 0。
    idx 为 做 字符串查找 用 文本、「://」。
    若 idx 大于 0：
        结果 的 协议 为 做 字符串子串 用 文本、0、idx。
        若 结果 的 协议 等于 「https」：
            结果 的 端口 为 443。
        完。
        位置 为 idx 加 3。
    完。
    剩余 为 做 字符串子串 用 文本、位置、做 字符串长度 用 文本。
    斜线位 为 做 字符串查找 用 剩余、「/」。
    若 斜线位 小于 0：
        斜线位 为 做 字符串长度 用 剩余。
    完。
    主机部分 为 做 字符串子串 用 剩余、0、斜线位。
    冒号位 为 做 字符串查找 用 主机部分、「:」。
    若 冒号位 大于 0：
        结果 的 主机 为 做 字符串子串 用 主机部分、0、冒号位。
        结果 的 端口 为 做 字符串转整数 用 做 字符串子串 用 主机部分、冒号位 加 1、做 字符串长度 用 主机部分。
    否则：
        结果 的 主机 为 主机部分。
    完。
    若 斜线位 小于 做 字符串长度 用 剩余：
        路径串 为 做 字符串子串 用 剩余、斜线位、做 字符串长度 用 剩余。
        问号位 为 做 字符串查找 用 路径串、「?」。
        若 问号位 大于 0：
            结果 的 路径 为 做 字符串子串 用 路径串、0、问号位。
            结果 的 查询 为 做 字符串子串 用 路径串、问号位 加 1、做 字符串长度 用 路径串。
        否则：
            结果 的 路径 为 路径串。
            结果 的 查询 为 「」。
        完。
    否则：
        结果 的 路径 为 「/」。
        结果 的 查询 为 「」。
    完。
    返还 结果。
完。

HTTP响应 是 一种 事物，含 状态码、状态文本、头部、头部数、体。

HTTP_解析响应 接受 原始数据：
    若 原始数据 等于 空：
        返还 空。
    完。
    resp 为 做 堆_分配 用 HTTP响应。
    第一行结束 为 做 字符串查找 用 原始数据、「\r\n」。
    若 第一行结束 小于 0：
        返还 空。
    完。
    状态行 为 做 字符串子串 用 原始数据、0、第一行结束。
    sp1 为 做 字符串查找 用 状态行、「 」。
    若 sp1 大于 0：
        剩 为 做 字符串子串 用 状态行、sp1 加 1、做 字符串长度 用 状态行。
        sp2 为 做 字符串查找 用 剩、「 」。
        若 sp2 大于 0：
            resp 的 状态码 为 做 字符串转整数 用 做 字符串子串 用 剩、0、sp2。
            resp 的 状态文本 为 做 字符串子串 用 剩、sp2 加 1、做 字符串长度 用 剩。
        否则：
            resp 的 状态码 为 做 字符串转整数 用 剩。
            resp 的 状态文本 为 「」。
        完。
    完。
    头体分割 为 做 字符串查找 用 原始数据、「\r\n\r\n」。
    若 头体分割 大于 0：
        resp 的 体 为 做 字符串子串 用 原始数据、头体分割 加 4、做 字符串长度 用 原始数据。
    否则：
        resp 的 体 为 「」。
    完。
    返还 resp。
完。

NNIO_计算退避延迟 接受 重试次、基础延迟：
    可变 延迟 为 基础延迟。
    可变 i 为 0。
    只要 i 小于 重试次：
        延迟 为 延迟 乘 2。
        i 为 i 加 1。
    完。
    若 延迟 大于 60000：
        延迟 为 60000。
    完。
    抠动 为 延迟 除以 4。
    返还 延迟 加 抠动 取余于 100。
完。

NNIO_带重试连接 接受 主机、端口、最大重试：
    可变 重试次 为 0。
    只要 重试次 小于或等于 最大重试：
        fd 为 做 传控客户端_创建。
        若 fd 小于 0：
            NNIO_错误次数 为 NNIO_错误次数 加 1。
            返还 负 1。
        完。
        rc 为 做 传控客户端_连接 用 fd、主机、端口。
        若 rc 大于或等于 0：
            NNIO_连接成功数 为 NNIO_连接成功数 加 1。
            返还 fd。
        完。
        做 传控_关闭 用 fd。
        NNIO_重试次数 为 NNIO_重试次数 加 1。
        延迟 为 做 NNIO_计算退避延迟 用 重试次、1000。
        做 系统_休眠_毫秒 用 延迟。
        重试次 为 重试次 加 1。
    完。
    NNIO_连接失败数 为 NNIO_连接失败数 加 1。
    返还 负 1。
完。

网络_HTTP_通用请求 接受 方法、主机、端口、路径、请求体、头列表：
    fd 为 做 NNIO_带重试连接 用 主机、端口、NNIO_最大重试。
    若 fd 小于 0：
        返还 空。
    完。
    可变 请求 为 方法 加 「 」 加 路径 加 「 HTTP/1.1\r\n」。
    请求 为 请求 加 「Host: 」 加 主机 加 「\r\n」。
    若 请求体 不等于 空 并且 做 字符串长度 用 请求体 大于 0：
        请求 为 请求 加 「Content-Length: 」 加 做 整数转字符串 用 做 字符串长度 用 请求体 加 「\r\n」。
    完。
    若 做 是列表 用 头列表：
        可变 i 为 0。
        只要 i 小于 做 计数 用 头列表：
            请求 为 请求 加 头列表 的 第 i 项 的 第 0 项 加 「: 」 加 头列表 的 第 i 项 的 第 1 项 加 「\r\n」。
            i 为 i 加 1。
        完。
    完。
    请求 为 请求 加 「Connection: close\r\n\r\n」。
    若 请求体 不等于 空：
        请求 为 请求 加 请求体。
    完。
    做 传控_发送 用 fd、请求。
    NNIO_发送字节数 为 NNIO_发送字节数 加 做 字符串长度 用 请求。
    可变 响应 为 「」。
    可变 块 为 做 传控_接收 用 fd、NNIO_缓冲区大小。
    只要 块 不等于 空 并且 做 字符串长度 用 块 大于 0：
        响应 为 响应 加 块。
        NNIO_接收字节数 为 NNIO_接收字节数 加 做 字符串长度 用 块。
        块 为 做 传控_接收 用 fd、NNIO_缓冲区大小。
    完。
    做 传控_关闭 用 fd。
    返还 做 HTTP_解析响应 用 响应。
完。

网络_HTTP_PUT 接受 主机、端口、路径、数据：
    返还 做 网络_HTTP_通用请求 用 「PUT」、主机、端口、路径、数据、空。
完。

网络_HTTP_DELETE 接受 主机、端口、路径：
    返还 做 网络_HTTP_通用请求 用 「DELETE」、主机、端口、路径、空、空。
完。

网络_HTTP_HEAD 接受 主机、端口、路径：
    返还 做 网络_HTTP_通用请求 用 「HEAD」、主机、端口、路径、空、空。
完。

池连接 是 一种 事物，含 fd、主机、端口、在用、创建时间、上次使用。

连接池 是 一种 事物，含 连接数组、容量、当前数、空闲超时。

连接池_创建 接受 容量、空闲超时：
    池 为 做 堆_分配 用 连接池。
    池 的 容量 为 容量。
    池 的 当前数 为 0。
    池 的 空闲超时 为 空闲超时。
    池 的 连接数组 为 做 堆_分配_数组 用 容量 乘 48。
    可变 i 为 0。
    只要 i 小于 容量：
        做 内存_写32 用 池 的 连接数组 加 i 乘 48 加 16、0。
        i 为 i 加 1。
    完。
    返还 池。
完。

连接池_获取 接受 池、主机、端口：
    若 池 等于 0：
        返还 负 1。
    完。
    可变 i 为 0。
    只要 i 小于 池 的 容量：
        偏移 为 池 的 连接数组 加 i 乘 48。
        在用 为 做 内存_读32 用 偏移 加 24。
        若 在用 等于 0：
            cfd 为 做 内存_读32 用 偏移 加 16。
            若 cfd 大于 0：
                做 内存_写32 用 偏移 加 24、1。
                返还 cfd。
            完。
        完。
        i 为 i 加 1。
    完。
    fd 为 做 NNIO_带重试连接 用 主机、端口、1。
    若 fd 大于或等于 0 并且 池 的 当前数 小于 池 的 容量：
        偏移2 为 池 的 连接数组 加 池 的 当前数 乘 48。
        做 内存_写32 用 偏移2 加 16、fd。
        做 内存_写32 用 偏移2 加 24、1。
        池 的 当前数 为 池 的 当前数 加 1。
    完。
    返还 fd。
完。

连接池_归还 接受 池、fd：
    若 池 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 池 的 容量：
        偏移 为 池 的 连接数组 加 i 乘 48。
        若 做 内存_读32 用 偏移 加 16 等于 fd：
            做 内存_写32 用 偏移 加 24、0。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

NNIO_AI初始化 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    t 为 做 Tensor_创建 用 7。
    做 Tensor_写 用 t、0、NNIO_发送字节数。
    做 Tensor_写 用 t、1、NNIO_接收字节数。
    做 Tensor_写 用 t、2、NNIO_连接成功数。
    做 Tensor_写 用 t、3、NNIO_连接失败数。
    做 Tensor_写 用 t、4、NNIO_重试次数。
    做 Tensor_写 用 t、5、NNIO_错误次数。
    做 Tensor_写 用 t、6、NNIO_AI检测次数。
    做 AI总线_推理 用 bus、「NNIO_网络注册」、t。
    做 Tensor_释放 用 t。
完。

NNIO_AI网络健康检查 接受 零参：
    NNIO_AI检测次数 为 NNIO_AI检测次数 加 1。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    t 为 做 Tensor_创建 用 4。
    总连接 为 NNIO_连接成功数 加 NNIO_连接失败数。
    成功率 为 0。
    若 总连接 大于 0：
        成功率 为 NNIO_连接成功数 乘 1000 除以 总连接。
    完。
    做 Tensor_写 用 t、0、成功率。
    做 Tensor_写 用 t、1、NNIO_重试次数。
    做 Tensor_写 用 t、2、NNIO_错误次数。
    做 Tensor_写 用 t、3、NNIO_发送字节数 加 NNIO_接收字节数。
    健康 为 做 AI总线_推理 用 bus、「NNIO_健康评估」、t。
    做 Tensor_释放 用 t。
    返还 健康。
完。

NNIO_获取统计 接受 零参：
    s 为 做 堆_分配_数组 用 56。
    做 内存_写64 用 s 加 0、NNIO_发送字节数。
    做 内存_写64 用 s 加 8、NNIO_接收字节数。
    做 内存_写64 用 s 加 16、NNIO_连接成功数。
    做 内存_写64 用 s 加 24、NNIO_连接失败数。
    做 内存_写64 用 s 加 32、NNIO_重试次数。
    做 内存_写64 用 s 加 40、NNIO_错误次数。
    做 内存_写64 用 s 加 48、NNIO_AI检测次数。
    返还 s。
完。

NNIO_诊断打印 接受 零参：
    做 内核打印 用 「[NNIO] 发送=」、NNIO_发送字节数、「 接收=」、NNIO_接收字节数。
    做 内核打印 用 「[NNIO] 连接成功=」、NNIO_连接成功数、「 失败=」、NNIO_连接失败数。
    做 内核打印 用 「[NNIO] 重试=」、NNIO_重试次数、「 错误=」、NNIO_错误次数。
    做 内核打印 用 「[NNIO] AI检测=」、NNIO_AI检测次数。
完。

DNS_端口 为 53。

DNS_最大缓存 为 256。

可变 DNS_缓存数 为 0。

DNS缓存条目 是 一种 事物，含 域名、IP、过期时间。

DNS_缓存查找 接受 域名：
    返还 0。
完。

DNS_解析 接受 域名、DNS服务器：
    缓存 为 做 DNS_缓存查找 用 域名。
    若 缓存 不等于 0：
        返还 缓存。
    完。
    fd 为 做 数报_创建。
    若 fd 小于 0：
        返还 0。
    完。
    查询 为 做 DNS_构建A记录查询 用 域名。
    做 数报_发送到 用 fd、查询、DNS服务器、DNS_端口。
    响应 为 做 数报_接收从 用 fd、512。
    做 传控_关闭 用 fd。
    若 响应 等于 空：
        返还 0。
    完。
    返还 做 DNS_解析响应报文 用 响应。
完。

DNS_构建A记录查询 接受 域名：
    若 域名 等于 0：
        返还 「」。
    完。
    返还 「A 」 加 域名。
完。

DNS_解析响应报文 接受 响应：
    若 响应 等于 0：
        返还 0。
    完。
    若 做 字符串查找 用 响应、「.」 大于或等于 0：
        返还 响应。
    完。
    返还 0。
完。

网络字节序_主机转网络16 接受 值：
    返还 (值 位与 0xFF) 左移 8 位或 值 右移 8 位与 0xFF。
完。

网络字节序_网络转主机16 接受 值：
    返还 (值 位与 0xFF) 左移 8 位或 值 右移 8 位与 0xFF。
完。

网络字节序_主机转网络32 接受 值：
    b0 为 值 位与 0xFF。
    b1 为 值 右移 8 位与 0xFF。
    b2 为 值 右移 16 位与 0xFF。
    b3 为 值 右移 24 位与 0xFF。
    返还 b0 左移 24 位或 b1 左移 16 位或 b2 左移 8 位或 b3。
完。

网络字节序_网络转主机32 接受 值：
    返还 做 网络字节序_主机转网络32 用 值。
完。

NNIO_初始化 接受 零参：
    做 内核打印 用 「[NNIO] Nova原生网络IO v1.0 启动」。
    做 NNIO_AI初始化。
    做 内核打印 用 「[NNIO] AI智能网络管理已加载」。
    返还 真。
完。

可变 _谐振连接池_大小 为 16。

可变 _谐振连接池_复用次数 为 0。

可变 _谐振连接池_创建次数 为 0。

谐振连接池_获取 接受 目标地址：
    _谐振连接池_复用次数 为 _谐振连接池_复用次数 加 1。
    返还 0。
完。

谐振连接池_调整 接受 活跃数、空闲数：
    若 空闲数 大于 _谐振连接池_大小 除以 2：
        _谐振连接池_大小 为 _谐振连接池_大小 减 4。
        若 _谐振连接池_大小 小于 4：
            _谐振连接池_大小 为 4。
        完。
    完。
    做 否则如果 用 活跃数 大于 _谐振连接池_大小 乘 3 除以 4。
    块：
        _谐振连接池_大小 为 _谐振连接池_大小 加 8。
        若 _谐振连接池_大小 大于 128：
            _谐振连接池_大小 为 128。
        完。
    完。
    _谐振连接池_创建次数 为 _谐振连接池_创建次数 加 1。
完。

原生网络_完整统计 接受 零参：
    返还 一组：「谐振连接池大小」、_谐振连接池_大小、「谐振复用次数」、_谐振连接池_复用次数、「谐振调整次数」、_谐振连接池_创建次数。
完。

原生网络_统计 接受 零参：
    返还 一本字典：NNIO连接成功数 对应 NNIO_连接成功数、NNIO连接失败数 对应 NNIO_连接失败数、NNIO重试次数 对应 NNIO_重试次数、NNIO错误次数 对应 NNIO_错误次数、NNIOAI检测次数 对应 NNIO_AI检测次数、DNS缓存数 对应 DNS_缓存数、谐振连接池大小 对应 _谐振连接池_大小、谐振连接池复用次数 对应 _谐振连接池_复用次数、谐振连接池创建次数 对应 _谐振连接池_创建次数。
完。

原生网络_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 NNIO_连接失败数 大于 NNIO_连接成功数 除以 3 并且 NNIO_连接成功数 大于 50：
        做 追加 用 问题、「NNIO_连接失败数过高」。
    完。
    若 NNIO_错误次数 大于 100：
        做 追加 用 问题、「NNIO_错误次数=」 加 NNIO_错误次数。
    完。
    返还 做 计数 用 问题 等于 0。
完。

原生网络_综合报告 接受 零参：
    做 打印 用 「[原生网络] ═══ 综合报告 ═══」。
    做 打印 用 「  NNIO_连接成功数: 」 加 NNIO_连接成功数。
    做 打印 用 「  NNIO_连接失败数: 」 加 NNIO_连接失败数。
    做 打印 用 「  NNIO_重试次数: 」 加 NNIO_重试次数。
    做 打印 用 「  NNIO_错误次数: 」 加 NNIO_错误次数。
    做 打印 用 「  NNIO_AI检测次数: 」 加 NNIO_AI检测次数。
    做 打印 用 「  DNS_缓存数: 」 加 DNS_缓存数。
    做 打印 用 「  _谐振连接池_大小: 」 加 _谐振连接池_大小。
    做 打印 用 「  _谐振连接池_复用次数: 」 加 _谐振连接池_复用次数。
    做 打印 用 「  _谐振连接池_创建次数: 」 加 _谐振连接池_创建次数。
    做 打印 用 「  健康: 」 加 若 做 原生网络_健康评估 则 「正常」 否则 「异常」。
完。

原生网络_全局重置 接受 零参：
    NNIO_连接成功数 为 0。
    NNIO_连接失败数 为 0。
    NNIO_重试次数 为 0。
    NNIO_错误次数 为 0。
    NNIO_AI检测次数 为 0。
    DNS_缓存数 为 0。
    _谐振连接池_大小 为 0。
    _谐振连接池_复用次数 为 0。
    _谐振连接池_创建次数 为 0。
    做 打印 用 「[原生网络] 统计已重置」。
    返还 0。
完。

本章 收束。
