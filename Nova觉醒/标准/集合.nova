《集合》

※ NovaOS 标准库 · 集合 (Set) — 觉醒理论·不可绕过的理论架构
※ 原创理论: NovaSet — 代数集合论在定点OS中的完备实现
※ §1 集合公理:
※ 集合S = (D, n, frozen, ver) 其中 D=字典存储, n=元素计数,
※ frozen=不可变标志, ver=修改版本号(迭代安全)
※ 空集: ∅ = ({}, 0, false, 0)
※ 单例: {x} = ({hash(x)→x}, 1, false, 0)
※ §2 代数运算完备性:
※ 并集: A∪B, 交集: A∩B, 差集: A\B, 对称差: A△B
※ 满足: |A∪B| = |A| + |B| - |A∩B| (容斥原理)
※ 满足: A△B = (A∪B)\(A∩B)
※ 幂集: P(S) = {T | T⊆S}, |P(S)| = 2^|S|
※ 笛卡尔积: A×B = {(a,b) | a∈A, b∈B}, |A×B| = |A|·|B|
※ §3 相似度度量:
※ Jaccard: J(A,B) = |A∩B|/|A∪B| ∈ [0,1000] (×1000定点)
※ Dice: D(A,B) = 2|A∩B|/(|A|+|B|) ∈ [0,1000]
※ 重叠系数: O(A,B) = |A∩B|/min(|A|,|B|) ∈ [0,1000]
※ §4 高阶操作:
※ forAll(P): ∀x∈S, P(x) — 全称量词
※ exists(P): ∃x∈S, P(x) — 存在量词
※ partition(P): (S₁,S₂) 其中 S₁={x|P(x)}, S₂={x|¬P(x)
※ flatMap(f): ∪{f(x) | x∈S} — 单子展开
※ §5 多集(Bag)扩展:
※ 多集M = (D, n) 其中 D: key→count, 允许重复计数
※ 支持: 添加/删除/频率查询/最高频元素
※ 四阶段AI演进: NovaSet在阶段2提供高效集合推理基础设施

引用《..·中枢·类型定义》。
引用《..·中枢·自动扩容》。
引用《..·中枢·字符串替换》。
引用《..·中枢·重定位》。

集合_最大容量 为 1048576。
集合_幂集上限 为 20。
集合_笛卡尔上限 为 1000。
集合_错误_已冻结 为 -8001。
集合_错误_超容量 为 -8002。
集合_错误_幂集过大 为 -8003。

可变 集合_创建计数 为 0。
可变 集合_操作计数 为 0。
可变 集合_最大观测大小 为 0。

集合_创建 接受 )：
    集合_创建计数 为 集合_创建计数 加 1。
    返还 [做 字典, 0, 假, 0]。
完。

集合_创建_从列表 接受 列表)：
    集合 为 做 集合_创建。
    若 列表 等于 0：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 集合_添加 用 集合、列表[i]。
        i 为 i 加 1。
    完。
    返还 集合。
完。

集合_创建_从范围 接受 起始、结束、步长)：
    集合 为 做 集合_创建。
    若 步长 等于 0：
    可变 v 为 起始。
    若 步长 大于 0：
        只要 v 小于 结束：
            做 集合_添加 用 集合、v。
            v 为 v 加 步长。
        完。
    否则：
        只要 v 大于 结束：
            做 集合_添加 用 集合、v。
            v 为 v 加 步长。
        完。
    完。
    返还 集合。
完。

集合_添加 接受 集合、值)：
    若 集合[2]：
    若 集合[1] 不小于 集合_最大容量：
    键 为 做 转字符串 用 值。
    若 做 字典取 用 集合[0]、键 等于 空：
    做 字典设 用 集合[0]、键、值。
    集合[3] 为 集合[3] 加 1。
    集合_操作计数 为 集合_操作计数 加 1。
    若 集合[1] 大于 集合_最大观测大小：
    返还 0。
完。

集合_批量添加 接受 集合、值列表)：
    若 集合[2] 或者 值列表 等于 0：
    可变 已添加 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 值列表：
        键 为 做 转字符串 用 值列表[i]。
        若 做 字典取 用 集合[0]、键 等于 空：
            做 字典设 用 集合[0]、键、值列表[i]。
            集合[1] 为 集合[1] 加 1。
            已添加 为 已添加 加 1。
        完。
        i 为 i 加 1。
    完。
    集合[3] 为 集合[3] 加 1。
    集合_操作计数 为 集合_操作计数 加 1。
    若 集合[1] 大于 集合_最大观测大小：
    返还 已添加。
完。

集合_包含 接受 集合、值) ：返还 做 字典取 用 集合[0]、做 转字符串 用 值 !：

集合_删除 接受 集合、值)：
    若 集合[2]：
    键 为 做 转字符串 用 值。
    若 做 字典取 用 集合[0]、键 不等于 空：
        做 字典删除 用 集合[0]、键。
        集合[1] 为 集合[1] 减 1。
        集合[3] 为 集合[3] 加 1。
        返还 0。
    完。
    返还 -1。
完。

集合_批量删除 接受 集合、值列表)：
    若 集合[2] 或者 值列表 等于 0：
    可变 已删除 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 值列表：
        键 为 做 转字符串 用 值列表[i]。
        若 做 字典取 用 集合[0]、键 不等于 空：
            做 字典删除 用 集合[0]、键。
            集合[1] 为 集合[1] 减 1。
            已删除 为 已删除 加 1。
        完。
        i 为 i 加 1。
    完。
    若 已删除 大于 0：
    返还 已删除。
完。

集合_弹出 接受 集合)：
    若 集合[2] 或者 集合[1] 等于 0：
    键 为 做 字典_键列表 用 集合[0]。
    若 做 计数 用 键 等于 0：
    值 为 做 字典取 用 集合[0]、键[0]。
    做 字典删除 用 集合[0]、键[0]。
    集合[1] 为 集合[1] 减 1。
    集合[3] 为 集合[3] 加 1。
    返还 值。
完。

集合_清空 接受 集合)：
    若 集合[2]：
    集合[0] 为 做 字典。
    集合[1] 为 0。
    集合[3] 为 集合[3] 加 1。
    返还 0。
完。

集合_大小 接受 集合) ：返还 集合[1] }：
集合_为空 接受 集合) ：返还 集合[1]：
集合_版本 接受 集合) ：返还 集合[3] }：

集合_并集 接受 a、b)：
    结果 为 做 集合_创建。
    a键 为 做 字典_键列表 用 a[0]。
    b键 为 做 字典_键列表 用 b[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a键：
        做 字典设 用 结果[0]、a键[i]、做 字典取 用 a[0]、a键[i]。
        i 为 i 加 1。
    完。
    结果[1] 为 做 计数 用 a键。
    i 为 0。
    只要 i 小于 做 计数 用 b键：
        若 做 字典取 用 结果[0]、b键[i] 等于 空：
            做 字典设 用 结果[0]、b键[i]、做 字典取 用 b[0]、b键[i]。
            结果[1] 为 结果[1] 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_交集 接受 a、b)：
    结果 为 做 集合_创建。
    较小 为 a。
    较大 为 b。
    若 做 集合_大小 用 a 大于 做 集合_大小 用 b：
    键 为 做 字典_键列表 用 较小[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        若 做 字典取 用 较大[0]、键[i] 不等于 空：
            做 字典设 用 结果[0]、键[i]、做 字典取 用 较小[0]、键[i]。
            结果[1] 为 结果[1] 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_差集 接受 a、b)：
    结果 为 做 集合_创建。
    a键 为 做 字典_键列表 用 a[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a键：
        若 做 字典取 用 b[0]、a键[i] 等于 空：
            做 字典设 用 结果[0]、a键[i]、做 字典取 用 a[0]、a键[i]。
            结果[1] 为 结果[1] 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_对称差集 接受 a、b)：
    结果 为 做 集合_创建。
    a键 为 做 字典_键列表 用 a[0]。
    b键 为 做 字典_键列表 用 b[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a键：
        若 做 字典取 用 b[0]、a键[i] 等于 空：
            做 字典设 用 结果[0]、a键[i]、做 字典取 用 a[0]、a键[i]。
            结果[1] 为 结果[1] 加 1。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 b键：
        若 做 字典取 用 a[0]、b键[i] 等于 空：
            做 字典设 用 结果[0]、b键[i]、做 字典取 用 b[0]、b键[i]。
            结果[1] 为 结果[1] 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

※ 多集合并集 (合并N个集合)
集合_多并集 接受 集合列表)：
    若 集合列表 等于 0 或者 做 计数 用 集合列表 等于 0：
    可变 结果 为 做 集合_复制 用 集合列表[0]。
    可变 i 为 1。
    只要 i 小于 做 计数 用 集合列表：
        键 为 做 字典_键列表 用 集合列表[i][0]。
        可变 j 为 0。
        只要 j 小于 做 计数 用 键：
            若 做 字典取 用 结果[0]、键[j] 等于 空：
                做 字典设 用 结果[0]、键[j]、做 字典取 用 集合列表[i][0]、键[j]。
                结果[1] 为 结果[1] 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

※ 多集合交集 (N个集合的公共元素)
集合_多交集 接受 集合列表)：
    若 集合列表 等于 0 或者 做 计数 用 集合列表 等于 0：
    可变 最小idx 为 0。
    可变 最小大小 为 做 集合_大小 用 集合列表[0]。
    可变 i 为 1。
    只要 i 小于 做 计数 用 集合列表：
        若 做 集合_大小 用 集合列表[i] 小于 最小大小：
            最小大小 为 做 集合_大小 用 集合列表[i]。
            最小idx 为 i。
        完。
        i 为 i 加 1。
    完。
    结果 为 做 集合_创建。
    键 为 做 字典_键列表 用 集合列表[最小idx][0]。
    可变 k 为 0。
    只要 k 小于 做 计数 用 键：
        可变 全部包含 为 真。
        i 为 0。
        只要 i 小于 做 计数 用 集合列表：
            若 i 不等于 最小idx 并且 做 字典取 用 集合列表[i][0]、键[k] 等于 空：
                全部包含 为 假。
                i 为 做 计数 用 集合列表。
            完。
            i 为 i 加 1。
        完。
        若 全部包含：
            做 字典设 用 结果[0]、键[k]、做 字典取 用 集合列表[最小idx][0]、键[k]。
            结果[1] 为 结果[1] 加 1。
        完。
        k 为 k 加 1。
    完。
    返还 结果。
完。

集合_是子集 接受 a、b)：
    若 做 集合_大小 用 a 大于 做 集合_大小 用 b：
    a键 为 做 字典_键列表 用 a[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a键：
        若 做 字典取 用 b[0]、a键[i] 等于 空：
        i 为 i 加 1。
    完。
    返还 真。
完。

集合_是真子集 接受 a、b)：
    返还 做 集合_大小 用 a 小于 做 集合_大小 用 b 并且 做 集合_是子集 用 a、b。
完。

集合_是超集 接受 a、b) ：返还 做 集合_是子集 用 b、a }：

集合_相等 接受 a、b)：
    若 做 集合_大小 用 a 不等于 做 集合_大小 用 b：
    返还 做 集合_是子集 用 a、b。
完。

集合_不相交 接受 a、b)：
    较小 为 a。
    较大 为 b。
    若 做 集合_大小 用 a 大于 做 集合_大小 用 b：
    键 为 做 字典_键列表 用 较小[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        若 做 字典取 用 较大[0]、键[i] 不等于 空：
        i 为 i 加 1。
    完。
    返还 真。
完。

集合_Jaccard相似度 接受 a、b)：
    交大小 为 做 集合_大小 用 做 集合_交集 用 a、b。
    并大小 为 做 集合_大小 用 a 加 做 集合_大小 用 b 减 交大小。
    若 并大小 等于 0：
    返还 交大小 乘 1000 除以 并大小。
完。

集合_Dice相似度 接受 a、b)：
    交大小 为 做 集合_大小 用 做 集合_交集 用 a、b。
    总 为 做 集合_大小 用 a 加 做 集合_大小 用 b。
    若 总 等于 0：
    返还 2 乘 交大小 乘 1000 除以 总。
完。

集合_重叠系数 接受 a、b)：
    交大小 为 做 集合_大小 用 做 集合_交集 用 a、b。
    较小大小 为 做 集合_大小 用 a。
    若 做 集合_大小 用 b 小于 较小大小：
    若 较小大小 等于 0：
    返还 交大小 乘 1000 除以 较小大小。
完。

集合_遍历 接受 集合、回调)：
    键 为 做 字典_键列表 用 集合[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        值 为 做 字典取 用 集合[0]、键[i]。
        做 回调 用 值。
        i 为 i 加 1。
    完。
完。

集合_遍历_带索引 接受 集合、回调)：
    键 为 做 字典_键列表 用 集合[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        值 为 做 字典取 用 集合[0]、键[i]。
        做 回调 用 i、值。
        i 为 i 加 1。
    完。
完。

集合_过滤 接受 集合、条件函数)：
    结果 为 做 集合_创建。
    键 为 做 字典_键列表 用 集合[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        值 为 做 字典取 用 集合[0]、键[i]。
        若 做 条件函数 用 值：
            做 字典设 用 结果[0]、键[i]、值。
            结果[1] 为 结果[1] 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_映射 接受 集合、变换函数)：
    结果 为 做 集合_创建。
    键 为 做 字典_键列表 用 集合[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        值 为 做 字典取 用 集合[0]、键[i]。
        新值 为 做 变换函数 用 值。
        做 集合_添加 用 结果、新值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_flatMap 接受 集合、变换函数)：
    结果 为 做 集合_创建。
    键 为 做 字典_键列表 用 集合[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        值 为 做 字典取 用 集合[0]、键[i]。
        子集合 为 做 变换函数 用 值。
        若 子集合 不等于 空 并且 做 计数 用 子集合 不小于 4：
            子键 为 做 字典_键列表 用 子集合[0]。
            可变 j 为 0。
            只要 j 小于 做 计数 用 子键：
                若 做 字典取 用 结果[0]、子键[j] 等于 空：
                    做 字典设 用 结果[0]、子键[j]、做 字典取 用 子集合[0]、子键[j]。
                    结果[1] 为 结果[1] 加 1。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_归约 接受 集合、初始值、归约函数)：
    键 为 做 字典_键列表 用 集合[0]。
    可变 累积 为 初始值。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        值 为 做 字典取 用 集合[0]、键[i]。
        累积 为 做 归约函数 用 累积、值。
        i 为 i 加 1。
    完。
    返还 累积。
完。

集合_forAll 接受 集合、谓词)：
    键 为 做 字典_键列表 用 集合[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        若 !做 谓词 用 做 字典取 用 集合[0]、键[i]：
        i 为 i 加 1。
    完。
    返还 真。
完。

集合_exists 接受 集合、谓词)：
    键 为 做 字典_键列表 用 集合[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        若 做 谓词 用 做 字典取 用 集合[0]、键[i]：
        i 为 i 加 1。
    完。
    返还 假。
完。

集合_计数满足 接受 集合、谓词)：
    键 为 做 字典_键列表 用 集合[0]。
    可变 计 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        若 做 谓词 用 做 字典取 用 集合[0]、键[i]：
        i 为 i 加 1。
    完。
    返还 计。
完。

集合_partition 接受 集合、谓词)：
    满足 为 做 集合_创建。
    不满足 为 做 集合_创建。
    键 为 做 字典_键列表 用 集合[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        值 为 做 字典取 用 集合[0]、键[i]。
        若 做 谓词 用 值：
            做 字典设 用 满足[0]、键[i]、值。
            满足[1] 为 满足[1] 加 1。
        否则：
            做 字典设 用 不满足[0]、键[i]、值。
            不满足[1] 为 不满足[1] 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 [满足, 不满足]。
完。

集合_findFirst 接受 集合、谓词)：
    键 为 做 字典_键列表 用 集合[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        值 为 做 字典取 用 集合[0]、键[i]。
        若 做 谓词 用 值：
        i 为 i 加 1。
    完。
    返还 空。
完。

集合_最小 接受 集合、比较函数)：
    键 为 做 字典_键列表 用 集合[0]。
    若 做 计数 用 键 等于 0：
    可变 最小值 为 做 字典取 用 集合[0]、键[0]。
    可变 i 为 1。
    只要 i 小于 做 计数 用 键：
        v 为 做 字典取 用 集合[0]、键[i]。
        若 做 比较函数 用 v、最小值 小于 0：
        i 为 i 加 1。
    完。
    返还 最小值。
完。

集合_最大 接受 集合、比较函数)：
    键 为 做 字典_键列表 用 集合[0]。
    若 做 计数 用 键 等于 0：
    可变 最大值 为 做 字典取 用 集合[0]、键[0]。
    可变 i 为 1。
    只要 i 小于 做 计数 用 键：
        v 为 做 字典取 用 集合[0]、键[i]。
        若 做 比较函数 用 v、最大值 大于 0：
        i 为 i 加 1。
    完。
    返还 最大值。
完。

集合_幂集 接受 集合)：
    n 为 做 集合_大小 用 集合。
    若 n 大于 集合_幂集上限：
    元素 为 做 集合_到列表 用 集合。
    可变 结果 为 []。
    可变 总数 为 1。
    可变 b 为 0。
    只要 b 小于 n：
    可变 mask 为 0。
    只要 mask 小于 总数：
        子集 为 做 集合_创建。
        可变 bit 为 0。
        只要 bit 小于 n：
            可变 测试 为 1。
            可变 s 为 0。
            只要 s 小于 bit：
            若 mask 位与 测试：
                做 集合_添加 用 子集、元素[bit]。
            完。
            bit 为 bit 加 1。
        完。
        做 追加 用 结果、子集。
        mask 为 mask 加 1。
    完。
    返还 结果。
完。

集合_笛卡尔积 接受 a、b)：
    若 做 集合_大小 用 a 乘 做 集合_大小 用 b 大于 集合_笛卡尔上限：
        返还 []。
    完。
    可变 结果 为 []。
    a列 为 做 集合_到列表 用 a。
    b列 为 做 集合_到列表 用 b。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a列：
        可变 j 为 0。
        只要 j 小于 做 计数 用 b列：
            做 追加 用 结果、[a列[i]、b列[j]]。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_到列表 接受 集合)：
    键 为 做 字典_键列表 用 集合[0]。
    可变 结果 为 []。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        做 追加 用 结果、做 字典取 用 集合[0]、键[i]。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_到排序列表 接受 集合、比较函数)：
    列表 为 做 集合_到列表 用 集合。
    若 比较函数 不等于 0：
        做 列表_排序 用 列表、比较函数。
    否则：
        做 列表_排序 用 列表、默认比较。
    完。
    返还 列表。
完。

集合_冻结 接受 集合)：
    集合[2] 为 真。
    返还 集合。
完。

集合_是冻结 接受 集合) ：返还 集合[2] }：

集合_复制 接受 集合)：
    结果 为 做 集合_创建。
    键 为 做 字典_键列表 用 集合[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        做 字典设 用 结果[0]、键[i]、做 字典取 用 集合[0]、键[i]。
        i 为 i 加 1。
    完。
    结果[1] 为 集合[1]。
    返还 结果。
完。

集合_序列化 接受 集合)：
    列表 为 做 集合_到列表 用 集合。
    可变 结果 为 「{」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 i 大于 0：
        做 追加 用 部件、做 转字符串 用 列表[i]。
        i 为 i 加 1。
    完。
    做 追加 用 部件、「}」; 返还 做 字符串_连接列表 用 部件、「」。
完。

多集_创建 接受 ) ：返还 [做 字典、0、0] }：

多集_添加 接受 bag、值、次数)：
    若 次数 不大于 0：
    键 为 做 转字符串 用 值。
    现有 为 做 字典取 用 bag[0]、键。
    若 现有 等于 空：
        做 字典设 用 bag[0]、键、次数。
        bag[2] 为 bag[2] 加 1。
    否则：
        做 字典设 用 bag[0]、键、现有 加 次数。
    完。
    bag[1] 为 bag[1] 加 次数。
    返还 0。
完。

多集_删除 接受 bag、值、次数)：
    若 次数 不大于 0：
    键 为 做 转字符串 用 值。
    现有 为 做 字典取 用 bag[0]、键。
    若 现有 等于 空：
    若 现有 不大于 次数：
        做 字典删除 用 bag[0]、键。
        bag[1] 为 bag[1] 减 现有。
        bag[2] 为 bag[2] 减 1。
        返还 现有。
    完。
    做 字典设 用 bag[0]、键、现有 减 次数。
    bag[1] 为 bag[1] 减 次数。
    返还 次数。
完。

多集_频率 接受 bag、值)：
    r 为 做 字典取 用 bag[0]、做 转字符串 用 值。
    若 r 等于 空：
    返还 r。
完。

多集_总数 接受 bag) ：返还 bag[1] }：
多集_种类数 接受 bag) ：返还 bag[2] }：

多集_最高频 接受 bag)：
    键 为 做 字典_键列表 用 bag[0]。
    若 做 计数 用 键 等于 0：
    可变 最高键 为 键[0]。
    可变 最高频 为 做 字典取 用 bag[0]、键[0]。
    可变 i 为 1。
    只要 i 小于 做 计数 用 键：
        f 为 做 字典取 用 bag[0]、键[i]。
        若 f 大于 最高频：
        i 为 i 加 1。
    完。
    返还 [最高键, 最高频]。
完。

多集_TopN 接受 bag、n)：
    键 为 做 字典_键列表 用 bag[0]。
    可变 列表 为 []。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        做 追加 用 列表、[键[i]、做 字典取 用 bag[0]、键[i]]。
        i 为 i 加 1。
    完。
    可变 sorted 为 0。
    只要 sorted 小于 n 并且 sorted 小于 做 计数 用 列表：
        可变 最大idx 为 sorted。
        i 为 sorted 加 1。
        只要 i 小于 做 计数 用 列表：
            若 列表[i][1] 大于 列表[最大idx][1]：
            i 为 i 加 1。
        完。
        若 最大idx 不等于 sorted：
            临时 为 列表[sorted]。
            列表[sorted] 为 列表[最大idx]。
            列表[最大idx] 为 临时。
        完。
        sorted 为 sorted 加 1。
    完。
    可变 结果 为 []。
    i 为 0。
    只要 i 小于 n 并且 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表[i]。
        i 为 i 加 1。
    完。
    返还 结果。
完。

多集_转集合 接受 bag)：
    结果 为 做 集合_创建。
    键 为 做 字典_键列表 用 bag[0]。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        做 字典设 用 结果[0]、键[i]、键[i]。
        结果[1] 为 结果[1] 加 1。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_诊断 接受 )：
    返还 ：
        创建总数。
        操作总数。
        最大观测大小。
    完。
完。

集合_验证容斥 接受 a、b)：
    并大小 为 做 集合_大小 用 做 集合_并集 用 a、b。
    交大小 为 做 集合_大小 用 做 集合_交集 用 a、b。
    期望 为 做 集合_大小 用 a 加 做 集合_大小 用 b 减 交大小。
    返还 并大小 等于 期望。
完。

默认比较 接受 a、b)：
    sa 为 做 转字符串 用 a。
    sb 为 做 转字符串 用 b。
    返还 做 字符串_比较 用 sa、sb。
完。

可变 AI_集合_威胁计数 为 0。

AI_集合_风险评分 接受 操作类型、权限级别、来源可信度)：
    可变 风险 为 0。
    若 权限级别 大于 500：
    若 来源可信度 小于 200：
    若 风险 大于 500：
    返还 风险。
完。

AI_集合_异常行为检测 接受 操作频率、基线频率)：
    若 基线频率 大于 0 并且 操作频率 大于 基线频率 乘 5：
    返还 假。
完。

本章 收束。
