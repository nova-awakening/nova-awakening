《原生字符串》

引用《..·中枢·类型定义》。

引用《..·中枢·硬链接》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·张量数学引擎》。

字符串构建器_创建 接受 零参：
    返还 一组：一组：（空）、0。
完。

字符串构建器_追加 接受 构建器、文本：
    做 追加 用 构建器 的 第 0 项、文本。
    构建器 的 第 1 项 为 构建器 的 第 1 项 加 做 字符串长度 用 文本。
完。

字符串构建器_追加字符 接受 构建器、字符码：
    做 追加 用 构建器 的 第 0 项、做 字符 用 字符码。
    构建器 的 第 1 项 为 构建器 的 第 1 项 加 1。
完。

字符串构建器_追加行 接受 构建器、文本：
    做 追加 用 构建器 的 第 0 项、文本。
    做 追加 用 构建器 的 第 0 项、「\n」。
    构建器 的 第 1 项 为 构建器 的 第 1 项 加 做 字符串长度 用 文本 加 1。
完。

字符串构建器_长度 接受 构建器：
    返还 构建器 的 第 1 项。
完。

字符串构建器_清空 接受 构建器：
    构建器 的 第 0 项 为 一组：（空）。
    构建器 的 第 1 项 为 0。
完。

字符串构建器_构建 接受 构建器：
    部分 为 构建器 的 第 0 项。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 部分：
        结果 为 做 字符串拼接 用 结果、部分 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串分割 接受 文本、分隔符：
    结果 为 一组：（空）。
    长度 为 做 字符串长度 用 文本。
    分隔长度 为 做 字符串长度 用 分隔符。
    若 分隔长度 等于 0：
        可变 k 为 0。
        只要 k 小于 长度：
            做 追加 用 结果、做 字符串子串 用 文本、k、k 加 1。
            k 为 k 加 1。
        完。
        返还 结果。
    完。
    可变 开始 为 0。
    可变 i 为 0。
    只要 i 小于或等于 长度 减 分隔长度：
        若 做 字符串子串 用 文本、i、i 加 分隔长度 等于 分隔符：
            做 追加 用 结果、做 字符串子串 用 文本、开始、i。
            开始 为 i 加 分隔长度。
            i 为 开始。
        否则：
            i 为 i 加 1。
        完。
    完。
    做 追加 用 结果、做 字符串子串 用 文本、开始、长度。
    返还 结果。
完。

字符串分割N 接受 文本、分隔符、最大份数：
    结果 为 一组：（空）。
    长度 为 做 字符串长度 用 文本。
    分隔长度 为 做 字符串长度 用 分隔符。
    可变 开始 为 0。
    可变 i 为 0。
    可变 份 为 1。
    只要 i 小于或等于 长度 减 分隔长度 并且 份 小于 最大份数：
        若 做 字符串子串 用 文本、i、i 加 分隔长度 等于 分隔符：
            做 追加 用 结果、做 字符串子串 用 文本、开始、i。
            开始 为 i 加 分隔长度。
            i 为 开始。
            份 为 份 加 1。
        否则：
            i 为 i 加 1。
        完。
    完。
    做 追加 用 结果、做 字符串子串 用 文本、开始、长度。
    返还 结果。
完。

字符串连接 接受 列表、分隔符：
    若 做 计数 用 列表 等于 0：
        返还 「」。
    完。
    可变 结果 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        结果 为 做 字符串拼接 用 结果、做 字符串拼接 用 分隔符、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串去首尾空白 接受 文本：
    长度 为 做 字符串长度 用 文本。
    可变 开始 为 0。
    可变 结束 为 长度。
    只要 开始 小于 长度 并且 做 字符_是空白 用 做 字符串取字符码 用 文本、开始：
        开始 为 开始 加 1。
    完。
    只要 结束 大于 开始 并且 做 字符_是空白 用 做 字符串取字符码 用 文本、结束 减 1：
        结束 为 结束 减 1。
    完。
    返还 做 字符串子串 用 文本、开始、结束。
完。

字符串去首空白 接受 文本：
    长度 为 做 字符串长度 用 文本。
    可变 开始 为 0。
    只要 开始 小于 长度 并且 做 字符_是空白 用 做 字符串取字符码 用 文本、开始：
        开始 为 开始 加 1。
    完。
    返还 做 字符串子串 用 文本、开始、长度。
完。

字符串去尾空白 接受 文本：
    可变 结束 为 做 字符串长度 用 文本。
    只要 结束 大于 0 并且 做 字符_是空白 用 做 字符串取字符码 用 文本、结束 减 1：
        结束 为 结束 减 1。
    完。
    返还 做 字符串子串 用 文本、0、结束。
完。

字符串包含 接受 文本、子串：
    返还 做 字符串查找 用 文本、子串 大于或等于 0。
完。

字符串开头是 接受 文本、前缀：
    前缀长 为 做 字符串长度 用 前缀。
    若 做 字符串长度 用 文本 小于 前缀长：
        返还 假。
    完。
    返还 做 字符串子串 用 文本、0、前缀长 等于 前缀。
完。

字符串结尾是 接受 文本、后缀：
    文本长 为 做 字符串长度 用 文本。
    后缀长 为 做 字符串长度 用 后缀。
    若 文本长 小于 后缀长：
        返还 假。
    完。
    返还 做 字符串子串 用 文本、文本长 减 后缀长、文本长 等于 后缀。
完。

字符串替换 接受 文本、旧串、新串：
    旧长 为 做 字符串长度 用 旧串。
    若 旧长 等于 0：
        返还 文本。
    完。
    部分 为 做 字符串分割 用 文本、旧串。
    返还 做 字符串连接 用 部分、新串。
完。

字符串替换首个 接受 文本、旧串、新串：
    位置 为 做 字符串查找 用 文本、旧串。
    若 位置 小于 0：
        返还 文本。
    完。
    前 为 做 字符串子串 用 文本、0、位置。
    后 为 做 字符串子串 用 文本、位置 加 做 字符串长度 用 旧串、做 字符串长度 用 文本。
    返还 做 字符串拼接 用 前、做 字符串拼接 用 新串、后。
完。

字符串重复 接受 文本、次数：
    若 次数 小于或等于 0：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 次数：
        结果 为 做 字符串拼接 用 结果、文本。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串转大写 接受 文本：
    长度 为 做 字符串长度 用 文本。
    构建器 为 做 字符串构建器_创建。
    可变 i 为 0。
    只要 i 小于 长度：
        c 为 做 字符串取字符码 用 文本、i。
        若 c 大于或等于 0x61 并且 c 小于或等于 0x7A：
            c 为 c 减 32。
        完。
        做 字符串构建器_追加字符 用 构建器、c。
        i 为 i 加 1。
    完。
    返还 做 字符串构建器_构建 用 构建器。
完。

字符串转小写 接受 文本：
    长度 为 做 字符串长度 用 文本。
    构建器 为 做 字符串构建器_创建。
    可变 i 为 0。
    只要 i 小于 长度：
        c 为 做 字符串取字符码 用 文本、i。
        若 c 大于或等于 0x41 并且 c 小于或等于 0x5A：
            c 为 c 加 32。
        完。
        做 字符串构建器_追加字符 用 构建器、c。
        i 为 i 加 1。
    完。
    返还 做 字符串构建器_构建 用 构建器。
完。

字符串首字母大写 接受 文本：
    若 做 字符串长度 用 文本 等于 0：
        返还 「」。
    完。
    首字符 为 做 字符串转大写 用 做 字符串子串 用 文本、0、1。
    返还 做 字符串拼接 用 首字符、做 字符串子串 用 文本、1、做 字符串长度 用 文本。
完。

字符串左填充 接受 文本、目标长度、填充字符：
    当前长度 为 做 字符串长度 用 文本。
    若 当前长度 大于或等于 目标长度：
        返还 文本。
    完。
    返还 做 字符串拼接 用 做 字符串重复 用 填充字符、目标长度 减 当前长度、文本。
完。

字符串右填充 接受 文本、目标长度、填充字符：
    当前长度 为 做 字符串长度 用 文本。
    若 当前长度 大于或等于 目标长度：
        返还 文本。
    完。
    返还 做 字符串拼接 用 文本、做 字符串重复 用 填充字符、目标长度 减 当前长度。
完。

字符串居中 接受 文本、目标长度、填充字符：
    当前长度 为 做 字符串长度 用 文本。
    若 当前长度 大于或等于 目标长度：
        返还 文本。
    完。
    总填充 为 目标长度 减 当前长度。
    左 为 总填充 除以 2。
    右 为 总填充 减 左。
    返还 做 字符串拼接 用 做 字符串重复 用 填充字符、左、做 字符串拼接 用 文本、做 字符串重复 用 填充字符、右。
完。

字符串反转 接受 文本：
    长度 为 做 字符串长度 用 文本。
    构建器 为 做 字符串构建器_创建。
    可变 i 为 长度 减 1。
    只要 i 大于或等于 0：
        做 字符串构建器_追加字符 用 构建器、做 字符串取字符码 用 文本、i。
        i 为 i 减 1。
    完。
    返还 做 字符串构建器_构建 用 构建器。
完。

字符串计数 接受 文本、子串：
    子长 为 做 字符串长度 用 子串。
    若 子长 等于 0：
        返还 0。
    完。
    可变 次数 为 0。
    可变 位置 为 0。
    只要 位置 小于或等于 做 字符串长度 用 文本 减 子长：
        找到 为 做 字符串查找从 用 文本、子串、位置。
        若 找到 小于 0：
            跳出。
        完。
        次数 为 次数 加 1。
        位置 为 找到 加 子长。
    完。
    返还 次数。
完。

字符串查找从 接受 文本、子串、起始：
    文本长 为 做 字符串长度 用 文本。
    子长 为 做 字符串长度 用 子串。
    若 子长 等于 0：
        返还 起始。
    完。
    可变 i 为 起始。
    只要 i 小于或等于 文本长 减 子长：
        若 做 字符串子串 用 文本、i、i 加 子长 等于 子串：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

字符串最后查找 接受 文本、子串：
    文本长 为 做 字符串长度 用 文本。
    子长 为 做 字符串长度 用 子串。
    若 子长 等于 0：
        返还 文本长。
    完。
    可变 i 为 文本长 减 子长。
    只要 i 大于或等于 0：
        若 做 字符串子串 用 文本、i、i 加 子长 等于 子串：
            返还 i。
        完。
        i 为 i 减 1。
    完。
    返还 负 1。
完。

字符_是空白 接受 c：
    返还 c 等于 0x20 或者 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0D 或者 c 等于 0x0C。
完。

字符_是数字 接受 c：
    返还 c 大于或等于 0x30 并且 c 小于或等于 0x39。
完。

字符_是字母 接受 c：
    返还 c 大于或等于 0x41 并且 c 小于或等于 0x5A 或者 c 大于或等于 0x61 并且 c 小于或等于 0x7A。
完。

字符_是大写 接受 c：
    返还 c 大于或等于 0x41 并且 c 小于或等于 0x5A。
完。

字符_是小写 接受 c：
    返还 c 大于或等于 0x61 并且 c 小于或等于 0x7A。
完。

字符_是十六进制 接受 c：
    返还 c 大于或等于 0x30 并且 c 小于或等于 0x39 或者 c 大于或等于 0x41 并且 c 小于或等于 0x46 或者 c 大于或等于 0x61 并且 c 小于或等于 0x66。
完。

字符串转整数 接受 文本：
    长度 为 做 字符串长度 用 文本。
    若 长度 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    可变 符号 为 1。
    只要 i 小于 长度 并且 做 字符_是空白 用 做 字符串取字符码 用 文本、i：
        i 为 i 加 1。
    完。
    若 i 小于 长度 并且 做 字符串取字符码 用 文本、i 等于 0x2D：
        符号 为 负 1。
        i 为 i 加 1。
    否则若 i 小于 长度 并且 做 字符串取字符码 用 文本、i 等于 0x2B：
        i 为 i 加 1。
    完。
    可变 结果 为 0。
    只要 i 小于 长度 并且 做 字符_是数字 用 做 字符串取字符码 用 文本、i：
        结果 为 结果 乘 10 加 (做 字符串取字符码 用 文本、i 减 0x30)。
        i 为 i 加 1。
    完。
    返还 结果 乘 符号。
完。

字符串转16进制整数 接受 文本：
    长度 为 做 字符串长度 用 文本。
    可变 i 为 0。
    只要 i 小于 长度 并且 做 字符_是空白 用 做 字符串取字符码 用 文本、i：
        i 为 i 加 1。
    完。
    若 i 加 1 小于 长度 并且 做 字符串取字符码 用 文本、i 等于 0x30：
        下一个 为 做 字符串取字符码 用 文本、i 加 1。
        若 下一个 等于 0x78 或者 下一个 等于 0x58：
            i 为 i 加 2。
        完。
    完。
    可变 结果 为 0。
    只要 i 小于 长度：
        c 为 做 字符串取字符码 用 文本、i。
        若 c 大于或等于 0x30 并且 c 小于或等于 0x39：
            结果 为 结果 乘 16 加 (c 减 0x30)。
        否则若 c 大于或等于 0x41 并且 c 小于或等于 0x46：
            结果 为 结果 乘 16 加 (c 减 0x41 加 10)。
        否则若 c 大于或等于 0x61 并且 c 小于或等于 0x66：
            结果 为 结果 乘 16 加 (c 减 0x61 加 10)。
        否则：
            跳出。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

整数转字符串 接受 数值：
    若 数值 等于 0：
        返还 「0」。
    完。
    可变 负 为 假。
    若 数值 小于 0：
        负 为 真。
        数值 为 0 减 数值。
    完。
    可变 buf 为 「」。
    只要 数值 大于 0：
        buf 为 做 字符串拼接 用 做 字符 用 0x30 加 数值 取余于 10、buf。
        数值 为 数值 除以 10。
    完。
    若 负：
        buf 为 做 字符串拼接 用 「-」、buf。
    完。
    返还 buf。
完。

整数转16进制 接受 数值：
    若 数值 等于 0：
        返还 「0x0」。
    完。
    十六进制字符 为 「0123456789abcdef」。
    可变 buf 为 「」。
    可变 v 为 数值。
    只要 v 大于 0：
        buf 为 做 字符串拼接 用 做 字符串子串 用 十六进制字符、v 位与 0xF、(v 位与 0xF) 加 1、buf。
        v 为 v 右移 4。
    完。
    返还 做 字符串拼接 用 「0x」、buf。
完。

字符串_追加 接受 原始、追加内容：
    返还 做 字符串拼接 用 原始、追加内容。
完。

字符串_是空 接受 文本：
    返还 文本 等于 空 或者 做 字符串长度 用 文本 等于 0。
完。

字符串_是空白 接受 文本：
    若 做 字符串_是空 用 文本：
        返还 真。
    完。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        若 非 做 字符_是空白 用 做 字符串取字符码 用 文本、i：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

_KMP_构建失败表 接受 模式：
    模式长 为 做 字符串长度 用 模式。
    失败表 为 一组：0。
    可变 前缀长 为 0。
    可变 i 为 1。
    只要 i 小于 模式长：
        若 做 字符串取字符码 用 模式、i 等于 做 字符串取字符码 用 模式、前缀长：
            前缀长 为 前缀长 加 1。
            做 追加 用 失败表、前缀长。
            i 为 i 加 1。
        否则：
            若 前缀长 不等于 0：
                前缀长 为 失败表 的 第 前缀长 减 1 项。
            否则：
                做 追加 用 失败表、0。
                i 为 i 加 1。
            完。
        完。
    完。
    返还 失败表。
完。

字符串_KMP查找 接受 文本、模式：
    文本长 为 做 字符串长度 用 文本。
    模式长 为 做 字符串长度 用 模式。
    若 模式长 等于 0：
        返还 0。
    完。
    若 文本长 小于 模式长：
        返还 负 1。
    完。
    失败表 为 做 _KMP_构建失败表 用 模式。
    可变 ti 为 0。
    可变 pi 为 0。
    只要 ti 小于 文本长：
        若 做 字符串取字符码 用 文本、ti 等于 做 字符串取字符码 用 模式、pi：
            ti 为 ti 加 1。
            pi 为 pi 加 1。
        完。
        若 pi 等于 模式长：
            返还 ti 减 pi。
        否则若 ti 小于 文本长 并且 做 字符串取字符码 用 文本、ti 不等于 做 字符串取字符码 用 模式、pi：
            若 pi 不等于 0：
                pi 为 失败表 的 第 pi 减 1 项。
            否则：
                ti 为 ti 加 1。
            完。
        完。
    完。
    返还 负 1。
完。

字符串_KMP查找全部 接受 文本、模式：
    结果 为 一组：（空）。
    文本长 为 做 字符串长度 用 文本。
    模式长 为 做 字符串长度 用 模式。
    若 模式长 等于 0 或者 文本长 小于 模式长：
        返还 结果。
    完。
    失败表 为 做 _KMP_构建失败表 用 模式。
    可变 ti 为 0。
    可变 pi 为 0。
    只要 ti 小于 文本长：
        若 做 字符串取字符码 用 文本、ti 等于 做 字符串取字符码 用 模式、pi：
            ti 为 ti 加 1。
            pi 为 pi 加 1。
        完。
        若 pi 等于 模式长：
            做 追加 用 结果、ti 减 pi。
            pi 为 失败表 的 第 pi 减 1 项。
        否则若 ti 小于 文本长 并且 做 字符串取字符码 用 文本、ti 不等于 做 字符串取字符码 用 模式、pi：
            若 pi 不等于 0：
                pi 为 失败表 的 第 pi 减 1 项。
            否则：
                ti 为 ti 加 1。
            完。
        完。
    完。
    返还 结果。
完。

字符串_模板替换 接受 模板、键列表、值列表：
    可变 结果 为 模板。
    数量 为 做 计数 用 键列表。
    可变 i 为 0。
    只要 i 小于 数量：
        占位符 为 做 字符串拼接 用 「${」、做 字符串拼接 用 键列表 的 第 i 项、「}」。
        结果 为 做 字符串替换 用 结果、占位符、值列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串_格式化 接受 模板、参数列表：
    构建器 为 做 字符串构建器_创建。
    长度 为 做 字符串长度 用 模板。
    可变 i 为 0。
    可变 参数索引 为 0。
    只要 i 小于 长度：
        若 i 加 1 小于 长度 并且 做 字符串取字符码 用 模板、i 等于 0x7B 并且 做 字符串取字符码 用 模板、i 加 1 等于 0x7D：
            若 参数索引 小于 做 计数 用 参数列表：
                做 字符串构建器_追加 用 构建器、参数列表 的 第 参数索引 项。
                参数索引 为 参数索引 加 1。
            完。
            i 为 i 加 2。
        否则：
            做 字符串构建器_追加字符 用 构建器、做 字符串取字符码 用 模板、i。
            i 为 i 加 1。
        完。
    完。
    返还 做 字符串构建器_构建 用 构建器。
完。

字符串_编辑距离 接受 甲、乙：
    甲长 为 做 字符串长度 用 甲。
    乙长 为 做 字符串长度 用 乙。
    若 甲长 等于 0：
        返还 乙长。
    完。
    若 乙长 等于 0：
        返还 甲长。
    完。
    前行 为 一组：（空）。
    当前行 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于或等于 乙长：
        做 追加 用 前行、j。
        做 追加 用 当前行、0。
        j 为 j 加 1。
    完。
    可变 i 为 1。
    只要 i 小于或等于 甲长：
        当前行 的 第 0 项 为 i。
        j 为 1。
        只要 j 小于或等于 乙长：
            可变 代价 为 0。
            若 做 字符串取字符码 用 甲、i 减 1 不等于 做 字符串取字符码 用 乙、j 减 1：
                代价 为 1。
            完。
            删除 为 前行 的 第 j 项 加 1。
            插入 为 当前行 的 第 j 减 1 项 加 1。
            替换 为 前行 的 第 j 减 1 项 加 代价。
            可变 最小 为 删除。
            若 插入 小于 最小：
                最小 为 插入。
            完。
            若 替换 小于 最小：
                最小 为 替换。
            完。
            当前行 的 第 j 项 为 最小。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于或等于 乙长：
            前行 的 第 j 项 为 当前行 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 前行 的 第 乙长 项。
完。

字符串_相似度 接受 甲、乙：
    甲长 为 做 字符串长度 用 甲。
    乙长 为 做 字符串长度 用 乙。
    若 甲长 等于 0 并且 乙长 等于 0：
        返还 1000。
    完。
    可变 最大长 为 甲长。
    若 乙长 大于 最大长：
        最大长 为 乙长。
    完。
    距离 为 做 字符串_编辑距离 用 甲、乙。
    返还 (最大长 减 距离) 乘 1000 除以 最大长。
完。

_BASE64_编码表 为 「ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/」。

_Base64_字符索引 接受 c：
    若 c 大于或等于 0x41 并且 c 小于或等于 0x5A：
        返还 c 减 0x41。
    完。
    若 c 大于或等于 0x61 并且 c 小于或等于 0x7A：
        返还 c 减 0x61 加 26。
    完。
    若 c 大于或等于 0x30 并且 c 小于或等于 0x39：
        返还 c 减 0x30 加 52。
    完。
    若 c 等于 0x2B：
        返还 62。
    完。
    若 c 等于 0x2F：
        返还 63。
    完。
    返还 负 1。
完。

字符串_Base64编码 接受 数据：
    长度 为 做 字符串长度 用 数据。
    构建器 为 做 字符串构建器_创建。
    可变 i 为 0。
    只要 i 小于 长度：
        b0 为 做 字符串取字符码 用 数据、i。
        可变 b1 为 0。
        可变 b2 为 0。
        可变 填充 为 0。
        若 i 加 1 小于 长度：
            b1 为 做 字符串取字符码 用 数据、i 加 1。
        否则：
            填充 为 填充 加 1。
        完。
        若 i 加 2 小于 长度：
            b2 为 做 字符串取字符码 用 数据、i 加 2。
        否则：
            填充 为 填充 加 1。
        完。
        索引0 为 b0 右移 2 位与 0x3F。
        索引1 为 (b0 位与 0x03) 左移 4 位或 b1 右移 4 位与 0x0F。
        索引2 为 (b1 位与 0x0F) 左移 2 位或 b2 右移 6 位与 0x03。
        索引3 为 b2 位与 0x3F。
        做 字符串构建器_追加 用 构建器、做 字符串子串 用 _BASE64_编码表、索引0、索引0 加 1。
        做 字符串构建器_追加 用 构建器、做 字符串子串 用 _BASE64_编码表、索引1、索引1 加 1。
        若 填充 小于 2：
            做 字符串构建器_追加 用 构建器、做 字符串子串 用 _BASE64_编码表、索引2、索引2 加 1。
        否则：
            做 字符串构建器_追加 用 构建器、「=」。
        完。
        若 填充 小于 1：
            做 字符串构建器_追加 用 构建器、做 字符串子串 用 _BASE64_编码表、索引3、索引3 加 1。
        否则：
            做 字符串构建器_追加 用 构建器、「=」。
        完。
        i 为 i 加 3。
    完。
    返还 做 字符串构建器_构建 用 构建器。
完。

字符串_Base64解码 接受 编码文本：
    长度 为 做 字符串长度 用 编码文本。
    构建器 为 做 字符串构建器_创建。
    可变 i 为 0。
    只要 i 加 3 小于 长度：
        c0 为 做 _Base64_字符索引 用 做 字符串取字符码 用 编码文本、i。
        c1 为 做 _Base64_字符索引 用 做 字符串取字符码 用 编码文本、i 加 1。
        c2 为 做 _Base64_字符索引 用 做 字符串取字符码 用 编码文本、i 加 2。
        c3 为 做 _Base64_字符索引 用 做 字符串取字符码 用 编码文本、i 加 3。
        若 c0 小于 0 或者 c1 小于 0：
            跳出。
        完。
        做 字符串构建器_追加字符 用 构建器、c0 左移 2 位或 c1 右移 4 位与 0x03。
        若 c2 大于或等于 0：
            做 字符串构建器_追加字符 用 构建器、(c1 位与 0x0F) 左移 4 位或 c2 右移 2 位与 0x0F。
        完。
        若 c3 大于或等于 0：
            做 字符串构建器_追加字符 用 构建器、(c2 位与 0x03) 左移 6 位或 c3。
        完。
        i 为 i 加 4。
    完。
    返还 做 字符串构建器_构建 用 构建器。
完。

字符串池_创建 接受 容量：
    池 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 池、空。
        i 为 i 加 1。
    完。
    返还 一组：池、容量、0、0。
完。

_字符串池_哈希 接受 文本、容量：
    长度 为 做 字符串长度 用 文本。
    可变 h 为 5381。
    可变 i 为 0。
    只要 i 小于 长度：
        h 为 (h 左移 5) 加 h 加 做 字符串取字符码 用 文本、i。
        i 为 i 加 1。
    完。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    返还 h 取余于 容量。
完。

字符串池_驻留 接受 池、文本：
    索引 为 做 _字符串池_哈希 用 文本、池 的 第 1 项。
    可变 探测 为 索引。
    只要 池 的 第 0 项 的 第 探测 项 不等于 空：
        若 池 的 第 0 项 的 第 探测 项 等于 文本：
            池 的 第 3 项 为 池 的 第 3 项 加 1。
            返还 池 的 第 0 项 的 第 探测 项。
        完。
        探测 为 (探测 加 1) 取余于 池 的 第 1 项。
        若 探测 等于 索引：
            返还 文本。
        完。
    完。
    池 的 第 0 项 的 第 探测 项 为 文本。
    池 的 第 2 项 为 池 的 第 2 项 加 1。
    返还 文本。
完。

字符串池_查找 接受 池、文本：
    索引 为 做 _字符串池_哈希 用 文本、池 的 第 1 项。
    可变 探测 为 索引。
    只要 池 的 第 0 项 的 第 探测 项 不等于 空：
        若 池 的 第 0 项 的 第 探测 项 等于 文本：
            返还 真。
        完。
        探测 为 (探测 加 1) 取余于 池 的 第 1 项。
        若 探测 等于 索引：
            返还 假。
        完。
    完。
    返还 假。
完。

字符串池_统计 接受 池：
    返还 一组：池 的 第 2 项、池 的 第 1 项、池 的 第 3 项。
完。

_字符串_频谱向量 接受 文本：
    向量 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 128：
        做 追加 用 向量、0。
        k 为 k 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        c 为 做 字符串取字符码 用 文本、i。
        若 c 小于 128：
            向量 的 第 c 项 为 向量 的 第 c 项 加 1000。
        完。
        i 为 i 加 1。
    完。
    返还 向量。
完。

_定点平方根 接受 值：
    若 值 小于或等于 0：
        返还 0。
    完。
    可变 根 为 值 除以 2。
    可变 迭代 为 0。
    只要 迭代 小于 12 并且 根 大于 0：
        根 为 (根 加 值 乘 1000 除以 根) 除以 2。
        迭代 为 迭代 加 1。
    完。
    返还 根。
完。

字符串_谐振匹配度 接受 文本、模式：
    甲 为 做 _字符串_频谱向量 用 文本。
    乙 为 做 _字符串_频谱向量 用 模式。
    可变 点积 为 0。
    可变 模A 为 0。
    可变 模B 为 0。
    可变 i 为 0。
    只要 i 小于 128：
        点积 为 点积 加 甲 的 第 i 项 乘 乙 的 第 i 项 除以 1000。
        模A 为 模A 加 甲 的 第 i 项 乘 甲 的 第 i 项 除以 1000。
        模B 为 模B 加 乙 的 第 i 项 乘 乙 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    根A 为 做 _定点平方根 用 模A。
    根B 为 做 _定点平方根 用 模B。
    若 根A 等于 0 或者 根B 等于 0：
        返还 0。
    完。
    返还 点积 乘 1000 除以 (根A 乘 根B 除以 1000)。
完。

字符串_谐振匹配 接受 文本、模式、阈值：
    返还 做 字符串_谐振匹配度 用 文本、模式 大于或等于 阈值。
完。

字符串_是全数字 接受 文本：
    若 做 字符串长度 用 文本 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        若 非 做 字符_是数字 用 做 字符串取字符码 用 文本、i：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

字符串_是全字母 接受 文本：
    若 做 字符串长度 用 文本 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        若 非 做 字符_是字母 用 做 字符串取字符码 用 文本、i：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

字符串_是字母数字 接受 文本：
    若 做 字符串长度 用 文本 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        c 为 做 字符串取字符码 用 文本、i。
        若 非 做 字符_是字母 用 c 并且 非 做 字符_是数字 用 c：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

字符串_截断 接受 文本、最大长度、省略号：
    若 做 字符串长度 用 文本 小于或等于 最大长度：
        返还 文本。
    完。
    省略长 为 做 字符串长度 用 省略号。
    若 最大长度 小于或等于 省略长：
        返还 做 字符串子串 用 文本、0、最大长度。
    完。
    返还 做 字符串拼接 用 做 字符串子串 用 文本、0、最大长度 减 省略长、省略号。
完。

字符串_单词计数 接受 文本：
    长度 为 做 字符串长度 用 文本。
    若 长度 等于 0：
        返还 0。
    完。
    可变 词数 为 0。
    可变 在词中 为 假。
    可变 i 为 0。
    只要 i 小于 长度：
        若 做 字符_是空白 用 做 字符串取字符码 用 文本、i：
            若 在词中：
                词数 为 词数 加 1。
                在词中 为 假。
            完。
        否则：
            在词中 为 真。
        完。
        i 为 i 加 1。
    完。
    若 在词中：
        词数 为 词数 加 1。
    完。
    返还 词数。
完。

字符串_行分割 接受 文本：
    返还 做 字符串分割 用 文本、「\n」。
完。

字符串_蛇形转驼峰 接受 文本：
    部分 为 做 字符串分割 用 文本、「_」。
    若 做 计数 用 部分 等于 0：
        返还 文本。
    完。
    构建器 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 构建器、部分 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 部分：
        做 字符串构建器_追加 用 构建器、做 字符串首字母大写 用 部分 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 字符串构建器_构建 用 构建器。
完。

字符串_驼峰转蛇形 接受 文本：
    长度 为 做 字符串长度 用 文本。
    构建器 为 做 字符串构建器_创建。
    可变 i 为 0。
    只要 i 小于 长度：
        c 为 做 字符串取字符码 用 文本、i。
        若 做 字符_是大写 用 c 并且 i 大于 0：
            做 字符串构建器_追加 用 构建器、「_」。
            做 字符串构建器_追加字符 用 构建器、c 加 32。
        否则：
            做 字符串构建器_追加字符 用 构建器、c。
        完。
        i 为 i 加 1。
    完。
    返还 做 字符串构建器_构建 用 构建器。
完。

字符串_完整统计 接受 零参：
    返还 一组：「模块」、「原生字符串」、「构建器函数」、7、「分割连接函数」、3、「空白处理函数」、4、「查找匹配函数」、6、「替换格式函数」、4、「大小写函数」、4、「填充对齐函数」、3、「字符判断函数」、6、「转换函数」、5、「KMP搜索函数」、3、「模板引擎函数」、2、「编辑距离函数」、2、「Base64函数」、3、「字符串池函数」、4、「谐振搜索函数」、3、「文本工具函数」、7、「总函数数」、66。
完。

可变 AI字串_编码检测数 为 0。

AI字串_最优编码 接受 文本：
    AI字串_编码检测数 为 AI字串_编码检测数 加 1。
    长度 为 做 字符串长度 用 文本。
    若 长度 等于 0：
        返还 「空」。
    完。
    可变 i 为 0。
    只要 i 小于 长度 并且 i 小于 100：
        若 做 字符串字符 用 文本、i 大于 127：
            返还 「UTF-8」。
        完。
        i 为 i 加 1。
    完。
    返还 「ASCII」。
完。

AI字串_拼接策略 接受 段数、总长度估计：
    若 段数 大于 10：
        返还 「StringBuilder」。
    完。
    返还 「直接拼接」。
完。

本章 收束。
