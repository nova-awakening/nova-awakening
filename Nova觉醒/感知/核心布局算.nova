《核心布局算》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·创建销毁》。

引用《.·内联布局文》。

引用《.·弹性布局深度》。

引用《.·绝对位置计》。

布局Object_布局 接受 lo：
    若 非 (lo 的 标志 位与 布局标志_需要布局) 并且 非 (lo 的 标志 位与 布局标志_子树需要布局)：
        返还。
    完。
    若 lo 的 类型 等于 布局类型_块 或者 lo 的 类型 等于 布局类型_视图：
        做 布局块级_布局 用 lo。
    否则若 lo 的 类型 等于 布局类型_弹性：
        做 布局弹性_布局 用 lo。
    否则若 lo 的 类型 等于 布局类型_内联：
        做 布局Inline_布局 用 lo。
    否则若 lo 的 类型 等于 布局类型_文本：
        做 布局Text_布局 用 lo。
    否则若 lo 的 类型 等于 布局类型_表格：
        做 布局Table_布局 用 lo。
    完。
    lo 的 标志 为 lo 的 标志 位与 按位取反 布局标志_需要布局。
    lo 的 标志 为 lo 的 标志 位与 按位取反 布局标志_子树需要布局。
完。

布局块级_布局 接受 lo：
    s 为 lo 的 样式。
    若 s 等于 0：
        返还。
    完。
    可变 可用宽 为 lo 的 内容宽。
    若 lo 的 父 不等于 0 并且 可用宽 等于 0：
        可用宽 为 lo 的 父 的 内容宽。
    完。
    若 非 s 的 width 的 是auto：
        若 s 的 width 的 单位 等于 单位_百分比 并且 lo 的 父 不等于 0：
            lo 的 内容宽 为 lo 的 父 的 内容宽 乘 s 的 width 的 值 除以 10000。
        否则：
            lo 的 内容宽 为 s 的 width 的 值。
        完。
        可用宽 为 lo 的 内容宽。
    否则：
        若 lo 的 父 不等于 0：
            lo 的 内容宽 为 lo 的 父 的 内容宽 减 s 的 margin_left 的 值 减 s 的 margin_right 的 值 减 s 的 padding_left 的 值 减 s 的 padding_right 的 值 减 s 的 border_left_width 减 s 的 border_right_width。
            可用宽 为 lo 的 内容宽。
        完。
    完。
    可变 当前Y 为 0。
    可变 子 为 lo 的 第一子。
    只要 子 不等于 0：
        若 子 的 样式 不等于 0 并且 子 的 样式 的 display 等于 Display_none：
            子 为 子 的 后兄。
            继续。
        完。
        做 布局Object_布局 用 子。
        子样式 为 子 的 样式。
        若 子样式 不等于 0：
            子 的 x 为 子样式 的 margin_left 的 值 加 s 的 padding_left 的 值 加 s 的 border_left_width。
            子 的 y 为 当前Y 加 子样式 的 margin_top 的 值。
            若 子 的 类型 等于 布局类型_块 或者 子 的 类型 等于 布局类型_弹性 或者 子 的 类型 等于 布局类型_表格：
                当前Y 为 子 的 y 加 做 布局Object_边框盒高 用 子 加 子样式 的 margin_bottom 的 值。
            否则若 子 的 类型 等于 布局类型_文本：
                当前Y 为 子 的 y 加 子 的 内容高。
            否则：
                当前Y 为 子 的 y 加 做 布局Object_边框盒高 用 子。
            完。
        否则：
            子 的 x 为 0。
            子 的 y 为 当前Y。
            当前Y 为 当前Y 加 子 的 内容高。
        完。
        子 为 子 的 后兄。
    完。
    若 非 s 的 height 的 是auto：
        若 s 的 height 的 单位 等于 单位_百分比 并且 lo 的 父 不等于 0：
            lo 的 内容高 为 lo 的 父 的 内容高 乘 s 的 height 的 值 除以 10000。
        否则：
            lo 的 内容高 为 s 的 height 的 值。
        完。
    否则：
        lo 的 内容高 为 当前Y 加 s 的 padding_top 的 值 加 s 的 padding_bottom 的 值 加 s 的 border_top_width 加 s 的 border_bottom_width。
    完。
    做 布局Object_计算绝对位置 用 lo。
完。

本章 收束。
