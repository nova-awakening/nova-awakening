《播放控制接口》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《.·播放循环》。

引用《.·解码器》。

引用《..·中枢·整数转字符串》。

引用《.·解析序列》。

媒体元素_play 接受 me：
    若 me 等于 0：
        返还。
    完。
    若 me 的 networkState 等于 NETWORK_EMPTY：
        做 媒体元素_加载资源 用 me。
    完。
    若 me 的 ended 并且 me 的 readyState 大于或等于 HAVE_CURRENT_DATA：
        me 的 currentTime 为 0。
        me 的 ended 为 假。
    完。
    me 的 paused 为 假。
    me 的 autoplaying 为 假。
    做 媒体元素_触发事件 用 me、「play」。
    若 me 的 readyState 小于 HAVE_FUTURE_DATA：
        做 媒体元素_触发事件 用 me、「waiting」。
    否则：
        做 媒体元素_触发事件 用 me、「playing」。
        做 媒体元素_开始播放循环 用 me。
    完。
完。

媒体元素_pause 接受 me：
    若 me 等于 0 或者 me 的 paused：
        返还。
    完。
    me 的 paused 为 真。
    做 媒体元素_触发事件 用 me、「pause」。
完。

媒体元素_load 接受 me：
    若 me 等于 0：
        返还。
    完。
    做 媒体元素_停止播放 用 me。
    me 的 networkState 为 NETWORK_EMPTY。
    me 的 readyState 为 HAVE_NOTHING。
    me 的 currentTime 为 0。
    me 的 duration 为 负 1。
    me 的 error 为 0。
    me 的 paused 为 真。
    me 的 ended 为 假。
    做 媒体元素_加载资源 用 me。
完。

媒体元素_seek 接受 me、目标时间：
    若 me 等于 0 或者 me 的 readyState 等于 HAVE_NOTHING：
        返还。
    完。
    目标 为 目标时间。
    若 目标 小于 0：
        目标 为 0。
    完。
    若 me 的 duration 大于 0 并且 目标 大于 me 的 duration：
        目标 为 me 的 duration。
    完。
    me 的 seeking 为 真。
    做 媒体元素_触发事件 用 me、「seeking」。
    me 的 currentTime 为 目标。
    若 me 的 demuxer 不等于 0：
        做 Demuxer_seek 用 me 的 demuxer、目标。
    完。
    若 me 的 audioDecoder 不等于 0：
        做 音频Decoder_刷新 用 me 的 audioDecoder。
    完。
    若 me 的 videoDecoder 不等于 0：
        做 视频Decoder_刷新 用 me 的 videoDecoder。
    完。
    me 的 seeking 为 假。
    做 媒体元素_触发事件 用 me、「seeked」。
    若 me 的 readyState 大于或等于 HAVE_CURRENT_DATA：
        做 媒体元素_触发事件 用 me、「timeupdate」。
    完。
完。

媒体元素_setVolume 接受 me、volume：
    若 me 等于 0：
        返还。
    完。
    v 为 volume。
    若 v 小于 0：
        v 为 0。
    完。
    若 v 大于 1024：
        v 为 1024。
    完。
    若 me 的 volume 不等于 v：
        me 的 volume 为 v。
        做 媒体元素_触发事件 用 me、「volumechange」。
    完。
完。

媒体元素_setMuted 接受 me、muted：
    若 me 等于 0：
        返还。
    完。
    若 me 的 muted 不等于 muted：
        me 的 muted 为 muted。
        做 媒体元素_触发事件 用 me、「volumechange」。
    完。
完。

媒体元素_setPlaybackRate 接受 me、rate：
    若 me 等于 0：
        返还。
    完。
    若 rate 小于 256：
        rate 为 256。
    完。
    若 rate 大于 16384：
        rate 为 16384。
    完。
    若 me 的 playbackRate 不等于 rate：
        me 的 playbackRate 为 rate。
        做 媒体元素_触发事件 用 me、「ratechange」。
    完。
完。

媒体元素_加载资源 接受 me：
    若 me 等于 0 或者 me 的 src 等于 0：
        me 的 networkState 为 NETWORK_NO_SOURCE。
        返还。
    完。
    me 的 networkState 为 NETWORK_LOADING。
    me 的 currentSrc 为 me 的 src。
    me 的 demuxer 为 做 Demuxer_创建 用 me 的 currentSrc。
    若 me 的 demuxer 等于 0：
        me 的 error 为 MEDIA_ERR_SRC_NOT_SUPPORTED。
        做 媒体元素_触发事件 用 me、「error」。
        返还。
    完。
    格式 为 做 Demuxer_探测格式 用 me 的 demuxer。
    若 格式 等于 0：
        me 的 error 为 MEDIA_ERR_SRC_NOT_SUPPORTED。
        做 媒体元素_触发事件 用 me、「error」。
        返还。
    完。
    做 Demuxer_解析轨道 用 me 的 demuxer、me。
    若 me 的 音频轨道数 大于 0：
        me 的 audioDecoder 为 做 音频Decoder_创建 用 me 的 音频轨道 的 第 0 项 的 编解码器。
        me 的 audio渲染er 为 做 音频渲染er_创建 用 me 的 音频轨道 的 第 0 项 的 采样率、me 的 音频轨道 的 第 0 项 的 声道数。
    完。
    若 me 的 视频轨道数 大于 0：
        me 的 videoDecoder 为 做 视频Decoder_创建 用 me 的 视频轨道 的 第 0 项 的 编解码器。
        me 的 video渲染er 为 做 视频渲染er_创建 用 me 的 视频轨道 的 第 0 项 的 宽度、me 的 视频轨道 的 第 0 项 的 高度。
        me 的 videoWidth 为 me 的 视频轨道 的 第 0 项 的 宽度。
        me 的 videoHeight 为 me 的 视频轨道 的 第 0 项 的 高度。
    完。
    me 的 readyState 为 HAVE_METADATA。
    做 媒体元素_触发事件 用 me、「loadedmetadata」。
    me 的 readyState 为 HAVE_CURRENT_DATA。
    做 媒体元素_触发事件 用 me、「loadeddata」。
    me 的 networkState 为 NETWORK_IDLE。
    做 媒体元素_触发事件 用 me、「canplay」。
完。

NETWORK_EMPTY 为 0。

NETWORK_IDLE 为 1。

NETWORK_LOADING 为 2。

NETWORK_NO_SOURCE 为 3。

HAVE_NOTHING 为 0。

HAVE_METADATA 为 1。

HAVE_CURRENT_DATA 为 2。

HAVE_FUTURE_DATA 为 3。

HAVE_ENOUGH_DATA 为 4。

MEDIA_ERR_ABORTED 为 1。

MEDIA_ERR_NETWORK 为 2。

MEDIA_ERR_DECODE 为 3。

MEDIA_ERR_SRC_NOT_SUPPORTED 为 4。

媒体_网络状态名 接受 状态：
    若 状态 等于 NETWORK_EMPTY：
        返还 「EMPTY」。
    完。
    若 状态 等于 NETWORK_IDLE：
        返还 「IDLE」。
    完。
    若 状态 等于 NETWORK_LOADING：
        返还 「LOADING」。
    完。
    若 状态 等于 NETWORK_NO_SOURCE：
        返还 「NO_SOURCE」。
    完。
    返还 「UNKNOWN」。
完。

媒体_就绪状态名 接受 状态：
    若 状态 等于 HAVE_NOTHING：
        返还 「NOTHING」。
    完。
    若 状态 等于 HAVE_METADATA：
        返还 「METADATA」。
    完。
    若 状态 等于 HAVE_CURRENT_DATA：
        返还 「CURRENT_DATA」。
    完。
    若 状态 等于 HAVE_FUTURE_DATA：
        返还 「FUTURE_DATA」。
    完。
    若 状态 等于 HAVE_ENOUGH_DATA：
        返还 「ENOUGH_DATA」。
    完。
    返还 「UNKNOWN」。
完。

媒体_错误名 接受 错误码：
    若 错误码 等于 0：
        返还 「无错误」。
    完。
    若 错误码 等于 MEDIA_ERR_ABORTED：
        返还 「ABORTED」。
    完。
    若 错误码 等于 MEDIA_ERR_NETWORK：
        返还 「NETWORK」。
    完。
    若 错误码 等于 MEDIA_ERR_DECODE：
        返还 「DECODE」。
    完。
    若 错误码 等于 MEDIA_ERR_SRC_NOT_SUPPORTED：
        返还 「SRC_NOT_SUPPORTED」。
    完。
    返还 「UNKNOWN」。
完。

媒体元素 是 一种 事物，含 src、currentSrc、networkState、readyState、error、paused、ended、seeking、autoplaying、currentTime、duration、volume、muted、playbackRate、defaultPlaybackRate、videoWidth、videoHeight、loop、autoplay、controls、crossOrigin、preload、demuxer、audioDecoder、videoDecoder、audio渲染er、video渲染er、音频轨道、音频轨道数、视频轨道、视频轨道数、缓冲范围、事件处理器。

媒体元素_创建 接受 零参：
    me 为 新建 媒体元素。
    me 的 src 为 0。
    me 的 currentSrc 为 0。
    me 的 networkState 为 NETWORK_EMPTY。
    me 的 readyState 为 HAVE_NOTHING。
    me 的 error 为 0。
    me 的 paused 为 真。
    me 的 ended 为 假。
    me 的 seeking 为 假。
    me 的 autoplaying 为 假。
    me 的 currentTime 为 0。
    me 的 duration 为 负 1。
    me 的 volume 为 1024。
    me 的 muted 为 假。
    me 的 playbackRate 为 1024。
    me 的 defaultPlaybackRate 为 1024。
    me 的 videoWidth 为 0。
    me 的 videoHeight 为 0。
    me 的 loop 为 假。
    me 的 autoplay 为 假。
    me 的 controls 为 假。
    me 的 crossOrigin 为 0。
    me 的 preload 为 0。
    me 的 demuxer 为 0。
    me 的 audioDecoder 为 0。
    me 的 videoDecoder 为 0。
    me 的 audio渲染er 为 0。
    me 的 video渲染er 为 0。
    me 的 音频轨道 为 一组：（空）。
    me 的 音频轨道数 为 0。
    me 的 视频轨道 为 一组：（空）。
    me 的 视频轨道数 为 0。
    me 的 缓冲范围 为 一组：（空）。
    me 的 事件处理器 为 一组：（空）。
    返还 me。
完。

可变 播放_活跃元素数 为 0。

可变 播放_总帧数 为 0。

可变 播放_丢帧数 为 0。

媒体元素_开始播放循环 接受 me：
    若 me 等于 0 或者 me 的 paused：
        返还。
    完。
    播放_活跃元素数 为 播放_活跃元素数 加 1。
完。

媒体元素_停止播放 接受 me：
    若 me 等于 0：
        返还。
    完。
    me 的 paused 为 真。
    若 播放_活跃元素数 大于 0：
        播放_活跃元素数 为 播放_活跃元素数 减 1。
    完。
完。

媒体元素_播放帧 接受 me、帧时间us：
    若 me 等于 0 或者 me 的 paused：
        返还。
    完。
    增量 为 帧时间us 乘 me 的 playbackRate 除以 1024 除以 1000。
    me 的 currentTime 为 me 的 currentTime 加 增量。
    播放_总帧数 为 播放_总帧数 加 1。
    若 me 的 duration 大于 0 并且 me 的 currentTime 大于或等于 me 的 duration：
        若 me 的 loop：
            me 的 currentTime 为 0。
            做 媒体元素_触发事件 用 me、「seeked」。
        否则：
            me 的 currentTime 为 me 的 duration。
            me 的 ended 为 真。
            me 的 paused 为 真。
            做 媒体元素_触发事件 用 me、「ended」。
        完。
    完。
    做 媒体元素_触发事件 用 me、「timeupdate」。
完。

媒体元素_触发事件 接受 me、事件名：
    若 me 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 me 的 事件处理器：
        若 me 的 事件处理器 的 第 i 项 的 名称 等于 事件名：
            做 me 的 事件处理器 的 第 i 项 的 处理函数 用 me、事件名。
        完。
        i 为 i 加 1。
    完。
完。

媒体元素_注册事件 接受 me、事件名、处理函数：
    若 me 等于 0：
        返还。
    完。
    做 追加 用 me 的 事件处理器、一本字典：名称 对应 事件名、处理函数 对应 处理函数。
完。

媒体元素_移除事件 接受 me、事件名：
    若 me 等于 0：
        返还。
    完。
    可变 新列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 me 的 事件处理器：
        若 me 的 事件处理器 的 第 i 项 的 名称 不等于 事件名：
            做 追加 用 新列表、me 的 事件处理器 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    me 的 事件处理器 为 新列表。
完。

媒体元素_缓冲长度 接受 me：
    若 me 等于 0：
        返还 0。
    完。
    返还 做 计数 用 me 的 缓冲范围。
完。

媒体元素_缓冲起始 接受 me、索引：
    若 me 等于 0 或者 索引 大于或等于 做 计数 用 me 的 缓冲范围：
        返还 0。
    完。
    返还 me 的 缓冲范围 的 第 索引 项 的 起始。
完。

媒体元素_缓冲结束 接受 me、索引：
    若 me 等于 0 或者 索引 大于或等于 做 计数 用 me 的 缓冲范围：
        返还 0。
    完。
    返还 me 的 缓冲范围 的 第 索引 项 的 结束。
完。

媒体元素_添加缓冲范围 接受 me、起始、结束：
    若 me 等于 0：
        返还。
    完。
    做 追加 用 me 的 缓冲范围、一本字典：起始 对应 起始、结束 对应 结束。
完。

媒体元素_总缓冲时长 接受 me：
    若 me 等于 0：
        返还 0。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 me 的 缓冲范围：
        总 为 总 加 me 的 缓冲范围 的 第 i 项 的 结束 减 me 的 缓冲范围 的 第 i 项 的 起始。
        i 为 i 加 1。
    完。
    返还 总。
完。

可变 媒体_创建总数 为 0。

可变 媒体_销毁总数 为 0。

可变 媒体_错误总数 为 0。

媒体_统计 接受 零参：
    返还 一本字典：活跃播放 对应 播放_活跃元素数、总帧数 对应 播放_总帧数、丢帧数 对应 播放_丢帧数、创建 对应 媒体_创建总数、销毁 对应 媒体_销毁总数、错误 对应 媒体_错误总数。
完。

媒体_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 播放_丢帧数 大于 播放_总帧数 除以 10 并且 播放_总帧数 大于 100：
        做 追加 用 问题、「媒体丢帧率过高: 」 加 做 整数转字符串 用 播放_丢帧数 乘 100 除以 播放_总帧数 加 「%」。
    完。
    若 媒体_错误总数 大于 10：
        做 追加 用 问题、「媒体错误过多: 」 加 做 整数转字符串 用 媒体_错误总数。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「媒体子系统健康」。
    完。
    返还 问题。
完。

媒体_重置统计 接受 零参：
    播放_活跃元素数 为 0。
    播放_总帧数 为 0。
    播放_丢帧数 为 0。
    媒体_创建总数 为 0。
    媒体_销毁总数 为 0。
    媒体_错误总数 为 0。
完。

媒体_完整统计 接受 零参：
    返还 一组：播放_活跃元素数、播放_总帧数、播放_丢帧数、媒体_创建总数、媒体_错误总数。
完。

媒体元素_诊断 接受 me：
    若 me 等于 0：
        返还 「元素为空」。
    完。
    返还 一本字典：源 对应 me 的 currentSrc、网络状态 对应 做 媒体_网络状态名 用 me 的 networkState、就绪状态 对应 做 媒体_就绪状态名 用 me 的 readyState、暂停 对应 me 的 paused、结束 对应 me 的 ended、当前时间 对应 me 的 currentTime、时长 对应 me 的 duration、音量 对应 me 的 volume、播放速率 对应 me 的 playbackRate、视频宽 对应 me 的 videoWidth、视频高 对应 me 的 videoHeight、缓冲段数 对应 做 计数 用 me 的 缓冲范围、错误 对应 做 媒体_错误名 用 me 的 error。
完。

可变 AI_播放控制接_带宽估计 为 0。

可变 AI_播放控制接_丢包窗口 为 一组：（空）。

AI_播放控制接_带宽预测 接受 发送字节、耗时毫秒：
    若 耗时毫秒 等于 0：
        返还 AI_播放控制接_带宽估计。
    完。
    当前 为 发送字节 乘 1000 除以 耗时毫秒。
    AI_播放控制接_带宽估计 为 AI_播放控制接_带宽估计 乘 700 除以 1000 加 当前 乘 300 除以 1000。
    返还 AI_播放控制接_带宽估计。
完。

AI_播放控制接_拥塞建议 接受 丢包率：
    做 追加 用 AI_播放控制接_丢包窗口、丢包率。
    若 做 计数 用 AI_播放控制接_丢包窗口 大于 10：
        做 列表_移除 用 AI_播放控制接_丢包窗口、0。
    完。
    若 丢包率 大于 50：
        返还 「减半窗口」。
    完。
    若 丢包率 小于 5：
        返还 「增大窗口」。
    完。
    返还 「保持」。
完。

本章 收束。
