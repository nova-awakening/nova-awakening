《语义检查》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·推理·定点Q算术》。

引用《..·中枢·整数转字符串》。

引用《.·文档节点》。

着色器_顶点 为 0。

着色器_片元 为 1。

着色器_几何 为 2。

着色器_计算 为 3。

着色器_细分控 为 4。

着色器_细分求 为 5。

类型_VOID 为 0。

类型_BOOL 为 1。

类型_INT 为 2。

类型_UINT 为 3。

类型_FLOAT 为 4。

类型_DOUBLE 为 5。

类型_VEC2 为 10。

类型_VEC3 为 11。

类型_VEC4 为 12。

类型_IVEC2 为 13。

类型_IVEC3 为 14。

类型_IVEC4 为 15。

类型_BVEC2 为 16。

类型_BVEC3 为 17。

类型_BVEC4 为 18。

类型_MAT2 为 20。

类型_MAT3 为 21。

类型_MAT4 为 22。

类型_MAT2X3 为 23。

类型_MAT3X4 为 24。

类型_MAT4X2 为 25。

类型_SAMPLER2D 为 30。

类型_SAMPLER3D 为 31。

类型_SAMPLERCUBE 为 32。

类型_SAMPLER2DARRAY 为 33。

类型_STRUCT 为 40。

类型_ARRAY 为 41。

AST_PROGRAM 为 0。

AST_FUNC_DEF 为 1。

AST_VAR_DECL 为 2。

AST_ASSIGN 为 3。

AST_BINARY 为 4。

AST_UNARY 为 5。

AST_CALL 为 6。

AST_IDENT 为 7。

AST_INT_LIT 为 8。

AST_FLOAT_LIT 为 9。

AST_BOOL_LIT 为 10。

AST_IF 为 11。

AST_FOR 为 12。

AST_WHILE 为 13。

AST_RETURN 为 14。

AST_MEMBER 为 15。

AST_INDEX 为 16。

AST_SWIZZLE 为 17。

AST_TERNARY 为 18。

AST_STRUCT_DEF 为 19。

AST_DISCARD 为 20。

AST_BREAK 为 21。

AST_CONTINUE 为 22。

限定_无 为 0。

限定_IN 为 1。

限定_OUT 为 2。

限定_INOUT 为 3。

限定_UNIFORM 为 4。

限定_VARYING 为 5。

限定_CONST 为 6。

限定_FLAT 为 7。

限定_SMOOTH 为 8。

TOK_PLUS 为 43。

TOK_MINUS 为 45。

TOK_STAR 为 42。

TOK_SLASH 为 47。

TOK_PERCENT 为 37。

TOK_AND 为 38。

TOK_OR 为 124。

TOK_XOR 为 94。

TOK_LSHIFT 为 200。

TOK_RSHIFT 为 201。

TOK_EQ 为 202。

TOK_NE 为 203。

TOK_LT 为 60。

TOK_GT 为 62。

TOK_LE 为 204。

TOK_GE 为 205。

TOK_AND_AND 为 206。

TOK_OR_OR 为 207。

严重度_错误 为 3。

严重度_警告 为 2。

严重度_提示 为 1。

可变 全局_类型检查总数 为 0。

可变 全局_类型错误总数 为 0。

可变 全局_作用域检查总数 为 0。

可变 全局_限定符检查总数 为 0。

可变 全局_内建函数检查总数 为 0。

可变 全局_常量折叠次数 为 0。

可变 全局_死代码消除次数 为 0。

可变 全局_编译总数 为 0。

可变 全局_编译错误总数 为 0。

可变 全局_编译警告总数 为 0。

可变 全局_AI分析总数 为 0。

可变 全局_SPIRV生成总数 为 0。

可变 全局_优化次数 为 0。

可变 全局_最大作用域深度 为 0。

作用域_创建 接受 零参：
    返还 一本字典：层级 对应 一组：（空）、当前深度 对应 0。
完。

作用域_进入 接受 作用域：
    新层 为 一本字典：符号表 对应 一组：（空）、父层 对应 空。
    若 作用域 的 当前深度 大于 0：
        新层 的 父层 为 作用域 的 层级 的 第 作用域 的 当前深度 减 1 项。
    完。
    作用域 的 层级 为 作用域 的 层级 加 一组：新层。
    作用域 的 当前深度 为 作用域 的 当前深度 加 1。
    若 作用域 的 当前深度 大于 全局_最大作用域深度：
        全局_最大作用域深度 为 作用域 的 当前深度。
    完。
完。

作用域_离开 接受 作用域：
    若 作用域 的 当前深度 大于 0：
        作用域 的 当前深度 为 作用域 的 当前深度 减 1。
    完。
完。

作用域_声明 接受 作用域、名称、类型ID、限定符：
    若 作用域 的 当前深度 等于 0：
        返还 假。
    完。
    当前层 为 作用域 的 层级 的 第 作用域 的 当前深度 减 1 项。
    可变 k 为 0。
    只要 k 小于 当前层 的 符号表 的 长度：
        若 做 字符串_相等 用 当前层 的 符号表 的 第 k 项 的 名称、名称：
            返还 假。
        完。
        k 为 k 加 1。
    完。
    当前层 的 符号表 为 当前层 的 符号表 加 一组：一本字典：名称 对应 名称、类型ID 对应 类型ID、限定符 对应 限定符、已使用 对应 假、已赋值 对应 假。
    返还 真。
完。

作用域_查找 接受 作用域、名称：
    可变 深度 为 作用域 的 当前深度 减 1。
    只要 深度 大于或等于 0：
        层 为 作用域 的 层级 的 第 深度 项。
        可变 k 为 0。
        只要 k 小于 层 的 符号表 的 长度：
            若 做 字符串_相等 用 层 的 符号表 的 第 k 项 的 名称、名称：
                层 的 符号表 的 第 k 项 的 已使用 为 真。
                返还 层 的 符号表 的 第 k 项。
            完。
            k 为 k 加 1。
        完。
        深度 为 深度 减 1。
    完。
    返还 空。
完。

类型_获取分量数 接受 类型ID：
    做 匹配 用 类型ID。
完。

类型_获取矩阵维度 接受 类型ID：
    做 匹配 用 类型ID。
完。

类型_是标量 接受 类型ID：
    返还 类型ID 大于或等于 类型_BOOL 并且 类型ID 小于或等于 类型_DOUBLE。
完。

类型_是向量 接受 类型ID：
    返还 类型ID 大于或等于 类型_VEC2 并且 类型ID 小于或等于 类型_BVEC4。
完。

类型_是矩阵 接受 类型ID：
    返还 类型ID 大于或等于 类型_MAT2 并且 类型ID 小于或等于 类型_MAT4X2。
完。

类型_是采样器 接受 类型ID：
    返还 类型ID 大于或等于 类型_SAMPLER2D 并且 类型ID 小于或等于 类型_SAMPLER2DARRAY。
完。

类型_检查二元操作 接受 左类型、右类型、操作符：
    全局_类型检查总数 为 全局_类型检查总数 加 1。
    若 做 类型_是矩阵 用 左类型 并且 做 类型_是向量 用 右类型 并且 操作符 等于 TOK_STAR：
        矩阵维 为 做 类型_获取矩阵维度 用 左类型。
        向量分量 为 做 类型_获取分量数 用 右类型。
        若 矩阵维 不等于 空 并且 矩阵维 的 列 不等于 向量分量：
            全局_类型错误总数 为 全局_类型错误总数 加 1。
            返还 一本字典：有效 对应 假、错误 对应 「矩阵列数与向量分量数不匹配」。
        完。
        返还 一本字典：有效 对应 真、结果类型 对应 类型_VEC2 加 (矩阵维 的 行 减 2)。
    完。
    若 做 类型_是向量 用 左类型 并且 做 类型_是矩阵 用 右类型 并且 操作符 等于 TOK_STAR：
        向量分量 为 做 类型_获取分量数 用 左类型。
        矩阵维 为 做 类型_获取矩阵维度 用 右类型。
        若 矩阵维 不等于 空 并且 向量分量 不等于 矩阵维 的 行：
            全局_类型错误总数 为 全局_类型错误总数 加 1。
            返还 一本字典：有效 对应 假、错误 对应 「向量分量数与矩阵行数不匹配」。
        完。
        返还 一本字典：有效 对应 真、结果类型 对应 类型_VEC2 加 (矩阵维 的 列 减 2)。
    完。
    若 做 类型_是向量 用 左类型 并且 做 类型_是向量 用 右类型：
        若 做 类型_获取分量数 用 左类型 不等于 做 类型_获取分量数 用 右类型：
            全局_类型错误总数 为 全局_类型错误总数 加 1。
            返还 一本字典：有效 对应 假、错误 对应 「向量分量数不匹配」。
        完。
        返还 一本字典：有效 对应 真、结果类型 对应 左类型。
    完。
    若 做 类型_是标量 用 左类型 并且 做 类型_是向量 用 右类型：
        返还 一本字典：有效 对应 真、结果类型 对应 右类型。
    完。
    若 做 类型_是向量 用 左类型 并且 做 类型_是标量 用 右类型：
        返还 一本字典：有效 对应 真、结果类型 对应 左类型。
    完。
    若 左类型 等于 右类型：
        返还 一本字典：有效 对应 真、结果类型 对应 左类型。
    完。
    全局_类型错误总数 为 全局_类型错误总数 加 1。
    返还 一本字典：有效 对应 假、错误 对应 「不兼容的操作数类型」。
完。

内建_验证函数调用 接受 函数名、参数类型列表：
    全局_内建函数检查总数 为 全局_内建函数检查总数 加 1。
    参数数 为 参数类型列表 的 长度。
    做 匹配 用 函数名。
完。

内建_单参数浮点检查 接受 参数类型列表、参数数：
    若 参数数 不等于 1：
        返还 一本字典：有效 对应 假、错误 对应 「该函数需要恰好1个参数」。
    完。
    返还 一本字典：有效 对应 真、结果类型 对应 参数类型列表 的 第 0 项。
完。

限定符_检查合法性 接受 限定符、类型ID、着色器类型：
    全局_限定符检查总数 为 全局_限定符检查总数 加 1。
    若 做 类型_是采样器 用 类型ID 并且 限定符 不等于 限定_UNIFORM：
        返还 一本字典：有效 对应 假、错误 对应 「采样器类型必须声明为uniform」。
    完。
    若 着色器类型 等于 着色器_顶点 并且 限定符 等于 限定_OUT 并且 类型ID 等于 类型_VOID：
        返还 一本字典：有效 对应 假、错误 对应 「顶点着色器不允许void类型out变量」。
    完。
    若 着色器类型 等于 着色器_片元 并且 限定符 等于 限定_IN 并且 做 类型_是矩阵 用 类型ID：
        返还 一本字典：有效 对应 假、错误 对应 「片元着色器in变量不能为矩阵类型」。
    完。
    若 限定符 等于 限定_CONST：
        返还 一本字典：有效 对应 真、需要初始化 对应 真。
    完。
    返还 一本字典：有效 对应 真。
完。

着色器_检查阶段约束 接受 ast、着色器类型：
    可变 错误列表 为 一组：（空）。
    若 着色器类型 等于 着色器_顶点：
        若 非 做 GLSL_查找赋值 用 ast、「gl_Position」：
            错误列表 为 错误列表 加 一组：一本字典：严重度 对应 严重度_错误、消息 对应 「顶点着色器必须写入gl_Position」。
        完。
    完。
    若 着色器类型 等于 着色器_片元：
        若 做 GLSL_查找赋值 用 ast、「gl_Position」：
            错误列表 为 错误列表 加 一组：一本字典：严重度 对应 严重度_警告、消息 对应 「片元着色器不应写入gl_Position」。
        完。
    完。
    若 着色器类型 不等于 着色器_片元：
        若 做 GLSL_查找节点类型 用 ast、AST_DISCARD：
            错误列表 为 错误列表 加 一组：一本字典：严重度 对应 严重度_错误、消息 对应 「discard只允许在片元着色器中使用」。
        完。
    完。
    返还 错误列表。
完。

GLSL_语义检查 接受 ast、着色器类型：
    若 ast 等于 0：
        返还 0。
    完。
    作用域 为 做 作用域_创建。
    做 作用域_进入 用 作用域。
    做 GLSL_注册内建变量 用 作用域、着色器类型。
    可变 错误列表 为 一组：（空）。
    做 GLSL_收集全局声明 用 ast、作用域、着色器类型、错误列表。
    做 GLSL_检查节点 用 ast、作用域、着色器类型、错误列表。
    阶段错误 为 做 着色器_检查阶段约束 用 ast、着色器类型。
    错误列表 为 错误列表 加 阶段错误。
    做 GLSL_检查未使用变量 用 作用域、错误列表。
    做 作用域_离开 用 作用域。
    可变 错误数 为 0。
    可变 警告数 为 0。
    可变 m 为 0。
    只要 m 小于 错误列表 的 长度：
        若 错误列表 的 第 m 项 的 严重度 等于 严重度_错误：
            错误数 为 错误数 加 1。
        否则：
            警告数 为 警告数 加 1。
        完。
        m 为 m 加 1。
    完。
    全局_编译错误总数 为 全局_编译错误总数 加 错误数。
    全局_编译警告总数 为 全局_编译警告总数 加 警告数。
    返还 错误数。
完。

GLSL_注册内建变量 接受 作用域、着色器类型：
    做 作用域_声明 用 作用域、「gl_Position」、类型_VEC4、限定_OUT。
    做 作用域_声明 用 作用域、「gl_PointSize」、类型_FLOAT、限定_OUT。
    做 作用域_声明 用 作用域、「gl_VertexID」、类型_INT、限定_IN。
    做 作用域_声明 用 作用域、「gl_InstanceID」、类型_INT、限定_IN。
    若 着色器类型 等于 着色器_片元：
        做 作用域_声明 用 作用域、「gl_FragCoord」、类型_VEC4、限定_IN。
        做 作用域_声明 用 作用域、「gl_FrontFacing」、类型_BOOL、限定_IN。
        做 作用域_声明 用 作用域、「gl_FragDepth」、类型_FLOAT、限定_OUT。
        做 作用域_声明 用 作用域、「gl_PointCoord」、类型_VEC2、限定_IN。
    完。
完。

GLSL_收集全局声明 接受 ast、作用域、着色器类型、错误列表：
    若 ast 等于 0：
        返还。
    完。
    若 ast 的 类型 等于 AST_VAR_DECL：
        限定检查 为 做 限定符_检查合法性 用 ast 的 限定符、ast 的 类型ID、着色器类型。
        若 非 限定检查 的 有效：
            错误列表 为 错误列表 加 一组：一本字典：严重度 对应 严重度_错误、消息 对应 限定检查 的 错误。
        完。
        若 非 做 作用域_声明 用 作用域、ast 的 名称、ast 的 类型ID、ast 的 限定符：
            错误列表 为 错误列表 加 一组：一本字典：严重度 对应 严重度_错误、消息 对应 「重复声明变量: 」 加 ast 的 名称。
        完。
    完。
    做 GLSL_收集全局声明 用 ast 的 下一个、作用域、着色器类型、错误列表。
完。

GLSL_检查节点 接受 ast、作用域、着色器类型、错误列表：
    若 ast 等于 0：
        返还。
    完。
    做 匹配 用 ast 的 类型。
    做 GLSL_检查节点 用 ast 的 下一个、作用域、着色器类型、错误列表。
完。

GLSL_检查未使用变量 接受 作用域、错误列表：
    可变 d 为 0。
    只要 d 小于 作用域 的 当前深度：
        层 为 作用域 的 层级 的 第 d 项。
        可变 k 为 0。
        只要 k 小于 层 的 符号表 的 长度：
            若 非 层 的 符号表 的 第 k 项 的 已使用 并且 层 的 符号表 的 第 k 项 的 限定符 不等于 限定_OUT：
                错误列表 为 错误列表 加 一组：一本字典：严重度 对应 严重度_警告、消息 对应 「未使用的变量: 」 加 层 的 符号表 的 第 k 项 的 名称。
            完。
            k 为 k 加 1。
        完。
        d 为 d 加 1。
    完。
完。

GLSL_查找赋值 接受 ast、名称：
    若 ast 等于 0：
        返还 假。
    完。
    若 ast 的 类型 等于 AST_ASSIGN 并且 ast 的 左 不等于 0 并且 ast 的 左 的 类型 等于 AST_IDENT：
        若 做 字符串_相等 用 ast 的 左 的 名称、名称：
            返还 真。
        完。
    完。
    若 做 GLSL_查找赋值 用 ast 的 body、名称：
        返还 真。
    完。
    若 做 GLSL_查找赋值 用 ast 的 左、名称：
        返还 真。
    完。
    若 做 GLSL_查找赋值 用 ast 的 右、名称：
        返还 真。
    完。
    若 做 GLSL_查找赋值 用 ast 的 下一个、名称：
        返还 真。
    完。
    返还 假。
完。

GLSL_查找节点类型 接受 ast、目标类型：
    若 ast 等于 0：
        返还 假。
    完。
    若 ast 的 类型 等于 目标类型：
        返还 真。
    完。
    若 做 GLSL_查找节点类型 用 ast 的 body、目标类型：
        返还 真。
    完。
    若 做 GLSL_查找节点类型 用 ast 的 左、目标类型：
        返还 真。
    完。
    若 做 GLSL_查找节点类型 用 ast 的 右、目标类型：
        返还 真。
    完。
    若 做 GLSL_查找节点类型 用 ast 的 下一个、目标类型：
        返还 真。
    完。
    返还 假。
完。

GLSL_常量折叠 接受 节点：
    若 节点 等于 0：
        返还。
    完。
    若 节点 的 类型 等于 AST_BINARY 并且 节点 的 左 不等于 0 并且 节点 的 右 不等于 0：
        做 GLSL_常量折叠 用 节点 的 左。
        做 GLSL_常量折叠 用 节点 的 右。
        若 节点 的 左 的 类型 等于 AST_INT_LIT 并且 节点 的 右 的 类型 等于 AST_INT_LIT：
            可变 结果 为 0。
            可变 可折叠 为 真。
            做 匹配 用 节点 的 操作符。
            若 可折叠：
                节点 的 类型 为 AST_INT_LIT。
                节点 的 值 为 结果。
                节点 的 左 为 0。
                节点 的 右 为 0。
                全局_常量折叠次数 为 全局_常量折叠次数 加 1。
            完。
        完。
        若 节点 的 左 的 类型 等于 AST_FLOAT_LIT 并且 节点 的 右 的 类型 等于 AST_FLOAT_LIT：
            可变 结果 为 0.0。
            可变 可折叠 为 真。
            做 匹配 用 节点 的 操作符。
            若 可折叠：
                节点 的 类型 为 AST_FLOAT_LIT。
                节点 的 值 为 结果。
                节点 的 左 为 0。
                节点 的 右 为 0。
                全局_常量折叠次数 为 全局_常量折叠次数 加 1。
            完。
        完。
    完。
    做 GLSL_常量折叠 用 节点 的 body。
    做 GLSL_常量折叠 用 节点 的 下一个。
完。

GLSL_死代码消除 接受 节点：
    若 节点 等于 0：
        返还。
    完。
    若 节点 的 类型 等于 AST_IF 并且 节点 的 条件 不等于 0：
        若 节点 的 条件 的 类型 等于 AST_INT_LIT 并且 节点 的 条件 的 值 等于 0：
            节点 的 body 为 节点 的 else_body。
            节点 的 else_body 为 0。
            全局_死代码消除次数 为 全局_死代码消除次数 加 1。
        完。
        若 节点 的 条件 的 类型 等于 AST_INT_LIT 并且 节点 的 条件 的 值 不等于 0：
            节点 的 else_body 为 0。
            全局_死代码消除次数 为 全局_死代码消除次数 加 1。
        完。
        若 节点 的 条件 的 类型 等于 AST_BOOL_LIT 并且 节点 的 条件 的 值 等于 假：
            节点 的 body 为 节点 的 else_body。
            节点 的 else_body 为 0。
            全局_死代码消除次数 为 全局_死代码消除次数 加 1。
        完。
    完。
    若 节点 的 类型 等于 AST_WHILE 并且 节点 的 条件 不等于 0：
        若 节点 的 条件 的 类型 等于 AST_INT_LIT 并且 节点 的 条件 的 值 等于 0：
            节点 的 body 为 0。
            全局_死代码消除次数 为 全局_死代码消除次数 加 1。
        完。
    完。
    做 GLSL_死代码消除 用 节点 的 body。
    做 GLSL_死代码消除 用 节点 的 下一个。
完。

SPIRV_MAGIC 为 0x07230203。

SPIRV_VERSION 为 0x00010300。

SPIRV_初始化生成器 接受 零参：
    返还 一本字典：指令流 对应 一组：（空）、下一个ID 对应 1、入口点 对应 0、全局声明 对应 一组：（空）、注解 对应 一组：（空）。
完。

SPIRV_分配ID 接受 生成器：
    id 为 生成器 的 下一个ID。
    生成器 的 下一个ID 为 生成器 的 下一个ID 加 1。
    返还 id。
完。

SPIRV_发射头部 接受 生成器：
    生成器 的 指令流 为 生成器 的 指令流 加 一组：SPIRV_MAGIC、SPIRV_VERSION、0x4E6F7661、生成器 的 下一个ID、0。
完。

SPIRV_发射能力 接受 生成器：
    生成器 的 指令流 为 生成器 的 指令流 加 一组：0x00020011、1。
完。

SPIRV_发射内存模型 接受 生成器：
    生成器 的 指令流 为 生成器 的 指令流 加 一组：0x0003000E、0、1。
完。

ShaderCompiler_生成代码_真实 接受 sc、着色器类型：
    若 sc 等于 0 或者 sc 的 ast 等于 0：
        返还。
    完。
    若 sc 的 错误数 大于 0：
        返还。
    完。
    生成器 为 做 SPIRV_初始化生成器。
    做 SPIRV_发射能力 用 生成器。
    做 SPIRV_发射内存模型 用 生成器。
    做 SPIRV_发射头部 用 生成器。
    sc 的 SPIRV_指令流 为 生成器 的 指令流。
    sc 的 SPIRV_长度 为 生成器 的 指令流 的 长度。
    全局_SPIRV生成总数 为 全局_SPIRV生成总数 加 1。
完。

ShaderCompiler_语法分析_真实 接受 sc：
    若 sc 等于 0：
        返还。
    完。
    parser 为 做 GLSLParser_创建 用 sc 的 源码、sc 的 源码长度。
    sc 的 ast 为 做 GLSL_解析 用 parser。
    sc 的 错误数 为 parser 的 错误数。
完。

ShaderCompiler_语义检查_真实 接受 sc：
    若 sc 等于 0 或者 sc 的 ast 等于 0：
        返还。
    完。
    sc 的 错误数 为 sc 的 错误数 加 做 GLSL_语义检查 用 sc 的 ast、sc 的 着色器类型。
完。

ShaderCompiler_优化_真实 接受 sc：
    若 sc 等于 0 或者 sc 的 ast 等于 0：
        返还。
    完。
    做 GLSL_常量折叠 用 sc 的 ast。
    做 GLSL_死代码消除 用 sc 的 ast。
    全局_优化次数 为 全局_优化次数 加 1。
完。

ShaderCompiler_完整编译 接受 sc：
    若 sc 等于 0：
        返还 错误_无效参数。
    完。
    开始时间 为 做 系统_读取时间戳。
    做 ShaderCompiler_语法分析_真实 用 sc。
    若 sc 的 错误数 大于 0：
        返还 sc 的 错误数。
    完。
    做 ShaderCompiler_语义检查_真实 用 sc。
    做 ShaderCompiler_优化_真实 用 sc。
    做 SCAT_AI分析着色器 用 sc。
    若 sc 的 错误数 等于 0：
        做 ShaderCompiler_生成代码_真实 用 sc、sc 的 着色器类型。
    完。
    sc 的 编译时间 为 做 系统_读取时间戳 减 开始时间。
    全局_编译总数 为 全局_编译总数 加 1。
    返还 sc 的 错误数。
完。

SCAT_AI分析着色器 接受 sc：
    张量 为 做 Tensor_创建 用 8。
    ast深度 为 做 SCAT_计算AST深度 用 sc 的 ast。
    做 Tensor_写 用 张量、0、ast深度 除以 50.0。
    总检查 为 全局_类型检查总数。
    若 总检查 大于 0：
        做 Tensor_写 用 张量、1、全局_类型错误总数 除以 总检查。
    否则：
        做 Tensor_写 用 张量、1、0.0。
    完。
    函数调用数 为 做 SCAT_统计节点类型 用 sc 的 ast、AST_CALL。
    做 Tensor_写 用 张量、2、函数调用数 除以 100.0。
    做 Tensor_写 用 张量、3、全局_最大作用域深度 除以 10.0。
    if数 为 做 SCAT_统计节点类型 用 sc 的 ast、AST_IF。
    for数 为 做 SCAT_统计节点类型 用 sc 的 ast、AST_FOR。
    while数 为 做 SCAT_统计节点类型 用 sc 的 ast、AST_WHILE。
    做 Tensor_写 用 张量、4、(if数 加 for数 加 while数) 除以 50.0。
    做 Tensor_写 用 张量、5、0.0。
    做 Tensor_写 用 张量、6、if数 除以 20.0。
    做 Tensor_写 用 张量、7、sc 的 着色器类型 除以 5.0。
    复杂度评分 为 做 AI总线_推理 用 全局AI总线、「SCAT_复杂度」、张量。
    优化建议 为 做 AI总线_推理 用 全局AI总线、「SCAT_优化建议」、张量。
    错误风险 为 做 AI总线_风险评分 用 全局AI总线、「SCAT_错误预测」、张量。
    sc 的 AI_复杂度 为 复杂度评分。
    sc 的 AI_优化建议 为 优化建议。
    sc 的 AI_错误风险 为 错误风险。
    全局_AI分析总数 为 全局_AI分析总数 加 1。
    做 Tensor_释放 用 张量。
完。

SCAT_计算AST深度 接受 ast：
    若 ast 等于 0：
        返还 0。
    完。
    body深 为 做 SCAT_计算AST深度 用 ast 的 body。
    左深 为 做 SCAT_计算AST深度 用 ast 的 左。
    右深 为 做 SCAT_计算AST深度 用 ast 的 右。
    可变 最大 为 body深。
    若 左深 大于 最大：
        最大 为 左深。
    完。
    若 右深 大于 最大：
        最大 为 右深。
    完。
    返还 最大 加 1。
完。

SCAT_统计节点类型 接受 ast、目标类型：
    若 ast 等于 0：
        返还 0。
    完。
    可变 计数 为 0。
    若 ast 的 类型 等于 目标类型：
        计数 为 1。
    完。
    计数 为 计数 加 做 SCAT_统计节点类型 用 ast 的 body、目标类型。
    计数 为 计数 加 做 SCAT_统计节点类型 用 ast 的 左、目标类型。
    计数 为 计数 加 做 SCAT_统计节点类型 用 ast 的 右、目标类型。
    计数 为 计数 加 做 SCAT_统计节点类型 用 ast 的 下一个、目标类型。
    返还 计数。
完。

SCAT_获取统计信息 接受 零参：
    返还 一本字典：类型检查总数 对应 全局_类型检查总数、类型错误总数 对应 全局_类型错误总数、作用域检查总数 对应 全局_作用域检查总数、限定符检查总数 对应 全局_限定符检查总数、内建函数检查 对应 全局_内建函数检查总数、常量折叠次数 对应 全局_常量折叠次数、死代码消除 对应 全局_死代码消除次数、编译总数 对应 全局_编译总数、编译错误总数 对应 全局_编译错误总数、编译警告总数 对应 全局_编译警告总数、AI分析总数 对应 全局_AI分析总数、SPIRV生成总数 对应 全局_SPIRV生成总数、优化次数 对应 全局_优化次数、最大作用域深度 对应 全局_最大作用域深度。
完。

本章 收束。
