《属性树》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《..·中枢·内存初始化》。

属性树节点 是 一种 事物，含 id、父id、m_a、m_b、m_c、m_d、m_tx、m_ty、clip_x、clip_y、clip_w、clip_h、opacity、filter、已计算。

属性树 是 一种 事物，含 节点列表、节点数、容量。

属性树_创建 接受 零参：
    tree 为 做 堆_分配 用 属性树。
    tree 的 容量 为 64。
    tree 的 节点列表 为 做 堆_分配 用 tree 的 容量 乘 64。
    tree 的 节点数 为 0。
    做 属性树_添加根 用 tree。
    返还 tree。
完。

属性树_添加根 接受 tree：
    节点 为 tree 的 节点列表。
    做 内存_置零 用 节点、64。
    做 内存_写32 用 节点、0。
    做 内存_写32 用 节点 加 4、0。
    做 内存_写32 用 节点 加 8、10000。
    做 内存_写32 用 节点 加 20、10000。
    tree 的 节点数 为 1。
完。

属性树_添加节点 接受 tree、父id：
    若 tree 的 节点数 大于或等于 tree 的 容量：
        新容量 为 tree 的 容量 乘 2。
        新列表 为 做 堆_分配 用 新容量 乘 64。
        做 内存_复制 用 新列表、tree 的 节点列表、tree 的 节点数 乘 64。
        做 堆_释放 用 tree 的 节点列表。
        tree 的 节点列表 为 新列表。
        tree 的 容量 为 新容量。
    完。
    id 为 tree 的 节点数。
    节点 为 tree 的 节点列表 加 id 乘 64。
    做 内存_置零 用 节点、64。
    做 内存_写32 用 节点、id。
    做 内存_写32 用 节点 加 4、父id。
    做 内存_写32 用 节点 加 8、10000。
    做 内存_写32 用 节点 加 20、10000。
    tree 的 节点数 为 tree 的 节点数 加 1。
    返还 id。
完。

TILE_SIZE 为 256。

Tile 是 一种 事物，含 列、行、纹理ID、像素缓冲、优先级、已光栅化、在视口内、脏。

图层_创建分块 接受 层：
    若 层 的 宽度 小于或等于 0 或者 层 的 高度 小于或等于 0：
        返还。
    完。
    w_px 为 层 的 宽度 除以 100。
    h_px 为 层 的 高度 除以 100。
    层 的 分块列数 为 (w_px 加 TILE_SIZE 减 1) 除以 TILE_SIZE。
    层 的 分块行数 为 (h_px 加 TILE_SIZE 减 1) 除以 TILE_SIZE。
    层 的 分块数 为 层 的 分块列数 乘 层 的 分块行数。
    层 的 分块列表 为 做 堆_分配 用 层 的 分块数 乘 40。
    可变 r 为 0。
    只要 r 小于 层 的 分块行数：
        可变 c 为 0。
        只要 c 小于 层 的 分块列数：
            idx 为 r 乘 层 的 分块列数 加 c。
            tile 为 层 的 分块列表 加 idx 乘 40。
            做 内存_写32 用 tile、c。
            做 内存_写32 用 tile 加 4、r。
            做 内存_写32 用 tile 加 8、0。
            做 内存_写64 用 tile 加 12、0。
            做 内存_写32 用 tile 加 20、3。
            做 内存_写8 用 tile 加 24、0。
            做 内存_写8 用 tile 加 25、0。
            做 内存_写8 用 tile 加 26、1。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
完。

Tile_光栅化 接受 tile、层、绘制回调：
    列 为 做 内存_读32 用 tile。
    行 为 做 内存_读32 用 tile 加 4。
    缓冲大小 为 TILE_SIZE 乘 TILE_SIZE 乘 4。
    缓冲 为 做 堆_分配 用 缓冲大小。
    做 内存_置零 用 缓冲、缓冲大小。
    若 绘制回调 不等于 0：
        clip_x 为 列 乘 TILE_SIZE。
        clip_y 为 行 乘 TILE_SIZE。
        fn 为 绘制回调。
        args 为 做 堆_分配 用 56。
        做 内存_写64 用 args、缓冲。
        做 内存_写32 用 args 加 8、TILE_SIZE。
        做 内存_写32 用 args 加 12、TILE_SIZE。
        做 内存_写32 用 args 加 16、clip_x。
        做 内存_写32 用 args 加 20、clip_y。
        做 内存_写32 用 args 加 24、TILE_SIZE。
        做 内存_写32 用 args 加 28、TILE_SIZE。
        做 回调_调用 用 fn、args。
        做 堆_释放 用 args。
    完。
    做 内存_写64 用 tile 加 12、缓冲。
    做 内存_写8 用 tile 加 24、1。
    做 内存_写8 用 tile 加 26、0。
完。

TileManager_更新优先级 接受 层、视口x、视口y、视口w、视口h：
    可变 i 为 0。
    只要 i 小于 层 的 分块数：
        tile 为 层 的 分块列表 加 i 乘 40。
        列 为 做 内存_读32 用 tile。
        行 为 做 内存_读32 用 tile 加 4。
        tx 为 列 乘 TILE_SIZE 乘 100 加 层 的 x。
        ty 为 行 乘 TILE_SIZE 乘 100 加 层 的 y。
        tw 为 TILE_SIZE 乘 100。
        th 为 TILE_SIZE 乘 100。
        在视口 为 非 (tx 加 tw 小于 视口x 或者 tx 大于 视口x 加 视口w 或者 ty 加 th 小于 视口y 或者 ty 大于 视口y 加 视口h)。
        做 内存_写8 用 tile 加 25、在视口。
        若 在视口：
            做 内存_写32 用 tile 加 20、0。
        否则：
            做 内存_写32 用 tile 加 20、2。
        完。
        i 为 i 加 1。
    完。
完。

可变 AI_属性树_热节点缓存 为 0。

AI_属性树_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_属性树_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_属性树_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

本章 收束。
