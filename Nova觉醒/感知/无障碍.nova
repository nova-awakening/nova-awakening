《无障碍》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内核打印》。

可变 无障碍_启用 为 假。

可变 无障碍_屏幕阅读 为 假。

可变 无障碍_高对比度 为 假。

可变 无障碍_大字体 为 假。

可变 无障碍_键盘导航 为 假。

可变 无障碍_字体缩放 为 100。

可变 无障碍_光标放大 为 假。

可变 无障碍_减少动画 为 假。

可变 无障碍_语速 为 100。

无障碍_初始化 接受 零参：
    无障碍_启用 为 假。
    无障碍_屏幕阅读 为 假。
    无障碍_高对比度 为 假。
    无障碍_大字体 为 假。
    无障碍_键盘导航 为 假。
    无障碍_字体缩放 为 100。
    无障碍_光标放大 为 假。
    无障碍_减少动画 为 假。
    无障碍_语速 为 100。
    _辅助树 为 一组：（空）。
    做 内核信息 用 「无障碍框架: 已初始化」。
完。

无障碍_启用全部 接受 零参：
    无障碍_启用 为 真。
    无障碍_屏幕阅读 为 真。
    无障碍_高对比度 为 真。
    无障碍_大字体 为 真。
    无障碍_键盘导航 为 真。
    无障碍_字体缩放 为 150。
    无障碍_光标放大 为 真。
    无障碍_减少动画 为 真。
完。

角色_按钮 为 1。

角色_文本 为 2。

角色_输入框 为 3。

角色_链接 为 4。

角色_图像 为 5。

角色_列表 为 6。

角色_列表项 为 7。

角色_标题 为 8。

角色_菜单 为 9。

角色_对话框 为 10。

角色_滚动条 为 11。

角色_进度条 为 12。

辅助节点 是 一种 事物，含 ID、角色、名称、描述、值、可聚焦、已聚焦、已禁用、父ID、子节点。

可变 _辅助树 为 一组：（空）。

可变 _辅助序号 为 0。

可变 _当前焦点ID 为 0。

无障碍_注册节点 接受 角色、名称、描述、父ID：
    _辅助序号 为 _辅助序号 加 1。
    节点 为 新建 辅助节点。
    节点 的 ID 为 _辅助序号。
    节点 的 角色 为 角色。
    节点 的 名称 为 名称。
    节点 的 描述 为 描述。
    节点 的 值 为 「」。
    节点 的 可聚焦 为 角色 等于 角色_按钮 或者 角色 等于 角色_输入框 或者 角色 等于 角色_链接。
    节点 的 已聚焦 为 假。
    节点 的 已禁用 为 假。
    节点 的 父ID 为 父ID。
    节点 的 子节点 为 一组：（空）。
    做 追加 用 _辅助树、节点。
    返还 节点 的 ID。
完。

无障碍_朗读 接受 节点ID：
    若 非 无障碍_屏幕阅读：
        返还 「」。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _辅助树：
        若 _辅助树 的 第 i 项 的 ID 等于 节点ID：
            角色名 为 做 无障碍_角色名 用 _辅助树 的 第 i 项 的 角色。
            文本 为 角色名 加 「: 」 加 _辅助树 的 第 i 项 的 名称。
            若 _辅助树 的 第 i 项 的 描述 不等于 「」：
                文本 为 文本 加 「, 」 加 _辅助树 的 第 i 项 的 描述。
            完。
            若 _辅助树 的 第 i 项 的 值 不等于 「」：
                文本 为 文本 加 「, 值: 」 加 _辅助树 的 第 i 项 的 值。
            完。
            若 _辅助树 的 第 i 项 的 已禁用：
                文本 为 文本 加 「 (已禁用)」。
            完。
            返还 文本。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

无障碍_调整颜色 接受 前景、背景：
    若 非 无障碍_高对比度：
        返还 一组：前景、背景。
    完。
    返还 一组：0xFFFFFF、0x000000。
完。

无障碍_调整字号 接受 原始字号：
    若 非 无障碍_大字体：
        返还 原始字号。
    完。
    返还 原始字号 乘 无障碍_字体缩放 除以 100。
完。

无障碍_下一个焦点 接受 零参：
    若 非 无障碍_键盘导航：
        返还 负 1。
    完。
    可变 找到当前 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _辅助树：
        若 找到当前 并且 _辅助树 的 第 i 项 的 可聚焦 并且 非 _辅助树 的 第 i 项 的 已禁用：
            做 _设焦点 用 i。
            返还 _辅助树 的 第 i 项 的 ID。
        完。
        若 _辅助树 的 第 i 项 的 ID 等于 _当前焦点ID：
            找到当前 为 真。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 _辅助树：
        若 _辅助树 的 第 i 项 的 可聚焦 并且 非 _辅助树 的 第 i 项 的 已禁用：
            做 _设焦点 用 i。
            返还 _辅助树 的 第 i 项 的 ID。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

无障碍_上一个焦点 接受 零参：
    若 非 无障碍_键盘导航：
        返还 负 1。
    完。
    可变 最近可聚焦 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _辅助树：
        若 _辅助树 的 第 i 项 的 ID 等于 _当前焦点ID：
            若 最近可聚焦 大于或等于 0：
                做 _设焦点 用 最近可聚焦。
                返还 _辅助树 的 第 最近可聚焦 项 的 ID。
            完。
        完。
        若 _辅助树 的 第 i 项 的 可聚焦 并且 非 _辅助树 的 第 i 项 的 已禁用：
            最近可聚焦 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最近可聚焦 大于或等于 0：
        做 _设焦点 用 最近可聚焦。
        返还 _辅助树 的 第 最近可聚焦 项 的 ID。
    完。
    返还 负 1。
完。

_设焦点 接受 索引：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _辅助树：
        _辅助树 的 第 i 项 的 已聚焦 为 假。
        i 为 i 加 1。
    完。
    _辅助树 的 第 索引 项 的 已聚焦 为 真。
    _当前焦点ID 为 _辅助树 的 第 索引 项 的 ID。
完。

无障碍_角色名 接受 角色：
    若 角色 等于 角色_按钮：
        返还 「按钮」。
    完。
    若 角色 等于 角色_文本：
        返还 「文本」。
    完。
    若 角色 等于 角色_输入框：
        返还 「输入框」。
    完。
    若 角色 等于 角色_链接：
        返还 「链接」。
    完。
    若 角色 等于 角色_图像：
        返还 「图像」。
    完。
    若 角色 等于 角色_列表：
        返还 「列表」。
    完。
    若 角色 等于 角色_列表项：
        返还 「列表项」。
    完。
    若 角色 等于 角色_标题：
        返还 「标题」。
    完。
    若 角色 等于 角色_菜单：
        返还 「菜单」。
    完。
    若 角色 等于 角色_对话框：
        返还 「对话框」。
    完。
    若 角色 等于 角色_滚动条：
        返还 「滚动条」。
    完。
    若 角色 等于 角色_进度条：
        返还 「进度条」。
    完。
    返还 「元素」。
完。

无障碍_报告 接受 零参：
    可变 r 为 「═══ 无障碍框架 ═══\n」。
    r 为 r 加 「启用: 」 加 做 整数转字符串 用 无障碍_启用 加 「\n」。
    r 为 r 加 「屏幕阅读: 」 加 做 整数转字符串 用 无障碍_屏幕阅读 加 「\n」。
    r 为 r 加 「高对比度: 」 加 做 整数转字符串 用 无障碍_高对比度 加 「\n」。
    r 为 r 加 「大字体: 」 加 做 整数转字符串 用 无障碍_大字体 加 「 (」 加 做 整数转字符串 用 无障碍_字体缩放 加 「%)\n」。
    r 为 r 加 「键盘导航: 」 加 做 整数转字符串 用 无障碍_键盘导航 加 「\n」。
    r 为 r 加 「减少动画: 」 加 做 整数转字符串 用 无障碍_减少动画 加 「\n」。
    r 为 r 加 「辅助树节点: 」 加 做 整数转字符串 用 做 计数 用 _辅助树 加 「\n」。
    r 为 r 加 「当前焦点: 」 加 做 整数转字符串 用 _当前焦点ID 加 「\n」。
    返还 r。
完。

本章 收束。
