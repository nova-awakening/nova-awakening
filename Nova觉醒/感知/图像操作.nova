《图像操作》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《.·画布双像素》。

引用《.·内核直连层》。

引用《.·文字探测字》。

引用《.·光栅化》。

画布2D_draw图像 接受 ctx、图像数据、图宽、图高、dx、dy、dw、dh：
    若 ctx 等于 0 或者 图像数据 等于 0：
        返还。
    完。
    可变 py 为 0。
    只要 py 小于 dh：
        sy 为 py 乘 图高 除以 dh。
        可变 px 为 0。
        只要 px 小于 dw：
            sx 为 px 乘 图宽 除以 dw。
            src偏移 为 (sy 乘 图宽 加 sx) 乘 4。
            R 为 做 内存_读8 用 图像数据 加 src偏移。
            G 为 做 内存_读8 用 图像数据 加 src偏移 加 1。
            B 为 做 内存_读8 用 图像数据 加 src偏移 加 2。
            A 为 做 内存_读8 用 图像数据 加 src偏移 加 3。
            做 画布2D_写像素 用 ctx、dx 加 px、dy 加 py、R、G、B、A。
            px 为 px 加 1。
        完。
        py 为 py 加 1。
    完。
完。

图像Data 是 一种 事物，含 宽、高、数据。

画布2D_get图像Data 接受 ctx、x、y、w、h：
    若 ctx 等于 0：
        返还 0。
    完。
    img 为 做 堆_分配 用 图像Data。
    img 的 宽 为 w。
    img 的 高 为 h。
    img 的 数据 为 做 堆_分配_数组 用 w 乘 h 乘 4。
    可变 py 为 0。
    只要 py 小于 h：
        sy 为 y 加 py。
        若 sy 大于或等于 0 并且 sy 小于 ctx 的 高：
            做 内存_复制 用 img 的 数据 加 py 乘 w 乘 4、ctx 的 像素缓冲 加 (sy 乘 ctx 的 宽 加 x) 乘 4、w 乘 4。
        完。
        py 为 py 加 1。
    完。
    返还 img。
完。

画布2D_put图像Data 接受 ctx、img、x、y：
    若 ctx 等于 0 或者 img 等于 0：
        返还。
    完。
    可变 py 为 0。
    只要 py 小于 img 的 高：
        dy 为 y 加 py。
        若 dy 大于或等于 0 并且 dy 小于 ctx 的 高：
            做 内存_复制 用 ctx 的 像素缓冲 加 (dy 乘 ctx 的 宽 加 x) 乘 4、img 的 数据 加 py 乘 img 的 宽 乘 4、img 的 宽 乘 4。
        完。
        py 为 py 加 1。
    完。
完。

画布2D_fillText 接受 ctx、文字、x、y：
    若 ctx 等于 0 或者 ctx 的 状态 的 字体 等于 0：
        返还。
    完。
    s 为 ctx 的 状态。
    run 为 做 字体_shape文字 用 s 的 字体、文字、做 字节长度 用 文字、s 的 字号。
    若 run 等于 0：
        返还。
    完。
    可变 偏移x 为 x。
    若 s 的 文字对齐 等于 TextAlign_Center：
        偏移x 为 x 减 run 的 总宽度 除以 2。
    否则若 s 的 文字对齐 等于 TextAlign_Right 或者 s 的 文字对齐 等于 TextAlign_End：
        偏移x 为 x 减 run 的 总宽度。
    完。
    可变 光标 为 偏移x。
    可变 i 为 0。
    只要 i 小于 run 的 字形数：
        goff 为 run 的 字形 加 i 乘 8。
        glyphID 为 做 内存_读16 用 goff。
        推进 为 做 内存_读16 用 goff 加 6。
        bmp 为 做 字体_光栅化字形 用 s 的 字体、glyphID、s 的 字号。
        若 bmp 不等于 0：
            可变 gy 为 0。
            只要 gy 小于 bmp 的 高：
                可变 gx 为 0。
                只要 gx 小于 bmp 的 宽：
                    alpha 为 做 内存_读8 用 bmp 的 像素 加 gy 乘 bmp 的 宽 加 gx。
                    若 alpha 大于 0：
                        合成alpha 为 alpha 乘 s 的 填充颜色A 除以 255。
                        做 画布2D_写像素 用 ctx、光标 加 gx 加 bmp 的 基线偏移x、y 加 gy 减 bmp 的 基线偏移y、s 的 填充颜色R、s 的 填充颜色G、s 的 填充颜色B、合成alpha。
                    完。
                    gx 为 gx 加 1。
                完。
                gy 为 gy 加 1。
            完。
        完。
        光标 为 光标 加 推进。
        i 为 i 加 1。
    完。
完。

画布2D_measureText 接受 ctx、文字：
    若 ctx 等于 0 或者 ctx 的 状态 的 字体 等于 0：
        返还 0。
    完。
    run 为 做 字体_shape文字 用 ctx 的 状态 的 字体、文字、做 字节长度 用 文字、ctx 的 状态 的 字号。
    若 run 等于 0：
        返还 0。
    完。
    返还 run 的 总宽度。
完。

Q_cos 接受 角度：
    x 为 角度。
    x2 为 x 乘 x 除以 1024。
    x4 为 x2 乘 x2 除以 1024。
    返还 1024 减 x2 除以 2 加 x4 除以 24。
完。

Q_sin 接受 角度：
    x 为 角度。
    x2 为 x 乘 x 除以 1024。
    x3 为 x2 乘 x 除以 1024。
    x5 为 x3 乘 x2 除以 1024。
    返还 x 减 x3 除以 6 加 x5 除以 120。
完。

本章 收束。
