《优化提示》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·推理·定点Q算术》。

引用《..·执行·控制台输出》。

引用《..·中枢·字符串替换》。

引用《..·中枢·辅助存根》。

引用《..·中枢·内容可见性》。

WillChange_处理 接受 元素、属性：
    若 元素 等于 0 或者 属性 等于 0：
        返还。
    完。
    若 做 字符串_包含 用 属性、「transform」 或者 做 字符串_包含 用 属性、「opacity」：
    完。
    若 做 字符串_包含 用 属性、「scroll-position」：
    完。
完。

渲染Scheduler 是 一种 事物，含 有用户交互、有动画、有滚动、目标帧率、帧预算ms、需要样式、需要布局、需要绘制、需要合成、掉帧计数、连续掉帧。

可变 全局渲染调度 为 0。

渲染Scheduler_获取 接受 零参：
    若 全局渲染调度 等于 0：
        全局渲染调度 为 做 堆_分配 用 渲染Scheduler。
        全局渲染调度 的 有用户交互 为 假。
        全局渲染调度 的 有动画 为 假。
        全局渲染调度 的 有滚动 为 假。
        全局渲染调度 的 目标帧率 为 60。
        全局渲染调度 的 帧预算ms 为 16。
        全局渲染调度 的 需要样式 为 假。
        全局渲染调度 的 需要布局 为 假。
        全局渲染调度 的 需要绘制 为 假。
        全局渲染调度 的 需要合成 为 假。
        全局渲染调度 的 掉帧计数 为 0。
        全局渲染调度 的 连续掉帧 为 0。
    完。
    返还 全局渲染调度。
完。

渲染Scheduler_标记脏 接受 类型：
    rs 为 做 渲染Scheduler_获取。
    若 类型 等于 0：
        rs 的 需要样式 为 真。
        rs 的 需要布局 为 真。
        rs 的 需要绘制 为 真。
    否则若 类型 等于 1：
        rs 的 需要布局 为 真。
        rs 的 需要绘制 为 真。
    否则若 类型 等于 2：
        rs 的 需要绘制 为 真。
    否则若 类型 等于 3：
        rs 的 需要合成 为 真。
    完。
完。

渲染Scheduler_需要渲染 接受 零参：
    rs 为 做 渲染Scheduler_获取。
    返还 rs 的 需要样式 或者 rs 的 需要布局 或者 rs 的 需要绘制 或者 rs 的 需要合成。
完。

渲染Scheduler_执行 接受 零参：
    rs 为 做 渲染Scheduler_获取。
    开始 为 做 平台_获取高精度时间。
    若 rs 的 需要样式：
        rs 的 需要样式 为 假。
    完。
    若 rs 的 需要布局：
        rs 的 需要布局 为 假。
    完。
    已用 为 做 平台_获取高精度时间 减 开始。
    若 已用 大于 rs 的 帧预算ms：
        rs 的 掉帧计数 为 rs 的 掉帧计数 加 1。
        rs 的 连续掉帧 为 rs 的 连续掉帧 加 1。
        返还。
    完。
    若 rs 的 需要绘制：
        rs 的 需要绘制 为 假。
    完。
    若 rs 的 需要合成：
        rs 的 需要合成 为 假。
    完。
    rs 的 连续掉帧 为 0。
完。

NWCO_版本 为 「3.0.0-NWCO」。

NWCO_最大合成层 为 64。

NWCO_内存预算_MB 为 128。

NWCO_AI阈值 为 0.68。

NWCO_最大追踪属性 为 16。

可变 NWCO_层计数 为 0。

可变 NWCO_层内存_KB 为 0。

可变 NWCO_提升次数 为 0。

可变 NWCO_降级次数 为 0。

可变 NWCO_AI推理次数 为 0。

可变 NWCO_帧优化次数 为 0。

NWCO_合成层 是 一种 事物，含 元素ID 为 0、宽度 为 0、高度 为 0、内存KB 为 0、是GPU加速 为 假、变换提示 为 假、透明提示 为 假、滚动提示 为 假、内容提示 为 假、创建时间 为 0、最后访问 为 0、命中次数 为 0、z序 为 0、是活跃 为 假。

NWCO_属性追踪 是 一种 事物，含 属性名 为 0、属性类型 为 0、首次声明帧 为 0、最后使用帧 为 0、命中次数 为 0、已提升 为 假。

NWCO_层管理器 是 一种 事物，含 层池 为 0、层池容量 为 0、活跃层数 为 0、追踪表 为 0、追踪数 为 0、当前帧号 为 0、总内存KB 为 0、GPU可用 为 假。

可变 全局层管理器 为 0。

NWCO_初始化 接受 零参：
    全局层管理器 为 做 堆_分配 用 NWCO_层管理器。
    全局层管理器 的 层池容量 为 NWCO_最大合成层。
    全局层管理器 的 层池 为 做 堆_分配数组 用 NWCO_合成层、NWCO_最大合成层。
    全局层管理器 的 活跃层数 为 0。
    全局层管理器 的 追踪表 为 做 堆_分配数组 用 NWCO_属性追踪、NWCO_最大追踪属性。
    全局层管理器 的 追踪数 为 0。
    全局层管理器 的 当前帧号 为 0。
    全局层管理器 的 总内存KB 为 0。
    全局层管理器 的 GPU可用 为 真。
    NWCO_层计数 为 0。
    NWCO_层内存_KB 为 0。
    NWCO_提升次数 为 0。
    NWCO_降级次数 为 0。
    NWCO_AI推理次数 为 0。
    NWCO_帧优化次数 为 0。
    做 打印 用 「[NWCO] Will-Change合成优化引擎 v」 加 NWCO_版本 加 「 已初始化」。
完。

NWCO_获取管理器 接受 零参：
    若 全局层管理器 等于 0：
        做 NWCO_初始化。
    完。
    返还 全局层管理器。
完。

NWCO_属性分类 接受 属性名：
    若 做 字符串_等于 用 属性名、「transform」：
        返还 0。
    完。
    若 做 字符串_等于 用 属性名、「opacity」：
        返还 1。
    完。
    若 做 字符串_等于 用 属性名、「scroll-position」：
        返还 2。
    完。
    若 做 字符串_等于 用 属性名、「contents」：
        返还 3。
    完。
    返还 4。
完。

NWCO_属性需要GPU 接受 类型：
    返还 类型 等于 0 或者 类型 等于 1。
完。

NWCO_属性需要缓冲 接受 类型：
    返还 类型 等于 2。
完。

NWCO_解析will_change 接受 属性字符串：
    若 属性字符串 等于 0：
        返还 0。
    完。
    若 做 字符串_等于 用 属性字符串、「auto」：
        返还 0。
    完。
    mgr 为 做 NWCO_获取管理器。
    可变 计数 为 0。
    可变 起始 为 0。
    长度 为 做 字节长度 用 属性字符串。
    可变 i 为 0。
    只要 i 小于或等于 长度：
        b 为 若 i 小于 长度 则 做 字节取 用 属性字符串、i 否则 44。
        若 b 等于 44 或者 b 等于 32：
            若 i 大于 起始：
                名 为 做 字符串_子串 用 属性字符串、起始、i 减 起始。
                名T 为 做 字符串_去空白 用 名。
                若 做 字节长度 用 名T 大于 0：
                    做 NWCO_注册追踪属性 用 名T。
                    计数 为 计数 加 1。
                完。
            完。
            起始 为 i 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计数。
完。

NWCO_注册追踪属性 接受 属性名：
    mgr 为 做 NWCO_获取管理器。
    可变 i 为 0。
    只要 i 小于 mgr 的 追踪数：
        t 为 mgr 的 追踪表 的 第 i 项。
        若 做 字符串_等于 用 t 的 属性名、属性名：
            t 的 命中次数 为 t 的 命中次数 加 1。
            t 的 最后使用帧 为 mgr 的 当前帧号。
            返还 i。
        完。
        i 为 i 加 1。
    完。
    若 mgr 的 追踪数 大于或等于 NWCO_最大追踪属性：
        返还 负 1。
    完。
    idx 为 mgr 的 追踪数。
    mgr 的 追踪数 为 mgr 的 追踪数 加 1。
    新 为 mgr 的 追踪表 的 第 idx 项。
    新 的 属性名 为 属性名。
    新 的 属性类型 为 做 NWCO_属性分类 用 属性名。
    新 的 首次声明帧 为 mgr 的 当前帧号。
    新 的 最后使用帧 为 mgr 的 当前帧号。
    新 的 命中次数 为 1。
    新 的 已提升 为 假。
    返还 idx。
完。

NWCO_清除过期追踪 接受 过期帧数：
    mgr 为 做 NWCO_获取管理器。
    阈值 为 mgr 的 当前帧号 减 过期帧数。
    可变 i 为 0。
    只要 i 小于 mgr 的 追踪数：
        t 为 mgr 的 追踪表 的 第 i 项。
        若 t 的 最后使用帧 小于 阈值：
            mgr 的 追踪数 为 mgr 的 追踪数 减 1。
            若 i 小于 mgr 的 追踪数：
                mgr 的 追踪表 的 第 i 项 为 mgr 的 追踪表 的 第 mgr 的 追踪数 项。
            完。
            继续。
        完。
        i 为 i 加 1。
    完。
完。

NWCO_计算层内存 接受 宽、高：
    返还 宽 乘 高 乘 4 除以 1024。
完。

NWCO_能否分配 接受 需要KB：
    mgr 为 做 NWCO_获取管理器。
    预算KB 为 NWCO_内存预算_MB 乘 1024。
    返还 mgr 的 总内存KB 加 需要KB 小于或等于 预算KB。
完。

NWCO_分配合成层 接受 元素ID、宽、高：
    mgr 为 做 NWCO_获取管理器。
    若 mgr 的 活跃层数 大于或等于 mgr 的 层池容量：
        返还 负 1。
    完。
    需要KB 为 做 NWCO_计算层内存 用 宽、高。
    若 非 做 NWCO_能否分配 用 需要KB：
        返还 负 1。
    完。
    idx 为 mgr 的 活跃层数。
    mgr 的 活跃层数 为 mgr 的 活跃层数 加 1。
    层 为 mgr 的 层池 的 第 idx 项。
    层 的 元素ID 为 元素ID。
    层 的 宽度 为 宽。
    层 的 高度 为 高。
    层 的 内存KB 为 需要KB。
    层 的 是GPU加速 为 假。
    层 的 变换提示 为 假。
    层 的 透明提示 为 假。
    层 的 滚动提示 为 假。
    层 的 内容提示 为 假。
    层 的 创建时间 为 做 平台_获取高精度时间。
    层 的 最后访问 为 层 的 创建时间。
    层 的 命中次数 为 0。
    层 的 z序 为 idx。
    层 的 是活跃 为 真。
    mgr 的 总内存KB 为 mgr 的 总内存KB 加 需要KB。
    NWCO_层计数 为 NWCO_层计数 加 1。
    NWCO_层内存_KB 为 mgr 的 总内存KB。
    返还 idx。
完。

NWCO_释放合成层 接受 idx：
    mgr 为 做 NWCO_获取管理器。
    若 idx 小于 0 或者 idx 大于或等于 mgr 的 活跃层数：
        返还。
    完。
    层 为 mgr 的 层池 的 第 idx 项。
    mgr 的 总内存KB 为 mgr 的 总内存KB 减 层 的 内存KB。
    层 的 是活跃 为 假。
    层 的 元素ID 为 0。
    若 idx 小于 mgr 的 活跃层数 减 1：
        mgr 的 层池 的 第 idx 项 为 mgr 的 层池 的 第 mgr 的 活跃层数 减 1 项。
    完。
    mgr 的 活跃层数 为 mgr 的 活跃层数 减 1。
    NWCO_层计数 为 mgr 的 活跃层数。
    NWCO_层内存_KB 为 mgr 的 总内存KB。
完。

NWCO_查找元素层 接受 元素ID：
    mgr 为 做 NWCO_获取管理器。
    可变 i 为 0。
    只要 i 小于 mgr 的 活跃层数：
        若 mgr 的 层池 的 第 i 项 的 元素ID 等于 元素ID：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

NWCO_提升元素 接受 元素、宽、高：
    NWCO_提升次数 为 NWCO_提升次数 加 1。
    已有 为 做 NWCO_查找元素层 用 元素 的 ID。
    若 已有 大于或等于 0：
        mgr 为 做 NWCO_获取管理器。
        mgr 的 层池 的 第 已有 项 的 最后访问 为 做 平台_获取高精度时间。
        mgr 的 层池 的 第 已有 项 的 命中次数 为 mgr 的 层池 的 第 已有 项 的 命中次数 加 1。
        返还 已有。
    完。
    返还 做 NWCO_分配合成层 用 元素 的 ID、宽、高。
完。

NWCO_降级元素 接受 元素：
    NWCO_降级次数 为 NWCO_降级次数 加 1。
    idx 为 做 NWCO_查找元素层 用 元素 的 ID。
    若 idx 大于或等于 0：
        做 NWCO_释放合成层 用 idx。
    完。
完。

NWCO_设置层GPU 接受 层idx、启用：
    mgr 为 做 NWCO_获取管理器。
    若 层idx 小于 0 或者 层idx 大于或等于 mgr 的 活跃层数：
        返还。
    完。
    mgr 的 层池 的 第 层idx 项 的 是GPU加速 为 启用。
完。

NWCO_设置层提示 接受 层idx、变换、透明、滚动、内容：
    mgr 为 做 NWCO_获取管理器。
    若 层idx 小于 0 或者 层idx 大于或等于 mgr 的 活跃层数：
        返还。
    完。
    层 为 mgr 的 层池 的 第 层idx 项。
    层 的 变换提示 为 变换。
    层 的 透明提示 为 透明。
    层 的 滚动提示 为 滚动。
    层 的 内容提示 为 内容。
完。

NWCO_处理will_change 接受 元素、属性字符串：
    若 元素 等于 0 或者 属性字符串 等于 0：
        返还。
    完。
    若 做 字符串_等于 用 属性字符串、「auto」：
        做 NWCO_降级元素 用 元素。
        返还。
    完。
    宽 为 元素 的 宽度。
    高 为 元素 的 高度。
    若 宽 小于或等于 0：
        宽 为 100。
    完。
    若 高 小于或等于 0：
        高 为 100。
    完。
    层idx 为 做 NWCO_提升元素 用 元素、宽、高。
    若 层idx 小于 0：
        返还。
    完。
    有变换 为 做 字符串_包含 用 属性字符串、「transform」。
    有透明 为 做 字符串_包含 用 属性字符串、「opacity」。
    有滚动 为 做 字符串_包含 用 属性字符串、「scroll-position」。
    有内容 为 做 字符串_包含 用 属性字符串、「contents」。
    做 NWCO_设置层提示 用 层idx、有变换、有透明、有滚动、有内容。
    若 有变换 或者 有透明：
        做 NWCO_设置层GPU 用 层idx、真。
    完。
    做 NWCO_解析will_change 用 属性字符串。
完。

NWCO_LRU降级 接受 目标释放KB：
    mgr 为 做 NWCO_获取管理器。
    可变 已释放 为 0。
    只要 已释放 小于 目标释放KB 并且 mgr 的 活跃层数 大于 0：
        可变 最旧idx 为 0。
        可变 最旧时间 为 mgr 的 层池 的 第 0 项 的 最后访问。
        可变 i 为 1。
        只要 i 小于 mgr 的 活跃层数：
            若 mgr 的 层池 的 第 i 项 的 最后访问 小于 最旧时间：
                最旧时间 为 mgr 的 层池 的 第 i 项 的 最后访问。
                最旧idx 为 i。
            完。
            i 为 i 加 1。
        完。
        已释放 为 已释放 加 mgr 的 层池 的 第 最旧idx 项 的 内存KB。
        做 NWCO_释放合成层 用 最旧idx。
    完。
    返还 已释放。
完。

NWCO_检查内存压力 接受 零参：
    mgr 为 做 NWCO_获取管理器。
    预算KB 为 NWCO_内存预算_MB 乘 1024。
    若 mgr 的 总内存KB 大于 预算KB 乘 90 除以 100：
        目标 为 mgr 的 总内存KB 减 预算KB 乘 75 除以 100。
        做 NWCO_LRU降级 用 目标。
    完。
完。

NWCO_帧开始 接受 零参：
    mgr 为 做 NWCO_获取管理器。
    mgr 的 当前帧号 为 mgr 的 当前帧号 加 1。
    NWCO_帧优化次数 为 NWCO_帧优化次数 加 1。
完。

NWCO_帧结束 接受 零参：
    做 NWCO_清除过期追踪 用 300。
    做 NWCO_检查内存压力。
完。

NWCO_调整帧率 接受 rs：
    若 rs 的 连续掉帧 大于或等于 3：
        rs 的 目标帧率 为 30。
        rs 的 帧预算ms 为 33。
    否则：
        rs 的 目标帧率 为 60。
        rs 的 帧预算ms 为 16。
    完。
完。

渲染Scheduler_执行增强 接受 零参：
    rs 为 做 渲染Scheduler_获取。
    做 NWCO_帧开始。
    做 NWCO_调整帧率 用 rs。
    开始 为 做 平台_获取高精度时间。
    若 rs 的 需要样式：
        rs 的 需要样式 为 假。
    完。
    t1 为 做 平台_获取高精度时间 减 开始。
    若 rs 的 需要布局：
        rs 的 需要布局 为 假。
    完。
    t2 为 做 平台_获取高精度时间 减 开始。
    若 t2 大于 rs 的 帧预算ms：
        rs 的 掉帧计数 为 rs 的 掉帧计数 加 1。
        rs 的 连续掉帧 为 rs 的 连续掉帧 加 1。
        做 NWCO_帧结束。
        返还。
    完。
    若 rs 的 需要绘制：
        rs 的 需要绘制 为 假。
    完。
    t3 为 做 平台_获取高精度时间 减 开始。
    若 rs 的 需要合成：
        做 NWCO_合成所有活跃层。
        rs 的 需要合成 为 假。
    完。
    rs 的 连续掉帧 为 0。
    做 NWCO_帧结束。
完。

NWCO_合成所有活跃层 接受 零参：
    mgr 为 做 NWCO_获取管理器。
    可变 i 为 0。
    只要 i 小于 mgr 的 活跃层数：
        层 为 mgr 的 层池 的 第 i 项。
        若 层 的 是活跃：
            做 NWCO_合成单层 用 层。
        完。
        i 为 i 加 1。
    完。
完。

NWCO_合成单层 接受 层：
    若 层 的 是GPU加速：
        若 层 的 变换提示：
            做 图处_提交变换 用 层 的 元素ID。
        完。
        若 层 的 透明提示：
            做 图处_提交透明 用 层 的 元素ID。
        完。
    否则：
        做 CPU_合成层 用 层 的 元素ID、层 的 宽度、层 的 高度。
    完。
    若 层 的 滚动提示：
        做 滚动缓冲_更新 用 层 的 元素ID。
    完。
完。

NWCO_AI预测提升收益 接受 元素、宽、高：
    NWCO_AI推理次数 为 NWCO_AI推理次数 加 1。
    mgr 为 做 NWCO_获取管理器。
    面积 为 宽 乘 高。
    内存比 为 mgr 的 总内存KB 除以 (NWCO_内存预算_MB 乘 1024)。
    层比 为 mgr 的 活跃层数 除以 mgr 的 层池容量。
    tensor 为 做 Tensor_创建 用 6。
    做 Tensor_写 用 tensor、0、面积 除以 10000.0。
    做 Tensor_写 用 tensor、1、内存比。
    做 Tensor_写 用 tensor、2、层比。
    做 Tensor_写 用 tensor、3、mgr 的 当前帧号 除以 1000.0。
    做 Tensor_写 用 tensor、4、元素 的 动画计数。
    做 Tensor_写 用 tensor、5、元素 的 交互计数。
    推理 为 做 AI总线_推理 用 全局AI总线、「nwco_promote」、tensor。
    收益 为 做 Tensor_读 用 推理、0。
    做 Tensor_释放 用 tensor。
    做 Tensor_释放 用 推理。
    返还 收益。
完。

NWCO_智能提升 接受 元素、宽、高：
    收益 为 做 NWCO_AI预测提升收益 用 元素、宽、高。
    若 收益 大于或等于 NWCO_AI阈值：
        返还 做 NWCO_提升元素 用 元素、宽、高。
    完。
    返还 负 1。
完。

NWCO_AI异常检测 接受 零参：
    mgr 为 做 NWCO_获取管理器。
    tensor 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 tensor、0、mgr 的 活跃层数。
    做 Tensor_写 用 tensor、1、mgr 的 总内存KB 除以 1024.0。
    做 Tensor_写 用 tensor、2、NWCO_提升次数。
    做 Tensor_写 用 tensor、3、NWCO_降级次数。
    异常 为 做 AI总线_异常检测 用 全局AI总线、「nwco_anomaly」、tensor。
    分 为 做 Tensor_读 用 异常、0。
    做 Tensor_释放 用 tensor。
    做 Tensor_释放 用 异常。
    若 分 大于 0.85：
        做 打印 用 「[NWCO] AI异常: 合成层资源异常 score=」 加 分。
        做 NWCO_LRU降级 用 mgr 的 总内存KB 除以 4。
    完。
完。

NWCO_诊断报告 接受 零参：
    mgr 为 做 NWCO_获取管理器。
    做 打印 用 「[NWCO] === Will-Change合成优化诊断 ===」。
    做 打印 用 「  活跃层数=」 加 mgr 的 活跃层数 加 「 / 」 加 mgr 的 层池容量。
    做 打印 用 「  总内存=」 加 mgr 的 总内存KB 加 「KB / 」 加 NWCO_内存预算_MB 乘 1024 加 「KB」。
    做 打印 用 「  追踪属性=」 加 mgr 的 追踪数 加 「 帧号=」 加 mgr 的 当前帧号。
    做 打印 用 「  提升=」 加 做 整数转字符串 用 NWCO_提升次数 加 「 降级=」 加 做 整数转字符串 用 NWCO_降级次数。
    做 打印 用 「  AI推理=」 加 做 整数转字符串 用 NWCO_AI推理次数 加 「 帧优化=」 加 做 整数转字符串 用 NWCO_帧优化次数。
完。

NWCO_重置统计 接受 零参：
    NWCO_提升次数 为 0。
    NWCO_降级次数 为 0。
    NWCO_AI推理次数 为 0。
    NWCO_帧优化次数 为 0。
完。

本章 收束。
