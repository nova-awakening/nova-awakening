《HTML令牌定义》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

令牌类型_未初始化 为 0。

令牌类型_DOCTYPE 为 1。

令牌类型_开始标签 为 2。

令牌类型_结束标签 为 3。

令牌类型_注释 为 4。

令牌类型_字符 为 5。

令牌类型_文件结束 为 6。

HTML令牌 是 一种 事物，含 类型、标签名、标签名长度、自闭合、属性列表、属性数量、属性容量、DOCTYPE名称、DOCTYPE公共ID、DOCTYPE系统ID、强制怪异模式、数据、数据长度。

令牌Attribute 是 一种 事物，含 名称、名称长度、值、值长度。

状态_数据 为 0。

状态_字符引用 为 1。

状态_标签开始 为 2。

状态_结束标签开始 为 3。

状态_标签名 为 4。

状态_属性名前 为 5。

状态_属性名 为 6。

状态_属性名后 为 7。

状态_属性值前 为 8。

状态_属性值_双引号 为 9。

状态_属性值_单引号 为 10。

状态_属性值_无引号 为 11。

状态_属性值后 为 12。

状态_自闭合开始标签 为 13。

状态_伪注释 为 14。

状态_标记声明开始 为 15。

状态_注释开始 为 16。

状态_注释开始破折号 为 17。

状态_注释 为 18。

状态_注释小于号 为 19。

状态_注释结束破折号 为 20。

状态_注释结束 为 21。

状态_DOCTYPE 为 22。

状态_DOCTYPE前 为 23。

状态_DOCTYPE名称 为 24。

状态_DOCTYPE名称后 为 25。

状态_RCDATA 为 26。

状态_RAWTEXT 为 27。

状态_脚本数据 为 28。

状态_PLAINTEXT 为 29。

状态_RCDATA小于号 为 30。

状态_RCDATA结束标签开始 为 31。

状态_RCDATA结束标签名 为 32。

状态_RAWTEXT小于号 为 33。

状态_RAWTEXT结束标签开始 为 34。

状态_RAWTEXT结束标签名 为 35。

状态_脚本数据小于号 为 36。

状态_脚本数据结束标签开始 为 37。

状态_脚本数据结束标签名 为 38。

状态_脚本数据转义开始 为 39。

状态_脚本数据转义开始破折号 为 40。

状态_脚本数据已转义 为 41。

状态_脚本数据已转义破折号 为 42。

状态_脚本数据已转义破折号破折号 为 43。

状态_脚本数据已转义小于号 为 44。

状态_脚本数据已转义结束标签开始 为 45。

状态_脚本数据已转义结束标签名 为 46。

状态_脚本数据双重转义开始 为 47。

状态_脚本数据双重已转义 为 48。

本章 收束。
