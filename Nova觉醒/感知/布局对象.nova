《布局对象》

引用《.·样式解析》。

引用《.·文档节点》。

引用《.·内核直连层》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·时钟中枢》。

布局类型_块 为 1。

布局类型_内联 为 2。

布局类型_文本 为 3。

布局类型_行盒 为 4。

布局类型_弹性 为 5。

布局类型_网格 为 6。

布局类型_表格 为 7。

布局类型_表格行 为 8。

布局类型_表格单元 为 9。

布局类型_替换 为 10。

布局类型_视图 为 11。

布局标志_需要布局 为 0x01。

布局标志_子树需要布局 为 0x02。

布局标志_需要绘制 为 0x04。

布局标志_是浮动 为 0x08。

布局标志_是定位 为 0x10。

布局标志_是块级格式化上下文 为 0x20。

布局标志_有溢出 为 0x40。

布局标志_AI已预测 为 0x80。

布局标志_可跳过布局 为 0x100。

布局标志_低精度布局 为 0x200。

布局Object 是 一种 事物，含 父、第一子、最后子、前兄、后兄、类型、标志、DOM节点、样式、x、y、内容宽、内容高、绝对x、绝对y、子数量、预测宽、预测高、预测置信度、布局版本、父布局版本、布局精度、布局优先级、上次布局耗时、累计布局次数。

可变 全局NILE统计 为 新建 NILE统计结构。

NILE统计结构 是 一种 事物，含 布局总次数、跳过次数、粗略布局次数、精确布局次数、AI预测次数、总耗时_us。

布局Object_创建 接受 类型、dom节点：
    obj 为 做 堆_分配 用 布局Object。
    若 obj 等于 0：
        返还 空。
    完。
    obj 的 父 为 空。
    obj 的 第一子 为 空。
    obj 的 最后子 为 空。
    obj 的 前兄 为 空。
    obj 的 后兄 为 空。
    obj 的 类型 为 类型。
    obj 的 标志 为 布局标志_需要布局。
    obj 的 DOM节点 为 dom节点。
    obj 的 样式 为 空。
    obj 的 x 为 0。
    obj 的 y 为 0。
    obj 的 内容宽 为 0。
    obj 的 内容高 为 0。
    obj 的 绝对x 为 0。
    obj 的 绝对y 为 0。
    obj 的 子数量 为 0。
    obj 的 预测宽 为 0。
    obj 的 预测高 为 0。
    obj 的 预测置信度 为 0。
    obj 的 布局版本 为 0。
    obj 的 父布局版本 为 0。
    obj 的 布局精度 为 2。
    obj 的 布局优先级 为 128。
    obj 的 上次布局耗时 为 0。
    obj 的 累计布局次数 为 0。
    返还 obj。
完。

布局Object_销毁 接受 obj：
    若 obj 等于 空：
        返还。
    完。
    可变 child 为 obj 的 第一子。
    只要 child 不等于 空：
        next 为 child 的 后兄。
        做 布局Object_销毁 用 child。
        child 为 next。
    完。
    做 堆_释放 用 obj。
完。

布局Object_添加子节点 接受 父、子：
    若 父 等于 空 或者 子 等于 空：
        返还。
    完。
    子 的 父 为 父。
    子 的 前兄 为 父 的 最后子。
    子 的 后兄 为 空。
    若 父 的 最后子 不等于 空：
        父 的 最后子 的 后兄 为 子。
    否则：
        父 的 第一子 为 子。
    完。
    父 的 最后子 为 子。
    父 的 子数量 为 父 的 子数量 加 1。
    做 布局Object_标记脏 用 父。
完。

布局Object_移除子节点 接受 父、子：
    若 父 等于 空 或者 子 等于 空 或者 子 的 父 不等于 父：
        返还。
    完。
    若 子 的 前兄 不等于 空：
        子 的 前兄 的 后兄 为 子 的 后兄。
    否则：
        父 的 第一子 为 子 的 后兄。
    完。
    若 子 的 后兄 不等于 空：
        子 的 后兄 的 前兄 为 子 的 前兄。
    否则：
        父 的 最后子 为 子 的 前兄。
    完。
    子 的 父 为 空。
    子 的 前兄 为 空。
    子 的 后兄 为 空。
    父 的 子数量 为 父 的 子数量 减 1。
    做 布局Object_标记脏 用 父。
完。

布局Object_标记脏 接受 obj：
    若 obj 等于 空：
        返还。
    完。
    obj 的 标志 为 obj 的 标志 位或 布局标志_需要布局。
    可变 p 为 obj 的 父。
    只要 p 不等于 空：
        若 p 的 标志 位与 布局标志_子树需要布局 不等于 0：
            返还。
        完。
        p 的 标志 为 p 的 标志 位或 布局标志_子树需要布局。
        p 为 p 的 父。
    完。
完。

布局Object_清除脏标志 接受 obj：
    若 obj 等于 空：
        返还。
    完。
    obj 的 标志 为 obj 的 标志 位与 按位取反 布局标志_需要布局。
    obj 的 标志 为 obj 的 标志 位与 按位取反 布局标志_子树需要布局。
完。

布局Object_需要布局 接受 obj：
    若 obj 等于 空：
        返还 假。
    完。
    返还 obj 的 标志 位与 布局标志_需要布局 不等于 0。
完。

布局Object_AI预测尺寸 接受 obj：
    若 obj 等于 空：
        返还。
    完。
    特征 为 做 Tensor_创建 用 6。
    做 Tensor_写 用 特征、0、obj 的 类型。
    做 Tensor_写 用 特征、1、obj 的 内容宽。
    做 Tensor_写 用 特征、2、obj 的 内容高。
    做 Tensor_写 用 特征、3、obj 的 子数量。
    做 Tensor_写 用 特征、4、obj 的 布局版本。
    做 Tensor_写 用 特征、5、obj 的 累计布局次数。
    结果 为 做 AI总线_推理 用 全局AI总线、特征。
    做 Tensor_释放 用 特征。
    全局NILE统计 的 AI预测次数 为 全局NILE统计 的 AI预测次数 加 1。
    若 结果 大于 0：
        obj 的 预测宽 为 结果 右移 16。
        obj 的 预测高 为 结果 位与 0xFFFF。
        obj 的 预测置信度 为 8000。
        obj 的 标志 为 obj 的 标志 位或 布局标志_AI已预测。
    完。
完。

布局Object_可跳过布局 接受 obj：
    若 obj 等于 空：
        返还 假。
    完。
    若 obj 的 标志 位与 布局标志_可跳过布局 不等于 0：
        返还 真。
    完。
    若 obj 的 预测置信度 大于 7000：
        若 obj 的 父 不等于 空 并且 obj 的 父布局版本 等于 obj 的 父 的 布局版本：
            obj 的 标志 为 obj 的 标志 位或 布局标志_可跳过布局。
            返还 真。
        完。
    完。
    返还 假。
完。

布局Object_设置精度 接受 obj、视口顶、视口底：
    若 obj 等于 空：
        返还。
    完。
    若 obj 的 绝对y 加 obj 的 内容高 小于 视口顶 或者 obj 的 绝对y 大于 视口底：
        obj 的 布局精度 为 1。
        obj 的 布局优先级 为 32。
    否则：
        obj 的 布局精度 为 3。
        obj 的 布局优先级 为 200。
    完。
    可变 child 为 obj 的 第一子。
    只要 child 不等于 空：
        做 布局Object_设置精度 用 child、视口顶、视口底。
        child 为 child 的 后兄。
    完。
完。

布局Object_块级布局 接受 obj、可用宽：
    若 obj 等于 空：
        返还。
    完。
    开始 为 做 读取微秒。
    若 做 布局Object_可跳过布局 用 obj：
        obj 的 内容宽 为 obj 的 预测宽。
        obj 的 内容高 为 obj 的 预测高。
        全局NILE统计 的 跳过次数 为 全局NILE统计 的 跳过次数 加 1。
        返还。
    完。
    obj 的 内容宽 为 可用宽。
    可变 yOffset 为 0。
    可变 child 为 obj 的 第一子。
    只要 child 不等于 空：
        child 的 x 为 0。
        child 的 y 为 yOffset。
        若 obj 的 布局精度 小于或等于 1：
            child 的 内容宽 为 可用宽。
            child 的 内容高 为 2000。
            全局NILE统计 的 粗略布局次数 为 全局NILE统计 的 粗略布局次数 加 1。
        否则：
            做 布局Object_执行布局 用 child、可用宽。
            全局NILE统计 的 精确布局次数 为 全局NILE统计 的 精确布局次数 加 1。
        完。
        yOffset 为 yOffset 加 child 的 内容高。
        child 为 child 的 后兄。
    完。
    obj 的 内容高 为 yOffset。
    obj 的 布局版本 为 obj 的 布局版本 加 1。
    obj 的 累计布局次数 为 obj 的 累计布局次数 加 1。
    做 布局Object_清除脏标志 用 obj。
    耗时 为 做 读取微秒 减 开始。
    obj 的 上次布局耗时 为 耗时。
    全局NILE统计 的 总耗时_us 为 全局NILE统计 的 总耗时_us 加 耗时。
    全局NILE统计 的 布局总次数 为 全局NILE统计 的 布局总次数 加 1。
完。

布局Object_执行布局 接受 obj、可用宽：
    若 obj 等于 空：
        返还。
    完。
    若 obj 的 类型 等于 布局类型_块 或者 obj 的 类型 等于 布局类型_视图：
        做 布局Object_块级布局 用 obj、可用宽。
    否则：
        做 布局Object_块级布局 用 obj、可用宽。
    完。
完。

布局Object_计算绝对坐标 接受 obj：
    若 obj 等于 空：
        返还。
    完。
    若 obj 的 父 不等于 空：
        obj 的 绝对x 为 obj 的 父 的 绝对x 加 obj 的 x。
        obj 的 绝对y 为 obj 的 父 的 绝对y 加 obj 的 y。
    否则：
        obj 的 绝对x 为 obj 的 x。
        obj 的 绝对y 为 obj 的 y。
    完。
    可变 child 为 obj 的 第一子。
    只要 child 不等于 空：
        做 布局Object_计算绝对坐标 用 child。
        child 为 child 的 后兄。
    完。
完。

NILE_初始化 接受 零参：
    做 内核打印 用 「[NILE] Nova智能布局执行引擎 启动」。
    全局NILE统计 的 布局总次数 为 0。
    全局NILE统计 的 跳过次数 为 0。
    全局NILE统计 的 粗略布局次数 为 0。
    全局NILE统计 的 精确布局次数 为 0。
    全局NILE统计 的 AI预测次数 为 0。
    全局NILE统计 的 总耗时_us 为 0。
    做 内核打印 用 「[NILE] 初始化完成 — AI预测/注意力精度/增量布局/块级流 就绪」。
完。

NILE_打印诊断 接受 零参：
    做 内核打印 用 「╔════════════════════════════════════════════╗」。
    做 内核打印 用 「║  NILE 智能布局执行 诊断报告              ║」。
    做 内核打印 用 「╠════════════════════════════════════════════╣」。
    做 内核打印 用 「║ 布局总次:     {}」、全局NILE统计 的 布局总次数。
    做 内核打印 用 「║ AI跳过:       {}」、全局NILE统计 的 跳过次数。
    做 内核打印 用 「║ 粗略布局:     {}」、全局NILE统计 的 粗略布局次数。
    做 内核打印 用 「║ 精确布局:     {}」、全局NILE统计 的 精确布局次数。
    做 内核打印 用 「║ AI预测:       {}」、全局NILE统计 的 AI预测次数。
    做 内核打印 用 「║ 总耗时:       {}μs」、全局NILE统计 的 总耗时_us。
    做 内核打印 用 「╚════════════════════════════════════════════╝」。
完。

布局Object_命中测试 接受 obj、testX、testY：
    若 obj 等于 空：
        返还 空。
    完。
    若 testX 小于 obj 的 绝对x 或者 testX 大于 obj 的 绝对x 加 obj 的 内容宽：
        返还 空。
    完。
    若 testY 小于 obj 的 绝对y 或者 testY 大于 obj 的 绝对y 加 obj 的 内容高：
        返还 空。
    完。
    可变 child 为 obj 的 最后子。
    只要 child 不等于 空：
        hit 为 做 布局Object_命中测试 用 child、testX、testY。
        若 hit 不等于 空：
            返还 hit。
        完。
        child 为 child 的 前兄。
    完。
    返还 obj。
完。

语义_主内容 为 0。

语义_导航 为 1。

语义_侧边栏 为 2。

语义_页脚 为 3。

语义_广告 为 4。

布局Object_设置语义优先级 接受 obj、语义角色：
    若 obj 等于 空：
        返还。
    完。
    若 语义角色 等于 语义_主内容：
        obj 的 布局优先级 为 250。
        obj 的 布局精度 为 3。
    否则若 语义角色 等于 语义_导航：
        obj 的 布局优先级 为 200。
        obj 的 布局精度 为 3。
    否则若 语义角色 等于 语义_侧边栏：
        obj 的 布局优先级 为 128。
        obj 的 布局精度 为 2。
    否则若 语义角色 等于 语义_页脚：
        obj 的 布局优先级 为 64。
        obj 的 布局精度 为 1。
    否则若 语义角色 等于 语义_广告：
        obj 的 布局优先级 为 16。
        obj 的 布局精度 为 0。
    完。
完。

布局Object_统计树 接受 根、总节点数、总脏节点数：
    若 根 等于 空：
        返还。
    完。
    做 内存_写64 用 总节点数、做 内存_读64 用 总节点数 加 1。
    若 做 布局Object_需要布局 用 根：
        做 内存_写64 用 总脏节点数、做 内存_读64 用 总脏节点数 加 1。
    完。
    可变 child 为 根 的 第一子。
    只要 child 不等于 空：
        做 布局Object_统计树 用 child、总节点数、总脏节点数。
        child 为 child 的 后兄。
    完。
完。

布局Object_设置样式 接受 obj、样式：
    若 obj 等于 空：
        返还。
    完。
    obj 的 样式 为 样式。
    做 布局Object_标记脏 用 obj。
完。

布局Object_获取类型名称 接受 obj：
    若 obj 等于 空：
        返还 「空」。
    完。
    若 obj 的 类型 等于 布局类型_块：
        返还 「块」。
    完。
    若 obj 的 类型 等于 布局类型_内联：
        返还 「内联」。
    完。
    若 obj 的 类型 等于 布局类型_文本：
        返还 「文本」。
    完。
    若 obj 的 类型 等于 布局类型_弹性：
        返还 「弹性」。
    完。
    若 obj 的 类型 等于 布局类型_网格：
        返还 「网格」。
    完。
    若 obj 的 类型 等于 布局类型_视图：
        返还 「视图」。
    完。
    返还 「其他」。
完。

布局Object_获取边界 接受 根、结果x、结果y、结果宽、结果高：
    若 根 等于 空：
        返还。
    完。
    做 内存_写32 用 结果x、根 的 绝对x。
    做 内存_写32 用 结果y、根 的 绝对y。
    做 内存_写32 用 结果宽、根 的 内容宽。
    做 内存_写32 用 结果高、根 的 内容高。
完。

本章 收束。
