《Flex弹性布局》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·写回位置到》。

弹性布局_弹性调整 接受 容器、行：
    若 行 等于 0：
        返还。
    完。
    剩余 为 行 的 剩余空间。
    若 剩余 等于 0：
        返还。
    完。
    是增长 为 剩余 大于 0。
    可变 i 为 行 的 起始索引。
    只要 i 小于 行 的 结束索引：
        项 为 做 弹性布局_获取项 用 容器、i。
        项 的 已冻结 为 假。
        若 是增长 并且 项 的 flex_grow 等于 0：
            项 的 已冻结 为 真。
        完。
        若 非 是增长 并且 项 的 flex_shrink 等于 0：
            项 的 已冻结 为 真。
        完。
        i 为 i 加 1。
    完。
    可变 轮次 为 0。
    只要 轮次 小于 10：
        可变 总因子 为 0。
        可变 未冻结数 为 0。
        i 为 行 的 起始索引。
        只要 i 小于 行 的 结束索引：
            项 为 做 弹性布局_获取项 用 容器、i。
            若 非 项 的 已冻结：
                若 是增长：
                    总因子 为 总因子 加 项 的 flex_grow。
                否则：
                    总因子 为 总因子 加 项 的 flex_shrink。
                完。
                未冻结数 为 未冻结数 加 1。
            完。
            i 为 i 加 1。
        完。
        若 未冻结数 等于 0 或者 总因子 等于 0：
            跳出。
        完。
        可变 有冻结 为 假。
        i 为 行 的 起始索引。
        只要 i 小于 行 的 结束索引：
            项 为 做 弹性布局_获取项 用 容器、i。
            若 非 项 的 已冻结：
                因子 为 若 是增长 则 项 的 flex_grow 否则 项 的 flex_shrink。
                份额 为 剩余 乘 因子 除以 总因子。
                新尺寸 为 项 的 基准尺寸 加 份额。
                最小 为 0。
                最大 为 0x7FFFFFFF。
                若 新尺寸 小于 最小：
                    新尺寸 为 最小。
                    项 的 已冻结 为 真。
                    有冻结 为 真。
                完。
                若 新尺寸 大于 最大：
                    新尺寸 为 最大。
                    项 的 已冻结 为 真。
                    有冻结 为 真。
                完。
                项 的 主轴尺寸 为 新尺寸。
            完。
            i 为 i 加 1。
        完。
        若 非 有冻结：
            跳出。
        完。
        可变 新已用 为 0。
        i 为 行 的 起始索引。
        只要 i 小于 行 的 结束索引：
            项 为 做 弹性布局_获取项 用 容器、i。
            新已用 为 新已用 加 项 的 主轴尺寸 加 项 的 主轴margin前 加 项 的 主轴margin后。
            i 为 i 加 1。
        完。
        剩余 为 容器 的 可用主轴 减 新已用。
        轮次 为 轮次 加 1。
    完。
完。

弹性布局_计算交叉轴 接受 容器：
    可变 l 为 0。
    只要 l 小于 容器 的 行数：
        行 为 做 弹性布局_获取行 用 容器、l。
        可变 最大交叉 为 0。
        可变 i 为 行 的 起始索引。
        只要 i 小于 行 的 结束索引：
            项 为 做 弹性布局_获取项 用 容器、i。
            对齐 为 项 的 align_self。
            若 对齐 等于 ALIGN_AUTO：
                对齐 为 容器 的 交叉轴对齐。
            完。
            若 对齐 等于 ALIGN_STRETCH 并且 项 的 交叉轴尺寸 等于 0：
            否则：
                若 项 的 布局对象 不等于 0：
                    是行方向 为 容器 的 方向 等于 FLEX_DIR_ROW 或者 容器 的 方向 等于 FLEX_DIR_ROW_REVERSE。
                    若 是行方向：
                        项 的 交叉轴尺寸 为 项 的 布局对象 的 高度。
                    否则：
                        项 的 交叉轴尺寸 为 项 的 布局对象 的 宽度。
                    完。
                完。
            完。
            外部交叉 为 项 的 交叉轴尺寸 加 项 的 交叉margin前 加 项 的 交叉margin后。
            若 外部交叉 大于 最大交叉：
                最大交叉 为 外部交叉。
            完。
            i 为 i 加 1。
        完。
        行 的 交叉轴尺寸 为 最大交叉。
        i 为 行 的 起始索引。
        只要 i 小于 行 的 结束索引：
            项 为 做 弹性布局_获取项 用 容器、i。
            对齐 为 项 的 align_self。
            若 对齐 等于 ALIGN_AUTO：
                对齐 为 容器 的 交叉轴对齐。
            完。
            若 对齐 等于 ALIGN_STRETCH 并且 项 的 交叉轴尺寸 等于 0：
                项 的 交叉轴尺寸 为 行 的 交叉轴尺寸 减 项 的 交叉margin前 减 项 的 交叉margin后。
            完。
            i 为 i 加 1。
        完。
        l 为 l 加 1。
    完。
完。

本章 收束。
