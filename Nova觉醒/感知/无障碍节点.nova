《无障碍节点》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·整数转字符串》。

最大无障碍节点 为 256。

角色_按钮 为 1。

角色_链接 为 2。

角色_输入 为 3。

角色_文本 为 4。

角色_图像 为 5。

角色_标题 为 6。

角色_列表 为 7。

角色_区域 为 8。

无障碍节点 是 一种 事物，含 元素标识、角色、标签哈希、描述哈希、可聚焦、已聚焦、可见、层级、父节点、下一兄弟、首子节点。

可变 节点表 为 一组：（空）。

可变 节点数 为 0。

可变 焦点节点 为 负 1。

可变 朗读次数 为 0。

可变 导航次数 为 0。

无障碍_初始化 接受 零参：
    节点表 为 一组：（空）。
    节点数 为 0。
    焦点节点 为 负 1。
    总线 为 全局智能总线。
    若 总线 不等于 0：
        做 智能总线_注册模块 用 总线、「无障碍」、无障碍_智能回调。
    完。
完。

无障碍_注册节点 接受 元素标识、角色、标签、描述、可聚焦：
    若 节点数 大于或等于 最大无障碍节点：
        返还 负 1。
    完。
    节点 为 新建 无障碍节点。
    节点 的 元素标识 为 元素标识。
    节点 的 角色 为 角色。
    节点 的 标签哈希 为 做 哈希_字符串 用 标签。
    节点 的 描述哈希 为 做 哈希_字符串 用 描述。
    节点 的 可聚焦 为 可聚焦。
    节点 的 已聚焦 为 假。
    节点 的 可见 为 真。
    节点 的 层级 为 0。
    节点 的 父节点 为 负 1。
    节点 的 下一兄弟 为 负 1。
    节点 的 首子节点 为 负 1。
    做 追加 用 节点表、节点。
    索引 为 节点数。
    节点数 为 节点数 加 1。
    返还 索引。
完。

无障碍_设父 接受 子索引、父索引：
    若 子索引 大于或等于 节点数 或者 父索引 大于或等于 节点数：
        返还。
    完。
    节点表 的 第 子索引 项 的 父节点 为 父索引。
    节点表 的 第 子索引 项 的 层级 为 节点表 的 第 父索引 项 的 层级 加 1。
    若 节点表 的 第 父索引 项 的 首子节点 等于 负 1：
        节点表 的 第 父索引 项 的 首子节点 为 子索引。
    否则：
        可变 兄弟 为 节点表 的 第 父索引 项 的 首子节点。
        只要 节点表 的 第 兄弟 项 的 下一兄弟 不等于 负 1：
            兄弟 为 节点表 的 第 兄弟 项 的 下一兄弟。
        完。
        节点表 的 第 兄弟 项 的 下一兄弟 为 子索引。
    完。
完。

无障碍_聚焦下一个 接受 零参：
    导航次数 为 导航次数 加 1。
    可变 起始 为 焦点节点 加 1。
    若 起始 大于或等于 节点数：
        起始 为 0。
    完。
    可变 搜索 为 起始。
    可变 已检查 为 0。
    只要 已检查 小于 节点数：
        若 节点表 的 第 搜索 项 的 可聚焦 并且 节点表 的 第 搜索 项 的 可见：
            做 无障碍_设焦点 用 搜索。
            返还 搜索。
        完。
        搜索 为 (搜索 加 1) 取余于 节点数。
        已检查 为 已检查 加 1。
    完。
    返还 负 1。
完。

无障碍_聚焦上一个 接受 零参：
    导航次数 为 导航次数 加 1。
    可变 起始 为 焦点节点 减 1。
    若 起始 小于 0：
        起始 为 节点数 减 1。
    完。
    可变 搜索 为 起始。
    可变 已检查 为 0。
    只要 已检查 小于 节点数：
        若 节点表 的 第 搜索 项 的 可聚焦 并且 节点表 的 第 搜索 项 的 可见：
            做 无障碍_设焦点 用 搜索。
            返还 搜索。
        完。
        搜索 为 搜索 减 1。
        若 搜索 小于 0：
            搜索 为 节点数 减 1。
        完。
        已检查 为 已检查 加 1。
    完。
    返还 负 1。
完。

无障碍_设焦点 接受 索引：
    若 焦点节点 大于或等于 0 并且 焦点节点 小于 节点数：
        节点表 的 第 焦点节点 项 的 已聚焦 为 假。
    完。
    焦点节点 为 索引。
    若 索引 大于或等于 0 并且 索引 小于 节点数：
        节点表 的 第 索引 项 的 已聚焦 为 真。
        做 无障碍_朗读节点 用 索引。
    完。
完。

无障碍_朗读节点 接受 索引：
    若 索引 大于或等于 节点数：
        返还。
    完。
    朗读次数 为 朗读次数 加 1。
完。

无障碍_获取角色名 接受 角色：
    若 角色 等于 角色_按钮：
        返还 「按钮」。
    完。
    若 角色 等于 角色_链接：
        返还 「链接」。
    完。
    若 角色 等于 角色_输入：
        返还 「输入框」。
    完。
    若 角色 等于 角色_文本：
        返还 「文本」。
    完。
    若 角色 等于 角色_图像：
        返还 「图像」。
    完。
    若 角色 等于 角色_标题：
        返还 「标题」。
    完。
    若 角色 等于 角色_列表：
        返还 「列表」。
    完。
    若 角色 等于 角色_区域：
        返还 「区域」。
    完。
    返还 「未知」。
完。

无障碍_智能回调 接受 事件类型、数据：
    若 事件类型 等于 1：
        做 无障碍_聚焦下一个。
    完。
    若 事件类型 等于 2：
        做 无障碍_聚焦上一个。
    完。
完。

无障碍_完整统计 接受 零参：
    返还 一组：节点数、焦点节点、朗读次数、导航次数。
完。

无障碍_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 节点数 等于 0：
        做 追加 用 问题、「无障碍树为空」。
    完。
    无标签数 为 做 无障碍_统计无标签。
    若 无标签数 大于 节点数 除以 3 并且 节点数 大于 10：
        做 追加 用 问题、「大量元素缺少标签: 」 加 做 整数转字符串 用 无标签数 加 「/」 加 做 整数转字符串 用 节点数。
    完。
    不可聚焦交互 为 做 无障碍_统计不可聚焦交互。
    若 不可聚焦交互 大于 0：
        做 追加 用 问题、「交互元素不可聚焦: 」 加 做 整数转字符串 用 不可聚焦交互。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「无障碍树健康」。
    完。
    返还 问题。
完。

无障碍_统计无标签 接受 零参：
    可变 无标签 为 0。
    可变 i 为 0。
    只要 i 小于 节点数：
        若 节点表 的 第 i 项 的 标签哈希 等于 0：
            无标签 为 无标签 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 无标签。
完。

无障碍_统计不可聚焦交互 接受 零参：
    可变 数量 为 0。
    可变 i 为 0。
    只要 i 小于 节点数：
        角色 为 节点表 的 第 i 项 的 角色。
        若 (角色 等于 角色_按钮 或者 角色 等于 角色_链接 或者 角色 等于 角色_输入) 并且 非 节点表 的 第 i 项 的 可聚焦：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

无障碍_详细统计 接受 零参：
    返还 一本字典：节点数 对应 节点数、焦点节点 对应 焦点节点、朗读次数 对应 朗读次数、导航次数 对应 导航次数、按钮数 对应 做 无障碍_按角色统计 用 角色_按钮、链接数 对应 做 无障碍_按角色统计 用 角色_链接、输入数 对应 做 无障碍_按角色统计 用 角色_输入、图像数 对应 做 无障碍_按角色统计 用 角色_图像、标题数 对应 做 无障碍_按角色统计 用 角色_标题、无标签 对应 做 无障碍_统计无标签。
完。

无障碍_按角色统计 接受 目标角色：
    可变 数量 为 0。
    可变 i 为 0。
    只要 i 小于 节点数：
        若 节点表 的 第 i 项 的 角色 等于 目标角色：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

角色_复选框 为 9。

角色_单选框 为 10。

角色_滑块 为 11。

角色_选项卡 为 12。

角色_选项卡面板 为 13。

角色_对话框 为 14。

角色_警告 为 15。

角色_导航 为 16。

角色_菜单 为 17。

角色_菜单项 为 18。

角色_树 为 19。

角色_树项 为 20。

角色_表格 为 21。

角色_行 为 22。

角色_单元格 为 23。

角色_搜索 为 24。

角色_表单 为 25。

角色_主内容 为 26。

角色_补充 为 27。

角色_页脚 为 28。

角色_页头 为 29。

角色_进度条 为 30。

无障碍_获取角色名_扩展 接受 角色：
    若 角色 小于或等于 8：
        返还 做 无障碍_获取角色名 用 角色。
    完。
    若 角色 等于 角色_复选框：
        返还 「复选框」。
    完。
    若 角色 等于 角色_单选框：
        返还 「单选框」。
    完。
    若 角色 等于 角色_滑块：
        返还 「滑块」。
    完。
    若 角色 等于 角色_选项卡：
        返还 「选项卡」。
    完。
    若 角色 等于 角色_对话框：
        返还 「对话框」。
    完。
    若 角色 等于 角色_警告：
        返还 「警告」。
    完。
    若 角色 等于 角色_导航：
        返还 「导航」。
    完。
    若 角色 等于 角色_菜单：
        返还 「菜单」。
    完。
    若 角色 等于 角色_表格：
        返还 「表格」。
    完。
    若 角色 等于 角色_搜索：
        返还 「搜索」。
    完。
    若 角色 等于 角色_主内容：
        返还 「主要内容」。
    完。
    若 角色 等于 角色_进度条：
        返还 「进度条」。
    完。
    返还 「未知角色」。
完。

无障碍_是交互角色 接受 角色：
    返还 角色 等于 角色_按钮 或者 角色 等于 角色_链接 或者 角色 等于 角色_输入 或者 角色 等于 角色_复选框 或者 角色 等于 角色_单选框 或者 角色 等于 角色_滑块 或者 角色 等于 角色_菜单项 或者 角色 等于 角色_选项卡。
完。

无障碍_是地标角色 接受 角色：
    返还 角色 等于 角色_导航 或者 角色 等于 角色_主内容 或者 角色 等于 角色_搜索 或者 角色 等于 角色_表单 或者 角色 等于 角色_补充 或者 角色 等于 角色_页脚 或者 角色 等于 角色_页头。
完。

无障碍_按角色查找 接受 目标角色：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点数：
        若 节点表 的 第 i 项 的 角色 等于 目标角色：
            做 追加 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

无障碍_按标签查找 接受 标签哈希：
    可变 i 为 0。
    只要 i 小于 节点数：
        若 节点表 的 第 i 项 的 标签哈希 等于 标签哈希：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

无障碍_获取子节点 接受 父索引：
    可变 结果 为 一组：（空）。
    若 父索引 大于或等于 节点数：
        返还 结果。
    完。
    可变 子 为 节点表 的 第 父索引 项 的 首子节点。
    只要 子 不等于 负 1 并且 子 小于 节点数：
        做 追加 用 结果、子。
        子 为 节点表 的 第 子 项 的 下一兄弟。
    完。
    返还 结果。
完。

无障碍_获取可聚焦列表 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点数：
        若 节点表 的 第 i 项 的 可聚焦 并且 节点表 的 第 i 项 的 可见：
            做 追加 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

无障碍_获取地标列表 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点数：
        若 做 无障碍_是地标角色 用 节点表 的 第 i 项 的 角色：
            做 追加 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

无障碍_获取标题列表 接受 零参：
    返还 做 无障碍_按角色查找 用 角色_标题。
完。

无障碍_生成朗读文本 接受 索引：
    若 索引 大于或等于 节点数：
        返还 「」。
    完。
    节点 为 节点表 的 第 索引 项。
    角色名 为 做 无障碍_获取角色名_扩展 用 节点 的 角色。
    返还 角色名。
完。

无障碍_节点诊断 接受 索引：
    若 索引 大于或等于 节点数：
        返还 「无效索引」。
    完。
    节点 为 节点表 的 第 索引 项。
    返还 一本字典：元素 对应 节点 的 元素标识、角色 对应 做 无障碍_获取角色名_扩展 用 节点 的 角色、可聚焦 对应 节点 的 可聚焦、已聚焦 对应 节点 的 已聚焦、可见 对应 节点 的 可见、层级 对应 节点 的 层级、有标签 对应 节点 的 标签哈希 不等于 0。
完。

哈希_字符串 接受 s：
    若 s 等于 0：
        返还 0。
    完。
    长度 为 做 字节长度 用 s。
    可变 哈希 为 5381。
    可变 i 为 0。
    只要 i 小于 长度：
        哈希 为 (哈希 左移 5) 加 哈希 加 做 内存_读8 用 s 加 i。
        i 为 i 加 1。
    完。
    返还 哈希 位与 0x7FFFFFFF。
完。

无障碍_重置 接受 零参：
    节点表 为 一组：（空）。
    节点数 为 0。
    焦点节点 为 负 1。
    朗读次数 为 0。
    导航次数 为 0。
完。

本章 收束。
