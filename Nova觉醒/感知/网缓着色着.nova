《网缓着色着》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《..·中枢·内存初始化》。

SHADER_SOLID 为 0。

SHADER_LINEAR 为 1。

SHADER_RADIAL 为 2。

SHADER_SWEEP 为 3。

SHADER_BITMAP 为 4。

TILE_CLAMP 为 0。

TILE_REPEAT 为 1。

TILE_MIRROR 为 2。

TILE_DECAL 为 3。

SkShader 是 一种 事物，含 类型、x0、cx、色标颜色、色标位置、色标数、平铺模式、矩阵。

SkShader_线性渐变 接受 x0、y0、x1、y1、颜色数组、位置数组、数量、平铺：
    s 为 做 堆_分配 用 SkShader。
    s 的 类型 为 SHADER_LINEAR。
    s 的 x0 为 x0。
    s 的 y0 为 y0。
    s 的 x1 为 x1。
    s 的 y1 为 y1。
    s 的 色标颜色 为 颜色数组。
    s 的 色标位置 为 位置数组。
    s 的 色标数 为 数量。
    s 的 平铺模式 为 平铺。
    s 的 矩阵 的 第 0 项 为 1024。
    s 的 矩阵 的 第 3 项 为 1024。
    返还 s。
完。

SkShader_径向渐变 接受 cx、cy、半径、颜色数组、位置数组、数量、平铺：
    s 为 做 堆_分配 用 SkShader。
    s 的 类型 为 SHADER_RADIAL。
    s 的 cx 为 cx。
    s 的 cy 为 cy。
    s 的 半径 为 半径。
    s 的 色标颜色 为 颜色数组。
    s 的 色标位置 为 位置数组。
    s 的 色标数 为 数量。
    s 的 平铺模式 为 平铺。
    s 的 矩阵 的 第 0 项 为 1024。
    s 的 矩阵 的 第 3 项 为 1024。
    返还 s。
完。

SkShader_采样 接受 shader、x、y：
    若 shader 等于 0：
        返还 0xFF000000。
    完。
    做 匹配 用 shader 的 类型。
完。

SkShader_平铺 接受 t、模式：
    做 匹配 用 模式。
完。

SkShader_插值色标 接受 shader、t_Q10：
    若 shader 的 色标数 等于 0：
        返还 0xFF000000。
    完。
    若 t_Q10 小于 0：
        返还 0x00000000。
    完。
    若 shader 的 色标数 等于 1：
        返还 做 内存_读32 用 shader 的 色标颜色。
    完。
    可变 i 为 0。
    只要 i 小于 shader 的 色标数 减 1：
        p0 为 做 内存_读32 用 shader 的 色标位置 加 i 乘 4。
        p1 为 做 内存_读32 用 shader 的 色标位置 加 (i 加 1) 乘 4。
        若 t_Q10 大于或等于 p0 并且 t_Q10 小于或等于 p1：
            范围 为 p1 减 p0。
            若 范围 等于 0：
                范围 为 1。
            完。
            比例 为 (t_Q10 减 p0) 乘 256 除以 范围。
            c0 为 做 内存_读32 用 shader 的 色标颜色 加 i 乘 4。
            c1 为 做 内存_读32 用 shader 的 色标颜色 加 (i 加 1) 乘 4。
            返还 做 SkColor_lerp 用 c0、c1、比例。
        完。
        i 为 i 加 1。
    完。
    返还 做 内存_读32 用 shader 的 色标颜色 加 (shader 的 色标数 减 1) 乘 4。
完。

SkColor_lerp 接受 c0、c1、t256：
    a0 为 c0 右移 24 位与 0xFF。
    r0 为 c0 右移 16 位与 0xFF。
    g0 为 c0 右移 8 位与 0xFF。
    b0 为 c0 位与 0xFF。
    a1 为 c1 右移 24 位与 0xFF。
    r1 为 c1 右移 16 位与 0xFF。
    g1 为 c1 右移 8 位与 0xFF。
    b1 为 c1 位与 0xFF。
    a 为 a0 加 (a1 减 a0) 乘 t256 除以 256。
    r 为 r0 加 (r1 减 r0) 乘 t256 除以 256。
    g 为 g0 加 (g1 减 g0) 乘 t256 除以 256。
    b 为 b0 加 (b1 减 b0) 乘 t256 除以 256。
    返还 a 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
完。

SkColor_从RGBA 接受 r、g、b、a：
    返还 a 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
完。

SkColor_预乘Alpha 接受 c：
    a 为 做 SkColor_A 用 c。
    若 a 等于 255：
        返还 c。
    完。
    若 a 等于 0：
        返还 0。
    完。
    r 为 做 SkColor_R 用 c 乘 a 除以 255。
    g 为 做 SkColor_G 用 c 乘 a 除以 255。
    b 为 做 SkColor_B 用 c 乘 a 除以 255。
    返还 a 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
完。

SkColor_混合 接受 src、dst：
    sa 为 做 SkColor_A 用 src。
    若 sa 等于 255：
        返还 src。
    完。
    若 sa 等于 0：
        返还 dst。
    完。
    da 为 255 减 sa。
    r 为 (做 SkColor_R 用 src 乘 sa 加 做 SkColor_R 用 dst 乘 da) 除以 255。
    g 为 (做 SkColor_G 用 src 乘 sa 加 做 SkColor_G 用 dst 乘 da) 除以 255。
    b 为 (做 SkColor_B 用 src 乘 sa 加 做 SkColor_B 用 dst 乘 da) 除以 255。
    a 为 sa 加 做 SkColor_A 用 dst 乘 da 除以 255。
    返还 a 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
完。

SkShader_扫线着色 接受 shader、y、x起始、x结束、输出缓冲：
    若 shader 等于 0 或者 输出缓冲 等于 0：
        返还。
    完。
    可变 x 为 x起始。
    只要 x 小于 x结束：
        颜色 为 做 SkShader_采样 用 shader、x、y。
        做 内存_写32 用 输出缓冲 加 (x 减 x起始) 乘 4、颜色。
        x 为 x 加 1。
    完。
完。

SkShader_纯色 接受 颜色：
    s 为 做 堆_分配 用 SkShader。
    s 的 类型 为 SHADER_SOLID。
    s 的 色标颜色 为 做 堆_分配 用 4。
    做 内存_写32 用 s 的 色标颜色、颜色。
    s 的 色标数 为 1。
    s 的 平铺模式 为 TILE_CLAMP。
    返还 s。
完。

SkShader_位图 接受 位图数据、宽、高、平铺：
    s 为 做 堆_分配 用 SkShader。
    s 的 类型 为 SHADER_BITMAP。
    s 的 色标颜色 为 位图数据。
    s 的 x0 为 宽。
    s 的 y0 为 高。
    s 的 平铺模式 为 平铺。
    返还 s。
完。

Sk数学_sqrt 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 r 为 x。
    可变 迭代 为 0。
    只要 迭代 小于 20：
        新r 为 (r 加 x 除以 r) 除以 2。
        若 新r 大于或等于 r：
            跳出。
        完。
        r 为 新r。
        迭代 为 迭代 加 1。
    完。
    返还 r。
完。

Sk数学_atan2 接受 y、x：
    若 x 等于 0 并且 y 等于 0：
        返还 0。
    完。
    若 x 大于 0：
        返还 y 乘 256 除以 (x 加 做 Sk数学_sqrt 用 x 乘 x 加 y 乘 y)。
    完。
    返还 512 减 x 乘 256 除以 (y 加 做 Sk数学_sqrt 用 x 乘 x 加 y 乘 y)。
完。

Sk数学_abs 接受 x：
    若 x 小于 0：
        返还 0 减 x。
    完。
    返还 x。
完。

Sk数学_clamp 接受 值、最小、最大：
    若 值 小于 最小：
        返还 最小。
    完。
    若 值 大于 最大：
        返还 最大。
    完。
    返还 值。
完。

可变 着色器_创建总数 为 0。

可变 着色器_采样总数 为 0。

着色器_统计 接受 零参：
    返还 一本字典：创建 对应 着色器_创建总数、采样 对应 着色器_采样总数。
完。

着色器_健康检查 接受 零参：
    返还 「着色器子系统健康」。
完。

着色器_重置 接受 零参：
    着色器_创建总数 为 0。
    着色器_采样总数 为 0。
完。

着色器_完整统计 接受 零参：
    返还 一组：着色器_创建总数、着色器_采样总数。
完。

着色器_类型名 接受 类型：
    若 类型 等于 SHADER_SOLID：
        返还 「solid」。
    完。
    若 类型 等于 SHADER_LINEAR：
        返还 「linear-gradient」。
    完。
    若 类型 等于 SHADER_RADIAL：
        返还 「radial-gradient」。
    完。
    若 类型 等于 SHADER_SWEEP：
        返还 「sweep-gradient」。
    完。
    若 类型 等于 SHADER_BITMAP：
        返还 「bitmap」。
    完。
    返还 「unknown」。
完。

平铺_名称 接受 模式：
    若 模式 等于 TILE_CLAMP：
        返还 「clamp」。
    完。
    若 模式 等于 TILE_REPEAT：
        返还 「repeat」。
    完。
    若 模式 等于 TILE_MIRROR：
        返还 「mirror」。
    完。
    若 模式 等于 TILE_DECAL：
        返还 「decal」。
    完。
    返还 「unknown」。
完。

可变 AI_网缓着色着_调用数 为 0。

可变 AI_网缓着色着_异常数 为 0。

AI_网缓着色着_监控 接受 零参：
    AI_网缓着色着_调用数 为 AI_网缓着色着_调用数 加 1。
    返还 AI_网缓着色着_异常数 等于 0。
完。

AI_网缓着色着_健康检查 接受 零参：
    若 AI_网缓着色着_调用数 大于 0 并且 AI_网缓着色着_异常数 乘 100 除以 AI_网缓着色着_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
