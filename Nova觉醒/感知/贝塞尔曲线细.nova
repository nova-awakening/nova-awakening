《贝塞尔曲线细》

引用《..·稳态·图处显示》。

引用《..·推理·通信总线》。

引用《.·内核直连层》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《..·执行·底层内存操》。

定点精度 为 100。

平坦度_二次阈值 为 100。

平坦度_三次阈值 为 400。

最大细分深度 为 10。

默认细分深度 为 8。

边缘池_初始容量 为 256。

边缘池_增长因子 为 2。

边缘_结构大小 为 32。

填充规则_奇偶 为 0。

填充规则_非零缠绕 为 1。

动词_移动 为 0。

动词_直线 为 1。

动词_二次曲线 为 2。

动词_三次曲线 为 3。

动词_关闭 为 4。

AI_简单曲线阈值 为 50。

AI_复杂曲线阈值 为 500。

边缘 是 一种 事物，含 x起始、y起始、y结束、x增量、方向、x当前。

边缘池 是 一种 事物，含 边缘基址、数量、容量、统计_直线边数、统计_曲线细分数、统计_总细分递归数。

贝塞尔管理器 是 一种 事物，含 边缘池引用、AI最大深度、AI平坦度系数、统计_二次细分数、统计_三次细分数、统计_总输出边数、统计_AI跳过数、统计_最深递归。

边缘池_创建 接受 初始容量：
    可变 池 为 做 堆_分配 用 边缘池。
    若 池 等于 0：
        做 打印 用 「[贝塞尔] 错误: 边缘池分配失败」。
        返还 空。
    完。
    可变 实际容量 为 初始容量。
    若 实际容量 小于 边缘池_初始容量：
        实际容量 为 边缘池_初始容量。
    完。
    池 的 容量 为 实际容量。
    池 的 边缘基址 为 做 平台_申请内存 用 实际容量 乘 边缘_结构大小。
    若 池 的 边缘基址 等于 0：
        做 打印 用 「[贝塞尔] 错误: 边缘数组分配失败」。
        做 堆_释放 用 池。
        返还 空。
    完。
    做 内存_清零 用 池 的 边缘基址、实际容量 乘 边缘_结构大小。
    池 的 数量 为 0。
    池 的 统计_直线边数 为 0。
    池 的 统计_曲线细分数 为 0。
    池 的 统计_总细分递归数 为 0。
    返还 池。
完。

边缘池_销毁 接受 池：
    若 池 等于 空：
        返还。
    完。
    若 池 的 边缘基址 不等于 0：
        做 平台_释放内存 用 池 的 边缘基址、池 的 容量 乘 边缘_结构大小。
    完。
    做 堆_释放 用 池。
完。

边缘池_添加 接受 池、x0、y0、x1、y1：
    若 池 等于 空：
        返还。
    完。
    若 y0 等于 y1：
        返还。
    完。
    若 池 的 数量 大于或等于 池 的 容量：
        做 边缘池_扩容 用 池。
        若 池 的 数量 大于或等于 池 的 容量：
            返还。
        完。
    完。
    偏移 为 池 的 边缘基址 加 池 的 数量 乘 边缘_结构大小。
    可变 方向 为 1。
    可变 实际x0 为 x0。
    可变 实际y0 为 y0。
    可变 实际x1 为 x1。
    可变 实际y1 为 y1。
    若 y0 大于 y1：
        实际x0 为 x1。
        实际y0 为 y1。
        实际x1 为 x0。
        实际y1 为 y0。
        方向 为 负 1。
    完。
    像素y0 为 实际y0 除以 定点精度。
    像素y1 为 实际y1 除以 定点精度。
    若 像素y0 等于 像素y1：
        返还。
    完。
    dy 为 实际y1 减 实际y0。
    可变 x增量 为 0。
    若 dy 不等于 0：
        x增量 为 (实际x1 减 实际x0) 乘 定点精度 除以 dy。
    完。
    做 内存_写32 用 偏移、实际x0。
    做 内存_写32 用 偏移 加 4、实际x0。
    做 内存_写32 用 偏移 加 8、像素y0。
    做 内存_写32 用 偏移 加 12、像素y1。
    做 内存_写32 用 偏移 加 16、x增量。
    做 内存_写32 用 偏移 加 20、方向。
    池 的 数量 为 池 的 数量 加 1。
    池 的 统计_直线边数 为 池 的 统计_直线边数 加 1。
完。

边缘池_扩容 接受 池：
    新容量 为 池 的 容量 乘 边缘池_增长因子。
    新内存 为 做 平台_申请内存 用 新容量 乘 边缘_结构大小。
    若 新内存 等于 0：
        返还。
    完。
    做 内存_复制 用 新内存、池 的 边缘基址、池 的 数量 乘 边缘_结构大小。
    做 平台_释放内存 用 池 的 边缘基址、池 的 容量 乘 边缘_结构大小。
    池 的 边缘基址 为 新内存。
    池 的 容量 为 新容量。
完。

边缘池_清空 接受 池：
    若 池 等于 空：
        返还。
    完。
    池 的 数量 为 0。
完。

贝塞尔管理器_创建 接受 零参：
    可变 管理器 为 做 堆_分配 用 贝塞尔管理器。
    若 管理器 等于 0：
        做 打印 用 「[贝塞尔] 错误: 管理器分配失败」。
        返还 空。
    完。
    管理器 的 边缘池引用 为 0。
    管理器 的 AI最大深度 为 默认细分深度。
    管理器 的 AI平坦度系数 为 100。
    管理器 的 统计_二次细分数 为 0。
    管理器 的 统计_三次细分数 为 0。
    管理器 的 统计_总输出边数 为 0。
    管理器 的 统计_AI跳过数 为 0。
    管理器 的 统计_最深递归 为 0。
    返还 管理器。
完。

贝塞尔管理器_销毁 接受 管理器：
    若 管理器 等于 空：
        返还。
    完。
    做 堆_释放 用 管理器。
完。

贝塞尔_二次细分 接受 池、x0、y0、cx、cy、x1、y1、深度：
    若 池 等于 空：
        返还。
    完。
    池 的 统计_总细分递归数 为 池 的 统计_总细分递归数 加 1。
    mx 为 (x0 加 x1) 除以 2。
    my 为 (y0 加 y1) 除以 2。
    dx 为 cx 减 mx。
    dy 为 cy 减 my。
    距离平方 为 dx 乘 dx 加 dy 乘 dy。
    阈值 为 平坦度_二次阈值。
    若 距离平方 小于或等于 阈值 或者 深度 大于或等于 默认细分深度：
        做 边缘池_添加 用 池、x0、y0、x1、y1。
        池 的 统计_曲线细分数 为 池 的 统计_曲线细分数 加 1。
        返还。
    完。
    ax 为 (x0 加 cx) 除以 2。
    ay 为 (y0 加 cy) 除以 2。
    bx 为 (cx 加 x1) 除以 2。
    by 为 (cy 加 y1) 除以 2。
    abx 为 (ax 加 bx) 除以 2。
    aby 为 (ay 加 by) 除以 2。
    做 贝塞尔_二次细分 用 池、x0、y0、ax、ay、abx、aby、深度 加 1。
    做 贝塞尔_二次细分 用 池、abx、aby、bx、by、x1、y1、深度 加 1。
完。

贝塞尔_三次细分 接受 池、x0、y0、c1x、c1y、c2x、c2y、x1、y1、深度：
    若 池 等于 空：
        返还。
    完。
    池 的 统计_总细分递归数 为 池 的 统计_总细分递归数 加 1。
    dx1 为 c1x 减 (x0 乘 2 加 x1) 除以 3。
    dy1 为 c1y 减 (y0 乘 2 加 y1) 除以 3。
    dx2 为 c2x 减 (x0 加 x1 乘 2) 除以 3。
    dy2 为 c2y 减 (y0 加 y1 乘 2) 除以 3。
    d1 为 dx1 乘 dx1 加 dy1 乘 dy1。
    d2 为 dx2 乘 dx2 加 dy2 乘 dy2。
    可变 最大d 为 d1。
    若 d2 大于 最大d：
        最大d 为 d2。
    完。
    若 最大d 小于或等于 平坦度_三次阈值 或者 深度 大于或等于 默认细分深度：
        做 边缘池_添加 用 池、x0、y0、x1、y1。
        池 的 统计_曲线细分数 为 池 的 统计_曲线细分数 加 1。
        返还。
    完。
    ab_x 为 (x0 加 c1x) 除以 2。
    ab_y 为 (y0 加 c1y) 除以 2。
    bc_x 为 (c1x 加 c2x) 除以 2。
    bc_y 为 (c1y 加 c2y) 除以 2。
    cd_x 为 (c2x 加 x1) 除以 2。
    cd_y 为 (c2y 加 y1) 除以 2。
    abc_x 为 (ab_x 加 bc_x) 除以 2。
    abc_y 为 (ab_y 加 bc_y) 除以 2。
    bcd_x 为 (bc_x 加 cd_x) 除以 2。
    bcd_y 为 (bc_y 加 cd_y) 除以 2。
    abcd_x 为 (abc_x 加 bcd_x) 除以 2。
    abcd_y 为 (abc_y 加 bcd_y) 除以 2。
    做 贝塞尔_三次细分 用 池、x0、y0、ab_x、ab_y、abc_x、abc_y、abcd_x、abcd_y、深度 加 1。
    做 贝塞尔_三次细分 用 池、abcd_x、abcd_y、bcd_x、bcd_y、cd_x、cd_y、x1、y1、深度 加 1。
完。

贝塞尔_二次求值_x 接受 x0、cx、x1、t：
    inv_t 为 10000 减 t。
    结果 为 inv_t 乘 inv_t 乘 x0 除以 100000000。
    结果 为 结果 加 2 乘 inv_t 乘 t 乘 cx 除以 100000000。
    结果 为 结果 加 t 乘 t 乘 x1 除以 100000000。
    返还 结果。
完。

贝塞尔_二次求值_y 接受 y0、cy、y1、t：
    inv_t 为 10000 减 t。
    结果 为 inv_t 乘 inv_t 乘 y0 除以 100000000。
    结果 为 结果 加 2 乘 inv_t 乘 t 乘 cy 除以 100000000。
    结果 为 结果 加 t 乘 t 乘 y1 除以 100000000。
    返还 结果。
完。

贝塞尔_三次求值_x 接受 x0、c1x、c2x、x1、t：
    inv_t 为 10000 减 t。
    t2 为 t 乘 t 除以 10000。
    inv_t2 为 inv_t 乘 inv_t 除以 10000。
    结果 为 inv_t2 乘 inv_t 乘 x0 除以 100000000。
    结果 为 结果 加 3 乘 inv_t2 乘 t 乘 c1x 除以 100000000。
    结果 为 结果 加 3 乘 inv_t 乘 t2 乘 c2x 除以 100000000。
    结果 为 结果 加 t2 乘 t 乘 x1 除以 100000000。
    返还 结果。
完。

贝塞尔_二次包围盒 接受 x0、y0、cx、cy、x1、y1、结果盒：
    若 结果盒 等于 0：
        返还。
    完。
    可变 最小x 为 x0。
    可变 最小y 为 y0。
    可变 最大x 为 x0。
    可变 最大y 为 y0。
    若 cx 小于 最小x：
        最小x 为 cx。
    完。
    若 cy 小于 最小y：
        最小y 为 cy。
    完。
    若 cx 大于 最大x：
        最大x 为 cx。
    完。
    若 cy 大于 最大y：
        最大y 为 cy。
    完。
    若 x1 小于 最小x：
        最小x 为 x1。
    完。
    若 y1 小于 最小y：
        最小y 为 y1。
    完。
    若 x1 大于 最大x：
        最大x 为 x1。
    完。
    若 y1 大于 最大y：
        最大y 为 y1。
    完。
    做 内存_写32 用 结果盒、最小x。
    做 内存_写32 用 结果盒 加 4、最小y。
    做 内存_写32 用 结果盒 加 8、最大x。
    做 内存_写32 用 结果盒 加 12、最大y。
完。

贝塞尔_三次包围盒 接受 x0、y0、c1x、c1y、c2x、c2y、x1、y1、结果盒：
    若 结果盒 等于 0：
        返还。
    完。
    可变 最小x 为 x0。
    可变 最小y 为 y0。
    可变 最大x 为 x0。
    可变 最大y 为 y0。
    若 c1x 小于 最小x：
        最小x 为 c1x。
    完。
    若 c1y 小于 最小y：
        最小y 为 c1y。
    完。
    若 c1x 大于 最大x：
        最大x 为 c1x。
    完。
    若 c1y 大于 最大y：
        最大y 为 c1y。
    完。
    若 c2x 小于 最小x：
        最小x 为 c2x。
    完。
    若 c2y 小于 最小y：
        最小y 为 c2y。
    完。
    若 c2x 大于 最大x：
        最大x 为 c2x。
    完。
    若 c2y 大于 最大y：
        最大y 为 c2y。
    完。
    若 x1 小于 最小x：
        最小x 为 x1。
    完。
    若 y1 小于 最小y：
        最小y 为 y1。
    完。
    若 x1 大于 最大x：
        最大x 为 x1。
    完。
    若 y1 大于 最大y：
        最大y 为 y1。
    完。
    做 内存_写32 用 结果盒、最小x。
    做 内存_写32 用 结果盒 加 4、最小y。
    做 内存_写32 用 结果盒 加 8、最大x。
    做 内存_写32 用 结果盒 加 12、最大y。
完。

扫描线_填充路径 接受 帧缓冲、步幅、宽、高、池、填充色、填充规则：
    若 池 等于 空 或者 池 的 数量 等于 0 或者 帧缓冲 等于 0：
        返还。
    完。
    可变 y_min 为 999999。
    可变 y_max 为 负 999999。
    可变 i 为 0。
    只要 i 小于 池 的 数量：
        偏移 为 池 的 边缘基址 加 i 乘 边缘_结构大小。
        yt 为 做 内存_读32 用 偏移 加 8。
        yb 为 做 内存_读32 用 偏移 加 12。
        若 yt 小于 y_min：
            y_min 为 yt。
        完。
        若 yb 大于 y_max：
            y_max 为 yb。
        完。
        i 为 i 加 1。
    完。
    若 y_min 小于 0：
        y_min 为 0。
    完。
    若 y_max 大于 高：
        y_max 为 高。
    完。
    若 y_min 大于或等于 y_max：
        返还。
    完。
    活跃边容量 为 池 的 数量。
    活跃边列表 为 做 平台_申请内存 用 活跃边容量 乘 8。
    若 活跃边列表 等于 0：
        返还。
    完。
    可变 活跃边数 为 0。
    可变 y 为 y_min。
    只要 y 小于 y_max：
        可变 j 为 0。
        只要 j 小于 池 的 数量：
            偏移 为 池 的 边缘基址 加 j 乘 边缘_结构大小。
            yt 为 做 内存_读32 用 偏移 加 8。
            若 yt 等于 y 并且 活跃边数 小于 活跃边容量：
                x起始 为 做 内存_读32 用 偏移。
                做 内存_写32 用 偏移 加 4、x起始。
                做 内存_写64 用 活跃边列表 加 活跃边数 乘 8、偏移。
                活跃边数 为 活跃边数 加 1。
            完。
            j 为 j 加 1。
        完。
        可变 k 为 0。
        只要 k 小于 活跃边数：
            边偏移 为 做 内存_读64 用 活跃边列表 加 k 乘 8。
            yb 为 做 内存_读32 用 边偏移 加 12。
            若 y 大于或等于 yb：
                活跃边数 为 活跃边数 减 1。
                若 k 小于 活跃边数：
                    做 内存_写64 用 活跃边列表 加 k 乘 8、做 内存_读64 用 活跃边列表 加 活跃边数 乘 8。
                完。
            否则：
                k 为 k 加 1。
            完。
        完。
        做 扫描线_排序活跃边 用 活跃边列表、活跃边数。
        若 填充规则 等于 填充规则_奇偶：
            做 扫描线_奇偶填充 用 帧缓冲、步幅、宽、y、活跃边列表、活跃边数、填充色。
        否则：
            做 扫描线_缠绕填充 用 帧缓冲、步幅、宽、y、活跃边列表、活跃边数、填充色。
        完。
        可变 m 为 0。
        只要 m 小于 活跃边数：
            边偏移 为 做 内存_读64 用 活跃边列表 加 m 乘 8。
            x当前 为 做 内存_读32 用 边偏移 加 4。
            x增量 为 做 内存_读32 用 边偏移 加 16。
            做 内存_写32 用 边偏移 加 4、x当前 加 x增量。
            m 为 m 加 1。
        完。
        y 为 y 加 1。
    完。
    做 平台_释放内存 用 活跃边列表、活跃边容量 乘 8。
完。

扫描线_排序活跃边 接受 列表、数量：
    可变 i 为 1。
    只要 i 小于 数量：
        当前 为 做 内存_读64 用 列表 加 i 乘 8。
        当前x 为 做 内存_读32 用 当前 加 4。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0：
            前一个 为 做 内存_读64 用 列表 加 j 乘 8。
            前x 为 做 内存_读32 用 前一个 加 4。
            若 前x 大于 当前x：
                做 内存_写64 用 列表 加 (j 加 1) 乘 8、前一个。
                j 为 j 减 1。
            否则：
                跳出。
            完。
        完。
        做 内存_写64 用 列表 加 (j 加 1) 乘 8、当前。
        i 为 i 加 1。
    完。
完。

扫描线_奇偶填充 接受 帧缓冲、步幅、宽、y、列表、数量、颜色：
    可变 i 为 0。
    只要 i 加 1 小于 数量：
        边1 为 做 内存_读64 用 列表 加 i 乘 8。
        边2 为 做 内存_读64 用 列表 加 (i 加 1) 乘 8。
        x1 为 做 内存_读32 用 边1 加 4 除以 定点精度。
        x2 为 做 内存_读32 用 边2 加 4 除以 定点精度。
        若 x1 小于 0：
            x1 为 0。
        完。
        若 x2 大于 宽：
            x2 为 宽。
        完。
        行基址 为 帧缓冲 加 y 乘 步幅。
        可变 x 为 x1。
        只要 x 小于 x2：
            做 内存_写32 用 行基址 加 x 乘 4、颜色。
            x 为 x 加 1。
        完。
        i 为 i 加 2。
    完。
完。

扫描线_缠绕填充 接受 帧缓冲、步幅、宽、y、列表、数量、颜色：
    可变 缠绕数 为 0。
    可变 i 为 0。
    只要 i 小于 数量：
        边 为 做 内存_读64 用 列表 加 i 乘 8。
        方向 为 做 内存_读32 用 边 加 20。
        x当前 为 做 内存_读32 用 边 加 4 除以 定点精度。
        旧缠绕 为 缠绕数。
        缠绕数 为 缠绕数 加 方向。
        若 旧缠绕 等于 0 并且 缠绕数 不等于 0：
        否则若 旧缠绕 不等于 0 并且 缠绕数 等于 0：
            若 i 大于 0：
                前边 为 做 内存_读64 用 列表 加 (i 减 1) 乘 8。
                可变 起始x 为 做 内存_读32 用 前边 加 4 除以 定点精度。
                若 起始x 小于 0：
                    起始x 为 0。
                完。
                若 x当前 大于 宽：
                    x当前 为 宽。
                完。
                行基址 为 帧缓冲 加 y 乘 步幅。
                可变 x 为 起始x。
                只要 x 小于 x当前：
                    做 内存_写32 用 行基址 加 x 乘 4、颜色。
                    x 为 x 加 1。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

贝塞尔_AI更新参数 接受 管理器、帧耗时：
    若 管理器 等于 空：
        返还。
    完。
    若 帧耗时 大于 16000：
        若 管理器 的 AI最大深度 大于 4：
            管理器 的 AI最大深度 为 管理器 的 AI最大深度 减 1。
        完。
        管理器 的 AI平坦度系数 为 管理器 的 AI平坦度系数 加 20。
    否则若 帧耗时 小于 8000：
        若 管理器 的 AI最大深度 小于 最大细分深度：
            管理器 的 AI最大深度 为 管理器 的 AI最大深度 加 1。
        完。
        若 管理器 的 AI平坦度系数 大于 50：
            管理器 的 AI平坦度系数 为 管理器 的 AI平坦度系数 减 10。
        完。
    完。
完。

边缘池_打印统计 接受 池：
    若 池 等于 空：
        返还。
    完。
    做 打印 用 「[贝塞尔] ═══════════ 边缘池统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  边缘数:         」、做 转字符串 用 池 的 数量。
    做 打印 用 做 字符串拼接 用 「  容量:           」、做 转字符串 用 池 的 容量。
    做 打印 用 做 字符串拼接 用 「  直线边数:       」、做 转字符串 用 池 的 统计_直线边数。
    做 打印 用 做 字符串拼接 用 「  曲线细分数:     」、做 转字符串 用 池 的 统计_曲线细分数。
    做 打印 用 做 字符串拼接 用 「  总递归调用数:   」、做 转字符串 用 池 的 统计_总细分递归数。
    做 打印 用 「[贝塞尔] ═══════════════════════════════════════」。
完。

贝塞尔管理器_打印统计 接受 管理器：
    若 管理器 等于 空：
        返还。
    完。
    做 打印 用 「[贝塞尔] ── 管理器统计 ──」。
    做 打印 用 做 字符串拼接 用 「  二次细分数:     」、做 转字符串 用 管理器 的 统计_二次细分数。
    做 打印 用 做 字符串拼接 用 「  三次细分数:     」、做 转字符串 用 管理器 的 统计_三次细分数。
    做 打印 用 做 字符串拼接 用 「  总输出边数:     」、做 转字符串 用 管理器 的 统计_总输出边数。
    做 打印 用 做 字符串拼接 用 「  AI最大深度:     」、做 转字符串 用 管理器 的 AI最大深度。
    做 打印 用 做 字符串拼接 用 「  AI平坦度系数:   」、做 转字符串 用 管理器 的 AI平坦度系数。
完。

本章 收束。
