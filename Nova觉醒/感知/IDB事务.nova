《IDB事务》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·IndexedDB客户端》。

IDB_transaction 接受 db、存储名列表、存储数、模式：
    若 db 等于 0：
        返还 0。
    完。
    tx 为 做 堆_分配 用 IDBTransaction。
    tx 的 数据库 为 db。
    tx 的 模式 为 模式。
    tx 的 作用域 为 存储名列表。
    tx 的 作用域数 为 存储数。
    tx 的 已提交 为 假。
    tx 的 已中止 为 假。
    tx 的 操作队列 为 0。
    tx 的 oncomplete 为 0。
    tx 的 onerror 为 0。
    tx 的 onabort 为 0。
    返还 tx。
完。

IDB_commit 接受 tx：
    若 tx 等于 0 或者 tx 的 已中止：
        返还。
    完。
    tx 的 已提交 为 真。
    若 tx 的 oncomplete 不等于 0：
    完。
完。

IDB_abort 接受 tx：
    若 tx 等于 0 或者 tx 的 已提交：
        返还。
    完。
    tx 的 已中止 为 真。
    若 tx 的 onabort 不等于 0：
    完。
完。

BPlus_创建叶子 接受 零参：
    n 为 做 堆_分配 用 BPlus节点。
    n 的 是叶子 为 真。
    n 的 键数 为 0。
    n 的 下一叶子 为 0。
    返还 n。
完。

BPlus_查找 接受 根、键：
    若 根 等于 0 或者 键 等于 0：
        返还 0。
    完。
    可变 节点 为 根。
    只要 非 节点 的 是叶子：
        可变 i 为 0。
        只要 i 小于 节点 的 键数 并且 做 IDBKey_比较 用 键、节点 的 键 的 第 i 项 大于 0：
            i 为 i 加 1。
        完。
        节点 为 节点 的 子节点 的 第 i 项。
        若 节点 等于 0：
            返还 0。
        完。
    完。
    可变 i 为 0。
    只要 i 小于 节点 的 键数：
        若 做 IDBKey_比较 用 键、节点 的 键 的 第 i 项 等于 0：
            返还 节点 的 值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

BPlus_插入 接受 根、键、值：
    若 根 等于 0 或者 键 等于 0：
        返还。
    完。
    可变 节点 为 根。
    只要 非 节点 的 是叶子：
        可变 i 为 0。
        只要 i 小于 节点 的 键数 并且 做 IDBKey_比较 用 键、节点 的 键 的 第 i 项 大于 0：
            i 为 i 加 1。
        完。
        若 节点 的 子节点 的 第 i 项 等于 0：
            返还。
        完。
        节点 为 节点 的 子节点 的 第 i 项。
    完。
    可变 i 为 0。
    只要 i 小于 节点 的 键数：
        若 做 IDBKey_比较 用 键、节点 的 键 的 第 i 项 等于 0：
            节点 的 值 的 第 i 项 为 值。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 节点 的 键数 大于或等于 32：
        返还。
    完。
    i 为 节点 的 键数。
    只要 i 大于 0 并且 做 IDBKey_比较 用 键、节点 的 键 的 第 i 减 1 项 小于 0：
        节点 的 键 的 第 i 项 为 节点 的 键 的 第 i 减 1 项。
        节点 的 值 的 第 i 项 为 节点 的 值 的 第 i 减 1 项。
        i 为 i 减 1。
    完。
    节点 的 键 的 第 i 项 为 键。
    节点 的 值 的 第 i 项 为 值。
    节点 的 键数 为 节点 的 键数 加 1。
完。

BPlus_删除 接受 根、键：
    若 根 等于 0 或者 键 等于 0：
        返还 假。
    完。
    可变 节点 为 根。
    只要 非 节点 的 是叶子：
        可变 i 为 0。
        只要 i 小于 节点 的 键数 并且 做 IDBKey_比较 用 键、节点 的 键 的 第 i 项 大于 0：
            i 为 i 加 1。
        完。
        节点 为 节点 的 子节点 的 第 i 项。
        若 节点 等于 0：
            返还 假。
        完。
    完。
    可变 i 为 0。
    只要 i 小于 节点 的 键数：
        若 做 IDBKey_比较 用 键、节点 的 键 的 第 i 项 等于 0：
            可变 j 为 i。
            只要 j 小于 节点 的 键数 减 1：
                节点 的 键 的 第 j 项 为 节点 的 键 的 第 j 加 1 项。
                节点 的 值 的 第 j 项 为 节点 的 值 的 第 j 加 1 项。
                j 为 j 加 1。
            完。
            节点 的 键数 为 节点 的 键数 减 1。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

本章 收束。
