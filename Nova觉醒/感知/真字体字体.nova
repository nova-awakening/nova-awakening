《真字体字体》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·头部查水平》。

引用《.·内核直连层》。

TrueType字体 是 一种 事物，含 数据、大小、表数、head_偏移、head_大小、hhea_偏移、maxp_偏移、cmap_偏移、cmap_大小、loca_偏移、loca_大小、glyf_偏移、glyf_大小、hmtx_偏移、kern_偏移、os2_偏移、name_偏移、post_偏移、upem、loca格式、标志、上升、下降、行距、最大宽度、水平度量数、字形总数、cmap子表偏移、cmap子表格式、字形缓存、缓存容量。

读16 接受 数据、偏移：
    返还 做 内存_读8 用 数据 加 偏移 左移 8 位或 做 内存_读8 用 数据 加 偏移 加 1。
完。

读32 接受 数据、偏移：
    返还 做 内存_读8 用 数据 加 偏移 左移 24 位或 做 内存_读8 用 数据 加 偏移 加 1 左移 16 位或 做 内存_读8 用 数据 加 偏移 加 2 左移 8 位或 做 内存_读8 用 数据 加 偏移 加 3。
完。

读i16 接受 数据、偏移：
    v 为 做 读16 用 数据、偏移。
    若 v 大于或等于 0x8000：
        返还 v 减 0x10000。
    完。
    返还 v。
完。

表标签 接受 a、b、c、d：
    返还 a 左移 24 位或 b 左移 16 位或 c 左移 8 位或 d。
完。

TrueType_解析表目录 接受 font：
    data 为 font 的 数据。
    表数 为 做 读16 用 data、4。
    font 的 表数 为 表数。
    可变 i 为 0。
    只要 i 小于 表数：
        entry 为 12 加 i 乘 16。
        tag 为 做 读32 用 data、entry。
        偏移 为 做 读32 用 data、entry 加 8。
        大小 为 做 读32 用 data、entry 加 12。
        若 tag 等于 做 表标签 用 0x68、0x65、0x61、0x64：
            font 的 head_偏移 为 偏移。
            font 的 head_大小 为 大小。
        否则若 tag 等于 做 表标签 用 0x68、0x68、0x65、0x61：
            font 的 hhea_偏移 为 偏移。
        否则若 tag 等于 做 表标签 用 0x6D、0x61、0x78、0x70：
            font 的 maxp_偏移 为 偏移。
        否则若 tag 等于 做 表标签 用 0x63、0x6D、0x61、0x70：
            font 的 cmap_偏移 为 偏移。
            font 的 cmap_大小 为 大小。
        否则若 tag 等于 做 表标签 用 0x6C、0x6F、0x63、0x61：
            font 的 loca_偏移 为 偏移。
            font 的 loca_大小 为 大小。
        否则若 tag 等于 做 表标签 用 0x67、0x6C、0x79、0x66：
            font 的 glyf_偏移 为 偏移。
            font 的 glyf_大小 为 大小。
        否则若 tag 等于 做 表标签 用 0x68、0x6D、0x74、0x78：
            font 的 hmtx_偏移 为 偏移。
        否则若 tag 等于 做 表标签 用 0x6B、0x65、0x72、0x6E：
            font 的 kern_偏移 为 偏移。
        否则若 tag 等于 做 表标签 用 0x4F、0x53、0x2F、0x32：
            font 的 os2_偏移 为 偏移。
        否则若 tag 等于 做 表标签 用 0x6E、0x61、0x6D、0x65：
            font 的 name_偏移 为 偏移。
        否则若 tag 等于 做 表标签 用 0x70、0x6F、0x73、0x74：
            font 的 post_偏移 为 偏移。
        完。
        i 为 i 加 1。
    完。
    做 TrueType_解析head 用 font。
    做 TrueType_解析hhea 用 font。
    做 TrueType_解析maxp 用 font。
    做 TrueType_解析cmap 用 font。
    font 的 缓存容量 为 256。
    font 的 字形缓存 为 做 堆_分配 用 font 的 缓存容量 乘 8。
    做 内存_置零 用 font 的 字形缓存、font 的 缓存容量 乘 8。
完。

本章 收束。
