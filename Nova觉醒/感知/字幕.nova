《字幕》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·字符串替换》。

引用《.·时间范围》。

引用《.·文档事件》。

引用《..·中枢·内容可见性》。

TEXT_TRACK_SHOWING 为 2。

TEXT_TRACK_HIDDEN 为 1。

TEXT_TRACK_DISABLED 为 0。

TEXT_KIND_SUBTITLES 为 0。

TEXT_KIND_CAPTIONS 为 1。

VTT_最大Cue数 为 4096。

VTT_最大区域数 为 16。

MSE_OPEN 为 0。

MSE_CLOSED 为 1。

MSE_ENDED 为 2。

MSE_最大缓冲区 为 8。

MSE_最大追加大小 为 10485760。

字幕统计 是 一种 事物，含 VTT解析次数、Cue解析次数、时间戳解析次数、字幕更新次数、活跃Cue计算次数、字幕渲染次数、MSE创建次数、SourceBuffer创建次数、追加操作次数、追加字节总数、删除操作次数、endOfStream次数、AI语言调用次数、AI语言命中次数、总处理耗时_纳秒、错误总数、解析错误次数、溢出保护次数。

AI字幕状态 是 一种 事物，含 历史字幕语言、历史有字幕率、历史平均Cue数、历史平均Cue时长、连续有字幕计数、预测有字幕、采样计数器、学习权重。

可变 全局字幕统计 为 0。

可变 全局字幕AI 为 0。

可变 全局字幕已初始化 为 假。

字幕_初始化 接受 零参：
    若 全局字幕已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局字幕统计 为 做 堆_分配_清零 用 144。
    若 全局字幕统计 等于 空：
        返还 错误_内存不足。
    完。
    全局字幕AI 为 做 堆_分配_清零 用 40。
    若 全局字幕AI 不等于 空：
        全局字幕AI 的 学习权重 为 128。
    完。
    全局字幕已初始化 为 真。
    做 打印 用 「[字幕] 初始化完成」。
    返还 0。
完。

字幕_销毁 接受 零参：
    若 全局字幕统计 不等于 空：
        做 堆_释放 用 全局字幕统计。
        全局字幕统计 为 0。
    完。
    若 全局字幕AI 不等于 空：
        做 堆_释放 用 全局字幕AI。
        全局字幕AI 为 0。
    完。
    全局字幕已初始化 为 假。
    返还 0。
完。

字幕_重置 接受 零参：
    若 全局字幕统计 不等于 空：
        做 内存_置零 用 全局字幕统计、144。
    完。
    返还 0。
完。

媒体元素_更新字幕 接受 me：
    若 me 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 me 的 文本轨道数：
        track 为 me 的 文本轨道 的 第 i 项。
        若 track 等于 0 或者 track 的 mode 不等于 TEXT_TRACK_SHOWING：
            i 为 i 加 1。
            继续。
        完。
        track 的 活跃数 为 0。
        可变 cue 为 track 的 cue头。
        只要 cue 不等于 0：
            若 me 的 currentTime 大于或等于 cue 的 开始时间 并且 me 的 currentTime 小于 cue 的 结束时间：
                track 的 活跃数 为 track 的 活跃数 加 1。
            完。
            cue 为 cue 的 下一个。
        完。
        i 为 i 加 1。
    完。
完。

WebVTT_解析 接受 数据、长度：
    若 数据 等于 0 或者 长度 等于 0：
        返还 0。
    完。
    track 为 做 堆_分配 用 TextTrack。
    track 的 kind 为 TEXT_KIND_SUBTITLES。
    track 的 mode 为 TEXT_TRACK_HIDDEN。
    track 的 cue头 为 0。
    track 的 cue数 为 0。
    返还 track。
完。

媒体元素_addTextTrack 接受 me、kind、label、language：
    若 me 等于 0 或者 me 的 文本轨道数 大于或等于 16：
        返还 0。
    完。
    track 为 做 堆_分配 用 TextTrack。
    track 的 kind 为 kind。
    track 的 label 为 label。
    track 的 language 为 language。
    track 的 mode 为 TEXT_TRACK_HIDDEN。
    track 的 cue头 为 0。
    track 的 cue数 为 0。
    me 的 文本轨道 的 第 me 的 文本轨道数 项 为 track。
    me 的 文本轨道数 为 me 的 文本轨道数 加 1。
    返还 track。
完。

MSE_OPEN 为 0。

MSE_CLOSED 为 1。

MSE_ENDED 为 2。

媒体Source 是 一种 事物，含 状态、时长、缓冲区、缓冲区数、元素。

SourceBuffer 是 一种 事物，含 MIME类型、追加缓冲、缓冲大小、已缓冲、模式、正在更新、onupdateend。

媒体Source_创建 接受 零参：
    ms 为 做 堆_分配 用 媒体Source。
    ms 的 状态 为 MSE_CLOSED。
    ms 的 时长 为 负 1。
    ms 的 缓冲区数 为 0。
    ms 的 元素 为 0。
    返还 ms。
完。

媒体Source_addSourceBuffer 接受 ms、mimeType：
    若 ms 等于 0 或者 ms 的 状态 不等于 MSE_OPEN 或者 ms 的 缓冲区数 大于或等于 8：
        返还 0。
    完。
    sb 为 做 堆_分配 用 SourceBuffer。
    sb 的 MIME类型 为 mimeType。
    sb 的 追加缓冲 为 0。
    sb 的 缓冲大小 为 0。
    sb 的 已缓冲 为 做 TimeRanges_创建。
    sb 的 模式 为 0。
    sb 的 正在更新 为 假。
    sb 的 onupdateend 为 0。
    ms 的 缓冲区 的 第 ms 的 缓冲区数 项 为 sb。
    ms 的 缓冲区数 为 ms 的 缓冲区数 加 1。
    返还 sb。
完。

SourceBuffer_appendBuffer 接受 sb、数据、大小：
    若 sb 等于 0 或者 sb 的 正在更新：
        返还。
    完。
    sb 的 正在更新 为 真。
完。

媒体Source_endOfStream 接受 ms：
    若 ms 等于 0：
        返还。
    完。
    ms 的 状态 为 MSE_ENDED。
    若 全局字幕统计 不等于 空：
        全局字幕统计 的 endOfStream次数 为 全局字幕统计 的 endOfStream次数 加 1。
    完。
完。

VTT_解析时间戳 接受 文本、位置：
    若 文本 等于 空：
        返还 0。
    完。
    长度 为 做 字节长度 用 文本。
    若 位置 大于或等于 长度：
        返还 0。
    完。
    可变 p 为 位置。
    只要 p 小于 长度 并且 (做 字节取 用 文本、p 等于 32 或者 做 字节取 用 文本、p 等于 9)：
        p 为 p 加 1。
    完。
    可变 n1 为 0。
    只要 p 小于 长度 并且 做 字节取 用 文本、p 大于或等于 48 并且 做 字节取 用 文本、p 小于或等于 57：
        n1 为 n1 乘 10 加 (做 字节取 用 文本、p 减 48)。
        p 为 p 加 1。
    完。
    若 p 大于或等于 长度 或者 做 字节取 用 文本、p 不等于 58：
        返还 n1 乘 1000。
    完。
    p 为 p 加 1。
    可变 n2 为 0。
    只要 p 小于 长度 并且 做 字节取 用 文本、p 大于或等于 48 并且 做 字节取 用 文本、p 小于或等于 57：
        n2 为 n2 乘 10 加 (做 字节取 用 文本、p 减 48)。
        p 为 p 加 1。
    完。
    可变 小时 为 0。
    可变 分钟 为 n1。
    可变 秒 为 n2。
    若 p 小于 长度 并且 做 字节取 用 文本、p 等于 58：
        p 为 p 加 1。
        小时 为 n1。
        分钟 为 n2。
        秒 为 0。
        只要 p 小于 长度 并且 做 字节取 用 文本、p 大于或等于 48 并且 做 字节取 用 文本、p 小于或等于 57：
            秒 为 秒 乘 10 加 (做 字节取 用 文本、p 减 48)。
            p 为 p 加 1。
        完。
    完。
    可变 毫秒 为 0。
    若 p 小于 长度 并且 做 字节取 用 文本、p 等于 46：
        p 为 p 加 1。
        可变 位数 为 0。
        只要 p 小于 长度 并且 做 字节取 用 文本、p 大于或等于 48 并且 做 字节取 用 文本、p 小于或等于 57 并且 位数 小于 3：
            毫秒 为 毫秒 乘 10 加 (做 字节取 用 文本、p 减 48)。
            p 为 p 加 1。
            位数 为 位数 加 1。
        完。
        只要 位数 小于 3：
            毫秒 为 毫秒 乘 10。
            位数 为 位数 加 1。
        完。
    完。
    若 全局字幕统计 不等于 空：
        全局字幕统计 的 时间戳解析次数 为 全局字幕统计 的 时间戳解析次数 加 1。
    完。
    返还 小时 乘 3600000 加 分钟 乘 60000 加 秒 乘 1000 加 毫秒。
完。

VTT_查找箭头 接受 文本、起始：
    若 文本 等于 空：
        返还 负 1。
    完。
    长度 为 做 字节长度 用 文本。
    可变 i 为 起始。
    只要 i 加 4 小于 长度：
        若 做 字节取 用 文本、i 等于 45 并且 做 字节取 用 文本、i 加 1 等于 45 并且 做 字节取 用 文本、i 加 2 等于 62：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

WebVTT_解析_完整 接受 数据、长度：
    若 数据 等于 空 或者 长度 小于 6：
        若 全局字幕统计 不等于 空：
            全局字幕统计 的 解析错误次数 为 全局字幕统计 的 解析错误次数 加 1。
        完。
        返还 空。
    完。
    开始时间 为 做 平台_获取高精度时间。
    若 做 字节取 用 数据、0 不等于 87 或者 做 字节取 用 数据、1 不等于 69 或者 做 字节取 用 数据、2 不等于 66 或者 做 字节取 用 数据、3 不等于 86 或者 做 字节取 用 数据、4 不等于 84 或者 做 字节取 用 数据、5 不等于 84：
        若 全局字幕统计 不等于 空：
            全局字幕统计 的 解析错误次数 为 全局字幕统计 的 解析错误次数 加 1。
        完。
        返还 空。
    完。
    track 为 做 堆_分配 用 TextTrack。
    若 track 等于 空：
        返还 空。
    完。
    track 的 kind 为 TEXT_KIND_SUBTITLES。
    track 的 mode 为 TEXT_TRACK_HIDDEN。
    track 的 cue头 为 0。
    track 的 cue数 为 0。
    可变 行起始 为 0。
    可变 位置 为 0。
    只要 位置 小于 长度 并且 做 字节取 用 数据、位置 不等于 10：
        位置 为 位置 加 1。
    完。
    位置 为 位置 加 1。
    只要 位置 小于 长度 并且 track 的 cue数 小于 VTT_最大Cue数：
        只要 位置 小于 长度 并且 (做 字节取 用 数据、位置 等于 10 或者 做 字节取 用 数据、位置 等于 13)：
            位置 为 位置 加 1。
        完。
        若 位置 大于或等于 长度：
            止。
        完。
        行开始 为 位置。
        只要 位置 小于 长度 并且 做 字节取 用 数据、位置 不等于 10：
            位置 为 位置 加 1。
        完。
        行结束 为 位置。
        箭头位置 为 做 VTT_查找箭头 用 数据、行开始。
        若 箭头位置 大于或等于 行开始 并且 箭头位置 小于 行结束：
            cue开始 为 做 VTT_解析时间戳 用 数据、行开始。
            cue结束 为 做 VTT_解析时间戳 用 数据、箭头位置 加 3。
            位置 为 位置 加 1。
            文本开始 为 位置。
            只要 位置 小于 长度 并且 做 字节取 用 数据、位置 不等于 10：
                位置 为 位置 加 1。
            完。
            cue 为 做 堆_分配 用 TextTrackCue。
            若 cue 不等于 空：
                cue 的 开始时间 为 cue开始。
                cue 的 结束时间 为 cue结束。
                cue 的 文本 为 数据 加 文本开始。
                cue 的 垂直 为 0。
                cue 的 行号 为 负 1。
                cue 的 位置 为 512。
                cue 的 对齐 为 1。
                cue 的 大小 为 1024。
                cue 的 下一个 为 track 的 cue头。
                track 的 cue头 为 cue。
                track 的 cue数 为 track 的 cue数 加 1。
            完。
            若 全局字幕统计 不等于 空：
                全局字幕统计 的 Cue解析次数 为 全局字幕统计 的 Cue解析次数 加 1。
            完。
        完。
        位置 为 位置 加 1。
    完。
    若 全局字幕统计 不等于 空：
        全局字幕统计 的 VTT解析次数 为 全局字幕统计 的 VTT解析次数 加 1。
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        全局字幕统计 的 总处理耗时_纳秒 为 全局字幕统计 的 总处理耗时_纳秒 加 耗时。
    完。
    做 AI字幕_记录 用 track 的 cue数。
    返还 track。
完。

SourceBuffer_appendBuffer_完整 接受 sb、数据、大小：
    若 sb 等于 空 或者 数据 等于 空 或者 大小 小于或等于 0：
        返还 错误_无效参数。
    完。
    若 sb 的 正在更新 等于 真：
        若 全局字幕统计 不等于 空：
            全局字幕统计 的 错误总数 为 全局字幕统计 的 错误总数 加 1。
        完。
        返还 错误_已存在。
    完。
    若 大小 大于 MSE_最大追加大小：
        若 全局字幕统计 不等于 空：
            全局字幕统计 的 溢出保护次数 为 全局字幕统计 的 溢出保护次数 加 1。
        完。
        返还 错误_容量不足。
    完。
    sb 的 正在更新 为 真。
    若 sb 的 追加缓冲 等于 空：
        sb 的 追加缓冲 为 做 堆_分配 用 大小。
        若 sb 的 追加缓冲 等于 空：
            sb 的 正在更新 为 假。
            返还 错误_内存不足。
        完。
        做 内存_复制 用 sb 的 追加缓冲、数据、大小。
        sb 的 缓冲大小 为 大小。
    否则：
        新缓冲 为 做 堆_重新分配 用 sb 的 追加缓冲、sb 的 缓冲大小、sb 的 缓冲大小 加 大小。
        若 新缓冲 等于 空：
            sb 的 正在更新 为 假。
            返还 错误_内存不足。
        完。
        sb 的 追加缓冲 为 新缓冲。
        做 内存_复制 用 sb 的 追加缓冲 加 sb 的 缓冲大小、数据、大小。
        sb 的 缓冲大小 为 sb 的 缓冲大小 加 大小。
    完。
    sb 的 正在更新 为 假。
    若 全局字幕统计 不等于 空：
        全局字幕统计 的 追加操作次数 为 全局字幕统计 的 追加操作次数 加 1。
        全局字幕统计 的 追加字节总数 为 全局字幕统计 的 追加字节总数 加 大小。
    完。
    返还 0。
完。

SourceBuffer_remove 接受 sb、开始、结束：
    若 sb 等于 空：
        返还 错误_无效参数。
    完。
    若 sb 的 正在更新 等于 真：
        返还 错误_已存在。
    完。
    sb 的 正在更新 为 真。
    sb 的 正在更新 为 假。
    若 全局字幕统计 不等于 空：
        全局字幕统计 的 删除操作次数 为 全局字幕统计 的 删除操作次数 加 1。
    完。
    返还 0。
完。

SourceBuffer_abort 接受 sb：
    若 sb 等于 空：
        返还。
    完。
    sb 的 正在更新 为 假。
完。

媒体Source_open 接受 ms、me：
    若 ms 等于 空：
        返还。
    完。
    ms 的 状态 为 MSE_OPEN。
    ms 的 元素 为 me。
    若 全局字幕统计 不等于 空：
        全局字幕统计 的 MSE创建次数 为 全局字幕统计 的 MSE创建次数 加 1。
    完。
完。

媒体Source_销毁 接受 ms：
    若 ms 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 ms 的 缓冲区数：
        sb 为 ms 的 缓冲区 的 第 i 项。
        若 sb 不等于 空：
            若 sb 的 追加缓冲 不等于 空：
                做 堆_释放 用 sb 的 追加缓冲。
            完。
            若 sb 的 已缓冲 不等于 空：
                做 TimeRanges_销毁 用 sb 的 已缓冲。
            完。
            做 堆_释放 用 sb。
        完。
        i 为 i 加 1。
    完。
    做 堆_释放 用 ms。
完。

AI字幕_记录 接受 cue数：
    若 全局字幕AI 等于 空：
        返还。
    完。
    AI 为 全局字幕AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    AI 的 历史平均Cue数 为 (AI 的 历史平均Cue数 乘 7 加 cue数 乘 1024) 除以 8。
    若 cue数 大于 0：
        AI 的 连续有字幕计数 为 AI 的 连续有字幕计数 加 1。
        AI 的 历史有字幕率 为 (AI 的 历史有字幕率 乘 15 加 1024) 除以 16。
    否则：
        AI 的 连续有字幕计数 为 0。
        AI 的 历史有字幕率 为 AI 的 历史有字幕率 乘 15 除以 16。
    完。
    若 AI 的 采样计数器 小于 8：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 全局字幕统计 不等于 空：
        全局字幕统计 的 AI语言调用次数 为 全局字幕统计 的 AI语言调用次数 加 1。
    完。
    若 AI 的 历史有字幕率 大于 600 或者 AI 的 连续有字幕计数 大于 3：
        AI 的 预测有字幕 为 真。
    否则：
        AI 的 预测有字幕 为 假。
    完。
    若 AI 的 预测有字幕 等于 真 并且 cue数 大于 0：
        若 全局字幕统计 不等于 空：
            全局字幕统计 的 AI语言命中次数 为 全局字幕统计 的 AI语言命中次数 加 1。
        完。
    完。
完。

字幕_打印统计 接受 零参：
    若 全局字幕统计 等于 空：
        做 打印 用 「[字幕] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   字幕+MSE · 统计报告               │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ VTT解析:          」、做 转字符串 用 全局字幕统计 的 VTT解析次数。
    做 打印 用 做 字符串拼接 用 「│ Cue解析:          」、做 转字符串 用 全局字幕统计 的 Cue解析次数。
    做 打印 用 做 字符串拼接 用 「│ 时间戳解析:       」、做 转字符串 用 全局字幕统计 的 时间戳解析次数。
    做 打印 用 做 字符串拼接 用 「│ 字幕更新:         」、做 转字符串 用 全局字幕统计 的 字幕更新次数。
    做 打印 用 做 字符串拼接 用 「│ MSE创建:          」、做 转字符串 用 全局字幕统计 的 MSE创建次数。
    做 打印 用 做 字符串拼接 用 「│ SourceBuffer:     」、做 转字符串 用 全局字幕统计 的 SourceBuffer创建次数。
    做 打印 用 做 字符串拼接 用 「│ 追加操作:         」、做 转字符串 用 全局字幕统计 的 追加操作次数。
    做 打印 用 做 字符串拼接 用 「│ 追加字节(KB):     」、做 转字符串 用 全局字幕统计 的 追加字节总数 除以 1024。
    做 打印 用 做 字符串拼接 用 「│ 删除操作:         」、做 转字符串 用 全局字幕统计 的 删除操作次数。
    做 打印 用 做 字符串拼接 用 「│ endOfStream:      」、做 转字符串 用 全局字幕统计 的 endOfStream次数。
    做 打印 用 做 字符串拼接 用 「│ AI语言调用:       」、做 转字符串 用 全局字幕统计 的 AI语言调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI语言命中:       」、做 转字符串 用 全局字幕统计 的 AI语言命中次数。
    做 打印 用 做 字符串拼接 用 「│ 总耗时(μs):       」、做 转字符串 用 全局字幕统计 的 总处理耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 解析错误:         」、做 转字符串 用 全局字幕统计 的 解析错误次数。
    做 打印 用 做 字符串拼接 用 「│ 溢出保护:         」、做 转字符串 用 全局字幕统计 的 溢出保护次数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

字幕_导出诊断 接受 零参：
    若 全局字幕AI 等于 空：
        做 打印 用 「[字幕AI] 未初始化」。
        返还。
    完。
    AI 为 全局字幕AI。
    做 打印 用 「[字幕AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史有字幕率(Q10): 」、做 转字符串 用 AI 的 历史有字幕率。
    做 打印 用 做 字符串拼接 用 「  历史平均Cue数(Q10): 」、做 转字符串 用 AI 的 历史平均Cue数。
    做 打印 用 做 字符串拼接 用 「  连续有字幕: 」、做 转字符串 用 AI 的 连续有字幕计数。
    做 打印 用 做 字符串拼接 用 「  预测有字幕: 」、做 转字符串 用 AI 的 预测有字幕。
完。

字幕_获取统计 接受 零参：
    返还 全局字幕统计。
完。

字幕_获取AI状态 接受 零参：
    返还 全局字幕AI。
完。

字幕_预测有字幕 接受 零参：
    若 全局字幕AI 等于 空：
        返还 假。
    完。
    返还 全局字幕AI 的 预测有字幕。
完。

字幕_是否已初始化 接受 零参：
    返还 全局字幕已初始化。
完。

本章 收束。
