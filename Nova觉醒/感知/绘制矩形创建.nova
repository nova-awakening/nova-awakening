《绘制矩形创建》

引用《..·稳态·图处显示》。

引用《.·内核直连层》。

引用《..·推理·定点Q算术》。

SkRect 是 一种 事物，含 左、上、右、下。

SkRect_创建 接受 左、上、右、下：
    r 为 新建 SkRect。
    r 的 左 为 左。
    r 的 上 为 上。
    r 的 右 为 右。
    r 的 下 为 下。
    返还 r。
完。

SkRect_宽 接受 r：
    返还 r 的 右 减 r 的 左。
完。

SkRect_高 接受 r：
    返还 r 的 下 减 r 的 上。
完。

SkRect_包含点 接受 r、x、y：
    返还 x 大于或等于 r 的 左 并且 x 小于 r 的 右 并且 y 大于或等于 r 的 上 并且 y 小于 r 的 下。
完。

SkRRect 是 一种 事物，含 矩形、圆角_左上、圆角_右上、圆角_左下、圆角_右下。

SkRRect_创建 接受 矩形、半径：
    rr 为 新建 SkRRect。
    rr 的 矩形 为 矩形。
    rr 的 圆角_左上 为 半径。
    rr 的 圆角_右上 为 半径。
    rr 的 圆角_左下 为 半径。
    rr 的 圆角_右下 为 半径。
    返还 rr。
完。

SkMatrix 是 一种 事物，含 m00、m10、m20。

SkMatrix_单位矩阵 接受 零参：
    m 为 新建 SkMatrix。
    m 的 m00 为 10000。
    m 的 m01 为 0。
    m 的 m02 为 0。
    m 的 m10 为 0。
    m 的 m11 为 10000。
    m 的 m12 为 0。
    m 的 m20 为 0。
    m 的 m21 为 0。
    m 的 m22 为 10000。
    返还 m。
完。

SkMatrix_平移 接受 tx、ty：
    m 为 做 SkMatrix_单位矩阵。
    m 的 m02 为 tx 乘 10000 除以 100。
    m 的 m12 为 ty 乘 10000 除以 100。
    返还 m。
完。

SkMatrix_缩放 接受 sx、sy：
    m 为 做 SkMatrix_单位矩阵。
    m 的 m00 为 sx 乘 100。
    m 的 m11 为 sy 乘 100。
    返还 m。
完。

SkMatrix_乘 接受 a、b：
    结果 为 新建 SkMatrix。
    结果 的 m00 为 (a 的 m00 乘 b 的 m00 加 a 的 m01 乘 b 的 m10 加 a 的 m02 乘 b 的 m20) 除以 10000。
    结果 的 m01 为 (a 的 m00 乘 b 的 m01 加 a 的 m01 乘 b 的 m11 加 a 的 m02 乘 b 的 m21) 除以 10000。
    结果 的 m02 为 (a 的 m00 乘 b 的 m02 加 a 的 m01 乘 b 的 m12 加 a 的 m02 乘 b 的 m22) 除以 10000。
    结果 的 m10 为 (a 的 m10 乘 b 的 m00 加 a 的 m11 乘 b 的 m10 加 a 的 m12 乘 b 的 m20) 除以 10000。
    结果 的 m11 为 (a 的 m10 乘 b 的 m01 加 a 的 m11 乘 b 的 m11 加 a 的 m12 乘 b 的 m21) 除以 10000。
    结果 的 m12 为 (a 的 m10 乘 b 的 m02 加 a 的 m11 乘 b 的 m12 加 a 的 m12 乘 b 的 m22) 除以 10000。
    结果 的 m20 为 (a 的 m20 乘 b 的 m00 加 a 的 m21 乘 b 的 m10 加 a 的 m22 乘 b 的 m20) 除以 10000。
    结果 的 m21 为 (a 的 m20 乘 b 的 m01 加 a 的 m21 乘 b 的 m11 加 a 的 m22 乘 b 的 m21) 除以 10000。
    结果 的 m22 为 (a 的 m20 乘 b 的 m02 加 a 的 m21 乘 b 的 m12 加 a 的 m22 乘 b 的 m22) 除以 10000。
    返还 结果。
完。

SkMatrix_变换点 接受 m、x、y：
    新x 为 (m 的 m00 乘 x 加 m 的 m01 乘 y 加 m 的 m02 乘 100) 除以 10000。
    新y 为 (m 的 m10 乘 x 加 m 的 m11 乘 y 加 m 的 m12 乘 100) 除以 10000。
    返还 一组：新x、新y。
完。

NRGP_版本 为 1。

NRGP_AI特征维度 为 8。

NRGP_路径最大动词 为 256。

NRGP_路径最大点 为 512。

可变 NRGP_矩形操作数 为 0。

可变 NRGP_路径操作数 为 0。

可变 NRGP_变换操作数 为 0。

可变 NRGP_AI推理次数 为 0。

动词_移动 为 0。

动词_直线 为 1。

动词_二次曲线 为 2。

动词_三次曲线 为 3。

动词_关闭 为 4。

SkPath 是 一种 事物，含 动词数组、点数组、动词数、点数、填充规则。

SkPath_创建 接受 零参：
    p 为 做 堆_分配 用 SkPath。
    p 的 动词数组 为 做 数组_创建 用 NRGP_路径最大动词。
    p 的 点数组 为 做 数组_创建 用 NRGP_路径最大点 乘 2。
    p 的 动词数 为 0。
    p 的 点数 为 0。
    p 的 填充规则 为 1。
    NRGP_路径操作数 为 NRGP_路径操作数 加 1。
    返还 p。
完。

SkPath_移动到 接受 p、x、y：
    若 p 的 动词数 大于或等于 NRGP_路径最大动词：
        返还。
    完。
    p 的 动词数组 的 第 p 的 动词数 项 为 动词_移动。
    p 的 动词数 为 p 的 动词数 加 1。
    idx 为 p 的 点数 乘 2。
    p 的 点数组 的 第 idx 项 为 x。
    p 的 点数组 的 第 idx 加 1 项 为 y。
    p 的 点数 为 p 的 点数 加 1。
完。

SkPath_直线到 接受 p、x、y：
    若 p 的 动词数 大于或等于 NRGP_路径最大动词：
        返还。
    完。
    p 的 动词数组 的 第 p 的 动词数 项 为 动词_直线。
    p 的 动词数 为 p 的 动词数 加 1。
    idx 为 p 的 点数 乘 2。
    p 的 点数组 的 第 idx 项 为 x。
    p 的 点数组 的 第 idx 加 1 项 为 y。
    p 的 点数 为 p 的 点数 加 1。
完。

SkPath_二次曲线到 接受 p、cx、cy、x、y：
    若 p 的 动词数 大于或等于 NRGP_路径最大动词：
        返还。
    完。
    p 的 动词数组 的 第 p 的 动词数 项 为 动词_二次曲线。
    p 的 动词数 为 p 的 动词数 加 1。
    idx 为 p 的 点数 乘 2。
    p 的 点数组 的 第 idx 项 为 cx。
    p 的 点数组 的 第 idx 加 1 项 为 cy。
    p 的 点数组 的 第 idx 加 2 项 为 x。
    p 的 点数组 的 第 idx 加 3 项 为 y。
    p 的 点数 为 p 的 点数 加 2。
完。

SkPath_三次曲线到 接受 p、c1x、c1y、c2x、c2y、x、y：
    若 p 的 动词数 大于或等于 NRGP_路径最大动词：
        返还。
    完。
    p 的 动词数组 的 第 p 的 动词数 项 为 动词_三次曲线。
    p 的 动词数 为 p 的 动词数 加 1。
    idx 为 p 的 点数 乘 2。
    p 的 点数组 的 第 idx 项 为 c1x。
    p 的 点数组 的 第 idx 加 1 项 为 c1y。
    p 的 点数组 的 第 idx 加 2 项 为 c2x。
    p 的 点数组 的 第 idx 加 3 项 为 c2y。
    p 的 点数组 的 第 idx 加 4 项 为 x。
    p 的 点数组 的 第 idx 加 5 项 为 y。
    p 的 点数 为 p 的 点数 加 3。
完。

SkPath_关闭 接受 p：
    若 p 的 动词数 大于或等于 NRGP_路径最大动词：
        返还。
    完。
    p 的 动词数组 的 第 p 的 动词数 项 为 动词_关闭。
    p 的 动词数 为 p 的 动词数 加 1。
完。

SkPath_重置 接受 p：
    p 的 动词数 为 0。
    p 的 点数 为 0。
完。

SkPath_释放 接受 p：
    若 p 不等于 0：
        做 堆_释放 用 p 的 动词数组。
        做 堆_释放 用 p 的 点数组。
        做 堆_释放 用 p。
    完。
完。

SkPath_添加矩形 接受 p、r：
    做 SkPath_移动到 用 p、r 的 左、r 的 上。
    做 SkPath_直线到 用 p、r 的 右、r 的 上。
    做 SkPath_直线到 用 p、r 的 右、r 的 下。
    做 SkPath_直线到 用 p、r 的 左、r 的 下。
    做 SkPath_关闭 用 p。
完。

SkPath_边界框 接受 p：
    若 p 的 点数 等于 0：
        返还 做 SkRect_创建 用 0、0、0、0。
    完。
    可变 最小x 为 p 的 点数组 的 第 0 项。
    可变 最小y 为 p 的 点数组 的 第 1 项。
    可变 最大x 为 最小x。
    可变 最大y 为 最小y。
    可变 i 为 1。
    只要 i 小于 p 的 点数：
        px 为 p 的 点数组 的 第 i 乘 2 项。
        py 为 p 的 点数组 的 第 i 乘 2 加 1 项。
        若 px 小于 最小x：
            最小x 为 px。
        完。
        若 px 大于 最大x：
            最大x 为 px。
        完。
        若 py 小于 最小y：
            最小y 为 py。
        完。
        若 py 大于 最大y：
            最大y 为 py。
        完。
        i 为 i 加 1。
    完。
    返还 做 SkRect_创建 用 最小x、最小y、最大x、最大y。
完。

SkRect_交集 接受 a、b：
    左 为 a 的 左。
    若 b 的 左 大于 左：
        左 为 b 的 左。
    完。
    上 为 a 的 上。
    若 b 的 上 大于 上：
        上 为 b 的 上。
    完。
    右 为 a 的 右。
    若 b 的 右 小于 右：
        右 为 b 的 右。
    完。
    下 为 a 的 下。
    若 b 的 下 小于 下：
        下 为 b 的 下。
    完。
    若 左 大于或等于 右 或者 上 大于或等于 下：
        返还 做 SkRect_创建 用 0、0、0、0。
    完。
    NRGP_矩形操作数 为 NRGP_矩形操作数 加 1。
    返还 做 SkRect_创建 用 左、上、右、下。
完。

SkRect_并集 接受 a、b：
    左 为 a 的 左。
    若 b 的 左 小于 左：
        左 为 b 的 左。
    完。
    上 为 a 的 上。
    若 b 的 上 小于 上：
        上 为 b 的 上。
    完。
    右 为 a 的 右。
    若 b 的 右 大于 右：
        右 为 b 的 右。
    完。
    下 为 a 的 下。
    若 b 的 下 大于 下：
        下 为 b 的 下。
    完。
    NRGP_矩形操作数 为 NRGP_矩形操作数 加 1。
    返还 做 SkRect_创建 用 左、上、右、下。
完。

SkRect_内缩 接受 r、dx、dy：
    NRGP_矩形操作数 为 NRGP_矩形操作数 加 1。
    返还 做 SkRect_创建 用 r 的 左 加 dx、r 的 上 加 dy、r 的 右 减 dx、r 的 下 减 dy。
完。

SkRect_外扩 接受 r、dx、dy：
    NRGP_矩形操作数 为 NRGP_矩形操作数 加 1。
    返还 做 SkRect_创建 用 r 的 左 减 dx、r 的 上 减 dy、r 的 右 加 dx、r 的 下 加 dy。
完。

SkRect_是空 接受 r：
    返还 r 的 左 大于或等于 r 的 右 或者 r 的 上 大于或等于 r 的 下。
完。

SkRect_包含矩形 接受 a、b：
    返还 a 的 左 小于或等于 b 的 左 并且 a 的 上 小于或等于 b 的 上 并且 a 的 右 大于或等于 b 的 右 并且 a 的 下 大于或等于 b 的 下。
完。

SkRect_相交 接受 a、b：
    返还 a 的 左 小于 b 的 右 并且 b 的 左 小于 a 的 右 并且 a 的 上 小于 b 的 下 并且 b 的 上 小于 a 的 下。
完。

SkMatrix_行列式 接受 m：
    d 为 m 的 m00 乘 (m 的 m11 乘 m 的 m22 减 m 的 m12 乘 m 的 m21) 减 m 的 m01 乘 (m 的 m10 乘 m 的 m22 减 m 的 m12 乘 m 的 m20) 加 m 的 m02 乘 (m 的 m10 乘 m 的 m21 减 m 的 m11 乘 m 的 m20)。
    返还 d 除以 (10000 乘 10000)。
完。

SkMatrix_求逆 接受 m：
    det 为 做 SkMatrix_行列式 用 m。
    若 det 等于 0：
        返还 做 SkMatrix_单位矩阵。
    完。
    inv 为 新建 SkMatrix。
    inv 的 m00 为 (m 的 m11 乘 m 的 m22 减 m 的 m12 乘 m 的 m21) 乘 10000 除以 det。
    inv 的 m01 为 (m 的 m02 乘 m 的 m21 减 m 的 m01 乘 m 的 m22) 乘 10000 除以 det。
    inv 的 m02 为 (m 的 m01 乘 m 的 m12 减 m 的 m02 乘 m 的 m11) 乘 10000 除以 det。
    inv 的 m10 为 (m 的 m12 乘 m 的 m20 减 m 的 m10 乘 m 的 m22) 乘 10000 除以 det。
    inv 的 m11 为 (m 的 m00 乘 m 的 m22 减 m 的 m02 乘 m 的 m20) 乘 10000 除以 det。
    inv 的 m12 为 (m 的 m02 乘 m 的 m10 减 m 的 m00 乘 m 的 m12) 乘 10000 除以 det。
    inv 的 m20 为 (m 的 m10 乘 m 的 m21 减 m 的 m11 乘 m 的 m20) 乘 10000 除以 det。
    inv 的 m21 为 (m 的 m01 乘 m 的 m20 减 m 的 m00 乘 m 的 m21) 乘 10000 除以 det。
    inv 的 m22 为 (m 的 m00 乘 m 的 m11 减 m 的 m01 乘 m 的 m10) 乘 10000 除以 det。
    NRGP_变换操作数 为 NRGP_变换操作数 加 1。
    返还 inv。
完。

SkMatrix_旋转 接受 角度x100：
    PI_FP 为 31416。
    弧度 为 角度x100 乘 PI_FP 除以 18000。
    x 为 弧度 取余于 (2 乘 PI_FP)。
    若 x 小于 0：
        x 为 x 加 2 乘 PI_FP。
    完。
    可变 sin_val 为 0。
    可变 x_sin 为 x。
    若 x_sin 大于 PI_FP：
        x_sin 为 2 乘 PI_FP 减 x_sin。
    完。
    分子 为 16 乘 x_sin 乘 (PI_FP 减 x_sin)。
    分母 为 5 乘 PI_FP 乘 PI_FP 减 4 乘 x_sin 乘 (PI_FP 减 x_sin)。
    若 分母 不等于 0：
        sin_val 为 分子 乘 10000 除以 分母。
    完。
    若 x 大于 PI_FP：
        sin_val 为 负 sin_val。
    完。
    cos_x 为 (x 加 PI_FP 除以 2) 取余于 (2 乘 PI_FP)。
    可变 cos_val 为 0。
    可变 x_cos 为 cos_x。
    若 x_cos 大于 PI_FP：
        x_cos 为 2 乘 PI_FP 减 x_cos。
    完。
    分子2 为 16 乘 x_cos 乘 (PI_FP 减 x_cos)。
    分母2 为 5 乘 PI_FP 乘 PI_FP 减 4 乘 x_cos 乘 (PI_FP 减 x_cos)。
    若 分母2 不等于 0：
        cos_val 为 分子2 乘 10000 除以 分母2。
    完。
    若 cos_x 大于 PI_FP：
        cos_val 为 负 cos_val。
    完。
    m 为 新建 SkMatrix。
    m 的 m00 为 cos_val。
    m 的 m01 为 负 sin_val。
    m 的 m02 为 0。
    m 的 m10 为 sin_val。
    m 的 m11 为 cos_val。
    m 的 m12 为 0。
    m 的 m20 为 0。
    m 的 m21 为 0。
    m 的 m22 为 10000。
    NRGP_变换操作数 为 NRGP_变换操作数 加 1。
    返还 m。
完。

SkMatrix_变换矩形 接受 m、r：
    p0 为 做 SkMatrix_变换点 用 m、r 的 左、r 的 上。
    p1 为 做 SkMatrix_变换点 用 m、r 的 右、r 的 上。
    p2 为 做 SkMatrix_变换点 用 m、r 的 右、r 的 下。
    p3 为 做 SkMatrix_变换点 用 m、r 的 左、r 的 下。
    可变 最小x 为 p0 的 第 0 项。
    可变 最小y 为 p0 的 第 1 项。
    可变 最大x 为 p0 的 第 0 项。
    可变 最大y 为 p0 的 第 1 项。
    若 p1 的 第 0 项 小于 最小x：
        最小x 为 p1 的 第 0 项。
    完。
    若 p1 的 第 0 项 大于 最大x：
        最大x 为 p1 的 第 0 项。
    完。
    若 p1 的 第 1 项 小于 最小y：
        最小y 为 p1 的 第 1 项。
    完。
    若 p1 的 第 1 项 大于 最大y：
        最大y 为 p1 的 第 1 项。
    完。
    若 p2 的 第 0 项 小于 最小x：
        最小x 为 p2 的 第 0 项。
    完。
    若 p2 的 第 0 项 大于 最大x：
        最大x 为 p2 的 第 0 项。
    完。
    若 p2 的 第 1 项 小于 最小y：
        最小y 为 p2 的 第 1 项。
    完。
    若 p2 的 第 1 项 大于 最大y：
        最大y 为 p2 的 第 1 项。
    完。
    若 p3 的 第 0 项 小于 最小x：
        最小x 为 p3 的 第 0 项。
    完。
    若 p3 的 第 0 项 大于 最大x：
        最大x 为 p3 的 第 0 项。
    完。
    若 p3 的 第 1 项 小于 最小y：
        最小y 为 p3 的 第 1 项。
    完。
    若 p3 的 第 1 项 大于 最大y：
        最大y 为 p3 的 第 1 项。
    完。
    NRGP_变换操作数 为 NRGP_变换操作数 加 1。
    返还 做 SkRect_创建 用 最小x、最小y、最大x、最大y。
完。

NRGP_AI预测几何 接受 零参：
    特征 为 做 Tensor_创建 用 一组：NRGP_AI特征维度。
    做 Tensor_写 用 特征、一组：0、NRGP_矩形操作数。
    做 Tensor_写 用 特征、一组：1、NRGP_路径操作数。
    做 Tensor_写 用 特征、一组：2、NRGP_变换操作数。
    做 Tensor_写 用 特征、一组：3、0。
    做 Tensor_写 用 特征、一组：4、0。
    做 Tensor_写 用 特征、一组：5、0。
    做 Tensor_写 用 特征、一组：6、0。
    做 Tensor_写 用 特征、一组：7、0。
    预测 为 做 AI总线_推理 用 「NRGP几何预测」、特征。
    NRGP_AI推理次数 为 NRGP_AI推理次数 加 1。
    做 Tensor_释放 用 特征。
    做 Tensor_释放 用 预测。
完。

NRGP_获取统计 接受 零参：
    返还 一本字典：版本 对应 NRGP_版本、矩形操作数 对应 NRGP_矩形操作数、路径操作数 对应 NRGP_路径操作数、变换操作数 对应 NRGP_变换操作数、AI推理次数 对应 NRGP_AI推理次数。
完。

NRGP_重置统计 接受 零参：
    NRGP_矩形操作数 为 0。
    NRGP_路径操作数 为 0。
    NRGP_变换操作数 为 0。
    NRGP_AI推理次数 为 0。
完。

NRGP_填充矩形 接受 缓冲、宽、高、步进、r、颜色：
    可变 x0 为 r 的 左。
    可变 y0 为 r 的 上。
    可变 x1 为 r 的 右。
    可变 y1 为 r 的 下。
    若 x0 小于 0：
        x0 为 0。
    完。
    若 y0 小于 0：
        y0 为 0。
    完。
    若 x1 大于 宽：
        x1 为 宽。
    完。
    若 y1 大于 高：
        y1 为 高。
    完。
    若 x0 大于或等于 x1 或者 y0 大于或等于 y1：
        返还。
    完。
    可变 y 为 y0。
    只要 y 小于 y1：
        行偏移 为 缓冲 加 y 乘 步进。
        可变 x 为 x0。
        只要 x 小于 x1：
            做 内存_写32 用 行偏移 加 x 乘 4、颜色。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    NRGP_矩形操作数 为 NRGP_矩形操作数 加 1。
完。

NRGP_描边矩形 接受 缓冲、宽、高、步进、r、颜色：
    做 NRGP_水平线 用 缓冲、宽、高、步进、r 的 左、r 的 右、r 的 上、颜色。
    做 NRGP_水平线 用 缓冲、宽、高、步进、r 的 左、r 的 右、r 的 下 减 1、颜色。
    做 NRGP_垂直线 用 缓冲、宽、高、步进、r 的 左、r 的 上、r 的 下、颜色。
    做 NRGP_垂直线 用 缓冲、宽、高、步进、r 的 右 减 1、r 的 上、r 的 下、颜色。
    NRGP_矩形操作数 为 NRGP_矩形操作数 加 1。
完。

NRGP_水平线 接受 缓冲、宽、高、步进、x0、x1、y、颜色：
    若 y 小于 0 或者 y 大于或等于 高：
        返还。
    完。
    若 x0 小于 0：
        x0 为 0。
    完。
    若 x1 大于 宽：
        x1 为 宽。
    完。
    行 为 缓冲 加 y 乘 步进。
    可变 x 为 x0。
    只要 x 小于 x1：
        做 内存_写32 用 行 加 x 乘 4、颜色。
        x 为 x 加 1。
    完。
完。

NRGP_垂直线 接受 缓冲、宽、高、步进、x、y0、y1、颜色：
    若 x 小于 0 或者 x 大于或等于 宽：
        返还。
    完。
    若 y0 小于 0：
        y0 为 0。
    完。
    若 y1 大于 高：
        y1 为 高。
    完。
    可变 y 为 y0。
    只要 y 小于 y1：
        做 内存_写32 用 缓冲 加 y 乘 步进 加 x 乘 4、颜色。
        y 为 y 加 1。
    完。
完。

NRGP_清空矩形 接受 缓冲、宽、高、步进、r：
    做 NRGP_填充矩形 用 缓冲、宽、高、步进、r、0。
完。

SkRRect_包含点 接受 rr、x、y：
    r 为 rr 的 矩形。
    若 非 做 SkRect_包含点 用 r、x、y：
        返还 假。
    完。
    若 x 小于 r 的 左 加 rr 的 圆角_左上 并且 y 小于 r 的 上 加 rr 的 圆角_左上：
        dx 为 x 减 (r 的 左 加 rr 的 圆角_左上)。
        dy 为 y 减 (r 的 上 加 rr 的 圆角_左上)。
        返还 dx 乘 dx 加 dy 乘 dy 小于或等于 rr 的 圆角_左上 乘 rr 的 圆角_左上。
    完。
    若 x 大于或等于 r 的 右 减 rr 的 圆角_右上 并且 y 小于 r 的 上 加 rr 的 圆角_右上：
        dx 为 x 减 (r 的 右 减 rr 的 圆角_右上)。
        dy 为 y 减 (r 的 上 加 rr 的 圆角_右上)。
        返还 dx 乘 dx 加 dy 乘 dy 小于或等于 rr 的 圆角_右上 乘 rr 的 圆角_右上。
    完。
    若 x 小于 r 的 左 加 rr 的 圆角_左下 并且 y 大于或等于 r 的 下 减 rr 的 圆角_左下：
        dx 为 x 减 (r 的 左 加 rr 的 圆角_左下)。
        dy 为 y 减 (r 的 下 减 rr 的 圆角_左下)。
        返还 dx 乘 dx 加 dy 乘 dy 小于或等于 rr 的 圆角_左下 乘 rr 的 圆角_左下。
    完。
    若 x 大于或等于 r 的 右 减 rr 的 圆角_右下 并且 y 大于或等于 r 的 下 减 rr 的 圆角_右下：
        dx 为 x 减 (r 的 右 减 rr 的 圆角_右下)。
        dy 为 y 减 (r 的 下 减 rr 的 圆角_右下)。
        返还 dx 乘 dx 加 dy 乘 dy 小于或等于 rr 的 圆角_右下 乘 rr 的 圆角_右下。
    完。
    返还 真。
完。

可变 _谐振合并_次数 为 0。

可变 _谐振合并_节省数 为 0。

谐振矩形_可合并 接受 r1x、r1y、r1w、r1h、r2x、r2y、r2w、r2h：
    若 r1y 等于 r2y 并且 r1h 等于 r2h 并且 r1x 加 r1w 等于 r2x：
        返还 真。
    完。
    若 r1x 等于 r2x 并且 r1w 等于 r2w 并且 r1y 加 r1h 等于 r2y：
        返还 真。
    完。
    返还 假。
完。

谐振矩形_合并 接受 r1x、r1y、r1w、r1h、r2x、r2y、r2w、r2h：
    _谐振合并_次数 为 _谐振合并_次数 加 1。
    _谐振合并_节省数 为 _谐振合并_节省数 加 1。
    若 r1y 等于 r2y 并且 r1h 等于 r2h：
        返还 一本字典：x 对应 r1x、y 对应 r1y、w 对应 r1w 加 r2w、h 对应 r1h。
    完。
    返还 一本字典：x 对应 r1x、y 对应 r1y、w 对应 r1w、h 对应 r1h 加 r2h。
完。

绘制矩形_完整统计 接受 零参：
    返还 一组：「谐振合并次数」、_谐振合并_次数、「谐振节省数」、_谐振合并_节省数。
完。

本章 收束。
