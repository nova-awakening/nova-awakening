《字形缓存与显示列表》

引用《..·稳态·图处显示》。

引用《..·推理·通信总线》。

引用《.·样式滤镜》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《.·网缓画布结》。

引用《.·绘制画布绘》。

引用《.·显示列表执》。

引用《..·中枢·内容可见性》。

引用《.·内核直连层》。

引用《..·中枢·内存初始化》。

引用《.·绘制矩形创建》。

字形缓存_默认容量 为 2048。

哈希表容量 为 4096。

FNV_OFFSET_BASIS 为 2166136261。

FNV_PRIME 为 16777619。

图集宽度 为 1024。

图集高度 为 1024。

单元格宽 为 32。

单元格高 为 32。

每行单元数 为 32。

图集最大字形数 为 1024。

AI_字形预测阈值 为 0.5。

DL_背景 为 1。

DL_边框 为 2。

DL_文本 为 3。

DL_图片 为 4。

DL_阴影 为 5。

DL_圆角矩形 为 6。

字形条目 是 一种 事物，含 字符码、字号、字体哈希、位图指针、宽、高、图集索引、访问时间、访问次数、哈希链接。

字形图集 是 一种 事物，含 位图数据、宽、高、已用单元数、空闲单元栈、空闲栈顶。

GCLR统计 是 一种 事物，含 查找总数、命中数、未命中数、淘汰数、光栅化数、图集上传数、显示列表执行数、显示列表合并数、AI预测数、总绘制耗时、跳过次数。

可变 全局GCLR统计 为 新建 GCLR统计。

可变 字形条目数组 为 0。

可变 字形条目数量 为 0。

可变 字形条目容量 为 0。

可变 哈希表 为 0。

可变 全局图集 为 0。

可变 AI绘制全局统计 为 0。

字形哈希 接受 字符码、字号、字体哈希：
    可变 h 为 FNV_OFFSET_BASIS。
    h 为 (h 位异或 字符码 位与 0xFF) 乘 FNV_PRIME。
    h 为 (h 位异或 字符码 右移 8 位与 0xFF) 乘 FNV_PRIME。
    h 为 (h 位异或 字符码 右移 16 位与 0xFF) 乘 FNV_PRIME。
    h 为 (h 位异或 字符码 右移 24 位与 0xFF) 乘 FNV_PRIME。
    h 为 h 位异或 字号 乘 31。
    h 为 h 位异或 字体哈希。
    返还 h 位与 哈希表容量 减 1。
完。

AI绘制_初始化字形缓存 接受 零参：
    字形条目容量 为 字形缓存_默认容量。
    字形条目数组 为 做 堆_分配 用 字形条目容量 乘 48。
    字形条目数量 为 0。
    哈希表 为 做 堆_分配 用 哈希表容量 乘 4。
    可变 i 为 0。
    只要 i 小于 哈希表容量：
        做 内存_分32 用 哈希表 加 i 乘 4、0xFFFFFFFF。
        i 为 i 加 1。
    完。
    全局图集 为 做 堆_分配 用 字形图集。
    若 全局图集 不等于 0：
        全局图集 的 位图数据 为 做 堆_分配 用 图集宽度 乘 图集高度 乘 4。
        全局图集 的 宽 为 图集宽度。
        全局图集 的 高 为 图集高度。
        全局图集 的 已用单元数 为 0。
        全局图集 的 空闲单元栈 为 做 堆_分配 用 图集最大字形数 乘 4。
        全局图集 的 空闲栈顶 为 0。
    完。
    全局GCLR统计 为 新建 GCLR统计。
    AI绘制全局统计 为 & 全局GCLR统计。
完。

AI绘制_查字形 接受 字符码、字号、字体哈希：
    全局GCLR统计 的 查找总数 为 全局GCLR统计 的 查找总数 加 1。
    若 哈希表 等于 0：
        返还 0。
    完。
    bucket 为 做 字形哈希 用 字符码、字号、字体哈希。
    可变 idx 为 做 内存_读32 用 哈希表 加 bucket 乘 4。
    只要 idx 不等于 0xFFFFFFFF 并且 idx 小于 字形条目数量：
        偏移 为 字形条目数组 加 idx 乘 48。
        若 做 内存_读32 用 偏移 等于 字符码 并且 做 内存_读32 用 偏移 加 4 等于 字号 并且 做 内存_读32 用 偏移 加 8 等于 字体哈希：
            做 内存_分64 用 偏移 加 28、做 平台_获取高精度时间。
            cnt 为 做 内存_读32 用 偏移 加 36 加 1。
            做 内存_分32 用 偏移 加 36、cnt。
            全局GCLR统计 的 命中数 为 全局GCLR统计 的 命中数 加 1。
            返还 做 内存_读64 用 偏移 加 12。
        完。
        idx 为 做 内存_读32 用 偏移 加 40。
    完。
    全局GCLR统计 的 未命中数 为 全局GCLR统计 的 未命中数 加 1。
    返还 0。
完。

AI绘制_存字形 接受 字符码、字号、字体哈希、位图、宽、高：
    若 字形条目数组 等于 0：
        返还。
    完。
    若 字形条目数量 大于或等于 字形条目容量：
        做 字形_LRU淘汰。
    完。
    若 字形条目数量 大于或等于 字形条目容量：
        返还。
    完。
    atlas_id 为 做 图集_分配单元。
    idx 为 字形条目数量。
    偏移 为 字形条目数组 加 idx 乘 48。
    做 内存_分32 用 偏移、字符码。
    做 内存_分32 用 偏移 加 4、字号。
    做 内存_分32 用 偏移 加 8、字体哈希。
    做 内存_分64 用 偏移 加 12、位图。
    做 内存_分16 用 偏移 加 20、宽。
    做 内存_分16 用 偏移 加 22、高。
    做 内存_分32 用 偏移 加 24、atlas_id。
    做 内存_分64 用 偏移 加 28、做 平台_获取高精度时间。
    做 内存_分32 用 偏移 加 36、1。
    bucket 为 做 字形哈希 用 字符码、字号、字体哈希。
    old_head 为 做 内存_读32 用 哈希表 加 bucket 乘 4。
    做 内存_分32 用 偏移 加 40、old_head。
    做 内存_分32 用 哈希表 加 bucket 乘 4、idx。
    字形条目数量 为 字形条目数量 加 1。
    全局GCLR统计 的 光栅化数 为 全局GCLR统计 的 光栅化数 加 1。
完。

字形_LRU淘汰 接受 零参：
    若 字形条目数量 等于 0：
        返还。
    完。
    可变 最旧时间 为 0xFFFFFFFFFFFFFFFF。
    可变 最旧索引 为 0。
    可变 i 为 0。
    只要 i 小于 字形条目数量：
        t 为 做 内存_读64 用 字形条目数组 加 i 乘 48 加 28。
        若 t 小于 最旧时间：
            最旧时间 为 t。
            最旧索引 为 i。
        完。
        i 为 i 加 1。
    完。
    偏移 为 字形条目数组 加 最旧索引 乘 48。
    atlas_id 为 做 内存_读32 用 偏移 加 24。
    做 图集_回收单元 用 atlas_id。
    若 最旧索引 小于 字形条目数量 减 1：
        最后 为 字形条目数组 加 (字形条目数量 减 1) 乘 48。
        做 内存_复制 用 偏移、最后、48。
    完。
    字形条目数量 为 字形条目数量 减 1。
    全局GCLR统计 的 淘汰数 为 全局GCLR统计 的 淘汰数 加 1。
    做 字形_重建哈希表。
完。

字形_重建哈希表 接受 零参：
    可变 i 为 0。
    只要 i 小于 哈希表容量：
        做 内存_分32 用 哈希表 加 i 乘 4、0xFFFFFFFF。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 字形条目数量：
        偏移 为 字形条目数组 加 i 乘 48。
        bucket 为 做 字形哈希 用 做 内存_读32 用 偏移、做 内存_读32 用 偏移 加 4、做 内存_读32 用 偏移 加 8。
        old 为 做 内存_读32 用 哈希表 加 bucket 乘 4。
        做 内存_分32 用 偏移 加 40、old。
        做 内存_分32 用 哈希表 加 bucket 乘 4、i。
        i 为 i 加 1。
    完。
完。

图集_分配单元 接受 零参：
    若 全局图集 等于 0：
        返还 0。
    完。
    若 全局图集 的 空闲栈顶 大于 0：
        全局图集 的 空闲栈顶 为 全局图集 的 空闲栈顶 减 1。
        返还 做 内存_读32 用 全局图集 的 空闲单元栈 加 全局图集 的 空闲栈顶 乘 4。
    完。
    若 全局图集 的 已用单元数 大于或等于 图集最大字形数：
        返还 0。
    完。
    id 为 全局图集 的 已用单元数。
    全局图集 的 已用单元数 为 全局图集 的 已用单元数 加 1。
    返还 id。
完。

图集_回收单元 接受 id：
    若 全局图集 等于 0：
        返还。
    完。
    若 全局图集 的 空闲栈顶 大于或等于 图集最大字形数：
        返还。
    完。
    做 内存_分32 用 全局图集 的 空闲单元栈 加 全局图集 的 空闲栈顶 乘 4、id。
    全局图集 的 空闲栈顶 为 全局图集 的 空闲栈顶 加 1。
完。

图集_获取位置 接受 id：
    x 为 id 取余于 每行单元数 乘 单元格宽。
    y 为 id 除以 每行单元数 乘 单元格高。
    返还 一本字典：x 对应 x、y 对应 y。
完。

图集_上传字形 接受 id、位图、宽、高：
    若 全局图集 等于 0 或者 全局图集 的 位图数据 等于 0：
        返还。
    完。
    pos 为 做 图集_获取位置 用 id。
    可变 row 为 0。
    只要 row 小于 高 并且 row 小于 单元格高：
        dst 为 全局图集 的 位图数据 加 ((pos 的 y 加 row) 乘 图集宽度 加 pos 的 x) 乘 4。
        src 为 位图 加 row 乘 宽 乘 4。
        复制宽 为 宽。
        若 复制宽 大于 单元格宽：
            复制宽 为 单元格宽。
        完。
        做 内存_复制 用 dst、src、复制宽 乘 4。
        row 为 row 加 1。
    完。
    全局GCLR统计 的 图集上传数 为 全局GCLR统计 的 图集上传数 加 1。
完。

字形_光栅化并缓存 接受 字体、字符码、字号：
    字体哈希 为 字体 的 哈希值。
    已有 为 做 AI绘制_查字形 用 字符码、字号、字体哈希。
    若 已有 不等于 0：
        返还 已有。
    完。
    glyph 为 做 字体_光栅化 用 字体、字符码、字号。
    若 glyph 等于 0 或者 glyph 的 位图 等于 0：
        返还 0。
    完。
    做 AI绘制_存字形 用 字符码、字号、字体哈希、glyph 的 位图、glyph 的 宽、glyph 的 高。
    若 全局图集 不等于 0：
        atlas_id 为 做 内存_读32 用 字形条目数组 加 (字形条目数量 减 1) 乘 48 加 24。
        做 图集_上传字形 用 atlas_id、glyph 的 位图、glyph 的 宽、glyph 的 高。
    完。
    返还 glyph 的 位图。
完。

AI绘制_合并矩形 接受 显示列表、数量：
    若 数量 小于或等于 1：
        返还 数量。
    完。
    可变 写指针 为 0。
    可变 i 为 0。
    只要 i 小于 数量：
        当前偏移 为 显示列表 加 i 乘 48。
        当前类型 为 做 内存_读32 用 当前偏移。
        当前色 为 做 内存_读32 用 当前偏移 加 4。
        若 i 加 1 小于 数量：
            下一偏移 为 显示列表 加 (i 加 1) 乘 48。
            下一类型 为 做 内存_读32 用 下一偏移。
            下一色 为 做 内存_读32 用 下一偏移 加 4。
            若 当前类型 等于 下一类型 并且 当前色 等于 下一色 并且 (当前类型 等于 DL_背景 或者 当前类型 等于 DL_边框)：
                x1 为 做 内存_读32 用 当前偏移 加 8。
                y1 为 做 内存_读32 用 当前偏移 加 12。
                w1 为 做 内存_读32 用 当前偏移 加 16。
                h1 为 做 内存_读32 用 当前偏移 加 20。
                x2 为 做 内存_读32 用 下一偏移 加 8。
                y2 为 做 内存_读32 用 下一偏移 加 12。
                w2 为 做 内存_读32 用 下一偏移 加 16。
                h2 为 做 内存_读32 用 下一偏移 加 20。
                min_x 为 x1。
                若 x2 小于 min_x：
                    min_x 为 x2。
                完。
                min_y 为 y1。
                若 y2 小于 min_y：
                    min_y 为 y2。
                完。
                max_r 为 x1 加 w1。
                若 x2 加 w2 大于 max_r：
                    max_r 为 x2 加 w2。
                完。
                max_b 为 y1 加 h1。
                若 y2 加 h2 大于 max_b：
                    max_b 为 y2 加 h2。
                完。
                写偏移 为 显示列表 加 写指针 乘 48。
                做 内存_复制 用 写偏移、当前偏移、48。
                做 内存_分32 用 写偏移 加 8、min_x。
                做 内存_分32 用 写偏移 加 12、min_y。
                做 内存_分32 用 写偏移 加 16、max_r 减 min_x。
                做 内存_分32 用 写偏移 加 20、max_b 减 min_y。
                写指针 为 写指针 加 1。
                i 为 i 加 2。
                全局GCLR统计 的 显示列表合并数 为 全局GCLR统计 的 显示列表合并数 加 1。
                继续。
            完。
        完。
        若 写指针 不等于 i：
            做 内存_复制 用 显示列表 加 写指针 乘 48、当前偏移、48。
        完。
        写指针 为 写指针 加 1。
        i 为 i 加 1。
    完。
    返还 写指针。
完。

AI绘制_智能执行显示列表 接受 canvas、显示列表、数量、渲染精度：
    若 canvas 等于 0 或者 显示列表 等于 0 或者 数量 等于 0：
        返还。
    完。
    全局GCLR统计 的 显示列表执行数 为 全局GCLR统计 的 显示列表执行数 加 1。
    开始 为 做 平台_获取高精度时间。
    合并后数量 为 做 AI绘制_合并矩形 用 显示列表、数量。
    若 渲染精度 小于或等于 1：
        做 Sk画布_执行低精度显示列表 用 canvas、显示列表、合并后数量。
    否则：
        做 Sk画布_执行显示列表 用 canvas、显示列表、合并后数量。
    完。
    耗时 为 做 平台_获取高精度时间 减 开始。
    全局GCLR统计 的 总绘制耗时 为 全局GCLR统计 的 总绘制耗时 加 耗时。
完。

Sk画布_执行低精度显示列表 接受 canvas、显示列表、数量：
    可变 i 为 0。
    只要 i 小于 数量：
        偏移 为 显示列表 加 i 乘 48。
        类型 为 做 内存_读32 用 偏移。
        若 类型 等于 DL_背景 或者 类型 等于 DL_边框：
            颜色 为 做 内存_读32 用 偏移 加 4。
            x 为 做 内存_读32 用 偏移 加 8。
            y 为 做 内存_读32 用 偏移 加 12。
            w 为 做 内存_读32 用 偏移 加 16。
            h 为 做 内存_读32 用 偏移 加 20。
            rect 为 做 SkRect_创建 用 x、y、x 加 w、y 加 h。
            paint 为 做 Sk绘制_创建。
            做 Sk绘制_设置颜色 用 paint、做 SkColor_从RGBA 用 颜色。
            做 Sk画布_drawRect 用 canvas、rect、paint。
        完。
        全局GCLR统计 的 跳过次数 为 全局GCLR统计 的 跳过次数 加 1。
        i 为 i 加 1。
    完。
完。

AI_GCLR预测字形 接受 当前字符、字号、字体哈希：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    feature 为 做 Tensor_创建 用 8。
    做 Tensor_写 用 feature、0、当前字符 除以 65536.0。
    做 Tensor_写 用 feature、1、字号 除以 72.0。
    做 Tensor_写 用 feature、2、字体哈希 除以 4294967296.0。
    做 Tensor_写 用 feature、3、全局GCLR统计 的 命中数 除以 (全局GCLR统计 的 查找总数 加 1)。
    做 Tensor_写 用 feature、4、字形条目数量 除以 字形条目容量。
    做 Tensor_写 用 feature、5、全局GCLR统计 的 淘汰数 除以 100.0。
    做 Tensor_写 用 feature、6、0.0。
    做 Tensor_写 用 feature、7、0.0。
    预测 为 做 AI总线_推理 用 bus、「GCLR_字形预测」、feature。
    做 Tensor_释放 用 feature。
    全局GCLR统计 的 AI预测数 为 全局GCLR统计 的 AI预测数 加 1。
完。

GCLR_获取统计 接受 零参：
    返还 一本字典：查找总数 对应 全局GCLR统计 的 查找总数、命中数 对应 全局GCLR统计 的 命中数、未命中数 对应 全局GCLR统计 的 未命中数、淘汰数 对应 全局GCLR统计 的 淘汰数、光栅化数 对应 全局GCLR统计 的 光栅化数、显示列表执行数 对应 全局GCLR统计 的 显示列表执行数、显示列表合并数 对应 全局GCLR统计 的 显示列表合并数、AI预测数 对应 全局GCLR统计 的 AI预测数、总绘制耗时 对应 全局GCLR统计 的 总绘制耗时、命中率 对应 全局GCLR统计 的 命中数 乘 100 除以 (全局GCLR统计 的 查找总数 加 1)。
完。

GCLR_打印统计 接受 零参：
    s 为 做 GCLR_获取统计。
    做 内核打印 用 「═══ GCLR字形缓存统计 ═══」。
    做 内核打印 用 「查找={} 命中={} 未命中={} 命中率={}%」、s 的 查找总数、s 的 命中数、s 的 未命中数、s 的 命中率。
    做 内核打印 用 「光栅化={} 淘汰={} 图集上传={}」、s 的 光栅化数、s 的 淘汰数、全局GCLR统计 的 图集上传数。
    做 内核打印 用 「显示列表[执行/合并]={}/{} AI预测={}」、s 的 显示列表执行数、s 的 显示列表合并数、s 的 AI预测数。
    做 内核打印 用 「总绘制耗时={}ms 缓存用量={}/{}」、s 的 总绘制耗时、字形条目数量、字形条目容量。
完。

GCLR_重置统计 接受 零参：
    全局GCLR统计 为 新建 GCLR统计。
完。

GCLR_批量渲染文本 接受 canvas、字体、文本、长度、x、y、字号：
    若 canvas 等于 0 或者 字体 等于 0 或者 文本 等于 0 或者 长度 等于 0：
        返还。
    完。
    字体哈希 为 字体 的 哈希值。
    可变 光标x 为 x。
    可变 i 为 0。
    只要 i 小于 长度：
        字符码 为 做 内存_读32 用 文本 加 i 乘 4。
        位图 为 做 字形_光栅化并缓存 用 字体、字符码、字号。
        若 位图 不等于 0：
            idx 为 做 字形_查找条目索引 用 字符码、字号、字体哈希。
            gw 为 单元格宽。
            gh 为 单元格高。
            若 idx 不等于 0xFFFFFFFF：
                偏移 为 字形条目数组 加 idx 乘 48。
                gw 为 做 内存_读16 用 偏移 加 20。
                gh 为 做 内存_读16 用 偏移 加 22。
            完。
            做 Sk画布_drawBitmap 用 canvas、位图、光标x、y、gw、gh。
            光标x 为 光标x 加 gw 加 1。
        完。
        i 为 i 加 1。
    完。
    若 长度 大于 0：
        最后字符 为 做 内存_读32 用 文本 加 (长度 减 1) 乘 4。
        做 AI_GCLR预测字形 用 最后字符、字号、字体哈希。
    完。
完。

字形_查找条目索引 接受 字符码、字号、字体哈希：
    若 哈希表 等于 0：
        返还 0xFFFFFFFF。
    完。
    bucket 为 做 字形哈希 用 字符码、字号、字体哈希。
    可变 idx 为 做 内存_读32 用 哈希表 加 bucket 乘 4。
    只要 idx 不等于 0xFFFFFFFF 并且 idx 小于 字形条目数量：
        偏移 为 字形条目数组 加 idx 乘 48。
        若 做 内存_读32 用 偏移 等于 字符码 并且 做 内存_读32 用 偏移 加 4 等于 字号 并且 做 内存_读32 用 偏移 加 8 等于 字体哈希：
            返还 idx。
        完。
        idx 为 做 内存_读32 用 偏移 加 40。
    完。
    返还 0xFFFFFFFF。
完。

GCLR_销毁 接受 零参：
    若 字形条目数组 不等于 0：
        做 堆_释放 用 字形条目数组。
        字形条目数组 为 0。
    完。
    若 哈希表 不等于 0：
        做 堆_释放 用 哈希表。
        哈希表 为 0。
    完。
    若 全局图集 不等于 0：
        若 全局图集 的 位图数据 不等于 0：
            做 堆_释放 用 全局图集 的 位图数据。
        完。
        若 全局图集 的 空闲单元栈 不等于 0：
            做 堆_释放 用 全局图集 的 空闲单元栈。
        完。
        做 堆_释放 用 全局图集。
        全局图集 为 0。
    完。
    字形条目数量 为 0。
    字形条目容量 为 0。
    做 GCLR_重置统计。
    做 内核打印 用 「GCLR字形缓存已销毁」。
完。

可变 _谐振预热_高频表 为 一组：（空）。

可变 _谐振预热_命中数 为 0。

谐振字形_记录频率 接受 字符码：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _谐振预热_高频表：
        若 _谐振预热_高频表 的 第 i 项 的 码 等于 字符码：
            _谐振预热_高频表 的 第 i 项 的 次数 为 _谐振预热_高频表 的 第 i 项 的 次数 加 1。
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 _谐振预热_高频表、一本字典：码 对应 字符码、次数 对应 1。
完。

谐振字形_预热 接受 字体、字号、前N：
    可变 已预热 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _谐振预热_高频表 并且 已预热 小于 前N：
        最大idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 _谐振预热_高频表：
            若 _谐振预热_高频表 的 第 j 项 的 次数 大于 _谐振预热_高频表 的 第 最大idx 项 的 次数：
                最大idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最大idx 不等于 i：
            临时 为 _谐振预热_高频表 的 第 i 项。
            _谐振预热_高频表 的 第 i 项 为 _谐振预热_高频表 的 第 最大idx 项。
            _谐振预热_高频表 的 第 最大idx 项 为 临时。
        完。
        做 字形_光栅化并缓存 用 字体、_谐振预热_高频表 的 第 i 项 的 码、字号。
        已预热 为 已预热 加 1。
        i 为 i 加 1。
    完。
    _谐振预热_命中数 为 _谐振预热_命中数 加 已预热。
    返还 已预热。
完。

GCLR_完整统计 接受 零参：
    s 为 做 GCLR_获取统计。
    返还 一组：「GCLR版本」、2、「查找总数」、s 的 查找总数、「命中数」、s 的 命中数、「命中率」、s 的 命中率、「光栅化数」、s 的 光栅化数、「AI预测数」、s 的 AI预测数、「谐振预热数」、_谐振预热_命中数。
完。

本章 收束。
