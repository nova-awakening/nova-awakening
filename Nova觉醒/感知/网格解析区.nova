《网格解析区》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·辅助存根》。

Named网格Line 是 一种 事物，含 名称、轴、索引、下一个。

网格Area 是 一种 事物，含 名称、行起始、行结束、列起始、列结束。

网格AreaMap 是 一种 事物，含 区域、区域数。

网格_解析区域模板 接受 模板字符串、区域map：
    若 模板字符串 等于 0 或者 区域map 等于 0：
        返还。
    完。
    可变 行号 为 1。
    可变 pos 为 0。
    len 为 做 字节长度 用 模板字符串。
    只要 pos 小于 len：
        若 做 字节取 用 模板字符串、pos 等于 34：
            pos 为 pos 加 1。
            可变 列号 为 1。
            只要 pos 小于 len 并且 做 字节取 用 模板字符串、pos 不等于 34：
                只要 pos 小于 len 并且 做 字节取 用 模板字符串、pos 等于 32：
                    pos 为 pos 加 1。
                完。
                若 pos 大于或等于 len 或者 做 字节取 用 模板字符串、pos 等于 34：
                    跳出。
                完。
                名称起始 为 pos。
                只要 pos 小于 len 并且 做 字节取 用 模板字符串、pos 不等于 32 并且 做 字节取 用 模板字符串、pos 不等于 34：
                    pos 为 pos 加 1。
                完。
                名称 为 做 字符串_子串 用 模板字符串、名称起始、pos。
                若 名称 不等于 0 并且 做 字节取 用 名称、0 不等于 46：
                    做 网格_注册区域单元 用 区域map、名称、行号、列号。
                完。
                列号 为 列号 加 1。
            完。
            若 pos 小于 len：
                pos 为 pos 加 1。
            完。
            行号 为 行号 加 1。
        完。
        pos 为 pos 加 1。
    完。
完。

网格_注册区域单元 接受 map、名称、行、列：
    若 map 等于 0 或者 名称 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 map 的 区域数：
        若 做 字符串_相等 用 map 的 区域 的 第 i 项 的 名称、名称：
            若 行 小于 map 的 区域 的 第 i 项 的 行起始：
                map 的 区域 的 第 i 项 的 行起始 为 行。
            完。
            若 行 加 1 大于 map 的 区域 的 第 i 项 的 行结束：
                map 的 区域 的 第 i 项 的 行结束 为 行 加 1。
            完。
            若 列 小于 map 的 区域 的 第 i 项 的 列起始：
                map 的 区域 的 第 i 项 的 列起始 为 列。
            完。
            若 列 加 1 大于 map 的 区域 的 第 i 项 的 列结束：
                map 的 区域 的 第 i 项 的 列结束 为 列 加 1。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 map 的 区域数 大于或等于 64：
        返还。
    完。
    map 的 区域 的 第 map 的 区域数 项 的 名称 为 做 字符串_复制 用 名称。
    map 的 区域 的 第 map 的 区域数 项 的 行起始 为 行。
    map 的 区域 的 第 map 的 区域数 项 的 行结束 为 行 加 1。
    map 的 区域 的 第 map 的 区域数 项 的 列起始 为 列。
    map 的 区域 的 第 map 的 区域数 项 的 列结束 为 列 加 1。
    map 的 区域数 为 map 的 区域数 加 1。
完。

网格_查找区域 接受 map、名称：
    若 map 等于 0 或者 名称 等于 0：
        返还 负 1。
    完。
    可变 i 为 0。
    只要 i 小于 map 的 区域数：
        若 做 字符串_相等 用 map 的 区域 的 第 i 项 的 名称、名称：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

网格_解析命名线 接受 命名线头、名称、轴：
    若 命名线头 等于 0 或者 名称 等于 0：
        返还 负 1。
    完。
    可变 line 为 命名线头。
    只要 line 不等于 0：
        若 line 的 轴 等于 轴 并且 做 字符串_相等 用 line 的 名称、名称：
            返还 line 的 索引。
        完。
        line 为 line 的 下一个。
    完。
    返还 负 1。
完。

本章 收束。
