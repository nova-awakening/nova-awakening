《内存管理纹》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·网缓画布结》。

引用《..·中枢·内存初始化》。

引用《.·内核直连层》。

LayerTreeHost_检查内存 接受 lth：
    若 lth 等于 0：
        返还。
    完。
    若 lth 的 纹理内存使用 小于或等于 lth 的 纹理内存限制：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 lth 的 层数量 并且 lth 的 纹理内存使用 大于 lth 的 纹理内存限制：
        cl 为 做 内存_读64 用 lth 的 层列表 加 i 乘 8。
        若 cl 不等于 0：
            总瓦片 为 cl 的 瓦片列数 乘 cl 的 瓦片行数。
            可变 j 为 0。
            只要 j 小于 总瓦片：
                tile 为 做 内存_读64 用 cl 的 瓦片列表 加 j 乘 8。
                若 tile 不等于 0 并且 tile 的 优先级 小于 1000 并且 tile 的 像素数据 不等于 0：
                    大小 为 tile 的 宽 乘 tile 的 高 乘 4。
                    做 堆_释放 用 tile 的 像素数据。
                    tile 的 像素数据 为 0。
                    tile 的 状态 为 TILE_STATE_IDLE。
                    tile 的 需要光栅化 为 真。
                    lth 的 纹理内存使用 为 lth 的 纹理内存使用 减 大小。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

Transform_是3D 接受 变换字符串：
    若 变换字符串 等于 0：
        返还 假。
    完。
    若 做 字符串包含 用 变换字符串、「translate3d」：
        返还 真。
    完。
    若 做 字符串包含 用 变换字符串、「rotate3d」：
        返还 真。
    完。
    若 做 字符串包含 用 变换字符串、「scale3d」：
        返还 真。
    完。
    若 做 字符串包含 用 变换字符串、「matrix3d」：
        返还 真。
    完。
    若 做 字符串包含 用 变换字符串、「perspective」：
        返还 真。
    完。
    若 做 字符串包含 用 变换字符串、「translateZ」：
        返还 真。
    完。
    若 做 字符串包含 用 变换字符串、「rotateX」：
        返还 真。
    完。
    若 做 字符串包含 用 变换字符串、「rotateY」：
        返还 真。
    完。
    返还 假。
完。

绘制Layer_重放显示列表 接受 pl、canvas：
    若 pl 等于 0 或者 pl 的 显示列表 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 pl 的 显示列表数量：
        cmd 为 pl 的 显示列表 的 第 i 项。
        若 cmd 的 类型 等于 0：
            做 Sk画布_fillRect 用 canvas、cmd 的 x、cmd 的 y、cmd 的 w、cmd 的 h、cmd 的 颜色。
        否则若 cmd 的 类型 等于 1：
            做 Sk画布_drawText 用 canvas、cmd 的 x、cmd 的 y、cmd 的 文本、cmd 的 颜色。
        否则若 cmd 的 类型 等于 2：
            做 Sk画布_draw图像 用 canvas、cmd 的 x、cmd 的 y、cmd 的 图像。
        完。
        i 为 i 加 1。
    完。
完。

Sk画布_translate 接受 c、x、y：
    c 的 偏移x 为 c 的 偏移x 加 x。
    c 的 偏移y 为 c 的 偏移y 加 y。
完。

Nova缓冲itmap_创建 接受 w、h：
    若 w 小于或等于 0 或者 h 小于或等于 0：
        返还 0。
    完。
    若 w 大于 32768 或者 h 大于 32768：
        返还 0。
    完。
    bmp 为 做 堆_分配 用 32。
    做 内存_写32 用 bmp、w。
    做 内存_写32 用 bmp 加 4、h。
    做 内存_写32 用 bmp 加 8、w 乘 4。
    pixel_size 为 w 乘 h 乘 4。
    pixels 为 做 堆_分配 用 pixel_size。
    若 pixels 等于 0：
        做 堆_释放 用 bmp。
        返还 0。
    完。
    做 内存_置零 用 pixels、pixel_size。
    做 内存_写64 用 bmp 加 16、pixels。
    返还 bmp。
完。

AI合成统计 是 一种 事物，含 图处层总数、纹理内存使用、纹理内存限制、合成帧数、AI提升次数、AI降级次数、AI释放纹理次数。

可变 AI合成全局统计 为 0。

AI合成_初始化 接受 零参：
    AI合成全局统计 为 做 堆_分配 用 AI合成统计。
    AI合成全局统计 的 图处层总数 为 0。
    AI合成全局统计 的 纹理内存使用 为 0。
    AI合成全局统计 的 纹理内存限制 为 256 乘 1024 乘 1024。
    AI合成全局统计 的 合成帧数 为 0。
    AI合成全局统计 的 AI提升次数 为 0。
    AI合成全局统计 的 AI降级次数 为 0。
    AI合成全局统计 的 AI释放纹理次数 为 0。
完。

AI合成_智能提升判断 接受 层：
    若 层 等于 0：
        返还 假。
    完。
    若 层 的 标志 位与 层标志_是合成的：
        返还 假。
    完。
    若 层 的 累计绘制次数 大于 10 并且 层 的 上次绘制耗时 大于 500：
        若 AI合成全局统计 不等于 0：
            AI合成全局统计 的 AI提升次数 为 AI合成全局统计 的 AI提升次数 加 1。
        完。
        返还 真。
    完。
    若 层 的 注意力权重 大于或等于 8000 并且 层 的 视口可见 大于 0：
        若 AI合成全局统计 不等于 0：
            AI合成全局统计 的 AI提升次数 为 AI合成全局统计 的 AI提升次数 加 1。
        完。
        返还 真。
    完。
    返还 假。
完。

本章 收束。
