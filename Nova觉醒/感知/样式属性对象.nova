《样式属性对象》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·日志中枢》。

引用《..·中枢·公共智能》。

引用《..·引导·引导加载器》。

引用《.·图绘渲染上下》。

引用《.·文档事件》。

引用《.·内核直连层》。

引用《.·CSS属性过渡》。

引用《..·中枢·内存初始化》。

引用《..·标准·加法运算符》。

引用《..·中枢·内容可见性》。

动画状态_idle 为 0。

动画状态_pending 为 1。

动画状态_running 为 2。

动画状态_paused 为 3。

动画状态_finished 为 4。

方向_normal 为 0。

方向_reverse 为 1。

方向_alternate 为 2。

方向_alternate_reverse 为 3。

填充_none 为 0。

填充_forwards 为 1。

填充_backwards 为 2。

填充_both 为 3。

缓动_linear 为 0。

缓动_ease 为 1。

缓动_ease_in 为 2。

缓动_ease_out 为 3。

缓动_ease_in_out 为 4。

缓动_step_start 为 5。

缓动_step_end 为 6。

可变 动画_总创建数 为 0。

可变 动画_活跃数 为 0。

可变 动画_完成数 为 0。

Animation 是 一种 事物，含 ID、效果、状态、当前时间、开始时间、播放速率、onfinish、oncancel。

KeyframeEffect 是 一种 事物，含 名称、目标元素、持续时间、延迟、迭代次数、方向、填充模式、缓动、关键帧列表、关键帧数。

Keyframe 是 一种 事物，含 偏移、属性列表、属性数、缓动。

Animation_创建 接受 效果：
    anim 为 新建 Animation：ID 为 动画_总创建数、效果 为 效果、状态 为 动画状态_idle、当前时间 为 0、开始时间 为 0、播放速率 为 1000、onfinish 为 0、oncancel 为 0。
    动画_总创建数 为 动画_总创建数 加 1。
    返还 anim。
完。

Animation_play 接受 anim：
    若 anim 等于 空：
        返还。
    完。
    anim 的 状态 为 动画状态_running。
    anim 的 开始时间 为 做 平台_获取高精度时间。
    动画_活跃数 为 动画_活跃数 加 1。
完。

Animation_pause 接受 anim：
    若 anim 等于 空 或者 anim 的 状态 不等于 动画状态_running：
        返还。
    完。
    anim 的 状态 为 动画状态_paused。
    动画_活跃数 为 动画_活跃数 减 1。
完。

Animation_cancel 接受 anim：
    若 anim 等于 空：
        返还。
    完。
    若 anim 的 状态 等于 动画状态_running：
        动画_活跃数 为 动画_活跃数 减 1。
    完。
    anim 的 状态 为 动画状态_idle。
    anim 的 当前时间 为 0。
    若 anim 的 oncancel 不等于 0：
        做 调用回调 用 anim 的 oncancel。
    完。
完。

Animation_finish 接受 anim：
    若 anim 等于 空：
        返还。
    完。
    若 anim 的 状态 等于 动画状态_running：
        动画_活跃数 为 动画_活跃数 减 1。
    完。
    anim 的 状态 为 动画状态_finished。
    动画_完成数 为 动画_完成数 加 1。
    若 anim 的 onfinish 不等于 0：
        做 调用回调 用 anim 的 onfinish。
    完。
完。

KeyframeEffect_创建 接受 名称、元素、持续时间：
    返还 新建 KeyframeEffect：名称 为 名称、目标元素 为 元素、持续时间 为 持续时间、延迟 为 0、迭代次数 为 1000、方向 为 方向_normal、填充模式 为 填充_none、缓动 为 缓动_ease、关键帧列表 为 一组：（空）、关键帧数 为 0。
完。

KeyframeEffect_添加关键帧 接受 效果、偏移、属性列表、属性数：
    若 效果 等于 空：
        返还。
    完。
    做 追加 用 效果 的 关键帧列表、新建 Keyframe：偏移 为 偏移、属性列表 为 属性列表、属性数 为 属性数、缓动 为 效果 的 缓动。
    效果 的 关键帧数 为 效果 的 关键帧数 加 1。
完。

TimingFunction_创建 接受 类型：
    返还 类型。
完。

TimingFunction_计算 接受 类型、t：
    若 类型 等于 缓动_linear：
        返还 t。
    完。
    若 类型 等于 缓动_ease：
        返还 做 cubic_bezier 用 t、250、100、250、1000。
    完。
    若 类型 等于 缓动_ease_in：
        返还 做 cubic_bezier 用 t、420、0、1000、1000。
    完。
    若 类型 等于 缓动_ease_out：
        返还 做 cubic_bezier 用 t、0、0、580、1000。
    完。
    若 类型 等于 缓动_ease_in_out：
        返还 做 cubic_bezier 用 t、420、0、580、1000。
    完。
    若 类型 等于 缓动_step_start：
        返还 1000。
    完。
    若 类型 等于 缓动_step_end：
        若 t 大于或等于 1000：
            返还 1000。
        完。
        返还 0。
    完。
    返还 t。
完。

cubic_bezier 接受 t、x1、y1、x2、y2：
    t2 为 t 乘 t 除以 1000。
    t3 为 t2 乘 t 除以 1000。
    结果 为 3 乘 (1000 减 t) 乘 (1000 减 t) 除以 1000 乘 t 除以 1000 乘 y1 除以 1000 加 3 乘 (1000 减 t) 乘 t2 除以 1000 乘 y2 除以 1000 加 t3。
    若 结果 大于 1000：
        结果 为 1000。
    完。
    若 结果 小于 0：
        结果 为 0。
    完。
    返还 结果。
完。

Animation_更新 接受 anim、当前时间：
    若 anim 等于 空 或者 anim 的 状态 不等于 动画状态_running：
        返还。
    完。
    效果 为 anim 的 效果。
    若 效果 等于 空：
        返还。
    完。
    已过 为 (当前时间 减 anim 的 开始时间) 乘 anim 的 播放速率 除以 1000。
    本地时间 为 已过 减 效果 的 延迟。
    若 本地时间 小于 0：
        返还。
    完。
    迭代时长 为 效果 的 持续时间。
    若 迭代时长 小于或等于 0：
        迭代时长 为 1。
    完。
    总时长 为 迭代时长 乘 效果 的 迭代次数 除以 1000。
    若 本地时间 大于或等于 总时长：
        做 Animation_finish 用 anim。
        返还。
    完。
    当前迭代 为 本地时间 除以 迭代时长。
    迭代进度 为 本地时间 取余于 迭代时长 乘 1000 除以 迭代时长。
    可变 有效进度 为 迭代进度。
    若 效果 的 方向 等于 方向_reverse：
        有效进度 为 1000 减 迭代进度。
    否则若 效果 的 方向 等于 方向_alternate：
        若 当前迭代 取余于 2 等于 1：
            有效进度 为 1000 减 迭代进度。
        完。
    否则若 效果 的 方向 等于 方向_alternate_reverse：
        若 当前迭代 取余于 2 等于 0：
            有效进度 为 1000 减 迭代进度。
        完。
    完。
    有效进度 为 做 TimingFunction_计算 用 效果 的 缓动、有效进度。
    anim 的 当前时间 为 当前时间。
    做 Animation_应用关键帧 用 效果、有效进度。
完。

Animation_应用关键帧 接受 效果、进度：
    若 效果 等于 空 或者 效果 的 关键帧数 小于 2：
        返还。
    完。
    可变 前帧 为 效果 的 关键帧列表 的 第 0 项。
    可变 后帧 为 效果 的 关键帧列表 的 第 效果 的 关键帧数 减 1 项。
    可变 i 为 0。
    只要 i 小于 效果 的 关键帧数 减 1：
        若 效果 的 关键帧列表 的 第 i 项 的 偏移 小于或等于 进度 并且 效果 的 关键帧列表 的 第 i 加 1 项 的 偏移 大于或等于 进度：
            前帧 为 效果 的 关键帧列表 的 第 i 项。
            后帧 为 效果 的 关键帧列表 的 第 i 加 1 项。
            跳出。
        完。
        i 为 i 加 1。
    完。
    帧范围 为 后帧 的 偏移 减 前帧 的 偏移。
    可变 因子 为 0。
    若 帧范围 大于 0：
        因子 为 (进度 减 前帧 的 偏移) 乘 1000 除以 帧范围。
    完。
完。

CSSAnimation_应用 接受 元素、样式：
    若 元素 等于 0 或者 样式 等于 0：
        返还。
    完。
    若 样式 的 animation_name 等于 0：
        返还。
    完。
    规则 为 做 Keyframes_查找 用 样式 的 animation_name。
    若 规则 等于 0：
        返还。
    完。
    持续时间 为 样式 的 animation_duration。
    若 持续时间 小于或等于 0：
        持续时间 为 0。
    完。
    ke 为 做 KeyframeEffect_创建 用 样式 的 animation_name、元素、持续时间。
    ke 的 延迟 为 样式 的 animation_delay。
    ke 的 迭代次数 为 样式 的 animation_iteration_count。
    ke 的 方向 为 样式 的 animation_direction。
    ke 的 填充模式 为 样式 的 animation_fill_mode。
    ke 的 缓动 为 做 TimingFunction_创建 用 样式 的 animation_timing。
    anim 为 做 Animation_创建 用 ke。
    若 样式 的 animation_play_state 等于 0：
        做 Animation_play 用 anim。
    完。
完。

RAFEntry 是 一种 事物，含 回调、ID、已取消。

可变 全局RAF列表 为 0。

可变 全局RAF数量 为 0。

可变 全局RAF容量 为 0。

可变 全局RAF_下一个ID 为 1。

RAF_请求 接受 回调：
    若 全局RAF列表 等于 0：
        全局RAF容量 为 64。
        全局RAF列表 为 做 堆_分配_数组 用 全局RAF容量 乘 16。
        全局RAF数量 为 0。
    完。
    若 全局RAF数量 大于或等于 全局RAF容量：
        返还 0。
    完。
    id 为 全局RAF_下一个ID。
    全局RAF_下一个ID 为 全局RAF_下一个ID 加 1。
    偏移 为 全局RAF列表 加 全局RAF数量 乘 16。
    做 内存_写64 用 偏移、回调。
    做 内存_写32 用 偏移 加 8、id。
    做 内存_写8 用 偏移 加 12、0。
    全局RAF数量 为 全局RAF数量 加 1。
    返还 id。
完。

RAF_取消 接受 id：
    可变 i 为 0。
    只要 i 小于 全局RAF数量：
        偏移 为 全局RAF列表 加 i 乘 16。
        若 做 内存_读32 用 偏移 加 8 等于 id：
            做 内存_写8 用 偏移 加 12、1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

RAF_执行所有 接受 Nova运行时实例、时间戳：
    若 全局RAF列表 等于 0 或者 全局RAF数量 等于 0：
        返还。
    完。
    当前列表 为 全局RAF列表。
    当前数量 为 全局RAF数量。
    全局RAF列表 为 做 堆_分配_数组 用 全局RAF容量 乘 16。
    全局RAF数量 为 0。
    可变 i 为 0。
    只要 i 小于 当前数量：
        偏移 为 当前列表 加 i 乘 16。
        已取消 为 做 内存_读8 用 偏移 加 12。
        若 非 已取消：
            回调 为 做 内存_读64 用 偏移。
            若 回调 不等于 0 并且 Nova运行时实例 不等于 0：
                args 为 做 堆_分配 用 8。
                做 内存_写64 用 args、做 Nova值_创建_Number 用 Nova运行时实例、时间戳。
                帧 为 做 InterpreterFrame_创建 用 0、0。
                做 Interpreter_调用函数 用 Nova运行时实例、回调、Nova运行时实例 的 undefined值、args、1、帧。
            完。
        完。
        i 为 i 加 1。
    完。
    做 堆_释放 用 当前列表。
完。

本章 收束。
