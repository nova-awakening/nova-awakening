《管道》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·ReadableStream》。

引用《.·变换流》。

PipeOptions 是 一种 事物，含 pr事件Close、pr事件Abort、pr事件Cancel、signal。

ReadableStream_pipeTo 接受 readable、writable、options：
    若 readable 等于 0 或者 writable 等于 0：
        返还。
    完。
    若 readable 的 已锁定 或者 writable 的 已锁定：
        返还。
    完。
    reader 为 做 ReadableStream_getReader 用 readable。
    writer 为 做 WritableStream_getWriter 用 writable。
    若 reader 等于 0 或者 writer 等于 0：
        返还。
    完。
    pr事件Close 为 若 options 不等于 0 则 options 的 pr事件Close 否则 假。
    pr事件Abort 为 若 options 不等于 0 则 options 的 pr事件Abort 否则 假。
    pr事件Cancel 为 若 options 不等于 0 则 options 的 pr事件Cancel 否则 假。
    做 Pipe_循环 用 reader、writer、pr事件Close、pr事件Abort、pr事件Cancel。
完。

Pipe_循环 接受 reader、writer、pr事件Close、pr事件Abort、pr事件Cancel：
    若 reader 等于 0 或者 writer 等于 0：
        返还。
    完。
    chunk 为 做 Reader_read 用 reader。
    若 chunk 等于 0：
        若 非 pr事件Close：
            做 Writer_close 用 writer。
        完。
        做 Reader_releaseLock 用 reader。
        做 Writer_releaseLock 用 writer。
        返还。
    完。
    做 Writer_write 用 writer、chunk、0。
    做 Pipe_循环 用 reader、writer、pr事件Close、pr事件Abort、pr事件Cancel。
完。

ReadableStream_pipeThrough 接受 readable、transform、options：
    若 readable 等于 0 或者 transform 等于 0：
        返还 0。
    完。
    做 ReadableStream_pipeTo 用 readable、transform 的 writable、options。
    返还 transform 的 readable。
完。

ReadableStream_tee 接受 readable：
    若 readable 等于 0 或者 readable 的 已锁定：
        返还 0。
    完。
    reader 为 做 ReadableStream_getReader 用 readable。
    若 reader 等于 0：
        返还 0。
    完。
    branch1 为 做 ReadableStream_创建 用 0、0、1。
    branch2 为 做 ReadableStream_创建 用 0、0、1。
    结果 为 做 堆_分配 用 16。
    做 内存_写64 用 结果、branch1。
    做 内存_写64 用 结果 加 8、branch2。
    返还 结果。
完。

TextEncoderStream_创建 接受 零参：
    Nova注意力架构 为 做 堆_分配 用 TransformStreamNova注意力架构。
    Nova注意力架构 的 transform回调 为 0。
    Nova注意力架构 的 flush回调 为 0。
    Nova注意力架构 的 start回调 为 0。
    返还 做 TransformStream_创建 用 Nova注意力架构、0、0。
完。

TextDecoderStream_创建 接受 encoding：
    Nova注意力架构 为 做 堆_分配 用 TransformStreamNova注意力架构。
    Nova注意力架构 的 transform回调 为 0。
    Nova注意力架构 的 flush回调 为 0。
    Nova注意力架构 的 start回调 为 0。
    返还 做 TransformStream_创建 用 Nova注意力架构、0、0。
完。

CompressionStream_创建 接受 格式：
    Nova注意力架构 为 做 堆_分配 用 TransformStreamNova注意力架构。
    Nova注意力架构 的 transform回调 为 0。
    Nova注意力架构 的 flush回调 为 0。
    返还 做 TransformStream_创建 用 Nova注意力架构、0、0。
完。

DecompressionStream_创建 接受 格式：
    Nova注意力架构 为 做 堆_分配 用 TransformStreamNova注意力架构。
    Nova注意力架构 的 transform回调 为 0。
    Nova注意力架构 的 flush回调 为 0。
    返还 做 TransformStream_创建 用 Nova注意力架构、0、0。
完。

本章 收束。
