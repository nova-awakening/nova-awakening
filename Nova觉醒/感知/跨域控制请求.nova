《跨域控制请求》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·辅助存根》。

引用《..·中枢·整数转字符串》。

引用《.·暴露头过滤》。

引用《..·外联·HTTP发送与解析》。

引用《..·中枢·字符串替换》。

引用《.·兼容桥》。

引用《.·历史与位置接口》。

预检缓存大小 为 256。

预检缓存_默认TTL 为 86400。

白名单最大数 为 128。

审计日志最大数 为 512。

威胁检测窗口大小 为 64。

外部。

全局AI总线。

Origin 是 一种 事物，含 协议、域名、端口。

预检缓存项 是 一种 事物，含 原源键、方法、允许方法、允许头、允许凭证、最大缓存秒、创建时间、命中次数。

CORS审计日志 是 一种 事物，含 时间戳、原源、目标URL、方法、结果、原因、威胁分数。

CORS统计 是 一种 事物，含 总请求数、允许数、拒绝数、预检请求数、预检缓存命中数、凭证请求数、威胁检测数、AI分析次数。

可变 cors_预检缓存 为 一组：（空）。

可变 cors_白名单 为 一组：（空）。

可变 cors_审计日志 为 一组：（空）。

可变 cors_统计 为 新建 CORS统计。

可变 cors_已初始化 为 假。

CORS_MODE_SAME_ORIGIN 为 0。

CORS_MODE_CORS 为 1。

CORS_MODE_NO_CORS 为 2。

CORS_MODE_NAVIGATE 为 3。

CORS_初始化 接受 零参：
    若 cors_已初始化：
        返还 真。
    完。
    cors_预检缓存 为 一组：（空）。
    cors_白名单 为 一组：（空）。
    cors_审计日志 为 一组：（空）。
    cors_统计 的 总请求数 为 0。
    cors_统计 的 允许数 为 0。
    cors_统计 的 拒绝数 为 0。
    cors_统计 的 预检请求数 为 0。
    cors_统计 的 预检缓存命中数 为 0。
    cors_统计 的 凭证请求数 为 0。
    cors_统计 的 威胁检测数 为 0。
    cors_统计 的 AI分析次数 为 0。
    cors_已初始化 为 真。
    返还 真。
完。

Origin_解析 接受 原源字符串：
    o 为 新建 Origin。
    若 原源字符串 等于 空 或者 做 字符串_长度 用 原源字符串 等于 0：
        返还 o。
    完。
    协议结束 为 做 字符串_查找 用 原源字符串、「://」。
    若 协议结束 小于 0：
        o 的 域名 为 原源字符串。
        返还 o。
    完。
    o 的 协议 为 做 字符串_子串 用 原源字符串、0、协议结束。
    剩余 为 做 字符串_子串 用 原源字符串、协议结束 加 3、做 字符串_长度 用 原源字符串。
    端口位置 为 做 字符串_查找 用 剩余、「:」。
    若 端口位置 大于或等于 0：
        o 的 域名 为 做 字符串_子串 用 剩余、0、端口位置。
        o 的 端口 为 做 字符串_转整数 用 做 字符串_子串 用 剩余、端口位置 加 1、做 字符串_长度 用 剩余。
    否则：
        o 的 域名 为 剩余。
        o 的 端口 为 做 Origin_默认端口 用 o 的 协议。
    完。
    返还 o。
完。

Origin_默认端口 接受 协议：
    若 做 字符串_相等 用 协议、「http」：
        返还 80。
    完。
    若 做 字符串_相等 用 协议、「https」：
        返还 443。
    完。
    若 做 字符串_相等 用 协议、「ftp」：
        返还 21。
    完。
    返还 0。
完。

Origin_序列化 接受 原源：
    若 原源 等于 空：
        返还 「null」。
    完。
    可变 结果 为 原源 的 协议 加 「://」 加 原源 的 域名。
    默认端口 为 做 Origin_默认端口 用 原源 的 协议。
    若 原源 的 端口 不等于 0 并且 原源 的 端口 不等于 默认端口：
        结果 为 结果 加 「:」 加 做 转文本 用 原源 的 端口。
    完。
    返还 结果。
完。

Origin_相等 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 假。
    完。
    若 非 做 字符串_相等_忽略大小写 用 a 的 协议、b 的 协议：
        返还 假。
    完。
    若 非 做 字符串_相等_忽略大小写 用 a 的 域名、b 的 域名：
        返还 假。
    完。
    端口A 为 a 的 端口。
    端口B 为 b 的 端口。
    若 端口A 等于 0：
        端口A 为 做 Origin_默认端口 用 a 的 协议。
    完。
    若 端口B 等于 0：
        端口B 为 做 Origin_默认端口 用 b 的 协议。
    完。
    返还 端口A 等于 端口B。
完。

CORS_是简单方法 接受 方法：
    若 做 字符串_相等 用 方法、「GET」：
        返还 真。
    完。
    若 做 字符串_相等 用 方法、「HEAD」：
        返还 真。
    完。
    若 做 字符串_相等 用 方法、「POST」：
        返还 真。
    完。
    返还 假。
完。

CORS_是简单头 接受 头名：
    若 做 字符串_相等_忽略大小写 用 头名、「accept」：
        返还 真。
    完。
    若 做 字符串_相等_忽略大小写 用 头名、「accept-language」：
        返还 真。
    完。
    若 做 字符串_相等_忽略大小写 用 头名、「content-language」：
        返还 真。
    完。
    若 做 字符串_相等_忽略大小写 用 头名、「content-type」：
        返还 真。
    完。
    返还 假。
完。

CORS_是简单Content_Type 接受 值：
    若 做 字符串_包含_忽略大小写 用 值、「text/plain」：
        返还 真。
    完。
    若 做 字符串_包含_忽略大小写 用 值、「multipart/form-data」：
        返还 真。
    完。
    若 做 字符串_包含_忽略大小写 用 值、「application/x-www-form-urlencoded」：
        返还 真。
    完。
    返还 假。
完。

CORS_需要预检 接受 方法、头列表、头数：
    若 非 做 CORS_是简单方法 用 方法：
        返还 真。
    完。
    可变 i 为 0。
    只要 i 小于 头数：
        头名 为 做 内存_读64 用 头列表 加 i 乘 16。
        若 非 做 CORS_是简单头 用 头名：
            返还 真。
        完。
        若 做 字符串_相等_忽略大小写 用 头名、「content-type」：
            头值 为 做 内存_读64 用 头列表 加 i 乘 16 加 8。
            若 非 做 CORS_是简单Content_Type 用 头值：
                返还 真。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

CORSResult 是 一种 事物，含 允许、错误、允许方法、允许头、允许凭证、最大缓存秒。

CORS_检查响应 接受 请求源、响应、含凭证：
    若 非 cors_已初始化：
        做 CORS_初始化。
    完。
    cors_统计 的 总请求数 为 cors_统计 的 总请求数 加 1。
    若 含凭证：
        cors_统计 的 凭证请求数 为 cors_统计 的 凭证请求数 加 1。
    完。
    若 响应 等于 0：
        cors_统计 的 拒绝数 为 cors_统计 的 拒绝数 加 1。
        返还 做 CORS_拒绝 用 「网络错误」。
    完。
    result 为 新建 CORSResult。
    result 的 允许 为 假。
    原源串 为 做 Origin_序列化 用 请求源。
    若 做 CORS_在白名单 用 原源串：
        result 的 允许 为 真。
        cors_统计 的 允许数 为 cors_统计 的 允许数 加 1。
        做 CORS_审计记录 用 原源串、「」、「」、0、「白名单允许」、0.0。
        返还 result。
    完。
    allow_origin 为 做 HTTP_获取响应头 用 响应、「Access-Control-Allow-Origin」。
    若 allow_origin 等于 0：
        result 的 错误 为 「缺少Access-Control-Allow-Origin头」。
        cors_统计 的 拒绝数 为 cors_统计 的 拒绝数 加 1。
        做 CORS_审计记录 用 原源串、「」、「」、1、result 的 错误、0.0。
        返还 result。
    完。
    若 做 字符串_相等 用 allow_origin、「*」：
        若 含凭证：
            result 的 错误 为 「带凭证请求不允许Access-Control-Allow-Origin: *」。
            cors_统计 的 拒绝数 为 cors_统计 的 拒绝数 加 1。
            做 CORS_审计记录 用 原源串、「」、「」、1、result 的 错误、0.5。
            返还 result。
        完。
        result 的 允许 为 真。
    否则：
        若 非 做 字符串_相等 用 allow_origin、原源串：
            result 的 错误 为 「Access-Control-Allow-Origin不匹配请求源」。
            cors_统计 的 拒绝数 为 cors_统计 的 拒绝数 加 1。
            做 CORS_审计记录 用 原源串、「」、「」、1、result 的 错误、0.0。
            返还 result。
        完。
        result 的 允许 为 真。
    完。
    若 含凭证：
        allow_cred 为 做 HTTP_获取响应头 用 响应、「Access-Control-Allow-Credentials」。
        若 非 做 字符串_相等 用 allow_cred、「true」：
            result 的 允许 为 假。
            result 的 错误 为 「带凭证请求需要Allow-Credentials: true」。
            cors_统计 的 拒绝数 为 cors_统计 的 拒绝数 加 1。
            返还 result。
        完。
        result 的 允许凭证 为 真。
    完。
    max_age 为 做 HTTP_获取响应头 用 响应、「Access-Control-Max-Age」。
    若 max_age 不等于 0：
        result 的 最大缓存秒 为 做 字符串_转整数 用 max_age。
    完。
    result 的 允许方法 为 做 HTTP_获取响应头 用 响应、「Access-Control-Allow-Methods」。
    result 的 允许头 为 做 HTTP_获取响应头 用 响应、「Access-Control-Allow-Headers」。
    cors_统计 的 允许数 为 cors_统计 的 允许数 加 1。
    做 CORS_审计记录 用 原源串、「」、「」、0、「允许」、0.0。
    返还 result。
完。

CORS_拒绝 接受 原因：
    r 为 新建 CORSResult。
    r 的 允许 为 假。
    r 的 错误 为 原因。
    返还 r。
完。

CORS_预检缓存_查找 接受 原源、方法：
    当前时间 为 做 系统_获取时间戳。
    可变 i 为 0。
    只要 i 小于 做 计数 用 cors_预检缓存：
        项 为 cors_预检缓存 的 第 i 项。
        若 做 字符串_相等 用 项 的 原源键、原源 并且 做 字符串_相等 用 项 的 方法、方法：
            过期 为 项 的 创建时间 加 项 的 最大缓存秒 乘 1000。
            若 当前时间 大于 过期：
                cors_预检缓存 为 做 数组_删除 用 cors_预检缓存、i。
                返还 空。
            完。
            项 的 命中次数 为 项 的 命中次数 加 1。
            cors_统计 的 预检缓存命中数 为 cors_统计 的 预检缓存命中数 加 1。
            返还 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

CORS_预检缓存_存储 接受 result、原源、方法：
    若 result 等于 空 或者 非 result 的 允许：
        返还。
    完。
    若 做 计数 用 cors_预检缓存 大于或等于 预检缓存大小：
        可变 最旧索引 为 0。
        可变 最旧时间 为 cors_预检缓存 的 第 0 项 的 创建时间。
        可变 i 为 1。
        只要 i 小于 做 计数 用 cors_预检缓存：
            若 cors_预检缓存 的 第 i 项 的 创建时间 小于 最旧时间：
                最旧索引 为 i。
                最旧时间 为 cors_预检缓存 的 第 i 项 的 创建时间。
            完。
            i 为 i 加 1。
        完。
        cors_预检缓存 为 做 数组_删除 用 cors_预检缓存、最旧索引。
    完。
    项 为 新建 预检缓存项。
    项 的 原源键 为 原源。
    项 的 方法 为 方法。
    项 的 允许方法 为 result 的 允许方法。
    项 的 允许头 为 result 的 允许头。
    项 的 允许凭证 为 result 的 允许凭证。
    项 的 最大缓存秒 为 result 的 最大缓存秒。
    若 项 的 最大缓存秒 等于 0：
        项 的 最大缓存秒 为 预检缓存_默认TTL。
    完。
    项 的 创建时间 为 做 系统_获取时间戳。
    项 的 命中次数 为 0。
    做 追加 用 cors_预检缓存、项。
完。

CORS_添加白名单 接受 原源：
    若 做 计数 用 cors_白名单 大于或等于 白名单最大数：
        返还 假。
    完。
    若 做 CORS_在白名单 用 原源：
        返还 真。
    完。
    做 追加 用 cors_白名单、原源。
    返还 真。
完。

CORS_移除白名单 接受 原源：
    可变 i 为 0。
    只要 i 小于 做 计数 用 cors_白名单：
        若 做 字符串_相等 用 cors_白名单 的 第 i 项、原源：
            cors_白名单 为 做 数组_删除 用 cors_白名单、i。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

CORS_在白名单 接受 原源：
    可变 i 为 0。
    只要 i 小于 做 计数 用 cors_白名单：
        若 做 字符串_相等 用 cors_白名单 的 第 i 项、原源：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

CORS_审计记录 接受 原源、目标、方法、结果、原因、威胁分：
    日志 为 新建 CORS审计日志。
    日志 的 时间戳 为 做 系统_获取时间戳。
    日志 的 原源 为 原源。
    日志 的 目标URL 为 目标。
    日志 的 方法 为 方法。
    日志 的 结果 为 结果。
    日志 的 原因 为 原因。
    日志 的 威胁分数 为 威胁分。
    做 追加 用 cors_审计日志、日志。
    若 做 计数 用 cors_审计日志 大于 审计日志最大数：
        cors_审计日志 为 做 数组_切片 用 cors_审计日志、1、做 计数 用 cors_审计日志。
    完。
完。

CORS_安全审计响应 接受 响应：
    若 响应 等于 0：
        返还 空。
    完。
    可变 警告列表 为 一组：（空）。
    allow_origin 为 做 HTTP_获取响应头 用 响应、「Access-Control-Allow-Origin」。
    allow_cred 为 做 HTTP_获取响应头 用 响应、「Access-Control-Allow-Credentials」。
    若 做 字符串_相等 用 allow_origin、「*」 并且 做 字符串_相等 用 allow_cred、「true」：
        做 追加 用 警告列表、「危险: 通配符+凭证同时启用」。
    完。
    allow_methods 为 做 HTTP_获取响应头 用 响应、「Access-Control-Allow-Methods」。
    若 做 字符串_相等 用 allow_methods、「*」：
        做 追加 用 警告列表、「警告: 允许所有HTTP方法」。
    完。
    allow_headers 为 做 HTTP_获取响应头 用 响应、「Access-Control-Allow-Headers」。
    若 做 字符串_相等 用 allow_headers、「*」：
        做 追加 用 警告列表、「警告: 允许所有请求头」。
    完。
    expose_headers 为 做 HTTP_获取响应头 用 响应、「Access-Control-Expose-Headers」。
    若 expose_headers 不等于 0：
        若 做 字符串_包含_忽略大小写 用 expose_headers、「Authorization」：
            做 追加 用 警告列表、「警告: 暴露Authorization头」。
        完。
        若 做 字符串_包含_忽略大小写 用 expose_headers、「Set-Cookie」：
            做 追加 用 警告列表、「危险: 暴露Set-Cookie头」。
        完。
    完。
    返还 警告列表。
完。

CORS_AI威胁检测 接受 原源、目标URL、方法：
    若 全局AI总线 等于 空：
        返还 0.0。
    完。
    特征 为 做 Tensor_创建 用 12。
    做 Tensor_写 用 特征、0、做 字符串_长度 用 原源。
    做 Tensor_写 用 特征、1、做 字符串_长度 用 目标URL。
    做 Tensor_写 用 特征、2、做 CORS_方法编码 用 方法。
    做 Tensor_写 用 特征、3、cors_统计 的 总请求数。
    做 Tensor_写 用 特征、4、cors_统计 的 拒绝数。
    做 Tensor_写 用 特征、5、cors_统计 的 威胁检测数。
    可变 近期拒绝 为 0。
    可变 近期总数 为 0。
    可变 k 为 做 计数 用 cors_审计日志 减 1。
    只要 k 大于或等于 0 并且 近期总数 小于 威胁检测窗口大小：
        近期总数 为 近期总数 加 1。
        若 cors_审计日志 的 第 k 项 的 结果 等于 1：
            近期拒绝 为 近期拒绝 加 1。
        完。
        k 为 k 减 1。
    完。
    做 Tensor_写 用 特征、6、近期拒绝。
    做 Tensor_写 用 特征、7、近期总数。
    做 Tensor_写 用 特征、8、cors_统计 的 凭证请求数。
    做 Tensor_写 用 特征、9、cors_统计 的 预检请求数。
    做 Tensor_写 用 特征、10、做 计数 用 cors_白名单。
    做 Tensor_写 用 特征、11、做 计数 用 cors_预检缓存。
    威胁分 为 做 AI总线_异常检测 用 全局AI总线、「CORS威胁」、特征。
    做 Tensor_释放 用 特征。
    cors_统计 的 AI分析次数 为 cors_统计 的 AI分析次数 加 1。
    若 威胁分 大于 0.7：
        cors_统计 的 威胁检测数 为 cors_统计 的 威胁检测数 加 1。
    完。
    返还 威胁分。
完。

CORS_方法编码 接受 方法：
    若 做 字符串_相等 用 方法、「GET」：
        返还 0。
    完。
    若 做 字符串_相等 用 方法、「POST」：
        返还 1。
    完。
    若 做 字符串_相等 用 方法、「PUT」：
        返还 2。
    完。
    若 做 字符串_相等 用 方法、「DELETE」：
        返还 3。
    完。
    若 做 字符串_相等 用 方法、「PATCH」：
        返还 4。
    完。
    若 做 字符串_相等 用 方法、「OPTIONS」：
        返还 5。
    完。
    若 做 字符串_相等 用 方法、「HEAD」：
        返还 6。
    完。
    返还 7。
完。

CORS_获取统计 接受 零参：
    返还 cors_统计。
完。

CORS_打印统计 接受 零参：
    做 内核打印 用 「═══ NCRC CORS统计 ═══」。
    做 内核打印 用 「总请求: 」 加 做 转文本 用 cors_统计 的 总请求数。
    做 内核打印 用 「允许/拒绝: 」 加 做 转文本 用 cors_统计 的 允许数 加 「/」 加 做 转文本 用 cors_统计 的 拒绝数。
    做 内核打印 用 「预检/缓存命中: 」 加 做 转文本 用 cors_统计 的 预检请求数 加 「/」 加 做 转文本 用 cors_统计 的 预检缓存命中数。
    做 内核打印 用 「凭证请求: 」 加 做 转文本 用 cors_统计 的 凭证请求数。
    做 内核打印 用 「威胁检测: 」 加 做 转文本 用 cors_统计 的 威胁检测数。
    做 内核打印 用 「AI分析: 」 加 做 转文本 用 cors_统计 的 AI分析次数。
    做 内核打印 用 「白名单条数: 」 加 做 转文本 用 做 计数 用 cors_白名单。
    做 内核打印 用 「缓存条数: 」 加 做 转文本 用 做 计数 用 cors_预检缓存。
    做 内核打印 用 「审计日志数: 」 加 做 转文本 用 做 计数 用 cors_审计日志。
完。

CORS_发送预检 接受 原源、目标URL、方法、自定义头：
    cors_统计 的 预检请求数 为 cors_统计 的 预检请求数 加 1。
    原源串 为 做 Origin_序列化 用 原源。
    缓存 为 做 CORS_预检缓存_查找 用 原源串、方法。
    若 缓存 不等于 空：
        result 为 新建 CORSResult。
        result 的 允许 为 真。
        result 的 允许方法 为 缓存 的 允许方法。
        result 的 允许头 为 缓存 的 允许头。
        result 的 允许凭证 为 缓存 的 允许凭证。
        做 CORS_审计记录 用 原源串、目标URL、方法、2、「预检缓存命中」、0.0。
        返还 result。
    完。
    威胁 为 做 CORS_AI威胁检测 用 原源串、目标URL、方法。
    若 威胁 大于 0.9：
        做 CORS_审计记录 用 原源串、目标URL、方法、1、「AI威胁检测拦截」、威胁。
        返还 做 CORS_拒绝 用 「AI检测到高威胁跨域请求」。
    完。
    响应 为 做 HTTP_发送请求 用 「OPTIONS」、目标URL、一组：「Origin: 」 加 原源串、「Access-Control-Request-Method: 」 加 方法、「Access-Control-Request-Headers: 」 加 自定义头。
    result 为 做 CORS_检查响应 用 原源、响应、假。
    若 result 的 允许：
        做 CORS_预检缓存_存储 用 result、原源串、方法。
    完。
    警告 为 做 CORS_安全审计响应 用 响应。
    若 警告 不等于 空 并且 做 计数 用 警告 大于 0：
        可变 w 为 0。
        只要 w 小于 做 计数 用 警告：
            做 内核打印 用 「[NCRC 安全警告] 」 加 警告 的 第 w 项。
            w 为 w 加 1。
        完。
    完。
    返还 result。
完。

CORS_缓存_清理过期 接受 零参：
    当前时间 为 做 系统_获取时间戳。
    可变 i 为 做 计数 用 cors_预检缓存 减 1。
    只要 i 大于或等于 0：
        项 为 cors_预检缓存 的 第 i 项。
        过期 为 项 的 创建时间 加 项 的 最大缓存秒 乘 1000。
        若 当前时间 大于 过期：
            cors_预检缓存 为 做 数组_删除 用 cors_预检缓存、i。
        完。
        i 为 i 减 1。
    完。
完。

CORS_重置 接受 零参：
    cors_预检缓存 为 一组：（空）。
    cors_白名单 为 一组：（空）。
    cors_审计日志 为 一组：（空）。
    cors_统计 为 新建 CORS统计。
    cors_已初始化 为 假。
    做 内核打印 用 「[NCRC] CORS子系统已重置」。
完。

可变 _谐振CORS_评分次数 为 0。

谐振CORS_威胁评分 接受 来源、方法、自定义头数：
    可变 风险 为 0。
    若 方法 不等于 「GET」 并且 方法 不等于 「HEAD」：
        风险 为 风险 加 300。
    完。
    若 方法 等于 「DELETE」 或者 方法 等于 「PUT」：
        风险 为 风险 加 200。
    完。
    风险 为 风险 加 自定义头数 乘 50。
    若 做 字符串长度 用 来源 大于 256：
        风险 为 风险 加 400。
    完。
    _谐振CORS_评分次数 为 _谐振CORS_评分次数 加 1。
    返还 风险。
完。

跨域控制_完整统计 接受 零参：
    返还 一组：「预检缓存数」、做 计数 用 cors_预检缓存、「白名单数」、做 计数 用 cors_白名单、「审计日志数」、做 计数 用 cors_审计日志、「谐振威胁评分次数」、_谐振CORS_评分次数。
完。

谐振CORS_是否高风险 接受 来源、方法、头数：
    返还 做 谐振CORS_威胁评分 用 来源、方法、头数 大于 500。
完。

本章 收束。
