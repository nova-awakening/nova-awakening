《特性检测》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

引用《..·中枢·沙箱隔离进》。

SUPPORTS_COND_PROP 为 0。

SUPPORTS_COND_SELECTOR 为 1。

SUPPORTS_COND_AND 为 2。

SUPPORTS_COND_OR 为 3。

SUPPORTS_COND_NOT 为 4。

SupportsCondition 是 一种 事物，含 类型、属性名、属性值、选择器、子条件、子条件数、是否否定。

Supports_求值 接受 条件：
    若 条件 等于 0：
        返还 假。
    完。
    若 条件 的 类型 等于 SUPPORTS_COND_PROP：
        返还 做 CSS_属性是否支持 用 条件 的 属性名、条件 的 属性值。
    完。
    若 条件 的 类型 等于 SUPPORTS_COND_SELECTOR：
        返还 做 CSS_选择器是否支持 用 条件 的 选择器。
    完。
    若 条件 的 类型 等于 SUPPORTS_COND_AND：
        可变 i 为 0。
        只要 i 小于 条件 的 子条件数：
            若 非 做 Supports_求值 用 做 内存_读64 用 条件 的 子条件 加 i 乘 8：
                返还 假。
            完。
            i 为 i 加 1。
        完。
        返还 真。
    完。
    若 条件 的 类型 等于 SUPPORTS_COND_OR：
        可变 i 为 0。
        只要 i 小于 条件 的 子条件数：
            若 做 Supports_求值 用 做 内存_读64 用 条件 的 子条件 加 i 乘 8：
                返还 真。
            完。
            i 为 i 加 1。
        完。
        返还 假。
    完。
    若 条件 的 类型 等于 SUPPORTS_COND_NOT：
        若 条件 的 子条件数 大于 0：
            返还 非 做 Supports_求值 用 做 内存_读64 用 条件 的 子条件。
        完。
    完。
    返还 假。
完。

CSS_属性是否支持 接受 属性名、属性值：
    若 属性名 等于 0：
        返还 假。
    完。
    若 做 字符串_相等 用 属性名、「display」：
        返还 真。
    完。
    若 做 字符串_相等 用 属性名、「grid-template-columns」：
        返还 真。
    完。
    若 做 字符串_相等 用 属性名、「gap」：
        返还 真。
    完。
    若 做 字符串_相等 用 属性名、「container-type」：
        返还 真。
    完。
    若 做 字符串_相等 用 属性名、「container-name」：
        返还 真。
    完。
    若 做 字符串_相等 用 属性名、「aspect-ratio」：
        返还 真。
    完。
    若 做 字符串_相等 用 属性名、「content-visibility」：
        返还 真。
    完。
    若 做 字符串_相等 用 属性名、「scroll-snap-type」：
        返还 真。
    完。
    若 做 字符串_相等 用 属性名、「overscroll-behavior」：
        返还 真。
    完。
    若 做 字符串_开头匹配 用 属性名、「--」：
        返还 真。
    完。
    返还 真。
完。

CSS_选择器是否支持 接受 选择器字符串：
    若 选择器字符串 等于 0：
        返还 假。
    完。
    若 做 字符串_包含 用 选择器字符串、「:has」：
        返还 真。
    完。
    若 做 字符串_包含 用 选择器字符串、「:is」：
        返还 真。
    完。
    若 做 字符串_包含 用 选择器字符串、「:where」：
        返还 真。
    完。
    若 做 字符串_包含 用 选择器字符串、「:not」：
        返还 真。
    完。
    返还 真。
完。

CSS_supports_JS 接受 属性名、属性值：
    返还 做 CSS_属性是否支持 用 属性名、属性值。
完。

CSS_supports_JS_条件 接受 条件字符串：
    若 条件字符串 等于 0 或者 做 字节长度 用 条件字符串 等于 0：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
