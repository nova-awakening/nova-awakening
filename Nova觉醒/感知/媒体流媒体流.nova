《媒体流媒体流》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·字符串替换》。

最大轨道数 为 16。

最大流数 为 32。

媒体流统计 是 一种 事物，含 流创建次数、流销毁次数、轨道创建次数、轨道停止次数、轨道克隆次数、addTrack次数、removeTrack次数、getUserMedia次数、getDisplayMedia次数、权限请求次数、权限授予次数、权限拒绝次数、约束应用次数、AI设备调用次数、AI设备命中次数、总处理耗时_纳秒、错误总数、静音切换次数。

AI媒体设备状态 是 一种 事物，含 历史音频请求率、历史视频请求率、历史屏幕共享率、历史平均分辨率_宽、历史平均分辨率_高、连续音频请求数、预测下次请求类型、采样计数器、学习权重。

可变 全局媒体流统计 为 0。

可变 全局设备AI 为 0。

可变 全局媒体流已初始化 为 假。

媒体流_初始化全局 接受 零参：
    若 全局媒体流已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局媒体流统计 为 做 堆_分配_清零 用 144。
    若 全局媒体流统计 等于 空：
        返还 错误_内存不足。
    完。
    全局设备AI 为 做 堆_分配_清零 用 48。
    若 全局设备AI 不等于 空：
        全局设备AI 的 预测下次请求类型 为 1。
        全局设备AI 的 学习权重 为 128。
    完。
    全局媒体流已初始化 为 真。
    做 打印 用 「[媒体流] 初始化完成」。
    返还 0。
完。

媒体流_销毁全局 接受 零参：
    若 全局媒体流统计 不等于 空：
        做 堆_释放 用 全局媒体流统计。
        全局媒体流统计 为 0。
    完。
    若 全局设备AI 不等于 空：
        做 堆_释放 用 全局设备AI。
        全局设备AI 为 0。
    完。
    全局媒体流已初始化 为 假。
    返还 0。
完。

TRACK_KIND_AUDIO 为 0。

TRACK_KIND_VIDEO 为 1。

TRACK_STATE_LIVE 为 0。

TRACK_STATE_ENDED 为 1。

媒体StreamTrack 是 一种 事物，含 id、kind、label、状态、启用、静音、宽、高、帧率、采样率、onended、onmute、onunmute。

媒体Stream 是 一种 事物，含 id、轨道列表、轨道数量、轨道容量、活跃。

媒体Stream_创建 接受 零参：
    ms 为 做 堆_分配 用 媒体Stream。
    ms 的 id 为 0。
    ms 的 轨道容量 为 8。
    ms 的 轨道列表 为 做 堆_分配_数组 用 8 乘 8。
    ms 的 轨道数量 为 0。
    ms 的 活跃 为 真。
    返还 ms。
完。

媒体Stream_addTrack 接受 ms、track：
    若 ms 等于 0 或者 track 等于 0：
        返还。
    完。
    若 ms 的 轨道数量 小于 ms 的 轨道容量：
        做 内存_写64 用 ms 的 轨道列表 加 ms 的 轨道数量 乘 8、track。
        ms 的 轨道数量 为 ms 的 轨道数量 加 1。
    完。
完。

媒体Stream_get音频Tracks 接受 ms：
    若 ms 等于 0：
        返还 一组：0、0。
    完。
    结果 为 做 堆_分配_数组 用 ms 的 轨道数量 乘 8。
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 ms 的 轨道数量：
        t 为 做 内存_读64 用 ms 的 轨道列表 加 i 乘 8。
        若 t 不等于 0 并且 t 的 kind 等于 TRACK_KIND_AUDIO：
            做 内存_写64 用 结果 加 计数 乘 8、t。
            计数 为 计数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：结果、计数。
完。

媒体Stream_get视频Tracks 接受 ms：
    若 ms 等于 0：
        返还 一组：0、0。
    完。
    结果 为 做 堆_分配_数组 用 ms 的 轨道数量 乘 8。
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 ms 的 轨道数量：
        t 为 做 内存_读64 用 ms 的 轨道列表 加 i 乘 8。
        若 t 不等于 0 并且 t 的 kind 等于 TRACK_KIND_VIDEO：
            做 内存_写64 用 结果 加 计数 乘 8、t。
            计数 为 计数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：结果、计数。
完。

Navigator_getUser媒体 接受 Nova运行时实例、约束：
    stream 为 做 媒体Stream_创建。
    audio 为 做 堆_分配 用 媒体StreamTrack。
    audio 的 kind 为 TRACK_KIND_AUDIO。
    audio 的 状态 为 TRACK_STATE_LIVE。
    audio 的 启用 为 真。
    audio 的 静音 为 假。
    audio 的 采样率 为 48000。
    做 媒体Stream_addTrack 用 stream、audio。
    video 为 做 堆_分配 用 媒体StreamTrack。
    video 的 kind 为 TRACK_KIND_VIDEO。
    video 的 状态 为 TRACK_STATE_LIVE。
    video 的 启用 为 真。
    video 的 静音 为 假。
    video 的 宽 为 1280。
    video 的 高 为 720。
    video 的 帧率 为 30。
    做 媒体Stream_addTrack 用 stream、video。
    返还 stream。
完。

Navigator_getDisplay媒体 接受 Nova运行时实例、约束：
    stream 为 做 媒体Stream_创建。
    video 为 做 堆_分配 用 媒体StreamTrack。
    video 的 kind 为 TRACK_KIND_VIDEO。
    video 的 状态 为 TRACK_STATE_LIVE。
    video 的 启用 为 真。
    video 的 宽 为 1920。
    video 的 高 为 1080。
    video 的 帧率 为 30。
    做 媒体Stream_addTrack 用 stream、video。
    若 全局媒体流统计 不等于 空：
        全局媒体流统计 的 getDisplayMedia次数 为 全局媒体流统计 的 getDisplayMedia次数 加 1。
    完。
    做 AI媒体设备_记录 用 2、1920、1080。
    返还 stream。
完。

媒体StreamTrack_stop 接受 track：
    若 track 等于 空：
        返还。
    完。
    若 track 的 状态 等于 TRACK_STATE_ENDED：
        返还。
    完。
    track 的 状态 为 TRACK_STATE_ENDED。
    track 的 启用 为 假。
    若 track 的 onended 不等于 空：
    完。
    若 全局媒体流统计 不等于 空：
        全局媒体流统计 的 轨道停止次数 为 全局媒体流统计 的 轨道停止次数 加 1。
    完。
完。

媒体StreamTrack_clone 接受 track：
    若 track 等于 空：
        返还 空。
    完。
    新轨道 为 做 堆_分配 用 媒体StreamTrack。
    若 新轨道 等于 空：
        返还 空。
    完。
    新轨道 的 kind 为 track 的 kind。
    新轨道 的 label 为 track 的 label。
    新轨道 的 状态 为 TRACK_STATE_LIVE。
    新轨道 的 启用 为 track 的 启用。
    新轨道 的 静音 为 track 的 静音。
    新轨道 的 宽 为 track 的 宽。
    新轨道 的 高 为 track 的 高。
    新轨道 的 帧率 为 track 的 帧率。
    新轨道 的 采样率 为 track 的 采样率。
    新轨道 的 onended 为 0。
    新轨道 的 onmute 为 0。
    新轨道 的 onunmute 为 0。
    若 全局媒体流统计 不等于 空：
        全局媒体流统计 的 轨道克隆次数 为 全局媒体流统计 的 轨道克隆次数 加 1。
        全局媒体流统计 的 轨道创建次数 为 全局媒体流统计 的 轨道创建次数 加 1。
    完。
    返还 新轨道。
完。

媒体StreamTrack_设置启用 接受 track、启用：
    若 track 等于 空：
        返还。
    完。
    track 的 启用 为 启用。
完。

媒体StreamTrack_设置静音 接受 track、静音：
    若 track 等于 空：
        返还。
    完。
    旧值 为 track 的 静音。
    track 的 静音 为 静音。
    若 旧值 不等于 静音：
        若 静音 等于 真 并且 track 的 onmute 不等于 空：
        否则若 静音 等于 假 并且 track 的 onunmute 不等于 空：
        完。
        若 全局媒体流统计 不等于 空：
            全局媒体流统计 的 静音切换次数 为 全局媒体流统计 的 静音切换次数 加 1。
        完。
    完。
完。

媒体StreamTrack_获取设置 接受 track：
    若 track 等于 空：
        返还 空。
    完。
    设置 为 做 堆_分配_清零 用 64。
    若 设置 等于 空：
        返还 空。
    完。
    做 内存_写32 用 设置、track 的 宽。
    做 内存_写32 用 设置 加 4、track 的 高。
    做 内存_写32 用 设置 加 8、track 的 帧率。
    做 内存_写32 用 设置 加 12、track 的 采样率。
    做 内存_写8 用 设置 加 16、track 的 kind。
    返还 设置。
完。

媒体StreamTrack_获取能力 接受 track：
    若 track 等于 空：
        返还 空。
    完。
    能力 为 做 堆_分配_清零 用 128。
    若 能力 等于 空：
        返还 空。
    完。
    若 track 的 kind 等于 TRACK_KIND_VIDEO：
        做 内存_写32 用 能力、160。
        做 内存_写32 用 能力 加 4、3840。
        做 内存_写32 用 能力 加 8、120。
        做 内存_写32 用 能力 加 12、2160。
        做 内存_写32 用 能力 加 16、1。
        做 内存_写32 用 能力 加 20、60。
    否则若 track 的 kind 等于 TRACK_KIND_AUDIO：
        做 内存_写32 用 能力 加 24、8000。
        做 内存_写32 用 能力 加 28、96000。
        做 内存_写32 用 能力 加 32、1。
        做 内存_写32 用 能力 加 36、2。
    完。
    返还 能力。
完。

媒体StreamTrack_应用约束 接受 track、约束宽、约束高、约束帧率：
    若 track 等于 空：
        返还 错误_无效参数。
    完。
    若 track 的 kind 不等于 TRACK_KIND_VIDEO：
        返还 错误_无效参数。
    完。
    若 约束宽 大于或等于 160 并且 约束宽 小于或等于 3840：
        track 的 宽 为 约束宽。
    完。
    若 约束高 大于或等于 120 并且 约束高 小于或等于 2160：
        track 的 高 为 约束高。
    完。
    若 约束帧率 大于或等于 1 并且 约束帧率 小于或等于 60：
        track 的 帧率 为 约束帧率。
    完。
    若 全局媒体流统计 不等于 空：
        全局媒体流统计 的 约束应用次数 为 全局媒体流统计 的 约束应用次数 加 1。
    完。
    返还 0。
完。

媒体StreamTrack_销毁 接受 track：
    若 track 等于 空：
        返还。
    完。
    做 堆_释放 用 track。
完。

媒体Stream_removeTrack 接受 ms、track：
    若 ms 等于 空 或者 track 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 ms 的 轨道数量：
        若 做 内存_读64 用 ms 的 轨道列表 加 i 乘 8 等于 track：
            ms 的 轨道数量 为 ms 的 轨道数量 减 1。
            若 i 小于 ms 的 轨道数量：
                做 内存_写64 用 ms 的 轨道列表 加 i 乘 8、做 内存_读64 用 ms 的 轨道列表 加 ms 的 轨道数量 乘 8。
            完。
            若 全局媒体流统计 不等于 空：
                全局媒体流统计 的 removeTrack次数 为 全局媒体流统计 的 removeTrack次数 加 1。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

媒体Stream_clone 接受 ms：
    若 ms 等于 空：
        返还 空。
    完。
    新流 为 做 媒体Stream_创建。
    若 新流 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 ms 的 轨道数量：
        原轨道 为 做 内存_读64 用 ms 的 轨道列表 加 i 乘 8。
        若 原轨道 不等于 空：
            克隆轨道 为 做 媒体StreamTrack_clone 用 原轨道。
            若 克隆轨道 不等于 空：
                做 媒体Stream_addTrack 用 新流、克隆轨道。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 新流。
完。

媒体Stream_销毁 接受 ms：
    若 ms 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 ms 的 轨道数量：
        track 为 做 内存_读64 用 ms 的 轨道列表 加 i 乘 8。
        若 track 不等于 空：
            做 媒体StreamTrack_stop 用 track。
        完。
        i 为 i 加 1。
    完。
    若 ms 的 轨道列表 不等于 空：
        做 堆_释放 用 ms 的 轨道列表。
    完。
    做 堆_释放 用 ms。
    若 全局媒体流统计 不等于 空：
        全局媒体流统计 的 流销毁次数 为 全局媒体流统计 的 流销毁次数 加 1。
    完。
完。

媒体Stream_获取轨道数 接受 ms：
    若 ms 等于 空：
        返还 0。
    完。
    返还 ms 的 轨道数量。
完。

媒体Stream_是否活跃 接受 ms：
    若 ms 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 ms 的 轨道数量：
        track 为 做 内存_读64 用 ms 的 轨道列表 加 i 乘 8。
        若 track 不等于 空 并且 track 的 状态 等于 TRACK_STATE_LIVE：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

AI媒体设备_记录 接受 请求类型、宽、高：
    若 全局设备AI 等于 空：
        返还。
    完。
    AI 为 全局设备AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    若 请求类型 等于 0：
        AI 的 历史音频请求率 为 (AI 的 历史音频请求率 乘 15 加 1024) 除以 16。
        AI 的 连续音频请求数 为 AI 的 连续音频请求数 加 1。
    否则若 请求类型 等于 1：
        AI 的 历史视频请求率 为 (AI 的 历史视频请求率 乘 15 加 1024) 除以 16。
        AI 的 连续音频请求数 为 0。
    否则若 请求类型 等于 2：
        AI 的 历史屏幕共享率 为 (AI 的 历史屏幕共享率 乘 15 加 1024) 除以 16。
        AI 的 连续音频请求数 为 0。
    完。
    AI 的 历史平均分辨率_宽 为 (AI 的 历史平均分辨率_宽 乘 7 加 宽) 除以 8。
    AI 的 历史平均分辨率_高 为 (AI 的 历史平均分辨率_高 乘 7 加 高) 除以 8。
    若 AI 的 采样计数器 小于 8：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 全局媒体流统计 不等于 空：
        全局媒体流统计 的 AI设备调用次数 为 全局媒体流统计 的 AI设备调用次数 加 1。
    完。
    若 AI 的 历史视频请求率 大于 AI 的 历史音频请求率 并且 AI 的 历史视频请求率 大于 AI 的 历史屏幕共享率：
        AI 的 预测下次请求类型 为 1。
    否则若 AI 的 历史屏幕共享率 大于 AI 的 历史视频请求率：
        AI 的 预测下次请求类型 为 2。
    否则：
        AI 的 预测下次请求类型 为 0。
    完。
    若 AI 的 预测下次请求类型 等于 请求类型：
        若 全局媒体流统计 不等于 空：
            全局媒体流统计 的 AI设备命中次数 为 全局媒体流统计 的 AI设备命中次数 加 1。
        完。
    完。
完。

媒体流_打印统计 接受 零参：
    若 全局媒体流统计 等于 空：
        做 打印 用 「[媒体流] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   媒体流+轨道 · 统计报告            │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 流创建/销毁:      」、做 字符串拼接 用 做 转字符串 用 全局媒体流统计 的 流创建次数、做 字符串拼接 用 「/」、做 转字符串 用 全局媒体流统计 的 流销毁次数。
    做 打印 用 做 字符串拼接 用 「│ 轨道创建:         」、做 转字符串 用 全局媒体流统计 的 轨道创建次数。
    做 打印 用 做 字符串拼接 用 「│ 轨道停止:         」、做 转字符串 用 全局媒体流统计 的 轨道停止次数。
    做 打印 用 做 字符串拼接 用 「│ 轨道克隆:         」、做 转字符串 用 全局媒体流统计 的 轨道克隆次数。
    做 打印 用 做 字符串拼接 用 「│ addTrack:         」、做 转字符串 用 全局媒体流统计 的 addTrack次数。
    做 打印 用 做 字符串拼接 用 「│ removeTrack:      」、做 转字符串 用 全局媒体流统计 的 removeTrack次数。
    做 打印 用 做 字符串拼接 用 「│ getUserMedia:     」、做 转字符串 用 全局媒体流统计 的 getUserMedia次数。
    做 打印 用 做 字符串拼接 用 「│ getDisplayMedia:  」、做 转字符串 用 全局媒体流统计 的 getDisplayMedia次数。
    做 打印 用 做 字符串拼接 用 「│ 权限请求:         」、做 转字符串 用 全局媒体流统计 的 权限请求次数。
    做 打印 用 做 字符串拼接 用 「│ 权限授予:         」、做 转字符串 用 全局媒体流统计 的 权限授予次数。
    做 打印 用 做 字符串拼接 用 「│ 约束应用:         」、做 转字符串 用 全局媒体流统计 的 约束应用次数。
    做 打印 用 做 字符串拼接 用 「│ 静音切换:         」、做 转字符串 用 全局媒体流统计 的 静音切换次数。
    做 打印 用 做 字符串拼接 用 「│ AI设备调用:       」、做 转字符串 用 全局媒体流统计 的 AI设备调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI设备命中:       」、做 转字符串 用 全局媒体流统计 的 AI设备命中次数。
    做 打印 用 做 字符串拼接 用 「│ 总耗时(μs):       」、做 转字符串 用 全局媒体流统计 的 总处理耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 错误:             」、做 转字符串 用 全局媒体流统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

媒体流_导出诊断 接受 零参：
    若 全局设备AI 等于 空：
        做 打印 用 「[设备AI] 未初始化」。
        返还。
    完。
    AI 为 全局设备AI。
    做 打印 用 「[设备AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史音频请求率(Q10): 」、做 转字符串 用 AI 的 历史音频请求率。
    做 打印 用 做 字符串拼接 用 「  历史视频请求率(Q10): 」、做 转字符串 用 AI 的 历史视频请求率。
    做 打印 用 做 字符串拼接 用 「  历史屏幕共享率(Q10): 」、做 转字符串 用 AI 的 历史屏幕共享率。
    做 打印 用 做 字符串拼接 用 「  历史平均分辨率: 」、做 字符串拼接 用 做 转字符串 用 AI 的 历史平均分辨率_宽、做 字符串拼接 用 「×」、做 转字符串 用 AI 的 历史平均分辨率_高。
    做 打印 用 做 字符串拼接 用 「  预测请求类型: 」、做 转字符串 用 AI 的 预测下次请求类型。
完。

媒体流_获取统计 接受 零参：
    返还 全局媒体流统计。
完。

媒体流_获取AI状态 接受 零参：
    返还 全局设备AI。
完。

媒体流_是否已初始化 接受 零参：
    返还 全局媒体流已初始化。
完。

本章 收束。
