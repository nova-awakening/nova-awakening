《矩阵创建》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《.·精确三角函数》。

引用《..·中枢·内存初始化》。

引用《.·样式子网格》。

引用《..·执行·底层内存操》。

Matrix4x4 是 一种 事物，含 m。

Matrix4x4_创建 接受 零参：
    mat 为 做 堆_分配 用 Matrix4x4。
    mat 的 m 为 做 堆_分配_数组 用 16 乘 4。
    做 Matrix4x4_单位化 用 mat。
    返还 mat。
完。

Matrix4x4_单位化 接受 mat：
    做 内存_清零 用 mat 的 m、64。
    做 内存_写32 用 mat 的 m、10000。
    做 内存_写32 用 mat 的 m 加 20、10000。
    做 内存_写32 用 mat 的 m 加 40、10000。
    做 内存_写32 用 mat 的 m 加 60、10000。
完。

Matrix4x4_获取 接受 mat、行、列：
    返还 做 内存_读32 用 mat 的 m 加 (行 乘 4 加 列) 乘 4。
完。

Matrix4x4_设置 接受 mat、行、列、值：
    做 内存_写32 用 mat 的 m 加 (行 乘 4 加 列) 乘 4、值。
完。

Matrix4x4_乘 接受 a、b：
    结果 为 做 Matrix4x4_创建。
    可变 i 为 0。
    只要 i 小于 4：
        可变 j 为 0。
        只要 j 小于 4：
            可变 和 为 0。
            可变 k 为 0。
            只要 k 小于 4：
                和 为 和 加 做 Matrix4x4_获取 用 a、i、k 乘 做 Matrix4x4_获取 用 b、k、j 除以 10000。
                k 为 k 加 1。
            完。
            做 Matrix4x4_设置 用 结果、i、j、和。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

Transform_translate 接受 tx、ty：
    mat 为 做 Matrix4x4_创建。
    做 Matrix4x4_设置 用 mat、0、3、tx 乘 100。
    做 Matrix4x4_设置 用 mat、1、3、ty 乘 100。
    返还 mat。
完。

Transform_translate3d 接受 tx、ty、tz：
    mat 为 做 Matrix4x4_创建。
    做 Matrix4x4_设置 用 mat、0、3、tx 乘 100。
    做 Matrix4x4_设置 用 mat、1、3、ty 乘 100。
    做 Matrix4x4_设置 用 mat、2、3、tz 乘 100。
    返还 mat。
完。

Transform_scale 接受 sx、sy：
    mat 为 做 Matrix4x4_创建。
    做 Matrix4x4_设置 用 mat、0、0、sx 乘 100。
    做 Matrix4x4_设置 用 mat、1、1、sy 乘 100。
    返还 mat。
完。

Transform_scale3d 接受 sx、sy、sz：
    mat 为 做 Matrix4x4_创建。
    做 Matrix4x4_设置 用 mat、0、0、sx 乘 100。
    做 Matrix4x4_设置 用 mat、1、1、sy 乘 100。
    做 Matrix4x4_设置 用 mat、2、2、sz 乘 100。
    返还 mat。
完。

Transform_rotate 接受 角度_度：
    弧度 为 角度_度 乘 175 除以 10000。
    cos_v 为 做 定点_cos_精确 用 弧度。
    sin_v 为 做 定点_sin_精确 用 弧度。
    mat 为 做 Matrix4x4_创建。
    做 Matrix4x4_设置 用 mat、0、0、cos_v。
    做 Matrix4x4_设置 用 mat、0、1、0 减 sin_v。
    做 Matrix4x4_设置 用 mat、1、0、sin_v。
    做 Matrix4x4_设置 用 mat、1、1、cos_v。
    返还 mat。
完。

Transform_rotate3d 接受 x、y、z、角度_度：
    弧度 为 角度_度 乘 175 除以 10000。
    cos_v 为 做 定点_cos_精确 用 弧度。
    sin_v 为 做 定点_sin_精确 用 弧度。
    one_c 为 10000 减 cos_v。
    长度 为 做 整数_sqrt 用 x 乘 x 加 y 乘 y 加 z 乘 z。
    若 长度 等于 0：
        返还 做 Matrix4x4_创建。
    完。
    nx 为 x 乘 10000 除以 长度。
    ny 为 y 乘 10000 除以 长度。
    nz 为 z 乘 10000 除以 长度。
    mat 为 做 Matrix4x4_创建。
    做 Matrix4x4_设置 用 mat、0、0、cos_v 加 nx 乘 nx 乘 one_c 除以 100000000。
    做 Matrix4x4_设置 用 mat、0、1、nx 乘 ny 乘 one_c 除以 100000000 减 nz 乘 sin_v 除以 10000。
    做 Matrix4x4_设置 用 mat、0、2、nx 乘 nz 乘 one_c 除以 100000000 加 ny 乘 sin_v 除以 10000。
    做 Matrix4x4_设置 用 mat、1、0、ny 乘 nx 乘 one_c 除以 100000000 加 nz 乘 sin_v 除以 10000。
    做 Matrix4x4_设置 用 mat、1、1、cos_v 加 ny 乘 ny 乘 one_c 除以 100000000。
    做 Matrix4x4_设置 用 mat、1、2、ny 乘 nz 乘 one_c 除以 100000000 减 nx 乘 sin_v 除以 10000。
    做 Matrix4x4_设置 用 mat、2、0、nz 乘 nx 乘 one_c 除以 100000000 减 ny 乘 sin_v 除以 10000。
    做 Matrix4x4_设置 用 mat、2、1、nz 乘 ny 乘 one_c 除以 100000000 加 nx 乘 sin_v 除以 10000。
    做 Matrix4x4_设置 用 mat、2、2、cos_v 加 nz 乘 nz 乘 one_c 除以 100000000。
    返还 mat。
完。

Transform_rotateX 接受 角度_度：
    返还 做 Transform_rotate3d 用 10000、0、0、角度_度。
完。

Transform_rotateY 接受 角度_度：
    返还 做 Transform_rotate3d 用 0、10000、0、角度_度。
完。

Transform_rotateZ 接受 角度_度：
    返还 做 Transform_rotate 用 角度_度。
完。

Transform_skewX 接受 角度_度：
    tan_v 为 做 定点_tan 用 角度_度 乘 175 除以 10000。
    mat 为 做 Matrix4x4_创建。
    做 Matrix4x4_设置 用 mat、0、1、tan_v。
    返还 mat。
完。

Transform_skewY 接受 角度_度：
    tan_v 为 做 定点_tan 用 角度_度 乘 175 除以 10000。
    mat 为 做 Matrix4x4_创建。
    做 Matrix4x4_设置 用 mat、1、0、tan_v。
    返还 mat。
完。

Transform_perspective 接受 距离：
    若 距离 小于或等于 0：
        返还 做 Matrix4x4_创建。
    完。
    mat 为 做 Matrix4x4_创建。
    做 Matrix4x4_设置 用 mat、3、2、0 减 10000 乘 100 除以 距离。
    返还 mat。
完。

Transform_matrix 接受 a、b、c、d、tx、ty：
    mat 为 做 Matrix4x4_创建。
    做 Matrix4x4_设置 用 mat、0、0、a 乘 100。
    做 Matrix4x4_设置 用 mat、1、0、b 乘 100。
    做 Matrix4x4_设置 用 mat、0、1、c 乘 100。
    做 Matrix4x4_设置 用 mat、1、1、d 乘 100。
    做 Matrix4x4_设置 用 mat、0、3、tx 乘 100。
    做 Matrix4x4_设置 用 mat、1、3、ty 乘 100。
    返还 mat。
完。

可变 AI_矩阵创建_调用数 为 0。

可变 AI_矩阵创建_异常数 为 0。

AI_矩阵创建_监控 接受 零参：
    AI_矩阵创建_调用数 为 AI_矩阵创建_调用数 加 1。
    返还 AI_矩阵创建_异常数 等于 0。
完。

AI_矩阵创建_健康检查 接受 零参：
    若 AI_矩阵创建_调用数 大于 0 并且 AI_矩阵创建_异常数 乘 100 除以 AI_矩阵创建_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
