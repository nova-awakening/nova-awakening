《格式检测》

引用《..·稳态·图处显示》。

引用《.·图压缩基础》。

引用《.·内核直连层》。

引用《..·中枢·字符串替换》。

引用《.·图像解码器》。

引用《.·内存管理纹》。

引用《..·执行·控制台输出》。

Sk图像_检测格式 接受 数据、长度：
    若 长度 小于 4：
        返还 0。
    完。
    b0 为 做 字节取 用 数据、0。
    b1 为 做 字节取 用 数据、1。
    b2 为 做 字节取 用 数据、2。
    b3 为 做 字节取 用 数据、3。
    若 b0 等于 0x89 并且 b1 等于 0x50 并且 b2 等于 0x4E 并且 b3 等于 0x47：
        返还 IMAGE_FORMAT_PNG。
    完。
    若 b0 等于 0xFF 并且 b1 等于 0xD8 并且 b2 等于 0xFF：
        返还 IMAGE_FORMAT_JPEG。
    完。
    若 b0 等于 0x52 并且 b1 等于 0x49 并且 b2 等于 0x46 并且 b3 等于 0x46 并且 长度 大于或等于 12：
        若 做 字节取 用 数据、8 等于 0x57 并且 做 字节取 用 数据、9 等于 0x45 并且 做 字节取 用 数据、10 等于 0x42 并且 做 字节取 用 数据、11 等于 0x50：
            返还 IMAGE_FORMAT_WEBP。
        完。
    完。
    若 b0 等于 0x47 并且 b1 等于 0x49 并且 b2 等于 0x46 并且 b3 等于 0x38：
        返还 IMAGE_FORMAT_GIF。
    完。
    若 b0 等于 0x42 并且 b1 等于 0x4D：
        返还 IMAGE_FORMAT_BMP。
    完。
    返还 0。
完。

Sk图像_预读尺寸 接受 img：
    若 img 等于 0 或者 img 的 原始数据 等于 0：
        返还。
    完。
    做 匹配 用 img 的 图像格式。
    img 的 颜色类型 为 COLORTYPE_RGBA8888。
完。

JPEG_预读尺寸 接受 img：
    若 img 等于 0 或者 img 的 原始数据 等于 0：
        返还。
    完。
    可变 i 为 2。
    只要 i 小于 img 的 原始长度 减 8：
        若 做 字节取 用 img 的 原始数据、i 不等于 0xFF：
            i 为 i 加 1。
            继续。
        完。
        标记 为 做 字节取 用 img 的 原始数据、i 加 1。
        若 标记 等于 0xC0 或者 标记 等于 0xC2：
            img 的 高 为 做 字节取 用 img 的 原始数据、i 加 5 左移 8 位或 做 字节取 用 img 的 原始数据、i 加 6。
            img 的 宽 为 做 字节取 用 img 的 原始数据、i 加 7 左移 8 位或 做 字节取 用 img 的 原始数据、i 加 8。
            返还。
        完。
        段长 为 做 字节取 用 img 的 原始数据、i 加 2 左移 8 位或 做 字节取 用 img 的 原始数据、i 加 3。
        i 为 i 加 2 加 段长。
    完。
完。

Sk图像_确保解码 接受 img：
    若 img 等于 0 或者 img 的 已解码：
        返还。
    完。
    若 img 的 原始数据 等于 0：
        返还。
    完。
    做 匹配 用 img 的 图像格式。
    img 的 已解码 为 真。
完。

BMP_签名 为 0x4D42。

BMP_头大小_V3 为 40。

BMP_头大小_V4 为 108。

BMP_头大小_V5 为 124。

BMP头信息 是 一种 事物，含 宽、高、位深、压缩方式、图像大小、像素偏移、颜色表大小、红掩码、绿掩码、蓝掩码、透明掩码。

BMP_解析头 接受 数据、长度：
    若 数据 等于 0 或者 长度 小于 54：
        返还 空。
    完。
    签名 为 做 字节取 用 数据、0 位或 做 字节取 用 数据、1 左移 8。
    若 签名 不等于 BMP_签名：
        做 打印 用 「[BMP] 错误: 无效的BMP签名」。
        返还 空。
    完。
    可变 头 为 做 堆_分配 用 BMP头信息。
    若 头 等于 0：
        返还 空。
    完。
    头 的 像素偏移 为 做 字节取 用 数据、10 位或 做 字节取 用 数据、11 左移 8 位或 做 字节取 用 数据、12 左移 16 位或 做 字节取 用 数据、13 左移 24。
    DIB头大小 为 做 字节取 用 数据、14 位或 做 字节取 用 数据、15 左移 8 位或 做 字节取 用 数据、16 左移 16 位或 做 字节取 用 数据、17 左移 24。
    若 DIB头大小 大于或等于 BMP_头大小_V3：
        头 的 宽 为 做 字节取 用 数据、18 位或 做 字节取 用 数据、19 左移 8 位或 做 字节取 用 数据、20 左移 16 位或 做 字节取 用 数据、21 左移 24。
        头 的 高 为 做 字节取 用 数据、22 位或 做 字节取 用 数据、23 左移 8 位或 做 字节取 用 数据、24 左移 16 位或 做 字节取 用 数据、25 左移 24。
        头 的 位深 为 做 字节取 用 数据、28 位或 做 字节取 用 数据、29 左移 8。
        头 的 压缩方式 为 做 字节取 用 数据、30 位或 做 字节取 用 数据、31 左移 8 位或 做 字节取 用 数据、32 左移 16 位或 做 字节取 用 数据、33 左移 24。
        头 的 图像大小 为 做 字节取 用 数据、34 位或 做 字节取 用 数据、35 左移 8 位或 做 字节取 用 数据、36 左移 16 位或 做 字节取 用 数据、37 左移 24。
    完。
    若 长度 大于或等于 50：
        头 的 颜色表大小 为 做 字节取 用 数据、46 位或 做 字节取 用 数据、47 左移 8 位或 做 字节取 用 数据、48 左移 16 位或 做 字节取 用 数据、49 左移 24。
    完。
    若 头 的 压缩方式 等于 3 并且 DIB头大小 大于或等于 56：
        头 的 红掩码 为 做 字节取 用 数据、54 位或 做 字节取 用 数据、55 左移 8 位或 做 字节取 用 数据、56 左移 16 位或 做 字节取 用 数据、57 左移 24。
        头 的 绿掩码 为 做 字节取 用 数据、58 位或 做 字节取 用 数据、59 左移 8 位或 做 字节取 用 数据、60 左移 16 位或 做 字节取 用 数据、61 左移 24。
        头 的 蓝掩码 为 做 字节取 用 数据、62 位或 做 字节取 用 数据、63 左移 8 位或 做 字节取 用 数据、64 左移 16 位或 做 字节取 用 数据、65 左移 24。
    否则：
        头 的 红掩码 为 0x00FF0000。
        头 的 绿掩码 为 0x0000FF00。
        头 的 蓝掩码 为 0x000000FF。
        头 的 透明掩码 为 0xFF000000。
    完。
    返还 头。
完。

BMP_解码 接受 数据、长度：
    头 为 做 BMP_解析头 用 数据、长度。
    若 头 等于 空：
        返还 0。
    完。
    若 头 的 宽 小于或等于 0 或者 头 的 宽 大于 65536：
        做 堆_释放 用 头。
        返还 0。
    完。
    可变 实际高 为 头 的 高。
    可变 底到顶 为 真。
    若 实际高 小于 0：
        实际高 为 0 减 实际高。
        底到顶 为 假。
    完。
    若 实际高 大于 65536：
        做 堆_释放 用 头。
        返还 0。
    完。
    bm 为 做 Nova缓冲itmap_创建 用 头 的 宽、实际高、像素格式_RGBA8888。
    若 bm 等于 0：
        做 堆_释放 用 头。
        返还 0。
    完。
    可变 源行字节 为 0。
    若 头 的 位深 等于 24：
        源行字节 为 (头 的 宽 乘 3 加 3) 除以 4 乘 4。
    否则若 头 的 位深 等于 32：
        源行字节 为 头 的 宽 乘 4。
    否则若 头 的 位深 等于 16：
        源行字节 为 (头 的 宽 乘 2 加 3) 除以 4 乘 4。
    否则若 头 的 位深 等于 8：
        源行字节 为 (头 的 宽 加 3) 除以 4 乘 4。
    否则：
        源行字节 为 (头 的 宽 加 31) 除以 32 乘 4。
    完。
    可变 y 为 0。
    只要 y 小于 实际高：
        可变 目标y 为 y。
        若 底到顶 等于 真：
            目标y 为 实际高 减 1 减 y。
        完。
        源行偏移 为 头 的 像素偏移 加 y 乘 源行字节。
        若 源行偏移 加 源行字节 大于 长度：
            跳出。
        完。
        目标行基址 为 bm 的 像素数据 加 目标y 乘 bm 的 行字节数。
        若 头 的 位深 等于 24：
            可变 x 为 0。
            只要 x 小于 头 的 宽：
                源偏移 为 源行偏移 加 x 乘 3。
                b 为 做 字节取 用 数据、源偏移。
                g 为 做 字节取 用 数据、源偏移 加 1。
                r 为 做 字节取 用 数据、源偏移 加 2。
                颜色 为 255 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
                做 内存_写32 用 目标行基址 加 x 乘 4、颜色。
                x 为 x 加 1。
            完。
        否则若 头 的 位深 等于 32：
            可变 x 为 0。
            只要 x 小于 头 的 宽：
                源偏移 为 源行偏移 加 x 乘 4。
                b 为 做 字节取 用 数据、源偏移。
                g 为 做 字节取 用 数据、源偏移 加 1。
                r 为 做 字节取 用 数据、源偏移 加 2。
                a 为 做 字节取 用 数据、源偏移 加 3。
                颜色 为 a 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
                做 内存_写32 用 目标行基址 加 x 乘 4、颜色。
                x 为 x 加 1。
            完。
        否则若 头 的 位深 等于 8：
            调色板偏移 为 54。
            可变 x 为 0。
            只要 x 小于 头 的 宽：
                索引 为 做 字节取 用 数据、源行偏移 加 x。
                pal偏移 为 调色板偏移 加 索引 乘 4。
                若 pal偏移 加 3 小于 长度：
                    b 为 做 字节取 用 数据、pal偏移。
                    g 为 做 字节取 用 数据、pal偏移 加 1。
                    r 为 做 字节取 用 数据、pal偏移 加 2。
                    做 内存_写32 用 目标行基址 加 x 乘 4、255 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
                完。
                x 为 x 加 1。
            完。
        完。
        y 为 y 加 1。
    完。
    做 堆_释放 用 头。
    可变 结果 为 做 堆_分配 用 解码结果。
    若 结果 不等于 0：
        结果 的 宽 为 bm 的 宽。
        结果 的 高 为 bm 的 高。
        结果 的 像素数据 为 bm 的 像素数据。
    完。
    返还 结果。
完。

解码结果 是 一种 事物，含 宽、高、像素数据。

ICO_检测 接受 数据、长度：
    若 长度 小于 6：
        返还 假。
    完。
    若 做 字节取 用 数据、0 等于 0 并且 做 字节取 用 数据、1 等于 0 并且 做 字节取 用 数据、2 等于 1 并且 做 字节取 用 数据、3 等于 0：
        返还 真。
    完。
    若 做 字节取 用 数据、0 等于 0 并且 做 字节取 用 数据、1 等于 0 并且 做 字节取 用 数据、2 等于 2 并且 做 字节取 用 数据、3 等于 0：
        返还 真。
    完。
    返还 假。
完。

格式_名称 接受 格式：
    若 格式 等于 IMAGE_FORMAT_PNG：
        返还 「PNG」。
    完。
    若 格式 等于 IMAGE_FORMAT_JPEG：
        返还 「JPEG」。
    完。
    若 格式 等于 IMAGE_FORMAT_WEBP：
        返还 「WebP」。
    完。
    若 格式 等于 IMAGE_FORMAT_GIF：
        返还 「GIF」。
    完。
    若 格式 等于 IMAGE_FORMAT_BMP：
        返还 「BMP」。
    完。
    若 格式 等于 IMAGE_FORMAT_ICO：
        返还 「ICO」。
    完。
    返还 「未知」。
完。

格式_支持透明 接受 格式：
    若 格式 等于 IMAGE_FORMAT_PNG：
        返还 真。
    完。
    若 格式 等于 IMAGE_FORMAT_WEBP：
        返还 真。
    完。
    若 格式 等于 IMAGE_FORMAT_GIF：
        返还 真。
    完。
    若 格式 等于 IMAGE_FORMAT_ICO：
        返还 真。
    完。
    返还 假。
完。

格式_支持动画 接受 格式：
    若 格式 等于 IMAGE_FORMAT_GIF：
        返还 真。
    完。
    若 格式 等于 IMAGE_FORMAT_WEBP：
        返还 真。
    完。
    返还 假。
完。

格式_是有损 接受 格式：
    若 格式 等于 IMAGE_FORMAT_JPEG：
        返还 真。
    完。
    返还 假。
完。

AI_推荐编码格式 接受 宽、高、有透明、是照片、是动画：
    若 是动画 等于 真：
        返还 IMAGE_FORMAT_WEBP。
    完。
    若 是照片 等于 真 并且 有透明 等于 假：
        返还 IMAGE_FORMAT_JPEG。
    完。
    若 有透明 等于 真：
        返还 IMAGE_FORMAT_PNG。
    完。
    若 宽 小于或等于 256 并且 高 小于或等于 256：
        返还 IMAGE_FORMAT_PNG。
    完。
    若 宽 乘 高 大于 500000：
        返还 IMAGE_FORMAT_JPEG。
    完。
    返还 IMAGE_FORMAT_PNG。
完。

AI_估算压缩大小 接受 宽、高、格式：
    原始大小 为 宽 乘 高 乘 4。
    若 格式 等于 IMAGE_FORMAT_PNG：
        返还 原始大小 乘 40 除以 100。
    完。
    若 格式 等于 IMAGE_FORMAT_JPEG：
        返还 原始大小 乘 10 除以 100。
    完。
    若 格式 等于 IMAGE_FORMAT_WEBP：
        返还 原始大小 乘 7 除以 100。
    完。
    返还 原始大小。
完。

像素_RGBA转BGRA 接受 像素基址、像素数：
    若 像素基址 等于 0 或者 像素数 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 像素数：
        偏移 为 像素基址 加 i 乘 4。
        r 为 做 内存_读8 用 偏移。
        b 为 做 内存_读8 用 偏移 加 2。
        做 内存_写8 用 偏移、b。
        做 内存_写8 用 偏移 加 2、r。
        i 为 i 加 1。
    完。
完。

像素_RGB转RGBA 接受 源、目标、像素数：
    若 源 等于 0 或者 目标 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 像素数：
        r 为 做 内存_读8 用 源 加 i 乘 3。
        g 为 做 内存_读8 用 源 加 i 乘 3 加 1。
        b 为 做 内存_读8 用 源 加 i 乘 3 加 2。
        做 内存_写8 用 目标 加 i 乘 4、r。
        做 内存_写8 用 目标 加 i 乘 4 加 1、g。
        做 内存_写8 用 目标 加 i 乘 4 加 2、b。
        做 内存_写8 用 目标 加 i 乘 4 加 3、255。
        i 为 i 加 1。
    完。
完。

像素_灰度转RGBA 接受 源、目标、像素数：
    若 源 等于 0 或者 目标 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 像素数：
        灰 为 做 内存_读8 用 源 加 i。
        做 内存_写8 用 目标 加 i 乘 4、灰。
        做 内存_写8 用 目标 加 i 乘 4 加 1、灰。
        做 内存_写8 用 目标 加 i 乘 4 加 2、灰。
        做 内存_写8 用 目标 加 i 乘 4 加 3、255。
        i 为 i 加 1。
    完。
完。

像素_预乘Alpha 接受 像素基址、像素数：
    若 像素基址 等于 0 或者 像素数 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 像素数：
        偏移 为 像素基址 加 i 乘 4。
        a 为 做 内存_读8 用 偏移 加 3。
        若 a 小于 255：
            r 为 做 内存_读8 用 偏移 乘 a 除以 255。
            g 为 做 内存_读8 用 偏移 加 1 乘 a 除以 255。
            b 为 做 内存_读8 用 偏移 加 2 乘 a 除以 255。
            做 内存_写8 用 偏移、r。
            做 内存_写8 用 偏移 加 1、g。
            做 内存_写8 用 偏移 加 2、b。
        完。
        i 为 i 加 1。
    完。
完。

可变 统计_格式检测次数 为 0。

可变 统计_PNG检测数 为 0。

可变 统计_JPEG检测数 为 0。

可变 统计_BMP检测数 为 0。

可变 统计_WEBP检测数 为 0。

可变 统计_GIF检测数 为 0。

可变 统计_未知格式数 为 0。

可变 统计_BMP解码数 为 0。

可变 统计_解码失败数 为 0。

格式检测_打印统计 接受 零参：
    做 打印 用 「[格式检测] ═══════════ 格式检测统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  检测总次数:   」、做 转字符串 用 统计_格式检测次数。
    做 打印 用 做 字符串拼接 用 「  PNG检测数:    」、做 转字符串 用 统计_PNG检测数。
    做 打印 用 做 字符串拼接 用 「  JPEG检测数:   」、做 转字符串 用 统计_JPEG检测数。
    做 打印 用 做 字符串拼接 用 「  BMP检测数:    」、做 转字符串 用 统计_BMP检测数。
    做 打印 用 做 字符串拼接 用 「  WebP检测数:   」、做 转字符串 用 统计_WEBP检测数。
    做 打印 用 做 字符串拼接 用 「  GIF检测数:    」、做 转字符串 用 统计_GIF检测数。
    做 打印 用 做 字符串拼接 用 「  未知格式数:   」、做 转字符串 用 统计_未知格式数。
    做 打印 用 做 字符串拼接 用 「  BMP解码数:    」、做 转字符串 用 统计_BMP解码数。
    做 打印 用 做 字符串拼接 用 「  解码失败数:   」、做 转字符串 用 统计_解码失败数。
    做 打印 用 「[格式检测] ═══════════════════════════════════════」。
完。

本章 收束。
