《HTMLTreeBuilder格式化列表》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·推理·定点Q算术》。

引用《.·元素栈创建》。

引用《..·标准·内存》。

NHTB_版本 为 「3.0.0-NHTB」。

格式化标记_标记 为 0xFFFFFFFF。

插入模式_初始 为 0。

插入模式_BeforeHtml 为 1。

插入模式_BeforeHead 为 2。

插入模式_InHead 为 3。

插入模式_InHeadNoscript 为 4。

插入模式_AfterHead 为 5。

插入模式_InBody 为 6。

插入模式_Text 为 7。

插入模式_InTable 为 8。

插入模式_InTableText 为 9。

插入模式_InCaption 为 10。

插入模式_InColumnGroup 为 11。

插入模式_InTableBody 为 12。

插入模式_InRow 为 13。

插入模式_InCell 为 14。

插入模式_InSelect 为 15。

插入模式_InSelectInTable 为 16。

插入模式_InTemplate 为 17。

插入模式_AfterBody 为 18。

插入模式_InFrameset 为 19。

插入模式_AfterFrameset 为 20。

插入模式_AfterAfterBody 为 21。

插入模式_AfterAfterFrameset 为 22。

NHTB_AAA_最大迭代 为 8。

NHTB_AAA_内层最大 为 3。

NHTB_格式化列表初始容量 为 32。

NHTB_开放栈初始容量 为 64。

NHTB_格式化标签 为 一组：「b」、「big」、「code」、「em」、「font」、「i」、「s」、「small」、「strike」、「strong」、「tt」、「u」。

可变 NHTB_创建次数 为 0。

可变 NHTB_AAA执行次数 为 0。

可变 NHTB_重建次数 为 0。

可变 NHTB_FP插入次数 为 0。

可变 NHTB_模式切换次数 为 0。

可变 NHTB_AI推理次数 为 0。

可变 NHTB_异常检测次数 为 0。

格式化元素列表 是 一种 事物，含 元素、数量、容量。

格式化列表_创建 接受 零参：
    列表 为 做 堆_分配 用 格式化元素列表。
    列表 的 容量 为 32。
    列表 的 元素 为 做 堆_分配_数组 用 列表 的 容量 乘 8。
    列表 的 数量 为 0。
    返还 列表。
完。

格式化列表_添加 接受 列表、元素：
    若 列表 的 数量 大于或等于 列表 的 容量：
        新容量 为 列表 的 容量 乘 2。
        新存储 为 做 堆_分配_数组 用 新容量 乘 8。
        做 内存_复制 用 新存储、列表 的 元素、列表 的 数量 乘 8。
        做 堆_释放 用 列表 的 元素。
        列表 的 元素 为 新存储。
        列表 的 容量 为 新容量。
    完。
    做 内存_写64 用 列表 的 元素 加 列表 的 数量 乘 8、元素。
    列表 的 数量 为 列表 的 数量 加 1。
完。

格式化列表_添加标记 接受 列表：
    做 格式化列表_添加 用 列表、格式化标记_标记。
完。

格式化列表_移除 接受 列表、元素：
    可变 i 为 0。
    只要 i 小于 列表 的 数量：
        当前 为 做 内存_读64 用 列表 的 元素 加 i 乘 8。
        若 当前 等于 元素：
            可变 j 为 i。
            只要 j 小于 列表 的 数量 减 1：
                下一个 为 做 内存_读64 用 列表 的 元素 加 (j 加 1) 乘 8。
                做 内存_写64 用 列表 的 元素 加 j 乘 8、下一个。
                j 为 j 加 1。
            完。
            列表 的 数量 为 列表 的 数量 减 1。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

格式化列表_查找 接受 列表、元素：
    可变 i 为 0。
    只要 i 小于 列表 的 数量：
        若 做 内存_读64 用 列表 的 元素 加 i 乘 8 等于 元素：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

格式化列表_获取 接受 列表、索引：
    若 索引 小于 0 或者 索引 大于或等于 列表 的 数量：
        返还 0。
    完。
    返还 做 内存_读64 用 列表 的 元素 加 索引 乘 8。
完。

格式化列表_替换 接受 列表、旧元素、新元素：
    idx 为 做 格式化列表_查找 用 列表、旧元素。
    若 idx 大于或等于 0：
        做 内存_写入64 用 列表 的 元素 加 idx 乘 8、新元素。
        返还 真。
    完。
    返还 假。
完。

格式化列表_清除到标记 接受 列表：
    只要 列表 的 数量 大于 0：
        列表 的 数量 为 列表 的 数量 减 1。
        当前 为 做 内存_读64 用 列表 的 元素 加 列表 的 数量 乘 8。
        若 当前 等于 格式化标记_标记：
            返还。
        完。
    完。
完。

格式化列表_重建 接受 列表、tb：
    NHTB_重建次数 为 NHTB_重建次数 加 1。
    若 列表 的 数量 等于 0：
        返还。
    完。
    可变 marker位置 为 负 1。
    可变 k 为 列表 的 数量 减 1。
    只要 k 大于或等于 0：
        若 做 内存_读64 用 列表 的 元素 加 k 乘 8 等于 格式化标记_标记：
            marker位置 为 k。
            返还。
        完。
        elem 为 做 内存_读64 用 列表 的 元素 加 k 乘 8。
        若 做 元素栈_包含 用 tb 的 开放元素栈、elem：
            返还。
        完。
        k 为 k 减 1。
    完。
    可变 start 为 marker位置 加 1。
    若 start 小于 0：
        start 为 0。
    完。
    可变 i 为 start。
    只要 i 小于 列表 的 数量：
        原元素 为 做 内存_读64 用 列表 的 元素 加 i 乘 8。
        若 原元素 不等于 格式化标记_标记：
            新元素 为 做 NHTB_克隆元素 用 tb、原元素。
            做 NHTB_插入元素 用 tb、新元素。
            做 元素栈_入栈 用 tb 的 开放元素栈、新元素。
            做 内存_写入64 用 列表 的 元素 加 i 乘 8、新元素。
        完。
        i 为 i 加 1。
    完。
完。

格式化列表_是格式化标签 接受 标签名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 NHTB_格式化标签：
        若 NHTB_格式化标签 的 第 i 项 等于 标签名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

格式化列表_释放 接受 列表：
    若 列表 等于 0：
        返还。
    完。
    做 堆_释放 用 列表 的 元素。
    做 堆_释放 用 列表。
完。

HTMLTreeBuilder 是 一种 事物，含 插入模式、原始插入模式、文档、开放元素栈、格式化列表、head指针、form指针、令牌izer、脚本已启用、frameset_ok、foster_parenting、已暂停、创建元素数、创建文本数。

HTMLTreeBuilder_创建 接受 文档、令牌izer：
    NHTB_创建次数 为 NHTB_创建次数 加 1。
    tb 为 做 堆_分配 用 HTMLTreeBuilder。
    tb 的 插入模式 为 插入模式_初始。
    tb 的 原始插入模式 为 插入模式_初始。
    tb 的 文档 为 文档。
    tb 的 开放元素栈 为 做 元素栈_创建。
    tb 的 格式化列表 为 做 格式化列表_创建。
    tb 的 head指针 为 0。
    tb 的 form指针 为 0。
    tb 的 令牌izer 为 令牌izer。
    tb 的 脚本已启用 为 真。
    tb 的 frameset_ok 为 真。
    tb 的 foster_parenting 为 假。
    tb 的 已暂停 为 假。
    tb 的 创建元素数 为 0。
    tb 的 创建文本数 为 0。
    返还 tb。
完。

HTMLTreeBuilder_释放 接受 tb：
    若 tb 等于 0：
        返还。
    完。
    做 格式化列表_释放 用 tb 的 格式化列表。
    做 元素栈_释放 用 tb 的 开放元素栈。
    做 堆_释放 用 tb。
完。

NHTB_切换插入模式 接受 tb、新模式：
    NHTB_模式切换次数 为 NHTB_模式切换次数 加 1。
    tb 的 插入模式 为 新模式。
完。

NHTB_保存并切换模式 接受 tb、新模式：
    tb 的 原始插入模式 为 tb 的 插入模式。
    做 NHTB_切换插入模式 用 tb、新模式。
完。

NHTB_恢复原始模式 接受 tb：
    做 NHTB_切换插入模式 用 tb、tb 的 原始插入模式。
完。

NHTB_当前节点 接受 tb：
    返还 做 元素栈_栈顶 用 tb 的 开放元素栈。
完。

NHTB_插入元素 接受 tb、元素：
    tb 的 创建元素数 为 tb 的 创建元素数 加 1。
    若 tb 的 foster_parenting 等于 真：
        做 NHTB_Foster插入 用 tb、元素。
        返还。
    完。
    父节点 为 做 NHTB_当前节点 用 tb。
    若 父节点 不等于 0：
        做 DOM_追加子节点 用 tb 的 文档、父节点、元素。
    完。
完。

NHTB_插入文本 接受 tb、文本：
    tb 的 创建文本数 为 tb 的 创建文本数 加 1。
    文本节点 为 做 DOM_创建文本节点 用 tb 的 文档、文本。
    做 NHTB_插入元素 用 tb、文本节点。
完。

NHTB_克隆元素 接受 tb、原元素：
    标签 为 做 DOM_获取标签名 用 tb 的 文档、原元素。
    新元素 为 做 DOM_创建元素 用 tb 的 文档、标签。
    做 DOM_复制属性 用 tb 的 文档、原元素、新元素。
    返还 新元素。
完。

NHTB_Foster插入 接受 tb、元素：
    NHTB_FP插入次数 为 NHTB_FP插入次数 加 1。
    table元素 为 做 NHTB_栈中查找标签 用 tb、「table」。
    若 table元素 等于 0：
        栈底 为 做 元素栈_栈底 用 tb 的 开放元素栈。
        做 DOM_追加子节点 用 tb 的 文档、栈底、元素。
        返还。
    完。
    父节点 为 做 DOM_获取父节点 用 tb 的 文档、table元素。
    若 父节点 不等于 0：
        做 DOM_插入在前 用 tb 的 文档、父节点、元素、table元素。
    否则：
        做 DOM_追加子节点 用 tb 的 文档、table元素、元素。
    完。
完。

NHTB_栈中查找标签 接受 tb、标签名：
    栈大小 为 做 元素栈_大小 用 tb 的 开放元素栈。
    可变 i 为 栈大小 减 1。
    只要 i 大于或等于 0：
        elem 为 做 元素栈_获取 用 tb 的 开放元素栈、i。
        若 做 DOM_获取标签名 用 tb 的 文档、elem 等于 标签名：
            返还 elem。
        完。
        i 为 i 减 1。
    完。
    返还 0。
完。

NHTB_AAA_执行 接受 tb、标签名：
    NHTB_AAA执行次数 为 NHTB_AAA执行次数 加 1。
    可变 外层迭代 为 0。
    只要 外层迭代 小于 NHTB_AAA_最大迭代：
        格式元素 为 做 NHTB_AAA_查找格式化元素 用 tb、标签名。
        若 格式元素 等于 0：
            做 NHTB_栈弹至标签 用 tb、标签名。
            返还。
        完。
        若 做 元素栈_包含 用 tb 的 开放元素栈、格式元素 等于 假：
            做 格式化列表_移除 用 tb 的 格式化列表、格式元素。
            返还。
        完。
        进一步块 为 做 NHTB_AAA_查找进一步块 用 tb、格式元素。
        若 进一步块 等于 0：
            做 NHTB_栈弹至元素 用 tb、格式元素。
            做 格式化列表_移除 用 tb 的 格式化列表、格式元素。
            返还。
        完。
        共同祖先 为 做 NHTB_AAA_查找共同祖先 用 tb、格式元素。
        可变 节点 为 进一步块。
        可变 最后节点 为 进一步块。
        可变 内层迭代 为 0。
        只要 内层迭代 小于 NHTB_AAA_内层最大：
            内层迭代 为 内层迭代 加 1。
            节点 为 做 NHTB_栈中上一个 用 tb、节点。
            若 节点 等于 格式元素：
                返还。
            完。
            若 节点 等于 0：
                返还。
            完。
            若 做 格式化列表_查找 用 tb 的 格式化列表、节点 小于 0：
                做 元素栈_移除 用 tb 的 开放元素栈、节点。
                继续。
            完。
            新元素 为 做 NHTB_克隆元素 用 tb、节点。
            做 格式化列表_替换 用 tb 的 格式化列表、节点、新元素。
            做 元素栈_替换 用 tb 的 开放元素栈、节点、新元素。
            节点 为 新元素。
            若 最后节点 等于 进一步块：
                最后节点 为 节点。
            完。
            做 DOM_移除子节点 用 tb 的 文档、最后节点。
            做 DOM_追加子节点 用 tb 的 文档、节点、最后节点。
            最后节点 为 节点。
        完。
        若 共同祖先 不等于 0：
            做 DOM_移除子节点 用 tb 的 文档、最后节点。
            做 DOM_追加子节点 用 tb 的 文档、共同祖先、最后节点。
        完。
        新格式 为 做 NHTB_克隆元素 用 tb、格式元素。
        做 DOM_移动所有子节点 用 tb 的 文档、进一步块、新格式。
        做 DOM_追加子节点 用 tb 的 文档、进一步块、新格式。
        做 格式化列表_移除 用 tb 的 格式化列表、格式元素。
        做 格式化列表_添加 用 tb 的 格式化列表、新格式。
        做 元素栈_移除 用 tb 的 开放元素栈、格式元素。
        做 元素栈_插入在后 用 tb 的 开放元素栈、进一步块、新格式。
        外层迭代 为 外层迭代 加 1。
    完。
完。

NHTB_AAA_查找格式化元素 接受 tb、标签名：
    可变 i 为 tb 的 格式化列表 的 数量 减 1。
    只要 i 大于或等于 0：
        elem 为 做 格式化列表_获取 用 tb 的 格式化列表、i。
        若 elem 等于 格式化标记_标记：
            返还 0。
        完。
        若 做 DOM_获取标签名 用 tb 的 文档、elem 等于 标签名：
            返还 elem。
        完。
        i 为 i 减 1。
    完。
    返还 0。
完。

NHTB_AAA_查找进一步块 接受 tb、格式元素：
    格式idx 为 做 元素栈_查找 用 tb 的 开放元素栈、格式元素。
    若 格式idx 小于 0：
        返还 0。
    完。
    栈大小 为 做 元素栈_大小 用 tb 的 开放元素栈。
    可变 i 为 格式idx 加 1。
    只要 i 小于 栈大小：
        elem 为 做 元素栈_获取 用 tb 的 开放元素栈、i。
        若 做 NHTB_是特殊元素 用 tb、elem：
            返还 elem。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

NHTB_AAA_查找共同祖先 接受 tb、格式元素：
    idx 为 做 元素栈_查找 用 tb 的 开放元素栈、格式元素。
    若 idx 小于或等于 0：
        返还 0。
    完。
    返还 做 元素栈_获取 用 tb 的 开放元素栈、idx 减 1。
完。

NHTB_是特殊元素 接受 tb、元素：
    标签 为 做 DOM_获取标签名 用 tb 的 文档、元素。
    若 标签 等于 「address」 或者 标签 等于 「applet」 或者 标签 等于 「area」 或者 标签 等于 「article」 或者 标签 等于 「aside」 或者 标签 等于 「blockquote」 或者 标签 等于 「body」 或者 标签 等于 「caption」 或者 标签 等于 「center」 或者 标签 等于 「dd」 或者 标签 等于 「details」 或者 标签 等于 「dialog」 或者 标签 等于 「dir」 或者 标签 等于 「div」 或者 标签 等于 「dl」 或者 标签 等于 「dt」 或者 标签 等于 「fieldset」 或者 标签 等于 「figcaption」 或者 标签 等于 「figure」 或者 标签 等于 「footer」 或者 标签 等于 「form」 或者 标签 等于 「h1」 或者 标签 等于 「h2」 或者 标签 等于 「h3」 或者 标签 等于 「h4」 或者 标签 等于 「h5」 或者 标签 等于 「h6」 或者 标签 等于 「header」 或者 标签 等于 「li」 或者 标签 等于 「listing」 或者 标签 等于 「main」 或者 标签 等于 「marquee」 或者 标签 等于 「menu」 或者 标签 等于 「nav」 或者 标签 等于 「object」 或者 标签 等于 「ol」 或者 标签 等于 「p」 或者 标签 等于 「pre」 或者 标签 等于 「section」 或者 标签 等于 「summary」 或者 标签 等于 「table」 或者 标签 等于 「tbody」 或者 标签 等于 「td」 或者 标签 等于 「template」 或者 标签 等于 「tfoot」 或者 标签 等于 「th」 或者 标签 等于 「thead」 或者 标签 等于 「tr」 或者 标签 等于 「ul」：
        返还 真。
    完。
    返还 假。
完。

NHTB_栈弹至标签 接受 tb、标签名：
    只要 做 元素栈_大小 用 tb 的 开放元素栈 大于 0：
        栈顶 为 做 元素栈_栈顶 用 tb 的 开放元素栈。
        做 元素栈_弹栈 用 tb 的 开放元素栈。
        若 做 DOM_获取标签名 用 tb 的 文档、栈顶 等于 标签名：
            返还。
        完。
    完。
完。

NHTB_栈弹至元素 接受 tb、元素：
    只要 做 元素栈_大小 用 tb 的 开放元素栈 大于 0：
        栈顶 为 做 元素栈_栈顶 用 tb 的 开放元素栈。
        做 元素栈_弹栈 用 tb 的 开放元素栈。
        若 栈顶 等于 元素：
            返还。
        完。
    完。
完。

NHTB_栈中上一个 接受 tb、元素：
    idx 为 做 元素栈_查找 用 tb 的 开放元素栈、元素。
    若 idx 小于或等于 0：
        返还 0。
    完。
    返还 做 元素栈_获取 用 tb 的 开放元素栈、idx 减 1。
完。

NHTB_AI检测异常 接受 tb：
    NHTB_AI推理次数 为 NHTB_AI推理次数 加 1。
    tensor 为 做 Tensor_创建 用 8。
    做 Tensor_写 用 tensor、0、tb 的 插入模式。
    做 Tensor_写 用 tensor、1、做 元素栈_大小 用 tb 的 开放元素栈。
    做 Tensor_写 用 tensor、2、tb 的 格式化列表 的 数量。
    做 Tensor_写 用 tensor、3、tb 的 创建元素数。
    做 Tensor_写 用 tensor、4、tb 的 创建文本数。
    做 Tensor_写 用 tensor、5、NHTB_AAA执行次数。
    做 Tensor_写 用 tensor、6、NHTB_FP插入次数。
    做 Tensor_写 用 tensor、7、NHTB_重建次数。
    推理 为 做 AI总线_异常检测 用 全局AI总线、「nhtb_anomaly」、tensor。
    异常分 为 做 Tensor_读 用 推理、0。
    做 Tensor_释放 用 tensor。
    做 Tensor_释放 用 推理。
    若 异常分 大于 0.8：
        NHTB_异常检测次数 为 NHTB_异常检测次数 加 1。
        做 打印 用 「[NHTB-AI] 解析异常 score=」 加 异常分。
    完。
    返还 异常分。
完。

NHTB_AI预测插入模式 接受 tb、标签名：
    NHTB_AI推理次数 为 NHTB_AI推理次数 加 1。
    tensor 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 tensor、0、tb 的 插入模式。
    做 Tensor_写 用 tensor、1、做 元素栈_大小 用 tb 的 开放元素栈。
    做 Tensor_写 用 tensor、2、tb 的 格式化列表 的 数量。
    做 Tensor_写 用 tensor、3、0.0。
    推理 为 做 AI总线_推理 用 全局AI总线、「nhtb_predict」、tensor。
    预测模式 为 做 Tensor_读 用 推理、0。
    做 Tensor_释放 用 tensor。
    做 Tensor_释放 用 推理。
    返还 预测模式。
完。

NHTB_初始化 接受 零参：
    NHTB_创建次数 为 0。
    NHTB_AAA执行次数 为 0。
    NHTB_重建次数 为 0。
    NHTB_FP插入次数 为 0。
    NHTB_模式切换次数 为 0。
    NHTB_AI推理次数 为 0。
    NHTB_异常检测次数 为 0。
    做 打印 用 「[NHTB] HTML树构建器 v」 加 NHTB_版本 加 「 已初始化」。
完。

NHTB_诊断报告 接受 零参：
    做 打印 用 「[NHTB] === HTML树构建诊断 ===」。
    做 打印 用 「  创建=」 加 做 整数转字符串 用 NHTB_创建次数 加 「 AAA=」 加 做 整数转字符串 用 NHTB_AAA执行次数 加 「 重建=」 加 做 整数转字符串 用 NHTB_重建次数。
    做 打印 用 「  FP=」 加 做 整数转字符串 用 NHTB_FP插入次数 加 「 模式切换=」 加 做 整数转字符串 用 NHTB_模式切换次数。
    做 打印 用 「  AI推理=」 加 做 整数转字符串 用 NHTB_AI推理次数 加 「 异常=」 加 做 整数转字符串 用 NHTB_异常检测次数。
完。

本章 收束。
