《SVG路径解析》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·字符串替换》。

引用《.·边界框计算》。

引用《..·中枢·内容可见性》。

引用《.·SVG路径解析辅助》。

SVG命令_M 为 77。

SVG命令_m 为 109。

SVG命令_L 为 76。

SVG命令_l 为 108。

SVG命令_H 为 72。

SVG命令_h 为 104。

SVG命令_V 为 86。

SVG命令_v 为 118。

SVG命令_C 为 67。

SVG命令_c 为 99。

SVG命令_S 为 83。

SVG命令_s 为 115。

SVG命令_Q 为 81。

SVG命令_q 为 113。

SVG命令_T 为 84。

SVG命令_t 为 116。

SVG命令_A 为 65。

SVG命令_a 为 97。

SVG命令_Z 为 90。

SVG命令_z 为 122。

最大路径段数 为 8192。

路径解析统计 是 一种 事物，含 解析总次数、M命令次数、L命令次数、C命令次数、Q命令次数、A命令次数、Z命令次数、H_V命令次数、S_T命令次数、隐式重复次数、相对坐标次数、绝对坐标次数、总段数、总解析耗时_纳秒、最大单次耗时、AI复杂度调用次数、AI复杂度命中次数、解析错误次数。

AI路径解析状态 是 一种 事物，含 历史平均段数、历史平均命令种类数、历史曲线比例、连续简单路径数、连续复杂路径数、预测下次复杂、采样计数器、学习权重。

可变 全局路径解析统计 为 0。

可变 全局路径解析AI 为 0。

可变 全局路径解析已初始化 为 假。

SVGPathSegment 是 一种 事物，含 命令、参数、下一个。

SVGPath 是 一种 事物，含 段链头、段链尾、段数、bbox_x、bbox_y、bbox_w、bbox_h、已闭合、锁。

SVG路径解析_初始化 接受 零参：
    若 全局路径解析已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局路径解析统计 为 做 堆_分配_清零 用 144。
    若 全局路径解析统计 等于 空：
        返还 错误_内存不足。
    完。
    全局路径解析AI 为 做 堆_分配_清零 用 40。
    若 全局路径解析AI 不等于 空：
        全局路径解析AI 的 学习权重 为 128。
    完。
    全局路径解析已初始化 为 真。
    做 打印 用 「[SVG路径解析] 初始化完成」。
    返还 0。
完。

SVG路径解析_销毁 接受 零参：
    若 全局路径解析统计 不等于 空：
        做 堆_释放 用 全局路径解析统计。
        全局路径解析统计 为 0。
    完。
    若 全局路径解析AI 不等于 空：
        做 堆_释放 用 全局路径解析AI。
        全局路径解析AI 为 0。
    完。
    全局路径解析已初始化 为 假。
    返还 0。
完。

SVGPath_创建 接受 零参：
    path 为 做 堆_分配 用 SVGPath。
    若 path 等于 空：
        返还 空。
    完。
    path 的 段链头 为 0。
    path 的 段链尾 为 0。
    path 的 段数 为 0。
    path 的 bbox_x 为 0。
    path 的 bbox_y 为 0。
    path 的 bbox_w 为 0。
    path 的 bbox_h 为 0。
    path 的 已闭合 为 假。
    path 的 锁 为 做 渲染_互斥锁创建。
    返还 path。
完。

SVGPath_添加段 接受 path、命令、p0、p1、p2、p3、p4、p5、p6：
    若 path 等于 空：
        返还。
    完。
    若 path 的 段数 大于或等于 最大路径段数：
        若 全局路径解析统计 不等于 空：
            全局路径解析统计 的 解析错误次数 为 全局路径解析统计 的 解析错误次数 加 1。
        完。
        返还。
    完。
    seg 为 做 堆_分配 用 SVGPathSegment。
    若 seg 等于 空：
        返还。
    完。
    seg 的 命令 为 命令。
    seg 的 参数 为 做 堆_分配 用 7 乘 4。
    若 seg 的 参数 不等于 空：
        做 内存_写32 用 seg 的 参数、p0。
        做 内存_写32 用 seg 的 参数 加 4、p1。
        做 内存_写32 用 seg 的 参数 加 8、p2。
        做 内存_写32 用 seg 的 参数 加 12、p3。
        做 内存_写32 用 seg 的 参数 加 16、p4。
        做 内存_写32 用 seg 的 参数 加 20、p5。
        做 内存_写32 用 seg 的 参数 加 24、p6。
    完。
    seg 的 下一个 为 0。
    若 path 的 段链尾 不等于 0：
        尾 为 path 的 段链尾。
        尾 的 下一个 为 seg。
    否则：
        path 的 段链头 为 seg。
    完。
    path 的 段链尾 为 seg。
    path 的 段数 为 path 的 段数 加 1。
    若 命令 等于 SVG命令_Z：
        path 的 已闭合 为 真。
    完。
完。

SVGPath_销毁 接受 path：
    若 path 等于 空：
        返还。
    完。
    可变 seg 为 path 的 段链头。
    只要 seg 不等于 0：
        下一个 为 seg 的 下一个。
        若 seg 的 参数 不等于 空：
            做 堆_释放 用 seg 的 参数。
        完。
        做 堆_释放 用 seg。
        seg 为 下一个。
    完。
    做 堆_释放 用 path。
完。

SVGPathParser 是 一种 事物，含 输入、长度、位置、当前x、当前y、起点x、起点y、上一控制x、上一控制y、上一命令。

SVGPath_解析 接受 d字符串：
    若 d字符串 等于 0：
        返还 0。
    完。
    path 为 做 SVGPath_创建。
    parser 为 做 堆_分配 用 SVGPathParser。
    parser 的 输入 为 d字符串。
    parser 的 长度 为 做 字节长度 用 d字符串。
    parser 的 位置 为 0。
    parser 的 当前x 为 0。
    parser 的 当前y 为 0。
    parser 的 起点x 为 0。
    parser 的 起点y 为 0。
    parser 的 上一命令 为 0。
    只要 parser 的 位置 小于 parser 的 长度：
        做 SVGParser_跳过空白 用 parser。
        若 parser 的 位置 大于或等于 parser 的 长度：
            跳出。
        完。
        ch 为 做 字节取 用 parser 的 输入、parser 的 位置。
        若 做 SVGParser_是命令 用 ch：
            parser 的 上一命令 为 ch。
            parser 的 位置 为 parser 的 位置 加 1。
        否则：
            ch 为 parser 的 上一命令。
        完。
        若 ch 等于 SVG命令_M 或者 ch 等于 SVG命令_m：
            x 为 做 SVGParser_读数字 用 parser。
            y 为 做 SVGParser_读数字 用 parser。
            若 ch 等于 SVG命令_m：
                x 为 x 加 parser 的 当前x。
                y 为 y 加 parser 的 当前y。
            完。
            做 SVGPath_添加段 用 path、SVG命令_M、x、y、0、0、0、0、0。
            parser 的 当前x 为 x。
            parser 的 当前y 为 y。
            parser 的 起点x 为 x。
            parser 的 起点y 为 y。
            parser 的 上一命令 为 若 ch 等于 SVG命令_M 则 SVG命令_L 否则 SVG命令_l。
        否则若 ch 等于 SVG命令_L 或者 ch 等于 SVG命令_l：
            x 为 做 SVGParser_读数字 用 parser。
            y 为 做 SVGParser_读数字 用 parser。
            若 ch 等于 SVG命令_l：
                x 为 x 加 parser 的 当前x。
                y 为 y 加 parser 的 当前y。
            完。
            做 SVGPath_添加段 用 path、SVG命令_L、x、y、0、0、0、0、0。
            parser 的 当前x 为 x。
            parser 的 当前y 为 y。
        否则若 ch 等于 SVG命令_H 或者 ch 等于 SVG命令_h：
            x 为 做 SVGParser_读数字 用 parser。
            若 ch 等于 SVG命令_h：
                x 为 x 加 parser 的 当前x。
            完。
            做 SVGPath_添加段 用 path、SVG命令_L、x、parser 的 当前y、0、0、0、0、0。
            parser 的 当前x 为 x。
        否则若 ch 等于 SVG命令_V 或者 ch 等于 SVG命令_v：
            y 为 做 SVGParser_读数字 用 parser。
            若 ch 等于 SVG命令_v：
                y 为 y 加 parser 的 当前y。
            完。
            做 SVGPath_添加段 用 path、SVG命令_L、parser 的 当前x、y、0、0、0、0、0。
            parser 的 当前y 为 y。
        否则若 ch 等于 SVG命令_C 或者 ch 等于 SVG命令_c：
            x1 为 做 SVGParser_读数字 用 parser。
            y1 为 做 SVGParser_读数字 用 parser。
            x2 为 做 SVGParser_读数字 用 parser。
            y2 为 做 SVGParser_读数字 用 parser。
            x 为 做 SVGParser_读数字 用 parser。
            y 为 做 SVGParser_读数字 用 parser。
            若 ch 等于 SVG命令_c：
                x1 为 x1 加 parser 的 当前x。
                y1 为 y1 加 parser 的 当前y。
                x2 为 x2 加 parser 的 当前x。
                y2 为 y2 加 parser 的 当前y。
                x 为 x 加 parser 的 当前x。
                y 为 y 加 parser 的 当前y。
            完。
            做 SVGPath_添加段 用 path、SVG命令_C、x1、y1、x2、y2、x、y、0。
            parser 的 上一控制x 为 x2。
            parser 的 上一控制y 为 y2。
            parser 的 当前x 为 x。
            parser 的 当前y 为 y。
        否则若 ch 等于 SVG命令_S 或者 ch 等于 SVG命令_s：
            x1 为 2 乘 parser 的 当前x 减 parser 的 上一控制x。
            y1 为 2 乘 parser 的 当前y 减 parser 的 上一控制y。
            x2 为 做 SVGParser_读数字 用 parser。
            y2 为 做 SVGParser_读数字 用 parser。
            x 为 做 SVGParser_读数字 用 parser。
            y 为 做 SVGParser_读数字 用 parser。
            若 ch 等于 SVG命令_s：
                x2 为 x2 加 parser 的 当前x。
                y2 为 y2 加 parser 的 当前y。
                x 为 x 加 parser 的 当前x。
                y 为 y 加 parser 的 当前y。
            完。
            做 SVGPath_添加段 用 path、SVG命令_C、x1、y1、x2、y2、x、y、0。
            parser 的 上一控制x 为 x2。
            parser 的 上一控制y 为 y2。
            parser 的 当前x 为 x。
            parser 的 当前y 为 y。
        否则若 ch 等于 SVG命令_Q 或者 ch 等于 SVG命令_q：
            cx 为 做 SVGParser_读数字 用 parser。
            cy 为 做 SVGParser_读数字 用 parser。
            x 为 做 SVGParser_读数字 用 parser。
            y 为 做 SVGParser_读数字 用 parser。
            若 ch 等于 SVG命令_q：
                cx 为 cx 加 parser 的 当前x。
                cy 为 cy 加 parser 的 当前y。
                x 为 x 加 parser 的 当前x。
                y 为 y 加 parser 的 当前y。
            完。
            做 SVGPath_添加段 用 path、SVG命令_Q、cx、cy、x、y、0、0、0。
            parser 的 上一控制x 为 cx。
            parser 的 上一控制y 为 cy。
            parser 的 当前x 为 x。
            parser 的 当前y 为 y。
        否则若 ch 等于 SVG命令_T 或者 ch 等于 SVG命令_t：
            cx 为 2 乘 parser 的 当前x 减 parser 的 上一控制x。
            cy 为 2 乘 parser 的 当前y 减 parser 的 上一控制y。
            x 为 做 SVGParser_读数字 用 parser。
            y 为 做 SVGParser_读数字 用 parser。
            若 ch 等于 SVG命令_t：
                x 为 x 加 parser 的 当前x。
                y 为 y 加 parser 的 当前y。
            完。
            做 SVGPath_添加段 用 path、SVG命令_Q、cx、cy、x、y、0、0、0。
            parser 的 上一控制x 为 cx。
            parser 的 上一控制y 为 cy。
            parser 的 当前x 为 x。
            parser 的 当前y 为 y。
        否则若 ch 等于 SVG命令_A 或者 ch 等于 SVG命令_a：
            rx 为 做 SVGParser_读数字 用 parser。
            ry 为 做 SVGParser_读数字 用 parser。
            角度 为 做 SVGParser_读数字 用 parser。
            大弧 为 做 SVGParser_读数字 用 parser。
            顺时针 为 做 SVGParser_读数字 用 parser。
            x 为 做 SVGParser_读数字 用 parser。
            y 为 做 SVGParser_读数字 用 parser。
            若 ch 等于 SVG命令_a：
                x 为 x 加 parser 的 当前x。
                y 为 y 加 parser 的 当前y。
            完。
            做 SVGPath_添加段 用 path、SVG命令_A、rx、ry、角度、大弧、顺时针、x、y。
            parser 的 当前x 为 x。
            parser 的 当前y 为 y。
        否则若 ch 等于 SVG命令_Z 或者 ch 等于 SVG命令_z：
            做 SVGPath_添加段 用 path、SVG命令_Z、parser 的 起点x、parser 的 起点y、0、0、0、0、0。
            parser 的 当前x 为 parser 的 起点x。
            parser 的 当前y 为 parser 的 起点y。
        完。
    完。
    做 堆_释放 用 parser。
    做 SVGPath_计算边界 用 path。
    若 全局路径解析统计 不等于 空：
        全局路径解析统计 的 解析总次数 为 全局路径解析统计 的 解析总次数 加 1。
        全局路径解析统计 的 总段数 为 全局路径解析统计 的 总段数 加 path 的 段数。
    完。
    做 AI路径_后解析学习 用 path。
    返还 path。
完。

SVGPath_解析d_完整 接受 d属性：
    若 d属性 等于 空：
        若 全局路径解析统计 不等于 空：
            全局路径解析统计 的 解析错误次数 为 全局路径解析统计 的 解析错误次数 加 1。
        完。
        返还 空。
    完。
    开始时间 为 做 平台_获取高精度时间。
    若 全局路径解析AI 不等于 空：
        做 AI路径_预解析分析 用 d属性。
    完。
    path 为 做 SVGPath_解析d 用 d属性。
    若 全局路径解析统计 不等于 空：
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        全局路径解析统计 的 总解析耗时_纳秒 为 全局路径解析统计 的 总解析耗时_纳秒 加 耗时。
        若 耗时 大于 全局路径解析统计 的 最大单次耗时：
            全局路径解析统计 的 最大单次耗时 为 耗时。
        完。
    完。
    返还 path。
完。

SVGPath_分析复杂度 接受 path：
    若 path 等于 空：
        返还 0。
    完。
    可变 M数 为 0。
    可变 L数 为 0。
    可变 C数 为 0。
    可变 Q数 为 0。
    可变 A数 为 0。
    可变 Z数 为 0。
    可变 其他数 为 0。
    可变 seg 为 path 的 段链头。
    只要 seg 不等于 空：
        若 seg 的 命令 等于 SVG命令_M：
            M数 为 M数 加 1。
        否则若 seg 的 命令 等于 SVG命令_L：
            L数 为 L数 加 1。
        否则若 seg 的 命令 等于 SVG命令_C：
            C数 为 C数 加 1。
        否则若 seg 的 命令 等于 SVG命令_Q：
            Q数 为 Q数 加 1。
        否则若 seg 的 命令 等于 SVG命令_A：
            A数 为 A数 加 1。
        否则若 seg 的 命令 等于 SVG命令_Z：
            Z数 为 Z数 加 1。
        否则：
            其他数 为 其他数 加 1。
        完。
        seg 为 seg 的 下一个。
    完。
    若 全局路径解析统计 不等于 空：
        全局路径解析统计 的 M命令次数 为 全局路径解析统计 的 M命令次数 加 M数。
        全局路径解析统计 的 L命令次数 为 全局路径解析统计 的 L命令次数 加 L数。
        全局路径解析统计 的 C命令次数 为 全局路径解析统计 的 C命令次数 加 C数。
        全局路径解析统计 的 Q命令次数 为 全局路径解析统计 的 Q命令次数 加 Q数。
        全局路径解析统计 的 A命令次数 为 全局路径解析统计 的 A命令次数 加 A数。
        全局路径解析统计 的 Z命令次数 为 全局路径解析统计 的 Z命令次数 加 Z数。
    完。
    评分 为 M数 加 L数 加 Z数 加 C数 乘 3 加 Q数 乘 3 加 A数 乘 5 加 其他数 乘 2。
    返还 评分。
完。

SVGPath_命令种类数 接受 path：
    若 path 等于 空：
        返还 0。
    完。
    可变 存在 为 做 堆_分配_清零 用 20。
    若 存在 等于 空：
        返还 0。
    完。
    可变 seg 为 path 的 段链头。
    只要 seg 不等于 空：
        可变 idx 为 0。
        若 seg 的 命令 等于 SVG命令_M：
            idx 为 0。
        否则若 seg 的 命令 等于 SVG命令_L：
            idx 为 1。
        否则若 seg 的 命令 等于 SVG命令_C：
            idx 为 2。
        否则若 seg 的 命令 等于 SVG命令_Q：
            idx 为 3。
        否则若 seg 的 命令 等于 SVG命令_A：
            idx 为 4。
        否则若 seg 的 命令 等于 SVG命令_Z：
            idx 为 5。
        否则若 seg 的 命令 等于 SVG命令_H 或者 seg 的 命令 等于 SVG命令_V：
            idx 为 6。
        否则若 seg 的 命令 等于 SVG命令_S 或者 seg 的 命令 等于 SVG命令_T：
            idx 为 7。
        否则：
            idx 为 8。
        完。
        做 内存_写8 用 存在 加 idx、1。
        seg 为 seg 的 下一个。
    完。
    可变 种类 为 0。
    可变 i 为 0。
    只要 i 小于 10：
        若 做 内存_读8 用 存在 加 i 等于 1：
            种类 为 种类 加 1。
        完。
        i 为 i 加 1。
    完。
    做 堆_释放 用 存在。
    返还 种类。
完。

SVGPath_曲线段比例 接受 path：
    若 path 等于 空 或者 path 的 段数 等于 0：
        返还 0。
    完。
    可变 曲线数 为 0。
    可变 seg 为 path 的 段链头。
    只要 seg 不等于 空：
        若 seg 的 命令 等于 SVG命令_C 或者 seg 的 命令 等于 SVG命令_Q 或者 seg 的 命令 等于 SVG命令_A 或者 seg 的 命令 等于 SVG命令_S 或者 seg 的 命令 等于 SVG命令_T：
            曲线数 为 曲线数 加 1。
        完。
        seg 为 seg 的 下一个。
    完。
    返还 曲线数 乘 1024 除以 path 的 段数。
完。

AI路径_预解析分析 接受 d属性：
    若 全局路径解析AI 等于 空 或者 d属性 等于 空：
        返还。
    完。
    若 全局路径解析统计 不等于 空：
        全局路径解析统计 的 AI复杂度调用次数 为 全局路径解析统计 的 AI复杂度调用次数 加 1。
    完。
    长度 为 做 字节长度 用 d属性。
    可变 命令数 为 0。
    可变 曲线命令 为 0。
    可变 i 为 0。
    只要 i 小于 长度 并且 i 小于 200：
        ch 为 做 字节取 用 d属性、i。
        若 做 SVGParser_是命令 用 ch 等于 真：
            命令数 为 命令数 加 1。
            若 ch 等于 67 或者 ch 等于 99 或者 ch 等于 81 或者 ch 等于 113 或者 ch 等于 65 或者 ch 等于 97 或者 ch 等于 83 或者 ch 等于 115 或者 ch 等于 84 或者 ch 等于 116：
                曲线命令 为 曲线命令 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    AI 为 全局路径解析AI。
    可变 预测复杂 为 假。
    若 曲线命令 大于 3 或者 长度 大于 500：
        预测复杂 为 真。
    完。
    若 AI 的 预测下次复杂 等于 真 并且 预测复杂 等于 真：
        若 全局路径解析统计 不等于 空：
            全局路径解析统计 的 AI复杂度命中次数 为 全局路径解析统计 的 AI复杂度命中次数 加 1。
        完。
    完。
    AI 的 预测下次复杂 为 预测复杂。
完。

AI路径_后解析学习 接受 path：
    若 全局路径解析AI 等于 空 或者 path 等于 空：
        返还。
    完。
    AI 为 全局路径解析AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    AI 的 历史平均段数 为 (AI 的 历史平均段数 乘 7 加 path 的 段数 乘 1024) 除以 8。
    种类 为 做 SVGPath_命令种类数 用 path。
    AI 的 历史平均命令种类数 为 (AI 的 历史平均命令种类数 乘 7 加 种类 乘 1024) 除以 8。
    曲线比 为 做 SVGPath_曲线段比例 用 path。
    AI 的 历史曲线比例 为 (AI 的 历史曲线比例 乘 7 加 曲线比) 除以 8。
    若 曲线比 大于 200：
        AI 的 连续复杂路径数 为 AI 的 连续复杂路径数 加 1。
        AI 的 连续简单路径数 为 0。
    否则：
        AI 的 连续简单路径数 为 AI 的 连续简单路径数 加 1。
        AI 的 连续复杂路径数 为 0。
    完。
    若 AI 的 连续复杂路径数 大于 3 或者 AI 的 历史曲线比例 大于 300：
        AI 的 预测下次复杂 为 真。
    否则若 AI 的 连续简单路径数 大于 5：
        AI 的 预测下次复杂 为 假。
    完。
完。

SVG路径解析_打印统计 接受 零参：
    若 全局路径解析统计 等于 空：
        做 打印 用 「[SVG路径解析] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   SVG路径解析器 · 统计报告          │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 解析总次数:       」、做 转字符串 用 全局路径解析统计 的 解析总次数。
    做 打印 用 做 字符串拼接 用 「│ M命令:            」、做 转字符串 用 全局路径解析统计 的 M命令次数。
    做 打印 用 做 字符串拼接 用 「│ L命令:            」、做 转字符串 用 全局路径解析统计 的 L命令次数。
    做 打印 用 做 字符串拼接 用 「│ C命令:            」、做 转字符串 用 全局路径解析统计 的 C命令次数。
    做 打印 用 做 字符串拼接 用 「│ Q命令:            」、做 转字符串 用 全局路径解析统计 的 Q命令次数。
    做 打印 用 做 字符串拼接 用 「│ A命令:            」、做 转字符串 用 全局路径解析统计 的 A命令次数。
    做 打印 用 做 字符串拼接 用 「│ Z命令:            」、做 转字符串 用 全局路径解析统计 的 Z命令次数。
    做 打印 用 做 字符串拼接 用 「│ 总段数:           」、做 转字符串 用 全局路径解析统计 的 总段数。
    做 打印 用 做 字符串拼接 用 「│ 隐式重复:         」、做 转字符串 用 全局路径解析统计 的 隐式重复次数。
    做 打印 用 做 字符串拼接 用 「│ 总耗时(μs):       」、做 转字符串 用 全局路径解析统计 的 总解析耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 最大单次(μs):     」、做 转字符串 用 全局路径解析统计 的 最大单次耗时 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ AI复杂度调用:     」、做 转字符串 用 全局路径解析统计 的 AI复杂度调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI复杂度命中:     」、做 转字符串 用 全局路径解析统计 的 AI复杂度命中次数。
    做 打印 用 做 字符串拼接 用 「│ 解析错误:         」、做 转字符串 用 全局路径解析统计 的 解析错误次数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

SVG路径解析_导出诊断 接受 零参：
    若 全局路径解析AI 等于 空：
        做 打印 用 「[SVG路径解析 AI] 未初始化」。
        返还。
    完。
    AI 为 全局路径解析AI。
    做 打印 用 「[SVG路径解析 AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史平均段数(Q10): 」、做 转字符串 用 AI 的 历史平均段数。
    做 打印 用 做 字符串拼接 用 「  历史命令种类(Q10): 」、做 转字符串 用 AI 的 历史平均命令种类数。
    做 打印 用 做 字符串拼接 用 「  历史曲线比例(Q10): 」、做 转字符串 用 AI 的 历史曲线比例。
    做 打印 用 做 字符串拼接 用 「  连续简单路径: 」、做 转字符串 用 AI 的 连续简单路径数。
    做 打印 用 做 字符串拼接 用 「  连续复杂路径: 」、做 转字符串 用 AI 的 连续复杂路径数。
    做 打印 用 做 字符串拼接 用 「  预测下次复杂: 」、做 转字符串 用 AI 的 预测下次复杂。
完。

SVG路径解析_获取统计 接受 零参：
    返还 全局路径解析统计。
完。

SVG路径解析_获取AI状态 接受 零参：
    返还 全局路径解析AI。
完。

SVG路径解析_是复杂 接受 path：
    若 path 等于 空：
        返还 假。
    完。
    评分 为 做 SVGPath_分析复杂度 用 path。
    若 评分 大于 50：
        返还 真。
    完。
    返还 假。
完。

SVG路径解析_获取段数 接受 path：
    若 path 等于 空：
        返还 0。
    完。
    返还 path 的 段数。
完。

本章 收束。
