《图层管理获》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·辅助存根》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

CascadeLayer 是 一种 事物，含 名称、全名、顺序、子层列表、子层数量、子层容量、规则列表、规则数量、规则容量、父层。

CascadeLayerManager 是 一种 事物，含 根、层列表、层数量、层容量、下一个顺序、未分层规则、未分层规则数。

可变 全局层管理器 为 0。

CascadeLayerManager_获取 接受 零参：
    若 全局层管理器 等于 0：
        全局层管理器 为 做 堆_分配 用 CascadeLayerManager。
        全局层管理器 的 根 为 做 CascadeLayer_创建 用 「」、0。
        全局层管理器 的 层容量 为 64。
        全局层管理器 的 层列表 为 做 堆_分配_数组 用 64 乘 8。
        全局层管理器 的 层数量 为 0。
        全局层管理器 的 下一个顺序 为 0。
        全局层管理器 的 未分层规则 为 做 堆_分配_数组 用 256 乘 8。
        全局层管理器 的 未分层规则数 为 0。
    完。
    返还 全局层管理器。
完。

CascadeLayer_创建 接受 名称、父：
    层 为 做 堆_分配 用 CascadeLayer。
    层 的 名称 为 若 名称 不等于 0 则 做 字符串_复制 用 名称 否则 「」。
    层 的 全名 为 「」。
    层 的 顺序 为 0。
    层 的 子层容量 为 16。
    层 的 子层列表 为 做 堆_分配_数组 用 16 乘 8。
    层 的 子层数量 为 0。
    层 的 规则容量 为 128。
    层 的 规则列表 为 做 堆_分配_数组 用 128 乘 8。
    层 的 规则数量 为 0。
    层 的 父层 为 父。
    若 父 不等于 0 并且 做 字符串_长度 用 父 的 全名 大于 0：
        层 的 全名 为 做 字符串_连接 用 做 字符串_连接 用 父 的 全名、「.」、层 的 名称。
    否则：
        层 的 全名 为 层 的 名称。
    完。
    返还 层。
完。

CascadeLayer_查找或创建 接受 管理器、层名路径：
    若 管理器 等于 0 或者 层名路径 等于 0：
        返还 管理器 的 根。
    完。
    可变 当前 为 管理器 的 根。
    可变 开始 为 0。
    可变 结束 为 0。
    len 为 做 字节长度 用 层名路径。
    只要 结束 小于或等于 len：
        若 结束 等于 len 或者 做 字节取 用 层名路径、结束 等于 46：
            段名 为 做 字符串_子串 用 层名路径、开始、结束。
            子层 为 做 CascadeLayer_查找子 用 当前、段名。
            若 子层 等于 0：
                子层 为 做 CascadeLayer_创建 用 段名、当前。
                子层 的 顺序 为 管理器 的 下一个顺序。
                管理器 的 下一个顺序 为 管理器 的 下一个顺序 加 1。
            完。
            当前 为 子层。
            开始 为 结束 加 1。
        完。
        结束 为 结束 加 1。
    完。
    返还 当前。
完。

CascadeLayer_查找或创建_旧 接受 管理器、层名路径_unused：
    可变 已有 为 做 CascadeLayer_查找子 用 0、层名路径_unused。
    若 已有 不等于 0：
        返还 已有。
    完。
    新层 为 做 CascadeLayer_创建 用 层名路径、当前。
    新层 的 顺序 为 管理器 的 下一个顺序。
    管理器 的 下一个顺序 为 管理器 的 下一个顺序 加 1。
    若 当前 的 子层数量 小于 当前 的 子层容量：
        做 内存_写64 用 当前 的 子层列表 加 当前 的 子层数量 乘 8、新层。
        当前 的 子层数量 为 当前 的 子层数量 加 1。
    完。
    若 管理器 的 层数量 小于 管理器 的 层容量：
        做 内存_写64 用 管理器 的 层列表 加 管理器 的 层数量 乘 8、新层。
        管理器 的 层数量 为 管理器 的 层数量 加 1。
    完。
    返还 新层。
完。

CascadeLayer_查找子 接受 父、名称：
    可变 i 为 0。
    只要 i 小于 父 的 子层数量：
        子 为 做 内存_读64 用 父 的 子层列表 加 i 乘 8。
        若 子 不等于 0 并且 做 字符串_相等 用 子 的 名称、名称：
            返还 子。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

CascadeLayer_添加规则 接受 层、规则：
    若 层 等于 0 或者 规则 等于 0：
        返还。
    完。
    若 层 的 规则数量 小于 层 的 规则容量：
        做 内存_写64 用 层 的 规则列表 加 层 的 规则数量 乘 8、规则。
        层 的 规则数量 为 层 的 规则数量 加 1。
    完。
完。

CascadeLayer_比较优先级 接受 层a、层b：
    若 层a 等于 0 并且 层b 等于 0：
        返还 0。
    完。
    若 层a 等于 0：
        返还 1。
    完。
    若 层b 等于 0：
        返还 负 1。
    完。
    返还 层a 的 顺序 减 层b 的 顺序。
完。

可变 AI_图层管理获_帧率窗口 为 一组：（空）。

AI_图层管理获_帧率预测 接受 当前帧耗时：
    做 追加 用 AI_图层管理获_帧率窗口、当前帧耗时。
    若 做 计数 用 AI_图层管理获_帧率窗口 大于 30：
        做 列表_移除 用 AI_图层管理获_帧率窗口、0。
    完。
    若 做 计数 用 AI_图层管理获_帧率窗口 小于 3：
        返还 60。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_图层管理获_帧率窗口：
        总 为 总 加 AI_图层管理获_帧率窗口 的 第 i 项。
        i 为 i 加 1。
    完。
    平均帧耗时 为 总 除以 做 计数 用 AI_图层管理获_帧率窗口。
    若 平均帧耗时 等于 0：
        返还 60。
    完。
    返还 1000 除以 平均帧耗时。
完。

AI_图层管理获_质量降级建议 接受 帧率：
    若 帧率 小于 30：
        返还 「降低分辨率」。
    完。
    若 帧率 小于 45：
        返还 「跳过阴影」。
    完。
    返还 「全质量」。
完。

本章 收束。
