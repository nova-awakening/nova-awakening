《绘制图层绘》

引用《..·中枢·类型定义》。
引用《.·创建销毁》。
引用《.·文档节点》。
引用《.·内核直连层》。
引用《.·从布局树构》。
引用《..·中枢·字符串替换》。

    若 层.标志 位与 层标志_有溢出裁剪：
        绘制Layer_添加显示项(显示列表, 显示项 ：
            类型。
        })。
    完。
    若 s.border_radius_tl 大于 0 或者 s.border_radius_tr 大于 0：
        绘制Layer_添加显示项(显示列表, 显示项 ：
            类型。
        })。
    完。

    若 s.opacity 小于 100：
        绘制Layer_添加显示项(显示列表, 显示项 ：
            类型。
        })。
    完。
完。

绘制Layer_绘制内容 接受 层、显示列表、偏移x、偏移y)：
    ※ 绘制层关联的布局对象的所有非层子节点
    lo 为 层.布局对象。
    可变 子lo 为 lo.第一子。
    只要 子lo 不等于 0：
        若 子lo.样式 不等于 0 并且 子lo.样式.display 等于 Display_none：
            子lo 为 子lo.后兄。
            继续。
        完。

        ※ 如果子lo有自己的层,跳过(由z-order列表处理)
        若 做 绘制Layer_需要自己的层 用 子lo：
            子lo 为 子lo.后兄。
            继续。
        完。

        ※ 绘制子布局对象
        做 绘制Layer_绘制布局对象 用 子lo、显示列表、偏移x、偏移y。

        子lo 为 子lo.后兄。
    完。
完。

绘制Layer_绘制布局对象 接受 lo、显示列表、偏移x、偏移y)：
    s 为 lo.样式。
    若 s 等于 0：
    若 s.visibility 等于 Visibility_hidden：

    绘制x 为 lo.绝对x 加 偏移x。
    绘制y 为 lo.绝对y 加 偏移y。
    绘制w 为 做 布局Object_边框盒宽 用 lo。
    绘制h 为 做 布局Object_边框盒高 用 lo。

    ※ 背景
    若 s.background_color 不等于 0x00000000：
        绘制Layer_添加显示项(显示列表, 显示项 ：
            类型。
            x: 绘制x, y: 绘制y, w: 绘制w, h: 绘制h,。
            颜色: s.background_color。
        })。
    完。

    ※ 文本
    若 lo.类型 等于 布局类型_文本：
        文本 为 做 CharacterData_data 用 lo.DOM节点。
        若 文本 不等于 0：
            绘制Layer_添加显示项(显示列表, 显示项 ：
                类型。
                x: 绘制x, y: 绘制y, w: 绘制w, h: 绘制h,。
                颜色: s.color,。
                文本指针。
                文本长度。
                字号: s.font_size,。
                字重: s.font_weight,。
                字体: s.font_family。
            })。
        完。
    完。

    ※ 边框
    若 s.border_top_width 大于 0：
        绘制Layer_添加显示项(显示列表, 显示项 ：
            类型。
            x: 绘制x, y: 绘制y, w: 绘制w, h: 绘制h,。
            边框宽: s.border_top_width,。
            边框颜色: s.border_top_color。
        })。
    完。

    ※ 递归绘制子节点
    可变 子 为 lo.第一子。
    只要 子 不等于 0：
        若 !做 绘制Layer_需要自己的层 用 子：
            做 绘制Layer_绘制布局对象 用 子、显示列表、偏移x、偏移y。
        完。
        子 为 子.后兄。
    完。
完。

绘制Layer_添加显示项 接受 显示列表、项)：
    若 显示列表 等于 0：
    若 显示列表.显示列表数量 不小于 显示列表.显示列表容量：
        ※ 扩容
        新容量 为 显示列表.显示列表容量 乘 2。
        新列表 为 做 堆_分配_数组 用 新容量 乘 48。
        做 内存_复制 用 新列表、显示列表.显示列表、显示列表.显示列表数量 乘 48。
        做 堆_释放 用 显示列表.显示列表。
        显示列表.显示列表 为 新列表。
        显示列表.显示列表容量 为 新容量。
    完。
    做 内存_复制 用 显示列表.显示列表 加 显示列表.显示列表数量 乘 48、项、48。
    显示列表.显示列表数量 为 显示列表.显示列表数量 加 1。

本章 收束。
