《表单数据》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·整数转字符串》。

引用《.·内核直连层》。

引用《..·中枢·辅助存根》。

FormDataEntry 是 一种 事物，含 名称、值、文件名、是文件、下一个。

FormData 是 一种 事物，含 头、数量。

FormData_创建 接受 零参：
    fd 为 做 堆_分配 用 FormData。
    fd 的 头 为 0。
    fd 的 数量 为 0。
    返还 fd。
完。

FormData_append 接受 fd、名称、值：
    若 fd 等于 0 或者 名称 等于 0：
        返还。
    完。
    entry 为 做 堆_分配 用 FormDataEntry。
    entry 的 名称 为 做 字符串_复制 用 名称。
    entry 的 值 为 值。
    entry 的 文件名 为 0。
    entry 的 是文件 为 假。
    entry 的 下一个 为 fd 的 头。
    fd 的 头 为 entry。
    fd 的 数量 为 fd 的 数量 加 1。
完。

FormData_get 接受 fd、名称：
    若 fd 等于 0 或者 名称 等于 0：
        返还 0。
    完。
    可变 entry 为 fd 的 头。
    只要 entry 不等于 0：
        若 做 字符串_相等 用 entry 的 名称、名称：
            返还 entry 的 值。
        完。
        entry 为 entry 的 下一个。
    完。
    返还 0。
完。

FormData_delete 接受 fd、名称：
    若 fd 等于 0 或者 名称 等于 0：
        返还。
    完。
    可变 prev 为 0。
    可变 entry 为 fd 的 头。
    只要 entry 不等于 0：
        若 做 字符串_相等 用 entry 的 名称、名称：
            若 prev 等于 0：
                fd 的 头 为 entry 的 下一个。
            否则：
                prev 的 下一个 为 entry 的 下一个。
            完。
            fd 的 数量 为 fd 的 数量 减 1。
            entry 为 若 prev 等于 0 则 fd 的 头 否则 prev 的 下一个。
            继续。
        完。
        prev 为 entry。
        entry 为 entry 的 下一个。
    完。
完。

FormData_has 接受 fd、名称：
    返还 做 FormData_get 用 fd、名称 不等于 0。
完。

URLSearchParams 是 一种 事物，含 参数、数量。

URLSearchParams_创建 接受 查询字符串：
    usp 为 做 堆_分配 用 URLSearchParams。
    usp 的 参数 为 0。
    usp 的 数量 为 0。
    若 查询字符串 不等于 0：
        做 URLSearchParams_解析 用 usp、查询字符串。
    完。
    返还 usp。
完。

URLSearchParams_解析 接受 usp、qs：
    若 usp 等于 0 或者 qs 等于 0：
        返还。
    完。
    可变 pos 为 0。
    len 为 做 字节长度 用 qs。
    若 len 大于 0 并且 做 字节取 用 qs、0 等于 63：
        pos 为 1。
    完。
    只要 pos 小于 len：
        可变 eq 为 pos。
        只要 eq 小于 len 并且 做 字节取 用 qs、eq 不等于 61 并且 做 字节取 用 qs、eq 不等于 38：
            eq 为 eq 加 1。
        完。
        可变 end 为 eq。
        若 end 小于 len 并且 做 字节取 用 qs、end 等于 61：
            end 为 end 加 1。
        完。
        只要 end 小于 len 并且 做 字节取 用 qs、end 不等于 38：
            end 为 end 加 1。
        完。
        pos 为 end 加 1。
    完。
完。

URLSearchParams_get 接受 usp、名称：
    若 usp 等于 0 或者 名称 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 usp 的 数量：
        off 为 usp 的 数据 加 i 乘 16。
        key 为 做 内存_读64 用 off。
        若 做 字符串_相等 用 key、名称：
            返还 做 内存_读64 用 off 加 8。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

URLSearchParams_toString 接受 usp：
    若 usp 等于 0 或者 usp 的 数量 等于 0：
        返还 「」。
    完。
    buf 为 做 堆_分配 用 usp 的 数量 乘 64。
    可变 pos 为 0。
    可变 i 为 0。
    只要 i 小于 usp 的 数量：
        off 为 usp 的 数据 加 i 乘 16。
        key 为 做 内存_读64 用 off。
        val 为 做 内存_读64 用 off 加 8。
        若 i 大于 0：
            做 字节写 用 buf、pos、38。
            pos 为 pos 加 1。
        完。
        klen 为 做 字节长度 用 key。
        做 内存_复制 用 buf 加 pos、key、klen。
        pos 为 pos 加 klen。
        做 字节写 用 buf、pos、61。
        pos 为 pos 加 1。
        vlen 为 做 字节长度 用 val。
        做 内存_复制 用 buf 加 pos、val、vlen。
        pos 为 pos 加 vlen。
        i 为 i 加 1。
    完。
    做 字节写 用 buf、pos、0。
    返还 buf。
完。

本章 收束。
