《CSS_RGB_HSL解析》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·样式解析值》。

引用《.·内核直连层》。

引用《.·CSS属性字符判定》。

引用《.·CSS值解析》。

CSS解析_rgb函数 接受 文本、长度、pos：
    可变 p 为 pos。
    可变 r 为 0。
    可变 g 为 0。
    可变 b 为 0。
    可变 a 为 255。
    只要 p 小于 长度 并且 做 内存_读8 用 文本 加 p 不等于 0x28：
        p 为 p 加 1。
    完。
    p 为 p 加 1。
    只要 p 小于 长度 并且 做 CSS_是空白 用 做 内存_读8 用 文本 加 p：
        p 为 p 加 1。
    完。
    r_val 为 做 CSS解析_数字或长度 用 文本、长度、p。
    若 r_val 不等于 0：
        若 r_val 的 单位 等于 单位_百分比：
            r 为 r_val 的 整数值 乘 255 除以 10000。
        否则：
            r 为 r_val 的 整数值 除以 100。
        完。
    完。
    只要 p 小于 长度 并且 做 内存_读8 用 文本 加 p 不等于 0x2C 并且 做 内存_读8 用 文本 加 p 不等于 0x20 并且 做 内存_读8 用 文本 加 p 不等于 0x29：
        p 为 p 加 1。
    完。
    若 p 小于 长度 并且 (做 内存_读8 用 文本 加 p 等于 0x2C 或者 做 内存_读8 用 文本 加 p 等于 0x20)：
        p 为 p 加 1。
    完。
    只要 p 小于 长度 并且 做 CSS_是空白 用 做 内存_读8 用 文本 加 p：
        p 为 p 加 1。
    完。
    g_val 为 做 CSS解析_数字或长度 用 文本、长度、p。
    若 g_val 不等于 0：
        若 g_val 的 单位 等于 单位_百分比：
            g 为 g_val 的 整数值 乘 255 除以 10000。
        否则：
            g 为 g_val 的 整数值 除以 100。
        完。
    完。
    只要 p 小于 长度 并且 做 内存_读8 用 文本 加 p 不等于 0x2C 并且 做 内存_读8 用 文本 加 p 不等于 0x20 并且 做 内存_读8 用 文本 加 p 不等于 0x29：
        p 为 p 加 1。
    完。
    若 p 小于 长度 并且 (做 内存_读8 用 文本 加 p 等于 0x2C 或者 做 内存_读8 用 文本 加 p 等于 0x20)：
        p 为 p 加 1。
    完。
    只要 p 小于 长度 并且 做 CSS_是空白 用 做 内存_读8 用 文本 加 p：
        p 为 p 加 1。
    完。
    b_val 为 做 CSS解析_数字或长度 用 文本、长度、p。
    若 b_val 不等于 0：
        若 b_val 的 单位 等于 单位_百分比：
            b 为 b_val 的 整数值 乘 255 除以 10000。
        否则：
            b 为 b_val 的 整数值 除以 100。
        完。
    完。
    只要 p 小于 长度 并且 做 内存_读8 用 文本 加 p 不等于 0x2C 并且 做 内存_读8 用 文本 加 p 不等于 0x2F 并且 做 内存_读8 用 文本 加 p 不等于 0x29：
        p 为 p 加 1。
    完。
    若 p 小于 长度 并且 (做 内存_读8 用 文本 加 p 等于 0x2C 或者 做 内存_读8 用 文本 加 p 等于 0x2F)：
        p 为 p 加 1。
        只要 p 小于 长度 并且 做 CSS_是空白 用 做 内存_读8 用 文本 加 p：
            p 为 p 加 1。
        完。
        a_val 为 做 CSS解析_数字或长度 用 文本、长度、p。
        若 a_val 不等于 0：
            若 a_val 的 单位 等于 单位_百分比：
                a 为 a_val 的 整数值 乘 255 除以 10000。
            否则：
                a 为 a_val 的 整数值 乘 255 除以 100。
            完。
        完。
    完。
    若 r 小于 0：
        r 为 0。
    完。
    若 r 大于 255：
        r 为 255。
    完。
    若 g 小于 0：
        g 为 0。
    完。
    若 g 大于 255：
        g 为 255。
    完。
    若 b 小于 0：
        b 为 0。
    完。
    若 b 大于 255：
        b 为 255。
    完。
    若 a 小于 0：
        a 为 0。
    完。
    若 a 大于 255：
        a 为 255。
    完。
    v 为 做 CSSParsedValue_创建 用 CSS值_颜色。
    v 的 颜色值 为 a 乘 0x1000000 加 r 乘 0x10000 加 g 乘 0x100 加 b。
    返还 v。
完。

CSS解析_hsl函数 接受 文本、长度、pos：
    可变 p 为 pos。
    只要 p 小于 长度 并且 做 内存_读8 用 文本 加 p 不等于 0x28：
        p 为 p 加 1。
    完。
    p 为 p 加 1。
    只要 p 小于 长度 并且 做 CSS_是空白 用 做 内存_读8 用 文本 加 p：
        p 为 p 加 1。
    完。
    h_val 为 做 CSS解析_数字或长度 用 文本、长度、p。
    可变 h 为 0。
    若 h_val 不等于 0：
        h 为 h_val 的 整数值 除以 100。
    完。
    只要 p 小于 长度 并且 做 内存_读8 用 文本 加 p 不等于 0x2C 并且 做 内存_读8 用 文本 加 p 不等于 0x20 并且 做 内存_读8 用 文本 加 p 不等于 0x29：
        p 为 p 加 1。
    完。
    若 p 小于 长度 并且 (做 内存_读8 用 文本 加 p 等于 0x2C 或者 做 内存_读8 用 文本 加 p 等于 0x20)：
        p 为 p 加 1。
    完。
    只要 p 小于 长度 并且 做 CSS_是空白 用 做 内存_读8 用 文本 加 p：
        p 为 p 加 1。
    完。
    s_val 为 做 CSS解析_数字或长度 用 文本、长度、p。
    可变 s 为 0。
    若 s_val 不等于 0：
        s 为 s_val 的 整数值 除以 100。
    完。
    只要 p 小于 长度 并且 做 内存_读8 用 文本 加 p 不等于 0x2C 并且 做 内存_读8 用 文本 加 p 不等于 0x20 并且 做 内存_读8 用 文本 加 p 不等于 0x29：
        p 为 p 加 1。
    完。
    若 p 小于 长度 并且 (做 内存_读8 用 文本 加 p 等于 0x2C 或者 做 内存_读8 用 文本 加 p 等于 0x20)：
        p 为 p 加 1。
    完。
    只要 p 小于 长度 并且 做 CSS_是空白 用 做 内存_读8 用 文本 加 p：
        p 为 p 加 1。
    完。
    l_val 为 做 CSS解析_数字或长度 用 文本、长度、p。
    可变 l 为 0。
    若 l_val 不等于 0：
        l 为 l_val 的 整数值 除以 100。
    完。
    可变 a 为 255。
    只要 p 小于 长度 并且 做 内存_读8 用 文本 加 p 不等于 0x2C 并且 做 内存_读8 用 文本 加 p 不等于 0x2F 并且 做 内存_读8 用 文本 加 p 不等于 0x29：
        p 为 p 加 1。
    完。
    若 p 小于 长度 并且 (做 内存_读8 用 文本 加 p 等于 0x2C 或者 做 内存_读8 用 文本 加 p 等于 0x2F)：
        p 为 p 加 1。
        只要 p 小于 长度 并且 做 CSS_是空白 用 做 内存_读8 用 文本 加 p：
            p 为 p 加 1。
        完。
        a_val 为 做 CSS解析_数字或长度 用 文本、长度、p。
        若 a_val 不等于 0：
            若 a_val 的 单位 等于 单位_百分比：
                a 为 a_val 的 整数值 乘 255 除以 10000。
            否则：
                a 为 a_val 的 整数值 乘 255 除以 100。
            完。
        完。
    完。
    RGB 为 做 HSL转RGB 用 h、s、l。
    r 为 RGB 除以 0x10000。
    g 为 RGB 除以 0x100 取余于 256。
    b 为 RGB 取余于 256。
    若 a 小于 0：
        a 为 0。
    完。
    若 a 大于 255：
        a 为 255。
    完。
    v 为 做 CSSParsedValue_创建 用 CSS值_颜色。
    v 的 颜色值 为 a 乘 0x1000000 加 r 乘 0x10000 加 g 乘 0x100 加 b。
    返还 v。
完。

HSL转RGB 接受 h、s、l：
    可变 H 为 h 取余于 360。
    若 H 小于 0：
        H 为 H 加 360。
    完。
    S 为 s。
    若 S 大于 100：
        S 为 100。
    完。
    若 S 小于 0：
        S 为 0。
    完。
    L 为 l。
    若 L 大于 100：
        L 为 100。
    完。
    若 L 小于 0：
        L 为 0。
    完。
    若 S 等于 0：
        灰 为 L 乘 255 除以 100。
        返还 灰 乘 0x10000 加 灰 乘 0x100 加 灰。
    完。
    可变 q 为 0。
    若 L 小于 50：
        q 为 L 乘 (100 加 S) 除以 100。
    否则：
        q 为 L 加 S 减 L 乘 S 除以 100。
    完。
    p2 为 2 乘 L 减 q。
    r 为 做 HSL_Hue2RGB 用 p2、q、H 加 120 乘 255 除以 100。
    g 为 做 HSL_Hue2RGB 用 p2、q、H 乘 255 除以 100。
    b 为 做 HSL_Hue2RGB 用 p2、q、H 减 120 乘 255 除以 100。
    若 r 大于 255：
        r 为 255。
    完。
    若 r 小于 0：
        r 为 0。
    完。
    若 g 大于 255：
        g 为 255。
    完。
    若 g 小于 0：
        g 为 0。
    完。
    若 b 大于 255：
        b 为 255。
    完。
    若 b 小于 0：
        b 为 0。
    完。
    返还 r 乘 0x10000 加 g 乘 0x100 加 b。
完。

HSL_Hue2RGB 接受 p、q、t：
    可变 T 为 t。
    若 T 小于 0：
        T 为 T 加 360。
    完。
    若 T 大于 360：
        T 为 T 减 360。
    完。
    若 T 小于 60：
        返还 p 加 (q 减 p) 乘 T 除以 60。
    完。
    若 T 小于 180：
        返还 q。
    完。
    若 T 小于 240：
        返还 p 加 (q 减 p) 乘 (240 减 T) 除以 60。
    完。
    返还 p。
完。

可变 AI_感知甲乙_样式缓存命中 为 0。

AI_感知甲乙_选择器复杂度 接受 选择器长度、组合子数：
    若 组合子数 大于 5：
        返还 「高复杂度」。
    完。
    若 组合子数 大于 2：
        返还 「中复杂度」。
    完。
    返还 「低复杂度」。
完。

AI_感知甲乙_布局缓存 接受 元素哈希：
    AI_感知甲乙_样式缓存命中 为 AI_感知甲乙_样式缓存命中 加 1。
    返还 元素哈希。
完。

本章 收束。
