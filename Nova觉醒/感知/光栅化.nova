《光栅化》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《.·字形度量》。

引用《.·字体扩展渲》。

引用《.·字映射字符》。

引用《..·执行·控制台输出》。

字体_光栅化字形 接受 font、glyphID、像素大小：
    若 font 等于 0：
        返还 0。
    完。
    cached 为 做 Glyph缓存_查找 用 0、glyphID、像素大小。
    若 cached 不等于 0：
        返还 cached。
    完。
    outline 为 做 字体_获取字形轮廓 用 font、glyphID。
    若 outline 等于 0：
        返还 0。
    完。
    scale 为 像素大小 乘 1024 除以 font 的 unitsPerEm。
    宽度 为 (outline 的 xMax 减 outline 的 xMin) 乘 scale 除以 1024 加 2。
    高度 为 (outline 的 yMax 减 outline 的 yMin) 乘 scale 除以 1024 加 2。
    若 宽度 小于或等于 0：
        宽度 为 1。
    完。
    若 高度 小于或等于 0：
        高度 为 1。
    完。
    位图 为 做 堆_分配 用 宽度 乘 高度。
    做 内存_填充 用 位图、0、宽度 乘 高度。
    可变 sy 为 0。
    只要 sy 小于 高度：
        glyph_y 为 sy 乘 1024 除以 scale 加 outline 的 yMin。
        可变 sx 为 0。
        只要 sx 小于 宽度：
            glyph_x 为 sx 乘 1024 除以 scale 加 outline 的 xMin。
            可变 cov 为 0。
            若 glyph_x 大于或等于 outline 的 xMin 并且 glyph_x 小于或等于 outline 的 xMax 并且 glyph_y 大于或等于 outline 的 yMin 并且 glyph_y 小于或等于 outline 的 yMax：
                dx_min 为 glyph_x 减 outline 的 xMin。
                dx_max 为 outline 的 xMax 减 glyph_x。
                dy_min 为 glyph_y 减 outline 的 yMin。
                dy_max 为 outline 的 yMax 减 glyph_y。
                可变 边距 为 dx_min。
                若 dx_max 小于 边距：
                    边距 为 dx_max。
                完。
                若 dy_min 小于 边距：
                    边距 为 dy_min。
                完。
                若 dy_max 小于 边距：
                    边距 为 dy_max。
                完。
                cov 为 若 边距 大于 64 则 255 否则 边距 乘 255 除以 64。
            完。
            若 cov 大于 255：
                cov 为 255。
            完。
            做 字节写 用 位图、sy 乘 宽度 加 sx、cov。
            sx 为 sx 加 1。
        完。
        sy 为 sy 加 1。
    完。
    metrics 为 做 字体_获取字形度量 用 font、glyphID。
    aw 为 0。
    若 metrics 不等于 0：
        aw 为 metrics 的 advanceWidth 乘 scale 除以 1024。
    完。
    返还 做 Glyph缓存_插入 用 0、glyphID、像素大小、位图、宽度、高度、outline 的 xMin 乘 scale 除以 1024、outline 的 yMin 乘 scale 除以 1024、aw。
完。

GlyphRun 是 一种 事物，含 字形ID、位置X、位置Y、字形数、字体、像素大小、总宽度。

Text布局_排版 接受 font、文本、长度、像素大小：
    若 font 等于 0 或者 文本 等于 0 或者 长度 等于 0：
        返还 0。
    完。
    run 为 做 堆_分配 用 GlyphRun。
    run 的 字体 为 font。
    run 的 像素大小 为 像素大小。
    run 的 字形数 为 0。
    run 的 总宽度 为 0。
    容量 为 长度。
    run 的 字形ID 为 做 堆_分配_数组 用 容量 乘 2。
    run 的 位置X 为 做 堆_分配_数组 用 容量 乘 4。
    run 的 位置Y 为 做 堆_分配_数组 用 容量 乘 4。
    scale 为 像素大小 乘 1024 除以 font 的 unitsPerEm。
    可变 cursorX 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        b0 为 做 字节取 用 文本、i。
        可变 cp 为 0。
        可变 字节数 为 1。
        若 b0 小于 0x80：
            cp 为 b0。
            字节数 为 1。
        否则若 b0 小于 0xE0 并且 i 加 1 小于 长度：
            cp 为 (b0 位与 0x1F) 左移 6 位或 做 字节取 用 文本、i 加 1 位与 0x3F。
            字节数 为 2。
        否则若 b0 小于 0xF0 并且 i 加 2 小于 长度：
            cp 为 (b0 位与 0x0F) 左移 12 位或 (做 字节取 用 文本、i 加 1 位与 0x3F) 左移 6 位或 做 字节取 用 文本、i 加 2 位与 0x3F。
            字节数 为 3。
        否则若 i 加 3 小于 长度：
            cp 为 (b0 位与 0x07) 左移 18 位或 (做 字节取 用 文本、i 加 1 位与 0x3F) 左移 12 位或 (做 字节取 用 文本、i 加 2 位与 0x3F) 左移 6 位或 做 字节取 用 文本、i 加 3 位与 0x3F。
            字节数 为 4。
        否则：
            cp 为 0xFFFD。
            字节数 为 1。
        完。
        glyphID 为 做 字体_字符到字形 用 font、cp。
        做 内存_写16 用 run 的 字形ID 加 run 的 字形数 乘 2、glyphID。
        做 内存_写32 用 run 的 位置X 加 run 的 字形数 乘 4、cursorX。
        做 内存_写32 用 run 的 位置Y 加 run 的 字形数 乘 4、0。
        run 的 字形数 为 run 的 字形数 加 1。
        metrics 为 做 字体_获取字形度量 用 font、glyphID。
        若 metrics 不等于 0：
            cursorX 为 cursorX 加 metrics 的 advanceWidth 乘 scale 除以 1024。
        完。
        若 i 加 1 小于 长度 并且 font 的 kern偏移 大于 0：
            nextCp 为 做 字节取 用 文本、i 加 1。
            nextGlyph 为 做 字体_字符到字形 用 font、nextCp。
            kern 为 做 字体_获取kern值 用 font、glyphID、nextGlyph。
            cursorX 为 cursorX 加 kern 乘 scale 除以 1024。
        完。
        i 为 i 加 字节数。
    完。
    run 的 总宽度 为 cursorX。
    返还 run。
完。

字体_获取kern值 接受 font、left、right：
    若 font 等于 0 或者 font 的 kern偏移 等于 0：
        返还 0。
    完。
    返还 0。
完。

CJK_抗锯齿_阈值 为 32。

CJK_码点范围_低 为 0x4E00。

CJK_码点范围_高 为 0x9FFF。

可变 光栅化_汉字命中数 为 0。

光栅化_是否汉字 接受 码点：
    返还 码点 大于或等于 CJK_码点范围_低 并且 码点 小于或等于 CJK_码点范围_高。
完。

光栅化_中文抗锯齿覆盖率 接受 边距：
    若 边距 大于 CJK_抗锯齿_阈值：
        返还 255。
    完。
    返还 边距 乘 255 除以 CJK_抗锯齿_阈值。
完。

可变 AI_光栅化_样式缓存命中 为 0。

AI_光栅化_选择器复杂度 接受 选择器长度、组合子数：
    若 组合子数 大于 5：
        返还 「高复杂度」。
    完。
    若 组合子数 大于 2：
        返还 「中复杂度」。
    完。
    返还 「低复杂度」。
完。

AI_光栅化_布局缓存 接受 元素哈希：
    AI_光栅化_样式缓存命中 为 AI_光栅化_样式缓存命中 加 1。
    返还 元素哈希。
完。

本章 收束。
