《CSS作用域规则》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·计算》。

引用《..·执行·控制台输出》。

引用《.·内核直连层》。

引用《.·选择匹配》。

引用《.·原子驻留》。

引用《.·HTTP头部管理》。

引用《..·中枢·整数转字符串》。

引用《.·文档节点》。

ScopeRule 是 一种 事物，含 上界选择器、下界选择器、规则列表、规则数量、规则容量。

ScopeRule_创建 接受 上界、下界：
    sr 为 做 堆_分配 用 ScopeRule。
    sr 的 上界选择器 为 上界。
    sr 的 下界选择器 为 下界。
    sr 的 规则容量 为 64。
    sr 的 规则列表 为 做 堆_分配_数组 用 64 乘 8。
    sr 的 规则数量 为 0。
    返还 sr。
完。

ScopeRule_匹配元素 接受 sr、元素：
    若 sr 等于 0 或者 元素 等于 0：
        返还 假。
    完。
    可变 祖先 为 元素。
    可变 在范围内 为 假。
    只要 祖先 不等于 0：
        若 做 Selector_匹配 用 sr 的 上界选择器、祖先：
            在范围内 为 真。
            跳出。
        完。
        祖先 为 祖先 的 父节点。
    完。
    若 非 在范围内：
        返还 假。
    完。
    若 sr 的 下界选择器 不等于 0：
        可变 检查 为 元素。
        只要 检查 不等于 0 并且 检查 不等于 祖先：
            若 做 Selector_匹配 用 sr 的 下界选择器、检查：
                返还 假。
            完。
            检查 为 检查 的 父节点。
        完。
    完。
    返还 真。
完。

CSS解析_数字 接受 str：
    若 str 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    len 为 做 字节长度 用 str。
    只要 i 小于 len 并且 做 字节取 用 str、i 等于 32：
        i 为 i 加 1。
    完。
    可变 负 为 假。
    若 i 小于 len 并且 做 字节取 用 str、i 等于 0x2D：
        负 为 真。
        i 为 i 加 1。
    完。
    可变 result 为 0。
    只要 i 小于 len 并且 做 字节取 用 str、i 大于或等于 0x30 并且 做 字节取 用 str、i 小于或等于 0x39：
        result 为 result 乘 10 加 (做 字节取 用 str、i 减 0x30)。
        i 为 i 加 1。
    完。
    若 负：
        result 为 0 减 result。
    完。
    返还 result。
完。

整数转字符串 接受 n：
    缓冲 为 做 堆_分配 用 20。
    可变 val 为 n。
    若 val 等于 0：
        做 内存_写8 用 缓冲、0x30。
        做 内存_写8 用 缓冲 加 1、0。
        返还 缓冲。
    完。
    可变 负 为 假。
    若 val 小于 0：
        负 为 真。
        val 为 0 减 val。
    完。
    可变 i 为 0。
    临时 为 做 堆_分配 用 20。
    只要 val 大于 0：
        做 内存_写8 用 临时 加 i、val 取余于 10 加 0x30。
        val 为 val 除以 10。
        i 为 i 加 1。
    完。
    可变 pos 为 0。
    若 负：
        做 内存_写8 用 缓冲、0x2D。
        pos 为 1。
    完。
    可变 j 为 i 减 1。
    只要 j 大于或等于 0：
        做 内存_写8 用 缓冲 加 pos、做 内存_读8 用 临时 加 j。
        pos 为 pos 加 1。
        j 为 j 减 1。
    完。
    做 内存_写8 用 缓冲 加 pos、0。
    做 堆_释放 用 临时。
    返还 缓冲。
完。

Selector_匹配 接受 sel、elem：
    若 sel 等于 0 或者 elem 等于 0：
        返还 假。
    完。
    若 sel 的 类型 等于 SEL_TAG：
        扩展 为 做 元素_获取扩展 用 elem。
        若 扩展 等于 0：
            返还 假。
        完。
        返还 做 字符串_相等_不区分大小写 用 扩展 的 标签名、sel 的 名称。
    完。
    若 sel 的 类型 等于 SEL_CLASS：
        返还 做 元素_hasClass 用 elem、sel 的 名称。
    完。
    若 sel 的 类型 等于 SEL_ID：
        返还 做 字符串_相等 用 做 元素_getAttribute 用 elem、「id」、sel 的 名称。
    完。
    若 sel 的 类型 等于 SEL_UNIVERSAL：
        返还 真。
    完。
    若 sel 的 类型 等于 SEL_ATTR：
        返还 做 Selector_匹配属性 用 sel、elem。
    完。
    若 sel 的 类型 等于 SEL_PSEUDO_CLASS：
        返还 做 Selector_匹配伪类 用 sel、elem。
    完。
    若 sel 的 类型 等于 SEL_COMPOUND：
        可变 i 为 0。
        只要 i 小于 sel 的 子项数量：
            若 非 做 Selector_匹配 用 做 内存_读64 用 sel 的 子项列表 加 i 乘 8、elem：
                返还 假。
            完。
            i 为 i 加 1。
        完。
        返还 真。
    完。
    若 sel 的 类型 等于 SEL_COMPLEX：
        返还 做 Selector_匹配复杂 用 sel、elem。
    完。
    返还 假。
完。

本章 收束。
