《网缓图像从位》

引用《.·图位图像素缓》。

引用《..·稳态·图处显示》。

引用《..·推理·通信总线》。

引用《.·格式检测》。

引用《.·内核直连层》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《..·执行·底层内存操》。

IMAGE_SOURCE_CPU 为 0。

IMAGE_SOURCE_图处 为 1。

IMAGE_SOURCE_LAZY 为 2。

IMAGE_FORMAT_PNG 为 1。

IMAGE_FORMAT_JPEG 为 2。

IMAGE_FORMAT_WEBP 为 3。

IMAGE_FORMAT_GIF 为 4。

IMAGE_FORMAT_BMP 为 5。

IMAGE_FORMAT_ICO 为 6。

Sk图像 是 一种 事物，含 来源、宽、高、颜色类型、位图、gpu纹理、原始数据、原始长度、图像格式、已解码、引用计数、唯一ID。

可变 Sk图像_下一ID 为 1。

Sk图像_从位图 接受 bm：
    若 bm 等于 0：
        返还 0。
    完。
    img 为 做 堆_分配 用 Sk图像。
    img 的 来源 为 IMAGE_SOURCE_CPU。
    img 的 宽 为 bm 的 宽。
    img 的 高 为 bm 的 高。
    img 的 颜色类型 为 bm 的 颜色类型。
    img 的 位图 为 bm。
    bm 的 引用计数 为 bm 的 引用计数 加 1。
    img 的 gpu纹理 为 0。
    img 的 原始数据 为 0。
    img 的 原始长度 为 0。
    img 的 已解码 为 真。
    img 的 引用计数 为 1。
    img 的 唯一ID 为 Sk图像_下一ID。
    Sk图像_下一ID 为 Sk图像_下一ID 加 1。
    返还 img。
完。

Sk图像_从图处纹理 接受 tex、宽、高：
    若 tex 等于 0：
        返还 0。
    完。
    img 为 做 堆_分配 用 Sk图像。
    img 的 来源 为 IMAGE_SOURCE_图处。
    img 的 宽 为 宽。
    img 的 高 为 高。
    img 的 颜色类型 为 COLORTYPE_RGBA8888。
    img 的 位图 为 0。
    img 的 gpu纹理 为 tex。
    tex 的 引用计数 为 tex 的 引用计数 加 1。
    img 的 已解码 为 真。
    img 的 引用计数 为 1。
    img 的 唯一ID 为 Sk图像_下一ID。
    Sk图像_下一ID 为 Sk图像_下一ID 加 1。
    返还 img。
完。

Sk图像_从编码数据 接受 数据、长度：
    若 数据 等于 0 或者 长度 小于 4：
        返还 0。
    完。
    img 为 做 堆_分配 用 Sk图像。
    img 的 来源 为 IMAGE_SOURCE_LAZY。
    img 的 原始数据 为 数据。
    img 的 原始长度 为 长度。
    img 的 图像格式 为 做 Sk图像_检测格式 用 数据、长度。
    img 的 已解码 为 假。
    img 的 位图 为 0。
    img 的 gpu纹理 为 0。
    img 的 引用计数 为 1。
    img 的 唯一ID 为 Sk图像_下一ID。
    Sk图像_下一ID 为 Sk图像_下一ID 加 1。
    做 Sk图像_预读尺寸 用 img。
    返还 img。
完。

Sk图像_增引用 接受 img：
    若 img 等于 0：
        返还。
    完。
    img 的 引用计数 为 img 的 引用计数 加 1。
完。

Sk图像_减引用 接受 img：
    若 img 等于 0：
        返还。
    完。
    img 的 引用计数 为 img 的 引用计数 减 1。
    若 img 的 引用计数 小于或等于 0：
        做 Sk图像_销毁 用 img。
    完。
完。

Sk图像_销毁 接受 img：
    若 img 等于 0：
        返还。
    完。
    若 img 的 位图 不等于 0：
        bm 为 img 的 位图。
        bm 的 引用计数 为 bm 的 引用计数 减 1。
        若 bm 的 引用计数 小于或等于 0：
            若 bm 的 像素 不等于 0：
                做 平台_释放内存 用 bm 的 像素、bm 的 宽 乘 bm 的 高 乘 4。
            完。
            做 堆_释放 用 bm。
        完。
    完。
    若 img 的 gpu纹理 不等于 0：
        tex 为 img 的 gpu纹理。
        tex 的 引用计数 为 tex 的 引用计数 减 1。
        若 tex 的 引用计数 小于或等于 0：
            做 堆_释放 用 tex。
        完。
    完。
    若 img 的 原始数据 不等于 0 并且 img 的 来源 等于 IMAGE_SOURCE_LAZY：
    完。
    做 堆_释放 用 img。
完。

Sk图像_获取像素 接受 img、x、y：
    若 img 等于 0：
        返还 0。
    完。
    若 img 的 已解码 等于 假：
        做 Sk图像_确保解码 用 img。
    完。
    若 img 的 位图 等于 0：
        返还 0。
    完。
    若 x 小于 0 或者 x 大于或等于 img 的 宽 或者 y 小于 0 或者 y 大于或等于 img 的 高：
        返还 0。
    完。
    偏移 为 (y 乘 img 的 宽 加 x) 乘 4。
    返还 做 内存_读32 用 img 的 位图 的 像素 加 偏移。
完。

Sk图像_设置像素 接受 img、x、y、颜色：
    若 img 等于 0 或者 img 的 位图 等于 0：
        返还。
    完。
    若 x 小于 0 或者 x 大于或等于 img 的 宽 或者 y 小于 0 或者 y 大于或等于 img 的 高：
        返还。
    完。
    偏移 为 (y 乘 img 的 宽 加 x) 乘 4。
    做 内存_写32 用 img 的 位图 的 像素 加 偏移、颜色。
完。

Sk图像_裁剪 接受 img、左、上、右、下：
    若 img 等于 0：
        返还 空。
    完。
    若 img 的 已解码 等于 假：
        做 Sk图像_确保解码 用 img。
    完。
    若 img 的 位图 等于 0：
        返还 空。
    完。
    可变 实际左 为 左。
    可变 实际上 为 上。
    可变 实际右 为 右。
    可变 实际下 为 下。
    若 实际左 小于 0：
        实际左 为 0。
    完。
    若 实际上 小于 0：
        实际上 为 0。
    完。
    若 实际右 大于 img 的 宽：
        实际右 为 img 的 宽。
    完。
    若 实际下 大于 img 的 高：
        实际下 为 img 的 高。
    完。
    若 实际左 大于或等于 实际右 或者 实际上 大于或等于 实际下：
        返还 空。
    完。
    新宽 为 实际右 减 实际左。
    新高 为 实际下 减 实际上。
    新位图 为 做 Sk位图_创建 用 新宽、新高、img 的 颜色类型。
    若 新位图 等于 0：
        返还 空。
    完。
    可变 y 为 0。
    只要 y 小于 新高：
        源行 为 img 的 位图 的 像素 加 ((实际上 加 y) 乘 img 的 宽 加 实际左) 乘 4。
        目标行 为 新位图 的 像素 加 y 乘 新宽 乘 4。
        做 内存_复制 用 目标行、源行、新宽 乘 4。
        y 为 y 加 1。
    完。
    返还 做 Sk图像_从位图 用 新位图。
完。

Sk图像_缩放 接受 img、新宽、新高：
    若 img 等于 0 或者 新宽 小于或等于 0 或者 新高 小于或等于 0：
        返还 空。
    完。
    若 img 的 已解码 等于 假：
        做 Sk图像_确保解码 用 img。
    完。
    若 img 的 位图 等于 0：
        返还 空。
    完。
    新位图 为 做 Sk位图_创建 用 新宽、新高、img 的 颜色类型。
    若 新位图 等于 0：
        返还 空。
    完。
    x比例 为 img 的 宽 乘 10000 除以 新宽。
    y比例 为 img 的 高 乘 10000 除以 新高。
    可变 y 为 0。
    只要 y 小于 新高：
        源y 为 y 乘 y比例 除以 10000。
        若 源y 大于或等于 img 的 高：
            源y 为 img 的 高 减 1。
        完。
        可变 x 为 0。
        只要 x 小于 新宽：
            源x 为 x 乘 x比例 除以 10000。
            若 源x 大于或等于 img 的 宽：
                源x 为 img 的 宽 减 1。
            完。
            颜色 为 做 内存_读32 用 img 的 位图 的 像素 加 (源y 乘 img 的 宽 加 源x) 乘 4。
            做 内存_写32 用 新位图 的 像素 加 (y 乘 新宽 加 x) 乘 4、颜色。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 做 Sk图像_从位图 用 新位图。
完。

Sk图像_双线性缩放 接受 img、新宽、新高：
    若 img 等于 0 或者 新宽 小于或等于 0 或者 新高 小于或等于 0：
        返还 空。
    完。
    若 img 的 已解码 等于 假：
        做 Sk图像_确保解码 用 img。
    完。
    若 img 的 位图 等于 0：
        返还 空。
    完。
    新位图 为 做 Sk位图_创建 用 新宽、新高、img 的 颜色类型。
    若 新位图 等于 0：
        返还 空。
    完。
    x比例 为 (img 的 宽 减 1) 乘 10000 除以 (新宽 减 1)。
    y比例 为 (img 的 高 减 1) 乘 10000 除以 (新高 减 1)。
    可变 y 为 0。
    只要 y 小于 新高：
        源y_定点 为 y 乘 y比例。
        源y 为 源y_定点 除以 10000。
        y分数 为 源y_定点 取余于 10000 乘 256 除以 10000。
        若 源y 大于或等于 img 的 高 减 1：
            源y 为 img 的 高 减 2。
        完。
        可变 x 为 0。
        只要 x 小于 新宽：
            源x_定点 为 x 乘 x比例。
            源x 为 源x_定点 除以 10000。
            x分数 为 源x_定点 取余于 10000 乘 256 除以 10000。
            若 源x 大于或等于 img 的 宽 减 1：
                源x 为 img 的 宽 减 2。
            完。
            基址 为 img 的 位图 的 像素。
            c00 为 做 内存_读32 用 基址 加 (源y 乘 img 的 宽 加 源x) 乘 4。
            c10 为 做 内存_读32 用 基址 加 (源y 乘 img 的 宽 加 源x 加 1) 乘 4。
            c01 为 做 内存_读32 用 基址 加 ((源y 加 1) 乘 img 的 宽 加 源x) 乘 4。
            c11 为 做 内存_读32 用 基址 加 ((源y 加 1) 乘 img 的 宽 加 源x 加 1) 乘 4。
            结果 为 做 Sk图像_双线性插值 用 c00、c10、c01、c11、x分数、y分数。
            做 内存_写32 用 新位图 的 像素 加 (y 乘 新宽 加 x) 乘 4、结果。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 做 Sk图像_从位图 用 新位图。
完。

Sk图像_双线性插值 接受 c00、c10、c01、c11、fx、fy：
    ifx 为 256 减 fx。
    ify 为 256 减 fy。
    a 为 (做 Sk颜色_A 用 c00 乘 ifx 乘 ify 加 做 Sk颜色_A 用 c10 乘 fx 乘 ify 加 做 Sk颜色_A 用 c01 乘 ifx 乘 fy 加 做 Sk颜色_A 用 c11 乘 fx 乘 fy) 除以 65536。
    r 为 (做 Sk颜色_R 用 c00 乘 ifx 乘 ify 加 做 Sk颜色_R 用 c10 乘 fx 乘 ify 加 做 Sk颜色_R 用 c01 乘 ifx 乘 fy 加 做 Sk颜色_R 用 c11 乘 fx 乘 fy) 除以 65536。
    g 为 (做 Sk颜色_G 用 c00 乘 ifx 乘 ify 加 做 Sk颜色_G 用 c10 乘 fx 乘 ify 加 做 Sk颜色_G 用 c01 乘 ifx 乘 fy 加 做 Sk颜色_G 用 c11 乘 fx 乘 fy) 除以 65536。
    b 为 (做 Sk颜色_B 用 c00 乘 ifx 乘 ify 加 做 Sk颜色_B 用 c10 乘 fx 乘 ify 加 做 Sk颜色_B 用 c01 乘 ifx 乘 fy 加 做 Sk颜色_B 用 c11 乘 fx 乘 fy) 除以 65536。
    返还 a 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
完。

Sk颜色_A 接受 c：
    返还 c 右移 24 位与 255。
完。

Sk颜色_R 接受 c：
    返还 c 右移 16 位与 255。
完。

Sk颜色_G 接受 c：
    返还 c 右移 8 位与 255。
完。

Sk颜色_B 接受 c：
    返还 c 位与 255。
完。

Sk图像_绘制到帧缓冲 接受 img、帧缓冲、步幅、帧宽、帧高、目标x、目标y：
    若 img 等于 0 或者 帧缓冲 等于 0：
        返还。
    完。
    若 img 的 已解码 等于 假：
        做 Sk图像_确保解码 用 img。
    完。
    若 img 的 位图 等于 0：
        返还。
    完。
    可变 源x起始 为 0。
    可变 源y起始 为 0。
    可变 绘制宽 为 img 的 宽。
    可变 绘制高 为 img 的 高。
    若 目标x 小于 0：
        源x起始 为 0 减 目标x。
        绘制宽 为 绘制宽 加 目标x。
        目标x 为 0。
    完。
    若 目标y 小于 0：
        源y起始 为 0 减 目标y。
        绘制高 为 绘制高 加 目标y。
        目标y 为 0。
    完。
    若 目标x 加 绘制宽 大于 帧宽：
        绘制宽 为 帧宽 减 目标x。
    完。
    若 目标y 加 绘制高 大于 帧高：
        绘制高 为 帧高 减 目标y。
    完。
    若 绘制宽 小于或等于 0 或者 绘制高 小于或等于 0：
        返还。
    完。
    可变 y 为 0。
    只要 y 小于 绘制高：
        源行 为 img 的 位图 的 像素 加 ((源y起始 加 y) 乘 img 的 宽 加 源x起始) 乘 4。
        目标行 为 帧缓冲 加 (目标y 加 y) 乘 步幅 加 目标x 乘 4。
        可变 x 为 0。
        只要 x 小于 绘制宽：
            源颜色 为 做 内存_读32 用 源行 加 x 乘 4。
            srcA 为 做 Sk颜色_A 用 源颜色。
            若 srcA 等于 255：
                做 内存_写32 用 目标行 加 x 乘 4、源颜色。
            否则若 srcA 大于 0：
                目标颜色 为 做 内存_读32 用 目标行 加 x 乘 4。
                invA 为 255 减 srcA。
                outA 为 srcA 加 做 Sk颜色_A 用 目标颜色 乘 invA 除以 255。
                outR 为 做 Sk颜色_R 用 源颜色 加 做 Sk颜色_R 用 目标颜色 乘 invA 除以 255。
                outG 为 做 Sk颜色_G 用 源颜色 加 做 Sk颜色_G 用 目标颜色 乘 invA 除以 255。
                outB 为 做 Sk颜色_B 用 源颜色 加 做 Sk颜色_B 用 目标颜色 乘 invA 除以 255。
                做 内存_写32 用 目标行 加 x 乘 4、outA 左移 24 位或 outR 左移 16 位或 outG 左移 8 位或 outB。
            完。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
完。

Sk图像_绘制缩放到帧缓冲 接受 img、帧缓冲、步幅、帧宽、帧高、目标矩形：
    若 img 等于 0 或者 帧缓冲 等于 0：
        返还。
    完。
    目标宽 为 (目标矩形 的 右 减 目标矩形 的 左) 除以 100。
    目标高 为 (目标矩形 的 下 减 目标矩形 的 上) 除以 100。
    若 目标宽 小于或等于 0 或者 目标高 小于或等于 0：
        返还。
    完。
    缩放图 为 做 Sk图像_缩放 用 img、目标宽、目标高。
    若 缩放图 不等于 0：
        做 Sk图像_绘制到帧缓冲 用 缩放图、帧缓冲、步幅、帧宽、帧高、目标矩形 的 左 除以 100、目标矩形 的 上 除以 100。
        做 Sk图像_减引用 用 缩放图。
    完。
完。

COLORTYPE_RGBA8888 为 1。

COLORTYPE_RGB888 为 2。

COLORTYPE_GRAY8 为 3。

COLORTYPE_ALPHA8 为 4。

Sk位图 是 一种 事物，含 宽、高、颜色类型、像素、步幅、引用计数。

Sk位图_创建 接受 宽、高、颜色类型：
    若 宽 小于或等于 0 或者 高 小于或等于 0：
        返还 0。
    完。
    可变 每像素字节 为 4。
    若 颜色类型 等于 COLORTYPE_RGB888：
        每像素字节 为 3。
    完。
    若 颜色类型 等于 COLORTYPE_GRAY8：
        每像素字节 为 1。
    完。
    若 颜色类型 等于 COLORTYPE_ALPHA8：
        每像素字节 为 1。
    完。
    像素大小 为 宽 乘 高 乘 每像素字节。
    可变 bm 为 做 堆_分配 用 Sk位图。
    若 bm 等于 0：
        返还 0。
    完。
    bm 的 宽 为 宽。
    bm 的 高 为 高。
    bm 的 颜色类型 为 颜色类型。
    bm 的 步幅 为 宽 乘 每像素字节。
    bm 的 像素 为 做 平台_申请内存 用 像素大小。
    若 bm 的 像素 等于 0：
        做 堆_释放 用 bm。
        返还 0。
    完。
    做 内存_清零 用 bm 的 像素、像素大小。
    bm 的 引用计数 为 1。
    返还 bm。
完。

Sk位图_销毁 接受 bm：
    若 bm 等于 0：
        返还。
    完。
    若 bm 的 像素 不等于 0：
        每像素字节 为 4。
        若 bm 的 颜色类型 等于 COLORTYPE_GRAY8 或者 bm 的 颜色类型 等于 COLORTYPE_ALPHA8：
            每像素字节 为 1。
        完。
        若 bm 的 颜色类型 等于 COLORTYPE_RGB888：
            每像素字节 为 3。
        完。
        做 平台_释放内存 用 bm 的 像素、bm 的 宽 乘 bm 的 高 乘 每像素字节。
    完。
    做 堆_释放 用 bm。
完。

Sk图像_AI推荐缩放 接受 原宽、原高、显示宽、显示高：
    若 显示宽 小于或等于 0 或者 显示高 小于或等于 0：
        返还 1。
    完。
    若 原宽 小于或等于 显示宽 并且 原高 小于或等于 显示高：
        返还 1。
    完。
    x比 为 原宽 除以 显示宽。
    y比 为 原高 除以 显示高。
    可变 比 为 x比。
    若 y比 大于 比：
        比 为 y比。
    完。
    若 比 大于或等于 8：
        返还 8。
    完。
    若 比 大于或等于 4：
        返还 4。
    完。
    若 比 大于或等于 2：
        返还 2。
    完。
    返还 1。
完。

可变 统计_图像创建数 为 0。

可变 统计_图像销毁数 为 0。

可变 统计_解码次数 为 0。

可变 统计_缩放次数 为 0。

可变 统计_裁剪次数 为 0。

可变 统计_绘制次数 为 0。

Sk图像_打印统计 接受 零参：
    做 打印 用 「[图像] ═══════════ 图像系统统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  图像创建数: 」、做 转字符串 用 统计_图像创建数。
    做 打印 用 做 字符串拼接 用 「  图像销毁数: 」、做 转字符串 用 统计_图像销毁数。
    做 打印 用 做 字符串拼接 用 「  解码次数:   」、做 转字符串 用 统计_解码次数。
    做 打印 用 做 字符串拼接 用 「  缩放次数:   」、做 转字符串 用 统计_缩放次数。
    做 打印 用 做 字符串拼接 用 「  裁剪次数:   」、做 转字符串 用 统计_裁剪次数。
    做 打印 用 做 字符串拼接 用 「  绘制次数:   」、做 转字符串 用 统计_绘制次数。
    做 打印 用 做 字符串拼接 用 「  下一ID:     」、做 转字符串 用 Sk图像_下一ID。
    做 打印 用 「[图像] ═══════════════════════════════════════」。
完。

Sk图像_打印信息 接受 img：
    若 img 等于 0：
        做 打印 用 「[图像] 空图像」。
        返还。
    完。
    做 打印 用 「[图像] ── 图像信息 ──」。
    做 打印 用 做 字符串拼接 用 「  ID:       」、做 转字符串 用 img 的 唯一ID。
    做 打印 用 做 字符串拼接 用 「  尺寸:     」、做 字符串拼接 用 做 转字符串 用 img 的 宽、做 字符串拼接 用 「×」、做 转字符串 用 img 的 高。
    做 打印 用 做 字符串拼接 用 「  来源:     」、做 转字符串 用 img 的 来源。
    做 打印 用 做 字符串拼接 用 「  已解码:   」、做 转字符串 用 img 的 已解码。
    做 打印 用 做 字符串拼接 用 「  引用计数: 」、做 转字符串 用 img 的 引用计数。
    若 img 的 图像格式 大于 0：
        做 打印 用 做 字符串拼接 用 「  格式:     」、做 转字符串 用 img 的 图像格式。
    完。
完。

本章 收束。
