《函数创建》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

缓动_线性 为 0。

缓动_ease 为 1。

缓动_ease_in 为 2。

缓动_ease_out 为 3。

缓动_ease_in_out 为 4。

缓动_step_start 为 5。

缓动_step_end 为 6。

缓动_自定义贝塞尔 为 7。

缓动_自定义步进 为 8。

TimingFunction 是 一种 事物，含 类型、x1、y1、x2、y2、步数、步位置。

TimingFunction_创建 接受 类型：
    tf 为 做 堆_分配 用 TimingFunction。
    tf 的 类型 为 类型。
    若 类型 等于 缓动_ease：
        tf 的 x1 为 2500。
        tf 的 y1 为 1000。
        tf 的 x2 为 2500。
        tf 的 y2 为 10000。
    否则若 类型 等于 缓动_ease_in：
        tf 的 x1 为 4200。
        tf 的 y1 为 0。
        tf 的 x2 为 10000。
        tf 的 y2 为 10000。
    否则若 类型 等于 缓动_ease_out：
        tf 的 x1 为 0。
        tf 的 y1 为 0。
        tf 的 x2 为 5800。
        tf 的 y2 为 10000。
    否则若 类型 等于 缓动_ease_in_out：
        tf 的 x1 为 4200。
        tf 的 y1 为 0。
        tf 的 x2 为 5800。
        tf 的 y2 为 10000。
    完。
    tf 的 步数 为 1。
    tf 的 步位置 为 1。
    返还 tf。
完。

TimingFunction_求值 接受 tf、t：
    若 tf 的 类型 等于 缓动_线性：
        返还 t。
    完。
    若 tf 的 类型 等于 缓动_自定义步进 或者 tf 的 类型 等于 缓动_step_start 或者 tf 的 类型 等于 缓动_step_end：
        返还 做 TimingFunction_步进求值 用 tf、t。
    完。
    返还 做 Nova三次方Bezier_求值 用 tf 的 x1、tf 的 y1、tf 的 x2、tf 的 y2、t。
完。

Nova三次方Bezier_求值 接受 x1、y1、x2、y2、t：
    可变 参数t 为 t。
    可变 迭代 为 0。
    只要 迭代 小于 8：
        当前x 为 做 Nova三次方Bezier_x 用 x1、x2、参数t。
        误差 为 当前x 减 t。
        若 误差 大于 负 50 并且 误差 小于 50：
            跳出。
        完。
        导数 为 做 Nova三次方Bezier_dx 用 x1、x2、参数t。
        若 导数 等于 0：
            跳出。
        完。
        参数t 为 参数t 减 误差 乘 10000 除以 导数。
        若 参数t 小于 0：
            参数t 为 0。
        完。
        若 参数t 大于 10000：
            参数t 为 10000。
        完。
        迭代 为 迭代 加 1。
    完。
    返还 做 Nova三次方Bezier_y 用 y1、y2、参数t。
完。

Nova三次方Bezier_x 接受 x1、x2、t：
    t2 为 t 乘 t 除以 10000。
    t3 为 t2 乘 t 除以 10000。
    mt 为 10000 减 t。
    mt2 为 mt 乘 mt 除以 10000。
    返还 3 乘 mt2 乘 t 除以 10000 乘 x1 除以 10000 加 3 乘 mt 乘 t2 除以 10000 乘 x2 除以 10000 加 t3。
完。

Nova三次方Bezier_y 接受 y1、y2、t：
    t2 为 t 乘 t 除以 10000。
    t3 为 t2 乘 t 除以 10000。
    mt 为 10000 减 t。
    mt2 为 mt 乘 mt 除以 10000。
    返还 3 乘 mt2 乘 t 除以 10000 乘 y1 除以 10000 加 3 乘 mt 乘 t2 除以 10000 乘 y2 除以 10000 加 t3。
完。

Nova三次方Bezier_dx 接受 x1、x2、t：
    mt 为 10000 减 t。
    返还 3 乘 mt 乘 mt 除以 10000 乘 x1 除以 10000 加 6 乘 mt 乘 t 除以 10000 乘 (x2 减 x1) 除以 10000 加 3 乘 t 乘 t 除以 10000 乘 (10000 减 x2) 除以 10000。
完。

TimingFunction_步进求值 接受 tf、t：
    若 tf 的 步数 等于 0：
        返还 t。
    完。
    步大小 为 10000 除以 tf 的 步数。
    当前步 为 t 除以 步大小。
    若 tf 的 步位置 等于 0：
        返还 (当前步 加 1) 乘 步大小。
    完。
    返还 当前步 乘 步大小。
完。

本章 收束。
