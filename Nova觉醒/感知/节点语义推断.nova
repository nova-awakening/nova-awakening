《节点语义推断》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·内容可见性》。

语义_无 为 0。

语义_导航 为 1。

语义_主内容 为 2。

语义_侧边 为 3。

语义_表单 为 4。

语义_交互 为 5。

语义_媒体 为 6。

语义_广告 为 7。

最大分析 为 256。

衰减基数 为 1024。

节点语义 是 一种 事物，含 节点标识、语义类型、重要度、注意力权重、可见性、交互频率、最近分析。

可变 语义表 为 一组：（空）。

可变 语义数 为 0。

可变 分析次数 为 0。

可变 事件优化 为 0。

语义_初始化 接受 零参：
    语义表 为 一组：（空）。
    语义数 为 0。
完。

语义_分析节点 接受 节点标识、标签哈希、属性掩码：
    分析次数 为 分析次数 加 1。
    类型 为 做 语义_推断类型 用 标签哈希、属性掩码。
    重要度 为 做 语义_计算重要度 用 类型、属性掩码。
    索引 为 做 语义_查找 用 节点标识。
    若 索引 大于或等于 0：
        语义表 的 第 索引 项 的 语义类型 为 类型。
        语义表 的 第 索引 项 的 重要度 为 重要度。
        语义表 的 第 索引 项 的 最近分析 为 做 平台_获取高精度时间。
        返还 索引。
    完。
    若 语义数 大于或等于 最大分析：
        返还 负 1。
    完。
    语义 为 新建 节点语义。
    语义 的 节点标识 为 节点标识。
    语义 的 语义类型 为 类型。
    语义 的 重要度 为 重要度。
    语义 的 注意力权重 为 衰减基数 除以 2。
    语义 的 可见性 为 真。
    语义 的 交互频率 为 0。
    语义 的 最近分析 为 做 平台_获取高精度时间。
    做 追加 用 语义表、语义。
    新索引 为 语义数。
    语义数 为 语义数 加 1。
    返还 新索引。
完。

语义_推断类型 接受 标签哈希、属性掩码：
    若 属性掩码 位与 1 不等于 0：
        返还 语义_导航。
    完。
    若 属性掩码 位与 2 不等于 0：
        返还 语义_主内容。
    完。
    若 属性掩码 位与 4 不等于 0：
        返还 语义_表单。
    完。
    若 属性掩码 位与 8 不等于 0：
        返还 语义_交互。
    完。
    若 属性掩码 位与 16 不等于 0：
        返还 语义_媒体。
    完。
    若 属性掩码 位与 32 不等于 0：
        返还 语义_广告。
    完。
    返还 语义_无。
完。

语义_计算重要度 接受 类型、属性掩码：
    若 类型 等于 语义_主内容：
        返还 900。
    完。
    若 类型 等于 语义_交互：
        返还 800。
    完。
    若 类型 等于 语义_表单：
        返还 750。
    完。
    若 类型 等于 语义_导航：
        返还 600。
    完。
    若 类型 等于 语义_媒体：
        返还 500。
    完。
    若 类型 等于 语义_侧边：
        返还 300。
    完。
    若 类型 等于 语义_广告：
        返还 100。
    完。
    返还 400。
完。

语义_查找 接受 节点标识：
    可变 序号 为 0。
    只要 序号 小于 语义数：
        若 语义表 的 第 序号 项 的 节点标识 等于 节点标识：
            返还 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 负 1。
完。

语义_事件优先级 接受 事件类型、节点标识：
    索引 为 做 语义_查找 用 节点标识。
    若 索引 小于 0：
        返还 128。
    完。
    语义 为 语义表 的 第 索引 项。
    可变 优先级 为 128。
    若 语义 的 注意力权重 大于 衰减基数 乘 3 除以 4：
        优先级 为 255。
    否则若 语义 的 重要度 大于 700：
        优先级 为 200。
    否则若 语义 的 交互频率 大于 10：
        优先级 为 180。
    完。
    若 优先级 大于 128：
        事件优化 为 事件优化 加 1。
    完。
    返还 优先级。
完。

语义_更新注意力 接受 节点标识、注意力：
    索引 为 做 语义_查找 用 节点标识。
    若 索引 小于 0：
        返还。
    完。
    语义表 的 第 索引 项 的 注意力权重 为 (语义表 的 第 索引 项 的 注意力权重 乘 900 加 注意力 乘 124) 除以 衰减基数。
完。

语义_记录交互 接受 节点标识：
    索引 为 做 语义_查找 用 节点标识。
    若 索引 小于 0：
        返还。
    完。
    语义表 的 第 索引 项 的 交互频率 为 语义表 的 第 索引 项 的 交互频率 加 1。
完。

语义_设可见性 接受 节点标识、可见：
    索引 为 做 语义_查找 用 节点标识。
    若 索引 小于 0：
        返还。
    完。
    语义表 的 第 索引 项 的 可见性 为 可见。
完。

语义_获取重要度 接受 节点标识：
    索引 为 做 语义_查找 用 节点标识。
    若 索引 小于 0：
        返还 400。
    完。
    返还 语义表 的 第 索引 项 的 重要度。
完。

语义_获取类型名 接受 类型：
    若 类型 等于 语义_导航：
        返还 「导航」。
    完。
    若 类型 等于 语义_主内容：
        返还 「主内容」。
    完。
    若 类型 等于 语义_侧边：
        返还 「侧边」。
    完。
    若 类型 等于 语义_表单：
        返还 「表单」。
    完。
    若 类型 等于 语义_交互：
        返还 「交互」。
    完。
    若 类型 等于 语义_媒体：
        返还 「媒体」。
    完。
    若 类型 等于 语义_广告：
        返还 「广告」。
    完。
    返还 「未知」。
完。

语义_完整统计 接受 零参：
    返还 一组：语义数、分析次数、事件优化。
完。

本章 收束。
