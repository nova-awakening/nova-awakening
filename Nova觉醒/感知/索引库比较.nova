《索引库比较》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·自动扩容》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·重定位》。

引用《.·消息端》。

引用《..·中枢·辅助存根》。

引用《.·内核直连层》。

IDB_KEY_NUMBER 为 1。

IDB_KEY_DATE 为 2。

IDB_KEY_STRING 为 3。

IDB_KEY_BINARY 为 4。

IDB_KEY_ARRAY 为 5。

IDBKey 是 一种 事物，含 类型、数值、字符串、数组、数组长度。

IDBKeyRange 是 一种 事物，含 下界、上界、下界开放、上界开放。

可变 IDB_自增当前 为 0。

可变 IDB_查询统计_比较次数 为 0。

可变 IDB_查询统计_提取次数 为 0。

IDBKey_比较 接受 a、b：
    IDB_查询统计_比较次数 为 IDB_查询统计_比较次数 加 1。
    若 a 等于 0 并且 b 等于 0：
        返还 0。
    完。
    若 a 等于 0：
        返还 负 1。
    完。
    若 b 等于 0：
        返还 1。
    完。
    若 a 的 类型 不等于 b 的 类型：
        返还 a 的 类型 减 b 的 类型。
    完。
    做 匹配 用 a 的 类型。
完。

IDBKey_数字 接受 n：
    k 为 做 堆_分配 用 IDBKey。
    k 的 类型 为 IDB_KEY_NUMBER。
    k 的 数值 为 n。
    返还 k。
完。

IDBKey_字符串 接受 s：
    k 为 做 堆_分配 用 IDBKey。
    k 的 类型 为 IDB_KEY_STRING。
    k 的 字符串 为 做 字符串_复制 用 s。
    返还 k。
完。

IDBKey_日期 接受 时间戳：
    k 为 做 堆_分配 用 IDBKey。
    k 的 类型 为 IDB_KEY_DATE。
    k 的 数值 为 时间戳。
    返还 k。
完。

IDBKey_数组 接受 元素列表、长度：
    k 为 做 堆_分配 用 IDBKey。
    k 的 类型 为 IDB_KEY_ARRAY。
    k 的 数组 为 元素列表。
    k 的 数组长度 为 长度。
    返还 k。
完。

IDBKey_复制 接受 key：
    若 key 等于 0：
        返还 0。
    完。
    做 匹配 用 key 的 类型。
完。

IDBKey_有效 接受 key：
    若 key 等于 0：
        返还 假。
    完。
    若 key 的 类型 小于 IDB_KEY_NUMBER 或者 key 的 类型 大于 IDB_KEY_ARRAY：
        返还 假。
    完。
    若 key 的 类型 等于 IDB_KEY_STRING 并且 key 的 字符串 等于 0：
        返还 假。
    完。
    若 key 的 类型 等于 IDB_KEY_ARRAY：
        可变 i 为 0。
        只要 i 小于 key 的 数组长度：
            若 非 做 IDBKey_有效 用 key 的 数组 的 第 i 项：
                返还 假。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 真。
完。

IDBKeyRange_仅 接受 值：
    r 为 做 堆_分配 用 IDBKeyRange。
    r 的 下界 为 值。
    r 的 上界 为 值。
    r 的 下界开放 为 假。
    r 的 上界开放 为 假。
    返还 r。
完。

IDBKeyRange_下界 接受 下界、开放：
    r 为 做 堆_分配 用 IDBKeyRange。
    r 的 下界 为 下界。
    r 的 上界 为 0。
    r 的 下界开放 为 开放。
    r 的 上界开放 为 真。
    返还 r。
完。

IDBKeyRange_上界 接受 上界、开放：
    r 为 做 堆_分配 用 IDBKeyRange。
    r 的 下界 为 0。
    r 的 上界 为 上界。
    r 的 下界开放 为 真。
    r 的 上界开放 为 开放。
    返还 r。
完。

IDBKeyRange_范围 接受 下界、上界、下开放、上开放：
    r 为 做 堆_分配 用 IDBKeyRange。
    r 的 下界 为 下界。
    r 的 上界 为 上界。
    r 的 下界开放 为 下开放。
    r 的 上界开放 为 上开放。
    返还 r。
完。

IDBKeyRange_包含 接受 范围、key：
    若 范围 等于 0 或者 key 等于 0：
        返还 假。
    完。
    若 范围 的 下界 不等于 0：
        cmp 为 做 IDBKey_比较 用 key、范围 的 下界。
        若 范围 的 下界开放 并且 cmp 小于或等于 0：
            返还 假。
        完。
        若 非 范围 的 下界开放 并且 cmp 小于 0：
            返还 假。
        完。
    完。
    若 范围 的 上界 不等于 0：
        cmp 为 做 IDBKey_比较 用 key、范围 的 上界。
        若 范围 的 上界开放 并且 cmp 大于或等于 0：
            返还 假。
        完。
        若 非 范围 的 上界开放 并且 cmp 大于 0：
            返还 假。
        完。
    完。
    返还 真。
完。

IDB_提取键 接受 值、keyPath：
    IDB_查询统计_提取次数 为 IDB_查询统计_提取次数 加 1。
    若 值 等于 0 或者 keyPath 等于 0：
        返还 0。
    完。
    若 做 字符串查找 用 keyPath、「,」 大于或等于 0：
        路径列表 为 做 字符串_分割 用 keyPath、「,」。
        可变 键数组 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 路径列表：
            子键 为 做 IDB_提取单键 用 值、路径列表 的 第 i 项。
            若 子键 等于 0：
                返还 0。
            完。
            做 追加 用 键数组、子键。
            i 为 i 加 1。
        完。
        返还 做 IDBKey_数组 用 键数组、做 计数 用 键数组。
    完。
    返还 做 IDB_提取单键 用 值、keyPath。
完。

IDB_提取单键 接受 值、keyPath：
    obj 为 做 StructuredClone_反序列化 用 值、0。
    若 obj 等于 0：
        返还 0。
    完。
    返还 做 Nova对象_获取属性ByPath 用 0、obj、keyPath。
完。

IDB_自增键_下一个 接受 store：
    若 store 等于 0：
        返还 0。
    完。
    若 store 的 当前自增 等于 0：
        store 的 当前自增 为 1。
    完。
    key 为 做 IDBKey_数字 用 store 的 当前自增。
    store 的 当前自增 为 store 的 当前自增 加 1。
    返还 key。
完。

智能_IDB_建议索引 接受 store：
    若 store 等于 0：
        返还 0。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    可变 建议列表 为 一组：（空）。
    查询日志 为 store 的 查询日志。
    若 查询日志 等于 0：
        返还 建议列表。
    完。
    可变 字段频率 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 查询日志：
        路径 为 查询日志 的 第 i 项 的 keyPath。
        若 路径 不等于 0：
            当前 为 做 字典取 用 字段频率、路径。
            若 当前 等于 空：
                做 字典设 用 字段频率、路径、1。
            否则：
                做 字典设 用 字段频率、路径、当前 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    键列表 为 做 字典_键列表 用 字段频率。
    i 为 0。
    只要 i 小于 做 计数 用 键列表：
        路径 为 键列表 的 第 i 项。
        频率 为 做 字典取 用 字段频率、路径。
        若 频率 大于 5 并且 非 做 IDB_索引已存在 用 store、路径：
            做 追加 用 建议列表、一本字典：路径 对应 路径、频率 对应 频率。
        完。
        i 为 i 加 1。
    完。
    返还 建议列表。
完。

IDB_索引已存在 接受 store、keyPath：
    若 store 的 索引列表 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 store 的 索引列表：
        若 做 字符串_相等 用 store 的 索引列表 的 第 i 项 的 keyPath、keyPath：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

IDB_查询统计 接受 零参：
    返还 一本字典：比较次数 对应 IDB_查询统计_比较次数、提取次数 对应 IDB_查询统计_提取次数。
完。

IDB_Key_类型名 接受 类型：
    若 类型 等于 1：
        返还 「数字」。
    完。
    若 类型 等于 2：
        返还 「字符串」。
    完。
    若 类型 等于 3：
        返还 「日期」。
    完。
    若 类型 等于 4：
        返还 「数组」。
    完。
    返还 「未知」。
完。

IDB_Key_有效 接受 key：
    若 key 等于 空：
        返还 假。
    完。
    若 key 的 类型 小于 1 或者 key 的 类型 大于 4：
        返还 假。
    完。
    返还 真。
完。

IDB_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 IDB_查询统计_比较次数 大于 1000000：
        做 追加 用 问题、「IDB比较次数过多，考虑优化索引」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「IDB健康」。
    完。
    返还 问题。
完。

本章 收束。
