《解码器》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内容可见性》。

解码器_最大帧队列 为 32。

音频环形缓冲_默认大小 为 88200。

视频帧格式_YUV420 为 0。

视频帧格式_NV12 为 1。

视频帧格式_RGB 为 2。

视频帧格式_RGBA 为 3。

解码器统计 是 一种 事物，含 音频解码总帧数、视频解码总帧数、音频解码失败数、视频解码失败数、音频渲染帧数、视频渲染帧数、音频丢帧数、视频丢帧数、YUV转换次数、环形缓冲溢出数、环形缓冲下溢数、音频Decoder创建数、视频Decoder创建数、总解码耗时_纳秒、总渲染耗时_纳秒、AI解码调用次数、AI解码命中次数、错误总数。

AI解码状态 是 一种 事物，含 历史解码速率、历史丢帧率、历史缓冲水位、连续丢帧计数、连续成功计数、预测下帧成功、当前质量级别、采样计数器、学习权重。

可变 全局解码器统计 为 0。

可变 全局解码AI 为 0。

可变 全局解码器已初始化 为 假。

解码器_初始化全局 接受 零参：
    若 全局解码器已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局解码器统计 为 做 堆_分配_清零 用 144。
    若 全局解码器统计 等于 空：
        返还 错误_内存不足。
    完。
    全局解码AI 为 做 堆_分配_清零 用 48。
    若 全局解码AI 不等于 空：
        全局解码AI 的 预测下帧成功 为 真。
        全局解码AI 的 当前质量级别 为 1。
        全局解码AI 的 学习权重 为 128。
    完。
    全局解码器已初始化 为 真。
    做 打印 用 「[解码器] 初始化完成」。
    返还 0。
完。

解码器_销毁全局 接受 零参：
    若 全局解码器统计 不等于 空：
        做 堆_释放 用 全局解码器统计。
        全局解码器统计 为 0。
    完。
    若 全局解码AI 不等于 空：
        做 堆_释放 用 全局解码AI。
        全局解码AI 为 0。
    完。
    全局解码器已初始化 为 假。
    返还 0。
完。

解码器_重置统计 接受 零参：
    若 全局解码器统计 不等于 空：
        做 内存_置零 用 全局解码器统计、144。
    完。
    返还 0。
完。

音频Frame 是 一种 事物，含 采样、采样数、采样率、声道数、时间戳。

视频Frame 是 一种 事物，含 数据、宽度、高度、步幅、格式、时间戳。

音频Decoder 是 一种 事物，含 编解码器、已初始化、状态。

视频Decoder 是 一种 事物，含 编解码器、已初始化、宽度、高度、状态。

音频Decoder_创建 接受 codec：
    dec 为 做 堆_分配 用 音频Decoder。
    dec 的 编解码器 为 codec。
    dec 的 已初始化 为 真。
    dec 的 状态 为 0。
    返还 dec。
完。

音频Decoder_解码 接受 dec、packet：
    若 dec 等于 0 或者 packet 等于 0：
        返还 0。
    完。
    返还 0。
完。

音频Decoder_刷新 接受 dec：
    若 dec 等于 0：
        返还。
    完。
完。

视频Decoder_创建 接受 codec：
    dec 为 做 堆_分配 用 视频Decoder。
    dec 的 编解码器 为 codec。
    dec 的 已初始化 为 真。
    dec 的 宽度 为 0。
    dec 的 高度 为 0。
    dec 的 状态 为 0。
    返还 dec。
完。

视频Decoder_解码 接受 dec、packet：
    若 dec 等于 0 或者 packet 等于 0：
        返还 0。
    完。
    返还 0。
完。

视频Decoder_刷新 接受 dec：
    若 dec 等于 0：
        返还。
    完。
完。

音频渲染er 是 一种 事物，含 采样率、声道数、缓冲、缓冲大小、写入位置、读取位置、设备。

音频渲染er_创建 接受 采样率、声道数：
    r 为 做 堆_分配 用 音频渲染er。
    r 的 采样率 为 采样率。
    r 的 声道数 为 声道数。
    r 的 缓冲大小 为 采样率 乘 声道数 乘 2。
    r 的 缓冲 为 做 堆_分配 用 r 的 缓冲大小。
    r 的 写入位置 为 0。
    r 的 读取位置 为 0。
    r 的 设备 为 0。
    返还 r。
完。

音频渲染er_渲染 接受 r、frame、volume：
    若 r 等于 空 或者 frame 等于 空：
        返还。
    完。
    若 r 的 缓冲 等于 空 或者 r 的 缓冲大小 等于 0：
        返还。
    完。
    开始时间 为 做 平台_获取高精度时间。
    字节每样本 为 2。
    总字节 为 frame 的 采样数 乘 frame 的 声道数 乘 字节每样本。
    可变 i 为 0。
    只要 i 小于 frame 的 采样数 乘 frame 的 声道数：
        原样本 为 做 内存_读16 用 frame 的 采样 加 i 乘 2。
        调节样本 为 原样本 乘 volume 除以 1024。
        若 调节样本 大于 32767：
            调节样本 为 32767。
        完。
        若 调节样本 小于 负 32768：
            调节样本 为 负 32768。
        完。
        写位 为 (r 的 写入位置 加 i 乘 2) 取余于 r 的 缓冲大小。
        做 内存_写16 用 r 的 缓冲 加 写位、调节样本。
        i 为 i 加 1。
    完。
    r 的 写入位置 为 (r 的 写入位置 加 总字节) 取余于 r 的 缓冲大小。
    可变 可用空间 为 0。
    若 r 的 写入位置 大于或等于 r 的 读取位置：
        可用空间 为 r 的 缓冲大小 减 (r 的 写入位置 减 r 的 读取位置)。
    否则：
        可用空间 为 r 的 读取位置 减 r 的 写入位置。
    完。
    若 可用空间 小于 总字节 并且 全局解码器统计 不等于 空：
        全局解码器统计 的 环形缓冲溢出数 为 全局解码器统计 的 环形缓冲溢出数 加 1。
    完。
    若 全局解码器统计 不等于 空：
        全局解码器统计 的 音频渲染帧数 为 全局解码器统计 的 音频渲染帧数 加 1。
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        全局解码器统计 的 总渲染耗时_纳秒 为 全局解码器统计 的 总渲染耗时_纳秒 加 耗时。
    完。
完。

音频渲染er_读取 接受 r、输出、帧数：
    若 r 等于 空 或者 输出 等于 空 或者 帧数 小于或等于 0：
        返还 0。
    完。
    若 r 的 缓冲 等于 空：
        返还 0。
    完。
    字节每样本 为 2。
    需要字节 为 帧数 乘 r 的 声道数 乘 字节每样本。
    可变 可用 为 0。
    若 r 的 写入位置 大于或等于 r 的 读取位置：
        可用 为 r 的 写入位置 减 r 的 读取位置。
    否则：
        可用 为 r 的 缓冲大小 减 r 的 读取位置 加 r 的 写入位置。
    完。
    若 可用 小于 需要字节：
        若 全局解码器统计 不等于 空：
            全局解码器统计 的 环形缓冲下溢数 为 全局解码器统计 的 环形缓冲下溢数 加 1。
        完。
        需要字节 为 可用。
    完。
    可变 j 为 0。
    只要 j 小于 需要字节：
        读位 为 (r 的 读取位置 加 j) 取余于 r 的 缓冲大小。
        做 内存_写8 用 输出 加 j、做 内存_读8 用 r 的 缓冲 加 读位。
        j 为 j 加 1。
    完。
    r 的 读取位置 为 (r 的 读取位置 加 需要字节) 取余于 r 的 缓冲大小。
    返还 需要字节 除以 (r 的 声道数 乘 字节每样本)。
完。

音频渲染er_销毁 接受 r：
    若 r 等于 空：
        返还。
    完。
    若 r 的 缓冲 不等于 空：
        做 堆_释放 用 r 的 缓冲。
    完。
    做 堆_释放 用 r。
完。

视频渲染er 是 一种 事物，含 宽度、高度、当前帧、纹理ID、需要更新、RGB缓冲、RGB大小、锁。

视频渲染er_创建 接受 宽度、高度：
    r 为 做 堆_分配 用 视频渲染er。
    若 r 等于 空：
        返还 空。
    完。
    r 的 宽度 为 宽度。
    r 的 高度 为 高度。
    r 的 当前帧 为 0。
    r 的 纹理ID 为 0。
    r 的 需要更新 为 假。
    r 的 RGB大小 为 宽度 乘 高度 乘 4。
    r 的 RGB缓冲 为 做 堆_分配_清零 用 r 的 RGB大小。
    r 的 锁 为 做 渲染_互斥锁创建。
    若 全局解码器统计 不等于 空：
        全局解码器统计 的 视频Decoder创建数 为 全局解码器统计 的 视频Decoder创建数 加 1。
    完。
    返还 r。
完。

视频渲染er_渲染 接受 r、frame：
    若 r 等于 空 或者 frame 等于 空：
        返还。
    完。
    开始时间 为 做 平台_获取高精度时间。
    做 渲染_互斥锁获取 用 r 的 锁。
    r 的 当前帧 为 frame。
    r 的 需要更新 为 真。
    若 frame 的 格式 等于 视频帧格式_YUV420 并且 r 的 RGB缓冲 不等于 空：
        做 视频_YUV420转RGB 用 frame 的 数据、r 的 RGB缓冲、frame 的 宽度、frame 的 高度。
    完。
    做 渲染_互斥锁释放 用 r 的 锁。
    若 全局解码器统计 不等于 空：
        全局解码器统计 的 视频渲染帧数 为 全局解码器统计 的 视频渲染帧数 加 1。
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        全局解码器统计 的 总渲染耗时_纳秒 为 全局解码器统计 的 总渲染耗时_纳秒 加 耗时。
    完。
完。

视频渲染er_销毁 接受 r：
    若 r 等于 空：
        返还。
    完。
    若 r 的 RGB缓冲 不等于 空：
        做 堆_释放 用 r 的 RGB缓冲。
    完。
    做 堆_释放 用 r。
完。

视频_YUV420转RGB 接受 yuv数据、rgb输出、宽、高：
    若 yuv数据 等于 空 或者 rgb输出 等于 空 或者 宽 小于或等于 0 或者 高 小于或等于 0：
        返还。
    完。
    Y平面 为 yuv数据。
    U平面 为 yuv数据 加 宽 乘 高。
    V平面 为 U平面 加 宽 乘 高 除以 4。
    半宽 为 宽 除以 2。
    可变 y 为 0。
    只要 y 小于 高：
        可变 x 为 0。
        只要 x 小于 宽：
            Y值 为 做 内存_读8 用 Y平面 加 y 乘 宽 加 x。
            U值 为 做 内存_读8 用 U平面 加 y 除以 2 乘 半宽 加 x 除以 2。
            V值 为 做 内存_读8 用 V平面 加 y 除以 2 乘 半宽 加 x 除以 2。
            C 为 Y值 减 16。
            D 为 U值 减 128。
            E 为 V值 减 128。
            R 为 298 乘 C 加 409 乘 E 加 128 右移 8。
            G 为 298 乘 C 减 100 乘 D 减 208 乘 E 加 128 右移 8。
            B 为 298 乘 C 加 516 乘 D 加 128 右移 8。
            若 R 小于 0：
                R 为 0。
            完。
            若 R 大于 255：
                R 为 255。
            完。
            若 G 小于 0：
                G 为 0。
            完。
            若 G 大于 255：
                G 为 255。
            完。
            若 B 小于 0：
                B 为 0。
            完。
            若 B 大于 255：
                B 为 255。
            完。
            偏移 为 (y 乘 宽 加 x) 乘 4。
            做 内存_写8 用 rgb输出 加 偏移、R。
            做 内存_写8 用 rgb输出 加 偏移 加 1、G。
            做 内存_写8 用 rgb输出 加 偏移 加 2、B。
            做 内存_写8 用 rgb输出 加 偏移 加 3、255。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    若 全局解码器统计 不等于 空：
        全局解码器统计 的 YUV转换次数 为 全局解码器统计 的 YUV转换次数 加 1。
    完。
完。

AI解码_记录帧 接受 成功、解码耗时：
    若 全局解码AI 等于 空：
        返还。
    完。
    AI 为 全局解码AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    若 成功 等于 真：
        AI 的 连续成功计数 为 AI 的 连续成功计数 加 1。
        AI 的 连续丢帧计数 为 0。
    否则：
        AI 的 连续丢帧计数 为 AI 的 连续丢帧计数 加 1。
        AI 的 连续成功计数 为 0。
        AI 的 历史丢帧率 为 (AI 的 历史丢帧率 乘 15 加 1024) 除以 16。
    完。
    若 AI 的 采样计数器 小于 16：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 全局解码器统计 不等于 空：
        全局解码器统计 的 AI解码调用次数 为 全局解码器统计 的 AI解码调用次数 加 1。
    完。
    若 AI 的 连续丢帧计数 大于 5 或者 AI 的 历史丢帧率 大于 100：
        若 AI 的 当前质量级别 大于 0：
            AI 的 当前质量级别 为 AI 的 当前质量级别 减 1。
            做 打印 用 「[AI解码] 降低质量级别」。
        完。
    否则若 AI 的 连续成功计数 大于 30 并且 AI 的 历史丢帧率 小于 20：
        若 AI 的 当前质量级别 小于 2：
            AI 的 当前质量级别 为 AI 的 当前质量级别 加 1。
            做 打印 用 「[AI解码] 提升质量级别」。
        完。
    完。
    AI 的 预测下帧成功 为 真。
    若 AI 的 连续丢帧计数 大于 3：
        AI 的 预测下帧成功 为 假。
    完。
    若 AI 的 预测下帧成功 等于 真 并且 成功 等于 真：
        若 全局解码器统计 不等于 空：
            全局解码器统计 的 AI解码命中次数 为 全局解码器统计 的 AI解码命中次数 加 1。
        完。
    完。
完。

解码器_打印统计 接受 零参：
    若 全局解码器统计 等于 空：
        做 打印 用 「[解码器] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   解码器+渲染器 · 统计报告          │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 音频解码帧:       」、做 转字符串 用 全局解码器统计 的 音频解码总帧数。
    做 打印 用 做 字符串拼接 用 「│ 视频解码帧:       」、做 转字符串 用 全局解码器统计 的 视频解码总帧数。
    做 打印 用 做 字符串拼接 用 「│ 音频失败:         」、做 转字符串 用 全局解码器统计 的 音频解码失败数。
    做 打印 用 做 字符串拼接 用 「│ 视频失败:         」、做 转字符串 用 全局解码器统计 的 视频解码失败数。
    做 打印 用 做 字符串拼接 用 「│ 音频渲染:         」、做 转字符串 用 全局解码器统计 的 音频渲染帧数。
    做 打印 用 做 字符串拼接 用 「│ 视频渲染:         」、做 转字符串 用 全局解码器统计 的 视频渲染帧数。
    做 打印 用 做 字符串拼接 用 「│ YUV转换:          」、做 转字符串 用 全局解码器统计 的 YUV转换次数。
    做 打印 用 做 字符串拼接 用 「│ 环形溢出:         」、做 转字符串 用 全局解码器统计 的 环形缓冲溢出数。
    做 打印 用 做 字符串拼接 用 「│ 环形下溢:         」、做 转字符串 用 全局解码器统计 的 环形缓冲下溢数。
    做 打印 用 做 字符串拼接 用 「│ 解码耗时(μs):     」、做 转字符串 用 全局解码器统计 的 总解码耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 渲染耗时(μs):     」、做 转字符串 用 全局解码器统计 的 总渲染耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ AI解码调用:       」、做 转字符串 用 全局解码器统计 的 AI解码调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI解码命中:       」、做 转字符串 用 全局解码器统计 的 AI解码命中次数。
    做 打印 用 做 字符串拼接 用 「│ 错误:             」、做 转字符串 用 全局解码器统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

解码器_导出诊断 接受 零参：
    若 全局解码AI 等于 空：
        做 打印 用 「[解码AI] 未初始化」。
        返还。
    完。
    AI 为 全局解码AI。
    做 打印 用 「[解码AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史丢帧率(Q10): 」、做 转字符串 用 AI 的 历史丢帧率。
    做 打印 用 做 字符串拼接 用 「  连续丢帧: 」、做 转字符串 用 AI 的 连续丢帧计数。
    做 打印 用 做 字符串拼接 用 「  连续成功: 」、做 转字符串 用 AI 的 连续成功计数。
    做 打印 用 做 字符串拼接 用 「  当前质量: 」、做 转字符串 用 AI 的 当前质量级别。
    做 打印 用 做 字符串拼接 用 「  预测成功: 」、做 转字符串 用 AI 的 预测下帧成功。
完。

解码器_获取统计 接受 零参：
    返还 全局解码器统计。
完。

解码器_获取AI状态 接受 零参：
    返还 全局解码AI。
完。

解码器_获取质量级别 接受 零参：
    若 全局解码AI 等于 空：
        返还 1。
    完。
    返还 全局解码AI 的 当前质量级别。
完。

解码器_是否已初始化 接受 零参：
    返还 全局解码器已初始化。
完。

音频Frame_创建 接受 采样数、采样率、声道数：
    若 采样数 小于或等于 0 或者 采样率 小于或等于 0 或者 声道数 小于或等于 0：
        返还 空。
    完。
    frame 为 做 堆_分配 用 音频Frame。
    若 frame 等于 空：
        返还 空。
    完。
    frame 的 采样数 为 采样数。
    frame 的 采样率 为 采样率。
    frame 的 声道数 为 声道数。
    frame 的 时间戳 为 0。
    frame 的 采样 为 做 堆_分配_清零 用 采样数 乘 声道数 乘 2。
    若 frame 的 采样 等于 空：
        做 堆_释放 用 frame。
        返还 空。
    完。
    返还 frame。
完。

音频Frame_销毁 接受 frame：
    若 frame 等于 空：
        返还。
    完。
    若 frame 的 采样 不等于 空：
        做 堆_释放 用 frame 的 采样。
    完。
    做 堆_释放 用 frame。
完。

视频Frame_创建 接受 宽、高、格式：
    若 宽 小于或等于 0 或者 高 小于或等于 0：
        返还 空。
    完。
    frame 为 做 堆_分配 用 视频Frame。
    若 frame 等于 空：
        返还 空。
    完。
    frame 的 宽度 为 宽。
    frame 的 高度 为 高。
    frame 的 格式 为 格式。
    frame 的 时间戳 为 0。
    可变 大小 为 宽 乘 高 乘 4。
    若 格式 等于 视频帧格式_YUV420：
        大小 为 宽 乘 高 乘 3 除以 2。
    否则若 格式 等于 视频帧格式_NV12：
        大小 为 宽 乘 高 乘 3 除以 2。
    完。
    frame 的 步幅 为 宽 乘 4。
    若 格式 等于 视频帧格式_YUV420 或者 格式 等于 视频帧格式_NV12：
        frame 的 步幅 为 宽。
    完。
    frame 的 数据 为 做 堆_分配_清零 用 大小。
    若 frame 的 数据 等于 空：
        做 堆_释放 用 frame。
        返还 空。
    完。
    返还 frame。
完。

视频Frame_销毁 接受 frame：
    若 frame 等于 空：
        返还。
    完。
    若 frame 的 数据 不等于 空：
        做 堆_释放 用 frame 的 数据。
    完。
    做 堆_释放 用 frame。
完。

视频Frame_获取像素大小 接受 frame：
    若 frame 等于 空：
        返还 0。
    完。
    若 frame 的 格式 等于 视频帧格式_YUV420：
        返还 frame 的 宽度 乘 frame 的 高度 乘 3 除以 2。
    完。
    若 frame 的 格式 等于 视频帧格式_NV12：
        返还 frame 的 宽度 乘 frame 的 高度 乘 3 除以 2。
    完。
    返还 frame 的 宽度 乘 frame 的 高度 乘 4。
完。

音频Decoder_销毁 接受 dec：
    若 dec 等于 空：
        返还。
    完。
    做 堆_释放 用 dec。
完。

视频Decoder_销毁 接受 dec：
    若 dec 等于 空：
        返还。
    完。
    做 堆_释放 用 dec。
完。

本章 收束。
