《网接口信号裁》

引用《..·推理·通信渠道》。

可变 全局NWAI统计 为 新建 NWAI统计结构。

NWAI统计结构 是 一种 事物，含 剪贴板读取次数、剪贴板写入次数、信号创建次数、信号中断次数、字体查询次数、权限检查次数、AI推理次数。

信号监听最大 为 16。

AbortSignal 是 一种 事物，含 已中断、原因、监听器列表、监听器数。

AbortController 是 一种 事物，含 信号。

AbortController_创建 接受 零参：
    ctrl 为 做 堆_分配 用 AbortController。
    若 ctrl 等于 0：
        返还 空。
    完。
    sig 为 做 堆_分配 用 AbortSignal。
    若 sig 等于 0：
        做 堆_释放 用 ctrl。
        返还 空。
    完。
    sig 的 已中断 为 0。
    sig 的 原因 为 0。
    sig 的 监听器列表 为 做 堆_分配 用 信号监听最大 乘 8。
    sig 的 监听器数 为 0。
    ctrl 的 信号 为 sig。
    全局NWAI统计 的 信号创建次数 为 全局NWAI统计 的 信号创建次数 加 1。
    返还 ctrl。
完。

AbortController_abort 接受 ctrl、原因：
    若 ctrl 等于 空 或者 ctrl 的 信号 等于 空：
        返还。
    完。
    sig 为 ctrl 的 信号。
    若 sig 的 已中断 等于 1：
        返还。
    完。
    sig 的 已中断 为 1。
    sig 的 原因 为 原因。
    可变 i 为 0。
    只要 i 小于 sig 的 监听器数：
        cb 为 做 内存_读64 用 sig 的 监听器列表 加 i 乘 8。
        若 cb 不等于 0：
            做 回调_执行 用 cb、sig。
        完。
        i 为 i 加 1。
    完。
    全局NWAI统计 的 信号中断次数 为 全局NWAI统计 的 信号中断次数 加 1。
完。

AbortSignal_addEventListener 接受 sig、回调：
    若 sig 等于 空 或者 sig 的 监听器列表 等于 0：
        返还。
    完。
    若 sig 的 监听器数 大于或等于 信号监听最大：
        返还。
    完。
    若 sig 的 已中断 等于 1：
        做 回调_执行 用 回调、sig。
        返还。
    完。
    做 内存_写64 用 sig 的 监听器列表 加 sig 的 监听器数 乘 8、回调。
    sig 的 监听器数 为 sig 的 监听器数 加 1。
完。

AbortSignal_timeout 接受 ms：
    ctrl 为 做 AbortController_创建。
    若 ctrl 等于 空：
        返还 空。
    完。
    做 定时器_注册 用 ms、以 零参 为 引，其做法是：做 AbortController_abort 用 ctrl、「TimeoutError」。
    返还 ctrl 的 信号。
完。

AbortSignal_any 接受 信号列表、数量：
    ctrl 为 做 AbortController_创建。
    若 ctrl 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 数量：
        sig 为 做 内存_读64 用 信号列表 加 i 乘 8。
        若 sig 不等于 0：
            若 sig 的 已中断 等于 1：
                做 AbortController_abort 用 ctrl、sig 的 原因。
                返还 ctrl 的 信号。
            完。
            做 AbortSignal_addEventListener 用 sig、以 零参 为 引，其做法是：做 AbortController_abort 用 ctrl、sig 的 原因。
        完。
        i 为 i 加 1。
    完。
    返还 ctrl 的 信号。
完。

字体缓存最大 为 128。

FontData 是 一种 事物，含 家族名、全名、PostScript名、风格、权重、数据大小。

可变 字体缓存列表 为 空。

可变 字体缓存数量 为 0。

FontAccess_初始化 接受 零参：
    字体缓存列表 为 做 堆_分配 用 字体缓存最大 乘 8。
    若 字体缓存列表 不等于 0：
        做 内存_置零 用 字体缓存列表、字体缓存最大 乘 8。
    完。
    字体缓存数量 为 0。
完。

FontAccess_queryLocalFonts 接受 零参：
    若 字体缓存数量 大于 0：
        全局NWAI统计 的 字体查询次数 为 全局NWAI统计 的 字体查询次数 加 1。
        返还 字体缓存列表。
    完。
    系统字体数 为 做 平台_枚举字体 用 空、0。
    若 系统字体数 等于 0：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 系统字体数 并且 i 小于 字体缓存最大：
        fd 为 做 堆_分配 用 FontData。
        若 fd 不等于 0：
            做 平台_获取字体信息 用 i、fd。
            做 内存_写64 用 字体缓存列表 加 字体缓存数量 乘 8、fd。
            字体缓存数量 为 字体缓存数量 加 1。
        完。
        i 为 i 加 1。
    完。
    全局NWAI统计 的 字体查询次数 为 全局NWAI统计 的 字体查询次数 加 1。
    返还 字体缓存列表。
完。

FontAccess_查找字体 接受 家族名：
    若 字体缓存列表 等于 空 或者 字体缓存数量 等于 0：
        做 FontAccess_queryLocalFonts。
    完。
    可变 i 为 0。
    只要 i 小于 字体缓存数量：
        fd 为 做 内存_读64 用 字体缓存列表 加 i 乘 8。
        若 fd 不等于 0 并且 做 字符串_相等 用 fd 的 家族名、家族名：
            返还 fd。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

FontData_blob 接受 fd：
    若 fd 等于 空：
        返还 空。
    完。
    数据 为 做 平台_读取字体数据 用 fd 的 全名。
    返还 数据。
完。

权限_拒绝 为 0。

权限_提示 为 1。

权限_允许 为 2。

权限策略项 是 一种 事物，含 接口名、策略、过期时间。

权限策略最大 为 32。

可变 权限策略列表 为 空。

可变 权限策略数 为 0。

AI权限_初始化 接受 零参：
    权限策略列表 为 做 堆_分配 用 权限策略最大 乘 8。
    若 权限策略列表 不等于 0：
        做 内存_置零 用 权限策略列表、权限策略最大 乘 8。
    完。
    权限策略数 为 0。
完。

AI权限_检查 接受 接口名、页面上下文：
    全局NWAI统计 的 权限检查次数 为 全局NWAI统计 的 权限检查次数 加 1。
    可变 i 为 0。
    只要 i 小于 权限策略数：
        p 为 做 内存_读64 用 权限策略列表 加 i 乘 8。
        若 p 不等于 0 并且 做 字符串_相等 用 p 的 接口名、接口名：
            返还 p 的 策略。
        完。
        i 为 i 加 1。
    完。
    bus 为 全局AI总线。
    若 bus 不等于 0：
        feat 为 做 Tensor_创建 用 3。
        做 Tensor_写 用 feat、0、做 字符串_哈希 用 接口名。
        做 Tensor_写 用 feat、1、页面上下文。
        做 Tensor_写 用 feat、2、0。
        风险 为 做 AI总线_风险评分 用 bus、feat。
        做 Tensor_释放 用 feat。
        全局NWAI统计 的 AI推理次数 为 全局NWAI统计 的 AI推理次数 加 1。
        若 风险 小于 30：
            返还 权限_允许。
        完。
        若 风险 小于 70：
            返还 权限_提示。
        完。
        返还 权限_拒绝。
    完。
    返还 权限_提示。
完。

AI权限_缓存策略 接受 接口名、策略：
    若 权限策略列表 等于 空 或者 权限策略数 大于或等于 权限策略最大：
        返还。
    完。
    entry 为 做 堆_分配 用 权限策略项。
    若 entry 等于 0：
        返还。
    完。
    entry 的 接口名 为 接口名。
    entry 的 策略 为 策略。
    entry 的 过期时间 为 0。
    做 内存_写64 用 权限策略列表 加 权限策略数 乘 8、entry。
    权限策略数 为 权限策略数 加 1。
完。

电池状态 是 一种 事物，含 电量百分比、正在充电、降级等级。

可变 全局电池 为 新建 电池状态。

电池_更新 接受 零参：
    全局电池 的 电量百分比 为 做 平台_获取电量。
    全局电池 的 正在充电 为 做 平台_是否充电。
    若 全局电池 的 正在充电 等于 1：
        全局电池 的 降级等级 为 0。
    否则若 全局电池 的 电量百分比 小于或等于 10：
        全局电池 的 降级等级 为 3。
    否则若 全局电池 的 电量百分比 小于或等于 25：
        全局电池 的 降级等级 为 2。
    否则若 全局电池 的 电量百分比 小于或等于 50：
        全局电池 的 降级等级 为 1。
    否则：
        全局电池 的 降级等级 为 0。
    完。
完。

电池_应否降级 接受 接口名：
    若 全局电池 的 降级等级 等于 0：
        返还 假。
    完。
    若 全局电池 的 降级等级 等于 3：
        返还 真。
    完。
    若 全局电池 的 降级等级 大于或等于 2：
        若 做 字符串_包含 用 接口名、「动画」 或者 做 字符串_包含 用 接口名、「视频」：
            返还 真。
        完。
    完。
    返还 假。
完。

API跟踪最大 为 64。

API调用记录 是 一种 事物，含 接口名哈希、调用次数、最后调用时间。

可变 API跟踪列表 为 空。

可变 API跟踪数 为 0。

API跟踪_初始化 接受 零参：
    API跟踪列表 为 做 堆_分配 用 API跟踪最大 乘 8。
    若 API跟踪列表 不等于 0：
        做 内存_置零 用 API跟踪列表、API跟踪最大 乘 8。
    完。
    API跟踪数 为 0。
完。

API跟踪_记录 接受 接口名：
    若 API跟踪列表 等于 空：
        返还。
    完。
    哈希 为 做 字符串_哈希 用 接口名。
    可变 i 为 0。
    只要 i 小于 API跟踪数：
        rec 为 做 内存_读64 用 API跟踪列表 加 i 乘 8。
        若 rec 不等于 0 并且 rec 的 接口名哈希 等于 哈希：
            rec 的 调用次数 为 rec 的 调用次数 加 1。
            rec 的 最后调用时间 为 做 读取毫秒。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 API跟踪数 大于或等于 API跟踪最大：
        返还。
    完。
    rec 为 做 堆_分配 用 API调用记录。
    若 rec 等于 0：
        返还。
    完。
    rec 的 接口名哈希 为 哈希。
    rec 的 调用次数 为 1。
    rec 的 最后调用时间 为 做 读取毫秒。
    做 内存_写64 用 API跟踪列表 加 API跟踪数 乘 8、rec。
    API跟踪数 为 API跟踪数 加 1。
完。

API跟踪_检测滥用 接受 阈值：
    若 API跟踪列表 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 API跟踪数：
        rec 为 做 内存_读64 用 API跟踪列表 加 i 乘 8。
        若 rec 不等于 0 并且 rec 的 调用次数 大于 阈值：
            做 内核打印 用 「[NWAI] API滥用警告: 哈希={} 次数={}」、rec 的 接口名哈希、rec 的 调用次数。
            返还 rec。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

NWAI_初始化 接受 零参：
    做 内核打印 用 「[NWAI] Nova Web API智能 启动」。
    做 FontAccess_初始化。
    做 AI权限_初始化。
    做 API跟踪_初始化。
    全局NWAI统计 的 剪贴板读取次数 为 0。
    全局NWAI统计 的 剪贴板写入次数 为 0。
    全局NWAI统计 的 信号创建次数 为 0。
    全局NWAI统计 的 信号中断次数 为 0。
    全局NWAI统计 的 字体查询次数 为 0。
    全局NWAI统计 的 权限检查次数 为 0。
    全局NWAI统计 的 AI推理次数 为 0。
    全局电池 为 新建 电池状态。
    做 内核打印 用 「[NWAI] 初始化完成 — 剪贴板/信号/字体/权限/电池/跟踪 就绪」。
完。

NWAI_打印诊断 接受 零参：
    做 内核打印 用 「╔════════════════════════════════════════════╗」。
    做 内核打印 用 「║  NWAI Web API智能 诊断报告                ║」。
    做 内核打印 用 「╠════════════════════════════════════════════╣」。
    做 内核打印 用 「║ 剪贴板读取:   {}」、全局NWAI统计 的 剪贴板读取次数。
    做 内核打印 用 「║ 剪贴板写入:   {}」、全局NWAI统计 的 剪贴板写入次数。
    做 内核打印 用 「║ 信号创建:     {}」、全局NWAI统计 的 信号创建次数。
    做 内核打印 用 「║ 信号中断:     {}」、全局NWAI统计 的 信号中断次数。
    做 内核打印 用 「║ 字体查询:     {}」、全局NWAI统计 的 字体查询次数。
    做 内核打印 用 「║ 权限检查:     {}」、全局NWAI统计 的 权限检查次数。
    做 内核打印 用 「║ AI推理:       {}」、全局NWAI统计 的 AI推理次数。
    做 内核打印 用 「║ 电池电量:     {}%」、全局电池 的 电量百分比。
    做 内核打印 用 「║ 降级等级:     {}」、全局电池 的 降级等级。
    做 内核打印 用 「║ 可见性:       {}」、全局可见状态。
    做 内核打印 用 「╚════════════════════════════════════════════╝」。
完。

可见_可见 为 0。

可见_隐藏 为 1。

可见_预渲染 为 2。

可变 全局可见状态 为 可见_可见。

可见性_变更 接受 新状态：
    旧状态 为 全局可见状态。
    全局可见状态 为 新状态。
    若 新状态 等于 可见_隐藏 并且 旧状态 等于 可见_可见：
        做 内核打印 用 「[NWAI] 页面进入隐藏状态 — 暂停非关键任务」。
    否则若 新状态 等于 可见_可见 并且 旧状态 等于 可见_隐藏：
        做 内核打印 用 「[NWAI] 页面恢复可见 — 恢复任务」。
    完。
完。

可见性_应否暂停 接受 零参：
    返还 全局可见状态 等于 可见_隐藏。
完。

引用《.·内核直连层》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·字符串替换》。

引用《.·权限接口权限》。

引用《..·中枢·整数转字符串》。

引用《.·原子驻留》。

引用《..·中枢·时钟中枢》。

ClipboardItem 是 一种 事物，含 类型列表、数据列表、类型数量。

Clipboard 是 一种 事物，含 读权限、写权限。

Clipboard_readText 接受 Nova运行时实例：
    文本 为 做 平台_剪贴板_读取文本。
    返还 文本。
完。

Clipboard_writeText 接受 Nova运行时实例、文本：
    若 文本 等于 0：
        返还。
    完。
    做 平台_剪贴板_写入文本 用 文本。
完。

Clipboard_read 接受 Nova运行时实例：
    items 为 做 堆_分配_数组 用 8 乘 8。
    可变 数量 为 0。
    文本 为 做 平台_剪贴板_读取文本。
    若 文本 不等于 0：
        item 为 做 堆_分配 用 ClipboardItem。
        item 的 类型数量 为 1。
        item 的 类型列表 为 做 堆_分配_数组 用 8。
        做 内存_写64 用 item 的 类型列表、「text/plain」。
        item 的 数据列表 为 做 堆_分配_数组 用 8。
        做 内存_写64 用 item 的 数据列表、文本。
        做 内存_写64 用 items 加 数量 乘 8、item。
        数量 为 数量 加 1。
    完。
    图片 为 做 平台_剪贴板_读取图片。
    若 图片 不等于 0：
        item 为 做 堆_分配 用 ClipboardItem。
        item 的 类型数量 为 1。
        item 的 类型列表 为 做 堆_分配_数组 用 8。
        做 内存_写64 用 item 的 类型列表、「image/png」。
        item 的 数据列表 为 做 堆_分配_数组 用 8。
        做 内存_写64 用 item 的 数据列表、图片。
        做 内存_写64 用 items 加 数量 乘 8、item。
        数量 为 数量 加 1。
    完。
    返还 一组：items、数量。
完。

Clipboard_write 接受 Nova运行时实例、items、数量：
    若 items 等于 0 或者 数量 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 数量：
        item 为 做 内存_读64 用 items 加 i 乘 8。
        若 item 不等于 0 并且 item 的 类型数量 大于 0：
            类型 为 做 内存_读64 用 item 的 类型列表。
            数据 为 做 内存_读64 用 item 的 数据列表。
            若 做 字符串_相等 用 类型、「text/plain」：
                做 平台_剪贴板_写入文本 用 数据。
            完。
        完。
        i 为 i 加 1。
    完。
完。

本章 收束。
