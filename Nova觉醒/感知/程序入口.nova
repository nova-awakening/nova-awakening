《程序入口》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·推理·AI总线初始化》。

引用《.·渲染管线计》。

引用《..·推理·AI总线资源调度》。

引用《.·文档事件》。

引用《..·执行·内核分配页》。

引用《.·文档节点》。

引用《.·更新活跃样》。

引用《..·中枢·内容可见性》。

引用《.·字形缓存与显示列表》。

引用《..·执行·窗口创建》。

引用《.·核心布局算》。

引用《.·渲染总线驱动》。

main 接受 零参：
    做 分配器_初始化。
    做 元素_初始化原子表。
    视口宽初始 为 1920 乘 100。
    视口高初始 为 1080 乘 100。
    做 AI总线_初始化_视口 用 视口宽初始、视口高初始。
    browser 为 做 Browser_创建。
    做 Browser_导航 用 browser、「about:blank」。
    做 Browser_主循环 用 browser。
    做 PlatformWindow_销毁 用 browser 的 平台窗口。
完。

AI浏览器_智能渲染 接受 tab：
    做 渲染管线_AI驱动更新 用 tab。
完。

本章 收束。
