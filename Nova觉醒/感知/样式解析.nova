《样式解析》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《.·CSS单位与颜色解析》。

引用《.·原子驻留》。

引用《.·比较数值》。

引用《.·CSS_RGB_HSL解析》。

引用《.·环境上下文》。

引用《..·中枢·字符串替换》。

引用《.·CSS属性字符判定》。

引用《.·AI样式推断》。

引用《..·执行·内存管理封装》。

引用《..·中枢·整数转字符串》。

引用《.·AI样式缓存》。

引用《..·中枢·内存初始化》。

引用《..·中枢·重定位》。

引用《.·HTML令牌处理》。

引用《.·先扫描找符号》。

引用《.·样式媒体媒体》。

引用《.·CSS值解析》。

引用《.·沙箱创建与过滤》。

引用《..·执行·底层系统调用原语》。

引用《..·中枢·辅助存根》。

引用《.·子进程主循》。

引用《..·执行·控制台输出》。

引用《..·推理·AI总线资源调度》。

引用《.·内核直连层》。

引用《.·解析快照》。

引用《.·样式删除属性》。

引用《.·优化提示》。

引用《.·CSS令牌化》。

引用《..·推理·AI鼠标事件处理》。

引用《.·CSS声明解析》。

引用《.·CSS标识符函数解析》。

引用《..·中枢·内核打印》。

引用《..·执行·底层内存操》。

引用《.·样式解析值》。

样式档 是 一种 事物，含 名称、来源、文本、令牌器、媒体值、最近查询、最近容器、最近声明、最近属性、最近意图、最近影响、最近结果、解析数、声明数、值数、颜色数、媒查数、容器数、命中数、漂移数、健康。

样式节点 是 一种 事物，含 属性名、属性值、属性ID、意图、影响、命中缓存。

样式摘要 是 一种 事物，含 名称、来源、解析数、声明数、值数、颜色数、媒查数、容器数、命中数、健康、暗色、减动。

可变 样式账本 为 一组：（空）。

可变 样式建数 为 0。

可变 样式总解析 为 0。

可变 样式总声明 为 0。

可变 样式总颜色 为 0。

可变 样式总媒查 为 0。

可变 样式总容器 为 0。

可变 样式总命中 为 0。

样式解析_布串 接受 值：
    若 值：
        返还 「true」。
    完。
    返还 「false」。
完。

样式解析_取数 接受 值：
    若 值 等于 空：
        返还 0。
    完。
    返还 值。
完。

样式解析_建档 接受 名称、来源：
    档 为 做 堆_分配 用 样式档。
    档 的 名称 为 名称。
    档 的 来源 为 来源。
    档 的 文本 为 「」。
    档 的 令牌器 为 0。
    档 的 媒体值 为 0。
    档 的 最近查询 为 「」。
    档 的 最近容器 为 0。
    档 的 最近声明 为 0。
    档 的 最近属性 为 「」。
    档 的 最近意图 为 「」。
    档 的 最近影响 为 「」。
    档 的 最近结果 为 0。
    档 的 解析数 为 0。
    档 的 声明数 为 0。
    档 的 值数 为 0。
    档 的 颜色数 为 0。
    档 的 媒查数 为 0。
    档 的 容器数 为 0。
    档 的 命中数 为 0。
    档 的 漂移数 为 0。
    档 的 健康 为 「未初始化」。
    返还 档。
完。

样式解析_登记 接受 档：
    做 追加 用 样式账本、档。
    样式建数 为 样式建数 加 1。
    返还 档。
完。

样式解析_创建 接受 名称、来源：
    档 为 做 样式解析_建档 用 名称、来源。
    做 样式解析_初始化 用 档。
    返还 做 样式解析_登记 用 档。
完。

样式解析_初始化 接受 档：
    做 AI样式_初始化缓存。
    档 的 媒体值 为 做 媒体Values_获取。
    档 的 健康 为 「运行」。
    返还 档。
完。

样式解析_重置 接受 档：
    档 的 文本 为 「」。
    档 的 令牌器 为 0。
    档 的 最近查询 为 「」。
    档 的 最近容器 为 0。
    档 的 最近声明 为 0。
    档 的 最近属性 为 「」。
    档 的 最近意图 为 「」。
    档 的 最近影响 为 「」。
    档 的 最近结果 为 0。
    档 的 解析数 为 0。
    档 的 声明数 为 0。
    档 的 值数 为 0。
    档 的 颜色数 为 0。
    档 的 媒查数 为 0。
    档 的 容器数 为 0。
    档 的 命中数 为 0。
    档 的 漂移数 为 0。
    档 的 健康 为 「运行」。
    做 AI样式_清空缓存。
    返还 档。
完。

样式解析_加载文本 接受 档、来源、文本：
    档 的 来源 为 来源。
    档 的 文本 为 文本。
    档 的 令牌器 为 做 CSS令牌izer_创建 用 文本。
    档 的 解析数 为 档 的 解析数 加 1。
    样式总解析 为 样式总解析 加 1。
    返还 档 的 令牌器。
完。

样式解析_载入样表 接受 档、来源、文本：
    令牌器 为 做 样式解析_加载文本 用 档、来源、文本。
    档 的 最近结果 为 令牌器。
    返还 令牌器。
完。

样式解析_解析值 接受 档、文本、位置：
    长度 为 做 字符串_长度 用 文本。
    结果 为 做 CSS解析_值 用 文本、长度、位置。
    档 的 值数 为 档 的 值数 加 1。
    样式总解析 为 样式总解析 加 1。
    档 的 最近结果 为 结果。
    返还 结果。
完。

样式解析_解析声明 接受 档、令牌器、属性令牌：
    声明 为 做 CSSParser_解析声明 用 令牌器、属性令牌。
    档 的 声明数 为 档 的 声明数 加 1。
    样式总声明 为 样式总声明 加 1。
    档 的 最近声明 为 声明。
    返还 声明。
完。

样式解析_解析属性ID 接受 属性名：
    返还 做 CSSParser_属性名转ID 用 属性名、做 字符串_长度 用 属性名。
完。

样式解析_声明节点 接受 属性名、属性值：
    节点 为 做 堆_分配 用 样式节点。
    节点 的 属性名 为 属性名。
    节点 的 属性值 为 属性值。
    节点 的 属性ID 为 做 样式解析_解析属性ID 用 属性名。
    节点 的 意图 为 做 AI样式_推断意图 用 节点 的 属性ID、属性值。
    节点 的 影响 为 做 AI样式_分析影响 用 节点 的 属性ID。
    节点 的 命中缓存 为 假。
    返还 节点。
完。

样式解析_解析属性 接受 档、属性名、属性值：
    节点 为 做 样式解析_声明节点 用 属性名、属性值。
    档 的 声明数 为 档 的 声明数 加 1。
    档 的 最近属性 为 属性名。
    档 的 最近意图 为 节点 的 意图。
    档 的 最近影响 为 节点 的 影响。
    若 做 字符串_相等 用 属性名、「transform」：
        档 的 最近结果 为 做 Transform_解析 用 属性值。
    否则若 做 字符串_相等 用 属性名、「scroll-snap-type」：
        档 的 最近结果 为 做 ScrollSnap_解析类型 用 属性值。
    否则若 做 字符串_相等 用 属性名、「scroll-snap-align」：
        档 的 最近结果 为 做 ScrollSnap_解析对齐 用 属性值。
    否则若 做 字符串_相等 用 属性名、「will-change」：
        档 的 最近结果 为 做 NWCO_解析will_change 用 属性值。
    否则：
        档 的 最近结果 为 节点。
    完。
    样式总声明 为 样式总声明 加 1。
    返还 节点。
完。

样式解析_解析颜色函数 接受 档、文本：
    长度 为 做 字符串_长度 用 文本。
    结果 为 做 CSS解析_rgb函数 用 文本、长度、0。
    若 结果 等于 0：
        结果 为 做 CSS解析_hsl函数 用 文本、长度、0。
    完。
    档 的 颜色数 为 档 的 颜色数 加 1。
    样式总颜色 为 样式总颜色 加 1。
    档 的 最近结果 为 结果。
    返还 结果。
完。

样式解析_解析颜色值 接受 档、文本：
    长度 为 做 字符串_长度 用 文本。
    结果 为 做 CSS解析_十六进制颜色 用 文本、长度、0。
    若 结果 等于 0：
        结果 为 做 CSS_解析颜色十六进制 用 文本、0。
    完。
    档 的 颜色数 为 档 的 颜色数 加 1。
    样式总颜色 为 样式总颜色 加 1。
    档 的 最近结果 为 结果。
    返还 结果。
完。

样式解析_解析命名颜色 接受 档、名称：
    长度 为 做 字符串_长度 用 名称。
    结果 为 做 CSS解析_命名颜色 用 名称、长度。
    若 结果 等于 0：
        结果 为 做 CSSParser_命名颜色 用 名称、长度。
    完。
    若 结果 等于 0：
        结果 为 做 CSS_命名颜色 用 名称。
    完。
    档 的 颜色数 为 档 的 颜色数 加 1。
    样式总颜色 为 样式总颜色 加 1。
    档 的 最近结果 为 结果。
    返还 结果。
完。

样式解析_解析标识 接受 档、文本：
    长度 为 做 字符串_长度 用 文本。
    结果 为 做 CSS解析_标识符或函数 用 文本、长度、0。
    档 的 值数 为 档 的 值数 加 1。
    档 的 最近结果 为 结果。
    返还 结果。
完。

样式解析_解析单位 接受 档、单位名：
    长度 为 做 字符串_长度 用 单位名。
    结果 为 做 CSSParser_解析单位 用 单位名、长度。
    若 结果 等于 0：
        结果 为 做 CSS_解析单位 用 单位名。
    完。
    档 的 最近结果 为 结果。
    返还 结果。
完。

样式解析_关键字 接受 档、名称：
    长度 为 做 字符串_长度 用 名称。
    结果 为 做 CSSParser_关键字转ID 用 名称、长度。
    档 的 最近结果 为 结果。
    返还 结果。
完。

样式解析_写缓存 接受 档、规则哈希、属性名、解析值：
    属性ID 为 做 样式解析_解析属性ID 用 属性名。
    做 AI样式_写缓存 用 规则哈希、属性ID、解析值。
    档 的 最近属性 为 属性名。
    返还 解析值。
完。

样式解析_查缓存 接受 档、规则哈希、属性名：
    属性ID 为 做 样式解析_解析属性ID 用 属性名。
    命中 为 做 AI样式_查缓存 用 规则哈希、属性ID。
    若 命中 不等于 0 并且 命中 不等于 空：
        档 的 命中数 为 档 的 命中数 加 1。
        样式总命中 为 样式总命中 加 1。
    完。
    档 的 最近属性 为 属性名。
    档 的 最近结果 为 命中。
    返还 命中。
完。

样式解析_失效规则 接受 规则哈希：
    做 AI样式_无效化_规则 用 规则哈希。
完。

样式解析_失效属性 接受 属性名：
    属性ID 为 做 样式解析_解析属性ID 用 属性名。
    做 AI样式_无效化_属性 用 属性ID。
完。

样式解析_缓存统计 接受 档：
    统计 为 做 AI样式_缓存统计。
    档 的 最近结果 为 统计。
    返还 统计。
完。

样式解析_缓存命中率 接受 档：
    命中率 为 做 AI样式_缓存命中率。
    档 的 最近结果 为 命中率。
    返还 命中率。
完。

样式解析_预热缓存 接受 档、常用规则列表：
    做 AI样式_缓存预热 用 常用规则列表。
    档 的 最近结果 为 常用规则列表。
完。

样式解析_推断意图 接受 档、属性名、值字符串：
    属性ID 为 做 样式解析_解析属性ID 用 属性名。
    意图 为 做 AI样式_推断意图 用 属性ID、值字符串。
    档 的 最近属性 为 属性名。
    档 的 最近意图 为 意图。
    返还 意图。
完。

样式解析_分析影响 接受 档、属性名：
    属性ID 为 做 样式解析_解析属性ID 用 属性名。
    影响 为 做 AI样式_分析影响 用 属性ID。
    档 的 最近属性 为 属性名。
    档 的 最近影响 为 影响。
    返还 影响。
完。

样式解析_处理变化 接受 档、元素、属性名、新值：
    属性ID 为 做 样式解析_解析属性ID 用 属性名。
    做 AI样式_处理变化 用 元素、属性ID、新值。
    档 的 最近属性 为 属性名。
    档 的 最近结果 为 新值。
完。

样式解析_解析媒体 接受 档、查询字符串：
    结果 为 做 媒体查询_解析 用 查询字符串。
    档 的 最近查询 为 查询字符串。
    档 的 媒查数 为 档 的 媒查数 加 1。
    样式总媒查 为 样式总媒查 加 1。
    档 的 最近结果 为 结果。
    返还 结果。
完。

样式解析_匹配媒体 接受 档、查询字符串：
    命中 为 做 Window_match媒体 用 查询字符串。
    档 的 最近查询 为 查询字符串。
    档 的 媒查数 为 档 的 媒查数 加 1。
    样式总媒查 为 样式总媒查 加 1。
    档 的 最近结果 为 命中。
    返还 命中。
完。

样式解析_更新视口 接受 档、宽、高：
    做 媒体Values_从视口更新 用 宽、高。
    做 媒体查询_更新视口 用 宽、高。
    档 的 媒体值 为 做 媒体Values_获取。
    档 的 最近结果 为 档 的 媒体值。
    返还 档 的 媒体值。
完。

样式解析_设置配色 接受 档、暗色模式：
    做 媒体Values_设置配色方案 用 暗色模式。
    档 的 媒体值 为 做 媒体Values_获取。
    档 的 最近结果 为 档 的 媒体值。
    返还 档 的 媒体值。
完。

样式解析_设置动效 接受 档、减弱：
    做 媒体Values_设置动效偏好 用 减弱。
    档 的 媒体值 为 做 媒体Values_获取。
    档 的 最近结果 为 档 的 媒体值。
    返还 档 的 媒体值。
完。

样式解析_注册容器 接受 档、元素、名称、类型：
    结果 为 做 ContainerQuery_注册容器 用 元素、名称、类型。
    档 的 最近容器 为 结果。
    档 的 容器数 为 档 的 容器数 加 1。
    样式总容器 为 样式总容器 加 1。
    返还 结果。
完。

样式解析_更新容器 接受 档、元素、内联、块：
    做 ContainerQuery_更新尺寸 用 元素、内联、块。
    档 的 容器数 为 档 的 容器数 加 1。
    样式总容器 为 样式总容器 加 1。
    档 的 最近结果 为 元素。
    返还 元素。
完。

样式解析_求值容器 接受 档、查询、元素：
    结果 为 做 ContainerQuery_求值 用 查询、元素。
    档 的 最近容器 为 查询。
    档 的 最近结果 为 结果。
    返还 结果。
完。

样式解析_媒体摘要 接受 档：
    值 为 做 媒体Values_获取。
    返还 一组：做 媒体环境_获取视口宽 用 值、做 媒体环境_获取视口高 用 值、做 样式解析_布串 用 做 媒体环境_是暗色模式 用 值、做 媒体环境_设备像素比 用 值。
完。

样式解析_统一解析 接受 档、属性名、属性值、查询字符串：
    节点 为 做 样式解析_解析属性 用 档、属性名、属性值。
    解析值 为 做 样式解析_解析值 用 档、属性值、0。
    命中媒体 为 做 样式解析_匹配媒体 用 档、查询字符串。
    节点 的 命中缓存 为 假。
    若 做 样式解析_查缓存 用 档、属性名、属性名 不等于 0：
        节点 的 命中缓存 为 真。
    否则：
        做 样式解析_写缓存 用 档、属性名、属性名、解析值。
    完。
    档 的 最近结果 为 一组：节点、解析值、命中媒体。
    返还 档 的 最近结果。
完。

样式解析_摘要 接受 档：
    值 为 做 媒体Values_获取。
    返还 新建 样式摘要：名称 为 档 的 名称、来源 为 档 的 来源、解析数 为 档 的 解析数、声明数 为 档 的 声明数、值数 为 档 的 值数、颜色数 为 档 的 颜色数、媒查数 为 档 的 媒查数、容器数 为 档 的 容器数、命中数 为 档 的 命中数、健康 为 档 的 健康、暗色 为 做 媒体环境_是暗色模式 用 值、减动 为 0。
完。

样式解析_状态串 接受 档：
    摘 为 做 样式解析_摘要 用 档。
    返还 做 字符串拼接 用 「样式[」、做 字符串拼接 用 摘 的 名称、做 字符串拼接 用 「] 声明=」、做 字符串拼接 用 做 整数转字符串 用 摘 的 声明数、做 字符串拼接 用 「 媒体=」、做 字符串拼接 用 做 整数转字符串 用 摘 的 媒查数、做 字符串拼接 用 「 容器=」、做 整数转字符串 用 摘 的 容器数。
完。

样式解析_健康 接受 档：
    若 档 等于 0 或者 档 等于 空：
        返还 「缺失」。
    完。
    若 档 的 令牌器 等于 0：
        返还 「待装载」。
    完。
    若 档 的 漂移数 大于 0：
        返还 「漂移」。
    完。
    若 做 样式解析_缓存命中率 用 档 小于 0：
        返还 「异常」。
    完。
    返还 「运行」。
完。

样式解析_自检 接受 档：
    做 样式解析_初始化 用 档。
    做 样式解析_加载文本 用 档、「自检」、「body { color: #fff; }」。
    做 样式解析_解析属性 用 档、「color」、「#fff」。
    做 样式解析_解析颜色值 用 档、「#fff」。
    做 样式解析_解析颜色函数 用 档、「rgb(255,255,255)」。
    做 样式解析_解析命名颜色 用 档、「white」。
    做 样式解析_解析单位 用 档、「px」。
    做 样式解析_更新视口 用 档、1280、720。
    做 样式解析_匹配媒体 用 档、「(min-width: 600px)」。
    档 的 健康 为 做 样式解析_健康 用 档。
    返还 档 的 健康。
完。

样式解析_全局统计 接受 零参：
    返还 一组：样式建数、样式总解析、样式总声明、样式总颜色、样式总媒查、样式总容器、样式总命中。
完。

样式解析_按名查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 样式账本：
        若 样式账本 的 第 i 项 的 名称 等于 名称：
            返还 样式账本 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

样式解析_最近结果 接受 档：
    返还 档 的 最近结果。
完。

样式解析_最近意图 接受 档：
    返还 档 的 最近意图。
完。

样式解析_最近影响 接受 档：
    返还 档 的 最近影响。
完。

样式解析_最近属性 接受 档：
    返还 档 的 最近属性。
完。

样式解析_最近查询 接受 档：
    返还 档 的 最近查询。
完。

样式解析_媒体值 接受 档：
    返还 档 的 媒体值。
完。

样式解析_账本数 接受 零参：
    返还 做 计数 用 样式账本。
完。

样式解析_示例流程 接受 零参：
    档 为 做 样式解析_创建 用 「样式解析」、「感知」。
    做 样式解析_加载文本 用 档、「示例」、「body { color: #222; background: rgb(255,255,255); }」。
    做 样式解析_解析属性 用 档、「color」、「#222」。
    做 样式解析_解析颜色值 用 档、「#222」。
    做 样式解析_解析颜色函数 用 档、「rgb(255,255,255)」。
    做 样式解析_设置配色 用 档、假。
    做 样式解析_设置动效 用 档、假。
    做 样式解析_更新视口 用 档、1440、900。
    做 样式解析_匹配媒体 用 档、「(min-width: 1024px)」。
    做 样式解析_注册容器 用 档、「根」、「主容器」、CONTAINER_TYPE_SIZE。
    做 样式解析_更新容器 用 档、「根」、1440、900。
    做 样式解析_推断意图 用 档、「background-color」、「#fff」。
    做 样式解析_分析影响 用 档、「background-color」。
    做 样式解析_统一解析 用 档、「width」、「100vw」、「(orientation: landscape)」。
    返还 做 样式解析_摘要 用 档。
完。

HTML令牌izer_处理_属性名 接受 tk、令牌、c：
    若 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0C 或者 c 等于 0x20：
        tk 的 状态 为 状态_属性名后。
    否则若 c 等于 0x2F：
        做 HTML令牌izer_提交属性 用 tk、令牌。
        tk 的 状态 为 状态_自闭合开始标签。
    否则若 c 等于 0x3E：
        做 HTML令牌izer_提交属性 用 tk、令牌。
        做 HTML令牌izer_完成标签名 用 令牌。
        若 令牌 的 类型 等于 令牌类型_开始标签：
            做 HTML令牌izer_记录开始标签 用 tk、令牌。
        完。
        tk 的 状态 为 状态_数据。
    否则若 c 等于 0x3D：
        tk 的 状态 为 状态_属性值前。
    否则：
        做 HTML令牌izer_追加属性名字符 用 tk、做 HTML令牌izer_转小写 用 c。
    完。
完。

HTML令牌izer_处理_属性名后 接受 tk、令牌、c：
    若 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0C 或者 c 等于 0x20：
        返还。
    完。
    若 c 等于 0x3D：
        tk 的 状态 为 状态_属性值前。
    否则若 c 等于 0x2F：
        做 HTML令牌izer_提交属性 用 tk、令牌。
        tk 的 状态 为 状态_自闭合开始标签。
    否则若 c 等于 0x3E：
        做 HTML令牌izer_提交属性 用 tk、令牌。
        做 HTML令牌izer_完成标签名 用 令牌。
        若 令牌 的 类型 等于 令牌类型_开始标签：
            做 HTML令牌izer_记录开始标签 用 tk、令牌。
        完。
        tk 的 状态 为 状态_数据。
    否则：
        做 HTML令牌izer_提交属性 用 tk、令牌。
        tk 的 当前属性名长度 为 0。
        tk 的 当前属性值长度 为 0。
        做 HTML令牌izer_追加属性名字符 用 tk、做 HTML令牌izer_转小写 用 c。
        tk 的 状态 为 状态_属性名。
    完。
完。

HTML令牌izer_处理_属性值前 接受 tk、令牌、c：
    若 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0C 或者 c 等于 0x20：
        返还。
    完。
    若 c 等于 0x22：
        tk 的 状态 为 状态_属性值_双引号。
    否则若 c 等于 0x27：
        tk 的 状态 为 状态_属性值_单引号。
    否则若 c 等于 0x3E：
        做 HTML令牌izer_提交属性 用 tk、令牌。
        做 HTML令牌izer_完成标签名 用 令牌。
        若 令牌 的 类型 等于 令牌类型_开始标签：
            做 HTML令牌izer_记录开始标签 用 tk、令牌。
        完。
        tk 的 状态 为 状态_数据。
    否则：
        做 HTML令牌izer_追加属性值字符 用 tk、c。
        tk 的 状态 为 状态_属性值_无引号。
    完。
完。

HTML令牌izer_处理_属性值双引号 接受 tk、令牌、c：
    若 c 等于 0x22：
        做 HTML令牌izer_提交属性 用 tk、令牌。
        tk 的 状态 为 状态_属性值后。
    否则若 c 等于 0x26：
        tk 的 返回状态 为 状态_属性值_双引号。
        tk 的 状态 为 状态_字符引用。
    否则：
        做 HTML令牌izer_追加属性值字符 用 tk、c。
    完。
完。

HTML令牌izer_处理_属性值单引号 接受 tk、令牌、c：
    若 c 等于 0x27：
        做 HTML令牌izer_提交属性 用 tk、令牌。
        tk 的 状态 为 状态_属性值后。
    否则若 c 等于 0x26：
        tk 的 返回状态 为 状态_属性值_单引号。
        tk 的 状态 为 状态_字符引用。
    否则：
        做 HTML令牌izer_追加属性值字符 用 tk、c。
    完。
完。

HTML令牌izer 是 一种 事物，含 状态、返回状态、输入、输入长度、位置、当前令牌、临时缓冲、临时缓冲长度、临时缓冲容量、当前属性名、当前属性名长度、当前属性值、当前属性值长度、最后开始标签、最后开始标签长度、字符引用码点、总令牌数、行号、列号。

HTML令牌izer_创建 接受 零参：
    tk 为 做 堆_分配 用 HTML令牌izer。
    tk 的 状态 为 状态_数据。
    tk 的 返回状态 为 状态_数据。
    tk 的 输入 为 0。
    tk 的 输入长度 为 0。
    tk 的 位置 为 0。
    tk 的 当前令牌 为 0。
    tk 的 临时缓冲 为 做 堆_分配_数组 用 1024。
    tk 的 临时缓冲长度 为 0。
    tk 的 临时缓冲容量 为 1024。
    tk 的 当前属性名 为 做 堆_分配_数组 用 256。
    tk 的 当前属性名长度 为 0。
    tk 的 当前属性值 为 做 堆_分配_数组 用 4096。
    tk 的 当前属性值长度 为 0。
    tk 的 最后开始标签 为 做 堆_分配_数组 用 64。
    tk 的 最后开始标签长度 为 0。
    tk 的 字符引用码点 为 0。
    tk 的 总令牌数 为 0。
    tk 的 行号 为 1。
    tk 的 列号 为 1。
    返还 tk。
完。

HTML令牌izer_设置输入 接受 tk、html字符串：
    tk 的 输入 为 html字符串。
    tk 的 输入长度 为 做 字符串_长度 用 html字符串。
    tk 的 位置 为 0。
    tk 的 行号 为 1。
    tk 的 列号 为 1。
完。

HTML令牌_创建 接受 零参：
    令牌 为 做 堆_分配 用 HTML令牌。
    令牌 的 类型 为 令牌类型_未初始化。
    令牌 的 标签名 为 做 堆_分配_数组 用 128。
    令牌 的 标签名长度 为 0。
    令牌 的 自闭合 为 假。
    令牌 的 属性列表 为 做 堆_分配_数组 用 16 乘 40。
    令牌 的 属性数量 为 0。
    令牌 的 属性容量 为 16。
    令牌 的 DOCTYPE名称 为 0。
    令牌 的 DOCTYPE公共ID 为 0。
    令牌 的 DOCTYPE系统ID 为 0。
    令牌 的 强制怪异模式 为 假。
    令牌 的 数据 为 做 堆_分配_数组 用 4096。
    令牌 的 数据长度 为 0。
    返还 令牌。
完。

HTML令牌_重置 接受 令牌：
    令牌 的 类型 为 令牌类型_未初始化。
    令牌 的 标签名长度 为 0。
    令牌 的 自闭合 为 假。
    令牌 的 属性数量 为 0。
    令牌 的 数据长度 为 0。
    令牌 的 强制怪异模式 为 假。
完。

DOM令牌List 是 一种 事物，含 元素、属性名、令牌、令牌数量、令牌容量。

DOM令牌List_创建 接受 元素、属性名：
    tl 为 做 堆_分配 用 DOM令牌List。
    tl 的 元素 为 元素。
    tl 的 属性名 为 做 字符串_复制 用 属性名。
    tl 的 令牌容量 为 16。
    tl 的 令牌 为 做 堆_分配_数组 用 tl 的 令牌容量 乘 8。
    tl 的 令牌数量 为 0。
    做 DOM令牌List_从属性同步 用 tl。
    返还 tl。
完。

DOM令牌List_从属性同步 接受 tl：
    值 为 做 元素_getAttribute 用 tl 的 元素、tl 的 属性名。
    若 值 等于 0：
        tl 的 令牌数量 为 0。
        返还。
    完。
    tl 的 令牌数量 为 0。
    长度 为 做 字符串_长度 用 值。
    可变 pos 为 0。
    只要 pos 小于 长度：
        只要 pos 小于 长度 并且 做 内存_读8 用 值 加 pos 等于 0x20：
            pos 为 pos 加 1。
        完。
        起始 为 pos。
        只要 pos 小于 长度 并且 做 内存_读8 用 值 加 pos 不等于 0x20：
            pos 为 pos 加 1。
        完。
        若 pos 大于 起始 并且 tl 的 令牌数量 小于 tl 的 令牌容量：
            做 内存_写64 用 tl 的 令牌 加 tl 的 令牌数量 乘 8、做 字符串_复制_N 用 值 加 起始、pos 减 起始。
            tl 的 令牌数量 为 tl 的 令牌数量 加 1。
        完。
    完。
完。

DOM令牌List_同步到属性 接受 tl：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 tl 的 令牌数量：
        若 i 大于 0：
            结果 为 做 字符串_连接 用 结果、「 」。
        完。
        结果 为 做 字符串_连接 用 结果、做 内存_读64 用 tl 的 令牌 加 i 乘 8。
        i 为 i 加 1。
    完。
    做 元素_setAttribute 用 tl 的 元素、tl 的 属性名、结果。
完。

DOM令牌List_contains 接受 tl、令牌：
    可变 i 为 0。
    只要 i 小于 tl 的 令牌数量：
        若 做 字符串_相等 用 做 内存_读64 用 tl 的 令牌 加 i 乘 8、令牌：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

DOM令牌List_add 接受 tl、令牌：
    若 做 DOM令牌List_contains 用 tl、令牌：
        返还。
    完。
    若 tl 的 令牌数量 大于或等于 tl 的 令牌容量：
        返还。
    完。
    做 内存_写64 用 tl 的 令牌 加 tl 的 令牌数量 乘 8、做 字符串_复制 用 令牌。
    tl 的 令牌数量 为 tl 的 令牌数量 加 1。
    做 DOM令牌List_同步到属性 用 tl。
完。

DOM令牌List_remove 接受 tl、令牌：
    可变 i 为 0。
    只要 i 小于 tl 的 令牌数量：
        若 做 字符串_相等 用 做 内存_读64 用 tl 的 令牌 加 i 乘 8、令牌：
            可变 j 为 i。
            只要 j 小于 tl 的 令牌数量 减 1：
                做 内存_写64 用 tl 的 令牌 加 j 乘 8、做 内存_读64 用 tl 的 令牌 加 (j 加 1) 乘 8。
                j 为 j 加 1。
            完。
            tl 的 令牌数量 为 tl 的 令牌数量 减 1。
            做 DOM令牌List_同步到属性 用 tl。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

DOM令牌List_toggle 接受 tl、令牌：
    若 做 DOM令牌List_contains 用 tl、令牌：
        做 DOM令牌List_remove 用 tl、令牌。
        返还 假。
    完。
    做 DOM令牌List_add 用 tl、令牌。
    返还 真。
完。

DOM令牌List_replace 接受 tl、旧令牌、新令牌：
    可变 i 为 0。
    只要 i 小于 tl 的 令牌数量：
        若 做 字符串_相等 用 做 内存_读64 用 tl 的 令牌 加 i 乘 8、旧令牌：
            做 内存_写64 用 tl 的 令牌 加 i 乘 8、做 字符串_复制 用 新令牌。
            做 DOM令牌List_同步到属性 用 tl。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

查询参数_最大数量 为 128。

URLSearchParams 是 一种 事物，含 键、值、数量。

查询参数统计 是 一种 事物，含 解析次数、查询次数、设置次数、删除次数、序列化次数、排序次数、总参数数。

可变 全局_查询参数统计 为 空。

查询参数统计_初始化 接受 零参：
    若 全局_查询参数统计 等于 空：
        全局_查询参数统计 为 新建 查询参数统计。
        全局_查询参数统计 的 解析次数 为 0。
        全局_查询参数统计 的 查询次数 为 0。
        全局_查询参数统计 的 设置次数 为 0。
        全局_查询参数统计 的 删除次数 为 0。
        全局_查询参数统计 的 序列化次数 为 0。
        全局_查询参数统计 的 排序次数 为 0。
        全局_查询参数统计 的 总参数数 为 0。
    完。
完。

URLSearchParams_创建 接受 零参：
    params 为 做 堆_分配 用 URLSearchParams。
    params 的 键 为 一组：（空）。
    params 的 值 为 一组：（空）。
    params 的 数量 为 0。
    返还 params。
完。

URLSearchParams_解析 接受 查询字符串：
    做 查询参数统计_初始化。
    全局_查询参数统计 的 解析次数 为 全局_查询参数统计 的 解析次数 加 1。
    params 为 做 URLSearchParams_创建。
    若 查询字符串 等于 0：
        返还 params。
    完。
    可变 输入 为 查询字符串。
    若 做 字节长度 用 输入 大于 0 并且 做 字节取 用 输入、0 等于 63：
        输入 为 做 字符串_截取 用 输入、1、做 字节长度 用 输入。
    完。
    长度 为 做 字节长度 用 输入。
    可变 i 为 0。
    只要 i 小于 长度 并且 params 的 数量 小于 查询参数_最大数量：
        键开始 为 i。
        只要 i 小于 长度 并且 做 字节取 用 输入、i 不等于 61 并且 做 字节取 用 输入、i 不等于 38：
            i 为 i 加 1。
        完。
        键 为 做 URL_百分比解码 用 做 字符串_截取 用 输入、键开始、i。
        可变 值 为 「」。
        若 i 小于 长度 并且 做 字节取 用 输入、i 等于 61：
            i 为 i 加 1。
            值开始 为 i。
            只要 i 小于 长度 并且 做 字节取 用 输入、i 不等于 38：
                i 为 i 加 1。
            完。
            值 为 做 URL_百分比解码 用 做 字符串_截取 用 输入、值开始、i。
        完。
        若 做 字节长度 用 键 大于 0：
            做 追加 用 params 的 键、键。
            做 追加 用 params 的 值、值。
            params 的 数量 为 params 的 数量 加 1。
            全局_查询参数统计 的 总参数数 为 全局_查询参数统计 的 总参数数 加 1。
        完。
        若 i 小于 长度 并且 做 字节取 用 输入、i 等于 38：
            i 为 i 加 1。
        完。
    完。
    返还 params。
完。

URLSearchParams_get 接受 params、键：
    做 查询参数统计_初始化。
    全局_查询参数统计 的 查询次数 为 全局_查询参数统计 的 查询次数 加 1。
    若 params 等于 0 或者 键 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        若 做 字符串_相等 用 params 的 键 的 第 i 项、键：
            返还 params 的 值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

URLSearchParams_getAll 接受 params、键：
    做 查询参数统计_初始化。
    全局_查询参数统计 的 查询次数 为 全局_查询参数统计 的 查询次数 加 1。
    可变 结果 为 一组：（空）。
    若 params 等于 0 或者 键 等于 0：
        返还 结果。
    完。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        若 做 字符串_相等 用 params 的 键 的 第 i 项、键：
            做 追加 用 结果、params 的 值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

URLSearchParams_has 接受 params、键：
    若 params 等于 0 或者 键 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        若 做 字符串_相等 用 params 的 键 的 第 i 项、键：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

URLSearchParams_has值 接受 params、键、值：
    若 params 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        若 做 字符串_相等 用 params 的 键 的 第 i 项、键 并且 做 字符串_相等 用 params 的 值 的 第 i 项、值：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

URLSearchParams_set 接受 params、键、值：
    做 查询参数统计_初始化。
    全局_查询参数统计 的 设置次数 为 全局_查询参数统计 的 设置次数 加 1。
    若 params 等于 0：
        返还。
    完。
    可变 已找到 为 假。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        若 做 字符串_相等 用 params 的 键 的 第 i 项、键：
            若 非 已找到：
                params 的 值 的 第 i 项 为 值。
                已找到 为 真。
                i 为 i 加 1。
            否则：
                做 URLSearchParams_移除索引 用 params、i。
            完。
        否则：
            i 为 i 加 1。
        完。
    完。
    若 非 已找到 并且 params 的 数量 小于 查询参数_最大数量：
        做 追加 用 params 的 键、键。
        做 追加 用 params 的 值、值。
        params 的 数量 为 params 的 数量 加 1。
    完。
完。

URLSearchParams_append 接受 params、键、值：
    若 params 等于 0 或者 params 的 数量 大于或等于 查询参数_最大数量：
        返还。
    完。
    做 追加 用 params 的 键、键。
    做 追加 用 params 的 值、值。
    params 的 数量 为 params 的 数量 加 1。
完。

URLSearchParams_delete 接受 params、键：
    做 查询参数统计_初始化。
    全局_查询参数统计 的 删除次数 为 全局_查询参数统计 的 删除次数 加 1。
    若 params 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        若 做 字符串_相等 用 params 的 键 的 第 i 项、键：
            做 URLSearchParams_移除索引 用 params、i。
        否则：
            i 为 i 加 1。
        完。
    完。
完。

URLSearchParams_delete值 接受 params、键、值：
    若 params 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        若 做 字符串_相等 用 params 的 键 的 第 i 项、键 并且 做 字符串_相等 用 params 的 值 的 第 i 项、值：
            做 URLSearchParams_移除索引 用 params、i。
        否则：
            i 为 i 加 1。
        完。
    完。
完。

URLSearchParams_移除索引 接受 params、索引：
    若 索引 小于 0 或者 索引 大于或等于 params 的 数量：
        返还。
    完。
    可变 j 为 索引。
    只要 j 小于 params 的 数量 减 1：
        params 的 键 的 第 j 项 为 params 的 键 的 第 j 加 1 项。
        params 的 值 的 第 j 项 为 params 的 值 的 第 j 加 1 项。
        j 为 j 加 1。
    完。
    params 的 数量 为 params 的 数量 减 1。
完。

URLSearchParams_sort 接受 params：
    做 查询参数统计_初始化。
    全局_查询参数统计 的 排序次数 为 全局_查询参数统计 的 排序次数 加 1。
    若 params 等于 0 或者 params 的 数量 小于或等于 1：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 params 的 数量 减 1：
        可变 j 为 0。
        只要 j 小于 params 的 数量 减 1 减 i：
            若 做 字符串_比较 用 params 的 键 的 第 j 项、params 的 键 的 第 j 加 1 项 大于 0：
                临时键 为 params 的 键 的 第 j 项。
                临时值 为 params 的 值 的 第 j 项。
                params 的 键 的 第 j 项 为 params 的 键 的 第 j 加 1 项。
                params 的 值 的 第 j 项 为 params 的 值 的 第 j 加 1 项。
                params 的 键 的 第 j 加 1 项 为 临时键。
                params 的 值 的 第 j 加 1 项 为 临时值。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

URLSearchParams_序列化 接受 params：
    做 查询参数统计_初始化。
    全局_查询参数统计 的 序列化次数 为 全局_查询参数统计 的 序列化次数 加 1。
    若 params 等于 0 或者 params 的 数量 等于 0：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        若 i 大于 0：
            结果 为 做 字符串拼接 用 结果、「&」。
        完。
        结果 为 做 字符串拼接 用 结果、做 URL_百分比编码 用 params 的 键 的 第 i 项、0。
        结果 为 做 字符串拼接 用 结果、「=」。
        结果 为 做 字符串拼接 用 结果、做 URL_百分比编码 用 params 的 值 的 第 i 项、0。
        i 为 i 加 1。
    完。
    返还 结果。
完。

URLSearchParams_size 接受 params：
    若 params 等于 0：
        返还 0。
    完。
    返还 params 的 数量。
完。

URLSearchParams_keys 接受 params：
    可变 结果 为 一组：（空）。
    若 params 等于 0：
        返还 结果。
    完。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        做 追加 用 结果、params 的 键 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

URLSearchParams_values 接受 params：
    可变 结果 为 一组：（空）。
    若 params 等于 0：
        返还 结果。
    完。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        做 追加 用 结果、params 的 值 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

URLSearchParams_entries 接受 params：
    可变 结果 为 一组：（空）。
    若 params 等于 0：
        返还 结果。
    完。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        做 追加 用 结果、一组：params 的 键 的 第 i 项、params 的 值 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

URLSearchParams_forEach 接受 params、回调：
    若 params 等于 0 或者 回调 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        做 调用回调 用 回调、params 的 值 的 第 i 项、params 的 键 的 第 i 项、params。
        i 为 i 加 1。
    完。
完。

URLSearchParams_合并 接受 目标、源：
    若 目标 等于 0 或者 源 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 源 的 数量 并且 目标 的 数量 小于 查询参数_最大数量：
        做 追加 用 目标 的 键、源 的 键 的 第 i 项。
        做 追加 用 目标 的 值、源 的 值 的 第 i 项。
        目标 的 数量 为 目标 的 数量 加 1。
        i 为 i 加 1。
    完。
完。

URLSearchParams_克隆 接受 params：
    新 为 做 URLSearchParams_创建。
    若 params 等于 0：
        返还 新。
    完。
    可变 i 为 0。
    只要 i 小于 params 的 数量：
        做 追加 用 新 的 键、params 的 键 的 第 i 项。
        做 追加 用 新 的 值、params 的 值 的 第 i 项。
        i 为 i 加 1。
    完。
    新 的 数量 为 params 的 数量。
    返还 新。
完。

查询参数_打印统计 接受 零参：
    做 查询参数统计_初始化。
    做 打印行 用 「=== 查询参数统计 ===」。
    做 打印行 用 「解析: 」 加 做 整数转字符串 用 全局_查询参数统计 的 解析次数。
    做 打印行 用 「查询: 」 加 做 整数转字符串 用 全局_查询参数统计 的 查询次数。
    做 打印行 用 「设置: 」 加 做 整数转字符串 用 全局_查询参数统计 的 设置次数。
    做 打印行 用 「删除: 」 加 做 整数转字符串 用 全局_查询参数统计 的 删除次数。
    做 打印行 用 「序列化: 」 加 做 整数转字符串 用 全局_查询参数统计 的 序列化次数。
    做 打印行 用 「排序: 」 加 做 整数转字符串 用 全局_查询参数统计 的 排序次数。
    做 打印行 用 「总参数: 」 加 做 整数转字符串 用 全局_查询参数统计 的 总参数数。
完。

可变 安全_同源检查次数 为 0。

可变 安全_CORS检查次数 为 0。

可变 安全_CSP拦截次数 为 0。

可变 安全_混合内容拦截 为 0。

URL解析结果 是 一种 事物，含 协议、主机、端口、路径、查询、片段、用户信息、有效。

URL_协议匹配 接受 url、协议：
    返还 做 字符串_前缀匹配 用 url、协议。
完。

URL_完整解析 接受 url：
    结果 为 新建 URL解析结果：协议 为 「」、主机 为 「」、端口 为 0、路径 为 「/」、查询 为 「」、片段 为 「」、用户信息 为 「」、有效 为 假。
    若 url 等于 0：
        返还 结果。
    完。
    len 为 做 字节长度 用 url。
    若 len 等于 0：
        返还 结果。
    完。
    可变 pos 为 0。
    可变 协议结束 为 0。
    只要 协议结束 小于 len 并且 做 字节取 用 url、协议结束 不等于 0x3A：
        协议结束 为 协议结束 加 1。
    完。
    若 协议结束 小于 len：
        结果 的 协议 为 做 字符串_子串_小写 用 url、0、协议结束。
        pos 为 协议结束 加 1。
    完。
    若 pos 加 1 小于 len 并且 做 字节取 用 url、pos 等于 0x2F 并且 做 字节取 用 url、pos 加 1 等于 0x2F：
        pos 为 pos 加 2。
        可变 权限结束 为 pos。
        只要 权限结束 小于 len 并且 做 字节取 用 url、权限结束 不等于 0x2F 并且 做 字节取 用 url、权限结束 不等于 0x3F 并且 做 字节取 用 url、权限结束 不等于 0x23：
            权限结束 为 权限结束 加 1。
        完。
        可变 用户结束 为 pos。
        可变 有用户 为 假。
        可变 扫描 为 pos。
        只要 扫描 小于 权限结束：
            若 做 字节取 用 url、扫描 等于 0x40：
                用户结束 为 扫描。
                有用户 为 真。
                跳出。
            完。
            扫描 为 扫描 加 1。
        完。
        若 有用户：
            结果 的 用户信息 为 做 字符串_子串 用 url、pos、用户结束。
            pos 为 用户结束 加 1。
        完。
        可变 主机结束 为 pos。
        只要 主机结束 小于 权限结束 并且 做 字节取 用 url、主机结束 不等于 0x3A：
            主机结束 为 主机结束 加 1。
        完。
        结果 的 主机 为 做 字符串_子串_小写 用 url、pos、主机结束。
        若 主机结束 小于 权限结束 并且 做 字节取 用 url、主机结束 等于 0x3A：
            可变 端口值 为 0。
            可变 pi 为 主机结束 加 1。
            只要 pi 小于 权限结束 并且 做 字节取 用 url、pi 大于或等于 0x30 并且 做 字节取 用 url、pi 小于或等于 0x39：
                端口值 为 端口值 乘 10 加 (做 字节取 用 url、pi 减 0x30)。
                pi 为 pi 加 1。
            完。
            结果 的 端口 为 端口值。
        完。
        pos 为 权限结束。
    完。
    若 结果 的 端口 等于 0：
        若 结果 的 协议 等于 「https」：
            结果 的 端口 为 443。
        否则若 结果 的 协议 等于 「http」：
            结果 的 端口 为 80。
        否则若 结果 的 协议 等于 「ftp」：
            结果 的 端口 为 21。
        否则若 结果 的 协议 等于 「ws」：
            结果 的 端口 为 80。
        否则若 结果 的 协议 等于 「wss」：
            结果 的 端口 为 443。
        完。
    完。
    可变 路径结束 为 pos。
    只要 路径结束 小于 len 并且 做 字节取 用 url、路径结束 不等于 0x3F 并且 做 字节取 用 url、路径结束 不等于 0x23：
        路径结束 为 路径结束 加 1。
    完。
    若 路径结束 大于 pos：
        结果 的 路径 为 做 字符串_子串 用 url、pos、路径结束。
    完。
    pos 为 路径结束。
    若 pos 小于 len 并且 做 字节取 用 url、pos 等于 0x3F：
        pos 为 pos 加 1。
        可变 查询结束 为 pos。
        只要 查询结束 小于 len 并且 做 字节取 用 url、查询结束 不等于 0x23：
            查询结束 为 查询结束 加 1。
        完。
        结果 的 查询 为 做 字符串_子串 用 url、pos、查询结束。
        pos 为 查询结束。
    完。
    若 pos 小于 len 并且 做 字节取 用 url、pos 等于 0x23：
        结果 的 片段 为 做 字符串_子串 用 url、pos 加 1、len。
    完。
    结果 的 有效 为 做 字符串长度 用 结果 的 主机 大于 0 或者 结果 的 协议 等于 「data」 或者 结果 的 协议 等于 「blob」。
    返还 结果。
完。

URL_提取主机 接受 url：
    解析 为 做 URL_完整解析 用 url。
    返还 解析 的 主机。
完。

URL_提取端口 接受 url：
    解析 为 做 URL_完整解析 用 url。
    返还 解析 的 端口。
完。

URL_提取协议 接受 url：
    解析 为 做 URL_完整解析 用 url。
    返还 解析 的 协议。
完。

URL_同源检查 接受 url_a、url_b：
    安全_同源检查次数 为 安全_同源检查次数 加 1。
    a 为 做 URL_完整解析 用 url_a。
    b 为 做 URL_完整解析 用 url_b。
    若 a 的 协议 等于 「data」 或者 b 的 协议 等于 「data」：
        返还 假。
    完。
    若 a 的 协议 等于 「blob」 或者 b 的 协议 等于 「blob」：
        返还 假。
    完。
    若 a 的 协议 等于 「file」 并且 b 的 协议 等于 「file」：
        返还 真。
    完。
    若 a 的 协议 不等于 b 的 协议：
        返还 假。
    完。
    若 a 的 主机 不等于 b 的 主机：
        返还 假。
    完。
    若 a 的 端口 不等于 b 的 端口：
        返还 假。
    完。
    返还 真。
完。

URL_同站检查 接受 url_a、url_b：
    a 为 做 URL_完整解析 用 url_a。
    b 为 做 URL_完整解析 用 url_b。
    若 a 的 协议 不等于 b 的 协议：
        返还 假。
    完。
    域a 为 做 提取eTLD加1 用 a 的 主机。
    域b 为 做 提取eTLD加1 用 b 的 主机。
    返还 域a 等于 域b。
完。

提取eTLD加1 接受 主机：
    若 主机 等于 0：
        返还 主机。
    完。
    len 为 做 字符串长度 用 主机。
    可变 点数 为 0。
    可变 最后点 为 负 1。
    可变 倒数第二点 为 负 1。
    可变 i 为 len 减 1。
    只要 i 大于或等于 0：
        若 做 字节取 用 主机、i 等于 0x2E：
            点数 为 点数 加 1。
            若 点数 等于 1：
                最后点 为 i。
            完。
            若 点数 等于 2：
                倒数第二点 为 i。
                跳出。
            完。
        完。
        i 为 i 减 1。
    完。
    若 倒数第二点 大于或等于 0：
        返还 做 字符串_子串 用 主机、倒数第二点 加 1、len。
    完。
    返还 主机。
完。

CORS_检查 接受 请求源、响应头：
    安全_CORS检查次数 为 安全_CORS检查次数 加 1。
    允许源 为 做 响应头_获取 用 响应头、「Access-Control-Allow-Origin」。
    若 允许源 等于 空：
        返还 假。
    完。
    若 允许源 等于 「*」：
        返还 真。
    完。
    返还 允许源 等于 请求源。
完。

CORS_预检检查 接受 请求方法、请求头、响应头：
    允许方法 为 做 响应头_获取 用 响应头、「Access-Control-Allow-Methods」。
    若 允许方法 等于 空：
        返还 假。
    完。
    若 非 做 字符串_包含 用 允许方法、请求方法：
        返还 假。
    完。
    允许头 为 做 响应头_获取 用 响应头、「Access-Control-Allow-Headers」。
    若 请求头 不等于 空 并且 允许头 不等于 空：
        若 非 做 字符串_包含 用 允许头、请求头：
            返还 假。
        完。
    完。
    返还 真。
完。

混合内容_检查 接受 页面url、资源url：
    页面 为 做 URL_完整解析 用 页面url。
    资源 为 做 URL_完整解析 用 资源url。
    若 页面 的 协议 等于 「https」 并且 资源 的 协议 等于 「http」：
        安全_混合内容拦截 为 安全_混合内容拦截 加 1。
        返还 假。
    完。
    返还 真。
完。

CSP_检查 接受 指令、源、url：
    若 指令 等于 空 或者 源 等于 空：
        返还 真。
    完。
    若 做 字符串_包含 用 源、「'none'」：
        安全_CSP拦截次数 为 安全_CSP拦截次数 加 1。
        返还 假。
    完。
    若 做 字符串_包含 用 源、「'self'」：
        返还 真。
    完。
    若 做 字符串_包含 用 源、「*」：
        返还 真。
    完。
    资源主机 为 做 URL_提取主机 用 url。
    若 做 字符串_包含 用 源、资源主机：
        返还 真。
    完。
    安全_CSP拦截次数 为 安全_CSP拦截次数 加 1。
    返还 假。
完。

平台_创建渲染进程 接受 零参：
    fds 为 做 堆_分配 用 8。
    做 平台_socketpair 用 fds。
    pid 为 做 系统调用_fork。
    若 pid 等于 0：
        做 系统调用_close 用 做 内存_读32 用 fds。
        做 Sandbox_应用 用 做 内存_读32 用 fds 加 4。
        做 渲染er_主循环 用 做 内存_读32 用 fds 加 4。
        做 系统调用_exit 用 0。
    完。
    做 系统调用_close 用 做 内存_读32 用 fds 加 4。
    proc 为 做 堆_分配 用 24。
    做 内存_写64 用 proc、pid。
    做 内存_写64 用 proc 加 8、做 内存_读32 用 fds。
    做 堆_释放 用 fds。
    返还 proc。
完。

安全模型_统计 接受 零参：
    返还 一本字典：同源检查 对应 安全_同源检查次数、CORS检查 对应 安全_CORS检查次数、CSP拦截 对应 安全_CSP拦截次数、混合内容拦截 对应 安全_混合内容拦截。
完。

安全模型_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 安全_CSP拦截次数 大于 100：
        做 追加 用 问题、「CSP拦截过多: 」 加 做 整数转字符串 用 安全_CSP拦截次数。
    完。
    若 安全_混合内容拦截 大于 50：
        做 追加 用 问题、「混合内容拦截过多」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「安全模型健康」。
    完。
    返还 问题。
完。

安全模型_重置 接受 零参：
    安全_同源检查次数 为 0。
    安全_CORS检查次数 为 0。
    安全_CSP拦截次数 为 0。
    安全_混合内容拦截 为 0。
完。

安全模型_同源通过率 接受 零参：
    若 安全_同源检查次数 等于 0：
        返还 0。
    完。
    返还 (安全_同源检查次数 减 安全_CSP拦截次数) 乘 100 除以 安全_同源检查次数。
完。

安全模型_CORS通过率 接受 零参：
    若 安全_CORS检查次数 等于 0：
        返还 0。
    完。
    返还 安全_CORS检查次数 乘 100 除以 安全_同源检查次数。
完。

SECURITY_NONE 为 0。

SECURITY_SAME_ORIGIN 为 1。

SECURITY_CORS 为 2。

SECURITY_BLOCKED 为 3。

安全_级别名 接受 级别：
    若 级别 等于 SECURITY_NONE：
        返还 「NONE」。
    完。
    若 级别 等于 SECURITY_SAME_ORIGIN：
        返还 「SAME_ORIGIN」。
    完。
    若 级别 等于 SECURITY_CORS：
        返还 「CORS」。
    完。
    若 级别 等于 SECURITY_BLOCKED：
        返还 「BLOCKED」。
    完。
    返还 「UNKNOWN」。
完。

安全模型_完整统计 接受 零参：
    返还 一组：安全_同源检查次数、安全_CORS检查次数、安全_CSP拦截次数、安全_混合内容拦截。
完。

CSS解析_十六进制颜色 接受 文本、长度、pos：
    可变 p 为 pos 加 1。
    可变 hex_len 为 0。
    hex起始 为 p。
    只要 p 小于 长度 并且 做 CSS_是十六进制 用 做 内存_读8 用 文本 加 p：
        p 为 p 加 1。
        hex_len 为 hex_len 加 1。
    完。
    v 为 做 CSSParsedValue_创建 用 CSS值_颜色。
    若 hex_len 等于 3：
        r 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始。
        g 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 1。
        b 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 2。
        v 的 颜色值 为 r 乘 17 乘 0x10000 加 g 乘 17 乘 0x100 加 b 乘 17 加 0xFF000000。
    否则若 hex_len 等于 4：
        r 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始。
        g 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 1。
        b 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 2。
        a 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 3。
        v 的 颜色值 为 a 乘 17 乘 0x1000000 加 r 乘 17 乘 0x10000 加 g 乘 17 乘 0x100 加 b 乘 17。
    否则若 hex_len 等于 6：
        r 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 乘 16 加 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 1。
        g 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 2 乘 16 加 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 3。
        b 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 4 乘 16 加 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 5。
        v 的 颜色值 为 0xFF000000 加 r 乘 0x10000 加 g 乘 0x100 加 b。
    否则若 hex_len 等于 8：
        r 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 乘 16 加 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 1。
        g 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 2 乘 16 加 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 3。
        b 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 4 乘 16 加 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 5。
        a 为 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 6 乘 16 加 做 CSS_hex数字 用 做 内存_读8 用 文本 加 hex起始 加 7。
        v 的 颜色值 为 a 乘 0x1000000 加 r 乘 0x10000 加 g 乘 0x100 加 b。
    否则：
        返还 0。
    完。
    返还 v。
完。

CSS解析_命名颜色 接受 名称、名称长度：
    v 为 做 CSSParsedValue_创建 用 CSS值_颜色。
    若 做 字符串_等于N 用 名称、名称长度、「transparent」：
        v 的 颜色值 为 0x00000000。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「currentColor」：
        v 的 关键字ID 为 关键字_currentColor。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「currentcolor」：
        v 的 关键字ID 为 关键字_currentColor。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「black」：
        v 的 颜色值 为 0xFF000000。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「white」：
        v 的 颜色值 为 0xFFFFFFFF。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「red」：
        v 的 颜色值 为 0xFFFF0000。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「green」：
        v 的 颜色值 为 0xFF008000。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「blue」：
        v 的 颜色值 为 0xFF0000FF。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「yellow」：
        v 的 颜色值 为 0xFFFFFF00。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「orange」：
        v 的 颜色值 为 0xFFFFA500。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「purple」：
        v 的 颜色值 为 0xFF800080。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「pink」：
        v 的 颜色值 为 0xFFFFC0CB。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「gray」：
        v 的 颜色值 为 0xFF808080。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「grey」：
        v 的 颜色值 为 0xFF808080。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「aliceblue」：
        v 的 颜色值 为 0xFFF0F8FF。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「antiquewhite」：
        v 的 颜色值 为 0xFFFAEBD7。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「aqua」：
        v 的 颜色值 为 0xFF00FFFF。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「aquamarine」：
        v 的 颜色值 为 0xFF7FFFD4。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「azure」：
        v 的 颜色值 为 0xFFF0FFFF。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「beige」：
        v 的 颜色值 为 0xFFF5F5DC。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「bisque」：
        v 的 颜色值 为 0xFFFFE4C4。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「blanchedalmond」：
        v 的 颜色值 为 0xFFFFEBCD。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「blueviolet」：
        v 的 颜色值 为 0xFF8A2BE2。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「brown」：
        v 的 颜色值 为 0xFFA52A2A。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「burlywood」：
        v 的 颜色值 为 0xFFDEB887。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「cadetblue」：
        v 的 颜色值 为 0xFF5F9EA0。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「chartreuse」：
        v 的 颜色值 为 0xFF7FFF00。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「chocolate」：
        v 的 颜色值 为 0xFFD2691E。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「coral」：
        v 的 颜色值 为 0xFFFF7F50。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「cornflowerblue」：
        v 的 颜色值 为 0xFF6495ED。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「cornsilk」：
        v 的 颜色值 为 0xFFFFF8DC。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「crimson」：
        v 的 颜色值 为 0xFFDC143C。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「cyan」：
        v 的 颜色值 为 0xFF00FFFF。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkblue」：
        v 的 颜色值 为 0xFF00008B。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkcyan」：
        v 的 颜色值 为 0xFF008B8B。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkgoldenrod」：
        v 的 颜色值 为 0xFFB8860B。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkgray」：
        v 的 颜色值 为 0xFFA9A9A9。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkgreen」：
        v 的 颜色值 为 0xFF006400。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkkhaki」：
        v 的 颜色值 为 0xFFBDB76B。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkm智能体a」：
        v 的 颜色值 为 0xFF8B008B。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkolivegreen」：
        v 的 颜色值 为 0xFF556B2F。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkorange」：
        v 的 颜色值 为 0xFFFF8C00。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkorchid」：
        v 的 颜色值 为 0xFF9932CC。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkred」：
        v 的 颜色值 为 0xFF8B0000。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darksalmon」：
        v 的 颜色值 为 0xFFE9967A。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkseagreen」：
        v 的 颜色值 为 0xFF8FBC8F。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkslateblue」：
        v 的 颜色值 为 0xFF483D8B。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkslategray」：
        v 的 颜色值 为 0xFF2F4F4F。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkturquoise」：
        v 的 颜色值 为 0xFF00CED1。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「darkviolet」：
        v 的 颜色值 为 0xFF9400D3。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「deeppink」：
        v 的 颜色值 为 0xFFFF1493。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「deepskyblue」：
        v 的 颜色值 为 0xFF00BFFF。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「dimgray」：
        v 的 颜色值 为 0xFF696969。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「dodgerblue」：
        v 的 颜色值 为 0xFF1E90FF。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「firebrick」：
        v 的 颜色值 为 0xFFB22222。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「fNova低秩适配lwhite」：
        v 的 颜色值 为 0xFFFFFAF0。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「forestgreen」：
        v 的 颜色值 为 0xFF228B22。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「fuchsia」：
        v 的 颜色值 为 0xFFFF00FF。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「gainsboro」：
        v 的 颜色值 为 0xFFDCDCDC。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「ghostwhite」：
        v 的 颜色值 为 0xFFF8F8FF。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「gold」：
        v 的 颜色值 为 0xFFFFD700。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「goldenrod」：
        v 的 颜色值 为 0xFFDAA520。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「greenyellow」：
        v 的 颜色值 为 0xFFADFF2F。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「honeydew」：
        v 的 颜色值 为 0xFFF0FFF0。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「hotpink」：
        v 的 颜色值 为 0xFFFF69B4。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「indianred」：
        v 的 颜色值 为 0xFFCD5C5C。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「indigo」：
        v 的 颜色值 为 0xFF4B0082。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「ivory」：
        v 的 颜色值 为 0xFFFFFFF0。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「khaki」：
        v 的 颜色值 为 0xFFF0E68C。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lavender」：
        v 的 颜色值 为 0xFFE6E6FA。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lawngreen」：
        v 的 颜色值 为 0xFF7CFC00。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lemonchiffon」：
        v 的 颜色值 为 0xFFFFFACD。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lightblue」：
        v 的 颜色值 为 0xFFADD8E6。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lightcoral」：
        v 的 颜色值 为 0xFFF08080。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lightcyan」：
        v 的 颜色值 为 0xFFE0FFFF。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lightgray」：
        v 的 颜色值 为 0xFFD3D3D3。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lightgreen」：
        v 的 颜色值 为 0xFF90EE90。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lightpink」：
        v 的 颜色值 为 0xFFFFB6C1。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lightsalmon」：
        v 的 颜色值 为 0xFFFFA07A。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lightseagreen」：
        v 的 颜色值 为 0xFF20B2AA。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lightskyblue」：
        v 的 颜色值 为 0xFF87CEFA。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lightslategray」：
        v 的 颜色值 为 0xFF778899。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lightsteelblue」：
        v 的 颜色值 为 0xFFB0C4DE。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lightyellow」：
        v 的 颜色值 为 0xFFFFFFE0。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「lime」：
        v 的 颜色值 为 0xFF00FF00。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「limegreen」：
        v 的 颜色值 为 0xFF32CD32。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「linen」：
        v 的 颜色值 为 0xFFFAF0E6。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「m智能体a」：
        v 的 颜色值 为 0xFFFF00FF。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「maroon」：
        v 的 颜色值 为 0xFF800000。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「mediumaquamarine」：
        v 的 颜色值 为 0xFF66CDAA。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「mediumblue」：
        v 的 颜色值 为 0xFF0000CD。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「mediumorchid」：
        v 的 颜色值 为 0xFFBA55D3。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「mediumpurple」：
        v 的 颜色值 为 0xFF9370DB。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「mediumseagreen」：
        v 的 颜色值 为 0xFF3CB371。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「mediumslateblue」：
        v 的 颜色值 为 0xFF7B68EE。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「mediumspringgreen」：
        v 的 颜色值 为 0xFF00FA9A。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「mediumturquoise」：
        v 的 颜色值 为 0xFF48D1CC。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「mediumvioletred」：
        v 的 颜色值 为 0xFFC71585。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「midnightblue」：
        v 的 颜色值 为 0xFF191970。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「mintcream」：
        v 的 颜色值 为 0xFFF5FFFA。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「mistyrose」：
        v 的 颜色值 为 0xFFFFE4E1。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「moccasin」：
        v 的 颜色值 为 0xFFFFE4B5。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「navajowhite」：
        v 的 颜色值 为 0xFFFFDEAD。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「navy」：
        v 的 颜色值 为 0xFF000080。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「oldlace」：
        v 的 颜色值 为 0xFFFDF5E6。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「olive」：
        v 的 颜色值 为 0xFF808000。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「olivedrab」：
        v 的 颜色值 为 0xFF6B8E23。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「orangered」：
        v 的 颜色值 为 0xFFFF4500。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「orchid」：
        v 的 颜色值 为 0xFFDA70D6。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「palegoldenrod」：
        v 的 颜色值 为 0xFFEEE8AA。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「palegreen」：
        v 的 颜色值 为 0xFF98FB98。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「paleturquoise」：
        v 的 颜色值 为 0xFFAFEEEE。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「palevioletred」：
        v 的 颜色值 为 0xFFDB7093。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「papayawhip」：
        v 的 颜色值 为 0xFFFFEFD5。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「peachpuff」：
        v 的 颜色值 为 0xFFFFDAB9。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「peru」：
        v 的 颜色值 为 0xFFCD853F。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「plum」：
        v 的 颜色值 为 0xFFDDA0DD。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「powderblue」：
        v 的 颜色值 为 0xFFB0E0E6。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「rebeccapurple」：
        v 的 颜色值 为 0xFF663399。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「rosybrown」：
        v 的 颜色值 为 0xFFBC8F8F。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「royalblue」：
        v 的 颜色值 为 0xFF4169E1。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「saddlebrown」：
        v 的 颜色值 为 0xFF8B4513。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「salmon」：
        v 的 颜色值 为 0xFFFA8072。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「sandybrown」：
        v 的 颜色值 为 0xFFF4A460。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「seagreen」：
        v 的 颜色值 为 0xFF2E8B57。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「sea终端」：
        v 的 颜色值 为 0xFFFFF5EE。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「sienna」：
        v 的 颜色值 为 0xFFA0522D。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「silver」：
        v 的 颜色值 为 0xFFC0C0C0。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「skyblue」：
        v 的 颜色值 为 0xFF87CEEB。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「slateblue」：
        v 的 颜色值 为 0xFF6A5ACD。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「slategray」：
        v 的 颜色值 为 0xFF708090。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「snow」：
        v 的 颜色值 为 0xFFFFFAFA。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「springgreen」：
        v 的 颜色值 为 0xFF00FF7F。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「steelblue」：
        v 的 颜色值 为 0xFF4682B4。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「tan」：
        v 的 颜色值 为 0xFFD2B48C。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「teal」：
        v 的 颜色值 为 0xFF008080。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「thistle」：
        v 的 颜色值 为 0xFFD8BFD8。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「tomato」：
        v 的 颜色值 为 0xFFFF6347。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「turquoise」：
        v 的 颜色值 为 0xFF40E0D0。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「violet」：
        v 的 颜色值 为 0xFFEE82EE。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「wheat」：
        v 的 颜色值 为 0xFFF5DEB3。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「whitesmoke」：
        v 的 颜色值 为 0xFFF5F5F5。
        返还 v。
    完。
    若 做 字符串_等于N 用 名称、名称长度、「yellowgreen」：
        v 的 颜色值 为 0xFF9ACD32。
        返还 v。
    完。
    返还 0。
完。

状态_数据 为 0。

状态_RCDATA 为 1。

状态_RAWTEXT 为 2。

状态_脚本数据 为 3。

状态_PLAINTEXT 为 4。

状态_标签打开 为 5。

状态_结束标签打开 为 6。

状态_标签名 为 7。

状态_RCDATA小于号 为 8。

状态_RCDATA结束标签打开 为 9。

状态_RCDATA结束标签名 为 10。

状态_RAWTEXT小于号 为 11。

状态_RAWTEXT结束标签打开 为 12。

状态_RAWTEXT结束标签名 为 13。

状态_脚本小于号 为 14。

状态_脚本结束标签打开 为 15。

状态_脚本结束标签名 为 16。

状态_脚本转义开始 为 17。

状态_脚本转义 为 18。

状态_脚本转义破折号 为 19。

状态_脚本双转义开始 为 20。

状态_脚本双转义 为 21。

状态_CDATA段 为 22。

HTML令牌izer_匹配RAWTEXT结束标签 接受 tk：
    返还 做 HTML令牌izer_匹配RCDATA结束标签 用 tk。
完。

HTML令牌izer_匹配RCDATA结束标签 接受 tk：
    若 tk 的 临时缓冲 等于 空 或者 tk 的 最后开始标签 等于 空：
        返还 假。
    完。
    缓冲名 为 做 字符串_转小写 用 tk 的 临时缓冲。
    期望名 为 做 字符串_转小写 用 tk 的 最后开始标签。
    返还 缓冲名 等于 期望名。
完。

HTML令牌izer_切换到RCDATA 接受 tk：
    tk 的 状态 为 状态_RCDATA。
    tk 的 返回状态 为 状态_数据。
    若 tk 的 AI启用：
        做 智能_令牌状态_AI通知 用 tk、「RCDATA」、tk 的 最后开始标签。
    完。
完。

HTML令牌izer_切换到RAWTEXT 接受 tk：
    tk 的 状态 为 状态_RAWTEXT。
    tk 的 返回状态 为 状态_数据。
    若 tk 的 AI启用：
        做 智能_令牌状态_AI通知 用 tk、「RAWTEXT」、tk 的 最后开始标签。
    完。
完。

HTML令牌izer_切换到脚本数据 接受 tk：
    tk 的 状态 为 状态_脚本数据。
    tk 的 返回状态 为 状态_数据。
    若 tk 的 AI启用：
        做 智能_令牌状态_AI通知 用 tk、「脚本数据」、tk 的 最后开始标签。
    完。
完。

HTML令牌izer_切换到PLAINTEXT 接受 tk：
    tk 的 状态 为 状态_PLAINTEXT。
    tk 的 返回状态 为 状态_数据。
完。

HTML令牌izer_切换到CDATA 接受 tk：
    tk 的 状态 为 状态_CDATA段。
完。

HTML令牌izer_重置到数据 接受 tk：
    tk 的 状态 为 状态_数据。
    tk 的 临时缓冲 为 「」。
完。

HTML令牌izer_处理RCDATA小于号 接受 tk、字符：
    若 字符 等于 「/」：
        tk 的 临时缓冲 为 「」。
        tk 的 状态 为 状态_RCDATA结束标签打开。
    否则：
        做 HTML令牌izer_发射字符 用 tk、「<」。
        tk 的 状态 为 状态_RCDATA。
        做 HTML令牌izer_重新消费 用 tk、字符。
    完。
完。

HTML令牌izer_处理RCDATA结束标签名 接受 tk、字符：
    代码 为 做 编码 用 字符。
    若 代码 大于或等于 65 并且 代码 小于或等于 90 或者 代码 大于或等于 97 并且 代码 小于或等于 122：
        tk 的 临时缓冲 为 tk 的 临时缓冲 加 字符。
        返还。
    完。
    若 字符 等于 「>」 并且 做 HTML令牌izer_匹配RCDATA结束标签 用 tk：
        做 HTML令牌izer_发射结束标签 用 tk、tk 的 临时缓冲。
        tk 的 状态 为 状态_数据。
        返还。
    完。
    若 (字符 等于 「\t」 或者 字符 等于 「\n」 或者 字符 等于 「 」) 并且 做 HTML令牌izer_匹配RCDATA结束标签 用 tk：
        tk 的 状态 为 状态_结束标签打开。
        返还。
    完。
    做 HTML令牌izer_发射字符 用 tk、「</」 加 tk 的 临时缓冲。
    tk 的 状态 为 状态_RCDATA。
    做 HTML令牌izer_重新消费 用 tk、字符。
完。

HTML令牌izer_处理RAWTEXT小于号 接受 tk、字符：
    若 字符 等于 「/」：
        tk 的 临时缓冲 为 「」。
        tk 的 状态 为 状态_RAWTEXT结束标签打开。
    否则：
        做 HTML令牌izer_发射字符 用 tk、「<」。
        tk 的 状态 为 状态_RAWTEXT。
        做 HTML令牌izer_重新消费 用 tk、字符。
    完。
完。

HTML令牌izer_处理RAWTEXT结束标签名 接受 tk、字符：
    代码 为 做 编码 用 字符。
    若 代码 大于或等于 65 并且 代码 小于或等于 90 或者 代码 大于或等于 97 并且 代码 小于或等于 122：
        tk 的 临时缓冲 为 tk 的 临时缓冲 加 字符。
        返还。
    完。
    若 字符 等于 「>」 并且 做 HTML令牌izer_匹配RAWTEXT结束标签 用 tk：
        做 HTML令牌izer_发射结束标签 用 tk、tk 的 临时缓冲。
        tk 的 状态 为 状态_数据。
        返还。
    完。
    做 HTML令牌izer_发射字符 用 tk、「</」 加 tk 的 临时缓冲。
    tk 的 状态 为 状态_RAWTEXT。
    做 HTML令牌izer_重新消费 用 tk、字符。
完。

HTML令牌izer_处理脚本小于号 接受 tk、字符：
    若 字符 等于 「/」：
        tk 的 临时缓冲 为 「」。
        tk 的 状态 为 状态_脚本结束标签打开。
    否则若 字符 等于 「!」：
        tk 的 状态 为 状态_脚本转义开始。
        做 HTML令牌izer_发射字符 用 tk、「<!」。
    否则：
        做 HTML令牌izer_发射字符 用 tk、「<」。
        tk 的 状态 为 状态_脚本数据。
        做 HTML令牌izer_重新消费 用 tk、字符。
    完。
完。

HTML令牌izer_处理CDATA段 接受 tk、字符：
    若 字符 等于 「]」 并且 tk 的 CDATA右括号数 等于 0：
        tk 的 CDATA右括号数 为 1。
    否则若 字符 等于 「]」 并且 tk 的 CDATA右括号数 等于 1：
        tk 的 CDATA右括号数 为 2。
    否则若 字符 等于 「>」 并且 tk 的 CDATA右括号数 等于 2：
        tk 的 状态 为 状态_数据。
        tk 的 CDATA右括号数 为 0。
        返还。
    否则：
        若 tk 的 CDATA右括号数 大于 0：
            可变 j 为 0。
            只要 j 小于 tk 的 CDATA右括号数：
                做 HTML令牌izer_发射字符 用 tk、「]」。
                j 为 j 加 1。
            完。
            tk 的 CDATA右括号数 为 0。
        完。
        做 HTML令牌izer_发射字符 用 tk、字符。
    完。
完。

HTML令牌izer_状态名称 接受 状态：
    若 状态 等于 状态_数据：
        返还 「数据」。
    完。
    若 状态 等于 状态_RCDATA：
        返还 「RCDATA」。
    完。
    若 状态 等于 状态_RAWTEXT：
        返还 「RAWTEXT」。
    完。
    若 状态 等于 状态_脚本数据：
        返还 「脚本数据」。
    完。
    若 状态 等于 状态_PLAINTEXT：
        返还 「PLAINTEXT」。
    完。
    若 状态 等于 状态_CDATA段：
        返还 「CDATA」。
    完。
    返还 「未知(」 加 做 整数转字符串 用 状态 加 「)」。
完。

HTML令牌izer_发射字符 接受 tk、文本：
    做 追加 用 tk 的 输出队列、一本字典：类型 对应 「字符」、数据 对应 文本。
完。

HTML令牌izer_发射结束标签 接受 tk、名称：
    做 追加 用 tk 的 输出队列、一本字典：类型 对应 「结束标签」、名称 对应 做 字符串_转小写 用 名称。
完。

HTML令牌izer_重新消费 接受 tk、字符：
    tk 的 重新消费 为 真。
    tk 的 重新消费字符 为 字符。
完。

智能_HTML令牌izer_AI节点语义 接受 节点：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 节点 等于 0：
        返还。
    完。
    角色 为 做 AI总线_语义分类 用 bus、节点。
    节点 的 语义角色 为 角色。
完。

智能_HTML令牌izer_AI预加载提示 接受 url、资源类型：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 url 等于 0：
        返还。
    完。
    优先级 为 做 AI总线_资源优先级 用 bus、url。
完。

智能_令牌状态_AI通知 接受 tk、状态名、标签名：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    做 AI总线_令牌状态变更 用 bus、状态名、标签名。
完。

智能_令牌izer_AI预测下一状态 接受 tk：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 负 1。
    完。
    返还 做 AI总线_预测状态 用 bus、tk 的 状态、tk 的 最后开始标签。
完。

HTML令牌izer_处理_属性值无引号 接受 tk、令牌、c：
    若 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0C 或者 c 等于 0x20：
        做 HTML令牌izer_提交属性 用 tk、令牌。
        tk 的 状态 为 状态_属性名前。
    否则若 c 等于 0x3E：
        做 HTML令牌izer_提交属性 用 tk、令牌。
        做 HTML令牌izer_完成标签名 用 令牌。
        若 令牌 的 类型 等于 令牌类型_开始标签：
            做 HTML令牌izer_记录开始标签 用 tk、令牌。
        完。
        tk 的 状态 为 状态_数据。
    否则若 c 等于 0x26：
        tk 的 返回状态 为 状态_属性值_无引号。
        tk 的 状态 为 状态_字符引用。
    否则：
        做 HTML令牌izer_追加属性值字符 用 tk、c。
    完。
完。

HTML令牌izer_处理_属性值后 接受 tk、令牌、c：
    若 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0C 或者 c 等于 0x20：
        tk 的 状态 为 状态_属性名前。
    否则若 c 等于 0x2F：
        tk 的 状态 为 状态_自闭合开始标签。
    否则若 c 等于 0x3E：
        做 HTML令牌izer_完成标签名 用 令牌。
        若 令牌 的 类型 等于 令牌类型_开始标签：
            做 HTML令牌izer_记录开始标签 用 tk、令牌。
        完。
        tk 的 状态 为 状态_数据。
    否则：
        tk 的 位置 为 tk 的 位置 减 1。
        tk 的 状态 为 状态_属性名前。
    完。
完。

HTML令牌izer_处理_自闭合 接受 tk、令牌、c：
    若 c 等于 0x3E：
        令牌 的 自闭合 为 真。
        做 HTML令牌izer_完成标签名 用 令牌。
        若 令牌 的 类型 等于 令牌类型_开始标签：
            做 HTML令牌izer_记录开始标签 用 tk、令牌。
        完。
        tk 的 状态 为 状态_数据。
    否则：
        tk 的 位置 为 tk 的 位置 减 1。
        tk 的 状态 为 状态_属性名前。
    完。
完。

HTML令牌izer_处理_标记声明开始 接受 tk、令牌、c：
    若 c 等于 0x2D：
        下一个 为 做 HTML令牌izer_窥视字符 用 tk。
        若 下一个 等于 0x2D：
            做 HTML令牌izer_消费字符 用 tk。
            令牌 的 数据长度 为 0。
            tk 的 状态 为 状态_注释开始。
            返还。
        完。
    完。
    若 做 HTML令牌izer_匹配不区分大小写 用 tk、「OCTYPE」：
        tk 的 状态 为 状态_DOCTYPE。
        返还。
    完。
    tk 的 状态 为 状态_伪注释。
完。

HTML令牌izer_处理_注释开始 接受 tk、令牌、c：
    若 c 等于 0x2D：
        tk 的 状态 为 状态_注释开始破折号。
    否则若 c 等于 0x3E：
        令牌 的 类型 为 令牌类型_注释。
        tk 的 状态 为 状态_数据。
    否则：
        做 HTML令牌izer_追加数据 用 令牌、c。
        tk 的 状态 为 状态_注释。
    完。
完。

HTML令牌izer_处理_注释 接受 tk、令牌、c：
    若 c 等于 0x2D：
        tk 的 状态 为 状态_注释结束破折号。
    否则：
        做 HTML令牌izer_追加数据 用 令牌、c。
    完。
完。

HTML令牌izer_处理_注释结束破折号 接受 tk、令牌、c：
    若 c 等于 0x2D：
        tk 的 状态 为 状态_注释结束。
    否则：
        做 HTML令牌izer_追加数据 用 令牌、0x2D。
        做 HTML令牌izer_追加数据 用 令牌、c。
        tk 的 状态 为 状态_注释。
    完。
完。

HTML令牌izer_处理_注释结束 接受 tk、令牌、c：
    若 c 等于 0x3E：
        令牌 的 类型 为 令牌类型_注释。
        tk 的 状态 为 状态_数据。
    否则若 c 等于 0x2D：
        做 HTML令牌izer_追加数据 用 令牌、0x2D。
    否则：
        做 HTML令牌izer_追加数据 用 令牌、0x2D。
        做 HTML令牌izer_追加数据 用 令牌、0x2D。
        做 HTML令牌izer_追加数据 用 令牌、c。
        tk 的 状态 为 状态_注释。
    完。
完。

本章 收束。
