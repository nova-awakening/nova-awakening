《内联布局文》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·创建销毁》。

引用《.·文档节点》。

引用《.·绝对位置计》。

引用《.·核心布局算》。

引用《..·中枢·字符串替换》。

布局Inline_布局 接受 lo：
    可变 子 为 lo 的 第一子。
    可变 行内X 为 0。
    只要 子 不等于 0：
        做 布局Object_布局 用 子。
        子 的 x 为 行内X。
        子 的 y 为 0。
        行内X 为 行内X 加 做 布局Object_边框盒宽 用 子。
        子 为 子 的 后兄。
    完。
    lo 的 内容宽 为 行内X。
    若 lo 的 样式 不等于 0 并且 lo 的 样式 的 line_height 大于 0：
        lo 的 内容高 为 lo 的 样式 的 line_height。
    否则若 lo 的 样式 不等于 0：
        lo 的 内容高 为 lo 的 样式 的 font_size 乘 120 除以 100。
    完。
完。

布局Text_布局 接受 lo：
    若 lo 的 DOM节点 等于 0：
        返还。
    完。
    文本 为 做 CharacterData_data 用 lo 的 DOM节点。
    若 文本 等于 0：
        返还。
    完。
    文本长度 为 做 字符串_长度 用 文本。
    可变 字号 为 1600。
    若 lo 的 样式 不等于 0：
        字号 为 lo 的 样式 的 font_size。
    完。
    字符宽 为 字号 乘 60 除以 100。
    lo 的 内容宽 为 文本长度 乘 字符宽。
    若 lo 的 父 不等于 0 并且 lo 的 内容宽 大于 lo 的 父 的 内容宽 并且 lo 的 父 的 内容宽 大于 0：
        行宽 为 lo 的 父 的 内容宽。
        行数 为 (lo 的 内容宽 加 行宽 减 1) 除以 行宽。
        lo 的 内容宽 为 行宽。
        行高 为 字号 乘 120 除以 100。
        若 lo 的 样式 不等于 0 并且 lo 的 样式 的 line_height 大于 0：
            行高 为 lo 的 样式 的 line_height。
        完。
        lo 的 内容高 为 行数 乘 行高。
    否则：
        行高 为 字号 乘 120 除以 100。
        若 lo 的 样式 不等于 0 并且 lo 的 样式 的 line_height 大于 0：
            行高 为 lo 的 样式 的 line_height。
        完。
        lo 的 内容高 为 行高。
    完。
完。

布局Table_布局 接受 lo：
    可变 列数 为 0。
    可变 行数 为 0。
    可变 行 为 lo 的 第一子。
    若 行 不等于 0：
        可变 单元 为 行 的 第一子。
        只要 单元 不等于 0：
            列数 为 列数 加 1。
            单元 为 单元 的 后兄。
        完。
    完。
    若 列数 等于 0：
        列数 为 1。
    完。
    列宽 为 lo 的 内容宽 除以 列数。
    可变 当前Y 为 0。
    行 为 lo 的 第一子。
    只要 行 不等于 0：
        行数 为 行数 加 1。
        可变 行高 为 0。
        可变 列索引 为 0。
        可变 单元 为 行 的 第一子。
        只要 单元 不等于 0：
            单元 的 内容宽 为 列宽。
            做 布局Object_布局 用 单元。
            单元 的 x 为 列索引 乘 列宽。
            单元 的 y 为 0。
            若 单元 的 内容高 大于 行高：
                行高 为 单元 的 内容高。
            完。
            列索引 为 列索引 加 1。
            单元 为 单元 的 后兄。
        完。
        行 的 x 为 0。
        行 的 y 为 当前Y。
        行 的 内容宽 为 lo 的 内容宽。
        行 的 内容高 为 行高。
        当前Y 为 当前Y 加 行高。
        行 为 行 的 后兄。
    完。
    lo 的 内容高 为 当前Y。
    做 布局Object_计算绝对位置 用 lo。
完。

本章 收束。
