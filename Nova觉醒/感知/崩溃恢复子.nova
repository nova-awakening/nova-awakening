《崩溃恢复子》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·IPC通道》。

引用《.·内核直连层》。

引用《.·沙箱》。

引用《..·执行·平台时间》。

引用《..·中枢·内存初始化》。

引用《..·推理·AI总线资源调度》。

引用《..·推理·总线预测》。

引用《..·推理·AI鼠标事件处理》。

进程管理器_处理崩溃 接受 pm、崩溃PID：
    可变 i 为 0。
    只要 i 小于 pm 的 进程数：
        若 pm 的 进程表 的 第 i 项 的 PID 等于 崩溃PID：
            pm 的 进程表 的 第 i 项 的 状态 为 2。
            pm 的 进程表 的 第 i 项 的 崩溃次数 为 pm 的 进程表 的 第 i 项 的 崩溃次数 加 1。
            若 pm 的 进程表 的 第 i 项 的 类型 等于 进程类型_渲染器：
                若 pm 的 进程表 的 第 i 项 的 崩溃次数 小于 3：
                    做 进程管理器_重启渲染器 用 pm、i。
                完。
            否则若 pm 的 进程表 的 第 i 项 的 类型 等于 进程类型_图处：
                pm 的 图处进程 为 0。
                做 进程管理器_启动图处 用 pm。
            否则若 pm 的 进程表 的 第 i 项 的 类型 等于 进程类型_网络：
                pm 的 网络进程 为 0。
                做 进程管理器_启动网络 用 pm。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

进程管理器_重启渲染器 接受 pm、idx：
    旧站点键 为 pm 的 进程表 的 第 idx 项 的 站点键。
    做 IPC通道_关闭 用 pm 的 进程表 的 第 idx 项 的 IPC通道。
    通道 为 做 IPC通道_创建。
    PID 为 做 系统调用 用 系统调用_进程_创建、进程类型_渲染器、0、0、0、0。
    若 PID 大于 0：
        pm 的 进程表 的 第 idx 项 的 PID 为 PID。
        pm 的 进程表 的 第 idx 项 的 状态 为 0。
        pm 的 进程表 的 第 idx 项 的 IPC通道 为 通道。
        pm 的 进程表 的 第 idx 项 的 站点键 为 旧站点键。
        通道 的 对端PID 为 PID。
        做 沙箱_应用 用 PID、做 沙箱_渲染器策略。
    完。
完。

Nova通信_路由消息 接受 pm、消息：
    若 pm 等于 0 或者 消息 等于 0：
        返还。
    完。
    类型 为 消息 的 类型。
    若 类型 大于或等于 400 并且 类型 小于 500：
        做 Nova通信_处理AI请求 用 pm、消息。
        返还。
    完。
    若 类型 大于或等于 200 并且 类型 小于 300：
        若 pm 的 网络进程 不等于 0：
            网络info 为 pm 的 网络进程。
            做 IPC通道_发送 用 网络info 的 IPC通道、类型、消息 的 数据、消息 的 大小 减 16。
        完。
        返还。
    完。
    若 类型 大于或等于 300 并且 类型 小于 400：
        若 pm 的 图处进程 不等于 0：
            图处info 为 pm 的 图处进程。
            做 IPC通道_发送 用 图处info 的 IPC通道、类型、消息 的 数据、消息 的 大小 减 16。
        完。
        返还。
    完。
完。

Nova通信_处理AI请求 接受 pm、消息：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    类型 为 消息 的 类型。
    发送者 为 消息 的 发送者PID。
    回复通道 为 做 Nova通信_查找通道 用 pm、发送者。
    若 回复通道 等于 0：
        返还。
    完。
    若 类型 等于 Nova通信_请求_AI语义分类：
        特征数据 为 消息 的 数据。
        角色 为 5。
        结果 为 做 堆_分配_数组 用 4。
        做 内存_写32 用 结果、角色。
        做 IPC通道_发送 用 回复通道、Nova通信_响应_AI语义分类、结果、4。
        做 堆_释放 用 结果。
    否则若 类型 等于 Nova通信_请求_AI渲染质量：
        x 为 做 内存_读32 用 消息 的 数据。
        y 为 做 内存_读32 用 消息 的 数据 加 4。
        w 为 做 内存_读32 用 消息 的 数据 加 8。
        h 为 做 内存_读32 用 消息 的 数据 加 12。
        语义 为 做 内存_读32 用 消息 的 数据 加 16。
        级别 为 做 AI总线_渲染质量 用 bus、x、y、w、h、语义、真。
        结果 为 做 堆_分配_数组 用 4。
        做 内存_写32 用 结果、级别。
        做 IPC通道_发送 用 回复通道、Nova通信_响应_AI渲染质量、结果、4。
        做 堆_释放 用 结果。
    否则若 类型 等于 Nova通信_请求_AI资源优先级：
        url 为 消息 的 数据。
        优先级 为 做 AI总线_资源优先级 用 bus、url。
        结果 为 做 堆_分配_数组 用 4。
        做 内存_写32 用 结果、优先级。
        做 IPC通道_发送 用 回复通道、Nova通信_响应_AI资源优先级、结果、4。
        做 堆_释放 用 结果。
    否则若 类型 等于 Nova通信_通知_AI行为事件：
        事件类型 为 做 内存_读32 用 消息 的 数据。
        x 为 做 内存_读32 用 消息 的 数据 加 4。
        y 为 做 内存_读32 用 消息 的 数据 加 8。
        做 AI总线_感知_鼠标移动 用 bus、x、y。
    完。
完。

Nova通信_查找通道 接受 pm、PID：
    可变 i 为 0。
    只要 i 小于 pm 的 进程数：
        若 pm 的 进程表 的 第 i 项 的 PID 等于 PID：
            返还 pm 的 进程表 的 第 i 项 的 IPC通道。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

本章 收束。
