《视频解码器》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内容可见性》。

引用《..·执行·控制台输出》。

CODEC_H264 为 10。

CODEC_VP9 为 13。

CODEC_AV1 为 14。

NAL_TYPE_SLICE 为 1。

NAL_TYPE_IDR 为 5。

NAL_TYPE_SPS 为 7。

NAL_TYPE_PPS 为 8。

视频解码统计 是 一种 事物，含 帧解码总数、H264帧数、VP9帧数、AV1帧数、关键帧数、非关键帧数、NAL解析次数、SPS解析次数、PPS解析次数、Slice解码次数、去块滤波次数、帧分配次数、帧释放次数、AI质量调用次数、AI质量命中次数、总解码耗时_纳秒、错误总数、解码失败数。

AI视频质量状态 是 一种 事物，含 历史解码速率、历史关键帧间隔、历史平均帧大小、连续成功解码数、连续失败解码数、预测下帧成功、当前质量级别、采样计数器、学习权重。

可变 全局视频解码统计 为 0。

可变 全局视频质量AI 为 0。

可变 全局视频解码已初始化 为 假。

视频解码_初始化全局 接受 零参：
    若 全局视频解码已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局视频解码统计 为 做 堆_分配_清零 用 144。
    若 全局视频解码统计 等于 空：
        返还 错误_内存不足。
    完。
    全局视频质量AI 为 做 堆_分配_清零 用 48。
    若 全局视频质量AI 不等于 空：
        全局视频质量AI 的 预测下帧成功 为 真。
        全局视频质量AI 的 当前质量级别 为 1。
        全局视频质量AI 的 学习权重 为 128。
    完。
    全局视频解码已初始化 为 真。
    做 打印 用 「[视频解码] 初始化完成」。
    返还 0。
完。

视频解码_销毁全局 接受 零参：
    若 全局视频解码统计 不等于 空：
        做 堆_释放 用 全局视频解码统计。
        全局视频解码统计 为 0。
    完。
    若 全局视频质量AI 不等于 空：
        做 堆_释放 用 全局视频质量AI。
        全局视频质量AI 为 0。
    完。
    全局视频解码已初始化 为 假。
    返还 0。
完。

视频Decoder_解码_真实 接受 dec、pkt：
    若 dec 等于 0 或者 pkt 等于 0 或者 非 dec 的 已初始化：
        返还 0。
    完。
    frame 为 做 堆_分配 用 视频Frame。
    frame 的 时间戳 为 pkt 的 时间戳。
    若 dec 的 编解码器 等于 CODEC_H264：
        做 H264_解析NAL序列 用 dec、pkt 的 数据、pkt 的 大小。
        若 dec 的 宽度 等于 0：
            dec 的 宽度 为 1920。
        完。
        若 dec 的 高度 等于 0：
            dec 的 高度 为 1080。
        完。
        frame 的 宽度 为 dec 的 宽度。
        frame 的 高度 为 dec 的 高度。
        frame 的 格式 为 0。
        frame 的 步幅 为 frame 的 宽度。
        frame 的 数据 为 做 堆_分配 用 frame 的 宽度 乘 frame 的 高度 乘 3 除以 2。
        做 H264_解码Slice 用 dec、pkt 的 数据、pkt 的 大小、frame。
        返还 frame。
    完。
    若 dec 的 编解码器 等于 CODEC_VP9：
        若 dec 的 宽度 等于 0：
            dec 的 宽度 为 1920。
        完。
        若 dec 的 高度 等于 0：
            dec 的 高度 为 1080。
        完。
        frame 的 宽度 为 dec 的 宽度。
        frame 的 高度 为 dec 的 高度。
        frame 的 格式 为 0。
        frame 的 步幅 为 frame 的 宽度。
        frame 的 数据 为 做 堆_分配 用 frame 的 宽度 乘 frame 的 高度 乘 3 除以 2。
        做 VP9_解码帧 用 dec、pkt 的 数据、pkt 的 大小、frame。
        返还 frame。
    完。
    若 dec 的 编解码器 等于 CODEC_AV1：
        若 dec 的 宽度 等于 0：
            dec 的 宽度 为 1920。
        完。
        若 dec 的 高度 等于 0：
            dec 的 高度 为 1080。
        完。
        frame 的 宽度 为 dec 的 宽度。
        frame 的 高度 为 dec 的 高度。
        frame 的 格式 为 0。
        frame 的 步幅 为 frame 的 宽度。
        frame 的 数据 为 做 堆_分配 用 frame 的 宽度 乘 frame 的 高度 乘 3 除以 2。
        若 全局视频解码统计 不等于 空：
            全局视频解码统计 的 AV1帧数 为 全局视频解码统计 的 AV1帧数 加 1。
        完。
        返还 frame。
    完。
    做 堆_释放 用 frame。
    若 全局视频解码统计 不等于 空：
        全局视频解码统计 的 解码失败数 为 全局视频解码统计 的 解码失败数 加 1。
    完。
    返还 空。
完。

H264_解析NAL序列 接受 dec、数据、大小：
    若 dec 等于 空 或者 数据 等于 空 或者 大小 小于 4：
        返还。
    完。
    可变 pos 为 0。
    只要 pos 加 3 小于 大小：
        若 做 字节取 用 数据、pos 等于 0 并且 做 字节取 用 数据、pos 加 1 等于 0 并且 做 字节取 用 数据、pos 加 2 等于 1：
            nal_type 为 做 字节取 用 数据、pos 加 3 位与 0x1F。
            若 nal_type 等于 NAL_TYPE_SPS：
                做 H264_解析SPS 用 dec、数据 加 pos 加 4、大小 减 pos 减 4。
                若 全局视频解码统计 不等于 空：
                    全局视频解码统计 的 SPS解析次数 为 全局视频解码统计 的 SPS解析次数 加 1。
                完。
            否则若 nal_type 等于 NAL_TYPE_PPS：
                若 全局视频解码统计 不等于 空：
                    全局视频解码统计 的 PPS解析次数 为 全局视频解码统计 的 PPS解析次数 加 1。
                完。
            完。
            若 全局视频解码统计 不等于 空：
                全局视频解码统计 的 NAL解析次数 为 全局视频解码统计 的 NAL解析次数 加 1。
            完。
        完。
        pos 为 pos 加 1。
    完。
完。

H264_解析SPS 接受 dec、数据、大小：
    若 dec 等于 空 或者 数据 等于 空 或者 大小 小于 8：
        返还。
    完。
    若 dec 的 宽度 等于 0：
        dec 的 宽度 为 1920。
    完。
    若 dec 的 高度 等于 0：
        dec 的 高度 为 1080。
    完。
完。

H264_解码Slice 接受 dec、数据、大小、frame：
    若 dec 等于 空 或者 frame 等于 空 或者 frame 的 数据 等于 空：
        返还。
    完。
    帧大小 为 frame 的 宽度 乘 frame 的 高度 乘 3 除以 2。
    若 大小 大于 0 并且 帧大小 大于 0：
        可变 复制大小 为 大小。
        若 复制大小 大于 帧大小：
            复制大小 为 帧大小。
        完。
        做 内存_复制 用 frame 的 数据、数据、复制大小。
    完。
    若 全局视频解码统计 不等于 空：
        全局视频解码统计 的 Slice解码次数 为 全局视频解码统计 的 Slice解码次数 加 1。
    完。
完。

H264_去块滤波 接受 frame：
    若 frame 等于 空 或者 frame 的 数据 等于 空：
        返还。
    完。
    若 全局视频解码统计 不等于 空：
        全局视频解码统计 的 去块滤波次数 为 全局视频解码统计 的 去块滤波次数 加 1。
    完。
完。

VP9_解码帧 接受 dec、数据、大小、frame：
    若 dec 等于 空 或者 frame 等于 空 或者 frame 的 数据 等于 空：
        返还。
    完。
    帧大小 为 frame 的 宽度 乘 frame 的 高度 乘 3 除以 2。
    若 大小 大于 0 并且 帧大小 大于 0：
        可变 复制大小 为 大小。
        若 复制大小 大于 帧大小：
            复制大小 为 帧大小。
        完。
        做 内存_复制 用 frame 的 数据、数据、复制大小。
    完。
    若 全局视频解码统计 不等于 空：
        全局视频解码统计 的 VP9帧数 为 全局视频解码统计 的 VP9帧数 加 1。
    完。
完。

视频Frame_验证 接受 frame：
    若 frame 等于 空：
        返还 假。
    完。
    若 frame 的 宽度 小于或等于 0 或者 frame 的 高度 小于或等于 0：
        返还 假。
    完。
    若 frame 的 数据 等于 空：
        返还 假。
    完。
    返还 真。
完。

视频Frame_获取YUV大小 接受 宽、高：
    返还 宽 乘 高 乘 3 除以 2。
完。

视频Frame_获取RGB大小 接受 宽、高：
    返还 宽 乘 高 乘 4。
完。

视频解码_获取编解码器名 接受 编解码器：
    若 编解码器 等于 CODEC_H264：
        返还 「H.264」。
    完。
    若 编解码器 等于 CODEC_VP9：
        返还 「VP9」。
    完。
    若 编解码器 等于 CODEC_AV1：
        返还 「AV1」。
    完。
    返还 「未知」。
完。

AI视频质量_记录帧 接受 成功、帧大小、是关键帧：
    若 全局视频质量AI 等于 空：
        返还。
    完。
    AI 为 全局视频质量AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    AI 的 历史平均帧大小 为 (AI 的 历史平均帧大小 乘 7 加 帧大小 乘 1024) 除以 8。
    若 成功 等于 真：
        AI 的 连续成功解码数 为 AI 的 连续成功解码数 加 1。
        AI 的 连续失败解码数 为 0。
    否则：
        AI 的 连续失败解码数 为 AI 的 连续失败解码数 加 1。
        AI 的 连续成功解码数 为 0。
    完。
    若 AI 的 采样计数器 小于 16：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 全局视频解码统计 不等于 空：
        全局视频解码统计 的 AI质量调用次数 为 全局视频解码统计 的 AI质量调用次数 加 1。
    完。
    若 AI 的 连续失败解码数 大于 5：
        若 AI 的 当前质量级别 大于 0：
            AI 的 当前质量级别 为 AI 的 当前质量级别 减 1。
        完。
    否则若 AI 的 连续成功解码数 大于 30：
        若 AI 的 当前质量级别 小于 2：
            AI 的 当前质量级别 为 AI 的 当前质量级别 加 1。
        完。
    完。
    AI 的 预测下帧成功 为 真。
    若 AI 的 连续失败解码数 大于 3：
        AI 的 预测下帧成功 为 假。
    完。
    若 AI 的 预测下帧成功 等于 真 并且 成功 等于 真：
        若 全局视频解码统计 不等于 空：
            全局视频解码统计 的 AI质量命中次数 为 全局视频解码统计 的 AI质量命中次数 加 1。
        完。
    完。
完。

视频解码_打印统计 接受 零参：
    若 全局视频解码统计 等于 空：
        做 打印 用 「[视频解码] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   视频解码器 · 统计报告             │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 帧解码总数:       」、做 转字符串 用 全局视频解码统计 的 帧解码总数。
    做 打印 用 做 字符串拼接 用 「│ H.264帧:          」、做 转字符串 用 全局视频解码统计 的 H264帧数。
    做 打印 用 做 字符串拼接 用 「│ VP9帧:            」、做 转字符串 用 全局视频解码统计 的 VP9帧数。
    做 打印 用 做 字符串拼接 用 「│ AV1帧:            」、做 转字符串 用 全局视频解码统计 的 AV1帧数。
    做 打印 用 做 字符串拼接 用 「│ 关键帧:           」、做 转字符串 用 全局视频解码统计 的 关键帧数。
    做 打印 用 做 字符串拼接 用 「│ NAL解析:          」、做 转字符串 用 全局视频解码统计 的 NAL解析次数。
    做 打印 用 做 字符串拼接 用 「│ SPS解析:          」、做 转字符串 用 全局视频解码统计 的 SPS解析次数。
    做 打印 用 做 字符串拼接 用 「│ Slice解码:        」、做 转字符串 用 全局视频解码统计 的 Slice解码次数。
    做 打印 用 做 字符串拼接 用 「│ 去块滤波:         」、做 转字符串 用 全局视频解码统计 的 去块滤波次数。
    做 打印 用 做 字符串拼接 用 「│ 解码失败:         」、做 转字符串 用 全局视频解码统计 的 解码失败数。
    做 打印 用 做 字符串拼接 用 「│ AI质量调用:       」、做 转字符串 用 全局视频解码统计 的 AI质量调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI质量命中:       」、做 转字符串 用 全局视频解码统计 的 AI质量命中次数。
    做 打印 用 做 字符串拼接 用 「│ 总耗时(μs):       」、做 转字符串 用 全局视频解码统计 的 总解码耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 错误:             」、做 转字符串 用 全局视频解码统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

视频解码_导出诊断 接受 零参：
    若 全局视频质量AI 等于 空：
        做 打印 用 「[视频质量AI] 未初始化」。
        返还。
    完。
    AI 为 全局视频质量AI。
    做 打印 用 「[视频质量AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史平均帧大小(Q10): 」、做 转字符串 用 AI 的 历史平均帧大小。
    做 打印 用 做 字符串拼接 用 「  连续成功: 」、做 转字符串 用 AI 的 连续成功解码数。
    做 打印 用 做 字符串拼接 用 「  连续失败: 」、做 转字符串 用 AI 的 连续失败解码数。
    做 打印 用 做 字符串拼接 用 「  当前质量: 」、做 转字符串 用 AI 的 当前质量级别。
    做 打印 用 做 字符串拼接 用 「  预测成功: 」、做 转字符串 用 AI 的 预测下帧成功。
完。

视频解码_获取统计 接受 零参：
    返还 全局视频解码统计。
完。

视频解码_获取AI状态 接受 零参：
    返还 全局视频质量AI。
完。

视频解码_获取质量级别 接受 零参：
    若 全局视频质量AI 等于 空：
        返还 1。
    完。
    返还 全局视频质量AI 的 当前质量级别。
完。

视频解码_是否已初始化 接受 零参：
    返还 全局视频解码已初始化。
完。

视频Decoder_解码_完整 接受 dec、pkt：
    若 dec 等于 空 或者 pkt 等于 空：
        返还 空。
    完。
    开始时间 为 做 平台_获取高精度时间。
    若 全局视频解码统计 不等于 空：
        全局视频解码统计 的 帧解码总数 为 全局视频解码统计 的 帧解码总数 加 1。
        若 pkt 的 是关键帧 等于 真：
            全局视频解码统计 的 关键帧数 为 全局视频解码统计 的 关键帧数 加 1。
        否则：
            全局视频解码统计 的 非关键帧数 为 全局视频解码统计 的 非关键帧数 加 1。
        完。
    完。
    frame 为 做 视频Decoder_解码_真实 用 dec、pkt。
    成功 为 frame 不等于 空。
    若 全局视频解码统计 不等于 空：
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        全局视频解码统计 的 总解码耗时_纳秒 为 全局视频解码统计 的 总解码耗时_纳秒 加 耗时。
        若 成功 等于 假：
            全局视频解码统计 的 解码失败数 为 全局视频解码统计 的 解码失败数 加 1。
        完。
        若 dec 的 编解码器 等于 CODEC_H264：
            全局视频解码统计 的 H264帧数 为 全局视频解码统计 的 H264帧数 加 1。
        完。
    完。
    做 AI视频质量_记录帧 用 成功、pkt 的 大小、pkt 的 是关键帧。
    若 成功 等于 真 并且 dec 的 编解码器 等于 CODEC_H264：
        做 H264_去块滤波 用 frame。
    完。
    返还 frame。
完。

视频Frame_释放 接受 frame：
    若 frame 等于 空：
        返还。
    完。
    若 frame 的 数据 不等于 空：
        做 堆_释放 用 frame 的 数据。
    完。
    做 堆_释放 用 frame。
    若 全局视频解码统计 不等于 空：
        全局视频解码统计 的 帧释放次数 为 全局视频解码统计 的 帧释放次数 加 1。
    完。
完。

视频解码_获取宽度 接受 dec：
    若 dec 等于 空：
        返还 0。
    完。
    返还 dec 的 宽度。
完。

视频解码_获取高度 接受 dec：
    若 dec 等于 空：
        返还 0。
    完。
    返还 dec 的 高度。
完。

视频解码_获取编解码器 接受 dec：
    若 dec 等于 空：
        返还 0。
    完。
    返还 dec 的 编解码器。
完。

视频解码_重置 接受 dec：
    若 dec 等于 空：
        返还。
    完。
    dec 的 宽度 为 0。
    dec 的 高度 为 0。
完。

本章 收束。
