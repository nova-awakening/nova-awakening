《CSS单位与颜色解析》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·CSS令牌化》。

引用《.·内核直连层》。

引用《..·执行·底层内存操》。

CSSParser_解析单位 接受 单位、长度：
    若 做 字符串_等于N 用 单位、长度、「px」：
        返还 单位_px。
    完。
    若 做 字符串_等于N 用 单位、长度、「em」：
        返还 单位_em。
    完。
    若 做 字符串_等于N 用 单位、长度、「rem」：
        返还 单位_rem。
    完。
    若 做 字符串_等于N 用 单位、长度、「vh」：
        返还 单位_vh。
    完。
    若 做 字符串_等于N 用 单位、长度、「vw」：
        返还 单位_vw。
    完。
    若 做 字符串_等于N 用 单位、长度、「pt」：
        返还 单位_pt。
    完。
    返还 单位_px。
完。

CSSParser_解析十六进制颜色 接受 值、长度：
    可变 r 为 0。
    可变 g 为 0。
    可变 b 为 0。
    可变 a 为 0xFF。
    若 长度 等于 3：
        r 为 做 CSSParser_十六进制字符 用 做 内存_读8 用 值。
        r 为 r 乘 16 加 r。
        g 为 做 CSSParser_十六进制字符 用 做 内存_读8 用 值 加 1。
        g 为 g 乘 16 加 g。
        b 为 做 CSSParser_十六进制字符 用 做 内存_读8 用 值 加 2。
        b 为 b 乘 16 加 b。
    否则若 长度 大于或等于 6：
        r 为 做 CSSParser_十六进制字符 用 做 内存_读8 用 值 乘 16 加 做 CSSParser_十六进制字符 用 做 内存_读8 用 值 加 1。
        g 为 做 CSSParser_十六进制字符 用 做 内存_读8 用 值 加 2 乘 16 加 做 CSSParser_十六进制字符 用 做 内存_读8 用 值 加 3。
        b 为 做 CSSParser_十六进制字符 用 做 内存_读8 用 值 加 4 乘 16 加 做 CSSParser_十六进制字符 用 做 内存_读8 用 值 加 5。
    完。
    若 长度 等于 8：
        a 为 做 CSSParser_十六进制字符 用 做 内存_读8 用 值 加 6 乘 16 加 做 CSSParser_十六进制字符 用 做 内存_读8 用 值 加 7。
    完。
    返还 r 乘 0x1000000 加 g 乘 0x10000 加 b 乘 0x100 加 a。
完。

CSSParser_十六进制字符 接受 c：
    若 c 大于或等于 0x30 并且 c 小于或等于 0x39：
        返还 c 减 0x30。
    完。
    若 c 大于或等于 0x41 并且 c 小于或等于 0x46：
        返还 c 减 0x41 加 10。
    完。
    若 c 大于或等于 0x61 并且 c 小于或等于 0x66：
        返还 c 减 0x61 加 10。
    完。
    返还 0。
完。

CSSParser_命名颜色 接受 名、长度：
    若 做 字符串_等于N 用 名、长度、「black」：
        返还 0x000000FF。
    完。
    若 做 字符串_等于N 用 名、长度、「white」：
        返还 0xFFFFFFFF。
    完。
    若 做 字符串_等于N 用 名、长度、「red」：
        返还 0xFF0000FF。
    完。
    若 做 字符串_等于N 用 名、长度、「green」：
        返还 0x008000FF。
    完。
    若 做 字符串_等于N 用 名、长度、「blue」：
        返还 0x0000FFFF。
    完。
    若 做 字符串_等于N 用 名、长度、「yellow」：
        返还 0xFFFF00FF。
    完。
    若 做 字符串_等于N 用 名、长度、「orange」：
        返还 0xFFA500FF。
    完。
    若 做 字符串_等于N 用 名、长度、「purple」：
        返还 0x800080FF。
    完。
    若 做 字符串_等于N 用 名、长度、「gray」：
        返还 0x808080FF。
    完。
    若 做 字符串_等于N 用 名、长度、「grey」：
        返还 0x808080FF。
    完。
    若 做 字符串_等于N 用 名、长度、「transparent」：
        返还 0x00000000。
    完。
    返还 0。
完。

CSSParser_关键字转ID 接受 名、长度：
    若 做 字符串_等于N 用 名、长度、「inherit」：
        返还 1。
    完。
    若 做 字符串_等于N 用 名、长度、「initial」：
        返还 2。
    完。
    若 做 字符串_等于N 用 名、长度、「unset」：
        返还 3。
    完。
    若 做 字符串_等于N 用 名、长度、「auto」：
        返还 4。
    完。
    若 做 字符串_等于N 用 名、长度、「none」：
        返还 5。
    完。
    若 做 字符串_等于N 用 名、长度、「block」：
        返还 6。
    完。
    若 做 字符串_等于N 用 名、长度、「inline」：
        返还 7。
    完。
    若 做 字符串_等于N 用 名、长度、「inline-block」：
        返还 8。
    完。
    若 做 字符串_等于N 用 名、长度、「flex」：
        返还 9。
    完。
    若 做 字符串_等于N 用 名、长度、「grid」：
        返还 10。
    完。
    若 做 字符串_等于N 用 名、长度、「hidden」：
        返还 11。
    完。
    若 做 字符串_等于N 用 名、长度、「visible」：
        返还 12。
    完。
    若 做 字符串_等于N 用 名、长度、「relative」：
        返还 13。
    完。
    若 做 字符串_等于N 用 名、长度、「absolute」：
        返还 14。
    完。
    若 做 字符串_等于N 用 名、长度、「fixed」：
        返还 15。
    完。
    若 做 字符串_等于N 用 名、长度、「sticky」：
        返还 16。
    完。
    若 做 字符串_等于N 用 名、长度、「static」：
        返还 17。
    完。
    若 做 字符串_等于N 用 名、长度、「center」：
        返还 18。
    完。
    若 做 字符串_等于N 用 名、长度、「left」：
        返还 19。
    完。
    若 做 字符串_等于N 用 名、长度、「right」：
        返还 20。
    完。
    若 做 字符串_等于N 用 名、长度、「bold」：
        返还 21。
    完。
    若 做 字符串_等于N 用 名、长度、「normal」：
        返还 22。
    完。
    若 做 字符串_等于N 用 名、长度、「row」：
        返还 23。
    完。
    若 做 字符串_等于N 用 名、长度、「column」：
        返还 24。
    完。
    若 做 字符串_等于N 用 名、长度、「wrap」：
        返还 25。
    完。
    若 做 字符串_等于N 用 名、长度、「nowrap」：
        返还 26。
    完。
    若 做 字符串_等于N 用 名、长度、「space-between」：
        返还 27。
    完。
    若 做 字符串_等于N 用 名、长度、「space-around」：
        返还 28。
    完。
    若 做 字符串_等于N 用 名、长度、「stretch」：
        返还 29。
    完。
    若 做 字符串_等于N 用 名、长度、「pointer」：
        返还 30。
    完。
    若 做 字符串_等于N 用 名、长度、「border-box」：
        返还 31。
    完。
    若 做 字符串_等于N 用 名、长度、「content-box」：
        返还 32。
    完。
    若 做 字符串_等于N 用 名、长度、「scroll」：
        返还 33。
    完。
    若 做 字符串_等于N 用 名、长度、「collapse」：
        返还 34。
    完。
    若 做 字符串_等于N 用 名、长度、「separate」：
        返还 35。
    完。
    若 做 字符串_等于N 用 名、长度、「both」：
        返还 36。
    完。
    若 做 字符串_等于N 用 名、长度、「top」：
        返还 37。
    完。
    若 做 字符串_等于N 用 名、长度、「bottom」：
        返还 38。
    完。
    若 做 字符串_等于N 用 名、长度、「middle」：
        返还 39。
    完。
    若 做 字符串_等于N 用 名、长度、「baseline」：
        返还 40。
    完。
    若 做 字符串_等于N 用 名、长度、「underline」：
        返还 41。
    完。
    若 做 字符串_等于N 用 名、长度、「overline」：
        返还 42。
    完。
    若 做 字符串_等于N 用 名、长度、「line-through」：
        返还 43。
    完。
    若 做 字符串_等于N 用 名、长度、「uppercase」：
        返还 44。
    完。
    若 做 字符串_等于N 用 名、长度、「lowercase」：
        返还 45。
    完。
    若 做 字符串_等于N 用 名、长度、「capitalize」：
        返还 46。
    完。
    若 做 字符串_等于N 用 名、长度、「italic」：
        返还 47。
    完。
    若 做 字符串_等于N 用 名、长度、「oblique」：
        返还 48。
    完。
    若 做 字符串_等于N 用 名、长度、「pre」：
        返还 49。
    完。
    若 做 字符串_等于N 用 名、长度、「pre-wrap」：
        返还 50。
    完。
    若 做 字符串_等于N 用 名、长度、「pre-line」：
        返还 51。
    完。
    若 做 字符串_等于N 用 名、长度、「break-word」：
        返还 52。
    完。
    若 做 字符串_等于N 用 名、长度、「break-all」：
        返还 53。
    完。
    若 做 字符串_等于N 用 名、长度、「keep-all」：
        返还 54。
    完。
    若 做 字符串_等于N 用 名、长度、「solid」：
        返还 55。
    完。
    若 做 字符串_等于N 用 名、长度、「dashed」：
        返还 56。
    完。
    若 做 字符串_等于N 用 名、长度、「dotted」：
        返还 57。
    完。
    若 做 字符串_等于N 用 名、长度、「double」：
        返还 58。
    完。
    若 做 字符串_等于N 用 名、长度、「groove」：
        返还 59。
    完。
    若 做 字符串_等于N 用 名、长度、「ridge」：
        返还 60。
    完。
    若 做 字符串_等于N 用 名、长度、「inset」：
        返还 61。
    完。
    若 做 字符串_等于N 用 名、长度、「outset」：
        返还 62。
    完。
    若 做 字符串_等于N 用 名、长度、「thin」：
        返还 63。
    完。
    若 做 字符串_等于N 用 名、长度、「thick」：
        返还 64。
    完。
    若 做 字符串_等于N 用 名、长度、「row-reverse」：
        返还 65。
    完。
    若 做 字符串_等于N 用 名、长度、「column-reverse」：
        返还 66。
    完。
    若 做 字符串_等于N 用 名、长度、「wrap-reverse」：
        返还 67。
    完。
    若 做 字符串_等于N 用 名、长度、「space-evenly」：
        返还 68。
    完。
    若 做 字符串_等于N 用 名、长度、「flex-start」：
        返还 69。
    完。
    若 做 字符串_等于N 用 名、长度、「flex-end」：
        返还 70。
    完。
    若 做 字符串_等于N 用 名、长度、「start」：
        返还 71。
    完。
    若 做 字符串_等于N 用 名、长度、「end」：
        返还 72。
    完。
    若 做 字符串_等于N 用 名、长度、「self-start」：
        返还 73。
    完。
    若 做 字符串_等于N 用 名、长度、「self-end」：
        返还 74。
    完。
    若 做 字符串_等于N 用 名、长度、「cover」：
        返还 75。
    完。
    若 做 字符串_等于N 用 名、长度、「contain2」：
        返还 76。
    完。
    若 做 字符串_等于N 用 名、长度、「fill」：
        返还 77。
    完。
    若 做 字符串_等于N 用 名、长度、「scale-down」：
        返还 78。
    完。
    若 做 字符串_等于N 用 名、长度、「smooth」：
        返还 79。
    完。
    若 做 字符串_等于N 用 名、长度、「no-repeat」：
        返还 80。
    完。
    若 做 字符串_等于N 用 名、长度、「repeat-x」：
        返还 81。
    完。
    若 做 字符串_等于N 用 名、长度、「repeat-y」：
        返还 82。
    完。
    若 做 字符串_等于N 用 名、长度、「repeat」：
        返还 83。
    完。
    若 做 字符串_等于N 用 名、长度、「ellipsis」：
        返还 84。
    完。
    若 做 字符串_等于N 用 名、长度、「clip」：
        返还 85。
    完。
    若 做 字符串_等于N 用 名、长度、「table-cell」：
        返还 86。
    完。
    若 做 字符串_等于N 用 名、长度、「table-row」：
        返还 87。
    完。
    若 做 字符串_等于N 用 名、长度、「list-item」：
        返还 88。
    完。
    若 做 字符串_等于N 用 名、长度、「contents」：
        返还 89。
    完。
    若 做 字符串_等于N 用 名、长度、「inline-flex」：
        返还 90。
    完。
    若 做 字符串_等于N 用 名、长度、「inline-grid」：
        返还 91。
    完。
    若 做 字符串_等于N 用 名、长度、「table」：
        返还 92。
    完。
    返还 0。
完。

CSSParser_计算特异度 接受 选择器：
    若 选择器 等于 0：
        返还 0。
    完。
    可变 a 为 0。
    可变 b 为 0。
    可变 c 为 0。
    可变 当前 为 选择器。
    只要 当前 不等于 0：
        若 当前 的 类型 等于 选择器类型_id：
            a 为 a 加 1。
        否则若 当前 的 类型 等于 选择器类型_class 或者 当前 的 类型 等于 选择器类型_属性 或者 当前 的 类型 等于 选择器类型_伪类：
            b 为 b 加 1。
        否则若 当前 的 类型 等于 选择器类型_标签 或者 当前 的 类型 等于 选择器类型_伪元素：
            c 为 c 加 1。
        完。
        当前 为 当前 的 下一个。
    完。
    返还 a 乘 10000 加 b 乘 100 加 c。
完。

CSSParser_跳过AT规则 接受 tk、令牌：
    内部令牌 为 做 堆_分配 用 CSS令牌。
    可变 大括号深度 为 0。
    只要 1：
        做 CSS令牌izer_下一个令牌 用 tk、内部令牌。
        若 内部令牌 的 类型 等于 CSS_令牌_EOF：
            返还。
        完。
        若 内部令牌 的 类型 等于 CSS_令牌_左大括号：
            大括号深度 为 大括号深度 加 1。
        完。
        若 内部令牌 的 类型 等于 CSS_令牌_右大括号：
            若 大括号深度 等于 0：
                返还。
            完。
            大括号深度 为 大括号深度 减 1。
            若 大括号深度 等于 0：
                返还。
            完。
        完。
        若 内部令牌 的 类型 等于 CSS_令牌_分号 并且 大括号深度 等于 0：
            返还。
        完。
    完。
    可变 AI_样式解析甲_样式缓存命中 为 0。
    AI_样式解析甲_选择器复杂度 接受 选择器长度、组合子数：
        若 组合子数 大于 5：
            返还 「高复杂度」。
        完。
        若 组合子数 大于 2：
            返还 「中复杂度」。
        完。
        返还 「低复杂度」。
    完。
    AI_样式解析甲_布局缓存 接受 元素哈希：
        AI_样式解析甲_样式缓存命中 为 AI_样式解析甲_样式缓存命中 加 1。
        返还 元素哈希。
    完。
完。

本章 收束。

本章 收束。

本章 收束。
