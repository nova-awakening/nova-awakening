《工线定时器》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·辅助存根》。

引用《..·中枢·内容可见性》。

引用《.·消息端》。

NWTS_版本 为 1。

定时器_类型_单次 为 0。

定时器_类型_间隔 为 1。

定时器_类型_动画帧 为 2。

定时器_类型_空闲 为 3。

定时器_状态_活跃 为 0。

定时器_状态_暂停 为 1。

定时器_状态_已取消 为 2。

定时器_状态_已触发 为 3。

最小延迟_嵌套 为 4。

最大定时器数 为 4096。

时间轮_毫秒槽数 为 256。

时间轮_秒槽数 为 64。

时间轮_分槽数 为 64。

漂移阈值 为 16。

嵌套深度阈值 为 5。

WORKLET_PAINT 为 0。

WORKLET_AUDIO 为 1。

WORKLET_ANIMATION 为 2。

WORKLET_LAYOUT 为 3。

最大WORKLET模块 为 8。

可变 NWTS_创建计数 为 0。

可变 NWTS_触发计数 为 0。

可变 NWTS_取消计数 为 0。

可变 NWTS_漂移补偿计数 为 0。

可变 NWTS_最大漂移 为 0。

可变 NWTS_总漂移累计 为 0。

可变 NWTS_消息循环计数 为 0。

可变 NWTS_批合并计数 为 0。

可变 NWTS_AI调度计数 为 0。

可变 NWTS_错误计数 为 0。

工线Timer 是 一种 事物，含 ID、回调、延迟、实际延迟、类型、状态、下次触发、创建时间、触发次数、嵌套深度、累计漂移、下一个、时间轮下一。

时间轮 是 一种 事物，含 毫秒槽、秒槽、分槽、当前毫秒、当前秒、当前分、基准时间、tick计数。

工线GlobalScope 是 一种 事物，含 定时器ID、定时器头、定时器数、时间轮、端口、是关闭、名称、安全来源、消息队列头、消息队列尾、消息数、动画帧回调、空闲回调、空闲截止。

工线消息 是 一种 事物，含 数据、大小、来源、转移对象、转移数、下一个。

Worklet 是 一种 事物，含 类型、模块、模块数、已准备、注册表、注册数。

时间轮_创建 接受 零参：
    tw 为 做 堆_分配 用 时间轮。
    可变 i 为 0。
    只要 i 小于 时间轮_毫秒槽数：
        tw 的 毫秒槽 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 时间轮_秒槽数：
        tw 的 秒槽 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 时间轮_分槽数：
        tw 的 分槽 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    tw 的 当前毫秒 为 0。
    tw 的 当前秒 为 0。
    tw 的 当前分 为 0。
    tw 的 基准时间 为 做 平台_获取高精度时间。
    tw 的 tick计数 为 0。
    返还 tw。
完。

时间轮_插入 接受 tw、timer：
    若 tw 等于 0 或者 timer 等于 0：
        返还。
    完。
    delta 为 timer 的 下次触发 减 tw 的 基准时间 减 tw 的 tick计数。
    若 delta 小于 0：
        delta 为 0。
    完。
    若 delta 小于 时间轮_毫秒槽数：
        槽 为 (tw 的 当前毫秒 加 delta) 取余于 时间轮_毫秒槽数。
        timer 的 时间轮下一 为 tw 的 毫秒槽 的 第 槽 项。
        tw 的 毫秒槽 的 第 槽 项 为 timer。
    否则若 delta 小于 时间轮_毫秒槽数 乘 时间轮_秒槽数：
        槽 为 (tw 的 当前秒 加 delta 除以 时间轮_毫秒槽数) 取余于 时间轮_秒槽数。
        timer 的 时间轮下一 为 tw 的 秒槽 的 第 槽 项。
        tw 的 秒槽 的 第 槽 项 为 timer。
    否则：
        槽 为 (tw 的 当前分 加 delta 除以 (时间轮_毫秒槽数 乘 时间轮_秒槽数)) 取余于 时间轮_分槽数。
        timer 的 时间轮下一 为 tw 的 分槽 的 第 槽 项。
        tw 的 分槽 的 第 槽 项 为 timer。
    完。
完。

时间轮_降级 接受 源槽、目标tw：
    可变 t 为 源槽。
    只要 t 不等于 0：
        下一 为 t 的 时间轮下一。
        做 时间轮_插入 用 目标tw、t。
        t 为 下一。
    完。
完。

时间轮_tick 接受 tw：
    若 tw 等于 0：
        返还 0。
    完。
    tw 的 tick计数 为 tw 的 tick计数 加 1。
    就绪 为 tw 的 毫秒槽 的 第 tw 的 当前毫秒 项。
    tw 的 毫秒槽 的 第 tw 的 当前毫秒 项 为 0。
    tw 的 当前毫秒 为 (tw 的 当前毫秒 加 1) 取余于 时间轮_毫秒槽数。
    若 tw 的 当前毫秒 等于 0：
        做 时间轮_降级 用 tw 的 秒槽 的 第 tw 的 当前秒 项、tw。
        tw 的 秒槽 的 第 tw 的 当前秒 项 为 0。
        tw 的 当前秒 为 (tw 的 当前秒 加 1) 取余于 时间轮_秒槽数。
        若 tw 的 当前秒 等于 0：
            做 时间轮_降级 用 tw 的 分槽 的 第 tw 的 当前分 项、tw。
            tw 的 分槽 的 第 tw 的 当前分 项 为 0。
            tw 的 当前分 为 (tw 的 当前分 加 1) 取余于 时间轮_分槽数。
        完。
    完。
    返还 就绪。
完。

工线GlobalScope_创建 接受 名称、来源：
    gs 为 做 堆_分配 用 工线GlobalScope。
    gs 的 定时器ID 为 0。
    gs 的 定时器头 为 0。
    gs 的 定时器数 为 0。
    gs 的 时间轮 为 做 时间轮_创建。
    gs 的 端口 为 0。
    gs 的 是关闭 为 假。
    gs 的 名称 为 名称。
    gs 的 安全来源 为 来源。
    gs 的 消息队列头 为 0。
    gs 的 消息队列尾 为 0。
    gs 的 消息数 为 0。
    gs 的 动画帧回调 为 0。
    gs 的 空闲回调 为 0。
    gs 的 空闲截止 为 0。
    返还 gs。
完。

NWTS_计算实际延迟 接受 请求延迟、嵌套深度：
    实际 为 请求延迟。
    若 嵌套深度 大于 嵌套深度阈值 并且 实际 小于 最小延迟_嵌套：
        实际 为 最小延迟_嵌套。
    完。
    返还 实际。
完。

工线Scope_setTimeout 接受 gs、回调、延迟：
    若 gs 等于 0 或者 回调 等于 0：
        返还 0。
    完。
    若 gs 的 是关闭：
        返还 0。
    完。
    若 gs 的 定时器数 大于或等于 最大定时器数：
        NWTS_错误计数 为 NWTS_错误计数 加 1。
        返还 0。
    完。
    NWTS_创建计数 为 NWTS_创建计数 加 1。
    gs 的 定时器ID 为 gs 的 定时器ID 加 1。
    实际延迟 为 做 NWTS_计算实际延迟 用 延迟、0。
    t 为 做 堆_分配 用 工线Timer。
    t 的 ID 为 gs 的 定时器ID。
    t 的 回调 为 回调。
    t 的 延迟 为 延迟。
    t 的 实际延迟 为 实际延迟。
    t 的 类型 为 定时器_类型_单次。
    t 的 状态 为 定时器_状态_活跃。
    t 的 创建时间 为 做 平台_获取高精度时间。
    t 的 下次触发 为 t 的 创建时间 加 实际延迟。
    t 的 触发次数 为 0。
    t 的 嵌套深度 为 0。
    t 的 累计漂移 为 0。
    t 的 下一个 为 gs 的 定时器头。
    gs 的 定时器头 为 t。
    gs 的 定时器数 为 gs 的 定时器数 加 1。
    做 时间轮_插入 用 gs 的 时间轮、t。
    返还 t 的 ID。
完。

工线Scope_setInterval 接受 gs、回调、延迟：
    若 gs 等于 0 或者 回调 等于 0：
        返还 0。
    完。
    若 gs 的 是关闭：
        返还 0。
    完。
    若 gs 的 定时器数 大于或等于 最大定时器数：
        NWTS_错误计数 为 NWTS_错误计数 加 1。
        返还 0。
    完。
    NWTS_创建计数 为 NWTS_创建计数 加 1。
    gs 的 定时器ID 为 gs 的 定时器ID 加 1。
    实际延迟 为 做 NWTS_计算实际延迟 用 延迟、0。
    若 实际延迟 小于 10：
        实际延迟 为 10。
    完。
    t 为 做 堆_分配 用 工线Timer。
    t 的 ID 为 gs 的 定时器ID。
    t 的 回调 为 回调。
    t 的 延迟 为 延迟。
    t 的 实际延迟 为 实际延迟。
    t 的 类型 为 定时器_类型_间隔。
    t 的 状态 为 定时器_状态_活跃。
    t 的 创建时间 为 做 平台_获取高精度时间。
    t 的 下次触发 为 t 的 创建时间 加 实际延迟。
    t 的 触发次数 为 0。
    t 的 嵌套深度 为 0。
    t 的 累计漂移 为 0。
    t 的 下一个 为 gs 的 定时器头。
    gs 的 定时器头 为 t。
    gs 的 定时器数 为 gs 的 定时器数 加 1。
    做 时间轮_插入 用 gs 的 时间轮、t。
    返还 t 的 ID。
完。

工线Scope_clearTimeout 接受 gs、id：
    若 gs 等于 0：
        返还。
    完。
    NWTS_取消计数 为 NWTS_取消计数 加 1。
    可变 prev 为 0。
    可变 t 为 gs 的 定时器头。
    只要 t 不等于 0：
        若 t 的 ID 等于 id：
            t 的 状态 为 定时器_状态_已取消。
            若 prev 等于 0：
                gs 的 定时器头 为 t 的 下一个。
            否则：
                prev 的 下一个 为 t 的 下一个。
            完。
            gs 的 定时器数 为 gs 的 定时器数 减 1。
            做 堆_释放 用 t。
            返还。
        完。
        prev 为 t。
        t 为 t 的 下一个。
    完。
完。

工线Scope_clearInterval 接受 gs、id：
    做 工线Scope_clearTimeout 用 gs、id。
完。

工线Scope_requestAnimationFrame 接受 gs、回调：
    若 gs 等于 0 或者 回调 等于 0 或者 gs 的 是关闭：
        返还 0。
    完。
    NWTS_创建计数 为 NWTS_创建计数 加 1。
    gs 的 定时器ID 为 gs 的 定时器ID 加 1。
    t 为 做 堆_分配 用 工线Timer。
    t 的 ID 为 gs 的 定时器ID。
    t 的 回调 为 回调。
    t 的 延迟 为 16。
    t 的 实际延迟 为 16。
    t 的 类型 为 定时器_类型_动画帧。
    t 的 状态 为 定时器_状态_活跃。
    t 的 创建时间 为 做 平台_获取高精度时间。
    t 的 下次触发 为 t 的 创建时间 加 16。
    t 的 触发次数 为 0。
    t 的 嵌套深度 为 0。
    t 的 累计漂移 为 0。
    t 的 下一个 为 gs 的 定时器头。
    gs 的 定时器头 为 t。
    gs 的 定时器数 为 gs 的 定时器数 加 1。
    做 时间轮_插入 用 gs 的 时间轮、t。
    返还 t 的 ID。
完。

工线Scope_requestIdleCallback 接受 gs、回调、超时：
    若 gs 等于 0 或者 回调 等于 0 或者 gs 的 是关闭：
        返还 0。
    完。
    NWTS_创建计数 为 NWTS_创建计数 加 1。
    gs 的 定时器ID 为 gs 的 定时器ID 加 1。
    t 为 做 堆_分配 用 工线Timer。
    t 的 ID 为 gs 的 定时器ID。
    t 的 回调 为 回调。
    t 的 延迟 为 50。
    t 的 实际延迟 为 50。
    t 的 类型 为 定时器_类型_空闲。
    t 的 状态 为 定时器_状态_活跃。
    t 的 创建时间 为 做 平台_获取高精度时间。
    若 超时 大于 0：
        gs 的 空闲截止 为 t 的 创建时间 加 超时。
    否则：
        gs 的 空闲截止 为 0。
    完。
    t 的 下次触发 为 t 的 创建时间 加 50。
    t 的 触发次数 为 0。
    t 的 嵌套深度 为 0。
    t 的 累计漂移 为 0。
    t 的 下一个 为 gs 的 定时器头。
    gs 的 定时器头 为 t。
    gs 的 定时器数 为 gs 的 定时器数 加 1。
    做 时间轮_插入 用 gs 的 时间轮、t。
    返还 t 的 ID。
完。

NWTS_触发定时器 接受 gs、timer：
    若 gs 等于 0 或者 timer 等于 0：
        返还。
    完。
    若 timer 的 状态 不等于 定时器_状态_活跃：
        返还。
    完。
    NWTS_触发计数 为 NWTS_触发计数 加 1。
    timer 的 触发次数 为 timer 的 触发次数 加 1。
    现在 为 做 平台_获取高精度时间。
    漂移 为 现在 减 timer 的 下次触发。
    若 漂移 小于 0：
        漂移 为 0 减 漂移。
    完。
    timer 的 累计漂移 为 timer 的 累计漂移 加 漂移。
    NWTS_总漂移累计 为 NWTS_总漂移累计 加 漂移。
    若 漂移 大于 NWTS_最大漂移：
        NWTS_最大漂移 为 漂移。
    完。
    若 timer 的 回调 不等于 0：
        若 timer 的 类型 等于 定时器_类型_动画帧：
            做 Nova回调_调用_1参 用 timer 的 回调、现在。
        否则：
            做 Nova回调_调用 用 timer 的 回调。
        完。
    完。
    若 timer 的 类型 等于 定时器_类型_间隔：
        目标 为 timer 的 下次触发 加 timer 的 实际延迟。
        若 漂移 大于 漂移阈值：
            NWTS_漂移补偿计数 为 NWTS_漂移补偿计数 加 1。
            目标 为 现在 加 timer 的 实际延迟 减 漂移。
            若 目标 小于或等于 现在：
                目标 为 现在 加 1。
            完。
        完。
        timer 的 下次触发 为 目标。
        timer 的 嵌套深度 为 timer 的 嵌套深度 加 1。
        做 时间轮_插入 用 gs 的 时间轮、timer。
    否则：
        timer 的 状态 为 定时器_状态_已触发。
        做 工线Scope_clearTimeout 用 gs、timer 的 ID。
    完。
完。

工线_消息循环 接受 gs：
    若 gs 等于 0 或者 gs 的 是关闭：
        返还。
    完。
    NWTS_消息循环计数 为 NWTS_消息循环计数 加 1。
    现在 为 做 平台_获取高精度时间。
    可变 就绪 为 做 时间轮_tick 用 gs 的 时间轮。
    只要 就绪 不等于 0：
        下一 为 就绪 的 时间轮下一。
        做 NWTS_触发定时器 用 gs、就绪。
        就绪 为 下一。
    完。
    可变 t 为 gs 的 定时器头。
    只要 t 不等于 0：
        下一 为 t 的 下一个。
        若 t 的 状态 等于 定时器_状态_活跃 并且 现在 大于或等于 t 的 下次触发：
            做 NWTS_触发定时器 用 gs、t。
        完。
        t 为 下一。
    完。
    可变 消息数 为 0。
    只要 gs 的 消息队列头 不等于 0 并且 消息数 小于 64：
        msg 为 gs 的 消息队列头。
        gs 的 消息队列头 为 msg 的 下一个。
        若 gs 的 消息队列头 等于 0：
            gs 的 消息队列尾 为 0。
        完。
        gs 的 消息数 为 gs 的 消息数 减 1。
        若 gs 的 端口 不等于 0：
            做 MessagePort_派发消息 用 gs 的 端口。
        完。
        做 堆_释放 用 msg。
        消息数 为 消息数 加 1。
    完。
完。

工线_投递消息 接受 gs、数据、大小、来源：
    若 gs 等于 0 或者 gs 的 是关闭：
        返还 假。
    完。
    msg 为 做 堆_分配 用 工线消息。
    msg 的 数据 为 数据。
    msg 的 大小 为 大小。
    msg 的 来源 为 来源。
    msg 的 转移对象 为 0。
    msg 的 转移数 为 0。
    msg 的 下一个 为 0。
    若 gs 的 消息队列尾 不等于 0：
        gs 的 消息队列尾 的 下一个 为 msg。
    否则：
        gs 的 消息队列头 为 msg。
    完。
    gs 的 消息队列尾 为 msg。
    gs 的 消息数 为 gs 的 消息数 加 1。
    返还 真。
完。

Worklet_创建 接受 类型：
    w 为 做 堆_分配 用 Worklet。
    w 的 类型 为 类型。
    w 的 模块数 为 0。
    w 的 已准备 为 假。
    w 的 注册表 为 0。
    w 的 注册数 为 0。
    返还 w。
完。

Worklet_add模块 接受 w、url：
    若 w 等于 0 或者 url 等于 0 或者 w 的 模块数 大于或等于 最大WORKLET模块：
        返还 假。
    完。
    w 的 模块 的 第 w 的 模块数 项 为 做 字符串_复制 用 url。
    w 的 模块数 为 w 的 模块数 加 1。
    返还 真。
完。

绘制Worklet_register绘制 接受 名称、类class：
    若 名称 等于 0：
        返还 假。
    完。
    返还 真。
完。

音频Worklet_register处理器 接受 名称、处理器class：
    若 名称 等于 0：
        返还 假。
    完。
    返还 真。
完。

动画Worklet_register动画器 接受 名称、动画器class：
    若 名称 等于 0：
        返还 假。
    完。
    返还 真。
完。

布局Worklet_register布局 接受 名称、布局class：
    若 名称 等于 0：
        返还 假。
    完。
    返还 真。
完。

智能_工线定时器_AI初始化 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    t 为 做 Tensor_创建 用 8。
    做 Tensor_写 用 t、0、NWTS_版本。
    做 Tensor_写 用 t、1、NWTS_创建计数。
    做 Tensor_写 用 t、2、NWTS_触发计数。
    做 Tensor_写 用 t、3、NWTS_取消计数。
    做 Tensor_写 用 t、4、NWTS_漂移补偿计数。
    做 Tensor_写 用 t、5、NWTS_最大漂移。
    做 Tensor_写 用 t、6、NWTS_消息循环计数。
    做 Tensor_写 用 t、7、NWTS_批合并计数。
    做 AI总线_推理 用 bus、「NWTS_模块注册」、t。
    做 Tensor_释放 用 t。
完。

NWTS_AI批量合并评估 接受 gs：
    NWTS_AI调度计数 为 NWTS_AI调度计数 加 1。
    若 gs 等于 0 或者 gs 的 定时器数 小于 2：
        返还 0。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    t 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 t、0、gs 的 定时器数。
    做 Tensor_写 用 t、1、NWTS_最大漂移。
    做 Tensor_写 用 t、2、NWTS_触发计数。
    做 Tensor_写 用 t、3、NWTS_消息循环计数。
    结果 为 做 AI总线_推理 用 bus、「NWTS_批量合并」、t。
    做 Tensor_释放 用 t。
    若 结果 大于 0：
        NWTS_批合并计数 为 NWTS_批合并计数 加 1。
    完。
    返还 结果。
完。

NWTS_AI异常检测 接受 gs：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    t 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 t、0、NWTS_错误计数。
    做 Tensor_写 用 t、1、NWTS_最大漂移。
    做 Tensor_写 用 t、2、gs 的 定时器数。
    做 Tensor_写 用 t、3、NWTS_取消计数。
    风险 为 做 AI总线_风险评分 用 bus、「NWTS_异常」、t。
    做 Tensor_释放 用 t。
    返还 风险。
完。

NWTS_获取统计 接受 零参：
    s 为 做 堆_分配_数组 用 80。
    做 内存_写64 用 s 加 0、NWTS_创建计数。
    做 内存_写64 用 s 加 8、NWTS_触发计数。
    做 内存_写64 用 s 加 16、NWTS_取消计数。
    做 内存_写64 用 s 加 24、NWTS_漂移补偿计数。
    做 内存_写64 用 s 加 32、NWTS_最大漂移。
    做 内存_写64 用 s 加 40、NWTS_总漂移累计。
    做 内存_写64 用 s 加 48、NWTS_消息循环计数。
    做 内存_写64 用 s 加 56、NWTS_批合并计数。
    做 内存_写64 用 s 加 64、NWTS_AI调度计数。
    返还 s。
完。

NWTS_重置统计 接受 零参：
    NWTS_创建计数 为 0。
    NWTS_触发计数 为 0。
    NWTS_取消计数 为 0。
    NWTS_漂移补偿计数 为 0。
    NWTS_最大漂移 为 0。
    NWTS_总漂移累计 为 0。
    NWTS_消息循环计数 为 0。
    NWTS_批合并计数 为 0。
    NWTS_AI调度计数 为 0。
    NWTS_错误计数 为 0。
完。

NWTS_诊断打印 接受 零参：
    做 内核打印 用 「[NWTS] 创建=」、NWTS_创建计数、「 触发=」、NWTS_触发计数、「 取消=」、NWTS_取消计数。
    做 内核打印 用 「[NWTS] 漂移补偿=」、NWTS_漂移补偿计数、「 最大漂移=」、NWTS_最大漂移、「ms」。
    若 NWTS_触发计数 大于 0：
        平均漂移 为 NWTS_总漂移累计 除以 NWTS_触发计数。
        做 内核打印 用 「[NWTS] 平均漂移=」、平均漂移、「ms」。
    完。
    做 内核打印 用 「[NWTS] 消息循环=」、NWTS_消息循环计数、「 批合并=」、NWTS_批合并计数。
    做 内核打印 用 「[NWTS] AI调度=」、NWTS_AI调度计数、「 错误=」、NWTS_错误计数。
完。

本章 收束。
