《AI智能渲染》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·创建销毁》。

引用《..·中枢·内容可见性》。

引用《.·绘制图层绘》。

引用《.·从布局树构》。

AI渲染_智能绘制 接受 层、显示列表、偏移x、偏移y：
    若 层 等于 0：
        返还。
    完。
    若 层 的 标志 位与 层标志_AI可跳过绘制：
        返还。
    完。
    开始 为 做 平台_获取高精度时间。
    若 层 的 渲染精度 等于 1：
        做 AI渲染_粗略绘制 用 层、显示列表、偏移x、偏移y。
    否则：
        做 绘制Layer_绘制 用 层、显示列表、偏移x、偏移y。
    完。
    耗时 为 做 平台_获取高精度时间 减 开始。
    层 的 上次绘制耗时 为 耗时。
    层 的 累计绘制次数 为 层 的 累计绘制次数 加 1。
完。

AI渲染_粗略绘制 接受 层、显示列表、偏移x、偏移y：
    lo 为 层 的 布局对象。
    若 lo 等于 0：
        返还。
    完。
    s 为 lo 的 样式。
    若 s 等于 0：
        返还。
    完。
    绘制x 为 lo 的 绝对x 加 偏移x。
    绘制y 为 lo 的 绝对y 加 偏移y。
    绘制w 为 做 布局Object_边框盒宽 用 lo。
    绘制h 为 做 布局Object_边框盒高 用 lo。
    若 s 的 background_color 不等于 0x00000000：
        做 绘制Layer_添加显示项 用 显示列表、新建 显示项：类型 为 显示项_背景、x 为 绘制x、y 为 绘制y、w 为 绘制w、h 为 绘制h、颜色 为 s 的 background_color。
    完。
完。

AI渲染_设置语义优先级 接受 层：
    若 层 等于 0：
        返还。
    完。
    lo 为 层 的 布局对象。
    若 lo 等于 0 或者 lo 的 DOM节点 等于 0：
        返还。
    完。
    角色 为 lo 的 DOM节点 的 语义角色。
    若 角色 等于 语义_正文 或者 角色 等于 语义_标题：
        层 的 渲染优先级 为 255。
    否则若 角色 等于 语义_视频 或者 角色 等于 语义_弹窗：
        层 的 渲染优先级 为 250。
    否则若 角色 等于 语义_导航栏 或者 角色 等于 语义_搜索框：
        层 的 渲染优先级 为 240。
    否则若 角色 等于 语义_广告：
        层 的 渲染优先级 为 30。
    否则若 角色 等于 语义_Cookie同意：
        层 的 渲染优先级 为 20。
    否则若 角色 等于 语义_页脚：
        层 的 渲染优先级 为 80。
    完。
完。

AI渲染_负载自适应 接受 根层、图处负载百分比：
    若 图处负载百分比 大于 80：
        做 AI渲染_降级低优先级 用 根层、100。
    否则若 图处负载百分比 大于 60：
        做 AI渲染_降级低优先级 用 根层、50。
    否则若 图处负载百分比 小于 40：
        做 AI渲染_恢复精度 用 根层。
    完。
完。

AI渲染_降级低优先级 接受 层、阈值：
    若 层 等于 0：
        返还。
    完。
    若 层 的 渲染优先级 小于 阈值 并且 层 的 渲染精度 大于 1：
        层 的 渲染精度 为 1。
        层 的 标志 为 层 的 标志 位或 层标志_AI低精度绘制。
    完。
    可变 子 为 层 的 第一子层。
    只要 子 不等于 0：
        做 AI渲染_降级低优先级 用 子、阈值。
        子 为 子 的 后兄层。
    完。
完。

AI渲染_恢复精度 接受 层：
    若 层 等于 0：
        返还。
    完。
    若 层 的 标志 位与 层标志_AI低精度绘制：
        层 的 渲染精度 为 2。
        层 的 标志 为 层 的 标志 位与 按位取反 层标志_AI低精度绘制。
    完。
    可变 子 为 层 的 第一子层。
    只要 子 不等于 0：
        做 AI渲染_恢复精度 用 子。
        子 为 子 的 后兄层。
    完。
完。

本章 收束。
