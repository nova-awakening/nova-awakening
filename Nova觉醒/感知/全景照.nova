《全景照》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·日志中枢》。

引用《..·执行·内核界面》。

引用《.·文档节点》。

引用《..·中枢·硬链接》。

引用《..·标准·原生字符串》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·界面元素发现》。

引用《..·执行·浏览器系统》。

引用《..·中枢·整数转字符串》。

界节点 是 一种 事物，含 标识、角色、名称、值、左、上、宽、高、可见、可动、可输、可禁、路径、选择。

全景照 是 一种 事物，含 编号、窗数、焦窗、页号、页题、页址、页类、页名、页源、页摘、节点数、脚本数、样式数、壳缓、壳动、壳果、核态、核副、视觉态、视觉副、声音态、声音副、形象态、形象副、节点列。

可变 照号 为 1。

可变 上次节点数 为 0。

可变 上次窗口数 为 0。

可变 取照数 为 0。

可变 取树数 为 0。

可变 找点数 为 0。

截串 接受 文本、上限：
    若 文本 等于 空：
        返还 「」。
    完。
    若 做 字符串长度 用 文本 小于或等于 上限：
        返还 文本。
    完。
    返还 做 子字符串 用 文本、0、上限。
完。

空照 接受 零参：
    照 为 新建 全景照。
    照 的 编号 为 照号。
    照 的 窗数 为 0。
    照 的 焦窗 为 0。
    照 的 页号 为 0。
    照 的 页题 为 「」。
    照 的 页址 为 「」。
    照 的 页类 为 「」。
    照 的 页名 为 「」。
    照 的 页源 为 「」。
    照 的 页摘 为 「」。
    照 的 节点数 为 0。
    照 的 脚本数 为 0。
    照 的 样式数 为 0。
    照 的 壳缓 为 「」。
    照 的 壳动 为 「」。
    照 的 壳果 为 「」。
    照 的 核态 为 「」。
    照 的 核副 为 「」。
    照 的 视觉态 为 「」。
    照 的 视觉副 为 「」。
    照 的 声音态 为 「」。
    照 的 声音副 为 「」。
    照 的 形象态 为 「」。
    照 的 形象副 为 「」。
    照 的 节点列 为 一组：（空）。
    返还 照。
完。

点建 接受 元素：
    点 为 新建 界节点。
    点 的 标识 为 元素 的 元素ID。
    点 的 角色 为 元素 的 角色。
    点 的 名称 为 元素 的 名称。
    点 的 值 为 元素 的 值。
    点 的 左 为 元素 的 左上x。
    点 的 上 为 元素 的 左上y。
    点 的 宽 为 元素 的 宽。
    点 的 高 为 元素 的 高。
    点 的 可见 为 元素 的 可见。
    点 的 可动 为 元素 的 可点击。
    点 的 可输 为 元素 的 可输入。
    点 的 可禁 为 元素 的 已禁用。
    点 的 路径 为 元素 的 DOM路径。
    点 的 选择 为 元素 的 选择器。
    返还 点。
完。

点串 接受 点：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"标识\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 点 的 标识。
    做 字符串构建器_追加 用 建、「,\"角色\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 点 的 角色。
    做 字符串构建器_追加 用 建、「\",\"名称\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 点 的 名称。
    做 字符串构建器_追加 用 建、「\",\"值\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 点 的 值。
    做 字符串构建器_追加 用 建、「\",\"区框\":[」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 点 的 左。
    做 字符串构建器_追加 用 建、「,」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 点 的 上。
    做 字符串构建器_追加 用 建、「,」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 点 的 宽。
    做 字符串构建器_追加 用 建、「,」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 点 的 高。
    做 字符串构建器_追加 用 建、「],\"可见\":」。
    做 字符串构建器_追加 用 建、做 真串 用 点 的 可见。
    做 字符串构建器_追加 用 建、「,\"可动\":」。
    做 字符串构建器_追加 用 建、做 真串 用 点 的 可动。
    做 字符串构建器_追加 用 建、「,\"可输\":」。
    做 字符串构建器_追加 用 建、做 真串 用 点 的 可输。
    做 字符串构建器_追加 用 建、「,\"可禁\":」。
    做 字符串构建器_追加 用 建、做 真串 用 点 的 可禁。
    做 字符串构建器_追加 用 建、「,\"路径\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 点 的 路径。
    做 字符串构建器_追加 用 建、「\",\"选择\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 点 的 选择。
    做 字符串构建器_追加 用 建、「\"}」。
    返还 做 字符串构建器_构建 用 建。
完。

点列串 接受 列：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「[」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列：
        若 i 大于 0：
            做 字符串构建器_追加 用 建、「,」。
        完。
        做 字符串构建器_追加 用 建、做 点串 用 列 的 第 i 项。
        i 为 i 加 1。
    完。
    做 字符串构建器_追加 用 建、「]」。
    返还 做 字符串构建器_构建 用 建。
完。

点采 接受 零参：
    原列 为 做 UI_扫描全部元素。
    点列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 原列：
        做 追加 用 点列、做 点建 用 原列 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 点列。
完。

页填 接受 照、页：
    若 页 等于 空：
        返还。
    完。
    若 做 计数 用 页 小于 10：
        返还。
    完。
    照 的 页号 为 页 的 第 0 项。
    照 的 页题 为 做 截串 用 页 的 第 1 项、120。
    照 的 页址 为 做 截串 用 页 的 第 2 项、240。
    照 的 页类 为 做 截串 用 页 的 第 3 项、64。
    照 的 页名 为 做 截串 用 页 的 第 4 项、120。
    照 的 页源 为 做 截串 用 页 的 第 5 项、256。
    照 的 页摘 为 做 截串 用 页 的 第 6 项、256。
    照 的 节点数 为 页 的 第 7 项。
    照 的 脚本数 为 页 的 第 8 项。
    照 的 样式数 为 页 的 第 9 项。
完。

桌填 接受 照、桌：
    若 桌 等于 空：
        返还。
    完。
    照 的 窗数 为 做 计数 用 桌 的 窗口列表。
    照 的 焦窗 为 桌 的 焦点窗口。
    照 的 壳缓 为 做 截串 用 桌 的 壳输入缓冲、200。
    照 的 壳动 为 做 截串 用 桌 的 壳最后动作、120。
    照 的 壳果 为 做 截串 用 桌 的 壳最后结果、240。
    照 的 核态 为 做 截串 用 桌 的 核心状态、64。
    照 的 核副 为 做 截串 用 桌 的 核心副文本、128。
    照 的 视觉态 为 做 截串 用 桌 的 视觉状态、64。
    照 的 视觉副 为 做 截串 用 桌 的 视觉副文本、128。
    照 的 声音态 为 做 截串 用 桌 的 声音状态、64。
    照 的 声音副 为 做 截串 用 桌 的 声音副文本、128。
    照 的 形象态 为 做 截串 用 桌 的 形象状态、64。
    照 的 形象副 为 做 截串 用 桌 的 形象副文本、128。
完。

照采 接受 零参：
    照 为 做 空照。
    桌 为 做 浏览器系统会话_获取桌面上下文。
    页 为 做 浏览器系统会话_获取当前页面上下文。
    照 的 节点列 为 做 点采。
    照 的 节点数 为 做 计数 用 照 的 节点列。
    做 桌填 用 照、桌。
    做 页填 用 照、页。
    取照数 为 取照数 加 1。
    上次节点数 为 照 的 节点数。
    上次窗口数 为 照 的 窗数。
    照号 为 照号 加 1。
    返还 照。
完。

照串 接受 照：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"编号\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 照 的 编号。
    做 字符串构建器_追加 用 建、「,\"窗数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 照 的 窗数。
    做 字符串构建器_追加 用 建、「,\"焦窗\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 照 的 焦窗。
    做 字符串构建器_追加 用 建、「,\"页号\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 照 的 页号。
    做 字符串构建器_追加 用 建、「,\"页题\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 页题。
    做 字符串构建器_追加 用 建、「\",\"页址\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 页址。
    做 字符串构建器_追加 用 建、「\",\"页类\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 页类。
    做 字符串构建器_追加 用 建、「\",\"页名\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 页名。
    做 字符串构建器_追加 用 建、「\",\"页源\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 页源。
    做 字符串构建器_追加 用 建、「\",\"页摘\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 页摘。
    做 字符串构建器_追加 用 建、「\",\"节点数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 照 的 节点数。
    做 字符串构建器_追加 用 建、「,\"脚本数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 照 的 脚本数。
    做 字符串构建器_追加 用 建、「,\"样式数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 照 的 样式数。
    做 字符串构建器_追加 用 建、「,\"壳缓\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 壳缓。
    做 字符串构建器_追加 用 建、「\",\"壳动\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 壳动。
    做 字符串构建器_追加 用 建、「\",\"壳果\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 壳果。
    做 字符串构建器_追加 用 建、「\",\"核态\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 核态。
    做 字符串构建器_追加 用 建、「\",\"核副\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 核副。
    做 字符串构建器_追加 用 建、「\",\"视觉态\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 视觉态。
    做 字符串构建器_追加 用 建、「\",\"视觉副\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 视觉副。
    做 字符串构建器_追加 用 建、「\",\"声音态\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 声音态。
    做 字符串构建器_追加 用 建、「\",\"声音副\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 声音副。
    做 字符串构建器_追加 用 建、「\",\"形象态\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 形象态。
    做 字符串构建器_追加 用 建、「\",\"形象副\":\"」。
    做 字符串构建器_追加 用 建、做 串义 用 照 的 形象副。
    做 字符串构建器_追加 用 建、「\",\"界树\":」。
    做 字符串构建器_追加 用 建、做 点列串 用 照 的 节点列。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

树串 接受 零参：
    取树数 为 取树数 加 1。
    点列 为 做 点采。
    返还 做 点列串 用 点列。
完。

点含 接受 点、词：
    若 做 字符串长度 用 词 等于 0：
        返还 假。
    完。
    若 做 字符串查找 用 点 的 名称、词 大于或等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 点 的 角色、词 大于或等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 点 的 路径、词 大于或等于 0：
        返还 真。
    完。
    若 做 字符串查找 用 点 的 选择、词 大于或等于 0：
        返还 真。
    完。
    返还 假。
完。

点找 接受 词：
    找点数 为 找点数 加 1。
    点列 为 做 点采。
    结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 点列：
        若 做 点含 用 点列 的 第 i 项、词：
            做 追加 用 结果、点列 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

点找串 接受 词：
    结果 为 做 点找 用 词。
    返还 做 点列串 用 结果。
完。

点号 接受 编号：
    点列 为 做 点采。
    可变 i 为 0。
    只要 i 小于 做 计数 用 点列：
        若 点列 的 第 i 项 的 标识 等于 编号：
            返还 点列 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

点号串 接受 编号：
    点 为 做 点号 用 编号。
    若 点 等于 空：
        返还 「{}」。
    完。
    返还 做 点串 用 点。
完。

照摘要 接受 照：
    返还 「窗」 加 做 整数转字符串 用 照 的 窗数 加 「 节点」 加 做 整数转字符串 用 照 的 节点数 加 「 页[」 加 照 的 页类 加 「] 」 加 照 的 页题 加 「 动作[」 加 照 的 壳动 加 「]」。
完。

照差 接受 前、后：
    若 前 等于 空：
        返还 「初始照」。
    完。
    差 为 一组：（空）。
    若 前 的 窗数 不等于 后 的 窗数：
        做 追加 用 差、「窗数:」 加 做 整数转字符串 用 前 的 窗数 加 「->」 加 做 整数转字符串 用 后 的 窗数。
    完。
    若 前 的 焦窗 不等于 后 的 焦窗：
        做 追加 用 差、「焦窗:」 加 做 整数转字符串 用 前 的 焦窗 加 「->」 加 做 整数转字符串 用 后 的 焦窗。
    完。
    若 前 的 页址 不等于 后 的 页址：
        做 追加 用 差、「页址:」 加 后 的 页址。
    完。
    若 前 的 页题 不等于 后 的 页题：
        做 追加 用 差、「页题:」 加 后 的 页题。
    完。
    若 前 的 节点数 不等于 后 的 节点数：
        做 追加 用 差、「节点:」 加 做 整数转字符串 用 前 的 节点数 加 「->」 加 做 整数转字符串 用 后 的 节点数。
    完。
    若 前 的 壳动 不等于 后 的 壳动：
        做 追加 用 差、「动作:」 加 后 的 壳动。
    完。
    若 前 的 壳果 不等于 后 的 壳果：
        做 追加 用 差、「结果:」 加 后 的 壳果。
    完。
    若 做 计数 用 差 等于 0：
        返还 「无差」。
    完。
    建 为 做 字符串构建器_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 差：
        若 i 大于 0：
            做 字符串构建器_追加 用 建、「 | 」。
        完。
        做 字符串构建器_追加 用 建、差 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 字符串构建器_构建 用 建。
完。

照统 接受 零参：
    返还 「{\"照号\":」 加 做 整数转字符串 用 照号 加 「,\"取照数\":」 加 做 整数转字符串 用 取照数 加 「,\"取树数\":」 加 做 整数转字符串 用 取树数 加 「,\"找点数\":」 加 做 整数转字符串 用 找点数 加 「,\"上次节点数\":」 加 做 整数转字符串 用 上次节点数 加 「,\"上次窗口数\":」 加 做 整数转字符串 用 上次窗口数 加 「}」。
完。

照样 接受 零参：
    照 为 做 空照。
    照 的 页题 为 「样页」。
    照 的 页址 为 「nova://sample」。
    照 的 页类 为 「system」。
    照 的 页名 为 「样页」。
    照 的 页摘 为 「样例全景照」。
    照 的 窗数 为 1。
    照 的 焦窗 为 1。
    点 为 新建 界节点。
    点 的 标识 为 1。
    点 的 角色 为 「button」。
    点 的 名称 为 「开始」。
    点 的 值 为 「」。
    点 的 左 为 10。
    点 的 上 为 10。
    点 的 宽 为 80。
    点 的 高 为 24。
    点 的 可见 为 真。
    点 的 可动 为 真。
    点 的 可输 为 假。
    点 的 可禁 为 假。
    点 的 路径 为 「body > button」。
    点 的 选择 为 「#start」。
    照 的 节点列 为 一组：点。
    照 的 节点数 为 1。
    返还 照。
完。

可变 AI_全景照_运行指标 为 0。

可变 AI_全景照_异常指标 为 0。

AI_全景照_自诊断 接受 零参：
    AI_全景照_运行指标 为 0。
    返还 AI_全景照_运行指标。
完。

AI_全景照_优化建议 接受 零参：
    指标 为 做 AI_全景照_自诊断。
    返还 「全景照状态正常」。
完。

本章 收束。
