《CSSStyleDeclaration》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《.·原子驻留》。

引用《.·HTTP头部管理》。

引用《..·中枢·内存初始化》。

引用《..·中枢·辅助存根》。

引用《..·中枢·整数转字符串》。

CSS规则_样式 为 1。

CSS规则_媒体 为 4。

CSS规则_字体 为 5。

CSS规则_关键帧组 为 7。

CSS规则_关键帧 为 8。

CSS规则_支持 为 12。

CSS规则_导入 为 3。

CSS规则_命名空间 为 10。

CSS规则_层 为 16。

CSSRule 是 一种 事物，含 类型、CSS文本、父规则、父样式表、数据。

CSS样式Rule 是 一种 事物，含 选择器文本、样式声明。

CSS媒体Rule 是 一种 事物，含 媒体文本、媒体查询、子规则、子规则数量、子规则容量。

CSSKeyframesRule_Data 是 一种 事物，含 名称、关键帧、关键帧数量。

CSS样式Declaration 是 一种 事物，含 声明、声明数量、声明容量、所属规则、所属元素、是内联。

CSS样式Declaration_创建 接受 是内联、元素：
    sd 为 做 堆_分配 用 CSS样式Declaration。
    sd 的 声明容量 为 64。
    sd 的 声明 为 做 堆_分配_数组 用 sd 的 声明容量 乘 24。
    sd 的 声明数量 为 0。
    sd 的 所属规则 为 0。
    sd 的 所属元素 为 元素。
    sd 的 是内联 为 是内联。
    返还 sd。
完。

CSS样式Declaration_getPropertyValue 接受 sd、属性名：
    可变 i 为 0。
    只要 i 小于 sd 的 声明数量：
        偏移 为 sd 的 声明 加 i 乘 24。
        名 为 做 内存_读64 用 偏移。
        若 做 字符串_相等_不区分大小写 用 名、属性名：
            返还 做 内存_读64 用 偏移 加 8。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

CSS样式Declaration_setProperty 接受 sd、属性名、值、优先级：
    可变 i 为 0。
    只要 i 小于 sd 的 声明数量：
        偏移 为 sd 的 声明 加 i 乘 24。
        名 为 做 内存_读64 用 偏移。
        若 做 字符串_相等_不区分大小写 用 名、属性名：
            做 内存_写64 用 偏移 加 8、做 字符串_复制 用 值。
            做 内存_写32 用 偏移 加 16、优先级。
            做 CSS样式Declaration_通知变更 用 sd。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 sd 的 声明数量 大于或等于 sd 的 声明容量：
        返还。
    完。
    偏移 为 sd 的 声明 加 sd 的 声明数量 乘 24。
    做 内存_写64 用 偏移、做 字符串_复制 用 属性名。
    做 内存_写64 用 偏移 加 8、做 字符串_复制 用 值。
    做 内存_写32 用 偏移 加 16、优先级。
    sd 的 声明数量 为 sd 的 声明数量 加 1。
    做 CSS样式Declaration_通知变更 用 sd。
完。

CSS样式Declaration_removeProperty 接受 sd、属性名：
    可变 i 为 0。
    只要 i 小于 sd 的 声明数量：
        偏移 为 sd 的 声明 加 i 乘 24。
        名 为 做 内存_读64 用 偏移。
        若 做 字符串_相等_不区分大小写 用 名、属性名：
            旧值 为 做 内存_读64 用 偏移 加 8。
            可变 j 为 i。
            只要 j 小于 sd 的 声明数量 减 1：
                做 内存_复制 用 sd 的 声明 加 j 乘 24、sd 的 声明 加 (j 加 1) 乘 24、24。
                j 为 j 加 1。
            完。
            sd 的 声明数量 为 sd 的 声明数量 减 1。
            做 CSS样式Declaration_通知变更 用 sd。
            返还 旧值。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

CSS样式Declaration_getPropertyPriority 接受 sd、属性名：
    可变 i 为 0。
    只要 i 小于 sd 的 声明数量：
        偏移 为 sd 的 声明 加 i 乘 24。
        若 做 字符串_相等_不区分大小写 用 做 内存_读64 用 偏移、属性名：
            若 做 内存_读32 用 偏移 加 16 等于 1：
                返还 「important」。
            完。
            返还 「」。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

CSS样式Declaration_item 接受 sd、索引：
    若 索引 大于或等于 sd 的 声明数量：
        返还 「」。
    完。
    返还 做 内存_读64 用 sd 的 声明 加 索引 乘 24。
完。

CSS样式Declaration_length 接受 sd：
    返还 sd 的 声明数量。
完。

CSS样式Declaration_cssText 接受 sd：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 sd 的 声明数量：
        偏移 为 sd 的 声明 加 i 乘 24。
        若 i 大于 0：
            结果 为 做 字符串_连接 用 结果、「 」。
        完。
        结果 为 做 字符串_连接 用 结果、做 内存_读64 用 偏移。
        结果 为 做 字符串_连接 用 结果、「: 」。
        结果 为 做 字符串_连接 用 结果、做 内存_读64 用 偏移 加 8。
        若 做 内存_读32 用 偏移 加 16 等于 1：
            结果 为 做 字符串_连接 用 结果、「 !important」。
        完。
        结果 为 做 字符串_连接 用 结果、「;」。
        i 为 i 加 1。
    完。
    返还 结果。
完。

CSS样式Declaration_通知变更 接受 sd：
    若 sd 的 是内联 并且 sd 的 所属元素 不等于 0：
        css 为 做 CSS样式Declaration_cssText 用 sd。
        做 元素_setAttribute 用 sd 的 所属元素、「style」、css。
        sd 的 所属元素 的 节点标志 为 sd 的 所属元素 的 节点标志 位或 节点标志_样式脏。
    完。
完。

可变 AI_样式样式创_样式缓存命中 为 0。

AI_样式样式创_选择器复杂度 接受 选择器长度、组合子数：
    若 组合子数 大于 5：
        返还 「高复杂度」。
    完。
    若 组合子数 大于 2：
        返还 「中复杂度」。
    完。
    返还 「低复杂度」。
完。

AI_样式样式创_布局缓存 接受 元素哈希：
    AI_样式样式创_样式缓存命中 为 AI_样式样式创_样式缓存命中 加 1。
    返还 元素哈希。
完。

本章 收束。
