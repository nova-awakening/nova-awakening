《样式解析为》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内联样式解析》。

引用《..·中枢·内存初始化》。

引用《.·计算长度零》。

引用《.·样式继承》。

引用《.·应用样式声明》。

引用《.·列表》。

引用《.·原子驻留》。

引用《.·ComputedStyle创建》。

引用《.·文档节点》。

样式Engine_解析元素样式 接受 engine、元素：
    若 元素 等于 0：
        返还。
    完。
    若 非 (元素 的 节点标志 位与 节点标志_是元素)：
        返还。
    完。
    engine 的 总样式计算次数 为 engine 的 总样式计算次数 加 1。
    扩展 为 做 元素_获取扩展 用 元素。
    若 扩展 等于 0：
        返还。
    完。
    style 为 做 Computed样式_创建。
    父 为 元素 的 父节点。
    若 父 不等于 0 并且 父 的 节点标志 位与 节点标志_是元素：
        父扩展 为 做 元素_获取扩展 用 父。
        若 父扩展 不等于 0 并且 父扩展 的 计算样式 不等于 0：
            做 Computed样式_继承 用 style、父扩展 的 计算样式。
        完。
    完。
    做 Computed样式_应用UA默认 用 style、扩展 的 标签名原子。
    可变 匹配规则列表 为 一组：（空）。
    做 样式Engine_收集匹配规则 用 engine、元素、扩展、匹配规则列表。
    做 样式Engine_按特异度排序 用 匹配规则列表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 匹配规则列表：
        规则 为 匹配规则列表 的 第 i 项。
        若 规则 不等于 0：
            可变 j 为 0。
            只要 j 小于 规则 的 声明数量：
                声明偏移 为 规则 的 声明列表 加 j 乘 24。
                声明 为 做 堆_分配 用 CSS声明。
                声明 的 属性ID 为 做 内存_读32 用 声明偏移。
                声明 的 值 为 做 内存_读64 用 声明偏移 加 8。
                声明 的 重要 为 做 内存_读32 用 声明偏移 加 16。
                做 Computed样式_应用声明 用 style、声明。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    数据 为 扩展 的 元素数据。
    若 数据 不等于 0 并且 数据 的 内联样式 不等于 0：
        做 样式Engine_应用内联样式 用 style、数据 的 内联样式。
    完。
    扩展 的 计算样式 为 style。
完。

样式Engine_收集匹配规则 接受 engine、元素、扩展、结果列表：
    若 扩展 的 id原子 不等于 0：
        桶 为 扩展 的 id原子 取余于 256。
        规则 为 做 内存_读64 用 engine 的 id规则索引 加 桶 乘 16。
        若 规则 不等于 0：
            若 做 CSS_选择器匹配 用 规则 的 选择器列表、元素：
                做 追加 用 结果列表、规则。
                engine 的 总规则匹配次数 为 engine 的 总规则匹配次数 加 1。
            完。
        完。
    完。
    className 为 做 元素_className 用 元素。
    若 className 不等于 0：
        class原子 为 做 原子_驻留 用 className。
        桶 为 class原子 取余于 256。
        规则 为 做 内存_读64 用 engine 的 class规则索引 加 桶 乘 16。
        若 规则 不等于 0：
            若 做 CSS_选择器匹配 用 规则 的 选择器列表、元素：
                做 追加 用 结果列表、规则。
                engine 的 总规则匹配次数 为 engine 的 总规则匹配次数 加 1。
            完。
        完。
    完。
    标签桶 为 扩展 的 标签名原子 取余于 256。
    标签规则 为 做 内存_读64 用 engine 的 标签规则索引 加 标签桶 乘 16。
    若 标签规则 不等于 0：
        若 做 CSS_选择器匹配 用 标签规则 的 选择器列表、元素：
            做 追加 用 结果列表、标签规则。
            engine 的 总规则匹配次数 为 engine 的 总规则匹配次数 加 1。
        完。
    完。
    可变 k 为 0。
    只要 k 小于 engine 的 通用规则数量：
        规则 为 做 内存_读64 用 engine 的 通用规则 加 k 乘 8。
        若 规则 不等于 0 并且 做 CSS_选择器匹配 用 规则 的 选择器列表、元素：
            做 追加 用 结果列表、规则。
            engine 的 总规则匹配次数 为 engine 的 总规则匹配次数 加 1。
        完。
        k 为 k 加 1。
    完。
完。

样式Engine_按特异度排序 接受 规则列表：
    n 为 做 计数 用 规则列表。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 0。
        只要 j 小于 n 减 i 减 1：
            若 规则列表 的 第 j 项 的 特异度 大于 规则列表 的 第 j 加 1 项 的 特异度：
                临时 为 规则列表 的 第 j 项。
                规则列表 的 第 j 项 为 规则列表 的 第 j 加 1 项。
                规则列表 的 第 j 加 1 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

样式Engine_应用内联样式 接受 style、内联声明列表：
    可变 i 为 0。
    只要 i 小于 32：
        声明偏移 为 内联声明列表 加 i 乘 24。
        属性ID 为 做 内存_读32 用 声明偏移。
        若 属性ID 等于 0：
            跳出。
        完。
        声明 为 做 堆_分配 用 CSS声明。
        声明 的 属性ID 为 属性ID。
        声明 的 值 为 做 内存_读64 用 声明偏移 加 8。
        声明 的 重要 为 假。
        做 Computed样式_应用声明 用 style、声明。
        i 为 i 加 1。
    完。
完。

可变 AI_样式解析为_热节点缓存 为 0。

AI_样式解析为_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_样式解析为_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_样式解析为_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

本章 收束。
