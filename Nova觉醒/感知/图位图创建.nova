《图位图创建》

引用《..·稳态·图处显示》。

引用《..·推理·通信总线》。

引用《.·内核直连层》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《..·执行·底层内存操》。

像素格式_RGBA8888 为 0。

像素格式_BGRA8888 为 1。

像素格式_RGB565 为 2。

像素格式_Alpha8 为 3。

像素格式_Gray8 为 4。

像素格式_RGBA_F16 为 5。

位图_最大宽度 为 16384。

位图_最大高度 为 16384。

位图_对齐字节 为 4。

颜色_透明 为 0x00000000。

颜色_黑色 为 0xFF000000。

颜色_白色 为 0xFFFFFFFF。

颜色_红色 为 0xFFFF0000。

颜色_绿色 为 0xFF00FF00。

颜色_蓝色 为 0xFF0000FF。

字体粗细_细 为 100。

字体粗细_超轻 为 200。

字体粗细_轻 为 300。

字体粗细_正常 为 400。

字体粗细_中等 为 500。

字体粗细_半粗 为 600。

字体粗细_粗 为 700。

字体粗细_超粗 为 800。

字体粗细_黑 为 900。

文本对齐_左 为 0。

文本对齐_居中 为 1。

文本对齐_右 为 2。

Nova位图 是 一种 事物，含 像素数据、宽、高、行字节数、像素格式、每像素字节、引用计数、不可变、颜色空间、预乘Alpha、拥有像素、像素大小、统计_读取次数、统计_写入次数。

Nova位图_创建 接受 宽、高、格式：
    若 宽 小于或等于 0 或者 高 小于或等于 0：
        做 打印 用 「[位图] 错误: 无效尺寸」。
        返还 空。
    完。
    若 宽 大于 位图_最大宽度 或者 高 大于 位图_最大高度：
        做 打印 用 「[位图] 错误: 尺寸超过上限」。
        返还 空。
    完。
    可变 bm 为 做 堆_分配 用 Nova位图。
    若 bm 等于 0：
        做 打印 用 「[位图] 错误: 结构分配失败」。
        返还 空。
    完。
    bm 的 宽 为 宽。
    bm 的 高 为 高。
    bm 的 像素格式 为 格式。
    bm 的 引用计数 为 1。
    bm 的 不可变 为 假。
    bm 的 颜色空间 为 0。
    bm 的 预乘Alpha 为 假。
    bm 的 统计_读取次数 为 0。
    bm 的 统计_写入次数 为 0。
    若 格式 等于 像素格式_RGBA8888 或者 格式 等于 像素格式_BGRA8888：
        bm 的 每像素字节 为 4。
    否则若 格式 等于 像素格式_RGB565：
        bm 的 每像素字节 为 2。
    否则若 格式 等于 像素格式_RGBA_F16：
        bm 的 每像素字节 为 8。
    否则：
        bm 的 每像素字节 为 1。
    完。
    bm 的 行字节数 为 宽 乘 bm 的 每像素字节。
    若 bm 的 行字节数 取余于 位图_对齐字节 不等于 0：
        bm 的 行字节数 为 bm 的 行字节数 加 (位图_对齐字节 减 bm 的 行字节数 取余于 位图_对齐字节)。
    完。
    bm 的 像素大小 为 bm 的 行字节数 乘 高。
    bm 的 像素数据 为 做 平台_申请内存 用 bm 的 像素大小。
    若 bm 的 像素数据 等于 0：
        做 打印 用 「[位图] 错误: 像素内存分配失败」。
        做 堆_释放 用 bm。
        返还 空。
    完。
    做 内存_清零 用 bm 的 像素数据、bm 的 像素大小。
    bm 的 拥有像素 为 真。
    返还 bm。
完。

Nova位图_从外部像素 接受 宽、高、格式、像素指针、行字节数：
    若 宽 小于或等于 0 或者 高 小于或等于 0 或者 像素指针 等于 0：
        返还 空。
    完。
    可变 bm 为 做 堆_分配 用 Nova位图。
    若 bm 等于 0：
        返还 空。
    完。
    bm 的 宽 为 宽。
    bm 的 高 为 高。
    bm 的 像素格式 为 格式。
    bm 的 像素数据 为 像素指针。
    bm 的 行字节数 为 行字节数。
    bm 的 引用计数 为 1。
    bm 的 不可变 为 假。
    bm 的 拥有像素 为 假。
    bm 的 颜色空间 为 0。
    bm 的 预乘Alpha 为 假。
    bm 的 统计_读取次数 为 0。
    bm 的 统计_写入次数 为 0。
    若 格式 等于 像素格式_RGBA8888 或者 格式 等于 像素格式_BGRA8888：
        bm 的 每像素字节 为 4。
    否则若 格式 等于 像素格式_RGB565：
        bm 的 每像素字节 为 2。
    否则：
        bm 的 每像素字节 为 1。
    完。
    bm 的 像素大小 为 行字节数 乘 高。
    返还 bm。
完。

Nova位图_增引用 接受 bm：
    若 bm 等于 空：
        返还。
    完。
    bm 的 引用计数 为 bm 的 引用计数 加 1。
完。

Nova位图_减引用 接受 bm：
    若 bm 等于 空：
        返还。
    完。
    bm 的 引用计数 为 bm 的 引用计数 减 1。
    若 bm 的 引用计数 小于或等于 0：
        做 Nova位图_销毁 用 bm。
    完。
完。

Nova位图_销毁 接受 bm：
    若 bm 等于 空：
        返还。
    完。
    若 bm 的 拥有像素 等于 真 并且 bm 的 像素数据 不等于 0：
        做 平台_释放内存 用 bm 的 像素数据、bm 的 像素大小。
    完。
    做 堆_释放 用 bm。
完。

Nova位图_获取像素 接受 bm、x、y：
    若 bm 等于 空：
        返还 0。
    完。
    若 x 小于 0 或者 x 大于或等于 bm 的 宽 或者 y 小于 0 或者 y 大于或等于 bm 的 高：
        返还 0。
    完。
    bm 的 统计_读取次数 为 bm 的 统计_读取次数 加 1。
    偏移 为 y 乘 bm 的 行字节数 加 x 乘 bm 的 每像素字节。
    若 bm 的 每像素字节 等于 4：
        返还 做 内存_读32 用 bm 的 像素数据 加 偏移。
    否则若 bm 的 每像素字节 等于 2：
        值 为 做 内存_读16 用 bm 的 像素数据 加 偏移。
        r 为 (值 右移 11 位与 31) 乘 255 除以 31。
        g 为 (值 右移 5 位与 63) 乘 255 除以 63。
        b 为 (值 位与 31) 乘 255 除以 31。
        返还 255 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
    否则：
        值 为 做 内存_读8 用 bm 的 像素数据 加 偏移。
        返还 值 左移 24 位或 值 左移 16 位或 值 左移 8 位或 值。
    完。
完。

Nova位图_设置像素 接受 bm、x、y、颜色：
    若 bm 等于 空 或者 bm 的 不可变 等于 真：
        返还。
    完。
    若 x 小于 0 或者 x 大于或等于 bm 的 宽 或者 y 小于 0 或者 y 大于或等于 bm 的 高：
        返还。
    完。
    bm 的 统计_写入次数 为 bm 的 统计_写入次数 加 1。
    偏移 为 y 乘 bm 的 行字节数 加 x 乘 bm 的 每像素字节。
    若 bm 的 每像素字节 等于 4：
        做 内存_写32 用 bm 的 像素数据 加 偏移、颜色。
    否则若 bm 的 每像素字节 等于 2：
        r 为 颜色 右移 16 位与 255。
        g 为 颜色 右移 8 位与 255。
        b 为 颜色 位与 255。
        值 为 r 右移 3 左移 11 位或 g 右移 2 左移 5 位或 b 右移 3。
        做 内存_写16 用 bm 的 像素数据 加 偏移、值。
    否则：
        做 内存_写8 用 bm 的 像素数据 加 偏移、颜色 右移 24 位与 255。
    完。
完。

Nova位图_安全混合像素 接受 bm、x、y、颜色：
    若 bm 等于 空 或者 bm 的 不可变 等于 真：
        返还。
    完。
    若 x 小于 0 或者 x 大于或等于 bm 的 宽 或者 y 小于 0 或者 y 大于或等于 bm 的 高：
        返还。
    完。
    srcA 为 颜色 右移 24 位与 255。
    若 srcA 等于 255：
        做 Nova位图_设置像素 用 bm、x、y、颜色。
        返还。
    完。
    若 srcA 等于 0：
        返还。
    完。
    dst 为 做 Nova位图_获取像素 用 bm、x、y。
    invA 为 255 减 srcA。
    outR 为 (颜色 右移 16 位与 255) 加 (dst 右移 16 位与 255) 乘 invA 除以 255。
    outG 为 (颜色 右移 8 位与 255) 加 (dst 右移 8 位与 255) 乘 invA 除以 255。
    outB 为 (颜色 位与 255) 加 (dst 位与 255) 乘 invA 除以 255。
    outA 为 srcA 加 (dst 右移 24 位与 255) 乘 invA 除以 255。
    做 Nova位图_设置像素 用 bm、x、y、outA 左移 24 位或 outR 左移 16 位或 outG 左移 8 位或 outB。
完。

Nova位图_清除 接受 bm、颜色：
    若 bm 等于 空 或者 bm 的 不可变 等于 真：
        返还。
    完。
    若 bm 的 每像素字节 等于 4：
        可变 偏移 为 0。
        只要 偏移 小于 bm 的 像素大小：
            做 内存_写32 用 bm 的 像素数据 加 偏移、颜色。
            偏移 为 偏移 加 4。
        完。
    否则：
        可变 y 为 0。
        只要 y 小于 bm 的 高：
            可变 x 为 0。
            只要 x 小于 bm 的 宽：
                做 Nova位图_设置像素 用 bm、x、y、颜色。
                x 为 x 加 1。
            完。
            y 为 y 加 1。
        完。
    完。
完。

Nova位图_复制行 接受 bm、目标y、源y：
    若 bm 等于 空 或者 bm 的 不可变 等于 真：
        返还。
    完。
    若 目标y 小于 0 或者 目标y 大于或等于 bm 的 高 或者 源y 小于 0 或者 源y 大于或等于 bm 的 高：
        返还。
    完。
    若 目标y 等于 源y：
        返还。
    完。
    源偏移 为 源y 乘 bm 的 行字节数。
    目标偏移 为 目标y 乘 bm 的 行字节数。
    做 内存_复制 用 bm 的 像素数据 加 目标偏移、bm 的 像素数据 加 源偏移、bm 的 宽 乘 bm 的 每像素字节。
完。

Nova位图_填充矩形 接受 bm、左、上、右、下、颜色：
    若 bm 等于 空 或者 bm 的 不可变 等于 真：
        返还。
    完。
    可变 x起 为 左。
    可变 y起 为 上。
    可变 x止 为 右。
    可变 y止 为 下。
    若 x起 小于 0：
        x起 为 0。
    完。
    若 y起 小于 0：
        y起 为 0。
    完。
    若 x止 大于 bm 的 宽：
        x止 为 bm 的 宽。
    完。
    若 y止 大于 bm 的 高：
        y止 为 bm 的 高。
    完。
    若 x起 大于或等于 x止 或者 y起 大于或等于 y止：
        返还。
    完。
    srcA 为 颜色 右移 24 位与 255。
    可变 y 为 y起。
    只要 y 小于 y止：
        行基址 为 bm 的 像素数据 加 y 乘 bm 的 行字节数。
        可变 x 为 x起。
        只要 x 小于 x止：
            若 srcA 等于 255：
                做 内存_写32 用 行基址 加 x 乘 bm 的 每像素字节、颜色。
            否则若 srcA 大于 0：
                做 Nova位图_安全混合像素 用 bm、x、y、颜色。
            完。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
完。

Nova位图_复制到 接受 源、目标、目标x、目标y：
    若 源 等于 空 或者 目标 等于 空 或者 目标 的 不可变 等于 真：
        返还。
    完。
    可变 y 为 0。
    只要 y 小于 源 的 高：
        dy 为 目标y 加 y。
        若 dy 小于 0：
            y 为 y 加 1。
            继续。
        完。
        若 dy 大于或等于 目标 的 高：
            跳出。
        完。
        可变 x 为 0。
        只要 x 小于 源 的 宽：
            dx 为 目标x 加 x。
            若 dx 大于或等于 0 并且 dx 小于 目标 的 宽：
                颜色 为 做 Nova位图_获取像素 用 源、x、y。
                做 Nova位图_安全混合像素 用 目标、dx、dy、颜色。
            完。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
完。

Sk字体 是 一种 事物，含 字体族、大小、粗细、斜体、抗锯齿、亚像素、行距、上升、下降、行间距、下划线偏移、下划线粗细、删除线偏移。

Sk字体_创建 接受 字体族、大小：
    若 大小 小于或等于 0：
        大小 为 1600。
    完。
    可变 f 为 做 堆_分配 用 Sk字体。
    若 f 等于 0：
        返还 空。
    完。
    f 的 字体族 为 字体族。
    f 的 大小 为 大小。
    f 的 粗细 为 字体粗细_正常。
    f 的 斜体 为 假。
    f 的 抗锯齿 为 真。
    f 的 亚像素 为 真。
    f 的 行距 为 大小 乘 120 除以 100。
    f 的 上升 为 大小 乘 80 除以 100。
    f 的 下降 为 大小 乘 20 除以 100。
    f 的 行间距 为 大小 乘 10 除以 100。
    f 的 下划线偏移 为 大小 乘 15 除以 100。
    f 的 下划线粗细 为 大小 乘 7 除以 100。
    f 的 删除线偏移 为 大小 乘 40 除以 100。
    返还 f。
完。

Sk字体_销毁 接受 f：
    若 f 等于 空：
        返还。
    完。
    做 堆_释放 用 f。
完。

Sk字体_设置粗细 接受 f、粗细：
    若 f 等于 空：
        返还。
    完。
    若 粗细 小于 100：
        粗细 为 100。
    完。
    若 粗细 大于 900：
        粗细 为 900。
    完。
    f 的 粗细 为 粗细。
完。

Sk字体_设置斜体 接受 f、斜体：
    若 f 等于 空：
        返还。
    完。
    f 的 斜体 为 斜体。
完。

Sk字体_设置大小 接受 f、大小：
    若 f 等于 空 或者 大小 小于或等于 0：
        返还。
    完。
    f 的 大小 为 大小。
    f 的 行距 为 大小 乘 120 除以 100。
    f 的 上升 为 大小 乘 80 除以 100。
    f 的 下降 为 大小 乘 20 除以 100。
    f 的 行间距 为 大小 乘 10 除以 100。
    f 的 下划线偏移 为 大小 乘 15 除以 100。
    f 的 下划线粗细 为 大小 乘 7 除以 100。
    f 的 删除线偏移 为 大小 乘 40 除以 100。
完。

Sk字体_测量文本宽度 接受 font、文本、长度：
    若 font 等于 空 或者 文本 等于 0 或者 长度 小于或等于 0：
        返还 0。
    完。
    可变 宽度 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        c 为 做 内存_读8 用 文本 加 i。
        若 c 等于 0：
            跳出。
        完。
        若 c 大于 0x7F：
            宽度 为 宽度 加 font 的 大小。
            若 c 大于或等于 0xF0：
                i 为 i 加 3。
            否则若 c 大于或等于 0xE0：
                i 为 i 加 2。
            否则若 c 大于或等于 0xC0：
                i 为 i 加 1。
            完。
        否则：
            若 c 等于 32：
                宽度 为 宽度 加 font 的 大小 乘 25 除以 100。
            否则若 c 大于或等于 48 并且 c 小于或等于 57：
                宽度 为 宽度 加 font 的 大小 乘 60 除以 100。
            否则若 c 等于 105 或者 c 等于 108 或者 c 等于 49：
                宽度 为 宽度 加 font 的 大小 乘 30 除以 100。
            否则若 c 等于 109 或者 c 等于 119：
                宽度 为 宽度 加 font 的 大小 乘 90 除以 100。
            否则：
                宽度 为 宽度 加 font 的 大小 乘 55 除以 100。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 宽度。
完。

Sk字体_测量文本高度 接受 font：
    若 font 等于 空：
        返还 0。
    完。
    返还 font 的 上升 加 font 的 下降 加 font 的 行间距。
完。

Sk字体_获取行高 接受 font：
    若 font 等于 空：
        返还 0。
    完。
    返还 font 的 行距。
完。

Sk字体_计算文本行数 接受 font、文本、长度、最大宽度：
    若 font 等于 空 或者 文本 等于 0 或者 最大宽度 小于或等于 0：
        返还 1。
    完。
    总宽度 为 做 Sk字体_测量文本宽度 用 font、文本、长度。
    若 总宽度 小于或等于 最大宽度：
        返还 1。
    完。
    返还 (总宽度 加 最大宽度 减 1) 除以 最大宽度。
完。

颜色_提取A 接受 颜色：
    返还 颜色 右移 24 位与 255。
完。

颜色_提取R 接受 颜色：
    返还 颜色 右移 16 位与 255。
完。

颜色_提取G 接受 颜色：
    返还 颜色 右移 8 位与 255。
完。

颜色_提取B 接受 颜色：
    返还 颜色 位与 255。
完。

颜色_创建 接受 a、r、g、b：
    返还 (a 位与 255) 左移 24 位或 (r 位与 255) 左移 16 位或 (g 位与 255) 左移 8 位或 b 位与 255。
完。

颜色_从RGB 接受 r、g、b：
    返还 做 颜色_创建 用 255、r、g、b。
完。

颜色_设置透明度 接受 颜色、alpha：
    返还 颜色 位与 0x00FFFFFF 位或 (alpha 位与 255) 左移 24。
完。

颜色_亮度 接受 颜色：
    r 为 做 颜色_提取R 用 颜色。
    g 为 做 颜色_提取G 用 颜色。
    b 为 做 颜色_提取B 用 颜色。
    返还 r 乘 299 加 g 乘 587 加 b 乘 114。
完。

颜色_混合 接受 c1、c2、比例：
    inv 为 256 减 比例。
    a 为 (做 颜色_提取A 用 c1 乘 inv 加 做 颜色_提取A 用 c2 乘 比例) 除以 256。
    r 为 (做 颜色_提取R 用 c1 乘 inv 加 做 颜色_提取R 用 c2 乘 比例) 除以 256。
    g 为 (做 颜色_提取G 用 c1 乘 inv 加 做 颜色_提取G 用 c2 乘 比例) 除以 256。
    b 为 (做 颜色_提取B 用 c1 乘 inv 加 做 颜色_提取B 用 c2 乘 比例) 除以 256。
    返还 做 颜色_创建 用 a、r、g、b。
完。

颜色_反色 接受 颜色：
    返还 做 颜色_创建 用 做 颜色_提取A 用 颜色、255 减 做 颜色_提取R 用 颜色、255 减 做 颜色_提取G 用 颜色、255 减 做 颜色_提取B 用 颜色。
完。

颜色_灰度化 接受 颜色：
    灰 为 做 颜色_亮度 用 颜色 除以 1000。
    返还 做 颜色_创建 用 做 颜色_提取A 用 颜色、灰、灰、灰。
完。

Nova位图_AI推荐格式 接受 宽、高、有透明、是图标：
    若 是图标 等于 真 并且 宽 小于或等于 48 并且 高 小于或等于 48：
        返还 像素格式_RGBA8888。
    完。
    若 有透明 等于 假 并且 宽 乘 高 大于 500000：
        返还 像素格式_RGB565。
    完。
    返还 像素格式_RGBA8888。
完。

Nova位图_AI估算内存 接受 宽、高、格式：
    可变 每像素 为 4。
    若 格式 等于 像素格式_RGB565：
        每像素 为 2。
    完。
    若 格式 等于 像素格式_Alpha8 或者 格式 等于 像素格式_Gray8：
        每像素 为 1。
    完。
    若 格式 等于 像素格式_RGBA_F16：
        每像素 为 8。
    完。
    返还 宽 乘 高 乘 每像素。
完。

可变 全局_位图创建数 为 0。

可变 全局_位图销毁数 为 0。

可变 全局_位图内存总量 为 0。

Nova位图_打印信息 接受 bm：
    若 bm 等于 空：
        做 打印 用 「[位图] 空位图」。
        返还。
    完。
    做 打印 用 「[位图] ── 位图信息 ──」。
    做 打印 用 做 字符串拼接 用 「  尺寸:       」、做 字符串拼接 用 做 转字符串 用 bm 的 宽、做 字符串拼接 用 「×」、做 转字符串 用 bm 的 高。
    做 打印 用 做 字符串拼接 用 「  像素格式:   」、做 转字符串 用 bm 的 像素格式。
    做 打印 用 做 字符串拼接 用 「  每像素字节: 」、做 转字符串 用 bm 的 每像素字节。
    做 打印 用 做 字符串拼接 用 「  行字节数:   」、做 转字符串 用 bm 的 行字节数。
    做 打印 用 做 字符串拼接 用 「  像素大小:   」、做 转字符串 用 bm 的 像素大小。
    做 打印 用 做 字符串拼接 用 「  引用计数:   」、做 转字符串 用 bm 的 引用计数。
    做 打印 用 做 字符串拼接 用 「  不可变:     」、做 转字符串 用 bm 的 不可变。
    做 打印 用 做 字符串拼接 用 「  读取次数:   」、做 转字符串 用 bm 的 统计_读取次数。
    做 打印 用 做 字符串拼接 用 「  写入次数:   」、做 转字符串 用 bm 的 统计_写入次数。
完。

Sk字体_打印信息 接受 f：
    若 f 等于 空：
        做 打印 用 「[字体] 空字体」。
        返还。
    完。
    做 打印 用 「[字体] ── 字体信息 ──」。
    做 打印 用 做 字符串拼接 用 「  大小(×100): 」、做 转字符串 用 f 的 大小。
    做 打印 用 做 字符串拼接 用 「  粗细:       」、做 转字符串 用 f 的 粗细。
    做 打印 用 做 字符串拼接 用 「  斜体:       」、做 转字符串 用 f 的 斜体。
    做 打印 用 做 字符串拼接 用 「  行距(×100): 」、做 转字符串 用 f 的 行距。
    做 打印 用 做 字符串拼接 用 「  上升(×100): 」、做 转字符串 用 f 的 上升。
    做 打印 用 做 字符串拼接 用 「  下降(×100): 」、做 转字符串 用 f 的 下降。
完。

位图系统_打印统计 接受 零参：
    做 打印 用 「[位图] ═══════════ 位图系统统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  创建数: 」、做 转字符串 用 全局_位图创建数。
    做 打印 用 做 字符串拼接 用 「  销毁数: 」、做 转字符串 用 全局_位图销毁数。
    做 打印 用 做 字符串拼接 用 「  内存总量: 」、做 转字符串 用 全局_位图内存总量。
    做 打印 用 「[位图] ═══════════════════════════════════════」。
完。

本章 收束。
