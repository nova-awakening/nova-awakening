《文档观察》

引用《.·文档节点》。

引用《.·文档事件》。

引用《.·全景照》。

引用《..·中枢·硬链接》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·辅助存根》。

引用《.·URL同源解析》。

引用《..·中枢·整数转字符串》。

引用《..·推理·Microtask微任务》。

引用《.·历史State管理》。

引用《.·导航流程》。

引用《..·执行·平台时间》。

引用《..·中枢·哈希整数》。

引用《..·中枢·时钟中枢》。

引用《..·中枢·内容可见性》。

观察档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、性能数、变更数、交叉数、尺寸数、遍历数、令牌数、信号数、成功数、失败数、最近目标、最近阈值、最近尺寸、最近耗时。

观察请求 是 一种 事物，含 动作、目标、参数、选项。

观察回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

观察摘要 是 一种 事物，含 名称、性能数、变更数、交叉数、尺寸数、遍历数、令牌数、信号数、成功数、失败数、健康。

观察项 是 一种 事物，含 类型、目标、数据、来源、时间。

可变 观察账本 为 一组：（空）。

可变 观察构建数 为 0。

可变 观察总动作 为 0。

可变 观察总成功 为 0。

可变 观察总失败 为 0。

文档观察_建档 接受 名称：
    档 为 新建 观察档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 性能数 为 0。
    档 的 变更数 为 0。
    档 的 交叉数 为 0。
    档 的 尺寸数 为 0。
    档 的 遍历数 为 0。
    档 的 令牌数 为 0。
    档 的 信号数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    档 的 最近目标 为 「」。
    档 的 最近阈值 为 「」。
    档 的 最近尺寸 为 「」。
    档 的 最近耗时 为 0。
    返还 档。
完。

文档观察_登记 接受 档：
    做 追加 用 观察账本、档。
    观察构建数 为 观察构建数 加 1。
    返还 档。
完。

文档观察_完整统计 接受 零参：
    返还 一组：观察构建数、观察总动作、观察总成功、观察总失败。
完。

文档观察_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 观察回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

文档观察_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 性能数 大于 0 或者 档 的 变更数 大于 0 或者 档 的 遍历数 大于 0：
        档 的 最近健康 为 「运行」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「空载」。
    返还 档 的 最近健康。
完。

文档观察_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    档 的 最近错误 为 「」。
    档 的 最近统计 为 做 文档观察_完整统计。
完。

文档观察_成功 接受 档、来源、数据：
    观察总动作 为 观察总动作 加 1。
    观察总成功 为 观察总成功 加 1。
    若 档 等于 空：
        返还 做 文档观察_建回证 用 真、真、来源、「」、数据、做 文档观察_完整统计。
    完。
    档 的 最近来源 为 来源。
    档 的 最近结果 为 数据。
    档 的 最近错误 为 「」。
    档 的 成功数 为 档 的 成功数 加 1。
    档 的 最近统计 为 做 文档观察_完整统计。
    做 文档观察_刷新健康 用 档。
    返还 做 文档观察_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

文档观察_失败 接受 档、来源、错因：
    观察总动作 为 观察总动作 加 1。
    观察总失败 为 观察总失败 加 1。
    若 档 等于 空：
        返还 做 文档观察_建回证 用 真、假、来源、错因、0、做 文档观察_完整统计。
    完。
    档 的 最近来源 为 来源。
    档 的 最近错误 为 错因。
    档 的 失败数 为 档 的 失败数 加 1。
    档 的 最近统计 为 做 文档观察_完整统计。
    做 文档观察_刷新健康 用 档。
    返还 做 文档观察_建回证 用 真、假、来源、错因、0、档 的 最近统计。
完。

文档观察_初始化 接受 档：
    做 文档节点_创建 用 「观察节点」。
    做 文档事件_创建 用 「观察事件」。
    档 的 是否初始化 为 真。
    档 的 最近动作 为 「初始化」。
    档 的 最近来源 为 「文档观察_初始化」。
    档 的 最近统计 为 做 文档观察_完整统计。
    做 文档观察_刷新健康 用 档。
    返还 档。
完。

文档观察_创建 接受 名称：
    档 为 做 文档观察_建档 用 名称。
    做 文档观察_初始化 用 档。
    返还 做 文档观察_登记 用 档。
完。

文档观察_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 非 档 的 是否初始化：
        返还 假。
    完。
    返还 真。
完。

文档观察_建项 接受 类型、目标、数据、来源、时间：
    项 为 新建 观察项。
    项 的 类型 为 类型。
    项 的 目标 为 目标。
    项 的 数据 为 数据。
    项 的 来源 为 来源。
    项 的 时间 为 时间。
    返还 项。
完。

文档观察_切串 接受 文本、上限：
    若 文本 等于 空：
        返还 「」。
    完。
    若 做 字符串长度 用 文本 小于或等于 上限：
        返还 文本。
    完。
    返还 做 子字符串 用 文本、0、上限。
完。

文档观察_分词 接受 文本：
    结果 为 一组：（空）。
    若 文本 等于 空：
        返还 结果。
    完。
    可变 开始 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        码 为 做 字符串取字符码 用 文本、i。
        若 码 等于 32 或者 码 等于 9 或者 码 等于 10 或者 码 等于 13：
            若 i 大于 开始：
                做 追加 用 结果、做 子字符串 用 文本、开始、i 减 开始。
            完。
            开始 为 i 加 1。
        完。
        i 为 i 加 1。
    完。
    若 开始 小于 做 字符串长度 用 文本：
        做 追加 用 结果、做 子字符串 用 文本、开始、做 字符串长度 用 文本 减 开始。
    完。
    返还 结果。
完。

文档观察_含词 接受 列、词：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列：
        若 列 的 第 i 项 等于 词：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

文档观察_性能样本 接受 档、名称、数值、单位：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「性能样本」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「性能样本」、一组：名称、数值、单位、「文档观察」。
    档 的 性能数 为 档 的 性能数 加 1。
    档 的 最近目标 为 名称。
    档 的 最近耗时 为 数值。
    返还 做 文档观察_成功 用 档、「性能样本」、做 文档观察_建项 用 「性能」、名称、做 整数转字符串 用 数值 加 单位、「性能观察」、档 的 性能数。
完。

文档观察_性能快照 接受 档：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「性能快照」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「性能快照」、一组：（空）、「全景照」。
    照 为 做 照采。
    档 的 性能数 为 档 的 性能数 加 1。
    档 的 最近目标 为 照 的 页题。
    档 的 最近耗时 为 照 的 节点数。
    返还 做 文档观察_成功 用 档、「性能快照」、一组：一组：「窗数」、照 的 窗数、一组：「节点数」、照 的 节点数、一组：「脚本数」、照 的 脚本数、一组：「样式数」、照 的 样式数、一组：「摘要」、做 照摘要 用 照。
完。

文档观察_变更通知 接受 档、目标、类型、值：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「变更通知」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「变更通知」、一组：目标、类型、值、「观察器」。
    档 的 变更数 为 档 的 变更数 加 1。
    档 的 最近目标 为 目标。
    返还 做 文档观察_成功 用 档、「变更通知」、做 文档观察_建项 用 「变更」、目标、类型 加 「:」 加 值、「Mutation观察」、档 的 变更数。
完。

文档观察_字符变更 接受 档、目标、文本：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「字符变更」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「字符变更」、一组：目标、文本、「观察器」。
    档 的 变更数 为 档 的 变更数 加 1。
    档 的 最近目标 为 目标。
    返还 做 文档观察_成功 用 档、「字符变更」、做 文档观察_建项 用 「字符」、目标、做 文档观察_切串 用 文本、48、「Mutation观察」、档 的 变更数。
完。

文档观察_尺寸观察 接受 档、目标、宽、高：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「尺寸观察」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「尺寸观察」、一组：目标、宽、高、「尺寸观察」。
    档 的 尺寸数 为 档 的 尺寸数 加 1。
    档 的 最近目标 为 目标。
    档 的 最近尺寸 为 做 整数转字符串 用 宽 加 「x」 加 做 整数转字符串 用 高。
    返还 做 文档观察_成功 用 档、「尺寸观察」、做 文档观察_建项 用 「尺寸」、目标、档 的 最近尺寸、「Resize观察」、档 的 尺寸数。
完。

文档观察_交叉观察 接受 档、目标、比率、阈值：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「交叉观察」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「交叉观察」、一组：目标、比率、阈值、「交叉观察」。
    档 的 交叉数 为 档 的 交叉数 加 1。
    档 的 最近目标 为 目标。
    档 的 最近阈值 为 阈值。
    返还 做 文档观察_成功 用 档、「交叉观察」、做 文档观察_建项 用 「交叉」、目标、「可见比:」 加 做 整数转字符串 用 比率 加 「%」、「Intersection观察」、档 的 交叉数。
完。

文档观察_树遍历 接受 档、词：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「树遍历」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「树遍历」、一组：词、「树Walker」。
    结果 为 做 点找 用 词。
    档 的 遍历数 为 档 的 遍历数 加 1。
    档 的 最近目标 为 词。
    返还 做 文档观察_成功 用 档、「树遍历」、结果。
完。

文档观察_节点迭代 接受 档、编号：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「节点迭代」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「节点迭代」、一组：编号、「节点Iterator」。
    点 为 做 点号 用 编号。
    若 点 等于 空：
        返还 做 文档观察_失败 用 档、「节点迭代」、「节点不存在」。
    完。
    档 的 遍历数 为 档 的 遍历数 加 1。
    返还 做 文档观察_成功 用 档、「节点迭代」、点。
完。

文档观察_令牌列 接受 档、文本：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「令牌列」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「令牌列」、一组：文本、「文档令牌」。
    结果 为 做 文档观察_分词 用 文本。
    档 的 令牌数 为 档 的 令牌数 加 1。
    返还 做 文档观察_成功 用 档、「令牌列」、结果。
完。

文档观察_令牌含有 接受 档、文本、词：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「令牌含有」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「令牌含有」、一组：文本、词、「文档令牌」。
    结果 为 做 文档观察_分词 用 文本。
    档 的 令牌数 为 档 的 令牌数 加 1。
    返还 做 文档观察_成功 用 档、「令牌含有」、一组：词、做 文档观察_含词 用 结果、词。
完。

文档观察_令牌追加 接受 档、文本、词：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「令牌追加」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「令牌追加」、一组：文本、词、「文档令牌」。
    结果 为 做 文档观察_分词 用 文本。
    若 非 做 文档观察_含词 用 结果、词：
        做 追加 用 结果、词。
    完。
    档 的 令牌数 为 档 的 令牌数 加 1。
    返还 做 文档观察_成功 用 档、「令牌追加」、结果。
完。

文档观察_观察总览 接受 档：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「观察总览」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「观察总览」、一组：（空）、「文档观察」。
    照 为 做 照采。
    点串文本 为 做 点找串 用 「按钮」。
    档 的 性能数 为 档 的 性能数 加 1。
    档 的 遍历数 为 档 的 遍历数 加 1。
    档 的 信号数 为 档 的 信号数 加 1。
    返还 做 文档观察_成功 用 档、「观察总览」、一组：做 照摘要 用 照、点串文本、做 照统。
完。

文档观察_信号 接受 档、名称、值、来源：
    若 非 做 文档观察_检查档 用 档：
        返还 做 文档观察_失败 用 档、「信号」、「观察档不可用」。
    完。
    做 文档观察_记录 用 档、「信号」、一组：名称、值、来源、「文档观察」。
    档 的 信号数 为 档 的 信号数 加 1。
    返还 做 文档观察_成功 用 档、「信号」、做 文档观察_建项 用 「信号」、名称、值、来源、档 的 信号数。
完。

文档观察_摘要 接受 档：
    摘要 为 新建 观察摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 性能数 为 档 的 性能数。
    摘要 的 变更数 为 档 的 变更数。
    摘要 的 交叉数 为 档 的 交叉数。
    摘要 的 尺寸数 为 档 的 尺寸数。
    摘要 的 遍历数 为 档 的 遍历数。
    摘要 的 令牌数 为 档 的 令牌数。
    摘要 的 信号数 为 档 的 信号数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 文档观察_刷新健康 用 档。
    返还 摘要。
完。

文档观察_自检 接受 档：
    若 非 做 文档观察_检查档 用 档：
        返还 假。
    完。
    若 做 文档观察_性能样本 用 档、「首屏」、16、「ms」 的 已成 等于 假：
        返还 假。
    完。
    若 做 文档观察_性能快照 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 文档观察_变更通知 用 档、「#app」、「attributes」、「class=ready」 的 已成 等于 假：
        返还 假。
    完。
    若 做 文档观察_尺寸观察 用 档、「.白璃窗」、960、720 的 已成 等于 假：
        返还 假。
    完。
    若 做 文档观察_交叉观察 用 档、「.白璃卡」、87、「0.5」 的 已成 等于 假：
        返还 假。
    完。
    若 做 文档观察_令牌列 用 档、「glass shell pure ai」 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

文档观察_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 文档观察_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        做 文档观察_初始化 用 档。
        返还 做 文档观察_成功 用 档、「处理」、「已初始化」。
    完。
    若 请求 的 动作 等于 「性能样本」：
        返还 做 文档观察_性能样本 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「性能快照」：
        返还 做 文档观察_性能快照 用 档。
    完。
    若 请求 的 动作 等于 「变更通知」：
        返还 做 文档观察_变更通知 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「字符变更」：
        返还 做 文档观察_字符变更 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「尺寸观察」：
        返还 做 文档观察_尺寸观察 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「交叉观察」：
        返还 做 文档观察_交叉观察 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「树遍历」：
        返还 做 文档观察_树遍历 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「节点迭代」：
        返还 做 文档观察_节点迭代 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「令牌列」：
        返还 做 文档观察_令牌列 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「令牌含有」：
        返还 做 文档观察_令牌含有 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「令牌追加」：
        返还 做 文档观察_令牌追加 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「观察总览」：
        返还 做 文档观察_观察总览 用 档。
    完。
    若 请求 的 动作 等于 「信号」：
        返还 做 文档观察_信号 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「摘要」：
        返还 做 文档观察_成功 用 档、「摘要」、做 文档观察_摘要 用 档。
    完。
    返还 做 文档观察_失败 用 档、「处理」、「未知动作」。
完。

Timer 是 一种 事物，含 ID、回调、延迟、触发时间、是重复、已取消、下一个。

TimerManager 是 一种 事物，含 链表头、下一个ID、Nova运行时实例。

TimerManager_创建 接受 Nova运行时实例：
    tm 为 做 堆_分配 用 TimerManager。
    tm 的 链表头 为 0。
    tm 的 下一个ID 为 1。
    tm 的 Nova运行时实例 为 Nova运行时实例。
    返还 tm。
完。

网接口_setTimeout 接受 tm、回调、延迟毫秒：
    返还 做 Timer_添加 用 tm、回调、延迟毫秒、假。
完。

网接口_setInterval 接受 tm、回调、间隔毫秒：
    返还 做 Timer_添加 用 tm、回调、间隔毫秒、真。
完。

Timer_添加 接受 tm、回调、延迟、是重复：
    若 tm 等于 0：
        返还 0。
    完。
    timer 为 做 堆_分配 用 Timer。
    timer 的 ID 为 tm 的 下一个ID。
    tm 的 下一个ID 为 tm 的 下一个ID 加 1。
    timer 的 回调 为 回调。
    timer 的 延迟 为 延迟。
    timer 的 触发时间 为 做 平台_获取高精度时间 加 延迟 乘 1000。
    timer 的 是重复 为 是重复。
    timer 的 已取消 为 假。
    若 tm 的 链表头 等于 0 或者 timer 的 触发时间 小于 tm 的 链表头 的 触发时间：
        timer 的 下一个 为 tm 的 链表头。
        tm 的 链表头 为 timer。
    否则：
        可变 当前 为 tm 的 链表头。
        只要 当前 的 下一个 不等于 0 并且 当前 的 下一个 的 触发时间 小于或等于 timer 的 触发时间：
            当前 为 当前 的 下一个。
        完。
        timer 的 下一个 为 当前 的 下一个。
        当前 的 下一个 为 timer。
    完。
    返还 timer 的 ID。
完。

网接口_clearTimeout 接受 tm、id：
    若 tm 等于 0：
        返还。
    完。
    可变 当前 为 tm 的 链表头。
    只要 当前 不等于 0：
        若 当前 的 ID 等于 id：
            当前 的 已取消 为 真。
            返还。
        完。
        当前 为 当前 的 下一个。
    完。
完。

网接口_clearInterval 接受 tm、id：
    做 网接口_clearTimeout 用 tm、id。
完。

Timer_处理到期 接受 tm、当前时间：
    若 tm 等于 0：
        返还。
    完。
    只要 tm 的 链表头 不等于 0 并且 tm 的 链表头 的 触发时间 小于或等于 当前时间：
        timer 为 tm 的 链表头。
        tm 的 链表头 为 timer 的 下一个。
        若 非 timer 的 已取消：
            做 Nova_调用函数 用 tm 的 Nova运行时实例、timer 的 回调、0、0。
            若 timer 的 是重复 并且 非 timer 的 已取消：
                timer 的 触发时间 为 当前时间 加 timer 的 延迟 乘 1000。
                做 Timer_添加 用 tm、timer 的 回调、timer 的 延迟、真。
            完。
        完。
        做 堆_释放 用 timer。
    完。
完。

RAFCallback 是 一种 事物，含 ID、回调、已取消、下一个。

全局RAF链 为 0。

全局RAF_ID 为 0。

网接口_requestAnimationFrame 接受 回调：
    raf 为 做 堆_分配 用 RAFCallback。
    全局RAF_ID 为 全局RAF_ID 加 1。
    raf 的 ID 为 全局RAF_ID。
    raf 的 回调 为 回调。
    raf 的 已取消 为 假。
    raf 的 下一个 为 全局RAF链。
    全局RAF链 为 raf。
    返还 raf 的 ID。
完。

网接口_cancelAnimationFrame 接受 id：
    可变 当前 为 全局RAF链。
    只要 当前 不等于 0：
        若 当前 的 ID 等于 id：
            当前 的 已取消 为 真。
            返还。
        完。
        当前 为 当前 的 下一个。
    完。
完。

RAF_执行所有 接受 Nova运行时实例、时间戳：
    可变 当前 为 全局RAF链。
    全局RAF链 为 0。
    只要 当前 不等于 0：
        下一个 为 当前 的 下一个。
        若 非 当前 的 已取消：
            做 Nova_调用函数_带参 用 Nova运行时实例、当前 的 回调、时间戳。
            定时器_RAF执行数 为 定时器_RAF执行数 加 1。
        完。
        做 堆_释放 用 当前。
        当前 为 下一个。
    完。
完。

IdleCallback 是 一种 事物，含 ID、回调、超时、已取消、下一个。

可变 全局Idle链 为 0。

可变 全局Idle_ID 为 0。

网接口_requestIdleCallback 接受 回调、超时ms：
    idle 为 新建 IdleCallback。
    全局Idle_ID 为 全局Idle_ID 加 1。
    idle 的 ID 为 全局Idle_ID。
    idle 的 回调 为 回调。
    idle 的 超时 为 超时ms。
    idle 的 已取消 为 假。
    idle 的 下一个 为 全局Idle链。
    全局Idle链 为 idle。
    返还 idle 的 ID。
完。

网接口_cancelIdleCallback 接受 id：
    可变 当前 为 全局Idle链。
    只要 当前 不等于 0：
        若 当前 的 ID 等于 id：
            当前 的 已取消 为 真。
            返还。
        完。
        当前 为 当前 的 下一个。
    完。
完。

Idle_执行 接受 Nova运行时实例、剩余时间ms：
    可变 当前 为 全局Idle链。
    全局Idle链 为 0。
    只要 当前 不等于 0 并且 剩余时间ms 大于 0：
        下一个 为 当前 的 下一个。
        若 非 当前 的 已取消：
            做 Nova_调用函数_带参 用 Nova运行时实例、当前 的 回调、剩余时间ms。
            定时器_Idle执行数 为 定时器_Idle执行数 加 1。
        完。
        做 堆_释放 用 当前。
        当前 为 下一个。
    完。
    若 当前 不等于 0：
        可变 尾 为 当前。
        只要 尾 的 下一个 不等于 0：
            尾 为 尾 的 下一个。
        完。
        尾 的 下一个 为 全局Idle链。
        全局Idle链 为 当前。
    完。
完。

Timer_数量 接受 tm：
    若 tm 等于 0：
        返还 0。
    完。
    可变 数量 为 0。
    可变 当前 为 tm 的 链表头。
    只要 当前 不等于 0：
        若 非 当前 的 已取消：
            数量 为 数量 加 1。
        完。
        当前 为 当前 的 下一个。
    完。
    返还 数量。
完。

Timer_清理已取消 接受 tm：
    若 tm 等于 0：
        返还。
    完。
    只要 tm 的 链表头 不等于 0 并且 tm 的 链表头 的 已取消：
        旧 为 tm 的 链表头。
        tm 的 链表头 为 旧 的 下一个。
        做 堆_释放 用 旧。
    完。
    可变 当前 为 tm 的 链表头。
    只要 当前 不等于 0 并且 当前 的 下一个 不等于 0：
        若 当前 的 下一个 的 已取消：
            旧 为 当前 的 下一个。
            当前 的 下一个 为 旧 的 下一个。
            做 堆_释放 用 旧。
        否则：
            当前 为 当前 的 下一个。
        完。
    完。
完。

Timer_下一个触发时间 接受 tm：
    若 tm 等于 0 或者 tm 的 链表头 等于 0：
        返还 0。
    完。
    返还 tm 的 链表头 的 触发时间。
完。

Nova_调用函数 接受 Nova运行时实例、回调、参数1、参数2：
    若 回调 等于 0：
        返还。
    完。
    定时器_回调执行数 为 定时器_回调执行数 加 1。
    做 Nova调用 用 回调、参数1、参数2、2。
完。

Nova_调用函数_带参 接受 Nova运行时实例、回调、参数：
    若 回调 等于 0：
        返还。
    完。
    定时器_回调执行数 为 定时器_回调执行数 加 1。
    做 Nova调用 用 回调、参数、0、1。
完。

可变 定时器_创建总数 为 0。

可变 定时器_回调执行数 为 0。

可变 定时器_RAF执行数 为 0。

可变 定时器_Idle执行数 为 0。

可变 定时器_取消数 为 0。

定时器_统计 接受 零参：
    返还 一本字典：创建 对应 定时器_创建总数、回调执行 对应 定时器_回调执行数、RAF执行 对应 定时器_RAF执行数、Idle执行 对应 定时器_Idle执行数、取消 对应 定时器_取消数。
完。

定时器_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 定时器_创建总数 大于 100000 并且 定时器_取消数 小于 定时器_创建总数 除以 2：
        做 追加 用 问题、「定时器可能泄漏: 创建多取消少」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「定时器子系统健康」。
    完。
    返还 问题。
完。

定时器_重置统计 接受 零参：
    定时器_创建总数 为 0。
    定时器_回调执行数 为 0。
    定时器_RAF执行数 为 0。
    定时器_Idle执行数 为 0。
    定时器_取消数 为 0。
完。

定时器_完整统计 接受 零参：
    返还 一组：定时器_创建总数、定时器_回调执行数、定时器_RAF执行数、定时器_Idle执行数、定时器_取消数。
完。

IntersectionObserverEntry 是 一种 事物，含 时间、rootBounds_x、rootBounds_y、rootBounds_w、rootBounds_h、boundingClientRect_x、boundingClientRect_y、boundingClientRect_w、boundingClientRect_h、intersectionRect_x、intersectionRect_y、intersectionRect_w、intersectionRect_h、intersectionRatio、isIntersecting、target。

IntersectionObserver 是 一种 事物，含 回调、root、rootMargin、阈值、阈值数、目标列表、目标数、目标容量、待通知、待通知数。

IntersectionObserver_创建 接受 回调、root、rootMargin、阈值、阈值数：
    io 为 做 堆_分配 用 IntersectionObserver。
    io 的 回调 为 回调。
    io 的 root 为 root。
    io 的 rootMargin 的 第 0 项 为 rootMargin。
    io 的 rootMargin 的 第 1 项 为 rootMargin。
    io 的 rootMargin 的 第 2 项 为 rootMargin。
    io 的 rootMargin 的 第 3 项 为 rootMargin。
    可变 i 为 0。
    只要 i 小于 阈值数 并且 i 小于 16：
        io 的 阈值 的 第 i 项 为 阈值。
        i 为 i 加 1。
    完。
    io 的 阈值数 为 若 阈值数 大于 16 则 16 否则 阈值数。
    io 的 目标容量 为 64。
    io 的 目标列表 为 做 堆_分配_数组 用 64 乘 8。
    io 的 目标数 为 0。
    io 的 待通知 为 0。
    io 的 待通知数 为 0。
    返还 io。
完。

IntersectionObserver_observe 接受 io、target：
    若 io 等于 0 或者 target 等于 0 或者 io 的 目标数 大于或等于 io 的 目标容量：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 io 的 目标数：
        若 做 内存_读64 用 io 的 目标列表 加 i 乘 8 等于 target：
            返还。
        完。
        i 为 i 加 1。
    完。
    做 内存_写64 用 io 的 目标列表 加 io 的 目标数 乘 8、target。
    io 的 目标数 为 io 的 目标数 加 1。
完。

IntersectionObserver_unobserve 接受 io、target：
    若 io 等于 0 或者 target 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 io 的 目标数：
        若 做 内存_读64 用 io 的 目标列表 加 i 乘 8 等于 target：
            做 内存_写64 用 io 的 目标列表 加 i 乘 8、做 内存_读64 用 io 的 目标列表 加 (io 的 目标数 减 1) 乘 8。
            io 的 目标数 为 io 的 目标数 减 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

IntersectionObserver_disconnect 接受 io：
    若 io 等于 0：
        返还。
    完。
    io 的 目标数 为 0。
    io 的 待通知数 为 0。
完。

IntersectionObserver_检查 接受 io、viewportX、viewportY、viewportW、viewportH：
    若 io 等于 0 或者 io 的 目标数 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 io 的 目标数：
        target 为 做 内存_读64 用 io 的 目标列表 加 i 乘 8。
        若 target 等于 0：
            i 为 i 加 1。
            继续。
        完。
        交叉面积 为 0。
        元素面积 为 1。
        ratio 为 交叉面积 乘 1024 除以 元素面积。
        可变 t 为 0。
        只要 t 小于 io 的 阈值数：
            若 ratio 大于或等于 io 的 阈值 的 第 t 项：
            完。
            t 为 t 加 1。
        完。
        i 为 i 加 1。
    完。
    若 io 的 待通知数 大于 0 并且 io 的 回调 不等于 0：
        io 的 待通知数 为 0。
    完。
完。

矩形_交叉 接受 ax、ay、aw、ah、bx、by、bw、bh：
    左 为 若 ax 大于 bx 则 ax 否则 bx。
    上 为 若 ay 大于 by 则 ay 否则 by。
    右 为 若 ax 加 aw 小于 bx 加 bw 则 ax 加 aw 否则 bx 加 bw。
    下 为 若 ay 加 ah 小于 by 加 bh 则 ay 加 ah 否则 by 加 bh。
    若 右 小于或等于 左 或者 下 小于或等于 上：
        返还 0。
    完。
    返还 (右 减 左) 乘 (下 减 上)。
完。

矩形_面积 接受 w、h：
    返还 w 乘 h。
完。

矩形_包含 接受 ax、ay、aw、ah、px、py：
    返还 px 大于或等于 ax 并且 px 小于 ax 加 aw 并且 py 大于或等于 ay 并且 py 小于 ay 加 ah。
完。

ResizeObserverEntry 是 一种 事物，含 target、contentRect_w、contentRect_h、borderBoxSize_w、borderBoxSize_h。

ResizeObserver 是 一种 事物，含 回调、目标列表、目标数、目标容量、上次大小。

ResizeObserver_创建 接受 回调：
    ro 为 新建 ResizeObserver。
    ro 的 回调 为 回调。
    ro 的 目标列表 为 一组：（空）。
    ro 的 目标数 为 0。
    ro 的 目标容量 为 64。
    ro 的 上次大小 为 一组：（空）。
    返还 ro。
完。

ResizeObserver_observe 接受 ro、target：
    若 ro 等于 0 或者 target 等于 0 或者 ro 的 目标数 大于或等于 ro 的 目标容量：
        返还。
    完。
    做 追加 用 ro 的 目标列表、target。
    做 追加 用 ro 的 上次大小、一组：0、0。
    ro 的 目标数 为 ro 的 目标数 加 1。
完。

ResizeObserver_unobserve 接受 ro、target：
    若 ro 等于 0：
        返还。
    完。
    可变 新列表 为 一组：（空）。
    可变 新大小 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 ro 的 目标数：
        若 ro 的 目标列表 的 第 i 项 不等于 target：
            做 追加 用 新列表、ro 的 目标列表 的 第 i 项。
            做 追加 用 新大小、ro 的 上次大小 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    ro 的 目标列表 为 新列表。
    ro 的 上次大小 为 新大小。
    ro 的 目标数 为 做 计数 用 新列表。
完。

ResizeObserver_disconnect 接受 ro：
    若 ro 等于 0：
        返还。
    完。
    ro 的 目标列表 为 一组：（空）。
    ro 的 上次大小 为 一组：（空）。
    ro 的 目标数 为 0。
完。

PERF_MARK 为 0。

PERF_MEASURE 为 1。

PERF_NAVIGATION 为 2。

PERF_RESOURCE 为 3。

PERF_PAINT 为 4。

PERF_LONGTASK 为 5。

PERF_LAYOUT_SHIFT 为 6。

PerformanceEntry 是 一种 事物，含 名称、类型、起始时间、持续时间。

可变 性能条目列表 为 一组：（空）。

可变 性能条目数 为 0。

Performance_mark 接受 名称：
    entry 为 新建 PerformanceEntry。
    entry 的 名称 为 名称。
    entry 的 类型 为 PERF_MARK。
    entry 的 起始时间 为 做 平台_获取高精度时间。
    entry 的 持续时间 为 0。
    做 追加 用 性能条目列表、entry。
    性能条目数 为 性能条目数 加 1。
完。

Performance_measure 接受 名称、起始标记、结束标记：
    entry 为 新建 PerformanceEntry。
    entry 的 名称 为 名称。
    entry 的 类型 为 PERF_MEASURE。
    entry 的 起始时间 为 0。
    entry 的 持续时间 为 0。
    做 追加 用 性能条目列表、entry。
    性能条目数 为 性能条目数 加 1。
完。

Performance_获取条目 接受 类型：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 性能条目数：
        若 性能条目列表 的 第 i 项 的 类型 等于 类型：
            做 追加 用 结果、性能条目列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

Performance_清空 接受 零参：
    性能条目列表 为 一组：（空）。
    性能条目数 为 0。
完。

PERF类型_名称 接受 类型：
    若 类型 等于 PERF_MARK：
        返还 「mark」。
    完。
    若 类型 等于 PERF_MEASURE：
        返还 「measure」。
    完。
    若 类型 等于 PERF_NAVIGATION：
        返还 「navigation」。
    完。
    若 类型 等于 PERF_RESOURCE：
        返还 「resource」。
    完。
    若 类型 等于 PERF_PAINT：
        返还 「paint」。
    完。
    若 类型 等于 PERF_LONGTASK：
        返还 「longtask」。
    完。
    若 类型 等于 PERF_LAYOUT_SHIFT：
        返还 「layout-shift」。
    完。
    返还 「unknown」。
完。

可变 IO_总创建数 为 0。

可变 IO_总观察数 为 0。

可变 RO_总创建数 为 0。

可变 RO_总观察数 为 0。

观察者_统计 接受 零参：
    返还 一本字典：IO创建 对应 IO_总创建数、IO观察 对应 IO_总观察数、RO创建 对应 RO_总创建数、RO观察 对应 RO_总观察数、性能条目 对应 性能条目数。
完。

观察者_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 IO_总观察数 大于 10000：
        做 追加 用 问题、「IO观察目标过多」。
    完。
    若 性能条目数 大于 100000：
        做 追加 用 问题、「性能条目过多」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「观察者子系统健康」。
    完。
    返还 问题。
完。

观察者_重置统计 接受 零参：
    IO_总创建数 为 0。
    IO_总观察数 为 0。
    RO_总创建数 为 0。
    RO_总观察数 为 0。
完。

观察者_完整统计 接受 零参：
    返还 一组：IO_总创建数、IO_总观察数、RO_总创建数、RO_总观察数、性能条目数。
完。

Location 是 一种 事物，含 当前URL、浏览上下文。

Location_创建 接受 url：
    loc 为 做 堆_分配 用 Location。
    loc 的 当前URL 为 做 URL_解析 用 url。
    loc 的 浏览上下文 为 0。
    返还 loc。
完。

Location_assign 接受 loc、url：
    若 loc 等于 0：
        返还。
    完。
    做 Navigation_导航 用 loc 的 浏览上下文、url、真。
完。

Location_replace 接受 loc、url：
    若 loc 等于 0：
        返还。
    完。
    做 Navigation_导航 用 loc 的 浏览上下文、url、假。
完。

Location_reload 接受 loc：
    若 loc 等于 0 或者 loc 的 当前URL 等于 0：
        返还。
    完。
    做 Navigation_导航 用 loc 的 浏览上下文、loc 的 当前URL 的 完整、假。
完。

Location_href 接受 loc：
    若 loc 等于 0 或者 loc 的 当前URL 等于 0：
        返还 「」。
    完。
    返还 loc 的 当前URL 的 完整。
完。

Location_pathname 接受 loc：
    若 loc 等于 0 或者 loc 的 当前URL 等于 0：
        返还 「/」。
    完。
    返还 loc 的 当前URL 的 路径。
完。

Location_search 接受 loc：
    若 loc 等于 0 或者 loc 的 当前URL 等于 0：
        返还 「」。
    完。
    返还 loc 的 当前URL 的 查询。
完。

Location_hash 接受 loc：
    若 loc 等于 0 或者 loc 的 当前URL 等于 0：
        返还 「」。
    完。
    返还 loc 的 当前URL 的 哈希。
完。

历史Entry 是 一种 事物，含 URL、状态、标题、滚动位置X、滚动位置Y、文档快照、有快照。

Session历史 是 一种 事物，含 条目、条目数、条目容量、当前索引。

历史 是 一种 事物，含 会话、浏览上下文、location。

历史_创建 接受 零参：
    h 为 做 堆_分配 用 历史。
    sh 为 做 堆_分配 用 Session历史。
    sh 的 条目容量 为 256。
    sh 的 条目 为 做 堆_分配_数组 用 256 乘 8。
    sh 的 条目数 为 0。
    sh 的 当前索引 为 负 1。
    h 的 会话 为 sh。
    h 的 浏览上下文 为 0。
    h 的 location 为 0。
    返还 h。
完。

历史_length 接受 h：
    若 h 等于 0 或者 h 的 会话 等于 0：
        返还 0。
    完。
    返还 h 的 会话 的 条目数。
完。

历史_state 接受 h：
    若 h 等于 0 或者 h 的 会话 等于 0：
        返还 0。
    完。
    sh 为 h 的 会话。
    若 sh 的 当前索引 小于 0 或者 sh 的 当前索引 大于或等于 sh 的 条目数：
        返还 0。
    完。
    entry 为 做 内存_读64 用 sh 的 条目 加 sh 的 当前索引 乘 8。
    若 entry 等于 0：
        返还 0。
    完。
    返还 entry 的 状态。
完。

历史_pushState 接受 h、状态、标题、url：
    若 h 等于 0 或者 h 的 会话 等于 0：
        返还。
    完。
    sh 为 h 的 会话。
    若 url 不等于 0 并且 h 的 location 不等于 0 并且 h 的 location 的 当前URL 不等于 0：
        新url 为 做 URL_解析 用 url。
        若 新url 不等于 0 并且 非 做 URL_相同源 用 新url、h 的 location 的 当前URL：
            返还。
        完。
    完。
    sh 的 条目数 为 sh 的 当前索引 加 1。
    entry 为 做 堆_分配 用 历史Entry。
    若 url 不等于 0：
        entry 的 URL 为 做 URL_解析 用 url。
    否则若 h 的 location 不等于 0：
        entry 的 URL 为 h 的 location 的 当前URL。
    完。
    entry 的 状态 为 状态。
    entry 的 标题 为 若 标题 不等于 0 则 做 字符串_复制 用 标题 否则 0。
    entry 的 滚动位置X 为 0。
    entry 的 滚动位置Y 为 0。
    entry 的 文档快照 为 0。
    entry 的 有快照 为 假。
    若 sh 的 条目数 小于 sh 的 条目容量：
        做 内存_写64 用 sh 的 条目 加 sh 的 条目数 乘 8、entry。
        sh 的 条目数 为 sh 的 条目数 加 1。
        sh 的 当前索引 为 sh 的 条目数 减 1。
    完。
    若 url 不等于 0 并且 h 的 location 不等于 0：
        h 的 location 的 当前URL 为 entry 的 URL。
    完。
完。

历史_replaceState 接受 h、状态、标题、url：
    若 h 等于 0 或者 h 的 会话 等于 0：
        返还。
    完。
    sh 为 h 的 会话。
    若 sh 的 当前索引 小于 0：
        返还。
    完。
    若 url 不等于 0 并且 h 的 location 不等于 0 并且 h 的 location 的 当前URL 不等于 0：
        新url 为 做 URL_解析 用 url。
        若 新url 不等于 0 并且 非 做 URL_相同源 用 新url、h 的 location 的 当前URL：
            返还。
        完。
    完。
    entry 为 做 内存_读64 用 sh 的 条目 加 sh 的 当前索引 乘 8。
    若 entry 等于 0：
        返还。
    完。
    entry 的 状态 为 状态。
    若 标题 不等于 0：
        entry 的 标题 为 做 字符串_复制 用 标题。
    完。
    若 url 不等于 0：
        entry 的 URL 为 做 URL_解析 用 url。
        若 h 的 location 不等于 0：
            h 的 location 的 当前URL 为 entry 的 URL。
        完。
    完。
完。

历史_go 接受 h、delta：
    若 h 等于 0 或者 h 的 会话 等于 0：
        返还。
    完。
    若 delta 等于 0：
        若 h 的 location 不等于 0：
            做 Location_reload 用 h 的 location。
        完。
        返还。
    完。
    sh 为 h 的 会话。
    新索引 为 sh 的 当前索引 加 delta。
    若 新索引 小于 0 或者 新索引 大于或等于 sh 的 条目数：
        返还。
    完。
    若 sh 的 当前索引 大于或等于 0：
        当前 为 做 内存_读64 用 sh 的 条目 加 sh 的 当前索引 乘 8。
        若 当前 不等于 0：
        完。
    完。
    sh 的 当前索引 为 新索引。
    entry 为 做 内存_读64 用 sh 的 条目 加 新索引 乘 8。
    若 entry 等于 0：
        返还。
    完。
    若 h 的 location 不等于 0 并且 entry 的 URL 不等于 0：
        h 的 location 的 当前URL 为 entry 的 URL。
    完。
    做 历史_触发popstate 用 h、entry 的 状态。
完。

PERF_NAVIGATION 为 1。

PERF_RESOURCE 为 2。

PERF_MARK 为 3。

PERF_MEASURE 为 4。

PERF_PAINT 为 5。

PERF_LONGTASK 为 6。

PerformanceEntry 是 一种 事物，含 名称。

ResourceTiming 是 一种 事物，含 initiatorType、domainLookupStart、connectStart、requestStart、transferSize。

NavigationTiming 是 一种 事物，含 类型、redirectCount、unload事件Start、redirectStart、fetchStart、domContentLoadedEnd、load事件Start。

Performance 是 一种 事物，含 时间原点、条目列表、条目数量、条目容量、标记计数、观察者列表、观察者数量、导航计时。

可变 全局Performance 为 0。

Performance_获取 接受 零参：
    若 全局Performance 等于 0：
        全局Performance 为 做 堆_分配 用 Performance。
        全局Performance 的 时间原点 为 做 平台_获取高精度时间。
        全局Performance 的 条目容量 为 512。
        全局Performance 的 条目列表 为 做 堆_分配_数组 用 512 乘 8。
        全局Performance 的 条目数量 为 0。
        全局Performance 的 标记计数 为 0。
        全局Performance 的 观察者列表 为 做 堆_分配_数组 用 32 乘 8。
        全局Performance 的 观察者数量 为 0。
        全局Performance 的 导航计时 为 0。
    完。
    返还 全局Performance。
完。

Performance_now 接受 零参：
    perf 为 做 Performance_获取。
    返还 做 平台_获取高精度时间 减 perf 的 时间原点。
完。

Performance_添加条目 接受 perf、entry：
    若 perf 的 条目数量 大于或等于 perf 的 条目容量：
        返还。
    完。
    做 内存_写64 用 perf 的 条目列表 加 perf 的 条目数量 乘 8、entry。
    perf 的 条目数量 为 perf 的 条目数量 加 1。
    做 Performance_通知观察者 用 perf、entry。
完。

Performance_查找标记 接受 perf、名称：
    可变 i 为 perf 的 条目数量 减 1。
    只要 i 大于或等于 0：
        e 为 做 内存_读64 用 perf 的 条目列表 加 i 乘 8。
        若 e 不等于 0 并且 e 的 类型 等于 PERF_MARK 并且 做 字符串_相等 用 e 的 名称、名称：
            返还 e。
        完。
        i 为 i 减 1。
    完。
    返还 0。
完。

Performance_getEntries 接受 perf：
    返还 一组：perf 的 条目列表、perf 的 条目数量。
完。

Performance_getEntriesByType 接受 perf、类型：
    结果 为 做 堆_分配_数组 用 perf 的 条目数量 乘 8。
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 perf 的 条目数量：
        e 为 做 内存_读64 用 perf 的 条目列表 加 i 乘 8。
        若 e 不等于 0 并且 e 的 类型 等于 类型：
            做 内存_写64 用 结果 加 计数 乘 8、e。
            计数 为 计数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：结果、计数。
完。

Performance_getEntriesByName 接受 perf、名称：
    结果 为 做 堆_分配_数组 用 perf 的 条目数量 乘 8。
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 perf 的 条目数量：
        e 为 做 内存_读64 用 perf 的 条目列表 加 i 乘 8。
        若 e 不等于 0 并且 做 字符串_相等 用 e 的 名称、名称：
            做 内存_写64 用 结果 加 计数 乘 8、e。
            计数 为 计数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：结果、计数。
完。

Performance_clearMarks 接受 perf、名称：
    若 名称 等于 0：
        做 Performance_清除类型 用 perf、PERF_MARK。
    否则：
        做 Performance_清除名称 用 perf、PERF_MARK、名称。
    完。
完。

Performance_clearMeasures 接受 perf、名称：
    若 名称 等于 0：
        做 Performance_清除类型 用 perf、PERF_MEASURE。
    否则：
        做 Performance_清除名称 用 perf、PERF_MEASURE、名称。
    完。
完。

Performance_清除类型 接受 perf、类型：
    可变 写 为 0。
    可变 i 为 0。
    只要 i 小于 perf 的 条目数量：
        e 为 做 内存_读64 用 perf 的 条目列表 加 i 乘 8。
        若 e 不等于 0 并且 e 的 类型 不等于 类型：
            做 内存_写64 用 perf 的 条目列表 加 写 乘 8、e。
            写 为 写 加 1。
        完。
        i 为 i 加 1。
    完。
    perf 的 条目数量 为 写。
完。

Performance_清除名称 接受 perf、类型、名称：
    可变 写 为 0。
    可变 i 为 0。
    只要 i 小于 perf 的 条目数量：
        e 为 做 内存_读64 用 perf 的 条目列表 加 i 乘 8。
        若 e 不等于 0 并且 非 (e 的 类型 等于 类型 并且 做 字符串_相等 用 e 的 名称、名称)：
            做 内存_写64 用 perf 的 条目列表 加 写 乘 8、e。
            写 为 写 加 1。
        完。
        i 为 i 加 1。
    完。
    perf 的 条目数量 为 写。
完。

MutationObserver_通知characterData 接受 target、旧值：
    可变 i 为 0。
    只要 i 小于 全局_MO列表_数量：
        mo 为 做 内存_读64 用 全局_MO列表 加 i 乘 8。
        若 mo 不等于 0 并且 做 MO_观察了目标 用 mo、target、MUT_CHARACTER_DATA：
            record 为 做 堆_分配 用 MutationRecord。
            record 的 类型 为 「characterData」。
            record 的 目标 为 target。
            若 mo 的 记录旧值：
                record 的 旧值 为 旧值。
            完。
            做 MO_入队记录 用 mo、record。
        完。
        i 为 i 加 1。
    完。
完。

MutationObserver_刷新所有 接受 零参：
    可变 i 为 0。
    只要 i 小于 全局_MO列表_数量：
        mo 为 做 内存_读64 用 全局_MO列表 加 i 乘 8。
        若 mo 不等于 0 并且 mo 的 队列大小 大于 0：
            records 为 做 MutationObserver_takeRecords 用 mo。
            若 records 不等于 0 并且 mo 的 回调 不等于 0：
                做 回调_调用2 用 mo 的 回调、records、mo。
            完。
        完。
        i 为 i 加 1。
    完。
完。

PERF_TYPE_MARK 为 1。

PERF_TYPE_MEASURE 为 2。

PERF_TYPE_NAVIGATION 为 4。

PERF_TYPE_RESOURCE 为 8。

PERF_TYPE_PAINT 为 16。

PERF_TYPE_LONGTASK 为 32。

PERF_TYPE_LAYOUT_SHIFT 为 64。

PERF_TYPE_LARGEST_CONTENTFUL_PAINT 为 128。

PERF_TYPE_FIRST_INPUT 为 256。

PERF_TYPE_元素 为 512。

PerformanceObserverEntry 是 一种 事物，含 名称、类型、类型掩码、开始时间、持续时间、initiatorType、transferSize、渲染时间、加载时间、值、下一个。

PerformanceObserver 是 一种 事物，含 回调、类型过滤、buffered、队列头、队列大小。

PerformanceObserver_创建 接受 回调：
    po 为 做 堆_分配 用 PerformanceObserver。
    po 的 回调 为 回调。
    po 的 类型过滤 为 0。
    po 的 buffered 为 假。
    po 的 队列头 为 0。
    po 的 队列大小 为 0。
    返还 po。
完。

PerformanceObserver_observe 接受 po、类型过滤、buffered：
    若 po 等于 0：
        返还。
    完。
    po 的 类型过滤 为 类型过滤。
    po 的 buffered 为 buffered。
完。

PerformanceObserver_disconnect 接受 po：
    若 po 等于 0：
        返还。
    完。
    po 的 类型过滤 为 0。
    po 的 队列头 为 0。
    po 的 队列大小 为 0。
完。

PerformanceObserver_通知 接受 entry：
    若 entry 等于 0：
        返还。
    完。
完。

Performance_LCP记录 接受 元素、渲染时间、加载时间、大小：
    entry 为 做 堆_分配 用 PerformanceObserverEntry。
    entry 的 类型 为 「largest-contentful-paint」。
    entry 的 类型掩码 为 PERF_TYPE_LARGEST_CONTENTFUL_PAINT。
    entry 的 渲染时间 为 渲染时间。
    entry 的 加载时间 为 加载时间。
    做 PerformanceObserver_通知 用 entry。
完。

Performance_FID记录 接受 事件类型、开始时间、处理时间：
    entry 为 做 堆_分配 用 PerformanceObserverEntry。
    entry 的 类型 为 「first-input」。
    entry 的 类型掩码 为 PERF_TYPE_FIRST_INPUT。
    entry 的 开始时间 为 开始时间。
    entry 的 持续时间 为 处理时间 减 开始时间。
    做 PerformanceObserver_通知 用 entry。
完。

Performance_CLS记录 接受 值：
    entry 为 做 堆_分配 用 PerformanceObserverEntry。
    entry 的 类型 为 「layout-shift」。
    entry 的 类型掩码 为 PERF_TYPE_LAYOUT_SHIFT。
    entry 的 值 为 值。
    做 PerformanceObserver_通知 用 entry。
完。

PerformanceObserver 是 一种 事物，含 回调、观察类型、缓冲条目、缓冲数量、缓冲容量、已连接。

Performance_通知观察者 接受 perf、entry：
    可变 i 为 0。
    只要 i 小于 perf 的 观察者数量：
        o 为 做 内存_读64 用 perf 的 观察者列表 加 i 乘 8。
        若 o 不等于 0 并且 o 的 已连接：
            若 o 的 缓冲数量 小于 o 的 缓冲容量：
                做 内存_写64 用 o 的 缓冲条目 加 o 的 缓冲数量 乘 8、entry。
                o 的 缓冲数量 为 o 的 缓冲数量 加 1。
            完。
            若 o 的 回调 不等于 0：
                做 Microtask_排队_可调用 用 o 的 回调、o。
            完。
        完。
        i 为 i 加 1。
    完。
完。

PerformanceObserver_takeRecords 接受 观察者：
    若 观察者 等于 0：
        返还 一组：0、0。
    完。
    结果 为 一组：观察者 的 缓冲条目、观察者 的 缓冲数量。
    观察者 的 缓冲条目 为 做 堆_分配_数组 用 观察者 的 缓冲容量 乘 8。
    观察者 的 缓冲数量 为 0。
    返还 结果。
完。

Performance_记录资源 接受 名称、启动类型、dns开始、dns结束、连接开始、连接结束、请求开始、响应开始、响应结束、传输大小：
    perf 为 做 Performance_获取。
    rt 为 做 堆_分配 用 ResourceTiming。
    rt 的 initiatorType 为 做 字符串_复制 用 启动类型。
    rt 的 domainLookupStart 为 dns开始。
    rt 的 domainLookupEnd 为 dns结束。
    rt 的 connectStart 为 连接开始。
    rt 的 connectEnd 为 连接结束。
    rt 的 requestStart 为 请求开始。
    rt 的 responseStart 为 响应开始。
    rt 的 responseEnd 为 响应结束。
    rt 的 transferSize 为 传输大小。
    entry 为 做 堆_分配 用 PerformanceEntry。
    entry 的 名称 为 做 字符串_复制 用 名称。
    entry 的 类型 为 PERF_RESOURCE。
    entry 的 开始时间 为 dns开始。
    entry 的 持续时间 为 响应结束 减 dns开始。
    entry 的 扩展 为 rt。
    做 Performance_添加条目 用 perf、entry。
完。

Performance_记录首次绘制 接受 零参：
    perf 为 做 Performance_获取。
    entry 为 做 堆_分配 用 PerformanceEntry。
    entry 的 名称 为 「first-paint」。
    entry 的 类型 为 PERF_PAINT。
    entry 的 开始时间 为 做 Performance_now。
    entry 的 持续时间 为 0。
    做 Performance_添加条目 用 perf、entry。
完。

Performance_记录首次有内容绘制 接受 零参：
    perf 为 做 Performance_获取。
    entry 为 做 堆_分配 用 PerformanceEntry。
    entry 的 名称 为 「first-contentful-paint」。
    entry 的 类型 为 PERF_PAINT。
    entry 的 开始时间 为 做 Performance_now。
    entry 的 持续时间 为 0。
    做 Performance_添加条目 用 perf、entry。
完。

PerformanceEntry 是 一种 事物，含 名称、类型、开始时间、持续时间、下一个。

可变 性能条目头 为 0。

DevTools_Performance处理 接受 session、msg：
    若 session 等于 0 或者 msg 等于 0：
        返还。
    完。
    方法 为 msg 的 方法。
    若 做 字符串_相等 用 方法、「Performance.enable」：
        session 的 performance启用 为 真。
        msg 的 结果 为 「{}」。
    否则若 做 字符串_相等 用 方法、「Performance.getMetrics」：
    完。
完。

DeviceMetrics 是 一种 事物，含 宽度、高度、设备缩放、是移动、方向类型、方向角度。

DevTools_Emulation处理 接受 session、msg：
    若 session 等于 0 或者 msg 等于 0：
        返还。
    完。
    方法 为 msg 的 方法。
    若 做 字符串_相等 用 方法、「Emulation.setDeviceMetricsOverride」：
    否则若 做 字符串_相等 用 方法、「Emulation.clearDeviceMetricsOverride」：
    否则若 做 字符串_相等 用 方法、「Emulation.setGeolocationOverride」：
    否则若 做 字符串_相等 用 方法、「Emulation.setUser智能体Override」：
    否则若 做 字符串_相等 用 方法、「Emulation.setEmulated媒体」：
    否则若 做 字符串_相等 用 方法、「Emulation.setCPUThrottlingRate」：
    否则若 做 字符串_相等 用 方法、「Emulation.setTimezoneOverride」：
    完。
完。

DevTools_存储处理 接受 session、msg：
    若 session 等于 0 或者 msg 等于 0：
        返还。
    完。
    方法 为 msg 的 方法。
    若 做 字符串_相等 用 方法、「存储.get存储KeyForFrame」：
    否则若 做 字符串_相等 用 方法、「存储.clearDataForOrigin」：
    否则若 做 字符串_相等 用 方法、「存储.getCookies」：
    否则若 做 字符串_相等 用 方法、「存储.setCookies」：
    完。
完。

DevTools_解析参数_int 接受 json、key：
    若 json 等于 0 或者 key 等于 0：
        返还 0。
    完。
    位置 为 做 字符串_查找 用 json、key。
    若 位置 小于 0：
        返还 0。
    完。
    冒号 为 做 字符串_查找子 用 json、「:」、位置。
    若 冒号 小于 0：
        返还 0。
    完。
    返还 做 字符串转整数 用 做 字符串_子串 用 json、冒号 加 1、20。
完。

DevTools_解析参数_str 接受 json、key：
    若 json 等于 0 或者 key 等于 0：
        返还 「」。
    完。
    位置 为 做 字符串_查找 用 json、key。
    若 位置 小于 0：
        返还 「」。
    完。
    引号1 为 做 字符串_查找子 用 json、「\"」、位置 加 做 字符串长度 用 key 加 2。
    若 引号1 小于 0：
        返还 「」。
    完。
    引号2 为 做 字符串_查找子 用 json、「\"」、引号1 加 1。
    若 引号2 小于 0：
        返还 「」。
    完。
    返还 做 字符串_子串 用 json、引号1 加 1、引号2 减 引号1 减 1。
完。

DevTools_解析参数_bool 接受 json、key：
    若 json 等于 0 或者 key 等于 0：
        返还 假。
    完。
    位置 为 做 字符串_查找 用 json、key。
    若 位置 小于 0：
        返还 假。
    完。
    冒号 为 做 字符串_查找子 用 json、「:」、位置。
    若 冒号 小于 0：
        返还 假。
    完。
    剩余 为 做 字符串_子串 用 json、冒号 加 1、8。
    若 做 字符串_查找 用 剩余、「true」 大于或等于 0：
        返还 真。
    完。
    若 做 字符串_查找 用 剩余、「false」 大于或等于 0：
        返还 假。
    完。
    若 做 字符串_查找 用 剩余、「1」 大于或等于 0：
        返还 真。
    完。
    返还 假。
完。

DevTools_发送事件 接受 session、方法、参数json：
    若 session 等于 0：
        返还。
    完。
    msg 为 做 堆_分配 用 CDPMessage。
    msg 的 id 为 负 1。
    msg 的 方法 为 方法。
    msg 的 参数 为 参数json。
完。

最大标记 为 256。

最大度量 为 128。

最大资源 为 512。

长任务阈值 为 50000。

性能标记 是 一种 事物，含 名称哈希、时间戳。

性能度量 是 一种 事物，含 名称哈希、起始标记、结束标记、时长。

资源计时 是 一种 事物，含 地址哈希、起始时间、响应结束、传输大小、类型。

可变 标记表 为 一组：（空）。

可变 标记数 为 0。

可变 度量表 为 一组：（空）。

可变 度量数 为 0。

可变 资源表 为 一组：（空）。

可变 资源数 为 0。

可变 长任务数 为 0。

可变 起始时间 为 0。

性能_初始化 接受 零参：
    标记表 为 一组：（空）。
    标记数 为 0。
    度量表 为 一组：（空）。
    度量数 为 0。
    资源表 为 一组：（空）。
    资源数 为 0。
    起始时间 为 做 平台_获取高精度时间。
完。

性能_当前时间 接受 零参：
    返还 做 平台_获取高精度时间 减 起始时间。
完。

性能_标记 接受 名称：
    若 标记数 大于或等于 最大标记：
        返还。
    完。
    标记 为 新建 性能标记。
    标记 的 名称哈希 为 做 哈希_字符串 用 名称。
    标记 的 时间戳 为 做 性能_当前时间。
    做 追加 用 标记表、标记。
    标记数 为 标记数 加 1。
完。

性能_度量 接受 名称、起始名、结束名：
    若 度量数 大于或等于 最大度量：
        返还。
    完。
    起始哈希 为 做 哈希_字符串 用 起始名。
    结束哈希 为 做 哈希_字符串 用 结束名。
    可变 起始时 为 0。
    可变 结束时 为 0。
    可变 序号 为 0。
    只要 序号 小于 标记数：
        若 标记表 的 第 序号 项 的 名称哈希 等于 起始哈希：
            起始时 为 标记表 的 第 序号 项 的 时间戳。
        完。
        若 标记表 的 第 序号 项 的 名称哈希 等于 结束哈希：
            结束时 为 标记表 的 第 序号 项 的 时间戳。
        完。
        序号 为 序号 加 1。
    完。
    度量 为 新建 性能度量。
    度量 的 名称哈希 为 做 哈希_字符串 用 名称。
    度量 的 起始标记 为 起始时。
    度量 的 结束标记 为 结束时。
    度量 的 时长 为 结束时 减 起始时。
    做 追加 用 度量表、度量。
    度量数 为 度量数 加 1。
完。

性能_记录资源 接受 地址、类型、起始、结束、大小：
    若 资源数 大于或等于 最大资源：
        返还。
    完。
    资源 为 新建 资源计时。
    资源 的 地址哈希 为 做 哈希_字符串 用 地址。
    资源 的 起始时间 为 起始。
    资源 的 响应结束 为 结束。
    资源 的 传输大小 为 大小。
    资源 的 类型 为 类型。
    做 追加 用 资源表、资源。
    资源数 为 资源数 加 1。
完。

性能_记录长任务 接受 时长：
    若 时长 大于 长任务阈值：
        长任务数 为 长任务数 加 1。
    完。
完。

性能_获取标记 接受 名称：
    目标 为 做 哈希_字符串 用 名称。
    可变 序号 为 标记数 减 1。
    只要 序号 大于或等于 0：
        若 标记表 的 第 序号 项 的 名称哈希 等于 目标：
            返还 标记表 的 第 序号 项 的 时间戳。
        完。
        序号 为 序号 减 1。
    完。
    返还 0。
完。

性能_获取度量 接受 名称：
    目标 为 做 哈希_字符串 用 名称。
    可变 序号 为 0。
    只要 序号 小于 度量数：
        若 度量表 的 第 序号 项 的 名称哈希 等于 目标：
            返还 度量表 的 第 序号 项 的 时长。
        完。
        序号 为 序号 加 1。
    完。
    返还 0。
完。

性能_清空标记 接受 零参：
    标记表 为 一组：（空）。
    标记数 为 0。
完。

性能_清空度量 接受 零参：
    度量表 为 一组：（空）。
    度量数 为 0。
完。

性能_清空资源 接受 零参：
    资源表 为 一组：（空）。
    资源数 为 0。
完。

性能_完整统计 接受 零参：
    返还 一组：标记数、度量数、资源数、长任务数、做 性能_当前时间。
完。

文档观察_统计 接受 零参：
    返还 一本字典：观察总成功 对应 观察总成功、观察总失败 对应 观察总失败、定时器创建总数 对应 定时器_创建总数、IO总创建数 对应 IO_总创建数、IO总观察数 对应 IO_总观察数、RO总创建数 对应 RO_总创建数、RO总观察数 对应 RO_总观察数。
完。

文档观察_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 观察总失败 大于 观察总成功 除以 3 并且 观察总成功 大于 50：
        做 追加 用 问题、「观察总失败过高」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

文档观察_综合报告 接受 零参：
    做 打印 用 「[文档观察] ═══ 综合报告 ═══」。
    做 打印 用 「  观察总成功: 」 加 观察总成功。
    做 打印 用 「  观察总失败: 」 加 观察总失败。
    做 打印 用 「  定时器_创建总数: 」 加 定时器_创建总数。
    做 打印 用 「  IO_总创建数: 」 加 IO_总创建数。
    做 打印 用 「  IO_总观察数: 」 加 IO_总观察数。
    做 打印 用 「  RO_总创建数: 」 加 RO_总创建数。
    做 打印 用 「  RO_总观察数: 」 加 RO_总观察数。
    做 打印 用 「  健康: 」 加 若 做 文档观察_健康评估 则 「正常」 否则 「异常」。
完。

文档观察_全局重置 接受 零参：
    观察总成功 为 0。
    观察总失败 为 0。
    定时器_创建总数 为 0。
    IO_总创建数 为 0。
    IO_总观察数 为 0。
    RO_总创建数 为 0。
    RO_总观察数 为 0。
    做 打印 用 「[文档观察] 统计已重置」。
    返还 0。
完。

本章 收束。
