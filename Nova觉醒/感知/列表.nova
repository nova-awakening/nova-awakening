《列表》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·文档方法节》。

引用《..·执行·控制台输出》。

引用《.·原子驻留》。

引用《.·元素通过索》。

引用《.·内联样式解析》。

引用《..·中枢·辅助存根》。

引用《.·文档节点》。

元素_id 接受 节点：
    返还 做 元素_getAttribute 用 节点、「id」。
完。

元素_设置id 接受 节点、id值：
    做 元素_setAttribute 用 节点、「id」、id值。
    扩展 为 做 元素_获取扩展 用 节点。
    若 扩展 不等于 0：
        扩展 的 id原子 为 做 原子_驻留 用 id值。
    完。
完。

元素_className 接受 节点：
    返还 做 元素_getAttribute 用 节点、「class」。
完。

元素_设置className 接受 节点、class值：
    做 元素_setAttribute 用 节点、「class」、class值。
    做 元素_更新class列表 用 节点、class值。
完。

元素_更新class列表 接受 节点、class字符串：
    扩展 为 做 元素_获取扩展 用 节点。
    若 扩展 等于 0：
        返还。
    完。
    扩展 的 class数量 为 0。
    若 class字符串 等于 0：
        返还。
    完。
    len 为 做 字节长度 用 class字符串。
    可变 i 为 0。
    只要 i 小于 len 并且 扩展 的 class数量 小于 16：
        只要 i 小于 len 并且 做 字节取 用 class字符串、i 等于 32：
            i 为 i 加 1。
        完。
        若 i 大于或等于 len：
            跳出。
        完。
        start 为 i。
        只要 i 小于 len 并且 做 字节取 用 class字符串、i 不等于 32：
            i 为 i 加 1。
        完。
        seg 为 做 字符串_子串 用 class字符串、start、i。
        扩展 的 class原子 的 第 扩展 的 class数量 项 为 做 原子_驻留 用 seg。
        扩展 的 class数量 为 扩展 的 class数量 加 1。
    完。
完。

元素_classList_contains 接受 节点、class名：
    扩展 为 做 元素_获取扩展 用 节点。
    若 扩展 等于 0 或者 扩展 的 class数量 等于 0：
        返还 0。
    完。
    目标原子 为 做 原子_驻留 用 class名。
    可变 i 为 0。
    只要 i 小于 扩展 的 class数量：
        若 扩展 的 class原子 的 第 i 项 等于 目标原子：
            返还 1。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

元素_属性变更回调 接受 节点、名称原子、旧值、新值：
    若 名称原子 等于 原子_id：
        扩展 为 做 元素_获取扩展 用 节点。
        若 扩展 不等于 0：
            若 新值 不等于 0：
                扩展 的 id原子 为 做 原子_驻留 用 新值。
            否则：
                扩展 的 id原子 为 0。
            完。
            若 节点 的 树作用域 不等于 0：
                做 Document_更新id索引 用 节点 的 树作用域、节点、旧值、新值。
            完。
        完。
    完。
    若 名称原子 等于 原子_class：
        做 元素_更新class列表 用 节点、新值。
        做 节点_标记样式脏 用 节点。
    完。
    若 名称原子 等于 原子_style_attr：
        若 新值 不等于 0：
            做 元素_解析内联样式 用 节点、新值。
        完。
        做 节点_标记样式脏 用 节点。
    完。
    做 节点_标记样式脏 用 节点。
完。

元素_解析内联样式 接受 节点、样式文本：
    扩展 为 做 元素_获取扩展 用 节点。
    若 扩展 等于 0：
        返还。
    完。
    数据 为 扩展 的 元素数据。
    若 数据 等于 0：
        返还。
    完。
    数据 的 内联样式 为 做 CSS_解析内联样式 用 样式文本。
完。

本章 收束。
