《样式引擎Engine》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《.·样式令牌器》。

样式Engine 是 一种 事物，含 UA样式表、作者样式表列表、作者样式表数量、作者样式表容量、文档、标签规则索引、class规则索引、id规则索引、通用规则、通用规则数量、样式表已变、需要全量重计算、总样式计算次数、总规则匹配次数。

索引规则项 是 一种 事物，含 键原子、规则、特异度。

样式Engine_创建 接受 文档：
    engine 为 做 堆_分配 用 样式Engine。
    engine 的 文档 为 文档。
    engine 的 UA样式表 为 0。
    engine 的 作者样式表容量 为 16。
    engine 的 作者样式表列表 为 做 堆_分配_数组 用 engine 的 作者样式表容量 乘 8。
    engine 的 作者样式表数量 为 0。
    engine 的 标签规则索引 为 做 堆_分配_数组 用 256 乘 16。
    engine 的 class规则索引 为 做 堆_分配_数组 用 256 乘 16。
    engine 的 id规则索引 为 做 堆_分配_数组 用 256 乘 16。
    engine 的 通用规则 为 做 堆_分配_数组 用 64 乘 8。
    engine 的 通用规则数量 为 0。
    engine 的 样式表已变 为 假。
    engine 的 需要全量重计算 为 假。
    engine 的 总样式计算次数 为 0。
    engine 的 总规则匹配次数 为 0。
    返还 engine。
完。

样式Engine_添加样式表 接受 engine、css文本：
    样式表 为 做 CSSParser_解析样式表 用 css文本。
    若 样式表 等于 0：
        返还。
    完。
    若 engine 的 作者样式表数量 大于或等于 engine 的 作者样式表容量：
        新容量 为 engine 的 作者样式表容量 乘 2。
        新列表 为 做 堆_分配_数组 用 新容量 乘 8。
        做 内存_复制 用 新列表、engine 的 作者样式表列表、engine 的 作者样式表数量 乘 8。
        做 堆_释放 用 engine 的 作者样式表列表。
        engine 的 作者样式表列表 为 新列表。
        engine 的 作者样式表容量 为 新容量。
    完。
    做 内存_写64 用 engine 的 作者样式表列表 加 engine 的 作者样式表数量 乘 8、样式表。
    engine 的 作者样式表数量 为 engine 的 作者样式表数量 加 1。
    做 样式Engine_索引样式表 用 engine、样式表。
    engine 的 样式表已变 为 真。
    engine 的 需要全量重计算 为 真。
完。

样式Engine_索引样式表 接受 engine、样式表：
    可变 i 为 0。
    只要 i 小于 样式表 的 规则数量：
        规则 为 做 内存_读64 用 样式表 的 规则列表 加 i 乘 8。
        若 规则 不等于 0：
            选择器 为 规则 的 选择器列表。
            若 选择器 不等于 0：
                可变 末尾 为 选择器。
                只要 末尾 的 下一个 不等于 0：
                    末尾 为 末尾 的 下一个。
                完。
                若 末尾 的 类型 等于 选择器类型_id：
                    做 样式Engine_添加到索引 用 engine 的 id规则索引、末尾 的 值原子、规则、规则 的 特异度。
                否则若 末尾 的 类型 等于 选择器类型_class：
                    做 样式Engine_添加到索引 用 engine 的 class规则索引、末尾 的 值原子、规则、规则 的 特异度。
                否则若 末尾 的 类型 等于 选择器类型_标签：
                    做 样式Engine_添加到索引 用 engine 的 标签规则索引、末尾 的 值原子、规则、规则 的 特异度。
                否则：
                    若 engine 的 通用规则数量 小于 64：
                        做 内存_写64 用 engine 的 通用规则 加 engine 的 通用规则数量 乘 8、规则。
                        engine 的 通用规则数量 为 engine 的 通用规则数量 加 1。
                    完。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

样式Engine_添加到索引 接受 索引表、键原子、规则、特异度：
    桶 为 键原子 取余于 256。
    偏移 为 桶 乘 16。
    做 内存_写64 用 索引表 加 偏移、规则。
    做 内存_写32 用 索引表 加 偏移 加 8、特异度。
完。

本章 收束。
