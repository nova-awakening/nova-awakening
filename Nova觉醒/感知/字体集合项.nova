《字体集合项》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·字符串替换》。

引用《.·样式字体字》。

字体FaceSet 是 一种 事物，含 字体列表、字体数量、字体容量、状态、ready_promise、onloading、onloadingdone、onloadingerror。

可变 全局字体FaceSet 为 0。

字体FaceSet_获取 接受 零参：
    若 全局字体FaceSet 等于 0：
        全局字体FaceSet 为 做 堆_分配 用 字体FaceSet。
        全局字体FaceSet 的 字体容量 为 128。
        全局字体FaceSet 的 字体列表 为 做 堆_分配_数组 用 128 乘 8。
        全局字体FaceSet 的 字体数量 为 0。
        全局字体FaceSet 的 状态 为 1。
        全局字体FaceSet 的 ready_promise 为 0。
    完。
    返还 全局字体FaceSet。
完。

字体FaceSet_add 接受 ffs、ff：
    若 ffs 等于 0 或者 ff 等于 0：
        返还。
    完。
    若 ffs 的 字体数量 大于或等于 ffs 的 字体容量：
        返还。
    完。
    做 内存_写64 用 ffs 的 字体列表 加 ffs 的 字体数量 乘 8、ff。
    ffs 的 字体数量 为 ffs 的 字体数量 加 1。
    ff 的 引用计数 为 ff 的 引用计数 加 1。
完。

字体FaceSet_delete 接受 ffs、ff：
    若 ffs 等于 0 或者 ff 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 ffs 的 字体数量：
        若 做 内存_读64 用 ffs 的 字体列表 加 i 乘 8 等于 ff：
            ffs 的 字体数量 为 ffs 的 字体数量 减 1。
            做 内存_写64 用 ffs 的 字体列表 加 i 乘 8、做 内存_读64 用 ffs 的 字体列表 加 ffs 的 字体数量 乘 8。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

字体FaceSet_check 接受 ffs、字体描述：
    若 ffs 等于 0 或者 字体描述 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 ffs 的 字体数量：
        ff 为 做 内存_读64 用 ffs 的 字体列表 加 i 乘 8。
        若 ff 不等于 0 并且 ff 的 状态 等于 FF_STATUS_LOADED：
            若 做 字符串_包含 用 字体描述、ff 的 家族名：
                返还 真。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

字体FaceSet_load 接受 ffs、字体描述：
    若 ffs 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 ffs 的 字体数量：
        ff 为 做 内存_读64 用 ffs 的 字体列表 加 i 乘 8。
        若 ff 不等于 0 并且 ff 的 状态 等于 FF_STATUS_UNLOADED：
            若 做 字符串_包含 用 字体描述、ff 的 家族名：
                做 字体Face_load 用 ff。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

字体Display_获取策略 接受 ff、经过时间：
    若 ff 等于 0：
        返还 1。
    完。
    若 ff 的 状态 等于 FF_STATUS_LOADED：
        返还 2。
    完。
    若 ff 的 显示策略 等于 FONT_DISPLAY_BLOCK：
        若 经过时间 小于 3000000：
            返还 0。
        完。
        返还 1。
    完。
    若 ff 的 显示策略 等于 FONT_DISPLAY_SWAP：
        返还 1。
    完。
    若 ff 的 显示策略 等于 FONT_DISPLAY_FALLBACK：
        若 经过时间 小于 100000：
            返还 0。
        完。
        若 经过时间 小于 3000000：
            返还 1。
        完。
        返还 1。
    完。
    若 ff 的 显示策略 等于 FONT_DISPLAY_OPTIONAL：
        若 经过时间 小于 100000：
            返还 0。
        完。
        返还 1。
    完。
    若 经过时间 小于 3000000：
        返还 0。
    完。
    返还 1。
完。

本章 收束。
