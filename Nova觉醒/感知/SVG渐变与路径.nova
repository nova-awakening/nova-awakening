《SVG渐变与路径》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《.·边界框计算》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

最大色标数 为 16。

渐变扩展_Pad 为 0。

渐变扩展_Repeat 为 1。

渐变扩展_Reflect 为 2。

渐变单位_对象 为 0。

渐变单位_用户 为 1。

SVG命令_M 为 77。

SVG命令_m 为 109。

SVG命令_L 为 76。

SVG命令_l 为 108。

SVG命令_C 为 67。

SVG命令_c 为 99。

SVG命令_Q 为 81。

SVG命令_q 为 113。

SVG命令_Z 为 90。

SVG命令_z 为 122。

SVG命令_H 为 72。

SVG命令_h 为 104。

SVG命令_V 为 86。

SVG命令_v 为 118。

SVG命令_S 为 83。

SVG命令_s 为 115。

SVG命令_T 为 84。

SVG命令_t 为 116。

SVG命令_A 为 65。

SVG命令_a 为 97。

渐变统计 是 一种 事物，含 线性渐变创建数、径向渐变创建数、渐变采样总次数、线性采样次数、径向采样次数、色标插值次数、Pad扩展次数、Repeat扩展次数、Reflect扩展次数、路径展平总次数、贝塞尔细分次数、线段生成总数、扫描线填充次数、总采样耗时_纳秒、AI精度调用次数、AI精度命中次数、错误总数、溢出保护触发数。

AI渐变状态 是 一种 事物，含 历史色标数均值、历史渐变类型分布、历史路径复杂度、连续简单渐变数、预测精度级别、采样计数器、学习权重、上次渐变类型。

可变 全局渐变统计 为 0。

可变 全局渐变AI 为 0。

可变 全局渐变已初始化 为 假。

渐变色标 是 一种 事物，含 偏移、颜色R、颜色G、颜色B、颜色A。

线性渐变 是 一种 事物，含 x1、y1、x2、y2、色标、色标数、单位、扩展模式、变换、锁。

径向渐变 是 一种 事物，含 cx、cy、r、fx、fy、色标、色标数、单位、扩展模式、变换、锁。

SVG渐变_初始化 接受 零参：
    若 全局渐变已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局渐变统计 为 做 堆_分配_清零 用 144。
    若 全局渐变统计 等于 空：
        返还 错误_内存不足。
    完。
    全局渐变AI 为 做 堆_分配_清零 用 40。
    若 全局渐变AI 不等于 空：
        全局渐变AI 的 预测精度级别 为 8。
        全局渐变AI 的 学习权重 为 128。
    完。
    全局渐变已初始化 为 真。
    做 打印 用 「[SVG渐变] 初始化完成」。
    返还 0。
完。

SVG渐变_销毁 接受 零参：
    若 全局渐变统计 不等于 空：
        做 堆_释放 用 全局渐变统计。
        全局渐变统计 为 0。
    完。
    若 全局渐变AI 不等于 空：
        做 堆_释放 用 全局渐变AI。
        全局渐变AI 为 0。
    完。
    全局渐变已初始化 为 假。
    返还 0。
完。

SVG渐变_重置 接受 零参：
    若 全局渐变统计 不等于 空：
        做 内存_置零 用 全局渐变统计、144。
    完。
    返还 0。
完。

线性渐变_创建 接受 x1、y1、x2、y2：
    g 为 做 堆_分配 用 线性渐变。
    若 g 等于 空：
        返还 空。
    完。
    g 的 x1 为 x1。
    g 的 y1 为 y1。
    g 的 x2 为 x2。
    g 的 y2 为 y2。
    g 的 色标 为 做 堆_分配_清零 用 最大色标数 乘 8。
    g 的 色标数 为 0。
    g 的 单位 为 渐变单位_对象。
    g 的 扩展模式 为 渐变扩展_Pad。
    g 的 变换 为 0。
    g 的 锁 为 做 渲染_互斥锁创建。
    若 全局渐变统计 不等于 空：
        全局渐变统计 的 线性渐变创建数 为 全局渐变统计 的 线性渐变创建数 加 1。
    完。
    返还 g。
完。

径向渐变_创建 接受 cx、cy、r、fx、fy：
    g 为 做 堆_分配 用 径向渐变。
    若 g 等于 空：
        返还 空。
    完。
    g 的 cx 为 cx。
    g 的 cy 为 cy。
    g 的 r 为 r。
    g 的 fx 为 fx。
    g 的 fy 为 fy。
    g 的 色标 为 做 堆_分配_清零 用 最大色标数 乘 8。
    g 的 色标数 为 0。
    g 的 单位 为 渐变单位_对象。
    g 的 扩展模式 为 渐变扩展_Pad。
    g 的 变换 为 0。
    g 的 锁 为 做 渲染_互斥锁创建。
    若 全局渐变统计 不等于 空：
        全局渐变统计 的 径向渐变创建数 为 全局渐变统计 的 径向渐变创建数 加 1。
    完。
    返还 g。
完。

渐变_添加色标 接受 色标数组、色标数指针、偏移、R、G、B、A：
    若 色标数组 等于 空 或者 色标数指针 等于 空：
        返还 错误_无效参数。
    完。
    当前数 为 做 内存_读32 用 色标数指针。
    若 当前数 大于或等于 最大色标数：
        返还 错误_容量不足。
    完。
    可变 有效偏移 为 偏移。
    若 有效偏移 小于 0：
        有效偏移 为 0。
    完。
    若 有效偏移 大于 1024：
        有效偏移 为 1024。
    完。
    基 为 色标数组 加 当前数 乘 8。
    做 内存_写32 用 基、有效偏移。
    做 内存_写8 用 基 加 4、R。
    做 内存_写8 用 基 加 5、G。
    做 内存_写8 用 基 加 6、B。
    做 内存_写8 用 基 加 7、A。
    做 内存_写32 用 色标数指针、当前数 加 1。
    返还 0。
完。

渐变_销毁_线性 接受 g：
    若 g 等于 空：
        返还。
    完。
    若 g 的 色标 不等于 空：
        做 堆_释放 用 g 的 色标。
    完。
    做 堆_释放 用 g。
完。

渐变_销毁_径向 接受 g：
    若 g 等于 空：
        返还。
    完。
    若 g 的 色标 不等于 空：
        做 堆_释放 用 g 的 色标。
    完。
    做 堆_释放 用 g。
完。

SNova视觉网络radient_采样 接受 色标、色标数、t_Q10：
    若 色标数 等于 0：
        返还 0。
    完。
    若 色标数 等于 1：
        返还 色标 的 第 0 项 的 颜色R 左移 24 位或 色标 的 第 0 项 的 颜色G 左移 16 位或 色标 的 第 0 项 的 颜色B 左移 8 位或 色标 的 第 0 项 的 颜色A。
    完。
    可变 i 为 0。
    只要 i 小于 色标数 减 1：
        若 t_Q10 大于或等于 色标 的 第 i 项 的 偏移 并且 t_Q10 小于或等于 色标 的 第 i 加 1 项 的 偏移：
            范围 为 色标 的 第 i 加 1 项 的 偏移 减 色标 的 第 i 项 的 偏移。
            若 范围 等于 0：
                范围 为 1。
            完。
            比例 为 (t_Q10 减 色标 的 第 i 项 的 偏移) 乘 1024 除以 范围。
            inv 为 1024 减 比例。
            R 为 (色标 的 第 i 项 的 颜色R 乘 inv 加 色标 的 第 i 加 1 项 的 颜色R 乘 比例) 除以 1024。
            G 为 (色标 的 第 i 项 的 颜色G 乘 inv 加 色标 的 第 i 加 1 项 的 颜色G 乘 比例) 除以 1024。
            B 为 (色标 的 第 i 项 的 颜色B 乘 inv 加 色标 的 第 i 加 1 项 的 颜色B 乘 比例) 除以 1024。
            A 为 (色标 的 第 i 项 的 颜色A 乘 inv 加 色标 的 第 i 加 1 项 的 颜色A 乘 比例) 除以 1024。
            返还 R 左移 24 位或 G 左移 16 位或 B 左移 8 位或 A。
        完。
        i 为 i 加 1。
    完。
    返还 色标 的 第 色标数 减 1 项 的 颜色R 左移 24 位或 色标 的 第 色标数 减 1 项 的 颜色G 左移 16 位或 色标 的 第 色标数 减 1 项 的 颜色B 左移 8 位或 色标 的 第 色标数 减 1 项 的 颜色A。
完。

SVGPath_展平 接受 path、精度：
    若 path 等于 0：
        返还 0。
    完。
    容量 为 path 的 段数 乘 32。
    线段 为 做 堆_分配_数组 用 容量 乘 16。
    可变 线段数 为 0。
    可变 当前x 为 0。
    可变 当前y 为 0。
    可变 seg 为 path 的 段链头。
    只要 seg 不等于 0 并且 线段数 小于 容量：
        若 seg 的 命令 等于 SVG命令_M：
            当前x 为 seg 的 参数 的 第 0 项。
            当前y 为 seg 的 参数 的 第 1 项。
        否则若 seg 的 命令 等于 SVG命令_L 或者 seg 的 命令 等于 SVG命令_Z：
            偏移 为 线段数 乘 16。
            做 内存_写32 用 线段 加 偏移、当前x。
            做 内存_写32 用 线段 加 偏移 加 4、当前y。
            做 内存_写32 用 线段 加 偏移 加 8、seg 的 参数 的 第 0 项。
            做 内存_写32 用 线段 加 偏移 加 12、seg 的 参数 的 第 1 项。
            线段数 为 线段数 加 1。
            当前x 为 seg 的 参数 的 第 0 项。
            当前y 为 seg 的 参数 的 第 1 项。
        否则若 seg 的 命令 等于 SVG命令_C：
            线段数 为 做 SVG_细分三次贝塞尔 用 线段、线段数、容量、当前x、当前y、seg 的 参数 的 第 0 项、seg 的 参数 的 第 1 项、seg 的 参数 的 第 2 项、seg 的 参数 的 第 3 项、seg 的 参数 的 第 4 项、seg 的 参数 的 第 5 项、精度。
            当前x 为 seg 的 参数 的 第 4 项。
            当前y 为 seg 的 参数 的 第 5 项。
        否则若 seg 的 命令 等于 SVG命令_Q：
            线段数 为 做 SVG_细分二次贝塞尔 用 线段、线段数、容量、当前x、当前y、seg 的 参数 的 第 0 项、seg 的 参数 的 第 1 项、seg 的 参数 的 第 2 项、seg 的 参数 的 第 3 项、精度。
            当前x 为 seg 的 参数 的 第 2 项。
            当前y 为 seg 的 参数 的 第 3 项。
        完。
        seg 为 seg 的 下一个。
    完。
    返还 一组：线段、线段数。
完。

SVG_细分三次贝塞尔 接受 线段、线段数、容量、x0、y0、x1、y1、x2、y2、x3、y3、精度：
    若 线段数 大于或等于 容量：
        返还 线段数。
    完。
    dx 为 x3 减 x0。
    dy 为 y3 减 y0。
    d1 为 做 SVG_abs 用 (x1 减 x3) 乘 dy 减 (y1 减 y3) 乘 dx。
    d2 为 做 SVG_abs 用 (x2 减 x3) 乘 dy 减 (y2 减 y3) 乘 dx。
    若 (d1 加 d2) 乘 (d1 加 d2) 小于 精度 乘 (dx 乘 dx 加 dy 乘 dy)：
        偏移 为 线段数 乘 16。
        做 内存_写32 用 线段 加 偏移、x0。
        做 内存_写32 用 线段 加 偏移 加 4、y0。
        做 内存_写32 用 线段 加 偏移 加 8、x3。
        做 内存_写32 用 线段 加 偏移 加 12、y3。
        返还 线段数 加 1。
    完。
    mx01 为 (x0 加 x1) 除以 2。
    my01 为 (y0 加 y1) 除以 2。
    mx12 为 (x1 加 x2) 除以 2。
    my12 为 (y1 加 y2) 除以 2。
    mx23 为 (x2 加 x3) 除以 2。
    my23 为 (y2 加 y3) 除以 2。
    mx012 为 (mx01 加 mx12) 除以 2。
    my012 为 (my01 加 my12) 除以 2。
    mx123 为 (mx12 加 mx23) 除以 2。
    my123 为 (my12 加 my23) 除以 2。
    mx 为 (mx012 加 mx123) 除以 2。
    my 为 (my012 加 my123) 除以 2。
    线段数 为 做 SVG_细分三次贝塞尔 用 线段、线段数、容量、x0、y0、mx01、my01、mx012、my012、mx、my、精度。
    线段数 为 做 SVG_细分三次贝塞尔 用 线段、线段数、容量、mx、my、mx123、my123、mx23、my23、x3、y3、精度。
    返还 线段数。
完。

SVG_细分二次贝塞尔 接受 线段、线段数、容量、x0、y0、cx、cy、x1、y1、精度：
    cx1 为 x0 加 (cx 减 x0) 乘 683 除以 1024。
    cy1 为 y0 加 (cy 减 y0) 乘 683 除以 1024。
    cx2 为 x1 加 (cx 减 x1) 乘 683 除以 1024。
    cy2 为 y1 加 (cy 减 y1) 乘 683 除以 1024。
    返还 做 SVG_细分三次贝塞尔 用 线段、线段数、容量、x0、y0、cx1、cy1、cx2、cy2、x1、y1、精度。
完。

渐变_应用扩展模式 接受 t_Q10、扩展模式：
    若 t_Q10 大于或等于 0 并且 t_Q10 小于或等于 1024：
        返还 t_Q10。
    完。
    若 扩展模式 等于 渐变扩展_Pad：
        若 t_Q10 小于 0：
            返还 0。
        完。
        返还 1024。
    完。
    若 扩展模式 等于 渐变扩展_Repeat：
        可变 t 为 t_Q10 取余于 1024。
        若 t 小于 0：
            t 为 t 加 1024。
        完。
        若 全局渐变统计 不等于 空：
            全局渐变统计 的 Repeat扩展次数 为 全局渐变统计 的 Repeat扩展次数 加 1。
        完。
        返还 t。
    完。
    若 扩展模式 等于 渐变扩展_Reflect：
        可变 t 为 t_Q10 取余于 2048。
        若 t 小于 0：
            t 为 t 加 2048。
        完。
        若 t 大于 1024：
            t 为 2048 减 t。
        完。
        若 全局渐变统计 不等于 空：
            全局渐变统计 的 Reflect扩展次数 为 全局渐变统计 的 Reflect扩展次数 加 1。
        完。
        返还 t。
    完。
    返还 t_Q10。
完。

渐变_采样增强 接受 色标数组、色标数、t_Q10、扩展模式：
    若 色标数组 等于 空 或者 色标数 等于 0：
        返还 0。
    完。
    t 为 做 渐变_应用扩展模式 用 t_Q10、扩展模式。
    若 色标数 等于 1：
        基 为 色标数组。
        返还 做 内存_读8 用 基 加 4 左移 24 位或 做 内存_读8 用 基 加 5 左移 16 位或 做 内存_读8 用 基 加 6 左移 8 位或 做 内存_读8 用 基 加 7。
    完。
    可变 左 为 0。
    可变 右 为 色标数 减 1。
    只要 左 小于 右 减 1：
        中 为 (左 加 右) 除以 2。
        中偏移 为 做 内存_读32 用 色标数组 加 中 乘 8。
        若 t 小于或等于 中偏移：
            右 为 中。
        否则：
            左 为 中。
        完。
    完。
    基左 为 色标数组 加 左 乘 8。
    基右 为 色标数组 加 右 乘 8。
    偏移左 为 做 内存_读32 用 基左。
    偏移右 为 做 内存_读32 用 基右。
    范围 为 偏移右 减 偏移左。
    若 范围 小于或等于 0：
        范围 为 1。
    完。
    比例 为 (t 减 偏移左) 乘 1024 除以 范围。
    若 比例 小于 0：
        比例 为 0。
    完。
    若 比例 大于 1024：
        比例 为 1024。
    完。
    inv 为 1024 减 比例。
    R 为 (做 内存_读8 用 基左 加 4 乘 inv 加 做 内存_读8 用 基右 加 4 乘 比例) 除以 1024。
    G 为 (做 内存_读8 用 基左 加 5 乘 inv 加 做 内存_读8 用 基右 加 5 乘 比例) 除以 1024。
    B 为 (做 内存_读8 用 基左 加 6 乘 inv 加 做 内存_读8 用 基右 加 6 乘 比例) 除以 1024。
    A 为 (做 内存_读8 用 基左 加 7 乘 inv 加 做 内存_读8 用 基右 加 7 乘 比例) 除以 1024。
    若 全局渐变统计 不等于 空：
        全局渐变统计 的 色标插值次数 为 全局渐变统计 的 色标插值次数 加 1。
    完。
    返还 R 左移 24 位或 G 左移 16 位或 B 左移 8 位或 A。
完。

线性渐变_采样像素 接受 g、px、py：
    若 g 等于 空 或者 g 的 色标 等于 空：
        返还 0。
    完。
    dx 为 g 的 x2 减 g 的 x1。
    dy 为 g 的 y2 减 g 的 y1。
    长度平方 为 dx 乘 dx 加 dy 乘 dy。
    若 长度平方 等于 0：
        返还 做 渐变_采样增强 用 g 的 色标、g 的 色标数、0、g 的 扩展模式。
    完。
    t 为 ((px 减 g 的 x1) 乘 dx 加 (py 减 g 的 y1) 乘 dy) 乘 1024 除以 长度平方。
    若 全局渐变统计 不等于 空：
        全局渐变统计 的 线性采样次数 为 全局渐变统计 的 线性采样次数 加 1。
        全局渐变统计 的 渐变采样总次数 为 全局渐变统计 的 渐变采样总次数 加 1。
    完。
    返还 做 渐变_采样增强 用 g 的 色标、g 的 色标数、t、g 的 扩展模式。
完。

径向渐变_采样像素 接受 g、px、py：
    若 g 等于 空 或者 g 的 色标 等于 空 或者 g 的 r 小于或等于 0：
        返还 0。
    完。
    fdx 为 px 减 g 的 fx。
    fdy 为 py 减 g 的 fy。
    距离平方 为 fdx 乘 fdx 加 fdy 乘 fdy。
    可变 估计 为 g 的 r 除以 2。
    若 估计 小于或等于 0：
        估计 为 1。
    完。
    估计 为 (估计 加 距离平方 除以 估计) 除以 2。
    估计 为 (估计 加 距离平方 除以 估计) 除以 2。
    估计 为 (估计 加 距离平方 除以 估计) 除以 2。
    距离 为 估计。
    t 为 距离 乘 1024 除以 g 的 r。
    若 全局渐变统计 不等于 空：
        全局渐变统计 的 径向采样次数 为 全局渐变统计 的 径向采样次数 加 1。
        全局渐变统计 的 渐变采样总次数 为 全局渐变统计 的 渐变采样总次数 加 1。
    完。
    返还 做 渐变_采样增强 用 g 的 色标、g 的 色标数、t、g 的 扩展模式。
完。

AI渐变_记录渲染 接受 渐变类型、色标数、路径段数：
    若 全局渐变AI 等于 空：
        返还。
    完。
    AI 为 全局渐变AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    AI 的 历史色标数均值 为 (AI 的 历史色标数均值 乘 7 加 色标数 乘 1024) 除以 8。
    AI 的 历史路径复杂度 为 (AI 的 历史路径复杂度 乘 7 加 路径段数 乘 1024) 除以 8。
    AI 的 上次渐变类型 为 渐变类型。
    可变 是简单 为 假。
    若 色标数 小于或等于 2 并且 路径段数 小于或等于 10：
        是简单 为 真。
    完。
    若 是简单 等于 真：
        AI 的 连续简单渐变数 为 AI 的 连续简单渐变数 加 1。
    否则：
        AI 的 连续简单渐变数 为 0。
    完。
    若 AI 的 采样计数器 小于 16：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 全局渐变统计 不等于 空：
        全局渐变统计 的 AI精度调用次数 为 全局渐变统计 的 AI精度调用次数 加 1。
    完。
    若 AI 的 连续简单渐变数 大于 8：
        AI 的 预测精度级别 为 4。
    否则若 AI 的 历史路径复杂度 大于 50 乘 1024：
        AI 的 预测精度级别 为 16。
    否则：
        AI 的 预测精度级别 为 8。
    完。
    若 是简单 等于 真 并且 AI 的 预测精度级别 小于或等于 4：
        若 全局渐变统计 不等于 空：
            全局渐变统计 的 AI精度命中次数 为 全局渐变统计 的 AI精度命中次数 加 1。
        完。
    完。
完。

AI渐变_获取推荐精度 接受 零参：
    若 全局渐变AI 等于 空：
        返还 8。
    完。
    返还 全局渐变AI 的 预测精度级别。
完。

SVG渐变_打印统计 接受 零参：
    若 全局渐变统计 等于 空：
        做 打印 用 「[SVG渐变] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   SVG渐变+光栅化 · 统计报告         │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 线性渐变创建:     」、做 转字符串 用 全局渐变统计 的 线性渐变创建数。
    做 打印 用 做 字符串拼接 用 「│ 径向渐变创建:     」、做 转字符串 用 全局渐变统计 的 径向渐变创建数。
    做 打印 用 做 字符串拼接 用 「│ 渐变采样总次:     」、做 转字符串 用 全局渐变统计 的 渐变采样总次数。
    做 打印 用 做 字符串拼接 用 「│ 线性采样:         」、做 转字符串 用 全局渐变统计 的 线性采样次数。
    做 打印 用 做 字符串拼接 用 「│ 径向采样:         」、做 转字符串 用 全局渐变统计 的 径向采样次数。
    做 打印 用 做 字符串拼接 用 「│ 色标插值:         」、做 转字符串 用 全局渐变统计 的 色标插值次数。
    做 打印 用 做 字符串拼接 用 「│ Pad扩展:          」、做 转字符串 用 全局渐变统计 的 Pad扩展次数。
    做 打印 用 做 字符串拼接 用 「│ Repeat扩展:       」、做 转字符串 用 全局渐变统计 的 Repeat扩展次数。
    做 打印 用 做 字符串拼接 用 「│ Reflect扩展:      」、做 转字符串 用 全局渐变统计 的 Reflect扩展次数。
    做 打印 用 做 字符串拼接 用 「│ 路径展平:         」、做 转字符串 用 全局渐变统计 的 路径展平总次数。
    做 打印 用 做 字符串拼接 用 「│ 贝塞尔细分:       」、做 转字符串 用 全局渐变统计 的 贝塞尔细分次数。
    做 打印 用 做 字符串拼接 用 「│ 线段生成:         」、做 转字符串 用 全局渐变统计 的 线段生成总数。
    做 打印 用 做 字符串拼接 用 「│ AI精度调用:       」、做 转字符串 用 全局渐变统计 的 AI精度调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI精度命中:       」、做 转字符串 用 全局渐变统计 的 AI精度命中次数。
    做 打印 用 做 字符串拼接 用 「│ 总采样耗时(μs):   」、做 转字符串 用 全局渐变统计 的 总采样耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 错误:             」、做 转字符串 用 全局渐变统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

SVG渐变_导出诊断 接受 零参：
    若 全局渐变AI 等于 空：
        做 打印 用 「[SVG渐变 AI] 未初始化」。
        返还。
    完。
    AI 为 全局渐变AI。
    做 打印 用 「[SVG渐变 AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史色标数(Q10): 」、做 转字符串 用 AI 的 历史色标数均值。
    做 打印 用 做 字符串拼接 用 「  历史路径复杂度(Q10): 」、做 转字符串 用 AI 的 历史路径复杂度。
    做 打印 用 做 字符串拼接 用 「  连续简单渐变: 」、做 转字符串 用 AI 的 连续简单渐变数。
    做 打印 用 做 字符串拼接 用 「  推荐精度级别: 」、做 转字符串 用 AI 的 预测精度级别。
    做 打印 用 做 字符串拼接 用 「  上次类型: 」、做 转字符串 用 AI 的 上次渐变类型。
完。

SVG渐变_获取统计 接受 零参：
    返还 全局渐变统计。
完。

SVG渐变_获取AI状态 接受 零参：
    返还 全局渐变AI。
完。

SVG渐变_获取推荐精度 接受 零参：
    返还 做 AI渐变_获取推荐精度。
完。

SVG渐变_是否已初始化 接受 零参：
    返还 全局渐变已初始化。
完。

本章 收束。
