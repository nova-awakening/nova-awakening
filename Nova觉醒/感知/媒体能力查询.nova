《媒体能力查询》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·字符串替换》。

引用《.·文档事件》。

AUDIO_CTX_SUSPENDED 为 0。

AUDIO_CTX_RUNNING 为 1。

AUDIO_CTX_CLOSED 为 2。

音频节点_目标 为 0。

音频节点_增益 为 1。

音频节点_振荡器 为 2。

音频节点_双二阶 为 3。

音频节点_分析器 为 4。

音频节点_延迟 为 5。

音频节点_压缩 为 6。

音频节点_卷积 为 7。

波形_正弦 为 0。

波形_方波 为 1。

波形_锯齿 为 2。

波形_三角 为 3。

最大音频节点 为 64。

最大节点连接 为 8。

默认采样率 为 44100。

默认缓冲大小 为 2048。

媒体能力统计 是 一种 事物，含 canPlayType调用次数、probably返回次数、maybe返回次数、不支持返回次数、AudioContext创建次数、音频节点创建次数、节点连接次数、音频图处理次数、音频缓冲创建次数、振荡器启动次数、AI码率调用次数、AI码率切换次数、AI音频增强次数、AI视频超分次数、总处理耗时_纳秒、错误总数、MIME解析次数、codec字符串解析次数。

AI媒体能力状态 是 一种 事物，含 历史带宽估计、历史缓冲水位、历史丢帧率、当前码率档位、连续低带宽计数、连续高带宽计数、预测下次码率、采样计数器、学习权重。

可变 全局媒体能力统计 为 0。

可变 全局媒体能力AI 为 0。

可变 全局媒体能力已初始化 为 假。

媒体能力_初始化 接受 零参：
    若 全局媒体能力已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局媒体能力统计 为 做 堆_分配_清零 用 144。
    若 全局媒体能力统计 等于 空：
        返还 错误_内存不足。
    完。
    全局媒体能力AI 为 做 堆_分配_清零 用 48。
    若 全局媒体能力AI 不等于 空：
        全局媒体能力AI 的 当前码率档位 为 1。
        全局媒体能力AI 的 预测下次码率 为 1。
        全局媒体能力AI 的 学习权重 为 128。
    完。
    全局媒体能力已初始化 为 真。
    做 打印 用 「[媒体能力] 初始化完成」。
    返还 0。
完。

媒体能力_销毁 接受 零参：
    若 全局媒体能力统计 不等于 空：
        做 堆_释放 用 全局媒体能力统计。
        全局媒体能力统计 为 0。
    完。
    若 全局媒体能力AI 不等于 空：
        做 堆_释放 用 全局媒体能力AI。
        全局媒体能力AI 为 0。
    完。
    全局媒体能力已初始化 为 假。
    返还 0。
完。

媒体能力_重置 接受 零参：
    若 全局媒体能力统计 不等于 空：
        做 内存_置零 用 全局媒体能力统计、144。
    完。
    返还 0。
完。

媒体_canPlayType 接受 mimeType：
    若 mimeType 等于 0：
        返还 「」。
    完。
    若 做 字符串_包含 用 mimeType、「video/mp4」：
        返还 「probably」。
    完。
    若 做 字符串_包含 用 mimeType、「video/webm」：
        返还 「probably」。
    完。
    若 做 字符串_包含 用 mimeType、「audio/mp4」：
        返还 「probably」。
    完。
    若 做 字符串_包含 用 mimeType、「audio/mpeg」：
        返还 「probably」。
    完。
    若 做 字符串_包含 用 mimeType、「audio/ogg」：
        返还 「probably」。
    完。
    若 做 字符串_包含 用 mimeType、「audio/wav」：
        返还 「probably」。
    完。
    若 做 字符串_包含 用 mimeType、「audio/webm」：
        返还 「probably」。
    完。
    返还 「」。
完。

媒体元素_触发事件 接受 me、类型：
    若 me 等于 0：
        返还。
    完。
    evt 为 做 事件_创建2 用 类型、假、假。
    evt 的 是可信的 为 真。
完。

AUDIO_CTX_SUSPENDED 为 0。

AUDIO_CTX_RUNNING 为 1。

AUDIO_CTX_CLOSED 为 2。

音频Context 是 一种 事物，含 状态、采样率、currentTime、destination、节点列表、节点数。

音频节点 是 一种 事物，含 类型、输入数、输出数、连接、连接数、gain值、频率、波形。

音频Context_创建 接受 零参：
    ctx 为 做 堆_分配 用 音频Context。
    ctx 的 状态 为 AUDIO_CTX_SUSPENDED。
    ctx 的 采样率 为 44100。
    ctx 的 currentTime 为 0。
    ctx 的 节点数 为 0。
    dest 为 做 堆_分配 用 音频节点。
    dest 的 类型 为 0。
    dest 的 输入数 为 1。
    dest 的 输出数 为 0。
    dest 的 连接数 为 0。
    ctx 的 destination 为 dest。
    返还 ctx。
完。

音频Context_resume 接受 ctx：
    若 ctx 等于 0：
        返还。
    完。
    ctx 的 状态 为 AUDIO_CTX_RUNNING。
完。

音频Context_createGain 接受 ctx：
    若 ctx 等于 0：
        返还 0。
    完。
    n 为 做 堆_分配 用 音频节点。
    n 的 类型 为 1。
    n 的 gain值 为 1024。
    n 的 连接数 为 0。
    返还 n。
完。

音频Context_createOscillator 接受 ctx：
    若 ctx 等于 0：
        返还 0。
    完。
    n 为 做 堆_分配 用 音频节点。
    n 的 类型 为 2。
    n 的 频率 为 440 乘 1024。
    n 的 波形 为 0。
    n 的 连接数 为 0。
    返还 n。
完。

音频节点_connect 接受 src、dst：
    若 src 等于 0 或者 dst 等于 0 或者 src 的 连接数 大于或等于 8：
        返还。
    完。
    src 的 连接 的 第 src 的 连接数 项 为 dst。
    src 的 连接数 为 src 的 连接数 加 1。
完。

智能_媒体_自适应码率 接受 me：
    若 me 等于 0：
        返还。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
完。

智能_音频_增强 接受 frame：
    若 frame 等于 0：
        返还。
    完。
完。

智能_视频_超分 接受 frame、目标宽、目标高：
    若 frame 等于 0：
        返还。
    完。
    若 目标宽 小于或等于 0 或者 目标高 小于或等于 0：
        返还。
    完。
    若 全局媒体能力统计 不等于 空：
        全局媒体能力统计 的 AI视频超分次数 为 全局媒体能力统计 的 AI视频超分次数 加 1。
    完。
    可变 缩放X 为 目标宽 乘 1024 除以 frame 的 宽。
    可变 缩放Y 为 目标高 乘 1024 除以 frame 的 高。
    若 缩放X 小于或等于 1024 并且 缩放Y 小于或等于 1024：
        返还。
    完。
    可变 y 为 0。
    只要 y 小于 目标高：
        源Y 为 y 乘 1024 除以 缩放Y。
        若 源Y 大于或等于 frame 的 高 减 1：
            源Y 为 frame 的 高 减 2。
        完。
        可变 x 为 0。
        只要 x 小于 目标宽：
            源X 为 x 乘 1024 除以 缩放X。
            若 源X 大于或等于 frame 的 宽 减 1：
                源X 为 frame 的 宽 减 2。
            完。
            fx 为 x 乘 1024 除以 缩放X 位与 1023。
            fy 为 y 乘 1024 除以 缩放Y 位与 1023。
            偏移00 为 (源Y 乘 frame 的 宽 加 源X) 乘 4。
            偏移10 为 偏移00 加 4。
            偏移01 为 偏移00 加 frame 的 宽 乘 4。
            偏移11 为 偏移01 加 4。
            可变 ch 为 0。
            只要 ch 小于 4：
                v00 为 做 内存_读8 用 frame 的 像素 加 偏移00 加 ch。
                v10 为 做 内存_读8 用 frame 的 像素 加 偏移10 加 ch。
                v01 为 做 内存_读8 用 frame 的 像素 加 偏移01 加 ch。
                v11 为 做 内存_读8 用 frame 的 像素 加 偏移11 加 ch。
                上行 为 v00 乘 (1024 减 fx) 加 v10 乘 fx。
                下行 为 v01 乘 (1024 减 fx) 加 v11 乘 fx。
                最终 为 上行 乘 (1024 减 fy) 加 下行 乘 fy 右移 20。
                若 最终 大于 255：
                    最终 为 255。
                完。
                若 最终 小于 0：
                    最终 为 0。
                完。
                ch 为 ch 加 1。
            完。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
完。

媒体_canPlayType_增强 接受 mimeType：
    若 mimeType 等于 空：
        若 全局媒体能力统计 不等于 空：
            全局媒体能力统计 的 不支持返回次数 为 全局媒体能力统计 的 不支持返回次数 加 1。
        完。
        返还 「」。
    完。
    若 全局媒体能力统计 不等于 空：
        全局媒体能力统计 的 canPlayType调用次数 为 全局媒体能力统计 的 canPlayType调用次数 加 1。
        全局媒体能力统计 的 MIME解析次数 为 全局媒体能力统计 的 MIME解析次数 加 1。
    完。
    可变 有codecs 为 假。
    若 做 包含 用 mimeType、「codecs」：
        有codecs 为 真。
    完。
    若 有codecs 等于 真 并且 全局媒体能力统计 不等于 空：
        全局媒体能力统计 的 codec字符串解析次数 为 全局媒体能力统计 的 codec字符串解析次数 加 1。
    完。
    结果 为 做 媒体_canPlayType 用 mimeType。
    若 结果 等于 「」：
        若 全局媒体能力统计 不等于 空：
            全局媒体能力统计 的 不支持返回次数 为 全局媒体能力统计 的 不支持返回次数 加 1。
        完。
    否则若 有codecs 等于 真：
        若 全局媒体能力统计 不等于 空：
            全局媒体能力统计 的 probably返回次数 为 全局媒体能力统计 的 probably返回次数 加 1。
        完。
    否则：
        若 全局媒体能力统计 不等于 空：
            全局媒体能力统计 的 maybe返回次数 为 全局媒体能力统计 的 maybe返回次数 加 1。
        完。
    完。
    返还 结果。
完。

音频缓冲 是 一种 事物，含 声道数、帧数、采样率、数据、时长_Q10。

音频缓冲_创建 接受 声道数、帧数、采样率：
    若 声道数 小于或等于 0 或者 声道数 大于 8：
        返还 空。
    完。
    若 帧数 小于或等于 0 或者 采样率 小于或等于 0：
        返还 空。
    完。
    buf 为 做 堆_分配 用 音频缓冲。
    若 buf 等于 空：
        返还 空。
    完。
    buf 的 声道数 为 声道数。
    buf 的 帧数 为 帧数。
    buf 的 采样率 为 采样率。
    buf 的 数据 为 做 堆_分配_清零 用 声道数 乘 帧数 乘 4。
    若 buf 的 数据 等于 空：
        做 堆_释放 用 buf。
        返还 空。
    完。
    buf 的 时长_Q10 为 帧数 乘 1024 除以 采样率。
    若 全局媒体能力统计 不等于 空：
        全局媒体能力统计 的 音频缓冲创建次数 为 全局媒体能力统计 的 音频缓冲创建次数 加 1。
    完。
    返还 buf。
完。

音频缓冲_销毁 接受 buf：
    若 buf 等于 空：
        返还。
    完。
    若 buf 的 数据 不等于 空：
        做 堆_释放 用 buf 的 数据。
    完。
    做 堆_释放 用 buf。
完。

音频缓冲_获取声道数据 接受 buf、声道：
    若 buf 等于 空 或者 声道 大于或等于 buf 的 声道数：
        返还 空。
    完。
    返还 buf 的 数据 加 声道 乘 buf 的 帧数 乘 4。
完。

音频Context_处理图 接受 ctx、帧数：
    若 ctx 等于 空 或者 ctx 的 状态 不等于 AUDIO_CTX_RUNNING：
        返还。
    完。
    若 全局媒体能力统计 不等于 空：
        全局媒体能力统计 的 音频图处理次数 为 全局媒体能力统计 的 音频图处理次数 加 1。
    完。
    ctx 的 currentTime 为 ctx 的 currentTime 加 帧数 乘 1000 除以 ctx 的 采样率。
完。

音频Context_close 接受 ctx：
    若 ctx 等于 空：
        返还。
    完。
    ctx 的 状态 为 AUDIO_CTX_CLOSED。
完。

振荡器_start 接受 node、时间：
    若 node 等于 空 或者 node 的 类型 不等于 音频节点_振荡器：
        返还。
    完。
    若 全局媒体能力统计 不等于 空：
        全局媒体能力统计 的 振荡器启动次数 为 全局媒体能力统计 的 振荡器启动次数 加 1。
    完。
完。

振荡器_stop 接受 node、时间：
    若 node 等于 空 或者 node 的 类型 不等于 音频节点_振荡器：
        返还。
    完。
完。

AI媒体_自适应码率 接受 带宽、缓冲水位、丢帧率：
    若 全局媒体能力AI 等于 空：
        返还 1。
    完。
    AI 为 全局媒体能力AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    AI 的 历史带宽估计 为 (AI 的 历史带宽估计 乘 7 加 带宽) 除以 8。
    AI 的 历史缓冲水位 为 (AI 的 历史缓冲水位 乘 7 加 缓冲水位) 除以 8。
    AI 的 历史丢帧率 为 (AI 的 历史丢帧率 乘 7 加 丢帧率) 除以 8。
    可变 带宽等级 为 1。
    若 AI 的 历史带宽估计 小于 500000：
        带宽等级 为 0。
    完。
    若 AI 的 历史带宽估计 大于 2000000：
        带宽等级 为 2。
    完。
    若 AI 的 历史带宽估计 大于 5000000：
        带宽等级 为 3。
    完。
    若 带宽等级 小于或等于 0：
        AI 的 连续低带宽计数 为 AI 的 连续低带宽计数 加 1。
        AI 的 连续高带宽计数 为 0。
    否则若 带宽等级 大于或等于 2：
        AI 的 连续高带宽计数 为 AI 的 连续高带宽计数 加 1。
        AI 的 连续低带宽计数 为 0。
    完。
    若 AI 的 采样计数器 小于 4：
        返还 AI 的 当前码率档位。
    完。
    AI 的 采样计数器 为 0。
    若 全局媒体能力统计 不等于 空：
        全局媒体能力统计 的 AI码率调用次数 为 全局媒体能力统计 的 AI码率调用次数 加 1。
    完。
    旧档位 为 AI 的 当前码率档位。
    若 AI 的 历史缓冲水位 小于 300 或者 AI 的 历史丢帧率 大于 50：
        若 AI 的 当前码率档位 大于 0：
            AI 的 当前码率档位 为 AI 的 当前码率档位 减 1。
        完。
    否则若 AI 的 历史缓冲水位 大于 700 并且 AI 的 连续高带宽计数 大于 3：
        若 AI 的 当前码率档位 小于 3：
            AI 的 当前码率档位 为 AI 的 当前码率档位 加 1。
        完。
    完。
    若 AI 的 当前码率档位 不等于 旧档位：
        若 全局媒体能力统计 不等于 空：
            全局媒体能力统计 的 AI码率切换次数 为 全局媒体能力统计 的 AI码率切换次数 加 1。
        完。
        做 打印 用 做 字符串拼接 用 「[AI媒体] 码率切换: 」、做 字符串拼接 用 做 转字符串 用 旧档位、做 字符串拼接 用 「→」、做 转字符串 用 AI 的 当前码率档位。
    完。
    AI 的 预测下次码率 为 AI 的 当前码率档位。
    返还 AI 的 当前码率档位。
完。

媒体能力_打印统计 接受 零参：
    若 全局媒体能力统计 等于 空：
        做 打印 用 「[媒体能力] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   媒体能力+音频 · 统计报告          │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ canPlayType:      」、做 转字符串 用 全局媒体能力统计 的 canPlayType调用次数。
    做 打印 用 做 字符串拼接 用 「│ probably:         」、做 转字符串 用 全局媒体能力统计 的 probably返回次数。
    做 打印 用 做 字符串拼接 用 「│ maybe:            」、做 转字符串 用 全局媒体能力统计 的 maybe返回次数。
    做 打印 用 做 字符串拼接 用 「│ 不支持:           」、做 转字符串 用 全局媒体能力统计 的 不支持返回次数。
    做 打印 用 做 字符串拼接 用 「│ AudioContext:     」、做 转字符串 用 全局媒体能力统计 的 AudioContext创建次数。
    做 打印 用 做 字符串拼接 用 「│ 音频节点:         」、做 转字符串 用 全局媒体能力统计 的 音频节点创建次数。
    做 打印 用 做 字符串拼接 用 「│ 节点连接:         」、做 转字符串 用 全局媒体能力统计 的 节点连接次数。
    做 打印 用 做 字符串拼接 用 「│ 音频图处理:       」、做 转字符串 用 全局媒体能力统计 的 音频图处理次数。
    做 打印 用 做 字符串拼接 用 「│ 音频缓冲:         」、做 转字符串 用 全局媒体能力统计 的 音频缓冲创建次数。
    做 打印 用 做 字符串拼接 用 「│ 振荡器启动:       」、做 转字符串 用 全局媒体能力统计 的 振荡器启动次数。
    做 打印 用 做 字符串拼接 用 「│ AI码率调用:       」、做 转字符串 用 全局媒体能力统计 的 AI码率调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI码率切换:       」、做 转字符串 用 全局媒体能力统计 的 AI码率切换次数。
    做 打印 用 做 字符串拼接 用 「│ AI音频增强:       」、做 转字符串 用 全局媒体能力统计 的 AI音频增强次数。
    做 打印 用 做 字符串拼接 用 「│ AI视频超分:       」、做 转字符串 用 全局媒体能力统计 的 AI视频超分次数。
    做 打印 用 做 字符串拼接 用 「│ 总耗时(μs):       」、做 转字符串 用 全局媒体能力统计 的 总处理耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 错误:             」、做 转字符串 用 全局媒体能力统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

媒体能力_导出诊断 接受 零参：
    若 全局媒体能力AI 等于 空：
        做 打印 用 「[媒体能力AI] 未初始化」。
        返还。
    完。
    AI 为 全局媒体能力AI。
    做 打印 用 「[媒体能力AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史带宽(B/s): 」、做 转字符串 用 AI 的 历史带宽估计。
    做 打印 用 做 字符串拼接 用 「  历史缓冲水位(Q10): 」、做 转字符串 用 AI 的 历史缓冲水位。
    做 打印 用 做 字符串拼接 用 「  历史丢帧率(Q10): 」、做 转字符串 用 AI 的 历史丢帧率。
    做 打印 用 做 字符串拼接 用 「  当前码率档位: 」、做 转字符串 用 AI 的 当前码率档位。
    做 打印 用 做 字符串拼接 用 「  预测码率: 」、做 转字符串 用 AI 的 预测下次码率。
    做 打印 用 做 字符串拼接 用 「  连续低带宽: 」、做 转字符串 用 AI 的 连续低带宽计数。
    做 打印 用 做 字符串拼接 用 「  连续高带宽: 」、做 转字符串 用 AI 的 连续高带宽计数。
完。

媒体能力_获取统计 接受 零参：
    返还 全局媒体能力统计。
完。

媒体能力_获取AI状态 接受 零参：
    返还 全局媒体能力AI。
完。

媒体能力_获取当前码率 接受 零参：
    若 全局媒体能力AI 等于 空：
        返还 1。
    完。
    返还 全局媒体能力AI 的 当前码率档位。
完。

媒体能力_是否已初始化 接受 零参：
    返还 全局媒体能力已初始化。
完。

媒体_是视频类型 接受 mimeType：
    若 mimeType 等于 空：
        返还 假。
    完。
    若 做 包含 用 mimeType、「video/」：
        返还 真。
    完。
    返还 假。
完。

媒体_是音频类型 接受 mimeType：
    若 mimeType 等于 空：
        返还 假。
    完。
    若 做 包含 用 mimeType、「audio/」：
        返还 真。
    完。
    返还 假。
完。

音频Context_获取状态 接受 ctx：
    若 ctx 等于 空：
        返还 AUDIO_CTX_CLOSED。
    完。
    返还 ctx 的 状态。
完。

音频Context_获取采样率 接受 ctx：
    若 ctx 等于 空：
        返还 默认采样率。
    完。
    返还 ctx 的 采样率。
完。

本章 收束。
