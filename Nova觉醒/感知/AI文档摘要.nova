《AI文档摘要》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·整数转字符串》。

引用《..·执行·控制台输出》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

页面类型_未知 为 0。

页面类型_文章 为 1。

页面类型_表单 为 2。

页面类型_搜索 为 3。

页面类型_列表 为 4。

页面类型_商品 为 5。

页面类型_登录 为 6。

页面类型_媒体 为 7。

页面类型_首页 为 8。

关键词_最大数 为 32。

标题层级_最大 为 6。

AI页面摘要 是 一种 事物，含 页面类型、主内容区、导航区、页脚区、侧边栏区、广告区数量、正文节点数、图片数、链接数、表单数、输入框数、按钮数、视频数、音频数、DOM节点总数、最大DOM深度、平均DOM深度、深度总和、文本总长度、标题层级计数、关键词列表、关键词数、可读性分数、无障碍分数、SEO分数、内容密度。

文档分析统计 是 一种 事物，含 分析次数、质量评分次数、关键词提取次数、无障碍审计次数、SEO审计次数。

可变 全局_文档分析统计 为 空。

文档分析统计_初始化 接受 零参：
    若 全局_文档分析统计 等于 空：
        全局_文档分析统计 为 新建 文档分析统计。
        全局_文档分析统计 的 分析次数 为 0。
        全局_文档分析统计 的 质量评分次数 为 0。
        全局_文档分析统计 的 关键词提取次数 为 0。
        全局_文档分析统计 的 无障碍审计次数 为 0。
        全局_文档分析统计 的 SEO审计次数 为 0。
    完。
完。

AI文档_生成摘要 接受 文档：
    做 文档分析统计_初始化。
    全局_文档分析统计 的 分析次数 为 全局_文档分析统计 的 分析次数 加 1。
    摘要 为 做 堆_分配 用 AI页面摘要。
    摘要 的 页面类型 为 页面类型_未知。
    摘要 的 主内容区 为 0。
    摘要 的 导航区 为 0。
    摘要 的 页脚区 为 0。
    摘要 的 侧边栏区 为 0。
    摘要 的 广告区数量 为 0。
    摘要 的 正文节点数 为 0。
    摘要 的 图片数 为 0。
    摘要 的 链接数 为 0。
    摘要 的 表单数 为 0。
    摘要 的 输入框数 为 0。
    摘要 的 按钮数 为 0。
    摘要 的 视频数 为 0。
    摘要 的 音频数 为 0。
    摘要 的 DOM节点总数 为 0。
    摘要 的 最大DOM深度 为 0。
    摘要 的 平均DOM深度 为 0。
    摘要 的 深度总和 为 0。
    摘要 的 文本总长度 为 0。
    摘要 的 标题层级计数 为 做 堆_分配 用 标题层级_最大 乘 8。
    可变 h 为 0。
    只要 h 小于 标题层级_最大：
        做 内存_写64 用 摘要 的 标题层级计数 加 h 乘 8、0。
        h 为 h 加 1。
    完。
    摘要 的 关键词列表 为 做 堆_分配 用 关键词_最大数 乘 16。
    摘要 的 关键词数 为 0。
    摘要 的 可读性分数 为 0。
    摘要 的 无障碍分数 为 0。
    摘要 的 SEO分数 为 0。
    摘要 的 内容密度 为 0。
    若 文档 等于 0：
        返还 摘要。
    完。
    做 AI文档_遍历统计 用 文档、摘要、0。
    若 摘要 的 DOM节点总数 大于 0：
        摘要 的 平均DOM深度 为 摘要 的 深度总和 除以 摘要 的 DOM节点总数。
    完。
    若 摘要 的 DOM节点总数 大于 0：
        摘要 的 内容密度 为 摘要 的 正文节点数 乘 100 除以 摘要 的 DOM节点总数。
    完。
    摘要 的 页面类型 为 做 AI文档_推断类型 用 摘要。
    摘要 的 可读性分数 为 做 AI文档_可读性评分 用 摘要。
    摘要 的 无障碍分数 为 做 AI文档_无障碍评分 用 摘要。
    摘要 的 SEO分数 为 做 AI文档_SEO评分 用 摘要。
    返还 摘要。
完。

AI文档_推断类型 接受 摘要：
    若 摘要 的 表单数 大于 0 并且 摘要 的 输入框数 小于或等于 4 并且 摘要 的 DOM节点总数 小于 150：
        返还 页面类型_登录。
    完。
    若 摘要 的 视频数 大于 0 或者 摘要 的 音频数 大于 0：
        返还 页面类型_媒体。
    完。
    若 摘要 的 正文节点数 大于 20 并且 摘要 的 图片数 小于 摘要 的 正文节点数：
        返还 页面类型_文章。
    完。
    若 摘要 的 表单数 大于 0 并且 摘要 的 输入框数 大于 3：
        返还 页面类型_表单。
    完。
    若 摘要 的 图片数 大于 5 并且 摘要 的 按钮数 大于 2：
        返还 页面类型_商品。
    完。
    若 摘要 的 链接数 大于 30：
        返还 页面类型_搜索。
    完。
    若 摘要 的 链接数 大于 15 并且 摘要 的 图片数 大于 5：
        返还 页面类型_列表。
    完。
    返还 页面类型_首页。
完。

AI文档_遍历统计 接受 节点、摘要、深度：
    若 节点 等于 0：
        返还。
    完。
    摘要 的 DOM节点总数 为 摘要 的 DOM节点总数 加 1。
    摘要 的 深度总和 为 摘要 的 深度总和 加 深度。
    若 深度 大于 摘要 的 最大DOM深度：
        摘要 的 最大DOM深度 为 深度。
    完。
    若 节点 的 语义角色 等于 语义_正文 或者 节点 的 语义角色 等于 语义_标题：
        摘要 的 正文节点数 为 摘要 的 正文节点数 加 1。
    完。
    若 节点 的 语义角色 等于 语义_导航栏 并且 摘要 的 导航区 等于 0：
        摘要 的 导航区 为 节点。
    完。
    若 节点 的 语义角色 等于 语义_页脚 并且 摘要 的 页脚区 等于 0：
        摘要 的 页脚区 为 节点。
    完。
    若 节点 的 语义角色 等于 语义_侧边栏 并且 摘要 的 侧边栏区 等于 0：
        摘要 的 侧边栏区 为 节点。
    完。
    若 节点 的 语义角色 等于 语义_广告：
        摘要 的 广告区数量 为 摘要 的 广告区数量 加 1。
    完。
    若 节点 的 语义角色 等于 语义_主内容 并且 摘要 的 主内容区 等于 0：
        摘要 的 主内容区 为 节点。
    完。
    若 节点 的 标签名 等于 「img」：
        摘要 的 图片数 为 摘要 的 图片数 加 1。
    完。
    若 节点 的 标签名 等于 「a」：
        摘要 的 链接数 为 摘要 的 链接数 加 1。
    完。
    若 节点 的 标签名 等于 「form」：
        摘要 的 表单数 为 摘要 的 表单数 加 1。
    完。
    若 节点 的 标签名 等于 「input」 或者 节点 的 标签名 等于 「textarea」：
        摘要 的 输入框数 为 摘要 的 输入框数 加 1。
    完。
    若 节点 的 标签名 等于 「button」：
        摘要 的 按钮数 为 摘要 的 按钮数 加 1。
    完。
    若 节点 的 标签名 等于 「video」：
        摘要 的 视频数 为 摘要 的 视频数 加 1。
    完。
    若 节点 的 标签名 等于 「audio」：
        摘要 的 音频数 为 摘要 的 音频数 加 1。
    完。
    若 节点 的 标签名 等于 「h1」：
        做 AI文档_增加标题计数 用 摘要、0。
    完。
    若 节点 的 标签名 等于 「h2」：
        做 AI文档_增加标题计数 用 摘要、1。
    完。
    若 节点 的 标签名 等于 「h3」：
        做 AI文档_增加标题计数 用 摘要、2。
    完。
    若 节点 的 标签名 等于 「h4」：
        做 AI文档_增加标题计数 用 摘要、3。
    完。
    若 节点 的 标签名 等于 「h5」：
        做 AI文档_增加标题计数 用 摘要、4。
    完。
    若 节点 的 标签名 等于 「h6」：
        做 AI文档_增加标题计数 用 摘要、5。
    完。
    若 节点 的 节点类型 等于 3 并且 节点 的 文本内容 不等于 0：
        摘要 的 文本总长度 为 摘要 的 文本总长度 加 做 字符串长度 用 节点 的 文本内容。
    完。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        做 AI文档_遍历统计 用 子、摘要、深度 加 1。
        子 为 子 的 下一兄弟。
    完。
完。

AI文档_增加标题计数 接受 摘要、层级：
    若 层级 大于或等于 0 并且 层级 小于 标题层级_最大：
        旧 为 做 内存_读64 用 摘要 的 标题层级计数 加 层级 乘 8。
        做 内存_写64 用 摘要 的 标题层级计数 加 层级 乘 8、旧 加 1。
    完。
完。

AI文档_按角色查找 接受 文档、角色：
    返还 做 AI文档_角色搜索 用 文档、角色。
完。

AI文档_按角色查找_全部 接受 文档、角色：
    可变 结果 为 一组：（空）。
    做 AI文档_角色搜索_全部 用 文档、角色、结果。
    返还 结果。
完。

AI文档_角色搜索 接受 节点、角色：
    若 节点 等于 0：
        返还 0。
    完。
    若 节点 的 语义角色 等于 角色：
        返还 节点。
    完。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        结果 为 做 AI文档_角色搜索 用 子、角色。
        若 结果 不等于 0：
            返还 结果。
        完。
        子 为 子 的 下一兄弟。
    完。
    返还 0。
完。

AI文档_角色搜索_全部 接受 节点、角色、结果：
    若 节点 等于 0：
        返还。
    完。
    若 节点 的 语义角色 等于 角色：
        做 追加 用 结果、节点。
    完。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        做 AI文档_角色搜索_全部 用 子、角色、结果。
        子 为 子 的 下一兄弟。
    完。
完。

AI文档_可读性评分 接受 摘要：
    可变 分数 为 100。
    若 摘要 的 文本总长度 小于 100：
        分数 为 分数 减 20。
    完。
    若 摘要 的 平均DOM深度 大于 10：
        分数 为 分数 减 10。
    完。
    若 摘要 的 平均DOM深度 大于 20：
        分数 为 分数 减 15。
    完。
    若 摘要 的 内容密度 小于 10：
        分数 为 分数 减 15。
    完。
    h1数 为 做 内存_读64 用 摘要 的 标题层级计数。
    若 h1数 等于 1：
        分数 为 分数 加 5。
    完。
    若 h1数 大于 1：
        分数 为 分数 减 5。
    完。
    若 分数 小于 0：
        分数 为 0。
    完。
    若 分数 大于 100：
        分数 为 100。
    完。
    返还 分数。
完。

AI文档_无障碍评分 接受 摘要：
    做 文档分析统计_初始化。
    全局_文档分析统计 的 无障碍审计次数 为 全局_文档分析统计 的 无障碍审计次数 加 1。
    可变 分数 为 100。
    若 摘要 的 图片数 大于 0：
        分数 为 分数 减 5。
    完。
    若 摘要 的 输入框数 大于 0：
        分数 为 分数 减 5。
    完。
    若 摘要 的 导航区 等于 0：
        分数 为 分数 减 10。
    完。
    若 摘要 的 主内容区 等于 0：
        分数 为 分数 减 10。
    完。
    h2数 为 做 内存_读64 用 摘要 的 标题层级计数 加 1 乘 8。
    h3数 为 做 内存_读64 用 摘要 的 标题层级计数 加 2 乘 8。
    若 h3数 大于 0 并且 h2数 等于 0：
        分数 为 分数 减 10。
    完。
    若 分数 小于 0：
        分数 为 0。
    完。
    返还 分数。
完。

AI文档_SEO评分 接受 摘要：
    做 文档分析统计_初始化。
    全局_文档分析统计 的 SEO审计次数 为 全局_文档分析统计 的 SEO审计次数 加 1。
    可变 分数 为 100。
    h1数 为 做 内存_读64 用 摘要 的 标题层级计数。
    若 h1数 等于 0：
        分数 为 分数 减 20。
    完。
    若 h1数 大于 1：
        分数 为 分数 减 10。
    完。
    若 摘要 的 文本总长度 小于 300：
        分数 为 分数 减 15。
    完。
    若 摘要 的 图片数 大于 10：
        分数 为 分数 减 5。
    完。
    若 摘要 的 链接数 大于 100：
        分数 为 分数 减 10。
    完。
    若 摘要 的 DOM节点总数 大于 5000：
        分数 为 分数 减 10。
    完。
    若 分数 小于 0：
        分数 为 0。
    完。
    返还 分数。
完。

AI文档_质量评分 接受 文档：
    做 文档分析统计_初始化。
    全局_文档分析统计 的 质量评分次数 为 全局_文档分析统计 的 质量评分次数 加 1。
    摘要 为 做 AI文档_生成摘要 用 文档。
    可变 分数 为 100。
    若 摘要 的 最大DOM深度 大于 30：
        分数 为 分数 减 10。
    完。
    若 摘要 的 最大DOM深度 大于 50：
        分数 为 分数 减 20。
    完。
    若 摘要 的 DOM节点总数 大于 3000：
        分数 为 分数 减 10。
    完。
    若 摘要 的 DOM节点总数 大于 10000：
        分数 为 分数 减 20。
    完。
    若 摘要 的 广告区数量 大于 5：
        分数 为 分数 减 15。
    完。
    综合 为 (分数 加 摘要 的 可读性分数 加 摘要 的 无障碍分数 加 摘要 的 SEO分数) 除以 4。
    若 综合 小于 0：
        综合 为 0。
    完。
    若 综合 大于 100：
        综合 为 100。
    完。
    返还 综合。
完。

AI文档_类型名称 接受 类型：
    若 类型 等于 页面类型_文章：
        返还 「文章页」。
    完。
    若 类型 等于 页面类型_表单：
        返还 「表单页」。
    完。
    若 类型 等于 页面类型_搜索：
        返还 「搜索页」。
    完。
    若 类型 等于 页面类型_列表：
        返还 「列表页」。
    完。
    若 类型 等于 页面类型_商品：
        返还 「商品页」。
    完。
    若 类型 等于 页面类型_登录：
        返还 「登录页」。
    完。
    若 类型 等于 页面类型_媒体：
        返还 「媒体页」。
    完。
    若 类型 等于 页面类型_首页：
        返还 「首页」。
    完。
    返还 「未知」。
完。

AI文档_打印摘要 接受 摘要：
    若 摘要 等于 0：
        返还。
    完。
    做 打印行 用 「=== 页面分析摘要 ===」。
    做 打印行 用 「页面类型: 」 加 做 AI文档_类型名称 用 摘要 的 页面类型。
    做 打印行 用 「DOM节点数: 」 加 做 整数转字符串 用 摘要 的 DOM节点总数。
    做 打印行 用 「最大深度: 」 加 做 整数转字符串 用 摘要 的 最大DOM深度。
    做 打印行 用 「平均深度: 」 加 做 整数转字符串 用 摘要 的 平均DOM深度。
    做 打印行 用 「正文节点: 」 加 做 整数转字符串 用 摘要 的 正文节点数。
    做 打印行 用 「图片数: 」 加 做 整数转字符串 用 摘要 的 图片数。
    做 打印行 用 「链接数: 」 加 做 整数转字符串 用 摘要 的 链接数。
    做 打印行 用 「表单数: 」 加 做 整数转字符串 用 摘要 的 表单数。
    做 打印行 用 「输入框: 」 加 做 整数转字符串 用 摘要 的 输入框数。
    做 打印行 用 「按钮数: 」 加 做 整数转字符串 用 摘要 的 按钮数。
    做 打印行 用 「视频数: 」 加 做 整数转字符串 用 摘要 的 视频数。
    做 打印行 用 「广告区: 」 加 做 整数转字符串 用 摘要 的 广告区数量。
    做 打印行 用 「文本长度: 」 加 做 整数转字符串 用 摘要 的 文本总长度。
    做 打印行 用 「内容密度: 」 加 做 整数转字符串 用 摘要 的 内容密度 加 「%」。
    做 打印行 用 「可读性: 」 加 做 整数转字符串 用 摘要 的 可读性分数。
    做 打印行 用 「无障碍: 」 加 做 整数转字符串 用 摘要 的 无障碍分数。
    做 打印行 用 「SEO: 」 加 做 整数转字符串 用 摘要 的 SEO分数。
完。

文档分析_打印统计 接受 零参：
    做 文档分析统计_初始化。
    做 打印行 用 「=== 文档分析统计 ===」。
    做 打印行 用 「分析次数: 」 加 做 整数转字符串 用 全局_文档分析统计 的 分析次数。
    做 打印行 用 「质量评分: 」 加 做 整数转字符串 用 全局_文档分析统计 的 质量评分次数。
    做 打印行 用 「关键词提取: 」 加 做 整数转字符串 用 全局_文档分析统计 的 关键词提取次数。
    做 打印行 用 「无障碍审计: 」 加 做 整数转字符串 用 全局_文档分析统计 的 无障碍审计次数。
    做 打印行 用 「SEO审计: 」 加 做 整数转字符串 用 全局_文档分析统计 的 SEO审计次数。
完。

内容指纹_计算 接受 文本、长度：
    若 文本 等于 0 或者 长度 小于或等于 0：
        返还 0。
    完。
    可变 hash 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        hash 为 hash 乘 31 加 做 字节取 用 文本、i。
        i 为 i 加 1。
    完。
    返还 hash。
完。

内容指纹_相似度 接受 fp1、fp2：
    diff 为 fp1 位异或 fp2。
    可变 bits 为 0。
    可变 d 为 diff。
    只要 d 大于 0：
        bits 为 bits 加 (d 位与 1)。
        d 为 d 右移 1。
    完。
    返还 (32 减 bits) 乘 100 除以 32。
完。

计算_TF 接受 词频、总词数：
    若 总词数 小于或等于 0：
        返还 0。
    完。
    返还 词频 乘 1000 除以 总词数。
完。

计算_IDF 接受 文档总数、包含文档数：
    若 包含文档数 小于或等于 0：
        返还 0。
    完。
    比值 为 文档总数 乘 1000 除以 包含文档数。
    若 比值 小于或等于 1000：
        返还 0。
    完。
    可变 结果 为 0。
    可变 v 为 比值。
    只要 v 大于 1000：
        v 为 v 除以 2。
        结果 为 结果 加 1。
    完。
    返还 结果 乘 1000。
完。

计算_TFIDF 接受 词频、总词数、文档总数、包含文档数：
    tf 为 做 计算_TF 用 词频、总词数。
    idf 为 做 计算_IDF 用 文档总数、包含文档数。
    返还 tf 乘 idf 除以 1000。
完。

文档分析_重置统计 接受 零参：
    做 文档分析统计_初始化。
    全局_文档分析统计 的 分析次数 为 0。
    全局_文档分析统计 的 质量评分次数 为 0。
    全局_文档分析统计 的 关键词提取次数 为 0。
    全局_文档分析统计 的 无障碍审计次数 为 0。
    全局_文档分析统计 的 SEO审计次数 为 0。
完。

文档分析_获取分析次数 接受 零参：
    做 文档分析统计_初始化。
    返还 全局_文档分析统计 的 分析次数。
完。

文档分析_获取质量评分次数 接受 零参：
    做 文档分析统计_初始化。
    返还 全局_文档分析统计 的 质量评分次数。
完。

本章 收束。
