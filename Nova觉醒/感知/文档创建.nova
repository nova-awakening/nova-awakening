《文档创建》

引用《.·节点》。

引用《.·样式解析》。

引用《.·内核直连层》。

引用《.·原子驻留》。

引用《..·中枢·内存初始化》。

引用《..·中枢·整数转字符串》。

引用《.·文档节点》。

引用《..·中枢·辅助存根》。

文档状态_加载中 为 0。

文档状态_交互中 为 1。

文档状态_完成 为 2。

兼容模式_怪异 为 0。

兼容模式_有限怪异 为 1。

兼容模式_标准 为 2。

解析状态_未开始 为 0。

解析状态_解析中 为 1。

解析状态_已完成 为 2。

Document扩展 是 一种 事物，含 基础节点、URL、基础URL、来源、就绪状态、兼容模式、解析状态、内容类型、字符编码、文档元素、head元素、body元素、当前脚本、标题、id索引_键、id索引_值、id索引_数量、id索引_容量、样式引擎、脚本上下文、解析器、焦点元素、活跃元素、悬停元素、悬停开始时间、悬停已分发AI、悬停x、悬停y、选区、动画帧回调列表、下一个动画帧ID、cookie、节点总数、元素总数。

Document_创建 接受 url：
    节点 为 做 节点_创建 用 DOCUMENT_节点、节点标志_是文档 位或 节点标志_是容器节点 位或 节点标志_已连接。
    扩展 为 做 堆_分配 用 Document扩展。
    扩展 的 基础节点 为 节点。
    扩展 的 URL 为 做 字符串_复制 用 url。
    扩展 的 基础URL 为 做 字符串_复制 用 url。
    扩展 的 来源 为 0。
    扩展 的 就绪状态 为 文档状态_加载中。
    扩展 的 兼容模式 为 兼容模式_标准。
    扩展 的 解析状态 为 解析状态_未开始。
    扩展 的 内容类型 为 做 字符串_复制 用 「text/html」。
    扩展 的 字符编码 为 做 字符串_复制 用 「UTF-8」。
    扩展 的 文档元素 为 0。
    扩展 的 head元素 为 0。
    扩展 的 body元素 为 0。
    扩展 的 当前脚本 为 0。
    扩展 的 标题 为 0。
    扩展 的 id索引_容量 为 64。
    扩展 的 id索引_键 为 做 堆_分配_数组 用 扩展 的 id索引_容量 乘 4。
    扩展 的 id索引_值 为 做 堆_分配_数组 用 扩展 的 id索引_容量 乘 8。
    扩展 的 id索引_数量 为 0。
    扩展 的 样式引擎 为 0。
    扩展 的 脚本上下文 为 0。
    扩展 的 解析器 为 0。
    扩展 的 焦点元素 为 0。
    扩展 的 活跃元素 为 0。
    扩展 的 悬停元素 为 0。
    扩展 的 悬停开始时间 为 0。
    扩展 的 悬停已分发AI 为 假。
    扩展 的 悬停x 为 0。
    扩展 的 悬停y 为 0。
    扩展 的 选区 为 0。
    扩展 的 动画帧回调列表 为 0。
    扩展 的 下一个动画帧ID 为 1。
    扩展 的 cookie 为 0。
    扩展 的 节点总数 为 1。
    扩展 的 元素总数 为 0。
    节点 的 渲染样式 为 扩展。
    节点 的 树作用域 为 节点。
    返还 节点。
完。

Document_获取扩展 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    若 非 (节点 的 节点标志 位与 节点标志_是文档)：
        返还 0。
    完。
    返还 节点 的 渲染样式。
完。

Document_获取URL 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 0。
    完。
    返还 ext 的 URL。
完。

Document_获取标题 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 「」。
    完。
    返还 ext 的 标题。
完。

Document_设置标题 接受 doc、标题：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还。
    完。
    ext 的 标题 为 做 字符串_复制 用 标题。
完。

Document_获取就绪状态 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 文档状态_加载中。
    完。
    返还 ext 的 就绪状态。
完。

Document_设置就绪状态 接受 doc、状态：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还。
    完。
    ext 的 就绪状态 为 状态。
完。

Document_就绪状态名 接受 状态：
    若 状态 等于 文档状态_加载中：
        返还 「loading」。
    完。
    若 状态 等于 文档状态_交互中：
        返还 「interactive」。
    完。
    若 状态 等于 文档状态_完成：
        返还 「complete」。
    完。
    返还 「unknown」。
完。

Document_获取body 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 0。
    完。
    返还 ext 的 body元素。
完。

Document_获取head 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 0。
    完。
    返还 ext 的 head元素。
完。

Document_获取文档元素 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 0。
    完。
    返还 ext 的 文档元素。
完。

Document_获取内容类型 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 「text/html」。
    完。
    返还 ext 的 内容类型。
完。

Document_获取字符编码 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 「UTF-8」。
    完。
    返还 ext 的 字符编码。
完。

Document_获取兼容模式 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 兼容模式_标准。
    完。
    返还 ext 的 兼容模式。
完。

Document_兼容模式名 接受 模式：
    若 模式 等于 兼容模式_怪异：
        返还 「BackCompat」。
    完。
    若 模式 等于 兼容模式_有限怪异：
        返还 「CSS1Compat (limited)」。
    完。
    返还 「CSS1Compat」。
完。

Document_getElementById 接受 doc、id：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0 或者 id 等于 0：
        返还 0。
    完。
    哈希 为 做 字符串_哈希 用 id 取余于 ext 的 id索引_容量。
    可变 探测 为 哈希。
    可变 次数 为 0。
    只要 次数 小于 ext 的 id索引_容量：
        键 为 做 内存_读32 用 ext 的 id索引_键 加 探测 乘 4。
        若 键 等于 0：
            返还 0。
        完。
        若 键 等于 id 或者 做 字符串_相等 用 键、id：
            返还 做 内存_读64 用 ext 的 id索引_值 加 探测 乘 8。
        完。
        探测 为 (探测 加 1) 取余于 ext 的 id索引_容量。
        次数 为 次数 加 1。
    完。
    返还 0。
完。

Document_注册id 接受 doc、id、节点：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0 或者 id 等于 0：
        返还。
    完。
    若 ext 的 id索引_数量 大于或等于 ext 的 id索引_容量 乘 3 除以 4：
        做 Document_id索引_扩容 用 ext。
    完。
    哈希 为 做 字符串_哈希 用 id 取余于 ext 的 id索引_容量。
    可变 探测 为 哈希。
    只要 真：
        键 为 做 内存_读32 用 ext 的 id索引_键 加 探测 乘 4。
        若 键 等于 0：
            做 内存_写32 用 ext 的 id索引_键 加 探测 乘 4、id。
            做 内存_写64 用 ext 的 id索引_值 加 探测 乘 8、节点。
            ext 的 id索引_数量 为 ext 的 id索引_数量 加 1。
            返还。
        完。
        若 键 等于 id 或者 做 字符串_相等 用 键、id：
            做 内存_写64 用 ext 的 id索引_值 加 探测 乘 8、节点。
            返还。
        完。
        探测 为 (探测 加 1) 取余于 ext 的 id索引_容量。
    完。
完。

Document_注销id 接受 doc、id：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0 或者 id 等于 0：
        返还。
    完。
    哈希 为 做 字符串_哈希 用 id 取余于 ext 的 id索引_容量。
    可变 探测 为 哈希。
    可变 次数 为 0。
    只要 次数 小于 ext 的 id索引_容量：
        键 为 做 内存_读32 用 ext 的 id索引_键 加 探测 乘 4。
        若 键 等于 0：
            返还。
        完。
        若 键 等于 id 或者 做 字符串_相等 用 键、id：
            做 内存_写32 用 ext 的 id索引_键 加 探测 乘 4、0。
            做 内存_写64 用 ext 的 id索引_值 加 探测 乘 8、0。
            ext 的 id索引_数量 为 ext 的 id索引_数量 减 1。
            返还。
        完。
        探测 为 (探测 加 1) 取余于 ext 的 id索引_容量。
        次数 为 次数 加 1。
    完。
完。

Document_id索引_扩容 接受 ext：
    旧容量 为 ext 的 id索引_容量。
    新容量 为 旧容量 乘 2。
    新键 为 做 堆_分配_数组 用 新容量 乘 4。
    新值 为 做 堆_分配_数组 用 新容量 乘 8。
    可变 i 为 0。
    只要 i 小于 旧容量：
        键 为 做 内存_读32 用 ext 的 id索引_键 加 i 乘 4。
        若 键 不等于 0：
            值 为 做 内存_读64 用 ext 的 id索引_值 加 i 乘 8。
            新哈希 为 做 字符串_哈希 用 键 取余于 新容量。
            做 内存_写32 用 新键 加 新哈希 乘 4、键。
            做 内存_写64 用 新值 加 新哈希 乘 8、值。
        完。
        i 为 i 加 1。
    完。
    做 堆_释放 用 ext 的 id索引_键。
    做 堆_释放 用 ext 的 id索引_值。
    ext 的 id索引_键 为 新键。
    ext 的 id索引_值 为 新值。
    ext 的 id索引_容量 为 新容量。
完。

Document_createElement 接受 doc、标签名：
    若 doc 等于 0 或者 标签名 等于 0：
        返还 0。
    完。
    ext 为 做 Document_获取扩展 用 doc。
    节点 为 做 元素_创建 用 标签名、doc。
    若 ext 不等于 0：
        ext 的 元素总数 为 ext 的 元素总数 加 1。
    完。
    返还 节点。
完。

Document_createTextNode 接受 doc、文本：
    若 doc 等于 0：
        返还 0。
    完。
    ext 为 做 Document_获取扩展 用 doc。
    节点 为 做 节点_创建 用 TEXT_节点、0。
    节点 的 数据 为 做 字符串_复制 用 文本。
    若 ext 不等于 0：
        ext 的 节点总数 为 ext 的 节点总数 加 1。
    完。
    返还 节点。
完。

Document_createComment 接受 doc、文本：
    若 doc 等于 0：
        返还 0。
    完。
    节点 为 做 节点_创建 用 COMMENT_节点、0。
    节点 的 数据 为 做 字符串_复制 用 文本。
    返还 节点。
完。

Document_createDocumentFragment 接受 doc：
    若 doc 等于 0：
        返还 0。
    完。
    节点 为 做 节点_创建 用 DOCUMENT_FRAGMENT_节点、节点标志_是容器节点。
    返还 节点。
完。

ELEMENT_节点 为 1。

ATTRIBUTE_节点 为 2。

TEXT_节点 为 3。

CDATA_SECTION_节点 为 4。

PROCESSING_INSTRUCTION_节点 为 7。

COMMENT_节点 为 8。

DOCUMENT_节点 为 9。

DOCUMENT_TYPE_节点 为 10。

DOCUMENT_FRAGMENT_节点 为 11。

节点标志_是文档 为 0x0001。

节点标志_是容器节点 为 0x0002。

节点标志_已连接 为 0x0004。

节点标志_有子节点 为 0x0008。

节点类型_名称 接受 类型：
    若 类型 等于 ELEMENT_节点：
        返还 「ELEMENT_NODE」。
    完。
    若 类型 等于 TEXT_节点：
        返还 「TEXT_NODE」。
    完。
    若 类型 等于 COMMENT_节点：
        返还 「COMMENT_NODE」。
    完。
    若 类型 等于 DOCUMENT_节点：
        返还 「DOCUMENT_NODE」。
    完。
    若 类型 等于 DOCUMENT_TYPE_节点：
        返还 「DOCUMENT_TYPE_NODE」。
    完。
    若 类型 等于 DOCUMENT_FRAGMENT_节点：
        返还 「DOCUMENT_FRAGMENT_NODE」。
    完。
    返还 「UNKNOWN」。
完。

Document_获取焦点元素 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 0。
    完。
    返还 ext 的 焦点元素。
完。

Document_设置焦点 接受 doc、元素：
    若 doc 等于 0：
        返还。
    完。
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还。
    完。
    若 元素 不等于 0：
        若 元素 的 树作用域 不等于 doc：
            返还。
        完。
        做 Focus_设置 用 元素。
        返还。
    完。
    若 ext 的 焦点元素 不等于 0：
        做 Focus_设置 用 0。
    完。
完。

Document_hasFocus 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 假。
    完。
    返还 ext 的 焦点元素 不等于 0。
完。

Document_获取cookie 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 「」。
    完。
    若 ext 的 cookie 等于 0：
        返还 「」。
    完。
    返还 ext 的 cookie。
完。

Document_设置cookie 接受 doc、cookie_str：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还。
    完。
    ext 的 cookie 为 做 字符串_复制 用 cookie_str。
完。

Document_触发事件 接受 doc、目标、事件名：
    若 doc 等于 0 或者 目标 等于 0：
        返还。
    完。
    Document_事件计数 为 Document_事件计数 加 1。
完。

可变 Document_事件计数 为 0。

可变 Document_创建总数 为 0。

Document_统计 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 一本字典：URL 对应 「」、就绪状态 对应 「unknown」、节点总数 对应 0、元素总数 对应 0、id索引数 对应 0。
    完。
    返还 一本字典：URL 对应 ext 的 URL、就绪状态 对应 做 Document_就绪状态名 用 ext 的 就绪状态、兼容模式 对应 做 Document_兼容模式名 用 ext 的 兼容模式、内容类型 对应 ext 的 内容类型、字符编码 对应 ext 的 字符编码、节点总数 对应 ext 的 节点总数、元素总数 对应 ext 的 元素总数、id索引数 对应 ext 的 id索引_数量、有焦点 对应 ext 的 焦点元素 不等于 0、事件数 对应 Document_事件计数。
完。

Document_健康检查 接受 doc：
    可变 问题 为 一组：（空）。
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        做 追加 用 问题、「Document未初始化」。
        返还 问题。
    完。
    若 ext 的 节点总数 大于 100000：
        做 追加 用 问题、「DOM节点过多: 」 加 做 整数转字符串 用 ext 的 节点总数。
    完。
    若 ext 的 id索引_数量 大于 ext 的 id索引_容量 乘 9 除以 10：
        做 追加 用 问题、「id索引接近满: 」 加 做 整数转字符串 用 ext 的 id索引_数量 加 「/」 加 做 整数转字符串 用 ext 的 id索引_容量。
    完。
    若 ext 的 就绪状态 等于 文档状态_加载中 并且 Document_事件计数 大于 10000：
        做 追加 用 问题、「文档仍在加载但事件过多」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「Document健康」。
    完。
    返还 问题。
完。

Document_完整统计 接受 doc：
    ext 为 做 Document_获取扩展 用 doc。
    若 ext 等于 0：
        返还 一组：0、0、0、0。
    完。
    返还 一组：ext 的 节点总数、ext 的 元素总数、ext 的 id索引_数量、Document_事件计数。
完。

本章 收束。
