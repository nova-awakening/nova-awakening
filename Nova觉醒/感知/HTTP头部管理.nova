《HTTP头部管理》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·辅助存根》。

引用《..·中枢·字符串替换》。

HTTP头部条目 是 一种 事物，含 名、名长度、值、值长度。

HTTP头部列表 是 一种 事物，含 条目、数量、容量。

HTTP头部_创建 接受 零参：
    h 为 做 堆_分配 用 HTTP头部列表。
    h 的 容量 为 32。
    h 的 条目 为 做 堆_分配_数组 用 h 的 容量 乘 32。
    h 的 数量 为 0。
    返还 h。
完。

HTTP头部_添加 接受 h、名、值：
    若 h 的 数量 大于或等于 h 的 容量：
        返还。
    完。
    偏移 为 h 的 条目 加 h 的 数量 乘 32。
    名复制 为 做 字符串_复制 用 名。
    值复制 为 做 字符串_复制 用 值。
    做 内存_写64 用 偏移、名复制。
    做 内存_写32 用 偏移 加 8、做 字符串_长度 用 名。
    做 内存_写64 用 偏移 加 16、值复制。
    做 内存_写32 用 偏移 加 24、做 字符串_长度 用 值。
    h 的 数量 为 h 的 数量 加 1。
完。

HTTP头部_获取 接受 h、名：
    可变 i 为 0。
    只要 i 小于 h 的 数量：
        偏移 为 h 的 条目 加 i 乘 32。
        已有名 为 做 内存_读64 用 偏移。
        若 做 字符串_相等_不区分大小写 用 已有名、名：
            返还 做 内存_读64 用 偏移 加 16。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

字符串_相等_不区分大小写 接受 a、b：
    若 a 等于 b：
        返还 1。
    完。
    若 a 等于 0 或者 b 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 1：
        可变 ca 为 做 内存_读8 用 a 加 i。
        可变 cb 为 做 内存_读8 用 b 加 i。
        若 ca 大于或等于 0x41 并且 ca 小于或等于 0x5A：
            ca 为 ca 加 0x20。
        完。
        若 cb 大于或等于 0x41 并且 cb 小于或等于 0x5A：
            cb 为 cb 加 0x20。
        完。
        若 ca 不等于 cb：
            返还 0。
        完。
        若 ca 等于 0：
            返还 1。
        完。
        i 为 i 加 1。
    完。
完。

ResourceRequest 是 一种 事物，含 url、方法、请求头、请求体、请求体长度、资源类型、优先级、引荐者、来源、缓存模式、凭证模式、重定向模式、完整性、是导航、是预取。

ResourceRequest_创建 接受 url字符串、方法、资源类型：
    req 为 做 堆_分配 用 ResourceRequest。
    req 的 url 为 做 URL_解析 用 url字符串。
    req 的 方法 为 方法。
    req 的 请求头 为 做 HTTP头部_创建。
    req 的 请求体 为 0。
    req 的 请求体长度 为 0。
    req 的 资源类型 为 资源类型。
    req 的 引荐者 为 0。
    req 的 来源 为 0。
    req 的 缓存模式 为 0。
    req 的 凭证模式 为 1。
    req 的 重定向模式 为 0。
    req 的 完整性 为 0。
    req 的 是导航 为 假。
    req 的 是预取 为 假。
    若 资源类型 等于 资源类型_主文档 或者 资源类型 等于 资源类型_样式表：
        req 的 优先级 为 优先级_最高。
    否则若 资源类型 等于 资源类型_脚本：
        req 的 优先级 为 优先级_高。
    否则若 资源类型 等于 资源类型_字体：
        req 的 优先级 为 优先级_中。
    否则若 资源类型 等于 资源类型_图片：
        req 的 优先级 为 优先级_低。
    否则：
        req 的 优先级 为 优先级_低。
    完。
    做 HTTP头部_添加 用 req 的 请求头、「User-智能体」、「NovaBrowser/1.0 (NovaOS)」。
    做 HTTP头部_添加 用 req 的 请求头、「Accept」、「*/*」。
    做 HTTP头部_添加 用 req 的 请求头、「Accept-Encoding」、「gzip, deflate, br」。
    做 HTTP头部_添加 用 req 的 请求头、「Accept-Language」、「zh-CN,zh;q=0.9,en;q=0.8」。
    做 HTTP头部_添加 用 req 的 请求头、「Connection」、「keep-alive」。
    若 资源类型 等于 资源类型_主文档：
        做 HTTP头部_添加 用 req 的 请求头、「Accept」、「text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8」。
    否则若 资源类型 等于 资源类型_样式表：
        做 HTTP头部_添加 用 req 的 请求头、「Accept」、「text/css,*/*;q=0.1」。
    否则若 资源类型 等于 资源类型_脚本：
        做 HTTP头部_添加 用 req 的 请求头、「Accept」、「*/*」。
    完。
    返还 req。
完。

本章 收束。
