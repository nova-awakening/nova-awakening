《CSS颜色语义》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·样式滤镜》。

引用《.·样式子网格》。

引用《..·执行·控制台输出》。

引用《.·内核直连层》。

引用《.·AI样式推断》。

智能_CSSColorLevel4_AI样式意图 接受 属性ID、值：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    返还 0。
完。

智能_CSSColorLevel4_AI影响分析 接受 属性ID：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 2。
    完。
    返还 做 AI样式_分析影响 用 属性ID。
完。

CSS_跳过空白 接受 str、pos、len：
    可变 p 为 pos。
    只要 p 小于 len：
        ch 为 做 字节取 用 str、p。
        若 ch 不等于 0x20 并且 ch 不等于 0x09 并且 ch 不等于 0x0A：
            跳出。
        完。
        p 为 p 加 1。
    完。
    返还 p。
完。

CSS_解析有符号浮点_x10000 接受 str、pos：
    len 为 做 字节长度 用 str。
    可变 p 为 pos。
    p 为 做 CSS_跳过空白 用 str、p、len。
    可变 负 为 假。
    若 p 小于 len 并且 做 字节取 用 str、p 等于 0x2D：
        负 为 真。
        p 为 p 加 1。
    否则若 p 小于 len 并且 做 字节取 用 str、p 等于 0x2B：
        p 为 p 加 1。
    完。
    v 为 做 CSS_解析浮点_x10000 用 str、p。
    若 负：
        返还 0 减 v。
    完。
    返还 v。
完。

CSS_解析百分比_x10000 接受 str、pos：
    v 为 做 CSS_解析浮点_x10000 用 str、pos。
    返还 v 除以 100。
完。

CSS_子串_去百分比 接受 str、起始、终止、百分比_out：
    返还 str。
完。

CSS_子串_去空白 接受 str、起始、终止：
    若 str 等于 0：
        返还 「」。
    完。
    可变 s 为 起始。
    可变 e 为 终止。
    只要 s 小于 e 并且 做 字节取 用 str、s 等于 0x20：
        s 为 s 加 1。
    完。
    只要 e 大于 s 并且 做 字节取 用 str、e 减 1 等于 0x20：
        e 为 e 减 1。
    完。
    len 为 e 减 s。
    若 len 小于或等于 0：
        返还 「」。
    完。
    buf 为 做 堆_分配 用 len 加 1。
    做 内存_复制 用 buf、str 加 s、len。
    做 字节写 用 buf、len、0。
    返还 buf。
完。

Lab_to_sRGB 接受 c：
    L 为 c 的 分量1。
    a 为 c 的 分量2。
    B 为 c 的 分量3。
    fy 为 (L 加 1600) 乘 10000 除以 11600。
    fx 为 fy 加 a 乘 10000 除以 5000。
    fz 为 fy 减 B 乘 10000 除以 2000。
    delta 为 689。
    x 为 若 fx 大于 delta 则 fx 乘 fx 乘 fx 除以 100000000 否则 (fx 乘 11600 减 160000) 除以 9033。
    y 为 若 fy 大于 delta 则 fy 乘 fy 乘 fy 除以 100000000 否则 (fy 乘 11600 减 160000) 除以 9033。
    z 为 若 fz 大于 delta 则 fz 乘 fz 乘 fz 除以 100000000 否则 (fz 乘 11600 减 160000) 除以 9033。
    x 为 x 乘 9642 除以 10000。
    z 为 z 乘 8249 除以 10000。
    r_lin 为 (32406 乘 x 减 15372 乘 y 减 4986 乘 z) 除以 10000。
    g_lin 为 (负 9689 乘 x 加 18758 乘 y 加 416 乘 z) 除以 10000。
    b_lin 为 (557 乘 x 减 2040 乘 y 加 10570 乘 z) 除以 10000。
    返还 新建 CSSColor：空间 为 CS_SRGB、分量1 为 做 sRGB_加gamma 用 r_lin、分量2 为 做 sRGB_加gamma 用 g_lin、分量3 为 做 sRGB_加gamma 用 b_lin、alpha 为 c 的 alpha。
完。

LCH_to_Lab 接受 c：
    a 为 c 的 分量2 乘 做 整数_cos 用 c 的 分量3 除以 10000。
    b 为 c 的 分量2 乘 做 整数_sin 用 c 的 分量3 除以 10000。
    返还 新建 CSSColor：空间 为 CS_LAB、分量1 为 c 的 分量1、分量2 为 a、分量3 为 b、alpha 为 c 的 alpha。
完。

sRGB_to_HSL 接受 c：
    r 为 c 的 分量1。
    g 为 c 的 分量2。
    b 为 c 的 分量3。
    可变 max_v 为 r。
    若 g 大于 max_v：
        max_v 为 g。
    完。
    若 b 大于 max_v：
        max_v 为 b。
    完。
    可变 min_v 为 r。
    若 g 小于 min_v：
        min_v 为 g。
    完。
    若 b 小于 min_v：
        min_v 为 b。
    完。
    l 为 (max_v 加 min_v) 除以 2。
    若 max_v 等于 min_v：
        返还 新建 CSSColor：空间 为 CS_HSL、分量1 为 0、分量2 为 0、分量3 为 l、alpha 为 c 的 alpha。
    完。
    d 为 max_v 减 min_v。
    s 为 若 l 大于 5000 则 d 乘 10000 除以 (20000 减 max_v 减 min_v) 否则 d 乘 10000 除以 (max_v 加 min_v)。
    可变 h 为 0。
    若 max_v 等于 r：
        h 为 (g 减 b) 乘 600000 除以 d。
        若 h 小于 0：
            h 为 h 加 3600000。
        完。
    否则若 max_v 等于 g：
        h 为 (b 减 r) 乘 600000 除以 d 加 1200000。
    否则：
        h 为 (r 减 g) 乘 600000 除以 d 加 2400000。
    完。
    返还 新建 CSSColor：空间 为 CS_HSL、分量1 为 h、分量2 为 s、分量3 为 l、alpha 为 c 的 alpha。
完。

平台_检测暗色模式 接受 零参：
    返还 假。
完。

本章 收束。
