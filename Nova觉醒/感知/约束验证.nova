《约束验证》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·推理·定点Q算术》。

引用《..·中枢·辅助存根》。

引用《.·文档节点》。

引用《..·中枢·数学工具》。

引用《..·执行·节点操作》。

FormControl_验证 接受 控件：
    若 控件 等于 0：
        返还 验证_有效。
    完。
    若 控件 的 是禁用：
        返还 验证_有效。
    完。
    值 为 控件 的 值。
    值长 为 若 值 不等于 0 则 做 字节长度 用 值 否则 0。
    若 控件 的 是必填：
        若 控件 的 类型 等于 输入类型_checkbox：
            若 非 控件 的 是选中：
                控件 的 验证状态 为 验证_缺失。
                返还 验证_缺失。
            完。
        否则：
            若 值长 等于 0：
                控件 的 验证状态 为 验证_缺失。
                返还 验证_缺失。
            完。
        完。
    完。
    若 值长 等于 0：
        控件 的 验证状态 为 验证_有效。
        返还 验证_有效。
    完。
    若 控件 的 类型 等于 输入类型_email：
        若 非 做 验证_email格式 用 值：
            控件 的 验证状态 为 验证_类型不匹配。
            返还 验证_类型不匹配。
        完。
    否则若 控件 的 类型 等于 输入类型_url：
        若 非 做 验证_url格式 用 值：
            控件 的 验证状态 为 验证_类型不匹配。
            返还 验证_类型不匹配。
        完。
    否则若 控件 的 类型 等于 输入类型_number 或者 控件 的 类型 等于 输入类型_range：
        数值 为 做 字符串转数字 用 值。
        若 控件 的 最小值 不等于 0：
            最小 为 做 字符串转数字 用 控件 的 最小值。
            若 数值 小于 最小：
                控件 的 验证状态 为 验证_过小。
                返还 验证_过小。
            完。
        完。
        若 控件 的 最大值 不等于 0：
            最大 为 做 字符串转数字 用 控件 的 最大值。
            若 数值 大于 最大：
                控件 的 验证状态 为 验证_过大。
                返还 验证_过大。
            完。
        完。
    完。
    若 控件 的 最小长度 大于或等于 0 并且 值长 小于 控件 的 最小长度：
        控件 的 验证状态 为 验证_过短。
        返还 验证_过短。
    完。
    若 控件 的 最大长度 大于或等于 0 并且 值长 大于 控件 的 最大长度：
        控件 的 验证状态 为 验证_过长。
        返还 验证_过长。
    完。
    若 控件 的 模式 不等于 0：
        若 非 做 正则_匹配 用 控件 的 模式、值：
            控件 的 验证状态 为 验证_模式不匹配。
            返还 验证_模式不匹配。
        完。
    完。
    控件 的 验证状态 为 验证_有效。
    返还 验证_有效。
完。

验证_email格式 接受 值：
    长度 为 做 字节长度 用 值。
    可变 有at 为 假。
    可变 有点 为 假。
    可变 at位置 为 负 1。
    可变 i 为 0。
    只要 i 小于 长度：
        若 做 字节取 用 值、i 等于 64：
            有at 为 真。
            at位置 为 i。
        完。
        若 做 字节取 用 值、i 等于 46 并且 at位置 大于或等于 0：
            有点 为 真。
        完。
        i 为 i 加 1。
    完。
    返还 有at 并且 有点 并且 at位置 大于 0 并且 at位置 小于 长度 减 1。
完。

验证_url格式 接受 值：
    返还 做 字符串_前缀匹配 用 值、「http://」 或者 做 字符串_前缀匹配 用 值、「https://」。
完。

HTMLForm_验证全部 接受 form：
    若 form 等于 0 或者 form 的 novalidate：
        返还 真。
    完。
    可变 全部有效 为 真。
    可变 i 为 0。
    只要 i 小于 form 的 控件数：
        控件 为 form 的 控件 的 第 i 项。
        若 做 FormControl_验证 用 控件 不等于 验证_有效：
            全部有效 为 假。
        完。
        i 为 i 加 1。
    完。
    返还 全部有效。
完。

NFCV_版本 为 「3.0.0-NFCV」。

NFCV_AI阈值 为 0.72。

NFCV_最大错误消息长度 为 256。

可变 NFCV_验证次数 为 0。

可变 NFCV_有效次数 为 0。

可变 NFCV_无效次数 为 0。

可变 NFCV_报告次数 为 0。

可变 NFCV_AI推理次数 为 0。

可变 NFCV_异步验证次数 为 0。

可变 NFCV_跨字段验证次数 为 0。

NFCV_结果 是 一种 事物，含 状态、错误码 为 0、消息 为 0、置信度 为 100。

NFCV_规则 是 一种 事物，含 名称 为 0、参数1 为 0、参数2 为 0、启用 为 真。

NFCV_上下文 是 一种 事物，含 控件 为 0、表单 为 0、当前时间戳 为 0、来源 为 0。

NFCV_创建结果 接受 状态、错误码、消息：
    r 为 新建 NFCV_结果。
    r 的 状态 为 状态。
    r 的 错误码 为 错误码。
    r 的 消息 为 消息。
    r 的 置信度 为 100。
    返还 r。
完。

NFCV_状态转文本 接受 状态：
    若 状态 等于 验证_有效：
        返还 「valid」。
    完。
    若 状态 等于 验证_缺失：
        返还 「valueMissing」。
    完。
    若 状态 等于 验证_类型不匹配：
        返还 「typeMismatch」。
    完。
    若 状态 等于 验证_模式不匹配：
        返还 「patternMismatch」。
    完。
    若 状态 等于 验证_过小：
        返还 「rangeUnderflow」。
    完。
    若 状态 等于 验证_过大：
        返还 「rangeOverflow」。
    完。
    若 状态 等于 验证_过短：
        返还 「tooShort」。
    完。
    若 状态 等于 验证_过长：
        返还 「tooLong」。
    完。
    返还 「customError」。
完。

NFCV_默认错误消息 接受 控件、状态：
    若 状态 等于 验证_有效：
        返还 「」。
    完。
    若 状态 等于 验证_缺失：
        返还 「请填写此字段」。
    完。
    若 状态 等于 验证_类型不匹配：
        若 控件 的 类型 等于 输入类型_email：
            返还 「请输入有效的邮箱地址」。
        完。
        若 控件 的 类型 等于 输入类型_url：
            返还 「请输入有效的URL地址」。
        完。
        返还 「输入格式不正确」。
    完。
    若 状态 等于 验证_模式不匹配：
        返还 「输入不符合要求格式」。
    完。
    若 状态 等于 验证_过小：
        返还 「输入值小于允许最小值」。
    完。
    若 状态 等于 验证_过大：
        返还 「输入值超过允许最大值」。
    完。
    若 状态 等于 验证_过短：
        返还 「输入字符过短」。
    完。
    若 状态 等于 验证_过长：
        返还 「输入字符过长」。
    完。
    返还 「输入无效」。
完。

NFCV_裁剪错误消息 接受 消息：
    若 消息 等于 0：
        返还 「」。
    完。
    长度 为 做 字节长度 用 消息。
    若 长度 小于或等于 NFCV_最大错误消息长度：
        返还 消息。
    完。
    返还 做 字符串_子串 用 消息、0、NFCV_最大错误消息长度。
完。

NFCV_校验必填 接受 控件：
    若 非 控件 的 是必填：
        返还 验证_有效。
    完。
    若 控件 的 类型 等于 输入类型_checkbox 或者 控件 的 类型 等于 输入类型_radio：
        若 非 控件 的 是选中：
            返还 验证_缺失。
        完。
        返还 验证_有效。
    完。
    值 为 控件 的 值。
    若 值 等于 0 或者 做 字节长度 用 值 等于 0：
        返还 验证_缺失。
    完。
    返还 验证_有效。
完。

NFCV_校验长度 接受 控件：
    值 为 控件 的 值。
    长度 为 若 值 不等于 0 则 做 字节长度 用 值 否则 0。
    若 控件 的 最小长度 大于或等于 0 并且 长度 小于 控件 的 最小长度：
        返还 验证_过短。
    完。
    若 控件 的 最大长度 大于或等于 0 并且 长度 大于 控件 的 最大长度：
        返还 验证_过长。
    完。
    返还 验证_有效。
完。

NFCV_校验数值范围 接受 控件：
    值 为 控件 的 值。
    若 值 等于 0 或者 做 字节长度 用 值 等于 0：
        返还 验证_有效。
    完。
    数值 为 做 字符串转数字 用 值。
    若 控件 的 最小值 不等于 0：
        最小 为 做 字符串转数字 用 控件 的 最小值。
        若 数值 小于 最小：
            返还 验证_过小。
        完。
    完。
    若 控件 的 最大值 不等于 0：
        最大 为 做 字符串转数字 用 控件 的 最大值。
        若 数值 大于 最大：
            返还 验证_过大。
        完。
    完。
    返还 验证_有效。
完。

NFCV_校验类型 接受 控件：
    值 为 控件 的 值。
    若 值 等于 0 或者 做 字节长度 用 值 等于 0：
        返还 验证_有效。
    完。
    若 控件 的 类型 等于 输入类型_email：
        若 非 做 NFCV_验证邮箱 用 值：
            返还 验证_类型不匹配。
        完。
        返还 验证_有效。
    完。
    若 控件 的 类型 等于 输入类型_url：
        若 非 做 NFCV_验证URL 用 值：
            返还 验证_类型不匹配。
        完。
        返还 验证_有效。
    完。
    若 控件 的 类型 等于 输入类型_tel：
        若 非 做 NFCV_验证电话 用 值：
            返还 验证_类型不匹配。
        完。
        返还 验证_有效。
    完。
    若 控件 的 类型 等于 输入类型_number 或者 控件 的 类型 等于 输入类型_range：
        若 非 做 NFCV_验证数字字符串 用 值：
            返还 验证_类型不匹配。
        完。
        返还 验证_有效。
    完。
    返还 验证_有效。
完。

NFCV_校验模式 接受 控件：
    若 控件 的 模式 等于 0：
        返还 验证_有效。
    完。
    值 为 控件 的 值。
    若 值 等于 0 或者 做 字节长度 用 值 等于 0：
        返还 验证_有效。
    完。
    若 非 做 正则_匹配 用 控件 的 模式、值：
        返还 验证_模式不匹配。
    完。
    返还 验证_有效。
完。

NFCV_校验步长 接受 控件：
    若 控件 的 步长 等于 0：
        返还 验证_有效。
    完。
    值 为 控件 的 值。
    若 值 等于 0 或者 做 字节长度 用 值 等于 0：
        返还 验证_有效。
    完。
    数值 为 做 字符串转数字 用 值。
    基准 为 0。
    若 控件 的 最小值 不等于 0：
        基准 为 做 字符串转数字 用 控件 的 最小值。
    完。
    步长值 为 做 字符串转数字 用 控件 的 步长。
    若 步长值 小于或等于 0：
        返还 验证_有效。
    完。
    差 为 数值 减 基准。
    商 为 差 除以 步长值。
    近整 为 做 数学_四舍五入 用 商。
    偏差 为 做 数学_绝对值 用 商 减 近整。
    若 偏差 大于 0.000001：
        返还 验证_模式不匹配。
    完。
    返还 验证_有效。
完。

NFCV_验证邮箱 接受 值：
    长度 为 做 字节长度 用 值。
    若 长度 小于 3：
        返还 假。
    完。
    可变 at位置 为 负 1。
    可变 点位置 为 负 1。
    可变 i 为 0。
    只要 i 小于 长度：
        b 为 做 字节取 用 值、i。
        若 b 等于 64：
            at位置 为 i。
        完。
        若 b 等于 46：
            点位置 为 i。
        完。
        i 为 i 加 1。
    完。
    若 at位置 小于或等于 0：
        返还 假。
    完。
    若 点位置 小于或等于 at位置 加 1：
        返还 假。
    完。
    若 点位置 大于或等于 长度 减 1：
        返还 假。
    完。
    返还 真。
完。

NFCV_验证URL 接受 值：
    若 做 字符串_前缀匹配 用 值、「http://」：
        返还 真。
    完。
    若 做 字符串_前缀匹配 用 值、「https://」：
        返还 真。
    完。
    若 做 字符串_前缀匹配 用 值、「ftp://」：
        返还 真。
    完。
    返还 假。
完。

NFCV_验证电话 接受 值：
    长度 为 做 字节长度 用 值。
    若 长度 小于 5：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 长度：
        b 为 做 字节取 用 值、i。
        若 b 大于或等于 48 并且 b 小于或等于 57 或者 b 等于 43 或者 b 等于 45 或者 b 等于 32 或者 b 等于 40 或者 b 等于 41：
            i 为 i 加 1。
            继续。
        完。
        返还 假。
    完。
    返还 真。
完。

NFCV_验证数字字符串 接受 值：
    长度 为 做 字节长度 用 值。
    若 长度 等于 0：
        返还 假。
    完。
    可变 有数字 为 假。
    可变 i 为 0。
    只要 i 小于 长度：
        b 为 做 字节取 用 值、i。
        若 b 大于或等于 48 并且 b 小于或等于 57：
            有数字 为 真。
            i 为 i 加 1。
            继续。
        完。
        若 b 等于 45 或者 b 等于 43 或者 b 等于 46 或者 b 等于 101 或者 b 等于 69：
            i 为 i 加 1。
            继续。
        完。
        返还 假。
    完。
    返还 有数字。
完。

NFCV_执行规则管线 接受 控件：
    NFCV_验证次数 为 NFCV_验证次数 加 1。
    若 控件 等于 0：
        返还 验证_有效。
    完。
    若 控件 的 是禁用：
        返还 验证_有效。
    完。
    状态 为 做 NFCV_校验必填 用 控件。
    若 状态 不等于 验证_有效：
        返还 状态。
    完。
    值 为 控件 的 值。
    若 值 等于 0 或者 做 字节长度 用 值 等于 0：
        返还 验证_有效。
    完。
    状态 为 做 NFCV_校验类型 用 控件。
    若 状态 不等于 验证_有效：
        返还 状态。
    完。
    状态 为 做 NFCV_校验数值范围 用 控件。
    若 状态 不等于 验证_有效：
        返还 状态。
    完。
    状态 为 做 NFCV_校验长度 用 控件。
    若 状态 不等于 验证_有效：
        返还 状态。
    完。
    状态 为 做 NFCV_校验模式 用 控件。
    若 状态 不等于 验证_有效：
        返还 状态。
    完。
    状态 为 做 NFCV_校验步长 用 控件。
    若 状态 不等于 验证_有效：
        返还 状态。
    完。
    返还 验证_有效。
完。

NFCV_验证控件 接受 控件：
    状态 为 做 NFCV_执行规则管线 用 控件。
    控件 的 验证状态 为 状态。
    若 状态 等于 验证_有效：
        NFCV_有效次数 为 NFCV_有效次数 加 1。
    否则：
        NFCV_无效次数 为 NFCV_无效次数 加 1。
    完。
    返还 状态。
完。

FormControl_验证增强 接受 控件：
    返还 做 NFCV_验证控件 用 控件。
完。

FormControl_设置自定义错误 接受 控件、消息：
    若 控件 等于 0：
        返还。
    完。
    控件 的 自定义错误 为 做 NFCV_裁剪错误消息 用 消息。
    若 控件 的 自定义错误 不等于 0 并且 做 字节长度 用 控件 的 自定义错误 大于 0：
        控件 的 验证状态 为 验证_模式不匹配。
    否则：
        控件 的 验证状态 为 验证_有效。
    完。
完。

FormControl_获取验证消息 接受 控件：
    若 控件 等于 0：
        返还 「」。
    完。
    若 控件 的 自定义错误 不等于 0 并且 做 字节长度 用 控件 的 自定义错误 大于 0：
        返还 控件 的 自定义错误。
    完。
    返还 做 NFCV_默认错误消息 用 控件、控件 的 验证状态。
完。

FormControl_检查有效性 接受 控件：
    状态 为 做 NFCV_验证控件 用 控件。
    若 状态 等于 验证_有效：
        返还 真。
    完。
    返还 假。
完。

FormControl_报告有效性 接受 控件：
    NFCV_报告次数 为 NFCV_报告次数 加 1。
    若 做 FormControl_检查有效性 用 控件：
        做 DOM_派发事件 用 控件、「valid」、假、假。
        返还 真。
    完。
    消息 为 做 FormControl_获取验证消息 用 控件。
    做 DOM_设置验证气泡 用 控件、消息。
    做 DOM_派发事件 用 控件、「invalid」、真、真。
    返还 假。
完。

HTMLForm_报告有效性 接受 form：
    若 form 等于 0：
        返还 真。
    完。
    可变 全部有效 为 真。
    可变 i 为 0。
    只要 i 小于 form 的 控件数：
        控件 为 form 的 控件 的 第 i 项。
        若 非 做 FormControl_报告有效性 用 控件：
            若 全部有效 等于 真：
                做 DOM_聚焦 用 控件。
            完。
            全部有效 为 假。
        完。
        i 为 i 加 1。
    完。
    返还 全部有效。
完。

NFCV_按名称查找控件 接受 form、名称：
    若 form 等于 0 或者 名称 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 form 的 控件数：
        c 为 form 的 控件 的 第 i 项。
        若 c 不等于 0 并且 c 的 名称 不等于 0 并且 做 字符串_等于 用 c 的 名称、名称：
            返还 c。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

NFCV_验证确认密码 接受 form、主字段名、确认字段名：
    NFCV_跨字段验证次数 为 NFCV_跨字段验证次数 加 1。
    主控件 为 做 NFCV_按名称查找控件 用 form、主字段名。
    确认控件 为 做 NFCV_按名称查找控件 用 form、确认字段名。
    若 主控件 等于 0 或者 确认控件 等于 0：
        返还 真。
    完。
    若 非 做 字符串_等于 用 主控件 的 值、确认控件 的 值：
        做 FormControl_设置自定义错误 用 确认控件、「两次输入密码不一致」。
        返还 假。
    完。
    做 FormControl_设置自定义错误 用 确认控件、「」。
    返还 真。
完。

NFCV_验证数值区间 接受 form、最小字段名、最大字段名：
    NFCV_跨字段验证次数 为 NFCV_跨字段验证次数 加 1。
    最小控件 为 做 NFCV_按名称查找控件 用 form、最小字段名。
    最大控件 为 做 NFCV_按名称查找控件 用 form、最大字段名。
    若 最小控件 等于 0 或者 最大控件 等于 0：
        返还 真。
    完。
    若 做 字节长度 用 最小控件 的 值 等于 0 或者 做 字节长度 用 最大控件 的 值 等于 0：
        返还 真。
    完。
    minV 为 做 字符串转数字 用 最小控件 的 值。
    maxV 为 做 字符串转数字 用 最大控件 的 值。
    若 minV 大于 maxV：
        做 FormControl_设置自定义错误 用 最大控件、「最大值必须大于或等于最小值」。
        返还 假。
    完。
    做 FormControl_设置自定义错误 用 最大控件、「」。
    返还 真。
完。

NFCV_执行跨字段规则 接受 form：
    若 form 等于 0：
        返还 真。
    完。
    可变 全部有效 为 真。
    若 非 做 NFCV_验证确认密码 用 form、「password」、「confirm_password」：
        全部有效 为 假。
    完。
    若 非 做 NFCV_验证数值区间 用 form、「min」、「max」：
        全部有效 为 假。
    完。
    返还 全部有效。
完。

NFCV_异步验证开始 接受 控件、规则名：
    NFCV_异步验证次数 为 NFCV_异步验证次数 加 1。
    做 DOM_设置属性 用 控件、「data-validating」、「1」。
    做 DOM_设置属性 用 控件、「data-rule」、规则名。
完。

NFCV_异步验证结束 接受 控件：
    做 DOM_设置属性 用 控件、「data-validating」、「0」。
完。

NFCV_验证用户名唯一 接受 控件：
    做 NFCV_异步验证开始 用 控件、「uniqueUsername」。
    值 为 控件 的 值。
    若 做 字符串_等于 用 值、「admin」 或者 做 字符串_等于 用 值、「root」 或者 做 字符串_等于 用 值、「system」：
        做 NFCV_异步验证结束 用 控件。
        做 FormControl_设置自定义错误 用 控件、「该用户名已被占用」。
        返还 假。
    完。
    做 NFCV_异步验证结束 用 控件。
    做 FormControl_设置自定义错误 用 控件、「」。
    返还 真。
完。

NFCV_AI预测风险 接受 控件：
    NFCV_AI推理次数 为 NFCV_AI推理次数 加 1。
    长度 为 若 控件 的 值 不等于 0 则 做 字节长度 用 控件 的 值 否则 0。
    tensor 为 做 Tensor_创建 用 6。
    做 Tensor_写 用 tensor、0、控件 的 类型。
    做 Tensor_写 用 tensor、1、控件 的 是必填。
    做 Tensor_写 用 tensor、2、长度。
    做 Tensor_写 用 tensor、3、控件 的 最小长度。
    做 Tensor_写 用 tensor、4、控件 的 最大长度。
    做 Tensor_写 用 tensor、5、0.0。
    推理 为 做 AI总线_推理 用 全局AI总线、「nfcv_risk」、tensor。
    风险分 为 做 Tensor_读 用 推理、0。
    做 Tensor_释放 用 tensor。
    做 Tensor_释放 用 推理。
    返还 风险分。
完。

NFCV_AI辅助验证 接受 控件：
    风险分 为 做 NFCV_AI预测风险 用 控件。
    若 风险分 小于 NFCV_AI阈值：
        返还 验证_有效。
    完。
    s1 为 做 NFCV_校验长度 用 控件。
    若 s1 不等于 验证_有效：
        返还 s1。
    完。
    s2 为 做 NFCV_校验模式 用 控件。
    若 s2 不等于 验证_有效：
        返还 s2。
    完。
    返还 验证_有效。
完。

NFCV_验证控件_带AI 接受 控件：
    基础状态 为 做 NFCV_验证控件 用 控件。
    若 基础状态 不等于 验证_有效：
        返还 基础状态。
    完。
    AI状态 为 做 NFCV_AI辅助验证 用 控件。
    若 AI状态 不等于 验证_有效：
        控件 的 验证状态 为 AI状态。
        NFCV_无效次数 为 NFCV_无效次数 加 1。
        返还 AI状态。
    完。
    返还 验证_有效。
完。

NFCV_初始化 接受 零参：
    NFCV_验证次数 为 0。
    NFCV_有效次数 为 0。
    NFCV_无效次数 为 0。
    NFCV_报告次数 为 0。
    NFCV_AI推理次数 为 0。
    NFCV_异步验证次数 为 0。
    NFCV_跨字段验证次数 为 0。
    做 打印 用 「[NFCV] 约束验证引擎 v」 加 NFCV_版本 加 「 已初始化」。
完。

NFCV_诊断报告 接受 零参：
    做 打印 用 「[NFCV] === 约束验证诊断 ===」。
    做 打印 用 「  验证总数=」 加 做 整数转字符串 用 NFCV_验证次数 加 「 有效=」 加 做 整数转字符串 用 NFCV_有效次数 加 「 无效=」 加 做 整数转字符串 用 NFCV_无效次数。
    做 打印 用 「  报告次数=」 加 做 整数转字符串 用 NFCV_报告次数 加 「 跨字段=」 加 做 整数转字符串 用 NFCV_跨字段验证次数。
    做 打印 用 「  AI推理=」 加 做 整数转字符串 用 NFCV_AI推理次数 加 「 异步验证=」 加 做 整数转字符串 用 NFCV_异步验证次数。
完。

NFCV_重置控件状态 接受 控件：
    若 控件 等于 0：
        返还。
    完。
    控件 的 验证状态 为 验证_有效。
    控件 的 自定义错误 为 「」。
    做 DOM_清除验证气泡 用 控件。
完。

NFCV_重置表单状态 接受 form：
    若 form 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 form 的 控件数：
        做 NFCV_重置控件状态 用 form 的 控件 的 第 i 项。
        i 为 i 加 1。
    完。
完。

本章 收束。
