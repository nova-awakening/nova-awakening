《属性值中的实》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·推理·AI鼠标事件处理》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·推理·AI总线资源调度》。

实体表容量 为 256。

最大实体名长度 为 32。

最大解码输出倍数 为 4。

UNICODE_最大码点 为 0x10FFFF。

UNICODE_替换字符 为 0xFFFD。

实体_命名 为 0。

实体_十进制数字 为 1。

实体_十六进制数字 为 2。

上下文_文本 为 0。

上下文_属性值 为 1。

替换表容量 为 32。

实体解码结果 是 一种 事物，含 成功、码点1、码点2、消耗长度、类型。

实体表项 是 一种 事物，含 名称哈希、名称指针、名称长度、码点1、码点2、有效。

数字替换项 是 一种 事物，含 原始码点、替换码点。

实体解码统计 是 一种 事物，含 解码总次数、命名实体次数、数字实体次数、属性值解码次数、属性值跳过次数、失败次数、替换字符次数、哈希冲突次数、AI分析触发数。

可变 实体表 为 0。

可变 数字替换表 为 0。

可变 全局实体统计 为 新建 实体解码统计。

实体_FNV哈希 接受 名、长度：
    可变 h 为 2166136261。
    可变 i 为 0。
    只要 i 小于 长度：
        h 为 h 位异或 做 内存_读8 用 名 加 i。
        h 为 h 乘 16777619。
        i 为 i 加 1。
    完。
    返还 h。
完。

实体_是字母 接受 c：
    返还 c 大于或等于 0x41 并且 c 小于或等于 0x5A 或者 c 大于或等于 0x61 并且 c 小于或等于 0x7A。
完。

实体_是数字 接受 c：
    返还 c 大于或等于 0x30 并且 c 小于或等于 0x39。
完。

实体_是十六进制 接受 c：
    返还 做 实体_是数字 用 c 或者 c 大于或等于 0x41 并且 c 小于或等于 0x46 或者 c 大于或等于 0x61 并且 c 小于或等于 0x66。
完。

实体_十六进制值 接受 c：
    若 c 大于或等于 0x30 并且 c 小于或等于 0x39：
        返还 c 减 0x30。
    完。
    若 c 大于或等于 0x41 并且 c 小于或等于 0x46：
        返还 c 减 0x41 加 10。
    完。
    若 c 大于或等于 0x61 并且 c 小于或等于 0x66：
        返还 c 减 0x61 加 10。
    完。
    返还 0。
完。

实体_是字母数字或等号 接受 c：
    返还 c 等于 0x3D 或者 做 实体_是字母 用 c 或者 做 实体_是数字 用 c。
完。

实体表_插入 接受 名、长度、cp1、cp2：
    若 实体表 等于 0：
        返还。
    完。
    h 为 做 实体_FNV哈希 用 名、长度。
    可变 idx 为 h 取余于 实体表容量。
    可变 探测 为 0。
    只要 探测 小于 实体表容量：
        若 非 实体表 的 第 idx 项 的 有效：
            实体表 的 第 idx 项 的 名称哈希 为 h。
            实体表 的 第 idx 项 的 名称指针 为 名。
            实体表 的 第 idx 项 的 名称长度 为 长度。
            实体表 的 第 idx 项 的 码点1 为 cp1。
            实体表 的 第 idx 项 的 码点2 为 cp2。
            实体表 的 第 idx 项 的 有效 为 真。
            返还。
        完。
        全局实体统计 的 哈希冲突次数 为 全局实体统计 的 哈希冲突次数 加 1。
        idx 为 (idx 加 1) 取余于 实体表容量。
        探测 为 探测 加 1。
    完。
完。

HTMLEntity_初始化表 接受 零参：
    实体表 为 做 创建数组 用 实体表容量、新建 实体表项。
    做 实体表_插入 用 「amp」、3、0x26、0。
    做 实体表_插入 用 「lt」、2、0x3C、0。
    做 实体表_插入 用 「gt」、2、0x3E、0。
    做 实体表_插入 用 「quot」、4、0x22、0。
    做 实体表_插入 用 「apos」、4、0x27、0。
    做 实体表_插入 用 「nbsp」、4、0xA0、0。
    做 实体表_插入 用 「copy」、4、0xA9、0。
    做 实体表_插入 用 「reg」、3、0xAE、0。
    做 实体表_插入 用 「trade」、5、0x2122、0。
    做 实体表_插入 用 「mdash」、5、0x2014、0。
    做 实体表_插入 用 「ndash」、5、0x2013、0。
    做 实体表_插入 用 「lsquo」、5、0x2018、0。
    做 实体表_插入 用 「rsquo」、5、0x2019、0。
    做 实体表_插入 用 「ldquo」、5、0x201C、0。
    做 实体表_插入 用 「rdquo」、5、0x201D、0。
    做 实体表_插入 用 「hellip」、6、0x2026、0。
    做 实体表_插入 用 「bull」、4、0x2022、0。
    做 实体表_插入 用 「euro」、4、0x20AC、0。
    做 实体表_插入 用 「pound」、5、0xA3、0。
    做 实体表_插入 用 「yen」、3、0xA5、0。
    做 实体表_插入 用 「cent」、4、0xA2、0。
    做 实体表_插入 用 「iexcl」、5、0xA1、0。
    做 实体表_插入 用 「iquest」、6、0xBF、0。
    做 实体表_插入 用 「laquo」、5、0xAB、0。
    做 实体表_插入 用 「raquo」、5、0xBB、0。
    做 实体表_插入 用 「para」、4、0xB6、0。
    做 实体表_插入 用 「sect」、4、0xA7、0。
    做 实体表_插入 用 「deg」、3、0xB0、0。
    做 实体表_插入 用 「times」、5、0xD7、0。
    做 实体表_插入 用 「divide」、6、0xF7、0。
    做 实体表_插入 用 「infin」、5、0x221E、0。
    做 实体表_插入 用 「larr」、4、0x2190、0。
    做 实体表_插入 用 「rarr」、4、0x2192、0。
    做 实体表_插入 用 「uarr」、4、0x2191、0。
    做 实体表_插入 用 「darr」、4、0x2193、0。
    做 实体表_插入 用 「hearts」、6、0x2665、0。
    做 实体表_插入 用 「spades」、6、0x2660、0。
    做 实体表_插入 用 「clubs」、5、0x2663、0。
    做 实体表_插入 用 「diams」、5、0x2666、0。
    做 实体表_插入 用 「alpha」、5、0x03B1、0。
    做 实体表_插入 用 「beta」、4、0x03B2、0。
    做 实体表_插入 用 「gamma」、5、0x03B3、0。
    做 实体表_插入 用 「delta」、5、0x03B4、0。
    做 实体表_插入 用 「pi」、2、0x03C0、0。
    做 实体表_插入 用 「sigma」、5、0x03C3、0。
    做 实体表_插入 用 「omega」、5、0x03C9、0。
    做 实体表_插入 用 「Lambda」、6、0x039B、0。
    做 实体表_插入 用 「Sigma」、5、0x03A3、0。
    做 实体表_插入 用 「Omega」、5、0x03A9、0。
    数字替换表 为 做 创建数组 用 替换表容量、新建 数字替换项。
    做 数字替换表_插入 用 0x00、UNICODE_替换字符。
    做 数字替换表_插入 用 0x0D、0x0D。
    做 数字替换表_插入 用 0x80、0x20AC。
    做 数字替换表_插入 用 0x81、0x81。
    做 数字替换表_插入 用 0x82、0x201A。
    做 数字替换表_插入 用 0x83、0x0192。
    做 数字替换表_插入 用 0x84、0x201E。
    做 数字替换表_插入 用 0x85、0x2026。
    做 数字替换表_插入 用 0x86、0x2020。
    做 数字替换表_插入 用 0x87、0x2021。
    做 数字替换表_插入 用 0x88、0x02C6。
    做 数字替换表_插入 用 0x89、0x2030。
    做 数字替换表_插入 用 0x8A、0x0160。
    做 数字替换表_插入 用 0x8B、0x2039。
    做 数字替换表_插入 用 0x8C、0x0152。
    做 数字替换表_插入 用 0x8D、0x8D。
    做 数字替换表_插入 用 0x8E、0x017D。
    做 数字替换表_插入 用 0x91、0x2018。
    做 数字替换表_插入 用 0x92、0x2019。
    做 数字替换表_插入 用 0x93、0x201C。
    做 数字替换表_插入 用 0x94、0x201D。
    做 数字替换表_插入 用 0x95、0x2022。
    做 数字替换表_插入 用 0x96、0x2013。
    做 数字替换表_插入 用 0x97、0x2014。
    做 数字替换表_插入 用 0x98、0x02DC。
    做 数字替换表_插入 用 0x99、0x2122。
    做 数字替换表_插入 用 0x9A、0x0161。
    做 数字替换表_插入 用 0x9B、0x203A。
    做 数字替换表_插入 用 0x9C、0x0153。
    做 数字替换表_插入 用 0x9F、0x0178。
完。

数字替换表_插入 接受 原始、替换：
    若 数字替换表 等于 0：
        返还。
    完。
    idx 为 原始 取余于 替换表容量。
    数字替换表 的 第 idx 项 的 原始码点 为 原始。
    数字替换表 的 第 idx 项 的 替换码点 为 替换。
完。

实体表_查找 接受 名、长度：
    若 实体表 等于 0 或者 名 等于 0 或者 长度 等于 0：
        返还 新建 实体解码结果：成功 为 假。
    完。
    h 为 做 实体_FNV哈希 用 名、长度。
    可变 idx 为 h 取余于 实体表容量。
    可变 探测 为 0。
    只要 探测 小于 实体表容量：
        若 非 实体表 的 第 idx 项 的 有效：
            返还 新建 实体解码结果：成功 为 假。
        完。
        若 实体表 的 第 idx 项 的 名称哈希 等于 h 并且 实体表 的 第 idx 项 的 名称长度 等于 长度：
            可变 match 为 真。
            可变 j 为 0。
            只要 j 小于 长度：
                若 做 内存_读8 用 名 加 j 不等于 做 内存_读8 用 实体表 的 第 idx 项 的 名称指针 加 j：
                    match 为 假。
                    跳出。
                完。
                j 为 j 加 1。
            完。
            若 match：
                全局实体统计 的 命名实体次数 为 全局实体统计 的 命名实体次数 加 1。
                返还 新建 实体解码结果：成功 为 真、码点1 为 实体表 的 第 idx 项 的 码点1、码点2 为 实体表 的 第 idx 项 的 码点2、消耗长度 为 长度、类型 为 实体_命名。
            完。
        完。
        idx 为 (idx 加 1) 取余于 实体表容量。
        探测 为 探测 加 1。
    完。
    返还 新建 实体解码结果：成功 为 假。
完。

实体_码点安全检查 接受 cp：
    若 cp 大于 UNICODE_最大码点：
        返还 UNICODE_替换字符。
    完。
    若 cp 大于或等于 0xD800 并且 cp 小于或等于 0xDFFF：
        返还 UNICODE_替换字符。
    完。
    若 cp 大于或等于 0xFDD0 并且 cp 小于或等于 0xFDEF：
        返还 UNICODE_替换字符。
    完。
    若 cp 位与 0xFFFE 等于 0xFFFE：
        返还 UNICODE_替换字符。
    完。
    若 cp 不等于 0x09 并且 cp 不等于 0x0A 并且 cp 不等于 0x0C 并且 cp 不等于 0x0D 并且 cp 不等于 0x20：
        若 cp 大于或等于 0x01 并且 cp 小于或等于 0x08：
            返还 UNICODE_替换字符。
        完。
        若 cp 等于 0x0B：
            返还 UNICODE_替换字符。
        完。
        若 cp 大于或等于 0x0E 并且 cp 小于或等于 0x1F：
            返还 UNICODE_替换字符。
        完。
        若 cp 大于或等于 0x7F 并且 cp 小于或等于 0x9F：
            替换 为 做 数字替换表_查找 用 cp。
            若 替换 不等于 cp：
                全局实体统计 的 替换字符次数 为 全局实体统计 的 替换字符次数 加 1。
                返还 替换。
            完。
        完。
    完。
    返还 cp。
完。

数字替换表_查找 接受 cp：
    若 数字替换表 等于 0：
        返还 cp。
    完。
    idx 为 cp 取余于 替换表容量。
    若 数字替换表 的 第 idx 项 的 原始码点 等于 cp：
        返还 数字替换表 的 第 idx 项 的 替换码点。
    完。
    返还 cp。
完。

实体_解码数字 接受 输入、开始、输入长度：
    结果 为 新建 实体解码结果。
    结果 的 成功 为 假。
    可变 pos 为 开始 加 2。
    若 pos 大于或等于 输入长度：
        返还 结果。
    完。
    c 为 做 内存_读8 用 输入 加 pos。
    可变 是十六进制 为 假。
    若 c 等于 0x78 或者 c 等于 0x58：
        是十六进制 为 真。
        pos 为 pos 加 1。
    完。
    可变 码点 为 0。
    可变 有数字 为 假。
    只要 pos 小于 输入长度：
        ch 为 做 内存_读8 用 输入 加 pos。
        若 ch 等于 0x3B：
            pos 为 pos 加 1。
            跳出。
        完。
        若 是十六进制：
            若 非 做 实体_是十六进制 用 ch：
                跳出。
            完。
            码点 为 码点 乘 16 加 做 实体_十六进制值 用 ch。
        否则：
            若 非 做 实体_是数字 用 ch：
                跳出。
            完。
            码点 为 码点 乘 10 加 (ch 减 0x30)。
        完。
        若 码点 大于 0x10FFFF：
            码点 为 UNICODE_替换字符。
        完。
        有数字 为 真。
        pos 为 pos 加 1。
    完。
    若 非 有数字：
        返还 结果。
    完。
    码点 为 做 实体_码点安全检查 用 码点。
    结果 的 成功 为 真。
    结果 的 码点1 为 码点。
    结果 的 码点2 为 0。
    结果 的 消耗长度 为 pos 减 开始。
    结果 的 类型 为 若 是十六进制 则 实体_十六进制数字 否则 实体_十进制数字。
    全局实体统计 的 数字实体次数 为 全局实体统计 的 数字实体次数 加 1。
    返还 结果。
完。

实体_解码命名 接受 输入、开始、输入长度：
    结果 为 新建 实体解码结果。
    结果 的 成功 为 假。
    可变 pos 为 开始 加 1。
    名开始 为 pos。
    只要 pos 小于 输入长度 并且 pos 减 名开始 小于 最大实体名长度：
        ch 为 做 内存_读8 用 输入 加 pos。
        若 ch 等于 0x3B：
            pos 为 pos 加 1。
            跳出。
        完。
        若 非 做 实体_是字母 用 ch 并且 非 做 实体_是数字 用 ch：
            跳出。
        完。
        pos 为 pos 加 1。
    完。
    名长度 为 pos 减 名开始。
    若 pos 大于 名开始 并且 做 内存_读8 用 输入 加 pos 减 1 等于 0x3B：
        名长度 为 名长度 减 1。
    完。
    若 名长度 等于 0：
        返还 结果。
    完。
    查找结果 为 做 实体表_查找 用 输入 加 名开始、名长度。
    若 查找结果 的 成功：
        结果 的 成功 为 真。
        结果 的 码点1 为 查找结果 的 码点1。
        结果 的 码点2 为 查找结果 的 码点2。
        结果 的 消耗长度 为 pos 减 开始。
        结果 的 类型 为 实体_命名。
    完。
    返还 结果。
完。

HTMLEntity_解码 接受 输入、开始、输入长度：
    若 实体表 等于 0：
        做 HTMLEntity_初始化表。
    完。
    全局实体统计 的 解码总次数 为 全局实体统计 的 解码总次数 加 1。
    结果 为 新建 实体解码结果。
    结果 的 成功 为 假。
    若 开始 加 1 大于或等于 输入长度：
        返还 结果。
    完。
    下一个 为 做 内存_读8 用 输入 加 开始 加 1。
    若 下一个 等于 0x23：
        返还 做 实体_解码数字 用 输入、开始、输入长度。
    否则：
        若 做 实体_是字母 用 下一个：
            返还 做 实体_解码命名 用 输入、开始、输入长度。
        完。
    完。
    全局实体统计 的 失败次数 为 全局实体统计 的 失败次数 加 1。
    返还 结果。
完。

UTF8_编码码点 接受 码点、输出、pos：
    若 码点 小于 0x80：
        做 内存_则8 用 输出 加 pos、码点。
        返还 pos 加 1。
    完。
    若 码点 小于 0x800：
        做 内存_则8 用 输出 加 pos、0xC0 位或 码点 右移 6。
        做 内存_则8 用 输出 加 pos 加 1、0x80 位或 码点 位与 0x3F。
        返还 pos 加 2。
    完。
    若 码点 小于 0x10000：
        做 内存_则8 用 输出 加 pos、0xE0 位或 码点 右移 12。
        做 内存_则8 用 输出 加 pos 加 1、0x80 位或 码点 右移 6 位与 0x3F。
        做 内存_则8 用 输出 加 pos 加 2、0x80 位或 码点 位与 0x3F。
        返还 pos 加 3。
    完。
    做 内存_则8 用 输出 加 pos、0xF0 位或 码点 右移 18。
    做 内存_则8 用 输出 加 pos 加 1、0x80 位或 码点 右移 12 位与 0x3F。
    做 内存_则8 用 输出 加 pos 加 2、0x80 位或 码点 右移 6 位与 0x3F。
    做 内存_则8 用 输出 加 pos 加 3、0x80 位或 码点 位与 0x3F。
    返还 pos 加 4。
完。

HTMLEntity_解码属性值 接受 输入、输入长度：
    若 实体表 等于 0：
        做 HTMLEntity_初始化表。
    完。
    若 输入 等于 0 或者 输入长度 等于 0：
        返还 0。
    完。
    输出 为 做 堆_分配 用 输入长度 乘 最大解码输出倍数 加 1。
    若 输出 等于 0：
        返还 0。
    完。
    可变 输入pos 为 0。
    可变 输出pos 为 0。
    全局实体统计 的 属性值解码次数 为 全局实体统计 的 属性值解码次数 加 1。
    只要 输入pos 小于 输入长度：
        c 为 做 内存_读8 用 输入 加 输入pos。
        若 c 等于 0x26：
            结果 为 做 HTMLEntity_解码 用 输入、输入pos、输入长度。
            若 结果 的 成功：
                消耗后pos 为 输入pos 加 结果 的 消耗长度。
                最后消耗字符 为 做 内存_读8 用 输入 加 消耗后pos 减 1。
                若 最后消耗字符 不等于 0x3B：
                    下一个 为 0。
                    若 消耗后pos 小于 输入长度：
                        下一个 为 做 内存_读8 用 输入 加 消耗后pos。
                    完。
                    若 做 实体_是字母数字或等号 用 下一个：
                        做 内存_则8 用 输出 加 输出pos、0x26。
                        输出pos 为 输出pos 加 1。
                        输入pos 为 输入pos 加 1。
                        全局实体统计 的 属性值跳过次数 为 全局实体统计 的 属性值跳过次数 加 1。
                        继续。
                    完。
                完。
                输出pos 为 做 UTF8_编码码点 用 结果 的 码点1、输出、输出pos。
                若 结果 的 码点2 不等于 0：
                    输出pos 为 做 UTF8_编码码点 用 结果 的 码点2、输出、输出pos。
                完。
                输入pos 为 输入pos 加 结果 的 消耗长度。
            否则：
                做 内存_则8 用 输出 加 输出pos、0x26。
                输出pos 为 输出pos 加 1。
                输入pos 为 输入pos 加 1。
            完。
        否则：
            做 内存_则8 用 输出 加 输出pos、c。
            输出pos 为 输出pos 加 1。
            输入pos 为 输入pos 加 1。
        完。
    完。
    做 内存_则8 用 输出 加 输出pos、0。
    返还 输出。
完。

HTMLEntity_解码文本 接受 输入、输入长度：
    若 实体表 等于 0：
        做 HTMLEntity_初始化表。
    完。
    若 输入 等于 0 或者 输入长度 等于 0：
        返还 0。
    完。
    输出 为 做 堆_分配 用 输入长度 乘 最大解码输出倍数 加 1。
    若 输出 等于 0：
        返还 0。
    完。
    可变 输入pos 为 0。
    可变 输出pos 为 0。
    只要 输入pos 小于 输入长度：
        c 为 做 内存_读8 用 输入 加 输入pos。
        若 c 等于 0x26：
            结果 为 做 HTMLEntity_解码 用 输入、输入pos、输入长度。
            若 结果 的 成功：
                输出pos 为 做 UTF8_编码码点 用 结果 的 码点1、输出、输出pos。
                若 结果 的 码点2 不等于 0：
                    输出pos 为 做 UTF8_编码码点 用 结果 的 码点2、输出、输出pos。
                完。
                输入pos 为 输入pos 加 结果 的 消耗长度。
            否则：
                做 内存_则8 用 输出 加 输出pos、0x26。
                输出pos 为 输出pos 加 1。
                输入pos 为 输入pos 加 1。
            完。
        否则：
            做 内存_则8 用 输出 加 输出pos、c。
            输出pos 为 输出pos 加 1。
            输入pos 为 输入pos 加 1。
        完。
    完。
    做 内存_则8 用 输出 加 输出pos、0。
    返还 输出。
完。

HTMLEntity_解码单个 接受 输入、输入长度、上下文：
    若 上下文 等于 上下文_属性值：
        返还 做 HTMLEntity_解码属性值 用 输入、输入长度。
    完。
    返还 做 HTMLEntity_解码文本 用 输入、输入长度。
完。

智能_HTMLEntityParser_AI节点语义 接受 节点：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 节点 等于 0：
        返还。
    完。
    feature 为 做 Tensor_创建 用 8。
    做 Tensor_写 用 feature、0、全局实体统计 的 解码总次数 除以 10000.0。
    做 Tensor_写 用 feature、1、全局实体统计 的 命名实体次数 除以 5000.0。
    做 Tensor_写 用 feature、2、全局实体统计 的 数字实体次数 除以 5000.0。
    做 Tensor_写 用 feature、3、全局实体统计 的 失败次数 除以 1000.0。
    做 Tensor_写 用 feature、4、全局实体统计 的 属性值跳过次数 除以 1000.0。
    做 Tensor_写 用 feature、5、全局实体统计 的 替换字符次数 除以 100.0。
    做 Tensor_写 用 feature、6、全局实体统计 的 哈希冲突次数 除以 100.0。
    做 Tensor_写 用 feature、7、0.0。
    角色 为 做 AI总线_语义分类 用 bus、feature。
    做 Tensor_释放 用 feature。
    若 节点 不等于 0：
        节点 的 语义角色 为 角色。
    完。
    全局实体统计 的 AI分析触发数 为 全局实体统计 的 AI分析触发数 加 1。
完。

智能_HTMLEntityParser_AI预加载提示 接受 url、资源类型：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 url 等于 0：
        返还。
    完。
    feature 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 feature、0、资源类型 除以 10.0。
    做 Tensor_写 用 feature、1、全局实体统计 的 解码总次数 除以 10000.0。
    做 Tensor_写 用 feature、2、0.5。
    做 Tensor_写 用 feature、3、0.0。
    优先级 为 做 AI总线_资源优先级 用 bus、feature。
    做 Tensor_释放 用 feature。
    返还 优先级。
完。

实体_获取统计 接受 零参：
    返还 一本字典：解码总次数 对应 全局实体统计 的 解码总次数、命名实体次数 对应 全局实体统计 的 命名实体次数、数字实体次数 对应 全局实体统计 的 数字实体次数、属性值解码次数 对应 全局实体统计 的 属性值解码次数、属性值跳过次数 对应 全局实体统计 的 属性值跳过次数、失败次数 对应 全局实体统计 的 失败次数、替换字符次数 对应 全局实体统计 的 替换字符次数、哈希冲突次数 对应 全局实体统计 的 哈希冲突次数、AI分析触发数 对应 全局实体统计 的 AI分析触发数。
完。

实体_打印统计 接受 零参：
    s 为 做 实体_获取统计。
    做 内核打印 用 「═══ AHED实体解码统计 ═══」。
    做 内核打印 用 「解码[总/命名/数字]={}/{}/{}」、s 的 解码总次数、s 的 命名实体次数、s 的 数字实体次数。
    做 内核打印 用 「属性值[解码/跳过]={}/{} 失败={}」、s 的 属性值解码次数、s 的 属性值跳过次数、s 的 失败次数。
    做 内核打印 用 「替换字符={} 哈希冲突={} AI触发={}」、s 的 替换字符次数、s 的 哈希冲突次数、s 的 AI分析触发数。
完。

实体_重置统计 接受 零参：
    全局实体统计 为 新建 实体解码统计。
完。

本章 收束。
