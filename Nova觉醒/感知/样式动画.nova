《样式动画》

引用《.·文档节点》。

引用《.·视图过渡》。

引用《.·Animation_Timeline》。

引用《.·元素》。

引用《.·CSS属性过渡》。

引用《.·关键帧插值》。

引用《.·变换矩阵应》。

引用《.·正弦创建》。

引用《.·CSS动画》。

引用《.·矩阵创建》。

引用《.·合成帧提交核》。

引用《.·函数创建》。

引用《.·关键帧》。

引用《..·中枢·骨骼动画》。

引用《.·动画时间线》。

动画档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、时间数、关键帧数、插值数、时间线数、变换数、预测数、成功数、失败数。

动画请求 是 一种 事物，含 动作、目标、参数、选项。

动画回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

动画摘要 是 一种 事物，含 名称、时间数、关键帧数、插值数、时间线数、变换数、预测数、成功数、失败数、健康。

可变 动画账本 为 一组：（空）。

可变 动画构建数 为 0。

可变 动画总动作 为 0。

可变 动画总成功 为 0。

可变 动画总失败 为 0。

样式动画_建档 接受 名称：
    档 为 新建 动画档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 时间数 为 0。
    档 的 关键帧数 为 0。
    档 的 插值数 为 0。
    档 的 时间线数 为 0。
    档 的 变换数 为 0。
    档 的 预测数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    返还 档。
完。

样式动画_登记 接受 档：
    做 追加 用 动画账本、档。
    动画构建数 为 动画构建数 加 1。
    返还 档。
完。

样式动画_完整统计 接受 零参：
    返还 一组：动画构建数、动画总动作、动画总成功、动画总失败。
完。

样式动画_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 动画回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

样式动画_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 关键帧数 大于 0 或者 档 的 时间线数 大于 0 或者 档 的 变换数 大于 0：
        档 的 最近健康 为 「运行」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「空载」。
    返还 档 的 最近健康。
完。

样式动画_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    档 的 最近错误 为 「」。
    档 的 最近统计 为 做 样式动画_完整统计。
完。

样式动画_成功 接受 档、来源、数据：
    动画总动作 为 动画总动作 加 1。
    动画总成功 为 动画总成功 加 1。
    若 档 等于 空：
        返还 做 样式动画_建回证 用 真、真、来源、「」、数据、做 样式动画_完整统计。
    完。
    档 的 最近来源 为 来源。
    档 的 最近结果 为 数据。
    档 的 最近错误 为 「」。
    档 的 成功数 为 档 的 成功数 加 1。
    档 的 最近统计 为 做 样式动画_完整统计。
    做 样式动画_刷新健康 用 档。
    返还 做 样式动画_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

样式动画_失败 接受 档、来源、错因：
    动画总动作 为 动画总动作 加 1。
    动画总失败 为 动画总失败 加 1。
    若 档 等于 空：
        返还 做 样式动画_建回证 用 真、假、来源、错因、0、做 样式动画_完整统计。
    完。
    档 的 最近来源 为 来源。
    档 的 最近错误 为 错因。
    档 的 失败数 为 档 的 失败数 加 1。
    档 的 最近统计 为 做 样式动画_完整统计。
    做 样式动画_刷新健康 用 档。
    返还 做 样式动画_建回证 用 真、假、来源、错因、0、档 的 最近统计。
完。

样式动画_初始化 接受 档：
    做 AnimationTimeline_初始化。
    档 的 是否初始化 为 真。
    档 的 最近动作 为 「初始化」。
    档 的 最近来源 为 「样式动画_初始化」。
    档 的 最近统计 为 做 样式动画_完整统计。
    做 样式动画_刷新健康 用 档。
    返还 档。
完。

样式动画_创建 接受 名称：
    档 为 做 样式动画_建档 用 名称。
    做 样式动画_初始化 用 档。
    返还 做 样式动画_登记 用 档。
完。

样式动画_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 非 档 的 是否初始化：
        返还 假。
    完。
    返还 真。
完。

样式动画_新时间函数 接受 档、类型：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「新时间函数」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「新时间函数」、一组：类型、「时间函数」。
    结果 为 做 TimingFunction_创建 用 类型。
    档 的 时间数 为 档 的 时间数 加 1。
    返还 做 样式动画_成功 用 档、「新时间函数」、结果。
完。

样式动画_求值时间函数 接受 档、时间函数、t：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「求值时间函数」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「求值时间函数」、一组：时间函数、t、「时间函数」。
    结果 为 做 TimingFunction_求值 用 时间函数、t。
    档 的 时间数 为 档 的 时间数 加 1。
    返还 做 样式动画_成功 用 档、「求值时间函数」、结果。
完。

样式动画_贝塞尔 接受 档、x1、y1、x2、y2、t：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「贝塞尔」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「贝塞尔」、一组：x1、y1、x2、y2、t、「时间函数」。
    结果 为 做 Nova三次方Bezier_求值 用 x1、y1、x2、y2、t。
    档 的 时间数 为 档 的 时间数 加 1。
    返还 做 样式动画_成功 用 档、「贝塞尔」、结果。
完。

样式动画_步进 接受 档、时间函数、t：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「步进」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「步进」、一组：时间函数、t、「时间函数」。
    结果 为 做 TimingFunction_步进求值 用 时间函数、t。
    档 的 时间数 为 档 的 时间数 加 1。
    返还 做 样式动画_成功 用 档、「步进」、结果。
完。

样式动画_新效果 接受 档、名称、目标、持续时间：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「新效果」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「新效果」、一组：名称、目标、持续时间、「关键帧」。
    结果 为 做 KeyframeEffect_创建 用 名称、目标、持续时间。
    档 的 关键帧数 为 档 的 关键帧数 加 1。
    返还 做 样式动画_成功 用 档、「新效果」、结果。
完。

样式动画_加关键帧 接受 档、效果、偏移：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「加关键帧」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「加关键帧」、一组：效果、偏移、「关键帧」。
    结果 为 做 KeyframeEffect_添加关键帧 用 效果、偏移。
    档 的 关键帧数 为 档 的 关键帧数 加 1。
    返还 做 样式动画_成功 用 档、「加关键帧」、结果。
完。

样式动画_加属性 接受 档、关键帧、属性标识、值类型、数字值、颜色值：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「加属性」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「加属性」、一组：关键帧、属性标识、值类型、数字值、颜色值、「关键帧」。
    结果 为 做 Keyframe_添加属性 用 关键帧、属性标识、值类型、数字值、颜色值。
    档 的 关键帧数 为 档 的 关键帧数 加 1。
    返还 做 样式动画_成功 用 档、「加属性」、结果。
完。

样式动画_新动画 接受 档、效果：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「新动画」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「新动画」、一组：效果、「关键帧」。
    结果 为 做 Animation_创建 用 效果。
    档 的 关键帧数 为 档 的 关键帧数 加 1。
    返还 做 样式动画_成功 用 档、「新动画」、结果。
完。

样式动画_播放 接受 档、动画：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「播放」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「播放」、一组：动画、「关键帧」。
    结果 为 做 Animation_play 用 动画。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「播放」、结果。
完。

样式动画_暂停 接受 档、动画：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「暂停」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「暂停」、一组：动画、「关键帧」。
    结果 为 做 Animation_pause 用 动画。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「暂停」、结果。
完。

样式动画_取消 接受 档、动画：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「取消」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「取消」、一组：动画、「关键帧」。
    结果 为 做 Animation_cancel 用 动画。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「取消」、结果。
完。

样式动画_完成 接受 档、动画：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「完成」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「完成」、一组：动画、「关键帧」。
    结果 为 做 Animation_finish 用 动画。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「完成」、结果。
完。

样式动画_当前时间 接受 档、动画：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「当前时间」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「当前时间」、一组：动画、「关键帧」。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「当前时间」、做 Animation_计算当前时间 用 动画。
完。

样式动画_应用帧 接受 档、效果、进度：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「应用帧」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「应用帧」、一组：效果、进度、「关键帧插值」。
    结果 为 做 AnimationFrame_应用 用 效果、进度。
    档 的 插值数 为 档 的 插值数 加 1。
    返还 做 样式动画_成功 用 档、「应用帧」、结果。
完。

样式动画_查属性 接受 档、关键帧、属性标识：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「查属性」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「查属性」、一组：关键帧、属性标识、「关键帧插值」。
    档 的 插值数 为 档 的 插值数 加 1。
    返还 做 样式动画_成功 用 档、「查属性」、做 AnimationFrame_查找属性 用 关键帧、属性标识。
完。

样式动画_颜色插值 接受 档、颜色一、颜色二、t：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「颜色插值」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「颜色插值」、一组：颜色一、颜色二、t、「关键帧插值」。
    结果 为 做 颜色_插值 用 颜色一、颜色二、t。
    档 的 插值数 为 档 的 插值数 加 1。
    返还 做 样式动画_成功 用 档、「颜色插值」、结果。
完。

样式动画_应用属性 接受 档、元素、属性标识、值类型、值：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「应用属性」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「应用属性」、一组：元素、属性标识、值类型、值、「关键帧插值」。
    结果 为 做 AnimationFrame_应用属性 用 元素、属性标识、值类型、值。
    档 的 插值数 为 档 的 插值数 加 1。
    返还 做 样式动画_成功 用 档、「应用属性」、结果。
完。

样式动画_时间线注册 接受 档、动画：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「时间线注册」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「时间线注册」、一组：动画、「时间线」。
    结果 为 做 AnimationTimeline_注册 用 动画。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「时间线注册」、结果。
完。

样式动画_时间线更新 接受 档：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「时间线更新」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「时间线更新」、一组：（空）、「时间线」。
    结果 为 做 AnimationTimeline_每帧更新。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「时间线更新」、结果。
完。

样式动画_清理完成 接受 档：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「清理完成」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「清理完成」、一组：（空）、「时间线」。
    结果 为 做 AnimationTimeline_清理完成。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「清理完成」、结果。
完。

样式动画_智能创建 接受 档、名称、元素、时长、延迟、重复、方向、填充：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「智能创建」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「智能创建」、一组：名称、元素、时长、延迟、重复、方向、填充、「智能动画」。
    结果 为 做 动画_创建 用 名称、元素、时长、延迟、重复、方向、填充。
    档 的 预测数 为 档 的 预测数 加 1。
    返还 做 样式动画_成功 用 档、「智能创建」、结果。
完。

样式动画_智能加帧 接受 档、动画索引、百分比、属性、起始、结束、缓动：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「智能加帧」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「智能加帧」、一组：动画索引、百分比、属性、起始、结束、缓动、「智能动画」。
    结果 为 做 动画_添加帧 用 动画索引、百分比、属性、起始、结束、缓动。
    档 的 预测数 为 档 的 预测数 加 1。
    返还 做 样式动画_成功 用 档、「智能加帧」、结果。
完。

样式动画_智能启动 接受 档、动画索引：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「智能启动」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「智能启动」、一组：动画索引、「智能动画」。
    结果 为 做 动画_启动 用 动画索引。
    档 的 预测数 为 档 的 预测数 加 1。
    返还 做 样式动画_成功 用 档、「智能启动」、结果。
完。

样式动画_智能暂停 接受 档、动画索引：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「智能暂停」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「智能暂停」、一组：动画索引、「智能动画」。
    结果 为 做 动画_暂停 用 动画索引。
    档 的 预测数 为 档 的 预测数 加 1。
    返还 做 样式动画_成功 用 档、「智能暂停」、结果。
完。

样式动画_智能恢复 接受 档、动画索引：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「智能恢复」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「智能恢复」、一组：动画索引、「智能动画」。
    结果 为 做 动画_恢复 用 动画索引。
    档 的 预测数 为 档 的 预测数 加 1。
    返还 做 样式动画_成功 用 档、「智能恢复」、结果。
完。

样式动画_智能更新 接受 档：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「智能更新」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「智能更新」、一组：（空）、「智能动画」。
    结果 为 做 动画_更新帧。
    档 的 预测数 为 档 的 预测数 加 1。
    返还 做 样式动画_成功 用 档、「智能更新」、结果。
完。

样式动画_智能插值 接受 档、动画索引、帧索引：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「智能插值」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「智能插值」、一组：动画索引、帧索引、「智能动画」。
    结果 为 做 动画_插值 用 动画索引、帧索引。
    档 的 预测数 为 档 的 预测数 加 1。
    返还 做 样式动画_成功 用 档、「智能插值」、结果。
完。

样式动画_缓动计算 接受 档、类型、进度：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「缓动计算」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「缓动计算」、一组：类型、进度、「智能动画」。
    结果 为 做 缓动_计算 用 类型、进度。
    档 的 预测数 为 档 的 预测数 加 1。
    返还 做 样式动画_成功 用 档、「缓动计算」、结果。
完。

样式动画_智能统计 接受 档：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「智能统计」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「智能统计」、一组：（空）、「智能动画」。
    档 的 预测数 为 档 的 预测数 加 1。
    返还 做 样式动画_成功 用 档、「智能统计」、做 动画_完整统计。
完。

样式动画_过渡检查 接受 档、元素、旧样式、新样式：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「过渡检查」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「过渡检查」、一组：元素、旧样式、新样式、「过渡」。
    结果 为 做 Transition_检查并启动 用 元素、旧样式、新样式。
    档 的 关键帧数 为 档 的 关键帧数 加 1。
    返还 做 样式动画_成功 用 档、「过渡检查」、结果。
完。

样式动画_过渡创建 接受 档、元素、属性标识、值类型、旧值、新值、持续时间、延迟、缓动：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「过渡创建」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「过渡创建」、一组：元素、属性标识、值类型、旧值、新值、持续时间、延迟、缓动、「过渡」。
    结果 为 做 Transition_创建动画 用 元素、属性标识、值类型、旧值、新值、持续时间、延迟、缓动。
    档 的 关键帧数 为 档 的 关键帧数 加 1。
    返还 做 样式动画_成功 用 档、「过渡创建」、结果。
完。

样式动画_注册关键帧 接受 档、名称、规则：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「注册关键帧」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「注册关键帧」、一组：名称、规则、「过渡」。
    结果 为 做 Keyframes_注册 用 名称、规则。
    档 的 关键帧数 为 档 的 关键帧数 加 1。
    返还 做 样式动画_成功 用 档、「注册关键帧」、结果。
完。

样式动画_查关键帧 接受 档、名称：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「查关键帧」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「查关键帧」、一组：名称、「过渡」。
    档 的 关键帧数 为 档 的 关键帧数 加 1。
    返还 做 样式动画_成功 用 档、「查关键帧」、做 Keyframes_查找 用 名称。
完。

样式动画_新缓动 接受 档、类型：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「新缓动」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「新缓动」、一组：类型、「网动画」。
    结果 为 做 Easing_创建 用 类型。
    档 的 时间数 为 档 的 时间数 加 1。
    返还 做 样式动画_成功 用 档、「新缓动」、结果。
完。

样式动画_贝塞尔缓动 接受 档、x1、y1、x2、y2：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「贝塞尔缓动」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「贝塞尔缓动」、一组：x1、y1、x2、y2、「网动画」。
    结果 为 做 Easing_Nova三次方Bezier 用 x1、y1、x2、y2。
    档 的 时间数 为 档 的 时间数 加 1。
    返还 做 样式动画_成功 用 档、「贝塞尔缓动」、结果。
完。

样式动画_步进缓动 接受 档、步数、位置：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「步进缓动」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「步进缓动」、一组：步数、位置、「网动画」。
    结果 为 做 Easing_steps 用 步数、位置。
    档 的 时间数 为 档 的 时间数 加 1。
    返还 做 样式动画_成功 用 档、「步进缓动」、结果。
完。

样式动画_求缓动 接受 档、缓动器、t：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「求缓动」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「求缓动」、一组：缓动器、t、「网动画」。
    结果 为 做 Easing_计算 用 缓动器、t。
    档 的 时间数 为 档 的 时间数 加 1。
    返还 做 样式动画_成功 用 档、「求缓动」、结果。
完。

样式动画_元素动画 接受 档、元素、关键帧列表、帧数、持续时间、缓动、次数、填充、延迟：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「元素动画」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「元素动画」、一组：元素、关键帧列表、帧数、持续时间、缓动、次数、填充、延迟、「网动画」。
    结果 为 做 元素_animate 用 元素、关键帧列表、帧数、持续时间、缓动、次数、填充、延迟。
    档 的 关键帧数 为 档 的 关键帧数 加 1。
    返还 做 样式动画_成功 用 档、「元素动画」、结果。
完。

样式动画_文档动画 接受 档、结果列表、最大数：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「文档动画」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「文档动画」、一组：结果列表、最大数、「网动画」。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「文档动画」、做 Document_getAnimations 用 结果列表、最大数。
完。

样式动画_时间线跳动 接受 档：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「时间线跳动」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「时间线跳动」、一组：（空）、「网动画」。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「时间线跳动」、做 AnimationTimeline_tick。
完。

样式动画_新转换 接受 档、元素、属性标识、从值、到值、持续时间、延迟、缓动：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「新转换」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「新转换」、一组：元素、属性标识、从值、到值、持续时间、延迟、缓动、「网动画」。
    结果 为 做 CSSTransition_创建 用 元素、属性标识、从值、到值、持续时间、延迟、缓动。
    档 的 关键帧数 为 档 的 关键帧数 加 1。
    返还 做 样式动画_成功 用 档、「新转换」、结果。
完。

样式动画_新关键帧集 接受 档、元素、关键帧列表、帧数、名称、持续时间、次数、方向、填充、延迟、缓动：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「新关键帧集」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「新关键帧集」、一组：元素、关键帧列表、帧数、名称、持续时间、次数、方向、填充、延迟、缓动、「网动画」。
    结果 为 做 CSSKeyframes_创建 用 元素、关键帧列表、帧数、名称、持续时间、次数、方向、填充、延迟、缓动。
    档 的 关键帧数 为 档 的 关键帧数 加 1。
    返还 做 样式动画_成功 用 档、「新关键帧集」、结果。
完。

样式动画_图处加速 接受 档、动画：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「图处加速」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「图处加速」、一组：动画、「网动画」。
    档 的 预测数 为 档 的 预测数 加 1。
    返还 做 样式动画_成功 用 档、「图处加速」、做 智能_Animation_图处加速检测 用 动画。
完。

样式动画_帧预测 接受 档、动画、当前帧、预测帧数：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「帧预测」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「帧预测」、一组：动画、当前帧、预测帧数、「网动画」。
    档 的 预测数 为 档 的 预测数 加 1。
    返还 做 样式动画_成功 用 档、「帧预测」、做 智能_Animation_帧预测 用 动画、当前帧、预测帧数。
完。

样式动画_自动降级 接受 档、动画、设备性能评分：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「自动降级」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「自动降级」、一组：动画、设备性能评分、「网动画」。
    档 的 预测数 为 档 的 预测数 加 1。
    返还 做 样式动画_成功 用 档、「自动降级」、做 智能_Animation_自动降级 用 动画、设备性能评分。
完。

样式动画_时间轴 接受 档：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「时间轴」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「时间轴」、一组：（空）、「文档时间轴」。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「时间轴」、做 Timeline_获取。
完。

样式动画_当前轴时 接受 档：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「当前轴时」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「当前轴时」、一组：（空）、「文档时间轴」。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「当前轴时」、做 Timeline_currentTime。
完。

样式动画_反转 接受 档、动画：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「反转」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「反转」、一组：动画、「文档时间轴」。
    结果 为 做 Animation_reverse 用 动画。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「反转」、结果。
完。

样式动画_更新速率 接受 档、动画、速率：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「更新速率」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「更新速率」、一组：动画、速率、「文档时间轴」。
    结果 为 做 Animation_updatePlaybackRate 用 动画、速率。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「更新速率」、结果。
完。

样式动画_滴答 接受 档、动画、时间轴时间：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「滴答」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「滴答」、一组：动画、时间轴时间、「文档时间轴」。
    结果 为 做 Animation_tick 用 动画、时间轴时间。
    档 的 时间线数 为 档 的 时间线数 加 1。
    返还 做 样式动画_成功 用 档、「滴答」、结果。
完。

样式动画_新矩阵 接受 档：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「新矩阵」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「新矩阵」、一组：（空）、「样式变换」。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「新矩阵」、做 Matrix4x4_创建。
完。

样式动画_单位矩阵 接受 档、矩阵：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「单位矩阵」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「单位矩阵」、一组：矩阵、「样式变换」。
    结果 为 做 Matrix4x4_单位化 用 矩阵。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「单位矩阵」、结果。
完。

样式动画_矩阵乘 接受 档、矩阵一、矩阵二：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「矩阵乘」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「矩阵乘」、一组：矩阵一、矩阵二、「样式变换」。
    结果 为 做 Matrix4x4_乘 用 矩阵一、矩阵二。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「矩阵乘」、结果。
完。

样式动画_平移 接受 档、tx、ty：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「平移」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「平移」、一组：tx、ty、「样式变换」。
    结果 为 做 Transform_translate 用 tx、ty。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「平移」、结果。
完。

样式动画_缩放 接受 档、sx、sy：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「缩放」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「缩放」、一组：sx、sy、「样式变换」。
    结果 为 做 Transform_scale 用 sx、sy。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「缩放」、结果。
完。

样式动画_旋转 接受 档、角度_度：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「旋转」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「旋转」、一组：角度_度、「样式变换」。
    结果 为 做 Transform_rotate 用 角度_度。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「旋转」、结果。
完。

样式动画_斜切X 接受 档、角度_度：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「斜切X」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「斜切X」、一组：角度_度、「样式变换」。
    结果 为 做 Transform_skewX 用 角度_度。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「斜切X」、结果。
完。

样式动画_透视 接受 档、距离：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「透视」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「透视」、一组：距离、「样式变换」。
    结果 为 做 Transform_perspective 用 距离。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「透视」、结果。
完。

样式动画_变点 接受 档、矩阵、x、y：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「变点」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「变点」、一组：矩阵、x、y、「样式变换」。
    结果 为 做 Transform_变换点 用 矩阵、x、y。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「变点」、结果。
完。

样式动画_变矩形 接受 档、矩阵、矩形：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「变矩形」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「变矩形」、一组：矩阵、矩形、「样式变换」。
    结果 为 做 Transform_变换矩形 用 矩阵、矩形。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「变矩形」、结果。
完。

样式动画_分解二维 接受 档、矩阵：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「分解二维」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「分解二维」、一组：矩阵、「样式变换」。
    结果 为 做 Transform_分解2D 用 矩阵。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「分解二维」、结果。
完。

样式动画_插值变换 接受 档、分解一、分解二、t：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「插值变换」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「插值变换」、一组：分解一、分解二、t、「样式变换」。
    结果 为 做 Transform_插值 用 分解一、分解二、t。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「插值变换」、结果。
完。

样式动画_重组变换 接受 档、分解：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「重组变换」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「重组变换」、一组：分解、「样式变换」。
    结果 为 做 Transform_从分解重组 用 分解。
    档 的 变换数 为 档 的 变换数 加 1。
    返还 做 样式动画_成功 用 档、「重组变换」、结果。
完。

样式动画_巡检 接受 档、名称、元素：
    若 非 做 样式动画_检查档 用 档：
        返还 做 样式动画_失败 用 档、「巡检」、「动画档不可用」。
    完。
    做 样式动画_记录 用 档、「巡检」、一组：名称、元素、「样式动画」。
    可变 报告 为 一组：（空）。
    效果 为 做 样式动画_新效果 用 档、名称、元素、300 的 数据。
    做 追加 用 报告、做 样式动画_新时间函数 用 档、1 的 数据。
    做 追加 用 报告、做 样式动画_时间线注册 用 档、做 样式动画_新动画 用 档、效果 的 数据 的 数据。
    做 追加 用 报告、做 样式动画_时间线更新 用 档 的 数据。
    做 追加 用 报告、做 样式动画_智能统计 用 档 的 数据。
    返还 做 样式动画_成功 用 档、「巡检」、报告。
完。

样式动画_摘要 接受 档：
    摘要 为 新建 动画摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 时间数 为 档 的 时间数。
    摘要 的 关键帧数 为 档 的 关键帧数。
    摘要 的 插值数 为 档 的 插值数。
    摘要 的 时间线数 为 档 的 时间线数。
    摘要 的 变换数 为 档 的 变换数。
    摘要 的 预测数 为 档 的 预测数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 样式动画_刷新健康 用 档。
    返还 摘要。
完。

样式动画_健康 接受 档：
    若 档 等于 空：
        返还 「无档」。
    完。
    返还 做 样式动画_刷新健康 用 档。
完。

样式动画_自检 接受 档：
    若 非 做 样式动画_检查档 用 档：
        返还 假。
    完。
    若 做 样式动画_时间线更新 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 样式动画_智能统计 用 档 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

样式动画_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 样式动画_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        做 样式动画_初始化 用 档。
        返还 做 样式动画_成功 用 档、「处理」、「已初始化」。
    完。
    若 请求 的 动作 等于 「新效果」：
        返还 做 样式动画_新效果 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「播放」：
        返还 做 样式动画_播放 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「过渡」：
        返还 做 样式动画_过渡检查 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「变换」：
        返还 做 样式动画_平移 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「统计」：
        返还 做 样式动画_成功 用 档、「统计」、做 样式动画_摘要 用 档。
    完。
    若 请求 的 动作 等于 「巡检」：
        返还 做 样式动画_巡检 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    返还 做 样式动画_失败 用 档、「处理」、「未知动作」。
完。

本章 收束。
