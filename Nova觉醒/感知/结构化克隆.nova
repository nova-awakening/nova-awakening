《结构化克隆》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·标准·类型判断》。

引用《.·内核直连层》。

引用《..·中枢·辅助存根》。

引用《..·中枢·辅助有符号读》。

引用《..·推理·小整数SMI判定》。

引用《..·中枢·字符串替换》。

SC_TYPE_UNDEFINED 为 0。

SC_TYPE_NULL 为 1。

SC_TYPE_BOOLEAN 为 2。

SC_TYPE_NUMBER 为 3。

SC_TYPE_STRING 为 4。

SC_TYPE_OBJECT 为 5。

SC_TYPE_ARRAY 为 6。

SC_TYPE_DATE 为 7。

SC_TYPE_REGEXP 为 8。

SC_TYPE_MAP 为 9。

SC_TYPE_SET 为 10。

SC_TYPE_ARRAYBUFFER 为 11。

SC_TYPE_ERROR 为 12。

SC_TYPE_BLOB 为 13。

StructuredCloneData 是 一种 事物，含 数据、大小。

StructuredClone_序列化 接受 值：
    结果 为 新建 StructuredCloneData。
    结果 的 数据 为 做 堆_分配 用 4096。
    结果 的 大小 为 0。
    做 StructuredClone_写值 用 结果、值。
    返还 结果。
完。

StructuredClone_写值 接受 结果、值：
    若 值 等于 0：
        做 StructuredClone_写字节 用 结果、SC_TYPE_UNDEFINED。
        返还。
    完。
    若 做 JSValue_IsNumber 用 值：
        做 StructuredClone_写字节 用 结果、SC_TYPE_NUMBER。
        做 StructuredClone_写64 用 结果、做 Nova值_转数值 用 0、值。
        返还。
    完。
    若 做 JSValue_IsString 用 值：
        str 为 做 JSValue_获取字符串 用 值。
        len 为 做 字符串_长度 用 str。
        做 StructuredClone_写字节 用 结果、SC_TYPE_STRING。
        做 StructuredClone_写32 用 结果、len。
        做 StructuredClone_写数据 用 结果、str、len。
        返还。
    完。
    若 做 JSValue_IsBoolean 用 值：
        做 StructuredClone_写字节 用 结果、SC_TYPE_BOOLEAN。
        做 StructuredClone_写字节 用 结果、若 做 JSValue_ToBoolean 用 0、值 则 1 否则 0。
        返还。
    完。
    做 StructuredClone_写字节 用 结果、SC_TYPE_OBJECT。
    keys 为 做 Nova对象_获取所有键 用 0、值。
    key_count 为 做 Nova数组_长度 用 keys。
    做 StructuredClone_写变32 用 结果、key_count。
    可变 ki 为 0。
    只要 ki 小于 key_count：
        key 为 做 Nova数组_获取 用 keys、ki。
        val 为 做 Nova对象_获取属性 用 0、值、key。
        做 StructuredClone_序列化_字符串 用 结果、key。
        做 StructuredClone_序列化 用 结果、val。
        ki 为 ki 加 1。
    完。
完。

StructuredClone_反序列化 接受 数据、大小：
    若 数据 等于 0 或者 大小 等于 0：
        返还 0。
    完。
    可变 pos 为 0。
    返还 做 StructuredClone_反序列化_值 用 数据、大小、pos。
完。

StructuredClone_转移 接受 对象：
    若 对象 等于 0：
        返还 0。
    完。
    新对象 为 做 堆_分配 用 16。
    做 内存_写64 用 新对象、对象 的 数据指针。
    做 内存_写64 用 新对象 加 8、对象 的 字节长度。
    对象 的 数据指针 为 0。
    对象 的 字节长度 为 0。
    对象 的 已分离 为 真。
    返还 新对象。
完。

StructuredClone_写字节 接受 结果、字节：
    做 内存_写8 用 结果 的 数据 加 结果 的 大小、字节。
    结果 的 大小 为 结果 的 大小 加 1。
完。

StructuredClone_写32 接受 结果、值：
    做 内存_写32 用 结果 的 数据 加 结果 的 大小、值。
    结果 的 大小 为 结果 的 大小 加 4。
完。

StructuredClone_写64 接受 结果、值：
    做 内存_写64 用 结果 的 数据 加 结果 的 大小、值。
    结果 的 大小 为 结果 的 大小 加 8。
完。

StructuredClone_写数据 接受 结果、数据、长度：
    做 内存_复制 用 结果 的 数据 加 结果 的 大小、数据、长度。
    结果 的 大小 为 结果 的 大小 加 长度。
完。

本章 收束。
