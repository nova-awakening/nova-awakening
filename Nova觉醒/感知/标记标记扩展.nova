《标记标记扩展》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·辅助存根》。

引用《.·文档节点》。

元素_innerHTML_get 接受 节点：
    可变 html 为 「」。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        html 为 做 字符串_连接 用 html、做 节点_序列化HTML 用 子。
        子 为 子 的 后一兄弟。
    完。
    返还 html。
完。

元素_innerHTML_set 接受 节点、html文本：
    只要 节点 的 第一子节点 不等于 0：
        做 节点_removeChild 用 节点、节点 的 第一子节点。
    完。
    片段 为 做 HTML解析器_解析片段 用 html文本、节点。
    若 片段 不等于 0：
        可变 子 为 片段 的 第一子节点。
        只要 子 不等于 0：
            下一个 为 子 的 后一兄弟。
            做 节点_appendChild 用 节点、子。
            子 为 下一个。
        完。
    完。
完。

可变 _HTML序列化深度 为 0。

节点_序列化HTML 接受 节点：
    _HTML序列化深度 为 _HTML序列化深度 加 1。
    若 _HTML序列化深度 大于 5000：
        _HTML序列化深度 为 _HTML序列化深度 减 1。
        返还 「」。
    完。
    若 节点 的 节点类型 等于 TEXT_节点：
        文字 为 做 节点_获取字符数据 用 节点。
        若 文字 不等于 0：
            返还 做 HTML_转义文字 用 文字。
        完。
        返还 「」。
    完。
    若 节点 的 节点类型 等于 COMMENT_节点：
        数据 为 做 节点_获取字符数据 用 节点。
        返还 做 字符串_连接 用 「<!--」、做 字符串_连接 用 数据、「-->」。
    完。
    若 节点 的 节点类型 等于 元素_节点：
        标签 为 做 元素_tagName 用 节点。
        可变 html 为 做 字符串_连接 用 「<」、标签。
        扩展 为 做 元素_获取扩展 用 节点。
        若 扩展 不等于 0 并且 扩展 的 元素数据 不等于 0：
            数据 为 扩展 的 元素数据。
            可变 i 为 0。
            只要 i 小于 数据 的 属性数量：
                属性 为 做 元素Data_第N个属性 用 数据、i。
                html 为 做 字符串_连接 用 html、「 」。
                html 为 做 字符串_连接 用 html、属性 的 名称字符串。
                html 为 做 字符串_连接 用 html、「=\"」。
                html 为 做 字符串_连接 用 html、做 HTML_转义属性值 用 属性 的 值。
                html 为 做 字符串_连接 用 html、「\"」。
                i 为 i 加 1。
            完。
        完。
        html 为 做 字符串_连接 用 html、「>」。
        若 做 元素_是空元素 用 节点：
            返还 html。
        完。
        可变 子 为 节点 的 第一子节点。
        只要 子 不等于 0：
            html 为 做 字符串_连接 用 html、做 节点_序列化HTML 用 子。
            子 为 子 的 后一兄弟。
        完。
        html 为 做 字符串_连接 用 html、「</」。
        html 为 做 字符串_连接 用 html、标签。
        html 为 做 字符串_连接 用 html、「>」。
        返还 html。
    完。
    返还 「」。
完。

元素_是空元素 接受 节点：
    扩展 为 做 元素_获取扩展 用 节点。
    若 扩展 等于 0：
        返还 0。
    完。
    标签 为 扩展 的 标签名原子。
    若 标签 等于 原子_br：
        返还 1。
    完。
    若 标签 等于 原子_hr：
        返还 1。
    完。
    若 标签 等于 原子_img：
        返还 1。
    完。
    若 标签 等于 原子_input：
        返还 1。
    完。
    若 标签 等于 原子_link：
        返还 1。
    完。
    若 标签 等于 原子_meta：
        返还 1。
    完。
    返还 0。
完。

HTML_转义文字 接受 文字：
    若 文字 等于 0：
        返还 文字。
    完。
    len 为 做 字节长度 用 文字。
    buf 为 做 堆_分配 用 len 乘 6。
    可变 pos 为 0。
    可变 i 为 0。
    只要 i 小于 len：
        ch 为 做 字节取 用 文字、i。
        若 ch 等于 38：
            做 内存_复制 用 buf 加 pos、「&amp;」、5。
            pos 为 pos 加 5。
        否则若 ch 等于 60：
            做 内存_复制 用 buf 加 pos、「&lt;」、4。
            pos 为 pos 加 4。
        否则若 ch 等于 62：
            做 内存_复制 用 buf 加 pos、「&gt;」、4。
            pos 为 pos 加 4。
        否则：
            做 字节写 用 buf、pos、ch。
            pos 为 pos 加 1。
        完。
        i 为 i 加 1。
    完。
    做 字节写 用 buf、pos、0。
    返还 buf。
完。

HTML_转义属性值 接受 值：
    若 值 等于 0：
        返还 值。
    完。
    len 为 做 字节长度 用 值。
    buf 为 做 堆_分配 用 len 乘 6。
    可变 pos 为 0。
    可变 i 为 0。
    只要 i 小于 len：
        ch 为 做 字节取 用 值、i。
        若 ch 等于 38：
            做 内存_复制 用 buf 加 pos、「&amp;」、5。
            pos 为 pos 加 5。
        否则若 ch 等于 34：
            做 内存_复制 用 buf 加 pos、「&quot;」、6。
            pos 为 pos 加 6。
        否则：
            做 字节写 用 buf、pos、ch。
            pos 为 pos 加 1。
        完。
        i 为 i 加 1。
    完。
    做 字节写 用 buf、pos、0。
    返还 buf。
完。

元素_outerHTML_get 接受 节点：
    若 节点 等于 0：
        返还 「」。
    完。
    返还 做 节点_序列化HTML 用 节点。
完。

元素_outerHTML_set 接受 节点、html文本：
    若 节点 等于 0 或者 html文本 等于 0：
        返还。
    完。
    父 为 节点 的 父节点。
    若 父 等于 0：
        返还。
    完。
    片段 为 做 HTML解析器_解析片段 用 html文本、父。
    若 片段 不等于 0：
        可变 子 为 片段 的 第一子节点。
        只要 子 不等于 0：
            下一个 为 子 的 后一兄弟。
            做 节点_insertBefore 用 父、子、节点。
            子 为 下一个。
        完。
        做 节点_removeChild 用 父、节点。
    完。
完。

节点_textContent_get 接受 节点：
    若 节点 等于 0：
        返还 「」。
    完。
    若 节点 的 节点类型 等于 TEXT_节点 或者 节点 的 节点类型 等于 COMMENT_节点：
        返还 做 节点_获取字符数据 用 节点。
    完。
    可变 文本 为 「」。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        若 子 的 节点类型 等于 TEXT_节点：
            数据 为 做 节点_获取字符数据 用 子。
            若 数据 不等于 0：
                文本 为 做 字符串_连接 用 文本、数据。
            完。
        否则若 子 的 节点类型 等于 元素_节点：
            文本 为 做 字符串_连接 用 文本、做 节点_textContent_get 用 子。
        完。
        子 为 子 的 后一兄弟。
    完。
    返还 文本。
完。

节点_textContent_set 接受 节点、文本：
    若 节点 等于 0：
        返还。
    完。
    只要 节点 的 第一子节点 不等于 0：
        做 节点_removeChild 用 节点、节点 的 第一子节点。
    完。
    若 文本 不等于 0 并且 做 字节长度 用 文本 大于 0：
        文本节点 为 做 节点_创建 用 TEXT_节点、0。
        文本节点 的 数据 为 做 字符串_复制 用 文本。
        做 节点_appendChild 用 节点、文本节点。
    完。
完。

元素_innerText_get 接受 节点：
    若 节点 等于 0：
        返还 「」。
    完。
    可变 文本 为 「」。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        若 子 的 节点类型 等于 TEXT_节点：
            数据 为 做 节点_获取字符数据 用 子。
            若 数据 不等于 0：
                文本 为 做 字符串_连接 用 文本、数据。
            完。
        否则若 子 的 节点类型 等于 元素_节点：
            扩展 为 做 元素_获取扩展 用 子。
            若 扩展 不等于 0 并且 扩展 的 计算样式 不等于 0：
                若 扩展 的 计算样式 的 display 不等于 0：
                    文本 为 做 字符串_连接 用 文本、做 元素_innerText_get 用 子。
                完。
            否则：
                文本 为 做 字符串_连接 用 文本、做 元素_innerText_get 用 子。
            完。
        完。
        子 为 子 的 后一兄弟。
    完。
    返还 文本。
完。

元素_insertAdjacentHTML 接受 节点、位置、html文本：
    若 节点 等于 0 或者 html文本 等于 0：
        返还。
    完。
    片段 为 做 HTML解析器_解析片段 用 html文本、节点。
    若 片段 等于 0：
        返还。
    完。
    若 做 字符串_相等 用 位置、「beforebegin」：
        若 节点 的 父节点 不等于 0：
            可变 子 为 片段 的 第一子节点。
            只要 子 不等于 0：
                下一个 为 子 的 后一兄弟。
                做 节点_insertBefore 用 节点 的 父节点、子、节点。
                子 为 下一个。
            完。
        完。
    否则若 做 字符串_相等 用 位置、「afterbegin」：
        第一子 为 节点 的 第一子节点。
        可变 子 为 片段 的 第一子节点。
        只要 子 不等于 0：
            下一个 为 子 的 后一兄弟。
            做 节点_insertBefore 用 节点、子、第一子。
            子 为 下一个。
        完。
    否则若 做 字符串_相等 用 位置、「beforeend」：
        可变 子 为 片段 的 第一子节点。
        只要 子 不等于 0：
            下一个 为 子 的 后一兄弟。
            做 节点_appendChild 用 节点、子。
            子 为 下一个。
        完。
    否则若 做 字符串_相等 用 位置、「afterend」：
        若 节点 的 父节点 不等于 0：
            下一兄弟 为 节点 的 后一兄弟。
            可变 子 为 片段 的 第一子节点。
            只要 子 不等于 0：
                下一个 为 子 的 后一兄弟。
                做 节点_insertBefore 用 节点 的 父节点、子、下一兄弟。
                子 为 下一个。
            完。
        完。
    完。
完。

TEXT_节点 为 3。

COMMENT_节点 为 8。

元素_节点 为 1。

原子_br 为 100。

原子_hr 为 101。

原子_img 为 102。

原子_input 为 103。

原子_link 为 104。

原子_meta 为 105。

原子_area 为 106。

原子_base 为 107。

原子_col 为 108。

原子_embed 为 109。

原子_source 为 110。

原子_track 为 111。

原子_wbr 为 112。

原子_param 为 113。

元素_是空元素_扩展 接受 标签原子：
    若 标签原子 大于或等于 原子_br 并且 标签原子 小于或等于 原子_param：
        返还 真。
    完。
    返还 假。
完。

节点_appendChild 接受 父、子：
    若 父 等于 0 或者 子 等于 0：
        返还。
    完。
    DOM操作_总数 为 DOM操作_总数 加 1。
完。

节点_removeChild 接受 父、子：
    若 父 等于 0 或者 子 等于 0：
        返还。
    完。
    DOM操作_总数 为 DOM操作_总数 加 1。
完。

节点_insertBefore 接受 父、新子、参考子：
    若 父 等于 0 或者 新子 等于 0：
        返还。
    完。
    DOM操作_总数 为 DOM操作_总数 加 1。
完。

节点_创建 接受 类型、标志：
    DOM操作_总数 为 DOM操作_总数 加 1。
    返还 0。
完。

节点_获取字符数据 接受 节点：
    若 节点 等于 0：
        返还 「」。
    完。
    返还 节点 的 数据。
完。

元素_tagName 接受 节点：
    扩展 为 做 元素_获取扩展 用 节点。
    若 扩展 等于 0：
        返还 「div」。
    完。
    返还 扩展 的 标签名。
完。

HTML解析器_解析片段 接受 html、上下文：
    若 html 等于 0：
        返还 0。
    完。
    返还 0。
完。

可变 DOM操作_总数 为 0。

可变 innerHTML_设置数 为 0。

可变 innerHTML_获取数 为 0。

可变 outerHTML_设置数 为 0。

可变 textContent_设置数 为 0。

可变 insertAdjacent_总数 为 0。

DOM序列化_统计 接受 零参：
    返还 一本字典：DOM操作 对应 DOM操作_总数、innerHTML_set 对应 innerHTML_设置数、innerHTML_get 对应 innerHTML_获取数、outerHTML_set 对应 outerHTML_设置数、textContent_set 对应 textContent_设置数、insertAdjacent 对应 insertAdjacent_总数。
完。

DOM序列化_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 innerHTML_设置数 大于 10000：
        做 追加 用 问题、「innerHTML设置过多: 可能存在性能问题」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「DOM序列化健康」。
    完。
    返还 问题。
完。

DOM序列化_重置 接受 零参：
    DOM操作_总数 为 0。
    innerHTML_设置数 为 0。
    innerHTML_获取数 为 0。
    outerHTML_设置数 为 0。
    textContent_设置数 为 0。
    insertAdjacent_总数 为 0。
完。

DOM序列化_完整统计 接受 零参：
    返还 一组：DOM操作_总数、innerHTML_设置数、innerHTML_获取数、outerHTML_设置数。
完。

可变 AI_标记标记扩_热节点缓存 为 0。

AI_标记标记扩_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_标记标记扩_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_标记标记扩_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

标记标记扩展_统计 接受 零参：
    返还 一本字典：DOM操作总数 对应 DOM操作_总数、insertAdjacent总数 对应 insertAdjacent_总数。
完。

标记标记扩展_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 DOM操作_总数 大于 100000：
        做 追加 用 问题、「DOM操作_总数异常增长」。
    完。
    若 insertAdjacent_总数 大于 100000：
        做 追加 用 问题、「insertAdjacent_总数异常增长」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

标记标记扩展_综合报告 接受 零参：
    做 打印 用 「[标记标记扩展] ═══ 综合报告 ═══」。
    做 打印 用 「  DOM操作_总数: 」 加 DOM操作_总数。
    做 打印 用 「  insertAdjacent_总数: 」 加 insertAdjacent_总数。
    做 打印 用 「  健康: 」 加 若 做 标记标记扩展_健康评估 则 「正常」 否则 「异常」。
完。

标记标记扩展_全局重置 接受 零参：
    DOM操作_总数 为 0。
    insertAdjacent_总数 为 0。
    做 打印 用 「[标记标记扩展] 统计已重置」。
    返还 0。
完。

本章 收束。
