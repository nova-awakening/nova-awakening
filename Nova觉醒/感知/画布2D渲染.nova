《画布2D渲染》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《..·中枢·内存初始化》。

最大路径点 为 1024。

基数 为 1024。

画布状态 是 一种 事物，含 填充色、描边色、线宽、全局透明、变换矩阵、裁剪横、裁剪纵、裁剪宽、裁剪高、帧缓冲、宽度、高度。

可变 路径表 为 0。

可变 路径数 为 0。

可变 全局画布 为 0。

可变 绘制总数 为 0。

画布_创建 接受 帧缓冲、宽、高：
    全局画布 为 做 堆_分配 用 画布状态。
    全局画布 的 填充色 为 0xFF000000。
    全局画布 的 描边色 为 0xFF000000。
    全局画布 的 线宽 为 1。
    全局画布 的 全局透明 为 基数。
    全局画布 的 变换矩阵 为 0。
    全局画布 的 裁剪横 为 0。
    全局画布 的 裁剪纵 为 0。
    全局画布 的 裁剪宽 为 宽。
    全局画布 的 裁剪高 为 高。
    全局画布 的 帧缓冲 为 帧缓冲。
    全局画布 的 宽度 为 宽。
    全局画布 的 高度 为 高。
    路径表 为 做 堆_分配 用 最大路径点 乘 8。
    路径数 为 0。
    返还 全局画布。
完。

画布_设填充色 接受 颜色：
    若 全局画布 等于 0：
        返还。
    完。
    全局画布 的 填充色 为 颜色。
完。

画布_设描边色 接受 颜色：
    若 全局画布 等于 0：
        返还。
    完。
    全局画布 的 描边色 为 颜色。
完。

画布_设线宽 接受 宽度：
    若 全局画布 等于 0：
        返还。
    完。
    全局画布 的 线宽 为 宽度。
完。

画布_设透明 接受 透明度：
    若 全局画布 等于 0：
        返还。
    完。
    全局画布 的 全局透明 为 透明度。
完。

画布_填充矩形 接受 横、纵、宽、高：
    若 全局画布 等于 0：
        返还。
    完。
    绘制总数 为 绘制总数 加 1。
    可变 行 为 纵。
    只要 行 小于 纵 加 高 并且 行 小于 全局画布 的 高度：
        若 行 小于 0：
            行 为 行 加 1。
            继续。
        完。
        可变 列 为 横。
        只要 列 小于 横 加 宽 并且 列 小于 全局画布 的 宽度：
            若 列 大于或等于 0：
                做 画布_写像素 用 列、行、全局画布 的 填充色。
            完。
            列 为 列 加 1。
        完。
        行 为 行 加 1。
    完。
完。

画布_描边矩形 接受 横、纵、宽、高：
    若 全局画布 等于 0：
        返还。
    完。
    绘制总数 为 绘制总数 加 1。
    做 画布_水平线 用 横、纵、宽。
    做 画布_水平线 用 横、纵 加 高 减 1、宽。
    做 画布_垂直线 用 横、纵、高。
    做 画布_垂直线 用 横 加 宽 减 1、纵、高。
完。

画布_清除矩形 接受 横、纵、宽、高：
    若 全局画布 等于 0：
        返还。
    完。
    可变 行 为 纵。
    只要 行 小于 纵 加 高 并且 行 小于 全局画布 的 高度：
        若 行 大于或等于 0：
            可变 列 为 横。
            只要 列 小于 横 加 宽 并且 列 小于 全局画布 的 宽度：
                若 列 大于或等于 0：
                    做 画布_写像素 用 列、行、0。
                完。
                列 为 列 加 1。
            完。
        完。
        行 为 行 加 1。
    完。
完。

画布_水平线 接受 横、纵、长度：
    若 全局画布 等于 0 或者 纵 小于 0 或者 纵 大于或等于 全局画布 的 高度：
        返还。
    完。
    可变 列 为 横。
    只要 列 小于 横 加 长度 并且 列 小于 全局画布 的 宽度：
        若 列 大于或等于 0：
            做 画布_写像素 用 列、纵、全局画布 的 描边色。
        完。
        列 为 列 加 1。
    完。
完。

画布_垂直线 接受 横、纵、长度：
    若 全局画布 等于 0 或者 横 小于 0 或者 横 大于或等于 全局画布 的 宽度：
        返还。
    完。
    可变 行 为 纵。
    只要 行 小于 纵 加 长度 并且 行 小于 全局画布 的 高度：
        若 行 大于或等于 0：
            做 画布_写像素 用 横、行、全局画布 的 描边色。
        完。
        行 为 行 加 1。
    完。
完。

画布_开始路径 接受 零参：
    路径数 为 0。
完。

画布_移到 接受 横、纵：
    若 路径数 大于或等于 最大路径点：
        返还。
    完。
    做 内存_写32 用 路径表 加 路径数 乘 8、横。
    做 内存_写32 用 路径表 加 路径数 乘 8 加 4、纵。
    路径数 为 路径数 加 1。
完。

画布_线到 接受 横、纵：
    若 路径数 大于或等于 最大路径点：
        返还。
    完。
    做 内存_写32 用 路径表 加 路径数 乘 8、横。
    做 内存_写32 用 路径表 加 路径数 乘 8 加 4、纵。
    路径数 为 路径数 加 1。
完。

画布_描边路径 接受 零参：
    若 全局画布 等于 0 或者 路径数 小于 2：
        返还。
    完。
    绘制总数 为 绘制总数 加 1。
    可变 序号 为 0。
    只要 序号 小于 路径数 减 1：
        横1 为 做 内存_读32 用 路径表 加 序号 乘 8。
        纵1 为 做 内存_读32 用 路径表 加 序号 乘 8 加 4。
        横2 为 做 内存_读32 用 路径表 加 (序号 加 1) 乘 8。
        纵2 为 做 内存_读32 用 路径表 加 (序号 加 1) 乘 8 加 4。
        做 画布_画线 用 横1、纵1、横2、纵2。
        序号 为 序号 加 1。
    完。
完。

画布_画线 接受 横1、纵1、横2、纵2：
    可变 差横 为 横2 减 横1。
    可变 差纵 为 纵2 减 纵1。
    若 差横 小于 0：
        差横 为 0 减 差横。
    完。
    若 差纵 小于 0：
        差纵 为 0 减 差纵。
    完。
    步横 为 1。
    若 横2 小于 横1：
        步横 为 负 1。
    完。
    步纵 为 1。
    若 纵2 小于 纵1：
        步纵 为 负 1。
    完。
    可变 误差 为 差横 减 差纵。
    可变 当前横 为 横1。
    可变 当前纵 为 纵1。
    只要 真：
        做 画布_写像素 用 当前横、当前纵、全局画布 的 描边色。
        若 当前横 等于 横2 并且 当前纵 等于 纵2：
            跳出。
        完。
        双误差 为 误差 乘 2。
        若 双误差 大于 0 减 差纵：
            误差 为 误差 减 差纵。
            当前横 为 当前横 加 步横。
        完。
        若 双误差 小于 差横：
            误差 为 误差 加 差横。
            当前纵 为 当前纵 加 步纵。
        完。
    完。
完。

画布_写像素 接受 横、纵、颜色：
    若 全局画布 等于 0：
        返还。
    完。
    若 横 小于 0 或者 横 大于或等于 全局画布 的 宽度 或者 纵 小于 0 或者 纵 大于或等于 全局画布 的 高度：
        返还。
    完。
    做 内存_写32 用 全局画布 的 帧缓冲 加 (纵 乘 全局画布 的 宽度 加 横) 乘 4、颜色。
完。

画布_完整统计 接受 零参：
    返还 一组：绘制总数、路径数。
完。

本章 收束。
