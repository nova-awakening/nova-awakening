《矢量图创建》

引用《..·稳态·图处显示》。

引用《.·内核直连层》。

引用《..·推理·通信总线》。

引用《..·推理·定点Q算术》。

SVG命令_M 为 77。

SVG命令_m 为 109。

SVG命令_L 为 76。

SVG命令_l 为 108。

SVG命令_H 为 72。

SVG命令_h 为 104。

SVG命令_V 为 86。

SVG命令_v 为 118。

SVG命令_C 为 67。

SVG命令_c 为 99。

SVG命令_S 为 83。

SVG命令_s 为 115。

SVG命令_Q 为 81。

SVG命令_q 为 113。

SVG命令_T 为 84。

SVG命令_t 为 116。

SVG命令_A 为 65。

SVG命令_a 为 97。

SVG命令_Z 为 90。

SVG命令_z 为 122。

SVGPathSeg 是 一种 事物，含 命令、参数、下一个。

SVGPath 是 一种 事物，含 段链头、段链尾、段数、bbox_x、bbox_y、bbox_w、bbox_h。

SVGPath_创建 接受 零参：
    p 为 做 堆_分配 用 SVGPath。
    p 的 段链头 为 0。
    p 的 段链尾 为 0。
    p 的 段数 为 0。
    返还 p。
完。

SVGPath_添加段 接受 path、命令、p0、p1、p2、p3、p4、p5、p6：
    若 path 等于 0：
        返还。
    完。
    seg 为 做 堆_分配 用 SVGPathSeg。
    seg 的 命令 为 命令。
    seg 的 参数 的 第 0 项 为 p0。
    seg 的 参数 的 第 1 项 为 p1。
    seg 的 参数 的 第 2 项 为 p2。
    seg 的 参数 的 第 3 项 为 p3。
    seg 的 参数 的 第 4 项 为 p4。
    seg 的 参数 的 第 5 项 为 p5。
    seg 的 参数 的 第 6 项 为 p6。
    seg 的 下一个 为 0。
    若 path 的 段链尾 不等于 0：
        path 的 段链尾 的 下一个 为 seg。
    否则：
        path 的 段链头 为 seg。
    完。
    path 的 段链尾 为 seg。
    path 的 段数 为 path 的 段数 加 1。
完。

可变 SVG_总解析数 为 0。

可变 SVG_总段数 为 0。

可变 SVG_总平坦化线段 为 0。

可变 SVG_总弧线转换 为 0。

SVG_跳过空白 接受 数据、位置：
    只要 位置 小于 数据 的 长度：
        ch 为 数据 的 第 位置 项。
        若 ch 等于 32 或者 ch 等于 9 或者 ch 等于 10 或者 ch 等于 13 或者 ch 等于 44：
            位置 为 位置 加 1。
        否则：
            返还 位置。
        完。
    完。
    返还 位置。
完。

SVG_解析数字 接受 数据、位置：
    可变。
    pos 为 做 SVG_跳过空白 用 数据、位置。
    可变。
    符号 为 1。
    若 pos 小于 数据 的 长度 并且 数据 的 第 pos 项 等于 45：
        符号 为 负 1。
        pos 为 pos 加 1。
    否则若 pos 小于 数据 的 长度 并且 数据 的 第 pos 项 等于 43：
        pos 为 pos 加 1。
    完。
    可变。
    整数部 为 0。
    只要 pos 小于 数据 的 长度 并且 数据 的 第 pos 项 大于或等于 48 并且 数据 的 第 pos 项 小于或等于 57：
        整数部 为 整数部 乘 10 加 (数据 的 第 pos 项 减 48)。
        pos 为 pos 加 1。
    完。
    可变。
    小数部 为 0。
    可变。
    小数位 为 0。
    若 pos 小于 数据 的 长度 并且 数据 的 第 pos 项 等于 46：
        pos 为 pos 加 1。
        只要 pos 小于 数据 的 长度 并且 数据 的 第 pos 项 大于或等于 48 并且 数据 的 第 pos 项 小于或等于 57：
            小数部 为 小数部 乘 10 加 (数据 的 第 pos 项 减 48)。
            小数位 为 小数位 加 1。
            pos 为 pos 加 1。
        完。
    完。
    可变。
    分母 为 1。
    可变。
    j 为 0。
    只要 j 小于 小数位：
        分母 为 分母 乘 10。
        j 为 j 加 1。
    完。
    值 为 符号 乘 (整数部 乘 1024 加 小数部 乘 1024 除以 分母)。
    返还 一组：值、pos。
完。

SVG_是命令字符 接受 ch：
    返还 ch 等于 SVG命令_M 或者 ch 等于 SVG命令_m 或者 ch 等于 SVG命令_L 或者 ch 等于 SVG命令_l 或者 ch 等于 SVG命令_H 或者 ch 等于 SVG命令_h 或者 ch 等于 SVG命令_V 或者 ch 等于 SVG命令_v 或者 ch 等于 SVG命令_C 或者 ch 等于 SVG命令_c 或者 ch 等于 SVG命令_S 或者 ch 等于 SVG命令_s 或者 ch 等于 SVG命令_Q 或者 ch 等于 SVG命令_q 或者 ch 等于 SVG命令_T 或者 ch 等于 SVG命令_t 或者 ch 等于 SVG命令_A 或者 ch 等于 SVG命令_a 或者 ch 等于 SVG命令_Z 或者 ch 等于 SVG命令_z。
完。

SVG路径_解析 接受 数据：
    path 为 做 SVGPath_创建。
    可变。
    pos 为 0。
    可变。
    当前X 为 0。
    可变。
    当前Y 为 0。
    可变。
    起点X 为 0。
    可变。
    起点Y 为 0。
    SVG_总解析数 为 SVG_总解析数 加 1。
    只要 pos 小于 数据 的 长度：
        pos 为 做 SVG_跳过空白 用 数据、pos。
        若 pos 大于或等于 数据 的 长度：
            跳出。
        完。
        cmd 为 数据 的 第 pos 项。
        若 非 做 SVG_是命令字符 用 cmd：
            pos 为 pos 加 1。
            继续。
        完。
        pos 为 pos 加 1。
        若 cmd 等于 SVG命令_M 或者 cmd 等于 SVG命令_m：
            r1 为 做 SVG_解析数字 用 数据、pos。
            r2 为 做 SVG_解析数字 用 数据、r1 的 第 1 项。
            若 cmd 等于 SVG命令_m：
                当前X 为 当前X 加 r1 的 第 0 项。
                当前Y 为 当前Y 加 r2 的 第 0 项。
            否则：
                当前X 为 r1 的 第 0 项。
                当前Y 为 r2 的 第 0 项。
            完。
            起点X 为 当前X。
            起点Y 为 当前Y。
            做 SVGPath_添加段 用 path、cmd、当前X、当前Y、0、0、0、0、0。
            pos 为 r2 的 第 1 项。
        否则若 cmd 等于 SVG命令_L 或者 cmd 等于 SVG命令_l：
            r1 为 做 SVG_解析数字 用 数据、pos。
            r2 为 做 SVG_解析数字 用 数据、r1 的 第 1 项。
            若 cmd 等于 SVG命令_l：
                当前X 为 当前X 加 r1 的 第 0 项。
                当前Y 为 当前Y 加 r2 的 第 0 项。
            否则：
                当前X 为 r1 的 第 0 项。
                当前Y 为 r2 的 第 0 项。
            完。
            做 SVGPath_添加段 用 path、cmd、当前X、当前Y、0、0、0、0、0。
            pos 为 r2 的 第 1 项。
        否则若 cmd 等于 SVG命令_C 或者 cmd 等于 SVG命令_c：
            r1 为 做 SVG_解析数字 用 数据、pos。
            r2 为 做 SVG_解析数字 用 数据、r1 的 第 1 项。
            r3 为 做 SVG_解析数字 用 数据、r2 的 第 1 项。
            r4 为 做 SVG_解析数字 用 数据、r3 的 第 1 项。
            r5 为 做 SVG_解析数字 用 数据、r4 的 第 1 项。
            r6 为 做 SVG_解析数字 用 数据、r5 的 第 1 项。
            做 SVGPath_添加段 用 path、cmd、r1 的 第 0 项、r2 的 第 0 项、r3 的 第 0 项、r4 的 第 0 项、r5 的 第 0 项、r6 的 第 0 项、0。
            若 cmd 等于 SVG命令_c：
                当前X 为 当前X 加 r5 的 第 0 项。
                当前Y 为 当前Y 加 r6 的 第 0 项。
            否则：
                当前X 为 r5 的 第 0 项。
                当前Y 为 r6 的 第 0 项。
            完。
            pos 为 r6 的 第 1 项。
        否则若 cmd 等于 SVG命令_Q 或者 cmd 等于 SVG命令_q：
            r1 为 做 SVG_解析数字 用 数据、pos。
            r2 为 做 SVG_解析数字 用 数据、r1 的 第 1 项。
            r3 为 做 SVG_解析数字 用 数据、r2 的 第 1 项。
            r4 为 做 SVG_解析数字 用 数据、r3 的 第 1 项。
            做 SVGPath_添加段 用 path、cmd、r1 的 第 0 项、r2 的 第 0 项、r3 的 第 0 项、r4 的 第 0 项、0、0、0。
            若 cmd 等于 SVG命令_q：
                当前X 为 当前X 加 r3 的 第 0 项。
                当前Y 为 当前Y 加 r4 的 第 0 项。
            否则：
                当前X 为 r3 的 第 0 项。
                当前Y 为 r4 的 第 0 项。
            完。
            pos 为 r4 的 第 1 项。
        否则若 cmd 等于 SVG命令_Z 或者 cmd 等于 SVG命令_z：
            当前X 为 起点X。
            当前Y 为 起点Y。
            做 SVGPath_添加段 用 path、SVG命令_Z、0、0、0、0、0、0、0。
        否则：
            pos 为 pos 加 1。
        完。
        SVG_总段数 为 SVG_总段数 加 1。
    完。
    返还 path。
完。

三次贝塞尔_求值 接受 p0、p1、p2、p3、t_q10：
    t 为 t_q10。
    mt 为 1024 减 t。
    mt2 为 mt 乘 mt 除以 1024。
    mt3 为 mt2 乘 mt 除以 1024。
    t2 为 t 乘 t 除以 1024。
    t3 为 t2 乘 t 除以 1024。
    返还 (mt3 乘 p0 加 3 乘 mt2 乘 t 除以 1024 乘 p1 加 3 乘 mt 乘 t2 除以 1024 乘 p2 加 t3 乘 p3) 除以 1024。
完。

二次贝塞尔_求值 接受 p0、p1、p2、t_q10：
    t 为 t_q10。
    mt 为 1024 减 t。
    mt2 为 mt 乘 mt 除以 1024。
    t2 为 t 乘 t 除以 1024。
    返还 (mt2 乘 p0 加 2 乘 mt 乘 t 除以 1024 乘 p1 加 t2 乘 p2) 除以 1024。
完。

平坦化阈值 为 10。

最大递归深度 为 12。

三次贝塞尔_平坦化 接受 x0、y0、x1、y1、x2、y2、x3、y3、线段列表、深度：
    若 深度 大于 最大递归深度：
        线段列表 为 线段列表 加 一组：一组：x0、y0、x3、y3。
        SVG_总平坦化线段 为 SVG_总平坦化线段 加 1。
        返还 线段列表。
    完。
    dx 为 x3 减 x0。
    dy 为 y3 减 y0。
    d1x 为 x1 减 x0。
    d1y 为 y1 减 y0。
    d2x 为 x2 减 x0。
    d2y 为 y2 减 y0。
    cross1 为 d1x 乘 dy 除以 1024 减 d1y 乘 dx 除以 1024。
    cross2 为 d2x 乘 dy 除以 1024 减 d2y 乘 dx 除以 1024。
    若 cross1 小于 0：
        cross1 为 负 cross1。
    完。
    若 cross2 小于 0：
        cross2 为 负 cross2。
    完。
    平坦度 为 cross1。
    若 cross2 大于 平坦度：
        平坦度 为 cross2。
    完。
    若 平坦度 小于 平坦化阈值：
        线段列表 为 线段列表 加 一组：一组：x0、y0、x3、y3。
        SVG_总平坦化线段 为 SVG_总平坦化线段 加 1。
        返还 线段列表。
    完。
    mx01 为 (x0 加 x1) 除以 2。
    my01 为 (y0 加 y1) 除以 2。
    mx12 为 (x1 加 x2) 除以 2。
    my12 为 (y1 加 y2) 除以 2。
    mx23 为 (x2 加 x3) 除以 2。
    my23 为 (y2 加 y3) 除以 2。
    mx012 为 (mx01 加 mx12) 除以 2。
    my012 为 (my01 加 my12) 除以 2。
    mx123 为 (mx12 加 mx23) 除以 2。
    my123 为 (my12 加 my23) 除以 2。
    mx 为 (mx012 加 mx123) 除以 2。
    my 为 (my012 加 my123) 除以 2。
    线段列表 为 做 三次贝塞尔_平坦化 用 x0、y0、mx01、my01、mx012、my012、mx、my、线段列表、深度 加 1。
    线段列表 为 做 三次贝塞尔_平坦化 用 mx、my、mx123、my123、mx23、my23、x3、y3、线段列表、深度 加 1。
    返还 线段列表。
完。

SVGPath_计算边界框 接受 path：
    若 path 等于 空 或者 path 的 段链头 等于 0：
        返还。
    完。
    可变。
    minX 为 0x7FFFFFFF。
    可变。
    minY 为 0x7FFFFFFF。
    可变。
    maxX 为 负 0x7FFFFFFF。
    可变。
    maxY 为 负 0x7FFFFFFF。
    可变。
    seg 为 path 的 段链头。
    只要 seg 不等于 0：
        可变。
        i 为 0。
        只要 i 小于 2：
            x 为 seg 的 参数 的 第 i 乘 2 项。
            y 为 seg 的 参数 的 第 i 乘 2 加 1 项。
            若 x 小于 minX：
                minX 为 x。
            完。
            若 x 大于 maxX：
                maxX 为 x。
            完。
            若 y 小于 minY：
                minY 为 y。
            完。
            若 y 大于 maxY：
                maxY 为 y。
            完。
            i 为 i 加 1。
        完。
        seg 为 seg 的 下一个。
    完。
    path 的 bbox_x 为 minX。
    path 的 bbox_y 为 minY。
    path 的 bbox_w 为 maxX 减 minX。
    path 的 bbox_h 为 maxY 减 minY。
完。

AI_SVG复杂度评估 接受 path：
    若 path 等于 空：
        返还 0。
    完。
    可变。
    曲线数 为 0。
    可变。
    直线数 为 0。
    可变。
    seg 为 path 的 段链头。
    只要 seg 不等于 0：
        c 为 seg 的 命令。
        若 c 等于 SVG命令_C 或者 c 等于 SVG命令_c 或者 c 等于 SVG命令_S 或者 c 等于 SVG命令_s 或者 c 等于 SVG命令_Q 或者 c 等于 SVG命令_q 或者 c 等于 SVG命令_T 或者 c 等于 SVG命令_t 或者 c 等于 SVG命令_A 或者 c 等于 SVG命令_a：
            曲线数 为 曲线数 加 1。
        否则：
            直线数 为 直线数 加 1。
        完。
        seg 为 seg 的 下一个。
    完。
    特征 为 做 Tensor_创建 用 1、4。
    做 Tensor_写 用 特征、0、0、path 的 段数 乘 1.0。
    做 Tensor_写 用 特征、0、1、曲线数 乘 1.0。
    做 Tensor_写 用 特征、0、2、直线数 乘 1.0。
    做 Tensor_写 用 特征、0、3、path 的 bbox_w 乘 path 的 bbox_h 乘 1.0 除以 1048576。
    结果 为 做 AI总线_推理 用 「svg_complexity」、特征。
    可变。
    复杂度 为 0.0。
    若 结果 不等于 空：
        复杂度 为 做 Tensor_读 用 结果、0、0。
        做 Tensor_释放 用 结果。
    完。
    做 Tensor_释放 用 特征。
    返还 复杂度。
完。

NSVG_诊断报告 接受 零参：
    可变。
    报告 为 「[NSVG诊断报告]\n」。
    报告 为 报告 加 「总解析数: 」 加 SVG_总解析数 加 「\n」。
    报告 为 报告 加 「总段数: 」 加 SVG_总段数 加 「\n」。
    报告 为 报告 加 「平坦化线段: 」 加 SVG_总平坦化线段 加 「\n」。
    报告 为 报告 加 「弧线转换: 」 加 SVG_总弧线转换 加 「\n」。
    返还 报告。
完。

弧线_转贝塞尔 接受 x0、y0、rx、ry、旋转_q10、大弧、顺时针、x1、y1、path：
    SVG_总弧线转换 为 SVG_总弧线转换 加 1。
    若 rx 小于或等于 0 或者 ry 小于或等于 0：
        做 SVGPath_添加段 用 path、SVG命令_L、x1、y1、0、0、0、0、0。
        返还。
    完。
    mx 为 (x0 加 x1) 除以 2。
    my 为 (y0 加 y1) 除以 2。
    dx 为 (x1 减 x0) 除以 2。
    dy 为 (y1 减 y0) 除以 2。
    kappa 为 552。
    cx1 为 x0 加 dy 乘 kappa 除以 1024。
    cy1 为 y0 减 dx 乘 kappa 除以 1024。
    cx2 为 x1 减 dy 乘 kappa 除以 1024。
    cy2 为 y1 加 dx 乘 kappa 除以 1024。
    做 SVGPath_添加段 用 path、SVG命令_C、cx1、cy1、cx2、cy2、x1、y1、0。
完。

SVG变换 是 一种 事物，含 a、b、c、d、e、f。

SVG变换_单位 接受 零参：
    返还 新建 SVG变换：a 为 1024、b 为 0、c 为 0、d 为 1024、e 为 0、f 为 0。
完。

SVG变换_平移 接受 tx、ty：
    返还 新建 SVG变换：a 为 1024、b 为 0、c 为 0、d 为 1024、e 为 tx、f 为 ty。
完。

SVG变换_缩放 接受 sx、sy：
    返还 新建 SVG变换：a 为 sx、b 为 0、c 为 0、d 为 sy、e 为 0、f 为 0。
完。

SVG变换_应用 接受 变换、x_q10、y_q10：
    nx 为 (变换 的 a 乘 x_q10 加 变换 的 c 乘 y_q10) 除以 1024 加 变换 的 e。
    ny 为 (变换 的 b 乘 x_q10 加 变换 的 d 乘 y_q10) 除以 1024 加 变换 的 f。
    返还 一组：nx、ny。
完。

SVG变换_乘 接受 甲、乙：
    返还 新建 SVG变换：a 为 (甲 的 a 乘 乙 的 a 加 甲 的 c 乘 乙 的 b) 除以 1024、b 为 (甲 的 b 乘 乙 的 a 加 甲 的 d 乘 乙 的 b) 除以 1024、c 为 (甲 的 a 乘 乙 的 c 加 甲 的 c 乘 乙 的 d) 除以 1024、d 为 (甲 的 b 乘 乙 的 c 加 甲 的 d 乘 乙 的 d) 除以 1024、e 为 (甲 的 a 乘 乙 的 e 加 甲 的 c 乘 乙 的 f) 除以 1024 加 甲 的 e、f 为 (甲 的 b 乘 乙 的 e 加 甲 的 d 乘 乙 的 f) 除以 1024 加 甲 的 f。
完。

NSVG_AI诊断 接受 零参：
    特征 为 做 Tensor_创建 用 1、4。
    做 Tensor_写 用 特征、0、0、SVG_总解析数 乘 1.0。
    做 Tensor_写 用 特征、0、1、SVG_总段数 乘 1.0。
    做 Tensor_写 用 特征、0、2、SVG_总平坦化线段 乘 1.0。
    做 Tensor_写 用 特征、0、3、SVG_总弧线转换 乘 1.0。
    结果 为 做 AI总线_推理 用 「nsvg_health」、特征。
    若 结果 不等于 空：
        做 Tensor_释放 用 结果。
    完。
    做 Tensor_释放 用 特征。
完。

SVGPath_释放 接受 path：
    若 path 等于 空：
        返还。
    完。
    可变。
    seg 为 path 的 段链头。
    只要 seg 不等于 0：
        下 为 seg 的 下一个。
        做 堆_释放 用 seg。
        seg 为 下。
    完。
    做 堆_释放 用 path。
完。

本章 收束。
