《文字探测字》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《.·文字绘制最》。

引用《..·中枢·内存初始化》。

引用《.·字符字形标》。

引用《..·执行·命令写入》。

引用《.·光栅化》。

引用《..·执行·着色器管理》。

引用《.·字映射字符》。

ShapedGlyph 是 一种 事物，含 字形ID、x偏移、y偏移、x推进。

ShapedRun 是 一种 事物，含 字形、字形数、总宽度。

字体_shape文字 接受 font、文字、长度、字号：
    若 font 等于 0 或者 文字 等于 0 或者 长度 等于 0：
        返还 0。
    完。
    缩放 为 字号 乘 1024 除以 font 的 unitsPerEm。
    run 为 做 堆_分配 用 ShapedRun。
    run 的 字形 为 做 堆_分配_数组 用 长度 乘 8。
    run 的 字形数 为 0。
    run 的 总宽度 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        unicode 为 做 字体_读UTF8 用 文字、i、长度。
        字节数 为 做 字体_UTF8字节数 用 文字、i。
        若 unicode 等于 0：
            i 为 i 加 1。
            继续。
        完。
        glyphID 为 做 字体_字符到字形 用 font、unicode。
        metrics 为 做 字体_字形度量 用 font、glyphID。
        推进 为 若 metrics 不等于 0 则 metrics 的 advanceWidth 乘 缩放 除以 1024 否则 字号 除以 2。
        偏移 为 run 的 字形 加 run 的 字形数 乘 8。
        做 内存_写16 用 偏移、glyphID。
        做 内存_写16 用 偏移 加 2、0。
        做 内存_写16 用 偏移 加 4、0。
        做 内存_写16 用 偏移 加 6、推进。
        run 的 总宽度 为 run 的 总宽度 加 推进。
        run 的 字形数 为 run 的 字形数 加 1。
        i 为 i 加 字节数。
    完。
    返还 run。
完。

Glyph缓存 是 一种 事物，含 图集、条目、条目数、容量、图处上下文。

Glyph缓存_获取或光栅 接受 缓存、font、glyphID、字号：
    若 缓存 等于 0：
        返还 0。
    完。
    键 为 字号 左移 16 位或 glyphID。
    可变 i 为 0。
    只要 i 小于 缓存 的 条目数：
        若 做 内存_读32 用 缓存 的 条目 加 i 乘 12 等于 键：
            返还 缓存 的 条目 加 i 乘 12 加 4。
        完。
        i 为 i 加 1。
    完。
    bmp 为 做 字体_光栅化字形 用 font、glyphID、字号。
    若 bmp 等于 0：
        返还 0。
    完。
    区域idx 为 做 纹理图集_分配 用 缓存 的 图集、bmp 的 宽、bmp 的 高。
    若 区域idx 大于或等于 0：
        做 图处_上传纹理 用 缓存 的 图处上下文、缓存 的 图集 的 纹理ID、bmp 的 像素、bmp 的 宽 乘 bmp 的 高。
        若 缓存 的 条目数 小于 缓存 的 容量：
            偏移 为 缓存 的 条目 加 缓存 的 条目数 乘 12。
            做 内存_写32 用 偏移、键。
            做 内存_写32 用 偏移 加 4、区域idx。
            做 内存_写16 用 偏移 加 8、bmp 的 宽。
            做 内存_写16 用 偏移 加 10、bmp 的 高。
            缓存 的 条目数 为 缓存 的 条目数 加 1。
        完。
    完。
    返还 bmp。
完。

本章 收束。
