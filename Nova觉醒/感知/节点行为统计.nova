《节点行为统计》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·文档节点》。

引用《.·文档方法节》。

节点_AI记录点击_基础 接受 节点：
    若 节点 等于 0：
        返还。
    完。
    节点 的 点击次数 为 节点 的 点击次数 加 1。
    若 节点 的 父节点 不等于 0：
        节点 的 父节点 的 点击次数 为 节点 的 父节点 的 点击次数 加 1。
    完。
完。

节点_AI记录悬停_基础 接受 节点、时长：
    若 节点 等于 0：
        返还。
    完。
    节点 的 悬停总时长 为 节点 的 悬停总时长 加 时长。
完。

节点_AI记录滚动经过 接受 节点：
    若 节点 等于 0：
        返还。
    完。
    节点 的 滚动经过次数 为 节点 的 滚动经过次数 加 1。
    若 节点 的 滚动经过次数 大于 10 并且 节点 的 点击次数 等于 0：
        节点 的 注意力权重 为 节点 的 注意力权重 除以 2。
    完。
完。

节点_节点Type 接受 节点：
    返还 节点 的 节点类型。
完。

节点_parent节点 接受 节点：
    返还 节点 的 父节点。
完。

节点_parent元素 接受 节点：
    父 为 节点 的 父节点。
    若 父 不等于 0 并且 父 的 节点标志 位与 节点标志_是元素：
        返还 父。
    完。
    返还 0。
完。

节点_firstChild 接受 节点：
    返还 节点 的 第一子节点。
完。

节点_lastChild 接受 节点：
    返还 节点 的 最后子节点。
完。

节点_previousSibling 接受 节点：
    返还 节点 的 前一兄弟。
完。

节点_nextSibling 接受 节点：
    返还 节点 的 后一兄弟。
完。

节点_child节点s_length 接受 节点：
    返还 节点 的 子节点数。
完。

节点_ownerDocument 接受 节点：
    若 节点 的 节点标志 位与 节点标志_是文档：
        返还 0。
    完。
    返还 节点 的 树作用域。
完。

节点_isConnected 接受 节点：
    返还 节点 的 节点标志 位与 节点标志_已连接 不等于 0。
完。

节点_hasChild节点s 接受 节点：
    返还 节点 的 第一子节点 不等于 0。
完。

节点_appendChild 接受 父节点、新子：
    若 父节点 等于 0 或者 新子 等于 0：
        返还 0。
    完。
    若 非 (父节点 的 节点标志 位与 节点标志_是容器节点)：
        返还 0。
    完。
    若 做 节点_是祖先 用 新子、父节点：
        返还 0。
    完。
    若 新子 的 父节点 不等于 0：
        做 节点_removeChild 用 新子 的 父节点、新子。
    完。
    新子 的 父节点 为 父节点。
    新子 的 前一兄弟 为 父节点 的 最后子节点。
    新子 的 后一兄弟 为 0。
    若 父节点 的 最后子节点 不等于 0：
        父节点 的 最后子节点 的 后一兄弟 为 新子。
    否则：
        父节点 的 第一子节点 为 新子。
    完。
    父节点 的 最后子节点 为 新子。
    父节点 的 子节点数 为 父节点 的 子节点数 加 1。
    做 节点_增引用 用 新子。
    若 父节点 的 树作用域 不等于 0：
        做 节点_设置树作用域递归 用 新子、父节点 的 树作用域。
    完。
    若 父节点 的 节点标志 位与 节点标志_已连接：
        做 节点_设置已连接递归 用 新子。
    完。
    做 节点_标记子树样式脏 用 父节点。
    做 节点_子节点变更通知 用 父节点、新子、0。
    返还 新子。
完。

节点_insertBefore 接受 父节点、新子、参考子：
    若 父节点 等于 0 或者 新子 等于 0：
        返还 0。
    完。
    若 非 (父节点 的 节点标志 位与 节点标志_是容器节点)：
        返还 0。
    完。
    若 参考子 等于 0：
        返还 做 节点_appendChild 用 父节点、新子。
    完。
    若 参考子 的 父节点 不等于 父节点：
        返还 0。
    完。
    若 做 节点_是祖先 用 新子、父节点：
        返还 0。
    完。
    若 新子 的 父节点 不等于 0：
        做 节点_removeChild 用 新子 的 父节点、新子。
    完。
    新子 的 父节点 为 父节点。
    新子 的 后一兄弟 为 参考子。
    新子 的 前一兄弟 为 参考子 的 前一兄弟。
    若 参考子 的 前一兄弟 不等于 0：
        参考子 的 前一兄弟 的 后一兄弟 为 新子。
    否则：
        父节点 的 第一子节点 为 新子。
    完。
    参考子 的 前一兄弟 为 新子。
    父节点 的 子节点数 为 父节点 的 子节点数 加 1。
    做 节点_增引用 用 新子。
    若 父节点 的 树作用域 不等于 0：
        做 节点_设置树作用域递归 用 新子、父节点 的 树作用域。
    完。
    若 父节点 的 节点标志 位与 节点标志_已连接：
        做 节点_设置已连接递归 用 新子。
    完。
    做 节点_标记子树样式脏 用 父节点。
    做 节点_子节点变更通知 用 父节点、新子、0。
    返还 新子。
完。

节点_removeChild 接受 父节点、旧子：
    若 父节点 等于 0 或者 旧子 等于 0：
        返还 0。
    完。
    若 旧子 的 父节点 不等于 父节点：
        返还 0。
    完。
    做 节点_子节点变更通知 用 父节点、0、旧子。
    若 旧子 的 前一兄弟 不等于 0：
        旧子 的 前一兄弟 的 后一兄弟 为 旧子 的 后一兄弟。
    否则：
        父节点 的 第一子节点 为 旧子 的 后一兄弟。
    完。
    若 旧子 的 后一兄弟 不等于 0：
        旧子 的 后一兄弟 的 前一兄弟 为 旧子 的 前一兄弟。
    否则：
        父节点 的 最后子节点 为 旧子 的 前一兄弟。
    完。
    旧子 的 父节点 为 0。
    旧子 的 前一兄弟 为 0。
    旧子 的 后一兄弟 为 0。
    父节点 的 子节点数 为 父节点 的 子节点数 减 1。
    若 旧子 的 节点标志 位与 节点标志_已连接：
        做 节点_清除已连接递归 用 旧子。
    完。
    若 旧子 的 节点标志 位与 节点标志_有渲染对象：
        做 节点_分离布局树递归 用 旧子。
    完。
    做 节点_标记子树样式脏 用 父节点。
    做 节点_减引用 用 旧子。
    返还 旧子。
完。

本章 收束。
