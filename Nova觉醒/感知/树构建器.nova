《树构建器》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·标准·原生字符串》。

引用《..·执行·底层内存操》。

引用《.·原子驻留》。

引用《.·样式解析》。

引用《.·核心状态机》。

引用《..·推理·AI鼠标事件处理》。

引用《.·文档节点》。

引用《.·requestAnimationFrame》。

引用《.·元素栈创建》。

引用《.·文档接口属》。

引用《..·中枢·整数转字符串》。

引用《.·HTMLTreeBuilder格式化列表》。

引用《.·节点行为统计》。

引用《.·文档创建》。

引用《..·推理·AI总线资源调度》。

引用《.·算法》。

引用《.·文档查询接口》。

树构摘要 是 一种 事物，含 模式、模式名、开放数、格式数、建元素数、建文本数、有头、有表单、帧集可用。

HTMLDocumentParser 是 一种 事物，含 令牌izer、tree_builder、文档、已完成。

树构_模式名 接受 模式：
    若 模式 等于 插入模式_初始：
        返还 「初始」。
    完。
    若 模式 等于 插入模式_DOCTYPE前：
        返还 「DOCTYPE前」。
    完。
    若 模式 等于 插入模式_html前：
        返还 「html前」。
    完。
    若 模式 等于 插入模式_head前：
        返还 「head前」。
    完。
    若 模式 等于 插入模式_head中：
        返还 「head中」。
    完。
    若 模式 等于 插入模式_head后：
        返还 「head后」。
    完。
    若 模式 等于 插入模式_body中：
        返还 「body中」。
    完。
    若 模式 等于 插入模式_文本：
        返还 「文本」。
    完。
    若 模式 等于 插入模式_body后：
        返还 「body后」。
    完。
    若 模式 等于 插入模式_html后：
        返还 「html后」。
    完。
    若 模式 等于 插入模式_table中：
        返还 「表中」。
    完。
    若 模式 等于 插入模式_行中：
        返还 「行中」。
    完。
    若 模式 等于 插入模式_单元格中：
        返还 「格中」。
    完。
    若 模式 等于 插入模式_select中：
        返还 「选中」。
    完。
    返还 「未知」。
完。

树构_是标题元素 接受 标签原子：
    若 标签原子 等于 原子_h1：
        返还 1。
    完。
    若 标签原子 等于 原子_h2：
        返还 1。
    完。
    若 标签原子 等于 原子_h3：
        返还 1。
    完。
    若 标签原子 等于 原子_h4：
        返还 1。
    完。
    若 标签原子 等于 原子_h5：
        返还 1。
    完。
    若 标签原子 等于 原子_h6：
        返还 1。
    完。
    返还 0。
完。

树构_当前节点 接受 tb：
    若 tb 等于 0：
        返还 0。
    完。
    返还 做 元素栈_顶部 用 tb 的 开放元素栈。
完。

树构_关闭到标签 接受 tb、标签原子：
    若 做 元素栈_包含标签 用 tb 的 开放元素栈、标签原子：
        做 元素栈_弹出直到含 用 tb 的 开放元素栈、标签原子。
        返还 1。
    完。
    返还 0。
完。

树构_摘要 接受 tb：
    摘 为 做 堆_分配 用 树构摘要。
    摘 的 模式 为 tb 的 插入模式。
    摘 的 模式名 为 做 树构_模式名 用 tb 的 插入模式。
    摘 的 开放数 为 tb 的 开放元素栈 的 数量。
    摘 的 格式数 为 tb 的 格式化列表 的 数量。
    摘 的 建元素数 为 tb 的 创建元素数。
    摘 的 建文本数 为 tb 的 创建文本数。
    摘 的 有头 为 tb 的 head指针 不等于 0。
    摘 的 有表单 为 tb 的 form指针 不等于 0。
    摘 的 帧集可用 为 tb 的 frameset_ok。
    返还 摘。
完。

树构_摘要串 接受 tb：
    摘 为 做 树构_摘要 用 tb。
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"模式\":\"」。
    做 字符串构建器_追加 用 建、摘 的 模式名。
    做 字符串构建器_追加 用 建、「\",\"开放数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 开放数。
    做 字符串构建器_追加 用 建、「,\"格式数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 格式数。
    做 字符串构建器_追加 用 建、「,\"建元素数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 建元素数。
    做 字符串构建器_追加 用 建、「,\"建文本数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 建文本数。
    做 字符串构建器_追加 用 建、「,\"有头\":」。
    若 摘 的 有头：
        做 字符串构建器_追加 用 建、「true」。
    否则：
        做 字符串构建器_追加 用 建、「false」。
    完。
    做 字符串构建器_追加 用 建、「,\"有表单\":」。
    若 摘 的 有表单：
        做 字符串构建器_追加 用 建、「true」。
    否则：
        做 字符串构建器_追加 用 建、「false」。
    完。
    做 字符串构建器_追加 用 建、「,\"帧集可用\":」。
    若 摘 的 帧集可用：
        做 字符串构建器_追加 用 建、「true」。
    否则：
        做 字符串构建器_追加 用 建、「false」。
    完。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

树构_自检 接受 tb：
    可变 错数 为 0。
    若 tb 等于 0：
        返还 1。
    完。
    若 tb 的 开放元素栈 等于 0：
        错数 为 错数 加 1。
    完。
    若 tb 的 格式化列表 等于 0：
        错数 为 错数 加 1。
    完。
    若 tb 的 文档 等于 0：
        错数 为 错数 加 1。
    完。
    若 tb 的 插入模式 等于 插入模式_head中 并且 tb 的 head指针 等于 0：
        错数 为 错数 加 1。
    完。
    返还 错数。
完。

HTMLTreeBuilder_创建元素并插入 接受 tb、令牌、指定父：
    元素 为 做 Document_create元素 用 tb 的 文档、令牌 的 标签名。
    tb 的 创建元素数 为 tb 的 创建元素数 加 1。
    可变 i 为 0。
    只要 i 小于 令牌 的 属性数量：
        偏移 为 令牌 的 属性列表 加 i 乘 40。
        名 为 做 内存_读64 用 偏移。
        值 为 做 内存_读64 用 偏移 加 16。
        做 元素_setAttribute 用 元素、名、值。
        i 为 i 加 1。
    完。
    父 为 指定父。
    若 父 等于 0：
        父 为 做 树构_当前节点 用 tb。
    完。
    若 父 不等于 0：
        做 节点_appendChild 用 父、元素。
    完。
    做 智能_HTMLTreeBuilder_AI节点语义 用 元素。
    返还 元素。
完。

HTMLTreeBuilder_插入文本 接受 tb、数据、长度：
    若 长度 等于 0：
        返还。
    完。
    当前 为 做 树构_当前节点 用 tb。
    若 当前 等于 0：
        返还。
    完。
    最后子 为 当前 的 最后子节点。
    若 最后子 不等于 0 并且 最后子 的 节点类型 等于 TEXT_节点：
        文本 为 做 字符串_从缓冲 用 数据、长度。
        做 CharacterData_appendData 用 最后子、文本。
    否则：
        文本 为 做 字符串_从缓冲 用 数据、长度。
        文本节点 为 做 Document_createText节点 用 tb 的 文档、文本。
        做 节点_appendChild 用 当前、文本节点。
        tb 的 创建文本数 为 tb 的 创建文本数 加 1。
    完。
完。

HTMLTreeBuilder_关闭P元素 接受 tb：
    做 树构_关闭到标签 用 tb、原子_p。
完。

元素_是空元素_原子 接受 标签原子：
    若 标签原子 等于 原子_br：
        返还 1。
    完。
    若 标签原子 等于 原子_hr：
        返还 1。
    完。
    若 标签原子 等于 原子_img：
        返还 1。
    完。
    若 标签原子 等于 原子_input：
        返还 1。
    完。
    若 标签原子 等于 原子_link：
        返还 1。
    完。
    若 标签原子 等于 原子_meta：
        返还 1。
    完。
    若 标签原子 等于 原子_area：
        返还 1。
    完。
    若 标签原子 等于 原子_base：
        返还 1。
    完。
    若 标签原子 等于 原子_col：
        返还 1。
    完。
    若 标签原子 等于 原子_embed：
        返还 1。
    完。
    若 标签原子 等于 原子_param：
        返还 1。
    完。
    若 标签原子 等于 原子_source：
        返还 1。
    完。
    若 标签原子 等于 原子_track：
        返还 1。
    完。
    若 标签原子 等于 原子_wbr：
        返还 1。
    完。
    返还 0。
完。

是格式化元素 接受 标签原子：
    若 标签原子 等于 原子_b：
        返还 1。
    完。
    若 标签原子 等于 原子_i：
        返还 1。
    完。
    若 标签原子 等于 原子_em：
        返还 1。
    完。
    若 标签原子 等于 原子_strong：
        返还 1。
    完。
    若 标签原子 等于 原子_u：
        返还 1。
    完。
    若 标签原子 等于 原子_s：
        返还 1。
    完。
    若 标签原子 等于 原子_small：
        返还 1。
    完。
    若 标签原子 等于 原子_code：
        返还 1。
    完。
    若 标签原子 等于 原子_mark：
        返还 1。
    完。
    若 标签原子 等于 原子_a：
        返还 1。
    完。
    若 标签原子 等于 原子_font：
        返还 1。
    完。
    若 标签原子 等于 原子_big：
        返还 1。
    完。
    若 标签原子 等于 原子_strike：
        返还 1。
    完。
    若 标签原子 等于 原子_tt：
        返还 1。
    完。
    返还 0。
完。

是特殊元素 接受 标签原子：
    若 标签原子 等于 原子_html：
        返还 1。
    完。
    若 标签原子 等于 原子_table：
        返还 1。
    完。
    若 标签原子 等于 原子_td：
        返还 1。
    完。
    若 标签原子 等于 原子_th：
        返还 1。
    完。
    若 标签原子 等于 原子_caption：
        返还 1。
    完。
    若 标签原子 等于 原子_select：
        返还 1。
    完。
    若 标签原子 等于 原子_template：
        返还 1。
    完。
    返还 0。
完。

元素栈_在作用域中 接受 栈、标签原子：
    可变 i 为 栈 的 数量。
    只要 i 大于 0：
        i 为 i 减 1。
        元素 为 做 内存_读64 用 栈 的 元素列表 加 i 乘 8。
        扩展 为 做 元素_获取扩展 用 元素。
        若 扩展 不等于 0：
            若 扩展 的 标签名原子 等于 标签原子：
                返还 1。
            完。
            若 做 是特殊元素 用 扩展 的 标签名原子：
                返还 0。
            完。
        完。
    完。
    返还 0。
完。

HTMLTreeBuilder_重建格式化元素 接受 tb：
    格式列表 为 tb 的 格式化列表。
    若 格式列表 的 数量 等于 0：
        返还。
    完。
    可变 start 为 格式列表 的 数量 减 1。
    只要 start 大于 0：
        元 为 做 内存_读64 用 格式列表 的 元素 加 start 乘 8。
        若 元 等于 格式化标记_标记：
            start 为 start 加 1。
            跳出。
        完。
        start 为 start 减 1。
    完。
    可变 i 为 start。
    只要 i 小于 格式列表 的 数量：
        元 为 做 内存_读64 用 格式列表 的 元素 加 i 乘 8。
        若 元 不等于 格式化标记_标记：
            扩展 为 做 元素_获取扩展 用 元。
            若 扩展 不等于 0 并且 非 做 元素栈_包含标签 用 tb 的 开放元素栈、扩展 的 标签名原子：
                新元素 为 做 Document_create元素 用 tb 的 文档、扩展 的 标签名。
                当前 为 做 树构_当前节点 用 tb。
                若 当前 不等于 0：
                    做 节点_appendChild 用 当前、新元素。
                完。
                做 元素栈_压入 用 tb 的 开放元素栈、新元素。
                做 内存_写64 用 格式列表 的 元素 加 i 乘 8、新元素。
                做 智能_HTMLTreeBuilder_AI节点语义 用 新元素。
            完。
        完。
        i 为 i 加 1。
    完。
完。

HTMLDocumentParser_解析并摘要 接受 parser、html字符串：
    做 HTMLDocumentParser_解析 用 parser、html字符串。
    返还 做 树构_摘要串 用 parser 的 tree_builder。
完。

HTML解析器_解析并摘要 接受 html文本：
    文档 为 做 Document_创建 用 「about:blank」。
    parser 为 做 HTMLDocumentParser_创建 用 文档。
    返还 做 HTMLDocumentParser_解析并摘要 用 parser、html文本。
完。

智能_HTMLTreeBuilder_AI预加载提示 接受 url、资源类型：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 url 等于 0：
        返还。
    完。
    优先级 为 做 AI总线_资源优先级 用 bus、url。
完。

HTMLTreeBuilder_处理令牌 接受 tb、令牌：
    若 tb 的 插入模式 等于 插入模式_初始：
        做 HTMLTreeBuilder_初始模式 用 tb、令牌。
    否则若 tb 的 插入模式 等于 插入模式_DOCTYPE前 或者 tb 的 插入模式 等于 插入模式_html前：
        做 HTMLTreeBuilder_html前模式 用 tb、令牌。
    否则若 tb 的 插入模式 等于 插入模式_head前：
        做 HTMLTreeBuilder_head前模式 用 tb、令牌。
    否则若 tb 的 插入模式 等于 插入模式_head中：
        做 HTMLTreeBuilder_head中模式 用 tb、令牌。
    否则若 tb 的 插入模式 等于 插入模式_head后：
        做 HTMLTreeBuilder_head后模式 用 tb、令牌。
    否则若 tb 的 插入模式 等于 插入模式_body中：
        做 HTMLTreeBuilder_body中模式 用 tb、令牌。
    否则若 tb 的 插入模式 等于 插入模式_文本：
        做 HTMLTreeBuilder_文本模式 用 tb、令牌。
    否则若 tb 的 插入模式 等于 插入模式_body后：
        做 HTMLTreeBuilder_body后模式 用 tb、令牌。
    否则若 tb 的 插入模式 等于 插入模式_html后：
        做 HTMLTreeBuilder_html后模式 用 tb、令牌。
    否则若 tb 的 插入模式 等于 插入模式_table中：
        做 HTMLTreeBuilder_table中模式 用 tb、令牌。
    否则若 tb 的 插入模式 等于 插入模式_行中：
        做 HTMLTreeBuilder_行中模式 用 tb、令牌。
    否则若 tb 的 插入模式 等于 插入模式_单元格中：
        做 HTMLTreeBuilder_单元格中模式 用 tb、令牌。
    否则若 tb 的 插入模式 等于 插入模式_select中：
        做 HTMLTreeBuilder_select中模式 用 tb、令牌。
    完。
完。

HTMLTreeBuilder_初始模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_字符：
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_注释：
        注释 为 做 Comment_创建 用 令牌 的 数据。
        做 节点_appendChild 用 tb 的 文档、注释。
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_DOCTYPE：
        doctype 为 做 DocumentType_创建 用 令牌 的 DOCTYPE名称、「」、「」。
        做 节点_appendChild 用 tb 的 文档、doctype。
        若 令牌 的 强制怪异模式：
            扩展 为 做 Document_获取扩展 用 tb 的 文档。
            若 扩展 不等于 0：
                扩展 的 兼容模式 为 兼容模式_怪异。
            完。
        完。
        tb 的 插入模式 为 插入模式_html前。
        返还。
    完。
    扩展 为 做 Document_获取扩展 用 tb 的 文档。
    若 扩展 不等于 0：
        扩展 的 兼容模式 为 兼容模式_怪异。
    完。
    tb 的 插入模式 为 插入模式_html前。
    做 HTMLTreeBuilder_处理令牌 用 tb、令牌。
完。

HTMLTreeBuilder_html前模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_DOCTYPE：
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_注释：
        注释 为 做 Comment_创建 用 令牌 的 数据。
        做 节点_appendChild 用 tb 的 文档、注释。
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_开始标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_html：
            html 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、tb 的 文档。
            做 元素栈_压入 用 tb 的 开放元素栈、html。
            做 Document_设置文档元素 用 tb 的 文档、html。
            tb 的 插入模式 为 插入模式_head前。
            返还。
        完。
    完。
    html 为 做 Document_create元素 用 tb 的 文档、「html」。
    做 节点_appendChild 用 tb 的 文档、html。
    做 元素栈_压入 用 tb 的 开放元素栈、html。
    做 Document_设置文档元素 用 tb 的 文档、html。
    tb 的 插入模式 为 插入模式_head前。
    做 HTMLTreeBuilder_处理令牌 用 tb、令牌。
完。

HTMLTreeBuilder_head前模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_字符：
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_开始标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_head：
            head 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
            tb 的 head指针 为 head。
            做 元素栈_压入 用 tb 的 开放元素栈、head。
            做 Document_设置head 用 tb 的 文档、head。
            tb 的 插入模式 为 插入模式_head中。
            返还。
        完。
    完。
    head 为 做 Document_create元素 用 tb 的 文档、「head」。
    当前 为 做 树构_当前节点 用 tb。
    做 节点_appendChild 用 当前、head。
    tb 的 head指针 为 head。
    做 元素栈_压入 用 tb 的 开放元素栈、head。
    做 Document_设置head 用 tb 的 文档、head。
    tb 的 插入模式 为 插入模式_head中。
    做 HTMLTreeBuilder_处理令牌 用 tb、令牌。
完。

HTMLTreeBuilder_head中模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_字符：
        做 HTMLTreeBuilder_插入文本 用 tb、令牌 的 数据、令牌 的 数据长度。
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_注释：
        注释 为 做 Comment_创建 用 令牌 的 数据。
        当前 为 做 树构_当前节点 用 tb。
        做 节点_appendChild 用 当前、注释。
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_开始标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_title：
            做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
            做 HTML令牌izer_切换到RCDATA 用 tb 的 令牌izer。
            tb 的 原始插入模式 为 tb 的 插入模式。
            tb 的 插入模式 为 插入模式_文本。
            返还。
        完。
        若 标签原子 等于 原子_style：
            做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
            做 HTML令牌izer_切换到RAWTEXT 用 tb 的 令牌izer。
            tb 的 原始插入模式 为 tb 的 插入模式。
            tb 的 插入模式 为 插入模式_文本。
            返还。
        完。
        若 标签原子 等于 原子_script：
            做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
            做 HTML令牌izer_切换到脚本数据 用 tb 的 令牌izer。
            tb 的 原始插入模式 为 tb 的 插入模式。
            tb 的 插入模式 为 插入模式_文本。
            返还。
        完。
        若 标签原子 等于 原子_meta 或者 标签原子 等于 原子_link：
            做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
            若 令牌 的 自闭合 或者 做 元素_是空元素_原子 用 标签原子：
                做 元素栈_弹出 用 tb 的 开放元素栈。
            完。
            返还。
        完。
        返还 做 HTMLTreeBuilder_关闭head 用 tb、令牌。
    完。
    若 令牌 的 类型 等于 令牌类型_结束标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_head：
            做 元素栈_弹出 用 tb 的 开放元素栈。
            tb 的 插入模式 为 插入模式_head后。
            返还。
        完。
    完。
    做 HTMLTreeBuilder_关闭head 用 tb、令牌。
完。

HTMLTreeBuilder_head后模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_字符：
        做 HTMLTreeBuilder_插入文本 用 tb、令牌 的 数据、令牌 的 数据长度。
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_开始标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_body：
            body 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
            做 元素栈_压入 用 tb 的 开放元素栈、body。
            做 Document_设置body 用 tb 的 文档、body。
            tb 的 frameset_ok 为 假。
            tb 的 插入模式 为 插入模式_body中。
            返还。
        完。
    完。
    body 为 做 Document_create元素 用 tb 的 文档、「body」。
    当前 为 做 树构_当前节点 用 tb。
    做 节点_appendChild 用 当前、body。
    做 元素栈_压入 用 tb 的 开放元素栈、body。
    做 Document_设置body 用 tb 的 文档、body。
    tb 的 插入模式 为 插入模式_body中。
    做 HTMLTreeBuilder_处理令牌 用 tb、令牌。
完。

HTMLTreeBuilder_body中模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_字符：
        做 HTMLTreeBuilder_插入文本 用 tb、令牌 的 数据、令牌 的 数据长度。
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_注释：
        注释 为 做 Comment_创建 用 令牌 的 数据。
        当前 为 做 树构_当前节点 用 tb。
        做 节点_appendChild 用 当前、注释。
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_文件结束：
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_开始标签：
        做 HTMLTreeBuilder_body中_开始标签 用 tb、令牌。
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_结束标签：
        做 HTMLTreeBuilder_body中_结束标签 用 tb、令牌。
        返还。
    完。
完。

HTMLTreeBuilder_body中_开始标签 接受 tb、令牌：
    标签原子 为 做 原子_驻留 用 令牌 的 标签名。
    若 标签原子 等于 原子_div 或者 标签原子 等于 原子_p 或者 标签原子 等于 原子_section 或者 标签原子 等于 原子_article 或者 标签原子 等于 原子_nav 或者 标签原子 等于 原子_aside 或者 标签原子 等于 原子_header 或者 标签原子 等于 原子_footer 或者 标签原子 等于 原子_main：
        若 做 元素栈_包含标签 用 tb 的 开放元素栈、原子_p：
            做 HTMLTreeBuilder_关闭P元素 用 tb。
        完。
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        返还。
    完。
    若 做 树构_是标题元素 用 标签原子：
        若 做 元素栈_包含标签 用 tb 的 开放元素栈、原子_p：
            做 HTMLTreeBuilder_关闭P元素 用 tb。
        完。
        顶 为 做 树构_当前节点 用 tb。
        顶扩展 为 做 元素_获取扩展 用 顶。
        若 顶扩展 不等于 0 并且 做 树构_是标题元素 用 顶扩展 的 标签名原子：
            做 元素栈_弹出 用 tb 的 开放元素栈。
        完。
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        返还。
    完。
    若 标签原子 等于 原子_ul 或者 标签原子 等于 原子_ol：
        若 做 元素栈_包含标签 用 tb 的 开放元素栈、原子_p：
            做 HTMLTreeBuilder_关闭P元素 用 tb。
        完。
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        返还。
    完。
    若 标签原子 等于 原子_li：
        做 树构_关闭到标签 用 tb、原子_li。
        若 做 元素栈_包含标签 用 tb 的 开放元素栈、原子_p：
            做 HTMLTreeBuilder_关闭P元素 用 tb。
        完。
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        返还。
    完。
    若 标签原子 等于 原子_span 或者 标签原子 等于 原子_label 或者 标签原子 等于 原子_cite 或者 标签原子 等于 原子_q 或者 标签原子 等于 原子_sub 或者 标签原子 等于 原子_sup：
        做 HTMLTreeBuilder_重建格式化元素 用 tb。
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        返还。
    完。
    若 做 是格式化元素 用 标签原子：
        做 HTMLTreeBuilder_重建格式化元素 用 tb。
        若 标签原子 等于 原子_a 并且 做 元素栈_包含标签 用 tb 的 开放元素栈、原子_a：
            做 HTMLTreeBuilder_adoption_agency 用 tb、原子_a。
        完。
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        做 格式化列表_添加 用 tb 的 格式化列表、元素。
        返还。
    完。
    若 标签原子 等于 原子_dd 或者 标签原子 等于 原子_dt：
        做 树构_关闭到标签 用 tb、原子_dd。
        做 树构_关闭到标签 用 tb、原子_dt。
        若 做 元素栈_包含标签 用 tb 的 开放元素栈、原子_p：
            做 HTMLTreeBuilder_关闭P元素 用 tb。
        完。
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        返还。
    完。
    若 标签原子 等于 原子_pre 或者 标签原子 等于 原子_listing：
        若 做 元素栈_包含标签 用 tb 的 开放元素栈、原子_p：
            做 HTMLTreeBuilder_关闭P元素 用 tb。
        完。
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        tb 的 frameset_ok 为 假。
        返还。
    完。
    若 标签原子 等于 原子_br 或者 标签原子 等于 原子_hr 或者 标签原子 等于 原子_img：
        若 标签原子 等于 原子_hr 并且 做 元素栈_包含标签 用 tb 的 开放元素栈、原子_p：
            做 HTMLTreeBuilder_关闭P元素 用 tb。
        完。
        做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        返还。
    完。
    若 标签原子 等于 原子_input 或者 标签原子 等于 原子_textarea 或者 标签原子 等于 原子_select 或者 标签原子 等于 原子_button：
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        若 标签原子 不等于 原子_input：
            做 元素栈_压入 用 tb 的 开放元素栈、元素。
        完。
        返还。
    完。
    若 标签原子 等于 原子_form：
        若 tb 的 form指针 不等于 0：
            返还。
        完。
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        tb 的 form指针 为 元素。
        返还。
    完。
    若 标签原子 等于 原子_table：
        若 做 元素栈_包含标签 用 tb 的 开放元素栈、原子_p：
            做 HTMLTreeBuilder_关闭P元素 用 tb。
        完。
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        tb 的 插入模式 为 插入模式_table中。
        返还。
    完。
    若 标签原子 等于 原子_script：
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        做 HTML令牌izer_切换到脚本数据 用 tb 的 令牌izer。
        tb 的 原始插入模式 为 tb 的 插入模式。
        tb 的 插入模式 为 插入模式_文本。
        返还。
    完。
    若 标签原子 等于 原子_style：
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        做 HTML令牌izer_切换到RAWTEXT 用 tb 的 令牌izer。
        tb 的 原始插入模式 为 tb 的 插入模式。
        tb 的 插入模式 为 插入模式_文本。
        返还。
    完。
    若 标签原子 等于 原子_iframe 或者 标签原子 等于 原子_canvas 或者 标签原子 等于 原子_video 或者 标签原子 等于 原子_audio：
        元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
        返还。
    完。
    元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
    若 非 令牌 的 自闭合 并且 非 做 元素_是空元素_原子 用 标签原子：
        做 元素栈_压入 用 tb 的 开放元素栈、元素。
    完。
完。

HTMLTreeBuilder_body中_结束标签 接受 tb、令牌：
    标签原子 为 做 原子_驻留 用 令牌 的 标签名。
    若 标签原子 等于 原子_body：
        tb 的 插入模式 为 插入模式_body后。
        返还。
    完。
    若 标签原子 等于 原子_html：
        tb 的 插入模式 为 插入模式_body后。
        做 HTMLTreeBuilder_处理令牌 用 tb、令牌。
        返还。
    完。
    若 做 是格式化元素 用 标签原子：
        做 HTMLTreeBuilder_adoption_agency 用 tb、标签原子。
        返还。
    完。
    若 标签原子 等于 原子_dd 或者 标签原子 等于 原子_dt：
        做 树构_关闭到标签 用 tb、标签原子。
        返还。
    完。
    若 标签原子 等于 原子_div 或者 标签原子 等于 原子_p 或者 标签原子 等于 原子_section 或者 标签原子 等于 原子_article 或者 标签原子 等于 原子_nav 或者 标签原子 等于 原子_aside 或者 标签原子 等于 原子_header 或者 标签原子 等于 原子_footer 或者 标签原子 等于 原子_main 或者 标签原子 等于 原子_ul 或者 标签原子 等于 原子_ol 或者 做 树构_是标题元素 用 标签原子：
        做 树构_关闭到标签 用 tb、标签原子。
        返还。
    完。
    若 标签原子 等于 原子_li：
        做 树构_关闭到标签 用 tb、原子_li。
        返还。
    完。
    若 标签原子 等于 原子_form：
        tb 的 form指针 为 0。
        做 树构_关闭到标签 用 tb、原子_form。
        返还。
    完。
    做 树构_关闭到标签 用 tb、标签原子。
完。

HTMLTreeBuilder_table中模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_开始标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_tr：
            元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
            做 元素栈_压入 用 tb 的 开放元素栈、元素。
            tb 的 插入模式 为 插入模式_行中。
            返还。
        完。
        若 标签原子 等于 原子_td 或者 标签原子 等于 原子_th：
            tr 为 做 Document_create元素 用 tb 的 文档、「tr」。
            当前 为 做 树构_当前节点 用 tb。
            做 节点_appendChild 用 当前、tr。
            做 元素栈_压入 用 tb 的 开放元素栈、tr。
            tb 的 插入模式 为 插入模式_行中。
            做 HTMLTreeBuilder_处理令牌 用 tb、令牌。
            返还。
        完。
    完。
    若 令牌 的 类型 等于 令牌类型_结束标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_table：
            做 树构_关闭到标签 用 tb、原子_table。
            tb 的 插入模式 为 插入模式_body中。
            返还。
        完。
    完。
    做 HTMLTreeBuilder_body中模式 用 tb、令牌。
完。

HTMLTreeBuilder_行中模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_开始标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_td 或者 标签原子 等于 原子_th：
            元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
            做 元素栈_压入 用 tb 的 开放元素栈、元素。
            tb 的 插入模式 为 插入模式_单元格中。
            返还。
        完。
    完。
    若 令牌 的 类型 等于 令牌类型_结束标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_tr：
            做 树构_关闭到标签 用 tb、原子_tr。
            tb 的 插入模式 为 插入模式_table中。
            返还。
        完。
    完。
    做 HTMLTreeBuilder_table中模式 用 tb、令牌。
完。

HTMLTreeBuilder_单元格中模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_结束标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_td 或者 标签原子 等于 原子_th：
            做 树构_关闭到标签 用 tb、标签原子。
            tb 的 插入模式 为 插入模式_行中。
            返还。
        完。
    完。
    做 HTMLTreeBuilder_body中模式 用 tb、令牌。
完。

HTMLTreeBuilder_select中模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_开始标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_option：
            元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
            做 元素栈_压入 用 tb 的 开放元素栈、元素。
            返还。
        完。
    完。
    若 令牌 的 类型 等于 令牌类型_结束标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_select：
            做 树构_关闭到标签 用 tb、原子_select。
            tb 的 插入模式 为 插入模式_body中。
            返还。
        完。
        若 标签原子 等于 原子_option：
            做 元素栈_弹出 用 tb 的 开放元素栈。
            返还。
        完。
    完。
完。

本章 收束。
