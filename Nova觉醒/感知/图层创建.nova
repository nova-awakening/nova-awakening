《图层创建》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

TILE_大小 为 256。

TILE_最大数量 为 512。

合成状态_空闲 为 0。

合成状态_等待主线程 为 1。

合成状态_光栅化中 为 2。

合成状态_合成中 为 3。

合成状态_等待VSync 为 4。

调度_无待处理 为 0。

调度_有待处理_主线程 为 1。

调度_有待处理_合成 为 2。

调度_提交中 为 3。

合成类型_无 为 0。

合成类型_图处纹理 为 1。

合成类型_软件位图 为 2。

合成类型_视频帧 为 3。

合成类型_图绘 为 4。

Tile 是 一种 事物，含 纹理ID、层ID、格x、格y、像素x、像素y、状态、优先级、分辨率、上次使用帧。

Tile状态_未创建 为 0。

Tile状态_待光栅 为 1。

Tile状态_光栅中 为 2。

Tile状态_已就绪 为 3。

Tile状态_已失效 为 4。

CompositedLayer 是 一种 事物，含 ID、源绘制Layer、x、y、宽、高、Tile列数、Tile行数、Tiles、变换矩阵、透明度、有变换、有滤镜、合成类型、混合模式、裁剪区域、渲染精度、视口可见、语义重要性。

CompositedLayer_创建 接受 paintLayer、x、y、宽、高：
    cl 为 做 堆_分配 用 CompositedLayer。
    cl 的 源绘制Layer 为 paintLayer。
    cl 的 x 为 x。
    cl 的 y 为 y。
    cl 的 宽 为 宽。
    cl 的 高 为 高。
    cl 的 Tile列数 为 (宽 加 TILE_大小 减 1) 除以 TILE_大小。
    cl 的 Tile行数 为 (高 加 TILE_大小 减 1) 除以 TILE_大小。
    Tile总数 为 cl 的 Tile列数 乘 cl 的 Tile行数。
    cl 的 Tiles 为 做 堆_分配_数组 用 Tile总数 乘 32。
    可变 row 为 0。
    只要 row 小于 cl 的 Tile行数：
        可变 col 为 0。
        只要 col 小于 cl 的 Tile列数：
            idx 为 row 乘 cl 的 Tile列数 加 col。
            偏移 为 cl 的 Tiles 加 idx 乘 32。
            做 内存_写32 用 偏移、0。
            做 内存_写16 用 偏移 加 8、col。
            做 内存_写16 用 偏移 加 10、row。
            做 内存_写32 用 偏移 加 12、x 加 col 乘 TILE_大小。
            做 内存_写32 用 偏移 加 16、y 加 row 乘 TILE_大小。
            做 内存_写8 用 偏移 加 20、Tile状态_未创建。
            做 内存_写8 用 偏移 加 21、128。
            做 内存_写8 用 偏移 加 22、3。
            col 为 col 加 1。
        完。
        row 为 row 加 1。
    完。
    cl 的 透明度 为 255。
    cl 的 合成类型 为 合成类型_图处纹理。
    cl 的 渲染精度 为 2。
    返还 cl。
完。

本章 收束。
