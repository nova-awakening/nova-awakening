《索引库键范围》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·辅助存根》。

引用《..·推理·小整数SMI判定》。

引用《..·中枢·字符串替换》。

NIKR_版本 为 1。

键类型_无效 为 0。

键类型_数值 为 1。

键类型_日期 为 2。

键类型_字符串 为 3。

键类型_二进制 为 4。

键类型_数组 为 5。

键优先级_数值 为 1。

键优先级_日期 为 2。

键优先级_字符串 为 3。

键优先级_二进制 为 4。

键优先级_数组 为 5。

范围_无界 为 0xFFFFFFFF。

最大键路径深度 为 16。

最大复合键分量 为 32。

最大编码键长 为 4096。

游标_前进 为 0。

游标_后退 为 1。

游标_前进唯一 为 2。

游标_后退唯一 为 3。

可变 NIKR_范围创建计数 为 0。

可变 NIKR_包含测试计数 为 0。

可变 NIKR_键比较计数 为 0。

可变 NIKR_游标扫描计数 为 0。

可变 NIKR_交集计算计数 为 0。

可变 NIKR_编码操作计数 为 0。

可变 NIKR_AI预测计数 为 0。

可变 NIKR_AI命中计数 为 0。

可变 NIKR_错误计数 为 0。

IDB键 是 一种 事物，含 类型、数值、字符串值、二进制值、二进制长度、子键、子键数。

IDBKeyRange 是 一种 事物，含 下界、上界、下界开、上界开、是无界下、是无界上、选择度。

IDBRequest 是 一种 事物，含 结果、错误、错误码、onsuccess、onerror、就绪状态、事务、源对象。

IDB游标 是 一种 事物，含 范围、方向、当前键、当前主键、当前值、已结束、索引引用、迭代计数、源存储。

键路径 是 一种 事物，含 分量、分量数、是数组路径。

编码键 是 一种 事物，含 数据、长度。

IDB键_创建_数值 接受 值：
    k 为 做 堆_分配 用 IDB键。
    k 的 类型 为 键类型_数值。
    k 的 数值 为 值。
    k 的 字符串值 为 0。
    k 的 二进制值 为 0。
    k 的 二进制长度 为 0。
    k 的 子键 为 0。
    k 的 子键数 为 0。
    返还 k。
完。

IDB键_创建_字符串 接受 值：
    k 为 做 堆_分配 用 IDB键。
    k 的 类型 为 键类型_字符串。
    k 的 字符串值 为 做 字符串_复制 用 值。
    k 的 数值 为 0。
    k 的 二进制值 为 0。
    k 的 二进制长度 为 0。
    k 的 子键 为 0。
    k 的 子键数 为 0。
    返还 k。
完。

IDB键_创建_日期 接受 时间戳：
    k 为 做 堆_分配 用 IDB键。
    k 的 类型 为 键类型_日期。
    k 的 数值 为 时间戳。
    k 的 字符串值 为 0。
    k 的 二进制值 为 0。
    k 的 二进制长度 为 0。
    k 的 子键 为 0。
    k 的 子键数 为 0。
    返还 k。
完。

IDB键_创建_二进制 接受 缓冲区、长度：
    k 为 做 堆_分配 用 IDB键。
    k 的 类型 为 键类型_二进制。
    k 的 二进制值 为 缓冲区。
    k 的 二进制长度 为 长度。
    k 的 数值 为 0。
    k 的 字符串值 为 0。
    k 的 子键 为 0。
    k 的 子键数 为 0。
    返还 k。
完。

IDB键_创建_数组 接受 容量：
    k 为 做 堆_分配 用 IDB键。
    k 的 类型 为 键类型_数组。
    若 容量 大于 最大复合键分量：
        容量 为 最大复合键分量。
    完。
    k 的 子键 为 做 堆_分配_数组 用 容量 乘 8。
    k 的 子键数 为 0。
    k 的 数值 为 0。
    k 的 字符串值 为 0。
    k 的 二进制值 为 0。
    k 的 二进制长度 为 0。
    返还 k。
完。

IDB键_数组追加 接受 数组键、子键：
    若 数组键 等于 0 或者 数组键 的 类型 不等于 键类型_数组：
        返还 假。
    完。
    若 数组键 的 子键数 大于或等于 最大复合键分量：
        返还 假。
    完。
    做 内存_写64 用 数组键 的 子键 加 数组键 的 子键数 乘 8、子键。
    数组键 的 子键数 为 数组键 的 子键数 加 1。
    返还 真。
完。

IDB键_获取优先级 接受 键：
    若 键 等于 0：
        返还 0。
    完。
    若 键 的 类型 等于 键类型_数值：
        返还 键优先级_数值。
    完。
    若 键 的 类型 等于 键类型_日期：
        返还 键优先级_日期。
    完。
    若 键 的 类型 等于 键类型_字符串：
        返还 键优先级_字符串。
    完。
    若 键 的 类型 等于 键类型_二进制：
        返还 键优先级_二进制。
    完。
    若 键 的 类型 等于 键类型_数组：
        返还 键优先级_数组。
    完。
    返还 0。
完。

IDB键_释放 接受 键：
    若 键 等于 0：
        返还。
    完。
    若 键 的 类型 等于 键类型_字符串 并且 键 的 字符串值 不等于 0：
        做 堆_释放 用 键 的 字符串值。
    完。
    若 键 的 类型 等于 键类型_数组 并且 键 的 子键 不等于 0：
        可变 i 为 0。
        只要 i 小于 键 的 子键数：
            子 为 做 内存_读64 用 键 的 子键 加 i 乘 8。
            做 IDB键_释放 用 子。
            i 为 i 加 1。
        完。
        做 堆_释放 用 键 的 子键。
    完。
    做 堆_释放 用 键。
完。

IDB_比较键 接受 a、b：
    NIKR_键比较计数 为 NIKR_键比较计数 加 1。
    若 a 等于 0 并且 b 等于 0：
        返还 0。
    完。
    若 a 等于 0：
        返还 负 1。
    完。
    若 b 等于 0：
        返还 1。
    完。
    优a 为 做 IDB键_获取优先级 用 a。
    优b 为 做 IDB键_获取优先级 用 b。
    若 优a 不等于 优b：
        若 优a 小于 优b：
            返还 负 1。
        完。
        返还 1。
    完。
    若 a 的 类型 等于 键类型_数值 或者 a 的 类型 等于 键类型_日期：
        若 a 的 数值 小于 b 的 数值：
            返还 负 1。
        完。
        若 a 的 数值 大于 b 的 数值：
            返还 1。
        完。
        返还 0。
    完。
    若 a 的 类型 等于 键类型_字符串：
        返还 做 IDB_比较字符串 用 a 的 字符串值、b 的 字符串值。
    完。
    若 a 的 类型 等于 键类型_二进制：
        返还 做 IDB_比较二进制 用 a 的 二进制值、a 的 二进制长度、b 的 二进制值、b 的 二进制长度。
    完。
    若 a 的 类型 等于 键类型_数组：
        返还 做 IDB_比较数组键 用 a、b。
    完。
    返还 0。
完。

IDB_比较字符串 接受 s1、s2：
    若 s1 等于 0 并且 s2 等于 0：
        返还 0。
    完。
    若 s1 等于 0：
        返还 负 1。
    完。
    若 s2 等于 0：
        返还 1。
    完。
    len1 为 做 字符串_长度 用 s1。
    len2 为 做 字符串_长度 用 s2。
    最小长 为 len1。
    若 len2 小于 最小长：
        最小长 为 len2。
    完。
    可变 i 为 0。
    只要 i 小于 最小长：
        c1 为 做 内存_读8 用 s1 加 i。
        c2 为 做 内存_读8 用 s2 加 i。
        若 c1 小于 c2：
            返还 负 1。
        完。
        若 c1 大于 c2：
            返还 1。
        完。
        i 为 i 加 1。
    完。
    若 len1 小于 len2：
        返还 负 1。
    完。
    若 len1 大于 len2：
        返还 1。
    完。
    返还 0。
完。

IDB_比较二进制 接受 b1、len1、b2、len2：
    最小长 为 len1。
    若 len2 小于 最小长：
        最小长 为 len2。
    完。
    可变 i 为 0。
    只要 i 小于 最小长：
        v1 为 做 内存_读8 用 b1 加 i。
        v2 为 做 内存_读8 用 b2 加 i。
        若 v1 小于 v2：
            返还 负 1。
        完。
        若 v1 大于 v2：
            返还 1。
        完。
        i 为 i 加 1。
    完。
    若 len1 小于 len2：
        返还 负 1。
    完。
    若 len1 大于 len2：
        返还 1。
    完。
    返还 0。
完。

IDB_比较数组键 接受 a、b：
    最小数 为 a 的 子键数。
    若 b 的 子键数 小于 最小数：
        最小数 为 b 的 子键数。
    完。
    可变 i 为 0。
    只要 i 小于 最小数：
        子a 为 做 内存_读64 用 a 的 子键 加 i 乘 8。
        子b 为 做 内存_读64 用 b 的 子键 加 i 乘 8。
        cmp 为 做 IDB_比较键 用 子a、子b。
        若 cmp 不等于 0：
            返还 cmp。
        完。
        i 为 i 加 1。
    完。
    若 a 的 子键数 小于 b 的 子键数：
        返还 负 1。
    完。
    若 a 的 子键数 大于 b 的 子键数：
        返还 1。
    完。
    返还 0。
完。

IDBKeyRange_only 接受 值：
    NIKR_范围创建计数 为 NIKR_范围创建计数 加 1。
    r 为 做 堆_分配 用 IDBKeyRange。
    r 的 下界 为 值。
    r 的 上界 为 值。
    r 的 下界开 为 假。
    r 的 上界开 为 假。
    r 的 是无界下 为 假。
    r 的 是无界上 为 假。
    r 的 选择度 为 1。
    返还 r。
完。

IDBKeyRange_bound 接受 下、上、下开、上开：
    NIKR_范围创建计数 为 NIKR_范围创建计数 加 1。
    cmp 为 做 IDB_比较键 用 下、上。
    若 cmp 大于 0：
        NIKR_错误计数 为 NIKR_错误计数 加 1。
        返还 0。
    完。
    若 cmp 等于 0 并且 (下开 或者 上开)：
        NIKR_错误计数 为 NIKR_错误计数 加 1。
        返还 0。
    完。
    r 为 做 堆_分配 用 IDBKeyRange。
    r 的 下界 为 下。
    r 的 上界 为 上。
    r 的 下界开 为 下开。
    r 的 上界开 为 上开。
    r 的 是无界下 为 假。
    r 的 是无界上 为 假。
    r 的 选择度 为 5000。
    返还 r。
完。

IDBKeyRange_lowerBound 接受 下、开：
    NIKR_范围创建计数 为 NIKR_范围创建计数 加 1。
    r 为 做 堆_分配 用 IDBKeyRange。
    r 的 下界 为 下。
    r 的 上界 为 0。
    r 的 下界开 为 开。
    r 的 上界开 为 真。
    r 的 是无界下 为 假。
    r 的 是无界上 为 真。
    r 的 选择度 为 7500。
    返还 r。
完。

IDBKeyRange_upperBound 接受 上、开：
    NIKR_范围创建计数 为 NIKR_范围创建计数 加 1。
    r 为 做 堆_分配 用 IDBKeyRange。
    r 的 下界 为 0。
    r 的 上界 为 上。
    r 的 下界开 为 真。
    r 的 上界开 为 开。
    r 的 是无界下 为 真。
    r 的 是无界上 为 假。
    r 的 选择度 为 7500。
    返还 r。
完。

IDBKeyRange_全范围 接受 零参：
    NIKR_范围创建计数 为 NIKR_范围创建计数 加 1。
    r 为 做 堆_分配 用 IDBKeyRange。
    r 的 下界 为 0。
    r 的 上界 为 0。
    r 的 下界开 为 真。
    r 的 上界开 为 真。
    r 的 是无界下 为 真。
    r 的 是无界上 为 真。
    r 的 选择度 为 10000。
    返还 r。
完。

IDBKeyRange_释放 接受 范围：
    若 范围 等于 0：
        返还。
    完。
    做 堆_释放 用 范围。
完。

IDBKeyRange_includes 接受 范围、键：
    NIKR_包含测试计数 为 NIKR_包含测试计数 加 1。
    若 范围 等于 0：
        返还 真。
    完。
    若 键 等于 0：
        返还 假。
    完。
    若 非 范围 的 是无界下 并且 范围 的 下界 不等于 0：
        cmp 为 做 IDB_比较键 用 键、范围 的 下界。
        若 范围 的 下界开 并且 cmp 小于或等于 0：
            返还 假。
        完。
        若 非 范围 的 下界开 并且 cmp 小于 0：
            返还 假。
        完。
    完。
    若 非 范围 的 是无界上 并且 范围 的 上界 不等于 0：
        cmp 为 做 IDB_比较键 用 键、范围 的 上界。
        若 范围 的 上界开 并且 cmp 大于或等于 0：
            返还 假。
        完。
        若 非 范围 的 上界开 并且 cmp 大于 0：
            返还 假。
        完。
    完。
    返还 真。
完。

IDBKeyRange_交集 接受 范围1、范围2：
    NIKR_交集计算计数 为 NIKR_交集计算计数 加 1。
    若 范围1 等于 0：
        返还 范围2。
    完。
    若 范围2 等于 0：
        返还 范围1。
    完。
    新下界 为 0。
    新下开 为 假。
    新无界下 为 假。
    若 范围1 的 是无界下 并且 范围2 的 是无界下：
        新无界下 为 真。
    否则若 范围1 的 是无界下：
        新下界 为 范围2 的 下界。
        新下开 为 范围2 的 下界开。
    否则若 范围2 的 是无界下：
        新下界 为 范围1 的 下界。
        新下开 为 范围1 的 下界开。
    否则：
        cmp 为 做 IDB_比较键 用 范围1 的 下界、范围2 的 下界。
        若 cmp 大于 0：
            新下界 为 范围1 的 下界。
            新下开 为 范围1 的 下界开。
        否则若 cmp 小于 0：
            新下界 为 范围2 的 下界。
            新下开 为 范围2 的 下界开。
        否则：
            新下界 为 范围1 的 下界。
            新下开 为 范围1 的 下界开 或者 范围2 的 下界开。
        完。
    完。
    新上界 为 0。
    新上开 为 假。
    新无界上 为 假。
    若 范围1 的 是无界上 并且 范围2 的 是无界上：
        新无界上 为 真。
    否则若 范围1 的 是无界上：
        新上界 为 范围2 的 上界。
        新上开 为 范围2 的 上界开。
    否则若 范围2 的 是无界上：
        新上界 为 范围1 的 上界。
        新上开 为 范围1 的 上界开。
    否则：
        cmp 为 做 IDB_比较键 用 范围1 的 上界、范围2 的 上界。
        若 cmp 小于 0：
            新上界 为 范围1 的 上界。
            新上开 为 范围1 的 上界开。
        否则若 cmp 大于 0：
            新上界 为 范围2 的 上界。
            新上开 为 范围2 的 上界开。
        否则：
            新上界 为 范围1 的 上界。
            新上开 为 范围1 的 上界开 或者 范围2 的 上界开。
        完。
    完。
    若 非 新无界下 并且 非 新无界上 并且 新下界 不等于 0 并且 新上界 不等于 0：
        cmp 为 做 IDB_比较键 用 新下界、新上界。
        若 cmp 大于 0：
            返还 0。
        完。
        若 cmp 等于 0 并且 (新下开 或者 新上开)：
            返还 0。
        完。
    完。
    r 为 做 堆_分配 用 IDBKeyRange。
    r 的 下界 为 新下界。
    r 的 上界 为 新上界。
    r 的 下界开 为 新下开。
    r 的 上界开 为 新上开。
    r 的 是无界下 为 新无界下。
    r 的 是无界上 为 新无界上。
    r 的 选择度 为 0。
    返还 r。
完。

IDBKeyRange_是否重叠 接受 范围1、范围2：
    交集 为 做 IDBKeyRange_交集 用 范围1、范围2。
    若 交集 等于 0：
        返还 假。
    完。
    若 交集 不等于 范围1 并且 交集 不等于 范围2：
        做 IDBKeyRange_释放 用 交集。
    完。
    返还 真。
完。

IDBKeyRange_是否子集 接受 子范围、父范围：
    若 父范围 等于 0：
        返还 真。
    完。
    若 子范围 等于 0：
        返还 假。
    完。
    若 非 父范围 的 是无界下：
        若 子范围 的 是无界下：
            返还 假。
        完。
        cmp 为 做 IDB_比较键 用 子范围 的 下界、父范围 的 下界。
        若 cmp 小于 0：
            返还 假。
        完。
        若 cmp 等于 0 并且 父范围 的 下界开 并且 非 子范围 的 下界开：
            返还 假。
        完。
    完。
    若 非 父范围 的 是无界上：
        若 子范围 的 是无界上：
            返还 假。
        完。
        cmp 为 做 IDB_比较键 用 子范围 的 上界、父范围 的 上界。
        若 cmp 大于 0：
            返还 假。
        完。
        若 cmp 等于 0 并且 父范围 的 上界开 并且 非 子范围 的 上界开：
            返还 假。
        完。
    完。
    返还 真。
完。

键路径_解析 接受 路径字符串：
    若 路径字符串 等于 0：
        返还 0。
    完。
    kp 为 做 堆_分配 用 键路径。
    kp 的 分量数 为 0。
    kp 的 是数组路径 为 假。
    长度 为 做 字符串_长度 用 路径字符串。
    若 长度 等于 0：
        返还 kp。
    完。
    可变 起始 为 0。
    可变 i 为 0。
    只要 i 小于或等于 长度 并且 kp 的 分量数 小于 最大键路径深度：
        ch 为 0。
        若 i 小于 长度：
            ch 为 做 内存_读8 用 路径字符串 加 i。
        完。
        若 ch 等于 46 或者 i 等于 长度：
            若 i 大于 起始：
                分量长 为 i 减 起始。
                分量 为 做 堆_分配_数组 用 分量长 加 1。
                做 内存_复制 用 分量、路径字符串 加 起始、分量长。
                做 内存_写8 用 分量 加 分量长、0。
                kp 的 分量 的 第 kp 的 分量数 项 为 分量。
                kp 的 分量数 为 kp 的 分量数 加 1。
            完。
            起始 为 i 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 kp。
完。

IDB_提取键 接受 对象、键路径：
    若 对象 等于 0 或者 键路径 等于 0：
        返还 0。
    完。
    kp 为 做 键路径_解析 用 键路径。
    若 kp 等于 0 或者 kp 的 分量数 等于 0：
        返还 0。
    完。
    可变 当前 为 对象。
    可变 i 为 0。
    只要 i 小于 kp 的 分量数：
        当前 为 做 Nova对象_获取属性 用 0、当前、kp 的 分量 的 第 i 项。
        若 当前 等于 0：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 当前。
完。

IDB_提取键_多路径 接受 对象、路径数组、路径数：
    若 对象 等于 0 或者 路径数组 等于 0 或者 路径数 等于 0：
        返还 0。
    完。
    复合 为 做 IDB键_创建_数组 用 路径数。
    可变 i 为 0。
    只要 i 小于 路径数：
        路径 为 做 内存_读64 用 路径数组 加 i 乘 8。
        值 为 做 IDB_提取键 用 对象、路径。
        若 值 等于 0：
            做 IDB键_释放 用 复合。
            返还 0。
        完。
        做 IDB键_数组追加 用 复合、值。
        i 为 i 加 1。
    完。
    返还 复合。
完。

IDB键_编码 接受 键、输出：
    NIKR_编码操作计数 为 NIKR_编码操作计数 加 1。
    若 键 等于 0 或者 输出 等于 0：
        返还 0。
    完。
    输出 的 长度 为 0。
    返还 做 IDB键_编码_递归 用 键、输出。
完。

IDB键_编码_递归 接受 键、输出：
    若 输出 的 长度 大于或等于 最大编码键长 减 16：
        返还 假。
    完。
    若 键 的 类型 等于 键类型_数值：
        做 内存_写8 用 输出 的 数据 加 输出 的 长度、0x10。
        输出 的 长度 为 输出 的 长度 加 1。
        编码值 为 键 的 数值 位异或 0x8000000000000000。
        可变 b 为 7。
        只要 b 大于或等于 0：
            做 内存_写8 用 输出 的 数据 加 输出 的 长度、编码值 右移 b 乘 8 位与 0xFF。
            输出 的 长度 为 输出 的 长度 加 1。
            b 为 b 减 1。
        完。
        返还 真。
    完。
    若 键 的 类型 等于 键类型_日期：
        做 内存_写8 用 输出 的 数据 加 输出 的 长度、0x20。
        输出 的 长度 为 输出 的 长度 加 1。
        编码值 为 键 的 数值 位异或 0x8000000000000000。
        可变 b 为 7。
        只要 b 大于或等于 0：
            做 内存_写8 用 输出 的 数据 加 输出 的 长度、编码值 右移 b 乘 8 位与 0xFF。
            输出 的 长度 为 输出 的 长度 加 1。
            b 为 b 减 1。
        完。
        返还 真。
    完。
    若 键 的 类型 等于 键类型_字符串：
        做 内存_写8 用 输出 的 数据 加 输出 的 长度、0x30。
        输出 的 长度 为 输出 的 长度 加 1。
        len 为 做 字符串_长度 用 键 的 字符串值。
        可变 i 为 0。
        只要 i 小于 len 并且 输出 的 长度 小于 最大编码键长 减 4：
            ch 为 做 内存_读8 用 键 的 字符串值 加 i。
            若 ch 等于 0：
                做 内存_写8 用 输出 的 数据 加 输出 的 长度、0x00。
                做 内存_写8 用 输出 的 数据 加 输出 的 长度 加 1、0x01。
                输出 的 长度 为 输出 的 长度 加 2。
            否则：
                做 内存_写8 用 输出 的 数据 加 输出 的 长度、ch。
                输出 的 长度 为 输出 的 长度 加 1。
            完。
            i 为 i 加 1。
        完。
        做 内存_写8 用 输出 的 数据 加 输出 的 长度、0x00。
        做 内存_写8 用 输出 的 数据 加 输出 的 长度 加 1、0x00。
        输出 的 长度 为 输出 的 长度 加 2。
        返还 真。
    完。
    若 键 的 类型 等于 键类型_二进制：
        做 内存_写8 用 输出 的 数据 加 输出 的 长度、0x40。
        输出 的 长度 为 输出 的 长度 加 1。
        可变 i 为 0。
        只要 i 小于 键 的 二进制长度 并且 输出 的 长度 小于 最大编码键长 减 4：
            v 为 做 内存_读8 用 键 的 二进制值 加 i。
            若 v 等于 0：
                做 内存_写8 用 输出 的 数据 加 输出 的 长度、0x00。
                做 内存_写8 用 输出 的 数据 加 输出 的 长度 加 1、0x01。
                输出 的 长度 为 输出 的 长度 加 2。
            否则：
                做 内存_写8 用 输出 的 数据 加 输出 的 长度、v。
                输出 的 长度 为 输出 的 长度 加 1。
            完。
            i 为 i 加 1。
        完。
        做 内存_写8 用 输出 的 数据 加 输出 的 长度、0x00。
        做 内存_写8 用 输出 的 数据 加 输出 的 长度 加 1、0x00。
        输出 的 长度 为 输出 的 长度 加 2。
        返还 真。
    完。
    若 键 的 类型 等于 键类型_数组：
        做 内存_写8 用 输出 的 数据 加 输出 的 长度、0x50。
        输出 的 长度 为 输出 的 长度 加 1。
        可变 i 为 0。
        只要 i 小于 键 的 子键数：
            子 为 做 内存_读64 用 键 的 子键 加 i 乘 8。
            若 非 做 IDB键_编码_递归 用 子、输出：
                返还 假。
            完。
            i 为 i 加 1。
        完。
        做 内存_写8 用 输出 的 数据 加 输出 的 长度、0x00。
        输出 的 长度 为 输出 的 长度 加 1。
        返还 真。
    完。
    返还 假。
完。

IDB游标_创建 接受 范围、方向、索引、源：
    c 为 做 堆_分配 用 IDB游标。
    c 的 范围 为 范围。
    c 的 方向 为 方向。
    c 的 当前键 为 0。
    c 的 当前主键 为 0。
    c 的 当前值 为 0。
    c 的 已结束 为 假。
    c 的 索引引用 为 索引。
    c 的 迭代计数 为 0。
    c 的 源存储 为 源。
    NIKR_游标扫描计数 为 NIKR_游标扫描计数 加 1。
    返还 c。
完。

IDB游标_前进 接受 游标：
    若 游标 等于 0 或者 游标 的 已结束：
        返还 假。
    完。
    游标 的 迭代计数 为 游标 的 迭代计数 加 1。
    若 游标 的 方向 等于 游标_前进 或者 游标 的 方向 等于 游标_前进唯一：
        下一 为 做 IDB索引_下一键 用 游标 的 索引引用、游标 的 当前键。
        若 下一 等于 0：
            游标 的 已结束 为 真。
            返还 假。
        完。
        若 游标 的 方向 等于 游标_前进唯一：
            只要 下一 不等于 0 并且 做 IDB_比较键 用 下一、游标 的 当前键 等于 0：
                下一 为 做 IDB索引_下一键 用 游标 的 索引引用、下一。
            完。
        完。
        若 下一 等于 0：
            游标 的 已结束 为 真。
            返还 假。
        完。
        若 非 做 IDBKeyRange_includes 用 游标 的 范围、下一：
            游标 的 已结束 为 真。
            返还 假。
        完。
        游标 的 当前键 为 下一。
    否则：
        前一 为 做 IDB索引_前一键 用 游标 的 索引引用、游标 的 当前键。
        若 前一 等于 0：
            游标 的 已结束 为 真。
            返还 假。
        完。
        若 游标 的 方向 等于 游标_后退唯一：
            只要 前一 不等于 0 并且 做 IDB_比较键 用 前一、游标 的 当前键 等于 0：
                前一 为 做 IDB索引_前一键 用 游标 的 索引引用、前一。
            完。
        完。
        若 前一 等于 0：
            游标 的 已结束 为 真。
            返还 假。
        完。
        若 非 做 IDBKeyRange_includes 用 游标 的 范围、前一：
            游标 的 已结束 为 真。
            返还 假。
        完。
        游标 的 当前键 为 前一。
    完。
    返还 真。
完。

IDB游标_continue到 接受 游标、目标键：
    若 游标 等于 0 或者 游标 的 已结束 或者 目标键 等于 0：
        返还 假。
    完。
    若 游标 的 方向 等于 游标_前进 或者 游标 的 方向 等于 游标_前进唯一：
        若 做 IDB_比较键 用 目标键、游标 的 当前键 小于或等于 0：
            返还 假。
        完。
    否则：
        若 做 IDB_比较键 用 目标键、游标 的 当前键 大于或等于 0：
            返还 假。
        完。
    完。
    游标 的 当前键 为 做 IDB索引_查找键 用 游标 的 索引引用、目标键。
    若 游标 的 当前键 等于 0 或者 非 做 IDBKeyRange_includes 用 游标 的 范围、游标 的 当前键：
        游标 的 已结束 为 真。
        返还 假。
    完。
    返还 真。
完。

IDB游标_释放 接受 游标：
    若 游标 等于 0：
        返还。
    完。
    做 堆_释放 用 游标。
完。

IDBRequest_创建 接受 零参：
    r 为 做 堆_分配 用 IDBRequest。
    r 的 结果 为 0。
    r 的 错误 为 0。
    r 的 错误码 为 0。
    r 的 onsuccess 为 0。
    r 的 onerror 为 0。
    r 的 就绪状态 为 0。
    r 的 事务 为 0。
    r 的 源对象 为 0。
    返还 r。
完。

IDBRequest_创建_成功 接受 结果：
    r 为 做 IDBRequest_创建。
    r 的 结果 为 结果。
    r 的 就绪状态 为 1。
    返还 r。
完。

IDBRequest_完成_成功 接受 请求、结果：
    若 请求 等于 0：
        返还。
    完。
    请求 的 结果 为 结果。
    请求 的 就绪状态 为 1。
    若 请求 的 onsuccess 不等于 0：
        做 Nova事件_异步派发 用 请求 的 onsuccess、请求。
    完。
完。

IDBRequest_完成_失败 接受 请求、错误码：
    若 请求 等于 0：
        返还。
    完。
    请求 的 错误码 为 错误码。
    请求 的 就绪状态 为 1。
    若 请求 的 onerror 不等于 0：
        做 Nova事件_异步派发 用 请求 的 onerror、请求。
    完。
完。

智能_IndexedDB_AI初始化 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    t 为 做 Tensor_创建 用 8。
    做 Tensor_写 用 t、0、NIKR_版本。
    做 Tensor_写 用 t、1、NIKR_范围创建计数。
    做 Tensor_写 用 t、2、NIKR_包含测试计数。
    做 Tensor_写 用 t、3、NIKR_键比较计数。
    做 Tensor_写 用 t、4、NIKR_游标扫描计数。
    做 Tensor_写 用 t、5、NIKR_交集计算计数。
    做 Tensor_写 用 t、6、NIKR_编码操作计数。
    做 Tensor_写 用 t、7、NIKR_AI预测计数。
    做 AI总线_推理 用 bus、「NIKR_模块注册」、t。
    做 Tensor_释放 用 t。
完。

NIKR_AI估计选择度 接受 范围、总记录数、键分布：
    NIKR_AI预测计数 为 NIKR_AI预测计数 加 1。
    若 范围 等于 0 或者 总记录数 等于 0：
        返还 10000。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        若 范围 的 是无界下 并且 范围 的 是无界上：
            返还 10000。
        完。
        返还 5000。
    完。
    t 为 做 Tensor_创建 用 6。
    范围类型 为 0。
    若 范围 的 是无界下 并且 范围 的 是无界上：
        范围类型 为 3。
    否则若 范围 的 是无界下 或者 范围 的 是无界上：
        范围类型 为 2。
    否则若 范围 的 下界开 等于 假 并且 范围 的 上界开 等于 假：
        cmp 为 做 IDB_比较键 用 范围 的 下界、范围 的 上界。
        若 cmp 等于 0：
            范围类型 为 0。
        否则：
            范围类型 为 1。
        完。
    否则：
        范围类型 为 1。
    完。
    做 Tensor_写 用 t、0、范围类型。
    做 Tensor_写 用 t、1、总记录数。
    做 Tensor_写 用 t、2、键分布。
    历史命中 为 0。
    若 NIKR_包含测试计数 大于 0：
        历史命中 为 NIKR_AI命中计数 乘 10000 除以 NIKR_包含测试计数。
    完。
    做 Tensor_写 用 t、3、历史命中。
    做 Tensor_写 用 t、4、NIKR_游标扫描计数。
    做 Tensor_写 用 t、5、NIKR_键比较计数。
    结果 为 做 AI总线_推理 用 bus、「NIKR_选择度估计」、t。
    做 Tensor_释放 用 t。
    若 结果 小于 0：
        返还 5000。
    完。
    若 结果 大于 10000：
        返还 10000。
    完。
    NIKR_AI命中计数 为 NIKR_AI命中计数 加 1。
    返还 结果。
完。

NIKR_AI建议扫描策略 接受 范围、总记录数、索引数：
    选择度 为 做 NIKR_AI估计选择度 用 范围、总记录数、0。
    若 选择度 小于或等于 10：
        返还 2。
    完。
    若 选择度 小于或等于 3000：
        返还 1。
    完。
    若 索引数 大于 0 并且 选择度 小于或等于 7000：
        返还 1。
    完。
    返还 0。
完。

NIKR_AI异常检测 接受 操作类型、键、范围：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    t 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 t、0、操作类型。
    做 Tensor_写 用 t、1、NIKR_键比较计数。
    做 Tensor_写 用 t、2、NIKR_错误计数。
    做 Tensor_写 用 t、3、NIKR_游标扫描计数。
    风险 为 做 AI总线_风险评分 用 bus、「NIKR_异常」、t。
    做 Tensor_释放 用 t。
    返还 风险。
完。

NIKR_获取统计 接受 零参：
    s 为 做 堆_分配_数组 用 80。
    做 内存_写64 用 s 加 0、NIKR_范围创建计数。
    做 内存_写64 用 s 加 8、NIKR_包含测试计数。
    做 内存_写64 用 s 加 16、NIKR_键比较计数。
    做 内存_写64 用 s 加 24、NIKR_游标扫描计数。
    做 内存_写64 用 s 加 32、NIKR_交集计算计数。
    做 内存_写64 用 s 加 40、NIKR_编码操作计数。
    做 内存_写64 用 s 加 48、NIKR_AI预测计数。
    做 内存_写64 用 s 加 56、NIKR_AI命中计数。
    做 内存_写64 用 s 加 64、NIKR_错误计数。
    返还 s。
完。

NIKR_重置统计 接受 零参：
    NIKR_范围创建计数 为 0。
    NIKR_包含测试计数 为 0。
    NIKR_键比较计数 为 0。
    NIKR_游标扫描计数 为 0。
    NIKR_交集计算计数 为 0。
    NIKR_编码操作计数 为 0。
    NIKR_AI预测计数 为 0。
    NIKR_AI命中计数 为 0。
    NIKR_错误计数 为 0。
完。

NIKR_诊断打印 接受 零参：
    做 内核打印 用 「[NIKR] 范围创建=」、NIKR_范围创建计数。
    做 内核打印 用 「[NIKR] 包含测试=」、NIKR_包含测试计数。
    做 内核打印 用 「[NIKR] 键比较=」、NIKR_键比较计数。
    做 内核打印 用 「[NIKR] 游标扫描=」、NIKR_游标扫描计数。
    做 内核打印 用 「[NIKR] 交集计算=」、NIKR_交集计算计数。
    做 内核打印 用 「[NIKR] 编码操作=」、NIKR_编码操作计数。
    做 内核打印 用 「[NIKR] AI预测=」、NIKR_AI预测计数、「 命中=」、NIKR_AI命中计数。
    若 NIKR_AI预测计数 大于 0：
        命中率 为 NIKR_AI命中计数 乘 100 除以 NIKR_AI预测计数。
        做 内核打印 用 「[NIKR] AI命中率=」、命中率、「%」。
    完。
    做 内核打印 用 「[NIKR] 错误=」、NIKR_错误计数。
完。

本章 收束。
