《字形扫描线光栅化》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·字符字形标》。

引用《.·内核直连层》。

引用《.·文字绘制最》。

GlyphBitmap 是 一种 事物，含 宽、高、像素、基线偏移x、基线偏移y。

字体_光栅化字形 接受 font、glyphID、字号：
    outline 为 做 字体_提取字形 用 font、glyphID。
    若 outline 等于 0：
        返还 0。
    完。
    缩放 为 字号 乘 1024 除以 font 的 unitsPerEm。
    宽 为 (outline 的 xMax 减 outline 的 xMin) 乘 缩放 除以 1024 加 2。
    高 为 (outline 的 yMax 减 outline 的 yMin) 乘 缩放 除以 1024 加 2。
    若 宽 小于或等于 0 或者 高 小于或等于 0：
        返还 0。
    完。
    若 宽 大于 256：
        宽 为 256。
    完。
    若 高 大于 256：
        高 为 256。
    完。
    bmp 为 做 堆_分配 用 GlyphBitmap。
    bmp 的 宽 为 宽。
    bmp 的 高 为 高。
    bmp 的 像素 为 做 堆_分配_数组 用 宽 乘 高。
    做 内存_填零 用 bmp 的 像素、宽 乘 高。
    bmp 的 基线偏移x 为 outline 的 xMin 乘 缩放 除以 1024。
    bmp 的 基线偏移y 为 outline 的 yMax 乘 缩放 除以 1024。
    可变 轮廓 为 0。
    可变 点起始 为 0。
    只要 轮廓 小于 outline 的 轮廓数：
        点结束 为 做 读16 用 outline 的 轮廓结束 加 轮廓 乘 2。
        可变 p 为 点起始。
        只要 p 小于或等于 点结束：
            下一个 为 若 p 小于 点结束 则 p 加 1 否则 点起始。
            x0 为 (做 读16有符号 用 outline 的 点x 加 p 乘 2 减 outline 的 xMin) 乘 缩放 除以 1024。
            y0 为 (outline 的 yMax 减 做 读16有符号 用 outline 的 点y 加 p 乘 2) 乘 缩放 除以 1024。
            x1 为 (做 读16有符号 用 outline 的 点x 加 下一个 乘 2 减 outline 的 xMin) 乘 缩放 除以 1024。
            y1 为 (outline 的 yMax 减 做 读16有符号 用 outline 的 点y 加 下一个 乘 2) 乘 缩放 除以 1024。
            标志 为 做 内存_读8 用 outline 的 点标志 加 p。
            若 标志 位与 0x01：
                做 光栅化_线段 用 bmp、x0、y0、x1、y1。
            否则：
                做 光栅化_二次贝塞尔 用 bmp、x0、y0、x1、y1、x0、y0。
            完。
            p 为 p 加 1。
        完。
        点起始 为 点结束 加 1。
        轮廓 为 轮廓 加 1。
    完。
    做 光栅化_填充 用 bmp。
    返还 bmp。
完。

光栅化_线段 接受 bmp、x0、y0、x1、y1：
    若 y0 等于 y1：
        返还。
    完。
    可变 sx 为 x0。
    可变 sy 为 y0。
    可变 ex 为 x1。
    可变 ey 为 y1。
    方向 为 1。
    若 y0 大于 y1：
        sx 为 x1。
        sy 为 y1。
        ex 为 x0。
        ey 为 y0。
        方向 为 负 1。
    完。
    dy 为 ey 减 sy。
    若 dy 等于 0：
        返还。
    完。
    dx_per_y 为 (ex 减 sx) 乘 1024 除以 dy。
    可变 y 为 sy。
    可变 x_q10 为 sx 乘 1024。
    只要 y 小于 ey 并且 y 小于 bmp 的 高：
        若 y 大于或等于 0：
            x 为 x_q10 除以 1024。
            若 x 大于或等于 0 并且 x 小于 bmp 的 宽：
                偏移 为 y 乘 bmp 的 宽 加 x。
                当前 为 做 内存_读8 用 bmp 的 像素 加 偏移。
                做 内存_写8 用 bmp 的 像素 加 偏移、当前 加 方向 乘 32 位与 0xFF。
            完。
        完。
        x_q10 为 x_q10 加 dx_per_y。
        y 为 y 加 1。
    完。
完。

光栅化_填充 接受 bmp：
    可变 y 为 0。
    只要 y 小于 bmp 的 高：
        可变 wind 为 0。
        可变 x 为 0。
        只要 x 小于 bmp 的 宽：
            偏移 为 y 乘 bmp 的 宽 加 x。
            v 为 做 内存_读8 用 bmp 的 像素 加 偏移。
            wind 为 wind 加 v。
            若 wind 不等于 0：
                覆盖 为 若 wind 大于 255 则 255 否则 若 wind 小于 0 则 0 否则 wind。
                做 内存_写8 用 bmp 的 像素 加 偏移、覆盖。
            否则：
                做 内存_写8 用 bmp 的 像素 加 偏移、0。
            完。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
完。

本章 收束。
