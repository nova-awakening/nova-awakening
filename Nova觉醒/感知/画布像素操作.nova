《画布像素操作》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《.·属性自定义属》。

引用《.·样式滤镜》。

引用《..·执行·底层内存操》。

图像Data 是 一种 事物，含 宽、高、数据。

画布2D_create图像Data 接受 ctx、宽、高：
    id 为 做 堆_分配 用 图像Data。
    id 的 宽 为 宽。
    id 的 高 为 高。
    id 的 数据 为 做 堆_分配 用 宽 乘 高 乘 4。
    做 内存_清零 用 id 的 数据、宽 乘 高 乘 4。
    返还 id。
完。

画布2D_get图像Data 接受 ctx、x、y、宽、高：
    id 为 做 画布2D_create图像Data 用 ctx、宽、高。
    可变 dy 为 0。
    只要 dy 小于 高：
        可变 dx 为 0。
        只要 dx 小于 宽：
            px 为 x 加 dx。
            py 为 y 加 dy。
            可变 颜色 为 0。
            若 px 大于或等于 0 并且 px 小于 ctx 的 宽 并且 py 大于或等于 0 并且 py 小于 ctx 的 高：
                颜色 为 做 Nova缓冲itmap_获取像素 用 ctx 的 位图、px、py。
            完。
            dst 为 id 的 数据 加 (dy 乘 宽 加 dx) 乘 4。
            做 内存_写8 用 dst、做 SkColor_R 用 颜色。
            做 内存_写8 用 dst 加 1、做 SkColor_G 用 颜色。
            做 内存_写8 用 dst 加 2、做 SkColor_B 用 颜色。
            做 内存_写8 用 dst 加 3、做 SkColor_A 用 颜色。
            dx 为 dx 加 1。
        完。
        dy 为 dy 加 1。
    完。
    返还 id。
完。

画布2D_put图像Data 接受 ctx、id、x、y：
    若 id 等于 0：
        返还。
    完。
    可变 dy 为 0。
    只要 dy 小于 id 的 高：
        可变 dx 为 0。
        只要 dx 小于 id 的 宽：
            src 为 id 的 数据 加 (dy 乘 id 的 宽 加 dx) 乘 4。
            r 为 做 内存_读8 用 src。
            g 为 做 内存_读8 用 src 加 1。
            b 为 做 内存_读8 用 src 加 2。
            a 为 做 内存_读8 用 src 加 3。
            px 为 x 加 dx。
            py 为 y 加 dy。
            若 px 大于或等于 0 并且 px 小于 ctx 的 宽 并且 py 大于或等于 0 并且 py 小于 ctx 的 高：
                做 Nova缓冲itmap_设置像素 用 ctx 的 位图、px、py、做 SkColor 用 a、r、g、b。
            完。
            dx 为 dx 加 1。
        完。
        dy 为 dy 加 1。
    完。
完。

画布2D_createLinearGradient 接受 ctx、x0、y0、x1、y1：
    返还 做 LinearGradient_创建 用 x0、y0、x1、y1。
完。

画布2D_createRadialGradient 接受 ctx、x0、y0、r0、x1、y1、r1：
    返还 做 RadialGradient_创建 用 x1、y1、r1。
完。

画布2D_创建填充画笔 接受 ctx：
    p 为 做 Sk绘制_创建。
    p 的 颜色 为 ctx 的 当前状态 的 填充颜色。
    若 ctx 的 当前状态 的 全局透明度 小于 10000：
        a 为 做 SkColor_A 用 p 的 颜色 乘 ctx 的 当前状态 的 全局透明度 除以 10000。
        p 的 颜色 为 做 SkColor 用 a、做 SkColor_R 用 p 的 颜色、做 SkColor_G 用 p 的 颜色、做 SkColor_B 用 p 的 颜色。
    完。
    p 的 风格 为 Sk绘制风格_填充。
    p 的 抗锯齿 为 真。
    返还 p。
完。

画布2D_创建描边画笔 接受 ctx：
    p 为 做 Sk绘制_创建。
    p 的 颜色 为 ctx 的 当前状态 的 描边颜色。
    若 ctx 的 当前状态 的 全局透明度 小于 10000：
        a 为 做 SkColor_A 用 p 的 颜色 乘 ctx 的 当前状态 的 全局透明度 除以 10000。
        p 的 颜色 为 做 SkColor 用 a、做 SkColor_R 用 p 的 颜色、做 SkColor_G 用 p 的 颜色、做 SkColor_B 用 p 的 颜色。
    完。
    p 的 风格 为 Sk绘制风格_描边。
    p 的 描边宽度 为 ctx 的 当前状态 的 线宽。
    p 的 端帽 为 ctx 的 当前状态 的 线帽。
    p 的 连接 为 ctx 的 当前状态 的 线接。
    p 的 斜接限制 为 ctx 的 当前状态 的 斜接限。
    p 的 抗锯齿 为 真。
    返还 p。
完。

定点_cos 接受 角度：
    可变 x 为 角度 取余于 62832。
    若 x 小于 0：
        x 为 x 加 62832。
    完。
    x2 为 x 乘 x 除以 100000000。
    x4 为 x2 乘 x2 除以 10000。
    返还 10000 减 x2 乘 5000 除以 10000 加 x4 乘 417 除以 10000。
完。

定点_sin 接受 角度：
    返还 做 定点_cos 用 角度 减 15708。
完。

本章 收束。
