《SVG路径解析辅助》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《.·矢量图创建》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《.·边界框计算》。

引用《..·执行·控制台输出》。

SVG命令_M 为 77。

SVG命令_L 为 76。

SVG命令_C 为 67。

SVG命令_Q 为 81。

SVG命令_Z 为 90。

SVG命令_A 为 65。

SVG命令_H 为 72。

SVG命令_V 为 86。

SVG命令_S 为 83。

SVG命令_T 为 84。

圆周率_Q10 为 3217。

贝塞尔椭圆魔数 为 565。

解析器辅助统计 是 一种 事物，含 数字解析次数、命令识别次数、空白跳过次数、rect转换次数、circle转换次数、ellipse转换次数、line转换次数、polygon转换次数、polyline转换次数、star生成次数、弧线转换次数、路径反转次数、路径连接次数、AI形状预测次数、AI形状命中次数、解析错误次数、总解析耗时_纳秒、科学记数法次数。

AI形状状态 是 一种 事物，含 历史形状分布、最近形状类型、连续相同形状数、预测下次形状、历史平均顶点数、采样计数器、学习权重、历史圆角率。

可变 全局解析辅助统计 为 0。

可变 全局形状AI 为 0。

可变 全局解析辅助已初始化 为 假。

SVG辅助_初始化 接受 零参：
    若 全局解析辅助已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局解析辅助统计 为 做 堆_分配_清零 用 144。
    若 全局解析辅助统计 等于 空：
        返还 错误_内存不足。
    完。
    全局形状AI 为 做 堆_分配_清零 用 48。
    若 全局形状AI 不等于 空：
        全局形状AI 的 历史形状分布 为 做 堆_分配_清零 用 8 乘 8。
        全局形状AI 的 学习权重 为 128。
    完。
    全局解析辅助已初始化 为 真。
    做 打印 用 「[SVG辅助] 初始化完成」。
    返还 0。
完。

SVG辅助_销毁 接受 零参：
    若 全局形状AI 不等于 空：
        若 全局形状AI 的 历史形状分布 不等于 空：
            做 堆_释放 用 全局形状AI 的 历史形状分布。
        完。
        做 堆_释放 用 全局形状AI。
        全局形状AI 为 0。
    完。
    若 全局解析辅助统计 不等于 空：
        做 堆_释放 用 全局解析辅助统计。
        全局解析辅助统计 为 0。
    完。
    全局解析辅助已初始化 为 假。
    返还 0。
完。

SVG辅助_重置 接受 零参：
    若 全局解析辅助统计 不等于 空：
        做 内存_置零 用 全局解析辅助统计、144。
    完。
    返还 0。
完。

SVGParser_跳过空白 接受 p：
    只要 p 的 位置 小于 p 的 长度：
        ch 为 做 字节取 用 p 的 输入、p 的 位置。
        若 ch 等于 32 或者 ch 等于 9 或者 ch 等于 10 或者 ch 等于 13 或者 ch 等于 44：
            p 的 位置 为 p 的 位置 加 1。
        否则：
            返还。
        完。
    完。
完。

SVGParser_是命令 接受 ch：
    返还 ch 等于 77 或者 ch 等于 109 或者 ch 等于 76 或者 ch 等于 108 或者 ch 等于 72 或者 ch 等于 104 或者 ch 等于 86 或者 ch 等于 118 或者 ch 等于 67 或者 ch 等于 99 或者 ch 等于 83 或者 ch 等于 115 或者 ch 等于 81 或者 ch 等于 113 或者 ch 等于 84 或者 ch 等于 116 或者 ch 等于 65 或者 ch 等于 97 或者 ch 等于 90 或者 ch 等于 122。
完。

SVGParser_读数字 接受 p：
    做 SVGParser_跳过空白 用 p。
    若 p 的 位置 大于或等于 p 的 长度：
        返还 0。
    完。
    可变 符号 为 1。
    若 做 字节取 用 p 的 输入、p 的 位置 等于 45：
        符号 为 负 1。
        p 的 位置 为 p 的 位置 加 1。
    否则若 做 字节取 用 p 的 输入、p 的 位置 等于 43：
        p 的 位置 为 p 的 位置 加 1。
    完。
    可变 整数 为 0。
    只要 p 的 位置 小于 p 的 长度 并且 做 字节取 用 p 的 输入、p 的 位置 大于或等于 48 并且 做 字节取 用 p 的 输入、p 的 位置 小于或等于 57：
        整数 为 整数 乘 10 加 (做 字节取 用 p 的 输入、p 的 位置 减 48)。
        p 的 位置 为 p 的 位置 加 1。
    完。
    可变 小数 为 0。
    可变 除数 为 1。
    若 p 的 位置 小于 p 的 长度 并且 做 字节取 用 p 的 输入、p 的 位置 等于 46：
        p 的 位置 为 p 的 位置 加 1。
        只要 p 的 位置 小于 p 的 长度 并且 做 字节取 用 p 的 输入、p 的 位置 大于或等于 48 并且 做 字节取 用 p 的 输入、p 的 位置 小于或等于 57：
            小数 为 小数 乘 10 加 (做 字节取 用 p 的 输入、p 的 位置 减 48)。
            除数 为 除数 乘 10。
            p 的 位置 为 p 的 位置 加 1。
        完。
    完。
    结果 为 整数 乘 1024 加 小数 乘 1024 除以 除数。
    返还 结果 乘 符号。
完。

SVG_rect到path 接受 x、y、w、h、rx、ry：
    path 为 做 SVGPath_创建。
    若 rx 等于 0 并且 ry 等于 0：
        做 SVGPath_添加段 用 path、SVG命令_M、x、y、0、0、0、0、0。
        做 SVGPath_添加段 用 path、SVG命令_L、x 加 w、y、0、0、0、0、0。
        做 SVGPath_添加段 用 path、SVG命令_L、x 加 w、y 加 h、0、0、0、0、0。
        做 SVGPath_添加段 用 path、SVG命令_L、x、y 加 h、0、0、0、0、0。
        做 SVGPath_添加段 用 path、SVG命令_Z、x、y、0、0、0、0、0。
    否则：
        若 ry 等于 0：
            ry 为 rx。
        完。
        做 SVGPath_添加段 用 path、SVG命令_M、x 加 rx、y、0、0、0、0、0。
        做 SVGPath_添加段 用 path、SVG命令_L、x 加 w 减 rx、y、0、0、0、0、0。
        做 SVGPath_添加段 用 path、SVG命令_A、rx、ry、0、0、1、x 加 w、y 加 ry。
        做 SVGPath_添加段 用 path、SVG命令_L、x 加 w、y 加 h 减 ry、0、0、0、0、0。
        做 SVGPath_添加段 用 path、SVG命令_A、rx、ry、0、0、1、x 加 w 减 rx、y 加 h。
        做 SVGPath_添加段 用 path、SVG命令_L、x 加 rx、y 加 h、0、0、0、0、0。
        做 SVGPath_添加段 用 path、SVG命令_A、rx、ry、0、0、1、x、y 加 h 减 ry。
        做 SVGPath_添加段 用 path、SVG命令_L、x、y 加 ry、0、0、0、0、0。
        做 SVGPath_添加段 用 path、SVG命令_A、rx、ry、0、0、1、x 加 rx、y。
        做 SVGPath_添加段 用 path、SVG命令_Z、x 加 rx、y、0、0、0、0、0。
    完。
    做 SVGPath_计算边界 用 path。
    返还 path。
完。

SVG_circle到path 接受 cx、cy、r：
    返还 做 SVG_ellipse到path 用 cx、cy、r、r。
完。

SVG_ellipse到path 接受 cx、cy、rx、ry：
    path 为 做 SVGPath_创建。
    kx 为 rx 乘 565 除以 1024。
    ky 为 ry 乘 565 除以 1024。
    做 SVGPath_添加段 用 path、SVG命令_M、cx 加 rx、cy、0、0、0、0、0。
    做 SVGPath_添加段 用 path、SVG命令_C、cx 加 rx、cy 加 ky、cx 加 kx、cy 加 ry、cx、cy 加 ry、0。
    做 SVGPath_添加段 用 path、SVG命令_C、cx 减 kx、cy 加 ry、cx 减 rx、cy 加 ky、cx 减 rx、cy、0。
    做 SVGPath_添加段 用 path、SVG命令_C、cx 减 rx、cy 减 ky、cx 减 kx、cy 减 ry、cx、cy 减 ry、0。
    做 SVGPath_添加段 用 path、SVG命令_C、cx 加 kx、cy 减 ry、cx 加 rx、cy 减 ky、cx 加 rx、cy、0。
    做 SVGPath_添加段 用 path、SVG命令_Z、cx 加 rx、cy、0、0、0、0、0。
    做 SVGPath_计算边界 用 path。
    返还 path。
完。

SVG_line到path 接受 x1、y1、x2、y2：
    path 为 做 SVGPath_创建。
    做 SVGPath_添加段 用 path、SVG命令_M、x1、y1、0、0、0、0、0。
    做 SVGPath_添加段 用 path、SVG命令_L、x2、y2、0、0、0、0、0。
    做 SVGPath_计算边界 用 path。
    返还 path。
完。

SVG_polygon到path 接受 点数组、点数：
    若 点数组 等于 0 或者 点数 小于 2：
        返还 0。
    完。
    path 为 做 SVGPath_创建。
    做 SVGPath_添加段 用 path、SVG命令_M、做 内存_读32 用 点数组、做 内存_读32 用 点数组 加 4、0、0、0、0、0。
    可变 i 为 1。
    只要 i 小于 点数：
        做 SVGPath_添加段 用 path、SVG命令_L、做 内存_读32 用 点数组 加 i 乘 8、做 内存_读32 用 点数组 加 i 乘 8 加 4、0、0、0、0、0。
        i 为 i 加 1。
    完。
    做 SVGPath_添加段 用 path、SVG命令_Z、做 内存_读32 用 点数组、做 内存_读32 用 点数组 加 4、0、0、0、0、0。
    做 SVGPath_计算边界 用 path。
    返还 path。
完。

SVG_polyline到path 接受 点数组、点数：
    若 点数组 等于 0 或者 点数 小于 2：
        返还 0。
    完。
    path 为 做 SVGPath_创建。
    做 SVGPath_添加段 用 path、SVG命令_M、做 内存_读32 用 点数组、做 内存_读32 用 点数组 加 4、0、0、0、0、0。
    可变 i 为 1。
    只要 i 小于 点数：
        做 SVGPath_添加段 用 path、SVG命令_L、做 内存_读32 用 点数组 加 i 乘 8、做 内存_读32 用 点数组 加 i 乘 8 加 4、0、0、0、0、0。
        i 为 i 加 1。
    完。
    做 SVGPath_计算边界 用 path。
    若 全局解析辅助统计 不等于 空：
        全局解析辅助统计 的 polyline转换次数 为 全局解析辅助统计 的 polyline转换次数 加 1。
    完。
    返还 path。
完。

SVG_定点cos近似 接受 角度_Q10：
    x2 为 角度_Q10 乘 角度_Q10 除以 1024。
    x4 为 x2 乘 x2 除以 1024。
    返还 1024 减 x2 除以 2 加 x4 除以 24。
完。

SVG_定点sin近似 接受 角度_Q10：
    x2 为 角度_Q10 乘 角度_Q10 除以 1024。
    x3 为 x2 乘 角度_Q10 除以 1024。
    x5 为 x3 乘 x2 除以 1024。
    返还 角度_Q10 减 x3 除以 6 加 x5 除以 120。
完。

SVG_正多边形到path 接受 cx、cy、r、边数：
    若 边数 小于 3 或者 边数 大于 360：
        返还 空。
    完。
    若 r 小于或等于 0：
        返还 空。
    完。
    path 为 做 SVGPath_创建。
    若 path 等于 空：
        返还 空。
    完。
    步长 为 6434 除以 边数。
    可变 i 为 0。
    只要 i 小于 边数：
        角度 为 i 乘 步长 减 圆周率_Q10 除以 2。
        px 为 cx 加 r 乘 做 SVG_定点cos近似 用 角度 除以 1024。
        py 为 cy 加 r 乘 做 SVG_定点sin近似 用 角度 除以 1024。
        若 i 等于 0：
            做 SVGPath_添加段 用 path、SVG命令_M、px、py、0、0、0、0、0。
        否则：
            做 SVGPath_添加段 用 path、SVG命令_L、px、py、0、0、0、0、0。
        完。
        i 为 i 加 1。
    完。
    做 SVGPath_添加段 用 path、SVG命令_Z、0、0、0、0、0、0、0。
    做 SVGPath_计算边界 用 path。
    返还 path。
完。

SVG_星形到path 接受 cx、cy、外径、内径、角数：
    若 角数 小于 3 或者 角数 大于 36：
        返还 空。
    完。
    若 外径 小于或等于 0 或者 内径 小于或等于 0：
        返还 空。
    完。
    path 为 做 SVGPath_创建。
    若 path 等于 空：
        返还 空。
    完。
    总顶点 为 角数 乘 2。
    步长 为 6434 除以 总顶点。
    可变 i 为 0。
    只要 i 小于 总顶点：
        角度 为 i 乘 步长 减 圆周率_Q10 除以 2。
        可变 半径 为 外径。
        若 i 取余于 2 等于 1：
            半径 为 内径。
        完。
        px 为 cx 加 半径 乘 做 SVG_定点cos近似 用 角度 除以 1024。
        py 为 cy 加 半径 乘 做 SVG_定点sin近似 用 角度 除以 1024。
        若 i 等于 0：
            做 SVGPath_添加段 用 path、SVG命令_M、px、py、0、0、0、0、0。
        否则：
            做 SVGPath_添加段 用 path、SVG命令_L、px、py、0、0、0、0、0。
        完。
        i 为 i 加 1。
    完。
    做 SVGPath_添加段 用 path、SVG命令_Z、0、0、0、0、0、0、0。
    做 SVGPath_计算边界 用 path。
    若 全局解析辅助统计 不等于 空：
        全局解析辅助统计 的 star生成次数 为 全局解析辅助统计 的 star生成次数 加 1。
    完。
    返还 path。
完。

SVG_弧线到贝塞尔 接受 path、x1、y1、rx、ry、旋转、大弧标志、顺时针、x2、y2：
    若 path 等于 空：
        返还。
    完。
    若 rx 小于或等于 0 或者 ry 小于或等于 0：
        做 SVGPath_添加段 用 path、SVG命令_L、x2、y2、0、0、0、0、0。
        返还。
    完。
    dx 为 x2 减 x1。
    dy 为 y2 减 y1。
    距离平方 为 dx 乘 dx 加 dy 乘 dy。
    若 距离平方 小于 4：
        返还。
    完。
    中x 为 (x1 加 x2) 除以 2。
    中y 为 (y1 加 y2) 除以 2。
    可变 法x 为 0 减 dy。
    可变 法y 为 dx。
    可变 法长 为 法x。
    若 法长 小于 0：
        法长 为 0 减 法长。
    完。
    可变 法长y 为 法y。
    若 法长y 小于 0：
        法长y 为 0 减 法长y。
    完。
    总长 为 法长 加 法长y。
    若 总长 等于 0：
        总长 为 1。
    完。
    弧高 为 (rx 加 ry) 除以 4。
    若 大弧标志 等于 真：
        弧高 为 (rx 加 ry) 除以 2。
    完。
    控制x 为 中x 加 法x 乘 弧高 除以 总长。
    控制y 为 中y 加 法y 乘 弧高 除以 总长。
    cp1x 为 x1 加 (控制x 减 x1) 乘 683 除以 1024。
    cp1y 为 y1 加 (控制y 减 y1) 乘 683 除以 1024。
    cp2x 为 x2 加 (控制x 减 x2) 乘 683 除以 1024。
    cp2y 为 y2 加 (控制y 减 y2) 乘 683 除以 1024。
    做 SVGPath_添加段 用 path、SVG命令_C、cp1x、cp1y、cp2x、cp2y、x2、y2、0。
    若 全局解析辅助统计 不等于 空：
        全局解析辅助统计 的 弧线转换次数 为 全局解析辅助统计 的 弧线转换次数 加 1。
    完。
完。

SVG_计算路径长度 接受 线段、线段数：
    若 线段 等于 空 或者 线段数 小于或等于 0：
        返还 0。
    完。
    可变 总长度 为 0。
    可变 i 为 0。
    只要 i 小于 线段数：
        偏移 为 i 乘 16。
        dx 为 做 内存_读32 用 线段 加 偏移 加 8 减 做 内存_读32 用 线段 加 偏移。
        dy 为 做 内存_读32 用 线段 加 偏移 加 12 减 做 内存_读32 用 线段 加 偏移 加 4。
        可变 adx 为 dx。
        若 adx 小于 0：
            adx 为 0 减 adx。
        完。
        可变 ady 为 dy。
        若 ady 小于 0：
            ady 为 0 减 ady。
        完。
        总长度 为 总长度 加 adx 加 ady。
        i 为 i 加 1。
    完。
    返还 总长度。
完。

SVG_路径段数 接受 path：
    若 path 等于 空：
        返还 0。
    完。
    可变 计数 为 0。
    可变 seg 为 path 的 段链头。
    只要 seg 不等于 空：
        计数 为 计数 加 1。
        seg 为 seg 的 下一个。
    完。
    返还 计数。
完。

SVG_路径包含曲线 接受 path：
    若 path 等于 空：
        返还 假。
    完。
    可变 seg 为 path 的 段链头。
    只要 seg 不等于 空：
        若 seg 的 命令 等于 SVG命令_C 或者 seg 的 命令 等于 SVG命令_Q 或者 seg 的 命令 等于 SVG命令_S 或者 seg 的 命令 等于 SVG命令_T 或者 seg 的 命令 等于 SVG命令_A：
            返还 真。
        完。
        seg 为 seg 的 下一个。
    完。
    返还 假。
完。

SVGParser_读数字_增强 接受 p：
    做 SVGParser_跳过空白 用 p。
    若 p 的 位置 大于或等于 p 的 长度：
        返还 0。
    完。
    基础值 为 做 SVGParser_读数字 用 p。
    若 p 的 位置 小于 p 的 长度：
        ch 为 做 字节取 用 p 的 输入、p 的 位置。
        若 ch 等于 101 或者 ch 等于 69：
            p 的 位置 为 p 的 位置 加 1。
            可变 指数符号 为 1。
            若 p 的 位置 小于 p 的 长度：
                符号ch 为 做 字节取 用 p 的 输入、p 的 位置。
                若 符号ch 等于 45：
                    指数符号 为 负 1。
                    p 的 位置 为 p 的 位置 加 1。
                否则若 符号ch 等于 43：
                    p 的 位置 为 p 的 位置 加 1。
                完。
            完。
            可变 指数值 为 0。
            只要 p 的 位置 小于 p 的 长度 并且 做 字节取 用 p 的 输入、p 的 位置 大于或等于 48 并且 做 字节取 用 p 的 输入、p 的 位置 小于或等于 57：
                指数值 为 指数值 乘 10 加 (做 字节取 用 p 的 输入、p 的 位置 减 48)。
                p 的 位置 为 p 的 位置 加 1。
            完。
            可变 e 为 0。
            若 指数符号 大于 0：
                只要 e 小于 指数值 并且 e 小于 6：
                    基础值 为 基础值 乘 10。
                    e 为 e 加 1。
                完。
            否则：
                只要 e 小于 指数值 并且 e 小于 6：
                    基础值 为 基础值 除以 10。
                    e 为 e 加 1。
                完。
            完。
            若 全局解析辅助统计 不等于 空：
                全局解析辅助统计 的 科学记数法次数 为 全局解析辅助统计 的 科学记数法次数 加 1。
            完。
        完。
    完。
    若 全局解析辅助统计 不等于 空：
        全局解析辅助统计 的 数字解析次数 为 全局解析辅助统计 的 数字解析次数 加 1。
    完。
    返还 基础值。
完。

AI形状_记录 接受 形状类型、顶点数：
    若 全局形状AI 等于 空：
        返还。
    完。
    AI 为 全局形状AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    若 AI 的 历史形状分布 不等于 空 并且 形状类型 小于 8：
        当前 为 做 内存_读64 用 AI 的 历史形状分布 加 形状类型 乘 8。
        做 内存_写64 用 AI 的 历史形状分布 加 形状类型 乘 8、当前 加 1。
    完。
    AI 的 历史平均顶点数 为 (AI 的 历史平均顶点数 乘 7 加 顶点数 乘 1024) 除以 8。
    若 形状类型 等于 AI 的 最近形状类型：
        AI 的 连续相同形状数 为 AI 的 连续相同形状数 加 1。
    否则：
        AI 的 连续相同形状数 为 1。
        AI 的 最近形状类型 为 形状类型。
    完。
    若 AI 的 采样计数器 小于 16：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 全局解析辅助统计 不等于 空：
        全局解析辅助统计 的 AI形状预测次数 为 全局解析辅助统计 的 AI形状预测次数 加 1。
    完。
    可变 最大计数 为 0。
    可变 最大类型 为 0。
    若 AI 的 历史形状分布 不等于 空：
        可变 t 为 0。
        只要 t 小于 8：
            计数 为 做 内存_读64 用 AI 的 历史形状分布 加 t 乘 8。
            若 计数 大于 最大计数：
                最大计数 为 计数。
                最大类型 为 t。
            完。
            t 为 t 加 1。
        完。
    完。
    若 AI 的 连续相同形状数 大于 5：
        AI 的 预测下次形状 为 AI 的 最近形状类型。
    否则：
        AI 的 预测下次形状 为 最大类型。
    完。
    若 AI 的 预测下次形状 等于 形状类型：
        若 全局解析辅助统计 不等于 空：
            全局解析辅助统计 的 AI形状命中次数 为 全局解析辅助统计 的 AI形状命中次数 加 1。
        完。
    完。
完。

SVG辅助_打印统计 接受 零参：
    若 全局解析辅助统计 等于 空：
        做 打印 用 「[SVG辅助] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   SVG路径辅助 · 统计报告            │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 数字解析:         」、做 转字符串 用 全局解析辅助统计 的 数字解析次数。
    做 打印 用 做 字符串拼接 用 「│ 命令识别:         」、做 转字符串 用 全局解析辅助统计 的 命令识别次数。
    做 打印 用 做 字符串拼接 用 「│ rect转换:         」、做 转字符串 用 全局解析辅助统计 的 rect转换次数。
    做 打印 用 做 字符串拼接 用 「│ circle转换:       」、做 转字符串 用 全局解析辅助统计 的 circle转换次数。
    做 打印 用 做 字符串拼接 用 「│ ellipse转换:      」、做 转字符串 用 全局解析辅助统计 的 ellipse转换次数。
    做 打印 用 做 字符串拼接 用 「│ polygon转换:      」、做 转字符串 用 全局解析辅助统计 的 polygon转换次数。
    做 打印 用 做 字符串拼接 用 「│ star生成:         」、做 转字符串 用 全局解析辅助统计 的 star生成次数。
    做 打印 用 做 字符串拼接 用 「│ 弧线转换:         」、做 转字符串 用 全局解析辅助统计 的 弧线转换次数。
    做 打印 用 做 字符串拼接 用 「│ 科学记数法:       」、做 转字符串 用 全局解析辅助统计 的 科学记数法次数。
    做 打印 用 做 字符串拼接 用 「│ AI形状预测:       」、做 转字符串 用 全局解析辅助统计 的 AI形状预测次数。
    做 打印 用 做 字符串拼接 用 「│ AI形状命中:       」、做 转字符串 用 全局解析辅助统计 的 AI形状命中次数。
    做 打印 用 做 字符串拼接 用 「│ 解析错误:         」、做 转字符串 用 全局解析辅助统计 的 解析错误次数。
    做 打印 用 做 字符串拼接 用 「│ 总耗时(μs):       」、做 转字符串 用 全局解析辅助统计 的 总解析耗时_纳秒 除以 1000。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

SVG辅助_导出诊断 接受 零参：
    若 全局形状AI 等于 空：
        做 打印 用 「[SVG辅助 AI] 未初始化」。
        返还。
    完。
    AI 为 全局形状AI。
    做 打印 用 「[SVG辅助 AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  最近形状: 」、做 转字符串 用 AI 的 最近形状类型。
    做 打印 用 做 字符串拼接 用 「  连续相同: 」、做 转字符串 用 AI 的 连续相同形状数。
    做 打印 用 做 字符串拼接 用 「  预测下次: 」、做 转字符串 用 AI 的 预测下次形状。
    做 打印 用 做 字符串拼接 用 「  平均顶点(Q10): 」、做 转字符串 用 AI 的 历史平均顶点数。
完。

SVG辅助_获取统计 接受 零参：
    返还 全局解析辅助统计。
完。

SVG辅助_获取AI状态 接受 零参：
    返还 全局形状AI。
完。

SVG辅助_获取预测形状 接受 零参：
    若 全局形状AI 等于 空：
        返还 0。
    完。
    返还 全局形状AI 的 预测下次形状。
完。

本章 收束。
