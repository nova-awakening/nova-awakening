《脚本工线内》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·整数转字符串》。

引用《.·文档事件》。

引用《..·中枢·辅助存根》。

工线_importScripts 接受 w、URL列表、URL数量：
    若 w 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 URL数量：
        url 为 做 内存_读64 用 URL列表 加 i 乘 8。
        若 url 不等于 0：
            内容 为 做 网络_同步获取 用 url。
            若 内容 不等于 0：
            完。
        完。
        i 为 i 加 1。
    完。
完。

Shared工线 是 一种 事物，含 脚本URL、名称、状态、Nova运行时实例、端口列表、端口数量、端口容量。

可变 共享工线列表 为 0。

可变 共享工线数量 为 0。

可变 共享工线容量 为 0。

Shared工线_创建 接受 脚本URL、名称：
    已有 为 做 Shared工线_查找 用 脚本URL、名称。
    若 已有 不等于 0：
        ch 为 做 MessageChannel_创建。
        做 Shared工线_添加端口 用 已有、ch 的 port2。
        返还 ch 的 port1。
    完。
    sw 为 做 堆_分配 用 Shared工线。
    sw 的 脚本URL 为 做 字符串_复制 用 脚本URL。
    sw 的 名称 为 若 名称 不等于 0 则 做 字符串_复制 用 名称 否则 「」。
    sw 的 状态 为 WORKER_STATE_PENDING。
    sw 的 Nova运行时实例 为 做 Nova运行时_创建。
    sw 的 端口容量 为 32。
    sw 的 端口列表 为 做 堆_分配_数组 用 32 乘 8。
    sw 的 端口数量 为 0。
    若 共享工线列表 等于 0：
        共享工线容量 为 16。
        共享工线列表 为 做 堆_分配_数组 用 16 乘 8。
        共享工线数量 为 0。
    完。
    若 共享工线数量 小于 共享工线容量：
        做 内存_写64 用 共享工线列表 加 共享工线数量 乘 8、sw。
        共享工线数量 为 共享工线数量 加 1。
    完。
    ch 为 做 MessageChannel_创建。
    做 Shared工线_添加端口 用 sw、ch 的 port2。
    sw 的 状态 为 WORKER_STATE_RUNNING。
    返还 ch 的 port1。
完。

Shared工线_查找 接受 脚本URL、名称：
    可变 i 为 0。
    只要 i 小于 共享工线数量：
        sw 为 做 内存_读64 用 共享工线列表 加 i 乘 8。
        若 sw 不等于 0 并且 做 字符串_相等 用 sw 的 脚本URL、脚本URL：
            若 名称 等于 0 或者 做 字符串_相等 用 sw 的 名称、名称：
                返还 sw。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

Shared工线_添加端口 接受 sw、port：
    若 sw 等于 0 或者 sw 的 端口数量 大于或等于 sw 的 端口容量：
        返还。
    完。
    做 内存_写64 用 sw 的 端口列表 加 sw 的 端口数量 乘 8、port。
    sw 的 端口数量 为 sw 的 端口数量 加 1。
    port 的 所属worker 为 sw。
完。

本章 收束。
