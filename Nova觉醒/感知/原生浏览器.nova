《原生浏览器》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

进程类型_浏览器 为 0。

进程类型_渲染器 为 1。

进程类型_图处 为 2。

进程类型_网络 为 3。

进程类型_工具 为 4。

系统调用_进程_创建 为 600。

系统调用_进程_等待 为 601。

系统调用_进程_终止 为 602。

系统调用_进程_获取PID 为 603。

系统调用_共享内存_创建 为 610。

系统调用_共享内存_映射 为 611。

系统调用_共享内存_释放 为 612。

系统调用_管道_创建 为 620。

系统调用_管道_读 为 621。

系统调用_管道_写 为 622。

系统调用_管道_关闭 为 623。

系统调用_沙箱_进入 为 630。

系统调用_沙箱_策略 为 631。

进程信息 是 一种 事物，含 PID、类型、状态、站点键、IPC通道、沙箱级别、内存上限、CPU上限、创建时间、崩溃次数。

Nova通信_请求_导航 为 100。

Nova通信_请求_执行JS 为 101。

Nova通信_响应_标题更新 为 102。

Nova通信_响应_图标更新 为 103。

Nova通信_响应_加载完成 为 104。

Nova通信_请求_网络发送 为 200。

Nova通信_响应_网络数据 为 201。

Nova通信_响应_网络完成 为 202。

Nova通信_响应_网络错误 为 203。

Nova通信_请求_图处创建纹理 为 300。

Nova通信_请求_图处上传纹理 为 301。

Nova通信_请求_图处合成帧 为 302。

Nova通信_响应_图处帧完成 为 303。

Nova通信_请求_AI语义分类 为 400。

Nova通信_响应_AI语义分类 为 401。

Nova通信_请求_AI渲染质量 为 402。

Nova通信_响应_AI渲染质量 为 403。

Nova通信_请求_AI资源优先级 为 404。

Nova通信_响应_AI资源优先级 为 405。

Nova通信_通知_AI行为事件 为 410。

IPC消息 是 一种 事物，含 大小、类型、序列号、发送者PID、数据。

本章 收束。
