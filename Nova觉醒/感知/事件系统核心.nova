《事件系统核心》

引用《.·节点》。

引用《.·内核直连层》。

引用《..·推理·定点Q算术》。

引用《..·中枢·内核打印》。

引用《..·中枢·时钟中枢》。

事件阶段_无 为 0。

事件阶段_捕获 为 1。

事件阶段_目标 为 2。

事件阶段_冒泡 为 3。

ECPD_最大传播深度 为 64。

ECPD_衰减系数 为 15。

ECPD_能量阈值 为 100。

ECPD_最大监听器 为 256。

ECPD_队列容量 为 4096。

ECPD_批次大小 为 32。

ECPD_委托最大深度 为 8。

ECPD_AI预测窗口 为 16。

事件类型_click 为 1。

事件类型_dblclick 为 2。

事件类型_mousedown 为 3。

事件类型_mouseup 为 4。

事件类型_mousemove 为 5。

事件类型_mouseenter 为 6。

事件类型_mouseleave 为 7。

事件类型_mouseover 为 8。

事件类型_mouseout 为 9。

事件类型_wheel 为 10。

事件类型_keydown 为 11。

事件类型_keyup 为 12。

事件类型_keypress 为 13。

事件类型_focus 为 14。

事件类型_blur 为 15。

事件类型_focusin 为 16。

事件类型_focusout 为 17。

事件类型_input 为 18。

事件类型_change 为 19。

事件类型_submit 为 20。

事件类型_reset 为 21。

事件类型_scroll 为 22。

事件类型_resize 为 23。

事件类型_load 为 24。

事件类型_DOMContentLoaded 为 25。

事件类型_unload 为 26。

事件类型_error 为 27。

事件类型_abort 为 28。

事件类型_touchstart 为 29。

事件类型_touchmove 为 30。

事件类型_touchend 为 31。

事件类型_touchcancel 为 32。

事件类型_pointerdown 为 33。

事件类型_pointermove 为 34。

事件类型_pointerup 为 35。

事件类型_contextmenu 为 36。

事件类型_drag 为 37。

事件类型_dragstart 为 38。

事件类型_dragend 为 39。

事件类型_drop 为 40。

事件类型_dragover 为 41。

事件类型_transitionend 为 42。

事件类型_animationend 为 43。

事件类型_beforeunload 为 44。

事件类型_popstate 为 45。

事件类型_hashchange 为 46。

事件类型_message 为 47。

事件类型_storage 为 48。

事件类型_selectstart 为 49。

事件类型_selectionchange 为 50。

事件类型_copy 为 51。

事件类型_cut 为 52。

事件类型_paste 为 53。

事件类型_自定义起始 为 1000。

可变 ECPD_自定义类型计数 为 0。

可变 ECPD_总派发数 为 0。

可变 ECPD_总监听器数 为 0。

可变 ECPD_事件池指针 为 0。

事件 是 一种 事物，含 类型、类型原子、目标、当前目标、事件阶段、冒泡、可取消、可合成、已停止传播、已停止立即传播、已阻止默认、是受信任的、时间戳、能量、传播深度、AI优先级、用户数据。

事件监听器 是 一种 事物，含 类型原子、回调、捕获、仅一次、被动、已移除、优先级。

事件目标 是 一种 事物，含 监听器列表、监听器数、父节点、节点类型、委托标记。

事件队列项 是 一种 事物，含 事件、目标、时间戳。

ECPD统计 是 一种 事物，含 总派发数、总捕获次数、总冒泡次数、总截断数、总取消数、AI预测命中、AI预测总数、平均派发耗时、最大派发耗时、队列溢出次数。

可变 全局ECPD统计 为 新建 ECPD统计。

事件_创建 接受 类型原子、类型名、冒泡、可取消：
    ev 为 新建 事件：类型 为 类型名、类型原子 为 类型原子、目标 为 空、当前目标 为 空、事件阶段 为 事件阶段_无、冒泡 为 冒泡、可取消 为 可取消、可合成 为 假、已停止传播 为 假、已停止立即传播 为 假、已阻止默认 为 假、是受信任的 为 真、时间戳 为 做 读取微秒、能量 为 10000、传播深度 为 0、AI优先级 为 5、用户数据 为 空。
    返还 ev。
完。

事件_创建自定义 接受 类型名、冒泡、可取消、用户数据：
    ECPD_自定义类型计数 为 ECPD_自定义类型计数 加 1。
    原子 为 事件类型_自定义起始 加 ECPD_自定义类型计数。
    ev 为 做 事件_创建 用 原子、类型名、冒泡、可取消。
    ev 的 是受信任的 为 假。
    ev 的 用户数据 为 用户数据。
    返还 ev。
完。

事件_停止传播 接受 ev：
    若 ev 等于 空：
        返还。
    完。
    ev 的 已停止传播 为 真。
完。

事件_停止立即传播 接受 ev：
    若 ev 等于 空：
        返还。
    完。
    ev 的 已停止传播 为 真。
    ev 的 已停止立即传播 为 真。
完。

事件_阻止默认 接受 ev：
    若 ev 等于 空：
        返还。
    完。
    若 ev 的 可取消 等于 假：
        返还。
    完。
    ev 的 已阻止默认 为 真。
    全局ECPD统计 的 总取消数 为 全局ECPD统计 的 总取消数 加 1。
完。

监听器_添加 接受 目标、类型原子、回调、捕获、仅一次、被动：
    若 目标 等于 空：
        返还 假。
    完。
    若 目标 的 监听器数 大于或等于 ECPD_最大监听器：
        做 内核打印 用 「[ECPD警告] 监听器数达到上限」。
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 目标 的 监听器数：
        已有 为 目标 的 监听器列表 的 第 i 项。
        若 已有 的 类型原子 等于 类型原子 并且 已有 的 回调 等于 回调 并且 已有 的 捕获 等于 捕获：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    新监听 为 新建 事件监听器：类型原子 为 类型原子、回调 为 回调、捕获 为 捕获、仅一次 为 仅一次、被动 为 被动、已移除 为 假、优先级 为 5。
    目标 的 监听器列表 的 第 目标 的 监听器数 项 为 新监听。
    目标 的 监听器数 为 目标 的 监听器数 加 1。
    ECPD_总监听器数 为 ECPD_总监听器数 加 1。
    返还 真。
完。

监听器_移除 接受 目标、类型原子、回调、捕获：
    若 目标 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 目标 的 监听器数：
        l 为 目标 的 监听器列表 的 第 i 项。
        若 l 的 类型原子 等于 类型原子 并且 l 的 回调 等于 回调 并且 l 的 捕获 等于 捕获：
            l 的 已移除 为 真。
            ECPD_总监听器数 为 ECPD_总监听器数 减 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

监听器_压缩 接受 目标：
    若 目标 等于 空：
        返还。
    完。
    可变 写指针 为 0。
    可变 读指针 为 0。
    只要 读指针 小于 目标 的 监听器数：
        若 目标 的 监听器列表 的 第 读指针 项 的 已移除 等于 假：
            若 写指针 不等于 读指针：
                目标 的 监听器列表 的 第 写指针 项 为 目标 的 监听器列表 的 第 读指针 项。
            完。
            写指针 为 写指针 加 1。
        完。
        读指针 为 读指针 加 1。
    完。
    目标 的 监听器数 为 写指针。
完。

ECPD_构建传播路径 接受 目标节点：
    路径 为 一组：（空）。
    可变 当前 为 目标节点。
    可变 深度 为 0。
    只要 当前 不等于 空 并且 深度 小于 ECPD_最大传播深度：
        路径 的 第 深度 项 为 当前。
        当前 为 当前 的 父节点。
        深度 为 深度 加 1。
    完。
    可变 左 为 0。
    可变 右 为 深度 减 1。
    只要 左 小于 右：
        临时 为 路径 的 第 左 项。
        路径 的 第 左 项 为 路径 的 第 右 项。
        路径 的 第 右 项 为 临时。
        左 为 左 加 1。
        右 为 右 减 1。
    完。
    返还 路径。
完。

ECPD_派发事件 接受 目标节点、ev：
    若 目标节点 等于 空 或者 ev 等于 空：
        返还 假。
    完。
    开始时间 为 做 读取微秒。
    ev 的 目标 为 目标节点。
    ev 的 事件阶段 为 事件阶段_捕获。
    ECPD_总派发数 为 ECPD_总派发数 加 1。
    全局ECPD统计 的 总派发数 为 全局ECPD统计 的 总派发数 加 1。
    路径 为 做 ECPD_构建传播路径 用 目标节点。
    路径长度 为 做 计数 用 路径。
    若 路径长度 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 路径长度 减 1：
        若 ev 的 已停止传播：
            跳出。
        完。
        ev 的 当前目标 为 路径 的 第 i 项。
        ev 的 事件阶段 为 事件阶段_捕获。
        ev 的 传播深度 为 i。
        ev 的 能量 为 10000 乘 (1000 减 ECPD_衰减系数 乘 i) 除以 1000。
        若 ev 的 能量 小于 ECPD_能量阈值：
            全局ECPD统计 的 总截断数 为 全局ECPD统计 的 总截断数 加 1。
        完。
        做 ECPD_触发监听器 用 路径 的 第 i 项、ev、真。
        全局ECPD统计 的 总捕获次数 为 全局ECPD统计 的 总捕获次数 加 1。
        i 为 i 加 1。
    完。
    若 ev 的 已停止传播 等于 假：
        ev 的 当前目标 为 目标节点。
        ev 的 事件阶段 为 事件阶段_目标。
        ev 的 传播深度 为 路径长度 减 1。
        做 ECPD_触发监听器 用 目标节点、ev、真。
        做 ECPD_触发监听器 用 目标节点、ev、假。
    完。
    若 ev 的 冒泡 并且 ev 的 已停止传播 等于 假：
        i 为 路径长度 减 2。
        只要 i 大于或等于 0：
            若 ev 的 已停止传播：
                跳出。
            完。
            ev 的 当前目标 为 路径 的 第 i 项。
            ev 的 事件阶段 为 事件阶段_冒泡。
            ev 的 传播深度 为 路径长度 减 1 减 i。
            做 ECPD_触发监听器 用 路径 的 第 i 项、ev、假。
            全局ECPD统计 的 总冒泡次数 为 全局ECPD统计 的 总冒泡次数 加 1。
            i 为 i 减 1。
        完。
    完。
    耗时 为 做 读取微秒 减 开始时间。
    若 耗时 大于 全局ECPD统计 的 最大派发耗时：
        全局ECPD统计 的 最大派发耗时 为 耗时。
    完。
    返还 ev 的 已阻止默认 等于 假。
完。

ECPD_触发监听器 接受 目标、ev、仅捕获：
    若 目标 等于 空：
        返还。
    完。
    可变 i 为 0。
    可变 有删除 为 假。
    只要 i 小于 目标 的 监听器数：
        若 ev 的 已停止立即传播：
            跳出。
        完。
        l 为 目标 的 监听器列表 的 第 i 项。
        若 l 的 已移除：
            i 为 i 加 1。
            继续。
        完。
        若 l 的 类型原子 不等于 ev 的 类型原子：
            i 为 i 加 1。
            继续。
        完。
        若 仅捕获 并且 l 的 捕获 等于 假：
            i 为 i 加 1。
            继续。
        完。
        若 仅捕获 等于 假 并且 l 的 捕获：
            i 为 i 加 1。
            继续。
        完。
        做 l 的 回调 用 ev。
        若 l 的 仅一次：
            l 的 已移除 为 真。
            有删除 为 真。
        完。
        i 为 i 加 1。
    完。
    若 有删除：
        做 监听器_压缩 用 目标。
    完。
完。

ECPD_安装委托 接受 父目标、类型原子、匹配器、回调：
    若 父目标 等于 空：
        返还 假。
    完。
    委托回调 为 以 ev 为 引，其做法是：可变 当前 为 ev 的 目标。 可变 深度 为 0。 只要 当前 不等于 空 并且 当前 不等于 父目标 并且 深度 小于 ECPD_委托最大深度： 若 做 匹配器 用 当前： 做 回调 用 ev、当前。 返还。 完。 当前 为 当前 的 父节点。 深度 为 深度 加 1。 完。
    做 监听器_添加 用 父目标、类型原子、委托回调、假、假、假。
    父目标 的 委托标记 为 真。
    返还 真。
完。

可变 事件队列 为 一组：（空）。

可变 事件队列头 为 0。

可变 事件队列尾 为 0。

ECPD_入队 接受 目标、ev：
    若 ev 等于 空：
        返还 假。
    完。
    当前大小 为 事件队列尾 减 事件队列头。
    若 当前大小 大于或等于 ECPD_队列容量：
        全局ECPD统计 的 队列溢出次数 为 全局ECPD统计 的 队列溢出次数 加 1。
        做 内核打印 用 「[ECPD警告] 事件队列已满，丢弃事件」。
        返还 假。
    完。
    项 为 新建 事件队列项：事件 为 ev、目标 为 目标、时间戳 为 做 读取微秒。
    事件队列 的 第 事件队列尾 项 为 项。
    事件队列尾 为 事件队列尾 加 1。
    返还 真。
完。

ECPD_刷新队列 接受 零参：
    可变 已处理 为 0。
    只要 事件队列头 小于 事件队列尾 并且 已处理 小于 ECPD_批次大小：
        项 为 事件队列 的 第 事件队列头 项。
        做 ECPD_派发事件 用 项 的 目标、项 的 事件。
        事件队列头 为 事件队列头 加 1。
        已处理 为 已处理 加 1。
    完。
    若 事件队列头 等于 事件队列尾：
        事件队列头 为 0。
        事件队列尾 为 0。
    完。
    返还 已处理。
完。

AI事件预测记录 是 一种 事物，含 类型原子、时间戳、间隔。

可变 AI预测历史 为 一组：（空）。

可变 AI预测历史数 为 0。

ECPD_AI记录事件 接受 类型原子：
    now 为 做 读取微秒。
    可变 上次时间 为 0。
    可变 j 为 AI预测历史数 减 1。
    只要 j 大于或等于 0：
        若 AI预测历史 的 第 j 项 的 类型原子 等于 类型原子：
            上次时间 为 AI预测历史 的 第 j 项 的 时间戳。
            跳出。
        完。
        j 为 j 减 1。
    完。
    间隔 为 0。
    若 上次时间 大于 0：
        间隔 为 now 减 上次时间。
    完。
    若 AI预测历史数 大于或等于 ECPD_AI预测窗口：
        可变 k 为 0。
        只要 k 小于 ECPD_AI预测窗口 减 1：
            AI预测历史 的 第 k 项 为 AI预测历史 的 第 k 加 1 项。
            k 为 k 加 1。
        完。
        AI预测历史数 为 ECPD_AI预测窗口 减 1。
    完。
    AI预测历史 的 第 AI预测历史数 项 为 新建 AI事件预测记录：类型原子 为 类型原子、时间戳 为 now、间隔 为 间隔。
    AI预测历史数 为 AI预测历史数 加 1。
完。

ECPD_AI预测下次事件 接受 类型原子：
    可变 n 为 0。
    可变 sum_x 为 0。
    可变 sum_y 为 0。
    可变 sum_xy 为 0。
    可变 sum_xx 为 0。
    可变 i 为 0。
    只要 i 小于 AI预测历史数：
        若 AI预测历史 的 第 i 项 的 类型原子 等于 类型原子 并且 AI预测历史 的 第 i 项 的 间隔 大于 0：
            sum_x 为 sum_x 加 n。
            sum_y 为 sum_y 加 AI预测历史 的 第 i 项 的 间隔。
            sum_xy 为 sum_xy 加 n 乘 AI预测历史 的 第 i 项 的 间隔。
            sum_xx 为 sum_xx 加 n 乘 n。
            n 为 n 加 1。
        完。
        i 为 i 加 1。
    完。
    若 n 小于 2：
        返还 0。
    完。
    分母 为 n 乘 sum_xx 减 sum_x 乘 sum_x。
    若 分母 等于 0：
        返还 sum_y 除以 n。
    完。
    b 为 (n 乘 sum_xy 减 sum_x 乘 sum_y) 除以 分母。
    a 为 (sum_y 减 b 乘 sum_x) 除以 n。
    预测间隔 为 a 加 b 乘 n。
    若 预测间隔 小于 0：
        预测间隔 为 sum_y 除以 n。
    完。
    全局ECPD统计 的 AI预测总数 为 全局ECPD统计 的 AI预测总数 加 1。
    返还 预测间隔。
完。

ECPD_AI优先级评估 接受 ev：
    若 ev 等于 空：
        返还 5。
    完。
    bus 为 全局AI总线。
    若 bus 等于 空：
        若 ev 的 类型原子 小于或等于 事件类型_pointerup：
            返还 9。
        完。
        若 ev 的 类型原子 等于 事件类型_scroll：
            返还 8。
        完。
        若 ev 的 类型原子 等于 事件类型_resize：
            返还 7。
        完。
        返还 5。
    完。
    特征 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 特征、0、ev 的 类型原子。
    冒泡值 为 0。
    若 ev 的 冒泡：
        冒泡值 为 1。
    完。
    做 Tensor_写 用 特征、1、冒泡值。
    做 Tensor_写 用 特征、2、ev 的 能量。
    交互值 为 0。
    若 ev 的 类型原子 小于或等于 事件类型_pointerup：
        交互值 为 1。
    完。
    做 Tensor_写 用 特征、3、交互值。
    结果 为 做 AI总线_推理 用 bus、「事件优先级」、特征。
    优先级 为 做 Tensor_读 用 结果、0。
    做 Tensor_释放 用 特征。
    做 Tensor_释放 用 结果。
    若 优先级 小于 1：
        优先级 为 1。
    完。
    若 优先级 大于 10：
        优先级 为 10。
    完。
    返还 优先级。
完。

ECPD_打印统计 接受 零参：
    做 内核打印 用 「╔═══════════════════════════════════════╗」。
    做 内核打印 用 「║  ECPD 事件系统诊断报告                ║」。
    做 内核打印 用 「╠═══════════════════════════════════════╣」。
    做 内核打印 用 「║ 总派发数:     {}」、全局ECPD统计 的 总派发数。
    做 内核打印 用 「║ 捕获阶段次数: {}」、全局ECPD统计 的 总捕获次数。
    做 内核打印 用 「║ 冒泡阶段次数: {}」、全局ECPD统计 的 总冒泡次数。
    做 内核打印 用 「║ 截断事件数:   {}」、全局ECPD统计 的 总截断数。
    做 内核打印 用 「║ preventDefault: {}」、全局ECPD统计 的 总取消数。
    做 内核打印 用 「║ 最大派发耗时: {}微秒」、全局ECPD统计 的 最大派发耗时。
    做 内核打印 用 「║ 队列溢出次数: {}」、全局ECPD统计 的 队列溢出次数。
    若 全局ECPD统计 的 AI预测总数 大于 0：
        命中率 为 全局ECPD统计 的 AI预测命中 乘 100 除以 全局ECPD统计 的 AI预测总数。
        做 内核打印 用 「║ AI预测命中率: {}%」、命中率。
    完。
    做 内核打印 用 「║ 当前监听器总数: {}」、ECPD_总监听器数。
    做 内核打印 用 「║ 自定义事件类型: {}」、ECPD_自定义类型计数。
    做 内核打印 用 「╚═══════════════════════════════════════╝」。
完。

ECPD_初始化 接受 零参：
    全局ECPD统计 为 新建 ECPD统计。
    ECPD_自定义类型计数 为 0。
    ECPD_总派发数 为 0。
    ECPD_总监听器数 为 0。
    事件队列 为 一组：（空）。
    事件队列头 为 0。
    事件队列尾 为 0。
    AI预测历史 为 一组：（空）。
    AI预测历史数 为 0。
    做 内核打印 用 「[ECPD] 事件系统已初始化 (波动传播模型 v1.0)」。
完。

本章 收束。
