《影子文档》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·整数转字符串》。

影子_开放 为 0。

影子_封闭 为 1。

最大注册 为 128。

最大槽位 为 32。

回调_连接 为 0。

回调_断开 为 1。

回调_属性变 为 2。

回调_采纳 为 3。

自定义元素注册 是 一种 事物，含 标签哈希、构造回调、连接回调、断开回调、属性变回调、采纳回调、观察属性表、观察属性数、扩展标签哈希。

影子根 是 一种 事物，含 宿主元素、模式、首子节点、子节点数、槽位表、槽位数、样式内容、已分发。

可变 注册表 为 一组：（空）。

可变 注册数 为 0。

可变 影子根数 为 0。

可变 声明式处理数 为 0。

自定义元素_注册 接受 标签名、构造、连接、断开、属性变：
    若 注册数 大于或等于 最大注册：
        返还 错误_无资源。
    完。
    注册 为 新建 自定义元素注册。
    注册 的 标签哈希 为 做 哈希_字符串 用 标签名。
    注册 的 构造回调 为 构造。
    注册 的 连接回调 为 连接。
    注册 的 断开回调 为 断开。
    注册 的 属性变回调 为 属性变。
    注册 的 采纳回调 为 0。
    注册 的 观察属性表 为 做 堆_分配 用 16 乘 8。
    注册 的 观察属性数 为 0。
    注册 的 扩展标签哈希 为 0。
    做 追加 用 注册表、注册。
    注册数 为 注册数 加 1。
    返还 0。
完。

自定义元素_查找 接受 标签名：
    目标 为 做 哈希_字符串 用 标签名。
    可变 序号 为 0。
    只要 序号 小于 注册数：
        若 注册表 的 第 序号 项 的 标签哈希 等于 目标：
            返还 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 负 1。
完。

自定义元素_创建实例 接受 标签名：
    索引 为 做 自定义元素_查找 用 标签名。
    若 索引 小于 0：
        返还 0。
    完。
    注册 为 注册表 的 第 索引 项。
    可变 实例 为 0。
    若 注册 的 构造回调 不等于 0：
        实例 为 做 注册 的 构造回调。
    完。
    返还 实例。
完。

自定义元素_触发连接 接受 元素、标签名：
    索引 为 做 自定义元素_查找 用 标签名。
    若 索引 大于或等于 0 并且 注册表 的 第 索引 项 的 连接回调 不等于 0：
        做 注册表 的 第 索引 项 的 连接回调 用 元素。
    完。
完。

自定义元素_触发断开 接受 元素、标签名：
    索引 为 做 自定义元素_查找 用 标签名。
    若 索引 大于或等于 0 并且 注册表 的 第 索引 项 的 断开回调 不等于 0：
        做 注册表 的 第 索引 项 的 断开回调 用 元素。
    完。
完。

自定义元素_触发属性变 接受 元素、标签名、属性名、旧值、新值：
    索引 为 做 自定义元素_查找 用 标签名。
    若 索引 大于或等于 0 并且 注册表 的 第 索引 项 的 属性变回调 不等于 0：
        做 注册表 的 第 索引 项 的 属性变回调 用 元素、属性名、旧值、新值。
    完。
完。

影子_附加 接受 宿主、模式：
    根 为 做 堆_分配 用 影子根。
    根 的 宿主元素 为 宿主。
    根 的 模式 为 模式。
    根 的 首子节点 为 0。
    根 的 子节点数 为 0。
    根 的 槽位表 为 做 堆_分配 用 最大槽位 乘 16。
    根 的 槽位数 为 0。
    根 的 样式内容 为 0。
    根 的 已分发 为 假。
    影子根数 为 影子根数 加 1。
    返还 根。
完。

影子_添加槽位 接受 根、名称哈希：
    若 根 等于 0 或者 根 的 槽位数 大于或等于 最大槽位：
        返还 负 1。
    完。
    偏移 为 根 的 槽位表 加 根 的 槽位数 乘 16。
    做 内存_写64 用 偏移、名称哈希。
    做 内存_写64 用 偏移 加 8、0。
    索引 为 根 的 槽位数。
    根 的 槽位数 为 根 的 槽位数 加 1。
    返还 索引。
完。

影子_分发槽位 接受 根：
    若 根 等于 0 或者 根 的 已分发：
        返还。
    完。
    根 的 已分发 为 真。
完。

影子_设样式 接受 根、样式数据：
    若 根 等于 0：
        返还。
    完。
    根 的 样式内容 为 样式数据。
完。

声明式影子_处理 接受 模板节点、宿主节点：
    若 模板节点 等于 0 或者 宿主节点 等于 0：
        返还。
    完。
    声明式处理数 为 声明式处理数 加 1。
    模式 为 影子_开放。
    根 为 做 影子_附加 用 宿主节点、模式。
    若 根 等于 0：
        返还。
    完。
    做 影子_分发槽位 用 根。
完。

影子_获取根 接受 宿主：
    返还 影子宿主 的 影子根。
    返还 0。
完。

网组件_完整统计 接受 零参：
    返还 一组：注册数、影子根数、声明式处理数。
完。

网组件_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 注册数 大于 最大注册 乘 9 除以 10：
        做 追加 用 问题、「自定义元素注册接近上限: 」 加 做 整数转字符串 用 注册数。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「网组件子系统健康」。
    完。
    返还 问题。
完。

网组件_详细统计 接受 零参：
    返还 一本字典：注册数 对应 注册数、影子根数 对应 影子根数、声明式处理 对应 声明式处理数、最大注册 对应 最大注册。
完。

网组件_重置统计 接受 零参：
    注册数 为 0。
    影子根数 为 0。
    声明式处理数 为 0。
    注册表 为 一组：（空）。
完。

自定义元素_设观察属性 接受 标签名、属性名：
    索引 为 做 自定义元素_查找 用 标签名。
    若 索引 小于 0：
        返还。
    完。
    注册 为 注册表 的 第 索引 项。
    若 注册 的 观察属性数 大于或等于 16：
        返还。
    完。
    做 内存_写64 用 注册 的 观察属性表 加 注册 的 观察属性数 乘 8、做 哈希_字符串 用 属性名。
    注册 的 观察属性数 为 注册 的 观察属性数 加 1。
完。

自定义元素_设扩展标签 接受 标签名、扩展标签：
    索引 为 做 自定义元素_查找 用 标签名。
    若 索引 小于 0：
        返还。
    完。
    注册表 的 第 索引 项 的 扩展标签哈希 为 做 哈希_字符串 用 扩展标签。
完。

自定义元素_设采纳回调 接受 标签名、回调：
    索引 为 做 自定义元素_查找 用 标签名。
    若 索引 小于 0：
        返还。
    完。
    注册表 的 第 索引 项 的 采纳回调 为 回调。
完。

自定义元素_触发采纳 接受 元素、标签名、旧文档、新文档：
    索引 为 做 自定义元素_查找 用 标签名。
    若 索引 大于或等于 0 并且 注册表 的 第 索引 项 的 采纳回调 不等于 0：
        做 注册表 的 第 索引 项 的 采纳回调 用 元素、旧文档、新文档。
    完。
完。

自定义元素_是否观察 接受 标签名、属性名：
    索引 为 做 自定义元素_查找 用 标签名。
    若 索引 小于 0：
        返还 假。
    完。
    注册 为 注册表 的 第 索引 项。
    目标 为 做 哈希_字符串 用 属性名。
    可变 i 为 0。
    只要 i 小于 注册 的 观察属性数：
        若 做 内存_读64 用 注册 的 观察属性表 加 i 乘 8 等于 目标：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

自定义元素_已注册 接受 标签名：
    返还 做 自定义元素_查找 用 标签名 大于或等于 0。
完。

自定义元素_注册数 接受 零参：
    返还 注册数。
完。

自定义元素_获取注册 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 注册数：
        返还 0。
    完。
    返还 注册表 的 第 索引 项。
完。

影子_添加子节点 接受 根、节点：
    若 根 等于 0 或者 节点 等于 0：
        返还。
    完。
    根 的 子节点数 为 根 的 子节点数 加 1。
完。

影子_移除子节点 接受 根、节点：
    若 根 等于 0 或者 节点 等于 0：
        返还。
    完。
    若 根 的 子节点数 大于 0：
        根 的 子节点数 为 根 的 子节点数 减 1。
    完。
完。

影子_获取模式名 接受 模式：
    若 模式 等于 影子_开放：
        返还 「open」。
    完。
    返还 「closed」。
完。

影子_获取子节点数 接受 根：
    若 根 等于 0：
        返还 0。
    完。
    返还 根 的 子节点数。
完。

影子_获取槽位数 接受 根：
    若 根 等于 0：
        返还 0。
    完。
    返还 根 的 槽位数。
完。

影子_获取宿主 接受 根：
    若 根 等于 0：
        返还 0。
    完。
    返还 根 的 宿主元素。
完。

影子_有样式 接受 根：
    若 根 等于 0：
        返还 假。
    完。
    返还 根 的 样式内容 不等于 0。
完。

影子_诊断 接受 根：
    若 根 等于 0：
        返还 「影子根为空」。
    完。
    返还 一本字典：模式 对应 做 影子_获取模式名 用 根 的 模式、子节点数 对应 根 的 子节点数、槽位数 对应 根 的 槽位数、有样式 对应 根 的 样式内容 不等于 0、已分发 对应 根 的 已分发。
完。

声明式影子_解析模式 接受 属性值：
    若 属性值 等于 0：
        返还 影子_开放。
    完。
    若 做 字符串_相等 用 属性值、「open」：
        返还 影子_开放。
    完。
    若 做 字符串_相等 用 属性值、「closed」：
        返还 影子_封闭。
    完。
    返还 影子_开放。
完。

声明式影子_处理带模式 接受 模板节点、宿主节点、模式属性：
    若 模板节点 等于 0 或者 宿主节点 等于 0：
        返还。
    完。
    声明式处理数 为 声明式处理数 加 1。
    模式 为 做 声明式影子_解析模式 用 模式属性。
    根 为 做 影子_附加 用 宿主节点、模式。
    若 根 等于 0：
        返还。
    完。
    做 影子_分发槽位 用 根。
完。

哈希_字符串 接受 s：
    若 s 等于 0：
        返还 0。
    完。
    长度 为 做 字节长度 用 s。
    可变 哈希 为 5381。
    可变 i 为 0。
    只要 i 小于 长度：
        哈希 为 (哈希 左移 5) 加 哈希 加 做 内存_读8 用 s 加 i。
        i 为 i 加 1。
    完。
    返还 哈希 位与 0x7FFFFFFF。
完。

错误_无资源 为 负 12。

字符串_相等 接受 a、b：
    若 a 等于 b：
        返还 真。
    完。
    若 a 等于 0 或者 b 等于 0：
        返还 假。
    完。
    长a 为 做 字节长度 用 a。
    长b 为 做 字节长度 用 b。
    若 长a 不等于 长b：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 长a：
        若 做 内存_读8 用 a 加 i 不等于 做 内存_读8 用 b 加 i：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

网组件_自定义元素诊断 接受 标签名：
    索引 为 做 自定义元素_查找 用 标签名。
    若 索引 小于 0：
        返还 「未注册: 」 加 标签名。
    完。
    注册 为 注册表 的 第 索引 项。
    返还 一本字典：标签哈希 对应 注册 的 标签哈希、有构造 对应 注册 的 构造回调 不等于 0、有连接 对应 注册 的 连接回调 不等于 0、有断开 对应 注册 的 断开回调 不等于 0、有属性变 对应 注册 的 属性变回调 不等于 0、有采纳 对应 注册 的 采纳回调 不等于 0、观察属性数 对应 注册 的 观察属性数。
完。

可变 AI_影子文档_热节点缓存 为 0。

AI_影子文档_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_影子文档_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_影子文档_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

本章 收束。
