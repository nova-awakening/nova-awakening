《绘制画布绘》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《.·属性自定义属》。

引用《..·稳态·字体写字形》。

Sk画布_drawBitmap 接受 c、源位图、x、y、paint：
    c 的 绘制调用数 为 c 的 绘制调用数 加 1。
    目标 为 c 的 位图。
    px 为 x 除以 100。
    py 为 y 除以 100。
    可变 sy 为 0。
    只要 sy 小于 源位图 的 高：
        可变 sx 为 0。
        只要 sx 小于 源位图 的 宽：
            颜色 为 做 Nova缓冲itmap_获取像素 用 源位图、sx、sy。
            若 颜色 不等于 SK_ColorTRANSPARENT：
                做 Sk画布_混合像素 用 目标、px 加 sx、py 加 sy、颜色、paint 的 混合模式。
            完。
            sx 为 sx 加 1。
        完。
        sy 为 sy 加 1。
    完。
完。

Sk画布_drawPath 接受 c、path、paint：
    c 的 绘制调用数 为 c 的 绘制调用数 加 1。
完。

Sk画布_混合像素 接受 bm、x、y、源颜色、混合模式：
    若 x 小于 0 或者 x 大于或等于 bm 的 宽 或者 y 小于 0 或者 y 大于或等于 bm 的 高：
        返还。
    完。
    若 混合模式 等于 混合_Src：
        做 Nova缓冲itmap_设置像素 用 bm、x、y、源颜色。
        返还。
    完。
    源a 为 做 SkColor_A 用 源颜色。
    若 源a 等于 0：
        返还。
    完。
    若 源a 等于 255 并且 混合模式 等于 混合_SrcOver：
        做 Nova缓冲itmap_设置像素 用 bm、x、y、源颜色。
        返还。
    完。
    若 混合模式 等于 混合_SrcOver：
        目标颜色 为 做 Nova缓冲itmap_获取像素 用 bm、x、y。
        目标a 为 做 SkColor_A 用 目标颜色。
        反源a 为 255 减 源a。
        r 为 (做 SkColor_R 用 源颜色 乘 源a 加 做 SkColor_R 用 目标颜色 乘 反源a) 除以 255。
        g 为 (做 SkColor_G 用 源颜色 乘 源a 加 做 SkColor_G 用 目标颜色 乘 反源a) 除以 255。
        b 为 (做 SkColor_B 用 源颜色 乘 源a 加 做 SkColor_B 用 目标颜色 乘 反源a) 除以 255。
        a 为 源a 加 目标a 乘 反源a 除以 255。
        做 Nova缓冲itmap_设置像素 用 bm、x、y、做 SkColor 用 a、r、g、b。
        返还。
    完。
    若 混合模式 等于 混合_Multiply：
        目标颜色 为 做 Nova缓冲itmap_获取像素 用 bm、x、y。
        r 为 做 SkColor_R 用 源颜色 乘 做 SkColor_R 用 目标颜色 除以 255。
        g 为 做 SkColor_G 用 源颜色 乘 做 SkColor_G 用 目标颜色 除以 255。
        b 为 做 SkColor_B 用 源颜色 乘 做 SkColor_B 用 目标颜色 除以 255。
        做 Nova缓冲itmap_设置像素 用 bm、x、y、做 SkColor 用 255、r、g、b。
        返还。
    完。
    若 混合模式 等于 混合_Screen：
        目标颜色 为 做 Nova缓冲itmap_获取像素 用 bm、x、y。
        r 为 255 减 (255 减 做 SkColor_R 用 源颜色) 乘 (255 减 做 SkColor_R 用 目标颜色) 除以 255。
        g 为 255 减 (255 减 做 SkColor_G 用 源颜色) 乘 (255 减 做 SkColor_G 用 目标颜色) 除以 255。
        b 为 255 减 (255 减 做 SkColor_B 用 源颜色) 乘 (255 减 做 SkColor_B 用 目标颜色) 除以 255。
        做 Nova缓冲itmap_设置像素 用 bm、x、y、做 SkColor 用 255、r、g、b。
        返还。
    完。
    做 Nova缓冲itmap_设置像素 用 bm、x、y、源颜色。
完。

Sk画布_drawHLine 接受 c、x1、x2、y、高度、颜色、混合模式：
    bm 为 c 的 位图。
    可变 dy 为 0。
    只要 dy 小于 高度：
        可变 x 为 x1。
        只要 x 小于 x2：
            做 Sk画布_混合像素 用 bm、x、y 加 dy、颜色、混合模式。
            x 为 x 加 1。
        完。
        dy 为 dy 加 1。
    完。
完。

Sk画布_drawVLine 接受 c、x、y1、y2、宽度、颜色、混合模式：
    bm 为 c 的 位图。
    可变 dx 为 0。
    只要 dx 小于 宽度：
        可变 y 为 y1。
        只要 y 小于 y2：
            做 Sk画布_混合像素 用 bm、x 加 dx、y、颜色、混合模式。
            y 为 y 加 1。
        完。
        dx 为 dx 加 1。
    完。
完。

画布_绘制文本 接受 画布、x、y、文本、颜色、字号：
    若 画布 等于 空：
        返还。
    完。
    fb 为 画布 的 帧缓冲。
    缩放 为 字号 除以 16。
    若 缩放 小于 1：
        缩放 为 1。
    完。
    做 字体_渲染文本_透明 用 fb、x、y、文本、颜色、缩放。
完。

画布_设置像素 接受 画布、x、y、颜色：
    若 画布 等于 空：
        返还。
    完。
    若 x 小于 0 或者 x 大于或等于 画布 的 宽 或者 y 小于 0 或者 y 大于或等于 画布 的 高：
        返还。
    完。
    偏移 为 y 乘 画布 的 步幅 加 x 乘 4。
    做 内存_写32 用 画布 的 帧缓冲 加 偏移、颜色。
完。

本章 收束。
