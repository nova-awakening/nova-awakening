《触摸事件》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《.·事件系统核心》。

引用《..·中枢·内核打印》。

引用《.·文档节点》。

引用《..·中枢·高精度定时器hrtimer》。

引用《..·中枢·整数转字符串》。

触摸_最大点数 为 10。

手势_无 为 0。

手势_点击 为 1。

手势_双击 为 2。

手势_长按 为 3。

手势_滑动左 为 4。

手势_滑动右 为 5。

手势_滑动上 为 6。

手势_滑动下 为 7。

手势_捏合 为 8。

手势_张开 为 9。

手势_旋转 为 10。

滑动_阈值 为 30。

长按_阈值_ms 为 500。

双击_间隔_ms 为 300。

Touch 是 一种 事物，含 标识符、目标、客户端x、客户端y、页面x、页面y、屏幕x、屏幕y、半径x、半径y、旋转角度、压力。

TouchList 是 一种 事物，含 触摸列表、长度、容量。

Touch事件扩展 是 一种 事物，含 基础事件、touches、targetTouches、changedTouches、Ctrl键、Shift键、Alt键、Meta键。

触摸跟踪点 是 一种 事物，含 标识符、起始x、起始y、当前x、当前y、起始时间、活跃。

手势识别状态 是 一种 事物，含 活跃触摸数、跟踪点、上次点击时间、上次点击x、上次点击y、初始双指距离、初始双指角度、当前手势。

触摸事件统计 是 一种 事物，含 触摸开始次数、触摸移动次数、触摸结束次数、触摸取消次数、点击识别数、双击识别数、长按识别数、滑动识别数、捏合识别数、旋转识别数、TouchList创建数、事件创建数。

可变 全局_触摸统计 为 空。

可变 全局_手势状态 为 空。

触摸统计_初始化 接受 零参：
    若 全局_触摸统计 等于 空：
        全局_触摸统计 为 新建 触摸事件统计。
        全局_触摸统计 的 触摸开始次数 为 0。
        全局_触摸统计 的 触摸移动次数 为 0。
        全局_触摸统计 的 触摸结束次数 为 0。
        全局_触摸统计 的 触摸取消次数 为 0。
        全局_触摸统计 的 点击识别数 为 0。
        全局_触摸统计 的 双击识别数 为 0。
        全局_触摸统计 的 长按识别数 为 0。
        全局_触摸统计 的 滑动识别数 为 0。
        全局_触摸统计 的 捏合识别数 为 0。
        全局_触摸统计 的 旋转识别数 为 0。
        全局_触摸统计 的 TouchList创建数 为 0。
        全局_触摸统计 的 事件创建数 为 0。
    完。
完。

手势状态_初始化 接受 零参：
    若 全局_手势状态 等于 空：
        全局_手势状态 为 新建 手势识别状态。
        全局_手势状态 的 活跃触摸数 为 0。
        全局_手势状态 的 跟踪点 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 触摸_最大点数：
            pt 为 新建 触摸跟踪点。
            pt 的 活跃 为 假。
            pt 的 标识符 为 负 1。
            做 追加 用 全局_手势状态 的 跟踪点、pt。
            i 为 i 加 1。
        完。
        全局_手势状态 的 上次点击时间 为 0。
        全局_手势状态 的 上次点击x 为 0。
        全局_手势状态 的 上次点击y 为 0。
        全局_手势状态 的 初始双指距离 为 0。
        全局_手势状态 的 初始双指角度 为 0。
        全局_手势状态 的 当前手势 为 手势_无。
    完。
完。

Touch_创建 接受 标识符、目标、客户端x、客户端y、页面x、页面y、屏幕x、屏幕y：
    t 为 做 堆_分配 用 Touch。
    t 的 标识符 为 标识符。
    t 的 目标 为 目标。
    t 的 客户端x 为 客户端x。
    t 的 客户端y 为 客户端y。
    t 的 页面x 为 页面x。
    t 的 页面y 为 页面y。
    t 的 屏幕x 为 屏幕x。
    t 的 屏幕y 为 屏幕y。
    t 的 半径x 为 1。
    t 的 半径y 为 1。
    t 的 旋转角度 为 0。
    t 的 压力 为 100。
    返还 t。
完。

TouchList_创建 接受 零参：
    做 触摸统计_初始化。
    全局_触摸统计 的 TouchList创建数 为 全局_触摸统计 的 TouchList创建数 加 1。
    tl 为 做 堆_分配 用 TouchList。
    tl 的 容量 为 触摸_最大点数。
    tl 的 触摸列表 为 做 堆_分配_数组 用 tl 的 容量 乘 64。
    tl 的 长度 为 0。
    返还 tl。
完。

TouchList_添加 接受 tl、touch：
    若 tl 等于 0 或者 touch 等于 0：
        返还。
    完。
    若 tl 的 长度 大于或等于 tl 的 容量：
        返还。
    完。
    做 内存_复制 用 tl 的 触摸列表 加 tl 的 长度 乘 64、touch、64。
    tl 的 长度 为 tl 的 长度 加 1。
完。

TouchList_获取 接受 tl、索引：
    若 tl 等于 0 或者 索引 小于 0 或者 索引 大于或等于 tl 的 长度：
        返还 空。
    完。
    返还 tl 的 触摸列表 加 索引 乘 64。
完。

TouchList_按标识符查找 接受 tl、标识符：
    若 tl 等于 0：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 tl 的 长度：
        t 为 做 TouchList_获取 用 tl、i。
        若 t 不等于 空 并且 t 的 标识符 等于 标识符：
            返还 t。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

TouchList_移除 接受 tl、标识符：
    若 tl 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 tl 的 长度：
        t 为 做 TouchList_获取 用 tl、i。
        若 t 不等于 空 并且 t 的 标识符 等于 标识符：
            若 i 小于 tl 的 长度 减 1：
                做 内存_复制 用 tl 的 触摸列表 加 i 乘 64、tl 的 触摸列表 加 (tl 的 长度 减 1) 乘 64、64。
            完。
            tl 的 长度 为 tl 的 长度 减 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

TouchList_克隆 接受 tl：
    新 为 做 TouchList_创建。
    若 tl 等于 0：
        返还 新。
    完。
    可变 i 为 0。
    只要 i 小于 tl 的 长度：
        做 内存_复制 用 新 的 触摸列表 加 i 乘 64、tl 的 触摸列表 加 i 乘 64、64。
        i 为 i 加 1。
    完。
    新 的 长度 为 tl 的 长度。
    返还 新。
完。

Touch事件_创建 接受 类型、touches、changedTouches：
    做 触摸统计_初始化。
    全局_触摸统计 的 事件创建数 为 全局_触摸统计 的 事件创建数 加 1。
    基础 为 做 事件_创建 用 类型、真、真。
    基础 的 是受信任的 为 真。
    te 为 做 堆_分配 用 Touch事件扩展。
    te 的 基础事件 为 基础。
    te 的 touches 为 touches。
    te 的 targetTouches 为 做 TouchList_创建。
    te 的 changedTouches 为 changedTouches。
    te 的 Ctrl键 为 假。
    te 的 Shift键 为 假。
    te 的 Alt键 为 假。
    te 的 Meta键 为 假。
    返还 te。
完。

Touch事件_设置修饰键 接受 te、ctrl、shift、alt、meta：
    若 te 等于 0：
        返还。
    完。
    te 的 Ctrl键 为 ctrl。
    te 的 Shift键 为 shift。
    te 的 Alt键 为 alt。
    te 的 Meta键 为 meta。
完。

Touch事件_筛选目标触摸 接受 te、目标：
    若 te 等于 0 或者 te 的 touches 等于 0 或者 目标 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 te 的 touches 的 长度：
        t 为 做 TouchList_获取 用 te 的 touches、i。
        若 t 不等于 空 并且 t 的 目标 等于 目标：
            做 TouchList_添加 用 te 的 targetTouches、t。
        完。
        i 为 i 加 1。
    完。
完。

触摸_计算距离 接受 x1、y1、x2、y2：
    dx 为 x2 减 x1。
    dy 为 y2 减 y1。
    返还 做 整数_平方根 用 dx 乘 dx 加 dy 乘 dy。
完。

整数_平方根 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

手势_跟踪开始 接受 touch：
    做 手势状态_初始化。
    可变 i 为 0。
    只要 i 小于 触摸_最大点数：
        若 非 全局_手势状态 的 跟踪点 的 第 i 项 的 活跃：
            全局_手势状态 的 跟踪点 的 第 i 项 的 标识符 为 touch 的 标识符。
            全局_手势状态 的 跟踪点 的 第 i 项 的 起始x 为 touch 的 客户端x。
            全局_手势状态 的 跟踪点 的 第 i 项 的 起始y 为 touch 的 客户端y。
            全局_手势状态 的 跟踪点 的 第 i 项 的 当前x 为 touch 的 客户端x。
            全局_手势状态 的 跟踪点 的 第 i 项 的 当前y 为 touch 的 客户端y。
            全局_手势状态 的 跟踪点 的 第 i 项 的 起始时间 为 做 获取滴答。
            全局_手势状态 的 跟踪点 的 第 i 项 的 活跃 为 真。
            全局_手势状态 的 活跃触摸数 为 全局_手势状态 的 活跃触摸数 加 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

手势_跟踪移动 接受 touch：
    做 手势状态_初始化。
    可变 i 为 0。
    只要 i 小于 触摸_最大点数：
        若 全局_手势状态 的 跟踪点 的 第 i 项 的 活跃 并且 全局_手势状态 的 跟踪点 的 第 i 项 的 标识符 等于 touch 的 标识符：
            全局_手势状态 的 跟踪点 的 第 i 项 的 当前x 为 touch 的 客户端x。
            全局_手势状态 的 跟踪点 的 第 i 项 的 当前y 为 touch 的 客户端y。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

手势_跟踪结束 接受 touch：
    做 手势状态_初始化。
    可变 i 为 0。
    只要 i 小于 触摸_最大点数：
        若 全局_手势状态 的 跟踪点 的 第 i 项 的 活跃 并且 全局_手势状态 的 跟踪点 的 第 i 项 的 标识符 等于 touch 的 标识符：
            全局_手势状态 的 跟踪点 的 第 i 项 的 活跃 为 假。
            全局_手势状态 的 活跃触摸数 为 全局_手势状态 的 活跃触摸数 减 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

手势_识别单指 接受 跟踪点：
    做 触摸统计_初始化。
    dx 为 跟踪点 的 当前x 减 跟踪点 的 起始x。
    dy 为 跟踪点 的 当前y 减 跟踪点 的 起始y。
    距离 为 做 触摸_计算距离 用 跟踪点 的 起始x、跟踪点 的 起始y、跟踪点 的 当前x、跟踪点 的 当前y。
    持续 为 (做 获取滴答 减 跟踪点 的 起始时间) 除以 1000。
    若 距离 小于 滑动_阈值：
        若 持续 大于或等于 长按_阈值_ms：
            全局_触摸统计 的 长按识别数 为 全局_触摸统计 的 长按识别数 加 1。
            返还 手势_长按。
        完。
        现在 为 做 获取滴答 除以 1000。
        若 现在 减 全局_手势状态 的 上次点击时间 小于 双击_间隔_ms：
            点击距离 为 做 触摸_计算距离 用 跟踪点 的 起始x、跟踪点 的 起始y、全局_手势状态 的 上次点击x、全局_手势状态 的 上次点击y。
            若 点击距离 小于 滑动_阈值：
                全局_触摸统计 的 双击识别数 为 全局_触摸统计 的 双击识别数 加 1。
                全局_手势状态 的 上次点击时间 为 0。
                返还 手势_双击。
            完。
        完。
        全局_手势状态 的 上次点击时间 为 现在。
        全局_手势状态 的 上次点击x 为 跟踪点 的 起始x。
        全局_手势状态 的 上次点击y 为 跟踪点 的 起始y。
        全局_触摸统计 的 点击识别数 为 全局_触摸统计 的 点击识别数 加 1。
        返还 手势_点击。
    完。
    全局_触摸统计 的 滑动识别数 为 全局_触摸统计 的 滑动识别数 加 1。
    abs_dx 为 dx。
    若 abs_dx 小于 0：
        abs_dx 为 0 减 abs_dx。
    完。
    abs_dy 为 dy。
    若 abs_dy 小于 0：
        abs_dy 为 0 减 abs_dy。
    完。
    若 abs_dx 大于 abs_dy：
        若 dx 大于 0：
            返还 手势_滑动右。
        完。
        返还 手势_滑动左。
    完。
    若 dy 大于 0：
        返还 手势_滑动下。
    完。
    返还 手势_滑动上。
完。

手势_识别双指 接受 零参：
    做 触摸统计_初始化。
    做 手势状态_初始化。
    可变 点1 为 负 1。
    可变 点2 为 负 1。
    可变 i 为 0。
    只要 i 小于 触摸_最大点数：
        若 全局_手势状态 的 跟踪点 的 第 i 项 的 活跃：
            若 点1 小于 0：
                点1 为 i。
            否则若 点2 小于 0：
                点2 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    若 点1 小于 0 或者 点2 小于 0：
        返还 手势_无。
    完。
    p1 为 全局_手势状态 的 跟踪点 的 第 点1 项。
    p2 为 全局_手势状态 的 跟踪点 的 第 点2 项。
    当前距离 为 做 触摸_计算距离 用 p1 的 当前x、p1 的 当前y、p2 的 当前x、p2 的 当前y。
    初始距离 为 做 触摸_计算距离 用 p1 的 起始x、p1 的 起始y、p2 的 起始x、p2 的 起始y。
    若 初始距离 等于 0：
        返还 手势_无。
    完。
    比率 为 当前距离 乘 100 除以 初始距离。
    若 比率 小于 75：
        全局_触摸统计 的 捏合识别数 为 全局_触摸统计 的 捏合识别数 加 1。
        返还 手势_捏合。
    否则若 比率 大于 125：
        全局_触摸统计 的 捏合识别数 为 全局_触摸统计 的 捏合识别数 加 1。
        返还 手势_张开。
    完。
    返还 手势_无。
完。

触摸开始_处理 接受 touches：
    做 触摸统计_初始化。
    全局_触摸统计 的 触摸开始次数 为 全局_触摸统计 的 触摸开始次数 加 1。
    若 touches 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 touches 的 长度：
        t 为 做 TouchList_获取 用 touches、i。
        若 t 不等于 空：
            做 手势_跟踪开始 用 t。
        完。
        i 为 i 加 1。
    完。
完。

触摸移动_处理 接受 touches：
    做 触摸统计_初始化。
    全局_触摸统计 的 触摸移动次数 为 全局_触摸统计 的 触摸移动次数 加 1。
    若 touches 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 touches 的 长度：
        t 为 做 TouchList_获取 用 touches、i。
        若 t 不等于 空：
            做 手势_跟踪移动 用 t。
        完。
        i 为 i 加 1。
    完。
完。

触摸结束_处理 接受 changedTouches：
    做 触摸统计_初始化。
    全局_触摸统计 的 触摸结束次数 为 全局_触摸统计 的 触摸结束次数 加 1。
    若 changedTouches 等于 0：
        返还 手势_无。
    完。
    做 手势状态_初始化。
    可变 手势 为 手势_无。
    若 全局_手势状态 的 活跃触摸数 等于 2：
        手势 为 做 手势_识别双指。
    完。
    可变 i 为 0。
    只要 i 小于 changedTouches 的 长度：
        t 为 做 TouchList_获取 用 changedTouches、i。
        若 t 不等于 空：
            若 手势 等于 手势_无：
                可变 j 为 0。
                只要 j 小于 触摸_最大点数：
                    若 全局_手势状态 的 跟踪点 的 第 j 项 的 活跃 并且 全局_手势状态 的 跟踪点 的 第 j 项 的 标识符 等于 t 的 标识符：
                        手势 为 做 手势_识别单指 用 全局_手势状态 的 跟踪点 的 第 j 项。
                        j 为 触摸_最大点数。
                    完。
                    j 为 j 加 1。
                完。
            完。
            做 手势_跟踪结束 用 t。
        完。
        i 为 i 加 1。
    完。
    全局_手势状态 的 当前手势 为 手势。
    返还 手势。
完。

触摸取消_处理 接受 touches：
    做 触摸统计_初始化。
    全局_触摸统计 的 触摸取消次数 为 全局_触摸统计 的 触摸取消次数 加 1。
    做 手势状态_初始化。
    可变 i 为 0。
    只要 i 小于 触摸_最大点数：
        全局_手势状态 的 跟踪点 的 第 i 项 的 活跃 为 假。
        i 为 i 加 1。
    完。
    全局_手势状态 的 活跃触摸数 为 0。
    全局_手势状态 的 当前手势 为 手势_无。
完。

触摸_客户端到页面 接受 客户端x、客户端y、滚动x、滚动y：
    返还 一组：客户端x 加 滚动x、客户端y 加 滚动y。
完。

触摸_页面到屏幕 接受 页面x、页面y、窗口左、窗口上：
    返还 一组：页面x 加 窗口左、页面y 加 窗口上。
完。

智能_事件_AI语义感知 接受 节点：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 节点 等于 0：
        返还。
    完。
    做 节点_AI语义分析 用 节点。
完。

智能_事件_AI事件优先级 接受 事件类型、目标节点：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 128。
    完。
    若 目标节点 不等于 0 并且 目标节点 的 注意力权重 大于 8000：
        返还 255。
    完。
    返还 128。
完。

智能_触摸_AI手势预测 接受 手势历史、历史长度：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 手势历史 等于 0 或者 历史长度 小于 3：
        返还 手势_无。
    完。
    最后 为 手势历史 的 第 历史长度 减 1 项。
    倒二 为 手势历史 的 第 历史长度 减 2 项。
    若 最后 等于 倒二：
        返还 最后。
    完。
    返还 手势_无。
完。

触摸事件_打印统计 接受 零参：
    做 触摸统计_初始化。
    做 打印行 用 「=== 触摸事件统计 ===」。
    做 打印行 用 「触摸开始: 」 加 做 整数转字符串 用 全局_触摸统计 的 触摸开始次数。
    做 打印行 用 「触摸移动: 」 加 做 整数转字符串 用 全局_触摸统计 的 触摸移动次数。
    做 打印行 用 「触摸结束: 」 加 做 整数转字符串 用 全局_触摸统计 的 触摸结束次数。
    做 打印行 用 「触摸取消: 」 加 做 整数转字符串 用 全局_触摸统计 的 触摸取消次数。
    做 打印行 用 「点击: 」 加 做 整数转字符串 用 全局_触摸统计 的 点击识别数。
    做 打印行 用 「双击: 」 加 做 整数转字符串 用 全局_触摸统计 的 双击识别数。
    做 打印行 用 「长按: 」 加 做 整数转字符串 用 全局_触摸统计 的 长按识别数。
    做 打印行 用 「滑动: 」 加 做 整数转字符串 用 全局_触摸统计 的 滑动识别数。
    做 打印行 用 「捏合/张开: 」 加 做 整数转字符串 用 全局_触摸统计 的 捏合识别数。
    做 打印行 用 「旋转: 」 加 做 整数转字符串 用 全局_触摸统计 的 旋转识别数。
    做 打印行 用 「TouchList创建: 」 加 做 整数转字符串 用 全局_触摸统计 的 TouchList创建数。
    做 打印行 用 「事件创建: 」 加 做 整数转字符串 用 全局_触摸统计 的 事件创建数。
完。

本章 收束。
