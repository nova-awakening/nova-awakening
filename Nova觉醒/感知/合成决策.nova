《合成决策》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·创建销毁》。

引用《..·推理·AI鼠标事件处理》。

绘制Layer_更新合成 接受 层：
    s 为 层 的 布局对象 的 样式。
    若 s 等于 0：
        返还。
    完。
    可变 需要合成 为 假。
    若 s 的 transform 不等于 0：
        需要合成 为 真。
    完。
    若 s 的 opacity 小于 100：
        需要合成 为 真。
    完。
    若 s 的 position 等于 Position_fixed：
        需要合成 为 真。
    完。
    若 需要合成：
        层 的 类型 为 层类型_合成。
        层 的 标志 为 层 的 标志 位或 层标志_是合成的。
        若 层 的 图形层 等于 0：
            层 的 图形层 为 做 GraphicsLayer_创建 用 层。
        完。
    完。
    可变 子 为 层 的 第一子层。
    只要 子 不等于 0：
        做 绘制Layer_更新合成 用 子。
        子 为 子 的 后兄层。
    完。
完。

GraphicsLayer 是 一种 事物，含 所属绘制层、纹理ID、宽、高、脏区域_x、脏区域_y、脏区域_w、脏区域_h、变换矩阵、透明度。

GraphicsLayer_创建 接受 绘制层：
    gl 为 做 堆_分配 用 GraphicsLayer。
    gl 的 所属绘制层 为 绘制层。
    gl 的 纹理ID 为 0。
    lo 为 绘制层 的 布局对象。
    若 lo 不等于 0：
        gl 的 宽 为 做 布局Object_边框盒宽 用 lo。
        gl 的 高 为 做 布局Object_边框盒高 用 lo。
    完。
    gl 的 脏区域_x 为 0。
    gl 的 脏区域_y 为 0。
    gl 的 脏区域_w 为 gl 的 宽。
    gl 的 脏区域_h 为 gl 的 高。
    gl 的 变换矩阵 为 0。
    gl 的 透明度 为 100。
    返还 gl。
完。

AI渲染_更新层可见性 接受 层、视口上、视口下：
    若 层 等于 0：
        返还。
    完。
    lo 为 层 的 布局对象。
    若 lo 等于 0：
        返还。
    完。
    层上 为 lo 的 绝对y。
    层下 为 lo 的 绝对y 加 做 布局Object_边框盒高 用 lo。
    层x 为 lo 的 绝对x。
    层w 为 做 布局Object_边框盒宽 用 lo。
    层h 为 层下 减 层上。
    是否可见 为 假。
    若 层下 小于或等于 视口上 或者 层上 大于或等于 视口下：
        层 的 视口可见 为 0。
    否则若 层上 大于或等于 视口上 并且 层下 小于或等于 视口下：
        层 的 视口可见 为 2。
        是否可见 为 真。
    否则：
        层 的 视口可见 为 1。
        是否可见 为 真。
    完。
    bus 为 全局AI总线。
    若 bus 不等于 0：
        语义权重 为 Q_HALF。
        若 lo 的 DOM节点 不等于 0：
            层 的 注意力权重 为 lo 的 DOM节点 的 注意力权重。
            语义权重 为 层 的 注意力权重 乘 Q_ONE 除以 10000。
        完。
        级别 为 做 AI总线_渲染质量 用 bus、层x、层上、层w、层h、语义权重、是否可见。
        若 级别 等于 渲染级别_跳过：
            层 的 渲染精度 为 0。
            层 的 标志 为 层 的 标志 位或 层标志_AI可跳过绘制。
            层 的 标志 为 层 的 标志 位与 按位取反 层标志_AI高优先级。
        否则若 级别 小于或等于 渲染级别_低：
            层 的 渲染精度 为 1。
            层 的 标志 为 层 的 标志 位或 层标志_AI低精度绘制。
            层 的 标志 为 层 的 标志 位与 按位取反 层标志_AI可跳过绘制。
        否则若 级别 小于或等于 渲染级别_中：
            层 的 渲染精度 为 2。
            层 的 标志 为 层 的 标志 位与 按位取反 层标志_AI可跳过绘制。
            层 的 标志 为 层 的 标志 位与 按位取反 层标志_AI低精度绘制。
        否则：
            层 的 渲染精度 为 3。
            层 的 标志 为 层 的 标志 位或 层标志_AI高优先级。
            层 的 标志 为 层 的 标志 位与 按位取反 层标志_AI可跳过绘制。
            层 的 标志 为 层 的 标志 位与 按位取反 层标志_AI低精度绘制。
        完。
    否则：
        若 非 是否可见：
            层 的 渲染精度 为 0。
            层 的 标志 为 层 的 标志 位或 层标志_AI可跳过绘制。
        否则：
            层 的 渲染精度 为 2。
            层 的 标志 为 层 的 标志 位与 按位取反 层标志_AI可跳过绘制。
        完。
    完。
    可变 子 为 层 的 第一子层。
    只要 子 不等于 0：
        做 AI渲染_更新层可见性 用 子、视口上、视口下。
        子 为 子 的 后兄层。
    完。
完。

本章 收束。
