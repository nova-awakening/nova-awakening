《Origin从URL》

引用《.·文档节点》。

引用《..·推理·通信渠道》。

引用《..·推理·通信总线》。

引用《..·中枢·内核打印》。

引用《..·中枢·整数转字符串》。

引用《..·推理·定点Q算术》。

引用《..·中枢·辅助存根》。

引用《.·样式解析》。

引用《..·中枢·字符串替换》。

引用《.·原子驻留》。

协议_HTTP 为 1。

协议_HTTPS 为 2。

协议_FILE 为 3。

协议_DATA 为 4。

协议_BLOB 为 5。

协议_NULL 为 0。

Origin 是 一种 事物，含 协议、主机、主机哈希、端口、是opaque。

Origin_从URL 接受 url：
    o 为 做 堆_分配 用 Origin。
    o 的 是opaque 为 假。
    若 做 URL_协议匹配 用 url、「https」：
        o 的 协议 为 协议_HTTPS。
        o 的 端口 为 443。
    否则若 做 URL_协议匹配 用 url、「http」：
        o 的 协议 为 协议_HTTP。
        o 的 端口 为 80。
    否则若 做 URL_协议匹配 用 url、「file」：
        o 的 协议 为 协议_FILE。
        o 的 是opaque 为 真。
    否则若 做 URL_协议匹配 用 url、「data」：
        o 的 协议 为 协议_DATA。
        o 的 是opaque 为 真。
    否则：
        o 的 协议 为 协议_NULL。
        o 的 是opaque 为 真。
    完。
    o 的 主机 为 做 URL_提取主机 用 url。
    o 的 主机哈希 为 做 字符串_哈希 用 o 的 主机。
    显式端口 为 做 URL_提取端口 用 url。
    若 显式端口 大于 0：
        o 的 端口 为 显式端口。
    完。
    返还 o。
完。

Origin_同源 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 假。
    完。
    若 a 的 是opaque 或者 b 的 是opaque：
        返还 假。
    完。
    若 a 的 协议 不等于 b 的 协议：
        返还 假。
    完。
    若 a 的 端口 不等于 b 的 端口：
        返还 假。
    完。
    若 a 的 主机哈希 不等于 b 的 主机哈希：
        返还 假。
    完。
    返还 做 字符串_相等 用 a 的 主机、b 的 主机。
完。

Origin_同站 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 假。
    完。
    若 a 的 协议 不等于 b 的 协议：
        返还 假。
    完。
    eTLD1_a 为 做 提取eTLD加1 用 a 的 主机。
    eTLD1_b 为 做 提取eTLD加1 用 b 的 主机。
    返还 做 字符串_相等 用 eTLD1_a、eTLD1_b。
完。

Origin_序列化 接受 o：
    若 o 等于 0 或者 o 的 是opaque：
        返还 「null」。
    完。
    可变。
    结果 为 「」。
    若 o 的 协议 等于 协议_HTTPS：
        结果 为 「https://」。
    否则若 o 的 协议 等于 协议_HTTP：
        结果 为 「http://」。
    否则：
        结果 为 「unknown://」。
    完。
    结果 为 结果 加 o 的 主机。
    若 o 的 协议 等于 协议_HTTPS 并且 o 的 端口 不等于 443：
        结果 为 结果 加 「:」 加 o 的 端口。
    否则若 o 的 协议 等于 协议_HTTP 并且 o 的 端口 不等于 80：
        结果 为 结果 加 「:」 加 o 的 端口。
    完。
    返还 结果。
完。

Origin_释放 接受 o：
    若 o 不等于 0：
        若 o 的 主机 不等于 0：
            做 堆_释放 用 o 的 主机。
        完。
        做 堆_释放 用 o。
    完。
完。

CORS_类型_简单 为 0。

CORS_类型_预检 为 1。

CORS_类型_带凭证 为 2。

CORS_结果_允许 为 0。

CORS_结果_拒绝 为 1。

CORS_结果_预检待定 为 2。

CORS请求 是 一种 事物，含 请求源、目标URL、目标源、方法、头列表、头数量、类型、携带凭证。

CORS响应 是 一种 事物，含 Allow_Origin、Allow_Methods、Allow_Headers、Expose_Headers、Max_Age、Allow_Credentials。

CORS_缓存容量 为 256。

可变 CORS_缓存 为 做 数组_创建 用 CORS_缓存容量、空。

可变 CORS_缓存数量 为 0。

CORS缓存项 是 一种 事物，含 请求源_哈希、目标源_哈希、方法、结果、过期时间。

CORS_检查 接受 请求：
    若 做 Origin_同源 用 请求 的 请求源、请求 的 目标源：
        返还 CORS_结果_允许。
    完。
    缓存结果 为 做 CORS_查找缓存 用 请求。
    若 缓存结果 大于或等于 0：
        返还 缓存结果。
    完。
    若 做 CORS_需要预检 用 请求：
        请求 的 类型 为 CORS_类型_预检。
        返还 CORS_结果_预检待定。
    完。
    返还 CORS_结果_允许。
完。

CORS_需要预检 接受 请求：
    若 请求 的 方法 不等于 「GET」 并且 请求 的 方法 不等于 「HEAD」 并且 请求 的 方法 不等于 「POST」：
        返还 真。
    完。
    可变。
    i 为 0。
    只要 i 小于 请求 的 头数量：
        若 非 做 CORS_是安全头 用 请求 的 头列表 的 第 i 项：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

CORS_是安全头 接受 头名：
    若 做 字符串_忽略大小写相等 用 头名、「Accept」：
        返还 真。
    完。
    若 做 字符串_忽略大小写相等 用 头名、「Accept-Language」：
        返还 真。
    完。
    若 做 字符串_忽略大小写相等 用 头名、「Content-Language」：
        返还 真。
    完。
    若 做 字符串_忽略大小写相等 用 头名、「Content-Type」：
        返还 真。
    完。
    返还 假。
完。

CORS_查找缓存 接受 请求：
    源哈希 为 请求 的 请求源 的 主机哈希。
    目标哈希 为 请求 的 目标源 的 主机哈希。
    当前时间 为 做 平台_获取单调时间_毫秒。
    可变。
    i 为 0。
    只要 i 小于 CORS_缓存数量：
        项 为 CORS_缓存 的 第 i 项。
        若 项 的 请求源_哈希 等于 源哈希 并且 项 的 目标源_哈希 等于 目标哈希：
            若 做 字符串_相等 用 项 的 方法、请求 的 方法：
                若 当前时间 小于 项 的 过期时间：
                    返还 项 的 结果。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

CORS_处理预检响应 接受 请求、响应：
    若 响应 的 Allow_Origin 不等于 「*」：
        源串 为 做 Origin_序列化 用 请求 的 请求源。
        若 非 做 字符串_相等 用 响应 的 Allow_Origin、源串：
            返还 CORS_结果_拒绝。
        完。
    完。
    若 请求 的 携带凭证 并且 响应 的 Allow_Origin 等于 「*」：
        返还 CORS_结果_拒绝。
    完。
    若 非 做 CORS_方法在列表中 用 请求 的 方法、响应 的 Allow_Methods：
        返还 CORS_结果_拒绝。
    完。
    可变。
    i 为 0。
    只要 i 小于 请求 的 头数量：
        若 非 做 CORS_头在列表中 用 请求 的 头列表 的 第 i 项、响应 的 Allow_Headers：
            返还 CORS_结果_拒绝。
        完。
        i 为 i 加 1。
    完。
    做 CORS_写入缓存 用 请求、CORS_结果_允许、响应 的 Max_Age。
    返还 CORS_结果_允许。
完。

CORS_方法在列表中 接受 方法、列表：
    可变。
    i 为 0。
    只要 i 小于 做 数组_长度 用 列表：
        若 做 字符串_忽略大小写相等 用 列表 的 第 i 项、方法：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

CORS_头在列表中 接受 头名、列表：
    可变。
    i 为 0。
    只要 i 小于 做 数组_长度 用 列表：
        若 做 字符串_忽略大小写相等 用 列表 的 第 i 项、头名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

CORS_写入缓存 接受 请求、结果、max_age：
    若 CORS_缓存数量 大于或等于 CORS_缓存容量：
        CORS_缓存 的 第 0 项 为 新建 CORS缓存项：请求源_哈希 为 请求 的 请求源 的 主机哈希、目标源_哈希 为 请求 的 目标源 的 主机哈希、方法 为 请求 的 方法、结果 为 结果、过期时间 为 做 平台_获取单调时间_毫秒 加 max_age 乘 1000。
    否则：
        CORS_缓存 的 第 CORS_缓存数量 项 为 新建 CORS缓存项：请求源_哈希 为 请求 的 请求源 的 主机哈希、目标源_哈希 为 请求 的 目标源 的 主机哈希、方法 为 请求 的 方法、结果 为 结果、过期时间 为 做 平台_获取单调时间_毫秒 加 max_age 乘 1000。
        CORS_缓存数量 为 CORS_缓存数量 加 1。
    完。
完。

CSP_指令_default_src 为 0。

CSP_指令_script_src 为 1。

CSP_指令_style_src 为 2。

CSP_指令_img_src 为 3。

CSP_指令_font_src 为 4。

CSP_指令_connect_src 为 5。

CSP_指令_media_src 为 6。

CSP_指令_object_src 为 7。

CSP_指令_frame_src 为 8。

CSP_指令_base_uri 为 9。

CSP_指令_form_action 为 10。

CSP_指令_frame_ancestors 为 11。

CSP_指令总数 为 12。

CSP_源_self 为 0。

CSP_源_none 为 1。

CSP_源_unsafe_inline 为 2。

CSP_源_unsafe_eval 为 3。

CSP_源_nonce 为 4。

CSP_源_hash 为 5。

CSP_源_主机 为 6。

CSP_源_通配 为 7。

CSP源表达式 是 一种 事物，含 类型、值、哈希。

CSP指令 是 一种 事物，含 指令类型、源列表、源数量。

CSP策略 是 一种 事物，含 指令列表、指令数量、仅报告。

CSP_解析 接受 策略字符串：
    可变。
    策略 为 新建 CSP策略：指令列表 为 做 数组_创建 用 CSP_指令总数、空、指令数量 为 0、仅报告 为 假。
    指令串列表 为 做 字符串_分割 用 策略字符串、「;」。
    可变。
    i 为 0。
    只要 i 小于 做 数组_长度 用 指令串列表：
        指令串 为 做 字符串_去空白 用 指令串列表 的 第 i 项。
        若 做 字符串_长度 用 指令串 大于 0：
            做 CSP_解析单指令 用 策略、指令串。
        完。
        i 为 i 加 1。
    完。
    返还 策略。
完。

CSP_解析单指令 接受 策略、指令串：
    词列表 为 做 字符串_分割 用 指令串、「 」。
    若 做 数组_长度 用 词列表 小于 1：
        返还。
    完。
    指令名 为 词列表 的 第 0 项。
    指令类型 为 做 CSP_解析指令名 用 指令名。
    若 指令类型 小于 0：
        返还。
    完。
    可变。
    指令 为 新建 CSP指令：指令类型 为 指令类型、源列表 为 做 数组_创建 用 16、空、源数量 为 0。
    可变。
    j 为 1。
    只要 j 小于 做 数组_长度 用 词列表：
        源 为 做 CSP_解析源表达式 用 词列表 的 第 j 项。
        若 源 不等于 空 并且 指令 的 源数量 小于 16：
            指令 的 源列表 的 第 指令 的 源数量 项 为 源。
            指令 的 源数量 为 指令 的 源数量 加 1。
        完。
        j 为 j 加 1。
    完。
    策略 的 指令列表 的 第 策略 的 指令数量 项 为 指令。
    策略 的 指令数量 为 策略 的 指令数量 加 1。
完。

CSP_解析指令名 接受 名称：
    若 做 字符串_相等 用 名称、「default-src」：
        返还 CSP_指令_default_src。
    完。
    若 做 字符串_相等 用 名称、「script-src」：
        返还 CSP_指令_script_src。
    完。
    若 做 字符串_相等 用 名称、「style-src」：
        返还 CSP_指令_style_src。
    完。
    若 做 字符串_相等 用 名称、「img-src」：
        返还 CSP_指令_img_src。
    完。
    若 做 字符串_相等 用 名称、「font-src」：
        返还 CSP_指令_font_src。
    完。
    若 做 字符串_相等 用 名称、「connect-src」：
        返还 CSP_指令_connect_src。
    完。
    若 做 字符串_相等 用 名称、「media-src」：
        返还 CSP_指令_media_src。
    完。
    若 做 字符串_相等 用 名称、「frame-src」：
        返还 CSP_指令_frame_src。
    完。
    若 做 字符串_相等 用 名称、「frame-ancestors」：
        返还 CSP_指令_frame_ancestors。
    完。
    返还 负 1。
完。

CSP_解析源表达式 接受 词：
    若 做 字符串_相等 用 词、「'self'」：
        返还 新建 CSP源表达式：类型 为 CSP_源_self、值 为 「」、哈希 为 0。
    完。
    若 做 字符串_相等 用 词、「'none'」：
        返还 新建 CSP源表达式：类型 为 CSP_源_none、值 为 「」、哈希 为 0。
    完。
    若 做 字符串_相等 用 词、「'unsafe-inline'」：
        返还 新建 CSP源表达式：类型 为 CSP_源_unsafe_inline、值 为 「」、哈希 为 0。
    完。
    若 做 字符串_相等 用 词、「'unsafe-eval'」：
        返还 新建 CSP源表达式：类型 为 CSP_源_unsafe_eval、值 为 「」、哈希 为 0。
    完。
    若 做 字符串_前缀 用 词、「'nonce-」：
        nonce值 为 做 字符串_子串 用 词、7、做 字符串_长度 用 词 减 8。
        返还 新建 CSP源表达式：类型 为 CSP_源_nonce、值 为 nonce值、哈希 为 0。
    完。
    返还 新建 CSP源表达式：类型 为 CSP_源_主机、值 为 词、哈希 为 做 字符串_哈希 用 词。
完。

CSP_检查 接受 策略、指令类型、资源URL、请求源：
    可变。
    指令 为 空。
    可变。
    i 为 0。
    只要 i 小于 策略 的 指令数量：
        若 策略 的 指令列表 的 第 i 项 的 指令类型 等于 指令类型：
            指令 为 策略 的 指令列表 的 第 i 项。
            跳出。
        完。
        i 为 i 加 1。
    完。
    若 指令 等于 空：
        i 为 0。
        只要 i 小于 策略 的 指令数量：
            若 策略 的 指令列表 的 第 i 项 的 指令类型 等于 CSP_指令_default_src：
                指令 为 策略 的 指令列表 的 第 i 项。
                跳出。
            完。
            i 为 i 加 1。
        完。
    完。
    若 指令 等于 空：
        返还 真。
    完。
    资源源 为 做 Origin_从URL 用 资源URL。
    i 为 0。
    只要 i 小于 指令 的 源数量：
        源 为 指令 的 源列表 的 第 i 项。
        若 源 的 类型 等于 CSP_源_none：
            做 Origin_释放 用 资源源。
            返还 假。
        完。
        若 源 的 类型 等于 CSP_源_self：
            若 做 Origin_同源 用 资源源、请求源：
                做 Origin_释放 用 资源源。
                返还 真。
            完。
        完。
        若 源 的 类型 等于 CSP_源_通配：
            做 Origin_释放 用 资源源。
            返还 真。
        完。
        若 源 的 类型 等于 CSP_源_主机：
            若 做 CSP_主机匹配 用 源 的 值、资源源 的 主机：
                做 Origin_释放 用 资源源。
                返还 真。
            完。
        完。
        i 为 i 加 1。
    完。
    做 Origin_释放 用 资源源。
    返还 假。
完。

CSP_主机匹配 接受 模式、主机：
    若 做 字符串_前缀 用 模式、「*.」：
        后缀 为 做 字符串_子串 用 模式、1、做 字符串_长度 用 模式 减 1。
        返还 做 字符串_后缀匹配 用 主机、后缀。
    完。
    返还 做 字符串_忽略大小写相等 用 模式、主机。
完。

可变 全局_安全检查总数 为 0。

可变 全局_AI检测总数 为 0。

可变 全局_威胁拦截数 为 0。

可变 全局_CSP违规数 为 0。

可变 全局_CORS拒绝数 为 0。

AI安全_检测URL异常 接受 url、请求源：
    全局_AI检测总数 为 全局_AI检测总数 加 1。
    特征 为 做 Tensor_创建 用 1、6。
    做 Tensor_写 用 特征、0、0、做 字符串_长度 用 url 乘 1.0。
    做 Tensor_写 用 特征、0、1、做 计算_特殊字符密度 用 url。
    做 Tensor_写 用 特征、0、2、做 计算_编码字符比例 用 url。
    做 Tensor_写 用 特征、0、3、做 计算_路径深度 用 url 乘 1.0。
    做 Tensor_写 用 特征、0、4、请求源 的 主机哈希 乘 1.0。
    做 Tensor_写 用 特征、0、5、做 平台_获取单调时间_毫秒 乘 1.0。
    风险分 为 做 AI总线_风险评分 用 「url_threat_detector」、特征。
    做 Tensor_释放 用 特征。
    若 风险分 大于 0.85：
        全局_威胁拦截数 为 全局_威胁拦截数 加 1。
        做 内核日志 用 「NWSO: AI检测高风险URL — 」 加 url 加 「 风险分:」 加 风险分。
        返还 真。
    完。
    返还 假。
完。

计算_特殊字符密度 接受 url：
    总长 为 做 字符串_长度 用 url。
    若 总长 等于 0：
        返还 0.0。
    完。
    可变。
    特殊数 为 0。
    可变。
    i 为 0。
    只要 i 小于 总长：
        c 为 做 字符串_字符 用 url、i。
        若 c 等于 「<」 或者 c 等于 「>」 或者 c 等于 「"」 或者 c 等于 「\'」 或者 c 等于 「%」 或者 c 等于 「{」 或者 c 等于 「}」：
            特殊数 为 特殊数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 特殊数 乘 1.0 除以 总长。
完。

计算_编码字符比例 接受 url：
    总长 为 做 字符串_长度 用 url。
    若 总长 等于 0：
        返还 0.0。
    完。
    可变。
    编码数 为 0。
    可变。
    i 为 0。
    只要 i 小于 总长 减 2：
        若 做 字符串_字符 用 url、i 等于 「%」：
            编码数 为 编码数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 编码数 乘 3.0 除以 总长。
完。

计算_路径深度 接受 url：
    可变。
    深度 为 0。
    可变。
    i 为 0。
    只要 i 小于 做 字符串_长度 用 url：
        若 做 字符串_字符 用 url、i 等于 「/」：
            深度 为 深度 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 深度。
完。

AI安全_XSS威胁评估 接受 内容、上下文类型：
    特征 为 做 Tensor_创建 用 1、4。
    做 Tensor_写 用 特征、0、0、做 字符串_长度 用 内容 乘 1.0。
    做 Tensor_写 用 特征、0、1、上下文类型 乘 1.0。
    做 Tensor_写 用 特征、0、2、做 计算_特殊字符密度 用 内容。
    做 Tensor_写 用 特征、0、3、全局_威胁拦截数 乘 1.0。
    风险分 为 做 AI总线_风险评分 用 「xss_threat_analyzer」、特征。
    做 Tensor_释放 用 特征。
    返还 风险分。
完。

安全模型_获取诊断 接受 零参：
    可变。
    报告 为 「═══ NWSO 安全模型诊断报告 ═══\n」。
    报告 为 报告 加 「安全检查总数: 」 加 全局_安全检查总数 加 「\n」。
    报告 为 报告 加 「AI检测总数: 」 加 全局_AI检测总数 加 「\n」。
    报告 为 报告 加 「威胁拦截数: 」 加 全局_威胁拦截数 加 「\n」。
    报告 为 报告 加 「CSP违规数: 」 加 全局_CSP违规数 加 「\n」。
    报告 为 报告 加 「CORS拒绝数: 」 加 全局_CORS拒绝数 加 「\n」。
    报告 为 报告 加 「CORS缓存使用: 」 加 CORS_缓存数量 加 「/」 加 CORS_缓存容量 加 「\n」。
    返还 报告。
完。

本章 收束。
