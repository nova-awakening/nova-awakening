《样式令牌器》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·CSS令牌化》。

引用《.·CSS单位与颜色解析》。

引用《..·中枢·辅助存根》。

引用《.·CSS声明解析》。

引用《.·内核直连层》。

引用《.·原子驻留》。

CSS令牌izer_读数字 接受 tk、令牌：
    开始 为 tk 的 位置。
    可变 负号 为 假。
    若 tk 的 位置 小于 tk 的 输入长度 并且 做 内存_读8 用 tk 的 输入 加 tk 的 位置 等于 0x2D：
        负号 为 真。
        tk 的 位置 为 tk 的 位置 加 1。
    完。
    可变 整数部分 为 0。
    只要 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_是数字 用 做 CSS令牌izer_窥视 用 tk：
        整数部分 为 整数部分 乘 10 加 (做 CSS令牌izer_消费 用 tk 减 0x30)。
    完。
    可变 小数部分 为 0。
    可变 小数位 为 0。
    若 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_窥视 用 tk 等于 0x2E：
        做 CSS令牌izer_消费 用 tk。
        只要 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_是数字 用 做 CSS令牌izer_窥视 用 tk：
            小数部分 为 小数部分 乘 10 加 (做 CSS令牌izer_消费 用 tk 减 0x30)。
            小数位 为 小数位 加 1。
        完。
    完。
    可变 数值 为 整数部分 乘 100。
    若 小数位 等于 1：
        数值 为 数值 加 小数部分 乘 10。
    否则若 小数位 等于 2：
        数值 为 数值 加 小数部分。
    否则若 小数位 大于或等于 3：
        数值 为 数值 加 小数部分 除以 (小数位 减 2)。
    完。
    若 负号：
        数值 为 0 减 数值。
    完。
    令牌 的 数值 为 数值。
    若 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_窥视 用 tk 等于 0x25：
        做 CSS令牌izer_消费 用 tk。
        令牌 的 类型 为 CSS_令牌_百分比。
        返还。
    完。
    若 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_是名称起始 用 做 CSS令牌izer_窥视 用 tk：
        令牌 的 类型 为 CSS_令牌_尺寸。
        令牌 的 单位 为 tk 的 输入 加 tk 的 位置。
        单位开始 为 tk 的 位置。
        只要 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_是名称字符 用 做 CSS令牌izer_窥视 用 tk：
            做 CSS令牌izer_消费 用 tk。
        完。
        令牌 的 单位长度 为 tk 的 位置 减 单位开始。
        返还。
    完。
    令牌 的 类型 为 CSS_令牌_数字。
完。

CSS令牌izer_读标识符 接受 tk、令牌：
    令牌 的 值 为 tk 的 输入 加 tk 的 位置。
    开始 为 tk 的 位置。
    只要 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_是名称字符 用 做 CSS令牌izer_窥视 用 tk：
        做 CSS令牌izer_消费 用 tk。
    完。
    令牌 的 值长度 为 tk 的 位置 减 开始。
    若 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_窥视 用 tk 等于 0x28：
        做 CSS令牌izer_消费 用 tk。
        令牌 的 类型 为 CSS_令牌_函数。
    否则：
        令牌 的 类型 为 CSS_令牌_标识符。
    完。
完。

CSS令牌izer_消费 接受 tk：
    若 tk 的 位置 大于或等于 tk 的 输入长度：
        返还 0。
    完。
    c 为 做 内存_读8 用 tk 的 输入 加 tk 的 位置。
    tk 的 位置 为 tk 的 位置 加 1。
    返还 c。
完。

CSS令牌izer_窥视 接受 tk：
    若 tk 的 位置 大于或等于 tk 的 输入长度：
        返还 0。
    完。
    返还 做 内存_读8 用 tk 的 输入 加 tk 的 位置。
完。

CSS令牌izer_跳过空白 接受 tk：
    只要 tk 的 位置 小于 tk 的 输入长度：
        c 为 做 内存_读8 用 tk 的 输入 加 tk 的 位置。
        若 c 等于 0x20 或者 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0D 或者 c 等于 0x0C：
            tk 的 位置 为 tk 的 位置 加 1。
        否则：
            返还。
        完。
    完。
完。

CSS令牌izer_是数字 接受 c：
    返还 c 大于或等于 0x30 并且 c 小于或等于 0x39。
完。

CSS令牌izer_是名称起始 接受 c：
    返还 c 大于或等于 0x41 并且 c 小于或等于 0x5A 或者 c 大于或等于 0x61 并且 c 小于或等于 0x7A 或者 c 等于 0x5F 或者 c 等于 0x2D 或者 c 大于 0x7F。
完。

CSS令牌izer_是名称字符 接受 c：
    返还 做 CSS令牌izer_是名称起始 用 c 或者 做 CSS令牌izer_是数字 用 c。
完。

CSSParser_解析样式表 接受 css文本：
    样式表 为 做 堆_分配 用 CSS样式表。
    样式表 的 规则容量 为 32。
    样式表 的 规则列表 为 做 堆_分配_数组 用 样式表 的 规则容量 乘 8。
    样式表 的 规则数量 为 0。
    样式表 的 来源URL 为 0。
    样式表 的 是内联 为 假。
    tk 为 做 CSS令牌izer_创建 用 css文本。
    令牌 为 做 堆_分配 用 CSS令牌。
    只要 1：
        做 CSS令牌izer_下一个令牌 用 tk、令牌。
        若 令牌 的 类型 等于 CSS_令牌_EOF：
            跳出。
        完。
        若 令牌 的 类型 等于 CSS_令牌_AT关键字：
            做 CSSParser_跳过AT规则 用 tk、令牌。
            继续。
        完。
        规则 为 做 CSSParser_解析样式规则 用 tk、令牌。
        若 规则 不等于 0：
            若 样式表 的 规则数量 大于或等于 样式表 的 规则容量：
                新容量 为 样式表 的 规则容量 乘 2。
                新列表 为 做 堆_分配_数组 用 新容量 乘 8。
                做 内存_复制 用 新列表、样式表 的 规则列表、样式表 的 规则数量 乘 8。
                做 堆_释放 用 样式表 的 规则列表。
                样式表 的 规则列表 为 新列表。
                样式表 的 规则容量 为 新容量。
            完。
            做 内存_写64 用 样式表 的 规则列表 加 样式表 的 规则数量 乘 8、规则。
            样式表 的 规则数量 为 样式表 的 规则数量 加 1。
        完。
    完。
    返还 样式表。
完。

CSSParser_解析样式规则 接受 tk、首令牌：
    规则 为 做 堆_分配 用 CSS样式规则。
    规则 的 声明容量 为 16。
    规则 的 声明列表 为 做 堆_分配_数组 用 规则 的 声明容量 乘 24。
    规则 的 声明数量 为 0。
    规则 的 选择器列表 为 做 CSSParser_解析选择器组 用 tk、首令牌。
    规则 的 选择器数量 为 做 CSS选择器_计数 用 规则 的 选择器列表。
    规则 的 特异度 为 做 CSSParser_计算特异度 用 规则 的 选择器列表。
    令牌 为 做 堆_分配 用 CSS令牌。
    只要 1：
        做 CSS令牌izer_下一个令牌 用 tk、令牌。
        若 令牌 的 类型 等于 CSS_令牌_EOF 或者 令牌 的 类型 等于 CSS_令牌_右大括号：
            跳出。
        完。
        若 令牌 的 类型 等于 CSS_令牌_标识符：
            声明 为 做 CSSParser_解析声明 用 tk、令牌。
            若 声明 不等于 0：
                若 规则 的 声明数量 小于 规则 的 声明容量：
                    做 内存_复制 用 规则 的 声明列表 加 规则 的 声明数量 乘 24、声明、24。
                    规则 的 声明数量 为 规则 的 声明数量 加 1。
                完。
            完。
        完。
    完。
    返还 规则。
完。

CSSParser_解析选择器组 接受 tk、首令牌：
    选择器 为 做 堆_分配 用 CSS选择器。
    选择器 的 下一个 为 0。
    选择器 的 组合器 为 组合器_无。
    若 首令牌 的 类型 等于 CSS_令牌_标识符：
        选择器 的 类型 为 选择器类型_标签。
        选择器 的 值 为 做 字符串_复制_N 用 首令牌 的 值、首令牌 的 值长度。
        选择器 的 值原子 为 做 原子_驻留 用 选择器 的 值。
    否则若 首令牌 的 类型 等于 CSS_令牌_哈希：
        选择器 的 类型 为 选择器类型_id。
        选择器 的 值 为 做 字符串_复制_N 用 首令牌 的 值、首令牌 的 值长度。
        选择器 的 值原子 为 做 原子_驻留 用 选择器 的 值。
    否则若 首令牌 的 类型 等于 CSS_令牌_分隔符：
        c 为 做 内存_读8 用 首令牌 的 值。
        若 c 等于 0x2E：
            名令牌 为 做 堆_分配 用 CSS令牌。
            做 CSS令牌izer_下一个令牌 用 tk、名令牌。
            选择器 的 类型 为 选择器类型_class。
            选择器 的 值 为 做 字符串_复制_N 用 名令牌 的 值、名令牌 的 值长度。
            选择器 的 值原子 为 做 原子_驻留 用 选择器 的 值。
        否则若 c 等于 0x2A：
            选择器 的 类型 为 选择器类型_通用。
        完。
    完。
    可变 当前 为 选择器。
    令牌 为 做 堆_分配 用 CSS令牌。
    只要 1：
        做 CSS令牌izer_下一个令牌 用 tk、令牌。
        若 令牌 的 类型 等于 CSS_令牌_左大括号 或者 令牌 的 类型 等于 CSS_令牌_EOF：
            跳出。
        完。
        若 令牌 的 类型 等于 CSS_令牌_分隔符：
            c 为 做 内存_读8 用 令牌 的 值。
            若 c 等于 0x3E：
                当前 的 组合器 为 组合器_子。
            否则若 c 等于 0x2B：
                当前 的 组合器 为 组合器_相邻兄弟。
            否则若 c 等于 0x7E：
                当前 的 组合器 为 组合器_一般兄弟。
            否则若 c 等于 0x2E：
                名令牌2 为 做 堆_分配 用 CSS令牌。
                做 CSS令牌izer_下一个令牌 用 tk、名令牌2。
                新部分 为 做 堆_分配 用 CSS选择器。
                新部分 的 类型 为 选择器类型_class。
                新部分 的 值 为 做 字符串_复制_N 用 名令牌2 的 值、名令牌2 的 值长度。
                新部分 的 值原子 为 做 原子_驻留 用 新部分 的 值。
                新部分 的 组合器 为 组合器_无。
                新部分 的 下一个 为 0。
                当前 的 下一个 为 新部分。
                当前 为 新部分。
            完。
        否则若 令牌 的 类型 等于 CSS_令牌_哈希：
            新部分 为 做 堆_分配 用 CSS选择器。
            新部分 的 类型 为 选择器类型_id。
            新部分 的 值 为 做 字符串_复制_N 用 令牌 的 值、令牌 的 值长度。
            新部分 的 值原子 为 做 原子_驻留 用 新部分 的 值。
            新部分 的 组合器 为 组合器_无。
            新部分 的 下一个 为 0。
            当前 的 下一个 为 新部分。
            当前 为 新部分。
        否则若 令牌 的 类型 等于 CSS_令牌_标识符：
            当前 的 组合器 为 组合器_后代。
            新部分 为 做 堆_分配 用 CSS选择器。
            新部分 的 类型 为 选择器类型_标签。
            新部分 的 值 为 做 字符串_复制_N 用 令牌 的 值、令牌 的 值长度。
            新部分 的 值原子 为 做 原子_驻留 用 新部分 的 值。
            新部分 的 组合器 为 组合器_无。
            新部分 的 下一个 为 0。
            当前 的 下一个 为 新部分。
            当前 为 新部分。
        否则若 令牌 的 类型 等于 CSS_令牌_冒号：
            做 CSS令牌izer_下一个令牌 用 tk、令牌。
            若 令牌 的 类型 等于 CSS_令牌_标识符：
                伪类 为 做 堆_分配 用 CSS选择器。
                伪类 的 类型 为 选择器类型_伪类。
                伪类 的 值 为 做 字符串_复制 用 令牌 的 值。
                伪类 的 值原子 为 做 原子_驻留 用 伪类 的 值。
                伪类 的 组合器 为 组合器_无。
                伪类 的 下一个 为 0。
                当前 的 下一个 为 伪类。
                当前 为 伪类。
            完。
        完。
    完。
    返还 选择器。
完。

令牌_是数字字符 接受 ch：
    返还 ch 大于或等于 48 并且 ch 小于或等于 57。
完。

令牌_是十六进制字符 接受 ch：
    返还 ch 大于或等于 48 并且 ch 小于或等于 57 或者 ch 大于或等于 65 并且 ch 小于或等于 70 或者 ch 大于或等于 97 并且 ch 小于或等于 102。
完。

令牌_读取整数 接受 源、位置、长度：
    可变 结果 为 0。
    可变 i 为 位置。
    只要 i 小于 长度 并且 做 令牌_是数字字符 用 做 字符串_字符在 用 源、i：
        结果 为 结果 乘 10 加 (做 字符串_字符在 用 源、i 减 48)。
        i 为 i 加 1。
    完。
    返还 结果。
完。

令牌_读取浮点 接受 源、位置、长度：
    整数部分 为 做 令牌_读取整数 用 源、位置、长度。
    返还 整数部分。
完。

令牌_跳过空白 接受 源、位置、长度：
    可变 i 为 位置。
    只要 i 小于 长度：
        ch 为 做 字符串_字符在 用 源、i。
        若 ch 不等于 32 并且 ch 不等于 9 并且 ch 不等于 10 并且 ch 不等于 13：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 i。
完。

可变 AI_样式令牌器_样式缓存命中 为 0。

AI_样式令牌器_选择器复杂度 接受 选择器长度、组合子数：
    若 组合子数 大于 5：
        返还 「高复杂度」。
    完。
    若 组合子数 大于 2：
        返还 「中复杂度」。
    完。
    返还 「低复杂度」。
完。

AI_样式令牌器_布局缓存 接受 元素哈希：
    AI_样式令牌器_样式缓存命中 为 AI_样式令牌器_样式缓存命中 加 1。
    返还 元素哈希。
完。

本章 收束。
