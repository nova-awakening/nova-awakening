《ResourceResponse》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·辅助存根》。

引用《.·HTTP头部管理》。

引用《..·执行·平台时间》。

引用《.·内核直连层》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

ResourceResponse 是 一种 事物，含 url、状态码、状态文本、响应头、内容类型、内容长度、字符编码、最后修改、ETag、是从缓存。

ResourceResponse_创建 接受 零参：
    resp 为 做 堆_分配 用 ResourceResponse。
    resp 的 url 为 0。
    resp 的 状态码 为 0。
    resp 的 状态文本 为 0。
    resp 的 响应头 为 做 HTTP头部_创建。
    resp 的 内容类型 为 0。
    resp 的 内容长度 为 负 1。
    resp 的 字符编码 为 做 字符串_复制 用 「utf-8」。
    resp 的 最后修改 为 0。
    resp 的 ETag 为 0。
    resp 的 是从缓存 为 假。
    返还 resp。
完。

Nova系统调用_网络_创建套接字 为 400。

Nova系统调用_网络_连接 为 401。

Nova系统调用_网络_发送 为 402。

Nova系统调用_网络_接收 为 403。

Nova系统调用_网络_关闭 为 404。

Nova系统调用_网络_域名解析 为 405。

Nova系统调用_网络_安全握手 为 406。

Nova系统调用_网络_安全发送 为 407。

Nova系统调用_网络_安全接收 为 408。

Nova系统调用_网络_非阻塞轮询 为 409。

套接字类型_传控 为 1。

套接字类型_数报 为 2。

网络_域名解析 接受 主机名：
    返还 做 系统调用 用 Nova系统调用_网络_域名解析、主机名、做 字符串_长度 用 主机名、0、0、0。
完。

网络_传控连接 接受 IP地址、端口：
    套接字 为 做 系统调用 用 Nova系统调用_网络_创建套接字、套接字类型_传控、0、0、0、0。
    若 套接字 小于 0：
        返还 负 1。
    完。
    结果 为 做 系统调用 用 Nova系统调用_网络_连接、套接字、IP地址、端口、0、0。
    若 结果 小于 0：
        返还 负 1。
    完。
    返还 套接字。
完。

网络_安全连接 接受 套接字、主机名：
    返还 做 系统调用 用 Nova系统调用_网络_安全握手、套接字、主机名、做 字符串_长度 用 主机名、0、0。
完。

网络_发送 接受 套接字、数据、长度、是安全：
    若 是安全：
        返还 做 系统调用 用 Nova系统调用_网络_安全发送、套接字、数据、长度、0、0。
    完。
    返还 做 系统调用 用 Nova系统调用_网络_发送、套接字、数据、长度、0、0。
完。

网络_接收 接受 套接字、缓冲、缓冲大小、是安全：
    若 是安全：
        返还 做 系统调用 用 Nova系统调用_网络_安全接收、套接字、缓冲、缓冲大小、0、0。
    完。
    返还 做 系统调用 用 Nova系统调用_网络_接收、套接字、缓冲、缓冲大小、0、0。
完。

网络_关闭 接受 套接字：
    做 系统调用 用 Nova系统调用_网络_关闭、套接字、0、0、0、0。
完。

HTTP_构建请求报文 接受 req：
    url 为 req 的 url。
    可变 报文 为 「」。
    若 req 的 方法 等于 HTTP_GET：
        报文 为 「GET 」。
    否则若 req 的 方法 等于 HTTP_POST：
        报文 为 「POST 」。
    否则若 req 的 方法 等于 HTTP_PUT：
        报文 为 「PUT 」。
    否则若 req 的 方法 等于 HTTP_DELETE：
        报文 为 「DELETE 」。
    否则若 req 的 方法 等于 HTTP_HEAD：
        报文 为 「HEAD 」。
    否则：
        报文 为 「GET 」。
    完。
    报文 为 做 字符串_连接 用 报文、url 的 路径。
    若 url 的 查询长度 大于 0：
        报文 为 做 字符串_连接 用 报文、「?」。
        报文 为 做 字符串_连接 用 报文、url 的 查询。
    完。
    报文 为 做 字符串_连接 用 报文、「 HTTP/1.1\r\n」。
    报文 为 做 字符串_连接 用 报文、「Host: 」。
    报文 为 做 字符串_连接 用 报文、url 的 主机。
    报文 为 做 字符串_连接 用 报文、「\r\n」。
    头 为 req 的 请求头。
    可变 i 为 0。
    只要 i 小于 头 的 数量：
        偏移 为 头 的 条目 加 i 乘 32。
        名 为 做 内存_读64 用 偏移。
        值 为 做 内存_读64 用 偏移 加 16。
        报文 为 做 字符串_连接 用 报文、名。
        报文 为 做 字符串_连接 用 报文、「: 」。
        报文 为 做 字符串_连接 用 报文、值。
        报文 为 做 字符串_连接 用 报文、「\r\n」。
        i 为 i 加 1。
    完。
    若 req 的 请求体 不等于 0 并且 req 的 请求体长度 大于 0：
        报文 为 做 字符串_连接 用 报文、「Content-Length: 」。
        报文 为 做 字符串_连接 用 报文、做 整数转字符串 用 req 的 请求体长度。
        报文 为 做 字符串_连接 用 报文、「\r\n」。
    完。
    报文 为 做 字符串_连接 用 报文、「\r\n」。
    若 req 的 请求体 不等于 0 并且 req 的 请求体长度 大于 0：
        报文 为 做 字符串_连接 用 报文、req 的 请求体。
    完。
    返还 报文。
完。

HTTP_解析响应头 接受 数据、数据长度、resp：
    可变 pos 为 0。
    只要 pos 小于 数据长度 并且 做 内存_读8 用 数据 加 pos 不等于 0x20：
        pos 为 pos 加 1。
    完。
    pos 为 pos 加 1。
    resp 的 状态码 为 0。
    只要 pos 小于 数据长度 并且 做 内存_读8 用 数据 加 pos 大于或等于 0x30 并且 做 内存_读8 用 数据 加 pos 小于或等于 0x39：
        resp 的 状态码 为 resp 的 状态码 乘 10 加 (做 内存_读8 用 数据 加 pos 减 0x30)。
        pos 为 pos 加 1。
    完。
    只要 pos 小于 数据长度 减 1：
        若 做 内存_读8 用 数据 加 pos 等于 0x0D 并且 做 内存_读8 用 数据 加 pos 加 1 等于 0x0A：
            pos 为 pos 加 2。
            跳出。
        完。
        pos 为 pos 加 1。
    完。
    只要 pos 小于 数据长度 减 1：
        若 做 内存_读8 用 数据 加 pos 等于 0x0D 并且 做 内存_读8 用 数据 加 pos 加 1 等于 0x0A：
            pos 为 pos 加 2。
            跳出。
        完。
        名开始 为 pos。
        只要 pos 小于 数据长度 并且 做 内存_读8 用 数据 加 pos 不等于 0x3A：
            pos 为 pos 加 1。
        完。
        名 为 做 字符串_复制_N 用 数据 加 名开始、pos 减 名开始。
        pos 为 pos 加 1。
        只要 pos 小于 数据长度 并且 做 内存_读8 用 数据 加 pos 等于 0x20：
            pos 为 pos 加 1。
        完。
        值开始 为 pos。
        只要 pos 小于 数据长度 减 1：
            若 做 内存_读8 用 数据 加 pos 等于 0x0D 并且 做 内存_读8 用 数据 加 pos 加 1 等于 0x0A：
                跳出。
            完。
            pos 为 pos 加 1。
        完。
        值 为 做 字符串_复制_N 用 数据 加 值开始、pos 减 值开始。
        pos 为 pos 加 2。
        做 HTTP头部_添加 用 resp 的 响应头、名、值。
        若 做 字符串_相等_不区分大小写 用 名、「Content-Type」：
            resp 的 内容类型 为 值。
        完。
        若 做 字符串_相等_不区分大小写 用 名、「Content-Length」：
            resp 的 内容长度 为 做 字符串转整数 用 值。
        完。
    完。
    返还 pos。
完。

可变 AI_资源响应创_带宽估计 为 0。

可变 AI_资源响应创_丢包窗口 为 一组：（空）。

AI_资源响应创_带宽预测 接受 发送字节、耗时毫秒：
    若 耗时毫秒 等于 0：
        返还 AI_资源响应创_带宽估计。
    完。
    当前 为 发送字节 乘 1000 除以 耗时毫秒。
    AI_资源响应创_带宽估计 为 AI_资源响应创_带宽估计 乘 700 除以 1000 加 当前 乘 300 除以 1000。
    返还 AI_资源响应创_带宽估计。
完。

AI_资源响应创_拥塞建议 接受 丢包率：
    做 追加 用 AI_资源响应创_丢包窗口、丢包率。
    若 做 计数 用 AI_资源响应创_丢包窗口 大于 10：
        做 列表_移除 用 AI_资源响应创_丢包窗口、0。
    完。
    若 丢包率 大于 50：
        返还 「减半窗口」。
    完。
    若 丢包率 小于 5：
        返还 「增大窗口」。
    完。
    返还 「保持」。
完。

本章 收束。
