《Crypto随机值》

引用《.·内核直连层》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·执行·控制台输出》。

引用《..·中枢·加密框架》。

引用《..·外联·TLS密钥派生》。

引用《..·中枢·加密通信》。

Crypto_getRandomValues 接受 buffer、length：
    若 buffer 等于 0 或者 length 等于 0：
        返还。
    完。
    若 length 大于 65536：
        返还。
    完。
    做 随机字节 用 buffer、length。
完。

Crypto_randomUUID 接受 零参：
    buf 为 做 堆_分配 用 16。
    做 随机字节 用 buf、16。
    b6 为 做 字节取 用 buf、6。
    做 字节写 用 buf、6、b6 位与 0x0F 位或 0x40。
    b8 为 做 字节取 用 buf、8。
    做 字节写 用 buf、8、b8 位与 0x3F 位或 0x80。
    uuid_str 为 做 堆_分配 用 37。
    做 UUID_格式化 用 buf、uuid_str。
    做 堆_释放 用 buf。
    返还 uuid_str。
完。

ALGO_SHA1 为 1。

ALGO_SHA256 为 2。

ALGO_SHA384 为 3。

ALGO_SHA512 为 4。

ALGO_AES_CBC 为 10。

ALGO_AES_GCM 为 11。

ALGO_AES_CTR 为 12。

ALGO_RSA_OAEP 为 20。

ALGO_RSA_PSS 为 21。

ALGO_ECDSA 为 30。

ALGO_ECDH 为 31。

ALGO_HMAC 为 40。

ALGO_PBKDF2 为 50。

ALGO_HKDF 为 51。

SubtleCrypto_digest 接受 算法、数据、长度：
    若 数据 等于 0 或者 长度 等于 0：
        返还 0。
    完。
    若 算法 等于 ALGO_SHA256：
        结果 为 做 堆_分配 用 32。
        做 SHA256_计算 用 数据、长度、结果。
        返还 结果。
    否则若 算法 等于 ALGO_SHA384：
        结果 为 做 堆_分配 用 48。
        返还 结果。
    否则若 算法 等于 ALGO_SHA512：
        结果 为 做 堆_分配 用 64。
        返还 结果。
    否则若 算法 等于 ALGO_SHA1：
        结果 为 做 堆_分配 用 20。
        返还 结果。
    完。
    返还 0。
完。

可变 全局WCSI统计 为 新建 WCSI统计结构。

WCSI统计结构 是 一种 事物，含 加密次数、解密次数、签名次数、验签次数、摘要次数、密钥生成次数、权限查询次数、通知发送次数、AI检测次数。

密钥用途_加密 为 0x01。

密钥用途_解密 为 0x02。

密钥用途_签名 为 0x04。

密钥用途_验签 为 0x08。

密钥用途_派生密钥 为 0x10。

密钥用途_包装密钥 为 0x20。

密钥用途_解包密钥 为 0x40。

密钥类型_私钥 为 1。

密钥类型_公钥 为 2。

密钥类型_对称 为 3。

CryptoKey 是 一种 事物，含 类型、算法、可提取、用途、密钥数据、密钥长度、创建时间戳。

CryptoKey_创建 接受 类型、算法、可提取、用途、密钥数据、密钥长度：
    key 为 做 堆_分配 用 CryptoKey。
    若 key 等于 0：
        返还 空。
    完。
    key 的 类型 为 类型。
    key 的 算法 为 算法。
    key 的 可提取 为 可提取。
    key 的 用途 为 用途。
    key 的 密钥长度 为 密钥长度。
    key 的 密钥数据 为 做 堆_分配 用 密钥长度。
    若 key 的 密钥数据 不等于 0 并且 密钥数据 不等于 0：
        做 内存_复制 用 key 的 密钥数据、密钥数据、密钥长度。
    完。
    key 的 创建时间戳 为 做 系统_时间戳。
    返还 key。
完。

CryptoKey_释放 接受 key：
    若 key 等于 空：
        返还。
    完。
    若 key 的 密钥数据 不等于 0：
        做 内存_置零 用 key 的 密钥数据、key 的 密钥长度。
        做 堆_释放 用 key 的 密钥数据。
    完。
    做 堆_释放 用 key。
完。

CryptoKey_检查用途 接受 key、所需用途：
    若 key 等于 空：
        返还 假。
    完。
    返还 key 的 用途 位与 所需用途 不等于 0。
完。

SubtleCrypto_encrypt 接受 算法、key、iv、明文、明文长、结果长指针：
    若 key 等于 空 或者 明文 等于 0：
        返还 0。
    完。
    若 非 做 CryptoKey_检查用途 用 key、密钥用途_加密：
        返还 0。
    完。
    可变 密文 为 0。
    可变 密文长 为 0。
    若 算法 等于 ALGO_AES_CBC：
        密文长 为 (明文长 除以 16 加 1) 乘 16。
        密文 为 做 堆_分配 用 密文长。
        若 密文 等于 0：
            返还 0。
        完。
        做 AES_CBC_加密 用 key 的 密钥数据、key 的 密钥长度、iv、明文、明文长、密文。
    否则若 算法 等于 ALGO_AES_GCM：
        密文长 为 明文长 加 16。
        密文 为 做 堆_分配 用 密文长。
        若 密文 等于 0：
            返还 0。
        完。
        做 AES_GCM_加密 用 key 的 密钥数据、key 的 密钥长度、iv、明文、明文长、密文。
    否则若 算法 等于 ALGO_AES_CTR：
        密文长 为 明文长。
        密文 为 做 堆_分配 用 密文长。
        若 密文 等于 0：
            返还 0。
        完。
        做 AES_CTR_加密 用 key 的 密钥数据、key 的 密钥长度、iv、明文、明文长、密文。
    否则：
        做 内核打印 用 「[WCSI] 不支持的加密算法: {}」、算法。
        返还 0。
    完。
    若 结果长指针 不等于 0：
        做 指针写 用 结果长指针、密文长。
    完。
    全局WCSI统计 的 加密次数 为 全局WCSI统计 的 加密次数 加 1。
    返还 密文。
完。

SubtleCrypto_decrypt 接受 算法、key、iv、密文、密文长、结果长指针：
    若 key 等于 空 或者 密文 等于 0：
        返还 0。
    完。
    若 非 做 CryptoKey_检查用途 用 key、密钥用途_解密：
        返还 0。
    完。
    可变 明文 为 0。
    可变 明文长 为 0。
    若 算法 等于 ALGO_AES_CBC：
        明文长 为 密文长。
        明文 为 做 堆_分配 用 明文长。
        若 明文 等于 0：
            返还 0。
        完。
        做 AES_CBC_解密 用 key 的 密钥数据、key 的 密钥长度、iv、密文、密文长、明文。
    否则若 算法 等于 ALGO_AES_GCM：
        若 密文长 小于 16：
            返还 0。
        完。
        明文长 为 密文长 减 16。
        明文 为 做 堆_分配 用 明文长。
        若 明文 等于 0：
            返还 0。
        完。
        验证结果 为 做 AES_GCM_解密 用 key 的 密钥数据、key 的 密钥长度、iv、密文、密文长、明文。
        若 验证结果 不等于 0：
            做 内存_置零 用 明文、明文长。
            做 堆_释放 用 明文。
            返还 0。
        完。
    否则若 算法 等于 ALGO_AES_CTR：
        明文长 为 密文长。
        明文 为 做 堆_分配 用 明文长。
        若 明文 等于 0：
            返还 0。
        完。
        做 AES_CTR_解密 用 key 的 密钥数据、key 的 密钥长度、iv、密文、密文长、明文。
    否则：
        返还 0。
    完。
    若 结果长指针 不等于 0：
        做 指针写 用 结果长指针、明文长。
    完。
    全局WCSI统计 的 解密次数 为 全局WCSI统计 的 解密次数 加 1。
    返还 明文。
完。

SubtleCrypto_sign 接受 算法、key、数据、数据长、结果长指针：
    若 key 等于 空 或者 数据 等于 0：
        返还 0。
    完。
    若 非 做 CryptoKey_检查用途 用 key、密钥用途_签名：
        返还 0。
    完。
    可变 签名 为 0。
    可变 签名长 为 0。
    若 算法 等于 ALGO_HMAC：
        签名长 为 32。
        签名 为 做 堆_分配 用 签名长。
        若 签名 等于 0：
            返还 0。
        完。
        做 HMAC_计算 用 key 的 密钥数据、key 的 密钥长度、数据、数据长、签名。
    否则若 算法 等于 ALGO_ECDSA：
        签名长 为 64。
        签名 为 做 堆_分配 用 签名长。
        若 签名 等于 0：
            返还 0。
        完。
        做 ECDSA_签名 用 key 的 密钥数据、key 的 密钥长度、数据、数据长、签名。
    否则若 算法 等于 ALGO_RSA_PSS：
        签名长 为 key 的 密钥长度。
        签名 为 做 堆_分配 用 签名长。
        若 签名 等于 0：
            返还 0。
        完。
        做 RSA_PSS_签名 用 key 的 密钥数据、key 的 密钥长度、数据、数据长、签名。
    否则：
        返还 0。
    完。
    若 结果长指针 不等于 0：
        做 指针写 用 结果长指针、签名长。
    完。
    全局WCSI统计 的 签名次数 为 全局WCSI统计 的 签名次数 加 1。
    返还 签名。
完。

SubtleCrypto_verify 接受 算法、key、数据、数据长、签名、签名长：
    若 key 等于 空 或者 数据 等于 0 或者 签名 等于 0：
        返还 假。
    完。
    若 非 做 CryptoKey_检查用途 用 key、密钥用途_验签：
        返还 假。
    完。
    可变 结果 为 假。
    若 算法 等于 ALGO_HMAC：
        期望 为 做 堆_分配 用 32。
        若 期望 不等于 0：
            做 HMAC_计算 用 key 的 密钥数据、key 的 密钥长度、数据、数据长、期望。
            结果 为 做 内存_安全比较 用 签名、期望、32。
            做 堆_释放 用 期望。
        完。
    否则若 算法 等于 ALGO_ECDSA：
        结果 为 做 ECDSA_验签 用 key 的 密钥数据、key 的 密钥长度、数据、数据长、签名、签名长。
    否则若 算法 等于 ALGO_RSA_PSS：
        结果 为 做 RSA_PSS_验签 用 key 的 密钥数据、key 的 密钥长度、数据、数据长、签名、签名长。
    完。
    全局WCSI统计 的 验签次数 为 全局WCSI统计 的 验签次数 加 1。
    返还 结果。
完。

SubtleCrypto_generateKey 接受 算法、密钥长度位、可提取、用途：
    密钥字节 为 密钥长度位 除以 8。
    若 密钥字节 等于 0：
        密钥字节 为 32。
    完。
    若 算法 等于 ALGO_AES_CBC 或者 算法 等于 ALGO_AES_GCM 或者 算法 等于 ALGO_AES_CTR：
        若 密钥字节 不等于 16 并且 密钥字节 不等于 24 并且 密钥字节 不等于 32：
            密钥字节 为 32。
        完。
        raw 为 做 堆_分配 用 密钥字节。
        若 raw 等于 0：
            返还 空。
        完。
        做 随机字节 用 raw、密钥字节。
        key 为 做 CryptoKey_创建 用 密钥类型_对称、算法、可提取、用途、raw、密钥字节。
        做 内存_置零 用 raw、密钥字节。
        做 堆_释放 用 raw。
        全局WCSI统计 的 密钥生成次数 为 全局WCSI统计 的 密钥生成次数 加 1。
        返还 key。
    完。
    若 算法 等于 ALGO_HMAC：
        raw 为 做 堆_分配 用 密钥字节。
        若 raw 等于 0：
            返还 空。
        完。
        做 随机字节 用 raw、密钥字节。
        key 为 做 CryptoKey_创建 用 密钥类型_对称、ALGO_HMAC、可提取、用途、raw、密钥字节。
        做 内存_置零 用 raw、密钥字节。
        做 堆_释放 用 raw。
        全局WCSI统计 的 密钥生成次数 为 全局WCSI统计 的 密钥生成次数 加 1。
        返还 key。
    完。
    做 内核打印 用 「[WCSI] 不支持的密钥生成算法: {}」、算法。
    返还 空。
完。

SubtleCrypto_importKey 接受 格式、密钥数据、密钥长度、算法、可提取、用途：
    若 密钥数据 等于 0 或者 密钥长度 等于 0：
        返还 空。
    完。
    可变 类型 为 密钥类型_对称。
    若 格式 等于 1：
        类型 为 密钥类型_私钥。
    完。
    若 格式 等于 2：
        类型 为 密钥类型_公钥。
    完。
    返还 做 CryptoKey_创建 用 类型、算法、可提取、用途、密钥数据、密钥长度。
完。

SubtleCrypto_exportKey 接受 格式、key、结果长指针：
    若 key 等于 空：
        返还 0。
    完。
    若 key 的 可提取 等于 0：
        返还 0。
    完。
    导出 为 做 堆_分配 用 key 的 密钥长度。
    若 导出 等于 0：
        返还 0。
    完。
    做 内存_复制 用 导出、key 的 密钥数据、key 的 密钥长度。
    若 结果长指针 不等于 0：
        做 指针写 用 结果长指针、key 的 密钥长度。
    完。
    返还 导出。
完。

SubtleCrypto_deriveBits 接受 算法、基础密钥、盐、盐长、迭代次数、输出位数：
    若 基础密钥 等于 空：
        返还 0。
    完。
    输出字节 为 输出位数 除以 8。
    若 输出字节 等于 0：
        输出字节 为 32。
    完。
    输出 为 做 堆_分配 用 输出字节。
    若 输出 等于 0：
        返还 0。
    完。
    若 算法 等于 ALGO_PBKDF2：
        若 迭代次数 小于 100000：
            迭代次数 为 100000。
        完。
        做 PBKDF2_派生 用 基础密钥 的 密钥数据、基础密钥 的 密钥长度、盐、盐长、迭代次数、输出、输出字节。
    否则若 算法 等于 ALGO_HKDF：
        做 HKDF_派生 用 基础密钥 的 密钥数据、基础密钥 的 密钥长度、盐、盐长、输出、输出字节。
    否则：
        做 堆_释放 用 输出。
        返还 0。
    完。
    返还 输出。
完。

权限状态_已授权 为 0。

权限状态_已拒绝 为 1。

权限状态_待询问 为 2。

权限名_通知 为 1。

权限名_地理位置 为 2。

权限名_摄像头 为 3。

权限名_麦克风 为 4。

权限名_剥贴板 为 5。

权限名_屏幕唤醒 为 6。

权限状态 是 一种 事物，含 权限名、状态、变更回调。

Permissions_查询 接受 权限名：
    status 为 做 堆_分配 用 权限状态。
    若 status 等于 0：
        返还 空。
    完。
    status 的 权限名 为 权限名。
    status 的 变更回调 为 0。
    status 的 状态 为 做 内核_权限查询 用 权限名。
    全局WCSI统计 的 权限查询次数 为 全局WCSI统计 的 权限查询次数 加 1。
    返还 status。
完。

Permissions_请求 接受 权限名：
    当前 为 做 Permissions_查询 用 权限名。
    若 当前 不等于 空 并且 当前 的 状态 等于 权限状态_已授权：
        返还 当前。
    完。
    用户结果 为 做 内核_权限弹窗 用 权限名。
    若 当前 不等于 空：
        当前 的 状态 为 用户结果。
        做 内核_权限存储 用 权限名、用户结果。
    完。
    返还 当前。
完。

通知 是 一种 事物，含 标题、内容、图标、标签、需要交互、时间戳。

Notification_请求权限 接受 零参：
    状态 为 做 Permissions_请求 用 权限名_通知。
    若 状态 等于 空：
        返还 权限状态_已拒绝。
    完。
    返还 状态 的 状态。
完。

Notification_创建 接受 标题、内容、图标、标签：
    perm 为 做 Permissions_查询 用 权限名_通知。
    若 perm 等于 空 或者 perm 的 状态 不等于 权限状态_已授权：
        返还 空。
    完。
    n 为 做 堆_分配 用 通知。
    若 n 等于 0：
        返还 空。
    完。
    n 的 标题 为 标题。
    n 的 内容 为 内容。
    n 的 图标 为 图标。
    n 的 标签 为 标签。
    n 的 需要交互 为 0。
    n 的 时间戳 为 做 系统_时间戳。
    做 内核_显示通知 用 n。
    全局WCSI统计 的 通知发送次数 为 全局WCSI统计 的 通知发送次数 加 1。
    返还 n。
完。

Notification_关闭 接受 通知对象：
    若 通知对象 等于 空：
        返还。
    完。
    做 内核_关闭通知 用 通知对象。
    做 堆_释放 用 通知对象。
完。

AI_加密风险评估 接受 算法、密钥长度、操作类型：
    特征 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 特征、0、算法。
    做 Tensor_写 用 特征、1、密钥长度。
    做 Tensor_写 用 特征、2、操作类型。
    做 Tensor_写 用 特征、3、做 系统_时间戳。
    风险 为 做 AI总线_风险评分 用 全局AI总线、特征。
    做 Tensor_释放 用 特征。
    全局WCSI统计 的 AI检测次数 为 全局WCSI统计 的 AI检测次数 加 1。
    若 风险 大于 800：
        做 内核打印 用 「[WCSI] 高风险加密操作! 算法={} 密钥长={} 风险分={}」、算法、密钥长度、风险。
    完。
    返还 风险。
完。

WCSI_打印诊断 接受 零参：
    做 内核打印 用 「╔════════════════════════════════════════════╗」。
    做 内核打印 用 「║  WCSI Web加密安全智能 诊断报告         ║」。
    做 内核打印 用 「╠════════════════════════════════════════════╣」。
    做 内核打印 用 「║ 加密次数:    {}」、全局WCSI统计 的 加密次数。
    做 内核打印 用 「║ 解密次数:    {}」、全局WCSI统计 的 解密次数。
    做 内核打印 用 「║ 签名次数:    {}」、全局WCSI统计 的 签名次数。
    做 内核打印 用 「║ 验签次数:    {}」、全局WCSI统计 的 验签次数。
    做 内核打印 用 「║ 摘要次数:    {}」、全局WCSI统计 的 摘要次数。
    做 内核打印 用 「║ 密钥生成:    {}」、全局WCSI统计 的 密钥生成次数。
    做 内核打印 用 「║ 权限查询:    {}」、全局WCSI统计 的 权限查询次数。
    做 内核打印 用 「║ 通知发送:    {}」、全局WCSI统计 的 通知发送次数。
    做 内核打印 用 「║ AI检测:      {}」、全局WCSI统计 的 AI检测次数。
    做 内核打印 用 「╚════════════════════════════════════════════╝」。
完。

可变 _谐振熵池_采样次数 为 0。

可变 _谐振熵池_低熵警告 为 0。

谐振熵池_评估质量 接受 样本、样本长度：
    _谐振熵池_采样次数 为 _谐振熵池_采样次数 加 1。
    可变 出现表 为 一组：（空）。
    可变 唯一数 为 0。
    可变 i 为 0。
    只要 i 小于 样本长度：
        可变 已有 为 假。
        可变 j 为 0。
        只要 j 小于 唯一数：
            若 出现表 的 第 j 项 等于 样本 的 第 i 项：
                已有 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 已有 并且 唯一数 小于 256：
            做 追加 用 出现表、样本 的 第 i 项。
            唯一数 为 唯一数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 样本长度 大于 0 并且 唯一数 乘 100 除以 样本长度 小于 50：
        _谐振熵池_低熵警告 为 _谐振熵池_低熵警告 加 1。
    完。
    返还 唯一数。
完。

随机数_完整统计 接受 零参：
    返还 一组：「谐振采样次数」、_谐振熵池_采样次数、「谐振低熵警告」、_谐振熵池_低熵警告。
完。

本章 收束。
