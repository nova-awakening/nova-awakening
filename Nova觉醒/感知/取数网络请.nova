《取数网络请》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·标准·JS值转字符串》。

引用《.·内核直连层》。

引用《.·请求对象》。

引用《..·执行·接口绑定》。

引用《.·资源加载资源》。

引用《..·中枢·整数转字符串》。

引用《.·HTTP头部管理》。

引用《..·中枢·内存初始化》。

引用《..·推理·小整数SMI判定》。

引用《..·标准·加法运算符》。

引用《.·项解析》。

引用《..·中枢·辅助存根》。

取数_执行 接受 Nova运行时实例、url、选项：
    promise 为 做 Nova承诺_创建 用 Nova运行时实例。
    req 为 做 取数Request_创建 用 url、选项。
    若 req 的 信号 不等于 0：
        aborted 为 做 Nova对象_获取属性 用 Nova运行时实例、req 的 信号、「aborted」。
        若 aborted 不等于 0 并且 做 JSValue_IsTrue 用 Nova运行时实例、aborted：
            做 Nova承诺_reject 用 Nova运行时实例、promise、做 Nova值_创建_String 用 Nova运行时实例、「Abort错误: The operation was aborted」。
            返还 promise。
        完。
    完。
    方法 为 HTTP_GET。
    若 做 字符串_相等 用 req 的 方法、「POST」：
        方法 为 HTTP_POST。
    否则若 做 字符串_相等 用 req 的 方法、「PUT」：
        方法 为 HTTP_PUT。
    否则若 做 字符串_相等 用 req 的 方法、「DELETE」：
        方法 为 HTTP_DELETE。
    完。
    loader 为 做 ResourceLoader_创建 用 req 的 URL、资源类型_XHR。
    loader 的 请求 的 方法 为 方法。
    可变 i 为 0。
    只要 i 小于 req 的 头 的 数量：
        偏移 为 req 的 头 的 条目 加 i 乘 16。
        做 HTTP头部_添加 用 loader 的 请求 的 请求头、做 内存_读64 用 偏移、做 内存_读64 用 偏移 加 8。
        i 为 i 加 1。
    完。
    若 req 的 体 不等于 0 并且 req 的 体长度 大于 0：
        loader 的 请求 的 请求体 为 req 的 体。
        loader 的 请求 的 请求体长度 为 req 的 体长度。
    完。
    做 ResourceLoader_完整加载 用 loader。
    若 loader 的 状态 等于 请求状态_完成：
        response 为 做 取数Response_创建 用 loader 的 响应 的 状态码、loader 的 接收缓冲、loader 的 已接收字节、loader 的 响应 的 响应头。
        response 的 URL 为 做 字符串_复制 用 req 的 URL。
        js_resp 为 做 取数_包装Response 用 Nova运行时实例、response。
        做 Nova承诺_resolve 用 Nova运行时实例、promise、js_resp。
    否则：
        做 Nova承诺_reject 用 Nova运行时实例、promise、做 Nova值_创建_String 用 Nova运行时实例、「Type错误: Failed to fetch」。
    完。
    返还 promise。
完。

取数_包装Response 接受 Nova运行时实例、resp：
    obj 为 做 Nova对象_创建 用 Nova运行时实例、Nova运行时实例 的 Object原型。
    做 Nova对象_设置属性 用 Nova运行时实例、obj、「ok」、若 resp 的 OK 则 Nova运行时实例 的 true值 否则 Nova运行时实例 的 false值。
    做 Nova对象_设置属性 用 Nova运行时实例、obj、「status」、做 Nova值_创建_Number 用 Nova运行时实例、resp 的 状态码。
    做 Nova对象_设置属性 用 Nova运行时实例、obj、「statusText」、做 Nova值_创建_String 用 Nova运行时实例、resp 的 状态文本。
    做 Nova对象_设置属性 用 Nova运行时实例、obj、「redirected」、若 resp 的 是否重定向 则 Nova运行时实例 的 true值 否则 Nova运行时实例 的 false值。
    若 resp 的 URL 不等于 0：
        做 Nova对象_设置属性 用 Nova运行时实例、obj、「url」、做 Nova值_创建_String 用 Nova运行时实例、resp 的 URL。
    完。
    headers_obj 为 做 Nova对象_创建 用 Nova运行时实例、Nova运行时实例 的 Object原型。
    做 Nova对象_设置属性 用 Nova运行时实例、headers_obj、「get」、做 JSNative_创建 用 Nova运行时实例、1000、resp 的 头。
    做 Nova对象_设置属性 用 Nova运行时实例、headers_obj、「has」、做 JSNative_创建 用 Nova运行时实例、1001、resp 的 头。
    做 Nova对象_设置属性 用 Nova运行时实例、obj、「headers」、headers_obj。
    做 Nova对象_设置属性 用 Nova运行时实例、obj、「text」、做 JSNative_创建 用 Nova运行时实例、1010、resp。
    做 Nova对象_设置属性 用 Nova运行时实例、obj、「json」、做 JSNative_创建 用 Nova运行时实例、1011、resp。
    做 Nova对象_设置属性 用 Nova运行时实例、obj、「blob」、做 JSNative_创建 用 Nova运行时实例、1012、resp。
    做 Nova对象_设置属性 用 Nova运行时实例、obj、「arrayBuffer」、做 JSNative_创建 用 Nova运行时实例、1013、resp。
    做 Nova对象_设置属性 用 Nova运行时实例、obj、「clone」、做 JSNative_创建 用 Nova运行时实例、1014、resp。
    做 Nova对象_设置属性 用 Nova运行时实例、obj、「bodyUsed」、Nova运行时实例 的 false值。
    返还 obj。
完。

Nova承诺 是 一种 事物，含 状态、结果、then回调、catch回调、回调数量、回调容量。

Nova承诺_创建 接受 Nova运行时实例：
    p 为 做 NovaGC_新生代分配 用 Nova运行时实例 的 heap、128。
    做 内存_写32 用 p、JS类型_承诺。
    做 内存_写32 用 p 加 4、GC_白。
    做 内存_写32 用 p 加 8、0。
    做 内存_写64 用 p 加 16、0。
    做 内存_写64 用 p 加 24、做 堆_分配_数组 用 8 乘 8。
    做 内存_写64 用 p 加 32、做 堆_分配_数组 用 8 乘 8。
    做 内存_写32 用 p 加 40、0。
    做 内存_写32 用 p 加 44、8。
    返还 p。
完。

Nova承诺_resolve 接受 Nova运行时实例、promise、值：
    若 做 内存_读32 用 promise 加 8 不等于 0：
        返还。
    完。
    做 内存_写32 用 promise 加 8、1。
    做 内存_写64 用 promise 加 16、值。
    回调数 为 做 内存_读32 用 promise 加 40。
    then列表 为 做 内存_读64 用 promise 加 24。
    可变 i 为 0。
    只要 i 小于 回调数：
        回调 为 做 内存_读64 用 then列表 加 i 乘 8。
        若 回调 不等于 0：
            做 Nova运行时实例_排队微任务 用 Nova运行时实例、回调。
        完。
        i 为 i 加 1。
    完。
完。

Nova承诺_reject 接受 Nova运行时实例、promise、原因：
    若 做 内存_读32 用 promise 加 8 不等于 0：
        返还。
    完。
    做 内存_写32 用 promise 加 8、2。
    做 内存_写64 用 promise 加 16、原因。
    回调数 为 做 内存_读32 用 promise 加 40。
    catch列表 为 做 内存_读64 用 promise 加 32。
    可变 i 为 0。
    只要 i 小于 回调数：
        回调 为 做 内存_读64 用 catch列表 加 i 乘 8。
        若 回调 不等于 0：
            做 Nova运行时实例_排队微任务 用 Nova运行时实例、回调。
        完。
        i 为 i 加 1。
    完。
完。

Nova承诺_then 接受 Nova运行时实例、promise、on_fulfilled、on_rejected：
    新promise 为 做 Nova承诺_创建 用 Nova运行时实例。
    状态 为 做 内存_读32 用 promise 加 8。
    若 状态 等于 1：
        若 on_fulfilled 不等于 0：
            做 Nova运行时实例_排队微任务 用 Nova运行时实例、on_fulfilled。
        完。
    否则若 状态 等于 2：
        若 on_rejected 不等于 0：
            做 Nova运行时实例_排队微任务 用 Nova运行时实例、on_rejected。
        完。
    否则：
        回调数 为 做 内存_读32 用 promise 加 40。
        容量 为 做 内存_读32 用 promise 加 44。
        若 回调数 小于 容量：
            若 on_fulfilled 不等于 0：
                then列表 为 做 内存_读64 用 promise 加 24。
                做 内存_写64 用 then列表 加 回调数 乘 8、on_fulfilled。
            完。
            若 on_rejected 不等于 0：
                catch列表 为 做 内存_读64 用 promise 加 32。
                做 内存_写64 用 catch列表 加 回调数 乘 8、on_rejected。
            完。
            做 内存_写32 用 promise 加 40、回调数 加 1。
        完。
    完。
    返还 新promise。
完。

本章 收束。
