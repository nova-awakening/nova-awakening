《样式删除属性》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·推理·定点Q算术》。

引用《.·矩阵创建》。

引用《..·中枢·辅助存根》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《.·CSS值解析》。

NTAP_类型_平移 为 0x01。

NTAP_类型_平移X 为 0x02。

NTAP_类型_平移Y 为 0x03。

NTAP_类型_平移3D 为 0x04。

NTAP_类型_缩放 为 0x10。

NTAP_类型_缩放X 为 0x11。

NTAP_类型_缩放Y 为 0x12。

NTAP_类型_旋转 为 0x20。

NTAP_类型_旋转X 为 0x21。

NTAP_类型_旋转Y 为 0x22。

NTAP_类型_旋转Z 为 0x23。

NTAP_类型_斜切 为 0x30。

NTAP_类型_斜切X 为 0x31。

NTAP_类型_斜切Y 为 0x32。

NTAP_类型_透视 为 0x40。

NTAP_类型_矩阵 为 0x50。

NTAP_类型_矩阵3D 为 0x51。

NTAP_单位_DEG 为 1。

NTAP_单位_RAD 为 2。

NTAP_单位_GRAD 为 3。

NTAP_单位_TURN 为 4。

NTAP_缓动_线性 为 0。

NTAP_缓动_渐入 为 1。

NTAP_缓动_渐出 为 2。

NTAP_缓动_渐入渐出 为 3。

NTAP_缓动_弹跳 为 4。

NTAP_缓动_弹性 为 5。

NTAP_缓动_自定义 为 6。

NTAP_PI 为 314。

NTAP_PI_2 为 157。

NTAP_2PI 为 628。

NTAP_DEG2RAD 为 2。

NTAP_RAD2DEG 为 5729。

可变 NTAP_解析总数 为 0。

可变 NTAP_插值总数 为 0。

可变 NTAP_分解总数 为 0。

可变 NTAP_缓动计算数 为 0。

可变 NTAP_AI优化数 为 0。

可变 NTAP_解析失败数 为 0。

Transform_解析 接受 transform字符串：
    若 transform字符串 等于 0：
        返还 做 Matrix4x4_创建。
    完。
    长度 为 做 字符串_长度 用 transform字符串。
    若 长度 等于 0：
        返还 做 Matrix4x4_创建。
    完。
    若 做 字符串_相等 用 transform字符串、「none」：
        返还 做 Matrix4x4_创建。
    完。
    可变 结果 为 做 Matrix4x4_创建。
    可变 pos 为 0。
    只要 pos 小于 长度：
        只要 pos 小于 长度 并且 做 内存_读8 用 transform字符串 加 pos 等于 0x20：
            pos 为 pos 加 1。
        完。
        若 pos 大于或等于 长度：
            跳出。
        完。
        名起 为 pos。
        只要 pos 小于 长度 并且 做 内存_读8 用 transform字符串 加 pos 不等于 0x28 并且 做 内存_读8 用 transform字符串 加 pos 不等于 0x20：
            pos 为 pos 加 1。
        完。
        函数名 为 做 字符串_复制_N 用 transform字符串 加 名起、pos 减 名起。
        若 pos 小于 长度 并且 做 内存_读8 用 transform字符串 加 pos 等于 0x28：
            pos 为 pos 加 1。
        完。
        参数 为 做 堆_分配_数组 用 6 乘 4。
        可变 参数数 为 0。
        只要 pos 小于 长度 并且 做 内存_读8 用 transform字符串 加 pos 不等于 0x29：
            只要 pos 小于 长度 并且 (做 内存_读8 用 transform字符串 加 pos 等于 0x20 或者 做 内存_读8 用 transform字符串 加 pos 等于 0x2C)：
                pos 为 pos 加 1。
            完。
            val 为 做 CSS解析_数字或长度 用 transform字符串、长度、pos。
            若 val 不等于 0 并且 参数数 小于 6：
                做 内存_写32 用 参数 加 参数数 乘 4、val 的 整数值。
                参数数 为 参数数 加 1。
            完。
            只要 pos 小于 长度 并且 做 内存_读8 用 transform字符串 加 pos 不等于 0x2C 并且 做 内存_读8 用 transform字符串 加 pos 不等于 0x29：
                pos 为 pos 加 1。
            完。
        完。
        若 pos 小于 长度：
            pos 为 pos 加 1。
        完。
        可变 变换 为 0。
        p0 为 做 内存_读32 用 参数。
        p1 为 若 参数数 大于 1 则 做 内存_读32 用 参数 加 4 否则 p0。
        若 做 字符串_相等 用 函数名、「translate」：
            变换 为 做 Transform_translate 用 p0、p1。
        否则若 做 字符串_相等 用 函数名、「translateX」：
            变换 为 做 Transform_translate 用 p0、0。
        否则若 做 字符串_相等 用 函数名、「translateY」：
            变换 为 做 Transform_translate 用 0、p0。
        否则若 做 字符串_相等 用 函数名、「translate3d」：
            p2 为 若 参数数 大于 2 则 做 内存_读32 用 参数 加 8 否则 0。
            变换 为 做 Transform_translate3d 用 p0、p1、p2。
        否则若 做 字符串_相等 用 函数名、「scale」：
            变换 为 做 Transform_scale 用 p0、p1。
        否则若 做 字符串_相等 用 函数名、「scaleX」：
            变换 为 做 Transform_scale 用 p0、100。
        否则若 做 字符串_相等 用 函数名、「scaleY」：
            变换 为 做 Transform_scale 用 100、p0。
        否则若 做 字符串_相等 用 函数名、「rotate」：
            变换 为 做 Transform_rotate 用 p0。
        否则若 做 字符串_相等 用 函数名、「rotateX」：
            变换 为 做 Transform_rotateX 用 p0。
        否则若 做 字符串_相等 用 函数名、「rotateY」：
            变换 为 做 Transform_rotateY 用 p0。
        否则若 做 字符串_相等 用 函数名、「rotateZ」：
            变换 为 做 Transform_rotateZ 用 p0。
        否则若 做 字符串_相等 用 函数名、「skewX」：
            变换 为 做 Transform_skewX 用 p0。
        否则若 做 字符串_相等 用 函数名、「skewY」：
            变换 为 做 Transform_skewY 用 p0。
        否则若 做 字符串_相等 用 函数名、「skew」：
            变换 为 做 Matrix4x4_乘 用 做 Transform_skewX 用 p0、做 Transform_skewY 用 p1。
        否则若 做 字符串_相等 用 函数名、「perspective」：
            变换 为 做 Transform_perspective 用 p0。
        否则若 做 字符串_相等 用 函数名、「matrix」 并且 参数数 大于或等于 6：
            变换 为 做 Transform_matrix 用 做 内存_读32 用 参数、做 内存_读32 用 参数 加 4、做 内存_读32 用 参数 加 8、做 内存_读32 用 参数 加 12、做 内存_读32 用 参数 加 16、做 内存_读32 用 参数 加 20。
        完。
        若 变换 不等于 0：
            结果 为 做 Matrix4x4_乘 用 结果、变换。
        完。
        做 堆_释放 用 参数。
    完。
    返还 结果。
完。

Transform_应用Origin 接受 变换矩阵、origin_x、origin_y、元素宽、元素高：
    可变 ox 为 元素宽 除以 2。
    可变 oy 为 元素高 除以 2。
    若 origin_x 大于或等于 0：
        ox 为 origin_x。
    完。
    若 origin_y 大于或等于 0：
        oy 为 origin_y。
    完。
    前移 为 做 Transform_translate 用 ox、oy。
    后移 为 做 Transform_translate 用 0 减 ox、0 减 oy。
    中间 为 做 Matrix4x4_乘 用 前移、变换矩阵。
    返还 做 Matrix4x4_乘 用 中间、后移。
完。

NTAP_角度转换 接受 值、单位类型：
    若 单位类型 等于 NTAP_单位_DEG：
        返还 值。
    完。
    若 单位类型 等于 NTAP_单位_RAD：
        返还 值 乘 NTAP_RAD2DEG 除以 100。
    完。
    若 单位类型 等于 NTAP_单位_GRAD：
        返还 值 乘 90 除以 100。
    完。
    若 单位类型 等于 NTAP_单位_TURN：
        返还 值 乘 360。
    完。
    返还 值。
完。

NTAP_解析函数类型 接受 函数名：
    若 做 字符串_相等 用 函数名、「translate」：
        返还 NTAP_类型_平移。
    完。
    若 做 字符串_相等 用 函数名、「translateX」：
        返还 NTAP_类型_平移X。
    完。
    若 做 字符串_相等 用 函数名、「translateY」：
        返还 NTAP_类型_平移Y。
    完。
    若 做 字符串_相等 用 函数名、「translate3d」：
        返还 NTAP_类型_平移3D。
    完。
    若 做 字符串_相等 用 函数名、「scale」：
        返还 NTAP_类型_缩放。
    完。
    若 做 字符串_相等 用 函数名、「scaleX」：
        返还 NTAP_类型_缩放X。
    完。
    若 做 字符串_相等 用 函数名、「scaleY」：
        返还 NTAP_类型_缩放Y。
    完。
    若 做 字符串_相等 用 函数名、「rotate」：
        返还 NTAP_类型_旋转。
    完。
    若 做 字符串_相等 用 函数名、「rotateX」：
        返还 NTAP_类型_旋转X。
    完。
    若 做 字符串_相等 用 函数名、「rotateY」：
        返还 NTAP_类型_旋转Y。
    完。
    若 做 字符串_相等 用 函数名、「rotateZ」：
        返还 NTAP_类型_旋转Z。
    完。
    若 做 字符串_相等 用 函数名、「skew」：
        返还 NTAP_类型_斜切。
    完。
    若 做 字符串_相等 用 函数名、「skewX」：
        返还 NTAP_类型_斜切X。
    完。
    若 做 字符串_相等 用 函数名、「skewY」：
        返还 NTAP_类型_斜切Y。
    完。
    若 做 字符串_相等 用 函数名、「perspective」：
        返还 NTAP_类型_透视。
    完。
    若 做 字符串_相等 用 函数名、「matrix」：
        返还 NTAP_类型_矩阵。
    完。
    若 做 字符串_相等 用 函数名、「matrix3d」：
        返还 NTAP_类型_矩阵3D。
    完。
    返还 0。
完。

NTAP_矩阵分解 接受 矩阵指针：
    NTAP_分解总数 为 NTAP_分解总数 加 1。
    a 为 做 内存_读32 用 矩阵指针。
    b 为 做 内存_读32 用 矩阵指针 加 4。
    c 为 做 内存_读32 用 矩阵指针 加 16。
    d 为 做 内存_读32 用 矩阵指针 加 20。
    tx 为 做 内存_读32 用 矩阵指针 加 48。
    ty 为 做 内存_读32 用 矩阵指针 加 52。
    平移X 为 tx。
    平移Y 为 ty。
    sx_sq 为 a 乘 a 加 b 乘 b。
    sx 为 做 NTAP_定点平方根 用 sx_sq。
    det 为 a 乘 d 减 b 乘 c。
    可变 sy 为 0。
    若 sx 不等于 0：
        sy 为 det 除以 sx。
    完。
    旋转角 为 做 NTAP_定点atan2 用 b、a。
    可变 斜切X 为 0。
    若 sx_sq 不等于 0：
        斜切X 为 做 NTAP_定点atan2 用 a 乘 c 加 b 乘 d、sx_sq。
    完。
    返还 一本字典：平移X 对应 平移X、平移Y 对应 平移Y、缩放X 对应 sx、缩放Y 对应 sy、旋转 对应 旋转角、斜切X 对应 斜切X、斜切Y 对应 0。
完。

NTAP_定点平方根 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 g 为 x。
    若 g 大于 100：
        g 为 g 除以 2。
    完。
    g 为 (g 加 x 乘 100 除以 g) 除以 2。
    g 为 (g 加 x 乘 100 除以 g) 除以 2。
    g 为 (g 加 x 乘 100 除以 g) 除以 2。
    g 为 (g 加 x 乘 100 除以 g) 除以 2。
    返还 g。
完。

NTAP_定点atan2 接受 y、x：
    若 x 等于 0 并且 y 等于 0：
        返还 0。
    完。
    若 x 等于 0：
        若 y 大于 0：
            返还 9000。
        完。
        返还 负 9000。
    完。
    可变 ax 为 x。
    可变 ay 为 y。
    若 ax 小于 0：
        ax 为 0 减 ax。
    完。
    若 ay 小于 0：
        ay 为 0 减 ay。
    完。
    可变 角度 为 0。
    若 ay 小于或等于 ax：
        r 为 ay 乘 100 除以 ax。
        角度 为 r 乘 45 除以 100 减 r 乘 (r 减 100) 乘 14 除以 10000。
    否则：
        r 为 ax 乘 100 除以 ay。
        角度 为 9000 减 (r 乘 45 除以 100 减 r 乘 (r 减 100) 乘 14 除以 10000)。
    完。
    若 x 小于 0：
        角度 为 18000 减 角度。
    完。
    若 y 小于 0：
        角度 为 0 减 角度。
    完。
    返还 角度。
完。

NTAP_定点sin 接受 角度：
    可变 a 为 角度 取余于 36000。
    若 a 小于 0：
        a 为 a 加 36000。
    完。
    可变 符号 为 1。
    若 a 大于或等于 18000：
        a 为 a 减 18000。
        符号 为 负 1。
    完。
    若 a 大于 9000：
        a 为 18000 减 a。
    完。
    rad 为 a 乘 NTAP_PI 除以 18000。
    r3 为 rad 乘 rad 乘 rad 除以 10000 除以 6。
    返还 符号 乘 (rad 减 r3) 除以 100。
完。

NTAP_定点cos 接受 角度：
    返还 做 NTAP_定点sin 用 角度 加 9000。
完。

NTAP_缓动_线性 接受 t：
    返还 t。
完。

NTAP_缓动_渐入 接受 t：
    返还 t 乘 t 除以 100。
完。

NTAP_缓动_渐出 接受 t：
    inv 为 100 减 t。
    返还 100 减 inv 乘 inv 除以 100。
完。

NTAP_缓动_渐入渐出 接受 t：
    若 t 小于 50：
        返还 2 乘 t 乘 t 除以 100。
    否则：
        inv 为 100 减 t。
        返还 100 减 2 乘 inv 乘 inv 除以 100。
    完。
完。

NTAP_缓动_弹跳 接受 t：
    若 t 小于 36：
        返还 756 乘 t 乘 t 除以 10000。
    否则若 t 小于 73：
        t2 为 t 减 55。
        返还 756 乘 t2 乘 t2 除以 10000 加 75。
    否则若 t 小于 91：
        t3 为 t 减 82。
        返还 756 乘 t3 乘 t3 除以 10000 加 94。
    否则：
        t4 为 t 减 95。
        返还 756 乘 t4 乘 t4 除以 10000 加 98。
    完。
完。

NTAP_缓动_弹性 接受 t：
    衰减 为 100 减 6 乘 t 除以 100 加 18 乘 t 乘 t 除以 1000000。
    若 衰减 小于 0：
        衰减 为 0。
    完。
    振荡角 为 7 乘 t 乘 360 除以 100。
    余弦 为 做 NTAP_定点cos 用 振荡角。
    返还 100 减 衰减 乘 余弦 除以 100。
完。

NTAP_缓动_贝塞尔 接受 t、x1、y1、x2、y2：
    NTAP_缓动计算数 为 NTAP_缓动计算数 加 1。
    可变 s 为 t。
    可变 迭 为 0。
    只要 迭 小于 8：
        s1 为 100 减 s。
        bx 为 3 乘 s1 乘 s1 乘 s 乘 x1 除以 1000000 加 3 乘 s1 乘 s 乘 s 乘 x2 除以 1000000 加 s 乘 s 乘 s 除以 10000。
        差 为 bx 减 t。
        若 差 大于 负 1 并且 差 小于 1：
            跳出。
        完。
        dx 为 3 乘 s1 乘 s1 乘 x1 除以 10000 加 6 乘 s1 乘 s 乘 (x2 减 x1) 除以 10000 加 3 乘 s 乘 s 乘 (100 减 x2) 除以 10000。
        若 dx 等于 0：
            跳出。
        完。
        s 为 s 减 差 乘 100 除以 dx。
        若 s 小于 0：
            s 为 0。
        完。
        若 s 大于 100：
            s 为 100。
        完。
        迭 为 迭 加 1。
    完。
    s1 为 100 减 s。
    返还 3 乘 s1 乘 s1 乘 s 乘 y1 除以 1000000 加 3 乘 s1 乘 s 乘 s 乘 y2 除以 1000000 加 s 乘 s 乘 s 除以 10000。
完。

NTAP_计算缓动 接受 类型、t、参数：
    若 类型 等于 NTAP_缓动_线性：
        返还 做 NTAP_缓动_线性 用 t。
    完。
    若 类型 等于 NTAP_缓动_渐入：
        返还 做 NTAP_缓动_渐入 用 t。
    完。
    若 类型 等于 NTAP_缓动_渐出：
        返还 做 NTAP_缓动_渐出 用 t。
    完。
    若 类型 等于 NTAP_缓动_渐入渐出：
        返还 做 NTAP_缓动_渐入渐出 用 t。
    完。
    若 类型 等于 NTAP_缓动_弹跳：
        返还 做 NTAP_缓动_弹跳 用 t。
    完。
    若 类型 等于 NTAP_缓动_弹性：
        返还 做 NTAP_缓动_弹性 用 t。
    完。
    若 类型 等于 NTAP_缓动_自定义 并且 参数 不等于 0：
        返还 做 NTAP_缓动_贝塞尔 用 t、参数 的 控制点x1、参数 的 控制点y1、参数 的 控制点x2、参数 的 控制点y2。
    完。
    返还 t。
完。

NTAP_插值_标量 接受 起、止、t：
    返还 起 加 (止 减 起) 乘 t 除以 100。
完。

NTAP_插值_角度 接受 起、止、t：
    可变 差 为 止 减 起。
    若 差 大于 18000：
        差 为 差 减 36000。
    完。
    若 差 小于 负 18000：
        差 为 差 加 36000。
    完。
    结果 为 起 加 差 乘 t 除以 100。
    若 结果 小于 0：
        结果 为 结果 加 36000。
    完。
    若 结果 大于或等于 36000：
        结果 为 结果 减 36000。
    完。
    返还 结果。
完。

NTAP_插值_TRS 接受 起分解、止分解、t：
    NTAP_插值总数 为 NTAP_插值总数 加 1。
    返还 一本字典：平移X 对应 做 NTAP_插值_标量 用 起分解 的 平移X、止分解 的 平移X、t、平移Y 对应 做 NTAP_插值_标量 用 起分解 的 平移Y、止分解 的 平移Y、t、缩放X 对应 做 NTAP_插值_标量 用 起分解 的 缩放X、止分解 的 缩放X、t、缩放Y 对应 做 NTAP_插值_标量 用 起分解 的 缩放Y、止分解 的 缩放Y、t、旋转 对应 做 NTAP_插值_角度 用 起分解 的 旋转、止分解 的 旋转、t、斜切X 对应 做 NTAP_插值_标量 用 起分解 的 斜切X、止分解 的 斜切X、t、斜切Y 对应 做 NTAP_插值_标量 用 起分解 的 斜切Y、止分解 的 斜切Y、t。
完。

NTAP_TRS重组矩阵 接受 分解：
    T 为 做 Transform_translate 用 分解 的 平移X、分解 的 平移Y。
    R 为 做 Transform_rotate 用 分解 的 旋转。
    Sk 为 做 Transform_skewX 用 分解 的 斜切X。
    S 为 做 Transform_scale 用 分解 的 缩放X、分解 的 缩放Y。
    中1 为 做 Matrix4x4_乘 用 T、R。
    中2 为 做 Matrix4x4_乘 用 中1、Sk。
    返还 做 Matrix4x4_乘 用 中2、S。
完。

NTAP_最大关键帧 为 64。

NTAP_关键帧 是 一种 事物，含 百分比 为 0、变换字符串 为 0、缓动类型 为 0、贝塞尔参数 为 0、分解缓存 为 0。

NTAP_关键帧序列 是 一种 事物，含 帧列表 为 一组：（空）、帧数量 为 0、总时长毫秒 为 0、循环次数 为 1、当前循环 为 0、是否反向 为 假。

NTAP_添加关键帧 接受 序列、百分比、变换字符串、缓动类型：
    若 序列 的 帧数量 大于或等于 NTAP_最大关键帧：
        返还 假。
    完。
    新帧 为 新建 NTAP_关键帧：百分比 为 百分比、变换字符串 为 变换字符串、缓动类型 为 缓动类型、贝塞尔参数 为 0、分解缓存 为 0。
    可变 插入位 为 序列 的 帧数量。
    可变 i 为 0。
    只要 i 小于 序列 的 帧数量：
        若 序列 的 帧列表 的 第 i 项 的 百分比 大于 百分比：
            插入位 为 i。
            跳出。
        完。
        i 为 i 加 1。
    完。
    可变 j 为 序列 的 帧数量。
    只要 j 大于 插入位：
        序列 的 帧列表 的 第 j 项 为 序列 的 帧列表 的 第 j 减 1 项。
        j 为 j 减 1。
    完。
    序列 的 帧列表 的 第 插入位 项 为 新帧。
    序列 的 帧数量 为 序列 的 帧数量 加 1。
    返还 真。
完。

NTAP_采样关键帧 接受 序列、进度：
    若 序列 的 帧数量 等于 0：
        返还 做 Matrix4x4_创建。
    完。
    若 序列 的 帧数量 等于 1：
        返还 做 Transform_解析 用 序列 的 帧列表 的 第 0 项 的 变换字符串。
    完。
    可变 p 为 进度。
    若 序列 的 是否反向：
        p 为 100 减 p。
    完。
    可变 左 为 0。
    可变 右 为 序列 的 帧数量 减 1。
    可变 i 为 0。
    只要 i 小于 序列 的 帧数量 减 1：
        若 序列 的 帧列表 的 第 i 项 的 百分比 小于或等于 p 并且 序列 的 帧列表 的 第 i 加 1 项 的 百分比 大于或等于 p：
            左 为 i。
            右 为 i 加 1。
            跳出。
        完。
        i 为 i 加 1。
    完。
    左百 为 序列 的 帧列表 的 第 左 项 的 百分比。
    右百 为 序列 的 帧列表 的 第 右 项 的 百分比。
    可变 局部t 为 0。
    若 右百 大于 左百：
        局部t 为 (p 减 左百) 乘 100 除以 (右百 减 左百)。
    完。
    局部t 为 做 NTAP_计算缓动 用 序列 的 帧列表 的 第 左 项 的 缓动类型、局部t、序列 的 帧列表 的 第 左 项 的 贝塞尔参数。
    左矩 为 做 Transform_解析 用 序列 的 帧列表 的 第 左 项 的 变换字符串。
    右矩 为 做 Transform_解析 用 序列 的 帧列表 的 第 右 项 的 变换字符串。
    左分 为 做 NTAP_矩阵分解 用 左矩。
    右分 为 做 NTAP_矩阵分解 用 右矩。
    中间分 为 做 NTAP_插值_TRS 用 左分、右分、局部t。
    返还 做 NTAP_TRS重组矩阵 用 中间分。
完。

NTAP_AI_动画分析 接受 序列：
    NTAP_AI优化数 为 NTAP_AI优化数 加 1。
    特征大小 为 6。
    tensor 为 做 Tensor_创建 用 特征大小。
    做 Tensor_写 用 tensor、0、序列 的 帧数量。
    做 Tensor_写 用 tensor、1、序列 的 总时长毫秒。
    可变 最大缩放差 为 0。
    可变 最大旋转差 为 0。
    可变 最大平移差 为 0。
    可变 缓动复杂度 为 0。
    可变 i 为 0。
    只要 i 小于 序列 的 帧数量 减 1：
        帧A 为 序列 的 帧列表 的 第 i 项。
        帧B 为 序列 的 帧列表 的 第 i 加 1 项。
        矩A 为 做 Transform_解析 用 帧A 的 变换字符串。
        矩B 为 做 Transform_解析 用 帧B 的 变换字符串。
        分A 为 做 NTAP_矩阵分解 用 矩A。
        分B 为 做 NTAP_矩阵分解 用 矩B。
        sd 为 做 NTAP_绝对值 用 分B 的 缩放X 减 分A 的 缩放X 加 做 NTAP_绝对值 用 分B 的 缩放Y 减 分A 的 缩放Y。
        若 sd 大于 最大缩放差：
            最大缩放差 为 sd。
        完。
        rd 为 做 NTAP_绝对值 用 分B 的 旋转 减 分A 的 旋转。
        若 rd 大于 18000：
            rd 为 36000 减 rd。
        完。
        若 rd 大于 最大旋转差：
            最大旋转差 为 rd。
        完。
        td 为 做 NTAP_绝对值 用 分B 的 平移X 减 分A 的 平移X 加 做 NTAP_绝对值 用 分B 的 平移Y 减 分A 的 平移Y。
        若 td 大于 最大平移差：
            最大平移差 为 td。
        完。
        若 帧A 的 缓动类型 大于 NTAP_缓动_渐入渐出：
            缓动复杂度 为 缓动复杂度 加 2。
        否则若 帧A 的 缓动类型 大于 NTAP_缓动_线性：
            缓动复杂度 为 缓动复杂度 加 1。
        完。
        i 为 i 加 1。
    完。
    做 Tensor_写 用 tensor、2、最大缩放差。
    做 Tensor_写 用 tensor、3、最大旋转差。
    做 Tensor_写 用 tensor、4、最大平移差。
    做 Tensor_写 用 tensor、5、缓动复杂度。
    结果 为 做 AI总线_推理 用 全局AI总线、「NTAP_动画分析」、tensor、特征大小。
    可变 卡顿风险 为 0。
    可变 建议帧率 为 60。
    若 结果 不等于 0：
        卡顿风险 为 做 Tensor_读 用 结果、0。
        建议帧率 为 做 Tensor_读 用 结果、1。
        做 Tensor_释放 用 结果。
    完。
    做 Tensor_释放 用 tensor。
    返还 一本字典：卡顿风险 对应 卡顿风险、建议帧率 对应 建议帧率、最大缩放差 对应 最大缩放差、最大旋转差 对应 最大旋转差、最大平移差 对应 最大平移差。
完。

NTAP_AI_建议加密点 接受 序列：
    可变 建议点 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列 的 帧数量 减 1：
        帧A 为 序列 的 帧列表 的 第 i 项。
        帧B 为 序列 的 帧列表 的 第 i 加 1 项。
        间距 为 帧B 的 百分比 减 帧A 的 百分比。
        若 间距 大于 2000：
            矩A 为 做 Transform_解析 用 帧A 的 变换字符串。
            矩B 为 做 Transform_解析 用 帧B 的 变换字符串。
            分A 为 做 NTAP_矩阵分解 用 矩A。
            分B 为 做 NTAP_矩阵分解 用 矩B。
            总差 为 做 NTAP_绝对值 用 分B 的 缩放X 减 分A 的 缩放X 加 做 NTAP_绝对值 用 分B 的 旋转 减 分A 的 旋转 除以 100 加 做 NTAP_绝对值 用 分B 的 平移X 减 分A 的 平移X。
            若 总差 大于 5000：
                中点 为 (帧A 的 百分比 加 帧B 的 百分比) 除以 2。
                做 追加 用 建议点、中点。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 建议点。
完。

NTAP_绝对值 接受 x：
    若 x 小于 0：
        返还 0 减 x。
    完。
    返还 x。
完。

NTAP_限制范围 接受 值、最小、最大：
    若 值 小于 最小：
        返还 最小。
    完。
    若 值 大于 最大：
        返还 最大。
    完。
    返还 值。
完。

NTAP_类型名称 接受 类型码：
    若 类型码 等于 NTAP_类型_平移：
        返还 「translate」。
    完。
    若 类型码 等于 NTAP_类型_缩放：
        返还 「scale」。
    完。
    若 类型码 等于 NTAP_类型_旋转：
        返还 「rotate」。
    完。
    若 类型码 等于 NTAP_类型_斜切：
        返还 「skew」。
    完。
    若 类型码 等于 NTAP_类型_透视：
        返还 「perspective」。
    完。
    若 类型码 等于 NTAP_类型_矩阵：
        返还 「matrix」。
    完。
    返还 「未知」。
完。

NTAP_诊断打印 接受 零参：
    做 内核信息 用 「=== NTAP 样式变换属性解析 诊断报告 ===」。
    做 内核信息 用 「  解析总数:     」 加 NTAP_解析总数。
    做 内核信息 用 「  插值总数:     」 加 NTAP_插值总数。
    做 内核信息 用 「  分解总数:     」 加 NTAP_分解总数。
    做 内核信息 用 「  缓动计算数:   」 加 NTAP_缓动计算数。
    做 内核信息 用 「  AI优化数:     」 加 NTAP_AI优化数。
    做 内核信息 用 「  解析失败数:   」 加 NTAP_解析失败数。
    若 NTAP_解析总数 大于 0：
        成功率 为 (NTAP_解析总数 减 NTAP_解析失败数) 乘 100 除以 NTAP_解析总数。
        做 内核信息 用 「  解析成功率:   」 加 成功率 加 「%」。
    完。
    做 内核信息 用 「=========================」。
完。

NTAP_重置统计 接受 零参：
    NTAP_解析总数 为 0。
    NTAP_插值总数 为 0。
    NTAP_分解总数 为 0。
    NTAP_缓动计算数 为 0。
    NTAP_AI优化数 为 0。
    NTAP_解析失败数 为 0。
完。

本章 收束。
