《CSS动画》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·哈希整数》。

引用《.·内核直连层》。

引用《..·中枢·内容可见性》。

最大动画 为 128。

最大关键帧 为 32。

缓动_线性 为 0。

缓动_缓入 为 1。

缓动_缓出 为 2。

缓动_缓入出 为 3。

方向_正 为 0。

方向_反 为 1。

方向_交替 为 2。

填充_无 为 0。

填充_前 为 1。

填充_后 为 2。

填充_双 为 3。

状态_空闲 为 0。

状态_运行 为 1。

状态_暂停 为 2。

状态_完成 为 3。

基数 为 1024。

关键帧 是 一种 事物，含 百分比、属性标识、起始值、结束值、缓动。

动画实例 是 一种 事物，含 名称哈希、元素标识、状态、时长、延迟、重复次数、当前重复、方向、填充模式、关键帧表、关键帧数、起始时刻、当前进度、暂停偏移。

可变 动画表 为 一组：（空）。

可变 动画数 为 0。

可变 总启动 为 0。

可变 总完成 为 0。

可变 总帧更新 为 0。

动画_创建 接受 名称、元素、时长、延迟、重复、方向、填充：
    若 动画数 大于或等于 最大动画：
        返还 负 1。
    完。
    动画 为 新建 动画实例。
    动画 的 名称哈希 为 做 哈希_字符串 用 名称。
    动画 的 元素标识 为 元素。
    动画 的 状态 为 状态_空闲。
    动画 的 时长 为 时长。
    动画 的 延迟 为 延迟。
    动画 的 重复次数 为 重复。
    动画 的 当前重复 为 0。
    动画 的 方向 为 方向。
    动画 的 填充模式 为 填充。
    动画 的 关键帧表 为 做 堆_分配 用 最大关键帧 乘 24。
    动画 的 关键帧数 为 0。
    动画 的 起始时刻 为 0。
    动画 的 当前进度 为 0。
    动画 的 暂停偏移 为 0。
    做 追加 用 动画表、动画。
    索引 为 动画数。
    动画数 为 动画数 加 1。
    返还 索引。
完。

动画_添加帧 接受 动画索引、百分比、属性、起始、结束、缓动：
    若 动画索引 大于或等于 动画数：
        返还。
    完。
    动画 为 动画表 的 第 动画索引 项。
    若 动画 的 关键帧数 大于或等于 最大关键帧：
        返还。
    完。
    偏移 为 动画 的 关键帧表 加 动画 的 关键帧数 乘 24。
    做 内存_写32 用 偏移、百分比。
    做 内存_写32 用 偏移 加 4、属性。
    做 内存_写64 用 偏移 加 8、起始。
    做 内存_写64 用 偏移 加 16、结束。
    动画 的 关键帧数 为 动画 的 关键帧数 加 1。
完。

动画_启动 接受 动画索引：
    若 动画索引 大于或等于 动画数：
        返还。
    完。
    动画表 的 第 动画索引 项 的 状态 为 状态_运行。
    动画表 的 第 动画索引 项 的 起始时刻 为 做 平台_获取高精度时间 加 动画表 的 第 动画索引 项 的 延迟。
    动画表 的 第 动画索引 项 的 当前重复 为 0。
    动画表 的 第 动画索引 项 的 当前进度 为 0。
    总启动 为 总启动 加 1。
完。

动画_暂停 接受 动画索引：
    若 动画索引 大于或等于 动画数：
        返还。
    完。
    若 动画表 的 第 动画索引 项 的 状态 等于 状态_运行：
        动画表 的 第 动画索引 项 的 状态 为 状态_暂停。
        动画表 的 第 动画索引 项 的 暂停偏移 为 做 平台_获取高精度时间 减 动画表 的 第 动画索引 项 的 起始时刻。
    完。
完。

动画_恢复 接受 动画索引：
    若 动画索引 大于或等于 动画数：
        返还。
    完。
    若 动画表 的 第 动画索引 项 的 状态 等于 状态_暂停：
        动画表 的 第 动画索引 项 的 状态 为 状态_运行。
        动画表 的 第 动画索引 项 的 起始时刻 为 做 平台_获取高精度时间 减 动画表 的 第 动画索引 项 的 暂停偏移。
    完。
完。

动画_更新帧 接受 零参：
    总帧更新 为 总帧更新 加 1。
    现在 为 做 平台_获取高精度时间。
    可变 序号 为 0。
    只要 序号 小于 动画数：
        动画 为 动画表 的 第 序号 项。
        若 动画 的 状态 不等于 状态_运行：
            序号 为 序号 加 1。
            继续。
        完。
        若 现在 小于 动画 的 起始时刻：
            序号 为 序号 加 1。
            继续。
        完。
        已过 为 现在 减 动画 的 起始时刻。
        周期进度 为 已过 取余于 动画 的 时长。
        归一 为 周期进度 乘 基数 除以 动画 的 时长。
        若 动画 的 方向 等于 方向_反：
            归一 为 基数 减 归一。
        完。
        若 动画 的 方向 等于 方向_交替 并且 动画 的 当前重复 取余于 2 等于 1：
            归一 为 基数 减 归一。
        完。
        动画 的 当前进度 为 归一。
        当前重复 为 已过 除以 动画 的 时长。
        若 动画 的 重复次数 大于 0 并且 当前重复 大于或等于 动画 的 重复次数：
            动画 的 状态 为 状态_完成。
            总完成 为 总完成 加 1。
        完。
        动画 的 当前重复 为 当前重复。
        序号 为 序号 加 1。
    完。
完。

动画_插值 接受 动画索引、帧索引：
    若 动画索引 大于或等于 动画数：
        返还 0。
    完。
    动画 为 动画表 的 第 动画索引 项。
    若 帧索引 大于或等于 动画 的 关键帧数：
        返还 0。
    完。
    偏移 为 动画 的 关键帧表 加 帧索引 乘 24。
    起始 为 做 内存_读64 用 偏移 加 8。
    结束 为 做 内存_读64 用 偏移 加 16。
    进度 为 动画 的 当前进度。
    返还 起始 加 (结束 减 起始) 乘 进度 除以 基数。
完。

缓动_计算 接受 类型、进度：
    若 类型 等于 缓动_线性：
        返还 进度。
    完。
    若 类型 等于 缓动_缓入：
        返还 进度 乘 进度 除以 基数。
    完。
    若 类型 等于 缓动_缓出：
        返还 基数 减 (基数 减 进度) 乘 (基数 减 进度) 除以 基数。
    完。
    若 类型 等于 缓动_缓入出：
        若 进度 小于 基数 除以 2：
            返还 2 乘 进度 乘 进度 除以 基数。
        完。
        返还 基数 减 2 乘 (基数 减 进度) 乘 (基数 减 进度) 除以 基数。
    完。
    返还 进度。
完。

动画_活跃数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 动画数：
        若 动画表 的 第 序号 项 的 状态 等于 状态_运行：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

动画_完整统计 接受 零参：
    返还 一组：动画数、做 动画_活跃数、总启动、总完成、总帧更新。
完。

本章 收束。
