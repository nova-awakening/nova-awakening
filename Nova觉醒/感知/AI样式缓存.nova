《AI样式缓存》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《..·中枢·内存初始化》。

引用《..·中枢·内容可见性》。

样式缓存_默认容量 为 512。

样式缓存_最大容量 为 4096。

样式缓存_淘汰比例 为 25。

样式缓存_条目大小 为 28。

可变 AI样式缓存表 为 0。

可变 AI样式缓存数量 为 0。

可变 AI样式缓存容量 为 0。

可变 AI样式缓存_命中数 为 0。

可变 AI样式缓存_未命中数 为 0。

可变 AI样式缓存_淘汰数 为 0。

可变 AI样式缓存_无效化数 为 0。

样式缓存Entry 是 一种 事物，含 规则哈希、属性ID、解析值、命中次数、上次使用。

AI样式_初始化缓存 接受 零参：
    做 AI样式_初始化缓存_容量 用 样式缓存_默认容量。
完。

AI样式_初始化缓存_容量 接受 容量：
    若 容量 大于 样式缓存_最大容量：
        容量 为 样式缓存_最大容量。
    完。
    AI样式缓存容量 为 容量。
    AI样式缓存表 为 做 堆_分配_数组 用 AI样式缓存容量 乘 样式缓存_条目大小。
    AI样式缓存数量 为 0。
    AI样式缓存_命中数 为 0。
    AI样式缓存_未命中数 为 0。
    AI样式缓存_淘汰数 为 0。
    AI样式缓存_无效化数 为 0。
完。

AI样式_查缓存 接受 规则哈希、属性ID：
    可变 i 为 0。
    只要 i 小于 AI样式缓存数量：
        偏移 为 AI样式缓存表 加 i 乘 样式缓存_条目大小。
        若 做 内存_读32 用 偏移 等于 规则哈希 并且 做 内存_读32 用 偏移 加 4 等于 属性ID：
            旧命中 为 做 内存_读32 用 偏移 加 16。
            做 内存_写32 用 偏移 加 16、旧命中 加 1。
            做 内存_写64 用 偏移 加 20、做 平台_获取高精度时间。
            AI样式缓存_命中数 为 AI样式缓存_命中数 加 1。
            返还 做 内存_读64 用 偏移 加 8。
        完。
        i 为 i 加 1。
    完。
    AI样式缓存_未命中数 为 AI样式缓存_未命中数 加 1。
    返还 0。
完。

AI样式_写缓存 接受 规则哈希、属性ID、解析值：
    已存在 为 做 AI样式_查缓存位置 用 规则哈希、属性ID。
    若 已存在 大于或等于 0：
        偏移 为 AI样式缓存表 加 已存在 乘 样式缓存_条目大小。
        做 内存_写64 用 偏移 加 8、解析值。
        做 内存_写64 用 偏移 加 20、做 平台_获取高精度时间。
        返还。
    完。
    若 AI样式缓存数量 大于或等于 AI样式缓存容量：
        做 AI样式_LRU淘汰。
    完。
    若 AI样式缓存数量 小于 AI样式缓存容量：
        偏移 为 AI样式缓存表 加 AI样式缓存数量 乘 样式缓存_条目大小。
        做 内存_写32 用 偏移、规则哈希。
        做 内存_写32 用 偏移 加 4、属性ID。
        做 内存_写64 用 偏移 加 8、解析值。
        做 内存_写32 用 偏移 加 16、0。
        做 内存_写64 用 偏移 加 20、做 平台_获取高精度时间。
        AI样式缓存数量 为 AI样式缓存数量 加 1。
    完。
完。

AI样式_查缓存位置 接受 规则哈希、属性ID：
    可变 i 为 0。
    只要 i 小于 AI样式缓存数量：
        偏移 为 AI样式缓存表 加 i 乘 样式缓存_条目大小。
        若 做 内存_读32 用 偏移 等于 规则哈希 并且 做 内存_读32 用 偏移 加 4 等于 属性ID：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

AI样式_LRU淘汰 接受 零参：
    淘汰数 为 AI样式缓存容量 乘 样式缓存_淘汰比例 除以 100。
    若 淘汰数 小于 1：
        淘汰数 为 1。
    完。
    可变 已淘汰 为 0。
    只要 已淘汰 小于 淘汰数 并且 AI样式缓存数量 大于 0：
        可变 最旧索引 为 0。
        可变 最旧时间 为 做 内存_读64 用 AI样式缓存表 加 20。
        可变 i 为 1。
        只要 i 小于 AI样式缓存数量：
            时间 为 做 内存_读64 用 AI样式缓存表 加 i 乘 样式缓存_条目大小 加 20。
            若 时间 小于 最旧时间：
                最旧时间 为 时间。
                最旧索引 为 i。
            完。
            i 为 i 加 1。
        完。
        做 AI样式_移除条目 用 最旧索引。
        已淘汰 为 已淘汰 加 1。
        AI样式缓存_淘汰数 为 AI样式缓存_淘汰数 加 1。
    完。
完。

AI样式_移除条目 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 AI样式缓存数量：
        返还。
    完。
    最后 为 AI样式缓存数量 减 1。
    若 索引 不等于 最后：
        目标偏移 为 AI样式缓存表 加 索引 乘 样式缓存_条目大小。
        源偏移 为 AI样式缓存表 加 最后 乘 样式缓存_条目大小。
        做 内存_拷贝 用 目标偏移、源偏移、样式缓存_条目大小。
    完。
    AI样式缓存数量 为 AI样式缓存数量 减 1。
完。

AI样式_无效化_规则 接受 规则哈希：
    可变 i 为 0。
    只要 i 小于 AI样式缓存数量：
        偏移 为 AI样式缓存表 加 i 乘 样式缓存_条目大小。
        若 做 内存_读32 用 偏移 等于 规则哈希：
            做 AI样式_移除条目 用 i。
            AI样式缓存_无效化数 为 AI样式缓存_无效化数 加 1。
            继续。
        完。
        i 为 i 加 1。
    完。
完。

AI样式_无效化_属性 接受 属性ID：
    可变 i 为 0。
    只要 i 小于 AI样式缓存数量：
        偏移 为 AI样式缓存表 加 i 乘 样式缓存_条目大小。
        若 做 内存_读32 用 偏移 加 4 等于 属性ID：
            做 AI样式_移除条目 用 i。
            AI样式缓存_无效化数 为 AI样式缓存_无效化数 加 1。
            继续。
        完。
        i 为 i 加 1。
    完。
完。

AI样式_清空缓存 接受 零参：
    AI样式缓存数量 为 0。
    AI样式缓存_命中数 为 0。
    AI样式缓存_未命中数 为 0。
完。

AI样式_缓存命中率 接受 零参：
    总数 为 AI样式缓存_命中数 加 AI样式缓存_未命中数。
    若 总数 等于 0：
        返还 0。
    完。
    返还 AI样式缓存_命中数 乘 100 除以 总数。
完。

AI样式_批量写缓存 接受 条目列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目列表：
        条目 为 条目列表 的 第 i 项。
        做 AI样式_写缓存 用 条目 的 第 0 项、条目 的 第 1 项、条目 的 第 2 项。
        i 为 i 加 1。
    完。
完。

AI样式_缓存统计 接受 零参：
    返还 一组：AI样式缓存数量、AI样式缓存容量、AI样式缓存_命中数、AI样式缓存_未命中数、AI样式缓存_淘汰数、AI样式缓存_无效化数、做 AI样式_缓存命中率。
完。

AI样式_缓存预热 接受 常用规则列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 常用规则列表：
        规则 为 常用规则列表 的 第 i 项。
        做 AI样式_写缓存 用 规则 的 第 0 项、规则 的 第 1 项、规则 的 第 2 项。
        i 为 i 加 1。
    完。
完。

本章 收束。
