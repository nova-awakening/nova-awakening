《SkPath路径创建》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《..·中枢·内存初始化》。

引用《..·稳态·数学基础向量》。

路径动词_移动 为 0。

路径动词_直线 为 1。

路径动词_二次 为 2。

路径动词_三次 为 3。

路径动词_圆锥 为 4。

路径动词_关闭 为 5。

路径填充_缠绕 为 0。

路径填充_奇偶 为 1。

SkPath 是 一种 事物，含 动词列表、动词数量、动词容量、点列表、点数量、点容量、填充规则。

SkPath_创建 接受 零参：
    path 为 做 堆_分配 用 SkPath。
    path 的 动词容量 为 64。
    path 的 动词列表 为 做 堆_分配_数组 用 path 的 动词容量。
    path 的 动词数量 为 0。
    path 的 点容量 为 128。
    path 的 点列表 为 做 堆_分配_数组 用 path 的 点容量 乘 8。
    path 的 点数量 为 0。
    path 的 填充规则 为 路径填充_缠绕。
    返还 path。
完。

SkPath_moveTo 接受 path、x、y：
    做 SkPath_添加动词 用 path、路径动词_移动。
    做 SkPath_添加点 用 path、x、y。
完。

SkPath_lineTo 接受 path、x、y：
    做 SkPath_添加动词 用 path、路径动词_直线。
    做 SkPath_添加点 用 path、x、y。
完。

SkPath_quadTo 接受 path、cx、cy、x、y：
    做 SkPath_添加动词 用 path、路径动词_二次。
    做 SkPath_添加点 用 path、cx、cy。
    做 SkPath_添加点 用 path、x、y。
完。

SkPath_Nova三次方To 接受 path、c1x、c1y、c2x、c2y、x、y：
    做 SkPath_添加动词 用 path、路径动词_三次。
    做 SkPath_添加点 用 path、c1x、c1y。
    做 SkPath_添加点 用 path、c2x、c2y。
    做 SkPath_添加点 用 path、x、y。
完。

SkPath_close 接受 path：
    做 SkPath_添加动词 用 path、路径动词_关闭。
完。

SkPath_addRect 接受 path、rect：
    做 SkPath_moveTo 用 path、rect 的 左、rect 的 上。
    做 SkPath_lineTo 用 path、rect 的 右、rect 的 上。
    做 SkPath_lineTo 用 path、rect 的 右、rect 的 下。
    做 SkPath_lineTo 用 path、rect 的 左、rect 的 下。
    做 SkPath_close 用 path。
完。

SkPath_addRoundRect 接受 path、rect、rx、ry：
    左 为 rect 的 左。
    上 为 rect 的 上。
    右 为 rect 的 右。
    下 为 rect 的 下。
    做 SkPath_moveTo 用 path、左 加 rx、上。
    做 SkPath_lineTo 用 path、右 减 rx、上。
    做 SkPath_quadTo 用 path、右、上、右、上 加 ry。
    做 SkPath_lineTo 用 path、右、下 减 ry。
    做 SkPath_quadTo 用 path、右、下、右 减 rx、下。
    做 SkPath_lineTo 用 path、左 加 rx、下。
    做 SkPath_quadTo 用 path、左、下、左、下 减 ry。
    做 SkPath_lineTo 用 path、左、上 加 ry。
    做 SkPath_quadTo 用 path、左、上、左 加 rx、上。
    做 SkPath_close 用 path。
完。

SkPath_添加动词 接受 path、动词：
    若 path 的 动词数量 大于或等于 path 的 动词容量：
        新容量 为 path 的 动词容量 乘 2。
        新列表 为 做 堆_分配_数组 用 新容量。
        做 内存_复制 用 新列表、path 的 动词列表、path 的 动词数量。
        做 堆_释放 用 path 的 动词列表。
        path 的 动词列表 为 新列表。
        path 的 动词容量 为 新容量。
    完。
    做 内存_写8 用 path 的 动词列表 加 path 的 动词数量、动词。
    path 的 动词数量 为 path 的 动词数量 加 1。
完。

SkPath_添加点 接受 path、x、y：
    若 path 的 点数量 大于或等于 path 的 点容量：
        新容量 为 path 的 点容量 乘 2。
        新列表 为 做 堆_分配_数组 用 新容量 乘 8。
        做 内存_复制 用 新列表、path 的 点列表、path 的 点数量 乘 8。
        做 堆_释放 用 path 的 点列表。
        path 的 点列表 为 新列表。
        path 的 点容量 为 新容量。
    完。
    做 内存_写32 用 path 的 点列表 加 path 的 点数量 乘 8、x。
    做 内存_写32 用 path 的 点列表 加 path 的 点数量 乘 8 加 4、y。
    path 的 点数量 为 path 的 点数量 加 1。
完。

SkPath_addCircle 接受 path、cx、cy、半径：
    k 为 半径 乘 5523 除以 10000。
    做 SkPath_moveTo 用 path、cx 加 半径、cy。
    做 SkPath_Nova三次方To 用 path、cx 加 半径、cy 加 k、cx 加 k、cy 加 半径、cx、cy 加 半径。
    做 SkPath_Nova三次方To 用 path、cx 减 k、cy 加 半径、cx 减 半径、cy 加 k、cx 减 半径、cy。
    做 SkPath_Nova三次方To 用 path、cx 减 半径、cy 减 k、cx 减 k、cy 减 半径、cx、cy 减 半径。
    做 SkPath_Nova三次方To 用 path、cx 加 k、cy 减 半径、cx 加 半径、cy 减 k、cx 加 半径、cy。
    做 SkPath_close 用 path。
完。

SkPath_addOval 接受 path、rect：
    cx 为 (rect 的 左 加 rect 的 右) 除以 2。
    cy 为 (rect 的 上 加 rect 的 下) 除以 2。
    rx 为 (rect 的 右 减 rect 的 左) 除以 2。
    ry 为 (rect 的 下 减 rect 的 上) 除以 2。
    kx 为 rx 乘 5523 除以 10000。
    ky 为 ry 乘 5523 除以 10000。
    做 SkPath_moveTo 用 path、cx 加 rx、cy。
    做 SkPath_Nova三次方To 用 path、cx 加 rx、cy 加 ky、cx 加 kx、cy 加 ry、cx、cy 加 ry。
    做 SkPath_Nova三次方To 用 path、cx 减 kx、cy 加 ry、cx 减 rx、cy 加 ky、cx 减 rx、cy。
    做 SkPath_Nova三次方To 用 path、cx 减 rx、cy 减 ky、cx 减 kx、cy 减 ry、cx、cy 减 ry。
    做 SkPath_Nova三次方To 用 path、cx 加 kx、cy 减 ry、cx 加 rx、cy 减 ky、cx 加 rx、cy。
    做 SkPath_close 用 path。
完。

SkPath_addArc 接受 path、cx、cy、半径、起始角、扫描角：
    可变 剩余 为 扫描角。
    可变 当前角 为 起始角。
    只要 剩余 大于 0：
        段角 为 剩余。
        若 段角 大于 1571：
            段角 为 1571。
        完。
        cos1 为 做 定点_cos 用 当前角。
        sin1 为 做 定点_sin 用 当前角。
        cos2 为 做 定点_cos 用 当前角 加 段角。
        sin2 为 做 定点_sin 用 当前角 加 段角。
        x1 为 cx 加 半径 乘 cos1 除以 1000。
        y1 为 cy 加 半径 乘 sin1 除以 1000。
        x2 为 cx 加 半径 乘 cos2 除以 1000。
        y2 为 cy 加 半径 乘 sin2 除以 1000。
        若 剩余 等于 扫描角：
            做 SkPath_moveTo 用 path、x1、y1。
        完。
        做 SkPath_lineTo 用 path、x2、y2。
        当前角 为 当前角 加 段角。
        剩余 为 剩余 减 段角。
    完。
完。

SkPath_重置 接受 path：
    若 path 等于 空：
        返还。
    完。
    path 的 动词数量 为 0。
    path 的 点数量 为 0。
完。

SkPath_是空 接受 path：
    返还 path 等于 空 或者 path 的 动词数量 等于 0。
完。

SkPath_包围盒 接受 path：
    若 path 等于 空 或者 path 的 点数量 等于 0：
        返还 一组：0、0、0、0。
    完。
    可变 min_x 为 做 内存_读32 用 path 的 点列表。
    可变 min_y 为 做 内存_读32 用 path 的 点列表 加 4。
    可变 max_x 为 min_x。
    可变 max_y 为 min_y。
    可变 i 为 1。
    只要 i 小于 path 的 点数量：
        x 为 做 内存_读32 用 path 的 点列表 加 i 乘 8。
        y 为 做 内存_读32 用 path 的 点列表 加 i 乘 8 加 4。
        若 x 小于 min_x：
            min_x 为 x。
        完。
        若 x 大于 max_x：
            max_x 为 x。
        完。
        若 y 小于 min_y：
            min_y 为 y。
        完。
        若 y 大于 max_y：
            max_y 为 y。
        完。
        i 为 i 加 1。
    完。
    返还 一组：min_x、min_y、max_x、max_y。
完。

SkPath_变换 接受 path、矩阵：
    若 path 等于 空 或者 path 的 点数量 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 path 的 点数量：
        x 为 做 内存_读32 用 path 的 点列表 加 i 乘 8。
        y 为 做 内存_读32 用 path 的 点列表 加 i 乘 8 加 4。
        新点 为 做 矩阵_乘向量 用 矩阵、做 向量3_新 用 x、y、0。
        做 内存_写32 用 path 的 点列表 加 i 乘 8、新点 的 x。
        做 内存_写32 用 path 的 点列表 加 i 乘 8 加 4、新点 的 y。
        i 为 i 加 1。
    完。
完。

SkPath_反转 接受 path：
    若 path 等于 空 或者 path 的 点数量 小于 2：
        返还。
    完。
    可变 左 为 0。
    可变 右 为 path 的 点数量 减 1。
    只要 左 小于 右：
        lx 为 做 内存_读32 用 path 的 点列表 加 左 乘 8。
        ly 为 做 内存_读32 用 path 的 点列表 加 左 乘 8 加 4。
        rx 为 做 内存_读32 用 path 的 点列表 加 右 乘 8。
        ry 为 做 内存_读32 用 path 的 点列表 加 右 乘 8 加 4。
        做 内存_写32 用 path 的 点列表 加 左 乘 8、rx。
        做 内存_写32 用 path 的 点列表 加 左 乘 8 加 4、ry。
        做 内存_写32 用 path 的 点列表 加 右 乘 8、lx。
        做 内存_写32 用 path 的 点列表 加 右 乘 8 加 4、ly。
        左 为 左 加 1。
        右 为 右 减 1。
    完。
完。

SkPath_合并 接受 目标、源：
    若 目标 等于 空 或者 源 等于 空：
        返还。
    完。
    可变 vi 为 0。
    只要 vi 小于 源 的 动词数量：
        做 SkPath_添加动词 用 目标、做 内存_读8 用 源 的 动词列表 加 vi。
        vi 为 vi 加 1。
    完。
    可变 pi 为 0。
    只要 pi 小于 源 的 点数量：
        x 为 做 内存_读32 用 源 的 点列表 加 pi 乘 8。
        y 为 做 内存_读32 用 源 的 点列表 加 pi 乘 8 加 4。
        做 SkPath_添加点 用 目标、x、y。
        pi 为 pi 加 1。
    完。
完。

路径迭代器 是 一种 事物，含 路径、动词索引、点索引。

路径迭代器_创建 接受 path：
    返还 新建 路径迭代器：路径 为 path、动词索引 为 0、点索引 为 0。
完。

路径迭代器_下一个 接受 iter：
    若 iter 的 动词索引 大于或等于 iter 的 路径 的 动词数量：
        返还 负 1。
    完。
    动词 为 做 内存_读8 用 iter 的 路径 的 动词列表 加 iter 的 动词索引。
    iter 的 动词索引 为 iter 的 动词索引 加 1。
    若 动词 等于 路径动词_移动：
        iter 的 点索引 为 iter 的 点索引 加 1。
    否则若 动词 等于 路径动词_直线：
        iter 的 点索引 为 iter 的 点索引 加 1。
    否则若 动词 等于 路径动词_二次：
        iter 的 点索引 为 iter 的 点索引 加 2。
    否则若 动词 等于 路径动词_三次：
        iter 的 点索引 为 iter 的 点索引 加 3。
    完。
    返还 动词。
完。

定点_cos 接受 角度：
    返还 做 定点_sin 用 角度 加 1571。
完。

定点_sin 接受 角度：
    x 为 角度 取余于 6284。
    若 x 小于 0：
        x 为 x 加 6284。
    完。
    若 x 大于 3142：
        x 为 x 减 6284。
    完。
    x2 为 x 乘 x 除以 1000。
    x3 为 x2 乘 x 除以 1000。
    x5 为 x3 乘 x2 除以 1000。
    返还 x 减 x3 除以 6 加 x5 除以 120。
完。

可变 路径_总创建数 为 0。

路径_统计 接受 零参：
    返还 一本字典：总创建 对应 路径_总创建数。
完。

本章 收束。
