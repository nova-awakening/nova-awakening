《电池感知接口》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

省电_正常 为 0。

省电_轻度 为 1。

省电_中度 为 2。

省电_极端 为 3。

衰减基数 为 1024。

电池感知状态 是 一种 事物，含 电量百分比、充电中、省电级别、帧率上限、动画启用、视频质量、后台暂停、亮度因子、网络节流、可见、可见变化数、省电切换数、节省能量估计。

可变 全局电池 为 0。

电池感知_初始化 接受 零参：
    全局电池 为 做 堆_分配 用 电池感知状态。
    全局电池 的 电量百分比 为 100。
    全局电池 的 充电中 为 假。
    全局电池 的 省电级别 为 省电_正常。
    全局电池 的 帧率上限 为 60。
    全局电池 的 动画启用 为 真。
    全局电池 的 视频质量 为 1024。
    全局电池 的 后台暂停 为 假。
    全局电池 的 亮度因子 为 1024。
    全局电池 的 网络节流 为 假。
    全局电池 的 可见 为 真。
    全局电池 的 可见变化数 为 0。
    全局电池 的 省电切换数 为 0。
    全局电池 的 节省能量估计 为 0。
完。

电池感知_更新电量 接受 电量、充电：
    若 全局电池 等于 0：
        返还。
    完。
    全局电池 的 电量百分比 为 电量。
    全局电池 的 充电中 为 充电。
    若 充电：
        做 电池感知_设级别 用 省电_正常。
        返还。
    完。
    若 电量 小于 10：
        做 电池感知_设级别 用 省电_极端。
    否则若 电量 小于 25：
        做 电池感知_设级别 用 省电_中度。
    否则若 电量 小于 50：
        做 电池感知_设级别 用 省电_轻度。
    否则：
        做 电池感知_设级别 用 省电_正常。
    完。
完。

电池感知_设级别 接受 级别：
    若 全局电池 等于 0：
        返还。
    完。
    若 全局电池 的 省电级别 等于 级别：
        返还。
    完。
    全局电池 的 省电级别 为 级别。
    全局电池 的 省电切换数 为 全局电池 的 省电切换数 加 1。
    若 级别 等于 省电_正常：
        全局电池 的 帧率上限 为 60。
        全局电池 的 动画启用 为 真。
        全局电池 的 视频质量 为 衰减基数。
        全局电池 的 后台暂停 为 假。
        全局电池 的 亮度因子 为 衰减基数。
        全局电池 的 网络节流 为 假。
    否则若 级别 等于 省电_轻度：
        全局电池 的 帧率上限 为 30。
        全局电池 的 动画启用 为 真。
        全局电池 的 视频质量 为 768。
        全局电池 的 后台暂停 为 假。
        全局电池 的 亮度因子 为 900。
        全局电池 的 网络节流 为 假。
        全局电池 的 节省能量估计 为 全局电池 的 节省能量估计 加 100。
    否则若 级别 等于 省电_中度：
        全局电池 的 帧率上限 为 20。
        全局电池 的 动画启用 为 假。
        全局电池 的 视频质量 为 512。
        全局电池 的 后台暂停 为 真。
        全局电池 的 亮度因子 为 700。
        全局电池 的 网络节流 为 真。
        全局电池 的 节省能量估计 为 全局电池 的 节省能量估计 加 300。
    否则：
        全局电池 的 帧率上限 为 10。
        全局电池 的 动画启用 为 假。
        全局电池 的 视频质量 为 256。
        全局电池 的 后台暂停 为 真。
        全局电池 的 亮度因子 为 400。
        全局电池 的 网络节流 为 真。
        全局电池 的 节省能量估计 为 全局电池 的 节省能量估计 加 600。
    完。
完。

电池感知_可见性变化 接受 可见：
    若 全局电池 等于 0：
        返还。
    完。
    全局电池 的 可见 为 可见。
    全局电池 的 可见变化数 为 全局电池 的 可见变化数 加 1。
    若 非 可见：
        全局电池 的 后台暂停 为 真。
        全局电池 的 帧率上限 为 0。
        全局电池 的 动画启用 为 假。
        全局电池 的 节省能量估计 为 全局电池 的 节省能量估计 加 200。
    否则：
        做 电池感知_设级别 用 全局电池 的 省电级别。
    完。
完。

电池感知_获取帧率上限 接受 零参：
    若 全局电池 等于 0：
        返还 60。
    完。
    返还 全局电池 的 帧率上限。
完。

电池感知_是否动画 接受 零参：
    若 全局电池 等于 0：
        返还 真。
    完。
    返还 全局电池 的 动画启用。
完。

电池感知_视频质量 接受 零参：
    若 全局电池 等于 0：
        返还 衰减基数。
    完。
    返还 全局电池 的 视频质量。
完。

电池感知_亮度因子 接受 零参：
    若 全局电池 等于 0：
        返还 衰减基数。
    完。
    返还 全局电池 的 亮度因子。
完。

电池感知_是否后台暂停 接受 零参：
    若 全局电池 等于 0：
        返还 假。
    完。
    返还 全局电池 的 后台暂停。
完。

电池感知_完整统计 接受 零参：
    若 全局电池 等于 0：
        返还 一组：0、0、0、0、0。
    完。
    返还 一组：全局电池 的 电量百分比、全局电池 的 省电级别、全局电池 的 省电切换数、全局电池 的 可见变化数、全局电池 的 节省能量估计。
完。

本章 收束。
