《CSS选择器匹配》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《..·中枢·内存初始化》。

组合_后代 为 0。

组合_子代 为 1。

组合_相邻 为 2。

组合_通用 为 3。

匹配_标签 为 1。

匹配_类 为 2。

匹配_标识 为 3。

匹配_属性 为 4。

匹配_伪类 为 5。

布隆大小 为 256。

选择器部分 是 一种 事物，含 匹配类型、值哈希、组合子。

可变 布隆过滤 为 0。

可变 匹配总数 为 0。

可变 布隆命中 为 0。

可变 布隆排除 为 0。

选择器_初始化 接受 零参：
    布隆过滤 为 做 堆_分配 用 布隆大小 除以 8。
    可变 序号 为 0。
    只要 序号 小于 布隆大小 除以 8：
        做 内存_写8 用 布隆过滤 加 序号、0。
        序号 为 序号 加 1。
    完。
完。

选择器_布隆添加 接受 哈希：
    位1 为 哈希 取余于 布隆大小。
    位2 为 (哈希 右移 8) 取余于 布隆大小。
    做 内存_写8 用 布隆过滤 加 位1 除以 8、做 内存_读8 用 布隆过滤 加 位1 除以 8 位或 1 左移 位1 取余于 8。
    做 内存_写8 用 布隆过滤 加 位2 除以 8、做 内存_读8 用 布隆过滤 加 位2 除以 8 位或 1 左移 位2 取余于 8。
完。

选择器_布隆测试 接受 哈希：
    位1 为 哈希 取余于 布隆大小。
    位2 为 (哈希 右移 8) 取余于 布隆大小。
    字节1 为 做 内存_读8 用 布隆过滤 加 位1 除以 8。
    字节2 为 做 内存_读8 用 布隆过滤 加 位2 除以 8。
    返还 字节1 位与 1 左移 位1 取余于 8 不等于 0 并且 字节2 位与 1 左移 位2 取余于 8 不等于 0。
完。

选择器_计算特异度 接受 部分表、部分数：
    可变 标识数 为 0。
    可变 类数 为 0。
    可变 标签数 为 0。
    可变 序号 为 0。
    只要 序号 小于 部分数：
        偏移 为 部分表 加 序号 乘 12。
        类型 为 做 内存_读32 用 偏移。
        若 类型 等于 匹配_标识：
            标识数 为 标识数 加 1。
        否则若 类型 等于 匹配_类 或者 类型 等于 匹配_属性 或者 类型 等于 匹配_伪类：
            类数 为 类数 加 1。
        否则若 类型 等于 匹配_标签：
            标签数 为 标签数 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 标识数 左移 16 位或 类数 左移 8 位或 标签数。
完。

选择器_匹配元素 接受 部分表、部分数、元素标签、元素类列表、元素标识：
    匹配总数 为 匹配总数 加 1。
    可变 序号 为 部分数 减 1。
    只要 序号 大于或等于 0：
        偏移 为 部分表 加 序号 乘 12。
        类型 为 做 内存_读32 用 偏移。
        值 为 做 内存_读64 用 偏移 加 4。
        可变 匹配 为 假。
        若 类型 等于 匹配_标签：
            匹配 为 值 等于 元素标签。
        否则若 类型 等于 匹配_类：
            匹配 为 做 选择器_类列表包含 用 元素类列表、值。
        否则若 类型 等于 匹配_标识：
            匹配 为 值 等于 元素标识。
        完。
        若 非 匹配：
            返还 假。
        完。
        序号 为 序号 减 1。
    完。
    返还 真。
完。

选择器_类列表包含 接受 类列表、目标哈希：
    若 类列表 等于 0：
        返还 假。
    完。
    若 非 做 选择器_布隆测试 用 目标哈希：
        布隆排除 为 布隆排除 加 1。
        返还 假。
    完。
    布隆命中 为 布隆命中 加 1。
    返还 真。
完。

选择器_快速排除 接受 元素标签、选择器最后标签：
    若 选择器最后标签 等于 0：
        返还 假。
    完。
    返还 元素标签 不等于 选择器最后标签。
完。

选择器_匹配率 接受 零参：
    若 匹配总数 等于 0：
        返还 0。
    完。
    返还 (布隆命中 加 布隆排除) 乘 100 除以 匹配总数。
完。

选择器_完整统计 接受 零参：
    返还 一组：匹配总数、布隆命中、布隆排除。
完。

本章 收束。
