《BMP解码》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·内核打印》。

引用《..·中枢·内存初始化》。

引用《..·推理·AI总线资源调度》。

引用《.·属性自定义属》。

引用《.·内存管理纹》。

引用《..·中枢·整数转字符串》。

BMP_魔数 为 0x4D42。

BMP_压缩_无 为 0。

BMP_压缩_RLE8 为 1。

BMP_压缩_RLE4 为 2。

BMP_压缩_位域 为 3。

BMP_最大宽 为 65536。

BMP_最大高 为 65536。

BMP_最大文件 为 256 乘 1024 乘 1024。

BMP头信息 是 一种 事物，含 文件大小、像素偏移、头大小、宽、高、位数、压缩方式、图像大小、水平分辨率、垂直分辨率、颜色数、重要颜色数、翻转。

BMP解码统计 是 一种 事物，含 解码次数、成功次数、失败次数、解码24位次数、解码32位次数、解码8位次数、解码4位次数、解码1位次数、解码16位次数、RLE解压次数、总像素数、总字节数、验证失败次数、批量解码次数。

可变 全局_BMP统计 为 空。

BMP统计_初始化 接受 零参：
    若 全局_BMP统计 等于 空：
        全局_BMP统计 为 新建 BMP解码统计。
        全局_BMP统计 的 解码次数 为 0。
        全局_BMP统计 的 成功次数 为 0。
        全局_BMP统计 的 失败次数 为 0。
        全局_BMP统计 的 解码24位次数 为 0。
        全局_BMP统计 的 解码32位次数 为 0。
        全局_BMP统计 的 解码8位次数 为 0。
        全局_BMP统计 的 解码4位次数 为 0。
        全局_BMP统计 的 解码1位次数 为 0。
        全局_BMP统计 的 解码16位次数 为 0。
        全局_BMP统计 的 RLE解压次数 为 0。
        全局_BMP统计 的 总像素数 为 0。
        全局_BMP统计 的 总字节数 为 0。
        全局_BMP统计 的 验证失败次数 为 0。
        全局_BMP统计 的 批量解码次数 为 0。
    完。
完。

BMP_验证 接受 数据、长度：
    若 数据 等于 0 或者 长度 小于 54：
        返还 假。
    完。
    魔数 为 做 内存_读16 用 数据。
    若 魔数 不等于 BMP_魔数：
        返还 假。
    完。
    文件大小 为 做 内存_读32 用 数据 加 2。
    若 文件大小 大于 BMP_最大文件：
        返还 假。
    完。
    宽 为 做 内存_读32 用 数据 加 18。
    高 为 做 内存_读32 用 数据 加 22。
    若 高 小于 0：
        高 为 0 减 高。
    完。
    若 宽 小于或等于 0 或者 宽 大于 BMP_最大宽：
        返还 假。
    完。
    若 高 小于或等于 0 或者 高 大于 BMP_最大高：
        返还 假。
    完。
    位数 为 做 内存_读16 用 数据 加 28。
    若 位数 不等于 1 并且 位数 不等于 4 并且 位数 不等于 8 并且 位数 不等于 16 并且 位数 不等于 24 并且 位数 不等于 32：
        返还 假。
    完。
    返还 真。
完。

BMP_解析头 接受 数据、长度：
    头 为 新建 BMP头信息。
    头 的 文件大小 为 做 内存_读32 用 数据 加 2。
    头 的 像素偏移 为 做 内存_读32 用 数据 加 10。
    头 的 头大小 为 做 内存_读32 用 数据 加 14。
    头 的 宽 为 做 内存_读32 用 数据 加 18。
    头 的 高 为 做 内存_读32 用 数据 加 22。
    头 的 位数 为 做 内存_读16 用 数据 加 28。
    头 的 压缩方式 为 做 内存_读32 用 数据 加 30。
    头 的 图像大小 为 做 内存_读32 用 数据 加 34。
    头 的 水平分辨率 为 做 内存_读32 用 数据 加 38。
    头 的 垂直分辨率 为 做 内存_读32 用 数据 加 42。
    头 的 颜色数 为 做 内存_读32 用 数据 加 46。
    头 的 重要颜色数 为 做 内存_读32 用 数据 加 50。
    头 的 翻转 为 真。
    若 头 的 高 小于 0：
        头 的 高 为 0 减 头 的 高。
        头 的 翻转 为 假。
    完。
    返还 头。
完。

BMP_计算行字节 接受 宽、位数：
    可变 行字节 为 宽 乘 位数 除以 8。
    若 位数 等于 1：
        行字节 为 (宽 加 7) 除以 8。
    完。
    若 位数 等于 4：
        行字节 为 (宽 加 1) 除以 2。
    完。
    若 行字节 取余于 4 不等于 0：
        行字节 为 行字节 加 (4 减 行字节 取余于 4)。
    完。
    返还 行字节。
完。

BMP_读颜色表 接受 数据、头偏移、颜色数：
    可变 颜色表 为 做 堆_分配 用 颜色数 乘 4。
    可变 i 为 0。
    只要 i 小于 颜色数：
        偏移 为 头偏移 加 i 乘 4。
        b 为 做 内存_读8 用 数据 加 偏移。
        g 为 做 内存_读8 用 数据 加 偏移 加 1。
        r 为 做 内存_读8 用 数据 加 偏移 加 2。
        做 内存_写32 用 颜色表 加 i 乘 4、做 SkColor 用 255、r、g、b。
        i 为 i 加 1。
    完。
    返还 颜色表。
完。

BMP_解码 接受 数据、长度：
    做 BMP统计_初始化。
    全局_BMP统计 的 解码次数 为 全局_BMP统计 的 解码次数 加 1。
    全局_BMP统计 的 总字节数 为 全局_BMP统计 的 总字节数 加 长度。
    若 非 做 BMP_验证 用 数据、长度：
        全局_BMP统计 的 验证失败次数 为 全局_BMP统计 的 验证失败次数 加 1。
        全局_BMP统计 的 失败次数 为 全局_BMP统计 的 失败次数 加 1。
        返还 0。
    完。
    头 为 做 BMP_解析头 用 数据、长度。
    bm 为 做 Nova缓冲itmap_创建 用 头 的 宽、头 的 高、像素格式_RGBA8888。
    若 bm 等于 0：
        全局_BMP统计 的 失败次数 为 全局_BMP统计 的 失败次数 加 1。
        返还 0。
    完。
    可变 结果 为 0。
    若 头 的 压缩方式 等于 BMP_压缩_RLE8 并且 头 的 位数 等于 8：
        全局_BMP统计 的 RLE解压次数 为 全局_BMP统计 的 RLE解压次数 加 1。
        结果 为 做 BMP_解码_RLE8 用 数据、头、bm。
    否则若 头 的 压缩方式 等于 BMP_压缩_RLE4 并且 头 的 位数 等于 4：
        全局_BMP统计 的 RLE解压次数 为 全局_BMP统计 的 RLE解压次数 加 1。
        结果 为 做 BMP_解码_RLE4 用 数据、头、bm。
    否则若 头 的 位数 等于 1：
        全局_BMP统计 的 解码1位次数 为 全局_BMP统计 的 解码1位次数 加 1。
        结果 为 做 BMP_解码_1位 用 数据、头、bm。
    否则若 头 的 位数 等于 4：
        全局_BMP统计 的 解码4位次数 为 全局_BMP统计 的 解码4位次数 加 1。
        结果 为 做 BMP_解码_4位 用 数据、头、bm。
    否则若 头 的 位数 等于 8：
        全局_BMP统计 的 解码8位次数 为 全局_BMP统计 的 解码8位次数 加 1。
        结果 为 做 BMP_解码_8位 用 数据、头、bm。
    否则若 头 的 位数 等于 16：
        全局_BMP统计 的 解码16位次数 为 全局_BMP统计 的 解码16位次数 加 1。
        结果 为 做 BMP_解码_16位 用 数据、头、bm。
    否则若 头 的 位数 等于 24：
        全局_BMP统计 的 解码24位次数 为 全局_BMP统计 的 解码24位次数 加 1。
        结果 为 做 BMP_解码_24位 用 数据、头、bm。
    否则若 头 的 位数 等于 32：
        全局_BMP统计 的 解码32位次数 为 全局_BMP统计 的 解码32位次数 加 1。
        结果 为 做 BMP_解码_32位 用 数据、头、bm。
    完。
    若 结果 等于 0：
        全局_BMP统计 的 失败次数 为 全局_BMP统计 的 失败次数 加 1。
        返还 0。
    完。
    全局_BMP统计 的 成功次数 为 全局_BMP统计 的 成功次数 加 1。
    全局_BMP统计 的 总像素数 为 全局_BMP统计 的 总像素数 加 头 的 宽 乘 头 的 高。
    返还 bm。
完。

BMP_解码_24位 接受 数据、头、bm：
    源行字节 为 做 BMP_计算行字节 用 头 的 宽、24。
    可变 y 为 0。
    只要 y 小于 头 的 高：
        可变 目标y 为 y。
        若 头 的 翻转：
            目标y 为 头 的 高 减 1 减 y。
        完。
        源行 为 数据 加 头 的 像素偏移 加 y 乘 源行字节。
        可变 x 为 0。
        只要 x 小于 头 的 宽：
            b 为 做 内存_读8 用 源行 加 x 乘 3。
            g 为 做 内存_读8 用 源行 加 x 乘 3 加 1。
            r 为 做 内存_读8 用 源行 加 x 乘 3 加 2。
            做 Nova缓冲itmap_设置像素 用 bm、x、目标y、做 SkColor 用 255、r、g、b。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 1。
完。

BMP_解码_32位 接受 数据、头、bm：
    源行字节 为 做 BMP_计算行字节 用 头 的 宽、32。
    可变 y 为 0。
    只要 y 小于 头 的 高：
        可变 目标y 为 y。
        若 头 的 翻转：
            目标y 为 头 的 高 减 1 减 y。
        完。
        源行 为 数据 加 头 的 像素偏移 加 y 乘 源行字节。
        可变 x 为 0。
        只要 x 小于 头 的 宽：
            b 为 做 内存_读8 用 源行 加 x 乘 4。
            g 为 做 内存_读8 用 源行 加 x 乘 4 加 1。
            r 为 做 内存_读8 用 源行 加 x 乘 4 加 2。
            a 为 做 内存_读8 用 源行 加 x 乘 4 加 3。
            做 Nova缓冲itmap_设置像素 用 bm、x、目标y、做 SkColor 用 a、r、g、b。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 1。
完。

BMP_解码_8位 接受 数据、头、bm：
    可变 颜色数 为 头 的 颜色数。
    若 颜色数 等于 0：
        颜色数 为 256。
    完。
    颜色表 为 做 BMP_读颜色表 用 数据、14 加 头 的 头大小、颜色数。
    源行字节 为 做 BMP_计算行字节 用 头 的 宽、8。
    可变 y 为 0。
    只要 y 小于 头 的 高：
        可变 目标y 为 y。
        若 头 的 翻转：
            目标y 为 头 的 高 减 1 减 y。
        完。
        源行 为 数据 加 头 的 像素偏移 加 y 乘 源行字节。
        可变 x 为 0。
        只要 x 小于 头 的 宽：
            索引 为 做 内存_读8 用 源行 加 x。
            若 索引 小于 颜色数：
                做 Nova缓冲itmap_设置像素 用 bm、x、目标y、做 内存_读32 用 颜色表 加 索引 乘 4。
            完。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    做 堆_释放 用 颜色表。
    返还 1。
完。

BMP_解码_4位 接受 数据、头、bm：
    可变 颜色数 为 头 的 颜色数。
    若 颜色数 等于 0：
        颜色数 为 16。
    完。
    颜色表 为 做 BMP_读颜色表 用 数据、14 加 头 的 头大小、颜色数。
    源行字节 为 做 BMP_计算行字节 用 头 的 宽、4。
    可变 y 为 0。
    只要 y 小于 头 的 高：
        可变 目标y 为 y。
        若 头 的 翻转：
            目标y 为 头 的 高 减 1 减 y。
        完。
        源行 为 数据 加 头 的 像素偏移 加 y 乘 源行字节。
        可变 x 为 0。
        只要 x 小于 头 的 宽：
            字节 为 做 内存_读8 用 源行 加 x 除以 2。
            可变 索引 为 0。
            若 x 取余于 2 等于 0：
                索引 为 字节 右移 4 位与 0x0F。
            否则：
                索引 为 字节 位与 0x0F。
            完。
            若 索引 小于 颜色数：
                做 Nova缓冲itmap_设置像素 用 bm、x、目标y、做 内存_读32 用 颜色表 加 索引 乘 4。
            完。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    做 堆_释放 用 颜色表。
    返还 1。
完。

BMP_解码_1位 接受 数据、头、bm：
    颜色表 为 做 BMP_读颜色表 用 数据、14 加 头 的 头大小、2。
    源行字节 为 做 BMP_计算行字节 用 头 的 宽、1。
    可变 y 为 0。
    只要 y 小于 头 的 高：
        可变 目标y 为 y。
        若 头 的 翻转：
            目标y 为 头 的 高 减 1 减 y。
        完。
        源行 为 数据 加 头 的 像素偏移 加 y 乘 源行字节。
        可变 x 为 0。
        只要 x 小于 头 的 宽：
            字节 为 做 内存_读8 用 源行 加 x 除以 8。
            位偏移 为 7 减 x 取余于 8。
            索引 为 字节 右移 位偏移 位与 1。
            做 Nova缓冲itmap_设置像素 用 bm、x、目标y、做 内存_读32 用 颜色表 加 索引 乘 4。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    做 堆_释放 用 颜色表。
    返还 1。
完。

BMP_解码_16位 接受 数据、头、bm：
    源行字节 为 做 BMP_计算行字节 用 头 的 宽、16。
    可变 y 为 0。
    只要 y 小于 头 的 高：
        可变 目标y 为 y。
        若 头 的 翻转：
            目标y 为 头 的 高 减 1 减 y。
        完。
        源行 为 数据 加 头 的 像素偏移 加 y 乘 源行字节。
        可变 x 为 0。
        只要 x 小于 头 的 宽：
            像素 为 做 内存_读16 用 源行 加 x 乘 2。
            r 为 (像素 右移 10 位与 0x1F) 乘 255 除以 31。
            g 为 (像素 右移 5 位与 0x1F) 乘 255 除以 31。
            b 为 (像素 位与 0x1F) 乘 255 除以 31。
            做 Nova缓冲itmap_设置像素 用 bm、x、目标y、做 SkColor 用 255、r、g、b。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 1。
完。

BMP_解码_RLE8 接受 数据、头、bm：
    可变 颜色数 为 头 的 颜色数。
    若 颜色数 等于 0：
        颜色数 为 256。
    完。
    颜色表 为 做 BMP_读颜色表 用 数据、14 加 头 的 头大小、颜色数。
    可变 x 为 0。
    可变 y 为 0。
    可变 偏移 为 头 的 像素偏移。
    只要 偏移 小于 长度 减 1：
        计数 为 做 内存_读8 用 数据 加 偏移。
        值 为 做 内存_读8 用 数据 加 偏移 加 1。
        偏移 为 偏移 加 2。
        若 计数 大于 0：
            可变 i 为 0。
            只要 i 小于 计数 并且 x 小于 头 的 宽：
                可变 目标y 为 y。
                若 头 的 翻转：
                    目标y 为 头 的 高 减 1 减 y。
                完。
                若 值 小于 颜色数：
                    做 Nova缓冲itmap_设置像素 用 bm、x、目标y、做 内存_读32 用 颜色表 加 值 乘 4。
                完。
                x 为 x 加 1。
                i 为 i 加 1。
            完。
        否则：
            若 值 等于 0：
                x 为 0。
                y 为 y 加 1。
            否则若 值 等于 1：
                做 堆_释放 用 颜色表。
                返还 1。
            否则若 值 等于 2：
                x 为 x 加 做 内存_读8 用 数据 加 偏移。
                y 为 y 加 做 内存_读8 用 数据 加 偏移 加 1。
                偏移 为 偏移 加 2。
            否则：
                可变 i 为 0。
                只要 i 小于 值 并且 x 小于 头 的 宽：
                    索引 为 做 内存_读8 用 数据 加 偏移。
                    偏移 为 偏移 加 1。
                    可变 目标y 为 y。
                    若 头 的 翻转：
                        目标y 为 头 的 高 减 1 减 y。
                    完。
                    若 索引 小于 颜色数：
                        做 Nova缓冲itmap_设置像素 用 bm、x、目标y、做 内存_读32 用 颜色表 加 索引 乘 4。
                    完。
                    x 为 x 加 1。
                    i 为 i 加 1。
                完。
                若 值 取余于 2 不等于 0：
                    偏移 为 偏移 加 1。
                完。
            完。
        完。
    完。
    做 堆_释放 用 颜色表。
    返还 1。
完。

BMP_解码_RLE4 接受 数据、头、bm：
    可变 颜色数 为 头 的 颜色数。
    若 颜色数 等于 0：
        颜色数 为 16。
    完。
    颜色表 为 做 BMP_读颜色表 用 数据、14 加 头 的 头大小、颜色数。
    可变 x 为 0。
    可变 y 为 0。
    可变 偏移 为 头 的 像素偏移。
    只要 偏移 小于 长度 减 1：
        计数 为 做 内存_读8 用 数据 加 偏移。
        值 为 做 内存_读8 用 数据 加 偏移 加 1。
        偏移 为 偏移 加 2。
        若 计数 大于 0：
            可变 i 为 0。
            只要 i 小于 计数 并且 x 小于 头 的 宽：
                可变 索引 为 0。
                若 i 取余于 2 等于 0：
                    索引 为 值 右移 4 位与 0x0F。
                否则：
                    索引 为 值 位与 0x0F。
                完。
                可变 目标y 为 y。
                若 头 的 翻转：
                    目标y 为 头 的 高 减 1 减 y。
                完。
                若 索引 小于 颜色数：
                    做 Nova缓冲itmap_设置像素 用 bm、x、目标y、做 内存_读32 用 颜色表 加 索引 乘 4。
                完。
                x 为 x 加 1。
                i 为 i 加 1。
            完。
        否则：
            若 值 等于 0：
                x 为 0。
                y 为 y 加 1。
            否则若 值 等于 1：
                做 堆_释放 用 颜色表。
                返还 1。
            否则若 值 等于 2：
                x 为 x 加 做 内存_读8 用 数据 加 偏移。
                y 为 y 加 做 内存_读8 用 数据 加 偏移 加 1。
                偏移 为 偏移 加 2。
            否则：
                字节数 为 (值 加 1) 除以 2。
                可变 i 为 0。
                只要 i 小于 值 并且 x 小于 头 的 宽：
                    字节 为 做 内存_读8 用 数据 加 偏移 加 i 除以 2。
                    可变 索引 为 0。
                    若 i 取余于 2 等于 0：
                        索引 为 字节 右移 4 位与 0x0F。
                    否则：
                        索引 为 字节 位与 0x0F。
                    完。
                    可变 目标y 为 y。
                    若 头 的 翻转：
                        目标y 为 头 的 高 减 1 减 y。
                    完。
                    若 索引 小于 颜色数：
                        做 Nova缓冲itmap_设置像素 用 bm、x、目标y、做 内存_读32 用 颜色表 加 索引 乘 4。
                    完。
                    x 为 x 加 1。
                    i 为 i 加 1。
                完。
                偏移 为 偏移 加 字节数。
                若 字节数 取余于 2 不等于 0：
                    偏移 为 偏移 加 1。
                完。
            完。
        完。
    完。
    做 堆_释放 用 颜色表。
    返还 1。
完。

BMP_获取信息 接受 数据、长度：
    若 非 做 BMP_验证 用 数据、长度：
        返还 空。
    完。
    返还 做 BMP_解析头 用 数据、长度。
完。

BMP_批量解码 接受 数据列表、长度列表、数量：
    做 BMP统计_初始化。
    全局_BMP统计 的 批量解码次数 为 全局_BMP统计 的 批量解码次数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 数量：
        bm 为 做 BMP_解码 用 数据列表 的 第 i 项、长度列表 的 第 i 项。
        做 追加 用 结果、bm。
        i 为 i 加 1。
    完。
    返还 结果。
完。

智能_图像Decoder_AI加载优先级 接受 url：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 url 等于 0：
        返还 Q_HALF。
    完。
    返还 做 AI总线_资源优先级 用 bus、url。
完。

智能_图像Decoder_AI预取决策 接受 url：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 url 等于 0：
        返还 假。
    完。
    分数 为 做 AI总线_资源优先级 用 bus、url。
    返还 分数 大于 600。
完。

BMP_打印统计 接受 零参：
    做 BMP统计_初始化。
    做 打印行 用 「=== BMP解码统计 ===」。
    做 打印行 用 「解码次数: 」 加 做 整数转字符串 用 全局_BMP统计 的 解码次数。
    做 打印行 用 「成功: 」 加 做 整数转字符串 用 全局_BMP统计 的 成功次数。
    做 打印行 用 「失败: 」 加 做 整数转字符串 用 全局_BMP统计 的 失败次数。
    做 打印行 用 「24位: 」 加 做 整数转字符串 用 全局_BMP统计 的 解码24位次数。
    做 打印行 用 「32位: 」 加 做 整数转字符串 用 全局_BMP统计 的 解码32位次数。
    做 打印行 用 「16位: 」 加 做 整数转字符串 用 全局_BMP统计 的 解码16位次数。
    做 打印行 用 「8位: 」 加 做 整数转字符串 用 全局_BMP统计 的 解码8位次数。
    做 打印行 用 「4位: 」 加 做 整数转字符串 用 全局_BMP统计 的 解码4位次数。
    做 打印行 用 「1位: 」 加 做 整数转字符串 用 全局_BMP统计 的 解码1位次数。
    做 打印行 用 「RLE解压: 」 加 做 整数转字符串 用 全局_BMP统计 的 RLE解压次数。
    做 打印行 用 「总像素: 」 加 做 整数转字符串 用 全局_BMP统计 的 总像素数。
    做 打印行 用 「总字节: 」 加 做 整数转字符串 用 全局_BMP统计 的 总字节数。
    做 打印行 用 「验证失败: 」 加 做 整数转字符串 用 全局_BMP统计 的 验证失败次数。
    做 打印行 用 「批量: 」 加 做 整数转字符串 用 全局_BMP统计 的 批量解码次数。
完。

BMP_重置统计 接受 零参：
    做 BMP统计_初始化。
    全局_BMP统计 的 解码次数 为 0。
    全局_BMP统计 的 成功次数 为 0。
    全局_BMP统计 的 失败次数 为 0。
    全局_BMP统计 的 总像素数 为 0。
    全局_BMP统计 的 总字节数 为 0。
    全局_BMP统计 的 验证失败次数 为 0。
    全局_BMP统计 的 批量解码次数 为 0。
    全局_BMP统计 的 RLE解压次数 为 0。
完。

可变 _谐振BMP_分块次数 为 0。

可变 _谐振BMP_总行数 为 0。

谐振BMP_分块解码 接受 数据、宽度、高度、块高度：
    可变 已解码行 为 0。
    只要 已解码行 小于 高度：
        本次行数 为 块高度。
        若 已解码行 加 本次行数 大于 高度：
            本次行数 为 高度 减 已解码行。
        完。
        已解码行 为 已解码行 加 本次行数。
        _谐振BMP_分块次数 为 _谐振BMP_分块次数 加 1。
        _谐振BMP_总行数 为 _谐振BMP_总行数 加 本次行数。
    完。
    返还 已解码行。
完。

BMP解码_完整统计 接受 零参：
    返还 一组：「谐振分块次数」、_谐振BMP_分块次数、「谐振总行数」、_谐振BMP_总行数。
完。

谐振BMP_估算内存 接受 宽度、高度、位深：
    返还 宽度 乘 高度 乘 位深 除以 8。
完。

谐振BMP_需要分块 接受 高度：
    返还 高度 大于 1024。
完。

本章 收束。
