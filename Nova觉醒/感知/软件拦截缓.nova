《软件拦截缓》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·整数转字符串》。

引用《.·内核直连层》。

引用《..·中枢·辅助存根》。

引用《.·服务工线容》。

引用《..·中枢·内容可见性》。

引用《..·中枢·沙箱隔离进》。

Service工线_拦截取数 接受 请求URL、请求方法：
    容器 为 做 Service工线Container_获取。
    若 容器 的 控制器 等于 0：
        返还 0。
    完。
    sw 为 容器 的 控制器。
    若 sw 的 状态 不等于 SW_STATE_ACTIVATED：
        返还 0。
    完。
    若 sw 的 fetch回调 等于 0：
        返还 0。
    完。
    注册 为 sw 的 注册。
    若 注册 不等于 0 并且 非 做 字符串_开头匹配 用 请求URL、注册 的 作用域：
        返还 0。
    完。
    返还 0。
完。

Service工线_注入全局接口 接受 sw：
    若 sw 等于 0 或者 sw 的 Nova运行时实例 等于 0：
        返还。
    完。
完。

缓存Entry 是 一种 事物，含 请求URL、请求方法、响应状态码、响应头、响应体、响应体长度、时间戳。

缓存 是 一种 事物，含 名称、条目列表、条目数量、条目容量。

缓存存储 是 一种 事物，含 缓存列表、缓存数量、缓存容量。

缓存存储_创建 接受 零参：
    cs 为 做 堆_分配 用 缓存存储。
    cs 的 缓存容量 为 32。
    cs 的 缓存列表 为 做 堆_分配_数组 用 32 乘 8。
    cs 的 缓存数量 为 0。
    返还 cs。
完。

缓存存储_open 接受 cs、名称：
    可变 i 为 0。
    只要 i 小于 cs 的 缓存数量：
        c 为 做 内存_读64 用 cs 的 缓存列表 加 i 乘 8。
        若 c 不等于 0 并且 做 字符串_相等 用 c 的 名称、名称：
            返还 c。
        完。
        i 为 i 加 1。
    完。
    c 为 做 堆_分配 用 缓存。
    c 的 名称 为 做 字符串_复制 用 名称。
    c 的 条目容量 为 256。
    c 的 条目列表 为 做 堆_分配_数组 用 256 乘 8。
    c 的 条目数量 为 0。
    若 cs 的 缓存数量 小于 cs 的 缓存容量：
        做 内存_写64 用 cs 的 缓存列表 加 cs 的 缓存数量 乘 8、c。
        cs 的 缓存数量 为 cs 的 缓存数量 加 1。
    完。
    返还 c。
完。

缓存存储_has 接受 cs、名称：
    可变 i 为 0。
    只要 i 小于 cs 的 缓存数量：
        c 为 做 内存_读64 用 cs 的 缓存列表 加 i 乘 8。
        若 c 不等于 0 并且 做 字符串_相等 用 c 的 名称、名称：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

缓存存储_delete 接受 cs、名称：
    可变 i 为 0。
    只要 i 小于 cs 的 缓存数量：
        c 为 做 内存_读64 用 cs 的 缓存列表 加 i 乘 8。
        若 c 不等于 0 并且 做 字符串_相等 用 c 的 名称、名称：
            cs 的 缓存数量 为 cs 的 缓存数量 减 1。
            做 内存_写64 用 cs 的 缓存列表 加 i 乘 8、做 内存_读64 用 cs 的 缓存列表 加 cs 的 缓存数量 乘 8。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

缓存存储_keys 接受 cs：
    结果 为 做 堆_分配_数组 用 cs 的 缓存数量 乘 8。
    可变 i 为 0。
    只要 i 小于 cs 的 缓存数量：
        c 为 做 内存_读64 用 cs 的 缓存列表 加 i 乘 8。
        若 c 不等于 0：
            做 内存_写64 用 结果 加 i 乘 8、c 的 名称。
        完。
        i 为 i 加 1。
    完。
    返还 一组：结果、cs 的 缓存数量。
完。

缓存存储_match 接受 cs、请求URL：
    可变 i 为 0。
    只要 i 小于 cs 的 缓存数量：
        c 为 做 内存_读64 用 cs 的 缓存列表 加 i 乘 8。
        结果 为 做 缓存_match 用 c、请求URL、0。
        若 结果 不等于 0：
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

缓存_put 接受 cache、请求URL、响应状态、响应头、响应体、响应体长度：
    若 cache 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 cache 的 条目数量：
        entry偏移 为 cache 的 条目列表 加 i 乘 8。
        entry 为 做 内存_读64 用 entry偏移。
        若 entry 不等于 0 并且 做 字符串_相等 用 entry 的 请求URL、请求URL：
            entry 的 响应状态码 为 响应状态。
            若 响应头 不等于 0：
                entry 的 响应头 为 做 字符串_复制 用 响应头。
            完。
            若 响应体 不等于 0 并且 响应体长度 大于 0：
                entry 的 响应体 为 做 堆_分配 用 响应体长度。
                做 内存_复制 用 entry 的 响应体、响应体、响应体长度。
                entry 的 响应体长度 为 响应体长度。
            完。
            entry 的 时间戳 为 做 平台_获取高精度时间。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 cache 的 条目数量 大于或等于 cache 的 条目容量：
        返还。
    完。
    entry 为 做 堆_分配 用 缓存Entry。
    entry 的 请求URL 为 做 字符串_复制 用 请求URL。
    entry 的 请求方法 为 「GET」。
    entry 的 响应状态码 为 响应状态。
    entry 的 响应头 为 若 响应头 不等于 0 则 做 字符串_复制 用 响应头 否则 0。
    若 响应体 不等于 0 并且 响应体长度 大于 0：
        entry 的 响应体 为 做 堆_分配 用 响应体长度。
        做 内存_复制 用 entry 的 响应体、响应体、响应体长度。
        entry 的 响应体长度 为 响应体长度。
    否则：
        entry 的 响应体 为 0。
        entry 的 响应体长度 为 0。
    完。
    entry 的 时间戳 为 做 平台_获取高精度时间。
    做 内存_写64 用 cache 的 条目列表 加 cache 的 条目数量 乘 8、entry。
    cache 的 条目数量 为 cache 的 条目数量 加 1。
完。

缓存_match 接受 cache、请求URL、选项：
    若 cache 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 cache 的 条目数量：
        entry 为 做 内存_读64 用 cache 的 条目列表 加 i 乘 8。
        若 entry 不等于 0：
            可变 匹配 为 做 字符串_相等 用 entry 的 请求URL、请求URL。
            若 非 匹配 并且 选项 不等于 0：
            完。
            若 匹配：
                返还 entry。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

缓存_delete 接受 cache、请求URL：
    若 cache 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 cache 的 条目数量：
        entry 为 做 内存_读64 用 cache 的 条目列表 加 i 乘 8。
        若 entry 不等于 0 并且 做 字符串_相等 用 entry 的 请求URL、请求URL：
            cache 的 条目数量 为 cache 的 条目数量 减 1。
            做 内存_写64 用 cache 的 条目列表 加 i 乘 8、做 内存_读64 用 cache 的 条目列表 加 cache 的 条目数量 乘 8。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

可变 AI_软件拦截缓_调用数 为 0。

可变 AI_软件拦截缓_异常数 为 0。

AI_软件拦截缓_监控 接受 零参：
    AI_软件拦截缓_调用数 为 AI_软件拦截缓_调用数 加 1。
    返还 AI_软件拦截缓_异常数 等于 0。
完。

AI_软件拦截缓_健康检查 接受 零参：
    若 AI_软件拦截缓_调用数 大于 0 并且 AI_软件拦截缓_异常数 乘 100 除以 AI_软件拦截缓_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
