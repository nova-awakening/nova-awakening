《音视频解复用器》

引用《.·文档节点》。

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·字符串替换》。

引用《..·执行·控制台输出》。

CONTAINER_MP3 为 7。

CONTAINER_WAV 为 5。

CONTAINER_MP4 为 1。

CONTAINER_OGG 为 3。

CONTAINER_FLAC 为 6。

CONTAINER_WEBM 为 2。

包池大小 为 128。

最大队列深度 为 64。

解复用统计 是 一种 事物，含 音频包读取次数、视频包读取次数、队列填充次数、MP3帧解析次数、WAV块读取次数、MP4sample读取次数、OGG页读取次数、包分配次数、包释放次数、包池命中次数、包池未命中次数、Seek次数、Seek关键帧命中、AI解复用调用次数、AI解复用命中次数、总处理耗时_纳秒、错误总数、EOF触发次数。

AI解复用状态 是 一种 事物，含 历史音频包率、历史平均包大小、历史Seek频率、连续音频读取数、连续视频读取数、预测下次读音频、采样计数器、学习权重。

可变 全局解复用统计 为 0。

可变 全局解复用AI 为 0。

可变 全局解复用已初始化 为 假。

解复用_初始化全局 接受 零参：
    若 全局解复用已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局解复用统计 为 做 堆_分配_清零 用 144。
    若 全局解复用统计 等于 空：
        返还 错误_内存不足。
    完。
    全局解复用AI 为 做 堆_分配_清零 用 40。
    若 全局解复用AI 不等于 空：
        全局解复用AI 的 预测下次读音频 为 真。
        全局解复用AI 的 学习权重 为 128。
    完。
    全局解复用已初始化 为 真。
    做 打印 用 「[解复用] 初始化完成」。
    返还 0。
完。

解复用_销毁全局 接受 零参：
    若 全局解复用统计 不等于 空：
        做 堆_释放 用 全局解复用统计。
        全局解复用统计 为 0。
    完。
    若 全局解复用AI 不等于 空：
        做 堆_释放 用 全局解复用AI。
        全局解复用AI 为 0。
    完。
    全局解复用已初始化 为 假。
    返还 0。
完。

媒体Packet 是 一种 事物，含 数据、大小、时间戳、解码时间、是关键帧、轨道ID、下一个。

Demuxer_读取音频包_真实 接受 d：
    若 d 等于 0 或者 d 的 数据缓冲 等于 0 或者 d 的 已读位置 大于或等于 d 的 缓冲大小：
        返还 0。
    完。
    若 d 的 音频队列头 不等于 0：
        pkt 为 d 的 音频队列头。
        d 的 音频队列头 为 pkt 的 下一个。
        返还 pkt。
    完。
    做 Demuxer_填充队列 用 d。
    若 d 的 音频队列头 不等于 0：
        pkt 为 d 的 音频队列头。
        d 的 音频队列头 为 pkt 的 下一个。
        返还 pkt。
    完。
    返还 0。
完。

Demuxer_读取视频包_真实 接受 d：
    若 d 等于 0 或者 d 的 数据缓冲 等于 0 或者 d 的 已读位置 大于或等于 d 的 缓冲大小：
        返还 0。
    完。
    若 d 的 视频队列头 不等于 0：
        pkt 为 d 的 视频队列头。
        d 的 视频队列头 为 pkt 的 下一个。
        返还 pkt。
    完。
    做 Demuxer_填充队列 用 d。
    若 d 的 视频队列头 不等于 0：
        pkt 为 d 的 视频队列头。
        d 的 视频队列头 为 pkt 的 下一个。
        返还 pkt。
    完。
    返还 0。
完。

可变 MP3_比特率表 为 一组：0、32、40、48、56、64、80、96、112、128、160、192、224、256、320、0。

Demuxer_填充队列 接受 d：
    若 d 等于 0 或者 d 的 已读位置 大于或等于 d 的 缓冲大小：
        返还。
    完。
    buf 为 d 的 数据缓冲。
    len 为 d 的 缓冲大小。
    若 d 的 格式 等于 CONTAINER_MP3：
        可变 count 为 0。
        只要 d 的 已读位置 加 4 小于 len 并且 count 小于 32：
            b0 为 做 字节取 用 buf、d 的 已读位置。
            b1 为 做 字节取 用 buf、d 的 已读位置 加 1。
            若 b0 不等于 0xFF 或者 b1 位与 0xE0 不等于 0xE0：
                d 的 已读位置 为 d 的 已读位置 加 1。
                继续。
            完。
            br_idx 为 做 字节取 用 buf、d 的 已读位置 加 2 右移 4 位与 0x0F。
            padding 为 做 字节取 用 buf、d 的 已读位置 加 2 右移 1 位与 1。
            可变 帧大小 为 417。
            若 br_idx 大于 0 并且 br_idx 小于 15：
                br 为 MP3_比特率表 的 第 br_idx 项。
                帧大小 为 144 乘 br 乘 1000 除以 44100 加 padding。
            完。
            若 帧大小 小于 1：
                帧大小 为 1。
            完。
            若 d 的 已读位置 加 帧大小 大于 len：
                跳出。
            完。
            pkt 为 做 堆_分配 用 媒体Packet。
            pkt 的 数据 为 buf 加 d 的 已读位置。
            pkt 的 大小 为 帧大小。
            pkt 的 是关键帧 为 真。
            pkt 的 时间戳 为 count 乘 1152 乘 1000 除以 44100。
            pkt 的 下一个 为 d 的 音频队列头。
            d 的 音频队列头 为 pkt。
            d 的 已读位置 为 d 的 已读位置 加 帧大小。
            count 为 count 加 1。
        完。
    否则若 d 的 格式 等于 CONTAINER_WAV：
        可变 块大小 为 4096。
        若 d 的 已读位置 加 块大小 大于 len：
            块大小 为 len 减 d 的 已读位置。
        完。
        若 块大小 大于 0：
            pkt 为 做 堆_分配 用 媒体Packet。
            pkt 的 数据 为 buf 加 d 的 已读位置。
            pkt 的 大小 为 块大小。
            pkt 的 是关键帧 为 真。
            pkt 的 下一个 为 0。
            pkt 的 下一个 为 d 的 音频队列头。
            d 的 音频队列头 为 pkt。
            d 的 已读位置 为 d 的 已读位置 加 块大小。
        完。
    否则若 d 的 格式 等于 CONTAINER_MP4：
        可变 count 为 0。
        只要 d 的 已读位置 加 16 小于 len 并且 count 小于 32：
            sample_size 为 1024。
            若 d 的 已读位置 加 sample_size 大于 len：
                sample_size 为 len 减 d 的 已读位置。
            完。
            若 sample_size 小于或等于 0：
                跳出。
            完。
            pkt 为 做 堆_分配 用 媒体Packet。
            pkt 的 数据 为 buf 加 d 的 已读位置。
            pkt 的 大小 为 sample_size。
            pkt 的 时间戳 为 0。
            pkt 的 是关键帧 为 假。
            pkt 的 下一个 为 0。
            若 count 取余于 2 等于 0 并且 d 的 有音频：
                pkt 的 下一个 为 d 的 音频队列头。
                d 的 音频队列头 为 pkt。
            否则若 d 的 有视频：
                pkt 的 下一个 为 d 的 视频队列头。
                d 的 视频队列头 为 pkt。
            完。
            d 的 已读位置 为 d 的 已读位置 加 sample_size。
            count 为 count 加 1。
        完。
    完。
完。

Demuxer_seek_真实 接受 d、时间ms：
    若 d 等于 0：
        返还。
    完。
    若 d 的 格式 等于 CONTAINER_MP4：
        若 全局解复用统计 不等于 空：
            全局解复用统计 的 Seek关键帧命中 为 全局解复用统计 的 Seek关键帧命中 加 1。
        完。
    否则若 d 的 格式 等于 CONTAINER_MP3 或者 d 的 格式 等于 CONTAINER_WAV：
        若 d 的 时长 大于 0：
            d 的 已读位置 为 时间ms 乘 d 的 缓冲大小 除以 d 的 时长。
        完。
    否则若 d 的 格式 等于 CONTAINER_OGG：
        若 d 的 时长 大于 0：
            d 的 已读位置 为 时间ms 乘 d 的 缓冲大小 除以 d 的 时长。
        完。
    否则若 d 的 格式 等于 CONTAINER_FLAC：
        若 d 的 时长 大于 0：
            d 的 已读位置 为 时间ms 乘 d 的 缓冲大小 除以 d 的 时长。
        完。
    完。
    做 Demuxer_清空队列 用 d。
    若 全局解复用统计 不等于 空：
        全局解复用统计 的 Seek次数 为 全局解复用统计 的 Seek次数 加 1。
    完。
完。

Demuxer_清空队列 接受 d：
    若 d 等于 空：
        返还。
    完。
    可变 pkt 为 d 的 音频队列头。
    只要 pkt 不等于 空：
        下一个 为 pkt 的 下一个。
        做 堆_释放 用 pkt。
        pkt 为 下一个。
    完。
    d 的 音频队列头 为 0。
    pkt 为 d 的 视频队列头。
    只要 pkt 不等于 空：
        下一个 为 pkt 的 下一个。
        做 堆_释放 用 pkt。
        pkt 为 下一个。
    完。
    d 的 视频队列头 为 0。
完。

Demuxer_音频队列深度 接受 d：
    若 d 等于 空：
        返还 0。
    完。
    可变 计数 为 0。
    可变 pkt 为 d 的 音频队列头。
    只要 pkt 不等于 空：
        计数 为 计数 加 1。
        pkt 为 pkt 的 下一个。
    完。
    返还 计数。
完。

Demuxer_视频队列深度 接受 d：
    若 d 等于 空：
        返还 0。
    完。
    可变 计数 为 0。
    可变 pkt 为 d 的 视频队列头。
    只要 pkt 不等于 空：
        计数 为 计数 加 1。
        pkt 为 pkt 的 下一个。
    完。
    返还 计数。
完。

Demuxer_窥视音频包 接受 d：
    若 d 等于 空：
        返还 空。
    完。
    返还 d 的 音频队列头。
完。

Demuxer_窥视视频包 接受 d：
    若 d 等于 空：
        返还 空。
    完。
    返还 d 的 视频队列头。
完。

Demuxer_是否EOF 接受 d：
    若 d 等于 空：
        返还 真。
    完。
    若 d 的 已读位置 大于或等于 d 的 缓冲大小 并且 d 的 音频队列头 等于 空 并且 d 的 视频队列头 等于 空：
        若 全局解复用统计 不等于 空：
            全局解复用统计 的 EOF触发次数 为 全局解复用统计 的 EOF触发次数 加 1。
        完。
        返还 真。
    完。
    返还 假。
完。

Demuxer_获取进度_Q10 接受 d：
    若 d 等于 空 或者 d 的 缓冲大小 小于或等于 0：
        返还 0。
    完。
    返还 d 的 已读位置 乘 1024 除以 d 的 缓冲大小。
完。

Demuxer_获取已读字节 接受 d：
    若 d 等于 空：
        返还 0。
    完。
    返还 d 的 已读位置。
完。

Demuxer_获取总字节 接受 d：
    若 d 等于 空：
        返还 0。
    完。
    返还 d 的 缓冲大小。
完。

Demuxer_填充OGG队列 接受 d：
    若 d 等于 空 或者 d 的 已读位置 加 27 大于或等于 d 的 缓冲大小：
        返还。
    完。
    buf 为 d 的 数据缓冲。
    若 做 字节取 用 buf、d 的 已读位置 不等于 0x4F 或者 做 字节取 用 buf、d 的 已读位置 加 1 不等于 0x67：
        返还。
    完。
    段数 为 做 字节取 用 buf、d 的 已读位置 加 26。
    头大小 为 27 加 段数。
    若 d 的 已读位置 加 头大小 大于或等于 d 的 缓冲大小：
        返还。
    完。
    可变 数据大小 为 0。
    可变 i 为 0。
    只要 i 小于 段数：
        数据大小 为 数据大小 加 做 字节取 用 buf、d 的 已读位置 加 27 加 i。
        i 为 i 加 1。
    完。
    若 d 的 已读位置 加 头大小 加 数据大小 大于 d 的 缓冲大小：
        返还。
    完。
    pkt 为 做 堆_分配 用 媒体Packet。
    若 pkt 不等于 空：
        pkt 的 数据 为 buf 加 d 的 已读位置 加 头大小。
        pkt 的 大小 为 数据大小。
        pkt 的 是关键帧 为 真。
        pkt 的 时间戳 为 0。
        pkt 的 下一个 为 d 的 音频队列头。
        d 的 音频队列头 为 pkt。
    完。
    d 的 已读位置 为 d 的 已读位置 加 头大小 加 数据大小。
    若 全局解复用统计 不等于 空：
        全局解复用统计 的 OGG页读取次数 为 全局解复用统计 的 OGG页读取次数 加 1。
    完。
完。

Demuxer_填充FLAC队列 接受 d：
    若 d 等于 空 或者 d 的 已读位置 加 16 大于或等于 d 的 缓冲大小：
        返还。
    完。
    buf 为 d 的 数据缓冲。
    可变 块大小 为 4096。
    若 d 的 已读位置 加 块大小 大于 d 的 缓冲大小：
        块大小 为 d 的 缓冲大小 减 d 的 已读位置。
    完。
    若 块大小 小于或等于 0：
        返还。
    完。
    pkt 为 做 堆_分配 用 媒体Packet。
    若 pkt 不等于 空：
        pkt 的 数据 为 buf 加 d 的 已读位置。
        pkt 的 大小 为 块大小。
        pkt 的 是关键帧 为 真。
        pkt 的 下一个 为 d 的 音频队列头。
        d 的 音频队列头 为 pkt。
    完。
    d 的 已读位置 为 d 的 已读位置 加 块大小。
完。

AI解复用_记录读取 接受 是音频、包大小：
    若 全局解复用AI 等于 空：
        返还。
    完。
    AI 为 全局解复用AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    AI 的 历史平均包大小 为 (AI 的 历史平均包大小 乘 7 加 包大小 乘 1024) 除以 8。
    若 是音频 等于 真：
        AI 的 历史音频包率 为 (AI 的 历史音频包率 乘 15 加 1024) 除以 16。
        AI 的 连续音频读取数 为 AI 的 连续音频读取数 加 1。
        AI 的 连续视频读取数 为 0。
    否则：
        AI 的 历史音频包率 为 AI 的 历史音频包率 乘 15 除以 16。
        AI 的 连续视频读取数 为 AI 的 连续视频读取数 加 1。
        AI 的 连续音频读取数 为 0。
    完。
    若 AI 的 采样计数器 小于 16：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 全局解复用统计 不等于 空：
        全局解复用统计 的 AI解复用调用次数 为 全局解复用统计 的 AI解复用调用次数 加 1。
    完。
    若 AI 的 连续音频读取数 大于 5：
        AI 的 预测下次读音频 为 真。
    否则若 AI 的 连续视频读取数 大于 5：
        AI 的 预测下次读音频 为 假。
    完。
    若 AI 的 预测下次读音频 等于 真 并且 是音频 等于 真：
        若 全局解复用统计 不等于 空：
            全局解复用统计 的 AI解复用命中次数 为 全局解复用统计 的 AI解复用命中次数 加 1。
        完。
    完。
完。

解复用_打印统计 接受 零参：
    若 全局解复用统计 等于 空：
        做 打印 用 「[解复用] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   解复用器 · 统计报告               │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 音频包读取:       」、做 转字符串 用 全局解复用统计 的 音频包读取次数。
    做 打印 用 做 字符串拼接 用 「│ 视频包读取:       」、做 转字符串 用 全局解复用统计 的 视频包读取次数。
    做 打印 用 做 字符串拼接 用 「│ 队列填充:         」、做 转字符串 用 全局解复用统计 的 队列填充次数。
    做 打印 用 做 字符串拼接 用 「│ MP3帧解析:        」、做 转字符串 用 全局解复用统计 的 MP3帧解析次数。
    做 打印 用 做 字符串拼接 用 「│ WAV块读取:        」、做 转字符串 用 全局解复用统计 的 WAV块读取次数。
    做 打印 用 做 字符串拼接 用 「│ MP4sample:        」、做 转字符串 用 全局解复用统计 的 MP4sample读取次数。
    做 打印 用 做 字符串拼接 用 「│ OGG页读取:        」、做 转字符串 用 全局解复用统计 的 OGG页读取次数。
    做 打印 用 做 字符串拼接 用 「│ Seek次数:         」、做 转字符串 用 全局解复用统计 的 Seek次数。
    做 打印 用 做 字符串拼接 用 「│ Seek关键帧:       」、做 转字符串 用 全局解复用统计 的 Seek关键帧命中。
    做 打印 用 做 字符串拼接 用 「│ 包分配:           」、做 转字符串 用 全局解复用统计 的 包分配次数。
    做 打印 用 做 字符串拼接 用 「│ EOF触发:          」、做 转字符串 用 全局解复用统计 的 EOF触发次数。
    做 打印 用 做 字符串拼接 用 「│ AI解复用调用:     」、做 转字符串 用 全局解复用统计 的 AI解复用调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI解复用命中:     」、做 转字符串 用 全局解复用统计 的 AI解复用命中次数。
    做 打印 用 做 字符串拼接 用 「│ 总耗时(μs):       」、做 转字符串 用 全局解复用统计 的 总处理耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 错误:             」、做 转字符串 用 全局解复用统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

解复用_导出诊断 接受 零参：
    若 全局解复用AI 等于 空：
        做 打印 用 「[解复用AI] 未初始化」。
        返还。
    完。
    AI 为 全局解复用AI。
    做 打印 用 「[解复用AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史音频包率(Q10): 」、做 转字符串 用 AI 的 历史音频包率。
    做 打印 用 做 字符串拼接 用 「  历史平均包大小(Q10): 」、做 转字符串 用 AI 的 历史平均包大小。
    做 打印 用 做 字符串拼接 用 「  连续音频读取: 」、做 转字符串 用 AI 的 连续音频读取数。
    做 打印 用 做 字符串拼接 用 「  连续视频读取: 」、做 转字符串 用 AI 的 连续视频读取数。
    做 打印 用 做 字符串拼接 用 「  预测读音频: 」、做 转字符串 用 AI 的 预测下次读音频。
完。

解复用_获取统计 接受 零参：
    返还 全局解复用统计。
完。

解复用_获取AI状态 接受 零参：
    返还 全局解复用AI。
完。

解复用_是否已初始化 接受 零参：
    返还 全局解复用已初始化。
完。

媒体Packet_创建 接受 数据、大小、时间戳、是关键帧：
    pkt 为 做 堆_分配 用 媒体Packet。
    若 pkt 等于 空：
        返还 空。
    完。
    pkt 的 数据 为 数据。
    pkt 的 大小 为 大小。
    pkt 的 时间戳 为 时间戳。
    pkt 的 解码时间 为 时间戳。
    pkt 的 是关键帧 为 是关键帧。
    pkt 的 轨道ID 为 0。
    pkt 的 下一个 为 0。
    若 全局解复用统计 不等于 空：
        全局解复用统计 的 包分配次数 为 全局解复用统计 的 包分配次数 加 1。
    完。
    返还 pkt。
完。

媒体Packet_销毁 接受 pkt：
    若 pkt 等于 空：
        返还。
    完。
    做 堆_释放 用 pkt。
    若 全局解复用统计 不等于 空：
        全局解复用统计 的 包释放次数 为 全局解复用统计 的 包释放次数 加 1。
    完。
完。

媒体Packet_获取时间戳 接受 pkt：
    若 pkt 等于 空：
        返还 0。
    完。
    返还 pkt 的 时间戳。
完。

媒体Packet_获取大小 接受 pkt：
    若 pkt 等于 空：
        返还 0。
    完。
    返还 pkt 的 大小。
完。

媒体Packet_是否关键帧 接受 pkt：
    若 pkt 等于 空：
        返还 假。
    完。
    返还 pkt 的 是关键帧。
完。

Demuxer_获取格式名 接受 格式：
    若 格式 等于 CONTAINER_MP3：
        返还 「MP3」。
    完。
    若 格式 等于 CONTAINER_WAV：
        返还 「WAV」。
    完。
    若 格式 等于 CONTAINER_MP4：
        返还 「MP4」。
    完。
    若 格式 等于 CONTAINER_OGG：
        返还 「OGG」。
    完。
    若 格式 等于 CONTAINER_FLAC：
        返还 「FLAC」。
    完。
    若 格式 等于 CONTAINER_WEBM：
        返还 「WebM」。
    完。
    返还 「未知」。
完。

Demuxer_获取格式 接受 d：
    若 d 等于 空：
        返还 0。
    完。
    返还 d 的 格式。
完。

Demuxer_有音频 接受 d：
    若 d 等于 空：
        返还 假。
    完。
    返还 d 的 有音频。
完。

Demuxer_有视频 接受 d：
    若 d 等于 空：
        返还 假。
    完。
    返还 d 的 有视频。
完。

本章 收束。
