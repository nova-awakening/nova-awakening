《接口》

引用《..·中枢·阶段契约》。

引用《..·中枢·会话桥一体》。

引用《.·阶段装载》。

引用《.·标记标记扩展》。

引用《.·帧管线获取》。

引用《.·AI加载调度》。

引用《.·AI合成层纹理管理》。

引用《.·AI智能渲染》。

引用《.·BMP解码》。

引用《.·CSP安全策略解析》。

引用《.·CSSStyleSheet》。

引用《.·CSS_calc求值》。

引用《.·CSS包含策略》。

引用《.·CSS样式表对象》。

引用《.·CSS滤镜》。

引用《.·CSS网格布局》。

引用《.·CSS选择器AST》。

引用《.·CSS选择器匹配》。

引用《.·Clipboard剪贴板》。

引用《.·Crypto随机值》。

引用《.·DOM变更通知》。

引用《.·DevTools创建》。

引用《.·HTMLInput状态》。

引用《.·HTMLMediaElement元数据》。

引用《.·HTML_Select》。

引用《.·HTML_TextArea》。

引用《.·HTML令牌定义》。

引用《.·HTML实体表》。

引用《.·HTML文本域与选择》。

引用《.·HTML表单控件》。

引用《.·HarfBuzz文字整形》。

引用《.·RTCPeerConnection》。

引用《.·Reporting接口》。

引用《.·ResizeObserverEntry》。

引用《.·ResizeObserver主体》。

引用《.·Sk图像引用计数》。

引用《.·URL解析》。

引用《.·ValidityState验证》。

引用《.·WebCrypto密钥》。

引用《.·WebGL渲染上下文》。

引用《.·WebGL纹理3D》。

引用《.·WebRTC连接Offer》。

引用《.·WritableStream》。

引用《.·中止控制中止》。

引用《.·事件属性处理》。

引用《.·分配》。

引用《.·历史接口位》。

引用《.·原生浏览器》。

引用《.·变更观察器》。

引用《.·合成层管理》。

引用《.·启动序列多》。

引用《.·图像滤镜链》。

引用《.·图处绘制便》。

引用《.·图层创建》。

引用《.·图层管理获》。

引用《.·图绘VAO》。

引用《.·图绘变换反馈》。

引用《.·图绘纹理管线》。

引用《.·如果当前在视口》。

引用《.·媒体流媒体流》。

引用《.·媒体源》。

引用《.·字体扩展编》。

引用《.·字体渲染器》。

引用《.·字体集合项》。

引用《.·字形扫描线光栅化》。

引用《.·对象》。

引用《.·对齐》。

引用《.·导航预加载》。

引用《.·屏幕方向》。

引用《.·崩溃恢复子》。

引用《.·工线》。

引用《.·已注册属性定义》。

引用《.·布局对象》。

引用《.·布局布局》。

引用《.·帧缓冲对象》。

引用《.·弹性布局主》。

引用《.·弹性布局算》。

引用《.·弹性盒布局》。

引用《.·影子创建》。

引用《.·影子文档》。

引用《.·拖放控制器》。

引用《.·控制台接口》。

引用《.·播放控制接口》。

引用《.·数据传输》。

引用《.·数据传输拖》。

引用《.·文字排版引擎》。

引用《.·文本》。

引用《.·文本接口》。

引用《.·无障碍节点》。

引用《.·无障碍访问》。

引用《.·查看接口地》。

引用《.·标记元素》。

引用《.·树构建从文档》。

引用《.·树节点》。

引用《.·样式媒体》。

引用《.·样式属性对象》。

引用《.·样式逻辑属》。

引用《.·核心处理令》。

引用《.·注入防护防》。

引用《.·注视点光栅化》。

引用《.·混合模式》。

引用《.·滚动处理合》。

引用《.·特性检测》。

引用《.·瓦片优先级》。

引用《.·电池感知接口》。

引用《.·画布2D渲染》。

引用《.·画布双创建》。

引用《.·相对地址解析》。

引用《.·着色器操作》。

引用《.·着色编译器》。

引用《.·管道》。

引用《.·索引库光标》。

引用《.·索引库工厂》。

引用《.·结构克序列》。

引用《.·结构化克隆》。

引用《.·绘制像素图》。

引用《.·绘制调用》。

引用《.·网图处计算》。

引用《.·网接口信号裁》。

引用《.·网接口头部》。

引用《.·网格布局主》。

引用《.·网格解析区》。

引用《.·网缓绘制结》。

引用《.·脚本工线内》。

引用《.·自定元素自》。

引用《.·节点语义推断》。

引用《.·获取》。

引用《.·虚拟滚动》。

引用《.·视频解码器》。

引用《.·角色计算标》。

引用《.·资源加载器》。

引用《.·资源加载长》。

引用《.·资源取数资》。

引用《.·路径》。

引用《.·跳过不参与布》。

引用《.·软件注册生》。

引用《.·辅助数学》。

引用《.·输入处理》。

引用《.·违规报告》。

引用《.·选区管理》。

引用《.·选择接口》。

引用《.·透明度》。

引用《.·通用文档操》。

引用《.·采样器对象》。

引用《.·重新计算剩》。

引用《.·音频解码器》。

引用《.·页面生命周期》。

引用《.·项接口屏幕》。

引用《.·预检请求》。

引用《.·默认行为》。

可变 感知_调用计数 为 0。

可变 感知_最近阶段 为 「」。

可变 感知_最近总线消息 为 空。

感知接口_默认文档 接受 零参：
    会话 为 做 一体桥_默认页面会话。
    若 会话 不等于 空 并且 会话 的 文档指针 不等于 0：
        返还 会话 的 文档指针。
    完。
    返还 0。
完。

感知接口_文档根 接受 文档指针：
    若 文档指针 等于 0：
        返还 0。
    完。
    若 文档指针 的 根节点 不等于 0：
        返还 文档指针 的 根节点。
    完。
    返还 文档指针。
完。

感知接口_查询元素 接受 文档指针、选择器：
    根节点 为 做 感知接口_文档根 用 文档指针。
    若 根节点 等于 0：
        返还 0。
    完。
    返还 做 元素_querySelector 用 根节点、选择器。
完。

感知_阶段清单 接受 阶段：
    返还 做 感知阶段装载_清单 用 阶段。
完。

感知_加载阶段 接受 阶段：
    若 非 做 阶段契约_有效 用 阶段：
        返还 负 1。
    完。
    感知_最近阶段 为 阶段。
    返还 做 感知阶段装载_登记 用 阶段。
完。

感知_初始化 接受 零参：
    做 感知_加载阶段 用 阶段_boot。
    做 感知_加载阶段 用 阶段_浏览器外衣。
    做 事件循环_初始化。
    返还 0。
完。

感知_tick 接受 零参：
    返还 做 事件循环_推进一帧。
完。

感知_获取待处理数 接受 零参：
    返还 做 事件循环_待处理数。
完。

感知_处理批量 接受 数量：
    做 事件循环_批量处理 用 数量。
完。

感知_监听事件 接受 事件类型、回调：
    返还 做 事件_注册监听 用 事件类型、回调。
完。

感知_移除监听 接受 事件类型、回调：
    做 事件_移除监听 用 事件类型、回调。
完。

感知_触发事件 接受 事件类型、数据：
    做 事件_触发 用 事件类型、数据。
完。

感知_创建页面会话 接受 名称、网址：
    系统会话ID 为 做 一体桥_确保系统会话 用 「Nova觉醒」、「感知接口」。
    返还 做 一体桥_创建页面会话 用 系统会话ID、名称、网址。
完。

感知_绑定页面文档 接受 页面会话ID、文档指针：
    会话 为 做 一体桥_查页面会话 用 页面会话ID。
    若 会话 等于 空：
        返还 假。
    完。
    返还 做 一体桥_绑定页面资源 用 页面会话ID、会话 的 窗口ID、会话 的 标签ID、文档指针、会话 的 脚本上下文、会话 的 推理上下文。
完。

感知_查询元素 接受 选择器：
    返还 做 感知接口_查询元素 用 做 感知接口_默认文档、选择器。
完。

感知_查询元素_页面 接受 页面会话ID、选择器：
    会话 为 做 一体桥_查页面会话 用 页面会话ID。
    若 会话 等于 空：
        返还 0。
    完。
    做 一体桥_记录动作 用 页面会话ID、「感知_查询元素」。
    结果 为 做 感知接口_查询元素 用 会话 的 文档指针、选择器。
    做 一体桥_生成DOM快照 用 页面会话ID、会话 的 文档指针、「」、0、0。
    返还 结果。
完。

感知_获取文本 接受 元素：
    返还 做 元素_innerText_get 用 元素。
完。

感知_获取文本_页面 接受 页面会话ID、元素：
    做 一体桥_记录动作 用 页面会话ID、「感知_获取文本」。
    返还 做 元素_innerText_get 用 元素。
完。

感知_设置文本 接受 元素、文本：
    做 节点_textContent_set 用 元素、文本。
完。

感知_设置文本_页面 接受 页面会话ID、元素、文本：
    做 一体桥_记录动作 用 页面会话ID、「感知_设置文本」。
    做 节点_textContent_set 用 元素、文本。
完。

感知_当前帧率 接受 零参：
    返还 做 事件循环_统计 的 FPS。
完。

感知_请求重绘 接受 零参：
    做 Frame_标记需要绘制。
完。

感知_请求重绘_页面 接受 页面会话ID：
    做 一体桥_记录动作 用 页面会话ID、「感知_请求重绘」。
    做 Frame_标记需要绘制。
完。

感知_安全入口 接受 调用者、操作码：
    感知_调用计数 为 感知_调用计数 加 1。
    若 调用者 小于 0 或者 调用者 大于 8：
        返还 负 1。
    完。
    返还 0。
完。

感知_处理总线消息 接受 调用者、操作码、数据：
    感知_最近总线消息 为 一本字典：调用者 对应 调用者、操作码 对应 操作码、数据 对应 数据。
    若 操作码 等于 0x100：
        做 感知_请求重绘。
    完。
    返还 0。
完。

感知接口_统计 接受 零参：
    返还 一本字典：模块 对应 「感知接口」、最近阶段 对应 感知_最近阶段、感知调用计数 对应 感知_调用计数、最近总线消息 对应 感知_最近总线消息。
完。

感知_tick_含SOC 接受 零参：
    前处理数 为 做 事件循环_待处理数。
    做 事件循环_推进一帧。
    后处理数 为 做 事件循环_待处理数。
    本帧处理 为 前处理数 减 后处理数。
    若 本帧处理 大于 0：
        做 感知_记录雪崩 用 本帧处理。
    完。
    返还 本帧处理。
完。

本章 收束。
