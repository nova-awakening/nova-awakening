《滚动链控制》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《.·查找最近快》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《.·AI样式推断》。

引用《.·文档节点》。

引用《.·解析快照》。

引用《.·滚动容器创建》。

Overscroll_应该传播 接受 ssc、方向x、方向y：
    若 ssc 等于 0：
        返还 真。
    完。
    若 方向x 不等于 0：
        若 ssc 的 overscroll_x 等于 OVERSCROLL_CONTAIN 或者 ssc 的 overscroll_x 等于 OVERSCROLL_NONE：
            若 方向x 小于 0 并且 ssc 的 滚动x 小于或等于 0：
                返还 假。
            完。
            若 方向x 大于 0 并且 ssc 的 滚动x 大于或等于 ssc 的 内容宽 减 ssc 的 视口宽：
                返还 假。
            完。
        完。
    完。
    若 方向y 不等于 0：
        若 ssc 的 overscroll_y 等于 OVERSCROLL_CONTAIN 或者 ssc 的 overscroll_y 等于 OVERSCROLL_NONE：
            若 方向y 小于 0 并且 ssc 的 滚动y 小于或等于 0：
                返还 假。
            完。
            若 方向y 大于 0 并且 ssc 的 滚动y 大于或等于 ssc 的 内容高 减 ssc 的 视口高：
                返还 假。
            完。
        完。
    完。
    返还 真。
完。

Overscroll_应该弹性 接受 ssc：
    若 ssc 等于 0：
        返还 真。
    完。
    若 ssc 的 overscroll_x 等于 OVERSCROLL_NONE 并且 ssc 的 overscroll_y 等于 OVERSCROLL_NONE：
        返还 假。
    完。
    返还 真。
完。

元素_scrollTo 接受 元素、选项x、选项y、行为：
    ssc 为 做 元素_获取滚动容器 用 元素。
    若 ssc 等于 0：
        返还。
    完。
    可变 目标x 为 选项x。
    可变 目标y 为 选项y。
    若 目标x 小于 0：
        目标x 为 0。
    完。
    若 目标y 小于 0：
        目标y 为 0。
    完。
    若 目标x 大于 ssc 的 内容宽 减 ssc 的 视口宽：
        目标x 为 ssc 的 内容宽 减 ssc 的 视口宽。
    完。
    若 目标y 大于 ssc 的 内容高 减 ssc 的 视口高：
        目标y 为 ssc 的 内容高 减 ssc 的 视口高。
    完。
    若 行为 等于 SCROLL_BEHAVIOR_SMOOTH 或者 ssc 的 滚动行为 等于 SCROLL_BEHAVIOR_SMOOTH：
        做 ScrollSnap_平滑滚动到 用 ssc、目标x、目标y。
    否则：
        ssc 的 滚动x 为 目标x。
        ssc 的 滚动y 为 目标y。
        若 ssc 的 轴 不等于 SNAP_AXIS_NONE：
            做 ScrollSnap_滚动结束 用 ssc。
        完。
    完。
完。

元素_scrollBy 接受 元素、dx、dy、行为：
    ssc 为 做 元素_获取滚动容器 用 元素。
    若 ssc 等于 0：
        返还。
    完。
    做 元素_scrollTo 用 元素、ssc 的 滚动x 加 dx、ssc 的 滚动y 加 dy、行为。
完。

ScrollSnap_平滑滚动到 接受 ssc、目标x、目标y：
    若 ssc 等于 0：
        返还。
    完。
    ssc 的 目标x 为 目标x。
    ssc 的 目标y 为 目标y。
    若 ssc 的 snap上下文 不等于 0：
        ssc 的 snap上下文 的 当前滚动x 为 ssc 的 滚动x。
        ssc 的 snap上下文 的 当前滚动y 为 ssc 的 滚动y。
        做 ScrollSnap_动画到 用 ssc 的 snap上下文、ssc 的 元素、目标x、目标y、ssc 的 动画持续时间。
    完。
    ssc 的 滚动x 为 目标x。
    ssc 的 滚动y 为 目标y。
    做 元素_设置scrollLeft 用 ssc 的 元素、目标x。
    做 元素_设置scrollTop 用 ssc 的 元素、目标y。
完。

ScrollSnap_滚动结束 接受 ssc：
    若 ssc 等于 0 或者 ssc 的 snap上下文 等于 0：
        返还。
    完。
    做 ScrollSnap_处理滚动结束 用 ssc 的 snap上下文、ssc 的 元素、ssc 的 滚动x、ssc 的 滚动y、ssc 的 惯性vx、ssc 的 惯性vy。
    ssc 的 滚动x 为 ssc 的 snap上下文 的 当前滚动x。
    ssc 的 滚动y 为 ssc 的 snap上下文 的 当前滚动y。
    做 元素_设置scrollLeft 用 ssc 的 元素、ssc 的 滚动x。
    做 元素_设置scrollTop 用 ssc 的 元素、ssc 的 滚动y。
完。

滚动容器_是否可滚动 接受 元素：
    若 元素 等于 0：
        返还 假。
    完。
    扩展 为 做 元素_获取扩展 用 元素。
    若 扩展 等于 0：
        返还 假。
    完。
    若 扩展 的 滚动容器：
        返还 真。
    完。
    若 做 元素_scrollHeight 用 元素 大于 做 元素_clientHeight 用 元素：
        返还 真。
    完。
    若 做 元素_scrollWidth 用 元素 大于 做 元素_clientWidth 用 元素：
        返还 真。
    完。
    若 元素 的 标签名 等于 「body」 或者 元素 的 标签名 等于 「html」：
        返还 真。
    完。
    返还 假。
完。

滚动容器_同步状态 接受 ssc、元素：
    若 ssc 等于 0 或者 元素 等于 0：
        返还 0。
    完。
    扩展 为 做 元素_获取扩展 用 元素。
    若 扩展 等于 0：
        返还 0。
    完。
    ssc 的 元素 为 元素。
    ssc 的 内容宽 为 做 元素_scrollWidth 用 元素。
    ssc 的 内容高 为 做 元素_scrollHeight 用 元素。
    ssc 的 视口宽 为 做 元素_clientWidth 用 元素。
    ssc 的 视口高 为 做 元素_clientHeight 用 元素。
    ssc 的 滚动x 为 做 元素_scrollLeft 用 元素。
    ssc 的 滚动y 为 做 元素_scrollTop 用 元素。
    若 ssc 的 动画持续时间 等于 0：
        ssc 的 动画持续时间 为 300。
    完。
    若 ssc 的 snap上下文 等于 0：
        ssc 的 snap上下文 为 做 ScrollSnap_创建上下文 用 ssc 的 视口宽、ssc 的 视口高。
    完。
    若 ssc 的 snap上下文 不等于 0：
        做 ScrollSnap_从样式初始化 用 ssc 的 snap上下文、扩展。
        做 ScrollSnap_重建并排序 用 ssc 的 snap上下文、元素、ssc 的 内容宽 减 ssc 的 视口宽、ssc 的 内容高 减 ssc 的 视口高。
        ssc 的 轴 为 ssc 的 snap上下文 的 轴。
        ssc 的 严格度 为 ssc 的 snap上下文 的 严格。
    完。
    返还 ssc。
完。

元素_获取滚动容器 接受 元素：
    若 元素 等于 0：
        返还 0。
    完。
    若 非 做 滚动容器_是否可滚动 用 元素：
        返还 0。
    完。
    扩展 为 做 元素_获取扩展 用 元素。
    若 扩展 等于 0：
        返还 0。
    完。
    可变 ssc 为 扩展 的 滚动容器状态。
    若 ssc 等于 0：
        ssc 为 做 ScrollSnapContainer_创建。
        扩展 的 滚动容器状态 为 ssc。
        扩展 的 滚动容器 为 真。
    完。
    返还 做 滚动容器_同步状态 用 ssc、元素。
完。

智能_CSSScrollSnap_AI样式意图 接受 属性ID、值：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    返还 0。
完。

智能_CSSScrollSnap_AI影响分析 接受 属性ID：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 2。
    完。
    返还 做 AI样式_分析影响 用 属性ID。
完。

OVERSCROLL_AUTO 为 0。

OVERSCROLL_CONTAIN 为 1。

OVERSCROLL_NONE 为 2。

SCROLL_BEHAVIOR_AUTO 为 0。

SCROLL_BEHAVIOR_SMOOTH 为 1。

SNAP_AXIS_NONE 为 0。

SNAP_AXIS_X 为 1。

SNAP_AXIS_Y 为 2。

SNAP_AXIS_BOTH 为 3。

橡皮筋刚度 为 80。

橡皮筋阻尼 为 40。

橡皮筋最大拉伸 为 200。

惯性摩擦系数 为 15。

惯性最小速度 为 2。

可变 全局NOSC统计 为 新建 NOSC统计结构。

NOSC统计结构 是 一种 事物，含 滚动事件总数、过度滚动次数、橡皮筋触发次数、惯性滚动次数、链传播次数、AI推理次数。

橡皮筋状态 是 一种 事物，含 位移、速度、活动、方向。

橡皮筋_创建 接受 方向：
    rb 为 做 堆_分配 用 橡皮筋状态。
    若 rb 等于 0：
        返还 空。
    完。
    rb 的 位移 为 0。
    rb 的 速度 为 0。
    rb 的 活动 为 0。
    rb 的 方向 为 方向。
    返还 rb。
完。

橡皮筋_拉伸 接受 rb、增量：
    若 rb 等于 空：
        返还。
    完。
    rb 的 活动 为 1。
    可变 绝对位移 为 rb 的 位移。
    若 绝对位移 小于 0：
        绝对位移 为 0 减 绝对位移。
    完。
    比例 为 100 减 绝对位移 乘 100 除以 橡皮筋最大拉伸。
    若 比例 小于 10：
        比例 为 10。
    完。
    实际增量 为 增量 乘 比例 除以 100。
    rb 的 位移 为 rb 的 位移 加 实际增量。
    若 rb 的 位移 大于 橡皮筋最大拉伸：
        rb 的 位移 为 橡皮筋最大拉伸。
    完。
    若 rb 的 位移 小于 0 减 橡皮筋最大拉伸：
        rb 的 位移 为 0 减 橡皮筋最大拉伸。
    完。
    全局NOSC统计 的 橡皮筋触发次数 为 全局NOSC统计 的 橡皮筋触发次数 加 1。
完。

橡皮筋_释放 接受 rb：
    若 rb 等于 空：
        返还。
    完。
    若 rb 的 位移 不等于 0：
        rb 的 活动 为 2。
        rb 的 速度 为 0。
    否则：
        rb 的 活动 为 0。
    完。
完。

橡皮筋_更新 接受 rb、dt_ms：
    若 rb 等于 空 或者 rb 的 活动 不等于 2：
        返还 0。
    完。
    弹力 为 0 减 橡皮筋刚度 乘 rb 的 位移 除以 256。
    阻力 为 0 减 橡皮筋阻尼 乘 rb 的 速度 除以 256。
    加速度 为 弹力 加 阻力。
    rb 的 速度 为 rb 的 速度 加 加速度 乘 dt_ms 除以 16。
    rb 的 位移 为 rb 的 位移 加 rb 的 速度 乘 dt_ms 除以 16。
    可变 abs_pos 为 rb 的 位移。
    若 abs_pos 小于 0：
        abs_pos 为 0 减 abs_pos。
    完。
    可变 abs_vel 为 rb 的 速度。
    若 abs_vel 小于 0：
        abs_vel 为 0 减 abs_vel。
    完。
    若 abs_pos 小于 1 并且 abs_vel 小于 1：
        rb 的 位移 为 0。
        rb 的 速度 为 0。
        rb 的 活动 为 0。
        返还 0。
    完。
    返还 1。
完。

惯性状态 是 一种 事物，含 速度x、速度y、活动。

惯性_启动 接受 ssc、vx、vy：
    若 ssc 等于 0：
        返还。
    完。
    ssc 的 惯性_vx 为 vx。
    ssc 的 惯性_vy 为 vy。
    ssc 的 惯性_活动 为 1。
    全局NOSC统计 的 惯性滚动次数 为 全局NOSC统计 的 惯性滚动次数 加 1。
完。

惯性_更新 接受 ssc：
    若 ssc 等于 0 或者 ssc 的 惯性_活动 等于 0：
        返还 0。
    完。
    ssc 的 惯性_vx 为 ssc 的 惯性_vx 乘 (100 减 惯性摩擦系数) 除以 100。
    ssc 的 惯性_vy 为 ssc 的 惯性_vy 乘 (100 减 惯性摩擦系数) 除以 100。
    可变 avx 为 ssc 的 惯性_vx。
    可变 avy 为 ssc 的 惯性_vy。
    若 avx 小于 0：
        avx 为 0 减 avx。
    完。
    若 avy 小于 0：
        avy 为 0 减 avy。
    完。
    若 avx 小于 惯性最小速度 并且 avy 小于 惯性最小速度：
        ssc 的 惯性_vx 为 0。
        ssc 的 惯性_vy 为 0。
        ssc 的 惯性_活动 为 0。
        返还 0。
    完。
    新x 为 ssc 的 滚动x 加 ssc 的 惯性_vx。
    新y 为 ssc 的 滚动y 加 ssc 的 惯性_vy。
    可变 超出 为 0。
    若 新x 小于 0：
        新x 为 0。
        超出 为 1。
        ssc 的 惯性_vx 为 0。
    完。
    若 新y 小于 0：
        新y 为 0。
        超出 为 1。
        ssc 的 惯性_vy 为 0。
    完。
    最大x 为 ssc 的 内容宽 减 ssc 的 视口宽。
    最大y 为 ssc 的 内容高 减 ssc 的 视口高。
    若 新x 大于 最大x：
        新x 为 最大x。
        超出 为 1。
        ssc 的 惯性_vx 为 0。
    完。
    若 新y 大于 最大y：
        新y 为 最大y。
        超出 为 1。
        ssc 的 惯性_vy 为 0。
    完。
    ssc 的 滚动x 为 新x。
    ssc 的 滚动y 为 新y。
    若 超出 等于 1：
        全局NOSC统计 的 过度滚动次数 为 全局NOSC统计 的 过度滚动次数 加 1。
    完。
    返还 1。
完。

惯性_停止 接受 ssc：
    若 ssc 等于 0：
        返还。
    完。
    ssc 的 惯性_vx 为 0。
    ssc 的 惯性_vy 为 0。
    ssc 的 惯性_活动 为 0。
完。

触摸历史最大 为 8。

触摸点 是 一种 事物，含 x、y、时间戳。

触摸跟踪器 是 一种 事物，含 历史列表、历史数、开始x、开始y、开始时间。

可变 全局触摸跟踪 为 空。

触摸跟踪_初始化 接受 零参：
    全局触摸跟踪 为 做 堆_分配 用 触摸跟踪器。
    若 全局触摸跟踪 等于 0：
        返还。
    完。
    全局触摸跟踪 的 历史列表 为 做 堆_分配 用 触摸历史最大 乘 8。
    全局触摸跟踪 的 历史数 为 0。
完。

触摸跟踪_开始 接受 x、y、时间：
    若 全局触摸跟踪 等于 空：
        返还。
    完。
    全局触摸跟踪 的 开始x 为 x。
    全局触摸跟踪 的 开始y 为 y。
    全局触摸跟踪 的 开始时间 为 时间。
    全局触摸跟踪 的 历史数 为 0。
完。

触摸跟踪_移动 接受 x、y、时间：
    若 全局触摸跟踪 等于 空：
        返还。
    完。
    若 全局触摸跟踪 的 历史数 小于 触摸历史最大：
        pt 为 做 堆_分配 用 触摸点。
        若 pt 不等于 0：
            pt 的 x 为 x。
            pt 的 y 为 y。
            pt 的 时间戳 为 时间。
            做 内存_写64 用 全局触摸跟踪 的 历史列表 加 全局触摸跟踪 的 历史数 乘 8、pt。
            全局触摸跟踪 的 历史数 为 全局触摸跟踪 的 历史数 加 1。
        完。
    完。
完。

触摸跟踪_计算速度 接受 零参：
    若 全局触摸跟踪 等于 空 或者 全局触摸跟踪 的 历史数 小于 2：
        返还 一本字典：vx 对应 0、vy 对应 0。
    完。
    n 为 全局触摸跟踪 的 历史数。
    p1 为 做 内存_读64 用 全局触摸跟踪 的 历史列表 加 (n 减 2) 乘 8。
    p2 为 做 内存_读64 用 全局触摸跟踪 的 历史列表 加 (n 减 1) 乘 8。
    若 p1 等于 0 或者 p2 等于 0：
        返还 一本字典：vx 对应 0、vy 对应 0。
    完。
    dt 为 p2 的 时间戳 减 p1 的 时间戳。
    若 dt 等于 0：
        dt 为 1。
    完。
    vx 为 (p2 的 x 减 p1 的 x) 乘 16 除以 dt。
    vy 为 (p2 的 y 减 p1 的 y) 乘 16 除以 dt。
    返还 一本字典：vx 对应 vx、vy 对应 vy。
完。

AI滚动_预测意图 接受 ssc：
    若 ssc 等于 0：
        返还 0。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    feat 为 做 Tensor_创建 用 6。
    做 Tensor_写 用 feat、0、ssc 的 滚动x。
    做 Tensor_写 用 feat、1、ssc 的 滚动y。
    做 Tensor_写 用 feat、2、ssc 的 惯性_vx。
    做 Tensor_写 用 feat、3、ssc 的 惯性_vy。
    做 Tensor_写 用 feat、4、ssc 的 内容高。
    做 Tensor_写 用 feat、5、ssc 的 视口高。
    结果 为 做 AI总线_推理 用 bus、feat。
    做 Tensor_释放 用 feat。
    全局NOSC统计 的 AI推理次数 为 全局NOSC统计 的 AI推理次数 加 1。
    返还 结果。
完。

AI滚动_自适应参数 接受 ssc、意图：
    若 意图 等于 1：
        ssc 的 摩擦系数 为 5。
    否则若 意图 等于 2：
        做 元素_scrollTo 用 ssc 的 元素、0、0、SCROLL_BEHAVIOR_SMOOTH。
    否则若 意图 等于 3：
        ssc 的 摩擦系数 为 35。
    否则：
        ssc 的 摩擦系数 为 惯性摩擦系数。
    完。
完。

滚动链_查找父容器 接受 ssc：
    若 ssc 等于 0：
        返还 空。
    完。
    父 为 ssc 的 父元素。
    可变 深度 为 0。
    只要 父 不等于 空 并且 深度 小于 32：
        父ssc 为 做 元素_获取滚动容器 用 父。
        若 父ssc 不等于 0：
            返还 父ssc。
        完。
        父 为 父 的 父元素。
        深度 为 深度 加 1。
    完。
    返还 空。
完。

滚动链_传播 接受 ssc、dx、dy：
    若 ssc 等于 0：
        返还。
    完。
    方向x 为 0。
    方向y 为 0。
    若 dx 大于 0：
        方向x 为 1。
    否则若 dx 小于 0：
        方向x 为 负 1。
    完。
    若 dy 大于 0：
        方向y 为 1。
    否则若 dy 小于 0：
        方向y 为 负 1。
    完。
    若 做 Overscroll_应该传播 用 ssc、方向x、方向y 等于 假：
        返还。
    完。
    父 为 做 滚动链_查找父容器 用 ssc。
    若 父 不等于 空：
        做 元素_scrollBy 用 父 的 元素、dx、dy、SCROLL_BEHAVIOR_AUTO。
        全局NOSC统计 的 链传播次数 为 全局NOSC统计 的 链传播次数 加 1。
    完。
完。

恢复槽最大 为 32。

滚动恢复项 是 一种 事物，含 页面哈希、滚动x、滚动y。

可变 滚动恢复槽 为 空。

可变 滚动恢复数 为 0。

滚动恢复_初始化 接受 零参：
    滚动恢复槽 为 做 堆_分配 用 恢复槽最大 乘 8。
    若 滚动恢复槽 不等于 0：
        做 内存_置零 用 滚动恢复槽、恢复槽最大 乘 8。
    完。
    滚动恢复数 为 0。
完。

滚动恢复_保存 接受 页面哈希、x、y：
    若 滚动恢复槽 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 滚动恢复数：
        e 为 做 内存_读64 用 滚动恢复槽 加 i 乘 8。
        若 e 不等于 0 并且 e 的 页面哈希 等于 页面哈希：
            e 的 滚动x 为 x。
            e 的 滚动y 为 y。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 滚动恢复数 大于或等于 恢复槽最大：
        返还。
    完。
    entry 为 做 堆_分配 用 滚动恢复项。
    若 entry 等于 0：
        返还。
    完。
    entry 的 页面哈希 为 页面哈希。
    entry 的 滚动x 为 x。
    entry 的 滚动y 为 y。
    做 内存_写64 用 滚动恢复槽 加 滚动恢复数 乘 8、entry。
    滚动恢复数 为 滚动恢复数 加 1。
完。

滚动恢复_加载 接受 页面哈希、ssc：
    若 滚动恢复槽 等于 空 或者 ssc 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 滚动恢复数：
        e 为 做 内存_读64 用 滚动恢复槽 加 i 乘 8。
        若 e 不等于 0 并且 e 的 页面哈希 等于 页面哈希：
            做 元素_scrollTo 用 ssc 的 元素、e 的 滚动x、e 的 滚动y、SCROLL_BEHAVIOR_AUTO。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

NOSC_初始化 接受 零参：
    做 内核打印 用 「[NOSC] Nova过度滚动链智能 启动」。
    做 触摸跟踪_初始化。
    做 滚动恢复_初始化。
    全局NOSC统计 的 滚动事件总数 为 0。
    全局NOSC统计 的 过度滚动次数 为 0。
    全局NOSC统计 的 橡皮筋触发次数 为 0。
    全局NOSC统计 的 惯性滚动次数 为 0。
    全局NOSC统计 的 链传播次数 为 0。
    全局NOSC统计 的 AI推理次数 为 0。
    做 内核打印 用 「[NOSC] 初始化完成 — 橡皮筋/惯性/触摸/链传播/恢复/AI预测 就绪」。
完。

NOSC_打印诊断 接受 零参：
    做 内核打印 用 「╔════════════════════════════════════════════╗」。
    做 内核打印 用 「║  NOSC 过度滚动链智能 诊断报告             ║」。
    做 内核打印 用 「╠════════════════════════════════════════════╣」。
    做 内核打印 用 「║ 滚动事件:     {}」、全局NOSC统计 的 滚动事件总数。
    做 内核打印 用 「║ 过度滚动:     {}」、全局NOSC统计 的 过度滚动次数。
    做 内核打印 用 「║ 橡皮筋触发:   {}」、全局NOSC统计 的 橡皮筋触发次数。
    做 内核打印 用 「║ 惯性滚动:     {}」、全局NOSC统计 的 惯性滚动次数。
    做 内核打印 用 「║ 链传播:       {}」、全局NOSC统计 的 链传播次数。
    做 内核打印 用 「║ AI推理:       {}」、全局NOSC统计 的 AI推理次数。
    做 内核打印 用 「╚════════════════════════════════════════════╝」。
完。

本章 收束。
