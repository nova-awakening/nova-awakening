《屏幕方向》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·执行·平台时间》。

引用《.·内核直连层》。

ScreenOrientation 是 一种 事物，含 类型、角度、onchange。

可变 全局屏幕方向 为 0。

ScreenOrientation_获取 接受 零参：
    若 全局屏幕方向 等于 0：
        全局屏幕方向 为 做 堆_分配 用 ScreenOrientation。
        全局屏幕方向 的 类型 为 「portrait-primary」。
        全局屏幕方向 的 角度 为 0。
        全局屏幕方向 的 onchange 为 0。
    完。
    返还 全局屏幕方向。
完。

ScreenOrientation_lock 接受 类型：
    若 类型 等于 0：
        返还。
    完。
    若 做 Document_fullscreen元素 等于 0：
        返还。
    完。
    so 为 做 ScreenOrientation_获取。
    若 so 等于 0：
        返还。
    完。
    so 的 锁定类型 为 类型。
    so 的 已锁定 为 真。
    做 平台_设置屏幕方向 用 类型。
完。

ScreenOrientation_unlock 接受 零参：
    so 为 做 ScreenOrientation_获取。
    若 so 等于 0：
        返还。
    完。
    so 的 已锁定 为 假。
    so 的 锁定类型 为 0。
    做 平台_重置屏幕方向。
完。

BatteryManager 是 一种 事物，含 charging、chargingTime、dischargingTime、level、onchargingchange、onchargingtimechange、ondischargingtimechange、onlevelchange。

可变 全局电池 为 0。

Battery_获取 接受 零参：
    若 全局电池 等于 0：
        全局电池 为 做 堆_分配 用 BatteryManager。
        全局电池 的 charging 为 真。
        全局电池 的 chargingTime 为 0。
        全局电池 的 dischargingTime 为 0xFFFFFFFF。
        全局电池 的 level 为 1024。
    完。
    返还 全局电池。
完。

Navigator_vibrate 接受 模式、长度：
    若 模式 等于 0 或者 长度 等于 0：
        返还 假。
    完。
    返还 真。
完。

GamepadButton 是 一种 事物，含 pressed、touched、value。

Gamepad 是 一种 事物，含 id、index、connected、mapping、timestamp、buttons、buttons数、axes、axes数、vibrationActuator。

可变 全局手柄 为 一组：4。

u64。

可变 手柄数 为 0。

Gamepad_获取列表 接受 零参：
    返还 全局手柄。
完。

Gamepad_连接 接受 id、index、buttons数、axes数：
    若 index 大于或等于 4：
        返还。
    完。
    gp 为 做 堆_分配 用 Gamepad。
    gp 的 id 为 id。
    gp 的 index 为 index。
    gp 的 connected 为 真。
    gp 的 mapping 为 「standard」。
    gp 的 timestamp 为 做 平台_获取高精度时间。
    gp 的 buttons数 为 buttons数。
    gp 的 axes数 为 axes数。
    可变 i 为 0。
    只要 i 小于 17：
        gp 的 buttons 的 第 i 项 的 pressed 为 假。
        gp 的 buttons 的 第 i 项 的 value 为 0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 8：
        gp 的 axes 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    全局手柄 的 第 index 项 为 gp。
    若 index 大于或等于 手柄数：
        手柄数 为 index 加 1。
    完。
完。

Gamepad_断开 接受 index：
    若 index 大于或等于 4 或者 全局手柄 的 第 index 项 等于 0：
        返还。
    完。
    gp 为 全局手柄 的 第 index 项。
    gp 的 connected 为 假。
完。

Gamepad_更新 接受 index、buttons、axes：
    若 index 大于或等于 4 或者 全局手柄 的 第 index 项 等于 0：
        返还。
    完。
    gp 为 全局手柄 的 第 index 项。
    gp 的 timestamp 为 做 平台_获取高精度时间。
完。

地理位置 是 一种 事物，含 纬度、经度、精度、海拔、海拔精度、方向、速度、时间戳。

可变 最近位置 为 0。

可变 位置监控ID 为 0。

Geolocation_getCurrentPosition 接受 成功回调、错误回调：
    pos 为 新建 地理位置。
    pos 的 纬度 为 0。
    pos 的 经度 为 0。
    pos 的 精度 为 100。
    pos 的 海拔 为 0。
    pos 的 时间戳 为 做 平台_获取高精度时间。
    最近位置 为 pos。
    若 成功回调 不等于 0：
        做 成功回调 用 pos。
    完。
完。

Geolocation_watchPosition 接受 成功回调：
    位置监控ID 为 位置监控ID 加 1。
    返还 位置监控ID。
完。

Geolocation_clearWatch 接受 id：
    若 id 小于或等于 0：
        返还。
    完。
完。

权限_已授权 为 0。

权限_已拒绝 为 1。

权限_询问中 为 2。

Permissions_query 接受 权限名：
    若 做 字符串_相等 用 权限名、「geolocation」：
        返还 权限_询问中。
    完。
    若 做 字符串_相等 用 权限名、「notifications」：
        返还 权限_询问中。
    完。
    若 做 字符串_相等 用 权限名、「camera」：
        返还 权限_已拒绝。
    完。
    若 做 字符串_相等 用 权限名、「microphone」：
        返还 权限_已拒绝。
    完。
    若 做 字符串_相等 用 权限名、「clipboard-read」：
        返还 权限_询问中。
    完。
    若 做 字符串_相等 用 权限名、「clipboard-write」：
        返还 权限_已授权。
    完。
    返还 权限_已拒绝。
完。

权限_状态名 接受 状态：
    若 状态 等于 权限_已授权：
        返还 「granted」。
    完。
    若 状态 等于 权限_已拒绝：
        返还 「denied」。
    完。
    若 状态 等于 权限_询问中：
        返还 「prompt」。
    完。
    返还 「unknown」。
完。

可变 剪贴板_文本 为 0。

可变 剪贴板_写次数 为 0。

可变 剪贴板_读次数 为 0。

Clipboard_writeText 接受 文本：
    剪贴板_文本 为 文本。
    剪贴板_写次数 为 剪贴板_写次数 加 1。
完。

Clipboard_readText 接受 零参：
    剪贴板_读次数 为 剪贴板_读次数 加 1。
    返还 剪贴板_文本。
完。

通知 是 一种 事物，含 标题、正文、图标、标签、时间戳。

可变 通知_权限 为 权限_询问中。

可变 通知_总数 为 0。

Notification_requestPermission 接受 零参：
    通知_权限 为 权限_已授权。
    返还 通知_权限。
完。

Notification_创建 接受 标题、正文、图标：
    若 通知_权限 不等于 权限_已授权：
        返还 0。
    完。
    n 为 新建 通知。
    n 的 标题 为 标题。
    n 的 正文 为 正文。
    n 的 图标 为 图标。
    n 的 时间戳 为 做 平台_获取高精度时间。
    通知_总数 为 通知_总数 加 1。
    返还 n。
完。

设备接口_统计 接受 零参：
    返还 一本字典：手柄数 对应 手柄数、屏幕方向 对应 做 ScreenOrientation_获取 的 类型、屏幕角度 对应 做 ScreenOrientation_获取 的 角度、电池电量 对应 做 Battery_获取 的 level、充电中 对应 做 Battery_获取 的 charging、剪贴板写 对应 剪贴板_写次数、剪贴板读 对应 剪贴板_读次数、通知总数 对应 通知_总数。
完。

设备接口_健康检查 接受 零参：
    返还 「设备接口子系统健康」。
完。

设备接口_完整统计 接受 零参：
    返还 一组：手柄数、剪贴板_写次数、剪贴板_读次数、通知_总数。
完。

字符串_相等 接受 a、b：
    若 a 等于 b：
        返还 真。
    完。
    若 a 等于 0 或者 b 等于 0：
        返还 假。
    完。
    la 为 做 字节长度 用 a。
    lb 为 做 字节长度 用 b。
    若 la 不等于 lb：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 la：
        若 做 内存_读8 用 a 加 i 不等于 做 内存_读8 用 b 加 i：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

平台_设置屏幕方向 接受 类型：
    若 类型 等于 0：
        全局_屏幕方向锁 为 假。
    否则：
        全局_屏幕方向锁 为 真。
        全局_屏幕方向 为 类型。
    完。
完。

平台_重置屏幕方向 接受 零参：
    全局_屏幕方向锁 为 假。
完。

Document_fullscreen元素 接受 零参：
    返还 全局_fullscreen元素。
完。

可变 项_热节点缓存 为 0。

项_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        项_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

项_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

项_统计 接受 零参：
    返还 一本字典：剪贴板写次数 对应 剪贴板_写次数、剪贴板读次数 对应 剪贴板_读次数、通知总数 对应 通知_总数。
完。

项_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 剪贴板_写次数 大于 100000：
        做 追加 用 问题、「剪贴板_写次数异常增长」。
    完。
    若 剪贴板_读次数 大于 100000：
        做 追加 用 问题、「剪贴板_读次数异常增长」。
    完。
    若 通知_总数 大于 100000：
        做 追加 用 问题、「通知_总数异常增长」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

项_综合报告 接受 零参：
    做 打印 用 「[项] ═══ 综合报告 ═══」。
    做 打印 用 「  剪贴板_写次数: 」 加 剪贴板_写次数。
    做 打印 用 「  剪贴板_读次数: 」 加 剪贴板_读次数。
    做 打印 用 「  通知_总数: 」 加 通知_总数。
    做 打印 用 「  健康: 」 加 若 做 项_健康评估 则 「正常」 否则 「异常」。
完。

项_全局重置 接受 零参：
    剪贴板_写次数 为 0。
    剪贴板_读次数 为 0。
    通知_总数 为 0。
    做 打印 用 「[项] 统计已重置」。
    返还 0。
完。

本章 收束。
