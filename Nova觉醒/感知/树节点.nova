《树节点》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

显示_全部 为 0xFFFFFFFF。

显示_元素 为 0x01。

显示_文本 为 0x04。

显示_注释 为 0x80。

显示_文档 为 0x100。

过滤_接受 为 1。

过滤_拒绝 为 2。

过滤_跳过 为 3。

TreeWalker 是 一种 事物，含 根、当前节点、显示掩码、过滤器。

TreeWalker_创建 接受 根、显示掩码、过滤器：
    tw 为 做 堆_分配 用 TreeWalker。
    tw 的 根 为 根。
    tw 的 当前节点 为 根。
    tw 的 显示掩码 为 显示掩码。
    tw 的 过滤器 为 过滤器。
    返还 tw。
完。

TreeWalker_next节点 接受 tw：
    可变 节点 为 tw 的 当前节点。
    若 节点 的 第一子节点 不等于 0：
        节点 为 节点 的 第一子节点。
    否则：
        只要 节点 不等于 0 并且 节点 不等于 tw 的 根：
            若 节点 的 后一兄弟 不等于 0：
                节点 为 节点 的 后一兄弟。
                跳出。
            完。
            节点 为 节点 的 父节点。
        完。
        若 节点 等于 tw 的 根：
            返还 0。
        完。
    完。
    只要 节点 不等于 0：
        若 做 TreeWalker_接受 用 tw、节点：
            tw 的 当前节点 为 节点。
            返还 节点。
        完。
        若 节点 的 第一子节点 不等于 0：
            节点 为 节点 的 第一子节点。
        否则：
            只要 节点 不等于 0 并且 节点 不等于 tw 的 根：
                若 节点 的 后一兄弟 不等于 0：
                    节点 为 节点 的 后一兄弟。
                    跳出。
                完。
                节点 为 节点 的 父节点。
            完。
            若 节点 等于 tw 的 根：
                返还 0。
            完。
        完。
    完。
    返还 0。
完。

TreeWalker_previous节点 接受 tw：
    可变 节点 为 tw 的 当前节点。
    若 节点 等于 tw 的 根：
        返还 0。
    完。
    若 节点 的 前一兄弟 不等于 0：
        节点 为 节点 的 前一兄弟。
        只要 节点 的 最后子节点 不等于 0：
            节点 为 节点 的 最后子节点。
        完。
    否则：
        节点 为 节点 的 父节点。
        若 节点 等于 tw 的 根：
            返还 0。
        完。
    完。
    若 节点 不等于 0 并且 做 TreeWalker_接受 用 tw、节点：
        tw 的 当前节点 为 节点。
        返还 节点。
    完。
    返还 0。
完。

TreeWalker_parent节点 接受 tw：
    节点 为 tw 的 当前节点 的 父节点。
    若 节点 等于 0 或者 节点 等于 tw 的 根：
        返还 0。
    完。
    若 做 TreeWalker_接受 用 tw、节点：
        tw 的 当前节点 为 节点。
        返还 节点。
    完。
    返还 0。
完。

TreeWalker_firstChild 接受 tw：
    可变 子 为 tw 的 当前节点 的 第一子节点。
    只要 子 不等于 0：
        若 做 TreeWalker_接受 用 tw、子：
            tw 的 当前节点 为 子。
            返还 子。
        完。
        子 为 子 的 后一兄弟。
    完。
    返还 0。
完。

TreeWalker_接受 接受 tw、节点：
    若 节点 等于 0：
        返还 假。
    完。
    节点位 为 1 左移 节点 的 节点类型 减 1。
    若 非 (tw 的 显示掩码 位与 节点位)：
        返还 假。
    完。
    若 tw 的 过滤器 不等于 0：
        结果 为 做 调用 用 tw 的 过滤器、节点。
        返还 结果 等于 过滤_接受。
    完。
    返还 真。
完。

本章 收束。
