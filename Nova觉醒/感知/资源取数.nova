《资源取数》

引用《..·中枢·进程信号》。

引用《..·外联·网络子系统入口》。

引用《..·外联·TCP会话记录》。

引用《.·标记语言请》。

引用《.·解包》。

引用《.·安全策主机》。

引用《..·外联·URL解析辅助》。

引用《.·请求对象》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

引用《.·原子驻留》。

引用《.·HTTP头部管理》。

引用《.·取数Body接口》。

引用《.·取数网络请》。

引用《.·项解析》。

引用《.·解码器创建》。

引用《.·内核直连层》。

引用《.·兼容桥》。

引用《.·长连接帧》。

引用《.·资源加载资源》。

引用《.·Fetch_Headers》。

引用《.·Origin从URL》。

引用《..·外联·WebSocket长连接》。

引用《.·ResourceResponse》。

引用《..·中枢·辅助存根》。

引用《..·执行·控制台输出》。

引用《..·执行·平台时间》。

取数档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、头部数、请求数、响应数、解码数、调度数、长连数、成功数、失败数。

取数请求单 是 一种 事物，含 动作、目标、参数、选项。

取数回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

取数摘要 是 一种 事物，含 名称、头部数、请求数、响应数、解码数、调度数、长连数、成功数、失败数、健康。

可变 取数账本 为 一组：（空）。

可变 取数构建数 为 0。

可变 取数总动作 为 0。

可变 取数总成功 为 0。

可变 取数总失败 为 0。

资源取数_建档 接受 名称：
    档 为 新建 取数档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 头部数 为 0。
    档 的 请求数 为 0。
    档 的 响应数 为 0。
    档 的 解码数 为 0。
    档 的 调度数 为 0。
    档 的 长连数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    返还 档。
完。

资源取数_登记 接受 档：
    做 追加 用 取数账本、档。
    取数构建数 为 取数构建数 加 1。
    返还 档。
完。

资源取数_完整统计 接受 零参：
    返还 一组：取数构建数、取数总动作、取数总成功、取数总失败。
完。

资源取数_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 取数回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

资源取数_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 请求数 大于 0 或者 档 的 解码数 大于 0 或者 档 的 长连数 大于 0：
        档 的 最近健康 为 「运行」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「空载」。
    返还 档 的 最近健康。
完。

资源取数_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    档 的 最近错误 为 「」。
    档 的 最近统计 为 做 资源取数_完整统计。
完。

资源取数_成功 接受 档、来源、数据：
    取数总动作 为 取数总动作 加 1。
    取数总成功 为 取数总成功 加 1。
    若 档 等于 空：
        返还 做 资源取数_建回证 用 真、真、来源、「」、数据、做 资源取数_完整统计。
    完。
    档 的 最近来源 为 来源。
    档 的 最近结果 为 数据。
    档 的 最近错误 为 「」。
    档 的 成功数 为 档 的 成功数 加 1。
    档 的 最近统计 为 做 资源取数_完整统计。
    做 资源取数_刷新健康 用 档。
    返还 做 资源取数_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

资源取数_失败 接受 档、来源、错因：
    取数总动作 为 取数总动作 加 1。
    取数总失败 为 取数总失败 加 1。
    若 档 等于 空：
        返还 做 资源取数_建回证 用 真、假、来源、错因、0、做 资源取数_完整统计。
    完。
    档 的 最近来源 为 来源。
    档 的 最近错误 为 错因。
    档 的 失败数 为 档 的 失败数 加 1。
    档 的 最近统计 为 做 资源取数_完整统计。
    做 资源取数_刷新健康 用 档。
    返还 做 资源取数_建回证 用 真、假、来源、错因、0、档 的 最近统计。
完。

资源取数_初始化 接受 档：
    做 资源_初始化。
    做 CookieJar_初始化。
    档 的 是否初始化 为 真。
    档 的 最近动作 为 「初始化」。
    档 的 最近来源 为 「资源取数_初始化」。
    档 的 最近统计 为 做 资源取数_完整统计。
    做 资源取数_刷新健康 用 档。
    返还 档。
完。

资源取数_创建 接受 名称：
    档 为 做 资源取数_建档 用 名称。
    做 资源取数_初始化 用 档。
    返还 做 资源取数_登记 用 档。
完。

资源取数_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 非 档 的 是否初始化：
        返还 假。
    完。
    返还 真。
完。

资源取数_新头 接受 档：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「新头」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「新头」、一组：（空）、「头部」。
    结果 为 做 取数Headers_创建。
    档 的 头部数 为 档 的 头部数 加 1。
    返还 做 资源取数_成功 用 档、「新头」、结果。
完。

资源取数_加头 接受 档、头、名、值：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「加头」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「加头」、一组：头、名、值、「头部」。
    结果 为 做 取数Headers_append 用 头、名、值。
    档 的 头部数 为 档 的 头部数 加 1。
    返还 做 资源取数_成功 用 档、「加头」、结果。
完。

资源取数_设头 接受 档、头、名、值：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「设头」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「设头」、一组：头、名、值、「头部」。
    结果 为 做 取数Headers_set 用 头、名、值。
    档 的 头部数 为 档 的 头部数 加 1。
    返还 做 资源取数_成功 用 档、「设头」、结果。
完。

资源取数_取头 接受 档、头、名：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「取头」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「取头」、一组：头、名、「头部」。
    档 的 头部数 为 档 的 头部数 加 1。
    返还 做 资源取数_成功 用 档、「取头」、做 取数Headers_get 用 头、名。
完。

资源取数_删头 接受 档、头、名：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「删头」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「删头」、一组：头、名、「头部」。
    结果 为 做 取数Headers_delete 用 头、名。
    档 的 头部数 为 档 的 头部数 加 1。
    返还 做 资源取数_成功 用 档、「删头」、结果。
完。

资源取数_查禁头 接受 档、名：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「查禁头」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「查禁头」、一组：名、「头部」。
    档 的 头部数 为 档 的 头部数 加 1。
    返还 做 资源取数_成功 用 档、「查禁头」、做 取数Headers_是禁止头 用 名。
完。

资源取数_建请求 接受 档、地址、选项：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「建请求」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「建请求」、一组：地址、选项、「请求对象」。
    结果 为 做 取数Request_创建 用 地址、选项。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「建请求」、结果。
完。

资源取数_建响应 接受 档、状态码、体数据、体长度、响应头：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「建响应」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「建响应」、一组：状态码、体数据、体长度、响应头、「请求对象」。
    结果 为 做 取数Response_创建 用 状态码、体数据、体长度、响应头。
    档 的 响应数 为 档 的 响应数 加 1。
    返还 做 资源取数_成功 用 档、「建响应」、结果。
完。

资源取数_执行 接受 档、运行时、地址、选项：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「执行」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「执行」、一组：运行时、地址、选项、「取数执行」。
    结果 为 做 取数_执行 用 运行时、地址、选项。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「执行」、结果。
完。

资源取数_包装响应 接受 档、运行时、响应：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「包装响应」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「包装响应」、一组：运行时、响应、「取数执行」。
    结果 为 做 取数_包装Response 用 运行时、响应。
    档 的 响应数 为 档 的 响应数 加 1。
    返还 做 资源取数_成功 用 档、「包装响应」、结果。
完。

资源取数_新承诺 接受 档、运行时：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「新承诺」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「新承诺」、一组：运行时、「取数执行」。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「新承诺」、做 Nova承诺_创建 用 运行时。
完。

资源取数_承诺兑现 接受 档、运行时、承诺、值：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「承诺兑现」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「承诺兑现」、一组：运行时、承诺、值、「取数执行」。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「承诺兑现」、做 Nova承诺_resolve 用 运行时、承诺、值。
完。

资源取数_承诺拒绝 接受 档、运行时、承诺、原因：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「承诺拒绝」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「承诺拒绝」、一组：运行时、承诺、原因、「取数执行」。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「承诺拒绝」、做 Nova承诺_reject 用 运行时、承诺、原因。
完。

资源取数_承诺后续 接受 档、运行时、承诺、成功回调、失败回调：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「承诺后续」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「承诺后续」、一组：运行时、承诺、成功回调、失败回调、「取数执行」。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「承诺后续」、做 Nova承诺_then 用 运行时、承诺、成功回调、失败回调。
完。

资源取数_新标记请求 接受 档、运行时：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「新标记请求」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「新标记请求」、一组：运行时、「标记请求」。
    结果 为 做 XHR_创建 用 运行时。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「新标记请求」、结果。
完。

资源取数_打开标记请求 接受 档、请求体、方法、地址、异步：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「打开标记请求」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「打开标记请求」、一组：请求体、方法、地址、异步、「标记请求」。
    结果 为 做 XHR_open 用 请求体、方法、地址、异步。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「打开标记请求」、结果。
完。

资源取数_标记请求头 接受 档、请求体、名、值：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「标记请求头」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「标记请求头」、一组：请求体、名、值、「标记请求」。
    结果 为 做 XHR_setRequestHeader 用 请求体、名、值。
    档 的 头部数 为 档 的 头部数 加 1。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「标记请求头」、结果。
完。

资源取数_发送标记请求 接受 档、请求体、内容体：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「发送标记请求」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「发送标记请求」、一组：请求体、内容体、「标记请求」。
    结果 为 做 XHR_send 用 请求体、内容体。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「发送标记请求」、结果。
完。

资源取数_中止标记请求 接受 档、请求体：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「中止标记请求」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「中止标记请求」、一组：请求体、「标记请求」。
    结果 为 做 XHR_abort 用 请求体。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「中止标记请求」、结果。
完。

资源取数_取响应头 接受 档、请求体、名：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「取响应头」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「取响应头」、一组：请求体、名、「标记请求」。
    档 的 响应数 为 档 的 响应数 加 1。
    返还 做 资源取数_成功 用 档、「取响应头」、做 XHR_getResponseHeader 用 请求体、名。
完。

资源取数_取全部响应头 接受 档、请求体：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「取全部响应头」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「取全部响应头」、一组：请求体、「标记请求」。
    档 的 响应数 为 档 的 响应数 加 1。
    返还 做 资源取数_成功 用 档、「取全部响应头」、做 XHR_getAllResponseHeaders 用 请求体。
完。

资源取数_触发就绪 接受 档、请求体：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「触发就绪」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「触发就绪」、一组：请求体、「标记请求」。
    档 的 响应数 为 档 的 响应数 加 1。
    返还 做 资源取数_成功 用 档、「触发就绪」、做 XHR_触发readystatechange 用 请求体。
完。

资源取数_分块解码器 接受 档：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「分块解码器」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「分块解码器」、一组：（空）、「超传协议」。
    档 的 解码数 为 档 的 解码数 加 1。
    返还 做 资源取数_成功 用 档、「分块解码器」、做 ChunkedDecoder_创建。
完。

资源取数_分块解码 接受 档、解码器、数据、数据长度：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「分块解码」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「分块解码」、一组：解码器、数据、数据长度、「超传协议」。
    结果 为 做 ChunkedDecoder_解码 用 解码器、数据、数据长度。
    档 的 解码数 为 档 的 解码数 加 1。
    返还 做 资源取数_成功 用 档、「分块解码」、结果。
完。

资源取数_膨胀器 接受 档：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「膨胀器」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「膨胀器」、一组：（空）、「超传协议」。
    档 的 解码数 为 档 的 解码数 加 1。
    返还 做 资源取数_成功 用 档、「膨胀器」、做 Inflate_创建。
完。

资源取数_膨胀解码 接受 档、状态、输入、输入长度：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「膨胀解码」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「膨胀解码」、一组：状态、输入、输入长度、「超传协议」。
    结果 为 做 Inflate_解压 用 状态、输入、输入长度。
    档 的 解码数 为 档 的 解码数 加 1。
    返还 做 资源取数_成功 用 档、「膨胀解码」、结果。
完。

资源取数_压缩解包 接受 档、输入、输入长度：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「压缩解包」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「压缩解包」、一组：输入、输入长度、「超传协议」。
    结果 为 做 Gzip_解压 用 输入、输入长度。
    档 的 解码数 为 档 的 解码数 加 1。
    返还 做 资源取数_成功 用 档、「压缩解包」、结果。
完。

资源取数_内容类型 接受 档、头值：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「内容类型」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「内容类型」、一组：头值、「超传协议」。
    档 的 响应数 为 档 的 响应数 加 1。
    返还 做 资源取数_成功 用 档、「内容类型」、做 ContentType_解析 用 头值。
完。

资源取数_完整加载 接受 档、装载器：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「完整加载」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「完整加载」、一组：装载器、「超传协议」。
    档 的 请求数 为 档 的 请求数 加 1。
    档 的 响应数 为 档 的 响应数 加 1。
    返还 做 资源取数_成功 用 档、「完整加载」、做 ResourceLoader_完整加载 用 装载器。
完。

资源取数_解析存证 接受 档、头值、请求域：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「解析存证」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「解析存证」、一组：头值、请求域、「超传协议」。
    结果 为 做 Cookie_解析SetCookie 用 头值、请求域。
    档 的 响应数 为 档 的 响应数 加 1。
    返还 做 资源取数_成功 用 档、「解析存证」、结果。
完。

资源取数_存入存证 接受 档、存证：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「存入存证」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「存入存证」、一组：存证、「超传协议」。
    结果 为 做 Cookie_存入 用 存证。
    档 的 响应数 为 档 的 响应数 加 1。
    返还 做 资源取数_成功 用 档、「存入存证」、结果。
完。

资源取数_取存证头 接受 档、域、路径、是安全：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「取存证头」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「取存证头」、一组：域、路径、是安全、「超传协议」。
    档 的 头部数 为 档 的 头部数 加 1。
    返还 做 资源取数_成功 用 档、「取存证头」、做 Cookie_获取请求头 用 域、路径、是安全。
完。

资源取数_域匹配 接受 档、请求域、存证域：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「域匹配」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「域匹配」、一组：请求域、存证域、「超传协议」。
    档 的 响应数 为 档 的 响应数 加 1。
    返还 做 资源取数_成功 用 档、「域匹配」、做 Cookie_域匹配 用 请求域、存证域。
完。

资源取数_路径匹配 接受 档、请求路径、存证路径：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「路径匹配」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「路径匹配」、一组：请求路径、存证路径、「超传协议」。
    档 的 响应数 为 档 的 响应数 加 1。
    返还 做 资源取数_成功 用 档、「路径匹配」、做 Cookie_路径匹配 用 请求路径、存证路径。
完。

资源取数_重定向 接受 档、装载器、最大重定向数：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「重定向」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「重定向」、一组：装载器、最大重定向数、「超传协议」。
    档 的 响应数 为 档 的 响应数 加 1。
    返还 做 资源取数_成功 用 档、「重定向」、做 HTTP_处理重定向 用 装载器、最大重定向数。
完。

资源取数_需预检 接受 档、请求体：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「需预检」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「需预检」、一组：请求体、「超传协议」。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「需预检」、做 CORS_需要预检 用 请求体。
完。

资源取数_发预检 接受 档、装载器：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「发预检」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「发预检」、一组：装载器、「超传协议」。
    档 的 请求数 为 档 的 请求数 加 1。
    返还 做 资源取数_成功 用 档、「发预检」、做 CORS_发送预检 用 装载器。
完。

资源取数_资源提交 接受 档、地址、类型：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「资源提交」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「资源提交」、一组：地址、类型、「资源调度」。
    结果 为 做 资源_提交 用 地址、类型。
    档 的 调度数 为 档 的 调度数 加 1。
    返还 做 资源取数_成功 用 档、「资源提交」、结果。
完。

资源取数_资源调度 接受 档：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「资源调度」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「资源调度」、一组：（空）、「资源调度」。
    档 的 调度数 为 档 的 调度数 加 1。
    返还 做 资源取数_成功 用 档、「资源调度」、做 资源_调度。
完。

资源取数_资源完成 接受 档、索引、字节数、成功：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「资源完成」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「资源完成」、一组：索引、字节数、成功、「资源调度」。
    结果 为 做 资源_完成 用 索引、字节数、成功。
    档 的 调度数 为 档 的 调度数 加 1。
    返还 做 资源取数_成功 用 档、「资源完成」、结果。
完。

资源取数_资源预取 接受 档、地址：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「资源预取」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「资源预取」、一组：地址、「资源调度」。
    结果 为 做 资源_预取 用 地址。
    档 的 调度数 为 档 的 调度数 加 1。
    返还 做 资源取数_成功 用 档、「资源预取」、结果。
完。

资源取数_检查预取 接受 档、地址哈希：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「检查预取」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「检查预取」、一组：地址哈希、「资源调度」。
    档 的 调度数 为 档 的 调度数 加 1。
    返还 做 资源取数_成功 用 档、「检查预取」、做 资源_检查预取 用 地址哈希。
完。

资源取数_智能提升 接受 档、索引：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「智能提升」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「智能提升」、一组：索引、「资源调度」。
    结果 为 做 资源_智能提升 用 索引。
    档 的 调度数 为 档 的 调度数 加 1。
    返还 做 资源取数_成功 用 档、「智能提升」、结果。
完。

资源取数_智能降低 接受 档、索引：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「智能降低」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「智能降低」、一组：索引、「资源调度」。
    结果 为 做 资源_智能降低 用 索引。
    档 的 调度数 为 档 的 调度数 加 1。
    返还 做 资源取数_成功 用 档、「智能降低」、结果。
完。

资源取数_视口提升 接受 档、地址哈希列表：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「视口提升」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「视口提升」、一组：地址哈希列表、「资源调度」。
    结果 为 做 资源_视口提升 用 地址哈希列表。
    档 的 调度数 为 档 的 调度数 加 1。
    返还 做 资源取数_成功 用 档、「视口提升」、结果。
完。

资源取数_资源统计 接受 档：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「资源统计」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「资源统计」、一组：（空）、「资源调度」。
    档 的 调度数 为 档 的 调度数 加 1。
    返还 做 资源取数_成功 用 档、「资源统计」、一组：做 资源_完整统计、做 资源_详细统计。
完。

资源取数_资源健康 接受 档：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「资源健康」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「资源健康」、一组：（空）、「资源调度」。
    档 的 调度数 为 档 的 调度数 加 1。
    返还 做 资源取数_成功 用 档、「资源健康」、做 资源_健康检查。
完。

资源取数_长连创建 接受 档、地址：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「长连创建」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「长连创建」、一组：地址、「长连接」。
    结果 为 做 长连接_创建 用 地址。
    档 的 长连数 为 档 的 长连数 加 1。
    返还 做 资源取数_成功 用 档、「长连创建」、结果。
完。

资源取数_长连连接 接受 档、长连：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「长连连接」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「长连连接」、一组：长连、「长连接」。
    结果 为 做 长连接_连接 用 长连。
    档 的 长连数 为 档 的 长连数 加 1。
    返还 做 资源取数_成功 用 档、「长连连接」、结果。
完。

资源取数_长连发文 接受 档、长连、文本：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「长连发文」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「长连发文」、一组：长连、文本、「长连接」。
    结果 为 做 长连接_发送文本 用 长连、文本。
    档 的 长连数 为 档 的 长连数 加 1。
    返还 做 资源取数_成功 用 档、「长连发文」、结果。
完。

资源取数_长连发流 接受 档、长连、数据、长度：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「长连发流」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「长连发流」、一组：长连、数据、长度、「长连接」。
    结果 为 做 长连接_发送二进制 用 长连、数据、长度。
    档 的 长连数 为 档 的 长连数 加 1。
    返还 做 资源取数_成功 用 档、「长连发流」、结果。
完。

资源取数_长连发帧 接受 档、长连、操作码、负载、负载长度：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「长连发帧」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「长连发帧」、一组：长连、操作码、负载、负载长度、「长连接」。
    结果 为 做 长连接_发送帧 用 长连、操作码、负载、负载长度。
    档 的 长连数 为 档 的 长连数 加 1。
    返还 做 资源取数_成功 用 档、「长连发帧」、结果。
完。

资源取数_长连收帧 接受 档、长连：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「长连收帧」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「长连收帧」、一组：长连、「长连接」。
    档 的 长连数 为 档 的 长连数 加 1。
    返还 做 资源取数_成功 用 档、「长连收帧」、做 长连接_接收帧 用 长连。
完。

资源取数_长连关闭 接受 档、长连、状态码、原因：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「长连关闭」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「长连关闭」、一组：长连、状态码、原因、「长连接」。
    结果 为 做 长连接_关闭 用 长连、状态码、原因。
    档 的 长连数 为 档 的 长连数 加 1。
    返还 做 资源取数_成功 用 档、「长连关闭」、结果。
完。

资源取数_巡检 接受 档、运行时、地址、选项：
    若 非 做 资源取数_检查档 用 档：
        返还 做 资源取数_失败 用 档、「巡检」、「取数档不可用」。
    完。
    做 资源取数_记录 用 档、「巡检」、一组：运行时、地址、选项、「资源取数」。
    可变 报告 为 一组：（空）。
    头 为 做 资源取数_新头 用 档 的 数据。
    做 追加 用 报告、做 资源取数_设头 用 档、头、「accept」、「*/*」 的 数据。
    做 追加 用 报告、做 资源取数_查禁头 用 档、「host」 的 数据。
    做 追加 用 报告、做 资源取数_建请求 用 档、地址、选项 的 数据。
    做 追加 用 报告、做 资源取数_内容类型 用 档、「text/html; charset=utf-8」 的 数据。
    做 追加 用 报告、做 资源取数_资源统计 用 档 的 数据。
    做 追加 用 报告、做 资源取数_资源健康 用 档 的 数据。
    做 追加 用 报告、做 chunked_统计。
    做 追加 用 报告、做 chunked_健康检查。
    返还 做 资源取数_成功 用 档、「巡检」、报告。
完。

资源取数_摘要 接受 档：
    摘要 为 新建 取数摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 头部数 为 档 的 头部数。
    摘要 的 请求数 为 档 的 请求数。
    摘要 的 响应数 为 档 的 响应数。
    摘要 的 解码数 为 档 的 解码数。
    摘要 的 调度数 为 档 的 调度数。
    摘要 的 长连数 为 档 的 长连数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 资源取数_刷新健康 用 档。
    返还 摘要。
完。

资源取数_健康 接受 档：
    若 档 等于 空：
        返还 「无档」。
    完。
    返还 做 资源取数_刷新健康 用 档。
完。

资源取数_自检 接受 档：
    若 非 做 资源取数_检查档 用 档：
        返还 假。
    完。
    若 做 资源取数_资源健康 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 chunked_健康检查 等于 假：
        返还 假。
    完。
    若 做 资源取数_资源统计 用 档 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

资源取数_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 资源取数_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        做 资源取数_初始化 用 档。
        返还 做 资源取数_成功 用 档、「处理」、「已初始化」。
    完。
    若 请求 的 动作 等于 「执行」：
        返还 做 资源取数_执行 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「标记请求」：
        返还 做 资源取数_新标记请求 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「提交资源」：
        返还 做 资源取数_资源提交 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「调度资源」：
        返还 做 资源取数_资源调度 用 档。
    完。
    若 请求 的 动作 等于 「长连」：
        返还 做 资源取数_长连创建 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「统计」：
        返还 做 资源取数_成功 用 档、「统计」、做 资源取数_摘要 用 档。
    完。
    若 请求 的 动作 等于 「巡检」：
        返还 做 资源取数_巡检 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    返还 做 资源取数_失败 用 档、「处理」、「未知动作」。
完。

SAMESITE_NONE 为 0。

SAMESITE_LAX 为 1。

SAMESITE_STRICT 为 2。

Cookie 是 一种 事物，含 名称、值、域、路径、过期时间、maxAge、secure、httpOnly、sameSite、partitioned、创建时间、最后访问、下一个。

CookieJar 是 一种 事物，含 头、数量、每域最大、总最大、单条最大字节。

可变 全局CookieJar 为 0。

CookieJar_获取 接受 零参：
    若 全局CookieJar 等于 0：
        全局CookieJar 为 做 堆_分配 用 CookieJar。
        全局CookieJar 的 头 为 0。
        全局CookieJar 的 数量 为 0。
        全局CookieJar 的 每域最大 为 180。
        全局CookieJar 的 总最大 为 3000。
        全局CookieJar 的 单条最大字节 为 4096。
    完。
    返还 全局CookieJar。
完。

Cookie_设置 接受 cookie字符串、当前域、当前路径：
    若 cookie字符串 等于 0：
        返还。
    完。
    jar 为 做 CookieJar_获取。
    c 为 做 堆_分配 用 Cookie。
    c 的 域 为 当前域。
    c 的 路径 为 「/」。
    c 的 过期时间 为 0。
    c 的 maxAge 为 负 1。
    c 的 secure 为 假。
    c 的 httpOnly 为 假。
    c 的 sameSite 为 SAMESITE_LAX。
    c 的 partitioned 为 假。
    c 的 创建时间 为 做 平台_获取高精度时间。
    c 的 最后访问 为 c 的 创建时间。
    len 为 做 字节长度 用 cookie字符串。
    可变 pos 为 0。
    可变 eq 为 0。
    只要 eq 小于 len 并且 做 字节取 用 cookie字符串、eq 不等于 61 并且 做 字节取 用 cookie字符串、eq 不等于 59：
        eq 为 eq 加 1。
    完。
    若 eq 小于 len 并且 做 字节取 用 cookie字符串、eq 等于 61：
        c 的 名称 为 做 字符串_子串 用 cookie字符串、0、eq。
        可变 valEnd 为 eq 加 1。
        只要 valEnd 小于 len 并且 做 字节取 用 cookie字符串、valEnd 不等于 59：
            valEnd 为 valEnd 加 1。
        完。
        c 的 值 为 做 字符串_子串 用 cookie字符串、eq 加 1、valEnd。
        pos 为 valEnd。
    否则：
        返还。
    完。
    只要 pos 小于 len：
        若 做 字节取 用 cookie字符串、pos 等于 59：
            pos 为 pos 加 1。
        完。
        只要 pos 小于 len 并且 做 字节取 用 cookie字符串、pos 等于 32：
            pos 为 pos 加 1。
        完。
        若 pos 大于或等于 len：
            跳出。
        完。
        attr起始 为 pos。
        只要 pos 小于 len 并且 做 字节取 用 cookie字符串、pos 不等于 61 并且 做 字节取 用 cookie字符串、pos 不等于 59：
            pos 为 pos 加 1。
        完。
        attr 为 做 字符串_子串 用 cookie字符串、attr起始、pos。
        可变 val 为 「」。
        若 pos 小于 len 并且 做 字节取 用 cookie字符串、pos 等于 61：
            pos 为 pos 加 1。
            val起始 为 pos。
            只要 pos 小于 len 并且 做 字节取 用 cookie字符串、pos 不等于 59：
                pos 为 pos 加 1。
            完。
            val 为 做 字符串_子串 用 cookie字符串、val起始、pos。
        完。
        若 做 字符串_相等 用 attr、「Secure」 或者 做 字符串_相等 用 attr、「secure」：
            c 的 secure 为 真。
        否则若 做 字符串_相等 用 attr、「HttpOnly」 或者 做 字符串_相等 用 attr、「httponly」：
            c 的 httpOnly 为 真。
        否则若 做 字符串_相等 用 attr、「Path」 或者 做 字符串_相等 用 attr、「path」：
            c 的 路径 为 val。
        否则若 做 字符串_相等 用 attr、「Domain」 或者 做 字符串_相等 用 attr、「domain」：
            c 的 域 为 val。
        否则若 做 字符串_相等 用 attr、「SameSite」 或者 做 字符串_相等 用 attr、「samesite」：
            若 做 字符串_相等 用 val、「Strict」 或者 做 字符串_相等 用 val、「strict」：
                c 的 sameSite 为 SAMESITE_STRICT。
            否则若 做 字符串_相等 用 val、「None」 或者 做 字符串_相等 用 val、「none」：
                c 的 sameSite 为 SAMESITE_NONE。
            否则：
                c 的 sameSite 为 SAMESITE_LAX。
            完。
        否则若 做 字符串_相等 用 attr、「Partitioned」 或者 做 字符串_相等 用 attr、「partitioned」：
            c 的 partitioned 为 真。
        完。
    完。
    若 做 字节长度 用 c 的 名称 加 做 字节长度 用 c 的 值 大于 jar 的 单条最大字节：
        返还。
    完。
    做 Cookie_删除 用 c 的 名称、c 的 域、c 的 路径。
    c 的 下一个 为 jar 的 头。
    jar 的 头 为 c。
    jar 的 数量 为 jar 的 数量 加 1。
完。

Cookie_获取 接受 当前域、当前路径、是https：
    jar 为 做 CookieJar_获取。
    若 jar 等于 0 或者 jar 的 数量 等于 0：
        返还 「」。
    完。
    buf 为 做 堆_分配 用 4096。
    可变 pos 为 0。
    可变 first 为 真。
    现在 为 做 平台_获取高精度时间。
    可变 c 为 jar 的 头。
    只要 c 不等于 0：
        若 非 做 Cookie_域匹配 用 c 的 域、当前域：
            c 为 c 的 下一个。
            继续。
        完。
        若 非 做 Cookie_路径匹配 用 c 的 路径、当前路径：
            c 为 c 的 下一个。
            继续。
        完。
        若 c 的 secure 并且 非 是https：
            c 为 c 的 下一个。
            继续。
        完。
        若 c 的 httpOnly：
            c 为 c 的 下一个。
            继续。
        完。
        若 c 的 过期时间 大于 0 并且 现在 大于 c 的 过期时间：
            c 为 c 的 下一个。
            继续。
        完。
        若 非 first：
            做 字节写 用 buf、pos、59。
            pos 为 pos 加 1。
            做 字节写 用 buf、pos、32。
            pos 为 pos 加 1。
        完。
        nlen 为 做 字节长度 用 c 的 名称。
        做 内存_复制 用 buf 加 pos、c 的 名称、nlen。
        pos 为 pos 加 nlen。
        做 字节写 用 buf、pos、61。
        pos 为 pos 加 1。
        vlen 为 做 字节长度 用 c 的 值。
        做 内存_复制 用 buf 加 pos、c 的 值、vlen。
        pos 为 pos 加 vlen。
        first 为 假。
        c 的 最后访问 为 现在。
        c 为 c 的 下一个。
    完。
    做 字节写 用 buf、pos、0。
    返还 buf。
完。

Cookie_删除 接受 名称、域、路径：
    jar 为 做 CookieJar_获取。
    若 jar 等于 0 或者 名称 等于 0：
        返还。
    完。
    可变 prev 为 0。
    可变 c 为 jar 的 头。
    只要 c 不等于 0：
        若 做 字符串_相等 用 c 的 名称、名称 并且 做 Cookie_域匹配 用 c 的 域、域 并且 做 Cookie_路径匹配 用 c 的 路径、路径：
            若 prev 等于 0：
                jar 的 头 为 c 的 下一个。
            否则：
                prev 的 下一个 为 c 的 下一个。
            完。
            jar 的 数量 为 jar 的 数量 减 1。
            c 为 若 prev 等于 0 则 jar 的 头 否则 prev 的 下一个。
            继续。
        完。
        prev 为 c。
        c 为 c 的 下一个。
    完。
完。

Cookie_域匹配 接受 cookie域、请求域：
    若 cookie域 等于 0 或者 请求域 等于 0：
        返还 假。
    完。
    若 做 字符串_相等 用 cookie域、请求域：
        返还 真。
    完。
    若 做 字节取 用 cookie域、0 等于 46：
        返还 做 字符串_以结尾 用 请求域、cookie域。
    完。
    返还 假。
完。

Cookie_路径匹配 接受 cookie路径、请求路径：
    若 cookie路径 等于 0 或者 请求路径 等于 0：
        返还 真。
    完。
    返还 做 字符串_以开头 用 请求路径、cookie路径。
完。

CookieStore_get 接受 名称：
    jar 为 做 CookieJar_获取。
    可变 c 为 jar 的 头。
    只要 c 不等于 0：
        若 做 字符串_相等 用 c 的 名称、名称：
            返还 c。
        完。
        c 为 c 的 下一个。
    完。
    返还 0。
完。

CookieStore_getAll 接受 零参：
    返还 做 CookieJar_获取 的 头。
完。

CookieStore_set 接受 名称、值、选项：
    若 名称 等于 0 或者 值 等于 0：
        返还。
    完。
    jar 为 做 CookieJar_获取。
    cookie 为 做 堆_分配 用 Cookie。
    cookie 的 名称 为 名称。
    cookie 的 值 为 值。
    cookie 的 路径 为 「/」。
    cookie 的 域 为 0。
    cookie 的 httpOnly 为 假。
    cookie 的 secure 为 假。
    cookie 的 sameSite 为 0。
    cookie 的 过期时间 为 0。
    若 选项 不等于 0：
        若 选项 的 路径 不等于 0：
            cookie 的 路径 为 选项 的 路径。
        完。
        若 选项 的 域 不等于 0：
            cookie 的 域 为 选项 的 域。
        完。
        若 选项 的 过期时间 不等于 0：
            cookie 的 过期时间 为 选项 的 过期时间。
        完。
        cookie 的 secure 为 选项 的 secure。
        cookie 的 sameSite 为 选项 的 sameSite。
    完。
    做 CookieJar_添加 用 jar、cookie。
完。

SOP_允许DOM访问 接受 请求源、目标源：
    返还 做 Origin_同源 用 请求源、目标源。
完。

SOP_允许子资源请求 接受 请求源、目标URL：
    目标源 为 做 Origin_从URL 用 目标URL。
    若 做 Origin_同源 用 请求源、目标源：
        返还 真。
    完。
    返还 假。
完。

SOP_混合内容检查 接受 页面源、资源URL：
    若 页面源 的 协议 等于 协议_HTTPS：
        资源源 为 做 Origin_从URL 用 资源URL。
        若 资源源 的 协议 等于 协议_HTTP：
            返还 假。
        完。
    完。
    返还 真。
完。

CSP源_NONE 为 0。

CSP源_SELF 为 1。

CSP源_UNSAFE_INLINE 为 2。

CSP源_UNSAFE_EVAL 为 3。

CSP源_STRICT_DYNAMIC 为 4。

CSP源_NONCE 为 5。

CSP源_HASH 为 6。

CSP源_HOST 为 7。

CSP源_SCHEME 为 8。

CSP源条目 是 一种 事物，含 类型、值、值哈希、下一个。

CSP指令_default_src 为 0。

CSP指令_script_src 为 1。

CSP指令_style_src 为 2。

CSP指令_img_src 为 3。

CSP指令_font_src 为 4。

CSP指令_connect_src 为 5。

CSP指令_frame_src 为 6。

CSP指令_object_src 为 7。

CSP指令_base_uri 为 8。

CSP指令_form_action 为 9。

CSP指令_数量 为 10。

CSP策略 是 一种 事物，含 指令、是否强制、报告URL。

CSP_创建 接受 零参：
    策略 为 做 堆_分配 用 CSP策略。
    可变 i 为 0。
    只要 i 小于 CSP指令_数量：
        策略 的 指令 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    策略 的 是否强制 为 真。
    策略 的 报告URL 为 0。
    返还 策略。
完。

CSP_解析 接受 头字符串：
    策略 为 做 CSP_创建。
    可变 位置 为 0。
    长度 为 做 字节长度 用 头字符串。
    只要 位置 小于 长度：
        只要 位置 小于 长度 并且 (做 字节取 用 头字符串、位置 等于 32 或者 做 字节取 用 头字符串、位置 等于 59)：
            位置 为 位置 加 1。
        完。
        若 位置 大于或等于 长度：
            跳出。
        完。
        指令开始 为 位置。
        只要 位置 小于 长度 并且 做 字节取 用 头字符串、位置 不等于 32 并且 做 字节取 用 头字符串、位置 不等于 59：
            位置 为 位置 加 1。
        完。
        指令名 为 做 字符串_截取 用 头字符串、指令开始、位置。
        指令ID 为 做 CSP_指令名到ID 用 指令名。
        只要 位置 小于 长度 并且 做 字节取 用 头字符串、位置 不等于 59：
            只要 位置 小于 长度 并且 做 字节取 用 头字符串、位置 等于 32：
                位置 为 位置 加 1。
            完。
            若 位置 大于或等于 长度 或者 做 字节取 用 头字符串、位置 等于 59：
                跳出。
            完。
            源开始 为 位置。
            只要 位置 小于 长度 并且 做 字节取 用 头字符串、位置 不等于 32 并且 做 字节取 用 头字符串、位置 不等于 59：
                位置 为 位置 加 1。
            完。
            源值 为 做 字符串_截取 用 头字符串、源开始、位置。
            条目 为 做 堆_分配 用 CSP源条目。
            条目 的 类型 为 做 CSP_解析源类型 用 源值。
            条目 的 值 为 源值。
            条目 的 值哈希 为 做 字符串_哈希 用 源值。
            条目 的 下一个 为 策略 的 指令 的 第 指令ID 项。
            策略 的 指令 的 第 指令ID 项 为 条目。
        完。
    完。
    返还 策略。
完。

CSP_指令名到ID 接受 名称：
    若 做 字符串_相等 用 名称、「default-src」：
        返还 CSP指令_default_src。
    完。
    若 做 字符串_相等 用 名称、「script-src」：
        返还 CSP指令_script_src。
    完。
    若 做 字符串_相等 用 名称、「style-src」：
        返还 CSP指令_style_src。
    完。
    若 做 字符串_相等 用 名称、「img-src」：
        返还 CSP指令_img_src。
    完。
    若 做 字符串_相等 用 名称、「font-src」：
        返还 CSP指令_font_src。
    完。
    若 做 字符串_相等 用 名称、「connect-src」：
        返还 CSP指令_connect_src。
    完。
    若 做 字符串_相等 用 名称、「frame-src」：
        返还 CSP指令_frame_src。
    完。
    若 做 字符串_相等 用 名称、「object-src」：
        返还 CSP指令_object_src。
    完。
    若 做 字符串_相等 用 名称、「base-uri」：
        返还 CSP指令_base_uri。
    完。
    若 做 字符串_相等 用 名称、「form-action」：
        返还 CSP指令_form_action。
    完。
    返还 CSP指令_default_src。
完。

CSP_解析源类型 接受 源：
    若 做 字符串_相等 用 源、「'none'」：
        返还 CSP源_NONE。
    完。
    若 做 字符串_相等 用 源、「'self'」：
        返还 CSP源_SELF。
    完。
    若 做 字符串_相等 用 源、「'unsafe-inline'」：
        返还 CSP源_UNSAFE_INLINE。
    完。
    若 做 字符串_相等 用 源、「'unsafe-eval'」：
        返还 CSP源_UNSAFE_EVAL。
    完。
    若 做 字符串_相等 用 源、「'strict-dynamic'」：
        返还 CSP源_STRICT_DYNAMIC。
    完。
    若 做 字符串_前缀匹配 用 源、「'nonce-」：
        返还 CSP源_NONCE。
    完。
    若 做 字符串_前缀匹配 用 源、「'sha256-」 或者 做 字符串_前缀匹配 用 源、「'sha384-」：
        返还 CSP源_HASH。
    完。
    返还 CSP源_HOST。
完。

CSP_允许 接受 策略、指令类型、资源URL、页面源、nonce、内容哈希：
    若 策略 等于 0：
        返还 真。
    完。
    源链 为 策略 的 指令 的 第 指令类型 项。
    若 源链 等于 0：
        源链 为 策略 的 指令 的 第 CSP指令_default_src 项。
    完。
    若 源链 等于 0：
        返还 真。
    完。
    资源源 为 做 Origin_从URL 用 资源URL。
    可变 当前 为 源链。
    只要 当前 不等于 0：
        条目 为 当前。
        若 做 CSP_源匹配 用 条目、资源源、页面源、nonce、内容哈希：
            返还 真。
        完。
        当前 为 条目 的 下一个。
    完。
    若 策略 的 是否强制：
        做 CSP_报告违规 用 策略、指令类型、资源URL。
        返还 假。
    完。
    做 CSP_报告违规 用 策略、指令类型、资源URL。
    返还 真。
完。

CSP_源匹配 接受 条目、资源源、页面源、nonce、内容哈希：
    若 条目 的 类型 等于 CSP源_NONE：
        返还 假。
    完。
    若 条目 的 类型 等于 CSP源_SELF：
        返还 做 Origin_同源 用 资源源、页面源。
    完。
    若 条目 的 类型 等于 CSP源_NONCE 并且 nonce 不等于 0：
        返还 做 字符串_相等 用 条目 的 值、nonce。
    完。
    若 条目 的 类型 等于 CSP源_HASH 并且 内容哈希 不等于 0：
        返还 做 字符串_相等 用 条目 的 值、内容哈希。
    完。
    若 条目 的 类型 等于 CSP源_HOST：
        返还 做 CSP_主机匹配 用 条目 的 值、资源源 的 主机。
    完。
    若 条目 的 类型 等于 CSP源_UNSAFE_INLINE：
        返还 真。
    完。
    若 条目 的 类型 等于 CSP源_UNSAFE_EVAL：
        返还 真。
    完。
    返还 假。
完。

优先_最高 为 0。

优先_高 为 1。

优先_中 为 2。

优先_低 为 3。

优先_最低 为 4。

最大请求 为 128。

并发上限 为 6。

预取上限 为 16。

资源请求 是 一种 事物，含 地址哈希、类型、优先级、状态、开始时刻、完成时刻、字节数、重试次数。

可变 请求队列 为 一组：（空）。

可变 请求数 为 0。

可变 活跃请求 为 0。

可变 预取队列 为 一组：（空）。

可变 预取数 为 0。

可变 总请求 为 0。

可变 总完成 为 0。

可变 总字节 为 0。

可变 预取命中 为 0。

资源_初始化 接受 零参：
    请求队列 为 一组：（空）。
    请求数 为 0。
    活跃请求 为 0。
    预取队列 为 一组：（空）。
    预取数 为 0。
完。

资源_计算优先级 接受 地址哈希、类型：
    若 类型 等于 1：
        返还 优先_最高。
    完。
    若 类型 等于 2：
        返还 优先_高。
    完。
    若 类型 等于 3：
        返还 优先_中。
    完。
    若 类型 等于 4：
        返还 优先_低。
    完。
    返还 优先_中。
完。

资源_提交 接受 地址、类型：
    总请求 为 总请求 加 1。
    若 请求数 大于或等于 最大请求：
        返还 负 1。
    完。
    地址哈希 为 做 哈希_字符串 用 地址。
    预取结果 为 做 资源_检查预取 用 地址哈希。
    若 预取结果 大于或等于 0：
        预取命中 为 预取命中 加 1。
        返还 预取结果。
    完。
    请求 为 新建 资源请求。
    请求 的 地址哈希 为 地址哈希。
    请求 的 类型 为 类型。
    请求 的 优先级 为 做 资源_计算优先级 用 地址哈希、类型。
    请求 的 状态 为 0。
    请求 的 开始时刻 为 0。
    请求 的 完成时刻 为 0。
    请求 的 字节数 为 0。
    请求 的 重试次数 为 0。
    插入位 为 做 资源_找插入位 用 请求 的 优先级。
    做 追加 用 请求队列、请求。
    请求数 为 请求数 加 1。
    做 资源_调度。
    返还 请求数 减 1。
完。

资源_找插入位 接受 优先级：
    可变 序号 为 0。
    只要 序号 小于 请求数：
        若 请求队列 的 第 序号 项 的 优先级 大于 优先级：
            返还 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 请求数。
完。

资源_调度 接受 零参：
    可变 序号 为 0。
    只要 序号 小于 请求数 并且 活跃请求 小于 并发上限：
        若 请求队列 的 第 序号 项 的 状态 等于 0：
            请求队列 的 第 序号 项 的 状态 为 1。
            请求队列 的 第 序号 项 的 开始时刻 为 做 平台_获取高精度时间。
            活跃请求 为 活跃请求 加 1。
        完。
        序号 为 序号 加 1。
    完。
完。

资源_完成 接受 索引、字节数、成功：
    若 索引 大于或等于 请求数：
        返还。
    完。
    请求队列 的 第 索引 项 的 完成时刻 为 做 平台_获取高精度时间。
    请求队列 的 第 索引 项 的 字节数 为 字节数。
    若 成功：
        请求队列 的 第 索引 项 的 状态 为 2。
        总完成 为 总完成 加 1。
        总字节 为 总字节 加 字节数。
    否则：
        请求队列 的 第 索引 项 的 重试次数 为 请求队列 的 第 索引 项 的 重试次数 加 1。
        若 请求队列 的 第 索引 项 的 重试次数 小于 3：
            请求队列 的 第 索引 项 的 状态 为 0。
        否则：
            请求队列 的 第 索引 项 的 状态 为 3。
        完。
    完。
    活跃请求 为 活跃请求 减 1。
    做 资源_调度。
完。

资源_预取 接受 地址：
    若 预取数 大于或等于 预取上限：
        返还。
    完。
    地址哈希 为 做 哈希_字符串 用 地址。
    做 追加 用 预取队列、地址哈希。
    预取数 为 预取数 加 1。
    做 资源_提交 用 地址、4。
完。

资源_检查预取 接受 地址哈希：
    可变 序号 为 0。
    只要 序号 小于 预取数：
        若 预取队列 的 第 序号 项 等于 地址哈希：
            返还 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 负 1。
完。

资源_获取耗时 接受 索引：
    若 索引 大于或等于 请求数：
        返还 0。
    完。
    若 请求队列 的 第 索引 项 的 完成时刻 等于 0：
        返还 0。
    完。
    返还 请求队列 的 第 索引 项 的 完成时刻 减 请求队列 的 第 索引 项 的 开始时刻。
完。

资源_预取命中率 接受 零参：
    若 总请求 等于 0：
        返还 0。
    完。
    返还 预取命中 乘 100 除以 总请求。
完。

资源_完整统计 接受 零参：
    返还 一组：总请求、总完成、总字节、活跃请求、预取数、预取命中、做 资源_预取命中率。
完。

资源_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 活跃请求 大于或等于 并发上限：
        做 追加 用 问题、「并发请求已满: 」 加 做 整数转字符串 用 活跃请求。
    完。
    失败 为 总请求 减 总完成。
    若 失败 大于 总请求 除以 3 并且 总请求 大于 50：
        做 追加 用 问题、「请求失败率过高」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「资源加载健康」。
    完。
    返还 问题。
完。

资源_详细统计 接受 零参：
    返还 一本字典：总请求 对应 总请求、总完成 对应 总完成、总字节 对应 总字节、活跃 对应 活跃请求、队列 对应 请求数、预取数 对应 预取数、预取命中 对应 预取命中、命中率 对应 做 资源_预取命中率、并发上限 对应 并发上限。
完。

资源_重置统计 接受 零参：
    总请求 为 0。
    总完成 为 0。
    总字节 为 0。
    预取命中 为 0。
完。

类型_文档 为 1。

类型_样式表 为 2。

类型_脚本 为 3。

类型_图片 为 4。

类型_字体 为 5。

类型_媒体 为 6。

类型_XHR 为 7。

类型_预取 为 8。

资源_类型名 接受 类型：
    若 类型 等于 类型_文档：
        返还 「document」。
    完。
    若 类型 等于 类型_样式表：
        返还 「stylesheet」。
    完。
    若 类型 等于 类型_脚本：
        返还 「script」。
    完。
    若 类型 等于 类型_图片：
        返还 「image」。
    完。
    若 类型 等于 类型_字体：
        返还 「font」。
    完。
    若 类型 等于 类型_媒体：
        返还 「media」。
    完。
    若 类型 等于 类型_XHR：
        返还 「xhr」。
    完。
    若 类型 等于 类型_预取：
        返还 「prefetch」。
    完。
    返还 「unknown」。
完。

资源_优先级名 接受 优先级：
    若 优先级 等于 优先_最高：
        返还 「highest」。
    完。
    若 优先级 等于 优先_高：
        返还 「high」。
    完。
    若 优先级 等于 优先_中：
        返还 「medium」。
    完。
    若 优先级 等于 优先_低：
        返还 「low」。
    完。
    若 优先级 等于 优先_最低：
        返还 「lowest」。
    完。
    返还 「unknown」。
完。

资源_状态名 接受 状态：
    若 状态 等于 0：
        返还 「pending」。
    完。
    若 状态 等于 1：
        返还 「loading」。
    完。
    若 状态 等于 2：
        返还 「done」。
    完。
    若 状态 等于 3：
        返还 「failed」。
    完。
    返还 「unknown」。
完。

可变 智能_提升次数 为 0。

可变 智能_降低次数 为 0。

资源_智能提升 接受 索引：
    若 索引 大于或等于 请求数：
        返还。
    完。
    若 请求队列 的 第 索引 项 的 优先级 大于 优先_最高：
        请求队列 的 第 索引 项 的 优先级 为 请求队列 的 第 索引 项 的 优先级 减 1。
        智能_提升次数 为 智能_提升次数 加 1。
    完。
完。

资源_智能降低 接受 索引：
    若 索引 大于或等于 请求数：
        返还。
    完。
    若 请求队列 的 第 索引 项 的 优先级 小于 优先_最低：
        请求队列 的 第 索引 项 的 优先级 为 请求队列 的 第 索引 项 的 优先级 加 1。
        智能_降低次数 为 智能_降低次数 加 1。
    完。
完。

资源_视口提升 接受 视口内地址哈希列表：
    可变 i 为 0。
    只要 i 小于 请求数：
        可变 j 为 0。
        只要 j 小于 做 计数 用 视口内地址哈希列表：
            若 请求队列 的 第 i 项 的 地址哈希 等于 视口内地址哈希列表 的 第 j 项：
                做 资源_智能提升 用 i。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

资源_获取请求 接受 索引：
    若 索引 大于或等于 请求数：
        返还 0。
    完。
    返还 请求队列 的 第 索引 项。
完。

资源_按状态计数 接受 目标状态：
    可变 数量 为 0。
    可变 i 为 0。
    只要 i 小于 请求数：
        若 请求队列 的 第 i 项 的 状态 等于 目标状态：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

资源_待处理数 接受 零参：
    返还 做 资源_按状态计数 用 0。
完。

资源_加载中数 接受 零参：
    返还 做 资源_按状态计数 用 1。
完。

资源_已完成数 接受 零参：
    返还 做 资源_按状态计数 用 2。
完。

资源_已失败数 接受 零参：
    返还 做 资源_按状态计数 用 3。
完。

资源_平均耗时 接受 零参：
    若 总完成 等于 0：
        返还 0。
    完。
    可变 总耗时 为 0。
    可变 i 为 0。
    只要 i 小于 请求数：
        若 请求队列 的 第 i 项 的 状态 等于 2：
            总耗时 为 总耗时 加 做 资源_获取耗时 用 i。
        完。
        i 为 i 加 1。
    完。
    返还 总耗时 除以 总完成。
完。

资源_总带宽 接受 零参：
    耗时 为 做 资源_平均耗时。
    若 耗时 等于 0：
        返还 0。
    完。
    返还 总字节 乘 1000000 除以 耗时。
完。

资源_诊断 接受 索引：
    若 索引 大于或等于 请求数：
        返还 「无效索引」。
    完。
    请求 为 请求队列 的 第 索引 项。
    返还 一本字典：类型 对应 做 资源_类型名 用 请求 的 类型、优先级 对应 做 资源_优先级名 用 请求 的 优先级、状态 对应 做 资源_状态名 用 请求 的 状态、字节 对应 请求 的 字节数、耗时 对应 做 资源_获取耗时 用 索引、重试 对应 请求 的 重试次数。
完。

HTTP_处理重定向 接受 loader、最大重定向数：
    可变 重定向次数 为 0。
    只要 重定向次数 小于 最大重定向数：
        状态码 为 loader 的 响应 的 状态码。
        若 状态码 不等于 301 并且 状态码 不等于 302 并且 状态码 不等于 303 并且 状态码 不等于 307 并且 状态码 不等于 308：
            返还。
        完。
        location 为 做 HTTP头部_获取 用 loader 的 响应 的 响应头、「Location」。
        若 location 等于 0：
            返还。
        完。
        新url_str 为 location。
        若 做 内存_读8 用 location 等于 0x2F：
            原url 为 loader 的 请求 的 url。
            前缀 为 做 字符串_连接 用 原url 的 协议、「://」。
            前缀 为 做 字符串_连接 用 前缀、原url 的 主机。
            新url_str 为 做 字符串_连接 用 前缀、location。
        完。
        若 状态码 等于 303：
            loader 的 请求 的 方法 为 HTTP_GET。
            loader 的 请求 的 请求体 为 0。
            loader 的 请求 的 请求体长度 为 0。
        完。
        若 (状态码 等于 301 或者 状态码 等于 302) 并且 loader 的 请求 的 方法 等于 HTTP_POST：
            loader 的 请求 的 方法 为 HTTP_GET。
            loader 的 请求 的 请求体 为 0。
            loader 的 请求 的 请求体长度 为 0。
        完。
        loader 的 请求 的 url 为 做 URL_解析 用 新url_str。
        loader 的 响应 为 做 ResourceResponse_创建。
        若 loader 的 套接字 大于或等于 0：
            做 网络_关闭 用 loader 的 套接字。
            loader 的 套接字 为 负 1。
        完。
        做 ResourceLoader_开始加载 用 loader。
        重定向次数 为 重定向次数 加 1。
    完。
完。

CORS_需要预检 接受 请求：
    若 请求 的 方法 等于 HTTP_GET 或者 请求 的 方法 等于 HTTP_HEAD：
        返还 假。
    完。
    若 请求 的 方法 等于 HTTP_POST：
        ct 为 做 HTTP头部_获取 用 请求 的 请求头、「Content-Type」。
        若 ct 不等于 0：
            若 做 字符串_相等_不区分大小写 用 ct、「application/x-www-form-urlencoded」 或者 做 字符串_相等_不区分大小写 用 ct、「multipart/form-data」 或者 做 字符串_相等_不区分大小写 用 ct、「text/plain」：
                返还 假。
            完。
        完。
    完。
    返还 真。
完。

CORS_发送预检 接受 loader：
    原方法 为 loader 的 请求 的 方法。
    loader 的 请求 的 方法 为 HTTP_OPTIONS。
    做 HTTP头部_添加 用 loader 的 请求 的 请求头、「Access-Control-Request-Method」、若 原方法 等于 HTTP_POST 则 「POST」 否则 若 原方法 等于 HTTP_PUT 则 「PUT」 否则 「DELETE」。
    做 HTTP头部_添加 用 loader 的 请求 的 请求头、「Origin」、loader 的 请求 的 来源。
    做 ResourceLoader_开始加载 用 loader。
    允许源 为 做 HTTP头部_获取 用 loader 的 响应 的 响应头、「Access-Control-Allow-Origin」。
    若 允许源 等于 0：
        返还 假。
    完。
    若 非 做 字符串_相等 用 允许源、「*」 并且 非 做 字符串_相等 用 允许源、loader 的 请求 的 来源：
        返还 假。
    完。
    返还 真。
完。

CSP_DEFAULT_SRC 为 0。

CSP_SCRIPT_SRC 为 1。

CSP_STYLE_SRC 为 2。

CSP_IMG_SRC 为 3。

CSP_FONT_SRC 为 4。

CSP_CONNECT_SRC 为 5。

CSP_MEDIA_SRC 为 6。

CSP_OBJECT_SRC 为 7。

CSP_FRAME_SRC 为 8。

CSP_BASE_URI 为 9。

CSP_FORM_ACTION 为 10。

CSP_FRAME_ANCESTORS 为 11。

CSP_WORKER_SRC 为 12。

CSP_NAVIGATE_TO 为 13。

CSP_DIRECTIVE_COUNT 为 14。

SRC_NONE 为 0x0001。

SRC_SELF 为 0x0002。

SRC_UNSAFE_INLINE 为 0x0004。

SRC_UNSAFE_EVAL 为 0x0008。

SRC_STRICT_DYNAMIC 为 0x0010。

SRC_HTTPS 为 0x0020。

SRC_HTTP 为 0x0040。

SRC_DATA 为 0x0080。

SRC_BLOB 为 0x0100。

SRC_MEDIASTREAM 为 0x0200。

SRC_WILDCARD 为 0x0400。

CSP_ENFORCE 为 0。

CSP_REPORT_ONLY 为 1。

CSPSourceExpr 是 一种 事物，含 标志、主机、主机数、nonce、nonce数、hash、hash数。

CSPDirective 是 一种 事物，含 类型、源。

CSPPolicy 是 一种 事物，含 指令、模式、报告URL、原始头、下一个。

CSPState 是 一种 事物，含 策略头、策略数、违规、违规数。

CSPViolation 是 一种 事物，含 指令、被阻止URL、文档URL、原始指令、时间戳、下一个。

取数_执行 接受 request：
    若 request 等于 0 或者 request 的 url 等于 0：
        返还 做 Response_error。
    完。
    若 request 的 signal 不等于 0 并且 request 的 signal 的 aborted：
        返还 做 Response_error。
    完。
    若 request 的 mode 等于 MODE_CORS：
        需要预检 为 做 取数_需要预检 用 request。
        若 需要预检：
            预检结果 为 做 取数_预检请求 用 request。
            若 预检结果 等于 0：
                返还 做 Response_error。
            完。
        完。
    完。
    若 request 的 cache 不等于 CACHE_NO_STORE 并且 request 的 cache 不等于 CACHE_RELOAD：
        cached 为 做 取数_查缓存 用 request。
        若 cached 不等于 0：
            若 request 的 cache 等于 CACHE_FORCE_CACHE 或者 request 的 cache 等于 CACHE_ONLY_IF_CACHED：
                返还 cached。
            完。
        完。
    完。
    resp 为 做 取数_网络请求 用 request。
    若 resp 等于 0：
        返还 做 Response_error。
    完。
    若 resp 的 status 大于或等于 300 并且 resp 的 status 小于 400：
        若 request 的 redirect 等于 REDIRECT_ERROR：
            返还 做 Response_error。
        完。
        若 request 的 redirect 等于 REDIRECT_MANUAL：
            resp 的 类型 为 RESP_OPAQUE_REDIRECT。
            返还 resp。
        完。
        可变 重定向次数 为 0。
        只要 resp 的 status 大于或等于 300 并且 resp 的 status 小于 400 并且 重定向次数 小于 20：
            location 为 做 Headers_get 用 resp 的 headers、「location」。
            若 location 等于 0：
                跳出。
            完。
            request 的 url 为 location。
            request 的 已发送 为 假。
            resp 为 做 取数_网络请求 用 request。
            若 resp 等于 0：
                返还 做 Response_error。
            完。
            重定向次数 为 重定向次数 加 1。
        完。
        若 重定向次数 大于 0：
            resp 的 redirected 为 真。
        完。
    完。
    若 request 的 mode 等于 MODE_CORS：
        resp 的 类型 为 RESP_CORS。
    否则若 request 的 mode 等于 MODE_NO_CORS：
        resp 的 类型 为 RESP_OPAQUE。
    完。
    若 request 的 cache 不等于 CACHE_NO_STORE：
        做 取数_存缓存 用 request、resp。
    完。
    返还 resp。
完。

取数_需要预检 接受 request：
    若 request 等于 0：
        返还 假。
    完。
    若 request 的 method 不等于 METHOD_GET 并且 request 的 method 不等于 METHOD_HEAD 并且 request 的 method 不等于 METHOD_POST：
        返还 真。
    完。
    返还 假。
完。

取数_预检请求 接受 request：
    若 request 等于 0：
        返还 0。
    完。
    allow_origin 为 做 Headers_获取 用 preflight_resp 的 头、「Access-Control-Allow-Origin」。
    若 allow_origin 等于 0：
        返还 0。
    完。
    若 做 字符串_相等 用 allow_origin、「*」：
        返还 1。
    完。
    若 做 字符串_相等 用 allow_origin、request 的 来源：
        返还 1。
    完。
    返还 0。
完。

取数_网络请求 接受 request：
    若 request 等于 0：
        返还 0。
    完。
    h 为 做 Headers_创建。
    body 为 做 Body_创建 用 0、0。
    resp 为 做 Response_创建 用 body、200、「OK」、h。
    resp 的 url 为 request 的 url。
    返还 resp。
完。

取数_查缓存 接受 request：
    若 request 等于 0 或者 request 的 url 等于 0：
        返还 0。
    完。
    entry 为 做 HTTP缓存_查找 用 request 的 url。
    若 entry 不等于 0 并且 做 HTTP缓存_新鲜 用 entry：
        返还 entry 的 响应。
    完。
    返还 0。
完。

取数_存缓存 接受 request、response：
    做 HTTP缓存_存储 用 request 的 url、response。
完。

取数Headers 是 一种 事物，含 条目、数量、容量、保护级别。

取数Headers_创建 接受 零参：
    h 为 做 堆_分配 用 取数Headers。
    h 的 容量 为 32。
    h 的 条目 为 做 堆_分配_数组 用 h 的 容量 乘 16。
    h 的 数量 为 0。
    h 的 保护级别 为 0。
    返还 h。
完。

取数Headers_append 接受 h、名、值：
    若 h 的 保护级别 等于 4：
        返还。
    完。
    若 h 的 数量 大于或等于 h 的 容量：
        返还。
    完。
    规范名 为 做 字符串_小写化 用 名。
    若 h 的 保护级别 等于 1 并且 做 取数Headers_是禁止头 用 规范名：
        返还。
    完。
    偏移 为 h 的 条目 加 h 的 数量 乘 16。
    做 内存_写64 用 偏移、规范名。
    做 内存_写64 用 偏移 加 8、做 字符串_复制 用 值。
    h 的 数量 为 h 的 数量 加 1。
完。

取数Headers_set 接受 h、名、值：
    规范名 为 做 字符串_小写化 用 名。
    做 取数Headers_delete 用 h、规范名。
    做 取数Headers_append 用 h、规范名、值。
完。

取数Headers_get 接受 h、名：
    规范名 为 做 字符串_小写化 用 名。
    可变 i 为 0。
    只要 i 小于 h 的 数量：
        偏移 为 h 的 条目 加 i 乘 16。
        若 做 字符串_相等 用 做 内存_读64 用 偏移、规范名：
            返还 做 内存_读64 用 偏移 加 8。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

取数Headers_has 接受 h、名：
    返还 做 取数Headers_get 用 h、名 不等于 0。
完。

取数Headers_delete 接受 h、名：
    规范名 为 做 字符串_小写化 用 名。
    可变 w 为 0。
    可变 i 为 0。
    只要 i 小于 h 的 数量：
        偏移 为 h 的 条目 加 i 乘 16。
        若 非 做 字符串_相等 用 做 内存_读64 用 偏移、规范名：
            若 w 不等于 i：
                做 内存_复制 用 h 的 条目 加 w 乘 16、h 的 条目 加 i 乘 16、16。
            完。
            w 为 w 加 1。
        完。
        i 为 i 加 1。
    完。
    h 的 数量 为 w。
完。

取数Headers_是禁止头 接受 名：
    若 做 字符串_相等 用 名、「host」：
        返还 真。
    完。
    若 做 字符串_相等 用 名、「content-length」：
        返还 真。
    完。
    若 做 字符串_相等 用 名、「connection」：
        返还 真。
    完。
    若 做 字符串_相等 用 名、「cookie」：
        返还 真。
    完。
    若 做 字符串_相等 用 名、「origin」：
        返还 真。
    完。
    若 做 字符串_相等 用 名、「referer」：
        返还 真。
    完。
    返还 假。
完。

字符串_小写化 接受 str：
    若 str 等于 0：
        返还 0。
    完。
    长度 为 做 字符串_长度 用 str。
    结果 为 做 堆_分配 用 长度 加 1。
    可变 i 为 0。
    只要 i 小于 长度：
        c 为 做 内存_读8 用 str 加 i。
        若 c 大于或等于 0x41 并且 c 小于或等于 0x5A：
            c 为 c 加 0x20。
        完。
        做 内存_写8 用 结果 加 i、c。
        i 为 i 加 1。
    完。
    做 内存_写8 用 结果 加 长度、0。
    返还 结果。
完。

取数Response 是 一种 事物，含 status、statusText、headers、body、类型、url、redirected、ok。

Response_创建 接受 body、status、statusText、headers：
    resp 为 做 堆_分配 用 取数Response。
    resp 的 status 为 status。
    resp 的 statusText 为 statusText。
    resp 的 headers 为 若 headers 不等于 0 则 headers 否则 做 Headers_创建。
    resp 的 body 为 body。
    resp 的 类型 为 RESP_BASIC。
    resp 的 url 为 0。
    resp 的 redirected 为 假。
    resp 的 ok 为 status 大于或等于 200 并且 status 小于或等于 299。
    返还 resp。
完。

Response_error 接受 零参：
    resp 为 做 Response_创建 用 0、0、「」、0。
    resp 的 类型 为 RESP_ERROR。
    返还 resp。
完。

Response_redirect 接受 url、status：
    若 status 不等于 301 并且 status 不等于 302 并且 status 不等于 303 并且 status 不等于 307 并且 status 不等于 308：
        status 为 302。
    完。
    h 为 做 Headers_创建。
    做 Headers_set 用 h、「Location」、url。
    返还 做 Response_创建 用 0、status、「」、h。
完。

Response_json 接受 json字符串、status：
    body 为 做 Body_创建 用 json字符串、做 字节长度 用 json字符串。
    body 的 contentType 为 「application/json」。
    h 为 做 Headers_创建。
    做 Headers_set 用 h、「Content-Type」、「application/json」。
    返还 做 Response_创建 用 body、status、「OK」、h。
完。

Response_clone 接受 resp：
    若 resp 等于 0：
        返还 0。
    完。
    新body 为 做 Body_创建 用 resp 的 body 的 数据、resp 的 body 的 长度。
    新resp 为 做 Response_创建 用 新body、resp 的 status、resp 的 statusText、resp 的 headers。
    新resp 的 类型 为 resp 的 类型。
    新resp 的 url 为 resp 的 url。
    新resp 的 redirected 为 resp 的 redirected。
    返还 新resp。
完。

AbortSignal 是 一种 事物，含 aborted、reason、onabort、监听器、监听器数。

AbortController 是 一种 事物，含 signal。

AbortController_创建 接受 零参：
    ac 为 做 堆_分配 用 AbortController。
    sig 为 做 堆_分配 用 AbortSignal。
    sig 的 aborted 为 假。
    sig 的 reason 为 0。
    sig 的 onabort 为 0。
    sig 的 监听器 为 0。
    sig 的 监听器数 为 0。
    ac 的 signal 为 sig。
    返还 ac。
完。

AbortController_abort 接受 ac、reason：
    若 ac 等于 0 或者 ac 的 signal 等于 0：
        返还。
    完。
    sig 为 ac 的 signal。
    若 sig 的 aborted：
        返还。
    完。
    sig 的 aborted 为 真。
    sig 的 reason 为 reason。
    若 sig 的 onabort 不等于 0：
    完。
完。

AbortSignal_timeout 接受 ms：
    sig 为 做 堆_分配 用 AbortSignal。
    sig 的 aborted 为 假。
    sig 的 reason 为 0。
    sig 的 onabort 为 0。
    sig 的 监听器 为 0。
    sig 的 监听器数 为 0。
    返还 sig。
完。

AbortSignal_any 接受 signals、数量：
    sig 为 做 堆_分配 用 AbortSignal。
    sig 的 aborted 为 假。
    sig 的 reason 为 0。
    sig 的 onabort 为 0。
    可变 i 为 0。
    只要 i 小于 数量：
        s 为 做 内存_读64 用 signals 加 i 乘 8。
        若 s 不等于 0 并且 s 的 aborted：
            sig 的 aborted 为 真。
            sig 的 reason 为 s 的 reason。
            返还 sig。
        完。
        i 为 i 加 1。
    完。
    返还 sig。
完。

本章 收束。
