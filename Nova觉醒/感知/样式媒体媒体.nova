《样式媒体媒体》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·内核打印》。

引用《..·中枢·整数转字符串》。

引用《..·推理·定点Q算术》。

引用《..·中枢·辅助存根》。

引用《..·中枢·字符串替换》。

引用《.·历史与位置接口》。

媒体类型_全部 为 0。

媒体类型_屏幕 为 1。

媒体类型_打印 为 2。

媒体类型_语音 为 3。

特征_宽度 为 1。

特征_高度 为 2。

特征_最小宽度 为 3。

特征_最大宽度 为 4。

特征_最小高度 为 5。

特征_最大高度 为 6。

特征_方向 为 7。

特征_宽高比 为 8。

特征_分辨率 为 9。

特征_颜色位数 为 10。

特征_颜色索引 为 11。

特征_单色 为 12。

特征_悬停 为 13。

特征_指针 为 14。

特征_首选颜色方案 为 15。

特征_首选减少动画 为 16。

特征_首选对比度 为 17。

特征_显示模式 为 18。

特征_设备像素比 为 19。

比较_等于 为 0。

比较_大于等于 为 1。

比较_小于等于 为 2。

逻辑_AND 为 0。

逻辑_OR 为 1。

逻辑_NOT 为 2。

逻辑_ONLY 为 3。

方向_横屏 为 0。

方向_竖屏 为 1。

单位_PX 为 0。

单位_EM 为 1。

单位_REM 为 2。

单位_VW 为 3。

单位_VH 为 4。

单位_DPI 为 5。

单位_DPPX 为 6。

MQL_最大对象数 为 256。

MQL_最大监听器数 为 32。

MQL_最大条件数 为 16。

MRLPT_特征维度 为 10。

MRLPT_断点历史大小 为 32。

MRLPT_预测阈值 为 0.68。

媒体条件 是 一种 事物，含 特征类型、比较操作、值、值_字符串、单位。

媒体查询 是 一种 事物，含 媒体类型、修饰符、条件数组、条件数量。

媒体查询列表 是 一种 事物，含 查询数组、查询数量、原始字符串。

媒体QueryListObject 是 一种 事物，含 查询字符串、解析后、当前匹配、监听器列表、监听器数量、监听器容量、Nova运行时、已销毁、求值次数、上次变化时间戳。

可变 MQL_全局列表 为 做 数组_创建 用 MQL_最大对象数、0。

可变 MQL_全局数量 为 0。

可变 MQL_总创建数 为 0。

可变 MQL_总求值数 为 0。

可变 MQL_总变化数 为 0。

可变 视口_宽度 为 1920。

可变 视口_高度 为 1080。

可变 设备_宽度 为 1920。

可变 设备_高度 为 1080。

可变 设备_像素比 为 1.0。

可变 设备_颜色位数 为 24。

可变 设备_颜色索引 为 16777216。

可变 设备_分辨率_DPI 为 96。

可变 设备_单色位 为 0。

可变 设备_支持悬停 为 真。

可变 设备_指针类型 为 「fine」。

可变 首选_颜色方案 为 「light」。

可变 首选_减少动画 为 「no-preference」。

可变 首选_对比度 为 「no-preference」。

可变 显示_模式 为 「browser」。

可变 当前_媒体类型 为 媒体类型_屏幕。

可变 根字体大小_PX 为 16.0。

单位转换到PX 接受 值、单位：
    做 匹配 用 单位。
完。

分辨率转换到DPI 接受 值、单位：
    做 匹配 用 单位。
完。

解析_媒体类型 接受 文本：
    若 做 字符串_相等_忽略大小写 用 文本、「all」：
        返还 媒体类型_全部。
    完。
    若 做 字符串_相等_忽略大小写 用 文本、「screen」：
        返还 媒体类型_屏幕。
    完。
    若 做 字符串_相等_忽略大小写 用 文本、「print」：
        返还 媒体类型_打印。
    完。
    若 做 字符串_相等_忽略大小写 用 文本、「speech」：
        返还 媒体类型_语音。
    完。
    返还 负 1。
完。

解析_媒体特征名 接受 名称：
    可变 特征 为 负 1。
    可变 比较 为 比较_等于。
    可变 实际名 为 名称。
    若 做 字符串_开头 用 名称、「min-」：
        比较 为 比较_大于等于。
        实际名 为 做 字符串_子串 用 名称、4。
    否则若 做 字符串_开头 用 名称、「max-」：
        比较 为 比较_小于等于。
        实际名 为 做 字符串_子串 用 名称、4。
    完。
    若 做 字符串_相等_忽略大小写 用 实际名、「width」：
        特征 为 特征_宽度。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「height」：
        特征 为 特征_高度。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「orientation」：
        特征 为 特征_方向。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「aspect-ratio」：
        特征 为 特征_宽高比。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「resolution」：
        特征 为 特征_分辨率。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「color」：
        特征 为 特征_颜色位数。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「color-index」：
        特征 为 特征_颜色索引。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「monochrome」：
        特征 为 特征_单色。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「hover」：
        特征 为 特征_悬停。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「pointer」：
        特征 为 特征_指针。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「prefers-color-scheme」：
        特征 为 特征_首选颜色方案。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「prefers-reduced-motion」：
        特征 为 特征_首选减少动画。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「prefers-contrast」：
        特征 为 特征_首选对比度。
    否则若 做 字符串_相等_忽略大小写 用 实际名、「display-mode」：
        特征 为 特征_显示模式。
    否则若 做 字符串_包含 用 实际名、「device-pixel-ratio」：
        特征 为 特征_设备像素比。
    完。
    返还 一本字典：特征类型 对应 特征、比较操作 对应 比较。
完。

解析_媒体条件 接受 文本：
    去括号 为 做 字符串_去括号 用 做 字符串_trim 用 文本。
    冒号位 为 做 字符串_查找 用 去括号、「:」。
    若 冒号位 小于 0：
        解析结果 为 做 解析_媒体特征名 用 做 字符串_trim 用 去括号。
        返还 一本字典：特征类型 对应 解析结果 的 特征类型、比较操作 对应 比较_大于等于、值 对应 0、值_字符串 对应 「」、单位 对应 单位_PX。
    完。
    名称 为 做 字符串_trim 用 做 字符串_子串_范围 用 去括号、0、冒号位。
    值文本 为 做 字符串_trim 用 做 字符串_子串 用 去括号、冒号位 加 1。
    解析结果 为 做 解析_媒体特征名 用 名称。
    可变 数值 为 0.0。
    可变 单位 为 单位_PX。
    若 做 字符串_以结尾 用 值文本、「px」：
        数值 为 做 字符串_到浮点 用 做 字符串_子串_范围 用 值文本、0、做 字符串_长度 用 值文本 减 2。
        单位 为 单位_PX。
    否则若 做 字符串_以结尾 用 值文本、「em」：
        数值 为 做 字符串_到浮点 用 做 字符串_子串_范围 用 值文本、0、做 字符串_长度 用 值文本 减 2。
        单位 为 单位_EM。
    否则若 做 字符串_以结尾 用 值文本、「rem」：
        数值 为 做 字符串_到浮点 用 做 字符串_子串_范围 用 值文本、0、做 字符串_长度 用 值文本 减 3。
        单位 为 单位_REM。
    否则若 做 字符串_以结尾 用 值文本、「dpi」：
        数值 为 做 字符串_到浮点 用 做 字符串_子串_范围 用 值文本、0、做 字符串_长度 用 值文本 减 3。
        单位 为 单位_DPI。
    否则若 做 字符串_以结尾 用 值文本、「dppx」：
        数值 为 做 字符串_到浮点 用 做 字符串_子串_范围 用 值文本、0、做 字符串_长度 用 值文本 减 4。
        单位 为 单位_DPPX。
    否则：
        试数字 为 做 字符串_到浮点_安全 用 值文本。
        若 试数字 的 成功：
            数值 为 试数字 的 值。
        完。
    完。
    返还 一本字典：特征类型 对应 解析结果 的 特征类型、比较操作 对应 解析结果 的 比较操作、值 对应 做 单位转换到PX 用 数值、单位、值_字符串 对应 值文本、单位 对应 单位。
完。

媒体查询_解析 接受 查询文本：
    结果 为 做 堆_分配 用 媒体查询列表。
    结果 的 原始字符串 为 做 字符串_复制 用 查询文本。
    结果 的 查询数组 为 做 堆_分配_数组 用 8 乘 48。
    结果 的 查询数量 为 0。
    片段数组 为 做 字符串_分割 用 查询文本、「,」。
    可变 qi 为 0。
    只要 qi 小于 片段数组 的 长度 并且 qi 小于 8：
        片段 为 做 字符串_trim 用 片段数组 的 第 qi 项。
        若 做 字符串_长度 用 片段 大于 0：
            查询 为 做 解析_单个媒体查询 用 片段。
            结果 的 查询数组 的 第 结果 的 查询数量 项 为 查询。
            结果 的 查询数量 为 结果 的 查询数量 加 1。
        完。
        qi 为 qi 加 1。
    完。
    返还 结果。
完。

解析_单个媒体查询 接受 文本：
    查询 为 做 堆_分配 用 媒体查询。
    查询 的 媒体类型 为 媒体类型_全部。
    查询 的 修饰符 为 负 1。
    查询 的 条件数组 为 做 堆_分配_数组 用 MQL_最大条件数 乘 40。
    查询 的 条件数量 为 0。
    可变 剩余 为 做 字符串_trim 用 文本。
    若 做 字符串_开头_忽略大小写 用 剩余、「not 」：
        查询 的 修饰符 为 逻辑_NOT。
        剩余 为 做 字符串_trim 用 做 字符串_子串 用 剩余、4。
    否则若 做 字符串_开头_忽略大小写 用 剩余、「only 」：
        查询 的 修饰符 为 逻辑_ONLY。
        剩余 为 做 字符串_trim 用 做 字符串_子串 用 剩余、5。
    完。
    部分 为 做 字符串_分割_忽略大小写 用 剩余、「 and 」。
    可变 pi 为 0。
    只要 pi 小于 部分 的 长度：
        段 为 做 字符串_trim 用 部分 的 第 pi 项。
        若 做 字符串_包含 用 段、「(」：
            若 查询 的 条件数量 小于 MQL_最大条件数：
                查询 的 条件数组 的 第 查询 的 条件数量 项 为 做 解析_媒体条件 用 段。
                查询 的 条件数量 为 查询 的 条件数量 加 1。
            完。
        否则：
            类型 为 做 解析_媒体类型 用 段。
            若 类型 大于或等于 0：
                查询 的 媒体类型 为 类型。
            完。
        完。
        pi 为 pi 加 1。
    完。
    返还 查询。
完。

获取_媒体特征值 接受 特征类型：
    做 匹配 用 特征类型。
完。

求值_媒体条件 接受 条件：
    特征 为 条件 的 特征类型。
    若 特征 等于 特征_方向：
        若 视口_宽度 大于或等于 视口_高度：
            返还 做 字符串_相等_忽略大小写 用 条件 的 值_字符串、「landscape」。
        否则：
            返还 做 字符串_相等_忽略大小写 用 条件 的 值_字符串、「portrait」。
        完。
    完。
    若 特征 等于 特征_首选颜色方案：
        返还 做 字符串_相等_忽略大小写 用 条件 的 值_字符串、首选_颜色方案。
    完。
    若 特征 等于 特征_首选减少动画：
        返还 做 字符串_相等_忽略大小写 用 条件 的 值_字符串、首选_减少动画。
    完。
    若 特征 等于 特征_首选对比度：
        返还 做 字符串_相等_忽略大小写 用 条件 的 值_字符串、首选_对比度。
    完。
    若 特征 等于 特征_显示模式：
        返还 做 字符串_相等_忽略大小写 用 条件 的 值_字符串、显示_模式。
    完。
    若 特征 等于 特征_悬停：
        若 设备_支持悬停：
            返还 做 字符串_相等_忽略大小写 用 条件 的 值_字符串、「hover」。
        否则：
            返还 做 字符串_相等_忽略大小写 用 条件 的 值_字符串、「none」。
        完。
    完。
    若 特征 等于 特征_指针：
        返还 做 字符串_相等_忽略大小写 用 条件 的 值_字符串、设备_指针类型。
    完。
    当前值 为 做 获取_媒体特征值 用 特征。
    目标值 为 条件 的 值。
    做 匹配 用 条件 的 比较操作。
完。

求值_媒体查询 接受 查询：
    若 查询 的 媒体类型 不等于 媒体类型_全部 并且 查询 的 媒体类型 不等于 当前_媒体类型：
        基础结果 为 假。
        若 查询 的 修饰符 等于 逻辑_NOT：
            返还 非 基础结果。
        完。
        返还 基础结果。
    完。
    可变 i 为 0。
    只要 i 小于 查询 的 条件数量：
        若 非 做 求值_媒体条件 用 查询 的 条件数组 的 第 i 项：
            基础结果 为 假。
            若 查询 的 修饰符 等于 逻辑_NOT：
                返还 非 基础结果。
            完。
            返还 基础结果。
        完。
        i 为 i 加 1。
    完。
    基础结果 为 真。
    若 查询 的 修饰符 等于 逻辑_NOT：
        返还 非 基础结果。
    完。
    返还 基础结果。
完。

求值_媒体查询列表 接受 列表：
    若 列表 等于 0 或者 列表 的 查询数量 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 列表 的 查询数量：
        若 做 求值_媒体查询 用 列表 的 查询数组 的 第 i 项：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

Window_match媒体 接受 查询字符串：
    若 MQL_全局数量 大于或等于 MQL_最大对象数：
        做 内核打印 用 「[媒体查询] MQL对象池已满({}/{})」、MQL_全局数量、MQL_最大对象数。
        返还 0。
    完。
    obj 为 做 堆_分配 用 媒体QueryListObject。
    obj 的 查询字符串 为 做 字符串_复制 用 查询字符串。
    obj 的 解析后 为 做 媒体查询_解析 用 查询字符串。
    obj 的 监听器容量 为 MQL_最大监听器数。
    obj 的 监听器列表 为 做 堆_分配_数组 用 obj 的 监听器容量 乘 8。
    obj 的 监听器数量 为 0。
    obj 的 Nova运行时 为 0。
    obj 的 已销毁 为 假。
    obj 的 求值次数 为 0。
    obj 的 上次变化时间戳 为 0。
    obj 的 当前匹配 为 做 求值_媒体查询列表 用 obj 的 解析后。
    obj 的 求值次数 为 1。
    MQL_全局列表 的 第 MQL_全局数量 项 为 obj。
    MQL_全局数量 为 MQL_全局数量 加 1。
    MQL_总创建数 为 MQL_总创建数 加 1。
    返还 obj。
完。

媒体QueryListObject_matches 接受 obj：
    若 obj 等于 0 或者 obj 的 已销毁：
        返还 假。
    完。
    返还 obj 的 当前匹配。
完。

媒体QueryListObject_media 接受 obj：
    若 obj 等于 0：
        返还 「」。
    完。
    返还 obj 的 查询字符串。
完。

媒体QueryListObject_addEventListener 接受 obj、事件名、回调：
    若 obj 等于 0 或者 obj 的 已销毁：
        返还。
    完。
    若 非 做 字符串_相等 用 事件名、「change」：
        返还。
    完。
    若 obj 的 监听器数量 大于或等于 obj 的 监听器容量：
        新容量 为 obj 的 监听器容量 乘 2。
        新列表 为 做 堆_分配_数组 用 新容量 乘 8。
        可变 i 为 0。
        只要 i 小于 obj 的 监听器数量：
            做 内存_写64 用 新列表 加 i 乘 8、做 内存_读64 用 obj 的 监听器列表 加 i 乘 8。
            i 为 i 加 1。
        完。
        做 堆_释放 用 obj 的 监听器列表。
        obj 的 监听器列表 为 新列表。
        obj 的 监听器容量 为 新容量。
    完。
    可变 j 为 0。
    只要 j 小于 obj 的 监听器数量：
        若 做 内存_读64 用 obj 的 监听器列表 加 j 乘 8 等于 回调：
            返还。
        完。
        j 为 j 加 1。
    完。
    做 内存_写64 用 obj 的 监听器列表 加 obj 的 监听器数量 乘 8、回调。
    obj 的 监听器数量 为 obj 的 监听器数量 加 1。
完。

媒体QueryListObject_removeEventListener 接受 obj、事件名、回调：
    若 obj 等于 0 或者 obj 的 已销毁：
        返还。
    完。
    若 非 做 字符串_相等 用 事件名、「change」：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 obj 的 监听器数量：
        若 做 内存_读64 用 obj 的 监听器列表 加 i 乘 8 等于 回调：
            obj 的 监听器数量 为 obj 的 监听器数量 减 1。
            若 i 小于 obj 的 监听器数量：
                做 内存_写64 用 obj 的 监听器列表 加 i 乘 8、做 内存_读64 用 obj 的 监听器列表 加 obj 的 监听器数量 乘 8。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

媒体QueryListObject_销毁 接受 obj：
    若 obj 等于 0 或者 obj 的 已销毁：
        返还。
    完。
    obj 的 已销毁 为 真。
    若 obj 的 监听器列表 不等于 0：
        做 堆_释放 用 obj 的 监听器列表。
    完。
    若 obj 的 解析后 不等于 0：
        做 堆_释放 用 obj 的 解析后。
    完。
    若 obj 的 查询字符串 不等于 0：
        做 字符串_释放 用 obj 的 查询字符串。
    完。
    可变 i 为 0。
    只要 i 小于 MQL_全局数量：
        若 MQL_全局列表 的 第 i 项 等于 obj：
            MQL_全局数量 为 MQL_全局数量 减 1。
            MQL_全局列表 的 第 i 项 为 MQL_全局列表 的 第 MQL_全局数量 项。
            MQL_全局列表 的 第 MQL_全局数量 项 为 0。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

媒体查询_更新视口 接受 新宽、新高：
    宽变了 为 新宽 不等于 视口_宽度。
    高变了 为 新高 不等于 视口_高度。
    若 非 宽变了 并且 非 高变了：
        返还。
    完。
    视口_宽度 为 新宽。
    视口_高度 为 新高。
    做 MRLPT_记录视口变化 用 新宽、新高。
    做 媒体Query_视口变化通知。
完。

媒体查询_更新首选设置 接受 设置名、新值：
    可变 已变化 为 假。
    若 做 字符串_相等 用 设置名、「color-scheme」：
        若 非 做 字符串_相等 用 首选_颜色方案、新值：
            首选_颜色方案 为 新值。
            已变化 为 真。
        完。
    否则若 做 字符串_相等 用 设置名、「reduced-motion」：
        若 非 做 字符串_相等 用 首选_减少动画、新值：
            首选_减少动画 为 新值。
            已变化 为 真。
        完。
    否则若 做 字符串_相等 用 设置名、「contrast」：
        若 非 做 字符串_相等 用 首选_对比度、新值：
            首选_对比度 为 新值。
            已变化 为 真。
        完。
    完。
    若 已变化：
        做 媒体Query_视口变化通知。
    完。
完。

媒体Query_视口变化通知 接受 零参：
    当前时间戳 为 做 系统_获取时间戳。
    可变 变化数 为 0。
    可变 i 为 0。
    只要 i 小于 MQL_全局数量：
        obj 为 MQL_全局列表 的 第 i 项。
        若 obj 不等于 0 并且 非 obj 的 已销毁：
            旧匹配 为 obj 的 当前匹配。
            新匹配 为 做 求值_媒体查询列表 用 obj 的 解析后。
            obj 的 求值次数 为 obj 的 求值次数 加 1。
            MQL_总求值数 为 MQL_总求值数 加 1。
            若 新匹配 不等于 旧匹配：
                obj 的 当前匹配 为 新匹配。
                obj 的 上次变化时间戳 为 当前时间戳。
                变化数 为 变化数 加 1。
                可变 j 为 0。
                只要 j 小于 obj 的 监听器数量：
                    回调 为 做 内存_读64 用 obj 的 监听器列表 加 j 乘 8。
                    若 回调 不等于 0 并且 obj 的 Nova运行时 不等于 0：
                        事件 为 一本字典：matches 对应 新匹配、media 对应 obj 的 查询字符串、类型 对应 「change」。
                        做 Nova函数_Call_带参 用 obj 的 Nova运行时、回调、事件。
                    完。
                    j 为 j 加 1。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    MQL_总变化数 为 MQL_总变化数 加 变化数。
完。

可变 MRLPT_视口历史_宽 为 做 数组_创建 用 MRLPT_断点历史大小、0。

可变 MRLPT_视口历史_高 为 做 数组_创建 用 MRLPT_断点历史大小、0。

可变 MRLPT_历史写指针 为 0。

可变 MRLPT_预测命中 为 0。

可变 MRLPT_预测未中 为 0。

MRLPT_收集断点 接受 零参：
    可变 断点 为 做 数组_创建 用 64、0。
    可变 断点数 为 0。
    可变 i 为 0。
    只要 i 小于 MQL_全局数量 并且 断点数 小于 64：
        obj 为 MQL_全局列表 的 第 i 项。
        若 obj 不等于 0 并且 非 obj 的 已销毁 并且 obj 的 解析后 不等于 0：
            列表 为 obj 的 解析后。
            可变 qi 为 0。
            只要 qi 小于 列表 的 查询数量：
                查询 为 列表 的 查询数组 的 第 qi 项。
                可变 ci 为 0。
                只要 ci 小于 查询 的 条件数量：
                    条件 为 查询 的 条件数组 的 第 ci 项。
                    若 条件 的 特征类型 等于 特征_宽度 或者 条件 的 特征类型 等于 特征_最小宽度 或者 条件 的 特征类型 等于 特征_最大宽度：
                        可变 重复 为 假。
                        可变 di 为 0。
                        只要 di 小于 断点数：
                            若 断点 的 第 di 项 等于 条件 的 值：
                                重复 为 真。
                            完。
                            di 为 di 加 1。
                        完。
                        若 非 重复 并且 断点数 小于 64：
                            断点 的 第 断点数 项 为 条件 的 值。
                            断点数 为 断点数 加 1。
                        完。
                    完。
                    ci 为 ci 加 1。
                完。
                qi 为 qi 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 一本字典：断点 对应 断点、数量 对应 断点数。
完。

MRLPT_记录视口变化 接受 宽、高：
    idx 为 MRLPT_历史写指针 取余于 MRLPT_断点历史大小。
    MRLPT_视口历史_宽 的 第 idx 项 为 宽。
    MRLPT_视口历史_高 的 第 idx 项 为 高。
    MRLPT_历史写指针 为 MRLPT_历史写指针 加 1。
完。

MRLPT_构建特征 接受 零参：
    可变 特征 为 做 数组_创建 用 MRLPT_特征维度、0.0。
    特征 的 第 0 项 为 视口_宽度。
    特征 的 第 1 项 为 视口_高度。
    若 MRLPT_历史写指针 大于或等于 2：
        大小 为 MRLPT_断点历史大小。
        最新 为 (MRLPT_历史写指针 减 1) 取余于 大小。
        次新 为 (MRLPT_历史写指针 减 2) 取余于 大小。
        特征 的 第 2 项 为 MRLPT_视口历史_宽 的 第 最新 项 减 MRLPT_视口历史_宽 的 第 次新 项。
        特征 的 第 3 项 为 MRLPT_视口历史_高 的 第 最新 项 减 MRLPT_视口历史_高 的 第 次新 项。
    完。
    断点信息 为 做 MRLPT_收集断点。
    可变 最近距离 为 99999.0。
    可变 最近断点值 为 0.0。
    可变 bi 为 0。
    只要 bi 小于 断点信息 的 数量：
        距离 为 断点信息 的 断点 的 第 bi 项 减 视口_宽度。
        若 距离 小于 0：
            距离 为 负 距离。
        完。
        若 距离 小于 最近距离：
            最近距离 为 距离。
            最近断点值 为 断点信息 的 断点 的 第 bi 项。
        完。
        bi 为 bi 加 1。
    完。
    特征 的 第 4 项 为 最近距离。
    特征 的 第 5 项 为 最近断点值。
    特征 的 第 6 项 为 断点信息 的 数量。
    若 视口_高度 大于 0：
        特征 的 第 7 项 为 视口_宽度 除以 视口_高度。
    完。
    特征 的 第 8 项 为 设备_像素比。
    特征 的 第 9 项 为 MQL_全局数量。
    返还 特征。
完。

MRLPT_预测断点跳跃 接受 零参：
    若 MRLPT_历史写指针 小于 3：
        返还 空。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 空。
    完。
    特征 为 做 MRLPT_构建特征。
    张量 为 做 Tensor_创建 用 1、MRLPT_特征维度。
    可变 i 为 0。
    只要 i 小于 MRLPT_特征维度：
        做 Tensor_写 用 张量、0、i、特征 的 第 i 项。
        i 为 i 加 1。
    完。
    结果 为 做 AI总线_推理 用 「媒体查询_断点预测」、张量。
    做 Tensor_释放 用 张量。
    若 结果 的 置信度 小于 MRLPT_预测阈值：
        返还 空。
    完。
    返还 一本字典：即将跳跃 对应 结果 的 输出值 大于 0.5、目标断点 对应 特征 的 第 5 项、置信度 对应 结果 的 置信度。
完。

智能_CSS媒体Query_AI样式意图 接受 属性ID、值：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    张量 为 做 Tensor_创建 用 1、4。
    做 Tensor_写 用 张量、0、0、属性ID。
    做 Tensor_写 用 张量、0、1、值。
    做 Tensor_写 用 张量、0、2、视口_宽度。
    做 Tensor_写 用 张量、0、3、视口_高度。
    结果 为 做 AI总线_推理 用 「样式意图推断」、张量。
    做 Tensor_释放 用 张量。
    返还 结果 的 输出值。
完。

智能_CSS媒体Query_AI影响分析 接受 属性ID：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 2。
    完。
    张量 为 做 Tensor_创建 用 1、3。
    做 Tensor_写 用 张量、0、0、属性ID。
    做 Tensor_写 用 张量、0、1、视口_宽度。
    做 Tensor_写 用 张量、0、2、视口_高度。
    结果 为 做 AI总线_推理 用 「样式影响分析」、张量。
    做 Tensor_释放 用 张量。
    返还 结果 的 输出值。
完。

媒体查询_打印统计 接受 零参：
    做 内核打印 用 「═══ 媒体查询引擎统计 ═══」。
    做 内核打印 用 「  视口: {}x{} 像素比={} 方向={}」、视口_宽度、视口_高度、设备_像素比、若 视口_宽度 大于或等于 视口_高度 则 「landscape」 否则 「portrait」。
    做 内核打印 用 「  MQL对象: 活跃={} 总创建={}」、MQL_全局数量、MQL_总创建数。
    做 内核打印 用 「  求值次数: {} 变化事件: {}」、MQL_总求值数、MQL_总变化数。
    做 内核打印 用 「  首选: 颜色={}  动画={} 对比度={}」、首选_颜色方案、首选_减少动画、首选_对比度。
    若 MRLPT_预测命中 加 MRLPT_预测未中 大于 0：
        命中率 为 MRLPT_预测命中 乘 100 除以 (MRLPT_预测命中 加 MRLPT_预测未中)。
        做 内核打印 用 「  AI断点预测(MRLPT): 命中={} 未中={} 率={}%」、MRLPT_预测命中、MRLPT_预测未中、命中率。
    完。
    可变 i 为 0。
    只要 i 小于 MQL_全局数量：
        obj 为 MQL_全局列表 的 第 i 项。
        若 obj 不等于 0 并且 非 obj 的 已销毁：
            做 内核打印 用 「  MQL[{}]: \"{}\" matches={} 监听器={} 求值数={}」、i、obj 的 查询字符串、obj 的 当前匹配、obj 的 监听器数量、obj 的 求值次数。
        完。
        i 为 i 加 1。
    完。
完。

媒体查询_获取统计 接受 零参：
    返还 一本字典：视口宽 对应 视口_宽度、视口高 对应 视口_高度、活跃MQL 对应 MQL_全局数量、总创建 对应 MQL_总创建数、总求值 对应 MQL_总求值数、总变化 对应 MQL_总变化数、颜色方案 对应 首选_颜色方案、AI命中 对应 MRLPT_预测命中、AI未中 对应 MRLPT_预测未中。
完。

本章 收束。
