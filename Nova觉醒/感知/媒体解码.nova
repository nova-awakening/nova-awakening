《媒体解码》

引用《.·文档节点》。

引用《.·内核直连层》。

引用《..·执行·控制台输出》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内容可见性》。

CONTAINER_WAV 为 5。

CONTAINER_FLAC 为 6。

CONTAINER_MP3 为 7。

CODEC_PCM 为 10。

CODEC_MP3 为 11。

CODEC_FLAC 为 12。

读BE32 接受 d、o：
    返还 做 字节取 用 d、o 左移 24 位或 做 字节取 用 d、o 加 1 左移 16 位或 做 字节取 用 d、o 加 2 左移 8 位或 做 字节取 用 d、o 加 3。
完。

读BE16 接受 d、o：
    返还 做 字节取 用 d、o 左移 8 位或 做 字节取 用 d、o 加 1。
完。

读LE32 接受 d、o：
    返还 做 字节取 用 d、o 位或 做 字节取 用 d、o 加 1 左移 8 位或 做 字节取 用 d、o 加 2 左移 16 位或 做 字节取 用 d、o 加 3 左移 24。
完。

读LE16 接受 d、o：
    返还 做 字节取 用 d、o 位或 做 字节取 用 d、o 加 1 左移 8。
完。

Demuxer_探测格式_真实 接受 d：
    若 d 等于 0 或者 d 的 数据缓冲 等于 0 或者 d 的 缓冲大小 小于 12：
        返还 0。
    完。
    b 为 d 的 数据缓冲。
    b0 为 做 字节取 用 b、0。
    b1 为 做 字节取 用 b、1。
    b2 为 做 字节取 用 b、2。
    b3 为 做 字节取 用 b、3。
    若 做 字节取 用 b、4 等于 0x66 并且 做 字节取 用 b、5 等于 0x74 并且 做 字节取 用 b、6 等于 0x79 并且 做 字节取 用 b、7 等于 0x70：
        d 的 格式 为 CONTAINER_MP4。
        返还 CONTAINER_MP4。
    完。
    若 b0 等于 0x1A 并且 b1 等于 0x45 并且 b2 等于 0xDF 并且 b3 等于 0xA3：
        d 的 格式 为 CONTAINER_WEBM。
        返还 CONTAINER_WEBM。
    完。
    若 b0 等于 0x4F 并且 b1 等于 0x67 并且 b2 等于 0x67 并且 b3 等于 0x53：
        d 的 格式 为 CONTAINER_OGG。
        返还 CONTAINER_OGG。
    完。
    若 b0 等于 0x49 并且 b1 等于 0x44 并且 b2 等于 0x33：
        d 的 格式 为 CONTAINER_MP3。
        返还 CONTAINER_MP3。
    完。
    若 b0 等于 0xFF 并且 b1 位与 0xE0 等于 0xE0：
        d 的 格式 为 CONTAINER_MP3。
        返还 CONTAINER_MP3。
    完。
    若 b0 等于 0x52 并且 b1 等于 0x49 并且 b2 等于 0x46 并且 b3 等于 0x46：
        若 做 字节取 用 b、8 等于 0x57 并且 做 字节取 用 b、9 等于 0x41 并且 做 字节取 用 b、10 等于 0x56 并且 做 字节取 用 b、11 等于 0x45：
            d 的 格式 为 CONTAINER_WAV。
            返还 CONTAINER_WAV。
        完。
    完。
    若 b0 等于 0x66 并且 b1 等于 0x4C 并且 b2 等于 0x61 并且 b3 等于 0x43：
        d 的 格式 为 CONTAINER_FLAC。
        返还 CONTAINER_FLAC。
    完。
    返还 0。
完。

MP4_Box大小 接受 buf、off：
    若 off 加 8 大于 做 字节长度 用 buf：
        返还 0。
    完。
    sz 为 做 读BE32 用 buf、off。
    若 sz 等于 1 并且 off 加 16 小于或等于 做 字节长度 用 buf：
        返还 做 读BE32 用 buf、off 加 12。
    完。
    返还 sz。
完。

MP4_Box类型 接受 buf、off：
    若 off 加 8 大于 做 字节长度 用 buf：
        返还 0。
    完。
    返还 做 读BE32 用 buf、off 加 4。
完。

MP4_解析_完整 接受 d、me：
    buf 为 d 的 数据缓冲。
    len 为 d 的 缓冲大小。
    可变 off 为 0。
    只要 off 加 8 小于或等于 len：
        sz 为 做 MP4_Box大小 用 buf、off。
        tp 为 做 MP4_Box类型 用 buf、off。
        若 sz 小于 8 或者 off 加 sz 大于 len：
            跳出。
        完。
        若 tp 等于 0x6D6F6F76：
            做 MP4_遍历moov 用 d、me、buf、off 加 8、off 加 sz。
        完。
        若 tp 等于 0x6D646174：
            d 的 已读位置 为 off 加 8。
        完。
        off 为 off 加 sz。
    完。
完。

MP4_遍历moov 接受 d、me、buf、s、e：
    可变 off 为 s。
    只要 off 加 8 小于或等于 e：
        sz 为 做 MP4_Box大小 用 buf、off。
        tp 为 做 MP4_Box类型 用 buf、off。
        若 sz 小于 8：
            跳出。
        完。
        若 tp 等于 0x6D766864：
            ver 为 做 字节取 用 buf、off 加 8。
            可变 ts_off 为 off 加 20。
            若 ver 等于 1：
                ts_off 为 off 加 28。
            完。
            若 ts_off 加 8 小于或等于 e：
                timescale 为 做 读BE32 用 buf、ts_off。
                dur 为 做 读BE32 用 buf、ts_off 加 4。
                若 timescale 大于 0：
                    d 的 时长 为 dur 乘 1000 除以 timescale。
                完。
            完。
        完。
        若 tp 等于 0x7472616B：
            做 MP4_遍历trak 用 d、me、buf、off 加 8、off 加 sz。
        完。
        off 为 off 加 sz。
    完。
完。

MP4_遍历trak 接受 d、me、buf、s、e：
    可变 handler 为 0。
    可变 off 为 s。
    只要 off 加 8 小于或等于 e：
        sz 为 做 MP4_Box大小 用 buf、off。
        tp 为 做 MP4_Box类型 用 buf、off。
        若 sz 小于 8：
            跳出。
        完。
        若 tp 等于 0x6D646961：
            可变 m 为 off 加 8。
            只要 m 加 8 小于或等于 off 加 sz：
                ms 为 做 MP4_Box大小 用 buf、m。
                mt 为 做 MP4_Box类型 用 buf、m。
                若 ms 小于 8：
                    跳出。
                完。
                若 mt 等于 0x68646C72 并且 m 加 20 小于或等于 e：
                    handler 为 做 读BE32 用 buf、m 加 16。
                完。
                m 为 m 加 ms。
            完。
        完。
        off 为 off 加 sz。
    完。
    若 handler 等于 0x76696465 并且 me 的 视频轨道数 小于 4：
        d 的 有视频 为 真。
        me 的 视频轨道 的 第 me 的 视频轨道数 项 的 编解码器 为 CODEC_H264。
        me 的 视频轨道 的 第 me 的 视频轨道数 项 的 selected 为 me 的 视频轨道数 等于 0。
        me 的 视频轨道数 为 me 的 视频轨道数 加 1。
    完。
    若 handler 等于 0x736F756E 并且 me 的 音频轨道数 小于 4：
        d 的 有音频 为 真。
        me 的 音频轨道 的 第 me 的 音频轨道数 项 的 编解码器 为 CODEC_AAC。
        me 的 音频轨道 的 第 me 的 音频轨道数 项 的 采样率 为 44100。
        me 的 音频轨道 的 第 me 的 音频轨道数 项 的 声道数 为 2。
        me 的 音频轨道 的 第 me 的 音频轨道数 项 的 位深度 为 16。
        me 的 音频轨道 的 第 me 的 音频轨道数 项 的 enabled 为 me 的 音频轨道数 等于 0。
        me 的 音频轨道数 为 me 的 音频轨道数 加 1。
    完。
完。

WAV_解析_完整 接受 d、me：
    若 d 等于 0 或者 d 的 缓冲大小 小于 44：
        返还。
    完。
    buf 为 d 的 数据缓冲。
    d 的 有音频 为 真。
    d 的 有视频 为 假。
    me 的 音频轨道 的 第 0 项 的 编解码器 为 CODEC_PCM。
    me 的 音频轨道 的 第 0 项 的 采样率 为 做 读LE32 用 buf、24。
    me 的 音频轨道 的 第 0 项 的 声道数 为 做 读LE16 用 buf、22。
    me 的 音频轨道 的 第 0 项 的 位深度 为 做 读LE16 用 buf、34。
    me 的 音频轨道 的 第 0 项 的 enabled 为 真。
    me 的 音频轨道数 为 1。
    byteRate 为 做 读LE32 用 buf、28。
    d 的 已读位置 为 44。
    若 byteRate 大于 0：
        d 的 时长 为 (d 的 缓冲大小 减 44) 乘 1000 除以 byteRate。
    完。
完。

MP3_解析_完整 接受 d、me：
    若 d 等于 0：
        返还。
    完。
    buf 为 d 的 数据缓冲。
    len 为 d 的 缓冲大小。
    d 的 有音频 为 真。
    d 的 有视频 为 假。
    可变 off 为 0。
    若 len 大于或等于 10 并且 做 字节取 用 buf、0 等于 0x49 并且 做 字节取 用 buf、1 等于 0x44 并且 做 字节取 用 buf、2 等于 0x33：
        off 为 10 加 (做 字节取 用 buf、6 左移 21 位或 做 字节取 用 buf、7 左移 14 位或 做 字节取 用 buf、8 左移 7 位或 做 字节取 用 buf、9)。
    完。
    只要 off 加 4 小于 len：
        若 做 字节取 用 buf、off 等于 0xFF 并且 做 字节取 用 buf、off 加 1 位与 0xE0 等于 0xE0：
            h2 为 做 字节取 用 buf、off 加 2。
            h3 为 做 字节取 用 buf、off 加 3。
            sr_idx 为 h2 右移 2 位与 3。
            可变 sr 为 44100。
            若 sr_idx 等于 1：
                sr 为 48000。
            否则若 sr_idx 等于 2：
                sr 为 32000。
            完。
            me 的 音频轨道 的 第 0 项 的 编解码器 为 CODEC_MP3。
            me 的 音频轨道 的 第 0 项 的 采样率 为 sr。
            me 的 音频轨道 的 第 0 项 的 声道数 为 若 h3 右移 6 等于 3 则 1 否则 2。
            me 的 音频轨道 的 第 0 项 的 位深度 为 16。
            me 的 音频轨道 的 第 0 项 的 enabled 为 真。
            me 的 音频轨道数 为 1。
            d 的 已读位置 为 off。
            d 的 时长 为 (len 减 off) 乘 8 乘 1000 除以 128000。
            返还。
        完。
        off 为 off 加 1。
    完。
完。

OGG_解析_完整 接受 d、me：
    若 d 等于 0 或者 d 的 缓冲大小 小于 35：
        返还。
    完。
    d 的 有音频 为 真。
    d 的 有视频 为 假。
    buf 为 d 的 数据缓冲。
    nsegs 为 做 字节取 用 buf、26。
    ps 为 27 加 nsegs。
    若 ps 加 12 小于或等于 d 的 缓冲大小：
        若 做 字节取 用 buf、ps 等于 0x01 并且 做 字节取 用 buf、ps 加 1 等于 0x76：
            me 的 音频轨道 的 第 0 项 的 编解码器 为 CODEC_VORBIS。
            me 的 音频轨道 的 第 0 项 的 声道数 为 做 字节取 用 buf、ps 加 11。
            me 的 音频轨道 的 第 0 项 的 采样率 为 做 读LE32 用 buf、ps 加 12。
        否则若 做 字节取 用 buf、ps 等于 0x4F 并且 做 字节取 用 buf、ps 加 1 等于 0x70：
            me 的 音频轨道 的 第 0 项 的 编解码器 为 CODEC_OPUS。
            me 的 音频轨道 的 第 0 项 的 声道数 为 做 字节取 用 buf、ps 加 9。
            me 的 音频轨道 的 第 0 项 的 采样率 为 做 读LE32 用 buf、ps 加 12。
        完。
    完。
    me 的 音频轨道 的 第 0 项 的 位深度 为 16。
    me 的 音频轨道 的 第 0 项 的 enabled 为 真。
    me 的 音频轨道数 为 1。
完。

WEBM_解析_完整 接受 d、me：
    若 d 等于 0：
        返还。
    完。
    d 的 有音频 为 真。
    d 的 有视频 为 真。
    me 的 视频轨道 的 第 0 项 的 编解码器 为 CODEC_VP9。
    me 的 视频轨道 的 第 0 项 的 selected 为 真。
    me 的 视频轨道数 为 1。
    me 的 音频轨道 的 第 0 项 的 编解码器 为 CODEC_OPUS。
    me 的 音频轨道 的 第 0 项 的 采样率 为 48000。
    me 的 音频轨道 的 第 0 项 的 声道数 为 2。
    me 的 音频轨道 的 第 0 项 的 位深度 为 16。
    me 的 音频轨道 的 第 0 项 的 enabled 为 真。
    me 的 音频轨道数 为 1。
完。

FLAC_解析_完整 接受 d、me：
    若 d 等于 0 或者 d 的 缓冲大小 小于 42：
        返还。
    完。
    d 的 有音频 为 真。
    d 的 有视频 为 假。
    buf 为 d 的 数据缓冲。
    sr_hi 为 做 读BE16 用 buf、18。
    b20 为 做 字节取 用 buf、20。
    me 的 音频轨道 的 第 0 项 的 编解码器 为 CODEC_FLAC。
    me 的 音频轨道 的 第 0 项 的 采样率 为 sr_hi 左移 4 位或 b20 右移 4。
    me 的 音频轨道 的 第 0 项 的 声道数 为 (b20 右移 1 位与 7) 加 1。
    me 的 音频轨道 的 第 0 项 的 位深度 为 (b20 位与 1) 左移 4 位或 (做 字节取 用 buf、21 右移 4) 加 1。
    me 的 音频轨道 的 第 0 项 的 enabled 为 真。
    me 的 音频轨道数 为 1。
完。

Demuxer_解析轨道_真实 接受 d、me：
    若 d 等于 0 或者 me 等于 0：
        返还。
    完。
    开始时间 为 做 平台_获取高精度时间。
    若 d 的 格式 等于 0：
        做 Demuxer_探测格式_真实 用 d。
    完。
    若 d 的 格式 等于 CONTAINER_MP4：
        做 MP4_解析_完整 用 d、me。
    否则若 d 的 格式 等于 CONTAINER_WEBM：
        做 WEBM_解析_完整 用 d、me。
    否则若 d 的 格式 等于 CONTAINER_OGG：
        做 OGG_解析_完整 用 d、me。
    否则若 d 的 格式 等于 CONTAINER_MP3：
        做 MP3_解析_完整 用 d、me。
    否则若 d 的 格式 等于 CONTAINER_WAV：
        做 WAV_解析_完整 用 d、me。
    否则若 d 的 格式 等于 CONTAINER_FLAC：
        做 FLAC_解析_完整 用 d、me。
    完。
    me 的 duration 为 d 的 时长。
    若 全局媒体统计 不等于 空：
        全局媒体统计 的 轨道解析次数 为 全局媒体统计 的 轨道解析次数 加 1。
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        全局媒体统计 的 总解析耗时_纳秒 为 全局媒体统计 的 总解析耗时_纳秒 加 耗时。
    完。
    做 AI媒体_记录格式 用 d 的 格式、d 的 有视频、d 的 有音频。
完。

媒体解码统计 是 一种 事物，含 格式探测次数、轨道解析次数、MP4解析次数、WebM解析次数、OGG解析次数、MP3解析次数、WAV解析次数、FLAC解析次数、音频帧解码次数、视频帧解码次数、PCM渲染次数、重采样次数、总解析耗时_纳秒、总解码耗时_纳秒、AI格式调用次数、AI格式命中次数、错误总数、未知格式次数。

AI媒体状态 是 一种 事物，含 历史格式分布、历史音频占比、历史视频占比、连续同格式数、上次格式、预测下次格式、采样计数器、历史平均时长。

可变 全局媒体统计 为 0。

可变 全局媒体AI 为 0。

可变 全局媒体已初始化 为 假。

媒体解码_初始化 接受 零参：
    若 全局媒体已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局媒体统计 为 做 堆_分配_清零 用 144。
    若 全局媒体统计 等于 空：
        返还 错误_内存不足。
    完。
    全局媒体AI 为 做 堆_分配_清零 用 48。
    若 全局媒体AI 不等于 空：
        全局媒体AI 的 预测下次格式 为 CONTAINER_MP4。
    完。
    全局媒体已初始化 为 真。
    做 打印 用 「[媒体解码] 初始化完成」。
    返还 0。
完。

媒体解码_销毁 接受 零参：
    若 全局媒体统计 不等于 空：
        做 堆_释放 用 全局媒体统计。
        全局媒体统计 为 0。
    完。
    若 全局媒体AI 不等于 空：
        做 堆_释放 用 全局媒体AI。
        全局媒体AI 为 0。
    完。
    全局媒体已初始化 为 假。
    做 打印 用 「[媒体解码] 已销毁」。
    返还 0。
完。

媒体解码_重置 接受 零参：
    若 全局媒体统计 不等于 空：
        做 内存_置零 用 全局媒体统计、144。
    完。
    返还 0。
完。

PCM_渲染帧 接受 d、输出缓冲、帧数、声道数、位深：
    若 d 等于 空 或者 输出缓冲 等于 空 或者 帧数 小于或等于 0：
        返还 0。
    完。
    字节每样本 为 位深 除以 8。
    若 字节每样本 小于或等于 0：
        字节每样本 为 2。
    完。
    字节每帧 为 字节每样本 乘 声道数。
    需要字节 为 帧数 乘 字节每帧。
    若 d 的 已读位置 加 需要字节 大于 d 的 缓冲大小：
        需要字节 为 d 的 缓冲大小 减 d 的 已读位置。
        若 需要字节 小于或等于 0：
            返还 0。
        完。
    完。
    做 内存_复制 用 输出缓冲、d 的 数据缓冲 加 d 的 已读位置、需要字节。
    d 的 已读位置 为 d 的 已读位置 加 需要字节。
    若 全局媒体统计 不等于 空：
        全局媒体统计 的 PCM渲染次数 为 全局媒体统计 的 PCM渲染次数 加 1。
        全局媒体统计 的 音频帧解码次数 为 全局媒体统计 的 音频帧解码次数 加 帧数。
    完。
    返还 需要字节 除以 字节每帧。
完。

音频_重采样_线性 接受 输入、输入帧数、输入采样率、输出、输出采样率、声道数：
    若 输入 等于 空 或者 输出 等于 空 或者 输入帧数 小于或等于 0：
        返还 0。
    完。
    若 输入采样率 小于或等于 0 或者 输出采样率 小于或等于 0：
        返还 0。
    完。
    比率_Q16 为 输入采样率 乘 65536 除以 输出采样率。
    输出帧数 为 输入帧数 乘 输出采样率 除以 输入采样率。
    若 输出帧数 小于或等于 0：
        返还 0。
    完。
    可变 输出位置_Q16 为 0。
    可变 i 为 0。
    只要 i 小于 输出帧数：
        输入索引 为 输出位置_Q16 右移 16。
        小数部分 为 输出位置_Q16 位与 0xFFFF。
        若 输入索引 大于或等于 输入帧数 减 1：
            输入索引 为 输入帧数 减 2。
        完。
        若 输入索引 小于 0：
            输入索引 为 0。
        完。
        可变 ch 为 0。
        只要 ch 小于 声道数：
            样本A 为 做 内存_读16 用 输入 加 (输入索引 乘 声道数 加 ch) 乘 2。
            样本B 为 做 内存_读16 用 输入 加 ((输入索引 加 1) 乘 声道数 加 ch) 乘 2。
            插值 为 样本A 加 (样本B 减 样本A) 乘 小数部分 除以 65536。
            做 内存_写16 用 输出 加 (i 乘 声道数 加 ch) 乘 2、插值。
            ch 为 ch 加 1。
        完。
        输出位置_Q16 为 输出位置_Q16 加 比率_Q16。
        i 为 i 加 1。
    完。
    若 全局媒体统计 不等于 空：
        全局媒体统计 的 重采样次数 为 全局媒体统计 的 重采样次数 加 1。
    完。
    返还 输出帧数。
完。

媒体_获取当前位置_毫秒 接受 d：
    若 d 等于 空：
        返还 0。
    完。
    若 d 的 时长 小于或等于 0 或者 d 的 缓冲大小 小于或等于 0：
        返还 0。
    完。
    返还 d 的 已读位置 乘 d 的 时长 除以 d 的 缓冲大小。
完。

媒体_获取进度_Q10 接受 d：
    若 d 等于 空 或者 d 的 缓冲大小 小于或等于 0：
        返还 0。
    完。
    返还 d 的 已读位置 乘 1024 除以 d 的 缓冲大小。
完。

媒体_是否结束 接受 d：
    若 d 等于 空：
        返还 真。
    完。
    若 d 的 已读位置 大于或等于 d 的 缓冲大小：
        返还 真。
    完。
    返还 假。
完。

媒体_跳转 接受 d、目标毫秒：
    若 d 等于 空 或者 d 的 时长 小于或等于 0：
        返还 错误_无效参数。
    完。
    若 目标毫秒 小于 0：
        目标毫秒 为 0。
    完。
    若 目标毫秒 大于 d 的 时长：
        目标毫秒 为 d 的 时长。
    完。
    d 的 已读位置 为 目标毫秒 乘 d 的 缓冲大小 除以 d 的 时长。
    返还 0。
完。

AI媒体_记录格式 接受 格式、有视频、有音频：
    若 全局媒体AI 等于 空：
        返还。
    完。
    AI 为 全局媒体AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    若 有视频 等于 真：
        AI 的 历史视频占比 为 (AI 的 历史视频占比 乘 15 加 1024) 除以 16。
    否则：
        AI 的 历史视频占比 为 AI 的 历史视频占比 乘 15 除以 16。
    完。
    若 有音频 等于 真 并且 有视频 等于 假：
        AI 的 历史音频占比 为 (AI 的 历史音频占比 乘 15 加 1024) 除以 16。
    否则：
        AI 的 历史音频占比 为 AI 的 历史音频占比 乘 15 除以 16。
    完。
    若 格式 等于 AI 的 上次格式：
        AI 的 连续同格式数 为 AI 的 连续同格式数 加 1。
    否则：
        AI 的 连续同格式数 为 1。
        AI 的 上次格式 为 格式。
    完。
    若 AI 的 采样计数器 小于 8：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 全局媒体统计 不等于 空：
        全局媒体统计 的 AI格式调用次数 为 全局媒体统计 的 AI格式调用次数 加 1。
    完。
    若 AI 的 连续同格式数 大于 3：
        AI 的 预测下次格式 为 AI 的 上次格式。
    否则：
        AI 的 历史格式分布 为 格式。
        AI 的 预测下次格式 为 格式。
    完。
    若 AI 的 预测下次格式 等于 格式：
        若 全局媒体统计 不等于 空：
            全局媒体统计 的 AI格式命中次数 为 全局媒体统计 的 AI格式命中次数 加 1。
        完。
    完。
完。

媒体解码_打印统计 接受 零参：
    若 全局媒体统计 等于 空：
        做 打印 用 「[媒体解码] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   媒体解码器 · 统计报告             │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 格式探测:         」、做 转字符串 用 全局媒体统计 的 格式探测次数。
    做 打印 用 做 字符串拼接 用 「│ 轨道解析:         」、做 转字符串 用 全局媒体统计 的 轨道解析次数。
    做 打印 用 做 字符串拼接 用 「│ MP4解析:          」、做 转字符串 用 全局媒体统计 的 MP4解析次数。
    做 打印 用 做 字符串拼接 用 「│ WebM解析:         」、做 转字符串 用 全局媒体统计 的 WebM解析次数。
    做 打印 用 做 字符串拼接 用 「│ MP3解析:          」、做 转字符串 用 全局媒体统计 的 MP3解析次数。
    做 打印 用 做 字符串拼接 用 「│ WAV解析:          」、做 转字符串 用 全局媒体统计 的 WAV解析次数。
    做 打印 用 做 字符串拼接 用 「│ FLAC解析:         」、做 转字符串 用 全局媒体统计 的 FLAC解析次数。
    做 打印 用 做 字符串拼接 用 「│ 音频帧解码:       」、做 转字符串 用 全局媒体统计 的 音频帧解码次数。
    做 打印 用 做 字符串拼接 用 「│ PCM渲染:          」、做 转字符串 用 全局媒体统计 的 PCM渲染次数。
    做 打印 用 做 字符串拼接 用 「│ 重采样:           」、做 转字符串 用 全局媒体统计 的 重采样次数。
    做 打印 用 做 字符串拼接 用 「│ 总解析耗时(μs):   」、做 转字符串 用 全局媒体统计 的 总解析耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ AI格式调用:       」、做 转字符串 用 全局媒体统计 的 AI格式调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI格式命中:       」、做 转字符串 用 全局媒体统计 的 AI格式命中次数。
    做 打印 用 做 字符串拼接 用 「│ 错误:             」、做 转字符串 用 全局媒体统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

媒体解码_导出诊断 接受 零参：
    若 全局媒体AI 等于 空：
        做 打印 用 「[媒体AI] 未初始化」。
        返还。
    完。
    AI 为 全局媒体AI。
    做 打印 用 「[媒体AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史格式分布: 」、做 转字符串 用 AI 的 历史格式分布。
    做 打印 用 做 字符串拼接 用 「  历史音频占比(Q10): 」、做 转字符串 用 AI 的 历史音频占比。
    做 打印 用 做 字符串拼接 用 「  历史视频占比(Q10): 」、做 转字符串 用 AI 的 历史视频占比。
    做 打印 用 做 字符串拼接 用 「  连续同格式: 」、做 转字符串 用 AI 的 连续同格式数。
    做 打印 用 做 字符串拼接 用 「  预测格式: 」、做 转字符串 用 AI 的 预测下次格式。
完。

媒体解码_获取统计 接受 零参：
    返还 全局媒体统计。
完。

媒体解码_获取AI状态 接受 零参：
    返还 全局媒体AI。
完。

媒体解码_是否已初始化 接受 零参：
    返还 全局媒体已初始化。
完。

本章 收束。
