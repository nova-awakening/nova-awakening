《Clipboard剪贴板》

引用《.·文档事件》。

引用《.·内核直连层》。

引用《.·事件系统核心》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·字符串替换》。

引用《.·权限接口权限》。

引用《..·中枢·整数转字符串》。

ClipboardItem 是 一种 事物，含 类型列表、数据列表、数量。

Clipboard_readText 接受 零参：
    文本 为 做 平台_剪贴板_读取文本。
    返还 文本。
完。

Clipboard_writeText 接受 文本：
    若 文本 等于 0：
        返还。
    完。
    做 平台_剪贴板_写入文本 用 文本。
完。

Clipboard_read 接受 零参：
    结果 为 做 堆_分配_数组 用 8 乘 8。
    可变 数量 为 0。
    文本 为 做 平台_剪贴板_读取文本。
    若 文本 不等于 0：
        item 为 做 堆_分配 用 ClipboardItem。
        item 的 数量 为 1。
        item 的 类型列表 为 做 堆_分配_数组 用 8。
        做 内存_写64 用 item 的 类型列表、「text/plain」。
        item 的 数据列表 为 做 堆_分配_数组 用 8。
        做 内存_写64 用 item 的 数据列表、文本。
        做 内存_写64 用 结果 加 数量 乘 8、item。
        数量 为 数量 加 1。
    完。
    html 为 做 平台_剪贴板_读取HTML。
    若 html 不等于 0：
        item 为 做 堆_分配 用 ClipboardItem。
        item 的 数量 为 1。
        item 的 类型列表 为 做 堆_分配_数组 用 8。
        做 内存_写64 用 item 的 类型列表、「text/html」。
        item 的 数据列表 为 做 堆_分配_数组 用 8。
        做 内存_写64 用 item 的 数据列表、html。
        做 内存_写64 用 结果 加 数量 乘 8、item。
        数量 为 数量 加 1。
    完。
    返还 一组：结果、数量。
完。

Clipboard_write 接受 items、数量：
    可变 i 为 0。
    只要 i 小于 数量：
        item 为 做 内存_读64 用 items 加 i 乘 8。
        若 item 不等于 0：
            可变 j 为 0。
            只要 j 小于 item 的 数量：
                类型 为 做 内存_读64 用 item 的 类型列表 加 j 乘 8。
                数据 为 做 内存_读64 用 item 的 数据列表 加 j 乘 8。
                若 做 字符串_相等 用 类型、「text/plain」：
                    做 平台_剪贴板_写入文本 用 数据。
                否则若 做 字符串_相等 用 类型、「text/html」：
                    做 平台_剪贴板_写入HTML 用 数据。
                否则若 做 字符串_相等 用 类型、「image/png」：
                    做 平台_剪贴板_写入图片 用 数据、「png」。
                否则若 做 字符串_相等 用 类型、「text/uri-list」：
                    做 平台_剪贴板_写入文本 用 数据。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    全局CDTF统计 的 剪贴板写入次数 为 全局CDTF统计 的 剪贴板写入次数 加 1。
完。

拖放效果_无 为 0。

拖放效果_复制 为 1。

拖放效果_移动 为 2。

拖放效果_链接 为 3。

拖放事件_dragstart 为 1。

拖放事件_drag 为 2。

拖放事件_dragenter 为 3。

拖放事件_dragleave 为 4。

拖放事件_dragover 为 5。

拖放事件_drop 为 6。

拖放事件_dragend 为 7。

DT_最大条目数 为 32。

DataTransferItem 是 一种 事物，含 类型、MIME类型、数据、数据长度、文件名、最后修改时间。

DataTransfer 是 一种 事物，含 条目列表、条目数、dropEffect、effectAllowed、拖放图像、拖放图像_x、拖放图像_y、只读。

DataTransfer_创建 接受 零参：
    dt 为 做 堆_分配 用 DataTransfer。
    若 dt 等于 0：
        返还 空。
    完。
    dt 的 条目数 为 0。
    dt 的 dropEffect 为 拖放效果_无。
    dt 的 effectAllowed 为 拖放效果_复制 位或 拖放效果_移动 位或 拖放效果_链接。
    dt 的 拖放图像 为 0。
    dt 的 只读 为 假。
    返还 dt。
完。

DataTransfer_setData 接受 dt、MIME、数据：
    若 dt 等于 空 或者 dt 的 只读：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 dt 的 条目数：
        若 做 字符串_相等 用 dt 的 条目列表 的 第 i 项 的 MIME类型、MIME：
            dt 的 条目列表 的 第 i 项 的 数据 为 数据。
            dt 的 条目列表 的 第 i 项 的 数据长度 为 做 字符串_长度 用 数据。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 dt 的 条目数 大于或等于 DT_最大条目数：
        返还。
    完。
    idx 为 dt 的 条目数。
    dt 的 条目列表 的 第 idx 项 的 类型 为 0。
    dt 的 条目列表 的 第 idx 项 的 MIME类型 为 做 字符串_克隆 用 MIME。
    dt 的 条目列表 的 第 idx 项 的 数据 为 数据。
    dt 的 条目列表 的 第 idx 项 的 数据长度 为 做 字符串_长度 用 数据。
    dt 的 条目列表 的 第 idx 项 的 文件名 为 0。
    dt 的 条目列表 的 第 idx 项 的 最后修改时间 为 做 读取时钟。
    dt 的 条目数 为 dt 的 条目数 加 1。
完。

DataTransfer_getData 接受 dt、MIME：
    若 dt 等于 空：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 dt 的 条目数：
        若 做 字符串_相等 用 dt 的 条目列表 的 第 i 项 的 MIME类型、MIME：
            返还 dt 的 条目列表 的 第 i 项 的 数据。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

DataTransfer_clearData 接受 dt、MIME：
    若 dt 等于 空 或者 dt 的 只读：
        返还。
    完。
    若 MIME 等于 0：
        dt 的 条目数 为 0。
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 dt 的 条目数：
        若 做 字符串_相等 用 dt 的 条目列表 的 第 i 项 的 MIME类型、MIME：
            dt 的 条目数 为 dt 的 条目数 减 1。
            若 i 小于 dt 的 条目数：
                dt 的 条目列表 的 第 i 项 为 dt 的 条目列表 的 第 dt 的 条目数 项。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

DataTransfer_addFile 接受 dt、文件名、MIME、数据、长度：
    若 dt 等于 空 或者 dt 的 只读：
        返还。
    完。
    若 dt 的 条目数 大于或等于 DT_最大条目数：
        返还。
    完。
    idx 为 dt 的 条目数。
    dt 的 条目列表 的 第 idx 项 的 类型 为 1。
    dt 的 条目列表 的 第 idx 项 的 MIME类型 为 做 字符串_克隆 用 MIME。
    dt 的 条目列表 的 第 idx 项 的 数据 为 数据。
    dt 的 条目列表 的 第 idx 项 的 数据长度 为 长度。
    dt 的 条目列表 的 第 idx 项 的 文件名 为 做 字符串_克隆 用 文件名。
    dt 的 条目列表 的 第 idx 项 的 最后修改时间 为 做 读取时钟。
    dt 的 条目数 为 dt 的 条目数 加 1。
完。

DataTransfer_setDragImage 接受 dt、图像、x、y：
    若 dt 等于 空：
        返还。
    完。
    dt 的 拖放图像 为 图像。
    dt 的 拖放图像_x 为 x。
    dt 的 拖放图像_y 为 y。
完。

DataTransfer_释放 接受 dt：
    若 dt 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 dt 的 条目数：
        若 dt 的 条目列表 的 第 i 项 的 MIME类型 不等于 0：
            做 堆_释放 用 dt 的 条目列表 的 第 i 项 的 MIME类型。
        完。
        若 dt 的 条目列表 的 第 i 项 的 文件名 不等于 0：
            做 堆_释放 用 dt 的 条目列表 的 第 i 项 的 文件名。
        完。
        i 为 i 加 1。
    完。
    做 堆_释放 用 dt。
完。

权限_允许 为 0。

权限_拒绝 为 1。

权限_提示 为 2。

剪贴板权限 是 一种 事物，含 读权限、写权限、来源、授权时间、过期时间。

可变 权限表 为 空。

可变 权限表_大小 为 0。

权限表_容量 为 128。

剪贴板_检查权限 接受 来源、是写操作：
    若 权限表 等于 空：
        返还 权限_提示。
    完。
    可变 i 为 0。
    只要 i 小于 权限表_大小：
        p 为 权限表 加 i 乘 剪贴板权限_大小。
        若 做 字符串_相等 用 p 的 来源、来源：
            若 p 的 过期时间 不等于 0 并且 做 读取时钟 大于 p 的 过期时间：
                返还 权限_提示。
            完。
            若 是写操作：
                返还 p 的 写权限。
            完。
            返还 p 的 读权限。
        完。
        i 为 i 加 1。
    完。
    返还 权限_提示。
完。

剪贴板_授权 接受 来源、读、写、有效期秒：
    若 权限表 等于 空：
        权限表 为 做 堆_分配_数组 用 权限表_容量 乘 剪贴板权限_大小。
    完。
    若 权限表_大小 大于或等于 权限表_容量：
        返还。
    完。
    p 为 权限表 加 权限表_大小 乘 剪贴板权限_大小。
    p 的 读权限 为 读。
    p 的 写权限 为 写。
    p 的 来源 为 做 字符串_克隆 用 来源。
    p 的 授权时间 为 做 读取时钟。
    若 有效期秒 大于 0：
        p 的 过期时间 为 p 的 授权时间 加 有效期秒 乘 1000000000。
    否则：
        p 的 过期时间 为 0。
    完。
    权限表_大小 为 权限表_大小 加 1。
完。

拖放状态 是 一种 事物，含 正在拖放、源元素、数据传输、当前目标、起始X、起始Y、当前X、当前Y、拖放开始时间。

可变 全局拖放状态 为 新建 拖放状态。

拖放_开始 接受 元素、x、y：
    若 全局拖放状态 的 正在拖放：
        返还。
    完。
    若 元素 的 可拖放 等于 假：
        返还。
    完。
    全局拖放状态 的 正在拖放 为 真。
    全局拖放状态 的 源元素 为 元素。
    全局拖放状态 的 数据传输 为 做 DataTransfer_创建。
    全局拖放状态 的 当前目标 为 空。
    全局拖放状态 的 起始X 为 x。
    全局拖放状态 的 起始Y 为 y。
    全局拖放状态 的 当前X 为 x。
    全局拖放状态 的 当前Y 为 y。
    全局拖放状态 的 拖放开始时间 为 做 读取时钟。
    事件 为 做 创建拖放事件 用 拖放事件_dragstart、元素、全局拖放状态 的 数据传输。
    做 事件_分发 用 元素、事件。
    全局CDTF统计 的 拖放开始次数 为 全局CDTF统计 的 拖放开始次数 加 1。
完。

拖放_移动 接受 x、y：
    若 全局拖放状态 的 正在拖放 等于 假：
        返还。
    完。
    全局拖放状态 的 当前X 为 x。
    全局拖放状态 的 当前Y 为 y。
    目标 为 做 DOM_命中测试 用 x、y。
    若 目标 不等于 全局拖放状态 的 当前目标：
        若 全局拖放状态 的 当前目标 不等于 空：
            离开事件 为 做 创建拖放事件 用 拖放事件_dragleave、全局拖放状态 的 当前目标、全局拖放状态 的 数据传输。
            做 事件_分发 用 全局拖放状态 的 当前目标、离开事件。
        完。
        若 目标 不等于 空：
            进入事件 为 做 创建拖放事件 用 拖放事件_dragenter、目标、全局拖放状态 的 数据传输。
            做 事件_分发 用 目标、进入事件。
        完。
        全局拖放状态 的 当前目标 为 目标。
    完。
    若 目标 不等于 空：
        over事件 为 做 创建拖放事件 用 拖放事件_dragover、目标、全局拖放状态 的 数据传输。
        做 事件_分发 用 目标、over事件。
    完。
    drag事件 为 做 创建拖放事件 用 拖放事件_drag、全局拖放状态 的 源元素、全局拖放状态 的 数据传输。
    做 事件_分发 用 全局拖放状态 的 源元素、drag事件。
完。

拖放_释放 接受 x、y：
    若 全局拖放状态 的 正在拖放 等于 假：
        返还。
    完。
    目标 为 做 DOM_命中测试 用 x、y。
    若 目标 不等于 空：
        全局拖放状态 的 数据传输 的 只读 为 假。
        drop事件 为 做 创建拖放事件 用 拖放事件_drop、目标、全局拖放状态 的 数据传输。
        做 事件_分发 用 目标、drop事件。
        全局CDTF统计 的 拖放完成次数 为 全局CDTF统计 的 拖放完成次数 加 1。
    完。
    end事件 为 做 创建拖放事件 用 拖放事件_dragend、全局拖放状态 的 源元素、全局拖放状态 的 数据传输。
    做 事件_分发 用 全局拖放状态 的 源元素、end事件。
    做 DataTransfer_释放 用 全局拖放状态 的 数据传输。
    全局拖放状态 的 正在拖放 为 假。
    全局拖放状态 的 源元素 为 空。
    全局拖放状态 的 数据传输 为 空。
    全局拖放状态 的 当前目标 为 空。
完。

创建拖放事件 接受 事件类型、目标元素、dt：
    evt 为 做 事件_创建。
    evt 的 类型 为 事件类型。
    evt 的 目标 为 目标元素。
    evt 的 clientX 为 全局拖放状态 的 当前X。
    evt 的 clientY 为 全局拖放状态 的 当前Y。
    evt 的 dataTransfer 为 dt。
    evt 的 可冒泡 为 真。
    evt 的 可取消 为 真。
    返还 evt。
完。

格式转换_HTML到纯文本 接受 html：
    若 html 等于 0：
        返还 0。
    完。
    长度 为 做 字符串_长度 用 html。
    结果 为 做 堆_分配 用 长度 加 1。
    可变 在标签内 为 假。
    可变 src 为 0。
    可变 dst 为 0。
    只要 src 小于 长度：
        ch 为 做 字符取 用 html、src。
        若 ch 等于 60：
            在标签内 为 真。
        否则若 ch 等于 62：
            在标签内 为 假。
        否则若 在标签内 等于 假：
            做 字符写 用 结果、dst、ch。
            dst 为 dst 加 1。
        完。
        src 为 src 加 1。
    完。
    做 字符写 用 结果、dst、0。
    返还 结果。
完。

格式转换_纯文本到HTML 接受 文本：
    若 文本 等于 0：
        返还 0。
    完。
    长度 为 做 字符串_长度 用 文本。
    结果 为 做 字符串缓冲_创建 用 长度 乘 2 加 32。
    做 字符串缓冲_追加 用 结果、「<pre>」。
    可变 i 为 0。
    只要 i 小于 长度：
        ch 为 做 字符取 用 文本、i。
        若 ch 等于 38：
            做 字符串缓冲_追加 用 结果、「&amp;」。
        否则若 ch 等于 60：
            做 字符串缓冲_追加 用 结果、「&lt;」。
        否则若 ch 等于 62：
            做 字符串缓冲_追加 用 结果、「&gt;」。
        否则：
            做 字符串缓冲_追加字符 用 结果、ch。
        完。
        i 为 i 加 1。
    完。
    做 字符串缓冲_追加 用 结果、「</pre>」。
    返还 做 字符串缓冲_完成 用 结果。
完。

内容类型_普通文本 为 0。

内容类型_URL 为 1。

内容类型_邮件 为 2。

内容类型_电话 为 3。

内容类型_代码 为 4。

内容类型_数据 为 5。

AI_检测剪贴板内容类型 接受 文本：
    若 文本 等于 0：
        返还 内容类型_普通文本。
    完。
    bus 为 全局AI总线。
    若 bus 等于 空：
        返还 做 规则检测内容类型 用 文本。
    完。
    长度 为 做 字符串_长度 用 文本。
    特征 为 做 Tensor_创建 用 6。
    做 Tensor_写 用 特征、0、长度。
    做 Tensor_写 用 特征、1、若 做 字符串_包含 用 文本、「://」 则 1 否则 0。
    做 Tensor_写 用 特征、2、若 做 字符串_包含 用 文本、「@」 则 1 否则 0。
    做 Tensor_写 用 特征、3、做 统计数字字符 用 文本。
    做 Tensor_写 用 特征、4、若 做 字符串_包含 用 文本、「{」 则 1 否则 0。
    做 Tensor_写 用 特征、5、若 做 字符串_包含 用 文本、「(」 则 1 否则 0。
    结果 为 做 AI总线_推理 用 bus、「剪贴板内容分类」、特征。
    类型 为 做 Tensor_读 用 结果、0。
    做 Tensor_释放 用 特征。
    做 Tensor_释放 用 结果。
    返还 类型。
完。

规则检测内容类型 接受 文本：
    若 做 字符串_开头 用 文本、「http://」 或者 做 字符串_开头 用 文本、「https://」：
        返还 内容类型_URL。
    完。
    若 做 字符串_包含 用 文本、「@」 并且 做 字符串_包含 用 文本、「.」：
        返还 内容类型_邮件。
    完。
    数字比例 为 做 统计数字字符 用 文本 乘 100 除以 (做 字符串_长度 用 文本 加 1)。
    若 数字比例 大于 60：
        返还 内容类型_电话。
    完。
    若 做 字符串_包含 用 文本、「function」 或者 做 字符串_包含 用 文本、「{」 并且 做 字符串_包含 用 文本、「}」：
        返还 内容类型_代码。
    完。
    返还 内容类型_普通文本。
完。

统计数字字符 接受 文本：
    若 文本 等于 0：
        返还 0。
    完。
    长度 为 做 字符串_长度 用 文本。
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        ch 为 做 字符取 用 文本、i。
        若 ch 大于或等于 48 并且 ch 小于或等于 57：
            计数 为 计数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计数。
完。

AI_剪贴板安全检查 接受 文本：
    bus 为 全局AI总线。
    若 bus 等于 空：
        返还 0。
    完。
    特征 为 做 Tensor_创建 用 4。
    做 Tensor_写 用 特征、0、做 字符串_长度 用 文本。
    做 Tensor_写 用 特征、1、若 做 字符串_包含 用 文本、「password」 则 1 否则 0。
    做 Tensor_写 用 特征、2、若 做 字符串_包含 用 文本、「secret」 则 1 否则 0。
    做 Tensor_写 用 特征、3、若 做 字符串_包含 用 文本、「key」 则 1 否则 0。
    结果 为 做 AI总线_风险评分 用 bus、「剪贴板安全」、特征。
    风险分 为 做 Tensor_读 用 结果、0。
    做 Tensor_释放 用 特征。
    做 Tensor_释放 用 结果。
    返还 风险分。
完。

CDTF统计 是 一种 事物，含 剪贴板读取次数、剪贴板写入次数、拖放开始次数、拖放完成次数、格式转换次数、AI检测次数、AI安全拦截次数。

可变 全局CDTF统计 为 新建 CDTF统计。

CDTF_打印诊断 接受 零参：
    做 内核打印 用 「╔═══════════════════════════════════════╗」。
    做 内核打印 用 「║  CDTF 剪贴板数据传输框架 诊断       ║」。
    做 内核打印 用 「╠═══════════════════════════════════════╣」。
    做 内核打印 用 「║ 剪贴板读取:   {}」、全局CDTF统计 的 剪贴板读取次数。
    做 内核打印 用 「║ 剪贴板写入:   {}」、全局CDTF统计 的 剪贴板写入次数。
    做 内核打印 用 「║ 拖放开始:     {}」、全局CDTF统计 的 拖放开始次数。
    做 内核打印 用 「║ 拖放完成:     {}」、全局CDTF统计 的 拖放完成次数。
    做 内核打印 用 「║ 格式转换:     {}」、全局CDTF统计 的 格式转换次数。
    做 内核打印 用 「║ AI检测:       {}」、全局CDTF统计 的 AI检测次数。
    做 内核打印 用 「║ AI安全拦截:   {}」、全局CDTF统计 的 AI安全拦截次数。
    做 内核打印 用 「╚═══════════════════════════════════════╝」。
完。

CDTF_初始化 接受 零参：
    全局CDTF统计 为 新建 CDTF统计。
    全局拖放状态 为 新建 拖放状态。
    权限表 为 空。
    权限表_大小 为 0。
    做 内核打印 用 「[CDTF] 剪贴板数据传输框架已初始化 v1.0」。
完。

本章 收束。
