《沙箱》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·执行·平台时间》。

引用《..·中枢·内容可见性》。

引用《.·IPC通道》。

沙箱策略 是 一种 事物，含 允许文件读、允许文件写、允许网络、允许进程创建、允许图处、内存上限、允许的系统调用、系统调用数量。

沙箱_渲染器策略 接受 零参：
    策略 为 做 堆_分配 用 沙箱策略。
    策略 的 允许文件读 为 假。
    策略 的 允许文件写 为 假。
    策略 的 允许网络 为 假。
    策略 的 允许进程创建 为 假。
    策略 的 允许图处 为 假。
    策略 的 内存上限 为 512 乘 1024 乘 1024。
    返还 策略。
完。

沙箱_图处策略 接受 零参：
    策略 为 做 堆_分配 用 沙箱策略。
    策略 的 允许文件读 为 假。
    策略 的 允许文件写 为 假。
    策略 的 允许网络 为 假。
    策略 的 允许进程创建 为 假。
    策略 的 允许图处 为 真。
    策略 的 内存上限 为 1024 乘 1024 乘 1024。
    返还 策略。
完。

沙箱_网络策略 接受 零参：
    策略 为 做 堆_分配 用 沙箱策略。
    策略 的 允许文件读 为 真。
    策略 的 允许文件写 为 真。
    策略 的 允许网络 为 真。
    策略 的 允许进程创建 为 假。
    策略 的 允许图处 为 假。
    策略 的 内存上限 为 256 乘 1024 乘 1024。
    返还 策略。
完。

沙箱_应用 接受 PID、策略：
    若 策略 等于 0：
        返还。
    完。
    做 系统调用 用 系统调用_沙箱_策略、PID、策略 的 允许文件读 位或 策略 的 允许文件写 左移 1 位或 策略 的 允许网络 左移 2 位或 策略 的 允许进程创建 左移 3 位或 策略 的 允许图处 左移 4、策略 的 内存上限 右移 20、策略 的 允许的系统调用、策略 的 系统调用数量。
    做 系统调用 用 系统调用_沙箱_进入、PID、0、0、0、0。
完。

进程管理器 是 一种 事物，含 进程表、进程数、图处进程、网络进程、最大渲染器。

进程管理器_创建 接受 零参：
    pm 为 做 堆_分配 用 进程管理器。
    pm 的 进程数 为 0。
    pm 的 图处进程 为 0。
    pm 的 网络进程 为 0。
    pm 的 最大渲染器 为 20。
    返还 pm。
完。

进程管理器_启动图处 接受 pm：
    若 pm 等于 0 或者 pm 的 图处进程 不等于 0：
        返还。
    完。
    info 为 做 进程管理器_创建进程 用 pm、进程类型_图处。
    若 info 不等于 0：
        做 沙箱_应用 用 info 的 PID、做 沙箱_图处策略。
        pm 的 图处进程 为 info。
    完。
完。

进程管理器_启动网络 接受 pm：
    若 pm 等于 0 或者 pm 的 网络进程 不等于 0：
        返还。
    完。
    info 为 做 进程管理器_创建进程 用 pm、进程类型_网络。
    若 info 不等于 0：
        做 沙箱_应用 用 info 的 PID、做 沙箱_网络策略。
        pm 的 网络进程 为 info。
    完。
完。

进程管理器_获取渲染器 接受 pm、站点键：
    若 pm 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 pm 的 进程数：
        若 pm 的 进程表 的 第 i 项 的 类型 等于 进程类型_渲染器 并且 pm 的 进程表 的 第 i 项 的 站点键 等于 站点键 并且 pm 的 进程表 的 第 i 项 的 状态 等于 0：
            返还 pm 的 进程表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    渲染器数 为 做 进程管理器_计数渲染器 用 pm。
    若 渲染器数 大于或等于 pm 的 最大渲染器：
        返还 做 进程管理器_复用渲染器 用 pm。
    完。
    info 为 做 进程管理器_创建进程 用 pm、进程类型_渲染器。
    若 info 不等于 0：
        info 的 站点键 为 站点键。
        做 沙箱_应用 用 info 的 PID、做 沙箱_渲染器策略。
    完。
    返还 info。
完。

进程管理器_创建进程 接受 pm、类型：
    若 pm 的 进程数 大于或等于 64：
        返还 0。
    完。
    通道 为 做 IPC通道_创建。
    PID 为 做 系统调用 用 系统调用_进程_创建、类型、0、0、0、0。
    若 PID 小于或等于 0：
        返还 0。
    完。
    idx 为 pm 的 进程数。
    pm 的 进程表 的 第 idx 项 的 PID 为 PID。
    pm 的 进程表 的 第 idx 项 的 类型 为 类型。
    pm 的 进程表 的 第 idx 项 的 状态 为 0。
    pm 的 进程表 的 第 idx 项 的 IPC通道 为 通道。
    pm 的 进程表 的 第 idx 项 的 创建时间 为 做 平台_获取高精度时间。
    pm 的 进程表 的 第 idx 项 的 崩溃次数 为 0。
    通道 的 对端PID 为 PID。
    pm 的 进程数 为 pm 的 进程数 加 1。
    返还 pm 的 进程表 的 第 idx 项。
完。

进程管理器_计数渲染器 接受 pm：
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 pm 的 进程数：
        若 pm 的 进程表 的 第 i 项 的 类型 等于 进程类型_渲染器 并且 pm 的 进程表 的 第 i 项 的 状态 等于 0：
            计数 为 计数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计数。
完。

进程管理器_复用渲染器 接受 pm：
    可变 最旧idx 为 负 1。
    可变 最旧时间 为 0xFFFFFFFFFFFFFFFF。
    可变 i 为 0。
    只要 i 小于 pm 的 进程数：
        若 pm 的 进程表 的 第 i 项 的 类型 等于 进程类型_渲染器 并且 pm 的 进程表 的 第 i 项 的 状态 等于 0 并且 pm 的 进程表 的 第 i 项 的 创建时间 小于 最旧时间：
            最旧时间 为 pm 的 进程表 的 第 i 项 的 创建时间。
            最旧idx 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最旧idx 大于或等于 0：
        返还 pm 的 进程表 的 第 最旧idx 项。
    完。
    返还 0。
完。

可变 AI_沙箱_负载窗口 为 一组：（空）。

AI_沙箱_负载预测 接受 当前负载：
    做 追加 用 AI_沙箱_负载窗口、当前负载。
    若 做 计数 用 AI_沙箱_负载窗口 大于 20：
        做 列表_移除 用 AI_沙箱_负载窗口、0。
    完。
    若 做 计数 用 AI_沙箱_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_沙箱_负载窗口。
    趋势 为 AI_沙箱_负载窗口 的 第 n 减 1 项 减 AI_沙箱_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_沙箱_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
