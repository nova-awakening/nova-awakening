《标记媒体元素》

引用《.·文档节点》。

引用《.·文档事件》。

引用《.·内核直连层》。

引用《..·推理·通信总线》。

引用《..·中枢·内核打印》。

引用《..·中枢·整数转字符串》。

NETWORK_EMPTY 为 0。

NETWORK_IDLE 为 1。

NETWORK_LOADING 为 2。

NETWORK_NO_SOURCE 为 3。

HAVE_NOTHING 为 0。

HAVE_METADATA 为 1。

HAVE_CURRENT_DATA 为 2。

HAVE_FUTURE_DATA 为 3。

HAVE_ENOUGH_DATA 为 4。

MEDIA_ERR_ABORTED 为 1。

MEDIA_ERR_NETWORK 为 2。

MEDIA_ERR_DECODE 为 3。

MEDIA_ERR_SRC_NOT_SUPPORTED 为 4。

PRELOAD_NONE 为 0。

PRELOAD_METADATA 为 1。

PRELOAD_AUTO 为 2。

TRACK_AUDIO 为 0。

TRACK_VIDEO 为 1。

TRACK_TEXT 为 2。

TEXT_TRACK_DISABLED 为 0。

TEXT_TRACK_HIDDEN 为 1。

TEXT_TRACK_SHOWING 为 2。

TEXT_KIND_SUBTITLES 为 0。

TEXT_KIND_CAPTIONS 为 1。

TEXT_KIND_DESCRIPTIONS 为 2。

TEXT_KIND_CHAPTERS 为 3。

TEXT_KIND_METADATA 为 4。

CODEC_UNKNOWN 为 0。

CODEC_H264 为 1。

CODEC_H265 为 2。

CODEC_VP8 为 3。

CODEC_VP9 为 4。

CODEC_AV1 为 5。

CODEC_AAC 为 10。

CODEC_MP3 为 11。

CODEC_OPUS 为 12。

CODEC_VORBIS 为 13。

CODEC_FLAC 为 14。

CONTAINER_MP4 为 1。

CONTAINER_WEBM 为 2。

CONTAINER_OGG 为 3。

CONTAINER_MP3 为 4。

CONTAINER_WAV 为 5。

CONTAINER_HLS 为 6。

CONTAINER_DASH 为 7。

PLAY_STATE_IDLE 为 0。

PLAY_STATE_PLAYING 为 1。

PLAY_STATE_PAUSED 为 2。

PLAY_STATE_ENDED 为 3。

PLAY_STATE_SEEKING 为 4。

PLAY_STATE_STALLED 为 5。

PLAY_STATE_ERROR 为 6。

媒体_最大轨道数 为 16。

媒体_最大缓冲段 为 32。

媒体_最大源列表 为 8。

时间范围 是 一种 事物，含 起始、结束。

媒体轨道 是 一种 事物，含 ID、类型、标签、语言、编解码器、已启用、采样率、通道数、宽度、高度、帧率、比特率。

文本轨道 是 一种 事物，含 ID、种类、标签、语言、模式、提示列表、提示数。

文本提示 是 一种 事物，含 ID、起始时间、结束时间、文本、对齐、行号。

媒体源 是 一种 事物，含 地址、类型、媒体查询、优先级。

HTML媒体元素 是 一种 事物，含 节点基类、源地址、源列表、源数、当前源索引、网络状态、就绪状态、播放状态、错误码、当前时间、时长、播放速率、默认播放速率、音量、已静音、自动播放、循环播放、预加载、海报地址、已缓冲范围、已缓冲段数、已播放范围、已播放段数、可跳转范围、可跳转段数、音频轨道列表、音频轨道数、视频轨道列表、视频轨道数、文本轨道列表、文本轨道数、容器格式、视频编解码器、音频编解码器、视频宽度、视频高度、自然宽度、自然高度、播放回调、暂停回调、结束回调、错误回调、时间更新回调、加载元数据回调、跳转回调、缓冲回调。

媒体统计 是 一种 事物，含 播放次数、暂停次数、跳转次数、缓冲次数、错误次数、总播放时长、最大缓冲时长、码率切换次数。

可变 全局_媒体统计 为 空。

媒体统计_初始化 接受 零参：
    若 全局_媒体统计 等于 空：
        全局_媒体统计 为 新建 媒体统计。
        全局_媒体统计 的 播放次数 为 0。
        全局_媒体统计 的 暂停次数 为 0。
        全局_媒体统计 的 跳转次数 为 0。
        全局_媒体统计 的 缓冲次数 为 0。
        全局_媒体统计 的 错误次数 为 0。
        全局_媒体统计 的 总播放时长 为 0。
        全局_媒体统计 的 最大缓冲时长 为 0。
        全局_媒体统计 的 码率切换次数 为 0。
    完。
完。

媒体元素_创建 接受 零参：
    元素 为 做 堆_分配 用 HTML媒体元素。
    元素 的 源地址 为 0。
    元素 的 源列表 为 做 堆_分配 用 媒体_最大源列表 乘 8。
    元素 的 源数 为 0。
    元素 的 当前源索引 为 0。
    元素 的 网络状态 为 NETWORK_EMPTY。
    元素 的 就绪状态 为 HAVE_NOTHING。
    元素 的 播放状态 为 PLAY_STATE_IDLE。
    元素 的 错误码 为 0。
    元素 的 当前时间 为 0。
    元素 的 时长 为 0。
    元素 的 播放速率 为 1000。
    元素 的 默认播放速率 为 1000。
    元素 的 音量 为 1000。
    元素 的 已静音 为 假。
    元素 的 自动播放 为 假。
    元素 的 循环播放 为 假。
    元素 的 预加载 为 PRELOAD_AUTO。
    元素 的 海报地址 为 0。
    元素 的 已缓冲范围 为 做 堆_分配 用 媒体_最大缓冲段 乘 16。
    元素 的 已缓冲段数 为 0。
    元素 的 已播放范围 为 做 堆_分配 用 媒体_最大缓冲段 乘 16。
    元素 的 已播放段数 为 0。
    元素 的 可跳转范围 为 做 堆_分配 用 媒体_最大缓冲段 乘 16。
    元素 的 可跳转段数 为 0。
    元素 的 音频轨道列表 为 做 堆_分配 用 媒体_最大轨道数 乘 8。
    元素 的 音频轨道数 为 0。
    元素 的 视频轨道列表 为 做 堆_分配 用 媒体_最大轨道数 乘 8。
    元素 的 视频轨道数 为 0。
    元素 的 文本轨道列表 为 做 堆_分配 用 媒体_最大轨道数 乘 8。
    元素 的 文本轨道数 为 0。
    元素 的 容器格式 为 0。
    元素 的 视频编解码器 为 CODEC_UNKNOWN。
    元素 的 音频编解码器 为 CODEC_UNKNOWN。
    元素 的 视频宽度 为 0。
    元素 的 视频高度 为 0。
    元素 的 播放回调 为 0。
    元素 的 暂停回调 为 0。
    元素 的 结束回调 为 0。
    元素 的 错误回调 为 0。
    元素 的 时间更新回调 为 0。
    元素 的 加载元数据回调 为 0。
    元素 的 跳转回调 为 0。
    元素 的 缓冲回调 为 0。
    返还 元素。
完。

媒体元素_设置源 接受 元素、地址：
    若 元素 等于 0：
        返还。
    完。
    元素 的 源地址 为 地址。
    元素 的 网络状态 为 NETWORK_LOADING。
    元素 的 就绪状态 为 HAVE_NOTHING。
    做 媒体元素_加载 用 元素。
完。

媒体元素_添加源 接受 元素、地址、类型、优先级：
    若 元素 等于 0 或者 元素 的 源数 大于或等于 媒体_最大源列表：
        返还。
    完。
    源 为 新建 媒体源。
    源 的 地址 为 地址。
    源 的 类型 为 类型。
    源 的 优先级 为 优先级。
    做 内存_写64 用 元素 的 源列表 加 元素 的 源数 乘 8、源。
    元素 的 源数 为 元素 的 源数 加 1。
完。

媒体元素_选择最佳源 接受 元素：
    若 元素 等于 0 或者 元素 的 源数 等于 0：
        返还 空。
    完。
    可变 最佳 为 0。
    可变 最高优先级 为 负 1。
    可变 i 为 0。
    只要 i 小于 元素 的 源数：
        源 为 做 内存_读64 用 元素 的 源列表 加 i 乘 8。
        若 源 的 优先级 大于 最高优先级：
            最高优先级 为 源 的 优先级。
            最佳 为 源。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

媒体元素_加载 接受 元素：
    若 元素 等于 0：
        返还。
    完。
    元素 的 网络状态 为 NETWORK_LOADING。
    若 元素 的 源地址 等于 0 并且 元素 的 源数 大于 0：
        源 为 做 媒体元素_选择最佳源 用 元素。
        若 源 不等于 空：
            元素 的 源地址 为 源 的 地址。
        完。
    完。
    若 元素 的 源地址 等于 0：
        元素 的 网络状态 为 NETWORK_NO_SOURCE。
        返还。
    完。
    元素 的 容器格式 为 做 媒体_检测容器 用 元素 的 源地址。
完。

媒体元素_播放 接受 元素：
    做 媒体统计_初始化。
    若 元素 等于 0：
        返还。
    完。
    若 元素 的 网络状态 等于 NETWORK_NO_SOURCE：
        返还。
    完。
    元素 的 播放状态 为 PLAY_STATE_PLAYING。
    全局_媒体统计 的 播放次数 为 全局_媒体统计 的 播放次数 加 1。
    若 元素 的 播放回调 不等于 0：
        做 元素 的 播放回调 用 元素。
    完。
完。

媒体元素_暂停 接受 元素：
    做 媒体统计_初始化。
    若 元素 等于 0：
        返还。
    完。
    若 元素 的 播放状态 不等于 PLAY_STATE_PLAYING：
        返还。
    完。
    元素 的 播放状态 为 PLAY_STATE_PAUSED。
    全局_媒体统计 的 暂停次数 为 全局_媒体统计 的 暂停次数 加 1。
    若 元素 的 暂停回调 不等于 0：
        做 元素 的 暂停回调 用 元素。
    完。
完。

媒体元素_跳转 接受 元素、时间：
    做 媒体统计_初始化。
    若 元素 等于 0：
        返还。
    完。
    若 时间 小于 0：
        时间 为 0。
    完。
    若 元素 的 时长 大于 0 并且 时间 大于 元素 的 时长：
        时间 为 元素 的 时长。
    完。
    旧状态 为 元素 的 播放状态。
    元素 的 播放状态 为 PLAY_STATE_SEEKING。
    元素 的 当前时间 为 时间。
    全局_媒体统计 的 跳转次数 为 全局_媒体统计 的 跳转次数 加 1。
    若 元素 的 跳转回调 不等于 0：
        做 元素 的 跳转回调 用 元素。
    完。
    元素 的 播放状态 为 旧状态。
完。

媒体元素_停止 接受 元素：
    若 元素 等于 0：
        返还。
    完。
    元素 的 播放状态 为 PLAY_STATE_IDLE。
    元素 的 当前时间 为 0。
完。

媒体元素_结束 接受 元素：
    做 媒体统计_初始化。
    若 元素 等于 0：
        返还。
    完。
    若 元素 的 循环播放：
        元素 的 当前时间 为 0。
        做 媒体元素_播放 用 元素。
        返还。
    完。
    元素 的 播放状态 为 PLAY_STATE_ENDED。
    若 元素 的 结束回调 不等于 0：
        做 元素 的 结束回调 用 元素。
    完。
完。

媒体元素_设置音量 接受 元素、音量：
    若 元素 等于 0：
        返还。
    完。
    若 音量 小于 0：
        音量 为 0。
    完。
    若 音量 大于 1000：
        音量 为 1000。
    完。
    元素 的 音量 为 音量。
完。

媒体元素_切换静音 接受 元素：
    若 元素 等于 0：
        返还。
    完。
    元素 的 已静音 为 非 元素 的 已静音。
完。

媒体元素_设置速率 接受 元素、速率：
    若 元素 等于 0：
        返还。
    完。
    若 速率 小于 250：
        速率 为 250。
    完。
    若 速率 大于 4000：
        速率 为 4000。
    完。
    元素 的 播放速率 为 速率。
完。

媒体元素_添加缓冲段 接受 元素、起始、结束：
    若 元素 等于 0 或者 元素 的 已缓冲段数 大于或等于 媒体_最大缓冲段：
        返还。
    完。
    偏移 为 元素 的 已缓冲段数 乘 16。
    做 内存_写64 用 元素 的 已缓冲范围 加 偏移、起始。
    做 内存_写64 用 元素 的 已缓冲范围 加 偏移 加 8、结束。
    元素 的 已缓冲段数 为 元素 的 已缓冲段数 加 1。
    做 媒体_合并时间范围 用 元素 的 已缓冲范围、元素 的 已缓冲段数。
完。

媒体元素_获取缓冲比 接受 元素：
    若 元素 等于 0 或者 元素 的 时长 等于 0：
        返还 0。
    完。
    可变 总缓冲 为 0。
    可变 i 为 0。
    只要 i 小于 元素 的 已缓冲段数：
        偏移 为 i 乘 16。
        起始 为 做 内存_读64 用 元素 的 已缓冲范围 加 偏移。
        结束 为 做 内存_读64 用 元素 的 已缓冲范围 加 偏移 加 8。
        总缓冲 为 总缓冲 加 (结束 减 起始)。
        i 为 i 加 1。
    完。
    返还 总缓冲 乘 100 除以 元素 的 时长。
完。

媒体_合并时间范围 接受 范围、段数：
    若 段数 小于或等于 1：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 段数 减 1：
        若 范围 的 第 i 项 的 结束 大于或等于 范围 的 第 i 加 1 项 的 起始：
            若 范围 的 第 i 加 1 项 的 结束 大于 范围 的 第 i 项 的 结束：
                范围 的 第 i 项 的 结束 为 范围 的 第 i 加 1 项 的 结束。
            完。
            做 列表_移除 用 范围、i 加 1。
            段数 为 段数 减 1。
        否则：
            i 为 i 加 1。
        完。
    完。
完。

媒体元素_添加音频轨道 接受 元素、轨道：
    若 元素 等于 0 或者 元素 的 音频轨道数 大于或等于 媒体_最大轨道数：
        返还。
    完。
    做 内存_写64 用 元素 的 音频轨道列表 加 元素 的 音频轨道数 乘 8、轨道。
    元素 的 音频轨道数 为 元素 的 音频轨道数 加 1。
完。

媒体元素_添加视频轨道 接受 元素、轨道：
    若 元素 等于 0 或者 元素 的 视频轨道数 大于或等于 媒体_最大轨道数：
        返还。
    完。
    做 内存_写64 用 元素 的 视频轨道列表 加 元素 的 视频轨道数 乘 8、轨道。
    元素 的 视频轨道数 为 元素 的 视频轨道数 加 1。
完。

媒体元素_添加文本轨道 接受 元素、种类、标签、语言：
    若 元素 等于 0 或者 元素 的 文本轨道数 大于或等于 媒体_最大轨道数：
        返还 空。
    完。
    轨道 为 做 堆_分配 用 文本轨道。
    轨道 的 ID 为 元素 的 文本轨道数。
    轨道 的 种类 为 种类。
    轨道 的 标签 为 标签。
    轨道 的 语言 为 语言。
    轨道 的 模式 为 TEXT_TRACK_DISABLED。
    轨道 的 提示列表 为 一组：（空）。
    轨道 的 提示数 为 0。
    做 内存_写64 用 元素 的 文本轨道列表 加 元素 的 文本轨道数 乘 8、轨道。
    元素 的 文本轨道数 为 元素 的 文本轨道数 加 1。
    返还 轨道。
完。

文本轨道_添加提示 接受 轨道、起始、结束、文本：
    若 轨道 等于 0：
        返还。
    完。
    提示 为 新建 文本提示。
    提示 的 ID 为 轨道 的 提示数。
    提示 的 起始时间 为 起始。
    提示 的 结束时间 为 结束。
    提示 的 文本 为 文本。
    做 追加 用 轨道 的 提示列表、提示。
    轨道 的 提示数 为 轨道 的 提示数 加 1。
完。

文本轨道_当前提示 接受 轨道、当前时间：
    若 轨道 等于 0 或者 轨道 的 模式 等于 TEXT_TRACK_DISABLED：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 轨道 的 提示数：
        提示 为 轨道 的 提示列表 的 第 i 项。
        若 当前时间 大于或等于 提示 的 起始时间 并且 当前时间 小于 提示 的 结束时间：
            返还 提示。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

媒体_检测容器 接受 地址：
    若 地址 等于 0：
        返还 0。
    完。
    若 做 字符串_结尾匹配 用 地址、「.mp4」：
        返还 CONTAINER_MP4。
    完。
    若 做 字符串_结尾匹配 用 地址、「.webm」：
        返还 CONTAINER_WEBM。
    完。
    若 做 字符串_结尾匹配 用 地址、「.ogg」：
        返还 CONTAINER_OGG。
    完。
    若 做 字符串_结尾匹配 用 地址、「.mp3」：
        返还 CONTAINER_MP3。
    完。
    若 做 字符串_结尾匹配 用 地址、「.wav」：
        返还 CONTAINER_WAV。
    完。
    若 做 字符串_结尾匹配 用 地址、「.m3u8」：
        返还 CONTAINER_HLS。
    完。
    若 做 字符串_结尾匹配 用 地址、「.mpd」：
        返还 CONTAINER_DASH。
    完。
    返还 0。
完。

媒体_编解码器名称 接受 编解码器：
    若 编解码器 等于 CODEC_H264：
        返还 「H.264」。
    完。
    若 编解码器 等于 CODEC_H265：
        返还 「H.265/HEVC」。
    完。
    若 编解码器 等于 CODEC_VP8：
        返还 「VP8」。
    完。
    若 编解码器 等于 CODEC_VP9：
        返还 「VP9」。
    完。
    若 编解码器 等于 CODEC_AV1：
        返还 「AV1」。
    完。
    若 编解码器 等于 CODEC_AAC：
        返还 「AAC」。
    完。
    若 编解码器 等于 CODEC_MP3：
        返还 「MP3」。
    完。
    若 编解码器 等于 CODEC_OPUS：
        返还 「Opus」。
    完。
    若 编解码器 等于 CODEC_VORBIS：
        返还 「Vorbis」。
    完。
    若 编解码器 等于 CODEC_FLAC：
        返还 「FLAC」。
    完。
    返还 「未知」。
完。

媒体_容器名称 接受 容器：
    若 容器 等于 CONTAINER_MP4：
        返还 「MP4」。
    完。
    若 容器 等于 CONTAINER_WEBM：
        返还 「WebM」。
    完。
    若 容器 等于 CONTAINER_OGG：
        返还 「Ogg」。
    完。
    若 容器 等于 CONTAINER_MP3：
        返还 「MP3」。
    完。
    若 容器 等于 CONTAINER_WAV：
        返还 「WAV」。
    完。
    若 容器 等于 CONTAINER_HLS：
        返还 「HLS」。
    完。
    若 容器 等于 CONTAINER_DASH：
        返还 「DASH」。
    完。
    返还 「未知」。
完。

媒体元素_错误 接受 元素、错误码：
    做 媒体统计_初始化。
    若 元素 等于 0：
        返还。
    完。
    元素 的 错误码 为 错误码。
    元素 的 播放状态 为 PLAY_STATE_ERROR。
    全局_媒体统计 的 错误次数 为 全局_媒体统计 的 错误次数 加 1。
    若 元素 的 错误回调 不等于 0：
        做 元素 的 错误回调 用 元素。
    完。
完。

媒体_错误描述 接受 错误码：
    若 错误码 等于 MEDIA_ERR_ABORTED：
        返还 「播放被中止」。
    完。
    若 错误码 等于 MEDIA_ERR_NETWORK：
        返还 「网络错误」。
    完。
    若 错误码 等于 MEDIA_ERR_DECODE：
        返还 「解码错误」。
    完。
    若 错误码 等于 MEDIA_ERR_SRC_NOT_SUPPORTED：
        返还 「格式不支持」。
    完。
    返还 「未知错误」。
完。

媒体元素_绑定事件 接受 元素、事件名、回调：
    若 元素 等于 0 或者 回调 等于 0：
        返还。
    完。
    若 事件名 等于 「play」：
        元素 的 播放回调 为 回调。
    完。
    若 事件名 等于 「pause」：
        元素 的 暂停回调 为 回调。
    完。
    若 事件名 等于 「ended」：
        元素 的 结束回调 为 回调。
    完。
    若 事件名 等于 「error」：
        元素 的 错误回调 为 回调。
    完。
    若 事件名 等于 「timeupdate」：
        元素 的 时间更新回调 为 回调。
    完。
    若 事件名 等于 「loadedmetadata」：
        元素 的 加载元数据回调 为 回调。
    完。
    若 事件名 等于 「seeking」：
        元素 的 跳转回调 为 回调。
    完。
    若 事件名 等于 「progress」：
        元素 的 缓冲回调 为 回调。
    完。
完。

媒体_打印统计 接受 零参：
    做 媒体统计_初始化。
    做 打印行 用 「=== 媒体引擎统计 ===」。
    做 打印行 用 「播放次数: 」 加 做 整数转字符串 用 全局_媒体统计 的 播放次数。
    做 打印行 用 「暂停次数: 」 加 做 整数转字符串 用 全局_媒体统计 的 暂停次数。
    做 打印行 用 「跳转次数: 」 加 做 整数转字符串 用 全局_媒体统计 的 跳转次数。
    做 打印行 用 「缓冲次数: 」 加 做 整数转字符串 用 全局_媒体统计 的 缓冲次数。
    做 打印行 用 「错误次数: 」 加 做 整数转字符串 用 全局_媒体统计 的 错误次数。
    做 打印行 用 「码率切换: 」 加 做 整数转字符串 用 全局_媒体统计 的 码率切换次数。
完。

媒体元素_打印信息 接受 元素：
    若 元素 等于 0：
        返还。
    完。
    做 打印行 用 「=== 媒体元素信息 ===」。
    做 打印行 用 「源: 」 加 元素 的 源地址。
    做 打印行 用 「容器: 」 加 做 媒体_容器名称 用 元素 的 容器格式。
    做 打印行 用 「视频: 」 加 做 媒体_编解码器名称 用 元素 的 视频编解码器。
    做 打印行 用 「音频: 」 加 做 媒体_编解码器名称 用 元素 的 音频编解码器。
    做 打印行 用 「时长: 」 加 做 整数转字符串 用 元素 的 时长 加 「ms」。
    做 打印行 用 「当前: 」 加 做 整数转字符串 用 元素 的 当前时间 加 「ms」。
    做 打印行 用 「音频轨道: 」 加 做 整数转字符串 用 元素 的 音频轨道数。
    做 打印行 用 「视频轨道: 」 加 做 整数转字符串 用 元素 的 视频轨道数。
    做 打印行 用 「文本轨道: 」 加 做 整数转字符串 用 元素 的 文本轨道数。
    做 打印行 用 「缓冲比: 」 加 做 整数转字符串 用 做 媒体元素_获取缓冲比 用 元素 加 「%」。
完。

本章 收束。
