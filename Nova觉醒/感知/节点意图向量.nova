《节点意图向量》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

节点_AI更新意图 接受 节点、当前时间：
    若 节点 等于 0：
        返还。
    完。
    可变 v 为 0。
    若 节点 的 语义角色 等于 语义_正文 或者 节点 的 语义角色 等于 语义_段落：
        v 为 0xE0100A0000000000。
    否则若 节点 的 语义角色 等于 语义_按钮 或者 节点 的 语义角色 等于 语义_链接：
        v 为 0x40E0100000000000。
    否则若 节点 的 语义角色 等于 语义_搜索框 或者 节点 的 语义角色 等于 语义_表单：
        v 为 0x2060100000C00000。
    否则若 节点 的 语义角色 等于 语义_广告：
        v 为 0x0010E00000000000。
    否则若 节点 的 语义角色 等于 语义_弹窗 或者 节点 的 语义角色 等于 语义_Cookie同意：
        v 为 0x1080000000C00000。
    否则若 节点 的 语义角色 等于 语义_代码：
        v 为 0xC010000000004000。
    否则：
        v 为 0x4040400000000000。
    完。
    若 节点 的 点击次数 大于 3：
        提升 为 节点 的 点击次数 乘 8。
        若 提升 大于 200：
            提升 为 200。
        完。
    完。
    节点 的 意图向量 为 v。
    节点 的 意图预测时间戳 为 当前时间。
完。

节点_AI更新注意力 接受 节点、视口上、视口下、鼠标x、鼠标y：
    若 节点 等于 0：
        返还。
    完。
    lo 为 节点 的 布局对象。
    若 lo 等于 0：
        节点 的 注意力权重 为 0。
        返还。
    完。
    节点上 为 lo 的 绝对y。
    节点下 为 lo 的 绝对y 加 lo 的 内容高。
    可变 可见分 为 0。
    若 节点下 小于或等于 视口上 或者 节点上 大于或等于 视口下：
        可见分 为 0。
        节点 的 视口可见性 为 0。
    否则若 节点上 大于或等于 视口上 并且 节点下 小于或等于 视口下：
        可见分 为 5000。
        节点 的 视口可见性 为 2。
    否则：
        可见分 为 2500。
        节点 的 视口可见性 为 1。
    完。
    可变 语义分 为 0。
    若 节点 的 语义角色 等于 语义_正文 或者 节点 的 语义角色 等于 语义_标题：
        语义分 为 3000。
    否则若 节点 的 语义角色 等于 语义_广告 或者 节点 的 语义角色 等于 语义_Cookie同意：
        语义分 为 负 2000。
    否则若 节点 的 语义角色 等于 语义_视频：
        语义分 为 2000。
    否则若 节点 的 语义角色 等于 语义_弹窗：
        语义分 为 1500。
    完。
    可变 鼠标分 为 0。
    若 可见分 大于 0：
        中心x 为 lo 的 绝对x 加 lo 的 内容宽 除以 2。
        中心y 为 lo 的 绝对y 加 lo 的 内容高 除以 2。
        dx 为 鼠标x 减 中心x。
        dy 为 鼠标y 减 中心y。
        若 dx 小于 0：
            dx 为 0 减 dx。
        完。
        若 dy 小于 0：
            dy 为 0 减 dy。
        完。
        距离 为 dx 加 dy。
        若 距离 小于 10000：
            鼠标分 为 (10000 减 距离) 除以 5。
        完。
    完。
    节点 的 注意力权重 为 可见分 加 语义分 加 鼠标分。
    若 节点 的 注意力权重 小于 0：
        节点 的 注意力权重 为 0。
    完。
    若 节点 的 注意力权重 大于 10000：
        节点 的 注意力权重 为 10000。
    完。
    若 节点 的 注意力权重 大于或等于 8000：
        节点 的 渲染精度 为 3。
        节点 的 节点标志 为 节点 的 节点标志 位或 节点标志_高注意力。
    否则若 节点 的 注意力权重 大于或等于 4000：
        节点 的 渲染精度 为 2。
        节点 的 节点标志 为 节点 的 节点标志 位与 按位取反 节点标志_高注意力。
    否则若 节点 的 注意力权重 大于或等于 1000：
        节点 的 渲染精度 为 1。
    否则：
        节点 的 渲染精度 为 0。
    完。
完。

本章 收束。
