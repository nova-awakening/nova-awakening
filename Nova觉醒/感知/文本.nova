《文本》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·属性自定义属》。

引用《.·文本绘制将》。

引用《.·字体扩展渲》。

引用《.·绘制画布绘》。

引用《.·文本度量接口》。

引用《..·中枢·字符串替换》。

画布2D_fillText 接受 ctx、文本、x、y、最大宽度：
    face 为 做 字体Face_创建 用 ctx 的 当前状态 的 字体族、ctx 的 当前状态 的 字号 除以 100。
    px 为 x 除以 100。
    py 为 y 除以 100。
    若 ctx 的 当前状态 的 文本对齐 等于 4：
        m 为 做 Text渲染er_度量 用 face、文本、做 字符串_长度 用 文本。
        px 为 px 减 m 的 宽度 除以 2。
    否则若 ctx 的 当前状态 的 文本对齐 等于 3 或者 ctx 的 当前状态 的 文本对齐 等于 1：
        m 为 做 Text渲染er_度量 用 face、文本、做 字符串_长度 用 文本。
        px 为 px 减 m 的 宽度。
    完。
    若 ctx 的 当前状态 的 文本基线 等于 0：
        py 为 py 加 face 的 上升 除以 64。
    否则若 ctx 的 当前状态 的 文本基线 等于 2：
        py 为 py 加 (face 的 上升 加 face 的 下降) 除以 128。
    完。
    可变 颜色 为 ctx 的 当前状态 的 填充颜色。
    若 ctx 的 当前状态 的 全局透明度 小于 10000：
        a 为 做 SkColor_A 用 颜色 乘 ctx 的 当前状态 的 全局透明度 除以 10000。
        颜色 为 做 SkColor 用 a、做 SkColor_R 用 颜色、做 SkColor_G 用 颜色、做 SkColor_B 用 颜色。
    完。
    做 Text渲染er_绘制 用 ctx 的 位图、face、文本、做 字符串_长度 用 文本、px、py、颜色。
完。

画布2D_strokeText 接受 ctx、文本、x、y、最大宽度：
    保存填充 为 ctx 的 当前状态 的 填充颜色。
    ctx 的 当前状态 的 填充颜色 为 ctx 的 当前状态 的 描边颜色。
    做 画布2D_fillText 用 ctx、文本、x、y、最大宽度。
    ctx 的 当前状态 的 填充颜色 为 保存填充。
完。

画布2D_measureText 接受 ctx、文本：
    face 为 做 字体Face_创建 用 ctx 的 当前状态 的 字体族、ctx 的 当前状态 的 字号 除以 100。
    m 为 做 Text渲染er_度量 用 face、文本、做 字符串_长度 用 文本。
    返还 m。
完。

画布2D_draw图像_3 接受 ctx、源位图、dx、dy：
    若 源位图 等于 0：
        返还。
    完。
    做 画布2D_draw图像_9 用 ctx、源位图、0、0、源位图 的 宽 乘 100、源位图 的 高 乘 100、dx、dy、源位图 的 宽 乘 100、源位图 的 高 乘 100。
完。

画布2D_draw图像_5 接受 ctx、源位图、dx、dy、dw、dh：
    若 源位图 等于 0：
        返还。
    完。
    做 画布2D_draw图像_9 用 ctx、源位图、0、0、源位图 的 宽 乘 100、源位图 的 高 乘 100、dx、dy、dw、dh。
完。

画布2D_draw图像_9 接受 ctx、源位图、sx、sy、sw、sh、dx、dy、dw、dh：
    若 源位图 等于 0：
        返还。
    完。
    目标 为 ctx 的 位图。
    src_x 为 sx 除以 100。
    src_y 为 sy 除以 100。
    src_w 为 sw 除以 100。
    src_h 为 sh 除以 100。
    dst_x 为 dx 除以 100。
    dst_y 为 dy 除以 100。
    dst_w 为 dw 除以 100。
    dst_h 为 dh 除以 100。
    若 dst_w 小于或等于 0 或者 dst_h 小于或等于 0 或者 src_w 小于或等于 0 或者 src_h 小于或等于 0：
        返还。
    完。
    可变 y 为 0。
    只要 y 小于 dst_h：
        可变 x 为 0。
        只要 x 小于 dst_w：
            sample_x 为 src_x 加 x 乘 src_w 除以 dst_w。
            sample_y 为 src_y 加 y 乘 src_h 除以 dst_h。
            若 sample_x 大于或等于 0 并且 sample_x 小于 源位图 的 宽 并且 sample_y 大于或等于 0 并且 sample_y 小于 源位图 的 高：
                颜色 为 做 Nova缓冲itmap_获取像素 用 源位图、sample_x、sample_y。
                若 ctx 的 当前状态 的 全局透明度 小于 10000：
                    a 为 做 SkColor_A 用 颜色 乘 ctx 的 当前状态 的 全局透明度 除以 10000。
                    颜色 为 颜色 位与 0x00FFFFFF 位或 a 左移 24。
                完。
                若 颜色 不等于 0x00000000：
                    px 为 dst_x 加 x。
                    py 为 dst_y 加 y。
                    若 px 大于或等于 0 并且 px 小于 目标 的 宽 并且 py 大于或等于 0 并且 py 小于 目标 的 高：
                        做 Sk画布_混合像素 用 目标、px、py、颜色、混合_SrcOver。
                    完。
                完。
            完。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
完。

本章 收束。
