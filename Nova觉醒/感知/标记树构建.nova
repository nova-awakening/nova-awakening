《标记树构建》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·样式解析》。

引用《.·核心状态机》。

引用《..·推理·AI鼠标事件处理》。

引用《.·树构建器》。

引用《.·文档查询接口》。

引用《.·元素栈创建》。

引用《.·文档节点》。

引用《.·文档接口属》。

引用《.·HTMLTreeBuilder格式化列表》。

引用《.·节点行为统计》。

引用《.·文档创建》。

引用《..·推理·AI总线资源调度》。

HTMLTreeBuilder_重建格式化元素 接受 tb：
    格式列表 为 tb 的 格式化列表。
    若 格式列表 的 数量 等于 0：
        返还。
    完。
    可变 start 为 格式列表 的 数量 减 1。
    只要 start 大于 0：
        元 为 做 内存_读64 用 格式列表 的 元素 加 start 乘 8。
        若 元 等于 格式化标记_标记：
            start 为 start 加 1。
            跳出。
        完。
        start 为 start 减 1。
    完。
    可变 i 为 start。
    只要 i 小于 格式列表 的 数量：
        元 为 做 内存_读64 用 格式列表 的 元素 加 i 乘 8。
        若 元 不等于 格式化标记_标记：
            扩展 为 做 元素_获取扩展 用 元。
            若 扩展 不等于 0 并且 非 做 元素栈_包含标签 用 tb 的 开放元素栈、扩展 的 标签名原子：
                新元素 为 做 Document_create元素 用 tb 的 文档、扩展 的 标签名。
                当前 为 做 元素栈_顶部 用 tb 的 开放元素栈。
                若 当前 不等于 0：
                    做 节点_appendChild 用 当前、新元素。
                完。
                做 元素栈_压入 用 tb 的 开放元素栈、新元素。
                做 内存_分64 用 格式列表 的 元素 加 i 乘 8、新元素。
            完。
        完。
        i 为 i 加 1。
    完。
完。

HTMLDocumentParser 是 一种 事物，含 令牌izer、tree_builder、文档、已完成。

HTMLDocumentParser_创建 接受 文档：
    parser 为 做 堆_分配 用 HTMLDocumentParser。
    parser 的 令牌izer 为 做 HTML令牌izer_创建。
    parser 的 tree_builder 为 做 HTMLTreeBuilder_创建 用 文档、parser 的 令牌izer。
    parser 的 文档 为 文档。
    parser 的 已完成 为 假。
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        扩展 的 解析器 为 parser。
        扩展 的 解析状态 为 解析状态_解析中。
    完。
    返还 parser。
完。

HTMLDocumentParser_解析 接受 parser、html字符串：
    做 HTML令牌izer_设置输入 用 parser 的 令牌izer、html字符串。
    做 元素_初始化原子表。
    令牌 为 做 HTML令牌_创建。
    只要 非 parser 的 已完成：
        结果 为 做 HTML令牌izer_下一个令牌 用 parser 的 令牌izer、令牌。
        做 HTMLTreeBuilder_处理令牌 用 parser 的 tree_builder、令牌。
        若 结果 等于 负 1：
            parser 的 已完成 为 真。
        完。
    完。
    做 Document_完成解析 用 parser 的 文档。
完。

HTML解析器_解析片段 接受 html文本、上下文元素：
    文档 为 做 Document_创建 用 「about:blank」。
    parser 为 做 HTMLDocumentParser_创建 用 文档。
    做 HTMLDocumentParser_解析 用 parser、html文本。
    返还 文档。
完。

智能_HTMLTreeBuilder_AI节点语义 接受 节点：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 节点 等于 0：
        返还。
    完。
    角色 为 做 AI总线_语义分类 用 bus、节点。
    节点 的 语义角色 为 角色。
完。

智能_HTMLTreeBuilder_AI预加载提示 接受 url、资源类型：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 url 等于 0：
        返还。
    完。
    优先级 为 做 AI总线_资源优先级 用 bus、url。
完。

本章 收束。
