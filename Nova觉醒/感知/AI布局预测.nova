《AI布局预测》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·推理·定点Q算术》。

引用《..·推理·AI总线资源调度》。

AI布局_预测尺寸 接受 lo：
    若 lo 等于 0：
        返还。
    完。
    s 为 lo 的 样式。
    若 s 等于 0：
        返还。
    完。
    若 非 s 的 width 的 是auto：
        若 s 的 width 的 单位 等于 单位_px 或者 s 的 width 的 单位 等于 单位_无：
            lo 的 预测宽 为 s 的 width 的 值。
            lo 的 预测置信度 为 9500。
        否则若 s 的 width 的 单位 等于 单位_百分比 并且 lo 的 父 不等于 0：
            lo 的 预测宽 为 lo 的 父 的 内容宽 乘 s 的 width 的 值 除以 10000。
            lo 的 预测置信度 为 8000。
        完。
    否则若 lo 的 父 不等于 0：
        lo 的 预测宽 为 lo 的 父 的 内容宽。
        lo 的 预测置信度 为 6000。
    完。
    若 非 s 的 height 的 是auto：
        若 s 的 height 的 单位 等于 单位_px 或者 s 的 height 的 单位 等于 单位_无：
            lo 的 预测高 为 s 的 height 的 值。
            若 lo 的 预测置信度 小于 9500：
                lo 的 预测置信度 为 9500。
            完。
        完。
    否则：
        若 lo 的 累计布局次数 大于 0 并且 lo 的 内容高 大于 0：
            lo 的 预测高 为 lo 的 内容高。
            若 lo 的 预测置信度 小于 7000：
                lo 的 预测置信度 为 7000。
            完。
        完。
    完。
    lo 的 标志 为 lo 的 标志 位或 布局标志_AI已预测。
完。

AI布局_可以跳过 接受 lo：
    若 lo 等于 0：
        返还 假。
    完。
    若 lo 的 标志 位与 布局标志_需要布局：
        返还 假。
    完。
    bus 为 全局AI总线。
    若 bus 不等于 0 并且 lo 的 累计布局次数 大于 2：
        特征 为 做 AI布局_提取特征 用 lo。
        若 特征 不等于 0：
            预测结果 为 做 AI总线_布局预测 用 bus、特征。
            若 预测结果 不等于 0：
                预测w 为 做 Tensor_读 用 预测结果、0、2。
                预测h 为 做 Tensor_读 用 预测结果、0、3。
                差w 为 lo 的 内容宽 乘 Q_ONE 减 预测w。
                差h 为 lo 的 内容高 乘 Q_ONE 减 预测h。
                若 差w 小于 0：
                    差w 为 负 差w。
                完。
                若 差h 小于 0：
                    差h 为 负 差h。
                完。
                若 差w 小于 lo 的 内容宽 乘 Q_ONE 除以 50 并且 差h 小于 lo 的 内容高 乘 Q_ONE 除以 50：
                    若 lo 的 父 不等于 0 并且 lo 的 父布局版本 等于 lo 的 父 的 布局版本：
                        lo 的 预测宽 为 预测w 右移 Q_SHIFT。
                        lo 的 预测高 为 预测h 右移 Q_SHIFT。
                        lo 的 预测置信度 为 9500。
                        做 Tensor_释放 用 预测结果。
                        做 Tensor_释放 用 特征。
                        lo 的 标志 为 lo 的 标志 位或 布局标志_可跳过布局。
                        返还 真。
                    完。
                完。
                做 Tensor_释放 用 预测结果。
            完。
            做 Tensor_释放 用 特征。
        完。
    完。
    若 lo 的 预测置信度 大于或等于 9000 并且 lo 的 父 不等于 0 并且 lo 的 父布局版本 等于 lo 的 父 的 布局版本：
        lo 的 标志 为 lo 的 标志 位或 布局标志_可跳过布局。
        返还 真。
    完。
    若 lo 的 布局精度 等于 0：
        lo 的 标志 为 lo 的 标志 位或 布局标志_可跳过布局。
        返还 真。
    完。
    返还 假。
完。

AI布局_提取特征 接受 lo：
    若 lo 等于 0：
        返还 0。
    完。
    特征 为 做 Tensor_创建 用 1、12。
    s 为 lo 的 样式。
    若 s 不等于 0：
        做 Tensor_写 用 特征、0、0、s 的 width 的 值 乘 Q_ONE 除以 10000。
        做 Tensor_写 用 特征、0、1、s 的 height 的 值 乘 Q_ONE 除以 10000。
        做 Tensor_写 用 特征、0、2、s 的 display 乘 Q_ONE 除以 10。
        做 Tensor_写 用 特征、0、3、s 的 position 乘 Q_ONE 除以 5。
        做 Tensor_写 用 特征、0、4、s 的 margin_top 乘 Q_ONE 除以 10000。
        做 Tensor_写 用 特征、0、5、s 的 margin_right 乘 Q_ONE 除以 10000。
        做 Tensor_写 用 特征、0、6、s 的 padding_top 乘 Q_ONE 除以 10000。
        做 Tensor_写 用 特征、0、7、s 的 padding_left 乘 Q_ONE 除以 10000。
    完。
    若 lo 的 父 不等于 0：
        做 Tensor_写 用 特征、0、8、lo 的 父 的 内容宽 乘 Q_ONE 除以 200000。
        做 Tensor_写 用 特征、0、9、lo 的 父 的 内容高 乘 Q_ONE 除以 200000。
    完。
    做 Tensor_写 用 特征、0、10、lo 的 内容宽 乘 Q_ONE 除以 200000。
    做 Tensor_写 用 特征、0、11、lo 的 内容高 乘 Q_ONE 除以 200000。
    返还 特征。
完。

本章 收束。
