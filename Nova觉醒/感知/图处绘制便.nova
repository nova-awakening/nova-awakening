《图处绘制便》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《.·着色器程序》。

引用《..·推理·总线预测》。

引用《.·图形上下文》。

Gr图处_绘制矩形 接受 ctx、x、y、宽、高、颜色、变换：
    若 ctx 等于 0：
        返还。
    完。
    顶点 为 做 堆_分配_数组 用 4 乘 8。
    做 内存_写32 用 顶点、x。
    做 内存_写32 用 顶点 加 4、y。
    做 内存_写32 用 顶点 加 8、x 加 宽。
    做 内存_写32 用 顶点 加 12、y。
    做 内存_写32 用 顶点 加 16、x 加 宽。
    做 内存_写32 用 顶点 加 20、y 加 高。
    做 内存_写32 用 顶点 加 24、x。
    做 内存_写32 用 顶点 加 28、y 加 高。
    op 为 做 堆_分配 用 GrDrawOp。
    op 的 图元类型 为 PRIM_TRIANGLE_FAN。
    op 的 顶点数 为 4。
    op 的 顶点偏移 为 0。
    op 的 颜色 的 第 0 项 为 (颜色 右移 16 位与 0xFF) 乘 4。
    op 的 颜色 的 第 1 项 为 (颜色 右移 8 位与 0xFF) 乘 4。
    op 的 颜色 的 第 2 项 为 (颜色 位与 0xFF) 乘 4。
    op 的 颜色 的 第 3 项 为 (颜色 右移 24 位与 0xFF) 乘 4。
    op 的 透明度 为 1024。
    op 的 纹理0 为 0。
    op 的 纹理1 为 0。
    做 GrDrawOp_执行 用 ctx、op。
    做 堆_释放 用 顶点。
    做 堆_释放 用 op。
完。

Gr图处_绘制纹理 接受 ctx、tex、源x、源y、源宽、源高、目标x、目标y、目标宽、目标高、透明度：
    若 ctx 等于 0 或者 tex 等于 0：
        返还。
    完。
    u0 为 源x 乘 1024 除以 tex 的 宽。
    v0 为 源y 乘 1024 除以 tex 的 高。
    u1 为 (源x 加 源宽) 乘 1024 除以 tex 的 宽。
    v1 为 (源y 加 源高) 乘 1024 除以 tex 的 高。
    op 为 做 堆_分配 用 GrDrawOp。
    op 的 图元类型 为 PRIM_TRIANGLE_FAN。
    op 的 顶点数 为 4。
    op 的 纹理0 为 tex。
    op 的 透明度 为 透明度。
    做 GrDrawOp_执行 用 ctx、op。
    做 堆_释放 用 op。
完。

GrClipRect 是 一种 事物，含 x。

GrClipStack 是 一种 事物，含 栈、深度。

GrClipStack_推入 接受 stack、x、y、宽、高：
    若 stack 等于 0 或者 stack 的 深度 大于或等于 32：
        返还。
    完。
    可变 新x 为 x。
    可变 新y 为 y。
    可变 新宽 为 宽。
    可变 新高 为 高。
    若 stack 的 深度 大于 0：
        父 为 stack 的 栈 的 第 stack 的 深度 减 1 项。
        左 为 若 新x 大于 父 的 x 则 新x 否则 父 的 x。
        上 为 若 新y 大于 父 的 y 则 新y 否则 父 的 y。
        右 为 若 新x 加 新宽 小于 父 的 x 加 父 的 宽 则 新x 加 新宽 否则 父 的 x 加 父 的 宽。
        下 为 若 新y 加 新高 小于 父 的 y 加 父 的 高 则 新y 加 新高 否则 父 的 y 加 父 的 高。
        新x 为 左。
        新y 为 上。
        新宽 为 若 右 大于 左 则 右 减 左 否则 0。
        新高 为 若 下 大于 上 则 下 减 上 否则 0。
    完。
    stack 的 栈 的 第 stack 的 深度 项 的 x 为 新x。
    stack 的 栈 的 第 stack 的 深度 项 的 y 为 新y。
    stack 的 栈 的 第 stack 的 深度 项 的 宽 为 新宽。
    stack 的 栈 的 第 stack 的 深度 项 的 高 为 新高。
    stack 的 深度 为 stack 的 深度 加 1。
完。

GrClipStack_弹出 接受 stack：
    若 stack 等于 0 或者 stack 的 深度 等于 0：
        返还。
    完。
    stack 的 深度 为 stack 的 深度 减 1。
完。

GrClipStack_应用 接受 ctx、stack：
    若 ctx 等于 0 或者 stack 等于 0 或者 stack 的 深度 等于 0：
        返还。
    完。
    rect 为 stack 的 栈 的 第 stack 的 深度 减 1 项。
    做 图处命令_设scissor 用 ctx 的 命令缓冲、rect 的 x、rect 的 y、rect 的 宽、rect 的 高。
完。

智能_图处_资源压力检测 接受 ctx：
    若 ctx 等于 0：
        返还 0。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        若 ctx 的 纹理内存用量 大于 ctx 的 纹理内存上限 乘 9 除以 10：
            返还 2。
        完。
        若 ctx 的 纹理内存用量 大于 ctx 的 纹理内存上限 乘 7 除以 10：
            返还 1。
        完。
        返还 0。
    完。
    特征 为 一组：ctx 的 纹理内存用量、ctx 的 纹理内存上限、ctx 的 绘制调用计数、ctx 的 纹理缓存 的 总数、ctx 的 帧号、0、0、0。
    返还 做 AI总线_图处压力预测 用 bus、特征。
完。

智能_图处_自适应质量 接受 ctx：
    若 ctx 等于 0：
        返还。
    完。
    压力 为 做 智能_图处_资源压力检测 用 ctx。
    若 压力 大于或等于 2：
        ctx 的 最大采样数 为 1。
        做 GrTexture缓存_激进清理 用 ctx 的 纹理缓存、ctx 的 帧号、30。
    否则若 压力 等于 1：
        ctx 的 最大采样数 为 2。
        做 GrTexture缓存_清理 用 ctx 的 纹理缓存、ctx 的 帧号。
    否则：
        ctx 的 最大采样数 为 4。
    完。
完。

GrTexture缓存_激进清理 接受 cache、当前帧、阈值：
    若 cache 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 256：
        可变 前驱 为 0。
        可变 当前 为 cache 的 桶 的 第 i 项。
        只要 当前 不等于 0：
            下一 为 当前 的 下一个。
            若 当前 的 引用计数 小于或等于 0 并且 当前帧 减 当前 的 最后使用帧 大于 阈值：
                若 前驱 等于 0：
                    cache 的 桶 的 第 i 项 为 下一。
                否则：
                    前驱 的 下一个 为 下一。
                完。
                cache 的 总数 为 cache 的 总数 减 1。
                cache 的 总内存 为 cache 的 总内存 减 当前 的 内存大小。
                做 堆_释放 用 当前。
            否则：
                前驱 为 当前。
            完。
            当前 为 下一。
        完。
        i 为 i 加 1。
    完。
完。

本章 收束。
