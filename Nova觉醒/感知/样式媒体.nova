《样式媒体》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·辅助存根》。

引用《.·HTTP头部管理》。

引用《..·执行·底层内存操》。

引用《.·CSS值解析》。

引用《.·内核直连层》。

引用《..·中枢·字符串替换》。

媒体Query 是 一种 事物，含 类型、特征名、特征值、比较、是否定。

媒体QueryList 是 一种 事物，含 查询、匹配、监听器、监听器数量。

媒体Query_解析 接受 查询字符串：
    mq 为 做 堆_分配 用 媒体Query。
    mq 的 类型 为 0。
    mq 的 特征名 为 0。
    mq 的 特征值 为 0。
    mq 的 比较 为 0。
    mq 的 是否定 为 假。
    长度 为 做 字符串_长度 用 查询字符串。
    可变 pos 为 0。
    只要 pos 小于 长度 并且 (做 内存_读8 用 查询字符串 加 pos 等于 0x20 或者 做 内存_读8 用 查询字符串 加 pos 等于 0x28)：
        pos 为 pos 加 1。
    完。
    若 pos 加 3 小于 长度 并且 做 字符串_等于N 用 查询字符串 加 pos、3、「not」：
        mq 的 是否定 为 真。
        pos 为 pos 加 4。
    完。
    名起 为 pos。
    只要 pos 小于 长度 并且 做 内存_读8 用 查询字符串 加 pos 不等于 0x3A 并且 做 内存_读8 用 查询字符串 加 pos 不等于 0x29：
        pos 为 pos 加 1。
    完。
    mq 的 特征名 为 做 字符串_复制_N 用 查询字符串 加 名起、pos 减 名起。
    若 做 字符串_长度 用 mq 的 特征名 大于 4：
        若 做 内存_读8 用 mq 的 特征名 等于 0x6D 并且 做 内存_读8 用 mq 的 特征名 加 1 等于 0x69 并且 做 内存_读8 用 mq 的 特征名 加 2 等于 0x6E 并且 做 内存_读8 用 mq 的 特征名 加 3 等于 0x2D：
            mq 的 比较 为 1。
        完。
        若 做 内存_读8 用 mq 的 特征名 等于 0x6D 并且 做 内存_读8 用 mq 的 特征名 加 1 等于 0x61 并且 做 内存_读8 用 mq 的 特征名 加 2 等于 0x78 并且 做 内存_读8 用 mq 的 特征名 加 3 等于 0x2D：
            mq 的 比较 为 2。
        完。
    完。
    若 pos 小于 长度 并且 做 内存_读8 用 查询字符串 加 pos 等于 0x3A：
        pos 为 pos 加 1。
        只要 pos 小于 长度 并且 做 内存_读8 用 查询字符串 加 pos 等于 0x20：
            pos 为 pos 加 1。
        完。
        值 为 做 CSS解析_数字或长度 用 查询字符串、长度、pos。
        若 值 不等于 0：
            mq 的 特征值 为 值 的 整数值。
        完。
    完。
    返还 mq。
完。

媒体Query_求值 接受 mq、视口宽、视口高、设备像素比：
    若 mq 等于 0：
        返还 真。
    完。
    可变 结果 为 真。
    若 mq 的 特征名 不等于 0：
        名 为 mq 的 特征名。
        实际名 为 名。
        若 mq 的 比较 等于 1：
            实际名 为 名 加 4。
        完。
        若 mq 的 比较 等于 2：
            实际名 为 名 加 4。
        完。
        可变 实际值 为 0。
        若 做 字符串_相等_不区分大小写 用 实际名、「width」：
            实际值 为 视口宽。
        否则若 做 字符串_相等_不区分大小写 用 实际名、「height」：
            实际值 为 视口高。
        否则若 做 字符串_相等_不区分大小写 用 实际名、「device-pixel-ratio」：
            实际值 为 设备像素比。
        完。
        若 mq 的 比较 等于 0：
            结果 为 实际值 等于 mq 的 特征值。
        否则若 mq 的 比较 等于 1：
            结果 为 实际值 大于或等于 mq 的 特征值。
        否则若 mq 的 比较 等于 2：
            结果 为 实际值 小于或等于 mq 的 特征值。
        完。
        若 做 字符串_相等_不区分大小写 用 名、「prefers-color-scheme」：
            结果 为 假。
        完。
    完。
    若 mq 的 是否定：
        结果 为 非 结果。
    完。
    返还 结果。
完。

match媒体 接受 查询字符串、视口宽、视口高：
    mql 为 做 堆_分配 用 媒体QueryList。
    mql 的 查询 为 做 媒体Query_解析 用 查询字符串。
    mql 的 匹配 为 做 媒体Query_求值 用 mql 的 查询、视口宽、视口高、100。
    mql 的 监听器 为 做 堆_分配_数组 用 8 乘 8。
    mql 的 监听器数量 为 0。
    返还 mql。
完。

媒体QueryList_addListener 接受 mql、回调：
    若 mql 的 监听器数量 大于或等于 8：
        返还。
    完。
    做 内存_写64 用 mql 的 监听器 加 mql 的 监听器数量 乘 8、回调。
    mql 的 监听器数量 为 mql 的 监听器数量 加 1。
完。

媒体QueryList_更新 接受 mql、视口宽、视口高：
    旧匹配 为 mql 的 匹配。
    mql 的 匹配 为 做 媒体Query_求值 用 mql 的 查询、视口宽、视口高、100。
    若 旧匹配 不等于 mql 的 匹配：
        可变 i 为 0。
        只要 i 小于 mql 的 监听器数量：
            回调 为 做 内存_读64 用 mql 的 监听器 加 i 乘 8。
            若 回调 不等于 0：
                做 调用 用 回调、mql。
            完。
            i 为 i 加 1。
        完。
    完。
完。

本章 收束。
