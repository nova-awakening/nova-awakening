《消息语义装载》

引用《.·网络消息管消》。

引用《.·语义检查》。

引用《.·验证逻辑》。

引用《.·查看接口》。

引用《.·解析》。

引用《.·URL辅助函数》。

引用《.·国标码初始化》。

引用《.·原生原生语义》。

感知消息语义装载_模块清单 接受 零参：
    返还 一组：「./网络消息管消.nova」、「./语义检查.nova」、「./验证逻辑.nova」、「./查看接口.nova」、「./解析.nova」、「./URL辅助函数.nova」、「./国标码初始化.nova」、「./原生原生语义.nova」。
完。

感知消息语义装载_统计 接受 零参：
    返还 一本字典：模块数 对应 做 计数 用 做 感知消息语义装载_模块清单。
完。

本章 收束。
