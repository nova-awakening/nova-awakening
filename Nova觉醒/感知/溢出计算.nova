《溢出计算》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·样式值解析》。

引用《.·内核直连层》。

引用《..·中枢·字符串替换》。

布局块级Flow_计算overflow 接受 layout：
    可变 scroll_宽 为 layout 的 宽度。
    可变 scroll_高 为 layout 的 高度。
    可变 子 为 layout 的 第一子。
    只要 子 不等于 0：
        子右 为 子 的 x 加 子 的 宽度 加 子 的 padding_right 加 子 的 border_right 加 子 的 margin_right。
        子底 为 子 的 y 加 子 的 高度 加 子 的 padding_bottom 加 子 的 border_bottom 加 子 的 margin_bottom。
        若 子右 大于 scroll_宽：
            scroll_宽 为 子右。
        完。
        若 子底 大于 scroll_高：
            scroll_高 为 子底。
        完。
        子 为 子 的 后一兄弟布局。
    完。
    layout 的 滚动宽度 为 scroll_宽。
    layout 的 滚动高度 为 scroll_高。
完。

LineBox 是 一种 事物，含 x、y、宽度、高度、基线、剩余宽度、项列表、项数量。

布局Inline_布局 接受 layout：
    若 layout 等于 0：
        返还。
    完。
    可用宽 为 layout 的 宽度。
    可变 当前行 为 新建 LineBox。
    当前行 的 x 为 0。
    当前行 的 y 为 0。
    当前行 的 高度 为 0。
    当前行 的 剩余宽度 为 可用宽。
    可变 总高 为 0。
    可变 子 为 layout 的 第一子。
    只要 子 不等于 0：
        若 子 的 布局类型 等于 布局类型_文本：
            做 布局Inline_布局文本到行 用 layout、子、当前行、可用宽、总高。
        否则：
            子宽 为 子 的 宽度 加 子 的 margin_left 加 子 的 margin_right 加 子 的 padding_left 加 子 的 padding_right 加 子 的 border_left 加 子 的 border_right。
            子高 为 子 的 高度 加 子 的 margin_top 加 子 的 margin_bottom 加 子 的 padding_top 加 子 的 padding_bottom 加 子 的 border_top 加 子 的 border_bottom。
            若 子宽 大于 当前行 的 剩余宽度 并且 当前行 的 宽度 大于 0：
                总高 为 总高 加 当前行 的 高度。
                当前行 的 x 为 0。
                当前行 的 y 为 总高。
                当前行 的 宽度 为 0。
                当前行 的 高度 为 0。
                当前行 的 剩余宽度 为 可用宽。
            完。
            子 的 x 为 当前行 的 x 加 当前行 的 宽度 加 子 的 margin_left。
            子 的 y 为 当前行 的 y 加 子 的 margin_top。
            做 布局块级Flow_布局 用 子。
            当前行 的 宽度 为 当前行 的 宽度 加 子宽。
            当前行 的 剩余宽度 为 当前行 的 剩余宽度 减 子宽。
            若 子高 大于 当前行 的 高度：
                当前行 的 高度 为 子高。
            完。
        完。
        子 为 子 的 后一兄弟布局。
    完。
    总高 为 总高 加 当前行 的 高度。
    layout 的 高度 为 总高。
完。

布局Inline_布局文本到行 接受 父、文本layout、行、可用宽、总高：
    文本 为 文本layout 的 文本内容。
    若 文本 等于 0：
        返还。
    完。
    文本长度 为 做 字符串_长度 用 文本。
    若 文本长度 等于 0：
        返还。
    完。
    字号 为 1600。
    若 文本layout 的 样式 不等于 0：
        字号 为 文本layout 的 样式 的 font_size。
        若 字号 小于或等于 0：
            字号 为 1600。
        完。
    完。
    行高 为 字号 乘 120 除以 100。
    可变 pos 为 0。
    只要 pos 小于 文本长度：
        词开始 为 pos。
        只要 pos 小于 文本长度：
            c 为 做 内存_读8 用 文本 加 pos。
            若 c 等于 0x20 或者 c 等于 0x0A 或者 c 等于 0x09：
                跳出。
            完。
            pos 为 pos 加 1。
        完。
        词长度 为 pos 减 词开始。
        若 词长度 大于 0：
            可变 词宽 为 0。
            可变 i 为 词开始。
            只要 i 小于 词开始 加 词长度：
                c 为 做 内存_读8 用 文本 加 i。
                若 c 大于 0x7F：
                    词宽 为 词宽 加 字号。
                    若 c 大于或等于 0xE0：
                        i 为 i 加 2。
                    否则若 c 大于或等于 0xC0：
                        i 为 i 加 1。
                    完。
                否则：
                    词宽 为 词宽 加 字号 乘 60 除以 100。
                完。
                i 为 i 加 1。
            完。
            若 词宽 大于 行 的 剩余宽度 并且 行 的 宽度 大于 0：
                总高 为 总高 加 行 的 高度。
                行 的 x 为 0。
                行 的 y 为 总高。
                行 的 宽度 为 0。
                行 的 高度 为 0。
                行 的 剩余宽度 为 可用宽。
            完。
            行 的 宽度 为 行 的 宽度 加 词宽。
            行 的 剩余宽度 为 行 的 剩余宽度 减 词宽。
            若 行高 大于 行 的 高度：
                行 的 高度 为 行高。
            完。
        完。
        若 pos 小于 文本长度：
            c 为 做 内存_读8 用 文本 加 pos。
            若 c 等于 0x0A：
                总高 为 总高 加 行 的 高度。
                行 的 x 为 0。
                行 的 y 为 总高。
                行 的 宽度 为 0。
                行 的 高度 为 行高。
                行 的 剩余宽度 为 可用宽。
            否则：
                空格宽 为 字号 乘 25 除以 100。
                行 的 宽度 为 行 的 宽度 加 空格宽。
                行 的 剩余宽度 为 行 的 剩余宽度 减 空格宽。
            完。
            pos 为 pos 加 1。
        完。
    完。
    文本layout 的 高度 为 行高。
完。

可变 AI_溢出计算_样式缓存命中 为 0。

AI_溢出计算_选择器复杂度 接受 选择器长度、组合子数：
    若 组合子数 大于 5：
        返还 「高复杂度」。
    完。
    若 组合子数 大于 2：
        返还 「中复杂度」。
    完。
    返还 「低复杂度」。
完。

AI_溢出计算_布局缓存 接受 元素哈希：
    AI_溢出计算_样式缓存命中 为 AI_溢出计算_样式缓存命中 加 1。
    返还 元素哈希。
完。

本章 收束。
