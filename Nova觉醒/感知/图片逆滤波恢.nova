《图片逆滤波恢》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内容可见性》。

滤波_None 为 0。

滤波_Sub 为 1。

滤波_Up 为 2。

滤波_Average 为 3。

滤波_Paeth 为 4。

滤波_最大类型 为 4。

PNG颜色_灰度 为 0。

PNG颜色_RGB 为 2。

PNG颜色_索引 为 3。

PNG颜色_灰度Alpha 为 4。

PNG颜色_RGBA 为 6。

ADAM7_X起始 为 一组：0、4、0、2、0、1、0。

ADAM7_Y起始 为 一组：0、0、4、0、2、0、1。

ADAM7_X步长 为 一组：8、8、4、4、2、2、1。

ADAM7_Y步长 为 一组：8、8、8、4、4、2、2。

滤波统计 是 一种 事物，含 总行数、None行数、Sub行数、Up行数、Average行数、Paeth行数、无效滤波类型数、总像素转换数、灰度转换数、RGB转换数、RGBA直通数、索引色转换数、位深16转换数、总滤波耗时_纳秒、总转换耗时_纳秒、AI预测调用次数、AI预测命中次数、错误总数。

AI滤波状态 是 一种 事物，含 历史滤波分布、最近16行滤波序列、序列写位置、预测下行滤波类型、连续相同滤波计数、上次滤波类型、采样计数器、学习权重。

可变 全局滤波统计 为 0。

可变 全局滤波AI 为 0。

可变 全局滤波已初始化 为 假。

PNG滤波_初始化 接受 零参：
    若 全局滤波已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局滤波统计 为 做 堆_分配_清零 用 144。
    若 全局滤波统计 等于 空：
        返还 错误_内存不足。
    完。
    全局滤波AI 为 做 堆_分配_清零 用 48。
    若 全局滤波AI 不等于 空：
        全局滤波AI 的 历史滤波分布 为 做 堆_分配_清零 用 5 乘 8。
        全局滤波AI 的 最近16行滤波序列 为 做 堆_分配_清零 用 16。
        全局滤波AI 的 序列写位置 为 0。
        全局滤波AI 的 学习权重 为 128。
    完。
    全局滤波已初始化 为 真。
    做 打印 用 「[PNG滤波] 初始化完成」。
    返还 0。
完。

PNG滤波_销毁 接受 零参：
    若 全局滤波AI 不等于 空：
        若 全局滤波AI 的 历史滤波分布 不等于 空：
            做 堆_释放 用 全局滤波AI 的 历史滤波分布。
        完。
        若 全局滤波AI 的 最近16行滤波序列 不等于 空：
            做 堆_释放 用 全局滤波AI 的 最近16行滤波序列。
        完。
        做 堆_释放 用 全局滤波AI。
        全局滤波AI 为 0。
    完。
    若 全局滤波统计 不等于 空：
        做 堆_释放 用 全局滤波统计。
        全局滤波统计 为 0。
    完。
    全局滤波已初始化 为 假。
    做 打印 用 「[PNG滤波] 已销毁」。
    返还 0。
完。

PNG滤波_重置 接受 零参：
    若 全局滤波统计 不等于 空：
        做 内存_置零 用 全局滤波统计、144。
    完。
    若 全局滤波AI 不等于 空：
        若 全局滤波AI 的 历史滤波分布 不等于 空：
            做 内存_置零 用 全局滤波AI 的 历史滤波分布、40。
        完。
        全局滤波AI 的 序列写位置 为 0。
        全局滤波AI 的 连续相同滤波计数 为 0。
    完。
    返还 0。
完。

绝对值 接受 v：
    若 v 小于 0：
        返还 0 减 v。
    完。
    返还 v。
完。

Paeth预测 接受 a、b、c：
    p 为 a 加 b 减 c。
    pa 为 做 绝对值 用 p 减 a。
    pb 为 做 绝对值 用 p 减 b。
    pc 为 做 绝对值 用 p 减 c。
    若 pa 小于或等于 pb 并且 pa 小于或等于 pc：
        返还 a。
    完。
    若 pb 小于或等于 pc：
        返还 b。
    完。
    返还 c。
完。

PNG_逆滤波行 接受 dec、滤波类型、行数据、上一行：
    若 dec 等于 空 或者 行数据 等于 空：
        返还。
    完。
    若 滤波类型 大于 滤波_最大类型：
        若 全局滤波统计 不等于 空：
            全局滤波统计 的 无效滤波类型数 为 全局滤波统计 的 无效滤波类型数 加 1。
            全局滤波统计 的 错误总数 为 全局滤波统计 的 错误总数 加 1。
        完。
        返还。
    完。
    开始时间 为 做 平台_获取高精度时间。
    bpp 为 dec 的 通道数 乘 (dec 的 位深 除以 8)。
    若 bpp 小于或等于 0：
        bpp 为 1。
    完。
    行长 为 dec 的 行字节。
    可变 i 为 0。
    若 滤波类型 等于 滤波_None：
    否则若 滤波类型 等于 滤波_Sub：
        i 为 bpp。
        只要 i 小于 行长：
            原 为 做 内存_读8 用 行数据 加 i。
            左 为 做 内存_读8 用 行数据 加 i 减 bpp。
            做 内存_写8 用 行数据 加 i、原 加 左 位与 0xFF。
            i 为 i 加 1。
        完。
    否则若 滤波类型 等于 滤波_Up：
        若 上一行 等于 空：
        否则：
            只要 i 小于 行长：
                原 为 做 内存_读8 用 行数据 加 i。
                上 为 做 内存_读8 用 上一行 加 i。
                做 内存_写8 用 行数据 加 i、原 加 上 位与 0xFF。
                i 为 i 加 1。
            完。
        完。
    否则若 滤波类型 等于 滤波_Average：
        只要 i 小于 行长：
            原 为 做 内存_读8 用 行数据 加 i。
            可变 左 为 0。
            若 i 大于或等于 bpp：
                左 为 做 内存_读8 用 行数据 加 i 减 bpp。
            完。
            可变 上 为 0。
            若 上一行 不等于 空：
                上 为 做 内存_读8 用 上一行 加 i。
            完。
            做 内存_写8 用 行数据 加 i、原 加 (左 加 上) 除以 2 位与 0xFF。
            i 为 i 加 1。
        完。
    否则若 滤波类型 等于 滤波_Paeth：
        只要 i 小于 行长：
            原 为 做 内存_读8 用 行数据 加 i。
            可变 左 为 0。
            若 i 大于或等于 bpp：
                左 为 做 内存_读8 用 行数据 加 i 减 bpp。
            完。
            可变 上 为 0。
            若 上一行 不等于 空：
                上 为 做 内存_读8 用 上一行 加 i。
            完。
            可变 左上 为 0。
            若 i 大于或等于 bpp 并且 上一行 不等于 空：
                左上 为 做 内存_读8 用 上一行 加 i 减 bpp。
            完。
            做 内存_写8 用 行数据 加 i、原 加 做 Paeth预测 用 左、上、左上 位与 0xFF。
            i 为 i 加 1。
        完。
    完。
    若 全局滤波统计 不等于 空：
        全局滤波统计 的 总行数 为 全局滤波统计 的 总行数 加 1。
        若 滤波类型 等于 滤波_None：
            全局滤波统计 的 None行数 为 全局滤波统计 的 None行数 加 1。
        完。
        若 滤波类型 等于 滤波_Sub：
            全局滤波统计 的 Sub行数 为 全局滤波统计 的 Sub行数 加 1。
        完。
        若 滤波类型 等于 滤波_Up：
            全局滤波统计 的 Up行数 为 全局滤波统计 的 Up行数 加 1。
        完。
        若 滤波类型 等于 滤波_Average：
            全局滤波统计 的 Average行数 为 全局滤波统计 的 Average行数 加 1。
        完。
        若 滤波类型 等于 滤波_Paeth：
            全局滤波统计 的 Paeth行数 为 全局滤波统计 的 Paeth行数 加 1。
        完。
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        全局滤波统计 的 总滤波耗时_纳秒 为 全局滤波统计 的 总滤波耗时_纳秒 加 耗时。
    完。
    做 AI滤波_记录行 用 滤波类型。
完。

PNG_逆滤波_全部行 接受 dec、解压数据、解压大小：
    若 dec 等于 空 或者 解压数据 等于 空：
        返还 错误_无效参数。
    完。
    行步长 为 dec 的 行字节 加 1。
    若 行步长 乘 dec 的 高 大于 解压大小：
        做 打印 用 「[PNG滤波] 错误: 解压数据不足」。
        若 全局滤波统计 不等于 空：
            全局滤波统计 的 错误总数 为 全局滤波统计 的 错误总数 加 1。
        完。
        返还 错误_无效参数。
    完。
    可变 y 为 0。
    只要 y 小于 dec 的 高：
        行起始 为 解压数据 加 y 乘 行步长。
        滤波类型 为 做 内存_读8 用 行起始。
        行数据 为 行起始 加 1。
        可变 上一行 为 空。
        若 y 大于 0：
            上一行 为 解压数据 加 (y 减 1) 乘 行步长 加 1。
        完。
        做 PNG_逆滤波行 用 dec、滤波类型、行数据、上一行。
        做 PNG_行转RGBA 用 dec、y、行数据。
        y 为 y 加 1。
    完。
    返还 0。
完。

PNG_行转RGBA 接受 dec、行号、行数据：
    若 dec 等于 空 或者 行数据 等于 空 或者 dec 的 像素缓冲 等于 空：
        返还。
    完。
    输出偏移 为 dec 的 像素缓冲 加 行号 乘 dec 的 宽 乘 4。
    开始时间 为 做 平台_获取高精度时间。
    可变 x 为 0。
    若 dec 的 位深 等于 16：
        只要 x 小于 dec 的 宽：
            src 为 行数据 加 x 乘 dec 的 通道数 乘 2。
            dst 为 输出偏移 加 x 乘 4。
            若 dec 的 颜色类型 等于 PNG颜色_RGBA：
                做 内存_写8 用 dst、做 内存_读8 用 src。
                做 内存_写8 用 dst 加 1、做 内存_读8 用 src 加 2。
                做 内存_写8 用 dst 加 2、做 内存_读8 用 src 加 4。
                做 内存_写8 用 dst 加 3、做 内存_读8 用 src 加 6。
            否则若 dec 的 颜色类型 等于 PNG颜色_RGB：
                做 内存_写8 用 dst、做 内存_读8 用 src。
                做 内存_写8 用 dst 加 1、做 内存_读8 用 src 加 2。
                做 内存_写8 用 dst 加 2、做 内存_读8 用 src 加 4。
                做 内存_写8 用 dst 加 3、255。
            否则若 dec 的 颜色类型 等于 PNG颜色_灰度：
                g 为 做 内存_读8 用 src。
                做 内存_写8 用 dst、g。
                做 内存_写8 用 dst 加 1、g。
                做 内存_写8 用 dst 加 2、g。
                做 内存_写8 用 dst 加 3、255。
            否则若 dec 的 颜色类型 等于 PNG颜色_灰度Alpha：
                g 为 做 内存_读8 用 src。
                做 内存_写8 用 dst、g。
                做 内存_写8 用 dst 加 1、g。
                做 内存_写8 用 dst 加 2、g。
                做 内存_写8 用 dst 加 3、做 内存_读8 用 src 加 2。
            完。
            x 为 x 加 1。
        完。
        若 全局滤波统计 不等于 空：
            全局滤波统计 的 位深16转换数 为 全局滤波统计 的 位深16转换数 加 dec 的 宽。
        完。
    否则：
        只要 x 小于 dec 的 宽：
            src 为 行数据 加 x 乘 dec 的 通道数。
            dst 为 输出偏移 加 x 乘 4。
            若 dec 的 颜色类型 等于 PNG颜色_RGBA：
                做 内存_写8 用 dst、做 内存_读8 用 src。
                做 内存_写8 用 dst 加 1、做 内存_读8 用 src 加 1。
                做 内存_写8 用 dst 加 2、做 内存_读8 用 src 加 2。
                做 内存_写8 用 dst 加 3、做 内存_读8 用 src 加 3。
                若 全局滤波统计 不等于 空：
                    全局滤波统计 的 RGBA直通数 为 全局滤波统计 的 RGBA直通数 加 1。
                完。
            否则若 dec 的 颜色类型 等于 PNG颜色_RGB：
                做 内存_写8 用 dst、做 内存_读8 用 src。
                做 内存_写8 用 dst 加 1、做 内存_读8 用 src 加 1。
                做 内存_写8 用 dst 加 2、做 内存_读8 用 src 加 2。
                做 内存_写8 用 dst 加 3、255。
                若 全局滤波统计 不等于 空：
                    全局滤波统计 的 RGB转换数 为 全局滤波统计 的 RGB转换数 加 1。
                完。
            否则若 dec 的 颜色类型 等于 PNG颜色_灰度：
                g 为 做 内存_读8 用 src。
                做 内存_写8 用 dst、g。
                做 内存_写8 用 dst 加 1、g。
                做 内存_写8 用 dst 加 2、g。
                做 内存_写8 用 dst 加 3、255。
                若 全局滤波统计 不等于 空：
                    全局滤波统计 的 灰度转换数 为 全局滤波统计 的 灰度转换数 加 1。
                完。
            否则若 dec 的 颜色类型 等于 PNG颜色_灰度Alpha：
                g 为 做 内存_读8 用 src。
                做 内存_写8 用 dst、g。
                做 内存_写8 用 dst 加 1、g。
                做 内存_写8 用 dst 加 2、g。
                做 内存_写8 用 dst 加 3、做 内存_读8 用 src 加 1。
            否则若 dec 的 颜色类型 等于 PNG颜色_索引 并且 dec 的 调色板 不等于 0：
                idx 为 做 内存_读8 用 src。
                若 idx 小于 dec 的 调色板大小：
                    做 内存_写8 用 dst、做 内存_读8 用 dec 的 调色板 加 idx 乘 3。
                    做 内存_写8 用 dst 加 1、做 内存_读8 用 dec 的 调色板 加 idx 乘 3 加 1。
                    做 内存_写8 用 dst 加 2、做 内存_读8 用 dec 的 调色板 加 idx 乘 3 加 2。
                否则：
                    做 内存_写8 用 dst、0。
                    做 内存_写8 用 dst 加 1、0。
                    做 内存_写8 用 dst 加 2、0。
                完。
                做 内存_写8 用 dst 加 3、255。
                若 全局滤波统计 不等于 空：
                    全局滤波统计 的 索引色转换数 为 全局滤波统计 的 索引色转换数 加 1。
                完。
            完。
            x 为 x 加 1。
        完。
    完。
    若 全局滤波统计 不等于 空：
        全局滤波统计 的 总像素转换数 为 全局滤波统计 的 总像素转换数 加 dec 的 宽。
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        全局滤波统计 的 总转换耗时_纳秒 为 全局滤波统计 的 总转换耗时_纳秒 加 耗时。
    完。
完。

PNG_Adam7_获取遍数宽度 接受 原始宽、遍号：
    若 遍号 小于 0 或者 遍号 大于或等于 7：
        返还 0。
    完。
    起始 为 ADAM7_X起始 的 第 遍号 项。
    步长 为 ADAM7_X步长 的 第 遍号 项。
    若 原始宽 小于或等于 起始：
        返还 0。
    完。
    返还 (原始宽 减 起始 加 步长 减 1) 除以 步长。
完。

PNG_Adam7_获取遍数高度 接受 原始高、遍号：
    若 遍号 小于 0 或者 遍号 大于或等于 7：
        返还 0。
    完。
    起始 为 ADAM7_Y起始 的 第 遍号 项。
    步长 为 ADAM7_Y步长 的 第 遍号 项。
    若 原始高 小于或等于 起始：
        返还 0。
    完。
    返还 (原始高 减 起始 加 步长 减 1) 除以 步长。
完。

PNG_Adam7_写像素 接受 dec、遍号、遍行、遍列、像素值：
    若 dec 等于 空 或者 dec 的 像素缓冲 等于 空：
        返还。
    完。
    若 遍号 小于 0 或者 遍号 大于或等于 7：
        返还。
    完。
    目标X 为 ADAM7_X起始 的 第 遍号 项 加 遍列 乘 ADAM7_X步长 的 第 遍号 项。
    目标Y 为 ADAM7_Y起始 的 第 遍号 项 加 遍行 乘 ADAM7_Y步长 的 第 遍号 项。
    若 目标X 大于或等于 dec 的 宽 或者 目标Y 大于或等于 dec 的 高：
        返还。
    完。
    偏移 为 (目标Y 乘 dec 的 宽 加 目标X) 乘 4。
    做 内存_写32 用 dec 的 像素缓冲 加 偏移、像素值。
完。

AI滤波_记录行 接受 滤波类型：
    若 全局滤波AI 等于 空：
        返还。
    完。
    AI 为 全局滤波AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    若 AI 的 历史滤波分布 不等于 空 并且 滤波类型 小于或等于 滤波_最大类型：
        当前 为 做 内存_读64 用 AI 的 历史滤波分布 加 滤波类型 乘 8。
        做 内存_写64 用 AI 的 历史滤波分布 加 滤波类型 乘 8、当前 加 1。
    完。
    若 AI 的 最近16行滤波序列 不等于 空：
        做 内存_写8 用 AI 的 最近16行滤波序列 加 (AI 的 序列写位置 位与 15)、滤波类型。
        AI 的 序列写位置 为 AI 的 序列写位置 加 1。
    完。
    若 滤波类型 等于 AI 的 上次滤波类型：
        AI 的 连续相同滤波计数 为 AI 的 连续相同滤波计数 加 1。
    否则：
        AI 的 连续相同滤波计数 为 1。
        AI 的 上次滤波类型 为 滤波类型。
    完。
    若 AI 的 采样计数器 小于 32：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 全局滤波统计 不等于 空：
        全局滤波统计 的 AI预测调用次数 为 全局滤波统计 的 AI预测调用次数 加 1。
    完。
    可变 最大计数 为 0。
    可变 最大类型 为 0。
    若 AI 的 历史滤波分布 不等于 空：
        可变 t 为 0。
        只要 t 小于或等于 滤波_最大类型：
            计数 为 做 内存_读64 用 AI 的 历史滤波分布 加 t 乘 8。
            若 计数 大于 最大计数：
                最大计数 为 计数。
                最大类型 为 t。
            完。
            t 为 t 加 1。
        完。
    完。
    若 AI 的 连续相同滤波计数 大于 8：
        AI 的 预测下行滤波类型 为 AI 的 上次滤波类型。
    否则：
        AI 的 预测下行滤波类型 为 最大类型。
    完。
    若 AI 的 预测下行滤波类型 等于 滤波类型：
        若 全局滤波统计 不等于 空：
            全局滤波统计 的 AI预测命中次数 为 全局滤波统计 的 AI预测命中次数 加 1。
        完。
    完。
完。

PNG滤波_打印统计 接受 零参：
    若 全局滤波统计 等于 空：
        做 打印 用 「[PNG滤波] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   PNG逆滤波 · 统计报告              │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 总行数:           」、做 转字符串 用 全局滤波统计 的 总行数。
    做 打印 用 做 字符串拼接 用 「│ None:             」、做 转字符串 用 全局滤波统计 的 None行数。
    做 打印 用 做 字符串拼接 用 「│ Sub:              」、做 转字符串 用 全局滤波统计 的 Sub行数。
    做 打印 用 做 字符串拼接 用 「│ Up:               」、做 转字符串 用 全局滤波统计 的 Up行数。
    做 打印 用 做 字符串拼接 用 「│ Average:          」、做 转字符串 用 全局滤波统计 的 Average行数。
    做 打印 用 做 字符串拼接 用 「│ Paeth:            」、做 转字符串 用 全局滤波统计 的 Paeth行数。
    做 打印 用 做 字符串拼接 用 「│ 像素转换总数:     」、做 转字符串 用 全局滤波统计 的 总像素转换数。
    做 打印 用 做 字符串拼接 用 「│ 灰度转换:         」、做 转字符串 用 全局滤波统计 的 灰度转换数。
    做 打印 用 做 字符串拼接 用 「│ RGB转换:          」、做 转字符串 用 全局滤波统计 的 RGB转换数。
    做 打印 用 做 字符串拼接 用 「│ RGBA直通:         」、做 转字符串 用 全局滤波统计 的 RGBA直通数。
    做 打印 用 做 字符串拼接 用 「│ 索引色:           」、做 转字符串 用 全局滤波统计 的 索引色转换数。
    做 打印 用 做 字符串拼接 用 「│ 滤波耗时(μs):     」、做 转字符串 用 全局滤波统计 的 总滤波耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 转换耗时(μs):     」、做 转字符串 用 全局滤波统计 的 总转换耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ AI预测调用:       」、做 转字符串 用 全局滤波统计 的 AI预测调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI预测命中:       」、做 转字符串 用 全局滤波统计 的 AI预测命中次数。
    做 打印 用 做 字符串拼接 用 「│ 错误:             」、做 转字符串 用 全局滤波统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

PNG滤波_导出诊断 接受 零参：
    若 全局滤波AI 等于 空：
        做 打印 用 「[PNG滤波 AI] 未初始化」。
        返还。
    完。
    AI 为 全局滤波AI。
    做 打印 用 「[PNG滤波 AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  预测下行滤波: 」、做 转字符串 用 AI 的 预测下行滤波类型。
    做 打印 用 做 字符串拼接 用 「  连续相同: 」、做 转字符串 用 AI 的 连续相同滤波计数。
    做 打印 用 做 字符串拼接 用 「  上次类型: 」、做 转字符串 用 AI 的 上次滤波类型。
    若 全局滤波统计 不等于 空 并且 全局滤波统计 的 AI预测调用次数 大于 0：
        命中率 为 全局滤波统计 的 AI预测命中次数 乘 100 除以 全局滤波统计 的 AI预测调用次数。
        做 打印 用 做 字符串拼接 用 「  预测命中率(%): 」、做 转字符串 用 命中率。
    完。
完。

PNG滤波_获取统计 接受 零参：
    返还 全局滤波统计。
完。

PNG滤波_获取AI状态 接受 零参：
    返还 全局滤波AI。
完。

PNG滤波_获取预测类型 接受 零参：
    若 全局滤波AI 等于 空：
        返还 0。
    完。
    返还 全局滤波AI 的 预测下行滤波类型。
完。

本章 收束。
