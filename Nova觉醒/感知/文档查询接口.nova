《文档查询接口》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·文档节点》。

引用《.·样式解析为》。

引用《.·更新活跃样》。

引用《.·文档创建》。

引用《.·核心布局算》。

Document_get元素sByTagName 接受 文档、标签名：
    返还 做 元素_get元素sByTagName 用 文档、标签名。
完。

Document_get元素sByClassName 接受 文档、class名：
    返还 做 元素_get元素sByClassName 用 文档、class名。
完。

Document_querySelector 接受 文档、选择器：
    返还 做 元素_querySelector 用 文档、选择器。
完。

Document_querySelectorAll 接受 文档、选择器：
    返还 做 元素_querySelectorAll 用 文档、选择器。
完。

Document_设置就绪状态 接受 文档、新状态：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 等于 0：
        返还。
    完。
    旧状态 为 扩展 的 就绪状态。
    扩展 的 就绪状态 为 新状态。
    若 旧状态 不等于 新状态：
        做 事件_派发 用 文档、「readystatechange」。
    完。
    若 新状态 等于 文档状态_交互中 并且 旧状态 等于 文档状态_加载中：
        做 事件_派发 用 文档、「DOMContentLoaded」。
    完。
    若 新状态 等于 文档状态_完成：
        做 事件_派发 用 文档、「load」。
    完。
完。

Document_完成解析 接受 文档：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 等于 0：
        返还。
    完。
    扩展 的 解析状态 为 解析状态_已完成。
    做 Document_设置就绪状态 用 文档、文档状态_交互中。
    做 Script_执行延迟队列 用 文档。
    做 Document_设置就绪状态 用 文档、文档状态_完成。
完。

Document_更新样式和布局 接受 文档：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 等于 0：
        返还。
    完。
    若 扩展 的 样式引擎 不等于 0：
        做 样式Engine_更新活跃样式表 用 扩展 的 样式引擎。
    完。
    若 文档 的 节点标志 位与 (节点标志_已改样式 位或 节点标志_子树已改)：
        做 Document_样式重计算 用 文档。
    完。
    若 文档 的 节点标志 位与 节点标志_已改布局：
        做 Document_布局 用 文档。
    完。
完。

Document_样式重计算 接受 文档：
    可变 当前 为 做 节点_深度优先下一个 用 文档、文档。
    只要 当前 不等于 0：
        若 当前 的 节点标志 位与 节点标志_是元素：
            若 当前 的 节点标志 位与 节点标志_已改样式：
                扩展 为 做 Document_获取扩展 用 文档。
                若 扩展 不等于 0 并且 扩展 的 样式引擎 不等于 0：
                    做 样式Engine_解析元素样式 用 扩展 的 样式引擎、当前。
                完。
            完。
        完。
        当前 的 节点标志 为 当前 的 节点标志 位与 按位取反 节点标志_已改样式。
        当前 的 节点标志 为 当前 的 节点标志 位与 按位取反 节点标志_子树已改。
        当前 为 做 节点_深度优先下一个 用 当前、文档。
    完。
    文档 的 节点标志 为 文档 的 节点标志 位与 按位取反 节点标志_已改样式。
    文档 的 节点标志 为 文档 的 节点标志 位与 按位取反 节点标志_子树已改。
完。

Document_布局 接受 文档：
    若 文档 的 布局对象 不等于 0：
        做 布局Object_布局 用 文档 的 布局对象。
    完。
    文档 的 节点标志 为 文档 的 节点标志 位与 按位取反 节点标志_已改布局。
完。

本章 收束。
