《HTMLTreeBuilder_文本》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·元素栈创建》。

引用《.·节点行为统计》。

引用《.·HTMLTreeBuilder_body》。

引用《.·文档接口属》。

引用《.·文档节点》。

引用《.·原子驻留》。

引用《.·树构建器》。

HTMLTreeBuilder_文本模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_字符：
        做 HTMLTreeBuilder_插入文本 用 tb、令牌 的 数据、令牌 的 数据长度。
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_文件结束：
        做 元素栈_弹出 用 tb 的 开放元素栈。
        tb 的 插入模式 为 tb 的 原始插入模式。
        做 HTMLTreeBuilder_处理令牌 用 tb、令牌。
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_结束标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_script：
            脚本元素 为 做 元素栈_弹出 用 tb 的 开放元素栈。
            tb 的 插入模式 为 tb 的 原始插入模式。
            返还。
        完。
        做 元素栈_弹出 用 tb 的 开放元素栈。
        tb 的 插入模式 为 tb 的 原始插入模式。
        返还。
    完。
完。

HTMLTreeBuilder_body后模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_字符：
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_注释：
        html 为 做 Document_document元素 用 tb 的 文档。
        若 html 不等于 0：
            注释 为 做 Comment_创建 用 令牌 的 数据。
            做 节点_appendChild 用 html、注释。
        完。
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_结束标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_html：
            tb 的 插入模式 为 插入模式_html后。
            返还。
        完。
    完。
    若 令牌 的 类型 等于 令牌类型_文件结束：
        返还。
    完。
    tb 的 插入模式 为 插入模式_body中。
    做 HTMLTreeBuilder_处理令牌 用 tb、令牌。
完。

HTMLTreeBuilder_html后模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_注释：
        注释 为 做 Comment_创建 用 令牌 的 数据。
        做 节点_appendChild 用 tb 的 文档、注释。
        返还。
    完。
    若 令牌 的 类型 等于 令牌类型_文件结束：
        返还。
    完。
完。

HTMLTreeBuilder_table中模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_开始标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_tr：
            元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
            做 元素栈_压入 用 tb 的 开放元素栈、元素。
            tb 的 插入模式 为 插入模式_行中。
            返还。
        完。
        若 标签原子 等于 原子_td 或者 标签原子 等于 原子_th：
            tr 为 做 Document_create元素 用 tb 的 文档、「tr」。
            当前 为 做 元素栈_顶部 用 tb 的 开放元素栈。
            做 节点_appendChild 用 当前、tr。
            做 元素栈_压入 用 tb 的 开放元素栈、tr。
            tb 的 插入模式 为 插入模式_行中。
            做 HTMLTreeBuilder_处理令牌 用 tb、令牌。
            返还。
        完。
    完。
    若 令牌 的 类型 等于 令牌类型_结束标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_table：
            做 元素栈_弹出直到含 用 tb 的 开放元素栈、原子_table。
            tb 的 插入模式 为 插入模式_body中。
            返还。
        完。
    完。
    做 HTMLTreeBuilder_body中模式 用 tb、令牌。
完。

HTMLTreeBuilder_行中模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_开始标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_td 或者 标签原子 等于 原子_th：
            元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
            做 元素栈_压入 用 tb 的 开放元素栈、元素。
            tb 的 插入模式 为 插入模式_单元格中。
            返还。
        完。
    完。
    若 令牌 的 类型 等于 令牌类型_结束标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_tr：
            做 元素栈_弹出直到含 用 tb 的 开放元素栈、原子_tr。
            tb 的 插入模式 为 插入模式_table中。
            返还。
        完。
    完。
    做 HTMLTreeBuilder_table中模式 用 tb、令牌。
完。

HTMLTreeBuilder_单元格中模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_结束标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_td 或者 标签原子 等于 原子_th：
            做 元素栈_弹出直到含 用 tb 的 开放元素栈、标签原子。
            tb 的 插入模式 为 插入模式_行中。
            返还。
        完。
    完。
    做 HTMLTreeBuilder_body中模式 用 tb、令牌。
完。

HTMLTreeBuilder_select中模式 接受 tb、令牌：
    若 令牌 的 类型 等于 令牌类型_开始标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_option：
            元素 为 做 HTMLTreeBuilder_创建元素并插入 用 tb、令牌、0。
            做 元素栈_压入 用 tb 的 开放元素栈、元素。
            返还。
        完。
    完。
    若 令牌 的 类型 等于 令牌类型_结束标签：
        标签原子 为 做 原子_驻留 用 令牌 的 标签名。
        若 标签原子 等于 原子_select：
            做 元素栈_弹出直到含 用 tb 的 开放元素栈、原子_select。
            tb 的 插入模式 为 插入模式_body中。
            返还。
        完。
        若 标签原子 等于 原子_option：
            做 元素栈_弹出 用 tb 的 开放元素栈。
            返还。
        完。
    完。
完。

可变 AI_标记树构丙_调用数 为 0。

可变 AI_标记树构丙_异常数 为 0。

AI_标记树构丙_监控 接受 零参：
    AI_标记树构丙_调用数 为 AI_标记树构丙_调用数 加 1。
    返还 AI_标记树构丙_异常数 等于 0。
完。

AI_标记树构丙_健康检查 接受 零参：
    若 AI_标记树构丙_调用数 大于 0 并且 AI_标记树构丙_异常数 乘 100 除以 AI_标记树构丙_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
