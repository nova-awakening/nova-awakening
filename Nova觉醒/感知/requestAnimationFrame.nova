《requestAnimationFrame》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·推理·定点Q算术》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

引用《.·文档创建》。

引用《..·执行·控制台输出》。

NRAF_优先级_关键 为 0。

NRAF_优先级_普通 为 1。

NRAF_优先级_空闲 为 2。

NRAF_FPS_60 为 16667。

NRAF_FPS_30 为 33333。

NRAF_FPS_120 为 8333。

NRAF_最大回调 为 256。

NRAF_COOKIE_SECURE 为 0x01。

NRAF_COOKIE_HTTPONLY 为 0x02。

NRAF_COOKIE_SAMESITE 为 0x04。

页面类型_未知 为 0。

页面类型_文章 为 1。

页面类型_商品 为 2。

页面类型_搜索 为 3。

页面类型_视频 为 4。

页面类型_社交 为 5。

页面类型_应用 为 6。

页面类型_表单 为 7。

可变 NRAF_调度总数 为 0。

可变 NRAF_回调总数 为 0。

可变 NRAF_跳帧总数 为 0。

可变 NRAF_AI分析总数 为 0。

可变 NRAF_平均帧时间 为 16667。

可变 NRAF_最大帧时间 为 0。

Document_requestAnimationFrame 接受 文档、回调：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 等于 0：
        返还 0。
    完。
    id 为 扩展 的 下一个动画帧ID。
    扩展 的 下一个动画帧ID 为 扩展 的 下一个动画帧ID 加 1。
    若 扩展 的 rAF回调数 小于 64：
        做 内存_写64 用 扩展 的 rAF回调列表 加 扩展 的 rAF回调数 乘 16、回调。
        做 内存_写64 用 扩展 的 rAF回调列表 加 扩展 的 rAF回调数 乘 16 加 8、id。
        扩展 的 rAF回调数 为 扩展 的 rAF回调数 加 1。
    完。
    返还 id。
完。

Document_cancelAnimationFrame 接受 文档、id：
    若 文档 等于 0：
        返还。
    完。
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 扩展 的 rAF回调数量：
        若 扩展 的 rAF回调ID 的 第 i 项 等于 id：
            扩展 的 rAF回调 的 第 i 项 为 0。
            扩展 的 rAF回调ID 的 第 i 项 为 0。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

NRAF_Cookie 是 一种 事物，含 名称 为 0、值 为 0、域 为 0、路径 为 0、过期时间 为 0、标志 为 0、同站策略 为 0。

NRAF_最大Cookie 为 128。

NRAF_CookieJar 是 一种 事物，含 cookies 为 一组：（空）、数量 为 0。

Document_cookie_get 接受 文档：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        返还 扩展 的 cookie。
    完。
    返还 「」。
完。

Document_cookie_set 接受 文档、cookie值：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 等于 0：
        返还。
    完。
    解析结果 为 做 NRAF_解析Cookie字符串 用 cookie值。
    若 解析结果 不等于 0：
        做 NRAF_CookieJar_添加 用 扩展 的 cookieJar、解析结果。
    完。
    扩展 的 cookie 为 做 NRAF_CookieJar_序列化 用 扩展 的 cookieJar。
完。

NRAF_解析Cookie字符串 接受 字符串：
    若 字符串 等于 0：
        返还 0。
    完。
    c 为 新建 NRAF_Cookie。
    长度 为 做 字符串_长度 用 字符串。
    可变 等号位 为 负 1。
    可变 分号位 为 长度。
    可变 i 为 0。
    只要 i 小于 长度：
        ch 为 做 字节取 用 字符串、i。
        若 ch 等于 61 并且 等号位 小于 0：
            等号位 为 i。
        完。
        若 ch 等于 59：
            分号位 为 i。
            跳出。
        完。
        i 为 i 加 1。
    完。
    若 等号位 小于 0：
        返还 0。
    完。
    c 的 名称 为 做 字符串_截取_去空格 用 字符串、0、等号位。
    c 的 值 为 做 字符串_截取_去空格 用 字符串、等号位 加 1、分号位。
    可变 pos 为 分号位 加 1。
    只要 pos 小于 长度：
        只要 pos 小于 长度 并且 做 字节取 用 字符串、pos 等于 32：
            pos 为 pos 加 1。
        完。
        可变 下一分号 为 长度。
        可变 j 为 pos。
        只要 j 小于 长度：
            若 做 字节取 用 字符串、j 等于 59：
                下一分号 为 j。
                跳出。
            完。
            j 为 j 加 1。
        完。
        属性 为 做 字符串_截取_去空格 用 字符串、pos、下一分号。
        若 做 字符串_不区分大小写相等 用 属性、「secure」：
            c 的 标志 为 c 的 标志 位或 NRAF_COOKIE_SECURE。
        否则若 做 字符串_不区分大小写相等 用 属性、「httponly」：
            c 的 标志 为 c 的 标志 位或 NRAF_COOKIE_HTTPONLY。
        否则若 做 字符串_开头不区分大小写 用 属性、「path=」：
            c 的 路径 为 做 字符串_截取 用 属性、5、做 字符串_长度 用 属性。
        否则若 做 字符串_开头不区分大小写 用 属性、「domain=」：
            c 的 域 为 做 字符串_截取 用 属性、7、做 字符串_长度 用 属性。
        完。
        pos 为 下一分号 加 1。
    完。
    返还 c。
完。

NRAF_CookieJar_添加 接受 jar、cookie：
    若 jar 等于 0 或者 cookie 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 jar 的 数量：
        若 做 字符串_相等 用 jar 的 cookies 的 第 i 项 的 名称、cookie 的 名称：
            jar 的 cookies 的 第 i 项 为 cookie。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 jar 的 数量 小于 NRAF_最大Cookie：
        jar 的 cookies 的 第 jar 的 数量 项 为 cookie。
        jar 的 数量 为 jar 的 数量 加 1。
    完。
完。

NRAF_CookieJar_序列化 接受 jar：
    若 jar 等于 0 或者 jar 的 数量 等于 0：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 jar 的 数量：
        若 i 大于 0：
            结果 为 结果 加 「; 」。
        完。
        结果 为 结果 加 jar 的 cookies 的 第 i 项 的 名称 加 「=」 加 jar 的 cookies 的 第 i 项 的 值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

Document_设置文档元素 接受 文档、html元素：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        扩展 的 文档元素 为 html元素。
    完。
完。

Document_设置head 接受 文档、head元素：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        扩展 的 head元素 为 head元素。
    完。
完。

Document_设置body 接受 文档、body元素：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        扩展 的 body元素 为 body元素。
    完。
完。

NRAF_回调项 是 一种 事物，含 回调 为 0、id 为 0、优先级 为 1、预估时间 为 1000、已取消 为 假。

NRAF_执行帧回调 接受 文档、时间戳：
    NRAF_调度总数 为 NRAF_调度总数 加 1。
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 等于 0 或者 扩展 的 rAF回调数 等于 0：
        返还。
    完。
    帧开始 为 做 系统_微秒时间。
    帧预算 为 NRAF_平均帧时间。
    vsync周期 为 NRAF_FPS_60。
    可用时间 为 vsync周期 减 vsync周期 除以 10。
    可变 已用时间 为 0。
    可变 i 为 0。
    只要 i 小于 扩展 的 rAF回调数：
        回调 为 做 内存_读64 用 扩展 的 rAF回调列表 加 i 乘 16。
        若 回调 不等于 0：
            若 已用时间 加 1000 大于 可用时间：
                NRAF_跳帧总数 为 NRAF_跳帧总数 加 1。
                跳出。
            完。
            cb开始 为 做 系统_微秒时间。
            做 回调 用 时间戳。
            NRAF_回调总数 为 NRAF_回调总数 加 1。
            cb时间 为 做 系统_微秒时间 减 cb开始。
            已用时间 为 已用时间 加 cb时间。
        完。
        i 为 i 加 1。
    完。
    扩展 的 rAF回调数 为 0。
    帧总时间 为 做 系统_微秒时间 减 帧开始。
    NRAF_平均帧时间 为 (NRAF_平均帧时间 乘 7 加 帧总时间) 除以 8。
    若 帧总时间 大于 NRAF_最大帧时间：
        NRAF_最大帧时间 为 帧总时间。
    完。
完。

AI页面摘要 是 一种 事物，含 页面类型 为 0、主内容区 为 0、导航区 为 0、广告区数量 为 0、正文节点数 为 0、图片数 为 0、链接数 为 0、表单数 为 0、DOM节点总数 为 0、最大DOM深度 为 0。

NRAF_AI_分析页面 接受 文档：
    NRAF_AI分析总数 为 NRAF_AI分析总数 加 1。
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 等于 0：
        返还 0。
    完。
    摘要 为 新建 AI页面摘要。
    摘要 的 DOM节点总数 为 做 NRAF_统计DOM节点数 用 扩展 的 文档元素。
    摘要 的 最大DOM深度 为 做 NRAF_统计DOM深度 用 扩展 的 文档元素、0。
    摘要 的 图片数 为 做 NRAF_统计标签数 用 扩展 的 文档元素、「img」。
    摘要 的 链接数 为 做 NRAF_统计标签数 用 扩展 的 文档元素、「a」。
    摘要 的 表单数 为 做 NRAF_统计标签数 用 扩展 的 文档元素、「form」。
    特征大小 为 5。
    tensor 为 做 Tensor_创建 用 特征大小。
    做 Tensor_写 用 tensor、0、摘要 的 DOM节点总数。
    做 Tensor_写 用 tensor、1、摘要 的 图片数。
    做 Tensor_写 用 tensor、2、摘要 的 链接数。
    做 Tensor_写 用 tensor、3、摘要 的 表单数。
    做 Tensor_写 用 tensor、4、摘要 的 最大DOM深度。
    结果 为 做 AI总线_推理 用 全局AI总线、「NRAF_页面分析」、tensor、特征大小。
    若 结果 不等于 0：
        摘要 的 页面类型 为 做 Tensor_读 用 结果、0。
        做 Tensor_释放 用 结果。
    完。
    做 Tensor_释放 用 tensor。
    返还 摘要。
完。

NRAF_统计DOM节点数 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 计数 为 1。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        计数 为 计数 加 做 NRAF_统计DOM节点数 用 子。
        子 为 子 的 下一兄弟。
    完。
    返还 计数。
完。

NRAF_统计DOM深度 接受 节点、当前深度：
    若 节点 等于 0：
        返还 当前深度。
    完。
    可变 最大 为 当前深度。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        d 为 做 NRAF_统计DOM深度 用 子、当前深度 加 1。
        若 d 大于 最大：
            最大 为 d。
        完。
        子 为 子 的 下一兄弟。
    完。
    返还 最大。
完。

NRAF_统计标签数 接受 节点、标签名：
    若 节点 等于 0：
        返还 0。
    完。
    可变 计数 为 0。
    若 做 字符串_不区分大小写相等 用 节点 的 标签名、标签名：
        计数 为 1。
    完。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        计数 为 计数 加 做 NRAF_统计标签数 用 子、标签名。
        子 为 子 的 下一兄弟。
    完。
    返还 计数。
完。

NRAF_诊断打印 接受 零参：
    做 内核信息 用 「=== NRAF 动画帧调度诊断 ===」。
    做 内核信息 用 「  调度总数:   」 加 NRAF_调度总数。
    做 内核信息 用 「  回调总数:   」 加 NRAF_回调总数。
    做 内核信息 用 「  跳帧总数:   」 加 NRAF_跳帧总数。
    做 内核信息 用 「  AI分析总数: 」 加 NRAF_AI分析总数。
    做 内核信息 用 「  平均帧时(μs): 」 加 NRAF_平均帧时间。
    做 内核信息 用 「  最大帧时(μs): 」 加 NRAF_最大帧时间。
    若 NRAF_调度总数 大于 0：
        跳帧率 为 NRAF_跳帧总数 乘 100 除以 NRAF_调度总数。
        做 内核信息 用 「  跳帧率:     」 加 跳帧率 加 「%」。
    完。
    做 内核信息 用 「=========================」。
完。

NRAF_最大空闲回调 为 64。

可变 NRAF_空闲回调数 为 0。

可变 NRAF_空闲下一ID 为 1。

NRAF_空闲回调项 是 一种 事物，含 回调 为 0、id 为 0、超时毫秒 为 0、注册时间 为 0、已取消 为 假。

可变 NRAF_空闲回调列表 为 一组：（空）。

Document_requestIdleCallback 接受 文档、回调、超时毫秒：
    若 NRAF_空闲回调数 大于或等于 NRAF_最大空闲回调：
        返还 0。
    完。
    id 为 NRAF_空闲下一ID。
    NRAF_空闲下一ID 为 NRAF_空闲下一ID 加 1。
    项 为 新建 NRAF_空闲回调项：回调 为 回调、id 为 id、超时毫秒 为 超时毫秒、注册时间 为 做 系统_毫秒时间、已取消 为 假。
    NRAF_空闲回调列表 的 第 NRAF_空闲回调数 项 为 项。
    NRAF_空闲回调数 为 NRAF_空闲回调数 加 1。
    返还 id。
完。

Document_cancelIdleCallback 接受 文档、id：
    可变 i 为 0。
    只要 i 小于 NRAF_空闲回调数：
        若 NRAF_空闲回调列表 的 第 i 项 的 id 等于 id：
            NRAF_空闲回调列表 的 第 i 项 的 已取消 为 真。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

NRAF_执行空闲回调 接受 剩余微秒：
    若 NRAF_空闲回调数 等于 0：
        返还。
    完。
    可变 已用 为 0。
    可变 i 为 0。
    可变 已完成 为 0。
    只要 i 小于 NRAF_空闲回调数 并且 已用 小于 剩余微秒：
        项 为 NRAF_空闲回调列表 的 第 i 项。
        若 项 的 已取消 等于 假 并且 项 的 回调 不等于 0：
            已等待 为 做 系统_毫秒时间 减 项 的 注册时间。
            剩余时间 为 剩余微秒 减 已用。
            deadline 为 一本字典：剩余时间 对应 剩余时间、已超时 对应 项 的 超时毫秒 大于 0 并且 已等待 大于或等于 项 的 超时毫秒。
            cb开始 为 做 系统_微秒时间。
            做 项 的 回调 用 deadline。
            已用 为 已用 加 (做 系统_微秒时间 减 cb开始)。
        完。
        已完成 为 已完成 加 1。
        i 为 i 加 1。
    完。
    若 已完成 大于 0：
        可变 k 为 已完成。
        可变 w 为 0。
        只要 k 小于 NRAF_空闲回调数：
            NRAF_空闲回调列表 的 第 w 项 为 NRAF_空闲回调列表 的 第 k 项。
            w 为 w 加 1。
            k 为 k 加 1。
        完。
        NRAF_空闲回调数 为 NRAF_空闲回调数 减 已完成。
    完。
完。

NRAF_可见_可见 为 0。

NRAF_可见_隐藏 为 1。

NRAF_可见_预渲染 为 2。

Document_获取可见性 接受 文档：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        返还 扩展 的 可见状态。
    完。
    返还 NRAF_可见_可见。
完。

Document_设置可见性 接受 文档、状态：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 等于 0：
        返还。
    完。
    旧状态 为 扩展 的 可见状态。
    扩展 的 可见状态 为 状态。
    若 旧状态 不等于 状态：
        做 Document_触发事件 用 文档、「visibilitychange」。
        若 状态 等于 NRAF_可见_隐藏：
            扩展 的 动画暂停 为 真。
        否则若 状态 等于 NRAF_可见_可见：
            扩展 的 动画暂停 为 假。
        完。
    完。
完。

NRAF_性能最大条目 为 512。

NRAF_性能条目 是 一种 事物，含 类型 为 0、开始时间 为 0、持续时间 为 0、名称 为 0。

NRAF_性能时间线 是 一种 事物，含 条目列表 为 一组：（空）、数量 为 0。

可变 NRAF_全局性能时间线 为 新建 NRAF_性能时间线：数量 为 0。

NRAF_记录性能 接受 类型、名称、开始、持续：
    若 NRAF_全局性能时间线 的 数量 大于或等于 NRAF_性能最大条目：
        可变 i 为 0。
        只要 i 小于 NRAF_全局性能时间线 的 数量 减 1：
            NRAF_全局性能时间线 的 条目列表 的 第 i 项 为 NRAF_全局性能时间线 的 条目列表 的 第 i 加 1 项。
            i 为 i 加 1。
        完。
        NRAF_全局性能时间线 的 数量 为 NRAF_全局性能时间线 的 数量 减 1。
    完。
    项 为 新建 NRAF_性能条目：类型 为 类型、开始时间 为 开始、持续时间 为 持续、名称 为 名称。
    NRAF_全局性能时间线 的 条目列表 的 第 NRAF_全局性能时间线 的 数量 项 为 项。
    NRAF_全局性能时间线 的 数量 为 NRAF_全局性能时间线 的 数量 加 1。
完。

NRAF_查询长帧 接受 阈值微秒：
    可变 结果 为 一组：（空）。
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 NRAF_全局性能时间线 的 数量：
        项 为 NRAF_全局性能时间线 的 条目列表 的 第 i 项。
        若 项 的 持续时间 大于 阈值微秒：
            结果 的 第 计数 项 为 项。
            计数 为 计数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一本字典：条目 对应 结果、数量 对应 计数。
完。

NRAF_帧历史大小 为 16。

可变 NRAF_帧历史 为 一组：（空）。

可变 NRAF_帧历史指针 为 0。

NRAF_AI_预测下一帧时间 接受 零参：
    tensor 为 做 Tensor_创建 用 NRAF_帧历史大小。
    可变 i 为 0。
    只要 i 小于 NRAF_帧历史大小：
        idx 为 (NRAF_帧历史指针 加 i) 取余于 NRAF_帧历史大小。
        做 Tensor_写 用 tensor、i、NRAF_帧历史 的 第 idx 项。
        i 为 i 加 1。
    完。
    结果 为 做 AI总线_推理 用 全局AI总线、「NRAF_帧预测」、tensor、NRAF_帧历史大小。
    可变 预测 为 NRAF_平均帧时间。
    若 结果 不等于 0：
        预测 为 做 Tensor_读 用 结果、0。
        做 Tensor_释放 用 结果。
    完。
    做 Tensor_释放 用 tensor。
    返还 预测。
完。

NRAF_记录帧时间 接受 微秒：
    NRAF_帧历史 的 第 NRAF_帧历史指针 项 为 微秒。
    NRAF_帧历史指针 为 (NRAF_帧历史指针 加 1) 取余于 NRAF_帧历史大小。
完。

NRAF_页面类型名称 接受 类型：
    若 类型 等于 页面类型_文章：
        返还 「文章页」。
    完。
    若 类型 等于 页面类型_商品：
        返还 「商品页」。
    完。
    若 类型 等于 页面类型_搜索：
        返还 「搜索页」。
    完。
    若 类型 等于 页面类型_视频：
        返还 「视频页」。
    完。
    若 类型 等于 页面类型_社交：
        返还 「社交页」。
    完。
    若 类型 等于 页面类型_应用：
        返还 「应用页」。
    完。
    若 类型 等于 页面类型_表单：
        返还 「表单页」。
    完。
    返还 「未知」。
完。

NRAF_重置统计 接受 零参：
    NRAF_调度总数 为 0。
    NRAF_回调总数 为 0。
    NRAF_跳帧总数 为 0。
    NRAF_AI分析总数 为 0。
    NRAF_平均帧时间 为 16667。
    NRAF_最大帧时间 为 0。
    NRAF_全局性能时间线 的 数量 为 0。
    NRAF_帧历史指针 为 0。
完。

本章 收束。
