《控制台接口》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·标准·原生字符串》。

引用《.·内核直连层》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内容可见性》。

引用《..·标准·加法运算符》。

日志级别_LOG 为 0。

日志级别_WARN 为 1。

日志级别_ERROR 为 2。

日志级别_INFO 为 3。

日志级别_DEBUG 为 4。

ConsoleTimer 是 一种 事物，含 标签、开始时间。

控制台计时器 为 一组：16。

ConsoleTimer。

控制台计时器数 为 0。

Console_log 接受 args、参数数：
    做 Console_输出 用 日志级别_LOG、args、参数数。
完。

Console_warn 接受 args、参数数：
    做 Console_输出 用 日志级别_WARN、args、参数数。
完。

Console_error 接受 args、参数数：
    做 Console_输出 用 日志级别_ERROR、args、参数数。
完。

Console_输出 接受 级别、args、参数数：
    buf 为 做 堆_分配 用 4096。
    可变 pos 为 0。
    若 级别 等于 日志级别_WARN：
        pos 为 做 字符串_追加 用 buf、pos、「[WARN] 」。
    否则若 级别 等于 日志级别_ERROR：
        pos 为 做 字符串_追加 用 buf、pos、「[ERROR] 」。
    完。
    可变 i 为 0。
    只要 i 小于 参数数 并且 pos 小于 3900：
        若 i 大于 0：
            pos 为 做 字符串_追加 用 buf、pos、「 」。
        完。
        arg 为 做 内存_读64 用 args 加 i 乘 8。
        str 为 做 JSValue_ToString 用 0、arg。
        若 str 不等于 0：
            pos 为 做 字符串_追加 用 buf、pos、str。
        完。
        i 为 i 加 1。
    完。
    做 内存_写8 用 buf 加 pos、0。
    做 内核信息 用 buf。
    做 DevTools_Console消息 用 级别、buf。
    做 堆_释放 用 buf。
完。

Console_time 接受 标签：
    若 控制台计时器数 小于 16：
        控制台计时器 的 第 控制台计时器数 项 的 标签 为 标签。
        控制台计时器 的 第 控制台计时器数 项 的 开始时间 为 做 平台_获取高精度时间。
        控制台计时器数 为 控制台计时器数 加 1。
    完。
完。

Console_timeEnd 接受 标签：
    可变 i 为 0。
    只要 i 小于 控制台计时器数：
        若 做 字符串_相等 用 控制台计时器 的 第 i 项 的 标签、标签：
            耗时 为 做 平台_获取高精度时间 减 控制台计时器 的 第 i 项 的 开始时间。
            控制台计时器数 为 控制台计时器数 减 1。
            控制台计时器 的 第 i 项 为 控制台计时器 的 第 控制台计时器数 项。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

PerformanceEntry 是 一种 事物，含 名称、类型、开始时间、持续时间。

PerformanceTimeline 是 一种 事物，含 导航开始、条目、条目数。

Performance_now 接受 timeline：
    若 timeline 等于 0：
        返还 0。
    完。
    返还 (做 平台_获取高精度时间 减 timeline 的 导航开始) 除以 1000。
完。

Performance_mark 接受 timeline、名称：
    若 timeline 等于 0 或者 timeline 的 条目数 大于或等于 256：
        返还。
    完。
    idx 为 timeline 的 条目数。
    timeline 的 条目 的 第 idx 项 的 名称 为 名称。
    timeline 的 条目 的 第 idx 项 的 类型 为 「mark」。
    timeline 的 条目 的 第 idx 项 的 开始时间 为 做 Performance_now 用 timeline。
    timeline 的 条目 的 第 idx 项 的 持续时间 为 0。
    timeline 的 条目数 为 timeline 的 条目数 加 1。
完。

Performance_measure 接受 timeline、名称、开始标记、结束标记：
    若 timeline 等于 0：
        返还。
    完。
    开始 为 做 Performance_查找标记 用 timeline、开始标记。
    结束 为 做 Performance_查找标记 用 timeline、结束标记。
    若 开始 大于或等于 0 并且 结束 大于或等于 0 并且 timeline 的 条目数 小于 256：
        idx 为 timeline 的 条目数。
        timeline 的 条目 的 第 idx 项 的 名称 为 名称。
        timeline 的 条目 的 第 idx 项 的 类型 为 「measure」。
        timeline 的 条目 的 第 idx 项 的 开始时间 为 timeline 的 条目 的 第 开始 项 的 开始时间。
        timeline 的 条目 的 第 idx 项 的 持续时间 为 timeline 的 条目 的 第 结束 项 的 开始时间 减 timeline 的 条目 的 第 开始 项 的 开始时间。
        timeline 的 条目数 为 timeline 的 条目数 加 1。
    完。
完。

Performance_查找标记 接受 timeline、名称：
    可变 i 为 timeline 的 条目数 减 1。
    只要 i 大于或等于 0：
        若 做 字符串_相等 用 timeline 的 条目 的 第 i 项 的 名称、名称：
            返还 i。
        完。
        i 为 i 减 1。
    完。
    返还 负 1。
完。

本章 收束。
