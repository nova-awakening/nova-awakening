《CSS网格布局》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《..·中枢·内存初始化》。

最大轨道 为 64。

最大网格项 为 256。

大小_自动 为 负 1。

大小_最小内容 为 负 2。

大小_最大内容 为 负 3。

网格轨道 是 一种 事物，含 最小大小、最大大小、实际大小、弹性系数。

网格项 是 一种 事物，含 元素标识、列起始、列跨度、行起始、行跨度、最小宽、最小高、首选宽、首选高、实际横、实际纵、实际宽、实际高。

网格容器 是 一种 事物，含 列轨道表、行轨道表、列数、行数、列间距、行间距、容器宽、容器高、项表、项数、布局次数。

可变 全局网格 为 0。

可变 智能跳过 为 0。

网格_创建 接受 列数、行数、列间距、行间距：
    网格 为 做 堆_分配 用 网格容器。
    网格 的 列数 为 列数。
    网格 的 行数 为 行数。
    网格 的 列间距 为 列间距。
    网格 的 行间距 为 行间距。
    网格 的 列轨道表 为 做 堆_分配 用 最大轨道 乘 16。
    网格 的 行轨道表 为 做 堆_分配 用 最大轨道 乘 16。
    网格 的 项表 为 一组：（空）。
    网格 的 项数 为 0。
    网格 的 容器宽 为 0。
    网格 的 容器高 为 0。
    网格 的 布局次数 为 0。
    可变 序号 为 0。
    只要 序号 小于 列数：
        做 内存_写32 用 网格 的 列轨道表 加 序号 乘 16、大小_自动。
        做 内存_写32 用 网格 的 列轨道表 加 序号 乘 16 加 4、大小_自动。
        做 内存_写32 用 网格 的 列轨道表 加 序号 乘 16 加 8、0。
        做 内存_写32 用 网格 的 列轨道表 加 序号 乘 16 加 12、1。
        序号 为 序号 加 1。
    完。
    序号 为 0。
    只要 序号 小于 行数：
        做 内存_写32 用 网格 的 行轨道表 加 序号 乘 16、大小_自动。
        做 内存_写32 用 网格 的 行轨道表 加 序号 乘 16 加 4、大小_自动。
        做 内存_写32 用 网格 的 行轨道表 加 序号 乘 16 加 8、0。
        做 内存_写32 用 网格 的 行轨道表 加 序号 乘 16 加 12、1。
        序号 为 序号 加 1。
    完。
    返还 网格。
完。

网格_设列轨道 接受 网格、列号、最小、最大、弹性：
    若 列号 大于或等于 网格 的 列数：
        返还。
    完。
    做 内存_写32 用 网格 的 列轨道表 加 列号 乘 16、最小。
    做 内存_写32 用 网格 的 列轨道表 加 列号 乘 16 加 4、最大。
    做 内存_写32 用 网格 的 列轨道表 加 列号 乘 16 加 12、弹性。
完。

网格_设行轨道 接受 网格、行号、最小、最大、弹性：
    若 行号 大于或等于 网格 的 行数：
        返还。
    完。
    做 内存_写32 用 网格 的 行轨道表 加 行号 乘 16、最小。
    做 内存_写32 用 网格 的 行轨道表 加 行号 乘 16 加 4、最大。
    做 内存_写32 用 网格 的 行轨道表 加 行号 乘 16 加 12、弹性。
完。

网格_添加项 接受 网格、元素、列起、列跨、行起、行跨、最小宽、最小高：
    若 网格 的 项数 大于或等于 最大网格项：
        返还 负 1。
    完。
    项 为 新建 网格项。
    项 的 元素标识 为 元素。
    项 的 列起始 为 列起。
    项 的 列跨度 为 列跨。
    项 的 行起始 为 行起。
    项 的 行跨度 为 行跨。
    项 的 最小宽 为 最小宽。
    项 的 最小高 为 最小高。
    项 的 首选宽 为 最小宽。
    项 的 首选高 为 最小高。
    项 的 实际横 为 0。
    项 的 实际纵 为 0。
    项 的 实际宽 为 0。
    项 的 实际高 为 0。
    做 追加 用 网格 的 项表、项。
    索引 为 网格 的 项数。
    网格 的 项数 为 网格 的 项数 加 1。
    返还 索引。
完。

网格_布局 接受 网格、可用宽、可用高：
    网格 的 容器宽 为 可用宽。
    网格 的 容器高 为 可用高。
    网格 的 布局次数 为 网格 的 布局次数 加 1。
    做 网格_解析列轨道 用 网格、可用宽。
    做 网格_解析行轨道 用 网格、可用高。
    做 网格_放置项 用 网格。
完。

网格_解析列轨道 接受 网格、可用宽：
    总间距 为 (网格 的 列数 减 1) 乘 网格 的 列间距。
    可分配 为 可用宽 减 总间距。
    可变 固定占用 为 0。
    可变 弹性总 为 0。
    可变 序号 为 0。
    只要 序号 小于 网格 的 列数：
        最小 为 做 内存_读32 用 网格 的 列轨道表 加 序号 乘 16。
        弹性 为 做 内存_读32 用 网格 的 列轨道表 加 序号 乘 16 加 12。
        若 最小 大于 0：
            做 内存_写32 用 网格 的 列轨道表 加 序号 乘 16 加 8、最小。
            固定占用 为 固定占用 加 最小。
        否则：
            弹性总 为 弹性总 加 弹性。
        完。
        序号 为 序号 加 1。
    完。
    剩余 为 可分配 减 固定占用。
    若 剩余 大于 0 并且 弹性总 大于 0：
        序号 为 0。
        只要 序号 小于 网格 的 列数：
            最小 为 做 内存_读32 用 网格 的 列轨道表 加 序号 乘 16。
            若 最小 小于或等于 0：
                弹性 为 做 内存_读32 用 网格 的 列轨道表 加 序号 乘 16 加 12。
                分配 为 剩余 乘 弹性 除以 弹性总。
                做 内存_写32 用 网格 的 列轨道表 加 序号 乘 16 加 8、分配。
            完。
            序号 为 序号 加 1。
        完。
    完。
完。

网格_解析行轨道 接受 网格、可用高：
    总间距 为 (网格 的 行数 减 1) 乘 网格 的 行间距。
    可分配 为 可用高 减 总间距。
    可变 固定占用 为 0。
    可变 弹性总 为 0。
    可变 序号 为 0。
    只要 序号 小于 网格 的 行数：
        最小 为 做 内存_读32 用 网格 的 行轨道表 加 序号 乘 16。
        弹性 为 做 内存_读32 用 网格 的 行轨道表 加 序号 乘 16 加 12。
        若 最小 大于 0：
            做 内存_写32 用 网格 的 行轨道表 加 序号 乘 16 加 8、最小。
            固定占用 为 固定占用 加 最小。
        否则：
            弹性总 为 弹性总 加 弹性。
        完。
        序号 为 序号 加 1。
    完。
    剩余 为 可分配 减 固定占用。
    若 剩余 大于 0 并且 弹性总 大于 0：
        序号 为 0。
        只要 序号 小于 网格 的 行数：
            最小 为 做 内存_读32 用 网格 的 行轨道表 加 序号 乘 16。
            若 最小 小于或等于 0：
                弹性 为 做 内存_读32 用 网格 的 行轨道表 加 序号 乘 16 加 12。
                分配 为 剩余 乘 弹性 除以 弹性总。
                做 内存_写32 用 网格 的 行轨道表 加 序号 乘 16 加 8、分配。
            完。
            序号 为 序号 加 1。
        完。
    完。
完。

网格_放置项 接受 网格：
    可变 序号 为 0。
    只要 序号 小于 网格 的 项数：
        项 为 网格 的 项表 的 第 序号 项。
        可变 横偏移 为 0。
        可变 列 为 0。
        只要 列 小于 项 的 列起始：
            横偏移 为 横偏移 加 做 内存_读32 用 网格 的 列轨道表 加 列 乘 16 加 8 加 网格 的 列间距。
            列 为 列 加 1。
        完。
        可变 项宽 为 0。
        只要 列 小于 项 的 列起始 加 项 的 列跨度 并且 列 小于 网格 的 列数：
            项宽 为 项宽 加 做 内存_读32 用 网格 的 列轨道表 加 列 乘 16 加 8。
            若 列 大于 项 的 列起始：
                项宽 为 项宽 加 网格 的 列间距。
            完。
            列 为 列 加 1。
        完。
        可变 纵偏移 为 0。
        可变 行 为 0。
        只要 行 小于 项 的 行起始：
            纵偏移 为 纵偏移 加 做 内存_读32 用 网格 的 行轨道表 加 行 乘 16 加 8 加 网格 的 行间距。
            行 为 行 加 1。
        完。
        可变 项高 为 0。
        只要 行 小于 项 的 行起始 加 项 的 行跨度 并且 行 小于 网格 的 行数：
            项高 为 项高 加 做 内存_读32 用 网格 的 行轨道表 加 行 乘 16 加 8。
            若 行 大于 项 的 行起始：
                项高 为 项高 加 网格 的 行间距。
            完。
            行 为 行 加 1。
        完。
        项 的 实际横 为 横偏移。
        项 的 实际纵 为 纵偏移。
        项 的 实际宽 为 项宽。
        项 的 实际高 为 项高。
        序号 为 序号 加 1。
    完。
完。

网格_获取项位置 接受 网格、项索引：
    若 项索引 大于或等于 网格 的 项数：
        返还 一组：0、0、0、0。
    完。
    项 为 网格 的 项表 的 第 项索引 项。
    返还 一组：项 的 实际横、项 的 实际纵、项 的 实际宽、项 的 实际高。
完。

网格_智能初始化 接受 零参：
    总线 为 全局智能总线。
    若 总线 不等于 0：
        做 智能总线_注册模块 用 总线、「网格布局」、网格_智能回调。
    完。
完。

网格_智能回调 接受 事件、数据：
    若 事件 等于 1：
        智能跳过 为 智能跳过 加 1。
    完。
完。

网格_完整统计 接受 零参：
    若 全局网格 等于 0：
        返还 一组：0、0、0。
    完。
    返还 一组：全局网格 的 布局次数、全局网格 的 项数、智能跳过。
完。

本章 收束。
