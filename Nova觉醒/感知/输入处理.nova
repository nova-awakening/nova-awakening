《输入处理》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·事件系统核心》。

引用《.·内核直连层》。

引用《..·执行·VSync帧调度》。

引用《.·GBK索引》。

引用《..·中枢·辅助存根》。

引用《.·文档事件》。

引用《.·文档节点》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

Input_处理按键 接受 state、元素、键码、字符、修饰键：
    若 state 的 已禁用 或者 state 的 只读：
        返还。
    完。
    若 state 的 输入类型 等于 输入类型_checkbox 并且 键码 等于 0x20：
        state 的 已选中 为 非 state 的 已选中。
        事件 为 做 事件_创建 用 「change」、真、假。
        做 事件Target_dispatch事件 用 元素、事件。
        返还。
    完。
    若 state 的 输入类型 等于 输入类型_radio 并且 键码 等于 0x20：
        做 Input_选中Radio 用 state、元素。
        返还。
    完。
    若 state 的 输入类型 大于 输入类型_search：
        返还。
    完。
    当前值 为 state 的 值。
    可变 值长度 为 做 字符串_长度 用 当前值。
    若 修饰键 位与 1 并且 键码 等于 0x41：
        state 的 选区起始 为 0。
        state 的 选区结束 为 值长度。
        返还。
    完。
    若 修饰键 位与 1 并且 键码 等于 0x43：
        若 state 的 选区起始 不等于 state 的 选区结束：
            选中文本 为 做 字符串_子串 用 当前值、state 的 选区起始、state 的 选区结束 减 state 的 选区起始。
            做 剪贴板_写入文本 用 选中文本。
        完。
        返还。
    完。
    若 修饰键 位与 1 并且 键码 等于 0x56：
        粘贴文本 为 做 剪贴板_读取文本。
        若 粘贴文本 不等于 0：
            做 Input_插入文本 用 state、元素、粘贴文本。
        完。
        返还。
    完。
    若 修饰键 位与 1 并且 键码 等于 0x58：
        若 state 的 选区起始 不等于 state 的 选区结束：
            选中文本 为 做 字符串_子串 用 当前值、state 的 选区起始、state 的 选区结束 减 state 的 选区起始。
            做 剪贴板_写入文本 用 选中文本。
            做 Input_删除选区 用 state、元素。
        完。
        返还。
    完。
    若 键码 等于 0x08：
        若 state 的 选区起始 不等于 state 的 选区结束：
            做 Input_删除选区 用 state、元素。
        否则若 state 的 选区起始 大于 0：
            前半 为 做 字符串_子串 用 当前值、0、state 的 选区起始 减 1。
            后半 为 做 字符串_子串 用 当前值、state 的 选区起始、值长度 减 state 的 选区起始。
            state 的 值 为 做 字符串_连接 用 前半、后半。
            state 的 选区起始 为 state 的 选区起始 减 1。
            state 的 选区结束 为 state 的 选区起始。
            做 Input_触发input事件 用 元素。
        完。
        返还。
    完。
    若 键码 等于 0x2E：
        若 state 的 选区起始 不等于 state 的 选区结束：
            做 Input_删除选区 用 state、元素。
        否则若 state 的 选区起始 小于 值长度：
            前半 为 做 字符串_子串 用 当前值、0、state 的 选区起始。
            后半 为 做 字符串_子串 用 当前值、state 的 选区起始 加 1、值长度 减 state 的 选区起始 减 1。
            state 的 值 为 做 字符串_连接 用 前半、后半。
            做 Input_触发input事件 用 元素。
        完。
        返还。
    完。
    若 键码 等于 0x25：
        若 修饰键 位与 2：
            若 state 的 选区起始 大于 0：
                state 的 选区起始 为 state 的 选区起始 减 1。
            完。
        否则：
            若 state 的 选区起始 大于 0：
                state 的 选区起始 为 state 的 选区起始 减 1。
            完。
            state 的 选区结束 为 state 的 选区起始。
        完。
        返还。
    完。
    若 键码 等于 0x27：
        若 修饰键 位与 2：
            若 state 的 选区结束 小于 值长度：
                state 的 选区结束 为 state 的 选区结束 加 1。
            完。
        否则：
            若 state 的 选区结束 小于 值长度：
                state 的 选区结束 为 state 的 选区结束 加 1。
            完。
            state 的 选区起始 为 state 的 选区结束。
        完。
        返还。
    完。
    若 键码 等于 0x24：
        state 的 选区起始 为 0。
        state 的 选区结束 为 0。
        返还。
    完。
    若 键码 等于 0x23：
        state 的 选区起始 为 值长度。
        state 的 选区结束 为 值长度。
        返还。
    完。
    若 键码 等于 0x0D：
        若 state 的 所属表单 不等于 0：
            做 Form_提交 用 state 的 所属表单。
        完。
        返还。
    完。
    若 字符 大于或等于 0x20：
        字符buf 为 做 堆_分配 用 8。
        写入 为 做 UTF8_编码码点 用 字符、字符buf、0。
        做 内存_写8 用 字符buf 加 写入、0。
        做 Input_插入文本 用 state、元素、字符buf。
    完。
完。

Input_插入文本 接受 state、元素、文本：
    若 state 的 最大长度 大于或等于 0：
        当前长 为 做 字符串_长度 用 state 的 值。
        插入长 为 做 字符串_长度 用 文本。
        选区长 为 state 的 选区结束 减 state 的 选区起始。
        若 当前长 减 选区长 加 插入长 大于 state 的 最大长度：
            返还。
        完。
    完。
    当前值 为 state 的 值。
    值长度 为 做 字符串_长度 用 当前值。
    前半 为 做 字符串_子串 用 当前值、0、state 的 选区起始。
    后半 为 做 字符串_子串 用 当前值、state 的 选区结束、值长度 减 state 的 选区结束。
    state 的 值 为 做 字符串_连接 用 前半、文本。
    state 的 值 为 做 字符串_连接 用 state 的 值、后半。
    state 的 选区起始 为 state 的 选区起始 加 做 字符串_长度 用 文本。
    state 的 选区结束 为 state 的 选区起始。
    做 Input_触发input事件 用 元素。
完。

Input_删除选区 接受 state、元素：
    当前值 为 state 的 值。
    值长度 为 做 字符串_长度 用 当前值。
    前半 为 做 字符串_子串 用 当前值、0、state 的 选区起始。
    后半 为 做 字符串_子串 用 当前值、state 的 选区结束、值长度 减 state 的 选区结束。
    state 的 值 为 做 字符串_连接 用 前半、后半。
    state 的 选区结束 为 state 的 选区起始。
    做 Input_触发input事件 用 元素。
完。

Input_触发input事件 接受 元素：
    事件 为 做 事件_创建 用 「input」、真、假。
    事件 的 是受信任的 为 真。
    做 事件Target_dispatch事件 用 元素、事件。
完。

Input_选中Radio 接受 state、元素：
    若 state 的 名 等于 0：
        返还。
    完。
    若 state 的 所属表单 不等于 0：
    完。
    state 的 已选中 为 真。
    事件 为 做 事件_创建 用 「change」、真、假。
    做 事件Target_dispatch事件 用 元素、事件。
完。

可变 AI_输入处理_事件频率 为 0。

可变 AI_输入处理_节流阈值 为 16。

AI_输入处理_节流判断 接受 事件类型、间隔毫秒：
    AI_输入处理_事件频率 为 AI_输入处理_事件频率 加 1。
    若 事件类型 等于 「滚动」 并且 间隔毫秒 小于 AI_输入处理_节流阈值：
        返还 真。
    完。
    若 事件类型 等于 「移动」 并且 间隔毫秒 小于 AI_输入处理_节流阈值：
        返还 真。
    完。
    返还 假。
完。

AI_输入处理_优先级排序 接受 事件类型：
    若 事件类型 等于 「点击」：
        返还 100。
    完。
    若 事件类型 等于 「输入」：
        返还 90。
    完。
    若 事件类型 等于 「滚动」：
        返还 30。
    完。
    返还 50。
完。

本章 收束。
