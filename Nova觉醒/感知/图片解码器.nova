《图片解码器》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《.·解压》。

引用《.·图片逆滤波恢》。

引用《..·中枢·自动扩容》。

引用《.·历史与位置接口》。

引用《..·中枢·内核打印》。

引用《..·执行·底层内存操》。

引用《..·中枢·整数转字符串》。

PNG状态_初始 为 0。

PNG状态_头部已解析 为 1。

PNG状态_解码中 为 2。

PNG状态_完成 为 3。

PNG状态_错误 为 4。

PNG签名 为 一组：0x89、0x50、0x4E、0x47、0x0D、0x0A、0x1A、0x0A。

CHUNK_IHDR 为 0x49484452。

CHUNK_PLTE 为 0x504C5445。

CHUNK_IDAT 为 0x49444154。

CHUNK_IEND 为 0x49454E44。

CHUNK_tRNS 为 0x74524E53。

CHUNK_gAMA 为 0x67414D41。

CHUNK_cHRM 为 0x6348524D。

CHUNK_sRGB 为 0x73524742。

CHUNK_tEXt 为 0x74455874。

CHUNK_iTXt 为 0x69545874。

CHUNK_pHYs 为 0x70485973。

CHUNK_tIME 为 0x74494D45。

CHUNK_bKGD 为 0x624B4744。

CHUNK_iCCP 为 0x69434350。

PNG解码器 是 一种 事物，含 宽、高、位深、颜色类型、压缩方法、滤波方法、隔行方法、行字节、像素字节、调色板、调色板大小、透明度、透明度大小、伽马值、sRGB渲染意图、像素缓冲、像素大小、上一行缓冲、状态、错误码、元数据、物理_X分辨率、物理_Y分辨率、物理_单位、背景色。

可变 PNG统计_解码次数 为 0。

可变 PNG统计_总字节 为 0。

可变 PNG统计_错误次数 为 0。

PNG解码器_创建 接受 零参：
    dec 为 做 堆_分配 用 PNG解码器。
    dec 的 宽 为 0。
    dec 的 高 为 0。
    dec 的 位深 为 8。
    dec 的 颜色类型 为 0。
    dec 的 调色板 为 0。
    dec 的 调色板大小 为 0。
    dec 的 透明度 为 0。
    dec 的 透明度大小 为 0。
    dec 的 伽马值 为 0。
    dec 的 sRGB渲染意图 为 负 1。
    dec 的 像素缓冲 为 0。
    dec 的 上一行缓冲 为 0。
    dec 的 状态 为 PNG状态_初始。
    dec 的 错误码 为 0。
    dec 的 元数据 为 做 字典。
    dec 的 物理_X分辨率 为 0。
    dec 的 物理_Y分辨率 为 0。
    dec 的 物理_单位 为 0。
    dec 的 背景色 为 0。
    返还 dec。
完。

PNG_验证签名 接受 数据：
    可变 i 为 0。
    只要 i 小于 8：
        若 做 内存_读8 用 数据 加 i 不等于 PNG签名 的 第 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

PNG解码器_解析IHDR 接受 dec、数据、偏移、数据长度：
    若 偏移 加 13 大于 数据长度：
        dec 的 状态 为 PNG状态_错误。
        dec 的 错误码 为 3。
        返还。
    完。
    dec 的 宽 为 做 读32大端 用 数据 加 偏移。
    dec 的 高 为 做 读32大端 用 数据 加 偏移 加 4。
    dec 的 位深 为 做 内存_读8 用 数据 加 偏移 加 8。
    dec 的 颜色类型 为 做 内存_读8 用 数据 加 偏移 加 9。
    dec 的 压缩方法 为 做 内存_读8 用 数据 加 偏移 加 10。
    dec 的 滤波方法 为 做 内存_读8 用 数据 加 偏移 加 11。
    dec 的 隔行方法 为 做 内存_读8 用 数据 加 偏移 加 12。
    若 dec 的 宽 等于 0 或者 dec 的 高 等于 0：
        dec 的 状态 为 PNG状态_错误。
        dec 的 错误码 为 1。
        返还。
    完。
    若 dec 的 宽 大于 65535 或者 dec 的 高 大于 65535：
        dec 的 状态 为 PNG状态_错误。
        dec 的 错误码 为 2。
        返还。
    完。
    做 匹配 用 dec 的 颜色类型。
    dec 的 行字节 为 dec 的 宽 乘 dec 的 像素字节 乘 dec 的 位深 除以 8。
    dec 的 像素大小 为 dec 的 宽 乘 dec 的 高 乘 4。
    dec 的 像素缓冲 为 做 堆_分配_数组 用 dec 的 像素大小。
    dec 的 上一行缓冲 为 做 堆_分配_数组 用 dec 的 行字节。
    做 内存_清零 用 dec 的 上一行缓冲、dec 的 行字节。
    dec 的 状态 为 PNG状态_头部已解析。
完。

PNG解码 接受 数据、数据大小：
    PNG统计_解码次数 为 PNG统计_解码次数 加 1。
    PNG统计_总字节 为 PNG统计_总字节 加 数据大小。
    若 非 做 PNG_验证签名 用 数据：
        PNG统计_错误次数 为 PNG统计_错误次数 加 1。
        返还 0。
    完。
    dec 为 做 PNG解码器_创建。
    可变 偏移 为 8。
    IDAT缓冲 为 做 堆_分配_数组 用 数据大小。
    可变 IDAT大小 为 0。
    只要 偏移 加 8 小于或等于 数据大小：
        chunk长 为 做 读32大端 用 数据 加 偏移。
        chunk类型 为 做 读32大端 用 数据 加 偏移 加 4。
        chunk数据 为 偏移 加 8。
        若 chunk数据 加 chunk长 大于 数据大小：
            dec 的 状态 为 PNG状态_错误。
            dec 的 错误码 为 10。
            做 堆_释放 用 IDAT缓冲。
            PNG统计_错误次数 为 PNG统计_错误次数 加 1。
            返还 dec。
        完。
        若 chunk类型 等于 CHUNK_IHDR：
            做 PNG解码器_解析IHDR 用 dec、数据、chunk数据、数据大小。
            若 dec 的 状态 等于 PNG状态_错误：
                做 堆_释放 用 IDAT缓冲。
                PNG统计_错误次数 为 PNG统计_错误次数 加 1。
                返还 dec。
            完。
        否则若 chunk类型 等于 CHUNK_PLTE：
            dec 的 调色板 为 做 堆_分配_数组 用 chunk长。
            做 内存_复制 用 dec 的 调色板、数据 加 chunk数据、chunk长。
            dec 的 调色板大小 为 chunk长 除以 3。
        否则若 chunk类型 等于 CHUNK_tRNS：
            dec 的 透明度 为 做 堆_分配_数组 用 chunk长。
            做 内存_复制 用 dec 的 透明度、数据 加 chunk数据、chunk长。
            dec 的 透明度大小 为 chunk长。
        否则若 chunk类型 等于 CHUNK_gAMA：
            dec 的 伽马值 为 做 读32大端 用 数据 加 chunk数据。
        否则若 chunk类型 等于 CHUNK_sRGB：
            dec 的 sRGB渲染意图 为 做 内存_读8 用 数据 加 chunk数据。
        否则若 chunk类型 等于 CHUNK_pHYs：
            dec 的 物理_X分辨率 为 做 读32大端 用 数据 加 chunk数据。
            dec 的 物理_Y分辨率 为 做 读32大端 用 数据 加 chunk数据 加 4。
            dec 的 物理_单位 为 做 内存_读8 用 数据 加 chunk数据 加 8。
        否则若 chunk类型 等于 CHUNK_tEXt：
            做 PNG_解析tEXt 用 dec、数据 加 chunk数据、chunk长。
        否则若 chunk类型 等于 CHUNK_bKGD：
            做 PNG_解析bKGD 用 dec、数据 加 chunk数据、chunk长。
        否则若 chunk类型 等于 CHUNK_IDAT：
            做 内存_复制 用 IDAT缓冲 加 IDAT大小、数据 加 chunk数据、chunk长。
            IDAT大小 为 IDAT大小 加 chunk长。
        否则若 chunk类型 等于 CHUNK_IEND：
            跳出。
        完。
        偏移 为 chunk数据 加 chunk长 加 4。
    完。
    若 dec 的 状态 不等于 PNG状态_头部已解析：
        做 堆_释放 用 IDAT缓冲。
        dec 的 状态 为 PNG状态_错误。
        dec 的 错误码 为 11。
        PNG统计_错误次数 为 PNG统计_错误次数 加 1。
        返还 dec。
    完。
    dec 的 状态 为 PNG状态_解码中。
    解压大小 为 (dec 的 行字节 加 1) 乘 dec 的 高。
    解压缓冲 为 做 堆_分配_数组 用 解压大小。
    z 为 做 Zlib_创建。
    解压结果 为 做 Zlib_解压 用 z、IDAT缓冲、IDAT大小、解压缓冲、解压大小。
    若 解压结果 小于 0：
        做 堆_释放 用 IDAT缓冲。
        做 堆_释放 用 解压缓冲。
        dec 的 状态 为 PNG状态_错误。
        dec 的 错误码 为 20。
        PNG统计_错误次数 为 PNG统计_错误次数 加 1。
        返还 dec。
    完。
    若 dec 的 隔行方法 等于 1：
        做 PNG_解码_隔行 用 dec、解压缓冲、解压大小。
    否则：
        做 PNG_解码_逐行 用 dec、解压缓冲。
    完。
    若 dec 的 伽马值 大于 0 并且 dec 的 sRGB渲染意图 小于 0：
        做 PNG_应用伽马校正 用 dec。
    完。
    若 dec 的 透明度 不等于 0：
        做 PNG_应用透明度 用 dec。
    完。
    dec 的 状态 为 PNG状态_完成。
    做 堆_释放 用 IDAT缓冲。
    做 堆_释放 用 解压缓冲。
    返还 dec。
完。

PNG_解码_逐行 接受 dec、解压缓冲：
    做 内存_清零 用 dec 的 上一行缓冲、dec 的 行字节。
    可变 行 为 0。
    只要 行 小于 dec 的 高：
        行偏移 为 解压缓冲 加 行 乘 (dec 的 行字节 加 1)。
        滤波类型 为 做 内存_读8 用 行偏移。
        行像素 为 行偏移 加 1。
        若 滤波类型 大于 4：
            dec 的 错误码 为 30。
            返还。
        完。
        做 PNG_逆滤波行 用 dec、滤波类型、行像素、dec 的 上一行缓冲。
        做 PNG_行转RGBA 用 dec、行、行像素。
        做 内存_复制 用 dec 的 上一行缓冲、行像素、dec 的 行字节。
        行 为 行 加 1。
    完。
完。

Adam7_起始列 为 一组：0、4、0、2、0、1、0。

Adam7_起始行 为 一组：0、0、4、0、2、0、1。

Adam7_列增量 为 一组：8、8、4、4、2、2、1。

Adam7_行增量 为 一组：8、8、8、4、4、2、2。

PNG_解码_隔行 接受 dec、解压缓冲、缓冲大小：
    可变 偏移 为 0。
    可变 pass 为 0。
    只要 pass 小于 7：
        起始x 为 Adam7_起始列 的 第 pass 项。
        起始y 为 Adam7_起始行 的 第 pass 项。
        增x 为 Adam7_列增量 的 第 pass 项。
        增y 为 Adam7_行增量 的 第 pass 项。
        pass宽 为 (dec 的 宽 减 起始x 加 增x 减 1) 除以 增x。
        pass高 为 (dec 的 高 减 起始y 加 增y 减 1) 除以 增y。
        若 pass宽 小于或等于 0 或者 pass高 小于或等于 0：
            pass 为 pass 加 1。
            继续。
        完。
        pass行字节 为 pass宽 乘 dec 的 像素字节 乘 dec 的 位深 除以 8。
        pass上一行 为 做 堆_分配_数组 用 pass行字节。
        做 内存_清零 用 pass上一行、pass行字节。
        可变 y 为 0。
        只要 y 小于 pass高：
            若 偏移 大于或等于 缓冲大小：
                跳出。
            完。
            滤波 为 做 内存_读8 用 解压缓冲 加 偏移。
            偏移 为 偏移 加 1。
            行数据 为 解压缓冲 加 偏移。
            做 PNG_逆滤波行_自定义 用 dec、滤波、行数据、pass上一行、pass行字节、dec 的 像素字节。
            目标y 为 起始y 加 y 乘 增y。
            可变 x 为 0。
            只要 x 小于 pass宽：
                目标x 为 起始x 加 x 乘 增x。
                做 PNG_写像素_从行 用 dec、目标x、目标y、行数据、x。
                x 为 x 加 1。
            完。
            做 内存_复制 用 pass上一行、行数据、pass行字节。
            偏移 为 偏移 加 pass行字节。
            y 为 y 加 1。
        完。
        做 堆_释放 用 pass上一行。
        pass 为 pass 加 1。
    完。
完。

PNG_逆滤波行_自定义 接受 dec、滤波类型、行数据、上一行、行字节、bpp：
    可变 i 为 0。
    做 匹配 用 滤波类型。
完。

PNG_Paeth 接受 a、b、c：
    p 为 a 加 b 减 c。
    pa 为 p 减 a。
    若 pa 小于 0：
        pa 为 0 减 pa。
    完。
    pb 为 p 减 b。
    若 pb 小于 0：
        pb 为 0 减 pb。
    完。
    pc 为 p 减 c。
    若 pc 小于 0：
        pc 为 0 减 pc。
    完。
    若 pa 小于或等于 pb 并且 pa 小于或等于 pc：
        返还 a。
    完。
    若 pb 小于或等于 pc：
        返还 b。
    完。
    返还 c。
完。

PNG_写像素_从行 接受 dec、目标x、目标y、行数据、源x：
    若 目标x 大于或等于 dec 的 宽 或者 目标y 大于或等于 dec 的 高：
        返还。
    完。
    dst 为 dec 的 像素缓冲 加 (目标y 乘 dec 的 宽 加 目标x) 乘 4。
    src偏移 为 源x 乘 dec 的 像素字节。
    做 匹配 用 dec 的 颜色类型。
完。

PNG_应用伽马校正 接受 dec：
    若 dec 的 伽马值 等于 0 或者 dec 的 像素缓冲 等于 0：
        返还。
    完。
    伽马 为 100000.0 除以 dec 的 伽马值。
    可变 i 为 0。
    只要 i 小于 dec 的 像素大小：
        若 i 位与 3 不等于 3：
            v 为 做 内存_读8 用 dec 的 像素缓冲 加 i。
            校正 为 做 PNG_伽马表 用 v、伽马。
            做 内存_写8 用 dec 的 像素缓冲 加 i、校正。
        完。
        i 为 i 加 1。
    完。
完。

PNG_伽马表 接受 值、伽马：
    若 值 等于 0：
        返还 0。
    完。
    若 值 等于 255：
        返还 255。
    完。
    归一 为 值 除以 255.0。
    校正 为 做 整数 用 做 幂 用 归一、伽马 乘 255.0 加 0.5。
    若 校正 大于 255：
        返还 255。
    完。
    若 校正 小于 0：
        返还 0。
    完。
    返还 校正。
完。

PNG_应用透明度 接受 dec：
    若 dec 的 透明度 等于 0：
        返还。
    完。
    若 dec 的 颜色类型 等于 3：
        可变 y 为 0。
        只要 y 小于 dec 的 高：
            可变 x 为 0。
            只要 x 小于 dec 的 宽：
                idx 为 (y 乘 dec 的 宽 加 x) 乘 4 加 3。
                像素索引 为 做 内存_读8 用 dec 的 像素缓冲 加 (y 乘 dec 的 宽 加 x) 乘 4。
                若 像素索引 小于 dec 的 透明度大小：
                    做 内存_写8 用 dec 的 像素缓冲 加 idx、做 内存_读8 用 dec 的 透明度 加 像素索引。
                完。
                x 为 x 加 1。
            完。
            y 为 y 加 1。
        完。
    完。
完。

PNG_解析tEXt 接受 dec、数据、长度：
    若 dec 的 元数据 等于 0：
        返还。
    完。
    可变 关键字结束 为 0。
    只要 关键字结束 小于 长度 并且 做 内存_读8 用 数据 加 关键字结束 不等于 0：
        关键字结束 为 关键字结束 加 1。
    完。
    若 关键字结束 大于或等于 长度：
        返还。
    完。
    关键字 为 做 字符串_从内存 用 数据、关键字结束。
    值 为 做 字符串_从内存 用 数据 加 关键字结束 加 1、长度 减 关键字结束 减 1。
    做 字典设 用 dec 的 元数据、关键字、值。
完。

PNG_解析bKGD 接受 dec、数据、长度：
    若 dec 的 颜色类型 等于 3 并且 长度 大于或等于 1：
        dec 的 背景色 为 做 内存_读8 用 数据。
    否则若 (dec 的 颜色类型 等于 0 或者 dec 的 颜色类型 等于 4) 并且 长度 大于或等于 2：
        dec 的 背景色 为 做 读16大端 用 数据。
    否则若 (dec 的 颜色类型 等于 2 或者 dec 的 颜色类型 等于 6) 并且 长度 大于或等于 6：
        dec 的 背景色 为 做 读16大端 用 数据 左移 16 位或 做 读16大端 用 数据 加 2 左移 8 位或 做 读16大端 用 数据 加 4。
    完。
完。

PNG解码器_释放 接受 dec：
    若 dec 等于 0：
        返还。
    完。
    若 dec 的 像素缓冲 不等于 0：
        做 堆_释放 用 dec 的 像素缓冲。
    完。
    若 dec 的 上一行缓冲 不等于 0：
        做 堆_释放 用 dec 的 上一行缓冲。
    完。
    若 dec 的 调色板 不等于 0：
        做 堆_释放 用 dec 的 调色板。
    完。
    若 dec 的 透明度 不等于 0：
        做 堆_释放 用 dec 的 透明度。
    完。
    做 堆_释放 用 dec。
完。

读32大端 接受 ptr：
    返还 做 内存_读8 用 ptr 左移 24 位或 做 内存_读8 用 ptr 加 1 左移 16 位或 做 内存_读8 用 ptr 加 2 左移 8 位或 做 内存_读8 用 ptr 加 3。
完。

读16大端 接受 ptr：
    返还 做 内存_读8 用 ptr 左移 8 位或 做 内存_读8 用 ptr 加 1。
完。

PNG_统计 接受 零参：
    返还 一本字典：解码次数 对应 PNG统计_解码次数、总字节 对应 PNG统计_总字节、错误次数 对应 PNG统计_错误次数。
完。

PNG_打印统计 接受 零参：
    s 为 做 PNG_统计。
    做 打印行 用 「=== PNG解码统计 ===」。
    做 打印行 用 「解码次数: 」 加 做 整数转字符串 用 s 的 解码次数。
    做 打印行 用 「总字节: 」 加 做 整数转字符串 用 s 的 总字节。
    做 打印行 用 「错误次数: 」 加 做 整数转字符串 用 s 的 错误次数。
完。

PNG_重置统计 接受 零参：
    PNG统计_解码次数 为 0。
    PNG统计_总字节 为 0。
    PNG统计_错误次数 为 0。
完。

可变 AI图片_解码缓存 为 0。

AI图片_渐进解码建议 接受 文件大小、网络带宽：
    AI图片_解码缓存 为 AI图片_解码缓存 加 1。
    若 文件大小 大于 1048576 并且 网络带宽 小于 100000：
        返还 「渐进式」。
    完。
    返还 「一次性」。
完。

AI图片_缩放策略 接受 原始宽、原始高、显示宽、显示高：
    若 原始宽 大于 显示宽 乘 4：
        返还 「降采样后解码」。
    完。
    返还 「全分辨率解码」。
完。

本章 收束。
