《阶段装载》

引用《..·中枢·阶段契约》。

引用《.·内核直连层》。

引用《.·事件循环主体》。

引用《.·文档创建》。

引用《.·文档节点》。

引用《.·文档事件》。

引用《.·样式解析》。

引用《.·样式动画》。

引用《.·文档观察》。

引用《.·资源加载资源》。

引用《.·渲染画布装载》。

引用《.·媒体交互装载》。

引用《.·观察调度装载》。

引用《.·消息语义装载》。

可变 感知阶段装载_已装载 为 一组：（空）。

感知阶段装载_清单 接受 阶段：
    若 阶段 等于 阶段_boot：
        返还 一组：「./内核直连层.nova」、「./文档创建.nova」。
    完。
    若 阶段 等于 阶段_内核核心：
        返还 一组：「./事件循环主体.nova」、「./文档节点.nova」、「./样式解析.nova」、「./渲染画布装载.nova」、「./观察调度装载.nova」。
    完。
    若 阶段 等于 阶段_浏览器外衣：
        返还 一组：「./文档事件.nova」、「./样式动画.nova」、「./文档观察.nova」、「./媒体交互装载.nova」、「./观察调度装载.nova」。
    完。
    若 阶段 等于 阶段_AI运行时：
        返还 一组：「./资源加载资源.nova」、「./文档节点.nova」、「./文档事件.nova」、「./消息语义装载.nova」、「./渲染画布装载.nova」。
    完。
    若 阶段 等于 阶段_外部桥接：
        返还 一组：「./内核直连层.nova」、「./文档节点.nova」、「./文档事件.nova」、「./消息语义装载.nova」、「./观察调度装载.nova」。
    完。
    返还 一组：（空）。
完。

感知阶段装载_登记 接受 阶段：
    可变 i 为 0。
    只要 i 小于 做 计数 用 感知阶段装载_已装载：
        若 感知阶段装载_已装载 的 第 i 项 等于 阶段：
            返还 做 计数 用 做 感知阶段装载_清单 用 阶段。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 感知阶段装载_已装载、阶段。
    返还 做 计数 用 做 感知阶段装载_清单 用 阶段。
完。

本章 收束。
