《辅助数学》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

Q16_ONE 为 65536。

PI_Q16 为 205887。

TWO_PI_Q16 为 411775。

SkColor_lerp 接受 c0、c1、t256：
    inv 为 256 减 t256。
    a 为 ((c0 右移 24 位与 0xFF) 乘 inv 加 (c1 右移 24 位与 0xFF) 乘 t256) 除以 256。
    r 为 ((c0 右移 16 位与 0xFF) 乘 inv 加 (c1 右移 16 位与 0xFF) 乘 t256) 除以 256。
    g 为 ((c0 右移 8 位与 0xFF) 乘 inv 加 (c1 右移 8 位与 0xFF) 乘 t256) 除以 256。
    b 为 ((c0 位与 0xFF) 乘 inv 加 (c1 位与 0xFF) 乘 t256) 除以 256。
    返还 a 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
完。

SkColor_从RGBA 接受 r、g、b、a：
    返还 (a 位与 0xFF) 左移 24 位或 (r 位与 0xFF) 左移 16 位或 (g 位与 0xFF) 左移 8 位或 b 位与 0xFF。
完。

SkColor_提取R 接受 颜色：
    返还 颜色 右移 16 位与 0xFF。
完。

SkColor_提取G 接受 颜色：
    返还 颜色 右移 8 位与 0xFF。
完。

SkColor_提取B 接受 颜色：
    返还 颜色 位与 0xFF。
完。

SkColor_提取A 接受 颜色：
    返还 颜色 右移 24 位与 0xFF。
完。

SkColor_预乘Alpha 接受 颜色：
    a 为 做 SkColor_提取A 用 颜色。
    若 a 等于 255：
        返还 颜色。
    完。
    若 a 等于 0：
        返还 0。
    完。
    r 为 做 SkColor_提取R 用 颜色 乘 a 除以 255。
    g 为 做 SkColor_提取G 用 颜色 乘 a 除以 255。
    b 为 做 SkColor_提取B 用 颜色 乘 a 除以 255。
    返还 做 SkColor_从RGBA 用 r、g、b、a。
完。

Sk数学_sqrt 接受 v：
    若 v 小于或等于 0：
        返还 0。
    完。
    可变 x 为 v。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 v 除以 x) 除以 2。
    完。
    返还 x。
完。

Sk数学_abs 接受 v：
    若 v 小于 0：
        返还 负 v。
    完。
    返还 v。
完。

Sk数学_min 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

Sk数学_max 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

Sk数学_clamp 接受 值、最小、最大：
    若 值 小于 最小：
        返还 最小。
    完。
    若 值 大于 最大：
        返还 最大。
    完。
    返还 值。
完。

Sk数学_atan2 接受 y、x：
    若 x 等于 0 并且 y 等于 0：
        返还 0。
    完。
    若 x 大于 0 并且 y 大于或等于 0：
        返还 y 乘 256 除以 (x 加 y)。
    完。
    若 x 小于或等于 0 并且 y 大于 0：
        返还 256 加 负 x 乘 256 除以 (负 x 加 y)。
    完。
    若 x 小于 0 并且 y 小于或等于 0：
        返还 512 加 负 y 乘 256 除以 (负 x 减 y)。
    完。
    返还 768 加 x 乘 256 除以 (x 减 y)。
完。

Sk数学_sin_Q16 接受 角度_Q16：
    可变 a 为 角度_Q16 取余于 TWO_PI_Q16。
    若 a 小于 0：
        a 为 a 加 TWO_PI_Q16。
    完。
    x 为 a 乘 4 除以 TWO_PI_Q16。
    若 x 等于 0：
        返还 0。
    完。
    若 x 等于 1：
        返还 Q16_ONE。
    完。
    若 x 等于 2：
        返还 0。
    完。
    若 x 等于 3：
        返还 负 Q16_ONE。
    完。
    局部 为 a 取余于 (PI_Q16 除以 2)。
    比例 为 局部 乘 Q16_ONE 除以 (PI_Q16 除以 2)。
    若 x 小于 1：
        返还 比例。
    完。
    若 x 小于 2：
        返还 Q16_ONE 减 (比例 减 Q16_ONE)。
    完。
    若 x 小于 3：
        返还 负 (比例 减 Q16_ONE 乘 2)。
    完。
    返还 负 (Q16_ONE 减 (比例 减 Q16_ONE 乘 3))。
完。

Sk数学_cos_Q16 接受 角度_Q16：
    返还 做 Sk数学_sin_Q16 用 角度_Q16 加 PI_Q16 除以 2。
完。

Sk数学_距离 接受 x1、y1、x2、y2：
    dx 为 x2 减 x1。
    dy 为 y2 减 y1。
    返还 做 Sk数学_sqrt 用 dx 乘 dx 加 dy 乘 dy。
完。

矩阵2D 是 一种 事物，含 m00、m01、m02、m10、m11、m12。

矩阵_单位 接受 零参：
    m 为 做 堆_分配 用 矩阵2D。
    m 的 m00 为 Q16_ONE。
    m 的 m01 为 0。
    m 的 m02 为 0。
    m 的 m10 为 0。
    m 的 m11 为 Q16_ONE。
    m 的 m12 为 0。
    返还 m。
完。

矩阵_平移 接受 tx、ty：
    m 为 做 矩阵_单位。
    m 的 m02 为 tx。
    m 的 m12 为 ty。
    返还 m。
完。

矩阵_缩放 接受 sx、sy：
    m 为 做 矩阵_单位。
    m 的 m00 为 sx。
    m 的 m11 为 sy。
    返还 m。
完。

矩阵_变换点 接受 m、x、y、结果：
    若 m 等于 0 或者 结果 等于 0：
        返还。
    完。
    做 内存_写32 用 结果、(m 的 m00 乘 x 加 m 的 m01 乘 y) 除以 Q16_ONE 加 m 的 m02。
    做 内存_写32 用 结果 加 4、(m 的 m10 乘 x 加 m 的 m11 乘 y) 除以 Q16_ONE 加 m 的 m12。
完。

贝塞尔_二次求值 接受 p0、p1、p2、t256：
    inv 为 256 减 t256。
    a 为 inv 乘 inv 除以 256。
    b 为 2 乘 inv 乘 t256 除以 256。
    c 为 t256 乘 t256 除以 256。
    返还 (a 乘 p0 加 b 乘 p1 加 c 乘 p2) 除以 256。
完。

贝塞尔_三次求值 接受 p0、p1、p2、p3、t256：
    inv 为 256 减 t256。
    inv2 为 inv 乘 inv 除以 256。
    t2 为 t256 乘 t256 除以 256。
    a 为 inv2 乘 inv 除以 256。
    b 为 3 乘 inv2 乘 t256 除以 256。
    c 为 3 乘 inv 乘 t2 除以 256。
    d 为 t2 乘 t256 除以 256。
    返还 (a 乘 p0 加 b 乘 p1 加 c 乘 p2 加 d 乘 p3) 除以 256。
完。

矩形_裁剪 接受 x、y、宽、高、裁剪x、裁剪y、裁剪宽、裁剪高、结果：
    左 为 做 Sk数学_max 用 x、裁剪x。
    上 为 做 Sk数学_max 用 y、裁剪y。
    右 为 做 Sk数学_min 用 x 加 宽、裁剪x 加 裁剪宽。
    下 为 做 Sk数学_min 用 y 加 高、裁剪y 加 裁剪高。
    若 左 大于或等于 右 或者 上 大于或等于 下：
        返还 假。
    完。
    若 结果 不等于 0：
        做 内存_写32 用 结果、左。
        做 内存_写32 用 结果 加 4、上。
        做 内存_写32 用 结果 加 8、右 减 左。
        做 内存_写32 用 结果 加 12、下 减 上。
    完。
    返还 真。
完。

HSV转RGB 接受 色相、饱和度、明度：
    若 饱和度 等于 0：
        返还 做 SkColor_从RGBA 用 明度、明度、明度、255。
    完。
    区域 为 色相 除以 43。
    余数 为 (色相 减 区域 乘 43) 乘 6。
    p 为 明度 乘 (255 减 饱和度) 除以 255。
    q 为 明度 乘 (255 减 饱和度 乘 余数 除以 255) 除以 255。
    t 为 明度 乘 (255 减 饱和度 乘 (255 减 余数) 除以 255) 除以 255。
    若 区域 等于 0：
        返还 做 SkColor_从RGBA 用 明度、t、p、255。
    完。
    若 区域 等于 1：
        返还 做 SkColor_从RGBA 用 q、明度、p、255。
    完。
    若 区域 等于 2：
        返还 做 SkColor_从RGBA 用 p、明度、t、255。
    完。
    若 区域 等于 3：
        返还 做 SkColor_从RGBA 用 p、q、明度、255。
    完。
    若 区域 等于 4：
        返还 做 SkColor_从RGBA 用 t、p、明度、255。
    完。
    返还 做 SkColor_从RGBA 用 明度、p、q、255。
完。

可变 AI_辅助数学_调用数 为 0。

可变 AI_辅助数学_异常数 为 0。

AI_辅助数学_监控 接受 零参：
    AI_辅助数学_调用数 为 AI_辅助数学_调用数 加 1。
    返还 AI_辅助数学_异常数 等于 0。
完。

AI_辅助数学_健康检查 接受 零参：
    若 AI_辅助数学_调用数 大于 0 并且 AI_辅助数学_异常数 乘 100 除以 AI_辅助数学_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
