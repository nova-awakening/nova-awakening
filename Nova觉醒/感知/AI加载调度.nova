《AI加载调度》

引用《..·中枢·类型定义》。
引用《..·推理·通信渠道》。
引用《..·中枢·时钟中枢》。
引用《..·推理·AI总线资源调度》。
引用《..·中枢·整数转字符串》。
引用《..·标准·原生字符串》。

优先级_最高 为 0。
优先级_高 为 1。
优先级_普通 为 2。
优先级_低 为 3。
优先级_最低 为 4。

AI资源_关键样式 为 1。
AI资源_关键字体 为 2。
AI资源_关键脚本 为 3。
AI资源_图片 为 4。
AI资源_媒体 为 5。
AI资源_预取 为 6。
AI资源_广告 为 7。
AI资源_跟踪 为 8。
AI资源_未知 为 0。

重试_最大次数 为 3。
重试_基础延迟 为 1000。
重试_最大延迟 为 30000。

AI加载统计 是 一种 事物：
    总请求数,。
    被延迟数,。
    被拦截数,。
    重试次数,。
    预取命中数,。
    带宽估算,。
    最近调整时间,。
    优先级分布,。
完。

预加载提示 是 一种 事物：
    url,。
    类型,。
    优先级,。
    交叉源,。
    创建时间,。
完。

可变 AI加载全局统计 为 0。
可变 预加载队列 为 []。
可变 带宽_采样列表 为 []。
可变 带宽_估算值 为 0。

AI加载_初始化 接受 零参：
    AI加载全局统计 为 做 堆_分配 用 AI加载统计。
    AI加载全局统计.总请求数 为 0。
    AI加载全局统计.被延迟数 为 0。
    AI加载全局统计.被拦截数 为 0。
    AI加载全局统计.重试次数 为 0。
    AI加载全局统计.预取命中数 为 0。
    AI加载全局统计.带宽估算 为 0。
    AI加载全局统计.最近调整时间 为 0。
    AI加载全局统计.优先级分布 为 [0, 0, 0, 0, 0]。
    预加载队列 为 []。
    带宽_采样列表 为 []。
    带宽_估算值 为 0。
完。

AI加载_调整优先级 接受 请求、url：
    若 请求 等于 0：
    若 AI加载全局统计 不等于 0：
        AI加载全局统计.总请求数 为 AI加载全局统计.总请求数 加 1。
    完。

    bus 为 全局AI总线。
    若 bus 不等于 0 并且 url 不等于 0：
        分数 为 做 AI总线_资源优先级 用 bus、url。
        若 分数 大于 800：
            请求.优先级 为 优先级_最高。
            做 AI加载_记录优先级 用 优先级_最高。
        否则若 分数 大于 500：
            请求.优先级 为 优先级_高。
            做 AI加载_记录优先级 用 优先级_高。
        否则若 分数 大于 300：
            请求.优先级 为 优先级_普通。
            做 AI加载_记录优先级 用 优先级_普通。
        否则若 分数 大于 150：
            请求.优先级 为 优先级_低。
            做 AI加载_记录优先级 用 优先级_低。
            若 AI加载全局统计 不等于 0：
        否则：
            请求.优先级 为 优先级_最低。
            做 AI加载_记录优先级 用 优先级_最低。
            若 AI加载全局统计 不等于 0：
        完。

        若 带宽_估算值 大于 0 并且 带宽_估算值 小于 100000：
            若 请求.优先级 大于 优先级_普通：
                请求.优先级 为 优先级_最低。
            完。
        完。
        返还 。
    完。

    语义类型 为 做 AI加载_识别语义类型 用 url、请求.资源类型。
    选择 (语义类型) ：
        AI资源_关键样式: { 请求.优先级 = 优先级_最高 }。
        AI资源_关键字体: { 请求.优先级 = 优先级_最高 }。
        AI资源_关键脚本: { 请求.优先级 = 优先级_高 }。
        AI资源_图片: { 请求.优先级 = 优先级_低 }。
        AI资源_媒体: { 请求.优先级 = 优先级_低 }。
        AI资源_预取: { 请求.优先级 = 优先级_最低 }。
        AI资源_广告: { 请求.优先级 = 优先级_最低 }。
        AI资源_跟踪: { 请求.优先级 = 优先级_最低 }。
        默认: { 请求.优先级 = 优先级_普通 }。
    完。
    做 AI加载_记录优先级 用 请求.优先级。
完。

AI加载_记录优先级 接受 优先级：
    若 AI加载全局统计 等于 0：
    若 优先级 不小于 0 并且 优先级 不大于 4：
        AI加载全局统计.优先级分布[优先级] 为 AI加载全局统计.优先级分布[优先级] 加 1。
    完。
    AI加载全局统计.最近调整时间 为 做 读取毫秒。
完。

AI加载_识别语义类型 接受 url、资源类型：
    若 url 等于 0：

    若 做 字符串包含 用 url、「.css」：
    若 做 字符串包含 用 url、「.woff」 或者 字符串包含(url, 「.woff2」：
        做 字符串包含 用 url、「.ttf」 或者 做 字符串包含 用 url、「.otf」) { 返还 AI资源_关键字体 }。
    若 做 字符串包含 用 url、「.js」 或者 做 字符串包含 用 url、「.mjs」：
    若 做 字符串包含 用 url、「.png」 或者 字符串包含(url, 「.jpg」：
        做 字符串包含 用 url、「.jpeg」 或者 做 字符串包含 用 url、「.webp」 ||。
        做 字符串包含 用 url、「.gif」 或者 做 字符串包含 用 url、「.svg」 ||。
        做 字符串包含 用 url、「.ico」) { 返还 AI资源_图片 }。
    若 做 字符串包含 用 url、「.mp4」 或者 字符串包含(url, 「.webm」：
        做 字符串包含 用 url、「.mp3」 或者 做 字符串包含 用 url、「.ogg」) { 返还 AI资源_媒体 }。

    若 做 字符串包含 用 url、「analytics」 或者 字符串包含(url, 「tracker」：
        做 字符串包含 用 url、「beacon」 或者 做 字符串包含 用 url、「pixel」) { 返还 AI资源_跟踪 }。
    若 做 字符串包含 用 url、「ad.」 或者 字符串包含(url, 「/ads/」：
        做 字符串包含 用 url、「adserver」 或者 做 字符串包含 用 url、「doubleclick」) { 返还 AI资源_广告 }。

    返还 AI资源_未知。
完。

AI加载_智能重试 接受 请求、状态码：
    若 请求 等于 0：

    bus 为 全局AI总线。
    若 bus 不等于 0 并且 请求.URL 不等于 0：
        分数 为 做 AI总线_资源优先级 用 bus、请求.URL。
        若 分数 小于 200：
    完。

    若 请求.重试次数 不小于 重试_最大次数：

    若 状态码 等于 429：
        请求.重试延迟 为 做 AI加载_计算退避 用 请求.重试次数。
        请求.重试次数 为 请求.重试次数 加 1。
        若 AI加载全局统计 不等于 0：
        返还 真。
    完。
    若 状态码 不小于 500 并且 状态码 不大于 503：
        请求.重试延迟 为 做 AI加载_计算退避 用 请求.重试次数。
        请求.重试次数 为 请求.重试次数 加 1。
        若 AI加载全局统计 不等于 0：
        返还 真。
    完。
    若 状态码 等于 408：
        请求.重试延迟 为 做 AI加载_计算退避 用 请求.重试次数。
        请求.重试次数 为 请求.重试次数 加 1。
        返还 真。
    完。
    若 状态码 等于 404 或者 状态码 等于 403 或者 状态码 等于 401：
    返还 假。
完。

AI加载_计算退避 接受 重试次数：
    延迟 为 重试_基础延迟。
    可变 i 为 0。
    只要 i 小于 重试次数：
        延迟 为 延迟 乘 2。
        i 为 i 加 1。
    完。
    抖动 为 做 读取毫秒 取余于 (延迟 除以 4 加 1)。
    延迟 为 延迟 加 抖动。
    若 延迟 大于 重试_最大延迟：
    返还 延迟。
完。

AI加载_添加预加载 接受 url、类型、优先级：
    若 url 等于 0：
    可变 i 为 0。
    只要 i 小于 做 计数 用 预加载队列：
        若 做 字符串_相等 用 预加载队列[i].url、url：
        i 为 i 加 1。
    完。
    提示 为 预加载提示 {}。
    提示.url 为 url。
    提示.类型 为 类型。
    提示.优先级 为 优先级。
    提示.交叉源 为 做 AI加载_是跨源 用 url。
    提示.创建时间 为 做 读取毫秒。
    做 追加 用 预加载队列、提示。
完。

AI加载_是跨源 接受 url：
    若 url 等于 0：
    若 做 字符串包含 用 url、「://」：
        若 !做 字符串包含 用 url、做 当前文档_源：
    完。
    返还 假。
完。

AI加载_处理预加载队列 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 预加载队列：
        提示 为 预加载队列[i]。
        现在 为 做 读取毫秒。
        若 现在 减 提示.创建时间 大于 60000：
            做 删除 用 预加载队列、i。
            继续。
        完。
        i 为 i 加 1。
    完。
完。

AI加载_更新带宽 接受 字节数、耗时毫秒：
    若 耗时毫秒 不大于 0：
    速率 为 字节数 乘 1000 除以 耗时毫秒。
    做 追加 用 带宽_采样列表、速率。
    若 做 计数 用 带宽_采样列表 大于 20：
        带宽_采样列表 为 做 切片 用 带宽_采样列表、做 计数 用 带宽_采样列表 减 20。
    完。

    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 带宽_采样列表：
        总和 为 总和 加 带宽_采样列表[i]。
        i 为 i 加 1。
    完。
    带宽_估算值 为 总和 除以 做 计数 用 带宽_采样列表。
    若 AI加载全局统计 不等于 0：
完。

AI加载_连接优先级 接受 url：
    语义 为 做 AI加载_识别语义类型 用 url、0。
    若 语义 等于 AI资源_关键样式 或者 语义 等于 AI资源_关键脚本：
    若 语义 等于 AI资源_关键字体：
    若 语义 等于 AI资源_图片：
    若 语义 等于 AI资源_广告 或者 语义 等于 AI资源_跟踪：
    返还 优先级_普通。
完。

AI加载_统计 接受 零参：
    若 AI加载全局统计 等于 0：
    返还 ：
        总请求: AI加载全局统计.总请求数,。
        被延迟: AI加载全局统计.被延迟数,。
        被拦截: AI加载全局统计.被拦截数,。
        重试: AI加载全局统计.重试次数,。
        预取命中: AI加载全局统计.预取命中数,。
        带宽。
        预加载队列。
        优先级分布: AI加载全局统计.优先级分布。
    完。
完。

※ 智能加载健康检查。

智能加载_健康检查 接受 零参：
    可变 问题 为 []。
    若 AI加载全局统计.被拦截数 大于 AI加载全局统计.总请求数 除以 3：
        做 追加 用 问题、「加载拦截率过高」。
    完。
    若 AI加载全局统计.重试次数 大于 AI加载全局统计.总请求数 除以 2：
        做 追加 用 问题、「重试率过高」。
    完。
    若 做 计数 用 预加载队列 大于 500：
        做 追加 用 问题、「预加载队列过大: 」 加 做 整数转字符串 用 做 计数 用 预加载队列。
    完。
    若 做 计数 用 问题 等于 0：
    返还 问题。
完。

智能加载_清空预加载 接受 零参：
    预加载队列 为 []。
    AI加载全局统计.预取命中数 为 0。
完。

智能加载_带宽充足 接受 零参：
    返还 带宽_估算值 大于 1024 乘 1024。
完。

智能加载_重置 接受 零参：
    AI加载全局统计.总请求数 为 0。
    AI加载全局统计.被延迟数 为 0。
    AI加载全局统计.被拦截数 为 0。
    AI加载全局统计.重试次数 为 0。
完。

本章 收束。

本章 收束。

本章 收束。
