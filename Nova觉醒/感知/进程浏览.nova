《进程浏览》

引用《.·文档节点》。

引用《.·文档事件》。

引用《.·文档观察》。

引用《.·全景照》。

浏览档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、进程数、页数、标签数、快照数、树数、找点数、点选数、输文数、转页数、订阅数、成功数、失败数、焦点进程、焦点页面、进程表、页面表、订阅表。

浏览请求 是 一种 事物，含 动作、目标、参数、选项。

浏览回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

浏览摘要 是 一种 事物，含 名称、进程数、页数、标签数、快照数、树数、找点数、点选数、输文数、转页数、订阅数、成功数、失败数、健康。

浏览进程项 是 一种 事物，含 标识、名称、状态、会话、标签数、页面数。

浏览页项 是 一种 事物，含 标识、进程、标题、地址、类型、状态、文本、焦点、节点表。

浏览动作项 是 一种 事物，含 名称、目标、参数、来源。

可变 浏览账本 为 一组：（空）。

可变 浏览构建数 为 0。

可变 浏览总动作 为 0。

可变 浏览总成功 为 0。

可变 浏览总失败 为 0。

进程浏览_建档 接受 名称：
    档 为 新建 浏览档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 进程数 为 0。
    档 的 页数 为 0。
    档 的 标签数 为 0。
    档 的 快照数 为 0。
    档 的 树数 为 0。
    档 的 找点数 为 0。
    档 的 点选数 为 0。
    档 的 输文数 为 0。
    档 的 转页数 为 0。
    档 的 订阅数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    档 的 焦点进程 为 「」。
    档 的 焦点页面 为 「」。
    档 的 进程表 为 一组：（空）。
    档 的 页面表 为 一组：（空）。
    档 的 订阅表 为 一组：（空）。
    返还 档。
完。

进程浏览_登记 接受 档：
    做 追加 用 浏览账本、档。
    浏览构建数 为 浏览构建数 加 1。
    返还 档。
完。

进程浏览_完整统计 接受 零参：
    返还 一组：浏览构建数、浏览总动作、浏览总成功、浏览总失败。
完。

进程浏览_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 浏览回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

进程浏览_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    浏览总动作 为 浏览总动作 加 1。
完。

进程浏览_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 进程数 等于 0 或者 档 的 页数 等于 0：
        档 的 最近健康 为 「预热」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 0：
        档 的 最近健康 为 「监视」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「运行」。
    返还 档 的 最近健康。
完。

进程浏览_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 档 的 名称 等于 「」：
        返还 假。
    完。
    返还 真。
完。

进程浏览_成功 接受 档、来源、数据：
    档 的 最近结果 为 数据。
    档 的 成功数 为 档 的 成功数 加 1。
    浏览总成功 为 浏览总成功 加 1。
    档 的 最近统计 为 做 进程浏览_完整统计。
    做 进程浏览_刷新健康 用 档。
    返还 做 进程浏览_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

进程浏览_失败 接受 档、来源、错因：
    若 档 不等于 空：
        档 的 最近错误 为 错因。
        档 的 失败数 为 档 的 失败数 加 1。
        档 的 最近统计 为 做 进程浏览_完整统计。
        做 进程浏览_刷新健康 用 档。
    完。
    浏览总失败 为 浏览总失败 加 1。
    返还 做 进程浏览_建回证 用 真、假、来源、错因、空、做 进程浏览_完整统计。
完。

进程浏览_建进程项 接受 标识、名称、会话：
    项 为 新建 浏览进程项。
    项 的 标识 为 标识。
    项 的 名称 为 名称。
    项 的 状态 为 「运行」。
    项 的 会话 为 会话。
    项 的 标签数 为 0。
    项 的 页面数 为 0。
    返还 项。
完。

进程浏览_建页项 接受 标识、进程、标题、地址、类型：
    项 为 新建 浏览页项。
    项 的 标识 为 标识。
    项 的 进程 为 进程。
    项 的 标题 为 标题。
    项 的 地址 为 地址。
    项 的 类型 为 类型。
    项 的 状态 为 「可见」。
    项 的 文本 为 「」。
    项 的 焦点 为 假。
    项 的 节点表 为 一组：（空）。
    返还 项。
完。

进程浏览_找进程 接受 档、标识：
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 进程表：
        项 为 档 的 进程表 的 第 索引 项。
        若 项 的 标识 等于 标识：
            返还 项。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

进程浏览_找页面 接受 档、标识：
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 页面表：
        项 为 档 的 页面表 的 第 索引 项。
        若 项 的 标识 等于 标识：
            返还 项。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

进程浏览_焦点页 接受 档：
    返还 做 进程浏览_找页面 用 档、档 的 焦点页面。
完。

进程浏览_建界节点 接受 页、标识、角色、名称、值、左、上、宽、高、可动、可输：
    节 为 新建 界节点。
    节 的 标识 为 标识。
    节 的 角色 为 角色。
    节 的 名称 为 名称。
    节 的 值 为 值。
    节 的 左 为 左。
    节 的 上 为 上。
    节 的 宽 为 宽。
    节 的 高 为 高。
    节 的 可见 为 真。
    节 的 可动 为 可动。
    节 的 可输 为 可输。
    节 的 可禁 为 假。
    节 的 路径 为 页 的 地址 加 「#」 加 标识。
    节 的 选择 为 「[data-node='」 加 标识 加 「']」。
    返还 节。
完。

进程浏览_刷新页节点 接受 页：
    页 的 节点表 为 一组：（空）。
    做 追加 用 页 的 节点表、做 进程浏览_建界节点 用 页、页 的 标识 加 「-地址」、「输入」、「地址栏」、页 的 地址、20、16、760、44、真、真。
    做 追加 用 页 的 节点表、做 进程浏览_建界节点 用 页、页 的 标识 加 「-标题」、「文本」、页 的 标题、页 的 标题、28、78、480、28、假、假。
    做 追加 用 页 的 节点表、做 进程浏览_建界节点 用 页、页 的 标识 加 「-正文」、「文档」、「正文」、页 的 文本、28、120、980、660、真、真。
    做 追加 用 页 的 节点表、做 进程浏览_建界节点 用 页、页 的 标识 加 「-按钮」、「按钮」、「提交」、「提交」、820、16、120、44、真、假。
    返还 页 的 节点表。
完。

进程浏览_订阅 接受 档、名称、目标、来源：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「订阅」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「订阅」、一组：名称、目标、来源、「进程浏览」。
    项 为 新建 浏览动作项。
    项 的 名称 为 名称。
    项 的 目标 为 目标。
    项 的 参数 为 一组：（空）。
    项 的 来源 为 来源。
    做 追加 用 档 的 订阅表、项。
    档 的 订阅数 为 档 的 订阅数 加 1。
    返还 做 进程浏览_成功 用 档、「订阅」、项。
完。

进程浏览_建进程 接受 档、标识、名称、会话：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「建进程」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「建进程」、一组：标识、名称、会话、「进程浏览」。
    若 做 进程浏览_找进程 用 档、标识 不等于 空：
        返还 做 进程浏览_失败 用 档、「建进程」、「进程已存在」。
    完。
    项 为 做 进程浏览_建进程项 用 标识、名称、会话。
    做 追加 用 档 的 进程表、项。
    档 的 进程数 为 档 的 进程数 加 1。
    档 的 焦点进程 为 标识。
    返还 做 进程浏览_成功 用 档、「建进程」、项。
完。

进程浏览_开页 接受 档、页号、进程标识、标题、地址、类型：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「开页」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「开页」、一组：页号、进程标识、标题、地址、类型、「进程浏览」。
    进程 为 做 进程浏览_找进程 用 档、进程标识。
    若 进程 等于 空：
        返还 做 进程浏览_失败 用 档、「开页」、「浏览进程不存在」。
    完。
    页 为 做 进程浏览_建页项 用 页号、进程标识、标题、地址、类型。
    页 的 焦点 为 真。
    做 进程浏览_刷新页节点 用 页。
    做 追加 用 档 的 页面表、页。
    档 的 页数 为 档 的 页数 加 1。
    档 的 标签数 为 档 的 标签数 加 1。
    档 的 焦点页面 为 页号。
    进程 的 标签数 为 进程 的 标签数 加 1。
    进程 的 页面数 为 进程 的 页面数 加 1。
    返还 做 进程浏览_成功 用 档、「开页」、页。
完。

进程浏览_关页 接受 档、页号：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「关页」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「关页」、一组：页号、「进程浏览」。
    新表 为 一组：（空）。
    索引 为 0。
    命中 为 假。
    所属进程 为 「」。
    只要 索引 小于 做 计数 用 档 的 页面表：
        页 为 档 的 页面表 的 第 索引 项。
        若 页 的 标识 等于 页号：
            命中 为 真。
            所属进程 为 页 的 进程。
        否则：
            做 追加 用 新表、页。
        完。
        索引 为 索引 加 1。
    完。
    若 非 命中：
        返还 做 进程浏览_失败 用 档、「关页」、「页面不存在」。
    完。
    档 的 页面表 为 新表。
    档 的 页数 为 做 计数 用 新表。
    若 档 的 标签数 大于 0：
        档 的 标签数 为 档 的 标签数 减 1。
    完。
    进程 为 做 进程浏览_找进程 用 档、所属进程。
    若 进程 不等于 空 并且 进程 的 标签数 大于 0：
        进程 的 标签数 为 进程 的 标签数 减 1。
        进程 的 页面数 为 进程 的 标签数。
    完。
    若 档 的 焦点页面 等于 页号：
        若 档 的 页数 大于 0：
            档 的 焦点页面 为 档 的 页面表 的 第 0 项 的 标识。
            档 的 页面表 的 第 0 项 的 焦点 为 真。
        否则：
            档 的 焦点页面 为 「」。
        完。
    完。
    返还 做 进程浏览_成功 用 档、「关页」、页号。
完。

进程浏览_切页 接受 档、页号：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「切页」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「切页」、一组：页号、「进程浏览」。
    目标页 为 做 进程浏览_找页面 用 档、页号。
    若 目标页 等于 空：
        返还 做 进程浏览_失败 用 档、「切页」、「页面不存在」。
    完。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 页面表：
        档 的 页面表 的 第 索引 项 的 焦点 为 假。
        索引 为 索引 加 1。
    完。
    目标页 的 焦点 为 真。
    档 的 焦点页面 为 页号。
    档 的 焦点进程 为 目标页 的 进程。
    返还 做 进程浏览_成功 用 档、「切页」、页号。
完。

进程浏览_写文 接受 档、页号、文本：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「写文」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「写文」、一组：页号、文本、「进程浏览」。
    页 为 做 进程浏览_找页面 用 档、页号。
    若 页 等于 空：
        返还 做 进程浏览_失败 用 档、「写文」、「页面不存在」。
    完。
    页 的 文本 为 文本。
    页 的 状态 为 「更新」。
    档 的 输文数 为 档 的 输文数 加 1。
    做 进程浏览_刷新页节点 用 页。
    返还 做 进程浏览_成功 用 档、「写文」、页。
完。

进程浏览_转页 接受 档、页号、新地址、新标题：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「转页」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「转页」、一组：页号、新地址、新标题、「进程浏览」。
    页 为 做 进程浏览_找页面 用 档、页号。
    若 页 等于 空：
        返还 做 进程浏览_失败 用 档、「转页」、「页面不存在」。
    完。
    页 的 地址 为 新地址。
    页 的 标题 为 新标题。
    页 的 状态 为 「导航」。
    档 的 转页数 为 档 的 转页数 加 1。
    做 进程浏览_刷新页节点 用 页。
    返还 做 进程浏览_成功 用 档、「转页」、页。
完。

进程浏览_取树 接受 档、页号：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「取树」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「取树」、一组：页号、「进程浏览」。
    页 为 做 进程浏览_找页面 用 档、页号。
    若 页 等于 空：
        返还 做 进程浏览_失败 用 档、「取树」、「页面不存在」。
    完。
    档 的 树数 为 档 的 树数 加 1。
    做 进程浏览_刷新页节点 用 页。
    返还 做 进程浏览_成功 用 档、「取树」、页 的 节点表。
完。

进程浏览_找点 接受 档、页号、关键词：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「找点」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「找点」、一组：页号、关键词、「进程浏览」。
    页 为 做 进程浏览_找页面 用 档、页号。
    若 页 等于 空：
        返还 做 进程浏览_失败 用 档、「找点」、「页面不存在」。
    完。
    结果 为 一组：（空）。
    索引 为 0。
    只要 索引 小于 做 计数 用 页 的 节点表：
        节 为 页 的 节点表 的 第 索引 项。
        若 做 包含 用 节 的 名称、关键词 或者 做 包含 用 节 的 值、关键词 或者 做 包含 用 节 的 角色、关键词：
            做 追加 用 结果、节。
        完。
        索引 为 索引 加 1。
    完。
    档 的 找点数 为 档 的 找点数 加 1。
    返还 做 进程浏览_成功 用 档、「找点」、结果。
完。

进程浏览_点选 接受 档、页号、节点标识：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「点选」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「点选」、一组：页号、节点标识、「进程浏览」。
    页 为 做 进程浏览_找页面 用 档、页号。
    若 页 等于 空：
        返还 做 进程浏览_失败 用 档、「点选」、「页面不存在」。
    完。
    索引 为 0。
    只要 索引 小于 做 计数 用 页 的 节点表：
        节 为 页 的 节点表 的 第 索引 项。
        若 节 的 标识 等于 节点标识：
            页 的 状态 为 「交互」。
            档 的 点选数 为 档 的 点选数 加 1。
            返还 做 进程浏览_成功 用 档、「点选」、一组：节点标识、节 的 路径。
        完。
        索引 为 索引 加 1。
    完。
    返还 做 进程浏览_失败 用 档、「点选」、「节点不存在」。
完。

进程浏览_取照 接受 档：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「取照」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「取照」、一组：（空）、「进程浏览」。
    页 为 做 进程浏览_焦点页 用 档。
    照 为 新建 全景照。
    照 的 编号 为 档 的 快照数 加 1。
    照 的 窗数 为 档 的 进程数。
    照 的 焦窗 为 档 的 焦点进程。
    照 的 页号 为 档 的 焦点页面。
    照 的 页题 为 「」。
    照 的 页址 为 「」。
    照 的 页类 为 「」。
    照 的 页名 为 档 的 名称。
    照 的 页源 为 「进程浏览」。
    照 的 页摘 为 「」。
    照 的 节点数 为 0。
    照 的 脚本数 为 0。
    照 的 样式数 为 0。
    照 的 壳缓 为 「稳定」。
    照 的 壳动 为 档 的 最近动作。
    照 的 壳果 为 档 的 最近结果。
    照 的 核态 为 做 进程浏览_刷新健康 用 档。
    照 的 核副 为 档 的 最近错误。
    照 的 视觉态 为 「结构化」。
    照 的 视觉副 为 「白璃」。
    照 的 声音态 为 「静默」。
    照 的 声音副 为 「」。
    照 的 形象态 为 「浏览系统」。
    照 的 形象副 为 「内核即外衣」。
    照 的 节点列 为 一组：（空）。
    若 页 不等于 空：
        照 的 页题 为 页 的 标题。
        照 的 页址 为 页 的 地址。
        照 的 页类 为 页 的 类型。
        照 的 页摘 为 页 的 文本。
        照 的 节点数 为 做 计数 用 页 的 节点表。
        照 的 节点列 为 页 的 节点表。
    完。
    档 的 快照数 为 档 的 快照数 加 1。
    返还 做 进程浏览_成功 用 档、「取照」、照。
完。

进程浏览_异常 接受 档：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「异常」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「异常」、一组：（空）、「进程浏览」。
    问题 为 一组：（空）。
    若 档 的 进程数 等于 0：
        做 追加 用 问题、「进程为空」。
    完。
    若 档 的 页数 等于 0：
        做 追加 用 问题、「页面为空」。
    完。
    若 档 的 焦点页面 等于 「」：
        做 追加 用 问题、「焦点缺失」。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        做 追加 用 问题、「失败倒挂」。
    完。
    返还 做 进程浏览_成功 用 档、「异常」、问题。
完。

进程浏览_摘要 接受 档：
    摘要 为 新建 浏览摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 进程数 为 档 的 进程数。
    摘要 的 页数 为 档 的 页数。
    摘要 的 标签数 为 档 的 标签数。
    摘要 的 快照数 为 档 的 快照数。
    摘要 的 树数 为 档 的 树数。
    摘要 的 找点数 为 档 的 找点数。
    摘要 的 点选数 为 档 的 点选数。
    摘要 的 输文数 为 档 的 输文数。
    摘要 的 转页数 为 档 的 转页数。
    摘要 的 订阅数 为 档 的 订阅数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 进程浏览_刷新健康 用 档。
    返还 摘要。
完。

进程浏览_初始化 接受 档：
    若 非 做 进程浏览_检查档 用 档：
        返还 做 进程浏览_失败 用 档、「初始化」、「浏览档不可用」。
    完。
    做 进程浏览_记录 用 档、「初始化」、一组：（空）、「进程浏览」。
    档 的 进程表 为 一组：（空）。
    档 的 页面表 为 一组：（空）。
    档 的 订阅表 为 一组：（空）。
    档 的 进程数 为 0。
    档 的 页数 为 0。
    档 的 标签数 为 0。
    档 的 是否初始化 为 真。
    做 进程浏览_建进程 用 档、「核壳一号」、「浏览内核」、「主会话」。
    做 进程浏览_开页 用 档、「页一」、「核壳一号」、「觉醒桌面」、「nova://desk」、「桌面」。
    做 进程浏览_写文 用 档、「页一」、「白璃桌面已启动，结构化树已就绪。」。
    做 进程浏览_订阅 用 档、「快照流」、「页一」、「初始化」。
    返还 做 进程浏览_成功 用 档、「初始化」、「已初始化」。
完。

进程浏览_自检 接受 档：
    若 非 做 进程浏览_检查档 用 档：
        返还 假。
    完。
    若 做 进程浏览_初始化 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程浏览_建进程 用 档、「核壳二号」、「外联浏览」、「副会话」 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程浏览_开页 用 档、「页二」、「核壳二号」、「觉控中心」、「nova://control」、「控制」 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程浏览_转页 用 档、「页二」、「nova://control/live」、「实时控制」 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程浏览_写文 用 档、「页二」、「通过结构化接口接管系统状态。」 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程浏览_取树 用 档、「页二」 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程浏览_找点 用 档、「页二」、「地址」 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程浏览_点选 用 档、「页二」、「页二-按钮」 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程浏览_取照 用 档 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

进程浏览_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 进程浏览_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        返还 做 进程浏览_初始化 用 档。
    完。
    若 请求 的 动作 等于 「建进程」：
        返还 做 进程浏览_建进程 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「开页」：
        返还 做 进程浏览_开页 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项、请求 的 参数 的 第 4 项。
    完。
    若 请求 的 动作 等于 「关页」：
        返还 做 进程浏览_关页 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「切页」：
        返还 做 进程浏览_切页 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「写文」：
        返还 做 进程浏览_写文 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「转页」：
        返还 做 进程浏览_转页 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「取树」：
        返还 做 进程浏览_取树 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「找点」：
        返还 做 进程浏览_找点 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「点选」：
        返还 做 进程浏览_点选 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「取照」：
        返还 做 进程浏览_取照 用 档。
    完。
    若 请求 的 动作 等于 「订阅」：
        返还 做 进程浏览_订阅 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「异常」：
        返还 做 进程浏览_异常 用 档。
    完。
    若 请求 的 动作 等于 「摘要」：
        返还 做 进程浏览_成功 用 档、「摘要」、做 进程浏览_摘要 用 档。
    完。
    返还 做 进程浏览_失败 用 档、「处理」、「未知动作」。
完。

可变 AI_进程浏览_运行指标 为 0。

可变 AI_进程浏览_异常指标 为 0。

AI_进程浏览_自诊断 接受 零参：
    AI_进程浏览_运行指标 为 0。
    返还 AI_进程浏览_运行指标。
完。

AI_进程浏览_优化建议 接受 零参：
    指标 为 做 AI_进程浏览_自诊断。
    返还 「进程浏览状态正常」。
完。

本章 收束。
