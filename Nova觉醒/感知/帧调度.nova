《帧调度》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·张量数学引擎》。

引用《..·推理·定点Q算术》。

引用《.·图层树》。

引用《.·纹理管理》。

引用《..·执行·IO多路复用》。

引用《.·显示合成器》。

引用《.·滚动树》。

帧历史大小 为 60。

帧时间记录 是 一种 事物，含 帧号、vsync时间、主线程耗时、合成线程耗时、光栅化耗时、总帧时间、是Jank。

帧调度器 是 一种 事物，含 vsync_间隔、上一vsync、帧计数、需要提交、需要绘制、总帧时间、丢帧数、jank数、帧历史、帧历史索引、ewma_帧时间_q8、自适应刷新率、目标刷新率。

帧调度器_创建 接受 刷新率：
    sched 为 做 堆_分配 用 帧调度器。
    sched 的 vsync_间隔 为 1000000000 除以 刷新率。
    sched 的 上一vsync 为 0。
    sched 的 帧计数 为 0。
    sched 的 需要提交 为 假。
    sched 的 需要绘制 为 假。
    sched 的 总帧时间 为 0。
    sched 的 丢帧数 为 0。
    sched 的 jank数 为 0。
    sched 的 帧历史索引 为 0。
    sched 的 ewma_帧时间_q8 为 (sched 的 vsync_间隔 左移 8) 除以 2。
    sched 的 自适应刷新率 为 刷新率。
    sched 的 目标刷新率 为 刷新率。
    返还 sched。
完。

帧调度器_请求帧 接受 sched：
    sched 的 需要提交 为 真。
    sched 的 需要绘制 为 真。
完。

帧调度器_on_vsync 接受 sched、当前时间_ns：
    若 非 sched 的 需要绘制：
        返还 0。
    完。
    若 sched 的 上一vsync 不等于 0：
        delta 为 当前时间_ns 减 sched 的 上一vsync。
        若 delta 大于 sched 的 vsync_间隔 乘 3：
            sched 的 jank数 为 sched 的 jank数 加 1。
        完。
        若 delta 大于 sched 的 vsync_间隔 乘 2：
            sched 的 丢帧数 为 sched 的 丢帧数 加 1。
        完。
    完。
    sched 的 上一vsync 为 当前时间_ns。
    sched 的 帧计数 为 sched 的 帧计数 加 1。
    sched 的 需要绘制 为 假。
    返还 1。
完。

帧调度器_记录帧 接受 sched、主线程时间、合成时间、光栅时间：
    总时间 为 主线程时间 加 合成时间 加 光栅时间。
    idx 为 sched 的 帧历史索引 取余于 帧历史大小。
    sched 的 帧历史 的 第 idx 项 的 帧号 为 sched 的 帧计数。
    sched 的 帧历史 的 第 idx 项 的 vsync时间 为 sched 的 上一vsync。
    sched 的 帧历史 的 第 idx 项 的 主线程耗时 为 主线程时间。
    sched 的 帧历史 的 第 idx 项 的 合成线程耗时 为 合成时间。
    sched 的 帧历史 的 第 idx 项 的 光栅化耗时 为 光栅时间。
    sched 的 帧历史 的 第 idx 项 的 总帧时间 为 总时间。
    sched 的 帧历史 的 第 idx 项 的 是Jank 为 总时间 大于 sched 的 vsync_间隔。
    sched 的 帧历史索引 为 sched 的 帧历史索引 加 1。
    sched 的 总帧时间 为 sched 的 总帧时间 加 总时间。
    sched 的 ewma_帧时间_q8 为 224 乘 sched 的 ewma_帧时间_q8 加 32 乘 (总时间 左移 8) 右移 8。
完。

帧调度器_获取FPS 接受 sched：
    若 sched 的 帧计数 等于 0 或者 sched 的 总帧时间 等于 0：
        返还 0。
    完。
    返还 sched 的 帧计数 乘 1000000000 除以 sched 的 总帧时间。
完。

帧调度器_获取平均帧时间 接受 sched：
    若 sched 的 帧计数 等于 0：
        返还 0。
    完。
    返还 sched 的 总帧时间 除以 sched 的 帧计数。
完。

帧调度器_获取Jank率 接受 sched：
    若 sched 的 帧计数 等于 0：
        返还 0。
    完。
    返还 sched 的 jank数 乘 100 除以 sched 的 帧计数。
完。

帧调度器_获取丢帧率 接受 sched：
    若 sched 的 帧计数 等于 0：
        返还 0。
    完。
    返还 sched 的 丢帧数 乘 100 除以 sched 的 帧计数。
完。

帧调度器_AI自适应 接受 sched：
    预测帧时间 为 sched 的 ewma_帧时间_q8 右移 8。
    当前间隔 为 sched 的 vsync_间隔。
    若 预测帧时间 大于 当前间隔 乘 9 除以 10：
        若 sched 的 自适应刷新率 大于 30：
            sched 的 自适应刷新率 为 sched 的 自适应刷新率 减 10。
            sched 的 vsync_间隔 为 1000000000 除以 sched 的 自适应刷新率。
        完。
    完。
    做 否则如果 用 预测帧时间 小于 当前间隔 乘 6 除以 10。
    块：
        若 sched 的 自适应刷新率 小于 sched 的 目标刷新率：
            sched 的 自适应刷新率 为 sched 的 自适应刷新率 加 10。
            若 sched 的 自适应刷新率 大于 sched 的 目标刷新率：
                sched 的 自适应刷新率 为 sched 的 目标刷新率。
            完。
            sched 的 vsync_间隔 为 1000000000 除以 sched 的 自适应刷新率。
        完。
    完。
完。

帧调度器_预测下一帧时间 接受 sched：
    返还 sched 的 ewma_帧时间_q8 右移 8。
完。

帧调度器_有丢帧风险 接受 sched：
    预测 为 sched 的 ewma_帧时间_q8 右移 8。
    返还 预测 大于 sched 的 vsync_间隔 乘 8 除以 10。
完。

帧预算 是 一种 事物，含 主线程_ns、合成_ns、光栅_ns、总预算_ns。

帧调度器_计算预算 接受 sched：
    budget 为 新建 帧预算。
    budget 的 总预算_ns 为 sched 的 vsync_间隔。
    可变 主总 为 0。
    可变 合总 为 0。
    可变 光总 为 0。
    可变 有效数 为 0。
    可变 i 为 0。
    只要 i 小于 帧历史大小 并且 i 小于 sched 的 帧历史索引：
        idx 为 (sched 的 帧历史索引 减 1 减 i) 取余于 帧历史大小。
        主总 为 主总 加 sched 的 帧历史 的 第 idx 项 的 主线程耗时。
        合总 为 合总 加 sched 的 帧历史 的 第 idx 项 的 合成线程耗时。
        光总 为 光总 加 sched 的 帧历史 的 第 idx 项 的 光栅化耗时。
        有效数 为 有效数 加 1。
        i 为 i 加 1。
    完。
    若 有效数 大于 0：
        总 为 主总 加 合总 加 光总。
        若 总 大于 0：
            budget 的 主线程_ns 为 budget 的 总预算_ns 乘 主总 除以 总。
            budget 的 合成_ns 为 budget 的 总预算_ns 乘 合总 除以 总。
            budget 的 光栅_ns 为 budget 的 总预算_ns 乘 光总 除以 总。
        否则：
            budget 的 主线程_ns 为 budget 的 总预算_ns 乘 40 除以 100。
            budget 的 合成_ns 为 budget 的 总预算_ns 乘 30 除以 100。
            budget 的 光栅_ns 为 budget 的 总预算_ns 乘 30 除以 100。
        完。
    完。
    返还 budget。
完。

合成管线 是 一种 事物，含 图层树、动画宿主、显示器、调度器、宽、高、帧序号、活动动画数。

合成管线_创建 接受 宽、高：
    pipeline 为 做 堆_分配 用 合成管线。
    pipeline 的 图层树 为 做 图层树_创建 用 宽 乘 100、高 乘 100。
    pipeline 的 动画宿主 为 做 动画宿主_创建。
    pipeline 的 显示器 为 做 显示合成器_创建 用 宽、高。
    pipeline 的 调度器 为 做 帧调度器_创建 用 60。
    pipeline 的 宽 为 宽。
    pipeline 的 高 为 高。
    pipeline 的 帧序号 为 0。
    pipeline 的 活动动画数 为 0。
    做 纹理管理器_初始化。
    返还 pipeline。
完。

合成管线_提交帧 接受 pipeline：
    当前时间 为 做 平台_获取单调时间_ns。
    sched 为 pipeline 的 调度器。
    若 非 做 帧调度器_on_vsync 用 sched、当前时间：
        返还。
    完。
    动画开始 为 做 平台_获取单调时间_ns。
    pipeline 的 活动动画数 为 做 动画宿主_tick 用 pipeline 的 动画宿主、当前时间。
    动画时间 为 做 平台_获取单调时间_ns 减 动画开始。
    合成开始 为 做 平台_获取单调时间_ns。
    树 为 pipeline 的 图层树。
    pipeline 的 帧序号 为 pipeline 的 帧序号 加 1。
    frame 为 做 合成帧_构建 用 树。
    合成时间 为 做 平台_获取单调时间_ns 减 合成开始。
    绘制开始 为 做 平台_获取单调时间_ns。
    做 显示合成器_绘制帧 用 pipeline 的 显示器、frame。
    绘制时间 为 做 平台_获取单调时间_ns 减 绘制开始。
    做 帧调度器_记录帧 用 sched、动画时间、合成时间、绘制时间。
    做 帧调度器_AI自适应 用 sched。
    若 pipeline 的 活动动画数 大于 0：
        做 帧调度器_请求帧 用 sched。
    完。
完。

合成管线_性能报告 接受 pipeline：
    sched 为 pipeline 的 调度器。
    特征 为 做 Tensor_创建 用 8。
    做 Tensor_写 用 特征、0、做 帧调度器_获取FPS 用 sched。
    做 Tensor_写 用 特征、1、做 帧调度器_获取平均帧时间 用 sched 除以 1000。
    做 Tensor_写 用 特征、2、做 帧调度器_获取Jank率 用 sched。
    做 Tensor_写 用 特征、3、做 帧调度器_获取丢帧率 用 sched。
    做 Tensor_写 用 特征、4、sched 的 帧计数。
    做 Tensor_写 用 特征、5、sched 的 自适应刷新率。
    做 Tensor_写 用 特征、6、做 帧调度器_预测下一帧时间 用 sched 除以 1000。
    做 Tensor_写 用 特征、7、pipeline 的 活动动画数。
    做 全局AI总线 的 发送 用 「渲染.帧调度.性能」、特征。
    做 Tensor_释放 用 特征。
完。

JANK根因_主线程 为 0。

JANK根因_合成 为 1。

JANK根因_光栅 为 2。

JANK根因_混合 为 3。

Jank诊断结果 是 一种 事物，含 jank帧数、根因、最大帧时间_ns、平均Jank帧时间、主线程占比、合成占比、光栅占比。

帧调度器_诊断Jank 接受 sched：
    结果 为 新建 Jank诊断结果。
    可变 主总 为 0。
    可变 合总 为 0。
    可变 光总 为 0。
    可变 最大 为 0。
    可变 jank帧总时间 为 0。
    可变 jank数 为 0。
    可变 i 为 0。
    只要 i 小于 帧历史大小 并且 i 小于 sched 的 帧历史索引：
        idx 为 (sched 的 帧历史索引 减 1 减 i) 取余于 帧历史大小。
        记录 为 sched 的 帧历史 的 第 idx 项。
        若 记录 的 是Jank：
            主总 为 主总 加 记录 的 主线程耗时。
            合总 为 合总 加 记录 的 合成线程耗时。
            光总 为 光总 加 记录 的 光栅化耗时。
            jank帧总时间 为 jank帧总时间 加 记录 的 总帧时间。
            jank数 为 jank数 加 1。
            若 记录 的 总帧时间 大于 最大：
                最大 为 记录 的 总帧时间。
            完。
        完。
        i 为 i 加 1。
    完。
    结果 的 jank帧数 为 jank数。
    结果 的 最大帧时间_ns 为 最大。
    若 jank数 大于 0：
        结果 的 平均Jank帧时间 为 jank帧总时间 除以 jank数。
        总 为 主总 加 合总 加 光总。
        若 总 大于 0：
            结果 的 主线程占比 为 主总 乘 100 除以 总。
            结果 的 合成占比 为 合总 乘 100 除以 总。
            结果 的 光栅占比 为 光总 乘 100 除以 总。
        完。
        若 结果 的 主线程占比 大于 50：
            结果 的 根因 为 JANK根因_主线程。
        完。
        做 否则如果 用 结果 的 合成占比 大于 50。
        块：
            结果 的 根因 为 JANK根因_合成。
        完。
        做 否则如果 用 结果 的 光栅占比 大于 50。
        块：
            结果 的 根因 为 JANK根因_光栅。
        完。
        否则。
        块：
            结果 的 根因 为 JANK根因_混合。
        完。
    完。
    返还 结果。
完。

帧调度器_发送Jank诊断 接受 sched：
    诊断 为 做 帧调度器_诊断Jank 用 sched。
    若 诊断 的 jank帧数 等于 0：
        返还。
    完。
    特征 为 做 Tensor_创建 用 6。
    做 Tensor_写 用 特征、0、诊断 的 jank帧数。
    做 Tensor_写 用 特征、1、诊断 的 根因。
    做 Tensor_写 用 特征、2、诊断 的 最大帧时间_ns 除以 1000。
    做 Tensor_写 用 特征、3、诊断 的 主线程占比。
    做 Tensor_写 用 特征、4、诊断 的 合成占比。
    做 Tensor_写 用 特征、5、诊断 的 光栅占比。
    做 全局AI总线 的 发送 用 「渲染.Jank诊断」、特征。
    做 Tensor_释放 用 特征。
完。

最大脏区域数 为 16。

脏矩形 是 一种 事物，含 x、y、w、h。

脏区域管理器 是 一种 事物，含 区域列表、区域数、全屏脏。

脏区域_创建 接受 零参：
    mgr 为 做 堆_分配 用 脏区域管理器。
    mgr 的 区域数 为 0。
    mgr 的 全屏脏 为 假。
    返还 mgr。
完。

脏区域_添加 接受 mgr、x、y、w、h：
    若 mgr 的 全屏脏：
        返还。
    完。
    若 mgr 的 区域数 大于或等于 最大脏区域数：
        mgr 的 全屏脏 为 真。
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 mgr 的 区域数：
        r 为 mgr 的 区域列表 的 第 i 项。
        若 x 大于或等于 r 的 x 并且 y 大于或等于 r 的 y 并且 x 加 w 小于或等于 r 的 x 加 r 的 w 并且 y 加 h 小于或等于 r 的 y 加 r 的 h：
            返还。
        完。
        ox 为 做 最大值 用 x、r 的 x。
        oy 为 做 最大值 用 y、r 的 y。
        ow 为 做 最小值 用 x 加 w、r 的 x 加 r 的 w 减 ox。
        oh 为 做 最小值 用 y 加 h、r 的 y 加 r 的 h 减 oy。
        若 ow 大于 0 并且 oh 大于 0：
            重叠面积 为 ow 乘 oh。
            新面积 为 w 乘 h。
            若 重叠面积 乘 2 大于 新面积：
                nx 为 做 最小值 用 x、r 的 x。
                ny 为 做 最小值 用 y、r 的 y。
                mgr 的 区域列表 的 第 i 项 的 w 为 做 最大值 用 x 加 w、r 的 x 加 r 的 w 减 nx。
                mgr 的 区域列表 的 第 i 项 的 h 为 做 最大值 用 y 加 h、r 的 y 加 r 的 h 减 ny。
                mgr 的 区域列表 的 第 i 项 的 x 为 nx。
                mgr 的 区域列表 的 第 i 项 的 y 为 ny。
                返还。
            完。
        完。
        i 为 i 加 1。
    完。
    mgr 的 区域列表 的 第 mgr 的 区域数 项 为 新建 脏矩形：x 为 x、y 为 y、w 为 w、h 为 h。
    mgr 的 区域数 为 mgr 的 区域数 加 1。
完。

脏区域_重置 接受 mgr：
    mgr 的 区域数 为 0。
    mgr 的 全屏脏 为 假。
完。

脏区域_是否需要合成 接受 mgr：
    返还 mgr 的 全屏脏 或者 mgr 的 区域数 大于 0。
完。

脏区域_总面积 接受 mgr：
    若 mgr 的 全屏脏：
        返还 0xFFFFFFFF。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 mgr 的 区域数：
        总 为 总 加 mgr 的 区域列表 的 第 i 项 的 w 乘 mgr 的 区域列表 的 第 i 项 的 h。
        i 为 i 加 1。
    完。
    返还 总。
完。

缓冲状态_空闲 为 0。

缓冲状态_绘制中 为 1。

缓冲状态_就绪 为 2。

缓冲状态_显示中 为 3。

帧缓冲槽 是 一种 事物，含 缓冲地址、帧号、状态、完成时间。

多缓冲管理器 是 一种 事物，含 缓冲区、缓冲数、前缓冲索引、后缓冲索引、宽、高、步长。

多缓冲_创建 接受 宽、高、三缓冲：
    mgr 为 做 堆_分配 用 多缓冲管理器。
    mgr 的 宽 为 宽。
    mgr 的 高 为 高。
    mgr 的 步长 为 宽 乘 4。
    mgr 的 缓冲数 为 若 三缓冲 则 3 否则 2。
    可变 i 为 0。
    只要 i 小于 mgr 的 缓冲数：
        mgr 的 缓冲区 的 第 i 项 的 缓冲地址 为 做 堆_分配 用 宽 乘 高 乘 4。
        mgr 的 缓冲区 的 第 i 项 的 帧号 为 0。
        mgr 的 缓冲区 的 第 i 项 的 状态 为 缓冲状态_空闲。
        mgr 的 缓冲区 的 第 i 项 的 完成时间 为 0。
        i 为 i 加 1。
    完。
    mgr 的 前缓冲索引 为 0。
    mgr 的 后缓冲索引 为 1。
    mgr 的 缓冲区 的 第 0 项 的 状态 为 缓冲状态_显示中。
    返还 mgr。
完。

多缓冲_获取后缓冲 接受 mgr：
    idx 为 mgr 的 后缓冲索引。
    mgr 的 缓冲区 的 第 idx 项 的 状态 为 缓冲状态_绘制中。
    返还 mgr 的 缓冲区 的 第 idx 项 的 缓冲地址。
完。

多缓冲_提交后缓冲 接受 mgr、帧号：
    idx 为 mgr 的 后缓冲索引。
    mgr 的 缓冲区 的 第 idx 项 的 状态 为 缓冲状态_就绪。
    mgr 的 缓冲区 的 第 idx 项 的 帧号 为 帧号。
    mgr 的 缓冲区 的 第 idx 项 的 完成时间 为 做 平台_获取单调时间_ns。
完。

多缓冲_交换 接受 mgr：
    旧前 为 mgr 的 前缓冲索引。
    新前 为 mgr 的 后缓冲索引。
    若 mgr 的 缓冲区 的 第 新前 项 的 状态 不等于 缓冲状态_就绪：
        返还 假。
    完。
    mgr 的 缓冲区 的 第 旧前 项 的 状态 为 缓冲状态_空闲。
    mgr 的 缓冲区 的 第 新前 项 的 状态 为 缓冲状态_显示中。
    mgr 的 前缓冲索引 为 新前。
    可变 i 为 0。
    只要 i 小于 mgr 的 缓冲数：
        若 mgr 的 缓冲区 的 第 i 项 的 状态 等于 缓冲状态_空闲：
            mgr 的 后缓冲索引 为 i。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    mgr 的 后缓冲索引 为 旧前。
    返还 真。
完。

多缓冲_获取显示地址 接受 mgr：
    返还 mgr 的 缓冲区 的 第 mgr 的 前缓冲索引 项 的 缓冲地址。
完。

多缓冲_销毁 接受 mgr：
    可变 i 为 0。
    只要 i 小于 mgr 的 缓冲数：
        做 堆_释放 用 mgr 的 缓冲区 的 第 i 项 的 缓冲地址。
        i 为 i 加 1。
    完。
    做 堆_释放 用 mgr。
完。

阶段_主线程 为 0。

阶段_合成 为 1。

阶段_光栅 为 2。

阶段_GPU 为 3。

阶段数 为 4。

负载均衡器 是 一种 事物，含 阶段权重、阶段耗时累计、采样数、瓶颈阶段、均衡状态。

负载均衡器_创建 接受 零参：
    lb 为 做 堆_分配 用 负载均衡器。
    可变 i 为 0。
    只要 i 小于 阶段数：
        lb 的 阶段权重 的 第 i 项 为 64。
        lb 的 阶段耗时累计 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    lb 的 采样数 为 0。
    lb 的 瓶颈阶段 为 0。
    lb 的 均衡状态 为 0。
    返还 lb。
完。

负载均衡器_更新 接受 lb、主时间、合成时间、光栅时间、GPU时间：
    lb 的 阶段耗时累计 的 第 阶段_主线程 项 为 lb 的 阶段耗时累计 的 第 阶段_主线程 项 加 主时间。
    lb 的 阶段耗时累计 的 第 阶段_合成 项 为 lb 的 阶段耗时累计 的 第 阶段_合成 项 加 合成时间。
    lb 的 阶段耗时累计 的 第 阶段_光栅 项 为 lb 的 阶段耗时累计 的 第 阶段_光栅 项 加 光栅时间。
    lb 的 阶段耗时累计 的 第 阶段_GPU 项 为 lb 的 阶段耗时累计 的 第 阶段_GPU 项 加 GPU时间。
    lb 的 采样数 为 lb 的 采样数 加 1。
    若 lb 的 采样数 取余于 16 等于 0：
        可变 总 为 0。
        可变 最大值 为 0。
        可变 最大索引 为 0。
        可变 j 为 0。
        只要 j 小于 阶段数：
            总 为 总 加 lb 的 阶段耗时累计 的 第 j 项。
            若 lb 的 阶段耗时累计 的 第 j 项 大于 最大值：
                最大值 为 lb 的 阶段耗时累计 的 第 j 项。
                最大索引 为 j。
            完。
            j 为 j 加 1。
        完。
        lb 的 瓶颈阶段 为 最大索引。
        若 总 大于 0：
            可变 k 为 0。
            只要 k 小于 阶段数：
                lb 的 阶段权重 的 第 k 项 为 (lb 的 阶段耗时累计 的 第 k 项 左移 8) 除以 总。
                k 为 k 加 1。
            完。
        完。
        若 总 大于 0 并且 最大值 乘 100 除以 总 大于 60：
            lb 的 均衡状态 为 2。
        完。
        做 否则如果 用 总 大于 0 并且 最大值 乘 100 除以 总 大于 40。
        块：
            lb 的 均衡状态 为 1。
        完。
        否则。
        块：
            lb 的 均衡状态 为 0。
        完。
        可变 m 为 0。
        只要 m 小于 阶段数：
            lb 的 阶段耗时累计 的 第 m 项 为 0。
            m 为 m 加 1。
        完。
    完。
完。

负载均衡器_获取瓶颈 接受 lb：
    返还 lb 的 瓶颈阶段。
完。

负载均衡器_是否需要降级 接受 lb：
    返还 lb 的 均衡状态 等于 2。
完。

合成管线_调整大小 接受 pipeline、新宽、新高：
    若 新宽 等于 pipeline 的 宽 并且 新高 等于 pipeline 的 高：
        返还。
    完。
    pipeline 的 宽 为 新宽。
    pipeline 的 高 为 新高。
    做 图层树_调整大小 用 pipeline 的 图层树、新宽 乘 100、新高 乘 100。
    做 显示合成器_调整大小 用 pipeline 的 显示器、新宽、新高。
    做 帧调度器_请求帧 用 pipeline 的 调度器。
完。

合成管线_设置刷新率 接受 pipeline、hz：
    sched 为 pipeline 的 调度器。
    sched 的 目标刷新率 为 hz。
    sched 的 自适应刷新率 为 hz。
    sched 的 vsync_间隔 为 1000000000 除以 hz。
完。

合成管线_销毁 接受 pipeline：
    做 图层树_销毁 用 pipeline 的 图层树。
    做 动画宿主_销毁 用 pipeline 的 动画宿主。
    做 显示合成器_销毁 用 pipeline 的 显示器。
    做 堆_释放 用 pipeline 的 调度器。
    做 堆_释放 用 pipeline。
完。

本章 收束。
