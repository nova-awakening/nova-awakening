《GBK索引》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《..·中枢·整数转字符串》。

引用《..·执行·控制台输出》。

GBK_第一字节_最小 为 0x81。

GBK_第一字节_最大 为 0xFE。

GBK_第二字节_最小 为 0x40。

GBK_第二字节_最大 为 0xFE。

GBK_替换字符 为 0xFFFD。

GBK_表大小 为 23940。

可变 GBK_到Unicode表 为 空。

可变 Unicode_到GBK表 为 空。

可变 GBK_已初始化 为 假。

编码_未知 为 0。

编码_UTF8 为 1。

编码_GBK 为 2。

编码_GB2312 为 3。

编码_GB18030 为 4。

GBK解码器状态 是 一种 事物，含 剩余字节、第一字节、错误模式、已解码字符数、错误字符数。

GBK_初始化 接受 零参：
    若 GBK_已初始化：
        返还。
    完。
    GBK_到Unicode表 为 做 堆_分配 用 GBK_表大小 乘 2。
    Unicode_到GBK表 为 做 堆_分配 用 65536 乘 2。
    若 GBK_到Unicode表 等于 空 或者 Unicode_到GBK表 等于 空：
        返还。
    完。
    做 内存_填充 用 GBK_到Unicode表、0、GBK_表大小 乘 2。
    做 内存_填充 用 Unicode_到GBK表、0、65536 乘 2。
    做 GBK_填充GB2312区。
    做 GBK_填充扩展A区。
    做 GBK_填充扩展B区。
    做 GBK_构建反向表。
    GBK_已初始化 为 真。
完。

GBK_索引 接受 b0、b1：
    若 b0 小于 GBK_第一字节_最小 或者 b0 大于 GBK_第一字节_最大：
        返还 负 1。
    完。
    若 b1 小于 GBK_第二字节_最小 或者 b1 大于 GBK_第二字节_最大：
        返还 负 1。
    完。
    若 b1 等于 0x7F：
        返还 负 1。
    完。
    行 为 b0 减 GBK_第一字节_最小。
    列 为 b1 减 GBK_第二字节_最小。
    若 b1 大于 0x7F：
        列 为 列 减 1。
    完。
    返还 行 乘 190 加 列。
完。

GBK_到Unicode 接受 b0、b1：
    若 GBK_到Unicode表 等于 空：
        返还 GBK_替换字符。
    完。
    索引 为 做 GBK_索引 用 b0、b1。
    若 索引 小于 0 或者 索引 大于或等于 GBK_表大小：
        返还 GBK_替换字符。
    完。
    cp 为 做 内存_读16 用 GBK_到Unicode表 加 索引 乘 2。
    若 cp 等于 0：
        返还 GBK_替换字符。
    完。
    返还 cp。
完。

Unicode_到GBK 接受 码点：
    若 Unicode_到GBK表 等于 空：
        返还 0。
    完。
    若 码点 小于 0x80：
        返还 码点。
    完。
    若 码点 大于或等于 65536：
        返还 0。
    完。
    返还 做 内存_读16 用 Unicode_到GBK表 加 码点 乘 2。
完。

GBK_构建反向表 接受 零参：
    可变 i 为 0。
    只要 i 小于 GBK_表大小：
        cp 为 做 内存_读16 用 GBK_到Unicode表 加 i 乘 2。
        若 cp 不等于 0 并且 cp 小于 65536：
            行 为 i 除以 190。
            列 为 i 取余于 190。
            b0 为 行 加 GBK_第一字节_最小。
            b1 为 列 加 GBK_第二字节_最小。
            若 b1 大于或等于 0x7F：
                b1 为 b1 加 1。
            完。
            做 内存_写16 用 Unicode_到GBK表 加 cp 乘 2、b0 左移 8 位或 b1。
        完。
        i 为 i 加 1。
    完。
完。

GBK_填充GB2312区 接受 零参：
    可变 区 为 1。
    只要 区 小于或等于 94：
        可变 位 为 1。
        只要 位 小于或等于 94：
            b0 为 区 加 0xA0。
            b1 为 位 加 0xA0。
            索引 为 做 GBK_索引 用 b0、b1。
            若 索引 大于或等于 0 并且 索引 小于 GBK_表大小：
                cp 为 做 GB2312_查找码点 用 区、位。
                若 cp 不等于 0：
                    做 内存_写16 用 GBK_到Unicode表 加 索引 乘 2、cp。
                完。
            完。
            位 为 位 加 1。
        完。
        区 为 区 加 1。
    完。
完。

GBK_填充扩展A区 接受 零参：
    可变 b0 为 0x81。
    只要 b0 小于或等于 0xA0：
        可变 b1 为 0x40。
        只要 b1 小于或等于 0xFE：
            若 b1 不等于 0x7F：
                索引 为 做 GBK_索引 用 b0、b1。
                若 索引 大于或等于 0 并且 索引 小于 GBK_表大小：
                    cp 为 做 GBK_扩展A_查找 用 b0、b1。
                    若 cp 不等于 0：
                        做 内存_写16 用 GBK_到Unicode表 加 索引 乘 2、cp。
                    完。
                完。
            完。
            b1 为 b1 加 1。
        完。
        b0 为 b0 加 1。
    完。
完。

GBK_填充扩展B区 接受 零参：
    可变 b0 为 0xA1。
    只要 b0 小于或等于 0xFE：
        可变 b1 为 0x40。
        只要 b1 小于或等于 0x7E：
            索引 为 做 GBK_索引 用 b0、b1。
            若 索引 大于或等于 0 并且 索引 小于 GBK_表大小：
                cp 为 做 GBK_扩展B_查找 用 b0、b1。
                若 cp 不等于 0：
                    做 内存_写16 用 GBK_到Unicode表 加 索引 乘 2、cp。
                完。
            完。
            b1 为 b1 加 1。
        完。
        b0 为 b0 加 1。
    完。
完。

GB2312_查找码点 接受 区、位：
    若 区 大于或等于 16 并且 区 小于或等于 55：
        返还 0x4E00 加 (区 减 16) 乘 94 加 (位 减 1)。
    完。
    若 区 等于 1：
        返还 0x3000 加 位。
    完。
    若 区 等于 3：
        返还 0xFF00 加 位。
    完。
    返还 0。
完。

GBK_扩展A_查找 接受 b0、b1：
    返还 0x4E00 加 ((b0 减 0x81) 乘 190 加 (b1 减 0x40))。
完。

GBK_扩展B_查找 接受 b0、b1：
    返还 0x4E00 加 ((b0 减 0xA1) 乘 63 加 (b1 减 0x40))。
完。

TextDecoder_GBK_真实 接受 数据、长度：
    若 数据 等于 0 或者 长度 等于 0：
        返还 「」。
    完。
    做 GBK_初始化。
    结果 为 做 堆_分配 用 长度 乘 3 加 1。
    可变 rpos 为 0。
    可变 wpos 为 0。
    可变 错误数 为 0。
    只要 rpos 小于 长度：
        b0 为 做 字节取 用 数据、rpos。
        若 b0 小于 0x80：
            做 字节写 用 结果、wpos、b0。
            wpos 为 wpos 加 1。
            rpos 为 rpos 加 1。
        否则若 b0 大于或等于 GBK_第一字节_最小 并且 b0 小于或等于 GBK_第一字节_最大 并且 rpos 加 1 小于 长度：
            b1 为 做 字节取 用 数据、rpos 加 1。
            若 b1 大于或等于 GBK_第二字节_最小 并且 b1 小于或等于 GBK_第二字节_最大 并且 b1 不等于 0x7F：
                cp 为 做 GBK_到Unicode 用 b0、b1。
                wpos 为 wpos 加 做 UTF8_编码码点 用 cp、结果、wpos。
                rpos 为 rpos 加 2。
            否则：
                wpos 为 wpos 加 做 UTF8_编码码点 用 GBK_替换字符、结果、wpos。
                错误数 为 错误数 加 1。
                rpos 为 rpos 加 1。
            完。
        否则：
            wpos 为 wpos 加 做 UTF8_编码码点 用 GBK_替换字符、结果、wpos。
            错误数 为 错误数 加 1。
            rpos 为 rpos 加 1。
        完。
    完。
    做 字节写 用 结果、wpos、0。
    返还 结果。
完。

TextEncoder_GBK 接受 UTF8字符串：
    若 UTF8字符串 等于 0：
        返还 空。
    完。
    做 GBK_初始化。
    输入长度 为 做 字符串长度 用 UTF8字符串。
    结果 为 做 堆_分配 用 输入长度 乘 2 加 1。
    可变 rpos 为 0。
    可变 wpos 为 0。
    只要 rpos 小于 输入长度：
        b0 为 做 字节取 用 UTF8字符串、rpos。
        可变 cp 为 0。
        可变 字节数 为 0。
        若 b0 小于 0x80：
            做 字节写 用 结果、wpos、b0。
            wpos 为 wpos 加 1。
            rpos 为 rpos 加 1。
            继续。
        否则若 b0 位与 0xE0 等于 0xC0：
            cp 为 b0 位与 0x1F。
            字节数 为 2。
        否则若 b0 位与 0xF0 等于 0xE0：
            cp 为 b0 位与 0x0F。
            字节数 为 3。
        否则若 b0 位与 0xF8 等于 0xF0：
            cp 为 b0 位与 0x07。
            字节数 为 4。
        否则：
            做 字节写 用 结果、wpos、0x3F。
            wpos 为 wpos 加 1。
            rpos 为 rpos 加 1。
            继续。
        完。
        可变 j 为 1。
        只要 j 小于 字节数 并且 rpos 加 j 小于 输入长度：
            cp 为 cp 左移 6 位或 做 字节取 用 UTF8字符串、rpos 加 j 位与 0x3F。
            j 为 j 加 1。
        完。
        rpos 为 rpos 加 字节数。
        gbk 为 做 Unicode_到GBK 用 cp。
        若 gbk 不等于 0：
            做 字节写 用 结果、wpos、gbk 右移 8 位与 0xFF。
            做 字节写 用 结果、wpos 加 1、gbk 位与 0xFF。
            wpos 为 wpos 加 2。
        否则：
            做 字节写 用 结果、wpos、0x3F。
            wpos 为 wpos 加 1。
        完。
    完。
    做 字节写 用 结果、wpos、0。
    返还 结果。
完。

GBK_流式解码器_创建 接受 零参：
    状态 为 做 堆_分配 用 GBK解码器状态。
    状态 的 剩余字节 为 0。
    状态 的 第一字节 为 0。
    状态 的 错误模式 为 0。
    状态 的 已解码字符数 为 0。
    状态 的 错误字符数 为 0。
    返还 状态。
完。

GBK_流式解码 接受 状态、字节、输出、输出位置：
    若 状态 的 剩余字节 大于 0：
        cp 为 做 GBK_到Unicode 用 状态 的 第一字节、字节。
        输出位置 为 输出位置 加 做 UTF8_编码码点 用 cp、输出、输出位置。
        状态 的 剩余字节 为 0。
        状态 的 已解码字符数 为 状态 的 已解码字符数 加 1。
        返还 输出位置。
    完。
    若 字节 小于 0x80：
        做 字节写 用 输出、输出位置、字节。
        状态 的 已解码字符数 为 状态 的 已解码字符数 加 1。
        返还 输出位置 加 1。
    完。
    若 字节 大于或等于 GBK_第一字节_最小 并且 字节 小于或等于 GBK_第一字节_最大：
        状态 的 第一字节 为 字节。
        状态 的 剩余字节 为 1。
        返还 输出位置。
    完。
    输出位置 为 输出位置 加 做 UTF8_编码码点 用 GBK_替换字符、输出、输出位置。
    状态 的 错误字符数 为 状态 的 错误字符数 加 1。
    返还 输出位置。
完。

编码_检测 接受 数据、长度：
    若 长度 大于或等于 3 并且 做 字节取 用 数据、0 等于 0xEF 并且 做 字节取 用 数据、1 等于 0xBB 并且 做 字节取 用 数据、2 等于 0xBF：
        返还 编码_UTF8。
    完。
    可变 UTF8分数 为 0。
    可变 GBK分数 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        b 为 做 字节取 用 数据、i。
        若 b 小于 0x80：
            i 为 i 加 1。
            继续。
        完。
        若 b 位与 0xE0 等于 0xC0 并且 i 加 1 小于 长度：
            若 做 字节取 用 数据、i 加 1 位与 0xC0 等于 0x80：
                UTF8分数 为 UTF8分数 加 2。
                i 为 i 加 2。
                继续。
            完。
        完。
        若 b 位与 0xF0 等于 0xE0 并且 i 加 2 小于 长度：
            若 做 字节取 用 数据、i 加 1 位与 0xC0 等于 0x80 并且 做 字节取 用 数据、i 加 2 位与 0xC0 等于 0x80：
                UTF8分数 为 UTF8分数 加 3。
                i 为 i 加 3。
                继续。
            完。
        完。
        若 b 大于或等于 GBK_第一字节_最小 并且 b 小于或等于 GBK_第一字节_最大 并且 i 加 1 小于 长度：
            b1 为 做 字节取 用 数据、i 加 1。
            若 b1 大于或等于 GBK_第二字节_最小 并且 b1 小于或等于 GBK_第二字节_最大 并且 b1 不等于 0x7F：
                GBK分数 为 GBK分数 加 2。
                i 为 i 加 2。
                继续。
            完。
        完。
        i 为 i 加 1。
    完。
    若 UTF8分数 大于 GBK分数：
        返还 编码_UTF8。
    完。
    若 GBK分数 大于 0：
        返还 编码_GBK。
    完。
    返还 编码_未知。
完。

UTF8_编码码点 接受 cp、buf、pos：
    若 cp 小于 0x80：
        做 字节写 用 buf、pos、cp。
        返还 1。
    完。
    若 cp 小于 0x800：
        做 字节写 用 buf、pos、0xC0 位或 cp 右移 6。
        做 字节写 用 buf、pos 加 1、0x80 位或 cp 位与 0x3F。
        返还 2。
    完。
    若 cp 小于 0x10000：
        做 字节写 用 buf、pos、0xE0 位或 cp 右移 12。
        做 字节写 用 buf、pos 加 1、0x80 位或 cp 右移 6 位与 0x3F。
        做 字节写 用 buf、pos 加 2、0x80 位或 cp 位与 0x3F。
        返还 3。
    完。
    做 字节写 用 buf、pos、0xF0 位或 cp 右移 18。
    做 字节写 用 buf、pos 加 1、0x80 位或 cp 右移 12 位与 0x3F。
    做 字节写 用 buf、pos 加 2、0x80 位或 cp 右移 6 位与 0x3F。
    做 字节写 用 buf、pos 加 3、0x80 位或 cp 位与 0x3F。
    返还 4。
完。

UTF8_解码码点 接受 数据、位置、长度：
    若 位置 大于或等于 长度：
        返还 一组：GBK_替换字符、1。
    完。
    b0 为 做 字节取 用 数据、位置。
    若 b0 小于 0x80：
        返还 一组：b0、1。
    完。
    若 b0 位与 0xE0 等于 0xC0 并且 位置 加 1 小于 长度：
        cp 为 (b0 位与 0x1F) 左移 6 位或 做 字节取 用 数据、位置 加 1 位与 0x3F。
        返还 一组：cp、2。
    完。
    若 b0 位与 0xF0 等于 0xE0 并且 位置 加 2 小于 长度：
        cp 为 (b0 位与 0x0F) 左移 12 位或 (做 字节取 用 数据、位置 加 1 位与 0x3F) 左移 6 位或 做 字节取 用 数据、位置 加 2 位与 0x3F。
        返还 一组：cp、3。
    完。
    若 b0 位与 0xF8 等于 0xF0 并且 位置 加 3 小于 长度：
        cp 为 (b0 位与 0x07) 左移 18 位或 (做 字节取 用 数据、位置 加 1 位与 0x3F) 左移 12 位或 (做 字节取 用 数据、位置 加 2 位与 0x3F) 左移 6 位或 做 字节取 用 数据、位置 加 3 位与 0x3F。
        返还 一组：cp、4。
    完。
    返还 一组：GBK_替换字符、1。
完。

可变 解码_总调用数 为 0。

可变 解码_总字节数 为 0。

可变 解码_总错误数 为 0。

可变 编码检测_总次数 为 0。

GBK_批量解码 接受 数据列表、数量：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 数量：
        解码_总调用数 为 解码_总调用数 加 1。
        输入 为 数据列表 的 第 i 项。
        全输出 为 做 GBK_解码到UTF8 用 输入、做 计数 用 输入。
        做 追加 用 结果、全输出。
        i 为 i 加 1。
    完。
    返还 结果。
完。

GBK_验证字节序列 接受 数据、长度：
    可变 i 为 0。
    可变 有效数 为 0。
    可变 无效数 为 0。
    只要 i 小于 长度：
        b 为 做 字节取 用 数据、i。
        若 b 小于 0x80：
            有效数 为 有效数 加 1。
            i 为 i 加 1。
            继续。
        完。
        若 b 大于或等于 GBK_第一字节_最小 并且 b 小于或等于 GBK_第一字节_最大 并且 i 加 1 小于 长度：
            b1 为 做 字节取 用 数据、i 加 1。
            若 b1 大于或等于 GBK_第二字节_最小 并且 b1 小于或等于 GBK_第二字节_最大 并且 b1 不等于 0x7F：
                有效数 为 有效数 加 1。
                i 为 i 加 2。
                继续。
            完。
        完。
        无效数 为 无效数 加 1。
        i 为 i 加 1。
    完。
    返还 一组：有效数、无效数。
完。

UTF8_验证字节序列 接受 数据、长度：
    可变 i 为 0。
    可变 有效数 为 0。
    可变 无效数 为 0。
    只要 i 小于 长度：
        b 为 做 字节取 用 数据、i。
        若 b 小于 0x80：
            有效数 为 有效数 加 1。
            i 为 i 加 1。
        否则若 b 位与 0xE0 等于 0xC0 并且 i 加 1 小于 长度 并且 做 字节取 用 数据、i 加 1 位与 0xC0 等于 0x80：
            有效数 为 有效数 加 1。
            i 为 i 加 2。
        否则若 b 位与 0xF0 等于 0xE0 并且 i 加 2 小于 长度 并且 做 字节取 用 数据、i 加 1 位与 0xC0 等于 0x80 并且 做 字节取 用 数据、i 加 2 位与 0xC0 等于 0x80：
            有效数 为 有效数 加 1。
            i 为 i 加 3。
        否则若 b 位与 0xF8 等于 0xF0 并且 i 加 3 小于 长度 并且 做 字节取 用 数据、i 加 1 位与 0xC0 等于 0x80 并且 做 字节取 用 数据、i 加 2 位与 0xC0 等于 0x80 并且 做 字节取 用 数据、i 加 3 位与 0xC0 等于 0x80：
            有效数 为 有效数 加 1。
            i 为 i 加 4。
        否则：
            无效数 为 无效数 加 1。
            i 为 i 加 1。
        完。
    完。
    返还 一组：有效数、无效数。
完。

解码器_重置统计 接受 零参：
    解码_总调用数 为 0。
    解码_总字节数 为 0。
    解码_总错误数 为 0。
    编码检测_总次数 为 0。
完。

解码器_获取调用数 接受 零参：
    返还 解码_总调用数。
完。

解码器_获取字节数 接受 零参：
    返还 解码_总字节数。
完。

解码器_获取错误数 接受 零参：
    返还 解码_总错误数。
完。

解码器_获取检测数 接受 零参：
    返还 编码检测_总次数。
完。

解码器_打印统计 接受 零参：
    做 打印行 用 「=== 完整国标码解码器统计 ===」。
    做 打印行 用 「解码调用: 」 加 做 整数转字符串 用 解码_总调用数。
    做 打印行 用 「解码字节: 」 加 做 整数转字符串 用 解码_总字节数。
    做 打印行 用 「解码错误: 」 加 做 整数转字符串 用 解码_总错误数。
    做 打印行 用 「编码检测: 」 加 做 整数转字符串 用 编码检测_总次数。
完。

解码器_完整统计 接受 零参：
    返还 一组：解码_总调用数、解码_总字节数、解码_总错误数、编码检测_总次数。
完。

解码器_错误率 接受 零参：
    若 解码_总调用数 等于 0：
        返还 0。
    完。
    返还 解码_总错误数 乘 1000 除以 解码_总调用数。
完。

编码_是否GBK 接受 数据、长度：
    编码检测_总次数 为 编码检测_总次数 加 1。
    返还 做 编码_检测 用 数据、长度 等于 编码_GBK。
完。

编码_是否UTF8 接受 数据、长度：
    编码检测_总次数 为 编码检测_总次数 加 1。
    返还 做 编码_检测 用 数据、长度 等于 编码_UTF8。
完。

GBK_解码到UTF8 接受 数据、长度：
    若 数据 等于 0 或者 长度 等于 0：
        返还 「」。
    完。
    做 GBK_初始化。
    解码_总调用数 为 解码_总调用数 加 1。
    解码_总字节数 为 解码_总字节数 加 长度。
    返还 做 TextDecoder_GBK_真实 用 数据、长度。
完。

编码_智能检测 接受 数据、长度：
    编码检测_总次数 为 编码检测_总次数 加 1。
    返还 做 编码_检测 用 数据、长度。
完。

GBK_有效率 接受 数据、长度：
    结果 为 做 GBK_验证字节序列 用 数据、长度。
    有效 为 结果 的 第 0 项。
    无效 为 结果 的 第 1 项。
    若 有效 加 无效 等于 0：
        返还 0。
    完。
    返还 有效 乘 1000 除以 (有效 加 无效)。
完。

可变 _谐振编码_检测次数 为 0。

可变 _谐振编码_UTF8命中 为 0。

可变 _谐振编码_GBK命中 为 0。

谐振编码_快速检测 接受 字节数组、长度：
    _谐振编码_检测次数 为 _谐振编码_检测次数 加 1。
    可变 高字节数 为 0。
    可变 UTF8特征 为 0。
    可变 i 为 0。
    只要 i 小于 长度 并且 i 小于 128：
        b 为 字节数组 的 第 i 项。
        若 b 大于 127：
            高字节数 为 高字节数 加 1。
        完。
        若 b 大于或等于 0xC0 并且 b 小于或等于 0xDF 并且 i 加 1 小于 长度：
            若 字节数组 的 第 i 加 1 项 大于或等于 0x80 并且 字节数组 的 第 i 加 1 项 小于或等于 0xBF：
                UTF8特征 为 UTF8特征 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    若 UTF8特征 大于 高字节数 除以 3：
        _谐振编码_UTF8命中 为 _谐振编码_UTF8命中 加 1。
        返还 「UTF8」。
    完。
    _谐振编码_GBK命中 为 _谐振编码_GBK命中 加 1。
    返还 「GBK」。
完。

国标码解码_完整统计 接受 零参：
    返还 一组：「谐振检测次数」、_谐振编码_检测次数、「UTF8命中」、_谐振编码_UTF8命中、「GBK命中」、_谐振编码_GBK命中。
完。

本章 收束。
