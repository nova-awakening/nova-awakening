《媒体记录接口》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·内核打印》。

引用《..·中枢·高精度定时器hrtimer》。

引用《..·中枢·整数转字符串》。

录制_非活动 为 0。

录制_录制中 为 1。

录制_已暂停 为 2。

默认_视频码率 为 2500000。

默认_音频码率 为 128000。

默认_时间切片 为 1000。

最大数据块数 为 4096。

最大录制时长_ms 为 3600000。

媒体Recorder 是 一种 事物，含 stream、mimeType、状态、videoBitsPerSecond、audioBitsPerSecond、数据块、ondataavailable、onstop、onerror、onstart、onpause、onresume、时间切片、开始时间、暂停时间、总暂停时长、总帧数、丢帧数、数据块计数、总字节数。

媒体数据块 是 一种 事物，含 数据、大小、时间戳、类型、下一个。

录制统计 是 一种 事物，含 总录制次数、总录制时长_ms、总字节数、总数据块数、总帧数、总丢帧数、暂停次数、错误次数。

可变 全局_录制统计 为 空。

录制统计_初始化 接受 零参：
    若 全局_录制统计 等于 空：
        全局_录制统计 为 新建 录制统计。
        全局_录制统计 的 总录制次数 为 0。
        全局_录制统计 的 总录制时长_ms 为 0。
        全局_录制统计 的 总字节数 为 0。
        全局_录制统计 的 总数据块数 为 0。
        全局_录制统计 的 总帧数 为 0。
        全局_录制统计 的 总丢帧数 为 0。
        全局_录制统计 的 暂停次数 为 0。
        全局_录制统计 的 错误次数 为 0。
    完。
完。

媒体Recorder_创建 接受 stream、mimeType：
    做 录制统计_初始化。
    mr 为 做 堆_分配 用 媒体Recorder。
    mr 的 stream 为 stream。
    mr 的 mimeType 为 mimeType。
    mr 的 状态 为 录制_非活动。
    mr 的 videoBitsPerSecond 为 默认_视频码率。
    mr 的 audioBitsPerSecond 为 默认_音频码率。
    mr 的 数据块 为 0。
    mr 的 时间切片 为 默认_时间切片。
    mr 的 开始时间 为 0。
    mr 的 暂停时间 为 0。
    mr 的 总暂停时长 为 0。
    mr 的 总帧数 为 0。
    mr 的 丢帧数 为 0。
    mr 的 数据块计数 为 0。
    mr 的 总字节数 为 0。
    mr 的 ondataavailable 为 0。
    mr 的 onstop 为 0。
    mr 的 onerror 为 0。
    mr 的 onstart 为 0。
    mr 的 onpause 为 0。
    mr 的 onresume 为 0。
    返还 mr。
完。

媒体Recorder_创建_配置 接受 stream、mimeType、视频码率、音频码率、切片ms：
    mr 为 做 媒体Recorder_创建 用 stream、mimeType。
    若 视频码率 大于 0：
        mr 的 videoBitsPerSecond 为 视频码率。
    完。
    若 音频码率 大于 0：
        mr 的 audioBitsPerSecond 为 音频码率。
    完。
    若 切片ms 大于 0：
        mr 的 时间切片 为 切片ms。
    完。
    返还 mr。
完。

媒体Recorder_start 接受 mr、timeslice：
    做 录制统计_初始化。
    若 mr 等于 0：
        返还。
    完。
    若 mr 的 状态 等于 录制_录制中：
        做 媒体Recorder_触发错误 用 mr、「已在录制中」。
        返还。
    完。
    若 mr 的 stream 等于 0：
        做 媒体Recorder_触发错误 用 mr、「无效的媒体流」。
        返还。
    完。
    若 timeslice 大于 0：
        mr 的 时间切片 为 timeslice。
    完。
    mr 的 状态 为 录制_录制中。
    mr 的 开始时间 为 做 获取滴答。
    mr 的 总暂停时长 为 0。
    mr 的 总帧数 为 0。
    mr 的 丢帧数 为 0。
    mr 的 数据块计数 为 0。
    mr 的 总字节数 为 0。
    全局_录制统计 的 总录制次数 为 全局_录制统计 的 总录制次数 加 1。
    若 mr 的 onstart 不等于 0：
        做 调用回调 用 mr 的 onstart、mr。
    完。
完。

媒体Recorder_stop 接受 mr：
    做 录制统计_初始化。
    若 mr 等于 0 或者 mr 的 状态 等于 录制_非活动：
        返还。
    完。
    若 mr 的 状态 等于 录制_已暂停：
        mr 的 总暂停时长 为 mr 的 总暂停时长 加 (做 获取滴答 减 mr 的 暂停时间)。
    完。
    录制时长 为 (做 获取滴答 减 mr 的 开始时间 减 mr 的 总暂停时长) 除以 1000。
    全局_录制统计 的 总录制时长_ms 为 全局_录制统计 的 总录制时长_ms 加 录制时长。
    全局_录制统计 的 总字节数 为 全局_录制统计 的 总字节数 加 mr 的 总字节数。
    全局_录制统计 的 总帧数 为 全局_录制统计 的 总帧数 加 mr 的 总帧数。
    全局_录制统计 的 总丢帧数 为 全局_录制统计 的 总丢帧数 加 mr 的 丢帧数。
    做 媒体Recorder_刷新数据块 用 mr。
    mr 的 状态 为 录制_非活动。
    若 mr 的 onstop 不等于 0：
        做 调用回调 用 mr 的 onstop、mr。
    完。
完。

媒体Recorder_pause 接受 mr：
    做 录制统计_初始化。
    若 mr 等于 0 或者 mr 的 状态 不等于 录制_录制中：
        返还。
    完。
    mr 的 状态 为 录制_已暂停。
    mr 的 暂停时间 为 做 获取滴答。
    全局_录制统计 的 暂停次数 为 全局_录制统计 的 暂停次数 加 1。
    若 mr 的 onpause 不等于 0：
        做 调用回调 用 mr 的 onpause、mr。
    完。
完。

媒体Recorder_resume 接受 mr：
    若 mr 等于 0 或者 mr 的 状态 不等于 录制_已暂停：
        返还。
    完。
    mr 的 总暂停时长 为 mr 的 总暂停时长 加 (做 获取滴答 减 mr 的 暂停时间)。
    mr 的 状态 为 录制_录制中。
    若 mr 的 onresume 不等于 0：
        做 调用回调 用 mr 的 onresume、mr。
    完。
完。

媒体Recorder_requestData 接受 mr：
    若 mr 等于 0 或者 mr 的 状态 不等于 录制_录制中：
        返还。
    完。
    做 媒体Recorder_刷新数据块 用 mr。
完。

媒体Recorder_刷新数据块 接受 mr：
    若 mr 的 数据块计数 等于 0：
        返还。
    完。
    若 mr 的 ondataavailable 不等于 0：
        blob 为 做 媒体Recorder_组装Blob 用 mr。
        做 调用回调 用 mr 的 ondataavailable、blob。
        全局_录制统计 的 总数据块数 为 全局_录制统计 的 总数据块数 加 1。
    完。
完。

媒体Recorder_添加帧 接受 mr、帧数据、帧大小、帧类型：
    若 mr 等于 0 或者 mr 的 状态 不等于 录制_录制中：
        返还。
    完。
    若 mr 的 数据块计数 大于或等于 最大数据块数：
        mr 的 丢帧数 为 mr 的 丢帧数 加 1。
        返还。
    完。
    块 为 新建 媒体数据块。
    块 的 数据 为 帧数据。
    块 的 大小 为 帧大小。
    块 的 时间戳 为 做 获取滴答 减 mr 的 开始时间 减 mr 的 总暂停时长。
    块 的 类型 为 帧类型。
    块 的 下一个 为 mr 的 数据块。
    mr 的 数据块 为 块。
    mr 的 数据块计数 为 mr 的 数据块计数 加 1。
    mr 的 总帧数 为 mr 的 总帧数 加 1。
    mr 的 总字节数 为 mr 的 总字节数 加 帧大小。
完。

媒体Recorder_组装Blob 接受 mr：
    可变 总大小 为 0。
    可变 块 为 mr 的 数据块。
    只要 块 不等于 0：
        总大小 为 总大小 加 块 的 大小。
        块 为 块 的 下一个。
    完。
    返还 一组：mr 的 mimeType、总大小、mr 的 数据块计数。
完。

媒体Recorder_获取录制时长 接受 mr：
    若 mr 等于 0 或者 mr 的 开始时间 等于 0：
        返还 0。
    完。
    当前 为 做 获取滴答。
    可变 暂停 为 mr 的 总暂停时长。
    若 mr 的 状态 等于 录制_已暂停：
        暂停 为 暂停 加 (当前 减 mr 的 暂停时间)。
    完。
    返还 (当前 减 mr 的 开始时间 减 暂停) 除以 1000。
完。

媒体Recorder_触发错误 接受 mr、消息：
    做 录制统计_初始化。
    全局_录制统计 的 错误次数 为 全局_录制统计 的 错误次数 加 1。
    若 mr 的 onerror 不等于 0：
        做 调用回调 用 mr 的 onerror、消息。
    完。
完。

媒体Recorder_isTypeSupported 接受 mimeType：
    若 mimeType 等于 「video/webm」：
        返还 真。
    完。
    若 mimeType 等于 「video/webm; codecs=vp9」：
        返还 真。
    完。
    若 mimeType 等于 「video/webm; codecs=vp9,opus」：
        返还 真。
    完。
    若 mimeType 等于 「video/webm; codecs=vp8」：
        返还 真。
    完。
    若 mimeType 等于 「audio/webm」：
        返还 真。
    完。
    若 mimeType 等于 「audio/webm; codecs=opus」：
        返还 真。
    完。
    返还 假。
完。

媒体Recorder_销毁 接受 mr：
    若 mr 等于 0：
        返还。
    完。
    若 mr 的 状态 不等于 录制_非活动：
        做 媒体Recorder_stop 用 mr。
    完。
    mr 的 数据块 为 0。
完。

智能_RTC_自适应码率 接受 pc：
    若 pc 等于 0：
        返还。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    rtt 为 做 RTC_获取RTT 用 pc。
    丢包率 为 做 RTC_获取丢包率 用 pc。
    带宽 为 做 RTC_获取带宽估计 用 pc。
    若 rtt 小于 50 并且 丢包率 小于 1：
        做 RTC_设置视频码率 用 pc、4000000。
        做 RTC_设置分辨率 用 pc、1920、1080。
    否则若 rtt 小于 150 并且 丢包率 小于 5：
        做 RTC_设置视频码率 用 pc、2500000。
        做 RTC_设置分辨率 用 pc、1280、720。
    否则若 rtt 小于 300：
        做 RTC_设置视频码率 用 pc、1000000。
        做 RTC_设置分辨率 用 pc、640、480。
        做 RTC_启用FEC 用 pc、真。
    否则：
        做 RTC_设置视频码率 用 pc、500000。
        做 RTC_设置分辨率 用 pc、320、240。
        做 RTC_启用FEC 用 pc、真。
    完。
完。

智能_RTC_回声消除 接受 音频帧、参考帧：
    若 音频帧 等于 0：
        返还 音频帧。
    完。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    帧长 为 做 计数 用 音频帧。
    只要 i 小于 帧长：
        可变 参考值 为 0。
        若 参考帧 不等于 0 并且 i 小于 做 计数 用 参考帧：
            参考值 为 参考帧 的 第 i 项。
        完。
        差值 为 音频帧 的 第 i 项 减 参考值 乘 7 除以 10。
        做 追加 用 输出、差值。
        i 为 i 加 1。
    完。
    返还 输出。
完。

智能_RTC_降噪 接受 音频帧：
    若 音频帧 等于 0 或者 做 计数 用 音频帧 等于 0：
        返还 音频帧。
    完。
    可变 输出 为 一组：（空）。
    帧长 为 做 计数 用 音频帧。
    可变 i 为 0。
    只要 i 小于 帧长：
        可变 sum 为 0。
        可变 count 为 0。
        可变 j 为 i 减 2。
        只要 j 小于或等于 i 加 2：
            若 j 大于或等于 0 并且 j 小于 帧长：
                sum 为 sum 加 音频帧 的 第 j 项。
                count 为 count 加 1。
            完。
            j 为 j 加 1。
        完。
        若 count 大于 0：
            做 追加 用 输出、sum 除以 count。
        否则：
            做 追加 用 输出、音频帧 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 输出。
完。

智能_RTC_自动增益 接受 音频帧、目标电平：
    若 音频帧 等于 0 或者 做 计数 用 音频帧 等于 0：
        返还 音频帧。
    完。
    可变 峰值 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 音频帧：
        绝对值 为 音频帧 的 第 i 项。
        若 绝对值 小于 0：
            绝对值 为 0 减 绝对值。
        完。
        若 绝对值 大于 峰值：
            峰值 为 绝对值。
        完。
        i 为 i 加 1。
    完。
    若 峰值 等于 0：
        返还 音频帧。
    完。
    增益 为 目标电平 乘 100 除以 峰值。
    可变 输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 音频帧：
        做 追加 用 输出、音频帧 的 第 i 项 乘 增益 除以 100。
        i 为 i 加 1。
    完。
    返还 输出。
完。

RTC_获取RTT 接受 pc：
    返还 50。
完。

RTC_获取丢包率 接受 pc：
    若 pc 等于 空：
        返还 0。
    完。
    发包 为 pc 的 发包总数。
    丢包 为 pc 的 丢包总数。
    若 发包 加 丢包 等于 0：
        返还 0。
    完。
    返还 丢包 乘 100 除以 (发包 加 丢包)。
完。

RTC_获取带宽估计 接受 pc：
    返还 5000000。
完。

RTC_设置视频码率 接受 pc、码率：
    若 pc 等于 空：
        返还。
    完。
    pc 的 视频码率 为 码率。
    pc 的 需要重协商 为 真。
完。

RTC_设置分辨率 接受 pc、宽、高：
    若 pc 等于 空：
        返还。
    完。
    pc 的 视频宽 为 宽。
    pc 的 视频高 为 高。
    pc 的 需要重协商 为 真。
完。

RTC_启用FEC 接受 pc、启用：
    若 pc 等于 空：
        返还。
    完。
    pc 的 FEC启用 为 启用。
    pc 的 需要重协商 为 真。
完。

录制_打印统计 接受 零参：
    做 录制统计_初始化。
    做 打印行 用 「=== 媒体录制统计 ===」。
    做 打印行 用 「录制次数: 」 加 做 整数转字符串 用 全局_录制统计 的 总录制次数。
    做 打印行 用 「总时长: 」 加 做 整数转字符串 用 全局_录制统计 的 总录制时长_ms 除以 1000 加 「 秒」。
    做 打印行 用 「总字节: 」 加 做 整数转字符串 用 全局_录制统计 的 总字节数。
    做 打印行 用 「数据块: 」 加 做 整数转字符串 用 全局_录制统计 的 总数据块数。
    做 打印行 用 「总帧数: 」 加 做 整数转字符串 用 全局_录制统计 的 总帧数。
    做 打印行 用 「丢帧: 」 加 做 整数转字符串 用 全局_录制统计 的 总丢帧数。
    做 打印行 用 「暂停: 」 加 做 整数转字符串 用 全局_录制统计 的 暂停次数。
    做 打印行 用 「错误: 」 加 做 整数转字符串 用 全局_录制统计 的 错误次数。
完。

媒体Recorder_质量评估 接受 mr：
    若 mr 等于 0：
        返还 0。
    完。
    可变 得分 为 1000。
    若 mr 的 总帧数 大于 0：
        丢帧率 为 mr 的 丢帧数 乘 1000 除以 mr 的 总帧数。
        得分 为 得分 减 丢帧率。
    完。
    若 mr 的 数据块计数 等于 0 并且 mr 的 状态 等于 录制_录制中：
        得分 为 得分 减 200。
    完。
    若 得分 小于 0：
        得分 为 0。
    完。
    返还 得分。
完。

媒体Recorder_格式评估 接受 mimeType：
    若 mimeType 等于 「video/webm; codecs=vp9,opus」：
        返还 1000。
    完。
    若 mimeType 等于 「video/webm; codecs=vp9」：
        返还 900。
    完。
    若 mimeType 等于 「video/webm; codecs=vp8」：
        返还 800。
    完。
    若 mimeType 等于 「video/webm」：
        返还 700。
    完。
    若 mimeType 等于 「audio/webm; codecs=opus」：
        返还 850。
    完。
    若 mimeType 等于 「audio/webm」：
        返还 600。
    完。
    返还 0。
完。

媒体Recorder_获取码率建议 接受 mimeType：
    若 mimeType 等于 「video/webm; codecs=vp9,opus」：
        返还 2500000。
    完。
    若 mimeType 等于 「video/webm; codecs=vp9」：
        返还 2000000。
    完。
    若 mimeType 等于 「video/webm; codecs=vp8」：
        返还 1500000。
    完。
    若 mimeType 等于 「video/webm」：
        返还 1000000。
    完。
    若 mimeType 等于 「audio/webm; codecs=opus」：
        返还 128000。
    完。
    若 mimeType 等于 「audio/webm」：
        返还 96000。
    完。
    返还 500000。
完。

录制_重置统计 接受 零参：
    做 录制统计_初始化。
    全局_录制统计 的 总录制次数 为 0。
    全局_录制统计 的 总录制时长_ms 为 0。
    全局_录制统计 的 总字节数 为 0。
    全局_录制统计 的 总数据块数 为 0。
    全局_录制统计 的 总帧数 为 0。
    全局_录制统计 的 总丢帧数 为 0。
    全局_录制统计 的 暂停次数 为 0。
    全局_录制统计 的 错误次数 为 0。
完。

录制_获取总次数 接受 零参：
    返还 全局_录制统计 的 总录制次数。
完。

录制_获取总字节 接受 零参：
    返还 全局_录制统计 的 总字节数。
完。

录制_获取总帧数 接受 零参：
    返还 全局_录制统计 的 总帧数。
完。

录制_获取丢帧数 接受 零参：
    返还 全局_录制统计 的 总丢帧数。
完。

录制_获取错误数 接受 零参：
    返还 全局_录制统计 的 错误次数。
完。

录制_获取暂停数 接受 零参：
    返还 全局_录制统计 的 暂停次数。
完。

录制_完整统计 接受 零参：
    做 录制统计_初始化。
    返还 一组：全局_录制统计 的 总录制次数、全局_录制统计 的 总录制时长_ms、全局_录制统计 的 总字节数、全局_录制统计 的 总数据块数、全局_录制统计 的 总帧数、全局_录制统计 的 总丢帧数、全局_录制统计 的 暂停次数、全局_录制统计 的 错误次数。
完。

媒体Recorder_详细诊断 接受 mr：
    若 mr 等于 0：
        返还。
    完。
    做 打印行 用 「=== 媒体录制器诊断 ===」。
    做 打印行 用 「状态: 」 加 做 整数转字符串 用 mr 的 状态。
    做 打印行 用 「类型: 」 加 mr 的 mimeType。
    做 打印行 用 「数据块: 」 加 做 整数转字符串 用 mr 的 数据块计数。
    时长 为 做 媒体Recorder_获取录制时长 用 mr。
    做 打印行 用 「录制时长: 」 加 做 整数转字符串 用 时长 加 「 秒」。
    质量 为 做 媒体Recorder_质量评估 用 mr。
    做 打印行 用 「质量评分: 」 加 做 整数转字符串 用 质量 加 「/1000」。
    格式分 为 做 媒体Recorder_格式评估 用 mr 的 mimeType。
    做 打印行 用 「格式评分: 」 加 做 整数转字符串 用 格式分 加 「/1000」。
    建议码率 为 做 媒体Recorder_获取码率建议 用 mr 的 mimeType。
    做 打印行 用 「建议码率: 」 加 做 整数转字符串 用 建议码率 加 「 bps」。
完。

智能_RTC_全诊断 接受 pc：
    若 pc 等于 0：
        返还。
    完。
    rtt 为 做 RTC_获取RTT 用 pc。
    丢包 为 做 RTC_获取丢包率 用 pc。
    带宽 为 做 RTC_获取带宽估计 用 pc。
    做 打印行 用 「=== RTC 诊断 ===」。
    做 打印行 用 「RTT: 」 加 做 整数转字符串 用 rtt 加 「 ms」。
    做 打印行 用 「丢包率: 」 加 做 整数转字符串 用 丢包 加 「%」。
    做 打印行 用 「带宽: 」 加 做 整数转字符串 用 带宽 加 「 bps」。
    若 rtt 小于 50：
        做 打印行 用 「网络质量: 优秀」。
    否则若 rtt 小于 150：
        做 打印行 用 「网络质量: 良好」。
    否则若 rtt 小于 300：
        做 打印行 用 「网络质量: 一般」。
    否则：
        做 打印行 用 「网络质量: 差」。
    完。
完。

录制_平均帧率 接受 零参：
    做 录制统计_初始化。
    若 全局_录制统计 的 总录制时长_ms 等于 0：
        返还 0。
    完。
    返还 全局_录制统计 的 总帧数 乘 1000 除以 全局_录制统计 的 总录制时长_ms。
完。

智能_RTC_带宽估算 接受 历史RTT、历史丢包率：
    可变 估算 为 5000000。
    若 历史RTT 大于 200：
        估算 为 估算 乘 500 除以 1000。
    否则若 历史RTT 大于 100：
        估算 为 估算 乘 750 除以 1000。
    完。
    若 历史丢包率 大于 10：
        估算 为 估算 乘 (100 减 历史丢包率) 除以 100。
    完。
    若 估算 小于 300000：
        估算 为 300000。
    完。
    返还 估算。
完。

智能_RTC_音视频同步 接受 音频时戳、视频时戳：
    差值 为 音频时戳 减 视频时戳。
    若 差值 小于 0：
        差值 为 0 减 差值。
    完。
    若 差值 大于 40：
        返还 假。
    完。
    返还 真。
完。

智能_RTC_丢帧率 接受 总帧、丢帧：
    若 总帧 等于 0：
        返还 0。
    完。
    返还 丢帧 乘 1000 除以 总帧。
完。

录制_平均码率 接受 零参：
    做 录制统计_初始化。
    若 全局_录制统计 的 总录制时长_ms 等于 0：
        返还 0。
    完。
    返还 全局_录制统计 的 总字节数 乘 8000 除以 全局_录制统计 的 总录制时长_ms。
完。

可变 _谐振码率_当前 为 2000000。

可变 _谐振码率_调整次数 为 0。

谐振码率_自适应 接受 CPU负载、剩余空间MB：
    若 CPU负载 大于 800 或者 剩余空间MB 小于 100：
        _谐振码率_当前 为 500000。
    完。
    做 否则如果 用 CPU负载 大于 500 或者 剩余空间MB 小于 500。
    块：
        _谐振码率_当前 为 1000000。
    完。
    否则。
    块：
        _谐振码率_当前 为 2000000。
    完。
    _谐振码率_调整次数 为 _谐振码率_调整次数 加 1。
    返还 _谐振码率_当前。
完。

谐振码率_获取 接受 零参：
    返还 _谐振码率_当前。
完。

媒体记录_完整统计 接受 零参：
    返还 一组：「谐振当前码率」、_谐振码率_当前、「谐振调整次数」、_谐振码率_调整次数。
完。

谐振码率_是否低质量 接受 零参：
    返还 _谐振码率_当前 小于 800000。
完。

本章 收束。
