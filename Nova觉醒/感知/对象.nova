《对象》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·样式》。

引用《.·视图过渡快照》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·辅助存根》。

引用《.·创建过渡动》。

ViewTransition 是 一种 事物，含 状态、更新回调、旧快照列表、新快照列表、快照数量、快照容量、参与元素映射、参与数量、ready_promise、updateCallbackDone、finished_promise、动画列表、动画数量、动画持续、动画缓动。

ViewTransition_创建 接受 更新回调：
    vt 为 做 堆_分配 用 ViewTransition。
    vt 的 状态 为 VT_STATE_IDLE。
    vt 的 更新回调 为 更新回调。
    vt 的 快照容量 为 32。
    vt 的 旧快照列表 为 做 堆_分配_数组 用 32 乘 8。
    vt 的 新快照列表 为 做 堆_分配_数组 用 32 乘 8。
    vt 的 快照数量 为 0。
    vt 的 参与元素映射 为 做 堆_分配_数组 用 32 乘 16。
    vt 的 参与数量 为 0。
    vt 的 动画列表 为 做 堆_分配_数组 用 32 乘 8。
    vt 的 动画数量 为 0。
    vt 的 动画持续 为 250。
    返还 vt。
完。

Document_startViewTransition 接受 Nova运行时实例、文档、更新回调：
    vt 为 做 ViewTransition_创建 用 更新回调。
    做 VT_收集参与元素 用 vt、文档。
    vt 的 状态 为 VT_STATE_CAPTURING。
    做 VT_捕获快照 用 vt、假。
    vt 的 状态 为 VT_STATE_UPDATING。
    若 更新回调 不等于 0：
        做 Nova函数_Call_简 用 Nova运行时实例、更新回调。
    完。
    做 VT_捕获快照 用 vt、真。
    vt 的 状态 为 VT_STATE_ANIMATING。
    做 VT_创建动画 用 vt。
    返还 vt。
完。

VT_收集参与元素 接受 vt、文档：
    若 vt 等于 0 或者 文档 等于 0：
        返还。
    完。
    做 VT_收集递归 用 vt、文档 的 根元素。
完。

VT_收集递归 接受 vt、元素：
    若 元素 等于 0：
        返还。
    完。
    布局 为 元素 的 布局对象。
    若 布局 不等于 0 并且 布局 的 计算样式 不等于 0：
        名称 为 布局 的 计算样式 的 view_transition_name。
        若 名称 不等于 0 并且 非 做 字符串_相等 用 名称、「none」：
            若 vt 的 参与数量 小于 32：
                偏移 为 vt 的 参与元素映射 加 vt 的 参与数量 乘 16。
                做 内存_写64 用 偏移、做 字符串_复制 用 名称。
                做 内存_写64 用 偏移 加 8、元素。
                vt 的 参与数量 为 vt 的 参与数量 加 1。
            完。
        完。
    完。
    可变 子 为 元素 的 第一子节点。
    只要 子 不等于 0：
        若 子 的 节点类型 等于 1：
            做 VT_收集递归 用 vt、子。
        完。
        子 为 子 的 后一兄弟。
    完。
完。

VT_捕获快照 接受 vt、是新状态：
    若 vt 等于 0：
        返还。
    完。
    目标 为 若 是新状态 则 vt 的 新快照列表 否则 vt 的 旧快照列表。
    可变 i 为 0。
    只要 i 小于 vt 的 参与数量：
        偏移 为 vt 的 参与元素映射 加 i 乘 16。
        名称 为 做 内存_读64 用 偏移。
        元素 为 做 内存_读64 用 偏移 加 8。
        snap 为 做 VTSnapshot_创建 用 名称、元素。
        做 内存_写64 用 目标 加 i 乘 8、snap。
        i 为 i 加 1。
    完。
    vt 的 快照数量 为 vt 的 参与数量。
完。

本章 收束。
