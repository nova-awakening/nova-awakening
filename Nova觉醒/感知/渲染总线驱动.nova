《渲染总线驱动》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·推理·AI总线初始化》。

引用《..·推理·定点Q算术》。

引用《.·创建销毁》。

引用《..·推理·AI鼠标事件处理》。

引用《..·执行·IO多路复用》。

引用《..·中枢·内核打印》。

引用《..·稳态·图处统一操作》。

引用《..·中枢·整数转字符串》。

引用《..·推理·模型持久化》。

引用《.·从布局树构》。

Q10 为 1024。

Q_HALF 为 512。

渲染级别_跳过 为 0。

渲染级别_低 为 1。

渲染级别_中 为 2。

渲染级别_高 为 3。

渲染级别_极高 为 4。

语义_正文 为 0。

语义_标题 为 1。

语义_导航 为 2。

语义_广告 为 3。

语义_表单 为 4。

语义_媒体 为 5。

语义_装饰 为 6。

语义_脚本 为 11。

事件_鼠标移动 为 1。

事件_点击 为 2。

事件_滚动 为 3。

事件_键盘 为 4。

事件_悬停 为 5。

JND_强度基线 为 50。

JND_运动倍率 为 3。

注视半径 为 200。

高斯_σ平方_2 为 80000。

最大层深度 为 32。

统计_环大小 为 64。

渲染统计 是 一种 事物，含 总层数、跳过层数、低质量层数、中质量层数、高质量层数、总绘制时间_us。

注视点状态 是 一种 事物，含 x、y、运动速度、上次更新。

帧级诊断 是 一种 事物，含 环、写索引、总帧数、总绘制时间。

可变 全局_注视点 为 空。

可变 全局_渲染统计 为 空。

可变 全局_帧诊断 为 空。

注视点_初始化 接受 零参：
    若 全局_注视点 不等于 空：
        返还。
    完。
    f 为 新建 注视点状态。
    f 的 x 为 0。
    f 的 y 为 0。
    f 的 运动速度 为 0。
    f 的 上次更新 为 0。
    全局_注视点 为 f。
完。

注视点_更新 接受 新x、新y：
    若 全局_注视点 等于 空：
        做 注视点_初始化。
    完。
    f 为 全局_注视点。
    dx 为 新x 减 f 的 x。
    dy 为 新y 减 f 的 y。
    f 的 运动速度 为 做 平方根_近似 用 dx 乘 dx 加 dy 乘 dy。
    f 的 x 为 新x。
    f 的 y 为 新y。
    f 的 上次更新 为 做 平台_获取时间_ms。
完。

注视点_计算衰减 接受 区域x、区域y、区域w、区域h：
    若 全局_注视点 等于 空：
        返还 Q10。
    完。
    cx 为 区域x 加 区域w 除以 2。
    cy 为 区域y 加 区域h 除以 2。
    dx 为 cx 减 全局_注视点 的 x。
    dy 为 cy 减 全局_注视点 的 y。
    d2 为 dx 乘 dx 加 dy 乘 dy。
    衰减 为 Q10 乘 高斯_σ平方_2 除以 (高斯_σ平方_2 加 d2)。
    返还 衰减。
完。

平方根_近似 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 g 为 x 除以 2。
    若 g 等于 0：
        g 为 1。
    完。
    可变 i 为 0。
    只要 i 小于 8：
        g 为 (g 加 x 除以 g) 除以 2。
        i 为 i 加 1。
    完。
    返还 g。
完。

JND_计算 接受 区域x、区域y、区域w、区域h、运动中：
    衰减 为 做 注视点_计算衰减 用 区域x、区域y、区域w、区域h。
    距离补偿 为 JND_强度基线 乘 (Q10 减 衰减) 除以 Q10。
    可变 jnd 为 JND_强度基线 加 距离补偿。
    若 运动中：
        jnd 为 jnd 乘 JND_运动倍率。
    完。
    返还 jnd。
完。

语义_获取重要性 接受 角色：
    若 角色 等于 语义_正文：
        返还 900。
    完。
    若 角色 等于 语义_标题：
        返还 950。
    完。
    若 角色 等于 语义_媒体：
        返还 850。
    完。
    若 角色 等于 语义_表单：
        返还 750。
    完。
    若 角色 等于 语义_导航：
        返还 700。
    完。
    若 角色 等于 语义_装饰：
        返还 400。
    完。
    若 角色 等于 语义_广告：
        返还 100。
    完。
    若 角色 等于 语义_脚本：
        返还 0。
    完。
    返还 Q_HALF。
完。

质量_计算渲染级别 接受 语义重要性、衰减、是否可见、jnd：
    若 是否可见 等于 假：
        返还 渲染级别_跳过。
    完。
    若 语义重要性 小于或等于 0：
        返还 渲染级别_跳过。
    完。
    分数 为 语义重要性 乘 衰减 除以 Q10。
    若 jnd 大于 100：
        分数 为 分数 乘 80 除以 100。
    完。
    若 jnd 大于 200：
        分数 为 分数 乘 60 除以 100。
    完。
    若 分数 大于或等于 800：
        返还 渲染级别_极高。
    完。
    若 分数 大于或等于 500：
        返还 渲染级别_高。
    完。
    若 分数 大于或等于 250：
        返还 渲染级别_中。
    完。
    若 分数 大于或等于 50：
        返还 渲染级别_低。
    完。
    返还 渲染级别_跳过。
完。

AI渲染_粗略绘制 接受 层、显示列表、偏移x、偏移y：
    若 层 等于 0：
        返还。
    完。
    lo 为 层 的 布局对象。
    若 lo 等于 0：
        返还。
    完。
    背景色 为 做 布局Object_获取背景色 用 lo。
    若 背景色 等于 0：
        背景色 为 0xFFFFFF。
    完。
    x 为 lo 的 绝对x 加 偏移x。
    y 为 lo 的 绝对y 加 偏移y。
    w 为 做 布局Object_边框盒宽 用 lo。
    h 为 做 布局Object_边框盒高 用 lo。
    做 图处_填充矩形 用 层 的 渲染上下文、x、y、w、h、背景色。
完。

AI渲染_统计子层数量 接受 层：
    若 层 等于 0：
        返还 0。
    完。
    可变 数量 为 0。
    可变 子 为 层 的 第一子层。
    只要 子 不等于 0：
        数量 为 数量 加 1。
        子 为 子 的 后兄层。
    完。
    返还 数量。
完。

AI渲染_提取页面结构特征 接受 bus、层、视口上、视口高、语义重要性、衰减、是否可见、jnd、运动中、深度：
    若 bus 等于 0 或者 层 等于 0 或者 层 的 布局对象 等于 0：
        返还 0。
    完。
    lo 为 层 的 布局对象。
    可变 节点 为 0。
    若 lo 的 类型 不等于 布局类型_视图：
        节点 为 lo 的 DOM节点。
    完。
    特征 为 做 Tensor_创建 用 1、24。
    可变 归一宽 为 bus 的 视口宽。
    可变 归一高 为 视口高。
    若 归一宽 小于或等于 0：
        归一宽 为 1。
    完。
    若 归一高 小于或等于 0：
        归一高 为 1。
    完。
    区域x 为 lo 的 绝对x。
    区域y 为 lo 的 绝对y。
    区域w 为 做 布局Object_边框盒宽 用 lo。
    区域h 为 做 布局Object_边框盒高 用 lo。
    中心x 为 区域x 加 区域w 除以 2。
    中心y 为 区域y 加 区域h 除以 2。
    可变 视口面积 为 归一宽 乘 归一高。
    可变 区域高归一 为 区域h。
    子层数 为 做 AI渲染_统计子层数量 用 层。
    若 视口面积 小于或等于 0：
        视口面积 为 1。
    完。
    若 区域高归一 小于或等于 0：
        区域高归一 为 1。
    完。
    做 Tensor_写 用 特征、0、0、区域x 乘 Q10 除以 归一宽。
    做 Tensor_写 用 特征、0、1、(区域y 减 视口上) 乘 Q10 除以 归一高。
    做 Tensor_写 用 特征、0、2、区域w 乘 Q10 除以 归一宽。
    做 Tensor_写 用 特征、0、3、区域h 乘 Q10 除以 归一高。
    做 Tensor_写 用 特征、0、4、中心x 乘 Q10 除以 归一宽。
    做 Tensor_写 用 特征、0、5、(中心y 减 视口上) 乘 Q10 除以 归一高。
    做 Tensor_写 用 特征、0、6、区域w 乘 区域h 乘 Q10 除以 视口面积。
    做 Tensor_写 用 特征、0、7、区域w 乘 Q10 除以 区域高归一。
    做 Tensor_写 用 特征、0、8、若 是否可见 则 Q10 否则 0。
    做 Tensor_写 用 特征、0、9、语义重要性。
    做 Tensor_写 用 特征、0、10、衰减。
    做 Tensor_写 用 特征、0、11、jnd 乘 Q10 除以 256。
    做 Tensor_写 用 特征、0、12、若 深度 大于 最大层深度 则 Q10 否则 深度 乘 Q10 除以 最大层深度。
    做 Tensor_写 用 特征、0、13、若 子层数 大于 8 则 Q10 否则 子层数 乘 Q10 除以 8。
    做 Tensor_写 用 特征、0、14、若 lo 的 父 不等于 0 则 Q10 否则 0。
    做 Tensor_写 用 特征、0、15、lo 的 类型 乘 Q10 除以 8。
    做 Tensor_写 用 特征、0、16、lo 的 内容宽 乘 Q10 除以 归一宽。
    做 Tensor_写 用 特征、0、17、lo 的 内容高 乘 Q10 除以 归一高。
    做 Tensor_写 用 特征、0、18、若 节点 不等于 0 则 节点 的 注意力权重 乘 Q10 除以 10000 否则 0。
    做 Tensor_写 用 特征、0、19、若 节点 不等于 0 则 节点 的 视口可见性 乘 Q_HALF 否则 0。
    做 Tensor_写 用 特征、0、20、若 lo 的 累计布局次数 大于 16 则 Q10 否则 lo 的 累计布局次数 乘 Q10 除以 16。
    做 Tensor_写 用 特征、0、21、lo 的 预测置信度 乘 Q10 除以 10000。
    做 Tensor_写 用 特征、0、22、若 运动中 则 Q10 否则 0。
    做 Tensor_写 用 特征、0、23、若 层 的 渲染精度 大于 3 则 Q10 否则 层 的 渲染精度 乘 Q10 除以 3。
    返还 特征。
完。

AI渲染_页面结构重要性 接受 bus、层、视口上、视口高、位置、深度、语义重要性、衰减、是否可见、jnd、运动中：
    特征 为 做 AI渲染_提取页面结构特征 用 bus、层、视口上、视口高、语义重要性、衰减、是否可见、jnd、运动中、深度。
    若 特征 等于 0：
        返还 语义重要性。
    完。
    上下文 为 做 AI总线_页面结构增量推理 用 bus、特征、位置。
    做 Tensor_释放 用 特征。
    若 上下文 等于 0：
        返还 语义重要性。
    完。
    可变 累加 为 0。
    可变 i 为 0。
    只要 i 小于 24：
        累加 为 累加 加 做 Tensor_读 用 上下文、0、i。
        i 为 i 加 1。
    完。
    做 Tensor_释放 用 上下文。
    可变 结构重要性 为 累加 除以 24。
    若 结构重要性 小于 0：
        结构重要性 为 0。
    完。
    若 结构重要性 大于 Q10：
        结构重要性 为 Q10。
    完。
    返还 (语义重要性 乘 3 加 结构重要性) 除以 4。
完。

AI渲染_总线驱动绘制_递归 接受 bus、层、视口上、视口高、位置、深度：
    若 bus 等于 0 或者 层 等于 0：
        返还 位置。
    完。
    视口下 为 视口上 加 视口高。
    lo 为 层 的 布局对象。
    若 lo 等于 0：
        返还 位置。
    完。
    可变 节点 为 0。
    若 lo 的 类型 不等于 布局类型_视图：
        节点 为 lo 的 DOM节点。
    完。
    区域x 为 lo 的 绝对x。
    区域y 为 lo 的 绝对y。
    区域w 为 做 布局Object_边框盒宽 用 lo。
    区域h 为 做 布局Object_边框盒高 用 lo。
    是否可见 为 区域y 加 区域h 大于 视口上 并且 区域y 小于 视口下。
    可变 语义重要性 为 Q_HALF。
    若 节点 不等于 0：
        角色 为 做 AI总线_语义分类 用 bus、节点。
        语义重要性 为 做 语义_获取重要性 用 角色。
    完。
    衰减 为 做 注视点_计算衰减 用 区域x、区域y、区域w、区域h。
    运动中 为 假。
    若 全局_注视点 不等于 空 并且 全局_注视点 的 运动速度 大于 20：
        运动中 为 真。
    完。
    jnd 为 做 JND_计算 用 区域x、区域y、区域w、区域h、运动中。
    若 bus 的 页面结构模型 不等于 0 并且 bus 的 降级模式 小于 3：
        语义重要性 为 做 AI渲染_页面结构重要性 用 bus、层、视口上、视口高、位置、深度、语义重要性、衰减、是否可见、jnd、运动中。
    完。
    渲染级别 为 做 质量_计算渲染级别 用 语义重要性、衰减、是否可见、jnd。
    若 全局_渲染统计 不等于 空：
        全局_渲染统计 的 总层数 为 全局_渲染统计 的 总层数 加 1。
        若 渲染级别 等于 渲染级别_跳过：
            全局_渲染统计 的 跳过层数 为 全局_渲染统计 的 跳过层数 加 1。
        完。
        做 否则如果 用 渲染级别 等于 渲染级别_低。
        块：
            全局_渲染统计 的 低质量层数 为 全局_渲染统计 的 低质量层数 加 1。
        完。
        做 否则如果 用 渲染级别 等于 渲染级别_中。
        块：
            全局_渲染统计 的 中质量层数 为 全局_渲染统计 的 中质量层数 加 1。
        完。
        否则。
        块：
            全局_渲染统计 的 高质量层数 为 全局_渲染统计 的 高质量层数 加 1。
        完。
    完。
    若 渲染级别 等于 渲染级别_跳过：
        层 的 渲染精度 为 0。
    完。
    做 否则如果 用 渲染级别 等于 渲染级别_低。
    块：
        层 的 渲染精度 为 1。
        做 AI渲染_粗略绘制 用 层、层 的 显示列表、0、0。
    完。
    做 否则如果 用 渲染级别 小于或等于 渲染级别_中。
    块：
        层 的 渲染精度 为 2。
        做 绘制Layer_绘制 用 层、层 的 显示列表、0、0。
    完。
    否则。
    块：
        层 的 渲染精度 为 3。
        做 绘制Layer_绘制 用 层、层 的 显示列表、0、0。
    完。
    可变 下一个位置 为 位置 加 1。
    可变 子 为 层 的 第一子层。
    只要 子 不等于 0：
        下一个位置 为 做 AI渲染_总线驱动绘制_递归 用 bus、子、视口上、视口高、下一个位置、深度 加 1。
        子 为 子 的 后兄层。
    完。
    返还 下一个位置。
完。

AI渲染_总线驱动绘制 接受 bus、层、视口上、视口高：
    若 bus 等于 0 或者 层 等于 0：
        返还。
    完。
    若 bus 的 页面结构模型 不等于 0 并且 bus 的 降级模式 小于 3：
        做 AI总线_重置页面结构缓存 用 bus。
    完。
    做 AI渲染_总线驱动绘制_递归 用 bus、层、视口上、视口高、0、0。
完。

AI浏览器_分发事件到AI 接受 事件类型、x、y、时间戳：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    若 事件类型 等于 事件_鼠标移动 或者 事件类型 等于 事件_点击：
        做 注视点_更新 用 x、y。
    完。
    若 事件类型 等于 事件_鼠标移动：
        做 AI总线_鼠标移动 用 bus、x、y。
    完。
    做 否则如果 用 事件类型 等于 事件_点击。
    块：
        做 AI总线_点击 用 bus、x、y。
    完。
    做 否则如果 用 事件类型 等于 事件_滚动。
    块：
        做 AI总线_滚动 用 bus、y、时间戳。
    完。
    做 否则如果 用 事件类型 等于 事件_键盘。
    块：
        做 AI总线_键盘输入 用 bus。
    完。
    做 否则如果 用 事件类型 等于 事件_悬停。
    块：
        做 AI总线_悬停 用 bus、x、y。
    完。
完。

渲染统计_初始化 接受 零参：
    若 全局_渲染统计 不等于 空：
        返还。
    完。
    s 为 新建 渲染统计。
    s 的 总层数 为 0。
    s 的 跳过层数 为 0。
    s 的 低质量层数 为 0。
    s 的 中质量层数 为 0。
    s 的 高质量层数 为 0。
    s 的 总绘制时间_us 为 0。
    全局_渲染统计 为 s。
完。

渲染统计_帧开始 接受 零参：
    若 全局_渲染统计 等于 空：
        做 渲染统计_初始化。
    完。
    s 为 全局_渲染统计。
    s 的 总层数 为 0。
    s 的 跳过层数 为 0。
    s 的 低质量层数 为 0。
    s 的 中质量层数 为 0。
    s 的 高质量层数 为 0。
完。

渲染统计_获取 接受 零参：
    若 全局_渲染统计 等于 空：
        返还 0。
    完。
    返还 一组：全局_渲染统计 的 总层数、全局_渲染统计 的 跳过层数、全局_渲染统计 的 低质量层数、全局_渲染统计 的 中质量层数、全局_渲染统计 的 高质量层数。
完。

帧诊断_初始化 接受 零参：
    若 全局_帧诊断 不等于 空：
        返还。
    完。
    d 为 新建 帧级诊断。
    d 的 环 为 做 堆_分配_数组 用 统计_环大小 乘 4。
    d 的 写索引 为 0。
    d 的 总帧数 为 0。
    d 的 总绘制时间 为 0。
    全局_帧诊断 为 d。
完。

帧诊断_记录 接受 绘制时间_ms：
    若 全局_帧诊断 等于 空：
        做 帧诊断_初始化。
    完。
    d 为 全局_帧诊断。
    idx 为 d 的 写索引 取余于 统计_环大小。
    d 的 环 的 第 idx 项 为 绘制时间_ms。
    d 的 写索引 为 d 的 写索引 加 1。
    d 的 总帧数 为 d 的 总帧数 加 1。
    d 的 总绘制时间 为 d 的 总绘制时间 加 绘制时间_ms。
完。

帧诊断_平均绘制时间 接受 零参：
    若 全局_帧诊断 等于 空 或者 全局_帧诊断 的 总帧数 等于 0：
        返还 0。
    完。
    返还 全局_帧诊断 的 总绘制时间 除以 全局_帧诊断 的 总帧数。
完。

AI浏览器_获取诊断信息 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    返还 做 AI总线_获取诊断 用 bus。
完。

AI浏览器_获取渲染报告 接受 零参：
    可变 报告 为 一组：（空）。
    若 全局_渲染统计 不等于 空：
        报告 为 报告 加 一组：全局_渲染统计 的 总层数、全局_渲染统计 的 跳过层数。
    完。
    若 全局_帧诊断 不等于 空：
        报告 为 报告 加 一组：做 帧诊断_平均绘制时间、全局_帧诊断 的 总帧数。
    完。
    若 全局_注视点 不等于 空：
        报告 为 报告 加 一组：全局_注视点 的 x、全局_注视点 的 y、全局_注视点 的 运动速度。
    完。
    返还 报告。
完。

智能渲染_初始化 接受 零参：
    做 注视点_初始化。
    做 渲染统计_初始化。
    做 帧诊断_初始化。
    做 内核信息 用 「智能渲染总线已初始化」。
    返还 成功。
完。

智能渲染_卸载 接受 零参：
    若 全局_帧诊断 不等于 空：
        做 堆_释放 用 全局_帧诊断 的 环。
        做 堆_释放 用 全局_帧诊断。
        全局_帧诊断 为 空。
    完。
    全局_注视点 为 空。
    全局_渲染统计 为 空。
    返还 成功。
完。

可变 帧目标_ms 为 16。

可变 质量自适应_启用 为 真。

可变 质量自适应_连续超时 为 0。

帧调度_检查 接受 上帧时间_ms：
    若 非 质量自适应_启用：
        返还。
    完。
    若 上帧时间_ms 大于 帧目标_ms：
        质量自适应_连续超时 为 质量自适应_连续超时 加 1。
        若 质量自适应_连续超时 大于或等于 5：
            做 内核信息 用 「智能渲染: 连续超时, 降低质量」。
        完。
    否则：
        若 质量自适应_连续超时 大于 0：
            质量自适应_连续超时 为 质量自适应_连续超时 减 1。
        完。
    完。
完。

帧调度_设置目标 接受 fps：
    若 fps 小于或等于 0：
        fps 为 60。
    完。
    帧目标_ms 为 1000 除以 fps。
完。

帧调度_获取FPS 接受 零参：
    若 全局_帧诊断 等于 空 或者 全局_帧诊断 的 总帧数 等于 0：
        返还 0。
    完。
    平均 为 做 帧诊断_平均绘制时间。
    若 平均 小于或等于 0：
        返还 0。
    完。
    返还 1000 除以 平均。
完。

智能渲染_重置统计 接受 零参：
    若 全局_渲染统计 不等于 空：
        全局_渲染统计 的 总层数 为 0。
        全局_渲染统计 的 跳过层数 为 0。
        全局_渲染统计 的 低质量层数 为 0。
        全局_渲染统计 的 中质量层数 为 0。
        全局_渲染统计 的 高质量层数 为 0。
        全局_渲染统计 的 总绘制时间_us 为 0。
    完。
    若 全局_帧诊断 不等于 空：
        全局_帧诊断 的 总帧数 为 0。
        全局_帧诊断 的 总绘制时间 为 0。
        全局_帧诊断 的 写索引 为 0。
    完。
    质量自适应_连续超时 为 0。
完。

AI渲染_获取状态 接受 零参：
    返还 一组：帧目标_ms、质量自适应_启用、质量自适应_连续超时、做 帧调度_获取FPS。
完。

AI渲染_打印统计 接受 零参：
    做 打印行 用 「=== 智能渲染统计 ===」。
    若 全局_渲染统计 不等于 空：
        做 打印行 用 「总层: 」 加 做 整数转字符串 用 全局_渲染统计 的 总层数。
        做 打印行 用 「跳过: 」 加 做 整数转字符串 用 全局_渲染统计 的 跳过层数。
        做 打印行 用 「低质量: 」 加 做 整数转字符串 用 全局_渲染统计 的 低质量层数。
        做 打印行 用 「高质量: 」 加 做 整数转字符串 用 全局_渲染统计 的 高质量层数。
    完。
    做 打印行 用 「FPS: 」 加 做 整数转字符串 用 做 帧调度_获取FPS。
    做 打印行 用 「平均绘制: 」 加 做 整数转字符串 用 做 帧诊断_平均绘制时间 加 「ms」。
    做 打印行 用 「质量自适应: 」 加 做 整数转字符串 用 质量自适应_连续超时 加 「 次超时」。
完。

本章 收束。
