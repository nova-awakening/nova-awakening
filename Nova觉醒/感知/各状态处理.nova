《各状态处理》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·HTML令牌处理》。

HTML令牌izer_处理_数据状态 接受 tk、令牌、c：
    若 c 等于 0x3C：
        若 令牌 的 数据长度 大于 0：
            令牌 的 类型 为 令牌类型_字符。
            tk 的 位置 为 tk 的 位置 减 1。
            返还。
        完。
        tk 的 状态 为 状态_标签开始。
    否则若 c 等于 0x26：
        tk 的 返回状态 为 状态_数据。
        tk 的 状态 为 状态_字符引用。
    否则若 c 等于 0：
        做 HTML令牌izer_追加数据 用 令牌、c。
    否则：
        做 HTML令牌izer_追加数据 用 令牌、c。
    完。
完。

HTML令牌izer_处理_标签开始 接受 tk、令牌、c：
    若 c 等于 0x21：
        tk 的 状态 为 状态_标记声明开始。
    否则若 c 等于 0x2F：
        tk 的 状态 为 状态_结束标签开始。
    否则若 做 HTML令牌izer_是ASCII字母 用 c：
        令牌 的 类型 为 令牌类型_开始标签。
        令牌 的 标签名长度 为 0。
        做 HTML令牌izer_追加标签名 用 令牌、做 HTML令牌izer_转小写 用 c。
        tk 的 状态 为 状态_标签名。
    否则若 c 等于 0x3F：
        tk 的 状态 为 状态_伪注释。
    否则：
        做 HTML令牌izer_追加数据 用 令牌、0x3C。
        做 HTML令牌izer_追加数据 用 令牌、c。
        令牌 的 类型 为 令牌类型_字符。
        tk 的 状态 为 状态_数据。
    完。
完。

HTML令牌izer_处理_结束标签开始 接受 tk、令牌、c：
    若 做 HTML令牌izer_是ASCII字母 用 c：
        令牌 的 类型 为 令牌类型_结束标签。
        令牌 的 标签名长度 为 0。
        做 HTML令牌izer_追加标签名 用 令牌、做 HTML令牌izer_转小写 用 c。
        tk 的 状态 为 状态_标签名。
    否则若 c 等于 0x3E：
        tk 的 状态 为 状态_数据。
    否则：
        tk 的 状态 为 状态_伪注释。
    完。
完。

HTML令牌izer_处理_标签名 接受 tk、令牌、c：
    若 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0C 或者 c 等于 0x20：
        tk 的 状态 为 状态_属性名前。
    否则若 c 等于 0x2F：
        tk 的 状态 为 状态_自闭合开始标签。
    否则若 c 等于 0x3E：
        做 HTML令牌izer_完成标签名 用 令牌。
        若 令牌 的 类型 等于 令牌类型_开始标签：
            做 HTML令牌izer_记录开始标签 用 tk、令牌。
        完。
        tk 的 状态 为 状态_数据。
    否则：
        做 HTML令牌izer_追加标签名 用 令牌、做 HTML令牌izer_转小写 用 c。
    完。
完。

HTML令牌izer_处理_属性名前 接受 tk、令牌、c：
    若 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0C 或者 c 等于 0x20：
        返还。
    完。
    若 c 等于 0x2F：
        tk 的 状态 为 状态_自闭合开始标签。
        返还。
    完。
    若 c 等于 0x3E：
        做 HTML令牌izer_完成标签名 用 令牌。
        若 令牌 的 类型 等于 令牌类型_开始标签：
            做 HTML令牌izer_记录开始标签 用 tk、令牌。
        完。
        tk 的 状态 为 状态_数据。
        返还。
    完。
    tk 的 当前属性名长度 为 0。
    tk 的 当前属性值长度 为 0。
    做 HTML令牌izer_追加属性名字符 用 tk、做 HTML令牌izer_转小写 用 c。
    tk 的 状态 为 状态_属性名。
完。

本章 收束。
