《内核直连层》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·内存物理》。

引用《..·中枢·同步原语》。

引用《..·中枢·时钟中枢》。

引用《..·推理·通信渠道》。

引用《..·执行·平台时间》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存系统调用》。

引用《..·中枢·块内存分配器》。

引用《..·执行·IO多路复用》。

引用《..·中枢·读取调用》。

渲染_最大堆容量 为 268435456。

渲染_池块大小_小 为 64。

渲染_池块大小_中 为 256。

渲染_池块大小_大 为 1024。

渲染_池容量_小 为 4096。

渲染_池容量_中 为 1024。

渲染_池容量_大 为 256。

渲染_竞技场大小 为 65536。

渲染_最大竞技场数 为 64。

渲染_碎片率阈值 为 300。

渲染_文件最大大小 为 104857600。

渲染_读缓冲大小 为 8192。

渲染内存统计 是 一种 事物，含 堆已用字节、堆峰值字节、分配总次数、释放总次数、分配失败次数、池命中次数、池未命中次数、竞技场分配次数、竞技场重置次数、内存映射次数、内存取消映射次数、文件打开次数、文件读取字节、锁竞争次数、AI优化调用次数、AI碎片整理次数、总分配耗时_纳秒、最大单次分配耗时。

对象池 是 一种 事物，含 块大小、容量、已用数、空闲链表头、池内存起始、锁、命中计数、回退计数。

竞技场分配器 是 一种 事物，含 基地址、大小、已用偏移、锁、分配次数、重置次数、峰值使用、所属阶段。

AI内存状态 是 一种 事物，含 历史分配模式、历史碎片率、连续大分配计数、连续小分配计数、预测下次大小、策略_优先池、策略_预分配、采样窗口计数器、上次优化时间。

可变 渲染_堆已用 为 0。

可变 渲染_分配次数 为 0。

可变 渲染_堆锁 为 0。

可变 渲染_统计 为 0。

可变 渲染_池_小 为 0。

可变 渲染_池_中 为 0。

可变 渲染_池_大 为 0。

可变 渲染_竞技场列表 为 0。

可变 渲染_竞技场数 为 0。

可变 渲染_AI状态 为 0。

可变 渲染_已初始化 为 假。

渲染直连层_初始化 接受 零参：
    若 渲染_已初始化 等于 真：
        做 打印 用 「[内核直连层] 警告: 重复初始化」。
        返还 错误_已存在。
    完。
    渲染_统计 为 做 kmalloc 用 144。
    若 渲染_统计 等于 空：
        做 打印 用 「[内核直连层] 错误: 统计结构分配失败」。
        返还 错误_内存不足。
    完。
    做 内存_置零 用 渲染_统计、144。
    渲染_堆锁 为 做 自旋锁_创建。
    渲染_池_小 为 做 内核直连层_创建池 用 渲染_池块大小_小、渲染_池容量_小。
    渲染_池_中 为 做 内核直连层_创建池 用 渲染_池块大小_中、渲染_池容量_中。
    渲染_池_大 为 做 内核直连层_创建池 用 渲染_池块大小_大、渲染_池容量_大。
    渲染_竞技场列表 为 做 kmalloc 用 渲染_最大竞技场数 乘 8。
    若 渲染_竞技场列表 不等于 空：
        做 内存_置零 用 渲染_竞技场列表、渲染_最大竞技场数 乘 8。
    完。
    渲染_竞技场数 为 0。
    渲染_AI状态 为 做 kmalloc 用 72。
    若 渲染_AI状态 不等于 空：
        做 内存_置零 用 渲染_AI状态、72。
        渲染_AI状态 的 策略_优先池 为 真。
        渲染_AI状态 的 策略_预分配 为 假。
    完。
    渲染_已初始化 为 真。
    做 打印 用 「[内核直连层] 初始化完成: 堆+池+竞技场+AI就绪」。
    返还 0。
完。

渲染直连层_销毁 接受 零参：
    若 渲染_已初始化 等于 假：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 渲染_竞技场数：
        竞技场 为 做 内存_读64 用 渲染_竞技场列表 加 i 乘 8。
        若 竞技场 不等于 空：
            若 竞技场 的 基地址 不等于 空：
                做 kfree 用 竞技场 的 基地址。
            完。
            做 kfree 用 竞技场。
        完。
        i 为 i 加 1。
    完。
    若 渲染_竞技场列表 不等于 空：
        做 kfree 用 渲染_竞技场列表。
    完。
    做 内核直连层_销毁池 用 渲染_池_小。
    做 内核直连层_销毁池 用 渲染_池_中。
    做 内核直连层_销毁池 用 渲染_池_大。
    若 渲染_AI状态 不等于 空：
        做 kfree 用 渲染_AI状态。
    完。
    若 渲染_统计 不等于 空：
        做 kfree 用 渲染_统计。
    完。
    渲染_已初始化 为 假。
    做 打印 用 「[内核直连层] 已销毁」。
    返还 0。
完。

渲染直连层_重置统计 接受 零参：
    若 渲染_统计 等于 空：
        返还 错误_未初始化。
    完。
    保留已用 为 渲染_统计 的 堆已用字节。
    保留峰值 为 渲染_统计 的 堆峰值字节。
    做 内存_置零 用 渲染_统计、144。
    渲染_统计 的 堆已用字节 为 保留已用。
    渲染_统计 的 堆峰值字节 为 保留峰值。
    做 打印 用 「[内核直连层] 统计已重置(保留堆水位)」。
    返还 0。
完。

渲染_堆_分配 接受 大小：
    若 大小 等于 0：
        返还 空。
    完。
    若 大小 大于 渲染_最大堆容量：
        做 打印 用 「[内核直连层] 错误: 分配请求超过最大堆容量」。
        若 渲染_统计 不等于 空：
            渲染_统计 的 分配失败次数 为 渲染_统计 的 分配失败次数 加 1。
        完。
        返还 空。
    完。
    开始时间 为 做 平台_获取高精度时间。
    若 渲染_AI状态 不等于 空 并且 渲染_AI状态 的 策略_优先池 等于 真：
        池结果 为 做 内核直连层_池分配 用 大小。
        若 池结果 不等于 空：
            若 渲染_统计 不等于 空：
                渲染_统计 的 池命中次数 为 渲染_统计 的 池命中次数 加 1。
                耗时 为 做 平台_获取高精度时间 减 开始时间。
                渲染_统计 的 总分配耗时_纳秒 为 渲染_统计 的 总分配耗时_纳秒 加 耗时。
            完。
            返还 池结果。
        完。
        若 渲染_统计 不等于 空：
            渲染_统计 的 池未命中次数 为 渲染_统计 的 池未命中次数 加 1。
        完。
    完。
    做 自旋锁_获取 用 渲染_堆锁。
    ptr 为 做 kmalloc 用 大小。
    若 ptr 不等于 空：
        渲染_堆已用 为 渲染_堆已用 加 大小。
        渲染_分配次数 为 渲染_分配次数 加 1。
        若 渲染_统计 不等于 空：
            渲染_统计 的 堆已用字节 为 渲染_堆已用。
            渲染_统计 的 分配总次数 为 渲染_统计 的 分配总次数 加 1。
            若 渲染_堆已用 大于 渲染_统计 的 堆峰值字节：
                渲染_统计 的 堆峰值字节 为 渲染_堆已用。
            完。
        完。
    否则：
        若 渲染_统计 不等于 空：
            渲染_统计 的 分配失败次数 为 渲染_统计 的 分配失败次数 加 1。
        完。
        做 打印 用 「[内核直连层] 警告: kmalloc失败」。
    完。
    做 自旋锁_释放 用 渲染_堆锁。
    若 渲染_AI状态 不等于 空：
        做 AI内存_记录分配 用 大小。
    完。
    若 渲染_统计 不等于 空：
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        渲染_统计 的 总分配耗时_纳秒 为 渲染_统计 的 总分配耗时_纳秒 加 耗时。
        若 耗时 大于 渲染_统计 的 最大单次分配耗时：
            渲染_统计 的 最大单次分配耗时 为 耗时。
        完。
    完。
    返还 ptr。
完。

渲染_堆_释放 接受 ptr：
    若 ptr 等于 空：
        返还。
    完。
    若 做 内核直连层_池释放 用 ptr 等于 真：
        返还。
    完。
    做 自旋锁_获取 用 渲染_堆锁。
    做 kfree 用 ptr。
    渲染_分配次数 为 渲染_分配次数 减 1。
    若 渲染_统计 不等于 空：
        渲染_统计 的 释放总次数 为 渲染_统计 的 释放总次数 加 1。
    完。
    做 自旋锁_释放 用 渲染_堆锁。
完。

堆_分配_清零 接受 大小：
    ptr 为 做 堆_分配 用 大小。
    若 ptr 不等于 空：
        做 内存_置零 用 ptr、大小。
    完。
    返还 ptr。
完。

堆_分配_数组 接受 字节数：
    若 字节数 小于或等于 0：
        返还 空。
    完。
    返还 做 堆_分配_清零 用 字节数。
完。

堆_重新分配 接受 旧指针、旧大小、新大小：
    若 旧指针 等于 空：
        返还 做 堆_分配 用 新大小。
    完。
    若 新大小 等于 0：
        做 堆_释放 用 旧指针。
        返还 空。
    完。
    若 新大小 小于或等于 旧大小：
        返还 旧指针。
    完。
    新指针 为 做 堆_分配 用 新大小。
    若 新指针 等于 空：
        返还 空。
    完。
    做 内存_复制 用 新指针、旧指针、旧大小。
    做 堆_释放 用 旧指针。
    返还 新指针。
完。

内核直连层_创建池 接受 块大小、容量：
    若 块大小 等于 0 或者 容量 等于 0：
        返还 空。
    完。
    池 为 做 kmalloc 用 64。
    若 池 等于 空：
        返还 空。
    完。
    池 的 块大小 为 块大小。
    池 的 容量 为 容量。
    池 的 已用数 为 0。
    池 的 命中计数 为 0。
    池 的 回退计数 为 0。
    池 的 锁 为 做 自旋锁_创建。
    总大小 为 块大小 乘 容量。
    池 的 池内存起始 为 做 kmalloc 用 总大小。
    若 池 的 池内存起始 等于 空：
        做 kfree 用 池。
        返还 空。
    完。
    做 内存_置零 用 池 的 池内存起始、总大小。
    可变 i 为 0。
    只要 i 小于 容量 减 1：
        当前块 为 池 的 池内存起始 加 i 乘 块大小。
        下一块 为 池 的 池内存起始 加 (i 加 1) 乘 块大小。
        做 内存_写64 用 当前块、下一块。
        i 为 i 加 1。
    完。
    做 内存_写64 用 池 的 池内存起始 加 (容量 减 1) 乘 块大小、0。
    池 的 空闲链表头 为 池 的 池内存起始。
    返还 池。
完。

内核直连层_销毁池 接受 池：
    若 池 等于 空：
        返还。
    完。
    若 池 的 池内存起始 不等于 空：
        做 kfree 用 池 的 池内存起始。
    完。
    做 kfree 用 池。
完。

内核直连层_池分配 接受 大小：
    可变 目标池 为 空。
    若 大小 小于或等于 渲染_池块大小_小：
        目标池 为 渲染_池_小。
    否则若 大小 小于或等于 渲染_池块大小_中：
        目标池 为 渲染_池_中。
    否则若 大小 小于或等于 渲染_池块大小_大：
        目标池 为 渲染_池_大。
    否则：
        返还 空。
    完。
    若 目标池 等于 空：
        返还 空。
    完。
    做 自旋锁_获取 用 目标池 的 锁。
    若 目标池 的 空闲链表头 等于 0：
        做 自旋锁_释放 用 目标池 的 锁。
        目标池 的 回退计数 为 目标池 的 回退计数 加 1。
        返还 空。
    完。
    块 为 目标池 的 空闲链表头。
    目标池 的 空闲链表头 为 做 内存_读64 用 块。
    目标池 的 已用数 为 目标池 的 已用数 加 1。
    目标池 的 命中计数 为 目标池 的 命中计数 加 1。
    做 自旋锁_释放 用 目标池 的 锁。
    做 内存_置零 用 块、目标池 的 块大小。
    返还 块。
完。

内核直连层_池释放 接受 ptr：
    若 ptr 等于 空：
        返还 假。
    完。
    若 渲染_池_小 不等于 空：
        池 为 渲染_池_小。
        若 ptr 大于或等于 池 的 池内存起始 并且 ptr 小于 池 的 池内存起始 加 池 的 块大小 乘 池 的 容量：
            做 自旋锁_获取 用 池 的 锁。
            做 内存_写64 用 ptr、池 的 空闲链表头。
            池 的 空闲链表头 为 ptr。
            池 的 已用数 为 池 的 已用数 减 1。
            做 自旋锁_释放 用 池 的 锁。
            返还 真。
        完。
    完。
    若 渲染_池_中 不等于 空：
        池 为 渲染_池_中。
        若 ptr 大于或等于 池 的 池内存起始 并且 ptr 小于 池 的 池内存起始 加 池 的 块大小 乘 池 的 容量：
            做 自旋锁_获取 用 池 的 锁。
            做 内存_写64 用 ptr、池 的 空闲链表头。
            池 的 空闲链表头 为 ptr。
            池 的 已用数 为 池 的 已用数 减 1。
            做 自旋锁_释放 用 池 的 锁。
            返还 真。
        完。
    完。
    若 渲染_池_大 不等于 空：
        池 为 渲染_池_大。
        若 ptr 大于或等于 池 的 池内存起始 并且 ptr 小于 池 的 池内存起始 加 池 的 块大小 乘 池 的 容量：
            做 自旋锁_获取 用 池 的 锁。
            做 内存_写64 用 ptr、池 的 空闲链表头。
            池 的 空闲链表头 为 ptr。
            池 的 已用数 为 池 的 已用数 减 1。
            做 自旋锁_释放 用 池 的 锁。
            返还 真。
        完。
    完。
    返还 假。
完。

渲染_竞技场_创建 接受 阶段：
    若 渲染_竞技场数 大于或等于 渲染_最大竞技场数：
        做 打印 用 「[内核直连层] 错误: 竞技场数量超限」。
        返还 空。
    完。
    竞技场 为 做 kmalloc 用 64。
    若 竞技场 等于 空：
        返还 空。
    完。
    竞技场 的 大小 为 渲染_竞技场大小。
    竞技场 的 基地址 为 做 kmalloc 用 渲染_竞技场大小。
    若 竞技场 的 基地址 等于 空：
        做 kfree 用 竞技场。
        返还 空。
    完。
    做 内存_置零 用 竞技场 的 基地址、渲染_竞技场大小。
    竞技场 的 已用偏移 为 0。
    竞技场 的 锁 为 做 自旋锁_创建。
    竞技场 的 分配次数 为 0。
    竞技场 的 重置次数 为 0。
    竞技场 的 峰值使用 为 0。
    竞技场 的 所属阶段 为 阶段。
    若 渲染_竞技场列表 不等于 空：
        做 内存_写64 用 渲染_竞技场列表 加 渲染_竞技场数 乘 8、竞技场。
        渲染_竞技场数 为 渲染_竞技场数 加 1。
    完。
    返还 竞技场。
完。

渲染_竞技场_分配 接受 竞技场、大小：
    若 竞技场 等于 空 或者 大小 等于 0：
        返还 空。
    完。
    对齐大小 为 大小 加 7 位与 按位取反 7。
    做 自旋锁_获取 用 竞技场 的 锁。
    若 竞技场 的 已用偏移 加 对齐大小 大于 竞技场 的 大小：
        做 自旋锁_释放 用 竞技场 的 锁。
        返还 空。
    完。
    ptr 为 竞技场 的 基地址 加 竞技场 的 已用偏移。
    竞技场 的 已用偏移 为 竞技场 的 已用偏移 加 对齐大小。
    竞技场 的 分配次数 为 竞技场 的 分配次数 加 1。
    若 竞技场 的 已用偏移 大于 竞技场 的 峰值使用：
        竞技场 的 峰值使用 为 竞技场 的 已用偏移。
    完。
    做 自旋锁_释放 用 竞技场 的 锁。
    若 渲染_统计 不等于 空：
        渲染_统计 的 竞技场分配次数 为 渲染_统计 的 竞技场分配次数 加 1。
    完。
    返还 ptr。
完。

渲染_竞技场_重置 接受 竞技场：
    若 竞技场 等于 空：
        返还 错误_无效参数。
    完。
    做 自旋锁_获取 用 竞技场 的 锁。
    竞技场 的 已用偏移 为 0。
    竞技场 的 重置次数 为 竞技场 的 重置次数 加 1。
    做 自旋锁_释放 用 竞技场 的 锁。
    若 渲染_统计 不等于 空：
        渲染_统计 的 竞技场重置次数 为 渲染_统计 的 竞技场重置次数 加 1。
    完。
    返还 0。
完。

内存_读8 接受 地址：
    汇编。
    块：
        「movzx eax, byte [rdi]」。
    完。
完。

内存_写8 接受 地址、值：
    汇编。
    块：
        「mov byte [rdi], sil」。
    完。
完。

内存_读16 接受 地址：
    汇编。
    块：
        「movzx eax, word [rdi]」。
    完。
完。

内存_写16 接受 地址、值：
    汇编。
    块：
        「mov word [rdi], si」。
    完。
完。

内存_读32 接受 地址：
    汇编。
    块：
        「mov eax, [rdi]」。
    完。
完。

内存_写32 接受 地址、值：
    汇编。
    块：
        「mov [rdi], esi」。
    完。
完。

内存_读64 接受 地址：
    汇编。
    块：
        「mov rax, [rdi]」。
    完。
完。

内存_写64 接受 地址、值：
    汇编。
    块：
        「mov [rdi], rsi」。
    完。
完。

内存_置零 接受 地址、字节数：
    若 地址 等于 空 或者 字节数 小于或等于 0：
        返还。
    完。
    整块数 为 字节数 除以 8。
    可变 i 为 0。
    只要 i 小于 整块数：
        做 内存_写64 用 地址 加 i 乘 8、0。
        i 为 i 加 1。
    完。
    可变 j 为 整块数 乘 8。
    只要 j 小于 字节数：
        做 内存_写8 用 地址 加 j、0。
        j 为 j 加 1。
    完。
完。

内存_复制 接受 目标、源、字节数：
    若 目标 等于 空 或者 源 等于 空 或者 字节数 小于或等于 0：
        返还。
    完。
    整块数 为 字节数 除以 8。
    可变 i 为 0。
    只要 i 小于 整块数：
        做 内存_写64 用 目标 加 i 乘 8、做 内存_读64 用 源 加 i 乘 8。
        i 为 i 加 1。
    完。
    可变 j 为 整块数 乘 8。
    只要 j 小于 字节数：
        做 内存_写8 用 目标 加 j、做 内存_读8 用 源 加 j。
        j 为 j 加 1。
    完。
完。

内存_比较 接受 地址1、地址2、字节数：
    若 地址1 等于 空 或者 地址2 等于 空：
        返还 负 1。
    完。
    可变 i 为 0。
    只要 i 小于 字节数：
        a 为 做 内存_读8 用 地址1 加 i。
        b 为 做 内存_读8 用 地址2 加 i。
        若 a 不等于 b：
            返还 a 减 b。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

内存_填充 接受 地址、字节值、字节数：
    若 地址 等于 空 或者 字节数 小于或等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 字节数：
        做 内存_写8 用 地址 加 i、字节值。
        i 为 i 加 1。
    完。
完。

渲染_互斥锁创建 接受 零参：
    返还 做 互斥锁_创建。
完。

渲染_互斥锁获取 接受 锁：
    若 锁 等于 空：
        返还。
    完。
    做 互斥锁_获取 用 锁。
    若 渲染_统计 不等于 空：
        渲染_统计 的 锁竞争次数 为 渲染_统计 的 锁竞争次数 加 1。
    完。
完。

渲染_互斥锁释放 接受 锁：
    若 锁 等于 空：
        返还。
    完。
    做 互斥锁_释放 用 锁。
完。

渲染_自旋锁创建 接受 零参：
    返还 做 自旋锁_创建。
完。

渲染_自旋锁获取 接受 锁：
    做 自旋锁_获取 用 锁。
完。

渲染_自旋锁释放 接受 锁：
    做 自旋锁_释放 用 锁。
完。

渲染_文件打开 接受 路径、标志：
    若 路径 等于 空：
        返还 负 1。
    完。
    fd 为 做 sys_open 用 路径、标志、0。
    若 fd 大于或等于 0 并且 渲染_统计 不等于 空：
        渲染_统计 的 文件打开次数 为 渲染_统计 的 文件打开次数 加 1。
    完。
    返还 fd。
完。

渲染_文件读取 接受 fd、buf、大小：
    若 fd 小于 0 或者 buf 等于 空 或者 大小 小于或等于 0：
        返还 负 1。
    完。
    若 大小 大于 渲染_文件最大大小：
        做 打印 用 「[内核直连层] 警告: 文件读取超过上限，截断」。
        大小 为 渲染_文件最大大小。
    完。
    已读 为 做 sys_read 用 fd、buf、大小。
    若 已读 大于 0 并且 渲染_统计 不等于 空：
        渲染_统计 的 文件读取字节 为 渲染_统计 的 文件读取字节 加 已读。
    完。
    返还 已读。
完。

渲染_文件写入 接受 fd、buf、大小：
    若 fd 小于 0 或者 buf 等于 空 或者 大小 小于或等于 0：
        返还 负 1。
    完。
    返还 做 sys_write 用 fd、buf、大小。
完。

渲染_文件关闭 接受 fd：
    若 fd 小于 0：
        返还 负 1。
    完。
    返还 做 sys_close 用 fd。
完。

渲染_文件全部读取 接受 路径：
    若 路径 等于 空：
        返还 空。
    完。
    fd 为 做 渲染_文件打开 用 路径、0。
    若 fd 小于 0：
        返还 空。
    完。
    可变 总大小 为 0。
    buf 为 做 堆_分配 用 渲染_读缓冲大小。
    若 buf 等于 空：
        做 渲染_文件关闭 用 fd。
        返还 空。
    完。
    可变 结果 为 做 堆_分配 用 渲染_读缓冲大小。
    可变 结果容量 为 渲染_读缓冲大小。
    若 结果 等于 空：
        做 堆_释放 用 buf。
        做 渲染_文件关闭 用 fd。
        返还 空。
    完。
    可变 继续读取 为 真。
    只要 继续读取 等于 真：
        已读 为 做 渲染_文件读取 用 fd、buf、渲染_读缓冲大小。
        若 已读 小于或等于 0：
            继续读取 为 假。
        否则：
            若 总大小 加 已读 大于 结果容量：
                新容量 为 结果容量 乘 2。
                结果 为 做 堆_重新分配 用 结果、结果容量、新容量。
                若 结果 等于 空：
                    继续读取 为 假。
                否则：
                    结果容量 为 新容量。
                完。
            完。
            若 结果 不等于 空：
                做 内存_复制 用 结果 加 总大小、buf、已读。
                总大小 为 总大小 加 已读。
            完。
        完。
    完。
    做 堆_释放 用 buf。
    做 渲染_文件关闭 用 fd。
    返还 结果。
完。

渲染_内存映射 接受 大小：
    若 大小 小于或等于 0：
        返还 空。
    完。
    结果 为 做 sys_Nova内存映射 用 0、大小、3、0x22、负 1、0。
    若 结果 不等于 空 并且 渲染_统计 不等于 空：
        渲染_统计 的 内存映射次数 为 渲染_统计 的 内存映射次数 加 1。
    完。
    返还 结果。
完。

渲染_内存取消映射 接受 地址、大小：
    若 地址 等于 空 或者 大小 小于或等于 0：
        返还 负 1。
    完。
    结果 为 做 sys_munmap 用 地址、大小。
    若 渲染_统计 不等于 空：
        渲染_统计 的 内存取消映射次数 为 渲染_统计 的 内存取消映射次数 加 1。
    完。
    返还 结果。
完。

平台_获取单调时间毫秒 接受 零参：
    返还 做 平台_获取纳秒时间 除以 1000000。
完。

平台_获取单调时间秒 接受 零参：
    返还 做 平台_获取纳秒时间 除以 1000000000。
完。

平台_计时开始 接受 零参：
    返还 做 平台_获取高精度时间。
完。

平台_计时结束 接受 开始：
    结束 为 做 平台_获取高精度时间。
    若 结束 大于 开始：
        返还 结束 减 开始。
    完。
    返还 0。
完。

AI内存_记录分配 接受 大小：
    若 渲染_AI状态 等于 空：
        返还。
    完。
    状态 为 渲染_AI状态。
    状态 的 采样窗口计数器 为 状态 的 采样窗口计数器 加 1。
    状态 的 历史分配模式 为 (状态 的 历史分配模式 乘 7 加 大小) 除以 8。
    若 大小 大于 渲染_池块大小_大：
        状态 的 连续大分配计数 为 状态 的 连续大分配计数 加 1。
        状态 的 连续小分配计数 为 0。
    否则：
        状态 的 连续小分配计数 为 状态 的 连续小分配计数 加 1。
        状态 的 连续大分配计数 为 0。
    完。
    可变 碎片率 为 0。
    若 渲染_堆已用 大于 0 并且 渲染_分配次数 大于 0：
        碎片率 为 渲染_分配次数 乘 1024 除以 (渲染_堆已用 除以 1024 加 1)。
    完。
    状态 的 历史碎片率 为 (状态 的 历史碎片率 乘 15 加 碎片率) 除以 16。
    若 状态 的 采样窗口计数器 小于 256：
        返还。
    完。
    状态 的 采样窗口计数器 为 0。
    若 渲染_统计 不等于 空：
        渲染_统计 的 AI优化调用次数 为 渲染_统计 的 AI优化调用次数 加 1。
    完。
    若 状态 的 连续小分配计数 大于 100：
        状态 的 策略_优先池 为 真。
    完。
    若 状态 的 连续大分配计数 大于 20：
        状态 的 策略_优先池 为 假。
    完。
    若 状态 的 历史碎片率 大于 渲染_碎片率阈值：
        做 打印 用 「[AI内存] 碎片率过高，建议整理」。
        若 渲染_统计 不等于 空：
            渲染_统计 的 AI碎片整理次数 为 渲染_统计 的 AI碎片整理次数 加 1。
        完。
    完。
    状态 的 预测下次大小 为 状态 的 历史分配模式。
    若 状态 的 策略_预分配 等于 真 并且 状态 的 预测下次大小 大于 0：
    完。
    状态 的 上次优化时间 为 做 平台_获取高精度时间。
完。

渲染直连层_打印统计 接受 零参：
    若 渲染_统计 等于 空：
        做 打印 用 「[内核直连层] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   内核直连层 · 统计报告             │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 堆已用(KB):       」、做 转字符串 用 渲染_统计 的 堆已用字节 除以 1024。
    做 打印 用 做 字符串拼接 用 「│ 堆峰值(KB):       」、做 转字符串 用 渲染_统计 的 堆峰值字节 除以 1024。
    做 打印 用 做 字符串拼接 用 「│ 分配总次数:       」、做 转字符串 用 渲染_统计 的 分配总次数。
    做 打印 用 做 字符串拼接 用 「│ 释放总次数:       」、做 转字符串 用 渲染_统计 的 释放总次数。
    做 打印 用 做 字符串拼接 用 「│ 分配失败:         」、做 转字符串 用 渲染_统计 的 分配失败次数。
    做 打印 用 做 字符串拼接 用 「│ 池命中:           」、做 转字符串 用 渲染_统计 的 池命中次数。
    做 打印 用 做 字符串拼接 用 「│ 池未命中:         」、做 转字符串 用 渲染_统计 的 池未命中次数。
    做 打印 用 做 字符串拼接 用 「│ 竞技场分配:       」、做 转字符串 用 渲染_统计 的 竞技场分配次数。
    做 打印 用 做 字符串拼接 用 「│ 竞技场重置:       」、做 转字符串 用 渲染_统计 的 竞技场重置次数。
    做 打印 用 做 字符串拼接 用 「│ 内存映射:         」、做 转字符串 用 渲染_统计 的 内存映射次数。
    做 打印 用 做 字符串拼接 用 「│ 文件打开:         」、做 转字符串 用 渲染_统计 的 文件打开次数。
    做 打印 用 做 字符串拼接 用 「│ 文件读取(KB):     」、做 转字符串 用 渲染_统计 的 文件读取字节 除以 1024。
    做 打印 用 做 字符串拼接 用 「│ 锁竞争:           」、做 转字符串 用 渲染_统计 的 锁竞争次数。
    做 打印 用 做 字符串拼接 用 「│ AI优化调用:       」、做 转字符串 用 渲染_统计 的 AI优化调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI碎片整理:       」、做 转字符串 用 渲染_统计 的 AI碎片整理次数。
    做 打印 用 做 字符串拼接 用 「│ 总分配耗时(μs):   」、做 转字符串 用 渲染_统计 的 总分配耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 最大单次耗时(μs): 」、做 转字符串 用 渲染_统计 的 最大单次分配耗时 除以 1000。
    做 打印 用 「└──────────────────────────────────────┘」。
    若 渲染_池_小 不等于 空：
        做 打印 用 做 字符串拼接 用 「  池(64B):  已用=」、做 字符串拼接 用 做 转字符串 用 渲染_池_小 的 已用数、做 字符串拼接 用 「/」、做 转字符串 用 渲染_池_小 的 容量。
    完。
    若 渲染_池_中 不等于 空：
        做 打印 用 做 字符串拼接 用 「  池(256B): 已用=」、做 字符串拼接 用 做 转字符串 用 渲染_池_中 的 已用数、做 字符串拼接 用 「/」、做 转字符串 用 渲染_池_中 的 容量。
    完。
    若 渲染_池_大 不等于 空：
        做 打印 用 做 字符串拼接 用 「  池(1KB):  已用=」、做 字符串拼接 用 做 转字符串 用 渲染_池_大 的 已用数、做 字符串拼接 用 「/」、做 转字符串 用 渲染_池_大 的 容量。
    完。
完。

渲染直连层_导出诊断 接受 零参：
    若 渲染_统计 等于 空：
        做 打印 用 「[内核直连层] 诊断: 未初始化」。
        返还。
    完。
    做 打印 用 「[内核直连层] === 诊断信息 ===」。
    做 打印 用 做 字符串拼接 用 「  已初始化: 」、做 转字符串 用 渲染_已初始化。
    做 打印 用 做 字符串拼接 用 「  活跃分配数: 」、做 转字符串 用 渲染_分配次数。
    做 打印 用 做 字符串拼接 用 「  竞技场数: 」、做 转字符串 用 渲染_竞技场数。
    若 渲染_AI状态 不等于 空：
        状态 为 渲染_AI状态。
        做 打印 用 做 字符串拼接 用 「  AI历史分配模式: 」、做 转字符串 用 状态 的 历史分配模式。
        做 打印 用 做 字符串拼接 用 「  AI碎片率(Q10): 」、做 转字符串 用 状态 的 历史碎片率。
        做 打印 用 做 字符串拼接 用 「  AI策略_优先池: 」、做 转字符串 用 状态 的 策略_优先池。
        做 打印 用 做 字符串拼接 用 「  AI预测下次大小: 」、做 转字符串 用 状态 的 预测下次大小。
    完。
    可变 i 为 0。
    只要 i 小于 渲染_竞技场数：
        竞技场 为 做 内存_读64 用 渲染_竞技场列表 加 i 乘 8。
        若 竞技场 不等于 空：
            做 打印 用 做 字符串拼接 用 「  竞技场[」、做 字符串拼接 用 做 转字符串 用 i、做 字符串拼接 用 「] 已用=」、做 字符串拼接 用 做 转字符串 用 竞技场 的 已用偏移、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 竞技场 的 大小、做 字符串拼接 用 「 峰值=」、做 转字符串 用 竞技场 的 峰值使用。
        完。
        i 为 i 加 1。
    完。
完。

渲染_获取统计 接受 零参：
    返还 渲染_统计。
完。

渲染_堆统计 接受 零参：
    返还 渲染_堆已用。
完。

渲染_获取AI状态 接受 零参：
    返还 渲染_AI状态。
完。

渲染_检查健康 接受 零参：
    若 渲染_已初始化 等于 假：
        返还 错误_未初始化。
    完。
    若 渲染_统计 等于 空：
        返还 错误_未初始化。
    完。
    若 渲染_堆已用 大于 渲染_最大堆容量 乘 9 除以 10：
        做 打印 用 「[内核直连层] 警告: 堆使用率超过90%」。
        返还 错误_容量不足。
    完。
    返还 0。
完。

内核直连层_统计 接受 零参：
    返还 一本字典：渲染分配次数 对应 渲染_分配次数、渲染已初始化 对应 渲染_已初始化。
完。

内核直连层_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 渲染_分配次数 大于 100000：
        做 追加 用 问题、「渲染_分配次数异常增长」。
    完。
    若 渲染_已初始化 等于 假：
        做 追加 用 问题、「渲染_已初始化未就绪」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

内核直连层_综合报告 接受 零参：
    做 打印 用 「[内核直连层] ═══ 综合报告 ═══」。
    做 打印 用 「  渲染_分配次数: 」 加 渲染_分配次数。
    做 打印 用 「  渲染_已初始化: 」 加 渲染_已初始化。
    做 打印 用 「  健康: 」 加 若 做 内核直连层_健康评估 则 「正常」 否则 「异常」。
完。

内核直连层_全局重置 接受 零参：
    渲染_分配次数 为 0。
    渲染_已初始化 为 0。
    做 打印 用 「[内核直连层] 统计已重置」。
    返还 0。
完。

本章 收束。
