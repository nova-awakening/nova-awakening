《网缓绘制结》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

Sk绘制 是 一种 事物，含 颜色、样式、抗锯齿、抖动、描边宽度、描边端点、描边连接、斜接限制、混合模式、滤波质量、着色器、颜色滤镜、图像滤镜、遮罩滤镜、路径效果。

Sk绘制_创建 接受 零参：
    p 为 做 堆_分配 用 Sk绘制。
    p 的 颜色 的 r 为 0。
    p 的 颜色 的 g 为 0。
    p 的 颜色 的 b 为 0。
    p 的 颜色 的 a 为 1024。
    p 的 样式 为 PAINT_STYLE_FILL。
    p 的 抗锯齿 为 真。
    p 的 抖动 为 假。
    p 的 描边宽度 为 0。
    p 的 描边端点 为 STROKE_CAP_BUTT。
    p 的 描边连接 为 STROKE_JOIN_MITER。
    p 的 斜接限制 为 4096。
    p 的 混合模式 为 BLEND_SRC_OVER。
    p 的 滤波质量 为 FILTER_LOW。
    p 的 着色器 为 0。
    p 的 颜色滤镜 为 0。
    p 的 图像滤镜 为 0。
    p 的 遮罩滤镜 为 0。
    p 的 路径效果 为 0。
    返还 p。
完。

Sk绘制_复制 接受 src：
    若 src 等于 0：
        返还 做 Sk绘制_创建。
    完。
    p 为 做 堆_分配 用 Sk绘制。
    做 内存_复制 用 p、src、做 大小of 用 Sk绘制。
    返还 p。
完。

Sk绘制_设颜色 接受 p、r、g、b、a：
    若 p 等于 0：
        返还。
    完。
    p 的 颜色 的 r 为 r。
    p 的 颜色 的 g 为 g。
    p 的 颜色 的 b 为 b。
    p 的 颜色 的 a 为 a。
完。

Sk绘制_设颜色32 接受 p、argb：
    若 p 等于 0：
        返还。
    完。
    p 的 颜色 的 a 为 (argb 右移 24 位与 0xFF) 乘 4。
    p 的 颜色 的 r 为 (argb 右移 16 位与 0xFF) 乘 4。
    p 的 颜色 的 g 为 (argb 右移 8 位与 0xFF) 乘 4。
    p 的 颜色 的 b 为 (argb 位与 0xFF) 乘 4。
完。

Sk绘制_设透明度 接受 p、alpha_Q10：
    若 p 等于 0：
        返还。
    完。
    p 的 颜色 的 a 为 alpha_Q10。
完。

Sk绘制_是不透明 接受 p：
    若 p 等于 0：
        返还 真。
    完。
    若 p 的 颜色 的 a 小于 1024：
        返还 假。
    完。
    若 p 的 着色器 不等于 0：
        返还 假。
    完。
    返还 真。
完。

Nova缓冲lend_计算 接受 模式、Sc、Dc、Sa、Da：
    做 匹配 用 模式。
完。

Nova缓冲lend_像素 接受 模式、src_rgba、dst_rgba：
    Sa 为 src_rgba 右移 24 位与 0xFF。
    Sr 为 src_rgba 右移 16 位与 0xFF。
    Sg 为 src_rgba 右移 8 位与 0xFF。
    Sb 为 src_rgba 位与 0xFF。
    Da 为 dst_rgba 右移 24 位与 0xFF。
    Dr 为 dst_rgba 右移 16 位与 0xFF。
    Dg 为 dst_rgba 右移 8 位与 0xFF。
    Db 为 dst_rgba 位与 0xFF。
    sa 为 Sa 乘 4。
    sr 为 Sr 乘 4。
    sg 为 Sg 乘 4。
    sb 为 Sb 乘 4。
    da 为 Da 乘 4。
    dr 为 Dr 乘 4。
    dg 为 Dg 乘 4。
    db 为 Db 乘 4。
    oa 为 做 Nova缓冲lend_计算 用 模式、sa、da、sa、da。
    or_ 为 做 Nova缓冲lend_计算 用 模式、sr、dr、sa、da。
    og 为 做 Nova缓冲lend_计算 用 模式、sg、dg、sa、da。
    ob 为 做 Nova缓冲lend_计算 用 模式、sb、db、sa、da。
    oa 为 oa 除以 4。
    若 oa 大于 255：
        oa 为 255。
    完。
    若 oa 小于 0：
        oa 为 0。
    完。
    or_ 为 or_ 除以 4。
    若 or_ 大于 255：
        or_ 为 255。
    完。
    若 or_ 小于 0：
        or_ 为 0。
    完。
    og 为 og 除以 4。
    若 og 大于 255：
        og 为 255。
    完。
    若 og 小于 0：
        og 为 0。
    完。
    ob 为 ob 除以 4。
    若 ob 大于 255：
        ob 为 255。
    完。
    若 ob 小于 0：
        ob 为 0。
    完。
    返还 oa 左移 24 位或 or_ 左移 16 位或 og 左移 8 位或 ob。
完。

本章 收束。
