《样式滤镜》

引用《.·样式解析》。

引用《.·样式计算》。

引用《.·样式动画》。

引用《.·样式规则》。

引用《.·抗锯齿扫描》。

引用《..·执行·控制台输出》。

引用《..·标准·原生字符串》。

引用《.·CSS颜色语义》。

引用《.·SkPath路径创建》。

引用《..·中枢·辅助存根》。

引用《.·绘制画布绘》。

引用《..·中枢·字符串替换》。

引用《.·内核直连层》。

引用《..·中枢·内存初始化》。

引用《.·CSS颜色解析》。

引用《.·绘制矩形创建》。

引用《..·中枢·整数转字符串》。

引用《.·样式子网格》。

引用《.·CSS颜色混合》。

引用《..·中枢·最大池化与RNN》。

引用《.·画布双像素》。

引用《..·外联·URL解析辅助》。

引用《.·线性渐变光栅》。

滤镜档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、模糊数、亮度数、对比数、饱和数、色相数、阴影数、混合数、色混数、选择器数、玻璃包数、成功数、失败数、最近目标、最近滤镜、最近颜色。

滤镜请求 是 一种 事物，含 动作、目标、参数、选项。

滤镜回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

滤镜摘要 是 一种 事物，含 名称、模糊数、亮度数、对比数、饱和数、色相数、阴影数、混合数、色混数、选择器数、玻璃包数、成功数、失败数、健康。

滤镜项 是 一种 事物，含 类型、目标、值、内容、备注。

可变 滤镜账本 为 一组：（空）。

可变 滤镜构建数 为 0。

可变 滤镜总动作 为 0。

可变 滤镜总成功 为 0。

可变 滤镜总失败 为 0。

样式滤镜_建档 接受 名称：
    档 为 新建 滤镜档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 模糊数 为 0。
    档 的 亮度数 为 0。
    档 的 对比数 为 0。
    档 的 饱和数 为 0。
    档 的 色相数 为 0。
    档 的 阴影数 为 0。
    档 的 混合数 为 0。
    档 的 色混数 为 0。
    档 的 选择器数 为 0。
    档 的 玻璃包数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    档 的 最近目标 为 「」。
    档 的 最近滤镜 为 「」。
    档 的 最近颜色 为 「」。
    返还 档。
完。

样式滤镜_登记 接受 档：
    做 追加 用 滤镜账本、档。
    滤镜构建数 为 滤镜构建数 加 1。
    返还 档。
完。

样式滤镜_完整统计 接受 零参：
    返还 一组：滤镜构建数、滤镜总动作、滤镜总成功、滤镜总失败。
完。

样式滤镜_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 滤镜回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

样式滤镜_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    滤镜总动作 为 滤镜总动作 加 1。
完。

样式滤镜_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 玻璃包数 等于 0 或者 档 的 选择器数 等于 0：
        档 的 最近健康 为 「预热」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 0：
        档 的 最近健康 为 「监视」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「运行」。
    返还 档 的 最近健康。
完。

样式滤镜_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 档 的 名称 等于 「」：
        返还 假。
    完。
    返还 真。
完。

样式滤镜_成功 接受 档、来源、数据：
    档 的 最近结果 为 数据。
    档 的 成功数 为 档 的 成功数 加 1。
    滤镜总成功 为 滤镜总成功 加 1。
    档 的 最近统计 为 做 样式滤镜_完整统计。
    做 样式滤镜_刷新健康 用 档。
    返还 做 样式滤镜_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

样式滤镜_失败 接受 档、来源、错因：
    若 档 不等于 空：
        档 的 最近错误 为 错因。
        档 的 失败数 为 档 的 失败数 加 1。
        档 的 最近统计 为 做 样式滤镜_完整统计。
        做 样式滤镜_刷新健康 用 档。
    完。
    滤镜总失败 为 滤镜总失败 加 1。
    返还 做 样式滤镜_建回证 用 真、假、来源、错因、空、做 样式滤镜_完整统计。
完。

样式滤镜_建项 接受 类型、目标、值、内容、备注：
    项 为 新建 滤镜项。
    项 的 类型 为 类型。
    项 的 目标 为 目标。
    项 的 值 为 值。
    项 的 内容 为 内容。
    项 的 备注 为 备注。
    返还 项。
完。

样式滤镜_规则 接受 选择器、键、值：
    返还 选择器 加 「{」 加 键 加 「:」 加 值 加 「;}\n」。
完。

样式滤镜_滤镜规则 接受 选择器、值：
    返还 选择器 加 「{filter:」 加 值 加 「;}\n」。
完。

样式滤镜_混合规则 接受 选择器、值：
    返还 选择器 加 「{mix-blend-mode:」 加 值 加 「;}\n」。
完。

样式滤镜_背板规则 接受 选择器、值：
    返还 选择器 加 「{backdrop-filter:」 加 值 加 「;-webkit-backdrop-filter:」 加 值 加 「;}\n」。
完。

样式滤镜_模糊 接受 档、目标、半径：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「模糊」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「模糊」、一组：目标、半径、「样式滤镜」。
    档 的 模糊数 为 档 的 模糊数 加 1。
    档 的 最近目标 为 目标。
    档 的 最近滤镜 为 「blur(」 加 半径 加 「px)」。
    返还 做 样式滤镜_成功 用 档、「模糊」、做 样式滤镜_建项 用 「模糊」、目标、半径、做 样式滤镜_滤镜规则 用 目标、档 的 最近滤镜、「景深软化」。
完。

样式滤镜_亮度 接受 档、目标、百分比：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「亮度」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「亮度」、一组：目标、百分比、「样式滤镜」。
    档 的 亮度数 为 档 的 亮度数 加 1。
    档 的 最近目标 为 目标。
    档 的 最近滤镜 为 「brightness(」 加 百分比 加 「%)」。
    返还 做 样式滤镜_成功 用 档、「亮度」、做 样式滤镜_建项 用 「亮度」、目标、百分比、做 样式滤镜_滤镜规则 用 目标、档 的 最近滤镜、「提升亮区」。
完。

样式滤镜_对比 接受 档、目标、百分比：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「对比」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「对比」、一组：目标、百分比、「样式滤镜」。
    档 的 对比数 为 档 的 对比数 加 1。
    档 的 最近目标 为 目标。
    档 的 最近滤镜 为 「contrast(」 加 百分比 加 「%)」。
    返还 做 样式滤镜_成功 用 档、「对比」、做 样式滤镜_建项 用 「对比」、目标、百分比、做 样式滤镜_滤镜规则 用 目标、档 的 最近滤镜、「增强层次」。
完。

样式滤镜_饱和 接受 档、目标、百分比：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「饱和」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「饱和」、一组：目标、百分比、「样式滤镜」。
    档 的 饱和数 为 档 的 饱和数 加 1。
    档 的 最近目标 为 目标。
    档 的 最近滤镜 为 「saturate(」 加 百分比 加 「%)」。
    返还 做 样式滤镜_成功 用 档、「饱和」、做 样式滤镜_建项 用 「饱和」、目标、百分比、做 样式滤镜_滤镜规则 用 目标、档 的 最近滤镜、「控制纯度」。
完。

样式滤镜_色相 接受 档、目标、角度：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「色相」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「色相」、一组：目标、角度、「样式滤镜」。
    档 的 色相数 为 档 的 色相数 加 1。
    档 的 最近目标 为 目标。
    档 的 最近滤镜 为 「hue-rotate(」 加 角度 加 「deg)」。
    返还 做 样式滤镜_成功 用 档、「色相」、做 样式滤镜_建项 用 「色相」、目标、角度、做 样式滤镜_滤镜规则 用 目标、档 的 最近滤镜、「转动色轮」。
完。

样式滤镜_阴影 接受 档、目标、横向、纵向、模糊、颜色：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「阴影」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「阴影」、一组：目标、横向、纵向、模糊、颜色、「样式滤镜」。
    档 的 阴影数 为 档 的 阴影数 加 1。
    档 的 最近目标 为 目标。
    档 的 最近颜色 为 颜色。
    档 的 最近滤镜 为 「drop-shadow(」 加 横向 加 「px 」 加 纵向 加 「px 」 加 模糊 加 「px 」 加 颜色 加 「)」。
    返还 做 样式滤镜_成功 用 档、「阴影」、做 样式滤镜_建项 用 「阴影」、目标、颜色、做 样式滤镜_滤镜规则 用 目标、档 的 最近滤镜、「柔和浮层」。
完。

样式滤镜_混合 接受 档、目标、模式：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「混合」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「混合」、一组：目标、模式、「样式滤镜」。
    档 的 混合数 为 档 的 混合数 加 1。
    档 的 最近目标 为 目标。
    返还 做 样式滤镜_成功 用 档、「混合」、做 样式滤镜_建项 用 「混合」、目标、模式、做 样式滤镜_混合规则 用 目标、模式、「图层混合」。
完。

样式滤镜_色混 接受 档、目标、起色、终色、比例：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「色混」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「色混」、一组：目标、起色、终色、比例、「样式滤镜」。
    档 的 色混数 为 档 的 色混数 加 1。
    档 的 最近目标 为 目标。
    档 的 最近颜色 为 起色 加 「->」 加 终色。
    值 为 「color-mix(in srgb, 」 加 起色 加 「 」 加 比例 加 「%, 」 加 终色 加 「)」。
    内容 为 做 样式滤镜_规则 用 目标、「background」、值。
    返还 做 样式滤镜_成功 用 档、「色混」、做 样式滤镜_建项 用 「色混」、目标、比例、内容、「现代色彩混合」。
完。

样式滤镜_滤镜链 接受 档、目标、模糊值、亮度值、对比值、饱和值：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「滤镜链」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「滤镜链」、一组：目标、模糊值、亮度值、对比值、饱和值、「样式滤镜」。
    值 为 「blur(」 加 模糊值 加 「px) brightness(」 加 亮度值 加 「%) contrast(」 加 对比值 加 「%) saturate(」 加 饱和值 加 「%)」。
    档 的 模糊数 为 档 的 模糊数 加 1。
    档 的 亮度数 为 档 的 亮度数 加 1。
    档 的 对比数 为 档 的 对比数 加 1。
    档 的 饱和数 为 档 的 饱和数 加 1。
    档 的 最近目标 为 目标。
    档 的 最近滤镜 为 值。
    返还 做 样式滤镜_成功 用 档、「滤镜链」、做 样式滤镜_建项 用 「滤镜链」、目标、值、做 样式滤镜_滤镜规则 用 目标、值、「多段滤镜链」。
完。

样式滤镜_子网格信号 接受 档、目标、主轴、次轴：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「子网格信号」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「子网格信号」、一组：目标、主轴、次轴、「样式滤镜」。
    档 的 选择器数 为 档 的 选择器数 加 1。
    内容 为 目标 加 「{display:grid;grid-template-columns:subgrid;grid-template-rows:subgrid;}\n」。
    内容 为 内容 加 目标 加 「{--主轴:」 加 主轴 加 「;--次轴:」 加 次轴 加 「;}\n」。
    返还 做 样式滤镜_成功 用 档、「子网格信号」、做 样式滤镜_建项 用 「子网格」、目标、主轴 加 「,」 加 次轴、内容、「布局信号联动」。
完。

样式滤镜_选择器应用 接受 档、选择器、滤镜值、背板值、过渡时长：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「选择器应用」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「选择器应用」、一组：选择器、滤镜值、背板值、过渡时长、「样式滤镜」。
    档 的 选择器数 为 档 的 选择器数 加 1。
    档 的 最近目标 为 选择器。
    档 的 最近滤镜 为 滤镜值。
    内容 为 做 样式滤镜_滤镜规则 用 选择器、滤镜值。
    内容 为 内容 加 做 样式滤镜_背板规则 用 选择器、背板值。
    内容 为 内容 加 做 样式滤镜_规则 用 选择器、「transition」、「filter 」 加 过渡时长 加 「ms ease, backdrop-filter 」 加 过渡时长 加 「ms ease」。
    返还 做 样式滤镜_成功 用 档、「选择器应用」、做 样式滤镜_建项 用 「选择器」、选择器、过渡时长、内容、「结构化应用」。
完。

样式滤镜_白璃包 接受 档：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「白璃包」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「白璃包」、一组：（空）、「样式滤镜」。
    规则档 为 做 样式规则_登记 用 做 样式规则_建档 用 「白璃规则」。
    做 样式规则_初始化 用 规则档。
    现代 为 做 样式规则_现代包 用 规则档 的 数据。
    胶囊 为 做 样式滤镜_选择器应用 用 档、「.白璃胶囊」、「blur(18px) brightness(106%) contrast(108%)」、「blur(20px)」、220 的 数据。
    卡片 为 做 样式滤镜_滤镜链 用 档、「.白璃卡片」、「10」、「104」、「112」、「112」 的 数据。
    阴影 为 做 样式滤镜_阴影 用 档、「.白璃卡片」、「0」、「20」、「48」、「rgba(177,190,214,0.26)」 的 数据。
    色混 为 做 样式滤镜_色混 用 档、「.白璃桌面」、「rgba(255,255,255,0.90)」、「rgba(224,233,245,0.72)」、「72」 的 数据。
    混合 为 做 样式滤镜_混合 用 档、「.白璃高光」、「screen」 的 数据。
    子网格 为 做 样式滤镜_子网格信号 用 档、「.白璃主区」、「层次稳定」、「卡片呼吸」 的 数据。
    档 的 玻璃包数 为 档 的 玻璃包数 加 1。
    内容 为 现代 加 胶囊 的 内容 加 卡片 的 内容 加 阴影 的 内容 加 色混 的 内容 加 混合 的 内容 加 子网格 的 内容。
    内容 为 内容 加 「.白璃窗{background:rgba(255,255,255,0.76);border:1px solid rgba(255,255,255,0.68);box-shadow:0 22px 60px rgba(160,176,198,0.24);}\n」。
    内容 为 内容 加 「.白璃桌面{background:linear-gradient(180deg,rgba(248,250,253,1),rgba(234,240,247,1));}\n」。
    返还 做 样式滤镜_成功 用 档、「白璃包」、做 样式滤镜_建项 用 「白璃包」、「.白璃桌面」、「现代玻璃」、内容、「现代白玻璃界面」。
完。

样式滤镜_摘要 接受 档：
    摘要 为 新建 滤镜摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 模糊数 为 档 的 模糊数。
    摘要 的 亮度数 为 档 的 亮度数。
    摘要 的 对比数 为 档 的 对比数。
    摘要 的 饱和数 为 档 的 饱和数。
    摘要 的 色相数 为 档 的 色相数。
    摘要 的 阴影数 为 档 的 阴影数。
    摘要 的 混合数 为 档 的 混合数。
    摘要 的 色混数 为 档 的 色混数。
    摘要 的 选择器数 为 档 的 选择器数。
    摘要 的 玻璃包数 为 档 的 玻璃包数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 样式滤镜_刷新健康 用 档。
    返还 摘要。
完。

样式滤镜_初始化 接受 档：
    若 非 做 样式滤镜_检查档 用 档：
        返还 做 样式滤镜_失败 用 档、「初始化」、「滤镜档不可用」。
    完。
    做 样式滤镜_记录 用 档、「初始化」、一组：（空）、「样式滤镜」。
    档 的 是否初始化 为 真。
    返还 做 样式滤镜_成功 用 档、「初始化」、「已初始化」。
完。

样式滤镜_自检 接受 档：
    若 非 做 样式滤镜_检查档 用 档：
        返还 假。
    完。
    若 做 样式滤镜_初始化 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 样式滤镜_模糊 用 档、「.卡片」、「12」 的 已成 等于 假：
        返还 假。
    完。
    若 做 样式滤镜_亮度 用 档、「.卡片」、「104」 的 已成 等于 假：
        返还 假。
    完。
    若 做 样式滤镜_对比 用 档、「.卡片」、「108」 的 已成 等于 假：
        返还 假。
    完。
    若 做 样式滤镜_饱和 用 档、「.卡片」、「110」 的 已成 等于 假：
        返还 假。
    完。
    若 做 样式滤镜_色相 用 档、「.高光」、「8」 的 已成 等于 假：
        返还 假。
    完。
    若 做 样式滤镜_阴影 用 档、「.卡片」、「0」、「18」、「42」、「rgba(170,184,205,0.24)」 的 已成 等于 假：
        返还 假。
    完。
    若 做 样式滤镜_混合 用 档、「.高光」、「screen」 的 已成 等于 假：
        返还 假。
    完。
    若 做 样式滤镜_色混 用 档、「.桌面」、「rgba(255,255,255,0.92)」、「rgba(233,240,248,0.76)」、「68」 的 已成 等于 假：
        返还 假。
    完。
    若 做 样式滤镜_选择器应用 用 档、「.白璃窗」、「blur(20px) brightness(106%)」、「blur(18px)」、220 的 已成 等于 假：
        返还 假。
    完。
    若 做 样式滤镜_白璃包 用 档 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

样式滤镜_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 样式滤镜_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        返还 做 样式滤镜_初始化 用 档。
    完。
    若 请求 的 动作 等于 「模糊」：
        返还 做 样式滤镜_模糊 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「亮度」：
        返还 做 样式滤镜_亮度 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「对比」：
        返还 做 样式滤镜_对比 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「饱和」：
        返还 做 样式滤镜_饱和 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「色相」：
        返还 做 样式滤镜_色相 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「阴影」：
        返还 做 样式滤镜_阴影 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项、请求 的 参数 的 第 4 项。
    完。
    若 请求 的 动作 等于 「混合」：
        返还 做 样式滤镜_混合 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「色混」：
        返还 做 样式滤镜_色混 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「滤镜链」：
        返还 做 样式滤镜_滤镜链 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项、请求 的 参数 的 第 4 项。
    完。
    若 请求 的 动作 等于 「子网格信号」：
        返还 做 样式滤镜_子网格信号 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「选择器应用」：
        返还 做 样式滤镜_选择器应用 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「白璃包」：
        返还 做 样式滤镜_白璃包 用 档。
    完。
    若 请求 的 动作 等于 「摘要」：
        返还 做 样式滤镜_成功 用 档、「摘要」、做 样式滤镜_摘要 用 档。
    完。
    返还 做 样式滤镜_失败 用 档、「处理」、「未知动作」。
完。

SkStroke_描边路径 接受 原始路径、描边宽度、端帽、连接：
    半宽 为 描边宽度 除以 2。
    结果 为 做 SkPath_创建。
    可变 点idx 为 0。
    可变 动词idx 为 0。
    可变 prev_x 为 0。
    可变 prev_y 为 0。
    只要 动词idx 小于 原始路径 的 动词数量：
        动词 为 做 内存_读8 用 原始路径 的 动词列表 加 动词idx。
        若 动词 等于 路径动词_移动：
            prev_x 为 做 内存_读32 用 原始路径 的 点列表 加 点idx 乘 8。
            prev_y 为 做 内存_读32 用 原始路径 的 点列表 加 点idx 乘 8 加 4。
            点idx 为 点idx 加 1。
        否则若 动词 等于 路径动词_直线：
            cur_x 为 做 内存_读32 用 原始路径 的 点列表 加 点idx 乘 8。
            cur_y 为 做 内存_读32 用 原始路径 的 点列表 加 点idx 乘 8 加 4。
            dx 为 cur_x 减 prev_x。
            dy 为 cur_y 减 prev_y。
            长度 为 做 整数_sqrt 用 dx 乘 dx 加 dy 乘 dy。
            若 长度 大于 0：
                nx 为 0 减 dy 乘 半宽 除以 长度。
                ny 为 dx 乘 半宽 除以 长度。
                做 SkPath_moveTo 用 结果、prev_x 加 nx、prev_y 加 ny。
                做 SkPath_lineTo 用 结果、cur_x 加 nx、cur_y 加 ny。
                做 SkPath_lineTo 用 结果、cur_x 减 nx、cur_y 减 ny。
                做 SkPath_lineTo 用 结果、prev_x 减 nx、prev_y 减 ny。
                做 SkPath_close 用 结果。
                若 端帽 等于 Sk绘制端帽_圆：
                    做 SkPath_addArc 用 结果、cur_x、cur_y、半宽、0、3600。
                完。
            完。
            prev_x 为 cur_x。
            prev_y 为 cur_y。
            点idx 为 点idx 加 1。
        完。
        动词idx 为 动词idx 加 1。
    完。
    返还 结果。
完。

整数_sqrt 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    若 n 等于 1：
        返还 1。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

Sk画布_drawPath_完整 接受 c、path、paint：
    c 的 绘制调用数 为 c 的 绘制调用数 加 1。
    bm 为 c 的 位图。
    若 paint 的 风格 等于 Sk绘制风格_填充 或者 paint 的 风格 等于 Sk绘制风格_填充描边：
        若 paint 的 抗锯齿：
            做 SkScan_抗锯齿填充路径 用 bm、path、paint。
        否则：
            做 SkScan_填充路径 用 bm、path、paint。
        完。
    完。
    若 paint 的 风格 等于 Sk绘制风格_描边 或者 paint 的 风格 等于 Sk绘制风格_填充描边：
        描边路径 为 做 SkStroke_描边路径 用 path、paint 的 描边宽度、paint 的 端帽、paint 的 连接。
        若 paint 的 抗锯齿：
            做 SkScan_抗锯齿填充路径 用 bm、描边路径、paint。
        否则：
            做 SkScan_填充路径 用 bm、描边路径、paint。
        完。
    完。
完。

Sk画布_drawRRect_完整 接受 c、rrect、paint：
    c 的 绘制调用数 为 c 的 绘制调用数 加 1。
    做 SkScan_填充圆角矩形 用 c 的 位图、rrect、paint。
完。

Sk画布_drawCircle 接受 c、cx、cy、半径、paint：
    rect 为 做 SkRect_创建 用 cx 减 半径、cy 减 半径、cx 加 半径、cy 加 半径。
    rrect 为 做 SkRRect_创建 用 rect、半径。
    做 Sk画布_drawRRect_完整 用 c、rrect、paint。
完。

Sk画布_drawOval 接受 c、rect、paint：
    path 为 做 SkPath_创建。
    cx 为 (rect 的 左 加 rect 的 右) 除以 2。
    cy 为 (rect 的 上 加 rect 的 下) 除以 2。
    rx 为 (rect 的 右 减 rect 的 左) 除以 2。
    ry 为 (rect 的 下 减 rect 的 上) 除以 2。
    kx 为 rx 乘 5523 除以 10000。
    ky 为 ry 乘 5523 除以 10000。
    做 SkPath_moveTo 用 path、cx 加 rx、cy。
    做 SkPath_Nova三次方To 用 path、cx 加 rx、cy 加 ky、cx 加 kx、cy 加 ry、cx、cy 加 ry。
    做 SkPath_Nova三次方To 用 path、cx 减 kx、cy 加 ry、cx 减 rx、cy 加 ky、cx 减 rx、cy。
    做 SkPath_Nova三次方To 用 path、cx 减 rx、cy 减 ky、cx 减 kx、cy 减 ry、cx、cy 减 ry。
    做 SkPath_Nova三次方To 用 path、cx 加 kx、cy 减 ry、cx 加 rx、cy 减 ky、cx 加 rx、cy。
    做 SkPath_close 用 path。
    做 Sk画布_drawPath_完整 用 c、path、paint。
完。

PAINT_STYLE_FILL 为 0。

PAINT_STYLE_STROKE 为 1。

PAINT_STYLE_FILL_STROKE 为 2。

STROKE_CAP_BUTT 为 0。

STROKE_CAP_ROUND 为 1。

STROKE_CAP_SQUARE 为 2。

STROKE_JOIN_MITER 为 0。

STROKE_JOIN_ROUND 为 1。

STROKE_JOIN_BEVEL 为 2。

BLEND_CLEAR 为 0。

BLEND_SRC 为 1。

BLEND_DST 为 2。

BLEND_SRC_OVER 为 3。

BLEND_DST_OVER 为 4。

BLEND_SRC_IN 为 5。

BLEND_DST_IN 为 6。

BLEND_SRC_OUT 为 7。

BLEND_DST_OUT 为 8。

BLEND_SRC_ATOP 为 9。

BLEND_DST_ATOP 为 10。

BLEND_XOR 为 11。

BLEND_PLUS 为 12。

BLEND_MODULATE 为 13。

BLEND_SCREEN 为 14。

BLEND_OVERLAY 为 15。

BLEND_DARKEN 为 16。

BLEND_LIGHTEN 为 17。

BLEND_COLOR_DODGE 为 18。

BLEND_COLOR_BURN 为 19。

BLEND_HARD_LIGHT 为 20。

BLEND_SOFT_LIGHT 为 21。

BLEND_DIFFERENCE 为 22。

BLEND_EXCLUSION 为 23。

BLEND_MULTIPLY 为 24。

BLEND_HUE 为 25。

BLEND_SATURATION 为 26。

BLEND_COLOR 为 27。

BLEND_LUMINOSITY 为 28。

FILTER_NONE 为 0。

FILTER_LOW 为 1。

FILTER_MEDIUM 为 2。

FILTER_HIGH 为 3。

SkColor4f 是 一种 事物，含 r、g、b、a。

SkColor4f_从ARGB 接受 a、r、g、b：
    c 为 做 堆_分配 用 SkColor4f。
    c 的 r 为 r 乘 4。
    c 的 g 为 g 乘 4。
    c 的 b 为 b 乘 4。
    c 的 a 为 a 乘 4。
    返还 c。
完。

SkColor4f_从打包 接受 颜色32：
    c 为 做 堆_分配 用 SkColor4f。
    c 的 a 为 (颜色32 右移 24 位与 0xFF) 乘 4。
    c 的 r 为 (颜色32 右移 16 位与 0xFF) 乘 4。
    c 的 g 为 (颜色32 右移 8 位与 0xFF) 乘 4。
    c 的 b 为 (颜色32 位与 0xFF) 乘 4。
    返还 c。
完。

SkColor4f_打包 接受 c：
    若 c 等于 0：
        返还 0xFF000000。
    完。
    a 为 c 的 a 除以 4。
    若 a 大于 255：
        a 为 255。
    完。
    r 为 c 的 r 除以 4。
    若 r 大于 255：
        r 为 255。
    完。
    g 为 c 的 g 除以 4。
    若 g 大于 255：
        g 为 255。
    完。
    b 为 c 的 b 除以 4。
    若 b 大于 255：
        b 为 255。
    完。
    返还 a 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
完。

SkColor4f_预乘 接受 c：
    若 c 等于 0：
        返还。
    完。
    c 的 r 为 c 的 r 乘 c 的 a 除以 1024。
    c 的 g 为 c 的 g 乘 c 的 a 除以 1024。
    c 的 b 为 c 的 b 乘 c 的 a 除以 1024。
完。

SkColor4f_反预乘 接受 c：
    若 c 等于 0 或者 c 的 a 等于 0：
        返还。
    完。
    c 的 r 为 c 的 r 乘 1024 除以 c 的 a。
    c 的 g 为 c 的 g 乘 1024 除以 c 的 a。
    c 的 b 为 c 的 b 乘 1024 除以 c 的 a。
完。

RadialGradient 是 一种 事物，含 中心x、中心y、半径、色标、色标数。

RadialGradient_创建 接受 cx、cy、半径：
    g 为 做 堆_分配 用 RadialGradient。
    g 的 中心x 为 cx。
    g 的 中心y 为 cy。
    g 的 半径 为 半径。
    g 的 色标 为 做 堆_分配_数组 用 16 乘 8。
    g 的 色标数 为 0。
    返还 g。
完。

RadialGradient_填充矩形 接受 bm、gradient、rect：
    cx 为 gradient 的 中心x 除以 100。
    cy 为 gradient 的 中心y 除以 100。
    半径 为 gradient 的 半径 除以 100。
    若 半径 等于 0：
        返还。
    完。
    x1 为 rect 的 左 除以 100。
    y1 为 rect 的 上 除以 100。
    x2 为 rect 的 右 除以 100。
    y2 为 rect 的 下 除以 100。
    若 x1 小于 0：
        x1 为 0。
    完。
    若 y1 小于 0：
        y1 为 0。
    完。
    若 x2 大于 bm 的 宽：
        x2 为 bm 的 宽。
    完。
    若 y2 大于 bm 的 高：
        y2 为 bm 的 高。
    完。
    可变 y 为 y1。
    只要 y 小于 y2：
        可变 x 为 x1。
        只要 x 小于 x2：
            dx 为 x 减 cx。
            dy 为 y 减 cy。
            dist_sq 为 dx 乘 dx 加 dy 乘 dy。
            r_sq 为 半径 乘 半径。
            t 为 dist_sq 乘 10000 除以 r_sq。
            dist 为 做 整数_平方根 用 dist_sq。
            颜色 为 做 LinearGradient_采样_通用 用 gradient 的 色标、gradient 的 色标数、t。
            做 Sk画布_混合像素 用 bm、x、y、颜色、混合_SrcOver。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
完。

LinearGradient_采样_通用 接受 色标、色标数、t：
    若 色标数 等于 0：
        返还 0xFF000000。
    完。
    可变 ct 为 t。
    若 ct 小于 0：
        ct 为 0。
    完。
    若 ct 大于 10000：
        ct 为 10000。
    完。
    可变 i 为 0。
    只要 i 小于 色标数 减 1：
        pos0 为 做 内存_读32 用 色标 加 i 乘 8。
        pos1 为 做 内存_读32 用 色标 加 (i 加 1) 乘 8。
        若 ct 大于或等于 pos0 并且 ct 小于或等于 pos1：
            c0 为 做 内存_读32 用 色标 加 i 乘 8 加 4。
            c1 为 做 内存_读32 用 色标 加 (i 加 1) 乘 8 加 4。
            range 为 pos1 减 pos0。
            若 range 等于 0：
                返还 c0。
            完。
            返还 做 颜色_线性插值 用 c0、c1、(ct 减 pos0) 乘 256 除以 range。
        完。
        i 为 i 加 1。
    完。
    返还 做 内存_读32 用 色标 加 (色标数 减 1) 乘 8 加 4。
完。

SkScan_填充圆角矩形 接受 bm、rrect、paint：
    rect 为 rrect 的 矩形。
    rx 为 rrect 的 圆角_左上 除以 100。
    若 rx 小于或等于 0：
        做 Sk画布_drawRect_内部 用 bm、rect、paint。
        返还。
    完。
    path 为 做 SkPath_创建。
    做 SkPath_addRoundRect 用 path、rect、rrect 的 圆角_左上、rrect 的 圆角_左上。
    若 paint 的 抗锯齿：
        做 SkScan_抗锯齿填充路径 用 bm、path、paint。
    否则：
        做 SkScan_填充路径 用 bm、path、paint。
    完。
完。

Sk画布_drawRect_内部 接受 bm、rect、paint：
    颜色 为 paint 的 颜色。
    x1 为 rect 的 左 除以 100。
    y1 为 rect 的 上 除以 100。
    x2 为 rect 的 右 除以 100。
    y2 为 rect 的 下 除以 100。
    若 x1 小于 0：
        x1 为 0。
    完。
    若 y1 小于 0：
        y1 为 0。
    完。
    若 x2 大于 bm 的 宽：
        x2 为 bm 的 宽。
    完。
    若 y2 大于 bm 的 高：
        y2 为 bm 的 高。
    完。
    可变 y 为 y1。
    只要 y 小于 y2：
        可变 x 为 x1。
        只要 x 小于 x2：
            做 Sk画布_混合像素 用 bm、x、y、颜色、paint 的 混合模式。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
完。

SkColor 接受 a、r、g、b：
    返还 a 乘 0x1000000 加 r 乘 0x10000 加 g 乘 0x100 加 b。
完。

SkColor_A 接受 颜色：
    返还 颜色 除以 0x1000000 取余于 256。
完。

SkColor_R 接受 颜色：
    返还 颜色 除以 0x10000 取余于 256。
完。

SkColor_G 接受 颜色：
    返还 颜色 除以 0x100 取余于 256。
完。

SkColor_B 接受 颜色：
    返还 颜色 取余于 256。
完。

SkColor_从RGBA 接受 rgba：
    r 为 rgba 除以 0x1000000 取余于 256。
    g 为 rgba 除以 0x10000 取余于 256。
    b 为 rgba 除以 0x100 取余于 256。
    a 为 rgba 取余于 256。
    返还 做 SkColor 用 a、r、g、b。
完。

SK_ColorBLACK 为 0xFF000000。

SK_ColorWHITE 为 0xFFFFFFFF。

SK_ColorRED 为 0xFFFF0000。

SK_ColorGREEN 为 0xFF00FF00。

SK_ColorBLUE 为 0xFF0000FF。

SK_ColorTRANSPARENT 为 0x00000000。

Sk绘制风格_填充 为 0。

Sk绘制风格_描边 为 1。

Sk绘制风格_填充描边 为 2。

Sk绘制端帽_平 为 0。

Sk绘制端帽_圆 为 1。

Sk绘制端帽_方 为 2。

Sk绘制连接_尖 为 0。

Sk绘制连接_圆 为 1。

Sk绘制连接_斜 为 2。

混合_Clear 为 0。

混合_Src 为 1。

混合_Dst 为 2。

混合_SrcOver 为 3。

混合_DstOver 为 4。

混合_SrcIn 为 5。

混合_DstIn 为 6。

混合_SrcOut 为 7。

混合_DstOut 为 8。

混合_SrcATop 为 9。

混合_DstATop 为 10。

混合_Xor 为 11。

混合_Multiply 为 12。

混合_Screen 为 13。

混合_Overlay 为 14。

Sk绘制 是 一种 事物，含 颜色、风格、描边宽度、端帽、连接、斜接限制、抗锯齿、混合模式、透明度、着色器、遮罩滤镜、路径效果、颜色滤镜、图像滤镜。

Sk绘制_创建 接受 零参：
    p 为 做 堆_分配 用 Sk绘制。
    p 的 颜色 为 SK_ColorBLACK。
    p 的 风格 为 Sk绘制风格_填充。
    p 的 描边宽度 为 0。
    p 的 端帽 为 Sk绘制端帽_平。
    p 的 连接 为 Sk绘制连接_尖。
    p 的 斜接限制 为 400。
    p 的 抗锯齿 为 真。
    p 的 混合模式 为 混合_SrcOver。
    p 的 透明度 为 255。
    p 的 着色器 为 0。
    p 的 遮罩滤镜 为 0。
    p 的 路径效果 为 0。
    p 的 颜色滤镜 为 0。
    p 的 图像滤镜 为 0。
    返还 p。
完。

Sk绘制_设置颜色 接受 p、颜色：
    p 的 颜色 为 颜色。
完。

Sk绘制_设置风格 接受 p、风格：
    p 的 风格 为 风格。
完。

Sk绘制_设置描边宽度 接受 p、宽度：
    p 的 描边宽度 为 宽度。
完。

Sk绘制_设置抗锯齿 接受 p、开：
    p 的 抗锯齿 为 开。
完。

Sk绘制_设置混合模式 接受 p、模式：
    p 的 混合模式 为 模式。
完。

ClipboardItem 是 一种 事物，含 MIME类型、数据、数据长度、类型数。

Clipboard 是 一种 事物，含 文本、文本长度、HTML、HTML长度、图像、图像长度、读权限、写权限。

可变 全局剪贴板 为 0。

Clipboard_获取 接受 零参：
    若 全局剪贴板 等于 0：
        全局剪贴板 为 做 堆_分配 用 Clipboard。
        全局剪贴板 的 文本 为 0。
        全局剪贴板 的 文本长度 为 0。
        全局剪贴板 的 HTML 为 0。
        全局剪贴板 的 HTML长度 为 0。
        全局剪贴板 的 图像 为 0。
        全局剪贴板 的 图像长度 为 0。
        全局剪贴板 的 读权限 为 假。
        全局剪贴板 的 写权限 为 假。
    完。
    返还 全局剪贴板。
完。

Clipboard_writeText 接受 文本：
    cb 为 做 Clipboard_获取。
    若 cb 等于 0 或者 文本 等于 0：
        返还。
    完。
    若 非 做 Clipboard_检查写权限 用 cb：
        返还。
    完。
    cb 的 文本 为 做 字符串_复制 用 文本。
    cb 的 文本长度 为 做 字节长度 用 文本。
完。

Clipboard_readText 接受 零参：
    cb 为 做 Clipboard_获取。
    若 cb 等于 0：
        返还 0。
    完。
    若 非 做 Clipboard_检查读权限 用 cb：
        返还 0。
    完。
    返还 cb 的 文本。
完。

Clipboard_write 接受 items、数量：
    cb 为 做 Clipboard_获取。
    若 cb 等于 0 或者 items 等于 0 或者 非 做 Clipboard_检查写权限 用 cb：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 数量：
        item 为 做 内存_读64 用 items 加 i 乘 8。
        若 item 不等于 0：
            可变 j 为 0。
            只要 j 小于 item 的 类型数：
                若 做 字符串_相等 用 item 的 MIME类型 的 第 j 项、「text/plain」：
                    cb 的 文本 为 item 的 数据 的 第 j 项。
                    cb 的 文本长度 为 item 的 数据长度 的 第 j 项。
                否则若 做 字符串_相等 用 item 的 MIME类型 的 第 j 项、「text/html」：
                    cb 的 HTML 为 item 的 数据 的 第 j 项。
                    cb 的 HTML长度 为 item 的 数据长度 的 第 j 项。
                否则若 做 字符串_相等 用 item 的 MIME类型 的 第 j 项、「image/png」：
                    cb 的 图像 为 item 的 数据 的 第 j 项。
                    cb 的 图像长度 为 item 的 数据长度 的 第 j 项。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

Clipboard_read 接受 零参：
    cb 为 做 Clipboard_获取。
    若 cb 等于 0 或者 非 做 Clipboard_检查读权限 用 cb：
        返还 0。
    完。
    item 为 做 堆_分配 用 ClipboardItem。
    item 的 类型数 为 0。
    若 cb 的 文本 不等于 0：
        item 的 MIME类型 的 第 item 的 类型数 项 为 「text/plain」。
        item 的 数据 的 第 item 的 类型数 项 为 cb 的 文本。
        item 的 数据长度 的 第 item 的 类型数 项 为 cb 的 文本长度。
        item 的 类型数 为 item 的 类型数 加 1。
    完。
    若 cb 的 HTML 不等于 0：
        item 的 MIME类型 的 第 item 的 类型数 项 为 「text/html」。
        item 的 数据 的 第 item 的 类型数 项 为 cb 的 HTML。
        item 的 数据长度 的 第 item 的 类型数 项 为 cb 的 HTML长度。
        item 的 类型数 为 item 的 类型数 加 1。
    完。
    返还 item。
完。

Clipboard_检查读权限 接受 cb：
    若 cb 等于 0：
        返还 假。
    完。
    返还 cb 的 读权限。
完。

Clipboard_检查写权限 接受 cb：
    若 cb 等于 0：
        返还 假。
    完。
    返还 cb 的 写权限。
完。

DataTransfer 是 一种 事物，含 数据项、数据项数、文件列表、文件数、effectAllowed、dropEffect。

EFFECT_NONE 为 0。

EFFECT_COPY 为 1。

EFFECT_MOVE 为 2。

EFFECT_LINK 为 3。

DataTransfer_创建 接受 零参：
    dt 为 新建 DataTransfer。
    dt 的 数据项 为 一组：（空）。
    dt 的 数据项数 为 0。
    dt 的 文件列表 为 一组：（空）。
    dt 的 文件数 为 0。
    dt 的 effectAllowed 为 EFFECT_COPY。
    dt 的 dropEffect 为 EFFECT_NONE。
    返还 dt。
完。

DataTransfer_setData 接受 dt、格式、数据：
    若 dt 等于 0：
        返还。
    完。
    做 追加 用 dt 的 数据项、一本字典：格式 对应 格式、数据 对应 数据。
    dt 的 数据项数 为 dt 的 数据项数 加 1。
完。

DataTransfer_getData 接受 dt、格式：
    若 dt 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 dt 的 数据项数：
        若 做 字符串_相等 用 dt 的 数据项 的 第 i 项 的 格式、格式：
            返还 dt 的 数据项 的 第 i 项 的 数据。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

DataTransfer_clearData 接受 dt：
    若 dt 等于 0：
        返还。
    完。
    dt 的 数据项 为 一组：（空）。
    dt 的 数据项数 为 0。
完。

Selection 是 一种 事物，含 锚节点、锚偏移、焦点节点、焦点偏移、范围列表、范围数、类型。

SEL_NONE 为 0。

SEL_CARET 为 1。

SEL_RANGE 为 2。

Selection_获取 接受 零参：
    sel 为 新建 Selection。
    sel 的 锚节点 为 0。
    sel 的 锚偏移 为 0。
    sel 的 焦点节点 为 0。
    sel 的 焦点偏移 为 0。
    sel 的 范围列表 为 一组：（空）。
    sel 的 范围数 为 0。
    sel 的 类型 为 SEL_NONE。
    返还 sel。
完。

Selection_collapse 接受 sel、节点、偏移：
    若 sel 等于 0：
        返还。
    完。
    sel 的 锚节点 为 节点。
    sel 的 锚偏移 为 偏移。
    sel 的 焦点节点 为 节点。
    sel 的 焦点偏移 为 偏移。
    sel 的 类型 为 SEL_CARET。
完。

Selection_extend 接受 sel、节点、偏移：
    若 sel 等于 0：
        返还。
    完。
    sel 的 焦点节点 为 节点。
    sel 的 焦点偏移 为 偏移。
    sel 的 类型 为 SEL_RANGE。
完。

Selection_removeAllRanges 接受 sel：
    若 sel 等于 0：
        返还。
    完。
    sel 的 范围列表 为 一组：（空）。
    sel 的 范围数 为 0。
    sel 的 类型 为 SEL_NONE。
完。

Selection_是否折叠 接受 sel：
    若 sel 等于 0：
        返还 真。
    完。
    返还 sel 的 锚节点 等于 sel 的 焦点节点 并且 sel 的 锚偏移 等于 sel 的 焦点偏移。
完。

Selection_类型名 接受 类型：
    若 类型 等于 SEL_NONE：
        返还 「None」。
    完。
    若 类型 等于 SEL_CARET：
        返还 「Caret」。
    完。
    若 类型 等于 SEL_RANGE：
        返还 「Range」。
    完。
    返还 「Unknown」。
完。

Range 是 一种 事物，含 起始节点、起始偏移、结束节点、结束偏移、已折叠。

Range_创建 接受 零参：
    r 为 新建 Range。
    r 的 起始节点 为 0。
    r 的 起始偏移 为 0。
    r 的 结束节点 为 0。
    r 的 结束偏移 为 0。
    r 的 已折叠 为 真。
    返还 r。
完。

Range_setStart 接受 r、节点、偏移：
    若 r 等于 0：
        返还。
    完。
    r 的 起始节点 为 节点。
    r 的 起始偏移 为 偏移。
    r 的 已折叠 为 r 的 起始节点 等于 r 的 结束节点 并且 r 的 起始偏移 等于 r 的 结束偏移。
完。

Range_setEnd 接受 r、节点、偏移：
    若 r 等于 0：
        返还。
    完。
    r 的 结束节点 为 节点。
    r 的 结束偏移 为 偏移。
    r 的 已折叠 为 r 的 起始节点 等于 r 的 结束节点 并且 r 的 起始偏移 等于 r 的 结束偏移。
完。

Range_collapse 接受 r、到起始：
    若 r 等于 0：
        返还。
    完。
    若 到起始：
        r 的 结束节点 为 r 的 起始节点。
        r 的 结束偏移 为 r 的 起始偏移。
    否则：
        r 的 起始节点 为 r 的 结束节点。
        r 的 起始偏移 为 r 的 结束偏移。
    完。
    r 的 已折叠 为 真。
完。

可变 剪贴板_写次数 为 0。

可变 剪贴板_读次数 为 0。

可变 拖放_开始数 为 0。

可变 拖放_完成数 为 0。

剪贴拖选_统计 接受 零参：
    返还 一本字典：剪贴板写 对应 剪贴板_写次数、剪贴板读 对应 剪贴板_读次数、拖放开始 对应 拖放_开始数、拖放完成 对应 拖放_完成数。
完。

剪贴拖选_健康检查 接受 零参：
    返还 「剪贴拖选子系统健康」。
完。

剪贴拖选_重置 接受 零参：
    剪贴板_写次数 为 0。
    剪贴板_读次数 为 0。
    拖放_开始数 为 0。
    拖放_完成数 为 0。
完。

剪贴拖选_完整统计 接受 零参：
    返还 一组：剪贴板_写次数、剪贴板_读次数、拖放_开始数、拖放_完成数。
完。

sRGB_去gamma 接受 v：
    若 v 小于或等于 405：
        返还 v 乘 10000 除以 12920。
    完。
    x 为 (v 加 550) 乘 10000 除以 10550。
    x2 为 x 乘 x 除以 10000。
    sqrt_x 为 做 整数_sqrt 用 x 乘 100。
    结果 为 x2 乘 sqrt_x 除以 100。
    返还 结果。
完。

sRGB_加gamma 接受 v：
    若 v 小于或等于 31：
        返还 v 乘 12920 除以 10000。
    完。
    若 v 小于 0：
        返还 0。
    完。
    sqrt_v 为 做 整数_sqrt 用 v 乘 100。
    cbrt_v 为 做 整数_立方根 用 v。
    approx 为 (sqrt_v 乘 60 加 cbrt_v 乘 40) 除以 100。
    返还 approx 乘 10550 除以 10000 减 550。
完。

CSSColor_到RGBA8 接受 颜色：
    srgb 为 做 CSSColor_转换到 用 颜色、CS_SRGB。
    r 为 srgb 的 分量1 乘 255 除以 10000。
    g 为 srgb 的 分量2 乘 255 除以 10000。
    b 为 srgb 的 分量3 乘 255 除以 10000。
    a 为 srgb 的 alpha 乘 255 除以 10000。
    若 r 小于 0：
        r 为 0。
    完。
    若 r 大于 255：
        r 为 255。
    完。
    若 g 小于 0：
        g 为 0。
    完。
    若 g 大于 255：
        g 为 255。
    完。
    若 b 小于 0：
        b 为 0。
    完。
    若 b 大于 255：
        b 为 255。
    完。
    若 a 小于 0：
        a 为 0。
    完。
    若 a 大于 255：
        a 为 255。
    完。
    返还 a 左移 24 位或 r 左移 16 位或 g 左移 8 位或 b。
完。

CSSColor_从lightDark 接受 str：
    len 为 做 字节长度 用 str。
    可变 comma 为 0。
    可变 p 为 11。
    只要 p 小于 len：
        若 做 字节取 用 str、p 等于 0x2C：
            comma 为 p。
            跳出。
        完。
        p 为 p 加 1。
    完。
    若 comma 等于 0：
        返还 做 CSSColor_默认。
    完。
    亮色str 为 做 CSS_子串_去空白 用 str、11、comma。
    暗色str 为 做 CSS_子串_去空白 用 str、comma 加 1、len 减 1。
    暗模式 为 做 平台_检测暗色模式。
    若 暗模式：
        返还 做 CSSColor_解析 用 暗色str。
    完。
    返还 做 CSSColor_解析 用 亮色str。
完。

NEST_TYPE_RULE 为 0。

NEST_TYPE_AT_RULE 为 1。

NestedRule 是 一种 事物，含 父选择器、子选择器、展平选择器、类型、声明列表、声明数量、子规则列表、子规则数量。

CSSNesting_展平选择器 接受 父选择器、子选择器：
    若 父选择器 等于 0 或者 子选择器 等于 0：
        返还 子选择器。
    完。
    若 做 字符串_包含 用 子选择器、「&」：
        返还 做 字符串_替换 用 子选择器、「&」、父选择器。
    完。
    返还 做 字符串_连接 用 做 字符串_连接 用 父选择器、「 」、子选择器。
完。

CSSNesting_处理嵌套规则 接受 样式表、父规则、嵌套内容、内容长度：
    若 样式表 等于 0 或者 父规则 等于 0：
        返还。
    完。
完。

CSSNesting_展平所有 接受 样式表：
    若 样式表 等于 0：
        返还。
    完。
    可变 新规则列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 样式表 的 规则列表：
        规则 为 样式表 的 规则列表 的 第 i 项。
        做 CSSNesting_展平规则 用 规则、「」、新规则列表。
        i 为 i 加 1。
    完。
    样式表 的 规则列表 为 新规则列表。
完。

可变 _CSS展平深度 为 0。

CSSNesting_展平规则 接受 规则、父选择器、输出：
    若 规则 等于 0：
        返还。
    完。
    _CSS展平深度 为 _CSS展平深度 加 1。
    若 _CSS展平深度 大于 200：
        _CSS展平深度 为 _CSS展平深度 减 1。
        返还。
    完。
    完整选择器 为 规则 的 选择器。
    若 父选择器 不等于 「」：
        完整选择器 为 做 字符串替换 用 规则 的 选择器、「&」、父选择器。
        若 做 字符串查找 用 规则 的 选择器、「&」 小于 0：
            完整选择器 为 父选择器 加 「 」 加 规则 的 选择器。
        完。
    完。
    平面规则 为 做 CSS规则_创建 用 完整选择器、规则 的 声明列表。
    做 追加 用 输出、平面规则。
    若 规则 的 子规则 不等于 0：
        可变 j 为 0。
        只要 j 小于 做 计数 用 规则 的 子规则：
            做 CSSNesting_展平规则 用 规则 的 子规则 的 第 j 项、完整选择器、输出。
            j 为 j 加 1。
        完。
    完。
完。

画布2D_fill 接受 ctx：
    若 ctx 等于 0 或者 ctx 的 路径大小 等于 0：
        返还。
    完。
    s 为 ctx 的 状态。
    可变 偏移 为 0。
    只要 偏移 小于 ctx 的 路径大小：
        类型 为 做 内存_读8 用 ctx 的 路径命令 加 偏移。
        x1 为 做 内存_读32 用 ctx 的 路径命令 加 偏移 加 4。
        y1 为 做 内存_读32 用 ctx 的 路径命令 加 偏移 加 8。
        若 类型 等于 路径_LineTo 或者 类型 等于 路径_Close：
            做 画布2D_光栅线段 用 ctx、ctx 的 路径x、ctx 的 路径y、x1、y1。
        完。
        偏移 为 偏移 加 28。
    完。
    做 画布2D_扫描线填充 用 ctx。
完。

画布2D_stroke 接受 ctx：
    若 ctx 等于 0 或者 ctx 的 路径大小 等于 0：
        返还。
    完。
    s 为 ctx 的 状态。
    线宽 为 s 的 线宽 除以 1024。
    若 线宽 小于 1：
        线宽 为 1。
    完。
    可变 上x 为 0。
    可变 上y 为 0。
    可变 偏移 为 0。
    只要 偏移 小于 ctx 的 路径大小：
        类型 为 做 内存_读8 用 ctx 的 路径命令 加 偏移。
        x1 为 做 内存_读32 用 ctx 的 路径命令 加 偏移 加 4。
        y1 为 做 内存_读32 用 ctx 的 路径命令 加 偏移 加 8。
        若 类型 等于 路径_MoveTo：
            上x 为 x1。
            上y 为 y1。
        否则若 类型 等于 路径_LineTo 或者 类型 等于 路径_Close：
            做 画布2D_描边线段 用 ctx、上x、上y、x1、y1、线宽。
            上x 为 x1。
            上y 为 y1。
        完。
        偏移 为 偏移 加 28。
    完。
完。

画布2D_描边线段 接受 ctx、x0、y0、x1、y1、宽度：
    s 为 ctx 的 状态。
    tx0 为 (s 的 a 乘 x0 加 s 的 c 乘 y0) 除以 1024 加 s 的 e。
    ty0 为 (s 的 b 乘 x0 加 s 的 d 乘 y0) 除以 1024 加 s 的 f。
    tx1 为 (s 的 a 乘 x1 加 s 的 c 乘 y1) 除以 1024 加 s 的 e。
    ty1 为 (s 的 b 乘 x1 加 s 的 d 乘 y1) 除以 1024 加 s 的 f。
    dx 为 若 tx1 大于 tx0 则 tx1 减 tx0 否则 tx0 减 tx1。
    dy 为 若 ty1 大于 ty0 则 ty1 减 ty0 否则 ty0 减 ty1。
    sx 为 若 tx0 小于 tx1 则 1 否则 负 1。
    sy 为 若 ty0 小于 ty1 则 1 否则 负 1。
    可变 err 为 dx 减 dy。
    可变 cx 为 tx0。
    可变 cy 为 ty0。
    只要 真：
        可变 py 为 cy 减 宽度 除以 2。
        只要 py 小于或等于 cy 加 宽度 除以 2：
            可变 px 为 cx 减 宽度 除以 2。
            只要 px 小于或等于 cx 加 宽度 除以 2：
                做 画布2D_写像素 用 ctx、px、py、s 的 描边颜色R、s 的 描边颜色G、s 的 描边颜色B、s 的 描边颜色A。
                px 为 px 加 1。
            完。
            py 为 py 加 1。
        完。
        若 cx 等于 tx1 并且 cy 等于 ty1：
            跳出。
        完。
        e2 为 err 乘 2。
        若 e2 大于 负 dy：
            err 为 err 减 dy。
            cx 为 cx 加 sx。
        完。
        若 e2 小于 dx：
            err 为 err 加 dx。
            cy 为 cy 加 sy。
        完。
    完。
完。

画布2D_光栅线段 接受 ctx、x0、y0、x1、y1：
    若 ctx 的 扫描线缓冲 等于 0：
        返还。
    完。
    idx 为 ctx 的 扫描线数量。
    若 idx 大于或等于 ctx 的 扫描线容量：
        返还。
    完。
    off 为 ctx 的 扫描线缓冲 加 idx 乘 16。
    做 内存_写32 用 off、x0。
    做 内存_写32 用 off 加 4、y0。
    做 内存_写32 用 off 加 8、x1。
    做 内存_写32 用 off 加 12、y1。
    ctx 的 扫描线数量 为 idx 加 1。
完。

画布2D_扫描线填充 接受 ctx：
    s 为 ctx 的 状态。
    若 ctx 的 扫描线数量 等于 0：
        返还。
    完。
    bm 为 ctx 的 画布位图。
    颜色 为 s 的 填充色。
    可变 minY 为 999999。
    可变 maxY 为 0。
    可变 ei 为 0。
    只要 ei 小于 ctx 的 扫描线数量：
        off 为 ctx 的 扫描线缓冲 加 ei 乘 16。
        y0 为 做 内存_读32 用 off 加 4。
        y1 为 做 内存_读32 用 off 加 12。
        若 y0 小于 minY：
            minY 为 y0。
        完。
        若 y1 小于 minY：
            minY 为 y1。
        完。
        若 y0 大于 maxY：
            maxY 为 y0。
        完。
        若 y1 大于 maxY：
            maxY 为 y1。
        完。
        ei 为 ei 加 1。
    完。
    可变 scanY 为 minY。
    只要 scanY 小于或等于 maxY：
        scanY 为 scanY 加 1。
    完。
    ctx 的 扫描线数量 为 0。
完。

类型_文本 为 1。

类型_富文本 为 2。

类型_图像 为 3。

类型_文件 为 4。

拖拽_无 为 0。

拖拽_开始 为 1。

拖拽_移动 为 2。

拖拽_悬停 为 3。

拖拽_放下 为 4。

最大剪贴项 为 16。

剪贴项 是 一种 事物，含 类型、数据指针、数据长度、类型标签哈希。

拖拽状态 是 一种 事物，含 阶段、源元素、目标元素、数据类型、数据指针、数据长度、起始横、起始纵、当前横、当前纵、允许效果。

可变 剪贴板 为 一组：（空）。

可变 剪贴板数 为 0。

可变 全局拖拽 为 0。

可变 复制次数 为 0。

可变 粘贴次数 为 0。

可变 拖拽次数 为 0。

剪贴_初始化 接受 零参：
    剪贴板 为 一组：（空）。
    剪贴板数 为 0。
    全局拖拽 为 做 堆_分配 用 拖拽状态。
    全局拖拽 的 阶段 为 拖拽_无。
完。

剪贴_写入 接受 类型、数据、长度：
    复制次数 为 复制次数 加 1。
    剪贴板 为 一组：（空）。
    剪贴板数 为 0。
    若 剪贴板数 大于或等于 最大剪贴项：
        返还。
    完。
    项 为 新建 剪贴项。
    项 的 类型 为 类型。
    项 的 数据指针 为 做 堆_分配 用 长度。
    做 内存_复制 用 项 的 数据指针、数据、长度。
    项 的 数据长度 为 长度。
    项 的 类型标签哈希 为 类型。
    做 追加 用 剪贴板、项。
    剪贴板数 为 剪贴板数 加 1。
完。

剪贴_读取 接受 类型：
    粘贴次数 为 粘贴次数 加 1。
    可变 序号 为 0。
    只要 序号 小于 剪贴板数：
        若 剪贴板 的 第 序号 项 的 类型 等于 类型：
            返还 剪贴板 的 第 序号 项。
        完。
        序号 为 序号 加 1。
    完。
    若 剪贴板数 大于 0：
        返还 剪贴板 的 第 0 项。
    完。
    返还 0。
完。

剪贴_有内容 接受 零参：
    返还 剪贴板数 大于 0。
完。

剪贴_清空 接受 零参：
    可变 序号 为 0。
    只要 序号 小于 剪贴板数：
        若 剪贴板 的 第 序号 项 的 数据指针 不等于 0：
            做 堆_释放 用 剪贴板 的 第 序号 项 的 数据指针。
        完。
        序号 为 序号 加 1。
    完。
    剪贴板 为 一组：（空）。
    剪贴板数 为 0。
完。

拖拽_开始事件 接受 源元素、横、纵、类型、数据、长度：
    若 全局拖拽 等于 0：
        返还。
    完。
    拖拽次数 为 拖拽次数 加 1。
    全局拖拽 的 阶段 为 拖拽_开始。
    全局拖拽 的 源元素 为 源元素。
    全局拖拽 的 目标元素 为 0。
    全局拖拽 的 数据类型 为 类型。
    全局拖拽 的 数据指针 为 数据。
    全局拖拽 的 数据长度 为 长度。
    全局拖拽 的 起始横 为 横。
    全局拖拽 的 起始纵 为 纵。
    全局拖拽 的 当前横 为 横。
    全局拖拽 的 当前纵 为 纵。
    全局拖拽 的 允许效果 为 7。
完。

拖拽_移动事件 接受 横、纵：
    若 全局拖拽 等于 0 或者 全局拖拽 的 阶段 等于 拖拽_无：
        返还。
    完。
    全局拖拽 的 阶段 为 拖拽_移动。
    全局拖拽 的 当前横 为 横。
    全局拖拽 的 当前纵 为 纵。
完。

拖拽_悬停事件 接受 目标元素：
    若 全局拖拽 等于 0 或者 全局拖拽 的 阶段 等于 拖拽_无：
        返还。
    完。
    全局拖拽 的 阶段 为 拖拽_悬停。
    全局拖拽 的 目标元素 为 目标元素。
完。

拖拽_放下事件 接受 零参：
    若 全局拖拽 等于 0 或者 全局拖拽 的 阶段 等于 拖拽_无：
        返还 0。
    完。
    全局拖拽 的 阶段 为 拖拽_放下。
    目标 为 全局拖拽 的 目标元素。
    全局拖拽 的 阶段 为 拖拽_无。
    返还 目标。
完。

拖拽_取消 接受 零参：
    若 全局拖拽 等于 0：
        返还。
    完。
    全局拖拽 的 阶段 为 拖拽_无。
完。

拖拽_获取数据 接受 零参：
    若 全局拖拽 等于 0：
        返还 0。
    完。
    返还 全局拖拽 的 数据指针。
完。

拖拽_是否进行中 接受 零参：
    若 全局拖拽 等于 0：
        返还 假。
    完。
    返还 全局拖拽 的 阶段 不等于 拖拽_无。
完。

剪贴拖放_完整统计 接受 零参：
    返还 一组：复制次数、粘贴次数、拖拽次数、剪贴板数。
完。

本章 收束。
