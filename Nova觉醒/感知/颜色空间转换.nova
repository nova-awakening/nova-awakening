《颜色空间转换》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《.·解码》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内容可见性》。

引用《..·中枢·内存初始化》。

系数_Cr转R 为 1436。

系数_Cb转G 为 352。

系数_Cr转G 为 731。

系数_Cb转B 为 1815。

系数709_Cr转R 为 1613。

系数709_Cb转G 为 192。

系数709_Cr转G 为 479。

系数709_Cb转B 为 1900。

系数_R转Y 为 306。

系数_G转Y 为 601。

系数_B转Y 为 117。

系数_R转Cb 为 负 172。

系数_G转Cb 为 负 339。

系数_B转Cb 为 512。

系数_R转Cr 为 512。

系数_G转Cr 为 负 429。

系数_B转Cr 为 负 83。

采样_444 为 0。

采样_422 为 1。

采样_420 为 2。

采样_灰度 为 3。

色彩空间_BT601 为 0。

色彩空间_BT709 为 1。

色彩空间_灰度 为 2。

色彩空间_CMYK 为 3。

钳位表大小 为 768。

钳位表偏移 为 256。

颜色转换器 是 一种 事物，含 色彩空间、钳位表、Cr转R表、Cb转G表、Cr转G表、Cb转B表、已初始化、锁、AI状态、统计。

颜色转换统计 是 一种 事物，含 单像素转换次数、批量转换次数、批量像素总数、灰度转换次数、CMYK转换次数、查找表命中次数、直接计算次数、钳位触发次数、总耗时_纳秒、最大单次耗时、MCU解码总数、扫描行解码数、重启标记处理数、AI优化调用次数、AI质量调整次数、错误总数。

AI颜色状态 是 一种 事物，含 历史亮度均值、历史色度偏差、暗区像素比例、亮区像素比例、连续低质量帧数、自适应伽马、策略_使用查找表、策略_伽马校正、采样计数器。

可变 全局颜色转换器 为 0。

可变 全局颜色统计 为 0。

可变 全局颜色已初始化 为 假。

颜色转换_初始化 接受 色彩空间：
    若 全局颜色已初始化 等于 真：
        做 打印 用 「[颜色转换] 警告: 重复初始化」。
        返还 错误_已存在。
    完。
    全局颜色转换器 为 做 堆_分配 用 颜色转换器。
    若 全局颜色转换器 等于 空：
        返还 错误_内存不足。
    完。
    转换器 为 全局颜色转换器。
    转换器 的 色彩空间 为 色彩空间。
    转换器 的 已初始化 为 假。
    转换器 的 锁 为 做 渲染_互斥锁创建。
    全局颜色统计 为 做 堆_分配_清零 用 128。
    若 全局颜色统计 等于 空：
        做 堆_释放 用 全局颜色转换器。
        返还 错误_内存不足。
    完。
    转换器 的 统计 为 全局颜色统计。
    转换器 的 钳位表 为 做 堆_分配 用 钳位表大小。
    若 转换器 的 钳位表 不等于 空：
        做 颜色转换_构建钳位表 用 转换器。
    完。
    转换器 的 Cr转R表 为 做 堆_分配 用 256 乘 4。
    转换器 的 Cb转G表 为 做 堆_分配 用 256 乘 4。
    转换器 的 Cr转G表 为 做 堆_分配 用 256 乘 4。
    转换器 的 Cb转B表 为 做 堆_分配 用 256 乘 4。
    若 转换器 的 Cr转R表 不等于 空 并且 转换器 的 Cb转B表 不等于 空：
        做 颜色转换_构建颜色表 用 转换器。
    完。
    转换器 的 AI状态 为 做 堆_分配_清零 用 48。
    若 转换器 的 AI状态 不等于 空：
        AI 为 转换器 的 AI状态。
        AI 的 自适应伽马 为 1024。
        AI 的 策略_使用查找表 为 真。
        AI 的 策略_伽马校正 为 假。
    完。
    转换器 的 已初始化 为 真。
    全局颜色已初始化 为 真。
    做 打印 用 「[颜色转换] 初始化完成: 查找表+AI就绪」。
    返还 0。
完。

颜色转换_销毁 接受 零参：
    若 全局颜色转换器 等于 空：
        返还。
    完。
    转换器 为 全局颜色转换器。
    若 转换器 的 钳位表 不等于 空：
        做 堆_释放 用 转换器 的 钳位表。
    完。
    若 转换器 的 Cr转R表 不等于 空：
        做 堆_释放 用 转换器 的 Cr转R表。
    完。
    若 转换器 的 Cb转G表 不等于 空：
        做 堆_释放 用 转换器 的 Cb转G表。
    完。
    若 转换器 的 Cr转G表 不等于 空：
        做 堆_释放 用 转换器 的 Cr转G表。
    完。
    若 转换器 的 Cb转B表 不等于 空：
        做 堆_释放 用 转换器 的 Cb转B表。
    完。
    若 转换器 的 AI状态 不等于 空：
        做 堆_释放 用 转换器 的 AI状态。
    完。
    做 堆_释放 用 全局颜色转换器。
    若 全局颜色统计 不等于 空：
        做 堆_释放 用 全局颜色统计。
    完。
    全局颜色转换器 为 0。
    全局颜色统计 为 0。
    全局颜色已初始化 为 假。
    做 打印 用 「[颜色转换] 已销毁」。
完。

颜色转换_重置 接受 零参：
    若 全局颜色转换器 等于 空：
        返还 错误_未初始化。
    完。
    若 全局颜色统计 不等于 空：
        做 内存_置零 用 全局颜色统计、128。
    完。
    若 全局颜色转换器 的 AI状态 不等于 空：
        AI 为 全局颜色转换器 的 AI状态。
        AI 的 历史亮度均值 为 0。
        AI 的 历史色度偏差 为 0。
        AI 的 连续低质量帧数 为 0。
        AI 的 自适应伽马 为 1024。
        AI 的 采样计数器 为 0。
    完。
    返还 0。
完。

颜色转换_构建钳位表 接受 转换器：
    若 转换器 等于 空 或者 转换器 的 钳位表 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 256：
        做 内存_写8 用 转换器 的 钳位表 加 i、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 256：
        做 内存_写8 用 转换器 的 钳位表 加 256 加 i、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 256：
        做 内存_写8 用 转换器 的 钳位表 加 512 加 i、255。
        i 为 i 加 1。
    完。
完。

颜色转换_构建颜色表 接受 转换器：
    若 转换器 等于 空：
        返还。
    完。
    可变 Cr系数R 为 系数_Cr转R。
    可变 Cb系数G 为 系数_Cb转G。
    可变 Cr系数G 为 系数_Cr转G。
    可变 Cb系数B 为 系数_Cb转B。
    若 转换器 的 色彩空间 等于 色彩空间_BT709：
        Cr系数R 为 系数709_Cr转R。
        Cb系数G 为 系数709_Cb转G。
        Cr系数G 为 系数709_Cr转G。
        Cb系数B 为 系数709_Cb转B。
    完。
    可变 i 为 0。
    只要 i 小于 256：
        偏移 为 i 减 128。
        若 转换器 的 Cr转R表 不等于 空：
            做 内存_写32 用 转换器 的 Cr转R表 加 i 乘 4、Cr系数R 乘 偏移 右移 10。
        完。
        若 转换器 的 Cb转G表 不等于 空：
            做 内存_写32 用 转换器 的 Cb转G表 加 i 乘 4、0 减 (Cb系数G 乘 偏移 右移 10)。
        完。
        若 转换器 的 Cr转G表 不等于 空：
            做 内存_写32 用 转换器 的 Cr转G表 加 i 乘 4、0 减 (Cr系数G 乘 偏移 右移 10)。
        完。
        若 转换器 的 Cb转B表 不等于 空：
            做 内存_写32 用 转换器 的 Cb转B表 加 i 乘 4、Cb系数B 乘 偏移 右移 10。
        完。
        i 为 i 加 1。
    完。
完。

钳位 接受 v：
    若 v 小于 0：
        返还 0。
    完。
    若 v 大于 255：
        返还 255。
    完。
    返还 v。
完。

钳位_查表 接受 转换器、v：
    若 转换器 等于 空 或者 转换器 的 钳位表 等于 空：
        返还 做 钳位 用 v。
    完。
    索引 为 v 加 钳位表偏移。
    若 索引 小于 0：
        索引 为 0。
    完。
    若 索引 大于或等于 钳位表大小：
        索引 为 钳位表大小 减 1。
    完。
    返还 做 内存_读8 用 转换器 的 钳位表 加 索引。
完。

YCbCr转RGB 接受 Y、Cb、Cr：
    cb 为 Cb 减 128。
    cr 为 Cr 减 128。
    R 为 Y 加 (系数_Cr转R 乘 cr 右移 10)。
    G 为 Y 减 (系数_Cb转G 乘 cb 右移 10) 减 (系数_Cr转G 乘 cr 右移 10)。
    B 为 Y 加 (系数_Cb转B 乘 cb 右移 10)。
    返还 做 钳位 用 R 左移 16 位或 做 钳位 用 G 左移 8 位或 做 钳位 用 B。
完。

YCbCr转RGB_快速 接受 转换器、Y、Cb、Cr：
    若 转换器 等于 空 或者 转换器 的 Cr转R表 等于 空：
        返还 做 YCbCr转RGB 用 Y、Cb、Cr。
    完。
    R 为 Y 加 做 内存_读32 用 转换器 的 Cr转R表 加 Cr 乘 4。
    G 为 Y 加 做 内存_读32 用 转换器 的 Cb转G表 加 Cb 乘 4 加 做 内存_读32 用 转换器 的 Cr转G表 加 Cr 乘 4。
    B 为 Y 加 做 内存_读32 用 转换器 的 Cb转B表 加 Cb 乘 4。
    返还 做 钳位_查表 用 转换器、R 左移 16 位或 做 钳位_查表 用 转换器、G 左移 8 位或 做 钳位_查表 用 转换器、B。
完。

RGB转YCbCr 接受 R、G、B：
    若 R 小于 0：
        R 为 0。
    完。
    若 R 大于 255：
        R 为 255。
    完。
    若 G 小于 0：
        G 为 0。
    完。
    若 G 大于 255：
        G 为 255。
    完。
    若 B 小于 0：
        B 为 0。
    完。
    若 B 大于 255：
        B 为 255。
    完。
    Y 为 系数_R转Y 乘 R 加 系数_G转Y 乘 G 加 系数_B转Y 乘 B 右移 10。
    Cb 为 (系数_R转Cb 乘 R 加 系数_G转Cb 乘 G 加 系数_B转Cb 乘 B 右移 10) 加 128。
    Cr 为 (系数_R转Cr 乘 R 加 系数_G转Cr 乘 G 加 系数_B转Cr 乘 B 右移 10) 加 128。
    返还 做 钳位 用 Y 左移 16 位或 做 钳位 用 Cb 左移 8 位或 做 钳位 用 Cr。
完。

灰度转RGB 接受 Y：
    v 为 做 钳位 用 Y。
    返还 v 左移 16 位或 v 左移 8 位或 v。
完。

CMYK转RGB 接受 C、M、Y、K：
    R 为 (255 减 C) 乘 (255 减 K) 除以 255。
    G 为 (255 减 M) 乘 (255 减 K) 除以 255。
    B 为 (255 减 Y) 乘 (255 减 K) 除以 255。
    返还 做 钳位 用 R 左移 16 位或 做 钳位 用 G 左移 8 位或 做 钳位 用 B。
完。

颜色转换_批量YCbCr到RGB 接受 YCbCr缓冲、RGB缓冲、像素数：
    若 YCbCr缓冲 等于 空 或者 RGB缓冲 等于 空 或者 像素数 小于或等于 0：
        返还 错误_无效参数。
    完。
    转换器 为 全局颜色转换器。
    开始时间 为 做 平台_获取高精度时间。
    可变 i 为 0。
    可变 暗区计数 为 0。
    可变 亮区计数 为 0。
    可变 亮度总和 为 0。
    只要 i 小于 像素数：
        偏移4 为 i 乘 4。
        Y 为 做 内存_读8 用 YCbCr缓冲 加 偏移4。
        Cb 为 做 内存_读8 用 YCbCr缓冲 加 偏移4 加 1。
        Cr 为 做 内存_读8 用 YCbCr缓冲 加 偏移4 加 2。
        亮度总和 为 亮度总和 加 Y。
        若 Y 小于 40：
            暗区计数 为 暗区计数 加 1。
        完。
        若 Y 大于 220：
            亮区计数 为 亮区计数 加 1。
        完。
        可变 rgb 为 0。
        若 转换器 不等于 空 并且 转换器 的 AI状态 不等于 空 并且 转换器 的 AI状态 的 策略_使用查找表 等于 真：
            rgb 为 做 YCbCr转RGB_快速 用 转换器、Y、Cb、Cr。
            若 全局颜色统计 不等于 空：
                全局颜色统计 的 查找表命中次数 为 全局颜色统计 的 查找表命中次数 加 1。
            完。
        否则：
            rgb 为 做 YCbCr转RGB 用 Y、Cb、Cr。
            若 全局颜色统计 不等于 空：
                全局颜色统计 的 直接计算次数 为 全局颜色统计 的 直接计算次数 加 1。
            完。
        完。
        做 内存_写8 用 RGB缓冲 加 偏移4、rgb 右移 16 位与 0xFF。
        做 内存_写8 用 RGB缓冲 加 偏移4 加 1、rgb 右移 8 位与 0xFF。
        做 内存_写8 用 RGB缓冲 加 偏移4 加 2、rgb 位与 0xFF。
        做 内存_写8 用 RGB缓冲 加 偏移4 加 3、255。
        i 为 i 加 1。
    完。
    若 全局颜色统计 不等于 空：
        全局颜色统计 的 批量转换次数 为 全局颜色统计 的 批量转换次数 加 1。
        全局颜色统计 的 批量像素总数 为 全局颜色统计 的 批量像素总数 加 像素数。
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        全局颜色统计 的 总耗时_纳秒 为 全局颜色统计 的 总耗时_纳秒 加 耗时。
        若 耗时 大于 全局颜色统计 的 最大单次耗时：
            全局颜色统计 的 最大单次耗时 为 耗时。
        完。
    完。
    若 转换器 不等于 空 并且 像素数 大于 0：
        做 AI颜色_分析帧质量 用 转换器、亮度总和 除以 像素数、暗区计数 乘 1024 除以 像素数、亮区计数 乘 1024 除以 像素数。
    完。
    返还 0。
完。

颜色转换_批量灰度到RGB 接受 灰度缓冲、RGB缓冲、像素数：
    若 灰度缓冲 等于 空 或者 RGB缓冲 等于 空 或者 像素数 小于或等于 0：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 像素数：
        Y 为 做 内存_读8 用 灰度缓冲 加 i。
        v 为 做 钳位 用 Y。
        偏移 为 i 乘 4。
        做 内存_写8 用 RGB缓冲 加 偏移、v。
        做 内存_写8 用 RGB缓冲 加 偏移 加 1、v。
        做 内存_写8 用 RGB缓冲 加 偏移 加 2、v。
        做 内存_写8 用 RGB缓冲 加 偏移 加 3、255。
        i 为 i 加 1。
    完。
    若 全局颜色统计 不等于 空：
        全局颜色统计 的 灰度转换次数 为 全局颜色统计 的 灰度转换次数 加 1。
    完。
    返还 0。
完。

JPEG_解码扫描数据 接受 dec：
    若 dec 等于 空：
        返还 错误_无效参数。
    完。
    系数 为 做 堆_分配_数组 用 64 乘 4。
    若 系数 等于 空：
        返还 错误_内存不足。
    完。
    MCU宽 为 (dec 的 宽 加 7) 除以 8。
    MCU高 为 (dec 的 高 加 7) 除以 8。
    若 dec 的 采样模式 等于 采样_420：
        MCU宽 为 (dec 的 宽 加 15) 除以 16。
        MCU高 为 (dec 的 高 加 15) 除以 16。
    完。
    若 dec 的 采样模式 等于 采样_422：
        MCU宽 为 (dec 的 宽 加 15) 除以 16。
        MCU高 为 (dec 的 高 加 7) 除以 8。
    完。
    可变 mcu_y 为 0。
    只要 mcu_y 小于 MCU高：
        可变 mcu_x 为 0。
        只要 mcu_x 小于 MCU宽：
            若 dec 的 采样模式 等于 采样_420：
                可变 by 为 0。
                只要 by 小于 2：
                    可变 bx 为 0。
                    只要 bx 小于 2：
                        做 JPEG_解码块 用 dec、0、系数。
                        做 JPEG_逆量化 用 系数、dec 的 量化表 的 第 dec 的 分量量化表号 的 第 0 项 项。
                        做 JPEG_IDCT_8x8 用 系数。
                        做 JPEG_写块到像素 用 dec、系数、mcu_x 乘 16 加 bx 乘 8、mcu_y 乘 16 加 by 乘 8、0。
                        bx 为 bx 加 1。
                    完。
                    by 为 by 加 1。
                完。
                做 JPEG_解码块 用 dec、1、系数。
                做 JPEG_逆量化 用 系数、dec 的 量化表 的 第 dec 的 分量量化表号 的 第 1 项 项。
                做 JPEG_IDCT_8x8 用 系数。
                做 JPEG_写块到像素 用 dec、系数、mcu_x 乘 16、mcu_y 乘 16、1。
                做 JPEG_解码块 用 dec、2、系数。
                做 JPEG_逆量化 用 系数、dec 的 量化表 的 第 dec 的 分量量化表号 的 第 2 项 项。
                做 JPEG_IDCT_8x8 用 系数。
                做 JPEG_写块到像素 用 dec、系数、mcu_x 乘 16、mcu_y 乘 16、2。
            否则若 dec 的 采样模式 等于 采样_422：
                可变 bx 为 0。
                只要 bx 小于 2：
                    做 JPEG_解码块 用 dec、0、系数。
                    做 JPEG_逆量化 用 系数、dec 的 量化表 的 第 dec 的 分量量化表号 的 第 0 项 项。
                    做 JPEG_IDCT_8x8 用 系数。
                    做 JPEG_写块到像素 用 dec、系数、mcu_x 乘 16 加 bx 乘 8、mcu_y 乘 8、0。
                    bx 为 bx 加 1。
                完。
                做 JPEG_解码块 用 dec、1、系数。
                做 JPEG_逆量化 用 系数、dec 的 量化表 的 第 dec 的 分量量化表号 的 第 1 项 项。
                做 JPEG_IDCT_8x8 用 系数。
                做 JPEG_写块到像素 用 dec、系数、mcu_x 乘 16、mcu_y 乘 8、1。
                做 JPEG_解码块 用 dec、2、系数。
                做 JPEG_逆量化 用 系数、dec 的 量化表 的 第 dec 的 分量量化表号 的 第 2 项 项。
                做 JPEG_IDCT_8x8 用 系数。
                做 JPEG_写块到像素 用 dec、系数、mcu_x 乘 16、mcu_y 乘 8、2。
            否则：
                可变 c 为 0。
                只要 c 小于 dec 的 分量数：
                    做 JPEG_解码块 用 dec、c、系数。
                    做 JPEG_逆量化 用 系数、dec 的 量化表 的 第 dec 的 分量量化表号 的 第 c 项 项。
                    做 JPEG_IDCT_8x8 用 系数。
                    做 JPEG_写块到像素 用 dec、系数、mcu_x 乘 8、mcu_y 乘 8、c。
                    c 为 c 加 1。
                完。
            完。
            若 dec 的 重启间隔 大于 0：
                dec 的 重启计数 为 dec 的 重启计数 加 1。
                若 dec 的 重启计数 大于或等于 dec 的 重启间隔：
                    dec 的 DC预测 的 第 0 项 为 0。
                    dec 的 DC预测 的 第 1 项 为 0。
                    dec 的 DC预测 的 第 2 项 为 0。
                    dec 的 位缓冲 为 0。
                    dec 的 位计数 为 0。
                    dec 的 重启计数 为 0。
                    只要 dec 的 位置 小于 dec 的 数据大小 并且 做 内存_读8 用 dec 的 数据 加 dec 的 位置 不等于 0xFF：
                        dec 的 位置 为 dec 的 位置 加 1。
                    完。
                    dec 的 位置 为 dec 的 位置 加 2。
                    若 全局颜色统计 不等于 空：
                        全局颜色统计 的 重启标记处理数 为 全局颜色统计 的 重启标记处理数 加 1。
                    完。
                完。
            完。
            若 全局颜色统计 不等于 空：
                全局颜色统计 的 MCU解码总数 为 全局颜色统计 的 MCU解码总数 加 1。
            完。
            mcu_x 为 mcu_x 加 1。
        完。
        若 全局颜色统计 不等于 空：
            全局颜色统计 的 扫描行解码数 为 全局颜色统计 的 扫描行解码数 加 1。
        完。
        mcu_y 为 mcu_y 加 1。
    完。
    做 堆_释放 用 系数。
    返还 0。
完。

JPEG_写块到像素 接受 dec、系数、基x、基y、分量：
    若 dec 等于 空 或者 系数 等于 空：
        返还。
    完。
    若 dec 的 像素缓冲 等于 空：
        返还。
    完。
    可变 y 为 0。
    只要 y 小于 8：
        可变 x 为 0。
        只要 x 小于 8：
            像素x 为 基x 加 x。
            像素y 为 基y 加 y。
            若 像素x 小于 dec 的 宽 并且 像素y 小于 dec 的 高：
                值 为 (做 内存_读32 用 系数 加 (y 乘 8 加 x) 乘 4 右移 12) 加 128。
                v 为 做 钳位 用 值。
                偏移 为 (像素y 乘 dec 的 宽 加 像素x) 乘 4。
                若 分量 等于 0：
                    做 内存_写8 用 dec 的 像素缓冲 加 偏移、v。
                    做 内存_写8 用 dec 的 像素缓冲 加 偏移 加 3、255。
                否则若 分量 等于 1：
                    做 内存_写8 用 dec 的 像素缓冲 加 偏移 加 1、v。
                否则若 分量 等于 2：
                    做 内存_写8 用 dec 的 像素缓冲 加 偏移 加 2、v。
                完。
            完。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
完。

颜色转换_色度上采样_420 接受 YCbCr缓冲、宽、高：
    若 YCbCr缓冲 等于 空 或者 宽 小于或等于 0 或者 高 小于或等于 0：
        返还 错误_无效参数。
    完。
    半宽 为 (宽 加 1) 除以 2。
    半高 为 (高 加 1) 除以 2。
    可变 y 为 高 减 1。
    只要 y 大于或等于 0：
        可变 x 为 宽 减 1。
        只要 x 大于或等于 0：
            源x 为 x 除以 2。
            源y 为 y 除以 2。
            若 源x 大于或等于 半宽：
                源x 为 半宽 减 1。
            完。
            若 源y 大于或等于 半高：
                源y 为 半高 减 1。
            完。
            源偏移 为 (源y 乘 宽 加 源x) 乘 4。
            目标偏移 为 (y 乘 宽 加 x) 乘 4。
            Cb 为 做 内存_读8 用 YCbCr缓冲 加 源偏移 加 1。
            Cr 为 做 内存_读8 用 YCbCr缓冲 加 源偏移 加 2。
            做 内存_写8 用 YCbCr缓冲 加 目标偏移 加 1、Cb。
            做 内存_写8 用 YCbCr缓冲 加 目标偏移 加 2、Cr。
            x 为 x 减 1。
        完。
        y 为 y 减 1。
    完。
    返还 0。
完。

AI颜色_分析帧质量 接受 转换器、平均亮度、暗区比例、亮区比例：
    若 转换器 等于 空 或者 转换器 的 AI状态 等于 空：
        返还。
    完。
    AI 为 转换器 的 AI状态。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    若 全局颜色统计 不等于 空：
        全局颜色统计 的 AI优化调用次数 为 全局颜色统计 的 AI优化调用次数 加 1。
    完。
    AI 的 历史亮度均值 为 (AI 的 历史亮度均值 乘 7 加 平均亮度 乘 1024) 除以 8。
    AI 的 暗区像素比例 为 (AI 的 暗区像素比例 乘 7 加 暗区比例) 除以 8。
    AI 的 亮区像素比例 为 (AI 的 亮区像素比例 乘 7 加 亮区比例) 除以 8。
    可变 是过暗 为 假。
    若 AI 的 历史亮度均值 小于 60 乘 1024：
        是过暗 为 真。
    完。
    可变 是过亮 为 假。
    若 AI 的 历史亮度均值 大于 200 乘 1024：
        是过亮 为 真。
    完。
    可变 是低对比度 为 假。
    若 AI 的 暗区像素比例 小于 50 并且 AI 的 亮区像素比例 小于 50：
        是低对比度 为 真。
    完。
    若 AI 的 采样计数器 小于 16：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 是过暗 等于 真：
        AI 的 自适应伽马 为 1280。
        AI 的 策略_伽马校正 为 真。
        AI 的 连续低质量帧数 为 AI 的 连续低质量帧数 加 1。
    否则若 是过亮 等于 真：
        AI 的 自适应伽马 为 820。
        AI 的 策略_伽马校正 为 真。
        AI 的 连续低质量帧数 为 AI 的 连续低质量帧数 加 1。
    否则：
        AI 的 自适应伽马 为 1024。
        AI 的 策略_伽马校正 为 假。
        AI 的 连续低质量帧数 为 0。
    完。
    若 全局颜色统计 不等于 空 并且 全局颜色统计 的 批量像素总数 大于 100000：
        AI 的 策略_使用查找表 为 真。
    完。
    若 AI 的 连续低质量帧数 大于 5：
        做 打印 用 「[AI颜色] 连续低质量帧，建议检查图像源」。
        若 全局颜色统计 不等于 空：
            全局颜色统计 的 AI质量调整次数 为 全局颜色统计 的 AI质量调整次数 加 1。
        完。
    完。
完。

颜色转换_打印统计 接受 零参：
    若 全局颜色统计 等于 空：
        做 打印 用 「[颜色转换] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   颜色空间转换 · 统计报告           │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 批量转换次数:     」、做 转字符串 用 全局颜色统计 的 批量转换次数。
    做 打印 用 做 字符串拼接 用 「│ 批量像素总数:     」、做 转字符串 用 全局颜色统计 的 批量像素总数。
    做 打印 用 做 字符串拼接 用 「│ 查找表命中:       」、做 转字符串 用 全局颜色统计 的 查找表命中次数。
    做 打印 用 做 字符串拼接 用 「│ 直接计算:         」、做 转字符串 用 全局颜色统计 的 直接计算次数。
    做 打印 用 做 字符串拼接 用 「│ 灰度转换:         」、做 转字符串 用 全局颜色统计 的 灰度转换次数。
    做 打印 用 做 字符串拼接 用 「│ MCU解码总数:      」、做 转字符串 用 全局颜色统计 的 MCU解码总数。
    做 打印 用 做 字符串拼接 用 「│ 扫描行解码:       」、做 转字符串 用 全局颜色统计 的 扫描行解码数。
    做 打印 用 做 字符串拼接 用 「│ 重启标记处理:     」、做 转字符串 用 全局颜色统计 的 重启标记处理数。
    做 打印 用 做 字符串拼接 用 「│ AI优化调用:       」、做 转字符串 用 全局颜色统计 的 AI优化调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI质量调整:       」、做 转字符串 用 全局颜色统计 的 AI质量调整次数。
    做 打印 用 做 字符串拼接 用 「│ 总耗时(μs):       」、做 转字符串 用 全局颜色统计 的 总耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 最大单次耗时(μs): 」、做 转字符串 用 全局颜色统计 的 最大单次耗时 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 错误总数:         」、做 转字符串 用 全局颜色统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

颜色转换_导出诊断 接受 零参：
    若 全局颜色转换器 等于 空：
        做 打印 用 「[颜色转换] 诊断: 未初始化」。
        返还。
    完。
    转换器 为 全局颜色转换器。
    做 打印 用 做 字符串拼接 用 「[颜色转换] 色彩空间: 」、做 转字符串 用 转换器 的 色彩空间。
    做 打印 用 做 字符串拼接 用 「[颜色转换] 查找表就绪: 」、做 转字符串 用 转换器 的 Cr转R表 不等于 空。
    若 转换器 的 AI状态 不等于 空：
        AI 为 转换器 的 AI状态。
        做 打印 用 做 字符串拼接 用 「[AI颜色] 历史亮度(Q10): 」、做 转字符串 用 AI 的 历史亮度均值。
        做 打印 用 做 字符串拼接 用 「[AI颜色] 暗区比例(Q10): 」、做 转字符串 用 AI 的 暗区像素比例。
        做 打印 用 做 字符串拼接 用 「[AI颜色] 亮区比例(Q10): 」、做 转字符串 用 AI 的 亮区像素比例。
        做 打印 用 做 字符串拼接 用 「[AI颜色] 自适应伽马: 」、做 转字符串 用 AI 的 自适应伽马。
        做 打印 用 做 字符串拼接 用 「[AI颜色] 策略_查找表: 」、做 转字符串 用 AI 的 策略_使用查找表。
        做 打印 用 做 字符串拼接 用 「[AI颜色] 策略_伽马: 」、做 转字符串 用 AI 的 策略_伽马校正。
    完。
完。

颜色转换_获取转换器 接受 零参：
    返还 全局颜色转换器。
完。

颜色转换_获取统计 接受 零参：
    返还 全局颜色统计。
完。

颜色转换_设置色彩空间 接受 色彩空间：
    若 全局颜色转换器 等于 空：
        返还 错误_未初始化。
    完。
    全局颜色转换器 的 色彩空间 为 色彩空间。
    做 颜色转换_构建颜色表 用 全局颜色转换器。
    做 打印 用 做 字符串拼接 用 「[颜色转换] 色彩空间切换为: 」、做 转字符串 用 色彩空间。
    返还 0。
完。

本章 收束。
