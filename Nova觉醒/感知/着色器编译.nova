《着色器编译》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·辅助存根》。

ShaderCompiler_预处理 接受 sc：
    若 sc 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 sc 的 源码长度：
        ch 为 做 字节取 用 sc 的 源码、i。
        若 ch 等于 35：
            若 i 加 8 小于 sc 的 源码长度：
                若 做 字节取 用 sc 的 源码、i 加 1 等于 118：
                    sc 的 版本 为 300。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

ShaderCompiler_词法分析 接受 sc：
    若 sc 等于 0：
        返还。
    完。
    sc 的 位置 为 0。
    只要 sc 的 位置 小于 sc 的 源码长度 并且 sc 的 令牌数 小于 sc 的 令牌容量：
        ch 为 做 字节取 用 sc 的 源码、sc 的 位置。
        若 ch 等于 32 或者 ch 等于 9 或者 ch 等于 13：
            sc 的 位置 为 sc 的 位置 加 1。
            继续。
        完。
        若 ch 等于 10：
            sc 的 当前行 为 sc 的 当前行 加 1。
            sc 的 当前列 为 1。
            sc 的 位置 为 sc 的 位置 加 1。
            继续。
        完。
        若 ch 等于 47 并且 sc 的 位置 加 1 小于 sc 的 源码长度：
            next 为 做 字节取 用 sc 的 源码、sc 的 位置 加 1。
            若 next 等于 47：
                只要 sc 的 位置 小于 sc 的 源码长度 并且 做 字节取 用 sc 的 源码、sc 的 位置 不等于 10：
                    sc 的 位置 为 sc 的 位置 加 1。
                完。
                继续。
            完。
            若 next 等于 42：
                sc 的 位置 为 sc 的 位置 加 2。
                只要 sc 的 位置 加 1 小于 sc 的 源码长度：
                    若 做 字节取 用 sc 的 源码、sc 的 位置 等于 42 并且 做 字节取 用 sc 的 源码、sc 的 位置 加 1 等于 47：
                        sc 的 位置 为 sc 的 位置 加 2。
                        跳出。
                    完。
                    若 做 字节取 用 sc 的 源码、sc 的 位置 等于 10：
                        sc 的 当前行 为 sc 的 当前行 加 1。
                    完。
                    sc 的 位置 为 sc 的 位置 加 1。
                完。
                继续。
            完。
        完。
        若 ch 等于 35：
            做 GLSL_添加令牌 用 sc、GLSL_TOK_PREPROC、0。
            只要 sc 的 位置 小于 sc 的 源码长度 并且 做 字节取 用 sc 的 源码、sc 的 位置 不等于 10：
                sc 的 位置 为 sc 的 位置 加 1。
            完。
            继续。
        完。
        若 ch 大于或等于 65 并且 ch 小于或等于 90 或者 ch 大于或等于 97 并且 ch 小于或等于 122 或者 ch 等于 95：
            起始 为 sc 的 位置。
            只要 sc 的 位置 小于 sc 的 源码长度：
                c 为 做 字节取 用 sc 的 源码、sc 的 位置。
                若 非 (c 大于或等于 65 并且 c 小于或等于 90 或者 c 大于或等于 97 并且 c 小于或等于 122 或者 c 大于或等于 48 并且 c 小于或等于 57 或者 c 等于 95)：
                    跳出。
                完。
                sc 的 位置 为 sc 的 位置 加 1。
            完。
            词 为 做 字符串_子串 用 sc 的 源码、起始、sc 的 位置。
            是关键字 为 做 GLSL_是关键字 用 词。
            做 GLSL_添加令牌 用 sc、若 是关键字 则 GLSL_TOK_KEYWORD 否则 GLSL_TOK_IDENT、词。
            继续。
        完。
        若 ch 大于或等于 48 并且 ch 小于或等于 57：
            起始 为 sc 的 位置。
            可变 是浮点 为 假。
            只要 sc 的 位置 小于 sc 的 源码长度：
                c 为 做 字节取 用 sc 的 源码、sc 的 位置。
                若 c 等于 46：
                    是浮点 为 真。
                否则若 非 (c 大于或等于 48 并且 c 小于或等于 57) 并且 c 不等于 101 并且 c 不等于 69 并且 c 不等于 102：
                    跳出。
                完。
                sc 的 位置 为 sc 的 位置 加 1。
            完。
            做 GLSL_添加令牌 用 sc、若 是浮点 则 GLSL_TOK_FLOAT 否则 GLSL_TOK_INT、0。
            继续。
        完。
        若 ch 等于 59：
            做 GLSL_添加令牌 用 sc、GLSL_TOK_SEMICOL、0。
            sc 的 位置 为 sc 的 位置 加 1。
            继续。
        完。
        若 ch 等于 123：
            做 GLSL_添加令牌 用 sc、GLSL_TOK_LBRACE、0。
            sc 的 位置 为 sc 的 位置 加 1。
            继续。
        完。
        若 ch 等于 125：
            做 GLSL_添加令牌 用 sc、GLSL_TOK_RBRACE、0。
            sc 的 位置 为 sc 的 位置 加 1。
            继续。
        完。
        若 ch 等于 40：
            做 GLSL_添加令牌 用 sc、GLSL_TOK_LPAREN、0。
            sc 的 位置 为 sc 的 位置 加 1。
            继续。
        完。
        若 ch 等于 41：
            做 GLSL_添加令牌 用 sc、GLSL_TOK_RPAREN、0。
            sc 的 位置 为 sc 的 位置 加 1。
            继续。
        完。
        若 ch 等于 44：
            做 GLSL_添加令牌 用 sc、GLSL_TOK_COMMA、0。
            sc 的 位置 为 sc 的 位置 加 1。
            继续。
        完。
        若 ch 等于 46：
            做 GLSL_添加令牌 用 sc、GLSL_TOK_DOT、0。
            sc 的 位置 为 sc 的 位置 加 1。
            继续。
        完。
        做 GLSL_添加令牌 用 sc、GLSL_TOK_OPERATOR、ch。
        sc 的 位置 为 sc 的 位置 加 1。
    完。
    做 GLSL_添加令牌 用 sc、GLSL_TOK_EOF、0。
完。

GLSL_添加令牌 接受 sc、类型、值：
    若 sc 等于 0 或者 sc 的 令牌数 大于或等于 sc 的 令牌容量：
        返还。
    完。
    t 为 sc 的 令牌s 加 sc 的 令牌数 乘 做 大小of 用 GLSL令牌。
    t 的 类型 为 类型。
    t 的 值 为 值。
    t 的 行号 为 sc 的 当前行。
    t 的 列号 为 sc 的 当前列。
    sc 的 令牌数 为 sc 的 令牌数 加 1。
完。

GLSL_是关键字 接受 词：
    若 词 等于 0：
        返还 假。
    完。
    若 做 字符串_相等 用 词、「void」 或者 做 字符串_相等 用 词、「bool」 或者 做 字符串_相等 用 词、「int」 或者 做 字符串_相等 用 词、「uint」 或者 做 字符串_相等 用 词、「float」 或者 做 字符串_相等 用 词、「vec2」 或者 做 字符串_相等 用 词、「vec3」 或者 做 字符串_相等 用 词、「vec4」 或者 做 字符串_相等 用 词、「mat2」 或者 做 字符串_相等 用 词、「mat3」 或者 做 字符串_相等 用 词、「mat4」 或者 做 字符串_相等 用 词、「sampler2D」 或者 做 字符串_相等 用 词、「sampler3D」 或者 做 字符串_相等 用 词、「samplerCube」 或者 做 字符串_相等 用 词、「in」 或者 做 字符串_相等 用 词、「out」 或者 做 字符串_相等 用 词、「uniform」 或者 做 字符串_相等 用 词、「const」 或者 做 字符串_相等 用 词、「if」 或者 做 字符串_相等 用 词、「else」 或者 做 字符串_相等 用 词、「for」 或者 做 字符串_相等 用 词、「while」 或者 做 字符串_相等 用 词、「return」 或者 做 字符串_相等 用 词、「discard」 或者 做 字符串_相等 用 词、「struct」 或者 做 字符串_相等 用 词、「layout」 或者 做 字符串_相等 用 词、「precision」 或者 做 字符串_相等 用 词、「flat」 或者 做 字符串_相等 用 词、「smooth」：
        返还 真。
    完。
    返还 假。
完。

ShaderCompiler_语法分析 接受 sc：
    若 sc 等于 0：
        返还。
    完。
完。

ShaderCompiler_语义检查 接受 sc：
    若 sc 等于 0：
        返还。
    完。
完。

ShaderCompiler_优化 接受 sc：
    若 sc 等于 0：
        返还。
    完。
完。

ShaderCompiler_生成代码 接受 sc、着色器类型：
    若 sc 等于 0：
        返还。
    完。
完。

本章 收束。
