《CSS网格repeat》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·网格扫描行》。

引用《.·内核直连层》。

引用《..·执行·控制台输出》。

REPEAT_FIXED 为 0。

REPEAT_AUTO_FILL 为 1。

REPEAT_AUTO_FIT 为 2。

RepeatTrack 是 一种 事物，含 模式、次数、模板、模板数。

网格TrackDef 是 一种 事物，含 类型、值、最小值、最大值、线名。

网格_展开repeat 接受 repeat、容器大小、gap：
    若 repeat 等于 0：
        返还 0。
    完。
    若 repeat 的 模式 等于 REPEAT_FIXED：
        返还 repeat 的 次数。
    完。
    可变 单次大小 为 0。
    可变 i 为 0。
    只要 i 小于 repeat 的 模板数：
        若 repeat 的 模板 的 第 i 项 的 类型 等于 0：
            单次大小 为 单次大小 加 repeat 的 模板 的 第 i 项 的 值。
        否则若 repeat 的 模板 的 第 i 项 的 类型 等于 4：
            单次大小 为 单次大小 加 repeat 的 模板 的 第 i 项 的 最小值。
        否则：
            单次大小 为 单次大小 加 0。
        完。
        i 为 i 加 1。
    完。
    若 单次大小 小于或等于 0：
        返还 1。
    完。
    次数 为 (容器大小 加 gap) 除以 (单次大小 加 gap)。
    若 次数 小于 1：
        次数 为 1。
    完。
    返还 次数。
完。

FLOW_ROW 为 0。

FLOW_COLUMN 为 1。

FLOW_ROW_DENSE 为 2。

FLOW_COLUMN_DENSE 为 3。

网格Occupancy 是 一种 事物，含 行数、列数、位图、位图大小。

网格Occupancy_创建 接受 行数、列数：
    occ 为 做 堆_分配 用 网格Occupancy。
    occ 的 行数 为 行数。
    occ 的 列数 为 列数。
    occ 的 位图大小 为 (行数 乘 列数 加 7) 除以 8。
    occ 的 位图 为 做 堆_分配 用 occ 的 位图大小。
    做 内存_填充 用 occ 的 位图、0、occ 的 位图大小。
    返还 occ。
完。

网格Occupancy_占用 接受 occ、行、列：
    若 occ 等于 0 或者 行 大于或等于 occ 的 行数 或者 列 大于或等于 occ 的 列数：
        返还。
    完。
    bit 为 行 乘 occ 的 列数 加 列。
    byte_idx 为 bit 除以 8。
    bit_idx 为 bit 取余于 8。
    old 为 做 字节取 用 occ 的 位图、byte_idx。
    做 字节写 用 occ 的 位图、byte_idx、old 位或 1 左移 bit_idx。
完。

网格Occupancy_已占用 接受 occ、行、列：
    若 occ 等于 0 或者 行 大于或等于 occ 的 行数 或者 列 大于或等于 occ 的 列数：
        返还 真。
    完。
    bit 为 行 乘 occ 的 列数 加 列。
    byte_idx 为 bit 除以 8。
    bit_idx 为 bit 取余于 8。
    返还 做 字节取 用 occ 的 位图、byte_idx 位与 1 左移 bit_idx 不等于 0。
完。

网格Occupancy_区域可用 接受 occ、行起、列起、行跨、列跨：
    可变 r 为 0。
    只要 r 小于 行跨：
        可变 c 为 0。
        只要 c 小于 列跨：
            若 做 网格Occupancy_已占用 用 occ、行起 加 r、列起 加 c：
                返还 假。
            完。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 真。
完。

网格Occupancy_占用区域 接受 occ、行起、列起、行跨、列跨：
    可变 r 为 0。
    只要 r 小于 行跨：
        可变 c 为 0。
        只要 c 小于 列跨：
            做 网格Occupancy_占用 用 occ、行起 加 r、列起 加 c。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
完。

网格Placement 是 一种 事物，含 行起始、行结束、列起始、列结束、行跨、列跨、已放置。

网格_自动放置 接受 items、items数、occ、flow：
    若 items 等于 0 或者 occ 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 items数：
        item 为 items 加 i 乘 做 大小of 用 网格Placement。
        若 item 的 行起始 大于或等于 0 并且 item 的 列起始 大于或等于 0：
            item 的 已放置 为 真。
            做 网格Occupancy_占用区域 用 occ、item 的 行起始 减 1、item 的 列起始 减 1、item 的 行跨、item 的 列跨。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 items数：
        item 为 items 加 i 乘 做 大小of 用 网格Placement。
        若 非 item 的 已放置：
            若 item 的 行起始 大于或等于 0 并且 item 的 列起始 小于 0：
                放置列 为 做 网格_扫描行可用列 用 occ、item 的 行起始 减 1、item 的 列跨。
                item 的 列起始 为 放置列 加 1。
                item 的 列结束 为 放置列 加 item 的 列跨 加 1。
                item 的 已放置 为 真。
                做 网格Occupancy_占用区域 用 occ、item 的 行起始 减 1、放置列、item 的 行跨、item 的 列跨。
            否则若 item 的 列起始 大于或等于 0 并且 item 的 行起始 小于 0：
                放置行 为 做 网格_扫描列可用行 用 occ、item 的 列起始 减 1、item 的 行跨。
                item 的 行起始 为 放置行 加 1。
                item 的 行结束 为 放置行 加 item 的 行跨 加 1。
                item 的 已放置 为 真。
                做 网格Occupancy_占用区域 用 occ、放置行、item 的 列起始 减 1、item 的 行跨、item 的 列跨。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 游标行 为 0。
    可变 游标列 为 0。
    是dense 为 flow 等于 FLOW_ROW_DENSE 或者 flow 等于 FLOW_COLUMN_DENSE。
    i 为 0。
    只要 i 小于 items数：
        item 为 items 加 i 乘 做 大小of 用 网格Placement。
        若 非 item 的 已放置：
            若 是dense：
                游标行 为 0。
                游标列 为 0。
            完。
            若 flow 等于 FLOW_ROW 或者 flow 等于 FLOW_ROW_DENSE：
                可变 找到 为 假。
                可变 r 为 游标行。
                只要 r 小于 occ 的 行数 并且 非 找到：
                    起始列 为 若 r 等于 游标行 则 游标列 否则 0。
                    可变 c 为 起始列。
                    只要 c 小于或等于 occ 的 列数 减 item 的 列跨 并且 非 找到：
                        若 做 网格Occupancy_区域可用 用 occ、r、c、item 的 行跨、item 的 列跨：
                            item 的 行起始 为 r 加 1。
                            item 的 列起始 为 c 加 1。
                            item 的 行结束 为 r 加 item 的 行跨 加 1。
                            item 的 列结束 为 c 加 item 的 列跨 加 1。
                            item 的 已放置 为 真。
                            做 网格Occupancy_占用区域 用 occ、r、c、item 的 行跨、item 的 列跨。
                            游标行 为 r。
                            游标列 为 c 加 item 的 列跨。
                            若 游标列 大于或等于 occ 的 列数：
                                游标行 为 游标行 加 1。
                                游标列 为 0。
                            完。
                            找到 为 真。
                        完。
                        c 为 c 加 1。
                    完。
                    r 为 r 加 1。
                完。
                若 非 找到：
                    item 的 行起始 为 occ 的 行数 加 1。
                    item 的 列起始 为 1。
                    item 的 行结束 为 occ 的 行数 加 item 的 行跨 加 1。
                    item 的 列结束 为 item 的 列跨 加 1。
                    item 的 已放置 为 真。
                完。
            否则：
                可变 找到 为 假。
                可变 c 为 游标列。
                只要 c 小于 occ 的 列数 并且 非 找到：
                    起始行 为 若 c 等于 游标列 则 游标行 否则 0。
                    可变 r 为 起始行。
                    只要 r 小于或等于 occ 的 行数 减 item 的 行跨 并且 非 找到：
                        若 做 网格Occupancy_区域可用 用 occ、r、c、item 的 行跨、item 的 列跨：
                            item 的 行起始 为 r 加 1。
                            item 的 列起始 为 c 加 1。
                            item 的 已放置 为 真。
                            做 网格Occupancy_占用区域 用 occ、r、c、item 的 行跨、item 的 列跨。
                            游标行 为 r 加 item 的 行跨。
                            游标列 为 c。
                            找到 为 真。
                        完。
                        r 为 r 加 1。
                    完。
                    c 为 c 加 1。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

可变 AI_感知丙甲_调用数 为 0。

可变 AI_感知丙甲_异常数 为 0。

AI_感知丙甲_监控 接受 零参：
    AI_感知丙甲_调用数 为 AI_感知丙甲_调用数 加 1。
    返还 AI_感知丙甲_异常数 等于 0。
完。

AI_感知丙甲_健康检查 接受 零参：
    若 AI_感知丙甲_调用数 大于 0 并且 AI_感知丙甲_异常数 乘 100 除以 AI_感知丙甲_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
