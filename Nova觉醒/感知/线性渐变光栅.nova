《线性渐变光栅》

引用《..·稳态·图处显示》。

引用《..·推理·通信总线》。

引用《.·内核直连层》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《..·执行·底层内存操》。

色标_最大数量 为 32。

渐变_缓存行大小 为 256。

定点_坐标精度 为 100。

定点_色标精度 为 10000。

定点_插值精度 为 256。

颜色_掩码 为 255。

平铺_截断 为 0。

平铺_重复 为 1。

平铺_镜像 为 2。

平铺_抖动 为 3。

混合_源覆盖 为 0。

混合_源 为 1。

混合_目标上方 为 2。

AI_简单渐变阈值 为 2。

AI_缓存阈值 为 4。

渐变色标 是 一种 事物，含 位置、颜色。

线性渐变 是 一种 事物，含 起点x、起点y、终点x、终点y、色标基址、色标数、色标容量、轴dx、轴dy、轴长度平方、平铺模式、缓存基址、缓存有效、矩阵_m00、矩阵_m01、矩阵_m10、矩阵_m11、矩阵_有效、AI渐变类型、AI精度级别、统计_采样次数、统计_缓存命中次数、统计_填充像素数、统计_填充行数。

颜色_A 接受 颜色：
    返还 颜色 右移 24 位与 颜色_掩码。
完。

颜色_R 接受 颜色：
    返还 颜色 右移 16 位与 颜色_掩码。
完。

颜色_G 接受 颜色：
    返还 颜色 右移 8 位与 颜色_掩码。
完。

颜色_B 接受 颜色：
    返还 颜色 位与 颜色_掩码。
完。

颜色_组合 接受 a、r、g、b：
    可变 结果 为 (a 位与 颜色_掩码) 左移 24。
    结果 为 结果 位或 (r 位与 颜色_掩码) 左移 16。
    结果 为 结果 位或 (g 位与 颜色_掩码) 左移 8。
    结果 为 结果 位或 b 位与 颜色_掩码。
    返还 结果。
完。

线性渐变_创建 接受 x0、y0、x1、y1：
    可变 渐变 为 做 堆_分配 用 线性渐变。
    若 渐变 等于 0：
        做 打印 用 「[渐变] 错误: 分配失败」。
        返还 空。
    完。
    渐变 的 起点x 为 x0。
    渐变 的 起点y 为 y0。
    渐变 的 终点x 为 x1。
    渐变 的 终点y 为 y1。
    渐变 的 轴dx 为 x1 减 x0。
    渐变 的 轴dy 为 y1 减 y0。
    渐变 的 轴长度平方 为 渐变 的 轴dx 乘 渐变 的 轴dx 加 渐变 的 轴dy 乘 渐变 的 轴dy。
    渐变 的 色标容量 为 色标_最大数量。
    渐变 的 色标基址 为 做 平台_申请内存 用 渐变 的 色标容量 乘 8。
    若 渐变 的 色标基址 等于 0：
        做 打印 用 「[渐变] 错误: 色标数组分配失败」。
        做 堆_释放 用 渐变。
        返还 空。
    完。
    做 内存_清零 用 渐变 的 色标基址、渐变 的 色标容量 乘 8。
    渐变 的 色标数 为 0。
    渐变 的 平铺模式 为 平铺_截断。
    渐变 的 缓存基址 为 做 平台_申请内存 用 渐变_缓存行大小 乘 4。
    渐变 的 缓存有效 为 假。
    渐变 的 矩阵_m00 为 10000。
    渐变 的 矩阵_m01 为 0。
    渐变 的 矩阵_m10 为 0。
    渐变 的 矩阵_m11 为 10000。
    渐变 的 矩阵_有效 为 假。
    渐变 的 AI渐变类型 为 0。
    渐变 的 AI精度级别 为 2。
    渐变 的 统计_采样次数 为 0。
    渐变 的 统计_缓存命中次数 为 0。
    渐变 的 统计_填充像素数 为 0。
    渐变 的 统计_填充行数 为 0。
    返还 渐变。
完。

线性渐变_销毁 接受 渐变：
    若 渐变 等于 空：
        返还。
    完。
    若 渐变 的 色标基址 不等于 0：
        做 平台_释放内存 用 渐变 的 色标基址、渐变 的 色标容量 乘 8。
    完。
    若 渐变 的 缓存基址 不等于 0：
        做 平台_释放内存 用 渐变 的 缓存基址、渐变_缓存行大小 乘 4。
    完。
    做 堆_释放 用 渐变。
完。

线性渐变_添加色标 接受 渐变、位置、颜色：
    若 渐变 等于 空：
        返还。
    完。
    若 渐变 的 色标数 大于或等于 渐变 的 色标容量：
        做 打印 用 「[渐变] 警告: 色标数量达到上限」。
        返还。
    完。
    可变 有效位置 为 位置。
    若 有效位置 小于 0：
        有效位置 为 0。
    完。
    若 有效位置 大于 定点_色标精度：
        有效位置 为 定点_色标精度。
    完。
    偏移 为 渐变 的 色标基址 加 渐变 的 色标数 乘 8。
    做 内存_写32 用 偏移、有效位置。
    做 内存_写32 用 偏移 加 4、颜色。
    渐变 的 色标数 为 渐变 的 色标数 加 1。
    渐变 的 缓存有效 为 假。
    若 渐变 的 色标数 小于或等于 AI_简单渐变阈值：
        渐变 的 AI渐变类型 为 渐变 的 色标数。
    否则：
        渐变 的 AI渐变类型 为 0。
    完。
完。

线性渐变_设置平铺 接受 渐变、模式：
    若 渐变 等于 空：
        返还。
    完。
    渐变 的 平铺模式 为 模式。
    渐变 的 缓存有效 为 假。
完。

线性渐变_排序色标 接受 渐变：
    若 渐变 等于 空 或者 渐变 的 色标数 小于 2：
        返还。
    完。
    可变 已交换 为 真。
    只要 已交换 等于 真：
        已交换 为 假。
        可变 i 为 0。
        只要 i 小于 渐变 的 色标数 减 1：
            当前位置 为 做 内存_读32 用 渐变 的 色标基址 加 i 乘 8。
            下一位置 为 做 内存_读32 用 渐变 的 色标基址 加 (i 加 1) 乘 8。
            若 当前位置 大于 下一位置：
                tmp_pos 为 当前位置。
                tmp_col 为 做 内存_读32 用 渐变 的 色标基址 加 i 乘 8 加 4。
                做 内存_写32 用 渐变 的 色标基址 加 i 乘 8、下一位置。
                做 内存_写32 用 渐变 的 色标基址 加 i 乘 8 加 4、做 内存_读32 用 渐变 的 色标基址 加 (i 加 1) 乘 8 加 4。
                做 内存_写32 用 渐变 的 色标基址 加 (i 加 1) 乘 8、tmp_pos。
                做 内存_写32 用 渐变 的 色标基址 加 (i 加 1) 乘 8 加 4、tmp_col。
                已交换 为 真。
            完。
            i 为 i 加 1。
        完。
    完。
    渐变 的 缓存有效 为 假。
完。

颜色_线性插值 接受 c0、c1、分数：
    反分数 为 定点_插值精度 减 分数。
    a 为 (做 颜色_A 用 c0 乘 反分数 加 做 颜色_A 用 c1 乘 分数) 除以 定点_插值精度。
    r 为 (做 颜色_R 用 c0 乘 反分数 加 做 颜色_R 用 c1 乘 分数) 除以 定点_插值精度。
    g 为 (做 颜色_G 用 c0 乘 反分数 加 做 颜色_G 用 c1 乘 分数) 除以 定点_插值精度。
    b 为 (做 颜色_B 用 c0 乘 反分数 加 做 颜色_B 用 c1 乘 分数) 除以 定点_插值精度。
    返还 做 颜色_组合 用 a、r、g、b。
完。

颜色_预乘Alpha混合 接受 源颜色、目标颜色：
    srcA 为 做 颜色_A 用 源颜色。
    若 srcA 等于 255：
        返还 源颜色。
    完。
    若 srcA 等于 0：
        返还 目标颜色。
    完。
    invA 为 255 减 srcA。
    dstA 为 做 颜色_A 用 目标颜色。
    dstR 为 做 颜色_R 用 目标颜色。
    dstG 为 做 颜色_G 用 目标颜色。
    dstB 为 做 颜色_B 用 目标颜色。
    outA 为 srcA 加 dstA 乘 invA 除以 255。
    outR 为 做 颜色_R 用 源颜色 加 dstR 乘 invA 除以 255。
    outG 为 做 颜色_G 用 源颜色 加 dstG 乘 invA 除以 255。
    outB 为 做 颜色_B 用 源颜色 加 dstB 乘 invA 除以 255。
    返还 做 颜色_组合 用 outA、outR、outG、outB。
完。

线性渐变_采样 接受 渐变、t：
    若 渐变 等于 空 或者 渐变 的 色标数 等于 0：
        返还 做 颜色_组合 用 255、0、0、0。
    完。
    若 渐变 的 色标数 等于 1：
        返还 做 内存_读32 用 渐变 的 色标基址 加 4。
    完。
    渐变 的 统计_采样次数 为 渐变 的 统计_采样次数 加 1。
    可变 有效t 为 做 线性渐变_平铺 用 渐变、t。
    若 渐变 的 缓存有效 等于 真 并且 渐变 的 缓存基址 不等于 0：
        缓存索引 为 有效t 乘 (渐变_缓存行大小 减 1) 除以 定点_色标精度。
        若 缓存索引 小于 0：
            缓存索引 为 0。
        完。
        若 缓存索引 大于或等于 渐变_缓存行大小：
            缓存索引 为 渐变_缓存行大小 减 1。
        完。
        渐变 的 统计_缓存命中次数 为 渐变 的 统计_缓存命中次数 加 1。
        返还 做 内存_读32 用 渐变 的 缓存基址 加 缓存索引 乘 4。
    完。
    若 有效t 小于 0：
        有效t 为 0。
    完。
    若 有效t 大于 定点_色标精度：
        有效t 为 定点_色标精度。
    完。
    可变 左 为 0。
    可变 右 为 渐变 的 色标数 减 1。
    第一位置 为 做 内存_读32 用 渐变 的 色标基址。
    若 有效t 小于或等于 第一位置：
        返还 做 内存_读32 用 渐变 的 色标基址 加 4。
    完。
    最后位置 为 做 内存_读32 用 渐变 的 色标基址 加 (渐变 的 色标数 减 1) 乘 8。
    若 有效t 大于或等于 最后位置：
        返还 做 内存_读32 用 渐变 的 色标基址 加 (渐变 的 色标数 减 1) 乘 8 加 4。
    完。
    可变 i 为 0。
    只要 i 小于 渐变 的 色标数 减 1：
        pos0 为 做 内存_读32 用 渐变 的 色标基址 加 i 乘 8。
        pos1 为 做 内存_读32 用 渐变 的 色标基址 加 (i 加 1) 乘 8。
        若 有效t 大于或等于 pos0 并且 有效t 小于或等于 pos1：
            颜色0 为 做 内存_读32 用 渐变 的 色标基址 加 i 乘 8 加 4。
            颜色1 为 做 内存_读32 用 渐变 的 色标基址 加 (i 加 1) 乘 8 加 4。
            范围 为 pos1 减 pos0。
            若 范围 等于 0：
                返还 颜色0。
            完。
            分数 为 (有效t 减 pos0) 乘 定点_插值精度 除以 范围。
            返还 做 颜色_线性插值 用 颜色0、颜色1、分数。
        完。
        i 为 i 加 1。
    完。
    返还 做 内存_读32 用 渐变 的 色标基址 加 (渐变 的 色标数 减 1) 乘 8 加 4。
完。

线性渐变_平铺 接受 渐变、t：
    若 渐变 的 平铺模式 等于 平铺_截断：
        若 t 小于 0：
            返还 0。
        完。
        若 t 大于 定点_色标精度：
            返还 定点_色标精度。
        完。
        返还 t。
    完。
    若 渐变 的 平铺模式 等于 平铺_重复：
        可变 结果 为 t 取余于 定点_色标精度。
        若 结果 小于 0：
            结果 为 结果 加 定点_色标精度。
        完。
        返还 结果。
    完。
    若 渐变 的 平铺模式 等于 平铺_镜像：
        可变 周期 为 t 取余于 (定点_色标精度 乘 2)。
        若 周期 小于 0：
            周期 为 周期 加 定点_色标精度 乘 2。
        完。
        若 周期 大于 定点_色标精度：
            返还 定点_色标精度 乘 2 减 周期。
        完。
        返还 周期。
    完。
    若 t 小于 0 或者 t 大于 定点_色标精度：
        返还 负 1。
    完。
    返还 t。
完。

线性渐变_构建缓存 接受 渐变：
    若 渐变 等于 空 或者 渐变 的 缓存基址 等于 0：
        返还。
    完。
    若 渐变 的 色标数 小于 2：
        返还。
    完。
    做 线性渐变_排序色标 用 渐变。
    可变 i 为 0。
    只要 i 小于 渐变_缓存行大小：
        t 为 i 乘 定点_色标精度 除以 (渐变_缓存行大小 减 1)。
        颜色 为 做 线性渐变_采样 用 渐变、t。
        做 内存_写32 用 渐变 的 缓存基址 加 i 乘 4、颜色。
        i 为 i 加 1。
    完。
    渐变 的 缓存有效 为 真。
完。

线性渐变_填充矩形 接受 帧缓冲、步幅、宽、高、渐变、矩形：
    若 渐变 等于 空 或者 帧缓冲 等于 0：
        返还。
    完。
    若 渐变 的 轴长度平方 等于 0：
        返还。
    完。
    若 渐变 的 色标数 小于 2：
        返还。
    完。
    若 渐变 的 色标数 大于或等于 AI_缓存阈值 并且 渐变 的 缓存有效 等于 假：
        做 线性渐变_构建缓存 用 渐变。
    完。
    可变 x起始 为 矩形 的 左 除以 定点_坐标精度。
    可变 y起始 为 矩形 的 上 除以 定点_坐标精度。
    可变 x结束 为 矩形 的 右 除以 定点_坐标精度。
    可变 y结束 为 矩形 的 下 除以 定点_坐标精度。
    若 x起始 小于 0：
        x起始 为 0。
    完。
    若 y起始 小于 0：
        y起始 为 0。
    完。
    若 x结束 大于 宽：
        x结束 为 宽。
    完。
    若 y结束 大于 高：
        y结束 为 高。
    完。
    若 x起始 大于或等于 x结束 或者 y起始 大于或等于 y结束：
        返还。
    完。
    步长 为 1。
    若 渐变 的 AI精度级别 等于 1：
        步长 为 2。
    完。
    可变 y 为 y起始。
    只要 y 小于 y结束：
        做 线性渐变_填充行 用 帧缓冲、步幅、渐变、y、x起始、x结束、步长。
        渐变 的 统计_填充行数 为 渐变 的 统计_填充行数 加 1。
        y 为 y 加 1。
    完。
完。

线性渐变_填充行 接受 帧缓冲、步幅、渐变、y、x起始、x结束、步长：
    行基址 为 帧缓冲 加 y 乘 步幅。
    dx 为 渐变 的 轴dx。
    dy 为 渐变 的 轴dy。
    长度平方 为 渐变 的 轴长度平方。
    若 渐变 的 AI渐变类型 等于 1：
        填充色 为 做 内存_读32 用 渐变 的 色标基址 加 4。
        可变 x 为 x起始。
        只要 x 小于 x结束：
            像素地址 为 行基址 加 x 乘 4。
            做 内存_写32 用 像素地址、填充色。
            渐变 的 统计_填充像素数 为 渐变 的 统计_填充像素数 加 1。
            x 为 x 加 步长。
        完。
        返还。
    完。
    可变 x 为 x起始。
    只要 x 小于 x结束：
        px 为 x 乘 定点_坐标精度 减 渐变 的 起点x。
        py 为 y 乘 定点_坐标精度 减 渐变 的 起点y。
        点积 为 px 乘 dx 加 py 乘 dy。
        t 为 点积 乘 定点_色标精度 除以 长度平方。
        颜色 为 做 线性渐变_采样 用 渐变、t。
        若 渐变 的 平铺模式 等于 平铺_抖动 并且 t 小于 0：
            x 为 x 加 步长。
            继续。
        完。
        像素地址 为 行基址 加 x 乘 4。
        srcA 为 做 颜色_A 用 颜色。
        若 srcA 等于 255：
            做 内存_写32 用 像素地址、颜色。
        否则若 srcA 大于 0：
            dst 为 做 内存_读32 用 像素地址。
            混合后 为 做 颜色_预乘Alpha混合 用 颜色、dst。
            做 内存_写32 用 像素地址、混合后。
        完。
        渐变 的 统计_填充像素数 为 渐变 的 统计_填充像素数 加 1。
        x 为 x 加 步长。
    完。
完。

线性渐变_填充圆角矩形 接受 帧缓冲、步幅、宽、高、渐变、矩形、圆角半径：
    若 渐变 等于 空 或者 帧缓冲 等于 0：
        返还。
    完。
    可变 x起始 为 矩形 的 左 除以 定点_坐标精度。
    可变 y起始 为 矩形 的 上 除以 定点_坐标精度。
    可变 x结束 为 矩形 的 右 除以 定点_坐标精度。
    可变 y结束 为 矩形 的 下 除以 定点_坐标精度。
    若 x起始 小于 0：
        x起始 为 0。
    完。
    若 y起始 小于 0：
        y起始 为 0。
    完。
    若 x结束 大于 宽：
        x结束 为 宽。
    完。
    若 y结束 大于 高：
        y结束 为 高。
    完。
    r 为 圆角半径 除以 定点_坐标精度。
    矩形宽 为 x结束 减 x起始。
    矩形高 为 y结束 减 y起始。
    可变 y 为 y起始。
    只要 y 小于 y结束：
        可变 行x起始 为 x起始。
        可变 行x结束 为 x结束。
        dy_top 为 y 减 y起始。
        dy_bot 为 y结束 减 1 减 y。
        若 dy_top 小于 r：
            dx_圆角 为 r 减 做 线性渐变_整数平方根 用 r 乘 r 减 (r 减 dy_top) 乘 (r 减 dy_top)。
            行x起始 为 行x起始 加 dx_圆角。
            行x结束 为 行x结束 减 dx_圆角。
        完。
        若 dy_bot 小于 r：
            dx_圆角 为 r 减 做 线性渐变_整数平方根 用 r 乘 r 减 (r 减 dy_bot) 乘 (r 减 dy_bot)。
            行x起始 为 行x起始 加 dx_圆角。
            行x结束 为 行x结束 减 dx_圆角。
        完。
        若 行x起始 小于 行x结束：
            做 线性渐变_填充行 用 帧缓冲、步幅、渐变、y、行x起始、行x结束、1。
        完。
        y 为 y 加 1。
    完。
完。

线性渐变_整数平方根 接受 值：
    若 值 小于或等于 0：
        返还 0。
    完。
    可变 x 为 值。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 值 除以 x) 除以 2。
    完。
    返还 x。
完。

线性渐变_AI更新精度 接受 渐变、帧耗时_微秒：
    若 渐变 等于 空：
        返还。
    完。
    若 帧耗时_微秒 大于 16000：
        若 渐变 的 AI精度级别 大于 1：
            渐变 的 AI精度级别 为 渐变 的 AI精度级别 减 1。
        完。
    否则若 帧耗时_微秒 小于 8000：
        若 渐变 的 AI精度级别 小于 4：
            渐变 的 AI精度级别 为 渐变 的 AI精度级别 加 1。
        完。
    完。
完。

线性渐变_双色 接受 x0、y0、x1、y1、颜色1、颜色2：
    渐变 为 做 线性渐变_创建 用 x0、y0、x1、y1。
    若 渐变 等于 空：
        返还 空。
    完。
    做 线性渐变_添加色标 用 渐变、0、颜色1。
    做 线性渐变_添加色标 用 渐变、定点_色标精度、颜色2。
    返还 渐变。
完。

线性渐变_三色 接受 x0、y0、x1、y1、颜色1、颜色2、颜色3：
    渐变 为 做 线性渐变_创建 用 x0、y0、x1、y1。
    若 渐变 等于 空：
        返还 空。
    完。
    做 线性渐变_添加色标 用 渐变、0、颜色1。
    做 线性渐变_添加色标 用 渐变、定点_色标精度 除以 2、颜色2。
    做 线性渐变_添加色标 用 渐变、定点_色标精度、颜色3。
    返还 渐变。
完。

线性渐变_彩虹 接受 x0、y0、x1、y1：
    渐变 为 做 线性渐变_创建 用 x0、y0、x1、y1。
    若 渐变 等于 空：
        返还 空。
    完。
    做 线性渐变_添加色标 用 渐变、0、做 颜色_组合 用 255、255、0、0。
    做 线性渐变_添加色标 用 渐变、1667、做 颜色_组合 用 255、255、165、0。
    做 线性渐变_添加色标 用 渐变、3333、做 颜色_组合 用 255、255、255、0。
    做 线性渐变_添加色标 用 渐变、5000、做 颜色_组合 用 255、0、128、0。
    做 线性渐变_添加色标 用 渐变、6667、做 颜色_组合 用 255、0、0、255。
    做 线性渐变_添加色标 用 渐变、8333、做 颜色_组合 用 255、75、0、130。
    做 线性渐变_添加色标 用 渐变、定点_色标精度、做 颜色_组合 用 255、148、0、211。
    返还 渐变。
完。

线性渐变_打印统计 接受 渐变：
    若 渐变 等于 空：
        返还。
    完。
    做 打印 用 「[渐变] ═══════════ 线性渐变统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  色标数:         」、做 转字符串 用 渐变 的 色标数。
    做 打印 用 做 字符串拼接 用 「  平铺模式:       」、做 转字符串 用 渐变 的 平铺模式。
    做 打印 用 做 字符串拼接 用 「  缓存状态:       」、做 转字符串 用 渐变 的 缓存有效。
    做 打印 用 做 字符串拼接 用 「  采样次数:       」、做 转字符串 用 渐变 的 统计_采样次数。
    做 打印 用 做 字符串拼接 用 「  缓存命中次数:   」、做 转字符串 用 渐变 的 统计_缓存命中次数。
    若 渐变 的 统计_采样次数 大于 0：
        命中率 为 渐变 的 统计_缓存命中次数 乘 10000 除以 渐变 的 统计_采样次数。
        做 打印 用 做 字符串拼接 用 「  缓存命中率‱:   」、做 转字符串 用 命中率。
    完。
    做 打印 用 做 字符串拼接 用 「  填充像素数:     」、做 转字符串 用 渐变 的 统计_填充像素数。
    做 打印 用 做 字符串拼接 用 「  填充行数:       」、做 转字符串 用 渐变 的 统计_填充行数。
    做 打印 用 做 字符串拼接 用 「  AI渐变类型:     」、做 转字符串 用 渐变 的 AI渐变类型。
    做 打印 用 做 字符串拼接 用 「  AI精度级别:     」、做 转字符串 用 渐变 的 AI精度级别。
    做 打印 用 「[渐变] ═══════════════════════════════════════」。
完。

本章 收束。
