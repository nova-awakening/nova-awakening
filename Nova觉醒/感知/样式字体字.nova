《样式字体字》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·扩展归档逻辑》。

引用《.·HTTP头部管理》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·辅助存根》。

引用《..·中枢·内容可见性》。

FONT_DISPLAY_AUTO 为 0。

FONT_DISPLAY_BLOCK 为 1。

FONT_DISPLAY_SWAP 为 2。

FONT_DISPLAY_FALLBACK 为 3。

FONT_DISPLAY_OPTIONAL 为 4。

FF_STATUS_UNLOADED 为 0。

FF_STATUS_LOADING 为 1。

FF_STATUS_LOADED 为 2。

FF_STATUS_ERROR 为 3。

字体FaceSource 是 一种 事物，含 URL、格式、下一个。

字体Face 是 一种 事物，含 家族名、源列表、源数量、粗细_最小、粗细_最大、风格、拉伸、显示策略、unicode范围、unicode范围数、状态、字体数据、字体数据大小、平台字体、加载承诺、加载开始时间、引用计数。

字体Face_创建 接受 家族名、源URL：
    ff 为 做 堆_分配 用 字体Face。
    ff 的 家族名 为 做 字符串_复制 用 家族名。
    ff 的 源列表 为 0。
    ff 的 源数量 为 0。
    ff 的 粗细_最小 为 400。
    ff 的 粗细_最大 为 400。
    ff 的 风格 为 0。
    ff 的 拉伸 为 100。
    ff 的 显示策略 为 FONT_DISPLAY_AUTO。
    ff 的 unicode范围 为 0。
    ff 的 unicode范围数 为 0。
    ff 的 状态 为 FF_STATUS_UNLOADED。
    ff 的 字体数据 为 0。
    ff 的 字体数据大小 为 0。
    ff 的 平台字体 为 0。
    ff 的 加载承诺 为 0。
    ff 的 加载开始时间 为 0。
    ff 的 引用计数 为 1。
    若 源URL 不等于 0：
        做 字体Face_添加源 用 ff、源URL、0。
    完。
    返还 ff。
完。

字体Face_添加源 接受 ff、url、格式：
    若 ff 等于 0：
        返还。
    完。
    src 为 做 堆_分配 用 字体FaceSource。
    src 的 URL 为 做 字符串_复制 用 url。
    src 的 格式 为 若 格式 不等于 0 则 做 字符串_复制 用 格式 否则 0。
    src 的 下一个 为 ff 的 源列表。
    ff 的 源列表 为 src。
    ff 的 源数量 为 ff 的 源数量 加 1。
完。

字体Face_load 接受 ff：
    若 ff 等于 0：
        返还 0。
    完。
    若 ff 的 状态 等于 FF_STATUS_LOADED：
        返还 ff 的 加载承诺。
    完。
    若 ff 的 状态 等于 FF_STATUS_LOADING：
        返还 ff 的 加载承诺。
    完。
    ff 的 状态 为 FF_STATUS_LOADING。
    ff 的 加载开始时间 为 做 平台_获取高精度时间。
    可变 src 为 ff 的 源列表。
    只要 src 不等于 0：
        数据 为 做 网络_获取字体 用 src 的 URL。
        若 数据 不等于 0 并且 数据 的 长度 大于 0：
            ff 的 字体数据 为 数据 的 数据。
            ff 的 字体数据大小 为 数据 的 长度。
            若 src 的 格式 不等于 0 并且 做 字符串_相等 用 src 的 格式、「woff2」：
                ff 的 字体数据 为 做 WOFF2_解码 用 ff 的 字体数据、ff 的 字体数据大小。
            完。
            ff 的 平台字体 为 做 FreeType_创建字体 用 ff 的 字体数据、ff 的 字体数据大小。
            若 ff 的 平台字体 不等于 0：
                ff 的 状态 为 FF_STATUS_LOADED。
                返还 ff 的 加载承诺。
            完。
        完。
        src 为 src 的 下一个。
    完。
    ff 的 状态 为 FF_STATUS_ERROR。
    返还 ff 的 加载承诺。
完。

字体Face_匹配 接受 ff、家族名、粗细、风格：
    若 ff 等于 0：
        返还 假。
    完。
    若 非 做 字符串_相等_不区分大小写 用 ff 的 家族名、家族名：
        返还 假。
    完。
    若 粗细 小于 ff 的 粗细_最小 或者 粗细 大于 ff 的 粗细_最大：
        返还 假。
    完。
    若 风格 不等于 ff 的 风格 并且 ff 的 风格 不等于 0：
        返还 假。
    完。
    返还 真。
完。

可变 AI_样式字体字_样式缓存命中 为 0。

AI_样式字体字_选择器复杂度 接受 选择器长度、组合子数：
    若 组合子数 大于 5：
        返还 「高复杂度」。
    完。
    若 组合子数 大于 2：
        返还 「中复杂度」。
    完。
    返还 「低复杂度」。
完。

AI_样式字体字_布局缓存 接受 元素哈希：
    AI_样式字体字_样式缓存命中 为 AI_样式字体字_样式缓存命中 加 1。
    返还 元素哈希。
完。

本章 收束。
