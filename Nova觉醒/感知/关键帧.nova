《关键帧》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·函数创建》。

引用《.·内核直连层》。

引用《.·动画时间线》。

引用《..·中枢·辅助存根》。

引用《..·中枢·内容可见性》。

Keyframe 是 一种 事物，含 偏移、缓动、属性数量、属性列表。

KeyframeEffect 是 一种 事物，含 名称、关键帧、关键帧数量、目标元素、持续时间、延迟、结束延迟、迭代次数、方向、填充模式、缓动。

KeyframeProperty 是 一种 事物，含 属性ID、值类型、数字值、颜色值。

KeyframeEffect_创建 接受 名称、目标、持续时间：
    ke 为 做 堆_分配 用 KeyframeEffect。
    ke 的 名称 为 做 字符串_复制 用 名称。
    ke 的 目标元素 为 目标。
    ke 的 关键帧 为 做 堆_分配_数组 用 16 乘 32。
    ke 的 关键帧数量 为 0。
    ke 的 持续时间 为 持续时间。
    ke 的 延迟 为 0。
    ke 的 结束延迟 为 0。
    ke 的 迭代次数 为 100。
    ke 的 方向 为 0。
    ke 的 填充模式 为 0。
    ke 的 缓动 为 做 TimingFunction_创建 用 缓动_ease。
    返还 ke。
完。

KeyframeEffect_添加关键帧 接受 ke、偏移：
    若 ke 的 关键帧数量 大于或等于 16：
        返还 0。
    完。
    kf 为 做 堆_分配 用 Keyframe。
    kf 的 偏移 为 偏移。
    kf 的 缓动 为 ke 的 缓动。
    kf 的 属性数量 为 0。
    kf 的 属性列表 为 做 堆_分配_数组 用 32 乘 12。
    做 内存_写64 用 ke 的 关键帧 加 ke 的 关键帧数量 乘 8、kf。
    ke 的 关键帧数量 为 ke 的 关键帧数量 加 1。
    返还 kf。
完。

Keyframe_添加属性 接受 kf、属性ID、值类型、数字值、颜色值：
    若 kf 的 属性数量 大于或等于 32：
        返还。
    完。
    偏移 为 kf 的 属性列表 加 kf 的 属性数量 乘 12。
    做 内存_写32 用 偏移、属性ID。
    做 内存_写32 用 偏移 加 4、值类型。
    若 值类型 等于 1：
        做 内存_写32 用 偏移 加 8、颜色值。
    否则：
        做 内存_写32 用 偏移 加 8、数字值。
    完。
    kf 的 属性数量 为 kf 的 属性数量 加 1。
完。

动画状态_空闲 为 0。

动画状态_播放中 为 1。

动画状态_暂停 为 2。

动画状态_完成 为 3。

Animation 是 一种 事物，含 效果、时间线、开始时间、当前时间、播放速率、状态、ID、完成回调、取消回调。

可变 动画_下一个ID 为 1。

Animation_创建 接受 效果：
    anim 为 做 堆_分配 用 Animation。
    anim 的 效果 为 效果。
    anim 的 时间线 为 0。
    anim 的 开始时间 为 负 1。
    anim 的 当前时间 为 0。
    anim 的 播放速率 为 100。
    anim 的 状态 为 动画状态_空闲。
    anim 的 ID 为 动画_下一个ID。
    动画_下一个ID 为 动画_下一个ID 加 1。
    anim 的 完成回调 为 0。
    anim 的 取消回调 为 0。
    返还 anim。
完。

Animation_play 接受 anim：
    若 anim 的 状态 等于 动画状态_播放中：
        返还。
    完。
    anim 的 状态 为 动画状态_播放中。
    若 anim 的 开始时间 小于 0：
        anim 的 开始时间 为 做 平台_获取高精度时间 除以 1000。
    完。
    做 AnimationTimeline_注册 用 anim。
完。

Animation_pause 接受 anim：
    若 anim 的 状态 不等于 动画状态_播放中：
        返还。
    完。
    anim 的 状态 为 动画状态_暂停。
    anim 的 当前时间 为 做 Animation_计算当前时间 用 anim。
完。

Animation_cancel 接受 anim：
    anim 的 状态 为 动画状态_空闲。
    anim 的 开始时间 为 负 1。
    anim 的 当前时间 为 0。
完。

Animation_finish 接受 anim：
    若 anim 的 效果 等于 0：
        返还。
    完。
    ke 为 anim 的 效果。
    若 anim 的 播放速率 大于或等于 0：
        anim 的 当前时间 为 ke 的 持续时间。
    否则：
        anim 的 当前时间 为 0。
    完。
    anim 的 状态 为 动画状态_完成。
完。

Animation_计算当前时间 接受 anim：
    若 anim 的 状态 等于 动画状态_暂停：
        返还 anim 的 当前时间。
    完。
    若 anim 的 开始时间 小于 0：
        返还 0。
    完。
    现在 为 做 平台_获取高精度时间 除以 1000。
    返还 (现在 减 anim 的 开始时间) 乘 anim 的 播放速率 除以 100。
完。

本章 收束。
