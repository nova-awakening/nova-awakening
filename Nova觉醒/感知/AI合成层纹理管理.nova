《AI合成层纹理管理》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

纹理预算_默认 为 134217728。

纹理预算_低内存 为 67108864。

LRU_最大条目 为 512。

瓦片_默认大小 为 256。

纹理LRU条目 是 一种 事物，含 瓦片指针、层索引、瓦片索引、大小、上次使用帧、优先级。

纹理管理器 是 一种 事物，含 LRU列表、LRU数量、内存预算、当前使用、当前帧、清理次数、释放总量。

可变 纹理管理 为 0。

AI合成_初始化纹理管理 接受 预算：
    纹理管理 为 做 堆_分配 用 纹理管理器。
    纹理管理 的 LRU列表 为 一组：（空）。
    纹理管理 的 LRU数量 为 0。
    若 预算 大于 0：
        纹理管理 的 内存预算 为 预算。
    否则：
        纹理管理 的 内存预算 为 纹理预算_默认。
    完。
    纹理管理 的 当前使用 为 0。
    纹理管理 的 当前帧 为 0。
    纹理管理 的 清理次数 为 0。
    纹理管理 的 释放总量 为 0。
完。

AI合成_记录瓦片使用 接受 瓦片、层索引、瓦片索引、大小：
    若 纹理管理 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 纹理管理 的 LRU数量：
        若 纹理管理 的 LRU列表 的 第 i 项 的 瓦片指针 等于 瓦片：
            纹理管理 的 LRU列表 的 第 i 项 的 上次使用帧 为 纹理管理 的 当前帧。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 纹理管理 的 LRU数量 大于或等于 LRU_最大条目：
        返还。
    完。
    条目 为 新建 纹理LRU条目。
    条目 的 瓦片指针 为 瓦片。
    条目 的 层索引 为 层索引。
    条目 的 瓦片索引 为 瓦片索引。
    条目 的 大小 为 大小。
    条目 的 上次使用帧 为 纹理管理 的 当前帧。
    条目 的 优先级 为 0。
    做 追加 用 纹理管理 的 LRU列表、条目。
    纹理管理 的 LRU数量 为 纹理管理 的 LRU数量 加 1。
    纹理管理 的 当前使用 为 纹理管理 的 当前使用 加 大小。
完。

AI合成_LRU驱逐 接受 目标释放：
    若 纹理管理 等于 0 或者 纹理管理 的 LRU数量 等于 0：
        返还 0。
    完。
    可变 已释放 为 0。
    只要 已释放 小于 目标释放 并且 纹理管理 的 LRU数量 大于 0：
        可变 最旧索引 为 0。
        可变 最旧帧 为 纹理管理 的 LRU列表 的 第 0 项 的 上次使用帧。
        可变 最低优先 为 纹理管理 的 LRU列表 的 第 0 项 的 优先级。
        可变 i 为 1。
        只要 i 小于 纹理管理 的 LRU数量：
            条目 为 纹理管理 的 LRU列表 的 第 i 项。
            若 条目 的 优先级 小于 最低优先 或者 条目 的 优先级 等于 最低优先 并且 条目 的 上次使用帧 小于 最旧帧：
                最旧索引 为 i。
                最旧帧 为 条目 的 上次使用帧。
                最低优先 为 条目 的 优先级。
            完。
            i 为 i 加 1。
        完。
        驱逐 为 纹理管理 的 LRU列表 的 第 最旧索引 项。
        若 驱逐 的 瓦片指针 不等于 0 并且 驱逐 的 瓦片指针 的 像素数据 不等于 0：
            做 堆_释放 用 驱逐 的 瓦片指针 的 像素数据。
            驱逐 的 瓦片指针 的 像素数据 为 0。
            驱逐 的 瓦片指针 的 需要光栅化 为 真。
            已释放 为 已释放 加 驱逐 的 大小。
        完。
        做 列表_移除 用 纹理管理 的 LRU列表、最旧索引。
        纹理管理 的 LRU数量 为 纹理管理 的 LRU数量 减 1。
    完。
    纹理管理 的 当前使用 为 纹理管理 的 当前使用 减 已释放。
    纹理管理 的 释放总量 为 纹理管理 的 释放总量 加 已释放。
    返还 已释放。
完。

AI合成_检查内存预算 接受 零参：
    若 纹理管理 等于 0：
        返还。
    完。
    若 纹理管理 的 当前使用 大于 纹理管理 的 内存预算：
        超出 为 纹理管理 的 当前使用 减 纹理管理 的 内存预算。
        目标 为 超出 加 纹理管理 的 内存预算 除以 10。
        做 AI合成_LRU驱逐 用 目标。
        纹理管理 的 清理次数 为 纹理管理 的 清理次数 加 1。
    完。
完。

AI合成_清理不可见纹理 接受 lth：
    若 lth 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 lth 的 层数量：
        cl 为 做 内存_读64 用 lth 的 层列表 加 i 乘 8。
        若 cl 不等于 0 并且 cl 的 绘制层 不等于 0：
            pl 为 cl 的 绘制层。
            若 pl 的 视口可见 等于 0：
                总瓦片 为 cl 的 瓦片列数 乘 cl 的 瓦片行数。
                可变 j 为 0。
                只要 j 小于 总瓦片：
                    tile 为 做 内存_读64 用 cl 的 瓦片列表 加 j 乘 8。
                    若 tile 不等于 0 并且 tile 的 像素数据 不等于 0：
                        大小 为 tile 的 宽 乘 tile 的 高 乘 4。
                        做 堆_释放 用 tile 的 像素数据。
                        tile 的 像素数据 为 0。
                        tile 的 需要光栅化 为 真。
                        若 AI合成全局统计 不等于 0：
                            AI合成全局统计 的 纹理内存使用 为 AI合成全局统计 的 纹理内存使用 减 大小。
                            AI合成全局统计 的 AI释放纹理次数 为 AI合成全局统计 的 AI释放纹理次数 加 1。
                        完。
                        若 纹理管理 不等于 0：
                            纹理管理 的 当前使用 为 纹理管理 的 当前使用 减 大小。
                            纹理管理 的 释放总量 为 纹理管理 的 释放总量 加 大小。
                        完。
                    完。
                    j 为 j 加 1。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

AI合成_清理低优先级瓦片 接受 lth、优先级阈值：
    若 lth 等于 0：
        返还 0。
    完。
    可变 释放量 为 0。
    可变 i 为 0。
    只要 i 小于 lth 的 层数量：
        cl 为 做 内存_读64 用 lth 的 层列表 加 i 乘 8。
        若 cl 不等于 0：
            总瓦片 为 cl 的 瓦片列数 乘 cl 的 瓦片行数。
            可变 j 为 0。
            只要 j 小于 总瓦片：
                tile 为 做 内存_读64 用 cl 的 瓦片列表 加 j 乘 8。
                若 tile 不等于 0 并且 tile 的 像素数据 不等于 0 并且 tile 的 优先级 小于 优先级阈值：
                    大小 为 tile 的 宽 乘 tile 的 高 乘 4。
                    做 堆_释放 用 tile 的 像素数据。
                    tile 的 像素数据 为 0。
                    tile 的 需要光栅化 为 真。
                    释放量 为 释放量 加 大小。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 释放量。
完。

AI合成_调整瓦片优先级 接受 lth：
    若 lth 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 lth 的 层数量：
        cl 为 做 内存_读64 用 lth 的 层列表 加 i 乘 8。
        若 cl 不等于 0 并且 cl 的 绘制层 不等于 0：
            pl 为 cl 的 绘制层。
            可变 基础优先级 为 1000。
            若 pl 的 视口可见 等于 2：
                基础优先级 为 基础优先级 加 5000。
            否则若 pl 的 视口可见 等于 1：
                基础优先级 为 基础优先级 加 3000。
            完。
            基础优先级 为 基础优先级 加 pl 的 注意力权重 除以 2。
            若 pl 的 正在动画：
                基础优先级 为 基础优先级 加 2000。
            完。
            若 pl 的 是视频层：
                基础优先级 为 基础优先级 加 1500。
            完。
            总瓦片 为 cl 的 瓦片列数 乘 cl 的 瓦片行数。
            可变 j 为 0。
            只要 j 小于 总瓦片：
                tile 为 做 内存_读64 用 cl 的 瓦片列表 加 j 乘 8。
                若 tile 不等于 0：
                    tile 的 优先级 为 基础优先级。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

AI合成_预测即将可见 接受 lth、滚动方向、滚动速度：
    若 lth 等于 0 或者 纹理管理 等于 0：
        返还。
    完。
    预测距离 为 滚动速度 乘 3。
    可变 i 为 0。
    只要 i 小于 lth 的 层数量：
        cl 为 做 内存_读64 用 lth 的 层列表 加 i 乘 8。
        若 cl 不等于 0 并且 cl 的 绘制层 不等于 0：
            pl 为 cl 的 绘制层。
            若 pl 的 视口可见 等于 0：
                距视口 为 pl 的 到视口距离。
                若 距视口 大于 0 并且 距视口 小于 预测距离：
                    总瓦片 为 cl 的 瓦片列数 乘 cl 的 瓦片行数。
                    可变 j 为 0。
                    只要 j 小于 总瓦片：
                        tile 为 做 内存_读64 用 cl 的 瓦片列表 加 j 乘 8。
                        若 tile 不等于 0：
                            tile 的 优先级 为 tile 的 优先级 加 2000。
                        完。
                        j 为 j 加 1。
                    完。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

AI合成_帧结束 接受 零参：
    若 纹理管理 不等于 0：
        纹理管理 的 当前帧 为 纹理管理 的 当前帧 加 1。
        做 AI合成_检查内存预算。
    完。
完。

AI合成_纹理统计 接受 零参：
    若 纹理管理 等于 0：
        返还 0。
    完。
    返还 一本字典：当前使用 对应 纹理管理 的 当前使用、内存预算 对应 纹理管理 的 内存预算、LRU条目数 对应 纹理管理 的 LRU数量、清理次数 对应 纹理管理 的 清理次数、释放总量 对应 纹理管理 的 释放总量、使用率 对应 若 纹理管理 的 内存预算 大于 0 则 纹理管理 的 当前使用 乘 100 除以 纹理管理 的 内存预算 否则 0。
完。

本章 收束。
