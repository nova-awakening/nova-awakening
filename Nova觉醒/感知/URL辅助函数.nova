《URL辅助函数》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·字符串替换》。

引用《..·中枢·整数转字符串》。

主机类型_域名 为 0。

主机类型_IPv4 为 1。

主机类型_IPv6 为 2。

主机类型_不透明 为 3。

编码集_路径 为 0。

编码集_查询 为 1。

编码集_片段 为 2。

编码集_用户信息 为 3。

编码集_组件 为 4。

URL辅助统计 是 一种 事物，含 百分比编码次数、百分比解码次数、编码字节总数、解码字节总数、序列化次数、Origin提取次数、scheme识别次数、端口设置次数、主机类型检测次数、URL比较次数、URL规范化次数、IPv4解析次数、IPv6解析次数、AI模式调用次数、AI模式命中次数、错误总数、总处理耗时_纳秒、UTF8编码次数。

AI_URL状态 是 一种 事物，含 历史scheme分布、历史平均长度、历史查询参数率、历史片段率、连续https计数、预测下次scheme、采样计数器、学习权重。

可变 全局URL辅助统计 为 0。

可变 全局URL_AI 为 0。

可变 全局URL辅助已初始化 为 假。

URL辅助_初始化 接受 零参：
    若 全局URL辅助已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局URL辅助统计 为 做 堆_分配_清零 用 144。
    若 全局URL辅助统计 等于 空：
        返还 错误_内存不足。
    完。
    全局URL_AI 为 做 堆_分配_清零 用 40。
    若 全局URL_AI 不等于 空：
        全局URL_AI 的 预测下次scheme 为 1。
        全局URL_AI 的 学习权重 为 128。
    完。
    全局URL辅助已初始化 为 真。
    做 打印 用 「[URL辅助] 初始化完成」。
    返还 0。
完。

URL辅助_销毁 接受 零参：
    若 全局URL辅助统计 不等于 空：
        做 堆_释放 用 全局URL辅助统计。
        全局URL辅助统计 为 0。
    完。
    若 全局URL_AI 不等于 空：
        做 堆_释放 用 全局URL_AI。
        全局URL_AI 为 0。
    完。
    全局URL辅助已初始化 为 假。
    返还 0。
完。

URL辅助_重置 接受 零参：
    若 全局URL辅助统计 不等于 空：
        做 内存_置零 用 全局URL辅助统计、144。
    完。
    返还 0。
完。

URL_设置默认端口 接受 url：
    若 做 字符串_相等 用 url 的 scheme、「http」：
        url 的 默认端口 为 80。
        url 的 是特殊 为 真。
    否则若 做 字符串_相等 用 url 的 scheme、「https」：
        url 的 默认端口 为 443。
        url 的 是特殊 为 真。
    否则若 做 字符串_相等 用 url 的 scheme、「ftp」：
        url 的 默认端口 为 21。
        url 的 是特殊 为 真。
    否则若 做 字符串_相等 用 url 的 scheme、「ws」：
        url 的 默认端口 为 80。
        url 的 是特殊 为 真。
    否则若 做 字符串_相等 用 url 的 scheme、「wss」：
        url 的 默认端口 为 443。
        url 的 是特殊 为 真。
    否则若 做 字符串_相等 用 url 的 scheme、「file」：
        url 的 默认端口 为 0。
        url 的 是特殊 为 真。
    否则：
        url 的 默认端口 为 0。
        url 的 是特殊 为 假。
    完。
完。

URL_有效端口 接受 url：
    若 url 的 port 不等于 0：
        返还 url 的 port。
    完。
    返还 url 的 默认端口。
完。

URL_是空白 接受 ch：
    返还 ch 等于 32 或者 ch 等于 9 或者 ch 等于 10 或者 ch 等于 13。
完。

URL_是字母 接受 ch：
    返还 ch 大于或等于 65 并且 ch 小于或等于 90 或者 ch 大于或等于 97 并且 ch 小于或等于 122。
完。

URL_是数字 接受 ch：
    返还 ch 大于或等于 48 并且 ch 小于或等于 57。
完。

URL_是scheme字符 接受 ch：
    返还 做 URL_是字母 用 ch 或者 做 URL_是数字 用 ch 或者 ch 等于 43 或者 ch 等于 45 或者 ch 等于 46。
完。

URL_百分比解码 接受 s：
    若 s 等于 0：
        返还 0。
    完。
    长度 为 做 字节长度 用 s。
    结果 为 做 堆_分配_数组 用 长度 加 1。
    可变 ri 为 0。
    可变 si 为 0。
    只要 si 小于 长度：
        若 做 字节取 用 s、si 等于 37 并且 si 加 2 小于 长度：
            高 为 做 URL_hex值 用 做 字节取 用 s、si 加 1。
            低 为 做 URL_hex值 用 做 字节取 用 s、si 加 2。
            若 高 大于或等于 0 并且 低 大于或等于 0：
                做 内存_写8 用 结果 加 ri、高 乘 16 加 低。
                ri 为 ri 加 1。
                si 为 si 加 3。
                继续。
            完。
        完。
        做 内存_写8 用 结果 加 ri、做 字节取 用 s、si。
        ri 为 ri 加 1。
        si 为 si 加 1。
    完。
    做 内存_写8 用 结果 加 ri、0。
    返还 结果。
完。

URL_百分比编码 接受 s、编码集：
    若 s 等于 0：
        返还 0。
    完。
    长度 为 做 字节长度 用 s。
    结果 为 做 堆_分配_数组 用 长度 乘 3 加 1。
    可变 ri 为 0。
    可变 si 为 0。
    只要 si 小于 长度：
        ch 为 做 字节取 用 s、si。
        若 做 URL_需要编码 用 ch、编码集：
            做 内存_写8 用 结果 加 ri、37。
            做 内存_写8 用 结果 加 ri 加 1、做 URL_hex字符 用 ch 右移 4。
            做 内存_写8 用 结果 加 ri 加 2、做 URL_hex字符 用 ch 位与 0x0F。
            ri 为 ri 加 3。
        否则：
            做 内存_写8 用 结果 加 ri、ch。
            ri 为 ri 加 1。
        完。
        si 为 si 加 1。
    完。
    做 内存_写8 用 结果 加 ri、0。
    返还 结果。
完。

URL_需要编码 接受 ch、集合：
    若 做 URL_是字母 用 ch 或者 做 URL_是数字 用 ch：
        返还 假。
    完。
    若 ch 等于 45 或者 ch 等于 46 或者 ch 等于 95 或者 ch 等于 126：
        返还 假。
    完。
    返还 真。
完。

URL_序列化 接受 url：
    若 url 等于 0 或者 非 url 的 是有效：
        返还 「」。
    完。
    可变 结果 为 url 的 scheme。
    结果 为 做 字符串拼接 用 结果、「://」。
    若 url 的 username 不等于 0：
        结果 为 做 字符串拼接 用 结果、url 的 username。
        若 url 的 password 不等于 0：
            结果 为 做 字符串拼接 用 结果、「:」。
            结果 为 做 字符串拼接 用 结果、url 的 password。
        完。
        结果 为 做 字符串拼接 用 结果、「@」。
    完。
    结果 为 做 字符串拼接 用 结果、url 的 host。
    若 url 的 port 不等于 0 并且 url 的 port 不等于 url 的 默认端口：
        结果 为 做 字符串拼接 用 结果、「:」。
        结果 为 做 字符串拼接 用 结果、做 整数转字符串 用 url 的 port。
    完。
    结果 为 做 字符串拼接 用 结果、url 的 path。
    若 url 的 query 不等于 0 并且 url 的 query长度 大于 0：
        结果 为 做 字符串拼接 用 结果、「?」。
        结果 为 做 字符串拼接 用 结果、url 的 query。
    完。
    若 url 的 fragment 不等于 0 并且 url 的 fragment长度 大于 0：
        结果 为 做 字符串拼接 用 结果、「#」。
        结果 为 做 字符串拼接 用 结果、url 的 fragment。
    完。
    返还 结果。
完。

URL_提取Origin 接受 url：
    若 url 等于 0 或者 非 url 的 是有效：
        返还 0。
    完。
    可变 结果 为 url 的 scheme。
    结果 为 做 字符串拼接 用 结果、「://」。
    结果 为 做 字符串拼接 用 结果、url 的 host。
    若 url 的 port 不等于 0 并且 url 的 port 不等于 url 的 默认端口：
        结果 为 做 字符串拼接 用 结果、「:」。
        结果 为 做 字符串拼接 用 结果、做 整数转字符串 用 url 的 port。
    完。
    若 全局URL辅助统计 不等于 空：
        全局URL辅助统计 的 Origin提取次数 为 全局URL辅助统计 的 Origin提取次数 加 1。
    完。
    返还 结果。
完。

URL_检测主机类型 接受 host：
    若 host 等于 空：
        返还 主机类型_不透明。
    完。
    长度 为 做 字节长度 用 host。
    若 长度 等于 0：
        返还 主机类型_不透明。
    完。
    若 做 字节取 用 host、0 等于 91：
        若 全局URL辅助统计 不等于 空：
            全局URL辅助统计 的 IPv6解析次数 为 全局URL辅助统计 的 IPv6解析次数 加 1。
        完。
        返还 主机类型_IPv6。
    完。
    可变 全数字点 为 真。
    可变 点数 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        ch 为 做 字节取 用 host、i。
        若 ch 等于 46：
            点数 为 点数 加 1。
        否则若 ch 小于 48 或者 ch 大于 57：
            全数字点 为 假。
            止。
        完。
        i 为 i 加 1。
    完。
    若 全数字点 等于 真 并且 点数 等于 3：
        若 全局URL辅助统计 不等于 空：
            全局URL辅助统计 的 IPv4解析次数 为 全局URL辅助统计 的 IPv4解析次数 加 1。
        完。
        返还 主机类型_IPv4。
    完。
    若 全局URL辅助统计 不等于 空：
        全局URL辅助统计 的 主机类型检测次数 为 全局URL辅助统计 的 主机类型检测次数 加 1。
    完。
    返还 主机类型_域名。
完。

URL_相等 接受 urlA、urlB：
    若 urlA 等于 空 或者 urlB 等于 空：
        返还 假。
    完。
    若 全局URL辅助统计 不等于 空：
        全局URL辅助统计 的 URL比较次数 为 全局URL辅助统计 的 URL比较次数 加 1。
    完。
    若 做 字符串_相等 用 urlA 的 scheme、urlB 的 scheme 等于 假：
        返还 假。
    完。
    若 做 字符串_相等 用 urlA 的 host、urlB 的 host 等于 假：
        返还 假。
    完。
    若 做 URL_有效端口 用 urlA 不等于 做 URL_有效端口 用 urlB：
        返还 假。
    完。
    若 做 字符串_相等 用 urlA 的 path、urlB 的 path 等于 假：
        返还 假。
    完。
    若 urlA 的 query 不等于 空 并且 urlB 的 query 不等于 空：
        若 做 字符串_相等 用 urlA 的 query、urlB 的 query 等于 假：
            返还 假。
        完。
    否则若 urlA 的 query 不等于 urlB 的 query：
        返还 假。
    完。
    返还 真。
完。

URL_同源 接受 urlA、urlB：
    若 urlA 等于 空 或者 urlB 等于 空：
        返还 假。
    完。
    若 做 字符串_相等 用 urlA 的 scheme、urlB 的 scheme 等于 假：
        返还 假。
    完。
    若 做 字符串_相等 用 urlA 的 host、urlB 的 host 等于 假：
        返还 假。
    完。
    若 做 URL_有效端口 用 urlA 不等于 做 URL_有效端口 用 urlB：
        返还 假。
    完。
    返还 真。
完。

URL_规范化路径 接受 路径：
    若 路径 等于 空：
        返还 路径。
    完。
    长度 为 做 字节长度 用 路径。
    若 长度 等于 0：
        返还 路径。
    完。
    若 全局URL辅助统计 不等于 空：
        全局URL辅助统计 的 URL规范化次数 为 全局URL辅助统计 的 URL规范化次数 加 1。
    完。
    栈 为 做 堆_分配_清零 用 64 乘 8。
    若 栈 等于 空：
        返还 路径。
    完。
    可变 栈顶 为 0。
    可变 段起始 为 0。
    可变 i 为 0。
    若 做 字节取 用 路径、0 等于 47：
        段起始 为 1。
        i 为 1。
    完。
    只要 i 小于或等于 长度：
        可变 是分隔 为 假。
        若 i 等于 长度：
            是分隔 为 真。
        否则若 做 字节取 用 路径、i 等于 47：
            是分隔 为 真。
        完。
        若 是分隔 等于 真：
            段长 为 i 减 段起始。
            若 段长 等于 1 并且 做 字节取 用 路径、段起始 等于 46：
            否则若 段长 等于 2 并且 做 字节取 用 路径、段起始 等于 46 并且 做 字节取 用 路径、段起始 加 1 等于 46：
                若 栈顶 大于 0：
                    栈顶 为 栈顶 减 1。
                完。
            否则若 段长 大于 0：
                若 栈顶 小于 64：
                    做 内存_写64 用 栈 加 栈顶 乘 8、段起始。
                    栈顶 为 栈顶 加 1。
                完。
            完。
            段起始 为 i 加 1。
        完。
        i 为 i 加 1。
    完。
    结果 为 做 堆_分配 用 长度 加 1。
    若 结果 等于 空：
        做 堆_释放 用 栈。
        返还 路径。
    完。
    可变 写位置 为 0。
    做 内存_写8 用 结果、47。
    写位置 为 1。
    可变 s 为 0。
    只要 s 小于 栈顶：
        段偏移 为 做 内存_读64 用 栈 加 s 乘 8。
        可变 段结束 为 段偏移。
        只要 段结束 小于 长度 并且 做 字节取 用 路径、段结束 不等于 47：
            段结束 为 段结束 加 1。
        完。
        段字节 为 段结束 减 段偏移。
        可变 j 为 0。
        只要 j 小于 段字节：
            做 内存_写8 用 结果 加 写位置、做 字节取 用 路径、段偏移 加 j。
            写位置 为 写位置 加 1。
            j 为 j 加 1。
        完。
        若 s 小于 栈顶 减 1：
            做 内存_写8 用 结果 加 写位置、47。
            写位置 为 写位置 加 1。
        完。
        s 为 s 加 1。
    完。
    做 内存_写8 用 结果 加 写位置、0。
    做 堆_释放 用 栈。
    返还 结果。
完。

URL_scheme转小写 接受 s：
    若 s 等于 空：
        返还。
    完。
    长度 为 做 字节长度 用 s。
    可变 i 为 0。
    只要 i 小于 长度：
        ch 为 做 字节取 用 s、i。
        若 ch 大于或等于 65 并且 ch 小于或等于 90：
            做 内存_写8 用 s 加 i、ch 加 32。
        完。
        i 为 i 加 1。
    完。
完。

URL_host转小写 接受 s：
    若 s 等于 空：
        返还。
    完。
    长度 为 做 字节长度 用 s。
    可变 i 为 0。
    只要 i 小于 长度：
        ch 为 做 字节取 用 s、i。
        若 ch 大于或等于 65 并且 ch 小于或等于 90：
            做 内存_写8 用 s 加 i、ch 加 32。
        完。
        i 为 i 加 1。
    完。
完。

AI_URL_记录 接受 url：
    若 全局URL_AI 等于 空 或者 url 等于 空：
        返还。
    完。
    AI 为 全局URL_AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    可变 url_长度 为 0。
    若 url 的 path 不等于 空：
        url_长度 为 做 字节长度 用 url 的 path。
    完。
    AI 的 历史平均长度 为 (AI 的 历史平均长度 乘 7 加 url_长度 乘 1024) 除以 8。
    可变 有查询 为 假。
    若 url 的 query 不等于 空：
        有查询 为 真。
    完。
    可变 有片段 为 假。
    若 url 的 fragment 不等于 空：
        有片段 为 真。
    完。
    若 有查询 等于 真：
        AI 的 历史查询参数率 为 (AI 的 历史查询参数率 乘 15 加 1024) 除以 16。
    否则：
        AI 的 历史查询参数率 为 AI 的 历史查询参数率 乘 15 除以 16。
    完。
    若 有片段 等于 真：
        AI 的 历史片段率 为 (AI 的 历史片段率 乘 15 加 1024) 除以 16。
    否则：
        AI 的 历史片段率 为 AI 的 历史片段率 乘 15 除以 16。
    完。
    可变 当前scheme 为 2。
    若 做 字符串_相等 用 url 的 scheme、「https」：
        当前scheme 为 1。
        AI 的 连续https计数 为 AI 的 连续https计数 加 1。
    否则若 做 字符串_相等 用 url 的 scheme、「http」：
        当前scheme 为 0。
        AI 的 连续https计数 为 0。
    否则：
        AI 的 连续https计数 为 0。
    完。
    若 AI 的 采样计数器 小于 16：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 全局URL辅助统计 不等于 空：
        全局URL辅助统计 的 AI模式调用次数 为 全局URL辅助统计 的 AI模式调用次数 加 1。
    完。
    若 AI 的 连续https计数 大于 5：
        AI 的 预测下次scheme 为 1。
    否则：
        AI 的 预测下次scheme 为 当前scheme。
    完。
    若 AI 的 预测下次scheme 等于 当前scheme：
        若 全局URL辅助统计 不等于 空：
            全局URL辅助统计 的 AI模式命中次数 为 全局URL辅助统计 的 AI模式命中次数 加 1。
        完。
    完。
完。

URL辅助_打印统计 接受 零参：
    若 全局URL辅助统计 等于 空：
        做 打印 用 「[URL辅助] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   URL辅助函数 · 统计报告            │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 百分比编码:       」、做 转字符串 用 全局URL辅助统计 的 百分比编码次数。
    做 打印 用 做 字符串拼接 用 「│ 百分比解码:       」、做 转字符串 用 全局URL辅助统计 的 百分比解码次数。
    做 打印 用 做 字符串拼接 用 「│ 序列化:           」、做 转字符串 用 全局URL辅助统计 的 序列化次数。
    做 打印 用 做 字符串拼接 用 「│ Origin提取:       」、做 转字符串 用 全局URL辅助统计 的 Origin提取次数。
    做 打印 用 做 字符串拼接 用 「│ scheme识别:       」、做 转字符串 用 全局URL辅助统计 的 scheme识别次数。
    做 打印 用 做 字符串拼接 用 「│ 主机类型检测:     」、做 转字符串 用 全局URL辅助统计 的 主机类型检测次数。
    做 打印 用 做 字符串拼接 用 「│ URL比较:          」、做 转字符串 用 全局URL辅助统计 的 URL比较次数。
    做 打印 用 做 字符串拼接 用 「│ URL规范化:        」、做 转字符串 用 全局URL辅助统计 的 URL规范化次数。
    做 打印 用 做 字符串拼接 用 「│ IPv4解析:         」、做 转字符串 用 全局URL辅助统计 的 IPv4解析次数。
    做 打印 用 做 字符串拼接 用 「│ IPv6解析:         」、做 转字符串 用 全局URL辅助统计 的 IPv6解析次数。
    做 打印 用 做 字符串拼接 用 「│ AI模式调用:       」、做 转字符串 用 全局URL辅助统计 的 AI模式调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI模式命中:       」、做 转字符串 用 全局URL辅助统计 的 AI模式命中次数。
    做 打印 用 做 字符串拼接 用 「│ 总耗时(μs):       」、做 转字符串 用 全局URL辅助统计 的 总处理耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 错误:             」、做 转字符串 用 全局URL辅助统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

URL辅助_导出诊断 接受 零参：
    若 全局URL_AI 等于 空：
        做 打印 用 「[URL AI] 未初始化」。
        返还。
    完。
    AI 为 全局URL_AI。
    做 打印 用 「[URL AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史平均长度(Q10): 」、做 转字符串 用 AI 的 历史平均长度。
    做 打印 用 做 字符串拼接 用 「  历史查询参数率(Q10): 」、做 转字符串 用 AI 的 历史查询参数率。
    做 打印 用 做 字符串拼接 用 「  历史片段率(Q10): 」、做 转字符串 用 AI 的 历史片段率。
    做 打印 用 做 字符串拼接 用 「  连续https: 」、做 转字符串 用 AI 的 连续https计数。
    做 打印 用 做 字符串拼接 用 「  预测scheme: 」、做 转字符串 用 AI 的 预测下次scheme。
完。

URL辅助_获取统计 接受 零参：
    返还 全局URL辅助统计。
完。

URL辅助_获取AI状态 接受 零参：
    返还 全局URL_AI。
完。

URL辅助_是否已初始化 接受 零参：
    返还 全局URL辅助已初始化。
完。

本章 收束。
