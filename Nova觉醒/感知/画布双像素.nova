《画布双像素》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·变换》。

引用《.·内核直连层》。

引用《.·样式滤镜》。

画布2D_写像素 接受 ctx、x、y、r、g、b、a：
    若 ctx 等于 0：
        返还。
    完。
    若 x 小于 0 或者 x 大于或等于 ctx 的 宽 或者 y 小于 0 或者 y 大于或等于 ctx 的 高：
        返还。
    完。
    偏移 为 (y 乘 ctx 的 宽 加 x) 乘 4。
    s 为 ctx 的 状态。
    sa 为 a 乘 s 的 全局透明度 除以 255。
    若 sa 等于 255：
        做 内存_写8 用 ctx 的 像素缓冲 加 偏移、r。
        做 内存_写8 用 ctx 的 像素缓冲 加 偏移 加 1、g。
        做 内存_写8 用 ctx 的 像素缓冲 加 偏移 加 2、b。
        做 内存_写8 用 ctx 的 像素缓冲 加 偏移 加 3、255。
    否则若 sa 大于 0：
        dr 为 做 内存_读8 用 ctx 的 像素缓冲 加 偏移。
        dg 为 做 内存_读8 用 ctx 的 像素缓冲 加 偏移 加 1。
        db 为 做 内存_读8 用 ctx 的 像素缓冲 加 偏移 加 2。
        da 为 做 内存_读8 用 ctx 的 像素缓冲 加 偏移 加 3。
        inv 为 255 减 sa。
        做 内存_写8 用 ctx 的 像素缓冲 加 偏移、(r 乘 sa 加 dr 乘 inv) 除以 255。
        做 内存_写8 用 ctx 的 像素缓冲 加 偏移 加 1、(g 乘 sa 加 dg 乘 inv) 除以 255。
        做 内存_写8 用 ctx 的 像素缓冲 加 偏移 加 2、(b 乘 sa 加 db 乘 inv) 除以 255。
        做 内存_写8 用 ctx 的 像素缓冲 加 偏移 加 3、sa 加 da 乘 inv 除以 255。
    完。
完。

画布2D_fillRect 接受 ctx、x、y、w、h：
    若 ctx 等于 0：
        返还。
    完。
    s 为 ctx 的 状态。
    可变 py 为 y。
    只要 py 小于 y 加 h：
        可变 px 为 x。
        只要 px 小于 x 加 w：
            tx 为 (s 的 a 乘 px 加 s 的 c 乘 py) 除以 1024 加 s 的 e。
            ty 为 (s 的 b 乘 px 加 s 的 d 乘 py) 除以 1024 加 s 的 f。
            做 画布2D_写像素 用 ctx、tx、ty、s 的 填充颜色R、s 的 填充颜色G、s 的 填充颜色B、s 的 填充颜色A。
            px 为 px 加 1。
        完。
        py 为 py 加 1。
    完。
完。

画布2D_strokeRect 接受 ctx、x、y、w、h：
    做 画布2D_beginPath 用 ctx。
    做 画布2D_rect 用 ctx、x、y、w、h。
    做 画布2D_stroke 用 ctx。
完。

画布2D_clearRect 接受 ctx、x、y、w、h：
    若 ctx 等于 0：
        返还。
    完。
    可变 py 为 y。
    只要 py 小于 y 加 h 并且 py 小于 ctx 的 高：
        可变 px 为 x。
        只要 px 小于 x 加 w 并且 px 小于 ctx 的 宽：
            若 px 大于或等于 0 并且 py 大于或等于 0：
                偏移 为 (py 乘 ctx 的 宽 加 px) 乘 4。
                做 内存_写32 用 ctx 的 像素缓冲 加 偏移、0。
            完。
            px 为 px 加 1。
        完。
        py 为 py 加 1。
    完。
完。

画布Gradient 是 一种 事物，含 类型、x0、r0、色标、色标位置、色标数。

画布2D_createLinearGradient 接受 x0、y0、x1、y1：
    g 为 做 堆_分配 用 画布Gradient。
    g 的 类型 为 0。
    g 的 x0 为 x0。
    g 的 y0 为 y0。
    g 的 x1 为 x1。
    g 的 y1 为 y1。
    g 的 色标数 为 0。
    返还 g。
完。

画布2D_createRadialGradient 接受 x0、y0、r0、x1、y1、r1：
    g 为 做 堆_分配 用 画布Gradient。
    g 的 类型 为 1。
    g 的 x0 为 x0。
    g 的 y0 为 y0。
    g 的 r0 为 r0。
    g 的 x1 为 x1。
    g 的 y1 为 y1。
    g 的 r1 为 r1。
    g 的 色标数 为 0。
    返还 g。
完。

画布Gradient_addColorStop 接受 g、位置、R、G、B、A：
    若 g 等于 0 或者 g 的 色标数 大于或等于 16：
        返还。
    完。
    i 为 g 的 色标数。
    g 的 色标 的 第 i 项 为 R 左移 24 位或 G 左移 16 位或 B 左移 8 位或 A。
    g 的 色标位置 的 第 i 项 为 位置。
    g 的 色标数 为 g 的 色标数 加 1。
完。

本章 收束。
