《命中测试》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《.·创建销毁》。

引用《..·中枢·内核打印》。

引用《.·从布局树构》。

引用《..·中枢·整数转字符串》。

命中测试_最大结果 为 32。

命中测试_透明度阈值 为 1。

HitTestResult 是 一种 事物，含 目标节点、目标层、局部x、局部y。

HitTestResultEx 是 一种 事物，含 目标节点、目标层、局部x、局部y、z序、透明度、可交互、裁剪区域内。

HitTestMultiResult 是 一种 事物，含 结果列表、数量、容量。

命中测试统计 是 一种 事物，含 测试调用数、层遍历数、对象遍历数、命中成功数、命中失败数、变换测试数、裁剪跳过数。

可变 全局_命中统计 为 空。

命中统计_初始化 接受 零参：
    若 全局_命中统计 等于 空：
        全局_命中统计 为 新建 命中测试统计。
        全局_命中统计 的 测试调用数 为 0。
        全局_命中统计 的 层遍历数 为 0。
        全局_命中统计 的 对象遍历数 为 0。
        全局_命中统计 的 命中成功数 为 0。
        全局_命中统计 的 命中失败数 为 0。
        全局_命中统计 的 变换测试数 为 0。
        全局_命中统计 的 裁剪跳过数 为 0。
    完。
完。

HitTestMultiResult_创建 接受 零参：
    多结果 为 新建 HitTestMultiResult。
    多结果 的 结果列表 为 做 堆_分配 用 命中测试_最大结果 乘 64。
    多结果 的 数量 为 0。
    多结果 的 容量 为 命中测试_最大结果。
    返还 多结果。
完。

HitTestMultiResult_添加 接受 多结果、节点、层、lx、ly、z、alpha、交互：
    若 多结果 的 数量 大于或等于 多结果 的 容量：
        返还。
    完。
    项 为 新建 HitTestResultEx。
    项 的 目标节点 为 节点。
    项 的 目标层 为 层。
    项 的 局部x 为 lx。
    项 的 局部y 为 ly。
    项 的 z序 为 z。
    项 的 透明度 为 alpha。
    项 的 可交互 为 交互。
    项 的 裁剪区域内 为 真。
    做 内存_写64 用 多结果 的 结果列表 加 多结果 的 数量 乘 64、项。
    多结果 的 数量 为 多结果 的 数量 加 1。
完。

绘制Layer_命中测试 接受 层、测试x、测试y、结果：
    做 命中统计_初始化。
    全局_命中统计 的 测试调用数 为 全局_命中统计 的 测试调用数 加 1。
    若 层 等于 0：
        全局_命中统计 的 命中失败数 为 全局_命中统计 的 命中失败数 加 1。
        返还 0。
    完。
    lo 为 层 的 布局对象。
    若 lo 等于 0：
        返还 0。
    完。
    s 为 lo 的 样式。
    若 s 不等于 0 并且 s 的 visibility 等于 Visibility_hidden：
        返还 0。
    完。
    若 s 不等于 0 并且 s 的 pointer_事件s 等于 5：
        返还 0。
    完。
    全局_命中统计 的 层遍历数 为 全局_命中统计 的 层遍历数 加 1。
    若 s 不等于 0 并且 s 的 overflow 等于 Overflow_hidden：
        裁剪宽 为 做 布局Object_边框盒宽 用 lo。
        裁剪高 为 做 布局Object_边框盒高 用 lo。
        若 非 做 绘制Layer_点在矩形内 用 测试x、测试y、lo 的 绝对x、lo 的 绝对y、裁剪宽、裁剪高：
            全局_命中统计 的 裁剪跳过数 为 全局_命中统计 的 裁剪跳过数 加 1。
            返还 0。
        完。
    完。
    可变 有效x 为 测试x。
    可变 有效y 为 测试y。
    若 层 的 滚动x 不等于 0 或者 层 的 滚动y 不等于 0：
        有效x 为 测试x 加 层 的 滚动x。
        有效y 为 测试y 加 层 的 滚动y。
    完。
    若 层 的 有变换 并且 层 的 变换矩阵 不等于 0：
        全局_命中统计 的 变换测试数 为 全局_命中统计 的 变换测试数 加 1。
        逆变换结果 为 做 命中测试_逆变换 用 层 的 变换矩阵、有效x、有效y。
        有效x 为 逆变换结果 的 第 0 项。
        有效y 为 逆变换结果 的 第 1 项。
    完。
    可变 i 为 层 的 正z序数量 减 1。
    只要 i 大于或等于 0：
        子层 为 做 内存_读64 用 层 的 正z序列表 加 i 乘 8。
        若 做 绘制Layer_命中测试 用 子层、有效x、有效y、结果：
            全局_命中统计 的 命中成功数 为 全局_命中统计 的 命中成功数 加 1。
            返还 1。
        完。
        i 为 i 减 1。
    完。
    若 做 绘制Layer_命中测试内容 用 层、有效x、有效y、结果：
        全局_命中统计 的 命中成功数 为 全局_命中统计 的 命中成功数 加 1。
        返还 1。
    完。
    i 为 层 的 负z序数量 减 1。
    只要 i 大于或等于 0：
        子层 为 做 内存_读64 用 层 的 负z序列表 加 i 乘 8。
        若 做 绘制Layer_命中测试 用 子层、有效x、有效y、结果：
            全局_命中统计 的 命中成功数 为 全局_命中统计 的 命中成功数 加 1。
            返还 1。
        完。
        i 为 i 减 1。
    完。
    bw 为 做 布局Object_边框盒宽 用 lo。
    bh 为 做 布局Object_边框盒高 用 lo。
    若 s 不等于 0 并且 s 的 border_radius 大于 0：
        若 做 绘制Layer_点在圆角矩形内 用 有效x、有效y、lo 的 绝对x、lo 的 绝对y、bw、bh、s 的 border_radius：
            结果 的 目标节点 为 lo 的 DOM节点。
            结果 的 目标层 为 层。
            结果 的 局部x 为 有效x 减 lo 的 绝对x。
            结果 的 局部y 为 有效y 减 lo 的 绝对y。
            全局_命中统计 的 命中成功数 为 全局_命中统计 的 命中成功数 加 1。
            返还 1。
        完。
    否则若 做 绘制Layer_点在矩形内 用 有效x、有效y、lo 的 绝对x、lo 的 绝对y、bw、bh：
        结果 的 目标节点 为 lo 的 DOM节点。
        结果 的 目标层 为 层。
        结果 的 局部x 为 有效x 减 lo 的 绝对x。
        结果 的 局部y 为 有效y 减 lo 的 绝对y。
        全局_命中统计 的 命中成功数 为 全局_命中统计 的 命中成功数 加 1。
        返还 1。
    完。
    返还 0。
完。

绘制Layer_命中测试内容 接受 层、测试x、测试y、结果：
    lo 为 层 的 布局对象。
    可变 子 为 lo 的 最后子。
    只要 子 不等于 0：
        若 非 做 绘制Layer_需要自己的层 用 子：
            全局_命中统计 的 对象遍历数 为 全局_命中统计 的 对象遍历数 加 1。
            若 做 绘制Layer_命中测试布局对象 用 子、测试x、测试y、结果：
                返还 1。
            完。
        完。
        子 为 子 的 前兄。
    完。
    返还 0。
完。

绘制Layer_命中测试布局对象 接受 lo、测试x、测试y、结果：
    若 lo 的 样式 不等于 0 并且 lo 的 样式 的 display 等于 Display_none：
        返还 0。
    完。
    若 lo 的 样式 不等于 0 并且 lo 的 样式 的 opacity 小于 命中测试_透明度阈值：
        返还 0。
    完。
    全局_命中统计 的 对象遍历数 为 全局_命中统计 的 对象遍历数 加 1。
    可变 子 为 lo 的 最后子。
    只要 子 不等于 0：
        若 非 做 绘制Layer_需要自己的层 用 子：
            若 做 绘制Layer_命中测试布局对象 用 子、测试x、测试y、结果：
                返还 1。
            完。
        完。
        子 为 子 的 前兄。
    完。
    bw 为 做 布局Object_边框盒宽 用 lo。
    bh 为 做 布局Object_边框盒高 用 lo。
    若 做 绘制Layer_点在矩形内 用 测试x、测试y、lo 的 绝对x、lo 的 绝对y、bw、bh：
        结果 的 目标节点 为 lo 的 DOM节点。
        结果 的 局部x 为 测试x 减 lo 的 绝对x。
        结果 的 局部y 为 测试y 减 lo 的 绝对y。
        返还 1。
    完。
    返还 0。
完。

绘制Layer_命中测试_收集全部 接受 层、测试x、测试y、多结果：
    若 层 等于 0：
        返还。
    完。
    lo 为 层 的 布局对象。
    若 lo 等于 0：
        返还。
    完。
    s 为 lo 的 样式。
    若 s 不等于 0 并且 s 的 visibility 等于 Visibility_hidden：
        返还。
    完。
    可变 i 为 层 的 正z序数量 减 1。
    只要 i 大于或等于 0：
        做 绘制Layer_命中测试_收集全部 用 做 内存_读64 用 层 的 正z序列表 加 i 乘 8、测试x、测试y、多结果。
        i 为 i 减 1。
    完。
    bw 为 做 布局Object_边框盒宽 用 lo。
    bh 为 做 布局Object_边框盒高 用 lo。
    若 做 绘制Layer_点在矩形内 用 测试x、测试y、lo 的 绝对x、lo 的 绝对y、bw、bh：
        alpha 为 255。
        若 s 不等于 0：
            alpha 为 s 的 opacity。
        完。
        交互 为 真。
        若 s 不等于 0 并且 s 的 pointer_事件s 等于 5：
            交互 为 假。
        完。
        做 HitTestMultiResult_添加 用 多结果、lo 的 DOM节点、层、测试x 减 lo 的 绝对x、测试y 减 lo 的 绝对y、层 的 z序、alpha、交互。
    完。
    i 为 层 的 负z序数量 减 1。
    只要 i 大于或等于 0：
        做 绘制Layer_命中测试_收集全部 用 做 内存_读64 用 层 的 负z序列表 加 i 乘 8、测试x、测试y、多结果。
        i 为 i 减 1。
    完。
完。

绘制Layer_点在矩形内 接受 px、py、rx、ry、rw、rh：
    返还 px 大于或等于 rx 并且 px 小于 rx 加 rw 并且 py 大于或等于 ry 并且 py 小于 ry 加 rh。
完。

绘制Layer_点在圆角矩形内 接受 px、py、rx、ry、rw、rh、半径：
    若 非 做 绘制Layer_点在矩形内 用 px、py、rx、ry、rw、rh：
        返还 假。
    完。
    r 为 半径。
    若 r 大于 rw 除以 2：
        r 为 rw 除以 2。
    完。
    若 r 大于 rh 除以 2：
        r 为 rh 除以 2。
    完。
    若 px 小于 rx 加 r 并且 py 小于 ry 加 r：
        返还 做 命中测试_点在圆内 用 px、py、rx 加 r、ry 加 r、r。
    完。
    若 px 大于或等于 rx 加 rw 减 r 并且 py 小于 ry 加 r：
        返还 做 命中测试_点在圆内 用 px、py、rx 加 rw 减 r、ry 加 r、r。
    完。
    若 px 小于 rx 加 r 并且 py 大于或等于 ry 加 rh 减 r：
        返还 做 命中测试_点在圆内 用 px、py、rx 加 r、ry 加 rh 减 r、r。
    完。
    若 px 大于或等于 rx 加 rw 减 r 并且 py 大于或等于 ry 加 rh 减 r：
        返还 做 命中测试_点在圆内 用 px、py、rx 加 rw 减 r、ry 加 rh 减 r、r。
    完。
    返还 真。
完。

命中测试_点在圆内 接受 px、py、cx、cy、r：
    dx 为 px 减 cx。
    dy 为 py 减 cy。
    返还 dx 乘 dx 加 dy 乘 dy 小于或等于 r 乘 r。
完。

命中测试_点在椭圆内 接受 px、py、cx、cy、rx、ry：
    若 rx 小于或等于 0 或者 ry 小于或等于 0：
        返还 假。
    完。
    dx 为 px 减 cx。
    dy 为 py 减 cy。
    返还 dx 乘 dx 乘 ry 乘 ry 加 dy 乘 dy 乘 rx 乘 rx 小于或等于 rx 乘 rx 乘 ry 乘 ry。
完。

命中测试_逆变换 接受 矩阵、x、y：
    a 为 做 内存_读64 用 矩阵。
    b 为 做 内存_读64 用 矩阵 加 8。
    c 为 做 内存_读64 用 矩阵 加 16。
    d 为 做 内存_读64 用 矩阵 加 24。
    tx 为 做 内存_读64 用 矩阵 加 32。
    ty 为 做 内存_读64 用 矩阵 加 40。
    det 为 a 乘 d 减 b 乘 c。
    若 det 等于 0：
        返还 一组：x、y。
    完。
    ix 为 x 减 tx。
    iy 为 y 减 ty。
    新x 为 (d 乘 ix 减 b 乘 iy) 除以 det。
    新y 为 (a 乘 iy 减 c 乘 ix) 除以 det。
    返还 一组：新x、新y。
完。

命中测试_最近可交互节点 接受 层、测试x、测试y、最大距离：
    多结果 为 做 HitTestMultiResult_创建。
    做 绘制Layer_命中测试_收集全部 用 层、测试x、测试y、多结果。
    若 多结果 的 数量 大于 0：
        返还 做 内存_读64 用 多结果 的 结果列表。
    完。
    可变 步长 为 4。
    只要 步长 小于或等于 最大距离：
        可变 dx 为 0 减 步长。
        只要 dx 小于或等于 步长：
            可变 dy 为 0 减 步长。
            只要 dy 小于或等于 步长：
                结果 为 新建 HitTestResult。
                若 做 绘制Layer_命中测试 用 层、测试x 加 dx、测试y 加 dy、结果：
                    若 结果 的 目标节点 不等于 0：
                        返还 结果。
                    完。
                完。
                dy 为 dy 加 步长。
            完。
            dx 为 dx 加 步长。
        完。
        步长 为 步长 乘 2。
    完。
    返还 空。
完。

命中测试_打印统计 接受 零参：
    做 命中统计_初始化。
    做 打印行 用 「=== 命中测试统计 ===」。
    做 打印行 用 「测试调用: 」 加 做 整数转字符串 用 全局_命中统计 的 测试调用数。
    做 打印行 用 「层遍历: 」 加 做 整数转字符串 用 全局_命中统计 的 层遍历数。
    做 打印行 用 「对象遍历: 」 加 做 整数转字符串 用 全局_命中统计 的 对象遍历数。
    做 打印行 用 「命中成功: 」 加 做 整数转字符串 用 全局_命中统计 的 命中成功数。
    做 打印行 用 「命中失败: 」 加 做 整数转字符串 用 全局_命中统计 的 命中失败数。
    做 打印行 用 「变换测试: 」 加 做 整数转字符串 用 全局_命中统计 的 变换测试数。
    做 打印行 用 「裁剪跳过: 」 加 做 整数转字符串 用 全局_命中统计 的 裁剪跳过数。
完。

命中测试_完整统计 接受 零参：
    做 命中统计_初始化。
    返还 一组：全局_命中统计 的 测试调用数、全局_命中统计 的 层遍历数、全局_命中统计 的 对象遍历数、全局_命中统计 的 命中成功数、全局_命中统计 的 命中失败数、全局_命中统计 的 变换测试数、全局_命中统计 的 裁剪跳过数。
完。

命中测试_重置统计 接受 零参：
    若 全局_命中统计 不等于 空：
        全局_命中统计 的 测试调用数 为 0。
        全局_命中统计 的 层遍历数 为 0。
        全局_命中统计 的 对象遍历数 为 0。
        全局_命中统计 的 命中成功数 为 0。
        全局_命中统计 的 命中失败数 为 0。
        全局_命中统计 的 变换测试数 为 0。
        全局_命中统计 的 裁剪跳过数 为 0。
    完。
完。

命中测试_获取调用数 接受 零参：
    做 命中统计_初始化。
    返还 全局_命中统计 的 测试调用数。
完。

命中测试_获取成功数 接受 零参：
    做 命中统计_初始化。
    返还 全局_命中统计 的 命中成功数。
完。

命中测试_获取失败数 接受 零参：
    做 命中统计_初始化。
    返还 全局_命中统计 的 命中失败数。
完。

命中测试_获取层数 接受 零参：
    做 命中统计_初始化。
    返还 全局_命中统计 的 层遍历数。
完。

命中测试_命中率 接受 零参：
    做 命中统计_初始化。
    总 为 全局_命中统计 的 命中成功数 加 全局_命中统计 的 命中失败数。
    若 总 等于 0：
        返还 0。
    完。
    返还 全局_命中统计 的 命中成功数 乘 1000 除以 总。
完。

命中测试_点在多边形内 接受 测试x、测试y、顶点x列表、顶点y列表、顶点数：
    可变 交点数 为 0。
    可变 j 为 顶点数 减 1。
    可变 i 为 0。
    只要 i 小于 顶点数：
        yi 为 顶点y列表 的 第 i 项。
        yj 为 顶点y列表 的 第 j 项。
        xi 为 顶点x列表 的 第 i 项。
        xj 为 顶点x列表 的 第 j 项。
        若 yi 大于 测试y 不等于 (yj 大于 测试y)：
            交点x 为 xi 加 (xj 减 xi) 乘 (测试y 减 yi) 除以 (yj 减 yi)。
            若 测试x 小于 交点x：
                交点数 为 交点数 加 1。
            完。
        完。
        j 为 i。
        i 为 i 加 1。
    完。
    返还 交点数 取余于 2 等于 1。
完。

命中测试_点到矩形距离 接受 px、py、rx、ry、rw、rh：
    可变 dx 为 0。
    可变 dy 为 0。
    若 px 小于 rx：
        dx 为 rx 减 px。
    否则若 px 大于 rx 加 rw：
        dx 为 px 减 rx 减 rw。
    完。
    若 py 小于 ry：
        dy 为 ry 减 py。
    否则若 py 大于 ry 加 rh：
        dy 为 py 减 ry 减 rh。
    完。
    返还 dx 乘 dx 加 dy 乘 dy。
完。

命中测试_查找最近层 接受 x、y、层列表、层数：
    可变 最近索引 为 负 1。
    可变 最近距离 为 0x7FFFFFFF。
    可变 i 为 0。
    只要 i 小于 层数：
        层 为 层列表 的 第 i 项。
        若 层 不等于 0 并且 层 的 布局对象 不等于 0：
            lo 为 层 的 布局对象。
            bw 为 做 布局Object_边框盒宽 用 lo。
            bh 为 做 布局Object_边框盒高 用 lo。
            d 为 做 命中测试_点到矩形距离 用 x、y、lo 的 绝对x、lo 的 绝对y、bw、bh。
            若 d 小于 最近距离：
                最近距离 为 d。
                最近索引 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最近索引 大于或等于 0：
        返还 层列表 的 第 最近索引 项。
    完。
    返还 空。
完。

命中测试_触摸扩展 接受 px、py、rx、ry、rw、rh、扩展：
    返还 做 绘制Layer_点在矩形内 用 px、py、rx 减 扩展、ry 减 扩展、rw 加 扩展 乘 2、rh 加 扩展 乘 2。
完。

命中测试_点在梯形内 接受 px、py、x1、y1、w1、x2、y2、w2、高度：
    若 py 小于 y1 或者 py 大于 y1 加 高度：
        返还 假。
    完。
    比例 为 (py 减 y1) 乘 1000 除以 高度。
    当前x 为 x1 加 (x2 减 x1) 乘 比例 除以 1000。
    当前w 为 w1 加 (w2 减 w1) 乘 比例 除以 1000。
    返还 px 大于或等于 当前x 并且 px 小于或等于 当前x 加 当前w。
完。

可变 命中_预测次数 为 0。

可变 命中_预测命中数 为 0。

可变 命中_上次目标 为 空。

可变 命中_连续相同数 为 0。

命中测试_AI记录 接受 目标节点：
    命中_预测次数 为 命中_预测次数 加 1。
    若 目标节点 等于 命中_上次目标：
        命中_连续相同数 为 命中_连续相同数 加 1。
    否则：
        命中_连续相同数 为 0。
        命中_上次目标 为 目标节点。
    完。
完。

命中测试_AI预测目标 接受 零参：
    若 命中_连续相同数 大于或等于 3 并且 命中_上次目标 不等于 空：
        命中_预测命中数 为 命中_预测命中数 加 1。
        返还 命中_上次目标。
    完。
    返还 空。
完。

命中测试_AI预测率 接受 零参：
    若 命中_预测次数 等于 0：
        返还 0。
    完。
    返还 命中_预测命中数 乘 1000 除以 命中_预测次数。
完。

HitTestMultiResult_按z序排序 接受 多结果：
    可变 i 为 0。
    只要 i 小于 多结果 的 数量 减 1：
        可变 j 为 0。
        只要 j 小于 多结果 的 数量 减 1 减 i：
            off1 为 j 乘 64。
            off2 为 (j 加 1) 乘 64。
            z1 为 做 内存_读64 用 多结果 的 结果列表 加 off1 加 32。
            z2 为 做 内存_读64 用 多结果 的 结果列表 加 off2 加 32。
            若 z2 大于 z1：
                可变 k 为 0。
                只要 k 小于 64：
                    tmp 为 做 内存_读8 用 多结果 的 结果列表 加 off1 加 k。
                    做 内存_写68 用 多结果 的 结果列表 加 off1 加 k、做 内存_读8 用 多结果 的 结果列表 加 off2 加 k。
                    做 内存_写68 用 多结果 的 结果列表 加 off2 加 k、tmp。
                    k 为 k 加 1。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

HitTestMultiResult_过滤可交互 接受 多结果：
    可变 可交互数 为 0。
    可变 i 为 0。
    只要 i 小于 多结果 的 数量：
        off 为 i 乘 64。
        可交互 为 做 内存_读64 用 多结果 的 结果列表 加 off 加 48。
        若 可交互：
            可交互数 为 可交互数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 可交互数。
完。

HitTestMultiResult_释放 接受 多结果：
    若 多结果 不等于 空 并且 多结果 的 结果列表 不等于 0：
        做 堆_释放 用 多结果 的 结果列表。
        多结果 的 结果列表 为 0。
        多结果 的 数量 为 0。
    完。
完。

命中测试_完整诊断 接受 零参：
    做 命中统计_初始化。
    返还 一组：「测试调用」、全局_命中统计 的 测试调用数、「命中率」、做 命中测试_命中率、「层遍历」、全局_命中统计 的 层遍历数、「裁剪跳过」、全局_命中统计 的 裁剪跳过数、「AI预测率」、做 命中测试_AI预测率、「NRHT命中测试」、1。
完。

可变 _谐振热区_历史 为 一组：（空）。

可变 _谐振热区_预测次数 为 0。

可变 _谐振热区_命中次数 为 0。

谐振热区_记录点击 接受 x、y：
    做 追加 用 _谐振热区_历史、一本字典：x 对应 x、y 对应 y。
    若 做 计数 用 _谐振热区_历史 大于 64：
        可变 新表 为 一组：（空）。
        可变 i 为 做 计数 用 _谐振热区_历史 减 64。
        只要 i 小于 做 计数 用 _谐振热区_历史：
            做 追加 用 新表、_谐振热区_历史 的 第 i 项。
            i 为 i 加 1。
        完。
        _谐振热区_历史 为 新表。
    完。
完。

谐振热区_预测 接受 宽度、高度：
    若 做 计数 用 _谐振热区_历史 小于 4：
        返还 空。
    完。
    可变 总x 为 0。
    可变 总y 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _谐振热区_历史：
        总x 为 总x 加 _谐振热区_历史 的 第 i 项 的 x。
        总y 为 总y 加 _谐振热区_历史 的 第 i 项 的 y。
        i 为 i 加 1。
    完。
    _谐振热区_预测次数 为 _谐振热区_预测次数 加 1。
    返还 一本字典：x 对应 总x 除以 做 计数 用 _谐振热区_历史、y 对应 总y 除以 做 计数 用 _谐振热区_历史。
完。

本章 收束。
