《元素栈创建》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《.·文档节点》。

插入模式_初始 为 0。

插入模式_DOCTYPE前 为 1。

插入模式_html前 为 2。

插入模式_head前 为 3。

插入模式_head中 为 4。

插入模式_head_noscript中 为 5。

插入模式_head后 为 6。

插入模式_body中 为 7。

插入模式_文本 为 8。

插入模式_table中 为 9。

插入模式_table文本中 为 10。

插入模式_caption中 为 11。

插入模式_列组中 为 12。

插入模式_table_body中 为 13。

插入模式_行中 为 14。

插入模式_单元格中 为 15。

插入模式_select中 为 16。

插入模式_select_table中 为 17。

插入模式_template中 为 18。

插入模式_body后 为 19。

插入模式_frameset中 为 20。

插入模式_frameset后 为 21。

插入模式_html后 为 22。

插入模式_html_frameset后 为 23。

元素栈 是 一种 事物，含 元素列表、数量、容量。

元素栈_创建 接受 零参：
    栈 为 做 堆_分配 用 元素栈。
    栈 的 容量 为 64。
    栈 的 元素列表 为 做 堆_分配_数组 用 栈 的 容量 乘 8。
    栈 的 数量 为 0。
    返还 栈。
完。

元素栈_压入 接受 栈、元素：
    若 栈 的 数量 大于或等于 栈 的 容量：
        新容量 为 栈 的 容量 乘 2。
        新列表 为 做 堆_分配_数组 用 新容量 乘 8。
        做 内存_复制 用 新列表、栈 的 元素列表、栈 的 数量 乘 8。
        做 堆_释放 用 栈 的 元素列表。
        栈 的 元素列表 为 新列表。
        栈 的 容量 为 新容量。
    完。
    做 内存_写64 用 栈 的 元素列表 加 栈 的 数量 乘 8、元素。
    栈 的 数量 为 栈 的 数量 加 1。
完。

元素栈_弹出 接受 栈：
    若 栈 的 数量 等于 0：
        返还 0。
    完。
    栈 的 数量 为 栈 的 数量 减 1。
    返还 做 内存_读64 用 栈 的 元素列表 加 栈 的 数量 乘 8。
完。

元素栈_顶部 接受 栈：
    若 栈 的 数量 等于 0：
        返还 0。
    完。
    返还 做 内存_读64 用 栈 的 元素列表 加 (栈 的 数量 减 1) 乘 8。
完。

元素栈_第N个 接受 栈、n：
    若 n 大于或等于 栈 的 数量：
        返还 0。
    完。
    返还 做 内存_读64 用 栈 的 元素列表 加 n 乘 8。
完。

元素栈_包含标签 接受 栈、标签名原子：
    可变 i 为 栈 的 数量。
    只要 i 大于 0：
        i 为 i 减 1。
        元素 为 做 内存_读64 用 栈 的 元素列表 加 i 乘 8。
        扩展 为 做 元素_获取扩展 用 元素。
        若 扩展 不等于 0 并且 扩展 的 标签名原子 等于 标签名原子：
            返还 1。
        完。
    完。
    返还 0。
完。

元素栈_弹出直到标签 接受 栈、标签名原子：
    只要 栈 的 数量 大于 0：
        顶 为 做 元素栈_顶部 用 栈。
        扩展 为 做 元素_获取扩展 用 顶。
        做 元素栈_弹出 用 栈。
        若 扩展 不等于 0 并且 扩展 的 标签名原子 等于 标签名原子：
            返还。
        完。
    完。
完。

元素栈_弹出直到含 接受 栈、标签名原子：
    做 元素栈_弹出直到标签 用 栈、标签名原子。
完。

本章 收束。
