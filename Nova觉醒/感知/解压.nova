《解压》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《..·中枢·内容可见性》。

ZLIB_窗口大小 为 32768。

ZLIB_窗口掩码 为 32767。

最大字面量码 为 286。

最大距离码 为 30。

最大码长码 为 19。

最大频率编码位 为 15。

ADLER32_模数 为 65521。

Zlib统计 是 一种 事物，含 解压总次数、无压缩块数、固定频率编码块数、动态频率编码块数、总输入字节、总输出字节、LZ77回引次数、LZ77回引总长度、最大回引距离、最大回引长度、频率编码树构建次数、位读取总次数、Adler32校验次数、Adler32失败次数、AI预测调用次数、AI预测命中次数、总解压耗时_纳秒、错误总数。

AI_Zlib状态 是 一种 事物，含 历史压缩率均值、历史块类型分布、连续固定频率编码数、连续动态频率编码数、预测下块类型、采样计数器、历史回引率、学习权重。

可变 全局Zlib统计 为 0。

可变 全局Zlib_AI 为 0。

可变 全局Zlib已初始化 为 假。

Zlib流 是 一种 事物，含 输入、输入大小、输入位置、输出、输出大小、输出位置、字面量表、距离表、窗口、窗口位置、位缓冲、位计数、adler_a、adler_b、块计数、回引计数。

Zlib_创建 接受 零参：
    z 为 做 堆_分配 用 Zlib流。
    z 的 窗口 为 做 堆_分配_数组 用 32768。
    z 的 窗口位置 为 0。
    z 的 位缓冲 为 0。
    z 的 位计数 为 0。
    返还 z。
完。

Zlib_读位 接受 z、n：
    只要 z 的 位计数 小于 n：
        若 z 的 输入位置 大于或等于 z 的 输入大小：
            返还 0。
        完。
        z 的 位缓冲 为 z 的 位缓冲 位或 做 内存_读8 用 z 的 输入 加 z 的 输入位置 左移 z 的 位计数。
        z 的 输入位置 为 z 的 输入位置 加 1。
        z 的 位计数 为 z 的 位计数 加 8。
    完。
    值 为 z 的 位缓冲 位与 (1 左移 n) 减 1。
    z 的 位缓冲 为 z 的 位缓冲 右移 n。
    z 的 位计数 为 z 的 位计数 减 n。
    返还 值。
完。

Zlib_解压 接受 z、压缩数据、压缩大小、输出缓冲、输出大小：
    z 的 输入 为 压缩数据。
    z 的 输入大小 为 压缩大小。
    z 的 输入位置 为 2。
    z 的 输出 为 输出缓冲。
    z 的 输出大小 为 输出大小。
    z 的 输出位置 为 0。
    可变 最终块 为 0。
    只要 最终块 等于 0：
        最终块 为 做 Zlib_读位 用 z、1。
        块类型 为 做 Zlib_读位 用 z、2。
        若 块类型 等于 0：
            做 Zlib_解压_无压缩 用 z。
        否则若 块类型 等于 1：
            做 Zlib_解压_固定频率编码 用 z。
        否则若 块类型 等于 2：
            做 Zlib_解压_动态频率编码 用 z。
        完。
    完。
    返还 z 的 输出位置。
完。

Zlib_输出字节 接受 z、字节：
    若 z 的 输出位置 小于 z 的 输出大小：
        做 内存_写8 用 z 的 输出 加 z 的 输出位置、字节。
        z 的 输出位置 为 z 的 输出位置 加 1。
    完。
    做 内存_写8 用 z 的 窗口 加 (z 的 窗口位置 位与 32767)、字节。
    z 的 窗口位置 为 z 的 窗口位置 加 1。
完。

Zlib_复制回引 接受 z、距离、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        pos 为 z 的 窗口位置 减 距离 位与 32767。
        字节 为 做 内存_读8 用 z 的 窗口 加 pos。
        做 Zlib_输出字节 用 z、字节。
        i 为 i 加 1。
    完。
完。

Zlib_解压_固定频率编码 接受 z：
    可变 继续 为 真。
    只要 继续：
        码 为 做 Zlib_读固定频率编码码 用 z。
        若 码 小于 256：
            做 Zlib_输出字节 用 z、码。
        否则若 码 等于 256：
            继续 为 假。
        否则：
            长度 为 做 Zlib_解码长度 用 z、码。
            距离码 为 做 Zlib_读位 用 z、5。
            距离 为 做 Zlib_解码距离 用 z、距离码。
            做 Zlib_复制回引 用 z、距离、长度。
        完。
    完。
完。

Zlib_读固定频率编码码 接受 z：
    码7 为 做 Zlib_读位 用 z、7。
    若 码7 小于或等于 23：
        返还 256 加 码7。
    完。
    码8 为 码7 左移 1 位或 做 Zlib_读位 用 z、1。
    若 码8 大于或等于 48 并且 码8 小于或等于 191：
        返还 码8 减 48。
    完。
    若 码8 大于或等于 192 并且 码8 小于或等于 199：
        返还 280 加 码8 减 192。
    完。
    码9 为 码8 左移 1 位或 做 Zlib_读位 用 z、1。
    返还 144 加 码9 减 400。
完。

Zlib_解码长度 接受 z、码：
    若 码 小于或等于 264：
        返还 码 减 257 加 3。
    完。
    若 码 小于或等于 268：
        额外 为 做 Zlib_读位 用 z、1。
        返还 11 加 (码 减 265) 乘 2 加 额外。
    完。
    若 码 小于或等于 272：
        额外 为 做 Zlib_读位 用 z、2。
        返还 19 加 (码 减 269) 乘 4 加 额外。
    完。
    若 码 小于或等于 276：
        额外 为 做 Zlib_读位 用 z、3。
        返还 35 加 (码 减 273) 乘 8 加 额外。
    完。
    若 码 小于或等于 280：
        额外 为 做 Zlib_读位 用 z、4。
        返还 67 加 (码 减 277) 乘 16 加 额外。
    完。
    若 码 小于或等于 284：
        额外 为 做 Zlib_读位 用 z、5。
        返还 131 加 (码 减 281) 乘 32 加 额外。
    完。
    若 码 等于 285：
        返还 258。
    完。
    返还 3。
完。

Zlib_解码距离 接受 z、码：
    若 码 小于或等于 3：
        返还 码 加 1。
    完。
    额外位 为 (码 减 2) 除以 2。
    基值 为 (1 左移 额外位 加 1) 加 1。
    偏移量 为 (码 位与 1) 左移 额外位。
    额外 为 做 Zlib_读位 用 z、额外位。
    返还 基值 加 偏移量 加 额外。
完。

Zlib_解压_无压缩 接受 z：
    z 的 位缓冲 为 0。
    z 的 位计数 为 0。
    长度 为 做 内存_读8 用 z 的 输入 加 z 的 输入位置 位或 做 内存_读8 用 z 的 输入 加 z 的 输入位置 加 1 左移 8。
    z 的 输入位置 为 z 的 输入位置 加 4。
    可变 i 为 0。
    只要 i 小于 长度：
        做 Zlib_输出字节 用 z、做 内存_读8 用 z 的 输入 加 z 的 输入位置。
        z 的 输入位置 为 z 的 输入位置 加 1。
        i 为 i 加 1。
    完。
完。

Zlib_解压_动态频率编码 接受 z：
    若 z 等于 空：
        返还。
    完。
    HLIT 为 做 Zlib_读位 用 z、5 加 257。
    HDIST 为 做 Zlib_读位 用 z、5 加 1。
    HCLEN 为 做 Zlib_读位 用 z、4 加 4。
    若 HLIT 大于 最大字面量码：
        HLIT 为 最大字面量码。
    完。
    若 HDIST 大于 最大距离码：
        HDIST 为 最大距离码。
    完。
    若 HCLEN 大于 最大码长码：
        HCLEN 为 最大码长码。
    完。
    码长顺序 为 一组：16、17、18、0、8、7、9、6、10、5、11、4、12、3、13、2、14、1、15。
    码长码长 为 做 堆_分配_清零 用 最大码长码。
    若 码长码长 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 HCLEN：
        做 内存_写8 用 码长码长 加 码长顺序 的 第 i 项、做 Zlib_读位 用 z、3。
        i 为 i 加 1。
    完。
    码长树 为 做 Zlib_构建频率编码树 用 码长码长、最大码长码、7。
    做 堆_释放 用 码长码长。
    若 码长树 等于 空：
        若 全局Zlib统计 不等于 空：
            全局Zlib统计 的 错误总数 为 全局Zlib统计 的 错误总数 加 1。
        完。
        返还。
    完。
    总码长数 为 HLIT 加 HDIST。
    码长数组 为 做 堆_分配_清零 用 总码长数。
    若 码长数组 等于 空：
        做 堆_释放 用 码长树。
        返还。
    完。
    可变 idx 为 0。
    只要 idx 小于 总码长数：
        符号 为 做 Zlib_频率编码解码 用 z、码长树、7。
        若 符号 小于 16：
            做 内存_写8 用 码长数组 加 idx、符号。
            idx 为 idx 加 1。
        否则若 符号 等于 16：
            重复次数 为 做 Zlib_读位 用 z、2 加 3。
            上一个 为 0。
            若 idx 大于 0：
                上一个 为 做 内存_读8 用 码长数组 加 idx 减 1。
            完。
            可变 r 为 0。
            只要 r 小于 重复次数 并且 idx 小于 总码长数：
                做 内存_写8 用 码长数组 加 idx、上一个。
                idx 为 idx 加 1。
                r 为 r 加 1。
            完。
        否则若 符号 等于 17：
            重复次数 为 做 Zlib_读位 用 z、3 加 3。
            可变 r 为 0。
            只要 r 小于 重复次数 并且 idx 小于 总码长数：
                做 内存_写8 用 码长数组 加 idx、0。
                idx 为 idx 加 1。
                r 为 r 加 1。
            完。
        否则若 符号 等于 18：
            重复次数 为 做 Zlib_读位 用 z、7 加 11。
            可变 r 为 0。
            只要 r 小于 重复次数 并且 idx 小于 总码长数：
                做 内存_写8 用 码长数组 加 idx、0。
                idx 为 idx 加 1。
                r 为 r 加 1。
            完。
        完。
    完。
    字面量树 为 做 Zlib_构建频率编码树 用 码长数组、HLIT、最大频率编码位。
    距离树 为 做 Zlib_构建频率编码树 用 码长数组 加 HLIT、HDIST、最大频率编码位。
    做 堆_释放 用 码长数组。
    做 堆_释放 用 码长树。
    若 字面量树 等于 空：
        若 全局Zlib统计 不等于 空：
            全局Zlib统计 的 错误总数 为 全局Zlib统计 的 错误总数 加 1。
        完。
        返还。
    完。
    可变 继续 为 真。
    只要 继续 等于 真：
        码 为 做 Zlib_频率编码解码 用 z、字面量树、最大频率编码位。
        若 码 小于 256：
            做 Zlib_输出字节 用 z、码。
        否则若 码 等于 256：
            继续 为 假。
        否则：
            长度 为 做 Zlib_解码长度 用 z、码。
            距离码 为 0。
            若 距离树 不等于 空：
                距离码 为 做 Zlib_频率编码解码 用 z、距离树、最大频率编码位。
            完。
            距离 为 做 Zlib_解码距离 用 z、距离码。
            做 Zlib_复制回引 用 z、距离、长度。
            z 的 回引计数 为 z 的 回引计数 加 1。
        完。
    完。
    若 字面量树 不等于 空：
        做 堆_释放 用 字面量树。
    完。
    若 距离树 不等于 空：
        做 堆_释放 用 距离树。
    完。
    若 全局Zlib统计 不等于 空：
        全局Zlib统计 的 动态频率编码块数 为 全局Zlib统计 的 动态频率编码块数 加 1。
        全局Zlib统计 的 频率编码树构建次数 为 全局Zlib统计 的 频率编码树构建次数 加 3。
    完。
完。

Zlib_构建频率编码树 接受 码长数组、符号数、最大位：
    若 码长数组 等于 空 或者 符号数 小于或等于 0：
        返还 空。
    完。
    计数表 为 做 堆_分配_清零 用 (最大位 加 1) 乘 4。
    若 计数表 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 符号数：
        长度 为 做 内存_读8 用 码长数组 加 i。
        若 长度 大于 0 并且 长度 小于或等于 最大位：
            当前值 为 做 内存_读32 用 计数表 加 长度 乘 4。
            做 内存_写32 用 计数表 加 长度 乘 4、当前值 加 1。
        完。
        i 为 i 加 1。
    完。
    下一码 为 做 堆_分配_清零 用 (最大位 加 1) 乘 4。
    若 下一码 等于 空：
        做 堆_释放 用 计数表。
        返还 空。
    完。
    可变 码值 为 0。
    可变 位长 为 1。
    只要 位长 小于或等于 最大位：
        码值 为 码值 加 做 内存_读32 用 计数表 加 (位长 减 1) 乘 4 左移 1。
        做 内存_写32 用 下一码 加 位长 乘 4、码值。
        位长 为 位长 加 1。
    完。
    表大小 为 1 左移 最大位。
    若 最大位 大于 15：
        表大小 为 1 左移 15。
    完。
    表 为 做 堆_分配_清零 用 表大小 乘 2。
    若 表 等于 空：
        做 堆_释放 用 计数表。
        做 堆_释放 用 下一码。
        返还 空。
    完。
    i 为 0。
    只要 i 小于 符号数：
        长度 为 做 内存_读8 用 码长数组 加 i。
        若 长度 大于 0 并且 长度 小于或等于 最大位：
            当前码 为 做 内存_读32 用 下一码 加 长度 乘 4。
            做 内存_写32 用 下一码 加 长度 乘 4、当前码 加 1。
            可变 反转码 为 0。
            可变 b 为 0。
            只要 b 小于 长度：
                反转码 为 反转码 左移 1 位或 当前码 右移 b 位与 1。
                b 为 b 加 1。
            完。
            步长 为 1 左移 长度。
            可变 填充 为 反转码。
            只要 填充 小于 表大小：
                条目 为 长度 左移 8 位或 i 位与 0xFF。
                做 内存_写16 用 表 加 填充 乘 2、条目。
                填充 为 填充 加 步长。
            完。
        完。
        i 为 i 加 1。
    完。
    做 堆_释放 用 计数表。
    做 堆_释放 用 下一码。
    返还 表。
完。

Zlib_频率编码解码 接受 z、表、最大位：
    若 z 等于 空 或者 表 等于 空：
        返还 0。
    完。
    只要 z 的 位计数 小于 最大位：
        若 z 的 输入位置 大于或等于 z 的 输入大小：
            止。
        完。
        z 的 位缓冲 为 z 的 位缓冲 位或 做 内存_读8 用 z 的 输入 加 z 的 输入位置 左移 z 的 位计数。
        z 的 输入位置 为 z 的 输入位置 加 1。
        z 的 位计数 为 z 的 位计数 加 8。
    完。
    掩码 为 (1 左移 最大位) 减 1。
    索引 为 z 的 位缓冲 位与 掩码。
    条目 为 做 内存_读16 用 表 加 索引 乘 2。
    码长 为 条目 右移 8 位与 0xFF。
    符号 为 条目 位与 0xFF。
    若 码长 大于 0：
        z 的 位缓冲 为 z 的 位缓冲 右移 码长。
        z 的 位计数 为 z 的 位计数 减 码长。
    完。
    若 全局Zlib统计 不等于 空：
        全局Zlib统计 的 位读取总次数 为 全局Zlib统计 的 位读取总次数 加 1。
    完。
    返还 符号。
完。

Zlib_Adler32_更新 接受 z、字节：
    若 z 等于 空：
        返还。
    完。
    z 的 adler_a 为 (z 的 adler_a 加 字节) 取余于 ADLER32_模数。
    z 的 adler_b 为 (z 的 adler_b 加 z 的 adler_a) 取余于 ADLER32_模数。
完。

Zlib_Adler32_获取 接受 z：
    若 z 等于 空：
        返还 0。
    完。
    返还 z 的 adler_b 左移 16 位或 z 的 adler_a。
完。

Zlib_Adler32_验证 接受 z、期望值：
    若 z 等于 空：
        返还 假。
    完。
    计算值 为 做 Zlib_Adler32_获取 用 z。
    若 全局Zlib统计 不等于 空：
        全局Zlib统计 的 Adler32校验次数 为 全局Zlib统计 的 Adler32校验次数 加 1。
    完。
    若 计算值 不等于 期望值：
        做 打印 用 「[Zlib] Adler-32校验失败」。
        若 全局Zlib统计 不等于 空：
            全局Zlib统计 的 Adler32失败次数 为 全局Zlib统计 的 Adler32失败次数 加 1。
        完。
        返还 假。
    完。
    返还 真。
完。

Zlib_初始化全局 接受 零参：
    若 全局Zlib已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局Zlib统计 为 做 堆_分配_清零 用 144。
    若 全局Zlib统计 等于 空：
        返还 错误_内存不足。
    完。
    全局Zlib_AI 为 做 堆_分配_清零 用 40。
    若 全局Zlib_AI 不等于 空：
        全局Zlib_AI 的 学习权重 为 128。
    完。
    全局Zlib已初始化 为 真。
    做 打印 用 「[Zlib] 全局初始化完成」。
    返还 0。
完。

Zlib_销毁全局 接受 零参：
    若 全局Zlib统计 不等于 空：
        做 堆_释放 用 全局Zlib统计。
        全局Zlib统计 为 0。
    完。
    若 全局Zlib_AI 不等于 空：
        做 堆_释放 用 全局Zlib_AI。
        全局Zlib_AI 为 0。
    完。
    全局Zlib已初始化 为 假。
    做 打印 用 「[Zlib] 全局已销毁」。
    返还 0。
完。

Zlib_销毁 接受 z：
    若 z 等于 空：
        返还。
    完。
    若 z 的 窗口 不等于 空：
        做 堆_释放 用 z 的 窗口。
    完。
    若 z 的 字面量表 不等于 空：
        做 堆_释放 用 z 的 字面量表。
    完。
    若 z 的 距离表 不等于 空：
        做 堆_释放 用 z 的 距离表。
    完。
    做 堆_释放 用 z。
完。

Zlib_重置 接受 z：
    若 z 等于 空：
        返还 错误_无效参数。
    完。
    z 的 输入位置 为 0。
    z 的 输出位置 为 0。
    z 的 窗口位置 为 0。
    z 的 位缓冲 为 0。
    z 的 位计数 为 0。
    z 的 adler_a 为 1。
    z 的 adler_b 为 0。
    z 的 块计数 为 0。
    z 的 回引计数 为 0。
    若 z 的 窗口 不等于 空：
        做 内存_置零 用 z 的 窗口、ZLIB_窗口大小。
    完。
    返还 0。
完。

Zlib_解压_完整 接受 压缩数据、压缩大小、输出缓冲、输出大小：
    若 压缩数据 等于 空 或者 压缩大小 小于 6 或者 输出缓冲 等于 空：
        返还 负 1。
    完。
    开始时间 为 做 平台_获取高精度时间。
    z 为 做 Zlib_创建。
    若 z 等于 空：
        返还 负 1。
    完。
    z 的 adler_a 为 1。
    z 的 adler_b 为 0。
    若 全局Zlib_AI 不等于 空：
        做 AI_Zlib_预解压分析 用 压缩数据、压缩大小。
    完。
    结果 为 做 Zlib_解压 用 z、压缩数据、压缩大小、输出缓冲、输出大小。
    若 z 的 输入位置 加 4 小于或等于 压缩大小：
        期望 为 做 内存_读8 用 压缩数据 加 压缩大小 减 4 左移 24 位或 做 内存_读8 用 压缩数据 加 压缩大小 减 3 左移 16 位或 做 内存_读8 用 压缩数据 加 压缩大小 减 2 左移 8 位或 做 内存_读8 用 压缩数据 加 压缩大小 减 1。
        做 Zlib_Adler32_验证 用 z、期望。
    完。
    若 全局Zlib统计 不等于 空：
        全局Zlib统计 的 解压总次数 为 全局Zlib统计 的 解压总次数 加 1。
        全局Zlib统计 的 总输入字节 为 全局Zlib统计 的 总输入字节 加 压缩大小。
        全局Zlib统计 的 总输出字节 为 全局Zlib统计 的 总输出字节 加 结果。
        全局Zlib统计 的 LZ77回引次数 为 全局Zlib统计 的 LZ77回引次数 加 z 的 回引计数。
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        全局Zlib统计 的 总解压耗时_纳秒 为 全局Zlib统计 的 总解压耗时_纳秒 加 耗时。
    完。
    若 全局Zlib_AI 不等于 空 并且 压缩大小 大于 0：
        做 AI_Zlib_后解压学习 用 压缩大小、结果、z 的 回引计数。
    完。
    做 Zlib_销毁 用 z。
    返还 结果。
完。

AI_Zlib_预解压分析 接受 数据、大小：
    若 全局Zlib_AI 等于 空 或者 数据 等于 空：
        返还。
    完。
    若 全局Zlib统计 不等于 空：
        全局Zlib统计 的 AI预测调用次数 为 全局Zlib统计 的 AI预测调用次数 加 1。
    完。
    若 大小 小于 2：
        返还。
    完。
    CMF 为 做 内存_读8 用 数据。
    FLG 为 做 内存_读8 用 数据 加 1。
    压缩级别 为 FLG 右移 6 位与 0x03。
    可变 预期高压缩 为 假。
    若 压缩级别 大于或等于 2：
        预期高压缩 为 真。
    完。
    AI 为 全局Zlib_AI。
    若 预期高压缩 等于 真：
        AI 的 预测下块类型 为 2。
        AI 的 连续动态频率编码数 为 AI 的 连续动态频率编码数 加 1。
        AI 的 连续固定频率编码数 为 0。
    否则：
        AI 的 预测下块类型 为 1。
        AI 的 连续固定频率编码数 为 AI 的 连续固定频率编码数 加 1。
        AI 的 连续动态频率编码数 为 0。
    完。
    若 预期高压缩 等于 真 并且 大小 大于 1048576：
        做 打印 用 「[AI Zlib] 大型高压缩流，建议分块解压」。
    完。
完。

AI_Zlib_后解压学习 接受 输入大小、输出大小、回引数：
    若 全局Zlib_AI 等于 空：
        返还。
    完。
    AI 为 全局Zlib_AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    可变 压缩率 为 0。
    若 输入大小 大于 0：
        压缩率 为 输出大小 乘 1024 除以 输入大小。
    完。
    AI 的 历史压缩率均值 为 (AI 的 历史压缩率均值 乘 7 加 压缩率) 除以 8。
    可变 回引率 为 0。
    若 输出大小 大于 0：
        回引率 为 回引数 乘 1024 除以 输出大小。
    完。
    AI 的 历史回引率 为 (AI 的 历史回引率 乘 7 加 回引率) 除以 8。
完。

Zlib_打印统计 接受 零参：
    若 全局Zlib统计 等于 空：
        做 打印 用 「[Zlib] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   Zlib/Deflate解压 · 统计报告       │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 解压总次数:       」、做 转字符串 用 全局Zlib统计 的 解压总次数。
    做 打印 用 做 字符串拼接 用 「│ 无压缩块:         」、做 转字符串 用 全局Zlib统计 的 无压缩块数。
    做 打印 用 做 字符串拼接 用 「│ 固定频率编码块:    」、做 转字符串 用 全局Zlib统计 的 固定频率编码块数。
    做 打印 用 做 字符串拼接 用 「│ 动态频率编码块:    」、做 转字符串 用 全局Zlib统计 的 动态频率编码块数。
    做 打印 用 做 字符串拼接 用 「│ 输入(KB):         」、做 转字符串 用 全局Zlib统计 的 总输入字节 除以 1024。
    做 打印 用 做 字符串拼接 用 「│ 输出(KB):         」、做 转字符串 用 全局Zlib统计 的 总输出字节 除以 1024。
    做 打印 用 做 字符串拼接 用 「│ LZ77回引次数:     」、做 转字符串 用 全局Zlib统计 的 LZ77回引次数。
    做 打印 用 做 字符串拼接 用 「│ 频率编码树构建:    」、做 转字符串 用 全局Zlib统计 的 频率编码树构建次数。
    做 打印 用 做 字符串拼接 用 「│ Adler32校验:      」、做 转字符串 用 全局Zlib统计 的 Adler32校验次数。
    做 打印 用 做 字符串拼接 用 「│ Adler32失败:      」、做 转字符串 用 全局Zlib统计 的 Adler32失败次数。
    做 打印 用 做 字符串拼接 用 「│ AI预测调用:       」、做 转字符串 用 全局Zlib统计 的 AI预测调用次数。
    做 打印 用 做 字符串拼接 用 「│ 总耗时(μs):       」、做 转字符串 用 全局Zlib统计 的 总解压耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 错误总数:         」、做 转字符串 用 全局Zlib统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

Zlib_导出诊断 接受 零参：
    若 全局Zlib_AI 等于 空：
        做 打印 用 「[Zlib AI] 未初始化」。
        返还。
    完。
    AI 为 全局Zlib_AI。
    做 打印 用 「[Zlib AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史压缩率(Q10): 」、做 转字符串 用 AI 的 历史压缩率均值。
    做 打印 用 做 字符串拼接 用 「  历史回引率(Q10): 」、做 转字符串 用 AI 的 历史回引率。
    做 打印 用 做 字符串拼接 用 「  预测块类型: 」、做 转字符串 用 AI 的 预测下块类型。
    做 打印 用 做 字符串拼接 用 「  采样计数: 」、做 转字符串 用 AI 的 采样计数器。
完。

Zlib_获取统计 接受 零参：
    返还 全局Zlib统计。
完。

Zlib_获取AI状态 接受 零参：
    返还 全局Zlib_AI。
完。

本章 收束。
