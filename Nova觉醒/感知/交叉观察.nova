《交叉观察》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·推理·定点Q算术》。

引用《..·中枢·内存初始化》。

IntersectionObserver 是 一种 事物，含 回调、根、根margin、阈值、阈值数量、观察目标、目标数量、目标容量。

IntersectionEntry 是 一种 事物，含 目标、边界矩形、交叉矩形、根边界矩形、交叉比、是相交的、时间。

IntersectionObserver_创建 接受 回调、选项：
    io 为 做 堆_分配 用 IntersectionObserver。
    io 的 回调 为 回调。
    io 的 根 为 0。
    io 的 根margin 为 0。
    io 的 阈值 为 做 堆_分配_数组 用 8 乘 4。
    做 内存_写32 用 io 的 阈值、0。
    io 的 阈值数量 为 1。
    io 的 目标容量 为 32。
    io 的 观察目标 为 做 堆_分配_数组 用 io 的 目标容量 乘 8。
    io 的 目标数量 为 0。
    返还 io。
完。

IntersectionObserver_observe 接受 io、目标：
    若 io 的 目标数量 大于或等于 io 的 目标容量：
        返还。
    完。
    做 内存_写64 用 io 的 观察目标 加 io 的 目标数量 乘 8、目标。
    io 的 目标数量 为 io 的 目标数量 加 1。
完。

IntersectionObserver_unobserve 接受 io、目标：
    可变 i 为 0。
    只要 i 小于 io 的 目标数量：
        若 做 内存_读64 用 io 的 观察目标 加 i 乘 8 等于 目标：
            io 的 目标数量 为 io 的 目标数量 减 1。
            若 i 小于 io 的 目标数量：
                做 内存_写64 用 io 的 观察目标 加 i 乘 8、做 内存_读64 用 io 的 观察目标 加 io 的 目标数量 乘 8。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

IntersectionObserver_检查 接受 io、视口宽、视口高、滚动y：
    可变 i 为 0。
    只要 i 小于 io 的 目标数量：
        目标 为 做 内存_读64 用 io 的 观察目标 加 i 乘 8。
        若 目标 不等于 0 并且 目标 的 布局对象 不等于 0：
            layout 为 目标 的 布局对象。
            元素顶 为 layout 的 y 减 滚动y。
            元素底 为 元素顶 加 layout 的 高度。
            元素左 为 layout 的 x。
            元素右 为 元素左 加 layout 的 宽度。
            rm顶 为 做 内存_读32 用 io 的 解析margin 加 0。
            rm右 为 做 内存_读32 用 io 的 解析margin 加 4。
            rm底 为 做 内存_读32 用 io 的 解析margin 加 8。
            rm左 为 做 内存_读32 用 io 的 解析margin 加 12。
            根顶 为 0 减 rm顶。
            根底 为 视口高 加 rm底。
            根左 为 0 减 rm左。
            根右 为 视口宽 加 rm右。
            交叉顶 为 元素顶。
            若 交叉顶 小于 根顶：
                交叉顶 为 根顶。
            完。
            交叉底 为 元素底。
            若 交叉底 大于 根底：
                交叉底 为 根底。
            完。
            交叉左 为 元素左。
            若 交叉左 小于 根左：
                交叉左 为 根左。
            完。
            交叉右 为 元素右。
            若 交叉右 大于 根右：
                交叉右 为 根右。
            完。
            可变 交叉面积 为 0。
            若 交叉底 大于 交叉顶 并且 交叉右 大于 交叉左：
                交叉面积 为 (交叉底 减 交叉顶) 乘 (交叉右 减 交叉左)。
            完。
            元素面积 为 layout 的 宽度 乘 layout 的 高度。
            可变 交叉比 为 0。
            若 元素面积 大于 0：
                交叉比 为 交叉面积 乘 10000 除以 元素面积。
            完。
            旧交叉比 为 做 NIOC_读取旧交叉比 用 io、i。
            越过 为 做 NIOC_检查阈值越过 用 io、旧交叉比、交叉比。
            若 越过 并且 io 的 回调 不等于 0：
                entry 为 做 NIOC_创建Entry 用 目标、元素左、元素顶、元素右、元素底、交叉左、交叉顶、交叉右、交叉底、根左、根顶、根右、根底、交叉比。
                做 NIOC_添加待通知Entry 用 io、entry。
            完。
            做 NIOC_写入旧交叉比 用 io、i、交叉比。
        完。
        i 为 i 加 1。
    完。
    做 NIOC_触发批量回调 用 io。
    NIOC_检查总数 为 NIOC_检查总数 加 1。
完。

NIOC_最大阈值数 为 64。

NIOC_最大目标数 为 512。

NIOC_ENTRY_大小 为 48。

NIOC_待通知容量 为 128。

可变 NIOC_检查总数 为 0。

可变 NIOC_触发总数 为 0。

可变 NIOC_观察总数 为 0。

可变 NIOC_Entry总数 为 0。

NIOC_解析rootMargin 接受 字符串、长度、输出：
    若 字符串 等于 0 或者 长度 等于 0：
        做 内存_写32 用 输出、0。
        做 内存_写32 用 输出 加 4、0。
        做 内存_写32 用 输出 加 8、0。
        做 内存_写32 用 输出 加 12、0。
        返还。
    完。
    可变 值数组 为 一组：0、0、0、0。
    可变 值数量 为 0。
    可变 当前值 为 0。
    可变 有数字 为 假。
    可变 是负数 为 假。
    可变 p 为 0。
    只要 p 小于或等于 长度 并且 值数量 小于 4：
        可变 ch 为 0。
        若 p 小于 长度：
            ch 为 做 内存_读8 用 字符串 加 p。
        完。
        若 ch 大于或等于 0x30 并且 ch 小于或等于 0x39：
            当前值 为 当前值 乘 10 加 (ch 减 0x30)。
            有数字 为 真。
        否则：
            若 ch 等于 0x2D 并且 非 有数字：
                是负数 为 真。
            否则：
                若 有数字：
                    若 是负数：
                        当前值 为 0 减 当前值。
                    完。
                    值数组 的 第 值数量 项 为 当前值。
                    值数量 为 值数量 加 1。
                    当前值 为 0。
                    有数字 为 假。
                    是负数 为 假。
                完。
            完。
        完。
        p 为 p 加 1。
    完。
    若 值数量 等于 1：
        做 内存_写32 用 输出、值数组 的 第 0 项。
        做 内存_写32 用 输出 加 4、值数组 的 第 0 项。
        做 内存_写32 用 输出 加 8、值数组 的 第 0 项。
        做 内存_写32 用 输出 加 12、值数组 的 第 0 项。
    否则：
        若 值数量 等于 2：
            做 内存_写32 用 输出、值数组 的 第 0 项。
            做 内存_写32 用 输出 加 4、值数组 的 第 1 项。
            做 内存_写32 用 输出 加 8、值数组 的 第 0 项。
            做 内存_写32 用 输出 加 12、值数组 的 第 1 项。
        否则：
            若 值数量 等于 3：
                做 内存_写32 用 输出、值数组 的 第 0 项。
                做 内存_写32 用 输出 加 4、值数组 的 第 1 项。
                做 内存_写32 用 输出 加 8、值数组 的 第 2 项。
                做 内存_写32 用 输出 加 12、值数组 的 第 1 项。
            否则：
                做 内存_写32 用 输出、值数组 的 第 0 项。
                做 内存_写32 用 输出 加 4、值数组 的 第 1 项。
                做 内存_写32 用 输出 加 8、值数组 的 第 2 项。
                做 内存_写32 用 输出 加 12、值数组 的 第 3 项。
            完。
        完。
    完。
完。

NIOC_设置阈值 接受 io、阈值数组、数量：
    若 io 等于 0 或者 阈值数组 等于 0：
        返还。
    完。
    若 数量 大于 NIOC_最大阈值数：
        数量 为 NIOC_最大阈值数。
    完。
    可变 i 为 0。
    只要 i 小于 数量：
        做 内存_写32 用 io 的 阈值 加 i 乘 4、做 内存_读32 用 阈值数组 加 i 乘 4。
        i 为 i 加 1。
    完。
    io 的 阈值数量 为 数量。
    做 NIOC_阈值排序 用 io。
完。

NIOC_阈值排序 接受 io：
    可变 i 为 1。
    只要 i 小于 io 的 阈值数量：
        键 为 做 内存_读32 用 io 的 阈值 加 i 乘 4。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 做 内存_读32 用 io 的 阈值 加 j 乘 4 大于 键：
            做 内存_写32 用 io 的 阈值 加 (j 加 1) 乘 4、做 内存_读32 用 io 的 阈值 加 j 乘 4。
            j 为 j 减 1。
        完。
        做 内存_写32 用 io 的 阈值 加 (j 加 1) 乘 4、键。
        i 为 i 加 1。
    完。
完。

NIOC_检查阈值越过 接受 io、旧值、新值：
    若 旧值 等于 新值：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 io 的 阈值数量：
        th 为 做 内存_读32 用 io 的 阈值 加 i 乘 4。
        若 旧值 小于 th 并且 新值 大于或等于 th 或者 旧值 大于或等于 th 并且 新值 小于 th：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

NIOC_初始化旧状态 接受 io：
    若 io 等于 0：
        返还。
    完。
    io 的 旧交叉比 为 做 堆_分配 用 io 的 目标容量 乘 4。
    可变 i 为 0。
    只要 i 小于 io 的 目标容量：
        做 内存_写32 用 io 的 旧交叉比 加 i 乘 4、负 1。
        i 为 i 加 1。
    完。
完。

NIOC_读取旧交叉比 接受 io、索引：
    若 io 的 旧交叉比 等于 0 或者 索引 大于或等于 io 的 目标容量：
        返还 负 1。
    完。
    返还 做 内存_读32 用 io 的 旧交叉比 加 索引 乘 4。
完。

NIOC_写入旧交叉比 接受 io、索引、值：
    若 io 的 旧交叉比 等于 0 或者 索引 大于或等于 io 的 目标容量：
        返还。
    完。
    做 内存_写32 用 io 的 旧交叉比 加 索引 乘 4、值。
完。

NIOC_创建Entry 接受 目标、元左、元顶、元右、元底、交左、交顶、交右、交底、根左、根顶、根右、根底、交叉比：
    entry 为 做 堆_分配 用 NIOC_ENTRY_大小。
    若 entry 等于 0：
        返还 0。
    完。
    做 内存_写64 用 entry、目标。
    做 内存_写32 用 entry 加 8、元左。
    做 内存_写32 用 entry 加 12、元顶。
    做 内存_写32 用 entry 加 16、元右。
    做 内存_写32 用 entry 加 20、元底。
    做 内存_写32 用 entry 加 24、交左。
    做 内存_写32 用 entry 加 28、交顶。
    做 内存_写32 用 entry 加 32、交右。
    做 内存_写32 用 entry 加 36、交底。
    做 内存_写32 用 entry 加 40、交叉比。
    是相交 为 0。
    若 交叉比 大于 0：
        是相交 为 1。
    完。
    做 内存_写32 用 entry 加 44、是相交。
    NIOC_Entry总数 为 NIOC_Entry总数 加 1。
    返还 entry。
完。

可变 NIOC_待通知队列 为 0。

可变 NIOC_待通知数量 为 0。

NIOC_初始化通知队列 接受 零参：
    若 NIOC_待通知队列 等于 0：
        NIOC_待通知队列 为 做 堆_分配 用 NIOC_待通知容量 乘 8。
    完。
    NIOC_待通知数量 为 0。
完。

NIOC_添加待通知Entry 接受 io、entry：
    若 NIOC_待通知队列 等于 0：
        做 NIOC_初始化通知队列。
    完。
    若 NIOC_待通知数量 大于或等于 NIOC_待通知容量：
        返还。
    完。
    做 内存_写64 用 NIOC_待通知队列 加 NIOC_待通知数量 乘 8、entry。
    NIOC_待通知数量 为 NIOC_待通知数量 加 1。
完。

NIOC_触发批量回调 接受 io：
    若 NIOC_待通知数量 等于 0 或者 io 的 回调 等于 0：
        返还。
    完。
    做 平台_调用回调 用 io 的 回调、NIOC_待通知队列、NIOC_待通知数量、io。
    NIOC_触发总数 为 NIOC_触发总数 加 1。
    可变 i 为 0。
    只要 i 小于 NIOC_待通知数量：
        e 为 做 内存_读64 用 NIOC_待通知队列 加 i 乘 8。
        若 e 不等于 0：
            做 堆_释放 用 e。
        完。
        i 为 i 加 1。
    完。
    NIOC_待通知数量 为 0。
完。

IntersectionObserver_disconnect 接受 io：
    若 io 等于 0：
        返还。
    完。
    io 的 目标数量 为 0。
    若 io 的 旧交叉比 不等于 0：
        可变 i 为 0。
        只要 i 小于 io 的 目标容量：
            做 内存_写32 用 io 的 旧交叉比 加 i 乘 4、负 1。
            i 为 i 加 1。
        完。
    完。
    NIOC_待通知数量 为 0。
完。

IntersectionObserver_takeRecords 接受 io：
    若 NIOC_待通知数量 等于 0：
        返还 一组：0、0。
    完。
    结果 为 做 堆_分配 用 NIOC_待通知数量 乘 8。
    做 内存_拷贝 用 结果、NIOC_待通知队列、NIOC_待通知数量 乘 8。
    数量 为 NIOC_待通知数量。
    NIOC_待通知数量 为 0。
    返还 一组：结果、数量。
完。

NIOC_确保目标容量 接受 io：
    若 io 的 目标数量 小于 io 的 目标容量：
        返还。
    完。
    新容量 为 io 的 目标容量 乘 2。
    若 新容量 大于 NIOC_最大目标数：
        新容量 为 NIOC_最大目标数。
    完。
    若 新容量 小于或等于 io 的 目标容量：
        返还。
    完。
    新数组 为 做 堆_分配 用 新容量 乘 8。
    若 新数组 等于 0：
        返还。
    完。
    做 内存_拷贝 用 新数组、io 的 观察目标、io 的 目标数量 乘 8。
    做 堆_释放 用 io 的 观察目标。
    io 的 观察目标 为 新数组。
    若 io 的 旧交叉比 不等于 0：
        新旧 为 做 堆_分配 用 新容量 乘 4。
        若 新旧 不等于 0：
            做 内存_拷贝 用 新旧、io 的 旧交叉比、io 的 目标容量 乘 4。
            可变 k 为 io 的 目标容量。
            只要 k 小于 新容量：
                做 内存_写32 用 新旧 加 k 乘 4、负 1。
                k 为 k 加 1。
            完。
            做 堆_释放 用 io 的 旧交叉比。
            io 的 旧交叉比 为 新旧。
        完。
    完。
    io 的 目标容量 为 新容量。
完。

NIOC_observe 接受 安全：
完。

一组：io、目标。

块：
    若 io 等于 0 或者 目标 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 io 的 目标数量：
        若 做 内存_读64 用 io 的 观察目标 加 i 乘 8 等于 目标：
            返还。
        完。
        i 为 i 加 1。
    完。
    做 NIOC_确保目标容量 用 io。
    若 io 的 目标数量 大于或等于 io 的 目标容量：
        返还。
    完。
    做 内存_写64 用 io 的 观察目标 加 io 的 目标数量 乘 8、目标。
    io 的 目标数量 为 io 的 目标数量 加 1。
    NIOC_观察总数 为 NIOC_观察总数 加 1。
完。

NIOC_获取根矩形 接受 io、视口宽、视口高、输出：
    若 io 的 根 不等于 0 并且 io 的 根 的 布局对象 不等于 0：
        rl 为 io 的 根 的 布局对象。
        做 内存_写32 用 输出、rl 的 x。
        做 内存_写32 用 输出 加 4、rl 的 y。
        做 内存_写32 用 输出 加 8、rl 的 x 加 rl 的 宽度。
        做 内存_写32 用 输出 加 12、rl 的 y 加 rl 的 高度。
    否则：
        做 内存_写32 用 输出、0。
        做 内存_写32 用 输出 加 4、0。
        做 内存_写32 用 输出 加 8、视口宽。
        做 内存_写32 用 输出 加 12、视口高。
    完。
    若 io 的 解析margin 不等于 0：
        顶 为 做 内存_读32 用 io 的 解析margin。
        右 为 做 内存_读32 用 io 的 解析margin 加 4。
        底 为 做 内存_读32 用 io 的 解析margin 加 8。
        左 为 做 内存_读32 用 io 的 解析margin 加 12。
        做 内存_写32 用 输出、做 内存_读32 用 输出 减 左。
        做 内存_写32 用 输出 加 4、做 内存_读32 用 输出 加 4 减 顶。
        做 内存_写32 用 输出 加 8、做 内存_读32 用 输出 加 8 加 右。
        做 内存_写32 用 输出 加 12、做 内存_读32 用 输出 加 12 加 底。
    完。
完。

NIOC_完整创建 接受 回调、根元素、rootMargin字符串、rootMargin长度、阈值数组、阈值数：
    io 为 做 IntersectionObserver_创建 用 回调、0。
    若 io 等于 0：
        返还 0。
    完。
    io 的 根 为 根元素。
    io 的 解析margin 为 做 堆_分配 用 16。
    做 NIOC_解析rootMargin 用 rootMargin字符串、rootMargin长度、io 的 解析margin。
    若 阈值数组 不等于 0 并且 阈值数 大于 0：
        做 NIOC_设置阈值 用 io、阈值数组、阈值数。
    完。
    做 NIOC_初始化旧状态 用 io。
    做 NIOC_初始化通知队列。
    返还 io。
完。

IntersectionObserver_释放 接受 io：
    若 io 等于 0：
        返还。
    完。
    若 io 的 观察目标 不等于 0：
        做 堆_释放 用 io 的 观察目标。
    完。
    若 io 的 阈值 不等于 0：
        做 堆_释放 用 io 的 阈值。
    完。
    若 io 的 旧交叉比 不等于 0：
        做 堆_释放 用 io 的 旧交叉比。
    完。
    若 io 的 解析margin 不等于 0：
        做 堆_释放 用 io 的 解析margin。
    完。
    做 堆_释放 用 io。
完。

NIOC_创建延迟加载观察器 接受 加载回调：
    margin字符串 为 「200px 0px 200px 0px」。
    阈值 为 做 堆_分配 用 4。
    做 内存_写32 用 阈值、0。
    io 为 做 NIOC_完整创建 用 加载回调、0、margin字符串、18、阈值、1。
    做 堆_释放 用 阈值。
    返还 io。
完。

NIOC_创建无限滚动观察器 接受 加载更多回调、哨兵元素：
    阈值 为 做 堆_分配 用 4。
    做 内存_写32 用 阈值、100。
    io 为 做 NIOC_完整创建 用 加载更多回调、0、0、0、阈值、1。
    若 io 不等于 0 并且 哨兵元素 不等于 0：
        做 做 NIOC_observe 用 安全 用 io、哨兵元素。
    完。
    做 堆_释放 用 阈值。
    返还 io。
完。

可变 NIOC_AI预测次数 为 0。

NIOC_AI提取特征 接受 io、视口宽、视口高：
    t 为 做 Tensor_创建 用 10。
    若 t 等于 0：
        返还 0。
    完。
    做 Tensor_写 用 t、0、io 的 目标数量。
    做 Tensor_写 用 t、1、io 的 阈值数量。
    做 Tensor_写 用 t、2、视口宽。
    做 Tensor_写 用 t、3、视口高。
    可变 可见数 为 0。
    可变 总交叉比 为 0。
    可变 i 为 0。
    只要 i 小于 io 的 目标数量：
        比 为 做 NIOC_读取旧交叉比 用 io、i。
        若 比 大于 0：
            可见数 为 可见数 加 1。
            总交叉比 为 总交叉比 加 比。
        完。
        i 为 i 加 1。
    完。
    做 Tensor_写 用 t、4、可见数。
    平均交叉 为 0。
    若 可见数 大于 0：
        平均交叉 为 总交叉比 除以 可见数。
    完。
    做 Tensor_写 用 t、5、平均交叉。
    做 Tensor_写 用 t、6、NIOC_检查总数。
    做 Tensor_写 用 t、7、NIOC_触发总数。
    做 Tensor_写 用 t、8、可见数 乘 10000 除以 (io 的 目标数量 加 1)。
    做 Tensor_写 用 t、9、NIOC_Entry总数。
    返还 t。
完。

NIOC_AI预测可见性 接受 io、视口宽、视口高：
    特征 为 做 NIOC_AI提取特征 用 io、视口宽、视口高。
    若 特征 等于 0：
        返还。
    完。
    目标数 为 做 Tensor_读 用 特征、0。
    可见数 为 做 Tensor_读 用 特征、4。
    可见率 为 做 Tensor_读 用 特征、8。
    若 目标数 大于 100：
        做 全局AI总线 的 发布 用 「nioc_性能警告」、「目标过多」、目标数。
    完。
    若 可见率 小于 500 并且 目标数 大于 10：
        做 全局AI总线 的 发布 用 「nioc_优化建议」、「low_visibility」、可见率。
    完。
    若 NIOC_触发总数 大于 0 并且 NIOC_检查总数 大于 0：
        触发率 为 NIOC_触发总数 乘 100 除以 NIOC_检查总数。
        若 触发率 大于 80：
            做 AI总线_异常检测 用 「NIOC_触发率过高」、触发率。
        完。
    完。
    做 Tensor_释放 用 特征。
    NIOC_AI预测次数 为 NIOC_AI预测次数 加 1。
完。

NIOC_获取统计 接受 零参：
    返还 一组：NIOC_检查总数、NIOC_触发总数、NIOC_观察总数、NIOC_Entry总数、NIOC_AI预测次数。
完。

NIOC_重置统计 接受 零参：
    NIOC_检查总数 为 0。
    NIOC_触发总数 为 0。
    NIOC_观察总数 为 0。
    NIOC_Entry总数 为 0。
    NIOC_AI预测次数 为 0。
完。

本章 收束。
