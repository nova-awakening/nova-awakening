《创建初始化》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

绘制Layer_创建 接受 布局对象：
    层 为 做 堆_分配 用 绘制Layer。
    层 的 布局对象 为 布局对象。
    层 的 类型 为 层类型_普通。
    层 的 标志 为 层标志_需要重绘。
    层 的 父层 为 0。
    层 的 第一子层 为 0。
    层 的 最后子层 为 0。
    层 的 前兄层 为 0。
    层 的 后兄层 为 0。
    层 的 负z序列表 为 做 堆_分配_数组 用 16 乘 8。
    层 的 负z序数量 为 0。
    层 的 正z序列表 为 做 堆_分配_数组 用 16 乘 8。
    层 的 正z序数量 为 0。
    层 的 裁剪矩形_x 为 0。
    层 的 裁剪矩形_y 为 0。
    层 的 裁剪矩形_w 为 0。
    层 的 裁剪矩形_h 为 0。
    层 的 滚动偏移x 为 0。
    层 的 滚动偏移y 为 0。
    层 的 滚动内容宽 为 0。
    层 的 滚动内容高 为 0。
    层 的 变换矩阵 为 0。
    层 的 图形层 为 0。
    层 的 显示列表 为 做 堆_分配_数组 用 256 乘 48。
    层 的 显示列表数量 为 0。
    层 的 显示列表容量 为 256。
    样式 为 布局对象 的 样式。
    若 样式 不等于 0：
        层 的 z_index 为 样式 的 z_index。
        若 样式 的 transform 不等于 0：
            层 的 标志 为 层 的 标志 位或 层标志_有变换。
        完。
        若 样式 的 opacity 小于 100：
            层 的 标志 为 层 的 标志 位或 层标志_有透明度。
        完。
        若 样式 的 overflow_x 不等于 Overflow_visible 或者 样式 的 overflow_y 不等于 Overflow_visible：
            层 的 标志 为 层 的 标志 位或 层标志_有溢出裁剪。
        完。
        若 样式 的 position 等于 Position_fixed：
            层 的 标志 为 层 的 标志 位或 层标志_有固定定位。
        完。
        若 样式 的 position 不等于 Position_static 并且 非 样式 的 z_index_auto：
            层 的 标志 为 层 的 标志 位或 层标志_是堆叠上下文。
        完。
        若 样式 的 opacity 小于 100：
            层 的 标志 为 层 的 标志 位或 层标志_是堆叠上下文。
        完。
        若 样式 的 transform 不等于 0：
            层 的 标志 为 层 的 标志 位或 层标志_是堆叠上下文。
        完。
        若 样式 的 display 等于 Display_flex 或者 样式 的 display 等于 Display_grid：
        完。
    完。
    层 的 渲染精度 为 2。
    层 的 渲染优先级 为 128。
    层 的 视口可见 为 0。
    层 的 注意力权重 为 5000。
    层 的 上次绘制耗时 为 0。
    层 的 累计绘制次数 为 0。
    层 的 显示项缓存版本 为 0。
    返还 层。
完。

绘制Layer_添加子层 接受 父层、子层：
    子层 的 父层 为 父层。
    子层 的 前兄层 为 父层 的 最后子层。
    子层 的 后兄层 为 0。
    若 父层 的 最后子层 不等于 0：
        父层 的 最后子层 的 后兄层 为 子层。
    否则：
        父层 的 第一子层 为 子层。
    完。
    父层 的 最后子层 为 子层。
完。

绘制Layer_更新z序 接受 层：
    层 的 负z序数量 为 0。
    层 的 正z序数量 为 0。
    可变 子 为 层 的 第一子层。
    只要 子 不等于 0：
        若 子 的 标志 位与 层标志_是堆叠上下文：
            若 子 的 z_index 小于 0：
                做 内存_写64 用 层 的 负z序列表 加 层 的 负z序数量 乘 8、子。
                层 的 负z序数量 为 层 的 负z序数量 加 1。
            否则：
                做 内存_写64 用 层 的 正z序列表 加 层 的 正z序数量 乘 8、子。
                层 的 正z序数量 为 层 的 正z序数量 加 1。
            完。
        完。
        子 为 子 的 后兄层。
    完。
    做 绘制Layer_排序z序列表 用 层 的 负z序列表、层 的 负z序数量。
    做 绘制Layer_排序z序列表 用 层 的 正z序列表、层 的 正z序数量。
    子 为 层 的 第一子层。
    只要 子 不等于 0：
        做 绘制Layer_更新z序 用 子。
        子 为 子 的 后兄层。
    完。
完。

绘制Layer_排序z序列表 接受 列表、数量：
    可变 i 为 1。
    只要 i 小于 数量：
        当前 为 做 内存_读64 用 列表 加 i 乘 8。
        当前z 为 当前 的 z_index。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 做 内存_读64 用 列表 加 j 乘 8 的 z_index 大于 当前z：
            做 内存_写64 用 列表 加 (j 加 1) 乘 8、做 内存_读64 用 列表 加 j 乘 8。
            j 为 j 减 1。
        完。
        做 内存_写64 用 列表 加 (j 加 1) 乘 8、当前。
        i 为 i 加 1。
    完。
完。

本章 收束。
