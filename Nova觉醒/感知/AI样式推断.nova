《AI样式推断》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·整数转字符串》。

引用《.·文档方法节》。

引用《..·中枢·字符串替换》。

样式意图_未知 为 0。

样式意图_居中 为 1。

样式意图_隐藏 为 2。

样式意图_强调 为 3。

样式意图_响应式 为 4。

样式意图_动画 为 5。

样式意图_排版 为 6。

样式意图_装饰 为 7。

样式意图_布局 为 8。

样式意图_交互 为 9。

影响_无 为 0。

影响_仅重绘 为 1。

影响_重排 为 2。

影响_重组 为 3。

影响_全重建 为 4。

AI样式_推断意图 接受 属性ID、值字符串：
    若 属性ID 等于 CSS属性_display：
        若 做 字符串_相等 用 值字符串、「none」：
            返还 样式意图_隐藏。
        完。
        若 做 字符串_相等 用 值字符串、「flex」 或者 做 字符串_相等 用 值字符串、「grid」：
            返还 样式意图_布局。
        完。
    完。
    若 属性ID 等于 CSS属性_text_align：
        若 做 字符串_相等 用 值字符串、「center」：
            返还 样式意图_居中。
        完。
    完。
    若 属性ID 等于 CSS属性_margin：
        若 做 字符串_包含 用 值字符串、「auto」：
            返还 样式意图_居中。
        完。
    完。
    若 属性ID 等于 CSS属性_font_weight：
        返还 样式意图_强调。
    完。
    若 属性ID 等于 CSS属性_color：
        返还 样式意图_强调。
    完。
    若 属性ID 等于 CSS属性_border_radius：
        返还 样式意图_装饰。
    完。
    若 属性ID 等于 CSS属性_box_shadow：
        返还 样式意图_装饰。
    完。
    若 属性ID 等于 CSS属性_transition：
        返还 样式意图_动画。
    完。
    若 属性ID 等于 CSS属性_animation：
        返还 样式意图_动画。
    完。
    若 属性ID 等于 CSS属性_font_family：
        返还 样式意图_排版。
    完。
    若 属性ID 等于 CSS属性_font_size：
        返还 样式意图_排版。
    完。
    若 属性ID 等于 CSS属性_line_height：
        返还 样式意图_排版。
    完。
    若 属性ID 大于或等于 CSS属性_width 并且 属性ID 小于或等于 CSS属性_padding_left：
        返还 样式意图_布局。
    完。
    返还 样式意图_未知。
完。

AI样式_分析影响 接受 属性ID：
    若 属性ID 等于 CSS属性_color：
        返还 影响_仅重绘。
    完。
    若 属性ID 等于 CSS属性_background_color：
        返还 影响_仅重绘。
    完。
    若 属性ID 等于 CSS属性_border_color：
        返还 影响_仅重绘。
    完。
    若 属性ID 等于 CSS属性_box_shadow：
        返还 影响_仅重绘。
    完。
    若 属性ID 等于 CSS属性_background_image：
        返还 影响_仅重绘。
    完。
    若 属性ID 等于 CSS属性_width：
        返还 影响_重排。
    完。
    若 属性ID 等于 CSS属性_height：
        返还 影响_重排。
    完。
    若 属性ID 大于或等于 CSS属性_margin 并且 属性ID 小于或等于 CSS属性_padding_left：
        返还 影响_重排。
    完。
    若 属性ID 等于 CSS属性_font_size：
        返还 影响_重排。
    完。
    若 属性ID 等于 CSS属性_line_height：
        返还 影响_重排。
    完。
    若 属性ID 等于 CSS属性_border_width：
        返还 影响_重排。
    完。
    若 属性ID 等于 CSS属性_transform：
        返还 影响_重组。
    完。
    若 属性ID 等于 CSS属性_opacity：
        返还 影响_重组。
    完。
    若 属性ID 等于 CSS属性_z_index：
        返还 影响_重组。
    完。
    若 属性ID 等于 CSS属性_display：
        返还 影响_全重建。
    完。
    若 属性ID 等于 CSS属性_position：
        返还 影响_全重建。
    完。
    返还 影响_重排。
完。

AI样式_处理变化 接受 元素、属性ID、新值：
    影响 为 做 AI样式_分析影响 用 属性ID。
    若 影响 等于 影响_仅重绘：
        若 元素 不等于 0：
            元素 的 节点标志 为 元素 的 节点标志 位或 节点标志_已改样式。
        完。
    否则若 影响 等于 影响_重排：
        若 元素 不等于 0：
            元素 的 节点标志 为 元素 的 节点标志 位或 节点标志_已改布局。
            做 节点_标记子树样式脏 用 元素。
        完。
    否则若 影响 等于 影响_重组：
        若 元素 不等于 0：
            元素 的 节点标志 为 元素 的 节点标志 位或 节点标志_已改样式。
        完。
    否则：
        若 元素 不等于 0：
            元素 的 节点标志 为 元素 的 节点标志 位或 节点标志_已改布局。
            元素 的 节点标志 为 元素 的 节点标志 位或 节点标志_已改样式。
            做 节点_标记子树样式脏 用 元素。
        完。
    完。
完。

本章 收束。
