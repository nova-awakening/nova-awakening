《无障碍访问》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

AX_ROLE_UNKNOWN 为 0。

AX_ROLE_ALERT 为 1。

AX_ROLE_APPLICATION 为 2。

AX_ROLE_ARTICLE 为 3。

AX_ROLE_BANNER 为 4。

AX_ROLE_BUTTON 为 5。

AX_ROLE_CELL 为 6。

AX_ROLE_CHECKBOX 为 7。

AX_ROLE_COMBOBOX 为 8。

AX_ROLE_COMPLEMENTARY 为 9。

AX_ROLE_CONTENTINFO 为 10。

AX_ROLE_DIALOG 为 11。

AX_ROLE_DOCUMENT 为 12。

AX_ROLE_FORM 为 13。

AX_ROLE_GENERIC 为 14。

AX_ROLE_GRID 为 15。

AX_ROLE_GROUP 为 16。

AX_ROLE_HEADING 为 17。

AX_ROLE_IMAGE 为 18。

AX_ROLE_LINK 为 19。

AX_ROLE_LIST 为 20。

AX_ROLE_LISTITEM 为 21。

AX_ROLE_LOG 为 22。

AX_ROLE_MAIN 为 23。

AX_ROLE_MARQUEE 为 24。

AX_ROLE_MENU 为 25。

AX_ROLE_MENUBAR 为 26。

AX_ROLE_MENUITEM 为 27。

AX_ROLE_METER 为 28。

AX_ROLE_NAVIGATION 为 29。

AX_ROLE_NONE 为 30。

AX_ROLE_NOTE 为 31。

AX_ROLE_OPTION 为 32。

AX_ROLE_PARAGRAPH 为 33。

AX_ROLE_PRESENTATION 为 34。

AX_ROLE_PROGRESSBAR 为 35。

AX_ROLE_RADIO 为 36。

AX_ROLE_RADIOGROUP 为 37。

AX_ROLE_REGION 为 38。

AX_ROLE_ROW 为 39。

AX_ROLE_ROWGROUP 为 40。

AX_ROLE_SCROLLBAR 为 41。

AX_ROLE_SEARCH 为 42。

AX_ROLE_SEARCHBOX 为 43。

AX_ROLE_SEPARATOR 为 44。

AX_ROLE_SLIDER 为 45。

AX_ROLE_SPINBUTTON 为 46。

AX_ROLE_STATUS 为 47。

AX_ROLE_SWITCH 为 48。

AX_ROLE_TAB 为 49。

AX_ROLE_TABLE 为 50。

AX_ROLE_TABLIST 为 51。

AX_ROLE_TABPANEL 为 52。

AX_ROLE_TERM 为 53。

AX_ROLE_TEXTBOX 为 54。

AX_ROLE_TIMER 为 55。

AX_ROLE_TOOLBAR 为 56。

AX_ROLE_TOOLTIP 为 57。

AX_ROLE_TREE 为 58。

AX_ROLE_TREEITEM 为 59。

AX_ROLE_TREEGRID 为 60。

AX_ROLE_STATIC_TEXT 为 61。

AX_ROLE_ROOT_WEB_AREA 为 62。

AX_STATE_BUSY 为 0x0001。

AX_STATE_CHECKED 为 0x0002。

AX_STATE_DISABLED 为 0x0004。

AX_STATE_EXPANDED 为 0x0008。

AX_STATE_FOCUSABLE 为 0x0010。

AX_STATE_FOCUSED 为 0x0020。

AX_STATE_HIDDEN 为 0x0040。

AX_STATE_HASPOPUP 为 0x0080。

AX_STATE_INVALID 为 0x0100。

AX_STATE_MULTILINE 为 0x0200。

AX_STATE_MULTISEL 为 0x0400。

AX_STATE_PRESSED 为 0x0800。

AX_STATE_READONLY 为 0x1000。

AX_STATE_REQUIRED 为 0x2000。

AX_STATE_SELECTED 为 0x4000。

AX_STATE_VISITED 为 0x8000。

本章 收束。
