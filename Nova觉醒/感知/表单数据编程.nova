《表单数据编程》

※ §4 FormData — 编程式表单数据

引用《.·内核直连层》。
引用《..·执行·控制台输出》。
引用《..·中枢·整数转字符串》。
引用《..·中枢·辅助存根》。

FormDataEntry 是 一种 事物：
    名称,。
    值,
    文件,
    文件名,
完。

FormData 是 一种 事物：
    条目,
    数量,。
完。

FormData_创建 接受 form)：
    fd 为 做 堆_分配 用 FormData。
    fd.数量 为 0。

    若 form 不等于 0：
        ※ 从HTMLForm收集
        可变 i 为 0。
        只要 i 小于 form.控件数：
            控件 为 form.控件[i]。
            若 控件 不等于 0 并且 控件.名称 不等于 0 并且 !控件.是禁用：
                ※ 跳过未选中的checkbox/radio
                若 (控件.类型 等于 输入类型_checkbox 或者 控件.类型 等于 输入类型_radio) 并且 !控件.是选中：
                    i 为 i 加 1; 继续。
                完。
                做 FormData_append 用 fd、控件.名称、控件.值。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 fd。
完。

FormData_append 接受 fd、名称、值)：
    若 fd 等于 0 或者 fd.数量 不小于 256：
    fd.条目[fd.数量].名称 为 名称。
    fd.条目[fd.数量].值 为 值。
    fd.条目[fd.数量].文件 为 0。
    fd.数量 为 fd.数量 加 1。
完。

FormData_appendFile 接受 fd、名称、文件、文件名)：
    若 fd 等于 0 或者 fd.数量 不小于 256：
    fd.条目[fd.数量].名称 为 名称。
    fd.条目[fd.数量].值 为 0。
    fd.条目[fd.数量].文件 为 文件。
    fd.条目[fd.数量].文件名 为 文件名。
    fd.数量 为 fd.数量 加 1。
完。

FormData_get 接受 fd、名称)：
    若 fd 等于 0：
    可变 i 为 0。
    只要 i 小于 fd.数量：
        若 做 字符串_相等 用 fd.条目[i].名称、名称：
        i 为 i 加 1。
    完。
    返还 0。
完。

FormData_set 接受 fd、名称、值)：
    若 fd 等于 0：
    可变 i 为 0。
    只要 i 小于 fd.数量：
        若 做 字符串_相等 用 fd.条目[i].名称、名称：
            fd.条目[i].值 为 值; 返还。
        完。
        i 为 i 加 1。
    完。
    做 FormData_append 用 fd、名称、值。
完。

FormData_delete 接受 fd、名称)：
    若 fd 等于 0：
    可变 i 为 0。
    只要 i 小于 fd.数量：
        若 做 字符串_相等 用 fd.条目[i].名称、名称：
            fd.数量 为 fd.数量 减 1。
            fd.条目[i] 为 fd.条目[fd.数量]。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

FormData_has 接受 fd、名称) ：返还 做 FormData_get 用 fd、名称 !：

※ §5 表单编码

※ application/x-www-form-urlencoded
FormData_编码_urlencoded 接受 fd)：
    若 fd 等于 0 或者 fd.数量 等于 0：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 fd.数量：
        若 i 大于 0：
        结果 为 做 字符串拼接 用 结果、做 URL_百分比编码 用 fd.条目[i].名称、0。
        结果 为 做 字符串拼接 用 结果、「=」。
        若 fd.条目[i].值 不等于 0：
            结果 为 做 字符串拼接 用 结果、做 URL_百分比编码 用 fd.条目[i].值、0。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

※ multipart/form-data
FormData_编码_multipart 接受 fd、边界)：
    若 fd 等于 0 或者 fd.数量 等于 0：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 fd.数量：
        结果 为 做 字符串拼接 用 结果、「--」。
        结果 为 做 字符串拼接 用 结果、边界。
        结果 为 做 字符串拼接 用 结果、「\r\n」。
        结果 为 做 字符串拼接 用 结果、「Content-Disposition: form-data; name=\」"。
        结果 为 做 字符串拼接 用 结果、fd.条目[i].名称。
        结果 为 做 字符串拼接 用 结果、「\」"。
        若 fd.条目[i].文件名 不等于 0：
            结果 为 做 字符串拼接 用 结果、「; filename=\」"。
            结果 为 做 字符串拼接 用 结果、fd.条目[i].文件名。
            结果 为 做 字符串拼接 用 结果、「\」"。
        完。
        结果 为 做 字符串拼接 用 结果、「\r\n\r\n」。
        若 fd.条目[i].值 不等于 0：
            结果 为 做 字符串拼接 用 结果、fd.条目[i].值。
        完。
        结果 为 做 字符串拼接 用 结果、「\r\n」。
        i 为 i 加 1。
    完。
    结果 为 做 字符串拼接 用 结果、「--」。
    结果 为 做 字符串拼接 用 结果、边界。
    结果 为 做 字符串拼接 用 结果、「--\r\n」。
    返还 结果。
完。

※ §6 FormData迭代与高级操作

FormData_getAll 接受 fd、名称)：
    可变 结果 为 []。
    若 fd 等于 0：
    可变 i 为 0。
    只要 i 小于 fd.数量：
        若 做 字符串_相等 用 fd.条目[i].名称、名称：
            做 追加 用 结果、fd.条目[i].值。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

FormData_keys 接受 fd)：
    可变 结果 为 []。
    若 fd 等于 0：
    可变 i 为 0。
    只要 i 小于 fd.数量：
        做 追加 用 结果、fd.条目[i].名称。
        i 为 i 加 1。
    完。
    返还 结果。
完。

FormData_values 接受 fd)：
    可变 结果 为 []。
    若 fd 等于 0：
    可变 i 为 0。
    只要 i 小于 fd.数量：
        做 追加 用 结果、fd.条目[i].值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

FormData_entries 接受 fd)：
    可变 结果 为 []。
    若 fd 等于 0：
    可变 i 为 0。
    只要 i 小于 fd.数量：
        做 追加 用 结果、[fd.条目[i].名称、fd.条目[i].值]。
        i 为 i 加 1。
    完。
    返还 结果。
完。

FormData_forEach 接受 fd、回调)：
    若 fd 等于 0 或者 回调 等于 0：
    可变 i 为 0。
    只要 i 小于 fd.数量：
        做 回调 用 fd.条目[i].值、fd.条目[i].名称、fd。
        i 为 i 加 1。
    完。
完。

FormData_数量 接受 fd)：
    若 fd 等于 0：
    返还 fd.数量。
完。

FormData_清空 接受 fd)：
    若 fd 等于 0：
    fd.数量 为 0。
完。

FormData_克隆 接受 fd)：
    若 fd 等于 0：
    新fd 为 做 堆_分配 用 FormData。
    新fd.数量 为 0。
    可变 i 为 0。
    只要 i 小于 fd.数量：
        做 FormData_append 用 新fd、fd.条目[i].名称、fd.条目[i].值。
        i 为 i 加 1。
    完。
    返还 新fd。
完。

※ §7 URL百分比编码

URL_百分比编码 接受 文本、保留空格)：
    若 文本 等于 0：
    长度 为 做 字节长度 用 文本。
    可变 部件 为 []。
    可变 i 为 0。
    只要 i 小于 长度：
        ch 为 做 字节取 用 文本、i。
        若 ((ch 不小于 65 并且 ch 不大于 90) 或者 (ch 不小于 97 并且 ch 不大于 122) 或者 (ch 不小于 48 并且 ch 不大于 57) ||：
            ch 为 = 45 或者 ch 等于 95 或者 ch 等于 46 或者 ch 等于 126) ：
            做 追加 用 部件、做 字符从码 用 ch。
        否则若 ch 等于 32：
            若 保留空格：
            否则 { 做 追加 用 部件、「%20」 }。
        否则：
            做 追加 用 部件、「%」 加 做 十六进制_2位 用 ch。
        完。
        i 为 i 加 1。
    完。
    返还 做 字符串_连接列表 用 部件、「」。
完。

URL_百分比解码 接受 文本)：
    若 文本 等于 0：
    长度 为 做 字节长度 用 文本。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 长度：
        ch 为 做 字节取 用 文本、i。
        若 ch 等于 37 并且 i 加 2 小于 长度：
            高 为 做 十六进制字符值 用 做 字节取 用 文本、i 加 1。
            低 为 做 十六进制字符值 用 做 字节取 用 文本、i 加 2。
            结果 为 结果 加 做 字符从码 用 高 乘 16 加 低。
            i 为 i 加 3。
        否则若 ch 等于 43：
            结果 为 结果 加 「 」。
            i 为 i 加 1。
        否则：
            结果 为 结果 加 做 字符从码 用 ch。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

十六进制_2位 接受 值)：
    高 为 值 除以 16。
    低 为 值 取余于 16。
    返还 做 字符从码 用 做 十六进制字符 用 高 加 做 字符从码 用 做 十六进制字符 用 低。
完。

十六进制字符 接受 n)：
    若 n 小于 10：
    返还 65 加 n 减 10。
完。

十六进制字符值 接受 ch)：
    若 ch 不小于 48 并且 ch 不大于 57：
    若 ch 不小于 65 并且 ch 不大于 70：
    若 ch 不小于 97 并且 ch 不大于 102：
    返还 0。
完。

字符从码 接受 码)：
    buf 为 做 堆_分配 用 2。
    做 内存_写8 用 buf、码。
    做 内存_写8 用 buf 加 1、0。
    返还 buf。
完。

※ §8 URLSearchParams解析

URLSearchParams_解析 接受 查询字符串)：
    若 查询字符串 等于 0：
    fd 为 做 堆_分配 用 FormData。
    fd.数量 为 0。
    长度 为 做 字节长度 用 查询字符串。
    可变 位置 为 0。
    若 长度 大于 0 并且 做 字节取 用 查询字符串、0 等于 63：
    只要 位置 小于 长度：
        键开始 为 位置。
        只要 位置 小于 长度 并且 做 字节取 用 查询字符串、位置 不等于 61 并且 做 字节取 用 查询字符串、位置 不等于 38：
            位置 为 位置 加 1。
        完。
        键 为 做 URL_百分比解码 用 做 字符串_子串 用 查询字符串、键开始、位置 减 键开始。
        可变 值 为 「」。
        若 位置 小于 长度 并且 做 字节取 用 查询字符串、位置 等于 61：
            位置 为 位置 加 1。
            值开始 为 位置。
            只要 位置 小于 长度 并且 做 字节取 用 查询字符串、位置 不等于 38：
            值 为 做 URL_百分比解码 用 做 字符串_子串 用 查询字符串、值开始、位置 减 值开始。
        完。
        做 FormData_append 用 fd、键、值。
        若 位置 小于 长度：
    完。
    返还 fd。
完。

URLSearchParams_序列化 接受 fd)：
    返还 做 FormData_编码_urlencoded 用 fd。
完。

※ §9 输入类型常量

输入类型_text 为 0。
输入类型_password 为 1。
输入类型_checkbox 为 2。
输入类型_radio 为 3。
输入类型_file 为 4。
输入类型_hidden 为 5。
输入类型_submit 为 6。
输入类型_select 为 7。
输入类型_textarea 为 8。

※ §10 边界生成

可变 边界_计数器 为 0。

FormData_生成边界 接受 )：
    边界_计数器 为 边界_计数器 加 1。
    返还 「----NovaFormBoundary」 加 做 整数转字符串 用 边界_计数器。
完。

※ §11 FormData统计与健康检查

可变 FormData_创建总数 为 0。
可变 FormData_追加总数 为 0。
可变 FormData_编码总数 为 0。

FormData_统计 接受 )：
    返还 ：
        创建。
        追加。
        编码。
    完。
完。

FormData_健康检查 接受 )：
    返还 「FormData子系统健康」。
完。

FormData_重置统计 接受 )：
    FormData_创建总数 为 0。
    FormData_追加总数 为 0。
    FormData_编码总数 为 0。
完。

FormData_完整统计 接受 )：
    返还 [FormData_创建总数, FormData_追加总数, FormData_编码总数]。
完。

FormData_诊断 接受 fd)：
    若 fd 等于 0：
    返还 ：
        条目数: fd.数量,。
        键列表。
    完。
完。

可变 AI_表单数据编_热点函数 为 []。

AI_表单数据编_JIT候选 接受 函数名、调用次数)：
    若 调用次数 大于 100：
        做 追加 用 AI_表单数据编_热点函数、函数名。
        返还 真。
    完。
    返还 假。
完。

AI_表单数据编_内联建议 接受 函数体大小、调用次数)：
    若 函数体大小 小于 30 并且 调用次数 大于 50：
    返还 「不内联」。
完。

本章 收束。
