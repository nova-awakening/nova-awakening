《变换矩阵应》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·矩阵创建》。

引用《.·样式子网格》。

引用《.·绘制矩形创建》。

引用《.·精确三角函数》。

Transform_变换点 接受 mat、x、y：
    x4 为 x 乘 100。
    y4 为 y 乘 100。
    新x 为 (做 Matrix4x4_获取 用 mat、0、0 乘 x4 加 做 Matrix4x4_获取 用 mat、0、1 乘 y4 加 做 Matrix4x4_获取 用 mat、0、3 乘 10000) 除以 100000000。
    新y 为 (做 Matrix4x4_获取 用 mat、1、0 乘 x4 加 做 Matrix4x4_获取 用 mat、1、1 乘 y4 加 做 Matrix4x4_获取 用 mat、1、3 乘 10000) 除以 100000000。
    w 为 (做 Matrix4x4_获取 用 mat、3、0 乘 x4 加 做 Matrix4x4_获取 用 mat、3、1 乘 y4 加 做 Matrix4x4_获取 用 mat、3、3 乘 10000) 除以 100000000。
    若 w 不等于 0 并且 w 不等于 100：
        新x 为 新x 乘 100 除以 w。
        新y 为 新y 乘 100 除以 w。
    完。
    返还 一组：新x、新y。
完。

Transform_变换矩形 接受 mat、rect：
    p1 为 做 Transform_变换点 用 mat、rect 的 左、rect 的 上。
    p2 为 做 Transform_变换点 用 mat、rect 的 右、rect 的 上。
    p3 为 做 Transform_变换点 用 mat、rect 的 右、rect 的 下。
    p4 为 做 Transform_变换点 用 mat、rect 的 左、rect 的 下。
    可变 min_x 为 p1 的 第 0 项。
    可变 min_y 为 p1 的 第 1 项。
    可变 max_x 为 p1 的 第 0 项。
    可变 max_y 为 p1 的 第 1 项。
    若 p2 的 第 0 项 小于 min_x：
        min_x 为 p2 的 第 0 项。
    完。
    若 p2 的 第 0 项 大于 max_x：
        max_x 为 p2 的 第 0 项。
    完。
    若 p2 的 第 1 项 小于 min_y：
        min_y 为 p2 的 第 1 项。
    完。
    若 p2 的 第 1 项 大于 max_y：
        max_y 为 p2 的 第 1 项。
    完。
    若 p3 的 第 0 项 小于 min_x：
        min_x 为 p3 的 第 0 项。
    完。
    若 p3 的 第 0 项 大于 max_x：
        max_x 为 p3 的 第 0 项。
    完。
    若 p3 的 第 1 项 小于 min_y：
        min_y 为 p3 的 第 1 项。
    完。
    若 p3 的 第 1 项 大于 max_y：
        max_y 为 p3 的 第 1 项。
    完。
    若 p4 的 第 0 项 小于 min_x：
        min_x 为 p4 的 第 0 项。
    完。
    若 p4 的 第 0 项 大于 max_x：
        max_x 为 p4 的 第 0 项。
    完。
    若 p4 的 第 1 项 小于 min_y：
        min_y 为 p4 的 第 1 项。
    完。
    若 p4 的 第 1 项 大于 max_y：
        max_y 为 p4 的 第 1 项。
    完。
    返还 做 SkRect_创建 用 min_x、min_y、max_x、max_y。
完。

TransformDecomposition 是 一种 事物，含 平移x、平移y、平移z、缩放x、缩放y、缩放z、旋转、倾斜。

Transform_分解2D 接受 mat：
    d 为 新建 TransformDecomposition。
    d 的 平移x 为 做 Matrix4x4_获取 用 mat、0、3。
    d 的 平移y 为 做 Matrix4x4_获取 用 mat、1、3。
    d 的 平移z 为 0。
    a 为 做 Matrix4x4_获取 用 mat、0、0。
    b 为 做 Matrix4x4_获取 用 mat、1、0。
    c 为 做 Matrix4x4_获取 用 mat、0、1。
    dd 为 做 Matrix4x4_获取 用 mat、1、1。
    d 的 缩放x 为 做 整数_sqrt 用 a 乘 a 除以 10000 加 b 乘 b 除以 10000。
    若 d 的 缩放x 不等于 0：
        a 为 a 乘 10000 除以 d 的 缩放x。
        b 为 b 乘 10000 除以 d 的 缩放x。
    完。
    d 的 倾斜 为 a 乘 c 除以 10000 加 b 乘 dd 除以 10000。
    c 为 c 减 a 乘 d 的 倾斜 除以 10000。
    dd 为 dd 减 b 乘 d 的 倾斜 除以 10000。
    d 的 缩放y 为 做 整数_sqrt 用 c 乘 c 除以 10000 加 dd 乘 dd 除以 10000。
    d 的 旋转 为 做 定点_atan2 用 b、a。
    d 的 旋转 为 d 的 旋转 乘 5730 除以 10000。
    返还 d。
完。

Transform_插值 接受 d1、d2、t：
    inv 为 10000 减 t。
    结果 为 新建 TransformDecomposition。
    结果 的 平移x 为 (d1 的 平移x 乘 inv 加 d2 的 平移x 乘 t) 除以 10000。
    结果 的 平移y 为 (d1 的 平移y 乘 inv 加 d2 的 平移y 乘 t) 除以 10000。
    结果 的 缩放x 为 (d1 的 缩放x 乘 inv 加 d2 的 缩放x 乘 t) 除以 10000。
    结果 的 缩放y 为 (d1 的 缩放y 乘 inv 加 d2 的 缩放y 乘 t) 除以 10000。
    结果 的 旋转 为 (d1 的 旋转 乘 inv 加 d2 的 旋转 乘 t) 除以 10000。
    结果 的 倾斜 为 (d1 的 倾斜 乘 inv 加 d2 的 倾斜 乘 t) 除以 10000。
    返还 结果。
完。

Transform_从分解重组 接受 d：
    可变 结果 为 做 Matrix4x4_创建。
    结果 为 做 Matrix4x4_乘 用 结果、做 Transform_translate 用 d 的 平移x 除以 100、d 的 平移y 除以 100。
    结果 为 做 Matrix4x4_乘 用 结果、做 Transform_rotate 用 d 的 旋转。
    结果 为 做 Matrix4x4_乘 用 结果、做 Transform_scale 用 d 的 缩放x 除以 100、d 的 缩放y 除以 100。
    返还 结果。
完。

本章 收束。
