《导航预加载》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《.·ServiceWorker全局》。

引用《..·中枢·内容可见性》。

引用《.·缓存》。

引用《.·原子驻留》。

引用《.·ResourceResponse》。

引用《..·中枢·时钟中枢》。

SW_enableNavigationPreload 接受 reg：
    若 reg 等于 0：
        返还。
    完。
完。

SW_unregister 接受 reg：
    若 reg 等于 0：
        返还。
    完。
    若 reg 的 active 不等于 0：
        reg 的 active 的 状态 为 SW_STATE_REDUNDANT。
    完。
    若 reg 的 waiting 不等于 0：
        reg 的 waiting 的 状态 为 SW_STATE_REDUNDANT。
    完。
    若 reg 的 installing 不等于 0：
        reg 的 installing 的 状态 为 SW_STATE_REDUNDANT。
    完。
    容器 为 做 SW_获取容器。
    若 容器 不等于 0：
        可变 i 为 0。
        只要 i 小于 容器 的 注册数：
            若 做 内存_读64 用 容器 的 注册表 加 i 乘 8 等于 reg：
                做 内存_写64 用 容器 的 注册表 加 i 乘 8、0。
                返还。
            完。
            i 为 i 加 1。
        完。
    完。
完。

SW_策略_缓存First 接受 cache、requestURL：
    若 cache 等于 0 或者 requestURL 等于 0：
        返还 0。
    完。
    cached 为 做 缓存_match 用 cache、requestURL。
    若 cached 不等于 0：
        返还 cached。
    完。
    返还 0。
完。

SW_策略_NetworkFirst 接受 cache、requestURL：
    返还 做 缓存_match 用 cache、requestURL。
完。

SW_策略_StaleWhileRevalidate 接受 cache、requestURL：
    cached 为 做 缓存_match 用 cache、requestURL。
    返还 cached。
完。

AccessPattern 是 一种 事物，含 最近URL、最近时间、数量、写入位置。

可变 访问模式 为 0。

智能_SW_记录访问 接受 url：
    若 url 等于 0：
        返还。
    完。
    若 访问模式 等于 0：
        访问模式 为 做 堆_分配 用 AccessPattern。
        访问模式 的 数量 为 0。
        访问模式 的 写入位置 为 0。
    完。
    idx 为 访问模式 的 写入位置 取余于 64。
    访问模式 的 最近URL 的 第 idx 项 为 url。
    访问模式 的 最近时间 的 第 idx 项 为 做 平台_获取高精度时间。
    访问模式 的 写入位置 为 访问模式 的 写入位置 加 1。
    若 访问模式 的 数量 小于 64：
        访问模式 的 数量 为 访问模式 的 数量 加 1。
    完。
完。

智能_SW_预测预缓存 接受 cache：
    若 cache 等于 0 或者 访问模式 等于 0 或者 访问模式 的 数量 小于 5：
        返还。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    特征 为 做 Tensor_创建 用 8。
    可变 k 为 0。
    只要 k 小于 8 并且 k 小于 访问模式 的 数量：
        idx 为 (访问模式 的 写入位置 减 1 减 k 加 64) 取余于 64。
        做 Tensor_写 用 特征、k、做 字符串_哈希 用 访问模式 的 最近URL 的 第 idx 项。
        k 为 k 加 1。
    完。
    预测 为 做 AI总线_推理 用 bus、特征。
    做 Tensor_释放 用 特征。
    若 预测 大于 0：
        全局ASNP统计 的 AI预测次数 为 全局ASNP统计 的 AI预测次数 加 1。
    完。
完。

可变 全局ASNP统计 为 新建 ASNP统计结构。

ASNP统计结构 是 一种 事物，含 预加载次数、缓存命中次数、缓存未中次数、后台同步次数、推送通知次数、离线Fallback次数、AI预测次数、缓存清理次数。

预加载状态_禁用 为 0。

预加载状态_启用 为 1。

预加载状态_暂停 为 2。

导航预加载管理器 是 一种 事物，含 状态、头部值、超时_ms、并行请求数、最大并行数、总预加载次数。

导航预加载_创建 接受 零参：
    mgr 为 做 堆_分配 用 导航预加载管理器。
    若 mgr 等于 0：
        返还 空。
    完。
    mgr 的 状态 为 预加载状态_禁用。
    mgr 的 头部值 为 做 字符串_创建 用 「真」。
    mgr 的 超时_ms 为 5000。
    mgr 的 并行请求数 为 0。
    mgr 的 最大并行数 为 4。
    mgr 的 总预加载次数 为 0。
    返还 mgr。
完。

导航预加载_启用 接受 mgr、头部值：
    若 mgr 等于 空：
        返还 假。
    完。
    mgr 的 状态 为 预加载状态_启用。
    若 头部值 不等于 0：
        mgr 的 头部值 为 头部值。
    完。
    全局ASNP统计 的 预加载次数 为 全局ASNP统计 的 预加载次数 加 1。
    返还 真。
完。

导航预加载_禁用 接受 mgr：
    若 mgr 等于 空：
        返还。
    完。
    mgr 的 状态 为 预加载状态_禁用。
完。

导航预加载_获取状态 接受 mgr：
    若 mgr 等于 空：
        返还 预加载状态_禁用。
    完。
    返还 mgr 的 状态。
完。

导航预加载_发起请求 接受 mgr、url：
    若 mgr 等于 空 或者 mgr 的 状态 不等于 预加载状态_启用：
        返还 空。
    完。
    若 mgr 的 并行请求数 大于或等于 mgr 的 最大并行数：
        返还 空。
    完。
    mgr 的 并行请求数 为 mgr 的 并行请求数 加 1。
    mgr 的 总预加载次数 为 mgr 的 总预加载次数 加 1。
    req 为 做 网络_创建请求 用 url。
    若 req 不等于 0：
        做 网络_设置请求头 用 req、「Service-Worker-Navigation-Preload」、mgr 的 头部值。
        做 网络_设置超时 用 req、mgr 的 超时_ms。
    完。
    返还 req。
完。

导航预加载_完成请求 接受 mgr：
    若 mgr 等于 空：
        返还。
    完。
    若 mgr 的 并行请求数 大于 0：
        mgr 的 并行请求数 为 mgr 的 并行请求数 减 1。
    完。
完。

缓存版本_最大 为 16。

缓存版本项 是 一种 事物，含 名称、版本号、创建时间、条目数、总大小。

缓存版本管理器 是 一种 事物，含 版本列表、版本数、当前版本、自动清理。

缓存版本_创建管理器 接受 零参：
    vm 为 做 堆_分配 用 缓存版本管理器。
    若 vm 等于 0：
        返还 空。
    完。
    vm 的 版本列表 为 做 堆_分配 用 缓存版本_最大 乘 8。
    vm 的 版本数 为 0。
    vm 的 当前版本 为 0。
    vm 的 自动清理 为 1。
    返还 vm。
完。

缓存版本_添加 接受 vm、名称、版本号：
    若 vm 等于 空 或者 vm 的 版本数 大于或等于 缓存版本_最大：
        返还 假。
    完。
    entry 为 做 堆_分配 用 缓存版本项。
    若 entry 等于 0：
        返还 假。
    完。
    entry 的 名称 为 名称。
    entry 的 版本号 为 版本号。
    entry 的 创建时间 为 做 读取毫秒。
    entry 的 条目数 为 0。
    entry 的 总大小 为 0。
    做 内存_写64 用 vm 的 版本列表 加 vm 的 版本数 乘 8、entry。
    vm 的 版本数 为 vm 的 版本数 加 1。
    vm 的 当前版本 为 版本号。
    返还 真。
完。

缓存版本_清理旧版 接受 vm：
    若 vm 等于 空 或者 vm 的 自动清理 等于 0：
        返还 0。
    完。
    可变 已清理 为 0。
    可变 i 为 0。
    只要 i 小于 vm 的 版本数：
        entry 为 做 内存_读64 用 vm 的 版本列表 加 i 乘 8。
        若 entry 不等于 0 并且 entry 的 版本号 不等于 vm 的 当前版本：
            做 缓存_删除 用 entry 的 名称。
            做 堆_释放 用 entry。
            做 内存_写64 用 vm 的 版本列表 加 i 乘 8、0。
            已清理 为 已清理 加 1。
        完。
        i 为 i 加 1。
    完。
    全局ASNP统计 的 缓存清理次数 为 全局ASNP统计 的 缓存清理次数 加 已清理。
    返还 已清理。
完。

同步最大队列 为 64。

同步任务 是 一种 事物，含 标签、数据、URL、方法、重试次数、创建时间。

后台同步管理器 是 一种 事物，含 任务队列、任务数、是否在线、最大重试。

后台同步_创建管理器 接受 零参：
    mgr 为 做 堆_分配 用 后台同步管理器。
    若 mgr 等于 0：
        返还 空。
    完。
    mgr 的 任务队列 为 做 堆_分配 用 同步最大队列 乘 8。
    mgr 的 任务数 为 0。
    mgr 的 是否在线 为 1。
    mgr 的 最大重试 为 3。
    返还 mgr。
完。

后台同步_注册任务 接受 mgr、标签、url、数据、方法：
    若 mgr 等于 空 或者 mgr 的 任务数 大于或等于 同步最大队列：
        返还 假。
    完。
    task 为 做 堆_分配 用 同步任务。
    若 task 等于 0：
        返还 假。
    完。
    task 的 标签 为 标签。
    task 的 URL 为 url。
    task 的 数据 为 数据。
    task 的 方法 为 方法。
    task 的 重试次数 为 0。
    task 的 创建时间 为 做 读取毫秒。
    做 内存_写64 用 mgr 的 任务队列 加 mgr 的 任务数 乘 8、task。
    mgr 的 任务数 为 mgr 的 任务数 加 1。
    若 mgr 的 是否在线 等于 1：
        做 后台同步_执行单个 用 mgr、task。
    完。
    返还 真。
完。

后台同步_执行单个 接受 mgr、task：
    若 task 等于 空：
        返还 假。
    完。
    req 为 做 网络_创建请求 用 task 的 URL。
    若 req 等于 0：
        返还 假。
    完。
    做 网络_设置方法 用 req、task 的 方法。
    若 task 的 数据 不等于 0：
        做 网络_设置请求体 用 req、task 的 数据。
    完。
    响应 为 做 网络_发送 用 req。
    若 响应 不等于 0 并且 做 网络_状态码 用 响应 大于或等于 200 并且 做 网络_状态码 用 响应 小于 300：
        全局ASNP统计 的 后台同步次数 为 全局ASNP统计 的 后台同步次数 加 1。
        返还 真。
    完。
    task 的 重试次数 为 task 的 重试次数 加 1。
    返还 假。
完。

后台同步_执行所有 接受 mgr：
    若 mgr 等于 空：
        返还。
    完。
    mgr 的 是否在线 为 1。
    可变 i 为 0。
    只要 i 小于 mgr 的 任务数：
        task 为 做 内存_读64 用 mgr 的 任务队列 加 i 乘 8。
        若 task 不等于 0 并且 task 的 重试次数 小于 mgr 的 最大重试：
            ok 为 做 后台同步_执行单个 用 mgr、task。
            若 ok：
                做 堆_释放 用 task。
                做 内存_写64 用 mgr 的 任务队列 加 i 乘 8、0。
            完。
        完。
        i 为 i 加 1。
    完。
完。

推送订阅 是 一种 事物，含 端点URL、密钥_p256dh、密钥_auth、过期时间。

推送管理器 是 一种 事物，含 订阅信息、是否订阅、通知队列、队列数。

推送_订阅 接受 mgr、应用服务器密钥：
    若 mgr 等于 空：
        返还 假。
    完。
    sub 为 做 堆_分配 用 推送订阅。
    若 sub 等于 0：
        返还 假。
    完。
    sub 的 密钥_p256dh 为 做 加密_生成ECDH密钥对。
    sub 的 密钥_auth 为 做 加密_生成随机字节 用 16。
    sub 的 端点URL 为 做 推送服务_注册 用 应用服务器密钥、sub 的 密钥_p256dh、sub 的 密钥_auth。
    sub 的 过期时间 为 做 读取毫秒 加 86400000 乘 30。
    mgr 的 订阅信息 为 sub。
    mgr 的 是否订阅 为 1。
    返还 真。
完。

推送_取消订阅 接受 mgr：
    若 mgr 等于 空 或者 mgr 的 是否订阅 等于 0：
        返还。
    完。
    若 mgr 的 订阅信息 不等于 0：
        做 推送服务_注销 用 mgr 的 订阅信息 的 端点URL。
        做 堆_释放 用 mgr 的 订阅信息。
        mgr 的 订阅信息 为 空。
    完。
    mgr 的 是否订阅 为 0。
完。

推送_处理事件 接受 mgr、推送数据：
    若 mgr 等于 空 或者 推送数据 等于 0：
        返还。
    完。
    全局ASNP统计 的 推送通知次数 为 全局ASNP统计 的 推送通知次数 加 1。
    标题 为 做 JSON_获取字符串 用 推送数据、「标题」。
    正文 为 做 JSON_获取字符串 用 推送数据、「正文」。
    图标 为 做 JSON_获取字符串 用 推送数据、「图标」。
    做 通知_显示 用 标题、正文、图标。
完。

离线Fallback管理器 是 一种 事物，含 离线页面URL、离线图片URL、离线APIURL、是否离线。

离线Fallback_创建 接受 页面URL、图片URL：
    fb 为 做 堆_分配 用 离线Fallback管理器。
    若 fb 等于 0：
        返还 空。
    完。
    fb 的 离线页面URL 为 页面URL。
    fb 的 离线图片URL 为 图片URL。
    fb 的 离线APIURL 为 空。
    fb 的 是否离线 为 0。
    返还 fb。
完。

离线Fallback_设置API响应 接受 fb、apiURL：
    若 fb 等于 空：
        返还。
    完。
    fb 的 离线APIURL 为 apiURL。
完。

离线Fallback_处理请求 接受 fb、请求类型、requestURL：
    若 fb 等于 空：
        返还 空。
    完。
    全局ASNP统计 的 离线Fallback次数 为 全局ASNP统计 的 离线Fallback次数 加 1。
    若 请求类型 等于 0：
        若 fb 的 离线页面URL 不等于 0：
            返还 做 缓存_match 用 空、fb 的 离线页面URL。
        完。
    完。
    若 请求类型 等于 1：
        若 fb 的 离线图片URL 不等于 0：
            返还 做 缓存_match 用 空、fb 的 离线图片URL。
        完。
    完。
    若 请求类型 等于 2：
        若 fb 的 离线APIURL 不等于 0：
            返还 做 缓存_match 用 空、fb 的 离线APIURL。
        完。
    完。
    返还 空。
完。

ASNP_初始化 接受 零参：
    做 内核打印 用 「[ASNP] 自适应服务工线导航预加载 启动」。
    全局ASNP统计 的 预加载次数 为 0。
    全局ASNP统计 的 缓存命中次数 为 0。
    全局ASNP统计 的 缓存未中次数 为 0。
    全局ASNP统计 的 后台同步次数 为 0。
    全局ASNP统计 的 推送通知次数 为 0。
    全局ASNP统计 的 离线Fallback次数 为 0。
    全局ASNP统计 的 AI预测次数 为 0。
    全局ASNP统计 的 缓存清理次数 为 0。
    做 内核打印 用 「[ASNP] 初始化完成 — 预加载/缓存版本/后台同步/推送/离线Fallback 就绪」。
完。

ASNP_打印诊断 接受 零参：
    做 内核打印 用 「╔════════════════════════════════════════════╗」。
    做 内核打印 用 「║  ASNP 导航预加载智能 诊断报告            ║」。
    做 内核打印 用 「╠════════════════════════════════════════════╣」。
    做 内核打印 用 「║ 预加载:       {}」、全局ASNP统计 的 预加载次数。
    做 内核打印 用 「║ 缓存命中:     {}」、全局ASNP统计 的 缓存命中次数。
    做 内核打印 用 「║ 缓存未中:     {}」、全局ASNP统计 的 缓存未中次数。
    做 内核打印 用 「║ 后台同步:     {}」、全局ASNP统计 的 后台同步次数。
    做 内核打印 用 「║ 推送通知:     {}」、全局ASNP统计 的 推送通知次数。
    做 内核打印 用 「║ 离线Fallback: {}」、全局ASNP统计 的 离线Fallback次数。
    做 内核打印 用 「║ AI预测:       {}」、全局ASNP统计 的 AI预测次数。
    做 内核打印 用 「║ 缓存清理:     {}」、全局ASNP统计 的 缓存清理次数。
    做 内核打印 用 「╚════════════════════════════════════════════╝」。
完。

ASNP_AI智能预取 接受 cache、当前页面URL：
    若 cache 等于 0 或者 当前页面URL 等于 0：
        返还。
    完。
    做 智能_SW_记录访问 用 当前页面URL。
    做 智能_SW_预测预缓存 用 cache。
完。

ASNP_缓存匹配策略 接受 cache、requestURL、策略类型：
    若 策略类型 等于 0：
        返还 做 SW_策略_缓存First 用 cache、requestURL。
    完。
    若 策略类型 等于 1：
        返还 做 SW_策略_NetworkFirst 用 cache、requestURL。
    完。
    若 策略类型 等于 2：
        返还 做 SW_策略_StaleWhileRevalidate 用 cache、requestURL。
    完。
    返还 0。
完。

本章 收束。
