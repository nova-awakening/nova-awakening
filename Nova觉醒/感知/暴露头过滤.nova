《暴露头过滤》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《.·兼容桥》。

引用《..·外联·TLS数据收发》。

引用《.·样式解析》。

引用《.·内核直连层》。

引用《..·中枢·整数转字符串》。

引用《..·执行·底层系统调用原语》。

引用《..·外联·TLS创建》。

引用《..·外联·域名初始化》。

CORS_头可暴露 接受 头名、expose_headers：
    若 做 字符串_相等_忽略大小写 用 头名、「cache-control」：
        返还 真。
    完。
    若 做 字符串_相等_忽略大小写 用 头名、「content-language」：
        返还 真。
    完。
    若 做 字符串_相等_忽略大小写 用 头名、「content-length」：
        返还 真。
    完。
    若 做 字符串_相等_忽略大小写 用 头名、「content-type」：
        返还 真。
    完。
    若 做 字符串_相等_忽略大小写 用 头名、「expires」：
        返还 真。
    完。
    若 做 字符串_相等_忽略大小写 用 头名、「last-modified」：
        返还 真。
    完。
    若 做 字符串_相等_忽略大小写 用 头名、「pragma」：
        返还 真。
    完。
    若 expose_headers 不等于 0：
        若 做 字符串_相等 用 expose_headers、「*」：
            返还 真。
        完。
        若 做 字符串_包含_忽略大小写 用 expose_headers、头名：
            返还 真。
        完。
    完。
    返还 假。
完。

字符串_相等_忽略大小写 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 a 等于 b。
    完。
    la 为 做 字节长度 用 a。
    lb 为 做 字节长度 用 b。
    若 la 不等于 lb：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 la：
        ca 为 做 字节取 用 a、i。
        cb 为 做 字节取 用 b、i。
        若 ca 大于或等于 0x41 并且 ca 小于或等于 0x5A：
            ca 为 ca 加 32。
        完。
        若 cb 大于或等于 0x41 并且 cb 小于或等于 0x5A：
            cb 为 cb 加 32。
        完。
        若 ca 不等于 cb：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

字符串_包含_忽略大小写 接受 haystack、needle：
    若 haystack 等于 0 或者 needle 等于 0：
        返还 假。
    完。
    hlen 为 做 字节长度 用 haystack。
    nlen 为 做 字节长度 用 needle。
    若 nlen 等于 0：
        返还 真。
    完。
    若 nlen 大于 hlen：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于或等于 hlen 减 nlen：
        可变 j 为 0。
        可变 匹配 为 真。
        只要 j 小于 nlen：
            ca 为 做 字节取 用 haystack、i 加 j。
            cb 为 做 字节取 用 needle、j。
            若 ca 大于或等于 0x41 并且 ca 小于或等于 0x5A：
                ca 为 ca 加 32。
            完。
            若 cb 大于或等于 0x41 并且 cb 小于或等于 0x5A：
                cb 为 cb 加 32。
            完。
            若 ca 不等于 cb：
                匹配 为 假。
                j 为 nlen。
            完。
            j 为 j 加 1。
        完。
        若 匹配：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

HTTP_获取响应头 接受 响应、头名：
    若 响应 等于 0 或者 头名 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 响应 的 头数：
        若 做 字符串_相等_忽略大小写 用 响应 的 头名 的 第 i 项、头名：
            返还 响应 的 头值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

字符串转整数 接受 s：
    若 s 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    可变 result 为 0。
    只要 i 小于 做 字节长度 用 s 并且 做 字节取 用 s、i 大于或等于 0x30 并且 做 字节取 用 s、i 小于或等于 0x39：
        result 为 result 乘 10 加 (做 字节取 用 s、i 减 0x30)。
        i 为 i 加 1。
    完。
    返还 result。
完。

取数_完整链路 接受 req：
    若 req 等于 0：
        返还 0。
    完。
    sw 为 做 Service工线_获取当前。
    若 sw 不等于 0：
        resp 为 做 Service工线_fetch事件 用 sw、req。
        若 resp 不等于 0：
            返还 resp。
        完。
    完。
    cached 为 做 HTTP缓存_查找 用 req 的 url。
    若 cached 不等于 0 并且 做 HTTP缓存_新鲜 用 cached：
        返还 cached 的 响应。
    完。
    host 为 做 URL_提取主机 用 req 的 url。
    ip 为 做 域名_解析 用 host。
    若 ip 等于 0：
        返还 0。
    完。
    port 为 做 URL_提取端口 用 req 的 url。
    fd 为 做 平台_tcp_连接 用 ip、port。
    若 fd 小于 0：
        返还 0。
    完。
    tls 为 做 安全_创建会话 用 fd、host。
    做 安全_握手 用 tls。
    resp 为 做 HTTP_发送请求 用 fd、tls、req。
    做 安全_关闭 用 tls。
    做 系统调用_close 用 fd。
    若 resp 不等于 0 并且 做 HTTP缓存_可缓存 用 resp：
        做 HTTP缓存_存储 用 req 的 url、resp。
    完。
    返还 resp。
完。

本章 收束。
