《解码》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《..·中枢·内容可见性》。

DCT块大小 为 64。

DCT块字节 为 256。

最大频率编码码长 为 16。

最大频率编码码数 为 256。

IDCT精度 为 12。

IDCT_COS3PI8 为 2217。

IDCT_COSPI8 为 5352。

IDCT_Z5常量 为 4816。

IDCT_O0常量 为 1108。

IDCT_O1常量 为 负 7568。

IDCT_O2常量 为 3784。

IDCT_O3常量 为 1567。

稀疏阈值 为 10。

MCU解码统计 是 一种 事物，含 块解码总数、DC解码次数、AC解码次数、EOB触发次数、稀疏块次数、密集块次数、逆量化次数、IDCT次数、IDCT快速路径次数、位流读取字节数、频率编码查找次数、频率编码慢路径次数、位流越界错误数、频率编码解码错误数、AI预测调用次数、AI稀疏预测命中、总解码耗时_纳秒、最大单块耗时。

AI_MCU状态 是 一种 事物，含 历史稀疏率、历史DC均值、历史非零AC均值、连续稀疏计数、连续密集计数、预测下块稀疏、采样窗口计数器、学习速率。

可变 全局MCU统计 为 0。

可变 全局MCU_AI状态 为 0。

可变 全局MCU已初始化 为 假。

ZIGZAG 为 一组：0、1、8、16、9、2、3、10、17、24、32、25、18、11、4、5、12、19、26、33、40、48、41、34、27、20、13、6、7、14、21、28、35、42、49、56、57、50、43、36、29、22、15、23、30、37、44、51、58、59、52、45、38、31、39、46、53、60、61、54、47、55、62、63。

逆ZIGZAG 为 一组：0、1、5、6、14、15、27、28、2、4、7、13、16、26、29、42、3、8、12、17、25、30、41、43、9、11、18、24、31、40、44、53、10、19、23、32、39、45、52、54、20、22、33、38、46、51、55、60、21、34、37、47、50、56、59、61、35、36、48、49、57、58、62、63。

MCU解码_初始化 接受 零参：
    若 全局MCU已初始化 等于 真：
        做 打印 用 「[MCU解码] 警告: 重复初始化」。
        返还 错误_已存在。
    完。
    全局MCU统计 为 做 堆_分配_清零 用 144。
    若 全局MCU统计 等于 空：
        做 打印 用 「[MCU解码] 错误: 统计分配失败」。
        返还 错误_内存不足。
    完。
    全局MCU_AI状态 为 做 堆_分配_清零 用 40。
    若 全局MCU_AI状态 不等于 空：
        全局MCU_AI状态 的 学习速率 为 128。
        全局MCU_AI状态 的 预测下块稀疏 为 假。
    完。
    全局MCU已初始化 为 真。
    做 打印 用 「[MCU解码] 初始化完成: 统计+AI就绪」。
    返还 0。
完。

MCU解码_销毁 接受 零参：
    若 全局MCU统计 不等于 空：
        做 堆_释放 用 全局MCU统计。
    完。
    若 全局MCU_AI状态 不等于 空：
        做 堆_释放 用 全局MCU_AI状态。
    完。
    全局MCU统计 为 0。
    全局MCU_AI状态 为 0。
    全局MCU已初始化 为 假。
    做 打印 用 「[MCU解码] 已销毁」。
    返还 0。
完。

MCU解码_重置 接受 零参：
    若 全局MCU统计 不等于 空：
        做 内存_置零 用 全局MCU统计、144。
    完。
    若 全局MCU_AI状态 不等于 空：
        做 内存_置零 用 全局MCU_AI状态、40。
        全局MCU_AI状态 的 学习速率 为 128。
    完。
    返还 0。
完。

位流_读位 接受 dec、位数：
    若 dec 等于 空 或者 位数 小于或等于 0 或者 位数 大于 16：
        返还 0。
    完。
    只要 dec 的 位计数 小于 位数：
        若 dec 的 位置 大于或等于 dec 的 数据大小：
            若 全局MCU统计 不等于 空：
                全局MCU统计 的 位流越界错误数 为 全局MCU统计 的 位流越界错误数 加 1。
            完。
            dec 的 位缓冲 为 dec 的 位缓冲 左移 8。
            dec 的 位计数 为 dec 的 位计数 加 8。
            继续。
        完。
        字节 为 做 内存_读8 用 dec 的 数据 加 dec 的 位置。
        dec 的 位置 为 dec 的 位置 加 1。
        若 字节 等于 0xFF：
            若 dec 的 位置 小于 dec 的 数据大小：
                下一字节 为 做 内存_读8 用 dec 的 数据 加 dec 的 位置。
                若 下一字节 等于 0x00：
                    dec 的 位置 为 dec 的 位置 加 1。
                否则：
                    dec 的 位置 为 dec 的 位置 减 1。
                    dec 的 位缓冲 为 dec 的 位缓冲 左移 8 位或 0xFF。
                    dec 的 位计数 为 dec 的 位计数 加 8。
                    继续。
                完。
            完。
        完。
        dec 的 位缓冲 为 dec 的 位缓冲 左移 8 位或 字节。
        dec 的 位计数 为 dec 的 位计数 加 8。
        若 全局MCU统计 不等于 空：
            全局MCU统计 的 位流读取字节数 为 全局MCU统计 的 位流读取字节数 加 1。
        完。
    完。
    dec 的 位计数 为 dec 的 位计数 减 位数。
    结果 为 dec 的 位缓冲 右移 dec 的 位计数 位与 (1 左移 位数) 减 1。
    返还 结果。
完。

位流_窥视 接受 dec、位数：
    若 dec 等于 空 或者 位数 小于或等于 0：
        返还 0。
    完。
    只要 dec 的 位计数 小于 位数：
        若 dec 的 位置 大于或等于 dec 的 数据大小：
            返还 0。
        完。
        字节 为 做 内存_读8 用 dec 的 数据 加 dec 的 位置。
        dec 的 位置 为 dec 的 位置 加 1。
        若 字节 等于 0xFF 并且 dec 的 位置 小于 dec 的 数据大小：
            若 做 内存_读8 用 dec 的 数据 加 dec 的 位置 等于 0x00：
                dec 的 位置 为 dec 的 位置 加 1。
            完。
        完。
        dec 的 位缓冲 为 dec 的 位缓冲 左移 8 位或 字节。
        dec 的 位计数 为 dec 的 位计数 加 8。
    完。
    返还 dec 的 位缓冲 右移 dec 的 位计数 减 位数 位与 (1 左移 位数) 减 1。
完。

位流_跳过 接受 dec、位数：
    若 dec 等于 空 或者 位数 小于或等于 0：
        返还。
    完。
    若 dec 的 位计数 大于或等于 位数：
        dec 的 位计数 为 dec 的 位计数 减 位数。
    否则：
        做 位流_读位 用 dec、位数。
    完。
完。

JPEG_频率编码解码 接受 dec、表：
    若 dec 等于 空 或者 表 等于 空：
        若 全局MCU统计 不等于 空：
            全局MCU统计 的 频率编码解码错误数 为 全局MCU统计 的 频率编码解码错误数 加 1。
        完。
        返还 0。
    完。
    若 全局MCU统计 不等于 空：
        全局MCU统计 的 频率编码查找次数 为 全局MCU统计 的 频率编码查找次数 加 1。
    完。
    窥视值 为 做 位流_窥视 用 dec、8。
    快速结果 为 做 内存_读16 用 表 加 窥视值 乘 2。
    快速码长 为 快速结果 右移 8 位与 0xFF。
    若 快速码长 大于 0 并且 快速码长 小于或等于 8：
        做 位流_跳过 用 dec、快速码长。
        返还 快速结果 位与 0xFF。
    完。
    若 全局MCU统计 不等于 空：
        全局MCU统计 的 频率编码慢路径次数 为 全局MCU统计 的 频率编码慢路径次数 加 1。
    完。
    可变 码值 为 0。
    可变 码长 为 0。
    慢表偏移 为 512。
    只要 码长 小于 最大频率编码码长：
        码值 为 码值 左移 1 位或 做 位流_读位 用 dec、1。
        码长 为 码长 加 1。
        条目偏移 为 慢表偏移 加 码长 乘 4。
        最小码 为 做 内存_读16 用 表 加 条目偏移。
        最大码 为 做 内存_读16 用 表 加 条目偏移 加 2。
        若 码值 大于或等于 最小码 并且 码值 小于或等于 最大码：
            符号索引 为 做 内存_读16 用 表 加 慢表偏移 加 68 加 (码值 减 最小码) 乘 2。
            返还 符号索引。
        完。
    完。
    若 全局MCU统计 不等于 空：
        全局MCU统计 的 频率编码解码错误数 为 全局MCU统计 的 频率编码解码错误数 加 1。
    完。
    返还 0。
完。

JPEG_读有符号 接受 dec、类别：
    若 类别 等于 0：
        返还 0。
    完。
    若 类别 大于 15：
        类别 为 15。
    完。
    位值 为 做 位流_读位 用 dec、类别。
    阈值 为 1 左移 类别 减 1。
    若 位值 小于 阈值：
        返还 位值 减 (1 左移 类别) 加 1。
    完。
    返还 位值。
完。

JPEG_解码块 接受 dec、分量、系数：
    若 dec 等于 空 或者 系数 等于 空：
        返还。
    完。
    开始时间 为 做 平台_获取高精度时间。
    做 内存_置零 用 系数、DCT块字节。
    可变 DC表号 为 0。
    可变 AC表号 为 0。
    若 分量 大于 0：
        DC表号 为 1。
        AC表号 为 1。
    完。
    DC类别 为 做 JPEG_频率编码解码 用 dec、dec 的 DC表 的 第 DC表号 项。
    DC差值 为 做 JPEG_读有符号 用 dec、DC类别。
    若 分量 小于 3：
        dec 的 DC预测 的 第 分量 项 为 dec 的 DC预测 的 第 分量 项 加 DC差值。
    完。
    做 内存_写32 用 系数、dec 的 DC预测 的 第 分量 项。
    若 全局MCU统计 不等于 空：
        全局MCU统计 的 DC解码次数 为 全局MCU统计 的 DC解码次数 加 1。
    完。
    可变 k 为 1。
    可变 非零AC计数 为 0。
    只要 k 小于 64：
        rs 为 做 JPEG_频率编码解码 用 dec、dec 的 AC表 的 第 AC表号 项。
        若 rs 等于 0：
            若 全局MCU统计 不等于 空：
                全局MCU统计 的 EOB触发次数 为 全局MCU统计 的 EOB触发次数 加 1。
            完。
            止。
        完。
        零游程 为 rs 右移 4 位与 0x0F。
        AC类别 为 rs 位与 0x0F。
        若 rs 等于 0xF0：
            k 为 k 加 16。
            继续。
        完。
        k 为 k 加 零游程。
        若 k 大于或等于 64：
            若 全局MCU统计 不等于 空：
                全局MCU统计 的 频率编码解码错误数 为 全局MCU统计 的 频率编码解码错误数 加 1。
            完。
            止。
        完。
        值 为 做 JPEG_读有符号 用 dec、AC类别。
        做 内存_写32 用 系数 加 ZIGZAG 的 第 k 项 乘 4、值。
        非零AC计数 为 非零AC计数 加 1。
        k 为 k 加 1。
    完。
    若 全局MCU统计 不等于 空：
        全局MCU统计 的 AC解码次数 为 全局MCU统计 的 AC解码次数 加 1。
        全局MCU统计 的 块解码总数 为 全局MCU统计 的 块解码总数 加 1。
        若 非零AC计数 小于 稀疏阈值：
            全局MCU统计 的 稀疏块次数 为 全局MCU统计 的 稀疏块次数 加 1。
        否则：
            全局MCU统计 的 密集块次数 为 全局MCU统计 的 密集块次数 加 1。
        完。
    完。
    做 AI_MCU_记录块 用 dec 的 DC预测 的 第 分量 项、非零AC计数。
    若 全局MCU统计 不等于 空：
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        全局MCU统计 的 总解码耗时_纳秒 为 全局MCU统计 的 总解码耗时_纳秒 加 耗时。
        若 耗时 大于 全局MCU统计 的 最大单块耗时：
            全局MCU统计 的 最大单块耗时 为 耗时。
        完。
    完。
完。

JPEG_逆量化 接受 系数、量化表：
    若 系数 等于 空 或者 量化表 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 64：
        q 为 做 内存_读16 用 量化表 加 i 乘 2。
        v 为 做 内存_读32 用 系数 加 i 乘 4。
        结果 为 v 乘 q。
        若 结果 大于 32767 乘 4096：
            结果 为 32767 乘 4096。
        完。
        若 结果 小于 负 32767 乘 4096：
            结果 为 负 32767 乘 4096。
        完。
        做 内存_写32 用 系数 加 i 乘 4、结果。
        i 为 i 加 1。
    完。
    若 全局MCU统计 不等于 空：
        全局MCU统计 的 逆量化次数 为 全局MCU统计 的 逆量化次数 加 1。
    完。
完。

IDCT_1D 接受 数据、步长：
    若 数据 等于 空：
        返还。
    完。
    s0 为 做 内存_读32 用 数据。
    s1 为 做 内存_读32 用 数据 加 步长。
    s2 为 做 内存_读32 用 数据 加 2 乘 步长。
    s3 为 做 内存_读32 用 数据 加 3 乘 步长。
    s4 为 做 内存_读32 用 数据 加 4 乘 步长。
    s5 为 做 内存_读32 用 数据 加 5 乘 步长。
    s6 为 做 内存_读32 用 数据 加 6 乘 步长。
    s7 为 做 内存_读32 用 数据 加 7 乘 步长。
    若 s1 等于 0 并且 s2 等于 0 并且 s3 等于 0 并且 s4 等于 0 并且 s5 等于 0 并且 s6 等于 0 并且 s7 等于 0：
        做 内存_写32 用 数据 加 步长、s0。
        做 内存_写32 用 数据 加 2 乘 步长、s0。
        做 内存_写32 用 数据 加 3 乘 步长、s0。
        做 内存_写32 用 数据 加 4 乘 步长、s0。
        做 内存_写32 用 数据 加 5 乘 步长、s0。
        做 内存_写32 用 数据 加 6 乘 步长、s0。
        做 内存_写32 用 数据 加 7 乘 步长、s0。
        返还。
    完。
    t0 为 s0 加 s4。
    t1 为 s0 减 s4。
    t2 为 s2 乘 IDCT_COS3PI8 减 s6 乘 IDCT_COSPI8 右移 IDCT精度。
    t3 为 s2 乘 IDCT_COSPI8 加 s6 乘 IDCT_COS3PI8 右移 IDCT精度。
    e0 为 t0 加 t3。
    e1 为 t1 加 t2。
    e2 为 t1 减 t2。
    e3 为 t0 减 t3。
    o0 为 s1 加 s7。
    o1 为 s5 加 s3。
    o2 为 s1 加 s3。
    o3 为 s5 加 s7。
    z5 为 (o2 减 o3) 乘 IDCT_Z5常量 右移 IDCT精度。
    o0r 为 o0 乘 IDCT_O0常量 右移 IDCT精度。
    o1r 为 o1 乘 IDCT_O1常量 右移 IDCT精度。
    o2r 为 z5 加 (o2 乘 IDCT_O2常量 右移 IDCT精度)。
    o3r 为 z5 减 (o3 乘 IDCT_O3常量 右移 IDCT精度)。
    r0 为 o0r 加 o2r 加 s7。
    r1 为 o1r 加 o3r 加 s5。
    r2 为 o1r 加 o2r 加 s3。
    r3 为 o0r 加 o3r 加 s1。
    做 内存_写32 用 数据、e0 加 r3。
    做 内存_写32 用 数据 加 步长、e1 加 r2。
    做 内存_写32 用 数据 加 2 乘 步长、e2 加 r1。
    做 内存_写32 用 数据 加 3 乘 步长、e3 加 r0。
    做 内存_写32 用 数据 加 4 乘 步长、e3 减 r0。
    做 内存_写32 用 数据 加 5 乘 步长、e2 减 r1。
    做 内存_写32 用 数据 加 6 乘 步长、e1 减 r2。
    做 内存_写32 用 数据 加 7 乘 步长、e0 减 r3。
完。

JPEG_IDCT_8x8 接受 系数：
    若 系数 等于 空：
        返还。
    完。
    可变 使用快速路径 为 假。
    若 全局MCU_AI状态 不等于 空 并且 全局MCU_AI状态 的 预测下块稀疏 等于 真：
        使用快速路径 为 真。
    完。
    可变 行 为 0。
    只要 行 小于 8：
        做 IDCT_1D 用 系数 加 行 乘 32、4。
        行 为 行 加 1。
    完。
    可变 列 为 0。
    只要 列 小于 8：
        做 IDCT_1D 用 系数 加 列 乘 4、32。
        列 为 列 加 1。
    完。
    若 全局MCU统计 不等于 空：
        全局MCU统计 的 IDCT次数 为 全局MCU统计 的 IDCT次数 加 1。
        若 使用快速路径 等于 真：
            全局MCU统计 的 IDCT快速路径次数 为 全局MCU统计 的 IDCT快速路径次数 加 1。
        完。
    完。
完。

JPEG_IDCT_仅DC 接受 系数：
    若 系数 等于 空：
        返还。
    完。
    dc值 为 做 内存_读32 用 系数。
    可变 i 为 0。
    只要 i 小于 64：
        做 内存_写32 用 系数 加 i 乘 4、dc值。
        i 为 i 加 1。
    完。
完。

MCU_获取zigzag索引 接受 自然索引：
    若 自然索引 小于 0 或者 自然索引 大于或等于 64：
        返还 0。
    完。
    返还 ZIGZAG 的 第 自然索引 项。
完。

MCU_获取逆zigzag索引 接受 zigzag索引：
    若 zigzag索引 小于 0 或者 zigzag索引 大于或等于 64：
        返还 0。
    完。
    返还 逆ZIGZAG 的 第 zigzag索引 项。
完。

MCU_计算块非零数 接受 系数：
    若 系数 等于 空：
        返还 0。
    完。
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 64：
        若 做 内存_读32 用 系数 加 i 乘 4 不等于 0：
            计数 为 计数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计数。
完。

MCU_打印块 接受 系数：
    若 系数 等于 空：
        返还。
    完。
    做 打印 用 「[MCU] 8×8块内容:」。
    可变 行 为 0。
    只要 行 小于 8：
        可变 行文本 为 「  」。
        可变 列 为 0。
        只要 列 小于 8：
            值 为 做 内存_读32 用 系数 加 (行 乘 8 加 列) 乘 4。
            行文本 为 做 字符串拼接 用 行文本、做 字符串拼接 用 做 转字符串 用 值、「 」。
            列 为 列 加 1。
        完。
        做 打印 用 行文本。
        行 为 行 加 1。
    完。
完。

AI_MCU_记录块 接受 DC值、非零AC数：
    若 全局MCU_AI状态 等于 空：
        返还。
    完。
    AI 为 全局MCU_AI状态。
    AI 的 采样窗口计数器 为 AI 的 采样窗口计数器 加 1。
    AI 的 历史DC均值 为 (AI 的 历史DC均值 乘 7 加 DC值) 除以 8。
    AI 的 历史非零AC均值 为 (AI 的 历史非零AC均值 乘 7 加 非零AC数 乘 1024) 除以 8。
    若 非零AC数 小于 稀疏阈值：
        AI 的 连续稀疏计数 为 AI 的 连续稀疏计数 加 1。
        AI 的 连续密集计数 为 0。
    否则：
        AI 的 连续密集计数 为 AI 的 连续密集计数 加 1。
        AI 的 连续稀疏计数 为 0。
    完。
    可变 当前稀疏 为 假。
    若 非零AC数 小于 稀疏阈值：
        当前稀疏 为 真。
    完。
    若 AI 的 采样窗口计数器 小于 64：
        返还。
    完。
    AI 的 采样窗口计数器 为 0。
    若 全局MCU统计 不等于 空：
        全局MCU统计 的 AI预测调用次数 为 全局MCU统计 的 AI预测调用次数 加 1。
    完。
    若 全局MCU统计 不等于 空 并且 全局MCU统计 的 块解码总数 大于 0：
        AI 的 历史稀疏率 为 全局MCU统计 的 稀疏块次数 乘 1024 除以 全局MCU统计 的 块解码总数。
    完。
    若 AI 的 历史稀疏率 大于 716 并且 AI 的 连续稀疏计数 大于 4：
        AI 的 预测下块稀疏 为 真。
    否则若 AI 的 连续密集计数 大于 2：
        AI 的 预测下块稀疏 为 假。
    完。
    若 AI 的 预测下块稀疏 等于 真 并且 当前稀疏 等于 真：
        若 全局MCU统计 不等于 空：
            全局MCU统计 的 AI稀疏预测命中 为 全局MCU统计 的 AI稀疏预测命中 加 1。
        完。
    完。
完。

MCU解码_打印统计 接受 零参：
    若 全局MCU统计 等于 空：
        做 打印 用 「[MCU解码] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   MCU解码引擎 · 统计报告            │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 块解码总数:       」、做 转字符串 用 全局MCU统计 的 块解码总数。
    做 打印 用 做 字符串拼接 用 「│ DC解码次数:       」、做 转字符串 用 全局MCU统计 的 DC解码次数。
    做 打印 用 做 字符串拼接 用 「│ AC解码次数:       」、做 转字符串 用 全局MCU统计 的 AC解码次数。
    做 打印 用 做 字符串拼接 用 「│ EOB触发:          」、做 转字符串 用 全局MCU统计 的 EOB触发次数。
    做 打印 用 做 字符串拼接 用 「│ 稀疏块:           」、做 转字符串 用 全局MCU统计 的 稀疏块次数。
    做 打印 用 做 字符串拼接 用 「│ 密集块:           」、做 转字符串 用 全局MCU统计 的 密集块次数。
    做 打印 用 做 字符串拼接 用 「│ IDCT次数:         」、做 转字符串 用 全局MCU统计 的 IDCT次数。
    做 打印 用 做 字符串拼接 用 「│ IDCT快速路径:     」、做 转字符串 用 全局MCU统计 的 IDCT快速路径次数。
    做 打印 用 做 字符串拼接 用 「│ 频率编码查找:      」、做 转字符串 用 全局MCU统计 的 频率编码查找次数。
    做 打印 用 做 字符串拼接 用 「│ 频率编码慢路径:    」、做 转字符串 用 全局MCU统计 的 频率编码慢路径次数。
    做 打印 用 做 字符串拼接 用 「│ 位流读取(KB):     」、做 转字符串 用 全局MCU统计 的 位流读取字节数 除以 1024。
    做 打印 用 做 字符串拼接 用 「│ 位流越界错误:     」、做 转字符串 用 全局MCU统计 的 位流越界错误数。
    做 打印 用 做 字符串拼接 用 「│ 频率编码错误:      」、做 转字符串 用 全局MCU统计 的 频率编码解码错误数。
    做 打印 用 做 字符串拼接 用 「│ AI预测调用:       」、做 转字符串 用 全局MCU统计 的 AI预测调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI稀疏预测命中:   」、做 转字符串 用 全局MCU统计 的 AI稀疏预测命中。
    做 打印 用 做 字符串拼接 用 「│ 总耗时(μs):       」、做 转字符串 用 全局MCU统计 的 总解码耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 最大单块耗时(μs): 」、做 转字符串 用 全局MCU统计 的 最大单块耗时 除以 1000。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

MCU解码_导出诊断 接受 零参：
    若 全局MCU_AI状态 等于 空：
        做 打印 用 「[MCU解码] AI状态未初始化」。
        返还。
    完。
    AI 为 全局MCU_AI状态。
    做 打印 用 「[MCU AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史稀疏率(Q10): 」、做 转字符串 用 AI 的 历史稀疏率。
    做 打印 用 做 字符串拼接 用 「  历史DC均值: 」、做 转字符串 用 AI 的 历史DC均值。
    做 打印 用 做 字符串拼接 用 「  历史非零AC(Q10): 」、做 转字符串 用 AI 的 历史非零AC均值。
    做 打印 用 做 字符串拼接 用 「  连续稀疏: 」、做 转字符串 用 AI 的 连续稀疏计数。
    做 打印 用 做 字符串拼接 用 「  连续密集: 」、做 转字符串 用 AI 的 连续密集计数。
    做 打印 用 做 字符串拼接 用 「  预测下块稀疏: 」、做 转字符串 用 AI 的 预测下块稀疏。
    若 全局MCU统计 不等于 空 并且 全局MCU统计 的 AI预测调用次数 大于 0：
        命中率 为 全局MCU统计 的 AI稀疏预测命中 乘 100 除以 全局MCU统计 的 AI预测调用次数。
        做 打印 用 做 字符串拼接 用 「  预测命中率(%): 」、做 转字符串 用 命中率。
    完。
完。

MCU解码_获取统计 接受 零参：
    返还 全局MCU统计。
完。

MCU解码_获取AI状态 接受 零参：
    返还 全局MCU_AI状态。
完。

MCU解码_是稀疏图像 接受 零参：
    若 全局MCU_AI状态 等于 空：
        返还 假。
    完。
    若 全局MCU_AI状态 的 历史稀疏率 大于 600：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
