《索引库工厂》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·辅助存根》。

引用《.·索引库键范围》。

引用《..·中枢·整数转字符串》。

IDBDatabase 是 一种 事物，含 名称、版本、存储列表、存储数量、存储容量、活跃事务、活跃事务数、onclose、onversionchange、已关闭。

IDBFactory 是 一种 事物，含 数据库列表、数据库数量、数据库容量。

可变 全局IDB工厂 为 0。

IDBFactory_获取 接受 零参：
    若 全局IDB工厂 等于 0：
        全局IDB工厂 为 做 堆_分配 用 IDBFactory。
        全局IDB工厂 的 数据库容量 为 32。
        全局IDB工厂 的 数据库列表 为 做 堆_分配_数组 用 32 乘 8。
        全局IDB工厂 的 数据库数量 为 0。
    完。
    返还 全局IDB工厂。
完。

IDB_open 接受 名称、版本：
    工厂 为 做 IDBFactory_获取。
    可变 db 为 0。
    可变 i 为 0。
    只要 i 小于 工厂 的 数据库数量：
        候选 为 做 内存_读64 用 工厂 的 数据库列表 加 i 乘 8。
        若 候选 不等于 0 并且 做 字符串_相等 用 候选 的 名称、名称：
            db 为 候选。
            跳出。
        完。
        i 为 i 加 1。
    完。
    可变 需要升级 为 假。
    若 db 等于 0：
        db 为 做 堆_分配 用 IDBDatabase。
        db 的 名称 为 做 字符串_复制 用 名称。
        db 的 版本 为 0。
        db 的 存储容量 为 32。
        db 的 存储列表 为 做 堆_分配_数组 用 32 乘 8。
        db 的 存储数量 为 0。
        db 的 活跃事务 为 做 堆_分配_数组 用 16 乘 8。
        db 的 活跃事务数 为 0。
        db 的 已关闭 为 假。
        若 工厂 的 数据库数量 小于 工厂 的 数据库容量：
            做 内存_写64 用 工厂 的 数据库列表 加 工厂 的 数据库数量 乘 8、db。
            工厂 的 数据库数量 为 工厂 的 数据库数量 加 1。
        完。
        需要升级 为 真。
    完。
    若 版本 大于 db 的 版本：
        需要升级 为 真。
    完。
    req 为 做 IDBRequest_创建。
    req 的 结果 为 db。
    若 需要升级：
        db 的 版本 为 版本。
    完。
    返还 req。
完。

IDB_deleteDatabase 接受 名称：
    工厂 为 做 IDBFactory_获取。
    可变 i 为 0。
    只要 i 小于 工厂 的 数据库数量：
        db 为 做 内存_读64 用 工厂 的 数据库列表 加 i 乘 8。
        若 db 不等于 0 并且 做 字符串_相等 用 db 的 名称、名称：
            工厂 的 数据库数量 为 工厂 的 数据库数量 减 1。
            做 内存_写64 用 工厂 的 数据库列表 加 i 乘 8、做 内存_读64 用 工厂 的 数据库列表 加 工厂 的 数据库数量 乘 8。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

本章 收束。
