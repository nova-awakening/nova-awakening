《边界框计算》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·字符串替换》。

引用《..·中枢·内存初始化》。

引用《..·推理·AI鼠标事件处理》。

SVG命令_M 为 77。

SVG命令_L 为 76。

SVG命令_C 为 67。

SVG命令_Q 为 81。

SVG命令_Z 为 90。

SVG命令_A 为 65。

填充规则_偶奇 为 0。

填充规则_非零 为 1。

Q_HALF 为 512。

整数最大 为 0x7FFFFFFF。

整数最小 为 负 0x7FFFFFFF。

SVG几何统计 是 一种 事物，含 边界框计算次数、精确贝塞尔边界次数、命中测试总次数、命中成功次数、命中失败次数、面积计算次数、扫描线交点计算次数、视口裁剪跳过次数、视口裁剪保留次数、AI渲染决策次数、AI跳过渲染次数、AI降精度次数、AI高精度次数、总计算耗时_纳秒、错误总数、溢出保护次数、路径段遍历总数、三角函数调用次数。

AI_SVG渲染状态 是 一种 事物，含 历史可见率、历史平均复杂度、历史平均面积、连续跳过计数、连续渲染计数、预测下次可见、上次视口宽、上次视口高、采样计数器、学习权重。

可变 全局SVG几何统计 为 0。

可变 全局SVG_AI渲染 为 0。

可变 全局SVG几何已初始化 为 假。

SVG几何_初始化 接受 零参：
    若 全局SVG几何已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局SVG几何统计 为 做 堆_分配_清零 用 144。
    若 全局SVG几何统计 等于 空：
        返还 错误_内存不足。
    完。
    全局SVG_AI渲染 为 做 堆_分配_清零 用 48。
    若 全局SVG_AI渲染 不等于 空：
        全局SVG_AI渲染 的 预测下次可见 为 真。
        全局SVG_AI渲染 的 学习权重 为 128。
    完。
    全局SVG几何已初始化 为 真。
    做 打印 用 「[SVG几何] 初始化完成」。
    返还 0。
完。

SVG几何_销毁 接受 零参：
    若 全局SVG几何统计 不等于 空：
        做 堆_释放 用 全局SVG几何统计。
        全局SVG几何统计 为 0。
    完。
    若 全局SVG_AI渲染 不等于 空：
        做 堆_释放 用 全局SVG_AI渲染。
        全局SVG_AI渲染 为 0。
    完。
    全局SVG几何已初始化 为 假。
    返还 0。
完。

SVG几何_重置 接受 零参：
    若 全局SVG几何统计 不等于 空：
        做 内存_置零 用 全局SVG几何统计、144。
    完。
    返还 0。
完。

SVGPath_计算边界 接受 path：
    若 path 等于 0 或者 path 的 段链头 等于 0：
        返还。
    完。
    可变 min_x 为 0x7FFFFFFF。
    可变 min_y 为 0x7FFFFFFF。
    可变 max_x 为 负 0x7FFFFFFF。
    可变 max_y 为 负 0x7FFFFFFF。
    可变 seg 为 path 的 段链头。
    只要 seg 不等于 0：
        x 为 seg 的 参数 的 第 0 项。
        y 为 seg 的 参数 的 第 1 项。
        若 seg 的 命令 不等于 SVG命令_Z：
            若 x 小于 min_x：
                min_x 为 x。
            完。
            若 y 小于 min_y：
                min_y 为 y。
            完。
            若 x 大于 max_x：
                max_x 为 x。
            完。
            若 y 大于 max_y：
                max_y 为 y。
            完。
            若 seg 的 命令 等于 SVG命令_C：
                可变 j 为 2。
                只要 j 小于或等于 4：
                    px 为 seg 的 参数 的 第 j 乘 2 减 2 项。
                    py 为 seg 的 参数 的 第 j 乘 2 减 1 项。
                    若 px 小于 min_x：
                        min_x 为 px。
                    完。
                    若 py 小于 min_y：
                        min_y 为 py。
                    完。
                    若 px 大于 max_x：
                        max_x 为 px。
                    完。
                    若 py 大于 max_y：
                        max_y 为 py。
                    完。
                    j 为 j 加 1。
                完。
            完。
        完。
        seg 为 seg 的 下一个。
    完。
    path 的 bbox_x 为 min_x。
    path 的 bbox_y 为 min_y。
    path 的 bbox_w 为 max_x 减 min_x。
    path 的 bbox_h 为 max_y 减 min_y。
完。

智能_SVG_渲染精度 接受 path、视口x、视口y、视口w、视口h：
    若 path 等于 0：
        返还 0。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        若 path 的 bbox_x 加 path 的 bbox_w 小于 视口x 或者 path 的 bbox_x 大于 视口x 加 视口w：
            返还 0。
        完。
        若 path 的 bbox_y 加 path 的 bbox_h 小于 视口y 或者 path 的 bbox_y 大于 视口y 加 视口h：
            返还 0。
        完。
        返还 8。
    完。
    中心x 为 path 的 bbox_x 加 path 的 bbox_w 除以 2。
    中心y 为 path 的 bbox_y 加 path 的 bbox_h 除以 2。
    级别 为 做 AI总线_渲染质量 用 bus、中心x 除以 1024、中心y 除以 1024、path 的 bbox_w 除以 1024、path 的 bbox_h 除以 1024、Q_HALF、真。
    若 级别 小于或等于 1：
        返还 0。
    完。
    若 级别 小于或等于 3：
        返还 4。
    完。
    若 级别 小于或等于 6：
        返还 8。
    完。
    返还 16。
完。

SVG_abs 接受 v：
    若 v 小于 0：
        返还 负 v。
    完。
    返还 v。
完。

SVG_cos 接受 弧度：
    x2 为 弧度 乘 弧度 除以 1024。
    x4 为 x2 乘 x2 除以 1024。
    返还 1024 减 x2 除以 2 加 x4 除以 24。
完。

SVG_sin 接受 弧度：
    x2 为 弧度 乘 弧度 除以 1024。
    x3 为 x2 乘 弧度 除以 1024。
    x5 为 x3 乘 x2 除以 1024。
    返还 弧度 减 x3 除以 6 加 x5 除以 120。
完。

SVG_读数字_从 接受 s、i：
    若 s 等于 0：
        返还 0。
    完。
    len 为 做 字节长度 用 s。
    可变 p 为 i。
    只要 p 小于 len 并且 (做 字节取 用 s、p 等于 32 或者 做 字节取 用 s、p 等于 44)：
        p 为 p 加 1。
    完。
    可变 负 为 假。
    若 p 小于 len 并且 做 字节取 用 s、p 等于 0x2D：
        负 为 真。
        p 为 p 加 1。
    完。
    可变 整数部 为 0。
    只要 p 小于 len 并且 做 字节取 用 s、p 大于或等于 0x30 并且 做 字节取 用 s、p 小于或等于 0x39：
        整数部 为 整数部 乘 10 加 (做 字节取 用 s、p 减 0x30)。
        p 为 p 加 1。
    完。
    可变 小数部 为 0。
    可变 分母 为 1。
    若 p 小于 len 并且 做 字节取 用 s、p 等于 0x2E：
        p 为 p 加 1。
        只要 p 小于 len 并且 做 字节取 用 s、p 大于或等于 0x30 并且 做 字节取 用 s、p 小于或等于 0x39：
            小数部 为 小数部 乘 10 加 (做 字节取 用 s、p 减 0x30)。
            分母 为 分母 乘 10。
            p 为 p 加 1。
        完。
    完。
    result 为 整数部 乘 100 加 小数部 乘 100 除以 分母。
    若 负：
        result 为 0 减 result。
    完。
    返还 result。
完。

SVG_跳过数字 接受 s、i：
    返还 i 加 1。
完。

字符串_区间匹配 接受 s、i、模式：
    若 s 等于 0 或者 模式 等于 0：
        返还 假。
    完。
    slen 为 做 字节长度 用 s。
    plen 为 做 字节长度 用 模式。
    若 i 加 plen 大于 slen：
        返还 假。
    完。
    可变 j 为 0。
    只要 j 小于 plen：
        若 做 字节取 用 s、i 加 j 不等于 做 字节取 用 模式、j：
            返还 假。
        完。
        j 为 j 加 1。
    完。
    返还 真。
完。

SVG_边界框_包含点 接受 bbox_x、bbox_y、bbox_w、bbox_h、px、py：
    若 px 小于 bbox_x 或者 px 大于 bbox_x 加 bbox_w：
        返还 假。
    完。
    若 py 小于 bbox_y 或者 py 大于 bbox_y 加 bbox_h：
        返还 假。
    完。
    返还 真。
完。

SVG_边界框_相交 接受 ax、ay、aw、ah、bx、by、bw、bh：
    若 ax 加 aw 小于 bx 或者 bx 加 bw 小于 ax：
        返还 假。
    完。
    若 ay 加 ah 小于 by 或者 by 加 bh 小于 ay：
        返还 假。
    完。
    返还 真。
完。

SVG_边界框_合并 接受 路径A、路径B：
    若 路径A 等于 空 或者 路径B 等于 空：
        返还。
    完。
    可变 min_x 为 路径A 的 bbox_x。
    可变 min_y 为 路径A 的 bbox_y。
    可变 max_x 为 路径A 的 bbox_x 加 路径A 的 bbox_w。
    可变 max_y 为 路径A 的 bbox_y 加 路径A 的 bbox_h。
    若 路径B 的 bbox_x 小于 min_x：
        min_x 为 路径B 的 bbox_x。
    完。
    若 路径B 的 bbox_y 小于 min_y：
        min_y 为 路径B 的 bbox_y。
    完。
    若 路径B 的 bbox_x 加 路径B 的 bbox_w 大于 max_x：
        max_x 为 路径B 的 bbox_x 加 路径B 的 bbox_w。
    完。
    若 路径B 的 bbox_y 加 路径B 的 bbox_h 大于 max_y：
        max_y 为 路径B 的 bbox_y 加 路径B 的 bbox_h。
    完。
    路径A 的 bbox_x 为 min_x。
    路径A 的 bbox_y 为 min_y。
    路径A 的 bbox_w 为 max_x 减 min_x。
    路径A 的 bbox_h 为 max_y 减 min_y。
完。

SVG_边界框_面积 接受 path：
    若 path 等于 空：
        返还 0。
    完。
    返还 path 的 bbox_w 乘 path 的 bbox_h。
完。

SVG_边界框_中心X 接受 path：
    若 path 等于 空：
        返还 0。
    完。
    返还 path 的 bbox_x 加 path 的 bbox_w 除以 2。
完。

SVG_边界框_中心Y 接受 path：
    若 path 等于 空：
        返还 0。
    完。
    返还 path 的 bbox_y 加 path 的 bbox_h 除以 2。
完。

SVG_命中测试_展平路径 接受 线段、线段数、px、py、填充规则：
    若 线段 等于 空 或者 线段数 小于或等于 0：
        返还 假。
    完。
    可变 缠绕数 为 0。
    可变 交叉数 为 0。
    可变 i 为 0。
    只要 i 小于 线段数：
        偏移 为 i 乘 16。
        x0 为 做 内存_读32 用 线段 加 偏移。
        y0 为 做 内存_读32 用 线段 加 偏移 加 4。
        x1 为 做 内存_读32 用 线段 加 偏移 加 8。
        y1 为 做 内存_读32 用 线段 加 偏移 加 12。
        若 y0 等于 y1：
            i 为 i 加 1。
            继续。
        完。
        若 py 小于 y0 并且 py 小于 y1：
            i 为 i 加 1。
            继续。
        完。
        若 py 大于或等于 y0 并且 py 大于或等于 y1：
            i 为 i 加 1。
            继续。
        完。
        dx 为 x1 减 x0。
        dy 为 y1 减 y0。
        若 dy 等于 0：
            dy 为 1。
        完。
        x_交 为 x0 加 (py 减 y0) 乘 dx 除以 dy。
        若 x_交 大于 px：
            交叉数 为 交叉数 加 1。
            若 y1 大于 y0：
                缠绕数 为 缠绕数 加 1。
            否则：
                缠绕数 为 缠绕数 减 1。
            完。
        完。
        i 为 i 加 1。
    完。
    若 全局SVG几何统计 不等于 空：
        全局SVG几何统计 的 命中测试总次数 为 全局SVG几何统计 的 命中测试总次数 加 1。
        全局SVG几何统计 的 扫描线交点计算次数 为 全局SVG几何统计 的 扫描线交点计算次数 加 线段数。
    完。
    若 填充规则 等于 填充规则_偶奇：
        若 交叉数 取余于 2 等于 1：
            若 全局SVG几何统计 不等于 空：
                全局SVG几何统计 的 命中成功次数 为 全局SVG几何统计 的 命中成功次数 加 1。
            完。
            返还 真。
        完。
    否则：
        若 缠绕数 不等于 0：
            若 全局SVG几何统计 不等于 空：
                全局SVG几何统计 的 命中成功次数 为 全局SVG几何统计 的 命中成功次数 加 1。
            完。
            返还 真。
        完。
    完。
    若 全局SVG几何统计 不等于 空：
        全局SVG几何统计 的 命中失败次数 为 全局SVG几何统计 的 命中失败次数 加 1。
    完。
    返还 假。
完。

SVG_计算路径面积 接受 线段、线段数：
    若 线段 等于 空 或者 线段数 小于或等于 0：
        返还 0。
    完。
    可变 面积2倍 为 0。
    可变 i 为 0。
    只要 i 小于 线段数：
        偏移 为 i 乘 16。
        x0 为 做 内存_读32 用 线段 加 偏移。
        y0 为 做 内存_读32 用 线段 加 偏移 加 4。
        x1 为 做 内存_读32 用 线段 加 偏移 加 8。
        y1 为 做 内存_读32 用 线段 加 偏移 加 12。
        面积2倍 为 面积2倍 加 (x0 乘 y1 减 x1 乘 y0)。
        i 为 i 加 1。
    完。
    若 面积2倍 小于 0：
        面积2倍 为 0 减 面积2倍。
    完。
    若 全局SVG几何统计 不等于 空：
        全局SVG几何统计 的 面积计算次数 为 全局SVG几何统计 的 面积计算次数 加 1。
    完。
    返还 面积2倍 除以 2。
完。

SVG_视口裁剪 接受 path、视口x、视口y、视口w、视口h：
    若 path 等于 空：
        返还 假。
    完。
    若 做 SVG_边界框_相交 用 path 的 bbox_x、path 的 bbox_y、path 的 bbox_w、path 的 bbox_h、视口x、视口y、视口w、视口h 等于 假：
        若 全局SVG几何统计 不等于 空：
            全局SVG几何统计 的 视口裁剪跳过次数 为 全局SVG几何统计 的 视口裁剪跳过次数 加 1。
        完。
        返还 假。
    完。
    若 全局SVG几何统计 不等于 空：
        全局SVG几何统计 的 视口裁剪保留次数 为 全局SVG几何统计 的 视口裁剪保留次数 加 1。
    完。
    返还 真。
完。

SVG_视口裁剪_完全包含 接受 path、视口x、视口y、视口w、视口h：
    若 path 等于 空：
        返还 假。
    完。
    若 path 的 bbox_x 大于或等于 视口x 并且 path 的 bbox_y 大于或等于 视口y 并且 path 的 bbox_x 加 path 的 bbox_w 小于或等于 视口x 加 视口w 并且 path 的 bbox_y 加 path 的 bbox_h 小于或等于 视口y 加 视口h：
        返还 真。
    完。
    返还 假。
完。

SVG_评估路径复杂度 接受 path：
    若 path 等于 空：
        返还 0。
    完。
    可变 段数 为 0。
    可变 曲线段数 为 0。
    可变 seg 为 path 的 段链头。
    只要 seg 不等于 空：
        段数 为 段数 加 1。
        若 seg 的 命令 等于 SVG命令_C 或者 seg 的 命令 等于 SVG命令_Q 或者 seg 的 命令 等于 SVG命令_A：
            曲线段数 为 曲线段数 加 1。
        完。
        seg 为 seg 的 下一个。
    完。
    若 全局SVG几何统计 不等于 空：
        全局SVG几何统计 的 路径段遍历总数 为 全局SVG几何统计 的 路径段遍历总数 加 段数。
    完。
    返还 段数 加 曲线段数 乘 3。
完。

AI_SVG渲染_决策 接受 path、视口x、视口y、视口w、视口h：
    若 path 等于 空：
        返还 0。
    完。
    若 全局SVG_AI渲染 等于 空：
        若 做 SVG_视口裁剪 用 path、视口x、视口y、视口w、视口h 等于 假：
            返还 0。
        完。
        返还 8。
    完。
    AI 为 全局SVG_AI渲染。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    可见 为 做 SVG_视口裁剪 用 path、视口x、视口y、视口w、视口h。
    复杂度 为 做 SVG_评估路径复杂度 用 path。
    面积 为 做 SVG_边界框_面积 用 path。
    可变 视口面积 为 视口w 乘 视口h。
    若 视口面积 小于或等于 0：
        视口面积 为 1。
    完。
    面积比 为 面积 乘 1024 除以 视口面积。
    AI 的 历史平均复杂度 为 (AI 的 历史平均复杂度 乘 7 加 复杂度 乘 1024) 除以 8。
    AI 的 历史平均面积 为 (AI 的 历史平均面积 乘 7 加 面积比) 除以 8。
    若 可见 等于 真：
        AI 的 连续渲染计数 为 AI 的 连续渲染计数 加 1。
        AI 的 连续跳过计数 为 0。
    否则：
        AI 的 连续跳过计数 为 AI 的 连续跳过计数 加 1。
        AI 的 连续渲染计数 为 0。
    完。
    若 AI 的 采样计数器 大于或等于 32：
        AI 的 采样计数器 为 0。
        若 全局SVG几何统计 不等于 空：
            全局SVG几何统计 的 AI渲染决策次数 为 全局SVG几何统计 的 AI渲染决策次数 加 1。
        完。
        若 全局SVG几何统计 不等于 空：
            总测试 为 全局SVG几何统计 的 视口裁剪跳过次数 加 全局SVG几何统计 的 视口裁剪保留次数。
            若 总测试 大于 0：
                AI 的 历史可见率 为 全局SVG几何统计 的 视口裁剪保留次数 乘 1024 除以 总测试。
            完。
        完。
        若 AI 的 连续跳过计数 大于 10：
            AI 的 预测下次可见 为 假。
        否则若 AI 的 连续渲染计数 大于 5：
            AI 的 预测下次可见 为 真。
        完。
    完。
    若 可见 等于 假：
        若 全局SVG几何统计 不等于 空：
            全局SVG几何统计 的 AI跳过渲染次数 为 全局SVG几何统计 的 AI跳过渲染次数 加 1。
        完。
        返还 0。
    完。
    若 面积比 小于 10：
        若 全局SVG几何统计 不等于 空：
            全局SVG几何统计 的 AI降精度次数 为 全局SVG几何统计 的 AI降精度次数 加 1。
        完。
        返还 4。
    完。
    若 面积比 大于 200 或者 复杂度 大于 50：
        若 全局SVG几何统计 不等于 空：
            全局SVG几何统计 的 AI高精度次数 为 全局SVG几何统计 的 AI高精度次数 加 1。
        完。
        返还 16。
    完。
    返还 8。
完。

SVG几何_打印统计 接受 零参：
    若 全局SVG几何统计 等于 空：
        做 打印 用 「[SVG几何] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   SVG几何计算 · 统计报告            │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 边界框计算:       」、做 转字符串 用 全局SVG几何统计 的 边界框计算次数。
    做 打印 用 做 字符串拼接 用 「│ 命中测试:         」、做 转字符串 用 全局SVG几何统计 的 命中测试总次数。
    做 打印 用 做 字符串拼接 用 「│ 命中成功:         」、做 转字符串 用 全局SVG几何统计 的 命中成功次数。
    做 打印 用 做 字符串拼接 用 「│ 命中失败:         」、做 转字符串 用 全局SVG几何统计 的 命中失败次数。
    做 打印 用 做 字符串拼接 用 「│ 面积计算:         」、做 转字符串 用 全局SVG几何统计 的 面积计算次数。
    做 打印 用 做 字符串拼接 用 「│ 扫描线交点:       」、做 转字符串 用 全局SVG几何统计 的 扫描线交点计算次数。
    做 打印 用 做 字符串拼接 用 「│ 视口裁剪跳过:     」、做 转字符串 用 全局SVG几何统计 的 视口裁剪跳过次数。
    做 打印 用 做 字符串拼接 用 「│ 视口裁剪保留:     」、做 转字符串 用 全局SVG几何统计 的 视口裁剪保留次数。
    做 打印 用 做 字符串拼接 用 「│ AI渲染决策:       」、做 转字符串 用 全局SVG几何统计 的 AI渲染决策次数。
    做 打印 用 做 字符串拼接 用 「│ AI跳过渲染:       」、做 转字符串 用 全局SVG几何统计 的 AI跳过渲染次数。
    做 打印 用 做 字符串拼接 用 「│ AI降精度:         」、做 转字符串 用 全局SVG几何统计 的 AI降精度次数。
    做 打印 用 做 字符串拼接 用 「│ AI高精度:         」、做 转字符串 用 全局SVG几何统计 的 AI高精度次数。
    做 打印 用 做 字符串拼接 用 「│ 路径段遍历:       」、做 转字符串 用 全局SVG几何统计 的 路径段遍历总数。
    做 打印 用 做 字符串拼接 用 「│ 总计算耗时(μs):   」、做 转字符串 用 全局SVG几何统计 的 总计算耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ 错误:             」、做 转字符串 用 全局SVG几何统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

SVG几何_导出诊断 接受 零参：
    若 全局SVG_AI渲染 等于 空：
        做 打印 用 「[SVG几何 AI] 未初始化」。
        返还。
    完。
    AI 为 全局SVG_AI渲染。
    做 打印 用 「[SVG几何 AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史可见率(Q10): 」、做 转字符串 用 AI 的 历史可见率。
    做 打印 用 做 字符串拼接 用 「  历史复杂度(Q10): 」、做 转字符串 用 AI 的 历史平均复杂度。
    做 打印 用 做 字符串拼接 用 「  历史面积比(Q10): 」、做 转字符串 用 AI 的 历史平均面积。
    做 打印 用 做 字符串拼接 用 「  连续跳过: 」、做 转字符串 用 AI 的 连续跳过计数。
    做 打印 用 做 字符串拼接 用 「  连续渲染: 」、做 转字符串 用 AI 的 连续渲染计数。
    做 打印 用 做 字符串拼接 用 「  预测可见: 」、做 转字符串 用 AI 的 预测下次可见。
完。

SVG几何_获取统计 接受 零参：
    返还 全局SVG几何统计。
完。

SVG几何_获取AI状态 接受 零参：
    返还 全局SVG_AI渲染。
完。

SVG几何_推荐精度 接受 path、视口x、视口y、视口w、视口h：
    返还 做 AI_SVG渲染_决策 用 path、视口x、视口y、视口w、视口h。
完。

SVG几何_路径可见 接受 path、视口x、视口y、视口w、视口h：
    返还 做 SVG_视口裁剪 用 path、视口x、视口y、视口w、视口h。
完。

本章 收束。
