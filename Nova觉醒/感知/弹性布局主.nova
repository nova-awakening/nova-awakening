《弹性布局主》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·写回位置到》。

引用《.·Flex弹性布局》。

引用《.·主轴对齐》。

弹性布局_布局 接受 容器：
    若 容器 等于 0 或者 容器 的 项目数 等于 0：
        返还。
    完。
    做 弹性布局_解析基准 用 容器。
    做 弹性布局_确定行 用 容器。
    可变 i 为 0。
    只要 i 小于 容器 的 行数：
        行 为 做 弹性布局_获取行 用 容器、i。
        做 弹性布局_弹性调整 用 容器、行。
        i 为 i 加 1。
    完。
    做 弹性布局_计算交叉轴 用 容器。
    i 为 0。
    只要 i 小于 容器 的 行数：
        行 为 做 弹性布局_获取行 用 容器、i。
        做 弹性布局_主轴对齐 用 容器、行。
        i 为 i 加 1。
    完。
    做 弹性布局_交叉轴对齐 用 容器。
    做 弹性布局_写回位置 用 容器。
完。

弹性布局_解析基准 接受 容器：
    是行方向 为 容器 的 方向 等于 FLEX_DIR_ROW 或者 容器 的 方向 等于 FLEX_DIR_ROW_REVERSE。
    可变 i 为 0。
    只要 i 小于 容器 的 项目数：
        项 为 做 弹性布局_获取项 用 容器、i。
        若 项 的 flex_basis 小于 0：
            若 项 的 布局对象 不等于 0：
                若 是行方向：
                    项 的 基准尺寸 为 项 的 布局对象 的 宽度。
                否则：
                    项 的 基准尺寸 为 项 的 布局对象 的 高度。
                完。
            否则：
                项 的 基准尺寸 为 0。
            完。
        否则：
            项 的 基准尺寸 为 项 的 flex_basis。
        完。
        项 的 外部主轴尺寸 为 项 的 基准尺寸 加 项 的 主轴margin前 加 项 的 主轴margin后。
        项 的 假设主尺寸 为 项 的 基准尺寸。
        项 的 已冻结 为 假。
        i 为 i 加 1。
    完。
完。

弹性布局_确定行 接受 容器：
    若 容器 的 换行 等于 FLEX_WRAP_NOWRAP：
        容器 的 行数 为 1。
        容器 的 行 为 做 堆_分配_数组 用 做 大小of 用 弹性Line。
        行 为 容器 的 行。
        行 的 起始索引 为 0。
        行 的 结束索引 为 容器 的 项目数。
        行 的 主轴已用 为 做 弹性布局_计算行主轴总和 用 容器、0、容器 的 项目数。
        行 的 剩余空间 为 容器 的 可用主轴 减 行 的 主轴已用。
        返还。
    完。
    行容量 为 16。
    容器 的 行 为 做 堆_分配_数组 用 行容量 乘 做 大小of 用 弹性Line。
    容器 的 行数 为 0。
    可变 行开始 为 0。
    可变 当前主轴用量 为 0。
    间距 为 容器 的 列间距。
    可变 i 为 0。
    只要 i 小于 容器 的 项目数：
        项 为 做 弹性布局_获取项 用 容器、i。
        新用量 为 当前主轴用量 加 项 的 外部主轴尺寸。
        若 i 大于 行开始：
            新用量 为 新用量 加 间距。
        完。
        若 新用量 大于 容器 的 可用主轴 并且 i 大于 行开始：
            做 弹性布局_添加行 用 容器、行开始、i、当前主轴用量。
            行开始 为 i。
            当前主轴用量 为 项 的 外部主轴尺寸。
        否则：
            当前主轴用量 为 新用量。
        完。
        i 为 i 加 1。
    完。
    若 行开始 小于 容器 的 项目数：
        做 弹性布局_添加行 用 容器、行开始、容器 的 项目数、当前主轴用量。
    完。
完。

弹性布局_添加行 接受 容器、起始、结束、主轴用量：
    若 容器 的 行数 大于或等于 16：
        返还。
    完。
    行 为 容器 的 行 加 容器 的 行数 乘 做 大小of 用 弹性Line。
    行 的 起始索引 为 起始。
    行 的 结束索引 为 结束。
    行 的 主轴已用 为 主轴用量。
    行 的 剩余空间 为 容器 的 可用主轴 减 主轴用量。
    行 的 交叉轴尺寸 为 0。
    行 的 交叉轴位置 为 0。
    行 的 总grow 为 0。
    行 的 总shrink 为 0。
    可变 i 为 起始。
    只要 i 小于 结束：
        项 为 做 弹性布局_获取项 用 容器、i。
        行 的 总grow 为 行 的 总grow 加 项 的 flex_grow。
        行 的 总shrink 为 行 的 总shrink 加 项 的 flex_shrink。
        i 为 i 加 1。
    完。
    容器 的 行数 为 容器 的 行数 加 1。
完。

弹性布局_计算行主轴总和 接受 容器、起始、结束：
    可变 总和 为 0。
    间距 为 容器 的 列间距。
    可变 i 为 起始。
    只要 i 小于 结束：
        项 为 做 弹性布局_获取项 用 容器、i。
        总和 为 总和 加 项 的 外部主轴尺寸。
        若 i 大于 起始：
            总和 为 总和 加 间距。
        完。
        i 为 i 加 1。
    完。
    返还 总和。
完。

本章 收束。
