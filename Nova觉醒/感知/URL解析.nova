《URL解析》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·辅助存根》。

引用《..·执行·底层内存操》。

引用《.·内核直连层》。

引用《..·中枢·字符串替换》。

HTTP_GET 为 1。

HTTP_POST 为 2。

HTTP_PUT 为 3。

HTTP_DELETE 为 4。

HTTP_HEAD 为 5。

HTTP_OPTIONS 为 6。

HTTP_PATCH 为 7。

资源类型_主文档 为 1。

资源类型_样式表 为 2。

资源类型_脚本 为 3。

资源类型_图片 为 4。

资源类型_字体 为 5。

资源类型_XHR 为 6。

资源类型_媒体 为 7。

资源类型_清单 为 8。

资源类型_预取 为 9。

资源类型_长连接 为 10。

资源类型_Favicon 为 11。

优先级_最高 为 4。

优先级_高 为 3。

优先级_中 为 2。

优先级_低 为 1。

优先级_最低 为 0。

请求状态_未发送 为 0。

请求状态_已打开 为 1。

请求状态_发送中 为 2。

请求状态_接收头 为 3。

请求状态_接收体 为 4。

请求状态_完成 为 5。

请求状态_失败 为 6。

请求状态_取消 为 7。

URL 是 一种 事物，含 完整URL、协议、协议长度、主机、主机长度、端口、路径、路径长度、查询、查询长度、片段、片段长度、是安全的。

URL_解析 接受 url字符串：
    url 为 做 堆_分配 用 URL。
    url 的 完整URL 为 做 字符串_复制 用 url字符串。
    长度 为 做 字符串_长度 用 url字符串。
    可变 pos 为 0。
    url 的 协议 为 url字符串。
    可变 协议结束 为 0。
    只要 协议结束 小于 长度：
        若 做 内存_读8 用 url字符串 加 协议结束 等于 0x3A：
            跳出。
        完。
        协议结束 为 协议结束 加 1。
    完。
    url 的 协议 为 做 字符串_复制_N 用 url字符串、协议结束。
    url 的 协议长度 为 协议结束。
    url 的 是安全的 为 做 字符串_等于N 用 url 的 协议、url 的 协议长度、「https」。
    pos 为 协议结束 加 1。
    若 pos 小于 长度 减 1 并且 做 内存_读8 用 url字符串 加 pos 等于 0x2F 并且 做 内存_读8 用 url字符串 加 pos 加 1 等于 0x2F：
        pos 为 pos 加 2。
    完。
    主机开始 为 pos。
    只要 pos 小于 长度：
        c 为 做 内存_读8 用 url字符串 加 pos。
        若 c 等于 0x2F 或者 c 等于 0x3A 或者 c 等于 0x3F 或者 c 等于 0x23：
            跳出。
        完。
        pos 为 pos 加 1。
    完。
    url 的 主机 为 做 字符串_复制_N 用 url字符串 加 主机开始、pos 减 主机开始。
    url 的 主机长度 为 pos 减 主机开始。
    url 的 端口 为 0。
    若 pos 小于 长度 并且 做 内存_读8 用 url字符串 加 pos 等于 0x3A：
        pos 为 pos 加 1。
        只要 pos 小于 长度 并且 做 内存_读8 用 url字符串 加 pos 大于或等于 0x30 并且 做 内存_读8 用 url字符串 加 pos 小于或等于 0x39：
            url 的 端口 为 url 的 端口 乘 10 加 (做 内存_读8 用 url字符串 加 pos 减 0x30)。
            pos 为 pos 加 1。
        完。
    完。
    若 url 的 端口 等于 0：
        若 url 的 是安全的：
            url 的 端口 为 443。
        否则：
            url 的 端口 为 80。
        完。
    完。
    路径开始 为 pos。
    只要 pos 小于 长度：
        c 为 做 内存_读8 用 url字符串 加 pos。
        若 c 等于 0x3F 或者 c 等于 0x23：
            跳出。
        完。
        pos 为 pos 加 1。
    完。
    若 pos 大于 路径开始：
        url 的 路径 为 做 字符串_复制_N 用 url字符串 加 路径开始、pos 减 路径开始。
        url 的 路径长度 为 pos 减 路径开始。
    否则：
        url 的 路径 为 做 字符串_复制 用 「/」。
        url 的 路径长度 为 1。
    完。
    url 的 查询 为 0。
    url 的 查询长度 为 0。
    若 pos 小于 长度 并且 做 内存_读8 用 url字符串 加 pos 等于 0x3F：
        pos 为 pos 加 1。
        查询开始 为 pos。
        只要 pos 小于 长度 并且 做 内存_读8 用 url字符串 加 pos 不等于 0x23：
            pos 为 pos 加 1。
        完。
        url 的 查询 为 做 字符串_复制_N 用 url字符串 加 查询开始、pos 减 查询开始。
        url 的 查询长度 为 pos 减 查询开始。
    完。
    url 的 片段 为 0。
    url 的 片段长度 为 0。
    若 pos 小于 长度 并且 做 内存_读8 用 url字符串 加 pos 等于 0x23：
        pos 为 pos 加 1。
        url 的 片段 为 做 字符串_复制_N 用 url字符串 加 pos、长度 减 pos。
        url 的 片段长度 为 长度 减 pos。
    完。
    返还 url。
完。

本章 收束。
