《统一码编码》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

UTF8_编码码点 接受 cp、buf、pos：
    若 buf 等于 0：
        返还 0。
    完。
    若 cp 小于 0x80：
        做 字节写 用 buf、pos、cp。
        返还 1。
    否则若 cp 小于 0x800：
        做 字节写 用 buf、pos、0xC0 位或 cp 右移 6。
        做 字节写 用 buf、pos 加 1、0x80 位或 cp 位与 0x3F。
        返还 2。
    否则若 cp 小于 0x10000：
        做 字节写 用 buf、pos、0xE0 位或 cp 右移 12。
        做 字节写 用 buf、pos 加 1、0x80 位或 cp 右移 6 位与 0x3F。
        做 字节写 用 buf、pos 加 2、0x80 位或 cp 位与 0x3F。
        返还 3。
    否则若 cp 小于 0x110000：
        做 字节写 用 buf、pos、0xF0 位或 cp 右移 18。
        做 字节写 用 buf、pos 加 1、0x80 位或 cp 右移 12 位与 0x3F。
        做 字节写 用 buf、pos 加 2、0x80 位或 cp 右移 6 位与 0x3F。
        做 字节写 用 buf、pos 加 3、0x80 位或 cp 位与 0x3F。
        返还 4。
    完。
    做 字节写 用 buf、pos、0xEF。
    做 字节写 用 buf、pos 加 1、0xBF。
    做 字节写 用 buf、pos 加 2、0xBD。
    返还 3。
完。

UTF8_解码码点 接受 buf、pos、len：
    若 buf 等于 0 或者 pos 大于或等于 len：
        返还 0。
    完。
    b0 为 做 字节取 用 buf、pos。
    若 b0 小于 0x80：
        返还 b0。
    完。
    若 b0 位与 0xE0 等于 0xC0 并且 pos 加 1 小于 len：
        b1 为 做 字节取 用 buf、pos 加 1。
        若 b1 位与 0xC0 不等于 0x80：
            返还 0xFFFD。
        完。
        cp 为 (b0 位与 0x1F) 左移 6 位或 b1 位与 0x3F。
        若 cp 小于 0x80：
            返还 0xFFFD。
        完。
        返还 cp。
    完。
    若 b0 位与 0xF0 等于 0xE0 并且 pos 加 2 小于 len：
        b1 为 做 字节取 用 buf、pos 加 1。
        b2 为 做 字节取 用 buf、pos 加 2。
        若 b1 位与 0xC0 不等于 0x80 或者 b2 位与 0xC0 不等于 0x80：
            返还 0xFFFD。
        完。
        cp 为 (b0 位与 0x0F) 左移 12 位或 (b1 位与 0x3F) 左移 6 位或 b2 位与 0x3F。
        若 cp 小于 0x800：
            返还 0xFFFD。
        完。
        若 cp 大于或等于 0xD800 并且 cp 小于或等于 0xDFFF：
            返还 0xFFFD。
        完。
        返还 cp。
    完。
    若 b0 位与 0xF8 等于 0xF0 并且 pos 加 3 小于 len：
        b1 为 做 字节取 用 buf、pos 加 1。
        b2 为 做 字节取 用 buf、pos 加 2。
        b3 为 做 字节取 用 buf、pos 加 3。
        若 b1 位与 0xC0 不等于 0x80 或者 b2 位与 0xC0 不等于 0x80 或者 b3 位与 0xC0 不等于 0x80：
            返还 0xFFFD。
        完。
        cp 为 (b0 位与 0x07) 左移 18 位或 (b1 位与 0x3F) 左移 12 位或 (b2 位与 0x3F) 左移 6 位或 b3 位与 0x3F。
        若 cp 小于 0x10000 或者 cp 大于 0x10FFFF：
            返还 0xFFFD。
        完。
        返还 cp。
    完。
    返还 0xFFFD。
完。

UTF8_字节长度 接受 b0：
    若 b0 小于 0x80：
        返还 1。
    完。
    若 b0 位与 0xE0 等于 0xC0：
        返还 2。
    完。
    若 b0 位与 0xF0 等于 0xE0：
        返还 3。
    完。
    若 b0 位与 0xF8 等于 0xF0：
        返还 4。
    完。
    返还 1。
完。

本章 收束。
