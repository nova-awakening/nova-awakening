《CSS令牌化》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·样式令牌器》。

引用《..·中枢·字符串替换》。

CSS值_关键字 为 1。

CSS值_长度 为 2。

CSS值_百分比 为 3。

CSS值_颜色 为 4。

CSS值_数字 为 5。

CSS值_字符串 为 6。

CSS值_URL 为 7。

CSS值_函数 为 8。

CSSValue 是 一种 事物，含 类型、关键字ID、数值、单位、颜色、字符串值。

单位_px 为 0。

单位_em 为 1。

单位_rem 为 2。

单位_vh 为 3。

单位_vw 为 4。

单位_百分比 为 5。

单位_auto 为 6。

单位_pt 为 7。

单位_cm 为 8。

单位_mm 为 9。

CSS声明 是 一种 事物，含 属性ID、值、重要。

选择器类型_标签 为 1。

选择器类型_class 为 2。

选择器类型_id 为 3。

选择器类型_通用 为 4。

选择器类型_属性 为 5。

选择器类型_伪类 为 6。

选择器类型_伪元素 为 7。

组合器_后代 为 0。

组合器_子 为 1。

组合器_相邻兄弟 为 2。

组合器_一般兄弟 为 3。

组合器_无 为 4。

CSS选择器 是 一种 事物，含 类型、值、值原子、组合器、下一个。

CSS样式规则 是 一种 事物，含 选择器列表、选择器数量、声明列表、声明数量、声明容量、特异度。

CSS样式表 是 一种 事物，含 规则列表、规则数量、规则容量、来源URL、媒体查询、是内联。

CSS令牌izer 是 一种 事物，含 输入、输入长度、位置。

CSS令牌izer_创建 接受 css文本：
    tk 为 做 堆_分配 用 CSS令牌izer。
    tk 的 输入 为 css文本。
    tk 的 输入长度 为 做 字符串_长度 用 css文本。
    tk 的 位置 为 0。
    返还 tk。
完。

CSS令牌izer_下一个令牌 接受 tk、令牌：
    做 CSS令牌izer_跳过空白 用 tk。
    若 tk 的 位置 大于或等于 tk 的 输入长度：
        令牌 的 类型 为 CSS_令牌_EOF。
        返还。
    完。
    c 为 做 CSS令牌izer_消费 用 tk。
    若 c 等于 0x7B：
        令牌 的 类型 为 CSS_令牌_左大括号。
        返还。
    完。
    若 c 等于 0x7D：
        令牌 的 类型 为 CSS_令牌_右大括号。
        返还。
    完。
    若 c 等于 0x28：
        令牌 的 类型 为 CSS_令牌_左圆括号。
        返还。
    完。
    若 c 等于 0x29：
        令牌 的 类型 为 CSS_令牌_右圆括号。
        返还。
    完。
    若 c 等于 0x5B：
        令牌 的 类型 为 CSS_令牌_左方括号。
        返还。
    完。
    若 c 等于 0x5D：
        令牌 的 类型 为 CSS_令牌_右方括号。
        返还。
    完。
    若 c 等于 0x3A：
        令牌 的 类型 为 CSS_令牌_冒号。
        返还。
    完。
    若 c 等于 0x3B：
        令牌 的 类型 为 CSS_令牌_分号。
        返还。
    完。
    若 c 等于 0x2C：
        令牌 的 类型 为 CSS_令牌_逗号。
        返还。
    完。
    若 c 等于 0x23：
        令牌 的 类型 为 CSS_令牌_哈希。
        令牌 的 值 为 tk 的 输入 加 tk 的 位置。
        开始 为 tk 的 位置。
        只要 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_是名称字符 用 做 CSS令牌izer_窥视 用 tk：
            做 CSS令牌izer_消费 用 tk。
        完。
        令牌 的 值长度 为 tk 的 位置 减 开始。
        返还。
    完。
    若 c 等于 0x2E：
        若 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_是数字 用 做 CSS令牌izer_窥视 用 tk：
            tk 的 位置 为 tk 的 位置 减 1。
            做 CSS令牌izer_读数字 用 tk、令牌。
            返还。
        完。
        令牌 的 类型 为 CSS_令牌_分隔符。
        令牌 的 值 为 tk 的 输入 加 tk 的 位置 减 1。
        令牌 的 值长度 为 1。
        返还。
    完。
    若 c 等于 0x40：
        令牌 的 类型 为 CSS_令牌_AT关键字。
        令牌 的 值 为 tk 的 输入 加 tk 的 位置。
        开始 为 tk 的 位置。
        只要 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_是名称字符 用 做 CSS令牌izer_窥视 用 tk：
            做 CSS令牌izer_消费 用 tk。
        完。
        令牌 的 值长度 为 tk 的 位置 减 开始。
        返还。
    完。
    若 c 等于 0x22 或者 c 等于 0x27：
        令牌 的 类型 为 CSS_令牌_字符串。
        令牌 的 值 为 tk 的 输入 加 tk 的 位置。
        开始 为 tk 的 位置。
        引号 为 c。
        只要 tk 的 位置 小于 tk 的 输入长度：
            ch 为 做 CSS令牌izer_消费 用 tk。
            若 ch 等于 引号：
                跳出。
            完。
            若 ch 等于 0x5C 并且 tk 的 位置 小于 tk 的 输入长度：
                做 CSS令牌izer_消费 用 tk。
            完。
        完。
        令牌 的 值长度 为 tk 的 位置 减 开始 减 1。
        返还。
    完。
    若 做 CSS令牌izer_是数字 用 c 或者 c 等于 0x2D 并且 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_是数字 用 做 CSS令牌izer_窥视 用 tk：
        tk 的 位置 为 tk 的 位置 减 1。
        做 CSS令牌izer_读数字 用 tk、令牌。
        返还。
    完。
    若 做 CSS令牌izer_是名称起始 用 c：
        tk 的 位置 为 tk 的 位置 减 1。
        做 CSS令牌izer_读标识符 用 tk、令牌。
        返还。
    完。
    若 c 等于 0x2F 并且 tk 的 位置 小于 tk 的 输入长度 并且 做 CSS令牌izer_窥视 用 tk 等于 0x2A：
        做 CSS令牌izer_消费 用 tk。
        只要 tk 的 位置 小于 tk 的 输入长度 减 1：
            若 做 CSS令牌izer_消费 用 tk 等于 0x2A 并且 做 CSS令牌izer_窥视 用 tk 等于 0x2F：
                做 CSS令牌izer_消费 用 tk。
                跳出。
            完。
        完。
        做 CSS令牌izer_下一个令牌 用 tk、令牌。
        返还。
    完。
    令牌 的 类型 为 CSS_令牌_分隔符。
    令牌 的 值 为 tk 的 输入 加 tk 的 位置 减 1。
    令牌 的 值长度 为 1。
完。

可变 AI_样式令牌甲_样式缓存命中 为 0。

AI_样式令牌甲_选择器复杂度 接受 选择器长度、组合子数：
    若 组合子数 大于 5：
        返还 「高复杂度」。
    完。
    若 组合子数 大于 2：
        返还 「中复杂度」。
    完。
    返还 「低复杂度」。
完。

AI_样式令牌甲_布局缓存 接受 元素哈希：
    AI_样式令牌甲_样式缓存命中 为 AI_样式令牌甲_样式缓存命中 加 1。
    返还 元素哈希。
完。

本章 收束。
