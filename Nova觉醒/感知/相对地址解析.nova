《相对地址解析》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

引用《..·中枢·字符串替换》。

引用《..·外联·URL解析辅助》。

引用《..·中枢·内容可见性》。

引用《.·地址解析器地》。

URL类型_绝对 为 0。

URL类型_协议相对 为 1。

URL类型_绝对路径 为 2。

URL类型_相对路径 为 3。

URL类型_仅查询 为 4。

URL类型_仅片段 为 5。

URL类型_空 为 6。

URL类型_特殊 为 7。

最大路径段数 为 64。

最大URL长度 为 65536。

ParsedURL 是 一种 事物，含 scheme、username、password、host、host长度、host哈希、port、默认端口、path、path长度、query、query长度、fragment、fragment长度、是有效、是特殊、路径。

相对解析统计 是 一种 事物，含 解析总次数、绝对URL直通次数、协议相对次数、绝对路径次数、相对路径次数、仅查询次数、仅片段次数、data_URL次数、blob_URL次数、路径规范化次数、路径段弹出次数、基础URL提取次数、URL合并次数、总解析耗时_纳秒、AI导航调用次数、AI导航命中次数、错误总数、溢出保护次数。

AI导航状态 是 一种 事物，含 历史相对URL比例、历史平均路径深度、历史查询继承率、连续同域导航数、连续跨域导航数、预测下次同域、上次基础host哈希、采样计数器、学习权重。

可变 全局相对解析统计 为 0。

可变 全局导航AI 为 0。

可变 全局相对解析已初始化 为 假。

相对解析_初始化 接受 零参：
    若 全局相对解析已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局相对解析统计 为 做 堆_分配_清零 用 144。
    若 全局相对解析统计 等于 空：
        返还 错误_内存不足。
    完。
    全局导航AI 为 做 堆_分配_清零 用 48。
    若 全局导航AI 不等于 空：
        全局导航AI 的 预测下次同域 为 真。
        全局导航AI 的 学习权重 为 128。
    完。
    全局相对解析已初始化 为 真。
    做 打印 用 「[相对解析] 初始化完成」。
    返还 0。
完。

相对解析_销毁 接受 零参：
    若 全局相对解析统计 不等于 空：
        做 堆_释放 用 全局相对解析统计。
        全局相对解析统计 为 0。
    完。
    若 全局导航AI 不等于 空：
        做 堆_释放 用 全局导航AI。
        全局导航AI 为 0。
    完。
    全局相对解析已初始化 为 假。
    返还 0。
完。

相对解析_重置 接受 零参：
    若 全局相对解析统计 不等于 空：
        做 内存_置零 用 全局相对解析统计、144。
    完。
    返还 0。
完。

URL_检测类型 接受 url字符串：
    若 url字符串 等于 空：
        返还 URL类型_空。
    完。
    长度 为 做 字节长度 用 url字符串。
    若 长度 等于 0：
        返还 URL类型_空。
    完。
    首字符 为 做 字节取 用 url字符串、0。
    若 首字符 等于 35：
        返还 URL类型_仅片段。
    完。
    若 首字符 等于 63：
        返还 URL类型_仅查询。
    完。
    若 长度 大于或等于 2 并且 首字符 等于 47 并且 做 字节取 用 url字符串、1 等于 47：
        返还 URL类型_协议相对。
    完。
    若 首字符 等于 47：
        返还 URL类型_绝对路径。
    完。
    可变 i 为 0。
    只要 i 小于 长度 并且 i 小于 32：
        ch 为 做 字节取 用 url字符串、i。
        若 ch 等于 58：
            若 i 大于 0：
                若 i 等于 4 并且 做 字节取 用 url字符串、0 等于 100：
                    返还 URL类型_特殊。
                完。
                若 i 等于 4 并且 做 字节取 用 url字符串、0 等于 98：
                    返还 URL类型_特殊。
                完。
                返还 URL类型_绝对。
            完。
            止。
        完。
        若 做 URL_是scheme字符 用 ch 等于 假 并且 i 大于 0：
            止。
        完。
        i 为 i 加 1。
    完。
    返还 URL类型_相对路径。
完。

URL_解析相对 接受 相对URL、基础URL：
    若 相对URL 等于 0 或者 基础URL 等于 0：
        返还 0。
    完。
    长度 为 做 字节长度 用 相对URL。
    若 长度 大于 5：
        可变 i 为 0。
        只要 i 小于 长度 并且 做 URL_是scheme字符 用 做 字节取 用 相对URL、i：
            i 为 i 加 1。
        完。
        若 i 大于 0 并且 i 小于 长度 并且 做 字节取 用 相对URL、i 等于 58：
            返还 做 URL_解析 用 相对URL。
        完。
    完。
    结果 为 做 堆_分配 用 ParsedURL。
    结果 的 scheme 为 基础URL 的 scheme。
    结果 的 默认端口 为 基础URL 的 默认端口。
    结果 的 是特殊 为 基础URL 的 是特殊。
    若 长度 大于或等于 2 并且 做 字节取 用 相对URL、0 等于 47 并且 做 字节取 用 相对URL、1 等于 47：
        全URL 为 做 字符串拼接 用 基础URL 的 scheme、「:」。
        全URL 为 做 字符串拼接 用 全URL、相对URL。
        做 堆_释放 用 结果。
        返还 做 URL_解析 用 全URL。
    完。
    结果 的 host 为 基础URL 的 host。
    结果 的 host长度 为 基础URL 的 host长度。
    结果 的 host哈希 为 基础URL 的 host哈希。
    结果 的 port 为 基础URL 的 port。
    若 长度 大于 0 并且 做 字节取 用 相对URL、0 等于 47：
        结果 的 path 为 相对URL。
        结果 的 path长度 为 长度。
    否则：
        基础路径 为 基础URL 的 path。
        可变 最后斜杠 为 0。
        可变 k 为 0。
        只要 k 小于 基础URL 的 path长度：
            若 做 字节取 用 基础路径、k 等于 47：
                最后斜杠 为 k。
            完。
            k 为 k 加 1。
        完。
        前缀 为 做 字符串_截取 用 基础路径、0、最后斜杠 加 1。
        结果 的 path 为 做 字符串拼接 用 前缀、相对URL。
        结果 的 path长度 为 做 字节长度 用 结果 的 path。
    完。
    做 URL_规范化路径 用 结果。
    结果 的 是有效 为 真。
    返还 结果。
完。

URL_规范化路径 接受 url：
    若 url 等于 0 或者 url 的 路径 等于 0：
        返还。
    完。
    path 为 url 的 路径。
    len 为 做 字节长度 用 path。
    若 len 等于 0：
        返还。
    完。
    段 为 做 堆_分配 用 64 乘 8。
    可变 段数 为 0。
    可变 pos 为 0。
    若 做 内存_读8 用 path 等于 0x2F：
        pos 为 1。
    完。
    只要 pos 小于 len：
        可变 end 为 pos。
        只要 end 小于 len 并且 做 内存_读8 用 path 加 end 不等于 0x2F：
            end 为 end 加 1。
        完。
        seg_len 为 end 减 pos。
        若 seg_len 等于 1 并且 做 内存_读8 用 path 加 pos 等于 0x2E：
        否则若 seg_len 等于 2 并且 做 内存_读8 用 path 加 pos 等于 0x2E 并且 做 内存_读8 用 path 加 pos 加 1 等于 0x2E：
            若 段数 大于 0：
                段数 为 段数 减 1。
            完。
        否则若 seg_len 大于 0：
            做 内存_写64 用 段 加 段数 乘 8、pos 位或 seg_len 左移 32。
            段数 为 段数 加 1。
        完。
        pos 为 end 加 1。
    完。
    新路径 为 做 堆_分配 用 len 加 1。
    可变 out 为 0。
    可变 i 为 0。
    只要 i 小于 段数：
        做 内存_写8 用 新路径 加 out、0x2F。
        out 为 out 加 1。
        info 为 做 内存_读64 用 段 加 i 乘 8。
        seg_pos 为 info 位与 0xFFFFFFFF。
        seg_len 为 info 右移 32。
        做 内存_复制 用 新路径 加 out、path 加 seg_pos、seg_len。
        out 为 out 加 seg_len。
        i 为 i 加 1。
    完。
    若 out 等于 0：
        做 内存_写8 用 新路径、0x2F。
        out 为 1。
    完。
    做 内存_写8 用 新路径 加 out、0。
    url 的 路径 为 新路径。
    做 堆_释放 用 段。
    若 全局相对解析统计 不等于 空：
        全局相对解析统计 的 路径规范化次数 为 全局相对解析统计 的 路径规范化次数 加 1。
    完。
完。

URL_合并 接受 基础URL、相对字符串：
    若 基础URL 等于 空 或者 相对字符串 等于 空：
        返还 空。
    完。
    若 做 字节长度 用 相对字符串 等于 0：
        返还 基础URL。
    完。
    若 做 字节长度 用 相对字符串 大于 最大URL长度：
        若 全局相对解析统计 不等于 空：
            全局相对解析统计 的 溢出保护次数 为 全局相对解析统计 的 溢出保护次数 加 1。
            全局相对解析统计 的 错误总数 为 全局相对解析统计 的 错误总数 加 1。
        完。
        返还 空。
    完。
    开始时间 为 做 平台_获取高精度时间。
    类型 为 做 URL_检测类型 用 相对字符串。
    若 全局相对解析统计 不等于 空：
        全局相对解析统计 的 解析总次数 为 全局相对解析统计 的 解析总次数 加 1。
        若 类型 等于 URL类型_绝对：
            全局相对解析统计 的 绝对URL直通次数 为 全局相对解析统计 的 绝对URL直通次数 加 1。
        完。
        若 类型 等于 URL类型_协议相对：
            全局相对解析统计 的 协议相对次数 为 全局相对解析统计 的 协议相对次数 加 1。
        完。
        若 类型 等于 URL类型_绝对路径：
            全局相对解析统计 的 绝对路径次数 为 全局相对解析统计 的 绝对路径次数 加 1。
        完。
        若 类型 等于 URL类型_相对路径：
            全局相对解析统计 的 相对路径次数 为 全局相对解析统计 的 相对路径次数 加 1。
        完。
        若 类型 等于 URL类型_仅查询：
            全局相对解析统计 的 仅查询次数 为 全局相对解析统计 的 仅查询次数 加 1。
        完。
        若 类型 等于 URL类型_仅片段：
            全局相对解析统计 的 仅片段次数 为 全局相对解析统计 的 仅片段次数 加 1。
        完。
        若 类型 等于 URL类型_特殊：
            全局相对解析统计 的 data_URL次数 为 全局相对解析统计 的 data_URL次数 加 1。
        完。
    完。
    结果 为 做 URL_解析相对 用 相对字符串、基础URL。
    若 全局相对解析统计 不等于 空：
        全局相对解析统计 的 URL合并次数 为 全局相对解析统计 的 URL合并次数 加 1。
        耗时 为 做 平台_获取高精度时间 减 开始时间。
        全局相对解析统计 的 总解析耗时_纳秒 为 全局相对解析统计 的 总解析耗时_纳秒 加 耗时。
    完。
    若 全局导航AI 不等于 空 并且 结果 不等于 空：
        做 AI导航_记录 用 基础URL、结果、类型。
    完。
    返还 结果。
完。

URL_提取基础路径 接受 url：
    若 url 等于 空 或者 url 的 path 等于 空：
        返还 「/」。
    完。
    path 为 url 的 path。
    长度 为 做 字节长度 用 path。
    可变 最后斜杠 为 0。
    可变 i 为 长度 减 1。
    只要 i 大于或等于 0：
        若 做 字节取 用 path、i 等于 47：
            最后斜杠 为 i。
            止。
        完。
        i 为 i 减 1。
    完。
    结果 为 做 堆_分配 用 最后斜杠 加 2。
    若 结果 等于 空：
        返还 「/」。
    完。
    可变 j 为 0。
    只要 j 小于或等于 最后斜杠：
        做 内存_写8 用 结果 加 j、做 字节取 用 path、j。
        j 为 j 加 1。
    完。
    做 内存_写8 用 结果 加 j、0。
    若 全局相对解析统计 不等于 空：
        全局相对解析统计 的 基础URL提取次数 为 全局相对解析统计 的 基础URL提取次数 加 1。
    完。
    返还 结果。
完。

URL_路径深度 接受 path：
    若 path 等于 空：
        返还 0。
    完。
    长度 为 做 字节长度 用 path。
    可变 深度 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        若 做 字节取 用 path、i 等于 47：
            深度 为 深度 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 深度。
完。

URL_是data_URL 接受 url字符串：
    若 url字符串 等于 空：
        返还 假。
    完。
    若 做 字节长度 用 url字符串 小于 5：
        返还 假。
    完。
    若 做 字节取 用 url字符串、0 等于 100 并且 做 字节取 用 url字符串、1 等于 97 并且 做 字节取 用 url字符串、2 等于 116 并且 做 字节取 用 url字符串、3 等于 97 并且 做 字节取 用 url字符串、4 等于 58：
        返还 真。
    完。
    返还 假。
完。

URL_是blob_URL 接受 url字符串：
    若 url字符串 等于 空：
        返还 假。
    完。
    若 做 字节长度 用 url字符串 小于 5：
        返还 假。
    完。
    若 做 字节取 用 url字符串、0 等于 98 并且 做 字节取 用 url字符串、1 等于 108 并且 做 字节取 用 url字符串、2 等于 111 并且 做 字节取 用 url字符串、3 等于 98 并且 做 字节取 用 url字符串、4 等于 58：
        返还 真。
    完。
    返还 假。
完。

URL_是javascript_URL 接受 url字符串：
    若 url字符串 等于 空：
        返还 假。
    完。
    若 做 字节长度 用 url字符串 小于 11：
        返还 假。
    完。
    若 做 字节取 用 url字符串、0 等于 106 并且 做 字节取 用 url字符串、4 等于 115 并且 做 字节取 用 url字符串、10 等于 58：
        返还 真。
    完。
    返还 假。
完。

URL_简单哈希 接受 s：
    若 s 等于 空：
        返还 0。
    完。
    长度 为 做 字节长度 用 s。
    可变 哈希 为 5381。
    可变 i 为 0。
    只要 i 小于 长度：
        哈希 为 (哈希 左移 5) 加 哈希 加 做 字节取 用 s、i。
        i 为 i 加 1。
    完。
    返还 哈希。
完。

AI导航_记录 接受 基础URL、结果URL、URL类型：
    若 全局导航AI 等于 空：
        返还。
    完。
    AI 为 全局导航AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    可变 是相对 为 假。
    若 URL类型 等于 URL类型_相对路径 或者 URL类型 等于 URL类型_绝对路径 或者 URL类型 等于 URL类型_仅查询 或者 URL类型 等于 URL类型_仅片段：
        是相对 为 真。
    完。
    若 是相对 等于 真：
        AI 的 历史相对URL比例 为 (AI 的 历史相对URL比例 乘 15 加 1024) 除以 16。
    否则：
        AI 的 历史相对URL比例 为 AI 的 历史相对URL比例 乘 15 除以 16。
    完。
    若 结果URL 不等于 空 并且 结果URL 的 path 不等于 空：
        深度 为 做 URL_路径深度 用 结果URL 的 path。
        AI 的 历史平均路径深度 为 (AI 的 历史平均路径深度 乘 7 加 深度 乘 1024) 除以 8。
    完。
    可变 同域 为 假。
    若 结果URL 不等于 空 并且 结果URL 的 host 不等于 空：
        新哈希 为 做 URL_简单哈希 用 结果URL 的 host。
        若 新哈希 等于 AI 的 上次基础host哈希 并且 AI 的 上次基础host哈希 不等于 0：
            同域 为 真。
        完。
        AI 的 上次基础host哈希 为 新哈希。
    完。
    若 同域 等于 真：
        AI 的 连续同域导航数 为 AI 的 连续同域导航数 加 1。
        AI 的 连续跨域导航数 为 0。
    否则：
        AI 的 连续跨域导航数 为 AI 的 连续跨域导航数 加 1。
        AI 的 连续同域导航数 为 0。
    完。
    若 AI 的 采样计数器 小于 16：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 全局相对解析统计 不等于 空：
        全局相对解析统计 的 AI导航调用次数 为 全局相对解析统计 的 AI导航调用次数 加 1。
    完。
    若 AI 的 连续同域导航数 大于 5：
        AI 的 预测下次同域 为 真。
    否则若 AI 的 连续跨域导航数 大于 3：
        AI 的 预测下次同域 为 假。
    完。
    若 AI 的 预测下次同域 等于 真 并且 同域 等于 真：
        若 全局相对解析统计 不等于 空：
            全局相对解析统计 的 AI导航命中次数 为 全局相对解析统计 的 AI导航命中次数 加 1。
        完。
    完。
完。

相对解析_打印统计 接受 零参：
    若 全局相对解析统计 等于 空：
        做 打印 用 「[相对解析] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   相对URL解析 · 统计报告            │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 解析总次数:       」、做 转字符串 用 全局相对解析统计 的 解析总次数。
    做 打印 用 做 字符串拼接 用 「│ 绝对URL直通:      」、做 转字符串 用 全局相对解析统计 的 绝对URL直通次数。
    做 打印 用 做 字符串拼接 用 「│ 协议相对:         」、做 转字符串 用 全局相对解析统计 的 协议相对次数。
    做 打印 用 做 字符串拼接 用 「│ 绝对路径:         」、做 转字符串 用 全局相对解析统计 的 绝对路径次数。
    做 打印 用 做 字符串拼接 用 「│ 相对路径:         」、做 转字符串 用 全局相对解析统计 的 相对路径次数。
    做 打印 用 做 字符串拼接 用 「│ 仅查询:           」、做 转字符串 用 全局相对解析统计 的 仅查询次数。
    做 打印 用 做 字符串拼接 用 「│ 仅片段:           」、做 转字符串 用 全局相对解析统计 的 仅片段次数。
    做 打印 用 做 字符串拼接 用 「│ data/blob URL:    」、做 转字符串 用 全局相对解析统计 的 data_URL次数。
    做 打印 用 做 字符串拼接 用 「│ 路径规范化:       」、做 转字符串 用 全局相对解析统计 的 路径规范化次数。
    做 打印 用 做 字符串拼接 用 「│ 基础URL提取:      」、做 转字符串 用 全局相对解析统计 的 基础URL提取次数。
    做 打印 用 做 字符串拼接 用 「│ URL合并:          」、做 转字符串 用 全局相对解析统计 的 URL合并次数。
    做 打印 用 做 字符串拼接 用 「│ 总耗时(μs):       」、做 转字符串 用 全局相对解析统计 的 总解析耗时_纳秒 除以 1000。
    做 打印 用 做 字符串拼接 用 「│ AI导航调用:       」、做 转字符串 用 全局相对解析统计 的 AI导航调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI导航命中:       」、做 转字符串 用 全局相对解析统计 的 AI导航命中次数。
    做 打印 用 做 字符串拼接 用 「│ 错误:             」、做 转字符串 用 全局相对解析统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

相对解析_导出诊断 接受 零参：
    若 全局导航AI 等于 空：
        做 打印 用 「[导航AI] 未初始化」。
        返还。
    完。
    AI 为 全局导航AI。
    做 打印 用 「[导航AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史相对URL比例(Q10): 」、做 转字符串 用 AI 的 历史相对URL比例。
    做 打印 用 做 字符串拼接 用 「  历史平均路径深度(Q10): 」、做 转字符串 用 AI 的 历史平均路径深度。
    做 打印 用 做 字符串拼接 用 「  连续同域导航: 」、做 转字符串 用 AI 的 连续同域导航数。
    做 打印 用 做 字符串拼接 用 「  连续跨域导航: 」、做 转字符串 用 AI 的 连续跨域导航数。
    做 打印 用 做 字符串拼接 用 「  预测同域: 」、做 转字符串 用 AI 的 预测下次同域。
完。

相对解析_获取统计 接受 零参：
    返还 全局相对解析统计。
完。

相对解析_获取AI状态 接受 零参：
    返还 全局导航AI。
完。

相对解析_预测同域 接受 零参：
    若 全局导航AI 等于 空：
        返还 真。
    完。
    返还 全局导航AI 的 预测下次同域。
完。

相对解析_是否已初始化 接受 零参：
    返还 全局相对解析已初始化。
完。

本章 收束。
