《CSS滤镜》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《..·中枢·内存初始化》。

滤镜_模糊 为 0。

滤镜_亮度 为 1。

滤镜_对比 为 2。

滤镜_饱和 为 3。

滤镜_色调旋转 为 4。

滤镜_反色 为 5。

滤镜_灰度 为 6。

滤镜_褐色 为 7。

滤镜_阴影 为 8。

混合_正常 为 0。

混合_正片叠底 为 1。

混合_滤色 为 2。

混合_叠加 为 3。

混合_变暗 为 4。

混合_变亮 为 5。

最大滤镜链 为 8。

基数 为 1024。

滤镜项 是 一种 事物，含 类型、参数值。

可变 应用总数 为 0。

滤镜_应用亮度 接受 像素、系数：
    红 为 像素 右移 16 位与 0xFF。
    绿 为 像素 右移 8 位与 0xFF。
    蓝 为 像素 位与 0xFF。
    透明 为 像素 右移 24 位与 0xFF。
    红 为 红 乘 系数 除以 基数。
    绿 为 绿 乘 系数 除以 基数。
    蓝 为 蓝 乘 系数 除以 基数。
    若 红 大于 255：
        红 为 255。
    完。
    若 绿 大于 255：
        绿 为 255。
    完。
    若 蓝 大于 255：
        蓝 为 255。
    完。
    返还 透明 左移 24 位或 红 左移 16 位或 绿 左移 8 位或 蓝。
完。

滤镜_应用对比 接受 像素、系数：
    红 为 像素 右移 16 位与 0xFF。
    绿 为 像素 右移 8 位与 0xFF。
    蓝 为 像素 位与 0xFF。
    透明 为 像素 右移 24 位与 0xFF。
    红 为 (红 减 128) 乘 系数 除以 基数 加 128。
    绿 为 (绿 减 128) 乘 系数 除以 基数 加 128。
    蓝 为 (蓝 减 128) 乘 系数 除以 基数 加 128。
    若 红 小于 0：
        红 为 0。
    完。
    若 红 大于 255：
        红 为 255。
    完。
    若 绿 小于 0：
        绿 为 0。
    完。
    若 绿 大于 255：
        绿 为 255。
    完。
    若 蓝 小于 0：
        蓝 为 0。
    完。
    若 蓝 大于 255：
        蓝 为 255。
    完。
    返还 透明 左移 24 位或 红 左移 16 位或 绿 左移 8 位或 蓝。
完。

滤镜_应用饱和 接受 像素、系数：
    红 为 像素 右移 16 位与 0xFF。
    绿 为 像素 右移 8 位与 0xFF。
    蓝 为 像素 位与 0xFF。
    透明 为 像素 右移 24 位与 0xFF。
    灰 为 红 乘 77 加 绿 乘 150 加 蓝 乘 29 右移 8。
    红 为 灰 加 (红 减 灰) 乘 系数 除以 基数。
    绿 为 灰 加 (绿 减 灰) 乘 系数 除以 基数。
    蓝 为 灰 加 (蓝 减 灰) 乘 系数 除以 基数。
    若 红 小于 0：
        红 为 0。
    完。
    若 红 大于 255：
        红 为 255。
    完。
    若 绿 小于 0：
        绿 为 0。
    完。
    若 绿 大于 255：
        绿 为 255。
    完。
    若 蓝 小于 0：
        蓝 为 0。
    完。
    若 蓝 大于 255：
        蓝 为 255。
    完。
    返还 透明 左移 24 位或 红 左移 16 位或 绿 左移 8 位或 蓝。
完。

滤镜_应用灰度 接受 像素、系数：
    红 为 像素 右移 16 位与 0xFF。
    绿 为 像素 右移 8 位与 0xFF。
    蓝 为 像素 位与 0xFF。
    透明 为 像素 右移 24 位与 0xFF。
    灰 为 红 乘 77 加 绿 乘 150 加 蓝 乘 29 右移 8。
    红 为 红 加 (灰 减 红) 乘 系数 除以 基数。
    绿 为 绿 加 (灰 减 绿) 乘 系数 除以 基数。
    蓝 为 蓝 加 (灰 减 蓝) 乘 系数 除以 基数。
    返还 透明 左移 24 位或 红 左移 16 位或 绿 左移 8 位或 蓝。
完。

滤镜_应用反色 接受 像素、系数：
    红 为 像素 右移 16 位与 0xFF。
    绿 为 像素 右移 8 位与 0xFF。
    蓝 为 像素 位与 0xFF。
    透明 为 像素 右移 24 位与 0xFF。
    红 为 红 加 (255 减 红 乘 2) 乘 系数 除以 基数。
    绿 为 绿 加 (255 减 绿 乘 2) 乘 系数 除以 基数。
    蓝 为 蓝 加 (255 减 蓝 乘 2) 乘 系数 除以 基数。
    若 红 小于 0：
        红 为 0。
    完。
    若 红 大于 255：
        红 为 255。
    完。
    若 绿 小于 0：
        绿 为 0。
    完。
    若 绿 大于 255：
        绿 为 255。
    完。
    若 蓝 小于 0：
        蓝 为 0。
    完。
    若 蓝 大于 255：
        蓝 为 255。
    完。
    返还 透明 左移 24 位或 红 左移 16 位或 绿 左移 8 位或 蓝。
完。

滤镜_应用链 接受 像素、滤镜链、链长：
    应用总数 为 应用总数 加 1。
    可变 当前像素 为 像素。
    可变 序号 为 0。
    只要 序号 小于 链长 并且 序号 小于 最大滤镜链：
        类型 为 做 内存_读32 用 滤镜链 加 序号 乘 8。
        参数 为 做 内存_读32 用 滤镜链 加 序号 乘 8 加 4。
        若 类型 等于 滤镜_亮度：
            当前像素 为 做 滤镜_应用亮度 用 当前像素、参数。
        否则若 类型 等于 滤镜_对比：
            当前像素 为 做 滤镜_应用对比 用 当前像素、参数。
        否则若 类型 等于 滤镜_饱和：
            当前像素 为 做 滤镜_应用饱和 用 当前像素、参数。
        否则若 类型 等于 滤镜_灰度：
            当前像素 为 做 滤镜_应用灰度 用 当前像素、参数。
        否则若 类型 等于 滤镜_反色：
            当前像素 为 做 滤镜_应用反色 用 当前像素、参数。
        完。
        序号 为 序号 加 1。
    完。
    返还 当前像素。
完。

混合_合成 接受 底层、顶层、模式：
    底红 为 底层 右移 16 位与 0xFF。
    底绿 为 底层 右移 8 位与 0xFF。
    底蓝 为 底层 位与 0xFF。
    顶红 为 顶层 右移 16 位与 0xFF。
    顶绿 为 顶层 右移 8 位与 0xFF。
    顶蓝 为 顶层 位与 0xFF。
    可变 结红 为 顶红。
    可变 结绿 为 顶绿。
    可变 结蓝 为 顶蓝。
    若 模式 等于 混合_正片叠底：
        结红 为 底红 乘 顶红 除以 255。
        结绿 为 底绿 乘 顶绿 除以 255。
        结蓝 为 底蓝 乘 顶蓝 除以 255。
    否则若 模式 等于 混合_滤色：
        结红 为 255 减 (255 减 底红) 乘 (255 减 顶红) 除以 255。
        结绿 为 255 减 (255 减 底绿) 乘 (255 减 顶绿) 除以 255。
        结蓝 为 255 减 (255 减 底蓝) 乘 (255 减 顶蓝) 除以 255。
    否则若 模式 等于 混合_变暗：
        若 底红 小于 顶红：
            结红 为 底红。
        否则：
            结红 为 顶红。
        完。
        若 底绿 小于 顶绿：
            结绿 为 底绿。
        否则：
            结绿 为 顶绿。
        完。
        若 底蓝 小于 顶蓝：
            结蓝 为 底蓝。
        否则：
            结蓝 为 顶蓝。
        完。
    否则若 模式 等于 混合_变亮：
        若 底红 大于 顶红：
            结红 为 底红。
        否则：
            结红 为 顶红。
        完。
        若 底绿 大于 顶绿：
            结绿 为 底绿。
        否则：
            结绿 为 顶绿。
        完。
        若 底蓝 大于 顶蓝：
            结蓝 为 底蓝。
        否则：
            结蓝 为 顶蓝。
        完。
    完。
    返还 255 左移 24 位或 结红 左移 16 位或 结绿 左移 8 位或 结蓝。
完。

滤镜_应用到区域 接受 数据、宽、高、滤镜链、链长：
    像素数 为 宽 乘 高。
    可变 序号 为 0。
    只要 序号 小于 像素数：
        像素 为 做 内存_读32 用 数据 加 序号 乘 4。
        结果 为 做 滤镜_应用链 用 像素、滤镜链、链长。
        做 内存_写32 用 数据 加 序号 乘 4、结果。
        序号 为 序号 加 1。
    完。
完。

滤镜混合_完整统计 接受 零参：
    返还 一组：应用总数。
完。

本章 收束。
