《精确三角函数》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·整数转字符串》。

FP 为 10000。

FP_HALF 为 5000。

FP_TWO 为 20000。

FP_PI 为 31416。

FP_TWO_PI 为 62832。

FP_HALF_PI 为 15708。

FP_QRTR_PI 为 7854。

FP_E 为 27183。

FP_LN2 为 6931。

FP_SQRT2 为 14142。

FP_INV_SQRT2 为 7071。

CORDIC_级数 为 16。

CORDIC_增益 为 6073。

Q10 为 1024。

Q_ONE 为 1024。

定点_tan 接受 x：
    c 为 做 定点_cos_精确 用 x。
    若 c 等于 0：
        返还 100000000。
    完。
    返还 做 定点_sin_精确 用 x 乘 FP 除以 c。
完。

定点_cot 接受 x：
    s 为 做 定点_sin_精确 用 x。
    若 s 等于 0：
        返还 100000000。
    完。
    返还 做 定点_cos_精确 用 x 乘 FP 除以 s。
完。

定点_sec 接受 x：
    c 为 做 定点_cos_精确 用 x。
    若 c 等于 0：
        返还 100000000。
    完。
    返还 FP 乘 FP 除以 c。
完。

定点_csc 接受 x：
    s 为 做 定点_sin_精确 用 x。
    若 s 等于 0：
        返还 100000000。
    完。
    返还 FP 乘 FP 除以 s。
完。

定点_atan2 接受 y、x：
    若 x 等于 0 并且 y 等于 0：
        返还 0。
    完。
    若 x 等于 0：
        若 y 大于 0：
            返还 FP_HALF_PI。
        完。
        返还 0 减 FP_HALF_PI。
    完。
    可变 r 为 y 乘 FP 除以 x。
    若 r 大于 FP 或者 r 小于 负 FP：
        inv 为 x 乘 FP 除以 y。
        i2 为 inv 乘 inv 除以 FP。
        i3 为 i2 乘 inv 除以 FP。
        i5 为 i3 乘 i2 除以 FP。
        i7 为 i5 乘 i2 除以 FP。
        at 为 inv 减 i3 除以 3 加 i5 除以 5 减 i7 除以 7。
        若 y 大于 0：
            返还 FP_HALF_PI 减 at。
        完。
        返还 0 减 FP_HALF_PI 减 at。
    完。
    可变 结果 为 0。
    若 r 大于 8000：
        u 为 (r 减 FP) 乘 FP 除以 (r 加 FP)。
        u2 为 u 乘 u 除以 FP。
        u3 为 u2 乘 u 除以 FP。
        结果 为 FP_QRTR_PI 加 u 减 u3 除以 3。
    完。
    做 否则如果 用 r 小于 负 8000。
    块：
        u 为 (r 加 FP) 乘 FP 除以 (0 减 r 加 FP)。
        u2 为 u 乘 u 除以 FP。
        u3 为 u2 乘 u 除以 FP。
        结果 为 0 减 FP_QRTR_PI 加 u 减 u3 除以 3。
    完。
    否则。
    块：
        r2 为 r 乘 r 除以 FP。
        r3 为 r2 乘 r 除以 FP。
        r5 为 r3 乘 r2 除以 FP。
        r7 为 r5 乘 r2 除以 FP。
        结果 为 r 减 r3 除以 3 加 r5 除以 5 减 r7 除以 7。
    完。
    若 x 小于 0：
        若 y 大于或等于 0：
            结果 为 结果 加 FP_PI。
        否则：
            结果 为 结果 减 FP_PI。
        完。
    完。
    返还 结果。
完。

定点_asin 接受 x：
    若 x 大于或等于 FP：
        返还 FP_HALF_PI。
    完。
    若 x 小于或等于 负 FP：
        返还 0 减 FP_HALF_PI。
    完。
    x2 为 x 乘 x 除以 FP。
    余 为 做 定点_sqrt 用 FP 减 x2。
    返还 做 定点_atan2 用 x、余。
完。

定点_acos 接受 x：
    返还 FP_HALF_PI 减 做 定点_asin 用 x。
完。

定点_sqrt 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 g 为 x。
    若 g 大于 FP：
        g 为 g 除以 2。
    完。
    可变 i 为 0。
    只要 i 小于 20：
        若 g 小于或等于 0：
            g 为 1。
        完。
        新 为 (g 加 x 乘 FP 除以 g) 除以 2。
        若 做 绝对值 用 新 减 g 小于或等于 1：
            返还 新。
        完。
        g 为 新。
        i 为 i 加 1。
    完。
    返还 g。
完。

nova_ln_v2 接受 x：
    若 x 小于或等于 0：
        返还 负 10 乘 Q_ONE。
    完。
    LN2_Q 为 709。
    可变 k 为 0。
    可变 m 为 x。
    只要 m 大于或等于 2 乘 Q_ONE：
        m 为 m 除以 2。
        k 为 k 加 1。
    完。
    只要 m 小于 Q_ONE：
        m 为 m 乘 2。
        k 为 k 减 1。
    完。
    u 为 m 减 Q_ONE。
    two_plus_u 为 2 乘 Q_ONE 加 u。
    pade 为 做 Q除 用 2 乘 u、two_plus_u。
    u2 为 做 Q乘 用 u、u。
    denom2 为 3 乘 做 Q乘 用 two_plus_u、two_plus_u 除以 Q_ONE。
    correction 为 做 Q乘 用 pade、做 Q除 用 u2、denom2。
    返还 pade 加 correction 加 k 乘 LN2_Q。
完。

定点_ln 接受 x：
    若 x 小于或等于 0：
        返还 负 100000000。
    完。
    可变 k 为 0。
    可变 m 为 x。
    只要 m 大于 FP_TWO：
        m 为 m 除以 2。
        k 为 k 加 1。
    完。
    只要 m 小于 FP_HALF：
        m 为 m 乘 2。
        k 为 k 减 1。
    完。
    u 为 m 减 FP。
    u2 为 u 乘 u 除以 FP。
    u3 为 u2 乘 u 除以 FP。
    u4 为 u3 乘 u 除以 FP。
    结果 为 u 减 u2 除以 2 加 u3 除以 3 减 u4 除以 4。
    返还 结果 加 k 乘 FP_LN2。
完。

定点_pow 接受 base、exp：
    若 base 小于或等于 0：
        返还 0。
    完。
    返还 做 定点_exp 用 exp 乘 做 定点_ln 用 base 除以 FP。
完。

可变 CORDIC_ATAN_表 为 一组：7854、4636、2450、1244、624、312、156、78、39、20、10、5、2、1、1、0。

CORDIC_旋转 接受 x入、y入、目标角度：
    可变 x 为 x入。
    可变 y 为 y入。
    可变 z 为 目标角度。
    可变 i 为 0。
    只要 i 小于 CORDIC_级数：
        dx 为 y 右移 i。
        dy 为 x 右移 i。
        若 z 大于或等于 0：
            x 为 x 减 dx。
            y 为 y 加 dy。
            z 为 z 减 CORDIC_ATAN_表 的 第 i 项。
        否则：
            x 为 x 加 dx。
            y 为 y 减 dy。
            z 为 z 加 CORDIC_ATAN_表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    x 为 x 乘 CORDIC_增益 除以 FP。
    y 为 y 乘 CORDIC_增益 除以 FP。
    返还 一组：x、y。
完。

CORDIC_cos_sin 接受 角度：
    返还 做 CORDIC_旋转 用 FP、0、角度。
完。

矩阵_单位 接受 零参：
    返还 一组：FP、0、0、0、FP、0、0、0、FP。
完。

矩阵_平移 接受 tx、ty：
    返还 一组：FP、0、tx、0、FP、ty、0、0、FP。
完。

矩阵_缩放 接受 sx、sy：
    返还 一组：sx、0、0、0、sy、0、0、0、FP。
完。

矩阵_旋转 接受 角度：
    c 为 做 定点_cos_精确 用 角度。
    s 为 做 定点_sin_精确 用 角度。
    返还 一组：c、0 减 s、0、s、c、0、0、0、FP。
完。

矩阵_变换点 接受 m、x、y：
    rx 为 m 的 第 0 项 乘 x 除以 FP 加 m 的 第 1 项 乘 y 除以 FP 加 m 的 第 2 项。
    ry 为 m 的 第 3 项 乘 x 除以 FP 加 m 的 第 4 项 乘 y 除以 FP 加 m 的 第 5 项。
    返还 一组：rx、ry。
完。

贝塞尔2_采样 接受 p0x、p0y、p1x、p1y、p2x、p2y、t：
    u 为 FP 减 t。
    uu 为 u 乘 u 除以 FP。
    tt 为 t 乘 t 除以 FP。
    ut2 为 2 乘 u 乘 t 除以 FP。
    x 为 uu 乘 p0x 除以 FP 加 ut2 乘 p1x 除以 FP 加 tt 乘 p2x 除以 FP。
    y 为 uu 乘 p0y 除以 FP 加 ut2 乘 p1y 除以 FP 加 tt 乘 p2y 除以 FP。
    返还 一组：x、y。
完。

贝塞尔3_采样 接受 p0x、p0y、p1x、p1y、p2x、p2y、p3x、p3y、t：
    u 为 FP 减 t。
    uu 为 u 乘 u 除以 FP。
    uuu 为 uu 乘 u 除以 FP。
    tt 为 t 乘 t 除以 FP。
    ttt 为 tt 乘 t 除以 FP。
    c1 为 3 乘 t 乘 uu 除以 FP。
    c2 为 3 乘 tt 乘 u 除以 FP。
    x 为 uuu 乘 p0x 除以 FP 加 c1 乘 p1x 除以 FP 加 c2 乘 p2x 除以 FP 加 ttt 乘 p3x 除以 FP。
    y 为 uuu 乘 p0y 除以 FP 加 c1 乘 p1y 除以 FP 加 c2 乘 p2y 除以 FP 加 ttt 乘 p3y 除以 FP。
    返还 一组：x、y。
完。

贝塞尔3_长度 接受 点列表、采样数：
    若 采样数 小于或等于 0：
        采样数 为 32。
    完。
    可变 总长 为 0。
    可变 前_x 为 点列表 的 第 0 项。
    可变 前_y 为 点列表 的 第 1 项。
    可变 i 为 1。
    只要 i 小于或等于 采样数：
        t 为 i 乘 FP 除以 采样数。
        p 为 做 贝塞尔3_采样 用 点列表 的 第 0 项、点列表 的 第 1 项、点列表 的 第 2 项、点列表 的 第 3 项、点列表 的 第 4 项、点列表 的 第 5 项、点列表 的 第 6 项、点列表 的 第 7 项、t。
        dx 为 p 的 第 0 项 减 前_x。
        dy 为 p 的 第 1 项 减 前_y。
        总长 为 总长 加 做 定点_sqrt 用 dx 乘 dx 除以 FP 加 dy 乘 dy 除以 FP。
        前_x 为 p 的 第 0 项。
        前_y 为 p 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 总长。
完。

缓动_线性 接受 t：
    返还 t。
完。

缓动_渐入 接受 t：
    返还 t 乘 t 除以 FP。
完。

缓动_渐出 接受 t：
    u 为 FP 减 t。
    返还 FP 减 u 乘 u 除以 FP。
完。

缓动_渐入渐出 接受 t：
    若 t 小于 FP_HALF：
        返还 2 乘 t 乘 t 除以 FP。
    完。
    u 为 FP 减 t。
    返还 FP 减 2 乘 u 乘 u 除以 FP。
完。

缓动_贝塞尔 接受 t、x1、y1、x2、y2：
    p 为 做 贝塞尔3_采样 用 0、0、x1、y1、x2、y2、FP、FP、t。
    返还 p 的 第 1 项。
完。

缓动_弹性 接受 t：
    p 为 t 乘 FP_PI 乘 3 除以 FP。
    衰减 为 做 定点_exp 用 0 减 t 乘 4。
    返还 FP 减 衰减 乘 做 定点_cos_精确 用 p 除以 FP。
完。

智能_CSSTransform_AI样式意图 接受 属性ID、值：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    特征 为 做 Tensor_创建 用 1、4。
    做 Tensor_写 用 特征、0、0、属性ID 乘 Q10 除以 256。
    做 Tensor_写 用 特征、0、1、值。
    做 Tensor_写 用 特征、0、2、0。
    做 Tensor_写 用 特征、0、3、Q_ONE。
    结果 为 做 AI总线_分类 用 bus、特征、5。
    做 Tensor_释放 用 特征。
    返还 结果。
完。

智能_CSSTransform_AI影响分析 接受 属性ID：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 2。
    完。
    特征 为 做 Tensor_创建 用 1、2。
    做 Tensor_写 用 特征、0、0、属性ID 乘 Q10 除以 256。
    做 Tensor_写 用 特征、0、1、Q_ONE。
    结果 为 做 AI总线_分类 用 bus、特征、5。
    做 Tensor_释放 用 特征。
    返还 结果。
完。

定点_最大 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

定点_最小 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

定点_钳位 接受 x、lo、hi：
    若 x 小于 lo：
        返还 lo。
    完。
    若 x 大于 hi：
        返还 hi。
    完。
    返还 x。
完。

定点_插值 接受 a、b、t：
    返还 a 加 (b 减 a) 乘 t 除以 FP。
完。

可变 NCTF_计算次数 为 0。

可变 NCTF_AI推理次数 为 0。

可变 NCTF_插值次数 为 0。

可变 NCTF_三角次数 为 0。

NCTF_重置统计 接受 零参：
    NCTF_计算次数 为 0。
    NCTF_AI推理次数 为 0。
    NCTF_插值次数 为 0。
    NCTF_三角次数 为 0。
完。

NCTF_打印统计 接受 零参：
    做 打印行 用 「=== CSS变换函数统计 ===」。
    做 打印行 用 「计算次数: 」 加 做 整数转字符串 用 NCTF_计算次数。
    做 打印行 用 「AI推理: 」 加 做 整数转字符串 用 NCTF_AI推理次数。
    做 打印行 用 「插值: 」 加 做 整数转字符串 用 NCTF_插值次数。
    做 打印行 用 「三角函数: 」 加 做 整数转字符串 用 NCTF_三角次数。
完。

定点_tanh 接受 角度：
    x2 为 角度 乘 角度 除以 FP。
    x3 为 x2 乘 角度 除以 FP。
    结果 为 角度 减 x3 除以 3。
    若 结果 大于 FP：
        返还 FP。
    完。
    若 结果 小于 0 减 FP：
        返还 0 减 FP。
    完。
    NCTF_三角次数 为 NCTF_三角次数 加 1。
    返还 结果。
完。

定点_smoothstep 接受 a、b、t：
    tt 为 t 乘 t 除以 FP。
    factor 为 tt 乘 (3 乘 FP 减 2 乘 t) 除以 FP。
    NCTF_插值次数 为 NCTF_插值次数 加 1。
    返还 a 加 (b 减 a) 乘 factor 除以 FP。
完。

定点_sinc 接受 x：
    若 x 等于 0：
        返还 FP。
    完。
    s 为 做 定点_sin_精确 用 x。
    NCTF_三角次数 为 NCTF_三角次数 加 1。
    返还 s 乘 FP 除以 x。
完。

度转弧度 接受 度：
    返还 度 乘 FP_PI 除以 (180 乘 FP) 乘 FP。
完。

弧度转度 接受 弧度：
    返还 弧度 乘 180 乘 FP 除以 FP_PI。
完。

定点_幂 接受 底、指数：
    若 指数 等于 0：
        返还 FP。
    完。
    可变 结果 为 FP。
    可变 i 为 0。
    只要 i 小于 指数：
        结果 为 结果 乘 底 除以 FP。
        i 为 i 加 1。
    完。
    NCTF_计算次数 为 NCTF_计算次数 加 1。
    返还 结果。
完。

定点_sinh 接受 角度：
    ep 为 做 定点_exp 用 角度。
    en 为 做 定点_exp 用 0 减 角度。
    NCTF_三角次数 为 NCTF_三角次数 加 1。
    返还 (ep 减 en) 除以 2。
完。

定点_cosh 接受 角度：
    ep 为 做 定点_exp 用 角度。
    en 为 做 定点_exp 用 0 减 角度。
    NCTF_三角次数 为 NCTF_三角次数 加 1。
    返还 (ep 加 en) 除以 2。
完。

谐振缓动 接受 进度、频率、衰减率：
    振荡 为 做 定点_sin_精确 用 进度 乘 频率 除以 FP。
    衰减 为 做 定点_exp 用 0 减 进度 乘 衰减率 除以 FP。
    返还 FP 减 衰减 乘 (FP 减 进度 加 振荡) 除以 FP。
完。

谐振缓动_弹簧 接受 进度、强度：
    若 进度 大于或等于 FP：
        返还 FP。
    完。
    s 为 做 定点_sin_精确 用 进度 乘 FP_PI 乘 2 除以 FP。
    d 为 做 定点_exp 用 0 减 进度 乘 强度 除以 FP。
    返还 进度 加 s 乘 d 除以 FP。
完。

精确三角函数_完整统计 接受 零参：
    返还 一组：「8阶Taylor三角」、4、「反三角函数」、4、「CORDIC」、2、「exp/ln/sqrt」、3、「贝塞尔曲线」、3、「缓动函数」、5、「AI样式意图」、2、「双曲函数」、3、「计算次数」、NCTF_计算次数、「三角次数」、NCTF_三角次数、「谐振缓动」、2。
完。

本章 收束。
