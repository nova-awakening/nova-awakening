《CSS样式表对象》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·内存初始化》。

影响_无 为 0。

影响_重绘 为 1。

影响_重排 为 2。

影响_重组 为 3。

最大样式表 为 32。

最大规则 为 512。

缓存桶数 为 128。

样式规则 是 一种 事物，含 选择器哈希、属性数、属性表、特异度、来源、行号。

样式表 是 一种 事物，含 标识、规则表、规则数、已启用、媒体哈希。

可变 样式表列表 为 一组：（空）。

可变 样式表数 为 0。

可变 计算缓存 为 0。

可变 缓存命中 为 0。

可变 缓存未中 为 0。

可变 失效次数 为 0。

可变 规则匹配数 为 0。

样式对象_初始化 接受 零参：
    样式表列表 为 一组：（空）。
    样式表数 为 0。
    计算缓存 为 做 堆_分配 用 缓存桶数 乘 8。
    可变 序号 为 0。
    只要 序号 小于 缓存桶数：
        做 内存_写64 用 计算缓存 加 序号 乘 8、0。
        序号 为 序号 加 1。
    完。
完。

样式对象_添加表 接受 媒体哈希：
    若 样式表数 大于或等于 最大样式表：
        返还 负 1。
    完。
    表 为 新建 样式表。
    表 的 标识 为 样式表数。
    表 的 规则表 为 一组：（空）。
    表 的 规则数 为 0。
    表 的 已启用 为 真。
    表 的 媒体哈希 为 媒体哈希。
    做 追加 用 样式表列表、表。
    索引 为 样式表数。
    样式表数 为 样式表数 加 1。
    返还 索引。
完。

样式对象_添加规则 接受 表索引、选择器哈希、属性表、属性数、特异度：
    若 表索引 大于或等于 样式表数：
        返还 负 1。
    完。
    表 为 样式表列表 的 第 表索引 项。
    若 表 的 规则数 大于或等于 最大规则：
        返还 负 1。
    完。
    规则 为 新建 样式规则。
    规则 的 选择器哈希 为 选择器哈希。
    规则 的 属性数 为 属性数。
    规则 的 属性表 为 属性表。
    规则 的 特异度 为 特异度。
    规则 的 来源 为 表索引。
    规则 的 行号 为 表 的 规则数。
    做 追加 用 表 的 规则表、规则。
    表 的 规则数 为 表 的 规则数 加 1。
    做 样式对象_失效缓存。
    返还 表 的 规则数 减 1。
完。

样式对象_删除规则 接受 表索引、规则索引：
    若 表索引 大于或等于 样式表数：
        返还。
    完。
    表 为 样式表列表 的 第 表索引 项。
    若 规则索引 大于或等于 表 的 规则数：
        返还。
    完。
    表 的 规则表 的 第 规则索引 项 为 表 的 规则表 的 第 表 的 规则数 减 1 项。
    表 的 规则数 为 表 的 规则数 减 1。
    做 样式对象_失效缓存。
完。

样式对象_启用表 接受 表索引、启用：
    若 表索引 大于或等于 样式表数：
        返还。
    完。
    样式表列表 的 第 表索引 项 的 已启用 为 启用。
    做 样式对象_失效缓存。
完。

样式对象_匹配规则 接受 元素哈希：
    规则匹配数 为 规则匹配数 加 1。
    桶号 为 元素哈希 取余于 缓存桶数。
    缓存值 为 做 内存_读64 用 计算缓存 加 桶号 乘 8。
    若 缓存值 不等于 0：
        缓存命中 为 缓存命中 加 1。
        返还 缓存值。
    完。
    缓存未中 为 缓存未中 加 1。
    可变 最高特异 为 0。
    可变 最佳规则 为 0。
    可变 表序 为 0。
    只要 表序 小于 样式表数：
        表 为 样式表列表 的 第 表序 项。
        若 非 表 的 已启用：
            表序 为 表序 加 1。
            继续。
        完。
        可变 规则序 为 0。
        只要 规则序 小于 表 的 规则数：
            规则 为 表 的 规则表 的 第 规则序 项。
            若 规则 的 特异度 大于 最高特异：
                最高特异 为 规则 的 特异度。
                最佳规则 为 规则 的 属性表。
            完。
            规则序 为 规则序 加 1。
        完。
        表序 为 表序 加 1。
    完。
    做 内存_写64 用 计算缓存 加 桶号 乘 8、最佳规则。
    返还 最佳规则。
完。

样式对象_失效缓存 接受 零参：
    失效次数 为 失效次数 加 1。
    可变 序号 为 0。
    只要 序号 小于 缓存桶数：
        做 内存_写64 用 计算缓存 加 序号 乘 8、0。
        序号 为 序号 加 1。
    完。
    缓存命中 为 0。
    缓存未中 为 0。
完。

样式对象_分析影响 接受 属性标识：
    若 属性标识 小于 10：
        返还 影响_重排。
    完。
    若 属性标识 小于 30：
        返还 影响_重绘。
    完。
    若 属性标识 小于 50：
        返还 影响_重排。
    完。
    返还 影响_重绘。
完。

样式对象_推断意图 接受 属性列表、属性数：
    可变 有变换 为 假。
    可变 有透明 为 假。
    可变 有过渡 为 假。
    可变 序号 为 0。
    只要 序号 小于 属性数：
        属性 为 做 内存_读32 用 属性列表 加 序号 乘 8。
        若 属性 等于 40：
            有变换 为 真。
        完。
        若 属性 等于 41：
            有透明 为 真。
        完。
        若 属性 等于 42：
            有过渡 为 真。
        完。
        序号 为 序号 加 1。
    完。
    若 有变换 并且 有透明：
        返还 1。
    完。
    若 有过渡：
        返还 2。
    完。
    返还 0。
完。

样式对象_缓存命中率 接受 零参：
    总 为 缓存命中 加 缓存未中。
    若 总 等于 0：
        返还 0。
    完。
    返还 缓存命中 乘 100 除以 总。
完。

样式对象_总规则数 接受 零参：
    可变 总 为 0。
    可变 序号 为 0。
    只要 序号 小于 样式表数：
        总 为 总 加 样式表列表 的 第 序号 项 的 规则数。
        序号 为 序号 加 1。
    完。
    返还 总。
完。

样式对象_完整统计 接受 零参：
    返还 一组：样式表数、做 样式对象_总规则数、规则匹配数、缓存命中、缓存未中、做 样式对象_缓存命中率、失效次数。
完。

本章 收束。
