《AI布局决策》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·文档节点》。

引用《..·推理·AI鼠标事件处理》。

引用《..·推理·定点Q算术》。

引用《.·AI布局预测》。

引用《..·中枢·内容可见性》。

引用《.·核心布局算》。

AI布局_设置语义优先级 接受 lo、dom节点：
    若 lo 等于 0 或者 dom节点 等于 0：
        返还。
    完。
    bus 为 全局AI总线。
    若 bus 不等于 0：
        角色 为 做 AI总线_语义分类 用 bus、dom节点。
        dom节点 的 语义角色 为 角色。
        节点特征 为 做 节点_提取AI特征 用 dom节点。
        若 节点特征 不等于 0：
            注意力 为 做 AI总线_注意力权重 用 bus、节点特征。
            lo 的 布局优先级 为 注意力 乘 255 除以 Q_ONE。
            若 lo 的 布局优先级 大于 255：
                lo 的 布局优先级 为 255。
            完。
            若 注意力 大于 768：
                lo 的 布局精度 为 3。
            否则若 注意力 大于 384：
                lo 的 布局精度 为 2。
            否则若 注意力 大于 100：
                lo 的 布局精度 为 1。
                lo 的 标志 为 lo 的 标志 位或 布局标志_低精度布局。
            否则：
                lo 的 布局精度 为 0。
            完。
            做 Tensor_释放 用 节点特征。
        完。
        返还。
    完。
    角色 为 dom节点 的 语义角色。
    若 角色 等于 语义_正文 或者 角色 等于 语义_标题：
        lo 的 布局优先级 为 255。
        lo 的 布局精度 为 3。
    否则若 角色 等于 语义_广告：
        lo 的 布局优先级 为 50。
        lo 的 布局精度 为 1。
        lo 的 标志 为 lo 的 标志 位或 布局标志_低精度布局。
    否则若 角色 等于 语义_Cookie同意：
        lo 的 布局优先级 为 30。
        lo 的 布局精度 为 1。
    完。
完。

AI布局_更新精度 接受 根lo、视口上、视口下：
    若 根lo 等于 0：
        返还。
    完。
    做 AI布局_更新精度递归 用 根lo、视口上、视口下。
完。

AI布局_更新精度递归 接受 lo、视口上、视口下：
    若 lo 等于 0：
        返还。
    完。
    节点上 为 lo 的 绝对y。
    节点下 为 lo 的 绝对y 加 lo 的 内容高。
    若 节点下 小于 视口上 减 200000 或者 节点上 大于 视口下 加 200000：
        lo 的 布局精度 为 0。
        返还。
    否则若 节点下 小于 视口上 或者 节点上 大于 视口下：
        若 lo 的 布局精度 大于 1：
            lo 的 布局精度 为 1。
        完。
    否则：
        若 lo 的 布局精度 小于 2：
            lo 的 布局精度 为 2。
        完。
    完。
    可变 子 为 lo 的 第一子。
    只要 子 不等于 0：
        做 AI布局_更新精度递归 用 子、视口上、视口下。
        子 为 子 的 后兄。
    完。
完。

AI布局_智能布局 接受 lo：
    若 lo 等于 0：
        返还。
    完。
    若 做 AI布局_可以跳过 用 lo：
        若 lo 的 预测置信度 大于或等于 8000：
            lo 的 内容宽 为 lo 的 预测宽。
            lo 的 内容高 为 lo 的 预测高。
            返还。
        完。
    完。
    若 lo 的 布局精度 等于 1：
        做 AI布局_预测尺寸 用 lo。
        若 lo 的 预测置信度 大于或等于 5000：
            lo 的 内容宽 为 lo 的 预测宽。
            lo 的 内容高 为 lo 的 预测高。
            lo 的 标志 为 lo 的 标志 位与 按位取反 布局标志_需要布局。
            返还。
        完。
    完。
    开始 为 做 平台_获取高精度时间。
    做 布局Object_布局 用 lo。
    耗时 为 做 平台_获取高精度时间 减 开始。
    lo 的 上次布局耗时 为 耗时。
    lo 的 累计布局次数 为 lo 的 累计布局次数 加 1。
    lo 的 布局版本 为 lo 的 布局版本 加 1。
    lo 的 预测宽 为 lo 的 内容宽。
    lo 的 预测高 为 lo 的 内容高。
    lo 的 预测置信度 为 9500。
    若 lo 的 父 不等于 0：
        lo 的 父布局版本 为 lo 的 父 的 布局版本。
    完。
完。

本章 收束。
