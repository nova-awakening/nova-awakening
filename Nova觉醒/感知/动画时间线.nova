《动画时间线》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·关键帧》。

引用《.·关键帧插值》。

引用《.·函数创建》。

可变 全局动画列表 为 0。

可变 全局动画数量 为 0。

可变 全局动画容量 为 0。

AnimationTimeline_初始化 接受 零参：
    若 全局动画列表 不等于 0：
        返还。
    完。
    全局动画容量 为 128。
    全局动画列表 为 做 堆_分配_数组 用 全局动画容量 乘 8。
    全局动画数量 为 0。
完。

AnimationTimeline_注册 接受 anim：
    若 全局动画列表 等于 0：
        做 AnimationTimeline_初始化。
    完。
    若 全局动画数量 大于或等于 全局动画容量：
        返还。
    完。
    做 内存_写64 用 全局动画列表 加 全局动画数量 乘 8、anim。
    全局动画数量 为 全局动画数量 加 1。
完。

AnimationTimeline_每帧更新 接受 零参：
    若 全局动画列表 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 全局动画数量：
        anim 为 做 内存_读64 用 全局动画列表 加 i 乘 8。
        若 anim 等于 0 或者 anim 的 状态 不等于 动画状态_播放中：
            i 为 i 加 1。
            继续。
        完。
        ke 为 anim 的 效果。
        若 ke 等于 0：
            i 为 i 加 1。
            继续。
        完。
        当前时间 为 做 Animation_计算当前时间 用 anim 减 ke 的 延迟。
        若 当前时间 小于 0：
            若 ke 的 填充模式 等于 2 或者 ke 的 填充模式 等于 3：
                做 AnimationFrame_应用 用 ke、0。
            完。
            i 为 i 加 1。
            继续。
        完。
        可变 总时长 为 ke 的 持续时间。
        若 总时长 小于或等于 0：
            总时长 为 1。
        完。
        可变 迭代 为 当前时间 除以 总时长。
        可变 进度 为 当前时间 取余于 总时长 乘 10000 除以 总时长。
        可变 最大迭代 为 ke 的 迭代次数 除以 100。
        若 ke 的 迭代次数 小于 0：
            最大迭代 为 999999。
        完。
        若 迭代 大于或等于 最大迭代：
            若 ke 的 填充模式 等于 1 或者 ke 的 填充模式 等于 3：
                做 AnimationFrame_应用 用 ke、10000。
            完。
            anim 的 状态 为 动画状态_完成。
            i 为 i 加 1。
            继续。
        完。
        若 ke 的 方向 等于 1：
            进度 为 10000 减 进度。
        否则若 ke 的 方向 等于 2：
            若 迭代 取余于 2 等于 1：
                进度 为 10000 减 进度。
            完。
        否则若 ke 的 方向 等于 3：
            若 迭代 取余于 2 等于 0：
                进度 为 10000 减 进度。
            完。
        完。
        若 ke 的 缓动 不等于 0：
            进度 为 做 TimingFunction_求值 用 ke 的 缓动、进度。
        完。
        做 AnimationFrame_应用 用 ke、进度。
        若 ke 的 目标元素 不等于 0：
            ke 的 目标元素 的 节点标志 为 ke 的 目标元素 的 节点标志 位或 节点标志_样式脏。
        完。
        i 为 i 加 1。
    完。
    做 AnimationTimeline_清理完成。
完。

AnimationTimeline_清理完成 接受 零参：
    可变 w 为 0。
    可变 i 为 0。
    只要 i 小于 全局动画数量：
        anim 为 做 内存_读64 用 全局动画列表 加 i 乘 8。
        若 anim 不等于 0 并且 anim 的 状态 不等于 动画状态_完成：
            做 内存_写64 用 全局动画列表 加 w 乘 8、anim。
            w 为 w 加 1。
        完。
        i 为 i 加 1。
    完。
    全局动画数量 为 w。
完。

本章 收束。
