《影子创建》

引用《.·节点》。

引用《.·元素》。

引用《.·内核直连层》。

引用《.·文档节点》。

引用《..·中枢·字符串替换》。

ShadowMode_Open 为 0。

ShadowMode_Closed 为 1。

ShadowRoot 是 一种 事物，含 节点、模式、宿主、委托焦点、slot列表、slot数量、样式表列表、样式表数量、采用样式表、采用样式表数量、需要slot分配。

ShadowRoot_创建 接受 宿主元素、模式：
    sr 为 做 堆_分配 用 ShadowRoot。
    sr 的 节点 为 做 节点_创建 用 11、节点标志_是容器节点。
    sr 的 模式 为 模式。
    sr 的 宿主 为 宿主元素。
    sr 的 委托焦点 为 假。
    sr 的 slot列表 为 做 堆_分配_数组 用 16 乘 16。
    sr 的 slot数量 为 0。
    sr 的 样式表列表 为 做 堆_分配_数组 用 16 乘 8。
    sr 的 样式表数量 为 0。
    sr 的 采用样式表 为 做 堆_分配_数组 用 8 乘 8。
    sr 的 采用样式表数量 为 0。
    sr 的 需要slot分配 为 真。
    若 宿主元素 不等于 0：
        扩展 为 做 元素_获取扩展 用 宿主元素。
        若 扩展 不等于 0：
            扩展 的 shadowRoot 为 sr。
        完。
    完。
    返还 sr。
完。

元素_attachShadow 接受 元素、模式：
    若 元素 等于 0：
        返还 0。
    完。
    扩展 为 做 元素_获取扩展 用 元素。
    若 扩展 不等于 0 并且 扩展 的 shadowRoot 不等于 0：
        返还 0。
    完。
    若 非 做 元素_允许Shadow 用 元素：
        返还 0。
    完。
    返还 做 ShadowRoot_创建 用 元素、模式。
完。

元素_允许Shadow 接受 元素：
    扩展 为 做 元素_获取扩展 用 元素。
    若 扩展 等于 0：
        返还 假。
    完。
    标签 为 扩展 的 标签名。
    若 标签 不等于 0：
        长度 为 做 字符串_长度 用 标签。
        可变 i 为 0。
        只要 i 小于 长度：
            若 做 内存_读8 用 标签 加 i 等于 0x2D：
                返还 真。
            完。
            i 为 i 加 1。
        完。
    完。
    原子 为 扩展 的 标签名原子。
    若 原子 等于 原子_div 或者 原子 等于 原子_span 或者 原子 等于 原子_p 或者 原子 等于 原子_section 或者 原子 等于 原子_article 或者 原子 等于 原子_aside 或者 原子 等于 原子_header 或者 原子 等于 原子_footer 或者 原子 等于 原子_nav 或者 原子 等于 原子_main 或者 原子 等于 原子_body 或者 原子 等于 原子_h1 或者 原子 等于 原子_h2 或者 原子 等于 原子_h3：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
