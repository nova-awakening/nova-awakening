《CSS颜色解析》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·样式子网格》。

引用《.·样式滤镜》。

引用《.·CSS颜色混合》。

引用《.·内核直连层》。

引用《..·中枢·沙箱隔离进》。

CS_SRGB 为 0。

CS_SRGB_LINEAR 为 1。

CS_DISPLAY_P3 为 2。

CS_A98_RGB 为 3。

CS_PROPHOTO_RGB 为 4。

CS_REC2020 为 5。

CS_XYZ_D50 为 6。

CS_XYZ_D65 为 7。

CS_LAB 为 8。

CS_LCH 为 9。

CS_OKLAB 为 10。

CS_OKLCH 为 11。

CS_HSL 为 12。

CS_HWB 为 13。

CSSColor 是 一种 事物，含 空间、分量1、分量2、分量3、alpha。

CSSColor_解析 接受 值字符串：
    若 值字符串 等于 0：
        返还 做 CSSColor_默认。
    完。
    若 做 内存_读8 用 值字符串 等于 0x23：
        返还 做 CSSColor_从hex 用 值字符串。
    完。
    若 做 字符串_开头匹配 用 值字符串、「rgb」：
        返还 做 CSSColor_从rgb 用 值字符串。
    完。
    若 做 字符串_开头匹配 用 值字符串、「hsl」：
        返还 做 CSSColor_从hsl 用 值字符串。
    完。
    若 做 字符串_开头匹配 用 值字符串、「oklch」：
        返还 做 CSSColor_从oklch 用 值字符串。
    完。
    若 做 字符串_开头匹配 用 值字符串、「oklab」：
        返还 做 CSSColor_从oklab 用 值字符串。
    完。
    若 做 字符串_开头匹配 用 值字符串、「lab(」：
        返还 做 CSSColor_从lab 用 值字符串。
    完。
    若 做 字符串_开头匹配 用 值字符串、「lch(」：
        返还 做 CSSColor_从lch 用 值字符串。
    完。
    若 做 字符串_开头匹配 用 值字符串、「color(」：
        返还 做 CSSColor_从color函数 用 值字符串。
    完。
    若 做 字符串_开头匹配 用 值字符串、「color-mix」：
        返还 做 CSSColor_从colorMix 用 值字符串。
    完。
    若 做 字符串_开头匹配 用 值字符串、「light-dark」：
        返还 做 CSSColor_从lightDark 用 值字符串。
    完。
    若 做 字符串_开头匹配 用 值字符串、「hwb」：
        返还 做 CSSColor_从hwb 用 值字符串。
    完。
    返还 做 CSSColor_从名称 用 值字符串。
完。

CSSColor_默认 接受 零参：
    返还 新建 CSSColor：空间 为 CS_SRGB、分量1 为 0、分量2 为 0、分量3 为 0、alpha 为 10000。
完。

本章 收束。
