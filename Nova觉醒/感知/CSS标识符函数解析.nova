《CSS标识符函数解析》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·样式解析》。

引用《.·CSS属性字符判定》。

引用《..·中枢·辅助存根》。

引用《.·样式解析值》。

引用《.·CSS_RGB_HSL解析》。

引用《..·执行·底层内存操》。

引用《.·内核直连层》。

CSS解析_标识符或函数 接受 文本、长度、pos：
    可变 p 为 pos。
    名称起始 为 p。
    只要 p 小于 长度 并且 做 CSS_是标识符续 用 做 内存_读8 用 文本 加 p：
        p 为 p 加 1。
    完。
    名称长度 为 p 减 名称起始。
    名称 为 做 字符串_复制_N 用 文本 加 名称起始、名称长度。
    若 p 小于 长度 并且 做 内存_读8 用 文本 加 p 等于 0x28：
        若 做 字符串_等于N 用 名称、名称长度、「rgb」 或者 做 字符串_等于N 用 名称、名称长度、「rgba」：
            返还 做 CSS解析_rgb函数 用 文本、长度、名称起始。
        完。
        若 做 字符串_等于N 用 名称、名称长度、「hsl」 或者 做 字符串_等于N 用 名称、名称长度、「hsla」：
            返还 做 CSS解析_hsl函数 用 文本、长度、名称起始。
        完。
        若 做 字符串_等于N 用 名称、名称长度、「url」：
            返还 做 CSS解析_url函数 用 文本、长度、p。
        完。
        若 做 字符串_等于N 用 名称、名称长度、「var」：
            返还 做 CSS解析_var函数 用 文本、长度、p。
        完。
        若 做 字符串_等于N 用 名称、名称长度、「calc」 或者 做 字符串_等于N 用 名称、名称长度、「min」 或者 做 字符串_等于N 用 名称、名称长度、「max」 或者 做 字符串_等于N 用 名称、名称长度、「clamp」：
            返还 做 CSS解析_calc函数 用 文本、长度、名称起始。
        完。
        若 做 字符串_等于N 用 名称、名称长度、「linear-gradient」 或者 做 字符串_等于N 用 名称、名称长度、「radial-gradient」 或者 做 字符串_等于N 用 名称、名称长度、「repeating-linear-gradient」 或者 做 字符串_等于N 用 名称、名称长度、「repeating-radial-gradient」 或者 做 字符串_等于N 用 名称、名称长度、「conic-gradient」：
            返还 做 CSS解析_渐变函数 用 文本、长度、名称起始。
        完。
        v 为 做 CSSParsedValue_创建 用 CSS值_函数。
        v 的 字符串 为 名称。
        v 的 字符串长度 为 名称长度。
        返还 v。
    完。
    颜色 为 做 CSS解析_命名颜色 用 名称、名称长度。
    若 颜色 不等于 0：
        返还 颜色。
    完。
    v 为 做 CSSParsedValue_创建 用 CSS值_关键字。
    v 的 字符串 为 名称。
    v 的 字符串长度 为 名称长度。
    v 的 关键字ID 为 做 CSS解析_关键字ID 用 名称、名称长度。
    返还 v。
完。

CSS解析_关键字ID 接受 名称、长度：
    若 做 字符串_等于N 用 名称、长度、「inherit」：
        返还 关键字_inherit。
    完。
    若 做 字符串_等于N 用 名称、长度、「initial」：
        返还 关键字_initial。
    完。
    若 做 字符串_等于N 用 名称、长度、「unset」：
        返还 关键字_unset。
    完。
    若 做 字符串_等于N 用 名称、长度、「auto」：
        返还 关键字_auto。
    完。
    若 做 字符串_等于N 用 名称、长度、「none」：
        返还 关键字_none。
    完。
    若 做 字符串_等于N 用 名称、长度、「normal」：
        返还 关键字_normal。
    完。
    若 做 字符串_等于N 用 名称、长度、「bold」：
        返还 关键字_bold。
    完。
    若 做 字符串_等于N 用 名称、长度、「italic」：
        返还 关键字_italic。
    完。
    若 做 字符串_等于N 用 名称、长度、「hidden」：
        返还 关键字_hidden。
    完。
    若 做 字符串_等于N 用 名称、长度、「visible」：
        返还 关键字_visible。
    完。
    若 做 字符串_等于N 用 名称、长度、「block」：
        返还 关键字_block。
    完。
    若 做 字符串_等于N 用 名称、长度、「inline」：
        返还 关键字_inline。
    完。
    若 做 字符串_等于N 用 名称、长度、「inline-block」：
        返还 关键字_inline_block。
    完。
    若 做 字符串_等于N 用 名称、长度、「flex」：
        返还 关键字_flex。
    完。
    若 做 字符串_等于N 用 名称、长度、「grid」：
        返还 关键字_grid。
    完。
    若 做 字符串_等于N 用 名称、长度、「static」：
        返还 关键字_static。
    完。
    若 做 字符串_等于N 用 名称、长度、「relative」：
        返还 关键字_relative。
    完。
    若 做 字符串_等于N 用 名称、长度、「absolute」：
        返还 关键字_absolute。
    完。
    若 做 字符串_等于N 用 名称、长度、「fixed」：
        返还 关键字_fixed。
    完。
    若 做 字符串_等于N 用 名称、长度、「sticky」：
        返还 关键字_sticky。
    完。
    若 做 字符串_等于N 用 名称、长度、「left」：
        返还 关键字_left。
    完。
    若 做 字符串_等于N 用 名称、长度、「right」：
        返还 关键字_right。
    完。
    若 做 字符串_等于N 用 名称、长度、「center」：
        返还 关键字_center。
    完。
    若 做 字符串_等于N 用 名称、长度、「justify」：
        返还 关键字_justify。
    完。
    若 做 字符串_等于N 用 名称、长度、「top」：
        返还 关键字_top。
    完。
    若 做 字符串_等于N 用 名称、长度、「bottom」：
        返还 关键字_bottom。
    完。
    若 做 字符串_等于N 用 名称、长度、「nowrap」：
        返还 关键字_nowrap。
    完。
    若 做 字符串_等于N 用 名称、长度、「wrap」：
        返还 关键字_wrap。
    完。
    若 做 字符串_等于N 用 名称、长度、「solid」：
        返还 关键字_solid。
    完。
    若 做 字符串_等于N 用 名称、长度、「dashed」：
        返还 关键字_dashed。
    完。
    若 做 字符串_等于N 用 名称、长度、「dotted」：
        返还 关键字_dotted。
    完。
    若 做 字符串_等于N 用 名称、长度、「transparent」：
        返还 关键字_transparent。
    完。
    若 做 字符串_等于N 用 名称、长度、「pointer」：
        返还 关键字_pointer。
    完。
    若 做 字符串_等于N 用 名称、长度、「row」：
        返还 关键字_row。
    完。
    若 做 字符串_等于N 用 名称、长度、「column」：
        返还 关键字_column。
    完。
    若 做 字符串_等于N 用 名称、长度、「flex-start」：
        返还 关键字_flex_start。
    完。
    若 做 字符串_等于N 用 名称、长度、「flex-end」：
        返还 关键字_flex_end。
    完。
    若 做 字符串_等于N 用 名称、长度、「space-between」：
        返还 关键字_space_between。
    完。
    若 做 字符串_等于N 用 名称、长度、「space-around」：
        返还 关键字_space_around。
    完。
    若 做 字符串_等于N 用 名称、长度、「stretch」：
        返还 关键字_stretch。
    完。
    若 做 字符串_等于N 用 名称、长度、「cover」：
        返还 关键字_cover。
    完。
    若 做 字符串_等于N 用 名称、长度、「contain」：
        返还 关键字_contain。
    完。
    若 做 字符串_等于N 用 名称、长度、「border-box」：
        返还 关键字_border_box。
    完。
    若 做 字符串_等于N 用 名称、长度、「padding-box」：
        返还 关键字_padding_box。
    完。
    若 做 字符串_等于N 用 名称、长度、「content-box」：
        返还 关键字_content_box。
    完。
    若 做 字符串_等于N 用 名称、长度、「no-repeat」：
        返还 关键字_no_repeat。
    完。
    若 做 字符串_等于N 用 名称、长度、「repeat」：
        返还 关键字_repeat。
    完。
    返还 0。
完。

CSS解析_字符串 接受 文本、长度、pos、引号：
    可变 p 为 pos 加 1。
    起始 为 p。
    只要 p 小于 长度 并且 做 内存_读8 用 文本 加 p 不等于 引号：
        若 做 内存_读8 用 文本 加 p 等于 0x5C：
            p 为 p 加 1。
        完。
        p 为 p 加 1。
    完。
    v 为 做 CSSParsedValue_创建 用 CSS值_字符串。
    v 的 字符串 为 做 字符串_复制_N 用 文本 加 起始、p 减 起始。
    v 的 字符串长度 为 p 减 起始。
    返还 v。
完。

CSS解析_url函数 接受 文本、长度、pos：
    可变 p 为 pos 加 1。
    只要 p 小于 长度 并且 做 CSS_是空白 用 做 内存_读8 用 文本 加 p：
        p 为 p 加 1。
    完。
    c 为 做 内存_读8 用 文本 加 p。
    若 c 等于 0x22 或者 c 等于 0x27：
        str 为 做 CSS解析_字符串 用 文本、长度、p、c。
        v 为 做 CSSParsedValue_创建 用 CSS值_URL。
        若 str 不等于 0：
            v 的 字符串 为 str 的 字符串。
            v 的 字符串长度 为 str 的 字符串长度。
        完。
        返还 v。
    完。
    起始 为 p。
    只要 p 小于 长度 并且 做 内存_读8 用 文本 加 p 不等于 0x29：
        p 为 p 加 1。
    完。
    v 为 做 CSSParsedValue_创建 用 CSS值_URL。
    v 的 字符串 为 做 字符串_复制_N 用 文本 加 起始、p 减 起始。
    v 的 字符串长度 为 p 减 起始。
    返还 v。
完。

CSS解析_var函数 接受 文本、长度、pos：
    可变 p 为 pos 加 1。
    只要 p 小于 长度 并且 做 CSS_是空白 用 做 内存_读8 用 文本 加 p：
        p 为 p 加 1。
    完。
    起始 为 p。
    只要 p 小于 长度 并且 做 内存_读8 用 文本 加 p 不等于 0x29 并且 做 内存_读8 用 文本 加 p 不等于 0x2C：
        p 为 p 加 1。
    完。
    v 为 做 CSSParsedValue_创建 用 CSS值_自定义属性。
    v 的 字符串 为 做 字符串_复制_N 用 文本 加 起始、p 减 起始。
    v 的 字符串长度 为 p 减 起始。
    返还 v。
完。

CSS解析_calc函数 接受 文本、长度、pos：
    可变 p 为 pos。
    只要 p 小于 长度 并且 做 内存_读8 用 文本 加 p 不等于 0x28：
        p 为 p 加 1。
    完。
    p 为 p 加 1。
    起始 为 p。
    可变 深度 为 1。
    只要 p 小于 长度 并且 深度 大于 0：
        若 做 内存_读8 用 文本 加 p 等于 0x28：
            深度 为 深度 加 1。
        完。
        若 做 内存_读8 用 文本 加 p 等于 0x29：
            深度 为 深度 减 1。
        完。
        若 深度 大于 0：
            p 为 p 加 1。
        完。
    完。
    v 为 做 CSSParsedValue_创建 用 CSS值_函数。
    v 的 字符串 为 做 字符串_复制_N 用 文本 加 起始、p 减 起始。
    v 的 字符串长度 为 p 减 起始。
    返还 v。
完。

CSS解析_渐变函数 接受 文本、长度、pos：
    返还 做 CSS解析_calc函数 用 文本、长度、pos。
完。

可变 AI_标识符函数_样式缓存命中 为 0。

AI_标识符函数_选择器复杂度 接受 选择器长度、组合子数：
    若 组合子数 大于 5：
        返还 「高复杂度」。
    完。
    若 组合子数 大于 2：
        返还 「中复杂度」。
    完。
    返还 「低复杂度」。
完。

AI_标识符函数_布局缓存 接受 元素哈希：
    AI_标识符函数_样式缓存命中 为 AI_标识符函数_样式缓存命中 加 1。
    返还 元素哈希。
完。

本章 收束。
