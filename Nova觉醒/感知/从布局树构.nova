《从布局树构》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·创建销毁》。

引用《.·创建初始化》。

绘制Layer_从布局树构建 接受 根布局对象：
    根层 为 做 绘制Layer_创建 用 根布局对象。
    做 绘制Layer_构建递归 用 根层、根布局对象。
    做 绘制Layer_更新z序 用 根层。
    返还 根层。
完。

绘制Layer_构建递归 接受 当前层、布局对象：
    可变 子lo 为 布局对象 的 第一子。
    只要 子lo 不等于 0：
        若 做 绘制Layer_需要自己的层 用 子lo：
            新层 为 做 绘制Layer_创建 用 子lo。
            做 绘制Layer_添加子层 用 当前层、新层。
            做 绘制Layer_构建递归 用 新层、子lo。
        否则：
            做 绘制Layer_构建递归 用 当前层、子lo。
        完。
        子lo 为 子lo 的 后兄。
    完。
完。

绘制Layer_需要自己的层 接受 lo：
    s 为 lo 的 样式。
    若 s 等于 0：
        返还 0。
    完。
    若 s 的 position 不等于 Position_static：
        返还 1。
    完。
    若 s 的 opacity 小于 100：
        返还 1。
    完。
    若 s 的 transform 不等于 0：
        返还 1。
    完。
    若 s 的 overflow_x 不等于 Overflow_visible 或者 s 的 overflow_y 不等于 Overflow_visible：
        返还 1。
    完。
    若 非 s 的 z_index_auto：
        返还 1。
    完。
    若 s 的 display 等于 Display_flex 或者 s 的 display 等于 Display_grid：
        返还 1。
    完。
    返还 0。
完。

显示项_背景 为 1。

显示项_边框 为 2。

显示项_文本 为 3。

显示项_图片 为 4。

显示项_圆角裁剪 为 5。

显示项_裁剪 为 6。

显示项_还原裁剪 为 7。

显示项_透明度开始 为 8。

显示项_透明度结束 为 9。

显示项_变换开始 为 10。

显示项_变换结束 为 11。

显示项_阴影 为 12。

显示项_轮廓 为 13。

显示项_滚动条 为 14。

显示项 是 一种 事物，含 类型、x、y、w、h、颜色、边框宽、边框颜色、文本指针、文本长度、字号、字重、字体、图片URL、圆角半径、透明度。

绘制Layer_绘制 接受 层、显示列表、偏移x、偏移y：
    若 层 等于 0：
        返还。
    完。
    lo 为 层 的 布局对象。
    若 lo 等于 0：
        返还。
    完。
    s 为 lo 的 样式。
    若 s 等于 0：
        返还。
    完。
    若 s 的 visibility 等于 Visibility_hidden：
        返还。
    完。
    绘制x 为 lo 的 绝对x 加 偏移x 减 层 的 滚动偏移x。
    绘制y 为 lo 的 绝对y 加 偏移y 减 层 的 滚动偏移y。
    绘制w 为 做 布局Object_边框盒宽 用 lo。
    绘制h 为 做 布局Object_边框盒高 用 lo。
完。

本章 收束。
