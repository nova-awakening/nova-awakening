《Base64编解码》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·执行·控制台输出》。

可变 BASE64_表 为 「ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/」。

Base64_编码 接受 数据、长度：
    若 数据 等于 0 或者 长度 等于 0：
        返还 「」。
    完。
    输出长度 为 (长度 加 2) 除以 3 乘 4。
    结果 为 做 堆_分配 用 输出长度 加 1。
    可变 i 为 0。
    可变 o 为 0。
    只要 i 加 2 小于 长度：
        b0 为 做 字节取 用 数据、i。
        b1 为 做 字节取 用 数据、i 加 1。
        b2 为 做 字节取 用 数据、i 加 2。
        做 字节写 用 结果、o、做 字节取 用 BASE64_表、b0 右移 2。
        做 字节写 用 结果、o 加 1、做 字节取 用 BASE64_表、(b0 位与 3) 左移 4 位或 b1 右移 4。
        做 字节写 用 结果、o 加 2、做 字节取 用 BASE64_表、(b1 位与 15) 左移 2 位或 b2 右移 6。
        做 字节写 用 结果、o 加 3、做 字节取 用 BASE64_表、b2 位与 63。
        i 为 i 加 3。
        o 为 o 加 4。
    完。
    若 i 小于 长度：
        b0 为 做 字节取 用 数据、i。
        做 字节写 用 结果、o、做 字节取 用 BASE64_表、b0 右移 2。
        若 i 加 1 小于 长度：
            b1 为 做 字节取 用 数据、i 加 1。
            做 字节写 用 结果、o 加 1、做 字节取 用 BASE64_表、(b0 位与 3) 左移 4 位或 b1 右移 4。
            做 字节写 用 结果、o 加 2、做 字节取 用 BASE64_表、(b1 位与 15) 左移 2。
        否则：
            做 字节写 用 结果、o 加 1、做 字节取 用 BASE64_表、(b0 位与 3) 左移 4。
            做 字节写 用 结果、o 加 2、61。
        完。
        做 字节写 用 结果、o 加 3、61。
        o 为 o 加 4。
    完。
    做 字节写 用 结果、o、0。
    返还 结果。
完。

Base64_解码字符 接受 ch：
    若 ch 大于或等于 65 并且 ch 小于或等于 90：
        返还 ch 减 65。
    完。
    若 ch 大于或等于 97 并且 ch 小于或等于 122：
        返还 ch 减 71。
    完。
    若 ch 大于或等于 48 并且 ch 小于或等于 57：
        返还 ch 加 4。
    完。
    若 ch 等于 43：
        返还 62。
    完。
    若 ch 等于 47：
        返还 63。
    完。
    返还 负 1。
完。

Base64_解码 接受 base64、长度：
    若 base64 等于 0 或者 长度 等于 0：
        返还 「」。
    完。
    可变 pad 为 0。
    若 做 字节取 用 base64、长度 减 1 等于 61：
        pad 为 pad 加 1。
    完。
    若 长度 大于或等于 2 并且 做 字节取 用 base64、长度 减 2 等于 61：
        pad 为 pad 加 1。
    完。
    输出长度 为 长度 除以 4 乘 3 减 pad。
    结果 为 做 堆_分配 用 输出长度 加 1。
    可变 i 为 0。
    可变 o 为 0。
    只要 i 加 3 小于 长度：
        a 为 做 Base64_解码字符 用 做 字节取 用 base64、i。
        b 为 做 Base64_解码字符 用 做 字节取 用 base64、i 加 1。
        c 为 做 Base64_解码字符 用 做 字节取 用 base64、i 加 2。
        d 为 做 Base64_解码字符 用 做 字节取 用 base64、i 加 3。
        若 a 小于 0 或者 b 小于 0：
            返还 0。
        完。
        做 字节写 用 结果、o、a 左移 2 位或 b 右移 4。
        o 为 o 加 1。
        若 c 大于或等于 0：
            做 字节写 用 结果、o、(b 位与 15) 左移 4 位或 c 右移 2。
            o 为 o 加 1。
        完。
        若 d 大于或等于 0：
            做 字节写 用 结果、o、(c 位与 3) 左移 6 位或 d。
            o 为 o 加 1。
        完。
        i 为 i 加 4。
    完。
    做 字节写 用 结果、o、0。
    返还 结果。
完。

URL_是安全字符 接受 ch：
    若 ch 大于或等于 65 并且 ch 小于或等于 90：
        返还 真。
    完。
    若 ch 大于或等于 97 并且 ch 小于或等于 122：
        返还 真。
    完。
    若 ch 大于或等于 48 并且 ch 小于或等于 57：
        返还 真。
    完。
    若 ch 等于 45 或者 ch 等于 95 或者 ch 等于 46 或者 ch 等于 33：
        返还 真。
    完。
    若 ch 等于 126 或者 ch 等于 42 或者 ch 等于 39 或者 ch 等于 40 或者 ch 等于 41：
        返还 真。
    完。
    返还 假。
完。

URL_hex字符 接受 v：
    若 v 小于 10：
        返还 48 加 v。
    完。
    返还 65 加 v 减 10。
完。

URL_encodeComponent 接受 字符串：
    若 字符串 等于 0：
        返还 「」。
    完。
    len 为 做 字节长度 用 字符串。
    结果 为 做 堆_分配 用 len 乘 3 加 1。
    可变 rpos 为 0。
    可变 wpos 为 0。
    只要 rpos 小于 len：
        ch 为 做 字节取 用 字符串、rpos。
        若 做 URL_是安全字符 用 ch：
            做 字节写 用 结果、wpos、ch。
            wpos 为 wpos 加 1。
        否则：
            做 字节写 用 结果、wpos、37。
            做 字节写 用 结果、wpos 加 1、做 URL_hex字符 用 ch 右移 4。
            做 字节写 用 结果、wpos 加 2、做 URL_hex字符 用 ch 位与 15。
            wpos 为 wpos 加 3。
        完。
        rpos 为 rpos 加 1。
    完。
    做 字节写 用 结果、wpos、0。
    返还 结果。
完。

URL_hex值 接受 ch：
    若 ch 大于或等于 48 并且 ch 小于或等于 57：
        返还 ch 减 48。
    完。
    若 ch 大于或等于 65 并且 ch 小于或等于 70：
        返还 ch 减 55。
    完。
    若 ch 大于或等于 97 并且 ch 小于或等于 102：
        返还 ch 减 87。
    完。
    返还 负 1。
完。

URL_decodeComponent 接受 字符串：
    若 字符串 等于 0：
        返还 「」。
    完。
    len 为 做 字节长度 用 字符串。
    结果 为 做 堆_分配 用 len 加 1。
    可变 rpos 为 0。
    可变 wpos 为 0。
    只要 rpos 小于 len：
        ch 为 做 字节取 用 字符串、rpos。
        若 ch 等于 37 并且 rpos 加 2 小于 len：
            h 为 做 URL_hex值 用 做 字节取 用 字符串、rpos 加 1。
            l 为 做 URL_hex值 用 做 字节取 用 字符串、rpos 加 2。
            若 h 大于或等于 0 并且 l 大于或等于 0：
                做 字节写 用 结果、wpos、h 乘 16 加 l。
                wpos 为 wpos 加 1。
                rpos 为 rpos 加 3。
                继续。
            完。
        完。
        做 字节写 用 结果、wpos、ch。
        wpos 为 wpos 加 1。
        rpos 为 rpos 加 1。
    完。
    做 字节写 用 结果、wpos、0。
    返还 结果。
完。

URL_是URI安全字符 接受 ch：
    若 做 URL_是安全字符 用 ch：
        返还 真。
    完。
    若 ch 等于 59 或者 ch 等于 47 或者 ch 等于 63 或者 ch 等于 58：
        返还 真。
    完。
    若 ch 等于 64 或者 ch 等于 38 或者 ch 等于 61 或者 ch 等于 43：
        返还 真。
    完。
    若 ch 等于 36 或者 ch 等于 44 或者 ch 等于 35：
        返还 真。
    完。
    返还 假。
完。

URL_encodeURI 接受 字符串：
    若 字符串 等于 0：
        返还 「」。
    完。
    len 为 做 字节长度 用 字符串。
    结果 为 做 堆_分配 用 len 乘 3 加 1。
    可变 rpos 为 0。
    可变 wpos 为 0。
    只要 rpos 小于 len：
        ch 为 做 字节取 用 字符串、rpos。
        若 做 URL_是URI安全字符 用 ch：
            做 字节写 用 结果、wpos、ch。
            wpos 为 wpos 加 1。
        否则：
            做 字节写 用 结果、wpos、37。
            做 字节写 用 结果、wpos 加 1、做 URL_hex字符 用 ch 右移 4。
            做 字节写 用 结果、wpos 加 2、做 URL_hex字符 用 ch 位与 15。
            wpos 为 wpos 加 3。
        完。
        rpos 为 rpos 加 1。
    完。
    做 字节写 用 结果、wpos、0。
    返还 结果。
完。

可变 AI_接口基_威胁计数 为 0。

AI_接口基_风险评分 接受 操作类型、权限级别、来源可信度：
    可变 风险 为 0。
    若 权限级别 大于 500：
        风险 为 风险 加 300。
    完。
    若 来源可信度 小于 200：
        风险 为 风险 加 400。
    完。
    若 风险 大于 500：
        AI_接口基_威胁计数 为 AI_接口基_威胁计数 加 1。
    完。
    返还 风险。
完。

AI_接口基_异常行为检测 接受 操作频率、基线频率：
    若 基线频率 大于 0 并且 操作频率 大于 基线频率 乘 5：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
