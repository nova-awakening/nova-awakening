《时间范围》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·字符串替换》。

最大时间范围数 为 32。

最大音频轨道 为 4。

最大视频轨道 为 4。

最大文本轨道 为 16。

TRACK_AUDIO 为 0。

TRACK_VIDEO 为 1。

TRACK_TEXT 为 2。

TEXT_KIND_SUBTITLES 为 0。

TEXT_KIND_CAPTIONS 为 1。

TEXT_KIND_DESCRIPTIONS 为 2。

TEXT_KIND_CHAPTERS 为 3。

TEXT_KIND_METADATA 为 4。

TEXT_TRACK_DISABLED 为 0。

TEXT_TRACK_HIDDEN 为 1。

TEXT_TRACK_SHOWING 为 2。

CODEC_AAC 为 1。

CODEC_MP3 为 2。

CODEC_OPUS 为 3。

CODEC_VORBIS 为 4。

CODEC_FLAC 为 5。

CODEC_H264 为 10。

CODEC_H265 为 11。

CODEC_VP8 为 12。

CODEC_VP9 为 13。

CODEC_AV1 为 14。

CODEC_PCM 为 20。

时间范围统计 是 一种 事物，含 范围创建次数、范围添加次数、范围合并次数、范围包含查询次数、范围交集次数、范围归一化次数、轨道创建次数、轨道销毁次数、轨道切换次数、Cue添加次数、Cue移除次数、活跃Cue更新次数、AI轨道调用次数、AI轨道命中次数、总处理耗时_纳秒、错误总数、溢出保护次数、范围总长度_毫秒。

AI轨道状态 是 一种 事物，含 历史音频轨道率、历史视频轨道率、历史字幕轨道率、历史平均轨道数、连续单轨道计数、预测有字幕、采样计数器、学习权重。

可变 全局时间范围统计 为 0。

可变 全局轨道AI 为 0。

可变 全局时间范围已初始化 为 假。

时间范围_初始化全局 接受 零参：
    若 全局时间范围已初始化 等于 真：
        返还 错误_已存在。
    完。
    全局时间范围统计 为 做 堆_分配_清零 用 144。
    若 全局时间范围统计 等于 空：
        返还 错误_内存不足。
    完。
    全局轨道AI 为 做 堆_分配_清零 用 40。
    若 全局轨道AI 不等于 空：
        全局轨道AI 的 学习权重 为 128。
    完。
    全局时间范围已初始化 为 真。
    做 打印 用 「[时间范围] 初始化完成」。
    返还 0。
完。

时间范围_销毁全局 接受 零参：
    若 全局时间范围统计 不等于 空：
        做 堆_释放 用 全局时间范围统计。
        全局时间范围统计 为 0。
    完。
    若 全局轨道AI 不等于 空：
        做 堆_释放 用 全局轨道AI。
        全局轨道AI 为 0。
    完。
    全局时间范围已初始化 为 假。
    返还 0。
完。

TimeRange 是 一种 事物，含 开始、结束。

TimeRanges 是 一种 事物，含 范围、数量、容量、锁。

TimeRanges_创建 接受 零参：
    tr 为 做 堆_分配 用 TimeRanges。
    tr 的 数量 为 0。
    返还 tr。
完。

TimeRanges_添加 接受 tr、开始、结束：
    若 tr 等于 0 或者 tr 的 数量 大于或等于 32：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 tr 的 数量：
        若 开始 小于或等于 tr 的 范围 的 第 i 项 的 结束 并且 结束 大于或等于 tr 的 范围 的 第 i 项 的 开始：
            若 开始 小于 tr 的 范围 的 第 i 项 的 开始：
                tr 的 范围 的 第 i 项 的 开始 为 开始。
            完。
            若 结束 大于 tr 的 范围 的 第 i 项 的 结束：
                tr 的 范围 的 第 i 项 的 结束 为 结束。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
    tr 的 范围 的 第 tr 的 数量 项 的 开始 为 开始。
    tr 的 范围 的 第 tr 的 数量 项 的 结束 为 结束。
    tr 的 数量 为 tr 的 数量 加 1。
完。

TimeRanges_包含 接受 tr、时间：
    若 tr 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 tr 的 数量：
        若 时间 大于或等于 tr 的 范围 的 第 i 项 的 开始 并且 时间 小于或等于 tr 的 范围 的 第 i 项 的 结束：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

音频Track 是 一种 事物，含 id、label、language、enabled、采样率、声道数、位深度、编解码器、比特率。

视频Track 是 一种 事物，含 id、label、language、selected、宽度、高度、帧率、编解码器、比特率、色彩空间。

TextTrackCue 是 一种 事物，含 id、开始时间、结束时间、文本、垂直、行号、位置、对齐、大小、下一个。

TextTrack 是 一种 事物，含 id、label、language、kind、mode、cue头、cue数、活跃cue、活跃数。

TimeRanges_销毁 接受 tr：
    若 tr 等于 空：
        返还。
    完。
    做 堆_释放 用 tr。
完。

TimeRanges_总时长 接受 tr：
    若 tr 等于 空 或者 tr 的 数量 等于 0：
        返还 0。
    完。
    可变 总时长 为 0。
    可变 i 为 0。
    只要 i 小于 tr 的 数量：
        范围时长 为 tr 的 范围 的 第 i 项 的 结束 减 tr 的 范围 的 第 i 项 的 开始。
        若 范围时长 大于 0：
            总时长 为 总时长 加 范围时长。
        完。
        i 为 i 加 1。
    完。
    若 全局时间范围统计 不等于 空：
        全局时间范围统计 的 范围总长度_毫秒 为 总时长。
    完。
    返还 总时长。
完。

TimeRanges_起始 接受 tr、索引：
    若 tr 等于 空 或者 索引 大于或等于 tr 的 数量：
        返还 0。
    完。
    返还 tr 的 范围 的 第 索引 项 的 开始。
完。

TimeRanges_结束 接受 tr、索引：
    若 tr 等于 空 或者 索引 大于或等于 tr 的 数量：
        返还 0。
    完。
    返还 tr 的 范围 的 第 索引 项 的 结束。
完。

TimeRanges_归一化 接受 tr：
    若 tr 等于 空 或者 tr 的 数量 小于或等于 1：
        返还。
    完。
    可变 已排序 为 假。
    只要 已排序 等于 假：
        已排序 为 真。
        可变 i 为 0。
        只要 i 小于 tr 的 数量 减 1：
            若 tr 的 范围 的 第 i 项 的 开始 大于 tr 的 范围 的 第 i 加 1 项 的 开始：
                临时开始 为 tr 的 范围 的 第 i 项 的 开始。
                临时结束 为 tr 的 范围 的 第 i 项 的 结束。
                tr 的 范围 的 第 i 项 的 开始 为 tr 的 范围 的 第 i 加 1 项 的 开始。
                tr 的 范围 的 第 i 项 的 结束 为 tr 的 范围 的 第 i 加 1 项 的 结束。
                tr 的 范围 的 第 i 加 1 项 的 开始 为 临时开始。
                tr 的 范围 的 第 i 加 1 项 的 结束 为 临时结束。
                已排序 为 假。
            完。
            i 为 i 加 1。
        完。
    完。
    可变 写 为 0。
    可变 读 为 1。
    只要 读 小于 tr 的 数量：
        若 tr 的 范围 的 第 读 项 的 开始 小于或等于 tr 的 范围 的 第 写 项 的 结束：
            若 tr 的 范围 的 第 读 项 的 结束 大于 tr 的 范围 的 第 写 项 的 结束：
                tr 的 范围 的 第 写 项 的 结束 为 tr 的 范围 的 第 读 项 的 结束。
            完。
        否则：
            写 为 写 加 1。
            tr 的 范围 的 第 写 项 的 开始 为 tr 的 范围 的 第 读 项 的 开始。
            tr 的 范围 的 第 写 项 的 结束 为 tr 的 范围 的 第 读 项 的 结束。
        完。
        读 为 读 加 1。
    完。
    tr 的 数量 为 写 加 1。
    若 全局时间范围统计 不等于 空：
        全局时间范围统计 的 范围归一化次数 为 全局时间范围统计 的 范围归一化次数 加 1。
    完。
完。

TimeRanges_交集 接受 trA、trB：
    若 trA 等于 空 或者 trB 等于 空：
        返还 空。
    完。
    结果 为 做 TimeRanges_创建。
    若 结果 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 trA 的 数量：
        可变 j 为 0。
        只要 j 小于 trB 的 数量：
            交集开始 为 trA 的 范围 的 第 i 项 的 开始。
            若 trB 的 范围 的 第 j 项 的 开始 大于 交集开始：
                交集开始 为 trB 的 范围 的 第 j 项 的 开始。
            完。
            交集结束 为 trA 的 范围 的 第 i 项 的 结束。
            若 trB 的 范围 的 第 j 项 的 结束 小于 交集结束：
                交集结束 为 trB 的 范围 的 第 j 项 的 结束。
            完。
            若 交集开始 小于 交集结束：
                做 TimeRanges_添加 用 结果、交集开始、交集结束。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 全局时间范围统计 不等于 空：
        全局时间范围统计 的 范围交集次数 为 全局时间范围统计 的 范围交集次数 加 1。
    完。
    返还 结果。
完。

TimeRanges_最远缓冲点 接受 tr：
    若 tr 等于 空 或者 tr 的 数量 等于 0：
        返还 0。
    完。
    可变 最远 为 0。
    可变 i 为 0。
    只要 i 小于 tr 的 数量：
        若 tr 的 范围 的 第 i 项 的 结束 大于 最远：
            最远 为 tr 的 范围 的 第 i 项 的 结束。
        完。
        i 为 i 加 1。
    完。
    返还 最远。
完。

音频Track_创建 接受 id、label、language、codec、采样率、声道数：
    track 为 做 堆_分配 用 音频Track。
    若 track 等于 空：
        返还 空。
    完。
    track 的 id 为 id。
    track 的 label 为 label。
    track 的 language 为 language。
    track 的 enabled 为 假。
    track 的 编解码器 为 codec。
    track 的 采样率 为 采样率。
    track 的 声道数 为 声道数。
    track 的 位深度 为 16。
    track 的 比特率 为 0。
    若 全局时间范围统计 不等于 空：
        全局时间范围统计 的 轨道创建次数 为 全局时间范围统计 的 轨道创建次数 加 1。
    完。
    返还 track。
完。

视频Track_创建 接受 id、label、language、codec、宽、高、帧率：
    track 为 做 堆_分配 用 视频Track。
    若 track 等于 空：
        返还 空。
    完。
    track 的 id 为 id。
    track 的 label 为 label。
    track 的 language 为 language。
    track 的 selected 为 假。
    track 的 编解码器 为 codec。
    track 的 宽度 为 宽。
    track 的 高度 为 高。
    track 的 帧率 为 帧率。
    track 的 比特率 为 0。
    track 的 色彩空间 为 1。
    若 全局时间范围统计 不等于 空：
        全局时间范围统计 的 轨道创建次数 为 全局时间范围统计 的 轨道创建次数 加 1。
    完。
    返还 track。
完。

TextTrack_创建 接受 id、label、language、kind：
    track 为 做 堆_分配 用 TextTrack。
    若 track 等于 空：
        返还 空。
    完。
    track 的 id 为 id。
    track 的 label 为 label。
    track 的 language 为 language。
    track 的 kind 为 kind。
    track 的 mode 为 TEXT_TRACK_HIDDEN。
    track 的 cue头 为 0。
    track 的 cue数 为 0。
    track 的 活跃cue 为 0。
    track 的 活跃数 为 0。
    若 全局时间范围统计 不等于 空：
        全局时间范围统计 的 轨道创建次数 为 全局时间范围统计 的 轨道创建次数 加 1。
    完。
    返还 track。
完。

TextTrack_添加Cue 接受 track、开始、结束、文本：
    若 track 等于 空 或者 文本 等于 空：
        返还 错误_无效参数。
    完。
    cue 为 做 堆_分配 用 TextTrackCue。
    若 cue 等于 空：
        返还 错误_内存不足。
    完。
    cue 的 开始时间 为 开始。
    cue 的 结束时间 为 结束。
    cue 的 文本 为 文本。
    cue 的 垂直 为 0。
    cue 的 行号 为 负 1。
    cue 的 位置 为 512。
    cue 的 对齐 为 1。
    cue 的 大小 为 1024。
    cue 的 下一个 为 track 的 cue头。
    track 的 cue头 为 cue。
    track 的 cue数 为 track 的 cue数 加 1。
    若 全局时间范围统计 不等于 空：
        全局时间范围统计 的 Cue添加次数 为 全局时间范围统计 的 Cue添加次数 加 1。
    完。
    返还 0。
完。

TextTrack_更新活跃Cue 接受 track、当前时间：
    若 track 等于 空：
        返还。
    完。
    track 的 活跃数 为 0。
    可变 cue 为 track 的 cue头。
    只要 cue 不等于 空：
        若 当前时间 大于或等于 cue 的 开始时间 并且 当前时间 小于 cue 的 结束时间：
            track 的 活跃数 为 track 的 活跃数 加 1。
        完。
        cue 为 cue 的 下一个。
    完。
    若 全局时间范围统计 不等于 空：
        全局时间范围统计 的 活跃Cue更新次数 为 全局时间范围统计 的 活跃Cue更新次数 加 1。
    完。
完。

TextTrack_销毁 接受 track：
    若 track 等于 空：
        返还。
    完。
    可变 cue 为 track 的 cue头。
    只要 cue 不等于 空：
        下一个 为 cue 的 下一个。
        做 堆_释放 用 cue。
        cue 为 下一个。
    完。
    做 堆_释放 用 track。
    若 全局时间范围统计 不等于 空：
        全局时间范围统计 的 轨道销毁次数 为 全局时间范围统计 的 轨道销毁次数 加 1。
    完。
完。

AI轨道_记录 接受 音频数、视频数、文本数：
    若 全局轨道AI 等于 空：
        返还。
    完。
    AI 为 全局轨道AI。
    AI 的 采样计数器 为 AI 的 采样计数器 加 1。
    总轨道 为 音频数 加 视频数 加 文本数。
    AI 的 历史平均轨道数 为 (AI 的 历史平均轨道数 乘 7 加 总轨道 乘 1024) 除以 8。
    若 音频数 大于 0：
        AI 的 历史音频轨道率 为 (AI 的 历史音频轨道率 乘 15 加 1024) 除以 16。
    否则：
        AI 的 历史音频轨道率 为 AI 的 历史音频轨道率 乘 15 除以 16。
    完。
    若 视频数 大于 0：
        AI 的 历史视频轨道率 为 (AI 的 历史视频轨道率 乘 15 加 1024) 除以 16。
    否则：
        AI 的 历史视频轨道率 为 AI 的 历史视频轨道率 乘 15 除以 16。
    完。
    若 文本数 大于 0：
        AI 的 历史字幕轨道率 为 (AI 的 历史字幕轨道率 乘 15 加 1024) 除以 16。
    否则：
        AI 的 历史字幕轨道率 为 AI 的 历史字幕轨道率 乘 15 除以 16。
    完。
    若 总轨道 小于或等于 2：
        AI 的 连续单轨道计数 为 AI 的 连续单轨道计数 加 1。
    否则：
        AI 的 连续单轨道计数 为 0。
    完。
    若 AI 的 采样计数器 小于 8：
        返还。
    完。
    AI 的 采样计数器 为 0。
    若 全局时间范围统计 不等于 空：
        全局时间范围统计 的 AI轨道调用次数 为 全局时间范围统计 的 AI轨道调用次数 加 1。
    完。
    若 AI 的 历史字幕轨道率 大于 500：
        AI 的 预测有字幕 为 真。
    否则：
        AI 的 预测有字幕 为 假。
    完。
    若 AI 的 预测有字幕 等于 真 并且 文本数 大于 0：
        若 全局时间范围统计 不等于 空：
            全局时间范围统计 的 AI轨道命中次数 为 全局时间范围统计 的 AI轨道命中次数 加 1。
        完。
    完。
完。

时间范围_打印统计 接受 零参：
    若 全局时间范围统计 等于 空：
        做 打印 用 「[时间范围] 统计未初始化」。
        返还。
    完。
    做 打印 用 「┌──────────────────────────────────────┐」。
    做 打印 用 「│   时间范围+轨道 · 统计报告          │」。
    做 打印 用 「├──────────────────────────────────────┤」。
    做 打印 用 做 字符串拼接 用 「│ 范围创建:         」、做 转字符串 用 全局时间范围统计 的 范围创建次数。
    做 打印 用 做 字符串拼接 用 「│ 范围添加:         」、做 转字符串 用 全局时间范围统计 的 范围添加次数。
    做 打印 用 做 字符串拼接 用 「│ 范围合并:         」、做 转字符串 用 全局时间范围统计 的 范围合并次数。
    做 打印 用 做 字符串拼接 用 「│ 包含查询:         」、做 转字符串 用 全局时间范围统计 的 范围包含查询次数。
    做 打印 用 做 字符串拼接 用 「│ 范围交集:         」、做 转字符串 用 全局时间范围统计 的 范围交集次数。
    做 打印 用 做 字符串拼接 用 「│ 归一化:           」、做 转字符串 用 全局时间范围统计 的 范围归一化次数。
    做 打印 用 做 字符串拼接 用 「│ 轨道创建:         」、做 转字符串 用 全局时间范围统计 的 轨道创建次数。
    做 打印 用 做 字符串拼接 用 「│ 轨道销毁:         」、做 转字符串 用 全局时间范围统计 的 轨道销毁次数。
    做 打印 用 做 字符串拼接 用 「│ 轨道切换:         」、做 转字符串 用 全局时间范围统计 的 轨道切换次数。
    做 打印 用 做 字符串拼接 用 「│ Cue添加:          」、做 转字符串 用 全局时间范围统计 的 Cue添加次数。
    做 打印 用 做 字符串拼接 用 「│ 活跃Cue更新:      」、做 转字符串 用 全局时间范围统计 的 活跃Cue更新次数。
    做 打印 用 做 字符串拼接 用 「│ AI轨道调用:       」、做 转字符串 用 全局时间范围统计 的 AI轨道调用次数。
    做 打印 用 做 字符串拼接 用 「│ AI轨道命中:       」、做 转字符串 用 全局时间范围统计 的 AI轨道命中次数。
    做 打印 用 做 字符串拼接 用 「│ 范围总时长(ms):   」、做 转字符串 用 全局时间范围统计 的 范围总长度_毫秒。
    做 打印 用 做 字符串拼接 用 「│ 错误:             」、做 转字符串 用 全局时间范围统计 的 错误总数。
    做 打印 用 「└──────────────────────────────────────┘」。
完。

时间范围_导出诊断 接受 零参：
    若 全局轨道AI 等于 空：
        做 打印 用 「[轨道AI] 未初始化」。
        返还。
    完。
    AI 为 全局轨道AI。
    做 打印 用 「[轨道AI] === 诊断 ===」。
    做 打印 用 做 字符串拼接 用 「  历史音频率(Q10): 」、做 转字符串 用 AI 的 历史音频轨道率。
    做 打印 用 做 字符串拼接 用 「  历史视频率(Q10): 」、做 转字符串 用 AI 的 历史视频轨道率。
    做 打印 用 做 字符串拼接 用 「  历史字幕率(Q10): 」、做 转字符串 用 AI 的 历史字幕轨道率。
    做 打印 用 做 字符串拼接 用 「  平均轨道数(Q10): 」、做 转字符串 用 AI 的 历史平均轨道数。
    做 打印 用 做 字符串拼接 用 「  预测有字幕: 」、做 转字符串 用 AI 的 预测有字幕。
完。

时间范围_获取统计 接受 零参：
    返还 全局时间范围统计。
完。

时间范围_获取AI状态 接受 零参：
    返还 全局轨道AI。
完。

时间范围_是否已初始化 接受 零参：
    返还 全局时间范围已初始化。
完。

本章 收束。
