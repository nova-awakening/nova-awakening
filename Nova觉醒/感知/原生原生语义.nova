《原生原生语义》

引用《.·内核直连层》。

引用《..·推理·通信总线》。

引用《..·推理·定点Q算术》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

元素_节点 为 1。

ATTRIBUTE_节点 为 2。

TEXT_节点 为 3。

CDATA_SECTION_节点 为 4。

PROCESSING_INSTRUCTION_节点 为 7。

COMMENT_节点 为 8。

DOCUMENT_节点 为 9。

DOCUMENT_TYPE_节点 为 10。

DOCUMENT_FRAGMENT_节点 为 11。

节点标志_是容器节点 为 0x0001。

节点标志_是元素 为 0x0002。

节点标志_是文本 为 0x0004。

节点标志_是文档 为 0x0008。

节点标志_是文档片段 为 0x0010。

节点标志_已连接 为 0x0020。

节点标志_有事件监听器 为 0x0040。

节点标志_已改样式 为 0x0080。

节点标志_已改布局 为 0x0100。

节点标志_子树已改 为 0x0200。

节点标志_有渲染对象 为 0x0400。

节点标志_已分配布局 为 0x0800。

节点标志_自定义元素 为 0x1000。

节点标志_AI已分析 为 0x2000。

节点标志_高注意力 为 0x4000。

节点标志_可交互预测 为 0x8000。

文档位置_断开 为 0x01。

文档位置_在前 为 0x02。

文档位置_在后 为 0x04。

文档位置_包含 为 0x08。

文档位置_被包含 为 0x10。

插入类型_普通 为 0。

插入类型_解析器 为 1。

语义_未知 为 0。

语义_导航栏 为 1。

语义_页头 为 2。

语义_页脚 为 3。

语义_正文 为 4。

语义_侧边栏 为 5。

语义_标题 为 6。

语义_段落 为 7。

语义_列表 为 8。

语义_表单 为 9。

语义_按钮 为 10。

语义_链接 为 11。

语义_图片 为 12。

语义_视频 为 13。

语义_广告 为 14。

语义_评论区 为 15。

语义_搜索框 为 16。

语义_弹窗 为 17。

语义_代码 为 18。

语义_引用 为 19。

语义_卡片 为 20。

语义_轮播 为 21。

语义_面包屑 为 22。

语义_标签页 为 23。

语义_加载指示 为 24。

语义_Cookie同意 为 25。

意图_无 为 0。

意图_将阅读 为 1。

意图_将点击 为 2。

意图_将滚过 为 3。

意图_将输入 为 4。

意图_将关闭 为 5。

意图_将复制 为 6。

意图_将分享 为 7。

可变 NSAN_创建总数 为 0。

可变 NSAN_释放总数 为 0。

可变 NSAN_插入总数 为 0。

可变 NSAN_移除总数 为 0。

可变 NSAN_AI分析总数 为 0。

可变 NSAN_下一节点ID 为 1。

NovaNode 是 一种 事物，含 id、类型、标志、父、第一子、最后子、前兄弟、后兄弟、文档、语义角色、意图、注意力权重、标签名、命名空间、属性列表、属性数量、文本数据、文本长度、事件列表、事件数量、渲染对象、布局盒。

NovaNode_创建 接受 类型：
    n 为 做 堆_分配 用 NovaNode。
    若 n 等于 0：
        返还 0。
    完。
    n 的 id 为 NSAN_下一节点ID。
    NSAN_下一节点ID 为 NSAN_下一节点ID 加 1。
    n 的 类型 为 类型。
    n 的 标志 为 0。
    n 的 父 为 0。
    n 的 第一子 为 0。
    n 的 最后子 为 0。
    n 的 前兄弟 为 0。
    n 的 后兄弟 为 0。
    n 的 文档 为 0。
    n 的 语义角色 为 语义_未知。
    n 的 意图 为 意图_无。
    n 的 注意力权重 为 512。
    n 的 标签名 为 0。
    n 的 命名空间 为 0。
    n 的 属性列表 为 0。
    n 的 属性数量 为 0。
    n 的 文本数据 为 0。
    n 的 文本长度 为 0。
    n 的 事件列表 为 0。
    n 的 事件数量 为 0。
    n 的 渲染对象 为 0。
    n 的 布局盒 为 0。
    若 类型 等于 元素_节点：
        n 的 标志 为 n 的 标志 位或 节点标志_是元素 位或 节点标志_是容器节点。
    完。
    若 类型 等于 TEXT_节点：
        n 的 标志 为 n 的 标志 位或 节点标志_是文本。
    完。
    若 类型 等于 DOCUMENT_节点：
        n 的 标志 为 n 的 标志 位或 节点标志_是文档 位或 节点标志_是容器节点。
    完。
    若 类型 等于 DOCUMENT_FRAGMENT_节点：
        n 的 标志 为 n 的 标志 位或 节点标志_是文档片段 位或 节点标志_是容器节点。
    完。
    NSAN_创建总数 为 NSAN_创建总数 加 1。
    返还 n。
完。

NovaNode_追加子 接受 父、子：
    若 父 等于 0 或者 子 等于 0：
        返还。
    完。
    若 子 的 父 不等于 0：
        做 NovaNode_移除自己 用 子。
    完。
    子 的 父 为 父。
    子 的 前兄弟 为 父 的 最后子。
    子 的 后兄弟 为 0。
    若 父 的 最后子 不等于 0：
        父 的 最后子 的 后兄弟 为 子。
    否则：
        父 的 第一子 为 子。
    完。
    父 的 最后子 为 子。
    子 的 文档 为 父 的 文档。
    若 父 的 标志 位与 节点标志_已连接：
        子 的 标志 为 子 的 标志 位或 节点标志_已连接。
    完。
    NSAN_插入总数 为 NSAN_插入总数 加 1。
完。

NovaNode_插入之前 接受 父、子、参考节点：
    若 父 等于 0 或者 子 等于 0：
        返还。
    完。
    若 参考节点 等于 0：
        做 NovaNode_追加子 用 父、子。
        返还。
    完。
    若 子 的 父 不等于 0：
        做 NovaNode_移除自己 用 子。
    完。
    子 的 父 为 父。
    子 的 后兄弟 为 参考节点。
    子 的 前兄弟 为 参考节点 的 前兄弟。
    若 参考节点 的 前兄弟 不等于 0：
        参考节点 的 前兄弟 的 后兄弟 为 子。
    否则：
        父 的 第一子 为 子。
    完。
    参考节点 的 前兄弟 为 子。
    子 的 文档 为 父 的 文档。
    NSAN_插入总数 为 NSAN_插入总数 加 1。
完。

NovaNode_移除自己 接受 节点：
    若 节点 等于 0 或者 节点 的 父 等于 0：
        返还。
    完。
    p 为 节点 的 父。
    若 节点 的 前兄弟 不等于 0：
        节点 的 前兄弟 的 后兄弟 为 节点 的 后兄弟。
    否则：
        p 的 第一子 为 节点 的 后兄弟。
    完。
    若 节点 的 后兄弟 不等于 0：
        节点 的 后兄弟 的 前兄弟 为 节点 的 前兄弟。
    否则：
        p 的 最后子 为 节点 的 前兄弟。
    完。
    节点 的 父 为 0。
    节点 的 前兄弟 为 0。
    节点 的 后兄弟 为 0。
    节点 的 标志 为 节点 的 标志 位与 按位取反 节点标志_已连接。
    NSAN_移除总数 为 NSAN_移除总数 加 1。
完。

NovaNode_子节点数量 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 c 为 0。
    可变 ch 为 节点 的 第一子。
    只要 ch 不等于 0：
        c 为 c 加 1。
        ch 为 ch 的 后兄弟。
    完。
    返还 c。
完。

NovaNode_深度 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 d 为 0。
    可变 p 为 节点 的 父。
    只要 p 不等于 0：
        d 为 d 加 1。
        p 为 p 的 父。
    完。
    返还 d。
完。

NovaNode_是祖先 接受 祖先、后代：
    若 祖先 等于 0 或者 后代 等于 0：
        返还 假。
    完。
    可变 p 为 后代 的 父。
    只要 p 不等于 0：
        若 p 等于 祖先：
            返还 真。
        完。
        p 为 p 的 父。
    完。
    返还 假。
完。

NovaNode_比较文档位置 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 文档位置_断开。
    完。
    若 a 等于 b：
        返还 0。
    完。
    若 做 NovaNode_是祖先 用 a、b：
        返还 文档位置_包含 位或 文档位置_在前。
    完。
    若 做 NovaNode_是祖先 用 b、a：
        返还 文档位置_被包含 位或 文档位置_在后。
    完。
    返还 文档位置_在前。
完。

NovaAttr 是 一种 事物，含 名称、值。

NovaNode_获取属性 接受 节点、名称：
    若 节点 等于 0 或者 节点 的 属性列表 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 节点 的 属性数量：
        a 为 做 内存_读64 用 节点 的 属性列表 加 i 乘 8。
        若 a 不等于 0 并且 做 字符串_相等 用 a 的 名称、名称：
            返还 a 的 值。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

NovaNode_设置属性 接受 节点、名称、值：
    若 节点 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 节点 的 属性列表 不等于 0 并且 i 小于 节点 的 属性数量：
        a 为 做 内存_读64 用 节点 的 属性列表 加 i 乘 8。
        若 a 不等于 0 并且 做 字符串_相等 用 a 的 名称、名称：
            a 的 值 为 值。
            节点 的 标志 为 节点 的 标志 位或 节点标志_已改样式。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 节点 的 属性列表 等于 0：
        节点 的 属性列表 为 做 堆_分配 用 16 乘 8。
    完。
    attr 为 做 堆_分配 用 NovaAttr。
    attr 的 名称 为 名称。
    attr 的 值 为 值。
    做 内存_写64 用 节点 的 属性列表 加 节点 的 属性数量 乘 8、attr。
    节点 的 属性数量 为 节点 的 属性数量 加 1。
    节点 的 标志 为 节点 的 标志 位或 节点标志_已改样式。
完。

NovaNode_移除属性 接受 节点、名称：
    若 节点 等于 0 或者 节点 的 属性列表 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 节点 的 属性数量：
        a 为 做 内存_读64 用 节点 的 属性列表 加 i 乘 8。
        若 a 不等于 0 并且 做 字符串_相等 用 a 的 名称、名称：
            做 堆_释放 用 a。
            可变 j 为 i。
            只要 j 小于 节点 的 属性数量 减 1：
                做 内存_写64 用 节点 的 属性列表 加 j 乘 8、做 内存_读64 用 节点 的 属性列表 加 (j 加 1) 乘 8。
                j 为 j 加 1。
            完。
            节点 的 属性数量 为 节点 的 属性数量 减 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

NovaNode_获取文本内容 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    若 节点 的 类型 等于 TEXT_节点：
        返还 节点 的 文本数据。
    完。
    可变 ch 为 节点 的 第一子。
    若 ch 等于 0：
        返还 「」。
    完。
    若 ch 的 后兄弟 等于 0 并且 ch 的 类型 等于 TEXT_节点：
        返还 ch 的 文本数据。
    完。
    返还 0。
完。

NovaNode_设置文本内容 接受 节点、文本：
    若 节点 等于 0：
        返还。
    完。
    若 节点 的 类型 等于 TEXT_节点：
        节点 的 文本数据 为 文本。
        节点 的 文本长度 为 做 字符串_长度 用 文本。
        返还。
    完。
    可变 ch 为 节点 的 第一子。
    只要 ch 不等于 0：
        next 为 ch 的 后兄弟。
        做 NovaNode_移除自己 用 ch。
        ch 为 next。
    完。
    若 文本 不等于 0 并且 做 字符串_长度 用 文本 大于 0：
        tn 为 做 NovaNode_创建 用 TEXT_节点。
        tn 的 文本数据 为 文本。
        tn 的 文本长度 为 做 字符串_长度 用 文本。
        做 NovaNode_追加子 用 节点、tn。
    完。
完。

NovaNode_设置标志 接受 节点、标志：
    若 节点 等于 0：
        返还。
    完。
    节点 的 标志 为 节点 的 标志 位或 标志。
完。

NovaNode_清除标志 接受 节点、标志：
    若 节点 等于 0：
        返还。
    完。
    节点 的 标志 为 节点 的 标志 位与 按位取反 标志。
完。

NovaNode_有标志 接受 节点、标志：
    若 节点 等于 0：
        返还 假。
    完。
    返还 节点 的 标志 位与 标志 不等于 0。
完。

NovaNode_标记脏 接受 节点：
    若 节点 等于 0：
        返还。
    完。
    节点 的 标志 为 节点 的 标志 位或 节点标志_已改样式 位或 节点标志_已改布局。
    可变 p 为 节点 的 父。
    只要 p 不等于 0：
        p 的 标志 为 p 的 标志 位或 节点标志_子树已改。
        p 为 p 的 父。
    完。
完。

NSAN_AI提取特征 接受 节点：
    t 为 做 Tensor_创建 用 10。
    若 t 等于 0：
        返还 0。
    完。
    做 Tensor_写 用 t、0、节点 的 类型。
    做 Tensor_写 用 t、1、节点 的 语义角色。
    做 Tensor_写 用 t、2、节点 的 意图。
    做 Tensor_写 用 t、3、节点 的 注意力权重。
    做 Tensor_写 用 t、4、做 NovaNode_子节点数量 用 节点。
    做 Tensor_写 用 t、5、做 NovaNode_深度 用 节点。
    做 Tensor_写 用 t、6、节点 的 属性数量。
    做 Tensor_写 用 t、7、节点 的 文本长度。
    做 Tensor_写 用 t、8、节点 的 标志。
    做 Tensor_写 用 t、9、NSAN_AI分析总数。
    返还 t。
完。

NSAN_AI推断语义角色 接受 节点：
    若 节点 等于 0：
        返还 语义_未知。
    完。
    t 为 做 NSAN_AI提取特征 用 节点。
    若 t 等于 0：
        返还 语义_未知。
    完。
    结果 为 做 AI总线_推理 用 「nsan_semantic」、t。
    做 全局AI总线 的 发布 用 「nsan_role」、「infer」、结果。
    做 Tensor_释放 用 t。
    节点 的 语义角色 为 结果。
    节点 的 标志 为 节点 的 标志 位或 节点标志_AI已分析。
    NSAN_AI分析总数 为 NSAN_AI分析总数 加 1。
    返还 结果。
完。

NSAN_AI预测意图 接受 节点：
    若 节点 等于 0：
        返还 意图_无。
    完。
    t 为 做 NSAN_AI提取特征 用 节点。
    若 t 等于 0：
        返还 意图_无。
    完。
    结果 为 做 AI总线_推理 用 「nsan_intent」、t。
    做 Tensor_释放 用 t。
    节点 的 意图 为 结果。
    返还 结果。
完。

NSAN_获取统计 接受 零参：
    返还 一组：NSAN_创建总数、NSAN_释放总数、NSAN_插入总数、NSAN_移除总数、NSAN_AI分析总数。
完。

NSAN_重置统计 接受 零参：
    NSAN_创建总数 为 0。
    NSAN_释放总数 为 0。
    NSAN_插入总数 为 0。
    NSAN_移除总数 为 0。
    NSAN_AI分析总数 为 0。
完。

NSAN_自检 接受 零参：
    doc 为 做 NovaNode_创建 用 DOCUMENT_节点。
    若 doc 等于 0：
        返还 假。
    完。
    div 为 做 NovaNode_创建 用 元素_节点。
    txt 为 做 NovaNode_创建 用 TEXT_节点。
    做 NovaNode_追加子 用 doc、div。
    做 NovaNode_追加子 用 div、txt。
    若 做 NovaNode_子节点数量 用 doc 不等于 1：
        返还 假。
    完。
    若 做 NovaNode_深度 用 txt 不等于 2：
        返还 假。
    完。
    若 非 做 NovaNode_是祖先 用 doc、txt：
        返还 假。
    完。
    做 NovaNode_移除自己 用 div。
    若 做 NovaNode_子节点数量 用 doc 不等于 0：
        返还 假。
    完。
    返还 真。
完。

NovaNode_释放 接受 节点：
    若 节点 等于 0：
        返还。
    完。
    可变 ch 为 节点 的 第一子。
    只要 ch 不等于 0：
        next 为 ch 的 后兄弟。
        做 NovaNode_释放 用 ch。
        ch 为 next。
    完。
    若 节点 的 属性列表 不等于 0：
        可变 i 为 0。
        只要 i 小于 节点 的 属性数量：
            a 为 做 内存_读64 用 节点 的 属性列表 加 i 乘 8。
            若 a 不等于 0：
                做 堆_释放 用 a。
            完。
            i 为 i 加 1。
        完。
        做 堆_释放 用 节点 的 属性列表。
    完。
    若 节点 的 事件列表 不等于 0：
        做 堆_释放 用 节点 的 事件列表。
    完。
    做 堆_释放 用 节点。
    NSAN_释放总数 为 NSAN_释放总数 加 1。
完。

本章 收束。
