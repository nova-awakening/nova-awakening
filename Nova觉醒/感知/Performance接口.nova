《Performance接口》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《.·空闲》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《.·事件微任务》。

引用《.·超时》。

引用《.·事件循环主体》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内容可见性》。

引用《..·中枢·时钟中枢》。

可变 性能时间原点 为 0。

Performance_now 接受 零参：
    若 性能时间原点 等于 0：
        性能时间原点 为 做 平台_获取高精度时间。
    完。
    返还 做 平台_获取高精度时间 减 性能时间原点。
完。

Performance_timeOrigin 接受 零参：
    若 性能时间原点 等于 0：
        性能时间原点 为 做 平台_获取高精度时间。
    完。
    返还 性能时间原点。
完。

Scheduler_postTask 接受 回调、优先级、delay：
    若 回调 等于 0：
        返还。
    完。
    若 delay 大于 0：
        t 为 做 setTimeout 用 回调、delay。
        返还。
    完。
    任务优先级 为 TASK_PRIORITY_USER_VISIBLE。
    若 做 字符串_相等 用 优先级、「user-blocking」：
        任务优先级 为 TASK_PRIORITY_USER_BLOCKING。
    否则若 做 字符串_相等 用 优先级、「background」：
        任务优先级 为 TASK_PRIORITY_BACKGROUND。
    完。
    做 事件Loop_入队宏任务 用 回调、任务优先级。
完。

Scheduler_yield 接受 零参：
    loop 为 做 事件Loop_获取。
    若 loop 等于 0：
        返还。
    完。
    做 Microtask_清空。
    loop 的 已让出 为 真。
完。

智能_事件Loop_帧预算 接受 loop：
    若 loop 等于 0：
        返还 FRAME_BUDGET_MS。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 FRAME_BUDGET_MS。
    完。
    可变 平均耗时 为 0。
    可变 有效帧数 为 0。
    可变 i 为 0。
    只要 i 小于 16：
        若 loop 的 最近帧耗时 的 第 i 项 大于 0：
            平均耗时 为 平均耗时 加 loop 的 最近帧耗时 的 第 i 项。
            有效帧数 为 有效帧数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 有效帧数 大于 0：
        平均耗时 为 平均耗时 除以 有效帧数。
    完。
    若 平均耗时 大于 20：
        返还 33。
    完。
    若 平均耗时 小于 8：
        返还 8。
    完。
    返还 FRAME_BUDGET_MS。
完。

智能_事件Loop_长任务检测 接受 任务耗时、回调：
    若 任务耗时 小于或等于 LONG_TASK_MS：
        返还。
    完。
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
完。

智能_事件Loop_优先级排序 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    特征 为 做 Tensor_创建 用 3。
    做 Tensor_写 用 特征、0、做 读取毫秒。
    做 Tensor_写 用 特征、1、0。
    做 Tensor_写 用 特征、2、0。
    建议 为 做 AI总线_推理 用 bus、特征。
    做 Tensor_释放 用 特征。
    全局NHPT统计 的 AI推理次数 为 全局NHPT统计 的 AI推理次数 加 1。
完。

可变 全局NHPT统计 为 新建 NHPT统计结构。

NHPT统计结构 是 一种 事物，含 性能标记次数、性能度量次数、资源时序次数、长任务检测次数、AI推理次数、总帧数、掉帧数。

标记最大数 为 256。

性能标记项 是 一种 事物，含 名称、时间戳、详细、类型。

性能时间线 是 一种 事物，含 标记列表、标记数、最大标记数。

可变 全局性能时间线 为 空。

性能时间线_初始化 接受 零参：
    全局性能时间线 为 做 堆_分配 用 性能时间线。
    若 全局性能时间线 等于 0：
        返还。
    完。
    全局性能时间线 的 标记列表 为 做 堆_分配 用 标记最大数 乘 8。
    全局性能时间线 的 标记数 为 0。
    全局性能时间线 的 最大标记数 为 标记最大数。
完。

Performance_mark 接受 名称：
    若 全局性能时间线 等于 空 或者 全局性能时间线 的 标记数 大于或等于 全局性能时间线 的 最大标记数：
        返还 空。
    完。
    entry 为 做 堆_分配 用 性能标记项。
    若 entry 等于 0：
        返还 空。
    完。
    entry 的 名称 为 名称。
    entry 的 时间戳 为 做 Performance_now。
    entry 的 详细 为 0。
    entry 的 类型 为 0。
    做 内存_写64 用 全局性能时间线 的 标记列表 加 全局性能时间线 的 标记数 乘 8、entry。
    全局性能时间线 的 标记数 为 全局性能时间线 的 标记数 加 1。
    全局NHPT统计 的 性能标记次数 为 全局NHPT统计 的 性能标记次数 加 1。
    返还 entry。
完。

Performance_measure 接受 名称、开始标记、结束标记：
    若 全局性能时间线 等于 空：
        返还 空。
    完。
    可变 开始时间 为 0。
    可变 结束时间 为 做 Performance_now。
    若 开始标记 不等于 0：
        可变 i 为 0。
        只要 i 小于 全局性能时间线 的 标记数：
            e 为 做 内存_读64 用 全局性能时间线 的 标记列表 加 i 乘 8。
            若 e 不等于 0 并且 做 字符串_相等 用 e 的 名称、开始标记：
                开始时间 为 e 的 时间戳。
            完。
            i 为 i 加 1。
        完。
    完。
    若 结束标记 不等于 0：
        可变 j 为 0。
        只要 j 小于 全局性能时间线 的 标记数：
            e 为 做 内存_读64 用 全局性能时间线 的 标记列表 加 j 乘 8。
            若 e 不等于 0 并且 做 字符串_相等 用 e 的 名称、结束标记：
                结束时间 为 e 的 时间戳。
            完。
            j 为 j 加 1。
        完。
    完。
    entry 为 做 堆_分配 用 性能标记项。
    若 entry 等于 0：
        返还 空。
    完。
    entry 的 名称 为 名称。
    entry 的 时间戳 为 开始时间。
    entry 的 详细 为 结束时间 减 开始时间。
    entry 的 类型 为 1。
    全局NHPT统计 的 性能度量次数 为 全局NHPT统计 的 性能度量次数 加 1。
    返还 entry。
完。

Performance_clearMarks 接受 名称：
    若 全局性能时间线 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 全局性能时间线 的 标记数：
        e 为 做 内存_读64 用 全局性能时间线 的 标记列表 加 i 乘 8。
        若 e 不等于 0 并且 e 的 类型 等于 0：
            若 名称 等于 0 或者 做 字符串_相等 用 e 的 名称、名称：
                做 堆_释放 用 e。
                做 内存_写64 用 全局性能时间线 的 标记列表 加 i 乘 8、0。
            完。
        完。
        i 为 i 加 1。
    完。
完。

资源时序项 是 一种 事物，含 名称、开始时间、结束时间、传输大小、协议、状态码、DNS开始、DNS结束、连接开始、连接结束、请求开始、响应开始、响应结束。

资源时序最大 为 150。

可变 资源时序列表 为 空。

可变 资源时序数量 为 0。

资源时序_记录 接受 url、传输大小、状态码：
    若 资源时序列表 等于 空：
        资源时序列表 为 做 堆_分配 用 资源时序最大 乘 8。
        若 资源时序列表 等于 0：
            返还 空。
        完。
    完。
    若 资源时序数量 大于或等于 资源时序最大：
        返还 空。
    完。
    entry 为 做 堆_分配 用 资源时序项。
    若 entry 等于 0：
        返还 空。
    完。
    entry 的 名称 为 url。
    entry 的 开始时间 为 做 Performance_now。
    entry 的 传输大小 为 传输大小。
    entry 的 状态码 为 状态码。
    做 内存_写64 用 资源时序列表 加 资源时序数量 乘 8、entry。
    资源时序数量 为 资源时序数量 加 1。
    全局NHPT统计 的 资源时序次数 为 全局NHPT统计 的 资源时序次数 加 1。
    返还 entry。
完。

资源时序_完成 接受 entry：
    若 entry 等于 空：
        返还。
    完。
    entry 的 结束时间 为 做 Performance_now。
完。

Performance_getEntriesByType 接受 类型名称：
    若 做 字符串_相等 用 类型名称、「资源」：
        返还 资源时序列表。
    完。
    若 做 字符串_相等 用 类型名称、「标记」：
        若 全局性能时间线 不等于 空：
            返还 全局性能时间线 的 标记列表。
        完。
    完。
    返还 空。
完。

可变 全局FID 为 0。

可变 全局LCP 为 0。

可变 全局INP 为 0。

可变 INP交互计数 为 0。

可变 INP总延迟 为 0。

WebVitals_记录FID 接受 延迟_ms：
    若 全局FID 等于 0：
        全局FID 为 延迟_ms。
    完。
完。

WebVitals_记录LCP 接受 时间_ms：
    全局LCP 为 时间_ms。
完。

WebVitals_记录交互 接受 延迟_ms：
    INP交互计数 为 INP交互计数 加 1。
    INP总延迟 为 INP总延迟 加 延迟_ms。
    若 延迟_ms 大于 全局INP：
        全局INP 为 延迟_ms。
    完。
完。

WebVitals_获取 接受 零参：
    返还 一本字典：FID 对应 全局FID、LCP 对应 全局LCP、INP 对应 全局INP。
完。

可变 上一帧时间 为 0。

可变 帧率监控_缓冲 为 空。

可变 帧率监控_索引 为 0。

帧率监控_初始化 接受 零参：
    帧率监控_缓冲 为 做 堆_分配 用 64 乘 8。
    若 帧率监控_缓冲 不等于 0：
        做 内存_置零 用 帧率监控_缓冲、64 乘 8。
    完。
    帧率监控_索引 为 0。
    上一帧时间 为 0。
完。

帧率监控_记录帧 接受 零参：
    现在 为 做 Performance_now。
    若 上一帧时间 大于 0：
        帧时间 为 现在 减 上一帧时间。
        全局NHPT统计 的 总帧数 为 全局NHPT统计 的 总帧数 加 1。
        若 帧时间 大于 33：
            全局NHPT统计 的 掉帧数 为 全局NHPT统计 的 掉帧数 加 1。
        完。
        若 帧率监控_缓冲 不等于 0：
            做 内存_写64 用 帧率监控_缓冲 加 帧率监控_索引 取余于 64 乘 8、帧时间。
            帧率监控_索引 为 帧率监控_索引 加 1。
        完。
    完。
    上一帧时间 为 现在。
完。

帧率监控_获取平均FPS 接受 零参：
    若 帧率监控_缓冲 等于 空：
        返还 0。
    完。
    可变 总时间 为 0。
    可变 有效帧 为 0。
    可变 i 为 0。
    只要 i 小于 64：
        t 为 做 内存_读64 用 帧率监控_缓冲 加 i 乘 8。
        若 t 大于 0：
            总时间 为 总时间 加 t。
            有效帧 为 有效帧 加 1。
        完。
        i 为 i 加 1。
    完。
    若 有效帧 等于 0 或者 总时间 等于 0：
        返还 0。
    完。
    返还 1000 乘 有效帧 除以 总时间。
完。

NHPT_初始化 接受 零参：
    做 内核打印 用 「[NHPT] Nova高精度性能时间智能 启动」。
    做 性能时间线_初始化。
    做 帧率监控_初始化。
    全局NHPT统计 的 性能标记次数 为 0。
    全局NHPT统计 的 性能度量次数 为 0。
    全局NHPT统计 的 资源时序次数 为 0。
    全局NHPT统计 的 长任务检测次数 为 0。
    全局NHPT统计 的 AI推理次数 为 0。
    全局NHPT统计 的 总帧数 为 0。
    全局NHPT统计 的 掉帧数 为 0。
    做 内核打印 用 「[NHPT] 初始化完成 — performance.now/mark/measure/资源时序/Web Vitals/帧率监控 就绪」。
完。

NHPT_打印诊断 接受 零参：
    做 内核打印 用 「╔════════════════════════════════════════════╗」。
    做 内核打印 用 「║  NHPT 高精度性能时间 诊断报告             ║」。
    做 内核打印 用 「╠════════════════════════════════════════════╣」。
    做 内核打印 用 「║ 性能标记:     {}」、全局NHPT统计 的 性能标记次数。
    做 内核打印 用 「║ 性能度量:     {}」、全局NHPT统计 的 性能度量次数。
    做 内核打印 用 「║ 资源时序:     {}」、全局NHPT统计 的 资源时序次数。
    做 内核打印 用 「║ 总帧数:       {}」、全局NHPT统计 的 总帧数。
    做 内核打印 用 「║ 掉帧数:       {}」、全局NHPT统计 的 掉帧数。
    做 内核打印 用 「║ FID:          {}ms」、全局FID。
    做 内核打印 用 「║ LCP:          {}ms」、全局LCP。
    做 内核打印 用 「║ INP:          {}ms」、全局INP。
    做 内核打印 用 「║ 平均FPS:      {}」、做 帧率监控_获取平均FPS。
    做 内核打印 用 「║ 长任务检测:   {}」、全局NHPT统计 的 长任务检测次数。
    做 内核打印 用 「║ AI推理:       {}」、全局NHPT统计 的 AI推理次数。
    做 内核打印 用 「╚════════════════════════════════════════════╝」。
完。

长任务阈值 为 50。

长任务记录最大 为 64。

长任务记录项 是 一种 事物，含 开始时间、持续时间、任务类型、可拆分。

可变 长任务列表 为 空。

可变 长任务数量 为 0。

长任务_初始化 接受 零参：
    长任务列表 为 做 堆_分配 用 长任务记录最大 乘 8。
    若 长任务列表 不等于 0：
        做 内存_置零 用 长任务列表、长任务记录最大 乘 8。
    完。
    长任务数量 为 0。
完。

长任务_检测 接受 开始、任务类型：
    现在 为 做 Performance_now。
    持续 为 现在 减 开始。
    若 持续 小于 长任务阈值：
        返还 0。
    完。
    全局NHPT统计 的 长任务检测次数 为 全局NHPT统计 的 长任务检测次数 加 1。
    若 长任务列表 不等于 空 并且 长任务数量 小于 长任务记录最大：
        rec 为 做 堆_分配 用 长任务记录项。
        若 rec 不等于 0：
            rec 的 开始时间 为 开始。
            rec 的 持续时间 为 持续。
            rec 的 任务类型 为 任务类型。
            rec 的 可拆分 为 0。
            bus 为 全局AI总线。
            若 bus 不等于 0：
                feat 为 做 Tensor_创建 用 3。
                做 Tensor_写 用 feat、0、持续。
                做 Tensor_写 用 feat、1、任务类型。
                做 Tensor_写 用 feat、2、长任务数量。
                风险 为 做 AI总线_风险评分 用 bus、feat。
                若 风险 小于 50：
                    rec 的 可拆分 为 1。
                完。
                做 Tensor_释放 用 feat。
                全局NHPT统计 的 AI推理次数 为 全局NHPT统计 的 AI推理次数 加 1。
            完。
            做 内存_写64 用 长任务列表 加 长任务数量 乘 8、rec。
            长任务数量 为 长任务数量 加 1。
        完。
    完。
    返还 持续。
完。

长任务_获取最近 接受 数量：
    若 长任务列表 等于 空 或者 长任务数量 等于 0：
        返还 空。
    完。
    起始 为 0。
    若 长任务数量 大于 数量：
        起始 为 长任务数量 减 数量。
    完。
    返还 长任务列表 加 起始 乘 8。
完。

内存压力_无 为 0。

内存压力_中 为 1。

内存压力_高 为 2。

内存压力_严重 为 3。

内存压力状态 是 一种 事物，含 当前等级、总内存、已用内存、GC次数、上次检查时间。

可变 全局内存压力 为 新建 内存压力状态。

内存压力_检查 接受 零参：
    现在 为 做 Performance_now。
    全局内存压力 的 上次检查时间 为 现在。
    总 为 做 内核_查询内存总量。
    用 为 做 内核_查询内存已用。
    全局内存压力 的 总内存 为 总。
    全局内存压力 的 已用内存 为 用。
    若 总 等于 0：
        返还。
    完。
    占比 为 用 乘 100 除以 总。
    若 占比 大于或等于 95：
        全局内存压力 的 当前等级 为 内存压力_严重。
    否则若 占比 大于或等于 85：
        全局内存压力 的 当前等级 为 内存压力_高。
    否则若 占比 大于或等于 70：
        全局内存压力 的 当前等级 为 内存压力_中。
    否则：
        全局内存压力 的 当前等级 为 内存压力_无。
    完。
完。

内存压力_获取等级 接受 零参：
    返还 全局内存压力 的 当前等级。
完。

可变 帧预算_目标ms 为 16。

可变 帧预算_剩余ms 为 16。

帧预算_开始 接受 零参：
    fps 为 做 帧率监控_获取平均FPS。
    pressure 为 做 内存压力_获取等级。
    bus 为 全局AI总线。
    若 bus 不等于 0：
        feat 为 做 Tensor_创建 用 4。
        做 Tensor_写 用 feat、0、fps。
        做 Tensor_写 用 feat、1、pressure。
        做 Tensor_写 用 feat、2、全局NHPT统计 的 掉帧数。
        做 Tensor_写 用 feat、3、长任务数量。
        结果 为 做 AI总线_推理 用 bus、feat。
        做 Tensor_释放 用 feat。
        全局NHPT统计 的 AI推理次数 为 全局NHPT统计 的 AI推理次数 加 1。
        若 结果 大于 0 并且 全局NHPT统计 的 掉帧数 大于 全局NHPT统计 的 总帧数 除以 10：
            帧预算_目标ms 为 33。
        否则：
            帧预算_目标ms 为 16。
        完。
    完。
    帧预算_剩余ms 为 帧预算_目标ms。
完。

帧预算_消耗 接受 ms：
    帧预算_剩余ms 为 帧预算_剩余ms 减 ms。
    若 帧预算_剩余ms 小于 0：
        帧预算_剩余ms 为 0。
    完。
完。

帧预算_剩余时间 接受 零参：
    返还 帧预算_剩余ms。
完。

NHPT_启动全部 接受 零参：
    做 NHPT_初始化。
    做 长任务_初始化。
    全局内存压力 为 新建 内存压力状态。
    全局内存压力 的 当前等级 为 内存压力_无。
    做 内核打印 用 「[NHPT] 全子系统启动完成: 时间线/帧率/长任务/内存压力/AI调度」。
完。

NHPT_每帧刷新 接受 零参：
    做 帧率监控_记录帧。
    做 帧预算_开始。
    若 全局NHPT统计 的 总帧数 取余于 10 等于 0：
        做 内存压力_检查。
    完。
完。

本章 收束。
