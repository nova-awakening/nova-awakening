《帧管线获取》

引用《.·文档事件》。

引用《.·样式解析》。

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《.·兼容桥》。

引用《..·推理·定点Q算术》。

引用《..·中枢·内容可见性》。

引用《.·元素》。

FramePipeline 是 一种 事物，含 输入处理耗时、rAF耗时、微任务耗时、样式耗时、布局耗时、绘制耗时、合成耗时、帧号、帧开始时间、帧结束时间、帧总耗时、是否掉帧、连续掉帧、需要样式、需要布局、需要绘制、需要合成。

可变 全局帧管线 为 0。

FramePipeline_获取 接受 零参：
    若 全局帧管线 等于 0：
        全局帧管线 为 做 堆_分配 用 FramePipeline。
        全局帧管线 的 帧号 为 0。
        全局帧管线 的 连续掉帧 为 0。
        全局帧管线 的 需要样式 为 假。
        全局帧管线 的 需要布局 为 假。
        全局帧管线 的 需要绘制 为 假。
        全局帧管线 的 需要合成 为 假。
    完。
    返还 全局帧管线。
完。

Frame_执行一帧 接受 零参：
    fp 为 做 FramePipeline_获取。
    cfg 为 做 Config_获取。
    帧预算 为 cfg 的 帧预算us。
    fp 的 帧开始时间 为 做 平台_获取高精度时间。
    fp 的 帧号 为 fp 的 帧号 加 1。
    t0 为 做 平台_获取高精度时间。
    做 事件Loop_处理输入事件。
    fp 的 输入处理耗时 为 做 平台_获取高精度时间 减 t0。
    t1 为 做 平台_获取高精度时间。
    做 事件Loop_执行rAF。
    fp 的 rAF耗时 为 做 平台_获取高精度时间 减 t1。
    t2 为 做 平台_获取高精度时间。
    做 事件Loop_清空微任务。
    fp 的 微任务耗时 为 做 平台_获取高精度时间 减 t2。
    做 AnimationTimeline_tick。
    若 fp 的 需要样式：
        t3 为 做 平台_获取高精度时间。
        做 Frame_样式计算。
        fp 的 样式耗时 为 做 平台_获取高精度时间 减 t3。
        fp 的 需要样式 为 假。
        fp 的 需要布局 为 真。
    完。
    若 fp 的 需要布局：
        t4 为 做 平台_获取高精度时间。
        做 Frame_布局。
        fp 的 布局耗时 为 做 平台_获取高精度时间 减 t4。
        fp 的 需要布局 为 假。
        fp 的 需要绘制 为 真。
    完。
    若 fp 的 需要绘制：
        t5 为 做 平台_获取高精度时间。
        做 Frame_绘制。
        fp 的 绘制耗时 为 做 平台_获取高精度时间 减 t5。
        fp 的 需要绘制 为 假。
        fp 的 需要合成 为 真。
    完。
    若 fp 的 需要合成：
        t6 为 做 平台_获取高精度时间。
        做 Frame_合成提交。
        fp 的 合成耗时 为 做 平台_获取高精度时间 减 t6。
        fp 的 需要合成 为 假。
    完。
    fp 的 帧结束时间 为 做 平台_获取高精度时间。
    fp 的 帧总耗时 为 fp 的 帧结束时间 减 fp 的 帧开始时间。
    若 fp 的 帧总耗时 大于 帧预算：
        fp 的 是否丢帧 为 真。
        fp 的 连续丢帧 为 fp 的 连续丢帧 加 1。
    否则：
        fp 的 是否丢帧 为 假。
        fp 的 连续丢帧 为 0。
    完。
    做 帧统计_更新 用 fp。
    剩余时间 为 帧预算 减 fp 的 帧总耗时。
    若 剩余时间 大于 1000：
        做 Frame_空闲回调 用 剩余时间。
    完。
完。

统计窗口大小 为 60。

丢帧报警阈值 为 5。

帧预算_60fps 为 16667。

帧统计器 是 一种 事物，含 帧耗时窗口、窗口索引、窗口已填满、总帧数、丢帧总数、最大帧耗时、平均帧耗时、累计_输入耗时、累计_rAF耗时、累计_微任务耗时、累计_样式耗时、累计_布局耗时、累计_绘制耗时、累计_合成耗时、瓶颈阶段、AI帧预算建议。

可变 全局帧统计 为 0。

帧统计_获取 接受 零参：
    若 全局帧统计 等于 0：
        全局帧统计 为 做 堆_分配 用 帧统计器。
        全局帧统计 的 帧耗时窗口 为 做 数组_创建 用 统计窗口大小、0。
        全局帧统计 的 窗口索引 为 0。
        全局帧统计 的 窗口已填满 为 假。
        全局帧统计 的 总帧数 为 0。
        全局帧统计 的 丢帧总数 为 0。
        全局帧统计 的 最大帧耗时 为 0。
        全局帧统计 的 平均帧耗时 为 0.0。
        全局帧统计 的 累计_输入耗时 为 0。
        全局帧统计 的 累计_rAF耗时 为 0。
        全局帧统计 的 累计_微任务耗时 为 0。
        全局帧统计 的 累计_样式耗时 为 0。
        全局帧统计 的 累计_布局耗时 为 0。
        全局帧统计 的 累计_绘制耗时 为 0。
        全局帧统计 的 累计_合成耗时 为 0。
        全局帧统计 的 瓶颈阶段 为 0。
        全局帧统计 的 AI帧预算建议 为 16667.0。
    完。
    返还 全局帧统计。
完。

帧统计_更新 接受 fp：
    stats 为 做 帧统计_获取。
    stats 的 总帧数 为 stats 的 总帧数 加 1。
    stats 的 帧耗时窗口 的 第 stats 的 窗口索引 项 为 fp 的 帧总耗时。
    stats 的 窗口索引 为 (stats 的 窗口索引 加 1) 取余于 统计窗口大小。
    若 stats 的 总帧数 大于或等于 统计窗口大小：
        stats 的 窗口已填满 为 真。
    完。
    若 fp 的 是否丢帧：
        stats 的 丢帧总数 为 stats 的 丢帧总数 加 1。
    完。
    若 fp 的 帧总耗时 大于 stats 的 最大帧耗时：
        stats 的 最大帧耗时 为 fp 的 帧总耗时。
    完。
    stats 的 累计_输入耗时 为 stats 的 累计_输入耗时 加 fp 的 输入处理耗时。
    stats 的 累计_rAF耗时 为 stats 的 累计_rAF耗时 加 fp 的 rAF耗时。
    stats 的 累计_微任务耗时 为 stats 的 累计_微任务耗时 加 fp 的 微任务耗时。
    stats 的 累计_样式耗时 为 stats 的 累计_样式耗时 加 fp 的 样式耗时。
    stats 的 累计_布局耗时 为 stats 的 累计_布局耗时 加 fp 的 布局耗时。
    stats 的 累计_绘制耗时 为 stats 的 累计_绘制耗时 加 fp 的 绘制耗时。
    stats 的 累计_合成耗时 为 stats 的 累计_合成耗时 加 fp 的 合成耗时。
    stats 的 平均帧耗时 为 做 帧统计_计算窗口平均 用 stats。
    若 stats 的 总帧数 取余于 60 等于 0：
        做 帧统计_瓶颈检测 用 stats。
        做 帧统计_AI自适应 用 stats。
    完。
完。

帧统计_计算窗口平均 接受 stats：
    窗口量 为 统计窗口大小。
    若 非 stats 的 窗口已填满：
        窗口量 为 stats 的 总帧数。
    完。
    若 窗口量 等于 0：
        返还 0.0。
    完。
    可变。
    总和 为 0。
    可变。
    i 为 0。
    只要 i 小于 窗口量：
        总和 为 总和 加 stats 的 帧耗时窗口 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总和 乘 1.0 除以 窗口量。
完。

帧统计_瓶颈检测 接受 stats：
    最大 为 stats 的 累计_输入耗时。
    stats 的 瓶颈阶段 为 1。
    若 stats 的 累计_rAF耗时 大于 最大：
        最大 为 stats 的 累计_rAF耗时。
        stats 的 瓶颈阶段 为 2。
    完。
    若 stats 的 累计_样式耗时 大于 最大：
        最大 为 stats 的 累计_样式耗时。
        stats 的 瓶颈阶段 为 3。
    完。
    若 stats 的 累计_布局耗时 大于 最大：
        最大 为 stats 的 累计_布局耗时。
        stats 的 瓶颈阶段 为 4。
    完。
    若 stats 的 累计_绘制耗时 大于 最大：
        最大 为 stats 的 累计_绘制耗时。
        stats 的 瓶颈阶段 为 5。
    完。
    若 stats 的 累计_合成耗时 大于 最大：
        最大 为 stats 的 累计_合成耗时。
        stats 的 瓶颈阶段 为 6。
    完。
完。

帧统计_AI自适应 接受 stats：
    特征 为 做 Tensor_创建 用 1、5。
    做 Tensor_写 用 特征、0、0、stats 的 平均帧耗时。
    做 Tensor_写 用 特征、0、1、stats 的 丢帧总数 乘 1.0 除以 stats 的 总帧数。
    做 Tensor_写 用 特征、0、2、stats 的 瓶颈阶段 乘 1.0。
    做 Tensor_写 用 特征、0、3、stats 的 最大帧耗时 乘 1.0。
    做 Tensor_写 用 特征、0、4、做 平台_获取内存使用率。
    结果 为 做 AI总线_推理 用 「frame_budget_optimizer」、特征。
    若 结果 不等于 空：
        stats 的 AI帧预算建议 为 做 Tensor_读 用 结果、0、0。
        做 Tensor_释放 用 结果。
    完。
    做 Tensor_释放 用 特征。
完。

Frame_样式计算 接受 零参：
    文档 为 做 获取当前文档。
    若 文档 等于 0：
        返还。
    完。
    根节点 为 文档 的 根节点。
    若 根节点 等于 0：
        返还。
    完。
    做 CSS样式_重新计算 用 根节点。
完。

Frame_布局 接受 零参：
    文档 为 做 获取当前文档。
    若 文档 等于 0：
        返还。
    完。
    视口宽 为 做 获取视口宽度。
    视口高 为 做 获取视口高度。
    做 布局_执行 用 文档 的 根布局对象、视口宽、视口高。
完。

Frame_绘制 接受 零参：
    文档 为 做 获取当前文档。
    若 文档 等于 0：
        返还。
    完。
    做 绘制_生成显示列表 用 文档 的 根绘制层。
完。

Frame_合成提交 接受 零参：
    帧数据 为 做 合成器_收集帧数据。
    做 图形进程_提交帧 用 帧数据。
完。

Frame_空闲回调 接受 剩余时间_us：
    做 事件Loop_执行rIC 用 剩余时间_us。
    gc时间 为 剩余时间_us 除以 3。
    若 gc时间 大于 500：
        做 GC_增量收集 用 gc时间。
    完。
完。

Frame_标记需要样式 接受 零参：
    fp 为 做 FramePipeline_获取。
    fp 的 需要样式 为 真。
完。

Frame_标记需要布局 接受 零参：
    fp 为 做 FramePipeline_获取。
    fp 的 需要布局 为 真。
完。

Frame_标记需要绘制 接受 零参：
    fp 为 做 FramePipeline_获取。
    fp 的 需要绘制 为 真。
完。

Frame_标记需要合成 接受 零参：
    fp 为 做 FramePipeline_获取。
    fp 的 需要合成 为 真。
完。

帧管线_获取诊断 接受 零参：
    stats 为 做 帧统计_获取。
    可变。
    报告 为 「═══ NFRP 帧管线诊断报告 ═══\n」。
    报告 为 报告 加 「总帧数: 」 加 stats 的 总帧数 加 「\n」。
    报告 为 报告 加 「丢帧数: 」 加 stats 的 丢帧总数 加 「\n」。
    丢帧率 为 0.0。
    若 stats 的 总帧数 大于 0：
        丢帧率 为 stats 的 丢帧总数 乘 100.0 除以 stats 的 总帧数。
    完。
    报告 为 报告 加 「丢帧率: 」 加 丢帧率 加 「%\n」。
    报告 为 报告 加 「平均帧时: 」 加 stats 的 平均帧耗时 加 「us\n」。
    报告 为 报告 加 「最大帧时: 」 加 stats 的 最大帧耗时 加 「us\n」。
    报告 为 报告 加 「瓶颈阶段: 」 加 做 帧统计_瓶颈名称 用 stats 的 瓶颈阶段 加 「\n」。
    报告 为 报告 加 「AI帧预算建议: 」 加 stats 的 AI帧预算建议 加 「us\n」。
    返还 报告。
完。

帧统计_瓶颈名称 接受 阶段：
    若 阶段 等于 0：
        返还 「无」。
    完。
    若 阶段 等于 1：
        返还 「输入处理」。
    完。
    若 阶段 等于 2：
        返还 「rAF」。
    完。
    若 阶段 等于 3：
        返还 「样式计算」。
    完。
    若 阶段 等于 4：
        返还 「布局」。
    完。
    若 阶段 等于 5：
        返还 「绘制」。
    完。
    若 阶段 等于 6：
        返还 「合成」。
    完。
    返还 「未知」。
完。

帧调度器 是 一种 事物，含 目标帧率、vsync_间隔、上次vsync时间、当前帧率、最小帧率、最大帧率、自适应帧率、空闲降频、空闲计时器、空闲阈值_毫秒。

帧调度器_创建 接受 目标帧率：
    可变。
    调度器 为 做 堆_分配 用 帧调度器。
    调度器 的 目标帧率 为 目标帧率。
    调度器 的 vsync_间隔 为 1000000 除以 目标帧率。
    调度器 的 上次vsync时间 为 0。
    调度器 的 当前帧率 为 目标帧率 乘 1.0。
    调度器 的 最小帧率 为 15。
    调度器 的 最大帧率 为 120。
    调度器 的 自适应帧率 为 真。
    调度器 的 空闲降频 为 真。
    调度器 的 空闲计时器 为 0。
    调度器 的 空闲阈值_毫秒 为 3000。
    返还 调度器。
完。

帧调度器_on_vsync 接受 调度器、当前时间_ns：
    若 调度器 的 上次vsync时间 等于 0：
        调度器 的 上次vsync时间 为 当前时间_ns。
        返还 真。
    完。
    差值 为 当前时间_ns 减 调度器 的 上次vsync时间。
    目标间隔 为 调度器 的 vsync_间隔 乘 1000。
    若 差值 大于或等于 目标间隔：
        若 差值 大于 0：
            调度器 的 当前帧率 为 1000000000.0 除以 差值。
        完。
        调度器 的 上次vsync时间 为 当前时间_ns。
        调度器 的 空闲计时器 为 0。
        返还 真。
    完。
    返还 假。
完。

帧调度器_自适应更新 接受 调度器、丢帧率：
    若 非 调度器 的 自适应帧率：
        返还。
    完。
    若 丢帧率 大于 0.15：
        可变。
        新帧率 为 调度器 的 目标帧率 减 5。
        若 新帧率 小于 调度器 的 最小帧率：
            新帧率 为 调度器 的 最小帧率。
        完。
        调度器 的 目标帧率 为 新帧率。
        调度器 的 vsync_间隔 为 1000000 除以 新帧率。
    否则若 丢帧率 小于 0.02：
        可变。
        新帧率 为 调度器 的 目标帧率 加 5。
        若 新帧率 大于 调度器 的 最大帧率：
            新帧率 为 调度器 的 最大帧率。
        完。
        调度器 的 目标帧率 为 新帧率。
        调度器 的 vsync_间隔 为 1000000 除以 新帧率。
    完。
完。

帧调度器_检查空闲 接受 调度器：
    若 非 调度器 的 空闲降频：
        返还。
    完。
    调度器 的 空闲计时器 为 调度器 的 空闲计时器 加 1。
    空闲时间 为 调度器 的 空闲计时器 乘 调度器 的 vsync_间隔 除以 1000。
    若 空闲时间 大于 调度器 的 空闲阈值_毫秒：
        调度器 的 目标帧率 为 调度器 的 最小帧率。
        调度器 的 vsync_间隔 为 1000000 除以 调度器 的 最小帧率。
    完。
完。

渲染配置 是 一种 事物，含 帧预算us、启用AI优化、启用自适应帧率、启用空闲降频、GC_最大时间_us、布局缓存大小、合成层上限。

可变 全局渲染配置 为 0。

Config_获取 接受 零参：
    若 全局渲染配置 等于 0：
        全局渲染配置 为 做 堆_分配 用 渲染配置。
        全局渲染配置 的 帧预算us 为 16667。
        全局渲染配置 的 启用AI优化 为 真。
        全局渲染配置 的 启用自适应帧率 为 真。
        全局渲染配置 的 启用空闲降频 为 真。
        全局渲染配置 的 GC_最大时间_us 为 2000。
        全局渲染配置 的 布局缓存大小 为 1024。
        全局渲染配置 的 合成层上限 为 64。
    完。
    返还 全局渲染配置。
完。

帧管线_重置 接受 零参：
    fp 为 做 FramePipeline_获取。
    fp 的 帧号 为 0。
    fp 的 连续丢帧 为 0。
    fp 的 需要样式 为 假。
    fp 的 需要布局 为 假。
    fp 的 需要绘制 为 假。
    fp 的 需要合成 为 假。
    fp 的 输入处理耗时 为 0。
    fp 的 rAF耗时 为 0。
    fp 的 微任务耗时 为 0。
    fp 的 样式耗时 为 0。
    fp 的 布局耗时 为 0。
    fp 的 绘制耗时 为 0。
    fp 的 合成耗时 为 0。
完。

AI_可跳过布局 接受 零参：
    stats 为 做 帧统计_获取。
    特征 为 做 Tensor_创建 用 1、3。
    做 Tensor_写 用 特征、0、0、stats 的 累计_布局耗时 乘 1.0 除以 (stats 的 总帧数 加 1)。
    做 Tensor_写 用 特征、0、1、stats 的 平均帧耗时。
    做 Tensor_写 用 特征、0、2、stats 的 丢帧总数 乘 1.0。
    结果 为 做 AI总线_推理 用 「layout_skip_predictor」、特征。
    可变。
    可跳过 为 假。
    若 结果 不等于 空：
        可跳过 为 做 Tensor_读 用 结果、0、0 大于 0.8。
        做 Tensor_释放 用 结果。
    完。
    做 Tensor_释放 用 特征。
    返还 可跳过。
完。

帧统计_计算百分位 接受 stats、百分位：
    窗口量 为 统计窗口大小。
    若 非 stats 的 窗口已填满：
        窗口量 为 stats 的 总帧数。
    完。
    若 窗口量 等于 0：
        返还 0。
    完。
    可变。
    排序数据 为 做 数组_创建 用 窗口量、0。
    可变。
    i 为 0。
    只要 i 小于 窗口量：
        排序数据 的 第 i 项 为 stats 的 帧耗时窗口 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 1。
    只要 i 小于 窗口量：
        key 为 排序数据 的 第 i 项。
        可变。
        j 为 i 减 1。
        只要 j 大于或等于 0 并且 排序数据 的 第 j 项 大于 key：
            排序数据 的 第 j 加 1 项 为 排序数据 的 第 j 项。
            j 为 j 减 1。
        完。
        排序数据 的 第 j 加 1 项 为 key。
        i 为 i 加 1。
    完。
    索引 为 百分位 乘 窗口量 除以 100。
    若 索引 大于或等于 窗口量：
        索引 为 窗口量 减 1。
    完。
    返还 排序数据 的 第 索引 项。
完。

可变 _谐振帧率_目标us 为 16667。

可变 _谐振帧率_调节次数 为 0。

谐振帧率_自适应 接受 stats：
    p95 为 做 帧统计_计算百分位 用 stats、95。
    若 p95 大于 16000 并且 _谐振帧率_目标us 小于 33333：
        _谐振帧率_目标us 为 33333。
        _谐振帧率_调节次数 为 _谐振帧率_调节次数 加 1。
    完。
    做 否则如果 用 p95 小于 8000 并且 _谐振帧率_目标us 大于 16667。
    块：
        _谐振帧率_目标us 为 16667。
        _谐振帧率_调节次数 为 _谐振帧率_调节次数 加 1。
    完。
完。

谐振帧率_当前目标 接受 零参：
    返还 _谐振帧率_目标us。
完。

帧管线_完整统计 接受 零参：
    stats 为 做 帧统计_获取。
    返还 一组：「总帧数」、stats 的 总帧数、「丢帧数」、stats 的 丢帧总数、「平均帧耗时」、stats 的 平均帧耗时、「谐振目标帧率us」、_谐振帧率_目标us、「谐振调节次数」、_谐振帧率_调节次数。
完。

本章 收束。
