《文档接口属》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·文档节点》。

引用《..·中枢·辅助存根》。

引用《.·文档创建》。

引用《.·原子驻留》。

Document_document元素 接受 文档：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0 并且 扩展 的 文档元素 不等于 0：
        返还 扩展 的 文档元素。
    完。
    可变 子 为 文档 的 第一子节点。
    只要 子 不等于 0：
        若 子 的 节点标志 位与 节点标志_是元素：
            返还 子。
        完。
        子 为 子 的 后一兄弟。
    完。
    返还 0。
完。

Document_head 接受 文档：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        返还 扩展 的 head元素。
    完。
    返还 0。
完。

Document_body 接受 文档：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        返还 扩展 的 body元素。
    完。
    返还 0。
完。

Document_title_get 接受 文档：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        返还 扩展 的 标题。
    完。
    返还 0。
完。

Document_title_set 接受 文档、新标题：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        扩展 的 标题 为 做 字符串_复制 用 新标题。
    完。
完。

Document_URL_get 接受 文档：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        返还 扩展 的 URL。
    完。
    返还 0。
完。

Document_readyState 接受 文档：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 等于 0：
        返还 0。
    完。
    若 扩展 的 就绪状态 等于 文档状态_加载中：
        返还 「loading」。
    完。
    若 扩展 的 就绪状态 等于 文档状态_交互中：
        返还 「interactive」。
    完。
    返还 「complete」。
完。

Document_compatMode 接受 文档：
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0 并且 扩展 的 兼容模式 等于 兼容模式_怪异：
        返还 「BackCompat」。
    完。
    返还 「CSS1Compat」。
完。

Document_create元素 接受 文档、标签名：
    元素 为 做 元素_创建 用 标签名、文档。
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        扩展 的 节点总数 为 扩展 的 节点总数 加 1。
        扩展 的 元素总数 为 扩展 的 元素总数 加 1。
    完。
    返还 元素。
完。

Document_createText节点 接受 文档、文本内容：
    文本 为 做 Text_创建 用 文本内容。
    文本 的 树作用域 为 文档。
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        扩展 的 节点总数 为 扩展 的 节点总数 加 1。
    完。
    返还 文本。
完。

Document_createComment 接受 文档、数据：
    注释 为 做 Comment_创建 用 数据。
    注释 的 树作用域 为 文档。
    扩展 为 做 Document_获取扩展 用 文档。
    若 扩展 不等于 0：
        扩展 的 节点总数 为 扩展 的 节点总数 加 1。
    完。
    返还 注释。
完。

Document_createDocumentFragment 接受 文档：
    节点 为 做 节点_创建 用 DOCUMENT_FRAGMENT_节点、节点标志_是文档片段 位或 节点标志_是容器节点。
    节点 的 树作用域 为 文档。
    返还 节点。
完。

本章 收束。
