《节点》

引用《.·内核直连层》。

引用《..·推理·通信渠道》。

引用《..·中枢·硬链接》。

引用《..·中枢·内核打印》。

引用《.·节点行为统计》。

引用《..·中枢·整数转字符串》。

DOC_POS_DISCONNECTED 为 1。

DOC_POS_PRECEDING 为 2。

DOC_POS_FOLLOWING 为 4。

DOC_POS_CONTAINS 为 8。

DOC_POS_CONTAINED_BY 为 16。

DOC_POS_IMPLEMENTATION_SPECIFIC 为 32。

节点_最大遍历队列 为 1024。

节点操作统计 是 一种 事物，含 替换次数、克隆次数、规范化次数、遍历次数、位置比较次数、查找次数。

可变 全局_节点操作统计 为 空。

节点操作统计_初始化 接受 零参：
    若 全局_节点操作统计 等于 空：
        全局_节点操作统计 为 新建 节点操作统计。
        全局_节点操作统计 的 替换次数 为 0。
        全局_节点操作统计 的 克隆次数 为 0。
        全局_节点操作统计 的 规范化次数 为 0。
        全局_节点操作统计 的 遍历次数 为 0。
        全局_节点操作统计 的 位置比较次数 为 0。
        全局_节点操作统计 的 查找次数 为 0。
    完。
完。

节点_replaceChild 接受 父节点、新子、旧子：
    做 节点操作统计_初始化。
    全局_节点操作统计 的 替换次数 为 全局_节点操作统计 的 替换次数 加 1。
    若 父节点 等于 0 或者 新子 等于 0 或者 旧子 等于 0：
        返还 0。
    完。
    若 旧子 的 父节点 不等于 父节点：
        返还 0。
    完。
    若 新子 等于 旧子：
        返还 旧子。
    完。
    若 做 节点_是祖先 用 新子、父节点：
        返还 0。
    完。
    若 新子 的 父节点 不等于 0：
        做 节点_removeChild 用 新子 的 父节点、新子。
    完。
    下一个 为 旧子 的 后一兄弟。
    做 节点_removeChild 用 父节点、旧子。
    若 下一个 不等于 0：
        做 节点_insertBefore 用 父节点、新子、下一个。
    否则：
        做 节点_appendChild 用 父节点、新子。
    完。
    返还 旧子。
完。

节点_replaceChildren 接受 父节点、新子列表、数量：
    若 父节点 等于 0：
        返还。
    完。
    只要 父节点 的 第一子节点 不等于 0：
        做 节点_removeChild 用 父节点、父节点 的 第一子节点。
    完。
    可变 i 为 0。
    只要 i 小于 数量：
        若 新子列表 的 第 i 项 不等于 0：
            做 节点_appendChild 用 父节点、新子列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
完。

节点_cloneNode 接受 节点、深度克隆：
    做 节点操作统计_初始化。
    全局_节点操作统计 的 克隆次数 为 全局_节点操作统计 的 克隆次数 加 1。
    若 节点 等于 0：
        返还 0。
    完。
    克隆 为 做 堆_分配 用 节点 的 大小。
    克隆 的 节点类型 为 节点 的 节点类型。
    克隆 的 节点标志 为 节点 的 节点标志。
    克隆 的 标签名 为 节点 的 标签名。
    克隆 的 命名空间 为 节点 的 命名空间。
    克隆 的 语义角色 为 节点 的 语义角色。
    克隆 的 父节点 为 0。
    克隆 的 前一兄弟 为 0。
    克隆 的 后一兄弟 为 0。
    克隆 的 第一子节点 为 0。
    克隆 的 最后子节点 为 0。
    若 节点 的 属性列表 不等于 0：
        克隆 的 属性列表 为 做 节点_克隆属性 用 节点 的 属性列表。
    完。
    若 深度克隆：
        可变 子 为 节点 的 第一子节点。
        只要 子 不等于 0：
            子克隆 为 做 节点_cloneNode 用 子、真。
            若 子克隆 不等于 0：
                做 节点_appendChild 用 克隆、子克隆。
            完。
            子 为 子 的 后一兄弟。
        完。
    完。
    返还 克隆。
完。

节点_克隆属性 接受 属性列表：
    若 属性列表 等于 0：
        返还 0。
    完。
    返还 属性列表。
完。

节点_normalize 接受 节点：
    做 节点操作统计_初始化。
    全局_节点操作统计 的 规范化次数 为 全局_节点操作统计 的 规范化次数 加 1。
    若 节点 等于 0：
        返还。
    完。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        下一个 为 子 的 后一兄弟。
        若 子 的 节点类型 等于 3：
            若 子 的 文本内容 等于 0 或者 做 字符串长度 用 子 的 文本内容 等于 0：
                做 节点_removeChild 用 节点、子。
                子 为 下一个。
                继续。
            完。
            只要 下一个 不等于 0 并且 下一个 的 节点类型 等于 3：
                子 的 文本内容 为 做 字符串拼接 用 子 的 文本内容、下一个 的 文本内容。
                再下一个 为 下一个 的 后一兄弟。
                做 节点_removeChild 用 节点、下一个。
                下一个 为 再下一个。
            完。
        否则：
            做 节点_normalize 用 子。
        完。
        子 为 下一个。
    完。
完。

节点_是祖先 接受 可能祖先、节点：
    若 可能祖先 等于 0 或者 节点 等于 0：
        返还 0。
    完。
    可变 当前 为 节点 的 父节点。
    只要 当前 不等于 0：
        若 当前 等于 可能祖先：
            返还 1。
        完。
        当前 为 当前 的 父节点。
    完。
    返还 0。
完。

节点_contains 接受 节点、其他：
    若 节点 等于 0 或者 其他 等于 0：
        返还 假。
    完。
    若 节点 等于 其他：
        返还 真。
    完。
    返还 做 节点_是祖先 用 节点、其他 等于 1。
完。

节点_根节点 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 当前 为 节点。
    只要 当前 的 父节点 不等于 0：
        当前 为 当前 的 父节点。
    完。
    返还 当前。
完。

节点_深度 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 深度 为 0。
    可变 当前 为 节点 的 父节点。
    只要 当前 不等于 0：
        深度 为 深度 加 1。
        当前 为 当前 的 父节点。
    完。
    返还 深度。
完。

节点_祖先链 接受 节点：
    可变 链 为 一组：（空）。
    可变 当前 为 节点。
    只要 当前 不等于 0：
        做 追加 用 链、当前。
        当前 为 当前 的 父节点。
    完。
    返还 链。
完。

节点_最近公共祖先 接受 节点A、节点B：
    若 节点A 等于 0 或者 节点B 等于 0：
        返还 0。
    完。
    深度A 为 做 节点_深度 用 节点A。
    深度B 为 做 节点_深度 用 节点B。
    可变 a 为 节点A。
    可变 b 为 节点B。
    只要 深度A 大于 深度B：
        a 为 a 的 父节点。
        深度A 为 深度A 减 1。
    完。
    只要 深度B 大于 深度A：
        b 为 b 的 父节点。
        深度B 为 深度B 减 1。
    完。
    只要 a 不等于 0 并且 b 不等于 0：
        若 a 等于 b：
            返还 a。
        完。
        a 为 a 的 父节点。
        b 为 b 的 父节点。
    完。
    返还 0。
完。

节点_深度优先下一个 接受 节点、根：
    若 节点 等于 0：
        返还 0。
    完。
    若 节点 的 第一子节点 不等于 0：
        返还 节点 的 第一子节点。
    完。
    可变 当前 为 节点。
    只要 当前 不等于 0：
        若 当前 等于 根：
            返还 0。
        完。
        若 当前 的 后一兄弟 不等于 0：
            返还 当前 的 后一兄弟。
        完。
        当前 为 当前 的 父节点。
    完。
    返还 0。
完。

节点_深度优先前一个 接受 节点、根：
    若 节点 等于 0 或者 节点 等于 根：
        返还 0。
    完。
    若 节点 的 前一兄弟 不等于 0：
        可变 当前 为 节点 的 前一兄弟。
        只要 当前 的 最后子节点 不等于 0：
            当前 为 当前 的 最后子节点。
        完。
        返还 当前。
    完。
    返还 节点 的 父节点。
完。

节点_广度优先遍历 接受 根、回调：
    做 节点操作统计_初始化。
    全局_节点操作统计 的 遍历次数 为 全局_节点操作统计 的 遍历次数 加 1。
    若 根 等于 0：
        返还。
    完。
    可变 队列 为 一组：根。
    可变 头 为 0。
    可变 尾 为 1。
    只要 头 小于 尾：
        当前 为 队列 的 第 头 项。
        头 为 头 加 1。
        若 回调 不等于 0：
        完。
        可变 子 为 当前 的 第一子节点。
        只要 子 不等于 0 并且 尾 小于 节点_最大遍历队列：
            做 追加 用 队列、子。
            尾 为 尾 加 1。
            子 为 子 的 后一兄弟。
        完。
    完。
完。

节点_下一个兄弟元素 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 当前 为 节点 的 后一兄弟。
    只要 当前 不等于 0：
        若 当前 的 节点标志 位与 节点标志_是元素：
            返还 当前。
        完。
        当前 为 当前 的 后一兄弟。
    完。
    返还 0。
完。

节点_前一个兄弟元素 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 当前 为 节点 的 前一兄弟。
    只要 当前 不等于 0：
        若 当前 的 节点标志 位与 节点标志_是元素：
            返还 当前。
        完。
        当前 为 当前 的 前一兄弟。
    完。
    返还 0。
完。

节点_第N个子节点 接受 节点、n：
    若 节点 等于 0：
        返还 0。
    完。
    可变 当前 为 节点 的 第一子节点。
    可变 i 为 0。
    只要 当前 不等于 0 并且 i 小于 n：
        当前 为 当前 的 后一兄弟。
        i 为 i 加 1。
    完。
    返还 当前。
完。

节点_子节点数 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 计 为 0。
    可变 当前 为 节点 的 第一子节点。
    只要 当前 不等于 0：
        计 为 计 加 1。
        当前 为 当前 的 后一兄弟。
    完。
    返还 计。
完。

节点_子元素数 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 计 为 0。
    可变 当前 为 节点 的 第一子节点。
    只要 当前 不等于 0：
        若 当前 的 节点标志 位与 节点标志_是元素：
            计 为 计 加 1。
        完。
        当前 为 当前 的 后一兄弟。
    完。
    返还 计。
完。

节点_第一个子元素 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 当前 为 节点 的 第一子节点。
    只要 当前 不等于 0：
        若 当前 的 节点标志 位与 节点标志_是元素：
            返还 当前。
        完。
        当前 为 当前 的 后一兄弟。
    完。
    返还 0。
完。

节点_最后一个子元素 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 当前 为 节点 的 最后子节点。
    只要 当前 不等于 0：
        若 当前 的 节点标志 位与 节点标志_是元素：
            返还 当前。
        完。
        当前 为 当前 的 前一兄弟。
    完。
    返还 0。
完。

节点_compareDocumentPosition 接受 参考、其他：
    做 节点操作统计_初始化。
    全局_节点操作统计 的 位置比较次数 为 全局_节点操作统计 的 位置比较次数 加 1。
    若 参考 等于 其他：
        返还 0。
    完。
    若 参考 等于 0 或者 其他 等于 0：
        返还 DOC_POS_DISCONNECTED。
    完。
    根A 为 做 节点_根节点 用 参考。
    根B 为 做 节点_根节点 用 其他。
    若 根A 不等于 根B：
        返还 DOC_POS_DISCONNECTED 位或 DOC_POS_IMPLEMENTATION_SPECIFIC。
    完。
    若 做 节点_是祖先 用 参考、其他 等于 1：
        返还 DOC_POS_CONTAINED_BY 位或 DOC_POS_FOLLOWING。
    完。
    若 做 节点_是祖先 用 其他、参考 等于 1：
        返还 DOC_POS_CONTAINS 位或 DOC_POS_PRECEDING。
    完。
    可变 当前 为 做 节点_深度优先下一个 用 根A、根A。
    只要 当前 不等于 0：
        若 当前 等于 参考：
            返还 DOC_POS_FOLLOWING。
        完。
        若 当前 等于 其他：
            返还 DOC_POS_PRECEDING。
        完。
        当前 为 做 节点_深度优先下一个 用 当前、根A。
    完。
    返还 DOC_POS_DISCONNECTED。
完。

节点_按标签查找 接受 根、标签名：
    做 节点操作统计_初始化。
    全局_节点操作统计 的 查找次数 为 全局_节点操作统计 的 查找次数 加 1。
    可变 结果 为 一组：（空）。
    若 根 等于 0：
        返还 结果。
    完。
    可变 当前 为 做 节点_深度优先下一个 用 根、根。
    只要 当前 不等于 0：
        若 当前 的 标签名 等于 标签名：
            做 追加 用 结果、当前。
        完。
        当前 为 做 节点_深度优先下一个 用 当前、根。
    完。
    返还 结果。
完。

节点_按类型查找 接受 根、节点类型：
    可变 结果 为 一组：（空）。
    若 根 等于 0：
        返还 结果。
    完。
    可变 当前 为 做 节点_深度优先下一个 用 根、根。
    只要 当前 不等于 0：
        若 当前 的 节点类型 等于 节点类型：
            做 追加 用 结果、当前。
        完。
        当前 为 做 节点_深度优先下一个 用 当前、根。
    完。
    返还 结果。
完。

节点_子树节点数 接受 根：
    若 根 等于 0：
        返还 0。
    完。
    可变 计 为 0。
    可变 当前 为 做 节点_深度优先下一个 用 根、根。
    只要 当前 不等于 0：
        计 为 计 加 1。
        当前 为 做 节点_深度优先下一个 用 当前、根。
    完。
    返还 计。
完。

节点_子树最大深度 接受 根：
    若 根 等于 0：
        返还 0。
    完。
    根深度 为 做 节点_深度 用 根。
    可变 最大 为 0。
    可变 当前 为 做 节点_深度优先下一个 用 根、根。
    只要 当前 不等于 0：
        d 为 做 节点_深度 用 当前 减 根深度。
        若 d 大于 最大：
            最大 为 d。
        完。
        当前 为 做 节点_深度优先下一个 用 当前、根。
    完。
    返还 最大。
完。

节点操作_打印统计 接受 零参：
    做 节点操作统计_初始化。
    做 打印行 用 「=== 节点操作统计 ===」。
    做 打印行 用 「替换次数: 」 加 做 整数转字符串 用 全局_节点操作统计 的 替换次数。
    做 打印行 用 「克隆次数: 」 加 做 整数转字符串 用 全局_节点操作统计 的 克隆次数。
    做 打印行 用 「规范化次数: 」 加 做 整数转字符串 用 全局_节点操作统计 的 规范化次数。
    做 打印行 用 「遍历次数: 」 加 做 整数转字符串 用 全局_节点操作统计 的 遍历次数。
    做 打印行 用 「位置比较: 」 加 做 整数转字符串 用 全局_节点操作统计 的 位置比较次数。
    做 打印行 用 「查找次数: 」 加 做 整数转字符串 用 全局_节点操作统计 的 查找次数。
完。

节点操作_重置统计 接受 零参：
    全局_节点操作统计 的 替换次数 为 0。
    全局_节点操作统计 的 克隆次数 为 0。
    全局_节点操作统计 的 规范化次数 为 0。
    全局_节点操作统计 的 遍历次数 为 0。
    全局_节点操作统计 的 位置比较次数 为 0。
    全局_节点操作统计 的 查找次数 为 0。
完。

本章 收束。
