《节点语义分析》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《..·中枢·整数转字符串》。

引用《..·执行·组件网化》。

引用《.·文档节点》。

引用《..·中枢·字符串替换》。

引用《..·中枢·沙箱隔离进》。

节点_AI语义分析_启发式 接受 节点：
    若 节点 等于 0：
        返还。
    完。
    若 节点 的 节点标志 位与 节点标志_AI已分析：
        返还。
    完。
    若 节点 的 节点标志 位与 节点标志_是元素：
        扩展 为 做 元素_获取扩展 用 节点。
        若 扩展 不等于 0：
            标签 为 扩展 的 标签名原子。
            若 做 字符串_相等 用 标签、「nav」：
                节点 的 语义角色 为 语义_导航栏。
                节点 的 语义置信度 为 9800。
            否则若 做 字符串_相等 用 标签、「header」：
                节点 的 语义角色 为 语义_页头。
                节点 的 语义置信度 为 9800。
            否则若 做 字符串_相等 用 标签、「footer」：
                节点 的 语义角色 为 语义_页脚。
                节点 的 语义置信度 为 9800。
            否则若 做 字符串_相等 用 标签、「main」：
                节点 的 语义角色 为 语义_正文。
                节点 的 语义置信度 为 9800。
            否则若 做 字符串_相等 用 标签、「article」：
                节点 的 语义角色 为 语义_正文。
                节点 的 语义置信度 为 9500。
            否则若 做 字符串_相等 用 标签、「aside」：
                节点 的 语义角色 为 语义_侧边栏。
                节点 的 语义置信度 为 9800。
            否则若 做 字符串_相等 用 标签、「form」：
                节点 的 语义角色 为 语义_表单。
                节点 的 语义置信度 为 9800。
            否则若 做 字符串_相等 用 标签、「button」：
                节点 的 语义角色 为 语义_按钮。
                节点 的 语义置信度 为 9800。
            否则若 做 字符串_相等 用 标签、「a」：
                节点 的 语义角色 为 语义_链接。
                节点 的 语义置信度 为 9500。
            否则若 做 字符串_相等 用 标签、「img」：
                节点 的 语义角色 为 语义_图片。
                节点 的 语义置信度 为 9800。
            否则若 做 字符串_相等 用 标签、「video」：
                节点 的 语义角色 为 语义_视频。
                节点 的 语义置信度 为 9800。
            否则若 做 字符串_相等 用 标签、「code」：
                节点 的 语义角色 为 语义_代码。
                节点 的 语义置信度 为 9500。
            否则若 做 字符串_相等 用 标签、「pre」：
                节点 的 语义角色 为 语义_代码。
                节点 的 语义置信度 为 8000。
            否则若 做 字符串_相等 用 标签、「blockquote」：
                节点 的 语义角色 为 语义_引用。
                节点 的 语义置信度 为 9500。
            否则若 做 字符串_相等 用 标签、「dialog」：
                节点 的 语义角色 为 语义_弹窗。
                节点 的 语义置信度 为 9800。
            否则若 做 字符串_开头匹配 用 标签、「h」：
                节点 的 语义角色 为 语义_标题。
                节点 的 语义置信度 为 9800。
            否则若 做 字符串_相等 用 标签、「p」：
                节点 的 语义角色 为 语义_段落。
                节点 的 语义置信度 为 9000。
            否则若 做 字符串_相等 用 标签、「ul」 或者 做 字符串_相等 用 标签、「ol」：
                节点 的 语义角色 为 语义_列表。
                节点 的 语义置信度 为 9000。
            否则：
                做 节点_AI启发推断 用 节点、扩展。
            完。
        完。
    完。
    节点 的 节点标志 为 节点 的 节点标志 位或 节点标志_AI已分析。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        做 节点_AI语义分析_启发式 用 子。
        子 为 子 的 后一兄弟。
    完。
完。

节点_AI启发推断 接受 节点、扩展：
    若 扩展 等于 0：
        返还。
    完。
    role 为 做 元素_获取属性 用 节点、「role」。
    若 role 不等于 0：
        若 做 字符串_相等 用 role、「navigation」：
            节点 的 语义角色 为 语义_导航栏。
            节点 的 语义置信度 为 9000。
            返还。
        完。
        若 做 字符串_相等 用 role、「banner」：
            节点 的 语义角色 为 语义_页头。
            节点 的 语义置信度 为 9000。
            返还。
        完。
        若 做 字符串_相等 用 role、「main」：
            节点 的 语义角色 为 语义_正文。
            节点 的 语义置信度 为 9000。
            返还。
        完。
        若 做 字符串_相等 用 role、「complementary」：
            节点 的 语义角色 为 语义_侧边栏。
            节点 的 语义置信度 为 9000。
            返还。
        完。
        若 做 字符串_相等 用 role、「contentinfo」：
            节点 的 语义角色 为 语义_页脚。
            节点 的 语义置信度 为 9000。
            返还。
        完。
        若 做 字符串_相等 用 role、「search」：
            节点 的 语义角色 为 语义_搜索框。
            节点 的 语义置信度 为 9000。
            返还。
        完。
        若 做 字符串_相等 用 role、「dialog」：
            节点 的 语义角色 为 语义_弹窗。
            节点 的 语义置信度 为 9000。
            返还。
        完。
        若 做 字符串_相等 用 role、「button」：
            节点 的 语义角色 为 语义_按钮。
            节点 的 语义置信度 为 9000。
            返还。
        完。
        若 做 字符串_相等 用 role、「tab」：
            节点 的 语义角色 为 语义_标签页。
            节点 的 语义置信度 为 9000。
            返还。
        完。
    完。
    class名 为 做 元素_获取属性 用 节点、「class」。
    若 class名 不等于 0：
        若 做 字符串_包含 用 class名、「nav」 或者 做 字符串_包含 用 class名、「menu」：
            节点 的 语义角色 为 语义_导航栏。
            节点 的 语义置信度 为 7000。
            返还。
        完。
        若 做 字符串_包含 用 class名、「ad」 或者 做 字符串_包含 用 class名、「banner」 或者 做 字符串_包含 用 class名、「sponsor」：
            节点 的 语义角色 为 语义_广告。
            节点 的 语义置信度 为 7500。
            返还。
        完。
        若 做 字符串_包含 用 class名、「comment」：
            节点 的 语义角色 为 语义_评论区。
            节点 的 语义置信度 为 7000。
            返还。
        完。
        若 做 字符串_包含 用 class名、「card」：
            节点 的 语义角色 为 语义_卡片。
            节点 的 语义置信度 为 6500。
            返还。
        完。
        若 做 字符串_包含 用 class名、「carousel」 或者 做 字符串_包含 用 class名、「slider」：
            节点 的 语义角色 为 语义_轮播。
            节点 的 语义置信度 为 7000。
            返还。
        完。
        若 做 字符串_包含 用 class名、「modal」 或者 做 字符串_包含 用 class名、「popup」 或者 做 字符串_包含 用 class名、「overlay」：
            节点 的 语义角色 为 语义_弹窗。
            节点 的 语义置信度 为 7500。
            返还。
        完。
        若 做 字符串_包含 用 class名、「cookie」 或者 做 字符串_包含 用 class名、「consent」：
            节点 的 语义角色 为 语义_Cookie同意。
            节点 的 语义置信度 为 8000。
            返还。
        完。
        若 做 字符串_包含 用 class名、「search」：
            节点 的 语义角色 为 语义_搜索框。
            节点 的 语义置信度 为 6500。
            返还。
        完。
        若 做 字符串_包含 用 class名、「breadcrumb」：
            节点 的 语义角色 为 语义_面包屑。
            节点 的 语义置信度 为 7500。
            返还。
        完。
        若 做 字符串_包含 用 class名、「tab」：
            节点 的 语义角色 为 语义_标签页。
            节点 的 语义置信度 为 6500。
            返还。
        完。
        若 做 字符串_包含 用 class名、「loading」 或者 做 字符串_包含 用 class名、「spinner」：
            节点 的 语义角色 为 语义_加载指示。
            节点 的 语义置信度 为 7000。
            返还。
        完。
    完。
    做 节点_AI结构推断 用 节点。
完。

节点_AI结构推断 接受 节点：
    若 节点 等于 0 或者 节点 的 语义角色 不等于 语义_未知：
        返还。
    完。
    可变 链接数 为 0。
    可变 文本数 为 0。
    可变 图片数 为 0。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        若 子 的 节点标志 位与 节点标志_是元素：
            子扩 为 做 元素_获取扩展 用 子。
            若 子扩 不等于 0：
                若 做 字符串_相等 用 子扩 的 标签名原子、「a」：
                    链接数 为 链接数 加 1。
                完。
                若 做 字符串_相等 用 子扩 的 标签名原子、「img」：
                    图片数 为 图片数 加 1。
                完。
            完。
        完。
        若 子 的 节点类型 等于 TEXT_节点：
            文本数 为 文本数 加 1。
        完。
        子 为 子 的 后一兄弟。
    完。
    若 链接数 大于或等于 3 并且 文本数 小于或等于 链接数：
        节点 的 语义角色 为 语义_导航栏。
        节点 的 语义置信度 为 5000。
    否则若 文本数 大于或等于 3 并且 链接数 小于或等于 1：
        节点 的 语义角色 为 语义_正文。
        节点 的 语义置信度 为 5000。
    否则若 图片数 大于或等于 2 并且 链接数 大于或等于 2：
        节点 的 语义角色 为 语义_卡片。
        节点 的 语义置信度 为 4500。
    完。
完。

语义_未知 为 0。

语义_导航栏 为 1。

语义_页头 为 2。

语义_页脚 为 3。

语义_正文 为 4。

语义_侧边栏 为 5。

语义_表单 为 6。

语义_按钮 为 7。

语义_链接 为 8。

语义_图片 为 9。

语义_视频 为 10。

语义_代码 为 11。

语义_引用 为 12。

语义_弹窗 为 13。

语义_标题 为 14。

语义_段落 为 15。

语义_列表 为 16。

语义_搜索框 为 17。

语义_广告 为 18。

语义_评论区 为 19。

语义_卡片 为 20。

语义_轮播 为 21。

语义_Cookie同意 为 22。

语义_面包屑 为 23。

语义_标签页 为 24。

语义_加载指示 为 25。

语义_表格 为 26。

语义_音频 为 27。

语义_地图 为 28。

语义_登录 为 29。

语义_注册 为 30。

节点标志_是元素 为 0x0010。

节点标志_AI已分析 为 0x1000。

TEXT_节点 为 3。

语义_角色名 接受 角色：
    若 角色 等于 语义_未知：
        返还 「未知」。
    完。
    若 角色 等于 语义_导航栏：
        返还 「导航栏」。
    完。
    若 角色 等于 语义_页头：
        返还 「页头」。
    完。
    若 角色 等于 语义_页脚：
        返还 「页脚」。
    完。
    若 角色 等于 语义_正文：
        返还 「正文」。
    完。
    若 角色 等于 语义_侧边栏：
        返还 「侧边栏」。
    完。
    若 角色 等于 语义_表单：
        返还 「表单」。
    完。
    若 角色 等于 语义_按钮：
        返还 「按钮」。
    完。
    若 角色 等于 语义_链接：
        返还 「链接」。
    完。
    若 角色 等于 语义_图片：
        返还 「图片」。
    完。
    若 角色 等于 语义_视频：
        返还 「视频」。
    完。
    若 角色 等于 语义_代码：
        返还 「代码」。
    完。
    若 角色 等于 语义_弹窗：
        返还 「弹窗」。
    完。
    若 角色 等于 语义_标题：
        返还 「标题」。
    完。
    若 角色 等于 语义_段落：
        返还 「段落」。
    完。
    若 角色 等于 语义_列表：
        返还 「列表」。
    完。
    若 角色 等于 语义_搜索框：
        返还 「搜索框」。
    完。
    若 角色 等于 语义_广告：
        返还 「广告」。
    完。
    若 角色 等于 语义_评论区：
        返还 「评论区」。
    完。
    若 角色 等于 语义_卡片：
        返还 「卡片」。
    完。
    若 角色 等于 语义_轮播：
        返还 「轮播」。
    完。
    若 角色 等于 语义_Cookie同意：
        返还 「Cookie同意」。
    完。
    若 角色 等于 语义_面包屑：
        返还 「面包屑」。
    完。
    若 角色 等于 语义_标签页：
        返还 「标签页」。
    完。
    若 角色 等于 语义_加载指示：
        返还 「加载指示」。
    完。
    若 角色 等于 语义_登录：
        返还 「登录」。
    完。
    若 角色 等于 语义_注册：
        返还 「注册」。
    完。
    返还 「角色_」 加 做 整数转字符串 用 角色。
完。

语义_是交互角色 接受 角色：
    返还 角色 等于 语义_按钮 或者 角色 等于 语义_链接 或者 角色 等于 语义_表单 或者 角色 等于 语义_搜索框。
完。

语义_是地标角色 接受 角色：
    返还 角色 等于 语义_导航栏 或者 角色 等于 语义_页头 或者 角色 等于 语义_页脚 或者 角色 等于 语义_正文 或者 角色 等于 语义_侧边栏。
完。

语义_是内容角色 接受 角色：
    返还 角色 等于 语义_段落 或者 角色 等于 语义_标题 或者 角色 等于 语义_列表 或者 角色 等于 语义_代码 或者 角色 等于 语义_引用。
完。

语义_按角色查找 接受 根节点、目标角色：
    可变 结果 为 一组：（空）。
    做 语义_按角色查找_递归 用 根节点、目标角色、结果。
    返还 结果。
完。

语义_按角色查找_递归 接受 节点、目标角色、结果：
    若 节点 等于 0：
        返还。
    完。
    若 节点 的 语义角色 等于 目标角色：
        做 追加 用 结果、节点。
    完。
    可变 子 为 节点 的 第一子节点。
    只要 子 不等于 0：
        做 语义_按角色查找_递归 用 子、目标角色、结果。
        子 为 子 的 后一兄弟。
    完。
完。

语义_获取主内容 接受 根节点：
    结果 为 做 语义_按角色查找 用 根节点、语义_正文。
    若 做 计数 用 结果 大于 0：
        返还 结果 的 第 0 项。
    完。
    返还 0。
完。

语义_获取导航 接受 根节点：
    返还 做 语义_按角色查找 用 根节点、语义_导航栏。
完。

语义_获取广告 接受 根节点：
    返还 做 语义_按角色查找 用 根节点、语义_广告。
完。

语义_获取标题列表 接受 根节点：
    返还 做 语义_按角色查找 用 根节点、语义_标题。
完。

语义_页面质量评分 接受 根节点：
    可变 分数 为 50。
    标题 为 做 语义_按角色查找 用 根节点、语义_标题。
    若 做 计数 用 标题 大于或等于 1：
        分数 为 分数 加 10。
    完。
    导航 为 做 语义_按角色查找 用 根节点、语义_导航栏。
    若 做 计数 用 导航 大于或等于 1：
        分数 为 分数 加 10。
    完。
    正文 为 做 语义_按角色查找 用 根节点、语义_正文。
    若 做 计数 用 正文 大于或等于 1：
        分数 为 分数 加 15。
    完。
    页脚 为 做 语义_按角色查找 用 根节点、语义_页脚。
    若 做 计数 用 页脚 大于或等于 1：
        分数 为 分数 加 5。
    完。
    广告 为 做 语义_按角色查找 用 根节点、语义_广告。
    若 做 计数 用 广告 大于 5：
        分数 为 分数 减 15。
    完。
    若 分数 大于 100：
        分数 为 100。
    完。
    若 分数 小于 0：
        分数 为 0。
    完。
    返还 分数。
完。

可变 语义_分析总数 为 0。

可变 语义_高置信度数 为 0。

可变 语义_低置信度数 为 0。

可变 语义_未识别数 为 0。

语义_统计 接受 零参：
    返还 一本字典：分析总数 对应 语义_分析总数、高置信度 对应 语义_高置信度数、低置信度 对应 语义_低置信度数、未识别 对应 语义_未识别数。
完。

语义_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 语义_未识别数 大于 语义_分析总数 除以 2 并且 语义_分析总数 大于 100：
        做 追加 用 问题、「语义未识别率过高」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「语义分析引擎健康」。
    完。
    返还 问题。
完。

语义_重置统计 接受 零参：
    语义_分析总数 为 0。
    语义_高置信度数 为 0。
    语义_低置信度数 为 0。
    语义_未识别数 为 0。
完。

语义_完整统计 接受 零参：
    返还 一组：语义_分析总数、语义_高置信度数、语义_低置信度数、语义_未识别数。
完。

本章 收束。
