《图绘纹理管线》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《..·稳态·写寄存器》。

引用《..·中枢·内存初始化》。

引用《..·中枢·内容可见性》。

引用《..·稳态·帧缓冲》。

最大纹理 为 256。

最大绘制批次 为 64。

最大顶点 为 4096。

衰减基数 为 1024。

纹理对象 是 一种 事物，含 标识、宽度、高度、格式、数据指针、引用计数、最近使用、显存驻留。

绘制命令 是 一种 事物，含 类型、纹理索引、顶点起始、顶点数量、混合模式、裁剪横、裁剪纵、裁剪宽、裁剪高。

可变 纹理表 为 一组：（空）。

可变 纹理数 为 0。

可变 命令队列 为 一组：（空）。

可变 命令数 为 0。

可变 顶点缓冲 为 0。

可变 顶点写位 为 0。

可变 绘制调用数 为 0。

可变 批次合并数 为 0。

可变 纹理切换数 为 0。

可变 上次纹理 为 负 1。

可变 帧计数 为 0。

可变 智能跳帧数 为 0。

图绘_初始化 接受 零参：
    纹理表 为 一组：（空）。
    纹理数 为 0。
    命令队列 为 一组：（空）。
    命令数 为 0。
    顶点缓冲 为 做 堆_分配 用 最大顶点 乘 20。
    顶点写位 为 0。
    总线 为 全局智能总线。
    若 总线 不等于 0：
        做 智能总线_注册模块 用 总线、「图绘」、图绘_智能回调。
    完。
完。

图绘_创建纹理 接受 宽、高、格式、数据：
    若 纹理数 大于或等于 最大纹理：
        返还 负 1。
    完。
    纹理 为 新建 纹理对象。
    纹理 的 标识 为 纹理数。
    纹理 的 宽度 为 宽。
    纹理 的 高度 为 高。
    纹理 的 格式 为 格式。
    纹理 的 数据指针 为 数据。
    纹理 的 引用计数 为 1。
    纹理 的 最近使用 为 做 平台_获取高精度时间。
    纹理 的 显存驻留 为 真。
    做 追加 用 纹理表、纹理。
    索引 为 纹理数。
    纹理数 为 纹理数 加 1。
    返还 索引。
完。

图绘_提交矩形 接受 纹理索引、横、纵、宽、高、颜色：
    若 顶点写位 加 4 大于 最大顶点：
        做 图绘_刷新。
    完。
    基 为 顶点缓冲 加 顶点写位 乘 20。
    做 内存_写32 用 基、横。
    做 内存_写32 用 基 加 4、纵。
    做 内存_写32 用 基 加 8、宽。
    做 内存_写32 用 基 加 12、高。
    做 内存_写32 用 基 加 16、颜色。
    顶点写位 为 顶点写位 加 1。
    若 命令数 大于 0 并且 命令队列 的 第 命令数 减 1 项 的 纹理索引 等于 纹理索引：
        命令队列 的 第 命令数 减 1 项 的 顶点数量 为 命令队列 的 第 命令数 减 1 项 的 顶点数量 加 1。
        批次合并数 为 批次合并数 加 1。
    否则：
        若 命令数 大于或等于 最大绘制批次：
            做 图绘_刷新。
        完。
        命令 为 新建 绘制命令。
        命令 的 类型 为 1。
        命令 的 纹理索引 为 纹理索引。
        命令 的 顶点起始 为 顶点写位 减 1。
        命令 的 顶点数量 为 1。
        命令 的 混合模式 为 0。
        命令 的 裁剪横 为 0。
        命令 的 裁剪纵 为 0。
        命令 的 裁剪宽 为 0。
        命令 的 裁剪高 为 0。
        做 追加 用 命令队列、命令。
        命令数 为 命令数 加 1。
        若 纹理索引 不等于 上次纹理：
            纹理切换数 为 纹理切换数 加 1。
            上次纹理 为 纹理索引。
        完。
    完。
完。

图绘_刷新 接受 零参：
    可变 序号 为 0。
    只要 序号 小于 命令数：
        命令 为 命令队列 的 第 序号 项。
        可变 顶点 为 命令 的 顶点起始。
        只要 顶点 小于 命令 的 顶点起始 加 命令 的 顶点数量：
            基 为 顶点缓冲 加 顶点 乘 20。
            横 为 做 内存_读32 用 基。
            纵 为 做 内存_读32 用 基 加 4。
            宽 为 做 内存_读32 用 基 加 8。
            高 为 做 内存_读32 用 基 加 12。
            颜色 为 做 内存_读32 用 基 加 16。
            做 帧缓冲_填充矩形 用 横、纵、宽、高、颜色。
            顶点 为 顶点 加 1。
        完。
        绘制调用数 为 绘制调用数 加 1。
        序号 为 序号 加 1。
    完。
    命令队列 为 一组：（空）。
    命令数 为 0。
    顶点写位 为 0。
    上次纹理 为 负 1。
完。

图绘_结束帧 接受 零参：
    做 图绘_刷新。
    做 帧缓冲_翻转。
    帧计数 为 帧计数 加 1。
完。

图绘_智能回调 接受 事件、数据：
    若 事件 等于 1：
        帧预算 为 数据。
        若 帧预算 小于 2000 并且 命令数 大于 最大绘制批次 除以 2：
            智能跳帧数 为 智能跳帧数 加 1。
        完。
    完。
完。

图绘_释放纹理 接受 索引：
    若 索引 大于或等于 纹理数：
        返还。
    完。
    纹理表 的 第 索引 项 的 引用计数 为 纹理表 的 第 索引 项 的 引用计数 减 1。
    若 纹理表 的 第 索引 项 的 引用计数 小于或等于 0：
        纹理表 的 第 索引 项 的 显存驻留 为 假。
    完。
完。

图绘_淘汰纹理 接受 零参：
    可变 最旧 为 负 1。
    可变 最旧时间 为 0x7FFFFFFFFFFFFFFF。
    可变 序号 为 0。
    只要 序号 小于 纹理数：
        若 纹理表 的 第 序号 项 的 显存驻留 并且 纹理表 的 第 序号 项 的 引用计数 小于或等于 0 并且 纹理表 的 第 序号 项 的 最近使用 小于 最旧时间：
            最旧时间 为 纹理表 的 第 序号 项 的 最近使用。
            最旧 为 序号。
        完。
        序号 为 序号 加 1。
    完。
    若 最旧 大于或等于 0：
        纹理表 的 第 最旧 项 的 显存驻留 为 假。
    完。
完。

图绘_完整统计 接受 零参：
    返还 一组：纹理数、绘制调用数、批次合并数、纹理切换数、帧计数、智能跳帧数。
完。

本章 收束。
