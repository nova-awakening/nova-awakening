《HTML令牌处理》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《..·中枢·辅助存根》。

引用《.·样式解析》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

HTML令牌izer_处理_DOCTYPE 接受 tk、令牌、c：
    若 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0C 或者 c 等于 0x20：
        tk 的 状态 为 状态_DOCTYPE前。
    否则若 c 等于 0x3E：
        令牌 的 强制怪异模式 为 真。
        令牌 的 类型 为 令牌类型_DOCTYPE。
        tk 的 状态 为 状态_数据。
    否则：
        tk 的 位置 为 tk 的 位置 减 1。
        tk 的 状态 为 状态_DOCTYPE前。
    完。
完。

HTML令牌izer_处理_DOCTYPE前 接受 tk、令牌、c：
    若 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0C 或者 c 等于 0x20：
        返还。
    完。
    若 c 等于 0x3E：
        令牌 的 强制怪异模式 为 真。
        令牌 的 类型 为 令牌类型_DOCTYPE。
        tk 的 状态 为 状态_数据。
        返还。
    完。
    令牌 的 DOCTYPE名称 为 做 堆_分配_数组 用 64。
    做 内存_写8 用 令牌 的 DOCTYPE名称、做 HTML令牌izer_转小写 用 c。
    令牌 的 标签名长度 为 1。
    tk 的 状态 为 状态_DOCTYPE名称。
完。

HTML令牌izer_处理_DOCTYPE名称 接受 tk、令牌、c：
    若 c 等于 0x09 或者 c 等于 0x0A 或者 c 等于 0x0C 或者 c 等于 0x20：
        tk 的 状态 为 状态_DOCTYPE名称后。
    否则若 c 等于 0x3E：
        令牌 的 类型 为 令牌类型_DOCTYPE。
        tk 的 状态 为 状态_数据。
    否则：
        做 内存_写8 用 令牌 的 DOCTYPE名称 加 令牌 的 标签名长度、做 HTML令牌izer_转小写 用 c。
        令牌 的 标签名长度 为 令牌 的 标签名长度 加 1。
    完。
完。

HTML令牌izer_处理_RCDATA 接受 tk、令牌、c：
    若 c 等于 0x3C：
        若 做 HTML令牌izer_匹配RCDATA结束标签 用 tk：
            若 令牌 的 数据长度 大于 0：
                令牌 的 类型 为 令牌类型_字符。
            完。
            返还。
        完。
        做 HTML令牌izer_追加数据 用 令牌、c。
    否则若 c 等于 0x26：
        tk 的 返回状态 为 状态_RCDATA。
        tk 的 状态 为 状态_字符引用。
    否则：
        做 HTML令牌izer_追加数据 用 令牌、c。
    完。
完。

HTML令牌izer_处理_RAWTEXT 接受 tk、令牌、c：
    若 c 等于 0x3C：
        若 做 HTML令牌izer_匹配RAWTEXT结束标签 用 tk：
            若 令牌 的 数据长度 大于 0：
                令牌 的 类型 为 令牌类型_字符。
            完。
            返还。
        完。
        做 HTML令牌izer_追加数据 用 令牌、c。
    否则：
        做 HTML令牌izer_追加数据 用 令牌、c。
    完。
完。

HTML令牌izer_消费字符 接受 tk：
    若 tk 的 位置 大于或等于 tk 的 输入长度：
        返还 0。
    完。
    c 为 做 内存_读8 用 tk 的 输入 加 tk 的 位置。
    tk 的 位置 为 tk 的 位置 加 1。
    若 c 等于 0x0A：
        tk 的 行号 为 tk 的 行号 加 1。
        tk 的 列号 为 1。
    否则：
        tk 的 列号 为 tk 的 列号 加 1。
    完。
    返还 c。
完。

HTML令牌izer_窥视字符 接受 tk：
    若 tk 的 位置 大于或等于 tk 的 输入长度：
        返还 0。
    完。
    返还 做 内存_读8 用 tk 的 输入 加 tk 的 位置。
完。

HTML令牌izer_是ASCII字母 接受 c：
    返还 c 大于或等于 0x41 并且 c 小于或等于 0x5A 或者 c 大于或等于 0x61 并且 c 小于或等于 0x7A。
完。

HTML令牌izer_转小写 接受 c：
    若 c 大于或等于 0x41 并且 c 小于或等于 0x5A：
        返还 c 加 0x20。
    完。
    返还 c。
完。

HTML令牌izer_追加标签名 接受 令牌、c：
    做 内存_写8 用 令牌 的 标签名 加 令牌 的 标签名长度、c。
    令牌 的 标签名长度 为 令牌 的 标签名长度 加 1。
完。

HTML令牌izer_完成标签名 接受 令牌：
    做 内存_写8 用 令牌 的 标签名 加 令牌 的 标签名长度、0。
完。

HTML令牌izer_追加数据 接受 令牌、c：
    做 内存_写8 用 令牌 的 数据 加 令牌 的 数据长度、c。
    令牌 的 数据长度 为 令牌 的 数据长度 加 1。
完。

HTML令牌izer_追加属性名字符 接受 tk、c：
    做 内存_写8 用 tk 的 当前属性名 加 tk 的 当前属性名长度、c。
    tk 的 当前属性名长度 为 tk 的 当前属性名长度 加 1。
完。

HTML令牌izer_追加属性值字符 接受 tk、c：
    做 内存_写8 用 tk 的 当前属性值 加 tk 的 当前属性值长度、c。
    tk 的 当前属性值长度 为 tk 的 当前属性值长度 加 1。
完。

HTML令牌izer_提交属性 接受 tk、令牌：
    若 tk 的 当前属性名长度 等于 0：
        返还。
    完。
    做 内存_写8 用 tk 的 当前属性名 加 tk 的 当前属性名长度、0。
    做 内存_写8 用 tk 的 当前属性值 加 tk 的 当前属性值长度、0。
    可变 i 为 0。
    只要 i 小于 令牌 的 属性数量：
        偏移 为 令牌 的 属性列表 加 i 乘 40。
        已有名 为 做 内存_读64 用 偏移。
        若 做 字符串_相等 用 已有名、tk 的 当前属性名：
            返还。
        完。
        i 为 i 加 1。
    完。
    偏移 为 令牌 的 属性列表 加 令牌 的 属性数量 乘 40。
    名副本 为 做 字符串_复制_N 用 tk 的 当前属性名、tk 的 当前属性名长度。
    值副本 为 做 字符串_复制_N 用 tk 的 当前属性值、tk 的 当前属性值长度。
    做 内存_写64 用 偏移、名副本。
    做 内存_写32 用 偏移 加 8、tk 的 当前属性名长度。
    做 内存_写64 用 偏移 加 16、值副本。
    做 内存_写32 用 偏移 加 24、tk 的 当前属性值长度。
    令牌 的 属性数量 为 令牌 的 属性数量 加 1。
完。

HTML令牌izer_记录开始标签 接受 tk、令牌：
    做 内存_复制 用 tk 的 最后开始标签、令牌 的 标签名、令牌 的 标签名长度 加 1。
    tk 的 最后开始标签长度 为 令牌 的 标签名长度。
完。

HTML令牌izer_匹配不区分大小写 接受 tk、目标：
    目标长度 为 做 字符串_长度 用 目标。
    若 tk 的 位置 加 目标长度 大于 tk 的 输入长度：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 目标长度：
        a 为 做 HTML令牌izer_转小写 用 做 内存_读8 用 tk 的 输入 加 tk 的 位置 加 i。
        b 为 做 HTML令牌izer_转小写 用 做 内存_读8 用 目标 加 i。
        若 a 不等于 b：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    tk 的 位置 为 tk 的 位置 加 目标长度。
    返还 1。
完。

HTML令牌izer_匹配RCDATA结束标签 接受 tk：
    若 tk 的 最后开始标签长度 等于 0：
        返还 0。
    完。
    保存位置 为 tk 的 位置。
    若 做 HTML令牌izer_窥视字符 用 tk 不等于 0x2F：
        返还 0。
    完。
    tk 的 位置 为 tk 的 位置 加 1。
    可变 i 为 0。
    只要 i 小于 tk 的 最后开始标签长度：
        若 tk 的 位置 大于或等于 tk 的 输入长度：
            tk 的 位置 为 保存位置。
            返还 0。
        完。
        c 为 做 HTML令牌izer_转小写 用 做 内存_读8 用 tk 的 输入 加 tk 的 位置。
        期望 为 做 内存_读8 用 tk 的 最后开始标签 加 i。
        若 c 不等于 期望：
            tk 的 位置 为 保存位置。
            返还 0。
        完。
        tk 的 位置 为 tk 的 位置 加 1。
        i 为 i 加 1。
    完。
    若 tk 的 位置 小于 tk 的 输入长度：
        后续 为 做 内存_读8 用 tk 的 输入 加 tk 的 位置。
        若 后续 等于 0x3E 或者 后续 等于 0x20 或者 后续 等于 0x09 或者 后续 等于 0x0A：
            tk 的 状态 为 状态_数据。
            只要 tk 的 位置 小于 tk 的 输入长度 并且 做 内存_读8 用 tk 的 输入 加 tk 的 位置 不等于 0x3E：
                tk 的 位置 为 tk 的 位置 加 1。
            完。
            若 tk 的 位置 小于 tk 的 输入长度：
                tk 的 位置 为 tk 的 位置 加 1。
            完。
            返还 1。
        完。
    完。
    tk 的 位置 为 保存位置。
    返还 0。
完。

可变 AI_标记令牌乙_调用数 为 0。

可变 AI_标记令牌乙_异常数 为 0。

AI_标记令牌乙_监控 接受 零参：
    AI_标记令牌乙_调用数 为 AI_标记令牌乙_调用数 加 1。
    返还 AI_标记令牌乙_异常数 等于 0。
完。

AI_标记令牌乙_健康检查 接受 零参：
    若 AI_标记令牌乙_调用数 大于 0 并且 AI_标记令牌乙_异常数 乘 100 除以 AI_标记令牌乙_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
