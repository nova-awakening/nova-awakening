《CSS_calc求值》

引用《..·中枢·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核直连层》。

引用《..·推理·AI总线资源调度》。

引用《..·中枢·字符串替换》。

引用《.·CSS值解析》。

CSS_calc求值 接受 表达式、参照值_百分比、参照值_em：
    若 表达式 等于 0：
        返还 0。
    完。
    长度 为 做 字符串_长度 用 表达式。
    可变 pos 为 0。
    返还 做 CSS_calc_加法 用 表达式、长度、pos、参照值_百分比、参照值_em。
完。

CSS_calc_加法 接受 文本、长度、pos、参照百分比、参照em：
    可变 结果 为 做 CSS_calc_乘法 用 文本、长度、pos、参照百分比、参照em。
    只要 pos 小于 长度：
        只要 pos 小于 长度 并且 做 内存_读8 用 文本 加 pos 等于 0x20：
            pos 为 pos 加 1。
        完。
        若 pos 大于或等于 长度：
            跳出。
        完。
        op 为 做 内存_读8 用 文本 加 pos。
        若 op 等于 0x2B：
            pos 为 pos 加 1。
            只要 pos 小于 长度 并且 做 内存_读8 用 文本 加 pos 等于 0x20：
                pos 为 pos 加 1。
            完。
            右 为 做 CSS_calc_乘法 用 文本、长度、pos、参照百分比、参照em。
            结果 为 结果 加 右。
        否则若 op 等于 0x2D：
            pos 为 pos 加 1。
            只要 pos 小于 长度 并且 做 内存_读8 用 文本 加 pos 等于 0x20：
                pos 为 pos 加 1。
            完。
            右 为 做 CSS_calc_乘法 用 文本、长度、pos、参照百分比、参照em。
            结果 为 结果 减 右。
        否则：
            跳出。
        完。
    完。
    返还 结果。
完。

CSS_calc_乘法 接受 文本、长度、pos、参照百分比、参照em：
    可变 结果 为 做 CSS_calc_原子 用 文本、长度、pos、参照百分比、参照em。
    只要 pos 小于 长度：
        只要 pos 小于 长度 并且 做 内存_读8 用 文本 加 pos 等于 0x20：
            pos 为 pos 加 1。
        完。
        若 pos 大于或等于 长度：
            跳出。
        完。
        op 为 做 内存_读8 用 文本 加 pos。
        若 op 等于 0x2A：
            pos 为 pos 加 1。
            只要 pos 小于 长度 并且 做 内存_读8 用 文本 加 pos 等于 0x20：
                pos 为 pos 加 1。
            完。
            右 为 做 CSS_calc_原子 用 文本、长度、pos、参照百分比、参照em。
            结果 为 结果 乘 右 除以 100。
        否则若 op 等于 0x2F：
            pos 为 pos 加 1。
            只要 pos 小于 长度 并且 做 内存_读8 用 文本 加 pos 等于 0x20：
                pos 为 pos 加 1。
            完。
            右 为 做 CSS_calc_原子 用 文本、长度、pos、参照百分比、参照em。
            若 右 不等于 0：
                结果 为 结果 乘 100 除以 右。
            完。
        否则：
            跳出。
        完。
    完。
    返还 结果。
完。

CSS_calc_原子 接受 文本、长度、pos、参照百分比、参照em：
    只要 pos 小于 长度 并且 做 内存_读8 用 文本 加 pos 等于 0x20：
        pos 为 pos 加 1。
    完。
    若 pos 小于 长度 并且 做 内存_读8 用 文本 加 pos 等于 0x28：
        pos 为 pos 加 1。
        结果 为 做 CSS_calc_加法 用 文本、长度、pos、参照百分比、参照em。
        若 pos 小于 长度 并且 做 内存_读8 用 文本 加 pos 等于 0x29：
            pos 为 pos 加 1。
        完。
        返还 结果。
    完。
    val 为 做 CSS解析_数字或长度 用 文本、长度、pos。
    若 val 等于 0：
        返还 0。
    完。
    只要 pos 小于 长度 并且 做 内存_读8 用 文本 加 pos 不等于 0x20 并且 做 内存_读8 用 文本 加 pos 不等于 0x2B 并且 做 内存_读8 用 文本 加 pos 不等于 0x2D 并且 做 内存_读8 用 文本 加 pos 不等于 0x2A 并且 做 内存_读8 用 文本 加 pos 不等于 0x2F 并且 做 内存_读8 用 文本 加 pos 不等于 0x29：
        pos 为 pos 加 1。
    完。
    若 val 的 单位 等于 单位_百分比：
        返还 参照百分比 乘 val 的 整数值 除以 10000。
    完。
    若 val 的 单位 等于 单位_em：
        返还 参照em 乘 val 的 整数值 除以 100。
    完。
    若 val 的 单位 等于 单位_rem：
        返还 1600 乘 val 的 整数值 除以 100。
    完。
    若 val 的 单位 等于 单位_vw：
        返还 参照百分比 乘 val 的 整数值 除以 10000。
    完。
    返还 val 的 整数值。
完。

智能_历史接口_AI加载优先级 接受 url：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 url 等于 0：
        返还 Q_HALF。
    完。
    返还 做 AI总线_资源优先级 用 bus、url。
完。

智能_历史接口_AI预取决策 接受 url：
    bus 为 全局AI总线。
    若 bus 等于 0 或者 url 等于 0：
        返还 假。
    完。
    分数 为 做 AI总线_资源优先级 用 bus、url。
    返还 分数 大于 600。
完。

本章 收束。
