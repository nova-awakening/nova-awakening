《project》

项目名称 为 Nova觉醒。

项目类型 为 ai_gene_system。

入口模块 为 神经中枢 的 nova。

入口符号 为 none。

默认Profile 为 kernel 减 qemu。

镜像类型 为 kernel 减 elf。

Profile 为 analysis 减 host。

Profile 为 linux 减 user。

Profile 为 kernel 减 qemu。

Profile 为 kernel 减 physical。

子系统 为 中枢。

中枢 除以 _manifest 的 txt。

子系统 为 外联。

外联 除以 _manifest 的 txt。

子系统 为 引导。

引导 除以 _manifest 的 txt。

子系统 为 感知。

感知 除以 _manifest 的 txt。

子系统 为 推理。

推理 除以 _manifest 的 txt。

子系统 为 执行。

执行 除以 _manifest 的 txt。

子系统 为 记忆。

记忆 除以 _manifest 的 txt。

子系统 为 稳态。

稳态 除以 _manifest 的 txt。

子系统 为 标准。

标准 除以 _manifest 的 txt。

模态合同 为 视觉。

vision。

模态合同 为 语言。

language。

模态合同 为 听觉。

audio。

模态合同 为 触觉。

haptic。

模态合同 为 本体。

proprioception。

记忆拓扑_工作 为 记忆 除以 工作记忆。

记忆拓扑_语义 为 记忆 除以 语义记忆。

记忆拓扑_长期 为 记忆 除以 长期记忆。

学习合同 为 hebbian。

学习合同 为 reinforcement。

学习合同 为 meta。

学习合同 为 contrastive。

系统阶段 为 perception。

系统阶段 为 inference。

系统阶段 为 decision。

系统阶段 为 execution。

系统阶段 为 memory。

系统阶段 为 feedback。

系统阶段 为 evolution。

本章 收束。
