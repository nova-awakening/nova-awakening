《公平调度权重》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·进程描述符》。

权重表 为 一组：88761、71755、56483、46273、36291、29154、23254、18705、14949、11916、9548、7620、6100、4904、3906、3121、2501、1991、1586、1277、1024、820、655、526、423、335、272、215、172、137、110、87、70、56、45、36、29、23、18、15。

权重倒数表 为 一组：48388、59856、76040、92818、118348、147320、184698、229616、287308、360437、449829、563644、704093、875809、1099582、1376151、1717300、2157191、2708050、3363326、4194304、5237765、6557202、8165337、10153587、12820798、15790321、19976592、24970740、31350126、39045157、49367440、61356676、76695844、95443717、119304647、148102320、186737708、238609294、286331153。

nice到权重_Nova公平调度 接受 nice：
    索引 为 nice 加 20。
    若 索引 小于 0：
        索引 为 0。
    完。
    若 索引 大于 39：
        索引 为 39。
    完。
    返还 权重表 的 第 索引 项。
完。

nice到权重倒数 接受 nice：
    索引 为 nice 加 20。
    若 索引 小于 0：
        索引 为 0。
    完。
    若 索引 大于 39：
        索引 为 39。
    完。
    返还 权重倒数表 的 第 索引 项。
完。

RT_优先级_最大 为 99。

普通优先级_基础 为 100。

nice转内核优先级 接受 nice：
    返还 普通优先级_基础 加 nice 加 20。
完。

内核优先级转nice 接受 优先级：
    返还 优先级 减 普通优先级_基础 减 20。
完。

是RT优先级 接受 优先级：
    返还 优先级 小于或等于 RT_优先级_最大。
完。

NICE_0_权重 为 1024。

计算加权Nova虚拟运行时间增量 接受 实际时间_us、权重：
    若 权重 小于或等于 0：
        权重 为 NICE_0_权重。
    完。
    返还 实际时间_us 乘 NICE_0_权重 除以 权重。
完。

计算加权Nova虚拟运行时间增量_精确 接受 实际时间_us、nice：
    倒数 为 做 nice到权重倒数 用 nice。
    返还 实际时间_us 乘 倒数 除以 4194304。
完。

计算时间片 接受 权重、总权重、调度周期_us：
    若 总权重 小于或等于 0：
        返还 调度周期_us。
    完。
    片 为 调度周期_us 乘 权重 除以 总权重。
    若 片 小于 750：
        返还 750。
    完。
    返还 片。
完。

进程_设nice_Nova公平调度 接受 任务、nice：
    若 nice 小于 负 20：
        nice 为 负 20。
    完。
    若 nice 大于 19：
        nice 为 19。
    完。
    做 设置 用 任务、T_nice、nice。
    做 设置 用 任务、T_权重、做 nice到权重 用 nice。
完。

进程_获取权重 接受 任务：
    返还 任务 的 第 T_权重 项。
完。

进程_初始化权重 接受 任务：
    nice 为 任务 的 第 T_nice 项。
    做 设置 用 任务、T_权重、做 nice到权重 用 nice。
完。

负载权重_更新 接受 任务、旧nice、新nice：
    旧权重 为 做 nice到权重_Nova公平调度 用 旧nice。
    新权重 为 做 nice到权重_Nova公平调度 用 新nice。
    返还 一组：旧权重、新权重、新权重 减 旧权重。
完。

调度延迟_us 为 6000。

最小粒度_us 为 750。

唤醒粒度_us 为 1000。

计算调度周期 接受 进程数：
    nr_latency 为 调度延迟_us 除以 最小粒度_us。
    若 进程数 小于或等于 nr_latency：
        返还 调度延迟_us。
    完。
    返还 进程数 乘 最小粒度_us。
完。

Nova虚拟运行时间_之前 接受 a、b：
    返还 a 减 b 小于 0。
完。

Nova虚拟运行时间_之后 接受 a、b：
    返还 b 减 a 小于 0。
完。

权重转CPU百分比 接受 权重、总权重：
    若 总权重 小于或等于 0：
        返还 100。
    完。
    返还 权重 乘 100 除以 总权重。
完。

nice转预期CPU百分比 接受 nice、进程数：
    权重 为 做 nice到权重_Nova公平调度 用 nice。
    默认总 为 NICE_0_权重 乘 (进程数 减 1) 加 权重。
    若 默认总 等于 0：
        返还 0。
    完。
    返还 权重 乘 100 除以 默认总。
完。

nice_clamp 接受 nice：
    若 nice 小于 负 20：
        返还 负 20。
    完。
    若 nice 大于 19：
        返还 19。
    完。
    返还 nice。
完。

权重_最大 接受 零参：
    返还 权重表 的 第 0 项。
完。

权重_最小 接受 零参：
    返还 权重表 的 第 39 项。
完。

权重_默认 接受 零参：
    返还 NICE_0_权重。
完。

Nova公平调度权重_统计 接受 零参：
    返还 一组：做 权重_最大、做 权重_默认、做 权重_最小。
完。

Nova公平调度权重_完整统计 接受 零参：
    返还 一组：做 权重_最大、做 权重_默认、做 权重_最小。
完。

可变 AI_公平调度权_负载窗口 为 一组：（空）。

AI_公平调度权_负载预测 接受 当前负载：
    做 追加 用 AI_公平调度权_负载窗口、当前负载。
    若 做 计数 用 AI_公平调度权_负载窗口 大于 20：
        做 列表_移除 用 AI_公平调度权_负载窗口、0。
    完。
    若 做 计数 用 AI_公平调度权_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_公平调度权_负载窗口。
    趋势 为 AI_公平调度权_负载窗口 的 第 n 减 1 项 减 AI_公平调度权_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_公平调度权_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
