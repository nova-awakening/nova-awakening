《软中断挂起数》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·自动扩容》。

引用《..·执行·平台时间》。

引用《..·感知·内核直连层》。

引用《.·字符串替换》。

引用《.·内存初始化》。

软中断_定时器 为 0。

软中断_网络收 为 1。

软中断_网络发 为 2。

软中断_块设备 为 3。

软中断_调度 为 4。

软中断_读拷 为 5。

软中断_高精定 为 6。

软中断_任务 为 7。

最大软中断 为 10。

每轮最大 为 10。

速率限制 为 200。

软中断状态 是 一种 事物，含 挂起掩码、处理表、触发计数、执行计数、总执行、守护执行、溢出次数、正在处理。

可变 全局软中断 为 0。

软中断_初始化 接受 零参：
    全局软中断 为 做 堆_分配 用 软中断状态。
    全局软中断 的 挂起掩码 为 0。
    全局软中断 的 处理表 为 做 堆_分配 用 最大软中断 乘 8。
    全局软中断 的 触发计数 为 做 堆_分配 用 最大软中断 乘 4。
    全局软中断 的 执行计数 为 做 堆_分配 用 最大软中断 乘 4。
    可变 序号 为 0。
    只要 序号 小于 最大软中断：
        做 内存_写64 用 全局软中断 的 处理表 加 序号 乘 8、0。
        做 内存_写32 用 全局软中断 的 触发计数 加 序号 乘 4、0。
        做 内存_写32 用 全局软中断 的 执行计数 加 序号 乘 4、0。
        序号 为 序号 加 1。
    完。
    全局软中断 的 总执行 为 0。
    全局软中断 的 守护执行 为 0。
    全局软中断 的 溢出次数 为 0。
    全局软中断 的 正在处理 为 假。
完。

软中断_注册 接受 类型、处理函数：
    若 类型 大于或等于 最大软中断：
        返还。
    完。
    做 内存_写64 用 全局软中断 的 处理表 加 类型 乘 8、处理函数。
完。

软中断_触发 接受 类型：
    若 类型 大于或等于 最大软中断：
        返还。
    完。
    全局软中断 的 挂起掩码 为 全局软中断 的 挂起掩码 位或 1 左移 类型。
    做 内存_写32 用 全局软中断 的 触发计数 加 类型 乘 4、做 内存_读32 用 全局软中断 的 触发计数 加 类型 乘 4 加 1。
完。

软中断_处理 接受 零参：
    若 全局软中断 等于 0：
        返还。
    完。
    若 全局软中断 的 正在处理：
        返还。
    完。
    全局软中断 的 正在处理 为 真。
    可变 轮次 为 0。
    只要 全局软中断 的 挂起掩码 不等于 0 并且 轮次 小于 每轮最大：
        挂起 为 全局软中断 的 挂起掩码。
        全局软中断 的 挂起掩码 为 0。
        可变 类型 为 0。
        只要 类型 小于 最大软中断：
            若 挂起 位与 1 左移 类型 不等于 0：
                处理 为 做 内存_读64 用 全局软中断 的 处理表 加 类型 乘 8。
                若 处理 不等于 0：
                    做 处理。
                    做 内存_写32 用 全局软中断 的 执行计数 加 类型 乘 4、做 内存_读32 用 全局软中断 的 执行计数 加 类型 乘 4 加 1。
                    全局软中断 的 总执行 为 全局软中断 的 总执行 加 1。
                完。
            完。
            类型 为 类型 加 1。
        完。
        轮次 为 轮次 加 1。
    完。
    若 全局软中断 的 挂起掩码 不等于 0：
        全局软中断 的 溢出次数 为 全局软中断 的 溢出次数 加 1。
    完。
    全局软中断 的 正在处理 为 假。
完。

软中断_守护处理 接受 零参：
    若 全局软中断 等于 0：
        返还。
    完。
    若 全局软中断 的 挂起掩码 不等于 0：
        做 软中断_处理。
        全局软中断 的 守护执行 为 全局软中断 的 守护执行 加 1。
    完。
完。

软中断_挂起数 接受 零参：
    若 全局软中断 等于 0：
        返还 0。
    完。
    可变 数量 为 0。
    挂起 为 全局软中断 的 挂起掩码。
    可变 位 为 0。
    只要 位 小于 最大软中断：
        若 挂起 右移 位 位与 1：
            数量 为 数量 加 1。
        完。
        位 为 位 加 1。
    完。
    返还 数量。
完。

软中断_获取触发 接受 类型：
    若 类型 大于或等于 最大软中断：
        返还 0。
    完。
    返还 做 内存_读32 用 全局软中断 的 触发计数 加 类型 乘 4。
完。

软中断_获取执行 接受 类型：
    若 类型 大于或等于 最大软中断：
        返还 0。
    完。
    返还 做 内存_读32 用 全局软中断 的 执行计数 加 类型 乘 4。
完。

软中断_类型名 接受 类型：
    若 类型 等于 软中断_定时器：
        返还 「定时器」。
    完。
    若 类型 等于 软中断_网络收：
        返还 「网络接收」。
    完。
    若 类型 等于 软中断_网络发：
        返还 「网络发送」。
    完。
    若 类型 等于 软中断_块设备：
        返还 「块设备」。
    完。
    若 类型 等于 软中断_调度：
        返还 「调度」。
    完。
    若 类型 等于 软中断_读拷：
        返还 「RCU」。
    完。
    若 类型 等于 软中断_高精定：
        返还 「高精度定时」。
    完。
    若 类型 等于 软中断_任务：
        返还 「Tasklet」。
    完。
    返还 「未知(」 加 做 转字符串 用 类型 加 「)」。
完。

可变 软中断_已初始化标志 为 假。

可变 软中断_BH禁用深度 为 0。

可变 软中断_错误数 为 0。

可变 软中断_速率 为 0。

可变 软中断_速率窗口 为 0。

可变 软中断_速率时刻 为 0。

可变 软中断_峰值速率 为 0。

可变 软中断_速率EMA 为 0。

可变 软中断_延迟总计 为 0。

可变 软中断_延迟样本 为 0。

可变 软中断_最大延迟 为 0。

可变 软中断_超时次数 为 0。

软中断_超时阈值 为 2000000。

软中断_完整初始化 接受 零参：
    若 软中断_已初始化标志 等于 真：
        返还 错误_已存在。
    完。
    做 软中断_初始化。
    软中断_BH禁用深度 为 0。
    软中断_错误数 为 0。
    软中断_速率 为 0。
    软中断_速率窗口 为 0。
    软中断_速率时刻 为 0。
    软中断_峰值速率 为 0。
    软中断_速率EMA 为 0。
    软中断_延迟总计 为 0。
    软中断_延迟样本 为 0。
    软中断_最大延迟 为 0。
    软中断_超时次数 为 0。
    软中断_已初始化标志 为 真。
    做 打印 用 「[软中断] 完整初始化完成」。
    返还 0。
完。

软中断_注册_增强 接受 类型、处理函数：
    若 类型 大于或等于 最大软中断：
        软中断_错误数 为 软中断_错误数 加 1。
        返还 错误_无效参数。
    完。
    若 处理函数 等于 空：
        软中断_错误数 为 软中断_错误数 加 1。
        返还 错误_无效参数。
    完。
    已有 为 做 内存_读64 用 全局软中断 的 处理表 加 类型 乘 8。
    若 已有 不等于 0：
        做 打印 用 「[软中断] 警告: 类型」 加 做 转字符串 用 类型 加 「(」 加 做 软中断_类型名 用 类型 加 「)已有处理函数, 替换」。
    完。
    做 软中断_注册 用 类型、处理函数。
    做 打印 用 「[软中断] 注册: 」 加 做 软中断_类型名 用 类型。
    返还 0。
完。

软中断_处理_增强 接受 零参：
    若 全局软中断 等于 0：
        返还。
    完。
    若 全局软中断 的 正在处理：
        返还。
    完。
    若 软中断_BH禁用深度 大于 0：
        返还。
    完。
    全局软中断 的 正在处理 为 真。
    总开始 为 做 读取纳秒。
    可变 轮次 为 0。
    只要 全局软中断 的 挂起掩码 不等于 0 并且 轮次 小于 每轮最大：
        挂起 为 全局软中断 的 挂起掩码。
        全局软中断 的 挂起掩码 为 0。
        可变 类型 为 0。
        只要 类型 小于 最大软中断：
            若 挂起 位与 1 左移 类型 不等于 0：
                处理 为 做 内存_读64 用 全局软中断 的 处理表 加 类型 乘 8。
                若 处理 不等于 0：
                    开始 为 做 读取纳秒。
                    做 处理。
                    耗时 为 做 读取纳秒 减 开始。
                    做 软中断_记录延迟 用 耗时、类型。
                    做 内存_写32 用 全局软中断 的 执行计数 加 类型 乘 4、做 内存_读32 用 全局软中断 的 执行计数 加 类型 乘 4 加 1。
                    全局软中断 的 总执行 为 全局软中断 的 总执行 加 1。
                    软中断_速率窗口 为 软中断_速率窗口 加 1。
                完。
            完。
            类型 为 类型 加 1。
        完。
        轮次 为 轮次 加 1。
    完。
    若 全局软中断 的 挂起掩码 不等于 0：
        全局软中断 的 溢出次数 为 全局软中断 的 溢出次数 加 1。
    完。
    全局软中断 的 正在处理 为 假。
完。

软中断_记录延迟 接受 延迟、类型：
    软中断_延迟样本 为 软中断_延迟样本 加 1。
    软中断_延迟总计 为 软中断_延迟总计 加 延迟。
    若 延迟 大于 软中断_最大延迟：
        软中断_最大延迟 为 延迟。
    完。
    若 延迟 大于 软中断_超时阈值：
        软中断_超时次数 为 软中断_超时次数 加 1。
        做 打印 用 「[软中断] 警告: 」 加 做 软中断_类型名 用 类型 加 「 执行超时 」 加 做 转字符串 用 延迟 加 「ns」。
    完。
完。

软中断_平均延迟 接受 零参：
    若 软中断_延迟样本 等于 0：
        返还 0。
    完。
    返还 软中断_延迟总计 除以 软中断_延迟样本。
完。

软中断_BH_禁用 接受 零参：
    软中断_BH禁用深度 为 软中断_BH禁用深度 加 1。
完。

软中断_BH_启用 接受 零参：
    若 软中断_BH禁用深度 大于 0：
        软中断_BH禁用深度 为 软中断_BH禁用深度 减 1。
    完。
    若 软中断_BH禁用深度 等于 0 并且 全局软中断 不等于 0：
        若 全局软中断 的 挂起掩码 不等于 0：
            做 软中断_处理_增强。
        完。
    完。
完。

软中断_BH_已禁用 接受 零参：
    返还 软中断_BH禁用深度 大于 0。
完。

软中断_更新速率 接受 零参：
    现在 为 做 平台_获取高精度时间。
    间隔 为 现在 减 软中断_速率时刻。
    若 间隔 大于或等于 1000000：
        软中断_速率 为 软中断_速率窗口 乘 1000000 除以 间隔。
        软中断_速率EMA 为 (软中断_速率EMA 乘 7 加 软中断_速率) 除以 8。
        若 软中断_速率 大于 软中断_峰值速率：
            软中断_峰值速率 为 软中断_速率。
        完。
        软中断_速率窗口 为 0。
        软中断_速率时刻 为 现在。
    完。
完。

软中断_获取速率 接受 零参：
    返还 软中断_速率。
完。

软中断_获取峰值速率 接受 零参：
    返还 软中断_峰值速率。
完。

软中断_有挂起 接受 零参：
    若 全局软中断 等于 0：
        返还 假。
    完。
    返还 全局软中断 的 挂起掩码 不等于 0。
完。

软中断_处理全部 接受 零参：
    做 软中断_处理_增强。
完。

软中断_已注册 接受 类型：
    若 类型 大于或等于 最大软中断 或者 全局软中断 等于 0：
        返还 假。
    完。
    返还 做 内存_读64 用 全局软中断 的 处理表 加 类型 乘 8 不等于 0。
完。

软中断_已注册数 接受 零参：
    若 全局软中断 等于 0：
        返还 0。
    完。
    可变 数量 为 0。
    可变 i 为 0。
    只要 i 小于 最大软中断：
        若 做 内存_读64 用 全局软中断 的 处理表 加 i 乘 8 不等于 0：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

软中断_获取最活跃 接受 零参：
    若 全局软中断 等于 0：
        返还 0 减 1。
    完。
    可变 最大 为 0。
    可变 最大类型 为 0 减 1。
    可变 i 为 0。
    只要 i 小于 最大软中断：
        执行 为 做 内存_读32 用 全局软中断 的 执行计数 加 i 乘 4。
        若 执行 大于 最大：
            最大 为 执行。
            最大类型 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最大类型。
完。

软中断_完整统计 接受 零参：
    若 全局软中断 等于 0：
        空统计 为 做 字典。
        做 字典设 用 空统计、「已初始化」、假。
        返还 空统计。
    完。
    统计 为 做 字典。
    做 字典设 用 统计、「已初始化」、软中断_已初始化标志。
    做 字典设 用 统计、「总执行」、全局软中断 的 总执行。
    做 字典设 用 统计、「守护执行」、全局软中断 的 守护执行。
    做 字典设 用 统计、「溢出次数」、全局软中断 的 溢出次数。
    做 字典设 用 统计、「挂起数」、做 软中断_挂起数。
    做 字典设 用 统计、「挂起掩码」、全局软中断 的 挂起掩码。
    做 字典设 用 统计、「已注册数」、做 软中断_已注册数。
    做 字典设 用 统计、「最活跃类型」、做 软中断_获取最活跃。
    做 字典设 用 统计、「BH禁用深度」、软中断_BH禁用深度。
    做 字典设 用 统计、「当前速率」、软中断_速率。
    做 字典设 用 统计、「峰值速率」、软中断_峰值速率。
    做 字典设 用 统计、「速率EMA」、软中断_速率EMA。
    做 字典设 用 统计、「平均延迟(ns)」、做 软中断_平均延迟。
    做 字典设 用 统计、「最大延迟(ns)」、软中断_最大延迟。
    做 字典设 用 统计、「超时次数」、软中断_超时次数。
    做 字典设 用 统计、「延迟样本」、软中断_延迟样本。
    做 字典设 用 统计、「错误数」、软中断_错误数。
    可变 i 为 0。
    只要 i 小于 最大软中断：
        触发 为 做 内存_读32 用 全局软中断 的 触发计数 加 i 乘 4。
        执行 为 做 内存_读32 用 全局软中断 的 执行计数 加 i 乘 4。
        若 触发 大于 0 或者 执行 大于 0：
            做 字典设 用 统计、「触发_」 加 做 软中断_类型名 用 i、触发。
            做 字典设 用 统计、「执行_」 加 做 软中断_类型名 用 i、执行。
        完。
        i 为 i 加 1。
    完。
    返还 统计。
完。

软中断_打印完整统计 接受 零参：
    统计 为 做 软中断_完整统计。
    做 打印 用 「[软中断] ═══════ 完整统计 ═══════」。
    做 打印 用 「  已初始化:     」 加 做 转字符串 用 做 字典取 用 统计、「已初始化」。
    做 打印 用 「  总执行:       」 加 做 转字符串 用 做 字典取 用 统计、「总执行」。
    做 打印 用 「  守护执行:     」 加 做 转字符串 用 做 字典取 用 统计、「守护执行」。
    做 打印 用 「  溢出次数:     」 加 做 转字符串 用 做 字典取 用 统计、「溢出次数」。
    做 打印 用 「  挂起数:       」 加 做 转字符串 用 做 字典取 用 统计、「挂起数」。
    做 打印 用 「  已注册:       」 加 做 转字符串 用 做 字典取 用 统计、「已注册数」。
    做 打印 用 「  BH禁用深度:   」 加 做 转字符串 用 做 字典取 用 统计、「BH禁用深度」。
    做 打印 用 「  当前速率:     」 加 做 转字符串 用 做 字典取 用 统计、「当前速率」 加 「 次/秒」。
    做 打印 用 「  峰值速率:     」 加 做 转字符串 用 做 字典取 用 统计、「峰值速率」 加 「 次/秒」。
    做 打印 用 「  平均延迟:     」 加 做 转字符串 用 做 字典取 用 统计、「平均延迟(ns)」 加 「ns」。
    做 打印 用 「  最大延迟:     」 加 做 转字符串 用 做 字典取 用 统计、「最大延迟(ns)」 加 「ns」。
    做 打印 用 「  超时次数:     」 加 做 转字符串 用 做 字典取 用 统计、「超时次数」。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「  ── 各类型统计 ──」。
    可变 i 为 0。
    只要 i 小于 最大软中断：
        若 全局软中断 不等于 0：
            触发 为 做 内存_读32 用 全局软中断 的 触发计数 加 i 乘 4。
            执行 为 做 内存_读32 用 全局软中断 的 执行计数 加 i 乘 4。
            若 触发 大于 0 或者 执行 大于 0：
                做 打印 用 「  」 加 做 软中断_类型名 用 i 加 「: 触发=」 加 做 转字符串 用 触发 加 「 执行=」 加 做 转字符串 用 执行。
            完。
        完。
        i 为 i 加 1。
    完。
    做 打印 用 「[软中断] ═════════════════════════」。
    返还 0。
完。

软中断_dump 接受 零参：
    做 打印 用 「[软中断 dump] ──────────────────」。
    做 打印 用 「  已初始化: 」 加 做 转字符串 用 软中断_已初始化标志。
    若 全局软中断 不等于 0：
        做 打印 用 「  挂起掩码: 0x」 加 做 转字符串 用 全局软中断 的 挂起掩码。
        做 打印 用 「  正在处理: 」 加 做 转字符串 用 全局软中断 的 正在处理。
        做 打印 用 「  总执行: 」 加 做 转字符串 用 全局软中断 的 总执行。
        做 打印 用 「  溢出: 」 加 做 转字符串 用 全局软中断 的 溢出次数。
    完。
    做 打印 用 「  BH禁用深度: 」 加 做 转字符串 用 软中断_BH禁用深度。
    做 打印 用 「  速率: 」 加 做 转字符串 用 软中断_速率。
    做 打印 用 「  平均延迟: 」 加 做 转字符串 用 做 软中断_平均延迟 加 「ns」。
    做 打印 用 「  超时: 」 加 做 转字符串 用 软中断_超时次数。
    做 打印 用 「[软中断 dump] ──────────────────」。
完。

软中断_销毁 接受 零参：
    若 软中断_已初始化标志 等于 假：
        返还 0。
    完。
    全局软中断 为 0。
    软中断_已初始化标志 为 假。
    做 打印 用 「[软中断] 已销毁」。
    返还 0。
完。

软中断_为调度器提供负载 接受 零参：
    返还 一组：软中断_速率、软中断_峰值速率、做 软中断_挂起数。
完。

软中断_为AI提供延迟模式 接受 零参：
    返还 一组：做 软中断_平均延迟、软中断_最大延迟、软中断_超时次数、软中断_速率、软中断_延迟样本。
完。

软中断_为procfs提供统计 接受 零参：
    若 全局软中断 等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大软中断：
        触发 为 做 内存_读32 用 全局软中断 的 触发计数 加 i 乘 4。
        执行 为 做 内存_读32 用 全局软中断 的 执行计数 加 i 乘 4。
        做 追加 用 结果、一组：i、做 软中断_类型名 用 i、触发、执行。
        i 为 i 加 1。
    完。
    返还 结果。
完。

软中断_重置统计 接受 零参：
    若 全局软中断 等于 0：
        返还 错误_未初始化。
    完。
    全局软中断 的 总执行 为 0。
    全局软中断 的 守护执行 为 0。
    全局软中断 的 溢出次数 为 0。
    可变 i 为 0。
    只要 i 小于 最大软中断：
        做 内存_写32 用 全局软中断 的 触发计数 加 i 乘 4、0。
        做 内存_写32 用 全局软中断 的 执行计数 加 i 乘 4、0。
        i 为 i 加 1。
    完。
    软中断_速率 为 0。
    软中断_峰值速率 为 0。
    软中断_速率EMA 为 0。
    软中断_延迟总计 为 0。
    软中断_延迟样本 为 0。
    软中断_最大延迟 为 0。
    软中断_超时次数 为 0。
    做 打印 用 「[软中断] 统计已重置」。
    返还 0。
完。

软中断_为电源管理挂起 接受 零参：
    若 全局软中断 不等于 0 并且 全局软中断 的 挂起掩码 不等于 0：
        做 软中断_处理_增强。
    完。
    返还 0。
完。

软中断_为电源管理恢复 接受 零参：
    软中断_速率窗口 为 0。
    软中断_速率时刻 为 做 平台_获取高精度时间。
    返还 0。
完。

软中断挂起数_统计 接受 零参：
    返还 一本字典：软中断错误数 对应 软中断_错误数、软中断速率 对应 软中断_速率、软中断峰值速率 对应 软中断_峰值速率、软中断延迟总计 对应 软中断_延迟总计、软中断延迟样本 对应 软中断_延迟样本、软中断超时次数 对应 软中断_超时次数。
完。

软中断挂起数_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 软中断_错误数 大于 软中断_延迟总计 除以 3 并且 软中断_延迟总计 大于 50：
        做 追加 用 问题、「软中断_错误数过高」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

软中断挂起数_综合报告 接受 零参：
    做 打印 用 「[软中断挂起数] ═══ 综合报告 ═══」。
    做 打印 用 「  软中断_错误数: 」 加 软中断_错误数。
    做 打印 用 「  软中断_速率: 」 加 软中断_速率。
    做 打印 用 「  软中断_峰值速率: 」 加 软中断_峰值速率。
    做 打印 用 「  软中断_延迟总计: 」 加 软中断_延迟总计。
    做 打印 用 「  软中断_延迟样本: 」 加 软中断_延迟样本。
    做 打印 用 「  软中断_超时次数: 」 加 软中断_超时次数。
    做 打印 用 「  健康: 」 加 若 做 软中断挂起数_健康评估 则 「正常」 否则 「异常」。
完。

软中断挂起数_全局重置 接受 零参：
    软中断_错误数 为 0。
    软中断_速率 为 0。
    软中断_峰值速率 为 0。
    软中断_延迟总计 为 0。
    软中断_延迟样本 为 0。
    软中断_超时次数 为 0。
    做 打印 用 「[软中断挂起数] 统计已重置」。
    返还 0。
完。

本章 收束。
