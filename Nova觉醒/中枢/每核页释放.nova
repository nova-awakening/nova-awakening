《每核页释放》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·伙伴系统计》。

引用《.·链表后继》。

引用《.·同步原语》。

引用《.·尾部零》。

引用《.·双向链表操作》。

引用《.·物理页描述符》。

PCP_释放 接受 z、页号、迁移类型：
    若 迁移类型 大于或等于 MIGRATE_PCPTYPES：
        迁移类型 为 MIGRATE_MOVABLE。
    完。
    做 自旋锁_获取 用 z 的 PCP 的 锁。
    做 链表_头插 用 z 的 PCP 的 链表 的 第 迁移类型 项、页号。
    z 的 PCP 的 计数 为 z 的 PCP 的 计数 加 1。
    若 z 的 PCP 的 计数 大于 z 的 PCP 的 高水位：
        做 自旋锁_释放 用 z 的 PCP 的 锁。
        做 PCP_批量归还 用 z、迁移类型。
        返还。
    完。
    做 自旋锁_释放 用 z 的 PCP 的 锁。
完。

PCP_批量归还 接受 z、迁移类型：
    做 自旋锁_获取 用 z 的 PCP 的 锁。
    做 自旋锁_获取 用 z 的 锁。
    可变 归还数 为 0。
    只要 归还数 小于 z 的 PCP 的 批量 并且 非 做 链表_为空 用 z 的 PCP 的 链表 的 第 迁移类型 项：
        节点 为 做 链表_尾节点 用 z 的 PCP 的 链表 的 第 迁移类型 项。
        页号 为 做 链表_数据 用 节点。
        做 链表_删除 用 节点。
        z 的 PCP 的 计数 为 z 的 PCP 的 计数 减 1。
        做 __伙伴_放回 用 z、页号、0、迁移类型。
        归还数 为 归还数 加 1。
    完。
    做 自旋锁_释放 用 z 的 锁。
    做 自旋锁_释放 用 z 的 PCP 的 锁。
完。

__伙伴_取页 接受 z、阶、迁移类型：
    页号 为 做 __伙伴_取页_最小 用 z、阶、迁移类型。
    若 页号 大于或等于 0：
        返还 页号。
    完。
    若 迁移类型 小于 MIGRATE_PCPTYPES：
        回退 为 回退链 的 第 迁移类型 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 回退：
            页号 为 做 __伙伴_取页_最小 用 z、阶、回退 的 第 i 项。
            若 页号 大于或等于 0：
                z 的 回退次数 为 z 的 回退次数 加 1。
                若 阶 大于或等于 页块阶 除以 2：
                    做 设置页块迁移类型 用 z、页号、迁移类型。
                完。
                返还 页号。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 0 减 1。
完。

__伙伴_取页_最小 接受 z、请求阶、迁移类型：
    可变 当前阶 为 请求阶。
    只要 当前阶 小于或等于 最大阶：
        链 为 z 的 空闲区 的 第 当前阶 项 的 空闲链表 的 第 迁移类型 项。
        若 非 做 链表_为空 用 链：
            节点 为 做 链表_首节点 用 链。
            页号 为 做 链表_数据 用 节点。
            做 链表_删除 用 节点。
            z 的 空闲区 的 第 当前阶 项 的 空闲数 为 z 的 空闲区 的 第 当前阶 项 的 空闲数 减 1。
            块大小 为 1 左移 当前阶。
            若 页状态 不等于 空：
                做 位图_清除范围 用 页状态、页号、块大小。
            完。
            做 __伙伴_分裂 用 z、页号、当前阶、请求阶、迁移类型。
            分配大小 为 1 左移 请求阶。
            可变 j 为 0。
            只要 j 小于 分配大小：
                页 为 做 页描述符_获取 用 页号 加 j。
                若 页 不等于 空：
                    做 页描述符_清除标志 用 页、页_伙伴。
                    做 设置 用 页、PG_阶、请求阶。
                    做 页描述符_增引用 用 页。
                完。
                j 为 j 加 1。
            完。
            z 的 分配次数 为 z 的 分配次数 加 1。
            返还 页号。
        完。
        当前阶 为 当前阶 加 1。
    完。
    返还 0 减 1。
完。

__伙伴_分裂 接受 z、页号、原阶、目标阶、迁移类型：
    可变 当前阶 为 原阶。
    只要 当前阶 大于 目标阶：
        当前阶 为 当前阶 减 1。
        伙伴页号 为 页号 加 (1 左移 当前阶)。
        做 链表_尾插 用 z 的 空闲区 的 第 当前阶 项 的 空闲链表 的 第 迁移类型 项、伙伴页号。
        z 的 空闲区 的 第 当前阶 项 的 空闲数 为 z 的 空闲区 的 第 当前阶 项 的 空闲数 加 1。
        若 页状态 不等于 空：
            做 位图_设置范围 用 页状态、伙伴页号、1 左移 当前阶。
        完。
        伙伴页 为 做 页描述符_获取 用 伙伴页号。
        若 伙伴页 不等于 空：
            做 页描述符_设置标志 用 伙伴页、页_伙伴。
            做 设置 用 伙伴页、PG_阶、当前阶。
        完。
        z 的 分裂次数 为 z 的 分裂次数 加 1。
    完。
完。

__伙伴_放回 接受 z、页号、阶、迁移类型：
    可变 当前页 为 页号。
    可变 当前阶 为 阶。
    只要 当前阶 小于 最大阶：
        伙伴页 为 当前页 位异或 1 左移 当前阶。
        若 伙伴页 大于或等于 z 的 起始页号 加 z 的 跨越页数：
            中断。
        完。
        若 伙伴页 小于 z 的 起始页号：
            中断。
        完。
        若 非 做 __伙伴_是空闲 用 z、伙伴页、当前阶：
            中断。
        完。
        伙伴mt 为 做 获取页块迁移类型 用 z、伙伴页。
        做 __伙伴_移除 用 z、伙伴页、当前阶、伙伴mt。
        若 伙伴页 小于 当前页：
            当前页 为 伙伴页。
        完。
        当前阶 为 当前阶 加 1。
        z 的 合并次数 为 z 的 合并次数 加 1。
    完。
    做 链表_尾插 用 z 的 空闲区 的 第 当前阶 项 的 空闲链表 的 第 迁移类型 项、当前页。
    z 的 空闲区 的 第 当前阶 项 的 空闲数 为 z 的 空闲区 的 第 当前阶 项 的 空闲数 加 1。
    若 页状态 不等于 空：
        做 位图_设置范围 用 页状态、当前页、1 左移 当前阶。
    完。
    合并页 为 做 页描述符_获取 用 当前页。
    若 合并页 不等于 空：
        做 页描述符_设置标志 用 合并页、页_伙伴。
        做 设置 用 合并页、PG_阶、当前阶。
    完。
完。

__伙伴_是空闲 接受 z、页号、阶：
    若 页状态 等于 空：
        返还 假。
    完。
    若 非 做 位图_测试 用 页状态、页号：
        返还 假。
    完。
    页 为 做 页描述符_获取 用 页号。
    若 页 等于 空：
        返还 假。
    完。
    若 非 做 页描述符_测试标志 用 页、页_伙伴：
        返还 假。
    完。
    若 页 的 第 PG_阶 项 不等于 阶：
        返还 假。
    完。
    返还 真。
完。

__伙伴_移除 接受 z、页号、阶、迁移类型：
    头 为 z 的 空闲区 的 第 阶 项 的 空闲链表 的 第 迁移类型 项。
    可变 当前 为 做 链表_首节点_或空 用 头。
    只要 当前 不等于 空 并且 当前 不等于 头：
        若 做 链表_数据 用 当前 等于 页号：
            做 链表_删除 用 当前。
            z 的 空闲区 的 第 阶 项 的 空闲数 为 z 的 空闲区 的 第 阶 项 的 空闲数 减 1。
            返还 真。
        完。
        当前 为 做 链表_后继 用 当前。
    完。
    返还 假。
完。

伙伴系统_分配 接受 阶：
    返还 做 伙伴系统_分配_带标志 用 阶、GFP_KERNEL。
完。

伙伴系统_分配_带标志 接受 阶、gfp：
    若 阶 大于 最大阶：
        做 内核警告 用 「伙伴系统: 请求阶超限」。
        返还 0 减 1。
    完。
    首选zone 为 做 gfp_到_zone类型 用 gfp。
    迁移类型 为 做 gfp_到_迁移类型 用 gfp。
    是原子 为 gfp 位与 GFP_ATOMIC 不等于 0 或者 gfp 位与 __GFP_HIGH 不等于 0。
完。

可变 AI_每核页释放_运行指标 为 0。

可变 AI_每核页释放_异常指标 为 0。

AI_每核页释放_自诊断 接受 零参：
    AI_每核页释放_运行指标 为 0。
    返还 AI_每核页释放_运行指标。
完。

AI_每核页释放_优化建议 接受 零参：
    指标 为 做 AI_每核页释放_自诊断。
    返还 「每核页释放状态正常」。
完。

本章 收束。
