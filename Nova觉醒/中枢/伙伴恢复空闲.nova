《伙伴恢复空闲》

引用《.·类型定义》。

引用《.·同步原语》。

引用《.·整数转字符串》。

最大阶 为 11。

页大小 为 4096。

伙伴区域 是 一种 事物，含 起始页帧、总页数、空闲页数、空闲链、锁。

可变 区域表 为 一组：（空）。

可变 区域数 为 0。

可变 分配总数 为 0。

可变 释放总数 为 0。

可变 分裂次数 为 0。

可变 合并次数 为 0。

伙伴_初始化 接受 起始帧、页数：
    区域 为 新建 伙伴区域。
    区域 的 起始页帧 为 起始帧。
    区域 的 总页数 为 页数。
    区域 的 空闲页数 为 页数。
    区域 的 空闲链 为 做 堆_分配 用 最大阶 乘 8。
    可变 阶 为 0。
    只要 阶 小于 最大阶：
        做 内存_写64 用 区域 的 空闲链 加 阶 乘 8、0。
        阶 为 阶 加 1。
    完。
    区域 的 锁 为 做 自旋锁_创建。
    可变 帧 为 起始帧。
    只要 帧 加 (1 左移 最大阶 减 1) 小于或等于 起始帧 加 页数：
        做 伙伴_链入 用 区域、最大阶 减 1、帧。
        帧 为 帧 加 (1 左移 最大阶 减 1)。
    完。
    做 追加 用 区域表、区域。
    区域数 为 区域数 加 1。
完。

伙伴_链入 接受 区域、阶、帧号：
    地址 为 帧号 乘 页大小。
    做 内存_写64 用 地址、做 内存_读64 用 区域 的 空闲链 加 阶 乘 8。
    做 内存_写64 用 区域 的 空闲链 加 阶 乘 8、地址。
完。

伙伴_链出 接受 区域、阶：
    头 为 做 内存_读64 用 区域 的 空闲链 加 阶 乘 8。
    若 头 等于 0：
        返还 0。
    完。
    做 内存_写64 用 区域 的 空闲链 加 阶 乘 8、做 内存_读64 用 头。
    返还 头 除以 页大小。
完。

伙伴_分配 接受 区域索引、需要阶：
    若 区域索引 大于或等于 区域数 或者 需要阶 大于或等于 最大阶：
        返还 0。
    完。
    区域 为 区域表 的 第 区域索引 项。
    做 自旋锁_获取 用 区域 的 锁。
    可变 当前阶 为 需要阶。
    只要 当前阶 小于 最大阶：
        若 做 内存_读64 用 区域 的 空闲链 加 当前阶 乘 8 不等于 0：
            跳出。
        完。
        当前阶 为 当前阶 加 1。
    完。
    若 当前阶 大于或等于 最大阶：
        做 自旋锁_释放 用 区域 的 锁。
        返还 0。
    完。
    帧 为 做 伙伴_链出 用 区域、当前阶。
    只要 当前阶 大于 需要阶：
        当前阶 为 当前阶 减 1。
        伙伴帧 为 帧 加 (1 左移 当前阶)。
        做 伙伴_链入 用 区域、当前阶、伙伴帧。
        分裂次数 为 分裂次数 加 1。
    完。
    区域 的 空闲页数 为 区域 的 空闲页数 减 (1 左移 需要阶)。
    分配总数 为 分配总数 加 1。
    做 自旋锁_释放 用 区域 的 锁。
    返还 帧。
完。

伙伴_释放 接受 区域索引、帧号、阶：
    若 区域索引 大于或等于 区域数 或者 阶 大于或等于 最大阶：
        返还。
    完。
    区域 为 区域表 的 第 区域索引 项。
    做 自旋锁_获取 用 区域 的 锁。
    可变 当前帧 为 帧号。
    可变 当前阶 为 阶。
    只要 当前阶 小于 最大阶 减 1：
        伙伴帧 为 当前帧 位异或 1 左移 当前阶。
        若 非 做 伙伴_在空闲链中 用 区域、当前阶、伙伴帧：
            跳出。
        完。
        做 伙伴_从链中移除 用 区域、当前阶、伙伴帧。
        若 伙伴帧 小于 当前帧：
            当前帧 为 伙伴帧。
        完。
        当前阶 为 当前阶 加 1。
        合并次数 为 合并次数 加 1。
    完。
    做 伙伴_链入 用 区域、当前阶、当前帧。
    区域 的 空闲页数 为 区域 的 空闲页数 加 (1 左移 阶)。
    释放总数 为 释放总数 加 1。
    做 自旋锁_释放 用 区域 的 锁。
完。

伙伴_在空闲链中 接受 区域、阶、帧号：
    地址 为 帧号 乘 页大小。
    可变 当前 为 做 内存_读64 用 区域 的 空闲链 加 阶 乘 8。
    只要 当前 不等于 0：
        若 当前 等于 地址：
            返还 真。
        完。
        当前 为 做 内存_读64 用 当前。
    完。
    返还 假。
完。

伙伴_从链中移除 接受 区域、阶、帧号：
    目标 为 帧号 乘 页大小。
    可变 前驱 为 0。
    可变 当前 为 做 内存_读64 用 区域 的 空闲链 加 阶 乘 8。
    只要 当前 不等于 0：
        若 当前 等于 目标：
            若 前驱 等于 0：
                做 内存_写64 用 区域 的 空闲链 加 阶 乘 8、做 内存_读64 用 当前。
            否则：
                做 内存_写64 用 前驱、做 内存_读64 用 当前。
            完。
            返还。
        完。
        前驱 为 当前。
        当前 为 做 内存_读64 用 当前。
    完。
完。

伙伴_恢复空闲计数 接受 区域索引、空闲数：
    若 区域索引 小于 0 或者 区域索引 大于或等于 区域数：
        返还。
    完。
    区域表 的 第 区域索引 项 的 空闲页数 为 空闲数。
完。

伙伴_获取空闲 接受 区域索引：
    若 区域索引 大于或等于 区域数：
        返还 0。
    完。
    返还 区域表 的 第 区域索引 项 的 空闲页数。
完。

伙伴_各阶空闲数 接受 区域索引、阶：
    若 区域索引 大于或等于 区域数 或者 阶 大于或等于 最大阶：
        返还 0。
    完。
    可变 数量 为 0。
    可变 当前 为 做 内存_读64 用 区域表 的 第 区域索引 项 的 空闲链 加 阶 乘 8。
    只要 当前 不等于 0：
        数量 为 数量 加 1。
        当前 为 做 内存_读64 用 当前。
    完。
    返还 数量。
完。

伙伴系统_完整统计 接受 零参：
    返还 一组：分配总数、释放总数、分裂次数、合并次数、区域数。
完。

伙伴_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 区域数 等于 0：
        做 追加 用 问题、「伙伴系统未初始化: 无区域」。
        返还 问题。
    完。
    可变 i 为 0。
    可变 总空闲 为 0。
    可变 总页 为 0。
    只要 i 小于 区域数：
        总空闲 为 总空闲 加 区域表 的 第 i 项 的 空闲页数。
        总页 为 总页 加 区域表 的 第 i 项 的 总页数。
        i 为 i 加 1。
    完。
    若 总页 大于 0 并且 总空闲 乘 100 除以 总页 小于 5：
        做 追加 用 问题、「物理内存严重不足: 空闲率」 加 做 整数转字符串 用 总空闲 乘 100 除以 总页 加 「%」。
    完。
    若 分裂次数 大于 合并次数 乘 5 并且 合并次数 大于 100：
        做 追加 用 问题、「碎片化严重: 分裂/合并比=」 加 做 整数转字符串 用 分裂次数 除以 合并次数。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「伙伴系统健康」。
    完。
    返还 问题。
完。

伙伴_碎片化指数 接受 零参：
    若 区域数 等于 0：
        返还 0。
    完。
    可变 总碎片 为 0。
    可变 i 为 0。
    只要 i 小于 区域数：
        可变 低阶数 为 0。
        可变 高阶数 为 0。
        可变 阶 为 0。
        只要 阶 小于 最大阶：
            数量 为 做 伙伴_各阶空闲数 用 i、阶。
            若 阶 小于 3：
                低阶数 为 低阶数 加 数量。
            否则：
                高阶数 为 高阶数 加 数量。
            完。
            阶 为 阶 加 1。
        完。
        若 低阶数 加 高阶数 大于 0：
            总碎片 为 总碎片 加 低阶数 乘 100 除以 (低阶数 加 高阶数)。
        完。
        i 为 i 加 1。
    完。
    返还 总碎片 除以 区域数。
完。

伙伴_区域总页数 接受 区域索引：
    若 区域索引 大于或等于 区域数：
        返还 0。
    完。
    返还 区域表 的 第 区域索引 项 的 总页数。
完。

伙伴_区域使用率 接受 区域索引：
    若 区域索引 大于或等于 区域数：
        返还 0。
    完。
    区域 为 区域表 的 第 区域索引 项。
    若 区域 的 总页数 等于 0：
        返还 0。
    完。
    返还 (区域 的 总页数 减 区域 的 空闲页数) 乘 100 除以 区域 的 总页数。
完。

伙伴_区域空闲率 接受 区域索引：
    返还 100 减 做 伙伴_区域使用率 用 区域索引。
完。

伙伴_区域起始帧 接受 区域索引：
    若 区域索引 大于或等于 区域数：
        返还 0。
    完。
    返还 区域表 的 第 区域索引 项 的 起始页帧。
完。

伙伴_区域起始物理地址 接受 区域索引：
    返还 做 伙伴_区域起始帧 用 区域索引 乘 页大小。
完。

伙伴_区域末尾物理地址 接受 区域索引：
    若 区域索引 大于或等于 区域数：
        返还 0。
    完。
    返还 (区域表 的 第 区域索引 项 的 起始页帧 加 区域表 的 第 区域索引 项 的 总页数) 乘 页大小。
完。

伙伴_区域大小字节 接受 区域索引：
    返还 做 伙伴_区域总页数 用 区域索引 乘 页大小。
完。

伙伴_区域大小MB 接受 区域索引：
    返还 做 伙伴_区域大小字节 用 区域索引 除以 1024 除以 1024。
完。

伙伴_总空闲页 接受 零参：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 区域数：
        总 为 总 加 区域表 的 第 i 项 的 空闲页数。
        i 为 i 加 1。
    完。
    返还 总。
完。

伙伴_总页数 接受 零参：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 区域数：
        总 为 总 加 区域表 的 第 i 项 的 总页数。
        i 为 i 加 1。
    完。
    返还 总。
完。

伙伴_总使用页 接受 零参：
    返还 做 伙伴_总页数 减 做 伙伴_总空闲页。
完。

伙伴_总空闲字节 接受 零参：
    返还 做 伙伴_总空闲页 乘 页大小。
完。

伙伴_总空闲MB 接受 零参：
    返还 做 伙伴_总空闲字节 除以 1024 除以 1024。
完。

伙伴_全局使用率 接受 零参：
    总 为 做 伙伴_总页数。
    若 总 等于 0：
        返还 0。
    完。
    返还 做 伙伴_总使用页 乘 100 除以 总。
完。

伙伴_全局空闲率 接受 零参：
    返还 100 减 做 伙伴_全局使用率。
完。

伙伴_批量分配 接受 区域索引、阶、数量：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 数量：
        帧 为 做 伙伴_分配 用 区域索引、阶。
        若 帧 等于 0：
            跳出。
        完。
        做 追加 用 结果、帧。
        i 为 i 加 1。
    完。
    返还 结果。
完。

伙伴_批量释放 接受 区域索引、帧列表、阶：
    可变 i 为 0。
    只要 i 小于 做 计数 用 帧列表：
        做 伙伴_释放 用 区域索引、帧列表 的 第 i 项、阶。
        i 为 i 加 1。
    完。
完。

伙伴_字节转阶 接受 字节数：
    需要页 为 (字节数 加 页大小 减 1) 除以 页大小。
    可变 阶 为 0。
    只要 1 左移 阶 小于 需要页：
        阶 为 阶 加 1。
    完。
    若 阶 大于或等于 最大阶：
        返还 最大阶 减 1。
    完。
    返还 阶。
完。

伙伴_阶转页数 接受 阶：
    若 阶 大于或等于 最大阶：
        返还 1 左移 最大阶 减 1。
    完。
    返还 1 左移 阶。
完。

伙伴_阶转字节 接受 阶：
    返还 做 伙伴_阶转页数 用 阶 乘 页大小。
完。

伙伴_阶名称 接受 阶：
    若 阶 等于 0：
        返还 「4KB」。
    完。
    若 阶 等于 1：
        返还 「8KB」。
    完。
    若 阶 等于 2：
        返还 「16KB」。
    完。
    若 阶 等于 3：
        返还 「32KB」。
    完。
    若 阶 等于 4：
        返还 「64KB」。
    完。
    若 阶 等于 5：
        返还 「128KB」。
    完。
    若 阶 等于 6：
        返还 「256KB」。
    完。
    若 阶 等于 7：
        返还 「512KB」。
    完。
    若 阶 等于 8：
        返还 「1MB」。
    完。
    若 阶 等于 9：
        返还 「2MB」。
    完。
    若 阶 等于 10：
        返还 「4MB」。
    完。
    返还 「阶」 加 做 整数转字符串 用 阶。
完。

压力_正常 为 0。

压力_低 为 1。

压力_中 为 2。

压力_高 为 3。

压力_临界 为 4。

可变 内存_低水位 为 1024。

可变 内存_高水位 为 4096。

可变 内存_临界水位 为 256。

伙伴_内存压力 接受 零参：
    空闲 为 做 伙伴_总空闲页。
    若 空闲 小于 内存_临界水位：
        返还 压力_临界。
    完。
    若 空闲 小于 内存_低水位：
        返还 压力_高。
    完。
    若 空闲 小于 内存_高水位：
        返还 压力_中。
    完。
    返还 压力_正常。
完。

伙伴_压力名称 接受 级别：
    若 级别 等于 压力_正常：
        返还 「正常」。
    完。
    若 级别 等于 压力_低：
        返还 「低压」。
    完。
    若 级别 等于 压力_中：
        返还 「中压」。
    完。
    若 级别 等于 压力_高：
        返还 「高压」。
    完。
    若 级别 等于 压力_临界：
        返还 「临界」。
    完。
    返还 「未知」。
完。

伙伴_需要回收 接受 零参：
    返还 做 伙伴_内存压力 大于或等于 压力_中。
完。

伙伴_需要OOM 接受 零参：
    返还 做 伙伴_内存压力 大于或等于 压力_临界。
完。

伙伴_设置低水位 接受 值：
    若 值 大于 0：
        内存_低水位 为 值。
    完。
完。

伙伴_设置高水位 接受 值：
    若 值 大于 内存_低水位：
        内存_高水位 为 值。
    完。
完。

伙伴_设置临界水位 接受 值：
    若 值 大于 0 并且 值 小于 内存_低水位：
        内存_临界水位 为 值。
    完。
完。

伙伴_获取水位线 接受 零参：
    返还 一本字典：低 对应 内存_低水位、高 对应 内存_高水位、临界 对应 内存_临界水位。
完。

可变 分配失败总数 为 0。

可变 分配_上次时间戳 为 0。

可变 分配_窗口计数 为 0。

可变 释放_窗口计数 为 0。

伙伴_分配失败率 接受 零参：
    若 分配总数 等于 0：
        返还 0。
    完。
    返还 分配失败总数 乘 100 除以 分配总数。
完。

伙伴_合并率 接受 零参：
    若 释放总数 等于 0：
        返还 0。
    完。
    返还 合并次数 乘 100 除以 释放总数。
完。

伙伴_分裂率 接受 零参：
    若 分配总数 等于 0：
        返还 0。
    完。
    返还 分裂次数 乘 100 除以 分配总数。
完。

伙伴_净分配 接受 零参：
    返还 分配总数 减 释放总数。
完。

伙伴_重置统计 接受 零参：
    分配总数 为 0。
    释放总数 为 0。
    分裂次数 为 0。
    合并次数 为 0。
    分配失败总数 为 0。
    分配_窗口计数 为 0。
    释放_窗口计数 为 0。
完。

伙伴_打印区域信息 接受 区域索引：
    若 区域索引 大于或等于 区域数：
        返还。
    完。
    区域 为 区域表 的 第 区域索引 项。
    做 内核信息 用 「伙伴区域[」 加 做 整数转字符串 用 区域索引 加 「]: 起始=」 加 做 十六进制 用 区域 的 起始页帧 乘 页大小 加 「 总页=」 加 做 整数转字符串 用 区域 的 总页数 加 「 空闲=」 加 做 整数转字符串 用 区域 的 空闲页数 加 「 使用率=」 加 做 整数转字符串 用 做 伙伴_区域使用率 用 区域索引 加 「%」。
完。

伙伴_打印所有区域 接受 零参：
    可变 i 为 0。
    只要 i 小于 区域数：
        做 伙伴_打印区域信息 用 i。
        i 为 i 加 1。
    完。
    做 内核信息 用 「伙伴系统: 总空闲=」 加 做 整数转字符串 用 做 伙伴_总空闲MB 加 「MB」 加 「 压力=」 加 做 伙伴_压力名称 用 做 伙伴_内存压力 加 「 碎片=」 加 做 整数转字符串 用 做 伙伴_碎片化指数 加 「%」。
完。

伙伴_打印各阶分布 接受 区域索引：
    若 区域索引 大于或等于 区域数：
        返还。
    完。
    可变 阶 为 0。
    只要 阶 小于 最大阶：
        数量 为 做 伙伴_各阶空闲数 用 区域索引、阶。
        做 内核信息 用 「  阶」 加 做 整数转字符串 用 阶 加 「 (」 加 做 伙伴_阶名称 用 阶 加 「): 」 加 做 整数转字符串 用 数量 加 「块」。
        阶 为 阶 加 1。
    完。
完。

伙伴_紧急分配 接受 需要阶：
    可变 i 为 0。
    只要 i 小于 区域数：
        帧 为 做 伙伴_分配 用 i、需要阶。
        若 帧 不等于 0：
            返还 帧。
        完。
        i 为 i 加 1。
    完。
    分配失败总数 为 分配失败总数 加 1。
    返还 0。
完。

伙伴_智能分配 接受 字节数：
    阶 为 做 伙伴_字节转阶 用 字节数。
    可变 i 为 0。
    最佳区 为 负 1。
    可变 最大空闲 为 0。
    只要 i 小于 区域数：
        若 区域表 的 第 i 项 的 空闲页数 大于 最大空闲：
            最大空闲 为 区域表 的 第 i 项 的 空闲页数。
            最佳区 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最佳区 小于 0：
        分配失败总数 为 分配失败总数 加 1。
        返还 0。
    完。
    返还 做 伙伴_分配 用 最佳区、阶。
完。

BUDDY_ORDER_0 为 0。

BUDDY_ORDER_1 为 1。

BUDDY_ORDER_2 为 2。

BUDDY_ORDER_3 为 3。

BUDDY_ORDER_4 为 4。

BUDDY_ORDER_5 为 5。

BUDDY_ORDER_6 为 6。

BUDDY_ORDER_7 为 7。

BUDDY_ORDER_8 为 8。

BUDDY_ORDER_9 为 9。

BUDDY_ORDER_10 为 10。

GFP_KERNEL 为 0x01。

GFP_ATOMIC 为 0x02。

GFP_USER 为 0x04。

GFP_HIGHMEM 为 0x08。

GFP_DMA 为 0x10。

GFP_ZERO 为 0x20。

GFP_NOWARN 为 0x40。

GFP_NOFAIL 为 0x80。

伙伴_GFP标志名 接受 标志：
    若 标志 位与 GFP_ATOMIC：
        返还 「GFP_ATOMIC」。
    完。
    若 标志 位与 GFP_KERNEL：
        返还 「GFP_KERNEL」。
    完。
    若 标志 位与 GFP_USER：
        返还 「GFP_USER」。
    完。
    若 标志 位与 GFP_DMA：
        返还 「GFP_DMA」。
    完。
    返还 「GFP_0x」 加 做 十六进制 用 标志。
完。

伙伴_带标志分配 接受 需要阶、gfp：
    若 gfp 位与 GFP_ATOMIC：
        返还 做 伙伴_紧急分配 用 需要阶。
    完。
    若 做 伙伴_需要回收 并且 非 (gfp 位与 GFP_NOFAIL)：
        返还 0。
    完。
    返还 做 伙伴_智能分配 用 做 伙伴_阶转字节 用 需要阶。
完。

伙伴_详细统计 接受 零参：
    返还 一本字典：区域数 对应 区域数、总页数 对应 做 伙伴_总页数、空闲页数 对应 做 伙伴_总空闲页、使用页数 对应 做 伙伴_总使用页、空闲MB 对应 做 伙伴_总空闲MB、全局使用率 对应 做 伙伴_全局使用率、分配总数 对应 分配总数、释放总数 对应 释放总数、分裂次数 对应 分裂次数、合并次数 对应 合并次数、分配失败 对应 分配失败总数、碎片指数 对应 做 伙伴_碎片化指数、内存压力 对应 做 伙伴_压力名称 用 做 伙伴_内存压力、分配失败率 对应 做 伙伴_分配失败率、合并率 对应 做 伙伴_合并率、分裂率 对应 做 伙伴_分裂率。
完。

nova_ln_v2 接受 x：
    若 x 小于或等于 0：
        返还 负 10 乘 Q_ONE。
    完。
    LN2_Q 为 709。
    可变 k 为 0。
    可变 m 为 x。
    只要 m 大于或等于 2 乘 Q_ONE：
        m 为 m 除以 2。
        k 为 k 加 1。
    完。
    只要 m 小于 Q_ONE：
        m 为 m 乘 2。
        k 为 k 减 1。
    完。
    u 为 m 减 Q_ONE。
    two_plus_u 为 2 乘 Q_ONE 加 u。
    pade 为 做 Q除 用 2 乘 u、two_plus_u。
    u2 为 做 Q乘 用 u、u。
    denom2 为 3 乘 做 Q乘 用 two_plus_u、two_plus_u 除以 Q_ONE。
    correction 为 做 Q乘 用 pade、做 Q除 用 u2、denom2。
    返还 pade 加 correction 加 k 乘 LN2_Q。
完。

本章 收束。
