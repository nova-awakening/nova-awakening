《网络系统调用》

引用《.·系统调用表》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·进程任务》。

引用《..·记忆·文件描述符》。

引用《.·字符串替换》。

引用《..·外联·协议族注册》。

引用《.·自动扩容》。

引用《.·释放文件表》。

引用《..·外联·套接字API》。

引用《.·硬链接》。

引用《.·每处理器》。

引用《.·内存系统调用》。

引用《..·执行·平台时间》。

SHUT_RD 为 0。

SHUT_WR 为 1。

SHUT_RDWR 为 2。

可变 socket_创建次数 为 0。

可变 socket_连接次数 为 0。

可变 socket_发送字节 为 0。

可变 socket_接收字节 为 0。

sys_socket 接受 域、类型、协议、_d、_e、_f：
    实际类型 为 类型 位与 0xFF。
    标志 为 类型 位与 按位取反 0xFF。
    若 域 不等于 AF_INET 并且 域 不等于 AF_INET6 并且 域 不等于 AF_UNIX 并且 域 不等于 AF_NETLINK：
        返还 错误_不支持协议族。
    完。
    若 实际类型 不等于 SOCK_STREAM 并且 实际类型 不等于 SOCK_DGRAM 并且 实际类型 不等于 SOCK_RAW：
        返还 错误_不支持协议。
    完。
    sock 为 做 sock_创建 用 域、实际类型、协议。
    若 sock 等于 空：
        返还 错误_不支持协议。
    完。
    当前 为 做 获取当前进程。
    文件表 为 做 进程_文件表 用 当前。
    fd 为 做 文件表_分配 用 文件表。
    若 fd 小于 0：
        返还 错误_打开过多。
    完。
    文件 为 做 socket_文件_创建 用 sock、标志。
    做 文件表_设置 用 文件表、fd、文件。
    socket_创建次数 为 socket_创建次数 加 1。
    返还 fd。
完。

sys_bind 接受 fd、地址、地址长度、_d、_e、_f：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    sock 为 做 文件_获取socket 用 文件。
    若 sock 等于 空：
        返还 错误_不是socket。
    完。
    若 地址 等于 空 或者 地址长度 等于 0：
        返还 错误_无效参数。
    完。
    ops 为 做 socket_操作表 用 sock。
    若 ops 等于 空 或者 ops 的 bind 等于 空：
        返还 错误_不支持操作。
    完。
    返还 做 ops 的 bind 用 sock、地址、地址长度。
完。

sys_listen 接受 fd、backlog、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    sock 为 做 文件_获取socket 用 文件。
    若 sock 等于 空：
        返还 错误_不是socket。
    完。
    若 backlog 大于 128：
        backlog 为 128。
    完。
    若 backlog 小于 0：
        backlog 为 0。
    完。
    ops 为 做 socket_操作表 用 sock。
    若 ops 等于 空 或者 ops 的 listen 等于 空：
        返还 错误_不支持操作。
    完。
    返还 做 ops 的 listen 用 sock、backlog。
完。

sys_accept 接受 fd、地址、地址长度、_d、_e、_f：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    sock 为 做 文件_获取socket 用 文件。
    若 sock 等于 空：
        返还 错误_不是socket。
    完。
    新sock 为 做 sock_创建 用 做 socket_域 用 sock、做 socket_类型 用 sock、0。
    若 新sock 等于 空：
        返还 错误_内存不足。
    完。
    ops 为 做 socket_操作表 用 sock。
    若 ops 等于 空 或者 ops 的 accept 等于 空：
        返还 错误_不支持操作。
    完。
    结果 为 做 ops 的 accept 用 sock、新sock、0。
    若 结果 小于 0：
        返还 结果。
    完。
    文件表 为 做 进程_文件表 用 当前。
    新fd 为 做 文件表_分配 用 文件表。
    若 新fd 小于 0：
        返还 错误_打开过多。
    完。
    新文件 为 做 socket_文件_创建 用 新sock、0。
    做 文件表_设置 用 文件表、新fd、新文件。
    若 地址 不等于 空 并且 地址长度 不等于 空：
        做 ops 的 getname 用 新sock、地址、1。
    完。
    socket_连接次数 为 socket_连接次数 加 1。
    返还 新fd。
完。

sys_connect 接受 fd、地址、地址长度、_d、_e、_f：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    sock 为 做 文件_获取socket 用 文件。
    若 sock 等于 空：
        返还 错误_不是socket。
    完。
    若 地址 等于 空 或者 地址长度 等于 0：
        返还 错误_无效参数。
    完。
    ops 为 做 socket_操作表 用 sock。
    若 ops 等于 空 或者 ops 的 connect 等于 空：
        返还 错误_不支持操作。
    完。
    socket_连接次数 为 socket_连接次数 加 1。
    返还 做 ops 的 connect 用 sock、地址、地址长度、做 文件_标志 用 文件。
完。

sys_sendto 接受 fd、buf、len、flags、目标地址、目标长度：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    sock 为 做 文件_获取socket 用 文件。
    若 sock 等于 空：
        返还 错误_不是socket。
    完。
    ops 为 做 socket_操作表 用 sock。
    若 ops 等于 空 或者 ops 的 sendmsg 等于 空：
        返还 错误_不支持操作。
    完。
    msg 为 做 消息头_创建 用 buf、len、目标地址、目标长度、flags。
    已发 为 做 ops 的 sendmsg 用 sock、msg、len。
    若 已发 大于 0：
        socket_发送字节 为 socket_发送字节 加 已发。
    完。
    返还 已发。
完。

sys_recvfrom 接受 fd、buf、len、flags、来源地址、来源长度：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    sock 为 做 文件_获取socket 用 文件。
    若 sock 等于 空：
        返还 错误_不是socket。
    完。
    ops 为 做 socket_操作表 用 sock。
    若 ops 等于 空 或者 ops 的 recvmsg 等于 空：
        返还 错误_不支持操作。
    完。
    msg 为 做 消息头_创建 用 buf、len、来源地址、来源长度、flags。
    已收 为 做 ops 的 recvmsg 用 sock、msg、len、flags。
    若 已收 大于 0：
        socket_接收字节 为 socket_接收字节 加 已收。
    完。
    返还 已收。
完。

sys_shutdown 接受 fd、how、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    sock 为 做 文件_获取socket 用 文件。
    若 sock 等于 空：
        返还 错误_不是socket。
    完。
    若 how 小于 SHUT_RD 或者 how 大于 SHUT_RDWR：
        返还 错误_无效参数。
    完。
    ops 为 做 socket_操作表 用 sock。
    若 ops 等于 空 或者 ops 的 shutdown 等于 空：
        返还 错误_不支持操作。
    完。
    返还 做 ops 的 shutdown 用 sock、how。
完。

进程_获取文件_网络 接受 进程、fd：
    若 进程 等于 空：
        返还 空。
    完。
    文件表 为 做 进程_文件表 用 进程。
    若 文件表 等于 空：
        返还 空。
    完。
    返还 做 文件表_获取 用 文件表、fd。
完。

可变 网络_已初始化 为 假。

可变 shutdown_次数 为 0。

可变 getsockopt_次数 为 0。

可变 setsockopt_次数 为 0。

可变 getpeername_次数 为 0。

可变 getsockname_次数 为 0。

可变 sendfile_次数 为 0。

可变 网络_错误数 为 0。

可变 网络_延迟总计 为 0。

可变 网络_延迟样本 为 0。

可变 网络_最大延迟 为 0。

可变 网络_速率 为 0。

可变 网络_速率窗口 为 0。

可变 网络_速率时刻 为 0。

可变 网络_峰值速率 为 0。

sys_getsockopt 接受 fd、level、optname、optval、optlen、_f：
    若 fd 小于 0：
        网络_错误数 为 网络_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    getsockopt_次数 为 getsockopt_次数 加 1。
    返还 0。
完。

sys_setsockopt 接受 fd、level、optname、optval、optlen、_f：
    若 fd 小于 0：
        网络_错误数 为 网络_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    setsockopt_次数 为 setsockopt_次数 加 1。
    返还 0。
完。

sys_getpeername 接受 fd、addr、addrlen、_d、_e、_f：
    若 fd 小于 0：
        网络_错误数 为 网络_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    getpeername_次数 为 getpeername_次数 加 1。
    返还 0。
完。

sys_getsockname 接受 fd、addr、addrlen、_d、_e、_f：
    若 fd 小于 0：
        网络_错误数 为 网络_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    getsockname_次数 为 getsockname_次数 加 1。
    返还 0。
完。

sys_sendfile 接受 out_fd、in_fd、offset、count、_e、_f：
    若 out_fd 小于 0 或者 in_fd 小于 0：
        网络_错误数 为 网络_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    sendfile_次数 为 sendfile_次数 加 1。
    网络_速率窗口 为 网络_速率窗口 加 1。
    返还 count。
完。

网络_记录延迟 接受 延迟：
    网络_延迟样本 为 网络_延迟样本 加 1。
    网络_延迟总计 为 网络_延迟总计 加 延迟。
    若 延迟 大于 网络_最大延迟：
        网络_最大延迟 为 延迟。
    完。
完。

网络_平均延迟 接受 零参：
    若 网络_延迟样本 等于 0：
        返还 0。
    完。
    返还 网络_延迟总计 除以 网络_延迟样本。
完。

网络_更新速率 接受 零参：
    现在 为 做 平台时间。
    间隔 为 现在 减 网络_速率时刻。
    若 间隔 大于或等于 1000000：
        网络_速率 为 网络_速率窗口 乘 1000000 除以 间隔。
        若 网络_速率 大于 网络_峰值速率：
            网络_峰值速率 为 网络_速率。
        完。
        网络_速率窗口 为 0。
        网络_速率时刻 为 现在。
    完。
完。

注册网络系统调用 接受 零参：
    返还 0。
    返还 0。
完。

网络系统调用_初始化 接受 零参：
    若 网络_已初始化 等于 真：
        返还 错误_已存在。
    完。
    socket_创建次数 为 0。
    socket_连接次数 为 0。
    socket_发送字节 为 0。
    socket_接收字节 为 0。
    shutdown_次数 为 0。
    getsockopt_次数 为 0。
    setsockopt_次数 为 0。
    getpeername_次数 为 0。
    getsockname_次数 为 0。
    sendfile_次数 为 0。
    网络_错误数 为 0。
    网络_延迟总计 为 0。
    网络_延迟样本 为 0。
    网络_最大延迟 为 0。
    网络_速率 为 0。
    网络_速率窗口 为 0。
    网络_速率时刻 为 0。
    网络_峰值速率 为 0。
    网络_已初始化 为 真。
    做 打印 用 「[网络系统调用] 初始化完成」。
    返还 0。
完。

网络系统调用_完整统计 接受 零参：
    总操作 为 socket_创建次数 加 socket_连接次数 加 shutdown_次数 加 getsockopt_次数 加 setsockopt_次数 加 getpeername_次数 加 getsockname_次数 加 sendfile_次数。
    统计 为 做 字典。
    做 字典设 用 统计、「已初始化」、网络_已初始化。
    做 字典设 用 统计、「socket创建」、socket_创建次数。
    做 字典设 用 统计、「socket连接」、socket_连接次数。
    做 字典设 用 统计、「发送字节」、socket_发送字节。
    做 字典设 用 统计、「接收字节」、socket_接收字节。
    做 字典设 用 统计、「shutdown」、shutdown_次数。
    做 字典设 用 统计、「getsockopt」、getsockopt_次数。
    做 字典设 用 统计、「setsockopt」、setsockopt_次数。
    做 字典设 用 统计、「getpeername」、getpeername_次数。
    做 字典设 用 统计、「getsockname」、getsockname_次数。
    做 字典设 用 统计、「sendfile」、sendfile_次数。
    做 字典设 用 统计、「总操作」、总操作。
    做 字典设 用 统计、「平均延迟(ns)」、做 网络_平均延迟。
    做 字典设 用 统计、「最大延迟(ns)」、网络_最大延迟。
    做 字典设 用 统计、「当前速率」、网络_速率。
    做 字典设 用 统计、「峰值速率」、网络_峰值速率。
    做 字典设 用 统计、「错误数」、网络_错误数。
    返还 统计。
完。

网络系统调用_打印统计 接受 零参：
    统计 为 做 网络系统调用_完整统计。
    做 打印 用 「[网络系统调用] ═══════ 完整统计 ═══════」。
    做 打印 用 「  socket创建:   」 加 做 转字符串 用 做 字典取 用 统计、「socket创建」。
    做 打印 用 「  socket连接:   」 加 做 转字符串 用 做 字典取 用 统计、「socket连接」。
    做 打印 用 「  发送字节:     」 加 做 转字符串 用 做 字典取 用 统计、「发送字节」。
    做 打印 用 「  接收字节:     」 加 做 转字符串 用 做 字典取 用 统计、「接收字节」。
    做 打印 用 「  shutdown:     」 加 做 转字符串 用 做 字典取 用 统计、「shutdown」。
    做 打印 用 「  getsockopt:   」 加 做 转字符串 用 做 字典取 用 统计、「getsockopt」。
    做 打印 用 「  setsockopt:   」 加 做 转字符串 用 做 字典取 用 统计、「setsockopt」。
    做 打印 用 「  sendfile:     」 加 做 转字符串 用 做 字典取 用 统计、「sendfile」。
    做 打印 用 「  总操作:       」 加 做 转字符串 用 做 字典取 用 统计、「总操作」。
    做 打印 用 「  平均延迟:     」 加 做 转字符串 用 做 字典取 用 统计、「平均延迟(ns)」 加 「ns」。
    做 打印 用 「  当前速率:     」 加 做 转字符串 用 做 字典取 用 统计、「当前速率」 加 「 次/秒」。
    做 打印 用 「  峰值速率:     」 加 做 转字符串 用 做 字典取 用 统计、「峰值速率」 加 「 次/秒」。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[网络系统调用] ═════════════════════════」。
    返还 0。
完。

网络系统调用_dump 接受 零参：
    做 打印 用 「[网络系统调用 dump] ──────────────────」。
    做 打印 用 「  已初始化: 」 加 做 转字符串 用 网络_已初始化。
    做 打印 用 「  socket创建: 」 加 做 转字符串 用 socket_创建次数。
    做 打印 用 「  连接: 」 加 做 转字符串 用 socket_连接次数。
    做 打印 用 「  发送: 」 加 做 转字符串 用 socket_发送字节 加 「 字节」。
    做 打印 用 「  接收: 」 加 做 转字符串 用 socket_接收字节 加 「 字节」。
    做 打印 用 「  shutdown: 」 加 做 转字符串 用 shutdown_次数。
    做 打印 用 「  sendfile: 」 加 做 转字符串 用 sendfile_次数。
    做 打印 用 「  速率: 」 加 做 转字符串 用 网络_速率。
    做 打印 用 「  错误: 」 加 做 转字符串 用 网络_错误数。
    做 打印 用 「[网络系统调用 dump] ──────────────────」。
完。

网络系统调用_销毁 接受 零参：
    若 网络_已初始化 等于 假：
        返还 0。
    完。
    网络_已初始化 为 假。
    做 打印 用 「[网络系统调用] 已销毁」。
    返还 0。
完。

网络系统调用_为procfs提供统计 接受 零参：
    返还 一组：socket_创建次数、socket_连接次数、socket_发送字节、socket_接收字节、shutdown_次数、sendfile_次数、网络_速率。
完。

网络系统调用_为AI提供网络模式 接受 零参：
    返还 一组：socket_创建次数、socket_连接次数、socket_发送字节、socket_接收字节、做 网络_平均延迟、网络_最大延迟、网络_速率。
完。

网络系统调用_重置统计 接受 零参：
    socket_创建次数 为 0。
    socket_连接次数 为 0。
    socket_发送字节 为 0。
    socket_接收字节 为 0。
    shutdown_次数 为 0。
    getsockopt_次数 为 0。
    setsockopt_次数 为 0。
    getpeername_次数 为 0。
    getsockname_次数 为 0。
    sendfile_次数 为 0。
    网络_错误数 为 0。
    网络_延迟总计 为 0。
    网络_延迟样本 为 0。
    网络_最大延迟 为 0。
    网络_速率 为 0。
    网络_速率窗口 为 0。
    网络_峰值速率 为 0。
    做 打印 用 「[网络系统调用] 统计已重置」。
    返还 0。
完。

平台时间 接受 零参：
    返还 做 平台_获取高精度时间。
完。

网络系统调用_为调度器提供负载 接受 零参：
    返还 一组：网络_速率、网络_峰值速率、socket_创建次数 加 socket_连接次数。
完。

网络系统调用_为电源管理挂起 接受 零参：
    做 打印 用 「[网络] 挂起: 保存socket状态」。
    返还 网络系统调用_总数。
完。

网络系统调用_获取吞吐量 接受 零参：
    返还 socket_发送字节 加 socket_接收字节。
完。

网络系统调用_获取连接数 接受 零参：
    返还 socket_连接次数。
完。

网络系统调用_获取创建数 接受 零参：
    返还 socket_创建次数。
完。

本章 收束。
