《信号获取默》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·用户空间切》。

引用《.·信号动作设置》。

引用《.·进程描述符》。

引用《..·引导·端口IO》。

信号_获取默认动作 接受 信号号：
    若 信号号 等于 1：
        返还 动作_终止。
    完。
    若 信号号 等于 2：
        返还 动作_终止。
    完。
    若 信号号 等于 3：
        返还 动作_核心转储。
    完。
    若 信号号 等于 4：
        返还 动作_核心转储。
    完。
    若 信号号 等于 6：
        返还 动作_核心转储。
    完。
    若 信号号 等于 8：
        返还 动作_核心转储。
    完。
    若 信号号 等于 9：
        返还 动作_终止。
    完。
    若 信号号 等于 11：
        返还 动作_核心转储。
    完。
    若 信号号 等于 13：
        返还 动作_终止。
    完。
    若 信号号 等于 14：
        返还 动作_终止。
    完。
    若 信号号 等于 15：
        返还 动作_终止。
    完。
    若 信号号 等于 17：
        返还 动作_忽略。
    完。
    若 信号号 等于 18：
        返还 动作_继续。
    完。
    若 信号号 等于 19：
        返还 动作_停止。
    完。
    若 信号号 等于 20：
        返还 动作_停止。
    完。
    若 信号号 等于 21：
        返还 动作_停止。
    完。
    若 信号号 等于 22：
        返还 动作_停止。
    完。
    返还 动作_终止。
完。

信号_设置帧 接受 任务、信号号、处理器：
    保存的rip 为 做 进程_用户RIP 用 任务。
    保存的rsp 为 做 进程_用户RSP 用 任务。
    帧地址 为 保存的rsp 减 512。
    做 允许用户内存访问。
    做 内存写64 用 帧地址 加 0、保存的rip。
    做 内存写64 用 帧地址 加 8、保存的rsp。
    做 内存写64 用 帧地址 加 16、做 进程_用户RFLAGS 用 任务。
    做 内存写64 用 帧地址 加 24、信号号。
    做 内存写64 用 帧地址 加 32、0x050F0000000FC0C7。
    做 禁止用户内存访问。
    做 进程_设置用户RIP 用 任务、处理器。
    做 进程_设置用户RSP 用 任务、帧地址。
完。

信号_出队 接受 任务、信号号：
    挂起 为 做 进程_信号挂起 用 任务。
    做 进程_设置信号挂起 用 任务、挂起 位与 按位取反 (1 左移 信号号)。
完。

信号_清除挂起 接受 任务、掩码：
    挂起 为 做 进程_信号挂起 用 任务。
    做 进程_设置信号挂起 用 任务、挂起 位与 按位取反 掩码。
完。

signal_wake_up 接受 任务、信号号：
    状态 为 做 进程_状态 用 任务。
    若 状态 等于 进程_睡眠：
        做 进程_设置状态 用 任务、进程_就绪。
    完。
    若 状态 等于 进程_停止 并且 信号号 等于 18：
        做 进程_设置状态 用 任务、进程_就绪。
    完。
    若 信号号 等于 9：
        若 状态 不等于 进程_就绪 并且 状态 不等于 进程_僵尸：
            做 进程_设置状态 用 任务、进程_就绪。
        完。
    完。
完。

通知父进程_停止 接受 任务：
    父 为 做 进程_父 用 任务。
    若 父 不等于 空：
        做 信号_发送 用 父、17。
    完。
完。

信号_是致命 接受 信号号：
    动作 为 做 信号_获取默认动作 用 信号号。
    返还 动作 等于 动作_终止 或者 动作 等于 动作_核心转储。
完。

进程_获取信号处理器 接受 任务、信号号：
    若 任务 等于 空 或者 信号号 小于 1 或者 信号号 大于或等于 信号_数量：
        返还 SIG_DFL。
    完。
    处理器表 为 做 进程_sighand 用 任务。
    若 处理器表 等于 空：
        返还 SIG_DFL。
    完。
    返还 处理器表 的 第 信号号 项。
完。

进程_设置信号处理器 接受 任务、信号号、处理器：
    若 任务 等于 空 或者 信号号 小于 1 或者 信号号 大于或等于 信号_数量：
        返还。
    完。
    若 信号号 等于 9 或者 信号号 等于 19：
        返还。
    完。
    处理器表 为 做 进程_sighand 用 任务。
    若 处理器表 等于 空：
        返还。
    完。
    做 设置 用 处理器表、信号号、处理器。
完。

进程_重置信号处理器 接受 任务：
    处理器表 为 做 进程_sighand 用 任务。
    若 处理器表 等于 空：
        返还。
    完。
    可变 i 为 1。
    只要 i 小于 信号_数量：
        若 处理器表 的 第 i 项 不等于 SIG_IGN：
            做 设置 用 处理器表、i、SIG_DFL。
        完。
        i 为 i 加 1。
    完。
完。

进程_信号掩码 接受 任务：
    若 任务 等于 空：
        返还 0。
    完。
    返还 任务 的 第 T_信号掩码 项。
完。

进程_设置信号掩码 接受 任务、掩码：
    若 任务 等于 空：
        返还。
    完。
    掩码 为 掩码 位与 按位取反 不可屏蔽掩码。
    做 设置 用 任务、T_信号掩码、掩码。
完。

进程_信号挂起 接受 任务：
    若 任务 等于 空：
        返还 0。
    完。
    返还 任务 的 第 T_信号挂起 项。
完。

进程_设置信号挂起 接受 任务、值：
    若 任务 等于 空：
        返还。
    完。
    做 设置 用 任务、T_信号挂起、值。
完。

信号_统计 接受 零参：
    返还 一组：信号_发送总数、信号_处理总数、信号_忽略总数。
完。

可变 AI_信号获取默_负载窗口 为 一组：（空）。

AI_信号获取默_负载预测 接受 当前负载：
    做 追加 用 AI_信号获取默_负载窗口、当前负载。
    若 做 计数 用 AI_信号获取默_负载窗口 大于 20：
        做 列表_移除 用 AI_信号获取默_负载窗口、0。
    完。
    若 做 计数 用 AI_信号获取默_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_信号获取默_负载窗口。
    趋势 为 AI_信号获取默_负载窗口 的 第 n 减 1 项 减 AI_信号获取默_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_信号获取默_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
