《赫布收敛检》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·赫布追加》。

引用《.·整数转字符串》。

赫布_收敛检测 接受 walsh谱：
    n 为 做 计数 用 walsh谱。
    可变 零数 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 walsh谱 的 第 i 项 等于 0：
            零数 为 零数 加 1。
        完。
        i 为 i 加 1。
    完。
    支撑度 为 (n 减 零数) 乘 1000 除以 n。
    可变 全支撑 为 0。
    若 零数 等于 0：
        全支撑 为 1。
    完。
    返还 一组：全支撑、支撑度、零数。
完。

赫布_Nova联想网络学习 接受 模式列表、模式数、维度：
    可变 w 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度 乘 维度：
        做 赫布_追加 用 w、0。
        i 为 i 加 1。
    完。
    可变 pi 为 0。
    只要 pi 小于 模式数：
        偏移 为 pi 乘 维度。
        i 为 0。
        只要 i 小于 维度：
            可变 j 为 0。
            只要 j 小于 维度：
                若 i 不等于 j：
                    w 的 第 i 乘 维度 加 j 项 为 w 的 第 i 乘 维度 加 j 项 加 模式列表 的 第 偏移 加 i 项 乘 模式列表 的 第 偏移 加 j 项。
                完。
                j 为 j 加 1。
            完。
            i 为 i 加 1。
        完。
        pi 为 pi 加 1。
    完。
    返还 w。
完。

赫布_Nova联想网络回忆 接受 w、输入、维度、最大迭代：
    可变 状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 赫布_追加 用 状态、做 赫布_符号 用 输入 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 迭代 为 0。
    只要 迭代 小于 最大迭代：
        可变 变化 为 0。
        i 为 0。
        只要 i 小于 维度：
            可变 和 为 0。
            可变 j 为 0。
            只要 j 小于 维度：
                和 为 和 加 w 的 第 i 乘 维度 加 j 项 乘 状态 的 第 j 项。
                j 为 j 加 1。
            完。
            新值 为 做 赫布_符号 用 和。
            若 新值 不等于 状态 的 第 i 项：
                变化 为 变化 加 1。
            完。
            状态 的 第 i 项 为 新值。
            i 为 i 加 1。
        完。
        若 变化 等于 0：
            返还 状态。
        完。
        迭代 为 迭代 加 1。
    完。
    返还 状态。
完。

赫布_Nova联想网络容量 接受 维度：
    返还 维度 乘 138 除以 1000。
完。

赫布_竞争学习 接受 原型、输入、维度、学习率：
    k 为 做 计数 用 原型 除以 维度。
    可变 最近 为 0。
    可变 最近距 为 0。
    可变 首次 为 1。
    可变 pi 为 0。
    只要 pi 小于 k：
        可变 距离 为 0。
        可变 d 为 0。
        只要 d 小于 维度：
            差 为 原型 的 第 pi 乘 维度 加 d 项 减 输入 的 第 d 项。
            距离 为 距离 加 差 乘 差 除以 1000。
            d 为 d 加 1。
        完。
        若 首次 等于 1 或者 距离 小于 最近距：
            最近距 为 距离。
            最近 为 pi。
            首次 为 0。
        完。
        pi 为 pi 加 1。
    完。
    可变 d2 为 0。
    只要 d2 小于 维度：
        增量 为 输入 的 第 d2 项 减 原型 的 第 最近 乘 维度 加 d2 项。
        原型 的 第 最近 乘 维度 加 d2 项 为 原型 的 第 最近 乘 维度 加 d2 项 加 学习率 乘 增量 除以 1000。
        d2 为 d2 加 1。
    完。
    返还 最近。
完。

可变 在线零数累计 为 0。

赫布_在线支撑度更新 接受 旧值、新值、总维度：
    若 旧值 不等于 0 并且 新值 等于 0：
        在线零数累计 为 在线零数累计 加 1。
    完。
    若 旧值 等于 0 并且 新值 不等于 0：
        在线零数累计 为 在线零数累计 减 1。
    完。
    若 在线零数累计 小于 0：
        在线零数累计 为 0。
    完。
    返还 (总维度 减 在线零数累计) 乘 1000 除以 (总维度 加 1)。
完。

可变 曲线_损失EMA 为 0。

可变 曲线_步数 为 0。

可变 曲线_最低 为 999999。

赫布_曲线记录 接受 当前损失：
    曲线_步数 为 曲线_步数 加 1。
    曲线_损失EMA 为 (曲线_损失EMA 乘 900 加 当前损失 乘 100) 除以 1000。
    若 曲线_损失EMA 小于 曲线_最低：
        曲线_最低 为 曲线_损失EMA。
    完。
    返还 曲线_损失EMA。
完。

赫布_是否停滞 接受 容忍步数：
    若 曲线_步数 小于 容忍步数：
        返还 0。
    完。
    若 曲线_损失EMA 大于 曲线_最低 乘 1050 除以 1000：
        返还 1。
    完。
    返还 0。
完。

赫布_容量预警 接受 已存模式数、维度：
    容量 为 维度 乘 138 除以 1000。
    若 容量 小于或等于 0：
        返还 2。
    完。
    利用率 为 已存模式数 乘 1000 除以 容量。
    若 利用率 大于或等于 900：
        返还 2。
    完。
    若 利用率 大于或等于 700：
        返还 1。
    完。
    返还 0。
完。

可变 Oja_更新次数 为 0。

赫布_Oja更新 接受 权重、输入、维度、学习率：
    可变 y 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        y 为 y 加 权重 的 第 i 项 乘 输入 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 维度：
        增量 为 学习率 乘 (输入 的 第 i 项 乘 y 除以 1000 减 y 乘 y 除以 1000 乘 权重 的 第 i 项 除以 1000) 除以 1000。
        权重 的 第 i 项 为 权重 的 第 i 项 加 增量。
        i 为 i 加 1。
    完。
    Oja_更新次数 为 Oja_更新次数 加 1。
    返还 y。
完。

可变 BCM_阈值 为 500。

可变 BCM_更新次数 为 0。

赫布_BCM更新 接受 权重、输入、输出、维度、学习率：
    调制 为 输出 乘 (输出 减 BCM_阈值) 除以 1000。
    可变 i 为 0。
    只要 i 小于 维度：
        权重 的 第 i 项 为 权重 的 第 i 项 加 学习率 乘 输入 的 第 i 项 乘 调制 除以 1000000。
        i 为 i 加 1。
    完。
    BCM_阈值 为 (BCM_阈值 乘 950 加 输出 乘 输出 除以 1000 乘 50) 除以 1000。
    BCM_更新次数 为 BCM_更新次数 加 1。
完。

STDP_A正 为 100。

STDP_A负 为 80。

STDP_τ 为 20。

可变 STDP_更新次数 为 0。

_STDP_衰减 接受 差值：
    可变 d 为 差值。
    若 d 小于 0：
        d 为 0 减 d。
    完。
    返还 1000000 除以 (1000 加 50 乘 d)。
完。

赫布_STDP更新 接受 权重、pre时间、post时间、维度：
    Δt 为 post时间 减 pre时间。
    可变 变化量 为 0。
    若 Δt 大于 0：
        变化量 为 STDP_A正 乘 做 _STDP_衰减 用 Δt 除以 1000。
    否则：
        变化量 为 0 减 STDP_A负 乘 做 _STDP_衰减 用 Δt 除以 1000。
    完。
    可变 i 为 0。
    只要 i 小于 维度：
        权重 的 第 i 项 为 权重 的 第 i 项 加 变化量。
        i 为 i 加 1。
    完。
    STDP_更新次数 为 STDP_更新次数 加 1。
    返还 变化量。
完。

赫布收敛_完整统计 接受 零参：
    返还 一组：一组：「在线零数累计」、在线零数累计、一组：「曲线步数」、曲线_步数、一组：「曲线最低损失」、曲线_最低、一组：「Oja更新次数」、Oja_更新次数、一组：「BCM更新次数」、BCM_更新次数、一组：「BCM阈值」、BCM_阈值、一组：「STDP更新次数」、STDP_更新次数。
完。

赫布_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 曲线_步数 大于 10000 并且 曲线_最低 大于 0.5：
        做 追加 用 问题、「收敛缓慢: 步数=」 加 做 整数转字符串 用 曲线_步数 加 「 损失仍高」。
    完。
    若 BCM_阈值 小于 0.001：
        做 追加 用 问题、「BCM阈值过低，可能导致不稳定」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「赫布学习健康」。
    完。
    返还 问题。
完。

赫布_重置收敛 接受 零参：
    曲线_步数 为 0。
    曲线_最低 为 999999。
    Oja_更新次数 为 0。
    BCM_更新次数 为 0。
    STDP_更新次数 为 0。
完。

本章 收束。
