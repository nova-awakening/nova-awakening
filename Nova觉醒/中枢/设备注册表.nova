《设备注册表》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·字符串替换》。

设备_未知 为 0。

设备_就绪 为 1。

设备_忙碌 为 2。

设备_错误 为 3。

设备_离线 为 4。

设备_挂起 为 5。

最大设备数 为 256。

设备信息 是 一种 事物，含 标识、类型、状态、驱动名、中断号、基地址、错误计数、请求计数、响应总时、最近响应、健康分数、功耗级别。

可变 设备表 为 一组：（空）。

可变 设备数 为 0。

可变 热插拔次数 为 0。

可变 总错误数 为 0。

设备_注册 接受 类型、驱动名、中断号、基地址：
    若 设备数 大于或等于 最大设备数：
        返还 负 1。
    完。
    设备 为 新建 设备信息。
    设备 的 标识 为 设备数。
    设备 的 类型 为 类型。
    设备 的 状态 为 设备_就绪。
    设备 的 驱动名 为 驱动名。
    设备 的 中断号 为 中断号。
    设备 的 基地址 为 基地址。
    设备 的 错误计数 为 0。
    设备 的 请求计数 为 0。
    设备 的 响应总时 为 0。
    设备 的 最近响应 为 0。
    设备 的 健康分数 为 1024。
    设备 的 功耗级别 为 1。
    做 追加 用 设备表、设备。
    设备数 为 设备数 加 1。
    返还 设备 的 标识。
完。

设备_移除 接受 标识：
    若 标识 大于或等于 设备数：
        返还 错误_无效参数。
    完。
    设备表 的 第 标识 项 的 状态 为 设备_离线。
    热插拔次数 为 热插拔次数 加 1。
    返还 0。
完。

设备_记录请求 接受 标识、响应时间：
    若 标识 大于或等于 设备数：
        返还。
    完。
    设备 为 设备表 的 第 标识 项。
    设备 的 请求计数 为 设备 的 请求计数 加 1。
    设备 的 响应总时 为 设备 的 响应总时 加 响应时间。
    设备 的 最近响应 为 响应时间。
完。

设备_记录错误 接受 标识：
    若 标识 大于或等于 设备数：
        返还。
    完。
    设备表 的 第 标识 项 的 错误计数 为 设备表 的 第 标识 项 的 错误计数 加 1。
    总错误数 为 总错误数 加 1。
    设备表 的 第 标识 项 的 健康分数 为 设备表 的 第 标识 项 的 健康分数 乘 95 除以 100。
    若 设备表 的 第 标识 项 的 健康分数 小于 100：
        设备表 的 第 标识 项 的 状态 为 设备_错误。
    完。
完。

设备_获取健康 接受 标识：
    若 标识 大于或等于 设备数：
        返还 0。
    完。
    返还 设备表 的 第 标识 项 的 健康分数。
完。

设备_平均响应 接受 标识：
    若 标识 大于或等于 设备数：
        返还 0。
    完。
    若 设备表 的 第 标识 项 的 请求计数 等于 0：
        返还 0。
    完。
    返还 设备表 的 第 标识 项 的 响应总时 除以 设备表 的 第 标识 项 的 请求计数。
完。

设备_设功耗 接受 标识、级别：
    若 标识 大于或等于 设备数：
        返还。
    完。
    设备表 的 第 标识 项 的 功耗级别 为 级别。
完。

设备_总功耗 接受 零参：
    可变 总 为 0。
    可变 序号 为 0。
    只要 序号 小于 设备数：
        若 设备表 的 第 序号 项 的 状态 不等于 设备_离线：
            总 为 总 加 设备表 的 第 序号 项 的 功耗级别。
        完。
        序号 为 序号 加 1。
    完。
    返还 总。
完。

设备_在线数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 设备数：
        若 设备表 的 第 序号 项 的 状态 不等于 设备_离线：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

设备_错误数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 设备数：
        若 设备表 的 第 序号 项 的 状态 等于 设备_错误：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

错误_无效参数 为 0 减 22。

AI异常窗口 为 16。

健康恢复速率 为 5。

健康最大值 为 1024。

响应超时阈值 为 10000。

异常错误率阈值 为 10。

监控统计 是 一种 事物，含 注册次数、移除次数、请求记录次数、错误记录次数、健康查询次数、响应查询次数、功耗设置次数、功耗查询次数、批量查询次数、健康恢复次数、AI异常检测次数、AI异常发现数、枚举次数、状态转换次数、重置次数、错误次数。

可变 全局统计 为 新建 监控统计。

可变 监控模块已初始化 为 假。

可变 AI错误历史 为 一组：（空）。

可变 AI错误位置 为 0。

监控模块_初始化 接受 零参：
    若 监控模块已初始化 等于 真：
        返还 0 减 17。
    完。
    设备表 为 一组：（空）。
    设备数 为 0。
    热插拔次数 为 0。
    总错误数 为 0。
    AI错误历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI异常窗口：
        做 追加 用 AI错误历史、0。
        i 为 i 加 1。
    完。
    AI错误位置 为 0。
    监控模块已初始化 为 真。
    做 打印 用 「[设备监控] 初始化完成」。
    返还 0。
完。

监控模块_销毁 接受 零参：
    若 监控模块已初始化 等于 假：
        返还 0。
    完。
    设备表 为 一组：（空）。
    设备数 为 0。
    AI错误历史 为 一组：（空）。
    监控模块已初始化 为 假。
    做 打印 用 「[设备监控] 已销毁」。
    返还 0。
完。

设备_安全注册 接受 类型、驱动名、中断号、基地址：
    全局统计 的 注册次数 为 全局统计 的 注册次数 加 1。
    返还 做 设备_注册 用 类型、驱动名、中断号、基地址。
完。

设备_安全移除 接受 标识：
    全局统计 的 移除次数 为 全局统计 的 移除次数 加 1。
    返还 做 设备_移除 用 标识。
完。

设备_枚举所有 接受 零参：
    全局统计 的 枚举次数 为 全局统计 的 枚举次数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 设备数：
        做 追加 用 结果、一组：设备表 的 第 i 项 的 标识、设备表 的 第 i 项 的 类型、设备表 的 第 i 项 的 状态、设备表 的 第 i 项 的 驱动名。
        i 为 i 加 1。
    完。
    返还 结果。
完。

设备_按类型枚举 接受 类型：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 设备数：
        若 设备表 的 第 i 项 的 类型 等于 类型 并且 设备表 的 第 i 项 的 状态 不等于 设备_离线：
            做 追加 用 结果、设备表 的 第 i 项 的 标识。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

设备_按状态枚举 接受 状态：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 设备数：
        若 设备表 的 第 i 项 的 状态 等于 状态：
            做 追加 用 结果、设备表 的 第 i 项 的 标识。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

设备_查找按驱动名 接受 驱动名：
    可变 i 为 0。
    只要 i 小于 设备数：
        若 设备表 的 第 i 项 的 驱动名 等于 驱动名：
            返还 设备表 的 第 i 项 的 标识。
        完。
        i 为 i 加 1。
    完。
    返还 0 减 1。
完。

设备_健康恢复 接受 标识：
    若 标识 大于或等于 设备数：
        返还。
    完。
    设备表 的 第 标识 项 的 健康分数 为 设备表 的 第 标识 项 的 健康分数 加 健康恢复速率。
    若 设备表 的 第 标识 项 的 健康分数 大于 健康最大值：
        设备表 的 第 标识 项 的 健康分数 为 健康最大值。
    完。
    若 设备表 的 第 标识 项 的 健康分数 大于 300 并且 设备表 的 第 标识 项 的 状态 等于 设备_错误：
        设备表 的 第 标识 项 的 状态 为 设备_就绪。
        全局统计 的 健康恢复次数 为 全局统计 的 健康恢复次数 加 1。
    完。
完。

设备_健康Tick 接受 零参：
    可变 i 为 0。
    只要 i 小于 设备数：
        若 设备表 的 第 i 项 的 状态 不等于 设备_离线：
            做 设备_健康恢复 用 i。
        完。
        i 为 i 加 1。
    完。
完。

设备_最不健康 接受 零参：
    可变 最差标识 为 0 减 1。
    可变 最差分数 为 健康最大值 加 1。
    可变 i 为 0。
    只要 i 小于 设备数：
        若 设备表 的 第 i 项 的 状态 不等于 设备_离线 并且 设备表 的 第 i 项 的 健康分数 小于 最差分数：
            最差分数 为 设备表 的 第 i 项 的 健康分数。
            最差标识 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最差标识。
完。

设备_平均健康 接受 零参：
    可变 总分 为 0。
    在线 为 做 设备_在线数。
    若 在线 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 设备数：
        若 设备表 的 第 i 项 的 状态 不等于 设备_离线：
            总分 为 总分 加 设备表 的 第 i 项 的 健康分数。
        完。
        i 为 i 加 1。
    完。
    返还 总分 除以 在线。
完。

设备_设置状态 接受 标识、新状态：
    若 标识 大于或等于 设备数：
        返还 错误_无效参数。
    完。
    设备表 的 第 标识 项 的 状态 为 新状态。
    全局统计 的 状态转换次数 为 全局统计 的 状态转换次数 加 1。
    返还 0。
完。

设备_获取状态 接受 标识：
    若 标识 大于或等于 设备数：
        返还 设备_未知。
    完。
    返还 设备表 的 第 标识 项 的 状态。
完。

设备_状态名 接受 状态：
    若 状态 等于 设备_就绪：
        返还 「就绪」。
    完。
    若 状态 等于 设备_忙碌：
        返还 「忙碌」。
    完。
    若 状态 等于 设备_错误：
        返还 「错误」。
    完。
    若 状态 等于 设备_离线：
        返还 「离线」。
    完。
    若 状态 等于 设备_挂起：
        返还 「挂起」。
    完。
    返还 「未知」。
完。

设备_重置 接受 标识：
    若 标识 大于或等于 设备数：
        返还 错误_无效参数。
    完。
    设备表 的 第 标识 项 的 错误计数 为 0。
    设备表 的 第 标识 项 的 请求计数 为 0。
    设备表 的 第 标识 项 的 响应总时 为 0。
    设备表 的 第 标识 项 的 最近响应 为 0。
    设备表 的 第 标识 项 的 健康分数 为 健康最大值。
    设备表 的 第 标识 项 的 状态 为 设备_就绪。
    全局统计 的 重置次数 为 全局统计 的 重置次数 加 1。
    返还 0。
完。

设备_批量查询健康 接受 零参：
    全局统计 的 批量查询次数 为 全局统计 的 批量查询次数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 设备数：
        做 追加 用 结果、一组：设备表 的 第 i 项 的 标识、设备表 的 第 i 项 的 健康分数、设备表 的 第 i 项 的 状态。
        i 为 i 加 1。
    完。
    返还 结果。
完。

设备_批量设功耗 接受 级别：
    可变 i 为 0。
    只要 i 小于 设备数：
        若 设备表 的 第 i 项 的 状态 不等于 设备_离线：
            设备表 的 第 i 项 的 功耗级别 为 级别。
        完。
        i 为 i 加 1。
    完。
    全局统计 的 功耗设置次数 为 全局统计 的 功耗设置次数 加 1。
完。

设备_批量重置错误 接受 零参：
    可变 重置数 为 0。
    可变 i 为 0。
    只要 i 小于 设备数：
        若 设备表 的 第 i 项 的 状态 等于 设备_错误：
            做 设备_重置 用 i。
            重置数 为 重置数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 重置数。
完。

AI_记录错误率 接受 错误率：
    做 设置 用 AI错误历史、AI错误位置、错误率。
    AI错误位置 为 (AI错误位置 加 1) 取余于 AI异常窗口。
    全局统计 的 AI异常检测次数 为 全局统计 的 AI异常检测次数 加 1。
完。

AI_检测异常设备 接受 零参：
    可变 异常列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 设备数：
        若 设备表 的 第 i 项 的 状态 等于 设备_离线：
            i 为 i 加 1。
            继续。
        完。
        若 设备表 的 第 i 项 的 请求计数 大于 10：
            错误率 为 设备表 的 第 i 项 的 错误计数 乘 100 除以 设备表 的 第 i 项 的 请求计数。
            若 错误率 大于 异常错误率阈值：
                做 追加 用 异常列表、一组：设备表 的 第 i 项 的 标识、错误率。
                全局统计 的 AI异常发现数 为 全局统计 的 AI异常发现数 加 1。
            完。
            做 AI_记录错误率 用 错误率。
        完。
        i 为 i 加 1。
    完。
    返还 异常列表。
完。

AI_预测下次故障 接受 零参：
    可变 最差标识 为 0 减 1。
    可变 最高错误率 为 0。
    可变 i 为 0。
    只要 i 小于 设备数：
        若 设备表 的 第 i 项 的 状态 等于 设备_离线 或者 设备表 的 第 i 项 的 请求计数 小于 5：
            i 为 i 加 1。
            继续。
        完。
        错误率 为 设备表 的 第 i 项 的 错误计数 乘 100 除以 设备表 的 第 i 项 的 请求计数。
        若 错误率 大于 最高错误率：
            最高错误率 为 错误率。
            最差标识 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最差标识。
完。

AI_建议预防措施 接受 零参：
    异常 为 做 AI_检测异常设备。
    若 做 计数 用 异常 等于 0：
        返还 「所有设备正常」。
    完。
    若 做 计数 用 异常 等于 1：
        返还 「1个设备异常，建议检查」。
    完。
    返还 做 字符串拼接 用 做 转字符串 用 做 计数 用 异常、「个设备异常，建议立即排查」。
完。

设备监控_转储 接受 零参：
    做 打印 用 「[设备监控] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  总设备数: 」、做 转字符串 用 设备数。
    做 打印 用 做 字符串拼接 用 「  在线数: 」、做 转字符串 用 做 设备_在线数。
    做 打印 用 做 字符串拼接 用 「  错误数: 」、做 转字符串 用 做 设备_错误数。
    做 打印 用 做 字符串拼接 用 「  平均健康: 」、做 转字符串 用 做 设备_平均健康。
    做 打印 用 做 字符串拼接 用 「  总功耗: 」、做 转字符串 用 做 设备_总功耗。
    做 打印 用 做 字符串拼接 用 「  热插拔: 」、做 转字符串 用 热插拔次数。
    可变 i 为 0。
    只要 i 小于 设备数：
        做 打印 用 做 字符串拼接 用 「  [」、做 转字符串 用 i。
        做 打印 用 做 字符串拼接 用 「    驱动: 」、做 转字符串 用 设备表 的 第 i 项 的 驱动名。
        做 打印 用 做 字符串拼接 用 「    状态: 」、做 设备_状态名 用 设备表 的 第 i 项 的 状态。
        做 打印 用 做 字符串拼接 用 「    健康: 」、做 转字符串 用 设备表 的 第 i 项 的 健康分数。
        i 为 i 加 1。
    完。
    返还 0。
完。

设备管理_完整统计 接受 零参：
    返还 一组：设备数、做 设备_在线数、做 设备_错误数、热插拔次数、总错误数、做 设备_总功耗。
完。

监控模块_打印统计 接受 零参：
    做 打印 用 「[设备监控] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  注册次数: 」、做 转字符串 用 全局统计 的 注册次数。
    做 打印 用 做 字符串拼接 用 「  移除次数: 」、做 转字符串 用 全局统计 的 移除次数。
    做 打印 用 做 字符串拼接 用 「  请求记录: 」、做 转字符串 用 全局统计 的 请求记录次数。
    做 打印 用 做 字符串拼接 用 「  错误记录: 」、做 转字符串 用 全局统计 的 错误记录次数。
    做 打印 用 做 字符串拼接 用 「  健康查询: 」、做 转字符串 用 全局统计 的 健康查询次数。
    做 打印 用 做 字符串拼接 用 「  响应查询: 」、做 转字符串 用 全局统计 的 响应查询次数。
    做 打印 用 做 字符串拼接 用 「  功耗设置: 」、做 转字符串 用 全局统计 的 功耗设置次数。
    做 打印 用 做 字符串拼接 用 「  批量查询: 」、做 转字符串 用 全局统计 的 批量查询次数。
    做 打印 用 做 字符串拼接 用 「  健康恢复: 」、做 转字符串 用 全局统计 的 健康恢复次数。
    做 打印 用 做 字符串拼接 用 「  AI异常检测: 」、做 转字符串 用 全局统计 的 AI异常检测次数。
    做 打印 用 做 字符串拼接 用 「  AI异常发现: 」、做 转字符串 用 全局统计 的 AI异常发现数。
    做 打印 用 做 字符串拼接 用 「  枚举次数: 」、做 转字符串 用 全局统计 的 枚举次数。
    做 打印 用 做 字符串拼接 用 「  状态转换: 」、做 转字符串 用 全局统计 的 状态转换次数。
    做 打印 用 做 字符串拼接 用 「  重置次数: 」、做 转字符串 用 全局统计 的 重置次数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    做 打印 用 做 字符串拼接 用 「  总设备: 」、做 转字符串 用 设备数。
    做 打印 用 做 字符串拼接 用 「  在线: 」、做 转字符串 用 做 设备_在线数。
    返还 0。
完。

监控模块_注册到内核 接受 内核上下文：
    若 监控模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[设备监控] 已注册到内核上下文」。
    返还 0。
完。

监控模块_查询状态 接受 零参：
    若 监控模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

监控模块_健康检查 接受 零参：
    若 监控模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 注册次数 加 全局统计 的 请求记录次数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
