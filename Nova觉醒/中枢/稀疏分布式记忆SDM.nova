《稀疏分布式记忆SDM》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·链表后继》。

SDM写入 接受 地址、数据：
    可变 激活数 为 0。
    可变 i 为 0。
    只要 i 小于 SDM容量：
        距离 为 地址 位异或 SDM地址 的 第 i 项。
        可变 位数 为 0。
        可变 tmp 为 距离。
        只要 tmp 大于 0：
            位数 为 位数 加 (tmp 位与 1)。
            tmp 为 tmp 右移 1。
        完。
        若 位数 小于或等于 SDM动态半径 除以 10：
            SDM数据 的 第 i 项 为 SDM数据 的 第 i 项 加 数据。
            SDM写入计数 的 第 i 项 为 SDM写入计数 的 第 i 项 加 1。
            激活数 为 激活数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 激活数 小于 SDM容量 除以 8：
        SDM动态半径 为 SDM动态半径 加 SDM自适应步长。
    否则若 激活数 大于 SDM容量 除以 2：
        SDM动态半径 为 SDM动态半径 减 SDM自适应步长。
    完。
    若 SDM动态半径 小于 5：
        SDM动态半径 为 5。
    完。
    返还 激活数。
完。

SDM读取 接受 地址：
    可变 累加 为 0。
    可变 激活数 为 0。
    可变 i 为 0。
    只要 i 小于 SDM容量：
        距离 为 地址 位异或 SDM地址 的 第 i 项。
        可变 位数 为 0。
        可变 tmp 为 距离。
        只要 tmp 大于 0：
            位数 为 位数 加 (tmp 位与 1)。
            tmp 为 tmp 右移 1。
        完。
        若 位数 小于或等于 SDM动态半径 除以 10：
            累加 为 累加 加 SDM数据 的 第 i 项。
            激活数 为 激活数 加 1。
        完。
        i 为 i 加 1。
    完。
    总读取次数 为 总读取次数 加 1。
    若 激活数 大于 0：
        返还 累加 除以 激活数。
    完。
    返还 0。
完。

情景存入 接受 模式哈希、惊讶度：
    记忆时钟 为 记忆时钟 加 1。
    做 追加 用 情景记忆、一组：模式哈希、记忆时钟、惊讶度、1000、0。
    只要 做 计数 用 情景记忆 大于 情景容量：
        可变 最弱 为 0。
        可变 最弱分 为 999999。
        可变 i 为 0。
        只要 i 小于 做 计数 用 情景记忆：
            年龄 为 记忆时钟 减 情景记忆 的 第 i 项 的 第 1 项。
            保留 为 情景记忆 的 第 i 项 的 第 3 项 乘 1000 除以 (1000 加 年龄 乘 遗忘速率)。
            保留 为 保留 加 情景记忆 的 第 i 项 的 第 4 项 乘 复述增强。
            若 保留 小于 最弱分：
                最弱分 为 保留。
                最弱 为 i。
            完。
            i 为 i 加 1。
        完。
        总遗忘次数 为 总遗忘次数 加 1。
        做 链表_删除 用 情景记忆、最弱。
    完。
完。

情景检索 接受 查询哈希：
    可变 最佳 为 负 1。
    可变 最佳分 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 情景记忆：
        差 为 查询哈希 位异或 情景记忆 的 第 i 项 的 第 0 项。
        可变 匹配 为 0。
        若 差 等于 0：
            匹配 为 1000。
        否则若 差 小于 256：
            匹配 为 700。
        否则若 差 小于 65536：
            匹配 为 300。
        完。
        若 匹配 大于 最佳分：
            最佳分 为 匹配。
            最佳 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最佳 大于或等于 0 并且 最佳分 大于 200：
        情景记忆 的 第 最佳 项 的 第 4 项 为 情景记忆 的 第 最佳 项 的 第 4 项 加 1。
        返还 情景记忆 的 第 最佳 项 的 第 0 项。
    完。
    返还 0。
完。

语义_添加概念 接受 标签哈希：
    若 做 计数 用 语义节点 大于或等于 语义容量：
        返还 负 1。
    完。
    做 追加 用 语义节点、一组：标签哈希、500。
    返还 做 计数 用 语义节点 减 1。
完。

语义_添加关联 接受 源、目标、权重：
    若 做 计数 用 语义边 大于或等于 语义边容量：
        返还。
    完。
    做 追加 用 语义边、一组：源、目标、权重。
完。

语义_激活扩散 接受 起始节点、轮数：
    若 起始节点 大于或等于 做 计数 用 语义节点：
        返还。
    完。
    语义节点 的 第 起始节点 项 的 第 1 项 为 1000。
    可变 r 为 0。
    只要 r 小于 轮数：
        可变 i 为 0。
        只要 i 小于 做 计数 用 语义边：
            源 为 语义边 的 第 i 项 的 第 0 项。
            目标 为 语义边 的 第 i 项 的 第 1 项。
            权重 为 语义边 的 第 i 项 的 第 2 项。
            若 源 小于 做 计数 用 语义节点 并且 目标 小于 做 计数 用 语义节点：
                扩散量 为 语义节点 的 第 源 项 的 第 1 项 乘 权重 除以 1000。
                语义节点 的 第 目标 项 的 第 1 项 为 语义节点 的 第 目标 项 的 第 1 项 加 扩散量。
                若 语义节点 的 第 目标 项 的 第 1 项 大于 2000：
                    语义节点 的 第 目标 项 的 第 1 项 为 2000。
                完。
            完。
            i 为 i 加 1。
        完。
        可变 j 为 0。
        只要 j 小于 做 计数 用 语义节点：
            语义节点 的 第 j 项 的 第 1 项 为 语义节点 的 第 j 项 的 第 1 项 乘 900 除以 1000。
            j 为 j 加 1。
        完。
        r 为 r 加 1。
    完。
完。

语义_最活跃 接受 前N：
    可变 结果 为 一组：（空）。
    可变 n 为 0。
    只要 n 小于 前N 并且 n 小于 做 计数 用 语义节点：
        可变 最高 为 负 1。
        可变 最高值 为 负 1。
        可变 i 为 0。
        只要 i 小于 做 计数 用 语义节点：
            可变 已有 为 假。
            可变 j 为 0。
            只要 j 小于 做 计数 用 结果：
                若 结果 的 第 j 项 等于 i：
                    已有 为 真。
                完。
                j 为 j 加 1。
            完。
            若 非 已有 并且 语义节点 的 第 i 项 的 第 1 项 大于 最高值：
                最高值 为 语义节点 的 第 i 项 的 第 1 项。
                最高 为 i。
            完。
            i 为 i 加 1。
        完。
        若 最高 大于或等于 0：
            做 追加 用 结果、最高。
        完。
        n 为 n 加 1。
    完。
    返还 结果。
完。

本章 收束。
