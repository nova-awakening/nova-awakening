《进程信号》

引用《.·进程任务》。

引用《.·进程调度》。

引用《.·信号动作设置》。

引用《..·记忆·bio块IO》。

引用《.·信号获取默》。

引用《.·同步原语》。

引用《.·进程标识分》。

引用《.·进程描述符》。

引用《.·墙钟设置》。

引用《..·执行·归档解包》。

引用《.·整数转字符串》。

引用《.·内存初始化》。

引用《..·稳态·图处内存管理》。

引用《.·字符串替换》。

引用《.·读取调用》。

引用《.·进程退出》。

引用《.·链表后继》。

引用《.·高精度定时器hrtimer》。

引用《.·AI信号异常检测》。

引用《.·每处理器》。

引用《..·感知·内核直连层》。

引用《.·双向链表操作》。

信号_挂断 为 1。

信号_中断 为 2。

信号_退出 为 3。

信号_终止 为 15。

信号_继续 为 18。

信号_停止 为 19。

信号_用户一 为 10。

信号_用户二 为 12。

信号档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、默认动作数、注册动作数、投递数、广播数、待决数、阻塞数、解除数、处理数、成功数、失败数、最近信号、最近目标、最近策略、默认动作表、自定义动作表、待决队列、阻塞表。

信号请求 是 一种 事物，含 动作、目标、参数、选项。

信号回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

信号摘要 是 一种 事物，含 名称、默认动作数、注册动作数、投递数、广播数、待决数、阻塞数、解除数、处理数、成功数、失败数、健康。

信号动作项 是 一种 事物，含 信号、名称、处理名、策略、来源。

信号投递项 是 一种 事物，含 信号、目标、来源、参数、策略、是否阻塞、已处理。

可变 信号账本 为 一组：（空）。

可变 信号构建数 为 0。

可变 信号总动作 为 0。

可变 信号总成功 为 0。

可变 信号总失败 为 0。

进程信号_建档 接受 名称：
    档 为 新建 信号档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 默认动作数 为 0。
    档 的 注册动作数 为 0。
    档 的 投递数 为 0。
    档 的 广播数 为 0。
    档 的 待决数 为 0。
    档 的 阻塞数 为 0。
    档 的 解除数 为 0。
    档 的 处理数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    档 的 最近信号 为 0。
    档 的 最近目标 为 「」。
    档 的 最近策略 为 「」。
    档 的 默认动作表 为 一组：（空）。
    档 的 自定义动作表 为 一组：（空）。
    档 的 待决队列 为 一组：（空）。
    档 的 阻塞表 为 一组：（空）。
    返还 档。
完。

进程信号_登记 接受 档：
    做 追加 用 信号账本、档。
    信号构建数 为 信号构建数 加 1。
    返还 档。
完。

进程信号_完整统计 接受 零参：
    返还 一组：信号构建数、信号总动作、信号总成功、信号总失败。
完。

进程信号_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 信号回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

进程信号_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    信号总动作 为 信号总动作 加 1。
完。

进程信号_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 待决数 大于 32 或者 档 的 阻塞数 大于 64：
        档 的 最近健康 为 「拥塞」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 0：
        档 的 最近健康 为 「监视」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「运行」。
    返还 档 的 最近健康。
完。

进程信号_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 档 的 名称 等于 「」：
        返还 假。
    完。
    返还 真。
完。

进程信号_成功 接受 档、来源、数据：
    档 的 最近结果 为 数据。
    档 的 成功数 为 档 的 成功数 加 1。
    信号总成功 为 信号总成功 加 1。
    档 的 最近统计 为 做 进程信号_完整统计。
    做 进程信号_刷新健康 用 档。
    返还 做 进程信号_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

进程信号_失败 接受 档、来源、错因：
    若 档 不等于 空：
        档 的 最近错误 为 错因。
        档 的 失败数 为 档 的 失败数 加 1。
        档 的 最近统计 为 做 进程信号_完整统计。
        做 进程信号_刷新健康 用 档。
    完。
    信号总失败 为 信号总失败 加 1。
    返还 做 进程信号_建回证 用 真、假、来源、错因、空、做 进程信号_完整统计。
完。

进程信号_动作项 接受 信号、名称、处理名、策略、来源：
    项 为 新建 信号动作项。
    项 的 信号 为 信号。
    项 的 名称 为 名称。
    项 的 处理名 为 处理名。
    项 的 策略 为 策略。
    项 的 来源 为 来源。
    返还 项。
完。

进程信号_投递项 接受 信号、目标、来源、参数、策略：
    项 为 新建 信号投递项。
    项 的 信号 为 信号。
    项 的 目标 为 目标。
    项 的 来源 为 来源。
    项 的 参数 为 参数。
    项 的 策略 为 策略。
    项 的 是否阻塞 为 假。
    项 的 已处理 为 假。
    返还 项。
完。

进程信号_信号名 接受 信号：
    若 信号 等于 信号_挂断：
        返还 「挂断」。
    完。
    若 信号 等于 信号_中断：
        返还 「中断」。
    完。
    若 信号 等于 信号_退出：
        返还 「退出」。
    完。
    若 信号 等于 信号_终止：
        返还 「终止」。
    完。
    若 信号 等于 信号_继续：
        返还 「继续」。
    完。
    若 信号 等于 信号_停止：
        返还 「停止」。
    完。
    若 信号 等于 信号_用户一：
        返还 「用户一」。
    完。
    若 信号 等于 信号_用户二：
        返还 「用户二」。
    完。
    返还 「未知」。
完。

进程信号_默认动作 接受 信号：
    若 信号 等于 信号_挂断：
        返还 做 进程信号_动作项 用 信号、「关闭会话」、「会话回收」、「关闭」、「默认」。
    完。
    若 信号 等于 信号_中断：
        返还 做 进程信号_动作项 用 信号、「打断执行」、「前台打断」、「打断」、「默认」。
    完。
    若 信号 等于 信号_退出：
        返还 做 进程信号_动作项 用 信号、「保留转储」、「异常退出」、「转储」、「默认」。
    完。
    若 信号 等于 信号_终止：
        返还 做 进程信号_动作项 用 信号、「终止任务」、「终止处理」、「终止」、「默认」。
    完。
    若 信号 等于 信号_继续：
        返还 做 进程信号_动作项 用 信号、「恢复运行」、「继续处理」、「继续」、「默认」。
    完。
    若 信号 等于 信号_停止：
        返还 做 进程信号_动作项 用 信号、「挂起任务」、「停止处理」、「停止」、「默认」。
    完。
    若 信号 等于 信号_用户一：
        返还 做 进程信号_动作项 用 信号、「用户钩子一」、「用户处理一」、「回调」、「默认」。
    完。
    若 信号 等于 信号_用户二：
        返还 做 进程信号_动作项 用 信号、「用户钩子二」、「用户处理二」、「回调」、「默认」。
    完。
    返还 做 进程信号_动作项 用 信号、「忽略」、「空处理」、「忽略」、「默认」。
完。

进程信号_写入默认表 接受 档、信号：
    动作 为 做 进程信号_默认动作 用 信号。
    做 追加 用 档 的 默认动作表、动作。
    档 的 默认动作数 为 档 的 默认动作数 加 1。
    返还 动作。
完。

进程信号_初始化 接受 档：
    若 非 做 进程信号_检查档 用 档：
        返还 做 进程信号_失败 用 档、「初始化」、「信号档不可用」。
    完。
    做 进程信号_记录 用 档、「初始化」、一组：（空）、「进程信号」。
    档 的 默认动作表 为 一组：（空）。
    档 的 自定义动作表 为 一组：（空）。
    档 的 待决队列 为 一组：（空）。
    档 的 阻塞表 为 一组：（空）。
    档 的 默认动作数 为 0。
    档 的 注册动作数 为 0。
    档 的 待决数 为 0。
    档 的 阻塞数 为 0。
    做 进程信号_写入默认表 用 档、信号_挂断。
    做 进程信号_写入默认表 用 档、信号_中断。
    做 进程信号_写入默认表 用 档、信号_退出。
    做 进程信号_写入默认表 用 档、信号_终止。
    做 进程信号_写入默认表 用 档、信号_继续。
    做 进程信号_写入默认表 用 档、信号_停止。
    做 进程信号_写入默认表 用 档、信号_用户一。
    做 进程信号_写入默认表 用 档、信号_用户二。
    档 的 是否初始化 为 真。
    返还 做 进程信号_成功 用 档、「初始化」、「已初始化」。
完。

进程信号_找动作 接受 表、信号：
    索引 为 0。
    只要 索引 小于 做 计数 用 表：
        项 为 表 的 第 索引 项。
        若 项 的 信号 等于 信号：
            返还 项。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

进程信号_选动作 接受 档、信号：
    动作 为 做 进程信号_找动作 用 档 的 自定义动作表、信号。
    若 动作 不等于 空：
        返还 动作。
    完。
    返还 做 进程信号_找动作 用 档 的 默认动作表、信号。
完。

进程信号_注册动作 接受 档、信号、名称、处理名、策略、来源：
    若 非 做 进程信号_检查档 用 档：
        返还 做 进程信号_失败 用 档、「注册动作」、「信号档不可用」。
    完。
    做 进程信号_记录 用 档、「注册动作」、一组：信号、名称、处理名、策略、来源、「进程信号」。
    动作 为 做 进程信号_动作项 用 信号、名称、处理名、策略、来源。
    做 追加 用 档 的 自定义动作表、动作。
    档 的 注册动作数 为 档 的 注册动作数 加 1。
    档 的 最近信号 为 信号。
    档 的 最近策略 为 策略。
    返还 做 进程信号_成功 用 档、「注册动作」、动作。
完。

进程信号_阻塞 接受 档、目标、信号：
    若 非 做 进程信号_检查档 用 档：
        返还 做 进程信号_失败 用 档、「阻塞」、「信号档不可用」。
    完。
    做 进程信号_记录 用 档、「阻塞」、一组：目标、信号、「进程信号」。
    条目 为 一组：目标、信号。
    做 追加 用 档 的 阻塞表、条目。
    档 的 阻塞数 为 档 的 阻塞数 加 1。
    档 的 最近目标 为 目标。
    档 的 最近信号 为 信号。
    返还 做 进程信号_成功 用 档、「阻塞」、条目。
完。

进程信号_是否阻塞 接受 档、目标、信号：
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 阻塞表：
        条目 为 档 的 阻塞表 的 第 索引 项。
        若 条目 的 第 0 项 等于 目标 并且 条目 的 第 1 项 等于 信号：
            返还 真。
        完。
        索引 为 索引 加 1。
    完。
    返还 假。
完。

进程信号_解除阻塞 接受 档、目标、信号：
    若 非 做 进程信号_检查档 用 档：
        返还 做 进程信号_失败 用 档、「解除阻塞」、「信号档不可用」。
    完。
    做 进程信号_记录 用 档、「解除阻塞」、一组：目标、信号、「进程信号」。
    新表 为 一组：（空）。
    索引 为 0。
    命中 为 假。
    只要 索引 小于 做 计数 用 档 的 阻塞表：
        条目 为 档 的 阻塞表 的 第 索引 项。
        若 条目 的 第 0 项 等于 目标 并且 条目 的 第 1 项 等于 信号：
            命中 为 真。
        否则：
            做 追加 用 新表、条目。
        完。
        索引 为 索引 加 1。
    完。
    档 的 阻塞表 为 新表。
    若 命中：
        档 的 阻塞数 为 做 计数 用 档 的 阻塞表。
        档 的 解除数 为 档 的 解除数 加 1。
        返还 做 进程信号_成功 用 档、「解除阻塞」、一组：目标、信号。
    完。
    返还 做 进程信号_失败 用 档、「解除阻塞」、「目标未阻塞该信号」。
完。

进程信号_投递 接受 档、目标、信号、参数、来源：
    若 非 做 进程信号_检查档 用 档：
        返还 做 进程信号_失败 用 档、「投递」、「信号档不可用」。
    完。
    做 进程信号_记录 用 档、「投递」、一组：目标、信号、参数、来源、「进程信号」。
    动作 为 做 进程信号_选动作 用 档、信号。
    策略 为 「忽略」。
    若 动作 不等于 空：
        策略 为 动作 的 策略。
    完。
    项 为 做 进程信号_投递项 用 信号、目标、来源、参数、策略。
    若 做 进程信号_是否阻塞 用 档、目标、信号：
        项 的 是否阻塞 为 真。
    完。
    做 追加 用 档 的 待决队列、项。
    档 的 投递数 为 档 的 投递数 加 1。
    档 的 待决数 为 做 计数 用 档 的 待决队列。
    档 的 最近目标 为 目标。
    档 的 最近信号 为 信号。
    档 的 最近策略 为 策略。
    返还 做 进程信号_成功 用 档、「投递」、项。
完。

进程信号_广播 接受 档、目标组、信号、参数、来源：
    若 非 做 进程信号_检查档 用 档：
        返还 做 进程信号_失败 用 档、「广播」、「信号档不可用」。
    完。
    做 进程信号_记录 用 档、「广播」、一组：目标组、信号、参数、来源、「进程信号」。
    索引 为 0。
    结果 为 一组：（空）。
    只要 索引 小于 做 计数 用 目标组：
        做 追加 用 结果、做 进程信号_投递 用 档、目标组 的 第 索引 项、信号、参数、来源 的 数据。
        索引 为 索引 加 1。
    完。
    档 的 广播数 为 档 的 广播数 加 1。
    返还 做 进程信号_成功 用 档、「广播」、结果。
完。

进程信号_取待决 接受 档、目标：
    若 非 做 进程信号_检查档 用 档：
        返还 做 进程信号_失败 用 档、「取待决」、「信号档不可用」。
    完。
    做 进程信号_记录 用 档、「取待决」、一组：目标、「进程信号」。
    结果 为 一组：（空）。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 待决队列：
        项 为 档 的 待决队列 的 第 索引 项。
        若 项 的 目标 等于 目标 并且 项 的 已处理 等于 假：
            做 追加 用 结果、项。
        完。
        索引 为 索引 加 1。
    完。
    返还 做 进程信号_成功 用 档、「取待决」、结果。
完。

进程信号_动作结果 接受 动作、项：
    若 动作 等于 空：
        返还 一组：「忽略」、项 的 目标、做 进程信号_信号名 用 项 的 信号。
    完。
    返还 一组：动作 的 名称、动作 的 处理名、动作 的 策略、项 的 目标、做 进程信号_信号名 用 项 的 信号。
完。

进程信号_处理单个 接受 档、目标：
    若 非 做 进程信号_检查档 用 档：
        返还 做 进程信号_失败 用 档、「处理单个」、「信号档不可用」。
    完。
    做 进程信号_记录 用 档、「处理单个」、一组：目标、「进程信号」。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 待决队列：
        项 为 档 的 待决队列 的 第 索引 项。
        若 项 的 目标 等于 目标 并且 项 的 已处理 等于 假：
            若 项 的 是否阻塞：
                返还 做 进程信号_失败 用 档、「处理单个」、「目标当前阻塞该信号」。
            完。
            动作 为 做 进程信号_选动作 用 档、项 的 信号。
            项 的 已处理 为 真。
            档 的 处理数 为 档 的 处理数 加 1。
            档 的 待决数 为 档 的 待决数 减 1。
            返还 做 进程信号_成功 用 档、「处理单个」、做 进程信号_动作结果 用 动作、项。
        完。
        索引 为 索引 加 1。
    完。
    返还 做 进程信号_失败 用 档、「处理单个」、「未找到待处理信号」。
完。

进程信号_处理批量 接受 档、目标：
    若 非 做 进程信号_检查档 用 档：
        返还 做 进程信号_失败 用 档、「处理批量」、「信号档不可用」。
    完。
    做 进程信号_记录 用 档、「处理批量」、一组：目标、「进程信号」。
    结果 为 一组：（空）。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 待决队列：
        项 为 档 的 待决队列 的 第 索引 项。
        若 项 的 目标 等于 目标 并且 项 的 已处理 等于 假 并且 项 的 是否阻塞 等于 假：
            动作 为 做 进程信号_选动作 用 档、项 的 信号。
            项 的 已处理 为 真。
            档 的 处理数 为 档 的 处理数 加 1。
            档 的 待决数 为 档 的 待决数 减 1。
            做 追加 用 结果、做 进程信号_动作结果 用 动作、项。
        完。
        索引 为 索引 加 1。
    完。
    返还 做 进程信号_成功 用 档、「处理批量」、结果。
完。

进程信号_清理已处理 接受 档：
    若 非 做 进程信号_检查档 用 档：
        返还 做 进程信号_失败 用 档、「清理已处理」、「信号档不可用」。
    完。
    做 进程信号_记录 用 档、「清理已处理」、一组：（空）、「进程信号」。
    新队列 为 一组：（空）。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 待决队列：
        项 为 档 的 待决队列 的 第 索引 项。
        若 项 的 已处理 等于 假：
            做 追加 用 新队列、项。
        完。
        索引 为 索引 加 1。
    完。
    档 的 待决队列 为 新队列。
    档 的 待决数 为 做 计数 用 档 的 待决队列。
    返还 做 进程信号_成功 用 档、「清理已处理」、档 的 待决数。
完。

进程信号_异常 接受 档：
    若 非 做 进程信号_检查档 用 档：
        返还 做 进程信号_失败 用 档、「异常」、「信号档不可用」。
    完。
    做 进程信号_记录 用 档、「异常」、一组：（空）、「进程信号」。
    问题 为 一组：（空）。
    若 非 档 的 是否初始化：
        做 追加 用 问题、「未初始化」。
    完。
    若 档 的 待决数 大于 64：
        做 追加 用 问题、「待决积压」。
    完。
    若 档 的 阻塞数 大于 64：
        做 追加 用 问题、「阻塞过多」。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        做 追加 用 问题、「失败倒挂」。
    完。
    返还 做 进程信号_成功 用 档、「异常」、问题。
完。

进程信号_摘要 接受 档：
    摘要 为 新建 信号摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 默认动作数 为 档 的 默认动作数。
    摘要 的 注册动作数 为 档 的 注册动作数。
    摘要 的 投递数 为 档 的 投递数。
    摘要 的 广播数 为 档 的 广播数。
    摘要 的 待决数 为 档 的 待决数。
    摘要 的 阻塞数 为 档 的 阻塞数。
    摘要 的 解除数 为 档 的 解除数。
    摘要 的 处理数 为 档 的 处理数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 进程信号_刷新健康 用 档。
    返还 摘要。
完。

进程信号_自检 接受 档：
    若 非 做 进程信号_检查档 用 档：
        返还 假。
    完。
    若 做 进程信号_初始化 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程信号_注册动作 用 档、信号_用户一、「刷新快照」、「刷新处理」、「回调」、「自检」 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程信号_阻塞 用 档、「前台任务」、信号_停止 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程信号_投递 用 档、「前台任务」、信号_终止、一组：「退出」、「自检」 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程信号_投递 用 档、「前台任务」、信号_停止、一组：「挂起」、「自检」 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程信号_处理单个 用 档、「前台任务」 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程信号_解除阻塞 用 档、「前台任务」、信号_停止 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程信号_处理批量 用 档、「前台任务」 的 已成 等于 假：
        返还 假。
    完。
    若 做 进程信号_清理已处理 用 档 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

进程信号_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 进程信号_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        返还 做 进程信号_初始化 用 档。
    完。
    若 请求 的 动作 等于 「注册动作」：
        返还 做 进程信号_注册动作 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项、请求 的 参数 的 第 4 项。
    完。
    若 请求 的 动作 等于 「阻塞」：
        返还 做 进程信号_阻塞 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「解除阻塞」：
        返还 做 进程信号_解除阻塞 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「投递」：
        返还 做 进程信号_投递 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「广播」：
        返还 做 进程信号_广播 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「取待决」：
        返还 做 进程信号_取待决 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「处理单个」：
        返还 做 进程信号_处理单个 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「处理批量」：
        返还 做 进程信号_处理批量 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「清理已处理」：
        返还 做 进程信号_清理已处理 用 档。
    完。
    若 请求 的 动作 等于 「异常」：
        返还 做 进程信号_异常 用 档。
    完。
    若 请求 的 动作 等于 「摘要」：
        返还 做 进程信号_成功 用 档、「摘要」、做 进程信号_摘要 用 档。
    完。
    返还 做 进程信号_失败 用 档、「处理」、「未知动作」。
完。

信号_最大长度 为 256。

信号_滤波阶数 为 8。

信号_窗口大小 为 64。

信号_能量_引擎 接受 数据、长度：
    可变 能量 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        能量 为 能量 加 数据 的 第 i 项 乘 数据 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 能量 除以 长度。
完。

信号_均值 接受 数据、长度：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        总和 为 总和 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总和 除以 长度。
完。

信号_方差 接受 数据、长度：
    均 为 做 信号_均值 用 数据、长度。
    可变 方差 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        差 为 数据 的 第 i 项 减 均。
        方差 为 方差 加 差 乘 差 除以 1000。
        i 为 i 加 1。
    完。
    返还 方差 除以 长度。
完。

信号_过零率 接受 数据、长度：
    若 长度 小于 2：
        返还 0。
    完。
    可变 过零 为 0。
    可变 i 为 1。
    只要 i 小于 长度：
        若 数据 的 第 i 项 大于 0 并且 数据 的 第 i 减 1 项 小于 0 或者 数据 的 第 i 项 小于 0 并且 数据 的 第 i 减 1 项 大于 0：
            过零 为 过零 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 过零 乘 1000 除以 (长度 减 1)。
完。

信号_峰值数 接受 数据、长度、阈值：
    若 长度 小于 3：
        返还 0。
    完。
    可变 峰值 为 0。
    可变 i 为 1。
    只要 i 小于 长度 减 1：
        若 数据 的 第 i 项 大于 数据 的 第 i 减 1 项 并且 数据 的 第 i 项 大于 数据 的 第 i 加 1 项 并且 数据 的 第 i 项 大于 阈值：
            峰值 为 峰值 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 峰值。
完。

信号_最大值 接受 数据、长度：
    可变 最大 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 长度：
        若 数据 的 第 i 项 大于 最大：
            最大 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

信号_最小值 接受 数据、长度：
    可变 最小 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 长度：
        若 数据 的 第 i 项 小于 最小：
            最小 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小。
完。

信号_低通滤波 接受 数据、长度、截止频率：
    窗口 为 1000 除以 (截止频率 加 1)。
    若 窗口 小于 2：
        窗口 为 2。
    完。
    若 窗口 大于 信号_滤波阶数：
        窗口 为 信号_滤波阶数。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度：
        可变 和 为 0。
        可变 计 为 0。
        可变 j 为 0。
        只要 j 小于 窗口 并且 i 减 j 大于或等于 0：
            和 为 和 加 数据 的 第 i 减 j 项。
            计 为 计 加 1。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和 除以 计。
        i 为 i 加 1。
    完。
    返还 结果。
完。

信号_高通滤波 接受 数据、长度、截止频率：
    低通 为 做 信号_低通滤波 用 数据、长度、截止频率。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度：
        做 追加 用 结果、数据 的 第 i 项 减 低通 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

信号_自相关 接受 数据、长度、最大延迟：
    可变 结果 为 一组：（空）。
    可变 lag 为 0。
    只要 lag 小于 最大延迟 并且 lag 小于 长度 除以 2：
        可变 总 为 0。
        可变 i 为 0。
        只要 i 小于 长度 减 lag：
            总 为 总 加 数据 的 第 i 项 乘 数据 的 第 i 加 lag 项 除以 1000。
            i 为 i 加 1。
        完。
        做 追加 用 结果、总 除以 (长度 减 lag)。
        lag 为 lag 加 1。
    完。
    返还 结果。
完。

信号_检测周期 接受 数据、长度：
    自相关 为 做 信号_自相关 用 数据、长度、长度 除以 2。
    可变 最佳lag 为 0。
    可变 最佳值 为 0。
    可变 i 为 2。
    只要 i 小于 做 计数 用 自相关：
        若 自相关 的 第 i 项 大于 最佳值：
            最佳值 为 自相关 的 第 i 项。
            最佳lag 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最佳lag。
完。

信号_斜率 接受 数据、长度：
    可变 结果 为 一组：（空）。
    做 追加 用 结果、0。
    可变 i 为 1。
    只要 i 小于 长度：
        做 追加 用 结果、数据 的 第 i 项 减 数据 的 第 i 减 1 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

信号_包络_引擎 接受 数据、长度：
    可变 绝对值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度：
        值 为 数据 的 第 i 项。
        若 值 小于 0：
            值 为 0 减 值。
        完。
        做 追加 用 绝对值、值。
        i 为 i 加 1。
    完。
    返还 做 信号_低通滤波 用 绝对值、长度、50。
完。

信号_提取特征 接受 数据、长度：
    返还 一本字典：能量 对应 做 信号_能量 用 数据、长度、均值 对应 做 信号_均值 用 数据、长度、方差 对应 做 信号_方差 用 数据、长度、过零率 对应 做 信号_过零率 用 数据、长度、峰值数 对应 做 信号_峰值数 用 数据、长度、100、最大值 对应 做 信号_最大值 用 数据、长度、最小值 对应 做 信号_最小值 用 数据、长度、周期 对应 做 信号_检测周期 用 数据、长度。
完。

信号_动态范围 接受 数据、长度：
    若 长度 等于 0：
        返还 0。
    完。
    返还 做 信号_最大值 用 数据、长度 减 做 信号_最小值 用 数据、长度。
完。

信号_信噪比估计 接受 数据、长度：
    能量 为 做 信号_能量_引擎 用 数据、长度。
    方差 为 做 信号_方差 用 数据、长度。
    若 方差 等于 0：
        返还 1000。
    完。
    返还 能量 乘 1000 除以 方差。
完。

信号引擎_完整统计 接受 零参：
    返还 一组：信号处理_总次数。
完。

SIGHUP 为 1。

SIGINT 为 2。

SIGQUIT 为 3。

SIGILL 为 4。

SIGTRAP 为 5。

SIGABRT 为 6。

SIGBUS 为 7。

SIGFPE 为 8。

SIGKILL 为 9。

SIGUSR1 为 10。

SIGSEGV 为 11。

SIGUSR2 为 12。

SIGPIPE 为 13。

SIGALRM 为 14。

SIGTERM 为 15。

SIGSTKFLT 为 16。

SIGCHLD 为 17。

SIGCONT 为 18。

SIGSTOP 为 19。

SIGTSTP 为 20。

SIGTTIN 为 21。

SIGTTOU 为 22。

SIGURG 为 23。

SIGXCPU 为 24。

SIGXFSZ 为 25。

SIGVTALRM 为 26。

SIGPROF 为 27。

SIGWINCH 为 28。

SIGIO 为 29。

SIGPWR 为 30。

SIGSYS 为 31。

SIGRTMIN 为 32。

SIGRTMAX 为 64。

信号_数量 为 64。

信号_标准数 为 31。

SIG_DFL 为 0。

SIG_IGN 为 1。

SIG_ERR 为 负 1。

SIG_行为_终止 为 0。

SIG_行为_忽略 为 1。

SIG_行为_核心转储 为 2。

SIG_行为_停止 为 3。

SIG_行为_继续 为 4。

SA_NOCLDSTOP 为 0x00000001。

SA_NOCLDWAIT 为 0x00000002。

SA_SIGINFO 为 0x00000004。

SA_RESTART 为 0x10000000。

SA_节点FER 为 0x40000000。

SA_RESETHAND 为 0x80000000。

信号_默认行为 接受 信号号：
    若 信号号 等于 SIGQUIT 或者 信号号 等于 SIGILL 或者 信号号 等于 SIGABRT：
        返还 SIG_行为_核心转储。
    完。
    若 信号号 等于 SIGBUS 或者 信号号 等于 SIGFPE 或者 信号号 等于 SIGSEGV：
        返还 SIG_行为_核心转储。
    完。
    若 信号号 等于 SIGSYS 或者 信号号 等于 SIGTRAP 或者 信号号 等于 SIGXCPU：
        返还 SIG_行为_核心转储。
    完。
    若 信号号 等于 SIGXFSZ：
        返还 SIG_行为_核心转储。
    完。
    若 信号号 等于 SIGCHLD 或者 信号号 等于 SIGURG 或者 信号号 等于 SIGWINCH：
        返还 SIG_行为_忽略。
    完。
    若 信号号 等于 SIGSTOP 或者 信号号 等于 SIGTSTP：
        返还 SIG_行为_停止。
    完。
    若 信号号 等于 SIGTTIN 或者 信号号 等于 SIGTTOU：
        返还 SIG_行为_停止。
    完。
    若 信号号 等于 SIGCONT：
        返还 SIG_行为_继续。
    完。
    返还 SIG_行为_终止。
完。

信号_是致命_定义 接受 信号号：
    行为 为 做 信号_默认行为 用 信号号。
    返还 行为 等于 SIG_行为_终止 或者 行为 等于 SIG_行为_核心转储。
完。

信号_是不可屏蔽 接受 信号号：
    返还 信号号 等于 SIGKILL 或者 信号号 等于 SIGSTOP。
完。

信号_是实时 接受 信号号：
    返还 信号号 大于或等于 SIGRTMIN 并且 信号号 小于或等于 SIGRTMAX。
完。

信号_产生core 接受 信号号：
    返还 做 信号_默认行为 用 信号号 等于 SIG_行为_核心转储。
完。

sigset_空 接受 零参：
    返还 0x0000000000000000。
完。

sigset_满 接受 零参：
    返还 0xFFFFFFFFFFFFFFFF。
完。

sigset_添加 接受 集、信号：
    返还 集 位或 1 左移 信号 减 1。
完。

sigset_移除 接受 集、信号：
    返还 集 位与 按位取反 (1 左移 信号 减 1)。
完。

sigset_包含 接受 集、信号：
    返还 集 位与 1 左移 信号 减 1 不等于 0。
完。

sigset_合并 接受 a、b：
    返还 a 位或 b。
完。

sigset_交集 接受 a、b：
    返还 a 位与 b。
完。

sigset_为空 接受 集：
    返还 集 等于 0。
完。

不可屏蔽掩码 为 1 左移 SIGKILL 减 1 位或 1 左移 SIGSTOP 减 1。

sigset_过滤不可屏蔽 接受 集：
    返还 集 位与 按位取反 不可屏蔽掩码。
完。

信号_名称 接受 信号号：
    若 信号号 等于 SIGHUP：
        返还 「SIGHUP」。
    完。
    若 信号号 等于 SIGINT：
        返还 「SIGINT」。
    完。
    若 信号号 等于 SIGQUIT：
        返还 「SIGQUIT」。
    完。
    若 信号号 等于 SIGILL：
        返还 「SIGILL」。
    完。
    若 信号号 等于 SIGABRT：
        返还 「SIGABRT」。
    完。
    若 信号号 等于 SIGBUS：
        返还 「SIGBUS」。
    完。
    若 信号号 等于 SIGFPE：
        返还 「SIGFPE」。
    完。
    若 信号号 等于 SIGKILL：
        返还 「SIGKILL」。
    完。
    若 信号号 等于 SIGSEGV：
        返还 「SIGSEGV」。
    完。
    若 信号号 等于 SIGPIPE：
        返还 「SIGPIPE」。
    完。
    若 信号号 等于 SIGALRM：
        返还 「SIGALRM」。
    完。
    若 信号号 等于 SIGTERM：
        返还 「SIGTERM」。
    完。
    若 信号号 等于 SIGCHLD：
        返还 「SIGCHLD」。
    完。
    若 信号号 等于 SIGCONT：
        返还 「SIGCONT」。
    完。
    若 信号号 等于 SIGSTOP：
        返还 「SIGSTOP」。
    完。
    若 信号号 等于 SIGTSTP：
        返还 「SIGTSTP」。
    完。
    若 做 信号_是实时 用 信号号：
        返还 「SIGRT」 加 做 整数转字符串 用 信号号 减 SIGRTMIN。
    完。
    返还 「SIG」 加 做 整数转字符串 用 信号号。
完。

sigset_计数 接受 集：
    可变 计 为 0。
    可变 m 为 集。
    只要 m 不等于 0：
        计 为 计 加 (m 位与 1)。
        m 为 m 右移 1。
    完。
    返还 计。
完。

sigset_第一个 接受 集：
    可变 i 为 1。
    只要 i 小于或等于 信号_数量：
        若 做 sigset_包含 用 集、i：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

sigset_差集 接受 a、b：
    返还 a 位与 按位取反 b。
完。

sigset_取反 接受 集：
    返还 按位取反 集。
完。

信号_是停止信号 接受 信号号：
    返还 信号号 等于 SIGSTOP 或者 信号号 等于 SIGTSTP 或者 信号号 等于 SIGTTIN 或者 信号号 等于 SIGTTOU。
完。

信号_是忽略信号 接受 信号号：
    返还 信号号 等于 SIGCHLD 或者 信号号 等于 SIGURG 或者 信号号 等于 SIGWINCH 或者 信号号 等于 SIGIO。
完。

信号_是同步信号 接受 信号号：
    返还 信号号 等于 SIGILL 或者 信号号 等于 SIGTRAP 或者 信号号 等于 SIGBUS 或者 信号号 等于 SIGFPE 或者 信号号 等于 SIGSEGV 或者 信号号 等于 SIGSYS。
完。

致命信号掩码 为 1 左移 SIGKILL 减 1 位或 1 左移 SIGTERM 减 1 位或 1 左移 SIGHUP 减 1 位或 1 左移 SIGINT 减 1 位或 1 左移 SIGPIPE 减 1 位或 1 左移 SIGALRM 减 1。

sigset_有致命信号 接受 集：
    返还 集 位与 致命信号掩码 不等于 0。
完。

信号_行为名 接受 行为：
    若 行为 等于 SIG_行为_终止：
        返还 「Terminate」。
    完。
    若 行为 等于 SIG_行为_忽略：
        返还 「Ignore」。
    完。
    若 行为 等于 SIG_行为_核心转储：
        返还 「Core」。
    完。
    若 行为 等于 SIG_行为_停止：
        返还 「Stop」。
    完。
    若 行为 等于 SIG_行为_继续：
        返还 「Continue」。
    完。
    返还 「Unknown」。
完。

信号_有效 接受 信号号：
    返还 信号号 大于或等于 1 并且 信号号 小于或等于 信号_数量。
完。

信号定义_统计 接受 零参：
    返还 一组：信号_标准数、信号_数量。
完。

信号定义_完整统计 接受 零参：
    返还 一组：信号_最大值。
完。

外部。

VFS_创建文件。

外部。

VFS_写入。

外部。

VFS_关闭。

信号_默认 为 0。

信号_忽略 为 1。

SA_重启 为 1 左移 0。

SA_无子停止 为 1 左移 1。

SA_无子等待 为 1 左移 2。

SA_信息 为 1 左移 3。

SA_栈 为 1 左移 4。

SA_一次性 为 1 左移 5。

SA_处理器 为 0。

SA_标志 为 1。

SA_屏蔽 为 2。

SA_恢复 为 3。

信号动作_创建 接受 处理器、标志、屏蔽集：
    返还 一组：处理器、标志、屏蔽集、0。
完。

默认动作表 为 一组：0、1、1、2、2、2、2、2、2、1、1、2、1、1、1、1、0、0、4、3、3、3、3、0、1、1、1、1、0、1、1、2。

系统_sigaction 接受 信号号、新动作、旧动作_输出：
    若 信号号 小于 1 或者 信号号 大于或等于 信号_数量：
        返还 错误_无效参数。
    完。
    若 信号号 等于 信号_KILL 或者 信号号 等于 信号_STOP：
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    处理器表 为 当前 的 第 T_信号处理器 项。
    若 旧动作_输出 不等于 空：
        做 设置 用 旧动作_输出、0、处理器表 的 第 信号号 项。
    完。
    若 新动作 不等于 空：
        处理器表 的 第 信号号 项 为 新动作。
    完。
    返还 成功。
完。

信号_处理挂起 接受 任务：
    挂起 为 任务 的 第 T_信号挂起结构 项。
    若 非 做 挂起信号_有信号 用 挂起：
        返还。
    完。
    屏蔽 为 任务 的 第 T_信号屏蔽 项。
    可投递 为 挂起 的 第 SP_掩码 项 位与 按位取反 屏蔽。
    若 可投递 等于 0：
        返还。
    完。
    可变 信号号 为 1。
    只要 信号号 小于 信号_数量：
        若 可投递 位与 1 左移 信号号 不等于 0：
            做 信号_执行动作 用 任务、信号号。
            返还。
        完。
        信号号 为 信号号 加 1。
    完。
完。

信号_执行动作 接受 任务、信号号：
    挂起 为 任务 的 第 T_信号挂起结构 项。
    做 挂起信号_清除 用 挂起、信号号。
    处理器表 为 任务 的 第 T_信号处理器 项。
    动作 为 处理器表 的 第 信号号 项。
    若 动作 等于 空 或者 动作 等于 信号_默认：
        做 信号_执行默认 用 任务、信号号。
        返还。
    完。
    若 动作 等于 信号_忽略：
        返还。
    完。
    做 信号_设置用户帧 用 任务、信号号、动作。
    若 动作 的 第 SA_标志 项 位与 SA_一次性：
        处理器表 的 第 信号号 项 为 信号_默认。
    完。
    信号_投递总数 为 信号_投递总数 加 1。
完。

信号_执行默认 接受 任务、信号号：
    若 信号号 小于 0 或者 信号号 大于或等于 做 计数 用 默认动作表：
        返还。
    完。
    动作类型 为 默认动作表 的 第 信号号 项。
    若 动作类型 等于 0：
        返还。
    否则若 动作类型 等于 1：
        做 do_exit 用 任务、信号号。
    否则若 动作类型 等于 2：
        core限制 为 RLIM_INFINITY。
        若 core限制 大于 0：
            做 内核信息 用 「信号: 进程」 加 做 整数转字符串 用 做 进程_PID 用 任务 加 「 core dump (信号」 加 做 整数转字符串 用 信号号 加 「)」。
            做 生成_core_dump 用 任务、信号号。
        完。
        做 do_exit 用 任务、信号号 位或 0x80。
    否则若 动作类型 等于 3：
        做 停止进程 用 任务。
    否则若 动作类型 等于 4：
        做 继续进程 用 任务。
    完。
完。

生成_core_dump 接受 任务、信号号：
    pid 为 做 进程_PID 用 任务。
    core路径 为 「/tmp/core.」 加 做 整数转字符串 用 pid。
    fd 为 做 VFS_创建文件 用 core路径、0x180。
    若 fd 小于 0：
        做 内核警告 用 「core dump: 无法创建 」 加 core路径。
        返还。
    完。
    可变 头部 为 一组：（空）。
    做 追加 用 头部、0x7F。
    做 追加 用 头部、0x45。
    做 追加 用 头部、0x4C。
    做 追加 用 头部、0x46。
    做 追加 用 头部、2。
    做 追加 用 头部、1。
    做 追加 用 头部、1。
    做 追加 用 头部、0。
    做 VFS_写入 用 fd、头部。
    寄存器 为 任务 的 第 T_寄存器 项。
    若 寄存器 不等于 空：
        可变 注释 为 一组：信号号、pid。
        可变 i 为 0。
        只要 i 小于 16 并且 i 小于 做 计数 用 寄存器：
            做 追加 用 注释、寄存器 的 第 i 项。
            i 为 i 加 1。
        完。
        做 VFS_写入 用 fd、注释。
    完。
    若 任务 的 mm 不等于 空：
        可变 vma 为 任务 的 mm 的 首VMA。
        只要 vma 不等于 空：
            若 vma 的 标志 位与 1 不等于 0：
                大小 为 vma 的 结束 减 vma 的 起始。
                若 大小 大于 0 并且 大小 小于 64 乘 1024 乘 1024：
                    可变 段头 为 一组：vma 的 起始、vma 的 结束、vma 的 标志。
                    做 VFS_写入 用 fd、段头。
                完。
            完。
            vma 为 vma 的 下一个。
        完。
    完。
    做 VFS_关闭 用 fd。
    做 内核信息 用 「core dump: 」 加 core路径 加 「 (PID=」 加 做 整数转字符串 用 pid 加 「 信号=」 加 做 整数转字符串 用 信号号 加 「)」。
完。

可变 信号动作_创建数 为 0。

可变 信号动作_core_dump数 为 0。

信号动作_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 信号动作_core_dump数 大于 10：
        做 追加 用 问题、「core dump过多: 」 加 做 整数转字符串 用 信号动作_core_dump数。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「信号动作健康」。
    完。
    返还 问题。
完。

信号动作_统计 接受 零参：
    返还 一本字典：创建数 对应 信号动作_创建数、core_dump 对应 信号动作_core_dump数。
完。

信号动作_重置 接受 零参：
    信号动作_创建数 为 0。
    信号动作_core_dump数 为 0。
完。

SA_NOCLDSTOP 为 1。

SA_NOCLDWAIT 为 2。

SA_SIGINFO 为 4。

SA_RESTART 为 0x10000000。

SA_NODEFER 为 0x40000000。

SA_RESETHAND 为 0x80000000。

信号动作_标志名 接受 标志：
    若 标志 位与 SA_SIGINFO：
        返还 「SA_SIGINFO」。
    完。
    若 标志 位与 SA_RESTART：
        返还 「SA_RESTART」。
    完。
    若 标志 位与 SA_NODEFER：
        返还 「SA_NODEFER」。
    完。
    返还 「0x」 加 做 十六进制 用 标志。
完。

SIG_DFL 为 0。

SIG_IGN 为 1。

SIG_ERR 为 负 1。

动作_终止 为 0。

动作_忽略 为 1。

动作_核心转储 为 2。

动作_停止 为 3。

动作_继续 为 4。

SIGKILL_MASK 为 1 左移 9。

SIGSTOP_MASK 为 1 左移 19。

不可屏蔽掩码 为 SIGKILL_MASK 位或 SIGSTOP_MASK。

可变 信号_发送总数 为 0。

可变 信号_处理总数 为 0。

可变 信号_忽略总数 为 0。

信号_发送 接受 任务、信号号：
    若 任务 等于 空：
        返还 错误_无此进程。
    完。
    若 信号号 小于 0 或者 信号号 大于或等于 信号_数量：
        返还 错误_无效参数。
    完。
    若 信号号 等于 0：
        返还 成功。
    完。
    若 做 进程_状态 用 任务 等于 进程_僵尸 或者 做 进程_状态 用 任务 等于 进程_死亡：
        返还 错误_无此进程。
    完。
    若 信号号 等于 18：
        停止掩码 为 1 左移 19 位或 1 左移 20 位或 1 左移 21 位或 1 左移 22。
        做 信号_清除挂起 用 任务、停止掩码。
    完。
    若 信号号 等于 19 或者 信号号 等于 20 或者 信号号 等于 21 或者 信号号 等于 22：
        做 信号_清除挂起 用 任务、1 左移 18。
    完。
    若 信号号 不等于 9 并且 信号号 不等于 19：
        处理器 为 做 进程_获取信号处理器 用 任务、信号号。
        若 处理器 等于 SIG_IGN：
            信号_忽略总数 为 信号_忽略总数 加 1。
            返还 成功。
        完。
    完。
    旧挂起 为 做 进程_信号挂起 用 任务。
    做 进程_设置信号挂起 用 任务、旧挂起 位或 1 左移 信号号。
    信号_发送总数 为 信号_发送总数 加 1。
    做 signal_wake_up 用 任务、信号号。
    返还 成功。
完。

信号_发送给组 接受 pgid、信号号：
    可变 已发送 为 0。
    可变 错误 为 错误_无此进程。
    可变 pid 为 1。
    只要 pid 小于 做 进程_最大PID：
        任务 为 做 进程表_查找 用 pid。
        若 任务 不等于 空 并且 做 进程_组ID 用 任务 等于 pgid：
            结果 为 做 信号_发送 用 任务、信号号。
            若 结果 等于 成功：
                已发送 为 已发送 加 1。
            完。
            错误 为 结果。
        完。
        pid 为 pid 加 1。
    完。
    若 已发送 大于 0：
        返还 成功。
    完。
    返还 错误。
完。

信号_发送给全部 接受 信号号：
    可变 已发送 为 0。
    当前pid 为 做 进程_PID 用 做 获取当前进程。
    可变 pid 为 2。
    只要 pid 小于 做 进程_最大PID：
        若 pid 不等于 当前pid：
            任务 为 做 进程表_查找 用 pid。
            若 任务 不等于 空：
                做 信号_发送 用 任务、信号号。
                已发送 为 已发送 加 1。
            完。
        完。
        pid 为 pid 加 1。
    完。
    若 已发送 大于 0：
        返还 成功。
    完。
    返还 错误_无此进程。
完。

信号_检查并处理 接受 任务：
    若 任务 等于 空：
        返还。
    完。
    挂起 为 做 进程_信号挂起 用 任务。
    屏蔽 为 做 进程_信号掩码 用 任务。
    有效 为 挂起 位与 按位取反 屏蔽 位或 挂起 位与 不可屏蔽掩码。
    若 有效 等于 0：
        返还。
    完。
    若 有效 位与 SIGKILL_MASK 不等于 0：
        做 信号_出队 用 任务、9。
        做 do_exit 用 任务、9 位或 0x80。
        返还。
    完。
    若 有效 位与 SIGSTOP_MASK 不等于 0：
        做 信号_出队 用 任务、19。
        做 进程_设置状态 用 任务、进程_停止。
        做 通知父进程_停止 用 任务。
        做 调度。
        返还。
    完。
    可变 sig 为 1。
    只要 sig 小于 32：
        若 有效 右移 sig 位与 1：
            做 信号_出队 用 任务、sig。
            做 信号_处理一个 用 任务、sig。
            返还。
        完。
        sig 为 sig 加 1。
    完。
完。

信号_处理一个 接受 任务、信号号：
    做 AI信号_记录 用 做 进程_PID 用 任务、信号号。
    处理器 为 做 进程_获取信号处理器 用 任务、信号号。
    若 处理器 等于 SIG_IGN：
        信号_忽略总数 为 信号_忽略总数 加 1。
        返还。
    完。
    若 处理器 等于 SIG_DFL：
        做 信号_执行默认动作 用 任务、信号号。
        返还。
    完。
    做 信号_设置帧 用 任务、信号号、处理器。
    信号_处理总数 为 信号_处理总数 加 1。
完。

信号_执行默认动作 接受 任务、信号号：
    动作 为 做 信号_获取默认动作 用 信号号。
    若 动作 等于 动作_忽略：
        返还。
    完。
    若 动作 等于 动作_终止：
        做 do_exit 用 任务、信号号 位或 0x80。
        返还。
    完。
    若 动作 等于 动作_核心转储：
        pid 为 做 进程_PID 用 任务。
        做 内核信息 用 「signal: 核心转储 PID=」 加 做 整数转字符串 用 pid 加 「 sig=」 加 做 整数转字符串 用 信号号。
        做 内核信息 用 「  RIP=」 加 做 整数转十六进制 用 任务 的 保存的RIP 加 「 RSP=」 加 做 整数转十六进制 用 任务 的 保存的RSP。
        做 do_exit 用 任务、信号号 位或 0x80。
        返还。
    完。
    若 动作 等于 动作_停止：
        做 进程_设置状态 用 任务、进程_停止。
        做 通知父进程_停止 用 任务。
        做 调度。
        返还。
    完。
    若 动作 等于 动作_继续：
        若 做 进程_状态 用 任务 等于 进程_停止：
            做 进程_设置状态 用 任务、进程_就绪。
        完。
        返还。
    完。
完。

SI_信号号 为 0。

SI_错误码 为 1。

SI_代码 为 2。

SI_发送者PID 为 3。

SI_发送者UID 为 4。

SI_值 为 5。

信号信息_创建 接受 信号号、代码、发送者PID：
    返还 一组：信号号、0、代码、发送者PID、0、0。
完。

SI_USER 为 0。

SI_KERNEL 为 128。

SI_TIMER 为 负 2。

SI_MESGQ 为 负 3。

SI_ASYNCIO 为 负 4。

SI_TKILL 为 负 6。

可变 信号_发送总数 为 0。

可变 信号_投递总数 为 0。

可变 信号_丢弃数 为 0。

可变 信号_排队数 为 0。

SP_掩码 为 0。

SP_队列 为 1。

挂起信号_创建 接受 零参：
    返还 一组：0、做 链表_创建。
完。

挂起信号_有信号 接受 挂起：
    返还 挂起 的 第 SP_掩码 项 不等于 0。
完。

挂起信号_测试 接受 挂起、信号号：
    返还 挂起 的 第 SP_掩码 项 位与 1 左移 信号号 不等于 0。
完。

挂起信号_设置 接受 挂起、信号号：
    做 设置 用 挂起、SP_掩码、挂起 的 第 SP_掩码 项 位或 1 左移 信号号。
完。

挂起信号_清除 接受 挂起、信号号：
    做 设置 用 挂起、SP_掩码、挂起 的 第 SP_掩码 项 位与 按位取反 (1 左移 信号号)。
完。

当前进程_有待处理信号 接受 零参：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 假。
    完。
    返还 做 挂起信号_有信号 用 当前 的 第 T_信号挂起 项。
完。

信号_发送到进程 接受 目标、信号号、信息：
    若 目标 等于 空 或者 信号号 小于 1 或者 信号号 大于或等于 信号_数量：
        返还 错误_无效参数。
    完。
    若 做 信号_被忽略 用 目标、信号号：
        信号_丢弃数 为 信号_丢弃数 加 1。
        返还 成功。
    完。
    挂起 为 目标 的 第 T_信号挂起结构 项。
    若 信号号 小于或等于 31：
        若 做 挂起信号_测试 用 挂起、信号号：
            信号_丢弃数 为 信号_丢弃数 加 1。
            返还 成功。
        完。
    完。
    若 信号号 大于或等于 32：
        若 信号_排队数 大于 1024：
            信号_丢弃数 为 信号_丢弃数 加 1。
            返还 错误_队列满。
        完。
        做 链表_尾插 用 挂起 的 第 SP_队列 项、信息。
        信号_排队数 为 信号_排队数 加 1。
    完。
    做 挂起信号_设置 用 挂起、信号号。
    信号_发送总数 为 信号_发送总数 加 1。
    做 信号_唤醒目标 用 目标、信号号。
    返还 成功。
完。

系统_kill 接受 目标PID、信号号：
    若 信号号 小于 0 或者 信号号 大于或等于 信号_数量：
        返还 错误_无效参数。
    完。
    若 信号号 等于 0：
        进程 为 做 进程表_查找 用 目标PID。
        返还 做 如果选数值 用 进程 不等于 空、成功、错误_不存在。
    完。
    若 目标PID 大于 0：
        进程 为 做 进程表_查找 用 目标PID。
        若 进程 等于 空：
            返还 错误_不存在。
        完。
        信息 为 做 信号信息_创建 用 信号号、SI_USER、做 获取当前进程 的 第 T_进程号 项。
        返还 做 信号_发送到进程 用 进程、信号号、信息。
    否则若 目标PID 等于 0：
        返还 做 信号_发送到进程组 用 做 获取当前进程 的 第 T_进程组 项、信号号。
    否则若 目标PID 等于 负 1：
        返还 做 信号_发送到全部 用 信号号。
    否则：
        返还 做 信号_发送到进程组 用 0 减 目标PID、信号号。
    完。
完。

信号_发送到进程组 接受 进程组ID、信号号：
    进程列表 为 做 获取所有进程。
    信息 为 做 信号信息_创建 用 信号号、SI_USER、做 获取当前进程 的 第 T_进程号 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 进程列表：
        若 进程列表 的 第 i 项 的 第 T_进程组 项 等于 进程组ID：
            做 信号_发送到进程 用 进程列表 的 第 i 项、信号号、信息。
        完。
        i 为 i 加 1。
    完。
    返还 成功。
完。

信号_发送到全部 接受 信号号：
    进程列表 为 做 获取所有进程。
    信息 为 做 信号信息_创建 用 信号号、SI_USER、做 获取当前进程 的 第 T_进程号 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 进程列表：
        若 进程列表 的 第 i 项 的 第 T_进程号 项 大于 1：
            做 信号_发送到进程 用 进程列表 的 第 i 项、信号号、信息。
        完。
        i 为 i 加 1。
    完。
    返还 成功。
完。

信号_唤醒目标 接受 目标、信号号：
    若 信号号 等于 信号_KILL 或者 信号号 等于 信号_STOP：
        若 目标 的 第 T_状态 项 等于 进程_深睡 或者 目标 的 第 T_状态 项 等于 进程_睡眠：
            做 唤醒进程 用 目标。
        完。
    否则：
        若 目标 的 第 T_状态 项 等于 进程_睡眠：
            做 唤醒进程 用 目标。
        完。
    完。
    若 信号号 等于 信号_CONT：
        若 目标 的 第 T_状态 项 等于 进程_停止：
            做 继续进程 用 目标。
        完。
    完。
完。

信号_被忽略 接受 目标、信号号：
    若 信号号 等于 信号_KILL 或者 信号号 等于 信号_STOP：
        返还 假。
    完。
    处理器 为 目标 的 第 T_信号处理器 项。
    若 处理器 等于 空：
        返还 假。
    完。
    返还 处理器 的 第 信号号 项 等于 信号_忽略。
完。

如果选数值 接受 条件、真值、假值：
    若 条件：
        返还 真值。
    完。
    返还 假值。
完。

SIG_BLOCK 为 0。

SIG_UNBLOCK 为 1。

SIG_SETMASK 为 2。

系统_sigprocmask 接受 操作、新掩码：
    当前 为 做 获取当前进程。
    旧掩码 为 当前 的 第 T_信号屏蔽 项。
    若 操作 等于 SIG_BLOCK：
        做 设置 用 当前、T_信号屏蔽、旧掩码 位或 新掩码。
    否则若 操作 等于 SIG_UNBLOCK：
        做 设置 用 当前、T_信号屏蔽、旧掩码 位与 按位取反 新掩码。
    否则若 操作 等于 SIG_SETMASK：
        做 设置 用 当前、T_信号屏蔽、新掩码。
    否则：
        返还 错误_无效参数。
    完。
    不可屏蔽 为 1 左移 信号_KILL 位或 1 左移 信号_STOP。
    做 设置 用 当前、T_信号屏蔽、当前 的 第 T_信号屏蔽 项 位与 按位取反 不可屏蔽。
    返还 旧掩码。
完。

信号投递_统计 接受 零参：
    返还 一组：信号_发送总数、信号_投递总数、信号_丢弃数、信号_排队数。
完。

信号投递_完整统计 接受 零参：
    返还 一组：信号_发送总数、信号_投递总数、信号_丢弃数、信号_排队数。
完。

信号投递_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 信号_丢弃数 大于 信号_发送总数 除以 5 并且 信号_发送总数 大于 100：
        做 追加 用 问题、「信号丢弃率过高」。
    完。
    若 信号_排队数 大于 1024：
        做 追加 用 问题、「信号排队过多: 」 加 做 整数转字符串 用 信号_排队数。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「信号投递健康」。
    完。
    返还 问题。
完。

信号投递_重置 接受 零参：
    信号_发送总数 为 0。
    信号_投递总数 为 0。
    信号_丢弃数 为 0。
    信号_排队数 为 0。
完。

信号_丢弃率 接受 零参：
    若 信号_发送总数 等于 0：
        返还 0。
    完。
    返还 信号_丢弃数 乘 100 除以 信号_发送总数。
完。

信号_投递率 接受 零参：
    若 信号_发送总数 等于 0：
        返还 0。
    完。
    返还 信号_投递总数 乘 100 除以 信号_发送总数。
完。

定点基 为 1024。

可变 运算总数 为 0。

信号_上采样 接受 数据、长度、因子、输出：
    运算总数 为 运算总数 加 1。
    可变 写位 为 0。
    可变 读位 为 0。
    只要 读位 小于 长度：
        当前 为 做 内存_读32 用 数据 加 读位 乘 4。
        做 内存_写32 用 输出 加 写位 乘 4、当前。
        写位 为 写位 加 1。
        可变 插值 为 1。
        只要 插值 小于 因子：
            可变 下一个 为 当前。
            若 读位 加 1 小于 长度：
                下一个 为 做 内存_读32 用 数据 加 (读位 加 1) 乘 4。
            完。
            插值点 为 当前 加 (下一个 减 当前) 乘 插值 除以 因子。
            做 内存_写32 用 输出 加 写位 乘 4、插值点。
            写位 为 写位 加 1。
            插值 为 插值 加 1。
        完。
        读位 为 读位 加 1。
    完。
    返还 写位。
完。

信号_下采样 接受 数据、长度、因子、输出：
    运算总数 为 运算总数 加 1。
    可变 写位 为 0。
    可变 读位 为 0。
    只要 读位 小于 长度：
        可变 累加 为 0。
        可变 计 为 0。
        只要 计 小于 因子 并且 读位 加 计 小于 长度：
            累加 为 累加 加 做 内存_读32 用 数据 加 (读位 加 计) 乘 4。
            计 为 计 加 1。
        完。
        做 内存_写32 用 输出 加 写位 乘 4、累加 除以 计。
        写位 为 写位 加 1。
        读位 为 读位 加 因子。
    完。
    返还 写位。
完。

卷积 接受 信号、信号长、核、核长、输出：
    运算总数 为 运算总数 加 1。
    输出长 为 信号长 加 核长 减 1。
    可变 位 为 0。
    只要 位 小于 输出长：
        可变 和 为 0。
        可变 核位 为 0。
        只要 核位 小于 核长：
            信号位 为 位 减 核位。
            若 信号位 大于或等于 0 并且 信号位 小于 信号长：
                和 为 和 加 做 内存_读32 用 信号 加 信号位 乘 4 乘 做 内存_读32 用 核 加 核位 乘 4 除以 定点基。
            完。
            核位 为 核位 加 1。
        完。
        做 内存_写32 用 输出 加 位 乘 4、和。
        位 为 位 加 1。
    完。
    返还 输出长。
完。

信号_差分 接受 数据、长度、输出：
    可变 位 为 1。
    只要 位 小于 长度：
        做 内存_写32 用 输出 加 (位 减 1) 乘 4、做 内存_读32 用 数据 加 位 乘 4 减 做 内存_读32 用 数据 加 (位 减 1) 乘 4。
        位 为 位 加 1。
    完。
    返还 长度 减 1。
完。

信号_累积 接受 数据、长度、输出：
    可变 和 为 0。
    可变 位 为 0。
    只要 位 小于 长度：
        和 为 和 加 做 内存_读32 用 数据 加 位 乘 4。
        做 内存_写32 用 输出 加 位 乘 4、和。
        位 为 位 加 1。
    完。
完。

信号_能量 接受 数据、长度：
    可变 能 为 0。
    可变 位 为 0。
    只要 位 小于 长度：
        值 为 做 内存_读32 用 数据 加 位 乘 4。
        能 为 能 加 值 乘 值 除以 定点基。
        位 为 位 加 1。
    完。
    返还 能。
完。

信号_互相关 接受 甲、乙、长度、延迟、输出：
    运算总数 为 运算总数 加 1。
    可变 延位 为 0。
    只要 延位 小于 延迟：
        可变 和 为 0。
        可变 位 为 0。
        只要 位 小于 长度：
            乙位 为 位 加 延位。
            若 乙位 小于 长度：
                和 为 和 加 做 内存_读32 用 甲 加 位 乘 4 乘 做 内存_读32 用 乙 加 乙位 乘 4 除以 定点基。
            完。
            位 为 位 加 1。
        完。
        做 内存_写32 用 输出 加 延位 乘 4、和。
        延位 为 延位 加 1。
    完。
完。

信号_移动平均 接受 数据、长度、窗口、输出：
    运算总数 为 运算总数 加 1。
    可变 和 为 0。
    可变 位 为 0。
    只要 位 小于 窗口 并且 位 小于 长度：
        和 为 和 加 做 内存_读32 用 数据 加 位 乘 4。
        做 内存_写32 用 输出 加 位 乘 4、和 除以 (位 加 1)。
        位 为 位 加 1。
    完。
    只要 位 小于 长度：
        和 为 和 加 做 内存_读32 用 数据 加 位 乘 4 减 做 内存_读32 用 数据 加 (位 减 窗口) 乘 4。
        做 内存_写32 用 输出 加 位 乘 4、和 除以 窗口。
        位 为 位 加 1。
    完。
完。

信号_峰值检测 接受 数据、长度、阈值：
    可变 峰值数 为 0。
    可变 位 为 1。
    只要 位 小于 长度 减 1：
        当前 为 做 内存_读32 用 数据 加 位 乘 4。
        前 为 做 内存_读32 用 数据 加 (位 减 1) 乘 4。
        后 为 做 内存_读32 用 数据 加 (位 加 1) 乘 4。
        若 当前 大于 前 并且 当前 大于 后 并且 当前 大于 阈值：
            峰值数 为 峰值数 加 1。
        完。
        位 为 位 加 1。
    完。
    返还 峰值数。
完。

信号_归一化 接受 数据、长度：
    可变 最大 为 0。
    可变 位 为 0。
    只要 位 小于 长度：
        绝对值 为 做 内存_读32 用 数据 加 位 乘 4。
        若 绝对值 小于 0：
            绝对值 为 0 减 绝对值。
        完。
        若 绝对值 大于 最大：
            最大 为 绝对值。
        完。
        位 为 位 加 1。
    完。
    若 最大 等于 0：
        返还。
    完。
    位 为 0。
    只要 位 小于 长度：
        值 为 做 内存_读32 用 数据 加 位 乘 4。
        做 内存_写32 用 数据 加 位 乘 4、值 乘 定点基 除以 最大。
        位 为 位 加 1。
    完。
完。

张量_完整统计 接受 零参：
    返还 一组：运算总数。
完。

信号_终止 为 1。

信号_中断 为 2。

信号_退出 为 3。

信号_非法 为 4。

信号_陷阱 为 5。

信号_中止 为 6。

信号_段错 为 11。

信号_管道 为 13。

信号_闹钟 为 14。

信号_停止 为 19。

信号_继续 为 18。

信号_子终 为 17。

信号_紧急 为 23。

信号_用户一 为 10。

信号_用户二 为 12。

最大信号 为 64。

实时信号起始 为 32。

信号队列深度 为 32。

信号项 是 一种 事物，含 信号号、发送者、时间戳、附加值。

信号状态 是 一种 事物，含 处理表、屏蔽集、待处理集、队列头、队列尾、队列数量。

可变 发送总数 为 0。

可变 处理总数 为 0。

可变 忽略总数 为 0。

可变 丢弃总数 为 0。

可变 致命总数 为 0。

信号_创建状态 接受 零参：
    状态 为 做 堆_分配 用 信号状态。
    状态 的 处理表 为 做 堆_分配 用 最大信号 乘 8。
    可变 序号 为 0。
    只要 序号 小于 最大信号：
        做 内存_写64 用 状态 的 处理表 加 序号 乘 8、0。
        序号 为 序号 加 1。
    完。
    状态 的 屏蔽集 为 0。
    状态 的 待处理集 为 0。
    状态 的 队列头 为 0。
    状态 的 队列尾 为 0。
    状态 的 队列数量 为 0。
    返还 状态。
完。

信号_注册处理 接受 状态、信号号、处理函数：
    若 状态 等于 0 或者 信号号 大于或等于 最大信号：
        返还 错误_无效参数。
    完。
    若 信号号 等于 信号_停止 或者 信号号 等于 信号_终止：
        返还 错误_不允许。
    完。
    做 内存_写64 用 状态 的 处理表 加 信号号 乘 8、处理函数。
    返还 0。
完。

信号_屏蔽 接受 状态、信号号：
    若 状态 等于 0 或者 信号号 大于或等于 最大信号：
        返还。
    完。
    状态 的 屏蔽集 为 状态 的 屏蔽集 位或 1 左移 信号号。
完。

信号_解除屏蔽 接受 状态、信号号：
    若 状态 等于 0 或者 信号号 大于或等于 最大信号：
        返还。
    完。
    状态 的 屏蔽集 为 状态 的 屏蔽集 位与 按位取反 (1 左移 信号号)。
完。

信号_派发 接受 状态：
    若 状态 等于 0：
        返还。
    完。
    可变 信号号 为 0。
    只要 信号号 小于 最大信号：
        若 状态 的 待处理集 位与 1 左移 信号号 不等于 0：
            状态 的 待处理集 为 状态 的 待处理集 位与 按位取反 (1 左移 信号号)。
            处理函数 为 做 内存_读64 用 状态 的 处理表 加 信号号 乘 8。
            若 处理函数 不等于 0：
                做 处理函数 用 信号号。
                处理总数 为 处理总数 加 1。
            否则：
                做 信号_默认动作 用 信号号。
            完。
        完。
        信号号 为 信号号 加 1。
    完。
完。

信号_默认动作 接受 信号号：
    若 信号号 等于 信号_终止 或者 信号号 等于 信号_退出 或者 信号号 等于 信号_段错 或者 信号号 等于 信号_中止 或者 信号号 等于 信号_非法：
        致命总数 为 致命总数 加 1。
    完。
    若 信号号 等于 信号_停止：
        处理总数 为 处理总数 加 1。
    完。
    若 信号号 等于 信号_继续：
        处理总数 为 处理总数 加 1。
    完。
    若 信号号 等于 信号_子终：
        忽略总数 为 忽略总数 加 1。
    完。
完。

信号_弹出实时 接受 状态：
    若 状态 等于 0 或者 状态 的 队列头 等于 0：
        返还 0。
    完。
    项 为 状态 的 队列头。
    状态 的 队列头 为 做 内存_读64 用 项 加 24。
    若 状态 的 队列头 等于 0：
        状态 的 队列尾 为 0。
    完。
    状态 的 队列数量 为 状态 的 队列数量 减 1。
    返还 项。
完。

信号_有待处理 接受 状态：
    若 状态 等于 0：
        返还 假。
    完。
    返还 状态 的 待处理集 位与 按位取反 状态 的 屏蔽集 不等于 0。
完。

信号处理_完整统计 接受 零参：
    返还 一组：发送总数、处理总数、忽略总数、丢弃总数、致命总数。
完。

信号量_尝试获取 接受 sem：
    当前 为 做 原子_读取 用 sem 的 计数。
    若 当前 大于 0：
        返还 做 原子_比较交换 用 sem 的 计数、当前、当前 减 1。
    完。
    返还 假。
完。

信号量_释放 接受 sem：
    做 原子_加 用 sem 的 计数、1。
    若 非 做 等待队列_为空 用 sem 的 等待链表：
        做 自旋锁_获取 用 sem 的 锁。
        做 等待队列_唤醒一个 用 sem 的 等待链表。
        做 自旋锁_释放 用 sem 的 锁。
    完。
完。

信号量_可用数 接受 sem：
    返还 做 原子_读取 用 sem 的 计数。
完。

完成量 是 一种 事物，含 完成标志、等待链表。

完成量_创建_信号量版 接受 零参：
    返还 新建 完成量：完成标志 为 做 原子_创建 用 0、等待链表 为 做 等待队列_创建。
完。

完成量_初始化 接受 comp：
    做 原子_设置 用 comp 的 完成标志、0。
完。

完成量_等待 接受 comp：
    若 做 原子_读取 用 comp 的 完成标志 大于 0：
        做 原子_减 用 comp 的 完成标志、1。
        返还。
    完。
    可变 等待项 为 做 等待项_创建 用 做 获取当前进程、真。
    做 等待队列_加入 用 comp 的 等待链表、等待项。
    只要 真：
        做 进程_设置状态 用 做 获取当前进程、进程状态_不可中断睡眠。
        若 做 原子_读取 用 comp 的 完成标志 大于 0：
            做 原子_减 用 comp 的 完成标志、1。
            跳出。
        完。
        做 调度。
    完。
    做 进程_设置状态 用 做 获取当前进程、进程状态_运行。
    做 等待队列_移除 用 comp 的 等待链表、等待项。
完。

完成量_等待_超时 接受 comp、超时_毫秒：
    若 做 原子_读取 用 comp 的 完成标志 大于 0：
        做 原子_减 用 comp 的 完成标志、1。
        返还 0。
    完。
    截止时间 为 做 当前时间_秒 加 超时_毫秒。
    可变 等待项 为 做 等待项_创建 用 做 获取当前进程、真。
    做 等待队列_加入 用 comp 的 等待链表、等待项。
    只要 真：
        若 做 原子_读取 用 comp 的 完成标志 大于 0：
            做 原子_减 用 comp 的 完成标志、1。
            做 等待队列_移除 用 comp 的 等待链表、等待项。
            返还 0。
        完。
        若 做 当前时间_秒 大于或等于 截止时间：
            做 等待队列_移除 用 comp 的 等待链表、等待项。
            返还 错误码_超时。
        完。
        做 进程_设置状态 用 做 获取当前进程、进程状态_不可中断睡眠。
        做 schedule_timeout 用 截止时间 减 做 当前时间_秒。
    完。
完。

完成量_等待_可中断 接受 comp：
    若 做 原子_读取 用 comp 的 完成标志 大于 0：
        做 原子_减 用 comp 的 完成标志、1。
        返还 0。
    完。
    可变 等待项 为 做 等待项_创建 用 做 获取当前进程、真。
    做 等待队列_加入 用 comp 的 等待链表、等待项。
    只要 真：
        若 做 当前进程_有待处理信号：
            做 等待队列_移除 用 comp 的 等待链表、等待项。
            返还 错误码_被中断。
        完。
        若 做 原子_读取 用 comp 的 完成标志 大于 0：
            做 原子_减 用 comp 的 完成标志、1。
            做 等待队列_移除 用 comp 的 等待链表、等待项。
            返还 0。
        完。
        做 进程_设置状态 用 做 获取当前进程、进程状态_可中断睡眠。
        做 调度。
    完。
完。

本章 收束。
