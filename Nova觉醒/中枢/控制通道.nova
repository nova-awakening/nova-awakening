《控制通道》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·硬链接》。

引用《..·稳态·串口》。

引用《..·标准·原生字符串》。

引用《.·整数转字符串》。

AI控制_COM口 为 COM2。

AI控制_最大行长度 为 8192。

可变 AI控制_已初始化 为 假。

可变 AI控制_行缓冲 为 「」。

AI控制_初始化 接受 零参：
    若 AI控制_已初始化：
        返还。
    完。
    结果 为 做 串口_初始化_端口 用 AI控制_COM口。
    若 结果 不等于 成功：
        做 内核警告 用 「AI控制通道: COM2初始化失败, 将退化为无外部控制」。
    否则：
        做 内核信息 用 「AI控制通道: COM2就绪 (觉控一版 + REQ兼容)」。
    完。
    AI控制_行缓冲 为 「」。
    AI控制_已初始化 为 真。
完。

AI控制_tick 接受 零参：
    若 非 AI控制_已初始化：
        做 AI控制_初始化。
    完。
    可变 字节 为 做 串口_接收字节_端口 用 AI控制_COM口。
    只要 字节 大于或等于 0：
        若 字节 等于 10 或者 字节 等于 13：
            若 做 字符串长度 用 AI控制_行缓冲 大于 0：
                做 AI控制_处理行 用 AI控制_行缓冲。
                AI控制_行缓冲 为 「」。
            完。
        否则：
            若 做 字符串长度 用 AI控制_行缓冲 小于 AI控制_最大行长度：
                AI控制_行缓冲 为 AI控制_行缓冲 加 做 字符 用 字节。
            完。
        完。
        字节 为 做 串口_接收字节_端口 用 AI控制_COM口。
    完。
完。

AI控制_处理行 接受 行：
    文本 为 做 字符串去首尾空白 用 行。
    若 做 字符串长度 用 文本 等于 0：
        返还。
    完。
    回串 为 做 控串 用 文本。
    若 做 字符串长度 用 回串 大于 0：
        做 串口_打印行_端口 用 AI控制_COM口、回串。
    完。
完。

AI控制_处理命令 接受 请求ID、命令、参数：
    文本 为 「REQ 」 加 做 整数转字符串 用 请求ID 加 「 」 加 命令。
    若 做 字符串长度 用 参数 大于 0：
        文本 为 文本 加 「 」 加 参数。
    完。
    回串 为 做 控串 用 文本。
    若 做 字符串长度 用 回串 大于 0：
        做 串口_打印行_端口 用 AI控制_COM口、回串。
    完。
完。

本章 收束。
