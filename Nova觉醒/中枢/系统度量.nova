《系统度量》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《.·内核打印》。

引用《.·系统信息》。

度量_计数器 为 0。

度量_仪表 为 1。

度量_直方图 为 2。

度量点 是 一种 事物，含 名称、类型、值、最小值、最大值、累计、采样数、最后更新。

可变 _度量表 为 一组：（空）。

可变 _度量已初始化 为 假。

可变 _采样周期 为 0。

度量_初始化 接受 零参：
    若 _度量已初始化：
        返还。
    完。
    _度量表 为 一组：（空）。
    做 度量_注册 用 「cpu.使用率」、度量_仪表。
    做 度量_注册 用 「cpu.用户态」、度量_计数器。
    做 度量_注册 用 「cpu.内核态」、度量_计数器。
    做 度量_注册 用 「cpu.空闲」、度量_计数器。
    做 度量_注册 用 「cpu.中断数」、度量_计数器。
    做 度量_注册 用 「cpu.上下文切换」、度量_计数器。
    做 度量_注册 用 「mem.总量」、度量_仪表。
    做 度量_注册 用 「mem.已用」、度量_仪表。
    做 度量_注册 用 「mem.空闲」、度量_仪表。
    做 度量_注册 用 「mem.缓存」、度量_仪表。
    做 度量_注册 用 「mem.交换已用」、度量_仪表。
    做 度量_注册 用 「mem.页错误」、度量_计数器。
    做 度量_注册 用 「mem.GC次数」、度量_计数器。
    做 度量_注册 用 「mem.GC回收字节」、度量_计数器。
    做 度量_注册 用 「disk.读字节」、度量_计数器。
    做 度量_注册 用 「disk.写字节」、度量_计数器。
    做 度量_注册 用 「disk.读次数」、度量_计数器。
    做 度量_注册 用 「disk.写次数」、度量_计数器。
    做 度量_注册 用 「disk.队列深度」、度量_仪表。
    做 度量_注册 用 「net.收字节」、度量_计数器。
    做 度量_注册 用 「net.发字节」、度量_计数器。
    做 度量_注册 用 「net.收包数」、度量_计数器。
    做 度量_注册 用 「net.发包数」、度量_计数器。
    做 度量_注册 用 「net.丢包数」、度量_计数器。
    做 度量_注册 用 「net.连接数」、度量_仪表。
    做 度量_注册 用 「proc.总数」、度量_仪表。
    做 度量_注册 用 「proc.运行中」、度量_仪表。
    做 度量_注册 用 「proc.睡眠中」、度量_仪表。
    做 度量_注册 用 「proc.僵尸」、度量_仪表。
    做 度量_注册 用 「proc.创建总数」、度量_计数器。
    做 度量_注册 用 「syscall.总数」、度量_计数器。
    做 度量_注册 用 「syscall.失败数」、度量_计数器。
    做 度量_注册 用 「ai.推理次数」、度量_计数器。
    做 度量_注册 用 「ai.推理延迟us」、度量_仪表。
    做 度量_注册 用 「bundle.已装载」、度量_仪表。
    做 度量_注册 用 「bundle.加载失败」、度量_计数器。
    _度量已初始化 为 真。
    _采样周期 为 0。
完。

度量_注册 接受 名称、类型：
    m 为 新建 度量点。
    m 的 名称 为 名称。
    m 的 类型 为 类型。
    m 的 值 为 0。
    m 的 最小值 为 0。
    m 的 最大值 为 0。
    m 的 累计 为 0。
    m 的 采样数 为 0。
    m 的 最后更新 为 0。
    做 追加 用 _度量表、m。
完。

度量_查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _度量表：
        若 _度量表 的 第 i 项 的 名称 等于 名称：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

度量_递增 接受 名称：
    做 度量_增加 用 名称、1。
完。

度量_增加 接受 名称、增量：
    idx 为 做 度量_查找 用 名称。
    若 idx 小于 0：
        返还。
    完。
    _度量表 的 第 idx 项 的 值 为 _度量表 的 第 idx 项 的 值 加 增量。
    _度量表 的 第 idx 项 的 累计 为 _度量表 的 第 idx 项 的 累计 加 增量。
    _度量表 的 第 idx 项 的 采样数 为 _度量表 的 第 idx 项 的 采样数 加 1。
    _度量表 的 第 idx 项 的 最后更新 为 做 读取毫秒。
    若 _度量表 的 第 idx 项 的 值 大于 _度量表 的 第 idx 项 的 最大值：
        _度量表 的 第 idx 项 的 最大值 为 _度量表 的 第 idx 项 的 值。
    完。
完。

度量_设置 接受 名称、值：
    idx 为 做 度量_查找 用 名称。
    若 idx 小于 0：
        返还。
    完。
    旧值 为 _度量表 的 第 idx 项 的 值。
    _度量表 的 第 idx 项 的 值 为 值。
    _度量表 的 第 idx 项 的 累计 为 _度量表 的 第 idx 项 的 累计 加 值。
    _度量表 的 第 idx 项 的 采样数 为 _度量表 的 第 idx 项 的 采样数 加 1。
    _度量表 的 第 idx 项 的 最后更新 为 做 读取毫秒。
    若 值 大于 _度量表 的 第 idx 项 的 最大值：
        _度量表 的 第 idx 项 的 最大值 为 值。
    完。
    若 _度量表 的 第 idx 项 的 采样数 等于 1 或者 值 小于 _度量表 的 第 idx 项 的 最小值：
        _度量表 的 第 idx 项 的 最小值 为 值。
    完。
完。

度量_读取 接受 名称：
    idx 为 做 度量_查找 用 名称。
    若 idx 小于 0：
        返还 0。
    完。
    返还 _度量表 的 第 idx 项 的 值。
完。

度量_采样 接受 零参：
    _采样周期 为 _采样周期 加 1。
    做 度量_设置 用 「mem.总量」、做 物理内存_总量。
    做 度量_设置 用 「mem.空闲」、做 物理内存_空闲。
    做 度量_设置 用 「mem.已用」、做 物理内存_总量 减 做 物理内存_空闲。
    做 度量_设置 用 「mem.缓存」、做 物理内存_缓冲。
    做 度量_设置 用 「proc.总数」、做 进程_总数。
完。

度量_类型名 接受 类型：
    若 类型 等于 度量_计数器：
        返还 「计数器」。
    完。
    若 类型 等于 度量_仪表：
        返还 「仪表」。
    完。
    若 类型 等于 度量_直方图：
        返还 「直方图」。
    完。
    返还 「未知」。
完。

度量_导出文本 接受 零参：
    可变 r 为 「═══ 系统度量 ═══\n」。
    r 为 r 加 「采样周期: 」 加 做 整数转字符串 用 _采样周期 加 「\n」。
    r 为 r 加 「度量点: 」 加 做 整数转字符串 用 做 计数 用 _度量表 加 「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _度量表：
        m 为 _度量表 的 第 i 项。
        r 为 r 加 「  」 加 m 的 名称 加 「 = 」 加 做 整数转字符串 用 m 的 值。
        若 m 的 采样数 大于 1：
            r 为 r 加 「 (min=」 加 做 整数转字符串 用 m 的 最小值 加 「 max=」 加 做 整数转字符串 用 m 的 最大值 加 「)」。
        完。
        r 为 r 加 「\n」。
        i 为 i 加 1。
    完。
    返还 r。
完。

度量_重置 接受 零参：
    _度量表 为 一组：（空）。
    _度量已初始化 为 假。
    _采样周期 为 0。
完。

本章 收束。
