《墙钟设置》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·推理·通信渠道》。

引用《.·硬链接》。

引用《..·推理·定点Q算术》。

引用《.·恐慌》。

引用《..·推理·AI鼠标事件处理》。

引用《.·整数转字符串》。

引用《..·引导·特权指令》。

NS_PER_S 为 1000000000。

US_PER_S 为 1000000。

MS_PER_S 为 1000。

NS_PER_MS 为 1000000。

NS_PER_US 为 1000。

Q10 为 1024。

Q_ONE 为 1024。

Q_HALF 为 512。

CLOCK_SRC_TSC 为 1。

CLOCK_SRC_HPET 为 2。

CLOCK_SRC_ACPI 为 3。

CLOCK_SRC_PARAVIRT 为 4。

NTP_STATE_INIT 为 0。

NTP_STATE_LOCKING 为 1。

NTP_STATE_LOCKED 为 2。

NTP_STATE_HOLDOVER 为 3。

最大闰秒记录 为 128。

最大时区记录 为 64。

TSC_抖动阈值_ns 为 20000。

TSC_漂移阈值_ppm 为 250。

NTP_最大步进_ns 为 500000000。

NTP_最大slew_ppm 为 500。

EWMA_ALPHA_慢 为 64。

EWMA_ALPHA_中 为 205。

EWMA_ALPHA_快 为 512。

时钟源项 是 一种 事物，含 名称、类型、评分、可用、周期频率、读取函数。

墙钟状态 是 一种 事物，含 锚点秒、锚点纳秒、锚点计数器、当前源类型、频率Hz、频偏_ppm、单调偏移_ns、启动时戳_ns、上次更新_ns。

NTP状态 是 一种 事物，含 状态、偏移_ns、频偏_ppm、相位积分项_ns、上次同步_ns、连续成功次数、连续失败次数、抖动_ewma_ns、往返延迟_ewma_ns。

闰秒记录 是 一种 事物，含 生效UTC秒、类型、已执行。

时区记录 是 一种 事物，含 名称、UTC偏移分钟、夏令时标记。

时间快照 是 一种 事物，含 utc秒、utc纳秒、monotonic_ns、boottime_ns、源类型、估计误差_ns。

可变 墙钟_全局 为 新建 墙钟状态。

可变 NTP_全局 为 新建 NTP状态。

可变 时钟源列表 为 一组：（空）。

可变 当前时钟源名 为 「未初始化」。

可变 TSC频率 为 0。

可变 HPET_可用 为 假。

可变 tsc_不稳定 为 假。

可变 墙钟_秒 为 0。

可变 墙钟_纳秒 为 0。

可变 墙钟_上次同步TSC 为 0。

可变 闰秒表 为 一组：（空）。

可变 时区表 为 一组：（空）。

可变 当前时区名 为 「UTC」。

可变 当前时区偏移分钟 为 0。

可变 时间误差_ewma_ns 为 0。

可变 时间抖动_ewma_ns 为 0。

可变 时间跳变计数 为 0。

可变 同步失败计数 为 0。

墙钟_初始化 接受 零参：
    时钟源列表 为 一组：（空）。
    做 追加 用 时钟源列表、新建 时钟源项：名称 为 「tsc」、类型 为 CLOCK_SRC_TSC、评分 为 9000、可用 为 真、周期频率 为 做 TSC_探测频率、读取函数 为 TSC_读取。
    做 追加 用 时钟源列表、新建 时钟源项：名称 为 「hpet」、类型 为 CLOCK_SRC_HPET、评分 为 若 做 HPET_检测 则 8000 否则 0、可用 为 做 HPET_检测、周期频率 为 做 HPET_频率、读取函数 为 HPET_读取。
    做 追加 用 时钟源列表、新建 时钟源项：名称 为 「acpi_pm」、类型 为 CLOCK_SRC_ACPI、评分 为 3500、可用 为 做 ACPI_PM_可用、周期频率 为 3579545、读取函数 为 ACPI_PM_读取。
    做 时钟源_选择最优。
    墙钟_秒 为 0。
    墙钟_纳秒 为 0。
    墙钟_上次同步TSC 为 做 读取时间戳。
    墙钟_全局 的 锚点秒 为 0。
    墙钟_全局 的 锚点纳秒 为 0。
    墙钟_全局 的 锚点计数器 为 做 当前源_读取计数器。
    墙钟_全局 的 当前源类型 为 做 当前源_类型。
    墙钟_全局 的 频率Hz 为 做 当前源_频率。
    墙钟_全局 的 频偏_ppm 为 0。
    墙钟_全局 的 单调偏移_ns 为 0。
    墙钟_全局 的 启动时戳_ns 为 0。
    墙钟_全局 的 上次更新_ns 为 0。
    NTP_全局 的 状态 为 NTP_STATE_INIT。
    NTP_全局 的 偏移_ns 为 0。
    NTP_全局 的 频偏_ppm 为 0。
    NTP_全局 的 相位积分项_ns 为 0。
    NTP_全局 的 上次同步_ns 为 0。
    NTP_全局 的 连续成功次数 为 0。
    NTP_全局 的 连续失败次数 为 0。
    NTP_全局 的 抖动_ewma_ns 为 0。
    NTP_全局 的 往返延迟_ewma_ns 为 0。
    闰秒表 为 一组：（空）。
    时区表 为 一组：（空）。
    做 时区_加载默认。
    做 闰秒_加载默认。
完。

时区_加载默认 接受 零参：
    做 追加 用 时区表、新建 时区记录：名称 为 「UTC」、UTC偏移分钟 为 0、夏令时标记 为 假。
    做 追加 用 时区表、新建 时区记录：名称 为 「Asia/Shanghai」、UTC偏移分钟 为 480、夏令时标记 为 假。
    做 追加 用 时区表、新建 时区记录：名称 为 「Asia/Tokyo」、UTC偏移分钟 为 540、夏令时标记 为 假。
    做 追加 用 时区表、新建 时区记录：名称 为 「America/New_York」、UTC偏移分钟 为 负 300、夏令时标记 为 真。
    做 追加 用 时区表、新建 时区记录：名称 为 「America/Los_Angeles」、UTC偏移分钟 为 负 480、夏令时标记 为 真。
    做 追加 用 时区表、新建 时区记录：名称 为 「Europe/London」、UTC偏移分钟 为 0、夏令时标记 为 真。
    做 追加 用 时区表、新建 时区记录：名称 为 「Europe/Berlin」、UTC偏移分钟 为 60、夏令时标记 为 真。
    做 追加 用 时区表、新建 时区记录：名称 为 「Asia/Dubai」、UTC偏移分钟 为 240、夏令时标记 为 假。
    做 追加 用 时区表、新建 时区记录：名称 为 「Asia/Kolkata」、UTC偏移分钟 为 330、夏令时标记 为 假。
    做 追加 用 时区表、新建 时区记录：名称 为 「Australia/Sydney」、UTC偏移分钟 为 600、夏令时标记 为 真。
完。

闰秒_加载默认 接受 零参：
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 78796800、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 94694400、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 126230400、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 157766400、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 189302400、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 220924800、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 252460800、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 283996800、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 315532800、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 362793600、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 394329600、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 425865600、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 489024000、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 567993600、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 631152000、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 662688000、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 709948800、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 741484800、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 773020800、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 820454400、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 867715200、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 915148800、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 1136073600、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 1230768000、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 1341100800、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 1435708800、类型 为 1、已执行 为 真。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 1483228800、类型 为 1、已执行 为 真。
完。

墙钟_设置 接受 秒、纳秒：
    若 纳秒 小于 0：
        纳秒 为 0。
    完。
    若 纳秒 大于或等于 NS_PER_S：
        秒 为 秒 加 纳秒 除以 NS_PER_S。
        纳秒 为 纳秒 取余于 NS_PER_S。
    完。
    墙钟_秒 为 秒。
    墙钟_纳秒 为 纳秒。
    墙钟_上次同步TSC 为 做 读取时间戳。
    墙钟_全局 的 锚点秒 为 秒。
    墙钟_全局 的 锚点纳秒 为 纳秒。
    墙钟_全局 的 锚点计数器 为 做 当前源_读取计数器。
    墙钟_全局 的 上次更新_ns 为 秒 乘 NS_PER_S 加 纳秒。
完。

墙钟_设置_带误差 接受 秒、纳秒、误差_ns：
    做 墙钟_设置 用 秒、纳秒。
    时间误差_ewma_ns 为 (EWMA_ALPHA_中 乘 误差_ns 加 (Q10 减 EWMA_ALPHA_中) 乘 时间误差_ewma_ns) 除以 Q10。
完。

墙钟_获取 接受 零参：
    当前计数器 为 做 当前源_读取计数器。
    差计数 为 当前计数器 减 墙钟_全局 的 锚点计数器。
    差值ns 为 做 周期转纳秒 用 差计数、墙钟_全局 的 频率Hz。
    频偏修正_ns 为 差值ns 乘 墙钟_全局 的 频偏_ppm 除以 1000000。
    总纳秒 为 墙钟_全局 的 锚点纳秒 加 差值ns 加 频偏修正_ns。
    额外秒 为 总纳秒 除以 NS_PER_S。
    返还 一组：墙钟_全局 的 锚点秒 加 额外秒、总纳秒 取余于 NS_PER_S。
完。

墙钟_获取_ns 接受 零参：
    t 为 做 墙钟_获取。
    返还 t 的 第 0 项 乘 NS_PER_S 加 t 的 第 1 项。
完。

时钟源_当前时间_秒 接受 零参：
    时间 为 做 墙钟_获取。
    返还 时间 的 第 0 项。
完。

当前时间_us 接受 零参：
    时间 为 做 墙钟_获取。
    返还 时间 的 第 0 项 乘 US_PER_S 加 时间 的 第 1 项 除以 NS_PER_US。
完。

当前时间_ms 接受 零参：
    时间 为 做 墙钟_获取。
    返还 时间 的 第 0 项 乘 MS_PER_S 加 时间 的 第 1 项 除以 NS_PER_MS。
完。

单调钟_获取_ns 接受 零参：
    当前计数器 为 做 当前源_读取计数器。
    差计数 为 当前计数器 减 墙钟_全局 的 锚点计数器。
    返还 墙钟_全局 的 单调偏移_ns 加 做 周期转纳秒 用 差计数、墙钟_全局 的 频率Hz。
完。

启动钟_获取_ns 接受 零参：
    返还 做 单调钟_获取_ns 加 墙钟_全局 的 启动时戳_ns。
完。

时钟源_选择最优 接受 零参：
    可变 最佳 为 负 1。
    可变 最佳分 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 时钟源列表：
        若 时钟源列表 的 第 i 项 的 可用 并且 时钟源列表 的 第 i 项 的 评分 大于 最佳分：
            最佳分 为 时钟源列表 的 第 i 项 的 评分。
            最佳 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最佳 小于 0：
        做 内核恐慌 用 「无可用时钟源」。
        返还。
    完。
    当前时钟源名 为 时钟源列表 的 第 最佳 项 的 名称。
    墙钟_全局 的 当前源类型 为 时钟源列表 的 第 最佳 项 的 类型。
    墙钟_全局 的 频率Hz 为 时钟源列表 的 第 最佳 项 的 周期频率。
    TSC频率 为 若 当前时钟源名 等于 「tsc」 则 墙钟_全局 的 频率Hz 否则 TSC频率。
完。

当前源_读取计数器 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 时钟源列表：
        若 时钟源列表 的 第 i 项 的 名称 等于 当前时钟源名：
            返还 做 调用 用 时钟源列表 的 第 i 项 的 读取函数。
        完。
        i 为 i 加 1。
    完。
    返还 做 读取时间戳。
完。

当前源_频率 接受 零参：
    返还 墙钟_全局 的 频率Hz。
完。

当前源_类型 接受 零参：
    返还 墙钟_全局 的 当前源类型。
完。

周期转纳秒 接受 周期、频率Hz：
    若 频率Hz 小于或等于 0：
        返还 0。
    完。
    返还 周期 乘 NS_PER_S 除以 频率Hz。
完。

TSC周期转纳秒 接受 周期：
    返还 做 周期转纳秒 用 周期、TSC频率。
完。

TSC_标记不稳定 接受 原因：
    若 tsc_不稳定：
        返还。
    完。
    tsc_不稳定 为 真。
    做 内核警告 用 「TSC不稳定: 」 加 原因。
    可变 i 为 0。
    只要 i 小于 做 计数 用 时钟源列表：
        若 时钟源列表 的 第 i 项 的 名称 等于 「tsc」：
            时钟源列表 的 第 i 项 的 评分 为 0。
        完。
        i 为 i 加 1。
    完。
    做 时钟源_选择最优。
完。

TSC_是否稳定 接受 零参：
    返还 非 tsc_不稳定。
完。

TSC_探测频率 接受 零参：
    f 为 做 平台_读取TSC频率。
    若 f 小于或等于 0：
        f 为 2500000000。
    完。
    返还 f。
完。

TSC_稳定性巡检 接受 零参：
    若 非 做 HPET_检测：
        返还。
    完。
    tsc0 为 做 读取时间戳。
    h0 为 做 HPET_读取。
    做 mdelay 用 5。
    tsc1 为 做 读取时间戳。
    h1 为 做 HPET_读取。
    tsc_ns 为 做 TSC周期转纳秒 用 tsc1 减 tsc0。
    hpet_ns 为 做 HPET周期转纳秒 用 h1 减 h0。
    偏差 为 tsc_ns 减 hpet_ns。
    若 偏差 小于 0：
        偏差 为 负 偏差。
    完。
    时间抖动_ewma_ns 为 (EWMA_ALPHA_慢 乘 偏差 加 (Q10 减 EWMA_ALPHA_慢) 乘 时间抖动_ewma_ns) 除以 Q10。
    若 时间抖动_ewma_ns 大于 TSC_抖动阈值_ns：
        做 TSC_标记不稳定 用 「抖动超过阈值」。
    完。
完。

TSC_读取 接受 零参：
    返还 做 读取时间戳。
完。

HPET_读取 接受 零参：
    返还 做 平台_HPET_读取。
完。

ACPI_PM_读取 接受 零参：
    返还 做 平台_ACPI_PM_读取。
完。

HPET_检测 接受 零参：
    返还 做 平台_HPET_可用。
完。

HPET_频率 接受 零参：
    返还 做 平台_HPET_频率。
完。

ACPI_PM_可用 接受 零参：
    返还 做 平台_ACPI_PM_可用。
完。

HPET周期转纳秒 接受 周期：
    f 为 做 HPET_频率。
    若 f 小于或等于 0：
        返还 0。
    完。
    返还 周期 乘 NS_PER_S 除以 f。
完。

NTP_应用测量 接受 服务器UTC秒、服务器UTC纳秒、往返延迟_ns：
    本地 为 做 墙钟_获取。
    本地_ns 为 本地 的 第 0 项 乘 NS_PER_S 加 本地 的 第 1 项。
    服务器_ns 为 服务器UTC秒 乘 NS_PER_S 加 服务器UTC纳秒。
    偏移_ns 为 服务器_ns 减 本地_ns。
    NTP_全局 的 偏移_ns 为 偏移_ns。
    NTP_全局 的 往返延迟_ewma_ns 为 (EWMA_ALPHA_中 乘 往返延迟_ns 加 (Q10 减 EWMA_ALPHA_中) 乘 NTP_全局 的 往返延迟_ewma_ns) 除以 Q10。
    NTP_全局 的 抖动_ewma_ns 为 (EWMA_ALPHA_中 乘 做 绝对值 用 偏移_ns 加 (Q10 减 EWMA_ALPHA_中) 乘 NTP_全局 的 抖动_ewma_ns) 除以 Q10。
    若 做 绝对值 用 偏移_ns 大于 NTP_最大步进_ns：
        做 墙钟_强制步进 用 偏移_ns。
        NTP_全局 的 状态 为 NTP_STATE_LOCKING。
        NTP_全局 的 连续成功次数 为 0。
        NTP_全局 的 连续失败次数 为 0。
        返还。
    完。
    Kp 为 64。
    Ki 为 4。
    比例项 为 偏移_ns 乘 Kp 除以 Q10。
    NTP_全局 的 相位积分项_ns 为 NTP_全局 的 相位积分项_ns 加 偏移_ns 乘 Ki 除以 Q10。
    校正_ns 为 比例项 加 NTP_全局 的 相位积分项_ns。
    频偏_ppm 为 校正_ns 除以 1000000。
    若 频偏_ppm 大于 NTP_最大slew_ppm：
        频偏_ppm 为 NTP_最大slew_ppm。
    完。
    若 频偏_ppm 小于 负 NTP_最大slew_ppm：
        频偏_ppm 为 负 NTP_最大slew_ppm。
    完。
    墙钟_全局 的 频偏_ppm 为 频偏_ppm。
    NTP_全局 的 频偏_ppm 为 频偏_ppm。
    NTP_全局 的 上次同步_ns 为 本地_ns。
    NTP_全局 的 连续成功次数 为 NTP_全局 的 连续成功次数 加 1。
    NTP_全局 的 连续失败次数 为 0。
    若 NTP_全局 的 连续成功次数 大于 6：
        NTP_全局 的 状态 为 NTP_STATE_LOCKED。
    否则：
        NTP_全局 的 状态 为 NTP_STATE_LOCKING。
    完。
完。

NTP_同步失败 接受 零参：
    NTP_全局 的 连续失败次数 为 NTP_全局 的 连续失败次数 加 1。
    NTP_全局 的 连续成功次数 为 0。
    同步失败计数 为 同步失败计数 加 1。
    若 NTP_全局 的 连续失败次数 大于 8：
        NTP_全局 的 状态 为 NTP_STATE_HOLDOVER。
    完。
完。

墙钟_强制步进 接受 偏移_ns：
    旧 为 做 墙钟_获取_ns。
    新 为 旧 加 偏移_ns。
    若 新 小于 0：
        新 为 0。
    完。
    做 墙钟_设置 用 新 除以 NS_PER_S、新 取余于 NS_PER_S。
    时间跳变计数 为 时间跳变计数 加 1。
完。

NTP_当前状态 接受 零参：
    返还 一组：NTP_全局 的 状态、NTP_全局 的 偏移_ns、NTP_全局 的 频偏_ppm、NTP_全局 的 抖动_ewma_ns、NTP_全局 的 往返延迟_ewma_ns。
完。

闰秒_添加 接受 生效UTC秒、类型：
    若 做 计数 用 闰秒表 大于或等于 最大闰秒记录：
        返还 错误_空间不足。
    完。
    若 类型 不等于 1 并且 类型 不等于 负 1：
        返还 错误_参数无效。
    完。
    做 追加 用 闰秒表、新建 闰秒记录：生效UTC秒 为 生效UTC秒、类型 为 类型、已执行 为 假。
    返还 错误_成功。
完。

闰秒_检查并执行 接受 零参：
    t 为 做 墙钟_获取。
    当前秒 为 t 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 闰秒表：
        记录 为 闰秒表 的 第 i 项。
        若 非 记录 的 已执行 并且 当前秒 大于或等于 记录 的 生效UTC秒：
            若 记录 的 类型 等于 1：
                墙钟_全局 的 锚点秒 为 墙钟_全局 的 锚点秒 减 1。
            否则：
                墙钟_全局 的 锚点秒 为 墙钟_全局 的 锚点秒 加 1。
            完。
            记录 的 已执行 为 真。
            做 设置 用 闰秒表、i、记录。
        完。
        i 为 i 加 1。
    完。
完。

闰秒_统计 接受 零参：
    总 为 做 计数 用 闰秒表。
    已执行 为 0。
    可变 i 为 0。
    只要 i 小于 总：
        若 闰秒表 的 第 i 项 的 已执行：
            已执行 为 已执行 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：总、已执行。
完。

时区_设置 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 时区表：
        若 时区表 的 第 i 项 的 名称 等于 名称：
            当前时区名 为 名称。
            当前时区偏移分钟 为 时区表 的 第 i 项 的 UTC偏移分钟。
            返还 错误_成功。
        完。
        i 为 i 加 1。
    完。
    返还 错误_未找到。
完。

时区_获取 接受 零参：
    返还 一组：当前时区名、当前时区偏移分钟。
完。

本地时间_获取 接受 零参：
    utc 为 做 墙钟_获取。
    偏移秒 为 当前时区偏移分钟 乘 60。
    本地秒 为 utc 的 第 0 项 加 偏移秒。
    返还 一组：本地秒、utc 的 第 1 项。
完。

UTC转本地 接受 utc秒、utc纳秒：
    返还 一组：utc秒 加 当前时区偏移分钟 乘 60、utc纳秒。
完。

本地转UTC 接受 本地秒、本地纳秒：
    返还 一组：本地秒 减 当前时区偏移分钟 乘 60、本地纳秒。
完。

时间_格式化_ISO8601 接受 秒、纳秒：
    日期 为 做 时间_拆分UTC 用 秒。
    ns6 为 纳秒 除以 1000。
    s 为 做 整数补零 用 日期 的 年、4 加 「-」 加 做 整数补零 用 日期 的 月、2 加 「-」 加 做 整数补零 用 日期 的 日、2 加 「T」 加 做 整数补零 用 日期 的 时、2 加 「:」 加 做 整数补零 用 日期 的 分、2 加 「:」 加 做 整数补零 用 日期 的 秒、2 加 「.」 加 做 整数补零 用 ns6、6 加 「Z」。
    返还 s。
完。

时间_当前_ISO8601 接受 零参：
    t 为 做 墙钟_获取。
    返还 做 时间_格式化_ISO8601 用 t 的 第 0 项、t 的 第 1 项。
完。

时间_拆分UTC 接受 秒：
    可变 s 为 秒。
    可变 年 为 1970。
    只要 真：
        天数 为 若 做 闰年 用 年 则 366 否则 365。
        年秒 为 天数 乘 86400。
        若 s 大于或等于 年秒：
            s 为 s 减 年秒。
            年 为 年 加 1。
        否则：
            跳出。
        完。
    完。
    月长 为 一组：31、28、31、30、31、30、31、31、30、31、30、31。
    若 做 闰年 用 年：
        做 设置 用 月长、1、29。
    完。
    可变 月 为 1。
    可变 i 为 0。
    只要 i 小于 12：
        月秒 为 月长 的 第 i 项 乘 86400。
        若 s 大于或等于 月秒：
            s 为 s 减 月秒。
            月 为 月 加 1。
        否则：
            跳出。
        完。
        i 为 i 加 1。
    完。
    日 为 s 除以 86400 加 1。
    s 为 s 取余于 86400。
    时 为 s 除以 3600。
    s 为 s 取余于 3600。
    分 为 s 除以 60。
    秒内 为 s 取余于 60。
    返还 一本字典：年 对应 年、月 对应 月、日 对应 日、时 对应 时、分 对应 分、秒 对应 秒内。
完。

闰年 接受 年：
    若 年 取余于 400 等于 0：
        返还 真。
    完。
    若 年 取余于 100 等于 0：
        返还 假。
    完。
    返还 年 取余于 4 等于 0。
完。

整数补零 接受 n、位数：
    s 为 做 整数转字符串 用 n。
    只要 做 字符串长度 用 s 小于 位数：
        s 为 「0」 加 s。
    完。
    返还 s。
完。

udelay 接受 微秒：
    f 为 做 当前源_频率。
    若 f 小于或等于 0：
        可变 c 为 微秒 乘 1000。
        只要 c 大于 0：
            c 为 c 减 1。
        完。
        返还。
    完。
    目标周期 为 f 除以 US_PER_S 乘 微秒。
    开始 为 做 当前源_读取计数器。
    只要 做 当前源_读取计数器 减 开始 小于 目标周期：
        做 暂停。
    完。
完。

mdelay 接受 毫秒：
    可变 i 为 0。
    只要 i 小于 毫秒：
        做 udelay 用 1000。
        i 为 i 加 1。
    完。
完。

ndelay 接受 纳秒：
    f 为 做 当前源_频率。
    若 f 小于或等于 0：
        返还。
    完。
    目标周期 为 f 除以 NS_PER_S 乘 纳秒。
    若 目标周期 小于或等于 0：
        目标周期 为 1。
    完。
    开始 为 做 当前源_读取计数器。
    只要 做 当前源_读取计数器 减 开始 小于 目标周期：
        做 暂停。
    完。
完。

睡眠_直到 接受 目标秒、目标纳秒：
    只要 真：
        t 为 做 墙钟_获取。
        若 t 的 第 0 项 大于 目标秒 或者 t 的 第 0 项 等于 目标秒 并且 t 的 第 1 项 大于或等于 目标纳秒：
            返还。
        完。
        做 平台_让出CPU。
    完。
完。

时间_生成快照 接受 零参：
    utc 为 做 墙钟_获取。
    返还 新建 时间快照：utc秒 为 utc 的 第 0 项、utc纳秒 为 utc 的 第 1 项、monotonic_ns 为 做 单调钟_获取_ns、boottime_ns 为 做 启动钟_获取_ns、源类型 为 做 当前源_类型、估计误差_ns 为 时间误差_ewma_ns。
完。

时钟源_统计 接受 零参：
    返还 一组：当前时钟源名、TSC频率、做 计数 用 时钟源列表、tsc_不稳定、HPET_可用。
完。

时钟源_完整统计 接受 零参：
    ntp 为 做 NTP_当前状态。
    leap 为 做 闰秒_统计。
    返还 一组：当前时钟源名、TSC频率、做 计数 用 时钟源列表、tsc_不稳定、HPET_可用、墙钟_全局 的 频偏_ppm、ntp 的 第 0 项、ntp 的 第 1 项、ntp 的 第 2 项、ntp 的 第 3 项、ntp 的 第 4 项、时间误差_ewma_ns、时间抖动_ewma_ns、时间跳变计数、同步失败计数、leap 的 第 0 项、leap 的 第 1 项、当前时区名、当前时区偏移分钟。
完。

时间_诊断字符串 接受 零参：
    t 为 做 墙钟_获取。
    iso 为 做 时间_格式化_ISO8601 用 t 的 第 0 项、t 的 第 1 项。
    返还 「clock=」 加 当前时钟源名 加 「 now=」 加 iso 加 「 ppm=」 加 做 整数转字符串 用 墙钟_全局 的 频偏_ppm 加 「 err_ns=」 加 做 整数转字符串 用 时间误差_ewma_ns。
完。

可变 EWMA_偏移绝对值 为 0。

可变 EWMA_丢同步率 为 0。

智能_时钟_AI反馈NTP 接受 偏移_ns、同步成功：
    abs_off 为 做 绝对值 用 偏移_ns。
    EWMA_偏移绝对值 为 (EWMA_ALPHA_中 乘 abs_off 加 (Q10 减 EWMA_ALPHA_中) 乘 EWMA_偏移绝对值) 除以 Q10。
    若 同步成功：
        EWMA_丢同步率 为 (Q10 减 EWMA_ALPHA_中) 乘 EWMA_丢同步率 除以 Q10。
    否则：
        EWMA_丢同步率 为 (EWMA_ALPHA_中 乘 Q_ONE 加 (Q10 减 EWMA_ALPHA_中) 乘 EWMA_丢同步率) 除以 Q10。
    完。
完。

智能_时钟_AI评估风险 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    特征 为 做 Tensor_创建 用 1、6。
    做 Tensor_写 用 特征、0、0、EWMA_偏移绝对值 除以 1000。
    做 Tensor_写 用 特征、0、1、EWMA_丢同步率。
    做 Tensor_写 用 特征、0、2、时间抖动_ewma_ns 除以 1000。
    做 Tensor_写 用 特征、0、3、若 tsc_不稳定 则 Q_ONE 否则 0。
    做 Tensor_写 用 特征、0、4、做 绝对值 用 墙钟_全局 的 频偏_ppm。
    做 Tensor_写 用 特征、0、5、Q_ONE。
    分数 为 做 AI总线_注意力权重 用 bus、特征。
    做 Tensor_释放 用 特征。
    返还 分数。
完。

智能_时钟_AI可能切源 接受 零参：
    风险 为 做 智能_时钟_AI评估风险。
    若 风险 大于 820：
        若 当前时钟源名 等于 「tsc」：
            做 TSC_标记不稳定 用 「AI检测到高风险时钟漂移」。
        完。
        返还 真。
    完。
    返还 假。
完。

时间子系统_每秒维护 接受 零参：
    做 TSC_稳定性巡检。
    做 闰秒_检查并执行。
    做 智能_时钟_AI可能切源。
完。

时间子系统_每分钟维护 接受 零参：
    now 为 做 墙钟_获取。
    做 墙钟_设置 用 now 的 第 0 项、now 的 第 1 项。
完。

错误_空间不足 接受 零参：
    返还 负 12。
完。

错误_参数无效 接受 零参：
    返还 负 22。
完。

错误_未找到 接受 零参：
    返还 负 2。
完。

错误_成功 接受 零参：
    返还 0。
完。

当前时间_秒 接受 零参：
    返还 做 时钟源_当前时间_秒。
完。

当前时间_纳秒 接受 零参：
    返还 做 墙钟_获取_ns。
完。

当前时间_字符串 接受 零参：
    返还 做 时间_当前_ISO8601。
完。

可变 AI时钟_漂移累计 为 0。

可变 AI时钟_校准次数 为 0。

AI时钟_漂移补偿 接受 本地纳秒、参考纳秒：
    偏差 为 本地纳秒 减 参考纳秒。
    AI时钟_漂移累计 为 AI时钟_漂移累计 乘 900 除以 1000 加 偏差 乘 100 除以 1000。
    AI时钟_校准次数 为 AI时钟_校准次数 加 1。
    返还 AI时钟_漂移累计。
完。

AI时钟_同步策略 接受 漂移量：
    若 漂移量 大于 1000000000：
        返还 「NTP强制跳变」。
    完。
    若 漂移量 大于 1000000：
        返还 「渐进校准」。
    完。
    返还 「无需校准」。
完。

本章 收束。
