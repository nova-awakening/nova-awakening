《虚拟内存区》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·虚分配故障》。

VM_读 为 1 左移 0。

VM_写 为 1 左移 1。

VM_执行 为 1 左移 2。

VM_共享 为 1 左移 3。

VM_匿名 为 1 左移 4。

VM_文件 为 1 左移 5。

VM_增长向下 为 1 左移 6。

VM_锁定 为 1 左移 7。

VM_IO 为 1 左移 8。

VM_直传 为 1 左移 9。

VM_不可拷贝 为 1 左移 10。

VM_不可扩展 为 1 左移 11。

VM_混合映射 为 1 左移 12。

VMA_起始 为 0。

VMA_结束 为 1。

VMA_标志 为 2。

VMA_文件 为 3。

VMA_偏移 为 4。

VMA_MM 为 5。

VMA_匿名链 为 6。

VMA_操作 为 7。

VMA_创建 接受 起始、结束、标志、偏移：
    返还 一组：起始、结束、标志、空、0、空、空、空。
完。

VMA_创建_文件 接受 起始、结束、标志、文件、偏移：
    返还 一组：起始、结束、标志 位或 VM_文件、文件、偏移、空、空、空。
完。

VMA_创建_匿名 接受 起始、结束、标志：
    返还 一组：起始、结束、标志 位或 VM_匿名、空、0、空、空、空。
完。

VMA_页数 接受 vma：
    返还 做 VMA_大小 用 vma 除以 页大小。
完。

VMA_包含 接受 vma、地址：
    返还 地址 大于或等于 vma 的 第 VMA_起始 项 并且 地址 小于 vma 的 第 VMA_结束 项。
完。

VMA_可读 接受 vma：
    返还 vma 的 第 VMA_标志 项 位与 VM_读 不等于 0。
完。

VMA_可写_快速 接受 vma：
    返还 vma 的 第 VMA_标志 项 位与 VM_写 不等于 0。
完。

VMA_可执行_快速 接受 vma：
    返还 vma 的 第 VMA_标志 项 位与 VM_执行 不等于 0。
完。

VMA_是匿名_快速 接受 vma：
    返还 vma 的 第 VMA_标志 项 位与 VM_匿名 不等于 0。
完。

VMA_是文件 接受 vma：
    返还 vma 的 第 VMA_文件 项 不等于 空。
完。

VMA_是共享 接受 vma：
    返还 vma 的 第 VMA_标志 项 位与 VM_共享 不等于 0。
完。

VMA_是栈 接受 vma：
    返还 vma 的 第 VMA_标志 项 位与 VM_增长向下 不等于 0。
完。

VMA_是锁定 接受 vma：
    返还 vma 的 第 VMA_标志 项 位与 VM_锁定 不等于 0。
完。

VMA_获取MM 接受 vma：
    返还 vma 的 第 VMA_MM 项。
完。

VMA_重叠 接受 a、b：
    返还 a 的 第 VMA_起始 项 小于 b 的 第 VMA_结束 项 并且 a 的 第 VMA_结束 项 大于 b 的 第 VMA_起始 项。
完。

VMA_邻接 接受 a、b：
    返还 a 的 第 VMA_结束 项 等于 b 的 第 VMA_起始 项 或者 b 的 第 VMA_结束 项 等于 a 的 第 VMA_起始 项。
完。

VMA_可合并 接受 a、b：
    若 a 的 第 VMA_标志 项 不等于 b 的 第 VMA_标志 项：
        返还 假。
    完。
    若 a 的 第 VMA_文件 项 不等于 b 的 第 VMA_文件 项：
        返还 假。
    完。
    若 a 的 第 VMA_文件 项 不等于 空：
        a页数 为 做 VMA_页数 用 a。
        若 a 的 第 VMA_偏移 项 加 a页数 不等于 b 的 第 VMA_偏移 项：
            返还 假。
        完。
    完。
    返还 做 VMA_邻接 用 a、b。
完。

VMA_合并 接受 a、b：
    若 a 的 第 VMA_起始 项 大于 b 的 第 VMA_起始 项：
        临时 为 a。
        a 为 b。
        b 为 临时。
    完。
    做 设置 用 a、VMA_结束、b 的 第 VMA_结束 项。
    返还 a。
完。

VMA_拆分 接受 vma、拆分地址、新大小：
    若 拆分地址 小于或等于 vma 的 第 VMA_起始 项 或者 拆分地址 大于或等于 vma 的 第 VMA_结束 项：
        返还 空。
    完。
    右偏移 为 vma 的 第 VMA_偏移 项。
    若 vma 的 第 VMA_文件 项 不等于 空：
        右偏移 为 右偏移 加 (拆分地址 减 vma 的 第 VMA_起始 项) 除以 页大小。
    完。
    右vma 为 一组：拆分地址、vma 的 第 VMA_结束 项、vma 的 第 VMA_标志 项、vma 的 第 VMA_文件 项、右偏移、vma 的 第 VMA_MM 项、vma 的 第 VMA_匿名链 项、vma 的 第 VMA_操作 项。
    做 设置 用 vma、VMA_结束、拆分地址。
    返还 一组：vma、右vma。
完。

VMA_转PTE标志 接受 vma：
    可变 pte 为 PTE_存在 位或 PTE_用户。
    若 做 VMA_可写 用 vma：
        pte 为 pte 位或 PTE_可写。
    完。
    若 非 做 VMA_可执行 用 vma：
        pte 为 pte 位或 PTE_不可执行。
    完。
    返还 pte。
完。

VMA_权限字符串 接受 vma：
    可变 结果 为 「」。
    结果 为 结果 加 做 如果选字符 用 做 VMA_可读 用 vma、「r」、「-」。
    结果 为 结果 加 做 如果选字符 用 做 VMA_可写 用 vma、「w」、「-」。
    结果 为 结果 加 做 如果选字符 用 做 VMA_可执行 用 vma、「x」、「-」。
    结果 为 结果 加 做 如果选字符 用 做 VMA_是共享 用 vma、「s」、「p」。
    返还 结果。
完。

如果选字符 接受 条件、真值、假值：
    若 条件：
        返还 真值。
    完。
    返还 假值。
完。

VMA_插入 接受 mm、vma：
    若 mm 等于 空 或者 vma 等于 空：
        返还。
    完。
    做 追加 用 mm、vma。
完。

VMA_查找 接受 mm、地址：
    若 mm 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 mm：
        若 做 VMA_包含 用 mm 的 第 i 项、地址：
            返还 mm 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

VMA_查找空闲 接受 mm、提示地址、大小：
    可变 候选 为 0x400000。
    可变 i 为 0。
    只要 i 小于 做 计数 用 mm：
        若 候选 加 大小 小于或等于 mm 的 第 i 项 的 第 VMA_起始 项：
            返还 候选。
        完。
        若 mm 的 第 i 项 的 第 VMA_结束 项 大于 候选：
            候选 为 mm 的 第 i 项 的 第 VMA_结束 项。
        完。
        i 为 i 加 1。
    完。
    返还 候选。
完。

VMA_查找重叠 接受 mm、起始、结束：
    若 mm 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 mm：
        若 mm 的 第 i 项 的 第 VMA_起始 项 小于 结束 并且 mm 的 第 i 项 的 第 VMA_结束 项 大于 起始：
            返还 mm 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

VMA_结束 接受 vma：
    返还 vma 的 第 VMA_结束 项。
完。

VMA_解除映射 接受 mm、起始、大小：
    若 mm 等于 空：
        返还。
    完。
    结束 为 起始 加 大小。
    可变 i 为 0。
    只要 i 小于 做 计数 用 mm：
        若 mm 的 第 i 项 的 第 VMA_起始 项 大于或等于 起始 并且 mm 的 第 i 项 的 第 VMA_结束 项 小于或等于 结束：
            做 删除 用 mm、i。
        否则：
            i 为 i 加 1。
        完。
    完。
完。

VMA_更新页表保护 接受 mm、起始、大小、新标志：
    vma 为 做 VMA_查找 用 mm、起始。
    若 vma 不等于 空：
        做 设置 用 vma、VMA_标志、新标志。
    完。
完。

VMA_复制 接受 源mm：
    若 源mm 等于 空：
        返还 一组：（空）。
    完。
    可变 新mm 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 源mm：
        vma 为 源mm 的 第 i 项。
        做 追加 用 新mm、做 VMA_创建 用 vma 的 第 VMA_起始 项、vma 的 第 VMA_结束 项、vma 的 第 VMA_标志 项、0。
        i 为 i 加 1。
    完。
    返还 新mm。
完。

VMA_权限名 接受 标志：
    可变 s 为 「」。
    若 标志 位与 VM_READ 不等于 0：
        s 为 s 加 「r」。
    否则：
        s 为 s 加 「-」。
    完。
    若 标志 位与 VM_WRITE 不等于 0：
        s 为 s 加 「w」。
    否则：
        s 为 s 加 「-」。
    完。
    若 标志 位与 VM_EXEC 不等于 0：
        s 为 s 加 「x」。
    否则：
        s 为 s 加 「-」。
    完。
    返还 s。
完。

VMA_完整统计 接受 mm：
    若 mm 等于 空：
        返还 一组：0、0。
    完。
    可变 总大小 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 mm：
        总大小 为 总大小 加 (mm 的 第 i 项 的 第 VMA_结束 项 减 mm 的 第 i 项 的 第 VMA_起始 项)。
        i 为 i 加 1。
    完。
    返还 一组：做 计数 用 mm、总大小 除以 4096。
完。

可变 AI_虚拟内存区_运行指标 为 0。

可变 AI_虚拟内存区_异常指标 为 0。

AI_虚拟内存区_自诊断 接受 零参：
    AI_虚拟内存区_运行指标 为 0。
    返还 AI_虚拟内存区_运行指标。
完。

AI_虚拟内存区_优化建议 接受 零参：
    指标 为 做 AI_虚拟内存区_自诊断。
    返还 「虚拟内存区状态正常」。
完。

本章 收束。
