《哈希函数》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·块内存分配器》。

引用《.·硬链接》。

引用《..·记忆·VFS文件读写》。

引用《..·引导·端口IO》。

引用《.·整数转字符串》。

PF_R 为 0x4。

PF_W 为 0x2。

PF_X 为 0x1。

错误_文件不存在 为 负 2。

错误_格式 为 负 8。

错误_符号未找到 为 负 9。

错误_版本不匹配 为 负 10。

符号缓存_桶数 为 256。

搜索路径_最大 为 32。

可变 链接器_符号查找次数 为 0。

可变 链接器_符号缓存命中 为 0。

可变 链接器_重定位次数 为 0。

可变 链接器_加载库数 为 0。

符号缓存项 是 一种 事物，含 名称哈希、符号名、地址、库名、版本、下一个。

可变 符号缓存表 为 0。

可变 库搜索路径 为 一组：（空）。

elf_hash 接受 名称：
    可变 h 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 名称：
        h 为 (h 左移 4) 加 做 字符编码 用 名称、i。
        g 为 h 位与 0xF0000000。
        若 g 不等于 0：
            h 为 h 位异或 g 右移 24。
        完。
        h 为 h 位与 按位取反 g。
        i 为 i 加 1。
    完。
    返还 h。
完。

gnu_hash 接受 名称：
    可变 h 为 5381。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 名称：
        h 为 (h 左移 5) 加 h 加 做 字符编码 用 名称、i。
        h 为 h 位与 0xFFFFFFFF。
        i 为 i 加 1。
    完。
    返还 h。
完。

sysv_hash 接受 名称：
    可变 h 为 0。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 名称：
        h 为 (h 左移 4) 加 做 字符编码 用 名称、i。
        g 为 h 位与 0xF0000000。
        h 为 (h 位异或 g 右移 24) 位与 0x0FFFFFFF。
        i 为 i 加 1。
    完。
    返还 h。
完。

符号缓存_初始化 接受 零参：
    符号缓存表 为 做 分配内存 用 符号缓存_桶数 乘 8。
    可变 i 为 0。
    只要 i 小于 符号缓存_桶数：
        做 内存写64 用 符号缓存表 加 i 乘 8、0。
        i 为 i 加 1。
    完。
完。

符号缓存_查找 接受 名称：
    若 符号缓存表 等于 0：
        返还 0。
    完。
    哈希 为 做 gnu_hash 用 名称。
    桶 为 哈希 取余于 符号缓存_桶数。
    可变 项 为 做 内存读64 用 符号缓存表 加 桶 乘 8。
    只要 项 不等于 0：
        若 项 的 名称哈希 等于 哈希 并且 项 的 符号名 等于 名称：
            链接器_符号缓存命中 为 链接器_符号缓存命中 加 1。
            返还 项 的 地址。
        完。
        项 为 项 的 下一个。
    完。
    返还 0。
完。

符号缓存_插入 接受 名称、地址、库名、版本：
    若 符号缓存表 等于 0：
        做 符号缓存_初始化。
    完。
    哈希 为 做 gnu_hash 用 名称。
    桶 为 哈希 取余于 符号缓存_桶数。
    项 为 做 堆_分配 用 符号缓存项。
    项 的 名称哈希 为 哈希。
    项 的 符号名 为 名称。
    项 的 地址 为 地址。
    项 的 库名 为 库名。
    项 的 版本 为 版本。
    项 的 下一个 为 做 内存读64 用 符号缓存表 加 桶 乘 8。
    做 内存写64 用 符号缓存表 加 桶 乘 8、项。
完。

符号缓存_清空 接受 零参：
    若 符号缓存表 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 符号缓存_桶数：
        可变 项 为 做 内存读64 用 符号缓存表 加 i 乘 8。
        只要 项 不等于 0：
            下一个 为 项 的 下一个。
            做 释放内存 用 项。
            项 为 下一个。
        完。
        做 内存写64 用 符号缓存表 加 i 乘 8、0。
        i 为 i 加 1。
    完。
完。

搜索路径_添加 接受 路径：
    若 做 计数 用 库搜索路径 大于或等于 搜索路径_最大：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 库搜索路径：
        若 库搜索路径 的 第 i 项 等于 路径：
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 库搜索路径、路径。
完。

搜索路径_初始化 接受 零参：
    做 搜索路径_添加 用 「/lib」。
    做 搜索路径_添加 用 「/usr/lib」。
    做 搜索路径_添加 用 「/usr/local/lib」。
完。

搜索库文件 接受 库名：
    若 做 字符串长度 用 库名 大于 0 并且 做 字符编码 用 库名、0 等于 0x2F：
        若 做 文件存在 用 库名：
            返还 库名。
        完。
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 库搜索路径：
        完整路径 为 库搜索路径 的 第 i 项 加 「/」 加 库名。
        若 做 文件存在 用 完整路径：
            返还 完整路径。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

转换保护标志 接受 p_flags：
    可变 prot 为 0。
    若 p_flags 位与 PF_R 不等于 0：
        prot 为 prot 位或 0x1。
    完。
    若 p_flags 位与 PF_W 不等于 0：
        prot 为 prot 位或 0x2。
    完。
    若 p_flags 位与 PF_X 不等于 0：
        prot 为 prot 位或 0x4。
    完。
    返还 prot。
完。

页对齐 接受 大小：
    返还 大小 加 4095 位与 按位取反 4095。
完。

页对齐下 接受 地址：
    返还 地址 位与 按位取反 4095。
完。

读取C字符串 接受 地址：
    若 地址 等于 0：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 4096：
        c 为 做 内存读8 用 地址 加 i。
        若 c 等于 0：
            返还 结果。
        完。
        结果 为 结果 加 做 字符 用 c。
        i 为 i 加 1。
    完。
    返还 结果。
完。

读取C字符串N 接受 地址、最大长度：
    若 地址 等于 0：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 最大长度：
        c 为 做 内存读8 用 地址 加 i。
        若 c 等于 0：
            返还 结果。
        完。
        结果 为 结果 加 做 字符 用 c。
        i 为 i 加 1。
    完。
    返还 结果。
完。

文件存在 接受 路径：
    fd 为 做 vfs_open 用 路径、0。
    若 fd 大于或等于 0：
        做 vfs_close 用 fd。
        返还 真。
    完。
    返还 假。
完。

分配内存 接受 大小：
    返还 做 kmalloc 用 大小。
完。

释放内存 接受 地址：
    做 kfree 用 地址。
完。

分配对齐内存 接受 大小、对齐：
    真实大小 为 大小 加 对齐 加 8。
    原始 为 做 kmalloc 用 真实大小。
    若 原始 等于 0：
        返还 0。
    完。
    对齐后 为 原始 加 8 加 对齐 减 1 位与 按位取反 (对齐 减 1)。
    做 内存写64 用 对齐后 减 8、原始。
    返还 对齐后。
完。

释放对齐内存 接受 地址：
    若 地址 等于 0：
        返还。
    完。
    原始 为 做 内存读64 用 地址 减 8。
    做 kfree 用 原始。
完。

调用函数 接受 地址：
    若 地址 等于 0：
        返还。
    完。
    做 内联汇编_间接调用 用 地址。
完。

调用初始化函数列表 接受 init_array、数量：
    若 init_array 等于 0 或者 数量 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 数量：
        函数地址 为 做 内存读64 用 init_array 加 i 乘 8。
        若 函数地址 不等于 0 并且 函数地址 不等于 0xFFFFFFFFFFFFFFFF：
            做 调用函数 用 函数地址。
        完。
        i 为 i 加 1。
    完。
完。

调用终止函数列表 接受 fini_array、数量：
    若 fini_array 等于 0 或者 数量 等于 0：
        返还。
    完。
    可变 i 为 数量 减 1。
    只要 i 大于或等于 0：
        函数地址 为 做 内存读64 用 fini_array 加 i 乘 8。
        若 函数地址 不等于 0 并且 函数地址 不等于 0xFFFFFFFFFFFFFFFF：
            做 调用函数 用 函数地址。
        完。
        i 为 i 减 1。
    完。
完。

验证ELF魔数 接受 地址：
    若 做 内存读8 用 地址 不等于 0x7F：
        返还 假。
    完。
    若 做 内存读8 用 地址 加 1 不等于 0x45：
        返还 假。
    完。
    若 做 内存读8 用 地址 加 2 不等于 0x4C：
        返还 假。
    完。
    若 做 内存读8 用 地址 加 3 不等于 0x46：
        返还 假。
    完。
    返还 真。
完。

读取ELF类 接受 地址：
    返还 做 内存读8 用 地址 加 4。
完。

读取ELF数据编码 接受 地址：
    返还 做 内存读8 用 地址 加 5。
完。

动态链接_统计 接受 零参：
    返还 一本字典：已加载库数 对应 链接器_加载库数、符号查找次数 对应 链接器_符号查找次数、缓存命中次数 对应 链接器_符号缓存命中、重定位次数 对应 链接器_重定位次数、搜索路径数 对应 做 计数 用 库搜索路径、缓存命中率 对应 若 链接器_符号查找次数 大于 0 则 链接器_符号缓存命中 乘 100 除以 链接器_符号查找次数 否则 0。
完。

链接器_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 链接器_加载库数 大于 512：
        做 追加 用 问题、「加载库过多: 」 加 做 整数转字符串 用 链接器_加载库数。
    完。
    若 链接器_符号查找次数 大于 0：
        命中率 为 链接器_符号缓存命中 乘 100 除以 链接器_符号查找次数。
        若 命中率 小于 30：
            做 追加 用 问题、「符号缓存命中率过低: 」 加 做 整数转字符串 用 命中率 加 「%」。
        完。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「动态链接器健康」。
    完。
    返还 问题。
完。

链接器_重置统计 接受 零参：
    链接器_符号查找次数 为 0。
    链接器_符号缓存命中 为 0。
    链接器_重定位次数 为 0。
完。

链接器_缓存命中率 接受 零参：
    若 链接器_符号查找次数 等于 0：
        返还 0。
    完。
    返还 链接器_符号缓存命中 乘 100 除以 链接器_符号查找次数。
完。

可变 AI_哈希函数_缓存效率 为 0。

可变 AI_哈希函数_查找热度 为 0。

AI_哈希函数_缓存效率评估 接受 零参：
    若 链接器_符号查找次数 等于 0：
        返还 0。
    完。
    AI_哈希函数_缓存效率 为 链接器_符号缓存命中 乘 1000 除以 链接器_符号查找次数。
    返还 AI_哈希函数_缓存效率。
完。

AI_哈希函数_链接优化建议 接受 零参：
    效率 为 做 AI_哈希函数_缓存效率评估。
    若 效率 小于 300：
        返还 「缓存命中率过低，建议扩大符号缓存桶数」。
    完。
    若 效率 小于 600：
        返还 「缓存命中率偏低，建议预热常用符号」。
    完。
    若 链接器_加载库数 大于 256：
        返还 「加载库过多，建议延迟加载」。
    完。
    返还 「链接性能正常」。
完。

AI_哈希函数_符号热度追踪 接受 零参：
    AI_哈希函数_查找热度 为 链接器_符号查找次数 减 链接器_符号缓存命中。
    返还 AI_哈希函数_查找热度。
完。

本章 收束。
