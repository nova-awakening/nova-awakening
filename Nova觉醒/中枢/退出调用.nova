《退出调用》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·进程标识分》。

引用《.·信号获取默》。

引用《.·字符串替换》。

引用《.·信号动作设置》。

引用《.·进程描述符》。

引用《.·进程信号》。

引用《.·自动扩容》。

引用《..·执行·平台时间》。

引用《.·负载统计》。

引用《.·内存初始化》。

引用《.·时钟中枢》。

引用《..·标准·进程接口》。

引用《.·进程退出》。

引用《.·每处理器》。

引用《.·命名空间》。

引用《..·记忆·proc_meminfo》。

sys_exit 接受 退出码、_b、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    做 do_exit 用 当前、退出码 位与 0xFF。
    返还 0。
完。

sys_exit_group 接受 退出码、_b、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    线程组 为 做 进程_线程组 用 当前。
    若 线程组 不等于 空：
        可变 i 为 0。
        只要 i 小于 做 计数 用 线程组：
            线程 为 线程组 的 第 i 项。
            若 线程 不等于 当前：
                做 信号_发送 用 线程、SIGKILL。
            完。
            i 为 i 加 1。
        完。
    完。
    做 do_exit 用 当前、退出码 位与 0xFF。
    返还 0。
完。

sys_wait4 接受 pid、状态指针、选项、rusage、_e、_f：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_无此进程。
    完。
    子进程列表 为 做 进程_子列表 用 当前。
    若 子进程列表 等于 空 或者 做 计数 用 子进程列表 等于 0：
        返还 错误_无子进程。
    完。
    只要 真：
        可变 i 为 0。
        只要 i 小于 做 计数 用 子进程列表：
            子 为 子进程列表 的 第 i 项。
            子pid 为 做 进程_PID 用 子。
            可变 匹配 为 假。
            若 pid 等于 负 1：
                匹配 为 真。
            否则若 pid 大于 0 并且 子pid 等于 pid：
                匹配 为 真。
            否则若 pid 等于 0 并且 做 进程_组ID 用 子 等于 做 进程_组ID 用 当前：
                匹配 为 真。
            否则若 pid 小于 负 1 并且 做 进程_组ID 用 子 等于 负 pid：
                匹配 为 真。
            完。
            若 匹配 并且 做 进程_状态 用 子 等于 进程状态_僵尸：
                退出码 为 做 进程_退出码 用 子。
                若 状态指针 不等于 空：
                    做 设置 用 状态指针、0、退出码 左移 8。
                完。
                做 进程_从子列表移除 用 当前、子。
                做 进程_释放 用 子。
                返还 子pid。
            完。
            i 为 i 加 1。
        完。
        若 选项 位与 WNOHANG 不等于 0：
            返还 0。
        完。
        做 进程_设置状态 用 当前、进程状态_等待。
        做 调度。
    完。
完。

sys_kill 接受 pid、sig、_c、_d、_e、_f：
    若 sig 小于 0 或者 sig 大于或等于 最大信号：
        返还 错误_无效参数。
    完。
    若 pid 大于 0：
        目标 为 做 进程表_查找 用 pid。
        若 目标 等于 空：
            返还 错误_无此进程。
        完。
        返还 做 信号_发送 用 目标、sig。
    完。
    若 pid 等于 0：
        返还 做 信号_发送给组 用 做 进程_组ID 用 做 获取当前进程、sig。
    完。
    若 pid 等于 负 1：
        返还 做 信号_发送给全部 用 sig。
    完。
    返还 做 信号_发送给组 用 负 pid、sig。
完。

sys_rt_sigaction 接受 sig、act、oact、sigsetsize、_e、_f：
    若 sig 小于 1 或者 sig 大于或等于 最大信号：
        返还 错误_无效参数。
    完。
    若 sig 等于 SIGKILL 或者 sig 等于 SIGSTOP：
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    若 oact 不等于 空：
        旧处理器 为 做 进程_获取信号处理器 用 当前、sig。
        做 设置 用 oact、0、旧处理器。
    完。
    若 act 不等于 空：
        做 进程_设置信号处理器 用 当前、sig、act 的 第 0 项。
    完。
    返还 成功。
完。

sys_rt_sigprocmask 接受 how、set、oset、sigsetsize、_e、_f：
    当前 为 做 获取当前进程。
    当前掩码 为 做 进程_信号掩码 用 当前。
    若 oset 不等于 空：
        做 设置 用 oset、0、当前掩码。
    完。
    若 set 不等于 空：
        新集 为 set 的 第 0 项。
        新集 为 新集 位与 按位取反 (1 左移 SIGKILL) 位与 按位取反 (1 左移 SIGSTOP)。
        若 how 等于 0：
            做 进程_设置信号掩码 用 当前、当前掩码 位或 新集。
        否则若 how 等于 1：
            做 进程_设置信号掩码 用 当前、当前掩码 位与 按位取反 新集。
        否则若 how 等于 2：
            做 进程_设置信号掩码 用 当前、新集。
        否则：
            返还 错误_无效参数。
        完。
    完。
    返还 成功。
完。

sys_setpgid 接受 pid、pgid、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    可变 目标 为 当前。
    若 pid 不等于 0 并且 pid 不等于 做 进程_PID 用 当前：
        目标 为 做 进程表_查找 用 pid。
        若 目标 等于 空：
            返还 错误_无此进程。
        完。
        若 做 进程_父 用 目标 不等于 当前：
            返还 错误_权限。
        完。
    完。
    若 pgid 等于 0：
        pgid 为 做 进程_PID 用 目标。
    完。
    做 进程_设置组ID 用 目标、pgid。
    返还 成功。
完。

sys_getpgid 接受 pid、_b、_c、_d、_e、_f：
    若 pid 等于 0：
        返还 做 进程_组ID 用 做 获取当前进程。
    完。
    目标 为 做 进程表_查找 用 pid。
    若 目标 等于 空：
        返还 错误_无此进程。
    完。
    返还 做 进程_组ID 用 目标。
完。

sys_setsid 接受 _a、_b、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    pid 为 做 进程_PID 用 当前。
    若 做 进程_组ID 用 当前 等于 pid：
        返还 错误_权限。
    完。
    做 进程_设置组ID 用 当前、pid。
    做 进程_设置会话ID 用 当前、pid。
    返还 pid。
完。

sys_uname 接受 buf、_b、_c、_d、_e、_f：
    若 buf 等于 空：
        返还 错误_无效参数。
    完。
    uts 为 做 获取uname。
    若 uts 不等于 空：
        做 设置 用 buf、0、uts 的 第 0 项。
        做 设置 用 buf、1、uts 的 第 1 项。
        做 设置 用 buf、2、uts 的 第 2 项。
        做 设置 用 buf、3、uts 的 第 3 项。
        做 设置 用 buf、4、uts 的 第 4 项。
        做 设置 用 buf、5、uts 的 第 5 项。
    否则：
        做 设置 用 buf、0、「NovaOS」。
        做 设置 用 buf、1、「nova」。
        做 设置 用 buf、2、「1.0.0-M1」。
        做 设置 用 buf、3、「#1 SMP NovaOS M1 Prototype」。
        做 设置 用 buf、4、「x86_64」。
        做 设置 用 buf、5、「(none)」。
    完。
    返还 成功。
完。

sys_sysinfo 接受 buf、_b、_c、_d、_e、_f：
    若 buf 等于 空：
        返还 错误_无效参数。
    完。
    做 设置 用 buf、0、做 获取uptime秒。
    做 设置 用 buf、1、做 获取负载1分。
    做 设置 用 buf、2、做 获取负载5分。
    做 设置 用 buf、3、做 获取负载15分。
    做 设置 用 buf、4、做 获取总内存。
    做 设置 用 buf、5、做 获取空闲内存。
    做 设置 用 buf、6、做 共享页数 乘 4096。
    做 设置 用 buf、7、做 缓冲区_页数 乘 4096。
    做 设置 用 buf、8、做 获取交换总量KB 乘 1024。
    做 设置 用 buf、9、做 获取交换空闲KB 乘 1024。
    做 设置 用 buf、10、做 获取进程总数。
    返还 0。
完。

可变 退出_已初始化 为 假。

可变 exit_次数 为 0。

可变 exit_group_次数 为 0。

可变 wait_次数 为 0。

可变 waitid_次数 为 0。

可变 fork_次数 为 0。

可变 clone_次数 为 0。

可变 execve_次数 为 0。

可变 setsid_次数 为 0。

可变 uname_次数 为 0。

可变 sysinfo_次数 为 0。

可变 退出_错误数 为 0。

可变 退出_延迟总计 为 0。

可变 退出_延迟样本 为 0。

可变 退出_最大延迟 为 0。

可变 退出_速率 为 0。

可变 退出_速率窗口 为 0。

可变 退出_速率时刻 为 0。

可变 退出_峰值速率 为 0。

WNOHANG 为 1。

WUNTRACED 为 2。

WCONTINUED 为 8。

sys_wait4_增强 接受 pid、status、options、rusage、_e、_f：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        退出_错误数 为 退出_错误数 加 1。
        返还 错误_内存不足。
    完。
    若 options 位与 WNOHANG 不等于 0：
        子进程 为 做 查找已退出子进程 用 当前、pid。
        若 子进程 等于 空：
            返还 0。
        完。
        若 status 不等于 空：
            做 设置 用 status、0、做 子进程_退出码 用 子进程。
        完。
        wait_次数 为 wait_次数 加 1。
        退出_速率窗口 为 退出_速率窗口 加 1。
        返还 做 子进程_PID 用 子进程。
    完。
    可变 子进程 为 做 查找已退出子进程 用 当前、pid。
    只要 子进程 等于 空：
        做 进程_休眠_等待子进程 用 当前。
        子进程 为 做 查找已退出子进程 用 当前、pid。
    完。
    若 status 不等于 空：
        做 设置 用 status、0、做 子进程_退出码 用 子进程。
    完。
    wait_次数 为 wait_次数 加 1。
    退出_速率窗口 为 退出_速率窗口 加 1。
    返还 做 子进程_PID 用 子进程。
完。

sys_waitid 接受 idtype、id、infop、options、rusage、_f：
    若 infop 等于 空：
        退出_错误数 为 退出_错误数 加 1。
        返还 错误_无效参数。
    完。
    waitid_次数 为 waitid_次数 加 1。
    退出_速率窗口 为 退出_速率窗口 加 1。
    返还 0。
完。

CLONE_VM 为 0x00000100。

CLONE_FS 为 0x00000200。

CLONE_FILES 为 0x00000400。

CLONE_SIGHAND 为 0x00000800。

CLONE_THREAD 为 0x00010000。

sys_clone_增强 接受 flags、stack、ptid、ctid、tls、_f：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        退出_错误数 为 退出_错误数 加 1。
        返还 错误_内存不足。
    完。
    若 flags 位与 CLONE_THREAD 不等于 0：
    否则：
    完。
    clone_次数 为 clone_次数 加 1。
    退出_速率窗口 为 退出_速率窗口 加 1。
    返还 0。
完。

退出_记录延迟 接受 延迟：
    退出_延迟样本 为 退出_延迟样本 加 1。
    退出_延迟总计 为 退出_延迟总计 加 延迟。
    若 延迟 大于 退出_最大延迟：
        退出_最大延迟 为 延迟。
    完。
完。

退出_平均延迟 接受 零参：
    若 退出_延迟样本 等于 0：
        返还 0。
    完。
    返还 退出_延迟总计 除以 退出_延迟样本。
完。

退出_更新速率 接受 零参：
    现在 为 做 读取纳秒_退出。
    间隔 为 现在 减 退出_速率时刻。
    若 间隔 大于或等于 1000000000：
        退出_速率 为 退出_速率窗口 乘 1000000000 除以 间隔。
        若 退出_速率 大于 退出_峰值速率：
            退出_峰值速率 为 退出_速率。
        完。
        退出_速率窗口 为 0。
        退出_速率时刻 为 现在。
    完。
完。

注册退出系统调用 接受 零参：
    返还 0。
    返还 0。
完。

退出调用_初始化 接受 零参：
    若 退出_已初始化 等于 真：
        返还 错误_已存在。
    完。
    exit_次数 为 0。
    exit_group_次数 为 0。
    wait_次数 为 0。
    waitid_次数 为 0。
    fork_次数 为 0。
    clone_次数 为 0。
    execve_次数 为 0。
    setsid_次数 为 0。
    uname_次数 为 0。
    sysinfo_次数 为 0。
    退出_错误数 为 0。
    退出_已初始化 为 真。
    做 打印 用 「[退出调用] 初始化完成」。
    返还 0。
完。

退出调用_完整统计 接受 零参：
    总操作 为 exit_次数 加 exit_group_次数 加 wait_次数 加 waitid_次数 加 fork_次数 加 clone_次数 加 execve_次数 加 setsid_次数 加 uname_次数 加 sysinfo_次数。
    统计 为 做 字典。
    做 字典设 用 统计、「已初始化」、退出_已初始化。
    做 字典设 用 统计、「exit」、exit_次数。
    做 字典设 用 统计、「exit_group」、exit_group_次数。
    做 字典设 用 统计、「wait」、wait_次数。
    做 字典设 用 统计、「waitid」、waitid_次数。
    做 字典设 用 统计、「fork」、fork_次数。
    做 字典设 用 统计、「clone」、clone_次数。
    做 字典设 用 统计、「execve」、execve_次数。
    做 字典设 用 统计、「setsid」、setsid_次数。
    做 字典设 用 统计、「uname」、uname_次数。
    做 字典设 用 统计、「sysinfo」、sysinfo_次数。
    做 字典设 用 统计、「总操作」、总操作。
    做 字典设 用 统计、「平均延迟(ns)」、做 退出_平均延迟。
    做 字典设 用 统计、「最大延迟(ns)」、退出_最大延迟。
    做 字典设 用 统计、「速率」、退出_速率。
    做 字典设 用 统计、「峰值速率」、退出_峰值速率。
    做 字典设 用 统计、「错误数」、退出_错误数。
    返还 统计。
完。

退出调用_打印统计 接受 零参：
    统计 为 做 退出调用_完整统计。
    做 打印 用 「[退出调用] ═══════ 完整统计 ═══════」。
    做 打印 用 「  exit:         」 加 做 转字符串 用 做 字典取 用 统计、「exit」。
    做 打印 用 「  exit_group:   」 加 做 转字符串 用 做 字典取 用 统计、「exit_group」。
    做 打印 用 「  wait:         」 加 做 转字符串 用 做 字典取 用 统计、「wait」。
    做 打印 用 「  waitid:       」 加 做 转字符串 用 做 字典取 用 统计、「waitid」。
    做 打印 用 「  fork:         」 加 做 转字符串 用 做 字典取 用 统计、「fork」。
    做 打印 用 「  clone:        」 加 做 转字符串 用 做 字典取 用 统计、「clone」。
    做 打印 用 「  execve:       」 加 做 转字符串 用 做 字典取 用 统计、「execve」。
    做 打印 用 「  setsid:       」 加 做 转字符串 用 做 字典取 用 统计、「setsid」。
    做 打印 用 「  uname:        」 加 做 转字符串 用 做 字典取 用 统计、「uname」。
    做 打印 用 「  sysinfo:      」 加 做 转字符串 用 做 字典取 用 统计、「sysinfo」。
    做 打印 用 「  总操作:       」 加 做 转字符串 用 做 字典取 用 统计、「总操作」。
    做 打印 用 「  平均延迟:     」 加 做 转字符串 用 做 字典取 用 统计、「平均延迟(ns)」 加 「ns」。
    做 打印 用 「  速率:         」 加 做 转字符串 用 做 字典取 用 统计、「速率」 加 「 次/秒」。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[退出调用] ═════════════════════════」。
    返还 0。
完。

退出调用_dump 接受 零参：
    做 打印 用 「[退出调用 dump] ──────────────────」。
    做 打印 用 「  exit: 」 加 做 转字符串 用 exit_次数。
    做 打印 用 「  wait: 」 加 做 转字符串 用 wait_次数。
    做 打印 用 「  fork: 」 加 做 转字符串 用 fork_次数。
    做 打印 用 「  clone: 」 加 做 转字符串 用 clone_次数。
    做 打印 用 「  execve: 」 加 做 转字符串 用 execve_次数。
    做 打印 用 「  uname: 」 加 做 转字符串 用 uname_次数。
    做 打印 用 「  sysinfo: 」 加 做 转字符串 用 sysinfo_次数。
    做 打印 用 「  速率: 」 加 做 转字符串 用 退出_速率。
    做 打印 用 「  错误: 」 加 做 转字符串 用 退出_错误数。
    做 打印 用 「[退出调用 dump] ──────────────────」。
完。

退出调用_销毁 接受 零参：
    退出_已初始化 为 假。
    返还 0。
完。

退出调用_为procfs提供统计 接受 零参：
    返还 一组：exit_次数、exit_group_次数、wait_次数、fork_次数、clone_次数、execve_次数、setsid_次数、uname_次数、sysinfo_次数。
完。

退出调用_为AI提供进程模式 接受 零参：
    返还 一组：fork_次数、clone_次数、execve_次数、exit_次数、wait_次数、做 退出_平均延迟。
完。

退出调用_重置统计 接受 零参：
    exit_次数 为 0。
    exit_group_次数 为 0。
    wait_次数 为 0。
    waitid_次数 为 0。
    fork_次数 为 0。
    clone_次数 为 0。
    execve_次数 为 0。
    setsid_次数 为 0。
    uname_次数 为 0。
    sysinfo_次数 为 0。
    退出_错误数 为 0。
    退出_延迟总计 为 0。
    退出_延迟样本 为 0。
    退出_最大延迟 为 0。
    退出_速率 为 0。
    退出_速率窗口 为 0。
    退出_峰值速率 为 0。
    做 打印 用 「[退出调用] 统计已重置」。
    返还 0。
完。

进程_子列表 接受 进程：
    若 进程 等于 空：
        返还 一组：（空）。
    完。
    返还 进程 的 第 T_子进程列表 项。
完。

查找已退出子进程 接受 进程、pid：
    若 进程 等于 空：
        返还 空。
    完。
    子列表 为 做 进程_子列表 用 进程。
    若 子列表 等于 空 或者 做 计数 用 子列表 等于 0：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子列表：
        子 为 子列表 的 第 i 项。
        子pid 为 做 进程_PID 用 子。
        可变 匹配 为 假。
        若 pid 等于 负 1：
            匹配 为 真。
        否则若 pid 大于 0 并且 子pid 等于 pid：
            匹配 为 真。
        否则若 pid 等于 0 并且 做 进程_组ID 用 子 等于 做 进程_组ID 用 进程：
            匹配 为 真。
        否则若 pid 小于 负 1 并且 做 进程_组ID 用 子 等于 负 pid：
            匹配 为 真。
        完。
        若 匹配 并且 做 进程_状态 用 子 等于 进程_僵尸：
            返还 子。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

子进程_退出码 接受 子进程：
    若 子进程 等于 空：
        返还 0。
    完。
    返还 子进程 的 第 T_退出码 项。
完。

子进程_PID 接受 子进程：
    若 子进程 等于 空：
        返还 0。
    完。
    返还 做 进程_PID 用 子进程。
完。

进程_休眠_等待子进程 接受 进程：
    若 进程 等于 空：
        返还。
    完。
    做 进程_设置状态 用 进程、进程_睡眠。
    做 进程_设等待通道 用 进程、「wait」。
    做 调度。
完。

获取uptime秒 接受 零参：
    返还 做 平台_运行时间秒。
完。

获取负载1分 接受 零参：
    返还 做 负载_获取均值 的 第 0 项。
完。

获取负载5分 接受 零参：
    返还 做 负载_获取均值 的 第 1 项。
完。

获取负载15分 接受 零参：
    返还 做 负载_获取均值 的 第 2 项。
完。

获取总内存 接受 零参：
    返还 做 获取总内存KB 乘 1024。
完。

获取空闲内存 接受 零参：
    返还 做 获取空闲内存KB 乘 1024。
完。

获取进程总数 接受 零参：
    返还 做 进程_统计 的 第 2 项。
完。

读取纳秒_退出 接受 零参：
    可变 值 为 0。
    汇编。
    块：
        「rdtsc」。
        「shl rdx, 32」。
        「or rax, rdx」。
        「mov [rbp-8], rax」。
    完。
    返还 值。
完。

退出调用_为调度器提供进程负载 接受 零参：
    返还 一组：退出_速率、退出_峰值速率、fork_次数、clone_次数、exit_次数。
完。

退出调用_为电源管理挂起 接受 零参：
    返还 0。
完。

退出调用_获取fork_exec比 接受 零参：
    若 execve_次数 等于 0：
        返还 0。
    完。
    返还 fork_次数 乘 100 除以 execve_次数。
完。

本章 收束。
