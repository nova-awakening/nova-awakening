《中断表写条目》

引用《..·执行·端口》。

引用《..·引导·寄存器定义》。

引用《..·引导·引导加载器》。

引用《.·自动扩容》。

引用《.·字符串替换》。

引用《..·外联·IP解析与随机》。

引用《.·中断表初始化》。

IDT_条目数 为 256。

IDT_条目大小 为 16。

IDT_总大小 为 4096。

可变 IDT缓冲 为 空。

IDT_中断门 为 0x8E。

IDT_陷阱门 为 0x8F。

IDT_用户中断门 为 0xEE。

IDT_用户陷阱门 为 0xEF。

IDT_无效门 为 0x00。

向量_系统调用 为 0x80。

向量_接口C定时器 为 0xEF。

向量_接口C错误 为 0xFD。

向量_接口C伪中断 为 0xFF。

向量_调度IPI 为 0xF9。

向量_TLB_IPI 为 0xFA。

向量_函数调用IPI 为 0xFB。

向量_重调度IPI 为 0xFC。

向量_外部IRQ起始 为 0x30。

向量_外部IRQ结束 为 0xEE。

向量_动态起始 为 0x81。

向量_动态结束 为 0xEE。

IST_无 为 0。

IST_双重故障 为 1。

IST_NMI 为 2。

IST_调试 为 3。

IST_机器检查 为 4。

可变 向量分配位图 为 做 数组 用 u64、4。

可变 向量分配锁 为 新建 自旋锁。

IDTR 是 一种 事物，含 界限、基址。

IDT条目信息 是 一种 事物，含 向量号、偏移、选择子、类型、IST、已设置。

IDT_写条目 接受 向量号、处理函数地址、选择子、类型、IST编号：
    若 向量号 大于或等于 IDT_条目数：
        返还。
    完。
    偏移 为 向量号 乘 IDT_条目大小。
    做 缓冲写16 用 IDT缓冲、偏移、处理函数地址 位与 0xFFFF。
    做 缓冲写16 用 IDT缓冲、偏移 加 2、选择子。
    做 缓冲写 用 IDT缓冲、偏移 加 4、IST编号 位与 0x7。
    做 缓冲写 用 IDT缓冲、偏移 加 5、类型。
    做 缓冲写16 用 IDT缓冲、偏移 加 6、处理函数地址 右移 16 位与 0xFFFF。
    做 缓冲写32 用 IDT缓冲、偏移 加 8、处理函数地址 右移 32 位与 0xFFFFFFFF。
    做 缓冲写32 用 IDT缓冲、偏移 加 12、0。
完。

IDT_读条目 接受 向量号：
    若 向量号 大于或等于 IDT_条目数：
        返还 空。
    完。
    偏移 为 向量号 乘 IDT_条目大小。
    可变 信息 为 新建 IDT条目信息。
    信息 的 向量号 为 向量号。
    低16 为 做 缓冲读16 用 IDT缓冲、偏移。
    中16 为 做 缓冲读16 用 IDT缓冲、偏移 加 6。
    高32 为 做 缓冲读32 用 IDT缓冲、偏移 加 8。
    信息 的 偏移 为 低16 位或 中16 左移 16 位或 高32 左移 32。
    信息 的 选择子 为 做 缓冲读16 用 IDT缓冲、偏移 加 2。
    信息 的 IST 为 做 缓冲读 用 IDT缓冲、偏移 加 4 位与 0x7。
    信息 的 类型 为 做 缓冲读 用 IDT缓冲、偏移 加 5。
    信息 的 已设置 为 信息 的 类型 位与 0x80 不等于 0。
    返还 信息。
完。

IDT_清除条目 接受 向量号：
    若 向量号 大于或等于 IDT_条目数：
        返还。
    完。
    偏移 为 向量号 乘 IDT_条目大小。
    可变 i 为 0。
    只要 i 小于 IDT_条目大小：
        做 缓冲写 用 IDT缓冲、偏移 加 i、0。
        i 为 i 加 1。
    完。
    写条目_清除次数 为 写条目_清除次数 加 1。
完。

可变 写条目_写入次数 为 0。

可变 写条目_读取次数 为 0。

可变 写条目_清除次数 为 0。

可变 写条目_修改次数 为 0。

可变 写条目_批量次数 为 0。

可变 写条目_验证次数 为 0。

可变 写条目_验证失败 为 0。

可变 写条目_保存次数 为 0。

可变 写条目_恢复次数 为 0。

可变 写条目_错误数 为 0。

IDT_修改IST 接受 向量号、新IST：
    若 向量号 大于或等于 IDT_条目数：
        返还 错误_无效参数。
    完。
    若 新IST 大于 7：
        返还 错误_无效参数。
    完。
    偏移 为 向量号 乘 IDT_条目大小。
    做 缓冲写 用 IDT缓冲、偏移 加 4、新IST 位与 0x7。
    写条目_修改次数 为 写条目_修改次数 加 1。
    返还 0。
完。

IDT_修改类型 接受 向量号、新类型：
    若 向量号 大于或等于 IDT_条目数：
        返还 错误_无效参数。
    完。
    偏移 为 向量号 乘 IDT_条目大小。
    做 缓冲写 用 IDT缓冲、偏移 加 5、新类型。
    写条目_修改次数 为 写条目_修改次数 加 1。
    返还 0。
完。

IDT_修改处理函数 接受 向量号、新地址：
    若 向量号 大于或等于 IDT_条目数：
        返还 错误_无效参数。
    完。
    若 新地址 等于 0：
        返还 错误_无效参数。
    完。
    偏移 为 向量号 乘 IDT_条目大小。
    做 缓冲写16 用 IDT缓冲、偏移、新地址 位与 0xFFFF。
    做 缓冲写16 用 IDT缓冲、偏移 加 6、新地址 右移 16 位与 0xFFFF。
    做 缓冲写32 用 IDT缓冲、偏移 加 8、新地址 右移 32 位与 0xFFFFFFFF。
    写条目_修改次数 为 写条目_修改次数 加 1。
    返还 0。
完。

IDT_设置屏蔽 接受 向量号：
    若 向量号 大于或等于 IDT_条目数：
        返还 错误_无效参数。
    完。
    偏移 为 向量号 乘 IDT_条目大小。
    旧类型 为 做 缓冲读 用 IDT缓冲、偏移 加 5。
    做 缓冲写 用 IDT缓冲、偏移 加 5、旧类型 位与 0x7F。
    写条目_修改次数 为 写条目_修改次数 加 1。
    返还 0。
完。

IDT_取消屏蔽 接受 向量号：
    若 向量号 大于或等于 IDT_条目数：
        返还 错误_无效参数。
    完。
    偏移 为 向量号 乘 IDT_条目大小。
    旧类型 为 做 缓冲读 用 IDT缓冲、偏移 加 5。
    做 缓冲写 用 IDT缓冲、偏移 加 5、旧类型 位或 0x80。
    写条目_修改次数 为 写条目_修改次数 加 1。
    返还 0。
完。

IDT_批量写 接受 条目列表：
    若 条目列表 等于 空：
        返还 错误_无效参数。
    完。
    可变 成功 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目列表：
        项 为 条目列表 的 第 i 项。
        若 做 计数 用 项 大于或等于 5：
            做 IDT_写条目 用 项 的 第 0 项、项 的 第 1 项、项 的 第 2 项、项 的 第 3 项、项 的 第 4 项。
            成功 为 成功 加 1。
        完。
        i 为 i 加 1。
    完。
    写条目_批量次数 为 写条目_批量次数 加 1。
    返还 成功。
完。

IDT_批量清除 接受 起始向量、结束向量：
    若 起始向量 大于或等于 IDT_条目数：
        返还 错误_无效参数。
    完。
    若 结束向量 大于或等于 IDT_条目数：
        结束向量 为 IDT_条目数 减 1。
    完。
    可变 i 为 起始向量。
    只要 i 小于或等于 结束向量：
        做 IDT_清除条目 用 i。
        i 为 i 加 1。
    完。
    返还 0。
完。

IDT_门类型名 接受 类型：
    若 类型 等于 IDT_中断门：
        返还 「中断门(DPL=0)」。
    完。
    若 类型 等于 IDT_陷阱门：
        返还 「陷阱门(DPL=0)」。
    完。
    若 类型 等于 IDT_用户中断门：
        返还 「中断门(DPL=3)」。
    完。
    若 类型 等于 IDT_用户陷阱门：
        返还 「陷阱门(DPL=3)」。
    完。
    若 类型 等于 IDT_无效门：
        返还 「无效(P=0)」。
    完。
    若 类型 位与 0x80 等于 0：
        返还 「未使用」。
    完。
    返还 「未知(0x」 加 做 转字符串 用 类型 加 「)」。
完。

IDT_IST名 接受 IST编号：
    若 IST编号 等于 IST_无：
        返还 「无」。
    完。
    若 IST编号 等于 IST_双重故障：
        返还 「双重故障」。
    完。
    若 IST编号 等于 IST_NMI：
        返还 「NMI」。
    完。
    若 IST编号 等于 IST_调试：
        返还 「调试」。
    完。
    若 IST编号 等于 IST_机器检查：
        返还 「机器检查」。
    完。
    返还 「IST」 加 做 转字符串 用 IST编号。
完。

IDT_异常名 接受 向量号：
    若 向量号 等于 0：
        返还 「#DE 除零」。
    完。
    若 向量号 等于 1：
        返还 「#DB 调试」。
    完。
    若 向量号 等于 2：
        返还 「NMI 不可屏蔽」。
    完。
    若 向量号 等于 3：
        返还 「#BP 断点」。
    完。
    若 向量号 等于 4：
        返还 「#OF 溢出」。
    完。
    若 向量号 等于 5：
        返还 「#BR 越界」。
    完。
    若 向量号 等于 6：
        返还 「#UD 无效操作码」。
    完。
    若 向量号 等于 7：
        返还 「#NM 无FPU」。
    完。
    若 向量号 等于 8：
        返还 「#DF 双重故障」。
    完。
    若 向量号 等于 9：
        返还 「协处理器段溢出」。
    完。
    若 向量号 等于 10：
        返还 「#TS 无效TSS」。
    完。
    若 向量号 等于 11：
        返还 「#NP 段不在」。
    完。
    若 向量号 等于 12：
        返还 「#SS 栈段错」。
    完。
    若 向量号 等于 13：
        返还 「#GP 一般保护」。
    完。
    若 向量号 等于 14：
        返还 「#PF 缺页」。
    完。
    若 向量号 等于 16：
        返还 「#MF 浮点异常」。
    完。
    若 向量号 等于 17：
        返还 「#AC 对齐检查」。
    完。
    若 向量号 等于 18：
        返还 「#MC 机器检查」。
    完。
    若 向量号 等于 19：
        返还 「#XM SIMD浮点」。
    完。
    若 向量号 等于 20：
        返还 「#VE 虚拟化」。
    完。
    若 向量号 等于 21：
        返还 「#CP 控制保护」。
    完。
    若 向量号 等于 向量_系统调用：
        返还 「系统调用」。
    完。
    若 向量号 等于 向量_接口C定时器：
        返还 「APIC定时器」。
    完。
    若 向量号 等于 向量_接口C错误：
        返还 「APIC错误」。
    完。
    若 向量号 等于 向量_接口C伪中断：
        返还 「APIC伪中断」。
    完。
    若 向量号 大于或等于 向量_外部IRQ起始 并且 向量号 小于或等于 向量_外部IRQ结束：
        返还 「外部IRQ」 加 做 转字符串 用 向量号 减 向量_外部IRQ起始。
    完。
    返还 「向量」 加 做 转字符串 用 向量号。
完。

IDT_异常有错误码 接受 向量号：
    若 向量号 等于 8：
        返还 真。
    完。
    若 向量号 等于 10：
        返还 真。
    完。
    若 向量号 等于 11：
        返还 真。
    完。
    若 向量号 等于 12：
        返还 真。
    完。
    若 向量号 等于 13：
        返还 真。
    完。
    若 向量号 等于 14：
        返还 真。
    完。
    若 向量号 等于 17：
        返还 真。
    完。
    若 向量号 等于 21：
        返还 真。
    完。
    返还 假。
完。

IDT_验证条目 接受 向量号：
    若 向量号 大于或等于 IDT_条目数：
        返还 假。
    完。
    写条目_验证次数 为 写条目_验证次数 加 1。
    信息 为 做 IDT_读条目 用 向量号。
    若 信息 等于 空：
        返还 假。
    完。
    若 信息 的 已设置 等于 假：
        若 向量号 小于 32：
            写条目_验证失败 为 写条目_验证失败 加 1。
            返还 假。
        完。
        返还 真。
    完。
    若 信息 的 选择子 等于 0：
        写条目_验证失败 为 写条目_验证失败 加 1。
        返还 假。
    完。
    若 信息 的 偏移 等于 0 并且 信息 的 已设置 等于 真：
        写条目_验证失败 为 写条目_验证失败 加 1。
        返还 假。
    完。
    返还 真。
完。

IDT_验证全部 接受 零参：
    可变 错误数 为 0。
    可变 i 为 0。
    只要 i 小于 IDT_条目数：
        若 做 IDT_验证条目 用 i 等于 假：
            错误数 为 错误数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 错误数 大于 0：
        做 打印 用 「[IDT验证] 发现 」 加 做 转字符串 用 错误数 加 「 个异常条目」。
    否则：
        做 打印 用 「[IDT验证] 全部通过」。
    完。
    返还 错误数。
完。

可变 IDT_保存缓冲 为 空。

可变 IDT_已保存 为 假。

IDT_保存 接受 零参：
    若 IDT缓冲 等于 空：
        返还 错误_未初始化。
    完。
    若 IDT_保存缓冲 等于 空：
        IDT_保存缓冲 为 做 缓冲区 用 IDT_总大小。
    完。
    做 内存复制 用 IDT_保存缓冲、IDT缓冲、IDT_总大小。
    IDT_已保存 为 真。
    写条目_保存次数 为 写条目_保存次数 加 1。
    返还 0。
完。

IDT_恢复 接受 零参：
    若 IDT_已保存 等于 假：
        返还 错误_无效参数。
    完。
    若 IDT_保存缓冲 等于 空：
        返还 错误_无效参数。
    完。
    做 内存复制 用 IDT缓冲、IDT_保存缓冲、IDT_总大小。
    写条目_恢复次数 为 写条目_恢复次数 加 1。
    返还 0。
完。

IDT_dump条目 接受 向量号：
    若 向量号 大于或等于 IDT_条目数：
        返还。
    完。
    信息 为 做 IDT_读条目 用 向量号。
    若 信息 等于 空：
        返还。
    完。
    若 信息 的 已设置 等于 假 并且 向量号 大于或等于 32：
        返还。
    完。
    做 打印 用 「  [」 加 做 转字符串 用 向量号 加 「] 」 加 做 IDT_异常名 用 向量号 加 「 偏移=0x」 加 做 转字符串 用 信息 的 偏移 加 「 类型=」 加 做 IDT_门类型名 用 信息 的 类型 加 「 IST=」 加 做 IDT_IST名 用 信息 的 IST 加 「 P=」 加 做 转字符串 用 信息 的 已设置。
完。

IDT_dump全部 接受 零参：
    做 打印 用 「[IDT dump] ═══════════════════════」。
    可变 已设置数 为 0。
    可变 i 为 0。
    只要 i 小于 IDT_条目数：
        信息 为 做 IDT_读条目 用 i。
        若 信息 不等于 空 并且 信息 的 已设置 等于 真：
            做 IDT_dump条目 用 i。
            已设置数 为 已设置数 加 1。
        完。
        i 为 i 加 1。
    完。
    做 打印 用 「  已设置条目: 」 加 做 转字符串 用 已设置数 加 「/」 加 做 转字符串 用 IDT_条目数。
    做 打印 用 「[IDT dump] ═══════════════════════」。
完。

IDT_dump异常 接受 零参：
    做 打印 用 「[IDT 异常] ──────────────────」。
    可变 i 为 0。
    只要 i 小于 32：
        做 IDT_dump条目 用 i。
        i 为 i 加 1。
    完。
    做 打印 用 「[IDT 异常] ──────────────────」。
完。

IDT_已设置数 接受 零参：
    可变 数量 为 0。
    可变 i 为 0。
    只要 i 小于 IDT_条目数：
        偏移 为 i 乘 IDT_条目大小。
        类型 为 做 缓冲读 用 IDT缓冲、偏移 加 5。
        若 类型 位与 0x80 不等于 0：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

IDT_中断门数 接受 零参：
    可变 数量 为 0。
    可变 i 为 0。
    只要 i 小于 IDT_条目数：
        偏移 为 i 乘 IDT_条目大小。
        类型 为 做 缓冲读 用 IDT缓冲、偏移 加 5。
        若 类型 等于 IDT_中断门 或者 类型 等于 IDT_用户中断门：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

IDT_陷阱门数 接受 零参：
    可变 数量 为 0。
    可变 i 为 0。
    只要 i 小于 IDT_条目数：
        偏移 为 i 乘 IDT_条目大小。
        类型 为 做 缓冲读 用 IDT缓冲、偏移 加 5。
        若 类型 等于 IDT_陷阱门 或者 类型 等于 IDT_用户陷阱门：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

IDT_用户门数 接受 零参：
    可变 数量 为 0。
    可变 i 为 0。
    只要 i 小于 IDT_条目数：
        偏移 为 i 乘 IDT_条目大小。
        类型 为 做 缓冲读 用 IDT缓冲、偏移 加 5。
        若 类型 等于 IDT_用户中断门 或者 类型 等于 IDT_用户陷阱门：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

IDT_写条目_完整统计 接受 零参：
    统计 为 做 字典。
    做 字典设 用 统计、「总条目数」、IDT_条目数。
    做 字典设 用 统计、「已设置数」、做 IDT_已设置数。
    做 字典设 用 统计、「中断门数」、做 IDT_中断门数。
    做 字典设 用 统计、「陷阱门数」、做 IDT_陷阱门数。
    做 字典设 用 统计、「用户门数」、做 IDT_用户门数。
    做 字典设 用 统计、「写入次数」、写条目_写入次数。
    做 字典设 用 统计、「读取次数」、写条目_读取次数。
    做 字典设 用 统计、「清除次数」、写条目_清除次数。
    做 字典设 用 统计、「修改次数」、写条目_修改次数。
    做 字典设 用 统计、「批量次数」、写条目_批量次数。
    做 字典设 用 统计、「验证次数」、写条目_验证次数。
    做 字典设 用 统计、「验证失败」、写条目_验证失败。
    做 字典设 用 统计、「保存次数」、写条目_保存次数。
    做 字典设 用 统计、「恢复次数」、写条目_恢复次数。
    做 字典设 用 统计、「错误数」、写条目_错误数。
    做 字典设 用 统计、「IDT缓冲」、IDT缓冲 不等于 空。
    做 字典设 用 统计、「已保存」、IDT_已保存。
    返还 统计。
完。

IDT_写条目_打印统计 接受 零参：
    统计 为 做 IDT_写条目_完整统计。
    做 打印 用 「[IDT条目] ═══════ 完整统计 ═══════」。
    做 打印 用 「  已设置:     」 加 做 转字符串 用 做 字典取 用 统计、「已设置数」 加 「/」 加 做 转字符串 用 做 字典取 用 统计、「总条目数」。
    做 打印 用 「  中断门:     」 加 做 转字符串 用 做 字典取 用 统计、「中断门数」。
    做 打印 用 「  陷阱门:     」 加 做 转字符串 用 做 字典取 用 统计、「陷阱门数」。
    做 打印 用 「  用户门:     」 加 做 转字符串 用 做 字典取 用 统计、「用户门数」。
    做 打印 用 「  写入次数:   」 加 做 转字符串 用 做 字典取 用 统计、「写入次数」。
    做 打印 用 「  读取次数:   」 加 做 转字符串 用 做 字典取 用 统计、「读取次数」。
    做 打印 用 「  清除次数:   」 加 做 转字符串 用 做 字典取 用 统计、「清除次数」。
    做 打印 用 「  修改次数:   」 加 做 转字符串 用 做 字典取 用 统计、「修改次数」。
    做 打印 用 「  验证次数:   」 加 做 转字符串 用 做 字典取 用 统计、「验证次数」。
    做 打印 用 「  验证失败:   」 加 做 转字符串 用 做 字典取 用 统计、「验证失败」。
    做 打印 用 「  保存次数:   」 加 做 转字符串 用 做 字典取 用 统计、「保存次数」。
    做 打印 用 「  错误数:     」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[IDT条目] ═════════════════════════」。
    返还 0。
完。

IDT_写条目_销毁 接受 零参：
    若 IDT缓冲 不等于 空：
        做 内存填充 用 IDT缓冲、0、IDT_总大小。
    完。
    若 IDT_保存缓冲 不等于 空：
        IDT_保存缓冲 为 空。
    完。
    IDT_已保存 为 假。
    做 打印 用 「[IDT条目] 已销毁」。
    返还 0。
完。

IDT_为IRQ注册提供条目 接受 向量号、处理函数地址：
    返还 做 IDT_写条目 用 向量号、处理函数地址、GDT_内核代码、IDT_中断门、IST_无。
完。

IDT_为电源管理挂起 接受 零参：
    做 IDT_保存。
    返还 0。
完。

IDT_为电源管理恢复 接受 零参：
    做 IDT_恢复。
    做 IDT_加载。
    返还 0。
完。

IDT_为procfs提供信息 接受 零参：
    返还 一组：IDT_条目数、做 IDT_已设置数、做 IDT_中断门数、做 IDT_陷阱门数、做 IDT_用户门数。
完。

本章 收束。
