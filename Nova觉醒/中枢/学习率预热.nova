《学习率预热》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《.·打乱》。

引用《.·内核打印》。

可变 调度_查询次数 为 0。

可变 裁剪_触发次数 为 0。

可变 归一化_次数 为 0。

可变 早停_检查次数 为 0。

学习率_预热 接受 步数、预热步、目标率：
    调度_查询次数 为 调度_查询次数 加 1。
    若 步数 大于或等于 预热步：
        返还 目标率。
    完。
    返还 目标率 乘 步数 除以 预热步。
完。

学习率_余弦 接受 步数、总步数、最大率、最小率：
    调度_查询次数 为 调度_查询次数 加 1。
    若 总步数 小于或等于 0 或者 步数 大于或等于 总步数：
        返还 最小率。
    完。
    t 为 步数 乘 网络精度 除以 总步数。
    cos_val 为 网络精度 减 2 乘 t 乘 t 除以 网络精度。
    返还 最小率 加 (最大率 减 最小率) 乘 (网络精度 加 cos_val) 除以 (2 乘 网络精度)。
完。

学习率_阶梯 接受 步数、阶梯大小、初始率、衰减因子：
    调度_查询次数 为 调度_查询次数 加 1。
    可变 率 为 初始率。
    可变 s 为 步数。
    只要 s 大于或等于 阶梯大小：
        率 为 率 乘 衰减因子 除以 网络精度。
        s 为 s 减 阶梯大小。
    完。
    返还 率。
完。

学习率_指数 接受 步数、初始率、衰减率、衰减步：
    调度_查询次数 为 调度_查询次数 加 1。
    可变 率 为 初始率。
    可变 s 为 步数。
    只要 s 大于或等于 衰减步：
        率 为 率 乘 衰减率 除以 网络精度。
        s 为 s 减 衰减步。
    完。
    若 s 大于 0：
        分数 为 s 乘 网络精度 除以 衰减步。
        部分衰减 为 网络精度 减 分数 乘 (网络精度 减 衰减率) 除以 网络精度。
        率 为 率 乘 部分衰减 除以 网络精度。
    完。
    返还 率。
完。

学习率_多项式 接受 步数、总步数、初始率、终止率、幂：
    调度_查询次数 为 调度_查询次数 加 1。
    若 步数 大于或等于 总步数：
        返还 终止率。
    完。
    t 为 (总步数 减 步数) 乘 网络精度 除以 总步数。
    可变 衰减 为 t。
    若 幂 等于 2：
        衰减 为 t 乘 t 除以 网络精度。
    否则若 幂 等于 3：
        衰减 为 t 乘 t 除以 网络精度 乘 t 除以 网络精度。
    完。
    返还 终止率 加 (初始率 减 终止率) 乘 衰减 除以 网络精度。
完。

学习率_循环 接受 步数、周期步数、最小率、最大率：
    调度_查询次数 为 调度_查询次数 加 1。
    半周期 为 周期步数 除以 2。
    若 半周期 小于或等于 0：
        返还 最小率。
    完。
    位置 为 步数 取余于 周期步数。
    可变 率 为 0。
    若 位置 小于 半周期：
        率 为 最小率 加 (最大率 减 最小率) 乘 位置 除以 半周期。
    否则：
        率 为 最大率 减 (最大率 减 最小率) 乘 (位置 减 半周期) 除以 半周期。
    完。
    返还 率。
完。

学习率_单周期 接受 步数、总步数、最大率：
    调度_查询次数 为 调度_查询次数 加 1。
    最小率 为 最大率 除以 25。
    上升步 为 总步数 乘 30 除以 100。
    下降步 为 总步数 乘 70 除以 100。
    若 步数 小于 上升步：
        返还 最小率 加 (最大率 减 最小率) 乘 步数 除以 上升步。
    否则若 步数 小于 下降步：
        t 为 (步数 减 上升步) 乘 网络精度 除以 (下降步 减 上升步)。
        cos_val 为 网络精度 减 2 乘 t 乘 t 除以 网络精度。
        返还 最小率 加 (最大率 减 最小率) 乘 (网络精度 加 cos_val) 除以 (2 乘 网络精度)。
    否则：
        退火率 为 最小率 除以 10。
        t 为 (步数 减 下降步) 乘 网络精度 除以 (总步数 减 下降步)。
        返还 最小率 减 (最小率 减 退火率) 乘 t 除以 网络精度。
    完。
完。

学习率_预热余弦 接受 步数、预热步、总步数、最大率、最小率：
    调度_查询次数 为 调度_查询次数 加 1。
    若 步数 小于 预热步：
        返还 最大率 乘 步数 除以 预热步。
    完。
    返还 做 学习率_余弦 用 步数 减 预热步、总步数 减 预热步、最大率、最小率。
完。

学习率_预热线性 接受 步数、预热步、总步数、最大率：
    调度_查询次数 为 调度_查询次数 加 1。
    若 步数 小于 预热步：
        返还 最大率 乘 步数 除以 预热步。
    完。
    若 步数 大于或等于 总步数：
        返还 0。
    完。
    返还 最大率 乘 (总步数 减 步数) 除以 (总步数 减 预热步)。
完。

plateau_创建 接受 初始率、因子、耐心、最小率：
    返还 一组：初始率、因子、耐心、最小率、0、0x7FFFFFFF、初始率。
完。

plateau_步进 接受 状态、当前指标：
    调度_查询次数 为 调度_查询次数 加 1。
    若 当前指标 小于 状态 的 第 5 项：
        状态 的 第 5 项 为 当前指标。
        状态 的 第 4 项 为 0。
    否则：
        状态 的 第 4 项 为 状态 的 第 4 项 加 1。
        若 状态 的 第 4 项 大于或等于 状态 的 第 2 项：
            状态 的 第 6 项 为 状态 的 第 6 项 乘 状态 的 第 1 项 除以 网络精度。
            若 状态 的 第 6 项 小于 状态 的 第 3 项：
                状态 的 第 6 项 为 状态 的 第 3 项。
            完。
            状态 的 第 4 项 为 0。
        完。
    完。
    返还 状态 的 第 6 项。
完。

梯度裁剪_范数 接受 梯度、最大范数：
    可变 范数平方 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度：
        范数平方 为 范数平方 加 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 网络精度。
        i 为 i 加 1。
    完。
    范数 为 做 网络_开方 用 范数平方。
    若 范数 大于 最大范数：
        裁剪_触发次数 为 裁剪_触发次数 加 1。
        缩放 为 最大范数 乘 网络精度 除以 范数。
        i 为 0。
        只要 i 小于 做 计数 用 梯度：
            梯度 的 第 i 项 为 梯度 的 第 i 项 乘 缩放 除以 网络精度。
            i 为 i 加 1。
        完。
    完。
    返还 范数。
完。

梯度裁剪_值 接受 梯度、最大值：
    可变 裁剪数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度：
        若 梯度 的 第 i 项 大于 最大值：
            梯度 的 第 i 项 为 最大值。
            裁剪数 为 裁剪数 加 1。
        完。
        若 梯度 的 第 i 项 小于 0 减 最大值：
            梯度 的 第 i 项 为 0 减 最大值。
            裁剪数 为 裁剪数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 裁剪数 大于 0：
        裁剪_触发次数 为 裁剪_触发次数 加 1。
    完。
    返还 裁剪数。
完。

梯度累积_创建 接受 参数数：
    可变 累积梯度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数数：
        做 网络_追加 用 累积梯度、0。
        i 为 i 加 1。
    完。
    返还 一组：累积梯度、0。
完。

梯度累积_加 接受 状态、梯度：
    累积 为 状态 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度 并且 i 小于 做 计数 用 累积：
        累积 的 第 i 项 为 累积 的 第 i 项 加 梯度 的 第 i 项。
        i 为 i 加 1。
    完。
    状态 的 第 1 项 为 状态 的 第 1 项 加 1。
完。

梯度累积_获取 接受 状态：
    累积 为 状态 的 第 0 项。
    步数 为 状态 的 第 1 项。
    若 步数 小于或等于 0：
        返还 累积。
    完。
    可变 平均梯度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 累积：
        做 网络_追加 用 平均梯度、累积 的 第 i 项 除以 步数。
        i 为 i 加 1。
    完。
    返还 平均梯度。
完。

梯度累积_清零 接受 状态：
    累积 为 状态 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 累积：
        累积 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    状态 的 第 1 项 为 0。
完。

权重衰减 接受 参数、衰减系数：
    n 为 做 计数 用 参数。
    可变 i 为 0。
    只要 i 小于 n：
        参数 的 第 i 项 为 参数 的 第 i 项 减 衰减系数 乘 参数 的 第 i 项 除以 网络精度。
        i 为 i 加 1。
    完。
完。

L1正则化 接受 参数、系数：
    n 为 做 计数 用 参数。
    可变 i 为 0。
    只要 i 小于 n：
        若 参数 的 第 i 项 大于 0：
            参数 的 第 i 项 为 参数 的 第 i 项 减 系数。
            若 参数 的 第 i 项 小于 0：
                参数 的 第 i 项 为 0。
            完。
        否则若 参数 的 第 i 项 小于 0：
            参数 的 第 i 项 为 参数 的 第 i 项 加 系数。
            若 参数 的 第 i 项 大于 0：
                参数 的 第 i 项 为 0。
            完。
        完。
        i 为 i 加 1。
    完。
完。

弹性网正则化 接受 参数、L1系数、L2系数：
    做 权重衰减 用 参数、L2系数。
    做 L1正则化 用 参数、L1系数。
完。

L2损失 接受 参数、系数：
    可变 损失 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        损失 为 损失 加 参数 的 第 i 项 乘 参数 的 第 i 项 除以 网络精度。
        i 为 i 加 1。
    完。
    返还 系数 乘 损失 除以 (2 乘 网络精度)。
完。

早停_创建 接受 耐心、最小改善：
    返还 一组：耐心、最小改善、0、0x7FFFFFFF、假。
完。

早停_检查 接受 状态、当前损失：
    早停_检查次数 为 早停_检查次数 加 1。
    若 当前损失 小于 状态 的 第 3 项 减 状态 的 第 1 项：
        状态 的 第 3 项 为 当前损失。
        状态 的 第 2 项 为 0。
    否则：
        状态 的 第 2 项 为 状态 的 第 2 项 加 1。
        若 状态 的 第 2 项 大于或等于 状态 的 第 0 项：
            状态 的 第 4 项 为 真。
        完。
    完。
    返还 状态 的 第 4 项。
完。

层归一化 接受 输入、gamma、beta：
    归一化_次数 为 归一化_次数 加 1。
    n 为 做 计数 用 输入。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 输入 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 n：
        d 为 输入 的 第 i 项 减 均值。
        方差 为 方差 加 d 乘 d 除以 网络精度。
        i 为 i 加 1。
    完。
    方差 为 方差 除以 n。
    可变 标准差 为 做 网络_开方 用 方差。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    可变 输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        归一 为 (输入 的 第 i 项 减 均值) 乘 网络精度 除以 标准差。
        做 网络_追加 用 输出、归一 乘 gamma 的 第 i 项 除以 网络精度 加 beta 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 输出。
完。

层归一化_反向 接受 输入、gamma、输出梯度：
    归一化_次数 为 归一化_次数 加 1。
    n 为 做 计数 用 输入。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 输入 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 n：
        d 为 输入 的 第 i 项 减 均值。
        方差 为 方差 加 d 乘 d 除以 网络精度。
        i 为 i 加 1。
    完。
    方差 为 方差 除以 n。
    标准差 为 做 网络_开方 用 方差 加 1。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    可变 梯度 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        归一 为 (输入 的 第 i 项 减 均值) 乘 网络精度 除以 标准差。
        dg 为 输出梯度 的 第 i 项 乘 gamma 的 第 i 项 除以 网络精度。
        做 网络_追加 用 梯度、dg 除以 标准差。
        i 为 i 加 1。
    完。
    返还 梯度。
完。

rms归一化 接受 输入、gamma：
    归一化_次数 为 归一化_次数 加 1。
    n 为 做 计数 用 输入。
    可变 和平方 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和平方 为 和平方 加 输入 的 第 i 项 乘 输入 的 第 i 项 除以 网络精度。
        i 为 i 加 1。
    完。
    可变 rms 为 做 网络_开方 用 和平方 除以 n。
    若 rms 等于 0：
        rms 为 1。
    完。
    可变 输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 网络_追加 用 输出、输入 的 第 i 项 乘 gamma 的 第 i 项 除以 rms。
        i 为 i 加 1。
    完。
    返还 输出。
完。

调度_统计 接受 零参：
    返还 一组：调度_查询次数、裁剪_触发次数、归一化_次数、早停_检查次数。
完。

调度_打印统计 接受 零参：
    做 打印行 用 「=== 学习率调度/训练工具统计 ===」。
    做 打印行 用 「调度查询: 」 加 做 整数转字符串 用 调度_查询次数。
    做 打印行 用 「裁剪触发: 」 加 做 整数转字符串 用 裁剪_触发次数。
    做 打印行 用 「归一化次数: 」 加 做 整数转字符串 用 归一化_次数。
    做 打印行 用 「早停检查: 」 加 做 整数转字符串 用 早停_检查次数。
完。

余弦退火_计算 接受 初始率、当前步、总步数：
    调度_查询次数 为 调度_查询次数 加 1。
    若 总步数 小于或等于 0：
        返还 初始率。
    完。
    角度 为 当前步 乘 3142 除以 总步数。
    x2 为 角度 乘 角度 除以 1000。
    cos_v 为 1000 减 x2 除以 2。
    若 cos_v 小于 负 1000：
        cos_v 为 负 1000。
    完。
    返还 初始率 乘 (1000 加 cos_v) 除以 2000。
完。

多阶段调度_计算 接受 初始率、当前步、里程碑列表、里程碑数、衰减率：
    调度_查询次数 为 调度_查询次数 加 1。
    可变 lr 为 初始率。
    可变 i 为 0。
    只要 i 小于 里程碑数：
        若 当前步 大于或等于 里程碑列表 的 第 i 项：
            lr 为 lr 乘 衰减率 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 lr。
完。

一周期余弦_计算 接受 最小率、最大率、当前步、周期：
    调度_查询次数 为 调度_查询次数 加 1。
    若 周期 小于或等于 0：
        返还 最小率。
    完。
    半周期 为 周期 除以 2。
    若 当前步 小于 半周期：
        返还 最小率 加 (最大率 减 最小率) 乘 当前步 除以 半周期。
    否则：
        返还 最大率 减 (最大率 减 最小率) 乘 (当前步 减 半周期) 除以 半周期。
    完。
完。

调度_重置统计 接受 零参：
    调度_查询次数 为 0。
    裁剪_触发次数 为 0。
    归一化_次数 为 0。
    早停_检查次数 为 0。
完。

权重衰减_应用 接受 参数、衰减系数：
    n 为 做 计数 用 参数。
    可变 i 为 0。
    只要 i 小于 n：
        参数 的 第 i 项 为 参数 的 第 i 项 乘 (网络精度 减 衰减系数) 除以 网络精度。
        i 为 i 加 1。
    完。
    返还 参数。
完。

动量_更新 接受 速度、梯度、动量系数：
    n 为 做 计数 用 梯度。
    可变 i 为 0。
    只要 i 小于 n 并且 i 小于 做 计数 用 速度：
        速度 的 第 i 项 为 动量系数 乘 速度 的 第 i 项 除以 网络精度 加 (网络精度 减 动量系数) 乘 梯度 的 第 i 项 除以 网络精度。
        i 为 i 加 1。
    完。
    返还 速度。
完。

Q快速幂整数 接受 base、exp：
    可变 result 为 Q_ONE。
    可变 b 为 base。
    可变 e 为 exp。
    只要 e 大于 0：
        若 e 取余于 2 等于 1：
            result 为 做 Q乘 用 result、b。
        完。
        b 为 做 Q乘 用 b、b。
        e 为 e 除以 2。
    完。
    返还 result。
完。

自适应矩估计W_更新 接受 opt、层索引、权重、梯度、权重衰减率：
    NOGL_Adam次数 为 NOGL_Adam次数 加 1。
    opt 的 步数 为 opt 的 步数 加 1。
    若 层索引 大于或等于 opt 的 层数：
        返还。
    完。
    m_t 为 做 内存_读64 用 opt 的 m列表 加 层索引 乘 8。
    v_t 为 做 内存_读64 用 opt 的 v列表 加 层索引 乘 8。
    总 为 权重 的 行数 乘 权重 的 列数。
    b1 为 opt 的 beta1。
    b2 为 opt 的 beta2。
    b1_t 为 做 Q快速幂整数 用 b1、opt 的 步数。
    b2_t 为 做 Q快速幂整数 用 b2、opt 的 步数。
    m_校正 为 做 Q除 用 Q_ONE、Q_ONE 减 b1_t。
    v_校正 为 做 Q除 用 Q_ONE、Q_ONE 减 b2_t。
    可变 i 为 0。
    只要 i 小于 总：
        g 为 做 内存_读32有符号 用 梯度 的 数据 加 i 乘 4。
        m_old 为 做 内存_读32有符号 用 m_t 的 数据 加 i 乘 4。
        m_new 为 做 Q乘 用 b1、m_old 加 做 Q乘 用 Q_ONE 减 b1、g。
        做 内存_写32 用 m_t 的 数据 加 i 乘 4、m_new。
        v_old 为 做 内存_读32有符号 用 v_t 的 数据 加 i 乘 4。
        v_new 为 做 Q乘 用 b2、v_old 加 做 Q乘 用 Q_ONE 减 b2、做 Q乘 用 g、g。
        做 内存_写32 用 v_t 的 数据 加 i 乘 4、v_new。
        m_hat 为 做 Q乘 用 m_new、m_校正。
        v_hat 为 做 Q乘 用 v_new、v_校正。
        denom 为 做 Q平方根 用 v_hat 加 Q_EPSILON。
        delta 为 做 Q乘 用 opt 的 学习率、做 Q除 用 m_hat、denom。
        w 为 做 内存_读32有符号 用 权重 的 数据 加 i 乘 4。
        wd_decay 为 做 Q乘 用 权重衰减率、w。
        做 内存_写32 用 权重 的 数据 加 i 乘 4、w 减 delta 减 wd_decay。
        i 为 i 加 1。
    完。
完。

调度器_步进_v2 接受 s：
    NOGL_调度次数 为 NOGL_调度次数 加 1。
    s 的 当前步 为 s 的 当前步 加 1。
    若 s 的 策略 等于 NOGL_调度_固定：
        返还 s 的 初始LR。
    完。
    若 s 的 策略 等于 NOGL_调度_衰减：
        衰减_t 为 做 Q快速幂整数 用 s 的 衰减因子、s 的 当前步。
        lr 为 做 Q乘 用 s 的 初始LR、衰减_t。
        若 lr 小于 s 的 最小LR：
            lr 为 s 的 最小LR。
        完。
        返还 lr。
    完。
    若 s 的 策略 等于 NOGL_调度_余弦退火：
        t 为 s 的 当前步 取余于 s 的 周期步数 乘 Q_ONE 除以 s 的 周期步数。
        FP 为 10000。
        FP_PI 为 31416。
        pt 为 t 乘 FP_PI 除以 FP。
        pt2 为 pt 乘 pt 除以 FP。
        pt4 为 pt2 乘 pt2 除以 FP。
        cos_val 为 (FP 减 pt2 除以 2 加 pt4 除以 24) 乘 Q_ONE 除以 FP。
        range 为 s 的 初始LR 减 s 的 最小LR。
        返还 s 的 最小LR 加 做 Q乘 用 range、(Q_ONE 加 cos_val) 除以 2。
    完。
    若 s 的 策略 等于 NOGL_调度_周期：
        half 为 s 的 周期步数 除以 2。
        pos 为 s 的 当前步 取余于 s 的 周期步数。
        若 pos 小于 half：
            返还 s 的 最小LR 加 (s 的 初始LR 减 s 的 最小LR) 乘 pos 除以 half。
        完。
        返还 s 的 初始LR 减 (s 的 初始LR 减 s 的 最小LR) 乘 (pos 减 half) 除以 half。
    完。
    返还 s 的 初始LR。
完。

EWC_安全DIFF_MAX 为 3783。

弹性权重_损失项 接受 网络、ref：
    若 ref 等于 0 或者 ref 的 权重快照 等于 0 或者 ref 的 Fisher对角 等于 0：
        返还 0。
    完。
    N 为 做 网络_权重总数 用 网络。
    可变 损失 为 0。
    可变 i 为 0。
    只要 i 小于 N：
        w 为 做 网络_获取权重1D 用 网络、i。
        w_ref 为 做 Tensor_读1D 用 ref 的 权重快照、i。
        F_i 为 做 Tensor_读1D 用 ref 的 Fisher对角、i。
        可变 diff 为 w 减 w_ref。
        若 diff 大于 EWC_安全DIFF_MAX：
            diff 为 EWC_安全DIFF_MAX。
        完。
        若 diff 小于 0 减 EWC_安全DIFF_MAX：
            diff 为 0 减 EWC_安全DIFF_MAX。
        完。
        lf 为 ref 的 lambda 乘 F_i 除以 (2 乘 Q_ONE)。
        d2 为 做 Q乘 用 diff、diff。
        损失 为 损失 加 做 Q乘 用 lf、d2。
        i 为 i 加 1。
    完。
    返还 损失。
完。

本章 收束。
