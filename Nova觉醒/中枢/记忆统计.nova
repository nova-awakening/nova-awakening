《记忆统计》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·感知·内核直连层》。

引用《.·内存初始化》。

引用《.·内容可见性》。

工作容量 为 7。

情景容量 为 256。

语义容量 为 512。

衰减基数 为 1024。

衰减速率 为 995。

巩固阈值 为 600。

遗忘阈值 为 50。

工作项 是 一种 事物，含 键、值、重要度、时间戳。

情景项 是 一种 事物，含 内容哈希、上下文、强度、创建时刻、访问次数、最近访问。

语义节点项 是 一种 事物，含 标识、数据、连接数、连接表、激活度。

可变 工作记忆 为 一组：（空）。

可变 情景记忆 为 一组：（空）。

可变 语义节点 为 一组：（空）。

可变 写入次数 为 0。

可变 读取次数 为 0。

可变 巩固次数 为 0。

可变 遗忘次数 为 0。

可变 命中次数 为 0。

可变 未命中次数 为 0。

记忆_初始化 接受 零参：
    工作记忆 为 一组：（空）。
    情景记忆 为 一组：（空）。
    语义节点 为 一组：（空）。
完。

工作_写入 接受 键、值、重要度：
    写入次数 为 写入次数 加 1。
    可变 序号 为 0。
    只要 序号 小于 做 计数 用 工作记忆：
        若 工作记忆 的 第 序号 项 的 键 等于 键：
            工作记忆 的 第 序号 项 的 值 为 值。
            工作记忆 的 第 序号 项 的 重要度 为 重要度。
            工作记忆 的 第 序号 项 的 时间戳 为 做 平台_获取高精度时间。
            返还。
        完。
        序号 为 序号 加 1。
    完。
    若 做 计数 用 工作记忆 大于或等于 工作容量：
        可变 最弱 为 0。
        可变 最弱分 为 999999。
        序号 为 0。
        只要 序号 小于 做 计数 用 工作记忆：
            年龄 为 做 平台_获取高精度时间 减 工作记忆 的 第 序号 项 的 时间戳。
            分 为 工作记忆 的 第 序号 项 的 重要度 乘 1000 除以 (年龄 加 1)。
            若 分 小于 最弱分：
                最弱分 为 分。
                最弱 为 序号。
            完。
            序号 为 序号 加 1。
        完。
        淘汰 为 工作记忆 的 第 最弱 项。
        若 淘汰 的 重要度 大于 巩固阈值：
            做 情景_巩固 用 淘汰 的 键、淘汰 的 值。
        完。
        工作记忆 的 第 最弱 项 为 新建 工作项：键 为 键、值 为 值、重要度 为 重要度、时间戳 为 做 平台_获取高精度时间。
        返还。
    完。
    做 追加 用 工作记忆、新建 工作项：键 为 键、值 为 值、重要度 为 重要度、时间戳 为 做 平台_获取高精度时间。
完。

工作_读取 接受 键：
    读取次数 为 读取次数 加 1。
    可变 序号 为 0。
    只要 序号 小于 做 计数 用 工作记忆：
        若 工作记忆 的 第 序号 项 的 键 等于 键：
            工作记忆 的 第 序号 项 的 时间戳 为 做 平台_获取高精度时间。
            命中次数 为 命中次数 加 1。
            返还 工作记忆 的 第 序号 项 的 值。
        完。
        序号 为 序号 加 1。
    完。
    情景结果 为 做 情景_检索 用 键。
    若 情景结果 不等于 0：
        命中次数 为 命中次数 加 1。
        返还 情景结果。
    完。
    未命中次数 为 未命中次数 加 1。
    返还 0。
完。

情景_巩固 接受 键、值：
    巩固次数 为 巩固次数 加 1。
    若 做 计数 用 情景记忆 大于或等于 情景容量：
        做 情景_遗忘最弱。
    完。
    做 追加 用 情景记忆、新建 情景项：内容哈希 为 键、上下文 为 值、强度 为 衰减基数、创建时刻 为 做 平台_获取高精度时间、访问次数 为 1、最近访问 为 做 平台_获取高精度时间。
完。

情景_检索 接受 键：
    可变 序号 为 0。
    只要 序号 小于 做 计数 用 情景记忆：
        若 情景记忆 的 第 序号 项 的 内容哈希 等于 键：
            情景记忆 的 第 序号 项 的 访问次数 为 情景记忆 的 第 序号 项 的 访问次数 加 1。
            情景记忆 的 第 序号 项 的 最近访问 为 做 平台_获取高精度时间。
            情景记忆 的 第 序号 项 的 强度 为 情景记忆 的 第 序号 项 的 强度 加 50。
            若 情景记忆 的 第 序号 项 的 强度 大于 衰减基数：
                情景记忆 的 第 序号 项 的 强度 为 衰减基数。
            完。
            返还 情景记忆 的 第 序号 项 的 上下文。
        完。
        序号 为 序号 加 1。
    完。
    返还 0。
完。

情景_遗忘最弱 接受 零参：
    可变 最弱 为 0。
    可变 最弱强度 为 衰减基数 加 1。
    可变 序号 为 0。
    只要 序号 小于 做 计数 用 情景记忆：
        若 情景记忆 的 第 序号 项 的 强度 小于 最弱强度：
            最弱强度 为 情景记忆 的 第 序号 项 的 强度。
            最弱 为 序号。
        完。
        序号 为 序号 加 1。
    完。
    情景记忆 的 第 最弱 项 为 情景记忆 的 第 做 计数 用 情景记忆 减 1 项。
    遗忘次数 为 遗忘次数 加 1。
完。

记忆_衰减周期 接受 零参：
    可变 序号 为 0。
    只要 序号 小于 做 计数 用 情景记忆：
        情景记忆 的 第 序号 项 的 强度 为 情景记忆 的 第 序号 项 的 强度 乘 衰减速率 除以 衰减基数。
        若 情景记忆 的 第 序号 项 的 强度 小于 遗忘阈值：
            情景记忆 的 第 序号 项 为 情景记忆 的 第 做 计数 用 情景记忆 减 1 项。
            遗忘次数 为 遗忘次数 加 1。
            继续。
        完。
        序号 为 序号 加 1。
    完。
完。

语义_添加节点 接受 标识、数据：
    若 做 计数 用 语义节点 大于或等于 语义容量：
        返还 负 1。
    完。
    做 追加 用 语义节点、新建 语义节点项：标识 为 标识、数据 为 数据、连接数 为 0、连接表 为 做 堆_分配 用 32 乘 8、激活度 为 衰减基数。
    返还 做 计数 用 语义节点 减 1。
完。

语义_添加连接 接受 源索引、目标索引、权重：
    若 源索引 大于或等于 做 计数 用 语义节点：
        返还。
    完。
    节点 为 语义节点 的 第 源索引 项。
    若 节点 的 连接数 大于或等于 32：
        返还。
    完。
    做 内存_写32 用 节点 的 连接表 加 节点 的 连接数 乘 8、目标索引。
    做 内存_写32 用 节点 的 连接表 加 节点 的 连接数 乘 8 加 4、权重。
    节点 的 连接数 为 节点 的 连接数 加 1。
完。

语义_扩散激活 接受 源索引：
    若 源索引 大于或等于 做 计数 用 语义节点：
        返还。
    完。
    节点 为 语义节点 的 第 源索引 项。
    节点 的 激活度 为 衰减基数。
    可变 连接序 为 0。
    只要 连接序 小于 节点 的 连接数：
        目标 为 做 内存_读32 用 节点 的 连接表 加 连接序 乘 8。
        权重 为 做 内存_读32 用 节点 的 连接表 加 连接序 乘 8 加 4。
        若 目标 小于 做 计数 用 语义节点：
            增量 为 节点 的 激活度 乘 权重 除以 衰减基数。
            语义节点 的 第 目标 项 的 激活度 为 语义节点 的 第 目标 项 的 激活度 加 增量。
            若 语义节点 的 第 目标 项 的 激活度 大于 衰减基数：
                语义节点 的 第 目标 项 的 激活度 为 衰减基数。
            完。
        完。
        连接序 为 连接序 加 1。
    完。
完。

记忆_完整统计 接受 零参：
    返还 一组：做 计数 用 工作记忆、做 计数 用 情景记忆、做 计数 用 语义节点、写入次数、读取次数、巩固次数、遗忘次数、命中次数、未命中次数。
完。

可变 AI_记忆统计_热节点缓存 为 0。

AI_记忆统计_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_记忆统计_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_记忆统计_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

本章 收束。
