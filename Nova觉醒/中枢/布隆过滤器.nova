《布隆过滤器》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·内核打印》。

引用《..·推理·定点Q算术》。

引用《.·哈希整数》。

布隆_默认哈希数 为 3。

布隆_最大哈希数 为 16。

布隆_默认位数 为 65536。

布隆_最大位数 为 0x4000000。

计数布隆_计数器位 为 4。

计数布隆_最大值 为 15。

可扩展_初始层位数 为 8192。

可扩展_最大层数 为 32。

可扩展_扩展因子 为 2。

哈希种子_A 为 0x12345678。

哈希种子_B 为 0x87654321。

哈希种子_C 为 0xDEADBEEF。

哈希种子_D 为 0xCAFEBABE。

哈希种子_E 为 0xFEEDFACE。

哈希种子_F 为 0xC0FFEE11。

BFOT_特征维度 为 8。

BFOT_置信阈值 为 0.70。

布隆过滤器 是 一种 事物，含 位数组、总位数、哈希函数数、元素数、添加次数、查询次数、命中次数。

计数布隆过滤器 是 一种 事物，含 计数数组、总位数、哈希函数数、元素数。

可扩展布隆过滤器 是 一种 事物，含 层列表、层数、总元素数。

布隆统计 是 一种 事物，含 总创建数、总添加数、总查询数、总命中数、总未中数、总扩展数。

可变 全局布隆统计 为 新建 布隆统计。

哈希种子表 为 一组：0x12345678、0x87654321、0xDEADBEEF、0xCAFEBABE、0xFEEDFACE、0xC0FFEE11、0xBAADF00D、0xD15EA5ED、0x8BADF00D、0xDEADC0DE、0xFACEFEED、0xBEEFCAFE、0xA5A5A5A5、0x5A5A5A5A、0xFFFF0000、0x0000FFFF。

哈希_整数 接受 键：
    可变 h 为 0x811C9DC5。
    h 为 (h 位异或 键 位与 0xFF) 乘 0x01000193。
    h 为 (h 位异或 键 右移 8 位与 0xFF) 乘 0x01000193。
    h 为 (h 位异或 键 右移 16 位与 0xFF) 乘 0x01000193。
    h 为 (h 位异或 键 右移 24 位与 0xFF) 乘 0x01000193。
    返还 h 位与 0x7FFFFFFF。
完。

哈希_双重 接受 键、种子：
    可变 h 为 种子。
    h 为 h 位异或 键。
    h 为 h 位异或 h 右移 16。
    h 为 h 乘 0x85EBCA6B。
    h 为 h 位异或 h 右移 13。
    h 为 h 乘 0xC2B2AE35。
    h 为 h 位异或 h 右移 16。
    返还 h 位与 0x7FFFFFFF。
完。

布隆_生成哈希 接受 键、索引、总位：
    h1 为 做 哈希_整数 用 键。
    h2 为 做 哈希_双重 用 键、哈希种子表 的 第 索引 取余于 16 项。
    返还 (h1 加 索引 乘 h2 位与 0x7FFFFFFF) 取余于 总位。
完。

布隆_设位 接受 位数组、位索引：
    字节索引 为 位索引 除以 8。
    位偏移 为 位索引 取余于 8。
    若 字节索引 小于 做 计数 用 位数组：
        做 设置 用 位数组、字节索引、位数组 的 第 字节索引 项 位或 1 左移 位偏移。
    完。
完。

布隆_测位 接受 位数组、位索引：
    字节索引 为 位索引 除以 8。
    位偏移 为 位索引 取余于 8。
    若 字节索引 小于 做 计数 用 位数组：
        返还 位数组 的 第 字节索引 项 位与 1 左移 位偏移 不等于 0。
    完。
    返还 假。
完。

布隆_清位 接受 位数组、位索引：
    字节索引 为 位索引 除以 8。
    位偏移 为 位索引 取余于 8。
    若 字节索引 小于 做 计数 用 位数组：
        做 设置 用 位数组、字节索引、位数组 的 第 字节索引 项 位与 按位取反 (1 左移 位偏移)。
    完。
完。

布隆_统计置位数 接受 位数组、总字节：
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 总字节：
        可变 b 为 位数组 的 第 i 项。
        只要 b 不等于 0：
            计数 为 计数 加 (b 位与 1)。
            b 为 b 右移 1。
        完。
        i 为 i 加 1。
    完。
    返还 计数。
完。

布隆_创建 接受 位数：
    若 位数 小于或等于 0：
        位数 为 布隆_默认位数。
    完。
    若 位数 大于 布隆_最大位数：
        位数 为 布隆_最大位数。
    完。
    字节数 为 (位数 加 7) 除以 8。
    可变 位数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 字节数：
        做 追加 用 位数组、0。
        i 为 i 加 1。
    完。
    全局布隆统计 的 总创建数 为 全局布隆统计 的 总创建数 加 1。
    返还 新建 布隆过滤器：位数组 为 位数组、总位数 为 位数、哈希函数数 为 布隆_默认哈希数、元素数 为 0、添加次数 为 0、查询次数 为 0、命中次数 为 0。
完。

布隆_创建_最优 接受 预期元素数、目标误判率千分比：
    若 预期元素数 小于或等于 0：
        预期元素数 为 1000。
    完。
    若 目标误判率千分比 小于或等于 0：
        目标误判率千分比 为 10。
    完。
    每元素位数 为 若 目标误判率千分比 小于或等于 1 则 24 否则 若 目标误判率千分比 小于或等于 10 则 14 否则 若 目标误判率千分比 小于或等于 50 则 10 否则 7。
    位数 为 预期元素数 乘 每元素位数。
    哈希数 为 每元素位数 乘 693 除以 1000。
    若 哈希数 小于 2：
        哈希数 为 2。
    完。
    若 哈希数 大于 布隆_最大哈希数：
        哈希数 为 布隆_最大哈希数。
    完。
    bf 为 做 布隆_创建 用 位数。
    bf 的 哈希函数数 为 哈希数。
    返还 bf。
完。

布隆_添加 接受 过滤器、键：
    若 过滤器 的 总位数 小于或等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 过滤器 的 哈希函数数：
        h 为 做 布隆_生成哈希 用 键、i、过滤器 的 总位数。
        做 布隆_设位 用 过滤器 的 位数组、h。
        i 为 i 加 1。
    完。
    过滤器 的 元素数 为 过滤器 的 元素数 加 1。
    过滤器 的 添加次数 为 过滤器 的 添加次数 加 1。
    全局布隆统计 的 总添加数 为 全局布隆统计 的 总添加数 加 1。
完。

布隆_可能存在 接受 过滤器、键：
    过滤器 的 查询次数 为 过滤器 的 查询次数 加 1。
    全局布隆统计 的 总查询数 为 全局布隆统计 的 总查询数 加 1。
    若 过滤器 的 总位数 小于或等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 过滤器 的 哈希函数数：
        h 为 做 布隆_生成哈希 用 键、i、过滤器 的 总位数。
        若 非 做 布隆_测位 用 过滤器 的 位数组、h：
            全局布隆统计 的 总未中数 为 全局布隆统计 的 总未中数 加 1。
            返还 假。
        完。
        i 为 i 加 1。
    完。
    过滤器 的 命中次数 为 过滤器 的 命中次数 加 1。
    全局布隆统计 的 总命中数 为 全局布隆统计 的 总命中数 加 1。
    返还 真。
完。

布隆_误判率估算 接受 过滤器：
    若 过滤器 的 总位数 等于 0：
        返还 1000。
    完。
    字节数 为 (过滤器 的 总位数 加 7) 除以 8。
    置位数 为 做 布隆_统计置位数 用 过滤器 的 位数组、字节数。
    填充率 为 置位数 乘 1000 除以 过滤器 的 总位数。
    可变 结果 为 填充率。
    可变 j 为 1。
    只要 j 小于 过滤器 的 哈希函数数：
        结果 为 结果 乘 填充率 除以 1000。
        j 为 j 加 1。
    完。
    返还 结果。
完。

布隆_重置 接受 过滤器：
    字节数 为 (过滤器 的 总位数 加 7) 除以 8。
    可变 i 为 0。
    只要 i 小于 字节数：
        做 设置 用 过滤器 的 位数组、i、0。
        i 为 i 加 1。
    完。
    过滤器 的 元素数 为 0。
完。

计数布隆_创建 接受 位数、哈希数：
    若 位数 小于或等于 0：
        位数 为 布隆_默认位数。
    完。
    若 哈希数 小于或等于 0：
        哈希数 为 布隆_默认哈希数。
    完。
    字节数 为 (位数 加 1) 除以 2。
    可变 计数数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 字节数：
        做 追加 用 计数数组、0。
        i 为 i 加 1。
    完。
    返还 新建 计数布隆过滤器：计数数组 为 计数数组、总位数 为 位数、哈希函数数 为 哈希数、元素数 为 0。
完。

计数布隆_获取计数 接受 数组、位索引：
    字节索引 为 位索引 除以 2。
    若 字节索引 大于或等于 做 计数 用 数组：
        返还 0。
    完。
    若 位索引 取余于 2 等于 0：
        返还 数组 的 第 字节索引 项 位与 0x0F。
    否则：
        返还 数组 的 第 字节索引 项 右移 4 位与 0x0F。
    完。
完。

计数布隆_设置计数 接受 数组、位索引、值：
    字节索引 为 位索引 除以 2。
    若 字节索引 大于或等于 做 计数 用 数组：
        返还。
    完。
    若 值 大于 计数布隆_最大值：
        值 为 计数布隆_最大值。
    完。
    若 位索引 取余于 2 等于 0：
        做 设置 用 数组、字节索引、数组 的 第 字节索引 项 位与 0xF0 位或 值 位与 0x0F。
    否则：
        做 设置 用 数组、字节索引、数组 的 第 字节索引 项 位与 0x0F 位或 (值 位与 0x0F) 左移 4。
    完。
完。

计数布隆_添加 接受 过滤器、键：
    若 过滤器 的 总位数 小于或等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 过滤器 的 哈希函数数：
        h 为 做 布隆_生成哈希 用 键、i、过滤器 的 总位数。
        当前 为 做 计数布隆_获取计数 用 过滤器 的 计数数组、h。
        做 计数布隆_设置计数 用 过滤器 的 计数数组、h、当前 加 1。
        i 为 i 加 1。
    完。
    过滤器 的 元素数 为 过滤器 的 元素数 加 1。
完。

计数布隆_删除 接受 过滤器、键：
    若 过滤器 的 总位数 小于或等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 过滤器 的 哈希函数数：
        h 为 做 布隆_生成哈希 用 键、i、过滤器 的 总位数。
        若 做 计数布隆_获取计数 用 过滤器 的 计数数组、h 等于 0：
            返还。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 过滤器 的 哈希函数数：
        h 为 做 布隆_生成哈希 用 键、i、过滤器 的 总位数。
        当前 为 做 计数布隆_获取计数 用 过滤器 的 计数数组、h。
        若 当前 大于 0：
            做 计数布隆_设置计数 用 过滤器 的 计数数组、h、当前 减 1。
        完。
        i 为 i 加 1。
    完。
    过滤器 的 元素数 为 过滤器 的 元素数 减 1。
完。

计数布隆_可能存在 接受 过滤器、键：
    若 过滤器 的 总位数 小于或等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 过滤器 的 哈希函数数：
        h 为 做 布隆_生成哈希 用 键、i、过滤器 的 总位数。
        若 做 计数布隆_获取计数 用 过滤器 的 计数数组、h 等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

可扩展布隆_创建 接受 零参：
    初始层 为 做 布隆_创建 用 可扩展_初始层位数。
    返还 新建 可扩展布隆过滤器：层列表 为 一组：初始层、层数 为 1、总元素数 为 0。
完。

可扩展布隆_添加 接受 过滤器、键：
    当前层 为 过滤器 的 层列表 的 第 过滤器 的 层数 减 1 项。
    填充率 为 做 布隆_误判率估算 用 当前层。
    若 填充率 大于 500 并且 过滤器 的 层数 小于 可扩展_最大层数：
        新位数 为 当前层 的 总位数 乘 可扩展_扩展因子。
        若 新位数 大于 布隆_最大位数：
            新位数 为 布隆_最大位数。
        完。
        新层 为 做 布隆_创建 用 新位数。
        做 追加 用 过滤器 的 层列表、新层。
        过滤器 的 层数 为 过滤器 的 层数 加 1。
        全局布隆统计 的 总扩展数 为 全局布隆统计 的 总扩展数 加 1。
        当前层 为 新层。
    完。
    做 布隆_添加 用 当前层、键。
    过滤器 的 总元素数 为 过滤器 的 总元素数 加 1。
完。

可扩展布隆_可能存在 接受 过滤器、键：
    可变 i 为 过滤器 的 层数 减 1。
    只要 i 大于或等于 0：
        若 做 布隆_可能存在 用 过滤器 的 层列表 的 第 i 项、键：
            返还 真。
        完。
        i 为 i 减 1。
    完。
    返还 假。
完。

BFOT_构建特征 接受 过滤器：
    可变 特征 为 做 数组_创建 用 BFOT_特征维度、0.0。
    特征 的 第 0 项 为 过滤器 的 总位数。
    特征 的 第 1 项 为 过滤器 的 哈希函数数。
    特征 的 第 2 项 为 过滤器 的 元素数。
    特征 的 第 3 项 为 做 布隆_误判率估算 用 过滤器。
    特征 的 第 4 项 为 过滤器 的 添加次数。
    特征 的 第 5 项 为 过滤器 的 查询次数。
    特征 的 第 6 项 为 过滤器 的 命中次数。
    字节数 为 (过滤器 的 总位数 加 7) 除以 8。
    特征 的 第 7 项 为 做 布隆_统计置位数 用 过滤器 的 位数组、字节数。
    返还 特征。
完。

BFOT_建议参数 接受 过滤器：
    bus 为 全局AI总线。
    若 bus 等于 0：
        填充率 为 做 布隆_误判率估算 用 过滤器。
        若 填充率 大于 500：
            返还 一本字典：扩展 对应 真、新位数 对应 过滤器 的 总位数 乘 2、新哈希数 对应 过滤器 的 哈希函数数。
        完。
        返还 一本字典：扩展 对应 假、新位数 对应 过滤器 的 总位数、新哈希数 对应 过滤器 的 哈希函数数。
    完。
    特征 为 做 BFOT_构建特征 用 过滤器。
    张量 为 做 Tensor_创建 用 1、BFOT_特征维度。
    可变 i 为 0。
    只要 i 小于 BFOT_特征维度：
        做 Tensor_写 用 张量、0、i、特征 的 第 i 项。
        i 为 i 加 1。
    完。
    预测 为 做 AI总线_推理 用 「BFOT_布隆参数」、张量。
    做 Tensor_释放 用 张量。
    若 预测 的 置信度 小于 BFOT_置信阈值：
        返还 一本字典：扩展 对应 假、新位数 对应 过滤器 的 总位数、新哈希数 对应 过滤器 的 哈希函数数。
    完。
    返还 一本字典：扩展 对应 预测 的 输出向量 的 第 0 项 大于或等于 0.5、新位数 对应 预测 的 输出向量 的 第 1 项、新哈希数 对应 预测 的 输出向量 的 第 2 项。
完。

哈希表_统计 接受 表：
    桶 为 表 的 第 0 项。
    容量 为 表 的 第 1 项。
    已用 为 表 的 第 2 项。
    可变 最长探测链 为 0。
    可变 当前链 为 0。
    可变 总探测长 为 0。
    可变 链数 为 0。
    可变 i 为 0。
    只要 i 小于 容量：
        若 桶 的 第 i 项 的 第 2 项 等于 真：
            当前链 为 当前链 加 1。
            若 当前链 大于 最长探测链：
                最长探测链 为 当前链。
            完。
        否则：
            若 当前链 大于 0：
                总探测长 为 总探测长 加 当前链。
                链数 为 链数 加 1。
            完。
            当前链 为 0。
        完。
        i 为 i 加 1。
    完。
    若 当前链 大于 0：
        总探测长 为 总探测长 加 当前链。
        链数 为 链数 加 1。
    完。
    平均链长 为 若 链数 大于 0 则 总探测长 除以 链数 否则 0。
    返还 一组：容量、已用、做 哈希表_负载因子 用 表、最长探测链、平均链长。
完。

哈希表_完整统计 接受 表：
    返还 做 哈希表_诊断 用 表。
完。

布隆_打印统计 接受 零参：
    做 内核打印 用 「═══ 布隆过滤器统计 ═══」。
    做 内核打印 用 「创建={} 添加={} 查询={} 命中={} 未中={} 扩展={}」、全局布隆统计 的 总创建数、全局布隆统计 的 总添加数、全局布隆统计 的 总查询数、全局布隆统计 的 总命中数、全局布隆统计 的 总未中数、全局布隆统计 的 总扩展数。
完。

布隆_重置统计 接受 零参：
    全局布隆统计 为 新建 布隆统计。
完。

布隆_合并 接受 过滤器A、过滤器B：
    若 过滤器A 的 总位数 不等于 过滤器B 的 总位数：
        返还 空。
    完。
    结果 为 做 布隆_创建 用 过滤器A 的 总位数。
    字节数 为 (过滤器A 的 总位数 加 7) 除以 8。
    可变 i 为 0。
    只要 i 小于 字节数：
        做 设置 用 结果 的 位数组、i、过滤器A 的 位数组 的 第 i 项 位或 过滤器B 的 位数组 的 第 i 项。
        i 为 i 加 1。
    完。
    结果 的 元素数 为 过滤器A 的 元素数 加 过滤器B 的 元素数。
    返还 结果。
完。

布隆_交集 接受 过滤器A、过滤器B：
    若 过滤器A 的 总位数 不等于 过滤器B 的 总位数：
        返还 空。
    完。
    结果 为 做 布隆_创建 用 过滤器A 的 总位数。
    字节数 为 (过滤器A 的 总位数 加 7) 除以 8。
    可变 i 为 0。
    只要 i 小于 字节数：
        做 设置 用 结果 的 位数组、i、过滤器A 的 位数组 的 第 i 项 位与 过滤器B 的 位数组 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

布隆_子集检查 接受 子集、超集：
    若 子集 的 总位数 不等于 超集 的 总位数：
        返还 假。
    完。
    字节数 为 (子集 的 总位数 加 7) 除以 8。
    可变 i 为 0。
    只要 i 小于 字节数：
        若 子集 的 位数组 的 第 i 项 位与 超集 的 位数组 的 第 i 项 不等于 子集 的 位数组 的 第 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

衰减布隆_创建 接受 位数、衰减周期：
    基础 为 做 计数布隆_创建 用 位数、布隆_默认哈希数。
    返还 一组：基础、衰减周期、0、0。
完。

衰减布隆_添加 接受 衰减、键、时间戳：
    做 计数布隆_添加 用 衰减 的 第 0 项、键。
    衰减 的 第 2 项 为 时间戳。
    衰减 的 第 3 项 为 衰减 的 第 3 项 加 1。
完。

衰减布隆_衰减一轮 接受 衰减：
    基础 为 衰减 的 第 0 项。
    字节数 为 (基础 的 总位数 加 1) 除以 2。
    可变 i 为 0。
    只要 i 小于 基础 的 总位数：
        当前 为 做 计数布隆_获取计数 用 基础 的 计数数组、i。
        若 当前 大于 0：
            做 计数布隆_设置计数 用 基础 的 计数数组、i、当前 减 1。
        完。
        i 为 i 加 1。
    完。
完。

衰减布隆_检查并衰减 接受 衰减、当前时间：
    若 当前时间 减 衰减 的 第 2 项 大于或等于 衰减 的 第 1 项：
        做 衰减布隆_衰减一轮 用 衰减。
        衰减 的 第 2 项 为 当前时间。
    完。
完。

衰减布隆_可能存在 接受 衰减、键、当前时间：
    做 衰减布隆_检查并衰减 用 衰减、当前时间。
    返还 做 计数布隆_可能存在 用 衰减 的 第 0 项、键。
完。

概率布隆_创建 接受 位数：
    概率数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 位数：
        做 追加 用 概率数组、0。
        i 为 i 加 1。
    完。
    返还 一组：概率数组、位数、布隆_默认哈希数、0。
完。

概率布隆_添加 接受 过滤器、键、置信度：
    可变 i 为 0。
    只要 i 小于 过滤器 的 第 2 项：
        h 为 做 布隆_生成哈希 用 键、i、过滤器 的 第 1 项。
        当前 为 过滤器 的 第 0 项 的 第 h 项。
        新值 为 当前 加 置信度 减 当前 乘 置信度 除以 1000。
        若 新值 大于 1000：
            新值 为 1000。
        完。
        过滤器 的 第 0 项 的 第 h 项 为 新值。
        i 为 i 加 1。
    完。
    过滤器 的 第 3 项 为 过滤器 的 第 3 项 加 1。
完。

概率布隆_查询概率 接受 过滤器、键：
    可变 联合 为 1000。
    可变 i 为 0。
    只要 i 小于 过滤器 的 第 2 项：
        h 为 做 布隆_生成哈希 用 键、i、过滤器 的 第 1 项。
        p 为 过滤器 的 第 0 项 的 第 h 项。
        联合 为 联合 乘 p 除以 1000。
        i 为 i 加 1。
    完。
    返还 联合。
完。

概率布隆_可能存在 接受 过滤器、键、阈值：
    返还 做 概率布隆_查询概率 用 过滤器、键 大于或等于 阈值。
完。

布隆_全局完整统计 接受 零参：
    返还 一组：「模块」、「哈希表_布隆过滤器」、「基础布隆函数」、8、「计数布隆函数」、5、「可扩展布隆函数」、3、「BFOT优化函数」、2、「哈希表统计函数」、3、「合并交集函数」、3、「时间衰减函数」、5、「概率布隆函数」、4、「总函数数」、33。
完。

本章 收束。
