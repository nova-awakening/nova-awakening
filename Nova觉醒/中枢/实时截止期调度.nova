《实时截止期调度》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·链表后继》。

引用《.·实时调度》。

引用《.·时钟中枢》。

DL参数 是 一种 事物，含 runtime_ns、deadline_ns、period_ns、当前runtime、当前deadline、超额。

DL运行队列 是 一种 事物，含 任务列表、任务数、总带宽。

可变 dl_rq 为 空。

可变 dl_调度次数 为 0。

DL调度器_初始化 接受 零参：
    dl_rq 为 新建 DL运行队列。
    dl_rq 的 任务列表 为 一组：（空）。
    dl_rq 的 任务数 为 0。
    dl_rq 的 总带宽 为 0。
    做 内核信息 用 「SCHED_DEADLINE: 初始化 (EDF+CBS)」。
完。

DL_入队 接受 任务、参数：
    可变 插入位 为 做 计数 用 dl_rq 的 任务列表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 dl_rq 的 任务列表：
        现有 为 dl_rq 的 任务列表 的 第 i 项。
        若 参数 的 当前deadline 小于 现有 的 第 1 项 的 当前deadline：
            插入位 为 i。
            中断。
        完。
        i 为 i 加 1。
    完。
    新项 为 一组：任务、参数。
    若 插入位 小于 做 计数 用 dl_rq 的 任务列表：
        做 追加 用 dl_rq 的 任务列表、新项。
        可变 j 为 做 计数 用 dl_rq 的 任务列表 减 1。
        只要 j 大于 插入位：
            临时 为 dl_rq 的 任务列表 的 第 j 减 1 项。
            做 设置 用 dl_rq 的 任务列表、j 减 1、dl_rq 的 任务列表 的 第 j 项。
            做 设置 用 dl_rq 的 任务列表、j、临时。
            j 为 j 减 1。
        完。
    否则：
        做 追加 用 dl_rq 的 任务列表、新项。
    完。
    dl_rq 的 任务数 为 dl_rq 的 任务数 加 1。
完。

DL_出队 接受 任务：
    可变 i 为 0。
    只要 i 小于 做 计数 用 dl_rq 的 任务列表：
        若 dl_rq 的 任务列表 的 第 i 项 的 第 0 项 等于 任务：
            做 链表_删除 用 dl_rq 的 任务列表、i。
            dl_rq 的 任务数 为 dl_rq 的 任务数 减 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

DL_选择下一个 接受 零参：
    若 dl_rq 的 任务数 等于 0：
        返还 空。
    完。
    可变 最佳 为 0。
    可变 最早deadline 为 dl_rq 的 任务列表 的 第 0 项 的 第 1 项 的 当前deadline。
    可变 i 为 1。
    只要 i 小于 做 计数 用 dl_rq 的 任务列表：
        dl 为 dl_rq 的 任务列表 的 第 i 项 的 第 1 项 的 当前deadline。
        若 dl 小于 最早deadline 并且 dl_rq 的 任务列表 的 第 i 项 的 第 1 项 的 超额 等于 0：
            最早deadline 为 dl。
            最佳 为 i。
        完。
        i 为 i 加 1。
    完。
    若 dl_rq 的 任务列表 的 第 最佳 项 的 第 1 项 的 超额 不等于 0：
        返还 空。
    完。
    dl_调度次数 为 dl_调度次数 加 1。
    返还 dl_rq 的 任务列表 的 第 最佳 项 的 第 0 项。
完。

DL_tick 接受 当前任务：
    若 当前任务 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 dl_rq 的 任务列表：
        若 dl_rq 的 任务列表 的 第 i 项 的 第 0 项 等于 当前任务：
            参数 为 dl_rq 的 任务列表 的 第 i 项 的 第 1 项。
            参数 的 当前runtime 为 参数 的 当前runtime 加 1000000。
            若 参数 的 当前runtime 大于或等于 参数 的 runtime_ns：
                参数 的 超额 为 1。
                做 进程_设置需要调度 用 当前任务。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

DL_补充 接受 零参：
    当前时间 为 做 读取纳秒。
    可变 i 为 0。
    只要 i 小于 做 计数 用 dl_rq 的 任务列表：
        参数 为 dl_rq 的 任务列表 的 第 i 项 的 第 1 项。
        若 当前时间 大于或等于 参数 的 当前deadline：
            参数 的 当前runtime 为 0。
            参数 的 当前deadline 为 参数 的 当前deadline 加 参数 的 period_ns。
            参数 的 超额 为 0。
        完。
        i 为 i 加 1。
    完。
完。

DL_检查带宽 接受 新runtime、新period：
    若 新period 小于或等于 0：
        返还 假。
    完。
    新带宽 为 新runtime 乘 1000 除以 新period。
    总 为 dl_rq 的 总带宽 加 新带宽。
    返还 总 小于或等于 950。
完。

DL_有任务 接受 零参：
    返还 dl_rq 的 任务数 大于 0。
完。

DL_统计 接受 零参：
    返还 一组：dl_调度次数、dl_rq 的 任务数、dl_rq 的 总带宽。
完。

DL参数_创建 接受 runtime_ns、deadline_ns、period_ns：
    p 为 新建 DL参数。
    p 的 runtime_ns 为 runtime_ns。
    p 的 deadline_ns 为 deadline_ns。
    p 的 period_ns 为 period_ns。
    p 的 当前runtime 为 0。
    p 的 当前deadline 为 做 读取纳秒 加 deadline_ns。
    p 的 超额 为 0。
    返还 p。
完。

DL参数_验证 接受 runtime、deadline、period：
    若 runtime 小于或等于 0：
        返还 假。
    完。
    若 deadline 小于或等于 0：
        返还 假。
    完。
    若 period 小于或等于 0：
        返还 假。
    完。
    若 runtime 大于 deadline：
        返还 假。
    完。
    若 deadline 大于 period：
        返还 假。
    完。
    返还 真。
完。

DL_最近截止时间 接受 零参：
    若 dl_rq 的 任务数 等于 0：
        返还 0。
    完。
    可变 最小 为 dl_rq 的 任务列表 的 第 0 项 的 第 1 项 的 当前deadline。
    可变 i 为 1。
    只要 i 小于 做 计数 用 dl_rq 的 任务列表：
        dl 为 dl_rq 的 任务列表 的 第 i 项 的 第 1 项 的 当前deadline。
        若 dl 小于 最小：
            最小 为 dl。
        完。
        i 为 i 加 1。
    完。
    返还 最小。
完。

DL_超额任务数 接受 零参：
    可变 计 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 dl_rq 的 任务列表：
        若 dl_rq 的 任务列表 的 第 i 项 的 第 1 项 的 超额 不等于 0：
            计 为 计 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计。
完。

DL_完整统计 接受 零参：
    返还 一组：dl_调度次数、dl_rq 的 任务数、dl_rq 的 总带宽、做 DL_超额任务数。
完。

可变 AI_截止期调甲_负载窗口 为 一组：（空）。

AI_截止期调甲_负载预测 接受 当前负载：
    做 追加 用 AI_截止期调甲_负载窗口、当前负载。
    若 做 计数 用 AI_截止期调甲_负载窗口 大于 20：
        做 列表_移除 用 AI_截止期调甲_负载窗口、0。
    完。
    若 做 计数 用 AI_截止期调甲_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_截止期调甲_负载窗口。
    趋势 为 AI_截止期调甲_负载窗口 的 第 n 减 1 项 减 AI_截止期调甲_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_截止期调甲_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
