《突触网格修剪》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·感知·内核直连层》。

引用《.·内核打印》。

引用《.·内存初始化》。

引用《.·内容可见性》。

引用《.·整数转字符串》。

突触_修剪阈值 为 50。

突触_衰减率 为 2。

突触_最大强度 为 10000。

突触_强化增量 为 100。

突触_最大连接 为 4096。

知识_最大跳数 为 5。

知识_最小置信度 为 300。

知识_衰减因子 为 80。

伙伴_最大阶 为 11。

TNF_错误率阈值 为 10。

TNF_权重上限 为 1000。

TNF_权重下限 为 负 1000。

TNF_自动重置阈值 为 25。

自动化统计 是 一种 事物，含 修剪总数、强化总数、推理步数、碎片整理数、TNF自检数、TNF重置数、tick计数、上次修剪时间、上次碎片整理时间、上次推理时间。

可变 全局_自动化统计 为 空。

自动化_初始化 接受 零参：
    若 全局_自动化统计 等于 空：
        全局_自动化统计 为 做 堆_分配 用 自动化统计。
        全局_自动化统计 的 修剪总数 为 0。
        全局_自动化统计 的 强化总数 为 0。
        全局_自动化统计 的 推理步数 为 0。
        全局_自动化统计 的 碎片整理数 为 0。
        全局_自动化统计 的 TNF自检数 为 0。
        全局_自动化统计 的 TNF重置数 为 0。
        全局_自动化统计 的 tick计数 为 0。
        全局_自动化统计 的 上次修剪时间 为 0。
        全局_自动化统计 的 上次碎片整理时间 为 0。
        全局_自动化统计 的 上次推理时间 为 0。
    完。
完。

突触网格_修剪弱连接 接受 零参：
    做 自动化_初始化。
    做 内核调试 用 「自动化: 突触网格弱连接修剪」。
    若 突触网格 等于 空 或者 突触网格 的 连接数 等于 0：
        返还 0。
    完。
    可变 修剪数 为 0。
    可变 i 为 0。
    只要 i 小于 突触网格 的 连接数：
        偏移 为 突触网格 的 连接列表 加 i 乘 16。
        强度 为 做 内存_读32 用 偏移 加 8。
        最后活跃 为 做 内存_读32 用 偏移 加 12。
        若 强度 小于 突触_修剪阈值：
            做 突触网格_移除连接 用 突触网格、i。
            修剪数 为 修剪数 加 1。
        否则：
            新强度 为 强度 减 突触_衰减率。
            若 新强度 小于 0：
                新强度 为 0。
            完。
            做 内存_写32 用 偏移 加 8、新强度。
            i 为 i 加 1。
        完。
    完。
    全局_自动化统计 的 修剪总数 为 全局_自动化统计 的 修剪总数 加 修剪数。
    全局_自动化统计 的 上次修剪时间 为 做 平台_获取高精度时间。
    若 修剪数 大于 0：
        做 内核调试 用 「自动化: 修剪了 」 加 做 整数转字符串 用 修剪数 加 「 个弱连接, 剩余 」 加 做 整数转字符串 用 突触网格 的 连接数。
    完。
    返还 修剪数。
完。

突触网格_移除连接 接受 网格、索引：
    若 索引 小于 0 或者 索引 大于或等于 网格 的 连接数：
        返还。
    完。
    目标 为 网格 的 连接列表 加 索引 乘 16。
    最后 为 网格 的 连接列表 加 (网格 的 连接数 减 1) 乘 16。
    若 索引 小于 网格 的 连接数 减 1：
        做 内存_拷贝 用 目标、最后、16。
    完。
    网格 的 连接数 为 网格 的 连接数 减 1。
完。

突触网格_强化连接 接受 源、目标：
    做 自动化_初始化。
    若 突触网格 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 突触网格 的 连接数：
        偏移 为 突触网格 的 连接列表 加 i 乘 16。
        连接源 为 做 内存_读32 用 偏移。
        连接目标 为 做 内存_读32 用 偏移 加 4。
        若 连接源 等于 源 并且 连接目标 等于 目标：
            强度 为 做 内存_读32 用 偏移 加 8。
            强度 为 强度 加 突触_强化增量。
            若 强度 大于 突触_最大强度：
                强度 为 突触_最大强度。
            完。
            做 内存_写32 用 偏移 加 8、强度。
            做 内存_写32 用 偏移 加 12、做 平台_获取高精度时间 位与 0xFFFFFFFF。
            全局_自动化统计 的 强化总数 为 全局_自动化统计 的 强化总数 加 1。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 突触网格 的 连接数 小于 突触_最大连接：
        偏移 为 突触网格 的 连接列表 加 突触网格 的 连接数 乘 16。
        做 内存_写32 用 偏移、源。
        做 内存_写32 用 偏移 加 4、目标。
        做 内存_写32 用 偏移 加 8、突触_强化增量。
        做 内存_写32 用 偏移 加 12、做 平台_获取高精度时间 位与 0xFFFFFFFF。
        突触网格 的 连接数 为 突触网格 的 连接数 加 1。
    完。
完。

突触网格_查询强度 接受 源、目标：
    若 突触网格 等于 空：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 突触网格 的 连接数：
        偏移 为 突触网格 的 连接列表 加 i 乘 16。
        若 做 内存_读32 用 偏移 等于 源 并且 做 内存_读32 用 偏移 加 4 等于 目标：
            返还 做 内存_读32 用 偏移 加 8。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

突触网格_统计 接受 零参：
    若 突触网格 等于 空：
        返还 一组：0、0、0。
    完。
    可变 总强度 为 0。
    可变 最大强度 为 0。
    可变 i 为 0。
    只要 i 小于 突触网格 的 连接数：
        强度 为 做 内存_读32 用 突触网格 的 连接列表 加 i 乘 16 加 8。
        总强度 为 总强度 加 强度。
        若 强度 大于 最大强度：
            最大强度 为 强度。
        完。
        i 为 i 加 1。
    完。
    平均 为 0。
    若 突触网格 的 连接数 大于 0：
        平均 为 总强度 除以 突触网格 的 连接数。
    完。
    返还 一组：突触网格 的 连接数、平均、最大强度。
完。

知识图谱_推理一步 接受 零参：
    做 自动化_初始化。
    做 内核调试 用 「自动化: 知识图谱推理一步」。
    若 知识图谱 等于 空 或者 知识图谱 的 节点数 等于 0：
        返还 0。
    完。
    可变 新关系数 为 0。
    可变 i 为 0。
    只要 i 小于 知识图谱 的 节点数：
        节点 为 做 知识图谱_获取节点 用 知识图谱、i。
        若 节点 等于 空：
            i 为 i 加 1。
            继续。
        完。
        邻居列表 为 做 知识图谱_获取邻居 用 知识图谱、i。
        若 邻居列表 等于 空：
            i 为 i 加 1。
            继续。
        完。
        可变 j 为 0。
        只要 j 小于 做 计数 用 邻居列表：
            邻居 为 邻居列表 的 第 j 项。
            二阶邻居 为 做 知识图谱_获取邻居 用 知识图谱、邻居。
            若 二阶邻居 不等于 空：
                可变 k 为 0。
                只要 k 小于 做 计数 用 二阶邻居：
                    目标 为 二阶邻居 的 第 k 项。
                    若 目标 不等于 i 并且 非 做 知识图谱_有边 用 知识图谱、i、目标：
                        强度A 为 做 知识图谱_边权重 用 知识图谱、i、邻居。
                        强度B 为 做 知识图谱_边权重 用 知识图谱、邻居、目标。
                        推理置信度 为 强度A 乘 强度B 乘 知识_衰减因子 除以 10000。
                        若 推理置信度 大于或等于 知识_最小置信度：
                            做 知识图谱_添加边 用 知识图谱、i、目标、推理置信度。
                            新关系数 为 新关系数 加 1。
                        完。
                    完。
                    k 为 k 加 1。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    全局_自动化统计 的 推理步数 为 全局_自动化统计 的 推理步数 加 1。
    全局_自动化统计 的 上次推理时间 为 做 平台_获取高精度时间。
    若 新关系数 大于 0：
        做 内核调试 用 「自动化: 知识图谱推理发现 」 加 做 整数转字符串 用 新关系数 加 「 个新关系」。
    完。
    返还 新关系数。
完。

知识图谱_多跳推理 接受 起点、终点、最大跳数：
    若 知识图谱 等于 空：
        返还 空。
    完。
    若 最大跳数 大于 知识_最大跳数：
        最大跳数 为 知识_最大跳数。
    完。
    可变 路径 为 一组：起点。
    可变 已访问 为 一组：（空）。
    做 追加 用 已访问、起点。
    可变 当前 为 起点。
    可变 跳数 为 0。
    只要 跳数 小于 最大跳数 并且 当前 不等于 终点：
        邻居 为 做 知识图谱_获取邻居 用 知识图谱、当前。
        若 邻居 等于 空 或者 做 计数 用 邻居 等于 0：
            返还 空。
        完。
        可变 最佳 为 负 1。
        可变 最佳权重 为 0。
        可变 n 为 0。
        只要 n 小于 做 计数 用 邻居：
            候选 为 邻居 的 第 n 项。
            可变 已走过 为 假。
            可变 v 为 0。
            只要 v 小于 做 计数 用 已访问：
                若 已访问 的 第 v 项 等于 候选：
                    已走过 为 真。
                    跳出。
                完。
                v 为 v 加 1。
            完。
            若 非 已走过：
                权重 为 做 知识图谱_边权重 用 知识图谱、当前、候选。
                若 候选 等于 终点：
                    权重 为 权重 加 10000。
                完。
                若 权重 大于 最佳权重：
                    最佳 为 候选。
                    最佳权重 为 权重。
                完。
            完。
            n 为 n 加 1。
        完。
        若 最佳 小于 0：
            返还 空。
        完。
        做 追加 用 路径、最佳。
        做 追加 用 已访问、最佳。
        当前 为 最佳。
        跳数 为 跳数 加 1。
    完。
    若 当前 等于 终点：
        返还 路径。
    完。
    返还 空。
完。

外部。

伙伴_空闲链表。

伙伴_碎片整理 接受 零参：
    做 自动化_初始化。
    若 伙伴_空闲链表 等于 空：
        返还 0。
    完。
    可变 合并总数 为 0。
    可变 阶 为 0。
    只要 阶 小于 伙伴_最大阶 减 1：
        若 阶 大于或等于 做 计数 用 伙伴_空闲链表：
            跳出。
        完。
        链表 为 伙伴_空闲链表 的 第 阶 项。
        若 链表 等于 空：
            阶 为 阶 加 1。
            继续。
        完。
        块数 为 做 计数 用 链表。
        若 块数 小于 2：
            阶 为 阶 加 1。
            继续。
        完。
        块大小 为 4096 左移 阶。
        可变 i 为 0。
        只要 i 小于 做 计数 用 链表 减 1：
            地址A 为 链表 的 第 i 项。
            伙伴地址 为 地址A 位异或 块大小。
            可变 j 为 i 加 1。
            只要 j 小于 做 计数 用 链表：
                若 链表 的 第 j 项 等于 伙伴地址：
                    合并地址 为 地址A。
                    若 伙伴地址 小于 地址A：
                        合并地址 为 伙伴地址。
                    完。
                    做 伙伴_从链表移除 用 链表、j。
                    做 伙伴_从链表移除 用 链表、i。
                    若 阶 加 1 小于 做 计数 用 伙伴_空闲链表：
                        做 追加 用 伙伴_空闲链表 的 第 阶 加 1 项、合并地址。
                    完。
                    合并总数 为 合并总数 加 1。
                    跳出。
                完。
                j 为 j 加 1。
            完。
            i 为 i 加 1。
        完。
        阶 为 阶 加 1。
    完。
    全局_自动化统计 的 碎片整理数 为 全局_自动化统计 的 碎片整理数 加 合并总数。
    全局_自动化统计 的 上次碎片整理时间 为 做 平台_获取高精度时间。
    若 合并总数 大于 0：
        做 内核调试 用 「自动化: 伙伴碎片整理合并 」 加 做 整数转字符串 用 合并总数 加 「 对块」。
    完。
    返还 合并总数。
完。

伙伴_从链表移除 接受 链表、索引：
    若 索引 小于 0 或者 索引 大于或等于 做 计数 用 链表：
        返还。
    完。
    最后 为 做 计数 用 链表 减 1。
    若 索引 小于 最后：
        做 设置 用 链表、索引、链表 的 第 最后 项。
    完。
    做 删除 用 链表、最后。
完。

伙伴_碎片率 接受 零参：
    若 伙伴_空闲链表 等于 空：
        返还 0。
    完。
    可变 低阶计数 为 0。
    可变 高阶计数 为 0。
    可变 阶 为 0。
    只要 阶 小于 做 计数 用 伙伴_空闲链表：
        n 为 做 计数 用 伙伴_空闲链表 的 第 阶 项。
        若 阶 小于 3：
            低阶计数 为 低阶计数 加 n。
        否则：
            高阶计数 为 高阶计数 加 n。
        完。
        阶 为 阶 加 1。
    完。
    总数 为 低阶计数 加 高阶计数。
    若 总数 等于 0：
        返还 0。
    完。
    返还 低阶计数 乘 100 除以 总数。
完。

外部。

TNF_推理次数。

外部。

TNF_错误次数。

外部。

TNF_权重矩阵。

TNF_自检 接受 零参：
    做 自动化_初始化。
    全局_自动化统计 的 TNF自检数 为 全局_自动化统计 的 TNF自检数 加 1。
    若 TNF_推理次数 等于 0：
        返还 真。
    完。
    错误率 为 TNF_错误次数 乘 100 除以 TNF_推理次数。
    若 错误率 大于或等于 TNF_自动重置阈值：
        做 内核警告 用 「自动化: TNF错误率 」 加 做 整数转字符串 用 错误率 加 「% 超过自动重置阈值」。
        做 TNF_重置。
        返还 假。
    完。
    若 错误率 大于或等于 TNF_错误率阈值：
        做 内核警告 用 「自动化: TNF错误率偏高 」 加 做 整数转字符串 用 错误率 加 「%」。
        返还 假。
    完。
    返还 真。
完。

TNF_重置 接受 零参：
    做 自动化_初始化。
    做 内核警告 用 「自动化: TNF推理引擎重置」。
    全局_自动化统计 的 TNF重置数 为 全局_自动化统计 的 TNF重置数 加 1。
    若 TNF_权重矩阵 不等于 空：
        可变 钳位数 为 0。
        可变 i 为 0。
        只要 i 小于 做 计数 用 TNF_权重矩阵：
            若 TNF_权重矩阵 的 第 i 项 大于 TNF_权重上限：
                做 设置 用 TNF_权重矩阵、i、TNF_权重上限。
                钳位数 为 钳位数 加 1。
            完。
            若 TNF_权重矩阵 的 第 i 项 小于 TNF_权重下限：
                做 设置 用 TNF_权重矩阵、i、TNF_权重下限。
                钳位数 为 钳位数 加 1。
            完。
            i 为 i 加 1。
        完。
        若 钳位数 大于 0：
            做 内核调试 用 「自动化: 钳位了 」 加 做 整数转字符串 用 钳位数 加 「 个异常权重」。
        完。
    完。
    TNF_错误次数 为 0。
完。

TNF_健康报告 接受 零参：
    可变 总权重 为 0。
    可变 最大权重 为 0。
    可变 零权重 为 0。
    可变 权重数 为 0。
    若 TNF_权重矩阵 不等于 空：
        权重数 为 做 计数 用 TNF_权重矩阵。
        可变 i 为 0。
        只要 i 小于 权重数：
            w 为 TNF_权重矩阵 的 第 i 项。
            绝对值 为 w。
            若 绝对值 小于 0：
                绝对值 为 负 绝对值。
            完。
            总权重 为 总权重 加 绝对值。
            若 绝对值 大于 最大权重：
                最大权重 为 绝对值。
            完。
            若 w 等于 0：
                零权重 为 零权重 加 1。
            完。
            i 为 i 加 1。
        完。
    完。
    做 打印行 用 「=== TNF健康报告 ===」。
    做 打印行 用 「推理次数: 」 加 做 整数转字符串 用 TNF_推理次数。
    做 打印行 用 「错误次数: 」 加 做 整数转字符串 用 TNF_错误次数。
    若 TNF_推理次数 大于 0：
        做 打印行 用 「错误率: 」 加 做 整数转字符串 用 TNF_错误次数 乘 100 除以 TNF_推理次数 加 「%」。
    完。
    做 打印行 用 「权重数量: 」 加 做 整数转字符串 用 权重数。
    若 权重数 大于 0：
        做 打印行 用 「平均权重: 」 加 做 整数转字符串 用 总权重 除以 权重数。
        做 打印行 用 「最大权重: 」 加 做 整数转字符串 用 最大权重。
        做 打印行 用 「稀疏度: 」 加 做 整数转字符串 用 零权重 乘 100 除以 权重数 加 「%」。
    完。
完。

可变 自动化_tick计数 为 0。

自动化_tick 接受 零参：
    做 自动化_初始化。
    自动化_tick计数 为 自动化_tick计数 加 1。
    全局_自动化统计 的 tick计数 为 自动化_tick计数。
    若 自动化_tick计数 取余于 10 等于 0：
        做 突触网格_修剪弱连接。
    完。
    若 自动化_tick计数 取余于 50 等于 0：
        做 知识图谱_推理一步。
    完。
    若 自动化_tick计数 取余于 100 等于 0：
        做 伙伴_碎片整理。
    完。
    若 自动化_tick计数 取余于 200 等于 0：
        做 TNF_自检。
    完。
完。

自动化_完整统计 接受 零参：
    做 自动化_初始化。
    返还 一组：TNF_推理次数、TNF_错误次数、自动化_tick计数、全局_自动化统计 的 修剪总数、全局_自动化统计 的 强化总数、全局_自动化统计 的 碎片整理数、全局_自动化统计 的 TNF重置数。
完。

可变 自适应_修剪阈值 为 50。

自适应_高水位 为 3072。

自适应_低水位 为 512。

自动化_自适应修剪阈值 接受 零参：
    若 突触网格 等于 空：
        返还。
    完。
    当前连接数 为 突触网格 的 连接数。
    若 当前连接数 大于 自适应_高水位：
        自适应_修剪阈值 为 自适应_修剪阈值 加 10。
        若 自适应_修剪阈值 大于 500：
            自适应_修剪阈值 为 500。
        完。
        做 内核调试 用 「自动化: 连接过载, 提高修剪阈值至 」 加 做 整数转字符串 用 自适应_修剪阈值。
    完。
    做 否则如果 用 当前连接数 小于 自适应_低水位。
    块：
        自适应_修剪阈值 为 自适应_修剪阈值 减 5。
        若 自适应_修剪阈值 小于 10：
            自适应_修剪阈值 为 10。
        完。
    完。
完。

突触网格_连接重要性 接受 源、目标：
    若 突触网格 等于 空：
        返还 0。
    完。
    强度 为 做 突触网格_查询强度 用 源、目标。
    若 强度 等于 0：
        返还 0。
    完。
    可变 源度 为 0。
    可变 目标度 为 0。
    可变 i 为 0。
    只要 i 小于 突触网格 的 连接数：
        偏移 为 突触网格 的 连接列表 加 i 乘 16。
        s 为 做 内存_读32 用 偏移。
        d 为 做 内存_读32 用 偏移 加 4。
        若 s 等于 源 或者 d 等于 源：
            源度 为 源度 加 1。
        完。
        若 s 等于 目标 或者 d 等于 目标：
            目标度 为 目标度 加 1。
        完。
        i 为 i 加 1。
    完。
    中心度 为 (源度 加 目标度) 除以 2。
    若 中心度 等于 0：
        中心度 为 1。
    完。
    补偿 为 突触_最大强度 除以 中心度。
    返还 强度 加 补偿。
完。

突触网格_健康指标 接受 零参：
    若 突触网格 等于 空：
        返还 一组：0、0、0、0。
    完。
    连接数 为 突触网格 的 连接数。
    统计 为 做 突触网格_统计。
    平均强度 为 统计 的 第 1 项。
    最大强度 为 统计 的 第 2 项。
    密度分 为 连接数 乘 100 除以 突触_最大连接。
    可变 均衡度 为 0。
    若 最大强度 大于 0：
        均衡度 为 平均强度 乘 100 除以 最大强度。
    完。
    可变 非零数 为 0。
    可变 i 为 0。
    只要 i 小于 连接数：
        强度 为 做 内存_读32 用 突触网格 的 连接列表 加 i 乘 16 加 8。
        若 强度 大于 0：
            非零数 为 非零数 加 1。
        完。
        i 为 i 加 1。
    完。
    活力分 为 0。
    若 连接数 大于 0：
        活力分 为 非零数 乘 100 除以 连接数。
    完。
    返还 一组：密度分、均衡度、活力分、连接数。
完。

自动化_打印统计 接受 零参：
    做 自动化_初始化。
    做 打印行 用 「=== 内核自动化统计 ===」。
    做 打印行 用 「Tick次数: 」 加 做 整数转字符串 用 全局_自动化统计 的 tick计数。
    做 打印行 用 「突触修剪: 」 加 做 整数转字符串 用 全局_自动化统计 的 修剪总数。
    做 打印行 用 「突触强化: 」 加 做 整数转字符串 用 全局_自动化统计 的 强化总数。
    做 打印行 用 「推理步数: 」 加 做 整数转字符串 用 全局_自动化统计 的 推理步数。
    做 打印行 用 「碎片整理: 」 加 做 整数转字符串 用 全局_自动化统计 的 碎片整理数。
    做 打印行 用 「TNF自检: 」 加 做 整数转字符串 用 全局_自动化统计 的 TNF自检数。
    做 打印行 用 「TNF重置: 」 加 做 整数转字符串 用 全局_自动化统计 的 TNF重置数。
    做 打印行 用 「碎片率: 」 加 做 整数转字符串 用 做 伙伴_碎片率 加 「%」。
    做 打印行 用 「自适应修剪阈值: 」 加 做 整数转字符串 用 自适应_修剪阈值。
    健康 为 做 突触网格_健康指标。
    做 打印行 用 「密度分: 」 加 做 整数转字符串 用 健康 的 第 0 项 加 「%」。
    做 打印行 用 「均衡度: 」 加 做 整数转字符串 用 健康 的 第 1 项 加 「%」。
    做 打印行 用 「活力分: 」 加 做 整数转字符串 用 健康 的 第 2 项 加 「%」。
完。

本章 收束。
