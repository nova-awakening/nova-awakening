《多轮认知》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·感知·内核直连层》。

引用《.·内存初始化》。

最大脑区 为 16。

最大连接 为 64。

能量基数 为 1024。

衰减率 为 950。

抑制率 为 200。

兴奋阈值 为 600。

脑区 是 一种 事物，含 类型、激活度、输出值、能量、阈值、连接数、连接目标、连接权重、累计激活、触发次数。

可变 脑区表 为 一组：（空）。

可变 脑区数 为 0。

可变 认知轮次 为 0。

可变 总能量 为 0。

可变 注意力核 为 负 1。

可变 注意力强 为 0。

认知_初始化 接受 零参：
    脑区表 为 一组：（空）。
    脑区数 为 0。
    认知轮次 为 0。
    注意力核 为 负 1。
完。

脑区_注册 接受 类型、初始能量：
    若 脑区数 大于或等于 最大脑区：
        返还 负 1。
    完。
    区 为 新建 脑区。
    区 的 类型 为 类型。
    区 的 激活度 为 0。
    区 的 输出值 为 0。
    区 的 能量 为 初始能量。
    区 的 阈值 为 兴奋阈值。
    区 的 连接数 为 0。
    区 的 连接目标 为 做 堆_分配 用 最大连接 乘 4。
    区 的 连接权重 为 做 堆_分配 用 最大连接 乘 4。
    区 的 累计激活 为 0。
    区 的 触发次数 为 0。
    做 追加 用 脑区表、区。
    索引 为 脑区数。
    脑区数 为 脑区数 加 1。
    返还 索引。
完。

脑区_连接 接受 源、目标、权重：
    若 源 大于或等于 脑区数 或者 目标 大于或等于 脑区数：
        返还。
    完。
    区 为 脑区表 的 第 源 项。
    若 区 的 连接数 大于或等于 最大连接：
        返还。
    完。
    做 内存_写32 用 区 的 连接目标 加 区 的 连接数 乘 4、目标。
    做 内存_写32 用 区 的 连接权重 加 区 的 连接数 乘 4、权重。
    区 的 连接数 为 区 的 连接数 加 1。
完。

认知_周期 接受 外部输入、输入数：
    认知轮次 为 认知轮次 加 1。
    可变 序号 为 0。
    只要 序号 小于 脑区数：
        脑区表 的 第 序号 项 的 激活度 为 脑区表 的 第 序号 项 的 激活度 乘 衰减率 除以 能量基数。
        序号 为 序号 加 1。
    完。
    序号 为 0。
    只要 序号 小于 输入数 并且 序号 小于 脑区数：
        脑区表 的 第 序号 项 的 激活度 为 脑区表 的 第 序号 项 的 激活度 加 做 内存_读32 用 外部输入 加 序号 乘 4。
        序号 为 序号 加 1。
    完。
    序号 为 0。
    只要 序号 小于 脑区数：
        区 为 脑区表 的 第 序号 项。
        若 区 的 激活度 大于或等于 区 的 阈值：
            区 的 输出值 为 区 的 激活度 减 区 的 阈值。
            区 的 触发次数 为 区 的 触发次数 加 1。
            可变 连序 为 0。
            只要 连序 小于 区 的 连接数：
                目标 为 做 内存_读32 用 区 的 连接目标 加 连序 乘 4。
                权重 为 做 内存_读32 用 区 的 连接权重 加 连序 乘 4。
                若 目标 小于 脑区数：
                    传递 为 区 的 输出值 乘 权重 除以 能量基数。
                    若 权重 大于 0：
                        脑区表 的 第 目标 项 的 激活度 为 脑区表 的 第 目标 项 的 激活度 加 传递。
                    否则：
                        脑区表 的 第 目标 项 的 激活度 为 脑区表 的 第 目标 项 的 激活度 减 (0 减 传递)。
                        若 脑区表 的 第 目标 项 的 激活度 小于 0：
                            脑区表 的 第 目标 项 的 激活度 为 0。
                        完。
                    完。
                完。
                连序 为 连序 加 1。
            完。
            区 的 能量 为 区 的 能量 减 区 的 输出值 除以 10。
            若 区 的 能量 小于 0：
                区 的 能量 为 0。
            完。
        否则：
            区 的 输出值 为 0。
        完。
        区 的 累计激活 为 区 的 累计激活 加 区 的 激活度。
        序号 为 序号 加 1。
    完。
    做 认知_更新注意力。
    做 认知_侧抑制。
完。

认知_更新注意力 接受 零参：
    可变 最高 为 0。
    可变 最高激活 为 0。
    可变 序号 为 0。
    只要 序号 小于 脑区数：
        若 脑区表 的 第 序号 项 的 激活度 大于 最高激活：
            最高激活 为 脑区表 的 第 序号 项 的 激活度。
            最高 为 序号。
        完。
        序号 为 序号 加 1。
    完。
    注意力核 为 最高。
    注意力强 为 最高激活。
完。

认知_侧抑制 接受 零参：
    若 注意力核 小于 0：
        返还。
    完。
    可变 序号 为 0。
    只要 序号 小于 脑区数：
        若 序号 不等于 注意力核：
            脑区表 的 第 序号 项 的 激活度 为 脑区表 的 第 序号 项 的 激活度 乘 (能量基数 减 抑制率) 除以 能量基数。
        完。
        序号 为 序号 加 1。
    完。
完。

多轮认知 接受 轮数、外部输入、输入数：
    可变 胜者表 为 做 堆_分配 用 轮数 乘 4。
    可变 轮 为 0。
    只要 轮 小于 轮数：
        做 认知_周期 用 外部输入、输入数。
        做 内存_写32 用 胜者表 加 轮 乘 4、注意力核。
        轮 为 轮 加 1。
    完。
    返还 胜者表。
完。

认知_创建默认 接受 零参：
    做 认知_初始化。
    做 脑区_注册 用 1、80。
    做 脑区_注册 用 2、60。
    做 脑区_注册 用 3、90。
    做 脑区_注册 用 4、70。
    做 脑区_注册 用 5、50。
    做 脑区_注册 用 6、100。
    做 脑区_连接 用 0、2、800。
    做 脑区_连接 用 0、1、600。
    做 脑区_连接 用 1、2、700。
    做 脑区_连接 用 2、3、900。
    做 脑区_连接 用 3、5、850。
    做 脑区_连接 用 4、3、300。
    做 脑区_连接 用 4、5、负 200。
    做 脑区_连接 用 5、0、100。
完。

认知_能量补充 接受 量：
    可变 序号 为 0。
    只要 序号 小于 脑区数：
        脑区表 的 第 序号 项 的 能量 为 脑区表 的 第 序号 项 的 能量 加 量 除以 脑区数。
        若 脑区表 的 第 序号 项 的 能量 大于 能量基数：
            脑区表 的 第 序号 项 的 能量 为 能量基数。
        完。
        序号 为 序号 加 1。
    完。
    总能量 为 总能量 加 量。
完。

注意力_焦点 接受 零参：
    返还 注意力核。
完。

注意力_强度 接受 零参：
    返还 注意力强。
完。

脑区_获取激活 接受 索引：
    若 索引 大于或等于 脑区数：
        返还 0。
    完。
    返还 脑区表 的 第 索引 项 的 激活度。
完。

脑区_获取输出 接受 索引：
    若 索引 大于或等于 脑区数：
        返还 0。
    完。
    返还 脑区表 的 第 索引 项 的 输出值。
完。

神经网络_完整统计 接受 零参：
    返还 一组：脑区数、认知轮次、注意力核、注意力强、总能量。
完。

归一化指数_数值稳定 接受 scores、n：
    若 scores 等于 0 或者 n 等于 0：
        返还。
    完。
    可变 max_s 为 做 内存_读32有符号 用 scores。
    可变 i 为 1。
    只要 i 小于 n：
        v 为 做 内存_读32有符号 用 scores 加 i 乘 4。
        若 v 大于 max_s：
            max_s 为 v。
        完。
        i 为 i 加 1。
    完。
    可变 sum_exp 为 0。
    i 为 0。
    只要 i 小于 n：
        v 为 做 内存_读32有符号 用 scores 加 i 乘 4。
        e 为 做 Q_exp 用 v 减 max_s。
        做 内存_写32 用 scores 加 i 乘 4、e。
        sum_exp 为 sum_exp 加 e。
        i 为 i 加 1。
    完。
    若 sum_exp 等于 0：
        返还。
    完。
    i 为 0。
    只要 i 小于 n：
        e 为 做 内存_读32有符号 用 scores 加 i 乘 4。
        做 内存_写32 用 scores 加 i 乘 4、做 Q除 用 e 乘 Q_ONE、sum_exp。
        i 为 i 加 1。
    完。
完。

本章 收束。
