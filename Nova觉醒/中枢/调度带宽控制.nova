《调度带宽控制》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·墙钟设置》。

BW_配额 为 0。

BW_周期 为 1。

BW_剩余运行时间 为 2。

BW_已用时间 为 3。

BW_节流次数 为 4。

BW_已节流 为 5。

BW_周期开始 为 6。

BW_默认周期 为 100000。

BW_无限配额 为 负 1。

可变 带宽组列表 为 一组：（空）。

带宽组_创建 接受 配额、周期：
    若 周期 小于或等于 0：
        周期 为 BW_默认周期。
    完。
    组 为 一组：配额、周期、配额、0、0、假、0。
    做 追加 用 带宽组列表、组。
    返还 组。
完。

带宽组_不限制 接受 零参：
    返还 做 带宽组_创建 用 BW_无限配额、BW_默认周期。
完。

带宽组_已启用 接受 组：
    返还 组 的 第 BW_配额 项 不等于 BW_无限配额。
完。

带宽_消耗时间 接受 组、delta_us：
    若 非 做 带宽组_已启用 用 组：
        返还。
    完。
    做 设置 用 组、BW_剩余运行时间、组 的 第 BW_剩余运行时间 项 减 delta_us。
    做 设置 用 组、BW_已用时间、组 的 第 BW_已用时间 项 加 delta_us。
完。

带宽_需要节流 接受 组：
    若 非 做 带宽组_已启用 用 组：
        返还 假。
    完。
    返还 组 的 第 BW_剩余运行时间 项 小于或等于 0。
完。

带宽_节流 接受 组：
    若 组 的 第 BW_已节流 项：
        返还。
    完。
    做 设置 用 组、BW_已节流、真。
    做 设置 用 组、BW_节流次数、组 的 第 BW_节流次数 项 加 1。
完。

带宽_解除节流 接受 组：
    若 非 组 的 第 BW_已节流 项：
        返还。
    完。
    做 设置 用 组、BW_已节流、假。
完。

带宽_周期定时器 接受 组：
    若 非 做 带宽组_已启用 用 组：
        返还。
    完。
    做 设置 用 组、BW_剩余运行时间、组 的 第 BW_配额 项。
    做 设置 用 组、BW_已用时间、0。
    做 设置 用 组、BW_周期开始、做 当前时间_us。
    若 组 的 第 BW_已节流 项：
        做 带宽_解除节流 用 组。
    完。
完。

带宽_检查周期 接受 组：
    若 非 做 带宽组_已启用 用 组：
        返还。
    完。
    经过时间 为 做 当前时间_us 减 组 的 第 BW_周期开始 项。
    若 经过时间 大于或等于 组 的 第 BW_周期 项：
        做 带宽_周期定时器 用 组。
    完。
完。

带宽_设置配额 接受 组、新配额：
    做 设置 用 组、BW_配额、新配额。
    若 新配额 不等于 BW_无限配额 并且 新配额 小于 组 的 第 BW_剩余运行时间 项：
        做 设置 用 组、BW_剩余运行时间、新配额。
    完。
完。

带宽_设置周期 接受 组、新周期：
    若 新周期 小于 1000：
        新周期 为 1000。
    完。
    若 新周期 大于 1000000：
        新周期 为 1000000。
    完。
    做 设置 用 组、BW_周期、新周期。
完。

带宽_获取配额 接受 组：
    返还 组 的 第 BW_配额 项。
完。

带宽_获取周期 接受 组：
    返还 组 的 第 BW_周期 项。
完。

带宽_获取使用率 接受 组：
    若 组 的 第 BW_配额 项 等于 BW_无限配额 或者 组 的 第 BW_配额 项 等于 0：
        返还 0。
    完。
    返还 组 的 第 BW_已用时间 项 乘 100 除以 组 的 第 BW_配额 项。
完。

带宽_统计 接受 组：
    返还 一组：组 的 第 BW_配额 项、组 的 第 BW_周期 项、组 的 第 BW_剩余运行时间 项、组 的 第 BW_已用时间 项、组 的 第 BW_节流次数 项、组 的 第 BW_已节流 项。
完。

BW_突发配额 为 7。

BW_累积余量 为 8。

带宽组_创建_含突发 接受 配额、周期、突发：
    若 周期 小于或等于 0：
        周期 为 BW_默认周期。
    完。
    若 突发 小于 0：
        突发 为 0。
    完。
    组 为 一组：配额、周期、配额、0、0、假、0、突发、0。
    做 追加 用 带宽组列表、组。
    返还 组。
完。

带宽_消耗时间_含突发 接受 组、delta_us：
    若 非 做 带宽组_已启用 用 组：
        返还。
    完。
    做 设置 用 组、BW_剩余运行时间、组 的 第 BW_剩余运行时间 项 减 delta_us。
    做 设置 用 组、BW_已用时间、组 的 第 BW_已用时间 项 加 delta_us。
    若 组 的 第 BW_剩余运行时间 项 小于 0 并且 做 计数 用 组 大于 BW_累积余量：
        缺额 为 0 减 组 的 第 BW_剩余运行时间 项。
        若 组 的 第 BW_累积余量 项 大于或等于 缺额：
            做 设置 用 组、BW_累积余量、组 的 第 BW_累积余量 项 减 缺额。
            做 设置 用 组、BW_剩余运行时间、0。
        完。
    完。
完。

带宽_周期定时器_含突发 接受 组：
    若 非 做 带宽组_已启用 用 组：
        返还。
    完。
    若 组 的 第 BW_剩余运行时间 项 大于 0 并且 做 计数 用 组 大于 BW_突发配额：
        余量 为 组 的 第 BW_累积余量 项 加 组 的 第 BW_剩余运行时间 项。
        若 余量 大于 组 的 第 BW_突发配额 项：
            余量 为 组 的 第 BW_突发配额 项。
        完。
        做 设置 用 组、BW_累积余量、余量。
    完。
    做 设置 用 组、BW_剩余运行时间、组 的 第 BW_配额 项。
    做 设置 用 组、BW_已用时间、0。
    做 设置 用 组、BW_周期开始、做 当前时间_us。
    若 组 的 第 BW_已节流 项：
        做 带宽_解除节流 用 组。
    完。
完。

带宽_全部组数 接受 零参：
    返还 做 计数 用 带宽组列表。
完。

带宽_全部节流数 接受 零参：
    可变 总数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 带宽组列表：
        总数 为 总数 加 带宽组列表 的 第 i 项 的 第 BW_节流次数 项。
        i 为 i 加 1。
    完。
    返还 总数。
完。

带宽_当前节流组数 接受 零参：
    可变 计 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 带宽组列表：
        若 带宽组列表 的 第 i 项 的 第 BW_已节流 项：
            计 为 计 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计。
完。

带宽_完整统计 接受 零参：
    返还 一组：做 带宽_全部组数、做 带宽_全部节流数、做 带宽_当前节流组数。
完。

可变 AI_公平调度甲_负载窗口 为 一组：（空）。

AI_公平调度甲_负载预测 接受 当前负载：
    做 追加 用 AI_公平调度甲_负载窗口、当前负载。
    若 做 计数 用 AI_公平调度甲_负载窗口 大于 20：
        做 列表_移除 用 AI_公平调度甲_负载窗口、0。
    完。
    若 做 计数 用 AI_公平调度甲_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_公平调度甲_负载窗口。
    趋势 为 AI_公平调度甲_负载窗口 的 第 n 减 1 项 减 AI_公平调度甲_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_公平调度甲_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
