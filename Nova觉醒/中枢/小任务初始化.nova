《小任务初始化》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·自动扩容》。

引用《.·公平调度唤醒》。

引用《.·字符串替换》。

引用《.·软中断初始化》。

引用《.·时钟中枢》。

Tasklet 是 一种 事物，含 下一个、状态、计数、处理函数、数据。

TASKLET_状态_已调度 为 1。

TASKLET_状态_运行中 为 2。

Tasklet队列 是 一种 事物，含 头、尾、锁。

可变 tasklet_普通队列 为 新建 Tasklet队列。

可变 tasklet_高优先级队列 为 新建 Tasklet队列。

tasklet_初始化 接受 t、处理函数、数据：
    t 的 下一个 为 空。
    t 的 状态 为 0。
    t 的 计数 为 0。
    t 的 处理函数 为 处理函数。
    t 的 数据 为 数据。
完。

tasklet_调度 接受 t：
    若 t 的 状态 位与 TASKLET_状态_已调度：
        返还。
    完。
    t 的 状态 为 t 的 状态 位或 TASKLET_状态_已调度。
    旧中断 为 做 保存并关中断。
    t 的 下一个 为 空。
    若 tasklet_普通队列 的 头 等于 空：
        tasklet_普通队列 的 头 为 t。
    否则：
        可变 当前 为 tasklet_普通队列 的 头。
        只要 当前 的 下一个 不等于 空：
            当前 为 当前 的 下一个。
        完。
        当前 的 下一个 为 t。
    完。
    做 恢复中断 用 旧中断。
    做 软中断_触发 用 软中断_TASKLET。
完。

tasklet_高优先级_调度 接受 t：
    若 t 的 状态 位与 TASKLET_状态_已调度：
        返还。
    完。
    t 的 状态 为 t 的 状态 位或 TASKLET_状态_已调度。
    旧中断 为 做 保存并关中断。
    t 的 下一个 为 tasklet_高优先级队列 的 头。
    tasklet_高优先级队列 的 头 为 t。
    做 恢复中断 用 旧中断。
    做 软中断_触发 用 软中断_高优先级。
完。

tasklet_禁用 接受 t：
    t 的 计数 为 t 的 计数 加 1。
完。

tasklet_使能 接受 t：
    若 t 的 计数 大于 0：
        t 的 计数 为 t 的 计数 减 1。
    完。
完。

tasklet_杀死 接受 t：
    只要 t 的 状态 位与 TASKLET_状态_运行中：
        做 调度_让出。
    完。
    t 的 状态 为 0。
完。

tasklet_软中断处理 接受 零参：
    旧中断 为 做 保存并关中断。
    可变 列表 为 tasklet_普通队列 的 头。
    tasklet_普通队列 的 头 为 空。
    做 恢复中断 用 旧中断。
    只要 列表 不等于 空：
        t 为 列表。
        列表 为 列表 的 下一个。
        若 t 的 计数 等于 0：
            t 的 状态 为 t 的 状态 位或 TASKLET_状态_运行中。
            t 的 状态 为 t 的 状态 位与 按位取反 TASKLET_状态_已调度。
            若 t 的 处理函数 不等于 空：
                做 t 的 处理函数 用 t 的 数据。
            完。
            t 的 状态 为 t 的 状态 位与 按位取反 TASKLET_状态_运行中。
        否则：
            做 tasklet_调度 用 t。
        完。
    完。
完。

tasklet_高优先级_处理 接受 零参：
    旧中断 为 做 保存并关中断。
    可变 列表 为 tasklet_高优先级队列 的 头。
    tasklet_高优先级队列 的 头 为 空。
    做 恢复中断 用 旧中断。
    只要 列表 不等于 空：
        t 为 列表。
        列表 为 列表 的 下一个。
        若 t 的 计数 等于 0：
            t 的 状态 为 t 的 状态 位或 TASKLET_状态_运行中。
            t 的 状态 为 t 的 状态 位与 按位取反 TASKLET_状态_已调度。
            若 t 的 处理函数 不等于 空：
                做 t 的 处理函数 用 t 的 数据。
            完。
            t 的 状态 为 t 的 状态 位与 按位取反 TASKLET_状态_运行中。
        否则：
            做 tasklet_高优先级_调度 用 t。
        完。
    完。
完。

可变 tasklet_已初始化 为 假。

可变 tasklet_已注册到软中断 为 假。

可变 tasklet_调度总数 为 0。

可变 tasklet_执行总数 为 0。

可变 tasklet_高优先级执行总数 为 0。

可变 tasklet_创建总数 为 0。

可变 tasklet_销毁总数 为 0。

可变 tasklet_禁用总数 为 0。

可变 tasklet_使能总数 为 0。

可变 tasklet_杀死总数 为 0。

可变 tasklet_重排队总数 为 0。

可变 tasklet_超时总数 为 0。

可变 tasklet_错误数 为 0。

可变 tasklet_延迟总计 为 0。

可变 tasklet_延迟样本 为 0。

可变 tasklet_最大延迟 为 0。

tasklet_超时阈值 为 1000000。

tasklet_系统初始化 接受 零参：
    若 tasklet_已初始化 等于 真：
        返还 错误_已存在。
    完。
    tasklet_普通队列 的 头 为 空。
    tasklet_普通队列 的 尾 为 空。
    tasklet_高优先级队列 的 头 为 空。
    tasklet_高优先级队列 的 尾 为 空。
    若 tasklet_已注册到软中断 等于 假：
        做 软中断_注册 用 软中断_TASKLET、tasklet_软中断处理。
        做 软中断_注册 用 软中断_高优先级、tasklet_高优先级_处理。
        tasklet_已注册到软中断 为 真。
    完。
    tasklet_调度总数 为 0。
    tasklet_执行总数 为 0。
    tasklet_高优先级执行总数 为 0。
    tasklet_创建总数 为 0。
    tasklet_销毁总数 为 0。
    tasklet_禁用总数 为 0。
    tasklet_使能总数 为 0。
    tasklet_杀死总数 为 0。
    tasklet_重排队总数 为 0。
    tasklet_超时总数 为 0。
    tasklet_错误数 为 0。
    tasklet_延迟总计 为 0。
    tasklet_延迟样本 为 0。
    tasklet_最大延迟 为 0。
    tasklet_已初始化 为 真。
    做 打印 用 「[Tasklet] 系统初始化完成」。
    返还 0。
完。

tasklet_创建 接受 处理函数、数据：
    若 处理函数 等于 空：
        tasklet_错误数 为 tasklet_错误数 加 1。
        返还 空。
    完。
    t 为 新建 Tasklet。
    t 的 下一个 为 空。
    t 的 状态 为 0。
    t 的 计数 为 0。
    t 的 处理函数 为 处理函数。
    t 的 数据 为 数据。
    tasklet_创建总数 为 tasklet_创建总数 加 1。
    返还 t。
完。

tasklet_创建_禁用 接受 处理函数、数据：
    t 为 做 tasklet_创建 用 处理函数、数据。
    若 t 不等于 空：
        t 的 计数 为 1。
    完。
    返还 t。
完。

tasklet_销毁 接受 t：
    若 t 等于 空：
        返还。
    完。
    做 tasklet_杀死 用 t。
    tasklet_销毁总数 为 tasklet_销毁总数 加 1。
完。

tasklet_调度_增强 接受 t：
    若 t 等于 空：
        tasklet_错误数 为 tasklet_错误数 加 1。
        返还 错误_无效参数。
    完。
    tasklet_调度总数 为 tasklet_调度总数 加 1。
    做 tasklet_调度 用 t。
    返还 0。
完。

tasklet_高优先级_调度_增强 接受 t：
    若 t 等于 空：
        tasklet_错误数 为 tasklet_错误数 加 1。
        返还 错误_无效参数。
    完。
    tasklet_调度总数 为 tasklet_调度总数 加 1。
    做 tasklet_高优先级_调度 用 t。
    返还 0。
完。

tasklet_软中断处理_增强 接受 零参：
    旧中断 为 做 保存并关中断。
    可变 列表 为 tasklet_普通队列 的 头。
    tasklet_普通队列 的 头 为 空。
    做 恢复中断 用 旧中断。
    可变 处理数 为 0。
    只要 列表 不等于 空：
        t 为 列表。
        列表 为 列表 的 下一个。
        若 t 的 计数 等于 0：
            t 的 状态 为 t 的 状态 位或 TASKLET_状态_运行中。
            t 的 状态 为 t 的 状态 位与 按位取反 TASKLET_状态_已调度。
            若 t 的 处理函数 不等于 空：
                开始 为 做 读取纳秒。
                做 t 的 处理函数 用 t 的 数据。
                耗时 为 做 读取纳秒 减 开始。
                做 tasklet_记录延迟 用 耗时。
                若 耗时 大于 tasklet_超时阈值：
                    tasklet_超时总数 为 tasklet_超时总数 加 1。
                    做 打印 用 「[Tasklet] 警告: tasklet执行超时 」 加 做 转字符串 用 耗时 加 「ns」。
                完。
            完。
            t 的 状态 为 t 的 状态 位与 按位取反 TASKLET_状态_运行中。
            tasklet_执行总数 为 tasklet_执行总数 加 1。
            处理数 为 处理数 加 1。
        否则：
            做 tasklet_调度 用 t。
            tasklet_重排队总数 为 tasklet_重排队总数 加 1。
        完。
    完。
    返还 处理数。
完。

tasklet_记录延迟 接受 延迟：
    tasklet_延迟样本 为 tasklet_延迟样本 加 1。
    tasklet_延迟总计 为 tasklet_延迟总计 加 延迟。
    若 延迟 大于 tasklet_最大延迟：
        tasklet_最大延迟 为 延迟。
    完。
完。

tasklet_平均延迟 接受 零参：
    若 tasklet_延迟样本 等于 0：
        返还 0。
    完。
    返还 tasklet_延迟总计 除以 tasklet_延迟样本。
完。

tasklet_普通队列长度 接受 零参：
    可变 长度 为 0。
    可变 当前 为 tasklet_普通队列 的 头。
    只要 当前 不等于 空：
        长度 为 长度 加 1。
        当前 为 当前 的 下一个。
    完。
    返还 长度。
完。

tasklet_高优先级队列长度 接受 零参：
    可变 长度 为 0。
    可变 当前 为 tasklet_高优先级队列 的 头。
    只要 当前 不等于 空：
        长度 为 长度 加 1。
        当前 为 当前 的 下一个。
    完。
    返还 长度。
完。

tasklet_是否运行中 接受 t：
    若 t 等于 空：
        返还 假。
    完。
    返还 t 的 状态 位与 TASKLET_状态_运行中 不等于 0。
完。

tasklet_是否已调度 接受 t：
    若 t 等于 空：
        返还 假。
    完。
    返还 t 的 状态 位与 TASKLET_状态_已调度 不等于 0。
完。

tasklet_是否已禁用 接受 t：
    若 t 等于 空：
        返还 真。
    完。
    返还 t 的 计数 大于 0。
完。

tasklet_完整统计 接受 零参：
    统计 为 做 字典。
    做 字典设 用 统计、「已初始化」、tasklet_已初始化。
    做 字典设 用 统计、「调度总数」、tasklet_调度总数。
    做 字典设 用 统计、「执行总数」、tasklet_执行总数。
    做 字典设 用 统计、「高优先级执行」、tasklet_高优先级执行总数。
    做 字典设 用 统计、「创建总数」、tasklet_创建总数。
    做 字典设 用 统计、「销毁总数」、tasklet_销毁总数。
    做 字典设 用 统计、「禁用总数」、tasklet_禁用总数。
    做 字典设 用 统计、「使能总数」、tasklet_使能总数。
    做 字典设 用 统计、「杀死总数」、tasklet_杀死总数。
    做 字典设 用 统计、「重排队总数」、tasklet_重排队总数。
    做 字典设 用 统计、「超时总数」、tasklet_超时总数。
    做 字典设 用 统计、「普通队列长度」、做 tasklet_普通队列长度。
    做 字典设 用 统计、「高优先级队列长度」、做 tasklet_高优先级队列长度。
    做 字典设 用 统计、「平均延迟(ns)」、做 tasklet_平均延迟。
    做 字典设 用 统计、「最大延迟(ns)」、tasklet_最大延迟。
    做 字典设 用 统计、「延迟样本数」、tasklet_延迟样本。
    做 字典设 用 统计、「错误数」、tasklet_错误数。
    返还 统计。
完。

tasklet_打印完整统计 接受 零参：
    统计 为 做 tasklet_完整统计。
    做 打印 用 「[Tasklet] ═══════ 完整统计 ═══════」。
    做 打印 用 「  调度总数:     」 加 做 转字符串 用 做 字典取 用 统计、「调度总数」。
    做 打印 用 「  执行总数:     」 加 做 转字符串 用 做 字典取 用 统计、「执行总数」。
    做 打印 用 「  高优先级:     」 加 做 转字符串 用 做 字典取 用 统计、「高优先级执行」。
    做 打印 用 「  创建总数:     」 加 做 转字符串 用 做 字典取 用 统计、「创建总数」。
    做 打印 用 「  销毁总数:     」 加 做 转字符串 用 做 字典取 用 统计、「销毁总数」。
    做 打印 用 「  重排队:       」 加 做 转字符串 用 做 字典取 用 统计、「重排队总数」。
    做 打印 用 「  超时:         」 加 做 转字符串 用 做 字典取 用 统计、「超时总数」。
    做 打印 用 「  普通队列:     」 加 做 转字符串 用 做 字典取 用 统计、「普通队列长度」。
    做 打印 用 「  高优先级队列: 」 加 做 转字符串 用 做 字典取 用 统计、「高优先级队列长度」。
    做 打印 用 「  平均延迟:     」 加 做 转字符串 用 做 字典取 用 统计、「平均延迟(ns)」 加 「ns」。
    做 打印 用 「  最大延迟:     」 加 做 转字符串 用 做 字典取 用 统计、「最大延迟(ns)」 加 「ns」。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[Tasklet] ═════════════════════════」。
    返还 0。
完。

tasklet_dump 接受 零参：
    做 打印 用 「[Tasklet dump] ──────────────────」。
    做 打印 用 「  已初始化: 」 加 做 转字符串 用 tasklet_已初始化。
    做 打印 用 「  调度总数: 」 加 做 转字符串 用 tasklet_调度总数。
    做 打印 用 「  执行总数: 」 加 做 转字符串 用 tasklet_执行总数。
    做 打印 用 「  普通队列: 」 加 做 转字符串 用 做 tasklet_普通队列长度。
    做 打印 用 「  高优先级队列: 」 加 做 转字符串 用 做 tasklet_高优先级队列长度。
    做 打印 用 「  超时: 」 加 做 转字符串 用 tasklet_超时总数。
    做 打印 用 「  平均延迟: 」 加 做 转字符串 用 做 tasklet_平均延迟 加 「ns」。
    做 打印 用 「[Tasklet dump] ──────────────────」。
完。

tasklet_系统_销毁 接受 零参：
    若 tasklet_已初始化 等于 假：
        返还 0。
    完。
    tasklet_普通队列 的 头 为 空。
    tasklet_高优先级队列 的 头 为 空。
    tasklet_已初始化 为 假。
    做 打印 用 「[Tasklet] 系统已销毁」。
    返还 0。
完。

tasklet_为软中断提供处理函数 接受 零参：
    返还 一组：tasklet_软中断处理、tasklet_高优先级_处理。
完。

tasklet_为调度器提供负载 接受 零参：
    返还 一组：做 tasklet_普通队列长度、做 tasklet_高优先级队列长度、tasklet_调度总数、tasklet_执行总数。
完。

tasklet_为procfs提供统计 接受 零参：
    返还 一组：tasklet_调度总数、tasklet_执行总数、tasklet_高优先级执行总数、tasklet_创建总数、tasklet_销毁总数、tasklet_超时总数、做 tasklet_普通队列长度、做 tasklet_高优先级队列长度。
完。

tasklet_重置统计 接受 零参：
    tasklet_调度总数 为 0。
    tasklet_执行总数 为 0。
    tasklet_高优先级执行总数 为 0。
    tasklet_创建总数 为 0。
    tasklet_销毁总数 为 0。
    tasklet_禁用总数 为 0。
    tasklet_使能总数 为 0。
    tasklet_杀死总数 为 0。
    tasklet_重排队总数 为 0。
    tasklet_超时总数 为 0。
    tasklet_延迟总计 为 0。
    tasklet_延迟样本 为 0。
    tasklet_最大延迟 为 0。
    做 打印 用 「[Tasklet] 统计已重置」。
    返还 0。
完。

软中断_TASKLET 为 6。

软中断_高优先级 为 0。

tasklet_状态名 接受 状态：
    若 状态 等于 0：
        返还 「空闲」。
    完。
    可变 名称 为 「」。
    若 状态 位与 TASKLET_状态_已调度 不等于 0：
        名称 为 名称 加 「已调度 」。
    完。
    若 状态 位与 TASKLET_状态_运行中 不等于 0：
        名称 为 名称 加 「运行中 」。
    完。
    若 名称 等于 「」：
        返还 「未知(」 加 做 转字符串 用 状态 加 「)」。
    完。
    返还 名称。
完。

tasklet_dump_单个 接受 t：
    若 t 等于 空：
        返还。
    完。
    做 打印 用 「  Tasklet: 状态=」 加 做 tasklet_状态名 用 t 的 状态 加 「 计数=」 加 做 转字符串 用 t 的 计数 加 「 处理=」 加 做 转字符串 用 t 的 处理函数 不等于 空。
完。

tasklet_批量调度 接受 列表：
    若 列表 等于 空：
        返还 0。
    完。
    可变 成功数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 不等于 空：
            做 tasklet_调度_增强 用 列表 的 第 i 项。
            成功数 为 成功数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 成功数。
完。

tasklet_批量杀死 接受 列表：
    若 列表 等于 空：
        返还 0。
    完。
    可变 成功数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 不等于 空：
            做 tasklet_销毁 用 列表 的 第 i 项。
            成功数 为 成功数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 成功数。
完。

tasklet_为AI提供延迟模式 接受 零参：
    返还 一组：做 tasklet_平均延迟、tasklet_最大延迟、tasklet_超时总数、tasklet_执行总数、tasklet_延迟样本。
完。

保存并关中断 接受 零参：
    可变 旧标志 为 0。
    汇编。
    块：
        「pushfq」。
        「pop rax」。
        「mov [rbp-8], rax」。
        「cli」。
    完。
    返还 旧标志。
完。

恢复中断 接受 旧标志：
    汇编。
    块：
        「push [rbp+16]」。
        「popfq」。
    完。
完。

本章 收束。
