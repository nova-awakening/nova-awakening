《页表索引》

引用《..·引导·寄存器定义》。

引用《..·执行·端口》。

引用《.·类型定义》。

引用《.·日志中枢》。

引用《.·内存物理》。

引用《.·同步原语》。

引用《..·推理·通信总线》。

引用《.·内核打印》。

引用《..·推理·定点Q算术》。

引用《.·恐慌》。

引用《..·引导·端口IO》。

引用《.·每核页释放》。

引用《..·推理·AI总线桥接》。

引用《.·伙伴系统释放》。

引用《.·页表重建》。

页表_条目数 为 512。

页表_级数 为 4。

页大小_4K 为 0x1000。

页大小_2M 为 0x200000。

页大小_1G 为 0x40000000。

页表项_字节 为 8。

虚拟地址_位宽 为 48。

物理地址_掩码 为 0x000FFFFFFFFFF000。

标志位_掩码 为 0xFFF。

扩展标志_掩码 为 0x7FF0000000000000。

PTE_存在 为 0x001。

PTE_可写 为 0x002。

PTE_用户 为 0x004。

PTE_写穿 为 0x008。

PTE_禁缓存 为 0x010。

PTE_已访问 为 0x020。

PTE_脏 为 0x040。

PTE_大页 为 0x080。

PTE_全局 为 0x100。

PTE_COW标记 为 0x200。

PTE_惰性标记 为 0x400。

PTE_换出标记 为 0x800。

PTE_NX 为 0x8000000000000000。

INVPCID_单地址 为 0。

INVPCID_单PCID 为 1。

INVPCID_全部含全局 为 2。

INVPCID_全部不含全局 为 3。

PF_存在 为 0x01。

PF_写 为 0x02。

PF_用户 为 0x04。

PF_保留位 为 0x08。

PF_取指 为 0x10。

PF_PK 为 0x20。

TLB_统计_环大小 为 1024。

PCID_最大值 为 4096。

MAAPT_特征维度 为 12。

MAAPT_时间窗口 为 64。

MAAPT_空间邻域 为 16。

MAAPT_预取阈值 为 0.72。

MAAPT_衰减因子 为 0.93。

可变 内核_PML4物理 为 0。

可变 页表_总分配数 为 0。

可变 页表_总释放数 为 0。

可变 页表_映射次数 为 0。

可变 页表_取消映射次数 为 0。

可变 页表_大页映射数 为 0。

可变 TLB_刷新_单页次数 为 0。

可变 TLB_刷新_全局次数 为 0。

可变 TLB_未命中统计 为 0。

可变 COW_触发次数 为 0。

可变 COW_复制次数 为 0。

可变 页错误_总次数 为 0。

可变 页表锁 为 做 自旋锁_创建。

可变 PCID_位图 为 做 数组_创建 用 PCID_最大值 除以 64、0。

可变 PCID_下一个空闲 为 1。

可变 PCID_已分配数 为 0。

可变 TLB统计环 为 做 数组_创建 用 TLB_统计_环大小、0。

可变 TLB统计_写指针 为 0。

页内偏移 接受 虚拟地址：
    返还 虚拟地址 位与 0xFFF。
完。

索引到虚拟地址 接受 pml4、pdpt、pd、pt、偏移：
    原始 为 pml4 左移 39 位或 pdpt 左移 30 位或 pd 左移 21 位或 pt 左移 12 位或 偏移。
    若 原始 右移 47 位与 1：
        返还 原始 位或 0xFFFF000000000000。
    完。
    返还 原始。
完。

虚拟地址_是规范的 接受 虚拟地址：
    高位 为 虚拟地址 右移 47。
    返还 高位 等于 0 或者 高位 等于 0x1FFFF。
完。

页表项_创建 接受 物理地址、标志：
    返还 物理地址 位与 物理地址_掩码 位或 标志 位与 (标志位_掩码 位或 PTE_NX)。
完。

页表项_地址 接受 条目：
    返还 条目 位与 物理地址_掩码。
完。

页表项_标志 接受 条目：
    返还 条目 位与 标志位_掩码。
完。

页表项_用户 接受 条目：
    返还 条目 位与 PTE_用户 不等于 0。
完。

页表项_已访问 接受 条目：
    返还 条目 位与 PTE_已访问 不等于 0。
完。

页表项_脏 接受 条目：
    返还 条目 位与 PTE_脏 不等于 0。
完。

页表项_全局 接受 条目：
    返还 条目 位与 PTE_全局 不等于 0。
完。

页表项_是COW 接受 条目：
    返还 条目 位与 PTE_COW标记 不等于 0。
完。

页表项_是惰性 接受 条目：
    返还 条目 位与 PTE_惰性标记 不等于 0。
完。

页表项_已换出 接受 条目：
    返还 条目 位与 PTE_换出标记 不等于 0。
完。

页表项_不可执行 接受 条目：
    返还 条目 位与 PTE_NX 不等于 0。
完。

页表项_设置标志 接受 条目、新标志：
    返还 条目 位与 物理地址_掩码 位或 新标志。
完。

页表项_添加标志 接受 条目、追加：
    返还 条目 位或 追加。
完。

页表项_移除标志 接受 条目、移除：
    返还 条目 位与 按位取反 移除。
完。

页表项_标记COW 接受 条目：
    返还 做 页表项_添加标志 用 做 页表项_移除标志 用 条目、PTE_可写、PTE_COW标记。
完。

页表_读条目 接受 页表物理、索引：
    若 索引 大于或等于 页表_条目数：
        做 内核打印 用 「[页表] 读条目越界: 索引={} 最大={}」、索引、页表_条目数 减 1。
        返还 0。
    完。
    返还 做 内存读64 用 做 物理到虚拟 用 页表物理 加 索引 乘 页表项_字节。
完。

页表_写条目 接受 页表物理、索引、值：
    若 索引 大于或等于 页表_条目数：
        做 内核打印 用 「[页表] 写条目越界: 索引={}」、索引。
        返还。
    完。
    做 内存写64 用 做 物理到虚拟 用 页表物理 加 索引 乘 页表项_字节、值。
完。

页表_原子CAS条目 接受 页表物理、索引、期望值、新值：
    地址 为 做 物理到虚拟 用 页表物理 加 索引 乘 页表项_字节。
    返还 做 原子_比较交换64 用 地址、期望值、新值。
完。

页表_分配页 接受 零参：
    页号 为 做 伙伴系统_分配_带标志 用 0、GFP_KERNEL 位或 GFP_ZERO。
    若 页号 小于 0：
        做 内核打印 用 「[页表] 分配失败, 尝试紧急回收...」。
        回收数 为 做 内存_紧急回收 用 1。
        若 回收数 大于 0：
            页号 为 做 伙伴系统_分配_带标志 用 0、GFP_KERNEL 位或 GFP_ZERO。
        完。
        若 页号 小于 0：
            做 内核恐慌 用 「页表分配失败: 无可用物理页且回收无效」。
        完。
    完。
    物理地址 为 做 页号到地址 用 页号。
    虚拟 为 做 物理到虚拟 用 物理地址。
    可变 i 为 0。
    只要 i 小于 页大小_4K：
        做 内存写64 用 虚拟 加 i、0。
        i 为 i 加 8。
    完。
    页表_总分配数 为 页表_总分配数 加 1。
    返还 物理地址。
完。

页表_释放页 接受 物理地址：
    若 物理地址 等于 0：
        返还。
    完。
    页号 为 做 地址到页号 用 物理地址。
    做 伙伴系统_释放 用 页号、0。
    页表_总释放数 为 页表_总释放数 加 1。
完。

TLB_刷新单页 接受 虚拟地址：
    做 特权指令_invlpg 用 虚拟地址。
    TLB_刷新_单页次数 为 TLB_刷新_单页次数 加 1。
    TLB统计环 的 第 TLB统计_写指针 取余于 TLB_统计_环大小 项 为 虚拟地址。
    TLB统计_写指针 为 TLB统计_写指针 加 1。
完。

TLB_刷新全部 接受 零参：
    当前cr3 为 做 读CR3。
    做 写CR3 用 当前cr3。
    TLB_刷新_全局次数 为 TLB_刷新_全局次数 加 1。
完。

TLB_刷新_PCID 接受 pcid、虚拟地址：
    若 做 CPU支持_INVPCID：
        做 特权指令_invpcid 用 INVPCID_单地址、pcid、虚拟地址。
    否则：
        做 TLB_刷新单页 用 虚拟地址。
    完。
完。

TLB_刷新_整个PCID 接受 pcid：
    若 做 CPU支持_INVPCID：
        做 特权指令_invpcid 用 INVPCID_单PCID、pcid、0。
    否则：
        做 TLB_刷新全部。
    完。
完。

TLB_批量刷新 接受 虚拟地址数组、数量：
    若 数量 大于 32：
        做 TLB_刷新全部。
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 数量：
        做 TLB_刷新单页 用 虚拟地址数组 的 第 i 项。
        i 为 i 加 1。
    完。
完。

PCID_分配 接受 零参：
    可变 搜索起点 为 PCID_下一个空闲。
    可变 已扫描 为 0。
    只要 已扫描 小于 PCID_最大值：
        字索引 为 搜索起点 除以 64。
        位索引 为 搜索起点 取余于 64。
        若 PCID_位图 的 第 字索引 项 位与 1 左移 位索引 等于 0：
            PCID_位图 的 第 字索引 项 为 PCID_位图 的 第 字索引 项 位或 1 左移 位索引。
            PCID_下一个空闲 为 (搜索起点 加 1) 取余于 PCID_最大值。
            若 PCID_下一个空闲 等于 0：
                PCID_下一个空闲 为 1。
            完。
            PCID_已分配数 为 PCID_已分配数 加 1。
            返还 搜索起点。
        完。
        搜索起点 为 (搜索起点 加 1) 取余于 PCID_最大值。
        若 搜索起点 等于 0：
            搜索起点 为 1。
        完。
        已扫描 为 已扫描 加 1。
    完。
    做 PCID_代回收。
    返还 做 PCID_分配。
完。

PCID_释放 接受 pcid：
    若 pcid 小于或等于 0 或者 pcid 大于或等于 PCID_最大值：
        返还。
    完。
    字索引 为 pcid 除以 64。
    位索引 为 pcid 取余于 64。
    PCID_位图 的 第 字索引 项 为 PCID_位图 的 第 字索引 项 位与 按位取反 (1 左移 位索引)。
    PCID_已分配数 为 PCID_已分配数 减 1。
    做 TLB_刷新_整个PCID 用 pcid。
完。

PCID_代回收 接受 零参：
    做 内核打印 用 「[页表] PCID耗尽, 执行代回收...」。
    可变 回收数 为 0。
    目标 为 PCID_已分配数 除以 2。
    可变 i 为 1。
    只要 i 小于 PCID_最大值 并且 回收数 小于 目标：
        字索引 为 i 除以 64。
        位索引 为 i 取余于 64。
        若 PCID_位图 的 第 字索引 项 位与 1 左移 位索引 不等于 0：
            PCID_位图 的 第 字索引 项 为 PCID_位图 的 第 字索引 项 位与 按位取反 (1 左移 位索引)。
            回收数 为 回收数 加 1。
            PCID_已分配数 为 PCID_已分配数 减 1。
        完。
        i 为 i 加 1。
    完。
    做 TLB_刷新全部。
    做 内核打印 用 「[页表] PCID代回收完成, 回收{}个」、回收数。
完。

页表_遍历到PT 接受 PML4物理、虚拟地址、创建：
    若 非 做 虚拟地址_是规范的 用 虚拟地址：
        做 内核打印 用 「[页表] 非规范虚拟地址: 0x{:X}」、虚拟地址。
        返还 0。
    完。
    pml4_idx 为 做 PML4索引 用 虚拟地址。
    pml4_条目 为 做 页表_读条目 用 PML4物理、pml4_idx。
    可变 pdpt_物理 为 0。
    若 做 页表项_存在 用 pml4_条目：
        pdpt_物理 为 做 页表项_地址 用 pml4_条目。
    否则：
        若 非 创建：
            返还 0。
        完。
        pdpt_物理 为 做 页表_分配页。
        做 页表_写条目 用 PML4物理、pml4_idx、做 页表项_创建 用 pdpt_物理、PTE_存在 位或 PTE_可写 位或 PTE_用户。
    完。
    pdpt_idx 为 做 PDPT索引 用 虚拟地址。
    pdpt_条目 为 做 页表_读条目 用 pdpt_物理、pdpt_idx。
    若 做 页表项_存在 用 pdpt_条目 并且 做 页表项_是大页 用 pdpt_条目：
        做 内核打印 用 「[页表] 遍历命中1GB大页 @PDPT[{}]」、pdpt_idx。
        返还 负 1。
    完。
    可变 pd_物理 为 0。
    若 做 页表项_存在 用 pdpt_条目：
        pd_物理 为 做 页表项_地址 用 pdpt_条目。
    否则：
        若 非 创建：
            返还 0。
        完。
        pd_物理 为 做 页表_分配页。
        做 页表_写条目 用 pdpt_物理、pdpt_idx、做 页表项_创建 用 pd_物理、PTE_存在 位或 PTE_可写 位或 PTE_用户。
    完。
    pd_idx 为 做 PD索引 用 虚拟地址。
    pd_条目 为 做 页表_读条目 用 pd_物理、pd_idx。
    若 做 页表项_存在 用 pd_条目 并且 做 页表项_是大页 用 pd_条目：
        做 内核打印 用 「[页表] 遍历命中2MB大页 @PD[{}]」、pd_idx。
        返还 负 2。
    完。
    可变 pt_物理 为 0。
    若 做 页表项_存在 用 pd_条目：
        pt_物理 为 做 页表项_地址 用 pd_条目。
    否则：
        若 非 创建：
            返还 0。
        完。
        pt_物理 为 做 页表_分配页。
        做 页表_写条目 用 pd_物理、pd_idx、做 页表项_创建 用 pt_物理、PTE_存在 位或 PTE_可写 位或 PTE_用户。
    完。
    返还 pt_物理。
完。

页表_映射 接受 PML4物理、虚拟地址、物理地址、标志：
    做 自旋锁_获取 用 页表锁。
    pt_物理 为 做 页表_遍历到PT 用 PML4物理、虚拟地址、真。
    若 pt_物理 小于或等于 0：
        做 自旋锁_释放 用 页表锁。
        做 内核打印 用 「[页表] 映射失败: 遍历到PT失败 VA=0x{:X}」、虚拟地址。
        返还 假。
    完。
    pt_idx 为 做 PT索引 用 虚拟地址。
    已有 为 做 页表_读条目 用 pt_物理、pt_idx。
    若 做 页表项_存在 用 已有：
        做 内核打印 用 「[页表] 覆盖映射: VA=0x{:X} 旧PA=0x{:X}」、虚拟地址、做 页表项_地址 用 已有。
    完。
    做 页表_写条目 用 pt_物理、pt_idx、做 页表项_创建 用 物理地址、标志 位或 PTE_存在。
    做 TLB_刷新单页 用 虚拟地址。
    页表_映射次数 为 页表_映射次数 加 1。
    做 自旋锁_释放 用 页表锁。
    返还 真。
完。

页表_取消映射 接受 PML4物理、虚拟地址：
    做 自旋锁_获取 用 页表锁。
    pt_物理 为 做 页表_遍历到PT 用 PML4物理、虚拟地址、假。
    若 pt_物理 小于或等于 0：
        做 自旋锁_释放 用 页表锁。
        返还 假。
    完。
    pt_idx 为 做 PT索引 用 虚拟地址。
    条目 为 做 页表_读条目 用 pt_物理、pt_idx。
    若 非 做 页表项_存在 用 条目：
        做 自旋锁_释放 用 页表锁。
        返还 假。
    完。
    做 页表_写条目 用 pt_物理、pt_idx、0。
    做 TLB_刷新单页 用 虚拟地址。
    页表_取消映射次数 为 页表_取消映射次数 加 1。
    做 自旋锁_释放 用 页表锁。
    返还 真。
完。

页表_查询 接受 PML4物理、虚拟地址：
    pt_物理 为 做 页表_遍历到PT 用 PML4物理、虚拟地址、假。
    若 pt_物理 小于或等于 0：
        返还 负 1。
    完。
    pt_idx 为 做 PT索引 用 虚拟地址。
    条目 为 做 页表_读条目 用 pt_物理、pt_idx。
    若 非 做 页表项_存在 用 条目：
        返还 负 1。
    完。
    返还 做 页表项_地址 用 条目 位或 做 页内偏移 用 虚拟地址。
完。

页表_映射大页2M 接受 PML4物理、虚拟地址、物理地址、标志：
    若 虚拟地址 位与 页大小_2M 减 1 不等于 0：
        做 内核打印 用 「[页表] 2MB大页映射: 虚拟地址未对齐 0x{:X}」、虚拟地址。
        返还 假。
    完。
    若 物理地址 位与 页大小_2M 减 1 不等于 0：
        做 内核打印 用 「[页表] 2MB大页映射: 物理地址未对齐」。
        返还 假。
    完。
    做 自旋锁_获取 用 页表锁。
    pml4_idx 为 做 PML4索引 用 虚拟地址。
    pml4_条目 为 做 页表_读条目 用 PML4物理、pml4_idx。
    可变 pdpt_物理 为 0。
    若 做 页表项_存在 用 pml4_条目：
        pdpt_物理 为 做 页表项_地址 用 pml4_条目。
    否则：
        pdpt_物理 为 做 页表_分配页。
        做 页表_写条目 用 PML4物理、pml4_idx、做 页表项_创建 用 pdpt_物理、PTE_存在 位或 PTE_可写 位或 PTE_用户。
    完。
    pdpt_idx 为 做 PDPT索引 用 虚拟地址。
    pdpt_条目 为 做 页表_读条目 用 pdpt_物理、pdpt_idx。
    可变 pd_物理 为 0。
    若 做 页表项_存在 用 pdpt_条目：
        pd_物理 为 做 页表项_地址 用 pdpt_条目。
    否则：
        pd_物理 为 做 页表_分配页。
        做 页表_写条目 用 pdpt_物理、pdpt_idx、做 页表项_创建 用 pd_物理、PTE_存在 位或 PTE_可写 位或 PTE_用户。
    完。
    pd_idx 为 做 PD索引 用 虚拟地址。
    做 页表_写条目 用 pd_物理、pd_idx、做 页表项_创建 用 物理地址、标志 位或 PTE_存在 位或 PTE_大页。
    做 TLB_刷新单页 用 虚拟地址。
    页表_大页映射数 为 页表_大页映射数 加 1。
    页表_映射次数 为 页表_映射次数 加 1。
    做 自旋锁_释放 用 页表锁。
    返还 真。
完。

页表_映射大页1G 接受 PML4物理、虚拟地址、物理地址、标志：
    若 虚拟地址 位与 页大小_1G 减 1 不等于 0 或者 物理地址 位与 页大小_1G 减 1 不等于 0：
        做 内核打印 用 「[页表] 1GB大页映射: 地址未对齐」。
        返还 假。
    完。
    做 自旋锁_获取 用 页表锁。
    pml4_idx 为 做 PML4索引 用 虚拟地址。
    pml4_条目 为 做 页表_读条目 用 PML4物理、pml4_idx。
    可变 pdpt_物理 为 0。
    若 做 页表项_存在 用 pml4_条目：
        pdpt_物理 为 做 页表项_地址 用 pml4_条目。
    否则：
        pdpt_物理 为 做 页表_分配页。
        做 页表_写条目 用 PML4物理、pml4_idx、做 页表项_创建 用 pdpt_物理、PTE_存在 位或 PTE_可写 位或 PTE_用户。
    完。
    pdpt_idx 为 做 PDPT索引 用 虚拟地址。
    做 页表_写条目 用 pdpt_物理、pdpt_idx、做 页表项_创建 用 物理地址、标志 位或 PTE_存在 位或 PTE_大页。
    做 TLB_刷新单页 用 虚拟地址。
    页表_大页映射数 为 页表_大页映射数 加 1。
    做 自旋锁_释放 用 页表锁。
    返还 真。
完。

页表_标记范围COW 接受 PML4物理、起始VA、结束VA：
    可变 va 为 起始VA。
    可变 标记数 为 0。
    只要 va 小于 结束VA：
        pt_物理 为 做 页表_遍历到PT 用 PML4物理、va、假。
        若 pt_物理 大于 0：
            pt_idx 为 做 PT索引 用 va。
            条目 为 做 页表_读条目 用 pt_物理、pt_idx。
            若 做 页表项_存在 用 条目 并且 做 页表项_可写 用 条目：
                新条目 为 做 页表项_标记COW 用 条目。
                做 页表_写条目 用 pt_物理、pt_idx、新条目。
                标记数 为 标记数 加 1。
            完。
        完。
        va 为 va 加 页大小_4K。
    完。
    若 标记数 大于 0：
        做 TLB_刷新全部。
    完。
    返还 标记数。
完。

页表_COW处理 接受 PML4物理、错误VA：
    pt_物理 为 做 页表_遍历到PT 用 PML4物理、错误VA、假。
    若 pt_物理 小于或等于 0：
        返还 假。
    完。
    pt_idx 为 做 PT索引 用 错误VA。
    条目 为 做 页表_读条目 用 pt_物理、pt_idx。
    若 非 做 页表项_存在 用 条目 或者 非 做 页表项_是COW 用 条目：
        返还 假。
    完。
    旧物理 为 做 页表项_地址 用 条目。
    旧标志 为 做 页表项_标志 用 条目。
    新物理 为 做 页表_分配页。
    可变 i 为 0。
    只要 i 小于 页大小_4K：
        字节 为 做 内存读8 用 做 物理到虚拟 用 旧物理 加 i。
        做 内存写8 用 做 物理到虚拟 用 新物理 加 i、字节。
        i 为 i 加 1。
    完。
    新标志 为 (旧标志 位或 PTE_可写) 位与 按位取反 PTE_COW标记。
    成功 为 做 页表_原子CAS条目 用 pt_物理、pt_idx、条目、做 页表项_创建 用 新物理、新标志。
    若 非 成功：
        做 页表_释放页 用 新物理。
        返还 假。
    完。
    做 TLB_刷新单页 用 错误VA。
    COW_触发次数 为 COW_触发次数 加 1。
    COW_复制次数 为 COW_复制次数 加 1。
    做 页帧_减引用 用 做 地址到页号 用 旧物理。
    返还 真。
完。

页错误_分类 接受 错误码、错误地址、PML4物理：
    页错误_总次数 为 页错误_总次数 加 1。
    可变 类型 为 「未知」。
    可变 严重级 为 0。
    若 错误码 位与 PF_保留位 不等于 0：
        类型 为 「保留位违规」。
        严重级 为 2。
    否则若 错误码 位与 PF_存在 等于 0：
        pt_物理 为 做 页表_遍历到PT 用 PML4物理、错误地址、假。
        若 pt_物理 大于 0：
            条目 为 做 页表_读条目 用 pt_物理、做 PT索引 用 错误地址。
            若 做 页表项_已换出 用 条目：
                类型 为 「换入请求」。
                严重级 为 0。
            否则若 做 页表项_是惰性 用 条目：
                类型 为 「延迟分配」。
                严重级 为 0。
            否则：
                类型 为 「未映射访问」。
                严重级 为 1。
            完。
        否则：
            类型 为 「未映射访问」。
            严重级 为 1。
        完。
    否则若 错误码 位与 PF_写 不等于 0：
        pt_物理 为 做 页表_遍历到PT 用 PML4物理、错误地址、假。
        若 pt_物理 大于 0：
            条目 为 做 页表_读条目 用 pt_物理、做 PT索引 用 错误地址。
            若 做 页表项_是COW 用 条目：
                类型 为 「COW触发」。
                严重级 为 0。
            否则：
                类型 为 「写保护违规」。
                严重级 为 1。
            完。
        否则：
            类型 为 「写保护违规」。
            严重级 为 1。
        完。
    否则若 错误码 位与 PF_取指 不等于 0：
        类型 为 「NX执行违规」。
        严重级 为 1。
    否则：
        类型 为 「读保护违规」。
        严重级 为 1。
    完。
    返还 一本字典：类型 对应 类型、严重级 对应 严重级、错误码 对应 错误码、地址 对应 错误地址。
完。

页错误_处理 接受 错误码、错误地址、PML4物理：
    分类 为 做 页错误_分类 用 错误码、错误地址、PML4物理。
    若 分类 的 严重级 等于 2：
        做 内核恐慌 用 「致命页错误: {} @0x{:X} 错误码=0x{:X}」、分类 的 类型、错误地址、错误码。
    完。
    做 匹配 用 分类 的 类型。
完。

页表_内核初始化 接受 零参：
    做 内核打印 用 「[页表] 初始化四级页表管理子系统...」。
    内核_PML4物理 为 做 页表_分配页。
    做 内核打印 用 「[页表] 建立前4GB恒等映射(2MB大页)...」。
    可变 物理 为 0。
    只要 物理 小于 0x100000000：
        做 页表_映射大页2M 用 内核_PML4物理、物理、物理、PTE_可写 位或 PTE_全局。
        物理 为 物理 加 页大小_2M。
    完。
    做 内核打印 用 「[页表] 建立高半核直接映射窗口...」。
    高半核基址 为 0xFFFF800000000000。
    物理 为 0。
    只要 物理 小于 0x100000000：
        做 页表_映射大页2M 用 内核_PML4物理、高半核基址 加 物理、物理、PTE_可写 位或 PTE_全局 位或 PTE_NX。
        物理 为 物理 加 页大小_2M。
    完。
    做 写CR3 用 内核_PML4物理。
    做 内核打印 用 「[页表] 四级页表初始化完成, PML4=0x{:X}」、内核_PML4物理。
完。

页表_创建进程页表 接受 零参：
    新PML4 为 做 页表_分配页。
    可变 i 为 256。
    只要 i 小于 512：
        内核条目 为 做 页表_读条目 用 内核_PML4物理、i。
        若 做 页表项_存在 用 内核条目：
            做 页表_写条目 用 新PML4、i、内核条目。
        完。
        i 为 i 加 1。
    完。
    pcid 为 做 PCID_分配。
    返还 一本字典：PML4物理 对应 新PML4、PCID 对应 pcid。
完。

页表_销毁进程页表 接受 PML4物理、pcid：
    可变 pml4_i 为 0。
    只要 pml4_i 小于 256：
        pml4_条目 为 做 页表_读条目 用 PML4物理、pml4_i。
        若 做 页表项_存在 用 pml4_条目：
            pdpt_物理 为 做 页表项_地址 用 pml4_条目。
            做 页表_释放_PDPT 用 pdpt_物理。
            做 页表_释放页 用 pdpt_物理。
        完。
        pml4_i 为 pml4_i 加 1。
    完。
    做 页表_释放页 用 PML4物理。
    做 PCID_释放 用 pcid。
完。

页表_释放_PDPT 接受 pdpt_物理：
    可变 i 为 0。
    只要 i 小于 512：
        条目 为 做 页表_读条目 用 pdpt_物理、i。
        若 做 页表项_存在 用 条目 并且 非 做 页表项_是大页 用 条目：
            pd_物理 为 做 页表项_地址 用 条目。
            做 页表_释放_PD 用 pd_物理。
            做 页表_释放页 用 pd_物理。
        完。
        i 为 i 加 1。
    完。
完。

页表_释放_PD 接受 pd_物理：
    可变 i 为 0。
    只要 i 小于 512：
        条目 为 做 页表_读条目 用 pd_物理、i。
        若 做 页表项_存在 用 条目 并且 非 做 页表项_是大页 用 条目：
            做 页表_释放页 用 做 页表项_地址 用 条目。
        完。
        i 为 i 加 1。
    完。
完。

页表_Walk 接受 PML4物理、虚拟地址：
    做 内核打印 用 「[页表Walk] VA=0x{:X}」、虚拟地址。
    pml4_idx 为 做 PML4索引 用 虚拟地址。
    pml4_条目 为 做 页表_读条目 用 PML4物理、pml4_idx。
    做 内核打印 用 「  PML4[{}] = 0x{:X} 存在={} 可写={} 用户={}」、pml4_idx、pml4_条目、做 页表项_存在 用 pml4_条目、做 页表项_可写 用 pml4_条目、做 页表项_用户 用 pml4_条目。
    若 非 做 页表项_存在 用 pml4_条目：
        返还 空。
    完。
    pdpt_物理 为 做 页表项_地址 用 pml4_条目。
    pdpt_idx 为 做 PDPT索引 用 虚拟地址。
    pdpt_条目 为 做 页表_读条目 用 pdpt_物理、pdpt_idx。
    做 内核打印 用 「  PDPT[{}] = 0x{:X} 存在={} 大页={}」、pdpt_idx、pdpt_条目、做 页表项_存在 用 pdpt_条目、做 页表项_是大页 用 pdpt_条目。
    若 非 做 页表项_存在 用 pdpt_条目：
        返还 空。
    完。
    若 做 页表项_是大页 用 pdpt_条目：
        pa 为 做 页表项_地址 用 pdpt_条目 位或 虚拟地址 位与 页大小_1G 减 1。
        做 内核打印 用 「  → 1GB大页 PA=0x{:X}」、pa。
        返还 一本字典：物理地址 对应 pa、页大小 对应 页大小_1G、标志 对应 做 页表项_标志 用 pdpt_条目。
    完。
    pd_物理 为 做 页表项_地址 用 pdpt_条目。
    pd_idx 为 做 PD索引 用 虚拟地址。
    pd_条目 为 做 页表_读条目 用 pd_物理、pd_idx。
    做 内核打印 用 「  PD[{}] = 0x{:X} 存在={} 大页={}」、pd_idx、pd_条目、做 页表项_存在 用 pd_条目、做 页表项_是大页 用 pd_条目。
    若 非 做 页表项_存在 用 pd_条目：
        返还 空。
    完。
    若 做 页表项_是大页 用 pd_条目：
        pa 为 做 页表项_地址 用 pd_条目 位或 虚拟地址 位与 页大小_2M 减 1。
        做 内核打印 用 「  → 2MB大页 PA=0x{:X}」、pa。
        返还 一本字典：物理地址 对应 pa、页大小 对应 页大小_2M、标志 对应 做 页表项_标志 用 pd_条目。
    完。
    pt_物理 为 做 页表项_地址 用 pd_条目。
    pt_idx 为 做 PT索引 用 虚拟地址。
    pt_条目 为 做 页表_读条目 用 pt_物理、pt_idx。
    做 内核打印 用 「  PT[{}] = 0x{:X} 存在={} 可写={} 脏={} COW={}」、pt_idx、pt_条目、做 页表项_存在 用 pt_条目、做 页表项_可写 用 pt_条目、做 页表项_脏 用 pt_条目、做 页表项_是COW 用 pt_条目。
    若 非 做 页表项_存在 用 pt_条目：
        返还 空。
    完。
    pa 为 做 页表项_地址 用 pt_条目 位或 做 页内偏移 用 虚拟地址。
    做 内核打印 用 「  → 4KB页 PA=0x{:X}」、pa。
    返还 一本字典：物理地址 对应 pa、页大小 对应 页大小_4K、标志 对应 做 页表项_标志 用 pt_条目。
完。

可变 MAAPT_历史环 为 做 数组_创建 用 MAAPT_时间窗口、0。

可变 MAAPT_历史写指针 为 0。

可变 MAAPT_预取命中 为 0。

可变 MAAPT_预取未中 为 0。

MAAPT_记录访问 接受 虚拟地址：
    MAAPT_历史环 的 第 MAAPT_历史写指针 取余于 MAAPT_时间窗口 项 为 虚拟地址。
    MAAPT_历史写指针 为 MAAPT_历史写指针 加 1。
完。

MAAPT_时间特征 接受 零参：
    可变 特征 为 做 数组_创建 用 MAAPT_特征维度、0.0。
    窗口 为 MAAPT_时间窗口。
    可变 差分和 为 0.0。
    可变 差分方差 为 0.0。
    可变 步长频率 为 做 数组_创建 用 MAAPT_空间邻域、0。
    可变 有效数 为 0。
    可变 i 为 1。
    只要 i 小于 窗口 并且 i 小于 MAAPT_历史写指针：
        当前 为 MAAPT_历史环 的 第 (MAAPT_历史写指针 减 i) 取余于 窗口 项。
        前一 为 MAAPT_历史环 的 第 (MAAPT_历史写指针 减 i 减 1) 取余于 窗口 项。
        若 当前 不等于 0 并且 前一 不等于 0：
            差分 为 (当前 右移 12) 减 (前一 右移 12)。
            差分和 为 差分和 加 差分。
            有效数 为 有效数 加 1。
            绝对步长 为 差分。
            若 绝对步长 小于 0：
                绝对步长 为 负 绝对步长。
            完。
            若 绝对步长 小于 MAAPT_空间邻域：
                步长频率 的 第 绝对步长 项 为 步长频率 的 第 绝对步长 项 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    若 有效数 大于 0：
        均值 为 差分和 除以 有效数。
        特征 的 第 0 项 为 均值。
        特征 的 第 1 项 为 有效数。
        可变 j 为 1。
        只要 j 小于 窗口 并且 j 小于 MAAPT_历史写指针：
            当前 为 MAAPT_历史环 的 第 (MAAPT_历史写指针 减 j) 取余于 窗口 项。
            前一 为 MAAPT_历史环 的 第 (MAAPT_历史写指针 减 j 减 1) 取余于 窗口 项。
            若 当前 不等于 0 并且 前一 不等于 0：
                差分 为 (当前 右移 12) 减 (前一 右移 12)。
                差分方差 为 差分方差 加 (差分 减 均值) 乘 (差分 减 均值)。
            完。
            j 为 j 加 1。
        完。
        特征 的 第 2 项 为 差分方差 除以 有效数。
    完。
    可变 k 为 0。
    只要 k 小于 MAAPT_空间邻域 并且 k 加 3 小于 MAAPT_特征维度：
        特征 的 第 k 加 3 项 为 步长频率 的 第 k 项。
        k 为 k 加 1。
    完。
    返还 特征。
完。

MAAPT_预取决策 接受 零参：
    若 MAAPT_历史写指针 小于 8：
        返还 空。
    完。
    特征 为 做 MAAPT_时间特征。
    张量 为 做 Tensor_创建 用 1、MAAPT_特征维度。
    可变 i 为 0。
    只要 i 小于 MAAPT_特征维度：
        做 Tensor_写 用 张量、0、i、特征 的 第 i 项。
        i 为 i 加 1。
    完。
    预测 为 做 AI总线_推理 用 「MAAPT_页表预取」、张量。
    做 Tensor_释放 用 张量。
    若 预测 的 置信度 小于 MAAPT_预取阈值：
        返还 空。
    完。
    最近VA 为 MAAPT_历史环 的 第 (MAAPT_历史写指针 减 1) 取余于 MAAPT_时间窗口 项。
    预测步长 为 预测 的 输出值。
    预取VA 为 最近VA 右移 12 加 预测步长 左移 12。
    返还 一本字典：虚拟地址 对应 预取VA、置信度 对应 预测 的 置信度。
完。

MAAPT_执行预取 接受 PML4物理：
    决策 为 做 MAAPT_预取决策。
    若 决策 等于 空：
        返还。
    完。
    va 为 决策 的 虚拟地址。
    pt_物理 为 做 页表_遍历到PT 用 PML4物理、va、假。
    若 pt_物理 大于 0：
        条目 为 做 页表_读条目 用 pt_物理、做 PT索引 用 va。
        若 做 页表项_存在 用 条目：
            _ 为 做 内存读8 用 做 物理到虚拟 用 做 页表项_地址 用 条目。
            MAAPT_预取命中 为 MAAPT_预取命中 加 1。
        否则：
            MAAPT_预取未中 为 MAAPT_预取未中 加 1。
        完。
    完。
完。

页表_打印统计 接受 零参：
    做 内核打印 用 「═══ 页表管理统计 ═══」。
    做 内核打印 用 「  页表页分配: {}  释放: {}  净使用: {}」、页表_总分配数、页表_总释放数、页表_总分配数 减 页表_总释放数。
    做 内核打印 用 「  映射次数: {}  取消映射: {}  大页映射: {}」、页表_映射次数、页表_取消映射次数、页表_大页映射数。
    做 内核打印 用 「  TLB刷新: 单页={} 全局={}」、TLB_刷新_单页次数、TLB_刷新_全局次数。
    做 内核打印 用 「  COW: 触发={} 复制={}」、COW_触发次数、COW_复制次数。
    做 内核打印 用 「  页错误总数: {}」、页错误_总次数。
    做 内核打印 用 「  PCID: 已分配={}/{}」、PCID_已分配数、PCID_最大值。
    若 MAAPT_预取命中 加 MAAPT_预取未中 大于 0：
        命中率 为 MAAPT_预取命中 乘 100 除以 (MAAPT_预取命中 加 MAAPT_预取未中)。
        做 内核打印 用 「  AI预取(MAAPT): 命中={} 未中={} 命中率={}%」、MAAPT_预取命中、MAAPT_预取未中、命中率。
    完。
完。

页表_获取统计 接受 零参：
    返还 一本字典：分配数 对应 页表_总分配数、释放数 对应 页表_总释放数、映射数 对应 页表_映射次数、取消映射数 对应 页表_取消映射次数、大页映射数 对应 页表_大页映射数、TLB单页刷新 对应 TLB_刷新_单页次数、TLB全局刷新 对应 TLB_刷新_全局次数、COW触发 对应 COW_触发次数、COW复制 对应 COW_复制次数、页错误 对应 页错误_总次数、PCID使用 对应 PCID_已分配数、AI预取命中 对应 MAAPT_预取命中、AI预取未中 对应 MAAPT_预取未中。
完。

本章 收束。
