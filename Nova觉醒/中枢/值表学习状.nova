《值表学习状》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·强化学习》。

引用《.·整数转字符串》。

Q表_最大状态 为 1024。

Q表_最大动作 为 64。

Q_学习率 为 100。

Q_折扣因子 为 950。

Q_初始探索率 为 300。

Q_最小探索率 为 10。

Q_探索衰减 为 999。

Q学习器 是 一种 事物，含 Q表、状态数、动作数、探索率、总步数、总奖励、轮次。

可变 全局Q学习器 为 空。

可变 RL_总训练步 为 0。

可变 RL_总奖励 为 0。

可变 RL_轮次完成 为 0。

Q学习_初始化 接受 状态数、动作数：
    若 状态数 大于 Q表_最大状态：
        状态数 为 Q表_最大状态。
    完。
    若 动作数 大于 Q表_最大动作：
        动作数 为 Q表_最大动作。
    完。
    q 为 新建 Q学习器。
    q 的 状态数 为 状态数。
    q 的 动作数 为 动作数。
    q 的 探索率 为 Q_初始探索率。
    q 的 总步数 为 0。
    q 的 总奖励 为 0。
    q 的 轮次 为 0。
    q 的 Q表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 状态数 乘 动作数：
        做 追加 用 q 的 Q表、0。
        i 为 i 加 1。
    完。
    全局Q学习器 为 q。
    做 内核信息 用 「RL-Q学习: 初始化 状态=」 加 做 整数转字符串 用 状态数 加 「 动作=」 加 做 整数转字符串 用 动作数 加 「 ε=」 加 做 整数转字符串 用 q 的 探索率。
    返还 q。
完。

Q值_获取 接受 q、状态、动作：
    若 状态 大于或等于 q 的 状态数 或者 动作 大于或等于 q 的 动作数：
        返还 0。
    完。
    返还 q 的 Q表 的 第 状态 乘 q 的 动作数 加 动作 项。
完。

Q值_设置 接受 q、状态、动作、值：
    若 状态 大于或等于 q 的 状态数 或者 动作 大于或等于 q 的 动作数：
        返还。
    完。
    q 的 Q表 的 第 状态 乘 q 的 动作数 加 动作 项 为 值。
完。

Q学习_选动作 接受 q、状态：
    随机值 为 做 伪随机 用 q 的 总步数 取余于 1000。
    若 随机值 小于 q 的 探索率：
        返还 做 伪随机 用 q 的 总步数 加 状态 取余于 q 的 动作数。
    完。
    返还 做 Q学习_最优动作 用 q、状态。
完。

Q学习_最优动作 接受 q、状态：
    可变 最优动作 为 0。
    可变 最大Q 为 做 Q值_获取 用 q、状态、0。
    可变 a 为 1。
    只要 a 小于 q 的 动作数：
        qv 为 做 Q值_获取 用 q、状态、a。
        若 qv 大于 最大Q：
            最大Q 为 qv。
            最优动作 为 a。
        完。
        a 为 a 加 1。
    完。
    返还 最优动作。
完。

Q学习_更新 接受 q、状态、动作、奖励、下一状态：
    当前Q 为 做 Q值_获取 用 q、状态、动作。
    最优下一 为 做 Q学习_最优动作 用 q、下一状态。
    下一最大Q 为 做 Q值_获取 用 q、下一状态、最优下一。
    目标 为 奖励 乘 1000 加 Q_折扣因子 乘 下一最大Q 除以 1000。
    误差 为 目标 减 当前Q。
    新Q 为 当前Q 加 Q_学习率 乘 误差 除以 1000。
    做 Q值_设置 用 q、状态、动作、新Q。
    q 的 总步数 为 q 的 总步数 加 1。
    q 的 总奖励 为 q 的 总奖励 加 奖励。
    RL_总训练步 为 RL_总训练步 加 1。
    RL_总奖励 为 RL_总奖励 加 奖励。
    若 q 的 探索率 大于 Q_最小探索率：
        q 的 探索率 为 q 的 探索率 乘 Q_探索衰减 除以 1000。
        若 q 的 探索率 小于 Q_最小探索率：
            q 的 探索率 为 Q_最小探索率。
        完。
    完。
    返还 误差。
完。

本章 收束。
