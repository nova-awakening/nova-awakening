《内存虚拟》

引用《.·中断管理系统》。

引用《.·页表重建》。

引用《.·处理写时复》。

引用《.·页表遍历》。

引用《.·内核映射》。

引用《.·页表映射》。

引用《..·标准·原生字符串》。

引用《.·内存系统调用》。

引用《.·内存映射》。

引用《.·整数转字符串》。

引用《.·虚分配故障》。

引用《.·系统堆扩展》。

引用《.·虚拟内存区》。

引用《.·缺页异常处理》。

引用《.·虚拟内存映射》。

虚拟模式_用户 为 「用户」。

虚拟模式_内核 为 「内核」。

虚拟模式_混合 为 「混合」。

虚拟档 是 一种 事物，含 名称、模式、页表、区域表、缺页数、COW数、文件页数、栈扩数、映射数、取消数、保护数。

虚拟摘要 是 一种 事物，含 名称、模式、页表、区域数、映射数、缺页数、COW数、文件页数、栈扩数、映射页数、健康。

可变 虚拟账本 为 一组：（空）。

可变 虚拟建数 为 0。

虚拟布尔串 接受 值：
    若 值：
        返还 「true」。
    完。
    返还 「false」。
完。

虚拟模式名 接受 模式：
    若 模式 等于 虚拟模式_用户：
        返还 「用户」。
    完。
    若 模式 等于 虚拟模式_内核：
        返还 「内核」。
    完。
    若 模式 等于 虚拟模式_混合：
        返还 「混合」。
    完。
    返还 「未知」。
完。

内存虚拟_建档 接受 名称、模式、页表：
    档 为 做 堆_分配 用 虚拟档。
    若 档 等于 0：
        返还 0。
    完。
    档 的 名称 为 名称。
    档 的 模式 为 模式。
    档 的 页表 为 页表。
    档 的 区域表 为 一组：（空）。
    档 的 缺页数 为 0。
    档 的 COW数 为 0。
    档 的 文件页数 为 0。
    档 的 栈扩数 为 0。
    档 的 映射数 为 0。
    档 的 取消数 为 0。
    档 的 保护数 为 0。
    返还 档。
完。

内存虚拟_创建 接受 名称：
    档 为 做 内存虚拟_建档 用 名称、虚拟模式_混合、做 当前PML4。
    做 追加 用 虚拟账本、档。
    虚拟建数 为 虚拟建数 加 1。
    返还 档。
完。

内存虚拟_创建用户 接受 名称、页表：
    档 为 做 内存虚拟_建档 用 名称、虚拟模式_用户、页表。
    做 追加 用 虚拟账本、档。
    虚拟建数 为 虚拟建数 加 1。
    返还 档。
完。

内存虚拟_创建内核 接受 名称：
    档 为 做 内存虚拟_建档 用 名称、虚拟模式_内核、做 当前PML4。
    做 追加 用 虚拟账本、档。
    虚拟建数 为 虚拟建数 加 1。
    返还 档。
完。

内存虚拟_名称 接受 档：
    返还 档 的 名称。
完。

内存虚拟_模式 接受 档：
    返还 档 的 模式。
完。

内存虚拟_页表 接受 档：
    返还 档 的 页表。
完。

内存虚拟_区域数 接受 档：
    返还 做 计数 用 档 的 区域表。
完。

内存虚拟_映射页数 接受 档：
    返还 做 Nova内存映射_总映射页数。
完。

内存虚拟_加区域 接受 档、vma：
    做 追加 用 档 的 区域表、vma。
    返还 vma。
完。

内存虚拟_建匿名区域 接受 档、起始、结束、标志：
    vma 为 做 VMA_创建_匿名 用 起始、结束、标志。
    做 内存虚拟_加区域 用 档、vma。
    返还 vma。
完。

内存虚拟_建文件区域 接受 档、起始、结束、标志、文件、偏移：
    vma 为 做 VMA_创建_文件 用 起始、结束、标志、文件、偏移。
    做 内存虚拟_加区域 用 档、vma。
    返还 vma。
完。

内存虚拟_插区域 接受 mm、vma：
    做 VMA_插入 用 mm、vma。
完。

内存虚拟_查区域 接受 档、地址：
    可变 i 为 0。
    只要 i 小于 做 计数 用 档 的 区域表：
        vma 为 档 的 区域表 的 第 i 项。
        若 做 VMA_包含 用 vma、地址：
            返还 vma。
        完。
        i 为 i 加 1。
    完。
    返还 做 Nova内存映射_查找vma 用 地址。
完。

内存虚拟_合并相邻 接受 档：
    可变 i 为 0。
    只要 i 加 1 小于 做 计数 用 档 的 区域表：
        a 为 档 的 区域表 的 第 i 项。
        b 为 档 的 区域表 的 第 i 加 1 项。
        若 做 VMA_可合并 用 a、b 或者 做 VMA_邻接 用 a、b：
            档 的 区域表 的 第 i 项 为 做 VMA_合并 用 a、b。
        完。
        i 为 i 加 1。
    完。
完。

内存虚拟_拆区域 接受 档、vma、拆分地址、新大小：
    返还 做 VMA_拆分 用 vma、拆分地址、新大小。
完。

内存虚拟_区域权限串 接受 vma：
    返还 做 VMA_权限字符串 用 vma。
完。

内存虚拟_区域大小 接受 vma：
    返还 做 VMA_大小 用 vma。
完。

内存虚拟_区域页数 接受 vma：
    返还 做 VMA_页数 用 vma。
完。

内存虚拟_找空闲 接受 长度：
    返还 做 Nova内存映射_找空闲区域 用 长度。
完。

内存虚拟_映射匿名 接受 档、地址、长度、保护、标志：
    档 的 映射数 为 档 的 映射数 加 1。
    返还 做 do_Nova内存映射 用 地址、长度、保护、标志、负 1、0。
完。

内存虚拟_映射文件 接受 档、地址、长度、保护、标志、fd、偏移：
    档 的 映射数 为 档 的 映射数 加 1。
    返还 做 do_Nova内存映射 用 地址、长度、保护、标志、fd、偏移。
完。

内存虚拟_取消映射 接受 档、地址、长度：
    档 的 取消数 为 档 的 取消数 加 1。
    返还 做 do_munmap 用 地址、长度。
完。

内存虚拟_保护 接受 档、地址、长度、保护：
    档 的 保护数 为 档 的 保护数 加 1。
    返还 做 do_mprotect 用 地址、长度、保护。
完。

内存虚拟_同步 接受 档、地址、长度、标志：
    返还 做 do_msync 用 地址、长度、标志。
完。

内存虚拟_扩堆 接受 档、新brk：
    返还 做 do_brk 用 新brk。
完。

内存虚拟_VM标志名 接受 标志：
    返还 做 Nova内存映射_VM标志名 用 标志。
完。

内存虚拟_映射单页 接受 档、虚拟地址、物理地址、标志：
    档 的 映射数 为 档 的 映射数 加 1。
    返还 做 映射_单页 用 档 的 页表、虚拟地址、物理地址、标志。
完。

内存虚拟_映射范围 接受 档、虚拟起始、物理起始、大小、标志：
    档 的 映射数 为 档 的 映射数 加 1。
    返还 做 映射_范围 用 档 的 页表、虚拟起始、物理起始、大小、标志。
完。

内存虚拟_映射散列页 接受 档、虚拟起始、物理页号列表、标志：
    档 的 映射数 为 档 的 映射数 加 1。
    返还 做 映射_散列页 用 档 的 页表、虚拟起始、物理页号列表、标志。
完。

内存虚拟_大页映射 接受 档、虚拟地址、物理地址、标志：
    档 的 映射数 为 档 的 映射数 加 1。
    返还 做 映射_2MB大页 用 档 的 页表、虚拟地址、物理地址、标志。
完。

内存虚拟_取消单页 接受 档、虚拟地址：
    档 的 取消数 为 档 的 取消数 加 1。
    返还 做 取消映射_单页 用 档 的 页表、虚拟地址。
完。

内存虚拟_取消范围 接受 档、虚拟起始、大小：
    档 的 取消数 为 档 的 取消数 加 1。
    返还 做 取消映射_范围 用 档 的 页表、虚拟起始、大小。
完。

内存虚拟_取消范围_释放 接受 档、虚拟起始、大小：
    档 的 取消数 为 档 的 取消数 加 1。
    返还 做 取消映射_范围_释放 用 档 的 页表、虚拟起始、大小。
完。

内存虚拟_改权限 接受 档、虚拟地址、新标志：
    档 的 保护数 为 档 的 保护数 加 1。
    返还 做 映射_修改权限 用 档 的 页表、虚拟地址、新标志。
完。

内存虚拟_改范围权限 接受 档、虚拟起始、大小、新标志：
    档 的 保护数 为 档 的 保护数 加 1。
    返还 做 映射_修改范围权限 用 档 的 页表、虚拟起始、大小、新标志。
完。

内存虚拟_虚拟转物理 接受 档、虚拟地址：
    返还 做 页表_虚拟转物理 用 档 的 页表、虚拟地址。
完。

内存虚拟_已映射 接受 档、虚拟地址：
    返还 做 页表_已映射 用 档 的 页表、虚拟地址。
完。

内存虚拟_可写 接受 档、虚拟地址：
    返还 做 页表_可写 用 档 的 页表、虚拟地址。
完。

内存虚拟_是用户页 接受 档、虚拟地址：
    返还 做 页表_是用户页 用 档 的 页表、虚拟地址。
完。

内存虚拟_是COW页 接受 档、虚拟地址：
    返还 做 页表_是COW页 用 档 的 页表、虚拟地址。
完。

内存虚拟_遍历范围 接受 档、起始、结束、回调、用户数据：
    返还 做 页表_遍历范围 用 档 的 页表、起始、结束、回调、用户数据。
完。

内存虚拟_统计用户空间 接受 档：
    返还 做 页表_统计用户空间 用 档 的 页表。
完。

内存虚拟_查PTE 接受 档、虚拟地址：
    返还 做 页表_查找PTE 用 档 的 页表、虚拟地址。
完。

内存虚拟_查PTE位置 接受 档、虚拟地址：
    返还 做 页表_查找PTE位置 用 档 的 页表、虚拟地址。
完。

内存虚拟_权限串 接受 标志：
    返还 做 页表_权限字符串 用 标志。
完。

内存虚拟_重建页表 接受 档：
    做 页表_重建。
    档 的 页表 为 做 当前PML4。
    返还 档 的 页表。
完。

内存虚拟_重建内核页表 接受 档：
    做 页表_内核重建。
    档 的 页表 为 做 当前PML4。
    返还 档 的 页表。
完。

内存虚拟_刷新全部 接受 档：
    做 刷新全部TLB。
完。

内存虚拟_刷新单页 接受 档、虚拟地址：
    做 刷新单页TLB 用 虚拟地址。
完。

内存虚拟_映射内核页 接受 档、虚拟地址、物理地址、标志：
    档 的 映射数 为 档 的 映射数 加 1。
    返还 做 映射页_内核 用 档 的 页表、虚拟地址、物理地址、标志。
完。

内存虚拟_取消内核页 接受 档、虚拟地址：
    档 的 取消数 为 档 的 取消数 加 1。
    返还 做 取消映射页_内核 用 档 的 页表、虚拟地址。
完。

内存虚拟_查内核 接受 档、虚拟地址：
    返还 做 查页表_虚拟到物理 用 虚拟地址。
完。

内存虚拟_内核详情 接受 档、虚拟地址：
    返还 做 查页表_完整 用 虚拟地址。
完。

内存虚拟_判用户地址 接受 地址：
    返还 地址 小于或等于 用户空间_顶部。
完。

内存虚拟_判内核地址 接受 地址：
    返还 地址 大于或等于 内核空间_底部。
完。

内存虚拟_判直接映射 接受 地址：
    返还 地址 大于或等于 直接映射_底部 并且 地址 小于 直接映射_顶部。
完。

内存虚拟_判vmalloc 接受 地址：
    返还 地址 大于或等于 VMALLOC_底部 并且 地址 小于 VMALLOC_顶部。
完。

内存虚拟_处理缺页 接受 档、错误地址、错误码、寄存器：
    档 的 缺页数 为 档 的 缺页数 加 1。
    返还 做 缺页异常处理 用 错误地址、错误码、寄存器。
完。

内存虚拟_处理COW 接受 档、mm、vma、错误地址、旧pte：
    档 的 COW数 为 档 的 COW数 加 1。
    返还 做 处理COW缺页 用 mm、vma、错误地址、旧pte。
完。

内存虚拟_处理文件缺页 接受 档、mm、vma、错误地址、错误码：
    档 的 文件页数 为 档 的 文件页数 加 1。
    返还 做 处理文件缺页 用 mm、vma、错误地址、错误码。
完。

内存虚拟_处理栈扩展 接受 档、mm、错误地址、错误码：
    档 的 栈扩数 为 档 的 栈扩数 加 1。
    返还 做 处理栈扩展 用 mm、错误地址、错误码。
完。

内存虚拟_可扩栈 接受 mm、错误地址：
    返还 做 可以栈扩展 用 mm、错误地址。
完。

内存虚拟_查栈区域 接受 mm：
    返还 做 mm_查找栈VMA 用 mm。
完。

内存虚拟_预取页 接受 地址：
    做 异步_预取页 用 地址。
完。

内存虚拟_零页 接受 零参：
    返还 做 零页物理地址。
完。

内存虚拟_缺页统计 接受 零参：
    返还 做 缺页_统计_虚拟。
完。

内存虚拟_摘要 接受 档：
    摘 为 做 堆_分配 用 虚拟摘要。
    若 摘 等于 0：
        返还 0。
    完。
    摘 的 名称 为 档 的 名称。
    摘 的 模式 为 做 虚拟模式名 用 档 的 模式。
    摘 的 页表 为 档 的 页表。
    摘 的 区域数 为 做 计数 用 档 的 区域表。
    摘 的 映射数 为 档 的 映射数。
    摘 的 缺页数 为 档 的 缺页数。
    摘 的 COW数 为 档 的 COW数。
    摘 的 文件页数 为 档 的 文件页数。
    摘 的 栈扩数 为 档 的 栈扩数。
    摘 的 映射页数 为 做 内存虚拟_映射页数 用 档。
    摘 的 健康 为 做 内存虚拟_健康检查 用 档。
    返还 摘。
完。

内存虚拟_摘要串 接受 档：
    摘 为 做 内存虚拟_摘要 用 档。
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"名称\":\"」。
    做 字符串构建器_追加 用 建、摘 的 名称。
    做 字符串构建器_追加 用 建、「\",\"模式\":\"」。
    做 字符串构建器_追加 用 建、摘 的 模式。
    做 字符串构建器_追加 用 建、「\",\"页表\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 页表。
    做 字符串构建器_追加 用 建、「,\"区域数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 区域数。
    做 字符串构建器_追加 用 建、「,\"映射数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 映射数。
    做 字符串构建器_追加 用 建、「,\"缺页数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 缺页数。
    做 字符串构建器_追加 用 建、「,\"COW数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 COW数。
    做 字符串构建器_追加 用 建、「,\"文件页数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 文件页数。
    做 字符串构建器_追加 用 建、「,\"栈扩数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 栈扩数。
    做 字符串构建器_追加 用 建、「,\"映射页数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 映射页数。
    做 字符串构建器_追加 用 建、「,\"健康\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 健康。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

内存虚拟_批量摘要串 接受 列表：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「[」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 i 大于 0：
            做 字符串构建器_追加 用 建、「,」。
        完。
        做 字符串构建器_追加 用 建、做 内存虚拟_摘要串 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    做 字符串构建器_追加 用 建、「]」。
    返还 做 字符串构建器_构建 用 建。
完。

内存虚拟_统计串 接受 档：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"映射\":\"」。
    做 字符串构建器_追加 用 建、做 Nova内存映射_完整统计。
    做 字符串构建器_追加 用 建、「\",\"页表映射\":\"」。
    做 字符串构建器_追加 用 建、做 页表映射_完整统计。
    做 字符串构建器_追加 用 建、「\",\"页表遍历\":\"」。
    做 字符串构建器_追加 用 建、做 页表遍历_完整统计。
    做 字符串构建器_追加 用 建、「\",\"缺页\":\"」。
    做 字符串构建器_追加 用 建、做 内存虚拟_缺页统计。
    做 字符串构建器_追加 用 建、「\"}」。
    返还 做 字符串构建器_构建 用 建。
完。

内存虚拟_健康检查 接受 档：
    可变 错数 为 0。
    若 档 等于 0：
        返还 1。
    完。
    若 档 的 页表 等于 0：
        错数 为 错数 加 1。
    完。
    若 做 页表映射_健康检查 不等于 0：
        错数 为 错数 加 1。
    完。
    若 做 页表遍历_健康检查 不等于 0：
        错数 为 错数 加 1。
    完。
    若 做 计数 用 档 的 区域表 等于 0 并且 档 的 模式 不等于 虚拟模式_内核：
        错数 为 错数 加 1。
    完。
    返还 错数。
完。

内存虚拟_账本健康检查 接受 零参：
    可变 错数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 虚拟账本：
        错数 为 错数 加 做 内存虚拟_健康检查 用 虚拟账本 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 错数。
完。

内存虚拟_总览 接受 零参：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"建数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 虚拟建数。
    做 字符串构建器_追加 用 建、「,\"账本数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 计数 用 虚拟账本。
    做 字符串构建器_追加 用 建、「,\"健康错误\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 内存虚拟_账本健康检查。
    做 字符串构建器_追加 用 建、「,\"明细\":」。
    做 字符串构建器_追加 用 建、做 内存虚拟_批量摘要串 用 虚拟账本。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

内存虚拟_示例流程 接受 零参：
    档 为 做 内存虚拟_创建 用 「主虚拟」。
    空址 为 做 内存虚拟_找空闲 用 16384。
    区 为 做 内存虚拟_建匿名区域 用 档、空址、空址 加 16384、VM_读 位或 VM_写 位或 VM_匿名。
    做 内存虚拟_映射匿名 用 档、空址、16384、PROT_READ 位或 PROT_WRITE、MAP_PRIVATE。
    做 内存虚拟_改范围权限 用 档、空址、16384、PTE_存在 位或 PTE_可写。
    做 内存虚拟_预取页 用 空址。
    做 内存虚拟_区域权限串 用 区。
    做 内存虚拟_映射单页 用 档、空址 加 32768、0x100000、PTE_存在 位或 PTE_可写。
    做 内存虚拟_取消单页 用 档、空址 加 32768。
    做 内存虚拟_重建页表 用 档。
    返还 做 内存虚拟_总览。
完。

可变 AI_内存虚拟_运行指标 为 0。

可变 AI_内存虚拟_异常指标 为 0。

AI_内存虚拟_自诊断 接受 零参：
    AI_内存虚拟_运行指标 为 0。
    返还 AI_内存虚拟_运行指标。
完。

AI_内存虚拟_优化建议 接受 零参：
    指标 为 做 AI_内存虚拟_自诊断。
    返还 「内存虚拟状态正常」。
完。

本章 收束。
