《赫布追加》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·内核打印》。

引用《.·字符串替换》。

引用《.·整数转字符串》。

引用《.·数学工具》。

可变 赫布_学习率 为 100。

可变 _赫布_更新总次 为 0。

可变 _赫布_反赫布总次 为 0。

可变 _赫布_竞争总次 为 0。

赫布_追加 接受 lst、val：
    做 插入 用 lst、做 计数 用 lst、val。
完。

赫布_符号 接受 x：
    若 x 大于 0：
        返还 1。
    完。
    若 x 小于 0：
        返还 0 减 1。
    完。
    返还 0。
完。

赫布_绝对值 接受 x：
    若 x 小于 0：
        返还 0 减 x。
    完。
    返还 x。
完。

赫布_学习符号 接受 频谱列表、频谱数、维度、索引列表、k：
    做 打印 用 「Nova关联学习学习: 」 加 做 转字符串 用 频谱数 加 「个样本, 」 加 做 转字符串 用 k 加 「个系数」。
    可变 关联 为 一组：（空）。
    可变 ci 为 0。
    只要 ci 小于 k 乘 k：
        做 赫布_追加 用 关联、0。
        ci 为 ci 加 1。
    完。
    可变 si 为 0。
    只要 si 小于 频谱数：
        偏移 为 si 乘 维度。
        可变 符号表 为 一组：（空）。
        可变 ki 为 0。
        只要 ki 小于 k：
            idx 为 索引列表 的 第 ki 项。
            val 为 频谱列表 的 第 偏移 加 idx 项。
            做 赫布_追加 用 符号表、做 赫布_符号 用 val。
            ki 为 ki 加 1。
        完。
        可变 i 为 0。
        只要 i 小于 k：
            可变 j 为 0。
            只要 j 小于 k：
                关联 的 第 i 乘 k 加 j 项 为 关联 的 第 i 乘 k 加 j 项 加 符号表 的 第 i 项 乘 符号表 的 第 j 项。
                j 为 j 加 1。
            完。
            i 为 i 加 1。
        完。
        si 为 si 加 1。
    完。
    若 频谱数 大于 0：
        可变 ni 为 0。
        只要 ni 小于 k 乘 k：
            关联 的 第 ni 项 为 关联 的 第 ni 项 乘 1000 除以 频谱数。
            ni 为 ni 加 1。
        完。
    完。
    返还 关联。
完。

赫布_预测符号 接受 关联、已决定、k、n_k：
    可变 得分 为 0。
    可变 j 为 0。
    只要 j 小于 k：
        得分 为 得分 加 关联 的 第 k 乘 n_k 加 j 项 乘 已决定 的 第 j 项 除以 1000。
        j 为 j 加 1。
    完。
    若 得分 大于 0：
        返还 1。
    完。
    返还 0 减 1。
完。

可变 BCM_滑动阈值 为 500。

赫布_BCM更新 接受 权重、输入、输出、维度、学习率：
    调制 为 输出 乘 (输出 减 BCM_滑动阈值) 除以 1000。
    可变 i 为 0。
    只要 i 小于 维度：
        权重 的 第 i 项 为 权重 的 第 i 项 加 学习率 乘 输入 的 第 i 项 乘 调制 除以 1000000。
        i 为 i 加 1。
    完。
    BCM_滑动阈值 为 (BCM_滑动阈值 乘 950 加 输出 乘 输出 除以 1000 乘 50) 除以 1000。
完。

赫布_Oja更新 接受 权重、输入、维度、学习率：
    可变 输出 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        输出 为 输出 加 权重 的 第 i 项 乘 输入 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 维度：
        增量 为 输出 乘 (输入 的 第 i 项 减 输出 乘 权重 的 第 i 项 除以 1000) 除以 1000。
        权重 的 第 i 项 为 权重 的 第 i 项 加 学习率 乘 增量 除以 1000。
        i 为 i 加 1。
    完。
    返还 输出。
完。

赫布_衰减学习率 接受 初始学习率、当前步数、衰减速率、下限：
    因子 为 1000 减 当前步数 乘 衰减速率 除以 1000。
    若 因子 小于 下限：
        因子 为 下限。
    完。
    返还 初始学习率 乘 因子 除以 1000。
完。

赫布_信噪比 接受 关联、k：
    可变 总和 为 0。
    可变 总平方 为 0。
    n 为 k 乘 k。
    可变 i 为 0。
    只要 i 小于 n：
        总和 为 总和 加 关联 的 第 i 项。
        总平方 为 总平方 加 关联 的 第 i 项 乘 关联 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    均值 为 总和 除以 n。
    方差 为 总平方 除以 n 减 均值 乘 均值 除以 1000。
    若 方差 小于或等于 0：
        返还 0。
    完。
    返还 均值 乘 均值 除以 (方差 加 1)。
完。

赫布_反赫布更新 接受 权重、输入、输出、维度、学习率：
    可变 i 为 0。
    只要 i 小于 维度：
        权重 的 第 i 项 为 权重 的 第 i 项 减 学习率 乘 输入 的 第 i 项 乘 输出 除以 1000000。
        i 为 i 加 1。
    完。
    _赫布_反赫布总次 为 _赫布_反赫布总次 加 1。
完。

赫布_双向更新 接受 权重、输入、输出、维度、LTP率、LTD率：
    可变 i 为 0。
    只要 i 小于 维度：
        共活 为 输入 的 第 i 项 乘 输出。
        若 共活 大于 0：
            权重 的 第 i 项 为 权重 的 第 i 项 加 LTP率 乘 输入 的 第 i 项 乘 输出 除以 1000000。
        否则：
            权重 的 第 i 项 为 权重 的 第 i 项 减 LTD率 乘 做 赫布_绝对值 用 输入 的 第 i 项 乘 输出 除以 1000000。
        完。
        i 为 i 加 1。
    完。
    _赫布_更新总次 为 _赫布_更新总次 加 1。
完。

赫布_竞争找胜者 接受 权重矩阵、神经元数、维度、输入：
    可变 最佳 为 0。
    可变 最大相似 为 负 999999。
    可变 n 为 0。
    只要 n 小于 神经元数：
        可变 点积 为 0。
        可变 d 为 0。
        只要 d 小于 维度：
            点积 为 点积 加 权重矩阵 的 第 n 乘 维度 加 d 项 乘 输入 的 第 d 项 除以 1000。
            d 为 d 加 1。
        完。
        若 点积 大于 最大相似：
            最大相似 为 点积。
            最佳 为 n。
        完。
        n 为 n 加 1。
    完。
    返还 最佳。
完。

赫布_竞争学习 接受 权重矩阵、神经元数、维度、输入、学习率：
    胜者 为 做 赫布_竞争找胜者 用 权重矩阵、神经元数、维度、输入。
    可变 d 为 0。
    只要 d 小于 维度：
        差 为 输入 的 第 d 项 减 权重矩阵 的 第 胜者 乘 维度 加 d 项。
        权重矩阵 的 第 胜者 乘 维度 加 d 项 为 权重矩阵 的 第 胜者 乘 维度 加 d 项 加 学习率 乘 差 除以 1000。
        d 为 d 加 1。
    完。
    _赫布_竞争总次 为 _赫布_竞争总次 加 1。
    返还 胜者。
完。

赫布_权重归一 接受 权重、维度、最大范数：
    可变 范数平方 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        范数平方 为 范数平方 加 权重 的 第 i 项 乘 权重 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    若 范数平方 大于 最大范数 乘 最大范数 除以 1000：
        范数 为 做 整数开方 用 范数平方 乘 1000。
        若 范数 大于 0：
            i 为 0。
            只要 i 小于 维度：
                权重 的 第 i 项 为 权重 的 第 i 项 乘 最大范数 除以 范数。
                i 为 i 加 1。
            完。
        完。
    完。
完。

赫布_完整统计 接受 零参：
    返还 一组：一组：「全局学习率」、赫布_学习率、一组：「总更新次数」、_赫布_更新总次、一组：「反赫布次数」、_赫布_反赫布总次、一组：「竞争次数」、_赫布_竞争总次、一组：「BCM滑动阈值」、BCM_滑动阈值。
完。

赫布_Walsh共振学习 接受 关联、k、输入、学习率：
    可变 i 为 0。
    只要 i 小于 k：
        输入符号 为 做 赫布_符号 用 输入 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 k：
            现有 为 关联 的 第 i 乘 k 加 j 项。
            目标符号 为 做 赫布_符号 用 输入 的 第 j 项。
            共振信号 为 输入符号 乘 目标符号。
            衰减 为 做 赫布_绝对值 用 现有 除以 100。
            若 衰减 大于 50：
                衰减 为 50。
            完。
            关联 的 第 i 乘 k 加 j 项 为 现有 乘 (1000 减 衰减) 除以 1000 加 学习率 乘 共振信号 除以 1000。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

赫布_噪声修复 接受 关联、噪声输入、k、最大迭代：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 k：
        做 追加 用 结果、噪声输入 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 迭代 为 0。
    只要 迭代 小于 最大迭代：
        可变 变化 为 0。
        i 为 0。
        只要 i 小于 k：
            可变 得分 为 0。
            可变 j 为 0。
            只要 j 小于 k：
                得分 为 得分 加 关联 的 第 i 乘 k 加 j 项 乘 做 赫布_符号 用 结果 的 第 j 项 除以 1000。
                j 为 j 加 1。
            完。
            新值 为 做 赫布_符号 用 得分。
            若 新值 不等于 做 赫布_符号 用 结果 的 第 i 项：
                变化 为 变化 加 1。
            完。
            结果 的 第 i 项 为 新值 乘 1000。
            i 为 i 加 1。
        完。
        若 变化 等于 0：
            返还 结果。
        完。
        迭代 为 迭代 加 1。
    完。
    返还 结果。
完。

赫布_重置统计 接受 零参：
    _赫布_更新总次 为 0。
    _赫布_反赫布总次 为 0。
    _赫布_竞争总次 为 0。
    BCM_滑动阈值 为 500。
完。

赫布_获取学习率 接受 零参：
    返还 赫布_学习率。
完。

赫布_获取更新数 接受 零参：
    返还 _赫布_更新总次。
完。

赫布_获取反赫布数 接受 零参：
    返还 _赫布_反赫布总次。
完。

赫布_获取竞争数 接受 零参：
    返还 _赫布_竞争总次。
完。

赫布_获取BCM阈值 接受 零参：
    返还 BCM_滑动阈值。
完。

赫布_设置学习率 接受 新率：
    若 新率 大于 0 并且 新率 小于或等于 1000：
        赫布_学习率 为 新率。
    完。
完。

赫布_健康检查 接受 零参：
    可变 问题 为 0。
    若 赫布_学习率 小于或等于 0 或者 赫布_学习率 大于 1000：
        问题 为 问题 加 1。
    完。
    若 BCM_滑动阈值 小于 0：
        问题 为 问题 加 1。
    完。
    若 _赫布_更新总次 大于 100 并且 _赫布_反赫布总次 大于 _赫布_更新总次 乘 9 除以 10：
        问题 为 问题 加 1。
    完。
    返还 问题 等于 0。
完。

赫布_打印统计 接受 零参：
    做 打印行 用 「=== 赫布学习引擎统计 ===」。
    做 打印行 用 「学习率: 」 加 做 整数转字符串 用 赫布_学习率。
    做 打印行 用 「更新次数: 」 加 做 整数转字符串 用 _赫布_更新总次。
    做 打印行 用 「反赫布: 」 加 做 整数转字符串 用 _赫布_反赫布总次。
    做 打印行 用 「竞争: 」 加 做 整数转字符串 用 _赫布_竞争总次。
    做 打印行 用 「BCM阈值: 」 加 做 整数转字符串 用 BCM_滑动阈值。
完。

STDP_时间窗口 为 20。

STDP_增强率 为 100。

STDP_抑制率 为 80。

可变 _赫布_STDP总次 为 0。

赫布_STDP更新 接受 权重、前突触时间、后突触时间：
    _赫布_STDP总次 为 _赫布_STDP总次 加 1。
    差 为 后突触时间 减 前突触时间。
    若 差 大于 0 并且 差 小于或等于 STDP_时间窗口：
        衰减 为 1000 减 差 乘 1000 除以 STDP_时间窗口。
        增量 为 STDP_增强率 乘 衰减 除以 1000。
        返还 权重 加 增量。
    否则若 差 小于 0 并且 负 差 小于或等于 STDP_时间窗口：
        衰减 为 1000 减 负 差 乘 1000 除以 STDP_时间窗口。
        减量 为 STDP_抑制率 乘 衰减 除以 1000。
        新 为 权重 减 减量。
        若 新 小于 0：
            新 为 0。
        完。
        返还 新。
    完。
    返还 权重。
完。

赫布_批量STDP 接受 权重矩阵、神经元数、前时间、后时间：
    可变 i 为 0。
    只要 i 小于 神经元数：
        可变 j 为 0。
        只要 j 小于 神经元数：
            若 i 不等于 j：
                权重矩阵 的 第 i 乘 神经元数 加 j 项 为 做 赫布_STDP更新 用 权重矩阵 的 第 i 乘 神经元数 加 j 项、前时间 的 第 i 项、后时间 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

赫布_稀疏更新 接受 权重矩阵、维度、活跃向量、输入、学习率：
    可变 i 为 0。
    可变 更新数 为 0。
    只要 i 小于 维度：
        若 活跃向量 的 第 i 项 不等于 0：
            可变 j 为 0。
            只要 j 小于 维度：
                若 活跃向量 的 第 j 项 不等于 0：
                    差 为 输入 的 第 i 项 乘 输入 的 第 j 项 除以 1000。
                    权重矩阵 的 第 i 乘 维度 加 j 项 为 权重矩阵 的 第 i 乘 维度 加 j 项 加 学习率 乘 差 除以 1000。
                    更新数 为 更新数 加 1。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 更新数。
完。

衰减_系数 为 999。

赫布_权重衰减 接受 权重矩阵、维度：
    可变 i 为 0。
    只要 i 小于 维度 乘 维度：
        权重矩阵 的 第 i 项 为 权重矩阵 的 第 i 项 乘 衰减_系数 除以 1000。
        i 为 i 加 1。
    完。
完。

赫布_权重裁剪 接受 权重矩阵、维度、阈值：
    可变 裁剪数 为 0。
    可变 i 为 0。
    只要 i 小于 维度 乘 维度：
        若 做 赫布_绝对值 用 权重矩阵 的 第 i 项 小于 阈值：
            权重矩阵 的 第 i 项 为 0。
            裁剪数 为 裁剪数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 裁剪数。
完。

赫布_稀疏度 接受 权重矩阵、维度：
    可变 零数 为 0。
    总数 为 维度 乘 维度。
    可变 i 为 0。
    只要 i 小于 总数：
        若 权重矩阵 的 第 i 项 等于 0：
            零数 为 零数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 总数 等于 0：
        返还 0。
    完。
    返还 零数 乘 1000 除以 总数。
完。

赫布_获取STDP数 接受 零参：
    返还 _赫布_STDP总次。
完。

赫布_权重均值 接受 权重矩阵、维度：
    总数 为 维度 乘 维度。
    若 总数 等于 0：
        返还 0。
    完。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 总数：
        总和 为 总和 加 权重矩阵 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总和 除以 总数。
完。

赫布_权重最大值 接受 权重矩阵、维度：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 维度 乘 维度：
        v 为 做 赫布_绝对值 用 权重矩阵 的 第 i 项。
        若 v 大于 最大：
            最大 为 v。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

赫布_权重范数平方 接受 权重矩阵、维度：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 维度 乘 维度：
        总和 为 总和 加 权重矩阵 的 第 i 项 乘 权重矩阵 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 总和。
完。

赫布_正权重数 接受 权重矩阵、维度：
    可变 正数 为 0。
    可变 i 为 0。
    只要 i 小于 维度 乘 维度：
        若 权重矩阵 的 第 i 项 大于 0：
            正数 为 正数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 正数。
完。

本章 收束。
