《环境变量》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《.·内核打印》。

环境条目 是 一种 事物，含 键、值。

环境_最大条目 为 256。

可变 _全局环境 为 一组：（空）。

可变 _环境已初始化 为 假。

环境_初始化 接受 零参：
    若 _环境已初始化：
        返还。
    完。
    _全局环境 为 一组：（空）。
    做 环境_设置 用 「PATH」、「/usr/bin:/bin:/usr/local/bin」。
    做 环境_设置 用 「HOME」、「/root」。
    做 环境_设置 用 「SHELL」、「/bin/龙壳」。
    做 环境_设置 用 「LANG」、「zh_CN.UTF-8」。
    做 环境_设置 用 「TERM」、「nova-256color」。
    做 环境_设置 用 「EDITOR」、「龙文」。
    做 环境_设置 用 「NOVA_VERSION」、「1.0.0」。
    做 环境_设置 用 「NOVA_ARCH」、「x86_64」。
    做 环境_设置 用 「TMPDIR」、「/tmp」。
    做 环境_设置 用 「XDG_RUNTIME_DIR」、「/run/user/0」。
    _环境已初始化 为 真。
完。

环境_获取 接受 键：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _全局环境：
        若 _全局环境 的 第 i 项 的 键 等于 键：
            返还 _全局环境 的 第 i 项 的 值。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

环境_设置 接受 键、值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _全局环境：
        若 _全局环境 的 第 i 项 的 键 等于 键：
            _全局环境 的 第 i 项 的 值 为 值。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 _全局环境 大于或等于 环境_最大条目：
        返还 错误_资源不足。
    完。
    条目 为 新建 环境条目。
    条目 的 键 为 键。
    条目 的 值 为 值。
    做 追加 用 _全局环境、条目。
    返还 0。
完。

环境_删除 接受 键：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _全局环境：
        若 _全局环境 的 第 i 项 的 键 等于 键：
            做 列表_移除 用 _全局环境、i。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

环境_存在 接受 键：
    返还 做 环境_获取 用 键 不等于 空。
完。

环境_数量 接受 零参：
    返还 做 计数 用 _全局环境。
完。

环境_列出 接受 零参：
    可变 列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _全局环境：
        做 追加 用 列表、_全局环境 的 第 i 项 的 键 加 「=」 加 _全局环境 的 第 i 项 的 值。
        i 为 i 加 1。
    完。
    返还 列表。
完。

环境_复制 接受 零参：
    可变 副本 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _全局环境：
        条目 为 新建 环境条目。
        条目 的 键 为 _全局环境 的 第 i 项 的 键。
        条目 的 值 为 _全局环境 的 第 i 项 的 值。
        做 追加 用 副本、条目。
        i 为 i 加 1。
    完。
    返还 副本。
完。

环境_从副本恢复 接受 副本：
    _全局环境 为 副本。
完。

环境_展开路径 接受 路径：
    可变 结果 为 路径。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _全局环境：
        标记 为 「$」 加 _全局环境 的 第 i 项 的 键。
        若 做 字符串包含 用 结果、标记：
            结果 为 做 字符串替换 用 结果、标记、_全局环境 的 第 i 项 的 值。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

环境_查找PATH中 接受 可执行名：
    路径 为 做 环境_获取 用 「PATH」。
    若 路径 等于 空：
        返还 空。
    完。
    目录列表 为 做 字符串分割 用 路径、「:」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 目录列表：
        完整路径 为 目录列表 的 第 i 项 加 「/」 加 可执行名。
        若 做 文件_存在 用 完整路径：
            返还 完整路径。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

环境_报告 接受 零参：
    可变 r 为 「═══ 环境变量 ═══\n」。
    r 为 r 加 「总数: 」 加 做 整数转字符串 用 做 计数 用 _全局环境 加 「\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _全局环境：
        r 为 r 加 「  」 加 _全局环境 的 第 i 项 的 键 加 「=」 加 _全局环境 的 第 i 项 的 值 加 「\n」。
        i 为 i 加 1。
    完。
    返还 r。
完。

本章 收束。
