《异常入口除零》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·自动扩容》。

引用《.·中断请求通用》。

引用《.·字符串替换》。

引用《.·时钟中枢》。

引用《.·AI中断预测》。

可变 异常计数 为 一组：（空）。

可变 异常_已初始化 为 假。

可变 异常_总计数 为 0。

可变 异常_内核态计数 为 0。

可变 异常_用户态计数 为 0。

可变 异常_致命计数 为 0。

可变 异常_最后向量 为 0。

可变 异常_最后错误码 为 0。

可变 异常_最后CR2 为 0。

可变 异常_错误数 为 0。

可变 异常链 为 一组：（空）。

异常链_最大 为 32。

可变 异常链_写指针 为 0。

异常等级_可恢复 为 0。

异常等级_警告 为 1。

异常等级_严重 为 2。

异常等级_致命 为 3。

异常入口_初始化 接受 零参：
    若 异常_已初始化 等于 真：
        返还 错误_已存在。
    完。
    异常计数 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 32：
        做 追加 用 异常计数、0。
        i 为 i 加 1。
    完。
    异常链 为 一组：（空）。
    i 为 0。
    只要 i 小于 异常链_最大：
        做 追加 用 异常链、一组：0、0、0、0。
        i 为 i 加 1。
    完。
    异常链_写指针 为 0。
    异常_总计数 为 0。
    异常_内核态计数 为 0。
    异常_用户态计数 为 0。
    异常_致命计数 为 0。
    异常_错误数 为 0。
    异常_已初始化 为 真。
    返还 0。
完。

异常入口_记录 接受 向量号、错误码值：
    异常_总计数 为 异常_总计数 加 1。
    异常_最后向量 为 向量号。
    异常_最后错误码 为 错误码值。
    若 异常_已初始化 等于 真 并且 向量号 小于 32：
        做 设置 用 异常计数、向量号、异常计数 的 第 向量号 项 加 1。
    完。
    若 异常_已初始化 等于 真：
        做 设置 用 异常链、异常链_写指针、一组：向量号、错误码值、做 读取毫秒、0。
        异常链_写指针 为 (异常链_写指针 加 1) 取余于 异常链_最大。
    完。
    做 AI中断_记录 用 做 读取毫秒、向量号。
完。

异常入口_记录_缺页 接受 错误码值：
    做 异常入口_记录 用 14、错误码值。
    可变 cr2值 为 0。
    汇编。
    块：
        「mov rax, cr2」。
        「mov [rbp-8], rax」。
    完。
    异常_最后CR2 为 cr2值。
    若 异常_已初始化 等于 真 并且 异常链_写指针 大于 0：
        前一个 为 (异常链_写指针 减 1 加 异常链_最大) 取余于 异常链_最大。
        异常链 的 第 前一个 项 的 第 3 项 为 cr2值。
    完。
完。

异常入口_除零 接受 零参：
    汇编。
    块：
        「push 0」。
        「push 0」。
    完。
    做 保存所有寄存器。
    做 异常入口_记录 用 0、0。
    汇编。
    块：
        「mov rdi, rsp」。
    完。
    做 中断分发_内部 用 0、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_调试 接受 零参：
    汇编。
    块：
        「push 0」。
        「push 1」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 1、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_NMI 接受 零参：
    汇编。
    块：
        「push 0」。
        「push 2」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 2、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_断点 接受 零参：
    汇编。
    块：
        「push 0」。
        「push 3」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 3、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_溢出 接受 零参：
    汇编。
    块：
        「push 0」。
        「push 4」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 4、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_越界 接受 零参：
    汇编。
    块：
        「push 0」。
        「push 5」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 5、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_无效操作码 接受 零参：
    汇编。
    块：
        「push 0」。
        「push 6」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 6、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_无FPU 接受 零参：
    汇编。
    块：
        「push 0」。
        「push 7」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 7、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_双重故障 接受 零参：
    汇编。
    块：
        「push 8」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 8、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_无效TSS 接受 零参：
    汇编。
    块：
        「push 10」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 10、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_段不在 接受 零参：
    汇编。
    块：
        「push 11」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 11、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_栈段错 接受 零参：
    汇编。
    块：
        「push 12」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 12、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_一般保护 接受 零参：
    汇编。
    块：
        「push 13」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 13、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_缺页 接受 零参：
    汇编。
    块：
        「push 14」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 14、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_浮点 接受 零参：
    汇编。
    块：
        「push 0」。
        「push 16」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 16、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_对齐检查 接受 零参：
    汇编。
    块：
        「push 17」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 17、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_机器检查 接受 零参：
    汇编。
    块：
        「push 0」。
        「push 18」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 18、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_SIMD浮点 接受 零参：
    汇编。
    块：
        「push 0」。
        「push 19」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 19、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_虚拟化 接受 零参：
    汇编。
    块：
        「push 0」。
        「push 20」。
    完。
    做 保存所有寄存器。
    做 中断分发_内部 用 20、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_控制保护 接受 零参：
    汇编。
    块：
        「push 21」。
    完。
    做 保存所有寄存器。
    做 异常入口_记录 用 21、0。
    做 中断分发_内部 用 21、0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

异常入口_除零地址 接受 零参：
    返还 异常入口_除零。
完。

异常入口_调试地址 接受 零参：
    返还 异常入口_调试。
完。

异常入口_NMI地址 接受 零参：
    返还 异常入口_NMI。
完。

异常入口_断点地址 接受 零参：
    返还 异常入口_断点。
完。

异常入口_溢出地址 接受 零参：
    返还 异常入口_溢出。
完。

异常入口_越界地址 接受 零参：
    返还 异常入口_越界。
完。

异常入口_无效操作码地址 接受 零参：
    返还 异常入口_无效操作码。
完。

异常入口_无FPU地址 接受 零参：
    返还 异常入口_无FPU。
完。

异常入口_双重故障地址 接受 零参：
    返还 异常入口_双重故障。
完。

异常入口_无效TSS地址 接受 零参：
    返还 异常入口_无效TSS。
完。

异常入口_段不在地址 接受 零参：
    返还 异常入口_段不在。
完。

异常入口_栈段错地址 接受 零参：
    返还 异常入口_栈段错。
完。

异常入口_一般保护地址 接受 零参：
    返还 异常入口_一般保护。
完。

异常入口_缺页地址 接受 零参：
    返还 异常入口_缺页。
完。

异常入口_浮点地址 接受 零参：
    返还 异常入口_浮点。
完。

异常入口_对齐检查地址 接受 零参：
    返还 异常入口_对齐检查。
完。

异常入口_机器检查地址 接受 零参：
    返还 异常入口_机器检查。
完。

异常入口_SIMD浮点地址 接受 零参：
    返还 异常入口_SIMD浮点。
完。

异常入口_虚拟化地址 接受 零参：
    返还 异常入口_虚拟化。
完。

异常入口_控制保护地址 接受 零参：
    返还 异常入口_控制保护。
完。

异常_获取严重等级 接受 向量号：
    若 向量号 等于 8 或者 向量号 等于 18：
        返还 异常等级_致命。
    完。
    若 向量号 等于 13 或者 向量号 等于 14 或者 向量号 等于 12：
        返还 异常等级_严重。
    完。
    若 向量号 等于 10 或者 向量号 等于 11：
        返还 异常等级_严重。
    完。
    若 向量号 等于 1 或者 向量号 等于 3：
        返还 异常等级_可恢复。
    完。
    返还 异常等级_警告。
完。

异常_等级名 接受 等级：
    若 等级 等于 异常等级_可恢复：
        返还 「可恢复」。
    完。
    若 等级 等于 异常等级_警告：
        返还 「警告」。
    完。
    若 等级 等于 异常等级_严重：
        返还 「严重」。
    完。
    若 等级 等于 异常等级_致命：
        返还 「致命」。
    完。
    返还 「未知」。
完。

异常_是致命 接受 向量号：
    返还 做 异常_获取严重等级 用 向量号 等于 异常等级_致命。
完。

异常_需要信号 接受 向量号：
    若 向量号 等于 0：
        返还 真。
    完。
    若 向量号 等于 4：
        返还 真。
    完。
    若 向量号 等于 5：
        返还 真。
    完。
    若 向量号 等于 6：
        返还 真。
    完。
    若 向量号 等于 11：
        返还 真。
    完。
    若 向量号 等于 12：
        返还 真。
    完。
    若 向量号 等于 13：
        返还 真。
    完。
    若 向量号 等于 14：
        返还 真。
    完。
    若 向量号 等于 16：
        返还 真。
    完。
    若 向量号 等于 17：
        返还 真。
    完。
    若 向量号 等于 19：
        返还 真。
    完。
    返还 假。
完。

异常_对应信号 接受 向量号：
    若 向量号 等于 0 或者 向量号 等于 4 或者 向量号 等于 16 或者 向量号 等于 19：
        返还 8。
    完。
    若 向量号 等于 6：
        返还 4。
    完。
    若 向量号 等于 5 或者 向量号 等于 12 或者 向量号 等于 13 或者 向量号 等于 14：
        返还 11。
    完。
    若 向量号 等于 11 或者 向量号 等于 17：
        返还 7。
    完。
    返还 0。
完。

异常_名称 接受 向量号：
    若 向量号 等于 0：
        返还 「#DE 除零异常」。
    完。
    若 向量号 等于 1：
        返还 「#DB 调试异常」。
    完。
    若 向量号 等于 2：
        返还 「NMI 不可屏蔽中断」。
    完。
    若 向量号 等于 3：
        返还 「#BP 断点」。
    完。
    若 向量号 等于 4：
        返还 「#OF 溢出」。
    完。
    若 向量号 等于 5：
        返还 「#BR 边界越界」。
    完。
    若 向量号 等于 6：
        返还 「#UD 无效操作码」。
    完。
    若 向量号 等于 7：
        返还 「#NM 设备不可用(无FPU)」。
    完。
    若 向量号 等于 8：
        返还 「#DF 双重故障」。
    完。
    若 向量号 等于 10：
        返还 「#TS 无效TSS」。
    完。
    若 向量号 等于 11：
        返还 「#NP 段不存在」。
    完。
    若 向量号 等于 12：
        返还 「#SS 栈段故障」。
    完。
    若 向量号 等于 13：
        返还 「#GP 一般保护故障」。
    完。
    若 向量号 等于 14：
        返还 「#PF 缺页故障」。
    完。
    若 向量号 等于 16：
        返还 「#MF x87浮点异常」。
    完。
    若 向量号 等于 17：
        返还 「#AC 对齐检查」。
    完。
    若 向量号 等于 18：
        返还 「#MC 机器检查」。
    完。
    若 向量号 等于 19：
        返还 「#XM SIMD浮点异常」。
    完。
    若 向量号 等于 20：
        返还 「#VE 虚拟化异常」。
    完。
    若 向量号 等于 21：
        返还 「#CP 控制保护异常」。
    完。
    返还 「异常#」 加 做 转字符串 用 向量号。
完。

异常_检测重复模式 接受 零参：
    若 异常_已初始化 等于 假：
        返还 假。
    完。
    可变 连续数 为 1。
    可变 i 为 1。
    只要 i 小于 异常链_最大：
        当前 为 (异常链_写指针 减 i 加 异常链_最大) 取余于 异常链_最大。
        前一个 为 (异常链_写指针 减 i 减 1 加 异常链_最大) 取余于 异常链_最大。
        若 异常链 的 第 当前 项 的 第 0 项 等于 异常链 的 第 前一个 项 的 第 0 项 并且 异常链 的 第 当前 项 的 第 0 项 不等于 0：
            连续数 为 连续数 加 1。
        否则：
            若 连续数 大于 3：
                返还 真。
            完。
            连续数 为 1。
        完。
        i 为 i 加 1。
    完。
    返还 连续数 大于 3。
完。

异常_获取最频繁 接受 零参：
    若 异常_已初始化 等于 假：
        返还 0 减 1。
    完。
    可变 最大 为 0。
    可变 最大向量 为 0 减 1。
    可变 i 为 0。
    只要 i 小于 32：
        若 异常计数 的 第 i 项 大于 最大：
            最大 为 异常计数 的 第 i 项。
            最大向量 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最大向量。
完。

异常入口_完整统计 接受 零参：
    统计 为 做 字典。
    做 字典设 用 统计、「已初始化」、异常_已初始化。
    做 字典设 用 统计、「总计数」、异常_总计数。
    做 字典设 用 统计、「内核态计数」、异常_内核态计数。
    做 字典设 用 统计、「用户态计数」、异常_用户态计数。
    做 字典设 用 统计、「致命计数」、异常_致命计数。
    做 字典设 用 统计、「最后向量」、异常_最后向量。
    做 字典设 用 统计、「最后错误码」、异常_最后错误码。
    做 字典设 用 统计、「最后CR2」、异常_最后CR2。
    做 字典设 用 统计、「链写指针」、异常链_写指针。
    做 字典设 用 统计、「最频繁异常」、做 异常_获取最频繁。
    做 字典设 用 统计、「重复模式」、做 异常_检测重复模式。
    做 字典设 用 统计、「错误数」、异常_错误数。
    若 异常_已初始化 等于 真：
        做 字典设 用 统计、「除零次数」、异常计数 的 第 0 项。
        做 字典设 用 统计、「GP次数」、异常计数 的 第 13 项。
        做 字典设 用 统计、「缺页次数」、异常计数 的 第 14 项。
        做 字典设 用 统计、「双重故障次数」、异常计数 的 第 8 项。
    完。
    返还 统计。
完。

异常入口_打印统计 接受 零参：
    统计 为 做 异常入口_完整统计。
    做 打印 用 「[异常入口] ═══════ 完整统计 ═══════」。
    做 打印 用 「  总异常:       」 加 做 转字符串 用 做 字典取 用 统计、「总计数」。
    做 打印 用 「  内核态:       」 加 做 转字符串 用 做 字典取 用 统计、「内核态计数」。
    做 打印 用 「  用户态:       」 加 做 转字符串 用 做 字典取 用 统计、「用户态计数」。
    做 打印 用 「  致命:         」 加 做 转字符串 用 做 字典取 用 统计、「致命计数」。
    做 打印 用 「  最后异常:     」 加 做 异常_名称 用 做 字典取 用 统计、「最后向量」。
    做 打印 用 「  最后错误码:   0x」 加 做 转字符串 用 做 字典取 用 统计、「最后错误码」。
    做 打印 用 「  最后CR2:      0x」 加 做 转字符串 用 做 字典取 用 统计、「最后CR2」。
    做 打印 用 「  最频繁:       」 加 做 异常_名称 用 做 字典取 用 统计、「最频繁异常」。
    做 打印 用 「  重复模式:     」 加 做 转字符串 用 做 字典取 用 统计、「重复模式」。
    若 异常_已初始化 等于 真：
        做 打印 用 「  ── 各异常计数 ──」。
        可变 i 为 0。
        只要 i 小于 32：
            若 异常计数 的 第 i 项 大于 0：
                做 打印 用 「  」 加 做 异常_名称 用 i 加 「: 」 加 做 转字符串 用 异常计数 的 第 i 项 加 「 [」 加 做 异常_等级名 用 做 异常_获取严重等级 用 i 加 「]」。
            完。
            i 为 i 加 1。
        完。
    完。
    做 打印 用 「[异常入口] ═════════════════════════」。
    返还 0。
完。

异常入口_dump链 接受 零参：
    若 异常_已初始化 等于 假：
        做 打印 用 「[异常链] 未初始化」。
        返还。
    完。
    做 打印 用 「[异常链] ── 最近异常 ──」。
    可变 i 为 0。
    只要 i 小于 异常链_最大：
        索引 为 (异常链_写指针 减 1 减 i 加 异常链_最大) 取余于 异常链_最大。
        条目 为 异常链 的 第 索引 项。
        若 条目 的 第 2 项 大于 0：
            做 打印 用 「  [」 加 做 转字符串 用 i 加 「] 」 加 做 异常_名称 用 条目 的 第 0 项 加 「 错误码=0x」 加 做 转字符串 用 条目 的 第 1 项 加 「 时间=」 加 做 转字符串 用 条目 的 第 2 项 加 「 CR2=0x」 加 做 转字符串 用 条目 的 第 3 项。
        完。
        i 为 i 加 1。
    完。
完。

异常入口_dump 接受 零参：
    做 打印 用 「[异常入口 dump] ──────────────────」。
    做 打印 用 「  已初始化: 」 加 做 转字符串 用 异常_已初始化。
    做 打印 用 「  总计数: 」 加 做 转字符串 用 异常_总计数。
    做 打印 用 「  最后向量: 」 加 做 转字符串 用 异常_最后向量 加 「 (」 加 做 异常_名称 用 异常_最后向量 加 「)」。
    做 打印 用 「  最后错误码: 0x」 加 做 转字符串 用 异常_最后错误码。
    做 打印 用 「  最后CR2: 0x」 加 做 转字符串 用 异常_最后CR2。
    做 打印 用 「  链写指针: 」 加 做 转字符串 用 异常链_写指针。
    做 打印 用 「[异常入口 dump] ──────────────────」。
完。

异常入口_销毁 接受 零参：
    若 异常_已初始化 等于 假：
        返还 0。
    完。
    异常计数 为 一组：（空）。
    异常链 为 一组：（空）。
    异常_已初始化 为 假。
    做 打印 用 「[异常入口] 已销毁」。
    返还 0。
完。

异常入口_为信号子系统提供信号 接受 向量号：
    若 做 异常_需要信号 用 向量号 等于 假：
        返还 0。
    完。
    返还 做 异常_对应信号 用 向量号。
完。

异常入口_为进程管理提供异常信息 接受 零参：
    返还 一组：异常_最后向量、异常_最后错误码、异常_最后CR2、做 异常_获取严重等级 用 异常_最后向量。
完。

异常入口_为AI提供异常模式 接受 零参：
    返还 一组：异常_总计数、做 异常_获取最频繁、做 异常_检测重复模式。
完。

异常入口_为procfs提供统计 接受 零参：
    若 异常_已初始化 等于 假：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 32：
        若 异常计数 的 第 i 项 大于 0：
            做 追加 用 结果、一组：i、异常计数 的 第 i 项、做 异常_名称 用 i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

异常入口_重置统计 接受 零参：
    异常_总计数 为 0。
    异常_内核态计数 为 0。
    异常_用户态计数 为 0。
    异常_致命计数 为 0。
    异常_错误数 为 0。
    若 异常_已初始化 等于 真：
        可变 i 为 0。
        只要 i 小于 32：
            做 设置 用 异常计数、i、0。
            i 为 i 加 1。
        完。
    完。
    做 打印 用 「[异常入口] 统计已重置」。
    返还 0。
完。

本章 收束。
