《每处理器》

引用《.·日志中枢》。

引用《.·类型定义》。

引用《.·错误码》。

引用《.·字符串替换》。

最大CPU数 为 256。

默认CPU数 为 1。

每CPU最大键数 为 512。

键名最大长度 为 64。

快照缓冲大小 为 4096。

AI预测窗口 为 16。

AI预取阈值 为 8。

NUMA最大节点数 为 8。

缓存行大小 为 64。

热键衰减因子 为 950。

热键衰减缩放 为 1000。

PERCPU_当前进程 为 0。

PERCPU_内核栈顶 为 1。

PERCPU_中断栈顶 为 2。

PERCPU_TSS基址 为 3。

PERCPU_GDT基址 为 4。

PERCPU_IDT基址 为 5。

PERCPU_APIC_ID 为 6。

PERCPU_抢占计数 为 7。

PERCPU_软中断挂起 为 8。

PERCPU_空闲时间 为 9。

PERCPU_中断计数 为 10。

PERCPU_上下文切换 为 11。

PERCPU_运行队列长 为 12。

PERCPU_负载均值 为 13。

PERCPU_最后调度时间 为 14。

PERCPU_CPU频率 为 15。

PERCPU_温度 为 16。

PERCPU_功耗 为 17。

PERCPU_缓存未命中 为 18。

PERCPU_分支预测失败 为 19。

预定义键数 为 20。

每CPU数据区 是 一种 事物，含 键值对、键数量、APIC编号、NUMA节点、在线、活跃、读计数、写计数。

键注册项 是 一种 事物，含 键编号、键名、默认值、已注册、访问计数、最后访问时间。

AI负载预测器 是 一种 事物，含 历史窗口、窗口位置、窗口大小、预测值、累计误差、调整次数。

每处理器统计 是 一种 事物，含 总读次数、总写次数、跨CPU读次数、跨CPU写次数、键注册次数、键注销次数、快照次数、汇总次数、原子操作次数、比较交换成功数、比较交换失败数、AI预测次数、AI预测命中数、AI预取触发数、NUMA跨节点访问数、缓存行对齐修正数、迁移次数、错误次数。

可变 CPU数量 为 默认CPU数。

可变 当前CPU编号 为 0。

可变 每CPU区域列表 为 一组：（空）。

可变 键注册表 为 一组：（空）。

可变 下一个键编号 为 预定义键数。

可变 已初始化 为 假。

可变 全局统计 为 新建 每处理器统计。

可变 AI预测器 为 新建 AI负载预测器。

可变 NUMA拓扑 为 一组：（空）。

每处理器_初始化 接受 cpu数：
    若 已初始化 等于 真：
        做 打印 用 「[每处理器] 警告: 重复初始化」。
        返还 错误_已存在。
    完。
    若 cpu数 小于或等于 0：
        做 打印 用 「[每处理器] 错误: CPU数量无效」。
        返还 错误_无效参数。
    完。
    若 cpu数 大于 最大CPU数：
        cpu数 为 最大CPU数。
        做 打印 用 「[每处理器] 警告: CPU数量截断到最大值」。
    完。
    CPU数量 为 cpu数。
    每CPU区域列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 CPU数量：
        可变 区域 为 新建 每CPU数据区。
        区域 的 键值对 为 一组：（空）。
        区域 的 键数量 为 0。
        区域 的 APIC编号 为 i。
        区域 的 NUMA节点 为 i 除以 (CPU数量 除以 NUMA最大节点数 加 1)。
        区域 的 在线 为 真。
        区域 的 活跃 为 假。
        区域 的 读计数 为 0。
        区域 的 写计数 为 0。
        可变 j 为 0。
        只要 j 小于 预定义键数：
            做 追加 用 区域 的 键值对、一组：j、0。
            j 为 j 加 1。
        完。
        区域 的 键数量 为 预定义键数。
        做 追加 用 每CPU区域列表、区域。
        i 为 i 加 1。
    完。
    键注册表 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 预定义键数：
        可变 项 为 新建 键注册项。
        项 的 键编号 为 k。
        项 的 键名 为 k。
        项 的 默认值 为 0。
        项 的 已注册 为 真。
        项 的 访问计数 为 0。
        项 的 最后访问时间 为 0。
        做 追加 用 键注册表、项。
        k 为 k 加 1。
    完。
    NUMA拓扑 为 一组：（空）。
    可变 n 为 0。
    只要 n 小于 NUMA最大节点数：
        做 追加 用 NUMA拓扑、一组：（空）。
        n 为 n 加 1。
    完。
    可变 c 为 0。
    只要 c 小于 CPU数量：
        节点 为 每CPU区域列表 的 第 c 项 的 NUMA节点。
        做 追加 用 NUMA拓扑 的 第 节点 项、c。
        c 为 c 加 1。
    完。
    AI预测器 的 历史窗口 为 一组：（空）。
    可变 w 为 0。
    只要 w 小于 AI预测窗口：
        做 追加 用 AI预测器 的 历史窗口、0。
        w 为 w 加 1。
    完。
    AI预测器 的 窗口位置 为 0。
    AI预测器 的 窗口大小 为 AI预测窗口。
    AI预测器 的 预测值 为 0。
    AI预测器 的 累计误差 为 0。
    AI预测器 的 调整次数 为 0。
    已初始化 为 真。
    做 打印 用 做 字符串拼接 用 「[每处理器] 初始化完成, CPU数: 」、做 转字符串 用 CPU数量。
    返还 0。
完。

每处理器_销毁 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 CPU数量：
        每CPU区域列表 的 第 i 项 的 键值对 为 一组：（空）。
        每CPU区域列表 的 第 i 项 的 键数量 为 0。
        每CPU区域列表 的 第 i 项 的 在线 为 假。
        i 为 i 加 1。
    完。
    每CPU区域列表 为 一组：（空）。
    键注册表 为 一组：（空）。
    NUMA拓扑 为 一组：（空）。
    已初始化 为 假。
    做 打印 用 「[每处理器] 已销毁, 所有CPU区域已释放」。
    返还 0。
完。

获取当前CPU 接受 零参：
    返还 当前CPU编号。
完。

设置当前CPU 接受 id：
    若 id 小于 0 或者 id 大于或等于 CPU数量：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    当前CPU编号 为 id。
    返还 0。
完。

获取CPU数量 接受 零参：
    返还 CPU数量。
完。

CPU是否在线 接受 cpu：
    若 cpu 小于 0 或者 cpu 大于或等于 CPU数量：
        返还 假。
    完。
    返还 每CPU区域列表 的 第 cpu 项 的 在线。
完。

设置CPU在线 接受 cpu、在线：
    若 cpu 小于 0 或者 cpu 大于或等于 CPU数量：
        返还 错误_无效参数。
    完。
    每CPU区域列表 的 第 cpu 项 的 在线 为 在线。
    若 在线 等于 真：
        做 打印 用 做 字符串拼接 用 「[每处理器] CPU上线: 」、做 转字符串 用 cpu。
    否则：
        做 打印 用 做 字符串拼接 用 「[每处理器] CPU下线: 」、做 转字符串 用 cpu。
    完。
    返还 0。
完。

获取在线CPU数 接受 零参：
    可变 计数器 为 0。
    可变 i 为 0。
    只要 i 小于 CPU数量：
        若 每CPU区域列表 的 第 i 项 的 在线 等于 真：
            计数器 为 计数器 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计数器。
完。

this_cpu_read 接受 键：
    结果 为 做 per_cpu_read 用 当前CPU编号、键。
    全局统计 的 总读次数 为 全局统计 的 总读次数 加 1。
    返还 结果。
完。

this_cpu_write 接受 键、值：
    结果 为 做 per_cpu_write 用 当前CPU编号、键、值。
    全局统计 的 总写次数 为 全局统计 的 总写次数 加 1。
    返还 结果。
完。

per_cpu_read 接受 cpu、键：
    若 已初始化 等于 假：
        返还 空。
    完。
    若 cpu 小于 0 或者 cpu 大于或等于 CPU数量：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 空。
    完。
    若 每CPU区域列表 的 第 cpu 项 的 在线 等于 假：
        返还 空。
    完。
    数据 为 每CPU区域列表 的 第 cpu 项 的 键值对。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 的 第 0 项 等于 键：
            每CPU区域列表 的 第 cpu 项 的 读计数 为 每CPU区域列表 的 第 cpu 项 的 读计数 加 1。
            若 键 小于 做 计数 用 键注册表：
                键注册表 的 第 键 项 的 访问计数 为 键注册表 的 第 键 项 的 访问计数 加 1。
            完。
            若 cpu 不等于 当前CPU编号：
                全局统计 的 跨CPU读次数 为 全局统计 的 跨CPU读次数 加 1。
                若 每CPU区域列表 的 第 cpu 项 的 NUMA节点 不等于 每CPU区域列表 的 第 当前CPU编号 项 的 NUMA节点：
                    全局统计 的 NUMA跨节点访问数 为 全局统计 的 NUMA跨节点访问数 加 1。
                完。
            完。
            返还 数据 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

per_cpu_write 接受 cpu、键、值：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    若 cpu 小于 0 或者 cpu 大于或等于 CPU数量：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    若 每CPU区域列表 的 第 cpu 项 的 在线 等于 假：
        返还 错误_无效参数。
    完。
    数据 为 每CPU区域列表 的 第 cpu 项 的 键值对。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 的 第 0 项 等于 键：
            做 设置 用 数据 的 第 i 项、1、值。
            每CPU区域列表 的 第 cpu 项 的 写计数 为 每CPU区域列表 的 第 cpu 项 的 写计数 加 1。
            若 cpu 不等于 当前CPU编号：
                全局统计 的 跨CPU写次数 为 全局统计 的 跨CPU写次数 加 1。
            完。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    若 每CPU区域列表 的 第 cpu 项 的 键数量 大于或等于 每CPU最大键数：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_内存不足。
    完。
    做 追加 用 数据、一组：键、值。
    每CPU区域列表 的 第 cpu 项 的 键数量 为 每CPU区域列表 的 第 cpu 项 的 键数量 加 1。
    每CPU区域列表 的 第 cpu 项 的 写计数 为 每CPU区域列表 的 第 cpu 项 的 写计数 加 1。
    返还 0。
完。

per_cpu_delete 接受 cpu、键：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    若 cpu 小于 0 或者 cpu 大于或等于 CPU数量：
        返还 错误_无效参数。
    完。
    数据 为 每CPU区域列表 的 第 cpu 项 的 键值对。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 的 第 0 项 等于 键：
            做 删除 用 数据、i。
            每CPU区域列表 的 第 cpu 项 的 键数量 为 每CPU区域列表 的 第 cpu 项 的 键数量 减 1。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 错误_未找到。
完。

this_cpu_add 接受 键、增量：
    旧值 为 做 this_cpu_read 用 键。
    若 旧值 等于 空：
        旧值 为 0。
    完。
    做 this_cpu_write 用 键、旧值 加 增量。
    全局统计 的 原子操作次数 为 全局统计 的 原子操作次数 加 1。
    返还 旧值 加 增量。
完。

this_cpu_sub 接受 键、减量：
    旧值 为 做 this_cpu_read 用 键。
    若 旧值 等于 空：
        旧值 为 0。
    完。
    做 this_cpu_write 用 键、旧值 减 减量。
    全局统计 的 原子操作次数 为 全局统计 的 原子操作次数 加 1。
    返还 旧值 减 减量。
完。

this_cpu_inc 接受 键：
    返还 做 this_cpu_add 用 键、1。
完。

this_cpu_dec 接受 键：
    返还 做 this_cpu_sub 用 键、1。
完。

this_cpu_or 接受 键、掩码：
    旧值 为 做 this_cpu_read 用 键。
    若 旧值 等于 空：
        旧值 为 0。
    完。
    做 this_cpu_write 用 键、旧值 位或 掩码。
    全局统计 的 原子操作次数 为 全局统计 的 原子操作次数 加 1。
    返还 旧值 位或 掩码。
完。

this_cpu_and 接受 键、掩码：
    旧值 为 做 this_cpu_read 用 键。
    若 旧值 等于 空：
        旧值 为 0。
    完。
    做 this_cpu_write 用 键、旧值 位与 掩码。
    全局统计 的 原子操作次数 为 全局统计 的 原子操作次数 加 1。
    返还 旧值 位与 掩码。
完。

this_cpu_xchg 接受 键、新值：
    旧值 为 做 this_cpu_read 用 键。
    做 this_cpu_write 用 键、新值。
    全局统计 的 原子操作次数 为 全局统计 的 原子操作次数 加 1。
    返还 旧值。
完。

this_cpu_cmpxchg 接受 键、期望值、新值：
    当前值 为 做 this_cpu_read 用 键。
    若 当前值 等于 期望值：
        做 this_cpu_write 用 键、新值。
        全局统计 的 比较交换成功数 为 全局统计 的 比较交换成功数 加 1。
        全局统计 的 原子操作次数 为 全局统计 的 原子操作次数 加 1。
        返还 真。
    完。
    全局统计 的 比较交换失败数 为 全局统计 的 比较交换失败数 加 1。
    全局统计 的 原子操作次数 为 全局统计 的 原子操作次数 加 1。
    返还 假。
完。

per_cpu_sum 接受 键：
    可变 总和 为 0。
    可变 cpu 为 0。
    全局统计 的 汇总次数 为 全局统计 的 汇总次数 加 1。
    只要 cpu 小于 CPU数量：
        若 每CPU区域列表 的 第 cpu 项 的 在线 等于 真：
            值 为 做 per_cpu_read 用 cpu、键。
            若 值 不等于 空：
                总和 为 总和 加 值。
            完。
        完。
        cpu 为 cpu 加 1。
    完。
    返还 总和。
完。

per_cpu_max 接受 键：
    可变 最大 为 0。
    可变 已找到 为 假。
    可变 cpu 为 0。
    只要 cpu 小于 CPU数量：
        若 每CPU区域列表 的 第 cpu 项 的 在线 等于 真：
            值 为 做 per_cpu_read 用 cpu、键。
            若 值 不等于 空：
                若 已找到 等于 假 或者 值 大于 最大：
                    最大 为 值。
                    已找到 为 真。
                完。
            完。
        完。
        cpu 为 cpu 加 1。
    完。
    若 已找到 等于 假：
        返还 0。
    完。
    返还 最大。
完。

per_cpu_min 接受 键：
    可变 最小 为 0。
    可变 已找到 为 假。
    可变 cpu 为 0。
    只要 cpu 小于 CPU数量：
        若 每CPU区域列表 的 第 cpu 项 的 在线 等于 真：
            值 为 做 per_cpu_read 用 cpu、键。
            若 值 不等于 空：
                若 已找到 等于 假 或者 值 小于 最小：
                    最小 为 值。
                    已找到 为 真。
                完。
            完。
        完。
        cpu 为 cpu 加 1。
    完。
    若 已找到 等于 假：
        返还 0。
    完。
    返还 最小。
完。

per_cpu_avg 接受 键：
    在线数 为 做 获取在线CPU数。
    若 在线数 等于 0：
        返还 0。
    完。
    返还 做 per_cpu_sum 用 键 除以 在线数。
完。

per_cpu_方差 接受 键：
    均值 为 做 per_cpu_avg 用 键。
    可变 方差和 为 0。
    可变 计数器 为 0。
    可变 cpu 为 0。
    只要 cpu 小于 CPU数量：
        若 每CPU区域列表 的 第 cpu 项 的 在线 等于 真：
            值 为 做 per_cpu_read 用 cpu、键。
            若 值 不等于 空：
                差 为 值 减 均值。
                方差和 为 方差和 加 差 乘 差。
                计数器 为 计数器 加 1。
            完。
        完。
        cpu 为 cpu 加 1。
    完。
    若 计数器 等于 0：
        返还 0。
    完。
    返还 方差和 除以 计数器。
完。

per_cpu_collect 接受 键：
    可变 结果 为 一组：（空）。
    可变 cpu 为 0。
    只要 cpu 小于 CPU数量：
        若 每CPU区域列表 的 第 cpu 项 的 在线 等于 真：
            值 为 做 per_cpu_read 用 cpu、键。
            做 追加 用 结果、一组：cpu、值。
        完。
        cpu 为 cpu 加 1。
    完。
    返还 结果。
完。

每处理器_注册键 接受 键名、默认值：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    若 下一个键编号 大于或等于 每CPU最大键数：
        返还 错误_内存不足。
    完。
    新键 为 下一个键编号。
    下一个键编号 为 下一个键编号 加 1。
    可变 项 为 新建 键注册项。
    项 的 键编号 为 新键。
    项 的 键名 为 键名。
    项 的 默认值 为 默认值。
    项 的 已注册 为 真。
    项 的 访问计数 为 0。
    项 的 最后访问时间 为 0。
    做 追加 用 键注册表、项。
    可变 cpu 为 0。
    只要 cpu 小于 CPU数量：
        若 每CPU区域列表 的 第 cpu 项 的 在线 等于 真：
            做 per_cpu_write 用 cpu、新键、默认值。
        完。
        cpu 为 cpu 加 1。
    完。
    全局统计 的 键注册次数 为 全局统计 的 键注册次数 加 1。
    返还 新键。
完。

每处理器_注销键 接受 键编号：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    若 键编号 小于 预定义键数：
        返还 错误_无效参数。
    完。
    可变 cpu 为 0。
    只要 cpu 小于 CPU数量：
        做 per_cpu_delete 用 cpu、键编号。
        cpu 为 cpu 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键注册表：
        若 键注册表 的 第 i 项 的 键编号 等于 键编号：
            键注册表 的 第 i 项 的 已注册 为 假。
        完。
        i 为 i 加 1。
    完。
    全局统计 的 键注销次数 为 全局统计 的 键注销次数 加 1。
    返还 0。
完。

每处理器_枚举键 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键注册表：
        若 键注册表 的 第 i 项 的 已注册 等于 真：
            做 追加 用 结果、一组：键注册表 的 第 i 项 的 键编号、键注册表 的 第 i 项 的 键名。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

获取当前进程 接受 零参：
    返还 做 this_cpu_read 用 PERCPU_当前进程。
完。

设置当前进程 接受 任务：
    返还 做 this_cpu_write 用 PERCPU_当前进程、任务。
完。

获取抢占计数 接受 零参：
    返还 做 this_cpu_read 用 PERCPU_抢占计数。
完。

禁止抢占 接受 零参：
    做 this_cpu_add 用 PERCPU_抢占计数、1。
    返还 0。
完。

允许抢占 接受 零参：
    当前 为 做 this_cpu_read 用 PERCPU_抢占计数。
    若 当前 等于 空 或者 当前 小于或等于 0：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    做 this_cpu_sub 用 PERCPU_抢占计数、1。
    返还 0。
完。

是否可抢占 接受 零参：
    计数器 为 做 this_cpu_read 用 PERCPU_抢占计数。
    若 计数器 等于 空 或者 计数器 等于 0：
        返还 真。
    完。
    返还 假。
完。

记录中断 接受 零参：
    做 this_cpu_inc 用 PERCPU_中断计数。
    返还 0。
完。

记录上下文切换 接受 零参：
    做 this_cpu_inc 用 PERCPU_上下文切换。
    返还 0。
完。

获取软中断挂起 接受 零参：
    返还 做 this_cpu_read 用 PERCPU_软中断挂起。
完。

设置软中断挂起 接受 掩码：
    返还 做 this_cpu_or 用 PERCPU_软中断挂起、掩码。
完。

清除软中断挂起 接受 掩码：
    返还 做 this_cpu_and 用 PERCPU_软中断挂起、按位取反 掩码。
完。

AI_记录负载样本 接受 负载值：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    做 设置 用 AI预测器 的 历史窗口、AI预测器 的 窗口位置、负载值。
    AI预测器 的 窗口位置 为 (AI预测器 的 窗口位置 加 1) 取余于 AI预测器 的 窗口大小。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
    返还 0。
完。

AI_预测下一负载 接受 零参：
    可变 加权和 为 0。
    可变 权重和 为 0。
    可变 权重 为 1。
    可变 i 为 0。
    只要 i 小于 AI预测器 的 窗口大小：
        索引 为 (AI预测器 的 窗口位置 减 1 减 i 加 AI预测器 的 窗口大小) 取余于 AI预测器 的 窗口大小。
        样本 为 AI预测器 的 历史窗口 的 第 索引 项。
        加权和 为 加权和 加 样本 乘 权重。
        权重和 为 权重和 加 权重。
        权重 为 权重 乘 热键衰减因子 除以 热键衰减缩放。
        若 权重 小于或等于 0：
            权重 为 1。
        完。
        i 为 i 加 1。
    完。
    若 权重和 等于 0：
        返还 0。
    完。
    AI预测器 的 预测值 为 加权和 除以 权重和。
    返还 AI预测器 的 预测值。
完。

AI_评估预测精度 接受 实际值：
    误差 为 实际值 减 AI预测器 的 预测值。
    若 误差 小于 0：
        误差 为 0 减 误差。
    完。
    AI预测器 的 累计误差 为 AI预测器 的 累计误差 加 误差。
    AI预测器 的 调整次数 为 AI预测器 的 调整次数 加 1。
    阈值 为 实际值 除以 10。
    若 阈值 小于 1：
        阈值 为 1。
    完。
    若 误差 小于或等于 阈值：
        全局统计 的 AI预测命中数 为 全局统计 的 AI预测命中数 加 1。
    完。
    返还 误差。
完。

AI_建议迁移 接受 源CPU、目标CPU：
    若 源CPU 小于 0 或者 源CPU 大于或等于 CPU数量：
        返还 错误_无效参数。
    完。
    若 目标CPU 小于 0 或者 目标CPU 大于或等于 CPU数量：
        返还 错误_无效参数。
    完。
    源负载 为 做 per_cpu_read 用 源CPU、PERCPU_负载均值。
    目标负载 为 做 per_cpu_read 用 目标CPU、PERCPU_负载均值。
    若 源负载 等于 空：
        源负载 为 0。
    完。
    若 目标负载 等于 空：
        目标负载 为 0。
    完。
    若 源负载 大于 目标负载 乘 130 除以 100：
        返还 真。
    完。
    返还 假。
完。

AI_智能预取 接受 cpu：
    若 cpu 小于 0 或者 cpu 大于或等于 CPU数量：
        返还 错误_无效参数。
    完。
    可变 预取数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键注册表：
        若 键注册表 的 第 i 项 的 已注册 等于 真 并且 键注册表 的 第 i 项 的 访问计数 大于 AI预取阈值：
            做 per_cpu_read 用 cpu、键注册表 的 第 i 项 的 键编号。
            预取数 为 预取数 加 1。
        完。
        i 为 i 加 1。
    完。
    全局统计 的 AI预取触发数 为 全局统计 的 AI预取触发数 加 预取数。
    返还 预取数。
完。

获取CPU的NUMA节点 接受 cpu：
    若 cpu 小于 0 或者 cpu 大于或等于 CPU数量：
        返还 错误_无效参数。
    完。
    返还 每CPU区域列表 的 第 cpu 项 的 NUMA节点。
完。

获取NUMA节点的CPU列表 接受 节点：
    若 节点 小于 0 或者 节点 大于或等于 NUMA最大节点数：
        返还 一组：（空）。
    完。
    返还 NUMA拓扑 的 第 节点 项。
完。

是否同NUMA节点 接受 cpu1、cpu2：
    若 cpu1 小于 0 或者 cpu1 大于或等于 CPU数量：
        返还 假。
    完。
    若 cpu2 小于 0 或者 cpu2 大于或等于 CPU数量：
        返还 假。
    完。
    返还 每CPU区域列表 的 第 cpu1 项 的 NUMA节点 等于 每CPU区域列表 的 第 cpu2 项 的 NUMA节点。
完。

NUMA本地求和 接受 键：
    当前节点 为 每CPU区域列表 的 第 当前CPU编号 项 的 NUMA节点。
    本地CPU列表 为 NUMA拓扑 的 第 当前节点 项。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 本地CPU列表：
        cpu 为 本地CPU列表 的 第 i 项。
        值 为 做 per_cpu_read 用 cpu、键。
        若 值 不等于 空：
            总和 为 总和 加 值。
        完。
        i 为 i 加 1。
    完。
    返还 总和。
完。

per_cpu_快照 接受 cpu：
    若 cpu 小于 0 或者 cpu 大于或等于 CPU数量：
        返还 一组：（空）。
    完。
    数据 为 每CPU区域列表 的 第 cpu 项 的 键值对。
    可变 快照 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 快照、一组：数据 的 第 i 项 的 第 0 项、数据 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    全局统计 的 快照次数 为 全局统计 的 快照次数 加 1。
    返还 快照。
完。

per_cpu_批量写 接受 cpu、键值列表：
    若 cpu 小于 0 或者 cpu 大于或等于 CPU数量：
        返还 错误_无效参数。
    完。
    若 键值列表 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键值列表：
        做 per_cpu_write 用 cpu、键值列表 的 第 i 项 的 第 0 项、键值列表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 0。
完。

per_cpu_全部清零 接受 键：
    可变 cpu 为 0。
    只要 cpu 小于 CPU数量：
        若 每CPU区域列表 的 第 cpu 项 的 在线 等于 真：
            做 per_cpu_write 用 cpu、键、0。
        完。
        cpu 为 cpu 加 1。
    完。
    返还 0。
完。

per_cpu_迁移 接受 源cpu、目标cpu、键：
    若 源cpu 小于 0 或者 源cpu 大于或等于 CPU数量：
        返还 错误_无效参数。
    完。
    若 目标cpu 小于 0 或者 目标cpu 大于或等于 CPU数量：
        返还 错误_无效参数。
    完。
    值 为 做 per_cpu_read 用 源cpu、键。
    若 值 等于 空：
        返还 错误_未找到。
    完。
    做 per_cpu_write 用 目标cpu、键、值。
    做 per_cpu_write 用 源cpu、键、0。
    全局统计 的 迁移次数 为 全局统计 的 迁移次数 加 1。
    返还 0。
完。

每处理器_转储CPU 接受 cpu：
    若 cpu 小于 0 或者 cpu 大于或等于 CPU数量：
        返还 错误_无效参数。
    完。
    区域 为 每CPU区域列表 的 第 cpu 项。
    做 打印 用 做 字符串拼接 用 「  [CPU 」、做 转字符串 用 cpu。
    做 打印 用 做 字符串拼接 用 「    APIC: 」、做 转字符串 用 区域 的 APIC编号。
    做 打印 用 做 字符串拼接 用 「    NUMA节点: 」、做 转字符串 用 区域 的 NUMA节点。
    做 打印 用 做 字符串拼接 用 「    在线: 」、做 转字符串 用 区域 的 在线。
    做 打印 用 做 字符串拼接 用 「    键数量: 」、做 转字符串 用 区域 的 键数量。
    做 打印 用 做 字符串拼接 用 「    读计数: 」、做 转字符串 用 区域 的 读计数。
    做 打印 用 做 字符串拼接 用 「    写计数: 」、做 转字符串 用 区域 的 写计数。
    返还 0。
完。

每处理器_转储全部 接受 零参：
    做 打印 用 「[每处理器] ═══ 全部CPU状态 ═══」。
    做 打印 用 做 字符串拼接 用 「  总CPU数: 」、做 转字符串 用 CPU数量。
    做 打印 用 做 字符串拼接 用 「  在线数: 」、做 转字符串 用 做 获取在线CPU数。
    做 打印 用 做 字符串拼接 用 「  当前CPU: 」、做 转字符串 用 当前CPU编号。
    可变 cpu 为 0。
    只要 cpu 小于 CPU数量：
        做 每处理器_转储CPU 用 cpu。
        cpu 为 cpu 加 1。
    完。
    返还 0。
完。

每处理器_打印统计 接受 零参：
    做 打印 用 「[每处理器] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  总读次数: 」、做 转字符串 用 全局统计 的 总读次数。
    做 打印 用 做 字符串拼接 用 「  总写次数: 」、做 转字符串 用 全局统计 的 总写次数。
    做 打印 用 做 字符串拼接 用 「  跨CPU读: 」、做 转字符串 用 全局统计 的 跨CPU读次数。
    做 打印 用 做 字符串拼接 用 「  跨CPU写: 」、做 转字符串 用 全局统计 的 跨CPU写次数。
    做 打印 用 做 字符串拼接 用 「  键注册次数: 」、做 转字符串 用 全局统计 的 键注册次数。
    做 打印 用 做 字符串拼接 用 「  键注销次数: 」、做 转字符串 用 全局统计 的 键注销次数。
    做 打印 用 做 字符串拼接 用 「  快照次数: 」、做 转字符串 用 全局统计 的 快照次数。
    做 打印 用 做 字符串拼接 用 「  汇总次数: 」、做 转字符串 用 全局统计 的 汇总次数。
    做 打印 用 做 字符串拼接 用 「  原子操作: 」、做 转字符串 用 全局统计 的 原子操作次数。
    做 打印 用 做 字符串拼接 用 「  CAS成功: 」、做 转字符串 用 全局统计 的 比较交换成功数。
    做 打印 用 做 字符串拼接 用 「  CAS失败: 」、做 转字符串 用 全局统计 的 比较交换失败数。
    做 打印 用 做 字符串拼接 用 「  AI预测次数: 」、做 转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI预测命中: 」、做 转字符串 用 全局统计 的 AI预测命中数。
    做 打印 用 做 字符串拼接 用 「  AI预取触发: 」、做 转字符串 用 全局统计 的 AI预取触发数。
    做 打印 用 做 字符串拼接 用 「  NUMA跨节点: 」、做 转字符串 用 全局统计 的 NUMA跨节点访问数。
    做 打印 用 做 字符串拼接 用 「  迁移次数: 」、做 转字符串 用 全局统计 的 迁移次数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    若 全局统计 的 AI预测次数 大于 0：
        命中率 为 全局统计 的 AI预测命中数 乘 100 除以 全局统计 的 AI预测次数。
        做 打印 用 做 字符串拼接 用 「  AI预测命中率%: 」、做 转字符串 用 命中率。
    完。
    返还 0。
完。

每处理器_注册到内核 接受 内核上下文：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    做 打印 用 「[每处理器] 已注册到内核上下文」。
    返还 0。
完。

每处理器_查询状态 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

每处理器_健康检查 接受 零参：
    若 已初始化 等于 假：
        返还 假。
    完。
    在线 为 做 获取在线CPU数。
    若 在线 等于 0：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 总读次数 加 全局统计 的 总写次数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
