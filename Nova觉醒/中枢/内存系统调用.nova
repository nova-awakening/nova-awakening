《内存系统调用》

引用《.·系统调用表》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·进程任务》。

引用《.·内存虚拟》。

引用《.·链表后继》。

引用《..·记忆·文件描述符》。

引用《.·自动扩容》。

引用《.·字符串替换》。

引用《.·每处理器》。

PROT_NONE 为 0x0。

PROT_READ 为 0x1。

PROT_WRITE 为 0x2。

PROT_EXEC 为 0x4。

MAP_SHARED 为 0x01。

MAP_PRIVATE 为 0x02。

MAP_FIXED 为 0x10。

MAP_ANONYMOUS 为 0x20。

MAP_GROWSDOWN 为 0x0100。

MAP_POPULATE 为 0x8000。

VMA_FLAG_锁定 为 0x10000000。

VMA_FLAG_可释放 为 0x20000000。

VMA_FLAG_已预分配 为 0x40000000。

PAGE_SIZE 为 4096。

PAGE_SHIFT 为 12。

PAGE_MASK 为 按位取反 (PAGE_SIZE 减 1)。

可变 Nova内存映射_总次数 为 0。

可变 munmap_总次数 为 0。

可变 brk_总次数 为 0。

sys_brk 接受 新断点、_b、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 0。
    完。
    mm 为 做 进程_mm 用 当前。
    若 mm 等于 空：
        返还 0。
    完。
    当前brk 为 做 mm_brk 用 mm。
    若 新断点 等于 0：
        返还 当前brk。
    完。
    新brk 为 做 页对齐上 用 新断点。
    旧brk 为 做 页对齐上 用 当前brk。
    若 新brk 小于 做 mm_brk_start 用 mm：
        返还 当前brk。
    完。
    数据限制 为 做 进程_资源限制 用 当前、RLIMIT_DATA。
    若 数据限制 大于 0 并且 新brk 减 做 mm_brk_start 用 mm 大于 数据限制：
        返还 当前brk。
    完。
    若 新brk 大于 旧brk：
        大小 为 新brk 减 旧brk。
        结果 为 做 do_brk_expand 用 mm、旧brk、大小。
        若 结果 小于 0：
            返还 当前brk。
        完。
    否则若 新brk 小于 旧brk：
        做 do_brk_shrink 用 mm、新brk、旧brk 减 新brk。
    完。
    做 mm_设置brk 用 mm、新断点。
    brk_总次数 为 brk_总次数 加 1。
    返还 新断点。
完。

do_brk_expand 接受 mm、地址、大小：
    若 做 VMA_查找重叠 用 mm、地址、地址 加 大小 不等于 空：
        返还 错误_内存不足。
    完。
    vma 为 做 VMA_创建 用 地址、地址 加 大小、PROT_READ 位或 PROT_WRITE、MAP_PRIVATE 位或 MAP_ANONYMOUS。
    做 VMA_插入 用 mm、vma。
    返还 成功。
完。

do_brk_shrink 接受 mm、地址、大小：
    做 VMA_解除映射 用 mm、地址、大小。
    返还 成功。
完。

sys_Nova内存映射 接受 地址、长度、保护、标志、fd、偏移：
    若 长度 等于 0：
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_坏文件描述符。
    完。
    mm 为 做 进程_mm 用 当前。
    若 mm 等于 空：
        返还 错误_内存不足。
    完。
    对齐长度 为 做 页对齐上 用 长度。
    若 标志 位与 MAP_SHARED 不等于 0 并且 标志 位与 MAP_PRIVATE 不等于 0：
        返还 错误_无效参数。
    完。
    若 标志 位与 MAP_SHARED 等于 0 并且 标志 位与 MAP_PRIVATE 等于 0：
        返还 错误_无效参数。
    完。
    可变 文件 为 空。
    若 标志 位与 MAP_ANONYMOUS 等于 0：
        文件 为 做 进程_获取文件 用 当前、fd。
        若 文件 等于 空：
            返还 错误_坏文件描述符。
        完。
        若 保护 位与 PROT_READ 不等于 0 并且 非 做 文件_可读 用 文件：
            返还 错误_权限。
        完。
        若 保护 位与 PROT_WRITE 不等于 0 并且 非 做 文件_可写 用 文件：
            返还 错误_权限。
        完。
    完。
    可变 映射地址 为 地址。
    若 标志 位与 MAP_FIXED 不等于 0：
        若 映射地址 位与 PAGE_SIZE 减 1 不等于 0：
            返还 错误_无效参数。
        完。
        做 VMA_解除映射 用 mm、映射地址、对齐长度。
    否则：
        映射地址 为 做 VMA_查找空闲 用 mm、地址、对齐长度。
        若 映射地址 等于 0：
            返还 错误_内存不足。
        完。
    完。
    vma 为 做 VMA_创建 用 映射地址、映射地址 加 对齐长度、保护、标志。
    若 文件 不等于 空：
        做 VMA_设置文件 用 vma、文件、偏移。
    完。
    做 VMA_插入 用 mm、vma。
    若 标志 位与 MAP_POPULATE 不等于 0：
        做 VMA_预分配 用 mm、映射地址、对齐长度。
    完。
    Nova内存映射_总次数 为 Nova内存映射_总次数 加 1。
    返还 映射地址。
完。

sys_munmap 接受 地址、长度、_c、_d、_e、_f：
    若 长度 等于 0：
        返还 错误_无效参数。
    完。
    若 地址 位与 PAGE_SIZE 减 1 不等于 0：
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    mm 为 做 进程_mm 用 当前。
    若 mm 等于 空：
        返还 错误_内存不足。
    完。
    对齐长度 为 做 页对齐上 用 长度。
    结果 为 做 VMA_解除映射 用 mm、地址、对齐长度。
    若 结果 小于 0：
        返还 结果。
    完。
    munmap_总次数 为 munmap_总次数 加 1。
    返还 成功。
完。

sys_mprotect 接受 地址、长度、保护、_d、_e、_f：
    若 地址 位与 PAGE_SIZE 减 1 不等于 0：
        返还 错误_无效参数。
    完。
    若 长度 等于 0：
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    mm 为 做 进程_mm 用 当前。
    若 mm 等于 空：
        返还 错误_内存不足。
    完。
    对齐长度 为 做 页对齐上 用 长度。
    vma 为 做 VMA_查找 用 mm、地址。
    若 vma 等于 空：
        返还 错误_内存不足。
    完。
    若 做 VMA_起始 用 vma 小于 地址：
        做 VMA_拆分 用 mm、vma、地址。
        vma 为 做 VMA_查找 用 mm、地址。
    完。
    若 做 VMA_结束 用 vma 大于 地址 加 对齐长度：
        做 VMA_拆分 用 mm、vma、地址 加 对齐长度。
    完。
    做 VMA_设置保护 用 vma、保护。
    做 VMA_更新页表保护 用 mm、地址、对齐长度、保护。
    返还 成功。
完。

RLIMIT_DATA 为 2。

页对齐上 接受 地址：
    返还 地址 加 PAGE_SIZE 减 1 位与 PAGE_MASK。
完。

页对齐下 接受 地址：
    返还 地址 位与 PAGE_MASK。
完。

注册内存系统调用 接受 零参：
    做 系统调用表_注册 用 9、系统调用_mmap。
    做 系统调用表_注册 用 11、系统调用_munmap。
    做 系统调用表_注册 用 12、系统调用_brk。
完。

内存系统调用_统计 接受 零参：
    返还 一组：brk_总次数、Nova内存映射_总次数、munmap_总次数。
完。

MADV_NORMAL 为 0。

MADV_RANDOM 为 1。

MADV_SEQUENTIAL 为 2。

MADV_WILLNEED 为 3。

MADV_DONTNEED 为 4。

MADV_FREE 为 8。

MADV_HUGEPAGE 为 14。

可变 madvise_总次数 为 0。

可变 mprotect_总次数 为 0。

可变 mlock_总次数 为 0。

可变 内存系统调用_错误数 为 0。

可变 内存系统调用_已初始化 为 假。

sys_madvise 接受 地址、长度、建议、_d、_e、_f：
    若 地址 位与 PAGE_SIZE 减 1 不等于 0：
        返还 错误_无效参数。
    完。
    若 长度 等于 0：
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    mm 为 做 进程_mm 用 当前。
    若 mm 等于 空：
        返还 错误_内存不足。
    完。
    对齐长度 为 做 页对齐上 用 长度。
    若 建议 等于 MADV_DONTNEED：
        做 VMA_释放物理页 用 mm、地址、对齐长度。
    否则若 建议 等于 MADV_WILLNEED：
        做 VMA_预分配 用 mm、地址、对齐长度。
    否则若 建议 等于 MADV_FREE：
        做 VMA_标记可释放 用 mm、地址、对齐长度。
    完。
    madvise_总次数 为 madvise_总次数 加 1。
    返还 0。
完。

MS_ASYNC 为 1。

MS_SYNC 为 4。

MS_INVALIDATE 为 2。

可变 msync_总次数 为 0。

sys_msync 接受 地址、长度、标志、_d、_e、_f：
    若 地址 位与 PAGE_SIZE 减 1 不等于 0：
        返还 错误_无效参数。
    完。
    若 长度 等于 0：
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    mm 为 做 进程_mm 用 当前。
    若 mm 等于 空：
        返还 错误_内存不足。
    完。
    vma 为 做 VMA_查找 用 mm、地址。
    若 vma 等于 空：
        返还 错误_内存不足。
    完。
    若 做 VMA_是匿名 用 vma：
        返还 0。
    完。
    msync_总次数 为 msync_总次数 加 1。
    返还 0。
完。

sys_mlock 接受 地址、长度、_c、_d、_e、_f：
    若 地址 位与 PAGE_SIZE 减 1 不等于 0：
        返还 错误_无效参数。
    完。
    若 长度 等于 0：
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    mm 为 做 进程_mm 用 当前。
    若 mm 等于 空：
        返还 错误_内存不足。
    完。
    vma 为 做 VMA_查找 用 mm、地址。
    若 vma 等于 空：
        返还 错误_内存不足。
    完。
    做 VMA_设置锁定 用 vma、真。
    做 VMA_预分配 用 mm、地址、做 页对齐上 用 长度。
    mlock_总次数 为 mlock_总次数 加 1。
    返还 0。
完。

sys_munlock 接受 地址、长度、_c、_d、_e、_f：
    若 地址 位与 PAGE_SIZE 减 1 不等于 0：
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    mm 为 做 进程_mm 用 当前。
    若 mm 等于 空：
        返还 错误_内存不足。
    完。
    vma 为 做 VMA_查找 用 mm、地址。
    若 vma 等于 空：
        返还 错误_内存不足。
    完。
    做 VMA_设置锁定 用 vma、假。
    返还 0。
完。

mm_brk 接受 mm：
    若 mm 等于 空：
        返还 0。
    完。
    返还 mm 的 堆止。
完。

mm_brk_start 接受 mm：
    若 mm 等于 空：
        返还 0。
    完。
    返还 mm 的 堆起。
完。

mm_设置brk 接受 mm、新值：
    若 mm 等于 空：
        返还。
    完。
    mm 的 堆止 为 新值。
完。

进程_mm 接受 进程：
    若 进程 等于 空：
        返还 空。
    完。
    返还 进程 的 第 13 项。
完。

进程_资源限制 接受 进程、类型：
    若 进程 等于 空：
        返还 0。
    完。
    档 为 做 进程任务_查按PID 用 做 进程_PID 用 进程。
    若 档 等于 空 或者 档 的 限制 等于 空：
        返还 0。
    完。
    若 类型 小于 0 或者 类型 大于或等于 做 计数 用 档 的 限制：
        返还 0。
    完。
    返还 档 的 限制 的 第 类型 项 的 soft。
完。

进程_获取文件 接受 进程、fd：
    若 进程 等于 空 或者 fd 小于 0：
        返还 空。
    完。
    文件表 为 做 进程_文件表 用 进程。
    若 文件表 等于 空：
        返还 空。
    完。
    返还 做 文件表_获取 用 文件表、fd。
完。

文件_可读 接受 文件：
    若 文件 等于 空：
        返还 假。
    完。
    返还 做 文件描述符_可读 用 文件。
完。

文件_可写 接受 文件：
    若 文件 等于 空：
        返还 假。
    完。
    返还 做 文件描述符_可写 用 文件。
完。

VMA_创建 接受 起始、结束、保护、标志：
    返还 一组：起始、结束、保护、标志、空、0。
完。

VMA_插入 接受 mm、vma：
    若 mm 等于 空 或者 vma 等于 空：
        返还。
    完。
    若 mm 的 VMA链 等于 空：
        mm 的 VMA链 为 做 链表_创建。
    完。
    可变 当前 为 做 链表_首节点_或空 用 mm 的 VMA链。
    只要 当前 不等于 空：
        现有 为 做 链表_数据 用 当前。
        若 做 VMA_起始 用 现有 大于 做 VMA_起始 用 vma：
            做 链表_插入前 用 当前、vma。
            mm 的 VMA数 为 mm 的 VMA数 加 1。
            返还。
        完。
        当前 为 做 链表_后继 用 当前。
    完。
    做 链表_尾插 用 mm 的 VMA链、vma。
    mm 的 VMA数 为 mm 的 VMA数 加 1。
完。

VMA_解除映射 接受 mm、地址、大小：
    若 mm 等于 空 或者 mm 的 VMA链 等于 空：
        返还 错误_无效参数。
    完。
    若 大小 小于或等于 0：
        返还 错误_无效参数。
    完。
    结束 为 地址 加 大小。
    可变 当前 为 做 链表_首节点_或空 用 mm 的 VMA链。
    可变 命中 为 假。
    只要 当前 不等于 空：
        下一个 为 做 链表_后继 用 当前。
        vma 为 做 链表_数据 用 当前。
        若 做 VMA_结束 用 vma 小于或等于 地址 或者 做 VMA_起始 用 vma 大于或等于 结束：
            当前 为 下一个。
            继续。
        完。
        命中 为 真。
        若 地址 小于或等于 做 VMA_起始 用 vma 并且 结束 大于或等于 做 VMA_结束 用 vma：
            mm 的 VMA数 为 mm 的 VMA数 减 1。
            做 链表_删除 用 当前。
        否则若 地址 大于 做 VMA_起始 用 vma 并且 结束 小于 做 VMA_结束 用 vma：
            做 VMA_拆分 用 mm、vma、结束。
            vma 的 第 1 项 为 地址。
        否则若 地址 小于或等于 做 VMA_起始 用 vma：
            旧起始 为 做 VMA_起始 用 vma。
            vma 的 第 0 项 为 结束。
            若 vma 的 第 4 项 不等于 空：
                vma 的 第 5 项 为 vma 的 第 5 项 加 (结束 减 旧起始) 除以 PAGE_SIZE。
            完。
        否则：
            vma 的 第 1 项 为 地址。
        完。
        当前 为 下一个。
    完。
    若 非 命中：
        返还 错误_无效参数。
    完。
    返还 成功。
完。

VMA_查找 接受 mm、地址：
    若 mm 等于 空 或者 mm 的 VMA链 等于 空：
        返还 空。
    完。
    可变 当前 为 做 链表_首节点_或空 用 mm 的 VMA链。
    只要 当前 不等于 空：
        vma 为 做 链表_数据 用 当前。
        若 做 VMA_起始 用 vma 小于或等于 地址 并且 地址 小于 做 VMA_结束 用 vma：
            返还 vma。
        完。
        当前 为 做 链表_后继 用 当前。
    完。
    返还 空。
完。

VMA_查找重叠 接受 mm、起始、结束：
    若 mm 等于 空 或者 mm 的 VMA链 等于 空：
        返还 空。
    完。
    若 起始 大于或等于 结束：
        返还 空。
    完。
    可变 当前 为 做 链表_首节点_或空 用 mm 的 VMA链。
    只要 当前 不等于 空：
        vma 为 做 链表_数据 用 当前。
        若 起始 小于 做 VMA_结束 用 vma 并且 结束 大于 做 VMA_起始 用 vma：
            返还 vma。
        完。
        当前 为 做 链表_后继 用 当前。
    完。
    返还 空。
完。

VMA_查找空闲 接受 mm、建议地址、大小：
    若 大小 小于或等于 0：
        返还 0。
    完。
    可变 候选 为 建议地址。
    若 候选 小于 0x400000：
        候选 为 0x400000。
    完。
    若 mm 等于 空 或者 mm 的 VMA链 等于 空：
        返还 候选。
    完。
    可变 当前 为 做 链表_首节点_或空 用 mm 的 VMA链。
    只要 当前 不等于 空：
        vma 为 做 链表_数据 用 当前。
        若 候选 加 大小 小于或等于 做 VMA_起始 用 vma：
            返还 候选。
        完。
        若 做 VMA_结束 用 vma 大于 候选：
            候选 为 做 VMA_结束 用 vma。
        完。
        当前 为 做 链表_后继 用 当前。
    完。
    返还 候选。
完。

VMA_设置文件 接受 vma、文件、偏移：
    若 vma 等于 空：
        返还。
    完。
    vma 的 第 4 项 为 文件。
    vma 的 第 5 项 为 偏移。
完。

VMA_预分配 接受 mm、地址、大小：
    若 mm 等于 空 或者 大小 小于或等于 0：
        返还 错误_无效参数。
    完。
    若 mm 的 VMA链 等于 空：
        返还 错误_无效参数。
    完。
    结束 为 地址 加 大小。
    可变 当前 为 做 链表_首节点_或空 用 mm 的 VMA链。
    可变 覆盖页数 为 0。
    只要 当前 不等于 空：
        vma 为 做 链表_数据 用 当前。
        若 做 VMA_结束 用 vma 小于或等于 地址 或者 做 VMA_起始 用 vma 大于或等于 结束：
            当前 为 做 链表_后继 用 当前。
            继续。
        完。
        若 vma 的 第 3 项 位与 VMA_FLAG_已预分配 等于 0：
            vma 的 第 3 项 为 vma 的 第 3 项 位或 VMA_FLAG_已预分配。
            覆盖页数 为 覆盖页数 加 做 VMA_页数 用 vma。
        完。
        当前 为 做 链表_后继 用 当前。
    完。
    mm 的 常驻页数 为 mm 的 常驻页数 加 覆盖页数。
    返还 成功。
完。

VMA_拆分 接受 mm、vma、地址：
    若 mm 等于 空 或者 vma 等于 空：
        返还 空。
    完。
    若 地址 小于或等于 做 VMA_起始 用 vma 或者 地址 大于或等于 做 VMA_结束 用 vma：
        返还 空。
    完。
    右偏移 为 vma 的 第 5 项。
    若 vma 的 第 4 项 等于 空：
        右偏移 为 右偏移 加 (地址 减 做 VMA_起始 用 vma) 除以 PAGE_SIZE。
    完。
    右 为 一组：地址、vma 的 第 1 项、vma 的 第 2 项、vma 的 第 3 项、vma 的 第 4 项、右偏移。
    vma 的 第 1 项 为 地址。
    若 mm 的 VMA链 不等于 空：
        节点 为 做 链表_查找 用 mm 的 VMA链、vma。
        若 节点 不等于 空：
            做 链表_插入后 用 节点、右。
        否则：
            做 链表_尾插 用 mm 的 VMA链、右。
        完。
    否则：
        mm 的 VMA链 为 做 链表_创建。
        做 链表_尾插 用 mm 的 VMA链、右。
    完。
    mm 的 VMA数 为 mm 的 VMA数 加 1。
    返还 右。
完。

VMA_设置保护 接受 vma、保护：
    若 vma 不等于 空：
        vma 的 第 2 项 为 保护。
    完。
完。

VMA_更新页表保护 接受 mm、地址、大小、保护：
    若 mm 等于 空 或者 mm 的 VMA链 等于 空：
        返还 错误_无效参数。
    完。
    结束 为 地址 加 大小。
    可变 当前 为 做 链表_首节点_或空 用 mm 的 VMA链。
    只要 当前 不等于 空：
        vma 为 做 链表_数据 用 当前。
        若 做 VMA_结束 用 vma 小于或等于 地址 或者 做 VMA_起始 用 vma 大于或等于 结束：
            当前 为 做 链表_后继 用 当前。
            继续。
        完。
        vma 的 第 2 项 为 保护。
        当前 为 做 链表_后继 用 当前。
    完。
    返还 成功。
完。

VMA_起始 接受 vma：
    返还 vma 的 第 0 项。
完。

VMA_结束 接受 vma：
    返还 vma 的 第 1 项。
完。

VMA_是匿名 接受 vma：
    若 vma 等于 空：
        返还 真。
    完。
    返还 vma 的 第 4 项 等于 空。
完。

VMA_释放物理页 接受 mm、地址、大小：
    若 mm 等于 空 或者 mm 的 VMA链 等于 空：
        返还 错误_无效参数。
    完。
    若 大小 小于或等于 0：
        返还 错误_无效参数。
    完。
    结束 为 地址 加 大小。
    可变 当前 为 做 链表_首节点_或空 用 mm 的 VMA链。
    可变 释放页数 为 0。
    只要 当前 不等于 空：
        vma 为 做 链表_数据 用 当前。
        若 做 VMA_结束 用 vma 小于或等于 地址 或者 做 VMA_起始 用 vma 大于或等于 结束：
            当前 为 做 链表_后继 用 当前。
            继续。
        完。
        vma 的 第 3 项 为 vma 的 第 3 项 位或 VMA_FLAG_可释放。
        若 vma 的 第 3 项 位与 VMA_FLAG_已预分配 不等于 0：
            vma 的 第 3 项 为 vma 的 第 3 项 位与 按位取反 VMA_FLAG_已预分配。
            释放页数 为 释放页数 加 做 VMA_页数 用 vma。
        完。
        当前 为 做 链表_后继 用 当前。
    完。
    若 释放页数 大于 0：
        若 mm 的 常驻页数 大于 释放页数：
            mm 的 常驻页数 为 mm 的 常驻页数 减 释放页数。
        否则：
            mm 的 常驻页数 为 0。
        完。
    完。
    返还 成功。
完。

VMA_标记可释放 接受 mm、地址、大小：
    若 mm 等于 空 或者 mm 的 VMA链 等于 空：
        返还 错误_无效参数。
    完。
    若 大小 小于或等于 0：
        返还 错误_无效参数。
    完。
    结束 为 地址 加 大小。
    可变 当前 为 做 链表_首节点_或空 用 mm 的 VMA链。
    只要 当前 不等于 空：
        vma 为 做 链表_数据 用 当前。
        若 做 VMA_结束 用 vma 小于或等于 地址 或者 做 VMA_起始 用 vma 大于或等于 结束：
            当前 为 做 链表_后继 用 当前。
            继续。
        完。
        vma 的 第 3 项 为 vma 的 第 3 项 位或 VMA_FLAG_可释放。
        当前 为 做 链表_后继 用 当前。
    完。
    返还 成功。
完。

VMA_设置锁定 接受 vma、锁定：
    若 vma 等于 空：
        返还。
    完。
    若 锁定：
        vma 的 第 3 项 为 vma 的 第 3 项 位或 VMA_FLAG_锁定。
    否则：
        vma 的 第 3 项 为 vma 的 第 3 项 位与 按位取反 VMA_FLAG_锁定。
    完。
完。

内存系统调用_完整统计 接受 零参：
    统计 为 做 字典。
    做 字典设 用 统计、「已初始化」、内存系统调用_已初始化。
    做 字典设 用 统计、「brk次数」、brk_总次数。
    做 字典设 用 统计、「mmap次数」、Nova内存映射_总次数。
    做 字典设 用 统计、「munmap次数」、munmap_总次数。
    做 字典设 用 统计、「mprotect次数」、mprotect_总次数。
    做 字典设 用 统计、「madvise次数」、madvise_总次数。
    做 字典设 用 统计、「msync次数」、msync_总次数。
    做 字典设 用 统计、「mlock次数」、mlock_总次数。
    做 字典设 用 统计、「总操作数」、brk_总次数 加 Nova内存映射_总次数 加 munmap_总次数 加 mprotect_总次数 加 madvise_总次数 加 msync_总次数 加 mlock_总次数。
    做 字典设 用 统计、「错误数」、内存系统调用_错误数。
    返还 统计。
完。

内存系统调用_打印统计 接受 零参：
    统计 为 做 内存系统调用_完整统计。
    做 打印 用 「[内存系统调用] ═══════ 完整统计 ═══════」。
    做 打印 用 「  brk:          」 加 做 转字符串 用 做 字典取 用 统计、「brk次数」。
    做 打印 用 「  mmap:         」 加 做 转字符串 用 做 字典取 用 统计、「mmap次数」。
    做 打印 用 「  munmap:       」 加 做 转字符串 用 做 字典取 用 统计、「munmap次数」。
    做 打印 用 「  mprotect:     」 加 做 转字符串 用 做 字典取 用 统计、「mprotect次数」。
    做 打印 用 「  madvise:      」 加 做 转字符串 用 做 字典取 用 统计、「madvise次数」。
    做 打印 用 「  msync:        」 加 做 转字符串 用 做 字典取 用 统计、「msync次数」。
    做 打印 用 「  mlock:        」 加 做 转字符串 用 做 字典取 用 统计、「mlock次数」。
    做 打印 用 「  总操作:       」 加 做 转字符串 用 做 字典取 用 统计、「总操作数」。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[内存系统调用] ═════════════════════════」。
    返还 0。
完。

内存系统调用_dump 接受 零参：
    做 打印 用 「[内存系统调用 dump] ──────────────────」。
    做 打印 用 「  brk: 」 加 做 转字符串 用 brk_总次数。
    做 打印 用 「  mmap: 」 加 做 转字符串 用 Nova内存映射_总次数。
    做 打印 用 「  munmap: 」 加 做 转字符串 用 munmap_总次数。
    做 打印 用 「  mprotect: 」 加 做 转字符串 用 mprotect_总次数。
    做 打印 用 「  madvise: 」 加 做 转字符串 用 madvise_总次数。
    做 打印 用 「[内存系统调用 dump] ──────────────────」。
完。

内存系统调用_销毁 接受 零参：
    brk_总次数 为 0。
    Nova内存映射_总次数 为 0。
    munmap_总次数 为 0。
    mprotect_总次数 为 0。
    madvise_总次数 为 0。
    msync_总次数 为 0。
    mlock_总次数 为 0。
    内存系统调用_已初始化 为 假。
    返还 0。
完。

内存系统调用_为procfs提供统计 接受 零参：
    返还 一组：brk_总次数、Nova内存映射_总次数、munmap_总次数、mprotect_总次数、madvise_总次数。
完。

内存系统调用_重置统计 接受 零参：
    brk_总次数 为 0。
    Nova内存映射_总次数 为 0。
    munmap_总次数 为 0。
    mprotect_总次数 为 0。
    madvise_总次数 为 0。
    msync_总次数 为 0。
    mlock_总次数 为 0。
    内存系统调用_错误数 为 0。
    做 打印 用 「[内存系统调用] 统计已重置」。
    返还 0。
完。

系统调用表_注册 接受 号、函数：
    若 号 小于 0 或者 号 大于或等于 512：
        返还 负 1。
    完。
    做 内存_写64 用 系统调用表 加 号 乘 8、以 零参 为 引，其做法是：
    返还 0。
完。

内存系统调用_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 内存系统调用_错误数 大于 100：
        做 追加 用 问题、「内存系统调用_错误数=」 加 内存系统调用_错误数。
    完。
    返还 做 计数 用 问题 等于 0。
完。

内存系统调用_综合报告 接受 零参：
    做 打印 用 「[内存系统调用] ═══ 综合报告 ═══」。
    做 打印 用 「  Nova内存映射_总次数: 」 加 Nova内存映射_总次数。
    做 打印 用 「  munmap_总次数: 」 加 munmap_总次数。
    做 打印 用 「  brk_总次数: 」 加 brk_总次数。
    做 打印 用 「  madvise_总次数: 」 加 madvise_总次数。
    做 打印 用 「  mprotect_总次数: 」 加 mprotect_总次数。
    做 打印 用 「  mlock_总次数: 」 加 mlock_总次数。
    做 打印 用 「  内存系统调用_错误数: 」 加 内存系统调用_错误数。
    做 打印 用 「  内存系统调用_已初始化: 」 加 内存系统调用_已初始化。
    做 打印 用 「  msync_总次数: 」 加 msync_总次数。
    做 打印 用 「  健康: 」 加 若 做 内存系统调用_健康评估 则 「正常」 否则 「异常」。
完。

内存系统调用_全局重置 接受 零参：
    Nova内存映射_总次数 为 0。
    munmap_总次数 为 0。
    brk_总次数 为 0。
    madvise_总次数 为 0。
    mprotect_总次数 为 0。
    mlock_总次数 为 0。
    内存系统调用_错误数 为 0。
    内存系统调用_已初始化 为 0。
    msync_总次数 为 0。
    做 打印 用 「[内存系统调用] 统计已重置」。
    返还 0。
完。

本章 收束。
