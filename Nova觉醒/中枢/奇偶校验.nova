《奇偶校验》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·感知·内核直连层》。

引用《.·整数转字符串》。

引用《.·内核打印》。

可变 操作总数 为 0。

奇偶校验 接受 值：
    操作总数 为 操作总数 加 1。
    可变 v 为 值。
    v 为 v 位异或 v 右移 32。
    v 为 v 位异或 v 右移 16。
    v 为 v 位异或 v 右移 8。
    v 为 v 位异或 v 右移 4。
    v 为 v 位异或 v 右移 2。
    v 为 v 位异或 v 右移 1。
    返还 v 位与 1。
完。

位计数 接受 值：
    操作总数 为 操作总数 加 1。
    可变 v 为 值。
    v 为 v 减 (v 右移 1 位与 0x5555555555555555)。
    v 为 (v 位与 0x3333333333333333) 加 (v 右移 2 位与 0x3333333333333333)。
    v 为 v 加 (v 右移 4) 位与 0x0F0F0F0F0F0F0F0F。
    返还 v 乘 0x0101010101010101 右移 56。
完。

前导零 接受 值：
    操作总数 为 操作总数 加 1。
    若 值 等于 0：
        返还 64。
    完。
    可变 n 为 0。
    可变 v 为 值。
    若 v 位与 0xFFFFFFFF00000000 等于 0：
        n 为 n 加 32。
        v 为 v 左移 32。
    完。
    若 v 位与 0xFFFF000000000000 等于 0：
        n 为 n 加 16。
        v 为 v 左移 16。
    完。
    若 v 位与 0xFF00000000000000 等于 0：
        n 为 n 加 8。
        v 为 v 左移 8。
    完。
    若 v 位与 0xF000000000000000 等于 0：
        n 为 n 加 4。
        v 为 v 左移 4。
    完。
    若 v 位与 0xC000000000000000 等于 0：
        n 为 n 加 2。
        v 为 v 左移 2。
    完。
    若 v 位与 0x8000000000000000 等于 0：
        n 为 n 加 1。
    完。
    返还 n。
完。

尾随零 接受 值：
    操作总数 为 操作总数 加 1。
    若 值 等于 0：
        返还 64。
    完。
    可变 n 为 0。
    可变 v 为 值。
    若 v 位与 0xFFFFFFFF 等于 0：
        n 为 n 加 32。
        v 为 v 右移 32。
    完。
    若 v 位与 0xFFFF 等于 0：
        n 为 n 加 16。
        v 为 v 右移 16。
    完。
    若 v 位与 0xFF 等于 0：
        n 为 n 加 8。
        v 为 v 右移 8。
    完。
    若 v 位与 0xF 等于 0：
        n 为 n 加 4。
        v 为 v 右移 4。
    完。
    若 v 位与 0x3 等于 0：
        n 为 n 加 2。
        v 为 v 右移 2。
    完。
    若 v 位与 0x1 等于 0：
        n 为 n 加 1。
    完。
    返还 n。
完。

最高位 接受 值：
    若 值 等于 0：
        返还 负 1。
    完。
    返还 63 减 做 前导零 用 值。
完。

最低位 接受 值：
    若 值 等于 0：
        返还 负 1。
    完。
    返还 做 尾随零 用 值。
完。

是二幂 接受 值：
    返还 值 大于 0 并且 值 位与 值 减 1 等于 0。
完。

上对齐二幂 接受 值：
    若 值 小于或等于 1：
        返还 1。
    完。
    位数 为 做 最高位 用 值 减 1 加 1。
    返还 1 左移 位数。
完。

字节反转32 接受 值：
    操作总数 为 操作总数 加 1。
    返还 (值 位与 0xFF) 左移 24 位或 (值 位与 0xFF00) 左移 8 位或 值 右移 8 位与 0xFF00 位或 值 右移 24 位与 0xFF。
完。

字节反转64 接受 值：
    操作总数 为 操作总数 加 1。
    可变 v 为 值。
    v 为 (v 位与 0x00FF00FF00FF00FF) 左移 8 位或 v 右移 8 位与 0x00FF00FF00FF00FF。
    v 为 (v 位与 0x0000FFFF0000FFFF) 左移 16 位或 v 右移 16 位与 0x0000FFFF0000FFFF。
    v 为 v 左移 32 位或 v 右移 32。
    返还 v。
完。

左旋转 接受 值、位数：
    操作总数 为 操作总数 加 1。
    移位 为 位数 位与 63。
    返还 值 左移 移位 位或 值 右移 64 减 移位。
完。

右旋转 接受 值、位数：
    操作总数 为 操作总数 加 1。
    移位 为 位数 位与 63。
    返还 值 右移 移位 位或 值 左移 64 减 移位。
完。

位域提取 接受 值、起始、宽度：
    返还 值 右移 起始 位与 (1 左移 宽度) 减 1。
完。

位域插入 接受 目标、起始、宽度、新值：
    掩码 为 (1 左移 宽度) 减 1 左移 起始。
    返还 目标 位与 按位取反 掩码 位或 新值 左移 起始 位与 掩码。
完。

位反转8 接受 值：
    可变 v 为 值 位与 0xFF。
    v 为 (v 位与 0xF0) 右移 4 位或 (v 位与 0x0F) 左移 4。
    v 为 (v 位与 0xCC) 右移 2 位或 (v 位与 0x33) 左移 2。
    v 为 (v 位与 0xAA) 右移 1 位或 (v 位与 0x55) 左移 1。
    返还 v。
完。

校验和16 接受 数据、长度：
    操作总数 为 操作总数 加 1。
    可变 和 为 0。
    可变 位 为 0。
    只要 位 加 1 小于 长度：
        和 为 和 加 (做 内存_读8 用 数据 加 位 左移 8) 加 做 内存_读8 用 数据 加 位 加 1。
        位 为 位 加 2。
    完。
    若 位 小于 长度：
        和 为 和 加 (做 内存_读8 用 数据 加 位 左移 8)。
    完。
    只要 和 大于 0xFFFF：
        和 为 (和 位与 0xFFFF) 加 (和 右移 16)。
    完。
    返还 按位取反 和 位与 0xFFFF。
完。

CRC32_多项式 为 0xEDB88320。

CRC32 接受 数据、长度：
    操作总数 为 操作总数 加 1。
    可变 crc 为 0xFFFFFFFF。
    可变 i 为 0。
    只要 i 小于 长度：
        crc 为 crc 位异或 做 内存_读8 用 数据 加 i。
        可变 位 为 0。
        只要 位 小于 8：
            若 crc 位与 1：
                crc 为 crc 右移 1 位异或 CRC32_多项式。
            否则：
                crc 为 crc 右移 1。
            完。
            位 为 位 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 crc 位异或 0xFFFFFFFF。
完。

纠缠_FNV基 为 14695981039346656037。

纠缠_FNV素 为 1099511628211。

纠缠_旋转种 为 0xA5B3C1D7E9F20814。

量子纠缠校验_计算 接受 数据、长度：
    操作总数 为 操作总数 加 1。
    可变 甲 为 纠缠_FNV基。
    可变 乙 为 纠缠_旋转种。
    可变 i 为 0。
    只要 i 小于 长度：
        字节 为 做 内存_读8 用 数据 加 i。
        甲 为 (甲 位异或 字节) 乘 纠缠_FNV素。
        乙 为 做 左旋转 用 乙、13 位异或 字节 乘 0x0101010101010101。
        i 为 i 加 1。
    完。
    纠缠态 为 甲 位异或 做 左旋转 用 乙、32。
    返还 一组：甲、乙、纠缠态。
完。

量子纠缠校验_验证 接受 数据、长度、期望：
    实际 为 做 量子纠缠校验_计算 用 数据、长度。
    返还 实际 的 第 0 项 等于 期望 的 第 0 项 并且 实际 的 第 1 项 等于 期望 的 第 1 项 并且 实际 的 第 2 项 等于 期望 的 第 2 项。
完。

Morton编码 接受 x、y：
    操作总数 为 操作总数 加 1。
    可变 mx 为 x 位与 0xFFFF。
    可变 my 为 y 位与 0xFFFF。
    mx 为 (mx 位或 mx 左移 8) 位与 0x00FF00FF。
    mx 为 (mx 位或 mx 左移 4) 位与 0x0F0F0F0F。
    mx 为 (mx 位或 mx 左移 2) 位与 0x33333333。
    mx 为 (mx 位或 mx 左移 1) 位与 0x55555555。
    my 为 (my 位或 my 左移 8) 位与 0x00FF00FF。
    my 为 (my 位或 my 左移 4) 位与 0x0F0F0F0F。
    my 为 (my 位或 my 左移 2) 位与 0x33333333。
    my 为 (my 位或 my 左移 1) 位与 0x55555555。
    返还 mx 位或 my 左移 1。
完。

Morton解码 接受 码：
    操作总数 为 操作总数 加 1。
    可变 mx 为 码 位与 0x55555555。
    可变 my 为 码 右移 1 位与 0x55555555。
    mx 为 (mx 位或 mx 右移 1) 位与 0x33333333。
    mx 为 (mx 位或 mx 右移 2) 位与 0x0F0F0F0F。
    mx 为 (mx 位或 mx 右移 4) 位与 0x00FF00FF。
    mx 为 (mx 位或 mx 右移 8) 位与 0x0000FFFF。
    my 为 (my 位或 my 右移 1) 位与 0x33333333。
    my 为 (my 位或 my 右移 2) 位与 0x0F0F0F0F。
    my 为 (my 位或 my 右移 4) 位与 0x00FF00FF。
    my 为 (my 位或 my 右移 8) 位与 0x0000FFFF。
    返还 一组：mx、my。
完。

批量位计数 接受 数据、长度：
    操作总数 为 操作总数 加 1。
    可变 总计 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        总计 为 总计 加 做 位计数 用 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总计。
完。

批量奇偶校验 接受 数据、长度：
    操作总数 为 操作总数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度：
        做 追加 用 结果、做 奇偶校验 用 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

批量异或 接受 数据A、数据B、长度：
    操作总数 为 操作总数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度：
        做 追加 用 结果、数据A 的 第 i 项 位异或 数据B 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

汉明距离 接受 a、b：
    返还 做 位计数 用 a 位异或 b。
完。

批量汉明距离 接受 数据A、数据B、长度：
    操作总数 为 操作总数 加 1。
    可变 总距 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        总距 为 总距 加 做 汉明距离 用 数据A 的 第 i 项、数据B 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总距。
完。

位矩阵转置8x8 接受 行数据：
    操作总数 为 操作总数 加 1。
    可变 结果 为 一组：0、0、0、0、0、0、0、0。
    可变 r 为 0。
    只要 r 小于 8：
        可变 c 为 0。
        只要 c 小于 8：
            若 行数据 的 第 r 项 位与 1 左移 7 减 c：
                结果 的 第 c 项 为 结果 的 第 c 项 位或 1 左移 7 减 r。
            完。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 结果。
完。

GF2_乘法 接受 a、b：
    操作总数 为 操作总数 加 1。
    可变 结果 为 0。
    可变 乘数 为 b。
    可变 位 为 0。
    只要 乘数 不等于 0：
        若 乘数 位与 1：
            结果 为 结果 位异或 a 左移 位。
        完。
        乘数 为 乘数 右移 1。
        位 为 位 加 1。
    完。
    返还 结果。
完。

GF2_模约简 接受 值、多项式：
    操作总数 为 操作总数 加 1。
    多项式度 为 做 最高位 用 多项式。
    若 多项式度 小于 0：
        返还 值。
    完。
    可变 余 为 值。
    可变 余度 为 做 最高位 用 余。
    只要 余度 大于或等于 多项式度：
        余 为 余 位异或 多项式 左移 余度 减 多项式度。
        余度 为 做 最高位 用 余。
    完。
    返还 余。
完。

GF256_乘法 接受 a、b：
    积 为 做 GF2_乘法 用 a 位与 0xFF、b 位与 0xFF。
    返还 做 GF2_模约简 用 积、0x11B。
完。

位操作_完整统计 接受 零参：
    返还 一组：一组：「操作总数」、操作总数、一组：「功能」、「CRC32+纠缠校验+Morton+GF2+批量位操作+矩阵转置+CRC16+位散布」。
完。

CRC16_多项式 为 0x1021。

CRC16_CCITT 接受 数据、长度：
    操作总数 为 操作总数 加 1。
    可变 crc 为 0xFFFF。
    可变 i 为 0。
    只要 i 小于 长度：
        crc 为 crc 位异或 做 内存_读8 用 数据 加 i 左移 8。
        可变 位 为 0。
        只要 位 小于 8：
            若 crc 位与 0x8000：
                crc 为 (crc 左移 1 位异或 CRC16_多项式) 位与 0xFFFF。
            否则：
                crc 为 crc 左移 1 位与 0xFFFF。
            完。
            位 为 位 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 crc。
完。

CRC16_MODBUS 接受 数据、长度：
    操作总数 为 操作总数 加 1。
    可变 crc 为 0xFFFF。
    可变 i 为 0。
    只要 i 小于 长度：
        crc 为 crc 位异或 做 内存_读8 用 数据 加 i。
        可变 位 为 0。
        只要 位 小于 8：
            若 crc 位与 1：
                crc 为 crc 右移 1 位异或 0xA001。
            否则：
                crc 为 crc 右移 1。
            完。
            位 为 位 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 crc。
完。

位散布 接受 源、掩码：
    操作总数 为 操作总数 加 1。
    可变 结果 为 0。
    可变 源位 为 0。
    可变 位 为 0。
    只要 位 小于 64：
        若 掩码 位与 1 左移 位：
            若 源 位与 1 左移 源位：
                结果 为 结果 位或 1 左移 位。
            完。
            源位 为 源位 加 1。
        完。
        位 为 位 加 1。
    完。
    返还 结果。
完。

位收集 接受 源、掩码：
    操作总数 为 操作总数 加 1。
    可变 结果 为 0。
    可变 结果位 为 0。
    可变 位 为 0。
    只要 位 小于 64：
        若 掩码 位与 1 左移 位：
            若 源 位与 1 左移 位：
                结果 为 结果 位或 1 左移 结果位。
            完。
            结果位 为 结果位 加 1。
        完。
        位 为 位 加 1。
    完。
    返还 结果。
完。

Morton编码3D 接受 x、y、z：
    操作总数 为 操作总数 加 1。
    可变 mx 为 x 位与 0x3FF。
    可变 my 为 y 位与 0x3FF。
    可变 mz 为 z 位与 0x3FF。
    mx 为 做 Morton展开10 用 mx。
    my 为 做 Morton展开10 用 my。
    mz 为 做 Morton展开10 用 mz。
    返还 mx 位或 my 左移 1 位或 mz 左移 2。
完。

Morton展开10 接受 v：
    可变 x 为 v 位与 0x3FF。
    x 为 (x 位或 x 左移 16) 位与 0x030000FF。
    x 为 (x 位或 x 左移 8) 位与 0x0300F00F。
    x 为 (x 位或 x 左移 4) 位与 0x030C30C3。
    x 为 (x 位或 x 左移 2) 位与 0x09249249。
    返还 x。
完。

Morton解码3D 接受 码：
    操作总数 为 操作总数 加 1。
    x 为 做 Morton压缩10 用 码。
    y 为 做 Morton压缩10 用 码 右移 1。
    z 为 做 Morton压缩10 用 码 右移 2。
    返还 一组：x、y、z。
完。

Morton压缩10 接受 v：
    可变 x 为 v 位与 0x09249249。
    x 为 (x 位或 x 右移 2) 位与 0x030C30C3。
    x 为 (x 位或 x 右移 4) 位与 0x0300F00F。
    x 为 (x 位或 x 右移 8) 位与 0x030000FF。
    x 为 (x 位或 x 右移 16) 位与 0x3FF。
    返还 x。
完。

位操作_重置统计 接受 零参：
    操作总数 为 0。
完。

位操作_获取操作数 接受 零参：
    返还 操作总数。
完。

位操作_打印统计 接受 零参：
    做 打印行 用 「=== 位操作统计 ===」。
    做 打印行 用 「操作总数: 」 加 做 整数转字符串 用 操作总数。
    做 打印行 用 「功能集: CRC32/CRC16-CCITT/CRC16-MODBUS/纠缠校验」。
    做 打印行 用 「         Morton2D/Morton3D/GF2/位散布/位收集」。
    做 打印行 用 「         矩阵转置/批量位操作/汉明距离」。
完。

位操作_健康检查 接受 零参：
    可变 问题 为 0。
    若 做 奇偶校验 用 0x00 不等于 0：
        问题 为 问题 加 1。
    完。
    若 做 奇偶校验 用 0x01 不等于 1：
        问题 为 问题 加 1。
    完。
    若 做 位计数 用 0xFF 不等于 8：
        问题 为 问题 加 1。
    完。
    编码值 为 做 Morton编码 用 123、456。
    解码值 为 做 Morton解码 用 编码值。
    若 解码值 的 第 0 项 不等于 123 或者 解码值 的 第 1 项 不等于 456：
        问题 为 问题 加 1。
    完。
    若 做 GF256_乘法 用 1、0x53 不等于 0x53：
        问题 为 问题 加 1。
    完。
    返还 问题 等于 0。
完。

位反转32 接受 值：
    操作总数 为 操作总数 加 1。
    可变 v 为 值。
    v 为 (v 位与 0x55555555) 左移 1 位或 v 右移 1 位与 0x55555555。
    v 为 (v 位与 0x33333333) 左移 2 位或 v 右移 2 位与 0x33333333。
    v 为 (v 位与 0x0F0F0F0F) 左移 4 位或 v 右移 4 位与 0x0F0F0F0F。
    v 为 (v 位与 0x00FF00FF) 左移 8 位或 v 右移 8 位与 0x00FF00FF。
    v 为 v 左移 16 位或 v 右移 16。
    返还 v 位与 0xFFFFFFFF。
完。

位反转N 接受 值、位数：
    操作总数 为 操作总数 加 1。
    可变 结果 为 0。
    可变 v 为 值。
    可变 i 为 0。
    只要 i 小于 位数：
        结果 为 结果 左移 1 位或 v 位与 1。
        v 为 v 右移 1。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 AI校验_快速路径数 为 0。

AI校验_算法选择 接受 数据大小：
    AI校验_快速路径数 为 AI校验_快速路径数 加 1。
    若 数据大小 小于 64：
        返还 「XOR」。
    完。
    若 数据大小 小于 4096：
        返还 「CRC32」。
    完。
    返还 「CRC32C硬件加速」。
完。

本章 收束。
