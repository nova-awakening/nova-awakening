《数学计算反正》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《.·内核打印》。

引用《.·张量数学引擎》。

可变 数学_调用次数 为 0。

可变 数学_迭代总数 为 0。

反正切 接受 x：
    数学_调用次数 为 数学_调用次数 加 1。
    可变 归约 为 假。
    可变 负 为 假。
    可变 v 为 x。
    若 v 小于 0：
        负 为 真。
        v 为 0 减 v。
    完。
    若 v 大于 数学_精度：
        归约 为 真。
        v 为 数学_精度 乘 数学_精度 除以 v。
    完。
    v2 为 v 乘 v 除以 数学_精度。
    可变 结果 为 v。
    可变 项 为 v 乘 v2 除以 数学_精度。
    结果 为 结果 减 项 除以 3。
    项 为 项 乘 v2 除以 数学_精度。
    结果 为 结果 加 项 除以 5。
    项 为 项 乘 v2 除以 数学_精度。
    结果 为 结果 减 项 除以 7。
    项 为 项 乘 v2 除以 数学_精度。
    结果 为 结果 加 项 除以 9。
    若 归约：
        结果 为 数学_半PI 减 结果。
    完。
    若 负：
        结果 为 0 减 结果。
    完。
    返还 结果。
完。

反正切2 接受 y、x：
    数学_调用次数 为 数学_调用次数 加 1。
    若 x 大于 0：
        返还 做 反正切 用 y 乘 数学_精度 除以 x。
    完。
    若 x 小于 0 并且 y 大于或等于 0：
        返还 做 反正切 用 y 乘 数学_精度 除以 x 加 数学_PI。
    完。
    若 x 小于 0 并且 y 小于 0：
        返还 做 反正切 用 y 乘 数学_精度 除以 x 减 数学_PI。
    完。
    若 x 等于 0 并且 y 大于 0：
        返还 数学_半PI。
    完。
    若 x 等于 0 并且 y 小于 0：
        返还 0 减 数学_半PI。
    完。
    返还 0。
完。

反正弦 接受 x：
    数学_调用次数 为 数学_调用次数 加 1。
    若 x 大于或等于 数学_精度：
        返还 数学_半PI。
    完。
    若 x 小于或等于 0 减 数学_精度：
        返还 0 减 数学_半PI。
    完。
    分母 为 做 定点平方根 用 数学_精度 乘 数学_精度 减 x 乘 x。
    若 分母 等于 0：
        返还 数学_半PI。
    完。
    返还 做 反正切 用 x 乘 数学_精度 除以 分母。
完。

反余弦 接受 x：
    数学_调用次数 为 数学_调用次数 加 1。
    返还 数学_半PI 减 做 反正弦 用 x。
完。

定点_指数 接受 x：
    数学_调用次数 为 数学_调用次数 加 1。
    可变 结果 为 数学_精度。
    可变 项 为 数学_精度。
    可变 i 为 1。
    只要 i 小于或等于 15：
        项 为 项 乘 x 除以 (i 乘 数学_精度)。
        结果 为 结果 加 项。
        数学_迭代总数 为 数学_迭代总数 加 1。
        若 项 等于 0：
            中断。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

定点_自然对数 接受 x：
    数学_调用次数 为 数学_调用次数 加 1。
    若 x 小于或等于 0：
        返还 0 减 0x7FFFFFFF。
    完。
    若 x 等于 数学_精度：
        返还 0。
    完。
    可变 k 为 0。
    可变 v 为 x。
    只要 v 大于 2000：
        v 为 v 除以 2。
        k 为 k 加 1。
    完。
    只要 v 小于 500：
        v 为 v 乘 2。
        k 为 k 减 1。
    完。
    t 为 (v 减 数学_精度) 乘 数学_精度 除以 (v 加 数学_精度)。
    t2 为 t 乘 t 除以 数学_精度。
    可变 结果 为 t。
    可变 幂次 为 t 乘 t2 除以 数学_精度。
    结果 为 结果 加 幂次 除以 3。
    幂次 为 幂次 乘 t2 除以 数学_精度。
    结果 为 结果 加 幂次 除以 5。
    幂次 为 幂次 乘 t2 除以 数学_精度。
    结果 为 结果 加 幂次 除以 7。
    幂次 为 幂次 乘 t2 除以 数学_精度。
    结果 为 结果 加 幂次 除以 9。
    结果 为 结果 乘 2。
    返还 结果 加 k 乘 693。
完。

定点_常用对数 接受 x：
    数学_调用次数 为 数学_调用次数 加 1。
    返还 做 定点_自然对数 用 x 乘 数学_精度 除以 2303。
完。

定点_二进制对数 接受 x：
    数学_调用次数 为 数学_调用次数 加 1。
    返还 做 定点_自然对数 用 x 乘 数学_精度 除以 693。
完。

定点_双曲正弦 接受 x：
    数学_调用次数 为 数学_调用次数 加 1。
    正 为 做 定点_指数 用 x。
    负 为 做 定点_指数 用 0 减 x。
    返还 (正 减 负) 除以 2。
完。

定点_双曲余弦 接受 x：
    数学_调用次数 为 数学_调用次数 加 1。
    正 为 做 定点_指数 用 x。
    负 为 做 定点_指数 用 0 减 x。
    返还 (正 加 负) 除以 2。
完。

定点_双曲正切 接受 x：
    数学_调用次数 为 数学_调用次数 加 1。
    正 为 做 定点_指数 用 x。
    负 为 做 定点_指数 用 0 减 x。
    分母 为 正 加 负。
    若 分母 等于 0：
        返还 0。
    完。
    返还 (正 减 负) 乘 数学_精度 除以 分母。
完。

定点_反双曲正切 接受 x：
    数学_调用次数 为 数学_调用次数 加 1。
    若 x 大于或等于 数学_精度 或者 x 小于或等于 0 减 数学_精度：
        返还 0。
    完。
    分子 为 数学_精度 加 x。
    分母 为 数学_精度 减 x。
    若 分母 等于 0：
        返还 0x7FFFFFFF。
    完。
    返还 做 定点_自然对数 用 分子 乘 数学_精度 除以 分母 除以 2。
完。

定点_整数幂 接受 底、指数：
    数学_调用次数 为 数学_调用次数 加 1。
    若 指数 等于 0：
        返还 数学_精度。
    完。
    可变 负 为 假。
    可变 n 为 指数。
    若 n 小于 0：
        负 为 真。
        n 为 0 减 n。
    完。
    可变 结果 为 数学_精度。
    可变 基 为 底。
    只要 n 大于 0：
        若 n 取余于 2 等于 1：
            结果 为 结果 乘 基 除以 数学_精度。
        完。
        基 为 基 乘 基 除以 数学_精度。
        n 为 n 除以 2。
        数学_迭代总数 为 数学_迭代总数 加 1。
    完。
    若 负：
        若 结果 等于 0：
            返还 0x7FFFFFFF。
        完。
        返还 数学_精度 乘 数学_精度 除以 结果。
    完。
    返还 结果。
完。

定点_幂 接受 底、指数：
    数学_调用次数 为 数学_调用次数 加 1。
    若 底 小于或等于 0：
        返还 0。
    完。
    对数 为 做 定点_自然对数 用 底。
    返还 做 定点_指数 用 指数 乘 对数 除以 数学_精度。
完。

定点_N次根 接受 x、n：
    数学_调用次数 为 数学_调用次数 加 1。
    若 x 小于或等于 0 或者 n 小于或等于 0：
        返还 0。
    完。
    对数 为 做 定点_自然对数 用 x。
    返还 做 定点_指数 用 对数 除以 n。
完。

定点_立方根 接受 x：
    数学_调用次数 为 数学_调用次数 加 1。
    若 x 等于 0：
        返还 0。
    完。
    可变 负 为 假。
    可变 v 为 x。
    若 v 小于 0：
        负 为 真。
        v 为 0 减 v。
    完。
    可变 r 为 v。
    可变 迭代 为 0。
    只要 迭代 小于 20：
        若 r 等于 0：
            中断。
        完。
        r 为 (2 乘 r 加 v 乘 数学_精度 除以 (r 乘 r 除以 数学_精度)) 除以 3。
        迭代 为 迭代 加 1。
        数学_迭代总数 为 数学_迭代总数 加 1。
    完。
    若 负：
        返还 0 减 r。
    完。
    返还 r。
完。

阶乘 接受 n：
    数学_调用次数 为 数学_调用次数 加 1。
    若 n 小于或等于 1：
        返还 1。
    完。
    可变 结果 为 1。
    可变 i 为 2。
    只要 i 小于或等于 n：
        结果 为 结果 乘 i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

组合数 接受 n、k：
    数学_调用次数 为 数学_调用次数 加 1。
    若 k 大于 n 或者 k 小于 0：
        返还 0。
    完。
    若 k 等于 0 或者 k 等于 n：
        返还 1。
    完。
    可变 实际k 为 k。
    若 实际k 大于 n 减 实际k：
        实际k 为 n 减 实际k。
    完。
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 实际k：
        结果 为 结果 乘 (n 减 i) 除以 (i 加 1)。
        i 为 i 加 1。
    完。
    返还 结果。
完。

排列数 接受 n、k：
    数学_调用次数 为 数学_调用次数 加 1。
    若 k 大于 n 或者 k 小于 0：
        返还 0。
    完。
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 k：
        结果 为 结果 乘 (n 减 i)。
        i 为 i 加 1。
    完。
    返还 结果。
完。

最大公约数 接受 a、b：
    数学_调用次数 为 数学_调用次数 加 1。
    可变 x 为 a。
    可变 y 为 b。
    若 x 小于 0：
        x 为 0 减 x。
    完。
    若 y 小于 0：
        y 为 0 减 y。
    完。
    只要 y 不等于 0：
        t 为 y。
        y 为 x 取余于 y。
        x 为 t。
    完。
    返还 x。
完。

最小公倍数 接受 a、b：
    g 为 做 最大公约数 用 a、b。
    若 g 等于 0：
        返还 0。
    完。
    返还 a 除以 g 乘 b。
完。

多项式求值 接受 系数、x：
    数学_调用次数 为 数学_调用次数 加 1。
    n 为 做 计数 用 系数。
    若 n 等于 0：
        返还 0。
    完。
    可变 结果 为 系数 的 第 n 减 1 项。
    可变 i 为 n 减 2。
    只要 i 大于或等于 0：
        结果 为 结果 乘 x 除以 数学_精度 加 系数 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 结果。
完。

多项式导数 接受 系数：
    数学_调用次数 为 数学_调用次数 加 1。
    n 为 做 计数 用 系数。
    若 n 小于或等于 1：
        返还 一组：0。
    完。
    可变 导系数 为 一组：（空）。
    可变 i 为 1。
    只要 i 小于 n：
        做 追加 用 导系数、系数 的 第 i 项 乘 i。
        i 为 i 加 1。
    完。
    返还 导系数。
完。

数值导数 接受 系数、x、h：
    数学_调用次数 为 数学_调用次数 加 1。
    若 h 等于 0：
        h 为 数学_精度 除以 100。
    完。
    f正 为 做 多项式求值 用 系数、x 加 h。
    f负 为 做 多项式求值 用 系数、x 减 h。
    返还 (f正 减 f负) 乘 数学_精度 除以 (2 乘 h)。
完。

数值二阶导 接受 系数、x、h：
    数学_调用次数 为 数学_调用次数 加 1。
    若 h 等于 0：
        h 为 数学_精度 除以 100。
    完。
    f正 为 做 多项式求值 用 系数、x 加 h。
    f中 为 做 多项式求值 用 系数、x。
    f负 为 做 多项式求值 用 系数、x 减 h。
    返还 (f正 减 2 乘 f中 加 f负) 乘 数学_精度 除以 (h 乘 h 除以 数学_精度)。
完。

辛普森积分 接受 系数、a、b、n：
    数学_调用次数 为 数学_调用次数 加 1。
    若 n 取余于 2 不等于 0：
        n 为 n 加 1。
    完。
    若 n 小于 2：
        n 为 2。
    完。
    h 为 (b 减 a) 乘 数学_精度 除以 n。
    可变 总和 为 做 多项式求值 用 系数、a 加 做 多项式求值 用 系数、b。
    可变 i 为 1。
    只要 i 小于 n：
        xi 为 a 加 i 乘 h 除以 数学_精度。
        若 i 取余于 2 等于 0：
            总和 为 总和 加 2 乘 做 多项式求值 用 系数、xi。
        否则：
            总和 为 总和 加 4 乘 做 多项式求值 用 系数、xi。
        完。
        数学_迭代总数 为 数学_迭代总数 加 1。
        i 为 i 加 1。
    完。
    返还 总和 乘 h 除以 (3 乘 数学_精度)。
完。

梯形积分 接受 系数、a、b、n：
    数学_调用次数 为 数学_调用次数 加 1。
    若 n 小于 1：
        n 为 1。
    完。
    h 为 (b 减 a) 乘 数学_精度 除以 n。
    可变 总和 为 (做 多项式求值 用 系数、a 加 做 多项式求值 用 系数、b) 除以 2。
    可变 i 为 1。
    只要 i 小于 n：
        xi 为 a 加 i 乘 h 除以 数学_精度。
        总和 为 总和 加 做 多项式求值 用 系数、xi。
        数学_迭代总数 为 数学_迭代总数 加 1。
        i 为 i 加 1。
    完。
    返还 总和 乘 h 除以 数学_精度。
完。

线性插值 接受 x0、y0、x1、y1、x：
    数学_调用次数 为 数学_调用次数 加 1。
    若 x1 等于 x0：
        返还 y0。
    完。
    返还 y0 加 (y1 减 y0) 乘 (x 减 x0) 除以 (x1 减 x0)。
完。

拉格朗日插值 接受 xs、ys、x：
    数学_调用次数 为 数学_调用次数 加 1。
    n 为 做 计数 用 xs。
    可变 结果 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 基 为 数学_精度。
        可变 j 为 0。
        只要 j 小于 n：
            若 j 不等于 i：
                分子 为 x 减 xs 的 第 j 项。
                分母 为 xs 的 第 i 项 减 xs 的 第 j 项。
                若 分母 不等于 0：
                    基 为 基 乘 分子 除以 分母。
                完。
            完。
            j 为 j 加 1。
        完。
        结果 为 结果 加 ys 的 第 i 项 乘 基 除以 数学_精度。
        i 为 i 加 1。
    完。
    返还 结果。
完。

牛顿求根 接受 系数、初始猜测、最大迭代、容差：
    数学_调用次数 为 数学_调用次数 加 1。
    导系数 为 做 多项式导数 用 系数。
    可变 x 为 初始猜测。
    可变 迭代 为 0。
    只要 迭代 小于 最大迭代：
        fx 为 做 多项式求值 用 系数、x。
        若 fx 大于 0 减 容差 并且 fx 小于 容差：
            中断。
        完。
        dfx 为 做 多项式求值 用 导系数、x。
        若 dfx 等于 0：
            中断。
        完。
        x 为 x 减 fx 乘 数学_精度 除以 dfx。
        数学_迭代总数 为 数学_迭代总数 加 1。
        迭代 为 迭代 加 1。
    完。
    返还 x。
完。

行列式2x2 接受 a、b、c、d：
    数学_调用次数 为 数学_调用次数 加 1。
    返还 a 乘 d 除以 数学_精度 减 b 乘 c 除以 数学_精度。
完。

行列式3x3 接受 m：
    数学_调用次数 为 数学_调用次数 加 1。
    a 为 m 的 第 0 项 乘 (m 的 第 4 项 乘 m 的 第 8 项 除以 数学_精度 减 m 的 第 5 项 乘 m 的 第 7 项 除以 数学_精度) 除以 数学_精度。
    b 为 m 的 第 1 项 乘 (m 的 第 3 项 乘 m 的 第 8 项 除以 数学_精度 减 m 的 第 5 项 乘 m 的 第 6 项 除以 数学_精度) 除以 数学_精度。
    c 为 m 的 第 2 项 乘 (m 的 第 3 项 乘 m 的 第 7 项 除以 数学_精度 减 m 的 第 4 项 乘 m 的 第 6 项 除以 数学_精度) 除以 数学_精度。
    返还 a 减 b 加 c。
完。

下取整 接受 x：
    返还 x 除以 数学_精度 乘 数学_精度。
完。

上取整 接受 x：
    若 x 取余于 数学_精度 等于 0：
        返还 x。
    完。
    若 x 大于 0：
        返还 (x 除以 数学_精度 加 1) 乘 数学_精度。
    完。
    返还 x 除以 数学_精度 乘 数学_精度。
完。

四舍五入 接受 x：
    若 x 大于或等于 0：
        返还 (x 加 数学_精度 除以 2) 除以 数学_精度 乘 数学_精度。
    完。
    返还 (x 减 数学_精度 除以 2) 除以 数学_精度 乘 数学_精度。
完。

绝对值 接受 x：
    若 x 小于 0：
        返还 0 减 x。
    完。
    返还 x。
完。

符号 接受 x：
    若 x 大于 0：
        返还 1。
    完。
    若 x 小于 0：
        返还 0 减 1。
    完。
    返还 0。
完。

二分法求根 接受 系数、a、b、最大迭代、容差：
    数学_调用次数 为 数学_调用次数 加 1。
    fa 为 做 多项式求值 用 系数、a。
    fb 为 做 多项式求值 用 系数、b。
    若 fa 大于 0 并且 fb 大于 0 或者 fa 小于 0 并且 fb 小于 0：
        返还 (a 加 b) 除以 2。
    完。
    可变 左 为 a。
    可变 右 为 b。
    可变 迭代 为 0。
    只要 迭代 小于 最大迭代：
        中 为 (左 加 右) 除以 2。
        fm 为 做 多项式求值 用 系数、中。
        若 fm 大于 0 减 容差 并且 fm 小于 容差：
            返还 中。
        完。
        fl 为 做 多项式求值 用 系数、左。
        若 fl 大于 0 并且 fm 小于 0 或者 fl 小于 0 并且 fm 大于 0：
            右 为 中。
        否则：
            左 为 中。
        完。
        数学_迭代总数 为 数学_迭代总数 加 1。
        迭代 为 迭代 加 1。
    完。
    返还 (左 加 右) 除以 2。
完。

割线法求根 接受 系数、x0、x1、最大迭代、容差：
    数学_调用次数 为 数学_调用次数 加 1。
    可变 a 为 x0。
    可变 b 为 x1。
    可变 fa 为 做 多项式求值 用 系数、a。
    可变 fb 为 做 多项式求值 用 系数、b。
    可变 迭代 为 0。
    只要 迭代 小于 最大迭代：
        若 fb 大于 0 减 容差 并且 fb 小于 容差：
            返还 b。
        完。
        差 为 fb 减 fa。
        若 差 等于 0：
            返还 b。
        完。
        新x 为 b 减 fb 乘 (b 减 a) 除以 差。
        a 为 b。
        fa 为 fb。
        b 为 新x。
        fb 为 做 多项式求值 用 系数、b。
        数学_迭代总数 为 数学_迭代总数 加 1。
        迭代 为 迭代 加 1。
    完。
    返还 b。
完。

数值_溢出阈值 为 0x70000000。

数值_下溢阈值 为 1。

数值_安全乘 接受 左、右：
    若 右 等于 0：
        返还 0。
    完。
    可变 abs_左 为 左。
    可变 abs_右 为 右。
    若 abs_左 小于 0：
        abs_左 为 0 减 abs_左。
    完。
    若 abs_右 小于 0：
        abs_右 为 0 减 abs_右。
    完。
    若 abs_左 大于 数值_溢出阈值 除以 abs_右：
        若 左 大于 0 并且 右 大于 0 或者 左 小于 0 并且 右 小于 0：
            返还 数值_溢出阈值。
        完。
        返还 0 减 数值_溢出阈值。
    完。
    返还 左 乘 右。
完。

数值_安全除 接受 被除、除数：
    若 除数 等于 0：
        返还 0x7FFFFFFF。
    完。
    返还 被除 除以 除数。
完。

数值_检查溢出 接受 x：
    若 x 大于 数值_溢出阈值：
        返还 1。
    完。
    若 x 小于 0 减 数值_溢出阈值：
        返还 负 1。
    完。
    返还 0。
完。

数学_打印统计 接受 零参：
    做 打印行 用 「=== 数学计算统计 ===」。
    做 打印行 用 「函数调用: 」 加 做 整数转字符串 用 数学_调用次数。
    做 打印行 用 「迭代总数: 」 加 做 整数转字符串 用 数学_迭代总数。
完。

可变 _谐振数学_精度级别 为 2。

可变 _谐振数学_调整次数 为 0。

谐振数学_设置精度 接受 级别：
    若 级别 大于或等于 0 并且 级别 小于或等于 3：
        _谐振数学_精度级别 为 级别。
        _谐振数学_调整次数 为 _谐振数学_调整次数 加 1。
    完。
完。

谐振数学_获取迭代数 接受 零参：
    若 _谐振数学_精度级别 等于 0：
        返还 4。
    完。
    若 _谐振数学_精度级别 等于 1：
        返还 8。
    完。
    若 _谐振数学_精度级别 等于 3：
        返还 32。
    完。
    返还 16。
完。

数学计算_完整统计 接受 零参：
    返还 一组：「函数调用」、数学_调用次数、「迭代总数」、数学_迭代总数、「谐振精度级别」、_谐振数学_精度级别、「谐振调整次数」、_谐振数学_调整次数。
完。

可变 AI数学_查表命中 为 0。

AI数学_三角查表 接受 角度：
    AI数学_查表命中 为 AI数学_查表命中 加 1。
    若 角度 小于 0：
        角度 为 0 减 角度。
    完。
    若 角度 大于 360：
        角度 为 角度 取余于 360。
    完。
    返还 角度 取余于 90。
完。

本章 收束。
