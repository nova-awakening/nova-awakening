《进程任务》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·进程凭证》。

引用《.·进程描述符》。

引用《..·标准·原生字符串》。

引用《.·进程组》。

引用《.·进程限制》。

引用《.·整数转字符串》。

引用《.·进程退出》。

引用《.·空闲进程》。

引用《.·执行分叉》。

引用《.·克隆标志验证》。

引用《.·安全管理上》。

任务角色_用户 为 「用户」。

任务角色_内核 为 「内核」。

任务角色_空闲 为 「空闲」。

任务角色_服务 为 「服务」。

任务档 是 一种 事物，含 名称、角色、任务、凭证、限制、组号、会话号、父号、标志。

任务摘要 是 一种 事物，含 名称、角色、标识、组号、会话号、父号、状态、优先级、nice值、权重、调度、内核线程、退出中、等待状态。

可变 任务账本 为 一组：（空）。

可变 任务建数 为 0。

可变 任务退数 为 0。

可变 任务等数 为 0。

可变 任务克隆数 为 0。

可变 任务执行数 为 0。

任务布尔串 接受 值：
    若 值：
        返还 「true」。
    完。
    返还 「false」。
完。

任务角色名 接受 角色：
    若 角色 等于 任务角色_用户：
        返还 「用户」。
    完。
    若 角色 等于 任务角色_内核：
        返还 「内核」。
    完。
    若 角色 等于 任务角色_空闲：
        返还 「空闲」。
    完。
    若 角色 等于 任务角色_服务：
        返还 「服务」。
    完。
    返还 「未知」。
完。

任务状态名 接受 状态：
    若 状态 等于 进程_运行：
        返还 「运行」。
    完。
    若 状态 等于 进程_等待：
        返还 「等待」。
    完。
    若 状态 等于 进程_就绪：
        返还 「就绪」。
    完。
    若 状态 等于 进程_停止：
        返还 「停止」。
    完。
    返还 「未知」。
完。

进程任务_建档 接受 任务、名称、角色：
    档 为 做 堆_分配 用 任务档。
    档 的 名称 为 名称。
    档 的 角色 为 角色。
    档 的 任务 为 任务。
    档 的 凭证 为 做 凭证_创建_root。
    档 的 限制 为 做 默认资源限制。
    档 的 组号 为 做 进程_PGID 用 任务。
    档 的 会话号 为 做 进程_SID 用 任务。
    档 的 父号 为 0。
    父 为 做 进程_父 用 任务。
    若 父 不等于 0：
        档 的 父号 为 做 进程_PID 用 父。
    完。
    档 的 标志 为 做 进程_标志 用 任务。
    返还 档。
完。

进程任务_挂账 接受 档：
    做 追加 用 任务账本、档。
    任务建数 为 任务建数 加 1。
    返还 档。
完。

进程任务_创建 接受 标识、名称、优先级、角色：
    任务 为 做 进程_创建 用 标识、名称、优先级。
    档 为 做 进程任务_建档 用 任务、名称、角色。
    返还 做 进程任务_挂账 用 档。
完。

进程任务_创建用户 接受 标识、名称、优先级：
    返还 做 进程任务_创建 用 标识、名称、优先级、任务角色_用户。
完。

进程任务_创建服务 接受 标识、名称、优先级：
    返还 做 进程任务_创建 用 标识、名称、优先级、任务角色_服务。
完。

进程任务_创建内核线程 接受 名称、优先级：
    任务 为 做 kernel_thread 用 名称、优先级。
    档 为 做 进程任务_建档 用 任务、名称、任务角色_内核。
    返还 做 进程任务_挂账 用 档。
完。

进程任务_创建空闲 接受 CPU号：
    任务 为 做 idle进程_创建 用 CPU号。
    档 为 做 进程任务_建档 用 任务、「空闲」 加 做 整数转字符串 用 CPU号、任务角色_空闲。
    返还 做 进程任务_挂账 用 档。
完。

进程任务_查按PID 接受 pid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务账本：
        档 为 任务账本 的 第 i 项。
        若 做 进程_PID 用 档 的 任务 等于 pid：
            返还 档。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

进程任务_查按名称 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务账本：
        档 为 任务账本 的 第 i 项。
        若 档 的 名称 等于 名称：
            返还 档。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

进程任务_PID 接受 档：
    返还 做 进程_PID 用 档 的 任务。
完。

进程任务_名称 接受 档：
    返还 档 的 名称。
完。

进程任务_角色 接受 档：
    返还 档 的 角色。
完。

进程任务_状态 接受 档：
    返还 做 进程_状态 用 档 的 任务。
完。

进程任务_优先级 接受 档：
    返还 做 进程_优先级 用 档 的 任务。
完。

进程任务_nice 接受 档：
    返还 做 进程_nice 用 档 的 任务。
完。

进程任务_权重 接受 档：
    返还 做 进程_权重 用 档 的 任务。
完。

进程任务_调度策略 接受 档：
    返还 做 进程_调度策略 用 档 的 任务。
完。

进程任务_父号 接受 档：
    返还 档 的 父号。
完。

进程任务_组号 接受 档：
    返还 档 的 组号。
完。

进程任务_会话号 接受 档：
    返还 档 的 会话号。
完。

进程任务_UID 接受 档：
    返还 做 进程_UID 用 档 的 任务。
完。

进程任务_GID 接受 档：
    返还 做 进程_GID 用 档 的 任务。
完。

进程任务_TGID 接受 档：
    返还 做 进程_TGID 用 档 的 任务。
完。

进程任务_是内核线程 接受 档：
    返还 做 进程_是内核线程 用 档 的 任务。
完。

进程任务_是退出中 接受 档：
    返还 做 进程_是正在退出 用 档 的 任务。
完。

进程任务_是运行 接受 档：
    返还 做 进程_是运行 用 档 的 任务。
完。

进程任务_状态名 接受 档：
    返还 做 任务状态名 用 做 进程任务_状态 用 档。
完。

进程任务_设状态 接受 档、状态：
    做 进程_设置状态 用 档 的 任务、状态。
完。

进程任务_设优先级 接受 档、优先级：
    做 设置 用 档 的 任务、T_优先级、优先级。
    做 设置 用 档 的 任务、T_权重、做 nice到权重 用 做 进程_nice 用 档 的 任务。
完。

进程任务_设nice 接受 档、nice值：
    做 设置 用 档 的 任务、T_nice值、nice值。
    做 设置 用 档 的 任务、T_优先级、做 nice到优先级 用 nice值。
    做 设置 用 档 的 任务、T_权重、做 nice到权重 用 nice值。
完。

进程任务_设标志 接受 档、标志：
    做 进程_设标志 用 档 的 任务、标志。
    档 的 标志 为 做 进程_标志 用 档 的 任务。
完。

进程任务_清标志 接受 档、标志：
    做 进程_清标志 用 档 的 任务、标志。
    档 的 标志 为 做 进程_标志 用 档 的 任务。
完。

进程任务_分配凭证 接受 档、uid、gid：
    档 的 凭证 为 做 凭证_创建 用 uid、gid。
    返还 档 的 凭证。
完。

进程任务_继承凭证 接受 父档、子档：
    子档 的 凭证 为 做 凭证_复制 用 父档 的 凭证。
    返还 子档 的 凭证。
完。

进程任务_设UID 接受 档、uid：
    返还 做 do_setuid 用 档 的 凭证、uid。
完。

进程任务_设GID 接受 档、gid：
    返还 做 do_setgid 用 档 的 凭证、gid。
完。

进程任务_授权 接受 档、能力：
    做 凭证_授予能力 用 档 的 凭证、能力。
完。

进程任务_撤权 接受 档、能力：
    做 凭证_撤销能力 用 档 的 凭证、能力。
完。

进程任务_是root 接受 档：
    返还 做 凭证_是root 用 档 的 凭证。
完。

进程任务_有能力 接受 档、能力：
    返还 做 凭证_有能力 用 档 的 凭证、能力。
完。

进程任务_权限检查 接受 档、所需uid：
    返还 做 凭证_权限检查 用 档 的 凭证、所需uid。
完。

进程任务_配置默认限制 接受 档：
    档 的 限制 为 做 默认资源限制。
    返还 档 的 限制。
完。

进程任务_继承限制 接受 父档、子档：
    做 rlimit_继承 用 父档 的 任务、子档 的 任务。
    子档 的 限制 为 父档 的 限制。
完。

进程任务_设限制 接受 档、资源、新soft、新hard：
    返还 做 sys_setrlimit 用 资源、新soft、新hard、档 的 限制、做 进程任务_是root 用 档。
完。

进程任务_查限制 接受 档、资源：
    返还 做 sys_getrlimit 用 资源、档 的 限制。
完。

进程任务_检查文件数 接受 档、新增：
    返还 做 进程限制_检查文件数 用 档 的 任务、新增。
完。

进程任务_检查内存 接受 档、请求大小：
    返还 做 进程限制_检查内存 用 档 的 任务、请求大小。
完。

进程任务_检查进程数 接受 档、当前进程数：
    返还 做 检查进程数限制 用 档 的 限制、当前进程数。
完。

进程任务_设置组 接受 档、pgid：
    若 做 进程组_查找 用 pgid 等于 0：
        做 进程组_创建 用 pgid、做 进程任务_PID 用 档。
    完。
    做 进程组_添加成员 用 pgid、做 进程任务_PID 用 档。
    做 do_setpgid 用 做 进程任务_PID 用 档、pgid。
    档 的 组号 为 pgid。
    返还 pgid。
完。

进程任务_建会话 接受 档：
    sid 为 做 do_setsid。
    档 的 会话号 为 sid。
    返还 sid。
完。

进程任务_前台组 接受 档、pgid：
    做 会话_设置前台组 用 档 的 会话号、pgid。
完。

进程任务_取前台组 接受 档：
    返还 做 会话_获取前台组 用 档 的 会话号。
完。

进程任务_发组信号 接受 档、信号：
    返还 做 向进程组发送信号 用 档 的 组号、信号。
完。

进程任务_是孤儿组 接受 档：
    返还 做 进程组_是孤儿 用 档 的 组号。
完。

进程任务_分叉 接受 父档、名称：
    子任务 为 做 do_fork 用 父档 的 任务、名称。
    子档 为 做 进程任务_建档 用 子任务、名称、父档 的 角色。
    子档 的 父号 为 做 进程任务_PID 用 父档。
    子档 的 组号 为 父档 的 组号。
    子档 的 会话号 为 父档 的 会话号。
    做 进程任务_继承凭证 用 父档、子档。
    做 进程任务_继承限制 用 父档、子档。
    做 进程任务_挂账 用 子档。
    任务克隆数 为 任务克隆数 加 1。
    返还 子档。
完。

进程任务_克隆 接受 父档、名称、标志：
    若 非 做 验证clone标志 用 标志：
        返还 0。
    完。
    子任务 为 做 do_clone 用 父档 的 任务、名称、标志。
    子档 为 做 进程任务_建档 用 子任务、名称、父档 的 角色。
    子档 的 父号 为 做 进程任务_PID 用 父档。
    子档 的 组号 为 父档 的 组号。
    子档 的 会话号 为 父档 的 会话号。
    做 进程任务_继承凭证 用 父档、子档。
    做 进程任务_继承限制 用 父档、子档。
    做 进程任务_挂账 用 子档。
    任务克隆数 为 任务克隆数 加 1。
    返还 子档。
完。

进程任务_执行 接受 档、程序路径、参数、环境变量：
    任务执行数 为 任务执行数 加 1。
    返还 做 do_exec 用 档 的 任务、程序路径、参数、环境变量。
完。

进程任务_退出 接受 档、退出码：
    任务退数 为 任务退数 加 1。
    做 do_exit 用 档 的 任务、退出码。
完。

进程任务_组退出 接受 档、退出码：
    任务退数 为 任务退数 加 1。
    做 do_exit_group 用 档 的 任务、退出码。
完。

进程任务_等待 接受 父档：
    任务等数 为 任务等数 加 1。
    返还 做 do_wait 用 父档 的 任务。
完。

进程任务_等待指定 接受 父档、pid、选项：
    任务等数 为 任务等数 加 1。
    返还 做 do_waitpid 用 父档 的 任务、pid、选项。
完。

进程任务_回收僵尸 接受 档：
    做 回收僵尸 用 档 的 任务。
完。

进程任务_关闭文件 接受 档：
    做 关闭所有文件 用 档 的 任务。
完。

进程任务_释放地址空间 接受 档：
    做 释放地址空间 用 档 的 任务。
完。

进程任务_通知父 接受 档：
    做 通知父进程 用 档 的 任务。
完。

进程任务_转交孤儿 接受 档：
    做 孤儿转交 用 档 的 任务。
完。

进程任务_虚拟运行时 接受 档：
    返还 做 进程_虚拟运行时间 用 档 的 任务。
完。

进程任务_增加虚拟运行时 接受 档、增量：
    做 进程_增加虚拟运行时间 用 档 的 任务、增量。
完。

进程任务_摘要 接受 档：
    摘 为 做 堆_分配 用 任务摘要。
    摘 的 名称 为 档 的 名称。
    摘 的 角色 为 做 任务角色名 用 档 的 角色。
    摘 的 标识 为 做 进程任务_PID 用 档。
    摘 的 组号 为 档 的 组号。
    摘 的 会话号 为 档 的 会话号。
    摘 的 父号 为 档 的 父号。
    摘 的 状态 为 做 进程任务_状态名 用 档。
    摘 的 优先级 为 做 进程任务_优先级 用 档。
    摘 的 nice值 为 做 进程任务_nice 用 档。
    摘 的 权重 为 做 进程任务_权重 用 档。
    摘 的 调度 为 做 进程任务_调度策略 用 档。
    摘 的 内核线程 为 做 进程任务_是内核线程 用 档。
    摘 的 退出中 为 做 进程任务_是退出中 用 档。
    摘 的 等待状态 为 非 做 进程任务_是运行 用 档。
    返还 摘。
完。

进程任务_摘要串 接受 档：
    摘 为 做 进程任务_摘要 用 档。
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"名称\":\"」。
    做 字符串构建器_追加 用 建、摘 的 名称。
    做 字符串构建器_追加 用 建、「\",\"角色\":\"」。
    做 字符串构建器_追加 用 建、摘 的 角色。
    做 字符串构建器_追加 用 建、「\",\"标识\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 标识。
    做 字符串构建器_追加 用 建、「,\"状态\":\"」。
    做 字符串构建器_追加 用 建、摘 的 状态。
    做 字符串构建器_追加 用 建、「\",\"优先级\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 优先级。
    做 字符串构建器_追加 用 建、「,\"nice\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 nice值。
    做 字符串构建器_追加 用 建、「,\"权重\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 权重。
    做 字符串构建器_追加 用 建、「,\"组号\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 组号。
    做 字符串构建器_追加 用 建、「,\"会话号\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 会话号。
    做 字符串构建器_追加 用 建、「,\"内核线程\":」。
    做 字符串构建器_追加 用 建、做 任务布尔串 用 摘 的 内核线程。
    做 字符串构建器_追加 用 建、「,\"退出中\":」。
    做 字符串构建器_追加 用 建、做 任务布尔串 用 摘 的 退出中。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

进程任务_批量摘要串 接受 列表：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「[」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 i 大于 0：
            做 字符串构建器_追加 用 建、「,」。
        完。
        做 字符串构建器_追加 用 建、做 进程任务_摘要串 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    做 字符串构建器_追加 用 建、「]」。
    返还 做 字符串构建器_构建 用 建。
完。

进程任务_统计串 接受 零参：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"建数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 任务建数。
    做 字符串构建器_追加 用 建、「,\"退数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 任务退数。
    做 字符串构建器_追加 用 建、「,\"等数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 任务等数。
    做 字符串构建器_追加 用 建、「,\"克隆数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 任务克隆数。
    做 字符串构建器_追加 用 建、「,\"执行数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 任务执行数。
    做 字符串构建器_追加 用 建、「,\"fork总数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 fork_计数。
    做 字符串构建器_追加 用 建、「,\"fork失败\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 fork_失败计数。
    做 字符串构建器_追加 用 建、「,\"账本数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 计数 用 任务账本。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

进程任务_系统串 接受 零参：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"任务\":」。
    做 字符串构建器_追加 用 建、做 进程任务_统计串。
    做 字符串构建器_追加 用 建、「,\"进程组\":\"」。
    做 字符串构建器_追加 用 建、做 进程组_完整统计。
    做 字符串构建器_追加 用 建、「\",\"退出\":\"」。
    做 字符串构建器_追加 用 建、做 进程退出_完整统计。
    做 字符串构建器_追加 用 建、「\",\"限制\":\"」。
    做 字符串构建器_追加 用 建、做 进程限制_完整统计。
    做 字符串构建器_追加 用 建、「\",\"空闲\":\"」。
    做 字符串构建器_追加 用 建、做 idle_完整统计。
    做 字符串构建器_追加 用 建、「\"}」。
    返还 做 字符串构建器_构建 用 建。
完。

进程任务_健康检查 接受 档：
    可变 错数 为 0。
    若 档 等于 0：
        返还 1。
    完。
    若 档 的 任务 等于 0：
        错数 为 错数 加 1。
    完。
    若 档 的 凭证 等于 0：
        错数 为 错数 加 1。
    完。
    若 档 的 限制 等于 0：
        错数 为 错数 加 1。
    完。
    若 做 凭证_健康检查 不等于 0：
        错数 为 错数 加 1。
    完。
    若 做 进程组_健康检查 不等于 0：
        错数 为 错数 加 1。
    完。
    返还 错数。
完。

进程任务_账本健康检查 接受 零参：
    可变 错数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务账本：
        错数 为 错数 加 做 进程任务_健康检查 用 任务账本 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 错数。
完。

进程任务_活跃数 接受 零参：
    可变 数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务账本：
        若 非 做 进程任务_是退出中 用 任务账本 的 第 i 项：
            数 为 数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数。
完。

进程任务_用户数 接受 零参：
    可变 数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务账本：
        若 任务账本 的 第 i 项 的 角色 等于 任务角色_用户：
            数 为 数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数。
完。

进程任务_内核数 接受 零参：
    可变 数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务账本：
        若 任务账本 的 第 i 项 的 角色 等于 任务角色_内核 或者 任务账本 的 第 i 项 的 角色 等于 任务角色_空闲：
            数 为 数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数。
完。

进程任务_摘要总览 接受 零参：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"活跃数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 进程任务_活跃数。
    做 字符串构建器_追加 用 建、「,\"用户数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 进程任务_用户数。
    做 字符串构建器_追加 用 建、「,\"内核数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 进程任务_内核数。
    做 字符串构建器_追加 用 建、「,\"健康错误\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 进程任务_账本健康检查。
    做 字符串构建器_追加 用 建、「,\"明细\":」。
    做 字符串构建器_追加 用 建、做 进程任务_批量摘要串 用 任务账本。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

进程任务_示例流程 接受 零参：
    根 为 做 进程任务_创建服务 用 1、「系统根」、120。
    做 进程任务_分配凭证 用 根、0、0。
    做 进程任务_配置默认限制 用 根。
    做 进程任务_建会话 用 根。
    做 进程任务_设置组 用 根、1。
    服务 为 做 进程任务_创建服务 用 2、「外联服」、110。
    做 进程任务_继承凭证 用 根、服务。
    做 进程任务_继承限制 用 根、服务。
    做 进程任务_设置组 用 服务、1。
    用户 为 做 进程任务_创建用户 用 100、「前台用户」、100。
    做 进程任务_分配凭证 用 用户、1000、1000。
    做 进程任务_设置组 用 用户、100。
    做 进程任务_建会话 用 用户。
    做 进程任务_前台组 用 用户、100。
    子 为 做 进程任务_分叉 用 用户、「前台子进程」。
    做 进程任务_执行 用 子、「/系统/壳」、一组：「-c」、「echo」、一组：（空）。
    做 进程任务_检查文件数 用 子、1。
    做 进程任务_检查内存 用 子、4096。
    工作者 为 做 进程任务_创建内核线程 用 「后台核」、90。
    空闲 为 做 进程任务_创建空闲 用 0。
    做 进程任务_发组信号 用 用户、15。
    做 进程任务_等待指定 用 用户、做 进程任务_PID 用 子、0。
    做 进程任务_退出 用 子、0。
    做 进程任务_回收僵尸 用 子。
    返还 做 进程任务_摘要总览。
完。

可变 AI_进程任务_运行指标 为 0。

可变 AI_进程任务_异常指标 为 0。

AI_进程任务_自诊断 接受 零参：
    AI_进程任务_运行指标 为 0。
    返还 AI_进程任务_运行指标。
完。

AI_进程任务_优化建议 接受 零参：
    指标 为 做 AI_进程任务_自诊断。
    返还 「进程任务状态正常」。
完。

本章 收束。
