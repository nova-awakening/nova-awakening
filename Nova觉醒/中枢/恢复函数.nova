《恢复函数》

引用《.·突触网格导》。

引用《.·整数转字符串》。

引用《.·公平调度时间片》。

引用《.·检查点校验和》。

引用《.·伙伴恢复空闲》。

恢复_TNF权重 接受 快照：
    若 快照 等于 空 或者 做 计数 用 快照 等于 0：
        返还。
    完。
    做 TNF_导入权重 用 快照。
    做 内核信息 用 「检查点恢复: TNF权重」。
完。

恢复_赫布连接 接受 快照：
    若 快照 等于 空 或者 做 计数 用 快照 等于 0：
        返还。
    完。
    做 赫布_导入连接 用 快照。
    做 内核信息 用 「检查点恢复: 赫布连接」。
完。

恢复_突触网格 接受 快照：
    若 快照 等于 空 或者 做 计数 用 快照 等于 0：
        返还。
    完。
    做 突触网格_导入 用 快照。
    做 内核信息 用 「检查点恢复: 突触网格」。
完。

恢复_AI记忆 接受 快照：
    若 快照 等于 空 或者 做 计数 用 快照 等于 0：
        返还。
    完。
    做 记忆_导入全部 用 快照。
    做 内核信息 用 「检查点恢复: AI记忆」。
完。

恢复_注意力 接受 快照：
    若 快照 等于 空 或者 做 计数 用 快照 等于 0：
        返还。
    完。
    做 注意力_导入状态 用 快照。
完。

恢复_运行队列 接受 快照：
    若 快照 等于 空 或者 做 计数 用 快照 等于 0：
        返还。
    完。
    做 调度_清空运行队列。
    可变 i 为 0。
    只要 i 小于 做 计数 用 快照：
        条目 为 快照 的 第 i 项。
        做 调度_插入任务 用 条目 的 第 0 项、条目 的 第 1 项、条目 的 第 2 项。
        i 为 i 加 1。
    完。
    做 内核信息 用 「检查点恢复: 运行队列 (」 加 做 整数转字符串 用 做 计数 用 快照 加 「 任务)」。
完。

恢复_伙伴系统 接受 快照：
    若 快照 等于 空 或者 做 计数 用 快照 等于 0：
        返还。
    完。
    可变 z 为 0。
    只要 z 小于 做 计数 用 快照 并且 z 小于 4：
        做 伙伴_恢复空闲计数 用 z、快照 的 第 z 项。
        z 为 z 加 1。
    完。
    做 内核信息 用 「检查点恢复: 伙伴系统空闲计数」。
完。

恢复_SLUB 接受 快照：
    若 快照 等于 空 或者 做 计数 用 快照 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 快照 并且 i 小于 做 计数 用 SLUB_缓存列表：
        快 为 快照 的 第 i 项。
        现 为 SLUB_缓存列表 的 第 i 项。
        若 现 的 对象大小 不等于 快 的 第 1 项：
            做 内核警告 用 「检查点: SLUB缓存'」 加 快 的 第 0 项 加 「'对象大小不匹配!」。
        完。
        i 为 i 加 1。
    完。
    做 内核信息 用 「检查点恢复: SLUB验证完成」。
完。

恢复_进程表 接受 快照：
    若 快照 等于 空 或者 做 计数 用 快照 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 快照：
        副本 为 快照 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 进程表：
            若 进程表 的 第 j 项 的 pid 等于 副本 的 pid：
                进程表 的 第 j 项 的 状态 为 副本 的 状态。
                进程表 的 第 j 项 的 信号挂起 为 副本 的 信号挂起。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    做 内核信息 用 「检查点恢复: 进程表 (」 加 做 整数转字符串 用 做 计数 用 快照 加 「 进程)」。
完。

恢复_PID位图 接受 快照：
    若 快照 等于 空：
        返还。
    完。
    PID位图 为 快照。
    做 内核信息 用 「检查点恢复: PID位图」。
完。

恢复_挂载表 接受 快照：
    若 快照 等于 空：
        返还。
    完。
    挂载表 为 快照。
    做 内核信息 用 「检查点恢复: 挂载表 (」 加 做 整数转字符串 用 做 计数 用 快照 加 「 挂载点)」。
完。

恢复_打开文件 接受 快照：
    若 快照 等于 空 或者 做 计数 用 快照 等于 0：
        返还。
    完。
    可变 恢复数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 快照：
        条目 为 快照 的 第 i 项。
        结果 为 做 VFS_重新打开 用 条目 的 第 0 项、条目 的 第 1 项、条目 的 第 2 项、条目 的 第 3 项、条目 的 第 4 项。
        若 结果 大于或等于 0：
            恢复数 为 恢复数 加 1。
        完。
        i 为 i 加 1。
    完。
    做 内核信息 用 「检查点恢复: 打开文件 (」 加 做 整数转字符串 用 恢复数 加 「/」 加 做 整数转字符串 用 做 计数 用 快照 加 「)」。
完。

恢复_socket表 接受 快照：
    若 快照 等于 空 或者 做 计数 用 快照 等于 0：
        返还。
    完。
    可变 重建数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 快照：
        条目 为 快照 的 第 i 项。
        若 条目 的 第 3 项 等于 10：
            做 socket_重建监听 用 条目 的 第 0 项、条目 的 第 1 项。
            重建数 为 重建数 加 1。
        完。
        i 为 i 加 1。
    完。
    做 内核信息 用 「检查点恢复: socket (」 加 做 整数转字符串 用 重建数 加 「 监听重建)」。
完。

恢复_路由表 接受 快照：
    若 快照 等于 空：
        返还。
    完。
    做 路由_导入全部 用 快照。
完。

恢复_定时器 接受 快照：
    若 快照 等于 空 或者 做 计数 用 快照 等于 0：
        返还。
    完。
    做 定时器_清空。
    可变 i 为 0。
    只要 i 小于 做 计数 用 快照：
        t 为 快照 的 第 i 项。
        做 定时器_重建 用 t 的 第 0 项、t 的 第 1 项、t 的 第 2 项。
        i 为 i 加 1。
    完。
    做 内核信息 用 「检查点恢复: 定时器 (」 加 做 整数转字符串 用 做 计数 用 快照 加 「 个)」。
完。

本章 收束。
