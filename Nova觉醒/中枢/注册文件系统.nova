《注册文件系统》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·推理·通信渠道》。

引用《..·感知·内核直连层》。

引用《.·内核打印》。

引用《..·推理·定点Q算术》。

引用《.·内存初始化》。

引用《.·每处理器》。

调用号_读取 为 0。

调用号_写入 为 1。

调用号_打开 为 2。

调用号_关闭 为 3。

调用号_状态 为 4。

调用号_文件状态 为 5。

调用号_定位 为 8。

调用号_管道 为 22。

调用号_复制描述 为 32。

调用号_同步 为 74。

调用号_截断 为 76。

调用号_读目录 为 78。

调用号_重命名 为 82。

调用号_创建目录 为 83。

调用号_删除目录 为 84。

调用号_链接 为 86。

调用号_删除文件 为 87。

调用号_符号链接 为 88。

调用号_读链接 为 89。

调用号_改权限 为 90。

调用号_改属主 为 92。

最大文件调用 为 100。

权限_无 为 0。

权限_读 为 1。

权限_写 为 2。

权限_管理 为 4。

权限_ROOT 为 8。

审计_无 为 0。

审计_基本 为 1。

审计_详细 为 2。

审计_完整 为 3。

可变 文件调用表 为 0。

可变 调用计数表 为 0。

可变 调用权限表 为 0。

可变 调用审计级表 为 0。

可变 调用限速表 为 0。

可变 调用上次时间表 为 0。

可变 调用错误表 为 0。

可变 总调用次数 为 0。

可变 总错误次数 为 0。

可变 权限拒绝次数 为 0。

可变 参数错误次数 为 0。

可变 限速拒绝次数 为 0。

可变 审计记录数 为 0。

可变 AI异常检测次数 为 0。

可变 审计环形缓冲 为 0。

可变 审计缓冲大小 为 256。

可变 审计写指针 为 0。

注册文件系统调用 接受 零参：
    文件调用表 为 做 堆_分配 用 最大文件调用 乘 8。
    调用计数表 为 做 堆_分配 用 最大文件调用 乘 4。
    调用权限表 为 做 堆_分配 用 最大文件调用 乘 4。
    调用审计级表 为 做 堆_分配 用 最大文件调用 乘 4。
    调用限速表 为 做 堆_分配 用 最大文件调用 乘 4。
    调用上次时间表 为 做 堆_分配 用 最大文件调用 乘 8。
    调用错误表 为 做 堆_分配 用 最大文件调用 乘 4。
    审计环形缓冲 为 做 堆_分配 用 审计缓冲大小 乘 32。
    可变 序号 为 0。
    只要 序号 小于 最大文件调用：
        做 内存_写64 用 文件调用表 加 序号 乘 8、0。
        做 内存_写32 用 调用计数表 加 序号 乘 4、0。
        做 内存_写32 用 调用权限表 加 序号 乘 4、权限_无。
        做 内存_写32 用 调用审计级表 加 序号 乘 4、审计_基本。
        做 内存_写32 用 调用限速表 加 序号 乘 4、0。
        做 内存_写64 用 调用上次时间表 加 序号 乘 8、0。
        做 内存_写32 用 调用错误表 加 序号 乘 4、0。
        序号 为 序号 加 1。
    完。
    做 文件调用_注册完整 用 调用号_打开、系统_打开文件、权限_读、审计_详细。
    做 文件调用_注册完整 用 调用号_关闭、系统_关闭文件、权限_无、审计_基本。
    做 文件调用_注册完整 用 调用号_读取、系统_读取文件、权限_读、审计_基本。
    做 文件调用_注册完整 用 调用号_写入、系统_写入文件、权限_写、审计_详细。
    做 文件调用_注册完整 用 调用号_定位、系统_定位文件、权限_读、审计_基本。
    做 文件调用_注册完整 用 调用号_状态、系统_文件状态、权限_读、审计_基本。
    做 文件调用_注册完整 用 调用号_创建目录、系统_创建目录、权限_写、审计_详细。
    做 文件调用_注册完整 用 调用号_删除目录、系统_删除目录、权限_写、审计_完整。
    做 文件调用_注册完整 用 调用号_删除文件、系统_删除文件、权限_写、审计_完整。
    做 文件调用_注册完整 用 调用号_重命名、系统_重命名、权限_写、审计_完整。
    做 文件调用_注册完整 用 调用号_读目录、系统_读目录、权限_读、审计_基本。
    做 文件调用_注册完整 用 调用号_改权限、系统_改权限、权限_管理、审计_完整。
    做 文件调用_注册完整 用 调用号_截断、系统_截断文件、权限_写、审计_详细。
    做 文件调用_注册完整 用 调用号_同步、系统_同步文件、权限_写、审计_基本。
    做 文件调用_注册完整 用 调用号_复制描述、系统_复制描述符、权限_读、审计_基本。
    做 文件调用_注册完整 用 调用号_管道、系统_创建管道、权限_写、审计_详细。
    做 文件调用_注册完整 用 调用号_链接、系统_硬链接、权限_写、审计_完整。
    做 文件调用_注册完整 用 调用号_符号链接、系统_符号链接、权限_写、审计_详细。
    做 文件调用_注册完整 用 调用号_读链接、系统_读链接、权限_读、审计_基本。
    做 内核打印 用 「文件系统调用注册完成, 共注册19个调用\n」。
完。

文件调用_注册 接受 号、处理函数：
    若 号 大于或等于 最大文件调用 或者 文件调用表 等于 0：
        返还。
    完。
    做 内存_写64 用 文件调用表 加 号 乘 8、处理函数。
完。

文件调用_注册完整 接受 号、处理函数、所需权限、审计级：
    若 号 大于或等于 最大文件调用：
        返还。
    完。
    做 内存_写64 用 文件调用表 加 号 乘 8、处理函数。
    做 内存_写32 用 调用权限表 加 号 乘 4、所需权限。
    做 内存_写32 用 调用审计级表 加 号 乘 4、审计级。
完。

文件调用_取消注册 接受 号：
    若 号 大于或等于 最大文件调用 或者 文件调用表 等于 0：
        返还。
    完。
    做 内存_写64 用 文件调用表 加 号 乘 8、0。
    做 内存_写32 用 调用权限表 加 号 乘 4、0。
完。

文件调用_设置限速 接受 号、最小间隔ms：
    若 号 大于或等于 最大文件调用 或者 调用限速表 等于 0：
        返还。
    完。
    做 内存_写32 用 调用限速表 加 号 乘 4、最小间隔ms。
完。

文件调用_确保已初始化 接受 零参：
    若 文件调用表 不等于 0 并且 调用权限表 不等于 0 并且 调用限速表 不等于 0 并且 调用上次时间表 不等于 0 并且 调用错误表 不等于 0：
        返还 0。
    完。
    做 注册文件系统调用。
    若 文件调用表 等于 0 或者 调用权限表 等于 0 或者 调用限速表 等于 0 或者 调用上次时间表 等于 0 或者 调用错误表 等于 0：
        返还 错误_内存不足。
    完。
    返还 0。
完。

文件调用_分发 接受 号、参数一、参数二、参数三：
    总调用次数 为 总调用次数 加 1。
    调用时间 为 做 当前时间_毫秒。
    初始化结果 为 做 文件调用_确保已初始化。
    若 初始化结果 小于 0：
        总错误次数 为 总错误次数 加 1。
        参数错误次数 为 参数错误次数 加 1。
        返还 初始化结果。
    完。
    若 号 大于或等于 最大文件调用 或者 文件调用表 等于 0：
        总错误次数 为 总错误次数 加 1。
        参数错误次数 为 参数错误次数 加 1。
        返还 错误_无效参数。
    完。
    处理 为 做 内存_读64 用 文件调用表 加 号 乘 8。
    若 处理 等于 0：
        总错误次数 为 总错误次数 加 1。
        做 调用错误_增加 用 号。
        返还 0 减 38。
    完。
    权限检查 为 做 文件调用_检查权限 用 号。
    若 权限检查 小于 0：
        权限拒绝次数 为 权限拒绝次数 加 1。
        总错误次数 为 总错误次数 加 1。
        做 审计_记录事件 用 号、0、调用时间、权限检查。
        返还 权限检查。
    完。
    限速检查 为 做 文件调用_检查限速 用 号、调用时间。
    若 限速检查 小于 0：
        限速拒绝次数 为 限速拒绝次数 加 1。
        总错误次数 为 总错误次数 加 1。
        返还 限速检查。
    完。
    做 审计_记录进入 用 号、参数一、调用时间。
    做 内存_写32 用 调用计数表 加 号 乘 4、做 内存_读32 用 调用计数表 加 号 乘 4 加 1。
    做 内存_写64 用 调用上次时间表 加 号 乘 8、调用时间。
    结果 为 做 处理 用 参数一、参数二、参数三。
    做 审计_记录事件 用 号、参数一、调用时间、结果。
    若 结果 小于 0：
        总错误次数 为 总错误次数 加 1。
        做 调用错误_增加 用 号。
    完。
    返还 结果。
完。

文件调用_检查权限 接受 号：
    初始化结果 为 做 文件调用_确保已初始化。
    若 初始化结果 小于 0：
        返还 初始化结果。
    完。
    所需 为 做 内存_读32 用 调用权限表 加 号 乘 4。
    若 所需 等于 权限_无：
        返还 0。
    完。
    当前权限 为 做 获取当前进程权限。
    若 当前权限 位与 权限_ROOT：
        返还 0。
    完。
    若 所需 位与 权限_读 并且 非 (当前权限 位与 权限_读)：
        返还 错误_权限不足。
    完。
    若 所需 位与 权限_写 并且 非 (当前权限 位与 权限_写)：
        返还 错误_权限不足。
    完。
    若 所需 位与 权限_管理 并且 非 (当前权限 位与 权限_管理)：
        返还 错误_权限不足。
    完。
    返还 0。
完。

获取当前进程权限 接受 零参：
    进程 为 做 获取当前进程。
    若 进程 等于 空：
        返还 权限_无。
    完。
    uid 为 进程 的 有效uid。
    若 uid 等于 0：
        返还 权限_ROOT 位或 权限_管理 位或 权限_写 位或 权限_读。
    完。
    返还 进程 的 文件权限位。
完。

文件调用_检查限速 接受 号、当前时间：
    初始化结果 为 做 文件调用_确保已初始化。
    若 初始化结果 小于 0：
        返还 初始化结果。
    完。
    最小间隔 为 做 内存_读32 用 调用限速表 加 号 乘 4。
    若 最小间隔 等于 0：
        返还 0。
    完。
    上次时间 为 做 内存_读64 用 调用上次时间表 加 号 乘 8。
    若 上次时间 大于 0 并且 当前时间 减 上次时间 小于 最小间隔：
        返还 错误_操作过频。
    完。
    返还 0。
完。

审计_记录进入 接受 号、参数、时间：
    若 调用审计级表 等于 0：
        返还。
    完。
    审计级 为 做 内存_读32 用 调用审计级表 加 号 乘 4。
    若 审计级 小于 审计_详细：
        返还。
    完。
    做 审计_写环形 用 号、参数、时间、0。
完。

审计_记录事件 接受 号、参数、时间、结果：
    若 调用审计级表 等于 0：
        返还。
    完。
    审计级 为 做 内存_读32 用 调用审计级表 加 号 乘 4。
    若 审计级 小于 审计_基本：
        返还。
    完。
    若 结果 小于 0 或者 审计级 大于或等于 审计_详细：
        做 审计_写环形 用 号、参数、时间、结果。
    完。
完。

审计_写环形 接受 号、参数、时间、结果：
    若 审计环形缓冲 等于 0：
        返还。
    完。
    偏移 为 审计写指针 乘 32。
    做 内存_写32 用 审计环形缓冲 加 偏移、号。
    做 内存_写64 用 审计环形缓冲 加 偏移 加 4、参数。
    做 内存_写64 用 审计环形缓冲 加 偏移 加 12、时间。
    做 内存_写32 用 审计环形缓冲 加 偏移 加 20、结果。
    进程 为 做 获取当前进程。
    若 进程 不等于 空：
        做 内存_写32 用 审计环形缓冲 加 偏移 加 24、进程 的 pid。
    完。
    审计写指针 为 (审计写指针 加 1) 取余于 审计缓冲大小。
    审计记录数 为 审计记录数 加 1。
完。

审计_读取记录 接受 索引：
    若 审计环形缓冲 等于 0 或者 索引 大于或等于 审计缓冲大小：
        返还 空。
    完。
    偏移 为 索引 乘 32。
    返还 一组：做 内存_读32 用 审计环形缓冲 加 偏移、做 内存_读64 用 审计环形缓冲 加 偏移 加 4、做 内存_读64 用 审计环形缓冲 加 偏移 加 12、做 内存_读32 用 审计环形缓冲 加 偏移 加 20、做 内存_读32 用 审计环形缓冲 加 偏移 加 24。
完。

调用错误_增加 接受 号：
    若 调用错误表 等于 0 或者 号 大于或等于 最大文件调用：
        返还。
    完。
    做 内存_写32 用 调用错误表 加 号 乘 4、做 内存_读32 用 调用错误表 加 号 乘 4 加 1。
完。

调用错误_获取 接受 号：
    若 调用错误表 等于 0 或者 号 大于或等于 最大文件调用：
        返还 0。
    完。
    返还 做 内存_读32 用 调用错误表 加 号 乘 4。
完。

文件调用_获取计数 接受 号：
    若 号 大于或等于 最大文件调用 或者 调用计数表 等于 0：
        返还 0。
    完。
    返还 做 内存_读32 用 调用计数表 加 号 乘 4。
完。

文件调用_获取错误率 接受 号：
    总数 为 做 文件调用_获取计数 用 号。
    若 总数 等于 0：
        返还 0。
    完。
    错误数 为 做 调用错误_获取 用 号。
    返还 错误数 乘 10000 除以 总数。
完。

文件调用_AI异常检测 接受 零参：
    AI异常检测次数 为 AI异常检测次数 加 1。
    bus 为 全局AI总线。
    若 bus 等于 空：
        返还 做 文件调用_启发式异常检测。
    完。
    特征 为 做 Tensor_创建 用 16。
    若 特征 等于 空：
        返还 做 文件调用_启发式异常检测。
    完。
    做 Tensor_清零 用 特征、16。
    做 Tensor_写 用 特征、0、总调用次数。
    做 Tensor_写 用 特征、1、总错误次数。
    做 Tensor_写 用 特征、2、权限拒绝次数。
    做 Tensor_写 用 特征、3、限速拒绝次数。
    做 Tensor_写 用 特征、4、做 文件调用_获取计数 用 调用号_打开。
    做 Tensor_写 用 特征、5、做 文件调用_获取计数 用 调用号_读取。
    做 Tensor_写 用 特征、6、做 文件调用_获取计数 用 调用号_写入。
    做 Tensor_写 用 特征、7、做 文件调用_获取计数 用 调用号_删除文件。
    做 Tensor_写 用 特征、8、做 文件调用_获取错误率 用 调用号_打开。
    做 Tensor_写 用 特征、9、做 文件调用_获取错误率 用 调用号_写入。
    异常分 为 做 AI总线_异常检测 用 bus、「fs_syscall_anomaly」、特征。
    做 Tensor_释放 用 特征。
    若 异常分 大于或等于 7000：
        返还 2。
    完。
    若 异常分 大于或等于 4000：
        返还 1。
    完。
    返还 0。
完。

文件调用_启发式异常检测 接受 零参：
    可变 异常计 为 0。
    若 总调用次数 大于 100：
        错误率 为 总错误次数 乘 100 除以 总调用次数。
        若 错误率 大于 30：
            异常计 为 异常计 加 2。
        否则若 错误率 大于 15：
            异常计 为 异常计 加 1。
        完。
    完。
    若 权限拒绝次数 大于 50：
        异常计 为 异常计 加 1。
    完。
    若 做 文件调用_获取计数 用 调用号_删除文件 大于 200：
        异常计 为 异常计 加 1。
    完。
    若 异常计 大于或等于 3：
        返还 2。
    完。
    若 异常计 大于或等于 1：
        返还 1。
    完。
    返还 0。
完。

文件系统调用_完整统计 接受 零参：
    返还 一组：总调用次数、总错误次数、权限拒绝次数、参数错误次数、限速拒绝次数、审计记录数、AI异常检测次数、做 文件调用_获取计数 用 调用号_打开、做 文件调用_获取计数 用 调用号_读取、做 文件调用_获取计数 用 调用号_写入、做 文件调用_获取计数 用 调用号_关闭、做 文件调用_获取计数 用 调用号_删除文件。
完。

文件调用_打印诊断 接受 零参：
    做 内核打印 用 「═══ 文件系统调用诊断 ═══\n」。
    做 内核打印 用 「总调用=」、总调用次数、「 总错误=」、总错误次数、「\n」。
    做 内核打印 用 「权限拒绝=」、权限拒绝次数、「 参数错误=」、参数错误次数、「 限速拒绝=」、限速拒绝次数、「\n」。
    做 内核打印 用 「审计记录=」、审计记录数、「 AI检测=」、AI异常检测次数、「\n」。
    做 内核打印 用 「打开=」、做 文件调用_获取计数 用 调用号_打开、「 读=」、做 文件调用_获取计数 用 调用号_读取、「\n」。
    做 内核打印 用 「写=」、做 文件调用_获取计数 用 调用号_写入、「 关闭=」、做 文件调用_获取计数 用 调用号_关闭、「\n」。
    做 内核打印 用 「删除=」、做 文件调用_获取计数 用 调用号_删除文件、「 重命名=」、做 文件调用_获取计数 用 调用号_重命名、「\n」。
完。

可变 调用热点_最大号 为 0。

可变 调用热点_最大次数 为 0。

文件调用_热点追踪 接受 零参：
    若 调用计数表 等于 0：
        返还。
    完。
    调用热点_最大号 为 0。
    调用热点_最大次数 为 0。
    可变 i 为 0。
    只要 i 小于 最大文件调用：
        c 为 做 文件调用_获取计数 用 i。
        若 c 大于 调用热点_最大次数：
            调用热点_最大号 为 i。
            调用热点_最大次数 为 c。
        完。
        i 为 i 加 1。
    完。
完。

文件调用_频率检查 接受 号、阈值：
    c 为 做 文件调用_获取计数 用 号。
    若 c 大于 阈值：
        做 内核警告 用 「文件调用 %d 频率异常: %d > %d\n」、号、c、阈值。
        返还 真。
    完。
    返还 假。
完。

文件调用_重置计数 接受 零参：
    若 调用计数表 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 最大文件调用：
        做 内存_写32 用 调用计数表 加 i 乘 4、0。
        i 为 i 加 1。
    完。
    总调用次数 为 0。
    总错误次数 为 0。
    权限拒绝次数 为 0。
    参数错误次数 为 0。
    限速拒绝次数 为 0。
    AI异常检测次数 为 0。
完。

文件调用_名称 接受 号：
    若 号 等于 调用号_打开：
        返还 「打开」。
    完。
    若 号 等于 调用号_读取：
        返还 「读取」。
    完。
    若 号 等于 调用号_写入：
        返还 「写入」。
    完。
    若 号 等于 调用号_关闭：
        返还 「关闭」。
    完。
    若 号 等于 调用号_删除文件：
        返还 「删除」。
    完。
    若 号 等于 调用号_重命名：
        返还 「重命名」。
    完。
    返还 「未知」。
完。

文件调用_获取热点信息 接受 零参：
    做 文件调用_热点追踪。
    返还 一组：调用热点_最大号、调用热点_最大次数、做 文件调用_名称 用 调用热点_最大号。
完。

注册文件系统_统计 接受 零参：
    返还 一本字典：总调用次数 对应 总调用次数、总错误次数 对应 总错误次数、权限拒绝次数 对应 权限拒绝次数、参数错误次数 对应 参数错误次数、限速拒绝次数 对应 限速拒绝次数、AI异常检测次数 对应 AI异常检测次数、调用热点最大次数 对应 调用热点_最大次数。
完。

注册文件系统_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 总错误次数 大于 100：
        做 追加 用 问题、「总错误次数=」 加 总错误次数。
    完。
    若 参数错误次数 大于 100：
        做 追加 用 问题、「参数错误次数=」 加 参数错误次数。
    完。
    返还 做 计数 用 问题 等于 0。
完。

注册文件系统_综合报告 接受 零参：
    做 打印 用 「[注册文件系统] ═══ 综合报告 ═══」。
    做 打印 用 「  总调用次数: 」 加 总调用次数。
    做 打印 用 「  总错误次数: 」 加 总错误次数。
    做 打印 用 「  权限拒绝次数: 」 加 权限拒绝次数。
    做 打印 用 「  参数错误次数: 」 加 参数错误次数。
    做 打印 用 「  限速拒绝次数: 」 加 限速拒绝次数。
    做 打印 用 「  AI异常检测次数: 」 加 AI异常检测次数。
    做 打印 用 「  调用热点_最大次数: 」 加 调用热点_最大次数。
    做 打印 用 「  健康: 」 加 若 做 注册文件系统_健康评估 则 「正常」 否则 「异常」。
完。

注册文件系统_全局重置 接受 零参：
    总调用次数 为 0。
    总错误次数 为 0。
    权限拒绝次数 为 0。
    参数错误次数 为 0。
    限速拒绝次数 为 0。
    AI异常检测次数 为 0。
    调用热点_最大次数 为 0。
    做 打印 用 「[注册文件系统] 统计已重置」。
    返还 0。
完。

本章 收束。
