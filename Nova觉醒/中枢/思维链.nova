《思维链》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

CoT_最大步数 为 16。

CoT推理链 是 一种 事物，含 链ID、问题、步骤、中间结果、最终结论、置信度。

可变 CoT_下一个链ID 为 1。

可变 CoT_总链数 为 0。

CoT_开始推理 接受 问题：
    链 为 新建 CoT推理链。
    链 的 链ID 为 CoT_下一个链ID。
    CoT_下一个链ID 为 CoT_下一个链ID 加 1。
    链 的 问题 为 问题。
    链 的 步骤 为 一组：（空）。
    链 的 中间结果 为 一组：（空）。
    链 的 置信度 为 0。
    CoT_总链数 为 CoT_总链数 加 1。
    返还 链。
完。

CoT_添加步骤 接受 链、推理步骤、中间结论：
    若 链 等于 空 或者 做 计数 用 链 的 步骤 大于或等于 CoT_最大步数：
        返还。
    完。
    做 追加 用 链 的 步骤、推理步骤。
    做 追加 用 链 的 中间结果、中间结论。
完。

CoT_得出结论 接受 链、结论、置信度：
    若 链 等于 空：
        返还。
    完。
    链 的 最终结论 为 结论。
    链 的 置信度 为 置信度。
完。

CoT_回溯 接受 链、回到步骤：
    若 链 等于 空 或者 回到步骤 大于或等于 做 计数 用 链 的 步骤：
        返还。
    完。
    只要 做 计数 用 链 的 步骤 大于 回到步骤：
        做 删除 用 链 的 步骤、做 计数 用 链 的 步骤 减 1。
        做 删除 用 链 的 中间结果、做 计数 用 链 的 中间结果 减 1。
    完。
    链 的 最终结论 为 「」。
    链 的 置信度 为 0。
完。

智能体最大数 为 16。

智能体信息 是 一种 事物，含 智能体ID、名称、角色、能力列表、忙碌、任务数、成功数。

智能体消息 是 一种 事物，含 发送者ID、接收者ID、类型、内容、时间戳。

可变 智能体列表 为 一组：（空）。

可变 智能体消息队列 为 一组：（空）。

可变 智能体_下一个ID 为 1。

智能体_注册 接受 名称、角色、能力列表：
    若 做 计数 用 智能体列表 大于或等于 智能体最大数：
        返还 负 1。
    完。
    智能体 为 新建 智能体信息。
    智能体 的 智能体ID 为 智能体_下一个ID。
    智能体_下一个ID 为 智能体_下一个ID 加 1。
    智能体 的 名称 为 名称。
    智能体 的 角色 为 角色。
    智能体 的 能力列表 为 能力列表。
    智能体 的 忙碌 为 假。
    智能体 的 任务数 为 0。
    智能体 的 成功数 为 0。
    做 追加 用 智能体列表、智能体。
    做 内核信息 用 「多智能体: 注册 [」 加 角色 加 「] 」 加 名称 加 「 能力=」 加 做 整数转字符串 用 做 计数 用 能力列表。
    返还 智能体 的 智能体ID。
完。

智能体_发送消息 接受 发送者ID、接收者ID、类型、内容：
    Nova推理执行_时钟 为 Nova推理执行_时钟 加 1。
    msg 为 新建 智能体消息。
    msg 的 发送者ID 为 发送者ID。
    msg 的 接收者ID 为 接收者ID。
    msg 的 类型 为 类型。
    msg 的 内容 为 内容。
    msg 的 时间戳 为 Nova推理执行_时钟。
    做 追加 用 智能体消息队列、msg。
完。

智能体_接收消息 接受 接收者ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 智能体消息队列：
        若 智能体消息队列 的 第 i 项 的 接收者ID 等于 接收者ID：
            msg 为 智能体消息队列 的 第 i 项。
            做 删除 用 智能体消息队列、i。
            返还 msg。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

智能体_委托任务 接受 规划者ID、任务描述：
    可变 最佳 为 负 1。
    可变 最少任务 为 999999。
    可变 i 为 0。
    只要 i 小于 做 计数 用 智能体列表：
        若 智能体列表 的 第 i 项 的 角色 等于 「executor」 并且 非 智能体列表 的 第 i 项 的 忙碌：
            若 智能体列表 的 第 i 项 的 任务数 小于 最少任务：
                最少任务 为 智能体列表 的 第 i 项 的 任务数。
                最佳 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最佳 大于或等于 0：
        智能体列表 的 第 最佳 项 的 任务数 为 智能体列表 的 第 最佳 项 的 任务数 加 1。
        智能体列表 的 第 最佳 项 的 忙碌 为 真。
        做 智能体_发送消息 用 规划者ID、智能体列表 的 第 最佳 项 的 智能体ID、0、任务描述。
        返还 智能体列表 的 第 最佳 项 的 智能体ID。
    完。
    返还 负 1。
完。

智能体_广播 接受 发送者ID、内容：
    可变 i 为 0。
    只要 i 小于 做 计数 用 智能体列表：
        若 智能体列表 的 第 i 项 的 智能体ID 不等于 发送者ID：
            做 智能体_发送消息 用 发送者ID、智能体列表 的 第 i 项 的 智能体ID、2、内容。
        完。
        i 为 i 加 1。
    完。
完。

本章 收束。
