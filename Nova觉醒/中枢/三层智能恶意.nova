《三层智能恶意》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·同步原语》。

引用《.·进程调度》。

引用《.·中断管理系统》。

引用《.·进程信号》。

引用《.·公共智能》。

引用《..·外联·TCP会话记录》。

引用《..·引导·引导加载器》。

引用《.·一层文件扫描》。

引用《.·时钟中枢》。

引用《..·感知·内核直连层》。

引用《..·记忆·VFS读写同步》。

引用《.·字符串替换》。

引用《.·整数转字符串》。

AI_L3_输入维度 为 16。

AI_L3_隐层维度 为 24。

AI_L3_输出维度 为 1。

NGRAM_大小 为 3。

恶意模式_最大 为 64。

完整性_最大文件 为 256。

完整性_检查间隔_毫秒 为 60000。

Rootkit_检查间隔_毫秒 为 30000。

可变 AI_L3_权重1 为 一组：（空）。

可变 AI_L3_偏置1 为 一组：（空）。

可变 AI_L3_权重2 为 一组：（空）。

可变 AI_L3_偏置2 为 一组：（空）。

可变 AI_L3_已初始化 为 假。

可变 AI_L3_总评估数 为 0。

可变 AI_L3_高分数 为 0。

可变 AI_L3_在线学习样本 为 一组：（空）。

可变 AI_L3_误报反馈数 为 0。

可变 AI_L3_漏报反馈数 为 0。

恶意模式 是 一种 事物，含 名称、序列、危险权重、描述。

可变 恶意模式库 为 一组：（空）。

恶意模式_初始化 接受 零参：
    恶意模式库 为 一组：（空）。
    做 追加 用 恶意模式库、新建 恶意模式：名称 为 「勒索加密」、序列 为 一组：2、1、82、危险权重 为 400、描述 为 「open→write→rename循环」。
    做 追加 用 恶意模式库、新建 恶意模式：名称 为 「反弹Shell」、序列 为 一组：41、42、33、危险权重 为 450、描述 为 「socket→connect→dup2」。
    做 追加 用 恶意模式库、新建 恶意模式：名称 为 「提权执行」、序列 为 一组：57、105、59、危险权重 为 350、描述 为 「fork→setuid→execve」。
    做 追加 用 恶意模式库、新建 恶意模式：名称 为 「进程注入」、序列 为 一组：101、9、1、危险权重 为 400、描述 为 「ptrace→mmap→write」。
    做 追加 用 恶意模式库、新建 恶意模式：名称 为 「批量删除」、序列 为 一组：87、87、87、危险权重 为 300、描述 为 「连续unlink」。
    做 追加 用 恶意模式库、新建 恶意模式：名称 为 「隐藏操作」、序列 为 一组：82、2、1、危险权重 为 150、描述 为 「rename→open→write」。
    做 追加 用 恶意模式库、新建 恶意模式：名称 为 「密钥窃取」、序列 为 一组：2、0、42、危险权重 为 350、描述 为 「open→read→connect」。
    做 追加 用 恶意模式库、新建 恶意模式：名称 为 「挖矿启动」、序列 为 一组：9、56、203、危险权重 为 300、描述 为 「mmap→clone→affinity」。
完。

AI_L3_初始化 接受 零参：
    AI_L3_权重1 为 一组：（空）。
    AI_L3_偏置1 为 一组：（空）。
    AI_L3_权重2 为 一组：（空）。
    AI_L3_偏置2 为 一组：（空）。
    可变 seed 为 1337。
    可变 i 为 0。
    只要 i 小于 AI_L3_输入维度 乘 AI_L3_隐层维度：
        seed 为 seed 乘 6364136223846793005 加 1442695040888963407 位与 0x7FFFFFFF。
        做 追加 用 AI_L3_权重1、seed 取余于 200 减 100。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 AI_L3_隐层维度：
        做 追加 用 AI_L3_偏置1、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 AI_L3_隐层维度 乘 AI_L3_输出维度：
        seed 为 seed 乘 6364136223846793005 加 1442695040888963407 位与 0x7FFFFFFF。
        做 追加 用 AI_L3_权重2、seed 取余于 200 减 100。
        i 为 i 加 1。
    完。
    做 追加 用 AI_L3_偏置2、0。
    做 恶意模式_初始化。
    AI_L3_已初始化 为 真。
完。

智能_评估恶意概率 接受 档案、可疑标志：
    若 非 AI_L3_已初始化：
        做 AI_L3_初始化。
    完。
    AI_L3_总评估数 为 AI_L3_总评估数 加 1。
    可变 规则分数 为 0。
    若 可疑标志 位与 可疑_批量删除：
        规则分数 为 规则分数 加 400。
    完。
    若 可疑标志 位与 可疑_加密大量文件：
        规则分数 为 规则分数 加 500。
    完。
    若 可疑标志 位与 可疑_提权尝试：
        规则分数 为 规则分数 加 200。
    完。
    若 可疑标志 位与 可疑_修改系统文件：
        规则分数 为 规则分数 加 150。
    完。
    若 可疑标志 位与 可疑_网络外连：
        规则分数 为 规则分数 加 100。
    完。
    若 可疑标志 位与 可疑_注入代码：
        规则分数 为 规则分数 加 300。
    完。
    若 可疑标志 位与 可疑_隐藏进程：
        规则分数 为 规则分数 加 250。
    完。
    若 可疑标志 位与 可疑_挖矿特征：
        规则分数 为 规则分数 加 350。
    完。
    模式分数 为 做 恶意模式_匹配 用 档案 的 系统调用序列。
    特征 为 做 AI_L3_提取特征 用 档案、可疑标志、规则分数、模式分数。
    AI分数 为 做 AI_L3_前向传播 用 特征。
    综合分数 为 规则分数 乘 40 除以 100 加 模式分数 乘 30 除以 100 加 AI分数 乘 30 除以 100。
    若 做 计数 用 档案 的 系统调用序列 大于或等于 100：
        窗口时间 为 做 读取毫秒 减 档案 的 创建时间。
        若 窗口时间 大于 0 并且 窗口时间 小于 1000：
            综合分数 为 综合分数 加 100。
        完。
    完。
    若 综合分数 大于 1000：
        综合分数 为 1000。
    完。
    若 综合分数 大于 800：
        AI_L3_高分数 为 AI_L3_高分数 加 1。
    完。
    做 AI_L3_记录样本 用 档案 的 PID、可疑标志、综合分数、特征。
    返还 综合分数。
完。

AI_L3_提取特征 接受 档案、可疑标志、规则分数、模式分数：
    返还 一组：规则分数 除以 10、模式分数 除以 10、做 计数 用 档案 的 系统调用序列 除以 10、做 计数 用 档案 的 文件操作 除以 5、做 计数 用 档案 的 网络连接 乘 10、档案 的 权限变更 乘 100、(可疑标志 位与 0x01) 乘 100、(可疑标志 位与 0x02) 乘 50、(可疑标志 位与 0x04) 乘 100、(可疑标志 位与 0x08) 乘 50、(可疑标志 位与 0x10) 乘 100、(可疑标志 位与 0x20) 乘 80、(可疑标志 位与 0x40) 乘 120、(可疑标志 位与 0x80) 乘 90、档案 的 异常分数 除以 10、(做 读取毫秒 减 档案 的 创建时间) 除以 1000。
完。

AI_L3_前向传播 接受 特征：
    隐层 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI_L3_隐层维度：
        可变 和 为 AI_L3_偏置1 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 AI_L3_输入维度：
            和 为 和 加 特征 的 第 j 项 乘 AI_L3_权重1 的 第 i 乘 AI_L3_输入维度 加 j 项 除以 100。
            j 为 j 加 1。
        完。
        若 和 小于 0：
            和 为 0。
        完。
        做 追加 用 隐层、和。
        i 为 i 加 1。
    完。
    可变 输出 为 AI_L3_偏置2 的 第 0 项。
    i 为 0。
    只要 i 小于 AI_L3_隐层维度：
        输出 为 输出 加 隐层 的 第 i 项 乘 AI_L3_权重2 的 第 i 项 除以 100。
        i 为 i 加 1。
    完。
    若 输出 大于 500：
        返还 1000。
    完。
    若 输出 小于 负 500：
        返还 0。
    完。
    返还 500 加 输出。
完。

恶意模式_匹配 接受 系统调用序列：
    若 做 计数 用 系统调用序列 小于 NGRAM_大小：
        返还 0。
    完。
    可变 最高分 为 0。
    可变 p 为 0。
    只要 p 小于 做 计数 用 恶意模式库：
        模式 为 恶意模式库 的 第 p 项。
        模式长 为 做 计数 用 模式 的 序列。
        可变 命中 为 0。
        可变 i 为 0。
        只要 i 加 模式长 小于或等于 做 计数 用 系统调用序列：
            可变 匹配 为 真。
            可变 j 为 0。
            只要 j 小于 模式长：
                若 系统调用序列 的 第 i 加 j 项 不等于 模式 的 序列 的 第 j 项：
                    匹配 为 假。
                    跳出。
                完。
                j 为 j 加 1。
            完。
            若 匹配：
                命中 为 命中 加 1。
            完。
            i 为 i 加 1。
        完。
        若 命中 大于 0：
            分数 为 模式 的 危险权重 乘 命中。
            若 分数 大于 最高分：
                最高分 为 分数。
            完。
        完。
        p 为 p 加 1。
    完。
    若 最高分 大于 1000：
        最高分 为 1000。
    完。
    返还 最高分。
完。

学习样本 是 一种 事物，含 PID、可疑标志、评估分数、特征、标签、时间戳。

AI_L3_记录样本 接受 PID、可疑标志、分数、特征：
    做 追加 用 AI_L3_在线学习样本、新建 学习样本：PID 为 PID、可疑标志 为 可疑标志、评估分数 为 分数、特征 为 特征、标签 为 负 1、时间戳 为 做 读取毫秒。
    若 做 计数 用 AI_L3_在线学习样本 大于 1024：
        AI_L3_在线学习样本 为 做 子列表 用 AI_L3_在线学习样本、512、1024。
    完。
完。

AI_L3_反馈_误报 接受 PID：
    AI_L3_误报反馈数 为 AI_L3_误报反馈数 加 1。
    可变 i 为 做 计数 用 AI_L3_在线学习样本 减 1。
    只要 i 大于或等于 0：
        若 AI_L3_在线学习样本 的 第 i 项 的 PID 等于 PID：
            AI_L3_在线学习样本 的 第 i 项 的 标签 为 0。
            返还。
        完。
        i 为 i 减 1。
    完。
完。

AI_L3_反馈_漏报 接受 PID：
    AI_L3_漏报反馈数 为 AI_L3_漏报反馈数 加 1。
    可变 i 为 做 计数 用 AI_L3_在线学习样本 减 1。
    只要 i 大于或等于 0：
        若 AI_L3_在线学习样本 的 第 i 项 的 PID 等于 PID：
            AI_L3_在线学习样本 的 第 i 项 的 标签 为 1。
            返还。
        完。
        i 为 i 减 1。
    完。
完。

AI_L3_在线训练 接受 零参：
    可变 训练数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_L3_在线学习样本：
        样本 为 AI_L3_在线学习样本 的 第 i 项。
        若 样本 的 标签 大于或等于 0：
            预测 为 做 AI_L3_前向传播 用 样本 的 特征。
            目标 为 样本 的 标签 乘 1000。
            误差 为 目标 减 预测。
            学习率 为 1。
            可变 j 为 0。
            只要 j 小于 AI_L3_隐层维度：
                AI_L3_权重2 的 第 j 项 为 AI_L3_权重2 的 第 j 项 加 误差 乘 学习率 除以 1000。
                j 为 j 加 1。
            完。
            训练数 为 训练数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 训练数 大于 0：
        做 内核信息 用 「[AI-L3] 在线训练: 」 加 做 整数转字符串 用 训练数 加 「个样本」。
    完。
完。

完整性条目 是 一种 事物，含 路径、哈希、大小、权限、建立时间、上次检查时间、变更次数。

可变 完整性_基线 为 一组：（空）。

可变 完整性_违规 为 一组：（空）。

可变 完整性_上次检查 为 0。

完整性_建立基线 接受 零参：
    完整性_基线 为 一组：（空）。
    完整性_违规 为 一组：（空）。
    做 完整性_添加 用 「/boot/nova-kernel」。
    做 完整性_添加 用 「/boot/boot.cfg」。
    做 完整性_添加 用 「/etc/nova/ai.conf」。
    做 完整性_添加 用 「/etc/passwd」。
    做 完整性_添加 用 「/etc/shadow」。
    做 完整性_添加 用 「/etc/fstab」。
    做 完整性_添加 用 「/etc/hosts」。
    做 完整性_添加 用 「/usr/lib/libnova.so」。
    做 完整性_添加 用 「/usr/lib/libcrypto.so」。
    做 完整性_添加 用 「/var/nova/模型/安全模型.bin」。
    完整性_上次检查 为 做 读取毫秒。
    做 内核信息 用 「[完整性] 基线建立: 」 加 做 整数转字符串 用 做 计数 用 完整性_基线 加 「个文件」。
完。

完整性_添加 接受 路径：
    哈希 为 做 计算文件哈希 用 路径。
    大小 为 做 获取文件大小_路径 用 路径。
    做 追加 用 完整性_基线、新建 完整性条目：路径 为 路径、哈希 为 哈希、大小 为 大小、权限 为 做 获取文件权限 用 路径、建立时间 为 做 读取毫秒、上次检查时间 为 做 读取毫秒、变更次数 为 0。
完。

完整性_检查 接受 零参：
    可变 违规数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 完整性_基线：
        条目 为 完整性_基线 的 第 i 项。
        当前哈希 为 做 计算文件哈希 用 条目 的 路径。
        当前大小 为 做 获取文件大小_路径 用 条目 的 路径。
        可变 变更类型 为 「」。
        若 当前哈希 不等于 条目 的 哈希：
            变更类型 为 「内容变更」。
        否则若 当前大小 不等于 条目 的 大小：
            变更类型 为 「大小变更」。
        否则若 当前哈希 等于 0 并且 条目 的 哈希 不等于 0：
            变更类型 为 「文件丢失」。
        完。
        若 变更类型 不等于 「」：
            违规数 为 违规数 加 1。
            条目 的 变更次数 为 条目 的 变更次数 加 1。
            做 追加 用 完整性_违规、一本字典：路径 对应 条目 的 路径、类型 对应 变更类型、时间 对应 做 读取毫秒、旧哈希 对应 条目 的 哈希、新哈希 对应 当前哈希。
            做 内核警告 用 「[完整性] 」 加 变更类型 加 「: 」 加 条目 的 路径。
        完。
        条目 的 上次检查时间 为 做 读取毫秒。
        i 为 i 加 1。
    完。
    完整性_上次检查 为 做 读取毫秒。
    返还 违规数。
完。

完整性_定期检查 接受 零参：
    现在 为 做 读取毫秒。
    若 现在 减 完整性_上次检查 小于 完整性_检查间隔_毫秒：
        返还。
    完。
    做 完整性_检查。
完。

可变 系统调用表_基线哈希 为 0。

可变 IDT_基线哈希 为 0。

可变 Rootkit_上次检查 为 0。

可变 Rootkit_检测数 为 0。

Rootkit_建立基线 接受 零参：
    系统调用表_基线哈希 为 做 计算内存区域哈希 用 做 获取系统调用表地址、512 乘 8。
    IDT_基线哈希 为 做 计算内存区域哈希 用 做 获取IDT地址、256 乘 16。
    Rootkit_上次检查 为 做 读取毫秒。
    做 内核信息 用 「[Rootkit] 基线建立: 系统调用表哈希=」 加 做 整数转字符串 用 系统调用表_基线哈希。
完。

Rootkit_检查 接受 零参：
    现在 为 做 读取毫秒。
    若 现在 减 Rootkit_上次检查 小于 Rootkit_检查间隔_毫秒：
        返还。
    完。
    Rootkit_上次检查 为 现在。
    可变 异常 为 假。
    当前系统调用哈希 为 做 计算内存区域哈希 用 做 获取系统调用表地址、512 乘 8。
    若 当前系统调用哈希 不等于 系统调用表_基线哈希 并且 系统调用表_基线哈希 不等于 0：
        做 内核警告 用 「[Rootkit] 系统调用表被篡改!」。
        异常 为 真。
    完。
    当前IDT哈希 为 做 计算内存区域哈希 用 做 获取IDT地址、256 乘 16。
    若 当前IDT哈希 不等于 IDT_基线哈希 并且 IDT_基线哈希 不等于 0：
        做 内核警告 用 「[Rootkit] 中断描述符表被篡改!」。
        异常 为 真。
    完。
    若 异常：
        Rootkit_检测数 为 Rootkit_检测数 加 1。
    完。
完。

计算内存区域哈希 接受 地址、大小：
    若 地址 等于 0 或者 大小 等于 0：
        返还 0。
    完。
    可变 哈希 为 0x811c9dc5。
    可变 i 为 0。
    只要 i 小于 大小：
        哈希 为 (哈希 位异或 做 内存_读8 用 地址 加 i) 乘 0x01000193。
        i 为 i 加 1。
    完。
    返还 哈希。
完。

是系统关键路径 接受 路径：
    返还 做 字符串_开头是 用 路径、「/boot/」 或者 做 字符串_开头是 用 路径、「/etc/」 或者 做 字符串_开头是 用 路径、「/usr/lib/」 或者 做 字符串_开头是 用 路径、「/var/nova/模型/」。
完。

AI_L3_统计 接受 零参：
    返还 一本字典：总评估 对应 AI_L3_总评估数、高分检测 对应 AI_L3_高分数、在线样本 对应 做 计数 用 AI_L3_在线学习样本、误报反馈 对应 AI_L3_误报反馈数、漏报反馈 对应 AI_L3_漏报反馈数、恶意模式 对应 做 计数 用 恶意模式库、完整性文件 对应 做 计数 用 完整性_基线、完整性违规 对应 做 计数 用 完整性_违规、Rootkit检测 对应 Rootkit_检测数。
完。

安全防护_统计 接受 零参：
    返还 一本字典：扫描总数 对应 扫描_文件总数、威胁发现 对应 扫描_威胁发现、行为可疑 对应 行为_可疑事件、行为拦截 对应 行为_拦截次数、隔离区 对应 做 计数 用 隔离区、完整性违规 对应 做 计数 用 完整性_违规、威胁库 对应 做 计数 用 威胁库、AI评估 对应 AI_L3_总评估数、Rootkit 对应 Rootkit_检测数。
完。

本章 收束。
