《物理页描述符》

引用《.·类型定义》。

引用《.·自动扩容》。

引用《.·内核打印》。

引用《.·字符串替换》。

PG_页号 为 0。

PG_标志 为 1。

PG_引用 为 2。

PG_映射 为 3。

PG_区域 为 4。

PG_阶 为 5。

PG_LRU类型 为 6。

PG_私有 为 7。

页_已锁 为 1 左移 0。

页_脏 为 1 左移 1。

页_已引用 为 1 左移 2。

页_活跃 为 1 左移 3。

页_Nova块缓存 为 1 左移 4。

页_伙伴 为 1 左移 5。

页_保留 为 1 左移 6。

页_内核 为 1 左移 7。

页_回写中 为 1 左移 8。

页_交换缓存 为 1 左移 9。

页_组合 为 1 左移 10。

页_组合尾 为 1 左移 11。

页_映射到磁盘 为 1 左移 12。

页_不可回收 为 1 左移 13。

页_空闲 为 1 左移 14。

可变 页描述符表 为 一组：（空）。

可变 页描述符_总数 为 0。

页描述符_初始化 接受 总页数：
    页描述符_总数 为 总页数。
    可变 i 为 0。
    只要 i 小于 总页数：
        做 追加 用 页描述符表、一组：i、0、0、0、区域_普通、0、负 1、0。
        i 为 i 加 1。
    完。
    做 内核信息数值 用 「页描述符表创建」、总页数。
完。

页描述符_获取 接受 页号：
    若 页号 大于或等于 0 并且 页号 小于 页描述符_总数：
        返还 页描述符表 的 第 页号 项。
    完。
    返还 空。
完。

页描述符_有效 接受 页号：
    返还 页号 大于或等于 0 并且 页号 小于 页描述符_总数。
完。

页描述符_增引用 接受 页：
    做 设置 用 页、PG_引用、页 的 第 PG_引用 项 加 1。
完。

页描述符_减引用 接受 页：
    做 设置 用 页、PG_引用、页 的 第 PG_引用 项 减 1。
    返还 页 的 第 PG_引用 项。
完。

页描述符_获取引用 接受 页：
    返还 页 的 第 PG_引用 项。
完。

页描述符_只有一个引用 接受 页：
    返还 页 的 第 PG_引用 项 等于 1。
完。

页描述符_设置标志 接受 页、标志：
    做 设置 用 页、PG_标志、页 的 第 PG_标志 项 位或 标志。
完。

页描述符_清除标志 接受 页、标志：
    做 设置 用 页、PG_标志、页 的 第 PG_标志 项 位与 按位取反 标志。
完。

页描述符_测试标志 接受 页、标志：
    返还 页 的 第 PG_标志 项 位与 标志 不等于 0。
完。

页描述符_测试并设置 接受 页、标志：
    旧值 为 页 的 第 PG_标志 项 位与 标志 不等于 0。
    做 设置 用 页、PG_标志、页 的 第 PG_标志 项 位或 标志。
    返还 旧值。
完。

页描述符_测试并清除 接受 页、标志：
    旧值 为 页 的 第 PG_标志 项 位与 标志 不等于 0。
    做 设置 用 页、PG_标志、页 的 第 PG_标志 项 位与 按位取反 标志。
    返还 旧值。
完。

页_是空闲 接受 页：
    返还 做 页描述符_测试标志 用 页、页_伙伴。
完。

页_是保留 接受 页：
    返还 做 页描述符_测试标志 用 页、页_保留。
完。

页_是Nova块缓存 接受 页：
    返还 做 页描述符_测试标志 用 页、页_Nova块缓存。
完。

页_是脏 接受 页：
    返还 做 页描述符_测试标志 用 页、页_脏。
完。

页_是锁定 接受 页：
    返还 做 页描述符_测试标志 用 页、页_已锁。
完。

页_是活跃 接受 页：
    返还 做 页描述符_测试标志 用 页、页_活跃。
完。

页_可回收 接受 页：
    返还 非 做 页描述符_测试标志 用 页、页_保留 并且 非 做 页描述符_测试标志 用 页、页_已锁 并且 非 做 页描述符_测试标志 用 页、页_不可回收。
完。

物理_页号到地址 接受 页号：
    返还 页号 乘 页大小。
完。

物理_地址到页号 接受 物理地址：
    返还 物理地址 除以 页大小。
完。

页号到虚拟 接受 页号：
    返还 做 物理到虚拟 用 页号 乘 页大小。
完。

页描述符_设置映射 接受 页、映射：
    做 设置 用 页、PG_映射、映射。
完。

页描述符_获取映射 接受 页：
    返还 页 的 第 PG_映射 项。
完。

页描述符_设置私有 接受 页、私有：
    做 设置 用 页、PG_私有、私有。
完。

页描述符_获取私有 接受 页：
    返还 页 的 第 PG_私有 项。
完。

页描述符_设置组合 接受 首页号、阶：
    页数 为 1 左移 阶。
    首页 为 做 页描述符_获取 用 首页号。
    若 首页 等于 空：
        返还。
    完。
    做 页描述符_设置标志 用 首页、页_组合。
    做 设置 用 首页、PG_阶、阶。
    可变 i 为 1。
    只要 i 小于 页数：
        尾页 为 做 页描述符_获取 用 首页号 加 i。
        若 尾页 不等于 空：
            做 页描述符_设置标志 用 尾页、页_组合尾。
            做 设置 用 尾页、PG_私有、首页号。
        完。
        i 为 i 加 1。
    完。
完。

页描述符_是组合首页 接受 页：
    返还 做 页描述符_测试标志 用 页、页_组合。
完。

页描述符_是组合尾页 接受 页：
    返还 做 页描述符_测试标志 用 页、页_组合尾。
完。

页描述符_组合阶 接受 页号：
    页 为 做 页描述符_获取 用 页号。
    若 页 等于 空：
        返还 0。
    完。
    若 做 页描述符_测试标志 用 页、页_组合尾：
        首页号 为 页 的 第 PG_私有 项。
        首页 为 做 页描述符_获取 用 首页号。
        若 首页 不等于 空：
            返还 首页 的 第 PG_阶 项。
        完。
    完。
    返还 页 的 第 PG_阶 项。
完。

页描述符_标记内核保留 接受 起始页号、页数：
    可变 i 为 0。
    只要 i 小于 页数：
        页 为 做 页描述符_获取 用 起始页号 加 i。
        若 页 不等于 空：
            做 页描述符_设置标志 用 页、页_保留 位或 页_内核。
            做 设置 用 页、PG_引用、1。
        完。
        i 为 i 加 1。
    完。
完。

页描述符_设置区域 接受 起始页号、结束页号、区域类型：
    可变 i 为 起始页号。
    只要 i 小于 结束页号 并且 i 小于 页描述符_总数：
        页 为 做 页描述符_获取 用 i。
        若 页 不等于 空：
            做 设置 用 页、PG_区域、区域类型。
        完。
        i 为 i 加 1。
    完。
完。

页_标志名 接受 标志：
    若 标志 位与 页_已锁 不等于 0：
        返还 「locked」。
    完。
    若 标志 位与 页_脏 不等于 0：
        返还 「dirty」。
    完。
    若 标志 位与 页_活跃 不等于 0：
        返还 「active」。
    完。
    若 标志 位与 页_Nova块缓存 不等于 0：
        返还 「Nova块缓存」。
    完。
    若 标志 位与 页_伙伴 不等于 0：
        返还 「Nova伙伴分配」。
    完。
    若 标志 位与 页_保留 不等于 0：
        返还 「reserved」。
    完。
    返还 「none」。
完。

页_区域名 接受 区域：
    若 区域 等于 0：
        返还 「直传」。
    完。
    若 区域 等于 1：
        返还 「直传32」。
    完。
    若 区域 等于 2：
        返还 「Normal」。
    完。
    返还 「unknown」。
完。

可变 页描述符_已初始化 为 假。

可变 页_get总数 为 0。

可变 页_put总数 为 0。

可变 页_分配总数 为 0。

可变 页_释放总数 为 0。

可变 页_LRU添加总数 为 0。

可变 页_LRU移除总数 为 0。

可变 页_锁定总数 为 0。

可变 页_脏标记总数 为 0。

可变 页描述符_错误数 为 0。

迁移_不可迁移 为 0。

迁移_可移动 为 1。

迁移_可回收 为 2。

迁移_高阶原子 为 3。

迁移类型名 接受 类型：
    若 类型 等于 迁移_不可迁移：
        返还 「不可迁移」。
    完。
    若 类型 等于 迁移_可移动：
        返还 「可移动」。
    完。
    若 类型 等于 迁移_可回收：
        返还 「可回收」。
    完。
    若 类型 等于 迁移_高阶原子：
        返还 「高阶原子」。
    完。
    返还 「未知」。
完。

get_page 接受 页：
    若 页 等于 空：
        页描述符_错误数 为 页描述符_错误数 加 1。
        返还。
    完。
    页 的 第 页_引用计数偏移 项 为 页 的 第 页_引用计数偏移 项 加 1。
    页_get总数 为 页_get总数 加 1。
完。

put_page 接受 页：
    若 页 等于 空：
        页描述符_错误数 为 页描述符_错误数 加 1。
        返还。
    完。
    页 的 第 页_引用计数偏移 项 为 页 的 第 页_引用计数偏移 项 减 1。
    页_put总数 为 页_put总数 加 1。
    若 页 的 第 页_引用计数偏移 项 等于 0：
        做 页_释放回伙伴 用 页。
    完。
完。

page_count 接受 页：
    若 页 等于 空：
        返还 0。
    完。
    返还 页 的 第 页_引用计数偏移 项。
完。

page_ref_frozen 接受 页：
    若 页 等于 空：
        返还 假。
    完。
    返还 页 的 第 页_引用计数偏移 项 等于 0。
完。

页_引用计数偏移 为 1。

页_设置标志 接受 页、标志：
    若 页 等于 空：
        返还。
    完。
    页 的 第 0 项 为 页 的 第 0 项 位或 标志。
完。

页_清除标志 接受 页、标志：
    若 页 等于 空：
        返还。
    完。
    页 的 第 0 项 为 页 的 第 0 项 位与 按位取反 标志。
完。

页_测试标志 接受 页、标志：
    若 页 等于 空：
        返还 假。
    完。
    返还 页 的 第 0 项 位与 标志 不等于 0。
完。

页_设置锁定 接受 页：
    做 页_设置标志 用 页、页_已锁。
    页_锁定总数 为 页_锁定总数 加 1。
完。

页_清除锁定 接受 页：
    做 页_清除标志 用 页、页_已锁。
完。

页_是否锁定 接受 页：
    返还 做 页_测试标志 用 页、页_已锁。
完。

页_设置脏 接受 页：
    做 页_设置标志 用 页、页_脏。
    页_脏标记总数 为 页_脏标记总数 加 1。
完。

页_清除脏 接受 页：
    做 页_清除标志 用 页、页_脏。
完。

页_是否脏 接受 页：
    返还 做 页_测试标志 用 页、页_脏。
完。

页_设置活跃 接受 页：
    做 页_设置标志 用 页、页_活跃。
完。

页_清除活跃 接受 页：
    做 页_清除标志 用 页、页_活跃。
完。

页_是否活跃 接受 页：
    返还 做 页_测试标志 用 页、页_活跃。
完。

页大小_描述 为 4096。

页_到PFN 接受 页：
    若 页 等于 空：
        返还 0。
    完。
    返还 页 的 第 4 项。
完。

PFN_到物理地址 接受 pfn：
    返还 pfn 乘 页大小_描述。
完。

物理地址_到PFN 接受 地址：
    返还 地址 除以 页大小_描述。
完。

页_到物理地址 接受 页：
    返还 做 页_到PFN 用 页 乘 页大小_描述。
完。

页_标志全名 接受 标志：
    可变 名称 为 「」。
    若 标志 位与 页_已锁 不等于 0：
        名称 为 名称 加 「L」。
    完。
    若 标志 位与 页_脏 不等于 0：
        名称 为 名称 加 「D」。
    完。
    若 标志 位与 页_活跃 不等于 0：
        名称 为 名称 加 「A」。
    完。
    若 标志 位与 页_Nova块缓存 不等于 0：
        名称 为 名称 加 「S」。
    完。
    若 标志 位与 页_伙伴 不等于 0：
        名称 为 名称 加 「B」。
    完。
    若 标志 位与 页_保留 不等于 0：
        名称 为 名称 加 「R」。
    完。
    若 名称 等于 「」：
        名称 为 「-」。
    完。
    返还 名称。
完。

页描述符_完整统计 接受 零参：
    统计 为 做 字典。
    做 字典设 用 统计、「已初始化」、页描述符_已初始化。
    做 字典设 用 统计、「总页数」、页描述符_总数。
    做 字典设 用 统计、「get_page总数」、页_get总数。
    做 字典设 用 统计、「put_page总数」、页_put总数。
    做 字典设 用 统计、「分配总数」、页_分配总数。
    做 字典设 用 统计、「释放总数」、页_释放总数。
    做 字典设 用 统计、「LRU添加」、页_LRU添加总数。
    做 字典设 用 统计、「LRU移除」、页_LRU移除总数。
    做 字典设 用 统计、「锁定总数」、页_锁定总数。
    做 字典设 用 统计、「脏标记总数」、页_脏标记总数。
    做 字典设 用 统计、「活跃引用」、页_get总数 减 页_put总数。
    做 字典设 用 统计、「错误数」、页描述符_错误数。
    返还 统计。
完。

页描述符_打印统计 接受 零参：
    统计 为 做 页描述符_完整统计。
    做 打印 用 「[页描述符] ═══════ 完整统计 ═══════」。
    做 打印 用 「  总页数:       」 加 做 转字符串 用 做 字典取 用 统计、「总页数」。
    做 打印 用 「  get_page:     」 加 做 转字符串 用 做 字典取 用 统计、「get_page总数」。
    做 打印 用 「  put_page:     」 加 做 转字符串 用 做 字典取 用 统计、「put_page总数」。
    做 打印 用 「  分配:         」 加 做 转字符串 用 做 字典取 用 统计、「分配总数」。
    做 打印 用 「  释放:         」 加 做 转字符串 用 做 字典取 用 统计、「释放总数」。
    做 打印 用 「  LRU添加:      」 加 做 转字符串 用 做 字典取 用 统计、「LRU添加」。
    做 打印 用 「  LRU移除:      」 加 做 转字符串 用 做 字典取 用 统计、「LRU移除」。
    做 打印 用 「  锁定:         」 加 做 转字符串 用 做 字典取 用 统计、「锁定总数」。
    做 打印 用 「  脏标记:       」 加 做 转字符串 用 做 字典取 用 统计、「脏标记总数」。
    做 打印 用 「  活跃引用:     」 加 做 转字符串 用 做 字典取 用 统计、「活跃引用」。
    做 打印 用 「  错误:         」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[页描述符] ═════════════════════════」。
    返还 0。
完。

页描述符_dump 接受 零参：
    做 打印 用 「[页描述符 dump] ──────────────────」。
    做 打印 用 「  总页数: 」 加 做 转字符串 用 页描述符_总数。
    做 打印 用 「  get: 」 加 做 转字符串 用 页_get总数。
    做 打印 用 「  put: 」 加 做 转字符串 用 页_put总数。
    做 打印 用 「  分配: 」 加 做 转字符串 用 页_分配总数。
    做 打印 用 「  释放: 」 加 做 转字符串 用 页_释放总数。
    做 打印 用 「  脏: 」 加 做 转字符串 用 页_脏标记总数。
    做 打印 用 「  错误: 」 加 做 转字符串 用 页描述符_错误数。
    做 打印 用 「[页描述符 dump] ──────────────────」。
完。

页描述符_销毁 接受 零参：
    页描述符_已初始化 为 假。
    返还 0。
完。

页描述符_为伙伴系统提供页状态 接受 零参：
    返还 一组：页描述符_总数、页_分配总数、页_释放总数、页_get总数、页_put总数。
完。

页描述符_为回收提供LRU信息 接受 零参：
    返还 一组：页_LRU添加总数、页_LRU移除总数、页_脏标记总数、页_锁定总数。
完。

页描述符_为AI提供页使用模式 接受 零参：
    返还 一组：页_分配总数、页_释放总数、页_脏标记总数、页_get总数 减 页_put总数。
完。

页描述符_重置统计 接受 零参：
    页_get总数 为 0。
    页_put总数 为 0。
    页_分配总数 为 0。
    页_释放总数 为 0。
    页_LRU添加总数 为 0。
    页_LRU移除总数 为 0。
    页_锁定总数 为 0。
    页_脏标记总数 为 0。
    页描述符_错误数 为 0。
    做 打印 用 「[页描述符] 统计已重置」。
    返还 0。
完。

页_复合 为 1 左移 8。

页_复合头 为 1 左移 9。

页_是复合页 接受 页：
    若 页 等于 空：
        返还 假。
    完。
    返还 页 的 第 0 项 位与 页_复合 不等于 0。
完。

页_是复合头 接受 页：
    若 页 等于 空：
        返还 假。
    完。
    返还 页 的 第 0 项 位与 页_复合头 不等于 0。
完。

页_复合阶 接受 页：
    若 做 页_是复合头 用 页 等于 假：
        返还 0。
    完。
    返还 页 的 第 5 项。
完。

页_复合页数 接受 页：
    返还 1 左移 做 页_复合阶 用 页。
完。

页_映射计数偏移 为 2。

页_映射计数 接受 页：
    若 页 等于 空：
        返还 0。
    完。
    返还 页 的 第 页_映射计数偏移 项。
完。

页_增加映射 接受 页：
    若 页 等于 空：
        返还。
    完。
    页 的 第 页_映射计数偏移 项 为 页 的 第 页_映射计数偏移 项 加 1。
完。

页_减少映射 接受 页：
    若 页 等于 空：
        返还。
    完。
    若 页 的 第 页_映射计数偏移 项 大于 0：
        页 的 第 页_映射计数偏移 项 为 页 的 第 页_映射计数偏移 项 减 1。
    完。
完。

页_是否已映射 接受 页：
    返还 做 页_映射计数 用 页 大于 0。
完。

页描述符_为procfs提供页统计 接受 零参：
    返还 一组：页描述符_总数、页_分配总数、页_释放总数、页_脏标记总数、页_锁定总数。
完。

页描述符_为调度器提供内存活跃度 接受 零参：
    返还 一组：页_get总数 减 页_put总数、页_脏标记总数。
完。

页描述符_为电源管理提供页活跃度 接受 零参：
    返还 一组：页_分配总数 加 页_释放总数、页_脏标记总数。
完。

页_释放回伙伴 接受 页：
    若 页 等于 0 或者 页 等于 空：
        返还。
    完。
    order 为 页 的 阶数。
    若 order 小于 0 或者 order 大于 最大阶：
        order 为 0。
    完。
    可变 pfn 为 页 的 页帧号。
    可变 cur_order 为 order。
    只要 cur_order 小于 最大阶：
        buddy_pfn 为 pfn 位异或 1 左移 cur_order。
        buddy 为 做 页帧号到页描述符 用 buddy_pfn。
        若 buddy 等于 空 或者 buddy 的 已分配 或者 buddy 的 阶数 不等于 cur_order：
            cur_order 为 最大阶。
        否则：
            做 伙伴系统_移除空闲 用 buddy、cur_order。
            若 buddy_pfn 小于 pfn：
                pfn 为 buddy_pfn。
            完。
            cur_order 为 cur_order 加 1。
        完。
    完。
    merged 为 做 页帧号到页描述符 用 pfn。
    若 merged 不等于 空：
        merged 的 已分配 为 假。
        merged 的 阶数 为 cur_order。
        做 伙伴系统_添加空闲 用 merged、cur_order。
    完。
    页_释放总数 为 页_释放总数 加 1。
完。

本章 收束。
