《内存读写锁》

引用《.·类型定义》。

引用《.·同步原语》。

引用《.·内核打印》。

引用《.·高精度定时器hrtimer》。

引用《.·内容可见性》。

引用《.·整数转字符串》。

锁_空闲 为 0。

锁_读持有 为 1。

锁_写持有 为 2。

最大锁深度 为 8。

最大自旋次数 为 1000。

退避初始 为 1。

退避最大 为 64。

可变 全局锁序号 为 0。

读写锁 是 一种 事物，含 状态、读者数、写者标识、等待读者、等待写者、获取次数、争用次数、最长等待、总等待时间、递归深度、锁序号、创建时间。

可变 锁总获取 为 0。

可变 锁总争用 为 0。

可变 死锁检测数 为 0。

可变 锁总自旋 为 0。

可变 锁总超时 为 0。

可变 锁总升级 为 0。

可变 锁总降级 为 0。

可变 锁总创建 为 0。

可变 锁总销毁 为 0。

可变 持有栈 为 一组：（空）。

最大热点数 为 16。

争用热点 是 一种 事物，含 锁引用、争用次数、总等待。

可变 争用热点表 为 一组：（空）。

内存锁_创建 接受 零参：
    锁 为 新建 读写锁。
    锁 的 状态 为 锁_空闲。
    锁 的 读者数 为 0。
    锁 的 写者标识 为 0。
    锁 的 等待读者 为 0。
    锁 的 等待写者 为 0。
    锁 的 获取次数 为 0。
    锁 的 争用次数 为 0。
    锁 的 最长等待 为 0。
    锁 的 总等待时间 为 0。
    锁 的 递归深度 为 0。
    全局锁序号 为 全局锁序号 加 1。
    锁 的 锁序号 为 全局锁序号。
    锁 的 创建时间 为 做 平台_获取高精度时间。
    锁总创建 为 锁总创建 加 1。
    返还 锁。
完。

内存锁_销毁 接受 锁：
    若 锁 等于 0：
        返还。
    完。
    若 锁 的 状态 不等于 锁_空闲：
        做 内核警告 用 「内存锁: 销毁非空闲锁, 状态=」 加 做 整数转字符串 用 锁 的 状态。
    完。
    做 内存锁_出栈 用 锁。
    锁总销毁 为 锁总销毁 加 1。
完。

内存锁_尝试读 接受 锁：
    若 锁 等于 0：
        返还 假。
    完。
    锁总获取 为 锁总获取 加 1。
    若 锁 的 状态 等于 锁_写持有：
        锁 的 等待读者 为 锁 的 等待读者 加 1。
        锁总争用 为 锁总争用 加 1。
        锁 的 争用次数 为 锁 的 争用次数 加 1。
        做 内存锁_记录争用 用 锁。
        返还 假。
    完。
    锁 的 状态 为 锁_读持有。
    锁 的 读者数 为 锁 的 读者数 加 1。
    锁 的 获取次数 为 锁 的 获取次数 加 1。
    返还 真。
完。

内存锁_尝试写 接受 锁：
    若 锁 等于 0：
        返还 假。
    完。
    锁总获取 为 锁总获取 加 1。
    若 锁 的 状态 等于 锁_写持有 并且 锁 的 写者标识 等于 做 获取当前进程标识：
        锁 的 递归深度 为 锁 的 递归深度 加 1。
        锁 的 获取次数 为 锁 的 获取次数 加 1。
        返还 真。
    完。
    若 锁 的 状态 不等于 锁_空闲：
        锁 的 等待写者 为 锁 的 等待写者 加 1。
        锁总争用 为 锁总争用 加 1。
        锁 的 争用次数 为 锁 的 争用次数 加 1。
        做 内存锁_记录争用 用 锁。
        返还 假。
    完。
    若 做 内存锁_检查死锁 用 锁：
        死锁检测数 为 死锁检测数 加 1。
        做 内核警告 用 「内存锁: 死锁检测, 锁序号=」 加 做 整数转字符串 用 锁 的 锁序号。
        返还 假。
    完。
    若 非 做 内存锁_检查排序 用 锁：
        做 内核警告 用 「内存锁: 锁排序违规, 锁序号=」 加 做 整数转字符串 用 锁 的 锁序号。
    完。
    锁 的 状态 为 锁_写持有。
    锁 的 写者标识 为 做 获取当前进程标识。
    锁 的 获取次数 为 锁 的 获取次数 加 1。
    锁 的 递归深度 为 1。
    做 内存锁_入栈 用 锁。
    返还 真。
完。

内存锁_读_自旋 接受 锁：
    若 锁 等于 0：
        返还 假。
    完。
    开始 为 做 平台_获取高精度时间。
    可变 自旋数 为 0。
    可变 退避 为 退避初始。
    只要 自旋数 小于 最大自旋次数：
        若 做 内存锁_尝试读 用 锁：
            等待 为 做 平台_获取高精度时间 减 开始。
            锁 的 总等待时间 为 锁 的 总等待时间 加 等待。
            若 等待 大于 锁 的 最长等待：
                锁 的 最长等待 为 等待。
            完。
            锁总自旋 为 锁总自旋 加 自旋数。
            返还 真。
        完。
        可变 p 为 0。
        只要 p 小于 退避：
            p 为 p 加 1。
        完。
        退避 为 退避 乘 2。
        若 退避 大于 退避最大：
            退避 为 退避最大。
        完。
        自旋数 为 自旋数 加 1。
    完。
    锁总自旋 为 锁总自旋 加 自旋数。
    返还 假。
完。

内存锁_写_自旋 接受 锁：
    若 锁 等于 0：
        返还 假。
    完。
    开始 为 做 平台_获取高精度时间。
    可变 自旋数 为 0。
    可变 退避 为 退避初始。
    只要 自旋数 小于 最大自旋次数：
        若 做 内存锁_尝试写 用 锁：
            等待 为 做 平台_获取高精度时间 减 开始。
            锁 的 总等待时间 为 锁 的 总等待时间 加 等待。
            若 等待 大于 锁 的 最长等待：
                锁 的 最长等待 为 等待。
            完。
            锁总自旋 为 锁总自旋 加 自旋数。
            返还 真。
        完。
        可变 p 为 0。
        只要 p 小于 退避：
            p 为 p 加 1。
        完。
        退避 为 退避 乘 2。
        若 退避 大于 退避最大：
            退避 为 退避最大。
        完。
        自旋数 为 自旋数 加 1。
    完。
    锁总自旋 为 锁总自旋 加 自旋数。
    返还 假。
完。

内存锁_读_超时 接受 锁、超时_微秒：
    若 锁 等于 0：
        返还 假。
    完。
    开始 为 做 平台_获取高精度时间。
    截止 为 开始 加 超时_微秒。
    只要 做 平台_获取高精度时间 小于 截止：
        若 做 内存锁_尝试读 用 锁：
            等待 为 做 平台_获取高精度时间 减 开始。
            锁 的 总等待时间 为 锁 的 总等待时间 加 等待。
            若 等待 大于 锁 的 最长等待：
                锁 的 最长等待 为 等待。
            完。
            返还 真。
        完。
        做 schedule_timeout 用 1。
    完。
    锁总超时 为 锁总超时 加 1。
    返还 假。
完。

内存锁_写_超时 接受 锁、超时_微秒：
    若 锁 等于 0：
        返还 假。
    完。
    开始 为 做 平台_获取高精度时间。
    截止 为 开始 加 超时_微秒。
    只要 做 平台_获取高精度时间 小于 截止：
        若 做 内存锁_尝试写 用 锁：
            等待 为 做 平台_获取高精度时间 减 开始。
            锁 的 总等待时间 为 锁 的 总等待时间 加 等待。
            若 等待 大于 锁 的 最长等待：
                锁 的 最长等待 为 等待。
            完。
            返还 真。
        完。
        做 schedule_timeout 用 1。
    完。
    锁总超时 为 锁总超时 加 1。
    返还 假。
完。

内存锁_释放读 接受 锁：
    若 锁 等于 0：
        返还。
    完。
    锁 的 读者数 为 锁 的 读者数 减 1。
    若 锁 的 读者数 小于或等于 0：
        锁 的 读者数 为 0。
        锁 的 状态 为 锁_空闲。
    完。
完。

内存锁_释放写 接受 锁：
    若 锁 等于 0：
        返还。
    完。
    若 锁 的 递归深度 大于 1：
        锁 的 递归深度 为 锁 的 递归深度 减 1。
        返还。
    完。
    锁 的 状态 为 锁_空闲。
    锁 的 写者标识 为 0。
    锁 的 递归深度 为 0。
    做 内存锁_出栈 用 锁。
完。

内存锁_升级 接受 锁：
    若 锁 等于 0：
        返还 假。
    完。
    若 锁 的 状态 不等于 锁_读持有 或者 锁 的 读者数 不等于 1：
        返还 假。
    完。
    锁 的 读者数 为 0。
    锁 的 状态 为 锁_写持有。
    锁 的 写者标识 为 做 获取当前进程标识。
    锁 的 递归深度 为 1。
    做 内存锁_入栈 用 锁。
    锁总升级 为 锁总升级 加 1。
    返还 真。
完。

内存锁_降级 接受 锁：
    若 锁 等于 0 或者 锁 的 状态 不等于 锁_写持有：
        返还。
    完。
    锁 的 状态 为 锁_读持有。
    锁 的 读者数 为 1。
    锁 的 写者标识 为 0。
    锁 的 递归深度 为 0。
    做 内存锁_出栈 用 锁。
    锁总降级 为 锁总降级 加 1。
完。

内存锁_检查死锁 接受 锁：
    可变 序号 为 0。
    只要 序号 小于 做 计数 用 持有栈：
        若 持有栈 的 第 序号 项 等于 锁：
            返还 真。
        完。
        序号 为 序号 加 1。
    完。
    返还 假。
完。

内存锁_检查排序 接受 锁：
    可变 i 为 0。
    只要 i 小于 做 计数 用 持有栈：
        若 持有栈 的 第 i 项 不等于 0 并且 持有栈 的 第 i 项 的 锁序号 大于或等于 锁 的 锁序号：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

内存锁_入栈 接受 锁：
    若 做 计数 用 持有栈 小于 最大锁深度：
        做 追加 用 持有栈、锁。
    完。
完。

内存锁_出栈 接受 锁：
    可变 序号 为 做 计数 用 持有栈 减 1。
    只要 序号 大于或等于 0：
        若 持有栈 的 第 序号 项 等于 锁：
            若 序号 小于 做 计数 用 持有栈 减 1：
                持有栈 的 第 序号 项 为 持有栈 的 第 做 计数 用 持有栈 减 1 项。
            完。
            做 删除 用 持有栈、做 计数 用 持有栈 减 1。
            返还。
        完。
        序号 为 序号 减 1。
    完。
完。

内存锁_记录争用 接受 锁：
    可变 i 为 0。
    只要 i 小于 做 计数 用 争用热点表：
        若 争用热点表 的 第 i 项 的 锁引用 等于 锁：
            争用热点表 的 第 i 项 的 争用次数 为 争用热点表 的 第 i 项 的 争用次数 加 1。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 争用热点表 小于 最大热点数：
        热点 为 新建 争用热点。
        热点 的 锁引用 为 锁。
        热点 的 争用次数 为 1。
        热点 的 总等待 为 0。
        做 追加 用 争用热点表、热点。
    完。
完。

内存锁_打印争用热点 接受 零参：
    做 打印行 用 「=== 锁争用热点 ===」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 争用热点表：
        h 为 争用热点表 的 第 i 项。
        做 打印行 用 「锁序号=」 加 做 整数转字符串 用 h 的 锁引用 的 锁序号 加 「 争用=」 加 做 整数转字符串 用 h 的 争用次数。
        i 为 i 加 1。
    完。
完。

内存锁_是空闲 接受 锁：
    若 锁 等于 0：
        返还 真。
    完。
    返还 锁 的 状态 等于 锁_空闲。
完。

内存锁_读者数 接受 锁：
    若 锁 等于 0：
        返还 0。
    完。
    返还 锁 的 读者数。
完。

内存锁_是写持有 接受 锁：
    若 锁 等于 0：
        返还 假。
    完。
    返还 锁 的 状态 等于 锁_写持有。
完。

内存锁_持有深度 接受 零参：
    返还 做 计数 用 持有栈。
完。

内存锁_统计 接受 零参：
    返还 一组：锁总获取、锁总争用、死锁检测数、锁总自旋、锁总超时、锁总升级、锁总降级。
完。

内存锁_打印统计 接受 零参：
    做 打印行 用 「=== 内存读写锁统计 ===」。
    做 打印行 用 「总获取: 」 加 做 整数转字符串 用 锁总获取。
    做 打印行 用 「总争用: 」 加 做 整数转字符串 用 锁总争用。
    做 打印行 用 「死锁检测: 」 加 做 整数转字符串 用 死锁检测数。
    做 打印行 用 「总自旋: 」 加 做 整数转字符串 用 锁总自旋。
    做 打印行 用 「总超时: 」 加 做 整数转字符串 用 锁总超时。
    做 打印行 用 「升级次数: 」 加 做 整数转字符串 用 锁总升级。
    做 打印行 用 「降级次数: 」 加 做 整数转字符串 用 锁总降级。
    做 打印行 用 「创建锁数: 」 加 做 整数转字符串 用 锁总创建。
    做 打印行 用 「销毁锁数: 」 加 做 整数转字符串 用 锁总销毁。
    做 打印行 用 「持有栈深: 」 加 做 整数转字符串 用 做 计数 用 持有栈。
    做 打印行 用 「争用热点: 」 加 做 整数转字符串 用 做 计数 用 争用热点表。
    若 锁总获取 大于 0：
        做 打印行 用 「争用率: 」 加 做 整数转字符串 用 锁总争用 乘 100 除以 锁总获取 加 「%」。
    完。
完。

内存锁_健康检查 接受 锁：
    若 锁 等于 0：
        返还 假。
    完。
    若 锁 的 状态 等于 锁_写持有 并且 锁 的 写者标识 等于 0：
        返还 假。
    完。
    若 锁 的 状态 等于 锁_读持有 并且 锁 的 读者数 等于 0：
        返还 假。
    完。
    若 锁 的 状态 等于 锁_空闲 并且 锁 的 读者数 大于 0：
        返还 假。
    完。
    若 锁 的 状态 等于 锁_空闲 并且 锁 的 递归深度 大于 0：
        返还 假。
    完。
    返还 真。
完。

内存锁_批量健康检查 接受 零参：
    可变 异常数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 持有栈：
        若 持有栈 的 第 i 项 不等于 0 并且 非 做 内存锁_健康检查 用 持有栈 的 第 i 项：
            异常数 为 异常数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 异常数。
完。

内存锁_重置统计 接受 零参：
    锁总获取 为 0。
    锁总争用 为 0。
    死锁检测数 为 0。
    锁总自旋 为 0。
    锁总超时 为 0。
    锁总升级 为 0。
    锁总降级 为 0。
    锁总创建 为 0。
    锁总销毁 为 0。
    争用热点表 为 一组：（空）。
完。

内存锁_尝试升级 接受 锁：
    若 锁 等于 0：
        返还 假。
    完。
    若 锁 的 状态 不等于 锁_读持有：
        返还 假。
    完。
    若 锁 的 读者数 不等于 1：
        返还 假。
    完。
    若 做 原子比较交换 用 & 锁 的 状态、锁_读持有、锁_写持有：
        锁 的 写者标识 为 做 获取当前线程标识。
        锁 的 读者数 为 0。
        锁总升级 为 锁总升级 加 1。
        返还 真。
    完。
    返还 假。
完。

可变 AI_内存尝锁_运行指标 为 0。

可变 AI_内存尝锁_异常指标 为 0。

AI_内存尝锁_自诊断 接受 零参：
    AI_内存尝锁_运行指标 为 0。
    返还 AI_内存尝锁_运行指标。
完。

AI_内存尝锁_优化建议 接受 零参：
    指标 为 做 AI_内存尝锁_自诊断。
    返还 「内存尝锁状态正常」。
完。

本章 收束。
