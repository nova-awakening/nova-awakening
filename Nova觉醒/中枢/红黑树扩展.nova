《红黑树扩展》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·红黑树创建》。

引用《.·红黑树删除》。

引用《.·红黑树后继》。

引用《.·整数转字符串》。

红黑树_中序遍历_扩展 接受 树、回调：
    可变 节点 为 做 红黑树_最小 用 树。
    只要 节点 不等于 空：
        做 回调 用 节点。
        节点 为 做 红黑树_后继 用 树、节点。
    完。
完。

红黑树_逆序遍历 接受 树、回调：
    可变 节点 为 做 红黑树_最大 用 树。
    只要 节点 不等于 空：
        做 回调 用 节点。
        节点 为 做 红黑树_前驱 用 树、节点。
    完。
完。

红黑树_范围查询_扩展 接受 树、最小键、最大键、回调：
    可变 节点 为 做 红黑树_查找_大于等于 用 树、最小键。
    只要 节点 不等于 空：
        键 为 做 红黑树_节点键 用 节点。
        若 键 大于 最大键：
            中断。
        完。
        做 回调 用 节点。
        节点 为 做 红黑树_后继 用 树、节点。
    完。
完。

红黑树_查找_大于等于 接受 树、键：
    可变 最佳 为 空。
    可变 当前 为 做 红黑树_根 用 树。
    只要 当前 不等于 空：
        当前键 为 做 红黑树_节点键 用 当前。
        若 当前键 大于或等于 键：
            最佳 为 当前。
            当前 为 做 红黑树_左子 用 当前。
        否则：
            当前 为 做 红黑树_右子 用 当前。
        完。
    完。
    返还 最佳。
完。

红黑树_查找_小于等于 接受 树、键：
    可变 最佳 为 空。
    可变 当前 为 做 红黑树_根 用 树。
    只要 当前 不等于 空：
        当前键 为 做 红黑树_节点键 用 当前。
        若 当前键 小于或等于 键：
            最佳 为 当前。
            当前 为 做 红黑树_右子 用 当前。
        否则：
            当前 为 做 红黑树_左子 用 当前。
        完。
    完。
    返还 最佳。
完。

红黑树_高度_扩展 接受 树：
    返还 做 红黑树_节点高度 用 做 红黑树_根 用 树。
完。

红黑树_节点高度 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    左高 为 做 红黑树_节点高度 用 做 红黑树_左子 用 节点。
    右高 为 做 红黑树_节点高度 用 做 红黑树_右子 用 节点。
    若 左高 大于 右高：
        返还 左高 加 1。
    完。
    返还 右高 加 1。
完。

红黑树_转数组 接受 树：
    可变 结果 为 一组：（空）。
    做 红黑树_中序遍历 用 树、以 节点 为 引，其做法是：做 追加 用 结果、一组：做 红黑树_节点键 用 节点、做 红黑树_节点值 用 节点。
    返还 结果。
完。

RB_键 为 0。

RB_值 为 1。

RB_左 为 2。

RB_右 为 3。

RB_父 为 4。

RB_色 为 5。

红黑树_根 接受 树：
    若 树 等于 空：
        返还 空。
    完。
    返还 树 的 第 0 项。
完。

红黑树_左子 接受 节点：
    若 节点 等于 空：
        返还 空。
    完。
    返还 节点 的 第 RB_左 项。
完。

红黑树_右子 接受 节点：
    若 节点 等于 空：
        返还 空。
    完。
    返还 节点 的 第 RB_右 项。
完。

红黑树_节点键 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    返还 节点 的 第 RB_键 项。
完。

红黑树_节点值 接受 节点：
    若 节点 等于 空：
        返还 空。
    完。
    返还 节点 的 第 RB_值 项。
完。

红黑树_后继_扩展 接受 树、节点：
    若 节点 等于 空：
        返还 空。
    完。
    若 节点 的 第 RB_右 项 不等于 空：
        可变 n 为 节点 的 第 RB_右 项。
        只要 n 的 第 RB_左 项 不等于 空：
            n 为 n 的 第 RB_左 项。
        完。
        返还 n。
    完。
    可变 p 为 节点 的 第 RB_父 项。
    可变 c 为 节点。
    只要 p 不等于 空 并且 c 等于 p 的 第 RB_右 项：
        c 为 p。
        p 为 p 的 第 RB_父 项。
    完。
    返还 p。
完。

红黑树_前驱_扩展 接受 树、节点：
    若 节点 等于 空：
        返还 空。
    完。
    若 节点 的 第 RB_左 项 不等于 空：
        可变 n 为 节点 的 第 RB_左 项。
        只要 n 的 第 RB_右 项 不等于 空：
            n 为 n 的 第 RB_右 项。
        完。
        返还 n。
    完。
    可变 p 为 节点 的 第 RB_父 项。
    可变 c 为 节点。
    只要 p 不等于 空 并且 c 等于 p 的 第 RB_左 项：
        c 为 p。
        p 为 p 的 第 RB_父 项。
    完。
    返还 p。
完。

红黑树_最大_扩展 接受 树：
    根 为 做 红黑树_根 用 树。
    若 根 等于 空：
        返还 空。
    完。
    可变 n 为 根。
    只要 n 的 第 RB_右 项 不等于 空：
        n 为 n 的 第 RB_右 项。
    完。
    返还 n。
完。

红黑树_范围计数 接受 树、最小键、最大键：
    可变 计数 为 0。
    可变 节点 为 做 红黑树_查找_大于等于 用 树、最小键。
    只要 节点 不等于 空：
        键 为 做 红黑树_节点键 用 节点。
        若 键 大于 最大键：
            中断。
        完。
        计数 为 计数 加 1。
        节点 为 做 红黑树_后继_扩展 用 树、节点。
    完。
    返还 计数。
完。

红黑树_第K小 接受 树、k：
    可变 节点 为 做 红黑树_最小_扩展 用 树。
    可变 i 为 1。
    只要 节点 不等于 空 并且 i 小于 k：
        节点 为 做 红黑树_后继_扩展 用 树、节点。
        i 为 i 加 1。
    完。
    返还 节点。
完。

红黑树_最小_扩展 接受 树：
    根 为 做 红黑树_根 用 树。
    若 根 等于 空：
        返还 空。
    完。
    可变 n 为 根。
    只要 n 的 第 RB_左 项 不等于 空：
        n 为 n 的 第 RB_左 项。
    完。
    返还 n。
完。

红黑树_删除范围 接受 树、最小键、最大键：
    可变 待删除 为 一组：（空）。
    可变 节点 为 做 红黑树_查找_大于等于 用 树、最小键。
    只要 节点 不等于 空：
        键 为 做 红黑树_节点键 用 节点。
        若 键 大于 最大键：
            中断。
        完。
        做 追加 用 待删除、键。
        节点 为 做 红黑树_后继_扩展 用 树、节点。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 待删除：
        做 红黑树_删除 用 树、待删除 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 计数 用 待删除。
完。

红黑树_验证有序 接受 树：
    可变 上一键 为 负 9999999。
    可变 有序 为 真。
    可变 节点 为 做 红黑树_最小_扩展 用 树。
    只要 节点 不等于 空：
        键 为 做 红黑树_节点键 用 节点。
        若 键 小于 上一键：
            有序 为 假。
        完。
        上一键 为 键。
        节点 为 做 红黑树_后继_扩展 用 树、节点。
    完。
    返还 有序。
完。

红黑树_颜色名 接受 节点：
    若 节点 等于 空：
        返还 「nil」。
    完。
    若 节点 的 第 RB_色 项 等于 0：
        返还 「red」。
    完。
    返还 「black」。
完。

AI范围窗口 为 16。

扩展统计 是 一种 事物，含 中序遍历次数、逆序遍历次数、范围查询次数、范围查询命中、大于等于查找次数、小于等于查找次数、第K小查找次数、范围计数次数、范围删除次数、转数组次数、验证次数、验证通过、AI预测次数、AI预测命中、批量操作次数、错误次数。

可变 全局统计 为 新建 扩展统计。

可变 扩展模块已初始化 为 假。

可变 AI范围历史 为 一组：（空）。

可变 AI范围位置 为 0。

红黑树扩展_初始化 接受 零参：
    若 扩展模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI范围历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI范围窗口：
        做 追加 用 AI范围历史、一组：0、0。
        i 为 i 加 1。
    完。
    AI范围位置 为 0。
    扩展模块已初始化 为 真。
    做 打印 用 「[红黑树扩展] 初始化完成」。
    返还 0。
完。

红黑树扩展_销毁 接受 零参：
    若 扩展模块已初始化 等于 假：
        返还 0。
    完。
    AI范围历史 为 一组：（空）。
    扩展模块已初始化 为 假。
    做 打印 用 「[红黑树扩展] 已销毁」。
    返还 0。
完。

红黑树_安全中序遍历 接受 树、回调：
    若 树 等于 空 或者 回调 等于 空：
        返还。
    完。
    全局统计 的 中序遍历次数 为 全局统计 的 中序遍历次数 加 1。
    做 红黑树_中序遍历_扩展 用 树、回调。
完。

红黑树_安全逆序遍历 接受 树、回调：
    若 树 等于 空 或者 回调 等于 空：
        返还。
    完。
    全局统计 的 逆序遍历次数 为 全局统计 的 逆序遍历次数 加 1。
    做 红黑树_逆序遍历 用 树、回调。
完。

红黑树_安全范围查询 接受 树、最小键、最大键、回调：
    若 树 等于 空：
        返还。
    完。
    全局统计 的 范围查询次数 为 全局统计 的 范围查询次数 加 1。
    做 AI_记录范围 用 最小键、最大键。
    做 红黑树_范围查询_扩展 用 树、最小键、最大键、回调。
完。

红黑树_安全范围计数 接受 树、最小键、最大键：
    若 树 等于 空：
        返还 0。
    完。
    全局统计 的 范围计数次数 为 全局统计 的 范围计数次数 加 1。
    返还 做 红黑树_范围计数 用 树、最小键、最大键。
完。

红黑树_安全第K小 接受 树、k：
    若 树 等于 空 或者 k 小于或等于 0：
        返还 空。
    完。
    全局统计 的 第K小查找次数 为 全局统计 的 第K小查找次数 加 1。
    返还 做 红黑树_第K小 用 树、k。
完。

红黑树_安全删除范围 接受 树、最小键、最大键：
    若 树 等于 空：
        返还 0。
    完。
    全局统计 的 范围删除次数 为 全局统计 的 范围删除次数 加 1。
    返还 做 红黑树_删除范围 用 树、最小键、最大键。
完。

红黑树_收集范围 接受 树、最小键、最大键：
    若 树 等于 空：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 节点 为 做 红黑树_查找_大于等于 用 树、最小键。
    只要 节点 不等于 空：
        键 为 做 红黑树_节点键 用 节点。
        若 键 大于 最大键：
            止。
        完。
        做 追加 用 结果、一组：键、做 红黑树_节点值 用 节点。
        节点 为 做 红黑树_后继_扩展 用 树、节点。
    完。
    返还 结果。
完。

红黑树_收集前N 接受 树、n：
    若 树 等于 空 或者 n 小于或等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 节点 为 做 红黑树_最小_扩展 用 树。
    可变 已收集 为 0。
    只要 节点 不等于 空 并且 已收集 小于 n：
        做 追加 用 结果、一组：做 红黑树_节点键 用 节点、做 红黑树_节点值 用 节点。
        节点 为 做 红黑树_后继_扩展 用 树、节点。
        已收集 为 已收集 加 1。
    完。
    返还 结果。
完。

红黑树_收集后N 接受 树、n：
    若 树 等于 空 或者 n 小于或等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 节点 为 做 红黑树_最大_扩展 用 树。
    可变 已收集 为 0。
    只要 节点 不等于 空 并且 已收集 小于 n：
        做 追加 用 结果、一组：做 红黑树_节点键 用 节点、做 红黑树_节点值 用 节点。
        节点 为 做 红黑树_前驱_扩展 用 树、节点。
        已收集 为 已收集 加 1。
    完。
    返还 结果。
完。

红黑树_节点数 接受 树：
    若 树 等于 空：
        返还 0。
    完。
    返还 树 的 第 1 项。
完。

红黑树_安全验证 接受 树：
    全局统计 的 验证次数 为 全局统计 的 验证次数 加 1。
    有序 为 做 红黑树_验证有序 用 树。
    若 有序 等于 真：
        全局统计 的 验证通过 为 全局统计 的 验证通过 加 1。
    完。
    返还 有序。
完。

AI_记录范围 接受 最小键、最大键：
    做 设置 用 AI范围历史、AI范围位置、一组：最小键、最大键。
    AI范围位置 为 (AI范围位置 加 1) 取余于 AI范围窗口。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
完。

AI_预测下次范围 接受 零参：
    可变 总最小 为 0。
    可变 总最大 为 0。
    可变 有效数 为 0。
    可变 i 为 0。
    只要 i 小于 AI范围窗口：
        若 AI范围历史 的 第 i 项 的 第 0 项 不等于 0 或者 AI范围历史 的 第 i 项 的 第 1 项 不等于 0：
            总最小 为 总最小 加 AI范围历史 的 第 i 项 的 第 0 项。
            总最大 为 总最大 加 AI范围历史 的 第 i 项 的 第 1 项。
            有效数 为 有效数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 有效数 等于 0：
        返还 一组：0、0。
    完。
    全局统计 的 AI预测命中 为 全局统计 的 AI预测命中 加 1。
    返还 一组：总最小 除以 有效数、总最大 除以 有效数。
完。

AI_建议预加载范围 接受 树：
    预测 为 做 AI_预测下次范围。
    若 预测 的 第 0 项 等于 0 并且 预测 的 第 1 项 等于 0：
        返还 一组：（空）。
    完。
    返还 做 红黑树_收集范围 用 树、预测 的 第 0 项、预测 的 第 1 项。
完。

红黑树扩展_转储 接受 树：
    若 树 等于 空：
        做 打印 用 「[红黑树扩展] 空树」。
        返还 0。
    完。
    做 打印 用 「[红黑树扩展] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  节点数: 」、做 整数转字符串 用 做 红黑树_节点数 用 树。
    做 打印 用 做 字符串拼接 用 「  高度: 」、做 整数转字符串 用 做 红黑树_高度_扩展 用 树。
    做 打印 用 做 字符串拼接 用 「  有序: 」、做 整数转字符串 用 做 红黑树_验证有序 用 树。
    最小 为 做 红黑树_最小_扩展 用 树。
    最大 为 做 红黑树_最大_扩展 用 树。
    若 最小 不等于 空：
        做 打印 用 做 字符串拼接 用 「  最小键: 」、做 整数转字符串 用 做 红黑树_节点键 用 最小。
    完。
    若 最大 不等于 空：
        做 打印 用 做 字符串拼接 用 「  最大键: 」、做 整数转字符串 用 做 红黑树_节点键 用 最大。
    完。
    返还 0。
完。

红黑树扩展_完整统计 接受 树：
    返还 一组：做 红黑树_节点数 用 树、做 红黑树_高度_扩展 用 树、做 红黑树_验证有序 用 树。
完。

红黑树扩展_打印统计 接受 零参：
    做 打印 用 「[红黑树扩展] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  中序遍历: 」、做 整数转字符串 用 全局统计 的 中序遍历次数。
    做 打印 用 做 字符串拼接 用 「  逆序遍历: 」、做 整数转字符串 用 全局统计 的 逆序遍历次数。
    做 打印 用 做 字符串拼接 用 「  范围查询: 」、做 整数转字符串 用 全局统计 的 范围查询次数。
    做 打印 用 做 字符串拼接 用 「  范围命中: 」、做 整数转字符串 用 全局统计 的 范围查询命中。
    做 打印 用 做 字符串拼接 用 「  ≥查找: 」、做 整数转字符串 用 全局统计 的 大于等于查找次数。
    做 打印 用 做 字符串拼接 用 「  ≤查找: 」、做 整数转字符串 用 全局统计 的 小于等于查找次数。
    做 打印 用 做 字符串拼接 用 「  第K小: 」、做 整数转字符串 用 全局统计 的 第K小查找次数。
    做 打印 用 做 字符串拼接 用 「  范围计数: 」、做 整数转字符串 用 全局统计 的 范围计数次数。
    做 打印 用 做 字符串拼接 用 「  范围删除: 」、做 整数转字符串 用 全局统计 的 范围删除次数。
    做 打印 用 做 字符串拼接 用 「  转数组: 」、做 整数转字符串 用 全局统计 的 转数组次数。
    做 打印 用 做 字符串拼接 用 「  验证次数: 」、做 整数转字符串 用 全局统计 的 验证次数。
    做 打印 用 做 字符串拼接 用 「  验证通过: 」、做 整数转字符串 用 全局统计 的 验证通过。
    做 打印 用 做 字符串拼接 用 「  AI预测: 」、做 整数转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI命中: 」、做 整数转字符串 用 全局统计 的 AI预测命中。
    做 打印 用 做 字符串拼接 用 「  批量操作: 」、做 整数转字符串 用 全局统计 的 批量操作次数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 整数转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

红黑树扩展_注册到内核 接受 内核上下文：
    若 扩展模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[红黑树扩展] 已注册到内核上下文」。
    返还 0。
完。

红黑树扩展_查询状态 接受 零参：
    若 扩展模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

红黑树扩展_健康检查 接受 零参：
    若 扩展模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 范围查询次数 加 全局统计 的 中序遍历次数：
        返还 假。
    完。
    返还 真。
完。

红黑树_键值对数组 接受 树：
    若 树 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 转数组次数 为 全局统计 的 转数组次数 加 1。
    返还 做 红黑树_转数组 用 树。
完。

红黑树_中位数键 接受 树：
    总数 为 做 红黑树_节点数 用 树。
    若 总数 等于 0：
        返还 0。
    完。
    中间 为 (总数 加 1) 除以 2。
    节点 为 做 红黑树_第K小 用 树、中间。
    若 节点 等于 空：
        返还 0。
    完。
    返还 做 红黑树_节点键 用 节点。
完。

本章 收束。
