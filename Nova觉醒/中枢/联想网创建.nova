《联想网创建》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·TNF推理引擎》。

引用《.·整数转字符串》。

引用《.·内核打印》。

联想网_最大维度 为 512。

联想网_默认β 为 1000。

联想网_收敛阈值 为 5。

联想网统计 是 一种 事物，含 创建次数、存储次数、回忆次数、同步回忆次数、现代回忆次数、能量计算次数、总回忆步数、收敛次数、未收敛次数。

可变 全局_联想网统计 为 空。

联想网统计_初始化 接受 零参：
    若 全局_联想网统计 等于 空：
        全局_联想网统计 为 新建 联想网统计。
        全局_联想网统计 的 创建次数 为 0。
        全局_联想网统计 的 存储次数 为 0。
        全局_联想网统计 的 回忆次数 为 0。
        全局_联想网统计 的 同步回忆次数 为 0。
        全局_联想网统计 的 现代回忆次数 为 0。
        全局_联想网统计 的 能量计算次数 为 0。
        全局_联想网统计 的 总回忆步数 为 0。
        全局_联想网统计 的 收敛次数 为 0。
        全局_联想网统计 的 未收敛次数 为 0。
    完。
完。

Nova联想网络_创建 接受 维度：
    做 联想网统计_初始化。
    若 维度 小于或等于 0：
        维度 为 8。
    完。
    若 维度 大于 联想网_最大维度：
        维度 为 联想网_最大维度。
    完。
    可变 W 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度 乘 维度：
        做 TNF_追加 用 W、0。
        i 为 i 加 1。
    完。
    全局_联想网统计 的 创建次数 为 全局_联想网统计 的 创建次数 加 1。
    返还 一组：W、维度、一组：（空）。
完。

Nova联想网络_存储 接受 网络、模式：
    做 联想网统计_初始化。
    d 为 网络 的 第 1 项。
    若 做 计数 用 模式 小于 d：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 d：
        可变 j 为 0。
        只要 j 小于 d：
            若 i 不等于 j：
                网络 的 第 0 项 的 第 i 乘 d 加 j 项 为 网络 的 第 0 项 的 第 i 乘 d 加 j 项 加 模式 的 第 i 项 乘 模式 的 第 j 项 除以 1000。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    做 TNF_追加 用 网络 的 第 2 项、模式。
    全局_联想网统计 的 存储次数 为 全局_联想网统计 的 存储次数 加 1。
完。

Nova联想网络_批量存储 接受 网络、模式列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 模式列表：
        做 Nova联想网络_存储 用 网络、模式列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

Nova联想网络_删除模式 接受 网络、模式：
    d 为 网络 的 第 1 项。
    若 做 计数 用 模式 小于 d：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 d：
        可变 j 为 0。
        只要 j 小于 d：
            若 i 不等于 j：
                网络 的 第 0 项 的 第 i 乘 d 加 j 项 为 网络 的 第 0 项 的 第 i 乘 d 加 j 项 减 模式 的 第 i 项 乘 模式 的 第 j 项 除以 1000。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

Nova联想网络_回忆 接受 网络、输入、步数：
    做 联想网统计_初始化。
    d 为 网络 的 第 1 项。
    可变 状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d：
        若 i 小于 做 计数 用 输入：
            做 TNF_追加 用 状态、输入 的 第 i 项。
        否则：
            做 TNF_追加 用 状态、0。
        完。
        i 为 i 加 1。
    完。
    可变 step 为 0。
    只要 step 小于 步数：
        i 为 做 TNF_随机 取余于 d。
        可变 h 为 0。
        可变 j 为 0。
        只要 j 小于 d：
            h 为 h 加 网络 的 第 0 项 的 第 i 乘 d 加 j 项 乘 状态 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        若 h 大于或等于 0：
            状态 的 第 i 项 为 1000。
        否则：
            状态 的 第 i 项 为 0 减 1000。
        完。
        step 为 step 加 1。
    完。
    全局_联想网统计 的 回忆次数 为 全局_联想网统计 的 回忆次数 加 1。
    全局_联想网统计 的 总回忆步数 为 全局_联想网统计 的 总回忆步数 加 步数。
    返还 状态。
完。

Nova联想网络_同步回忆 接受 网络、输入、最大迭代：
    做 联想网统计_初始化。
    d 为 网络 的 第 1 项。
    可变 状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d：
        若 i 小于 做 计数 用 输入：
            做 TNF_追加 用 状态、输入 的 第 i 项。
        否则：
            做 TNF_追加 用 状态、0。
        完。
        i 为 i 加 1。
    完。
    可变 iter 为 0。
    可变 已收敛 为 假。
    只要 iter 小于 最大迭代 并且 非 已收敛：
        可变 新状态 为 一组：（空）。
        i 为 0。
        只要 i 小于 d：
            可变 h 为 0。
            可变 j 为 0。
            只要 j 小于 d：
                h 为 h 加 网络 的 第 0 项 的 第 i 乘 d 加 j 项 乘 状态 的 第 j 项 除以 1000。
                j 为 j 加 1。
            完。
            若 h 大于或等于 0：
                做 TNF_追加 用 新状态、1000。
            否则：
                做 TNF_追加 用 新状态、0 减 1000。
            完。
            i 为 i 加 1。
        完。
        可变 变化 为 0。
        i 为 0。
        只要 i 小于 d：
            若 新状态 的 第 i 项 不等于 状态 的 第 i 项：
                变化 为 变化 加 1。
            完。
            i 为 i 加 1。
        完。
        状态 为 新状态。
        若 变化 小于或等于 联想网_收敛阈值：
            已收敛 为 真。
        完。
        iter 为 iter 加 1。
    完。
    全局_联想网统计 的 同步回忆次数 为 全局_联想网统计 的 同步回忆次数 加 1。
    若 已收敛：
        全局_联想网统计 的 收敛次数 为 全局_联想网统计 的 收敛次数 加 1。
    否则：
        全局_联想网统计 的 未收敛次数 为 全局_联想网统计 的 未收敛次数 加 1。
    完。
    返还 状态。
完。

Nova联想网络_能量 接受 网络、状态：
    做 联想网统计_初始化。
    全局_联想网统计 的 能量计算次数 为 全局_联想网统计 的 能量计算次数 加 1。
    d 为 网络 的 第 1 项。
    可变 E 为 0。
    可变 i 为 0。
    只要 i 小于 d：
        可变 j 为 i 加 1。
        只要 j 小于 d：
            E 为 E 减 网络 的 第 0 项 的 第 i 乘 d 加 j 项 乘 状态 的 第 i 项 乘 状态 的 第 j 项 除以 1000000。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 E。
完。

Nova联想网络_模式相似度 接受 模式a、模式b：
    n 为 做 计数 用 模式a。
    若 n 等于 0 或者 n 不等于 做 计数 用 模式b：
        返还 0。
    完。
    可变 匹配 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 模式a 的 第 i 项 大于 0 并且 模式b 的 第 i 项 大于 0 或者 模式a 的 第 i 项 小于或等于 0 并且 模式b 的 第 i 项 小于或等于 0：
            匹配 为 匹配 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 匹配 乘 1000 除以 n。
完。

Nova联想网络_找最近模式 接受 网络、查询：
    模式列表 为 网络 的 第 2 项。
    可变 最佳索引 为 负 1。
    可变 最佳相似 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 模式列表：
        相似 为 做 Nova联想网络_模式相似度 用 查询、模式列表 的 第 i 项。
        若 相似 大于 最佳相似：
            最佳相似 为 相似。
            最佳索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最佳索引。
完。

Nova联想网络_容量 接受 网络：
    d 为 网络 的 第 1 项。
    返还 d 乘 138 除以 1000。
完。

Nova联想网络_负载率 接受 网络：
    已存 为 做 计数 用 网络 的 第 2 项。
    容量 为 做 Nova联想网络_容量 用 网络。
    若 容量 等于 0：
        返还 1000。
    完。
    返还 已存 乘 1000 除以 容量。
完。

Nova联想网络_权重归一化 接受 网络：
    d 为 网络 的 第 1 项。
    可变 最大值 为 0。
    可变 i 为 0。
    只要 i 小于 d 乘 d：
        v 为 网络 的 第 0 项 的 第 i 项。
        若 v 小于 0：
            v 为 0 减 v。
        完。
        若 v 大于 最大值：
            最大值 为 v。
        完。
        i 为 i 加 1。
    完。
    若 最大值 等于 0：
        返还。
    完。
    i 为 0。
    只要 i 小于 d 乘 d：
        网络 的 第 0 项 的 第 i 项 为 网络 的 第 0 项 的 第 i 项 乘 1000 除以 最大值。
        i 为 i 加 1。
    完。
完。

Nova联想网络_权重稀疏率 接受 网络：
    d 为 网络 的 第 1 项。
    总数 为 d 乘 d。
    若 总数 等于 0：
        返还 0。
    完。
    可变 零数 为 0。
    可变 i 为 0。
    只要 i 小于 总数：
        若 网络 的 第 0 项 的 第 i 项 等于 0：
            零数 为 零数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 零数 乘 1000 除以 总数。
完。

Nova联想网络_重置 接受 网络：
    d 为 网络 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 d 乘 d：
        网络 的 第 0 项 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    网络 的 第 2 项 为 一组：（空）。
完。

Nova联想网络_序列化 接受 网络：
    d 为 网络 的 第 1 项。
    可变 数据 为 一组：d、做 计数 用 网络 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 d 乘 d：
        做 TNF_追加 用 数据、网络 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 网络 的 第 2 项：
        可变 j 为 0。
        只要 j 小于 d：
            做 TNF_追加 用 数据、网络 的 第 2 项 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数据。
完。

Nova联想网络_反序列化 接受 数据：
    若 做 计数 用 数据 小于 2：
        返还 空。
    完。
    d 为 数据 的 第 0 项。
    M 为 数据 的 第 1 项。
    若 做 计数 用 数据 小于 2 加 d 乘 d：
        返还 空。
    完。
    可变 W 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d 乘 d：
        做 TNF_追加 用 W、数据 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    可变 模式 为 一组：（空）。
    偏移 为 2 加 d 乘 d。
    i 为 0。
    只要 i 小于 M：
        可变 p 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 d：
            若 偏移 加 i 乘 d 加 j 小于 做 计数 用 数据：
                做 TNF_追加 用 p、数据 的 第 偏移 加 i 乘 d 加 j 项。
            否则：
                做 TNF_追加 用 p、0。
            完。
            j 为 j 加 1。
        完。
        做 TNF_追加 用 模式、p。
        i 为 i 加 1。
    完。
    返还 一组：W、d、模式。
完。

现代Nova联想网络_回忆 接受 模式列表、查询、β：
    做 联想网统计_初始化。
    若 β 小于或等于 0：
        β 为 联想网_默认β。
    完。
    M 为 做 计数 用 模式列表。
    若 M 等于 0：
        返还 查询。
    完。
    d 为 做 计数 用 查询。
    可变 分数 为 一组：（空）。
    可变 max_s 为 0 减 0x7FFFFFFF。
    可变 i 为 0。
    只要 i 小于 M：
        可变 s 为 0。
        可变 j 为 0。
        只要 j 小于 d：
            若 j 小于 做 计数 用 模式列表 的 第 i 项：
                s 为 s 加 模式列表 的 第 i 项 的 第 j 项 乘 查询 的 第 j 项 除以 1000。
            完。
            j 为 j 加 1。
        完。
        s 为 β 乘 s 除以 1000。
        做 TNF_追加 用 分数、s。
        若 s 大于 max_s：
            max_s 为 s。
        完。
        i 为 i 加 1。
    完。
    可变 exps 为 一组：（空）。
    可变 总 为 0。
    i 为 0。
    只要 i 小于 M：
        可变 e 为 1000 加 (分数 的 第 i 项 减 max_s)。
        若 e 小于 1：
            e 为 1。
        完。
        做 TNF_追加 用 exps、e。
        总 为 总 加 e。
        i 为 i 加 1。
    完。
    若 总 等于 0：
        总 为 1。
    完。
    可变 输出 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 d：
        做 TNF_追加 用 输出、0。
        j 为 j 加 1。
    完。
    i 为 0。
    只要 i 小于 M：
        α 为 exps 的 第 i 项 乘 1000 除以 总。
        j 为 0。
        只要 j 小于 d：
            若 j 小于 做 计数 用 模式列表 的 第 i 项：
                输出 的 第 j 项 为 输出 的 第 j 项 加 α 乘 模式列表 的 第 i 项 的 第 j 项 除以 1000。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    全局_联想网统计 的 现代回忆次数 为 全局_联想网统计 的 现代回忆次数 加 1。
    返还 输出。
完。

现代Nova联想网络_多步回忆 接受 模式列表、查询、β、步数：
    可变 状态 为 查询。
    可变 step 为 0。
    只要 step 小于 步数：
        状态 为 做 现代Nova联想网络_回忆 用 模式列表、状态、β。
        step 为 step 加 1。
    完。
    返还 状态。
完。

联想网_打印统计 接受 零参：
    做 联想网统计_初始化。
    做 打印行 用 「=== 联想网络统计 ===」。
    做 打印行 用 「创建: 」 加 做 整数转字符串 用 全局_联想网统计 的 创建次数。
    做 打印行 用 「存储: 」 加 做 整数转字符串 用 全局_联想网统计 的 存储次数。
    做 打印行 用 「异步回忆: 」 加 做 整数转字符串 用 全局_联想网统计 的 回忆次数。
    做 打印行 用 「同步回忆: 」 加 做 整数转字符串 用 全局_联想网统计 的 同步回忆次数。
    做 打印行 用 「现代回忆: 」 加 做 整数转字符串 用 全局_联想网统计 的 现代回忆次数。
    做 打印行 用 「能量计算: 」 加 做 整数转字符串 用 全局_联想网统计 的 能量计算次数。
    做 打印行 用 「总步数: 」 加 做 整数转字符串 用 全局_联想网统计 的 总回忆步数。
    做 打印行 用 「收敛: 」 加 做 整数转字符串 用 全局_联想网统计 的 收敛次数。
    做 打印行 用 「未收敛: 」 加 做 整数转字符串 用 全局_联想网统计 的 未收敛次数。
完。

联想网_完整统计 接受 零参：
    做 联想网统计_初始化。
    返还 一组：全局_联想网统计 的 创建次数、全局_联想网统计 的 存储次数、全局_联想网统计 的 回忆次数、全局_联想网统计 的 同步回忆次数、全局_联想网统计 的 现代回忆次数、全局_联想网统计 的 能量计算次数、全局_联想网统计 的 总回忆步数、全局_联想网统计 的 收敛次数。
完。

联想网_重置统计 接受 零参：
    若 全局_联想网统计 不等于 空：
        全局_联想网统计 的 创建次数 为 0。
        全局_联想网统计 的 存储次数 为 0。
        全局_联想网统计 的 回忆次数 为 0。
        全局_联想网统计 的 同步回忆次数 为 0。
        全局_联想网统计 的 现代回忆次数 为 0。
        全局_联想网统计 的 能量计算次数 为 0。
        全局_联想网统计 的 总回忆步数 为 0。
        全局_联想网统计 的 收敛次数 为 0。
        全局_联想网统计 的 未收敛次数 为 0。
    完。
完。

联想网_获取创建数 接受 零参：
    做 联想网统计_初始化。
    返还 全局_联想网统计 的 创建次数。
完。

联想网_获取收敛率 接受 零参：
    做 联想网统计_初始化。
    总 为 全局_联想网统计 的 收敛次数 加 全局_联想网统计 的 未收敛次数。
    若 总 等于 0：
        返还 0。
    完。
    返还 全局_联想网统计 的 收敛次数 乘 1000 除以 总。
完。

联想网_获取存储数 接受 零参：
    做 联想网统计_初始化。
    返还 全局_联想网统计 的 存储次数。
完。

联想网_获取回忆数 接受 零参：
    做 联想网统计_初始化。
    返还 全局_联想网统计 的 回忆次数。
完。

联想网_容量估计 接受 维度：
    返还 维度 乘 140 除以 1000。
完。

联想网_存储利用率 接受 网络、已存储数：
    容量 为 做 联想网_容量估计 用 网络 的 第 0 项。
    若 容量 等于 0：
        返还 1000。
    完。
    返还 已存储数 乘 1000 除以 容量。
完。

联想网_模式相似度 接受 模式A、模式B、维度：
    可变 点积 为 0。
    可变 范A 为 0。
    可变 范B 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        点积 为 点积 加 模式A 的 第 i 项 乘 模式B 的 第 i 项 除以 1000。
        范A 为 范A 加 模式A 的 第 i 项 乘 模式A 的 第 i 项 除以 1000。
        范B 为 范B 加 模式B 的 第 i 项 乘 模式B 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    若 范A 等于 0 或者 范B 等于 0：
        返还 0。
    完。
    可变 sqA 为 范A。
    可变 iter 为 0。
    只要 iter 小于 8：
        sqA 为 (sqA 加 范A 乘 1000 除以 sqA) 除以 2。
        iter 为 iter 加 1。
    完。
    可变 sqB 为 范B。
    iter 为 0。
    只要 iter 小于 8：
        sqB 为 (sqB 加 范B 乘 1000 除以 sqB) 除以 2。
        iter 为 iter 加 1。
    完。
    若 sqA 等于 0 或者 sqB 等于 0：
        返还 0。
    完。
    返还 点积 乘 1000 除以 (sqA 乘 sqB 除以 1000)。
完。

联想网_干扰检测 接受 模式列表、维度：
    模式数 为 做 计数 用 模式列表。
    可变 最大相似 为 0。
    可变 i 为 0。
    只要 i 小于 模式数：
        可变 j 为 i 加 1。
        只要 j 小于 模式数：
            sim 为 做 联想网_模式相似度 用 模式列表 的 第 i 项、模式列表 的 第 j 项、维度。
            若 sim 大于 最大相似：
                最大相似 为 sim。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 最大相似。
完。

联想网_加噪声 接受 模式、维度、翻转率_千分：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        随机值 为 (i 乘 7 加 13) 取余于 1000。
        若 随机值 小于 翻转率_千分：
            做 追加 用 结果、0 减 模式 的 第 i 项。
        否则：
            做 追加 用 结果、模式 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

联想网_鲁棒性测试 接受 模式列表、查询、维度、β、翻转率：
    噪声查询 为 做 联想网_加噪声 用 查询、维度、翻转率。
    结果 为 做 现代Nova联想网络_回忆 用 模式列表、噪声查询、β。
    返还 做 联想网_模式相似度 用 结果、查询、维度。
完。

可变 AI联想_联想命中 为 0。

AI联想_相似度检索 接受 查询向量、阈值：
    AI联想_联想命中 为 AI联想_联想命中 加 1。
    若 阈值 小于 500：
        返还 「宽松检索」。
    完。
    返还 「精确检索」。
完。

本章 收束。
