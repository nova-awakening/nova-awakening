《负载统计》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《..·感知·内核直连层》。

引用《.·内存初始化》。

衰减系数 为 978。

基数 为 1024。

均值间隔 为 5000000000。

预测深度 为 8。

热迁移阈值 为 256。

冷迁移阈值 为 64。

最大核数 为 256。

核负载 是 一种 事物，含 即时负载、任务数、利用率、加权负载、运行总时、空闲总时、切换次数、迁入数、迁出数、预测趋势、历史环、历史写位、热度分级、功耗估算。

可变 负载表 为 一组：（空）。

可变 核数 为 1。

可变 均值一分 为 0。

可变 均值五分 为 0。

可变 均值十五分 为 0。

可变 上次均值时刻 为 0。

可变 全局迁移次数 为 0。

可变 神经均衡权重 为 512。

负载_初始化 接受 数量：
    核数 为 数量。
    若 核数 大于 最大核数：
        核数 为 最大核数。
    完。
    负载表 为 一组：（空）。
    可变 序号 为 0。
    只要 序号 小于 核数：
        核 为 新建 核负载。
        核 的 即时负载 为 0。
        核 的 任务数 为 0。
        核 的 利用率 为 0。
        核 的 加权负载 为 0。
        核 的 运行总时 为 0。
        核 的 空闲总时 为 0。
        核 的 切换次数 为 0。
        核 的 迁入数 为 0。
        核 的 迁出数 为 0。
        核 的 预测趋势 为 0。
        核 的 历史环 为 做 堆_分配 用 预测深度 乘 4。
        核 的 历史写位 为 0。
        核 的 热度分级 为 1。
        核 的 功耗估算 为 0。
        做 追加 用 负载表、核。
        序号 为 序号 加 1。
    完。
    均值一分 为 0。
    均值五分 为 0。
    均值十五分 为 0。
    上次均值时刻 为 做 时钟_读取。
完。

负载_采样 接受 核号、任务数：
    若 核号 大于或等于 核数：
        返还。
    完。
    核 为 负载表 的 第 核号 项。
    核 的 任务数 为 任务数。
    核 的 利用率 为 核 的 利用率 乘 衰减系数 除以 基数。
    若 任务数 大于 0：
        核 的 利用率 为 核 的 利用率 加 基数 除以 10。
        若 核 的 利用率 大于 基数：
            核 的 利用率 为 基数。
        完。
    完。
    核 的 加权负载 为 核 的 加权负载 乘 衰减系数 除以 基数 加 任务数 乘 10。
    核 的 即时负载 为 核 的 加权负载。
    做 内存_写32 用 核 的 历史环 加 核 的 历史写位 乘 4、核 的 利用率。
    核 的 历史写位 为 (核 的 历史写位 加 1) 取余于 预测深度。
    核 的 预测趋势 为 做 负载_计算趋势 用 核。
    做 负载_更新热度 用 核。
    做 负载_估算功耗 用 核。
完。

负载_计算趋势 接受 核：
    可变 前半和 为 0。
    可变 后半和 为 0。
    半深 为 预测深度 除以 2。
    可变 索引 为 0。
    只要 索引 小于 半深：
        前半和 为 前半和 加 做 内存_读32 用 核 的 历史环 加 索引 乘 4。
        索引 为 索引 加 1。
    完。
    只要 索引 小于 预测深度：
        后半和 为 后半和 加 做 内存_读32 用 核 的 历史环 加 索引 乘 4。
        索引 为 索引 加 1。
    完。
    若 前半和 等于 0：
        返还 0。
    完。
    返还 (后半和 减 前半和) 乘 基数 除以 前半和。
完。

负载_更新热度 接受 核：
    若 核 的 利用率 大于 800：
        核 的 热度分级 为 3。
    否则若 核 的 利用率 大于 400：
        核 的 热度分级 为 2。
    否则若 核 的 利用率 大于 100：
        核 的 热度分级 为 1。
    否则：
        核 的 热度分级 为 0。
    完。
完。

负载_估算功耗 接受 核：
    核 的 功耗估算 为 核 的 利用率 乘 核 的 利用率 除以 基数 加 核 的 切换次数 除以 100。
完。

负载_记录切换 接受 核号：
    若 核号 小于 核数：
        负载表 的 第 核号 项 的 切换次数 为 负载表 的 第 核号 项 的 切换次数 加 1。
    完。
完。

负载_更新均值 接受 零参：
    现在 为 做 时钟_读取。
    若 现在 减 上次均值时刻 小于 均值间隔：
        返还。
    完。
    上次均值时刻 为 现在。
    可变 活跃 为 0。
    可变 序号 为 0。
    只要 序号 小于 核数：
        活跃 为 活跃 加 负载表 的 第 序号 项 的 任务数。
        序号 为 序号 加 1。
    完。
    活跃百 为 活跃 乘 100。
    均值一分 为 (均值一分 乘 1884 加 活跃百 乘 164) 除以 2048。
    均值五分 为 (均值五分 乘 2014 加 活跃百 乘 34) 除以 2048。
    均值十五分 为 (均值十五分 乘 2037 加 活跃百 乘 11) 除以 2048。
完。

负载_找最闲 接受 零参：
    可变 最闲核 为 0。
    可变 最低 为 负载表 的 第 0 项 的 加权负载。
    可变 序号 为 1。
    只要 序号 小于 核数：
        综合分 为 负载表 的 第 序号 项 的 加权负载 乘 3 减 负载表 的 第 序号 项 的 预测趋势。
        当前最低综合 为 最低 乘 3 减 负载表 的 第 最闲核 项 的 预测趋势。
        若 综合分 小于 当前最低综合：
            最低 为 负载表 的 第 序号 项 的 加权负载。
            最闲核 为 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 最闲核。
完。

负载_找最忙 接受 零参：
    可变 最忙核 为 0。
    可变 最高 为 负载表 的 第 0 项 的 加权负载。
    可变 序号 为 1。
    只要 序号 小于 核数：
        综合分 为 负载表 的 第 序号 项 的 加权负载 乘 3 加 负载表 的 第 序号 项 的 预测趋势。
        当前最高综合 为 最高 乘 3 加 负载表 的 第 最忙核 项 的 预测趋势。
        若 综合分 大于 当前最高综合：
            最高 为 负载表 的 第 序号 项 的 加权负载。
            最忙核 为 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 最忙核。
完。

负载_需均衡 接受 零参：
    若 核数 小于或等于 1：
        返还 假。
    完。
    忙核 为 做 负载_找最忙。
    闲核 为 做 负载_找最闲。
    差值 为 负载表 的 第 忙核 项 的 加权负载 减 负载表 的 第 闲核 项 的 加权负载。
    动态阈值 为 负载表 的 第 忙核 项 的 加权负载 乘 神经均衡权重 除以 (基数 乘 4)。
    返还 差值 大于 动态阈值。
完。

负载_神经均衡决策 接受 零参：
    若 非 做 负载_需均衡：
        返还 负 1。
    完。
    忙核 为 做 负载_找最忙。
    闲核 为 做 负载_找最闲。
    若 负载表 的 第 忙核 项 的 热度分级 大于或等于 3 并且 负载表 的 第 闲核 项 的 热度分级 小于或等于 1：
        负载表 的 第 忙核 项 的 迁出数 为 负载表 的 第 忙核 项 的 迁出数 加 1。
        负载表 的 第 闲核 项 的 迁入数 为 负载表 的 第 闲核 项 的 迁入数 加 1。
        全局迁移次数 为 全局迁移次数 加 1。
        若 全局迁移次数 取余于 10 等于 0：
            做 负载_调整神经权重。
        完。
        返还 闲核。
    完。
    返还 负 1。
完。

负载_调整神经权重 接受 零参：
    可变 总方差 为 0。
    可变 总均 为 0。
    可变 序号 为 0。
    只要 序号 小于 核数：
        总均 为 总均 加 负载表 的 第 序号 项 的 加权负载。
        序号 为 序号 加 1。
    完。
    总均 为 总均 除以 核数。
    序号 为 0。
    只要 序号 小于 核数：
        偏差 为 负载表 的 第 序号 项 的 加权负载 减 总均。
        总方差 为 总方差 加 偏差 乘 偏差。
        序号 为 序号 加 1。
    完。
    总方差 为 总方差 除以 核数。
    若 总方差 大于 基数 乘 基数：
        神经均衡权重 为 神经均衡权重 乘 90 除以 100。
        若 神经均衡权重 小于 128：
            神经均衡权重 为 128。
        完。
    否则若 总方差 小于 基数 乘 64：
        神经均衡权重 为 神经均衡权重 乘 110 除以 100。
        若 神经均衡权重 大于 1024：
            神经均衡权重 为 1024。
        完。
    完。
完。

负载_总功耗 接受 零参：
    可变 总 为 0。
    可变 序号 为 0。
    只要 序号 小于 核数：
        总 为 总 加 负载表 的 第 序号 项 的 功耗估算。
        序号 为 序号 加 1。
    完。
    返还 总。
完。

负载_获取均值 接受 零参：
    返还 一组：均值一分、均值五分、均值十五分。
完。

负载_获取利用率 接受 核号：
    若 核号 大于或等于 核数：
        返还 0。
    完。
    返还 负载表 的 第 核号 项 的 利用率 乘 100 除以 基数。
完。

负载_总任务 接受 零参：
    可变 总 为 0。
    可变 序号 为 0。
    只要 序号 小于 核数：
        总 为 总 加 负载表 的 第 序号 项 的 任务数。
        序号 为 序号 加 1。
    完。
    返还 总。
完。

负载_总切换 接受 零参：
    可变 总 为 0。
    可变 序号 为 0。
    只要 序号 小于 核数：
        总 为 总 加 负载表 的 第 序号 项 的 切换次数。
        序号 为 序号 加 1。
    完。
    返还 总。
完。

负载_平均利用率 接受 零参：
    若 核数 等于 0：
        返还 0。
    完。
    可变 总 为 0。
    可变 序号 为 0。
    只要 序号 小于 核数：
        总 为 总 加 负载表 的 第 序号 项 的 利用率。
        序号 为 序号 加 1。
    完。
    返还 总 乘 100 除以 (核数 乘 基数)。
完。

负载_完整统计 接受 零参：
    返还 一组：均值一分、均值五分、均值十五分、做 负载_总任务、做 负载_总切换、做 负载_平均利用率、做 负载_总功耗、全局迁移次数、神经均衡权重。
完。

负载_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 均值一分 大于 核数 乘 基数 乘 2：
        做 追加 用 问题、「1分钟负载过高」。
    完。
    若 做 负载_平均利用率 大于 95：
        做 追加 用 问题、「CPU利用率过高: 」 加 做 整数转字符串 用 做 负载_平均利用率 加 「%」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「负载统计健康」。
    完。
    返还 问题。
完。

负载_是否过载 接受 零参：
    返还 均值一分 大于 核数 乘 基数。
完。

负载_重置迁移 接受 零参：
    全局迁移次数 为 0。
完。

负载_趋势 接受 零参：
    若 均值一分 大于 均值五分：
        返还 「上升」。
    完。
    若 均值一分 小于 均值五分：
        返还 「下降」。
    完。
    返还 「稳定」。
完。

负载_空闲核数 接受 零参：
    可变 空闲 为 0。
    可变 i 为 0。
    只要 i 小于 核数：
        若 负载表 的 第 i 项 的 利用率 小于 基数 除以 10：
            空闲 为 空闲 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 空闲。
完。

本章 收束。
