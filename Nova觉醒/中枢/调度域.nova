《调度域》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·内容可见性》。

域级_核心 为 0。

域级_封装 为 1。

域级_节点 为 2。

域级_全局 为 3。

最大域数 为 16。

均衡间隔_核心 为 4。

均衡间隔_封装 为 16。

均衡间隔_节点 为 64。

调度域 是 一种 事物，含 级别、核掩码、核数量、均衡间隔、上次均衡、均衡尝试、均衡成功、均衡失败、迁移量、不均衡累计、父域、子域列表、子域数、神经均衡权重。

可变 域表 为 一组：（空）。

可变 域数 为 0。

可变 全局尝试 为 0。

可变 全局成功 为 0。

可变 全局失败 为 0。

域_创建 接受 级别、核掩码、核数量：
    若 域数 大于或等于 最大域数：
        返还 负 1。
    完。
    域 为 新建 调度域。
    域 的 级别 为 级别。
    域 的 核掩码 为 核掩码。
    域 的 核数量 为 核数量。
    若 级别 等于 域级_核心：
        域 的 均衡间隔 为 均衡间隔_核心。
    否则若 级别 等于 域级_封装：
        域 的 均衡间隔 为 均衡间隔_封装。
    否则：
        域 的 均衡间隔 为 均衡间隔_节点。
    完。
    域 的 上次均衡 为 0。
    域 的 均衡尝试 为 0。
    域 的 均衡成功 为 0。
    域 的 均衡失败 为 0。
    域 的 迁移量 为 0。
    域 的 不均衡累计 为 0。
    域 的 父域 为 0。
    域 的 子域列表 为 做 堆_分配 用 最大域数 乘 8。
    域 的 子域数 为 0。
    域 的 神经均衡权重 为 512。
    做 追加 用 域表、域。
    索引 为 域数。
    域数 为 域数 加 1。
    返还 索引。
完。

域_设父 接受 子索引、父索引：
    若 子索引 大于或等于 域数 或者 父索引 大于或等于 域数：
        返还。
    完。
    域表 的 第 子索引 项 的 父域 为 父索引。
    父 为 域表 的 第 父索引 项。
    做 内存_写64 用 父 的 子域列表 加 父 的 子域数 乘 8、子索引。
    父 的 子域数 为 父 的 子域数 加 1。
完。

域_检查均衡 接受 域索引、当前时刻：
    若 域索引 大于或等于 域数：
        返还 假。
    完。
    域 为 域表 的 第 域索引 项。
    返还 当前时刻 减 域 的 上次均衡 大于或等于 域 的 均衡间隔。
完。

域_执行均衡 接受 域索引、负载获取函数：
    若 域索引 大于或等于 域数：
        返还。
    完。
    域 为 域表 的 第 域索引 项。
    域 的 均衡尝试 为 域 的 均衡尝试 加 1。
    全局尝试 为 全局尝试 加 1。
    域 的 上次均衡 为 做 平台_获取高精度时间。
    可变 最忙核 为 负 1。
    可变 最闲核 为 负 1。
    可变 最高负载 为 0。
    可变 最低负载 为 999999。
    可变 位 为 0。
    只要 位 小于 64：
        若 域 的 核掩码 位与 1 左移 位 不等于 0：
            负载 为 做 负载获取函数 用 位。
            若 负载 大于 最高负载：
                最高负载 为 负载。
                最忙核 为 位。
            完。
            若 负载 小于 最低负载：
                最低负载 为 负载。
                最闲核 为 位。
            完。
        完。
        位 为 位 加 1。
    完。
    若 最忙核 等于 负 1 或者 最闲核 等于 负 1 或者 最忙核 等于 最闲核：
        域 的 均衡失败 为 域 的 均衡失败 加 1。
        全局失败 为 全局失败 加 1。
        返还。
    完。
    差值 为 最高负载 减 最低负载。
    阈值 为 最高负载 乘 域 的 神经均衡权重 除以 (1024 乘 4)。
    若 差值 大于 阈值：
        域 的 均衡成功 为 域 的 均衡成功 加 1。
        域 的 迁移量 为 域 的 迁移量 加 1。
        全局成功 为 全局成功 加 1。
        域 的 不均衡累计 为 0。
    否则：
        域 的 均衡失败 为 域 的 均衡失败 加 1。
        全局失败 为 全局失败 加 1。
        域 的 不均衡累计 为 域 的 不均衡累计 加 1。
    完。
    若 域 的 不均衡累计 大于 10：
        域 的 神经均衡权重 为 域 的 神经均衡权重 乘 90 除以 100。
        若 域 的 神经均衡权重 小于 128：
            域 的 神经均衡权重 为 128。
        完。
        域 的 不均衡累计 为 0。
    完。
完。

域_遍历均衡 接受 当前时刻、负载获取函数：
    可变 序号 为 0。
    只要 序号 小于 域数：
        若 做 域_检查均衡 用 序号、当前时刻：
            做 域_执行均衡 用 序号、负载获取函数。
        完。
        序号 为 序号 加 1。
    完。
完。

域_获取信息 接受 域索引：
    若 域索引 大于或等于 域数：
        返还 一组：0、0、0、0、0。
    完。
    域 为 域表 的 第 域索引 项。
    返还 一组：域 的 级别、域 的 核数量、域 的 均衡尝试、域 的 均衡成功、域 的 迁移量。
完。

调度域_完整统计 接受 零参：
    返还 一组：域数、全局尝试、全局成功、全局失败。
完。

本章 收束。
