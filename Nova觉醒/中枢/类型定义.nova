《类型定义》

引用《.·错误码》。

引用《.·整数转字符串》。

引用《.·硬链接》。

真 为 1。

假 为 0。

空指针 为 0。

整数8_最大 为 127。

整数8_最小 为 0 减 128。

无符号8_最大 为 255。

整数16_最大 为 32767。

整数16_最小 为 0 减 32768。

无符号16_最大 为 65535。

整数32_最大 为 2147483647。

整数32_最小 为 0 减 2147483648。

无符号32_最大 为 4294967295。

整数64_最大 为 9223372036854775807。

类型_空 为 0。

类型_整数 为 1。

类型_无符号 为 2。

类型_浮点 为 3。

类型_指针 为 4。

类型_数组 为 5。

类型_字符串 为 6。

类型_布尔 为 7。

类型_结构体 为 8。

大小_字节 为 1。

大小_字 为 2。

大小_双字 为 4。

大小_四字 为 8。

大小_指针 为 8。

页大小 为 4096。

页移位 为 12。

大页大小 为 2097152。

大页移位 为 21。

巨页大小 为 1073741824。

巨页移位 为 30。

内核基址 为 0xFFFFFFFF80000000。

内核偏移 为 0xFFFF800000000000。

物理内存映射基址 为 0xFFFF880000000000。

用户空间上限 为 0x00007FFFFFFFFFFF。

KB 为 1024。

MB 为 1048576。

GB 为 1073741824。

TB 为 1099511627776。

对齐到 接受 值、对齐：
    掩码 为 对齐 减 1。
    返还 值 加 掩码 位与 0 减 对齐。
完。

向下对齐 接受 值、对齐：
    返还 值 位与 0 减 对齐。
完。

是对齐的 接受 值、对齐：
    返还 值 位与 对齐 减 1 等于 0。
完。

地址到页号 接受 地址：
    返还 地址 右移 页移位。
完。

页号到地址 接受 页号：
    返还 页号 左移 页移位。
完。

物理到虚拟 接受 物理地址：
    返还 物理地址 加 物理内存映射基址。
完。

虚拟到物理 接受 虚拟地址：
    返还 虚拟地址 减 物理内存映射基址。
完。

是内核地址 接受 地址：
    返还 地址 大于或等于 内核偏移。
完。

是用户地址 接受 地址：
    返还 地址 小于或等于 用户空间上限。
完。

在范围内 接受 值、最小、最大：
    返还 值 大于或等于 最小 并且 值 小于或等于 最大。
完。

取小 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

取大 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

钳制 接受 值、最小、最大：
    若 值 小于 最小：
        返还 最小。
    完。
    若 值 大于 最大：
        返还 最大。
    完。
    返还 值。
完。

绝对值 接受 值：
    若 值 小于 0：
        返还 0 减 值。
    完。
    返还 值。
完。

符号 接受 值：
    若 值 大于 0：
        返还 1。
    完。
    若 值 小于 0：
        返还 0 减 1。
    完。
    返还 0。
完。

是2的幂 接受 值：
    若 值 小于或等于 0：
        返还 假。
    完。
    返还 值 位与 值 减 1 等于 0。
完。

向上取2的幂 接受 值：
    若 值 小于或等于 1：
        返还 1。
    完。
    可变 v 为 值 减 1。
    v 为 v 位或 v 右移 1。
    v 为 v 位或 v 右移 2。
    v 为 v 位或 v 右移 4。
    v 为 v 位或 v 右移 8。
    v 为 v 位或 v 右移 16。
    v 为 v 位或 v 右移 32。
    返还 v 加 1。
完。

log2 接受 值：
    若 值 小于或等于 0：
        返还 0。
    完。
    可变 n 为 0。
    可变 v 为 值。
    只要 v 大于 1：
        v 为 v 右移 1。
        n 为 n 加 1。
    完。
    返还 n。
完。

计数前导零 接受 值：
    若 值 等于 0：
        返还 64。
    完。
    可变 n 为 0。
    可变 v 为 值。
    若 v 位与 0xFFFFFFFF00000000 等于 0：
        n 为 n 加 32。
        v 为 v 左移 32。
    完。
    若 v 位与 0xFFFF000000000000 等于 0：
        n 为 n 加 16。
        v 为 v 左移 16。
    完。
    若 v 位与 0xFF00000000000000 等于 0：
        n 为 n 加 8。
        v 为 v 左移 8。
    完。
    若 v 位与 0xF000000000000000 等于 0：
        n 为 n 加 4。
        v 为 v 左移 4。
    完。
    若 v 位与 0xC000000000000000 等于 0：
        n 为 n 加 2。
        v 为 v 左移 2。
    完。
    若 v 位与 0x8000000000000000 等于 0：
        n 为 n 加 1。
    完。
    返还 n。
完。

计数尾随零 接受 值：
    若 值 等于 0：
        返还 64。
    完。
    可变 n 为 0。
    可变 v 为 值。
    若 v 位与 0xFFFFFFFF 等于 0：
        n 为 n 加 32。
        v 为 v 右移 32。
    完。
    若 v 位与 0xFFFF 等于 0：
        n 为 n 加 16。
        v 为 v 右移 16。
    完。
    若 v 位与 0xFF 等于 0：
        n 为 n 加 8。
        v 为 v 右移 8。
    完。
    若 v 位与 0xF 等于 0：
        n 为 n 加 4。
        v 为 v 右移 4。
    完。
    若 v 位与 0x3 等于 0：
        n 为 n 加 2。
        v 为 v 右移 2。
    完。
    若 v 位与 0x1 等于 0：
        n 为 n 加 1。
    完。
    返还 n。
完。

计数置位 接受 值：
    可变 n 为 0。
    可变 v 为 值。
    只要 v 不等于 0：
        v 为 v 位与 v 减 1。
        n 为 n 加 1。
    完。
    返还 n。
完。

字节交换16 接受 值：
    返还 (值 位与 0xFF) 左移 8 位或 值 右移 8 位与 0xFF。
完。

字节交换32 接受 值：
    b0 为 (值 位与 0xFF) 左移 24。
    b1 为 (值 右移 8 位与 0xFF) 左移 16。
    b2 为 (值 右移 16 位与 0xFF) 左移 8。
    b3 为 值 右移 24 位与 0xFF。
    返还 b0 位或 b1 位或 b2 位或 b3。
完。

字节交换64 接受 值：
    低 为 做 字节交换32 用 值 位与 0xFFFFFFFF。
    高 为 做 字节交换32 用 值 右移 32 位与 0xFFFFFFFF。
    返还 低 左移 32 位或 高。
完。

提取位域 接受 值、起始位、位数：
    返还 值 右移 起始位 位与 (1 左移 位数) 减 1。
完。

设置位域 接受 值、起始位、位数、新值：
    掩码 为 (1 左移 位数) 减 1 左移 起始位。
    返还 值 位与 0 减 1 减 掩码 加 1 位或 新值 左移 起始位 位与 掩码。
完。

安全加法 接受 a、b：
    若 b 大于 0 并且 a 大于 整数64_最大 减 b：
        返还 整数64_最大。
    完。
    若 b 小于 0 并且 a 小于 0 减 整数64_最大 减 b：
        返还 0 减 整数64_最大。
    完。
    返还 a 加 b。
完。

安全乘法 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 0。
    完。
    若 a 大于 0 并且 b 大于 0 并且 a 大于 整数64_最大 除以 b：
        返还 整数64_最大。
    完。
    若 a 小于 0 并且 b 小于 0 并且 a 小于 整数64_最大 除以 b：
        返还 整数64_最大。
    完。
    返还 a 乘 b。
完。

安全除法 接受 a、b：
    若 b 等于 0：
        返还 0。
    完。
    返还 a 除以 b。
完。

FNV1a_哈希 接受 数据、长度：
    可变 哈希 为 0xcbf29ce484222325。
    可变 i 为 0。
    只要 i 小于 长度：
        哈希 为 哈希 位异或 数据 的 第 i 项 位与 0xFF。
        哈希 为 哈希 乘 0x100000001b3。
        i 为 i 加 1。
    完。
    返还 哈希。
完。

混合哈希 接受 a、b：
    可变 h 为 a。
    h 为 h 位异或 b 加 0x9e3779b9 加 (h 左移 6) 加 (h 右移 2)。
    返还 h。
完。

内存比较 接受 a、b、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        若 a 的 第 i 项 小于 b 的 第 i 项：
            返还 0 减 1。
        完。
        若 a 的 第 i 项 大于 b 的 第 i 项：
            返还 1。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

格式化大小 接受 字节数：
    若 字节数 大于或等于 GB：
        返还 做 整数转字符串 用 字节数 除以 GB 加 「 GB」。
    否则若 字节数 大于或等于 MB：
        返还 做 整数转字符串 用 字节数 除以 MB 加 「 MB」。
    否则若 字节数 大于或等于 KB：
        返还 做 整数转字符串 用 字节数 除以 KB 加 「 KB」。
    完。
    返还 做 整数转字符串 用 字节数 加 「 B」。
完。

整数转十六进制 接受 值：
    hex字符 为 「0123456789ABCDEF」。
    若 值 等于 0：
        返还 「0x0」。
    完。
    可变 结果 为 「」。
    可变 v 为 值。
    只要 v 大于 0：
        结果 为 hex字符 的 第 v 位与 0xF 项 加 结果。
        v 为 v 右移 4。
    完。
    返还 「0x」 加 结果。
完。

截断到8位 接受 值：
    返还 值 位与 0xFF。
完。

截断到16位 接受 值：
    返还 值 位与 0xFFFF。
完。

截断到32位 接受 值：
    返还 值 位与 0xFFFFFFFF。
完。

符号扩展8 接受 值：
    若 值 位与 0x80：
        返还 值 位或 0 减 256。
    完。
    返还 值 位与 0xFF。
完。

符号扩展16 接受 值：
    若 值 位与 0x8000：
        返还 值 位或 0 减 65536。
    完。
    返还 值 位与 0xFFFF。
完。

符号扩展32 接受 值：
    若 值 位与 0x80000000：
        返还 值 位或 0 减 4294967296。
    完。
    返还 值 位与 0xFFFFFFFF。
完。

旋转左 接受 值、位数：
    返还 值 左移 位数 位或 值 右移 64 减 位数。
完。

旋转右 接受 值、位数：
    返还 值 右移 位数 位或 值 左移 64 减 位数。
完。

CRC32_多项式 为 0xEDB88320。

CRC32 接受 数据、长度：
    可变 crc 为 0xFFFFFFFF。
    可变 i 为 0。
    只要 i 小于 长度：
        crc 为 crc 位异或 数据 的 第 i 项 位与 0xFF。
        可变 j 为 0。
        只要 j 小于 8：
            若 crc 位与 1 不等于 0：
                crc 为 crc 右移 1 位异或 CRC32_多项式。
            否则：
                crc 为 crc 右移 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 crc 位异或 0xFFFFFFFF。
完。

CRC32_更新 接受 crc、字节：
    crc 为 crc 位异或 字节 位与 0xFF。
    可变 j 为 0。
    只要 j 小于 8：
        若 crc 位与 1 不等于 0：
            crc 为 crc 右移 1 位异或 CRC32_多项式。
        否则：
            crc 为 crc 右移 1。
        完。
        j 为 j 加 1。
    完。
    返还 crc。
完。

安全内存复制 接受 目标、源、长度、目标大小：
    若 长度 大于 目标大小：
        长度 为 目标大小。
    完。
    做 内存复制 用 目标、源、长度。
    返还 长度。
完。

安全内存移动 接受 目标、源、长度：
    若 目标 等于 源 或者 长度 等于 0：
        返还。
    完。
    若 目标 小于 源：
        可变 i 为 0。
        只要 i 小于 长度：
            目标 的 第 i 项 为 源 的 第 i 项。
            i 为 i 加 1。
        完。
    否则：
        可变 i 为 长度 减 1。
        只要 i 大于或等于 0：
            目标 的 第 i 项 为 源 的 第 i 项。
            i 为 i 减 1。
        完。
    完。
完。

内存查找 接受 数据、长度、值：
    可变 i 为 0。
    只要 i 小于 长度：
        若 数据 的 第 i 项 等于 值：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 0 减 1。
完。

字符串转整数 接受 字符串：
    可变 结果 为 0。
    可变 负数 为 假。
    可变 i 为 0。
    n 为 做 字符串长度 用 字符串。
    只要 i 小于 n 并且 字符串 的 第 i 项 等于 32：
        i 为 i 加 1。
    完。
    若 i 小于 n 并且 字符串 的 第 i 项 等于 45：
        负数 为 真。
        i 为 i 加 1。
    否则若 i 小于 n 并且 字符串 的 第 i 项 等于 43：
        i 为 i 加 1。
    完。
    只要 i 小于 n 并且 字符串 的 第 i 项 大于或等于 48 并且 字符串 的 第 i 项 小于或等于 57：
        结果 为 结果 乘 10 加 (字符串 的 第 i 项 减 48)。
        i 为 i 加 1。
    完。
    若 负数：
        返还 0 减 结果。
    完。
    返还 结果。
完。

字符串转十六进制 接受 字符串：
    可变 结果 为 0。
    可变 i 为 0。
    n 为 做 字符串长度 用 字符串。
    若 n 大于或等于 2 并且 字符串 的 第 0 项 等于 48 并且 (字符串 的 第 1 项 等于 120 或者 字符串 的 第 1 项 等于 88)：
        i 为 2。
    完。
    只要 i 小于 n：
        ch 为 字符串 的 第 i 项。
        若 ch 大于或等于 48 并且 ch 小于或等于 57：
            结果 为 结果 左移 4 位或 ch 减 48。
        否则若 ch 大于或等于 65 并且 ch 小于或等于 70：
            结果 为 结果 左移 4 位或 ch 减 55。
        否则若 ch 大于或等于 97 并且 ch 小于或等于 102：
            结果 为 结果 左移 4 位或 ch 减 87。
        否则：
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

环形缓冲 是 一种 事物，含 数据、容量、头、尾、掩码。

环形缓冲_创建 接受 大小：
    容量 为 做 向上取2的幂 用 大小。
    rb 为 做 堆_分配 用 环形缓冲。
    rb 的 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 rb 的 数据、0。
        i 为 i 加 1。
    完。
    rb 的 容量 为 容量。
    rb 的 头 为 0。
    rb 的 尾 为 0。
    rb 的 掩码 为 容量 减 1。
    返还 rb。
完。

环形缓冲_写入 接受 rb、值：
    下一头 为 rb 的 头 加 1 位与 rb 的 掩码。
    若 下一头 等于 rb 的 尾：
        返还 0 减 1。
    完。
    做 设置 用 rb 的 数据、rb 的 头、值。
    rb 的 头 为 下一头。
    返还 0。
完。

环形缓冲_读取 接受 rb：
    若 rb 的 头 等于 rb 的 尾：
        返还 0 减 1。
    完。
    值 为 rb 的 数据 的 第 rb 的 尾 项。
    rb 的 尾 为 rb 的 尾 加 1 位与 rb 的 掩码。
    返还 值。
完。

环形缓冲_可读 接受 rb：
    返还 rb 的 头 不等于 rb 的 尾。
完。

环形缓冲_可写 接受 rb：
    返还 rb 的 头 加 1 位与 rb 的 掩码 不等于 rb 的 尾。
完。

环形缓冲_已用 接受 rb：
    返还 rb 的 头 减 rb 的 尾 位与 rb 的 掩码。
完。

环形缓冲_空余 接受 rb：
    返还 rb 的 容量 减 1 减 做 环形缓冲_已用 用 rb。
完。

环形缓冲_重置 接受 rb：
    rb 的 头 为 0。
    rb 的 尾 为 0。
完。

是空指针 接受 指针：
    返还 指针 等于 空指针。
完。

是有效地址 接受 地址：
    返还 地址 不等于 空指针。
完。

是有效页对齐 接受 地址：
    返还 做 是对齐的 用 地址、页大小。
完。

是有效用户指针 接受 地址、大小：
    若 非 做 是用户地址 用 地址：
        返还 假。
    完。
    若 地址 加 大小 小于 地址：
        返还 假。
    完。
    若 非 做 是用户地址 用 地址 加 大小 减 1：
        返还 假。
    完。
    返还 真。
完。

缓存行大小 为 64。

SIMD对齐_128 为 16。

SIMD对齐_256 为 32。

SIMD对齐_512 为 64。

缓存行对齐 接受 地址：
    返还 做 对齐到 用 地址、缓存行大小。
完。

是缓存行对齐 接受 地址：
    返还 做 是对齐的 用 地址、缓存行大小。
完。

类型定义_诊断 接受 零参：
    返还 一本字典：页大小 对应 页大小、大页大小 对应 大页大小、巨页大小 对应 巨页大小、指针大小 对应 大小_指针、缓存行 对应 缓存行大小、内核基址 对应 内核基址、用户上限 对应 用户空间上限。
完。

可变 AI_类型定义_运行指标 为 0。

可变 AI_类型定义_异常指标 为 0。

AI_类型定义_自诊断 接受 零参：
    AI_类型定义_运行指标 为 0。
    返还 AI_类型定义_运行指标。
完。

AI_类型定义_优化建议 接受 零参：
    指标 为 做 AI_类型定义_自诊断。
    返还 「类型定义状态正常」。
完。

本章 收束。
