《链表移到头部》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·双向链表操作》。

引用《.·链表后继》。

引用《.·字符串替换》。

链表_移到头部 接受 头、节点：
    做 链表_删除 用 节点。
    做 链表_插入之间 用 节点、头、头 的 第 链_后继 项。
完。

链表_移到尾部 接受 头、节点：
    做 链表_删除 用 节点。
    做 链表_插入之间 用 节点、头 的 第 链_前驱 项、头。
完。

链表_拼接 接受 目标头、源头：
    若 做 链表_为空 用 源头：
        返还。
    完。
    源首 为 源头 的 第 链_后继 项。
    源末 为 源头 的 第 链_前驱 项。
    目标首 为 目标头 的 第 链_后继 项。
    做 设置 用 目标头、链_后继、源首。
    做 设置 用 源首、链_前驱、目标头。
    做 设置 用 源末、链_后继、目标首。
    做 设置 用 目标首、链_前驱、源末。
    做 设置 用 源头、链_前驱、源头。
    做 设置 用 源头、链_后继、源头。
完。

链表_查找 接受 头、判断函数：
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        若 做 判断函数 用 当前 的 第 链_数据 项：
            返还 当前。
        完。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 空。
完。

链表_查找数据 接受 头、目标：
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        若 当前 的 第 链_数据 项 等于 目标：
            返还 当前。
        完。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 空。
完。

链表_包含 接受 头、目标：
    返还 做 链表_查找数据 用 头、目标 不等于 空。
完。

链表_过滤删除 接受 头、应该删除函数：
    可变 删除数 为 0。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        下一个 为 当前 的 第 链_后继 项。
        若 做 应该删除函数 用 当前 的 第 链_数据 项：
            做 链表_删除 用 当前。
            删除数 为 删除数 加 1。
        完。
        当前 为 下一个。
    完。
    返还 删除数。
完。

链表_有序插入 接受 头、数据、比较函数：
    新节点 为 一组：空、空、数据。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        若 做 比较函数 用 数据、当前 的 第 链_数据 项 小于 0：
            做 链表_插入之间 用 新节点、当前 的 第 链_前驱 项、当前。
            返还 新节点。
        完。
        当前 为 当前 的 第 链_后继 项。
    完。
    做 链表_插入之间 用 新节点、头 的 第 链_前驱 项、头。
    返还 新节点。
完。

链表_反转 接受 头：
    可变 当前 为 头。
    可变 已处理 为 假。
    只要 当前 不等于 头 或者 已处理 等于 假：
        已处理 为 真。
        临时 为 当前 的 第 链_后继 项。
        做 设置 用 当前、链_后继、当前 的 第 链_前驱 项。
        做 设置 用 当前、链_前驱、临时。
        当前 为 临时。
    完。
完。

链表_环检测 接受 头：
    若 做 链表_为空 用 头：
        返还 空。
    完。
    可变 慢 为 头 的 第 链_后继 项。
    可变 快 为 头 的 第 链_后继 项。
    只要 快 不等于 头 并且 快 的 第 链_后继 项 不等于 头：
        慢 为 慢 的 第 链_后继 项。
        快 为 快 的 第 链_后继 项。
        快 为 快 的 第 链_后继 项。
        若 慢 等于 快：
            可变 入口 为 头 的 第 链_后继 项。
            只要 入口 不等于 慢：
                入口 为 入口 的 第 链_后继 项。
                慢 为 慢 的 第 链_后继 项。
            完。
            返还 入口。
        完。
    完。
    返还 空。
完。

_链表_分割 接受 节点、步长：
    可变 当前 为 节点。
    可变 i 为 1。
    只要 i 小于 步长 并且 当前 的 第 链_后继 项 不等于 空：
        当前 为 当前 的 第 链_后继 项。
        i 为 i 加 1。
    完。
    下半 为 当前 的 第 链_后继 项。
    做 设置 用 当前、链_后继、空。
    返还 下半。
完。

_链表_合并 接受 左、右、比较函数：
    哨兵 为 一组：空、空、0。
    可变 尾 为 哨兵。
    只要 左 不等于 空 并且 右 不等于 空：
        若 做 比较函数 用 左 的 第 链_数据 项、右 的 第 链_数据 项 小于或等于 0：
            做 设置 用 尾、链_后继、左。
            做 设置 用 左、链_前驱、尾。
            左 为 左 的 第 链_后继 项。
        否则：
            做 设置 用 尾、链_后继、右。
            做 设置 用 右、链_前驱、尾。
            右 为 右 的 第 链_后继 项。
        完。
        尾 为 尾 的 第 链_后继 项。
    完。
    若 左 不等于 空：
        做 设置 用 尾、链_后继、左。
        做 设置 用 左、链_前驱、尾。
    完。
    若 右 不等于 空：
        做 设置 用 尾、链_后继、右。
        做 设置 用 右、链_前驱、尾。
    完。
    返还 哨兵 的 第 链_后继 项。
完。

链表_归并排序 接受 头、比较函数：
    若 做 链表_为空 用 头：
        返还。
    完。
    可变 首 为 头 的 第 链_后继 项。
    可变 长度 为 0。
    可变 当前 为 首。
    只要 当前 不等于 头：
        长度 为 长度 加 1。
        当前 为 当前 的 第 链_后继 项。
    完。
    做 设置 用 头 的 第 链_前驱 项、链_后继、空。
    可变 步长 为 1。
    只要 步长 小于 长度：
        可变 剩余 为 首。
        首 为 空。
        可变 尾 为 空。
        只要 剩余 不等于 空：
            左 为 剩余。
            右 为 做 _链表_分割 用 左、步长。
            剩余 为 做 _链表_分割 用 右、步长。
            合并结果 为 做 _链表_合并 用 左、右、比较函数。
            若 首 等于 空：
                首 为 合并结果。
            否则：
                做 设置 用 尾、链_后继、合并结果。
                做 设置 用 合并结果、链_前驱、尾。
            完。
            尾 为 合并结果。
            只要 尾 的 第 链_后继 项 不等于 空：
                尾 为 尾 的 第 链_后继 项。
            完。
        完。
        步长 为 步长 乘 2。
    完。
    做 设置 用 头、链_后继、首。
    做 设置 用 首、链_前驱、头。
    当前 为 首。
    只要 当前 的 第 链_后继 项 不等于 空：
        当前 为 当前 的 第 链_后继 项。
    完。
    做 设置 用 当前、链_后继、头。
    做 设置 用 头、链_前驱、当前。
完。

链表_惯性排序 接受 头、比较函数：
    若 做 链表_为空 用 头：
        返还。
    完。
    可变 段列表 为 一组：（空）。
    可变 段首 为 头 的 第 链_后继 项。
    只要 段首 不等于 头：
        可变 段尾 为 段首。
        只要 段尾 的 第 链_后继 项 不等于 头 并且 做 比较函数 用 段尾 的 第 链_数据 项、段尾 的 第 链_后继 项 的 第 链_数据 项 小于或等于 0：
            段尾 为 段尾 的 第 链_后继 项。
        完。
        下段 为 段尾 的 第 链_后继 项。
        做 设置 用 段尾、链_后继、空。
        做 追加 用 段列表、段首。
        段首 为 下段。
    完。
    只要 做 计数 用 段列表 大于 1：
        可变 新段列表 为 一组：（空）。
        可变 i 为 0。
        只要 i 加 1 小于 做 计数 用 段列表：
            做 追加 用 新段列表、做 _链表_合并 用 段列表 的 第 i 项、段列表 的 第 i 加 1 项、比较函数。
            i 为 i 加 2。
        完。
        若 i 小于 做 计数 用 段列表：
            做 追加 用 新段列表、段列表 的 第 i 项。
        完。
        段列表 为 新段列表。
    完。
    可变 首 为 段列表 的 第 0 项。
    做 设置 用 头、链_后继、首。
    做 设置 用 首、链_前驱、头。
    可变 当前 为 首。
    只要 当前 的 第 链_后继 项 不等于 空：
        当前 为 当前 的 第 链_后继 项。
    完。
    做 设置 用 当前、链_后继、头。
    做 设置 用 头、链_前驱、当前。
完。

链_后继 为 1。

链_前驱 为 0。

链_数据 为 2。

AI局部窗口 为 16。

移动排序统计 是 一种 事物，含 移到头次数、移到尾次数、拼接次数、查找次数、查找命中、安全遍历次数、过滤删除次数、过滤删除节点数、有序插入次数、反转次数、环检测次数、环检测命中、归并排序次数、惯性排序次数、惯性段平均长度、AI局部记录数、AI局部命中数、批量移动次数、错误次数。

可变 全局统计 为 新建 移动排序统计。

可变 移动模块已初始化 为 假。

可变 AI局部历史 为 一组：（空）。

可变 AI局部位置 为 0。

链表移动_初始化 接受 零参：
    若 移动模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI局部历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI局部窗口：
        做 追加 用 AI局部历史、0。
        i 为 i 加 1。
    完。
    AI局部位置 为 0。
    移动模块已初始化 为 真。
    做 打印 用 「[链表移动] 初始化完成」。
    返还 0。
完。

链表移动_销毁 接受 零参：
    若 移动模块已初始化 等于 假：
        返还 0。
    完。
    AI局部历史 为 一组：（空）。
    移动模块已初始化 为 假。
    做 打印 用 「[链表移动] 已销毁」。
    返还 0。
完。

链表_安全移到头部 接受 头、节点：
    若 头 等于 空 或者 节点 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还。
    完。
    全局统计 的 移到头次数 为 全局统计 的 移到头次数 加 1。
    做 链表_移到头部 用 头、节点。
    做 AI_记录局部 用 节点 的 第 链_数据 项。
完。

链表_安全移到尾部 接受 头、节点：
    若 头 等于 空 或者 节点 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还。
    完。
    全局统计 的 移到尾次数 为 全局统计 的 移到尾次数 加 1。
    做 链表_移到尾部 用 头、节点。
完。

链表_批量移到头 接受 头、节点列表：
    若 头 等于 空 或者 节点列表 等于 空：
        返还 0。
    完。
    全局统计 的 批量移动次数 为 全局统计 的 批量移动次数 加 1。
    可变 已移动 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点列表：
        若 节点列表 的 第 i 项 不等于 空：
            做 链表_移到头部 用 头、节点列表 的 第 i 项。
            已移动 为 已移动 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 已移动。
完。

链表_条件移到头 接受 头、条件函数：
    若 头 等于 空 或者 条件函数 等于 空：
        返还 0。
    完。
    可变 已移动 为 0。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        下一个 为 当前 的 第 链_后继 项。
        若 做 条件函数 用 当前 的 第 链_数据 项 等于 真：
            做 链表_移到头部 用 头、当前。
            已移动 为 已移动 加 1。
        完。
        当前 为 下一个。
    完。
    返还 已移动。
完。

链表_安全查找 接受 头、判断函数：
    若 头 等于 空 或者 判断函数 等于 空：
        返还 空。
    完。
    全局统计 的 查找次数 为 全局统计 的 查找次数 加 1。
    结果 为 做 链表_查找 用 头、判断函数。
    若 结果 不等于 空：
        全局统计 的 查找命中 为 全局统计 的 查找命中 加 1。
    完。
    返还 结果。
完。

链表_长度计算 接受 头：
    若 头 等于 空：
        返还 0。
    完。
    可变 长度 为 0。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        长度 为 长度 加 1。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 长度。
完。

链表_是否有序 接受 头、比较函数：
    若 头 等于 空 或者 比较函数 等于 空：
        返还 真。
    完。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头 并且 当前 的 第 链_后继 项 不等于 头：
        若 做 比较函数 用 当前 的 第 链_数据 项、当前 的 第 链_后继 项 的 第 链_数据 项 大于 0：
            返还 假。
        完。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 真。
完。

AI_记录局部 接受 数据：
    做 设置 用 AI局部历史、AI局部位置、数据。
    AI局部位置 为 (AI局部位置 加 1) 取余于 AI局部窗口。
    全局统计 的 AI局部记录数 为 全局统计 的 AI局部记录数 加 1。
完。

AI_预测下次移动目标 接受 零参：
    可变 最频数据 为 0。
    可变 最频次 为 0。
    可变 i 为 0。
    只要 i 小于 AI局部窗口：
        若 AI局部历史 的 第 i 项 等于 0：
            i 为 i 加 1。
            继续。
        完。
        当前 为 AI局部历史 的 第 i 项。
        可变 频 为 0。
        可变 j 为 0。
        只要 j 小于 AI局部窗口：
            若 AI局部历史 的 第 j 项 等于 当前：
                频 为 频 加 1。
            完。
            j 为 j 加 1。
        完。
        若 频 大于 最频次：
            最频次 为 频。
            最频数据 为 当前。
        完。
        i 为 i 加 1。
    完。
    若 最频次 大于 AI局部窗口 除以 3：
        全局统计 的 AI局部命中数 为 全局统计 的 AI局部命中数 加 1。
    完。
    返还 最频数据。
完。

AI_建议预取节点 接受 头：
    热数据 为 做 AI_预测下次移动目标。
    若 热数据 等于 0：
        返还 空。
    完。
    返还 做 链表_查找数据 用 头、热数据。
完。

链表移动_转储 接受 头：
    做 打印 用 「[链表移动] ═══ 状态转储 ═══」。
    若 头 等于 空：
        做 打印 用 「  空链表」。
        返还 0。
    完。
    做 打印 用 做 字符串拼接 用 「  长度: 」、做 转字符串 用 做 链表_长度计算 用 头。
    若 头 的 第 链_后继 项 不等于 头：
        做 打印 用 做 字符串拼接 用 「  首元素: 」、做 转字符串 用 头 的 第 链_后继 项 的 第 链_数据 项。
    完。
    若 头 的 第 链_前驱 项 不等于 头：
        做 打印 用 做 字符串拼接 用 「  末元素: 」、做 转字符串 用 头 的 第 链_前驱 项 的 第 链_数据 项。
    完。
    返还 0。
完。

链表移动_打印统计 接受 零参：
    做 打印 用 「[链表移动] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  移到头: 」、做 转字符串 用 全局统计 的 移到头次数。
    做 打印 用 做 字符串拼接 用 「  移到尾: 」、做 转字符串 用 全局统计 的 移到尾次数。
    做 打印 用 做 字符串拼接 用 「  拼接: 」、做 转字符串 用 全局统计 的 拼接次数。
    做 打印 用 做 字符串拼接 用 「  查找次数: 」、做 转字符串 用 全局统计 的 查找次数。
    做 打印 用 做 字符串拼接 用 「  查找命中: 」、做 转字符串 用 全局统计 的 查找命中。
    做 打印 用 做 字符串拼接 用 「  安全遍历: 」、做 转字符串 用 全局统计 的 安全遍历次数。
    做 打印 用 做 字符串拼接 用 「  过滤删除: 」、做 转字符串 用 全局统计 的 过滤删除次数。
    做 打印 用 做 字符串拼接 用 「  删除节点: 」、做 转字符串 用 全局统计 的 过滤删除节点数。
    做 打印 用 做 字符串拼接 用 「  有序插入: 」、做 转字符串 用 全局统计 的 有序插入次数。
    做 打印 用 做 字符串拼接 用 「  反转: 」、做 转字符串 用 全局统计 的 反转次数。
    做 打印 用 做 字符串拼接 用 「  环检测: 」、做 转字符串 用 全局统计 的 环检测次数。
    做 打印 用 做 字符串拼接 用 「  归并排序: 」、做 转字符串 用 全局统计 的 归并排序次数。
    做 打印 用 做 字符串拼接 用 「  惯性排序: 」、做 转字符串 用 全局统计 的 惯性排序次数。
    做 打印 用 做 字符串拼接 用 「  批量移动: 」、做 转字符串 用 全局统计 的 批量移动次数。
    做 打印 用 做 字符串拼接 用 「  AI局部记录: 」、做 转字符串 用 全局统计 的 AI局部记录数。
    做 打印 用 做 字符串拼接 用 「  AI局部命中: 」、做 转字符串 用 全局统计 的 AI局部命中数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

链表移动_注册到内核 接受 内核上下文：
    若 移动模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[链表移动] 已注册到内核上下文」。
    返还 0。
完。

链表移动_查询状态 接受 零参：
    若 移动模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

链表移动_健康检查 接受 零参：
    若 移动模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 移到头次数 加 全局统计 的 查找次数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
