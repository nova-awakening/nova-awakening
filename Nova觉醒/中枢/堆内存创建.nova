《堆内存创建》

引用《..·中枢·系统调用表》。

引用《..·推理·通信渠道》。

引用《..·感知·内核直连层》。

引用《.·字符串替换》。

引用《.·内存初始化》。

引用《.·内容可见性》。

引用《..·执行·底层内存操》。

可变 堆内存创建_当前堆 为 0。

GC状态_空闲 为 0。

GC状态_标记中 为 1。

GC状态_清除中 为 2。

GC状态_压缩中 为 3。

对象头大小 为 16。

对象头_标记偏移 为 8。

对象头_年龄偏移 为 9。

对象头_大小偏移 为 10。

对象头_转发偏移 为 14。

标记_白色 为 0。

标记_灰色 为 1。

标记_黑色 为 2。

默认_新生代大小 为 2 乘 1024 乘 1024。

默认_老生代大小 为 16 乘 1024 乘 1024。

大对象阈值 为 256 乘 1024。

晋升年龄阈值 为 2。

根集_初始容量 为 256。

灰色列表_初始容量 为 1024。

AI_堆增长_保守 为 0。

AI_堆增长_均衡 为 1。

AI_堆增长_激进 为 2。

堆内存 是 一种 事物，含 新生代_from、新生代_to、新生代大小、新生代已用、新生代分配指针、老生代起始、老生代大小、老生代已用、老生代分配指针、大对象列表、大对象数量、大对象总字节、GC状态、GC次数、上次GC时间、增量标记进度、根集合、根集合数量、根集合容量、灰色列表、灰色列表数量、灰色列表容量、记忆集、记忆集数量、记忆集容量、AI增长策略、AI预测新生代压力、AI预测老生代压力、AI推荐GC间隔、AI历史分配速率、统计_总分配字节、统计_新生代分配次数、统计_老生代分配次数、统计_大对象分配次数、统计_MinorGC次数、统计_MajorGC次数、统计_总GC暂停微秒、统计_最长暂停微秒、统计_晋升字节总数、统计_分配失败次数。

大对象节点 是 一种 事物，含 数据地址、大小、下一个。

堆内存_创建 接受 新生代大小、老生代大小：
    若 新生代大小 等于 0：
        新生代大小 为 默认_新生代大小。
    完。
    若 老生代大小 等于 0：
        老生代大小 为 默认_老生代大小。
    完。
    可变 heap 为 做 堆_分配 用 堆内存。
    若 heap 等于 0：
        做 打印 用 「[堆内存] 致命错误: 堆结构分配失败」。
        返还 空。
    完。
    heap 的 新生代大小 为 新生代大小。
    heap 的 新生代_from 为 做 平台_申请内存 用 新生代大小。
    heap 的 新生代_to 为 做 平台_申请内存 用 新生代大小。
    若 heap 的 新生代_from 等于 0 或者 heap 的 新生代_to 等于 0：
        做 打印 用 「[堆内存] 致命错误: 新生代分配失败」。
        若 heap 的 新生代_from 不等于 0：
            做 平台_释放内存 用 heap 的 新生代_from、新生代大小。
        完。
        若 heap 的 新生代_to 不等于 0：
            做 平台_释放内存 用 heap 的 新生代_to、新生代大小。
        完。
        做 堆_释放 用 heap。
        返还 空。
    完。
    做 内存_清零 用 heap 的 新生代_from、新生代大小。
    做 内存_清零 用 heap 的 新生代_to、新生代大小。
    heap 的 新生代已用 为 0。
    heap 的 新生代分配指针 为 heap 的 新生代_from。
    heap 的 老生代大小 为 老生代大小。
    heap 的 老生代起始 为 做 平台_申请内存 用 老生代大小。
    若 heap 的 老生代起始 等于 0：
        做 打印 用 「[堆内存] 致命错误: 老生代分配失败」。
        做 平台_释放内存 用 heap 的 新生代_from、新生代大小。
        做 平台_释放内存 用 heap 的 新生代_to、新生代大小。
        做 堆_释放 用 heap。
        返还 空。
    完。
    做 内存_清零 用 heap 的 老生代起始、老生代大小。
    heap 的 老生代已用 为 0。
    heap 的 老生代分配指针 为 heap 的 老生代起始。
    heap 的 大对象列表 为 0。
    heap 的 大对象数量 为 0。
    heap 的 大对象总字节 为 0。
    heap 的 GC状态 为 GC状态_空闲。
    heap 的 GC次数 为 0。
    heap 的 上次GC时间 为 0。
    heap 的 增量标记进度 为 0。
    heap 的 根集合容量 为 根集_初始容量。
    heap 的 根集合 为 做 平台_申请内存 用 heap 的 根集合容量 乘 8。
    若 heap 的 根集合 不等于 0：
        做 内存_清零 用 heap 的 根集合、heap 的 根集合容量 乘 8。
    完。
    heap 的 根集合数量 为 0。
    heap 的 灰色列表容量 为 灰色列表_初始容量。
    heap 的 灰色列表 为 做 平台_申请内存 用 heap 的 灰色列表容量 乘 8。
    若 heap 的 灰色列表 不等于 0：
        做 内存_清零 用 heap 的 灰色列表、heap 的 灰色列表容量 乘 8。
    完。
    heap 的 灰色列表数量 为 0。
    heap 的 记忆集容量 为 根集_初始容量。
    heap 的 记忆集 为 做 平台_申请内存 用 heap 的 记忆集容量 乘 8。
    若 heap 的 记忆集 不等于 0：
        做 内存_清零 用 heap 的 记忆集、heap 的 记忆集容量 乘 8。
    完。
    heap 的 记忆集数量 为 0。
    heap 的 AI增长策略 为 AI_堆增长_均衡。
    heap 的 AI预测新生代压力 为 0。
    heap 的 AI预测老生代压力 为 0。
    heap 的 AI推荐GC间隔 为 5000。
    heap 的 AI历史分配速率 为 0。
    heap 的 统计_总分配字节 为 0。
    heap 的 统计_新生代分配次数 为 0。
    heap 的 统计_老生代分配次数 为 0。
    heap 的 统计_大对象分配次数 为 0。
    heap 的 统计_MinorGC次数 为 0。
    heap 的 统计_MajorGC次数 为 0。
    heap 的 统计_总GC暂停微秒 为 0。
    heap 的 统计_最长暂停微秒 为 0。
    heap 的 统计_晋升字节总数 为 0。
    heap 的 统计_分配失败次数 为 0。
    堆内存创建_当前堆 为 heap。
    做 打印 用 「[堆内存] 堆创建完成」。
    做 打印 用 做 字符串拼接 用 「  新生代: 」、做 转字符串 用 新生代大小。
    做 打印 用 做 字符串拼接 用 「  老生代: 」、做 转字符串 用 老生代大小。
    返还 heap。
完。

堆内存_销毁 接受 heap：
    若 heap 等于 空：
        返还。
    完。
    若 heap 的 新生代_from 不等于 0：
        做 平台_释放内存 用 heap 的 新生代_from、heap 的 新生代大小。
    完。
    若 heap 的 新生代_to 不等于 0：
        做 平台_释放内存 用 heap 的 新生代_to、heap 的 新生代大小。
    完。
    若 heap 的 老生代起始 不等于 0：
        做 平台_释放内存 用 heap 的 老生代起始、heap 的 老生代大小。
    完。
    做 堆内存_释放大对象 用 heap。
    若 heap 的 根集合 不等于 0：
        做 平台_释放内存 用 heap 的 根集合、heap 的 根集合容量 乘 8。
    完。
    若 heap 的 灰色列表 不等于 0：
        做 平台_释放内存 用 heap 的 灰色列表、heap 的 灰色列表容量 乘 8。
    完。
    若 heap 的 记忆集 不等于 0：
        做 平台_释放内存 用 heap 的 记忆集、heap 的 记忆集容量 乘 8。
    完。
    若 堆内存创建_当前堆 等于 heap：
        堆内存创建_当前堆 为 0。
    完。
    做 打印 用 「[堆内存] 堆已销毁」。
    做 堆_释放 用 heap。
完。

堆内存_释放大对象 接受 heap：
    可变 节点 为 heap 的 大对象列表。
    只要 节点 不等于 0：
        下一个 为 做 内存_读64 用 节点 加 16。
        数据 为 做 内存_读64 用 节点。
        大小 为 做 内存_读32 用 节点 加 8。
        若 数据 不等于 0：
            做 平台_释放内存 用 数据、大小。
        完。
        做 堆_释放 用 节点。
        节点 为 下一个。
    完。
    heap 的 大对象列表 为 0。
    heap 的 大对象数量 为 0。
    heap 的 大对象总字节 为 0。
完。

堆内存_新生代分配 接受 heap、大小：
    若 heap 等于 空 或者 大小 等于 0：
        返还 0。
    完。
    对齐大小 为 大小 加 对象头大小 加 7 位与 按位取反 7。
    若 对齐大小 大于 大对象阈值：
        返还 做 堆内存_大对象分配 用 heap、对齐大小。
    完。
    若 heap 的 新生代已用 加 对齐大小 小于或等于 heap 的 新生代大小：
        ptr 为 heap 的 新生代分配指针。
        heap 的 新生代分配指针 为 heap 的 新生代分配指针 加 对齐大小。
        heap 的 新生代已用 为 heap 的 新生代已用 加 对齐大小。
        heap 的 统计_总分配字节 为 heap 的 统计_总分配字节 加 对齐大小。
        heap 的 统计_新生代分配次数 为 heap 的 统计_新生代分配次数 加 1。
        做 内存_写8 用 ptr 加 对象头_标记偏移、标记_白色。
        做 内存_写8 用 ptr 加 对象头_年龄偏移、0。
        做 内存_写32 用 ptr 加 对象头_大小偏移、对齐大小。
        做 内存_写16 用 ptr 加 对象头_转发偏移、0。
        返还 ptr。
    完。
    做 堆内存_MinorGC 用 heap。
    若 heap 的 新生代已用 加 对齐大小 小于或等于 heap 的 新生代大小：
        ptr 为 heap 的 新生代分配指针。
        heap 的 新生代分配指针 为 heap 的 新生代分配指针 加 对齐大小。
        heap 的 新生代已用 为 heap 的 新生代已用 加 对齐大小。
        heap 的 统计_总分配字节 为 heap 的 统计_总分配字节 加 对齐大小。
        heap 的 统计_新生代分配次数 为 heap 的 统计_新生代分配次数 加 1。
        做 内存_写8 用 ptr 加 对象头_标记偏移、标记_白色。
        做 内存_写8 用 ptr 加 对象头_年龄偏移、0。
        做 内存_写32 用 ptr 加 对象头_大小偏移、对齐大小。
        返还 ptr。
    完。
    返还 做 堆内存_老生代分配 用 heap、对齐大小。
完。

堆内存_老生代分配 接受 heap、大小：
    若 heap 等于 空 或者 大小 等于 0：
        返还 0。
    完。
    若 heap 的 老生代已用 加 大小 小于或等于 heap 的 老生代大小：
        ptr 为 heap 的 老生代分配指针。
        heap 的 老生代分配指针 为 heap 的 老生代分配指针 加 大小。
        heap 的 老生代已用 为 heap 的 老生代已用 加 大小。
        heap 的 统计_总分配字节 为 heap 的 统计_总分配字节 加 大小。
        heap 的 统计_老生代分配次数 为 heap 的 统计_老生代分配次数 加 1。
        做 内存_写8 用 ptr 加 对象头_标记偏移、标记_白色。
        做 内存_写8 用 ptr 加 对象头_年龄偏移、晋升年龄阈值。
        做 内存_写32 用 ptr 加 对象头_大小偏移、大小。
        返还 ptr。
    完。
    做 打印 用 「[堆内存] 老生代满, 触发Major GC」。
    做 堆内存_MajorGC 用 heap。
    若 heap 的 老生代已用 加 大小 小于或等于 heap 的 老生代大小：
        ptr 为 heap 的 老生代分配指针。
        heap 的 老生代分配指针 为 heap 的 老生代分配指针 加 大小。
        heap 的 老生代已用 为 heap 的 老生代已用 加 大小。
        heap 的 统计_总分配字节 为 heap 的 统计_总分配字节 加 大小。
        heap 的 统计_老生代分配次数 为 heap 的 统计_老生代分配次数 加 1。
        做 内存_写8 用 ptr 加 对象头_标记偏移、标记_白色。
        做 内存_写32 用 ptr 加 对象头_大小偏移、大小。
        返还 ptr。
    完。
    做 打印 用 「[堆内存] 致命错误: 内存耗尽」。
    heap 的 统计_分配失败次数 为 heap 的 统计_分配失败次数 加 1。
    返还 0。
完。

堆内存_大对象分配 接受 heap、大小：
    若 heap 等于 空 或者 大小 等于 0：
        返还 0。
    完。
    数据 为 做 平台_申请内存 用 大小。
    若 数据 等于 0：
        做 打印 用 「[堆内存] 大对象分配失败」。
        heap 的 统计_分配失败次数 为 heap 的 统计_分配失败次数 加 1。
        返还 0。
    完。
    做 内存_清零 用 数据、大小。
    做 内存_写8 用 数据 加 对象头_标记偏移、标记_白色。
    做 内存_写8 用 数据 加 对象头_年龄偏移、晋升年龄阈值。
    做 内存_写32 用 数据 加 对象头_大小偏移、大小。
    节点 为 做 堆_分配 用 大对象节点。
    若 节点 不等于 0：
        做 内存_写64 用 节点、数据。
        做 内存_写32 用 节点 加 8、大小。
        做 内存_写64 用 节点 加 16、heap 的 大对象列表。
        heap 的 大对象列表 为 节点。
        heap 的 大对象数量 为 heap 的 大对象数量 加 1。
        heap 的 大对象总字节 为 heap 的 大对象总字节 加 大小。
    完。
    heap 的 统计_总分配字节 为 heap 的 统计_总分配字节 加 大小。
    heap 的 统计_大对象分配次数 为 heap 的 统计_大对象分配次数 加 1。
    返还 数据。
完。

堆内存_MinorGC 接受 heap：
    若 heap 等于 空：
        返还。
    完。
    开始 为 做 平台_获取高精度时间。
    heap 的 GC状态 为 GC状态_清除中。
    可变 to_指针 为 heap 的 新生代_to。
    to_上限 为 heap 的 新生代_to 加 heap 的 新生代大小。
    可变 扫描指针 为 to_指针。
    可变 i 为 0。
    只要 i 小于 heap 的 根集合数量：
        根 为 做 内存_读64 用 heap 的 根集合 加 i 乘 8。
        若 根 不等于 0 并且 做 堆内存_在新生代 用 heap、根：
            to_指针 为 做 堆内存_复制对象 用 heap、根、to_指针、to_上限。
        完。
        i 为 i 加 1。
    完。
    可变 j 为 0。
    只要 j 小于 heap 的 记忆集数量：
        宿主 为 做 内存_读64 用 heap 的 记忆集 加 j 乘 8。
        若 宿主 不等于 0：
            大小 为 做 内存_读32 用 宿主 加 对象头_大小偏移。
            可变 偏移 为 对象头大小。
            只要 偏移 小于 大小：
                字段 为 做 内存_读64 用 宿主 加 偏移。
                若 字段 不等于 0 并且 做 堆内存_在新生代 用 heap、字段：
                    to_指针 为 做 堆内存_复制对象 用 heap、字段、to_指针、to_上限。
                完。
                偏移 为 偏移 加 8。
            完。
        完。
        j 为 j 加 1。
    完。
    只要 扫描指针 小于 to_指针：
        大小 为 做 内存_读32 用 扫描指针 加 对象头_大小偏移。
        若 大小 等于 0：
            跳出。
        完。
        可变 偏移 为 对象头大小。
        只要 偏移 小于 大小：
            字段 为 做 内存_读64 用 扫描指针 加 偏移。
            若 字段 不等于 0 并且 做 堆内存_在新生代 用 heap、字段：
                转发 为 做 内存_读16 用 字段 加 对象头_转发偏移。
                若 转发 不等于 0：
                    做 内存_写64 用 扫描指针 加 偏移、转发。
                否则：
                    to_指针 为 做 堆内存_复制对象 用 heap、字段、to_指针、to_上限。
                完。
            完。
            偏移 为 偏移 加 8。
        完。
        扫描指针 为 扫描指针 加 大小。
    完。
    临时 为 heap 的 新生代_from。
    heap 的 新生代_from 为 heap 的 新生代_to。
    heap 的 新生代_to 为 临时。
    heap 的 新生代已用 为 to_指针 减 heap 的 新生代_from。
    heap 的 新生代分配指针 为 to_指针。
    heap 的 记忆集数量 为 0。
    heap 的 GC状态 为 GC状态_空闲。
    heap 的 GC次数 为 heap 的 GC次数 加 1。
    heap 的 统计_MinorGC次数 为 heap 的 统计_MinorGC次数 加 1。
    耗时 为 做 平台_获取高精度时间 减 开始。
    heap 的 统计_总GC暂停微秒 为 heap 的 统计_总GC暂停微秒 加 耗时。
    若 耗时 大于 heap 的 统计_最长暂停微秒：
        heap 的 统计_最长暂停微秒 为 耗时。
    完。
    heap 的 上次GC时间 为 做 平台_获取高精度时间。
完。

堆内存_复制对象 接受 heap、源、to_指针、to_上限：
    大小 为 做 内存_读32 用 源 加 对象头_大小偏移。
    若 大小 等于 0 或者 to_指针 加 大小 大于 to_上限：
        返还 to_指针。
    完。
    转发 为 做 内存_读16 用 源 加 对象头_转发偏移。
    若 转发 不等于 0：
        返还 to_指针。
    完。
    年龄 为 做 内存_读8 用 源 加 对象头_年龄偏移。
    若 年龄 大于或等于 晋升年龄阈值：
        做 堆内存_晋升 用 heap、源、大小。
        返还 to_指针。
    完。
    做 内存_复制 用 to_指针、源、大小。
    做 内存_写8 用 to_指针 加 对象头_年龄偏移、年龄 加 1。
    做 内存_写16 用 源 加 对象头_转发偏移、to_指针 位与 65535。
    返还 to_指针 加 大小。
完。

堆内存_晋升 接受 heap、对象、大小：
    若 heap 的 老生代已用 加 大小 大于 heap 的 老生代大小：
        返还。
    完。
    新位置 为 heap 的 老生代分配指针。
    做 内存_复制 用 新位置、对象、大小。
    heap 的 老生代分配指针 为 新位置 加 大小。
    heap 的 老生代已用 为 heap 的 老生代已用 加 大小。
    heap 的 统计_晋升字节总数 为 heap 的 统计_晋升字节总数 加 大小。
    做 内存_写16 用 对象 加 对象头_转发偏移、新位置 位与 65535。
完。

堆内存_在新生代 接受 heap、地址：
    返还 地址 大于或等于 heap 的 新生代_from 并且 地址 小于 heap 的 新生代_from 加 heap 的 新生代大小。
完。

堆内存_MajorGC 接受 heap：
    若 heap 等于 空：
        返还。
    完。
    开始 为 做 平台_获取高精度时间。
    做 打印 用 「[堆内存] Major GC开始」。
    heap 的 统计_MajorGC次数 为 heap 的 统计_MajorGC次数 加 1。
    heap 的 GC次数 为 heap 的 GC次数 加 1。
    耗时 为 做 平台_获取高精度时间 减 开始。
    heap 的 统计_总GC暂停微秒 为 heap 的 统计_总GC暂停微秒 加 耗时。
    若 耗时 大于 heap 的 统计_最长暂停微秒：
        heap 的 统计_最长暂停微秒 为 耗时。
    完。
    heap 的 上次GC时间 为 做 平台_获取高精度时间。
完。

堆内存_添加根 接受 heap、对象：
    若 heap 等于 空 或者 对象 等于 0：
        返还。
    完。
    若 heap 的 根集合数量 大于或等于 heap 的 根集合容量：
        做 堆内存_扩容根集 用 heap。
    完。
    若 heap 的 根集合数量 小于 heap 的 根集合容量：
        做 内存_写64 用 heap 的 根集合 加 heap 的 根集合数量 乘 8、对象。
        heap 的 根集合数量 为 heap 的 根集合数量 加 1。
    完。
完。

堆内存_移除根 接受 heap、对象：
    若 heap 等于 空 或者 对象 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 heap 的 根集合数量：
        若 做 内存_读64 用 heap 的 根集合 加 i 乘 8 等于 对象：
            heap 的 根集合数量 为 heap 的 根集合数量 减 1。
            做 内存_写64 用 heap 的 根集合 加 i 乘 8、做 内存_读64 用 heap 的 根集合 加 heap 的 根集合数量 乘 8。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

堆内存_扩容根集 接受 heap：
    新容量 为 heap 的 根集合容量 乘 2。
    新内存 为 做 平台_申请内存 用 新容量 乘 8。
    若 新内存 等于 0：
        返还。
    完。
    做 内存_复制 用 新内存、heap 的 根集合、heap 的 根集合数量 乘 8。
    做 平台_释放内存 用 heap 的 根集合、heap 的 根集合容量 乘 8。
    heap 的 根集合 为 新内存。
    heap 的 根集合容量 为 新容量。
完。

堆内存_新生代使用率 接受 heap：
    若 heap 等于 空 或者 heap 的 新生代大小 等于 0：
        返还 0。
    完。
    返还 heap 的 新生代已用 乘 10000 除以 heap 的 新生代大小。
完。

堆内存_老生代使用率 接受 heap：
    若 heap 等于 空 或者 heap 的 老生代大小 等于 0：
        返还 0。
    完。
    返还 heap 的 老生代已用 乘 10000 除以 heap 的 老生代大小。
完。

堆内存_总使用率 接受 heap：
    若 heap 等于 空：
        返还 0。
    完。
    总量 为 heap 的 新生代大小 加 heap 的 老生代大小。
    已用 为 heap 的 新生代已用 加 heap 的 老生代已用 加 heap 的 大对象总字节。
    若 总量 等于 0：
        返还 0。
    完。
    返还 已用 乘 10000 除以 总量。
完。

堆内存_AI更新预测 接受 heap：
    若 heap 等于 空：
        返还。
    完。
    heap 的 AI预测新生代压力 为 做 堆内存_新生代使用率 用 heap。
    heap 的 AI预测老生代压力 为 做 堆内存_老生代使用率 用 heap。
    若 heap 的 AI预测老生代压力 大于 8000：
        heap 的 AI推荐GC间隔 为 1000。
    否则若 heap 的 AI预测老生代压力 大于 5000：
        heap 的 AI推荐GC间隔 为 3000。
    否则：
        heap 的 AI推荐GC间隔 为 10000。
    完。
完。

堆内存_AI应该扩容 接受 heap：
    若 heap 等于 空：
        返还 假。
    完。
    若 heap 的 AI预测老生代压力 大于 9000 并且 heap 的 AI历史分配速率 大于 1000000：
        返还 真。
    完。
    返还 假。
完。

堆内存_打印统计 接受 heap：
    若 heap 等于 空：
        返还。
    完。
    做 打印 用 「[堆内存] ═══════════ 堆内存统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  新生代大小:       」、做 转字符串 用 heap 的 新生代大小。
    做 打印 用 做 字符串拼接 用 「  新生代已用:       」、做 转字符串 用 heap 的 新生代已用。
    做 打印 用 做 字符串拼接 用 「  新生代使用率‱:   」、做 转字符串 用 做 堆内存_新生代使用率 用 heap。
    做 打印 用 做 字符串拼接 用 「  老生代大小:       」、做 转字符串 用 heap 的 老生代大小。
    做 打印 用 做 字符串拼接 用 「  老生代已用:       」、做 转字符串 用 heap 的 老生代已用。
    做 打印 用 做 字符串拼接 用 「  老生代使用率‱:   」、做 转字符串 用 做 堆内存_老生代使用率 用 heap。
    做 打印 用 做 字符串拼接 用 「  大对象数量:       」、做 转字符串 用 heap 的 大对象数量。
    做 打印 用 做 字符串拼接 用 「  大对象总字节:     」、做 转字符串 用 heap 的 大对象总字节。
    做 打印 用 做 字符串拼接 用 「  根集合数量:       」、做 转字符串 用 heap 的 根集合数量。
    做 打印 用 「[堆内存] ── GC统计 ──」。
    做 打印 用 做 字符串拼接 用 「  GC总次数:         」、做 转字符串 用 heap 的 GC次数。
    做 打印 用 做 字符串拼接 用 「  Minor GC次数:     」、做 转字符串 用 heap 的 统计_MinorGC次数。
    做 打印 用 做 字符串拼接 用 「  Major GC次数:     」、做 转字符串 用 heap 的 统计_MajorGC次数。
    做 打印 用 做 字符串拼接 用 「  总GC暂停(μs):     」、做 转字符串 用 heap 的 统计_总GC暂停微秒。
    做 打印 用 做 字符串拼接 用 「  最长暂停(μs):     」、做 转字符串 用 heap 的 统计_最长暂停微秒。
    做 打印 用 做 字符串拼接 用 「  晋升字节总数:     」、做 转字符串 用 heap 的 统计_晋升字节总数。
    做 打印 用 「[堆内存] ── 分配统计 ──」。
    做 打印 用 做 字符串拼接 用 「  总分配字节:       」、做 转字符串 用 heap 的 统计_总分配字节。
    做 打印 用 做 字符串拼接 用 「  新生代分配次数:   」、做 转字符串 用 heap 的 统计_新生代分配次数。
    做 打印 用 做 字符串拼接 用 「  老生代分配次数:   」、做 转字符串 用 heap 的 统计_老生代分配次数。
    做 打印 用 做 字符串拼接 用 「  大对象分配次数:   」、做 转字符串 用 heap 的 统计_大对象分配次数。
    做 打印 用 做 字符串拼接 用 「  分配失败次数:     」、做 转字符串 用 heap 的 统计_分配失败次数。
    做 打印 用 「[堆内存] ── AI管理 ──」。
    做 打印 用 做 字符串拼接 用 「  AI增长策略:       」、做 转字符串 用 heap 的 AI增长策略。
    做 打印 用 做 字符串拼接 用 「  AI新生代压力‱:   」、做 转字符串 用 heap 的 AI预测新生代压力。
    做 打印 用 做 字符串拼接 用 「  AI老生代压力‱:   」、做 转字符串 用 heap 的 AI预测老生代压力。
    做 打印 用 做 字符串拼接 用 「  AI推荐GC间隔(ms): 」、做 转字符串 用 heap 的 AI推荐GC间隔。
    做 打印 用 「[堆内存] ═══════════════════════════════════════」。
完。

本章 收束。
