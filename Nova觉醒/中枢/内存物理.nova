《内存物理》

引用《.·同步原语》。

引用《.·内存虚拟》。

物页档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、总页数、空闲页数、已用页数、保留页数、区数、分配数、释放数、映射数、压缩数、紧缩数、成功数、失败数、最近区、最近页、最近用途、锁、区表。

物页请求 是 一种 事物，含 动作、目标、参数、选项。

物页回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

物页摘要 是 一种 事物，含 名称、总页数、空闲页数、已用页数、保留页数、区数、分配数、释放数、映射数、压缩数、紧缩数、成功数、失败数、健康。

物页区项 是 一种 事物，含 名称、起始页、结束页、用途、页表。

物页帧项 是 一种 事物，含 页号、状态、标志、用途、引用数、映射。

可变 物页账本 为 一组：（空）。

可变 物页构建数 为 0。

可变 物页总动作 为 0。

可变 物页总成功 为 0。

可变 物页总失败 为 0。

内存物理_建档 接受 名称：
    档 为 新建 物页档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 总页数 为 0。
    档 的 空闲页数 为 0。
    档 的 已用页数 为 0。
    档 的 保留页数 为 0。
    档 的 区数 为 0。
    档 的 分配数 为 0。
    档 的 释放数 为 0。
    档 的 映射数 为 0。
    档 的 压缩数 为 0。
    档 的 紧缩数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    档 的 最近区 为 「」。
    档 的 最近页 为 负 1。
    档 的 最近用途 为 「」。
    档 的 锁 为 做 同步原语_创建互斥 用 名称 加 「锁」。
    档 的 区表 为 一组：（空）。
    返还 档。
完。

内存物理_登记 接受 档：
    做 追加 用 物页账本、档。
    物页构建数 为 物页构建数 加 1。
    返还 档。
完。

内存物理_完整统计 接受 零参：
    返还 一组：物页构建数、物页总动作、物页总成功、物页总失败。
完。

内存物理_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 物页回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

内存物理_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    物页总动作 为 物页总动作 加 1。
完。

内存物理_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 空闲页数 小于 32：
        档 的 最近健康 为 「紧张」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 0：
        档 的 最近健康 为 「监视」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「运行」。
    返还 档 的 最近健康。
完。

内存物理_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 档 的 名称 等于 「」：
        返还 假。
    完。
    返还 真。
完。

内存物理_成功 接受 档、来源、数据：
    档 的 最近结果 为 数据。
    档 的 成功数 为 档 的 成功数 加 1。
    物页总成功 为 物页总成功 加 1。
    档 的 最近统计 为 做 内存物理_完整统计。
    做 内存物理_刷新健康 用 档。
    返还 做 内存物理_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

内存物理_失败 接受 档、来源、错因：
    若 档 不等于 空：
        档 的 最近错误 为 错因。
        档 的 失败数 为 档 的 失败数 加 1。
        档 的 最近统计 为 做 内存物理_完整统计。
        做 内存物理_刷新健康 用 档。
    完。
    物页总失败 为 物页总失败 加 1。
    返还 做 内存物理_建回证 用 真、假、来源、错因、空、做 内存物理_完整统计。
完。

内存物理_建区 接受 名称、起始页、结束页、用途：
    区 为 新建 物页区项。
    区 的 名称 为 名称。
    区 的 起始页 为 起始页。
    区 的 结束页 为 结束页。
    区 的 用途 为 用途。
    区 的 页表 为 一组：（空）。
    返还 区。
完。

内存物理_建页 接受 页号、状态、标志、用途：
    页 为 新建 物页帧项。
    页 的 页号 为 页号。
    页 的 状态 为 状态。
    页 的 标志 为 标志。
    页 的 用途 为 用途。
    页 的 引用数 为 0。
    页 的 映射 为 一组：（空）。
    返还 页。
完。

内存物理_找区 接受 档、名称：
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 区表：
        区 为 档 的 区表 的 第 索引 项。
        若 区 的 名称 等于 名称：
            返还 区。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

内存物理_找页 接受 区、页号：
    索引 为 0。
    只要 索引 小于 做 计数 用 区 的 页表：
        页 为 区 的 页表 的 第 索引 项。
        若 页 的 页号 等于 页号：
            返还 页。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

内存物理_加区 接受 档、名称、起始页、结束页、用途：
    若 非 做 内存物理_检查档 用 档：
        返还 做 内存物理_失败 用 档、「加区」、「物页档不可用」。
    完。
    做 内存物理_记录 用 档、「加区」、一组：名称、起始页、结束页、用途、「内存物理」。
    若 做 内存物理_找区 用 档、名称 不等于 空：
        返还 做 内存物理_失败 用 档、「加区」、「物理区已存在」。
    完。
    区 为 做 内存物理_建区 用 名称、起始页、结束页、用途。
    当前页 为 起始页。
    只要 当前页 小于或等于 结束页：
        做 追加 用 区 的 页表、做 内存物理_建页 用 当前页、「空闲」、「」、用途。
        当前页 为 当前页 加 1。
    完。
    做 追加 用 档 的 区表、区。
    档 的 区数 为 档 的 区数 加 1。
    档 的 总页数 为 档 的 总页数 加 做 计数 用 区 的 页表。
    档 的 空闲页数 为 档 的 空闲页数 加 做 计数 用 区 的 页表。
    档 的 最近区 为 名称。
    返还 做 内存物理_成功 用 档、「加区」、区。
完。

内存物理_取空闲页 接受 区：
    索引 为 0。
    只要 索引 小于 做 计数 用 区 的 页表：
        页 为 区 的 页表 的 第 索引 项。
        若 页 的 状态 等于 「空闲」：
            返还 页。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

内存物理_分配 接受 档、区名、用途、标志：
    若 非 做 内存物理_检查档 用 档：
        返还 做 内存物理_失败 用 档、「分配」、「物页档不可用」。
    完。
    做 内存物理_记录 用 档、「分配」、一组：区名、用途、标志、「内存物理」。
    区 为 做 内存物理_找区 用 档、区名。
    若 区 等于 空：
        返还 做 内存物理_失败 用 档、「分配」、「物理区不存在」。
    完。
    页 为 做 内存物理_取空闲页 用 区。
    若 页 等于 空：
        返还 做 内存物理_失败 用 档、「分配」、「物理区空闲页不足」。
    完。
    页 的 状态 为 「已用」。
    页 的 标志 为 标志。
    页 的 用途 为 用途。
    页 的 引用数 为 1。
    档 的 分配数 为 档 的 分配数 加 1。
    档 的 空闲页数 为 档 的 空闲页数 减 1。
    档 的 已用页数 为 档 的 已用页数 加 1。
    档 的 最近区 为 区名。
    档 的 最近页 为 页 的 页号。
    档 的 最近用途 为 用途。
    返还 做 内存物理_成功 用 档、「分配」、页。
完。

内存物理_批量分配 接受 档、区名、数量、用途、标志：
    若 非 做 内存物理_检查档 用 档：
        返还 做 内存物理_失败 用 档、「批量分配」、「物页档不可用」。
    完。
    做 内存物理_记录 用 档、「批量分配」、一组：区名、数量、用途、标志、「内存物理」。
    结果 为 一组：（空）。
    索引 为 0。
    只要 索引 小于 数量：
        回证 为 做 内存物理_分配 用 档、区名、用途、标志。
        若 非 回证 的 已成：
            返还 回证。
        完。
        做 追加 用 结果、回证 的 数据。
        索引 为 索引 加 1。
    完。
    返还 做 内存物理_成功 用 档、「批量分配」、结果。
完。

内存物理_释放 接受 档、区名、页号：
    若 非 做 内存物理_检查档 用 档：
        返还 做 内存物理_失败 用 档、「释放」、「物页档不可用」。
    完。
    做 内存物理_记录 用 档、「释放」、一组：区名、页号、「内存物理」。
    区 为 做 内存物理_找区 用 档、区名。
    若 区 等于 空：
        返还 做 内存物理_失败 用 档、「释放」、「物理区不存在」。
    完。
    页 为 做 内存物理_找页 用 区、页号。
    若 页 等于 空：
        返还 做 内存物理_失败 用 档、「释放」、「物理页不存在」。
    完。
    若 页 的 状态 等于 「保留」：
        返还 做 内存物理_失败 用 档、「释放」、「保留页不可释放」。
    完。
    若 页 的 状态 等于 「空闲」：
        返还 做 内存物理_失败 用 档、「释放」、「物理页已空闲」。
    完。
    页 的 状态 为 「空闲」。
    页 的 标志 为 「」。
    页 的 用途 为 区 的 用途。
    页 的 引用数 为 0。
    页 的 映射 为 一组：（空）。
    档 的 释放数 为 档 的 释放数 加 1。
    档 的 空闲页数 为 档 的 空闲页数 加 1。
    若 档 的 已用页数 大于 0：
        档 的 已用页数 为 档 的 已用页数 减 1。
    完。
    档 的 最近区 为 区名。
    档 的 最近页 为 页号。
    返还 做 内存物理_成功 用 档、「释放」、页号。
完。

内存物理_保留范围 接受 档、区名、起始页、结束页、用途：
    若 非 做 内存物理_检查档 用 档：
        返还 做 内存物理_失败 用 档、「保留范围」、「物页档不可用」。
    完。
    做 内存物理_记录 用 档、「保留范围」、一组：区名、起始页、结束页、用途、「内存物理」。
    区 为 做 内存物理_找区 用 档、区名。
    若 区 等于 空：
        返还 做 内存物理_失败 用 档、「保留范围」、「物理区不存在」。
    完。
    索引 为 0。
    命中数 为 0。
    只要 索引 小于 做 计数 用 区 的 页表：
        页 为 区 的 页表 的 第 索引 项。
        若 页 的 页号 大于或等于 起始页 并且 页 的 页号 小于或等于 结束页：
            若 页 的 状态 等于 「空闲」 并且 档 的 空闲页数 大于 0：
                档 的 空闲页数 为 档 的 空闲页数 减 1。
            完。
            页 的 状态 为 「保留」。
            页 的 用途 为 用途。
            命中数 为 命中数 加 1。
        完。
        索引 为 索引 加 1。
    完。
    档 的 保留页数 为 档 的 保留页数 加 命中数。
    档 的 最近区 为 区名。
    档 的 最近用途 为 用途。
    返还 做 内存物理_成功 用 档、「保留范围」、命中数。
完。

内存物理_建立映射 接受 档、区名、页号、虚拟地址、权限：
    若 非 做 内存物理_检查档 用 档：
        返还 做 内存物理_失败 用 档、「建立映射」、「物页档不可用」。
    完。
    做 内存物理_记录 用 档、「建立映射」、一组：区名、页号、虚拟地址、权限、「内存物理」。
    区 为 做 内存物理_找区 用 档、区名。
    若 区 等于 空：
        返还 做 内存物理_失败 用 档、「建立映射」、「物理区不存在」。
    完。
    页 为 做 内存物理_找页 用 区、页号。
    若 页 等于 空：
        返还 做 内存物理_失败 用 档、「建立映射」、「物理页不存在」。
    完。
    若 页 的 状态 等于 「空闲」：
        返还 做 内存物理_失败 用 档、「建立映射」、「空闲页不可映射」。
    完。
    做 追加 用 页 的 映射、一组：虚拟地址、权限。
    页 的 引用数 为 页 的 引用数 加 1。
    档 的 映射数 为 档 的 映射数 加 1。
    档 的 最近页 为 页号。
    档 的 最近用途 为 权限。
    返还 做 内存物理_成功 用 档、「建立映射」、页 的 映射。
完。

内存物理_页压 接受 档、区名：
    若 非 做 内存物理_检查档 用 档：
        返还 做 内存物理_失败 用 档、「页压」、「物页档不可用」。
    完。
    做 内存物理_记录 用 档、「页压」、一组：区名、「内存物理」。
    区 为 做 内存物理_找区 用 档、区名。
    若 区 等于 空：
        返还 做 内存物理_失败 用 档、「页压」、「物理区不存在」。
    完。
    已用 为 0。
    保留 为 0。
    索引 为 0。
    只要 索引 小于 做 计数 用 区 的 页表：
        页 为 区 的 页表 的 第 索引 项。
        若 页 的 状态 等于 「已用」：
            已用 为 已用 加 1。
        完。
        若 页 的 状态 等于 「保留」：
            保留 为 保留 加 1。
        完。
        索引 为 索引 加 1。
    完。
    档 的 压缩数 为 档 的 压缩数 加 1。
    返还 做 内存物理_成功 用 档、「页压」、一组：区名、已用、保留、做 计数 用 区 的 页表。
完。

内存物理_紧缩建议 接受 档：
    若 非 做 内存物理_检查档 用 档：
        返还 做 内存物理_失败 用 档、「紧缩建议」、「物页档不可用」。
    完。
    做 内存物理_记录 用 档、「紧缩建议」、一组：（空）、「内存物理」。
    建议 为 一组：（空）。
    若 档 的 空闲页数 小于 64：
        做 追加 用 建议、「回收缓存页」。
    完。
    若 档 的 保留页数 大于 档 的 总页数 除以 3：
        做 追加 用 建议、「复核保留区」。
    完。
    若 档 的 已用页数 大于 档 的 空闲页数：
        做 追加 用 建议、「触发跨区搬移」。
    完。
    档 的 紧缩数 为 档 的 紧缩数 加 1。
    返还 做 内存物理_成功 用 档、「紧缩建议」、建议。
完。

内存物理_异常 接受 档：
    若 非 做 内存物理_检查档 用 档：
        返还 做 内存物理_失败 用 档、「异常」、「物页档不可用」。
    完。
    做 内存物理_记录 用 档、「异常」、一组：（空）、「内存物理」。
    问题 为 一组：（空）。
    若 档 的 区数 等于 0：
        做 追加 用 问题、「无物理区」。
    完。
    若 档 的 空闲页数 小于 16：
        做 追加 用 问题、「空闲页告急」。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        做 追加 用 问题、「失败倒挂」。
    完。
    若 档 的 保留页数 大于 档 的 总页数 除以 2：
        做 追加 用 问题、「保留过高」。
    完。
    返还 做 内存物理_成功 用 档、「异常」、问题。
完。

内存物理_摘要 接受 档：
    摘要 为 新建 物页摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 总页数 为 档 的 总页数。
    摘要 的 空闲页数 为 档 的 空闲页数。
    摘要 的 已用页数 为 档 的 已用页数。
    摘要 的 保留页数 为 档 的 保留页数。
    摘要 的 区数 为 档 的 区数。
    摘要 的 分配数 为 档 的 分配数。
    摘要 的 释放数 为 档 的 释放数。
    摘要 的 映射数 为 档 的 映射数。
    摘要 的 压缩数 为 档 的 压缩数。
    摘要 的 紧缩数 为 档 的 紧缩数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 内存物理_刷新健康 用 档。
    返还 摘要。
完。

内存物理_初始化 接受 档：
    若 非 做 内存物理_检查档 用 档：
        返还 做 内存物理_失败 用 档、「初始化」、「物页档不可用」。
    完。
    做 内存物理_记录 用 档、「初始化」、一组：（空）、「内存物理」。
    档 的 区表 为 一组：（空）。
    档 的 总页数 为 0。
    档 的 空闲页数 为 0。
    档 的 已用页数 为 0。
    档 的 保留页数 为 0。
    档 的 区数 为 0。
    档 的 是否初始化 为 真。
    做 内存物理_加区 用 档、「低端区」、0、63、「DMA」。
    做 内存物理_加区 用 档、「常规区」、64、511、「常规」。
    做 内存物理_加区 用 档、「高端区」、512、1023、「高端」。
    做 内存物理_保留范围 用 档、「低端区」、0、7、「引导」。
    做 内存物理_保留范围 用 档、「常规区」、64、79、「内核镜像」。
    返还 做 内存物理_成功 用 档、「初始化」、「已初始化」。
完。

内存物理_自检 接受 档：
    若 非 做 内存物理_检查档 用 档：
        返还 假。
    完。
    若 做 内存物理_初始化 用 档 的 已成 等于 假：
        返还 假。
    完。
    一页 为 做 内存物理_分配 用 档、「常规区」、「页缓存」、「可写」。
    若 非 一页 的 已成：
        返还 假。
    完。
    多页 为 做 内存物理_批量分配 用 档、「高端区」、4、「白璃贴图」、「只读」。
    若 非 多页 的 已成：
        返还 假。
    完。
    若 做 内存物理_建立映射 用 档、「常规区」、一页 的 数据 的 页号、「0x400000」、「rw」 的 已成 等于 假：
        返还 假。
    完。
    若 做 内存物理_页压 用 档、「常规区」 的 已成 等于 假：
        返还 假。
    完。
    若 做 内存物理_紧缩建议 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 内存物理_释放 用 档、「常规区」、一页 的 数据 的 页号 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

内存物理_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 内存物理_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        返还 做 内存物理_初始化 用 档。
    完。
    若 请求 的 动作 等于 「加区」：
        返还 做 内存物理_加区 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「分配」：
        返还 做 内存物理_分配 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「批量分配」：
        返还 做 内存物理_批量分配 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「释放」：
        返还 做 内存物理_释放 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「保留范围」：
        返还 做 内存物理_保留范围 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「建立映射」：
        返还 做 内存物理_建立映射 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「页压」：
        返还 做 内存物理_页压 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「紧缩建议」：
        返还 做 内存物理_紧缩建议 用 档。
    完。
    若 请求 的 动作 等于 「异常」：
        返还 做 内存物理_异常 用 档。
    完。
    若 请求 的 动作 等于 「摘要」：
        返还 做 内存物理_成功 用 档、「摘要」、做 内存物理_摘要 用 档。
    完。
    返还 做 内存物理_失败 用 档、「处理」、「未知动作」。
完。

可变 AI_内存物理_运行指标 为 0。

可变 AI_内存物理_异常指标 为 0。

AI_内存物理_自诊断 接受 零参：
    AI_内存物理_运行指标 为 0。
    返还 AI_内存物理_运行指标。
完。

AI_内存物理_优化建议 接受 零参：
    指标 为 做 AI_内存物理_自诊断。
    返还 「内存物理状态正常」。
完。

本章 收束。
