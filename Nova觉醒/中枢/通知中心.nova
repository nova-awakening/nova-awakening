《通知中心》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《.·内核打印》。

通知_信息 为 0。

通知_警告 为 1。

通知_错误 为 2。

通知_紧急 为 3。

通知类型_系统 为 0。

通知类型_安全 为 1。

通知类型_应用 为 2。

通知类型_网络 为 3。

通知类型_AI 为 4。

通知_最大数 为 512。

通知_过期秒 为 604800。

通知 是 一种 事物，含 ID、优先级、类型、标题、内容、来源、时间戳、已读、已过期。

可变 _通知队列 为 一组：（空）。

可变 _通知序号 为 0。

可变 _订阅者列表 为 一组：（空）。

通知订阅 是 一种 事物，含 订阅者ID、类型过滤、优先级过滤、回调。

通知_发布 接受 优先级、类型、标题、内容、来源：
    若 做 计数 用 _通知队列 大于或等于 通知_最大数：
        做 通知_清理过期 用 做 读取毫秒 除以 1000。
        若 做 计数 用 _通知队列 大于或等于 通知_最大数：
            做 通知_淘汰最旧已读。
        完。
    完。
    _通知序号 为 _通知序号 加 1。
    n 为 新建 通知。
    n 的 ID 为 _通知序号。
    n 的 优先级 为 优先级。
    n 的 类型 为 类型。
    n 的 标题 为 标题。
    n 的 内容 为 内容。
    n 的 来源 为 来源。
    n 的 时间戳 为 做 读取毫秒 除以 1000。
    n 的 已读 为 假。
    n 的 已过期 为 假。
    做 追加 用 _通知队列、n。
    做 通知_分发 用 n。
    返还 n 的 ID。
完。

通知_分发 接受 n：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _订阅者列表：
        sub 为 _订阅者列表 的 第 i 项。
        类型匹配 为 sub 的 类型过滤 等于 负 1 或者 sub 的 类型过滤 等于 n 的 类型。
        优先级匹配 为 sub 的 优先级过滤 等于 负 1 或者 n 的 优先级 大于或等于 sub 的 优先级过滤。
        若 类型匹配 并且 优先级匹配：
            做 内核信息 用 「[通知→」 加 做 整数转字符串 用 sub 的 订阅者ID 加 「] 」 加 n 的 标题。
        完。
        i 为 i 加 1。
    完。
完。

通知_订阅 接受 订阅者ID、类型过滤、优先级过滤：
    sub 为 新建 通知订阅。
    sub 的 订阅者ID 为 订阅者ID。
    sub 的 类型过滤 为 类型过滤。
    sub 的 优先级过滤 为 优先级过滤。
    sub 的 回调 为 0。
    做 追加 用 _订阅者列表、sub。
完。

通知_取消订阅 接受 订阅者ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _订阅者列表：
        若 _订阅者列表 的 第 i 项 的 订阅者ID 等于 订阅者ID：
            做 列表_移除 用 _订阅者列表、i。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

通知_标记已读 接受 ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _通知队列：
        若 _通知队列 的 第 i 项 的 ID 等于 ID：
            _通知队列 的 第 i 项 的 已读 为 真。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

通知_标记全部已读 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _通知队列：
        _通知队列 的 第 i 项 的 已读 为 真。
        i 为 i 加 1。
    完。
完。

通知_未读数 接受 零参：
    可变 n 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _通知队列：
        若 非 _通知队列 的 第 i 项 的 已读 并且 非 _通知队列 的 第 i 项 的 已过期：
            n 为 n 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 n。
完。

通知_获取未读 接受 最大数：
    可变 结果 为 一组：（空）。
    可变 i 为 做 计数 用 _通知队列 减 1。
    只要 i 大于或等于 0 并且 做 计数 用 结果 小于 最大数：
        若 非 _通知队列 的 第 i 项 的 已读 并且 非 _通知队列 的 第 i 项 的 已过期：
            做 追加 用 结果、_通知队列 的 第 i 项。
        完。
        i 为 i 减 1。
    完。
    返还 结果。
完。

通知_清理过期 接受 当前时间：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _通知队列：
        若 非 _通知队列 的 第 i 项 的 已过期 并且 当前时间 减 _通知队列 的 第 i 项 的 时间戳 大于 通知_过期秒：
            _通知队列 的 第 i 项 的 已过期 为 真。
        完。
        i 为 i 加 1。
    完。
完。

通知_淘汰最旧已读 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _通知队列：
        若 _通知队列 的 第 i 项 的 已读 或者 _通知队列 的 第 i 项 的 已过期：
            做 列表_移除 用 _通知队列、i。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 _通知队列 大于 0：
        做 列表_移除 用 _通知队列、0。
    完。
完。

通知_优先级名 接受 优先级：
    若 优先级 等于 通知_信息：
        返还 「信息」。
    完。
    若 优先级 等于 通知_警告：
        返还 「警告」。
    完。
    若 优先级 等于 通知_错误：
        返还 「错误」。
    完。
    若 优先级 等于 通知_紧急：
        返还 「紧急」。
    完。
    返还 「未知」。
完。

通知_类型名 接受 类型：
    若 类型 等于 通知类型_系统：
        返还 「系统」。
    完。
    若 类型 等于 通知类型_安全：
        返还 「安全」。
    完。
    若 类型 等于 通知类型_应用：
        返还 「应用」。
    完。
    若 类型 等于 通知类型_网络：
        返还 「网络」。
    完。
    若 类型 等于 通知类型_AI：
        返还 「AI」。
    完。
    返还 「未知」。
完。

通知_报告 接受 零参：
    可变 r 为 「═══ 通知中心 ═══\n」。
    r 为 r 加 「总数: 」 加 做 整数转字符串 用 做 计数 用 _通知队列 加 「\n」。
    r 为 r 加 「未读: 」 加 做 整数转字符串 用 做 通知_未读数 加 「\n」。
    r 为 r 加 「订阅者: 」 加 做 整数转字符串 用 做 计数 用 _订阅者列表 加 「\n」。
    返还 r。
完。

通知_重置 接受 零参：
    _通知队列 为 一组：（空）。
    _通知序号 为 0。
    _订阅者列表 为 一组：（空）。
完。

本章 收束。
