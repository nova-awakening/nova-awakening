《APIC高级中断控制》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·自动扩容》。

引用《..·感知·内核直连层》。

引用《.·字符串替换》。

引用《.·内存初始化》。

引用《.·内容可见性》。

中控_寄存器基 为 0xFEE00000。

中控_标识偏移 为 0x20。

中控_版本偏移 为 0x30。

中控_优先偏移 为 0x80。

中控_结束偏移 为 0xB0。

中控_伪中断偏移 为 0xF0。

中控_命令低偏移 为 0x300。

中控_命令高偏移 为 0x310。

中控_定时偏移 为 0x320。

中控_热感偏移 为 0x330。

中控_性能偏移 为 0x340。

中控_LINT0偏移 为 0x350。

中控_LINT1偏移 为 0x360。

中控_错误偏移 为 0x370。

中控_初始计数偏移 为 0x380。

中控_当前计数偏移 为 0x390。

中控_分频偏移 为 0x3E0。

中断类型_定时器 为 0。

中断类型_热感 为 1。

中断类型_错误 为 2。

中断类型_伪 为 3。

中断类型_IPI 为 4。

中断类型_性能 为 5。

中断类型_外部 为 6。

中断类型_LINT0 为 7。

中断类型_LINT1 为 8。

中断类型_最大 为 9。

可变 中控基址 为 0。

可变 中控已启用 为 假。

可变 中控标识 为 0。

可变 中控_统计_已初始化 为 假。

可变 中断总数 为 0。

可变 核间中断数 为 0。

可变 定时中断数 为 0。

可变 热感中断数 为 0。

可变 错误中断数 为 0。

可变 伪中断数 为 0。

可变 性能中断数 为 0。

可变 外部中断数 为 0。

可变 LINT0中断数 为 0。

可变 LINT1中断数 为 0。

可变 中断速率 为 0。

可变 上次速率时刻 为 0。

可变 速率窗口计数 为 0。

可变 峰值速率 为 0。

可变 速率_EMA 为 0。

可变 速率_更新次数 为 0。

可变 IPI_单播数 为 0。

可变 IPI_广播数 为 0。

可变 IPI_自身数 为 0。

可变 IPI_超时数 为 0。

可变 IPI_INIT数 为 0。

可变 IPI_SIPI数 为 0。

可变 EOI_总数 为 0。

可变 向量计数表 为 一组：（空）。

可变 向量计数_已初始化 为 假。

可变 保存_定时器LVT 为 0。

可变 保存_LINT0 为 0。

可变 保存_LINT1 为 0。

可变 保存_错误LVT 为 0。

可变 保存_热感LVT 为 0。

可变 保存_性能LVT 为 0。

可变 保存_TPR 为 0。

可变 保存_分频 为 0。

可变 保存_初始计数 为 0。

可变 保存_SVR 为 0。

可变 已保存 为 假。

可变 统计_错误数 为 0。

可变 统计_读取数 为 0。

可变 统计_写入数 为 0。

中控_读寄存器 接受 偏移：
    统计_读取数 为 统计_读取数 加 1。
    返还 做 内存_读32 用 中控基址 加 偏移。
完。

中控_写寄存器 接受 偏移、值：
    统计_写入数 为 统计_写入数 加 1。
    做 内存_写32 用 中控基址 加 偏移、值。
完。

中控_初始化 接受 基址：
    若 中控_统计_已初始化 等于 真：
        做 打印 用 「[APIC统计] 警告: 重复初始化, 跳过」。
        返还 错误_已存在。
    完。
    中控基址 为 基址。
    若 中控基址 等于 0：
        中控基址 为 中控_寄存器基。
    完。
    中控标识 为 做 中控_读寄存器 用 中控_标识偏移 右移 24 位与 0xFF。
    伪值 为 做 中控_读寄存器 用 中控_伪中断偏移。
    做 中控_写寄存器 用 中控_伪中断偏移、伪值 位或 0x1FF。
    向量计数表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 256：
        做 追加 用 向量计数表、0。
        i 为 i 加 1。
    完。
    向量计数_已初始化 为 真。
    中断总数 为 0。
    核间中断数 为 0。
    定时中断数 为 0。
    热感中断数 为 0。
    错误中断数 为 0。
    伪中断数 为 0。
    性能中断数 为 0。
    外部中断数 为 0。
    LINT0中断数 为 0。
    LINT1中断数 为 0。
    IPI_单播数 为 0。
    IPI_广播数 为 0。
    IPI_自身数 为 0。
    IPI_超时数 为 0。
    IPI_INIT数 为 0。
    IPI_SIPI数 为 0。
    EOI_总数 为 0。
    中断速率 为 0。
    峰值速率 为 0。
    速率_EMA 为 0。
    速率_更新次数 为 0。
    统计_错误数 为 0。
    统计_读取数 为 0。
    统计_写入数 为 0。
    中控已启用 为 真。
    中控_统计_已初始化 为 真。
    上次速率时刻 为 做 平台_获取高精度时间。
    做 打印 用 「[APIC统计] 初始化完成 ID=」 加 做 转字符串 用 中控标识。
    返还 0。
完。

中控_发送结束信号 接受 零参：
    做 中控_写寄存器 用 中控_结束偏移、0。
    EOI_总数 为 EOI_总数 加 1。
完。

中控_设优先级 接受 优先级：
    若 优先级 大于 255：
        统计_错误数 为 统计_错误数 加 1。
        返还 错误_无效参数。
    完。
    做 中控_写寄存器 用 中控_优先偏移、优先级 位与 0xFF。
    返还 0。
完。

中控_获取优先级 接受 零参：
    返还 做 中控_读寄存器 用 中控_优先偏移 位与 0xFF。
完。

中控_发送核间中断 接受 目标核、向量：
    若 中控已启用 等于 假：
        统计_错误数 为 统计_错误数 加 1。
        返还 错误_未初始化。
    完。
    高值 为 (目标核 位与 0xFF) 左移 24。
    低值 为 向量 位与 0xFF。
    做 中控_写寄存器 用 中控_命令高偏移、高值。
    做 中控_写寄存器 用 中控_命令低偏移、低值。
    核间中断数 为 核间中断数 加 1。
    IPI_单播数 为 IPI_单播数 加 1。
    可变 等待 为 0。
    只要 等待 小于 10000：
        若 做 中控_读寄存器 用 中控_命令低偏移 位与 1 左移 12 等于 0：
            返还 0。
        完。
        等待 为 等待 加 1。
    完。
    IPI_超时数 为 IPI_超时数 加 1。
    统计_错误数 为 统计_错误数 加 1。
    返还 错误_超时。
完。

中控_广播核间中断 接受 向量：
    若 中控已启用 等于 假：
        统计_错误数 为 统计_错误数 加 1。
        返还 错误_未初始化。
    完。
    低值 为 向量 位与 0xFF 位或 3 左移 18。
    做 中控_写寄存器 用 中控_命令高偏移、0。
    做 中控_写寄存器 用 中控_命令低偏移、低值。
    核间中断数 为 核间中断数 加 1。
    IPI_广播数 为 IPI_广播数 加 1。
    返还 0。
完。

中控_发送自身IPI 接受 向量：
    低值 为 向量 位与 0xFF 位或 1 左移 18。
    做 中控_写寄存器 用 中控_命令高偏移、0。
    做 中控_写寄存器 用 中控_命令低偏移、低值。
    核间中断数 为 核间中断数 加 1。
    IPI_自身数 为 IPI_自身数 加 1。
    返还 0。
完。

中控_发送INIT_IPI 接受 目标核：
    高值 为 (目标核 位与 0xFF) 左移 24。
    低值 为 0x00004500。
    做 中控_写寄存器 用 中控_命令高偏移、高值。
    做 中控_写寄存器 用 中控_命令低偏移、低值。
    IPI_INIT数 为 IPI_INIT数 加 1。
    返还 0。
完。

中控_发送SIPI 接受 目标核、启动页：
    高值 为 (目标核 位与 0xFF) 左移 24。
    低值 为 0x00004600 位或 启动页 位与 0xFF。
    做 中控_写寄存器 用 中控_命令高偏移、高值。
    做 中控_写寄存器 用 中控_命令低偏移、低值。
    IPI_SIPI数 为 IPI_SIPI数 加 1。
    返还 0。
完。

中控_配置定时器 接受 分频、初始值、周期：
    做 中控_写寄存器 用 中控_分频偏移、分频 位与 0xF。
    定时配置 为 32。
    若 周期：
        定时配置 为 定时配置 位或 1 左移 17。
    完。
    做 中控_写寄存器 用 中控_定时偏移、定时配置。
    做 中控_写寄存器 用 中控_初始计数偏移、初始值。
    返还 0。
完。

中控_停止定时器 接受 零参：
    做 中控_写寄存器 用 中控_定时偏移、做 中控_读寄存器 用 中控_定时偏移 位或 0x10000。
    做 中控_写寄存器 用 中控_初始计数偏移、0。
    返还 0。
完。

中控_读当前计数 接受 零参：
    返还 做 中控_读寄存器 用 中控_当前计数偏移。
完。

中控_配置热感中断 接受 向量：
    做 中控_写寄存器 用 中控_热感偏移、向量 位与 0xFF。
    返还 0。
完。

中控_配置性能中断 接受 向量：
    做 中控_写寄存器 用 中控_性能偏移、向量 位与 0xFF。
    返还 0。
完。

中控_记录中断 接受 类型：
    中断总数 为 中断总数 加 1。
    速率窗口计数 为 速率窗口计数 加 1。
    若 类型 等于 中断类型_定时器：
        定时中断数 为 定时中断数 加 1。
    否则若 类型 等于 中断类型_热感：
        热感中断数 为 热感中断数 加 1。
    否则若 类型 等于 中断类型_错误：
        错误中断数 为 错误中断数 加 1。
    否则若 类型 等于 中断类型_伪：
        伪中断数 为 伪中断数 加 1。
    否则若 类型 等于 中断类型_IPI：
        核间中断数 为 核间中断数 加 1。
    否则若 类型 等于 中断类型_性能：
        性能中断数 为 性能中断数 加 1。
    否则若 类型 等于 中断类型_外部：
        外部中断数 为 外部中断数 加 1。
    否则若 类型 等于 中断类型_LINT0：
        LINT0中断数 为 LINT0中断数 加 1。
    否则若 类型 等于 中断类型_LINT1：
        LINT1中断数 为 LINT1中断数 加 1。
    完。
完。

中控_记录中断_向量 接受 向量号、类型：
    做 中控_记录中断 用 类型。
    若 向量计数_已初始化 等于 真 并且 向量号 小于 256 并且 向量号 大于或等于 0：
        做 设置 用 向量计数表、向量号、向量计数表 的 第 向量号 项 加 1。
    完。
完。

中控_获取向量计数 接受 向量号：
    若 向量计数_已初始化 等于 假 或者 向量号 大于或等于 256 或者 向量号 小于 0：
        返还 0。
    完。
    返还 向量计数表 的 第 向量号 项。
完。

中控_获取最热向量 接受 前N：
    若 向量计数_已初始化 等于 假：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 n 为 0。
    只要 n 小于 前N：
        可变 最大值 为 0。
        可变 最大向量 为 0 减 1。
        可变 i 为 0。
        只要 i 小于 256：
            若 向量计数表 的 第 i 项 大于 最大值：
                可变 已有 为 假。
                可变 j 为 0。
                只要 j 小于 做 计数 用 结果：
                    若 结果 的 第 j 项 的 第 0 项 等于 i：
                        已有 为 真。
                    完。
                    j 为 j 加 1。
                完。
                若 已有 等于 假：
                    最大值 为 向量计数表 的 第 i 项。
                    最大向量 为 i。
                完。
            完。
            i 为 i 加 1。
        完。
        若 最大向量 大于或等于 0 并且 最大值 大于 0：
            做 追加 用 结果、一组：最大向量、最大值。
        完。
        n 为 n 加 1。
    完。
    返还 结果。
完。

中控_更新速率 接受 零参：
    现在 为 做 平台_获取高精度时间。
    间隔 为 现在 减 上次速率时刻。
    若 间隔 大于或等于 1000000：
        中断速率 为 速率窗口计数 乘 1000000 除以 间隔。
        速率_EMA 为 (速率_EMA 乘 7 加 中断速率) 除以 8。
        若 中断速率 大于 峰值速率：
            峰值速率 为 中断速率。
        完。
        速率窗口计数 为 0。
        上次速率时刻 为 现在。
        速率_更新次数 为 速率_更新次数 加 1。
    完。
完。

中控_获取标识 接受 零参：
    返还 中控标识。
完。

中控_获取速率 接受 零参：
    返还 中断速率。
完。

中控_获取峰值速率 接受 零参：
    返还 峰值速率。
完。

中控_获取平滑速率 接受 零参：
    返还 速率_EMA。
完。

中控_保存寄存器 接受 零参：
    若 中控已启用 等于 假：
        返还 错误_未初始化。
    完。
    保存_定时器LVT 为 做 中控_读寄存器 用 中控_定时偏移。
    保存_LINT0 为 做 中控_读寄存器 用 中控_LINT0偏移。
    保存_LINT1 为 做 中控_读寄存器 用 中控_LINT1偏移。
    保存_错误LVT 为 做 中控_读寄存器 用 中控_错误偏移。
    保存_热感LVT 为 做 中控_读寄存器 用 中控_热感偏移。
    保存_性能LVT 为 做 中控_读寄存器 用 中控_性能偏移。
    保存_TPR 为 做 中控_读寄存器 用 中控_优先偏移。
    保存_分频 为 做 中控_读寄存器 用 中控_分频偏移。
    保存_初始计数 为 做 中控_读寄存器 用 中控_初始计数偏移。
    保存_SVR 为 做 中控_读寄存器 用 中控_伪中断偏移。
    已保存 为 真。
    做 打印 用 「[APIC统计] 寄存器已保存」。
    返还 0。
完。

中控_恢复寄存器 接受 零参：
    若 已保存 等于 假：
        返还 错误_无效参数。
    完。
    做 中控_写寄存器 用 中控_伪中断偏移、保存_SVR。
    做 中控_写寄存器 用 中控_优先偏移、保存_TPR。
    做 中控_写寄存器 用 中控_定时偏移、保存_定时器LVT。
    做 中控_写寄存器 用 中控_LINT0偏移、保存_LINT0。
    做 中控_写寄存器 用 中控_LINT1偏移、保存_LINT1。
    做 中控_写寄存器 用 中控_错误偏移、保存_错误LVT。
    做 中控_写寄存器 用 中控_热感偏移、保存_热感LVT。
    做 中控_写寄存器 用 中控_性能偏移、保存_性能LVT。
    做 中控_写寄存器 用 中控_分频偏移、保存_分频。
    做 中控_写寄存器 用 中控_初始计数偏移、保存_初始计数。
    做 打印 用 「[APIC统计] 寄存器已恢复」。
    返还 0。
完。

中控_中断类型名 接受 类型：
    若 类型 等于 中断类型_定时器：
        返还 「定时器」。
    完。
    若 类型 等于 中断类型_热感：
        返还 「温度」。
    完。
    若 类型 等于 中断类型_错误：
        返还 「错误」。
    完。
    若 类型 等于 中断类型_伪：
        返还 「伪中断」。
    完。
    若 类型 等于 中断类型_IPI：
        返还 「IPI」。
    完。
    若 类型 等于 中断类型_性能：
        返还 「性能」。
    完。
    若 类型 等于 中断类型_外部：
        返还 「外部」。
    完。
    若 类型 等于 中断类型_LINT0：
        返还 「LINT0」。
    完。
    若 类型 等于 中断类型_LINT1：
        返还 「LINT1」。
    完。
    返还 「未知」。
完。

中控_完整统计 接受 零参：
    统计 为 做 字典。
    做 字典设 用 统计、「基址」、中控基址。
    做 字典设 用 统计、「已启用」、中控已启用。
    做 字典设 用 统计、「标识」、中控标识。
    做 字典设 用 统计、「中断总数」、中断总数。
    做 字典设 用 统计、「定时中断」、定时中断数。
    做 字典设 用 统计、「IPI中断」、核间中断数。
    做 字典设 用 统计、「热感中断」、热感中断数。
    做 字典设 用 统计、「性能中断」、性能中断数。
    做 字典设 用 统计、「错误中断」、错误中断数。
    做 字典设 用 统计、「伪中断」、伪中断数。
    做 字典设 用 统计、「外部中断」、外部中断数。
    做 字典设 用 统计、「LINT0中断」、LINT0中断数。
    做 字典设 用 统计、「LINT1中断」、LINT1中断数。
    做 字典设 用 统计、「EOI总数」、EOI_总数。
    做 字典设 用 统计、「当前速率」、中断速率。
    做 字典设 用 统计、「峰值速率」、峰值速率。
    做 字典设 用 统计、「平滑速率」、速率_EMA。
    做 字典设 用 统计、「IPI单播」、IPI_单播数。
    做 字典设 用 统计、「IPI广播」、IPI_广播数。
    做 字典设 用 统计、「IPI自身」、IPI_自身数。
    做 字典设 用 统计、「IPI超时」、IPI_超时数。
    做 字典设 用 统计、「IPI_INIT」、IPI_INIT数。
    做 字典设 用 统计、「IPI_SIPI」、IPI_SIPI数。
    做 字典设 用 统计、「寄存器读取数」、统计_读取数。
    做 字典设 用 统计、「寄存器写入数」、统计_写入数。
    做 字典设 用 统计、「错误数」、统计_错误数。
    做 字典设 用 统计、「已保存」、已保存。
    返还 统计。
完。

中控_打印完整统计 接受 零参：
    统计 为 做 中控_完整统计。
    做 打印 用 「[APIC统计] ═══════ 完整统计 ═══════」。
    做 打印 用 「  基址:         0x」 加 做 转字符串 用 做 字典取 用 统计、「基址」。
    做 打印 用 「  标识(ID):     」 加 做 转字符串 用 做 字典取 用 统计、「标识」。
    做 打印 用 「  中断总数:     」 加 做 转字符串 用 做 字典取 用 统计、「中断总数」。
    做 打印 用 「  ── 按类型 ──」。
    做 打印 用 「  定时器:       」 加 做 转字符串 用 做 字典取 用 统计、「定时中断」。
    做 打印 用 「  IPI:          」 加 做 转字符串 用 做 字典取 用 统计、「IPI中断」。
    做 打印 用 「  热感:         」 加 做 转字符串 用 做 字典取 用 统计、「热感中断」。
    做 打印 用 「  性能:         」 加 做 转字符串 用 做 字典取 用 统计、「性能中断」。
    做 打印 用 「  错误:         」 加 做 转字符串 用 做 字典取 用 统计、「错误中断」。
    做 打印 用 「  伪中断:       」 加 做 转字符串 用 做 字典取 用 统计、「伪中断」。
    做 打印 用 「  外部:         」 加 做 转字符串 用 做 字典取 用 统计、「外部中断」。
    做 打印 用 「  EOI总数:      」 加 做 转字符串 用 做 字典取 用 统计、「EOI总数」。
    做 打印 用 「  ── 速率 ──」。
    做 打印 用 「  当前速率:     」 加 做 转字符串 用 做 字典取 用 统计、「当前速率」 加 「 次/秒」。
    做 打印 用 「  峰值速率:     」 加 做 转字符串 用 做 字典取 用 统计、「峰值速率」 加 「 次/秒」。
    做 打印 用 「  平滑速率:     」 加 做 转字符串 用 做 字典取 用 统计、「平滑速率」 加 「 次/秒」。
    做 打印 用 「  ── IPI详情 ──」。
    做 打印 用 「  IPI单播:      」 加 做 转字符串 用 做 字典取 用 统计、「IPI单播」。
    做 打印 用 「  IPI广播:      」 加 做 转字符串 用 做 字典取 用 统计、「IPI广播」。
    做 打印 用 「  IPI_INIT:     」 加 做 转字符串 用 做 字典取 用 统计、「IPI_INIT」。
    做 打印 用 「  IPI_SIPI:     」 加 做 转字符串 用 做 字典取 用 统计、「IPI_SIPI」。
    做 打印 用 「  IPI超时:      」 加 做 转字符串 用 做 字典取 用 统计、「IPI超时」。
    热向量 为 做 中控_获取最热向量 用 5。
    若 做 计数 用 热向量 大于 0：
        做 打印 用 「  ── 最热向量 ──」。
        可变 h 为 0。
        只要 h 小于 做 计数 用 热向量：
            做 打印 用 「  向量」 加 做 转字符串 用 热向量 的 第 h 项 的 第 0 项 加 「: 」 加 做 转字符串 用 热向量 的 第 h 项 的 第 1 项 加 「 次」。
            h 为 h 加 1。
        完。
    完。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[APIC统计] ═════════════════════════」。
    返还 0。
完。

中控_dump 接受 零参：
    做 打印 用 「[APIC统计 dump] ──────────────────」。
    做 打印 用 「  已初始化: 」 加 做 转字符串 用 中控_统计_已初始化。
    做 打印 用 「  基址: 0x」 加 做 转字符串 用 中控基址。
    做 打印 用 「  ID: 」 加 做 转字符串 用 中控标识。
    做 打印 用 「  中断总数: 」 加 做 转字符串 用 中断总数。
    做 打印 用 「  EOI总数: 」 加 做 转字符串 用 EOI_总数。
    做 打印 用 「  速率: 」 加 做 转字符串 用 中断速率 加 「 峰值: 」 加 做 转字符串 用 峰值速率。
    做 打印 用 「  寄存器读取: 」 加 做 转字符串 用 统计_读取数。
    做 打印 用 「  寄存器写入: 」 加 做 转字符串 用 统计_写入数。
    做 打印 用 「  向量计数已初始化: 」 加 做 转字符串 用 向量计数_已初始化。
    做 打印 用 「  寄存器已保存: 」 加 做 转字符串 用 已保存。
    做 打印 用 「[APIC统计 dump] ──────────────────」。
完。

中控_销毁 接受 零参：
    若 中控_统计_已初始化 等于 假：
        返还 0。
    完。
    向量计数表 为 一组：（空）。
    向量计数_已初始化 为 假。
    中控已启用 为 假。
    中控_统计_已初始化 为 假。
    做 打印 用 「[APIC统计] 已销毁」。
    返还 0。
完。

中控_为调度器提供中断负载 接受 零参：
    返还 一组：中断速率、峰值速率、速率_EMA。
完。

中控_为电源管理挂起 接受 零参：
    做 中控_保存寄存器。
    返还 0。
完。

中控_为电源管理恢复 接受 零参：
    做 中控_恢复寄存器。
    返还 0。
完。

中控_为SMP启动发送IPI 接受 目标核、启动页：
    做 中控_发送INIT_IPI 用 目标核。
    做 中控_发送SIPI 用 目标核、启动页。
    返还 0。
完。

中控_重置统计 接受 零参：
    中断总数 为 0。
    核间中断数 为 0。
    定时中断数 为 0。
    热感中断数 为 0。
    错误中断数 为 0。
    伪中断数 为 0。
    性能中断数 为 0。
    外部中断数 为 0。
    LINT0中断数 为 0。
    LINT1中断数 为 0。
    EOI_总数 为 0。
    IPI_单播数 为 0。
    IPI_广播数 为 0。
    IPI_自身数 为 0。
    IPI_超时数 为 0。
    中断速率 为 0。
    峰值速率 为 0。
    速率_EMA 为 0。
    若 向量计数_已初始化 等于 真：
        可变 i 为 0。
        只要 i 小于 256：
            做 设置 用 向量计数表、i、0。
            i 为 i 加 1。
        完。
    完。
    做 打印 用 「[APIC统计] 统计已重置」。
    返还 0。
完。

本章 收束。
