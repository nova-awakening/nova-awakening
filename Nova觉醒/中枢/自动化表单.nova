《自动化表单》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·哈希整数》。

引用《.·字符串替换》。

引用《.·内存初始化》。

引用《.·内容可见性》。

引用《..·感知·内核直连层》。

外部。

字符串取字符。

外部。

字符串转字节。

最大表单 为 64。

最大账号 为 128。

速率窗口 为 60。

最大速率 为 10。

表单信息 是 一种 事物，含 地址哈希、字段数、用户名字段、密码字段、邮箱字段、验证码字段、提交按钮、发现时刻。

账号记录 是 一种 事物，含 用户名哈希、状态、创建时刻、最近登录、登录次数、失败次数。

可变 表单表 为 一组：（空）。

可变 表单数 为 0。

可变 账号表 为 一组：（空）。

可变 账号数 为 0。

可变 总尝试 为 0。

可变 总成功 为 0。

可变 总失败 为 0。

可变 验证码处理 为 0。

可变 验证码成功 为 0。

可变 速率计数 为 0。

可变 速率起始 为 0。

自动化_初始化 接受 零参：
    表单表 为 一组：（空）。
    表单数 为 0。
    账号表 为 一组：（空）。
    账号数 为 0。
    速率起始 为 做 平台_获取高精度时间。
完。

自动化_发现表单 接受 页面地址、字段列表、字段数：
    若 表单数 大于或等于 最大表单：
        返还 负 1。
    完。
    表单 为 新建 表单信息。
    表单 的 地址哈希 为 做 哈希_字符串 用 页面地址。
    表单 的 字段数 为 字段数。
    表单 的 用户名字段 为 负 1。
    表单 的 密码字段 为 负 1。
    表单 的 邮箱字段 为 负 1。
    表单 的 验证码字段 为 负 1。
    表单 的 提交按钮 为 负 1。
    表单 的 发现时刻 为 做 平台_获取高精度时间。
    可变 序号 为 0。
    只要 序号 小于 字段数：
        字段类型 为 做 内存_读32 用 字段列表 加 序号 乘 8。
        字段名哈希 为 做 内存_读32 用 字段列表 加 序号 乘 8 加 4。
        若 字段类型 等于 1：
            表单 的 用户名字段 为 序号。
        完。
        若 字段类型 等于 2：
            表单 的 密码字段 为 序号。
        完。
        若 字段类型 等于 3：
            表单 的 邮箱字段 为 序号。
        完。
        若 字段类型 等于 4：
            表单 的 验证码字段 为 序号。
        完。
        若 字段类型 等于 5：
            表单 的 提交按钮 为 序号。
        完。
        序号 为 序号 加 1。
    完。
    做 追加 用 表单表、表单。
    表单数 为 表单数 加 1。
    返还 表单数 减 1。
完。

自动化_检查速率 接受 零参：
    现在 为 做 平台_获取高精度时间。
    若 现在 减 速率起始 大于 60000000：
        速率计数 为 0。
        速率起始 为 现在。
    完。
    若 速率计数 大于或等于 最大速率：
        返还 假。
    完。
    速率计数 为 速率计数 加 1。
    返还 真。
完。

自动化_尝试注册 接受 表单索引、用户名、密码、邮箱：
    总尝试 为 总尝试 加 1。
    若 非 做 自动化_检查速率：
        总失败 为 总失败 加 1。
        返还 错误_频率过高。
    完。
    若 表单索引 大于或等于 表单数：
        总失败 为 总失败 加 1。
        返还 错误_无效参数。
    完。
    表单 为 表单表 的 第 表单索引 项。
    若 表单 的 用户名字段 小于 0 或者 表单 的 密码字段 小于 0：
        总失败 为 总失败 加 1。
        返还 错误_表单不完整。
    完。
    账号 为 新建 账号记录。
    账号 的 用户名哈希 为 做 哈希_字符串 用 用户名。
    账号 的 状态 为 1。
    账号 的 创建时刻 为 做 平台_获取高精度时间。
    账号 的 最近登录 为 0。
    账号 的 登录次数 为 0。
    账号 的 失败次数 为 0。
    若 账号数 小于 最大账号：
        做 追加 用 账号表、账号。
        账号数 为 账号数 加 1。
    完。
    总成功 为 总成功 加 1。
    返还 0。
完。

自动化_处理验证码 接受 图像数据、宽、高：
    验证码处理 为 验证码处理 加 1。
    若 图像数据 等于 0 或者 宽 等于 0 或者 高 等于 0：
        返还 「」。
    完。
    可变 像素总和 为 0。
    可变 序号 为 0。
    只要 序号 小于 宽 乘 高：
        像素总和 为 像素总和 加 做 内存_读8 用 图像数据 加 序号。
        序号 为 序号 加 1。
    完。
    平均值 为 像素总和 除以 (宽 乘 高)。
    可变 结果 为 「」。
    可变 区域 为 0。
    只要 区域 小于 6：
        区域起始 为 区域 乘 宽 除以 6。
        区域结束 为 (区域 加 1) 乘 宽 除以 6。
        可变 区域和 为 0。
        可变 像素 为 区域起始。
        只要 像素 小于 区域结束：
            可变 行 为 0。
            只要 行 小于 高：
                区域和 为 区域和 加 做 内存_读8 用 图像数据 加 行 乘 宽 加 像素。
                行 为 行 加 1。
            完。
            像素 为 像素 加 1。
        完。
        区域均值 为 区域和 除以 ((区域结束 减 区域起始) 乘 高)。
        字符估计 为 区域均值 乘 10 除以 256 加 48。
        结果 为 结果 加 做 字符串取字符 用 字符估计。
        区域 为 区域 加 1。
    完。
    验证码成功 为 验证码成功 加 1。
    返还 结果。
完。

自动化_查找账号 接受 用户名：
    目标哈希 为 做 哈希_字符串 用 用户名。
    可变 序号 为 0。
    只要 序号 小于 账号数：
        若 账号表 的 第 序号 项 的 用户名哈希 等于 目标哈希：
            返还 账号表 的 第 序号 项。
        完。
        序号 为 序号 加 1。
    完。
    返还 0。
完。

自动化_记录登录 接受 用户名、成功：
    账号 为 做 自动化_查找账号 用 用户名。
    若 账号 等于 0：
        返还。
    完。
    若 成功：
        账号 的 登录次数 为 账号 的 登录次数 加 1。
        账号 的 最近登录 为 做 平台_获取高精度时间。
    否则：
        账号 的 失败次数 为 账号 的 失败次数 加 1。
    完。
完。

自动化注册_完整统计 接受 零参：
    返还 一组：总尝试、总成功、总失败、表单数、账号数、验证码处理、验证码成功。
完。

可变 AI_新系统自动_威胁计数 为 0。

AI_新系统自动_风险评分 接受 操作类型、权限级别、来源可信度：
    可变 风险 为 0。
    若 权限级别 大于 500：
        风险 为 风险 加 300。
    完。
    若 来源可信度 小于 200：
        风险 为 风险 加 400。
    完。
    若 风险 大于 500：
        AI_新系统自动_威胁计数 为 AI_新系统自动_威胁计数 加 1。
    完。
    返还 风险。
完。

AI_新系统自动_异常行为检测 接受 操作频率、基线频率：
    若 基线频率 大于 0 并且 操作频率 大于 基线频率 乘 5：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
