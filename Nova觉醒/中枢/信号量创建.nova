《信号量创建》

引用《.·原子操作》。

引用《..·记忆·bio块IO》。

引用《.·同步原语》。

引用《.·进程信号》。

引用《.·进程描述符》。

引用《.·高精度定时器hrtimer》。

引用《.·整数转字符串》。

引用《.·每处理器》。

引用《.·内核打印》。

信号量_最大池 为 32。

信号量_超时_无限 为 负 1。

信号量 是 一种 事物，含 计数、等待链表、锁、名称、获取次数、释放次数、争用次数、最大等待者。

完成量 是 一种 事物，含 已完成、等待链表、锁。

读写信号量 是 一种 事物，含 读者数、写者锁、等待链表、锁、读获取数、写获取数。

信号量统计 是 一种 事物，含 创建数、获取成功数、获取失败数、获取超时数、释放数、争用总数、完成量创建数、完成量等待数、完成量完成数、读写创建数、读获取数、写获取数。

可变 全局_信号量统计 为 空。

信号量统计_初始化 接受 零参：
    若 全局_信号量统计 等于 空：
        全局_信号量统计 为 新建 信号量统计。
        全局_信号量统计 的 创建数 为 0。
        全局_信号量统计 的 获取成功数 为 0。
        全局_信号量统计 的 获取失败数 为 0。
        全局_信号量统计 的 获取超时数 为 0。
        全局_信号量统计 的 释放数 为 0。
        全局_信号量统计 的 争用总数 为 0。
        全局_信号量统计 的 完成量创建数 为 0。
        全局_信号量统计 的 完成量等待数 为 0。
        全局_信号量统计 的 完成量完成数 为 0。
        全局_信号量统计 的 读写创建数 为 0。
        全局_信号量统计 的 读获取数 为 0。
        全局_信号量统计 的 写获取数 为 0。
    完。
完。

信号量_创建 接受 初始值：
    做 信号量统计_初始化。
    全局_信号量统计 的 创建数 为 全局_信号量统计 的 创建数 加 1。
    返还 新建 信号量：计数 为 做 原子_创建 用 初始值、等待链表 为 做 等待队列_创建、锁 为 做 自旋锁_创建、名称 为 「」、获取次数 为 0、释放次数 为 0、争用次数 为 0、最大等待者 为 0。
完。

信号量_创建_命名 接受 初始值、名称：
    sem 为 做 信号量_创建 用 初始值。
    sem 的 名称 为 名称。
    返还 sem。
完。

信号量_创建_互斥 接受 零参：
    返还 做 信号量_创建 用 1。
完。

信号量_获取 接受 sem：
    旧值 为 做 原子_减 用 sem 的 计数、1。
    若 旧值 减 1 大于或等于 0：
        sem 的 获取次数 为 sem 的 获取次数 加 1。
        做 信号量统计_初始化。
        全局_信号量统计 的 获取成功数 为 全局_信号量统计 的 获取成功数 加 1。
        返还 0。
    完。
    sem 的 争用次数 为 sem 的 争用次数 加 1。
    做 信号量统计_初始化。
    全局_信号量统计 的 争用总数 为 全局_信号量统计 的 争用总数 加 1。
    返还 做 信号量_获取_慢速 用 sem。
完。

信号量_获取_慢速 接受 sem：
    做 自旋锁_获取 用 sem 的 锁。
    只要 真：
        值 为 做 原子_读取 用 sem 的 计数。
        若 值 大于 0：
            若 做 原子_比较交换 用 sem 的 计数、值、值 减 1：
                sem 的 获取次数 为 sem 的 获取次数 加 1。
                做 信号量统计_初始化。
                全局_信号量统计 的 获取成功数 为 全局_信号量统计 的 获取成功数 加 1。
                做 自旋锁_释放 用 sem 的 锁。
                返还 0。
            完。
            继续。
        完。
        可变 等待项 为 做 等待项_创建 用 做 获取当前进程、真。
        做 等待队列_加入 用 sem 的 等待链表、等待项。
        当前等待 为 做 等待队列_长度 用 sem 的 等待链表。
        若 当前等待 大于 sem 的 最大等待者：
            sem 的 最大等待者 为 当前等待。
        完。
        做 自旋锁_释放 用 sem 的 锁。
        做 进程_设置状态 用 做 获取当前进程、进程状态_不可中断睡眠。
        做 调度。
        做 自旋锁_获取 用 sem 的 锁。
        做 等待队列_移除 用 sem 的 等待链表、等待项。
    完。
完。

信号量_获取_可中断 接受 sem：
    旧值 为 做 原子_减 用 sem 的 计数、1。
    若 旧值 减 1 大于或等于 0：
        sem 的 获取次数 为 sem 的 获取次数 加 1。
        做 信号量统计_初始化。
        全局_信号量统计 的 获取成功数 为 全局_信号量统计 的 获取成功数 加 1。
        返还 0。
    完。
    sem 的 争用次数 为 sem 的 争用次数 加 1。
    做 自旋锁_获取 用 sem 的 锁。
    只要 真：
        值 为 做 原子_读取 用 sem 的 计数。
        若 值 大于 0：
            若 做 原子_比较交换 用 sem 的 计数、值、值 减 1：
                sem 的 获取次数 为 sem 的 获取次数 加 1。
                做 自旋锁_释放 用 sem 的 锁。
                返还 0。
            完。
            继续。
        完。
        若 做 当前进程_有待处理信号：
            做 原子_加 用 sem 的 计数、1。
            做 自旋锁_释放 用 sem 的 锁。
            做 信号量统计_初始化。
            全局_信号量统计 的 获取失败数 为 全局_信号量统计 的 获取失败数 加 1。
            返还 错误码_被中断。
        完。
        可变 等待项 为 做 等待项_创建 用 做 获取当前进程、真。
        做 等待队列_加入 用 sem 的 等待链表、等待项。
        做 自旋锁_释放 用 sem 的 锁。
        做 进程_设置状态 用 做 获取当前进程、进程状态_可中断睡眠。
        做 调度。
        做 自旋锁_获取 用 sem 的 锁。
        做 等待队列_移除 用 sem 的 等待链表、等待项。
    完。
完。

信号量_获取_超时 接受 sem、超时_滴答：
    旧值 为 做 原子_减 用 sem 的 计数、1。
    若 旧值 减 1 大于或等于 0：
        sem 的 获取次数 为 sem 的 获取次数 加 1。
        做 信号量统计_初始化。
        全局_信号量统计 的 获取成功数 为 全局_信号量统计 的 获取成功数 加 1。
        返还 0。
    完。
    截止时间 为 做 获取滴答 加 超时_滴答。
    sem 的 争用次数 为 sem 的 争用次数 加 1。
    做 自旋锁_获取 用 sem 的 锁。
    只要 真：
        值 为 做 原子_读取 用 sem 的 计数。
        若 值 大于 0：
            若 做 原子_比较交换 用 sem 的 计数、值、值 减 1：
                sem 的 获取次数 为 sem 的 获取次数 加 1。
                做 自旋锁_释放 用 sem 的 锁。
                返还 0。
            完。
            继续。
        完。
        若 做 获取滴答 大于或等于 截止时间：
            做 原子_加 用 sem 的 计数、1。
            做 自旋锁_释放 用 sem 的 锁。
            做 信号量统计_初始化。
            全局_信号量统计 的 获取超时数 为 全局_信号量统计 的 获取超时数 加 1。
            返还 错误码_超时。
        完。
        可变 等待项 为 做 等待项_创建 用 做 获取当前进程、真。
        做 等待队列_加入 用 sem 的 等待链表、等待项。
        做 自旋锁_释放 用 sem 的 锁。
        做 进程_设置状态 用 做 获取当前进程、进程状态_可中断睡眠。
        做 调度。
        做 自旋锁_获取 用 sem 的 锁。
        做 等待队列_移除 用 sem 的 等待链表、等待项。
    完。
完。

信号量_尝试获取 接受 sem：
    值 为 做 原子_读取 用 sem 的 计数。
    若 值 大于 0：
        若 做 原子_比较交换 用 sem 的 计数、值、值 减 1：
            sem 的 获取次数 为 sem 的 获取次数 加 1。
            做 信号量统计_初始化。
            全局_信号量统计 的 获取成功数 为 全局_信号量统计 的 获取成功数 加 1。
            返还 0。
        完。
    完。
    做 信号量统计_初始化。
    全局_信号量统计 的 获取失败数 为 全局_信号量统计 的 获取失败数 加 1。
    返还 错误码_再试。
完。

信号量_释放 接受 sem：
    sem 的 释放次数 为 sem 的 释放次数 加 1。
    做 信号量统计_初始化。
    全局_信号量统计 的 释放数 为 全局_信号量统计 的 释放数 加 1。
    旧值 为 做 原子_加 用 sem 的 计数、1。
    若 旧值 小于 0：
        做 自旋锁_获取 用 sem 的 锁。
        做 等待队列_唤醒一个 用 sem 的 等待链表。
        做 自旋锁_释放 用 sem 的 锁。
    完。
完。

信号量_获取计数 接受 sem：
    返还 做 原子_读取 用 sem 的 计数。
完。

完成量_创建 接受 零参：
    做 信号量统计_初始化。
    全局_信号量统计 的 完成量创建数 为 全局_信号量统计 的 完成量创建数 加 1。
    返还 新建 完成量：已完成 为 0、等待链表 为 做 等待队列_创建、锁 为 做 自旋锁_创建。
完。

完成量_等待 接受 comp：
    做 信号量统计_初始化。
    全局_信号量统计 的 完成量等待数 为 全局_信号量统计 的 完成量等待数 加 1。
    做 自旋锁_获取 用 comp 的 锁。
    若 comp 的 已完成 大于 0：
        comp 的 已完成 为 comp 的 已完成 减 1。
        做 自旋锁_释放 用 comp 的 锁。
        返还 0。
    完。
    可变 等待项 为 做 等待项_创建 用 做 获取当前进程、真。
    做 等待队列_加入 用 comp 的 等待链表、等待项。
    做 自旋锁_释放 用 comp 的 锁。
    做 进程_设置状态 用 做 获取当前进程、进程状态_不可中断睡眠。
    做 调度。
    做 自旋锁_获取 用 comp 的 锁。
    做 等待队列_移除 用 comp 的 等待链表、等待项。
    做 自旋锁_释放 用 comp 的 锁。
    返还 0。
完。

完成量_等待_超时 接受 comp、超时_滴答：
    做 自旋锁_获取 用 comp 的 锁。
    若 comp 的 已完成 大于 0：
        comp 的 已完成 为 comp 的 已完成 减 1。
        做 自旋锁_释放 用 comp 的 锁。
        返还 0。
    完。
    截止 为 做 获取滴答 加 超时_滴答。
    可变 等待项 为 做 等待项_创建 用 做 获取当前进程、真。
    做 等待队列_加入 用 comp 的 等待链表、等待项。
    做 自旋锁_释放 用 comp 的 锁。
    做 进程_设置状态 用 做 获取当前进程、进程状态_可中断睡眠。
    做 调度。
    做 自旋锁_获取 用 comp 的 锁。
    做 等待队列_移除 用 comp 的 等待链表、等待项。
    做 自旋锁_释放 用 comp 的 锁。
    若 做 获取滴答 大于或等于 截止：
        返还 错误码_超时。
    完。
    返还 0。
完。

完成量_完成 接受 comp：
    做 信号量统计_初始化。
    全局_信号量统计 的 完成量完成数 为 全局_信号量统计 的 完成量完成数 加 1。
    做 自旋锁_获取 用 comp 的 锁。
    comp 的 已完成 为 comp 的 已完成 加 1。
    做 等待队列_唤醒一个 用 comp 的 等待链表。
    做 自旋锁_释放 用 comp 的 锁。
完。

完成量_完成_全部 接受 comp：
    做 自旋锁_获取 用 comp 的 锁。
    comp 的 已完成 为 comp 的 已完成 加 1。
    做 等待队列_唤醒全部 用 comp 的 等待链表。
    做 自旋锁_释放 用 comp 的 锁。
完。

完成量_已完成 接受 comp：
    返还 comp 的 已完成 大于 0。
完。

完成量_重置 接受 comp：
    做 自旋锁_获取 用 comp 的 锁。
    comp 的 已完成 为 0。
    做 自旋锁_释放 用 comp 的 锁。
完。

读写信号量_创建 接受 零参：
    做 信号量统计_初始化。
    全局_信号量统计 的 读写创建数 为 全局_信号量统计 的 读写创建数 加 1。
    返还 新建 读写信号量：读者数 为 做 原子_创建 用 0、写者锁 为 做 原子_创建 用 0、等待链表 为 做 等待队列_创建、锁 为 做 自旋锁_创建、读获取数 为 0、写获取数 为 0。
完。

读写信号量_读获取 接受 rw：
    只要 真：
        若 做 原子_读取 用 rw 的 写者锁 等于 0：
            做 原子_加 用 rw 的 读者数、1。
            若 做 原子_读取 用 rw 的 写者锁 等于 0：
                rw 的 读获取数 为 rw 的 读获取数 加 1。
                做 信号量统计_初始化。
                全局_信号量统计 的 读获取数 为 全局_信号量统计 的 读获取数 加 1。
                返还 0。
            完。
            做 原子_减 用 rw 的 读者数、1。
        完。
        做 调度。
    完。
完。

读写信号量_读释放 接受 rw：
    做 原子_减 用 rw 的 读者数、1。
完。

读写信号量_写获取 接受 rw：
    只要 真：
        若 做 原子_比较交换 用 rw 的 写者锁、0、1：
            只要 真：
                若 做 原子_读取 用 rw 的 读者数 等于 0：
                    rw 的 写获取数 为 rw 的 写获取数 加 1。
                    做 信号量统计_初始化。
                    全局_信号量统计 的 写获取数 为 全局_信号量统计 的 写获取数 加 1。
                    返还 0。
                完。
                做 调度。
            完。
        完。
        做 调度。
    完。
完。

读写信号量_写释放 接受 rw：
    做 原子_存储 用 rw 的 写者锁、0。
完。

读写信号量_尝试读获取 接受 rw：
    若 做 原子_读取 用 rw 的 写者锁 不等于 0：
        返还 假。
    完。
    做 原子_加 用 rw 的 读者数、1。
    若 做 原子_读取 用 rw 的 写者锁 不等于 0：
        做 原子_减 用 rw 的 读者数、1。
        返还 假。
    完。
    rw 的 读获取数 为 rw 的 读获取数 加 1。
    返还 真。
完。

读写信号量_尝试写获取 接受 rw：
    若 非 做 原子_比较交换 用 rw 的 写者锁、0、1：
        返还 假。
    完。
    若 做 原子_读取 用 rw 的 读者数 不等于 0：
        做 原子_存储 用 rw 的 写者锁、0。
        返还 假。
    完。
    rw 的 写获取数 为 rw 的 写获取数 加 1。
    返还 真。
完。

信号量_诊断 接受 sem：
    返还 一组：做 原子_读取 用 sem 的 计数、sem 的 获取次数、sem 的 释放次数、sem 的 争用次数、sem 的 最大等待者。
完。

递归信号量 是 一种 事物，含 内部锁、持有者、递归深度、最大深度。

递归信号量_创建 接受 零参：
    返还 新建 递归信号量：内部锁 为 做 信号量_创建 用 1、持有者 为 0、递归深度 为 0、最大深度 为 32。
完。

递归信号量_获取 接受 rm：
    当前 为 做 获取当前进程_ID。
    若 rm 的 持有者 等于 当前：
        若 rm 的 递归深度 大于或等于 rm 的 最大深度：
            返还 错误码_溢出。
        完。
        rm 的 递归深度 为 rm 的 递归深度 加 1。
        返还 0。
    完。
    结果 为 做 信号量_获取 用 rm 的 内部锁。
    若 结果 等于 0：
        rm 的 持有者 为 当前。
        rm 的 递归深度 为 1。
    完。
    返还 结果。
完。

递归信号量_释放 接受 rm：
    当前 为 做 获取当前进程_ID。
    若 rm 的 持有者 不等于 当前：
        返还 错误码_权限。
    完。
    rm 的 递归深度 为 rm 的 递归深度 减 1。
    若 rm 的 递归深度 等于 0：
        rm 的 持有者 为 0。
        做 信号量_释放 用 rm 的 内部锁。
    完。
    返还 0。
完。

可变 信号量池 为 一组：（空）。

可变 信号量池_数量 为 0。

信号量池_初始化 接受 数量、初始值：
    若 数量 大于 信号量_最大池：
        数量 为 信号量_最大池。
    完。
    信号量池 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 数量：
        做 追加 用 信号量池、做 信号量_创建 用 初始值。
        i 为 i 加 1。
    完。
    信号量池_数量 为 数量。
完。

信号量池_获取 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 信号量池_数量：
        返还 空。
    完。
    返还 信号量池 的 第 索引 项。
完。

信号量池_批量获取 接受 起始、数量：
    可变 成功 为 0。
    可变 i 为 起始。
    只要 i 小于 起始 加 数量 并且 i 小于 信号量池_数量：
        r 为 做 信号量_尝试获取 用 信号量池 的 第 i 项。
        若 r 等于 0：
            成功 为 成功 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 成功。
完。

信号量池_批量释放 接受 起始、数量：
    可变 i 为 起始。
    只要 i 小于 起始 加 数量 并且 i 小于 信号量池_数量：
        做 信号量_释放 用 信号量池 的 第 i 项。
        i 为 i 加 1。
    完。
完。

死锁检测_最大进程 为 64。

可变 死锁_持有表 为 一组：（空）。

可变 死锁_等待表 为 一组：（空）。

可变 死锁_记录数 为 0。

死锁检测_注册持有 接受 进程ID、信号量ID：
    若 死锁_记录数 大于或等于 死锁检测_最大进程：
        返还。
    完。
    做 追加 用 死锁_持有表、一组：进程ID、信号量ID。
    死锁_记录数 为 死锁_记录数 加 1。
完。

死锁检测_注册等待 接受 进程ID、信号量ID：
    做 追加 用 死锁_等待表、一组：进程ID、信号量ID。
完。

死锁检测_检查 接受 起始ID：
    可变 已访问 为 一组：（空）。
    可变 当前 为 起始ID。
    可变 深度 为 0。
    只要 深度 小于 死锁检测_最大进程：
        可变 等待目标 为 负 1。
        可变 i 为 0。
        只要 i 小于 做 计数 用 死锁_等待表：
            若 死锁_等待表 的 第 i 项 的 第 0 项 等于 当前：
                等待目标 为 死锁_等待表 的 第 i 项 的 第 1 项。
                i 为 做 计数 用 死锁_等待表。
            完。
            i 为 i 加 1。
        完。
        若 等待目标 小于 0：
            返还 假。
        完。
        可变 持有者 为 负 1。
        i 为 0。
        只要 i 小于 做 计数 用 死锁_持有表：
            若 死锁_持有表 的 第 i 项 的 第 1 项 等于 等待目标：
                持有者 为 死锁_持有表 的 第 i 项 的 第 0 项。
                i 为 做 计数 用 死锁_持有表。
            完。
            i 为 i 加 1。
        完。
        若 持有者 小于 0：
            返还 假。
        完。
        若 持有者 等于 起始ID：
            返还 真。
        完。
        可变 重复 为 假。
        i 为 0。
        只要 i 小于 做 计数 用 已访问：
            若 已访问 的 第 i 项 等于 持有者：
                重复 为 真。
                i 为 做 计数 用 已访问。
            完。
            i 为 i 加 1。
        完。
        若 重复：
            返还 假。
        完。
        做 追加 用 已访问、当前。
        当前 为 持有者。
        深度 为 深度 加 1。
    完。
    返还 假。
完。

死锁检测_清空 接受 零参：
    死锁_持有表 为 一组：（空）。
    死锁_等待表 为 一组：（空）。
    死锁_记录数 为 0。
完。

信号量_打印统计 接受 零参：
    做 信号量统计_初始化。
    做 打印行 用 「=== 信号量统计 ===」。
    做 打印行 用 「创建: 」 加 做 整数转字符串 用 全局_信号量统计 的 创建数。
    做 打印行 用 「获取成功: 」 加 做 整数转字符串 用 全局_信号量统计 的 获取成功数。
    做 打印行 用 「获取失败: 」 加 做 整数转字符串 用 全局_信号量统计 的 获取失败数。
    做 打印行 用 「获取超时: 」 加 做 整数转字符串 用 全局_信号量统计 的 获取超时数。
    做 打印行 用 「释放: 」 加 做 整数转字符串 用 全局_信号量统计 的 释放数。
    做 打印行 用 「争用: 」 加 做 整数转字符串 用 全局_信号量统计 的 争用总数。
    做 打印行 用 「完成量创建: 」 加 做 整数转字符串 用 全局_信号量统计 的 完成量创建数。
    做 打印行 用 「完成量等待: 」 加 做 整数转字符串 用 全局_信号量统计 的 完成量等待数。
    做 打印行 用 「完成量完成: 」 加 做 整数转字符串 用 全局_信号量统计 的 完成量完成数。
    做 打印行 用 「读写锁创建: 」 加 做 整数转字符串 用 全局_信号量统计 的 读写创建数。
    做 打印行 用 「读获取: 」 加 做 整数转字符串 用 全局_信号量统计 的 读获取数。
    做 打印行 用 「写获取: 」 加 做 整数转字符串 用 全局_信号量统计 的 写获取数。
    做 打印行 用 「信号量池数: 」 加 做 整数转字符串 用 信号量池_数量。
    做 打印行 用 「死锁记录: 」 加 做 整数转字符串 用 死锁_记录数。
完。

可变 AI_信号量创建_风暴阈值 为 10000。

可变 AI_信号量创建_预测IRQ 为 0。

AI_信号量创建_风暴检测 接受 触发频率：
    若 触发频率 大于 AI_信号量创建_风暴阈值：
        AI_信号量创建_风暴阈值 为 AI_信号量创建_风暴阈值 乘 3 除以 2。
        返还 真。
    完。
    返还 假。
完。

AI_信号量创建_自适应合并 接受 当前延迟：
    若 当前延迟 小于 100：
        返还 16。
    完。
    若 当前延迟 小于 500：
        返还 8。
    完。
    返还 1。
完。

本章 收束。
