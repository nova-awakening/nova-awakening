《中断请求管理》

引用《.·日志中枢》。

引用《.·自动扩容》。

引用《.·字符串替换》。

引用《.·内核打印》。

引用《.·页表重建》。

IRQ_无 为 0。

IRQ_已处理 为 1。

IRQ_唤醒线程 为 2。

IRQF_共享 为 0x80。

IRQF_定时器 为 0x200。

IRQF_不平衡 为 0x400。

IRQF_边沿触发 为 0x01。

IRQF_电平触发 为 0x02。

IRQF_线程化 为 0x800。

IRQF_不挂起 为 0x4000。

IRQF_每CPU 为 0x100。

ID_处理链 为 0。

ID_计数 为 1。

ID_名称 为 2。

ID_已禁用 为 3。

ID_标志 为 4。

ID_亲和CPU 为 5。

ID_伪中断数 为 6。

ID_线程计数 为 7。

ID_最后触发时间 为 8。

ID_连续伪中断 为 9。

ID_已自动禁用 为 10。

ID_线程待处理 为 11。

HC_处理函数 为 0。

HC_名称 为 1。

HC_标志 为 2。

HC_线程函数 为 3。

HC_触发次数 为 4。

最大IRQ 为 256。

可变 IRQ描述符表 为 一组：（空）。

可变 IRQ总计数 为 0。

可变 IRQ伪总计 为 0。

可变 IRQ_管理_已初始化 为 假。

可变 管理_注册总数 为 0。

可变 管理_注销总数 为 0。

可变 管理_启用总数 为 0。

可变 管理_禁用总数 为 0。

可变 管理_线程唤醒总数 为 0。

可变 管理_亲和设置总数 为 0。

可变 管理_风暴禁用总数 为 0。

可变 管理_重触发总数 为 0。

可变 管理_错误总数 为 0。

可变 管理_批量注册总数 为 0。

可变 管理_批量注销总数 为 0。

可变 IRQ_线程化_回退同步执行 为 真。

伪中断风暴阈值 为 100000。

连续伪中断重置间隔 为 1000。

IRQ描述符_创建 接受 零参：
    返还 一组：一组：（空）、0、「」、0、0、0 减 1、0、0、0、0、假、0。
完。

IRQ_初始化 接受 零参：
    若 IRQ_管理_已初始化 等于 真：
        做 内核警告 用 「[IRQ管理] 警告: 重复初始化, 跳过」。
        返还 错误_已存在。
    完。
    IRQ描述符表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大IRQ：
        做 追加 用 IRQ描述符表、做 IRQ描述符_创建。
        i 为 i 加 1。
    完。
    IRQ总计数 为 0。
    IRQ伪总计 为 0。
    管理_注册总数 为 0。
    管理_注销总数 为 0。
    管理_启用总数 为 0。
    管理_禁用总数 为 0。
    管理_线程唤醒总数 为 0。
    管理_亲和设置总数 为 0。
    管理_风暴禁用总数 为 0。
    管理_重触发总数 为 0。
    管理_错误总数 为 0。
    管理_批量注册总数 为 0。
    管理_批量注销总数 为 0。
    IRQ_管理_已初始化 为 真。
    做 早期打印OK 用 「[IRQ管理] 初始化完成 (256向量)」。
    返还 0。
完。

IRQ_注册 接受 IRQ号、名称、处理函数：
    返还 做 IRQ_注册_带标志 用 IRQ号、名称、处理函数、0。
完。

IRQ_注册_线程化 接受 IRQ号、名称、硬中断函数、线程函数、标志：
    若 IRQ_管理_已初始化 等于 假：
        管理_错误总数 为 管理_错误总数 加 1。
        返还 错误_未初始化。
    完。
    若 IRQ号 大于或等于 最大IRQ：
        管理_错误总数 为 管理_错误总数 加 1。
        返还 错误_无效参数。
    完。
    若 硬中断函数 等于 空 并且 线程函数 等于 空：
        管理_错误总数 为 管理_错误总数 加 1。
        返还 错误_无效参数。
    完。
    若 名称 等于 空 或者 名称 等于 「」：
        管理_错误总数 为 管理_错误总数 加 1。
        返还 错误_无效参数。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    若 做 计数 用 描述符 的 第 ID_处理链 项 大于 0：
        若 标志 位与 IRQF_共享 等于 0 或者 描述符 的 第 ID_标志 项 位与 IRQF_共享 等于 0：
            管理_错误总数 为 管理_错误总数 加 1。
            返还 错误_设备忙。
        完。
    完。
    做 追加 用 描述符 的 第 ID_处理链 项、一组：硬中断函数、名称、标志 位或 IRQF_线程化、线程函数、0。
    若 描述符 的 第 ID_名称 项 等于 「」：
        做 设置 用 描述符、ID_名称、名称。
    完。
    做 设置 用 描述符、ID_标志、描述符 的 第 ID_标志 项 位或 标志 位或 IRQF_线程化。
    管理_注册总数 为 管理_注册总数 加 1。
    做 内核调试 用 「[IRQ管理] 线程化注册: IRQ」 加 做 转字符串 用 IRQ号 加 「 - 」 加 名称。
    返还 0。
完。

IRQ_注册_带标志 接受 IRQ号、名称、处理函数、标志：
    若 IRQ_管理_已初始化 等于 假：
        管理_错误总数 为 管理_错误总数 加 1。
        返还 错误_未初始化。
    完。
    若 IRQ号 大于或等于 最大IRQ：
        管理_错误总数 为 管理_错误总数 加 1。
        返还 错误_无效参数。
    完。
    若 处理函数 等于 空：
        管理_错误总数 为 管理_错误总数 加 1。
        返还 错误_无效参数。
    完。
    若 名称 等于 空 或者 名称 等于 「」：
        管理_错误总数 为 管理_错误总数 加 1。
        返还 错误_无效参数。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    若 描述符 的 第 ID_已自动禁用 项 等于 真：
        做 内核警告 用 「[IRQ管理] IRQ」 加 做 转字符串 用 IRQ号 加 「 已因风暴被自动禁用」。
        管理_错误总数 为 管理_错误总数 加 1。
        返还 错误_设备忙。
    完。
    若 做 计数 用 描述符 的 第 ID_处理链 项 大于 0：
        若 标志 位与 IRQF_共享 等于 0 或者 描述符 的 第 ID_标志 项 位与 IRQF_共享 等于 0：
            管理_错误总数 为 管理_错误总数 加 1。
            返还 错误_设备忙。
        完。
    完。
    做 追加 用 描述符 的 第 ID_处理链 项、一组：处理函数、名称、标志、空、0。
    若 描述符 的 第 ID_名称 项 等于 「」：
        做 设置 用 描述符、ID_名称、名称。
    完。
    做 设置 用 描述符、ID_标志、描述符 的 第 ID_标志 项 位或 标志。
    管理_注册总数 为 管理_注册总数 加 1。
    做 内核调试 用 「[IRQ管理] 注册: IRQ」 加 做 转字符串 用 IRQ号 加 「 - 」 加 名称。
    返还 0。
完。

IRQ_处理 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    若 描述符 的 第 ID_已禁用 项 大于 0：
        返还。
    完。
    若 描述符 的 第 ID_已自动禁用 项 等于 真：
        返还。
    完。
    做 设置 用 描述符、ID_计数、描述符 的 第 ID_计数 项 加 1。
    IRQ总计数 为 IRQ总计数 加 1。
    处理链 为 描述符 的 第 ID_处理链 项。
    可变 已处理 为 假。
    可变 需要线程 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 处理链：
        处理项 为 处理链 的 第 i 项。
        处理函数 为 处理项 的 第 HC_处理函数 项。
        若 处理函数 不等于 空：
            结果 为 做 处理函数 用 IRQ号。
            做 设置 用 处理项、HC_触发次数、处理项 的 第 HC_触发次数 项 加 1。
            若 结果 等于 IRQ_已处理：
                已处理 为 真。
            否则若 结果 等于 IRQ_唤醒线程：
                已处理 为 真。
                需要线程 为 真。
            完。
        完。
        i 为 i 加 1。
    完。
    若 已处理 等于 假 并且 做 计数 用 处理链 大于 0：
        做 设置 用 描述符、ID_伪中断数、描述符 的 第 ID_伪中断数 项 加 1。
        做 设置 用 描述符、ID_连续伪中断、描述符 的 第 ID_连续伪中断 项 加 1。
        IRQ伪总计 为 IRQ伪总计 加 1。
        若 描述符 的 第 ID_连续伪中断 项 大于或等于 伪中断风暴阈值：
            做 设置 用 描述符、ID_已自动禁用、真。
            管理_风暴禁用总数 为 管理_风暴禁用总数 加 1。
            做 内核警告 用 「[IRQ管理] IRQ」 加 做 转字符串 用 IRQ号 加 「 伪中断风暴! 已自动禁用」。
        完。
    否则若 已处理 等于 真：
        做 设置 用 描述符、ID_连续伪中断、0。
    完。
    若 需要线程 等于 真：
        做 IRQ_安排线程处理 用 IRQ号。
    完。
    若 做 计数 用 处理链 等于 0：
        做 内核警告 用 「[IRQ管理] 未注册IRQ: 」 加 做 转字符串 用 IRQ号。
    完。
完。

IRQ_安排线程处理 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 错误_无效参数。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    若 做 计数 用 描述符 的 第 ID_处理链 项 等于 0：
        返还 错误_不存在。
    完。
    做 设置 用 描述符、ID_线程计数、描述符 的 第 ID_线程计数 项 加 1。
    做 设置 用 描述符、ID_线程待处理、描述符 的 第 ID_线程待处理 项 加 1。
    管理_线程唤醒总数 为 管理_线程唤醒总数 加 1。
    若 IRQ_线程化_回退同步执行 等于 真：
        做 IRQ_执行线程处理 用 IRQ号。
    完。
    返还 0。
完。

IRQ_执行线程处理 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    只要 描述符 的 第 ID_线程待处理 项 大于 0：
        处理链 为 描述符 的 第 ID_处理链 项。
        可变 已执行 为 假。
        可变 i 为 0。
        只要 i 小于 做 计数 用 处理链：
            处理项 为 处理链 的 第 i 项。
            若 处理项 的 第 HC_线程函数 项 不等于 空：
                做 处理项 的 第 HC_线程函数 项 用 IRQ号。
                已执行 为 真。
            完。
            i 为 i 加 1。
        完。
        做 设置 用 描述符、ID_线程待处理、描述符 的 第 ID_线程待处理 项 减 1。
        若 已执行 等于 假：
            中断。
        完。
    完。
    若 描述符 的 第 ID_线程待处理 项 小于 0：
        做 设置 用 描述符、ID_线程待处理、0。
    完。
完。

IRQ_重触发 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 错误_无效参数。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    若 描述符 的 第 ID_已禁用 项 大于 0：
        返还 错误_设备忙。
    完。
    管理_重触发总数 为 管理_重触发总数 加 1。
    做 内核调试 用 「[IRQ管理] 重触发: IRQ」 加 做 转字符串 用 IRQ号。
    做 IRQ_处理 用 IRQ号。
    返还 0。
完。

IRQ_注销 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 错误_无效参数。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    做 设置 用 描述符、ID_处理链、一组：（空）。
    做 设置 用 描述符、ID_名称、「」。
    做 设置 用 描述符、ID_标志、0。
    做 设置 用 描述符、ID_已自动禁用、假。
    做 设置 用 描述符、ID_连续伪中断、0。
    管理_注销总数 为 管理_注销总数 加 1。
    返还 0。
完。

IRQ_注销_处理 接受 IRQ号、处理函数：
    若 IRQ号 大于或等于 最大IRQ：
        返还 错误_无效参数。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    处理链 为 描述符 的 第 ID_处理链 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 处理链：
        若 处理链 的 第 i 项 的 第 HC_处理函数 项 等于 处理函数：
            做 删除 用 处理链、i。
            管理_注销总数 为 管理_注销总数 加 1。
            若 做 计数 用 处理链 等于 0：
                做 设置 用 描述符、ID_名称、「」。
                做 设置 用 描述符、ID_标志、0。
            完。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    管理_错误总数 为 管理_错误总数 加 1。
    返还 错误_不存在。
完。

IRQ_注销_按名称 接受 IRQ号、名称：
    若 IRQ号 大于或等于 最大IRQ：
        返还 错误_无效参数。
    完。
    若 名称 等于 空 或者 名称 等于 「」：
        返还 错误_无效参数。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    处理链 为 描述符 的 第 ID_处理链 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 处理链：
        若 处理链 的 第 i 项 的 第 HC_名称 项 等于 名称：
            做 删除 用 处理链、i。
            管理_注销总数 为 管理_注销总数 加 1。
            若 做 计数 用 处理链 等于 0：
                做 设置 用 描述符、ID_名称、「」。
                做 设置 用 描述符、ID_标志、0。
            否则：
                做 设置 用 描述符、ID_名称、处理链 的 第 0 项 的 第 HC_名称 项。
            完。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    管理_错误总数 为 管理_错误总数 加 1。
    返还 错误_不存在。
完。

IRQ_批量注册 接受 IRQ列表：
    若 IRQ列表 等于 空：
        返还 错误_无效参数。
    完。
    可变 成功数 为 0。
    可变 失败数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 IRQ列表：
        项 为 IRQ列表 的 第 i 项。
        结果 为 做 IRQ_注册_带标志 用 项 的 第 0 项、项 的 第 1 项、项 的 第 2 项、项 的 第 3 项。
        若 结果 等于 0：
            成功数 为 成功数 加 1。
        否则：
            失败数 为 失败数 加 1。
        完。
        i 为 i 加 1。
    完。
    管理_批量注册总数 为 管理_批量注册总数 加 1。
    若 失败数 大于 0：
        做 内核警告 用 「[IRQ管理] 批量注册: 」 加 做 转字符串 用 成功数 加 「成功, 」 加 做 转字符串 用 失败数 加 「失败」。
    完。
    返还 成功数。
完。

IRQ_批量注销 接受 IRQ号列表：
    若 IRQ号列表 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 IRQ号列表：
        做 IRQ_注销 用 IRQ号列表 的 第 i 项。
        i 为 i 加 1。
    完。
    管理_批量注销总数 为 管理_批量注销总数 加 1。
    返还 0。
完。

IRQ_批量禁用 接受 IRQ号列表：
    若 IRQ号列表 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 IRQ号列表：
        做 IRQ_禁用 用 IRQ号列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 0。
完。

IRQ_批量使能 接受 IRQ号列表：
    若 IRQ号列表 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 IRQ号列表：
        做 IRQ_使能 用 IRQ号列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 0。
完。

IRQ_禁用 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 错误_无效参数。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    做 设置 用 描述符、ID_已禁用、描述符 的 第 ID_已禁用 项 加 1。
    管理_禁用总数 为 管理_禁用总数 加 1。
    返还 0。
完。

IRQ_使能 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 错误_无效参数。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    若 描述符 的 第 ID_已禁用 项 大于 0：
        做 设置 用 描述符、ID_已禁用、描述符 的 第 ID_已禁用 项 减 1。
    完。
    管理_启用总数 为 管理_启用总数 加 1。
    返还 0。
完。

IRQ_已禁用 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 真。
    完。
    返还 IRQ描述符表 的 第 IRQ号 项 的 第 ID_已禁用 项 大于 0。
完。

IRQ_恢复自动禁用 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 错误_无效参数。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    若 描述符 的 第 ID_已自动禁用 项 等于 假：
        返还 0。
    完。
    做 设置 用 描述符、ID_已自动禁用、假。
    做 设置 用 描述符、ID_连续伪中断、0。
    做 内核信息 用 「[IRQ管理] IRQ」 加 做 转字符串 用 IRQ号 加 「 已恢复(从自动禁用)」。
    返还 0。
完。

IRQ_设置亲和 接受 IRQ号、CPU掩码：
    若 IRQ号 大于或等于 最大IRQ：
        返还 错误_无效参数。
    完。
    若 CPU掩码 等于 0：
        返还 错误_无效参数。
    完。
    做 设置 用 IRQ描述符表 的 第 IRQ号 项、ID_亲和CPU、CPU掩码。
    管理_亲和设置总数 为 管理_亲和设置总数 加 1。
    返还 0。
完。

IRQ_获取亲和 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 0。
    完。
    返还 IRQ描述符表 的 第 IRQ号 项 的 第 ID_亲和CPU 项。
完。

IRQ_计数 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 0。
    完。
    返还 IRQ描述符表 的 第 IRQ号 项 的 第 ID_计数 项。
完。

IRQ_名称 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 「」。
    完。
    返还 IRQ描述符表 的 第 IRQ号 项 的 第 ID_名称 项。
完。

IRQ_获取总计数 接受 零参：
    返还 IRQ总计数。
完。

IRQ_获取处理链长度 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 0。
    完。
    返还 做 计数 用 IRQ描述符表 的 第 IRQ号 项 的 第 ID_处理链 项。
完。

IRQ_获取所有名称 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 一组：（空）。
    完。
    处理链 为 IRQ描述符表 的 第 IRQ号 项 的 第 ID_处理链 项。
    可变 名称列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 处理链：
        做 追加 用 名称列表、处理链 的 第 i 项 的 第 HC_名称 项。
        i 为 i 加 1。
    完。
    返还 名称列表。
完。

IRQ_是线程化 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 假。
    完。
    返还 IRQ描述符表 的 第 IRQ号 项 的 第 ID_标志 项 位与 IRQF_线程化 不等于 0。
完。

IRQ_伪中断数 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还 0。
    完。
    返还 IRQ描述符表 的 第 IRQ号 项 的 第 ID_伪中断数 项。
完。

IRQ_活跃列表 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大IRQ：
        若 IRQ描述符表 的 第 i 项 的 第 ID_计数 项 大于 0 或者 做 计数 用 IRQ描述符表 的 第 i 项 的 第 ID_处理链 项 大于 0：
            做 追加 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

IRQ_已注册列表 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大IRQ：
        若 做 计数 用 IRQ描述符表 的 第 i 项 的 第 ID_处理链 项 大于 0：
            做 追加 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

IRQ_打印统计 接受 零参：
    可变 i 为 0。
    做 内核信息 用 「       CPU0      名称           handler数  伪中断  标志」。
    只要 i 小于 最大IRQ：
        描述符 为 IRQ描述符表 的 第 i 项。
        若 描述符 的 第 ID_计数 项 大于 0 或者 做 计数 用 描述符 的 第 ID_处理链 项 大于 0：
            可变 行 为 「IRQ」 加 做 转字符串 用 i 加 「: 」。
            行 为 行 加 做 转字符串 用 描述符 的 第 ID_计数 项 加 「  」。
            行 为 行 加 描述符 的 第 ID_名称 项 加 「  」。
            行 为 行 加 「h=」 加 做 转字符串 用 做 计数 用 描述符 的 第 ID_处理链 项 加 「  」。
            行 为 行 加 「伪=」 加 做 转字符串 用 描述符 的 第 ID_伪中断数 项。
            若 描述符 的 第 ID_已禁用 项 大于 0：
                行 为 行 加 「 [禁用]」。
            完。
            若 描述符 的 第 ID_已自动禁用 项 等于 真：
                行 为 行 加 「 [风暴禁用]」。
            完。
            若 描述符 的 第 ID_标志 项 位与 IRQF_共享 不等于 0：
                行 为 行 加 「 [共享]」。
            完。
            若 描述符 的 第 ID_标志 项 位与 IRQF_线程化 不等于 0：
                行 为 行 加 「 [线程]」。
            完。
            做 内核信息 用 行。
        完。
        i 为 i 加 1。
    完。
    做 内核信息 用 「─── 总计 ───」。
    做 内核信息 用 「  总触发: 」 加 做 转字符串 用 IRQ总计数。
    做 内核信息 用 「  总伪中断: 」 加 做 转字符串 用 IRQ伪总计。
    做 内核信息 用 「  总注册: 」 加 做 转字符串 用 管理_注册总数。
    做 内核信息 用 「  总注销: 」 加 做 转字符串 用 管理_注销总数。
完。

IRQ_完整统计 接受 零参：
    已注册 为 做 计数 用 做 IRQ_已注册列表。
    活跃 为 做 计数 用 做 IRQ_活跃列表。
    可变 共享数 为 0。
    可变 最热IRQ 为 0。
    可变 最热计数 为 0。
    可变 自动禁用数 为 0。
    可变 i 为 0。
    只要 i 小于 最大IRQ：
        描述符 为 IRQ描述符表 的 第 i 项。
        若 做 计数 用 描述符 的 第 ID_处理链 项 大于 1：
            共享数 为 共享数 加 1。
        完。
        若 描述符 的 第 ID_已自动禁用 项 等于 真：
            自动禁用数 为 自动禁用数 加 1。
        完。
        若 描述符 的 第 ID_计数 项 大于 最热计数：
            最热计数 为 描述符 的 第 ID_计数 项。
            最热IRQ 为 i。
        完。
        i 为 i 加 1。
    完。
    统计 为 做 字典。
    做 字典设 用 统计、「总触发数」、IRQ总计数。
    做 字典设 用 统计、「总伪中断数」、IRQ伪总计。
    做 字典设 用 统计、「总注册操作」、管理_注册总数。
    做 字典设 用 统计、「总注销操作」、管理_注销总数。
    做 字典设 用 统计、「总启用操作」、管理_启用总数。
    做 字典设 用 统计、「总禁用操作」、管理_禁用总数。
    做 字典设 用 统计、「线程唤醒总数」、管理_线程唤醒总数。
    做 字典设 用 统计、「亲和设置总数」、管理_亲和设置总数。
    做 字典设 用 统计、「风暴禁用总数」、管理_风暴禁用总数。
    做 字典设 用 统计、「重触发总数」、管理_重触发总数。
    做 字典设 用 统计、「批量注册总数」、管理_批量注册总数。
    做 字典设 用 统计、「错误总数」、管理_错误总数。
    做 字典设 用 统计、「当前已注册」、已注册。
    做 字典设 用 统计、「当前活跃」、活跃。
    做 字典设 用 统计、「当前共享IRQ」、共享数。
    做 字典设 用 统计、「当前自动禁用」、自动禁用数。
    做 字典设 用 统计、「最热IRQ号」、最热IRQ。
    做 字典设 用 统计、「最热触发数」、最热计数。
    做 字典设 用 统计、「最大IRQ数」、最大IRQ。
    返还 统计。
完。

IRQ_打印完整统计 接受 零参：
    统计 为 做 IRQ_完整统计。
    做 打印 用 「[IRQ管理] ═══════ 完整统计 ═══════」。
    做 打印 用 「  总触发数:       」 加 做 转字符串 用 做 字典取 用 统计、「总触发数」。
    做 打印 用 「  总伪中断数:     」 加 做 转字符串 用 做 字典取 用 统计、「总伪中断数」。
    做 打印 用 「  总注册操作:     」 加 做 转字符串 用 做 字典取 用 统计、「总注册操作」。
    做 打印 用 「  总注销操作:     」 加 做 转字符串 用 做 字典取 用 统计、「总注销操作」。
    做 打印 用 「  线程唤醒总数:   」 加 做 转字符串 用 做 字典取 用 统计、「线程唤醒总数」。
    做 打印 用 「  风暴禁用总数:   」 加 做 转字符串 用 做 字典取 用 统计、「风暴禁用总数」。
    做 打印 用 「  当前已注册:     」 加 做 转字符串 用 做 字典取 用 统计、「当前已注册」。
    做 打印 用 「  当前活跃:       」 加 做 转字符串 用 做 字典取 用 统计、「当前活跃」。
    做 打印 用 「  当前共享IRQ:    」 加 做 转字符串 用 做 字典取 用 统计、「当前共享IRQ」。
    做 打印 用 「  最热IRQ:        」 加 做 转字符串 用 做 字典取 用 统计、「最热IRQ号」。
    做 打印 用 「  最热触发数:     」 加 做 转字符串 用 做 字典取 用 统计、「最热触发数」。
    做 打印 用 「  错误总数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误总数」。
    做 打印 用 「[IRQ管理] ═════════════════════════」。
    返还 0。
完。

IRQ_dump 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ：
        返还。
    完。
    描述符 为 IRQ描述符表 的 第 IRQ号 项。
    做 打印 用 「[IRQ dump] IRQ=」 加 做 转字符串 用 IRQ号。
    做 打印 用 「  触发次数=」 加 做 转字符串 用 描述符 的 第 ID_计数 项。
    做 打印 用 「  名称=」 加 描述符 的 第 ID_名称 项。
    做 打印 用 「  handler数=」 加 做 转字符串 用 做 计数 用 描述符 的 第 ID_处理链 项。
    做 打印 用 「  伪中断=」 加 做 转字符串 用 描述符 的 第 ID_伪中断数 项。
    做 打印 用 「  连续伪中断=」 加 做 转字符串 用 描述符 的 第 ID_连续伪中断 项。
    做 打印 用 「  禁用深度=」 加 做 转字符串 用 描述符 的 第 ID_已禁用 项。
    做 打印 用 「  自动禁用=」 加 做 转字符串 用 描述符 的 第 ID_已自动禁用 项。
    做 打印 用 「  线程处理=」 加 做 转字符串 用 描述符 的 第 ID_线程计数 项。
    处理链 为 描述符 的 第 ID_处理链 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 处理链：
        项 为 处理链 的 第 i 项。
        做 打印 用 「  handler[」 加 做 转字符串 用 i 加 「]: 」 加 项 的 第 HC_名称 项 加 「 触发=」 加 做 转字符串 用 项 的 第 HC_触发次数 项。
        i 为 i 加 1。
    完。
完。

IRQ_dump全部 接受 零参：
    可变 i 为 0。
    只要 i 小于 最大IRQ：
        若 做 计数 用 IRQ描述符表 的 第 i 项 的 第 ID_处理链 项 大于 0 或者 IRQ描述符表 的 第 i 项 的 第 ID_计数 项 大于 0：
            做 IRQ_dump 用 i。
        完。
        i 为 i 加 1。
    完。
完。

IRQ_管理_销毁 接受 零参：
    若 IRQ_管理_已初始化 等于 假：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 最大IRQ：
        做 设置 用 IRQ描述符表 的 第 i 项、ID_处理链、一组：（空）。
        i 为 i 加 1。
    完。
    IRQ描述符表 为 一组：（空）。
    IRQ_管理_已初始化 为 假。
    做 内核信息 用 「[IRQ管理] 已销毁」。
    返还 0。
完。

IRQ_为调度器提供负载 接受 零参：
    可变 负载 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大IRQ：
        描述符 为 IRQ描述符表 的 第 i 项。
        若 描述符 的 第 ID_计数 项 大于 0 并且 描述符 的 第 ID_已禁用 项 等于 0：
            做 追加 用 负载、一组：i、描述符 的 第 ID_计数 项、描述符 的 第 ID_亲和CPU 项。
        完。
        i 为 i 加 1。
    完。
    返还 负载。
完。

IRQ_为电源管理提供活跃列表 接受 零参：
    返还 做 IRQ_已注册列表。
完。

IRQ_挂起全部 接受 零参：
    可变 i 为 0。
    可变 禁用数 为 0。
    只要 i 小于 最大IRQ：
        描述符 为 IRQ描述符表 的 第 i 项。
        若 做 计数 用 描述符 的 第 ID_处理链 项 大于 0：
            若 描述符 的 第 ID_标志 项 位与 IRQF_不挂起 等于 0：
                做 IRQ_禁用 用 i。
                禁用数 为 禁用数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    做 内核信息 用 「[IRQ管理] 挂起: 禁用了 」 加 做 转字符串 用 禁用数 加 「 个IRQ」。
    返还 禁用数。
完。

IRQ_恢复全部 接受 零参：
    可变 i 为 0。
    可变 启用数 为 0。
    只要 i 小于 最大IRQ：
        描述符 为 IRQ描述符表 的 第 i 项。
        若 做 计数 用 描述符 的 第 ID_处理链 项 大于 0 并且 描述符 的 第 ID_已禁用 项 大于 0：
            若 描述符 的 第 ID_标志 项 位与 IRQF_不挂起 等于 0：
                做 IRQ_使能 用 i。
                启用数 为 启用数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    做 内核信息 用 「[IRQ管理] 恢复: 启用了 」 加 做 转字符串 用 启用数 加 「 个IRQ」。
    返还 启用数。
完。

可变 AI_中断请求管_风暴阈值 为 10000。

可变 AI_中断请求管_预测IRQ 为 0。

AI_中断请求管_风暴检测 接受 触发频率：
    若 触发频率 大于 AI_中断请求管_风暴阈值：
        AI_中断请求管_风暴阈值 为 AI_中断请求管_风暴阈值 乘 3 除以 2。
        返还 真。
    完。
    返还 假。
完。

AI_中断请求管_自适应合并 接受 当前延迟：
    若 当前延迟 小于 100：
        返还 16。
    完。
    若 当前延迟 小于 500：
        返还 8。
    完。
    返还 1。
完。

本章 收束。
