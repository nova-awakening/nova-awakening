《灵敏度调节》

引用《.·类型定义》。

引用《.·错误码》。

AI模块数 为 21。

可变 全局灵敏度 为 500。

可变 通道灵敏度 为 一组：（空）。

可变 通道疲劳 为 一组：（空）。

可变 灵敏_已初始化 为 假。

灵敏_初始化 接受 零参：
    通道灵敏度 为 一组：（空）。
    通道疲劳 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI模块数：
        做 追加 用 通道灵敏度、500。
        做 追加 用 通道疲劳、0。
        i 为 i 加 1。
    完。
    全局灵敏度 为 500。
    灵敏_已初始化 为 真。
    做 内核信息 用 「灵敏度调节: 全局+21通道独立 AGC+疲劳模型」。
完。

灵敏_调整全局 接受 系统负载：
    若 系统负载 大于 80：
        全局灵敏度 为 200。
    否则若 系统负载 大于 60：
        全局灵敏度 为 350。
    否则若 系统负载 大于 40：
        全局灵敏度 为 500。
    否则：
        全局灵敏度 为 700。
    完。
完。

灵敏_施加 接受 模块索引、原始信号：
    若 非 灵敏_已初始化 或者 模块索引 大于或等于 AI模块数：
        返还 原始信号。
    完。
    通道 为 通道灵敏度 的 第 模块索引 项。
    疲劳 为 通道疲劳 的 第 模块索引 项。
    有效 为 全局灵敏度 乘 通道 除以 1000 乘 (1000 减 疲劳) 除以 1000。
    返还 原始信号 乘 有效 除以 500。
完。

灵敏_记录刺激 接受 模块索引、强度：
    若 模块索引 大于或等于 AI模块数：
        返还。
    完。
    若 强度 大于 800：
        通道疲劳 的 第 模块索引 项 为 通道疲劳 的 第 模块索引 项 加 50。
    否则若 强度 大于 500：
        通道疲劳 的 第 模块索引 项 为 通道疲劳 的 第 模块索引 项 加 20。
    完。
    若 通道疲劳 的 第 模块索引 项 大于 900：
        通道疲劳 的 第 模块索引 项 为 900。
    完。
完。

灵敏_恢复tick 接受 零参：
    可变 i 为 0。
    只要 i 小于 AI模块数：
        若 通道疲劳 的 第 i 项 大于 0：
            通道疲劳 的 第 i 项 为 通道疲劳 的 第 i 项 减 10。
        完。
        若 通道疲劳 的 第 i 项 小于 0：
            通道疲劳 的 第 i 项 为 0。
        完。
        i 为 i 加 1。
    完。
完。

灵敏_设置通道 接受 模块索引、灵敏度：
    若 模块索引 小于 AI模块数：
        通道灵敏度 的 第 模块索引 项 为 灵敏度。
    完。
完。

可变 AGC_目标电平 为 500。

可变 AGC_攻击速率 为 100。

可变 AGC_释放速率 为 20。

可变 AGC_当前增益 为 一组：（空）。

AGC_初始化 接受 零参：
    AGC_当前增益 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI模块数：
        做 追加 用 AGC_当前增益、1000。
        i 为 i 加 1。
    完。
完。

AGC_处理 接受 模块索引、信号强度：
    若 模块索引 大于或等于 AI模块数：
        返还 信号强度。
    完。
    若 做 计数 用 AGC_当前增益 小于或等于 模块索引：
        返还 信号强度。
    完。
    增益 为 AGC_当前增益 的 第 模块索引 项。
    输出 为 信号强度 乘 增益 除以 1000。
    若 输出 大于 AGC_目标电平：
        增益 为 增益 减 AGC_攻击速率。
        若 增益 小于 100：
            增益 为 100。
        完。
    否则若 输出 小于 AGC_目标电平 乘 600 除以 1000：
        增益 为 增益 加 AGC_释放速率。
        若 增益 大于 5000：
            增益 为 5000。
        完。
    完。
    做 设置 用 AGC_当前增益、模块索引、增益。
    返还 输出。
完。

灵敏_预设_高性能 接受 零参：
    全局灵敏度 为 800。
    可变 i 为 0。
    只要 i 小于 AI模块数：
        做 设置 用 通道灵敏度、i、800。
        i 为 i 加 1。
    完。
完。

灵敏_预设_节能 接受 零参：
    全局灵敏度 为 200。
    可变 i 为 0。
    只要 i 小于 AI模块数：
        做 设置 用 通道灵敏度、i、200。
        i 为 i 加 1。
    完。
完。

灵敏_预设_均衡 接受 零参：
    全局灵敏度 为 500。
    可变 i 为 0。
    只要 i 小于 AI模块数：
        做 设置 用 通道灵敏度、i、500。
        i 为 i 加 1。
    完。
完。

灵敏_统计 接受 零参：
    返还 一组：全局灵敏度、通道灵敏度、通道疲劳。
完。

灵敏_获取全局 接受 零参：
    返还 全局灵敏度。
完。

灵敏_获取通道 接受 i：
    若 i 小于 AI模块数：
        返还 通道灵敏度 的 第 i 项。
    完。
    返还 0。
完。

灵敏_获取疲劳 接受 i：
    若 i 小于 AI模块数：
        返还 通道疲劳 的 第 i 项。
    完。
    返还 0。
完。

灵敏_预设名 接受 零参：
    若 全局灵敏度 大于或等于 700：
        返还 「high_perf」。
    完。
    若 全局灵敏度 小于或等于 250：
        返还 「power_save」。
    完。
    返还 「balanced」。
完。

灵敏_平均疲劳 接受 零参：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 AI模块数：
        总 为 总 加 通道疲劳 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总 除以 AI模块数。
完。

灵敏_完整统计 接受 零参：
    返还 一组：全局灵敏度、做 灵敏_平均疲劳、AI模块数。
完。

本章 收束。
