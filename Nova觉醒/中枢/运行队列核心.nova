《运行队列核心》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·同步原语》。

引用《.·内容可见性》。

优先级数 为 40。

实时优先级数 为 100。

最大核数 为 64。

运行队列 是 一种 事物，含 锁、任务总数、公平任务数、实时任务数、公平队头、实时队头、空闲任务、当前任务、核号、时钟、切换次数、最近入队时刻、最近出队时刻、入队总数、出队总数、迁入数、迁出数、负载值。

可变 每核队列 为 一组：（空）。

可变 核数 为 1。

队列_初始化 接受 处理器数：
    核数 为 处理器数。
    若 核数 大于 最大核数：
        核数 为 最大核数。
    完。
    每核队列 为 一组：（空）。
    可变 序号 为 0。
    只要 序号 小于 核数：
        队 为 新建 运行队列。
        队 的 锁 为 做 自旋锁_创建。
        队 的 任务总数 为 0。
        队 的 公平任务数 为 0。
        队 的 实时任务数 为 0。
        队 的 公平队头 为 0。
        队 的 实时队头 为 0。
        队 的 空闲任务 为 0。
        队 的 当前任务 为 0。
        队 的 核号 为 序号。
        队 的 时钟 为 0。
        队 的 切换次数 为 0。
        队 的 入队总数 为 0。
        队 的 出队总数 为 0。
        队 的 迁入数 为 0。
        队 的 迁出数 为 0。
        队 的 负载值 为 0。
        做 追加 用 每核队列、队。
        序号 为 序号 加 1。
    完。
完。

队列_入队 接受 核号、任务：
    若 核号 大于或等于 核数 或者 任务 等于 0：
        返还。
    完。
    队 为 每核队列 的 第 核号 项。
    做 自旋锁_获取 用 队 的 锁。
    策略 为 任务 的 第 T_调度策略 项。
    若 策略 大于或等于 1 并且 策略 小于或等于 2：
        任务 的 第 T_队列后继 项 为 队 的 实时队头。
        队 的 实时队头 为 任务。
        队 的 实时任务数 为 队 的 实时任务数 加 1。
    否则：
        若 队 的 公平队头 等于 0：
            队 的 公平队头 为 任务。
            任务 的 第 T_队列后继 项 为 0。
        否则：
            可变 游标 为 队 的 公平队头。
            可变 前驱 为 0。
            只要 游标 不等于 0：
                若 任务 的 第 T_虚拟运行 项 小于 游标 的 第 T_虚拟运行 项：
                    跳出。
                完。
                前驱 为 游标。
                游标 为 游标 的 第 T_队列后继 项。
            完。
            若 前驱 等于 0：
                任务 的 第 T_队列后继 项 为 队 的 公平队头。
                队 的 公平队头 为 任务。
            否则：
                任务 的 第 T_队列后继 项 为 前驱 的 第 T_队列后继 项。
                前驱 的 第 T_队列后继 项 为 任务。
            完。
        完。
        队 的 公平任务数 为 队 的 公平任务数 加 1。
    完。
    队 的 任务总数 为 队 的 任务总数 加 1。
    队 的 入队总数 为 队 的 入队总数 加 1。
    队 的 最近入队时刻 为 做 平台_获取高精度时间。
    队 的 负载值 为 队 的 任务总数 乘 100。
    做 自旋锁_释放 用 队 的 锁。
完。

队列_出队 接受 核号：
    若 核号 大于或等于 核数：
        返还 0。
    完。
    队 为 每核队列 的 第 核号 项。
    做 自旋锁_获取 用 队 的 锁。
    可变 选中 为 0。
    若 队 的 实时队头 不等于 0：
        选中 为 队 的 实时队头。
        队 的 实时队头 为 选中 的 第 T_队列后继 项。
        队 的 实时任务数 为 队 的 实时任务数 减 1。
    否则若 队 的 公平队头 不等于 0：
        选中 为 队 的 公平队头。
        队 的 公平队头 为 选中 的 第 T_队列后继 项。
        队 的 公平任务数 为 队 的 公平任务数 减 1。
    完。
    若 选中 不等于 0：
        队 的 任务总数 为 队 的 任务总数 减 1。
        队 的 出队总数 为 队 的 出队总数 加 1。
        队 的 最近出队时刻 为 做 平台_获取高精度时间。
        队 的 负载值 为 队 的 任务总数 乘 100。
        选中 的 第 T_队列后继 项 为 0。
    完。
    做 自旋锁_释放 用 队 的 锁。
    返还 选中。
完。

队列_迁移任务 接受 源核、目标核：
    若 源核 大于或等于 核数 或者 目标核 大于或等于 核数：
        返还 0。
    完。
    源队 为 每核队列 的 第 源核 项。
    做 自旋锁_获取 用 源队 的 锁。
    若 源队 的 公平队头 等于 0：
        做 自旋锁_释放 用 源队 的 锁。
        返还 0。
    完。
    迁移任务 为 源队 的 公平队头。
    源队 的 公平队头 为 迁移任务 的 第 T_队列后继 项。
    源队 的 公平任务数 为 源队 的 公平任务数 减 1。
    源队 的 任务总数 为 源队 的 任务总数 减 1。
    源队 的 迁出数 为 源队 的 迁出数 加 1。
    做 自旋锁_释放 用 源队 的 锁。
    迁移任务 的 第 T_队列后继 项 为 0。
    做 队列_入队 用 目标核、迁移任务。
    每核队列 的 第 目标核 项 的 迁入数 为 每核队列 的 第 目标核 项 的 迁入数 加 1。
    返还 迁移任务。
完。

队列_获取负载 接受 核号：
    若 核号 大于或等于 核数：
        返还 0。
    完。
    返还 每核队列 的 第 核号 项 的 负载值。
完。

队列_获取任务数 接受 核号：
    若 核号 大于或等于 核数：
        返还 0。
    完。
    返还 每核队列 的 第 核号 项 的 任务总数。
完。

运行队列_完整统计 接受 零参：
    可变 总任务 为 0。
    可变 总入队 为 0。
    可变 总出队 为 0。
    可变 总迁移 为 0。
    可变 序号 为 0。
    只要 序号 小于 核数：
        总任务 为 总任务 加 每核队列 的 第 序号 项 的 任务总数。
        总入队 为 总入队 加 每核队列 的 第 序号 项 的 入队总数。
        总出队 为 总出队 加 每核队列 的 第 序号 项 的 出队总数。
        总迁移 为 总迁移 加 每核队列 的 第 序号 项 的 迁入数。
        序号 为 序号 加 1。
    完。
    返还 一组：核数、总任务、总入队、总出队、总迁移。
完。

可变 AI_运行队列完_负载窗口 为 一组：（空）。

AI_运行队列完_负载预测 接受 当前负载：
    做 追加 用 AI_运行队列完_负载窗口、当前负载。
    若 做 计数 用 AI_运行队列完_负载窗口 大于 20：
        做 列表_移除 用 AI_运行队列完_负载窗口、0。
    完。
    若 做 计数 用 AI_运行队列完_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_运行队列完_负载窗口。
    趋势 为 AI_运行队列完_负载窗口 的 第 n 减 1 项 减 AI_运行队列完_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_运行队列完_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
