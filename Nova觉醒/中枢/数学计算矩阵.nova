《数学计算矩阵》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·整数转字符串》。

引用《.·线代绝对值》。

引用《.·张量数学引擎》。

矩阵求逆 接受 A：
    n 为 A 的 第 0 项。
    可变 aug 为 一组：n、2 乘 n。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 n：
            做 追加 用 aug、A 的 第 2 加 i 乘 n 加 j 项。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 n：
            若 i 等于 j：
                做 追加 用 aug、线代精度。
            否则：
                做 追加 用 aug、0。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 col 为 0。
    只要 col 小于 n：
        若 aug 的 第 2 加 col 乘 2 乘 n 加 col 项 等于 0：
            返还 空。
        完。
        主元 为 aug 的 第 2 加 col 乘 2 乘 n 加 col 项。
        可变 j2 为 0。
        只要 j2 小于 2 乘 n：
            aug 的 第 2 加 col 乘 2 乘 n 加 j2 项 为 aug 的 第 2 加 col 乘 2 乘 n 加 j2 项 乘 线代精度 除以 主元。
            j2 为 j2 加 1。
        完。
        i 为 0。
        只要 i 小于 n：
            若 i 不等于 col：
                因子 为 aug 的 第 2 加 i 乘 2 乘 n 加 col 项。
                j2 为 0。
                只要 j2 小于 2 乘 n：
                    aug 的 第 2 加 i 乘 2 乘 n 加 j2 项 为 aug 的 第 2 加 i 乘 2 乘 n 加 j2 项 减 因子 乘 aug 的 第 2 加 col 乘 2 乘 n 加 j2 项 除以 线代精度。
                    j2 为 j2 加 1。
                完。
            完。
            i 为 i 加 1。
        完。
        col 为 col 加 1。
    完。
    可变 逆 为 一组：n、n。
    i 为 0。
    只要 i 小于 n：
        可变 j3 为 0。
        只要 j3 小于 n：
            做 追加 用 逆、aug 的 第 2 加 i 乘 2 乘 n 加 n 加 j3 项。
            j3 为 j3 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 逆。
完。

最大特征值 接受 A、迭代次数：
    n 为 A 的 第 0 项。
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 v、线代精度。
        i 为 i 加 1。
    完。
    可变 特征值 为 0。
    可变 iter 为 0。
    只要 iter 小于 迭代次数：
        可变 w 为 一组：（空）。
        i 为 0。
        只要 i 小于 n：
            可变 和 为 0。
            可变 j 为 0。
            只要 j 小于 n：
                和 为 和 加 A 的 第 2 加 i 乘 n 加 j 项 乘 v 的 第 j 项 除以 线代精度。
                j 为 j 加 1。
            完。
            做 追加 用 w、和。
            i 为 i 加 1。
        完。
        可变 最大 为 做 线代_绝对值 用 w 的 第 0 项。
        i 为 1。
        只要 i 小于 n：
            av 为 做 线代_绝对值 用 w 的 第 i 项。
            若 av 大于 最大：
                最大 为 av。
            完。
            i 为 i 加 1。
        完。
        若 最大 等于 0：
            最大 为 1。
        完。
        特征值 为 最大。
        i 为 0。
        只要 i 小于 n：
            v 的 第 i 项 为 w 的 第 i 项 乘 线代精度 除以 最大。
            i 为 i 加 1。
        完。
        iter 为 iter 加 1。
    完。
    返还 特征值。
完。

正交化 接受 向量组：
    n 为 做 计数 用 向量组。
    维度 为 做 计数 用 向量组 的 第 0 项。
    可变 正交 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 u 为 一组：（空）。
        可变 d 为 0。
        只要 d 小于 维度：
            做 追加 用 u、向量组 的 第 i 项 的 第 d 项。
            d 为 d 加 1。
        完。
        可变 j 为 0。
        只要 j 小于 做 计数 用 正交：
            可变 投影分子 为 0。
            可变 投影分母 为 0。
            d 为 0。
            只要 d 小于 维度：
                投影分子 为 投影分子 加 u 的 第 d 项 乘 正交 的 第 j 项 的 第 d 项。
                投影分母 为 投影分母 加 正交 的 第 j 项 的 第 d 项 乘 正交 的 第 j 项 的 第 d 项。
                d 为 d 加 1。
            完。
            若 投影分母 不等于 0：
                投影 为 投影分子 乘 线代精度 除以 投影分母。
                d 为 0。
                只要 d 小于 维度：
                    u 的 第 d 项 为 u 的 第 d 项 减 投影 乘 正交 的 第 j 项 的 第 d 项 除以 线代精度。
                    d 为 d 加 1。
                完。
            完。
            j 为 j 加 1。
        完。
        可变 范数平方 为 0。
        d 为 0。
        只要 d 小于 维度：
            范数平方 为 范数平方 加 u 的 第 d 项 乘 u 的 第 d 项。
            d 为 d 加 1。
        完。
        范数 为 做 平方根 用 范数平方。
        若 范数 大于 0：
            d 为 0。
            只要 d 小于 维度：
                u 的 第 d 项 为 u 的 第 d 项 乘 线代精度 除以 范数。
                d 为 d 加 1。
            完。
            做 追加 用 正交、u。
        完。
        i 为 i 加 1。
    完。
    返还 正交。
完。

线代精度 为 1000。

AI稳定窗口 为 16。

Cholesky分解 接受 A：
    若 A 等于 空 或者 A 的 第 0 项 不等于 A 的 第 1 项：
        返还 空。
    完。
    全局统计 的 Cholesky分解数 为 全局统计 的 Cholesky分解数 加 1。
    n 为 A 的 第 0 项。
    可变 L 为 一组：n、n。
    可变 i 为 0。
    只要 i 小于 n 乘 n：
        做 追加 用 L、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于或等于 i：
            可变 和 为 0。
            可变 k 为 0。
            只要 k 小于 j：
                和 为 和 加 L 的 第 2 加 i 乘 n 加 k 项 乘 L 的 第 2 加 j 乘 n 加 k 项 除以 线代精度。
                k 为 k 加 1。
            完。
            若 i 等于 j：
                差 为 A 的 第 2 加 i 乘 n 加 i 项 减 和。
                若 差 小于或等于 0：
                    返还 空。
                完。
                L 的 第 2 加 i 乘 n 加 j 项 为 做 平方根 用 差 乘 线代精度。
            否则：
                若 L 的 第 2 加 j 乘 n 加 j 项 等于 0：
                    返还 空。
                完。
                L 的 第 2 加 i 乘 n 加 j 项 为 (A 的 第 2 加 i 乘 n 加 j 项 减 和) 乘 线代精度 除以 L 的 第 2 加 j 乘 n 加 j 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 L。
完。

条件数_估算 接受 A：
    若 A 等于 空 或者 A 的 第 0 项 不等于 A 的 第 1 项：
        返还 0。
    完。
    全局统计 的 条件数估算数 为 全局统计 的 条件数估算数 加 1。
    范数A 为 做 矩阵_无穷范数_辅助 用 A。
    逆A 为 做 矩阵求逆 用 A。
    若 逆A 等于 空：
        返还 999999。
    完。
    范数逆 为 做 矩阵_无穷范数_辅助 用 逆A。
    若 线代精度 等于 0：
        返还 0。
    完。
    返还 范数A 乘 范数逆 除以 线代精度。
完。

矩阵_无穷范数_辅助 接受 m：
    若 m 等于 空：
        返还 0。
    完。
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 行：
        可变 行和 为 0。
        可变 j 为 0。
        只要 j 小于 列：
            可变 v 为 m 的 第 2 加 i 乘 列 加 j 项。
            若 v 小于 0：
                v 为 0 减 v。
            完。
            行和 为 行和 加 v。
            j 为 j 加 1。
        完。
        若 行和 大于 最大：
            最大 为 行和。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

矩阵_是否病态 接受 A：
    条件 为 做 条件数_估算 用 A。
    若 条件 大于 10000：
        返还 真。
    完。
    返还 假。
完。

矩阵_安全求逆 接受 A：
    若 A 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 空。
    完。
    全局统计 的 求逆次数 为 全局统计 的 求逆次数 加 1。
    若 做 矩阵_是否病态 用 A 等于 真：
        全局统计 的 病态矩阵数 为 全局统计 的 病态矩阵数 加 1。
    完。
    做 AI_记录数值稳定性 用 A。
    返还 做 矩阵求逆 用 A。
完。

矩阵_安全特征值 接受 A、迭代：
    若 A 等于 空：
        返还 0。
    完。
    全局统计 的 特征值计算数 为 全局统计 的 特征值计算数 加 1。
    返还 做 最大特征值 用 A、迭代。
完。

矩阵_安全正交化 接受 向量组：
    若 向量组 等于 空：
        返还 空。
    完。
    全局统计 的 正交化次数 为 全局统计 的 正交化次数 加 1。
    返还 做 正交化 用 向量组。
完。

批量_矩阵求逆 接受 矩阵列表：
    若 矩阵列表 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 批量操作数 为 全局统计 的 批量操作数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 矩阵列表：
        做 追加 用 结果、做 矩阵求逆 用 矩阵列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

批量_特征值 接受 矩阵列表、迭代：
    若 矩阵列表 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 批量操作数 为 全局统计 的 批量操作数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 矩阵列表：
        做 追加 用 结果、做 最大特征值 用 矩阵列表 的 第 i 项、迭代。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 AI稳定历史 为 一组：（空）。

可变 AI稳定位置 为 0。

AI_记录数值稳定性 接受 A：
    若 A 等于 空：
        返还。
    完。
    条件 为 做 条件数_估算 用 A。
    若 做 计数 用 AI稳定历史 小于 AI稳定窗口：
        做 追加 用 AI稳定历史、条件。
    否则：
        做 设置 用 AI稳定历史、AI稳定位置、条件。
    完。
    AI稳定位置 为 (AI稳定位置 加 1) 取余于 AI稳定窗口。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
完。

AI_预测数值稳定性 接受 零参：
    若 做 计数 用 AI稳定历史 等于 0：
        返还 「无数据」。
    完。
    可变 总条件 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI稳定历史：
        总条件 为 总条件 加 AI稳定历史 的 第 i 项。
        i 为 i 加 1。
    完。
    平均条件 为 总条件 除以 做 计数 用 AI稳定历史。
    若 平均条件 大于 10000：
        全局统计 的 AI预测命中 为 全局统计 的 AI预测命中 加 1。
        返还 「严重病态-建议正则化」。
    完。
    若 平均条件 大于 1000：
        返还 「轻度病态」。
    完。
    返还 「数值稳定」。
完。

AI_建议正则化强度 接受 零参：
    稳定性 为 做 AI_预测数值稳定性。
    若 稳定性 等于 「严重病态-建议正则化」：
        返还 100。
    完。
    若 稳定性 等于 「轻度病态」：
        返还 10。
    完。
    返还 0。
完。

求逆统计 是 一种 事物，含 求逆次数、特征值计算数、正交化次数、Cholesky分解数、条件数估算数、病态矩阵数、奇异矩阵数、批量操作数、AI预测次数、AI预测命中、最大维度、总运算量、正则化建议数、精度警告数、错误次数。

可变 全局统计 为 新建 求逆统计。

可变 求逆模块已初始化 为 假。

求逆模块_初始化 接受 零参：
    若 求逆模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI稳定历史 为 一组：（空）。
    AI稳定位置 为 0。
    求逆模块已初始化 为 真。
    做 打印 用 「[矩阵求逆] 初始化完成」。
    返还 0。
完。

求逆模块_销毁 接受 零参：
    若 求逆模块已初始化 等于 假：
        返还 0。
    完。
    AI稳定历史 为 一组：（空）。
    求逆模块已初始化 为 假。
    做 打印 用 「[矩阵求逆] 已销毁」。
    返还 0。
完。

求逆模块_转储 接受 零参：
    做 打印 用 「[矩阵求逆] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  求逆次数: 」、做 整数转字符串 用 全局统计 的 求逆次数。
    做 打印 用 做 字符串拼接 用 「  特征值计算: 」、做 整数转字符串 用 全局统计 的 特征值计算数。
    做 打印 用 做 字符串拼接 用 「  病态矩阵: 」、做 整数转字符串 用 全局统计 的 病态矩阵数。
    做 打印 用 做 字符串拼接 用 「  AI稳定性: 」、做 AI_预测数值稳定性。
    返还 0。
完。

求逆模块_打印统计 接受 零参：
    做 打印 用 「[矩阵求逆] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  求逆次数: 」、做 整数转字符串 用 全局统计 的 求逆次数。
    做 打印 用 做 字符串拼接 用 「  特征值: 」、做 整数转字符串 用 全局统计 的 特征值计算数。
    做 打印 用 做 字符串拼接 用 「  正交化: 」、做 整数转字符串 用 全局统计 的 正交化次数。
    做 打印 用 做 字符串拼接 用 「  Cholesky: 」、做 整数转字符串 用 全局统计 的 Cholesky分解数。
    做 打印 用 做 字符串拼接 用 「  条件数估算: 」、做 整数转字符串 用 全局统计 的 条件数估算数。
    做 打印 用 做 字符串拼接 用 「  病态矩阵: 」、做 整数转字符串 用 全局统计 的 病态矩阵数。
    做 打印 用 做 字符串拼接 用 「  奇异矩阵: 」、做 整数转字符串 用 全局统计 的 奇异矩阵数。
    做 打印 用 做 字符串拼接 用 「  批量操作: 」、做 整数转字符串 用 全局统计 的 批量操作数。
    做 打印 用 做 字符串拼接 用 「  AI预测: 」、做 整数转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI命中: 」、做 整数转字符串 用 全局统计 的 AI预测命中。
    做 打印 用 做 字符串拼接 用 「  最大维度: 」、做 整数转字符串 用 全局统计 的 最大维度。
    做 打印 用 做 字符串拼接 用 「  总运算量: 」、做 整数转字符串 用 全局统计 的 总运算量。
    做 打印 用 做 字符串拼接 用 「  正则化建议: 」、做 整数转字符串 用 全局统计 的 正则化建议数。
    做 打印 用 做 字符串拼接 用 「  精度警告: 」、做 整数转字符串 用 全局统计 的 精度警告数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 整数转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

求逆模块_注册到内核 接受 内核上下文：
    若 求逆模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[矩阵求逆] 已注册到内核上下文」。
    返还 0。
完。

求逆模块_查询状态 接受 零参：
    若 求逆模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

求逆模块_健康检查 接受 零参：
    若 求逆模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 求逆次数：
        返还 假。
    完。
    返还 真。
完。

矩阵_逆存在 接受 A：
    若 A 等于 空 或者 A 的 第 0 项 不等于 A 的 第 1 项：
        返还 假。
    完。
    逆 为 做 矩阵求逆 用 A。
    返还 逆 不等于 空。
完。

矩阵_秩估算 接受 A：
    若 A 等于 空：
        返还 0。
    完。
    行 为 A 的 第 0 项。
    列 为 A 的 第 1 项。
    可变 复制 为 一组：行、列。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        做 追加 用 复制、A 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    可变 秩 为 0。
    可变 col 为 0。
    可变 row 为 0。
    只要 col 小于 列 并且 row 小于 行：
        可变 主元行 为 row。
        可变 主元值 为 0。
        可变 v 为 复制 的 第 2 加 row 乘 列 加 col 项。
        若 v 小于 0：
            v 为 0 减 v。
        完。
        主元值 为 v。
        i 为 row 加 1。
        只要 i 小于 行：
            可变 av 为 复制 的 第 2 加 i 乘 列 加 col 项。
            若 av 小于 0：
                av 为 0 减 av。
            完。
            若 av 大于 主元值：
                主元值 为 av。
                主元行 为 i。
            完。
            i 为 i 加 1。
        完。
        若 主元值 等于 0：
            col 为 col 加 1。
            继续。
        完。
        若 主元行 不等于 row：
            可变 j 为 0。
            只要 j 小于 列：
                临时 为 复制 的 第 2 加 row 乘 列 加 j 项。
                复制 的 第 2 加 row 乘 列 加 j 项 为 复制 的 第 2 加 主元行 乘 列 加 j 项。
                复制 的 第 2 加 主元行 乘 列 加 j 项 为 临时。
                j 为 j 加 1。
            完。
        完。
        i 为 row 加 1。
        只要 i 小于 行：
            若 复制 的 第 2 加 row 乘 列 加 col 项 不等于 0：
                因子 为 复制 的 第 2 加 i 乘 列 加 col 项 乘 线代精度 除以 复制 的 第 2 加 row 乘 列 加 col 项。
                可变 j 为 col。
                只要 j 小于 列：
                    复制 的 第 2 加 i 乘 列 加 j 项 为 复制 的 第 2 加 i 乘 列 加 j 项 减 因子 乘 复制 的 第 2 加 row 乘 列 加 j 项 除以 线代精度。
                    j 为 j 加 1。
                完。
            完。
            i 为 i 加 1。
        完。
        秩 为 秩 加 1。
        row 为 row 加 1。
        col 为 col 加 1。
    完。
    返还 秩。
完。

矩阵_Tick 接受 零参：
    若 全局统计 的 求逆次数 大于 0 并且 全局统计 的 求逆次数 取余于 100 等于 0：
        稳定性 为 做 AI_预测数值稳定性。
        若 稳定性 等于 「严重病态-建议正则化」：
            全局统计 的 正则化建议数 为 全局统计 的 正则化建议数 加 1。
        完。
    完。
完。

矩阵_获取求逆次数 接受 零参：
    返还 全局统计 的 求逆次数。
完。

矩阵_获取病态数 接受 零参：
    返还 全局统计 的 病态矩阵数。
完。

矩阵_获取AI稳定性 接受 零参：
    返还 做 AI_预测数值稳定性。
完。

本章 收束。
