《块缓分配器生》

※ NovaOS Nova块缓存分配器 - 生产级实现
※ 用于小对象的高效内存分配，减少内存碎片

引用《.·类型定义》。
引用《.·同步原语》。
引用《..·执行·内核智能》。

引用《.·原子操作》。
引用《.·内存物理》。
引用《.·同步原语》。
引用《.·物理页面分配》。
引用《.·伙伴系统释放》。
引用《.·哈希函数》。

※ Nova块缓存着色偏移（减少缓存冲突）
最大着色偏移 为 256。
缓存行大小 为 64。

※ Nova块缓存标志
Nova块缓存标志_HWCACHE对齐 为 (1 左移 0)
Nova块缓存标志_毒化 为 (1 左移 1)
Nova块缓存标志_红区 为 (1 左移 2)
Nova块缓存标志_追踪 为 (1 左移 3)
Nova块缓存标志_回收 为 (1 左移 4)
Nova块缓存标志_PANIC 为 (1 左移 5)

※ Nova块缓存对象状态
对象状态_空闲 为 0。
对象状态_已分配 为 1。
对象状态_已毒化 为 2。

※ Nova块缓存描述符
Nova块缓存 是 一种 事物：
    链表: 结构体 ：
        前一个。
        下一个。
    },。

    ※ 对象管理
    空闲对象。 ※ 空闲对象数量。
    活跃对象。 ※ 已分配对象数量。

    ※ 空闲对象链表（使用对象本身存储链表指针）
    空闲链表。
    ※ 着色偏移
    着色偏移。

    ※ 关联的页面
    页面。
    ※ 所属缓存
    缓存。
    ※ 虚拟地址
    虚拟地址。
完。

※ Nova块缓存缓存（每种对象大小一个）
Nova块缓存缓存 是 一种 事物：
    名称。

    ※ 对象属性
    对象大小。
    对象对齐。
    标志。

    ※ 每个Nova块缓存的对象数量
    每Nova块缓存对象数。

    ※ Nova块缓存大小（页数）
    Nova块缓存阶数。

    ※ 着色
    着色数。
    下一个着色。

    ※ Nova块缓存链表
    完全链表。 ※ 所有对象已分配。
    部分链表。 ※ 部分对象已分配。
    空闲链表。 ※ 所有对象空闲。

    ※ 锁保护
    锁。

    ※ 每CPU缓存
    每CPU缓存。
    ※ 统计信息
    总Nova块缓存数。
    活跃Nova块缓存数。
    总对象数。
    活跃对象数。

    分配次数。
    释放次数。
    分配失败次数。

    ※ 构造/析构函数
    构造函数: 做 函数 用 对象: *无符号长整数,。
    析构函数: 做 函数 用 对象: *无符号长整数,。

    ※ 链表节点（全局缓存链表）
    全局链表: 结构体 ：
        前一个。
        下一个。
    },。
完。

※ 每CPU缓存（减少锁竞争）
每CPU缓存 是 一种 事物：
    可用对象。
    限制。

    ※ 对象数组
    对象数组。
    ※ 统计
    分配次数。
    释放次数。
完。

※ 全局Nova块缓存分配器
Nova块缓存分配器 是 一种 事物：
    ※ 全局缓存链表
    缓存链表头。
    缓存数量。

    ※ 通用大小缓存（kmalloc使用）
    通用缓存: [13]*Nova块缓存缓存,

    ※ 全局锁
    锁。

    ※ 统计
    总分配字节。
    总释放字节。
完。

变量 全局Nova块缓存分配器: Nova块缓存分配器。

※ 初始化Nova块缓存分配器
初始化Nova块缓存分配器 接受 )：
    做 打印 用 「初始化Nova块缓存分配器...\n」。

    做 初始化自旋锁 用 &全局Nova块缓存分配器.锁。

    ※ 初始化缓存链表
    头 为 &全局Nova块缓存分配器.缓存链表头。
    头.全局链表.前一个 为 头。
    头.全局链表.下一个 为 头。

    ※ 创建通用大小缓存
    大小 为 8。
    对于 i 为 0; i 小于 13; i++ ：
        名称 为 做 格式化字符串 用 「kmalloc-%d」、大小。
        全局Nova块缓存分配器.通用缓存[i] 为 做 创建Nova块缓存缓存 用 名称、大小、0、0、空、空。
        大小 乘以 2。
    完。

    做 打印 用 「Nova块缓存分配器初始化完成\n」。
完。

※ 创建Nova块缓存缓存
创建Nova块缓存缓存 接受 名称、对象大小、对齐：
                  标志: 无符号整数, 构造: 做 函数 用 *无符号长整数,。
                  析构: 做 函数 用 *无符号长整数): *Nova块缓存缓存 ：

    ※ 分配缓存结构
    缓存 为 做 分配内存 用 做 大小为 用 Nova块缓存缓存。
    若 缓存 等于 空：
        返还 空。
    完。

    缓存.名称 为 名称。
    缓存.对象大小 为 对象大小。
    缓存.标志 为 标志。
    缓存.构造函数 为 构造。
    缓存.析构函数 为 析构。

    ※ 计算对齐
    若 对齐 等于 0：
        对齐 为 8
    完。
    若 标志 位与 Nova块缓存标志_HWCACHE对齐) 不等于 0：
        对齐 为 缓存行大小。
    完。
    缓存.对象对齐 为 对齐。

    ※ 对齐对象大小
    缓存.对象大小 为 做 对齐到 用 对象大小、对齐。

    ※ 计算Nova块缓存大小和对象数量
    做 计算Nova块缓存布局 用 缓存。

    ※ 初始化链表
    做 初始化Nova块缓存链表 用 &缓存.完全链表。
    做 初始化Nova块缓存链表 用 &缓存.部分链表。
    做 初始化Nova块缓存链表 用 &缓存.空闲链表。

    做 初始化自旋锁 用 &缓存.锁。

    ※ 初始化每CPU缓存
    对于 i 为 0; i 小于 最大CPU数; i++ ：
        缓存.每CPU缓存[i].限制 为 64。
    完。

    ※ 添加到全局链表
    做 自旋锁加锁 用 &全局Nova块缓存分配器.锁。

    头 为 &全局Nova块缓存分配器.缓存链表头。
    缓存.全局链表.下一个 为 头.全局链表.下一个。
    缓存.全局链表.前一个 为 头。
    头.全局链表.下一个.全局链表.前一个 为 缓存。
    头.全局链表.下一个 为 缓存。

    全局Nova块缓存分配器.缓存数量++。

    做 自旋锁解锁 用 &全局Nova块缓存分配器.锁。

    做 打印 用 「创建Nova块缓存缓存: %s (对象大小=%u、对齐=%u)\n」、名称、缓存.对象大小、对齐。

    返还 缓存。
完。

※ 计算Nova块缓存布局
计算Nova块缓存布局 接受 缓存)：
    ※ 从1页开始尝试
    对于 阶数 为 做 无符号整数 用 0; 阶数 小于 最大阶数; 阶数++ ：
        Nova块缓存大小 为 (做 无符号长整数 用 1 左移 阶数) 乘 页面大小。

        ※ 计算可容纳的对象数量
        对象数 为 Nova块缓存大小 除以 缓存.对象大小。

        若 对象数 不小于 8：
            缓存.Nova块缓存阶数 为 阶数。
            缓存.每Nova块缓存对象数 为 对象数。

            ※ 计算着色数量
            浪费空间 为 Nova块缓存大小 减 (对象数 乘 缓存.对象大小)。
            缓存.着色数 为 浪费空间 除以 缓存行大小。
            若 缓存.着色数 大于 最大着色偏移 除以 缓存行大小：
                缓存.着色数 为 最大着色偏移 除以 缓存行大小。
            完。

            返还。
        完。
    完。

    ※ 默认使用1页
    缓存.Nova块缓存阶数 为 0。
    缓存.每Nova块缓存对象数 为 页面大小 除以 缓存.对象大小。
    缓存.着色数 为 1。
完。

※ 从缓存分配对象
Nova块缓存分配 接受 缓存、标志)：
    若 缓存 等于 空：
        返还 空。
    完。

    ※ 尝试从每CPU缓存分配
    CPU 为 做 获取当前CPU编号。
    每CPU 为 &缓存.每CPU缓存[CPU]。

    若 每CPU.可用对象 大于 0：
        每CPU.可用对象--。
        对象 为 每CPU.对象数组[每CPU.可用对象]。
        每CPU.分配次数++。

        做 原子增加64位 用 &缓存.活跃对象数、1。
        做 原子增加64位 用 &缓存.分配次数、1。

        返还 对象。
    完。

    ※ 每CPU缓存为空，从Nova块缓存分配
    做 自旋锁加锁 用 &缓存.锁。

    ※ 尝试从部分链表分配
    Nova块缓存 为 缓存.部分链表.链表.下一个。
    若 Nova块缓存 等于 &缓存.部分链表：
        ※ 部分链表为空，尝试空闲链表
        Nova块缓存 为 缓存.空闲链表.链表.下一个。

        若 Nova块缓存 等于 &缓存.空闲链表：
            ※ 需要创建新Nova块缓存
            Nova块缓存 为 做 创建新Nova块缓存 用 缓存。
            若 Nova块缓存 等于 空：
                做 自旋锁解锁 用 &缓存.锁。
                做 原子增加64位 用 &缓存.分配失败次数、1。
                返还 空。
            完。
        完。
    完。

    ※ 从Nova块缓存分配对象
    对象 为 做 从Nova块缓存分配对象 用 Nova块缓存。

    ※ 更新Nova块缓存状态
    若 Nova块缓存.空闲对象 等于 0：
        ※ Nova块缓存已满，移到完全链表
        做 从Nova块缓存链表移除 用 Nova块缓存。
        做 添加到Nova块缓存链表 用 &缓存.完全链表、Nova块缓存。
    否则：
        ※ 从空闲变为部分
        做 从Nova块缓存链表移除 用 Nova块缓存。
        做 添加到Nova块缓存链表 用 &缓存.部分链表、Nova块缓存。
    完。

    做 自旋锁解锁 用 &缓存.锁。

    做 原子增加64位 用 &缓存.活跃对象数、1。
    做 原子增加64位 用 &缓存.分配次数、1。

    返还 对象。
完。

※ 释放对象到缓存
Nova块缓存释放 接受 缓存、对象)：
    若 缓存 等于 空 或 对象 等于 空：
        返还。
    完。

    ※ 尝试放入每CPU缓存
    CPU 为 做 获取当前CPU编号。
    每CPU 为 &缓存.每CPU缓存[CPU]。

    若 每CPU.可用对象 小于 每CPU.限制：
        每CPU.对象数组[每CPU.可用对象] 为 对象。
        每CPU.可用对象++。
        每CPU.释放次数++。

        做 原子减少64位 用 &缓存.活跃对象数、1。
        做 原子增加64位 用 &缓存.释放次数、1。

        返还。
    完。

    ※ 每CPU缓存已满，释放到Nova块缓存
    做 自旋锁加锁 用 &缓存.锁。

    ※ 查找对象所属的Nova块缓存
    Nova块缓存 为 做 查找对象Nova块缓存 用 缓存、对象。
    若 Nova块缓存 等于 空：
        做 自旋锁解锁 用 &缓存.锁。
        做 打印 用 「警告：释放无效对象 %p\n」、对象。
        返还。
    完。

    ※ 释放对象到Nova块缓存
    做 释放对象到Nova块缓存 用 Nova块缓存、对象。

    ※ 更新Nova块缓存状态
    若 Nova块缓存.活跃对象 等于 0：
        ※ Nova块缓存变为空闲
        做 从Nova块缓存链表移除 用 Nova块缓存。
        做 添加到Nova块缓存链表 用 &缓存.空闲链表、Nova块缓存。

        ※ 如果空闲Nova块缓存太多，销毁一些
        若 缓存.空闲链表.链表.下一个 不等于 &缓存.空闲链表 且：
           缓存.空闲链表.链表.下一个.链表.下一个 不等于 &缓存.空闲链表 ：
            ※ 至少保留一个空闲Nova块缓存
            销毁Nova块缓存 为 缓存.空闲链表.链表.下一个。
            做 从Nova块缓存链表移除 用 销毁Nova块缓存。
            做 销毁Nova块缓存结构 用 销毁Nova块缓存。
        完。
    否则：
        ※ 从完全变为部分
        做 从Nova块缓存链表移除 用 Nova块缓存。
        做 添加到Nova块缓存链表 用 &缓存.部分链表、Nova块缓存。
    完。

    做 自旋锁解锁 用 &缓存.锁。

    做 原子减少64位 用 &缓存.活跃对象数、1。
    做 原子增加64位 用 &缓存.释放次数、1。
完。

※ 创建新Nova块缓存
创建新Nova块缓存 接受 缓存)：
    ※ 分配页面
    页面 为 做 分配页面 用 缓存.Nova块缓存阶数、0。
    若 页面 等于 空：
        返还 空。
    完。

    ※ 分配Nova块缓存描述符
    Nova块缓存 为 做 分配内存 用 做 大小为 用 Nova块缓存。
    若 Nova块缓存 等于 空：
        做 释放页面 用 页面。
        返还 空。
    完。

    ※ 初始化Nova块缓存
    Nova块缓存.页面 为 页面。
    Nova块缓存.缓存 为 缓存。
    Nova块缓存.空闲对象 为 缓存.每Nova块缓存对象数。
    Nova块缓存.活跃对象 为 0。

    ※ 计算着色偏移
    Nova块缓存.着色偏移 为 (缓存.下一个着色 乘 缓存行大小) 取余于 最大着色偏移。
    缓存.下一个着色 为 (缓存.下一个着色 加 1) 取余于 缓存.着色数。

    ※ 映射页面到虚拟地址
    Nova块缓存.虚拟地址 为 做 映射页面到内核 用 页面。

    ※ 初始化空闲对象链表
    做 初始化Nova块缓存对象链表 用 Nova块缓存。

    ※ 调用构造函数
    若 缓存.构造函数 不等于 空：
        对于 i 为 做 无符号整数 用 0; i 小于 缓存.每Nova块缓存对象数; i++ ：
            对象地址 为 Nova块缓存.虚拟地址 加 Nova块缓存.着色偏移 加 (i 乘 缓存.对象大小)。
            缓存.做 构造函数 用 对象地址。
        完。
    完。

    ※ 添加到空闲链表
    做 添加到Nova块缓存链表 用 &缓存.空闲链表、Nova块缓存。

    缓存.总Nova块缓存数++。
    缓存.活跃Nova块缓存数++。
    缓存.总对象数 累加 缓存.每Nova块缓存对象数。

    返还 Nova块缓存。
完。

※ 初始化Nova块缓存对象链表
初始化Nova块缓存对象链表 接受 Nova块缓存)：
    缓存 为 Nova块缓存.缓存。

    对于 i 为 做 无符号整数 用 0; i 小于 缓存.每Nova块缓存对象数; i++ ：
        对象地址 为 Nova块缓存.虚拟地址 加 Nova块缓存.着色偏移 加 (i 乘 缓存.对象大小)。
        对象 为 (*无符号长整数)(对象地址)。

        若 i 小于 缓存.每Nova块缓存对象数 减 1：
            ※ 指向下一个对象
            下一个地址 为 对象地址 加 缓存.对象大小。
            *对象 = 下一个地址。
        否则：
            ※ 最后一个对象
            *对象 = 0。
        完。
    完。

    ※ 空闲链表头指向第一个对象
    Nova块缓存.空闲链表 为 (*无符号长整数)(Nova块缓存.虚拟地址 加 Nova块缓存.着色偏移)。
完。

※ 从Nova块缓存分配对象
从Nova块缓存分配对象 接受 Nova块缓存)：
    若 Nova块缓存.空闲对象 等于 0：
        返还 空。
    完。

    对象 为 Nova块缓存.空闲链表。
    Nova块缓存.空闲链表 为 (*无符号长整数)(*对象)。

    Nova块缓存.空闲对象--。
    Nova块缓存.活跃对象++。

    返还 对象。
完。

※ 释放对象到Nova块缓存
释放对象到Nova块缓存 接受 Nova块缓存、对象)：
    *对象 = (无符号长整数)(Nova块缓存.空闲链表)。
    Nova块缓存.空闲链表 为 对象。

    Nova块缓存.空闲对象++。
    Nova块缓存.活跃对象--。
完。

※ kmalloc - 通用内存分配
kmalloc 接受 大小、标志)：
    若 大小 等于 0：
        返还 空。
    完。

    ※ 查找合适的缓存
    索引 为 0。
    检查大小 为 8。

    只要 检查大小 小于 大小 且 索引 小于 12：
        检查大小 乘以 2。
        索引++。
    完。

    若 索引 不小于 13：
        ※ 太大，直接分配页面
        阶数 为 0。
        只要 做 无符号长整数 用 1 左移 (阶数 加 页面位移)) 小于 大小：
            阶数++。
        完。

        页面 为 做 分配页面 用 阶数、标志。
        若 页面 等于 空：
            返还 空。
        完。

        做 原子增加64位 用 &全局Nova块缓存分配器.总分配字节、(做 无符号长整数 用 1 左移 (阶数 加 页面位移))。
        返还 做 映射页面到内核 用 页面。
    完。

    缓存 为 全局Nova块缓存分配器.通用缓存[索引]。
    对象 为 做 Nova块缓存分配 用 缓存、标志。

    若 对象 不等于 空：
        做 原子增加64位 用 &全局Nova块缓存分配器.总分配字节、缓存.对象大小。
    完。

    返还 对象。
完。

※ kfree - 释放内存
kfree 接受 对象)：
    若 对象 等于 空：
        返还。
    完。

    ※ 查找对象所属的缓存
    缓存 为 做 查找对象缓存 用 对象。
    若 缓存 不等于 空：
        做 原子增加64位 用 &全局Nova块缓存分配器.总释放字节、缓存.对象大小。
        做 Nova块缓存释放 用 缓存、对象。
    完。
完。

※ 辅助函数
初始化Nova块缓存链表 接受 头)：
    头.链表.前一个 为 头。
    头.链表.下一个 为 头。
完。

添加到Nova块缓存链表 接受 头、Nova块缓存)：
    Nova块缓存.链表.下一个 为 头.链表.下一个。
    Nova块缓存.链表.前一个 为 头。
    头.链表.下一个.链表.前一个 为 Nova块缓存。
    头.链表.下一个 为 Nova块缓存。
完。

从Nova块缓存链表移除 接受 Nova块缓存)：
    Nova块缓存.链表.前一个.链表.下一个 为 Nova块缓存.链表.下一个。
    Nova块缓存.链表.下一个.链表.前一个 为 Nova块缓存.链表.前一个。
完。

对齐到 接受 值、对齐)：
    返还 (值 加 对齐 减 1) 位与 ~(对齐 减 1)。
完。

外部函数 做 映射页面到内核 用 页面: *页面: *无符号长整数。
外部函数 做 查找对象Nova块缓存 用 缓存: *Nova块缓存缓存、对象: *无符号长整数: *Nova块缓存。
外部函数 做 查找对象缓存 用 对象: *无符号长整数: *Nova块缓存缓存。
外部函数 做 销毁Nova块缓存结构 用 Nova块缓存: *Nova块缓存。
外部函数 做 分配内存 用 大小: 无符号整数: *无符号长整数。
外部函数 做 格式化字符串 用 格式: 字符串、参数: ...: 字符串。

可变 AI_块缓分配器_运行指标 为 0。
可变 AI_块缓分配器_异常指标 为 0。

AI_块缓分配器_自诊断 接受 )：
    AI_块缓分配器_运行指标 为 0。
    返还 AI_块缓分配器_运行指标。
完。

AI_块缓分配器_优化建议 接受 )：
    指标 为 做 AI_块缓分配器_自诊断。
    返还 「块缓分配器生状态正常」。
完。

本章 收束。
