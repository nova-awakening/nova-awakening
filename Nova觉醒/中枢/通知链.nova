《通知链》

引用《.·同步原语》。

引用《.·日志中枢》。

引用《.·类型定义》。

引用《.·错误码》。

引用《.·字符串替换》。

成功 为 0。

最大链数 为 32。

最大回调数 为 256。

默认优先级 为 0。

最高优先级 为 1000。

最低优先级 为 0 减 1000。

回滚标志 为 0x80000000。

AI预测窗口 为 16。

NOTIFY_DONE 为 0x0000。

NOTIFY_OK 为 0x0001。

NOTIFY_STOP 为 0x8000。

NOTIFY_BAD 为 0x8002。

NETDEV_UP 为 0x0001。

NETDEV_DOWN 为 0x0002。

NETDEV_CHANGE 为 0x0004。

CPU_ONLINE 为 0x0010。

CPU_DEAD 为 0x0020。

MEM_GOING_OFFLINE 为 0x0040。

SYS_RESTART 为 0x0100。

SYS_HALT 为 0x0200。

SYS_POWER_OFF 为 0x0400。

AI_EVOLVE 为 0x1000。

AI_CHECKPOINT 为 0x2000。

AI_ROLLBACK 为 0x4000。

链类型_原子 为 0。

链类型_阻塞 为 1。

链类型_裸 为 2。

通知块 是 一种 事物，含 回调、优先级、数据、事件过滤、名称、调用次数。

通知链 是 一种 事物，含 名称、链表、锁、类型、调用次数、回调总数、最后事件、最后事件时间。

通知链统计 是 一种 事物，含 链创建数、注册次数、注销次数、调用次数、回调执行次数、STOP次数、BAD次数、回滚次数、回滚回调数、过滤跳过数、AI预测次数、AI预测命中数、重复注册拒绝数、空回调跳过数、超限拒绝数、错误次数。

可变 已初始化 为 假。

可变 全局统计 为 新建 通知链统计。

可变 当前时间戳 为 0。

可变 AI事件历史 为 一组：（空）。

可变 AI事件位置 为 0。

可变 重启_通知链 为 空。

可变 恐慌_通知链 为 空。

可变 网络设备_通知链 为 空。

可变 CPU_通知链 为 空。

可变 内存_通知链 为 空。

可变 电源_通知链 为 空。

可变 AI_通知链 为 空。

通知链_全局初始化 接受 零参：
    若 已初始化 等于 真：
        返还 错误_已存在。
    完。
    重启_通知链 为 做 通知链_创建 用 「重启」、链类型_阻塞。
    恐慌_通知链 为 做 通知链_创建 用 「恐慌」、链类型_原子。
    网络设备_通知链 为 做 通知链_创建 用 「网络设备」、链类型_阻塞。
    CPU_通知链 为 做 通知链_创建 用 「CPU热插拔」、链类型_阻塞。
    内存_通知链 为 做 通知链_创建 用 「内存」、链类型_阻塞。
    电源_通知链 为 做 通知链_创建 用 「电源」、链类型_阻塞。
    AI_通知链 为 做 通知链_创建 用 「AI演进」、链类型_阻塞。
    AI事件历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI预测窗口：
        做 追加 用 AI事件历史、0。
        i 为 i 加 1。
    完。
    AI事件位置 为 0。
    已初始化 为 真。
    做 打印 用 「[通知链] 全局初始化完成 (重启/恐慌/网络/CPU/内存/电源/AI)」。
    返还 成功。
完。

通知链_全局销毁 接受 零参：
    若 已初始化 等于 假：
        返还 成功。
    完。
    重启_通知链 为 空。
    恐慌_通知链 为 空。
    网络设备_通知链 为 空。
    CPU_通知链 为 空。
    内存_通知链 为 空。
    电源_通知链 为 空。
    AI_通知链 为 空。
    AI事件历史 为 一组：（空）。
    已初始化 为 假。
    做 打印 用 「[通知链] 已销毁」。
    返还 成功。
完。

通知链_创建 接受 名称、类型：
    若 名称 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 空。
    完。
    可变 nc 为 新建 通知链。
    nc 的 名称 为 名称。
    nc 的 链表 为 一组：（空）。
    nc 的 锁 为 0。
    nc 的 类型 为 类型。
    nc 的 调用次数 为 0。
    nc 的 回调总数 为 0。
    nc 的 最后事件 为 0。
    nc 的 最后事件时间 为 0。
    全局统计 的 链创建数 为 全局统计 的 链创建数 加 1。
    返还 nc。
完。

通知链_注册 接受 nc、回调、优先级：
    若 nc 等于 空：
        返还 错误_无效参数。
    完。
    若 回调 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    若 nc 的 回调总数 大于或等于 最大回调数：
        全局统计 的 超限拒绝数 为 全局统计 的 超限拒绝数 加 1。
        返还 错误_内存不足。
    完。
    可变 k 为 0。
    只要 k 小于 做 计数 用 nc 的 链表：
        若 nc 的 链表 的 第 k 项 的 回调 等于 回调：
            全局统计 的 重复注册拒绝数 为 全局统计 的 重复注册拒绝数 加 1。
            返还 错误_已存在。
        完。
        k 为 k 加 1。
    完。
    可变 nb 为 新建 通知块。
    nb 的 回调 为 回调。
    nb 的 优先级 为 优先级。
    nb 的 数据 为 空。
    nb 的 事件过滤 为 0。
    nb 的 名称 为 「」。
    nb 的 调用次数 为 0。
    可变 插入位置 为 做 计数 用 nc 的 链表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 nc 的 链表：
        若 nc 的 链表 的 第 i 项 的 优先级 小于 优先级：
            插入位置 为 i。
            止。
        完。
        i 为 i 加 1。
    完。
    做 列表_插入 用 nc 的 链表、插入位置、nb。
    nc 的 回调总数 为 nc 的 回调总数 加 1。
    全局统计 的 注册次数 为 全局统计 的 注册次数 加 1。
    返还 成功。
完。

通知链_注册_默认优先级 接受 nc、回调：
    返还 做 通知链_注册 用 nc、回调、默认优先级。
完。

通知链_注册_带过滤 接受 nc、回调、优先级、事件掩码：
    结果 为 做 通知链_注册 用 nc、回调、优先级。
    若 结果 不等于 成功：
        返还 结果。
    完。
    可变 i 为 做 计数 用 nc 的 链表 减 1。
    只要 i 大于或等于 0：
        若 nc 的 链表 的 第 i 项 的 回调 等于 回调：
            nc 的 链表 的 第 i 项 的 事件过滤 为 事件掩码。
            返还 成功。
        完。
        i 为 i 减 1。
    完。
    返还 成功。
完。

通知链_注销 接受 nc、nb：
    若 nc 等于 空 或者 nb 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 nc 的 链表：
        若 nc 的 链表 的 第 i 项 等于 nb：
            做 删除 用 nc 的 链表、i。
            nc 的 回调总数 为 nc 的 回调总数 减 1。
            全局统计 的 注销次数 为 全局统计 的 注销次数 加 1。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    返还 错误_未找到。
完。

通知链_注销_按回调 接受 nc、回调：
    若 nc 等于 空 或者 回调 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 nc 的 链表：
        若 nc 的 链表 的 第 i 项 的 回调 等于 回调：
            做 删除 用 nc 的 链表、i。
            nc 的 回调总数 为 nc 的 回调总数 减 1。
            全局统计 的 注销次数 为 全局统计 的 注销次数 加 1。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    返还 错误_未找到。
完。

通知链_清空 接受 nc：
    若 nc 等于 空：
        返还 错误_无效参数。
    完。
    nc 的 链表 为 一组：（空）。
    nc 的 回调总数 为 0。
    返还 成功。
完。

通知链_调用 接受 nc、事件、数据：
    若 nc 等于 空：
        返还 NOTIFY_DONE。
    完。
    nc 的 调用次数 为 nc 的 调用次数 加 1。
    nc 的 最后事件 为 事件。
    nc 的 最后事件时间 为 当前时间戳。
    全局统计 的 调用次数 为 全局统计 的 调用次数 加 1。
    做 AI_记录事件 用 事件。
    可变 结果 为 NOTIFY_DONE。
    可变 i 为 0。
    只要 i 小于 做 计数 用 nc 的 链表：
        nb 为 nc 的 链表 的 第 i 项。
        若 nb 的 回调 等于 空：
            全局统计 的 空回调跳过数 为 全局统计 的 空回调跳过数 加 1。
            i 为 i 加 1。
            继续。
        完。
        若 nb 的 事件过滤 不等于 0 并且 nb 的 事件过滤 位与 事件 等于 0：
            全局统计 的 过滤跳过数 为 全局统计 的 过滤跳过数 加 1。
            i 为 i 加 1。
            继续。
        完。
        结果 为 做 nb 的 回调 用 nb、事件、数据。
        nb 的 调用次数 为 nb 的 调用次数 加 1。
        全局统计 的 回调执行次数 为 全局统计 的 回调执行次数 加 1。
        若 结果 位与 NOTIFY_STOP 不等于 0：
            全局统计 的 STOP次数 为 全局统计 的 STOP次数 加 1。
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

通知链_调用_可回滚 接受 nc、事件、数据：
    若 nc 等于 空：
        返还 NOTIFY_DONE。
    完。
    nc 的 调用次数 为 nc 的 调用次数 加 1。
    全局统计 的 调用次数 为 全局统计 的 调用次数 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 nc 的 链表：
        nb 为 nc 的 链表 的 第 i 项。
        若 nb 的 回调 等于 空：
            i 为 i 加 1。
            继续。
        完。
        若 nb 的 事件过滤 不等于 0 并且 nb 的 事件过滤 位与 事件 等于 0：
            i 为 i 加 1。
            继续。
        完。
        结果 为 做 nb 的 回调 用 nb、事件、数据。
        nb 的 调用次数 为 nb 的 调用次数 加 1。
        全局统计 的 回调执行次数 为 全局统计 的 回调执行次数 加 1。
        若 结果 等于 NOTIFY_BAD：
            全局统计 的 BAD次数 为 全局统计 的 BAD次数 加 1。
            可变 j 为 i 减 1。
            可变 回滚数 为 0。
            只要 j 大于或等于 0：
                若 nc 的 链表 的 第 j 项 的 回调 不等于 空：
                    做 nc 的 链表 的 第 j 项 的 回调 用 nc 的 链表 的 第 j 项、事件 位或 回滚标志、数据。
                    回滚数 为 回滚数 加 1。
                完。
                j 为 j 减 1。
            完。
            全局统计 的 回滚次数 为 全局统计 的 回滚次数 加 1。
            全局统计 的 回滚回调数 为 全局统计 的 回滚回调数 加 回滚数。
            返还 结果。
        完。
        若 结果 位与 NOTIFY_STOP 不等于 0：
            全局统计 的 STOP次数 为 全局统计 的 STOP次数 加 1。
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 NOTIFY_DONE。
完。

列表_插入 接受 列表、位置、值：
    做 追加 用 列表、值。
    可变 i 为 做 计数 用 列表 减 1。
    只要 i 大于 位置：
        临时 为 列表 的 第 i 减 1 项。
        做 设置 用 列表、i 减 1、列表 的 第 i 项。
        做 设置 用 列表、i、临时。
        i 为 i 减 1。
    完。
完。

通知链_长度 接受 nc：
    若 nc 等于 空：
        返还 0。
    完。
    返还 做 计数 用 nc 的 链表。
完。

通知链_调用计数 接受 nc：
    若 nc 等于 空：
        返还 0。
    完。
    返还 nc 的 调用次数。
完。

通知结果名 接受 结果：
    若 结果 等于 NOTIFY_DONE：
        返还 「完成」。
    完。
    若 结果 等于 NOTIFY_OK：
        返还 「已处理」。
    完。
    若 结果 等于 NOTIFY_BAD：
        返还 「拒绝」。
    完。
    若 结果 位与 NOTIFY_STOP 不等于 0：
        返还 「停止」。
    完。
    返还 「未知」。
完。

事件名称 接受 事件：
    若 事件 等于 NETDEV_UP：
        返还 「网络设备上线」。
    完。
    若 事件 等于 NETDEV_DOWN：
        返还 「网络设备下线」。
    完。
    若 事件 等于 CPU_ONLINE：
        返还 「CPU上线」。
    完。
    若 事件 等于 CPU_DEAD：
        返还 「CPU下线」。
    完。
    若 事件 等于 MEM_GOING_OFFLINE：
        返还 「内存离线」。
    完。
    若 事件 等于 SYS_RESTART：
        返还 「系统重启」。
    完。
    若 事件 等于 SYS_HALT：
        返还 「系统停机」。
    完。
    若 事件 等于 SYS_POWER_OFF：
        返还 「系统关机」。
    完。
    若 事件 等于 AI_EVOLVE：
        返还 「AI演进」。
    完。
    若 事件 等于 AI_CHECKPOINT：
        返还 「AI检查点」。
    完。
    若 事件 等于 AI_ROLLBACK：
        返还 「AI回滚」。
    完。
    返还 「未知事件」。
完。

注册_重启通知 接受 回调：
    返还 做 通知链_注册_默认优先级 用 重启_通知链、回调。
完。

注册_恐慌通知 接受 回调：
    返还 做 通知链_注册_默认优先级 用 恐慌_通知链、回调。
完。

注册_网络通知 接受 回调：
    返还 做 通知链_注册_默认优先级 用 网络设备_通知链、回调。
完。

注册_CPU通知 接受 回调：
    返还 做 通知链_注册_默认优先级 用 CPU_通知链、回调。
完。

注册_内存通知 接受 回调：
    返还 做 通知链_注册_默认优先级 用 内存_通知链、回调。
完。

注册_电源通知 接受 回调：
    返还 做 通知链_注册_默认优先级 用 电源_通知链、回调。
完。

注册_AI通知 接受 回调：
    返还 做 通知链_注册_默认优先级 用 AI_通知链、回调。
完。

发送_重启通知 接受 数据：
    返还 做 通知链_调用 用 重启_通知链、SYS_RESTART、数据。
完。

发送_恐慌通知 接受 数据：
    返还 做 通知链_调用 用 恐慌_通知链、SYS_HALT、数据。
完。

发送_CPU上线通知 接受 CPU编号：
    返还 做 通知链_调用 用 CPU_通知链、CPU_ONLINE、CPU编号。
完。

发送_CPU下线通知 接受 CPU编号：
    返还 做 通知链_调用 用 CPU_通知链、CPU_DEAD、CPU编号。
完。

发送_AI演进通知 接受 阶段：
    返还 做 通知链_调用 用 AI_通知链、AI_EVOLVE、阶段。
完。

AI_记录事件 接受 事件：
    做 设置 用 AI事件历史、AI事件位置、事件。
    AI事件位置 为 (AI事件位置 加 1) 取余于 AI预测窗口。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
完。

AI_预测下次事件 接受 零参：
    可变 最频事件 为 0。
    可变 最频次数 为 0。
    可变 i 为 0。
    只要 i 小于 AI预测窗口：
        若 AI事件历史 的 第 i 项 等于 0：
            i 为 i 加 1。
            继续。
        完。
        当前事件 为 AI事件历史 的 第 i 项。
        可变 频次 为 0。
        可变 j 为 0。
        只要 j 小于 AI预测窗口：
            若 AI事件历史 的 第 j 项 等于 当前事件：
                频次 为 频次 加 1。
            完。
            j 为 j 加 1。
        完。
        若 频次 大于 最频次数：
            最频次数 为 频次。
            最频事件 为 当前事件。
        完。
        i 为 i 加 1。
    完。
    若 最频次数 大于 AI预测窗口 除以 3：
        全局统计 的 AI预测命中数 为 全局统计 的 AI预测命中数 加 1。
    完。
    返还 最频事件。
完。

AI_事件相关性 接受 事件A、事件B：
    可变 跟随数 为 0。
    可变 A出现数 为 0。
    可变 i 为 0。
    只要 i 小于 AI预测窗口 减 1：
        若 AI事件历史 的 第 i 项 等于 事件A：
            A出现数 为 A出现数 加 1。
            若 AI事件历史 的 第 (i 加 1) 取余于 AI预测窗口 项 等于 事件B：
                跟随数 为 跟随数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    若 A出现数 等于 0：
        返还 0。
    完。
    返还 跟随数 乘 100 除以 A出现数。
完。

通知链_总注册数 接受 零参：
    可变 总数 为 0。
    若 重启_通知链 不等于 空：
        总数 为 总数 加 做 通知链_长度 用 重启_通知链。
    完。
    若 恐慌_通知链 不等于 空：
        总数 为 总数 加 做 通知链_长度 用 恐慌_通知链。
    完。
    若 网络设备_通知链 不等于 空：
        总数 为 总数 加 做 通知链_长度 用 网络设备_通知链。
    完。
    若 CPU_通知链 不等于 空：
        总数 为 总数 加 做 通知链_长度 用 CPU_通知链。
    完。
    若 内存_通知链 不等于 空：
        总数 为 总数 加 做 通知链_长度 用 内存_通知链。
    完。
    若 电源_通知链 不等于 空：
        总数 为 总数 加 做 通知链_长度 用 电源_通知链。
    完。
    若 AI_通知链 不等于 空：
        总数 为 总数 加 做 通知链_长度 用 AI_通知链。
    完。
    返还 总数。
完。

通知链_转储 接受 nc：
    若 nc 等于 空：
        做 打印 用 「[通知链] 空链」。
        返还 成功。
    完。
    做 打印 用 做 字符串拼接 用 「[通知链] 名称: 」、做 转字符串 用 nc 的 名称。
    做 打印 用 做 字符串拼接 用 「  回调数: 」、做 转字符串 用 nc 的 回调总数。
    做 打印 用 做 字符串拼接 用 「  调用次数: 」、做 转字符串 用 nc 的 调用次数。
    做 打印 用 做 字符串拼接 用 「  最后事件: 」、做 事件名称 用 nc 的 最后事件。
    可变 i 为 0。
    只要 i 小于 做 计数 用 nc 的 链表：
        做 打印 用 做 字符串拼接 用 「  [」、做 转字符串 用 i。
        做 打印 用 做 字符串拼接 用 「    优先级: 」、做 转字符串 用 nc 的 链表 的 第 i 项 的 优先级。
        做 打印 用 做 字符串拼接 用 「    调用次数: 」、做 转字符串 用 nc 的 链表 的 第 i 项 的 调用次数。
        i 为 i 加 1。
    完。
    返还 成功。
完。

通知链_转储全部 接受 零参：
    做 打印 用 「[通知链] ═══ 全部链转储 ═══」。
    做 通知链_转储 用 重启_通知链。
    做 通知链_转储 用 恐慌_通知链。
    做 通知链_转储 用 网络设备_通知链。
    做 通知链_转储 用 CPU_通知链。
    做 通知链_转储 用 内存_通知链。
    做 通知链_转储 用 电源_通知链。
    做 通知链_转储 用 AI_通知链。
    返还 成功。
完。

通知链_Tick 接受 时间戳：
    当前时间戳 为 时间戳。
完。

通知链_打印统计 接受 零参：
    做 打印 用 「[通知链] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  链创建数: 」、做 转字符串 用 全局统计 的 链创建数。
    做 打印 用 做 字符串拼接 用 「  注册次数: 」、做 转字符串 用 全局统计 的 注册次数。
    做 打印 用 做 字符串拼接 用 「  注销次数: 」、做 转字符串 用 全局统计 的 注销次数。
    做 打印 用 做 字符串拼接 用 「  调用次数: 」、做 转字符串 用 全局统计 的 调用次数。
    做 打印 用 做 字符串拼接 用 「  回调执行: 」、做 转字符串 用 全局统计 的 回调执行次数。
    做 打印 用 做 字符串拼接 用 「  STOP次数: 」、做 转字符串 用 全局统计 的 STOP次数。
    做 打印 用 做 字符串拼接 用 「  BAD次数: 」、做 转字符串 用 全局统计 的 BAD次数。
    做 打印 用 做 字符串拼接 用 「  回滚次数: 」、做 转字符串 用 全局统计 的 回滚次数。
    做 打印 用 做 字符串拼接 用 「  回滚回调: 」、做 转字符串 用 全局统计 的 回滚回调数。
    做 打印 用 做 字符串拼接 用 「  过滤跳过: 」、做 转字符串 用 全局统计 的 过滤跳过数。
    做 打印 用 做 字符串拼接 用 「  AI预测: 」、做 转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI命中: 」、做 转字符串 用 全局统计 的 AI预测命中数。
    做 打印 用 做 字符串拼接 用 「  重复注册拒绝: 」、做 转字符串 用 全局统计 的 重复注册拒绝数。
    做 打印 用 做 字符串拼接 用 「  空回调跳过: 」、做 转字符串 用 全局统计 的 空回调跳过数。
    做 打印 用 做 字符串拼接 用 「  超限拒绝: 」、做 转字符串 用 全局统计 的 超限拒绝数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    做 打印 用 做 字符串拼接 用 「  总注册回调: 」、做 转字符串 用 做 通知链_总注册数。
    返还 成功。
完。

通知链_注册到内核 接受 内核上下文：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    做 打印 用 「[通知链] 已注册到内核上下文」。
    返还 成功。
完。

通知链_查询状态 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

通知链_健康检查 接受 零参：
    若 已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 调用次数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
