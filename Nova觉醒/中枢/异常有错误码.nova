《异常有错误码》

引用《.·中断管理系统》。

引用《..·引导·引导加载器》。

引用《..·稳态·串口》。

引用《.·中断请求描述》。

帧_R15 为 0。

帧_R14 为 8。

帧_R13 为 16。

帧_R12 为 24。

帧_R11 为 32。

帧_R10 为 40。

帧_R9 为 48。

帧_R8 为 56。

帧_RBP 为 64。

帧_RDI 为 72。

帧_RSI 为 80。

帧_RDX 为 88。

帧_RCX 为 96。

帧_RBX 为 104。

帧_RAX 为 112。

帧_向量号 为 120。

帧_错误码 为 128。

帧_RIP 为 136。

帧_CS 为 144。

帧_RFLAGS 为 152。

帧_RSP 为 160。

帧_SS 为 168。

帧_总大小 为 176。

异常_除法错误 为 0。

异常_调试 为 1。

异常_NMI 为 2。

异常_断点 为 3。

异常_溢出 为 4。

异常_越界 为 5。

异常_无效操作码 为 6。

异常_设备不可用 为 7。

异常_双重故障 为 8。

异常_无效TSS 为 10。

异常_段不存在 为 11。

异常_栈段故障 为 12。

异常_一般保护 为 13。

异常_页面错误 为 14。

异常_浮点错误 为 16。

异常_对齐检查 为 17。

异常_机器检查 为 18。

异常_SIMD错误 为 19。

异常_虚拟化 为 20。

异常_控制保护 为 21。

中断_最大向量 为 256。

IRQ_起始向量 为 32。

中断统计 是 一种 事物，含 总中断数、异常数、IRQ数、嵌套中断数、最大嵌套深度、panic数、虚假中断数、各向量计数。

可变 全局_中断统计 为 空。

可变 当前嵌套深度 为 0。

可变 中断已禁用 为 假。

中断统计_初始化 接受 零参：
    若 全局_中断统计 等于 空：
        全局_中断统计 为 新建 中断统计。
        全局_中断统计 的 总中断数 为 0。
        全局_中断统计 的 异常数 为 0。
        全局_中断统计 的 IRQ数 为 0。
        全局_中断统计 的 嵌套中断数 为 0。
        全局_中断统计 的 最大嵌套深度 为 0。
        全局_中断统计 的 panic数 为 0。
        全局_中断统计 的 虚假中断数 为 0。
        全局_中断统计 的 各向量计数 为 做 堆_分配 用 中断_最大向量 乘 8。
        可变 i 为 0。
        只要 i 小于 中断_最大向量：
            做 内存_写64 用 全局_中断统计 的 各向量计数 加 i 乘 8、0。
            i 为 i 加 1。
        完。
    完。
完。

异常_有错误码 接受 向量号：
    若 向量号 等于 8：
        返还 真。
    完。
    若 向量号 等于 10：
        返还 真。
    完。
    若 向量号 等于 11：
        返还 真。
    完。
    若 向量号 等于 12：
        返还 真。
    完。
    若 向量号 等于 13：
        返还 真。
    完。
    若 向量号 等于 14：
        返还 真。
    完。
    若 向量号 等于 17：
        返还 真。
    完。
    若 向量号 等于 21：
        返还 真。
    完。
    返还 假。
完。

异常_名称 接受 向量号：
    若 向量号 等于 0：
        返还 「#DE 除法错误」。
    完。
    若 向量号 等于 1：
        返还 「#DB 调试」。
    完。
    若 向量号 等于 2：
        返还 「NMI 不可屏蔽中断」。
    完。
    若 向量号 等于 3：
        返还 「#BP 断点」。
    完。
    若 向量号 等于 4：
        返还 「#OF 溢出」。
    完。
    若 向量号 等于 5：
        返还 「#BR 越界」。
    完。
    若 向量号 等于 6：
        返还 「#UD 无效操作码」。
    完。
    若 向量号 等于 7：
        返还 「#NM 设备不可用」。
    完。
    若 向量号 等于 8：
        返还 「#DF 双重故障」。
    完。
    若 向量号 等于 10：
        返还 「#TS 无效TSS」。
    完。
    若 向量号 等于 11：
        返还 「#NP 段不存在」。
    完。
    若 向量号 等于 12：
        返还 「#SS 栈段故障」。
    完。
    若 向量号 等于 13：
        返还 「#GP 一般保护故障」。
    完。
    若 向量号 等于 14：
        返还 「#PF 页面错误」。
    完。
    若 向量号 等于 16：
        返还 「#MF 浮点错误」。
    完。
    若 向量号 等于 17：
        返还 「#AC 对齐检查」。
    完。
    若 向量号 等于 18：
        返还 「#MC 机器检查」。
    完。
    若 向量号 等于 19：
        返还 「#XM SIMD错误」。
    完。
    若 向量号 等于 20：
        返还 「#VE 虚拟化异常」。
    完。
    若 向量号 等于 21：
        返还 「#CP 控制保护」。
    完。
    返还 「未知异常」。
完。

异常_是致命 接受 向量号：
    若 向量号 等于 异常_双重故障：
        返还 真。
    完。
    若 向量号 等于 异常_机器检查：
        返还 真。
    完。
    返还 假。
完。

异常_可恢复 接受 向量号：
    若 向量号 等于 异常_页面错误：
        返还 真。
    完。
    若 向量号 等于 异常_调试：
        返还 真。
    完。
    若 向量号 等于 异常_断点：
        返还 真。
    完。
    若 向量号 等于 异常_溢出：
        返还 真。
    完。
    返还 假。
完。

中断_通用分发 接受 帧指针：
    做 中断统计_初始化。
    全局_中断统计 的 总中断数 为 全局_中断统计 的 总中断数 加 1。
    向量号 为 做 内存_读64 用 帧指针 加 帧_向量号。
    错误码 为 做 内存_读64 用 帧指针 加 帧_错误码。
    若 向量号 小于 中断_最大向量：
        旧计数 为 做 内存_读64 用 全局_中断统计 的 各向量计数 加 向量号 乘 8。
        做 内存_写64 用 全局_中断统计 的 各向量计数 加 向量号 乘 8、旧计数 加 1。
    完。
    当前嵌套深度 为 当前嵌套深度 加 1。
    若 当前嵌套深度 大于 1：
        全局_中断统计 的 嵌套中断数 为 全局_中断统计 的 嵌套中断数 加 1。
    完。
    若 当前嵌套深度 大于 全局_中断统计 的 最大嵌套深度：
        全局_中断统计 的 最大嵌套深度 为 当前嵌套深度。
    完。
    若 向量号 小于 32：
        全局_中断统计 的 异常数 为 全局_中断统计 的 异常数 加 1。
        做 中断_处理异常 用 向量号、错误码、帧指针。
    否则若 向量号 大于或等于 IRQ_起始向量 并且 向量号 小于 IRQ_起始向量 加 16：
        全局_中断统计 的 IRQ数 为 全局_中断统计 的 IRQ数 加 1。
        做 中断_处理IRQ 用 向量号 减 IRQ_起始向量、帧指针。
    否则若 向量号 等于 255：
        全局_中断统计 的 虚假中断数 为 全局_中断统计 的 虚假中断数 加 1。
    否则：
        做 中断_处理软件 用 向量号、帧指针。
    完。
    当前嵌套深度 为 当前嵌套深度 减 1。
完。

中断_处理异常 接受 向量号、错误码、帧指针：
    若 做 异常_是致命 用 向量号：
        做 中断_panic 用 向量号、错误码、帧指针。
        返还。
    完。
    若 向量号 等于 异常_页面错误：
        故障地址 为 做 读_CR2。
        做 异常_缺页处理 用 故障地址、错误码、帧指针。
        返还。
    完。
    若 向量号 等于 异常_一般保护：
        做 异常_GP处理 用 错误码、帧指针。
        返还。
    完。
    若 向量号 等于 异常_除法错误：
        做 异常_除法处理 用 帧指针。
        返还。
    完。
    若 向量号 等于 异常_无效操作码：
        做 异常_UD处理 用 帧指针。
        返还。
    完。
    若 向量号 等于 异常_断点：
        做 异常_断点处理 用 帧指针。
        返还。
    完。
    若 向量号 等于 异常_调试：
        做 异常_调试处理 用 帧指针。
        返还。
    完。
    若 非 做 异常_可恢复 用 向量号：
        做 中断_panic 用 向量号、错误码、帧指针。
    完。
完。

中断_处理IRQ 接受 irq号、帧指针：
    若 irq号 大于或等于 8：
        做 端口_写字节 用 0xA0、0x20。
    完。
    做 端口_写字节 用 0x20、0x20。
    做 IRQ_分发 用 irq号。
完。

中断_处理软件 接受 向量号、帧指针：
    若 向量号 等于 0x80：
        做 系统调用_入口 用 帧指针。
    否则：
        做 打印 用 「[中断] 软件中断: 向量」 加 向量号。
    完。
完。

中断_转储寄存器 接受 帧指针：
    做 串口_打印 用 「=== 寄存器转储 ===\n」。
    做 串口_打印 用 「RAX=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_RAX。
    做 串口_打印 用 「 RBX=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_RBX。
    做 串口_打印 用 「 RCX=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_RCX。
    做 串口_打印 用 「\nRDX=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_RDX。
    做 串口_打印 用 「 RSI=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_RSI。
    做 串口_打印 用 「 RDI=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_RDI。
    做 串口_打印 用 「\nRBP=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_RBP。
    做 串口_打印 用 「 RSP=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_RSP。
    做 串口_打印 用 「 RIP=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_RIP。
    做 串口_打印 用 「\nR8 =」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_R8。
    做 串口_打印 用 「 R9 =」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_R9。
    做 串口_打印 用 「 R10=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_R10。
    做 串口_打印 用 「\nR11=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_R11。
    做 串口_打印 用 「 R12=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_R12。
    做 串口_打印 用 「 R13=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_R13。
    做 串口_打印 用 「\nR14=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_R14。
    做 串口_打印 用 「 R15=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_R15。
    做 串口_打印 用 「\nCS =」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_CS。
    做 串口_打印 用 「 SS =」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_SS。
    做 串口_打印 用 「 FLG=」。
    做 串口_打印十六进制 用 做 内存_读64 用 帧指针 加 帧_RFLAGS。
    做 串口_打印 用 「\n」。
完。

中断_转储栈 接受 帧指针、深度：
    做 串口_打印 用 「=== 栈转储 ===\n」。
    rsp 为 做 内存_读64 用 帧指针 加 帧_RSP。
    可变 i 为 0。
    只要 i 小于 深度：
        做 串口_打印 用 「[RSP+」。
        做 串口_打印十六进制 用 i 乘 8。
        做 串口_打印 用 「] = 」。
        做 串口_打印十六进制 用 做 内存_读64 用 rsp 加 i 乘 8。
        做 串口_打印 用 「\n」。
        i 为 i 加 1。
    完。
完。

中断_panic 接受 向量号、错误码、帧指针：
    做 中断统计_初始化。
    全局_中断统计 的 panic数 为 全局_中断统计 的 panic数 加 1。
    汇编。
    块：
        「cli」。
    完。
    中断已禁用 为 真。
    做 串口_打印 用 「\n!!! KERNEL PANIC !!!\n」。
    做 串口_打印 用 「异常: 」。
    做 串口_打印 用 做 异常_名称 用 向量号。
    做 串口_打印 用 「\n向量号: 」。
    做 串口_打印十六进制 用 向量号。
    做 串口_打印 用 「  错误码: 」。
    做 串口_打印十六进制 用 错误码。
    做 串口_打印 用 「\n」。
    做 中断_转储寄存器 用 帧指针。
    做 中断_转储栈 用 帧指针、8。
    做 串口_打印 用 「系统已停止。\n」。
    只要 真：
        汇编。
        块：
            「hlt」。
        完。
    完。
完。

中断_存根_无错误码 接受 向量号：
    汇编。
    块：
        「push 0」。
    完。
    汇编。
    块：
        「push」。
    完。
    做 保存所有寄存器。
    汇编。
    块：
        「mov rdi, rsp」。
    完。
    做 中断_通用分发 用 0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

中断_存根_有错误码 接受 向量号：
    汇编。
    块：
        「push」。
    完。
    做 保存所有寄存器。
    汇编。
    块：
        「mov rdi, rsp」。
    完。
    做 中断_通用分发 用 0。
    做 恢复所有寄存器。
    汇编。
    块：
        「add rsp, 16」。
        「iretq」。
    完。
完。

读_CR2 接受 零参：
    可变 值 为 0。
    汇编。
    块：
        「mov rax, cr2」。
    完。
    返还 值。
完。

读_CR3 接受 零参：
    可变 值 为 0。
    汇编。
    块：
        「mov rax, cr3」。
    完。
    返还 值。
完。

中断_打印统计 接受 零参：
    做 中断统计_初始化。
    做 串口_打印 用 「=== 中断统计 ===\n」。
    做 串口_打印 用 「总中断数: 」。
    做 串口_打印十进制 用 全局_中断统计 的 总中断数。
    做 串口_打印 用 「\n」。
    做 串口_打印 用 「异常数: 」。
    做 串口_打印十进制 用 全局_中断统计 的 异常数。
    做 串口_打印 用 「\n」。
    做 串口_打印 用 「IRQ数: 」。
    做 串口_打印十进制 用 全局_中断统计 的 IRQ数。
    做 串口_打印 用 「\n」。
    做 串口_打印 用 「嵌套中断: 」。
    做 串口_打印十进制 用 全局_中断统计 的 嵌套中断数。
    做 串口_打印 用 「\n」。
    做 串口_打印 用 「最大嵌套: 」。
    做 串口_打印十进制 用 全局_中断统计 的 最大嵌套深度。
    做 串口_打印 用 「\n」。
    做 串口_打印 用 「panic数: 」。
    做 串口_打印十进制 用 全局_中断统计 的 panic数。
    做 串口_打印 用 「\n」。
    做 串口_打印 用 「虚假中断: 」。
    做 串口_打印十进制 用 全局_中断统计 的 虚假中断数。
    做 串口_打印 用 「\n」。
完。

可变 向量计数器 为 一组：（空）。

可变 向量计数_已初 为 假。

向量计数_初始化 接受 零参：
    若 向量计数_已初：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 256：
        做 追加 用 向量计数器、0。
        i 为 i 加 1。
    完。
    向量计数_已初 为 真。
完。

向量计数_记录 接受 向量号：
    做 向量计数_初始化。
    若 向量号 大于或等于 0 并且 向量号 小于 256：
        向量计数器 的 第 向量号 项 为 向量计数器 的 第 向量号 项 加 1。
    完。
完。

向量计数_获取 接受 向量号：
    做 向量计数_初始化。
    若 向量号 大于或等于 0 并且 向量号 小于 256：
        返还 向量计数器 的 第 向量号 项。
    完。
    返还 0。
完。

中断_打印向量统计 接受 零参：
    做 向量计数_初始化。
    做 串口_打印 用 「=== 向量触发统计 ===\n」。
    可变 i 为 0。
    只要 i 小于 256：
        若 向量计数器 的 第 i 项 大于 0：
            做 串口_打印 用 「向量」。
            做 串口_打印十进制 用 i。
            做 串口_打印 用 「: 」。
            做 串口_打印十进制 用 向量计数器 的 第 i 项。
            做 串口_打印 用 「\n」。
        完。
        i 为 i 加 1。
    完。
完。

中断_重置统计 接受 零参：
    做 中断统计_初始化。
    全局_中断统计 的 总中断数 为 0。
    全局_中断统计 的 异常数 为 0。
    全局_中断统计 的 IRQ数 为 0。
    全局_中断统计 的 嵌套中断数 为 0。
    全局_中断统计 的 最大嵌套深度 为 0。
    全局_中断统计 的 panic数 为 0。
    全局_中断统计 的 虚假中断数 为 0。
    向量计数_已初 为 假。
    向量计数器 为 一组：（空）。
完。

可变 AI_异常有错误_风暴阈值 为 10000。

可变 AI_异常有错误_预测IRQ 为 0。

AI_异常有错误_风暴检测 接受 触发频率：
    若 触发频率 大于 AI_异常有错误_风暴阈值：
        AI_异常有错误_风暴阈值 为 AI_异常有错误_风暴阈值 乘 3 除以 2。
        返还 真。
    完。
    返还 假。
完。

AI_异常有错误_自适应合并 接受 当前延迟：
    若 当前延迟 小于 100：
        返还 16。
    完。
    若 当前延迟 小于 500：
        返还 8。
    完。
    返还 1。
完。

本章 收束。
