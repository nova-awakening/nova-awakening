《安全审计》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·同步原语》。

引用《.·进程调度》。

引用《.·安全防护初始化》。

引用《.·公共智能》。

引用《..·引导·引导加载器》。

引用《.·整数转字符串》。

引用《.·墙钟设置》。

可变 安全_时钟 为 0。

审计日志_最大 为 2048。

安全级别_信息 为 0。

安全级别_警告 为 1。

安全级别_严重 为 2。

安全级别_关键 为 3。

审计日志 是 一种 事物，含 时间戳、级别、类型、来源、详情。

可变 审计日志列表 为 一组：（空）。

可变 安全_告警数 为 0。

可变 安全_拦截数 为 0。

安全审计_记录 接受 类型、来源、详情：
    若 做 计数 用 审计日志列表 大于或等于 审计日志_最大：
        做 删除 用 审计日志列表、0。
    完。
    log 为 新建 审计日志。
    log 的 时间戳 为 安全_时钟。
    log 的 类型 为 类型。
    log 的 来源 为 来源。
    log 的 详情 为 详情。
    若 类型 等于 「权限违规」 或者 类型 等于 「入侵检测」：
        log 的 级别 为 安全级别_关键。
        安全_告警数 为 安全_告警数 加 1。
    否则若 类型 等于 「域名拦截」 或者 类型 等于 「内存超限」：
        log 的 级别 为 安全级别_警告。
        安全_拦截数 为 安全_拦截数 加 1。
    否则：
        log 的 级别 为 安全级别_信息。
    完。
    做 追加 用 审计日志列表、log。
完。

安全审计_检测异常 接受 零参：
    可变 最近违规 为 0。
    窗口 为 100。
    起始 为 0。
    若 做 计数 用 审计日志列表 大于 窗口：
        起始 为 做 计数 用 审计日志列表 减 窗口。
    完。
    可变 i 为 起始。
    只要 i 小于 做 计数 用 审计日志列表：
        若 审计日志列表 的 第 i 项 的 级别 大于或等于 安全级别_警告：
            最近违规 为 最近违规 加 1。
        完。
        i 为 i 加 1。
    完。
    若 最近违规 大于 10：
        做 内核错误 用 「安全审计: 异常活动! 最近」 加 做 整数转字符串 用 窗口 加 「条中」 加 做 整数转字符串 用 最近违规 加 「条警告」。
        返还 真。
    完。
    返还 假。
完。

授权令牌 是 一种 事物，含 令牌ID、主体、权限列表、过期时间、已吊销。

可变 令牌列表 为 一组：（空）。

可变 下一个令牌ID_安全 为 1。

零信任_颁发令牌 接受 主体、权限、有效期：
    令牌 为 新建 授权令牌。
    令牌 的 令牌ID 为 下一个令牌ID_安全。
    下一个令牌ID_安全 为 下一个令牌ID_安全 加 1。
    令牌 的 主体 为 主体。
    令牌 的 权限列表 为 权限。
    令牌 的 过期时间 为 安全_时钟 加 有效期。
    令牌 的 已吊销 为 假。
    做 追加 用 令牌列表、令牌。
    返还 令牌 的 令牌ID。
完。

零信任_验证 接受 令牌ID、操作：
    可变 i 为 0。
    只要 i 小于 做 计数 用 令牌列表：
        若 令牌列表 的 第 i 项 的 令牌ID 等于 令牌ID：
            若 令牌列表 的 第 i 项 的 已吊销：
                返还 假。
            完。
            若 令牌列表 的 第 i 项 的 过期时间 小于 安全_时钟：
                令牌列表 的 第 i 项 的 已吊销 为 真。
                返还 假。
            完。
            可变 j 为 0。
            只要 j 小于 做 计数 用 令牌列表 的 第 i 项 的 权限列表：
                若 令牌列表 的 第 i 项 的 权限列表 的 第 j 项 等于 操作：
                    返还 真。
                完。
                j 为 j 加 1。
            完。
            做 安全审计_记录 用 「权限不足」、令牌列表 的 第 i 项 的 主体、操作。
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

零信任_吊销 接受 令牌ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 令牌列表：
        若 令牌列表 的 第 i 项 的 令牌ID 等于 令牌ID：
            令牌列表 的 第 i 项 的 已吊销 为 真。
            做 安全审计_记录 用 「令牌吊销」、「零信任」、「ID=」 加 做 整数转字符串 用 令牌ID。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

零信任_清理过期 接受 零参：
    可变 清理数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 令牌列表：
        若 令牌列表 的 第 i 项 的 过期时间 小于 安全_时钟 并且 非 令牌列表 的 第 i 项 的 已吊销：
            令牌列表 的 第 i 项 的 已吊销 为 真。
            清理数 为 清理数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 清理数。
完。

安全角色 是 一种 事物，含 名称、权限列表、父角色、优先级。

可变 角色列表 为 一组：（空）。

RBAC_创建角色 接受 名称、权限、父角色：
    角色 为 新建 安全角色：名称 为 名称、权限列表 为 权限、父角色 为 父角色、优先级 为 做 计数 用 角色列表。
    做 追加 用 角色列表、角色。
    返还 角色。
完。

RBAC_检查权限 接受 角色、操作：
    若 角色 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 角色 的 权限列表：
        若 角色 的 权限列表 的 第 i 项 等于 操作 或者 角色 的 权限列表 的 第 i 项 等于 「*」：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    若 角色 的 父角色 不等于 空：
        返还 做 RBAC_检查权限 用 角色 的 父角色、操作。
    完。
    返还 假。
完。

RBAC_初始化默认角色 接受 零参：
    root角色 为 做 RBAC_创建角色 用 「root」、一组：「*」、空。
    admin角色 为 做 RBAC_创建角色 用 「admin」、一组：「用户管理」、「系统配置」、「服务管理」、「安全管理」、root角色。
    用户角色 为 做 RBAC_创建角色 用 「user」、一组：「文件读写」、「网络访问」、「进程创建」、空。
    访客角色 为 做 RBAC_创建角色 用 「guest」、一组：「文件读」、空。
完。

沙箱策略 是 一种 事物，含 名称、隔离级别、允许网络、允许文件写、允许进程创建、内存配额_MB、CPU配额_百分比、磁盘配额_MB。

隔离_无 为 0。

隔离_基本 为 1。

隔离_严格 为 2。

隔离_完全 为 3。

可变 沙箱策略列表 为 一组：（空）。

沙箱_创建策略 接受 名称、级别：
    策略 为 新建 沙箱策略：名称 为 名称、隔离级别 为 级别、允许网络 为 级别 小于 隔离_严格、允许文件写 为 级别 小于 隔离_完全、允许进程创建 为 级别 小于 隔离_严格、内存配额_MB 为 256、CPU配额_百分比 为 50、磁盘配额_MB 为 1024。
    若 级别 等于 隔离_完全：
        策略 的 内存配额_MB 为 64。
        策略 的 CPU配额_百分比 为 10。
        策略 的 磁盘配额_MB 为 0。
    完。
    做 追加 用 沙箱策略列表、策略。
    返还 策略。
完。

沙箱_检查操作 接受 策略、操作类型：
    若 策略 等于 空：
        返还 真。
    完。
    若 操作类型 等于 「网络」 并且 非 策略 的 允许网络：
        返还 假。
    完。
    若 操作类型 等于 「文件写」 并且 非 策略 的 允许文件写：
        返还 假。
    完。
    若 操作类型 等于 「进程创建」 并且 非 策略 的 允许进程创建：
        返还 假。
    完。
    返还 真。
完。

AI行为基线 是 一种 事物，含 PID、正常系统调用频率、正常文件操作频率、正常网络频率、采样数、偏差阈值。

可变 AI行为基线表 为 一组：（空）。

可变 AI_威胁检测数 为 0。

AI安全_建立基线 接受 PID、系统调用率、文件操作率、网络率：
    做 追加 用 AI行为基线表、新建 AI行为基线：PID 为 PID、正常系统调用频率 为 系统调用率、正常文件操作频率 为 文件操作率、正常网络频率 为 网络率、采样数 为 1、偏差阈值 为 300。
完。

AI安全_检测偏差 接受 PID、当前系统调用率、当前文件率、当前网络率：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI行为基线表：
        若 AI行为基线表 的 第 i 项 的 PID 等于 PID：
            基线 为 AI行为基线表 的 第 i 项。
            可变 偏差分 为 0。
            若 基线 的 正常系统调用频率 大于 0：
                比率 为 当前系统调用率 乘 100 除以 基线 的 正常系统调用频率。
                若 比率 大于 200 或者 比率 小于 30：
                    偏差分 为 偏差分 加 100。
                完。
            完。
            若 基线 的 正常文件操作频率 大于 0：
                比率 为 当前文件率 乘 100 除以 基线 的 正常文件操作频率。
                若 比率 大于 300：
                    偏差分 为 偏差分 加 150。
                完。
            完。
            若 基线 的 正常网络频率 大于 0：
                比率 为 当前网络率 乘 100 除以 基线 的 正常网络频率。
                若 比率 大于 500：
                    偏差分 为 偏差分 加 200。
                完。
            完。
            若 偏差分 大于 基线 的 偏差阈值：
                AI_威胁检测数 为 AI_威胁检测数 加 1。
                做 安全审计_记录 用 「AI威胁」、「PID=」 加 做 整数转字符串 用 PID、「偏差=」 加 做 整数转字符串 用 偏差分。
                返还 真。
            完。
            基线 的 正常系统调用频率 为 (基线 的 正常系统调用频率 乘 7 加 当前系统调用率) 除以 8。
            基线 的 正常文件操作频率 为 (基线 的 正常文件操作频率 乘 7 加 当前文件率) 除以 8。
            基线 的 正常网络频率 为 (基线 的 正常网络频率 乘 7 加 当前网络率) 除以 8。
            基线 的 采样数 为 基线 的 采样数 加 1。
            返还 假。
        完。
        i 为 i 加 1。
    完。
    做 AI安全_建立基线 用 PID、当前系统调用率、当前文件率、当前网络率。
    返还 假。
完。

安全系统_初始化 接受 零参：
    审计日志列表 为 一组：（空）。
    令牌列表 为 一组：（空）。
    角色列表 为 一组：（空）。
    沙箱策略列表 为 一组：（空）。
    AI行为基线表 为 一组：（空）。
    安全_时钟 为 做 当前时间_纳秒 除以 1000000。
    做 RBAC_初始化默认角色。
    做 内核信息 用 「[安全] 审计+零信任+RBAC+沙箱+AI威胁分析 初始化完成」。
完。

安全系统_更新时钟 接受 零参：
    安全_时钟 为 做 当前时间_纳秒 除以 1000000。
完。

安全_统计 接受 零参：
    返还 一本字典：审计日志数 对应 做 计数 用 审计日志列表、告警数 对应 安全_告警数、拦截数 对应 安全_拦截数、令牌数 对应 做 计数 用 令牌列表、角色数 对应 做 计数 用 角色列表、沙箱策略数 对应 做 计数 用 沙箱策略列表、AI基线数 对应 做 计数 用 AI行为基线表、AI威胁检测 对应 AI_威胁检测数。
完。

安全_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 安全_告警数 大于 50：
        做 追加 用 问题、「告警过多: 」 加 做 整数转字符串 用 安全_告警数。
    完。
    若 AI_威胁检测数 大于 10：
        做 追加 用 问题、「AI检测威胁: 」 加 做 整数转字符串 用 AI_威胁检测数。
    完。
    异常 为 做 安全审计_检测异常。
    若 异常：
        做 追加 用 问题、「审计异常活动」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「安全系统健康」。
    完。
    返还 问题。
完。

本章 收束。
