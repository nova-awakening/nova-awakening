《公平调度睡眠》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·公平调度周期》。

引用《.·进程描述符》。

引用《.·内核打印》。

引用《.·高精度定时器hrtimer》。

引用《.·整数转字符串》。

RT_优先级数 为 100。

调度_实时FIFO 为 1。

调度_实时RR 为 2。

调度_普通 为 0。

调度_最大CPU 为 8。

优先级提升_IO 为 5。

优先级提升_交互 为 3。

负载均衡_阈值 为 20。

可变 RT_FIFO队列 为 一组：（空）。

可变 RT_进程数 为 0。

可变 Nova公平调度_当前执行时间 为 0。

调度统计 是 一种 事物，含 入队次数、出队次数、选择次数、tick次数、抢占次数、迁移次数、负载均衡次数、优先级提升次数、睡眠保存次数、睡眠恢复次数、RT入队次数、RT出队次数、RT选择次数、空闲选择次数、AI建议次数。

可变 全局_调度统计 为 空。

调度统计_初始化 接受 零参：
    若 全局_调度统计 等于 空：
        全局_调度统计 为 新建 调度统计。
        全局_调度统计 的 入队次数 为 0。
        全局_调度统计 的 出队次数 为 0。
        全局_调度统计 的 选择次数 为 0。
        全局_调度统计 的 tick次数 为 0。
        全局_调度统计 的 抢占次数 为 0。
        全局_调度统计 的 迁移次数 为 0。
        全局_调度统计 的 负载均衡次数 为 0。
        全局_调度统计 的 优先级提升次数 为 0。
        全局_调度统计 的 睡眠保存次数 为 0。
        全局_调度统计 的 睡眠恢复次数 为 0。
        全局_调度统计 的 RT入队次数 为 0。
        全局_调度统计 的 RT出队次数 为 0。
        全局_调度统计 的 RT选择次数 为 0。
        全局_调度统计 的 空闲选择次数 为 0。
        全局_调度统计 的 AI建议次数 为 0。
        可变 p 为 0。
        只要 p 小于 RT_优先级数：
            做 追加 用 RT_FIFO队列、一组：（空）。
            p 为 p 加 1。
        完。
    完。
完。

Nova公平调度_睡眠保存 接受 任务：
    做 调度统计_初始化。
    全局_调度统计 的 睡眠保存次数 为 全局_调度统计 的 睡眠保存次数 加 1。
    平均v 为 做 Nova公平调度_平均Nova虚拟运行时间。
    做 设置 用 任务、T_vlag、平均v 减 任务 的 第 T_虚拟运行时间 项。
    做 设置 用 任务、T_睡眠时间戳、做 获取滴答。
完。

Nova公平调度_唤醒恢复 接受 任务：
    做 调度统计_初始化。
    全局_调度统计 的 睡眠恢复次数 为 全局_调度统计 的 睡眠恢复次数 加 1。
    平均v 为 做 Nova公平调度_平均Nova虚拟运行时间。
    vlag 为 任务 的 第 T_vlag 项。
    任务 的 第 T_虚拟运行时间 项 为 平均v 减 vlag。
    睡眠时长 为 做 获取滴答 减 任务 的 第 T_睡眠时间戳 项。
    若 睡眠时长 大于 1000000：
        做 调度_优先级提升 用 任务、优先级提升_IO。
    完。
完。

RT_入队 接受 任务：
    做 调度统计_初始化。
    全局_调度统计 的 RT入队次数 为 全局_调度统计 的 RT入队次数 加 1。
    可变 优先级 为 任务 的 第 T_优先级 项。
    若 优先级 小于 0：
        优先级 为 0。
    完。
    若 优先级 大于或等于 RT_优先级数：
        优先级 为 RT_优先级数 减 1。
    完。
    做 追加 用 RT_FIFO队列 的 第 优先级 项、任务。
    RT_进程数 为 RT_进程数 加 1。
完。

RT_出队 接受 任务：
    做 调度统计_初始化。
    全局_调度统计 的 RT出队次数 为 全局_调度统计 的 RT出队次数 加 1。
    可变 优先级 为 任务 的 第 T_优先级 项。
    若 优先级 小于 0：
        优先级 为 0。
    完。
    若 优先级 大于或等于 RT_优先级数：
        优先级 为 RT_优先级数 减 1。
    完。
    队列 为 RT_FIFO队列 的 第 优先级 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 队列：
        若 队列 的 第 i 项 等于 任务：
            做 删除 用 队列、i。
            RT_进程数 为 RT_进程数 减 1。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    返还 错误_不存在。
完。

RT_选择下一个 接受 零参：
    做 调度统计_初始化。
    全局_调度统计 的 RT选择次数 为 全局_调度统计 的 RT选择次数 加 1。
    若 RT_进程数 小于或等于 0：
        返还 空。
    完。
    可变 p 为 0。
    只要 p 小于 RT_优先级数：
        若 做 计数 用 RT_FIFO队列 的 第 p 项 大于 0：
            任务 为 RT_FIFO队列 的 第 p 项 的 第 0 项。
            做 删除 用 RT_FIFO队列 的 第 p 项、0。
            RT_进程数 为 RT_进程数 减 1。
            做 进程_设置状态 用 任务、进程_运行。
            返还 任务。
        完。
        p 为 p 加 1。
    完。
    返还 空。
完。

RT_tick 接受 当前任务、delta_us：
    若 当前任务 等于 空：
        返还 假。
    完。
    若 当前任务 的 第 T_调度策略 项 等于 调度_实时FIFO：
        返还 假。
    完。
    若 当前任务 的 第 T_调度策略 项 等于 调度_实时RR：
        Nova公平调度_当前执行时间 为 Nova公平调度_当前执行时间 加 delta_us。
        若 Nova公平调度_当前执行时间 大于或等于 10000：
            Nova公平调度_当前执行时间 为 0。
            做 调度统计_初始化。
            全局_调度统计 的 抢占次数 为 全局_调度统计 的 抢占次数 加 1。
            返还 真。
        完。
    完。
    返还 假。
完。

调度_入队 接受 任务：
    做 调度统计_初始化。
    全局_调度统计 的 入队次数 为 全局_调度统计 的 入队次数 加 1。
    若 做 进程_是实时 用 任务：
        做 RT_入队 用 任务。
    否则：
        做 Nova公平调度_入队 用 任务。
    完。
完。

调度_出队 接受 任务：
    做 调度统计_初始化。
    全局_调度统计 的 出队次数 为 全局_调度统计 的 出队次数 加 1。
    若 做 进程_是实时 用 任务：
        做 RT_出队 用 任务。
    否则：
        做 Nova公平调度_出队 用 任务。
    完。
完。

调度_选择下一个 接受 零参：
    做 调度统计_初始化。
    全局_调度统计 的 选择次数 为 全局_调度统计 的 选择次数 加 1。
    rt 为 做 RT_选择下一个。
    若 rt 不等于 空：
        返还 rt。
    完。
    cfs 为 做 Nova公平调度_选择下一个。
    若 cfs 不等于 空：
        返还 cfs。
    完。
    全局_调度统计 的 空闲选择次数 为 全局_调度统计 的 空闲选择次数 加 1。
    返还 空。
完。

调度_tick 接受 当前任务、delta_us：
    做 调度统计_初始化。
    全局_调度统计 的 tick次数 为 全局_调度统计 的 tick次数 加 1。
    若 当前任务 等于 空：
        返还 假。
    完。
    若 做 进程_是实时 用 当前任务：
        返还 做 RT_tick 用 当前任务、delta_us。
    完。
    需要抢占 为 做 Nova公平调度_tick 用 当前任务、delta_us。
    若 需要抢占：
        全局_调度统计 的 抢占次数 为 全局_调度统计 的 抢占次数 加 1。
    完。
    返还 需要抢占。
完。

调度_优先级提升 接受 任务、提升量：
    做 调度统计_初始化。
    全局_调度统计 的 优先级提升次数 为 全局_调度统计 的 优先级提升次数 加 1。
    若 任务 等于 空：
        返还。
    完。
    若 做 进程_是实时 用 任务：
        返还。
    完。
    当前优先级 为 任务 的 第 T_优先级 项。
    新优先级 为 当前优先级 减 提升量。
    若 新优先级 小于 0：
        新优先级 为 0。
    完。
    任务 的 第 T_优先级 项 为 新优先级。
完。

调度_优先级衰减 接受 任务：
    若 任务 等于 空：
        返还。
    完。
    若 做 进程_是实时 用 任务：
        返还。
    完。
    当前优先级 为 任务 的 第 T_优先级 项。
    默认优先级 为 任务 的 第 T_默认优先级 项。
    若 当前优先级 小于 默认优先级：
        任务 的 第 T_优先级 项 为 当前优先级 加 1。
        若 任务 的 第 T_优先级 项 大于 默认优先级：
            任务 的 第 T_优先级 项 为 默认优先级。
        完。
    完。
完。

调度_负载均衡 接受 零参：
    做 调度统计_初始化。
    全局_调度统计 的 负载均衡次数 为 全局_调度统计 的 负载均衡次数 加 1。
    可变 最大负载CPU 为 0。
    可变 最大负载 为 0。
    可变 最小负载CPU 为 0。
    可变 最小负载 为 999999。
    可变 cpu 为 0。
    只要 cpu 小于 调度_最大CPU：
        负载 为 做 调度_获取CPU负载 用 cpu。
        若 负载 大于 最大负载：
            最大负载 为 负载。
            最大负载CPU 为 cpu。
        完。
        若 负载 小于 最小负载：
            最小负载 为 负载。
            最小负载CPU 为 cpu。
        完。
        cpu 为 cpu 加 1。
    完。
    若 最大负载 减 最小负载 大于 负载均衡_阈值：
        做 调度_迁移任务 用 最大负载CPU、最小负载CPU。
    完。
完。

调度_迁移任务 接受 源CPU、目标CPU：
    做 调度统计_初始化。
    全局_调度统计 的 迁移次数 为 全局_调度统计 的 迁移次数 加 1。
    任务 为 做 调度_获取可迁移任务 用 源CPU。
    若 任务 等于 空：
        返还。
    完。
    做 调度_出队 用 任务。
    任务 的 第 T_CPU亲和 项 为 目标CPU。
    做 调度_入队 用 任务。
完。

调度_获取CPU负载 接受 cpu：
    运行 为 做 调度_运行队列长度。
    返还 运行 乘 100。
完。

调度_获取可迁移任务 接受 cpu：
    返还 做 调度_运行队列长度 除以 2。
完。

调度_AI建议 接受 任务：
    做 调度统计_初始化。
    全局_调度统计 的 AI建议次数 为 全局_调度统计 的 AI建议次数 加 1。
    特征 为 一组：任务 的 第 T_优先级 项、任务 的 第 T_虚拟运行时间 项、任务 的 第 T_调度策略 项、任务 的 第 T_CPU亲和 项。
    建议 为 做 AI调度钩子_预测 用 特征。
    若 建议 不等于 空 并且 建议 的 置信度 大于 70：
        若 建议 的 动作 等于 「提升」：
            做 调度_优先级提升 用 任务、建议 的 提升量。
        否则若 建议 的 动作 等于 「迁移」：
            做 调度_迁移任务 用 任务 的 第 T_CPU亲和 项、建议 的 目标CPU。
        完。
    完。
完。

调度_打印统计 接受 零参：
    做 调度统计_初始化。
    做 打印行 用 「=== 调度统计 ===」。
    做 打印行 用 「入队: 」 加 做 整数转字符串 用 全局_调度统计 的 入队次数。
    做 打印行 用 「出队: 」 加 做 整数转字符串 用 全局_调度统计 的 出队次数。
    做 打印行 用 「选择: 」 加 做 整数转字符串 用 全局_调度统计 的 选择次数。
    做 打印行 用 「tick: 」 加 做 整数转字符串 用 全局_调度统计 的 tick次数。
    做 打印行 用 「抢占: 」 加 做 整数转字符串 用 全局_调度统计 的 抢占次数。
    做 打印行 用 「迁移: 」 加 做 整数转字符串 用 全局_调度统计 的 迁移次数。
    做 打印行 用 「负载均衡: 」 加 做 整数转字符串 用 全局_调度统计 的 负载均衡次数。
    做 打印行 用 「优先级提升: 」 加 做 整数转字符串 用 全局_调度统计 的 优先级提升次数。
    做 打印行 用 「睡眠保存: 」 加 做 整数转字符串 用 全局_调度统计 的 睡眠保存次数。
    做 打印行 用 「睡眠恢复: 」 加 做 整数转字符串 用 全局_调度统计 的 睡眠恢复次数。
    做 打印行 用 「RT入队: 」 加 做 整数转字符串 用 全局_调度统计 的 RT入队次数。
    做 打印行 用 「RT出队: 」 加 做 整数转字符串 用 全局_调度统计 的 RT出队次数。
    做 打印行 用 「RT选择: 」 加 做 整数转字符串 用 全局_调度统计 的 RT选择次数。
    做 打印行 用 「空闲选择: 」 加 做 整数转字符串 用 全局_调度统计 的 空闲选择次数。
    做 打印行 用 「AI建议: 」 加 做 整数转字符串 用 全局_调度统计 的 AI建议次数。
    做 打印行 用 「RT进程数: 」 加 做 整数转字符串 用 RT_进程数。
完。

本章 收束。
