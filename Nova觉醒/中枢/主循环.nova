《主循环》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·推理·通信渠道》。

引用《..·稳态·粒子系统》。

引用《.·骨骼动画》。

引用《.·硬链接》。

引用《.·字符串替换》。

引用《.·语音识别》。

引用《..·推理·定点Q算术》。

引用《.·整数转字符串》。

Q10 为 1024。

Q_ONE 为 1024。

Q_HALF 为 512。

助手状态_休眠 为 0。

助手状态_待命 为 1。

助手状态_聆听 为 2。

助手状态_思考 为 3。

助手状态_回答 为 4。

助手状态_执行 为 5。

助手状态_错误 为 6。

表情_中性 为 0。

表情_微笑 为 1。

表情_惊讶 为 2。

表情_思索 为 3。

表情_困惑 为 4。

表情_喜悦 为 5。

表情_关切 为 6。

意图_未知 为 0。

意图_查询 为 1。

意图_控制 为 2。

意图_导航 为 3。

意图_创作 为 4。

意图_闲聊 为 5。

意图_系统 为 6。

意图_取消 为 7。

最大上下文轮数 为 32。

最大分词长度 为 128。

意图阈值 为 614。

槽位最大数 为 16。

情绪_衰减_慢 为 992。

情绪_衰减_快 为 870。

情绪_激活阈值 为 614。

手势_无 为 0。

手势_挥手 为 1。

手势_点击 为 2。

手势_滑动左 为 3。

手势_滑动右 为 4。

手势_捏合 为 5。

手势_展开 为 6。

手势_最大关节数 为 21。

手势_轨迹缓冲 为 64。

最大粒子数 为 1024。

最大面板数 为 16。

光晕层数 为 4。

EWMA_ALPHA_慢 为 64。

EWMA_ALPHA_中 为 205。

EWMA_ALPHA_快 为 512。

对话轮 是 一种 事物，含 角色、文本、意图、时间戳、情绪快照。

NLP槽位 是 一种 事物，含 名称、值、置信度。

NLP结果 是 一种 事物，含 意图、置信度、槽位列表、原始文本。

情绪状态 是 一种 事物，含 愉悦、激活、信任、共鸣度。

手势帧 是 一种 事物，含 关节坐标、时间戳。

粒子 是 一种 事物，含 x、y、z、vx、vy、vz、生命、最大生命、颜色、大小、活跃。

全息面板 是 一种 事物，含 id、x、y、z、宽、高、内容类型、内容、透明度、可见。

可变 助手_时钟 为 0。

可变 主助手 为 空。

可变 上下文历史 为 一组：（空）。

可变 上下文头 为 0。

可变 上下文计数 为 0。

可变 当前情绪 为 新建 情绪状态：愉悦 为 0、激活 为 0、信任 为 Q_HALF、共鸣度 为 0。

可变 手势轨迹 为 一组：（空）。

可变 手势轨迹头 为 0。

可变 手势轨迹计数 为 0。

可变 当前手势 为 手势_无。

可变 粒子池 为 一组：（空）。

可变 面板列表 为 一组：（空）。

可变 渲染_帧数 为 0。

可变 粒子_活跃数 为 0。

可变 聆听超时_tick 为 0。

可变 思考超时_tick 为 0。

可变 连续沉默_tick 为 0。

可变 EWMA_响应延迟 为 0。

可变 EWMA_意图置信度 为 Q_HALF。

全息助手_初始化 接受 助手：
    主助手 为 助手。
    助手_时钟 为 0。
    助手 的 状态 为 助手状态_待命。
    助手 的 当前表情 为 表情_中性。
    上下文历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大上下文轮数：
        做 追加 用 上下文历史、新建 对话轮：角色 为 「」、文本 为 「」、意图 为 0、时间戳 为 0、情绪快照 为 0。
        i 为 i 加 1。
    完。
    上下文头 为 0。
    上下文计数 为 0。
    手势轨迹 为 一组：（空）。
    i 为 0。
    只要 i 小于 手势_轨迹缓冲：
        做 追加 用 手势轨迹、新建 手势帧：关节坐标 为 一组：（空）、时间戳 为 0。
        i 为 i 加 1。
    完。
    手势轨迹头 为 0。
    手势轨迹计数 为 0。
    粒子池 为 一组：（空）。
    i 为 0。
    只要 i 小于 最大粒子数：
        做 追加 用 粒子池、新建 粒子：x 为 0、y 为 0、z 为 0、vx 为 0、vy 为 0、vz 为 0、生命 为 0、最大生命 为 0、颜色 为 0、大小 为 0、活跃 为 假。
        i 为 i 加 1。
    完。
    面板列表 为 一组：（空）。
    当前情绪 为 新建 情绪状态：愉悦 为 0、激活 为 0、信任 为 Q_HALF、共鸣度 为 0。
完。

全息助手_tick 接受 助手：
    若 助手 等于 空：
        返还。
    完。
    助手_时钟 为 助手_时钟 加 1。
    做 动画_更新 用 助手。
    做 TTS_更新 用 助手。
    做 情绪_衰减。
    g 为 做 手势_检测。
    若 g 不等于 手势_无：
        做 手势_响应 用 助手、g。
    完。
    做 状态机_处理 用 助手。
    做 粒子_更新。
    做 全息_环境粒子 用 助手_时钟。
    渲染_帧数 为 渲染_帧数 加 1。
完。

状态机_处理 接受 助手：
    若 助手 的 状态 等于 助手状态_休眠：
        连续沉默_tick 为 连续沉默_tick 加 1。
        若 连续沉默_tick 取余于 5000 等于 0：
            做 粒子_发射_呼吸 用 助手。
        完。
        返还。
    完。
    若 助手 的 状态 等于 助手状态_待命：
        连续沉默_tick 为 连续沉默_tick 加 1。
        若 全局ASR 不等于 空 并且 全局ASR 的 识别结果 不等于 「」：
            若 做 文本包含_助手 用 全局ASR 的 识别结果、助手 的 唤醒词：
                做 转入_聆听 用 助手。
            完。
        完。
        若 连续沉默_tick 大于 30000：
            助手 的 状态 为 助手状态_休眠。
            助手 的 当前表情 为 表情_中性。
        完。
        返还。
    完。
    若 助手 的 状态 等于 助手状态_聆听：
        聆听超时_tick 为 聆听超时_tick 加 1。
        若 全局ASR 不等于 空 并且 非 全局ASR 的 正在录音 并且 全局ASR 的 识别结果 不等于 「」：
            做 转入_思考 用 助手、全局ASR 的 识别结果。
            全局ASR 的 识别结果 为 「」。
        完。
        若 聆听超时_tick 大于 600：
            助手 的 状态 为 助手状态_待命。
            助手 的 当前表情 为 表情_中性。
            连续沉默_tick 为 0。
        完。
        返还。
    完。
    若 助手 的 状态 等于 助手状态_思考：
        思考超时_tick 为 思考超时_tick 加 1。
        若 思考超时_tick 大于 300：
            助手 的 状态 为 助手状态_错误。
            做 TTS_说话 用 助手、「思考超时,请再说一次」、80。
        完。
        返还。
    完。
    若 助手 的 状态 等于 助手状态_回答：
        若 非 做 TTS_正在播放 用 助手：
            做 转入_聆听 用 助手。
        完。
        返还。
    完。
    若 助手 的 状态 等于 助手状态_执行：
        返还。
    完。
    若 助手 的 状态 等于 助手状态_错误：
        连续沉默_tick 为 连续沉默_tick 加 1。
        若 连续沉默_tick 大于 200：
            助手 的 状态 为 助手状态_待命。
            助手 的 当前表情 为 表情_中性。
            连续沉默_tick 为 0。
        完。
    完。
完。

转入_聆听 接受 助手：
    助手 的 状态 为 助手状态_聆听。
    助手 的 当前表情 为 表情_惊讶。
    做 TTS_说话 用 助手、「我在!」、100。
    做 ASR_开始录音。
    聆听超时_tick 为 0。
    连续沉默_tick 为 0。
    做 情绪_激活提升 用 200。
完。

转入_思考 接受 助手、文本：
    助手 的 状态 为 助手状态_思考。
    助手 的 当前表情 为 表情_思索。
    思考超时_tick 为 0。
    做 粒子_发射_思考 用 助手。
    nlp 为 做 NLP_处理 用 文本。
    EWMA_意图置信度 为 (EWMA_ALPHA_中 乘 nlp 的 置信度 加 (Q10 减 EWMA_ALPHA_中) 乘 EWMA_意图置信度) 除以 Q10。
    做 上下文_添加 用 「user」、文本、nlp 的 意图。
    ai意图 为 做 AI_意图推理 用 nlp。
    若 ai意图 大于 0 并且 nlp 的 置信度 小于 意图阈值：
        nlp 的 意图 为 ai意图。
    完。
    做 情绪_对话反馈 用 nlp 的 意图。
    应答 为 做 应答_生成 用 助手、nlp。
    做 上下文_添加 用 「assistant」、应答、nlp 的 意图。
    助手 的 状态 为 助手状态_回答。
    助手 的 当前表情 为 做 意图_到表情 用 nlp 的 意图。
    做 TTS_说话 用 助手、应答、100。
    助手 的 交互次数 为 助手 的 交互次数 加 1。
完。

NLP_处理 接受 文本：
    词列表 为 做 分词 用 文本。
    意图 为 做 意图_分类 用 词列表。
    槽位 为 做 槽位_提取 用 词列表、意图。
    置信度 为 做 意图_置信度 用 词列表、意图。
    返还 新建 NLP结果：意图 为 意图、置信度 为 置信度、槽位列表 为 槽位、原始文本 为 文本。
完。

分词 接受 文本：
    可变 词 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本 并且 i 小于 最大分词长度：
        c 为 做 字符串取字符 用 文本、i。
        若 c 等于 「 」 或者 c 等于 「,」 或者 c 等于 「。」 或者 c 等于 「?」 或者 c 等于 「!」 或者 c 等于 「？」 或者 c 等于 「！」：
            若 做 字符串长度 用 当前 大于 0：
                做 追加 用 词、当前。
                当前 为 「」。
            完。
        否则：
            当前 为 当前 加 c。
        完。
        i 为 i 加 1。
    完。
    若 做 字符串长度 用 当前 大于 0：
        做 追加 用 词、当前。
    完。
    返还 词。
完。

意图_分类 接受 词列表：
    可变 分数 为 一组：0、0、0、0、0、0、0、0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词列表：
        w 为 词列表 的 第 i 项。
        若 做 文本包含_助手 用 w、「查」 或者 做 文本包含_助手 用 w、「搜」 或者 做 文本包含_助手 用 w、「什么」 或者 做 文本包含_助手 用 w、「哪」：
            分数 的 第 意图_查询 项 为 分数 的 第 意图_查询 项 加 Q10。
        完。
        若 做 文本包含_助手 用 w、「打开」 或者 做 文本包含_助手 用 w、「关闭」 或者 做 文本包含_助手 用 w、「启动」 或者 做 文本包含_助手 用 w、「停止」：
            分数 的 第 意图_控制 项 为 分数 的 第 意图_控制 项 加 Q10。
        完。
        若 做 文本包含_助手 用 w、「去」 或者 做 文本包含_助手 用 w、「导航」 或者 做 文本包含_助手 用 w、「路线」：
            分数 的 第 意图_导航 项 为 分数 的 第 意图_导航 项 加 Q10。
        完。
        若 做 文本包含_助手 用 w、「写」 或者 做 文本包含_助手 用 w、「画」 或者 做 文本包含_助手 用 w、「创建」 或者 做 文本包含_助手 用 w、「生成」：
            分数 的 第 意图_创作 项 为 分数 的 第 意图_创作 项 加 Q10。
        完。
        若 做 文本包含_助手 用 w、「你好」 或者 做 文本包含_助手 用 w、「嗨」 或者 做 文本包含_助手 用 w、「聊」：
            分数 的 第 意图_闲聊 项 为 分数 的 第 意图_闲聊 项 加 Q10。
        完。
        若 做 文本包含_助手 用 w、「系统」 或者 做 文本包含_助手 用 w、「设置」 或者 做 文本包含_助手 用 w、「配置」：
            分数 的 第 意图_系统 项 为 分数 的 第 意图_系统 项 加 Q10。
        完。
        若 做 文本包含_助手 用 w、「取消」 或者 做 文本包含_助手 用 w、「算了」 或者 做 文本包含_助手 用 w、「停」：
            分数 的 第 意图_取消 项 为 分数 的 第 意图_取消 项 加 Q10。
        完。
        i 为 i 加 1。
    完。
    可变 最佳 为 意图_未知。
    可变 最高 为 0。
    i 为 0。
    只要 i 小于 8：
        若 分数 的 第 i 项 大于 最高：
            最高 为 分数 的 第 i 项。
            最佳 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

意图_置信度 接受 词列表、意图：
    若 意图 等于 意图_未知：
        返还 0。
    完。
    总 为 做 计数 用 词列表。
    若 总 等于 0：
        返还 0。
    完。
    返还 Q10 乘 做 计数 用 词列表 除以 (总 加 2)。
完。

槽位_提取 接受 词列表、意图：
    可变 槽位 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词列表 并且 做 计数 用 槽位 小于 槽位最大数：
        做 追加 用 槽位、新建 NLP槽位：名称 为 「实体」 加 做 整数转字符串 用 做 计数 用 槽位、值 为 词列表 的 第 i 项、置信度 为 Q_HALF。
        i 为 i 加 1。
    完。
    返还 槽位。
完。

意图_到表情 接受 意图：
    若 意图 等于 意图_闲聊：
        返还 表情_微笑。
    完。
    若 意图 等于 意图_查询：
        返还 表情_思索。
    完。
    若 意图 等于 意图_控制：
        返还 表情_中性。
    完。
    若 意图 等于 意图_创作：
        返还 表情_喜悦。
    完。
    若 意图 等于 意图_错误 或者 意图 等于 意图_未知：
        返还 表情_困惑。
    完。
    返还 表情_中性。
完。

上下文_添加 接受 角色、文本、意图：
    idx 为 上下文头 取余于 最大上下文轮数。
    做 设置 用 上下文历史、idx、新建 对话轮：角色 为 角色、文本 为 文本、意图 为 意图、时间戳 为 助手_时钟、情绪快照 为 当前情绪 的 愉悦。
    上下文头 为 上下文头 加 1。
    若 上下文计数 小于 最大上下文轮数：
        上下文计数 为 上下文计数 加 1。
    完。
完。

上下文_最近N 接受 n：
    可变 结果 为 一组：（空）。
    若 n 大于 上下文计数：
        n 为 上下文计数。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        idx 为 (上下文头 减 n 加 i 加 最大上下文轮数 乘 10) 取余于 最大上下文轮数。
        做 追加 用 结果、上下文历史 的 第 idx 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

上下文_相关性搜索 接受 查询词列表、最大结果：
    可变 分数 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 上下文计数：
        idx 为 (上下文头 减 上下文计数 加 i 加 最大上下文轮数 乘 10) 取余于 最大上下文轮数。
        可变 s 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 查询词列表：
            若 做 文本包含_助手 用 上下文历史 的 第 idx 项 的 文本、查询词列表 的 第 j 项：
                s 为 s 加 Q10。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 分数、一组：idx、s。
        i 为 i 加 1。
    完。
    做 排序_降序 用 分数。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 分数 并且 i 小于 最大结果：
        若 分数 的 第 i 项 的 第 1 项 大于 0：
            做 追加 用 结果、上下文历史 的 第 分数 的 第 i 项 的 第 0 项 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

排序_降序 接受 数组：
    可变 i 为 0。
    只要 i 小于 做 计数 用 数组：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 数组：
            若 数组 的 第 j 项 的 第 1 项 大于 数组 的 第 i 项 的 第 1 项：
                tmp 为 数组 的 第 i 项。
                做 设置 用 数组、i、数组 的 第 j 项。
                做 设置 用 数组、j、tmp。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

应答_生成 接受 助手、nlp：
    若 nlp 的 意图 等于 意图_闲聊：
        返还 「你好! 我是你的全息助手, 有什么我能帮你的吗?」。
    完。
    若 nlp 的 意图 等于 意图_取消：
        返还 「好的, 已取消」。
    完。
    若 nlp 的 意图 等于 意图_查询：
        返还 「正在为你查询: 」 加 nlp 的 原始文本。
    完。
    若 nlp 的 意图 等于 意图_控制：
        返还 「正在执行控制指令」。
    完。
    若 nlp 的 意图 等于 意图_导航：
        返还 「正在规划路线」。
    完。
    若 nlp 的 意图 等于 意图_创作：
        返还 「开始创作模式」。
    完。
    若 nlp 的 意图 等于 意图_系统：
        返还 「进入系统设置」。
    完。
    返还 「我听到了: 」 加 nlp 的 原始文本 加 「, 但我不太确定你需要什么」。
完。

情绪_衰减 接受 零参：
    当前情绪 的 愉悦 为 当前情绪 的 愉悦 乘 情绪_衰减_慢 除以 Q10。
    当前情绪 的 激活 为 当前情绪 的 激活 乘 情绪_衰减_快 除以 Q10。
    当前情绪 的 共鸣度 为 当前情绪 的 共鸣度 乘 情绪_衰减_慢 除以 Q10。
完。

情绪_激活提升 接受 量：
    当前情绪 的 激活 为 当前情绪 的 激活 加 量。
    若 当前情绪 的 激活 大于 Q_ONE：
        当前情绪 的 激活 为 Q_ONE。
    完。
完。

情绪_对话反馈 接受 意图：
    若 意图 等于 意图_闲聊：
        当前情绪 的 愉悦 为 当前情绪 的 愉悦 加 300。
        当前情绪 的 共鸣度 为 当前情绪 的 共鸣度 加 200。
    完。
    若 意图 等于 意图_创作：
        当前情绪 的 愉悦 为 当前情绪 的 愉悦 加 400。
        当前情绪 的 激活 为 当前情绪 的 激活 加 300。
    完。
    若 意图 等于 意图_取消：
        当前情绪 的 愉悦 为 当前情绪 的 愉悦 减 100。
    完。
    若 意图 等于 意图_未知：
        当前情绪 的 信任 为 当前情绪 的 信任 减 50。
        若 当前情绪 的 信任 小于 0：
            当前情绪 的 信任 为 0。
        完。
    完。
    若 当前情绪 的 愉悦 大于 Q_ONE：
        当前情绪 的 愉悦 为 Q_ONE。
    完。
    若 当前情绪 的 愉悦 小于 负 Q_ONE：
        当前情绪 的 愉悦 为 负 Q_ONE。
    完。
    若 当前情绪 的 激活 大于 Q_ONE：
        当前情绪 的 激活 为 Q_ONE。
    完。
完。

情绪_获取 接受 零参：
    返还 当前情绪。
完。

手势_输入帧 接受 关节、时间戳：
    idx 为 手势轨迹头 取余于 手势_轨迹缓冲。
    做 设置 用 手势轨迹、idx、新建 手势帧：关节坐标 为 关节、时间戳 为 时间戳。
    手势轨迹头 为 手势轨迹头 加 1。
    若 手势轨迹计数 小于 手势_轨迹缓冲：
        手势轨迹计数 为 手势轨迹计数 加 1。
    完。
完。

手势_检测 接受 零参：
    若 手势轨迹计数 小于 8：
        返还 手势_无。
    完。
    可变 dx_总 为 0。
    可变 dy_总 为 0。
    可变 i 为 0。
    只要 i 小于 7：
        idx0 为 (手势轨迹头 减 8 加 i 加 手势_轨迹缓冲 乘 10) 取余于 手势_轨迹缓冲。
        idx1 为 (idx0 加 1) 取余于 手势_轨迹缓冲。
        若 做 计数 用 手势轨迹 的 第 idx0 项 的 关节坐标 大于或等于 3 并且 做 计数 用 手势轨迹 的 第 idx1 项 的 关节坐标 大于或等于 3：
            dx_总 为 dx_总 加 (手势轨迹 的 第 idx1 项 的 关节坐标 的 第 0 项 减 手势轨迹 的 第 idx0 项 的 关节坐标 的 第 0 项)。
            dy_总 为 dy_总 加 (手势轨迹 的 第 idx1 项 的 关节坐标 的 第 1 项 减 手势轨迹 的 第 idx0 项 的 关节坐标 的 第 1 项)。
        完。
        i 为 i 加 1。
    完。
    abs_dx 为 做 绝对值 用 dx_总。
    abs_dy 为 做 绝对值 用 dy_总。
    若 abs_dx 大于 500 并且 abs_dx 大于 abs_dy 乘 2：
        若 dx_总 大于 0：
            返还 手势_滑动右。
        完。
        返还 手势_滑动左。
    完。
    若 abs_dy 大于 500 并且 abs_dy 大于 abs_dx 乘 2：
        返还 手势_挥手。
    完。
    返还 手势_无。
完。

手势_响应 接受 助手、手势：
    当前手势 为 手势。
    若 手势 等于 手势_挥手：
        若 助手 的 状态 等于 助手状态_休眠 或者 助手 的 状态 等于 助手状态_待命：
            做 转入_聆听 用 助手。
        完。
    完。
    若 手势 等于 手势_滑动左：
        做 面板_切换 用 负 1。
    完。
    若 手势 等于 手势_滑动右：
        做 面板_切换 用 1。
    完。
完。

粒子_更新 接受 零参：
    粒子_活跃数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 粒子池：
        若 粒子池 的 第 i 项 的 活跃：
            粒子池 的 第 i 项 的 x 为 粒子池 的 第 i 项 的 x 加 粒子池 的 第 i 项 的 vx。
            粒子池 的 第 i 项 的 y 为 粒子池 的 第 i 项 的 y 加 粒子池 的 第 i 项 的 vy。
            粒子池 的 第 i 项 的 z 为 粒子池 的 第 i 项 的 z 加 粒子池 的 第 i 项 的 vz。
            粒子池 的 第 i 项 的 vy 为 粒子池 的 第 i 项 的 vy 减 1。
            粒子池 的 第 i 项 的 生命 为 粒子池 的 第 i 项 的 生命 减 1。
            若 粒子池 的 第 i 项 的 生命 小于或等于 0：
                粒子池 的 第 i 项 的 活跃 为 假。
            否则：
                粒子_活跃数 为 粒子_活跃数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

粒子_发射 接受 x、y、z、vx、vy、vz、生命、颜色、大小：
    可变 i 为 0。
    只要 i 小于 做 计数 用 粒子池：
        若 非 粒子池 的 第 i 项 的 活跃：
            粒子池 的 第 i 项 的 x 为 x。
            粒子池 的 第 i 项 的 y 为 y。
            粒子池 的 第 i 项 的 z 为 z。
            粒子池 的 第 i 项 的 vx 为 vx。
            粒子池 的 第 i 项 的 vy 为 vy。
            粒子池 的 第 i 项 的 vz 为 vz。
            粒子池 的 第 i 项 的 生命 为 生命。
            粒子池 的 第 i 项 的 最大生命 为 生命。
            粒子池 的 第 i 项 的 颜色 为 颜色。
            粒子池 的 第 i 项 的 大小 为 大小。
            粒子池 的 第 i 项 的 活跃 为 真。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

粒子_发射_思考 接受 助手：
    可变 i 为 0。
    只要 i 小于 20：
        做 粒子_发射 用 0、100、0、i 乘 7 取余于 13 减 6、10 加 i 取余于 5、i 乘 3 取余于 7 减 3、60、0x00CCFF、3。
        i 为 i 加 1。
    完。
完。

粒子_发射_呼吸 接受 助手：
    可变 i 为 0。
    只要 i 小于 8：
        做 粒子_发射 用 0、50、0、i 减 4、3、0、120、0x336699、2。
        i 为 i 加 1。
    完。
完。

面板_创建 接受 内容类型、内容、x、y：
    若 做 计数 用 面板列表 大于或等于 最大面板数：
        返还 负 1。
    完。
    id 为 做 计数 用 面板列表。
    做 追加 用 面板列表、新建 全息面板：id 为 id、x 为 x、y 为 y、z 为 0、宽 为 400、高 为 300、内容类型 为 内容类型、内容 为 内容、透明度 为 Q_ONE、可见 为 真。
    返还 id。
完。

面板_切换 接受 方向：
    可变 可见id 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 面板列表：
        若 面板列表 的 第 i 项 的 可见：
            可见id 为 i。
            跳出。
        完。
        i 为 i 加 1。
    完。
    若 可见id 小于 0 或者 做 计数 用 面板列表 小于或等于 1：
        返还。
    完。
    面板列表 的 第 可见id 项 的 可见 为 假。
    下一个 为 (可见id 加 方向 加 做 计数 用 面板列表) 取余于 做 计数 用 面板列表。
    面板列表 的 第 下一个 项 的 可见 为 真。
完。

BASE64_字符表 为 「ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/」。

PCM转base64 接受 音频缓冲：
    若 音频缓冲 等于 空 或者 做 计数 用 音频缓冲 等于 0：
        返还 「」。
    完。
    可变 字节 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 音频缓冲：
        样本 为 音频缓冲 的 第 i 项。
        做 追加 用 字节、样本 位与 0xFF。
        做 追加 用 字节、样本 右移 8 位与 0xFF。
        i 为 i 加 1。
    完。
    可变 部件 为 一组：（空）。
    总 为 做 计数 用 字节。
    i 为 0。
    只要 i 小于 总：
        b0 为 字节 的 第 i 项。
        b1 为 0。
        若 i 加 1 小于 总：
            b1 为 字节 的 第 i 加 1 项。
        完。
        b2 为 0。
        若 i 加 2 小于 总：
            b2 为 字节 的 第 i 加 2 项。
        完。
        做 追加 用 部件、做 字符串取字符 用 BASE64_字符表、b0 右移 2 位与 0x3F。
        做 追加 用 部件、做 字符串取字符 用 BASE64_字符表、(b0 位与 3) 左移 4 位或 b1 右移 4 位与 0x0F。
        若 i 加 1 小于 总：
            做 追加 用 部件、做 字符串取字符 用 BASE64_字符表、(b1 位与 0x0F) 左移 2 位或 b2 右移 6 位与 3。
        否则：
            做 追加 用 部件、「=」。
        完。
        若 i 加 2 小于 总：
            做 追加 用 部件、做 字符串取字符 用 BASE64_字符表、b2 位与 0x3F。
        否则：
            做 追加 用 部件、「=」。
        完。
        i 为 i 加 3。
    完。
    返还 做 字符串_连接列表 用 部件、「」。
完。

AI_意图推理 接受 nlp：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    特征 为 做 Tensor_创建 用 1、8。
    做 Tensor_写 用 特征、0、0、nlp 的 意图 乘 Q10 除以 8。
    做 Tensor_写 用 特征、0、1、nlp 的 置信度。
    做 Tensor_写 用 特征、0、2、做 计数 用 nlp 的 槽位列表 乘 Q10 除以 槽位最大数。
    做 Tensor_写 用 特征、0、3、当前情绪 的 愉悦。
    做 Tensor_写 用 特征、0、4、当前情绪 的 激活。
    做 Tensor_写 用 特征、0、5、当前情绪 的 信任。
    做 Tensor_写 用 特征、0、6、EWMA_意图置信度。
    做 Tensor_写 用 特征、0、7、Q_ONE。
    结果 为 做 AI总线_分类 用 bus、特征、8。
    做 Tensor_释放 用 特征。
    返还 结果。
完。

AI_情绪预测 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    特征 为 做 Tensor_创建 用 1、4。
    做 Tensor_写 用 特征、0、0、当前情绪 的 愉悦。
    做 Tensor_写 用 特征、0、1、当前情绪 的 激活。
    做 Tensor_写 用 特征、0、2、当前情绪 的 信任。
    做 Tensor_写 用 特征、0、3、当前情绪 的 共鸣度。
    预测 为 做 AI总线_回归 用 bus、特征。
    做 Tensor_释放 用 特征。
    返还 预测。
完。

文本包含_助手 接受 文本、子串：
    返还 做 字符串_包含 用 文本、子串。
完。

绝对值 接受 x：
    若 x 小于 0：
        返还 负 x。
    完。
    返还 x。
完。

全息助手_完整统计 接受 零参：
    返还 一组：助手_时钟、主助手 的 交互次数、主助手 的 状态、当前情绪 的 愉悦、当前情绪 的 激活、当前情绪 的 信任、当前情绪 的 共鸣度、做 计数 用 面板列表、渲染_帧数、粒子_活跃数、上下文计数、EWMA_响应延迟、EWMA_意图置信度、当前手势。
完。

erf_整数精确 接受 x：
    若 x 大于 4 乘 Q_ONE：
        返还 Q_ONE。
    完。
    若 x 小于 负 4 乘 Q_ONE：
        返还 负 Q_ONE。
    完。
    ax 为 x。
    可变 符号 为 1。
    若 x 小于 0：
        ax 为 0 减 x。
        符号 为 负 1。
    完。
    p 为 335。
    t_denom 为 Q_ONE 加 做 Q乘 用 p、ax 除以 Q_ONE。
    t 为 做 Q除 用 Q_ONE、t_denom。
    a5 为 1087。
    a4 为 负 1488。
    a3 为 1456。
    a2 为 负 291。
    a1 为 261。
    p5 为 a5。
    p4 为 做 Q乘 用 p5、t 加 a4。
    p3 为 做 Q乘 用 p4、t 加 a3。
    p2 为 做 Q乘 用 p3、t 加 a2。
    poly 为 做 Q乘 用 做 Q乘 用 p2、t 加 a1、t。
    x2 为 做 Q乘 用 ax、ax。
    exp_neg 为 做 Q_exp 用 0 减 x2 除以 Q_ONE。
    correction 为 做 Q乘 用 poly、exp_neg 除以 Q_ONE。
    返还 符号 乘 (Q_ONE 减 correction)。
完。

激活_GELU精确 接受 t：
    若 t 等于 0：
        返还。
    完。
    总 为 t 的 行数 乘 t 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        x 为 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        x_scaled 为 做 Q乘 用 x、724。
        erf值 为 做 erf_整数精确 用 x_scaled。
        做 内存_写32 用 t 的 数据 加 i 乘 4、做 Q乘 用 x、Q_ONE 加 erf值 除以 2。
        i 为 i 加 1。
    完。
完。

激活_HexSigmoid_单值 接受 x：
    ax 为 x。
    可变 符号 为 1。
    若 x 小于 0：
        ax 为 0 减 x。
        符号 为 负 1。
    完。
    可变 out 为 0。
    若 ax 大于 6 乘 Q_ONE：
        out 为 Q_ONE。
    否则若 ax 大于 3 乘 Q_ONE：
        out 为 Q_ONE 减 (6 乘 Q_ONE 减 ax) 乘 3 除以 (3 乘 Q_ONE)。
    否则若 ax 大于 Q_ONE：
        out 为 970 减 (3 乘 Q_ONE 减 ax) 乘 82 除以 (2 乘 Q_ONE)。
    否则若 ax 大于 338：
        out 为 731 减 (Q_ONE 减 ax) 乘 238 除以 (Q_ONE 减 338)。
    否则：
        out 为 Q_HALF 加 ax 乘 250 除以 338。
    完。
    若 符号 小于 0：
        out 为 Q_ONE 减 out。
    完。
    返还 out。
完。

激活_HexSigmoid 接受 t：
    若 t 等于 0：
        返还。
    完。
    总 为 t 的 行数 乘 t 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        x 为 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        做 内存_写32 用 t 的 数据 加 i 乘 4、做 激活_HexSigmoid_单值 用 x。
        i 为 i 加 1。
    完。
完。

激活_门控线性 接受 x、门控：
    gate 为 做 激活_HexSigmoid_单值 用 门控。
    返还 做 Q乘 用 x、gate。
完。

前向_门控线性_层 接受 输入、门控张量、输出、n：
    若 输入 等于 0 或者 门控张量 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        x 为 做 内存_读32有符号 用 输入 加 i 乘 4。
        g 为 做 内存_读32有符号 用 门控张量 加 i 乘 4。
        做 内存_写32 用 输出 加 i 乘 4、做 激活_门控线性 用 x、g。
        i 为 i 加 1。
    完。
完。

本章 收束。
