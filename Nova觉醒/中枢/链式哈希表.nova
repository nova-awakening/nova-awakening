《链式哈希表》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·字符串替换》。

引用《.·哈希整数》。

引用《.·链表后继》。

可变 _链哈希_查找次数 为 0。

可变 _链哈希_碰撞次数 为 0。

可变 _链哈希_最大链长 为 0。

链式哈希_创建 接受 桶数：
    若 桶数 小于或等于 0：
        桶数 为 16。
    完。
    可变 实际桶数 为 16。
    只要 实际桶数 小于 桶数：
        实际桶数 为 实际桶数 乘 2。
    完。
    桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 实际桶数：
        做 追加 用 桶、一组：（空）。
        i 为 i 加 1。
    完。
    返还 一组：桶、实际桶数、0。
完。

链式哈希_插入 接受 表、键、值：
    桶 为 表 的 第 0 项。
    桶数 为 表 的 第 1 项。
    若 桶数 小于或等于 0：
        返还 错误_无效参数。
    完。
    索引 为 做 哈希_整数 用 键 取余于 桶数。
    链表 为 桶 的 第 索引 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 链表：
        若 链表 的 第 i 项 的 第 0 项 等于 键：
            做 设置 用 链表 的 第 i 项、1、值。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 链表、一组：键、值。
    做 设置 用 表、2、表 的 第 2 项 加 1。
    链长 为 做 计数 用 链表。
    若 链长 大于 1：
        _链哈希_碰撞次数 为 _链哈希_碰撞次数 加 1。
    完。
    若 链长 大于 _链哈希_最大链长：
        _链哈希_最大链长 为 链长。
    完。
    返还 成功。
完。

链式哈希_查找 接受 表、键：
    _链哈希_查找次数 为 _链哈希_查找次数 加 1。
    桶 为 表 的 第 0 项。
    桶数 为 表 的 第 1 项。
    若 桶数 小于或等于 0：
        返还 空。
    完。
    索引 为 做 哈希_整数 用 键 取余于 桶数。
    链表 为 桶 的 第 索引 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 链表：
        若 链表 的 第 i 项 的 第 0 项 等于 键：
            返还 链表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

链式哈希_删除 接受 表、键：
    桶 为 表 的 第 0 项。
    桶数 为 表 的 第 1 项。
    若 桶数 小于或等于 0：
        返还。
    完。
    索引 为 做 哈希_整数 用 键 取余于 桶数。
    链表 为 桶 的 第 索引 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 链表：
        若 链表 的 第 i 项 的 第 0 项 等于 键：
            做 链表_删除 用 链表、i。
            做 设置 用 表、2、表 的 第 2 项 减 1。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    返还 错误_不存在。
完。

链式哈希_遍历 接受 表、回调：
    桶 为 表 的 第 0 项。
    桶数 为 表 的 第 1 项。
    可变 b 为 0。
    只要 b 小于 桶数：
        链表 为 桶 的 第 b 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 链表：
            做 回调 用 链表 的 第 i 项 的 第 0 项、链表 的 第 i 项 的 第 1 项。
            i 为 i 加 1。
        完。
        b 为 b 加 1。
    完。
完。

链式哈希_大小 接受 表：
    返还 表 的 第 2 项。
完。

链式哈希_负载因子 接受 表：
    若 表 的 第 1 项 小于或等于 0：
        返还 0。
    完。
    返还 表 的 第 2 项 乘 1000 除以 表 的 第 1 项。
完。

链式哈希_碰撞诊断 接受 表：
    桶 为 表 的 第 0 项。
    桶数 为 表 的 第 1 项。
    可变 非空桶 为 0。
    可变 最长链 为 0。
    可变 总链长 为 0。
    可变 b 为 0。
    只要 b 小于 桶数：
        长度 为 做 计数 用 桶 的 第 b 项。
        若 长度 大于 0：
            非空桶 为 非空桶 加 1。
            总链长 为 总链长 加 长度。
            若 长度 大于 最长链：
                最长链 为 长度。
            完。
        完。
        b 为 b 加 1。
    完。
    平均链长 为 0。
    若 非空桶 大于 0：
        平均链长 为 总链长 乘 1000 除以 非空桶。
    完。
    负载 为 做 链式哈希_负载因子 用 表。
    退化阈值 为 负载 乘 2 除以 1000 加 4。
    退化 为 最长链 大于 退化阈值。
    返还 一组：非空桶、最长链、平均链长、退化。
完。

链式哈希_建议操作 接受 表：
    负载 为 做 链式哈希_负载因子 用 表。
    若 负载 大于 2000：
        返还 1。
    完。
    若 负载 小于 500 并且 表 的 第 1 项 大于 32：
        返还 负 1。
    完。
    返还 0。
完。

链式哈希_快照 接受 表：
    可变 快照 为 一组：（空）。
    桶 为 表 的 第 0 项。
    桶数 为 表 的 第 1 项。
    可变 b 为 0。
    只要 b 小于 桶数：
        链表 为 桶 的 第 b 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 链表：
            做 追加 用 快照、一组：链表 的 第 i 项 的 第 0 项、链表 的 第 i 项 的 第 1 项。
            i 为 i 加 1。
        完。
        b 为 b 加 1。
    完。
    返还 快照。
完。

链式哈希_完整统计 接受 表：
    诊断 为 做 链式哈希_碰撞诊断 用 表。
    返还 一组：一组：「元素数」、表 的 第 2 项、一组：「桶数」、表 的 第 1 项、一组：「负载因子」、做 链式哈希_负载因子 用 表、一组：「查找次数」、_链哈希_查找次数、一组：「碰撞插入」、_链哈希_碰撞次数、一组：「历史最大链」、_链哈希_最大链长、一组：「当前最长链」、诊断 的 第 1 项、一组：「平均链长」、诊断 的 第 2 项、一组：「哈希退化」、诊断 的 第 3 项。
完。

GOLDEN_RATIO_64 为 0x61C8864680B583EB。

哈希_黄金比例 接受 键、位数：
    返还 键 乘 GOLDEN_RATIO_64 右移 64 减 位数。
完。

哈希_Jenkins 接受 数据、长度：
    可变 h 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        h 为 h 加 数据 的 第 i 项。
        h 为 h 加 (h 左移 10)。
        h 为 h 位异或 h 右移 6。
        i 为 i 加 1。
    完。
    h 为 h 加 (h 左移 3)。
    h 为 h 位异或 h 右移 11。
    h 为 h 加 (h 左移 15)。
    返还 h。
完。

哈希_双重 接受 键、种子：
    返还 做 哈希_整数 用 键 位异或 种子。
完。

成功 为 0。

错误_无效参数 为 0 减 22。

错误_不存在 为 0 减 2。

AI碰撞窗口 为 16。

热桶阈值倍数 为 3。

退化链长阈值 为 8。

链式哈希统计 是 一种 事物，含 插入次数、插入碰撞、查找次数、查找命中、查找未命中、删除次数、遍历次数、快照次数、诊断次数、扩容次数、缩容次数、热桶检测数、热桶分裂数、AI预测次数、AI预测命中、批量操作次数、错误次数。

可变 全局统计 为 新建 链式哈希统计。

可变 链式模块已初始化 为 假。

可变 AI链长历史 为 一组：（空）。

可变 AI链长位置 为 0。

链式哈希模块_初始化 接受 零参：
    若 链式模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI链长历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI碰撞窗口：
        做 追加 用 AI链长历史、0。
        i 为 i 加 1。
    完。
    AI链长位置 为 0。
    链式模块已初始化 为 真。
    做 打印 用 「[链式哈希] 初始化完成」。
    返还 0。
完。

链式哈希模块_销毁 接受 零参：
    若 链式模块已初始化 等于 假：
        返还 0。
    完。
    AI链长历史 为 一组：（空）。
    链式模块已初始化 为 假。
    做 打印 用 「[链式哈希] 已销毁」。
    返还 0。
完。

链式哈希_安全插入 接受 表、键、值：
    若 表 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    全局统计 的 插入次数 为 全局统计 的 插入次数 加 1。
    结果 为 做 链式哈希_插入 用 表、键、值。
    做 AI_记录链长 用 表。
    返还 结果。
完。

链式哈希_安全查找 接受 表、键：
    若 表 等于 空：
        返还 空。
    完。
    全局统计 的 查找次数 为 全局统计 的 查找次数 加 1。
    结果 为 做 链式哈希_查找 用 表、键。
    若 结果 不等于 空：
        全局统计 的 查找命中 为 全局统计 的 查找命中 加 1。
    否则：
        全局统计 的 查找未命中 为 全局统计 的 查找未命中 加 1。
    完。
    返还 结果。
完。

链式哈希_安全删除 接受 表、键：
    若 表 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    全局统计 的 删除次数 为 全局统计 的 删除次数 加 1。
    返还 做 链式哈希_删除 用 表、键。
完。

链式哈希_自动插入 接受 表、键、值：
    若 表 等于 空：
        返还 错误_无效参数。
    完。
    建议 为 做 链式哈希_建议操作 用 表。
    若 建议 等于 1：
        做 链式哈希_扩容 用 表。
        全局统计 的 扩容次数 为 全局统计 的 扩容次数 加 1。
    完。
    返还 做 链式哈希_安全插入 用 表、键、值。
完。

链式哈希_自动删除 接受 表、键：
    若 表 等于 空：
        返还 错误_无效参数。
    完。
    结果 为 做 链式哈希_安全删除 用 表、键。
    建议 为 做 链式哈希_建议操作 用 表。
    若 建议 等于 0 减 1：
        做 链式哈希_缩容 用 表。
        全局统计 的 缩容次数 为 全局统计 的 缩容次数 加 1。
    完。
    返还 结果。
完。

链式哈希_扩容 接受 表：
    若 表 等于 空：
        返还。
    完。
    旧桶 为 表 的 第 0 项。
    旧桶数 为 表 的 第 1 项。
    新桶数 为 旧桶数 乘 2。
    新桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 新桶数：
        做 追加 用 新桶、一组：（空）。
        i 为 i 加 1。
    完。
    做 设置 用 表、0、新桶。
    做 设置 用 表、1、新桶数。
    做 设置 用 表、2、0。
    i 为 0。
    只要 i 小于 旧桶数：
        链表 为 旧桶 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 链表：
            做 链式哈希_插入 用 表、链表 的 第 j 项 的 第 0 项、链表 的 第 j 项 的 第 1 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

链式哈希_缩容 接受 表：
    若 表 等于 空：
        返还。
    完。
    若 表 的 第 1 项 小于或等于 16：
        返还。
    完。
    旧桶 为 表 的 第 0 项。
    旧桶数 为 表 的 第 1 项。
    新桶数 为 旧桶数 除以 2。
    新桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 新桶数：
        做 追加 用 新桶、一组：（空）。
        i 为 i 加 1。
    完。
    做 设置 用 表、0、新桶。
    做 设置 用 表、1、新桶数。
    做 设置 用 表、2、0。
    i 为 0。
    只要 i 小于 旧桶数：
        链表 为 旧桶 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 链表：
            做 链式哈希_插入 用 表、链表 的 第 j 项 的 第 0 项、链表 的 第 j 项 的 第 1 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

链式哈希_检测热桶 接受 表：
    若 表 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 热桶检测数 为 全局统计 的 热桶检测数 加 1。
    桶 为 表 的 第 0 项。
    桶数 为 表 的 第 1 项。
    平均长 为 做 链式哈希_负载因子 用 表 除以 1000。
    若 平均长 小于 1：
        平均长 为 1。
    完。
    可变 热桶列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 桶数：
        链长 为 做 计数 用 桶 的 第 i 项。
        若 链长 大于 平均长 乘 热桶阈值倍数：
            做 追加 用 热桶列表、一组：i、链长。
        完。
        i 为 i 加 1。
    完。
    返还 热桶列表。
完。

链式哈希_包含 接受 表、键：
    返还 做 链式哈希_查找 用 表、键 不等于 空。
完。

链式哈希_更新 接受 表、键、值：
    若 表 等于 空：
        返还 错误_无效参数。
    完。
    桶 为 表 的 第 0 项。
    桶数 为 表 的 第 1 项。
    若 桶数 小于或等于 0：
        返还 错误_无效参数。
    完。
    索引 为 做 哈希_整数 用 键 取余于 桶数。
    链表 为 桶 的 第 索引 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 链表：
        若 链表 的 第 i 项 的 第 0 项 等于 键：
            做 设置 用 链表 的 第 i 项、1、值。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    返还 错误_不存在。
完。

链式哈希_批量插入 接受 表、键值列表：
    若 表 等于 空 或者 键值列表 等于 空：
        返还 0。
    完。
    全局统计 的 批量操作次数 为 全局统计 的 批量操作次数 加 1。
    可变 已插入 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键值列表：
        做 链式哈希_自动插入 用 表、键值列表 的 第 i 项 的 第 0 项、键值列表 的 第 i 项 的 第 1 项。
        已插入 为 已插入 加 1。
        i 为 i 加 1。
    完。
    返还 已插入。
完。

链式哈希_批量查找 接受 表、键列表：
    若 表 等于 空 或者 键列表 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 批量操作次数 为 全局统计 的 批量操作次数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        做 追加 用 结果、一组：键列表 的 第 i 项、做 链式哈希_查找 用 表、键列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

链式哈希_清空 接受 表：
    若 表 等于 空：
        返还。
    完。
    桶 为 表 的 第 0 项。
    桶数 为 表 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 桶数：
        做 设置 用 桶、i、一组：（空）。
        i 为 i 加 1。
    完。
    做 设置 用 表、2、0。
完。

AI_记录链长 接受 表：
    若 表 等于 空：
        返还。
    完。
    诊断 为 做 链式哈希_碰撞诊断 用 表。
    做 设置 用 AI链长历史、AI链长位置、诊断 的 第 1 项。
    AI链长位置 为 (AI链长位置 加 1) 取余于 AI碰撞窗口。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
完。

AI_预测碰撞趋势 接受 零参：
    半 为 AI碰撞窗口 除以 2。
    可变 前半和 为 0。
    可变 后半和 为 0。
    可变 i 为 0。
    只要 i 小于 半：
        前半和 为 前半和 加 AI链长历史 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 i 小于 AI碰撞窗口：
        后半和 为 后半和 加 AI链长历史 的 第 i 项。
        i 为 i 加 1。
    完。
    前均 为 前半和 除以 半。
    后均 为 后半和 除以 半。
    若 后均 大于 前均 加 2：
        全局统计 的 AI预测命中 为 全局统计 的 AI预测命中 加 1。
        返还 「碰撞加剧-建议扩容」。
    完。
    若 后均 小于 前均：
        返还 「碰撞减少」。
    完。
    返还 「碰撞稳定」。
完。

AI_建议操作 接受 表：
    若 表 等于 空：
        返还 「无」。
    完。
    趋势 为 做 AI_预测碰撞趋势。
    若 趋势 等于 「碰撞加剧-建议扩容」：
        返还 「扩容」。
    完。
    负载 为 做 链式哈希_负载因子 用 表。
    若 负载 大于 2000：
        返还 「扩容」。
    完。
    若 负载 小于 300 并且 表 的 第 1 项 大于 32：
        返还 「缩容」。
    完。
    返还 「保持」。
完。

链式哈希_转储 接受 表：
    若 表 等于 空：
        做 打印 用 「[链式哈希] 空表」。
        返还 0。
    完。
    做 打印 用 「[链式哈希] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  元素数: 」、做 转字符串 用 表 的 第 2 项。
    做 打印 用 做 字符串拼接 用 「  桶数: 」、做 转字符串 用 表 的 第 1 项。
    做 打印 用 做 字符串拼接 用 「  负载因子‰: 」、做 转字符串 用 做 链式哈希_负载因子 用 表。
    诊断 为 做 链式哈希_碰撞诊断 用 表。
    做 打印 用 做 字符串拼接 用 「  非空桶: 」、做 转字符串 用 诊断 的 第 0 项。
    做 打印 用 做 字符串拼接 用 「  最长链: 」、做 转字符串 用 诊断 的 第 1 项。
    做 打印 用 做 字符串拼接 用 「  平均链长‰: 」、做 转字符串 用 诊断 的 第 2 项。
    做 打印 用 做 字符串拼接 用 「  退化: 」、做 转字符串 用 诊断 的 第 3 项。
    做 打印 用 做 字符串拼接 用 「  AI趋势: 」、做 AI_预测碰撞趋势。
    返还 0。
完。

链式哈希模块_打印统计 接受 零参：
    做 打印 用 「[链式哈希] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  插入次数: 」、做 转字符串 用 全局统计 的 插入次数。
    做 打印 用 做 字符串拼接 用 「  插入碰撞: 」、做 转字符串 用 全局统计 的 插入碰撞。
    做 打印 用 做 字符串拼接 用 「  查找次数: 」、做 转字符串 用 全局统计 的 查找次数。
    做 打印 用 做 字符串拼接 用 「  查找命中: 」、做 转字符串 用 全局统计 的 查找命中。
    做 打印 用 做 字符串拼接 用 「  查找未命中: 」、做 转字符串 用 全局统计 的 查找未命中。
    做 打印 用 做 字符串拼接 用 「  删除次数: 」、做 转字符串 用 全局统计 的 删除次数。
    做 打印 用 做 字符串拼接 用 「  遍历次数: 」、做 转字符串 用 全局统计 的 遍历次数。
    做 打印 用 做 字符串拼接 用 「  快照次数: 」、做 转字符串 用 全局统计 的 快照次数。
    做 打印 用 做 字符串拼接 用 「  扩容次数: 」、做 转字符串 用 全局统计 的 扩容次数。
    做 打印 用 做 字符串拼接 用 「  缩容次数: 」、做 转字符串 用 全局统计 的 缩容次数。
    做 打印 用 做 字符串拼接 用 「  热桶检测: 」、做 转字符串 用 全局统计 的 热桶检测数。
    做 打印 用 做 字符串拼接 用 「  AI预测: 」、做 转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI命中: 」、做 转字符串 用 全局统计 的 AI预测命中。
    做 打印 用 做 字符串拼接 用 「  批量操作: 」、做 转字符串 用 全局统计 的 批量操作次数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    若 全局统计 的 查找次数 大于 0：
        命中率 为 全局统计 的 查找命中 乘 100 除以 全局统计 的 查找次数。
        做 打印 用 做 字符串拼接 用 「  命中率%: 」、做 转字符串 用 命中率。
    完。
    返还 0。
完。

链式哈希模块_注册到内核 接受 内核上下文：
    若 链式模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[链式哈希] 已注册到内核上下文」。
    返还 0。
完。

链式哈希模块_查询状态 接受 零参：
    若 链式模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

链式哈希模块_健康检查 接受 零参：
    若 链式模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 插入次数 加 全局统计 的 查找次数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
