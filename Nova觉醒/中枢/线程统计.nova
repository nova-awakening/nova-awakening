《线程统计》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《..·执行·平台时间》。

引用《.·时钟中枢》。

线程_就绪 为 0。

线程_运行 为 1。

线程_阻塞 为 2。

线程_终止 为 3。

最大线程本地 为 64。

优先级继承深度 为 8。

线程描述 是 一种 事物，含 标识、组标识、状态、优先级、原始优先级、栈指针、栈大小、本地存储、本地数量、创建时刻、运行总时、阻塞总时、切换次数、持有锁数、等待锁标识、继承链深度。

可变 总创建数 为 0。

可变 总销毁数 为 0。

可变 活跃线程数 为 0。

可变 峰值线程数 为 0。

可变 线程表 为 一组：（空）。

可变 组表 为 一组：（空）。

可变 继承触发数 为 0。

线程_创建 接受 组标识、优先级、栈大小、入口：
    线程 为 新建 线程描述。
    线程 的 标识 为 总创建数。
    线程 的 组标识 为 组标识。
    线程 的 状态 为 线程_就绪。
    线程 的 优先级 为 优先级。
    线程 的 原始优先级 为 优先级。
    线程 的 栈指针 为 做 堆_分配 用 栈大小。
    线程 的 栈大小 为 栈大小。
    线程 的 本地存储 为 做 堆_分配 用 最大线程本地 乘 8。
    线程 的 本地数量 为 0。
    线程 的 创建时刻 为 做 平台_获取高精度时间。
    线程 的 运行总时 为 0。
    线程 的 阻塞总时 为 0。
    线程 的 切换次数 为 0。
    线程 的 持有锁数 为 0。
    线程 的 等待锁标识 为 负 1。
    线程 的 继承链深度 为 0。
    做 追加 用 线程表、线程。
    总创建数 为 总创建数 加 1。
    活跃线程数 为 活跃线程数 加 1。
    若 活跃线程数 大于 峰值线程数：
        峰值线程数 为 活跃线程数。
    完。
    返还 线程 的 标识。
完。

线程_销毁 接受 标识：
    可变 序号 为 0。
    只要 序号 小于 做 计数 用 线程表：
        若 线程表 的 第 序号 项 的 标识 等于 标识：
            线程表 的 第 序号 项 的 状态 为 线程_终止。
            做 堆_释放 用 线程表 的 第 序号 项 的 栈指针。
            做 堆_释放 用 线程表 的 第 序号 项 的 本地存储。
            总销毁数 为 总销毁数 加 1。
            活跃线程数 为 活跃线程数 减 1。
            返还 0。
        完。
        序号 为 序号 加 1。
    完。
    返还 错误_无效参数。
完。

线程_设本地 接受 标识、键、值：
    线程 为 做 线程_查找 用 标识。
    若 线程 等于 0：
        返还 错误_无效参数。
    完。
    若 键 大于或等于 最大线程本地：
        返还 错误_无效参数。
    完。
    做 内存_写64 用 线程 的 本地存储 加 键 乘 8、值。
    若 键 大于或等于 线程 的 本地数量：
        线程 的 本地数量 为 键 加 1。
    完。
    返还 0。
完。

线程_取本地 接受 标识、键：
    线程 为 做 线程_查找 用 标识。
    若 线程 等于 0：
        返还 0。
    完。
    若 键 大于或等于 最大线程本地：
        返还 0。
    完。
    返还 做 内存_读64 用 线程 的 本地存储 加 键 乘 8。
完。

线程_优先级继承 接受 等待线程、持有线程：
    若 等待线程 的 优先级 大于 持有线程 的 优先级：
        返还。
    完。
    若 持有线程 的 继承链深度 大于或等于 优先级继承深度：
        返还。
    完。
    持有线程 的 优先级 为 等待线程 的 优先级。
    持有线程 的 继承链深度 为 持有线程 的 继承链深度 加 1。
    继承触发数 为 继承触发数 加 1。
完。

线程_恢复优先级 接受 标识：
    线程 为 做 线程_查找 用 标识。
    若 线程 等于 0：
        返还。
    完。
    线程 的 优先级 为 线程 的 原始优先级。
    线程 的 继承链深度 为 0。
完。

线程_记录运行 接受 标识、时长：
    线程 为 做 线程_查找 用 标识。
    若 线程 不等于 0：
        线程 的 运行总时 为 线程 的 运行总时 加 时长。
        线程 的 切换次数 为 线程 的 切换次数 加 1。
    完。
完。

线程_记录阻塞 接受 标识、时长：
    线程 为 做 线程_查找 用 标识。
    若 线程 不等于 0：
        线程 的 阻塞总时 为 线程 的 阻塞总时 加 时长。
    完。
完。

线程_查找 接受 标识：
    可变 序号 为 0。
    只要 序号 小于 做 计数 用 线程表：
        若 线程表 的 第 序号 项 的 标识 等于 标识：
            返还 线程表 的 第 序号 项。
        完。
        序号 为 序号 加 1。
    完。
    返还 0。
完。

线程_组内数量 接受 组标识：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 做 计数 用 线程表：
        若 线程表 的 第 序号 项 的 组标识 等于 组标识 并且 线程表 的 第 序号 项 的 状态 不等于 线程_终止：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

线程_统计 接受 零参：
    返还 一组：总创建数、总销毁数、活跃线程数、峰值线程数、继承触发数、做 计数 用 线程表、做 计数 用 组表。
完。

线程_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 活跃线程数 大于 10000：
        做 追加 用 问题、「活跃线程过多: 」 加 做 整数转字符串 用 活跃线程数。
    完。
    泄漏 为 总创建数 减 总销毁数 减 活跃线程数。
    若 泄漏 大于 100：
        做 追加 用 问题、「可能存在线程泄漏: 」 加 做 整数转字符串 用 泄漏。
    完。
    若 继承触发数 大于 总创建数 并且 总创建数 大于 100：
        做 追加 用 问题、「优先级继承过于频繁」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「线程子系统健康」。
    完。
    返还 问题。
完。

线程_详细统计 接受 零参：
    返还 一本字典：总创建 对应 总创建数、总销毁 对应 总销毁数、活跃 对应 活跃线程数、峰值 对应 峰值线程数、继承触发 对应 继承触发数、线程表大小 对应 做 计数 用 线程表、组数 对应 做 计数 用 组表、就绪数 对应 做 线程_按状态统计 用 线程_就绪、运行数 对应 做 线程_按状态统计 用 线程_运行、阻塞数 对应 做 线程_按状态统计 用 线程_阻塞、终止数 对应 做 线程_按状态统计 用 线程_终止。
完。

线程_按状态统计 接受 目标状态：
    可变 数量 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 线程表：
        若 线程表 的 第 i 项 的 状态 等于 目标状态：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

线程_重置统计 接受 零参：
    总创建数 为 0。
    总销毁数 为 0。
    活跃线程数 为 0。
    峰值线程数 为 0。
    继承触发数 为 0。
完。

线程_获取状态名 接受 状态：
    若 状态 等于 线程_就绪：
        返还 「就绪」。
    完。
    若 状态 等于 线程_运行：
        返还 「运行」。
    完。
    若 状态 等于 线程_阻塞：
        返还 「阻塞」。
    完。
    若 状态 等于 线程_终止：
        返还 「终止」。
    完。
    返还 「未知」。
完。

线程_设置状态 接受 标识、新状态：
    线程 为 做 线程_查找 用 标识。
    若 线程 等于 0：
        返还。
    完。
    线程 的 状态 为 新状态。
完。

线程_是活跃 接受 标识：
    线程 为 做 线程_查找 用 标识。
    若 线程 等于 0：
        返还 假。
    完。
    返还 线程 的 状态 不等于 线程_终止。
完。

线程_获取优先级 接受 标识：
    线程 为 做 线程_查找 用 标识。
    若 线程 等于 0：
        返还 0。
    完。
    返还 线程 的 优先级。
完。

线程_设置优先级 接受 标识、优先级：
    线程 为 做 线程_查找 用 标识。
    若 线程 等于 0：
        返还。
    完。
    线程 的 优先级 为 优先级。
    线程 的 原始优先级 为 优先级。
完。

线程_获取运行时间 接受 标识：
    线程 为 做 线程_查找 用 标识。
    若 线程 等于 0：
        返还 0。
    完。
    返还 线程 的 运行总时。
完。

线程_获取阻塞时间 接受 标识：
    线程 为 做 线程_查找 用 标识。
    若 线程 等于 0：
        返还 0。
    完。
    返还 线程 的 阻塞总时。
完。

线程_获取切换次数 接受 标识：
    线程 为 做 线程_查找 用 标识。
    若 线程 等于 0：
        返还 0。
    完。
    返还 线程 的 切换次数。
完。

线程_CPU利用率 接受 标识：
    线程 为 做 线程_查找 用 标识。
    若 线程 等于 0：
        返还 0。
    完。
    总时间 为 线程 的 运行总时 加 线程 的 阻塞总时。
    若 总时间 等于 0：
        返还 0。
    完。
    返还 线程 的 运行总时 乘 100 除以 总时间。
完。

线程_获取活跃列表 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 线程表：
        若 线程表 的 第 i 项 的 状态 不等于 线程_终止：
            做 追加 用 结果、线程表 的 第 i 项 的 标识。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

线程_获取阻塞列表 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 线程表：
        若 线程表 的 第 i 项 的 状态 等于 线程_阻塞：
            做 追加 用 结果、线程表 的 第 i 项 的 标识。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

线程_获取组内列表 接受 组标识：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 线程表：
        若 线程表 的 第 i 项 的 组标识 等于 组标识 并且 线程表 的 第 i 项 的 状态 不等于 线程_终止：
            做 追加 用 结果、线程表 的 第 i 项 的 标识。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

线程_按优先级排序 接受 零参：
    可变 已排序 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 线程表：
        若 线程表 的 第 i 项 的 状态 不等于 线程_终止：
            做 追加 用 已排序、线程表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 已排序。
完。

线程组 是 一种 事物，含 组标识、名称、线程数、最大线程。

可变 下一组标识 为 1。

线程组_创建 接受 名称、最大线程：
    组 为 新建 线程组。
    组 的 组标识 为 下一组标识。
    下一组标识 为 下一组标识 加 1。
    组 的 名称 为 名称。
    组 的 线程数 为 0。
    组 的 最大线程 为 最大线程。
    做 追加 用 组表、组。
    返还 组 的 组标识。
完。

线程组_查找 接受 组标识：
    可变 i 为 0。
    只要 i 小于 做 计数 用 组表：
        若 组表 的 第 i 项 的 组标识 等于 组标识：
            返还 组表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

线程组_数量 接受 零参：
    返还 做 计数 用 组表。
完。

可变 死锁检测_运行次数 为 0。

可变 死锁检测_发现数 为 0。

线程_死锁检测 接受 零参：
    死锁检测_运行次数 为 死锁检测_运行次数 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 线程表：
        若 线程表 的 第 i 项 的 状态 等于 线程_阻塞 并且 线程表 的 第 i 项 的 等待锁标识 大于或等于 0：
            可变 访问过 为 一组：（空）。
            可变 当前 为 i。
            可变 检测到环 为 假。
            只要 当前 大于或等于 0 并且 当前 小于 做 计数 用 线程表 并且 非 检测到环：
                可变 j 为 0。
                只要 j 小于 做 计数 用 访问过：
                    若 访问过 的 第 j 项 等于 当前：
                        检测到环 为 真。
                        跳出。
                    完。
                    j 为 j 加 1。
                完。
                若 非 检测到环：
                    做 追加 用 访问过、当前。
                    锁标识 为 线程表 的 第 当前 项 的 等待锁标识。
                    当前 为 做 线程_查找持有锁的线程 用 锁标识。
                完。
            完。
            若 检测到环：
                死锁检测_发现数 为 死锁检测_发现数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

线程_查找持有锁的线程 接受 锁标识：
    可变 i 为 0。
    只要 i 小于 做 计数 用 线程表：
        若 线程表 的 第 i 项 的 持有锁数 大于 0：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

死锁检测_统计 接受 零参：
    返还 一本字典：运行次数 对应 死锁检测_运行次数、发现数 对应 死锁检测_发现数。
完。

线程_诊断 接受 标识：
    线程 为 做 线程_查找 用 标识。
    若 线程 等于 0：
        返还 「线程不存在」。
    完。
    返还 一本字典：标识 对应 线程 的 标识、组 对应 线程 的 组标识、状态 对应 做 线程_获取状态名 用 线程 的 状态、优先级 对应 线程 的 优先级、原始优先级 对应 线程 的 原始优先级、运行时间 对应 线程 的 运行总时、阻塞时间 对应 线程 的 阻塞总时、切换次数 对应 线程 的 切换次数、CPU利用率 对应 做 线程_CPU利用率 用 标识、持有锁 对应 线程 的 持有锁数、等待锁 对应 线程 的 等待锁标识、继承深度 对应 线程 的 继承链深度、本地变量 对应 线程 的 本地数量。
完。

线程_打印所有 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 线程表：
        若 线程表 的 第 i 项 的 状态 不等于 线程_终止：
            做 内核信息 用 「线程[」 加 做 整数转字符串 用 线程表 的 第 i 项 的 标识 加 「] 」 加 做 线程_获取状态名 用 线程表 的 第 i 项 的 状态 加 「 优先级=」 加 做 整数转字符串 用 线程表 的 第 i 项 的 优先级 加 「 运行=」 加 做 整数转字符串 用 线程表 的 第 i 项 的 运行总时 加 「us」。
        完。
        i 为 i 加 1。
    完。
完。

错误_无效参数 为 负 22。

线程_完整报告 接受 零参：
    返还 一本字典：活跃 对应 活跃线程数、峰值 对应 峰值线程数、创建 对应 总创建数、销毁 对应 总销毁数、继承 对应 继承触发数、死锁检测 对应 死锁检测_发现数。
完。

可变 AI_线程统计_负载窗口 为 一组：（空）。

AI_线程统计_负载预测 接受 当前负载：
    做 追加 用 AI_线程统计_负载窗口、当前负载。
    若 做 计数 用 AI_线程统计_负载窗口 大于 20：
        做 列表_移除 用 AI_线程统计_负载窗口、0。
    完。
    若 做 计数 用 AI_线程统计_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_线程统计_负载窗口。
    趋势 为 AI_线程统计_负载窗口 的 第 n 减 1 项 减 AI_线程统计_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_线程统计_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
