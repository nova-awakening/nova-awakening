《对象头》

引用《..·中枢·系统调用表》。

引用《..·推理·通信渠道》。

引用《.·字符串替换》。

引用《..·执行·底层内存操》。

引用《..·感知·内核直连层》。

引用《.·内存初始化》。

对象头大小 为 16。

头_Map偏移 为 0。

头_哈希偏移 为 4。

头_标记偏移 为 8。

头_年龄偏移 为 9。

头_大小偏移 为 10。

头_转发偏移 为 14。

标记_白色 为 0。

标记_灰色 为 1。

标记_黑色 为 2。

类型_JS对象 为 1。

类型_JS数组 为 2。

类型_JS函数 为 3。

类型_JS字符串 为 4。

类型_JS数字 为 5。

类型_JS布尔 为 6。

类型_JS正则 为 7。

类型_JS日期 为 8。

类型_JS错误 为 9。

类型_JS承诺 为 10。

类型_JS代理 为 11。

类型_JS映射 为 12。

类型_JS集合 为 13。

类型_JS弱映射 为 14。

类型_JS弱集合 为 15。

类型_JS类型数组 为 16。

类型_JS数组缓冲 为 17。

类型_JS生成器 为 18。

类型_JS符号 为 19。

类型_JS大整数 为 20。

类型_内部_Map 为 100。

类型_内部_属性数组 为 101。

类型_内部_元素数组 为 102。

类型_内部_上下文 为 103。

类型_内部_字节码 为 104。

类型_内部_代码 为 105。

MAP_标志_可扩展 为 1。

MAP_标志_有原型 为 2。

MAP_标志_是构造器 为 4。

MAP_标志_字典模式 为 8。

MAP_标志_已冻结 为 16。

MAP_标志_已密封 为 32。

属性_可写 为 1。

属性_可枚举 为 2。

属性_可配置 为 4。

属性_数据属性 为 8。

属性_默认标志 为 7。

MAP表_初始容量 为 256。

MAP表_最大容量 为 65536。

MAP_大小 为 64。

哈希_种子 为 2654435761。

AI_类型缓存_大小 为 64。

Map隐藏类 是 一种 事物，含 实例类型、实例大小、属性数量、属性描述符、原型、构造器、转换表、反向链接、标志、未使用属性数、元素类型、引用计数、AI热度。

属性描述 是 一种 事物，含 键、值偏移、标志、getter、setter。

Map表管理器 是 一种 事物，含 表基址、已用数、容量、空闲链头、哈希种子、哈希计数器、转换缓存、转换缓存大小、AI类型缓存、AI类型缓存游标、AI预测命中数、AI预测总数、统计_Map创建数、统计_Map回收数、统计_转换次数、统计_字典降级次数、统计_哈希碰撞次数。

对象头_获取Map索引 接受 对象：
    若 对象 等于 0 或者 对象 小于 4096：
        返还 0。
    完。
    返还 做 内存_读32 用 对象 加 头_Map偏移。
完。

对象头_设置Map索引 接受 对象、Map索引：
    若 对象 等于 0 或者 对象 小于 4096：
        返还。
    完。
    做 内存_写32 用 对象 加 头_Map偏移、Map索引。
完。

对象头_获取哈希 接受 对象：
    若 对象 等于 0：
        返还 0。
    完。
    返还 做 内存_读32 用 对象 加 头_哈希偏移。
完。

对象头_设置哈希 接受 对象、哈希值：
    若 对象 等于 0：
        返还。
    完。
    做 内存_写32 用 对象 加 头_哈希偏移、哈希值。
完。

对象头_获取标记 接受 对象：
    若 对象 等于 0：
        返还 标记_白色。
    完。
    返还 做 内存_读8 用 对象 加 头_标记偏移。
完。

对象头_设置标记 接受 对象、标记：
    若 对象 等于 0：
        返还。
    完。
    做 内存_写8 用 对象 加 头_标记偏移、标记。
完。

对象头_获取年龄 接受 对象：
    若 对象 等于 0：
        返还 0。
    完。
    返还 做 内存_读8 用 对象 加 头_年龄偏移。
完。

对象头_增加年龄 接受 对象：
    若 对象 等于 0：
        返还。
    完。
    年龄 为 做 内存_读8 用 对象 加 头_年龄偏移。
    若 年龄 小于 255：
        做 内存_写8 用 对象 加 头_年龄偏移、年龄 加 1。
    完。
完。

对象头_获取大小 接受 对象：
    若 对象 等于 0：
        返还 0。
    完。
    返还 做 内存_读32 用 对象 加 头_大小偏移。
完。

对象头_设置大小 接受 对象、大小：
    若 对象 等于 0：
        返还。
    完。
    做 内存_写32 用 对象 加 头_大小偏移、大小。
完。

对象头_获取转发 接受 对象：
    若 对象 等于 0：
        返还 0。
    完。
    返还 做 内存_读16 用 对象 加 头_转发偏移。
完。

对象头_设置转发 接受 对象、目标地址：
    若 对象 等于 0：
        返还。
    完。
    做 内存_写16 用 对象 加 头_转发偏移、目标地址 位与 65535。
完。

对象头_初始化 接受 对象、Map索引、大小：
    若 对象 等于 0：
        返还。
    完。
    做 内存_写32 用 对象 加 头_Map偏移、Map索引。
    做 内存_写32 用 对象 加 头_哈希偏移、0。
    做 内存_写8 用 对象 加 头_标记偏移、标记_白色。
    做 内存_写8 用 对象 加 头_年龄偏移、0。
    做 内存_写32 用 对象 加 头_大小偏移、大小。
    做 内存_写16 用 对象 加 头_转发偏移、0。
完。

Map表_创建 接受 零参：
    可变 管理器 为 做 堆_分配 用 Map表管理器。
    若 管理器 等于 0：
        做 打印 用 「[Map表] 错误: 管理器分配失败」。
        返还 空。
    完。
    管理器 的 容量 为 MAP表_初始容量。
    管理器 的 表基址 为 做 平台_申请内存 用 管理器 的 容量 乘 MAP_大小。
    若 管理器 的 表基址 等于 0：
        做 打印 用 「[Map表] 错误: Map数组分配失败」。
        做 堆_释放 用 管理器。
        返还 空。
    完。
    做 内存_清零 用 管理器 的 表基址、管理器 的 容量 乘 MAP_大小。
    管理器 的 已用数 为 0。
    管理器 的 空闲链头 为 0。
    管理器 的 哈希种子 为 哈希_种子。
    管理器 的 哈希计数器 为 0。
    管理器 的 转换缓存大小 为 64。
    管理器 的 转换缓存 为 做 平台_申请内存 用 管理器 的 转换缓存大小 乘 12。
    若 管理器 的 转换缓存 不等于 0：
        做 内存_清零 用 管理器 的 转换缓存、管理器 的 转换缓存大小 乘 12。
    完。
    管理器 的 AI类型缓存 为 做 平台_申请内存 用 AI_类型缓存_大小 乘 12。
    若 管理器 的 AI类型缓存 不等于 0：
        做 内存_清零 用 管理器 的 AI类型缓存、AI_类型缓存_大小 乘 12。
    完。
    管理器 的 AI类型缓存游标 为 0。
    管理器 的 AI预测命中数 为 0。
    管理器 的 AI预测总数 为 0。
    管理器 的 统计_Map创建数 为 0。
    管理器 的 统计_Map回收数 为 0。
    管理器 的 统计_转换次数 为 0。
    管理器 的 统计_字典降级次数 为 0。
    管理器 的 统计_哈希碰撞次数 为 0。
    做 Map表_创建内置 用 管理器。
    做 打印 用 「[Map表] 初始化完成」。
    做 打印 用 做 字符串拼接 用 「  容量: 」、做 转字符串 用 管理器 的 容量。
    返还 管理器。
完。

Map表_销毁 接受 管理器：
    若 管理器 等于 空：
        返还。
    完。
    若 管理器 的 表基址 不等于 0：
        可变 i 为 0。
        只要 i 小于 管理器 的 已用数：
            Map地址 为 管理器 的 表基址 加 i 乘 MAP_大小。
            描述符 为 做 内存_读64 用 Map地址 加 24。
            若 描述符 不等于 0：
                做 堆_释放 用 描述符。
            完。
            i 为 i 加 1。
        完。
        做 平台_释放内存 用 管理器 的 表基址、管理器 的 容量 乘 MAP_大小。
    完。
    若 管理器 的 转换缓存 不等于 0：
        做 平台_释放内存 用 管理器 的 转换缓存、管理器 的 转换缓存大小 乘 12。
    完。
    若 管理器 的 AI类型缓存 不等于 0：
        做 平台_释放内存 用 管理器 的 AI类型缓存、AI_类型缓存_大小 乘 12。
    完。
    做 打印 用 「[Map表] 已销毁」。
    做 堆_释放 用 管理器。
完。

Map表_创建内置 接受 管理器：
    做 Map表_分配Map 用 管理器、类型_JS对象、对象头大小、0。
    做 Map表_分配Map 用 管理器、类型_JS数组、对象头大小 加 16、0。
    做 Map表_分配Map 用 管理器、类型_JS函数、对象头大小 加 32、0。
    做 Map表_分配Map 用 管理器、类型_JS字符串、对象头大小 加 16、0。
完。

Map表_分配Map 接受 管理器、实例类型、实例大小、属性数：
    若 管理器 等于 空：
        返还 0。
    完。
    若 管理器 的 已用数 大于或等于 管理器 的 容量：
        做 Map表_扩容 用 管理器。
        若 管理器 的 已用数 大于或等于 管理器 的 容量：
            做 打印 用 「[Map表] 错误: 扩容失败, Map表已满」。
            返还 0。
        完。
    完。
    索引 为 管理器 的 已用数。
    Map地址 为 管理器 的 表基址 加 索引 乘 MAP_大小。
    做 内存_写32 用 Map地址、实例类型。
    做 内存_写32 用 Map地址 加 4、实例大小。
    做 内存_写32 用 Map地址 加 8、属性数。
    做 内存_写64 用 Map地址 加 16、0。
    做 内存_写64 用 Map地址 加 24、0。
    做 内存_写64 用 Map地址 加 32、0。
    做 内存_写64 用 Map地址 加 40、0。
    做 内存_写64 用 Map地址 加 48、0。
    做 内存_写32 用 Map地址 加 56、MAP_标志_可扩展。
    做 内存_写32 用 Map地址 加 60、0。
    管理器 的 已用数 为 管理器 的 已用数 加 1。
    管理器 的 统计_Map创建数 为 管理器 的 统计_Map创建数 加 1。
    返还 索引。
完。

Map表_扩容 接受 管理器：
    若 管理器 的 容量 大于或等于 MAP表_最大容量：
        返还。
    完。
    新容量 为 管理器 的 容量 乘 2。
    若 新容量 大于 MAP表_最大容量：
        新容量 为 MAP表_最大容量。
    完。
    新内存 为 做 平台_申请内存 用 新容量 乘 MAP_大小。
    若 新内存 等于 0：
        返还。
    完。
    做 内存_复制 用 新内存、管理器 的 表基址、管理器 的 已用数 乘 MAP_大小。
    做 内存_清零 用 新内存 加 管理器 的 已用数 乘 MAP_大小、(新容量 减 管理器 的 已用数) 乘 MAP_大小。
    做 平台_释放内存 用 管理器 的 表基址、管理器 的 容量 乘 MAP_大小。
    管理器 的 表基址 为 新内存。
    管理器 的 容量 为 新容量。
完。

Map表_获取Map 接受 管理器、索引：
    若 管理器 等于 空 或者 索引 大于或等于 管理器 的 已用数：
        返还 0。
    完。
    返还 管理器 的 表基址 加 索引 乘 MAP_大小。
完。

Map表_获取实例类型 接受 管理器、Map索引：
    Map地址 为 做 Map表_获取Map 用 管理器、Map索引。
    若 Map地址 等于 0：
        返还 0。
    完。
    返还 做 内存_读32 用 Map地址。
完。

Map表_获取实例大小 接受 管理器、Map索引：
    Map地址 为 做 Map表_获取Map 用 管理器、Map索引。
    若 Map地址 等于 0：
        返还 0。
    完。
    返还 做 内存_读32 用 Map地址 加 4。
完。

Map表_添加属性转换 接受 管理器、源Map索引、属性名哈希：
    若 管理器 等于 空：
        返还 源Map索引。
    完。
    管理器 的 AI预测总数 为 管理器 的 AI预测总数 加 1。
    AI预测 为 做 Map表_AI查找转换 用 管理器、源Map索引、属性名哈希。
    若 AI预测 不等于 0：
        管理器 的 AI预测命中数 为 管理器 的 AI预测命中数 加 1。
        返还 AI预测。
    完。
    源Map 为 做 Map表_获取Map 用 管理器、源Map索引。
    若 源Map 等于 0：
        返还 源Map索引。
    完。
    转换表 为 做 内存_读64 用 源Map 加 40。
    若 转换表 不等于 0：
        可变 搜索 为 转换表。
        只要 搜索 不等于 0：
            存储哈希 为 做 内存_读32 用 搜索。
            目标索引 为 做 内存_读32 用 搜索 加 4。
            若 存储哈希 等于 属性名哈希：
                做 Map表_AI记录转换 用 管理器、源Map索引、属性名哈希、目标索引。
                管理器 的 统计_转换次数 为 管理器 的 统计_转换次数 加 1。
                返还 目标索引。
            完。
            搜索 为 做 内存_读64 用 搜索 加 8。
        完。
    完。
    源属性数 为 做 内存_读32 用 源Map 加 8。
    源实例大小 为 做 内存_读32 用 源Map 加 4。
    新Map索引 为 做 Map表_分配Map 用 管理器、做 内存_读32 用 源Map、源实例大小 加 8、源属性数 加 1。
    若 新Map索引 等于 0：
        返还 源Map索引。
    完。
    新Map 为 做 Map表_获取Map 用 管理器、新Map索引。
    若 新Map 不等于 0：
        做 内存_写64 用 新Map 加 48、源Map索引。
        做 内存_写64 用 新Map 加 24、做 内存_读64 用 源Map 加 24。
        做 内存_写32 用 新Map 加 56、做 内存_读32 用 源Map 加 56。
    完。
    节点 为 做 平台_申请内存 用 16。
    若 节点 不等于 0：
        做 内存_写32 用 节点、属性名哈希。
        做 内存_写32 用 节点 加 4、新Map索引。
        做 内存_写64 用 节点 加 8、转换表。
        做 内存_写64 用 源Map 加 40、节点。
    完。
    做 Map表_AI记录转换 用 管理器、源Map索引、属性名哈希、新Map索引。
    管理器 的 统计_转换次数 为 管理器 的 统计_转换次数 加 1。
    返还 新Map索引。
完。

Map表_检查字典降级 接受 管理器、Map索引：
    Map地址 为 做 Map表_获取Map 用 管理器、Map索引。
    若 Map地址 等于 0：
        返还 假。
    完。
    属性数 为 做 内存_读32 用 Map地址 加 8。
    若 属性数 大于 64：
        做 内存_写32 用 Map地址 加 56、做 内存_读32 用 Map地址 加 56 位或 MAP_标志_字典模式。
        管理器 的 统计_字典降级次数 为 管理器 的 统计_字典降级次数 加 1。
        返还 真。
    完。
    返还 假。
完。

对象头_确保哈希 接受 管理器、对象：
    若 对象 等于 0：
        返还 0。
    完。
    现有哈希 为 做 内存_读32 用 对象 加 头_哈希偏移。
    若 现有哈希 不等于 0：
        返还 现有哈希。
    完。
    可变 哈希 为 管理器 的 哈希种子。
    哈希 为 哈希 位异或 对象 位与 4294967295。
    哈希 为 哈希 乘 16777619。
    哈希 为 哈希 位异或 管理器 的 哈希计数器。
    哈希 为 哈希 乘 16777619。
    管理器 的 哈希计数器 为 管理器 的 哈希计数器 加 1。
    若 哈希 等于 0：
        哈希 为 1。
    完。
    若 哈希 小于 0：
        哈希 为 0 减 哈希。
    完。
    做 内存_写32 用 对象 加 头_哈希偏移、哈希。
    返还 哈希。
完。

对象_是JS对象 接受 管理器、对象：
    Map索引 为 做 对象头_获取Map索引 用 对象。
    类型 为 做 Map表_获取实例类型 用 管理器、Map索引。
    返还 类型 等于 类型_JS对象。
完。

对象_是JS数组 接受 管理器、对象：
    Map索引 为 做 对象头_获取Map索引 用 对象。
    类型 为 做 Map表_获取实例类型 用 管理器、Map索引。
    返还 类型 等于 类型_JS数组。
完。

对象_是JS函数 接受 管理器、对象：
    Map索引 为 做 对象头_获取Map索引 用 对象。
    类型 为 做 Map表_获取实例类型 用 管理器、Map索引。
    返还 类型 等于 类型_JS函数。
完。

对象_是JS字符串 接受 管理器、对象：
    Map索引 为 做 对象头_获取Map索引 用 对象。
    类型 为 做 Map表_获取实例类型 用 管理器、Map索引。
    返还 类型 等于 类型_JS字符串。
完。

对象_获取类型名 接受 管理器、对象：
    Map索引 为 做 对象头_获取Map索引 用 对象。
    类型 为 做 Map表_获取实例类型 用 管理器、Map索引。
    若 类型 等于 类型_JS对象：
        返还 「对象」。
    完。
    若 类型 等于 类型_JS数组：
        返还 「数组」。
    完。
    若 类型 等于 类型_JS函数：
        返还 「函数」。
    完。
    若 类型 等于 类型_JS字符串：
        返还 「字符串」。
    完。
    若 类型 等于 类型_JS数字：
        返还 「数字」。
    完。
    若 类型 等于 类型_JS布尔：
        返还 「布尔」。
    完。
    若 类型 等于 类型_JS承诺：
        返还 「承诺」。
    完。
    若 类型 等于 类型_JS映射：
        返还 「映射」。
    完。
    若 类型 等于 类型_JS集合：
        返还 「集合」。
    完。
    返还 「未知」。
完。

Map表_AI记录转换 接受 管理器、源Map、属性哈希、目标Map：
    若 管理器 等于 空 或者 管理器 的 AI类型缓存 等于 0：
        返还。
    完。
    位置 为 管理器 的 AI类型缓存游标 取余于 AI_类型缓存_大小。
    基址 为 管理器 的 AI类型缓存 加 位置 乘 12。
    做 内存_写32 用 基址、源Map。
    做 内存_写32 用 基址 加 4、属性哈希。
    做 内存_写32 用 基址 加 8、目标Map。
    管理器 的 AI类型缓存游标 为 管理器 的 AI类型缓存游标 加 1。
完。

Map表_AI查找转换 接受 管理器、源Map、属性哈希：
    若 管理器 等于 空 或者 管理器 的 AI类型缓存 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 AI_类型缓存_大小：
        基址 为 管理器 的 AI类型缓存 加 i 乘 12。
        若 做 内存_读32 用 基址 等于 源Map 并且 做 内存_读32 用 基址 加 4 等于 属性哈希：
            返还 做 内存_读32 用 基址 加 8。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

Map表_冻结对象 接受 管理器、Map索引：
    Map地址 为 做 Map表_获取Map 用 管理器、Map索引。
    若 Map地址 等于 0：
        返还。
    完。
    标志 为 做 内存_读32 用 Map地址 加 56。
    标志 为 标志 位或 MAP_标志_已冻结。
    标志 为 标志 位与 按位取反 MAP_标志_可扩展。
    做 内存_写32 用 Map地址 加 56、标志。
完。

Map表_密封对象 接受 管理器、Map索引：
    Map地址 为 做 Map表_获取Map 用 管理器、Map索引。
    若 Map地址 等于 0：
        返还。
    完。
    标志 为 做 内存_读32 用 Map地址 加 56。
    标志 为 标志 位或 MAP_标志_已密封。
    标志 为 标志 位与 按位取反 MAP_标志_可扩展。
    做 内存_写32 用 Map地址 加 56、标志。
完。

Map表_是否冻结 接受 管理器、Map索引：
    Map地址 为 做 Map表_获取Map 用 管理器、Map索引。
    若 Map地址 等于 0：
        返还 假。
    完。
    返还 做 内存_读32 用 Map地址 加 56 位与 MAP_标志_已冻结 不等于 0。
完。

Map表_是否可扩展 接受 管理器、Map索引：
    Map地址 为 做 Map表_获取Map 用 管理器、Map索引。
    若 Map地址 等于 0：
        返还 假。
    完。
    返还 做 内存_读32 用 Map地址 加 56 位与 MAP_标志_可扩展 不等于 0。
完。

Map表_打印统计 接受 管理器：
    若 管理器 等于 空：
        返还。
    完。
    做 打印 用 「[Map表] ═══════════ 隐藏类统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  Map总数:         」、做 转字符串 用 管理器 的 已用数。
    做 打印 用 做 字符串拼接 用 「  Map容量:         」、做 转字符串 用 管理器 的 容量。
    做 打印 用 做 字符串拼接 用 「  Map创建数:       」、做 转字符串 用 管理器 的 统计_Map创建数。
    做 打印 用 做 字符串拼接 用 「  Map回收数:       」、做 转字符串 用 管理器 的 统计_Map回收数。
    做 打印 用 做 字符串拼接 用 「  转换次数:         」、做 转字符串 用 管理器 的 统计_转换次数。
    做 打印 用 做 字符串拼接 用 「  字典降级次数:     」、做 转字符串 用 管理器 的 统计_字典降级次数。
    做 打印 用 做 字符串拼接 用 「  哈希码总数:       」、做 转字符串 用 管理器 的 哈希计数器。
    做 打印 用 「[Map表] ── AI类型预测 ──」。
    做 打印 用 做 字符串拼接 用 「  AI预测总数:       」、做 转字符串 用 管理器 的 AI预测总数。
    做 打印 用 做 字符串拼接 用 「  AI预测命中数:     」、做 转字符串 用 管理器 的 AI预测命中数。
    若 管理器 的 AI预测总数 大于 0：
        命中率 为 管理器 的 AI预测命中数 乘 10000 除以 管理器 的 AI预测总数。
        做 打印 用 做 字符串拼接 用 「  AI命中率(‱):    」、做 转字符串 用 命中率。
    完。
    做 打印 用 「[Map表] ═══════════════════════════════════════」。
完。

对象头_打印调试 接受 管理器、对象：
    若 对象 等于 0：
        做 打印 用 「[对象头] 空对象」。
        返还。
    完。
    做 打印 用 「[对象头] ── 对象信息 ──」。
    做 打印 用 做 字符串拼接 用 「  地址:     」、做 转字符串 用 对象。
    做 打印 用 做 字符串拼接 用 「  Map索引:  」、做 转字符串 用 做 对象头_获取Map索引 用 对象。
    做 打印 用 做 字符串拼接 用 「  哈希码:   」、做 转字符串 用 做 对象头_获取哈希 用 对象。
    做 打印 用 做 字符串拼接 用 「  GC标记:   」、做 转字符串 用 做 对象头_获取标记 用 对象。
    做 打印 用 做 字符串拼接 用 「  GC年龄:   」、做 转字符串 用 做 对象头_获取年龄 用 对象。
    做 打印 用 做 字符串拼接 用 「  大小:     」、做 转字符串 用 做 对象头_获取大小 用 对象。
    类型名 为 做 对象_获取类型名 用 管理器、对象。
    做 打印 用 做 字符串拼接 用 「  类型:     」、类型名。
完。

本章 收束。
