《张量数学引擎》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

数学_精度 为 1000。

数学_PI 为 3142。

数学_E 为 2718。

数学_PI2 为 6283。

数学_半PI 为 1571。

绝对值 接受 n：
    若 n 小于 0：
        返还 0 减 n。
    完。
    返还 n。
完。

最大值 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

最小值 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

钳制_张量 接受 值、下限、上限：
    若 值 小于 下限：
        返还 下限。
    完。
    若 值 大于 上限：
        返还 上限。
    完。
    返还 值。
完。

符号 接受 n：
    若 n 大于 0：
        返还 1。
    完。
    若 n 小于 0：
        返还 0 减 1。
    完。
    返还 0。
完。

平方根 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

定点平方根 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    返还 做 平方根 用 n 乘 数学_精度。
完。

幂 接受 底数、指数：
    若 指数 等于 0：
        返还 1。
    完。
    若 指数 小于 0：
        返还 0。
    完。
    可变 结果 为 1。
    可变 b 为 底数。
    可变 e 为 指数。
    只要 e 大于 0：
        若 e 取余于 2 等于 1：
            结果 为 结果 乘 b。
        完。
        b 为 b 乘 b。
        e 为 e 除以 2。
    完。
    返还 结果。
完。

定点幂 接受 底数、指数：
    若 指数 等于 0：
        返还 数学_精度。
    完。
    若 底数 等于 0：
        返还 0。
    完。
    若 底数 等于 数学_精度：
        返还 数学_精度。
    完。
    ln_b 为 做 定点_自然对数 用 底数。
    返还 做 定点_指数 用 指数 乘 ln_b 除以 数学_精度。
完。

角度_归一化 接受 角度：
    可变 a 为 角度 取余于 数学_PI2。
    若 a 大于 数学_PI：
        a 为 a 减 数学_PI2。
    完。
    若 a 小于 0 减 数学_PI：
        a 为 a 加 数学_PI2。
    完。
    返还 a。
完。

正弦 接受 角度：
    x 为 做 角度_归一化 用 角度。
    x2 为 x 乘 x 除以 数学_精度。
    可变 项 为 x。
    可变 结果 为 x。
    项 为 0 减 项 乘 x2 除以 数学_精度 除以 6。
    结果 为 结果 加 项。
    项 为 0 减 项 乘 x2 除以 数学_精度 除以 20。
    结果 为 结果 加 项。
    项 为 0 减 项 乘 x2 除以 数学_精度 除以 42。
    结果 为 结果 加 项。
    项 为 0 减 项 乘 x2 除以 数学_精度 除以 72。
    结果 为 结果 加 项。
    返还 结果。
完。

余弦 接受 角度：
    返还 做 正弦 用 角度 加 数学_半PI。
完。

正切 接受 角度：
    c 为 做 余弦 用 角度。
    若 c 等于 0：
        返还 0x7FFFFFFF。
    完。
    返还 做 正弦 用 角度 乘 数学_精度 除以 c。
完。

反正弦 接受 x：
    若 x 大于 数学_精度：
        x 为 数学_精度。
    完。
    若 x 小于 0 减 数学_精度：
        x 为 0 减 数学_精度。
    完。
    x2 为 x 乘 x 除以 数学_精度。
    可变 结果 为 x。
    可变 项 为 x 乘 x2 除以 数学_精度 除以 6。
    结果 为 结果 加 项。
    项 为 项 乘 x2 除以 数学_精度 乘 3 除以 40。
    结果 为 结果 加 项。
    项 为 项 乘 x2 除以 数学_精度 乘 15 除以 336。
    结果 为 结果 加 项。
    全局统计 的 三角函数数 为 全局统计 的 三角函数数 加 1。
    返还 结果。
完。

反余弦 接受 x：
    返还 数学_半PI 减 做 反正弦 用 x。
完。

反正切 接受 x：
    若 做 绝对值 用 x 大于 数学_精度：
        若 x 大于 0：
            返还 数学_半PI 减 数学_精度 乘 数学_精度 除以 x。
        完。
        返还 0 减 数学_半PI 减 数学_精度 乘 数学_精度 除以 x。
    完。
    x2 为 x 乘 x 除以 数学_精度。
    可变 结果 为 x。
    可变 项 为 0 减 x 乘 x2 除以 数学_精度 除以 3。
    结果 为 结果 加 项。
    项 为 0 减 项 乘 x2 除以 数学_精度 乘 3 除以 5。
    结果 为 结果 加 项。
    项 为 0 减 项 乘 x2 除以 数学_精度 乘 5 除以 7。
    结果 为 结果 加 项。
    全局统计 的 三角函数数 为 全局统计 的 三角函数数 加 1。
    返还 结果。
完。

反正切2 接受 y、x：
    若 x 大于 0：
        返还 做 反正切 用 y 乘 数学_精度 除以 x。
    完。
    若 x 小于 0 并且 y 大于或等于 0：
        返还 做 反正切 用 y 乘 数学_精度 除以 x 加 数学_PI。
    完。
    若 x 小于 0 并且 y 小于 0：
        返还 做 反正切 用 y 乘 数学_精度 除以 x 减 数学_PI。
    完。
    若 y 大于 0：
        返还 数学_半PI。
    完。
    若 y 小于 0：
        返还 0 减 数学_半PI。
    完。
    返还 0。
完。

定点_指数 接受 x：
    全局统计 的 指数对数数 为 全局统计 的 指数对数数 加 1。
    可变 结果 为 数学_精度。
    可变 项 为 x。
    结果 为 结果 加 项。
    项 为 项 乘 x 除以 数学_精度 除以 2。
    结果 为 结果 加 项。
    项 为 项 乘 x 除以 数学_精度 除以 3。
    结果 为 结果 加 项。
    项 为 项 乘 x 除以 数学_精度 除以 4。
    结果 为 结果 加 项。
    项 为 项 乘 x 除以 数学_精度 除以 5。
    结果 为 结果 加 项。
    项 为 项 乘 x 除以 数学_精度 除以 6。
    结果 为 结果 加 项。
    返还 结果。
完。

定点_自然对数 接受 x：
    全局统计 的 指数对数数 为 全局统计 的 指数对数数 加 1。
    若 x 小于或等于 0：
        返还 0 减 999999。
    完。
    若 x 等于 数学_精度：
        返还 0。
    完。
    可变 归一 为 x。
    可变 修正 为 0。
    只要 归一 大于 2 乘 数学_精度：
        归一 为 归一 除以 2。
        修正 为 修正 加 693。
    完。
    只要 归一 小于 数学_精度 除以 2：
        归一 为 归一 乘 2。
        修正 为 修正 减 693。
    完。
    分子 为 归一 减 数学_精度。
    分母 为 归一 加 数学_精度。
    若 分母 等于 0：
        返还 修正。
    完。
    t 为 分子 乘 数学_精度 除以 分母。
    t2 为 t 乘 t 除以 数学_精度。
    可变 结果 为 t。
    结果 为 结果 加 t 乘 t2 除以 数学_精度 除以 3。
    结果 为 结果 加 t 乘 t2 除以 数学_精度 乘 t2 除以 数学_精度 除以 5。
    返还 结果 乘 2 加 修正。
完。

定点_对数10 接受 x：
    返还 做 定点_自然对数 用 x 乘 数学_精度 除以 2303。
完。

定点_对数2 接受 x：
    返还 做 定点_自然对数 用 x 乘 数学_精度 除以 693。
完。

双曲正弦 接受 x：
    正 为 做 定点_指数 用 x。
    负 为 做 定点_指数 用 0 减 x。
    返还 (正 减 负) 除以 2。
完。

双曲余弦 接受 x：
    正 为 做 定点_指数 用 x。
    负 为 做 定点_指数 用 0 减 x。
    返还 (正 加 负) 除以 2。
完。

双曲正切 接受 x：
    sh 为 做 双曲正弦 用 x。
    ch 为 做 双曲余弦 用 x。
    若 ch 等于 0：
        返还 数学_精度。
    完。
    返还 sh 乘 数学_精度 除以 ch。
完。

阶乘 接受 n：
    若 n 小于或等于 1：
        返还 1。
    完。
    若 n 大于 20：
        返还 0。
    完。
    可变 结果 为 1。
    可变 i 为 2。
    只要 i 小于或等于 n：
        结果 为 结果 乘 i。
        i 为 i 加 1。
    完。
    全局统计 的 组合运算数 为 全局统计 的 组合运算数 加 1。
    返还 结果。
完。

组合数 接受 n、k：
    若 k 小于 0 或者 k 大于 n：
        返还 0。
    完。
    若 k 等于 0 或者 k 等于 n：
        返还 1。
    完。
    若 k 大于 n 减 k：
        k 为 n 减 k。
    完。
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 k：
        结果 为 结果 乘 (n 减 i) 除以 (i 加 1)。
        i 为 i 加 1。
    完。
    全局统计 的 组合运算数 为 全局统计 的 组合运算数 加 1。
    返还 结果。
完。

排列数 接受 n、k：
    若 k 小于 0 或者 k 大于 n：
        返还 0。
    完。
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 k：
        结果 为 结果 乘 (n 减 i)。
        i 为 i 加 1。
    完。
    返还 结果。
完。

线性插值 接受 x0、y0、x1、y1、x：
    若 x1 等于 x0：
        返还 y0。
    完。
    返还 y0 加 (y1 减 y0) 乘 (x 减 x0) 除以 (x1 减 x0)。
完。

最大公约数 接受 a、b：
    若 a 小于 0：
        a 为 0 减 a。
    完。
    若 b 小于 0：
        b 为 0 减 b。
    完。
    只要 b 不等于 0：
        t 为 b。
        b 为 a 取余于 b。
        a 为 t。
    完。
    返还 a。
完。

最小公倍数 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 0。
    完。
    返还 做 绝对值 用 a 乘 b 除以 做 最大公约数 用 a、b。
完。

是否质数 接受 n：
    若 n 小于 2：
        返还 假。
    完。
    若 n 小于 4：
        返还 真。
    完。
    若 n 取余于 2 等于 0 或者 n 取余于 3 等于 0：
        返还 假。
    完。
    可变 i 为 5。
    只要 i 乘 i 小于或等于 n：
        若 n 取余于 i 等于 0 或者 n 取余于 (i 加 2) 等于 0：
            返还 假。
        完。
        i 为 i 加 6。
    完。
    返还 真。
完。

AI精度窗口 为 16。

可变 AI精度历史 为 一组：（空）。

可变 AI精度位置 为 0。

AI_记录精度 接受 运算类型、误差：
    若 做 计数 用 AI精度历史 小于 AI精度窗口：
        做 追加 用 AI精度历史、一组：运算类型、误差。
    否则：
        做 设置 用 AI精度历史、AI精度位置、一组：运算类型、误差。
    完。
    AI精度位置 为 (AI精度位置 加 1) 取余于 AI精度窗口。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
完。

AI_建议精度 接受 零参：
    可变 总误差 为 0。
    可变 有效 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI精度历史：
        若 AI精度历史 的 第 i 项 的 第 1 项 大于 0：
            总误差 为 总误差 加 AI精度历史 的 第 i 项 的 第 1 项。
            有效 为 有效 加 1。
        完。
        i 为 i 加 1。
    完。
    若 有效 等于 0：
        返还 「当前精度1000足够」。
    完。
    均误差 为 总误差 除以 有效。
    若 均误差 大于 100：
        返还 「建议提升精度到10000」。
    完。
    若 均误差 大于 10：
        返还 「当前精度基本可用」。
    完。
    返还 「精度优秀」。
完。

基础数学统计 是 一种 事物，含 绝对值计算数、平方根计算数、幂运算数、三角函数数、指数对数数、双曲函数数、组合运算数、插值计算数、AI预测次数、AI预测命中、最大输入值、溢出次数、批量运算数、精度不足数、错误次数。

可变 全局统计 为 新建 基础数学统计。

可变 基础数学已初始化 为 假。

基础数学_初始化 接受 零参：
    若 基础数学已初始化 等于 真：
        返还 0 减 17。
    完。
    AI精度历史 为 一组：（空）。
    AI精度位置 为 0。
    基础数学已初始化 为 真。
    做 打印 用 「[基础数学] 初始化完成」。
    返还 0。
完。

基础数学_销毁 接受 零参：
    若 基础数学已初始化 等于 假：
        返还 0。
    完。
    AI精度历史 为 一组：（空）。
    基础数学已初始化 为 假。
    做 打印 用 「[基础数学] 已销毁」。
    返还 0。
完。

基础数学_转储 接受 零参：
    做 打印 用 「[基础数学] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  精度: 」、做 整数转字符串 用 数学_精度。
    做 打印 用 做 字符串拼接 用 「  PI: 」、做 整数转字符串 用 数学_PI。
    做 打印 用 做 字符串拼接 用 「  AI建议: 」、做 AI_建议精度。
    返还 0。
完。

基础数学_打印统计 接受 零参：
    做 打印 用 「[基础数学] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  绝对值: 」、做 整数转字符串 用 全局统计 的 绝对值计算数。
    做 打印 用 做 字符串拼接 用 「  平方根: 」、做 整数转字符串 用 全局统计 的 平方根计算数。
    做 打印 用 做 字符串拼接 用 「  幂运算: 」、做 整数转字符串 用 全局统计 的 幂运算数。
    做 打印 用 做 字符串拼接 用 「  三角函数: 」、做 整数转字符串 用 全局统计 的 三角函数数。
    做 打印 用 做 字符串拼接 用 「  指数对数: 」、做 整数转字符串 用 全局统计 的 指数对数数。
    做 打印 用 做 字符串拼接 用 「  双曲函数: 」、做 整数转字符串 用 全局统计 的 双曲函数数。
    做 打印 用 做 字符串拼接 用 「  组合运算: 」、做 整数转字符串 用 全局统计 的 组合运算数。
    做 打印 用 做 字符串拼接 用 「  插值计算: 」、做 整数转字符串 用 全局统计 的 插值计算数。
    做 打印 用 做 字符串拼接 用 「  AI预测: 」、做 整数转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI命中: 」、做 整数转字符串 用 全局统计 的 AI预测命中。
    做 打印 用 做 字符串拼接 用 「  最大输入: 」、做 整数转字符串 用 全局统计 的 最大输入值。
    做 打印 用 做 字符串拼接 用 「  溢出: 」、做 整数转字符串 用 全局统计 的 溢出次数。
    做 打印 用 做 字符串拼接 用 「  批量运算: 」、做 整数转字符串 用 全局统计 的 批量运算数。
    做 打印 用 做 字符串拼接 用 「  精度不足: 」、做 整数转字符串 用 全局统计 的 精度不足数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 整数转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

基础数学_注册到内核 接受 内核上下文：
    若 基础数学已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[基础数学] 已注册到内核上下文」。
    返还 0。
完。

基础数学_查询状态 接受 零参：
    若 基础数学已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

基础数学_健康检查 接受 零参：
    若 基础数学已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 溢出次数 大于 100：
        返还 假。
    完。
    返还 真。
完。

定点_取整 接受 x：
    若 x 大于或等于 0：
        返还 (x 加 数学_精度 除以 2) 除以 数学_精度 乘 数学_精度。
    完。
    返还 (x 减 数学_精度 除以 2) 除以 数学_精度 乘 数学_精度。
完。

定点_下取整 接受 x：
    若 x 大于或等于 0：
        返还 x 除以 数学_精度 乘 数学_精度。
    完。
    返还 (x 减 数学_精度 加 1) 除以 数学_精度 乘 数学_精度。
完。

定点_上取整 接受 x：
    若 x 大于或等于 0：
        返还 (x 加 数学_精度 减 1) 除以 数学_精度 乘 数学_精度。
    完。
    返还 x 除以 数学_精度 乘 数学_精度。
完。

角度转弧度 接受 角度：
    返还 角度 乘 数学_PI 除以 180。
完。

弧度转角度 接受 弧度：
    返还 弧度 乘 180 除以 数学_PI。
完。

斐波那契 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    若 n 小于或等于 2：
        返还 1。
    完。
    可变 a 为 0。
    可变 b 为 1。
    可变 i 为 2。
    只要 i 小于或等于 n：
        t 为 a 加 b。
        a 为 b。
        b 为 t。
        i 为 i 加 1。
    完。
    返还 b。
完。

欧拉函数 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 结果 为 n。
    可变 p 为 2。
    可变 m 为 n。
    只要 p 乘 p 小于或等于 m：
        若 m 取余于 p 等于 0：
            只要 m 取余于 p 等于 0：
                m 为 m 除以 p。
            完。
            结果 为 结果 减 结果 除以 p。
        完。
        p 为 p 加 1。
    完。
    若 m 大于 1：
        结果 为 结果 减 结果 除以 m。
    完。
    返还 结果。
完。

快速幂模 接受 底数、指数、模数：
    若 模数 小于或等于 0：
        返还 0。
    完。
    可变 结果 为 1。
    可变 b 为 底数 取余于 模数。
    可变 e 为 指数。
    只要 e 大于 0：
        若 e 取余于 2 等于 1：
            结果 为 结果 乘 b 取余于 模数。
        完。
        b 为 b 乘 b 取余于 模数。
        e 为 e 除以 2。
    完。
    返还 结果。
完。

本章 收束。
