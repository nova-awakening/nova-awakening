《公平调度周期》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·公平调度核心》。

引用《.·红黑树删除》。

引用《.·红黑树创建》。

引用《.·进程描述符》。

Nova公平调度_周期 接受 零参：
    若 Nova公平调度进程数 小于或等于 调度_最大延迟因子：
        返还 调度延迟。
    完。
    返还 Nova公平调度进程数 乘 最小粒度。
完。

Nova公平调度_入队 接受 任务：
    若 任务 的 第 T_虚拟运行时间 项 小于 最小虚拟运行时间：
        做 设置 用 任务、T_虚拟运行时间、最小虚拟运行时间。
    完。
    若 任务 的 第 T_deadline 项 等于 0 或者 任务 的 第 T_deadline 项 小于 任务 的 第 T_虚拟运行时间 项：
        虚拟片 为 做 calc_delta_fair 用 任务 的 第 T_时间片 项、任务。
        做 设置 用 任务、T_deadline、任务 的 第 T_虚拟运行时间 项 加 虚拟片。
    完。
    做 红黑树_插入 用 Nova公平调度运行队列、任务 的 第 T_虚拟运行时间 项、任务。
    Nova公平调度进程数 为 Nova公平调度进程数 加 1。
    Nova公平调度总权重 为 Nova公平调度总权重 加 任务 的 第 T_权重 项。
    Nova公平调度_平均Nova虚拟运行时间加权和 为 Nova公平调度_平均Nova虚拟运行时间加权和 加 任务 的 第 T_权重 项 乘 任务 的 第 T_虚拟运行时间 项。
完。

Nova公平调度_出队 接受 任务：
    做 红黑树_删除 用 Nova公平调度运行队列、任务 的 第 T_虚拟运行时间 项。
    Nova公平调度进程数 为 Nova公平调度进程数 减 1。
    Nova公平调度总权重 为 Nova公平调度总权重 减 任务 的 第 T_权重 项。
    若 Nova公平调度总权重 小于 0：
        Nova公平调度总权重 为 0。
    完。
    Nova公平调度_平均Nova虚拟运行时间加权和 为 Nova公平调度_平均Nova虚拟运行时间加权和 减 任务 的 第 T_权重 项 乘 任务 的 第 T_虚拟运行时间 项。
完。

Nova公平调度_更新最小虚拟运行时间_带当前 接受 当前：
    可变 候选 为 最小虚拟运行时间。
    若 当前 不等于 空：
        候选 为 当前 的 第 T_虚拟运行时间 项。
    完。
    最小节点 为 做 红黑树_最小 用 Nova公平调度运行队列。
    若 最小节点 不等于 空：
        树最小 为 最小节点 的 第 RB_键 项。
        若 当前 不等于 空：
            若 树最小 小于 候选：
                候选 为 树最小。
            完。
        否则：
            候选 为 树最小。
        完。
    完。
    若 候选 大于 最小虚拟运行时间：
        最小虚拟运行时间 为 候选。
    完。
完。

Nova公平调度_平均Nova虚拟运行时间 接受 零参：
    若 Nova公平调度总权重 小于或等于 0：
        返还 最小虚拟运行时间。
    完。
    返还 Nova公平调度_平均Nova虚拟运行时间加权和 除以 Nova公平调度总权重。
完。

Nova公平调度_选择下一个 接受 零参：
    若 做 红黑树_为空 用 Nova公平调度运行队列：
        返还 空。
    完。
    结果 为 做 红黑树_删除最小 用 Nova公平调度运行队列。
    若 结果 等于 空：
        返还 空。
    完。
    任务 为 结果 的 第 1 项。
    Nova公平调度进程数 为 Nova公平调度进程数 减 1。
    Nova公平调度总权重 为 Nova公平调度总权重 减 任务 的 第 T_权重 项。
    若 Nova公平调度总权重 小于 0：
        Nova公平调度总权重 为 0。
    完。
    Nova公平调度_平均Nova虚拟运行时间加权和 为 Nova公平调度_平均Nova虚拟运行时间加权和 减 任务 的 第 T_权重 项 乘 任务 的 第 T_虚拟运行时间 项。
    做 进程_设置状态 用 任务、进程_运行。
    Nova公平调度_当前实体 为 任务。
    Nova公平调度次数 为 Nova公平调度次数 加 1。
    Nova公平调度_当前执行时间 为 0。
    若 任务 的 第 T_时间片 项 等于 0：
        做 设置 用 任务、T_时间片、基础时间片。
    完。
    返还 任务。
完。

Nova公平调度_计算时间片 接受 任务：
    若 Nova公平调度进程数 小于或等于 1：
        返还 基础时间片。
    完。
    周期 为 做 Nova公平调度_周期。
    权重 为 任务 的 第 T_权重 项。
    总 为 Nova公平调度总权重 加 权重。
    若 总 小于或等于 0：
        总 为 NICE_0_权重。
    完。
    片 为 周期 乘 权重 除以 总。
    若 片 小于 最小粒度：
        返还 最小粒度。
    完。
    返还 片。
完。

Nova公平调度_tick 接受 当前任务、delta_us：
    若 当前任务 等于 空：
        返还 假。
    完。
    若 当前任务 的 第 T_调度策略 项 不等于 调度_普通 并且 当前任务 的 第 T_调度策略 项 不等于 调度_批处理：
        返还 假。
    完。
    做 Nova公平调度_update_curr 用 当前任务、delta_us。
    时间片 为 做 Nova公平调度_计算时间片 用 当前任务。
    若 Nova公平调度_当前执行时间 大于或等于 时间片：
        返还 真。
    完。
    若 非 做 红黑树_为空 用 Nova公平调度运行队列：
        最左 为 做 红黑树_最小 用 Nova公平调度运行队列。
        若 最左 不等于 空：
            左任务 为 最左 的 第 RB_值 项。
            若 左任务 的 第 T_deadline 项 小于 当前任务 的 第 T_deadline 项：
                差值 为 当前任务 的 第 T_虚拟运行时间 项 减 最左 的 第 RB_键 项。
                若 差值 大于 唤醒粒度：
                    返还 真。
                完。
            完。
        完。
    完。
    返还 假。
完。

Nova公平调度_唤醒抢占检查 接受 当前、唤醒进程：
    若 当前 等于 空 或者 唤醒进程 等于 空：
        返还 假。
    完。
    若 唤醒进程 的 第 T_deadline 项 小于 当前 的 第 T_deadline 项：
        返还 真。
    完。
    差 为 当前 的 第 T_虚拟运行时间 项 减 唤醒进程 的 第 T_虚拟运行时间 项。
    返还 差 大于 唤醒粒度。
完。

Nova公平调度_设置新进程虚拟运行时间 接受 任务：
    若 任务 的 第 T_权重 项 等于 0：
        做 设置 用 任务、T_权重、做 nice_到_权重 用 任务 的 第 T_nice值 项。
    完。
    做 设置 用 任务、T_时间片、基础时间片。
    基准 为 做 Nova公平调度_平均Nova虚拟运行时间。
    若 基准 小于 最小虚拟运行时间：
        基准 为 最小虚拟运行时间。
    完。
    做 设置 用 任务、T_虚拟运行时间、基准。
    虚拟片 为 做 calc_delta_fair 用 任务 的 第 T_时间片 项、任务。
    做 设置 用 任务、T_deadline、基准 加 虚拟片。
完。

Nova公平调度_唤醒放置 接受 任务：
    平均v 为 做 Nova公平调度_平均Nova虚拟运行时间。
    若 任务 的 第 T_vlag 项 不等于 0：
        恢复v 为 平均v 减 任务 的 第 T_vlag 项。
        若 恢复v 小于 最小虚拟运行时间：
            恢复v 为 最小虚拟运行时间。
        完。
        做 设置 用 任务、T_虚拟运行时间、恢复v。
    否则：
        若 任务 的 第 T_虚拟运行时间 项 小于 最小虚拟运行时间：
            做 设置 用 任务、T_虚拟运行时间、最小虚拟运行时间。
        完。
    完。
    虚拟片 为 做 calc_delta_fair 用 任务 的 第 T_时间片 项、任务。
    做 设置 用 任务、T_deadline、任务 的 第 T_虚拟运行时间 项 加 虚拟片。
完。

可变 AI_公平调度调_负载窗口 为 一组：（空）。

AI_公平调度调_负载预测 接受 当前负载：
    做 追加 用 AI_公平调度调_负载窗口、当前负载。
    若 做 计数 用 AI_公平调度调_负载窗口 大于 20：
        做 列表_移除 用 AI_公平调度调_负载窗口、0。
    完。
    若 做 计数 用 AI_公平调度调_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_公平调度调_负载窗口。
    趋势 为 AI_公平调度调_负载窗口 的 第 n 减 1 项 减 AI_公平调度调_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_公平调度调_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
