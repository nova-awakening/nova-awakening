《系统自检》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《.·内核打印》。

引用《.·加密框架》。

引用《.·系统信息》。

自检_通过 为 0。

自检_警告 为 1。

自检_失败 为 2。

自检_跳过 为 3。

自检层_硬件 为 0。

自检层_内存 为 1。

自检层_文件系统 为 2。

自检层_网络 为 3。

自检层_安全 为 4。

自检层_AI 为 5。

自检层_Bundle 为 6。

自检_关键 为 1。

自检_非关键 为 0。

自检项 是 一种 事物，含 名称、层级、关键性、状态、耗时微秒、消息。

可变 _自检项列表 为 一组：（空）。

可变 _自检开始时间 为 0。

可变 _自检总耗时 为 0。

可变 _自检已完成 为 假。

POST_执行 接受 零参：
    _自检开始时间 为 做 读取毫秒。
    _自检项列表 为 一组：（空）。
    _自检已完成 为 假。
    做 内核信息 用 「═══ NovaOS POST 开机自检 ═══」。
    做 POST_检查项 用 「CPU标识」、自检层_硬件、自检_关键、POST_检查CPU。
    做 POST_检查项 用 「内存控制器」、自检层_硬件、自检_关键、POST_检查内存控制器。
    做 POST_检查项 用 「中断控制器」、自检层_硬件、自检_关键、POST_检查中断。
    做 POST_检查项 用 「时钟源」、自检层_硬件、自检_关键、POST_检查时钟。
    做 POST_检查项 用 「物理内存映射」、自检层_内存、自检_关键、POST_检查物理内存。
    做 POST_检查项 用 「页分配器」、自检层_内存、自检_关键、POST_检查页分配。
    做 POST_检查项 用 「堆分配器」、自检层_内存、自检_关键、POST_检查堆。
    做 POST_检查项 用 「内存回收器」、自检层_内存、自检_非关键、POST_检查GC。
    做 POST_检查项 用 「VFS挂载表」、自检层_文件系统、自检_关键、POST_检查VFS。
    做 POST_检查项 用 「根文件系统」、自检层_文件系统、自检_关键、POST_检查根FS。
    做 POST_检查项 用 「日志子系统」、自检层_文件系统、自检_非关键、POST_检查日志。
    做 POST_检查项 用 「网络协议栈」、自检层_网络、自检_非关键、POST_检查网络栈。
    做 POST_检查项 用 「回环接口」、自检层_网络、自检_非关键、POST_检查回环。
    做 POST_检查项 用 「加密框架」、自检层_安全、自检_关键、POST_检查加密。
    做 POST_检查项 用 「安全防护引擎」、自检层_安全、自检_非关键、POST_检查安全引擎。
    做 POST_检查项 用 「AI推理引擎」、自检层_AI、自检_非关键、POST_检查AI引擎。
    做 POST_检查项 用 「神经总线」、自检层_AI、自检_非关键、POST_检查神经总线。
    做 POST_检查项 用 「Bundle加载器」、自检层_Bundle、自检_非关键、POST_检查Bundle。
    _自检总耗时 为 做 读取毫秒 减 _自检开始时间。
    _自检已完成 为 真。
    报告 为 做 POST_生成报告。
    做 内核信息 用 报告。
    若 做 POST_有关键失败：
        做 内核信息 用 「POST: 关键自检项失败, 系统无法继续启动」。
        返还 假。
    完。
    做 内核信息 用 「POST: 自检通过, 系统可以继续启动」。
    返还 真。
完。

POST_检查项 接受 名称、层级、关键性、检查函数：
    开始 为 做 读取毫秒。
    项 为 新建 自检项。
    项 的 名称 为 名称。
    项 的 层级 为 层级。
    项 的 关键性 为 关键性。
    结果 为 做 检查函数。
    项 的 状态 为 结果 的 第 0 项。
    项 的 消息 为 结果 的 第 1 项。
    项 的 耗时微秒 为 (做 读取毫秒 减 开始) 乘 1000。
    做 追加 用 _自检项列表、项。
    标记 为 「  」。
    若 项 的 状态 等于 自检_通过：
        标记 为 「✓ 」。
    完。
    若 项 的 状态 等于 自检_警告：
        标记 为 「⚠ 」。
    完。
    若 项 的 状态 等于 自检_失败：
        标记 为 「✗ 」。
    完。
    若 项 的 状态 等于 自检_跳过：
        标记 为 「- 」。
    完。
    做 内核信息 用 「POST 」 加 标记 加 名称 加 「: 」 加 项 的 消息。
完。

POST_检查CPU 接受 零参：
    返还 一组：自检_通过、「x86-64 OK」。
完。

POST_检查内存控制器 接受 零参：
    总量 为 做 物理内存_总量。
    若 总量 等于 0：
        返还 一组：自检_失败、「无法检测物理内存」。
    完。
    返还 一组：自检_通过、做 整数转字符串 用 总量 除以 1048576 加 「MB」。
完。

POST_检查中断 接受 零参：
    返还 一组：自检_通过、「IDT已加载」。
完。

POST_检查时钟 接受 零参：
    t1 为 做 读取毫秒。
    t2 为 做 读取毫秒。
    若 t2 大于或等于 t1：
        返还 一组：自检_通过、「时钟源单调递增」。
    完。
    返还 一组：自检_警告、「时钟源可能不单调」。
完。

POST_检查物理内存 接受 零参：
    空闲 为 做 物理内存_空闲。
    若 空闲 等于 0：
        返还 一组：自检_失败、「无空闲物理内存」。
    完。
    返还 一组：自检_通过、做 整数转字符串 用 空闲 除以 1048576 加 「MB空闲」。
完。

POST_检查页分配 接受 零参：
    返还 一组：自检_通过、「伙伴系统就绪」。
完。

POST_检查堆 接受 零参：
    返还 一组：自检_通过、「堆分配器就绪」。
完。

POST_检查GC 接受 零参：
    返还 一组：自检_通过、「GC就绪」。
完。

POST_检查VFS 接受 零参：
    返还 一组：自检_通过、「VFS挂载表已初始化」。
完。

POST_检查根FS 接受 零参：
    返还 一组：自检_通过、「根文件系统已挂载」。
完。

POST_检查日志 接受 零参：
    返还 一组：自检_通过、「日志缓冲区就绪」。
完。

POST_检查网络栈 接受 零参：
    返还 一组：自检_通过、「TCP/IP协议栈已注册」。
完。

POST_检查回环 接受 零参：
    返还 一组：自检_通过、「lo 127.0.0.1 UP」。
完。

POST_检查加密 接受 零参：
    做 CSPRNG_确保初始化。
    r 为 做 CSPRNG_生成。
    若 r 等于 0：
        返还 一组：自检_警告、「CSPRNG可能未正确初始化」。
    完。
    返还 一组：自检_通过、「AES-256+SHA-256+HMAC+CSPRNG」。
完。

POST_检查安全引擎 接受 零参：
    返还 一组：自检_通过、「三层防御就绪」。
完。

POST_检查AI引擎 接受 零参：
    返还 一组：自检_通过、「推理引擎就绪」。
完。

POST_检查神经总线 接受 零参：
    返还 一组：自检_通过、「九脑区总线就绪」。
完。

POST_检查Bundle 接受 零参：
    返还 一组：自检_通过、「Bundle加载器就绪」。
完。

POST_有关键失败 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _自检项列表：
        若 _自检项列表 的 第 i 项 的 关键性 等于 自检_关键 并且 _自检项列表 的 第 i 项 的 状态 等于 自检_失败：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

POST_统计 接受 零参：
    可变 通过 为 0。
    可变 警告 为 0。
    可变 失败 为 0。
    可变 跳过 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _自检项列表：
        若 _自检项列表 的 第 i 项 的 状态 等于 自检_通过：
            通过 为 通过 加 1。
        完。
        若 _自检项列表 的 第 i 项 的 状态 等于 自检_警告：
            警告 为 警告 加 1。
        完。
        若 _自检项列表 的 第 i 项 的 状态 等于 自检_失败：
            失败 为 失败 加 1。
        完。
        若 _自检项列表 的 第 i 项 的 状态 等于 自检_跳过：
            跳过 为 跳过 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：通过、警告、失败、跳过。
完。

POST_层名 接受 层：
    若 层 等于 自检层_硬件：
        返还 「硬件」。
    完。
    若 层 等于 自检层_内存：
        返还 「内存」。
    完。
    若 层 等于 自检层_文件系统：
        返还 「文件系统」。
    完。
    若 层 等于 自检层_网络：
        返还 「网络」。
    完。
    若 层 等于 自检层_安全：
        返还 「安全」。
    完。
    若 层 等于 自检层_AI：
        返还 「AI」。
    完。
    若 层 等于 自检层_Bundle：
        返还 「Bundle」。
    完。
    返还 「未知」。
完。

POST_生成报告 接受 零参：
    统计 为 做 POST_统计。
    可变 r 为 「═══ POST自检报告 ═══\n」。
    r 为 r 加 「总项数: 」 加 做 整数转字符串 用 做 计数 用 _自检项列表 加 「\n」。
    r 为 r 加 「通过: 」 加 做 整数转字符串 用 统计 的 第 0 项 加 「  警告: 」 加 做 整数转字符串 用 统计 的 第 1 项。
    r 为 r 加 「  失败: 」 加 做 整数转字符串 用 统计 的 第 2 项 加 「  跳过: 」 加 做 整数转字符串 用 统计 的 第 3 项 加 「\n」。
    r 为 r 加 「总耗时: 」 加 做 整数转字符串 用 _自检总耗时 加 「ms\n」。
    返还 r。
完。

本章 收束。
