《调度核心》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·公平调度时间片》。

引用《.·进程描述符》。

引用《.·公平调度睡眠》。

引用《.·内容可见性》。

引用《.·每处理器》。

引用《.·负载均衡》。

引用《..·感知·内核直连层》。

引用《.·内存初始化》。

引用《.·上下文切换》。

引用《.·负载统计》。

调度_普通 为 0。

调度_实时先进先出 为 1。

调度_实时轮转 为 2。

调度_批量 为 3。

调度_截止期 为 4。

神经_衰减因子 为 922。

神经_基数 为 1024。

均衡周期 为 100。

最大进程画像 为 512。

进程画像 是 一种 事物，含 进程标识、运行均值、阻塞均值、切换频率、响应延迟、行为类型、预测置信。

可变 已初始化 为 假。

可变 需调度 为 假。

可变 禁用深度 为 0。

可变 总次数 为 0。

可变 均衡计数 为 0。

可变 画像表 为 一组：（空）。

可变 画像数 为 0。

可变 空闲进程 为 0。

调度_初始化 接受 零参：
    做 公平调度_初始化。
    画像表 为 做 堆_分配 用 最大进程画像 乘 48。
    画像数 为 0。
    已初始化 为 真。
    需调度 为 假。
    禁用深度 为 0。
    总次数 为 0。
    均衡计数 为 0。
完。

禁止抢占 接受 零参：
    禁用深度 为 禁用深度 加 1。
完。

允许抢占 接受 零参：
    禁用深度 为 禁用深度 减 1。
    若 禁用深度 小于 0：
        禁用深度 为 0。
    完。
    若 禁用深度 等于 0 并且 需调度：
        做 调度。
    完。
完。

标记需调度 接受 零参：
    需调度 为 真。
完。

清除需调度 接受 零参：
    需调度 为 假。
完。

调度 接受 零参：
    若 非 已初始化：
        返还。
    完。
    做 禁止抢占。
    做 清除需调度。
    旧 为 做 获取当前进程。
    若 旧 不等于 0 并且 旧 的 第 T_状态 项 等于 进程_运行：
        做 进程_设置状态 用 旧、进程_就绪。
        做 调度_入队 用 旧。
    完。
    可变 新 为 做 调度_选择下一个。
    若 新 等于 0：
        新 为 空闲进程。
    完。
    若 禁用深度 小于或等于 1 并且 新 不等于 空闲进程：
        替代 为 做 神经调度_决策 用 新、旧。
        若 替代 不等于 0 并且 替代 不等于 新 并且 替代 的 第 T_状态 项 等于 进程_就绪：
            做 调度_入队 用 新。
            新 为 替代。
        完。
        做 神经调度_更新画像 用 旧、新。
    完。
    若 新 不等于 旧：
        总次数 为 总次数 加 1。
        做 上下文切换 用 旧、新。
    完。
    禁用深度 为 禁用深度 减 1。
    若 禁用深度 小于 0：
        禁用深度 为 0。
    完。
完。

调度_tick 接受 增量：
    若 非 已初始化：
        返还。
    完。
    当前 为 做 获取当前进程。
    若 当前 等于 0：
        返还。
    完。
    做 进程_累加时间 用 当前、增量。
    画像 为 做 神经调度_查找画像 用 做 进程_标识 用 当前。
    若 画像 不等于 0：
        画像 的 运行均值 为 (画像 的 运行均值 乘 神经_衰减因子 加 增量 乘 (神经_基数 减 神经_衰减因子)) 除以 神经_基数。
    完。
    若 做 公平调度_需抢占 用 当前、增量：
        做 标记需调度。
    完。
    均衡计数 为 均衡计数 加 1。
    若 均衡计数 大于或等于 均衡周期：
        均衡计数 为 0。
        若 做 负载_需均衡：
            做 触发负载均衡。
        完。
    完。
    若 需调度 并且 禁用深度 等于 0：
        做 调度。
    完。
完。

唤醒 接受 任务：
    若 任务 等于 0：
        返还。
    完。
    若 任务 的 第 T_状态 项 等于 进程_睡眠 或者 任务 的 第 T_状态 项 等于 进程_深睡：
        做 进程_设置状态 用 任务、进程_就绪。
        做 调度_入队 用 任务。
        画像 为 做 神经调度_查找画像 用 做 进程_标识 用 任务。
        若 画像 不等于 0：
            画像 的 响应延迟 为 做 平台_获取高精度时间 减 任务 的 第 T_睡眠时刻 项。
        完。
        若 做 公平调度_唤醒抢占 用 做 获取当前进程、任务：
            做 标记需调度。
        完。
    完。
完。

睡眠 接受 零参：
    当前 为 做 获取当前进程。
    做 进程_设置状态 用 当前、进程_睡眠。
    当前 的 第 T_睡眠时刻 项 为 做 平台_获取高精度时间。
    做 调度。
完。

深度睡眠 接受 通道：
    当前 为 做 获取当前进程。
    做 进程_设置状态 用 当前、进程_深睡。
    做 进程_设等待通道 用 当前、通道。
    当前 的 第 T_睡眠时刻 项 为 做 平台_获取高精度时间。
    做 调度。
完。

让出 接受 零参：
    当前 为 做 获取当前进程。
    若 当前 等于 0：
        返还。
    完。
    做 设置 用 当前、T_虚拟运行、当前 的 第 T_虚拟运行 项 加 做 公平调度_周期。
    做 调度。
完。

设优先级 接受 任务、值：
    若 任务 等于 0：
        返还 错误_无效参数。
    完。
    若 值 小于 负 20：
        值 为 负 20。
    完。
    若 值 大于 19：
        值 为 19。
    完。
    做 进程_设优先 用 任务、值。
    返还 0。
完。

设调度策略 接受 任务、策略、优先级：
    若 任务 等于 0：
        返还 错误_无效参数。
    完。
    若 策略 大于 调度_截止期：
        返还 错误_无效参数。
    完。
    若 任务 的 第 T_状态 项 等于 进程_就绪：
        做 调度_出队 用 任务。
    完。
    做 进程_设策略 用 任务、策略。
    做 设置 用 任务、T_优先级、优先级。
    若 任务 的 第 T_状态 项 等于 进程_就绪：
        做 调度_入队 用 任务。
    完。
    返还 0。
完。

停止 接受 任务：
    若 任务 等于 0：
        返还。
    完。
    做 进程_设置状态 用 任务、进程_停止。
    若 任务 等于 做 获取当前进程：
        做 调度。
    完。
完。

继续 接受 任务：
    若 任务 等于 0：
        返还。
    完。
    若 任务 的 第 T_状态 项 等于 进程_停止：
        做 进程_设置状态 用 任务、进程_就绪。
        做 调度_入队 用 任务。
    完。
完。

神经调度_决策 接受 候选、旧进程：
    候选画像 为 做 神经调度_查找画像 用 做 进程_标识 用 候选。
    若 候选画像 等于 0：
        返还 0。
    完。
    若 候选画像 的 行为类型 等于 1 并且 候选画像 的 阻塞均值 大于 候选画像 的 运行均值 乘 3：
        备选 为 做 调度_选择下一个。
        若 备选 不等于 0：
            备选画像 为 做 神经调度_查找画像 用 做 进程_标识 用 备选。
            若 备选画像 不等于 0 并且 备选画像 的 行为类型 等于 0：
                做 调度_入队 用 候选。
                返还 备选。
            完。
            做 调度_入队 用 备选。
        完。
    完。
    若 候选画像 的 行为类型 等于 2 并且 候选画像 的 响应延迟 大于 5000：
        返还 候选。
    完。
    返还 0。
完。

神经调度_更新画像 接受 旧、新：
    若 旧 不等于 0 并且 旧 不等于 空闲进程：
        画像 为 做 神经调度_获取或创建 用 做 进程_标识 用 旧。
        画像 的 切换频率 为 (画像 的 切换频率 乘 神经_衰减因子 加 神经_基数 乘 (神经_基数 减 神经_衰减因子)) 除以 神经_基数。
        新类型 为 2。
        若 画像 的 运行均值 大于 画像 的 阻塞均值 乘 5 除以 2：
            新类型 为 0。
        否则若 画像 的 阻塞均值 大于 画像 的 运行均值 乘 7 除以 2：
            新类型 为 1。
        完。
        若 新类型 不等于 画像 的 行为类型：
            若 画像 的 预测置信 大于 700：
                画像 的 行为类型 为 新类型。
            完。
        否则：
            画像 的 行为类型 为 新类型。
        完。
    完。
    若 新 不等于 0 并且 新 不等于 空闲进程：
        画像 为 做 神经调度_获取或创建 用 做 进程_标识 用 新。
        画像 的 预测置信 为 (画像 的 预测置信 乘 9 加 神经_基数) 除以 10。
    完。
完。

神经调度_查找画像 接受 标识：
    可变 索引 为 0。
    只要 索引 小于 画像数：
        偏移 为 画像表 加 索引 乘 48。
        若 做 内存_读32 用 偏移 等于 标识：
            返还 偏移。
        完。
        索引 为 索引 加 1。
    完。
    返还 0。
完。

神经调度_获取或创建 接受 标识：
    已有 为 做 神经调度_查找画像 用 标识。
    若 已有 不等于 0：
        返还 已有。
    完。
    若 画像数 大于或等于 最大进程画像：
        可变 最低置信 为 999999。
        可变 最低索引 为 0。
        可变 搜索 为 0。
        只要 搜索 小于 画像数：
            偏移 为 画像表 加 搜索 乘 48。
            若 做 内存_读32 用 偏移 加 40 小于 最低置信：
                最低置信 为 做 内存_读32 用 偏移 加 40。
                最低索引 为 搜索。
            完。
            搜索 为 搜索 加 1。
        完。
        替换偏移 为 画像表 加 最低索引 乘 48。
        做 内存_写32 用 替换偏移、标识。
        做 内存_写32 用 替换偏移 加 4、0。
        做 内存_写32 用 替换偏移 加 8、0。
        做 内存_写32 用 替换偏移 加 12、0。
        做 内存_写32 用 替换偏移 加 16、0。
        做 内存_写32 用 替换偏移 加 20、2。
        做 内存_写32 用 替换偏移 加 40、512。
        返还 替换偏移。
    完。
    新偏移 为 画像表 加 画像数 乘 48。
    做 内存_写32 用 新偏移、标识。
    做 内存_写32 用 新偏移 加 4、0。
    做 内存_写32 用 新偏移 加 8、0。
    做 内存_写32 用 新偏移 加 12、0。
    做 内存_写32 用 新偏移 加 16、0。
    做 内存_写32 用 新偏移 加 20、2。
    做 内存_写32 用 新偏移 加 40、512。
    画像数 为 画像数 加 1。
    返还 新偏移。
完。

调度_统计 接受 零参：
    返还 一组：总次数、做 公平调度_进程数、做 实时_进程数、禁用深度、画像数。
完。

本章 收束。
