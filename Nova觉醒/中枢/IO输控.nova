《IO输控》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·同步原语》。

引用《..·感知·内核直连层》。

引用《.·内核打印》。

引用《.·内存初始化》。

引用《..·执行·底层内存操》。

引用《.·整数转字符串》。

最大输控 为 4。

最大引脚 为 24。

重定向_低 为 0x10。

屏蔽位 为 16。

传递_固定 为 0。

传递_最低 为 1。

触发_边沿 为 0。

触发_电平 为 1。

输控状态 是 一种 事物，含 基地址、引脚数、全局中断起始、已初始化。

重定向项 是 一种 事物，含 向量号、传递模式、目标处理器、触发模式、已屏蔽、触发次数。

可变 输控表 为 0。

可变 输控数 为 0。

可变 重定向表 为 0。

可变 路由总数 为 0。

可变 屏蔽总数 为 0。

输控_初始化 接受 零参：
    输控表 为 做 堆_分配 用 最大输控 乘 24。
    做 内存_清零 用 输控表、最大输控 乘 24。
    输控数 为 0。
    重定向表 为 做 堆_分配 用 最大输控 乘 最大引脚 乘 32。
    做 内存_清零 用 重定向表、最大输控 乘 最大引脚 乘 32。
完。

输控_注册 接受 基地址、引脚数、全局起始：
    若 输控数 大于或等于 最大输控：
        返还 负 1。
    完。
    偏移 为 输控表 加 输控数 乘 24。
    做 内存_写64 用 偏移、基地址。
    做 内存_写32 用 偏移 加 8、引脚数。
    做 内存_写32 用 偏移 加 12、全局起始。
    做 内存_写8 用 偏移 加 16、1。
    索引 为 输控数。
    输控数 为 输控数 加 1。
    可变 引脚 为 0。
    只要 引脚 小于 引脚数：
        做 输控_屏蔽引脚 用 索引、引脚。
        引脚 为 引脚 加 1。
    完。
    返还 索引。
完。

输控_读寄存器 接受 输控索引、寄存器号：
    若 输控索引 大于或等于 输控数：
        返还 0。
    完。
    基 为 做 内存_读64 用 输控表 加 输控索引 乘 24。
    做 内存_写32 用 基、寄存器号。
    返还 做 内存_读32 用 基 加 16。
完。

输控_写寄存器 接受 输控索引、寄存器号、值：
    若 输控索引 大于或等于 输控数：
        返还。
    完。
    基 为 做 内存_读64 用 输控表 加 输控索引 乘 24。
    做 内存_写32 用 基、寄存器号。
    做 内存_写32 用 基 加 16、值。
完。

输控_配置重定向 接受 输控索引、引脚、向量、目标处理器、触发：
    若 输控索引 大于或等于 输控数 或者 引脚 大于或等于 最大引脚：
        返还。
    完。
    路由总数 为 路由总数 加 1。
    低32 为 向量 位或 传递_固定 左移 8。
    若 触发 等于 触发_电平：
        低32 为 低32 位或 1 左移 15。
    完。
    高32 为 目标处理器 左移 24。
    做 输控_写寄存器 用 输控索引、重定向_低 加 引脚 乘 2、低32。
    做 输控_写寄存器 用 输控索引、重定向_低 加 引脚 乘 2 加 1、高32。
    条目偏移 为 重定向表 加 (输控索引 乘 最大引脚 加 引脚) 乘 32。
    做 内存_写32 用 条目偏移、向量。
    做 内存_写32 用 条目偏移 加 4、传递_固定。
    做 内存_写32 用 条目偏移 加 8、目标处理器。
    做 内存_写32 用 条目偏移 加 12、触发。
    做 内存_写8 用 条目偏移 加 16、0。
完。

输控_屏蔽引脚 接受 输控索引、引脚：
    若 输控索引 大于或等于 输控数 或者 引脚 大于或等于 最大引脚：
        返还。
    完。
    屏蔽总数 为 屏蔽总数 加 1。
    低32 为 做 输控_读寄存器 用 输控索引、重定向_低 加 引脚 乘 2。
    低32 为 低32 位或 1 左移 屏蔽位。
    做 输控_写寄存器 用 输控索引、重定向_低 加 引脚 乘 2、低32。
    条目偏移 为 重定向表 加 (输控索引 乘 最大引脚 加 引脚) 乘 32。
    做 内存_写8 用 条目偏移 加 16、1。
完。

输控_取消屏蔽 接受 输控索引、引脚：
    若 输控索引 大于或等于 输控数 或者 引脚 大于或等于 最大引脚：
        返还。
    完。
    低32 为 做 输控_读寄存器 用 输控索引、重定向_低 加 引脚 乘 2。
    低32 为 低32 位与 按位取反 (1 左移 屏蔽位)。
    做 输控_写寄存器 用 输控索引、重定向_低 加 引脚 乘 2、低32。
    条目偏移 为 重定向表 加 (输控索引 乘 最大引脚 加 引脚) 乘 32。
    做 内存_写8 用 条目偏移 加 16、0。
完。

输控_设亲和 接受 输控索引、引脚、新目标：
    若 输控索引 大于或等于 输控数 或者 引脚 大于或等于 最大引脚：
        返还。
    完。
    高32 为 新目标 左移 24。
    做 输控_写寄存器 用 输控索引、重定向_低 加 引脚 乘 2 加 1、高32。
    条目偏移 为 重定向表 加 (输控索引 乘 最大引脚 加 引脚) 乘 32。
    做 内存_写32 用 条目偏移 加 8、新目标。
完。

输控_记录触发 接受 输控索引、引脚：
    若 输控索引 大于或等于 输控数 或者 引脚 大于或等于 最大引脚：
        返还。
    完。
    条目偏移 为 重定向表 加 (输控索引 乘 最大引脚 加 引脚) 乘 32。
    做 内存_写64 用 条目偏移 加 24、做 内存_读64 用 条目偏移 加 24 加 1。
完。

输控_获取向量 接受 输控索引、引脚：
    若 输控索引 大于或等于 输控数 或者 引脚 大于或等于 最大引脚：
        返还 0。
    完。
    返还 做 内存_读32 用 重定向表 加 (输控索引 乘 最大引脚 加 引脚) 乘 32。
完。

输控_是否屏蔽 接受 输控索引、引脚：
    若 输控索引 大于或等于 输控数 或者 引脚 大于或等于 最大引脚：
        返还 真。
    完。
    返还 做 内存_读8 用 重定向表 加 (输控索引 乘 最大引脚 加 引脚) 乘 32 加 16 不等于 0。
完。

输控_总引脚数 接受 零参：
    可变 总 为 0。
    可变 序号 为 0。
    只要 序号 小于 输控数：
        总 为 总 加 做 内存_读32 用 输控表 加 序号 乘 24 加 8。
        序号 为 序号 加 1。
    完。
    返还 总。
完。

输控_完整统计 接受 零参：
    返还 一组：输控数、做 输控_总引脚数、路由总数、屏蔽总数。
完。

MSI_最大向量 为 32。

MSIX_最大表项 为 2048。

MSI_地址基 为 0xFEE00000。

MSI配置 是 一种 事物，含 设备ID、向量基、向量数、已启用、地址、数据。

MSIX表项 是 一种 事物，含 地址低32、地址高32、数据、已屏蔽。

可变 MSI设备表 为 0。

可变 MSI设备数 为 0。

最大MSI设备 为 64。

MSI_初始化 接受 零参：
    MSI设备表 为 做 堆_分配 用 最大MSI设备 乘 32。
    做 内存_清零 用 MSI设备表、最大MSI设备 乘 32。
    MSI设备数 为 0。
完。

MSI_注册设备 接受 设备ID、向量基、向量数：
    若 MSI设备数 大于或等于 最大MSI设备：
        返还 负 1。
    完。
    若 向量数 大于 MSI_最大向量：
        向量数 为 MSI_最大向量。
    完。
    偏移 为 MSI设备表 加 MSI设备数 乘 32。
    做 内存_写32 用 偏移、设备ID。
    做 内存_写32 用 偏移 加 4、向量基。
    做 内存_写32 用 偏移 加 8、向量数。
    做 内存_写8 用 偏移 加 12、1。
    做 内存_写32 用 偏移 加 16、MSI_地址基。
    做 内存_写32 用 偏移 加 20、向量基。
    索引 为 MSI设备数。
    MSI设备数 为 MSI设备数 加 1。
    返还 索引。
完。

MSI_启用 接受 设备索引：
    若 设备索引 大于或等于 MSI设备数：
        返还。
    完。
    做 内存_写8 用 MSI设备表 加 设备索引 乘 32 加 12、1。
完。

MSI_禁用 接受 设备索引：
    若 设备索引 大于或等于 MSI设备数：
        返还。
    完。
    做 内存_写8 用 MSI设备表 加 设备索引 乘 32 加 12、0。
完。

MSI_设置亲和 接受 设备索引、目标CPU：
    若 设备索引 大于或等于 MSI设备数：
        返还。
    完。
    地址 为 MSI_地址基 位或 目标CPU 左移 12。
    做 内存_写32 用 MSI设备表 加 设备索引 乘 32 加 16、地址。
完。

MSI_获取向量 接受 设备索引：
    若 设备索引 大于或等于 MSI设备数：
        返还 0。
    完。
    返还 做 内存_读32 用 MSI设备表 加 设备索引 乘 32 加 4。
完。

策略_固定 为 0。

策略_轮转 为 1。

策略_负载感知 为 2。

可变 默认路由策略 为 策略_固定。

可变 轮转计数器 为 0。

可变 最大CPU数 为 8。

输控_设置路由策略 接受 策略：
    若 策略 大于或等于 0 并且 策略 小于或等于 2：
        默认路由策略 为 策略。
    完。
完。

输控_轮转下一CPU 接受 零参：
    轮转计数器 为 (轮转计数器 加 1) 取余于 最大CPU数。
    返还 轮转计数器。
完。

输控_智能路由 接受 输控索引、引脚：
    若 默认路由策略 等于 策略_轮转：
        目标 为 做 输控_轮转下一CPU。
        做 输控_设亲和 用 输控索引、引脚、目标。
        返还 目标。
    否则若 默认路由策略 等于 策略_负载感知：
        目标 为 做 输控_查找最低负载CPU。
        做 输控_设亲和 用 输控索引、引脚、目标。
        返还 目标。
    完。
    返还 做 内存_读32 用 重定向表 加 (输控索引 乘 最大引脚 加 引脚) 乘 32 加 8。
完。

可变 CPU中断计数表 为 0。

CPU中断负载_初始化 接受 零参：
    CPU中断计数表 为 做 堆_分配 用 最大CPU数 乘 8。
    做 内存_清零 用 CPU中断计数表、最大CPU数 乘 8。
完。

CPU中断负载_记录 接受 CPU编号：
    若 CPU编号 大于或等于 最大CPU数：
        返还。
    完。
    做 内存_写64 用 CPU中断计数表 加 CPU编号 乘 8、做 内存_读64 用 CPU中断计数表 加 CPU编号 乘 8 加 1。
完。

CPU中断负载_获取 接受 CPU编号：
    若 CPU编号 大于或等于 最大CPU数：
        返还 0。
    完。
    返还 做 内存_读64 用 CPU中断计数表 加 CPU编号 乘 8。
完。

输控_查找最低负载CPU 接受 零参：
    可变 最低 为 0xFFFFFFFFFFFFFFFF。
    可变 最佳 为 0。
    可变 i 为 0。
    只要 i 小于 最大CPU数：
        负载 为 做 CPU中断负载_获取 用 i。
        若 负载 小于 最低：
            最低 为 负载。
            最佳 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

风暴_阈值 为 10000。

风暴_窗口 为 1000。

可变 风暴检测表 为 0。

可变 风暴事件数 为 0。

风暴检测_初始化 接受 零参：
    风暴检测表 为 做 堆_分配 用 最大输控 乘 最大引脚 乘 16。
    做 内存_清零 用 风暴检测表、最大输控 乘 最大引脚 乘 16。
    风暴事件数 为 0。
完。

风暴检测_记录 接受 输控索引、引脚、时间戳：
    若 输控索引 大于或等于 最大输控 或者 引脚 大于或等于 最大引脚：
        返还 假。
    完。
    偏移 为 风暴检测表 加 (输控索引 乘 最大引脚 加 引脚) 乘 16。
    上次时间 为 做 内存_读64 用 偏移。
    窗口计数 为 做 内存_读64 用 偏移 加 8。
    若 时间戳 减 上次时间 小于 风暴_窗口：
        窗口计数 为 窗口计数 加 1。
        做 内存_写64 用 偏移 加 8、窗口计数。
        若 窗口计数 大于 风暴_阈值：
            做 输控_屏蔽引脚 用 输控索引、引脚。
            风暴事件数 为 风暴事件数 加 1。
            返还 真。
        完。
    否则：
        做 内存_写64 用 偏移、时间戳。
        做 内存_写64 用 偏移 加 8、1。
    完。
    返还 假。
完。

输控_屏蔽全部 接受 输控索引：
    若 输控索引 大于或等于 输控数：
        返还。
    完。
    引脚数 为 做 内存_读32 用 输控表 加 输控索引 乘 24 加 8。
    可变 引脚 为 0。
    只要 引脚 小于 引脚数：
        做 输控_屏蔽引脚 用 输控索引、引脚。
        引脚 为 引脚 加 1。
    完。
完。

输控_取消屏蔽全部 接受 输控索引：
    若 输控索引 大于或等于 输控数：
        返还。
    完。
    引脚数 为 做 内存_读32 用 输控表 加 输控索引 乘 24 加 8。
    可变 引脚 为 0。
    只要 引脚 小于 引脚数：
        做 输控_取消屏蔽 用 输控索引、引脚。
        引脚 为 引脚 加 1。
    完。
完。

输控_重定向全部到CPU 接受 输控索引、目标CPU：
    若 输控索引 大于或等于 输控数：
        返还。
    完。
    引脚数 为 做 内存_读32 用 输控表 加 输控索引 乘 24 加 8。
    可变 引脚 为 0。
    只要 引脚 小于 引脚数：
        做 输控_设亲和 用 输控索引、引脚、目标CPU。
        引脚 为 引脚 加 1。
    完。
完。

输控_获取最热引脚 接受 输控索引：
    若 输控索引 大于或等于 输控数：
        返还 负 1。
    完。
    引脚数 为 做 内存_读32 用 输控表 加 输控索引 乘 24 加 8。
    可变 最大触发 为 0。
    可变 最热 为 负 1。
    可变 引脚 为 0。
    只要 引脚 小于 引脚数：
        触发 为 做 内存_读64 用 重定向表 加 (输控索引 乘 最大引脚 加 引脚) 乘 32 加 24。
        若 触发 大于 最大触发：
            最大触发 为 触发。
            最热 为 引脚。
        完。
        引脚 为 引脚 加 1。
    完。
    返还 最热。
完。

输控_打印统计 接受 零参：
    统计 为 做 输控_完整统计。
    做 打印行 用 「=== I/O APIC 输控统计 ===」。
    做 打印行 用 「输控数量: 」 加 做 整数转字符串 用 统计 的 第 0 项。
    做 打印行 用 「总引脚数: 」 加 做 整数转字符串 用 统计 的 第 1 项。
    做 打印行 用 「路由总数: 」 加 做 整数转字符串 用 统计 的 第 2 项。
    做 打印行 用 「屏蔽总数: 」 加 做 整数转字符串 用 统计 的 第 3 项。
    做 打印行 用 「风暴事件: 」 加 做 整数转字符串 用 风暴事件数。
    做 打印行 用 「MSI设备数: 」 加 做 整数转字符串 用 MSI设备数。
    做 打印行 用 「路由策略: 」 加 做 整数转字符串 用 默认路由策略。
完。

输控_打印引脚详情 接受 输控索引：
    若 输控索引 大于或等于 输控数：
        返还。
    完。
    引脚数 为 做 内存_读32 用 输控表 加 输控索引 乘 24 加 8。
    做 打印行 用 「=== 输控」 加 做 整数转字符串 用 输控索引 加 「 引脚详情 ===」。
    可变 引脚 为 0。
    只要 引脚 小于 引脚数：
        偏移 为 重定向表 加 (输控索引 乘 最大引脚 加 引脚) 乘 32。
        向量 为 做 内存_读32 用 偏移。
        目标 为 做 内存_读32 用 偏移 加 8。
        屏蔽 为 做 内存_读8 用 偏移 加 16。
        触发 为 做 内存_读64 用 偏移 加 24。
        做 打印行 用 「  引脚」 加 做 整数转字符串 用 引脚 加 「: 向量=」 加 做 整数转字符串 用 向量 加 「 CPU=」 加 做 整数转字符串 用 目标 加 「 屏蔽=」 加 做 整数转字符串 用 屏蔽 加 「 触发=」 加 做 整数转字符串 用 触发。
        引脚 为 引脚 加 1。
    完。
完。

输控_打印CPU负载 接受 零参：
    做 打印行 用 「=== CPU中断负载 ===」。
    可变 i 为 0。
    只要 i 小于 最大CPU数：
        做 打印行 用 「  CPU」 加 做 整数转字符串 用 i 加 「: 」 加 做 整数转字符串 用 做 CPU中断负载_获取 用 i 加 「 次」。
        i 为 i 加 1。
    完。
完。

输控子系统_完整初始化 接受 零参：
    做 输控_初始化。
    做 MSI_初始化。
    做 CPU中断负载_初始化。
    做 风暴检测_初始化。
    默认路由策略 为 策略_固定。
    轮转计数器 为 0。
完。

输控_重置统计 接受 零参：
    路由总数 为 0。
    屏蔽总数 为 0。
    风暴事件数 为 0。
    做 内存_清零 用 CPU中断计数表、最大CPU数 乘 8。
完。

可变 MSIX表基 为 0。

可变 MSIX表项数 为 0。

MSIX_每项大小 为 16。

MSIX_表初始化 接受 最大项数：
    MSIX表基 为 做 堆_分配 用 最大项数 乘 MSIX_每项大小。
    做 内存_清零 用 MSIX表基、最大项数 乘 MSIX_每项大小。
    MSIX表项数 为 0。
完。

MSIX_添加表项 接受 向量、目标CPU：
    若 MSIX表项数 大于或等于 MSIX_最大表项：
        返还 负 1。
    完。
    偏移 为 MSIX表基 加 MSIX表项数 乘 MSIX_每项大小。
    地址 为 MSI_地址基 位或 目标CPU 左移 12。
    做 内存_写32 用 偏移、地址 位与 0xFFFFFFFF。
    做 内存_写32 用 偏移 加 4、0。
    做 内存_写32 用 偏移 加 8、向量。
    做 内存_写8 用 偏移 加 12、0。
    索引 为 MSIX表项数。
    MSIX表项数 为 MSIX表项数 加 1。
    返还 索引。
完。

MSIX_屏蔽表项 接受 索引：
    若 索引 大于或等于 MSIX表项数：
        返还。
    完。
    做 内存_写8 用 MSIX表基 加 索引 乘 MSIX_每项大小 加 12、1。
完。

MSIX_取消屏蔽表项 接受 索引：
    若 索引 大于或等于 MSIX表项数：
        返还。
    完。
    做 内存_写8 用 MSIX表基 加 索引 乘 MSIX_每项大小 加 12、0。
完。

MSIX_设置亲和 接受 索引、目标CPU：
    若 索引 大于或等于 MSIX表项数：
        返还。
    完。
    地址 为 MSI_地址基 位或 目标CPU 左移 12。
    做 内存_写32 用 MSIX表基 加 索引 乘 MSIX_每项大小、地址 位与 0xFFFFFFFF。
完。

优先级_低 为 0。

优先级_中 为 1。

优先级_高 为 2。

优先级_关键 为 3。

可变 引脚优先级表 为 0。

引脚优先级_初始化 接受 零参：
    引脚优先级表 为 做 堆_分配 用 最大输控 乘 最大引脚。
    做 内存_清零 用 引脚优先级表、最大输控 乘 最大引脚。
完。

引脚优先级_设置 接受 输控索引、引脚、优先级：
    若 输控索引 大于或等于 最大输控 或者 引脚 大于或等于 最大引脚：
        返还。
    完。
    做 内存_写8 用 引脚优先级表 加 输控索引 乘 最大引脚 加 引脚、优先级。
完。

引脚优先级_获取 接受 输控索引、引脚：
    若 输控索引 大于或等于 最大输控 或者 引脚 大于或等于 最大引脚：
        返还 0。
    完。
    返还 做 内存_读8 用 引脚优先级表 加 输控索引 乘 最大引脚 加 引脚。
完。

均衡_不平衡阈值 为 500。

输控_自动均衡 接受 零参：
    可变 总负载 为 0。
    可变 i 为 0。
    只要 i 小于 最大CPU数：
        总负载 为 总负载 加 做 CPU中断负载_获取 用 i。
        i 为 i 加 1。
    完。
    平均 为 总负载 除以 最大CPU数。
    可变 调整数 为 0。
    可变 输控索引 为 0。
    只要 输控索引 小于 输控数：
        引脚数 为 做 内存_读32 用 输控表 加 输控索引 乘 24 加 8。
        可变 引脚 为 0。
        只要 引脚 小于 引脚数：
            当前CPU 为 做 内存_读32 用 重定向表 加 (输控索引 乘 最大引脚 加 引脚) 乘 32 加 8。
            当前负载 为 做 CPU中断负载_获取 用 当前CPU。
            若 当前负载 大于 平均 加 均衡_不平衡阈值：
                目标 为 做 输控_查找最低负载CPU。
                做 输控_设亲和 用 输控索引、引脚、目标。
                调整数 为 调整数 加 1。
            完。
            引脚 为 引脚 加 1。
        完。
        输控索引 为 输控索引 加 1。
    完。
    返还 调整数。
完。

可变 AI_中断管理系_风暴阈值 为 10000。

可变 AI_中断管理系_预测IRQ 为 0。

AI_中断管理系_风暴检测 接受 触发频率：
    若 触发频率 大于 AI_中断管理系_风暴阈值：
        AI_中断管理系_风暴阈值 为 AI_中断管理系_风暴阈值 乘 3 除以 2。
        返还 真。
    完。
    返还 假。
完。

AI_中断管理系_自适应合并 接受 当前延迟：
    若 当前延迟 小于 100：
        返还 16。
    完。
    若 当前延迟 小于 500：
        返还 8。
    完。
    返还 1。
完。

BLOCK_SIZE 为 8。

块矩阵乘 接受 A、B、C、m、n、k：
    若 A 等于 0 或者 B 等于 0 或者 C 等于 0：
        返还。
    完。
    可变 ii 为 0。
    只要 ii 小于 m：
        可变 jj 为 0。
        只要 jj 小于 n：
            可变 kk 为 0。
            只要 kk 小于 k：
                i_end 为 ii 加 BLOCK_SIZE。
                若 i_end 大于 m：
                    i_end 为 m。
                完。
                j_end 为 jj 加 BLOCK_SIZE。
                若 j_end 大于 n：
                    j_end 为 n。
                完。
                k_end 为 kk 加 BLOCK_SIZE。
                若 k_end 大于 k：
                    k_end 为 k。
                完。
                可变 i 为 ii。
                只要 i 小于 i_end：
                    可变 j 为 jj。
                    只要 j 小于 j_end：
                        可变 sum 为 做 内存_读32有符号 用 C 加 (i 乘 n 加 j) 乘 4。
                        可变 p 为 kk。
                        只要 p 小于 k_end：
                            a 为 做 内存_读32有符号 用 A 加 (i 乘 k 加 p) 乘 4。
                            b 为 做 内存_读32有符号 用 B 加 (p 乘 n 加 j) 乘 4。
                            sum 为 sum 加 做 Q乘 用 a、b。
                            p 为 p 加 1。
                        完。
                        做 内存_写32 用 C 加 (i 乘 n 加 j) 乘 4、sum。
                        j 为 j 加 1。
                    完。
                    i 为 i 加 1。
                完。
                kk 为 kk 加 BLOCK_SIZE。
            完。
            jj 为 jj 加 BLOCK_SIZE。
        完。
        ii 为 ii 加 BLOCK_SIZE。
    完。
完。

赫布预测调度_步进 接受 队列、当前时刻：
    若 队列 等于 0 或者 队列 的 长度 等于 0：
        返还 负 1。
    完。
    可变 最高分 为 负 32768。
    可变 最优索引 为 0。
    可变 i 为 0。
    只要 i 小于 队列 的 长度：
        任务 为 做 内存_读64 用 队列 的 数据 加 i 乘 8。
        若 任务 等于 0：
            i 为 i 加 1。
            继续。
        完。
        dt 为 当前时刻 减 任务 的 上次执行时刻。
        时序权重 为 做 Q_exp 用 0 减 dt 乘 Q_ONE 除以 20 除以 Q_ONE。
        得分 为 做 Q乘 用 任务 的 优先级、时序权重。
        若 dt 大于 200：
            得分 为 得分 加 dt 除以 10。
        完。
        若 得分 大于 最高分：
            最高分 为 得分。
            最优索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最优索引。
完。

CFS_选择下一任务 接受 就绪队列、队列长度：
    若 就绪队列 等于 0 或者 队列长度 等于 0：
        返还 负 1。
    完。
    可变 最小vruntime 为 2147483647。
    可变 最优 为 0。
    可变 i 为 0。
    只要 i 小于 队列长度：
        t 为 做 内存_读64 用 就绪队列 加 i 乘 8。
        若 t 等于 0 或者 t 的 权重 等于 0：
            i 为 i 加 1。
            继续。
        完。
        vr 为 t 的 已运行时间 乘 Q_ONE 除以 t 的 权重。
        若 vr 小于 最小vruntime：
            最小vruntime 为 vr。
            最优 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最优。
完。

CFS_更新vruntime 接受 任务、运行时长：
    若 任务 等于 0 或者 任务 的 权重 等于 0：
        返还。
    完。
    任务 的 已运行时间 为 任务 的 已运行时间 加 运行时长。
完。

STDP_A正 为 5。

STDP_A负 为 5。

STDP_TAU 为 20。

STDP_权重更新 接受 权重、dt、A正、A负、tau：
    可变 dw 为 0。
    若 dt 大于 0：
        decay 为 做 Q乘 用 A正 乘 Q_ONE 除以 10、做 Q_exp 用 0 减 dt 乘 Q_ONE 除以 tau。
        dw 为 decay 除以 Q_ONE。
    否则若 dt 小于 0：
        decay 为 做 Q乘 用 A负 乘 Q_ONE 除以 10、做 Q_exp 用 dt 乘 Q_ONE 除以 tau。
        dw 为 0 减 decay 除以 Q_ONE。
    完。
    返还 权重 加 dw。
完。

七态_级别数 为 7。

七态_步长 为 Q_ONE 除以 3。

七态量化_编码向量 接受 向量、输出、维度：
    若 向量 等于 0 或者 输出 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 维度：
        v 为 做 Tensor_读 用 向量、i。
        可变 级 为 v 除以 七态_步长。
        若 级 大于 3：
            级 为 3。
        完。
        若 级 小于 负 3：
            级 为 负 3。
        完。
        做 内存_写8 用 输出 加 i、级 加 3。
        i 为 i 加 1。
    完。
完。

七态量化_解码向量 接受 输入、向量、维度：
    若 输入 等于 0 或者 向量 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 维度：
        级 为 做 内存_读8 用 输入 加 i 减 3。
        做 Tensor_写 用 向量、i、级 乘 七态_步长。
        i 为 i 加 1。
    完。
完。

七态权重_矩阵向量乘 接受 量化权重、向量、输出、行、列：
    若 量化权重 等于 0 或者 向量 等于 0：
        返还。
    完。
    可变 r 为 0。
    只要 r 小于 行：
        可变 acc 为 0。
        可变 c 为 0。
        只要 c 小于 列：
            w级 为 做 内存_读8 用 量化权重 加 r 乘 列 加 c 减 3。
            x 为 做 Tensor_读 用 向量、c。
            若 w级 等于 1 或者 w级 等于 负 1：
                acc 为 acc 加 w级 乘 x。
            否则若 w级 等于 2 或者 w级 等于 负 2：
                acc 为 acc 加 w级 乘 x。
            否则若 w级 等于 3 或者 w级 等于 负 3：
                acc 为 acc 加 w级 乘 x。
            完。
            c 为 c 加 1。
        完。
        做 Tensor_写 用 输出、r、acc 除以 Q_ONE。
        r 为 r 加 1。
    完。
完。

BCM_权重更新 接受 权重、输入、输出、阈值：
    y_minus_theta 为 输出 减 阈值。
    dw 为 做 Q乘 用 做 Q乘 用 输出、y_minus_theta、输入 除以 (Q_ONE 乘 Q_ONE)。
    返还 权重 加 dw 除以 10。
完。

BCM_阈值更新 接受 阈值、输出、tau：
    返还 (tau 减 1) 乘 阈值 除以 tau 加 做 Q乘 用 输出、输出 除以 tau 除以 Q_ONE。
完。

NIET_预测能力 接受 中断历史、窗口大小：
    若 中断历史 等于 0 或者 窗口大小 等于 0：
        返还 0。
    完。
    可变 max_freq 为 0。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 窗口大小：
        v 为 做 内存_读32有符号 用 中断历史 加 i 乘 4。
        若 v 大于 max_freq：
            max_freq 为 v。
        完。
        总 为 总 加 v。
        i 为 i 加 1。
    完。
    若 总 等于 0：
        返还 0。
    完。
    返还 max_freq 乘 Q_ONE 除以 总。
完。

七态向量量化_压缩 接受 向量、压缩输出、维度：
    若 向量 等于 0 或者 压缩输出 等于 0 或者 维度 等于 0：
        返还。
    完。
    可变 sum_sq 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        v 为 做 Tensor_读 用 向量、i。
        sum_sq 为 sum_sq 加 做 Q乘 用 v、v。
        i 为 i 加 1。
    完。
    模长 为 做 Q开方 用 sum_sq。
    若 模长 等于 0：
        返还。
    完。
    i 为 0。
    只要 i 小于 维度：
        v 为 做 Tensor_读 用 向量、i。
        方向4 为 v 乘 7 除以 模长。
        若 方向4 大于 7：
            方向4 为 7。
        完。
        若 方向4 小于 负 7：
            方向4 为 负 7。
        完。
        幅值 为 v 乘 Q_ONE 除以 模长。
        若 幅值 小于 0：
            幅值 为 0 减 幅值。
        完。
        幅值4 为 幅值 乘 15 除以 Q_ONE。
        若 幅值4 大于 15：
            幅值4 为 15。
        完。
        编码 为 方向4 加 7 左移 4 位或 幅值4。
        做 内存_写8 用 压缩输出 加 i、编码。
        i 为 i 加 1。
    完。
完。

七态向量量化_解压 接受 压缩输入、向量输出、维度、模长_Q10：
    若 压缩输入 等于 0 或者 向量输出 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 维度：
        编码 为 做 内存_读8 用 压缩输入 加 i。
        方向4 为 (编码 右移 4 位与 15) 减 7。
        幅值4 为 编码 位与 15。
        v 为 方向4 乘 模长_Q10 除以 7 乘 幅值4 除以 15。
        做 Tensor_写 用 向量输出、i、v。
        i 为 i 加 1。
    完。
完。

七态向量量化_内积误差估计 接受 a_模长、b_模长、维度：
    误差_Q10 为 Q_ONE 乘 10 除以 做 Q开方 用 维度 乘 Q_ONE。
    返还 误差_Q10。
完。

PolarQuant_KV_编码块 接受 KV源、KV压缩、元素数：
    若 KV源 等于 0 或者 KV压缩 等于 0 或者 元素数 等于 0：
        返还。
    完。
    可变 sum_sq 为 0。
    可变 i 为 0。
    只要 i 小于 元素数：
        v 为 做 内存_读32有符号 用 KV源 加 i 乘 4。
        sum_sq 为 sum_sq 加 做 Q乘 用 v、v 除以 元素数。
        i 为 i 加 1。
    完。
    模长 为 做 Q开方 用 sum_sq。
    若 模长 等于 0：
        模长 为 1。
    完。
    i 为 0。
    只要 i 小于 元素数：
        v 为 做 内存_读32有符号 用 KV源 加 i 乘 4。
        dir4 为 v 乘 7 除以 模长。
        若 dir4 大于 7：
            dir4 为 7。
        完。
        若 dir4 小于 负 7：
            dir4 为 负 7。
        完。
        mag4 为 v 乘 Q_ONE 除以 模长。
        若 mag4 小于 0：
            mag4 为 0 减 mag4。
        完。
        mag4 为 mag4 乘 15 除以 Q_ONE。
        若 mag4 大于 15：
            mag4 为 15。
        完。
        做 内存_写8 用 KV压缩 加 i、dir4 加 7 左移 4 位或 mag4。
        i 为 i 加 1。
    完。
    做 内存_写32 用 KV压缩 加 元素数、模长。
完。

PolarQuant_KV_解码块 接受 KV压缩、KV输出、元素数：
    若 KV压缩 等于 0 或者 KV输出 等于 0：
        返还。
    完。
    模长 为 做 内存_读32有符号 用 KV压缩 加 元素数。
    可变 i 为 0。
    只要 i 小于 元素数：
        enc 为 做 内存_读8 用 KV压缩 加 i。
        dir4 为 (enc 右移 4 位与 15) 减 7。
        mag4 为 enc 位与 15。
        做 内存_写32 用 KV输出 加 i 乘 4、dir4 乘 模长 除以 7 乘 mag4 除以 15。
        i 为 i 加 1。
    完。
完。

七态_级别数 为 7。

七态_步长 为 Q_ONE 除以 3。

本章 收束。
