《整数转字符串》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·硬链接》。

引用《.·字符串替换》。

引用《..·外联·URL解析辅助》。

整数转字符串 接受 数值：
    若 数值 等于 0：
        返还 「0」。
    完。
    可变 结果 为 「」。
    可变 n 为 数值。
    可变 负数 为 假。
    若 n 小于 0：
        负数 为 真。
        n 为 0 减 n。
    完。
    只要 n 大于 0：
        余 为 n 取余于 10。
        结果 为 做 字符串拼接 用 做 转字符串 用 余、结果。
        n 为 n 除以 10。
    完。
    若 负数：
        结果 为 做 字符串拼接 用 「-」、结果。
    完。
    返还 结果。
完。

整数转十六进制 接受 数值：
    若 数值 等于 0：
        返还 「0x0」。
    完。
    hex字符 为 「0123456789ABCDEF」。
    可变 结果 为 「」。
    可变 n 为 数值。
    只要 n 大于 0：
        低4位 为 n 位与 0xF。
        结果 为 做 字符串拼接 用 做 字符串子串 用 hex字符、低4位、低4位 加 1、结果。
        n 为 n 右移 4。
    完。
    返还 做 字符串拼接 用 「0x」、结果。
完。

字符串转整数 接受 文本：
    长度 为 做 字符串长度 用 文本。
    若 长度 等于 0：
        返还 0。
    完。
    可变 结果 为 0。
    可变 i 为 0。
    可变 负数 为 假。
    若 做 字符串取字符 用 文本、0 等于 「-」：
        负数 为 真。
        i 为 1。
    完。
    只要 i 小于 长度：
        字符 为 做 编码 用 做 字符串取字符 用 文本、i。
        若 字符 大于或等于 48 并且 字符 小于或等于 57：
            结果 为 结果 乘 10 加 (字符 减 48)。
        完。
        i 为 i 加 1。
    完。
    若 负数：
        返还 0 减 结果。
    完。
    返还 结果。
完。

字符串重复 接受 文本、次数：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 次数：
        结果 为 做 字符串拼接 用 结果、文本。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串左填充 接受 文本、宽度、填充字符：
    长度 为 做 字符串长度 用 文本。
    若 长度 大于或等于 宽度：
        返还 文本。
    完。
    返还 做 字符串拼接 用 做 字符串重复 用 填充字符、宽度 减 长度、文本。
完。

格式化字节大小 接受 字节数：
    若 字节数 小于 1024：
        返还 做 字符串拼接 用 做 整数转字符串 用 字节数、「 B」。
    完。
    若 字节数 小于 1048576：
        返还 做 字符串拼接 用 做 整数转字符串 用 字节数 除以 1024、「 KB」。
    完。
    若 字节数 小于 1073741824：
        返还 做 字符串拼接 用 做 整数转字符串 用 字节数 除以 1048576、「 MB」。
    完。
    返还 做 字符串拼接 用 做 整数转字符串 用 字节数 除以 1073741824、「 GB」。
完。

字节到字符串 接受 数据：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 长度 用 数据：
        若 数据 的 第 i 项 等于 0：
            中断。
        完。
        结果 为 结果 加 做 字符 用 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串_相等 接受 a、b：
    返还 a 等于 b。
完。

字符串_开头是 接受 s、前缀：
    前缀长 为 做 字符串长度 用 前缀。
    若 做 字符串长度 用 s 小于 前缀长：
        返还 假。
    完。
    返还 做 字符串子串 用 s、0、前缀长 等于 前缀。
完。

字符串_结尾是 接受 s、后缀：
    s长 为 做 字符串长度 用 s。
    后缀长 为 做 字符串长度 用 后缀。
    若 s长 小于 后缀长：
        返还 假。
    完。
    返还 做 字符串子串 用 s、s长 减 后缀长、后缀长 等于 后缀。
完。

字符串_去首尾空白 接受 s：
    长度 为 做 字符串长度 用 s。
    若 长度 等于 0：
        返还 s。
    完。
    可变 起始 为 0。
    只要 起始 小于 长度：
        c 为 做 编码 用 做 字符串取字符 用 s、起始。
        若 c 不等于 32 并且 c 不等于 9 并且 c 不等于 10 并且 c 不等于 13：
            中断。
        完。
        起始 为 起始 加 1。
    完。
    可变 结束 为 长度 减 1。
    只要 结束 大于 起始：
        c 为 做 编码 用 做 字符串取字符 用 s、结束。
        若 c 不等于 32 并且 c 不等于 9 并且 c 不等于 10 并且 c 不等于 13：
            中断。
        完。
        结束 为 结束 减 1。
    完。
    返还 做 字符串子串 用 s、起始、结束 减 起始 加 1。
完。

字符串_转大写 接受 s：
    长度 为 做 字符串长度 用 s。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 长度：
        c 为 做 编码 用 做 字符串取字符 用 s、i。
        若 c 大于或等于 97 并且 c 小于或等于 122：
            c 为 c 减 32。
        完。
        结果 为 结果 加 做 字符 用 c。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串_转小写 接受 s：
    长度 为 做 字符串长度 用 s。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 长度：
        c 为 做 编码 用 做 字符串取字符 用 s、i。
        若 c 大于或等于 65 并且 c 小于或等于 90：
            c 为 c 加 32。
        完。
        结果 为 结果 加 做 字符 用 c。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串_分割 接受 s、分隔符：
    可变 结果 为 一组：（空）。
    可变 起始 为 0。
    分隔符长 为 做 字符串长度 用 分隔符。
    s长 为 做 字符串长度 用 s。
    只要 起始 小于或等于 s长：
        位置 为 做 字符串查找_从 用 s、分隔符、起始。
        若 位置 小于 0：
            做 追加 用 结果、做 字符串子串 用 s、起始、s长 减 起始。
            中断。
        完。
        做 追加 用 结果、做 字符串子串 用 s、起始、位置 减 起始。
        起始 为 位置 加 分隔符长。
    完。
    返还 结果。
完。

字符串_连接 接受 列表、分隔符：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 i 大于 0：
            结果 为 结果 加 分隔符。
        完。
        结果 为 结果 加 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串查找_从 接受 s、子串、起始：
    s长 为 做 字符串长度 用 s。
    子长 为 做 字符串长度 用 子串。
    若 子长 等于 0：
        返还 起始。
    完。
    可变 i 为 起始。
    只要 i 小于或等于 s长 减 子长：
        若 做 字符串子串 用 s、i、子长 等于 子串：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 0 减 1。
完。

AI格式窗口 为 16。

格式_十进制 为 0。

格式_十六进制 为 1。

格式_二进制 为 2。

格式_八进制 为 3。

转换统计 是 一种 事物，含 整数转字符串次数、十六进制转换次数、二进制转换次数、八进制转换次数、字符串转整数次数、字符串重复次数、格式化字节次数、左填充次数、右填充次数、分割次数、连接次数、比较次数、批量转换次数、AI预测次数、AI预测命中、错误次数。

可变 全局统计 为 新建 转换统计。

可变 转换模块已初始化 为 假。

可变 AI格式历史 为 一组：（空）。

可变 AI格式位置 为 0。

转换模块_初始化 接受 零参：
    若 转换模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI格式历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI格式窗口：
        做 追加 用 AI格式历史、0。
        i 为 i 加 1。
    完。
    AI格式位置 为 0。
    转换模块已初始化 为 真。
    做 打印 用 「[整数转换] 初始化完成」。
    返还 0。
完。

转换模块_销毁 接受 零参：
    若 转换模块已初始化 等于 假：
        返还 0。
    完。
    AI格式历史 为 一组：（空）。
    转换模块已初始化 为 假。
    做 打印 用 「[整数转换] 已销毁」。
    返还 0。
完。

整数转二进制 接受 数值：
    全局统计 的 二进制转换次数 为 全局统计 的 二进制转换次数 加 1。
    做 AI_记录格式 用 格式_二进制。
    若 数值 等于 0：
        返还 「0b0」。
    完。
    可变 结果 为 「」。
    可变 n 为 数值。
    若 n 小于 0：
        n 为 0 减 n。
    完。
    只要 n 大于 0：
        若 n 位与 1 等于 1：
            结果 为 做 字符串拼接 用 「1」、结果。
        否则：
            结果 为 做 字符串拼接 用 「0」、结果。
        完。
        n 为 n 右移 1。
    完。
    若 数值 小于 0：
        返还 做 字符串拼接 用 「-0b」、结果。
    完。
    返还 做 字符串拼接 用 「0b」、结果。
完。

整数转八进制 接受 数值：
    全局统计 的 八进制转换次数 为 全局统计 的 八进制转换次数 加 1。
    做 AI_记录格式 用 格式_八进制。
    若 数值 等于 0：
        返还 「0o0」。
    完。
    可变 结果 为 「」。
    可变 n 为 数值。
    若 n 小于 0：
        n 为 0 减 n。
    完。
    只要 n 大于 0：
        余 为 n 取余于 8。
        结果 为 做 字符串拼接 用 做 字符 用 48 加 余、结果。
        n 为 n 除以 8。
    完。
    若 数值 小于 0：
        返还 做 字符串拼接 用 「-0o」、结果。
    完。
    返还 做 字符串拼接 用 「0o」、结果。
完。

整数_安全转字符串 接受 数值：
    全局统计 的 整数转字符串次数 为 全局统计 的 整数转字符串次数 加 1。
    做 AI_记录格式 用 格式_十进制。
    返还 做 整数转字符串 用 数值。
完。

整数_格式化 接受 数值、格式：
    若 格式 等于 格式_十六进制：
        返还 做 整数转十六进制 用 数值。
    完。
    若 格式 等于 格式_二进制：
        返还 做 整数转二进制 用 数值。
    完。
    若 格式 等于 格式_八进制：
        返还 做 整数转八进制 用 数值。
    完。
    返还 做 整数转字符串 用 数值。
完。

字符串_是有效整数 接受 文本：
    若 文本 等于 空：
        返还 假。
    完。
    长度 为 做 字符串长度 用 文本。
    若 长度 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    若 做 编码 用 做 字符串取字符 用 文本、0 等于 45：
        i 为 1。
    完。
    若 i 大于或等于 长度：
        返还 假。
    完。
    只要 i 小于 长度：
        c 为 做 编码 用 做 字符串取字符 用 文本、i。
        若 c 小于 48 或者 c 大于 57：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

字符串_是有效十六进制 接受 文本：
    若 文本 等于 空：
        返还 假。
    完。
    长度 为 做 字符串长度 用 文本。
    可变 起始 为 0。
    若 长度 大于或等于 2 并且 做 字符串子串 用 文本、0、2 等于 「0x」：
        起始 为 2。
    完。
    若 起始 大于或等于 长度：
        返还 假。
    完。
    可变 i 为 起始。
    只要 i 小于 长度：
        c 为 做 编码 用 做 字符串取字符 用 文本、i。
        若 非 (c 大于或等于 48 并且 c 小于或等于 57 或者 c 大于或等于 65 并且 c 小于或等于 70 或者 c 大于或等于 97 并且 c 小于或等于 102)：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

格式化_千位分隔 接受 数值：
    文本 为 做 整数转字符串 用 数值。
    长度 为 做 字符串长度 用 文本。
    若 长度 小于或等于 3：
        返还 文本。
    完。
    可变 结果 为 「」。
    可变 计数 为 0。
    可变 i 为 长度 减 1。
    可变 负号 为 假。
    若 做 编码 用 做 字符串取字符 用 文本、0 等于 45：
        负号 为 真。
    完。
    只要 i 大于或等于 0：
        若 负号 等于 真 并且 i 等于 0：
            结果 为 做 字符串拼接 用 「-」、结果。
            止。
        完。
        结果 为 做 字符串拼接 用 做 字符串子串 用 文本、i、1、结果。
        计数 为 计数 加 1。
        若 计数 取余于 3 等于 0 并且 i 大于 0：
            若 负号 等于 假 或者 i 大于 1：
                结果 为 做 字符串拼接 用 「,」、结果。
            完。
        完。
        i 为 i 减 1。
    完。
    返还 结果。
完。

格式化_百分比 接受 分子、分母：
    若 分母 等于 0：
        返还 「0%」。
    完。
    百分 为 分子 乘 100 除以 分母。
    返还 做 字符串拼接 用 做 整数转字符串 用 百分、「%」。
完。

格式化_时间差 接受 毫秒：
    若 毫秒 小于 1000：
        返还 做 字符串拼接 用 做 整数转字符串 用 毫秒、「ms」。
    完。
    若 毫秒 小于 60000：
        返还 做 字符串拼接 用 做 整数转字符串 用 毫秒 除以 1000、「s」。
    完。
    若 毫秒 小于 3600000：
        返还 做 字符串拼接 用 做 整数转字符串 用 毫秒 除以 60000、「m」。
    完。
    返还 做 字符串拼接 用 做 整数转字符串 用 毫秒 除以 3600000、「h」。
完。

字符串右填充 接受 文本、宽度、填充字符：
    长度 为 做 字符串长度 用 文本。
    若 长度 大于或等于 宽度：
        返还 文本。
    完。
    返还 做 字符串拼接 用 文本、做 字符串重复 用 填充字符、宽度 减 长度。
完。

批量_整数转字符串 接受 数值列表：
    若 数值列表 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 批量转换次数 为 全局统计 的 批量转换次数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数值列表：
        做 追加 用 结果、做 整数转字符串 用 数值列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

批量_字符串转整数 接受 字符串列表：
    若 字符串列表 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 批量转换次数 为 全局统计 的 批量转换次数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字符串列表：
        做 追加 用 结果、做 字符串转整数 用 字符串列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

AI_记录格式 接受 格式：
    做 设置 用 AI格式历史、AI格式位置、格式。
    AI格式位置 为 (AI格式位置 加 1) 取余于 AI格式窗口。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
完。

AI_预测下次格式 接受 零参：
    可变 最频格式 为 0。
    可变 最频次 为 0。
    可变 i 为 0。
    只要 i 小于 AI格式窗口：
        当前 为 AI格式历史 的 第 i 项。
        可变 频 为 0。
        可变 j 为 0。
        只要 j 小于 AI格式窗口：
            若 AI格式历史 的 第 j 项 等于 当前：
                频 为 频 加 1。
            完。
            j 为 j 加 1。
        完。
        若 频 大于 最频次：
            最频次 为 频。
            最频格式 为 当前。
        完。
        i 为 i 加 1。
    完。
    若 最频次 大于 AI格式窗口 除以 3：
        全局统计 的 AI预测命中 为 全局统计 的 AI预测命中 加 1。
    完。
    返还 最频格式。
完。

AI_格式名 接受 格式：
    若 格式 等于 格式_十进制：
        返还 「十进制」。
    完。
    若 格式 等于 格式_十六进制：
        返还 「十六进制」。
    完。
    若 格式 等于 格式_二进制：
        返还 「二进制」。
    完。
    若 格式 等于 格式_八进制：
        返还 「八进制」。
    完。
    返还 「未知」。
完。

转换模块_转储 接受 零参：
    做 打印 用 「[整数转换] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  AI预测下次格式: 」、做 AI_格式名 用 做 AI_预测下次格式。
    返还 0。
完。

转换模块_打印统计 接受 零参：
    做 打印 用 「[整数转换] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  整数转字符串: 」、做 整数转字符串 用 全局统计 的 整数转字符串次数。
    做 打印 用 做 字符串拼接 用 「  十六进制转换: 」、做 整数转字符串 用 全局统计 的 十六进制转换次数。
    做 打印 用 做 字符串拼接 用 「  二进制转换: 」、做 整数转字符串 用 全局统计 的 二进制转换次数。
    做 打印 用 做 字符串拼接 用 「  八进制转换: 」、做 整数转字符串 用 全局统计 的 八进制转换次数。
    做 打印 用 做 字符串拼接 用 「  字符串转整数: 」、做 整数转字符串 用 全局统计 的 字符串转整数次数。
    做 打印 用 做 字符串拼接 用 「  字符串重复: 」、做 整数转字符串 用 全局统计 的 字符串重复次数。
    做 打印 用 做 字符串拼接 用 「  格式化字节: 」、做 整数转字符串 用 全局统计 的 格式化字节次数。
    做 打印 用 做 字符串拼接 用 「  左填充: 」、做 整数转字符串 用 全局统计 的 左填充次数。
    做 打印 用 做 字符串拼接 用 「  分割次数: 」、做 整数转字符串 用 全局统计 的 分割次数。
    做 打印 用 做 字符串拼接 用 「  连接次数: 」、做 整数转字符串 用 全局统计 的 连接次数。
    做 打印 用 做 字符串拼接 用 「  比较次数: 」、做 整数转字符串 用 全局统计 的 比较次数。
    做 打印 用 做 字符串拼接 用 「  批量转换: 」、做 整数转字符串 用 全局统计 的 批量转换次数。
    做 打印 用 做 字符串拼接 用 「  AI预测: 」、做 整数转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI命中: 」、做 整数转字符串 用 全局统计 的 AI预测命中。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 整数转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

转换模块_注册到内核 接受 内核上下文：
    若 转换模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[整数转换] 已注册到内核上下文」。
    返还 0。
完。

转换模块_查询状态 接受 零参：
    若 转换模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

转换模块_健康检查 接受 零参：
    若 转换模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 整数转字符串次数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
