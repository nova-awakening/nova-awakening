《处理亲和性》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·字符串替换》。

引用《.·链表后继》。

引用《.·进程标识分》。

引用《.·进程描述符》。

外部。

进程_当前CPU。

可变 系统CPU掩码 为 0xFF。

cpumask_设置 接受 掩码、cpu：
    返还 掩码 位或 1 左移 cpu。
完。

cpumask_清除 接受 掩码、cpu：
    返还 掩码 位与 按位取反 (1 左移 cpu)。
完。

cpumask_测试 接受 掩码、cpu：
    返还 掩码 位与 1 左移 cpu 不等于 0。
完。

cpumask_全部 接受 零参：
    返还 系统CPU掩码。
完。

cpumask_无 接受 零参：
    返还 0x0000000000000000。
完。

cpumask_单CPU 接受 cpu：
    返还 1 左移 cpu。
完。

cpumask_计数 接受 掩码：
    可变 计 为 0。
    可变 m 为 掩码。
    只要 m 不等于 0：
        计 为 计 加 (m 位与 1)。
        m 为 m 右移 1。
    完。
    返还 计。
完。

cpumask_第一个 接受 掩码：
    可变 i 为 0。
    只要 i 小于 64：
        若 掩码 位与 1 左移 i 不等于 0：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

cpumask_与 接受 a、b：
    返还 a 位与 b。
完。

cpumask_或 接受 a、b：
    返还 a 位或 b。
完。

sys_sched_setaffinity 接受 pid、掩码：
    若 做 cpumask_与 用 掩码、系统CPU掩码 等于 0：
        返还 错误_无效参数。
    完。
    返还 成功。
完。

sys_sched_getaffinity 接受 pid：
    返还 做 任务_获取CPU掩码 用 做 进程表_查找 用 pid。
完。

可以迁移到 接受 任务、cpu：
    若 非 做 cpumask_测试 用 系统CPU掩码、cpu：
        返还 假。
    完。
    任务掩码 为 做 任务_获取CPU掩码 用 任务。
    若 任务掩码 不等于 0 并且 非 做 cpumask_测试 用 任务掩码、cpu：
        返还 假。
    完。
    返还 真。
完。

设置系统CPU掩码 接受 CPU数：
    若 CPU数 大于 64：
        CPU数 为 64。
    完。
    若 CPU数 小于 1：
        CPU数 为 1。
    完。
    系统CPU掩码 为 (1 左移 CPU数) 减 1。
完。

可变 任务CPU掩码表 为 一组：（空）。

任务_获取CPU掩码 接受 任务：
    pid 为 做 进程_PID 用 任务。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务CPU掩码表：
        若 任务CPU掩码表 的 第 i 项 的 第 0 项 等于 pid：
            返还 任务CPU掩码表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 系统CPU掩码。
完。

任务_设置CPU掩码 接受 任务、掩码：
    若 做 cpumask_与 用 掩码、系统CPU掩码 等于 0：
        返还 错误_无效参数。
    完。
    pid 为 做 进程_PID 用 任务。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务CPU掩码表：
        若 任务CPU掩码表 的 第 i 项 的 第 0 项 等于 pid：
            做 设置 用 任务CPU掩码表 的 第 i 项、1、掩码。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 任务CPU掩码表、一组：pid、掩码。
    返还 成功。
完。

cpumask_下一个 接受 掩码、从：
    可变 i 为 从 加 1。
    只要 i 小于 64：
        若 做 cpumask_测试 用 掩码、i：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

cpumask_相等 接受 a、b：
    返还 a 等于 b。
完。

cpumask_子集 接受 子、父：
    返还 子 位与 父 等于 子。
完。

cpumask_为空 接受 掩码：
    返还 掩码 等于 0。
完。

CPU亲和性_统计 接受 零参：
    返还 一组：做 计数 用 任务CPU掩码表、做 cpumask_计数 用 系统CPU掩码。
完。

亲和性_迁移检查 接受 任务：
    当前cpu 为 做 进程_当前CPU 用 任务。
    掩码 为 做 任务_获取CPU掩码 用 任务。
    若 做 cpumask_测试 用 掩码、当前cpu：
        返还 假。
    完。
    返还 真。
完。

亲和性_选择目标CPU 接受 任务：
    掩码 为 做 任务_获取CPU掩码 用 任务。
    有效掩码 为 做 cpumask_与 用 掩码、系统CPU掩码。
    若 有效掩码 等于 0：
        返还 负 1。
    完。
    返还 做 cpumask_第一个 用 有效掩码。
完。

任务_清除CPU掩码 接受 pid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务CPU掩码表：
        若 任务CPU掩码表 的 第 i 项 的 第 0 项 等于 pid：
            做 链表_删除 用 任务CPU掩码表、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

cpumask_遍历 接受 掩码、回调：
    可变 cpu 为 做 cpumask_第一个 用 掩码。
    只要 cpu 大于或等于 0：
        做 回调 用 cpu。
        cpu 为 做 cpumask_下一个 用 掩码、cpu。
    完。
完。

cpumask_转列表 接受 掩码：
    可变 结果 为 一组：（空）。
    可变 cpu 为 做 cpumask_第一个 用 掩码。
    只要 cpu 大于或等于 0：
        做 追加 用 结果、cpu。
        cpu 为 做 cpumask_下一个 用 掩码、cpu。
    完。
    返还 结果。
完。

在线CPU数 接受 零参：
    返还 做 cpumask_计数 用 系统CPU掩码。
完。

cpumask_转字符串 接受 掩码：
    返还 「0x」 加 做 整数转十六进制 用 掩码。
完。

CPU亲和性_完整统计 接受 零参：
    返还 一组：做 计数 用 任务CPU掩码表、做 在线CPU数。
完。

可变 AI_处理亲和性_负载窗口 为 一组：（空）。

AI_处理亲和性_负载预测 接受 当前负载：
    做 追加 用 AI_处理亲和性_负载窗口、当前负载。
    若 做 计数 用 AI_处理亲和性_负载窗口 大于 20：
        做 列表_移除 用 AI_处理亲和性_负载窗口、0。
    完。
    若 做 计数 用 AI_处理亲和性_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_处理亲和性_负载窗口。
    趋势 为 AI_处理亲和性_负载窗口 的 第 n 减 1 项 减 AI_处理亲和性_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_处理亲和性_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
