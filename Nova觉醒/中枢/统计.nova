《统计》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·记忆·VFS文件读写》。

引用《..·记忆·VFS读写》。

引用《.·时钟中枢》。

引用《..·执行·探测》。

引用《.·键值缓存》。

统计_损失窗口 为 50。

统计_收敛阈值 为 10。

统计_最大层追踪 为 128。

训练统计器 是 一种 事物，含 训练轮次、已处理样本、总损失、最小损失、最大损失、损失历史、学习率历史、层统计、训练开始时间、总训练耗时、验证损失、验证准确率、最佳验证损失、早停计数器、已收敛、梯度范数历史、吞吐量历史。

层训练统计 是 一种 事物，含 层名、前向耗时、反向耗时、梯度范数、权重范数、更新次数、最大梯度、最小梯度。

可变 全局训练统计 为 0。

训练统计_初始化 接受 零参：
    全局训练统计 为 做 堆_分配 用 训练统计器。
    全局训练统计 的 训练轮次 为 0。
    全局训练统计 的 已处理样本 为 0。
    全局训练统计 的 总损失 为 0。
    全局训练统计 的 最小损失 为 999999。
    全局训练统计 的 最大损失 为 0。
    全局训练统计 的 损失历史 为 一组：（空）。
    全局训练统计 的 学习率历史 为 一组：（空）。
    全局训练统计 的 层统计 为 一组：（空）。
    全局训练统计 的 训练开始时间 为 做 读取毫秒。
    全局训练统计 的 总训练耗时 为 0。
    全局训练统计 的 验证损失 为 0。
    全局训练统计 的 验证准确率 为 0。
    全局训练统计 的 最佳验证损失 为 999999。
    全局训练统计 的 早停计数器 为 0。
    全局训练统计 的 已收敛 为 假。
    全局训练统计 的 梯度范数历史 为 一组：（空）。
    全局训练统计 的 吞吐量历史 为 一组：（空）。
    返还 全局训练统计。
完。

训练统计_记录损失 接受 损失：
    若 全局训练统计 等于 0：
        做 训练统计_初始化。
    完。
    全局训练统计 的 总损失 为 全局训练统计 的 总损失 加 损失。
    若 损失 小于 全局训练统计 的 最小损失：
        全局训练统计 的 最小损失 为 损失。
    完。
    若 损失 大于 全局训练统计 的 最大损失：
        全局训练统计 的 最大损失 为 损失。
    完。
    做 追加 用 全局训练统计 的 损失历史、损失。
    若 做 计数 用 全局训练统计 的 损失历史 大于 统计_损失窗口 乘 10：
        全局训练统计 的 损失历史 为 做 切片 用 全局训练统计 的 损失历史、做 计数 用 全局训练统计 的 损失历史 减 统计_损失窗口 乘 5、做 计数 用 全局训练统计 的 损失历史。
    完。
完。

训练统计_记录轮次 接受 样本数、学习率：
    若 全局训练统计 等于 0：
        做 训练统计_初始化。
    完。
    全局训练统计 的 训练轮次 为 全局训练统计 的 训练轮次 加 1。
    全局训练统计 的 已处理样本 为 全局训练统计 的 已处理样本 加 样本数。
    做 追加 用 全局训练统计 的 学习率历史、学习率。
完。

训练统计_记录验证 接受 验证损失、准确率：
    若 全局训练统计 等于 0：
        返还。
    完。
    全局训练统计 的 验证损失 为 验证损失。
    全局训练统计 的 验证准确率 为 准确率。
    若 验证损失 小于 全局训练统计 的 最佳验证损失：
        全局训练统计 的 最佳验证损失 为 验证损失。
        全局训练统计 的 早停计数器 为 0。
    否则：
        全局训练统计 的 早停计数器 为 全局训练统计 的 早停计数器 加 1。
    完。
完。

训练统计_检查收敛 接受 零参：
    若 全局训练统计 等于 0：
        返还 假。
    完。
    历史 为 全局训练统计 的 损失历史。
    若 做 计数 用 历史 小于 统计_损失窗口：
        返还 假。
    完。
    最近开始 为 做 计数 用 历史 减 统计_损失窗口。
    可变 最近均值 为 0。
    可变 i 为 最近开始。
    只要 i 小于 做 计数 用 历史：
        最近均值 为 最近均值 加 历史 的 第 i 项。
        i 为 i 加 1。
    完。
    最近均值 为 最近均值 除以 统计_损失窗口。
    前段开始 为 最近开始 减 统计_损失窗口。
    若 前段开始 小于 0：
        返还 假。
    完。
    可变 前段均值 为 0。
    i 为 前段开始。
    只要 i 小于 最近开始：
        前段均值 为 前段均值 加 历史 的 第 i 项。
        i 为 i 加 1。
    完。
    前段均值 为 前段均值 除以 统计_损失窗口。
    若 前段均值 等于 0：
        返还 假。
    完。
    改善率 为 (前段均值 减 最近均值) 乘 1000 除以 前段均值。
    若 改善率 小于 统计_收敛阈值：
        全局训练统计 的 已收敛 为 真。
        返还 真。
    完。
    返还 假。
完。

训练统计_应早停 接受 零参：
    若 全局训练统计 等于 0：
        返还 假。
    完。
    返还 全局训练统计 的 早停计数器 大于或等于 10。
完。

训练统计_记录梯度范数 接受 范数：
    若 全局训练统计 等于 0：
        返还。
    完。
    做 追加 用 全局训练统计 的 梯度范数历史、范数。
    若 做 计数 用 全局训练统计 的 梯度范数历史 大于 200：
        全局训练统计 的 梯度范数历史 为 做 切片 用 全局训练统计 的 梯度范数历史、100、200。
    完。
完。

训练统计_检查梯度爆炸 接受 零参：
    若 全局训练统计 等于 0：
        返还 假。
    完。
    历史 为 全局训练统计 的 梯度范数历史。
    若 做 计数 用 历史 小于 5：
        返还 假。
    完。
    最近 为 历史 的 第 做 计数 用 历史 减 1 项。
    前一个 为 历史 的 第 做 计数 用 历史 减 2 项。
    若 前一个 大于 0 并且 最近 除以 前一个 大于 100：
        返还 真。
    完。
    返还 假。
完。

训练统计_记录吞吐量 接受 样本每秒：
    若 全局训练统计 等于 0：
        返还。
    完。
    做 追加 用 全局训练统计 的 吞吐量历史、样本每秒。
完。

训练统计_添加层 接受 层名：
    若 全局训练统计 等于 0：
        做 训练统计_初始化。
    完。
    若 做 计数 用 全局训练统计 的 层统计 大于或等于 统计_最大层追踪：
        返还。
    完。
    ls 为 做 堆_分配 用 层训练统计。
    ls 的 层名 为 层名。
    ls 的 前向耗时 为 0。
    ls 的 反向耗时 为 0。
    ls 的 梯度范数 为 0。
    ls 的 权重范数 为 0。
    ls 的 更新次数 为 0。
    ls 的 最大梯度 为 0。
    ls 的 最小梯度 为 999999。
    做 追加 用 全局训练统计 的 层统计、ls。
完。

训练统计_更新层 接受 层索引、前向耗时、反向耗时、梯度范数：
    若 全局训练统计 等于 0：
        返还。
    完。
    若 层索引 小于 0 或者 层索引 大于或等于 做 计数 用 全局训练统计 的 层统计：
        返还。
    完。
    ls 为 全局训练统计 的 层统计 的 第 层索引 项。
    ls 的 前向耗时 为 ls 的 前向耗时 加 前向耗时。
    ls 的 反向耗时 为 ls 的 反向耗时 加 反向耗时。
    ls 的 梯度范数 为 梯度范数。
    ls 的 更新次数 为 ls 的 更新次数 加 1。
    若 梯度范数 大于 ls 的 最大梯度：
        ls 的 最大梯度 为 梯度范数。
    完。
    若 梯度范数 小于 ls 的 最小梯度：
        ls 的 最小梯度 为 梯度范数。
    完。
完。

TNF58B_统计 接受 零参：
    若 全局训练统计 等于 0：
        返还 一组：0、0、0、0。
    完。
    返还 一组：全局训练统计 的 训练轮次、全局训练统计 的 已处理样本、全局训练统计 的 总损失、做 计数 用 全局训练统计 的 层统计。
完。

打开数据流 接受 路径：
    文件 为 做 vfs_open 用 路径、0、0。
    若 文件 等于 空：
        返还 空。
    完。
    返还 一组：文件、0、假、0。
完。

数据流_结束 接受 流：
    若 流 等于 空：
        返还 真。
    完。
    返还 流 的 第 2 项。
完。

数据流_读批次 接受 流、批大小：
    若 流 等于 空 或者 流 的 第 2 项：
        返还 空。
    完。
    数据 为 做 vfs_read 用 流 的 第 0 项、空、批大小 乘 256。
    若 数据 等于 空 或者 做 计数 用 数据 等于 0：
        流 的 第 2 项 为 真。
        返还 空。
    完。
    流 的 第 1 项 为 流 的 第 1 项 加 做 计数 用 数据。
    流 的 第 3 项 为 流 的 第 3 项 加 1。
    返还 数据。
完。

数据流_重置 接受 流：
    若 流 等于 空：
        返还。
    完。
    做 vfs_seek 用 流 的 第 0 项、0。
    流 的 第 1 项 为 0。
    流 的 第 2 项 为 假。
完。

数据流_关闭 接受 流：
    若 流 不等于 空 并且 流 的 第 0 项 不等于 空：
        做 vfs_close 用 流 的 第 0 项。
    完。
完。

数据流_统计 接受 流：
    若 流 等于 空：
        返还 一组：0、0。
    完。
    返还 一组：流 的 第 1 项、流 的 第 3 项。
完。

向量哈希 接受 向量：
    可变 h 为 0x811c9dc5。
    可变 i 为 0。
    只要 i 小于 做 计数 用 向量：
        h 为 (h 位异或 向量 的 第 i 项 位与 0xFF) 乘 0x01000193。
        i 为 i 加 1。
    完。
    返还 h。
完。

向量哈希64 接受 向量：
    可变 h 为 0xcbf29ce484222325。
    可变 i 为 0。
    只要 i 小于 做 计数 用 向量：
        h 为 (h 位异或 向量 的 第 i 项 位与 0xFF) 乘 0x100000001b3。
        i 为 i 加 1。
    完。
    返还 h。
完。

稀疏KV_查找 接受 哈希：
    若 非 KV_已初始化：
        返还 空。
    完。
    返还 做 KV_查询 用 哈希。
完。

向量加权融合 接受 a、b、权重：
    若 a 等于 空：
        返还 b。
    完。
    若 b 等于 空：
        返还 a。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 并且 i 小于 做 计数 用 b：
        做 追加 用 结果、a 的 第 i 项 乘 (1000 减 权重) 除以 1000 加 b 的 第 i 项 乘 权重 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量点积 接受 a、b：
    可变 和 为 0。
    可变 i 为 0。
    长度 为 做 计数 用 a。
    若 做 计数 用 b 小于 长度：
        长度 为 做 计数 用 b。
    完。
    只要 i 小于 长度：
        和 为 和 加 a 的 第 i 项 乘 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

向量L2范数 接受 向量：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 向量：
        和 为 和 加 向量 的 第 i 项 乘 向量 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 整数平方根 用 和。
完。

向量余弦相似度 接受 a、b：
    点 为 做 向量点积 用 a、b。
    范数a 为 做 向量L2范数 用 a。
    范数b 为 做 向量L2范数 用 b。
    若 范数a 等于 0 或者 范数b 等于 0：
        返还 0。
    完。
    返还 点 乘 1000 除以 (范数a 乘 范数b)。
完。

调度_常量 为 0。

调度_warmup 为 1。

调度_cosine 为 2。

调度_step 为 3。

调度_onecycle 为 4。

学习率调度器 是 一种 事物，含 类型、基础学习率、最小学习率、最大学习率、warmup步数、总步数、step_size、衰减因子、当前步。

可变 全局调度器 为 0。

学习率调度_初始化 接受 类型、基础lr、最小lr、最大lr、warmup步、总步、step_sz、衰减：
    全局调度器 为 做 堆_分配 用 学习率调度器。
    全局调度器 的 类型 为 类型。
    全局调度器 的 基础学习率 为 基础lr。
    全局调度器 的 最小学习率 为 最小lr。
    全局调度器 的 最大学习率 为 最大lr。
    全局调度器 的 warmup步数 为 warmup步。
    全局调度器 的 总步数 为 总步。
    全局调度器 的 step_size 为 step_sz。
    全局调度器 的 衰减因子 为 衰减。
    全局调度器 的 当前步 为 0。
    返还 全局调度器。
完。

学习率调度_获取lr 接受 零参：
    若 全局调度器 等于 0：
        返还 1。
    完。
    s 为 全局调度器。
    s 的 当前步 为 s 的 当前步 加 1。
    若 s 的 当前步 小于或等于 s 的 warmup步数 并且 s 的 warmup步数 大于 0：
        返还 s 的 最小学习率 加 (s 的 基础学习率 减 s 的 最小学习率) 乘 s 的 当前步 除以 s 的 warmup步数。
    完。
    有效步 为 s 的 当前步 减 s 的 warmup步数。
    有效总 为 s 的 总步数 减 s 的 warmup步数。
    若 有效总 小于或等于 0：
        有效总 为 1。
    完。
    若 s 的 类型 等于 调度_cosine：
        进度 为 有效步 乘 1000 除以 有效总。
        可变 cos_val 为 0。
        若 进度 小于或等于 500：
            cos_val 为 1000 减 2 乘 进度 乘 进度 除以 1000。
        否则：
            x 为 1000 减 进度。
            cos_val 为 2 乘 x 乘 x 除以 1000 减 1000。
        完。
        返还 s 的 最小学习率 加 (s 的 基础学习率 减 s 的 最小学习率) 乘 (1000 加 cos_val) 除以 2000。
    完。
    若 s 的 类型 等于 调度_step：
        若 s 的 step_size 小于或等于 0：
            返还 s 的 基础学习率。
        完。
        衰减次数 为 有效步 除以 s 的 step_size。
        可变 lr 为 s 的 基础学习率。
        可变 d 为 0。
        只要 d 小于 衰减次数：
            lr 为 lr 乘 s 的 衰减因子 除以 1000。
            d 为 d 加 1。
        完。
        若 lr 小于 s 的 最小学习率：
            lr 为 s 的 最小学习率。
        完。
        返还 lr。
    完。
    若 s 的 类型 等于 调度_onecycle：
        进度 为 有效步 乘 100 除以 有效总。
        若 进度 小于 30：
            返还 s 的 最小学习率 加 (s 的 最大学习率 减 s 的 最小学习率) 乘 进度 除以 30。
        完。
        返还 s 的 最大学习率 减 (s 的 最大学习率 减 s 的 最小学习率) 乘 (进度 减 30) 除以 70。
    完。
    返还 s 的 基础学习率。
完。

可变 梯度_连续微小计数 为 0。

可变 梯度_NaN计数 为 0。

梯度_微小阈值 为 1。

梯度_爆炸比率 为 100。

训练统计_检查梯度消失 接受 零参：
    若 全局训练统计 等于 0：
        返还 假。
    完。
    历史 为 全局训练统计 的 梯度范数历史。
    若 做 计数 用 历史 小于 5：
        返还 假。
    完。
    可变 微小计 为 0。
    可变 i 为 做 计数 用 历史 减 5。
    只要 i 小于 做 计数 用 历史：
        若 历史 的 第 i 项 小于 梯度_微小阈值：
            微小计 为 微小计 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 微小计 大于或等于 5。
完。

训练统计_检查数值有效 接受 值：
    若 值 大于 999999999 或者 值 小于 负 999999999：
        梯度_NaN计数 为 梯度_NaN计数 加 1。
        返还 假。
    完。
    返还 真。
完。

可变 轮次开始时间 为 0。

可变 轮次耗时EMA 为 0。

可变 吞吐量EMA 为 0。

训练统计_轮次开始 接受 零参：
    轮次开始时间 为 做 读取毫秒。
完。

训练统计_轮次结束 接受 样本数：
    耗时 为 做 读取毫秒 减 轮次开始时间。
    若 耗时 小于或等于 0：
        耗时 为 1。
    完。
    若 轮次耗时EMA 等于 0：
        轮次耗时EMA 为 耗时。
    否则：
        轮次耗时EMA 为 (轮次耗时EMA 乘 900 加 耗时 乘 100) 除以 1000。
    完。
    吞吐 为 样本数 乘 1000 除以 耗时。
    若 吞吐量EMA 等于 0：
        吞吐量EMA 为 吞吐。
    否则：
        吞吐量EMA 为 (吞吐量EMA 乘 900 加 吞吐 乘 100) 除以 1000。
    完。
    做 训练统计_记录吞吐量 用 吞吐。
完。

训练统计_估算ETA 接受 总轮次：
    若 全局训练统计 等于 0 或者 轮次耗时EMA 等于 0：
        返还 0。
    完。
    剩余 为 总轮次 减 全局训练统计 的 训练轮次。
    若 剩余 小于或等于 0：
        返还 0。
    完。
    返还 剩余 乘 轮次耗时EMA。
完。

检查点_最大保留 为 5。

检查点记录 是 一种 事物，含 轮次、验证损失、时间戳。

可变 检查点列表 为 一组：（空）。

检查点_保存 接受 轮次、验证损失：
    cp 为 做 堆_分配 用 检查点记录。
    cp 的 轮次 为 轮次。
    cp 的 验证损失 为 验证损失。
    cp 的 时间戳 为 做 读取毫秒。
    做 追加 用 检查点列表、cp。
    若 做 计数 用 检查点列表 大于 检查点_最大保留：
        可变 最差idx 为 0。
        可变 最差损 为 检查点列表 的 第 0 项 的 验证损失。
        可变 i 为 1。
        只要 i 小于 做 计数 用 检查点列表：
            若 检查点列表 的 第 i 项 的 验证损失 大于 最差损：
                最差损 为 检查点列表 的 第 i 项 的 验证损失。
                最差idx 为 i。
            完。
            i 为 i 加 1。
        完。
        检查点列表 为 做 切片 用 检查点列表、0、最差idx 加 做 切片 用 检查点列表、最差idx 加 1、做 计数 用 检查点列表。
    完。
    返还 做 计数 用 检查点列表。
完。

检查点_最佳 接受 零参：
    若 做 计数 用 检查点列表 等于 0：
        返还 空。
    完。
    可变 最佳idx 为 0。
    可变 最佳损 为 检查点列表 的 第 0 项 的 验证损失。
    可变 i 为 1。
    只要 i 小于 做 计数 用 检查点列表：
        若 检查点列表 的 第 i 项 的 验证损失 小于 最佳损：
            最佳损 为 检查点列表 的 第 i 项 的 验证损失。
            最佳idx 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 检查点列表 的 第 最佳idx 项。
完。

TNF训练_完整统计 接受 零参：
    若 全局训练统计 等于 0：
        返还 一本字典：轮次 对应 0、损失 对应 0、样本 对应 0、收敛 对应 假。
    完。
    平均损失 为 0。
    若 全局训练统计 的 训练轮次 大于 0：
        平均损失 为 全局训练统计 的 总损失 除以 全局训练统计 的 训练轮次。
    完。
    当前lr 为 0。
    若 全局调度器 不等于 0：
        当前lr 为 做 学习率调度_获取lr。
    完。
    返还 一本字典：轮次 对应 全局训练统计 的 训练轮次、样本 对应 全局训练统计 的 已处理样本、总损失 对应 全局训练统计 的 总损失、平均损失 对应 平均损失、最小损失 对应 全局训练统计 的 最小损失、最大损失 对应 全局训练统计 的 最大损失、验证损失 对应 全局训练统计 的 验证损失、验证准确率 对应 全局训练统计 的 验证准确率、收敛 对应 全局训练统计 的 已收敛、早停计数 对应 全局训练统计 的 早停计数器、层数 对应 做 计数 用 全局训练统计 的 层统计、当前学习率 对应 当前lr、吞吐量 对应 吞吐量EMA、轮次耗时 对应 轮次耗时EMA、NaN计数 对应 梯度_NaN计数、检查点数 对应 做 计数 用 检查点列表。
完。

可变 _谐振损失_平滑值 为 0。

可变 _谐振损失_样本数 为 0。

可变 _谐振损失_衰减 为 900。

谐振损失_更新 接受 新损失：
    若 _谐振损失_样本数 等于 0：
        _谐振损失_平滑值 为 新损失。
    否则：
        _谐振损失_平滑值 为 _谐振损失_平滑值 乘 _谐振损失_衰减 除以 1000 加 新损失 乘 (1000 减 _谐振损失_衰减) 除以 1000。
    完。
    _谐振损失_样本数 为 _谐振损失_样本数 加 1。
完。

谐振损失_获取 接受 零参：
    返还 _谐振损失_平滑值。
完。

训练统计_完整统计 接受 零参：
    返还 一组：「谐振平滑损失」、_谐振损失_平滑值、「谐振样本数」、_谐振损失_样本数、「谐振衰减率」、_谐振损失_衰减。
完。

谐振损失_重置 接受 零参：
    _谐振损失_平滑值 为 0。
    _谐振损失_样本数 为 0。
完。

谐振损失_趋势 接受 零参：
    若 _谐振损失_样本数 小于 2：
        返还 0。
    完。
    返还 _谐振损失_平滑值。
完。

本章 收束。
