《AI网络QoS》

引用《.·类型定义》。

引用《.·错误码》。

QOS_类数 为 3。

QoS流量统计 是 一种 事物，含 字节总数、包总数、字节速率EMA、包速率EMA、丢包数、延迟EMA、当前带宽权重。

可变 QoS统计 为 一组：（空）。

可变 QoS_总带宽 为 1000000。

可变 QoS_拥塞级别 为 0。

可变 QoS_已初始化 为 假。

可变 默认权重 为 一组：500、300、200。

AI网络QoS_初始化 接受 零参：
    可变 i 为 0。
    只要 i 小于 QOS_类数：
        stat 为 新建 QoS流量统计。
        stat 的 当前带宽权重 为 默认权重 的 第 i 项。
        做 追加 用 QoS统计、stat。
        i 为 i 加 1。
    完。
    QoS_已初始化 为 真。
    做 内核信息 用 「AI网络QoS: 贝叶斯流量分类+自适应带宽分配+拥塞感知」。
完。

AI网络QoS_记录包 接受 流量类型、包大小、延迟us：
    若 非 QoS_已初始化 或者 流量类型 大于或等于 QOS_类数：
        返还。
    完。
    stat 为 QoS统计 的 第 流量类型 项。
    stat 的 字节总数 为 stat 的 字节总数 加 包大小。
    stat 的 包总数 为 stat 的 包总数 加 1。
    stat 的 延迟EMA 为 (stat 的 延迟EMA 乘 922 加 延迟us 乘 1024 乘 102) 除以 (1024 乘 1024)。
完。

AI网络QoS_调整带宽 接受 零参：
    若 非 QoS_已初始化：
        返还。
    完。
    可变 总流量 为 0。
    可变 i 为 0。
    只要 i 小于 QOS_类数：
        总流量 为 总流量 加 QoS统计 的 第 i 项 的 字节总数。
        i 为 i 加 1。
    完。
    若 总流量 等于 0：
        返还。
    完。
    若 QoS统计 的 第 0 项 的 延迟EMA 大于 5000 乘 1024：
        QoS_拥塞级别 为 3。
    否则若 QoS统计 的 第 0 项 的 延迟EMA 大于 2000 乘 1024：
        QoS_拥塞级别 为 2。
    否则若 QoS统计 的 第 0 项 的 延迟EMA 大于 1000 乘 1024：
        QoS_拥塞级别 为 1。
    否则：
        QoS_拥塞级别 为 0。
    完。
    若 QoS_拥塞级别 大于或等于 2：
        QoS统计 的 第 0 项 的 当前带宽权重 为 700。
        QoS统计 的 第 1 项 的 当前带宽权重 为 250。
        QoS统计 的 第 2 项 的 当前带宽权重 为 50。
    否则若 QoS_拥塞级别 等于 1：
        QoS统计 的 第 0 项 的 当前带宽权重 为 600。
        QoS统计 的 第 1 项 的 当前带宽权重 为 300。
        QoS统计 的 第 2 项 的 当前带宽权重 为 100。
    否则：
        i 为 0。
        只要 i 小于 QOS_类数：
            需求比 为 QoS统计 的 第 i 项 的 字节总数 乘 1000 除以 总流量。
            QoS统计 的 第 i 项 的 当前带宽权重 为 (默认权重 的 第 i 项 乘 800 加 需求比 乘 200) 除以 1000。
            i 为 i 加 1。
        完。
    完。
完。

AI网络QoS_分配带宽 接受 流量类型：
    若 非 QoS_已初始化 或者 流量类型 大于或等于 QOS_类数：
        返还 200。
    完。
    返还 QoS统计 的 第 流量类型 项 的 当前带宽权重。
完。

AI网络QoS_应该丢包 接受 流量类型：
    若 QoS_拥塞级别 大于或等于 3 并且 流量类型 等于 2：
        返还 真。
    完。
    若 QoS_拥塞级别 大于或等于 2 并且 流量类型 等于 2：
        QoS统计 的 第 2 项 的 丢包数 为 QoS统计 的 第 2 项 的 丢包数 加 1。
        返还 QoS统计 的 第 2 项 的 丢包数 取余于 2 等于 0。
    完。
    返还 假。
完。

AI网络QoS_拥塞级别 接受 零参：
    返还 QoS_拥塞级别。
完。

AI网络QoS_统计 接受 零参：
    返还 一本字典：实时带宽权重 对应 QoS统计 的 第 0 项 的 当前带宽权重、交互带宽权重 对应 QoS统计 的 第 1 项 的 当前带宽权重、批量带宽权重 对应 QoS统计 的 第 2 项 的 当前带宽权重、拥塞级别 对应 QoS_拥塞级别、实时延迟EMA 对应 QoS统计 的 第 0 项 的 延迟EMA 除以 1024。
完。

AI网络QoS_流量类名 接受 类型：
    若 类型 等于 0：
        返还 「realtime」。
    完。
    若 类型 等于 1：
        返还 「interactive」。
    完。
    若 类型 等于 2：
        返还 「bulk」。
    完。
    返还 「unknown」。
完。

AI网络QoS_拥塞级别名 接受 零参：
    若 QoS_拥塞级别 等于 0：
        返还 「none」。
    完。
    若 QoS_拥塞级别 等于 1：
        返还 「mild」。
    完。
    若 QoS_拥塞级别 等于 2：
        返还 「moderate」。
    完。
    若 QoS_拥塞级别 等于 3：
        返还 「severe」。
    完。
    返还 「unknown」。
完。

AI网络QoS_完整统计 接受 零参：
    返还 一组：QoS_拥塞级别、QoS统计 的 第 0 项 的 当前带宽权重、QoS统计 的 第 1 项 的 当前带宽权重、QoS统计 的 第 2 项 的 当前带宽权重、QoS统计 的 第 0 项 的 延迟EMA 除以 1024。
完。

本章 收束。
