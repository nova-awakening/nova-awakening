《账号信息生》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·人机验证突破》。

引用《.·硬链接》。

引用《.·内核打印》。

引用《.·整数转字符串》。

引用《..·标准·原生网络》。

账号信息 是 一种 事物，含 用户名、邮箱、密码、手机、姓名、生日。

可变 注册_随机种子 为 99999。

注册_伪随机 接受 零参：
    注册_随机种子 为 注册_随机种子 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    返还 注册_随机种子。
完。

账号_生成 接受 零参：
    info 为 新建 账号信息。
    前缀列表 为 一组：「nova」、「star」、「sky」、「moon」、「sun」、「wind」、「ocean」、「dream」。
    前缀 为 前缀列表 的 第 做 注册_伪随机 取余于 做 计数 用 前缀列表 项。
    数字 为 做 注册_伪随机 取余于 99999。
    info 的 用户名 为 前缀 加 做 整数转字符串 用 数字。
    邮箱域 为 一组：「gmail.com」、「outlook.com」、「yahoo.com」、「protonmail.com」。
    info 的 邮箱 为 info 的 用户名 加 「@」 加 邮箱域 的 第 做 注册_伪随机 取余于 做 计数 用 邮箱域 项。
    info 的 密码 为 做 生成安全密码 用 16。
    姓 为 一组：「Wang」、「Li」、「Zhang」、「Liu」、「Chen」、「Yang」、「Huang」、「Zhao」。
    名 为 一组：「Wei」、「Fang」、「Lei」、「Jie」、「Ming」、「Hao」、「Yu」、「Xin」。
    info 的 姓名 为 姓 的 第 做 注册_伪随机 取余于 做 计数 用 姓 项 加 「 」 加 名 的 第 做 注册_伪随机 取余于 做 计数 用 名 项。
    返还 info。
完。

生成安全密码 接受 长度：
    字符集 为 「abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%&*」。
    可变 密码 为 「」。
    可变 i 为 0。
    只要 i 小于 长度：
        idx 为 做 注册_伪随机 取余于 做 字符串长度 用 字符集。
        密码 为 密码 加 做 字符串_取字符 用 字符集、idx。
        i 为 i 加 1。
    完。
    返还 密码。
完。

验证码任务 是 一种 事物，含 类型、状态、图片数据、结果、尝试次数。

可变 验证码_总处理 为 0。

可变 验证码_总成功 为 0。

验证码_处理 接受 类型、元素：
    验证码_总处理 为 验证码_总处理 加 1。
    若 类型 等于 1：
        成功 为 做 验证_滑块 用 元素 的 x、元素 的 y、元素 的 x 加 200、元素 的 y。
        若 成功：
            验证码_总成功 为 验证码_总成功 加 1。
        完。
        返还 成功。
    否则若 类型 等于 2：
        返还 做 验证_图片选择 用 一组：（空）、「选择所有包含交通灯的图片」。
    否则若 类型 等于 3：
        返还 做 验证_reCAPTCHA_复选框 用 元素 的 x、元素 的 y。
    否则若 类型 等于 5：
        结果 为 做 验证_文字识别 用 一组：（空）。
        若 结果 不等于 空 并且 结果 不等于 「」：
            验证码_总成功 为 验证码_总成功 加 1。
            返还 真。
        完。
    完。
    返还 假。
完。

邮件 是 一种 事物，含 发件人、主题、正文、时间、验证链接。

可变 收件箱 为 一组：（空）。

邮箱验证_开始 接受 邮箱地址：
    做 内核调试 用 「邮箱验证: 监控 」 加 邮箱地址。
    可变 等待 为 0。
    只要 等待 小于 60：
        新邮件 为 做 邮箱_检查收件 用 邮箱地址。
        若 新邮件 不等于 空：
            链接 为 做 邮件_提取链接 用 新邮件 的 正文。
            若 链接 不等于 「」：
                做 内核调试 用 「邮箱验证: 找到验证链接 」 加 链接。
                做 标签页_新建 用 链接。
                返还 真。
            完。
        完。
        等待 为 等待 加 1。
    完。
    做 内核调试 用 「邮箱验证: 超时」。
    返还 假。
完。

邮箱_检查收件 接受 邮箱地址：
    若 邮箱地址 等于 0：
        返还 空。
    完。
    端点 为 做 字符串拼接 用 「https://api.mail.tm/messages?page=1&to=」、邮箱地址。
    响应 为 做 网络_HTTP_GET 用 端点、443、「/messages」。
    若 响应 等于 空：
        返还 空。
    完。
    返还 响应。
完。

邮件_提取链接 接受 正文：
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 正文 减 8：
        若 做 字符串取字节 用 正文、i 等于 104 并且 做 字符串取字节 用 正文、i 加 1 等于 116 并且 做 字符串取字节 用 正文、i 加 2 等于 116 并且 做 字符串取字节 用 正文、i 加 3 等于 112：
            可变 结束 为 i 加 4。
            只要 结束 小于 做 字符串长度 用 正文 并且 做 字符串取字节 用 正文、结束 不等于 32 并且 做 字符串取字节 用 正文、结束 不等于 34 并且 做 字符串取字节 用 正文、结束 不等于 10 并且 做 字符串取字节 用 正文、结束 不等于 60：
                结束 为 结束 加 1。
            完。
            链接 为 做 子字符串_注册 用 正文、i、结束。
            若 做 包含_注册 用 链接、「verify」 或者 做 包含_注册 用 链接、「confirm」 或者 做 包含_注册 用 链接、「activate」 或者 做 包含_注册 用 链接、「validate」：
                返还 链接。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

可变 AI_账号信息生_威胁计数 为 0。

AI_账号信息生_风险评分 接受 操作类型、权限级别、来源可信度：
    可变 风险 为 0。
    若 权限级别 大于 500：
        风险 为 风险 加 300。
    完。
    若 来源可信度 小于 200：
        风险 为 风险 加 400。
    完。
    若 风险 大于 500：
        AI_账号信息生_威胁计数 为 AI_账号信息生_威胁计数 加 1。
    完。
    返还 风险。
完。

AI_账号信息生_异常行为检测 接受 操作频率、基线频率：
    若 基线频率 大于 0 并且 操作频率 大于 基线频率 乘 5：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
