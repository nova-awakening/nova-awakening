《网络异常检测》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·规则匹配》。

引用《.·链表后继》。

引用《.·整数转字符串》。

智能_异常检测 接受 源IP、目标IP、目标端口、协议、传控标志：
    若 协议 等于 协议_传控 并且 传控标志 位与 0x02 不等于 0：
        智能_SYN计数 为 智能_SYN计数 加 1。
        若 智能_SYN计数 大于 智能_异常_SYN洪水阈值：
            做 内核警告 用 「防火墙AI: SYN洪水攻击检测! 」 加 做 IP转字符串 用 源IP 加 「 SYN/s=」 加 做 整数转字符串 用 智能_SYN计数。
            返还 动作_丢弃。
        完。
    完。
    扫描记录 为 做 查找端口扫描记录 用 源IP。
    若 扫描记录 不等于 空：
        若 非 做 端口已记录 用 扫描记录、目标端口：
            做 追加 用 扫描记录 的 第 1 项、目标端口。
            若 做 计数 用 扫描记录 的 第 1 项 大于 智能_异常_端口扫描阈值：
                做 内核警告 用 「防火墙AI: 端口扫描检测! 」 加 做 IP转字符串 用 源IP 加 「 扫描端口数=」 加 做 整数转字符串 用 做 计数 用 扫描记录 的 第 1 项。
                返还 动作_丢弃。
            完。
        完。
    否则：
        做 追加 用 智能_端口扫描检测、一组：源IP、一组：目标端口。
        若 做 计数 用 智能_端口扫描检测 大于 10000：
            做 链表_删除 用 智能_端口扫描检测、0。
        完。
    完。
    返还 动作_接受。
完。

智能_异常_重置tick 接受 零参：
    智能_SYN计数 为 0。
完。

查找端口扫描记录 接受 IP：
    可变 i 为 0。
    只要 i 小于 做 计数 用 智能_端口扫描检测：
        若 智能_端口扫描检测 的 第 i 项 的 第 0 项 等于 IP：
            返还 智能_端口扫描检测 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

端口已记录 接受 记录、端口：
    可变 i 为 0。
    只要 i 小于 做 计数 用 记录 的 第 1 项：
        若 记录 的 第 1 项 的 第 i 项 等于 端口：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

防火墙_添加规则_快速 接受 钩子、协议、源IP、源掩码、目标IP、目标掩码、源端口起、源端口止、目标端口起、目标端口止、动作、连接状态、优先级：
    规则 为 新建 防火墙规则。
    规则 的 规则ID 为 规则ID计数。
    规则ID计数 为 规则ID计数 加 1。
    规则 的 钩子点 为 钩子。
    规则 的 优先级 为 优先级。
    规则 的 启用 为 真。
    规则 的 源IP 为 源IP。
    规则 的 源IP掩码 为 源掩码。
    规则 的 目标IP 为 目标IP。
    规则 的 目标IP掩码 为 目标掩码。
    规则 的 源端口_起 为 源端口起。
    规则 的 源端口_止 为 源端口止。
    规则 的 目标端口_起 为 目标端口起。
    规则 的 目标端口_止 为 目标端口止。
    规则 的 协议 为 协议。
    规则 的 动作 为 动作。
    规则 的 连接状态 为 连接状态。
    规则 的 匹配次数 为 0。
    规则 的 匹配字节 为 0。
    做 追加 用 规则表 的 第 钩子 项、规则。
    返还 规则 的 规则ID。
完。

防火墙_删除规则 接受 规则ID：
    可变 h 为 0。
    只要 h 小于 钩子_数量：
        可变 i 为 0。
        只要 i 小于 做 计数 用 规则表 的 第 h 项：
            若 规则表 的 第 h 项 的 第 i 项 的 规则ID 等于 规则ID：
                做 链表_删除 用 规则表 的 第 h 项、i。
                返还 成功。
            完。
            i 为 i 加 1。
        完。
        h 为 h 加 1。
    完。
    返还 错误_不存在。
完。

防火墙_设置默认策略 接受 钩子、动作：
    若 钩子 大于或等于 0 并且 钩子 小于 钩子_数量：
        默认策略 的 第 钩子 项 为 动作。
    完。
完。

防火墙_启用 接受 零参：
    防火墙_已启用 为 真。
完。

防火墙_禁用 接受 零参：
    防火墙_已启用 为 假。
完。

防火墙_允许端口 接受 端口、协议：
    做 防火墙_添加规则_快速 用 钩子_INPUT、协议、0、0、0、0、0、0、端口、端口、动作_接受、0、100。
完。

防火墙_封锁IP 接受 IP：
    做 防火墙_添加规则_快速 用 钩子_INPUT、协议_任意、IP、0xFFFFFFFF、0、0、0、0、0、0、动作_丢弃、0、1。
完。

防火墙_允许子网 接受 网络、掩码：
    做 防火墙_添加规则_快速 用 钩子_INPUT、协议_任意、网络、掩码、0、0、0、0、0、0、动作_接受、0、50。
完。

本章 收束。
