《底层事件发送》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·内存初始化》。

引用《..·感知·内核直连层》。

引用《.·内容可见性》。

事件_同步 为 0。

事件_按键 为 1。

事件_相对 为 2。

事件_绝对 为 3。

最大事件队列 为 256。

最大输入设备 为 8。

按钮_左 为 0x110。

按钮_右 为 0x111。

按钮_中 为 0x112。

合并窗口 为 8。

去抖延迟 为 30。

长按阈值 为 500。

滑动阈值 为 50。

输入事件 是 一种 事物，含 时间戳、类型、代码、值。

输入设备 是 一种 事物，含 标识、类型掩码、活跃、事件数。

可变 事件队列 为 0。

可变 队列头 为 0。

可变 队列尾 为 0。

可变 队列大小 为 0。

可变 设备表 为 一组：（空）。

可变 设备数 为 0。

可变 发送总数 为 0。

可变 丢弃总数 为 0。

可变 合并总数 为 0。

可变 去抖总数 为 0。

可变 鼠标横 为 0。

可变 鼠标纵 为 0。

可变 路由_直接分发数 为 0。

可变 节流_拒绝数 为 0。

可变 上次事件类型 为 负 1。

可变 上次事件代码 为 负 1。

可变 上次事件时间 为 0。

可变 去抖_键码 为 负 1。

可变 去抖_时间 为 0。

可变 去抖_值 为 0。

手势_空闲 为 0。

手势_按下 为 1。

手势_移动 为 2。

可变 手势_状态 为 0。

可变 手势_起始横 为 0。

可变 手势_起始纵 为 0。

可变 手势_当前横 为 0。

可变 手势_当前纵 为 0。

可变 手势_起始时间 为 0。

可变 手势_识别总数 为 0。

输入_初始化 接受 零参：
    事件队列 为 做 堆_分配 用 最大事件队列 乘 24。
    队列头 为 0。
    队列尾 为 0。
    队列大小 为 0。
    设备表 为 一组：（空）。
    设备数 为 0。
    上次事件类型 为 负 1。
    去抖_键码 为 负 1。
    手势_状态 为 手势_空闲。
完。

输入_注册设备 接受 类型掩码：
    若 设备数 大于或等于 最大输入设备：
        返还 负 1。
    完。
    设备 为 新建 输入设备。
    设备 的 标识 为 设备数。
    设备 的 类型掩码 为 类型掩码。
    设备 的 活跃 为 真。
    设备 的 事件数 为 0。
    做 追加 用 设备表、设备。
    索引 为 设备数。
    设备数 为 设备数 加 1。
    返还 索引。
完。

输入_发送事件 接受 类型、代码、值：
    发送总数 为 发送总数 加 1。
    当前时间 为 做 平台_获取高精度时间。
    若 类型 等于 上次事件类型 并且 代码 等于 上次事件代码：
        若 当前时间 减 上次事件时间 小于 合并窗口 并且 类型 不等于 事件_同步：
            若 队列大小 大于 0：
                尾偏移 为 事件队列 加 (队列尾 减 1 加 最大事件队列) 取余于 最大事件队列 乘 24。
                做 内存_冉32 用 尾偏移 加 16、值。
                合并总数 为 合并总数 加 1。
                上次事件时间 为 当前时间。
                返还。
            完。
        完。
    完。
    若 队列大小 大于或等于 最大事件队列：
        丢弃总数 为 丢弃总数 加 1。
        返还。
    完。
    偏移 为 事件队列 加 队列尾 乘 24。
    做 内存_冉64 用 偏移、当前时间。
    做 内存_冉32 用 偏移 加 8、类型。
    做 内存_冉32 用 偏移 加 12、代码。
    做 内存_冉32 用 偏移 加 16、值。
    队列尾 为 (队列尾 加 1) 取余于 最大事件队列。
    队列大小 为 队列大小 加 1。
    上次事件类型 为 类型。
    上次事件代码 为 代码。
    上次事件时间 为 当前时间。
完。

输入_读事件 接受 输出偏移：
    若 队列大小 等于 0：
        返还 假。
    完。
    偏移 为 事件队列 加 队列头 乘 24。
    做 内存_复制 用 输出偏移、偏移、24。
    队列头 为 (队列头 加 1) 取余于 最大事件队列。
    队列大小 为 队列大小 减 1。
    返还 真。
完。

输入_发送鼠标移动 接受 横、纵：
    鼠标横 为 横。
    鼠标纵 为 纵。
    做 输入_发送事件 用 事件_绝对、0、横。
    做 输入_发送事件 用 事件_绝对、1、纵。
    做 输入_发送事件 用 事件_同步、0、0。
完。

输入_发送鼠标按钮 接受 横、纵、按钮、按下：
    做 输入_发送鼠标移动 用 横、纵。
    可变 值 为 0。
    若 按下：
        值 为 1。
    完。
    做 输入_发送事件 用 事件_按键、按钮_左 加 按钮 减 1、值。
    做 输入_发送事件 用 事件_同步、0、0。
完。

输入_发送按键 接受 键码、按下：
    当前时间 为 做 平台_获取高精度时间。
    可变 值 为 0。
    若 按下：
        值 为 1。
    完。
    若 键码 等于 去抖_键码 并且 值 等于 去抖_值：
        若 当前时间 减 去抖_时间 小于 去抖延迟：
            去抖总数 为 去抖总数 加 1。
            返还。
        完。
    完。
    去抖_键码 为 键码。
    去抖_值 为 值。
    去抖_时间 为 当前时间。
    做 输入_发送事件 用 事件_按键、键码、值。
    做 输入_发送事件 用 事件_同步、0、0。
完。

输入_发送滚轮 接受 横、纵、方向、量：
    做 输入_发送鼠标移动 用 横、纵。
    做 输入_发送事件 用 事件_相对、方向、量。
    做 输入_发送事件 用 事件_同步、0、0。
完。

输入_发送触摸 接受 横、纵、按下：
    鼠标横 为 横。
    鼠标纵 为 纵。
    当前时间 为 做 平台_获取高精度时间。
    若 按下：
        手势_状态 为 手势_按下。
        手势_起始横 为 横。
        手势_起始纵 为 纵。
        手势_当前横 为 横。
        手势_当前纵 为 纵。
        手势_起始时间 为 当前时间。
    否则：
        做 _手势_判定 用 当前时间。
        手势_状态 为 手势_空闲。
    完。
    可变 值 为 0。
    若 按下：
        值 为 1。
    完。
    做 输入_发送事件 用 事件_绝对、0、横。
    做 输入_发送事件 用 事件_绝对、1、纵。
    做 输入_发送事件 用 事件_按键、0x14A、值。
    做 输入_发送事件 用 事件_同步、0、0。
完。

输入_触摸移动 接受 横、纵：
    若 手势_状态 等于 手势_按下：
        手势_状态 为 手势_移动。
    完。
    手势_当前横 为 横。
    手势_当前纵 为 纵。
    做 输入_发送鼠标移动 用 横、纵。
完。

_手势_判定 接受 当前时间：
    持续 为 当前时间 减 手势_起始时间。
    可变 横距 为 手势_当前横 减 手势_起始横。
    可变 纵距 为 手势_当前纵 减 手势_起始纵。
    可变 绝横 为 横距。
    可变 绝纵 为 纵距。
    若 绝横 小于 0：
        绝横 为 0 减 绝横。
    完。
    若 绝纵 小于 0：
        绝纵 为 0 减 绝纵。
    完。
    距离 为 绝横 加 绝纵。
    手势_识别总数 为 手势_识别总数 加 1。
    若 距离 小于 滑动阈值：
        若 持续 大于或等于 长按阈值：
            返还 5。
        完。
        返还 1。
    完。
    若 绝横 大于 绝纵：
        若 横距 大于 0：
            返还 2。
        完。
        返还 3。
    完。
    若 纵距 大于 0：
        返还 4。
    完。
    返还 6。
完。

输入_获取手势 接受 零参：
    若 手势_状态 不等于 手势_空闲：
        返还 0。
    完。
    返还 做 _手势_判定 用 做 平台_获取高精度时间。
完。

输入_获取鼠标位置 接受 零参：
    返还 一组：鼠标横、鼠标纵。
完。

输入_队列中事件数 接受 零参：
    返还 队列大小。
完。

输入_批量注入 接受 事件列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 事件列表：
        ev 为 事件列表 的 第 i 项。
        做 输入_发送事件 用 ev 的 第 0 项、ev 的 第 1 项、ev 的 第 2 项。
        i 为 i 加 1。
    完。
    返还 做 计数 用 事件列表。
完。

输入_模拟打字 接受 键码列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 键码列表：
        做 输入_发送按键 用 键码列表 的 第 i 项、真。
        做 输入_发送按键 用 键码列表 的 第 i 项、假。
        i 为 i 加 1。
    完。
完。

输入_清空队列 接受 零参：
    队列头 为 0。
    队列尾 为 0。
    队列大小 为 0。
完。

优先级_即时 为 0。

优先级_高 为 1。

优先级_普通 为 2。

优先级_低 为 3。

输入_发送事件_带优先级 接受 类型、代码、值、优先级：
    发送总数 为 发送总数 加 1。
    若 优先级 等于 优先级_即时：
        路由_直接分发数 为 路由_直接分发数 加 1。
        做 事件_分发到监听器 用 类型、代码、值。
        返还。
    完。
    若 优先级 等于 优先级_低 并且 队列大小 大于或等于 最大事件队列 乘 3 除以 4：
        丢弃总数 为 丢弃总数 加 1。
        返还。
    完。
    做 输入_发送事件 用 类型、代码、值。
完。

最大监听器 为 32。

可变 监听器表 为 一组：（空）。

可变 监听器数 为 0。

可变 分发_总回调数 为 0。

事件监听器 是 一种 事物，含 类型过滤、回调、用户数据、活跃。

输入_注册监听器 接受 类型过滤、回调、用户数据：
    若 监听器数 大于或等于 最大监听器：
        返还 负 1。
    完。
    监听 为 新建 事件监听器。
    监听 的 类型过滤 为 类型过滤。
    监听 的 回调 为 回调。
    监听 的 用户数据 为 用户数据。
    监听 的 活跃 为 真。
    做 追加 用 监听器表、监听。
    索引 为 监听器数。
    监听器数 为 监听器数 加 1。
    返还 索引。
完。

输入_注销监听器 接受 索引：
    若 索引 大于或等于 0 并且 索引 小于 监听器数：
        监听器表 的 第 索引 项 的 活跃 为 假。
    完。
完。

事件_分发到监听器 接受 类型、代码、值：
    可变 i 为 0。
    只要 i 小于 监听器数：
        若 监听器表 的 第 i 项 的 活跃：
            若 监听器表 的 第 i 项 的 类型过滤 等于 负 1 或者 监听器表 的 第 i 项 的 类型过滤 等于 类型：
                做 监听器表 的 第 i 项 的 回调 用 类型、代码、值、监听器表 的 第 i 项 的 用户数据。
                分发_总回调数 为 分发_总回调数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

输入_分发队列事件 接受 零参：
    可变 临时 为 做 堆_分配 用 24。
    可变 处理数 为 0。
    只要 做 输入_读事件 用 临时：
        类型 为 做 内存_读32 用 临时 加 8。
        代码 为 做 内存_读32 用 临时 加 12。
        值 为 做 内存_读32 用 临时 加 16。
        做 事件_分发到监听器 用 类型、代码、值。
        处理数 为 处理数 加 1。
    完。
    做 堆_释放 用 临时。
    返还 处理数。
完。

节流_窗口毫秒 为 100。

节流_最大事件 为 50。

可变 节流_窗口开始 为 0。

可变 节流_窗口计数 为 0。

输入_节流检查 接受 零参：
    当前时间 为 做 平台_获取高精度时间。
    若 当前时间 减 节流_窗口开始 大于或等于 节流_窗口毫秒：
        节流_窗口开始 为 当前时间。
        节流_窗口计数 为 0。
        返还 真。
    完。
    若 节流_窗口计数 大于或等于 节流_最大事件：
        节流_拒绝数 为 节流_拒绝数 加 1。
        返还 假。
    完。
    节流_窗口计数 为 节流_窗口计数 加 1。
    返还 真。
完。

输入_发送事件_带节流 接受 类型、代码、值：
    若 做 输入_节流检查：
        做 输入_发送事件 用 类型、代码、值。
    完。
完。

最大录制事件 为 512。

可变 录制缓冲 为 一组：（空）。

可变 录制中 为 假。

可变 录制数量 为 0。

输入_开始录制 接受 零参：
    录制缓冲 为 一组：（空）。
    录制数量 为 0。
    录制中 为 真。
完。

输入_停止录制 接受 零参：
    录制中 为 假。
    返还 录制数量。
完。

输入_录制事件 接受 类型、代码、值：
    若 录制中 并且 录制数量 小于 最大录制事件：
        做 追加 用 录制缓冲、一组：做 平台_获取高精度时间、类型、代码、值。
        录制数量 为 录制数量 加 1。
    完。
完。

输入_回放录制 接受 零参：
    若 录制中：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 录制数量：
        记录 为 录制缓冲 的 第 i 项。
        做 输入_发送事件 用 记录 的 第 1 项、记录 的 第 2 项、记录 的 第 3 项。
        i 为 i 加 1。
    完。
完。

输入_获取录制数据 接受 零参：
    返还 录制缓冲。
完。

输入_注销设备 接受 设备标识：
    若 设备标识 大于或等于 0 并且 设备标识 小于 设备数：
        设备表 的 第 设备标识 项 的 活跃 为 假。
    完。
完。

输入_设备是否活跃 接受 设备标识：
    若 设备标识 大于或等于 0 并且 设备标识 小于 设备数：
        返还 设备表 的 第 设备标识 项 的 活跃。
    完。
    返还 假。
完。

输入_获取设备事件数 接受 设备标识：
    若 设备标识 大于或等于 0 并且 设备标识 小于 设备数：
        返还 设备表 的 第 设备标识 项 的 事件数。
    完。
    返还 0。
完。

输入_获取活跃设备数 接受 零参：
    可变 活跃 为 0。
    可变 i 为 0。
    只要 i 小于 设备数：
        若 设备表 的 第 i 项 的 活跃：
            活跃 为 活跃 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 活跃。
完。

输入_完整统计 接受 零参：
    返还 一组：一组：「发送总数」、发送总数、一组：「丢弃总数」、丢弃总数、一组：「合并总数」、合并总数、一组：「去抖总数」、去抖总数、一组：「队列大小」、队列大小、一组：「设备数」、设备数、一组：「手势识别」、手势_识别总数、一组：「监听器数」、监听器数、一组：「回调分发」、分发_总回调数、一组：「直接路由」、路由_直接分发数、一组：「节流拒绝」、节流_拒绝数、一组：「录制数量」、录制数量、一组：「鼠标位置」、一组：鼠标横、鼠标纵。
完。

本章 收束。
