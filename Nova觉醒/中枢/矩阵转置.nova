《矩阵转置》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·欧几里得模》。

引用《.·张量数学引擎》。

引用《.·内核打印》。

引用《.·整数转字符串》。

数学_精度 为 10000。

矩阵运算统计 是 一种 事物，含 转置次数、迹计算次数、哈达玛次数、行列式次数、逆矩阵次数、LU分解次数、向量点积次数、矩阵乘向量次数、批量运算次数。

可变 全局_矩阵统计 为 空。

矩阵统计_初始化 接受 零参：
    若 全局_矩阵统计 等于 空：
        全局_矩阵统计 为 新建 矩阵运算统计。
        全局_矩阵统计 的 转置次数 为 0。
        全局_矩阵统计 的 迹计算次数 为 0。
        全局_矩阵统计 的 哈达玛次数 为 0。
        全局_矩阵统计 的 行列式次数 为 0。
        全局_矩阵统计 的 逆矩阵次数 为 0。
        全局_矩阵统计 的 LU分解次数 为 0。
        全局_矩阵统计 的 向量点积次数 为 0。
        全局_矩阵统计 的 矩阵乘向量次数 为 0。
        全局_矩阵统计 的 批量运算次数 为 0。
    完。
完。

矩阵_转置 接受 m：
    做 矩阵统计_初始化。
    全局_矩阵统计 的 转置次数 为 全局_矩阵统计 的 转置次数 加 1。
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 列、行。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 结果、j、i、做 矩阵_取 用 m、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_原地转置 接受 m：
    n 为 m 的 第 0 项。
    若 n 不等于 m 的 第 1 项：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            tmp 为 做 矩阵_取 用 m、i、j。
            做 矩阵_设 用 m、i、j、做 矩阵_取 用 m、j、i。
            做 矩阵_设 用 m、j、i、tmp。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

矩阵_迹 接受 m：
    做 矩阵统计_初始化。
    全局_矩阵统计 的 迹计算次数 为 全局_矩阵统计 的 迹计算次数 加 1。
    n 为 m 的 第 0 项。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和 为 和 加 做 矩阵_取 用 m、i、i。
        i 为 i 加 1。
    完。
    返还 和。
完。

矩阵_哈达玛 接受 a、b：
    做 矩阵统计_初始化。
    全局_矩阵统计 的 哈达玛次数 为 全局_矩阵统计 的 哈达玛次数 加 1。
    行 为 a 的 第 0 项。
    列 为 a 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 行、列。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        结果 的 第 2 加 i 项 为 a 的 第 2 加 i 项 乘 b 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_行 接受 m、i：
    列 为 m 的 第 1 项。
    可变 行 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 列：
        做 追加 用 行、做 矩阵_取 用 m、i、j。
        j 为 j 加 1。
    完。
    返还 行。
完。

矩阵_列 接受 m、j：
    行 为 m 的 第 0 项。
    可变 列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 行：
        做 追加 用 列、做 矩阵_取 用 m、i、j。
        i 为 i 加 1。
    完。
    返还 列。
完。

矩阵_设行 接受 m、i、行数据：
    列 为 m 的 第 1 项。
    可变 j 为 0。
    只要 j 小于 列 并且 j 小于 做 计数 用 行数据：
        做 矩阵_设 用 m、i、j、行数据 的 第 j 项。
        j 为 j 加 1。
    完。
完。

矩阵_设列 接受 m、j、列数据：
    行 为 m 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 行 并且 i 小于 做 计数 用 列数据：
        做 矩阵_设 用 m、i、j、列数据 的 第 i 项。
        i 为 i 加 1。
    完。
完。

矩阵_交换行 接受 m、i1、i2：
    若 i1 等于 i2：
        返还。
    完。
    列 为 m 的 第 1 项。
    可变 j 为 0。
    只要 j 小于 列：
        tmp 为 做 矩阵_取 用 m、i1、j。
        做 矩阵_设 用 m、i1、j、做 矩阵_取 用 m、i2、j。
        做 矩阵_设 用 m、i2、j、tmp。
        j 为 j 加 1。
    完。
完。

矩阵_加 接受 a、b：
    行 为 a 的 第 0 项。
    列 为 a 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 行、列。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        结果 的 第 2 加 i 项 为 a 的 第 2 加 i 项 加 b 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_减 接受 a、b：
    行 为 a 的 第 0 项。
    列 为 a 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 行、列。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        结果 的 第 2 加 i 项 为 a 的 第 2 加 i 项 减 b 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_单位 接受 n：
    可变 m 为 做 矩阵_创建 用 n、n。
    可变 i 为 0。
    只要 i 小于 n：
        做 矩阵_设 用 m、i、i、数学_精度。
        i 为 i 加 1。
    完。
    返还 m。
完。

矩阵_零 接受 行、列：
    返还 做 矩阵_创建 用 行、列。
完。

矩阵_对角 接受 对角元素：
    n 为 做 计数 用 对角元素。
    可变 m 为 做 矩阵_创建 用 n、n。
    可变 i 为 0。
    只要 i 小于 n：
        做 矩阵_设 用 m、i、i、对角元素 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 m。
完。

矩阵_是方阵 接受 m：
    返还 m 的 第 0 项 等于 m 的 第 1 项。
完。

矩阵_是对称 接受 m：
    若 非 做 矩阵_是方阵 用 m：
        返还 假。
    完。
    n 为 m 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 做 矩阵_取 用 m、i、j 不等于 做 矩阵_取 用 m、j、i：
                返还 假。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

矩阵_Frobenius范数平方 接受 m：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        和 为 和 加 m 的 第 2 加 i 项 乘 m 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

矩阵_行列式_2x2 接受 m：
    返还 做 矩阵_取 用 m、0、0 乘 做 矩阵_取 用 m、1、1 减 做 矩阵_取 用 m、0、1 乘 做 矩阵_取 用 m、1、0。
完。

矩阵_行列式_3x3 接受 m：
    a 为 做 矩阵_取 用 m、0、0。
    b 为 做 矩阵_取 用 m、0、1。
    c 为 做 矩阵_取 用 m、0、2。
    d 为 做 矩阵_取 用 m、1、0。
    e 为 做 矩阵_取 用 m、1、1。
    f 为 做 矩阵_取 用 m、1、2。
    g 为 做 矩阵_取 用 m、2、0。
    h 为 做 矩阵_取 用 m、2、1。
    k 为 做 矩阵_取 用 m、2、2。
    返还 a 乘 (e 乘 k 减 f 乘 h) 减 b 乘 (d 乘 k 减 f 乘 g) 加 c 乘 (d 乘 h 减 e 乘 g)。
完。

矩阵_行列式 接受 m：
    做 矩阵统计_初始化。
    全局_矩阵统计 的 行列式次数 为 全局_矩阵统计 的 行列式次数 加 1。
    n 为 m 的 第 0 项。
    若 n 等于 1：
        返还 做 矩阵_取 用 m、0、0。
    完。
    若 n 等于 2：
        返还 做 矩阵_行列式_2x2 用 m。
    完。
    若 n 等于 3：
        返还 做 矩阵_行列式_3x3 用 m。
    完。
    可变 det 为 0。
    可变 符号 为 1。
    可变 j 为 0。
    只要 j 小于 n：
        子矩阵 为 做 矩阵_创建 用 n 减 1、n 减 1。
        可变 si 为 0。
        可变 i 为 1。
        只要 i 小于 n：
            可变 sj 为 0。
            可变 jj 为 0。
            只要 jj 小于 n：
                若 jj 不等于 j：
                    做 矩阵_设 用 子矩阵、si、sj、做 矩阵_取 用 m、i、jj。
                    sj 为 sj 加 1。
                完。
                jj 为 jj 加 1。
            完。
            si 为 si 加 1。
            i 为 i 加 1。
        完。
        det 为 det 加 符号 乘 做 矩阵_取 用 m、0、j 乘 做 矩阵_行列式 用 子矩阵。
        符号 为 0 减 符号。
        j 为 j 加 1。
    完。
    返还 det。
完。

矩阵_逆_2x2 接受 m：
    做 矩阵统计_初始化。
    全局_矩阵统计 的 逆矩阵次数 为 全局_矩阵统计 的 逆矩阵次数 加 1。
    det 为 做 矩阵_行列式_2x2 用 m。
    若 det 等于 0：
        返还 空。
    完。
    可变 结果 为 做 矩阵_创建 用 2、2。
    做 矩阵_设 用 结果、0、0、做 矩阵_取 用 m、1、1 乘 数学_精度 除以 det。
    做 矩阵_设 用 结果、0、1、(0 减 做 矩阵_取 用 m、0、1) 乘 数学_精度 除以 det。
    做 矩阵_设 用 结果、1、0、(0 减 做 矩阵_取 用 m、1、0) 乘 数学_精度 除以 det。
    做 矩阵_设 用 结果、1、1、做 矩阵_取 用 m、0、0 乘 数学_精度 除以 det。
    返还 结果。
完。

矩阵_LU分解 接受 m：
    做 矩阵统计_初始化。
    全局_矩阵统计 的 LU分解次数 为 全局_矩阵统计 的 LU分解次数 加 1。
    n 为 m 的 第 0 项。
    可变 L 为 做 矩阵_单位 用 n。
    可变 U 为 做 矩阵_创建 用 n、n。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 n：
            做 矩阵_设 用 U、i、j、做 矩阵_取 用 m、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 k 为 i 加 1。
        只要 k 小于 n：
            主元 为 做 矩阵_取 用 U、i、i。
            若 主元 等于 0：
                k 为 n。
                i 为 i 加 1。
                继续。
            完。
            因子 为 做 矩阵_取 用 U、k、i 乘 数学_精度 除以 主元。
            做 矩阵_设 用 L、k、i、因子。
            可变 j 为 i。
            只要 j 小于 n：
                旧值 为 做 矩阵_取 用 U、k、j。
                减值 为 因子 乘 做 矩阵_取 用 U、i、j 除以 数学_精度。
                做 矩阵_设 用 U、k、j、旧值 减 减值。
                j 为 j 加 1。
            完。
            k 为 k 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：L、U。
完。

矩阵_秩 接受 m：
    LU 为 做 矩阵_LU分解 用 m。
    U 为 LU 的 第 1 项。
    n 为 U 的 第 0 项。
    列 为 U 的 第 1 项。
    可变 秩 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 非零 为 假。
        可变 j 为 0。
        只要 j 小于 列：
            v 为 做 矩阵_取 用 U、i、j。
            若 v 大于 0 或者 v 小于 0：
                非零 为 真。
                j 为 列。
            完。
            j 为 j 加 1。
        完。
        若 非零：
            秩 为 秩 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 秩。
完。

向量_范数平方 接受 v：
    返还 做 向量_点积 用 v、v。
完。

向量_加 接受 a、b：
    n 为 做 计数 用 a。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、a 的 第 i 项 加 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_减 接受 a、b：
    n 为 做 计数 用 a。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、a 的 第 i 项 减 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_数乘 接受 v、s：
    n 为 做 计数 用 v。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、v 的 第 i 项 乘 s。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_归一化 接受 v：
    范数sq 为 做 向量_范数平方 用 v。
    若 范数sq 等于 0：
        返还 v。
    完。
    范数 为 做 平方根 用 范数sq。
    若 范数 等于 0：
        返还 v。
    完。
    返还 做 向量_数乘 用 v、数学_精度 除以 范数。
完。

向量_叉积_3D 接受 a、b：
    若 做 计数 用 a 不等于 3 或者 做 计数 用 b 不等于 3：
        返还 一组：0、0、0。
    完。
    返还 一组：a 的 第 1 项 乘 b 的 第 2 项 减 a 的 第 2 项 乘 b 的 第 1 项、a 的 第 2 项 乘 b 的 第 0 项 减 a 的 第 0 项 乘 b 的 第 2 项、a 的 第 0 项 乘 b 的 第 1 项 减 a 的 第 1 项 乘 b 的 第 0 项。
完。

向量_投影 接受 v、方向：
    点 为 做 向量_点积 用 v、方向。
    范数sq 为 做 向量_范数平方 用 方向。
    若 范数sq 等于 0：
        返还 做 向量_数乘 用 v、0。
    完。
    返还 做 向量_数乘 用 方向、点 除以 范数sq。
完。

向量_外积 接受 a、b：
    行 为 做 计数 用 a。
    列 为 做 计数 用 b。
    可变 m 为 做 矩阵_创建 用 行、列。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 m、i、j、a 的 第 i 项 乘 b 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 m。
完。

矩阵_向量乘 接受 m、v：
    做 矩阵统计_初始化。
    全局_矩阵统计 的 矩阵乘向量次数 为 全局_矩阵统计 的 矩阵乘向量次数 加 1。
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 行：
        可变 和 为 0。
        可变 j 为 0。
        只要 j 小于 列：
            和 为 和 加 做 矩阵_取 用 m、i、j 乘 v 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_余弦相似度 接受 a、b：
    点 为 做 向量_点积 用 a、b。
    范数a 为 做 平方根 用 做 向量_范数平方 用 a。
    范数b 为 做 平方根 用 做 向量_范数平方 用 b。
    若 范数a 等于 0 或者 范数b 等于 0：
        返还 0。
    完。
    返还 点 乘 数学_精度 除以 (范数a 乘 范数b)。
完。

向量_曼哈顿距离 接受 a、b：
    n 为 做 计数 用 a。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        d 为 a 的 第 i 项 减 b 的 第 i 项。
        若 d 小于 0：
            d 为 0 减 d。
        完。
        和 为 和 加 d。
        i 为 i 加 1。
    完。
    返还 和。
完。

向量_欧氏距离平方 接受 a、b：
    差 为 做 向量_减 用 a、b。
    返还 做 向量_范数平方 用 差。
完。

矩阵_批量向量乘 接受 m、向量列表：
    做 矩阵统计_初始化。
    全局_矩阵统计 的 批量运算次数 为 全局_矩阵统计 的 批量运算次数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 向量列表：
        做 追加 用 结果、做 矩阵_向量乘 用 m、向量列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量组_正交化 接受 向量组：
    n 为 做 计数 用 向量组。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 正交基 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 u 为 向量组 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 正交基：
            投影 为 做 向量_投影 用 u、正交基 的 第 j 项。
            u 为 做 向量_减 用 u、投影。
            j 为 j 加 1。
        完。
        范数sq 为 做 向量_范数平方 用 u。
        若 范数sq 大于 0：
            做 追加 用 正交基、做 向量_归一化 用 u。
        完。
        i 为 i 加 1。
    完。
    返还 正交基。
完。

矩阵_一范数 接受 m：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 最大列和 为 0。
    可变 j 为 0。
    只要 j 小于 列：
        可变 列和 为 0。
        可变 i 为 0。
        只要 i 小于 行：
            v 为 做 矩阵_取 用 m、i、j。
            若 v 小于 0：
                v 为 0 减 v。
            完。
            列和 为 列和 加 v。
            i 为 i 加 1。
        完。
        若 列和 大于 最大列和：
            最大列和 为 列和。
        完。
        j 为 j 加 1。
    完。
    返还 最大列和。
完。

矩阵_无穷范数 接受 m：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 最大行和 为 0。
    可变 i 为 0。
    只要 i 小于 行：
        可变 行和 为 0。
        可变 j 为 0。
        只要 j 小于 列：
            v 为 做 矩阵_取 用 m、i、j。
            若 v 小于 0：
                v 为 0 减 v。
            完。
            行和 为 行和 加 v。
            j 为 j 加 1。
        完。
        若 行和 大于 最大行和：
            最大行和 为 行和。
        完。
        i 为 i 加 1。
    完。
    返还 最大行和。
完。

矩阵_条件数_2x2 接受 m：
    逆 为 做 矩阵_逆_2x2 用 m。
    若 逆 等于 空：
        返还 0x7FFFFFFF。
    完。
    返还 做 矩阵_一范数 用 m 乘 做 矩阵_一范数 用 逆 除以 数学_精度。
完。

矩阵_最大特征值_估计 接受 m、最大迭代：
    n 为 m 的 第 0 项。
    若 n 不等于 m 的 第 1 项：
        返还 0。
    完。
    可变 b 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 b、数学_精度。
        i 为 i 加 1。
    完。
    b 为 做 向量_归一化 用 b。
    可变 迭代 为 0。
    只要 迭代 小于 最大迭代：
        新b 为 做 矩阵_向量乘 用 m、b。
        范数sq 为 做 向量_范数平方 用 新b。
        若 范数sq 等于 0：
            返还 0。
        完。
        b 为 做 向量_归一化 用 新b。
        迭代 为 迭代 加 1。
    完。
    Ab 为 做 矩阵_向量乘 用 m、b。
    特征值 为 做 向量_点积 用 b、Ab。
    范数 为 做 向量_范数平方 用 b。
    若 范数 等于 0：
        返还 0。
    完。
    返还 特征值 除以 范数。
完。

矩阵_稀疏度 接受 m：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    总数 为 行 乘 列。
    若 总数 等于 0：
        返还 0。
    完。
    可变 零数 为 0。
    可变 i 为 0。
    只要 i 小于 总数：
        若 m 的 第 2 加 i 项 等于 0：
            零数 为 零数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 零数 乘 100 除以 总数。
完。

矩阵_是对角占优 接受 m：
    若 非 做 矩阵_是方阵 用 m：
        返还 假。
    完。
    n 为 m 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 n：
        可变 对角 为 做 矩阵_取 用 m、i、i。
        若 对角 小于 0：
            对角 为 0 减 对角。
        完。
        可变 行和 为 0。
        可变 j 为 0。
        只要 j 小于 n：
            若 j 不等于 i：
                v 为 做 矩阵_取 用 m、i、j。
                若 v 小于 0：
                    v 为 0 减 v。
                完。
                行和 为 行和 加 v。
            完。
            j 为 j 加 1。
        完。
        若 对角 小于或等于 行和：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

矩阵_打印统计 接受 零参：
    做 矩阵统计_初始化。
    做 打印行 用 「=== 矩阵运算统计 ===」。
    做 打印行 用 「转置: 」 加 做 整数转字符串 用 全局_矩阵统计 的 转置次数。
    做 打印行 用 「迹: 」 加 做 整数转字符串 用 全局_矩阵统计 的 迹计算次数。
    做 打印行 用 「哈达玛: 」 加 做 整数转字符串 用 全局_矩阵统计 的 哈达玛次数。
    做 打印行 用 「行列式: 」 加 做 整数转字符串 用 全局_矩阵统计 的 行列式次数。
    做 打印行 用 「逆矩阵: 」 加 做 整数转字符串 用 全局_矩阵统计 的 逆矩阵次数。
    做 打印行 用 「LU分解: 」 加 做 整数转字符串 用 全局_矩阵统计 的 LU分解次数。
    做 打印行 用 「点积: 」 加 做 整数转字符串 用 全局_矩阵统计 的 向量点积次数。
    做 打印行 用 「矩阵乘向量: 」 加 做 整数转字符串 用 全局_矩阵统计 的 矩阵乘向量次数。
    做 打印行 用 「批量运算: 」 加 做 整数转字符串 用 全局_矩阵统计 的 批量运算次数。
完。

可变 AI_矩阵转置_调用数 为 0。

可变 AI_矩阵转置_异常数 为 0。

AI_矩阵转置_监控 接受 零参：
    AI_矩阵转置_调用数 为 AI_矩阵转置_调用数 加 1。
    返还 AI_矩阵转置_异常数 等于 0。
完。

AI_矩阵转置_健康检查 接受 零参：
    若 AI_矩阵转置_调用数 大于 0 并且 AI_矩阵转置_异常数 乘 100 除以 AI_矩阵转置_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
