《错误码》

引用《.·类型定义》。

引用《..·推理·通信渠道》。

引用《.·内核打印》。

引用《..·推理·定点Q算术》。

引用《.·整数转字符串》。

成功 为 0。

错误_操作不允许 为 0 减 1。

错误_不存在 为 0 减 2。

错误_无此进程 为 0 减 3。

错误_中断 为 0 减 4。

错误_IO错误 为 0 减 5。

错误_无此设备 为 0 减 6。

错误_参数过多 为 0 减 7。

错误_格式错误 为 0 减 8。

错误_坏文件描述符 为 0 减 9。

错误_无子进程 为 0 减 10。

错误_重试 为 0 减 11。

错误_无内存 为 0 减 12。

错误_无权限 为 0 减 13。

错误_坏地址 为 0 减 14。

错误_设备忙 为 0 减 16。

错误_已存在 为 0 减 17。

错误_跨设备链接 为 0 减 18。

错误_无设备 为 0 减 19。

错误_非目录 为 0 减 20。

错误_是目录 为 0 减 21。

错误_无效参数 为 0 减 22。

错误_文件表满 为 0 减 23。

错误_打开过多 为 0 减 24。

错误_不是终端 为 0 减 25。

错误_文本忙 为 0 减 26。

错误_文件过大 为 0 减 27。

错误_无空间 为 0 减 28。

错误_非法寻址 为 0 减 29。

错误_只读文件系统 为 0 减 30。

错误_链接过多 为 0 减 31。

错误_管道断裂 为 0 减 32。

错误_域错误 为 0 减 33。

错误_结果超范围 为 0 减 34。

错误_死锁 为 0 减 35。

错误_名称过长 为 0 减 36。

错误_无锁可用 为 0 减 37。

错误_ENOSYS 为 0 减 38。

错误_目录非空 为 0 减 39。

错误_符号链环 为 0 减 40。

错误_不是socket 为 0 减 88。

错误_消息过长 为 0 减 90。

错误_协议类型错 为 0 减 91。

错误_无协议选项 为 0 减 92。

错误_不支持协议 为 0 减 93。

错误_不支持socket类型 为 0 减 94。

错误_不支持操作 为 0 减 95。

错误_不支持协议族 为 0 减 97。

错误_地址已用 为 0 减 98。

错误_地址不可用 为 0 减 99。

错误_网络已断 为 0 减 100。

错误_网络不可达 为 0 减 101。

错误_网络连接重置 为 0 减 102。

错误_连接异常终止 为 0 减 103。

错误_连接重置 为 0 减 104。

错误_无缓冲空间 为 0 减 105。

错误_已连接 为 0 减 106。

错误_未连接 为 0 减 107。

错误_连接超时 为 0 减 110。

错误_连接拒绝 为 0 减 111。

错误_主机已断 为 0 减 112。

错误_主机不可达 为 0 减 113。

错误_连接进行中 为 0 减 115。

错误_IPC通道已满 为 0 减 200。

错误_IPC通道已关 为 0 减 201。

错误_IPC超时 为 0 减 202。

错误_IPC消息过大 为 0 减 203。

错误_IPC无接收者 为 0 减 204。

错误_IPC协议不匹配 为 0 减 205。

错误_信号量溢出 为 0 减 206。

错误_共享内存失败 为 0 减 207。

错误_认证失败 为 0 减 300。

错误_授权失败 为 0 减 301。

错误_令牌过期 为 0 减 302。

错误_令牌无效 为 0 减 303。

错误_访问被拒 为 0 减 304。

错误_沙箱违规 为 0 减 305。

错误_密钥无效 为 0 减 306。

错误_证书过期 为 0 减 307。

错误_完整性校验失败 为 0 减 308。

错误_超出访问频率 为 0 减 309。

错误_AI总线未连接 为 0 减 400。

错误_AI模型加载失败 为 0 减 401。

错误_AI推理超时 为 0 减 402。

错误_AI输入无效 为 0 减 403。

错误_AI输出异常 为 0 减 404。

错误_AI置信度低 为 0 减 405。

错误_AI资源耗尽 为 0 减 406。

错误_AI模型不存在 为 0 减 407。

错误_AI训练失败 为 0 减 408。

错误_AI特征异常 为 0 减 409。

错误_驱动未加载 为 0 减 500。

错误_驱动初始化失败 为 0 减 501。

错误_设备未就绪 为 0 减 502。

错误_设备已移除 为 0 减 503。

错误_DMA失败 为 0 减 504。

错误_中断失败 为 0 减 505。

错误_固件错误 为 0 减 506。

错误_电源异常 为 0 减 507。

错误_GPU故障 为 0 减 508。

错误_显存不足 为 0 减 509。

错误_权限 为 0 减 1。

错误_被中断 为 0 减 4。

错误_内存不足 为 0 减 12。

错误_无效地址 为 0 减 14。

错误_地址 为 0 减 14。

错误_忙 为 0 减 16。

错误_无资源 为 0 减 19。

错误_不支持设备 为 0 减 19。

错误_IO 为 0 减 5。

错误_超时 为 0 减 110。

错误_未初始化 为 0 减 99。

错误_拒绝连接 为 0 减 111。

错误_目标不匹配 为 0 减 113。

错误_资源临时不可用 为 0 减 11。

是错误 接受 值：
    返还 值 小于 0。
完。

是成功 接受 值：
    返还 值 大于或等于 0。
完。

错误_分类 接受 码：
    若 码 大于或等于 0：
        返还 「成功」。
    完。
    绝对值 为 0 减 码。
    域 为 绝对值 除以 100。
    若 域 等于 0：
        返还 「基本」。
    完。
    若 域 等于 1：
        返还 「网络」。
    完。
    若 域 等于 2：
        返还 「IPC」。
    完。
    若 域 等于 3：
        返还 「安全」。
    完。
    若 域 等于 4：
        返还 「AI」。
    完。
    若 域 等于 5：
        返还 「驱动」。
    完。
    返还 「未知域」。
完。

错误_严重度 接受 码：
    若 码 大于或等于 0：
        返还 0。
    完。
    a 为 0 减 码。
    若 a 等于 12 或者 a 等于 14 或者 a 等于 508 或者 a 等于 509：
        返还 950。
    完。
    若 a 等于 5 或者 a 等于 35 或者 a 等于 300 或者 a 等于 400：
        返还 750。
    完。
    若 a 等于 1 或者 a 等于 13 或者 a 等于 110 或者 a 等于 111：
        返还 500。
    完。
    若 a 等于 2 或者 a 等于 4 或者 a 等于 11 或者 a 等于 22：
        返还 200。
    完。
    返还 400。
完。

错误_是可重试 接受 码：
    若 码 等于 错误_重试：
        返还 真。
    完。
    若 码 等于 错误_中断：
        返还 真。
    完。
    若 码 等于 错误_连接超时：
        返还 真。
    完。
    若 码 等于 错误_连接重置：
        返还 真。
    完。
    若 码 等于 错误_无缓冲空间：
        返还 真。
    完。
    若 码 等于 错误_AI推理超时：
        返还 真。
    完。
    若 码 等于 错误_IPC超时：
        返还 真。
    完。
    返还 假。
完。

错误_是网络相关 接受 码：
    若 码 大于或等于 0：
        返还 假。
    完。
    a 为 0 减 码。
    返还 a 大于或等于 88 并且 a 小于或等于 115。
完。

错误_是AI相关 接受 码：
    若 码 大于或等于 0：
        返还 假。
    完。
    a 为 0 减 码。
    返还 a 大于或等于 400 并且 a 小于或等于 499。
完。

错误码名称 接受 码：
    若 码 等于 0：
        返还 「成功」。
    完。
    若 码 等于 0 减 1：
        返还 「操作不允许」。
    完。
    若 码 等于 0 减 2：
        返还 「不存在」。
    完。
    若 码 等于 0 减 3：
        返还 「无此进程」。
    完。
    若 码 等于 0 减 4：
        返还 「被中断」。
    完。
    若 码 等于 0 减 5：
        返还 「IO错误」。
    完。
    若 码 等于 0 减 6：
        返还 「无此设备」。
    完。
    若 码 等于 0 减 7：
        返还 「参数过多」。
    完。
    若 码 等于 0 减 8：
        返还 「格式错误」。
    完。
    若 码 等于 0 减 9：
        返还 「坏文件描述符」。
    完。
    若 码 等于 0 减 10：
        返还 「无子进程」。
    完。
    若 码 等于 0 减 11：
        返还 「资源临时不可用」。
    完。
    若 码 等于 0 减 12：
        返还 「无内存」。
    完。
    若 码 等于 0 减 13：
        返还 「无权限」。
    完。
    若 码 等于 0 减 14：
        返还 「坏地址」。
    完。
    若 码 等于 0 减 16：
        返还 「设备忙」。
    完。
    若 码 等于 0 减 17：
        返还 「已存在」。
    完。
    若 码 等于 0 减 19：
        返还 「无设备」。
    完。
    若 码 等于 0 减 20：
        返还 「非目录」。
    完。
    若 码 等于 0 减 21：
        返还 「是目录」。
    完。
    若 码 等于 0 减 22：
        返还 「无效参数」。
    完。
    若 码 等于 0 减 28：
        返还 「无空间」。
    完。
    若 码 等于 0 减 30：
        返还 「只读文件系统」。
    完。
    若 码 等于 0 减 32：
        返还 「管道断裂」。
    完。
    若 码 等于 0 减 35：
        返还 「死锁」。
    完。
    若 码 等于 0 减 38：
        返还 「功能未接入」。
    完。
    若 码 等于 0 减 88：
        返还 「不是socket」。
    完。
    若 码 等于 0 减 98：
        返还 「地址已用」。
    完。
    若 码 等于 0 减 101：
        返还 「网络不可达」。
    完。
    若 码 等于 0 减 104：
        返还 「连接重置」。
    完。
    若 码 等于 0 减 106：
        返还 「已连接」。
    完。
    若 码 等于 0 减 107：
        返还 「未连接」。
    完。
    若 码 等于 0 减 110：
        返还 「连接超时」。
    完。
    若 码 等于 0 减 111：
        返还 「连接拒绝」。
    完。
    若 码 等于 0 减 113：
        返还 「主机不可达」。
    完。
    若 码 等于 0 减 200：
        返还 「IPC通道已满」。
    完。
    若 码 等于 0 减 201：
        返还 「IPC通道已关」。
    完。
    若 码 等于 0 减 202：
        返还 「IPC超时」。
    完。
    若 码 等于 0 减 300：
        返还 「认证失败」。
    完。
    若 码 等于 0 减 301：
        返还 「授权失败」。
    完。
    若 码 等于 0 减 302：
        返还 「令牌过期」。
    完。
    若 码 等于 0 减 305：
        返还 「沙箱违规」。
    完。
    若 码 等于 0 减 400：
        返还 「AI总线未连接」。
    完。
    若 码 等于 0 减 401：
        返还 「AI模型加载失败」。
    完。
    若 码 等于 0 减 402：
        返还 「AI推理超时」。
    完。
    若 码 等于 0 减 406：
        返还 「AI资源耗尽」。
    完。
    若 码 等于 0 减 500：
        返还 「驱动未加载」。
    完。
    若 码 等于 0 减 508：
        返还 「GPU故障」。
    完。
    若 码 等于 0 减 509：
        返还 「显存不足」。
    完。
    返还 「未知错误」。
完。

NERC_最大栈深度 为 16。

错误链节点 是 一种 事物，含 错误码、模块名、函数名、行号、时间戳、描述、下一个。

错误链_创建 接受 码、模块、函数、行、描述：
    返还 新建 错误链节点：错误码 为 码、模块名 为 模块、函数名 为 以 零参 为 引，其做法是：、行号 为 行、时间戳 为 做 系统_时间戳、描述 为 描述、下一个 为 0。
完。

错误链_包装 接受 外层码、模块、函数、行、描述、内层错误：
    节点 为 做 错误链_创建 用 外层码、模块、以 零参 为 引，其做法是：、行、描述。
    节点 的 下一个 为 内层错误。
    返还 节点。
完。

错误链_打印 接受 链：
    若 链 等于 0：
        返还。
    完。
    可变 当前 为 链。
    可变 深度 为 0。
    只要 当前 不等于 0 并且 深度 小于 NERC_最大栈深度：
        名 为 做 错误码名称 用 当前 的 错误码。
        做 内核打印 用 「  [{}/{}] 错误({}): {} @{}:{}」、深度、当前 的 模块名、当前 的 错误码、名、当前 的 函数名、当前 的 行号。
        若 当前 的 描述 不等于 0：
            做 内核打印 用 「    描述: {}」、当前 的 描述。
        完。
        当前 为 当前 的 下一个。
        深度 为 深度 加 1。
    完。
完。

错误链_根因 接受 链：
    若 链 等于 0：
        返还 0。
    完。
    可变 当前 为 链。
    只要 当前 的 下一个 不等于 0：
        当前 为 当前 的 下一个。
    完。
    返还 当前 的 错误码。
完。

NERC统计 是 一种 事物，含 总错误数、基本错误数、网络错误数、IPC错误数、安全错误数、AI错误数、驱动错误数、致命错误数、重试成功数、最后错误码、最后错误时间。

可变 全局NERC统计 为 新建 NERC统计。

NERC_记录错误 接受 码：
    若 码 大于或等于 0：
        返还。
    完。
    全局NERC统计 的 总错误数 为 全局NERC统计 的 总错误数 加 1。
    全局NERC统计 的 最后错误码 为 码。
    全局NERC统计 的 最后错误时间 为 做 系统_时间戳。
    a 为 0 减 码。
    域 为 a 除以 100。
    若 域 等于 0：
        全局NERC统计 的 基本错误数 为 全局NERC统计 的 基本错误数 加 1。
    否则若 域 等于 1：
        全局NERC统计 的 网络错误数 为 全局NERC统计 的 网络错误数 加 1。
    否则若 域 等于 2：
        全局NERC统计 的 IPC错误数 为 全局NERC统计 的 IPC错误数 加 1。
    否则若 域 等于 3：
        全局NERC统计 的 安全错误数 为 全局NERC统计 的 安全错误数 加 1。
    否则若 域 等于 4：
        全局NERC统计 的 AI错误数 为 全局NERC统计 的 AI错误数 加 1。
    否则若 域 等于 5：
        全局NERC统计 的 驱动错误数 为 全局NERC统计 的 驱动错误数 加 1。
    完。
    若 做 错误_严重度 用 码 大于 800：
        全局NERC统计 的 致命错误数 为 全局NERC统计 的 致命错误数 加 1。
    完。
完。

NERC_记录重试成功 接受 零参：
    全局NERC统计 的 重试成功数 为 全局NERC统计 的 重试成功数 加 1。
完。

NERC_获取统计 接受 零参：
    返还 全局NERC统计。
完。

NERC_重置统计 接受 零参：
    全局NERC统计 为 新建 NERC统计。
完。

AI_NERC_诊断错误 接受 码：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 做 NERC_规则诊断 用 码。
    完。
    t 为 做 Tensor_创建 用 6。
    做 Tensor_写 用 t、0、0 减 码。
    分类 为 做 错误_分类 用 码。
    若 分类 等于 「基本」：
        做 Tensor_写 用 t、1、100。
    否则若 分类 等于 「网络」：
        做 Tensor_写 用 t、1、200。
    否则若 分类 等于 「IPC」：
        做 Tensor_写 用 t、1、300。
    否则若 分类 等于 「安全」：
        做 Tensor_写 用 t、1、400。
    否则若 分类 等于 「AI」：
        做 Tensor_写 用 t、1、500。
    否则：
        做 Tensor_写 用 t、1、600。
    完。
    做 Tensor_写 用 t、2、做 错误_严重度 用 码。
    若 做 错误_是可重试 用 码：
        做 Tensor_写 用 t、3、1000。
    否则：
        做 Tensor_写 用 t、3、0。
    完。
    s 为 全局NERC统计。
    若 s 的 总错误数 大于 10：
        做 Tensor_写 用 t、4、s 的 致命错误数 乘 1000 除以 s 的 总错误数。
    否则：
        做 Tensor_写 用 t、4、0。
    完。
    做 Tensor_写 用 t、5、500。
    分数 为 做 AI总线_推理 用 bus、「nerc_error_diagnosis」、t。
    做 Tensor_释放 用 t。
    若 分数 大于 800：
        返还 「系统故障:建议重启相关服务」。
    完。
    若 分数 大于 600：
        返还 「配置问题:检查参数与环境」。
    完。
    若 分数 大于 400：
        返还 「临时故障:建议重试」。
    完。
    返还 「用户操作错误:检查输入参数」。
完。

NERC_规则诊断 接受 码：
    若 码 等于 错误_无内存 或者 码 等于 错误_显存不足：
        返还 「内存耗尽:释放缓存或增加交换分区」。
    完。
    若 码 等于 错误_无空间：
        返还 「磁盘空间不足:清理日志或临时文件」。
    完。
    若 做 错误_是网络相关 用 码：
        返还 「网络错误:检查网络连接状态」。
    完。
    若 做 错误_是AI相关 用 码：
        返还 「AI子系统错误:检查模型状态和资源」。
    完。
    若 做 错误_是可重试 用 码：
        返还 「可重试错误:建议指数退避重试」。
    完。
    返还 「未知错误:查看错误链获取更多信息」。
完。

NERC_诊断报告 接受 零参：
    s 为 全局NERC统计。
    做 内核打印 用 「╔═══════════════════════════════════════╗」。
    做 内核打印 用 「║   NERC 错误码子系统诊断报告       ║」。
    做 内核打印 用 「╠═══════════════════════════════════════╣」。
    做 内核打印 用 「║ 总错误数: {}」、s 的 总错误数。
    做 内核打印 用 「║ 基本: {}  网络: {}  IPC: {}」、s 的 基本错误数、s 的 网络错误数、s 的 IPC错误数。
    做 内核打印 用 「║ 安全: {}  AI: {}  驱动: {}」、s 的 安全错误数、s 的 AI错误数、s 的 驱动错误数。
    做 内核打印 用 「║ 致命错误: {}  重试成功: {}」、s 的 致命错误数、s 的 重试成功数。
    若 s 的 最后错误码 不等于 0：
        做 内核打印 用 「║ 最后错误: {} ({})」、做 错误码名称 用 s 的 最后错误码、s 的 最后错误码。
    完。
    做 内核打印 用 「╚═══════════════════════════════════════╝」。
完。

错误码描述 接受 码：
    若 码 等于 0：
        返还 「操作成功完成」。
    完。
    若 码 等于 0 减 1：
        返还 「当前用户/进程无权执行此操作」。
    完。
    若 码 等于 0 减 2：
        返还 「指定的文件或目录不存在」。
    完。
    若 码 等于 0 减 5：
        返还 「硬件设备或文件系统IO操作失败」。
    完。
    若 码 等于 0 减 11：
        返还 「资源临时不可用，请稍后重试」。
    完。
    若 码 等于 0 减 12：
        返还 「系统内存不足，无法分配请求的内存」。
    完。
    若 码 等于 0 减 13：
        返还 「权限不足，访问被拒绝」。
    完。
    若 码 等于 0 减 22：
        返还 「传入的参数无效或超出范围」。
    完。
    若 码 等于 0 减 28：
        返还 「磁盘空间不足，无法写入」。
    完。
    若 码 等于 0 减 38：
        返还 「请求的功能尚未接入」。
    完。
    若 码 等于 0 减 110：
        返还 「连接超时，服务器未在规定时间内响应」。
    完。
    若 码 等于 0 减 111：
        返还 「目标服务器拒绝连接」。
    完。
    若 码 等于 0 减 400：
        返还 「AI总线未初始化，无法进行推理」。
    完。
    若 码 等于 0 减 402：
        返还 「AI推理超时，模型计算时间超过阈值」。
    完。
    若 码 等于 0 减 508：
        返还 「GPU硬件发生故障，无法继续渲染」。
    完。
    若 码 等于 0 减 509：
        返还 「显存不足，无法分配纹理/帧缓冲」。
    完。
    返还 「未知错误(码=」 加 做 整数转字符串 用 码 加 「)」。
完。

NERC_自愈尝试 接受 码：
    若 码 等于 错误_无内存：
        做 内核打印 用 「[NERC自愈] 尝试释放内存缓存...」。
        返还 真。
    完。
    若 做 错误_是可重试 用 码：
        做 内核打印 用 「[NERC自愈] 可重试错误，建议重试操作」。
        返还 真。
    完。
    若 码 等于 错误_AI总线未连接：
        做 内核打印 用 「[NERC自愈] 尝试重新初始化AI总线...」。
        返还 真。
    完。
    返还 假。
完。

NERC_初始化 接受 零参：
    全局NERC统计 为 新建 NERC统计。
    做 内核打印 用 「[NERC] 错误码子系统已初始化」。
完。

可变 _谐振错误_频率表 为 一组：（空）。

可变 _谐振错误_总次数 为 0。

谐振错误_记录 接受 错误码：
    _谐振错误_总次数 为 _谐振错误_总次数 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _谐振错误_频率表：
        若 _谐振错误_频率表 的 第 i 项 的 码 等于 错误码：
            _谐振错误_频率表 的 第 i 项 的 次数 为 _谐振错误_频率表 的 第 i 项 的 次数 加 1。
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 _谐振错误_频率表、一本字典：码 对应 错误码、次数 对应 1。
完。

谐振错误_最常见 接受 前N：
    可变 结果 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 前N 并且 k 小于 做 计数 用 _谐振错误_频率表：
        可变 最大idx 为 k。
        可变 j 为 k 加 1。
        只要 j 小于 做 计数 用 _谐振错误_频率表：
            若 _谐振错误_频率表 的 第 j 项 的 次数 大于 _谐振错误_频率表 的 第 最大idx 项 的 次数：
                最大idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最大idx 不等于 k：
            临时 为 _谐振错误_频率表 的 第 k 项。
            _谐振错误_频率表 的 第 k 项 为 _谐振错误_频率表 的 第 最大idx 项。
            _谐振错误_频率表 的 第 最大idx 项 为 临时。
        完。
        做 追加 用 结果、_谐振错误_频率表 的 第 k 项。
        k 为 k 加 1。
    完。
    返还 结果。
完。

错误码_完整统计 接受 零参：
    返还 一组：「错误总次数」、_谐振错误_总次数、「错误种类数」、做 计数 用 _谐振错误_频率表。
完。

错误码_统计 接受 零参：
    返还 一本字典：模块 对应 「错误码」、全局NERC统计 对应 全局NERC统计、谐振错误_频率表 对应 _谐振错误_频率表、谐振错误_总次数 对应 _谐振错误_总次数。
完。

错误码_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 _谐振错误_频率表 大于 100：
        做 追加 用 问题、「_谐振错误_频率表过高: 」 加 做 整数转字符串 用 做 计数 用 _谐振错误_频率表。
    完。
    若 _谐振错误_总次数 大于 100：
        做 追加 用 问题、「_谐振错误_总次数过高: 」 加 做 整数转字符串 用 _谐振错误_总次数。
    完。
    返还 做 计数 用 问题 等于 0。
完。

错误码_综合报告 接受 零参：
    做 打印 用 「[错误码] ═══ 综合报告 ═══」。
    做 打印 用 「  全局NERC统计: 」 加 若 全局NERC统计 不等于 空 则 「已初始化」 否则 「未初始化」。
    做 打印 用 「  谐振错误_频率表: 」 加 做 整数转字符串 用 做 计数 用 _谐振错误_频率表。
    做 打印 用 「  谐振错误_总次数: 」 加 做 整数转字符串 用 _谐振错误_总次数。
    做 打印 用 「  健康: 」 加 若 做 错误码_健康评估 则 「正常」 否则 「异常」。
完。

错误码_全局重置 接受 零参：
    _谐振错误_频率表 为 一组：（空）。
    _谐振错误_总次数 为 0。
    做 打印 用 「[错误码] 全局统计已重置」。
    返还 0。
完。

本章 收束。
