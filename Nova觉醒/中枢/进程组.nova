《进程组》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·链表后继》。

引用《.·进程描述符》。

引用《.·每处理器》。

引用《.·整数转字符串》。

进程组 是 一种 事物，含 pgid、leader、成员、会话。

会话 是 一种 事物，含 sid、leader、控制终端、前台组。

可变 进程组表 为 一组：（空）。

可变 会话表 为 一组：（空）。

进程组_创建 接受 pgid、leader：
    pg 为 新建 进程组。
    pg 的 pgid 为 pgid。
    pg 的 leader 为 leader。
    pg 的 成员 为 一组：pgid。
    pg 的 会话 为 空。
    做 追加 用 进程组表、pg。
    返还 pg。
完。

进程组_查找 接受 pgid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 进程组表：
        若 进程组表 的 第 i 项 的 pgid 等于 pgid：
            返还 进程组表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

进程组_添加成员 接受 pgid、pid：
    pg 为 做 进程组_查找 用 pgid。
    若 pg 等于 空：
        返还 错误_不存在。
    完。
    做 追加 用 pg 的 成员、pid。
    返还 成功。
完。

进程组_移除成员 接受 pgid、pid：
    pg 为 做 进程组_查找 用 pgid。
    若 pg 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 pg 的 成员：
        若 pg 的 成员 的 第 i 项 等于 pid：
            做 链表_删除 用 pg 的 成员、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

do_setpgid 接受 pid、pgid：
    若 pid 等于 0：
        pid 为 做 获取当前PID。
    完。
    若 pgid 等于 0：
        pgid 为 pid。
    完。
    pg 为 做 进程组_查找 用 pgid。
    若 pg 等于 空：
        pg 为 做 进程组_创建 用 pgid、空。
    完。
    做 进程组_添加成员 用 pgid、pid。
    返还 成功。
完。

do_getpgid 接受 pid：
    若 pid 等于 0：
        pid 为 做 获取当前PID。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 进程组表：
        pg 为 进程组表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 pg 的 成员：
            若 pg 的 成员 的 第 j 项 等于 pid：
                返还 pg 的 pgid。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 pid。
完。

do_setsid 接受 零参：
    pid 为 做 获取当前PID。
    pg 为 做 进程组_查找 用 pid。
    若 pg 不等于 空 并且 pg 的 pgid 等于 pid：
        返还 错误_权限。
    完。
    s 为 新建 会话。
    s 的 sid 为 pid。
    s 的 leader 为 做 获取当前进程。
    s 的 控制终端 为 空。
    s 的 前台组 为 pid。
    做 追加 用 会话表、s。
    做 进程组_创建 用 pid、做 获取当前进程。
    返还 pid。
完。

sys_getsid 接受 pid：
    若 pid 等于 0：
        pid 为 做 获取当前PID。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 会话表：
        若 会话表 的 第 i 项 的 sid 等于 pid：
            返还 会话表 的 第 i 项 的 sid。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

向进程组发送信号 接受 pgid、信号：
    pg 为 做 进程组_查找 用 pgid。
    若 pg 等于 空：
        返还 错误_不存在。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 pg 的 成员：
        i 为 i 加 1。
    完。
    返还 成功。
完。

外部。

当前进程。

获取当前PID 接受 零参：
    若 当前进程 等于 空：
        返还 0。
    完。
    返还 做 进程_PID 用 当前进程。
完。

进程组_统计 接受 零参：
    返还 一组：做 计数 用 进程组表、做 计数 用 会话表。
完。

进程组_进程退出 接受 pid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 进程组表：
        做 进程组_移除成员 用 进程组表 的 第 i 项 的 pgid、pid。
        若 做 计数 用 进程组表 的 第 i 项 的 成员 等于 0：
            做 链表_删除 用 进程组表、i。
            继续。
        完。
        i 为 i 加 1。
    完。
完。

会话_设置前台组 接受 sid、pgid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 会话表：
        若 会话表 的 第 i 项 的 sid 等于 sid：
            会话表 的 第 i 项 的 前台组 为 pgid。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    返还 错误_不存在。
完。

会话_获取前台组 接受 sid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 会话表：
        若 会话表 的 第 i 项 的 sid 等于 sid：
            返还 会话表 的 第 i 项 的 前台组。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

进程组_是孤儿 接受 pgid：
    pg 为 做 进程组_查找 用 pgid。
    若 pg 等于 空：
        返还 真。
    完。
    返还 做 计数 用 pg 的 成员 等于 0。
完。

本章 收束。
