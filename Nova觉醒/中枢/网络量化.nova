《网络量化》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·打乱》。

可变 量化_调用总数 为 0。

可变 量化_累积误差 为 0。

可变 量化_最大误差 为 0。

可变 量化_自适应位数 为 8。

可变 量化_保真度 为 1000。

可变 量化_最近压缩质量 为 1000。

可变 量化_最近样本数 为 0。

可变 量化_最近位宽 为 8。

可变 损失_调用总数 为 0。

可变 损失_EMA 为 0。

可变 量化_最近零计数 为 0。

可变 量化_最近稀疏度 为 0。

可变 量化_最近敏感总和 为 0。

可变 量化_最近平均位宽 为 8。

量化_七态_状态数 为 7。

量化_七态_理论熵位 为 2807。

量化_七态_最大值 为 3。

量化_七态_最小值 为 负 3。

量化_中文密度比 为 1465。

量化_中文最优位宽 为 320。

量化_英文最优位宽 为 410。

量化_极坐标_方向位 为 4。

量化_极坐标_幅值位 为 4。

量化_极坐标_总位 为 8。

量化_JL投影_源维度 为 32。

量化_JL投影_目标维度 为 8。

量化_级数 接受 位数：
    bits 为 做 钳制 用 位数、1、16。
    可变 级数 为 1。
    可变 b 为 0。
    只要 b 小于 bits：
        级数 为 级数 乘 2。
        b 为 b 加 1。
    完。
    返还 级数。
完。

量化_对称恢复 接受 值、最大abs、半级：
    若 最大abs 等于 0 或者 半级 等于 0：
        返还 0。
    完。
    q_int 为 值 乘 半级 除以 最大abs。
    返还 q_int 乘 最大abs 除以 半级。
完。

网络量化_更新摘要 接受 零计数、敏感总和、平均位宽、稀疏度：
    量化_最近零计数 为 零计数。
    量化_最近敏感总和 为 敏感总和。
    量化_最近平均位宽 为 平均位宽。
    量化_最近稀疏度 为 稀疏度。
完。

网络量化_压缩质量 接受 原始、量化后：
    n 为 做 计数 用 原始。
    m 为 做 计数 用 量化后。
    若 n 等于 0 或者 m 等于 0：
        量化_最近压缩质量 为 0。
        量化_最近样本数 为 0。
        返还 0。
    完。
    可变 i 为 0。
    可变 原能量 为 0。
    可变 压能量 为 0。
    可变 差和 为 0。
    可变 基准和 为 0。
    可变 非零 为 0。
    只要 i 小于 n 并且 i 小于 m：
        原值 为 原始 的 第 i 项。
        压值 为 量化后 的 第 i 项。
        原绝对 为 原值。
        压绝对 为 压值。
        若 原绝对 小于 0：
            原绝对 为 0 减 原绝对。
        完。
        若 压绝对 小于 0：
            压绝对 为 0 减 压绝对。
        完。
        若 压值 不等于 0：
            非零 为 非零 加 1。
        完。
        原能量 为 原能量 加 原绝对。
        压能量 为 压能量 加 压绝对。
        差 为 原值 减 压值。
        若 差 小于 0：
            差 为 0 减 差。
        完。
        差和 为 差和 加 差。
        基准和 为 基准和 加 原绝对 加 压绝对 加 1。
        i 为 i 加 1。
    完。
    若 i 等于 0：
        量化_最近压缩质量 为 0。
        量化_最近样本数 为 0。
        返还 0。
    完。
    比例分 为 0。
    若 原能量 大于 0 并且 压能量 大于 0：
        小值 为 原能量。
        大值 为 压能量。
        若 压能量 小于 原能量：
            小值 为 压能量。
            大值 为 原能量。
        完。
        比例分 为 小值 乘 1000 除以 大值。
    否则若 原能量 等于 0 并且 压能量 等于 0：
        比例分 为 1000。
    完。
    误差分 为 1000 减 差和 乘 1000 除以 基准和。
    若 误差分 小于 0：
        误差分 为 0。
    完。
    覆盖分 为 非零 乘 1000 除以 m。
    若 覆盖分 大于 1000：
        覆盖分 为 1000。
    完。
    质量 为 比例分 乘 0.35 加 误差分 乘 0.45 加 覆盖分 乘 0.20。
    若 质量 小于 0：
        质量 为 0。
    完。
    若 质量 大于 1000：
        质量 为 1000。
    完。
    量化_最近压缩质量 为 质量。
    量化_最近样本数 为 i。
    返还 质量。
完。

量化_熵估计位宽 接受 参数：
    n 为 做 计数 用 参数。
    若 n 等于 0：
        返还 8。
    完。
    桶数 为 16。
    可变 最小 为 参数 的 第 0 项。
    可变 最大 为 参数 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 n：
        若 参数 的 第 i 项 小于 最小：
            最小 为 参数 的 第 i 项。
        完。
        若 参数 的 第 i 项 大于 最大：
            最大 为 参数 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    范围 为 最大 减 最小。
    若 范围 等于 0：
        返还 2。
    完。
    可变 桶 为 一组：（空）。
    可变 b 为 0。
    只要 b 小于 桶数：
        做 追加 用 桶、0。
        b 为 b 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        idx 为 (参数 的 第 i 项 减 最小) 乘 (桶数 减 1) 除以 范围。
        若 idx 小于 0：
            idx 为 0。
        完。
        若 idx 大于或等于 桶数：
            idx 为 桶数 减 1。
        完。
        桶 的 第 idx 项 为 桶 的 第 idx 项 加 1。
        i 为 i 加 1。
    完。
    可变 熵估计 为 0。
    i 为 0。
    只要 i 小于 桶数：
        若 桶 的 第 i 项 大于 0：
            p 为 桶 的 第 i 项 乘 1000 除以 n。
            可变 log2p 为 0。
            可变 x 为 p。
            只要 x 大于 1：
                log2p 为 log2p 加 1。
                x 为 x 除以 2。
            完。
            熵估计 为 熵估计 加 p 乘 (10 减 log2p)。
        完。
        i 为 i 加 1。
    完。
    熵估计 为 熵估计 除以 1000。
    位宽 为 熵估计 除以 100 加 1。
    若 位宽 小于 2：
        位宽 为 2。
    完。
    若 位宽 大于 16：
        位宽 为 16。
    完。
    返还 位宽。
完。

量化_自适应 接受 参数、目标保真度：
    量化_自适应位数 为 做 量化_熵估计位宽 用 参数。
    结果 为 做 量化 用 参数、量化_自适应位数。
    若 量化_保真度 小于 目标保真度 并且 量化_自适应位数 小于 16：
        量化_自适应位数 为 量化_自适应位数 加 1。
    完。
    若 量化_保真度 大于 目标保真度 加 50 并且 量化_自适应位数 大于 4：
        量化_自适应位数 为 量化_自适应位数 减 1。
    完。
    返还 结果。
完。

MSE损失 接受 预测、真实：
    若 做 计数 用 预测 等于 0：
        返还 0。
    完。
    损失_调用总数 为 损失_调用总数 加 1。
    可变 损失 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测 并且 i 小于 做 计数 用 真实：
        d 为 预测 的 第 i 项 减 真实 的 第 i 项。
        损失 为 损失 加 d 乘 d 除以 网络精度。
        i 为 i 加 1。
    完。
    r 为 损失 除以 做 计数 用 预测。
    损失_EMA 为 (损失_EMA 乘 900 加 r 乘 100) 除以 1000。
    返还 r。
完。

MAE损失 接受 预测、真实：
    若 做 计数 用 预测 等于 0：
        返还 0。
    完。
    损失_调用总数 为 损失_调用总数 加 1。
    可变 损失 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测 并且 i 小于 做 计数 用 真实：
        可变 d 为 预测 的 第 i 项 减 真实 的 第 i 项。
        若 d 小于 0：
            d 为 0 减 d。
        完。
        损失 为 损失 加 d。
        i 为 i 加 1。
    完。
    返还 损失 除以 做 计数 用 预测。
完。

Huber损失 接受 预测、真实、δ：
    若 做 计数 用 预测 等于 0：
        返还 0。
    完。
    损失_调用总数 为 损失_调用总数 加 1。
    可变 损失 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测 并且 i 小于 做 计数 用 真实：
        可变 d 为 预测 的 第 i 项 减 真实 的 第 i 项。
        若 d 小于 0：
            d 为 0 减 d。
        完。
        若 d 小于或等于 δ：
            损失 为 损失 加 d 乘 d 除以 (2 乘 网络精度)。
        否则：
            损失 为 损失 加 δ 乘 (d 减 δ 除以 2) 除以 网络精度。
        完。
        i 为 i 加 1。
    完。
    返还 损失 除以 做 计数 用 预测。
完。

焦点损失 接受 概率、标签索引、α权重、γ聚焦：
    若 标签索引 大于或等于 做 计数 用 概率：
        返还 10000。
    完。
    损失_调用总数 为 损失_调用总数 加 1。
    可变 p 为 概率 的 第 标签索引 项。
    若 p 小于或等于 0：
        p 为 1。
    完。
    若 p 大于 1000：
        p 为 1000。
    完。
    可变 log_val 为 0。
    可变 x 为 p。
    只要 x 小于 1000：
        log_val 为 log_val 加 1。
        x 为 x 乘 2。
    完。
    neg_log 为 log_val 乘 70。
    one_minus_p 为 1000 减 p。
    可变 focal_w 为 one_minus_p。
    若 γ聚焦 大于或等于 2000：
        focal_w 为 one_minus_p 乘 one_minus_p 除以 1000。
    完。
    返还 α权重 乘 focal_w 乘 neg_log 除以 1000000。
完。

交叉熵损失 接受 Nova归一化指数输出、标签索引：
    p 为 Nova归一化指数输出 的 第 标签索引 项。
    若 p 小于或等于 0：
        返还 10000。
    完。
    可变 log_val 为 0。
    可变 x 为 p。
    只要 x 小于 1000：
        log_val 为 log_val 加 1。
        x 为 x 乘 2。
    完。
    返还 log_val 乘 70。
完。

参数_统计 接受 参数：
    n 为 做 计数 用 参数。
    若 n 等于 0：
        返还 一组：0、0、0、0、0。
    完。
    可变 最小 为 参数 的 第 0 项。
    可变 最大 为 参数 的 第 0 项。
    可变 和 为 0。
    可变 和平方 为 0。
    可变 零计数 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 参数 的 第 i 项 小于 最小：
            最小 为 参数 的 第 i 项。
        完。
        若 参数 的 第 i 项 大于 最大：
            最大 为 参数 的 第 i 项。
        完。
        和 为 和 加 参数 的 第 i 项。
        和平方 为 和平方 加 参数 的 第 i 项 乘 参数 的 第 i 项 除以 网络精度。
        若 参数 的 第 i 项 等于 0：
            零计数 为 零计数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：最小、最大、和 除以 n、做 网络_开方 用 和平方 除以 n、零计数 乘 1000 除以 n。
完。

可变 梯度缓冲 为 一组：（空）。

可变 梯度_步数 为 0。

梯度_累积目标 为 4。

梯度_累积 接受 新梯度：
    若 做 计数 用 梯度缓冲 等于 0：
        梯度缓冲 为 新梯度。
        梯度_步数 为 1。
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 新梯度 并且 i 小于 做 计数 用 梯度缓冲：
        梯度缓冲 的 第 i 项 为 梯度缓冲 的 第 i 项 加 新梯度 的 第 i 项。
        i 为 i 加 1。
    完。
    梯度_步数 为 梯度_步数 加 1。
    返还 梯度_步数 大于或等于 梯度_累积目标。
完。

梯度_获取平均 接受 零参：
    若 梯度_步数 等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度缓冲：
        做 网络_追加 用 结果、梯度缓冲 的 第 i 项 除以 梯度_步数。
        i 为 i 加 1。
    完。
    梯度缓冲 为 一组：（空）。
    梯度_步数 为 0。
    返还 结果。
完。

Conv1D 接受 输入、核、步幅：
    输入长 为 做 计数 用 输入。
    核长 为 做 计数 用 核。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 核长 小于或等于 输入长：
        可变 sum 为 0。
        可变 j 为 0。
        只要 j 小于 核长：
            sum 为 sum 加 输入 的 第 i 加 j 项 乘 核 的 第 j 项。
            j 为 j 加 1。
        完。
        做 网络_追加 用 输出、sum。
        i 为 i 加 步幅。
    完。
    返还 输出。
完。

激活_SiLU 接受 t：
    若 t 等于 0：
        返还。
    完。
    总 为 t 的 行数 乘 t 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        x 为 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        gate 为 做 激活_HexSigmoid_单值 用 x。
        做 内存_写32 用 t 的 数据 加 i 乘 4、做 Q乘 用 x、gate。
        i 为 i 加 1。
    完。
完。

Conv1D_BN 接受 输入、核、步幅：
    conv 为 做 Conv1D 用 输入、核、步幅。
    n 为 做 计数 用 conv。
    若 n 等于 0：
        返还 conv。
    完。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 conv 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 n：
        d 为 conv 的 第 i 项 减 均值。
        方差 为 方差 加 d 乘 d 除以 1000。
        i 为 i 加 1。
    完。
    可变 标准差 为 做 网络_开方 用 方差 除以 n 乘 31。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 网络_追加 用 结果、(conv 的 第 i 项 减 均值) 乘 1000 除以 标准差。
        i 为 i 加 1。
    完。
    返还 结果。
完。

校准_桶数 为 128。

可变 校准_样本数 为 0。

校准状态 是 一种 事物，含 最小值、最大值、p99_下、p99_上、桶、样本数。

量化_校准_初始化 接受 零参：
    st 为 做 堆_分配 用 校准状态。
    st 的 最小值 为 999999999。
    st 的 最大值 为 负 999999999。
    st 的 p99_下 为 0。
    st 的 p99_上 为 0。
    st 的 桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 校准_桶数：
        做 网络_追加 用 st 的 桶、0。
        i 为 i 加 1。
    完。
    st 的 样本数 为 0。
    返还 st。
完。

量化_校准_收集 接受 st、参数：
    n 为 做 计数 用 参数。
    若 n 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        若 参数 的 第 i 项 小于 st 的 最小值：
            st 的 最小值 为 参数 的 第 i 项。
        完。
        若 参数 的 第 i 项 大于 st 的 最大值：
            st 的 最大值 为 参数 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    范围 为 st 的 最大值 减 st 的 最小值。
    若 范围 小于或等于 0：
        返还。
    完。
    i 为 0。
    只要 i 小于 n：
        idx 为 (参数 的 第 i 项 减 st 的 最小值) 乘 (校准_桶数 减 1) 除以 范围。
        若 idx 小于 0：
            idx 为 0。
        完。
        若 idx 大于或等于 校准_桶数：
            idx 为 校准_桶数 减 1。
        完。
        st 的 桶 的 第 idx 项 为 st 的 桶 的 第 idx 项 加 1。
        i 为 i 加 1。
    完。
    st 的 样本数 为 st 的 样本数 加 n。
    校准_样本数 为 校准_样本数 加 n。
完。

量化_校准_百分位 接受 st：
    若 st 的 样本数 等于 0：
        返还。
    完。
    下目标 为 st 的 样本数 乘 5 除以 1000。
    上目标 为 st 的 样本数 乘 995 除以 1000。
    范围 为 st 的 最大值 减 st 的 最小值。
    可变 累计 为 0。
    可变 i 为 0。
    只要 i 小于 校准_桶数：
        累计 为 累计 加 st 的 桶 的 第 i 项。
        若 累计 大于或等于 下目标 并且 st 的 p99_下 等于 0：
            st 的 p99_下 为 st 的 最小值 加 范围 乘 i 除以 (校准_桶数 减 1)。
        完。
        若 累计 大于或等于 上目标 并且 st 的 p99_上 等于 0：
            st 的 p99_上 为 st 的 最小值 加 范围 乘 i 除以 (校准_桶数 减 1)。
        完。
        i 为 i 加 1。
    完。
完。

量化_校准_获取范围 接受 st、模式：
    若 模式 等于 0：
        返还 一组：st 的 最小值、st 的 最大值。
    完。
    若 模式 等于 1：
        做 量化_校准_百分位 用 st。
        返还 一组：st 的 p99_下、st 的 p99_上。
    完。
    做 量化_校准_百分位 用 st。
    返还 一组：st 的 p99_下、st 的 p99_上。
完。

最大层数 为 64。

可变 层位宽 为 一组：（空）。

可变 层敏感度 为 一组：（空）。

混合精度_初始化 接受 层数：
    层位宽 为 一组：（空）。
    层敏感度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 层数 并且 i 小于 最大层数：
        做 网络_追加 用 层位宽、8。
        做 网络_追加 用 层敏感度、500。
        i 为 i 加 1。
    完。
完。

混合精度_敏感度分析 接受 层索引、参数：
    若 层索引 大于或等于 做 计数 用 层敏感度：
        返还。
    完。
    q8 为 做 量化 用 参数、8。
    q4 为 做 量化 用 参数、4。
    可变 mse8 为 0。
    可变 mse4 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        d8 为 参数 的 第 i 项 减 q8 的 第 i 项。
        d4 为 参数 的 第 i 项 减 q4 的 第 i 项。
        mse8 为 mse8 加 d8 乘 d8 除以 1000。
        mse4 为 mse4 加 d4 乘 d4 除以 1000。
        i 为 i 加 1。
    完。
    若 做 计数 用 参数 大于 0：
        mse8 为 mse8 除以 做 计数 用 参数。
        mse4 为 mse4 除以 做 计数 用 参数。
    完。
    若 mse8 大于 0：
        层敏感度 的 第 层索引 项 为 (mse4 减 mse8) 乘 1000 除以 mse8。
    完。
完。

混合精度_分配位宽 接受 目标平均位宽：
    可变 层数 为 做 计数 用 层敏感度。
    若 层数 等于 0：
        返还。
    完。
    可变 敏感_总和 为 0。
    可变 总位宽 为 0。
    可变 i 为 0。
    只要 i 小于 层数：
        敏感_总和 为 敏感_总和 加 层敏感度 的 第 i 项。
        i 为 i 加 1。
    完。
    敏感_均值 为 敏感_总和 除以 层数。
    i 为 0。
    只要 i 小于 层数：
        若 敏感_均值 小于或等于 0：
            层位宽 的 第 i 项 为 8。
        否则若 层敏感度 的 第 i 项 大于 敏感_均值 乘 3 除以 2：
            层位宽 的 第 i 项 为 16。
        否则若 层敏感度 的 第 i 项 大于 敏感_均值 除以 2：
            层位宽 的 第 i 项 为 8。
        否则：
            层位宽 的 第 i 项 为 4。
        完。
        总位宽 为 总位宽 加 层位宽 的 第 i 项。
        i 为 i 加 1。
    完。
    做 网络量化_更新摘要 用 量化_最近零计数、敏感_总和、总位宽 除以 层数、量化_最近稀疏度。
完。

混合精度_量化层 接受 参数、层索引：
    bits 为 8。
    若 层索引 小于 做 计数 用 层位宽：
        bits 为 层位宽 的 第 层索引 项。
    完。
    返还 做 量化 用 参数、bits。
完。

QAT_前向 接受 参数、位数：
    返还 做 量化 用 参数、位数。
完。

QAT_反向 接受 梯度、参数、位数：
    n 为 做 计数 用 梯度。
    若 n 等于 0：
        返还 梯度。
    完。
    可变 最大abs 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        可变 v 为 参数 的 第 i 项。
        若 v 小于 0：
            v 为 0 减 v。
        完。
        若 v 大于 最大abs：
            最大abs 为 v。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n 并且 i 小于 做 计数 用 参数：
        可变 v 为 参数 的 第 i 项。
        若 v 小于 0：
            v 为 0 减 v。
        完。
        若 v 大于 最大abs：
            做 网络_追加 用 结果、0。
        否则：
            做 网络_追加 用 结果、梯度 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

幅度剪枝 接受 参数、剪枝率：
    n 为 做 计数 用 参数。
    若 n 等于 0：
        返还 参数。
    完。
    可变 幅度和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 v 为 参数 的 第 i 项。
        若 v 小于 0：
            v 为 0 减 v。
        完。
        幅度和 为 幅度和 加 v。
        i 为 i 加 1。
    完。
    幅度均值 为 幅度和 除以 n。
    阈值 为 幅度均值 乘 剪枝率 除以 1000。
    可变 结果 为 一组：（空）。
    可变 被剪数 为 0。
    i 为 0。
    只要 i 小于 n：
        可变 v 为 参数 的 第 i 项。
        若 v 小于 0：
            v 为 0 减 v。
        完。
        若 v 小于 阈值：
            做 网络_追加 用 结果、0。
            被剪数 为 被剪数 加 1。
        否则：
            做 网络_追加 用 结果、参数 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

剪枝后量化 接受 参数、剪枝率、位数：
    剪枝结果 为 做 幅度剪枝 用 参数、剪枝率。
    返还 做 量化 用 剪枝结果、位数。
完。

稀疏度统计 接受 参数：
    n 为 做 计数 用 参数。
    若 n 等于 0：
        返还 0。
    完。
    可变 零计数 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 参数 的 第 i 项 等于 0：
            零计数 为 零计数 加 1。
        完。
        i 为 i 加 1。
    完。
    稀疏度 为 零计数 乘 1000 除以 n。
    做 网络量化_更新摘要 用 零计数、量化_最近敏感总和、量化_最近平均位宽、稀疏度。
    返还 稀疏度。
完。

网络量化_完整统计 接受 零参：
    返还 一本字典：量化调用数 对应 量化_调用总数、累积误差 对应 量化_累积误差、最大误差 对应 量化_最大误差、自适应位数 对应 量化_自适应位数、保真度 对应 量化_保真度、损失调用数 对应 损失_调用总数、损失EMA 对应 损失_EMA、梯度步数 对应 梯度_步数、校准样本 对应 校准_样本数、混合精度层数 对应 做 计数 用 层位宽、最近零计数 对应 量化_最近零计数、最近稀疏度 对应 量化_最近稀疏度、最近敏感总和 对应 量化_最近敏感总和、最近平均位宽 对应 量化_最近平均位宽、最近压缩质量 对应 量化_最近压缩质量、最近样本数 对应 量化_最近样本数、最近位宽 对应 量化_最近位宽。
完。

量化_重置 接受 零参：
    量化_调用总数 为 0。
    量化_累积误差 为 0。
    量化_最大误差 为 0。
    量化_自适应位数 为 8。
    量化_保真度 为 1000。
    量化_最近压缩质量 为 1000。
    量化_最近样本数 为 0。
    量化_最近位宽 为 8。
    损失_调用总数 为 0。
    损失_EMA 为 0。
    校准_样本数 为 0。
    梯度缓冲 为 一组：（空）。
    梯度_步数 为 0。
    层位宽 为 一组：（空）。
    层敏感度 为 一组：（空）。
    量化_最近零计数 为 0。
    量化_最近稀疏度 为 0。
    量化_最近敏感总和 为 0。
    量化_最近平均位宽 为 8。
完。

本章 收束。
