《字符设备分配》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·字符串替换》。

引用《.·哈希函数》。

字符设备_动态主号起始 为 234。

字符设备_动态主号结束 为 254。

可变 字符设备_下一个动态主号 为 234。

字符设备_分配主号 接受 零参：
    可变 主号 为 字符设备_下一个动态主号。
    只要 主号 小于或等于 字符设备_动态主号结束：
        桶号 为 主号 取余于 字符设备_哈希桶数。
        可变 已用 为 假。
        可变 当前 为 字符设备表 的 第 桶号 项。
        只要 当前 不等于 空：
            若 当前 的 主设备号 等于 主号：
                已用 为 真。
                跳出。
            完。
            当前 为 当前 的 下一个。
        完。
        若 非 已用：
            字符设备_下一个动态主号 为 主号 加 1。
            返还 主号。
        完。
        主号 为 主号 加 1。
    完。
    返还 0。
完。

设备类 是 一种 事物，含 名称、设备链表、设备数、属性组、设备u事件、下一个。

类设备节点 是 一种 事物，含 设备、名称、devno、下一个。

可变 全局设备类链表 为 空。

设备类_注册 接受 名称：
    可变 cls 为 做 分配内存 用 做 大小 用 设备类。
    若 cls 等于 空：
        返还 空。
    完。
    cls 的 名称 为 名称。
    cls 的 设备链表 为 空。
    cls 的 设备数 为 0。
    cls 的 属性组 为 空。
    cls 的 设备u事件 为 空。
    cls 的 下一个 为 全局设备类链表。
    全局设备类链表 为 cls。
    做 内核信息 用 「设备类注册: %s\n」、名称。
    返还 cls。
完。

设备类_创建设备 接受 cls、父设备、devno、名称：
    可变 节点 为 做 分配内存 用 做 大小 用 类设备节点。
    若 节点 等于 空：
        返还 错误码_内存不足。
    完。
    节点 的 名称 为 名称。
    节点 的 devno 为 devno。
    节点 的 设备 为 父设备。
    节点 的 下一个 为 cls 的 设备链表。
    cls 的 设备链表 为 节点。
    cls 的 设备数 为 cls 的 设备数 加 1。
    做 devtmpfs_创建节点 用 名称、devno。
    返还 0。
完。

设备类_销毁设备 接受 cls、devno：
    若 cls 等于 空：
        返还 错误码_不存在。
    完。
    可变 前一个 为 空。
    可变 当前 为 cls 的 设备链表。
    只要 当前 不等于 空：
        若 当前 的 devno 等于 devno：
            若 前一个 等于 空：
                cls 的 设备链表 为 当前 的 下一个。
            否则：
                前一个 的 下一个 为 当前 的 下一个。
            完。
            cls 的 设备数 为 cls 的 设备数 减 1。
            全局统计 的 设备销毁次数 为 全局统计 的 设备销毁次数 加 1。
            返还 0。
        完。
        前一个 为 当前。
        当前 为 当前 的 下一个。
    完。
    全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
    返还 错误码_不存在。
完。

设备类_注销 接受 名称：
    若 名称 等于 空：
        返还 错误码_不存在。
    完。
    可变 前一个 为 空。
    可变 当前 为 全局设备类链表。
    只要 当前 不等于 空：
        若 当前 的 名称 等于 名称：
            若 前一个 等于 空：
                全局设备类链表 为 当前 的 下一个。
            否则：
                前一个 的 下一个 为 当前 的 下一个。
            完。
            全局统计 的 类注销次数 为 全局统计 的 类注销次数 加 1。
            返还 0。
        完。
        前一个 为 当前。
        当前 为 当前 的 下一个。
    完。
    返还 错误码_不存在。
完。

设备类_查找 接受 名称：
    可变 当前 为 全局设备类链表。
    只要 当前 不等于 空：
        若 当前 的 名称 等于 名称：
            返还 当前。
        完。
        当前 为 当前 的 下一个。
    完。
    返还 空。
完。

设备类_设备数 接受 零参：
    可变 总数 为 0。
    可变 当前 为 全局设备类链表。
    只要 当前 不等于 空：
        总数 为 总数 加 当前 的 设备数。
        当前 为 当前 的 下一个。
    完。
    返还 总数。
完。

设备类_数量 接受 零参：
    可变 数量 为 0。
    可变 当前 为 全局设备类链表。
    只要 当前 不等于 空：
        数量 为 数量 加 1。
        当前 为 当前 的 下一个。
    完。
    返还 数量。
完。

设备属性 是 一种 事物，含 名称、值、模式、只读。

属性模式_读写 为 0x644。

属性模式_只读 为 0x444。

最大属性数 为 64。

设备属性_创建 接受 名称、值、模式：
    可变 属性 为 新建 设备属性。
    属性 的 名称 为 名称。
    属性 的 值 为 值。
    属性 的 模式 为 模式。
    若 模式 等于 属性模式_只读：
        属性 的 只读 为 真。
    否则：
        属性 的 只读 为 假。
    完。
    全局统计 的 属性创建次数 为 全局统计 的 属性创建次数 加 1。
    返还 属性。
完。

设备属性_读取 接受 属性列表、名称：
    若 属性列表 等于 空 或者 名称 等于 空：
        返还 空。
    完。
    全局统计 的 属性读取次数 为 全局统计 的 属性读取次数 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 属性列表：
        若 属性列表 的 第 i 项 的 名称 等于 名称：
            返还 属性列表 的 第 i 项 的 值。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

设备属性_写入 接受 属性列表、名称、新值：
    若 属性列表 等于 空 或者 名称 等于 空：
        返还 错误码_不存在。
    完。
    全局统计 的 属性写入次数 为 全局统计 的 属性写入次数 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 属性列表：
        若 属性列表 的 第 i 项 的 名称 等于 名称：
            若 属性列表 的 第 i 项 的 只读 等于 真：
                全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
                返还 0 减 1。
            完。
            属性列表 的 第 i 项 的 值 为 新值。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 错误码_不存在。
完。

设备属性_列表 接受 属性列表：
    若 属性列表 等于 空：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 属性列表：
        做 追加 用 结果、一组：属性列表 的 第 i 项 的 名称、属性列表 的 第 i 项 的 值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

KOBJ_ADD 为 0。

KOBJ_REMOVE 为 1。

KOBJ_CHANGE 为 2。

KOBJ_MOVE 为 3。

KOBJ_ONLINE 为 4。

KOBJ_OFFLINE 为 5。

u事件_发送 接受 设备名、动作：
    全局统计 的 u事件次数 为 全局统计 的 u事件次数 加 1。
    可变 动作名 为 「未知」。
    若 动作 等于 KOBJ_ADD：
        动作名 为 「添加」。
    否则若 动作 等于 KOBJ_REMOVE：
        动作名 为 「移除」。
    否则若 动作 等于 KOBJ_CHANGE：
        动作名 为 「变更」。
    否则若 动作 等于 KOBJ_MOVE：
        动作名 为 「移动」。
    否则若 动作 等于 KOBJ_ONLINE：
        动作名 为 「上线」。
    否则若 动作 等于 KOBJ_OFFLINE：
        动作名 为 「下线」。
    完。
    做 打印 用 做 字符串拼接 用 「[u事件] 」、做 字符串拼接 用 动作名、做 字符串拼接 用 「 」、做 转字符串 用 设备名。
    返还 0。
完。

可变 devtmpfs节点列表 为 一组：（空）。

devtmpfs最大节点 为 512。

devtmpfs_创建节点 接受 名称、devno：
    若 做 计数 用 devtmpfs节点列表 大于或等于 devtmpfs最大节点：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 0 减 12。
    完。
    做 追加 用 devtmpfs节点列表、一组：名称、devno。
    全局统计 的 devtmpfs创建次数 为 全局统计 的 devtmpfs创建次数 加 1。
    返还 0。
完。

devtmpfs_删除节点 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 devtmpfs节点列表：
        若 devtmpfs节点列表 的 第 i 项 的 第 0 项 等于 名称：
            做 删除 用 devtmpfs节点列表、i。
            全局统计 的 devtmpfs删除次数 为 全局统计 的 devtmpfs删除次数 加 1。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 错误码_不存在。
完。

devtmpfs_查找节点 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 devtmpfs节点列表：
        若 devtmpfs节点列表 的 第 i 项 的 第 0 项 等于 名称：
            返还 devtmpfs节点列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

devtmpfs_节点数 接受 零参：
    返还 做 计数 用 devtmpfs节点列表。
完。

AI驱动窗口 为 16。

可变 AI驱动历史 为 一组：（空）。

可变 AI驱动位置 为 0。

驱动匹配记录 是 一种 事物，含 设备类型、驱动名称、匹配成功。

AI_记录驱动匹配 接受 设备类型、驱动名、成功：
    可变 记录 为 新建 驱动匹配记录。
    记录 的 设备类型 为 设备类型。
    记录 的 驱动名称 为 驱动名。
    记录 的 匹配成功 为 成功。
    若 做 计数 用 AI驱动历史 小于 AI驱动窗口：
        做 追加 用 AI驱动历史、记录。
    否则：
        做 设置 用 AI驱动历史、AI驱动位置、记录。
    完。
    AI驱动位置 为 (AI驱动位置 加 1) 取余于 AI驱动窗口。
    全局统计 的 AI匹配记录数 为 全局统计 的 AI匹配记录数 加 1。
完。

AI_推荐驱动 接受 设备类型：
    可变 最佳驱动 为 空。
    可变 最佳频次 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI驱动历史：
        记录 为 AI驱动历史 的 第 i 项。
        若 记录 的 设备类型 等于 设备类型 并且 记录 的 匹配成功 等于 真：
            可变 频次 为 0。
            可变 j 为 0。
            只要 j 小于 做 计数 用 AI驱动历史：
                若 AI驱动历史 的 第 j 项 的 驱动名称 等于 记录 的 驱动名称 并且 AI驱动历史 的 第 j 项 的 匹配成功 等于 真：
                    频次 为 频次 加 1。
                完。
                j 为 j 加 1。
            完。
            若 频次 大于 最佳频次：
                最佳频次 为 频次。
                最佳驱动 为 记录 的 驱动名称。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最佳驱动 不等于 空：
        全局统计 的 AI推荐命中数 为 全局统计 的 AI推荐命中数 加 1。
    完。
    返还 最佳驱动。
完。

AI_驱动匹配准确率 接受 零参：
    可变 成功数 为 0。
    可变 总数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI驱动历史：
        总数 为 总数 加 1。
        若 AI驱动历史 的 第 i 项 的 匹配成功 等于 真：
            成功数 为 成功数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 总数 等于 0：
        返还 0。
    完。
    返还 成功数 乘 100 除以 总数。
完。

批量注册设备 接受 类名、设备列表：
    若 设备列表 等于 空：
        返还 0。
    完。
    cls 为 做 设备类_查找 用 类名。
    若 cls 等于 空：
        cls 为 做 设备类_注册 用 类名。
    完。
    若 cls 等于 空：
        返还 0 减 12。
    完。
    可变 已注册 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备列表：
        设备 为 设备列表 的 第 i 项。
        结果 为 做 设备类_创建设备 用 cls、空、设备 的 第 0 项、设备 的 第 1 项。
        若 结果 等于 0：
            已注册 为 已注册 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 已注册。
完。

分配统计 是 一种 事物，含 主号分配次数、主号分配失败、类注册次数、类注销次数、设备创建次数、设备销毁次数、属性创建次数、属性读取次数、属性写入次数、u事件次数、devtmpfs创建次数、devtmpfs删除次数、AI匹配记录数、AI推荐命中数、批量操作次数、错误次数。

可变 全局统计 为 新建 分配统计。

可变 分配模块已初始化 为 假。

错误码_不存在 为 0 减 2。

错误码_内存不足 为 0 减 12。

分配模块_初始化 接受 零参：
    若 分配模块已初始化 等于 真：
        返还 0 减 17。
    完。
    devtmpfs节点列表 为 一组：（空）。
    AI驱动历史 为 一组：（空）。
    AI驱动位置 为 0。
    分配模块已初始化 为 真。
    做 打印 用 「[设备分配] 初始化完成」。
    返还 0。
完。

分配模块_销毁 接受 零参：
    若 分配模块已初始化 等于 假：
        返还 0。
    完。
    全局设备类链表 为 空。
    devtmpfs节点列表 为 一组：（空）。
    AI驱动历史 为 一组：（空）。
    分配模块已初始化 为 假。
    做 打印 用 「[设备分配] 已销毁」。
    返还 0。
完。

分配模块_转储 接受 零参：
    做 打印 用 「[设备分配] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  设备类数: 」、做 转字符串 用 做 设备类_数量。
    做 打印 用 做 字符串拼接 用 「  总设备数: 」、做 转字符串 用 做 设备类_设备数。
    做 打印 用 做 字符串拼接 用 「  devtmpfs节点数: 」、做 转字符串 用 做 devtmpfs_节点数。
    做 打印 用 做 字符串拼接 用 「  下一个动态主号: 」、做 转字符串 用 字符设备_下一个动态主号。
    做 打印 用 做 字符串拼接 用 「  AI匹配准确率%: 」、做 转字符串 用 做 AI_驱动匹配准确率。
    返还 0。
完。

分配模块_打印统计 接受 零参：
    做 打印 用 「[设备分配] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  主号分配: 」、做 转字符串 用 全局统计 的 主号分配次数。
    做 打印 用 做 字符串拼接 用 「  分配失败: 」、做 转字符串 用 全局统计 的 主号分配失败。
    做 打印 用 做 字符串拼接 用 「  类注册: 」、做 转字符串 用 全局统计 的 类注册次数。
    做 打印 用 做 字符串拼接 用 「  类注销: 」、做 转字符串 用 全局统计 的 类注销次数。
    做 打印 用 做 字符串拼接 用 「  设备创建: 」、做 转字符串 用 全局统计 的 设备创建次数。
    做 打印 用 做 字符串拼接 用 「  设备销毁: 」、做 转字符串 用 全局统计 的 设备销毁次数。
    做 打印 用 做 字符串拼接 用 「  属性创建: 」、做 转字符串 用 全局统计 的 属性创建次数。
    做 打印 用 做 字符串拼接 用 「  属性读取: 」、做 转字符串 用 全局统计 的 属性读取次数。
    做 打印 用 做 字符串拼接 用 「  属性写入: 」、做 转字符串 用 全局统计 的 属性写入次数。
    做 打印 用 做 字符串拼接 用 「  u事件: 」、做 转字符串 用 全局统计 的 u事件次数。
    做 打印 用 做 字符串拼接 用 「  devtmpfs创建: 」、做 转字符串 用 全局统计 的 devtmpfs创建次数。
    做 打印 用 做 字符串拼接 用 「  devtmpfs删除: 」、做 转字符串 用 全局统计 的 devtmpfs删除次数。
    做 打印 用 做 字符串拼接 用 「  AI匹配记录: 」、做 转字符串 用 全局统计 的 AI匹配记录数。
    做 打印 用 做 字符串拼接 用 「  AI推荐命中: 」、做 转字符串 用 全局统计 的 AI推荐命中数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

分配模块_注册到内核 接受 内核上下文：
    若 分配模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[设备分配] 已注册到内核上下文」。
    返还 0。
完。

分配模块_查询状态 接受 零参：
    若 分配模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

分配模块_健康检查 接受 零参：
    若 分配模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 设备创建次数 加 全局统计 的 类注册次数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
