《矩阵分解》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·整数转字符串》。

引用《.·线代绝对值》。

引用《.·张量数学引擎》。

cholesky 接受 A：
    n 为 A 的 第 0 项。
    可变 L 为 一组：n、n。
    可变 i 为 0。
    只要 i 小于 n 乘 n：
        做 追加 用 L、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于或等于 i：
            可变 和 为 0。
            可变 k 为 0。
            只要 k 小于 j：
                和 为 和 加 L 的 第 2 加 i 乘 n 加 k 项 乘 L 的 第 2 加 j 乘 n 加 k 项 除以 线代精度。
                k 为 k 加 1。
            完。
            若 i 等于 j：
                diff 为 A 的 第 2 加 i 乘 n 加 i 项 减 和。
                若 diff 小于或等于 0：
                    返还 空。
                完。
                L 的 第 2 加 i 乘 n 加 j 项 为 做 平方根 用 diff。
            否则：
                分母 为 L 的 第 2 加 j 乘 n 加 j 项。
                若 分母 等于 0：
                    返还 空。
                完。
                L 的 第 2 加 i 乘 n 加 j 项 为 (A 的 第 2 加 i 乘 n 加 j 项 减 和) 乘 线代精度 除以 分母。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 L。
完。

行列式 接受 A：
    n 为 A 的 第 0 项。
    分解 为 做 lu分解 用 A。
    U 为 分解 的 第 1 项。
    可变 det 为 线代精度。
    可变 i 为 0。
    只要 i 小于 n：
        det 为 det 乘 U 的 第 2 加 i 乘 n 加 i 项 除以 线代精度。
        i 为 i 加 1。
    完。
    返还 det。
完。

矩阵秩 接受 A：
    行 为 A 的 第 0 项。
    列 为 A 的 第 1 项。
    可变 m 为 一组：行、列。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        做 追加 用 m、A 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    可变 秩 为 0。
    可变 col 为 0。
    可变 row 为 0。
    只要 row 小于 行 并且 col 小于 列：
        可变 主元行 为 row。
        可变 主元值 为 做 线代_绝对值 用 m 的 第 2 加 row 乘 列 加 col 项。
        i 为 row 加 1。
        只要 i 小于 行：
            v 为 做 线代_绝对值 用 m 的 第 2 加 i 乘 列 加 col 项。
            若 v 大于 主元值：
                主元值 为 v。
                主元行 为 i。
            完。
            i 为 i 加 1。
        完。
        若 主元值 等于 0：
            col 为 col 加 1。
            继续。
        完。
        若 主元行 不等于 row：
            可变 j 为 0。
            只要 j 小于 列：
                t 为 m 的 第 2 加 row 乘 列 加 j 项。
                m 的 第 2 加 row 乘 列 加 j 项 为 m 的 第 2 加 主元行 乘 列 加 j 项。
                m 的 第 2 加 主元行 乘 列 加 j 项 为 t。
                j 为 j 加 1。
            完。
        完。
        i 为 row 加 1。
        只要 i 小于 行：
            因子 为 m 的 第 2 加 i 乘 列 加 col 项 乘 线代精度 除以 m 的 第 2 加 row 乘 列 加 col 项。
            可变 j2 为 col。
            只要 j2 小于 列：
                m 的 第 2 加 i 乘 列 加 j2 项 为 m 的 第 2 加 i 乘 列 加 j2 项 减 因子 乘 m 的 第 2 加 row 乘 列 加 j2 项 除以 线代精度。
                j2 为 j2 加 1。
            完。
            i 为 i 加 1。
        完。
        秩 为 秩 加 1。
        row 为 row 加 1。
        col 为 col 加 1。
    完。
    返还 秩。
完。

稀疏矩阵_创建 接受 行数、列数：
    可变 行指针 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 行数：
        做 追加 用 行指针、0。
        i 为 i 加 1。
    完。
    返还 一组：一组：（空）、一组：（空）、行指针、行数、列数、0。
完。

稀疏矩阵_从密集 接受 A、m、n：
    可变 值 为 一组：（空）。
    可变 列idx 为 一组：（空）。
    可变 行ptr 为 一组：0。
    可变 nnz 为 0。
    可变 i 为 0。
    只要 i 小于 m：
        可变 j 为 0。
        只要 j 小于 n：
            若 A 的 第 i 乘 n 加 j 项 不等于 0：
                做 追加 用 值、A 的 第 i 乘 n 加 j 项。
                做 追加 用 列idx、j。
                nnz 为 nnz 加 1。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 行ptr、nnz。
        i 为 i 加 1。
    完。
    返还 一组：值、列idx、行ptr、m、n、nnz。
完。

稀疏矩阵_乘向量 接受 sp、x：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 sp 的 第 3 项：
        做 追加 用 结果、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 sp 的 第 3 项：
        可变 和 为 0。
        可变 k 为 sp 的 第 2 项 的 第 i 项。
        只要 k 小于 sp 的 第 2 项 的 第 i 加 1 项：
            和 为 和 加 sp 的 第 0 项 的 第 k 项 乘 x 的 第 sp 的 第 1 项 的 第 k 项 项 除以 线代精度。
            k 为 k 加 1。
        完。
        结果 的 第 i 项 为 和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

线代精度 为 1000。

AI稀疏窗口 为 16。

稀疏矩阵_非零数 接受 sp：
    若 sp 等于 空：
        返还 0。
    完。
    返还 sp 的 第 5 项。
完。

稀疏矩阵_稀疏率 接受 sp：
    若 sp 等于 空：
        返还 0。
    完。
    总元素 为 sp 的 第 3 项 乘 sp 的 第 4 项。
    若 总元素 等于 0：
        返还 0。
    完。
    返还 (总元素 减 sp 的 第 5 项) 乘 100 除以 总元素。
完。

稀疏矩阵_转密集 接受 sp：
    若 sp 等于 空：
        返还 空。
    完。
    全局统计 的 稀疏转密集数 为 全局统计 的 稀疏转密集数 加 1。
    行 为 sp 的 第 3 项。
    列 为 sp 的 第 4 项。
    可变 密集 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        做 追加 用 密集、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 行：
        可变 k 为 sp 的 第 2 项 的 第 i 项。
        只要 k 小于 sp 的 第 2 项 的 第 i 加 1 项：
            密集 的 第 i 乘 列 加 sp 的 第 1 项 的 第 k 项 项 为 sp 的 第 0 项 的 第 k 项。
            k 为 k 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 密集。
完。

稀疏矩阵_加法 接受 sp1、sp2：
    若 sp1 等于 空 或者 sp2 等于 空：
        返还 空。
    完。
    若 sp1 的 第 3 项 不等于 sp2 的 第 3 项 或者 sp1 的 第 4 项 不等于 sp2 的 第 4 项：
        返还 空。
    完。
    全局统计 的 稀疏加法数 为 全局统计 的 稀疏加法数 加 1。
    密集1 为 做 稀疏矩阵_转密集 用 sp1。
    密集2 为 做 稀疏矩阵_转密集 用 sp2。
    可变 i 为 0。
    只要 i 小于 sp1 的 第 3 项 乘 sp1 的 第 4 项：
        密集1 的 第 i 项 为 密集1 的 第 i 项 加 密集2 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 稀疏矩阵_从密集 用 密集1、sp1 的 第 3 项、sp1 的 第 4 项。
完。

稀疏矩阵_标量乘 接受 sp、标量：
    若 sp 等于 空：
        返还 空。
    完。
    可变 新值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 sp 的 第 0 项：
        做 追加 用 新值、sp 的 第 0 项 的 第 i 项 乘 标量 除以 线代精度。
        i 为 i 加 1。
    完。
    返还 一组：新值、sp 的 第 1 项、sp 的 第 2 项、sp 的 第 3 项、sp 的 第 4 项、sp 的 第 5 项。
完。

稀疏矩阵_取元素 接受 sp、行、列：
    若 sp 等于 空：
        返还 0。
    完。
    可变 k 为 sp 的 第 2 项 的 第 行 项。
    只要 k 小于 sp 的 第 2 项 的 第 行 加 1 项：
        若 sp 的 第 1 项 的 第 k 项 等于 列：
            返还 sp 的 第 0 项 的 第 k 项。
        完。
        k 为 k 加 1。
    完。
    返还 0。
完。

QR分解 接受 A：
    若 A 等于 空 或者 A 的 第 0 项 不等于 A 的 第 1 项：
        返还 空。
    完。
    全局统计 的 QR分解数 为 全局统计 的 QR分解数 加 1。
    n 为 A 的 第 0 项。
    可变 列向量 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 n：
        可变 列 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 n：
            做 追加 用 列、A 的 第 2 加 i 乘 n 加 j 项。
            i 为 i 加 1。
        完。
        做 追加 用 列向量、列。
        j 为 j 加 1。
    完。
    可变 Q列 为 一组：（空）。
    可变 R 为 一组：n、n。
    可变 idx 为 0。
    只要 idx 小于 n 乘 n：
        做 追加 用 R、0。
        idx 为 idx 加 1。
    完。
    j 为 0。
    只要 j 小于 n：
        可变 u 为 一组：（空）。
        可变 d 为 0。
        只要 d 小于 n：
            做 追加 用 u、列向量 的 第 j 项 的 第 d 项。
            d 为 d 加 1。
        完。
        可变 k 为 0。
        只要 k 小于 做 计数 用 Q列：
            可变 投影 为 0。
            d 为 0。
            只要 d 小于 n：
                投影 为 投影 加 列向量 的 第 j 项 的 第 d 项 乘 Q列 的 第 k 项 的 第 d 项 除以 线代精度。
                d 为 d 加 1。
            完。
            R 的 第 2 加 k 乘 n 加 j 项 为 投影。
            d 为 0。
            只要 d 小于 n：
                u 的 第 d 项 为 u 的 第 d 项 减 投影 乘 Q列 的 第 k 项 的 第 d 项 除以 线代精度。
                d 为 d 加 1。
            完。
            k 为 k 加 1。
        完。
        可变 范数平方 为 0。
        d 为 0。
        只要 d 小于 n：
            范数平方 为 范数平方 加 u 的 第 d 项 乘 u 的 第 d 项 除以 线代精度。
            d 为 d 加 1。
        完。
        可变 范数 为 做 平方根 用 范数平方 乘 线代精度。
        若 范数 等于 0：
            范数 为 1。
        完。
        R 的 第 2 加 j 乘 n 加 j 项 为 范数。
        d 为 0。
        只要 d 小于 n：
            u 的 第 d 项 为 u 的 第 d 项 乘 线代精度 除以 范数。
            d 为 d 加 1。
        完。
        做 追加 用 Q列、u。
        j 为 j 加 1。
    完。
    可变 Q 为 一组：n、n。
    idx 为 0。
    只要 idx 小于 n 乘 n：
        做 追加 用 Q、0。
        idx 为 idx 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        j 为 0。
        只要 j 小于 n：
            Q 的 第 2 加 i 乘 n 加 j 项 为 Q列 的 第 j 项 的 第 i 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：Q、R。
完。

分解_安全Cholesky 接受 A：
    若 A 等于 空：
        返还 空。
    完。
    全局统计 的 Cholesky次数 为 全局统计 的 Cholesky次数 加 1。
    返还 做 cholesky 用 A。
完。

分解_安全行列式 接受 A：
    若 A 等于 空：
        返还 0。
    完。
    全局统计 的 行列式次数 为 全局统计 的 行列式次数 加 1。
    返还 做 行列式 用 A。
完。

分解_安全秩 接受 A：
    若 A 等于 空：
        返还 0。
    完。
    全局统计 的 秩计算次数 为 全局统计 的 秩计算次数 加 1。
    返还 做 矩阵秩 用 A。
完。

分解_建议格式 接受 行、列、非零数：
    总元素 为 行 乘 列。
    若 总元素 等于 0：
        返还 「密集」。
    完。
    稀疏率 为 (总元素 减 非零数) 乘 100 除以 总元素。
    若 稀疏率 大于 70：
        返还 「稀疏CSR」。
    完。
    返还 「密集」。
完。

可变 AI稀疏历史 为 一组：（空）。

可变 AI稀疏位置 为 0。

AI_记录稀疏率 接受 稀疏率：
    若 做 计数 用 AI稀疏历史 小于 AI稀疏窗口：
        做 追加 用 AI稀疏历史、稀疏率。
    否则：
        做 设置 用 AI稀疏历史、AI稀疏位置、稀疏率。
    完。
    AI稀疏位置 为 (AI稀疏位置 加 1) 取余于 AI稀疏窗口。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
完。

AI_预测稀疏趋势 接受 零参：
    若 做 计数 用 AI稀疏历史 小于 4：
        返还 「数据不足」。
    完。
    半 为 做 计数 用 AI稀疏历史 除以 2。
    可变 前半和 为 0。
    可变 后半和 为 0。
    可变 i 为 0。
    只要 i 小于 半：
        前半和 为 前半和 加 AI稀疏历史 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 i 小于 做 计数 用 AI稀疏历史：
        后半和 为 后半和 加 AI稀疏历史 的 第 i 项。
        i 为 i 加 1。
    完。
    前均 为 前半和 除以 半。
    后均 为 后半和 除以 (做 计数 用 AI稀疏历史 减 半)。
    若 后均 大于 前均 加 10：
        全局统计 的 AI预测命中 为 全局统计 的 AI预测命中 加 1。
        返还 「稀疏度增加-考虑CSR」。
    完。
    若 后均 小于 前均 减 10：
        返还 「稀疏度降低-考虑密集」。
    完。
    返还 「稀疏度稳定」。
完。

分解统计 是 一种 事物，含 Cholesky次数、行列式次数、秩计算次数、QR分解数、稀疏创建数、稀疏转密集数、稀疏加法数、稀疏乘向量数、AI预测次数、AI预测命中、最大维度、总运算量、奇异矩阵数、批量操作数、错误次数。

可变 全局统计 为 新建 分解统计。

可变 分解模块已初始化 为 假。

分解模块_初始化 接受 零参：
    若 分解模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI稀疏历史 为 一组：（空）。
    AI稀疏位置 为 0。
    分解模块已初始化 为 真。
    做 打印 用 「[矩阵分解] 初始化完成」。
    返还 0。
完。

分解模块_销毁 接受 零参：
    若 分解模块已初始化 等于 假：
        返还 0。
    完。
    AI稀疏历史 为 一组：（空）。
    分解模块已初始化 为 假。
    做 打印 用 「[矩阵分解] 已销毁」。
    返还 0。
完。

分解模块_转储 接受 零参：
    做 打印 用 「[矩阵分解] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  Cholesky: 」、做 整数转字符串 用 全局统计 的 Cholesky次数。
    做 打印 用 做 字符串拼接 用 「  行列式: 」、做 整数转字符串 用 全局统计 的 行列式次数。
    做 打印 用 做 字符串拼接 用 「  QR分解: 」、做 整数转字符串 用 全局统计 的 QR分解数。
    做 打印 用 做 字符串拼接 用 「  AI稀疏趋势: 」、做 AI_预测稀疏趋势。
    返还 0。
完。

分解模块_打印统计 接受 零参：
    做 打印 用 「[矩阵分解] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  Cholesky: 」、做 整数转字符串 用 全局统计 的 Cholesky次数。
    做 打印 用 做 字符串拼接 用 「  行列式: 」、做 整数转字符串 用 全局统计 的 行列式次数。
    做 打印 用 做 字符串拼接 用 「  秩计算: 」、做 整数转字符串 用 全局统计 的 秩计算次数。
    做 打印 用 做 字符串拼接 用 「  QR分解: 」、做 整数转字符串 用 全局统计 的 QR分解数。
    做 打印 用 做 字符串拼接 用 「  稀疏创建: 」、做 整数转字符串 用 全局统计 的 稀疏创建数。
    做 打印 用 做 字符串拼接 用 「  稀疏转密集: 」、做 整数转字符串 用 全局统计 的 稀疏转密集数。
    做 打印 用 做 字符串拼接 用 「  稀疏加法: 」、做 整数转字符串 用 全局统计 的 稀疏加法数。
    做 打印 用 做 字符串拼接 用 「  稀疏乘向量: 」、做 整数转字符串 用 全局统计 的 稀疏乘向量数。
    做 打印 用 做 字符串拼接 用 「  AI预测: 」、做 整数转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI命中: 」、做 整数转字符串 用 全局统计 的 AI预测命中。
    做 打印 用 做 字符串拼接 用 「  最大维度: 」、做 整数转字符串 用 全局统计 的 最大维度。
    做 打印 用 做 字符串拼接 用 「  总运算量: 」、做 整数转字符串 用 全局统计 的 总运算量。
    做 打印 用 做 字符串拼接 用 「  奇异矩阵: 」、做 整数转字符串 用 全局统计 的 奇异矩阵数。
    做 打印 用 做 字符串拼接 用 「  批量操作: 」、做 整数转字符串 用 全局统计 的 批量操作数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 整数转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

分解模块_注册到内核 接受 内核上下文：
    若 分解模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[矩阵分解] 已注册到内核上下文」。
    返还 0。
完。

分解模块_查询状态 接受 零参：
    若 分解模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

分解模块_健康检查 接受 零参：
    若 分解模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 Cholesky次数 加 全局统计 的 行列式次数：
        返还 假。
    完。
    返还 真。
完。

分解_获取Cholesky次数 接受 零参：
    返还 全局统计 的 Cholesky次数。
完。

分解_获取行列式次数 接受 零参：
    返还 全局统计 的 行列式次数。
完。

分解_获取QR次数 接受 零参：
    返还 全局统计 的 QR分解数。
完。

分解_获取AI稀疏趋势 接受 零参：
    返还 做 AI_预测稀疏趋势。
完。

稀疏矩阵_行非零数 接受 sp、行号：
    若 sp 等于 空 或者 行号 大于或等于 sp 的 第 3 项：
        返还 0。
    完。
    返还 sp 的 第 2 项 的 第 行号 加 1 项 减 sp 的 第 2 项 的 第 行号 项。
完。

稀疏矩阵_内存节省率 接受 sp：
    若 sp 等于 空：
        返还 0。
    完。
    密集大小 为 sp 的 第 3 项 乘 sp 的 第 4 项。
    若 密集大小 等于 0：
        返还 0。
    完。
    稀疏大小 为 sp 的 第 5 项 乘 2 加 sp 的 第 3 项 加 1。
    若 稀疏大小 大于或等于 密集大小：
        返还 0。
    完。
    返还 (密集大小 减 稀疏大小) 乘 100 除以 密集大小。
完。

分解_Tick 接受 零参：
    若 全局统计 的 Cholesky次数 大于 0 并且 全局统计 的 Cholesky次数 取余于 50 等于 0：
        趋势 为 做 AI_预测稀疏趋势。
    完。
完。

分解_重置统计 接受 零参：
    全局统计 的 Cholesky次数 为 0。
    全局统计 的 行列式次数 为 0。
    全局统计 的 秩计算次数 为 0。
    全局统计 的 QR分解数 为 0。
    全局统计 的 错误次数 为 0。
完。

新系统张量数_统计 接受 零参：
    返还 一本字典：模块 对应 「新系统张量数」、全局统计 对应 全局统计、分解模块已初始化 对应 分解模块已初始化。
完。

新系统张量数_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 分解模块已初始化 等于 假：
        做 追加 用 问题、「分解模块已初始化未就绪」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

新系统张量数_综合报告 接受 零参：
    做 打印 用 「[新系统张量数] ═══ 综合报告 ═══」。
    做 打印 用 「  全局统计: 」 加 若 全局统计 不等于 空 则 「已初始化」 否则 「未初始化」。
    做 打印 用 「  分解模块已初始化: 」 加 若 分解模块已初始化 则 「是」 否则 「否」。
    做 打印 用 「  健康: 」 加 若 做 新系统张量数_健康评估 则 「正常」 否则 「异常」。
完。

新系统张量数_全局重置 接受 零参：
    分解模块已初始化 为 假。
    做 打印 用 「[新系统张量数] 全局统计已重置」。
    返还 0。
完。

本章 收束。
