《红黑树更新》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·字符串替换》。

红 为 0。

黑 为 1。

左子 为 0。

右子 为 1。

父节 为 2。

键位 为 3。

值位 为 4。

色位 为 5。

节点大小 为 6。

可变 插入次数 为 0。

可变 删除次数 为 0。

可变 旋转次数 为 0。

可变 查找次数 为 0。

红黑_新节点 接受 键、值：
    节点 为 做 堆_分配 用 节点大小 乘 8。
    若 节点 等于 0：
        返还 0。
    完。
    做 内存_写64 用 节点 加 左子 乘 8、0。
    做 内存_写64 用 节点 加 右子 乘 8、0。
    做 内存_写64 用 节点 加 父节 乘 8、0。
    做 内存_写64 用 节点 加 键位 乘 8、键。
    做 内存_写64 用 节点 加 值位 乘 8、值。
    做 内存_写64 用 节点 加 色位 乘 8、红。
    返还 节点。
完。

红黑_读 接受 节点、位：
    返还 做 内存_读64 用 节点 加 位 乘 8。
完。

红黑_写 接受 节点、位、值：
    做 内存_写64 用 节点 加 位 乘 8、值。
完。

红黑_左旋 接受 根指针、节点：
    旋转次数 为 旋转次数 加 1。
    右 为 做 红黑_读 用 节点、右子。
    做 红黑_写 用 节点、右子、做 红黑_读 用 右、左子。
    若 做 红黑_读 用 右、左子 不等于 0：
        做 红黑_写 用 做 红黑_读 用 右、左子、父节、节点。
    完。
    做 红黑_写 用 右、父节、做 红黑_读 用 节点、父节。
    若 做 红黑_读 用 节点、父节 等于 0：
        做 内存_写64 用 根指针、右。
    否则若 节点 等于 做 红黑_读 用 做 红黑_读 用 节点、父节、左子：
        做 红黑_写 用 做 红黑_读 用 节点、父节、左子、右。
    否则：
        做 红黑_写 用 做 红黑_读 用 节点、父节、右子、右。
    完。
    做 红黑_写 用 右、左子、节点。
    做 红黑_写 用 节点、父节、右。
完。

红黑_右旋 接受 根指针、节点：
    旋转次数 为 旋转次数 加 1。
    左 为 做 红黑_读 用 节点、左子。
    做 红黑_写 用 节点、左子、做 红黑_读 用 左、右子。
    若 做 红黑_读 用 左、右子 不等于 0：
        做 红黑_写 用 做 红黑_读 用 左、右子、父节、节点。
    完。
    做 红黑_写 用 左、父节、做 红黑_读 用 节点、父节。
    若 做 红黑_读 用 节点、父节 等于 0：
        做 内存_写64 用 根指针、左。
    否则若 节点 等于 做 红黑_读 用 做 红黑_读 用 节点、父节、右子：
        做 红黑_写 用 做 红黑_读 用 节点、父节、右子、左。
    否则：
        做 红黑_写 用 做 红黑_读 用 节点、父节、左子、左。
    完。
    做 红黑_写 用 左、右子、节点。
    做 红黑_写 用 节点、父节、左。
完。

红黑_插入 接受 根指针、键、值：
    插入次数 为 插入次数 加 1。
    新 为 做 红黑_新节点 用 键、值。
    根 为 做 内存_读64 用 根指针。
    若 根 等于 0：
        做 红黑_写 用 新、色位、黑。
        做 内存_写64 用 根指针、新。
        返还 新。
    完。
    可变 当前 为 根。
    可变 父 为 0。
    只要 当前 不等于 0：
        父 为 当前。
        若 键 小于 做 红黑_读 用 当前、键位：
            当前 为 做 红黑_读 用 当前、左子。
        否则若 键 大于 做 红黑_读 用 当前、键位：
            当前 为 做 红黑_读 用 当前、右子。
        否则：
            做 红黑_写 用 当前、值位、值。
            做 堆_释放 用 新。
            返还 当前。
        完。
    完。
    做 红黑_写 用 新、父节、父。
    若 键 小于 做 红黑_读 用 父、键位：
        做 红黑_写 用 父、左子、新。
    否则：
        做 红黑_写 用 父、右子、新。
    完。
    做 红黑_插入修复 用 根指针、新。
    返还 新。
完。

红黑_插入修复 接受 根指针、节点：
    可变 当前 为 节点。
    只要 做 红黑_读 用 当前、父节 不等于 0 并且 做 红黑_读 用 做 红黑_读 用 当前、父节、色位 等于 红：
        父 为 做 红黑_读 用 当前、父节。
        祖 为 做 红黑_读 用 父、父节。
        若 祖 等于 0：
            跳出。
        完。
        若 父 等于 做 红黑_读 用 祖、左子：
            叔 为 做 红黑_读 用 祖、右子。
            若 叔 不等于 0 并且 做 红黑_读 用 叔、色位 等于 红：
                做 红黑_写 用 父、色位、黑。
                做 红黑_写 用 叔、色位、黑。
                做 红黑_写 用 祖、色位、红。
                当前 为 祖。
            否则：
                若 当前 等于 做 红黑_读 用 父、右子：
                    当前 为 父。
                    做 红黑_左旋 用 根指针、当前。
                    父 为 做 红黑_读 用 当前、父节。
                    祖 为 做 红黑_读 用 父、父节。
                完。
                做 红黑_写 用 父、色位、黑。
                做 红黑_写 用 祖、色位、红。
                做 红黑_右旋 用 根指针、祖。
            完。
        否则：
            叔 为 做 红黑_读 用 祖、左子。
            若 叔 不等于 0 并且 做 红黑_读 用 叔、色位 等于 红：
                做 红黑_写 用 父、色位、黑。
                做 红黑_写 用 叔、色位、黑。
                做 红黑_写 用 祖、色位、红。
                当前 为 祖。
            否则：
                若 当前 等于 做 红黑_读 用 父、左子：
                    当前 为 父。
                    做 红黑_右旋 用 根指针、当前。
                    父 为 做 红黑_读 用 当前、父节。
                    祖 为 做 红黑_读 用 父、父节。
                完。
                做 红黑_写 用 父、色位、黑。
                做 红黑_写 用 祖、色位、红。
                做 红黑_左旋 用 根指针、祖。
            完。
        完。
    完。
    做 红黑_写 用 做 内存_读64 用 根指针、色位、黑。
完。

红黑_查找 接受 根指针、键：
    查找次数 为 查找次数 加 1。
    可变 当前 为 做 内存_读64 用 根指针。
    只要 当前 不等于 0：
        节点键 为 做 红黑_读 用 当前、键位。
        若 键 小于 节点键：
            当前 为 做 红黑_读 用 当前、左子。
        否则若 键 大于 节点键：
            当前 为 做 红黑_读 用 当前、右子。
        否则：
            返还 当前。
        完。
    完。
    返还 0。
完。

红黑_更新 接受 根指针、键、新值：
    节点 为 做 红黑_查找 用 根指针、键。
    若 节点 等于 0：
        返还 错误_不存在。
    完。
    做 红黑_写 用 节点、值位、新值。
    返还 0。
完。

红黑_最小 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 当前 为 节点。
    只要 做 红黑_读 用 当前、左子 不等于 0：
        当前 为 做 红黑_读 用 当前、左子。
    完。
    返还 当前。
完。

红黑_最大 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    可变 当前 为 节点。
    只要 做 红黑_读 用 当前、右子 不等于 0：
        当前 为 做 红黑_读 用 当前、右子。
    完。
    返还 当前。
完。

红黑_后继 接受 节点：
    若 节点 等于 0：
        返还 0。
    完。
    若 做 红黑_读 用 节点、右子 不等于 0：
        返还 做 红黑_最小 用 做 红黑_读 用 节点、右子。
    完。
    可变 当前 为 节点。
    可变 父 为 做 红黑_读 用 当前、父节。
    只要 父 不等于 0 并且 当前 等于 做 红黑_读 用 父、右子：
        当前 为 父。
        父 为 做 红黑_读 用 当前、父节。
    完。
    返还 父。
完。

红黑_大小 接受 根指针：
    可变 数量 为 0。
    可变 当前 为 做 红黑_最小 用 做 内存_读64 用 根指针。
    只要 当前 不等于 0：
        数量 为 数量 加 1。
        当前 为 做 红黑_后继 用 当前。
    完。
    返还 数量。
完。

红黑_黑高度 接受 节点：
    若 节点 等于 0：
        返还 1。
    完。
    左高 为 做 红黑_黑高度 用 做 红黑_读 用 节点、左子。
    右高 为 做 红黑_黑高度 用 做 红黑_读 用 节点、右子。
    若 左高 小于 0 或者 右高 小于 0 或者 左高 不等于 右高：
        返还 负 1。
    完。
    若 做 红黑_读 用 节点、色位 等于 黑：
        返还 左高 加 1。
    完。
    返还 左高。
完。

红黑_验证 接受 根指针：
    根 为 做 内存_读64 用 根指针。
    若 根 等于 0：
        返还 真。
    完。
    若 做 红黑_读 用 根、色位 不等于 黑：
        返还 假。
    完。
    返还 做 红黑_黑高度 用 根 大于 0。
完。

红黑_范围查询 接受 根指针、低键、高键、结果数组、最大数：
    可变 数量 为 0。
    可变 当前 为 做 红黑_最小 用 做 内存_读64 用 根指针。
    只要 当前 不等于 0 并且 数量 小于 最大数：
        键 为 做 红黑_读 用 当前、键位。
        若 键 大于或等于 低键 并且 键 小于或等于 高键：
            做 内存_写64 用 结果数组 加 数量 乘 16、键。
            做 内存_写64 用 结果数组 加 数量 乘 16 加 8、做 红黑_读 用 当前、值位。
            数量 为 数量 加 1。
        完。
        若 键 大于 高键：
            跳出。
        完。
        当前 为 做 红黑_后继 用 当前。
    完。
    返还 数量。
完。

更新统计 是 一种 事物，含 插入次数、更新次数、更新插入次数、删除次数、查找次数、查找命中、查找未命中、旋转次数、范围查询次数、范围查询命中、批量更新次数、条件更新次数、条件命中次数、验证次数、验证通过、AI访问记录数、AI预测命中、错误次数。

可变 全局更新统计 为 新建 更新统计。

可变 更新模块已初始化 为 假。

AI访问窗口 为 16。

可变 AI访问历史 为 一组：（空）。

可变 AI访问位置 为 0。

红黑更新_初始化 接受 零参：
    若 更新模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI访问历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI访问窗口：
        做 追加 用 AI访问历史、0。
        i 为 i 加 1。
    完。
    AI访问位置 为 0。
    更新模块已初始化 为 真。
    做 打印 用 「[红黑更新] 初始化完成」。
    返还 0。
完。

红黑更新_销毁 接受 零参：
    若 更新模块已初始化 等于 假：
        返还 0。
    完。
    AI访问历史 为 一组：（空）。
    更新模块已初始化 为 假。
    做 打印 用 「[红黑更新] 已销毁」。
    返还 0。
完。

红黑_安全查找 接受 根指针、键：
    全局更新统计 的 查找次数 为 全局更新统计 的 查找次数 加 1。
    结果 为 做 红黑_查找 用 根指针、键。
    若 结果 不等于 0：
        全局更新统计 的 查找命中 为 全局更新统计 的 查找命中 加 1。
        做 AI_记录访问 用 键。
    否则：
        全局更新统计 的 查找未命中 为 全局更新统计 的 查找未命中 加 1。
    完。
    返还 结果。
完。

红黑_更新插入 接受 根指针、键、值：
    全局更新统计 的 更新插入次数 为 全局更新统计 的 更新插入次数 加 1。
    节点 为 做 红黑_查找 用 根指针、键。
    若 节点 不等于 0：
        做 红黑_写 用 节点、值位、值。
        全局更新统计 的 更新次数 为 全局更新统计 的 更新次数 加 1。
        返还 节点。
    完。
    返还 做 红黑_插入 用 根指针、键、值。
完。

红黑_条件更新 接受 根指针、键、期望旧值、新值：
    全局更新统计 的 条件更新次数 为 全局更新统计 的 条件更新次数 加 1。
    节点 为 做 红黑_查找 用 根指针、键。
    若 节点 等于 0：
        返还 错误_不存在。
    完。
    当前值 为 做 红黑_读 用 节点、值位。
    若 当前值 不等于 期望旧值：
        返还 错误_不存在。
    完。
    做 红黑_写 用 节点、值位、新值。
    全局更新统计 的 条件命中次数 为 全局更新统计 的 条件命中次数 加 1。
    返还 0。
完。

红黑_增量更新 接受 根指针、键、增量：
    节点 为 做 红黑_查找 用 根指针、键。
    若 节点 等于 0：
        返还 错误_不存在。
    完。
    旧值 为 做 红黑_读 用 节点、值位。
    做 红黑_写 用 节点、值位、旧值 加 增量。
    全局更新统计 的 更新次数 为 全局更新统计 的 更新次数 加 1。
    返还 旧值 加 增量。
完。

红黑_最大值更新 接受 根指针、键、候选值：
    节点 为 做 红黑_查找 用 根指针、键。
    若 节点 等于 0：
        返还 做 红黑_插入 用 根指针、键、候选值。
    完。
    当前值 为 做 红黑_读 用 节点、值位。
    若 候选值 大于 当前值：
        做 红黑_写 用 节点、值位、候选值。
        全局更新统计 的 更新次数 为 全局更新统计 的 更新次数 加 1。
    完。
    返还 节点。
完。

红黑_最小值更新 接受 根指针、键、候选值：
    节点 为 做 红黑_查找 用 根指针、键。
    若 节点 等于 0：
        返还 做 红黑_插入 用 根指针、键、候选值。
    完。
    当前值 为 做 红黑_读 用 节点、值位。
    若 候选值 小于 当前值：
        做 红黑_写 用 节点、值位、候选值。
        全局更新统计 的 更新次数 为 全局更新统计 的 更新次数 加 1。
    完。
    返还 节点。
完。

红黑_批量插入 接受 根指针、键值数组、数量：
    若 键值数组 等于 0 或者 数量 小于或等于 0：
        返还 错误_不存在。
    完。
    全局更新统计 的 批量更新次数 为 全局更新统计 的 批量更新次数 加 1。
    可变 i 为 0。
    只要 i 小于 数量：
        键 为 做 内存_读64 用 键值数组 加 i 乘 16。
        值 为 做 内存_读64 用 键值数组 加 i 乘 16 加 8。
        做 红黑_插入 用 根指针、键、值。
        i 为 i 加 1。
    完。
    返还 数量。
完。

红黑_批量更新插入 接受 根指针、键值数组、数量：
    若 键值数组 等于 0 或者 数量 小于或等于 0：
        返还 错误_不存在。
    完。
    全局更新统计 的 批量更新次数 为 全局更新统计 的 批量更新次数 加 1。
    可变 已更新 为 0。
    可变 已插入 为 0。
    可变 i 为 0。
    只要 i 小于 数量：
        键 为 做 内存_读64 用 键值数组 加 i 乘 16。
        值 为 做 内存_读64 用 键值数组 加 i 乘 16 加 8。
        节点 为 做 红黑_查找 用 根指针、键。
        若 节点 不等于 0：
            做 红黑_写 用 节点、值位、值。
            已更新 为 已更新 加 1。
        否则：
            做 红黑_插入 用 根指针、键、值。
            已插入 为 已插入 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 已更新 乘 1000 加 已插入。
完。

红黑_求和 接受 根指针：
    可变 总和 为 0。
    可变 当前 为 做 红黑_最小 用 做 内存_读64 用 根指针。
    只要 当前 不等于 0：
        总和 为 总和 加 做 红黑_读 用 当前、值位。
        当前 为 做 红黑_后继 用 当前。
    完。
    返还 总和。
完。

红黑_最大值 接受 根指针：
    可变 最大 为 0。
    可变 已找到 为 假。
    可变 当前 为 做 红黑_最小 用 做 内存_读64 用 根指针。
    只要 当前 不等于 0：
        值 为 做 红黑_读 用 当前、值位。
        若 已找到 等于 假 或者 值 大于 最大：
            最大 为 值。
            已找到 为 真。
        完。
        当前 为 做 红黑_后继 用 当前。
    完。
    返还 最大。
完。

红黑_最小值 接受 根指针：
    可变 最小 为 0。
    可变 已找到 为 假。
    可变 当前 为 做 红黑_最小 用 做 内存_读64 用 根指针。
    只要 当前 不等于 0：
        值 为 做 红黑_读 用 当前、值位。
        若 已找到 等于 假 或者 值 小于 最小：
            最小 为 值。
            已找到 为 真。
        完。
        当前 为 做 红黑_后继 用 当前。
    完。
    返还 最小。
完。

红黑_计数满足条件 接受 根指针、阈值：
    可变 计数 为 0。
    可变 当前 为 做 红黑_最小 用 做 内存_读64 用 根指针。
    只要 当前 不等于 0：
        若 做 红黑_读 用 当前、值位 大于或等于 阈值：
            计数 为 计数 加 1。
        完。
        当前 为 做 红黑_后继 用 当前。
    完。
    返还 计数。
完。

AI_记录访问 接受 键：
    做 设置 用 AI访问历史、AI访问位置、键。
    AI访问位置 为 (AI访问位置 加 1) 取余于 AI访问窗口。
    全局更新统计 的 AI访问记录数 为 全局更新统计 的 AI访问记录数 加 1。
完。

AI_预测下次访问 接受 零参：
    可变 最频键 为 0。
    可变 最频次 为 0。
    可变 i 为 0。
    只要 i 小于 AI访问窗口：
        若 AI访问历史 的 第 i 项 等于 0：
            i 为 i 加 1。
            继续。
        完。
        当前 为 AI访问历史 的 第 i 项。
        可变 频 为 0。
        可变 j 为 0。
        只要 j 小于 AI访问窗口：
            若 AI访问历史 的 第 j 项 等于 当前：
                频 为 频 加 1。
            完。
            j 为 j 加 1。
        完。
        若 频 大于 最频次：
            最频次 为 频。
            最频键 为 当前。
        完。
        i 为 i 加 1。
    完。
    若 最频次 大于 AI访问窗口 除以 3：
        全局更新统计 的 AI预测命中 为 全局更新统计 的 AI预测命中 加 1。
    完。
    返还 最频键。
完。

AI_建议预取 接受 根指针：
    热键 为 做 AI_预测下次访问。
    若 热键 等于 0：
        返还 0。
    完。
    节点 为 做 红黑_查找 用 根指针、热键。
    返还 节点。
完。

红黑更新_转储 接受 根指针：
    做 打印 用 「[红黑更新] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  节点数: 」、做 转字符串 用 做 红黑_大小 用 根指针。
    根 为 做 内存_读64 用 根指针。
    若 根 不等于 0：
        做 打印 用 做 字符串拼接 用 「  黑高度: 」、做 转字符串 用 做 红黑_黑高度 用 根。
        做 打印 用 做 字符串拼接 用 「  验证: 」、做 转字符串 用 做 红黑_验证 用 根指针。
    完。
    返还 0。
完。

红黑更新_打印统计 接受 零参：
    做 打印 用 「[红黑更新] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  插入次数: 」、做 转字符串 用 插入次数。
    做 打印 用 做 字符串拼接 用 「  更新次数: 」、做 转字符串 用 全局更新统计 的 更新次数。
    做 打印 用 做 字符串拼接 用 「  更新插入: 」、做 转字符串 用 全局更新统计 的 更新插入次数。
    做 打印 用 做 字符串拼接 用 「  删除次数: 」、做 转字符串 用 删除次数。
    做 打印 用 做 字符串拼接 用 「  查找次数: 」、做 转字符串 用 全局更新统计 的 查找次数。
    做 打印 用 做 字符串拼接 用 「  查找命中: 」、做 转字符串 用 全局更新统计 的 查找命中。
    做 打印 用 做 字符串拼接 用 「  查找未命中: 」、做 转字符串 用 全局更新统计 的 查找未命中。
    做 打印 用 做 字符串拼接 用 「  旋转次数: 」、做 转字符串 用 旋转次数。
    做 打印 用 做 字符串拼接 用 「  范围查询: 」、做 转字符串 用 全局更新统计 的 范围查询次数。
    做 打印 用 做 字符串拼接 用 「  批量更新: 」、做 转字符串 用 全局更新统计 的 批量更新次数。
    做 打印 用 做 字符串拼接 用 「  条件更新: 」、做 转字符串 用 全局更新统计 的 条件更新次数。
    做 打印 用 做 字符串拼接 用 「  条件命中: 」、做 转字符串 用 全局更新统计 的 条件命中次数。
    做 打印 用 做 字符串拼接 用 「  验证次数: 」、做 转字符串 用 全局更新统计 的 验证次数。
    做 打印 用 做 字符串拼接 用 「  AI访问记录: 」、做 转字符串 用 全局更新统计 的 AI访问记录数。
    做 打印 用 做 字符串拼接 用 「  AI预测命中: 」、做 转字符串 用 全局更新统计 的 AI预测命中。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局更新统计 的 错误次数。
    若 全局更新统计 的 查找次数 大于 0：
        命中率 为 全局更新统计 的 查找命中 乘 100 除以 全局更新统计 的 查找次数。
        做 打印 用 做 字符串拼接 用 「  命中率%: 」、做 转字符串 用 命中率。
    完。
    返还 0。
完。

红黑更新_注册到内核 接受 内核上下文：
    若 更新模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[红黑更新] 已注册到内核上下文」。
    返还 0。
完。

红黑更新_查询状态 接受 零参：
    若 更新模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

红黑更新_健康检查 接受 零参：
    若 更新模块已初始化 等于 假：
        返还 假。
    完。
    若 全局更新统计 的 错误次数 乘 10 大于 全局更新统计 的 查找次数 加 插入次数：
        返还 假。
    完。
    返还 真。
完。

红黑树_完整统计 接受 根指针：
    返还 一组：做 红黑_大小 用 根指针、做 红黑_黑高度 用 做 内存_读64 用 根指针、插入次数、删除次数、旋转次数、查找次数。
完。

错误_不存在 为 0 减 2。

本章 收束。
