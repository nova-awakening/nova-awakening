《截止期调度》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·红黑树删除》。

引用《.·红黑树创建》。

引用《.·进程描述符》。

引用《.·整数转字符串》。

DL_最小runtime 为 1000。

DL_最小deadline 为 1000。

DL_最小period 为 1000。

DL_最大利用率 为 950。

可变 DL_队列 为 空。

可变 DL_任务数 为 0。

可变 DL_调度次数 为 0。

可变 DL_总利用率 为 0。

可变 DL_错过deadline次数 为 0。

DL_初始化 接受 零参：
    DL_队列 为 做 红黑树_创建。
    DL_任务数 为 0。
    DL_调度次数 为 0。
    DL_总利用率 为 0。
    DL_错过deadline次数 为 0。
完。

DL_设置参数 接受 任务、runtime、deadline、period：
    若 runtime 小于 DL_最小runtime：
        返还 假。
    完。
    若 deadline 小于 DL_最小deadline：
        返还 假。
    完。
    若 period 小于 DL_最小period：
        返还 假。
    完。
    若 runtime 大于 deadline：
        返还 假。
    完。
    若 deadline 大于 period：
        返还 假。
    完。
    利用率 为 runtime 乘 1000 除以 period。
    旧利用率 为 任务 的 第 T_dl_利用率 项。
    新总利用率 为 DL_总利用率 减 旧利用率 加 利用率。
    若 新总利用率 大于 DL_最大利用率：
        返还 假。
    完。
    做 设置 用 任务、T_dl_runtime、runtime。
    做 设置 用 任务、T_dl_deadline、deadline。
    做 设置 用 任务、T_dl_period、period。
    做 设置 用 任务、T_dl_利用率、利用率。
    DL_总利用率 为 新总利用率。
    返还 真。
完。

DL_补充 接受 任务、当前时间：
    runtime 为 任务 的 第 T_dl_runtime 项。
    做 设置 用 任务、T_dl_剩余runtime、runtime。
    period 为 任务 的 第 T_dl_period 项。
    deadline 为 任务 的 第 T_dl_deadline 项。
    旧绝对deadline 为 任务 的 第 T_dl_绝对deadline 项。
    新绝对deadline 为 当前时间 加 period。
    若 旧绝对deadline 大于 0 并且 当前时间 大于 旧绝对deadline：
        DL_错过deadline次数 为 DL_错过deadline次数 加 1。
    完。
    做 设置 用 任务、T_dl_绝对deadline、新绝对deadline。
完。

DL_入队 接受 任务、当前时间：
    剩余 为 任务 的 第 T_dl_剩余runtime 项。
    若 剩余 小于或等于 0：
        做 DL_补充 用 任务、当前时间。
    完。
    绝对deadline 为 任务 的 第 T_dl_绝对deadline 项。
    做 红黑树_插入 用 DL_队列、绝对deadline、任务。
    DL_任务数 为 DL_任务数 加 1。
    返还 真。
完。

DL_出队 接受 任务：
    绝对deadline 为 任务 的 第 T_dl_绝对deadline 项。
    做 红黑树_删除 用 DL_队列、绝对deadline。
    DL_任务数 为 DL_任务数 减 1。
    返还 真。
完。

DL_选择下一个 接受 当前时间：
    若 DL_任务数 等于 0：
        返还 空。
    完。
    结果 为 做 红黑树_删除最小 用 DL_队列。
    若 结果 等于 空：
        返还 空。
    完。
    任务 为 结果 的 第 1 项。
    DL_任务数 为 DL_任务数 减 1。
    DL_调度次数 为 DL_调度次数 加 1。
    做 进程_设置状态 用 任务、进程_运行。
    返还 任务。
完。

DL_tick 接受 当前任务、delta_us、当前时间：
    若 当前任务 等于 空：
        返还 假。
    完。
    剩余 为 当前任务 的 第 T_dl_剩余runtime 项。
    新剩余 为 剩余 减 delta_us。
    做 设置 用 当前任务、T_dl_剩余runtime、新剩余。
    若 新剩余 小于或等于 0：
        做 DL_补充 用 当前任务、当前时间。
        返还 真。
    完。
    绝对deadline 为 当前任务 的 第 T_dl_绝对deadline 项。
    若 当前时间 大于 绝对deadline：
        DL_错过deadline次数 为 DL_错过deadline次数 加 1。
        做 DL_补充 用 当前任务、当前时间。
        返还 真。
    完。
    返还 假。
完。

DL_检查抢占 接受 当前任务、新任务：
    若 当前任务 等于 空 或者 新任务 等于 空：
        返还 假。
    完。
    当前deadline 为 当前任务 的 第 T_dl_绝对deadline 项。
    新deadline 为 新任务 的 第 T_dl_绝对deadline 项。
    返还 新deadline 小于 当前deadline。
完。

DL_可调度 接受 runtime、period：
    利用率 为 runtime 乘 1000 除以 period。
    新总利用率 为 DL_总利用率 加 利用率。
    返还 新总利用率 小于或等于 DL_最大利用率。
完。

DL_获取总利用率 接受 零参：
    返还 DL_总利用率。
完。

DL_获取剩余带宽 接受 零参：
    返还 DL_最大利用率 减 DL_总利用率。
完。

DL_回收带宽 接受 任务：
    剩余 为 任务 的 第 T_dl_剩余runtime 项。
    若 剩余 大于 0：
        做 设置 用 任务、T_dl_剩余runtime、0。
    完。
完。

DL_统计 接受 零参：
    返还 一组：DL_任务数、DL_调度次数、DL_总利用率、DL_错过deadline次数、做 DL_获取剩余带宽。
完。

DL_重置统计 接受 零参：
    DL_调度次数 为 0。
    DL_错过deadline次数 为 0。
完。

DL_是否为空 接受 零参：
    返还 DL_任务数 等于 0。
完。

DL_获取任务数 接受 零参：
    返还 DL_任务数。
完。

DL_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 DL_超时次数 大于 DL_任务数 乘 10 并且 DL_任务数 大于 0：
        做 追加 用 问题、「截止期超时频繁」。
    完。
    若 DL_任务数 大于 256：
        做 追加 用 问题、「DL任务过多: 」 加 做 整数转字符串 用 DL_任务数。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「DL调度器健康」。
    完。
    返还 问题。
完。

DL_准入测试 接受 运行时间、截止期、周期：
    若 周期 等于 0：
        返还 假。
    完。
    若 运行时间 大于 截止期：
        返还 假。
    完。
    若 截止期 大于 周期：
        返还 假。
    完。
    返还 真。
完。

可变 AI_截止期调度_运行指标 为 0。

可变 AI_截止期调度_异常指标 为 0。

AI_截止期调度_自诊断 接受 零参：
    AI_截止期调度_运行指标 为 DL_调度次数。
    返还 AI_截止期调度_运行指标。
完。

AI_截止期调度_优化建议 接受 零参：
    指标 为 做 AI_截止期调度_自诊断。
    若 AI_截止期调度_运行指标 大于 10000：
        返还 「截止期调度负载较高」。
    完。
    返还 「截止期调度状态正常」。
完。

本章 收束。
