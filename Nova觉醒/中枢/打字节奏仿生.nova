《打字节奏仿生》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·鼠标轨迹仿生》。

引用《.·硬链接》。

引用《.·字符串替换》。

引用《.·整数转字符串》。

打字事件 是 一种 事物，含 键码、按下延迟、释放延迟。

仿生_打字 接受 文本：
    可变 事件列表 为 一组：（空）。
    可变 i 为 0。
    平均间隔 为 80。
    错误率 为 30。
    只要 i 小于 做 字符串长度 用 文本：
        ch 为 做 字符串取字节 用 文本、i。
        若 做 仿生_伪随机 取余于 1000 小于 错误率 并且 i 大于 0：
            错误键 为 ch 加 1 加 做 仿生_伪随机 取余于 3。
            错误事件 为 新建 打字事件。
            错误事件 的 键码 为 错误键。
            错误事件 的 按下延迟 为 做 仿生_高斯随机 用 平均间隔、25。
            错误事件 的 释放延迟 为 50 加 做 仿生_伪随机 取余于 40。
            做 追加 用 事件列表、错误事件。
            停顿事件 为 新建 打字事件。
            停顿事件 的 按下延迟 为 200 加 做 仿生_伪随机 取余于 300。
            做 追加 用 事件列表、停顿事件。
            退格事件 为 新建 打字事件。
            退格事件 的 键码 为 8。
            退格事件 的 按下延迟 为 40 加 做 仿生_伪随机 取余于 30。
            退格事件 的 释放延迟 为 30。
            做 追加 用 事件列表、退格事件。
        完。
        事件 为 新建 打字事件。
        事件 的 键码 为 ch。
        可变 间隔 为 做 仿生_高斯随机 用 平均间隔、30。
        若 ch 等于 32：
            间隔 为 间隔 加 20。
        完。
        若 ch 等于 46 或者 ch 等于 44：
            间隔 为 间隔 加 50。
        完。
        若 间隔 小于 30：
            间隔 为 30。
        完。
        事件 的 按下延迟 为 间隔。
        事件 的 释放延迟 为 50 加 做 仿生_伪随机 取余于 40。
        做 追加 用 事件列表、事件。
        i 为 i 加 1。
    完。
    返还 事件列表。
完。

浏览器指纹 是 一种 事物，含 User智能体、平台、语言、时区偏移、屏幕宽、屏幕高、色深、画布哈希、图绘渲染器、图绘厂商、字体列表哈希、插件数、触摸点数、硬件并发数、设备内存GB、Do_Not_Track。

可变 当前指纹 为 空。

指纹_预设数 为 5。

指纹_生成随机 接受 零参：
    fp 为 新建 浏览器指纹。
    r 为 做 仿生_伪随机 取余于 指纹_预设数。
    若 r 等于 0：
        fp 的 User智能体 为 「行业/5.0 (Nova系统 10.0; Win64; x64) Apple行业标准/537.36 (KHTML, like Nova引擎) Nova浏览器/120.0.0.0 Safari/537.36」。
        fp 的 平台 为 「Win32」。
        fp 的 屏幕宽 为 1920。
        fp 的 屏幕高 为 1080。
        fp 的 图绘渲染器 为 「ANGLE (Intel, Intel(R) UHD Graphics 630)」。
        fp 的 硬件并发数 为 8。
        fp 的 设备内存GB 为 16。
    否则若 r 等于 1：
        fp 的 User智能体 为 「行业/5.0 (Macintosh; Intel Mac OS X 10_15_7) Apple行业标准/537.36 Nova浏览器/119.0.0.0 Safari/537.36」。
        fp 的 平台 为 「MacIntel」。
        fp 的 屏幕宽 为 2560。
        fp 的 屏幕高 为 1440。
        fp 的 图绘渲染器 为 「Apple 图处」。
        fp 的 硬件并发数 为 10。
        fp 的 设备内存GB 为 16。
    否则若 r 等于 2：
        fp 的 User智能体 为 「行业/5.0 (X11; Linux x86_64) Apple行业标准/537.36 Nova浏览器/118.0.0.0 Safari/537.36」。
        fp 的 平台 为 「Linux x86_64」。
        fp 的 屏幕宽 为 1920。
        fp 的 屏幕高 为 1080。
        fp 的 图绘渲染器 为 「Mesa Intel(R) UHD Graphics」。
        fp 的 硬件并发数 为 8。
        fp 的 设备内存GB 为 32。
    否则若 r 等于 3：
        fp 的 User智能体 为 「行业/5.0 (Nova系统 10.0; Win64; x64; rv:121.0) Nova引擎/20100101 Firefox/121.0」。
        fp 的 平台 为 「Win32」。
        fp 的 屏幕宽 为 2560。
        fp 的 屏幕高 为 1440。
        fp 的 图绘渲染器 为 「ANGLE (NVIDIA GeForce RTX 3060)」。
        fp 的 硬件并发数 为 12。
        fp 的 设备内存GB 为 32。
    否则：
        fp 的 User智能体 为 「行业/5.0 (Macintosh; Intel Mac OS X 10_15_7) Apple行业标准/605.1.15 (KHTML, like Nova引擎) Version/17.1 Safari/605.1.15」。
        fp 的 平台 为 「MacIntel」。
        fp 的 屏幕宽 为 3024。
        fp 的 屏幕高 为 1964。
        fp 的 图绘渲染器 为 「Apple M2 Pro」。
        fp 的 硬件并发数 为 12。
        fp 的 设备内存GB 为 16。
    完。
    fp 的 语言 为 「zh-CN」。
    fp 的 时区偏移 为 负 480。
    fp 的 色深 为 24。
    fp 的 画布哈希 为 做 仿生_伪随机。
    fp 的 字体列表哈希 为 做 仿生_伪随机。
    fp 的 插件数 为 3 加 做 仿生_伪随机 取余于 5。
    fp 的 触摸点数 为 0。
    fp 的 Do_Not_Track 为 假。
    当前指纹 为 fp。
    返还 fp。
完。

指纹_应用到请求 接受 请求：
    若 当前指纹 等于 空：
        做 指纹_生成随机。
    完。
    做 网络_添加请求头 用 请求、「User-智能体」、当前指纹 的 User智能体。
    做 网络_添加请求头 用 请求、「Accept-Language」、当前指纹 的 语言 加 「,en;q=0.9」。
    做 网络_添加请求头 用 请求、「Sec-CH-UA-Platform」、「\"」 加 当前指纹 的 平台 加 「\"」。
完。

速度_慢速 为 1。

速度_正常 为 2。

速度_快速 为 3。

速度_极快 为 4。

AI打字窗口 为 16。

打字配置 是 一种 事物，含 平均间隔、标准差、错误率、空格额外延迟、标点额外延迟、思考停顿概率、思考停顿时长、最小间隔。

打字_获取配置 接受 速度：
    可变 配置 为 新建 打字配置。
    若 速度 等于 速度_慢速：
        配置 的 平均间隔 为 150。
        配置 的 标准差 为 50。
        配置 的 错误率 为 50。
        配置 的 空格额外延迟 为 40。
        配置 的 标点额外延迟 为 100。
        配置 的 思考停顿概率 为 80。
        配置 的 思考停顿时长 为 1500。
        配置 的 最小间隔 为 60。
    否则若 速度 等于 速度_正常：
        配置 的 平均间隔 为 80。
        配置 的 标准差 为 25。
        配置 的 错误率 为 30。
        配置 的 空格额外延迟 为 20。
        配置 的 标点额外延迟 为 50。
        配置 的 思考停顿概率 为 40。
        配置 的 思考停顿时长 为 800。
        配置 的 最小间隔 为 30。
    否则若 速度 等于 速度_快速：
        配置 的 平均间隔 为 45。
        配置 的 标准差 为 15。
        配置 的 错误率 为 20。
        配置 的 空格额外延迟 为 10。
        配置 的 标点额外延迟 为 25。
        配置 的 思考停顿概率 为 20。
        配置 的 思考停顿时长 为 400。
        配置 的 最小间隔 为 20。
    否则：
        配置 的 平均间隔 为 25。
        配置 的 标准差 为 8。
        配置 的 错误率 为 10。
        配置 的 空格额外延迟 为 5。
        配置 的 标点额外延迟 为 10。
        配置 的 思考停顿概率 为 5。
        配置 的 思考停顿时长 为 200。
        配置 的 最小间隔 为 10。
    完。
    返还 配置。
完。

仿生_打字_配置 接受 文本、速度：
    全局统计 的 打字操作数 为 全局统计 的 打字操作数 加 1。
    配置 为 做 打字_获取配置 用 速度。
    可变 事件列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        ch 为 做 字符串取字节 用 文本、i。
        若 做 仿生_伪随机 取余于 1000 小于 配置 的 错误率 并且 i 大于 0：
            错误键 为 ch 加 1 加 做 仿生_伪随机 取余于 3。
            可变 错误事件 为 新建 打字事件。
            错误事件 的 键码 为 错误键。
            错误事件 的 按下延迟 为 做 仿生_高斯随机 用 配置 的 平均间隔、配置 的 标准差。
            错误事件 的 释放延迟 为 50 加 做 仿生_伪随机 取余于 40。
            做 追加 用 事件列表、错误事件。
            全局统计 的 打字错误数 为 全局统计 的 打字错误数 加 1。
            可变 停顿 为 新建 打字事件。
            停顿 的 按下延迟 为 200 加 做 仿生_伪随机 取余于 300。
            做 追加 用 事件列表、停顿。
            可变 退格 为 新建 打字事件。
            退格 的 键码 为 8。
            退格 的 按下延迟 为 40 加 做 仿生_伪随机 取余于 30。
            退格 的 释放延迟 为 30。
            做 追加 用 事件列表、退格。
            全局统计 的 退格修正数 为 全局统计 的 退格修正数 加 1。
        完。
        可变 事件 为 新建 打字事件。
        事件 的 键码 为 ch。
        Z 为 做 仿生_高斯随机 用 0、配置 的 标准差。
        右偏修正 为 Z 乘 Z 除以 (配置 的 平均间隔 乘 2 加 1)。
        可变 间隔 为 配置 的 平均间隔 加 Z 加 右偏修正。
        若 ch 等于 32：
            间隔 为 间隔 加 配置 的 空格额外延迟。
        完。
        若 ch 等于 46 或者 ch 等于 44 或者 ch 等于 33 或者 ch 等于 63：
            间隔 为 间隔 加 配置 的 标点额外延迟。
        完。
        若 做 仿生_伪随机 取余于 1000 小于 配置 的 思考停顿概率：
            间隔 为 间隔 加 配置 的 思考停顿时长 加 做 仿生_伪随机 取余于 500。
            全局统计 的 思考停顿数 为 全局统计 的 思考停顿数 加 1。
        完。
        若 间隔 小于 配置 的 最小间隔：
            间隔 为 配置 的 最小间隔。
        完。
        事件 的 按下延迟 为 间隔。
        事件 的 释放延迟 为 50 加 做 仿生_伪随机 取余于 40。
        做 追加 用 事件列表、事件。
        全局统计 的 总按键数 为 全局统计 的 总按键数 加 1。
        i 为 i 加 1。
    完。
    做 AI_记录打字速度 用 做 字符串长度 用 文本、速度。
    返还 事件列表。
完。

仿生_粘贴 接受 文本：
    全局统计 的 粘贴操作数 为 全局统计 的 粘贴操作数 加 1。
    可变 事件列表 为 一组：（空）。
    可变 ctrl按下 为 新建 打字事件。
    ctrl按下 的 键码 为 17。
    ctrl按下 的 按下延迟 为 做 仿生_高斯随机 用 100、30。
    ctrl按下 的 释放延迟 为 0。
    做 追加 用 事件列表、ctrl按下。
    可变 v按下 为 新建 打字事件。
    v按下 的 键码 为 86。
    v按下 的 按下延迟 为 30 加 做 仿生_伪随机 取余于 20。
    v按下 的 释放延迟 为 40 加 做 仿生_伪随机 取余于 30。
    做 追加 用 事件列表、v按下。
    可变 ctrl释放 为 新建 打字事件。
    ctrl释放 的 键码 为 17。
    ctrl释放 的 按下延迟 为 20。
    ctrl释放 的 释放延迟 为 30。
    做 追加 用 事件列表、ctrl释放。
    返还 事件列表。
完。

表单字段 是 一种 事物，含 选择器、值、类型。

字段类型_文本 为 0。

字段类型_密码 为 1。

字段类型_邮箱 为 2。

字段类型_电话 为 3。

仿生_填充表单 接受 字段列表：
    若 字段列表 等于 空：
        返还 0。
    完。
    全局统计 的 表单填充数 为 全局统计 的 表单填充数 加 1。
    可变 已填 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字段列表：
        字段 为 字段列表 的 第 i 项。
        若 字段 的 类型 等于 字段类型_密码：
            做 仿生_粘贴 用 字段 的 值。
        否则：
            做 仿生_打字_配置 用 字段 的 值、速度_正常。
        完。
        已填 为 已填 加 1。
        i 为 i 加 1。
    完。
    返还 已填。
完。

仿生_Tab导航 接受 次数：
    全局统计 的 Tab导航数 为 全局统计 的 Tab导航数 加 1。
    可变 事件列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 次数：
        可变 tab事件 为 新建 打字事件。
        tab事件 的 键码 为 9。
        tab事件 的 按下延迟 为 做 仿生_高斯随机 用 200、60。
        tab事件 的 释放延迟 为 50 加 做 仿生_伪随机 取余于 30。
        做 追加 用 事件列表、tab事件。
        i 为 i 加 1。
    完。
    返还 事件列表。
完。

仿生_Enter提交 接受 零参：
    全局统计 的 Enter提交数 为 全局统计 的 Enter提交数 加 1。
    可变 事件 为 新建 打字事件。
    事件 的 键码 为 13。
    事件 的 按下延迟 为 做 仿生_高斯随机 用 300、100。
    事件 的 释放延迟 为 60 加 做 仿生_伪随机 取余于 40。
    返还 事件。
完。

指纹_轮换 接受 零参：
    全局统计 的 指纹轮换数 为 全局统计 的 指纹轮换数 加 1。
    返还 做 指纹_生成随机。
完。

指纹_获取当前 接受 零参：
    若 当前指纹 等于 空：
        做 指纹_生成随机。
    完。
    返还 当前指纹。
完。

指纹_校验一致性 接受 零参：
    若 当前指纹 等于 空：
        返还 假。
    完。
    若 当前指纹 的 屏幕宽 小于或等于 0 或者 当前指纹 的 屏幕高 小于或等于 0：
        返还 假。
    完。
    若 当前指纹 的 硬件并发数 小于或等于 0：
        返还 假。
    完。
    返还 真。
完。

可变 AI打字历史 为 一组：（空）。

可变 AI打字位置 为 0。

打字记录 是 一种 事物，含 文本长度、速度级别、总耗时、错误数。

AI_记录打字速度 接受 长度、速度：
    可变 记录 为 新建 打字记录。
    记录 的 文本长度 为 长度。
    记录 的 速度级别 为 速度。
    记录 的 总耗时 为 0。
    记录 的 错误数 为 0。
    若 做 计数 用 AI打字历史 小于 AI打字窗口：
        做 追加 用 AI打字历史、记录。
    否则：
        做 设置 用 AI打字历史、AI打字位置、记录。
    完。
    AI打字位置 为 (AI打字位置 加 1) 取余于 AI打字窗口。
    全局统计 的 AI学习次数 为 全局统计 的 AI学习次数 加 1。
完。

AI_推荐打字速度 接受 文本长度：
    若 文本长度 大于 200：
        返还 速度_快速。
    完。
    若 文本长度 大于 50：
        返还 速度_正常。
    完。
    返还 速度_慢速。
完。

AI_平均打字WPM 接受 零参：
    可变 总字数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI打字历史：
        总字数 为 总字数 加 AI打字历史 的 第 i 项 的 文本长度。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI打字历史 等于 0：
        返还 0。
    完。
    平均字数 为 总字数 除以 做 计数 用 AI打字历史。
    返还 60000 除以 80 乘 平均字数 除以 5。
完。

打字统计 是 一种 事物，含 打字操作数、总按键数、打字错误数、退格修正数、思考停顿数、粘贴操作数、表单填充数、Tab导航数、Enter提交数、指纹生成数、指纹轮换数、指纹应用数、AI学习次数、AI推荐次数、操作总数、错误次数。

可变 全局统计 为 新建 打字统计。

可变 打字模块已初始化 为 假。

打字模块_初始化 接受 零参：
    若 打字模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI打字历史 为 一组：（空）。
    AI打字位置 为 0。
    当前指纹 为 空。
    打字模块已初始化 为 真。
    做 打印 用 「[打字仿生] 初始化完成」。
    返还 0。
完。

打字模块_销毁 接受 零参：
    若 打字模块已初始化 等于 假：
        返还 0。
    完。
    AI打字历史 为 一组：（空）。
    当前指纹 为 空。
    打字模块已初始化 为 假。
    做 打印 用 「[打字仿生] 已销毁」。
    返还 0。
完。

打字模块_转储 接受 零参：
    做 打印 用 「[打字仿生] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  总按键数: 」、做 整数转字符串 用 全局统计 的 总按键数。
    做 打印 用 做 字符串拼接 用 「  打字错误: 」、做 整数转字符串 用 全局统计 的 打字错误数。
    做 打印 用 做 字符串拼接 用 「  AI估算WPM: 」、做 整数转字符串 用 做 AI_平均打字WPM。
    若 当前指纹 不等于 空：
        做 打印 用 做 字符串拼接 用 「  指纹平台: 」、做 转字符串 用 当前指纹 的 平台。
    完。
    返还 0。
完。

打字模块_打印统计 接受 零参：
    做 打印 用 「[打字仿生] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  打字操作: 」、做 整数转字符串 用 全局统计 的 打字操作数。
    做 打印 用 做 字符串拼接 用 「  总按键数: 」、做 整数转字符串 用 全局统计 的 总按键数。
    做 打印 用 做 字符串拼接 用 「  打字错误: 」、做 整数转字符串 用 全局统计 的 打字错误数。
    做 打印 用 做 字符串拼接 用 「  退格修正: 」、做 整数转字符串 用 全局统计 的 退格修正数。
    做 打印 用 做 字符串拼接 用 「  思考停顿: 」、做 整数转字符串 用 全局统计 的 思考停顿数。
    做 打印 用 做 字符串拼接 用 「  粘贴操作: 」、做 整数转字符串 用 全局统计 的 粘贴操作数。
    做 打印 用 做 字符串拼接 用 「  表单填充: 」、做 整数转字符串 用 全局统计 的 表单填充数。
    做 打印 用 做 字符串拼接 用 「  Tab导航: 」、做 整数转字符串 用 全局统计 的 Tab导航数。
    做 打印 用 做 字符串拼接 用 「  Enter提交: 」、做 整数转字符串 用 全局统计 的 Enter提交数。
    做 打印 用 做 字符串拼接 用 「  指纹生成: 」、做 整数转字符串 用 全局统计 的 指纹生成数。
    做 打印 用 做 字符串拼接 用 「  指纹轮换: 」、做 整数转字符串 用 全局统计 的 指纹轮换数。
    做 打印 用 做 字符串拼接 用 「  指纹应用: 」、做 整数转字符串 用 全局统计 的 指纹应用数。
    做 打印 用 做 字符串拼接 用 「  AI学习: 」、做 整数转字符串 用 全局统计 的 AI学习次数。
    做 打印 用 做 字符串拼接 用 「  操作总数: 」、做 整数转字符串 用 全局统计 的 操作总数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 整数转字符串 用 全局统计 的 错误次数。
    若 全局统计 的 总按键数 大于 0：
        错误率 为 全局统计 的 打字错误数 乘 1000 除以 全局统计 的 总按键数。
        做 打印 用 做 字符串拼接 用 「  错误率‰: 」、做 整数转字符串 用 错误率。
    完。
    返还 0。
完。

打字模块_注册到内核 接受 内核上下文：
    若 打字模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[打字仿生] 已注册到内核上下文」。
    返还 0。
完。

打字模块_查询状态 接受 零参：
    若 打字模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

打字模块_健康检查 接受 零参：
    若 打字模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 操作总数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
