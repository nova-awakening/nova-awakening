《自动回滚》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·三级回滚执》。

引用《.·时钟中枢》。

引用《.·热补丁创建》。

引用《.·整数转字符串》。

回滚_补丁级 为 1。

回滚_模块级 为 2。

回滚_检查点级 为 3。

可变 回滚_活跃 为 假。

可变 回滚_策略 为 回滚_检查点级。

可变 回滚_目标检查点 为 0。

可变 回滚_最近补丁ID 为 0。

可变 回滚_总次数 为 0。

可变 回滚_成功次数 为 0。

可变 回滚_失败次数 为 0。

可变 watchdog_已启用 为 假。

可变 watchdog_计数器 为 0。

可变 watchdog_阈值 为 300。

可变 watchdog_喂狗时间 为 0。

可变 watchdog_超时次数 为 0。

可变 智能_watchdog_启用 为 假。

可变 智能_watchdog_基线质量 为 0。

可变 智能_watchdog_当前质量 为 0。

可变 智能_watchdog_容忍度 为 200。

可变 智能_watchdog_采样数 为 0。

可变 智能_watchdog_检查点 为 0。

可变 心跳_计数 为 0。

可变 心跳_最后时间 为 0。

自动回滚_初始化 接受 零参：
    做 注册panic钩子 用 panic_回滚钩子。
    做 注册oops钩子 用 oops_回滚钩子。
    watchdog_已启用 为 真。
    watchdog_计数器 为 0。
    watchdog_喂狗时间 为 0。
    watchdog_超时次数 为 0。
    心跳_计数 为 0。
    心跳_最后时间 为 做 读取毫秒。
    做 内核信息 用 「自动回滚: 初始化 (panic钩子+oops钩子+watchdog+AI质量监控)」。
完。

panic_回滚钩子 接受 原因：
    若 回滚_活跃：
        返还。
    完。
    回滚_活跃 为 真。
    做 内核错误 用 「╔══════════════════════════════════════╗」。
    做 内核错误 用 「║  自动回滚: 检测到内核崩溃!          ║」。
    做 内核错误 用 「║  原因: 」 加 原因。
    做 内核错误 用 「╚══════════════════════════════════════╝」。
    结果 为 做 执行回滚。
    若 结果 等于 成功：
        回滚_成功次数 为 回滚_成功次数 加 1。
        做 内核信息 用 「自动回滚: ★ 成功恢复! 系统继续运行 ★」。
        回滚_活跃 为 假。
    否则：
        回滚_失败次数 为 回滚_失败次数 加 1。
        做 内核错误 用 「自动回滚: 恢复失败, 系统将停机」。
        回滚_活跃 为 假。
    完。
    回滚_总次数 为 回滚_总次数 加 1。
完。

oops_回滚钩子 接受 错误地址、错误码：
    做 内核警告 用 「自动回滚: 检测到oops @」 加 做 整数转字符串 用 错误地址。
    若 回滚_最近补丁ID 大于 0：
        补丁 为 做 查找补丁 用 回滚_最近补丁ID。
        若 补丁 不等于 空：
            做 内核信息 用 「自动回滚: 回滚最近热补丁 」 加 补丁 的 名称。
            做 热补丁_回滚 用 补丁。
        完。
    完。
完。

本章 收束。
