《TNF打包》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·数学工具》。

引用《.·TNF推理引擎》。

引用《.·整数转字符串》。

可变 打包_总操作数 为 0。

可变 打包_总字节数 为 0。

可变 点积_总次数 为 0。

可变 前向_总次数 为 0。

TNF_打包5 接受 t0、t1、t2、t3、t4：
    打包_总操作数 为 打包_总操作数 加 1。
    返还 t0 加 1 加 (t1 加 1) 乘 3 加 (t2 加 1) 乘 9 加 (t3 加 1) 乘 27 加 (t4 加 1) 乘 81。
完。

TNF_解包5 接受 字节：
    打包_总操作数 为 打包_总操作数 加 1。
    可变 结果 为 一组：（空）。
    可变 v 为 字节。
    可变 i 为 0。
    只要 i 小于 5：
        做 TNF_追加 用 结果、v 取余于 3 减 1。
        v 为 v 除以 3。
        i 为 i 加 1。
    完。
    返还 结果。
完。

TNF_批量打包 接受 三态数组：
    n 为 做 计数 用 三态数组。
    可变 字节数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 4 小于 n：
        做 追加 用 字节数组、做 TNF_打包5 用 三态数组 的 第 i 项、三态数组 的 第 i 加 1 项、三态数组 的 第 i 加 2 项、三态数组 的 第 i 加 3 项、三态数组 的 第 i 加 4 项。
        i 为 i 加 5。
    完。
    若 i 小于 n：
        可变 t 为 一组：0、0、0、0、0。
        可变 j 为 0。
        只要 i 加 j 小于 n 并且 j 小于 5：
            t 的 第 j 项 为 三态数组 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 字节数组、做 TNF_打包5 用 t 的 第 0 项、t 的 第 1 项、t 的 第 2 项、t 的 第 3 项、t 的 第 4 项。
    完。
    打包_总字节数 为 打包_总字节数 加 做 计数 用 字节数组。
    返还 字节数组。
完。

TNF_批量解包 接受 字节数组、总元素数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字节数组：
        五元 为 做 TNF_解包5 用 字节数组 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 5 并且 做 计数 用 结果 小于 总元素数：
            做 TNF_追加 用 结果、五元 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

TNF_打包压缩率 接受 原始大小：
    返还 (原始大小 加 4) 除以 5。
完。

TNF_向量_创建 接受 大小：
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        做 TNF_追加 用 v、平。
        i 为 i 加 1。
    完。
    返还 v。
完。

TNF_随机三态 接受 零参：
    返还 做 TNF_随机 取余于 3 减 1。
完。

TNF_向量_随机 接受 大小：
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        做 TNF_追加 用 v、做 TNF_随机三态。
        i 为 i 加 1。
    完。
    返还 v。
完。

TNF_向量_加 接受 a、b：
    n 为 做 计数 用 a。
    若 做 计数 用 b 小于 n：
        n 为 做 计数 用 b。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        v 为 a 的 第 i 项 加 b 的 第 i 项。
        若 v 大于 阳：
            v 为 阳。
        完。
        若 v 小于 阴：
            v 为 阴。
        完。
        做 TNF_追加 用 结果、v。
        i 为 i 加 1。
    完。
    返还 结果。
完。

TNF_向量_减 接受 a、b：
    n 为 做 计数 用 a。
    若 做 计数 用 b 小于 n：
        n 为 做 计数 用 b。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        v 为 a 的 第 i 项 减 b 的 第 i 项。
        若 v 大于 阳：
            v 为 阳。
        完。
        若 v 小于 阴：
            v 为 阴。
        完。
        做 TNF_追加 用 结果、v。
        i 为 i 加 1。
    完。
    返还 结果。
完。

TNF_向量_点积 接受 a、b：
    点积_总次数 为 点积_总次数 加 1。
    可变 sum 为 0。
    可变 i 为 0。
    n 为 做 计数 用 a。
    若 做 计数 用 b 小于 n：
        n 为 做 计数 用 b。
    完。
    只要 i 小于 n：
        sum 为 sum 加 a 的 第 i 项 乘 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 sum。
完。

TNF_向量_范数 接受 v：
    可变 sum 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        sum 为 sum 加 v 的 第 i 项 乘 v 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 数学_开方 用 sum。
完。

TNF_向量_余弦相似度 接受 a、b：
    点积 为 做 TNF_向量_点积 用 a、b。
    范数a 为 做 TNF_向量_范数 用 a。
    范数b 为 做 TNF_向量_范数 用 b。
    若 范数a 等于 0 或者 范数b 等于 0：
        返还 0。
    完。
    返还 点积 乘 1000 除以 (范数a 乘 范数b)。
完。

TNF_向量_汉明距离 接受 a、b：
    可变 距离 为 0。
    可变 i 为 0。
    n 为 做 计数 用 a。
    若 做 计数 用 b 小于 n：
        n 为 做 计数 用 b。
    完。
    只要 i 小于 n：
        若 a 的 第 i 项 不等于 b 的 第 i 项：
            距离 为 距离 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 距离。
完。

TNF_向量_稀疏度 接受 v：
    可变 零计数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        若 v 的 第 i 项 等于 平：
            零计数 为 零计数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 v 等于 0：
        返还 0。
    完。
    返还 零计数 乘 1000 除以 做 计数 用 v。
完。

TNF_向量_翻转 接受 v：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        做 TNF_追加 用 结果、0 减 v 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

TNF_向量_拼接 接受 a、b：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a：
        做 TNF_追加 用 结果、a 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 b：
        做 TNF_追加 用 结果、b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

TNF_向量_切片 接受 v、起始、长度：
    可变 结果 为 一组：（空）。
    可变 i 为 起始。
    只要 i 小于 起始 加 长度 并且 i 小于 做 计数 用 v：
        做 TNF_追加 用 结果、v 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

TNF_稀疏点积 接受 输入、权重：
    点积_总次数 为 点积_总次数 加 1。
    可变 sum 为 0。
    可变 i 为 0。
    n输入 为 做 计数 用 输入。
    n权重 为 做 计数 用 权重。
    只要 i 小于 n输入 并且 i 小于 n权重：
        若 权重 的 第 i 项 等于 阳：
            sum 为 sum 加 输入 的 第 i 项。
        否则若 权重 的 第 i 项 等于 阴：
            sum 为 sum 减 输入 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 sum。
完。

TNF_矩阵_创建 接受 行、列：
    可变 m 为 一组：（空）。
    可变 r 为 0。
    只要 r 小于 行：
        可变 row 为 一组：（空）。
        可变 c 为 0。
        只要 c 小于 列：
            做 TNF_追加 用 row、平。
            c 为 c 加 1。
        完。
        做 TNF_追加 用 m、row。
        r 为 r 加 1。
    完。
    返还 m。
完。

TNF_矩阵_随机 接受 行、列：
    可变 m 为 一组：（空）。
    可变 r 为 0。
    只要 r 小于 行：
        可变 row 为 一组：（空）。
        可变 c 为 0。
        只要 c 小于 列：
            做 TNF_追加 用 row、做 TNF_随机三态。
            c 为 c 加 1。
        完。
        做 TNF_追加 用 m、row。
        r 为 r 加 1。
    完。
    返还 m。
完。

TNF_矩阵_转置 接受 m、行、列：
    可变 结果 为 做 TNF_矩阵_创建 用 列、行。
    可变 r 为 0。
    只要 r 小于 行：
        可变 c 为 0。
        只要 c 小于 列：
            结果 的 第 c 项 的 第 r 项 为 m 的 第 r 项 的 第 c 项。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 结果。
完。

TNF_矩阵_乘 接受 a、b、a行、a列、b列：
    可变 结果 为 做 TNF_矩阵_创建 用 a行、b列。
    可变 r 为 0。
    只要 r 小于 a行：
        可变 c 为 0。
        只要 c 小于 b列：
            可变 sum 为 0。
            可变 k 为 0。
            只要 k 小于 a列：
                sum 为 sum 加 a 的 第 r 项 的 第 k 项 乘 b 的 第 k 项 的 第 c 项。
                k 为 k 加 1。
            完。
            结果 的 第 r 项 的 第 c 项 为 做 TNF_量化 用 sum。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 结果。
完。

TNF_矩阵_哈达玛 接受 a、b、行、列：
    可变 结果 为 做 TNF_矩阵_创建 用 行、列。
    可变 r 为 0。
    只要 r 小于 行：
        可变 c 为 0。
        只要 c 小于 列：
            结果 的 第 r 项 的 第 c 项 为 做 TNF_量化 用 a 的 第 r 项 的 第 c 项 乘 b 的 第 r 项 的 第 c 项。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 结果。
完。

TNF_矩阵_稀疏度 接受 m、行、列：
    可变 零计数 为 0。
    可变 总计 为 行 乘 列。
    可变 r 为 0。
    只要 r 小于 行：
        可变 c 为 0。
        只要 c 小于 列：
            若 m 的 第 r 项 的 第 c 项 等于 平：
                零计数 为 零计数 加 1。
            完。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
    若 总计 等于 0：
        返还 0。
    完。
    返还 零计数 乘 1000 除以 总计。
完。

TNF_矩阵_序列化 接受 m、行、列：
    可变 平面 为 一组：（空）。
    可变 r 为 0。
    只要 r 小于 行：
        可变 c 为 0。
        只要 c 小于 列：
            做 TNF_追加 用 平面、m 的 第 r 项 的 第 c 项。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 做 TNF_批量打包 用 平面。
完。

TNF_矩阵_反序列化 接受 字节数组、行、列：
    平面 为 做 TNF_批量解包 用 字节数组、行 乘 列。
    可变 m 为 一组：（空）。
    可变 r 为 0。
    只要 r 小于 行：
        可变 row 为 一组：（空）。
        可变 c 为 0。
        只要 c 小于 列：
            做 TNF_追加 用 row、平面 的 第 r 乘 列 加 c 项。
            c 为 c 加 1。
        完。
        做 TNF_追加 用 m、row。
        r 为 r 加 1。
    完。
    返还 m。
完。

TNF_前向 接受 输入、权重矩阵、行数：
    前向_总次数 为 前向_总次数 加 1。
    可变 输出 为 一组：（空）。
    可变 r 为 0。
    只要 r 小于 行数：
        sum 为 做 TNF_稀疏点积 用 输入、权重矩阵 的 第 r 项。
        做 TNF_追加 用 输出、做 TNF_量化 用 sum。
        r 为 r 加 1。
    完。
    返还 输出。
完。

TNF_前向_带偏置 接受 输入、权重矩阵、偏置、行数：
    前向_总次数 为 前向_总次数 加 1。
    可变 输出 为 一组：（空）。
    可变 r 为 0。
    只要 r 小于 行数：
        sum 为 做 TNF_稀疏点积 用 输入、权重矩阵 的 第 r 项。
        若 r 小于 做 计数 用 偏置：
            sum 为 sum 加 偏置 的 第 r 项。
        完。
        做 TNF_追加 用 输出、做 TNF_量化 用 sum。
        r 为 r 加 1。
    完。
    返还 输出。
完。

TNF_多层前向 接受 输入、层列表、层行数列表：
    可变 当前 为 输入。
    可变 l 为 0。
    只要 l 小于 做 计数 用 层列表：
        当前 为 做 TNF_前向 用 当前、层列表 的 第 l 项、层行数列表 的 第 l 项。
        l 为 l 加 1。
    完。
    返还 当前。
完。

TNF_残差前向 接受 输入、权重矩阵、行数：
    若 行数 不等于 做 计数 用 输入：
        返还 做 TNF_前向 用 输入、权重矩阵、行数。
    完。
    变换 为 做 TNF_前向 用 输入、权重矩阵、行数。
    返还 做 TNF_向量_加 用 输入、变换。
完。

TNF_打包_完整统计 接受 零参：
    返还 一组：打包_总操作数、打包_总字节数、点积_总次数、前向_总次数。
完。

TNF_矩阵_范数 接受 m、行、列：
    可变 最大 为 0。
    可变 r 为 0。
    只要 r 小于 行：
        可变 c 为 0。
        只要 c 小于 列：
            v 为 m 的 第 r 项 的 第 c 项。
            若 v 小于 0：
                v 为 0 减 v。
            完。
            若 v 大于 最大：
                最大 为 v。
            完。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 最大。
完。

TNF_矩阵_缩放 接受 m、行、列、因子：
    可变 结果 为 做 TNF_矩阵_创建 用 行、列。
    可变 r 为 0。
    只要 r 小于 行：
        可变 c 为 0。
        只要 c 小于 列：
            结果 的 第 r 项 的 第 c 项 为 做 TNF_量化 用 m 的 第 r 项 的 第 c 项 乘 因子 除以 1000。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 结果。
完。

TNF_矩阵_加 接受 a、b、行、列：
    可变 结果 为 做 TNF_矩阵_创建 用 行、列。
    可变 r 为 0。
    只要 r 小于 行：
        可变 c 为 0。
        只要 c 小于 列：
            结果 的 第 r 项 的 第 c 项 为 做 TNF_量化 用 a 的 第 r 项 的 第 c 项 加 b 的 第 r 项 的 第 c 项。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 结果。
完。

TNF_矩阵_行和 接受 m、行、列、行号：
    若 行号 小于 0 或者 行号 大于或等于 行：
        返还 0。
    完。
    可变 sum 为 0。
    可变 c 为 0。
    只要 c 小于 列：
        sum 为 sum 加 m 的 第 行号 项 的 第 c 项。
        c 为 c 加 1。
    完。
    返还 sum。
完。

TNF_矩阵_列和 接受 m、行、列、列号：
    若 列号 小于 0 或者 列号 大于或等于 列：
        返还 0。
    完。
    可变 sum 为 0。
    可变 r 为 0。
    只要 r 小于 行：
        sum 为 sum 加 m 的 第 r 项 的 第 列号 项。
        r 为 r 加 1。
    完。
    返还 sum。
完。

TNF_重置统计 接受 零参：
    打包_总操作数 为 0。
    打包_总字节数 为 0。
    点积_总次数 为 0。
    前向_总次数 为 0。
完。

TNF_获取打包操作数 接受 零参：
    返还 打包_总操作数。
完。

TNF_获取打包字节数 接受 零参：
    返还 打包_总字节数。
完。

TNF_获取点积次数 接受 零参：
    返还 点积_总次数。
完。

TNF_获取前向次数 接受 零参：
    返还 前向_总次数。
完。

TNF_打印统计 接受 零参：
    做 内核信息 用 「=== TNF 神经打包统计 ===」。
    做 内核信息 用 「打包操作: 」 加 做 整数转字符串 用 打包_总操作数。
    做 内核信息 用 「打包字节: 」 加 做 整数转字符串 用 打包_总字节数。
    做 内核信息 用 「点积次数: 」 加 做 整数转字符串 用 点积_总次数。
    做 内核信息 用 「前向次数: 」 加 做 整数转字符串 用 前向_总次数。
完。

TNF_矩阵_迹 接受 m、尺寸：
    可变 sum 为 0。
    可变 i 为 0。
    只要 i 小于 尺寸：
        sum 为 sum 加 m 的 第 i 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 sum。
完。

TNF_矩阵_单位 接受 尺寸：
    可变 m 为 做 TNF_矩阵_创建 用 尺寸、尺寸。
    可变 i 为 0。
    只要 i 小于 尺寸：
        m 的 第 i 项 的 第 i 项 为 正。
        i 为 i 加 1。
    完。
    返还 m。
完。

TNF_向量_最大值 接受 v：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        若 v 的 第 i 项 大于 最大：
            最大 为 v 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

TNF_向量_最小值 接受 v：
    若 做 计数 用 v 等于 0：
        返还 0。
    完。
    可变 最小 为 v 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 v：
        若 v 的 第 i 项 小于 最小：
            最小 为 v 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小。
完。

TNF_向量_总和 接受 v：
    可变 sum 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        sum 为 sum 加 v 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 sum。
完。

可变 _谐振打包_历史压缩率 为 一组：（空）。

可变 _谐振打包_预测次数 为 0。

谐振打包_记录压缩率 接受 原始大小、压缩大小：
    若 原始大小 大于 0：
        比率 为 压缩大小 乘 1000 除以 原始大小。
        做 追加 用 _谐振打包_历史压缩率、比率。
        若 做 计数 用 _谐振打包_历史压缩率 大于 32：
            可变 新表 为 一组：（空）。
            可变 i 为 做 计数 用 _谐振打包_历史压缩率 减 32。
            只要 i 小于 做 计数 用 _谐振打包_历史压缩率：
                做 追加 用 新表、_谐振打包_历史压缩率 的 第 i 项。
                i 为 i 加 1。
            完。
            _谐振打包_历史压缩率 为 新表。
        完。
    完。
完。

谐振打包_预测缓冲区大小 接受 原始大小：
    若 做 计数 用 _谐振打包_历史压缩率 等于 0：
        返还 原始大小。
    完。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _谐振打包_历史压缩率：
        总和 为 总和 加 _谐振打包_历史压缩率 的 第 i 项。
        i 为 i 加 1。
    完。
    平均率 为 总和 除以 做 计数 用 _谐振打包_历史压缩率。
    _谐振打包_预测次数 为 _谐振打包_预测次数 加 1。
    返还 原始大小 乘 平均率 除以 1000 加 64。
完。

神经打包_完整统计 接受 零参：
    返还 一组：「谐振预测次数」、_谐振打包_预测次数、「历史记录数」、做 计数 用 _谐振打包_历史压缩率。
完。

本章 收束。
