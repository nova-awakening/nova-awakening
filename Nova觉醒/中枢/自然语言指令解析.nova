《自然语言指令解析》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·字符串替换》。

引用《..·执行·控制台输出》。

最大意图数 为 16。

最大关键词数 为 256。

最大上下文轮数 为 16。

最大实体长度 为 128。

默认置信度 为 700。

低置信度阈值 为 400。

高置信度阈值 为 850。

关键词匹配加分 为 150。

上下文加分 为 100。

歧义惩罚 为 200。

AI校准窗口 为 32。

意图_未知 为 0。

意图_问候 为 1。

意图_查询 为 2。

意图_执行 为 3。

意图_导航 为 4。

意图_播放 为 5。

意图_设置 为 6。

意图_聊天 为 7。

意图_关闭 为 8。

意图_截图 为 9。

意图_拍照 为 10。

意图_翻译 为 11。

意图_计算 为 12。

意图_提醒 为 13。

意图_天气 为 14。

意图_帮助 为 15。

指令解析结果 是 一种 事物，含 意图、子意图、实体、参数列表、置信度、原文、上下文关联、时间戳。

关键词规则 是 一种 事物，含 关键词、意图、权重、是否前缀。

对话上下文 是 一种 事物，含 历史、轮数、最后意图、最后实体、会话开始时间。

NLU统计 是 一种 事物，含 解析总数、意图_问候数、意图_查询数、意图_执行数、意图_导航数、意图_播放数、意图_设置数、意图_聊天数、意图_关闭数、低置信度数、高置信度数、歧义解析数、上下文引用数、执行成功数、执行失败数、AI校准次数、关键词命中数、错误次数。

可变 关键词规则表 为 一组：（空）。

可变 规则数量 为 0。

可变 当前上下文 为 空。

可变 已初始化 为 假。

可变 全局统计 为 新建 NLU统计。

可变 AI校准缓冲 为 一组：（空）。

可变 AI校准位置 为 0。

指令解析_初始化 接受 零参：
    若 已初始化 等于 真：
        返还 错误_已存在。
    完。
    关键词规则表 为 一组：（空）。
    规则数量 为 0。
    做 指令解析_注册规则 用 「你好」、意图_问候、900、假。
    做 指令解析_注册规则 用 「嗨」、意图_问候、850、假。
    做 指令解析_注册规则 用 「早上好」、意图_问候、900、假。
    做 指令解析_注册规则 用 「晚上好」、意图_问候、900、假。
    做 指令解析_注册规则 用 「打开」、意图_执行、850、真。
    做 指令解析_注册规则 用 「启动」、意图_执行、850、真。
    做 指令解析_注册规则 用 「运行」、意图_执行、800、真。
    做 指令解析_注册规则 用 「搜索」、意图_查询、850、真。
    做 指令解析_注册规则 用 「查找」、意图_查询、850、真。
    做 指令解析_注册规则 用 「什么是」、意图_查询、800、真。
    做 指令解析_注册规则 用 「怎么」、意图_查询、750、真。
    做 指令解析_注册规则 用 「去」、意图_导航、700、真。
    做 指令解析_注册规则 用 「导航」、意图_导航、900、真。
    做 指令解析_注册规则 用 「前往」、意图_导航、850、真。
    做 指令解析_注册规则 用 「播放」、意图_播放、900、真。
    做 指令解析_注册规则 用 「放」、意图_播放、600、真。
    做 指令解析_注册规则 用 「听」、意图_播放、700、真。
    做 指令解析_注册规则 用 「设置」、意图_设置、850、真。
    做 指令解析_注册规则 用 「调」、意图_设置、650、真。
    做 指令解析_注册规则 用 「修改」、意图_设置、800、真。
    做 指令解析_注册规则 用 「关闭」、意图_关闭、900、假。
    做 指令解析_注册规则 用 「退出」、意图_关闭、900、假。
    做 指令解析_注册规则 用 「再见」、意图_关闭、850、假。
    做 指令解析_注册规则 用 「截图」、意图_截图、950、假。
    做 指令解析_注册规则 用 「截屏」、意图_截图、950、假。
    做 指令解析_注册规则 用 「拍照」、意图_拍照、950、假。
    做 指令解析_注册规则 用 「翻译」、意图_翻译、900、真。
    做 指令解析_注册规则 用 「计算」、意图_计算、900、真。
    做 指令解析_注册规则 用 「算」、意图_计算、700、真。
    做 指令解析_注册规则 用 「提醒」、意图_提醒、900、真。
    做 指令解析_注册规则 用 「天气」、意图_天气、900、假。
    做 指令解析_注册规则 用 「帮助」、意图_帮助、900、假。
    做 指令解析_注册规则 用 「怎么用」、意图_帮助、850、假。
    可变 ctx 为 新建 对话上下文。
    ctx 的 历史 为 一组：（空）。
    ctx 的 轮数 为 0。
    ctx 的 最后意图 为 意图_未知。
    ctx 的 最后实体 为 「」。
    ctx 的 会话开始时间 为 0。
    当前上下文 为 ctx。
    AI校准缓冲 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI校准窗口：
        做 追加 用 AI校准缓冲、0。
        i 为 i 加 1。
    完。
    AI校准位置 为 0。
    已初始化 为 真。
    做 打印 用 「[指令解析] NLU引擎初始化完成」。
    返还 0。
完。

指令解析_销毁 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    关键词规则表 为 一组：（空）。
    规则数量 为 0。
    当前上下文 为 空。
    AI校准缓冲 为 一组：（空）。
    已初始化 为 假。
    做 打印 用 「[指令解析] 已销毁」。
    返还 0。
完。

指令解析_注册规则 接受 关键词、意图、权重、是否前缀：
    若 规则数量 大于或等于 最大关键词数：
        返还 错误_内存不足。
    完。
    可变 规则 为 新建 关键词规则。
    规则 的 关键词 为 关键词。
    规则 的 意图 为 意图。
    规则 的 权重 为 权重。
    规则 的 是否前缀 为 是否前缀。
    做 追加 用 关键词规则表、规则。
    规则数量 为 规则数量 加 1。
    返还 0。
完。

文本包含 接受 文本、子串：
    若 文本 等于 空 或者 子串 等于 空：
        返还 假。
    完。
    结果 为 做 查找 用 文本、子串。
    若 结果 大于或等于 0：
        返还 真。
    完。
    返还 假。
完。

文本以开头 接受 文本、前缀：
    若 文本 等于 空 或者 前缀 等于 空：
        返还 假。
    完。
    返还 做 以开头 用 文本、前缀。
完。

指令_解析 接受 文本：
    若 已初始化 等于 假：
        做 指令解析_初始化。
    完。
    若 文本 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        可变 空结果 为 新建 指令解析结果。
        空结果 的 意图 为 意图_未知。
        空结果 的 置信度 为 0。
        返还 空结果。
    完。
    全局统计 的 解析总数 为 全局统计 的 解析总数 加 1。
    可变 结果 为 新建 指令解析结果。
    结果 的 原文 为 文本。
    结果 的 置信度 为 0。
    结果 的 意图 为 意图_未知。
    结果 的 子意图 为 0。
    结果 的 实体 为 「」。
    结果 的 参数列表 为 一组：（空）。
    结果 的 上下文关联 为 假。
    可变 最佳意图 为 意图_未知。
    可变 最佳权重 为 0。
    可变 匹配数 为 0。
    可变 候选意图列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 规则数量：
        规则 为 关键词规则表 的 第 i 项。
        可变 匹配 为 假。
        若 规则 的 是否前缀 等于 真：
            匹配 为 做 文本以开头 用 文本、规则 的 关键词。
        否则：
            匹配 为 做 文本包含 用 文本、规则 的 关键词。
        完。
        若 匹配 等于 真：
            全局统计 的 关键词命中数 为 全局统计 的 关键词命中数 加 1。
            匹配数 为 匹配数 加 1。
            做 追加 用 候选意图列表、一组：规则 的 意图、规则 的 权重。
            若 规则 的 权重 大于 最佳权重：
                最佳权重 为 规则 的 权重。
                最佳意图 为 规则 的 意图。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 不同意图数 为 0。
    可变 已见意图 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 做 计数 用 候选意图列表：
        候选 为 候选意图列表 的 第 j 项 的 第 0 项。
        可变 已见 为 假。
        可变 k 为 0。
        只要 k 小于 做 计数 用 已见意图：
            若 已见意图 的 第 k 项 等于 候选：
                已见 为 真。
            完。
            k 为 k 加 1。
        完。
        若 已见 等于 假：
            做 追加 用 已见意图、候选。
            不同意图数 为 不同意图数 加 1。
        完。
        j 为 j 加 1。
    完。
    若 不同意图数 大于 1：
        全局统计 的 歧义解析数 为 全局统计 的 歧义解析数 加 1。
        最佳权重 为 最佳权重 减 歧义惩罚。
    完。
    若 当前上下文 不等于 空 并且 当前上下文 的 最后意图 等于 最佳意图：
        最佳权重 为 最佳权重 加 上下文加分。
        结果 的 上下文关联 为 真。
        全局统计 的 上下文引用数 为 全局统计 的 上下文引用数 加 1。
    完。
    若 最佳意图 等于 意图_未知：
        最佳意图 为 意图_聊天。
        最佳权重 为 500。
    完。
    结果 的 意图 为 最佳意图。
    结果 的 置信度 为 最佳权重。
    若 结果 的 置信度 大于 1000：
        结果 的 置信度 为 1000。
    完。
    若 结果 的 置信度 小于 0：
        结果 的 置信度 为 0。
    完。
    结果 的 实体 为 做 指令_提取实体 用 文本、最佳意图。
    若 结果 的 置信度 小于 低置信度阈值：
        全局统计 的 低置信度数 为 全局统计 的 低置信度数 加 1。
    否则若 结果 的 置信度 大于 高置信度阈值：
        全局统计 的 高置信度数 为 全局统计 的 高置信度数 加 1。
    完。
    若 最佳意图 等于 意图_问候：
        全局统计 的 意图_问候数 为 全局统计 的 意图_问候数 加 1。
    否则若 最佳意图 等于 意图_查询：
        全局统计 的 意图_查询数 为 全局统计 的 意图_查询数 加 1。
    否则若 最佳意图 等于 意图_执行：
        全局统计 的 意图_执行数 为 全局统计 的 意图_执行数 加 1。
    否则若 最佳意图 等于 意图_导航：
        全局统计 的 意图_导航数 为 全局统计 的 意图_导航数 加 1。
    否则若 最佳意图 等于 意图_播放：
        全局统计 的 意图_播放数 为 全局统计 的 意图_播放数 加 1。
    否则若 最佳意图 等于 意图_设置：
        全局统计 的 意图_设置数 为 全局统计 的 意图_设置数 加 1。
    否则若 最佳意图 等于 意图_聊天：
        全局统计 的 意图_聊天数 为 全局统计 的 意图_聊天数 加 1。
    否则若 最佳意图 等于 意图_关闭：
        全局统计 的 意图_关闭数 为 全局统计 的 意图_关闭数 加 1。
    完。
    做 指令_更新上下文 用 结果。
    返还 结果。
完。

指令_提取实体 接受 文本、意图：
    若 文本 等于 空：
        返还 「」。
    完。
    若 意图 等于 意图_执行：
        若 做 文本包含 用 文本、「打开」：
            位置 为 做 查找 用 文本、「打开」。
            若 位置 大于或等于 0：
                返还 做 子串 用 文本、位置 加 6、做 字节长度 用 文本 减 位置 减 6。
            完。
        完。
        若 做 文本包含 用 文本、「启动」：
            位置 为 做 查找 用 文本、「启动」。
            若 位置 大于或等于 0：
                返还 做 子串 用 文本、位置 加 6、做 字节长度 用 文本 减 位置 减 6。
            完。
        完。
    完。
    若 意图 等于 意图_播放：
        若 做 文本包含 用 文本、「播放」：
            位置 为 做 查找 用 文本、「播放」。
            若 位置 大于或等于 0：
                返还 做 子串 用 文本、位置 加 6、做 字节长度 用 文本 减 位置 减 6。
            完。
        完。
    完。
    若 意图 等于 意图_翻译：
        若 做 文本包含 用 文本、「翻译」：
            位置 为 做 查找 用 文本、「翻译」。
            若 位置 大于或等于 0：
                返还 做 子串 用 文本、位置 加 6、做 字节长度 用 文本 减 位置 减 6。
            完。
        完。
    完。
    返还 文本。
完。

指令_更新上下文 接受 结果：
    若 当前上下文 等于 空：
        返还。
    完。
    当前上下文 的 最后意图 为 结果 的 意图。
    当前上下文 的 最后实体 为 结果 的 实体。
    当前上下文 的 轮数 为 当前上下文 的 轮数 加 1。
    若 做 计数 用 当前上下文 的 历史 小于 最大上下文轮数：
        做 追加 用 当前上下文 的 历史、一组：结果 的 意图、结果 的 实体、结果 的 置信度。
    否则：
        做 删除 用 当前上下文 的 历史、0。
        做 追加 用 当前上下文 的 历史、一组：结果 的 意图、结果 的 实体、结果 的 置信度。
    完。
完。

指令_重置上下文 接受 零参：
    若 当前上下文 等于 空：
        返还。
    完。
    当前上下文 的 历史 为 一组：（空）。
    当前上下文 的 轮数 为 0。
    当前上下文 的 最后意图 为 意图_未知。
    当前上下文 的 最后实体 为 「」。
完。

指令_获取上下文轮数 接受 零参：
    若 当前上下文 等于 空：
        返还 0。
    完。
    返还 当前上下文 的 轮数。
完。

指令_获取上轮意图 接受 零参：
    若 当前上下文 等于 空：
        返还 意图_未知。
    完。
    返还 当前上下文 的 最后意图。
完。

意图_获取名称 接受 意图：
    若 意图 等于 意图_问候：
        返还 「问候」。
    完。
    若 意图 等于 意图_查询：
        返还 「查询」。
    完。
    若 意图 等于 意图_执行：
        返还 「执行」。
    完。
    若 意图 等于 意图_导航：
        返还 「导航」。
    完。
    若 意图 等于 意图_播放：
        返还 「播放」。
    完。
    若 意图 等于 意图_设置：
        返还 「设置」。
    完。
    若 意图 等于 意图_聊天：
        返还 「聊天」。
    完。
    若 意图 等于 意图_关闭：
        返还 「关闭」。
    完。
    若 意图 等于 意图_截图：
        返还 「截图」。
    完。
    若 意图 等于 意图_拍照：
        返还 「拍照」。
    完。
    若 意图 等于 意图_翻译：
        返还 「翻译」。
    完。
    若 意图 等于 意图_计算：
        返还 「计算」。
    完。
    若 意图 等于 意图_提醒：
        返还 「提醒」。
    完。
    若 意图 等于 意图_天气：
        返还 「天气」。
    完。
    若 意图 等于 意图_帮助：
        返还 「帮助」。
    完。
    返还 「未知」。
完。

AI_记录解析反馈 接受 正确：
    若 正确 等于 真：
        做 设置 用 AI校准缓冲、AI校准位置、1。
    否则：
        做 设置 用 AI校准缓冲、AI校准位置、0。
    完。
    AI校准位置 为 (AI校准位置 加 1) 取余于 AI校准窗口。
    全局统计 的 AI校准次数 为 全局统计 的 AI校准次数 加 1。
完。

AI_计算准确率 接受 零参：
    可变 正确数 为 0。
    可变 i 为 0。
    只要 i 小于 AI校准窗口：
        若 AI校准缓冲 的 第 i 项 等于 1：
            正确数 为 正确数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 正确数 乘 100 除以 AI校准窗口。
完。

AI_建议置信度调整 接受 零参：
    准确率 为 做 AI_计算准确率。
    若 准确率 大于 80：
        返还 50。
    完。
    若 准确率 大于 60：
        返还 0。
    完。
    若 准确率 大于 40：
        返还 0 减 50。
    完。
    返还 0 减 100。
完。

指令_执行 接受 助手、指令：
    若 助手 等于 空 或者 指令 等于 空：
        全局统计 的 执行失败数 为 全局统计 的 执行失败数 加 1。
        返还 错误_无效参数。
    完。
    可变 执行结果 为 0。
    若 指令 的 意图 等于 意图_问候：
        做 打印 用 「[NLU] 执行: 问候」。
        执行结果 为 0。
    否则若 指令 的 意图 等于 意图_执行：
        做 打印 用 做 字符串拼接 用 「[NLU] 执行: 启动 」、做 转字符串 用 指令 的 实体。
        执行结果 为 0。
    否则若 指令 的 意图 等于 意图_查询：
        做 打印 用 做 字符串拼接 用 「[NLU] 执行: 查询 」、做 转字符串 用 指令 的 实体。
        执行结果 为 0。
    否则若 指令 的 意图 等于 意图_导航：
        做 打印 用 做 字符串拼接 用 「[NLU] 执行: 导航 」、做 转字符串 用 指令 的 实体。
        执行结果 为 0。
    否则若 指令 的 意图 等于 意图_播放：
        做 打印 用 做 字符串拼接 用 「[NLU] 执行: 播放 」、做 转字符串 用 指令 的 实体。
        执行结果 为 0。
    否则若 指令 的 意图 等于 意图_设置：
        做 打印 用 做 字符串拼接 用 「[NLU] 执行: 设置 」、做 转字符串 用 指令 的 实体。
        执行结果 为 0。
    否则若 指令 的 意图 等于 意图_关闭：
        做 打印 用 「[NLU] 执行: 关闭」。
        执行结果 为 0。
    否则若 指令 的 意图 等于 意图_截图：
        做 打印 用 「[NLU] 执行: 截图」。
        执行结果 为 0。
    否则若 指令 的 意图 等于 意图_翻译：
        做 打印 用 做 字符串拼接 用 「[NLU] 执行: 翻译 」、做 转字符串 用 指令 的 实体。
        执行结果 为 0。
    否则若 指令 的 意图 等于 意图_计算：
        做 打印 用 做 字符串拼接 用 「[NLU] 执行: 计算 」、做 转字符串 用 指令 的 实体。
        执行结果 为 0。
    否则若 指令 的 意图 等于 意图_天气：
        做 打印 用 「[NLU] 执行: 查询天气」。
        执行结果 为 0。
    否则若 指令 的 意图 等于 意图_帮助：
        做 打印 用 「[NLU] 执行: 显示帮助」。
        执行结果 为 0。
    否则：
        做 打印 用 「[NLU] 执行: 聊天模式」。
        执行结果 为 0。
    完。
    若 执行结果 等于 0：
        全局统计 的 执行成功数 为 全局统计 的 执行成功数 加 1。
    否则：
        全局统计 的 执行失败数 为 全局统计 的 执行失败数 加 1。
    完。
    返还 执行结果。
完。

指令_批量解析 接受 文本列表：
    若 文本列表 等于 空：
        返还 一组：（空）。
    完。
    可变 结果列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本列表：
        结果 为 做 指令_解析 用 文本列表 的 第 i 项。
        做 追加 用 结果列表、结果。
        i 为 i 加 1。
    完。
    返还 结果列表。
完。

指令_获取意图分布 接受 零参：
    可变 分布 为 一组：（空）。
    做 追加 用 分布、一组：「问候」、全局统计 的 意图_问候数。
    做 追加 用 分布、一组：「查询」、全局统计 的 意图_查询数。
    做 追加 用 分布、一组：「执行」、全局统计 的 意图_执行数。
    做 追加 用 分布、一组：「导航」、全局统计 的 意图_导航数。
    做 追加 用 分布、一组：「播放」、全局统计 的 意图_播放数。
    做 追加 用 分布、一组：「设置」、全局统计 的 意图_设置数。
    做 追加 用 分布、一组：「聊天」、全局统计 的 意图_聊天数。
    做 追加 用 分布、一组：「关闭」、全局统计 的 意图_关闭数。
    返还 分布。
完。

指令_转储结果 接受 结果：
    若 结果 等于 空：
        返还。
    完。
    做 打印 用 「[NLU] ── 解析结果 ──」。
    做 打印 用 做 字符串拼接 用 「  意图: 」、做 意图_获取名称 用 结果 的 意图。
    做 打印 用 做 字符串拼接 用 「  实体: 」、做 转字符串 用 结果 的 实体。
    做 打印 用 做 字符串拼接 用 「  置信度: 」、做 转字符串 用 结果 的 置信度。
    做 打印 用 做 字符串拼接 用 「  原文: 」、做 转字符串 用 结果 的 原文。
    做 打印 用 做 字符串拼接 用 「  上下文关联: 」、做 转字符串 用 结果 的 上下文关联。
完。

指令解析_打印统计 接受 零参：
    做 打印 用 「[指令解析] ═══ NLU统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  解析总数: 」、做 转字符串 用 全局统计 的 解析总数。
    做 打印 用 做 字符串拼接 用 「  问候: 」、做 转字符串 用 全局统计 的 意图_问候数。
    做 打印 用 做 字符串拼接 用 「  查询: 」、做 转字符串 用 全局统计 的 意图_查询数。
    做 打印 用 做 字符串拼接 用 「  执行: 」、做 转字符串 用 全局统计 的 意图_执行数。
    做 打印 用 做 字符串拼接 用 「  播放: 」、做 转字符串 用 全局统计 的 意图_播放数。
    做 打印 用 做 字符串拼接 用 「  聊天: 」、做 转字符串 用 全局统计 的 意图_聊天数。
    做 打印 用 做 字符串拼接 用 「  低置信度: 」、做 转字符串 用 全局统计 的 低置信度数。
    做 打印 用 做 字符串拼接 用 「  高置信度: 」、做 转字符串 用 全局统计 的 高置信度数。
    做 打印 用 做 字符串拼接 用 「  歧义解析: 」、做 转字符串 用 全局统计 的 歧义解析数。
    做 打印 用 做 字符串拼接 用 「  上下文引用: 」、做 转字符串 用 全局统计 的 上下文引用数。
    做 打印 用 做 字符串拼接 用 「  执行成功: 」、做 转字符串 用 全局统计 的 执行成功数。
    做 打印 用 做 字符串拼接 用 「  执行失败: 」、做 转字符串 用 全局统计 的 执行失败数。
    做 打印 用 做 字符串拼接 用 「  AI校准: 」、做 转字符串 用 全局统计 的 AI校准次数。
    做 打印 用 做 字符串拼接 用 「  关键词命中: 」、做 转字符串 用 全局统计 的 关键词命中数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    若 全局统计 的 解析总数 大于 0：
        成功率 为 全局统计 的 执行成功数 乘 100 除以 全局统计 的 解析总数。
        做 打印 用 做 字符串拼接 用 「  执行成功率%: 」、做 转字符串 用 成功率。
    完。
    返还 0。
完。

指令解析_注册到内核 接受 内核上下文：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    做 打印 用 「[指令解析] 已注册到内核上下文」。
    返还 0。
完。

指令解析_查询状态 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

指令解析_健康检查 接受 零参：
    若 已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 解析总数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
