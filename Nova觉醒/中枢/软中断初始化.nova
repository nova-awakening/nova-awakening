《软中断初始化》

引用《.·日志中枢》。

引用《.·类型定义》。

引用《.·错误码》。

引用《..·推理·通信渠道》。

引用《.·公平调度唤醒》。

引用《.·内核打印》。

引用《..·推理·定点Q算术》。

引用《.·页表重建》。

软中断_高优先级 为 0。

软中断_定时器 为 1。

软中断_网络发送 为 2。

软中断_网络接收 为 3。

软中断_块设备 为 4。

软中断_IRQ_POLL 为 5。

软中断_调度 为 6。

软中断_高精度定时器 为 7。

软中断_RCU 为 8。

软中断_TASKLET 为 9。

软中断_数量 为 10。

软中断_最大重启次数 为 10。

软中断_时间片_ns 为 2000000。

软中断_风暴阈值 为 1000。

EWMA_衰减 为 224。

EWMA_补 为 32。

Q_SHIFT 为 8。

软中断名称表 为 一组：「HI」、「TIMER」、「NET_TX」、「NET_RX」、「BLOCK」、「IRQ_POLL」、「SCHED」、「HRTIMER」、「RCU」、「TASKLET」。

软中断动作 是 一种 事物，含 处理函数、名称、优先级。

可变 软中断动作表 为 做 数组 用 软中断动作、10。

软中断_CPU状态 是 一种 事物，含 挂起、正在处理、处于中断上下文、嵌套计数、ksoftirqd_唤醒、禁用计数。

可变 软中断状态 为 新建 软中断_CPU状态。

软中断统计 是 一种 事物，含 触发次数、执行次数、累计耗时_ns、最大耗时_ns、总执行次数、ksoftirqd次数、风暴检测次数、被延迟次数。

可变 统计 为 新建 软中断统计。

TASKLET_状态_就绪 为 0。

TASKLET_状态_调度中 为 1。

TASKLET_状态_运行中 为 2。

TASKLET_状态_禁用 为 4。

最大TASKLET数 为 64。

Tasklet节点 是 一种 事物，含 回调函数、数据、状态、禁用计数、有效、名称。

可变 普通tasklet队列 为 做 数组 用 Tasklet节点、64。

可变 普通tasklet数量 为 0。

可变 高优tasklet队列 为 做 数组 用 Tasklet节点、64。

可变 高优tasklet数量 为 0。

频率追踪器 是 一种 事物，含 ewma_频率_q8、上次采样时间、窗口内计数、风暴标记。

可变 频率追踪 为 做 数组 用 频率追踪器、10。

频率采样窗口_ns 为 100000000。

软中断_初始化 接受 零参：
    可变 i 为 0。
    只要 i 小于 软中断_数量：
        软中断动作表 的 第 i 项 的 处理函数 为 空。
        软中断动作表 的 第 i 项 的 名称 为 软中断名称表 的 第 i 项。
        软中断动作表 的 第 i 项 的 优先级 为 i。
        统计 的 触发次数 的 第 i 项 为 0。
        统计 的 执行次数 的 第 i 项 为 0。
        统计 的 累计耗时_ns 的 第 i 项 为 0。
        统计 的 最大耗时_ns 的 第 i 项 为 0。
        频率追踪 的 第 i 项 的 ewma_频率_q8 为 0。
        频率追踪 的 第 i 项 的 上次采样时间 为 0。
        频率追踪 的 第 i 项 的 窗口内计数 为 0。
        频率追踪 的 第 i 项 的 风暴标记 为 假。
        i 为 i 加 1。
    完。
    软中断状态 的 挂起 为 0。
    软中断状态 的 正在处理 为 假。
    软中断状态 的 嵌套计数 为 0。
    软中断状态 的 禁用计数 为 0。
    统计 的 总执行次数 为 0。
    统计 的 ksoftirqd次数 为 0。
    统计 的 风暴检测次数 为 0。
    统计 的 被延迟次数 为 0。
    i 为 0。
    只要 i 小于 最大TASKLET数：
        普通tasklet队列 的 第 i 项 的 有效 为 假。
        高优tasklet队列 的 第 i 项 的 有效 为 假。
        i 为 i 加 1。
    完。
    做 软中断_注册 用 软中断_TASKLET、tasklet_软中断处理。
    做 软中断_注册 用 软中断_高优先级、tasklet_高优先级_处理。
    做 早期打印OK 用 「软中断子系统初始化 (10类型, AI风暴预测)」。
完。

软中断_注册 接受 编号、处理函数：
    若 编号 大于或等于 软中断_数量：
        返还。
    完。
    若 软中断动作表 的 第 编号 项 的 处理函数 不等于 空：
        做 内核警告 用 「软中断 %d (%s) 已注册, 覆盖」、编号、软中断名称表 的 第 编号 项。
    完。
    软中断动作表 的 第 编号 项 的 处理函数 为 处理函数。
完。

软中断_注销 接受 编号：
    若 编号 大于或等于 软中断_数量：
        返还。
    完。
    软中断动作表 的 第 编号 项 的 处理函数 为 空。
完。

软中断_触发 接受 编号：
    若 编号 大于或等于 软中断_数量：
        返还。
    完。
    软中断状态 的 挂起 为 软中断状态 的 挂起 位或 1 左移 编号。
    统计 的 触发次数 的 第 编号 项 为 统计 的 触发次数 的 第 编号 项 加 1。
    做 频率追踪_更新 用 编号。
    若 软中断状态 的 处于中断上下文 等于 假 并且 软中断状态 的 禁用计数 等于 0：
        若 频率追踪 的 第 编号 项 的 风暴标记：
            统计 的 被延迟次数 为 统计 的 被延迟次数 加 1。
            做 ksoftirqd_唤醒。
        完。
    完。
完。

频率追踪_更新 接受 编号：
    现在 为 做 读取纳秒时钟。
    频率追踪 的 第 编号 项 的 窗口内计数 为 频率追踪 的 第 编号 项 的 窗口内计数 加 1。
    经过 为 现在 减 频率追踪 的 第 编号 项 的 上次采样时间。
    若 经过 大于或等于 频率采样窗口_ns：
        频率 为 频率追踪 的 第 编号 项 的 窗口内计数 乘 1000000000 除以 经过。
        旧值 为 频率追踪 的 第 编号 项 的 ewma_频率_q8。
        频率追踪 的 第 编号 项 的 ewma_频率_q8 为 EWMA_衰减 乘 旧值 加 EWMA_补 乘 (频率 左移 Q_SHIFT) 右移 Q_SHIFT。
        频率追踪 的 第 编号 项 的 窗口内计数 为 0。
        频率追踪 的 第 编号 项 的 上次采样时间 为 现在。
        实际频率 为 频率追踪 的 第 编号 项 的 ewma_频率_q8 右移 Q_SHIFT。
        若 实际频率 大于 软中断_风暴阈值：
            若 频率追踪 的 第 编号 项 的 风暴标记 等于 假：
                频率追踪 的 第 编号 项 的 风暴标记 为 真。
                统计 的 风暴检测次数 为 统计 的 风暴检测次数 加 1。
                做 内核警告 用 「[AI] 软中断风暴: %s 频率=%d/s」、软中断名称表 的 第 编号 项、实际频率。
            完。
        否则：
            频率追踪 的 第 编号 项 的 风暴标记 为 假。
        完。
    完。
完。

软中断_执行 接受 零参：
    若 软中断状态 的 禁用计数 大于 0：
        返还。
    完。
    若 软中断状态 的 正在处理：
        返还。
    完。
    挂起位 为 软中断状态 的 挂起。
    若 挂起位 等于 0：
        返还。
    完。
    软中断状态 的 正在处理 为 真。
    开始时间 为 做 读取纳秒时钟。
    可变 重启次数 为 0。
    可变 当前挂起 为 挂起位。
    只要 当前挂起 不等于 0 并且 重启次数 小于 软中断_最大重启次数：
        软中断状态 的 挂起 为 软中断状态 的 挂起 位与 按位取反 当前挂起。
        可变 位 为 0。
        只要 位 小于 软中断_数量：
            若 当前挂起 位与 1 左移 位 不等于 0：
                动作 为 软中断动作表 的 第 位 项。
                若 动作 的 处理函数 不等于 空：
                    处理开始 为 做 读取纳秒时钟。
                    做 动作 的 处理函数。
                    处理耗时 为 做 读取纳秒时钟 减 处理开始。
                    统计 的 执行次数 的 第 位 项 为 统计 的 执行次数 的 第 位 项 加 1。
                    统计 的 累计耗时_ns 的 第 位 项 为 统计 的 累计耗时_ns 的 第 位 项 加 处理耗时。
                    若 处理耗时 大于 统计 的 最大耗时_ns 的 第 位 项：
                        统计 的 最大耗时_ns 的 第 位 项 为 处理耗时。
                    完。
                完。
            完。
            位 为 位 加 1。
        完。
        统计 的 总执行次数 为 统计 的 总执行次数 加 1。
        已用时间 为 做 读取纳秒时钟 减 开始时间。
        若 已用时间 大于或等于 软中断_时间片_ns：
            做 ksoftirqd_唤醒。
            跳出。
        完。
        当前挂起 为 软中断状态 的 挂起。
        重启次数 为 重启次数 加 1。
    完。
    若 软中断状态 的 挂起 不等于 0：
        做 ksoftirqd_唤醒。
    完。
    软中断状态 的 正在处理 为 假。
完。

IRQ_退出_软中断检查 接受 零参：
    软中断状态 的 嵌套计数 为 软中断状态 的 嵌套计数 减 1。
    若 软中断状态 的 嵌套计数 等于 0：
        软中断状态 的 处于中断上下文 为 假。
        若 软中断状态 的 挂起 不等于 0 并且 软中断状态 的 禁用计数 等于 0：
            做 软中断_执行。
        完。
    完。
完。

BH_禁用 接受 零参：
    软中断状态 的 禁用计数 为 软中断状态 的 禁用计数 加 1。
完。

BH_使能 接受 零参：
    若 软中断状态 的 禁用计数 等于 0：
        做 内核警告 用 「BH_使能: 不平衡的调用」。
        返还。
    完。
    软中断状态 的 禁用计数 为 软中断状态 的 禁用计数 减 1。
    若 软中断状态 的 禁用计数 等于 0 并且 软中断状态 的 挂起 不等于 0：
        做 软中断_执行。
    完。
完。

ksoftirqd_唤醒 接受 零参：
    若 软中断状态 的 ksoftirqd_唤醒 等于 假：
        软中断状态 的 ksoftirqd_唤醒 为 真。
        做 内核线程_唤醒 用 「ksoftirqd」。
    完。
完。

ksoftirqd_线程主函数 接受 零参：
    只要 真：
        若 软中断状态 的 ksoftirqd_唤醒 等于 假：
            做 内核线程_休眠 用 「ksoftirqd」。
            继续。
        完。
        软中断状态 的 ksoftirqd_唤醒 为 假。
        统计 的 ksoftirqd次数 为 统计 的 ksoftirqd次数 加 1。
        只要 软中断状态 的 挂起 不等于 0：
            做 软中断_执行。
            若 软中断状态 的 挂起 不等于 0：
                做 调度_让出。
            完。
        完。
    完。
完。

tasklet_初始化 接受 回调、数据、名称：
    若 普通tasklet数量 大于或等于 最大TASKLET数：
        做 内核警告 用 「tasklet队列已满」。
        返还 负 1。
    完。
    可变 i 为 0。
    只要 i 小于 最大TASKLET数：
        若 普通tasklet队列 的 第 i 项 的 有效 等于 假：
            普通tasklet队列 的 第 i 项 的 回调函数 为 回调。
            普通tasklet队列 的 第 i 项 的 数据 为 数据。
            普通tasklet队列 的 第 i 项 的 状态 为 TASKLET_状态_就绪。
            普通tasklet队列 的 第 i 项 的 禁用计数 为 0。
            普通tasklet队列 的 第 i 项 的 有效 为 真。
            普通tasklet队列 的 第 i 项 的 名称 为 名称。
            普通tasklet数量 为 普通tasklet数量 加 1。
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

tasklet_调度 接受 id：
    若 id 小于 0 或者 id 大于或等于 最大TASKLET数：
        返还。
    完。
    若 普通tasklet队列 的 第 id 项 的 有效 等于 假：
        返还。
    完。
    若 普通tasklet队列 的 第 id 项 的 状态 位与 TASKLET_状态_调度中 不等于 0：
        返还。
    完。
    普通tasklet队列 的 第 id 项 的 状态 为 普通tasklet队列 的 第 id 项 的 状态 位或 TASKLET_状态_调度中。
    做 软中断_触发 用 软中断_TASKLET。
完。

tasklet_高优先级_调度 接受 id：
    若 id 小于 0 或者 id 大于或等于 最大TASKLET数：
        返还。
    完。
    若 高优tasklet队列 的 第 id 项 的 有效 等于 假：
        返还。
    完。
    若 高优tasklet队列 的 第 id 项 的 状态 位与 TASKLET_状态_调度中 不等于 0：
        返还。
    完。
    高优tasklet队列 的 第 id 项 的 状态 为 高优tasklet队列 的 第 id 项 的 状态 位或 TASKLET_状态_调度中。
    做 软中断_触发 用 软中断_高优先级。
完。

tasklet_禁用 接受 id：
    若 id 小于 0 或者 id 大于或等于 最大TASKLET数：
        返还。
    完。
    普通tasklet队列 的 第 id 项 的 禁用计数 为 普通tasklet队列 的 第 id 项 的 禁用计数 加 1。
    普通tasklet队列 的 第 id 项 的 状态 为 普通tasklet队列 的 第 id 项 的 状态 位或 TASKLET_状态_禁用。
完。

tasklet_使能 接受 id：
    若 id 小于 0 或者 id 大于或等于 最大TASKLET数：
        返还。
    完。
    若 普通tasklet队列 的 第 id 项 的 禁用计数 大于 0：
        普通tasklet队列 的 第 id 项 的 禁用计数 为 普通tasklet队列 的 第 id 项 的 禁用计数 减 1。
    完。
    若 普通tasklet队列 的 第 id 项 的 禁用计数 等于 0：
        普通tasklet队列 的 第 id 项 的 状态 为 普通tasklet队列 的 第 id 项 的 状态 位与 按位取反 TASKLET_状态_禁用。
    完。
完。

tasklet_销毁 接受 id：
    若 id 小于 0 或者 id 大于或等于 最大TASKLET数：
        返还。
    完。
    若 普通tasklet队列 的 第 id 项 的 状态 位与 TASKLET_状态_运行中 不等于 0：
        做 内核警告 用 「tasklet %d 正在运行中, 无法销毁」、id。
        返还。
    完。
    普通tasklet队列 的 第 id 项 的 有效 为 假。
    普通tasklet数量 为 普通tasklet数量 减 1。
完。

tasklet_软中断处理 接受 零参：
    可变 i 为 0。
    只要 i 小于 最大TASKLET数：
        若 普通tasklet队列 的 第 i 项 的 有效 并且 普通tasklet队列 的 第 i 项 的 状态 位与 TASKLET_状态_调度中 不等于 0：
            若 普通tasklet队列 的 第 i 项 的 状态 位与 TASKLET_状态_禁用 不等于 0：
                i 为 i 加 1。
                继续。
            完。
            普通tasklet队列 的 第 i 项 的 状态 为 TASKLET_状态_运行中。
            若 普通tasklet队列 的 第 i 项 的 回调函数 不等于 空：
                做 普通tasklet队列 的 第 i 项 的 回调函数 用 普通tasklet队列 的 第 i 项 的 数据。
            完。
            普通tasklet队列 的 第 i 项 的 状态 为 TASKLET_状态_就绪。
        完。
        i 为 i 加 1。
    完。
完。

tasklet_高优先级_处理 接受 零参：
    可变 i 为 0。
    只要 i 小于 最大TASKLET数：
        若 高优tasklet队列 的 第 i 项 的 有效 并且 高优tasklet队列 的 第 i 项 的 状态 位与 TASKLET_状态_调度中 不等于 0：
            若 高优tasklet队列 的 第 i 项 的 状态 位与 TASKLET_状态_禁用 不等于 0：
                i 为 i 加 1。
                继续。
            完。
            高优tasklet队列 的 第 i 项 的 状态 为 TASKLET_状态_运行中。
            若 高优tasklet队列 的 第 i 项 的 回调函数 不等于 空：
                做 高优tasklet队列 的 第 i 项 的 回调函数 用 高优tasklet队列 的 第 i 项 的 数据。
            完。
            高优tasklet队列 的 第 i 项 的 状态 为 TASKLET_状态_就绪。
        完。
        i 为 i 加 1。
    完。
完。

软中断_挂起检查 接受 零参：
    返还 软中断状态 的 挂起 不等于 0。
完。

软中断_获取挂起 接受 零参：
    返还 软中断状态 的 挂起。
完。

软中断_获取统计 接受 编号：
    若 编号 大于或等于 软中断_数量：
        返还 空。
    完。
    返还 一本字典：名称 对应 软中断名称表 的 第 编号 项、触发 对应 统计 的 触发次数 的 第 编号 项、执行 对应 统计 的 执行次数 的 第 编号 项、累计耗时 对应 统计 的 累计耗时_ns 的 第 编号 项、最大耗时 对应 统计 的 最大耗时_ns 的 第 编号 项、风暴 对应 频率追踪 的 第 编号 项 的 风暴标记、ewma频率 对应 频率追踪 的 第 编号 项 的 ewma_频率_q8 右移 Q_SHIFT。
完。

软中断_打印统计 接受 零参：
    做 内核打印 用 「\n=== 软中断统计 ===\n」。
    做 内核打印 用 「总执行次数: %llu\n」、统计 的 总执行次数。
    做 内核打印 用 「ksoftirqd运行: %llu\n」、统计 的 ksoftirqd次数。
    做 内核打印 用 「风暴检测: %llu  延迟: %llu\n」、统计 的 风暴检测次数、统计 的 被延迟次数。
    做 内核打印 用 「\n%-10s %10s %10s %12s %12s %6s\n」、「类型」、「触发」、「执行」、「累计(us)」、「最大(us)」、「风暴」。
    可变 i 为 0。
    只要 i 小于 软中断_数量：
        风暴标 为 「否」。
        若 频率追踪 的 第 i 项 的 风暴标记：
            风暴标 为 「是」。
        完。
        做 内核打印 用 「%-10s %10llu %10llu %12llu %12llu %6s\n」、软中断名称表 的 第 i 项、统计 的 触发次数 的 第 i 项、统计 的 执行次数 的 第 i 项、统计 的 累计耗时_ns 的 第 i 项 除以 1000、统计 的 最大耗时_ns 的 第 i 项 除以 1000、风暴标。
        i 为 i 加 1。
    完。
完。

软中断_AI健康评估 接受 零参：
    总耗时 为 0。
    可变 最大耗时 为 0。
    可变 风暴数 为 0。
    可变 总执行 为 0。
    可变 i 为 0。
    只要 i 小于 软中断_数量：
        总耗时 为 总耗时 加 统计 的 累计耗时_ns 的 第 i 项。
        若 统计 的 最大耗时_ns 的 第 i 项 大于 最大耗时：
            最大耗时 为 统计 的 最大耗时_ns 的 第 i 项。
        完。
        若 频率追踪 的 第 i 项 的 风暴标记：
            风暴数 为 风暴数 加 1。
        完。
        总执行 为 总执行 加 统计 的 执行次数 的 第 i 项。
        i 为 i 加 1。
    完。
    利用率分 为 100。
    若 总耗时 大于 100000000：
        利用率分 为 100000000 乘 100 除以 总耗时。
    完。
    若 利用率分 大于 100：
        利用率分 为 100。
    完。
    延迟分 为 100。
    若 最大耗时 大于 1000000：
        延迟分 为 100 减 (最大耗时 减 1000000) 乘 100 除以 9000000。
        若 延迟分 小于 0：
            延迟分 为 0。
        完。
    完。
    风暴分 为 100 减 风暴数 乘 25。
    若 风暴分 小于 0：
        风暴分 为 0。
    完。
    均衡分 为 100。
    若 总执行 大于 0 并且 软中断_数量 大于 0：
        均值 为 总执行 除以 软中断_数量。
        若 均值 大于 0：
            可变 方差和 为 0。
            i 为 0。
            只要 i 小于 软中断_数量：
                差 为 统计 的 执行次数 的 第 i 项 减 均值。
                方差和 为 方差和 加 差 乘 差。
                i 为 i 加 1。
            完。
            变异 为 方差和 除以 (软中断_数量 乘 均值 乘 均值)。
            若 变异 大于 100：
                均衡分 为 0。
            否则：
                均衡分 为 100 减 变异。
            完。
        完。
    完。
    综合分 为 (利用率分 乘 30 加 延迟分 乘 25 加 风暴分 乘 25 加 均衡分 乘 20) 除以 100。
    做 内核打印 用 「\n=== 软中断AI健康评估 ===\n」。
    做 内核打印 用 「利用率分: %d/100 (权重30%%)\n」、利用率分。
    做 内核打印 用 「延迟分: %d/100 (权重25%%)\n」、延迟分。
    做 内核打印 用 「风暴分: %d/100 (权重25%%, 活跃风暴:%d)\n」、风暴分、风暴数。
    做 内核打印 用 「均衡分: %d/100 (权重20%%)\n」、均衡分。
    做 内核打印 用 「综合健康: %d/100\n」、综合分。
    若 综合分 大于或等于 80：
        做 内核打印 用 「状态: 优秀\n」。
    完。
    做 否则如果 用 综合分 大于或等于 60。
    块：
        做 内核打印 用 「状态: 良好\n」。
    完。
    做 否则如果 用 综合分 大于或等于 40。
    块：
        做 内核打印 用 「状态: 警告 - 建议检查软中断负载\n」。
    完。
    否则。
    块：
        做 内核打印 用 「状态: 危险 - 软中断子系统需要干预\n」。
    完。
    特征 为 做 Tensor_创建 用 6。
    做 Tensor_写 用 特征、0、利用率分。
    做 Tensor_写 用 特征、1、延迟分。
    做 Tensor_写 用 特征、2、风暴分。
    做 Tensor_写 用 特征、3、均衡分。
    做 Tensor_写 用 特征、4、综合分。
    做 Tensor_写 用 特征、5、风暴数。
    做 全局AI总线 的 发送 用 「内核.软中断.健康」、特征。
    做 Tensor_释放 用 特征。
    返还 综合分。
完。

proc_softirqs_读取 接受 缓冲区、长度：
    可变 偏移 为 0。
    偏移 为 偏移 加 做 缓冲区写入 用 缓冲区、偏移、「                    CPU0\n」。
    可变 i 为 0。
    只要 i 小于 软中断_数量：
        偏移 为 偏移 加 做 缓冲区写入格式 用 缓冲区、偏移、「%10s: %10llu\n」、软中断名称表 的 第 i 项、统计 的 执行次数 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 偏移。
完。

本章 收束。
