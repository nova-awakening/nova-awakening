《尾部零》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·字符串替换》。

位宽64 为 64。

位宽32 为 32。

位宽16 为 16。

位宽8 为 8。

全一64 为 0xFFFFFFFFFFFFFFFF。

全一32 为 0xFFFFFFFF。

全一16 为 0xFFFF。

全一8 为 0xFF。

位图无效位 为 0 减 1。

AI热区窗口 为 8。

AI热区阈值 为 4。

位图最大字数 为 16384。

位操作统计 是 一种 事物，含 尾部零调用数、前导零调用数、置位计数调用数、位图创建数、位图设置数、位图清除数、位图测试数、位图查找数、位图查找命中数、位图查找未命中数、范围操作数、逻辑运算数、迭代次数、AI预测次数、AI预测命中数、总位翻转数、连续查找最大长度、错误次数。

可变 全局统计 为 新建 位操作统计。

可变 已初始化 为 假。

尾部零 接受 值：
    全局统计 的 尾部零调用数 为 全局统计 的 尾部零调用数 加 1。
    若 值 等于 0：
        返还 位宽64。
    完。
    可变 n 为 0。
    可变 v 为 值。
    若 v 位与 全一32 等于 0：
        n 为 n 加 32。
        v 为 v 右移 32。
    完。
    若 v 位与 全一16 等于 0：
        n 为 n 加 16。
        v 为 v 右移 16。
    完。
    若 v 位与 全一8 等于 0：
        n 为 n 加 8。
        v 为 v 右移 8。
    完。
    若 v 位与 0xF 等于 0：
        n 为 n 加 4。
        v 为 v 右移 4。
    完。
    若 v 位与 0x3 等于 0：
        n 为 n 加 2。
        v 为 v 右移 2。
    完。
    若 v 位与 0x1 等于 0：
        n 为 n 加 1。
    完。
    返还 n。
完。

尾部零32 接受 值：
    全局统计 的 尾部零调用数 为 全局统计 的 尾部零调用数 加 1。
    若 值 等于 0：
        返还 位宽32。
    完。
    可变 n 为 0。
    可变 v 为 值 位与 全一32。
    若 v 位与 全一16 等于 0：
        n 为 n 加 16。
        v 为 v 右移 16。
    完。
    若 v 位与 全一8 等于 0：
        n 为 n 加 8。
        v 为 v 右移 8。
    完。
    若 v 位与 0xF 等于 0：
        n 为 n 加 4。
        v 为 v 右移 4。
    完。
    若 v 位与 0x3 等于 0：
        n 为 n 加 2。
        v 为 v 右移 2。
    完。
    若 v 位与 0x1 等于 0：
        n 为 n 加 1。
    完。
    返还 n。
完。

前导零 接受 值：
    全局统计 的 前导零调用数 为 全局统计 的 前导零调用数 加 1。
    若 值 等于 0：
        返还 位宽64。
    完。
    可变 n 为 0。
    可变 v 为 值。
    若 v 右移 32 等于 0：
        n 为 n 加 32。
        v 为 v 左移 32。
    完。
    若 v 右移 48 等于 0：
        n 为 n 加 16。
        v 为 v 左移 16。
    完。
    若 v 右移 56 等于 0：
        n 为 n 加 8。
        v 为 v 左移 8。
    完。
    若 v 右移 60 等于 0：
        n 为 n 加 4。
        v 为 v 左移 4。
    完。
    若 v 右移 62 等于 0：
        n 为 n 加 2。
        v 为 v 左移 2。
    完。
    若 v 右移 63 等于 0：
        n 为 n 加 1。
    完。
    返还 n。
完。

前导零32 接受 值：
    全局统计 的 前导零调用数 为 全局统计 的 前导零调用数 加 1。
    若 值 等于 0：
        返还 位宽32。
    完。
    可变 n 为 0。
    可变 v 为 值 位与 全一32。
    若 v 右移 16 等于 0：
        n 为 n 加 16。
        v 为 v 左移 16。
    完。
    若 v 右移 24 等于 0：
        n 为 n 加 8。
        v 为 v 左移 8。
    完。
    若 v 右移 28 等于 0：
        n 为 n 加 4。
        v 为 v 左移 4。
    完。
    若 v 右移 30 等于 0：
        n 为 n 加 2。
        v 为 v 左移 2。
    完。
    若 v 右移 31 等于 0：
        n 为 n 加 1。
    完。
    返还 n。
完。

计数置位 接受 值：
    全局统计 的 置位计数调用数 为 全局统计 的 置位计数调用数 加 1。
    可变 v 为 值。
    v 为 v 减 (v 右移 1 位与 0x5555555555555555)。
    v 为 (v 位与 0x3333333333333333) 加 (v 右移 2 位与 0x3333333333333333)。
    v 为 v 加 (v 右移 4) 位与 0x0F0F0F0F0F0F0F0F。
    v 为 v 加 (v 右移 8)。
    v 为 v 加 (v 右移 16)。
    v 为 v 加 (v 右移 32)。
    返还 v 位与 0x7F。
完。

计数置位32 接受 值：
    全局统计 的 置位计数调用数 为 全局统计 的 置位计数调用数 加 1。
    可变 v 为 值 位与 全一32。
    v 为 v 减 (v 右移 1 位与 0x55555555)。
    v 为 (v 位与 0x33333333) 加 (v 右移 2 位与 0x33333333)。
    v 为 v 加 (v 右移 4) 位与 0x0F0F0F0F。
    v 为 v 加 (v 右移 8)。
    v 为 v 加 (v 右移 16)。
    返还 v 位与 0x3F。
完。

左旋转 接受 值、位数：
    n 为 位数 取余于 位宽64。
    若 n 等于 0：
        返还 值。
    完。
    返还 值 左移 n 位或 值 右移 位宽64 减 n。
完。

右旋转 接受 值、位数：
    n 为 位数 取余于 位宽64。
    若 n 等于 0：
        返还 值。
    完。
    返还 值 右移 n 位或 值 左移 位宽64 减 n。
完。

位域提取 接受 值、起始位、宽度：
    若 宽度 小于或等于 0 或者 宽度 大于 位宽64：
        返还 0。
    完。
    若 起始位 小于 0 或者 起始位 大于或等于 位宽64：
        返还 0。
    完。
    掩码 为 (1 左移 宽度) 减 1。
    返还 值 右移 起始位 位与 掩码。
完。

位域设置 接受 值、起始位、宽度、新值：
    若 宽度 小于或等于 0 或者 宽度 大于 位宽64：
        返还 值。
    完。
    若 起始位 小于 0 或者 起始位 大于或等于 位宽64：
        返还 值。
    完。
    掩码 为 (1 左移 宽度) 减 1 左移 起始位。
    清除后 为 值 位与 按位取反 掩码。
    新域值 为 新值 左移 起始位 位与 掩码。
    返还 清除后 位或 新域值。
完。

查找最低置位 接受 值：
    返还 做 尾部零 用 值。
完。

查找最高置位 接受 值：
    若 值 等于 0：
        返还 位图无效位。
    完。
    返还 位宽64 减 1 减 做 前导零 用 值。
完。

清除最低置位 接受 值：
    返还 值 位与 值 减 1。
完。

隔离最低置位 接受 值：
    返还 值 位与 0 减 值。
完。

是否为2的幂 接受 值：
    若 值 小于或等于 0：
        返还 假。
    完。
    返还 值 位与 值 减 1 等于 0。
完。

向上取整到2的幂 接受 值：
    若 值 小于或等于 1：
        返还 1。
    完。
    可变 v 为 值 减 1。
    v 为 v 位或 v 右移 1。
    v 为 v 位或 v 右移 2。
    v 为 v 位或 v 右移 4。
    v 为 v 位或 v 右移 8。
    v 为 v 位或 v 右移 16。
    v 为 v 位或 v 右移 32。
    返还 v 加 1。
完。

log2整数 接受 值：
    若 值 小于或等于 0：
        返还 0。
    完。
    返还 位宽64 减 1 减 做 前导零 用 值。
完。

log2向上 接受 值：
    若 值 小于或等于 1：
        返还 0。
    完。
    基础 为 做 log2整数 用 值。
    若 1 左移 基础 小于 值：
        返还 基础 加 1。
    完。
    返还 基础。
完。

位反转64 接受 值：
    可变 v 为 值。
    v 为 v 右移 1 位与 0x5555555555555555 位或 (v 位与 0x5555555555555555) 左移 1。
    v 为 v 右移 2 位与 0x3333333333333333 位或 (v 位与 0x3333333333333333) 左移 2。
    v 为 v 右移 4 位与 0x0F0F0F0F0F0F0F0F 位或 (v 位与 0x0F0F0F0F0F0F0F0F) 左移 4。
    v 为 v 右移 8 位与 0x00FF00FF00FF00FF 位或 (v 位与 0x00FF00FF00FF00FF) 左移 8。
    v 为 v 右移 16 位与 0x0000FFFF0000FFFF 位或 (v 位与 0x0000FFFF0000FFFF) 左移 16。
    v 为 v 右移 32 位或 v 左移 32。
    返还 v。
完。

字节交换64 接受 值：
    可变 v 为 值。
    v 为 v 右移 8 位与 0x00FF00FF00FF00FF 位或 (v 位与 0x00FF00FF00FF00FF) 左移 8。
    v 为 v 右移 16 位与 0x0000FFFF0000FFFF 位或 (v 位与 0x0000FFFF0000FFFF) 左移 16。
    v 为 v 右移 32 位或 v 左移 32。
    返还 v。
完。

字节交换32 接受 值：
    可变 v 为 值 位与 全一32。
    v 为 v 右移 8 位与 0x00FF00FF 位或 (v 位与 0x00FF00FF) 左移 8。
    v 为 v 右移 16 位或 v 左移 16。
    返还 v 位与 全一32。
完。

位图_创建 接受 位数：
    若 位数 小于或等于 0：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 空。
    完。
    字数 为 (位数 加 63) 除以 64。
    若 字数 大于 位图最大字数：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 空。
    完。
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 字数：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    全局统计 的 位图创建数 为 全局统计 的 位图创建数 加 1。
    返还 一组：数据、位数。
完。

位图_克隆 接受 位图：
    若 位图 等于 空：
        返还 空。
    完。
    可变 新位图 为 做 位图_创建 用 位图 的 第 1 项。
    若 新位图 等于 空：
        返还 空。
    完。
    字数 为 做 计数 用 位图 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 字数：
        新位图 的 第 0 项 的 第 i 项 为 位图 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 新位图。
完。

位图_设置 接受 位图、位：
    若 位图 等于 空：
        返还 错误_无效参数。
    完。
    若 位 小于 0 或者 位 大于或等于 位图 的 第 1 项：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_越界。
    完。
    字索引 为 位 除以 64。
    位偏移 为 位 取余于 64。
    位图 的 第 0 项 的 第 字索引 项 为 位图 的 第 0 项 的 第 字索引 项 位或 1 左移 位偏移。
    全局统计 的 位图设置数 为 全局统计 的 位图设置数 加 1。
    全局统计 的 总位翻转数 为 全局统计 的 总位翻转数 加 1。
    返还 0。
完。

位图_清除 接受 位图、位：
    若 位图 等于 空：
        返还 错误_无效参数。
    完。
    若 位 小于 0 或者 位 大于或等于 位图 的 第 1 项：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_越界。
    完。
    字索引 为 位 除以 64。
    位偏移 为 位 取余于 64。
    位图 的 第 0 项 的 第 字索引 项 为 位图 的 第 0 项 的 第 字索引 项 位与 按位取反 (1 左移 位偏移)。
    全局统计 的 位图清除数 为 全局统计 的 位图清除数 加 1。
    全局统计 的 总位翻转数 为 全局统计 的 总位翻转数 加 1。
    返还 0。
完。

位图_测试 接受 位图、位：
    若 位图 等于 空：
        返还 假。
    完。
    若 位 小于 0 或者 位 大于或等于 位图 的 第 1 项：
        返还 假。
    完。
    字索引 为 位 除以 64。
    位偏移 为 位 取余于 64。
    全局统计 的 位图测试数 为 全局统计 的 位图测试数 加 1。
    返还 位图 的 第 0 项 的 第 字索引 项 右移 位偏移 位与 1。
完。

位图_切换 接受 位图、位：
    若 位图 等于 空：
        返还 错误_无效参数。
    完。
    若 位 小于 0 或者 位 大于或等于 位图 的 第 1 项：
        返还 错误_越界。
    完。
    字索引 为 位 除以 64。
    位偏移 为 位 取余于 64。
    位图 的 第 0 项 的 第 字索引 项 为 位图 的 第 0 项 的 第 字索引 项 位异或 1 左移 位偏移。
    全局统计 的 总位翻转数 为 全局统计 的 总位翻转数 加 1。
    返还 0。
完。

位图_获取总位数 接受 位图：
    若 位图 等于 空：
        返还 0。
    完。
    返还 位图 的 第 1 项。
完。

位图_设置范围 接受 位图、起始、长度：
    若 位图 等于 空：
        返还 错误_无效参数。
    完。
    若 起始 小于 0 或者 长度 小于或等于 0：
        返还 错误_无效参数。
    完。
    若 起始 加 长度 大于 位图 的 第 1 项：
        返还 错误_越界。
    完。
    可变 i 为 0。
    只要 i 小于 长度：
        做 位图_设置 用 位图、起始 加 i。
        i 为 i 加 1。
    完。
    全局统计 的 范围操作数 为 全局统计 的 范围操作数 加 1。
    返还 0。
完。

位图_清除范围 接受 位图、起始、长度：
    若 位图 等于 空：
        返还 错误_无效参数。
    完。
    若 起始 小于 0 或者 长度 小于或等于 0：
        返还 错误_无效参数。
    完。
    若 起始 加 长度 大于 位图 的 第 1 项：
        返还 错误_越界。
    完。
    可变 i 为 0。
    只要 i 小于 长度：
        做 位图_清除 用 位图、起始 加 i。
        i 为 i 加 1。
    完。
    全局统计 的 范围操作数 为 全局统计 的 范围操作数 加 1。
    返还 0。
完。

位图_切换范围 接受 位图、起始、长度：
    若 位图 等于 空：
        返还 错误_无效参数。
    完。
    若 起始 小于 0 或者 长度 小于或等于 0：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 长度：
        做 位图_切换 用 位图、起始 加 i。
        i 为 i 加 1。
    完。
    全局统计 的 范围操作数 为 全局统计 的 范围操作数 加 1。
    返还 0。
完。

位图_查找首个置位 接受 位图：
    若 位图 等于 空：
        返还 位图无效位。
    完。
    字数 为 做 计数 用 位图 的 第 0 项。
    可变 i 为 0。
    全局统计 的 位图查找数 为 全局统计 的 位图查找数 加 1。
    只要 i 小于 字数：
        若 位图 的 第 0 项 的 第 i 项 不等于 0：
            结果 为 i 乘 64 加 做 查找最低置位 用 位图 的 第 0 项 的 第 i 项。
            若 结果 小于 位图 的 第 1 项：
                全局统计 的 位图查找命中数 为 全局统计 的 位图查找命中数 加 1。
                返还 结果。
            完。
        完。
        i 为 i 加 1。
    完。
    全局统计 的 位图查找未命中数 为 全局统计 的 位图查找未命中数 加 1。
    返还 位图无效位。
完。

位图_查找首个清零 接受 位图：
    若 位图 等于 空：
        返还 位图无效位。
    完。
    字数 为 做 计数 用 位图 的 第 0 项。
    可变 i 为 0。
    全局统计 的 位图查找数 为 全局统计 的 位图查找数 加 1。
    只要 i 小于 字数：
        若 位图 的 第 0 项 的 第 i 项 不等于 全一64：
            翻转 为 按位取反 位图 的 第 0 项 的 第 i 项。
            位 为 i 乘 64 加 做 查找最低置位 用 翻转。
            若 位 小于 位图 的 第 1 项：
                全局统计 的 位图查找命中数 为 全局统计 的 位图查找命中数 加 1。
                返还 位。
            完。
        完。
        i 为 i 加 1。
    完。
    全局统计 的 位图查找未命中数 为 全局统计 的 位图查找未命中数 加 1。
    返还 位图无效位。
完。

位图_查找下一个置位 接受 位图、起始：
    若 位图 等于 空：
        返还 位图无效位。
    完。
    可变 i 为 起始。
    只要 i 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、i 等于 真：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 位图无效位。
完。

位图_查找下一个清零 接受 位图、起始：
    若 位图 等于 空：
        返还 位图无效位。
    完。
    可变 i 为 起始。
    只要 i 小于 位图 的 第 1 项：
        若 做 位图_测试 用 位图、i 等于 假：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 位图无效位。
完。

位图_查找连续清零 接受 位图、n：
    若 位图 等于 空 或者 n 小于或等于 0：
        返还 位图无效位。
    完。
    总位 为 位图 的 第 1 项。
    可变 起始 为 0。
    可变 连续 为 0。
    可变 i 为 0。
    全局统计 的 位图查找数 为 全局统计 的 位图查找数 加 1。
    只要 i 小于 总位：
        若 做 位图_测试 用 位图、i 等于 假：
            若 连续 等于 0：
                起始 为 i。
            完。
            连续 为 连续 加 1。
            若 连续 大于或等于 n：
                全局统计 的 位图查找命中数 为 全局统计 的 位图查找命中数 加 1。
                若 连续 大于 全局统计 的 连续查找最大长度：
                    全局统计 的 连续查找最大长度 为 连续。
                完。
                返还 起始。
            完。
        否则：
            连续 为 0。
        完。
        i 为 i 加 1。
    完。
    全局统计 的 位图查找未命中数 为 全局统计 的 位图查找未命中数 加 1。
    返还 位图无效位。
完。

位图_查找连续置位 接受 位图、n：
    若 位图 等于 空 或者 n 小于或等于 0：
        返还 位图无效位。
    完。
    总位 为 位图 的 第 1 项。
    可变 起始 为 0。
    可变 连续 为 0。
    可变 i 为 0。
    只要 i 小于 总位：
        若 做 位图_测试 用 位图、i 等于 真：
            若 连续 等于 0：
                起始 为 i。
            完。
            连续 为 连续 加 1。
            若 连续 大于或等于 n：
                返还 起始。
            完。
        否则：
            连续 为 0。
        完。
        i 为 i 加 1。
    完。
    返还 位图无效位。
完。

位图_计数置位 接受 位图：
    若 位图 等于 空：
        返还 0。
    完。
    字数 为 做 计数 用 位图 的 第 0 项。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 字数：
        总 为 总 加 做 计数置位 用 位图 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

位图_计数清零 接受 位图：
    若 位图 等于 空：
        返还 0。
    完。
    返还 位图 的 第 1 项 减 做 位图_计数置位 用 位图。
完。

位图_使用率 接受 位图：
    若 位图 等于 空 或者 位图 的 第 1 项 等于 0：
        返还 0。
    完。
    返还 做 位图_计数置位 用 位图 乘 100 除以 位图 的 第 1 项。
完。

位图_为空 接受 位图：
    若 位图 等于 空：
        返还 真。
    完。
    字数 为 做 计数 用 位图 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 字数：
        若 位图 的 第 0 项 的 第 i 项 不等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

位图_已满 接受 位图：
    若 位图 等于 空：
        返还 假。
    完。
    返还 做 位图_计数置位 用 位图 大于或等于 位图 的 第 1 项。
完。

位图_全设 接受 位图：
    若 位图 等于 空：
        返还 错误_无效参数。
    完。
    字数 为 做 计数 用 位图 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 字数：
        位图 的 第 0 项 的 第 i 项 为 全一64。
        i 为 i 加 1。
    完。
    返还 0。
完。

位图_全清 接受 位图：
    若 位图 等于 空：
        返还 错误_无效参数。
    完。
    字数 为 做 计数 用 位图 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 字数：
        位图 的 第 0 项 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    返还 0。
完。

位图_与 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 空。
    完。
    字数 为 做 计数 用 a 的 第 0 项。
    可变 结果 为 做 位图_创建 用 a 的 第 1 项。
    若 结果 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 字数：
        结果 的 第 0 项 的 第 i 项 为 a 的 第 0 项 的 第 i 项 位与 b 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    全局统计 的 逻辑运算数 为 全局统计 的 逻辑运算数 加 1。
    返还 结果。
完。

位图_或 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 空。
    完。
    字数 为 做 计数 用 a 的 第 0 项。
    可变 结果 为 做 位图_创建 用 a 的 第 1 项。
    若 结果 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 字数：
        结果 的 第 0 项 的 第 i 项 为 a 的 第 0 项 的 第 i 项 位或 b 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    全局统计 的 逻辑运算数 为 全局统计 的 逻辑运算数 加 1。
    返还 结果。
完。

位图_异或 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 空。
    完。
    字数 为 做 计数 用 a 的 第 0 项。
    可变 结果 为 做 位图_创建 用 a 的 第 1 项。
    若 结果 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 字数：
        结果 的 第 0 项 的 第 i 项 为 a 的 第 0 项 的 第 i 项 位异或 b 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    全局统计 的 逻辑运算数 为 全局统计 的 逻辑运算数 加 1。
    返还 结果。
完。

位图_非 接受 位图：
    若 位图 等于 空：
        返还 空。
    完。
    字数 为 做 计数 用 位图 的 第 0 项。
    可变 结果 为 做 位图_创建 用 位图 的 第 1 项。
    若 结果 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 字数：
        结果 的 第 0 项 的 第 i 项 为 按位取反 位图 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    全局统计 的 逻辑运算数 为 全局统计 的 逻辑运算数 加 1。
    返还 结果。
完。

位图_差集 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 空。
    完。
    字数 为 做 计数 用 a 的 第 0 项。
    可变 结果 为 做 位图_创建 用 a 的 第 1 项。
    若 结果 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 字数：
        结果 的 第 0 项 的 第 i 项 为 a 的 第 0 项 的 第 i 项 位与 按位取反 b 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    全局统计 的 逻辑运算数 为 全局统计 的 逻辑运算数 加 1。
    返还 结果。
完。

位图_相等 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 假。
    完。
    若 a 的 第 1 项 不等于 b 的 第 1 项：
        返还 假。
    完。
    字数 为 做 计数 用 a 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 字数：
        若 a 的 第 0 项 的 第 i 项 不等于 b 的 第 0 项 的 第 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

位图_是子集 接受 子集、超集：
    若 子集 等于 空 或者 超集 等于 空：
        返还 假。
    完。
    字数 为 做 计数 用 子集 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 字数：
        若 子集 的 第 0 项 的 第 i 项 位与 按位取反 超集 的 第 0 项 的 第 i 项 不等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

位图_相交 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 假。
    完。
    字数 为 做 计数 用 a 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 字数：
        若 a 的 第 0 项 的 第 i 项 位与 b 的 第 0 项 的 第 i 项 不等于 0：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

位图_遍历置位 接受 位图、回调：
    若 位图 等于 空 或者 回调 等于 空：
        返还 错误_无效参数。
    完。
    可变 位 为 做 位图_查找首个置位 用 位图。
    全局统计 的 迭代次数 为 全局统计 的 迭代次数 加 1。
    只要 位 不等于 位图无效位 并且 位 小于 位图 的 第 1 项：
        做 回调 用 位。
        位 为 做 位图_查找下一个置位 用 位图、位 加 1。
    完。
    返还 0。
完。

位图_遍历清零 接受 位图、回调：
    若 位图 等于 空 或者 回调 等于 空：
        返还 错误_无效参数。
    完。
    可变 位 为 做 位图_查找首个清零 用 位图。
    全局统计 的 迭代次数 为 全局统计 的 迭代次数 加 1。
    只要 位 不等于 位图无效位 并且 位 小于 位图 的 第 1 项：
        做 回调 用 位。
        位 为 做 位图_查找下一个清零 用 位图、位 加 1。
    完。
    返还 0。
完。

位图_收集置位 接受 位图：
    若 位图 等于 空：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 位 为 做 位图_查找首个置位 用 位图。
    只要 位 不等于 位图无效位 并且 位 小于 位图 的 第 1 项：
        做 追加 用 结果、位。
        位 为 做 位图_查找下一个置位 用 位图、位 加 1。
    完。
    返还 结果。
完。

AI位图热区 是 一种 事物，含 区域起始、区域长度、访问次数、最后访问、热度分数。

可变 热区列表 为 一组：（空）。

可变 热区数量 为 0。

AI_记录位图访问 接受 位图、位：
    若 位图 等于 空 或者 位 小于 0：
        返还。
    完。
    区域 为 位 除以 64。
    可变 i 为 0。
    只要 i 小于 热区数量：
        若 热区列表 的 第 i 项 的 区域起始 等于 区域 乘 64：
            热区列表 的 第 i 项 的 访问次数 为 热区列表 的 第 i 项 的 访问次数 加 1。
            热区列表 的 第 i 项 的 热度分数 为 热区列表 的 第 i 项 的 热度分数 加 1。
            全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
            返还。
        完。
        i 为 i 加 1。
    完。
    若 热区数量 小于 AI热区窗口：
        可变 新热区 为 新建 AI位图热区。
        新热区 的 区域起始 为 区域 乘 64。
        新热区 的 区域长度 为 64。
        新热区 的 访问次数 为 1。
        新热区 的 热度分数 为 1。
        做 追加 用 热区列表、新热区。
        热区数量 为 热区数量 加 1。
    完。
完。

AI_获取最热区域 接受 零参：
    可变 最热索引 为 0。
    可变 最热分数 为 0。
    可变 i 为 0。
    只要 i 小于 热区数量：
        若 热区列表 的 第 i 项 的 热度分数 大于 最热分数：
            最热分数 为 热区列表 的 第 i 项 的 热度分数。
            最热索引 为 i。
        完。
        i 为 i 加 1。
    完。
    若 热区数量 等于 0：
        返还 位图无效位。
    完。
    全局统计 的 AI预测命中数 为 全局统计 的 AI预测命中数 加 1。
    返还 热区列表 的 第 最热索引 项 的 区域起始。
完。

AI_建议查找起始 接受 位图：
    热起始 为 做 AI_获取最热区域。
    若 热起始 等于 位图无效位：
        返还 0。
    完。
    返还 热起始。
完。

位图_转储 接受 位图：
    若 位图 等于 空：
        做 打印 用 「[位图] 空位图」。
        返还 0。
    完。
    做 打印 用 做 字符串拼接 用 「[位图] 总位数: 」、做 转字符串 用 位图 的 第 1 项。
    做 打印 用 做 字符串拼接 用 「[位图] 置位数: 」、做 转字符串 用 做 位图_计数置位 用 位图。
    做 打印 用 做 字符串拼接 用 「[位图] 清零数: 」、做 转字符串 用 做 位图_计数清零 用 位图。
    做 打印 用 做 字符串拼接 用 「[位图] 使用率: 」、做 转字符串 用 做 位图_使用率 用 位图。
    返还 0。
完。

位操作_初始化 接受 零参：
    若 已初始化 等于 真：
        返还 错误_已存在。
    完。
    热区列表 为 一组：（空）。
    热区数量 为 0。
    已初始化 为 真。
    做 打印 用 「[位操作] 尾部零/位图引擎初始化完成」。
    返还 0。
完。

位操作_销毁 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    热区列表 为 一组：（空）。
    热区数量 为 0。
    已初始化 为 假。
    做 打印 用 「[位操作] 已销毁」。
    返还 0。
完。

位操作_打印统计 接受 零参：
    做 打印 用 「[位操作] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  尾部零调用: 」、做 转字符串 用 全局统计 的 尾部零调用数。
    做 打印 用 做 字符串拼接 用 「  前导零调用: 」、做 转字符串 用 全局统计 的 前导零调用数。
    做 打印 用 做 字符串拼接 用 「  置位计数调用: 」、做 转字符串 用 全局统计 的 置位计数调用数。
    做 打印 用 做 字符串拼接 用 「  位图创建数: 」、做 转字符串 用 全局统计 的 位图创建数。
    做 打印 用 做 字符串拼接 用 「  位图设置数: 」、做 转字符串 用 全局统计 的 位图设置数。
    做 打印 用 做 字符串拼接 用 「  位图清除数: 」、做 转字符串 用 全局统计 的 位图清除数。
    做 打印 用 做 字符串拼接 用 「  位图测试数: 」、做 转字符串 用 全局统计 的 位图测试数。
    做 打印 用 做 字符串拼接 用 「  位图查找数: 」、做 转字符串 用 全局统计 的 位图查找数。
    做 打印 用 做 字符串拼接 用 「  查找命中: 」、做 转字符串 用 全局统计 的 位图查找命中数。
    做 打印 用 做 字符串拼接 用 「  查找未命中: 」、做 转字符串 用 全局统计 的 位图查找未命中数。
    做 打印 用 做 字符串拼接 用 「  范围操作: 」、做 转字符串 用 全局统计 的 范围操作数。
    做 打印 用 做 字符串拼接 用 「  逻辑运算: 」、做 转字符串 用 全局统计 的 逻辑运算数。
    做 打印 用 做 字符串拼接 用 「  迭代次数: 」、做 转字符串 用 全局统计 的 迭代次数。
    做 打印 用 做 字符串拼接 用 「  AI预测次数: 」、做 转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI预测命中: 」、做 转字符串 用 全局统计 的 AI预测命中数。
    做 打印 用 做 字符串拼接 用 「  总位翻转: 」、做 转字符串 用 全局统计 的 总位翻转数。
    做 打印 用 做 字符串拼接 用 「  最大连续长度: 」、做 转字符串 用 全局统计 的 连续查找最大长度。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    若 全局统计 的 位图查找数 大于 0：
        命中率 为 全局统计 的 位图查找命中数 乘 100 除以 全局统计 的 位图查找数。
        做 打印 用 做 字符串拼接 用 「  查找命中率%: 」、做 转字符串 用 命中率。
    完。
    返还 0。
完。

位操作_注册到内核 接受 内核上下文：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    做 打印 用 「[位操作] 已注册到内核上下文」。
    返还 0。
完。

位操作_查询状态 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

位操作_健康检查 接受 零参：
    若 已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 位图查找数 加 全局统计 的 位图设置数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
