《界面元素发现》

引用《.·日志中枢》。

引用《..·推理·定点Q算术》。

引用《.·字符串替换》。

引用《..·稳态·绘制引擎》。

引用《..·标准·原生字符串》。

引用《.·内核打印》。

外部。

元素_根节点。

最大发现元素 为 512。

UI元素 是 一种 事物，含 元素ID、角色、名称、值、中心x、中心y、左上x、左上y、宽、高、可点击、可输入、可见、已禁用、DOM路径、选择器。

可变 已发现元素 为 一组：（空）。

可变 UI_扫描次数 为 0。

可变 UI_操作次数 为 0。

可变 UI_成功次数 为 0。

可变 UI_失败次数 为 0。

UI_扫描全部元素 接受 零参：
    已发现元素 为 一组：（空）。
    若 元素_根节点 等于 空：
        做 内核调试 用 「UI自动化: DOM根节点为空, 无法扫描」。
        返还 一组：（空）。
    完。
    做 UI_递归扫描 用 元素_根节点、「body」、0。
    UI_扫描次数 为 UI_扫描次数 加 1。
    返还 已发现元素。
完。

UI_递归扫描 接受 节点、路径、深度：
    若 节点 等于 空 或者 做 计数 用 已发现元素 大于或等于 最大发现元素：
        返还。
    完。
    若 非 节点 的 可见：
        返还。
    完。
    当前路径 为 路径 加 「 > 」 加 做 UI_标签名 用 节点 的 标签。
    若 节点 的 可交互 或者 节点 的 无障碍角色 等于 「heading」 或者 节点 的 无障碍角色 等于 「img」：
        elem 为 新建 UI元素。
        elem 的 元素ID 为 节点 的 节点ID。
        elem 的 角色 为 节点 的 无障碍角色。
        elem 的 名称 为 做 元素_提取文本 用 节点。
        elem 的 左上x 为 节点 的 布局_x。
        elem 的 左上y 为 节点 的 布局_y。
        elem 的 宽 为 节点 的 布局_宽。
        elem 的 高 为 节点 的 布局_高。
        elem 的 中心x 为 节点 的 布局_x 加 节点 的 布局_宽 除以 2。
        elem 的 中心y 为 节点 的 布局_y 加 节点 的 布局_高 除以 2。
        elem 的 可点击 为 节点 的 无障碍角色 等于 「button」 或者 节点 的 无障碍角色 等于 「link」。
        elem 的 可输入 为 节点 的 无障碍角色 等于 「textbox」 或者 节点 的 无障碍角色 等于 「combobox」。
        elem 的 可见 为 真。
        elem 的 已禁用 为 假。
        elem 的 DOM路径 为 当前路径。
        做 追加 用 已发现元素、elem。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点 的 子节点：
        做 UI_递归扫描 用 节点 的 子节点 的 第 i 项、当前路径、深度 加 1。
        i 为 i 加 1。
    完。
完。

UI_标签名 接受 标签：
    若 标签 等于 1：
        返还 「div」。
    完。
    若 标签 等于 2：
        返还 「span」。
    完。
    若 标签 等于 3：
        返还 「p」。
    完。
    若 标签 等于 4：
        返还 「a」。
    完。
    若 标签 等于 5：
        返还 「button」。
    完。
    若 标签 等于 6：
        返还 「input」。
    完。
    若 标签 等于 7：
        返还 「img」。
    完。
    若 标签 等于 10：
        返还 「h1」。
    完。
    若 标签 等于 11：
        返还 「h2」。
    完。
    若 标签 等于 15：
        返还 「form」。
    完。
    若 标签 等于 16：
        返还 「textarea」。
    完。
    若 标签 等于 17：
        返还 「select」。
    完。
    若 标签 等于 21：
        返还 「nav」。
    完。
    返还 「div」。
完。

最大扫描深度 为 32。

匹配缓存大小 为 128。

DOM变更队列大小 为 64。

等待超时_毫秒 为 30000。

等待间隔_毫秒 为 200。

角色_按钮 为 「button」。

角色_链接 为 「link」。

角色_文本框 为 「textbox」。

角色_组合框 为 「combobox」。

角色_复选框 为 「checkbox」。

角色_单选框 为 「radio」。

角色_滑块 为 「slider」。

角色_标题 为 「heading」。

角色_图片 为 「img」。

角色_列表 为 「list」。

角色_列表项 为 「listitem」。

角色_菜单 为 「menu」。

角色_菜单项 为 「menuitem」。

角色_导航 为 「navigation」。

角色_选项卡 为 「tab」。

外部。

全局AI总线。

匹配缓存项 是 一种 事物，含 选择器、元素ID、命中次数、最后访问。

DOM变更 是 一种 事物，含 类型、节点ID、时间戳。

UI发现统计 是 一种 事物，含 总扫描次数、总元素数、可点击元素数、可输入元素数、匹配查找次数、匹配命中次数、缓存命中次数、等待次数、等待超时次数、DOM变更次数、AI预测次数、AI预测命中数。

可变 ui_匹配缓存 为 一组：（空）。

可变 ui_dom变更队列 为 一组：（空）。

可变 ui_统计 为 新建 UI发现统计。

可变 ui_已初始化 为 假。

UI_初始化 接受 零参：
    若 ui_已初始化：
        返还。
    完。
    ui_匹配缓存 为 一组：（空）。
    ui_dom变更队列 为 一组：（空）。
    ui_统计 为 新建 UI发现统计。
    ui_已初始化 为 真。
完。

匹配_文本 为 0x01。

匹配_角色 为 0x02。

匹配_属性 为 0x04。

匹配_路径 为 0x08。

匹配_坐标 为 0x10。

匹配_正则 为 0x20。

UI_缓存查找 接受 选择器文本：
    可变 i 为 0。
    只要 i 小于 做 计数 用 ui_匹配缓存：
        若 ui_匹配缓存 的 第 i 项 的 选择器 等于 选择器文本：
            ui_匹配缓存 的 第 i 项 的 命中次数 为 ui_匹配缓存 的 第 i 项 的 命中次数 加 1。
            ui_匹配缓存 的 第 i 项 的 最后访问 为 做 系统_时钟。
            ui_统计 的 缓存命中次数 为 ui_统计 的 缓存命中次数 加 1。
            返还 ui_匹配缓存 的 第 i 项 的 元素ID。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

UI_缓存写入 接受 选择器文本、元素ID值：
    若 做 计数 用 ui_匹配缓存 大于或等于 匹配缓存大小：
        可变 最旧索引 为 0。
        可变 最旧时间 为 ui_匹配缓存 的 第 0 项 的 最后访问。
        可变 k 为 1。
        只要 k 小于 做 计数 用 ui_匹配缓存：
            若 ui_匹配缓存 的 第 k 项 的 最后访问 小于 最旧时间：
                最旧时间 为 ui_匹配缓存 的 第 k 项 的 最后访问。
                最旧索引 为 k。
            完。
            k 为 k 加 1。
        完。
        做 删除 用 ui_匹配缓存、最旧索引。
    完。
    项 为 新建 匹配缓存项。
    项 的 选择器 为 选择器文本。
    项 的 元素ID 为 元素ID值。
    项 的 命中次数 为 1。
    项 的 最后访问 为 做 系统_时钟。
    做 追加 用 ui_匹配缓存、项。
完。

UI_按文本查找 接受 文本、精确匹配：
    ui_统计 的 匹配查找次数 为 ui_统计 的 匹配查找次数 加 1。
    缓存ID 为 做 UI_缓存查找 用 「文本:」 加 文本。
    若 缓存ID 大于或等于 0：
        返还 做 UI_按ID获取 用 缓存ID。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        匹配 为 假。
        若 精确匹配：
            匹配 为 已发现元素 的 第 i 项 的 名称 等于 文本。
        否则：
            匹配 为 做 字符串_包含 用 已发现元素 的 第 i 项 的 名称、文本。
        完。
        若 匹配：
            做 追加 用 结果、已发现元素 的 第 i 项。
            ui_统计 的 匹配命中次数 为 ui_统计 的 匹配命中次数 加 1。
            做 UI_缓存写入 用 「文本:」 加 文本、已发现元素 的 第 i 项 的 元素ID。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

UI_按角色查找 接受 角色名称：
    ui_统计 的 匹配查找次数 为 ui_统计 的 匹配查找次数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        若 已发现元素 的 第 i 项 的 角色 等于 角色名称：
            做 追加 用 结果、已发现元素 的 第 i 项。
            ui_统计 的 匹配命中次数 为 ui_统计 的 匹配命中次数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

UI_按路径查找 接受 路径片段：
    ui_统计 的 匹配查找次数 为 ui_统计 的 匹配查找次数 加 1。
    缓存ID 为 做 UI_缓存查找 用 「路径:」 加 路径片段。
    若 缓存ID 大于或等于 0：
        返还 做 UI_按ID获取 用 缓存ID。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        若 做 字符串_尾部匹配 用 已发现元素 的 第 i 项 的 DOM路径、路径片段：
            做 追加 用 结果、已发现元素 的 第 i 项。
            ui_统计 的 匹配命中次数 为 ui_统计 的 匹配命中次数 加 1。
            做 UI_缓存写入 用 「路径:」 加 路径片段、已发现元素 的 第 i 项 的 元素ID。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

UI_按坐标查找 接受 x、y、半径：
    ui_统计 的 匹配查找次数 为 ui_统计 的 匹配查找次数 加 1。
    可变 最近元素 为 空。
    可变 最近距离 为 999999。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        dx 为 已发现元素 的 第 i 项 的 中心x 减 x。
        dy 为 已发现元素 的 第 i 项 的 中心y 减 y。
        距离平方 为 dx 乘 dx 加 dy 乘 dy。
        若 距离平方 小于或等于 半径 乘 半径：
            若 距离平方 小于 最近距离：
                最近距离 为 距离平方。
                最近元素 为 已发现元素 的 第 i 项。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最近元素 不等于 空：
        ui_统计 的 匹配命中次数 为 ui_统计 的 匹配命中次数 加 1。
    完。
    返还 最近元素。
完。

UI_按ID获取 接受 目标ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        若 已发现元素 的 第 i 项 的 元素ID 等于 目标ID：
            返还 已发现元素 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

UI_组合匹配 接受 文本、角色、路径、策略掩码：
    ui_统计 的 匹配查找次数 为 ui_统计 的 匹配查找次数 加 1。
    可变 最优元素 为 空。
    可变 最高分 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        可变 分数 为 0。
        若 策略掩码 位与 匹配_文本：
            若 做 字符串_包含 用 已发现元素 的 第 i 项 的 名称、文本：
                分数 为 分数 加 3。
                若 已发现元素 的 第 i 项 的 名称 等于 文本：
                    分数 为 分数 加 2。
                完。
            完。
        完。
        若 策略掩码 位与 匹配_角色：
            若 已发现元素 的 第 i 项 的 角色 等于 角色：
                分数 为 分数 加 2。
            完。
        完。
        若 策略掩码 位与 匹配_路径：
            若 做 字符串_包含 用 已发现元素 的 第 i 项 的 DOM路径、路径：
                分数 为 分数 加 1。
            完。
        完。
        若 分数 大于 最高分：
            最高分 为 分数。
            最优元素 为 已发现元素 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 最优元素 不等于 空：
        ui_统计 的 匹配命中次数 为 ui_统计 的 匹配命中次数 加 1。
    完。
    返还 最优元素。
完。

交互_可点击 为 0x01。

交互_可输入 为 0x02。

交互_可拖拽 为 0x04。

交互_可滚动 为 0x08。

交互_可选择 为 0x10。

交互_可展开 为 0x20。

UI_检测交互类型 接受 元素：
    若 元素 等于 空：
        返还 0。
    完。
    可变 类型 为 0。
    若 非 元素 的 已禁用：
        若 元素 的 角色 等于 角色_按钮 或者 元素 的 角色 等于 角色_链接 或者 元素 的 角色 等于 角色_菜单项 或者 元素 的 角色 等于 角色_选项卡：
            类型 为 类型 位或 交互_可点击。
            ui_统计 的 可点击元素数 为 ui_统计 的 可点击元素数 加 1。
        完。
    完。
    若 元素 的 角色 等于 角色_文本框 或者 元素 的 角色 等于 角色_组合框：
        类型 为 类型 位或 交互_可输入。
        ui_统计 的 可输入元素数 为 ui_统计 的 可输入元素数 加 1。
    完。
    若 元素 的 角色 等于 角色_复选框 或者 元素 的 角色 等于 角色_单选框：
        类型 为 类型 位或 交互_可选择。
    完。
    若 做 元素_属性读 用 元素 的 元素ID、「draggable」 等于 「true」：
        类型 为 类型 位或 交互_可拖拽。
    完。
    若 做 元素_属性读 用 元素 的 元素ID、「scrollable」 等于 「true」：
        类型 为 类型 位或 交互_可滚动。
    完。
    若 做 元素_属性读 用 元素 的 元素ID、「aria-expanded」 不等于 「」：
        类型 为 类型 位或 交互_可展开。
    完。
    返还 类型。
完。

UI_批量标注交互 接受 零参：
    可变 交互表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        类型 为 做 UI_检测交互类型 用 已发现元素 的 第 i 项。
        若 类型 大于 0：
            做 追加 用 交互表、一本字典：元素ID 对应 已发现元素 的 第 i 项 的 元素ID、交互类型 对应 类型。
        完。
        i 为 i 加 1。
    完。
    做 内核调试 用 「UI交互标注: 」 加 做 计数 用 交互表 加 「个可交互元素」。
    返还 交互表。
完。

UI_检测可见性 接受 元素：
    若 元素 等于 空：
        返还 假。
    完。
    若 元素 的 宽 小于或等于 0 或者 元素 的 高 小于或等于 0：
        返还 假。
    完。
    若 非 元素 的 可见：
        返还 假。
    完。
    视口宽 为 做 系统_视口宽。
    视口高 为 做 系统_视口高。
    若 元素 的 左上x 加 元素 的 宽 小于 0 或者 元素 的 左上x 大于 视口宽：
        返还 假。
    完。
    若 元素 的 左上y 加 元素 的 高 小于 0 或者 元素 的 左上y 大于 视口高：
        返还 假。
    完。
    顶层ID 为 做 元素_命中测试 用 元素 的 中心x、元素 的 中心y。
    若 顶层ID 不等于 元素 的 元素ID：
        返还 假。
    完。
    返还 真。
完。

等待_元素存在 为 0。

等待_元素可见 为 1。

等待_元素可交互 为 2。

等待_文本出现 为 3。

等待_元素消失 为 4。

等待_DOM稳定 为 5。

等待条件 是 一种 事物，含 类型、选择器、目标文本、超时毫秒、间隔毫秒。

UI_等待条件 接受 条件：
    ui_统计 的 等待次数 为 ui_统计 的 等待次数 加 1。
    开始时间 为 做 系统_时钟。
    可变 已过时间 为 0。
    只要 已过时间 小于 条件 的 超时毫秒：
        做 UI_扫描全部元素。
        满足 为 做 UI_检查等待条件 用 条件。
        若 满足：
            做 内核调试 用 「等待条件满足, 耗时」 加 已过时间 加 「ms」。
            返还 真。
        完。
        做 系统_休眠 用 条件 的 间隔毫秒。
        已过时间 为 做 系统_时钟 减 开始时间。
    完。
    ui_统计 的 等待超时次数 为 ui_统计 的 等待超时次数 加 1。
    做 内核调试 用 「等待超时: 」 加 条件 的 超时毫秒 加 「ms」。
    返还 假。
完。

UI_检查等待条件 接受 条件：
    若 条件 的 类型 等于 等待_元素存在：
        结果 为 做 UI_按文本查找 用 条件 的 选择器、假。
        返还 做 计数 用 结果 大于 0。
    完。
    若 条件 的 类型 等于 等待_元素可见：
        结果 为 做 UI_按文本查找 用 条件 的 选择器、假。
        若 做 计数 用 结果 大于 0：
            返还 做 UI_检测可见性 用 结果 的 第 0 项。
        完。
        返还 假。
    完。
    若 条件 的 类型 等于 等待_元素可交互：
        结果 为 做 UI_按文本查找 用 条件 的 选择器、假。
        若 做 计数 用 结果 大于 0：
            返还 做 UI_检测交互类型 用 结果 的 第 0 项 大于 0。
        完。
        返还 假。
    完。
    若 条件 的 类型 等于 等待_文本出现：
        结果 为 做 UI_按文本查找 用 条件 的 目标文本、假。
        返还 做 计数 用 结果 大于 0。
    完。
    若 条件 的 类型 等于 等待_元素消失：
        结果 为 做 UI_按文本查找 用 条件 的 选择器、假。
        返还 做 计数 用 结果 等于 0。
    完。
    若 条件 的 类型 等于 等待_DOM稳定：
        返还 做 计数 用 ui_dom变更队列 等于 0。
    完。
    返还 假。
完。

UI_等待元素出现 接受 文本、超时：
    条件 为 新建 等待条件。
    条件 的 类型 为 等待_元素存在。
    条件 的 选择器 为 文本。
    条件 的 超时毫秒 为 超时。
    条件 的 间隔毫秒 为 等待间隔_毫秒。
    返还 做 UI_等待条件 用 条件。
完。

UI_等待元素可交互 接受 文本、超时：
    条件 为 新建 等待条件。
    条件 的 类型 为 等待_元素可交互。
    条件 的 选择器 为 文本。
    条件 的 超时毫秒 为 超时。
    条件 的 间隔毫秒 为 等待间隔_毫秒。
    返还 做 UI_等待条件 用 条件。
完。

UI_等待DOM稳定 接受 超时：
    条件 为 新建 等待条件。
    条件 的 类型 为 等待_DOM稳定。
    条件 的 超时毫秒 为 超时。
    条件 的 间隔毫秒 为 等待间隔_毫秒。
    返还 做 UI_等待条件 用 条件。
完。

可变 dom_观察回调 为 空。

UI_注册DOM观察 接受 回调函数：
    dom_观察回调 为 回调函数。
    做 内核调试 用 「DOM变更观察已注册」。
完。

UI_DOM变更通知 接受 类型码、节点ID值：
    变更 为 新建 DOM变更。
    变更 的 类型 为 类型码。
    变更 的 节点ID 为 节点ID值。
    变更 的 时间戳 为 做 系统_时钟。
    若 做 计数 用 ui_dom变更队列 大于或等于 DOM变更队列大小：
        做 删除 用 ui_dom变更队列、0。
    完。
    做 追加 用 ui_dom变更队列、变更。
    ui_统计 的 DOM变更次数 为 ui_统计 的 DOM变更次数 加 1。
    若 dom_观察回调 不等于 空：
        做 dom_观察回调 用 变更。
    完。
完。

UI_获取最近变更 接受 最大数量：
    可变 结果 为 一组：（空）。
    总数 为 做 计数 用 ui_dom变更队列。
    可变 起始 为 总数 减 最大数量。
    若 起始 小于 0：
        起始 为 0。
    完。
    可变 i 为 起始。
    只要 i 小于 总数：
        做 追加 用 结果、ui_dom变更队列 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

UI_变更类型统计 接受 零参：
    可变 添加数 为 0。
    可变 删除数 为 0。
    可变 修改数 为 0。
    可变 属性变更数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 ui_dom变更队列：
        若 ui_dom变更队列 的 第 i 项 的 类型 等于 0：
            添加数 为 添加数 加 1。
        完。
        若 ui_dom变更队列 的 第 i 项 的 类型 等于 1：
            删除数 为 删除数 加 1。
        完。
        若 ui_dom变更队列 的 第 i 项 的 类型 等于 2：
            修改数 为 修改数 加 1。
        完。
        若 ui_dom变更队列 的 第 i 项 的 类型 等于 3：
            属性变更数 为 属性变更数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一本字典：添加 对应 添加数、删除 对应 删除数、修改 对应 修改数、属性变更 对应 属性变更数。
完。

AI_特征维度 为 12。

UI_AI元素预测 接受 意图描述、上下文元素列表：
    若 全局AI总线 等于 空：
        做 内核调试 用 「AI总线未就绪, 回退常规匹配」。
        返还 做 UI_按文本查找 用 意图描述、假。
    完。
    ui_统计 的 AI预测次数 为 ui_统计 的 AI预测次数 加 1。
    张量 为 做 Tensor_创建 用 AI_特征维度。
    可变 最佳元素 为 空。
    可变 最高置信度 为 0.0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 上下文元素列表：
        元素 为 上下文元素列表 的 第 i 项。
        做 Tensor_写 用 张量、0、做 UI_角色编码 用 元素 的 角色。
        做 Tensor_写 用 张量、1、元素 的 中心x。
        做 Tensor_写 用 张量、2、元素 的 中心y。
        做 Tensor_写 用 张量、3、元素 的 宽。
        做 Tensor_写 用 张量、4、元素 的 高。
        做 Tensor_写 用 张量、5、元素 的 可点击。
        做 Tensor_写 用 张量、6、元素 的 可输入。
        做 Tensor_写 用 张量、7、做 字符串_长度 用 元素 的 名称。
        做 Tensor_写 用 张量、8、做 字符串_计数 用 元素 的 DOM路径、「>」。
        做 Tensor_写 用 张量、9、元素 的 可见。
        做 Tensor_写 用 张量、10、做 UI_检测交互类型 用 元素。
        做 Tensor_写 用 张量、11、做 字符串_相似度 用 元素 的 名称、意图描述。
        置信度 为 做 AI总线_推理 用 全局AI总线、「element_predict」、张量。
        若 置信度 大于 最高置信度：
            最高置信度 为 置信度。
            最佳元素 为 元素。
        完。
        i 为 i 加 1。
    完。
    做 Tensor_释放 用 张量。
    若 最佳元素 不等于 空 并且 最高置信度 大于 0.6：
        ui_统计 的 AI预测命中数 为 ui_统计 的 AI预测命中数 加 1。
        做 内核调试 用 「AI预测命中: 置信度=」 加 最高置信度。
    完。
    返还 最佳元素。
完。

UI_角色编码 接受 角色名：
    若 角色名 等于 角色_按钮：
        返还 1。
    完。
    若 角色名 等于 角色_链接：
        返还 2。
    完。
    若 角色名 等于 角色_文本框：
        返还 3。
    完。
    若 角色名 等于 角色_组合框：
        返还 4。
    完。
    若 角色名 等于 角色_复选框：
        返还 5。
    完。
    若 角色名 等于 角色_单选框：
        返还 6。
    完。
    若 角色名 等于 角色_滑块：
        返还 7。
    完。
    若 角色名 等于 角色_标题：
        返还 8。
    完。
    若 角色名 等于 角色_图片：
        返还 9。
    完。
    若 角色名 等于 角色_菜单：
        返还 10。
    完。
    若 角色名 等于 角色_菜单项：
        返还 11。
    完。
    若 角色名 等于 角色_导航：
        返还 12。
    完。
    若 角色名 等于 角色_选项卡：
        返还 13。
    完。
    返还 0。
完。

UI_AI异常检测 接受 零参：
    若 全局AI总线 等于 空：
        返还 一组：（空）。
    完。
    可变 异常列表 为 一组：（空）。
    张量 为 做 Tensor_创建 用 4。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        元素 为 已发现元素 的 第 i 项。
        做 Tensor_写 用 张量、0、元素 的 宽。
        做 Tensor_写 用 张量、1、元素 的 高。
        做 Tensor_写 用 张量、2、元素 的 中心x。
        做 Tensor_写 用 张量、3、元素 的 中心y。
        异常分 为 做 AI总线_异常检测 用 全局AI总线、「ui_anomaly」、张量。
        若 异常分 大于 0.8：
            做 追加 用 异常列表、一本字典：元素 对应 元素、异常分 对应 异常分。
        完。
        i 为 i 加 1。
    完。
    做 Tensor_释放 用 张量。
    返还 异常列表。
完。

MCP_UI_扫描 接受 零参：
    做 UI_扫描全部元素。
    返还 一本字典：成功 对应 真、元素数 对应 做 计数 用 已发现元素、元素列表 对应 已发现元素。
完。

MCP_UI_查找 接受 参数：
    策略 为 参数 的 策略。
    若 策略 等于 「文本」：
        返还 一本字典：成功 对应 真、结果 对应 做 UI_按文本查找 用 参数 的 查询、参数 的 精确。
    完。
    若 策略 等于 「角色」：
        返还 一本字典：成功 对应 真、结果 对应 做 UI_按角色查找 用 参数 的 查询。
    完。
    若 策略 等于 「路径」：
        返还 一本字典：成功 对应 真、结果 对应 做 UI_按路径查找 用 参数 的 查询。
    完。
    若 策略 等于 「坐标」：
        返还 一本字典：成功 对应 真、结果 对应 做 UI_按坐标查找 用 参数 的 x、参数 的 y、参数 的 半径。
    完。
    若 策略 等于 「组合」：
        返还 一本字典：成功 对应 真、结果 对应 做 UI_组合匹配 用 参数 的 文本、参数 的 角色、参数 的 路径、参数 的 掩码。
    完。
    若 策略 等于 「AI」：
        返还 一本字典：成功 对应 真、结果 对应 做 UI_AI元素预测 用 参数 的 意图、已发现元素。
    完。
    返还 一本字典：成功 对应 假、错误 对应 「未知匹配策略: 」 加 策略。
完。

MCP_UI_等待 接受 参数：
    结果 为 做 UI_等待元素出现 用 参数 的 选择器、参数 的 超时。
    返还 一本字典：成功 对应 结果、耗时 对应 参数 的 超时。
完。

MCP_UI_交互检测 接受 参数：
    元素 为 做 UI_按ID获取 用 参数 的 元素ID。
    若 元素 等于 空：
        返还 一本字典：成功 对应 假、错误 对应 「元素不存在」。
    完。
    交互类型 为 做 UI_检测交互类型 用 元素。
    返还 一本字典：成功 对应 真、可点击 对应 交互类型 位与 交互_可点击 大于 0、可输入 对应 交互类型 位与 交互_可输入 大于 0、可拖拽 对应 交互类型 位与 交互_可拖拽 大于 0、可滚动 对应 交互类型 位与 交互_可滚动 大于 0、可选择 对应 交互类型 位与 交互_可选择 大于 0、可展开 对应 交互类型 位与 交互_可展开 大于 0。
完。

UI_获取统计 接受 零参：
    返还 ui_统计。
完。

UI_重置统计 接受 零参：
    ui_统计 为 新建 UI发现统计。
    做 内核调试 用 「UI发现统计已重置」。
完。

UI_打印诊断 接受 零参：
    做 内核调试 用 「═══ NAED UI元素发现诊断 ═══」。
    做 内核调试 用 「  总扫描次数: 」 加 ui_统计 的 总扫描次数。
    做 内核调试 用 「  发现元素数: 」 加 ui_统计 的 总元素数。
    做 内核调试 用 「  可点击元素: 」 加 ui_统计 的 可点击元素数。
    做 内核调试 用 「  可输入元素: 」 加 ui_统计 的 可输入元素数。
    做 内核调试 用 「  匹配查找: 」 加 ui_统计 的 匹配查找次数 加 「 命中: 」 加 ui_统计 的 匹配命中次数。
    可变 命中率 为 0。
    若 ui_统计 的 匹配查找次数 大于 0：
        命中率 为 ui_统计 的 匹配命中次数 乘 100 除以 ui_统计 的 匹配查找次数。
    完。
    做 内核调试 用 「  匹配命中率: 」 加 命中率 加 「%」。
    做 内核调试 用 「  缓存命中: 」 加 ui_统计 的 缓存命中次数。
    做 内核调试 用 「  等待次数: 」 加 ui_统计 的 等待次数 加 「 超时: 」 加 ui_统计 的 等待超时次数。
    做 内核调试 用 「  DOM变更: 」 加 ui_统计 的 DOM变更次数。
    做 内核调试 用 「  AI预测: 」 加 ui_统计 的 AI预测次数 加 「 命中: 」 加 ui_统计 的 AI预测命中数。
    可变 AI命中率 为 0。
    若 ui_统计 的 AI预测次数 大于 0：
        AI命中率 为 ui_统计 的 AI预测命中数 乘 100 除以 ui_统计 的 AI预测次数。
    完。
    做 内核调试 用 「  AI预测命中率: 」 加 AI命中率 加 「%」。
    做 内核调试 用 「  缓存条目数: 」 加 做 计数 用 ui_匹配缓存。
    做 内核调试 用 「  DOM变更队列: 」 加 做 计数 用 ui_dom变更队列。
    做 内核调试 用 「═══════════════════════════」。
完。

UI_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 做 计数 用 ui_匹配缓存 大于 匹配缓存大小 乘 90 除以 100：
        做 追加 用 问题、「缓存接近上限」。
    完。
    若 ui_统计 的 匹配查找次数 大于 10：
        命中率 为 ui_统计 的 匹配命中次数 乘 100 除以 ui_统计 的 匹配查找次数。
        若 命中率 小于 30：
            做 追加 用 问题、「匹配命中率过低(」 加 命中率 加 「%)」。
        完。
    完。
    若 ui_统计 的 等待次数 大于 5：
        超时率 为 ui_统计 的 等待超时次数 乘 100 除以 ui_统计 的 等待次数。
        若 超时率 大于 50：
            做 追加 用 问题、「等待超时率过高(」 加 超时率 加 「%)」。
        完。
    完。
    若 ui_统计 的 AI预测次数 大于 5：
        AI率 为 ui_统计 的 AI预测命中数 乘 100 除以 ui_统计 的 AI预测次数。
        若 AI率 小于 40：
            做 追加 用 问题、「AI预测命中率偏低(」 加 AI率 加 「%)」。
        完。
    完。
    返还 一本字典：健康 对应 做 计数 用 问题 等于 0、问题列表 对应 问题。
完。

本章 收束。
