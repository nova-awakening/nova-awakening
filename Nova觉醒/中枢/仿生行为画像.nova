《仿生行为画像》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《..·感知·内核直连层》。

引用《.·内容可见性》。

行为窗口 为 64。

特征维度 为 12。

衰减系数 为 922。

基数 为 1024。

匹配阈值 为 700。

学习速率 为 50。

行为画像 是 一种 事物，含 按键间隔均值、按键按住时长、鼠标速度均值、鼠标加速均值、鼠标曲率均值、点击间隔均值、滚动速度均值、打字节奏方差、操作模式分布、活跃时段分布、置信度、样本数。

行为样本 是 一种 事物，含 时间戳、类型、值一、值二。

可变 当前画像 为 0。

可变 样本环 为 0。

可变 样本写位 为 0。

可变 操作计数 为 0。

可变 验证尝试 为 0。

可变 验证成功 为 0。

可变 验证失败 为 0。

可变 异常检测数 为 0。

仿生_初始化 接受 零参：
    当前画像 为 做 堆_分配 用 行为画像。
    当前画像 的 按键间隔均值 为 150。
    当前画像 的 按键按住时长 为 80。
    当前画像 的 鼠标速度均值 为 400。
    当前画像 的 鼠标加速均值 为 200。
    当前画像 的 鼠标曲率均值 为 50。
    当前画像 的 点击间隔均值 为 500。
    当前画像 的 滚动速度均值 为 300。
    当前画像 的 打字节奏方差 为 30。
    当前画像 的 操作模式分布 为 做 堆_分配 用 8 乘 4。
    当前画像 的 活跃时段分布 为 做 堆_分配 用 24 乘 4。
    当前画像 的 置信度 为 0。
    当前画像 的 样本数 为 0。
    样本环 为 做 堆_分配 用 行为窗口 乘 24。
    样本写位 为 0。
完。

仿生_记录按键 接受 间隔、按住时长：
    操作计数 为 操作计数 加 1。
    当前画像 的 按键间隔均值 为 (当前画像 的 按键间隔均值 乘 衰减系数 加 间隔 乘 (基数 减 衰减系数)) 除以 基数。
    当前画像 的 按键按住时长 为 (当前画像 的 按键按住时长 乘 衰减系数 加 按住时长 乘 (基数 减 衰减系数)) 除以 基数。
    偏差 为 间隔 减 当前画像 的 按键间隔均值。
    若 偏差 小于 0：
        偏差 为 0 减 偏差。
    完。
    当前画像 的 打字节奏方差 为 (当前画像 的 打字节奏方差 乘 衰减系数 加 偏差 乘 (基数 减 衰减系数)) 除以 基数。
    做 仿生_写样本 用 0、间隔、按住时长。
    当前画像 的 样本数 为 当前画像 的 样本数 加 1。
完。

仿生_记录鼠标 接受 速度、加速度、曲率：
    操作计数 为 操作计数 加 1。
    当前画像 的 鼠标速度均值 为 (当前画像 的 鼠标速度均值 乘 衰减系数 加 速度 乘 (基数 减 衰减系数)) 除以 基数。
    当前画像 的 鼠标加速均值 为 (当前画像 的 鼠标加速均值 乘 衰减系数 加 加速度 乘 (基数 减 衰减系数)) 除以 基数。
    当前画像 的 鼠标曲率均值 为 (当前画像 的 鼠标曲率均值 乘 衰减系数 加 曲率 乘 (基数 减 衰减系数)) 除以 基数。
    做 仿生_写样本 用 1、速度、曲率。
完。

仿生_记录点击 接受 间隔：
    当前画像 的 点击间隔均值 为 (当前画像 的 点击间隔均值 乘 衰减系数 加 间隔 乘 (基数 减 衰减系数)) 除以 基数。
    做 仿生_写样本 用 2、间隔、0。
完。

仿生_记录滚动 接受 速度：
    当前画像 的 滚动速度均值 为 (当前画像 的 滚动速度均值 乘 衰减系数 加 速度 乘 (基数 减 衰减系数)) 除以 基数。
    做 仿生_写样本 用 3、速度、0。
完。

仿生_写样本 接受 类型、值一、值二：
    偏移 为 样本环 加 样本写位 乘 24。
    做 内存_写64 用 偏移、做 平台_获取高精度时间。
    做 内存_写32 用 偏移 加 8、类型。
    做 内存_写32 用 偏移 加 12、值一。
    做 内存_写32 用 偏移 加 16、值二。
    样本写位 为 (样本写位 加 1) 取余于 行为窗口。
完。

仿生_验证 接受 目标画像：
    验证尝试 为 验证尝试 加 1。
    若 当前画像 等于 0 或者 目标画像 等于 0：
        验证失败 为 验证失败 加 1。
        返还 假。
    完。
    可变 总分 为 0。
    总分 为 总分 加 做 仿生_特征相似 用 当前画像 的 按键间隔均值、目标画像 的 按键间隔均值、200。
    总分 为 总分 加 做 仿生_特征相似 用 当前画像 的 按键按住时长、目标画像 的 按键按住时长、100。
    总分 为 总分 加 做 仿生_特征相似 用 当前画像 的 鼠标速度均值、目标画像 的 鼠标速度均值、500。
    总分 为 总分 加 做 仿生_特征相似 用 当前画像 的 鼠标加速均值、目标画像 的 鼠标加速均值、300。
    总分 为 总分 加 做 仿生_特征相似 用 当前画像 的 鼠标曲率均值、目标画像 的 鼠标曲率均值、100。
    总分 为 总分 加 做 仿生_特征相似 用 当前画像 的 点击间隔均值、目标画像 的 点击间隔均值、400。
    总分 为 总分 加 做 仿生_特征相似 用 当前画像 的 滚动速度均值、目标画像 的 滚动速度均值、300。
    总分 为 总分 加 做 仿生_特征相似 用 当前画像 的 打字节奏方差、目标画像 的 打字节奏方差、50。
    平均分 为 总分 除以 8。
    若 平均分 大于或等于 匹配阈值：
        验证成功 为 验证成功 加 1。
        当前画像 的 置信度 为 (当前画像 的 置信度 乘 9 加 平均分) 除以 10。
        返还 真。
    完。
    验证失败 为 验证失败 加 1。
    异常检测数 为 异常检测数 加 1。
    返还 假。
完。

仿生_特征相似 接受 实际、期望、容差：
    差值 为 实际 减 期望。
    若 差值 小于 0：
        差值 为 0 减 差值。
    完。
    若 差值 大于或等于 容差：
        返还 0。
    完。
    返还 (容差 减 差值) 乘 基数 除以 容差。
完。

仿生_学习更新 接受 目标画像：
    若 当前画像 等于 0 或者 目标画像 等于 0：
        返还。
    完。
    目标画像 的 按键间隔均值 为 (目标画像 的 按键间隔均值 乘 (基数 减 学习速率) 加 当前画像 的 按键间隔均值 乘 学习速率) 除以 基数。
    目标画像 的 按键按住时长 为 (目标画像 的 按键按住时长 乘 (基数 减 学习速率) 加 当前画像 的 按键按住时长 乘 学习速率) 除以 基数。
    目标画像 的 鼠标速度均值 为 (目标画像 的 鼠标速度均值 乘 (基数 减 学习速率) 加 当前画像 的 鼠标速度均值 乘 学习速率) 除以 基数。
    目标画像 的 鼠标加速均值 为 (目标画像 的 鼠标加速均值 乘 (基数 减 学习速率) 加 当前画像 的 鼠标加速均值 乘 学习速率) 除以 基数。
    目标画像 的 鼠标曲率均值 为 (目标画像 的 鼠标曲率均值 乘 (基数 减 学习速率) 加 当前画像 的 鼠标曲率均值 乘 学习速率) 除以 基数。
完。

仿生_获取置信度 接受 零参：
    若 当前画像 等于 0：
        返还 0。
    完。
    返还 当前画像 的 置信度。
完。

仿生_导出画像 接受 零参：
    若 当前画像 等于 0：
        返还 空。
    完。
    全局统计 的 导出次数 为 全局统计 的 导出次数 加 1。
    返还 一组：当前画像 的 按键间隔均值、当前画像 的 按键按住时长、当前画像 的 鼠标速度均值、当前画像 的 鼠标加速均值、当前画像 的 鼠标曲率均值、当前画像 的 点击间隔均值、当前画像 的 滚动速度均值、当前画像 的 打字节奏方差、当前画像 的 置信度、当前画像 的 样本数。
完。

仿生_导入画像 接受 数据：
    若 数据 等于 空 或者 做 计数 用 数据 小于 10：
        返还 假。
    完。
    若 当前画像 等于 0：
        做 仿生_初始化。
    完。
    全局统计 的 导入次数 为 全局统计 的 导入次数 加 1。
    当前画像 的 按键间隔均值 为 数据 的 第 0 项。
    当前画像 的 按键按住时长 为 数据 的 第 1 项。
    当前画像 的 鼠标速度均值 为 数据 的 第 2 项。
    当前画像 的 鼠标加速均值 为 数据 的 第 3 项。
    当前画像 的 鼠标曲率均值 为 数据 的 第 4 项。
    当前画像 的 点击间隔均值 为 数据 的 第 5 项。
    当前画像 的 滚动速度均值 为 数据 的 第 6 项。
    当前画像 的 打字节奏方差 为 数据 的 第 7 项。
    当前画像 的 置信度 为 数据 的 第 8 项。
    当前画像 的 样本数 为 数据 的 第 9 项。
    返还 真。
完。

最大画像数 为 16。

可变 画像库 为 一组：（空）。

可变 画像库大小 为 0。

仿生_保存画像 接受 用户ID：
    若 当前画像 等于 0：
        返还 假。
    完。
    全局统计 的 画像保存数 为 全局统计 的 画像保存数 加 1。
    数据 为 做 仿生_导出画像。
    可变 i 为 0。
    只要 i 小于 画像库大小：
        若 画像库 的 第 i 项 的 第 0 项 等于 用户ID：
            做 设置 用 画像库、i、一组：用户ID、数据。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    若 画像库大小 大于或等于 最大画像数：
        返还 假。
    完。
    做 追加 用 画像库、一组：用户ID、数据。
    画像库大小 为 画像库大小 加 1。
    返还 真。
完。

仿生_加载画像 接受 用户ID：
    全局统计 的 画像加载数 为 全局统计 的 画像加载数 加 1。
    可变 i 为 0。
    只要 i 小于 画像库大小：
        若 画像库 的 第 i 项 的 第 0 项 等于 用户ID：
            返还 做 仿生_导入画像 用 画像库 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

仿生_列出画像 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 画像库大小：
        做 追加 用 结果、画像库 的 第 i 项 的 第 0 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

仿生_删除画像 接受 用户ID：
    可变 i 为 0。
    只要 i 小于 画像库大小：
        若 画像库 的 第 i 项 的 第 0 项 等于 用户ID：
            做 删除 用 画像库、i。
            画像库大小 为 画像库大小 减 1。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

仿生_检测异常 接受 零参：
    若 当前画像 等于 0：
        返还 假。
    完。
    全局统计 的 异常检测调用数 为 全局统计 的 异常检测调用数 加 1。
    可变 异常分 为 0。
    若 当前画像 的 按键间隔均值 小于 20 或者 当前画像 的 按键间隔均值 大于 500：
        异常分 为 异常分 加 300。
    完。
    若 当前画像 的 鼠标速度均值 小于 10 或者 当前画像 的 鼠标速度均值 大于 5000：
        异常分 为 异常分 加 300。
    完。
    若 当前画像 的 鼠标曲率均值 小于 5：
        异常分 为 异常分 加 400。
    完。
    若 当前画像 的 打字节奏方差 小于 3：
        异常分 为 异常分 加 400。
    完。
    若 异常分 大于 500：
        异常检测数 为 异常检测数 加 1。
        全局统计 的 异常发现数 为 全局统计 的 异常发现数 加 1。
        返还 真。
    完。
    返还 假。
完。

仿生_异常评分 接受 零参：
    若 当前画像 等于 0：
        返还 0。
    完。
    可变 分数 为 1000。
    若 当前画像 的 鼠标曲率均值 小于 10：
        分数 为 分数 减 200。
    完。
    若 当前画像 的 打字节奏方差 小于 5：
        分数 为 分数 减 200。
    完。
    若 当前画像 的 按键间隔均值 小于 30：
        分数 为 分数 减 150。
    完。
    若 当前画像 的 鼠标速度均值 大于 3000：
        分数 为 分数 减 150。
    完。
    若 当前画像 的 样本数 小于 10：
        分数 为 分数 减 100。
    完。
    若 分数 小于 0：
        分数 为 0。
    完。
    返还 分数。
完。

仿生_人类概率 接受 零参：
    评分 为 做 仿生_异常评分。
    返还 评分 除以 10。
完。

仿生_重置画像 接受 零参：
    若 当前画像 等于 0：
        返还。
    完。
    全局统计 的 画像重置数 为 全局统计 的 画像重置数 加 1。
    当前画像 的 按键间隔均值 为 150。
    当前画像 的 按键按住时长 为 80。
    当前画像 的 鼠标速度均值 为 400。
    当前画像 的 鼠标加速均值 为 200。
    当前画像 的 鼠标曲率均值 为 50。
    当前画像 的 点击间隔均值 为 500。
    当前画像 的 滚动速度均值 为 300。
    当前画像 的 打字节奏方差 为 30。
    当前画像 的 置信度 为 0。
    当前画像 的 样本数 为 0。
    样本写位 为 0。
    操作计数 为 0。
完。

仿生_销毁 接受 零参：
    若 当前画像 不等于 0：
        当前画像 为 0。
    完。
    样本环 为 0。
    画像库 为 一组：（空）。
    画像库大小 为 0。
    做 打印 用 「[仿生统计] 已销毁」。
完。

AI行为窗口 为 16。

可变 AI行为历史 为 一组：（空）。

可变 AI行为位置 为 0。

AI_记录行为评分 接受 评分：
    若 做 计数 用 AI行为历史 小于 AI行为窗口：
        做 追加 用 AI行为历史、评分。
    否则：
        做 设置 用 AI行为历史、AI行为位置、评分。
    完。
    AI行为位置 为 (AI行为位置 加 1) 取余于 AI行为窗口。
    全局统计 的 AI学习次数 为 全局统计 的 AI学习次数 加 1。
完。

AI_行为趋势 接受 零参：
    若 做 计数 用 AI行为历史 小于 4：
        返还 「数据不足」。
    完。
    半 为 做 计数 用 AI行为历史 除以 2。
    可变 前半和 为 0。
    可变 后半和 为 0。
    可变 i 为 0。
    只要 i 小于 半：
        前半和 为 前半和 加 AI行为历史 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 i 小于 做 计数 用 AI行为历史：
        后半和 为 后半和 加 AI行为历史 的 第 i 项。
        i 为 i 加 1。
    完。
    前均 为 前半和 除以 半。
    后均 为 后半和 除以 (做 计数 用 AI行为历史 减 半)。
    若 后均 大于 前均 加 50：
        返还 「仿真度提升」。
    完。
    若 后均 小于 前均 减 50：
        返还 「仿真度下降」。
    完。
    返还 「仿真度稳定」。
完。

AI_建议调整 接受 零参：
    评分 为 做 仿生_异常评分。
    若 评分 大于 800：
        返还 「当前仿生质量优秀」。
    完。
    若 评分 大于 500：
        返还 「增加鼠标曲率和节奏方差」。
    完。
    返还 「仿生参数需要大幅调整」。
完。

行为统计 是 一种 事物，含 初始化次数、按键记录数、鼠标记录数、点击记录数、滚动记录数、验证尝试数、验证成功数、验证失败数、异常检测调用数、异常发现数、画像保存数、画像加载数、画像重置数、导出次数、导入次数、AI学习次数、错误次数。

可变 全局统计 为 新建 行为统计。

可变 行为模块已初始化 为 假。

行为模块_初始化 接受 零参：
    若 行为模块已初始化 等于 真：
        返还 0 减 17。
    完。
    做 仿生_初始化。
    画像库 为 一组：（空）。
    画像库大小 为 0。
    AI行为历史 为 一组：（空）。
    AI行为位置 为 0。
    行为模块已初始化 为 真。
    全局统计 的 初始化次数 为 全局统计 的 初始化次数 加 1。
    做 打印 用 「[仿生统计] 初始化完成」。
    返还 0。
完。

行为模块_销毁 接受 零参：
    若 行为模块已初始化 等于 假：
        返还 0。
    完。
    做 仿生_销毁。
    AI行为历史 为 一组：（空）。
    行为模块已初始化 为 假。
    做 打印 用 「[仿生统计] 模块已销毁」。
    返还 0。
完。

仿生行为_完整统计 接受 零参：
    返还 一组：操作计数、验证尝试、验证成功、验证失败、异常检测数、做 仿生_获取置信度。
完。

行为模块_转储 接受 零参：
    做 打印 用 「[仿生统计] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  操作计数: 」、做 整数转字符串 用 操作计数。
    做 打印 用 做 字符串拼接 用 「  验证尝试: 」、做 整数转字符串 用 验证尝试。
    做 打印 用 做 字符串拼接 用 「  验证成功: 」、做 整数转字符串 用 验证成功。
    做 打印 用 做 字符串拼接 用 「  异常评分: 」、做 整数转字符串 用 做 仿生_异常评分。
    做 打印 用 做 字符串拼接 用 「  人类概率%: 」、做 整数转字符串 用 做 仿生_人类概率。
    做 打印 用 做 字符串拼接 用 「  置信度: 」、做 整数转字符串 用 做 仿生_获取置信度。
    做 打印 用 做 字符串拼接 用 「  画像库大小: 」、做 整数转字符串 用 画像库大小。
    做 打印 用 做 字符串拼接 用 「  AI趋势: 」、做 AI_行为趋势。
    做 打印 用 做 字符串拼接 用 「  AI建议: 」、做 AI_建议调整。
    返还 0。
完。

行为模块_打印统计 接受 零参：
    做 打印 用 「[仿生统计] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  初始化: 」、做 整数转字符串 用 全局统计 的 初始化次数。
    做 打印 用 做 字符串拼接 用 「  按键记录: 」、做 整数转字符串 用 全局统计 的 按键记录数。
    做 打印 用 做 字符串拼接 用 「  鼠标记录: 」、做 整数转字符串 用 全局统计 的 鼠标记录数。
    做 打印 用 做 字符串拼接 用 「  点击记录: 」、做 整数转字符串 用 全局统计 的 点击记录数。
    做 打印 用 做 字符串拼接 用 「  滚动记录: 」、做 整数转字符串 用 全局统计 的 滚动记录数。
    做 打印 用 做 字符串拼接 用 「  验证尝试: 」、做 整数转字符串 用 全局统计 的 验证尝试数。
    做 打印 用 做 字符串拼接 用 「  验证成功: 」、做 整数转字符串 用 全局统计 的 验证成功数。
    做 打印 用 做 字符串拼接 用 「  验证失败: 」、做 整数转字符串 用 全局统计 的 验证失败数。
    做 打印 用 做 字符串拼接 用 「  异常检测: 」、做 整数转字符串 用 全局统计 的 异常检测调用数。
    做 打印 用 做 字符串拼接 用 「  异常发现: 」、做 整数转字符串 用 全局统计 的 异常发现数。
    做 打印 用 做 字符串拼接 用 「  画像保存: 」、做 整数转字符串 用 全局统计 的 画像保存数。
    做 打印 用 做 字符串拼接 用 「  画像加载: 」、做 整数转字符串 用 全局统计 的 画像加载数。
    做 打印 用 做 字符串拼接 用 「  画像重置: 」、做 整数转字符串 用 全局统计 的 画像重置数。
    做 打印 用 做 字符串拼接 用 「  导出: 」、做 整数转字符串 用 全局统计 的 导出次数。
    做 打印 用 做 字符串拼接 用 「  导入: 」、做 整数转字符串 用 全局统计 的 导入次数。
    做 打印 用 做 字符串拼接 用 「  AI学习: 」、做 整数转字符串 用 全局统计 的 AI学习次数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 整数转字符串 用 全局统计 的 错误次数。
    若 验证尝试 大于 0：
        成功率 为 验证成功 乘 100 除以 验证尝试。
        做 打印 用 做 字符串拼接 用 「  验证成功率%: 」、做 整数转字符串 用 成功率。
    完。
    返还 0。
完。

行为模块_注册到内核 接受 内核上下文：
    若 行为模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[仿生统计] 已注册到内核上下文」。
    返还 0。
完。

行为模块_查询状态 接受 零参：
    若 行为模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

行为模块_健康检查 接受 零参：
    若 行为模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 操作计数：
        返还 假。
    完。
    返还 真。
完。

仿生_获取操作计数 接受 零参：
    返还 操作计数。
完。

仿生_获取验证尝试 接受 零参：
    返还 验证尝试。
完。

仿生_获取验证成功 接受 零参：
    返还 验证成功。
完。

仿生_获取验证失败 接受 零参：
    返还 验证失败。
完。

仿生_获取异常数 接受 零参：
    返还 异常检测数。
完。

仿生_获取画像库大小 接受 零参：
    返还 画像库大小。
完。

仿生_获取验证成功率 接受 零参：
    若 验证尝试 等于 0：
        返还 0。
    完。
    返还 验证成功 乘 100 除以 验证尝试。
完。

仿生_获取画像摘要 接受 零参：
    若 当前画像 等于 0：
        返还 「未初始化」。
    完。
    返还 做 字符串拼接 用 「置信:」、做 整数转字符串 用 当前画像 的 置信度。
完。

仿生_Tick 接受 零参：
    若 操作计数 大于 0 并且 操作计数 取余于 100 等于 0：
        做 仿生_检测异常。
        评分 为 做 仿生_异常评分。
        做 AI_记录行为评分 用 评分。
    完。
完。

仿生_重置统计 接受 零参：
    操作计数 为 0。
    验证尝试 为 0。
    验证成功 为 0。
    验证失败 为 0。
    异常检测数 为 0。
完。

本章 收束。
