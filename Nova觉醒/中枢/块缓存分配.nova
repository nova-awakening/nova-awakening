《块缓存分配》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·感知·内核直连层》。

引用《.·内存初始化》。

块页大小 为 4096。

最小块大小 为 8。

最大块大小 为 2048。

缓存级数 为 12。

每级容量 为 64。

碎片预测深 为 8。

块缓存 是 一种 事物，含 块大小、每页块数、空闲链头、空闲数、已用数、满页数、半页数、空页数、总分配、总释放、峰值占用、碎片率。

可变 缓存表 为 一组：（空）。

可变 缓存数 为 0。

可变 全局分配 为 0。

可变 全局释放 为 0。

可变 全局碎片 为 0。

可变 碎片历史 为 0。

可变 碎片写位 为 0。

可变 神经碎片权 为 512。

块分配_初始化 接受 零参：
    缓存表 为 一组：（空）。
    碎片历史 为 做 堆_分配 用 碎片预测深 乘 4。
    碎片写位 为 0。
    可变 大小 为 最小块大小。
    只要 大小 小于或等于 最大块大小 并且 缓存数 小于 缓存级数：
        缓存 为 新建 块缓存。
        缓存 的 块大小 为 大小。
        缓存 的 每页块数 为 若 大小 大于 0 则 块页大小 除以 大小 否则 1。
        缓存 的 空闲链头 为 0。
        缓存 的 空闲数 为 0。
        缓存 的 已用数 为 0。
        缓存 的 满页数 为 0。
        缓存 的 半页数 为 0。
        缓存 的 空页数 为 0。
        缓存 的 总分配 为 0。
        缓存 的 总释放 为 0。
        缓存 的 峰值占用 为 0。
        缓存 的 碎片率 为 0。
        做 追加 用 缓存表、缓存。
        缓存数 为 缓存数 加 1。
        大小 为 大小 乘 2。
    完。
完。

块分配_大小到级 接受 需要大小：
    可变 级 为 0。
    可变 大小 为 最小块大小。
    只要 级 小于 缓存数：
        若 大小 大于或等于 需要大小：
            返还 级。
        完。
        大小 为 大小 乘 2。
        级 为 级 加 1。
    完。
    返还 负 1。
完。

块分配_申请 接受 需要大小：
    级 为 做 块分配_大小到级 用 需要大小。
    若 级 小于 0 或者 级 大于或等于 缓存数：
        返还 0。
    完。
    缓存 为 缓存表 的 第 级 项。
    若 缓存 的 空闲链头 不等于 0：
        块 为 缓存 的 空闲链头。
        缓存 的 空闲链头 为 做 内存_读64 用 块。
        缓存 的 空闲数 为 缓存 的 空闲数 减 1。
        缓存 的 已用数 为 缓存 的 已用数 加 1。
        缓存 的 总分配 为 缓存 的 总分配 加 1。
        全局分配 为 全局分配 加 1。
        若 缓存 的 已用数 大于 缓存 的 峰值占用：
            缓存 的 峰值占用 为 缓存 的 已用数。
        完。
        返还 块。
    完。
    新页 为 做 堆_分配 用 块页大小。
    若 新页 等于 0：
        返还 0。
    完。
    可变 偏移 为 缓存 的 块大小。
    只要 偏移 加 缓存 的 块大小 小于或等于 块页大小：
        做 内存_写64 用 新页 加 偏移、缓存 的 空闲链头。
        缓存 的 空闲链头 为 新页 加 偏移。
        缓存 的 空闲数 为 缓存 的 空闲数 加 1。
        偏移 为 偏移 加 缓存 的 块大小。
    完。
    缓存 的 半页数 为 缓存 的 半页数 加 1。
    缓存 的 已用数 为 缓存 的 已用数 加 1。
    缓存 的 总分配 为 缓存 的 总分配 加 1。
    全局分配 为 全局分配 加 1。
    若 缓存 的 已用数 大于 缓存 的 峰值占用：
        缓存 的 峰值占用 为 缓存 的 已用数。
    完。
    返还 新页。
完。

块分配_释放 接受 指针、大小：
    级 为 做 块分配_大小到级 用 大小。
    若 级 小于 0 或者 级 大于或等于 缓存数：
        返还。
    完。
    缓存 为 缓存表 的 第 级 项。
    做 内存_写64 用 指针、缓存 的 空闲链头。
    缓存 的 空闲链头 为 指针。
    缓存 的 空闲数 为 缓存 的 空闲数 加 1。
    缓存 的 已用数 为 缓存 的 已用数 减 1。
    缓存 的 总释放 为 缓存 的 总释放 加 1。
    全局释放 为 全局释放 加 1。
完。

块分配_更新碎片 接受 零参：
    可变 总空闲 为 0。
    可变 总容量 为 0。
    可变 序号 为 0。
    只要 序号 小于 缓存数：
        总空闲 为 总空闲 加 缓存表 的 第 序号 项 的 空闲数 乘 缓存表 的 第 序号 项 的 块大小。
        总容量 为 总容量 加 (缓存表 的 第 序号 项 的 空闲数 加 缓存表 的 第 序号 项 的 已用数) 乘 缓存表 的 第 序号 项 的 块大小。
        序号 为 序号 加 1。
    完。
    若 总容量 大于 0：
        全局碎片 为 总空闲 乘 1024 除以 总容量。
    完。
    做 内存_写32 用 碎片历史 加 碎片写位 乘 4、全局碎片。
    碎片写位 为 (碎片写位 加 1) 取余于 碎片预测深。
完。

块分配_预测碎片趋势 接受 零参：
    可变 前半 为 0。
    可变 后半 为 0。
    半深 为 碎片预测深 除以 2。
    可变 序号 为 0。
    只要 序号 小于 半深：
        前半 为 前半 加 做 内存_读32 用 碎片历史 加 序号 乘 4。
        序号 为 序号 加 1。
    完。
    只要 序号 小于 碎片预测深：
        后半 为 后半 加 做 内存_读32 用 碎片历史 加 序号 乘 4。
        序号 为 序号 加 1。
    完。
    若 前半 等于 0：
        返还 0。
    完。
    返还 (后半 减 前半) 乘 1024 除以 前半。
完。

块分配_收缩空闲 接受 级：
    若 级 大于或等于 缓存数：
        返还 0。
    完。
    缓存 为 缓存表 的 第 级 项。
    可变 释放数 为 0。
    只要 缓存 的 空闲数 大于 缓存 的 每页块数 乘 2：
        块 为 缓存 的 空闲链头。
        若 块 等于 0：
            跳出。
        完。
        缓存 的 空闲链头 为 做 内存_读64 用 块。
        缓存 的 空闲数 为 缓存 的 空闲数 减 1。
        释放数 为 释放数 加 1。
    完。
    返还 释放数。
完。

块分配_获取级信息 接受 级：
    若 级 大于或等于 缓存数：
        返还 一组：0、0、0、0、0。
    完。
    缓存 为 缓存表 的 第 级 项。
    返还 一组：缓存 的 块大小、缓存 的 已用数、缓存 的 空闲数、缓存 的 总分配、缓存 的 峰值占用。
完。

块分配_完整统计 接受 零参：
    返还 一组：全局分配、全局释放、全局分配 减 全局释放、缓存数、全局碎片、做 块分配_预测碎片趋势、神经碎片权。
完。

本章 收束。
