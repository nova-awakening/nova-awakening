《伙伴系统释放》

引用《.·类型定义》。

引用《.·同步原语》。

引用《..·执行·内核智能》。

引用《.·伙伴系统计》。

引用《.·内存回收》。

引用《.·字符串替换》。

引用《.·每核页释放》。

引用《.·自动扩容》。

引用《.·内核打印》。

引用《.·整数转字符串》。

引用《.·物理页描述符》。

伙伴_分配页面 接受 gfp、阶、首选zone、迁移类型：
    若 阶 等于 0 并且 迁移类型 小于 MIGRATE_PCPTYPES：
        可变 i 为 0。
        只要 i 小于 活跃zone数：
            可变 z 为 所有zone 的 第 zone搜索顺序 的 第 i 项 项。
            若 非 z 的 已初始化：
                i 为 i 加 1。
                继续。
            完。
            若 z 的 类型 大于 首选zone：
                i 为 i 加 1。
                继续。
            完。
            页号 为 做 PCP_分配 用 z、迁移类型。
            若 页号 大于或等于 0：
                全局空闲页数 为 全局空闲页数 减 1。
                若 gfp 位与 __GFP_ZERO 不等于 0：
                    做 内存清零 用 做 页号到地址 用 页号、页大小。
                完。
                返还 页号。
            完。
            i 为 i 加 1。
        完。
    完。
    是原子 为 gfp 位与 GFP_ATOMIC 不等于 0 或者 gfp 位与 __GFP_HIGH 不等于 0。
    可变 i 为 0。
    只要 i 小于 活跃zone数：
        可变 z 为 所有zone 的 第 zone搜索顺序 的 第 i 项 项。
        若 非 z 的 已初始化：
            i 为 i 加 1。
            继续。
        完。
        若 z 的 类型 大于 首选zone：
            i 为 i 加 1。
            继续。
        完。
        水位级别 为 WMARK_LOW。
        若 是原子：
            水位级别 为 WMARK_MIN。
        完。
        若 非 做 zone_水位OK 用 z、阶、水位级别、首选zone：
            若 非 是原子：
                做 唤醒kswapd 用 z 的 类型。
                回收页数 为 做 直接内存回收 用 阶、gfp。
                若 回收页数 大于 0：
                    做 自旋锁_获取 用 z 的 锁。
                    重试页号 为 做 __伙伴_取页 用 z、阶、迁移类型。
                    做 自旋锁_释放 用 z 的 锁。
                    若 重试页号 大于或等于 0：
                        全局空闲页数 为 全局空闲页数 减 (1 左移 阶)。
                        返还 重试页号。
                    完。
                完。
            完。
            i 为 i 加 1。
            继续。
        完。
        做 自旋锁_获取 用 z 的 锁。
        页号 为 做 __伙伴_取页 用 z、阶、迁移类型。
        做 自旋锁_释放 用 z 的 锁。
        若 页号 大于或等于 0：
            分配大小 为 1 左移 阶。
            全局空闲页数 为 全局空闲页数 减 分配大小。
            若 gfp 位与 __GFP_ZERO 不等于 0：
                做 内存清零 用 做 页号到地址 用 页号、分配大小 乘 页大小。
            完。
            返还 页号。
        完。
        i 为 i 加 1。
    完。
    若 gfp 位与 __GFP_NOWARN 等于 0：
        做 内核警告 用 「伙伴系统: 分配失败」。
        做 内核警告数值 用 「  请求阶」、阶。
        做 内核警告数值 用 「  全局空闲页」、全局空闲页数。
    完。
    返还 0 减 1。
完。

伙伴系统_释放 接受 页号、阶：
    若 页号 小于 0 或者 阶 大于 最大阶：
        返还。
    完。
    页 为 做 页描述符_获取 用 页号。
    若 页 等于 空：
        返还。
    完。
    zone类型 为 页 的 第 PG_区域 项。
    可变 z 为 所有zone 的 第 zone类型 项。
    若 非 z 的 已初始化：
        返还。
    完。
    分配大小 为 1 左移 阶。
    迁移类型 为 做 获取页块迁移类型 用 z、页号。
    可变 j 为 0。
    只要 j 小于 分配大小：
        p 为 做 页描述符_获取 用 页号 加 j。
        若 p 不等于 空：
            做 页描述符_减引用 用 p。
        完。
        j 为 j 加 1。
    完。
    若 阶 等于 0 并且 迁移类型 小于 MIGRATE_PCPTYPES：
        做 PCP_释放 用 z、页号、迁移类型。
        全局空闲页数 为 全局空闲页数 加 1。
        z 的 释放次数 为 z 的 释放次数 加 1。
        返还。
    完。
    做 自旋锁_获取 用 z 的 锁。
    做 __伙伴_放回 用 z、页号、阶、迁移类型。
    做 自旋锁_释放 用 z 的 锁。
    全局空闲页数 为 全局空闲页数 加 分配大小。
    z 的 释放次数 为 z 的 释放次数 加 1。
完。

alloc_pages 接受 阶、gfp：
    页号 为 做 伙伴系统_分配_带标志 用 阶、gfp。
    若 页号 小于 0：
        返还 0。
    完。
    返还 做 物理到虚拟 用 做 页号到地址 用 页号。
完。

free_pages 接受 虚拟地址、阶：
    物理地址 为 做 虚拟到物理 用 虚拟地址。
    页号 为 做 地址到页号 用 物理地址。
    做 伙伴系统_释放 用 页号、阶。
完。

alloc_page 接受 gfp：
    返还 做 alloc_pages 用 0、gfp。
完。

free_page 接受 虚拟地址：
    做 free_pages 用 虚拟地址、0。
完。

__get_free_page 接受 gfp：
    返还 做 alloc_page 用 gfp。
完。

__get_free_pages 接受 gfp、阶：
    返还 做 alloc_pages 用 阶、gfp。
完。

get_zeroed_page 接受 gfp：
    返还 做 alloc_page 用 gfp 位或 __GFP_ZERO。
完。

伙伴系统_各阶空闲数 接受 零参：
    可变 结果 为 一组：（空）。
    可变 阶 为 0。
    只要 阶 小于或等于 最大阶：
        可变 阶总计 为 0。
        可变 i 为 0。
        只要 i 小于 活跃zone数：
            z 为 所有zone 的 第 zone搜索顺序 的 第 i 项 项。
            若 z 的 已初始化：
                阶总计 为 阶总计 加 z 的 空闲区 的 第 阶 项 的 空闲数。
            完。
            i 为 i 加 1。
        完。
        做 追加 用 结果、阶总计。
        阶 为 阶 加 1。
    完。
    返还 结果。
完。

伙伴系统_碎片指数 接受 零参：
    若 全局空闲页数 等于 0：
        返还 1000。
    完。
    各阶 为 做 伙伴系统_各阶空闲数。
    可变 大块页 为 0。
    可变 i 为 4。
    只要 i 小于或等于 最大阶：
        大块页 为 大块页 加 各阶 的 第 i 项 乘 (1 左移 i)。
        i 为 i 加 1。
    完。
    返还 1000 减 大块页 乘 1000 除以 全局空闲页数。
完。

伙伴系统_统计 接受 零参：
    返还 一组：全局总页数、全局空闲页数、全局总页数 减 全局空闲页数。
完。

伙伴系统_打印状态 接受 零参：
    做 内核信息 用 「═══ 伙伴系统状态 ═══」。
    做 内核信息数值 用 「总页数」、全局总页数。
    做 内核信息数值 用 「空闲页数」、全局空闲页数。
    做 内核信息数值 用 「碎片指数」、做 伙伴系统_碎片指数。
    可变 i 为 0。
    只要 i 小于 活跃zone数：
        z 为 所有zone 的 第 zone搜索顺序 的 第 i 项 项。
        若 非 z 的 已初始化：
            i 为 i 加 1。
            继续。
        完。
        做 内核信息 用 做 字符串拼接 用 「─── zone: 」、z 的 名称、「 ───」。
        做 内核信息数值 用 「  管理页数」、z 的 管理页数。
        做 内核信息数值 用 「  空闲页数」、做 zone_空闲页数 用 z。
        做 内核信息数值 用 「  水位MIN」、z 的 水位线 的 第 WMARK_MIN 项。
        做 内核信息数值 用 「  水位LOW」、z 的 水位线 的 第 WMARK_LOW 项。
        做 内核信息数值 用 「  水位HIGH」、z 的 水位线 的 第 WMARK_HIGH 项。
        做 内核信息数值 用 「  分配次数」、z 的 分配次数。
        做 内核信息数值 用 「  PCP命中」、z 的 PCP命中。
        做 内核信息数值 用 「  回退次数」、z 的 回退次数。
        做 内核信息数值 用 「  合并次数」、z 的 合并次数。
        可变 阶 为 0。
        只要 阶 小于或等于 最大阶：
            做 内核信息数值 用 做 字符串拼接 用 「  阶」、做 整数转字符串 用 阶、z 的 空闲区 的 第 阶 项 的 空闲数。
            阶 为 阶 加 1。
        完。
        i 为 i 加 1。
    完。
完。

伙伴_检查水位 接受 水位：
    z 为 所有zone 的 第 ZONE_NORMAL 项。
    若 非 z 的 已初始化：
        z 为 所有zone 的 第 ZONE_直传 项。
    完。
    返还 做 zone_水位OK 用 z、0、水位、ZONE_NORMAL。
完。

分配页面 接受 gfp标志：
    返还 做 alloc_page 用 gfp标志。
完。

页面地址 接受 页：
    若 页 等于 空：
        返还 0。
    完。
    返还 页。
完。

可变 伙伴释放_已初始化 为 假。

可变 释放_总次数 为 0。

可变 释放_合并次数 为 0。

可变 释放_0阶次数 为 0。

可变 释放_高阶次数 为 0。

可变 释放_错误数 为 0。

可变 释放_延迟总计 为 0。

可变 释放_延迟样本 为 0。

可变 释放_最大延迟 为 0。

可变 释放_速率 为 0。

可变 释放_速率窗口 为 0。

可变 释放_速率时刻 为 0。

可变 释放_峰值速率 为 0。

可变 分配_总次数 为 0。

可变 分配_失败次数 为 0。

可变 分配_0阶次数 为 0。

可变 分配_高阶次数 为 0。

可变 分配_回退次数 为 0。

伙伴释放_记录释放 接受 阶：
    释放_总次数 为 释放_总次数 加 1。
    释放_速率窗口 为 释放_速率窗口 加 1。
    若 阶 等于 0：
        释放_0阶次数 为 释放_0阶次数 加 1。
    否则：
        释放_高阶次数 为 释放_高阶次数 加 1。
    完。
完。

伙伴释放_记录合并 接受 零参：
    释放_合并次数 为 释放_合并次数 加 1。
完。

伙伴释放_记录分配 接受 阶、成功：
    分配_总次数 为 分配_总次数 加 1。
    若 成功 等于 假：
        分配_失败次数 为 分配_失败次数 加 1。
    完。
    若 阶 等于 0：
        分配_0阶次数 为 分配_0阶次数 加 1。
    否则：
        分配_高阶次数 为 分配_高阶次数 加 1。
    完。
完。

伙伴释放_记录延迟 接受 延迟：
    释放_延迟样本 为 释放_延迟样本 加 1。
    释放_延迟总计 为 释放_延迟总计 加 延迟。
    若 延迟 大于 释放_最大延迟：
        释放_最大延迟 为 延迟。
    完。
完。

伙伴释放_平均延迟 接受 零参：
    若 释放_延迟样本 等于 0：
        返还 0。
    完。
    返还 释放_延迟总计 除以 释放_延迟样本。
完。

伙伴释放_更新速率 接受 零参：
    可变 现在 为 0。
    汇编。
    块：
        「rdtsc」。
        「mov [rbp-8], rax」。
    完。
    间隔 为 现在 减 释放_速率时刻。
    若 间隔 大于或等于 1000000000：
        释放_速率 为 释放_速率窗口 乘 1000000000 除以 间隔。
        若 释放_速率 大于 释放_峰值速率：
            释放_峰值速率 为 释放_速率。
        完。
        释放_速率窗口 为 0。
        释放_速率时刻 为 现在。
    完。
完。

伙伴释放_初始化 接受 零参：
    若 伙伴释放_已初始化 等于 真：
        返还 0 减 17。
    完。
    伙伴释放_已初始化 为 真。
    做 打印 用 「[伙伴释放] 初始化完成」。
    返还 0。
完。

伙伴_碎片指数_计算 接受 零参：
    若 分配_总次数 等于 0：
        返还 0。
    完。
    返还 分配_失败次数 乘 100 除以 分配_总次数。
完。

伙伴_合并率 接受 零参：
    若 释放_总次数 等于 0：
        返还 0。
    完。
    返还 释放_合并次数 乘 100 除以 释放_总次数。
完。

伙伴释放_完整统计 接受 零参：
    统计 为 做 字典。
    做 字典设 用 统计、「已初始化」、伙伴释放_已初始化。
    做 字典设 用 统计、「释放总次数」、释放_总次数。
    做 字典设 用 统计、「释放合并次数」、释放_合并次数。
    做 字典设 用 统计、「释放0阶」、释放_0阶次数。
    做 字典设 用 统计、「释放高阶」、释放_高阶次数。
    做 字典设 用 统计、「分配总次数」、分配_总次数。
    做 字典设 用 统计、「分配失败」、分配_失败次数。
    做 字典设 用 统计、「分配0阶」、分配_0阶次数。
    做 字典设 用 统计、「分配高阶」、分配_高阶次数。
    做 字典设 用 统计、「分配回退」、分配_回退次数。
    做 字典设 用 统计、「碎片指数」、做 伙伴_碎片指数_计算。
    做 字典设 用 统计、「合并率(%)」、做 伙伴_合并率。
    做 字典设 用 统计、「平均延迟(ns)」、做 伙伴释放_平均延迟。
    做 字典设 用 统计、「最大延迟(ns)」、释放_最大延迟。
    做 字典设 用 统计、「速率」、释放_速率。
    做 字典设 用 统计、「峰值速率」、释放_峰值速率。
    做 字典设 用 统计、「错误数」、释放_错误数。
    返还 统计。
完。

伙伴释放_打印统计 接受 零参：
    统计 为 做 伙伴释放_完整统计。
    做 打印 用 「[伙伴释放] ═══════ 完整统计 ═══════」。
    做 打印 用 「  释放总数:     」 加 做 转字符串 用 做 字典取 用 统计、「释放总次数」。
    做 打印 用 「  释放合并:     」 加 做 转字符串 用 做 字典取 用 统计、「释放合并次数」。
    做 打印 用 「  释放0阶:      」 加 做 转字符串 用 做 字典取 用 统计、「释放0阶」。
    做 打印 用 「  释放高阶:     」 加 做 转字符串 用 做 字典取 用 统计、「释放高阶」。
    做 打印 用 「  分配总数:     」 加 做 转字符串 用 做 字典取 用 统计、「分配总次数」。
    做 打印 用 「  分配失败:     」 加 做 转字符串 用 做 字典取 用 统计、「分配失败」。
    做 打印 用 「  碎片指数:     」 加 做 转字符串 用 做 字典取 用 统计、「碎片指数」。
    做 打印 用 「  合并率:       」 加 做 转字符串 用 做 字典取 用 统计、「合并率(%)」 加 「%」。
    做 打印 用 「  平均延迟:     」 加 做 转字符串 用 做 字典取 用 统计、「平均延迟(ns)」 加 「ns」。
    做 打印 用 「  速率:         」 加 做 转字符串 用 做 字典取 用 统计、「速率」 加 「 次/秒」。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[伙伴释放] ═════════════════════════」。
    返还 0。
完。

伙伴释放_dump 接受 零参：
    做 打印 用 「[伙伴释放 dump] ──────────────────」。
    做 打印 用 「  释放: 」 加 做 转字符串 用 释放_总次数 加 「 (合并」 加 做 转字符串 用 释放_合并次数 加 「)」。
    做 打印 用 「  分配: 」 加 做 转字符串 用 分配_总次数 加 「 (失败」 加 做 转字符串 用 分配_失败次数 加 「)」。
    做 打印 用 「  碎片: 」 加 做 转字符串 用 做 伙伴_碎片指数_计算。
    做 打印 用 「  速率: 」 加 做 转字符串 用 释放_速率。
    做 打印 用 「[伙伴释放 dump] ──────────────────」。
完。

伙伴释放_销毁 接受 零参：
    伙伴释放_已初始化 为 假。
    返还 0。
完。

伙伴释放_为内存区域提供分配统计 接受 零参：
    返还 一组：分配_总次数、分配_失败次数、释放_总次数、释放_合并次数。
完。

伙伴释放_为AI提供碎片模式 接受 零参：
    返还 一组：做 伙伴_碎片指数_计算、做 伙伴_合并率、分配_高阶次数、分配_失败次数。
完。

伙伴释放_为回收提供压力 接受 零参：
    返还 一组：分配_失败次数、释放_速率、释放_峰值速率。
完。

伙伴释放_为procfs提供buddyinfo 接受 零参：
    返还 一组：分配_总次数、释放_总次数、释放_合并次数、分配_失败次数、做 伙伴_碎片指数_计算。
完。

伙伴释放_重置统计 接受 零参：
    释放_总次数 为 0。
    释放_合并次数 为 0。
    释放_0阶次数 为 0。
    释放_高阶次数 为 0。
    分配_总次数 为 0。
    分配_失败次数 为 0。
    分配_0阶次数 为 0。
    分配_高阶次数 为 0。
    分配_回退次数 为 0。
    释放_错误数 为 0。
    释放_延迟总计 为 0。
    释放_延迟样本 为 0。
    释放_最大延迟 为 0。
    释放_速率 为 0。
    释放_速率窗口 为 0。
    释放_峰值速率 为 0。
    做 打印 用 「[伙伴释放] 统计已重置」。
    返还 0。
完。

伙伴释放_为电源管理挂起 接受 零参：
    可变 挂起页数 为 全局空闲页数。
    全局_伙伴冻结 为 真。
    返还 挂起页数。
完。

伙伴释放_为调度器提供内存压力 接受 零参：
    返还 一组：释放_速率、分配_失败次数、做 伙伴_碎片指数_计算。
完。

本章 收束。
