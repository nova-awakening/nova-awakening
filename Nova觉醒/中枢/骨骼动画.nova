《骨骼动画》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·字符串替换》。

引用《..·执行·控制台输出》。

最大骨骼数 为 32。

最大关键帧数 为 128。

最大动画数 为 16。

帧率 为 60。

呼吸周期帧 为 180。

眨眼周期帧 为 240。

眨眼持续帧 为 5。

过渡混合帧数 为 15。

TTS_采样率 为 22050。

TTS_最大文本 为 1024。

TTS_缓冲大小 为 65536。

嘴型循环帧 为 12。

嘴型A帧数 为 4。

嘴型O帧数 为 4。

正弦缩放 为 1000。

动画_待命呼吸 为 0。

动画_说话手势 为 1。

动画_指向 为 2。

动画_点头 为 3。

动画_摇头 为 4。

动画_挥手 为 5。

动画_思考 为 6。

动画_鞠躬 为 7。

动画_数量 为 8。

骨骼_根 为 0。

骨骼_骨盆 为 1。

骨骼_脊柱下 为 2。

骨骼_脊柱中 为 3。

骨骼_脊柱上 为 4。

骨骼_颈 为 5。

骨骼_头 为 6。

骨骼_左肩 为 7。

骨骼_左上臂 为 8。

骨骼_左前臂 为 9。

骨骼_左手 为 10。

骨骼_右肩 为 11。

骨骼_右上臂 为 12。

骨骼_右前臂 为 13。

骨骼_右手 为 14。

骨骼_左髋 为 15。

骨骼_左大腿 为 16。

骨骼_左小腿 为 17。

骨骼_左脚 为 18。

骨骼_右髋 为 19。

骨骼_右大腿 为 20。

骨骼_右小腿 为 21。

骨骼_右脚 为 22。

骨骼总数 为 23。

向量3 是 一种 事物，含 x、y、z。

骨骼 是 一种 事物，含 编号、父骨骼、本地位置、本地旋转、世界位置、世界旋转、长度。

关键帧 是 一种 事物，含 时间帧、骨骼编号、位置偏移x、位置偏移y、位置偏移z、旋转角度、旋转轴。

动画片段 是 一种 事物，含 编号、名称、帧列表、总帧数、循环、速度。

动画播放器 是 一种 事物，含 当前动画、前一动画、当前帧、混合进度、播放中、暂停、速度倍率。

TTS引擎 是 一种 事物，含 当前文本、当前位置、总长度、音频缓冲、音量、语速、音调、正在播放、合成次数。

动画统计 是 一种 事物，含 动画播放次数、动画切换次数、帧更新次数、插值计算次数、混合计算次数、呼吸循环次数、眨眼次数、TTS合成次数、TTS播放帧数、嘴型切换次数、AI手势选择次数、AI手势命中次数、骨骼更新次数、过渡完成次数、错误次数、暂停次数、恢复次数。

可变 骨骼列表 为 一组：（空）。

可变 动画库 为 一组：（空）。

可变 播放器 为 空。

可变 全局TTS 为 空。

可变 已初始化 为 假。

可变 全局统计 为 新建 动画统计。

骨骼动画_初始化 接受 零参：
    若 已初始化 等于 真：
        返还 错误_已存在。
    完。
    骨骼列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 骨骼总数：
        可变 骨 为 新建 骨骼。
        骨 的 编号 为 i。
        骨 的 父骨骼 为 0 减 1。
        骨 的 长度 为 100。
        做 追加 用 骨骼列表、骨。
        i 为 i 加 1。
    完。
    骨骼列表 的 第 骨骼_骨盆 项 的 父骨骼 为 骨骼_根。
    骨骼列表 的 第 骨骼_脊柱下 项 的 父骨骼 为 骨骼_骨盆。
    骨骼列表 的 第 骨骼_脊柱中 项 的 父骨骼 为 骨骼_脊柱下。
    骨骼列表 的 第 骨骼_脊柱上 项 的 父骨骼 为 骨骼_脊柱中。
    骨骼列表 的 第 骨骼_颈 项 的 父骨骼 为 骨骼_脊柱上。
    骨骼列表 的 第 骨骼_头 项 的 父骨骼 为 骨骼_颈。
    骨骼列表 的 第 骨骼_左肩 项 的 父骨骼 为 骨骼_脊柱上。
    骨骼列表 的 第 骨骼_左上臂 项 的 父骨骼 为 骨骼_左肩。
    骨骼列表 的 第 骨骼_左前臂 项 的 父骨骼 为 骨骼_左上臂。
    骨骼列表 的 第 骨骼_左手 项 的 父骨骼 为 骨骼_左前臂。
    骨骼列表 的 第 骨骼_右肩 项 的 父骨骼 为 骨骼_脊柱上。
    骨骼列表 的 第 骨骼_右上臂 项 的 父骨骼 为 骨骼_右肩。
    骨骼列表 的 第 骨骼_右前臂 项 的 父骨骼 为 骨骼_右上臂。
    骨骼列表 的 第 骨骼_右手 项 的 父骨骼 为 骨骼_右前臂。
    骨骼列表 的 第 骨骼_左髋 项 的 父骨骼 为 骨骼_骨盆。
    骨骼列表 的 第 骨骼_左大腿 项 的 父骨骼 为 骨骼_左髋。
    骨骼列表 的 第 骨骼_左小腿 项 的 父骨骼 为 骨骼_左大腿。
    骨骼列表 的 第 骨骼_左脚 项 的 父骨骼 为 骨骼_左小腿。
    骨骼列表 的 第 骨骼_右髋 项 的 父骨骼 为 骨骼_骨盆。
    骨骼列表 的 第 骨骼_右大腿 项 的 父骨骼 为 骨骼_右髋。
    骨骼列表 的 第 骨骼_右小腿 项 的 父骨骼 为 骨骼_右大腿。
    骨骼列表 的 第 骨骼_右脚 项 的 父骨骼 为 骨骼_右小腿。
    骨骼列表 的 第 骨骼_脊柱下 项 的 长度 为 200。
    骨骼列表 的 第 骨骼_脊柱中 项 的 长度 为 200。
    骨骼列表 的 第 骨骼_脊柱上 项 的 长度 为 150。
    骨骼列表 的 第 骨骼_颈 项 的 长度 为 80。
    骨骼列表 的 第 骨骼_头 项 的 长度 为 120。
    骨骼列表 的 第 骨骼_左上臂 项 的 长度 为 250。
    骨骼列表 的 第 骨骼_左前臂 项 的 长度 为 230。
    骨骼列表 的 第 骨骼_左手 项 的 长度 为 100。
    骨骼列表 的 第 骨骼_右上臂 项 的 长度 为 250。
    骨骼列表 的 第 骨骼_右前臂 项 的 长度 为 230。
    骨骼列表 的 第 骨骼_右手 项 的 长度 为 100。
    骨骼列表 的 第 骨骼_左大腿 项 的 长度 为 400。
    骨骼列表 的 第 骨骼_左小腿 项 的 长度 为 380。
    骨骼列表 的 第 骨骼_右大腿 项 的 长度 为 400。
    骨骼列表 的 第 骨骼_右小腿 项 的 长度 为 380。
    动画库 为 一组：（空）。
    做 骨骼动画_注册预设动画。
    可变 p 为 新建 动画播放器。
    p 的 当前动画 为 动画_待命呼吸。
    p 的 前一动画 为 动画_待命呼吸。
    p 的 当前帧 为 0。
    p 的 混合进度 为 0。
    p 的 播放中 为 真。
    p 的 暂停 为 假。
    p 的 速度倍率 为 1000。
    播放器 为 p。
    可变 tts 为 新建 TTS引擎。
    tts 的 音频缓冲 为 一组：（空）。
    tts 的 音量 为 800。
    tts 的 语速 为 1000。
    tts 的 音调 为 1000。
    tts 的 正在播放 为 假。
    tts 的 合成次数 为 0。
    tts 的 当前文本 为 「」。
    tts 的 当前位置 为 0。
    tts 的 总长度 为 0。
    全局TTS 为 tts。
    已初始化 为 真。
    做 打印 用 「[骨骼动画] 初始化完成, 骨骼数: 23, 动画数: 8」。
    返还 0。
完。

骨骼动画_销毁 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    骨骼列表 为 一组：（空）。
    动画库 为 一组：（空）。
    播放器 为 空。
    全局TTS 为 空。
    已初始化 为 假。
    做 打印 用 「[骨骼动画] 已销毁」。
    返还 0。
完。

骨骼动画_注册预设动画 接受 零参：
    可变 idx 为 0。
    只要 idx 小于 动画_数量：
        可变 片段 为 新建 动画片段。
        片段 的 编号 为 idx。
        片段 的 帧列表 为 一组：（空）。
        片段 的 速度 为 1000。
        若 idx 等于 动画_待命呼吸：
            片段 的 名称 为 「待命呼吸」。
            片段 的 总帧数 为 呼吸周期帧。
            片段 的 循环 为 真。
        否则若 idx 等于 动画_说话手势：
            片段 的 名称 为 「说话手势」。
            片段 的 总帧数 为 60。
            片段 的 循环 为 真。
        否则若 idx 等于 动画_指向：
            片段 的 名称 为 「指向」。
            片段 的 总帧数 为 30。
            片段 的 循环 为 假。
        否则若 idx 等于 动画_点头：
            片段 的 名称 为 「点头」。
            片段 的 总帧数 为 24。
            片段 的 循环 为 假。
        否则若 idx 等于 动画_摇头：
            片段 的 名称 为 「摇头」。
            片段 的 总帧数 为 30。
            片段 的 循环 为 假。
        否则若 idx 等于 动画_挥手：
            片段 的 名称 为 「挥手」。
            片段 的 总帧数 为 36。
            片段 的 循环 为 假。
        否则若 idx 等于 动画_思考：
            片段 的 名称 为 「思考」。
            片段 的 总帧数 为 48。
            片段 的 循环 为 真。
        否则若 idx 等于 动画_鞠躬：
            片段 的 名称 为 「鞠躬」。
            片段 的 总帧数 为 40。
            片段 的 循环 为 假。
        完。
        做 追加 用 动画库、片段。
        idx 为 idx 加 1。
    完。
完。

动画_播放 接受 动画ID：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    若 动画ID 小于 0 或者 动画ID 大于或等于 动画_数量：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    若 播放器 的 当前动画 不等于 动画ID：
        播放器 的 前一动画 为 播放器 的 当前动画。
        播放器 的 混合进度 为 0。
        全局统计 的 动画切换次数 为 全局统计 的 动画切换次数 加 1。
    完。
    播放器 的 当前动画 为 动画ID。
    播放器 的 当前帧 为 0。
    播放器 的 播放中 为 真。
    播放器 的 暂停 为 假。
    全局统计 的 动画播放次数 为 全局统计 的 动画播放次数 加 1。
    返还 0。
完。

动画_暂停 接受 零参：
    若 播放器 等于 空：
        返还 错误_未初始化。
    完。
    播放器 的 暂停 为 真。
    全局统计 的 暂停次数 为 全局统计 的 暂停次数 加 1。
    返还 0。
完。

动画_恢复 接受 零参：
    若 播放器 等于 空：
        返还 错误_未初始化。
    完。
    播放器 的 暂停 为 假。
    全局统计 的 恢复次数 为 全局统计 的 恢复次数 加 1。
    返还 0。
完。

动画_停止 接受 零参：
    若 播放器 等于 空：
        返还 错误_未初始化。
    完。
    播放器 的 播放中 为 假。
    播放器 的 当前帧 为 0。
    返还 0。
完。

动画_设置速度 接受 倍率：
    若 播放器 等于 空：
        返还 错误_未初始化。
    完。
    若 倍率 小于 100：
        倍率 为 100。
    完。
    若 倍率 大于 5000：
        倍率 为 5000。
    完。
    播放器 的 速度倍率 为 倍率。
    返还 0。
完。

正弦近似 接受 角度：
    可变 a 为 角度 取余于 3600。
    若 a 小于 0：
        a 为 a 加 3600。
    完。
    可变 符号 为 1。
    若 a 大于或等于 1800：
        a 为 a 减 1800。
        符号 为 0 减 1。
    完。
    若 a 大于 900：
        a 为 1800 减 a。
    完。
    x 为 a 除以 10。
    积 为 x 乘 (180 减 x)。
    分子 为 4 乘 积。
    分母 为 40500 减 积。
    若 分母 等于 0：
        返还 0。
    完。
    返还 符号 乘 分子 乘 正弦缩放 除以 分母。
完。

插值_线性 接受 a、b、t：
    返还 a 乘 (1000 减 t) 除以 1000 加 b 乘 t 除以 1000。
完。

动画_更新 接受 助手：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    若 助手 等于 空：
        返还 错误_无效参数。
    完。
    若 播放器 等于 空：
        返还 错误_未初始化。
    完。
    若 播放器 的 暂停 等于 真：
        返还 0。
    完。
    全局统计 的 帧更新次数 为 全局统计 的 帧更新次数 加 1。
    助手 的 动画时钟 为 助手 的 动画时钟 加 1。
    若 播放器 的 播放中 等于 真：
        播放器 的 当前帧 为 播放器 的 当前帧 加 1。
        片段 为 动画库 的 第 播放器 的 当前动画 项。
        若 播放器 的 当前帧 大于或等于 片段 的 总帧数：
            若 片段 的 循环 等于 真：
                播放器 的 当前帧 为 0。
            否则：
                播放器 的 播放中 为 假。
                做 动画_播放 用 动画_待命呼吸。
            完。
        完。
    完。
    若 播放器 的 混合进度 小于 过渡混合帧数：
        播放器 的 混合进度 为 播放器 的 混合进度 加 1。
        全局统计 的 混合计算次数 为 全局统计 的 混合计算次数 加 1。
        若 播放器 的 混合进度 大于或等于 过渡混合帧数：
            全局统计 的 过渡完成次数 为 全局统计 的 过渡完成次数 加 1。
        完。
    完。
    做 骨骼动画_应用姿态 用 助手。
    做 骨骼动画_呼吸 用 助手。
    做 骨骼动画_眨眼 用 助手。
    返还 0。
完。

骨骼动画_应用姿态 接受 助手：
    若 播放器 等于 空：
        返还。
    完。
    动画 为 播放器 的 当前动画。
    帧 为 播放器 的 当前帧。
    全局统计 的 骨骼更新次数 为 全局统计 的 骨骼更新次数 加 1。
    若 动画 等于 动画_待命呼吸：
        呼吸幅度 为 做 正弦近似 用 帧 乘 100 乘 5 除以 正弦缩放。
        骨骼列表 的 第 骨骼_脊柱中 项 的 本地位置 为 呼吸幅度。
    否则若 动画 等于 动画_说话手势：
        手摆 为 做 正弦近似 用 帧 乘 300 乘 200 除以 正弦缩放。
        骨骼列表 的 第 骨骼_右上臂 项 的 本地旋转 为 0 减 450 加 手摆。
        骨骼列表 的 第 骨骼_右前臂 项 的 本地旋转 为 0 减 300 加 手摆 除以 2。
    否则若 动画 等于 动画_指向：
        展开 为 帧 乘 1000 除以 30。
        若 展开 大于 1000：
            展开 为 1000。
        完。
        骨骼列表 的 第 骨骼_右上臂 项 的 本地旋转 为 做 插值_线性 用 0、0 减 900、展开。
        骨骼列表 的 第 骨骼_右前臂 项 的 本地旋转 为 0。
        骨骼列表 的 第 骨骼_右手 项 的 本地旋转 为 0。
    否则若 动画 等于 动画_点头：
        点 为 做 正弦近似 用 帧 乘 300 乘 150 除以 正弦缩放。
        骨骼列表 的 第 骨骼_头 项 的 本地旋转 为 点。
    否则若 动画 等于 动画_摇头：
        摇 为 做 正弦近似 用 帧 乘 240 乘 200 除以 正弦缩放。
        骨骼列表 的 第 骨骼_颈 项 的 本地旋转 为 摇。
    否则若 动画 等于 动画_挥手：
        举 为 帧 乘 1000 除以 10。
        若 举 大于 1000：
            举 为 1000。
        完。
        骨骼列表 的 第 骨骼_右上臂 项 的 本地旋转 为 做 插值_线性 用 0、0 减 1500、举。
        摆 为 做 正弦近似 用 帧 乘 500 乘 300 除以 正弦缩放。
        骨骼列表 的 第 骨骼_右前臂 项 的 本地旋转 为 摆。
    否则若 动画 等于 动画_思考：
        骨骼列表 的 第 骨骼_右上臂 项 的 本地旋转 为 0 减 600。
        骨骼列表 的 第 骨骼_右前臂 项 的 本地旋转 为 0 减 900。
        微动 为 做 正弦近似 用 帧 乘 50 乘 20 除以 正弦缩放。
        骨骼列表 的 第 骨骼_头 项 的 本地旋转 为 0 减 50 加 微动。
    否则若 动画 等于 动画_鞠躬：
        鞠 为 帧 乘 1000 除以 20。
        若 鞠 大于 1000：
            鞠 为 1000。
        完。
        若 帧 大于 20：
            鞠 为 (40 减 帧) 乘 1000 除以 20。
        完。
        若 鞠 小于 0：
            鞠 为 0。
        完。
        骨骼列表 的 第 骨骼_脊柱上 项 的 本地旋转 为 做 插值_线性 用 0、450、鞠。
        骨骼列表 的 第 骨骼_颈 项 的 本地旋转 为 做 插值_线性 用 0、200、鞠。
    完。
    全局统计 的 插值计算次数 为 全局统计 的 插值计算次数 加 1。
完。

骨骼动画_呼吸 接受 助手：
    若 播放器 的 当前动画 不等于 动画_待命呼吸：
        返还。
    完。
    若 助手 的 动画时钟 取余于 呼吸周期帧 等于 0：
        全局统计 的 呼吸循环次数 为 全局统计 的 呼吸循环次数 加 1。
    完。
完。

骨骼动画_眨眼 接受 助手：
    若 助手 的 动画时钟 取余于 眨眼周期帧 等于 0：
        全局统计 的 眨眼次数 为 全局统计 的 眨眼次数 加 1。
    完。
完。

AI_选择手势 接受 意图、情绪值：
    全局统计 的 AI手势选择次数 为 全局统计 的 AI手势选择次数 加 1。
    若 意图 等于 1：
        全局统计 的 AI手势命中次数 为 全局统计 的 AI手势命中次数 加 1。
        返还 动画_挥手。
    完。
    若 意图 等于 2：
        全局统计 的 AI手势命中次数 为 全局统计 的 AI手势命中次数 加 1。
        返还 动画_思考。
    完。
    若 意图 等于 3：
        全局统计 的 AI手势命中次数 为 全局统计 的 AI手势命中次数 加 1。
        返还 动画_指向。
    完。
    若 意图 等于 4：
        返还 动画_指向。
    完。
    若 意图 等于 8：
        返还 动画_鞠躬。
    完。
    若 情绪值 大于 700：
        返还 动画_挥手。
    完。
    若 情绪值 小于 200：
        返还 动画_鞠躬。
    完。
    返还 动画_说话手势。
完。

AI_评估手势适配度 接受 动画ID、意图：
    若 意图 等于 1 并且 动画ID 等于 动画_挥手：
        返还 95。
    完。
    若 意图 等于 2 并且 动画ID 等于 动画_思考：
        返还 90。
    完。
    若 意图 等于 3 并且 动画ID 等于 动画_指向：
        返还 85。
    完。
    若 动画ID 等于 动画_说话手势：
        返还 60。
    完。
    若 动画ID 等于 动画_待命呼吸：
        返还 30。
    完。
    返还 50。
完。

TTS_初始化 接受 零参：
    若 全局TTS 不等于 空：
        返还 0。
    完。
    可变 tts 为 新建 TTS引擎。
    tts 的 音频缓冲 为 一组：（空）。
    tts 的 音量 为 800。
    tts 的 语速 为 1000。
    tts 的 音调 为 1000。
    tts 的 正在播放 为 假。
    tts 的 合成次数 为 0。
    tts 的 当前文本 为 「」。
    tts 的 当前位置 为 0。
    tts 的 总长度 为 0。
    全局TTS 为 tts。
    做 打印 用 做 字符串拼接 用 「[TTS] 初始化 采样率=」、做 转字符串 用 TTS_采样率。
    返还 0。
完。

TTS_合成 接受 文本：
    若 全局TTS 等于 空：
        做 TTS_初始化。
    完。
    若 文本 等于 空：
        返还 错误_无效参数。
    完。
    全局TTS 的 当前文本 为 文本。
    全局TTS 的 当前位置 为 0。
    全局TTS 的 总长度 为 做 字节长度 用 文本。
    全局TTS 的 音频缓冲 为 一组：（空）。
    全局TTS 的 正在播放 为 真。
    全局TTS 的 合成次数 为 全局TTS 的 合成次数 加 1。
    全局统计 的 TTS合成次数 为 全局统计 的 TTS合成次数 加 1。
    做 打印 用 做 字符串拼接 用 「[TTS] 合成: 」、文本。
    返还 0。
完。

TTS_说话 接受 助手、文本、语速：
    若 助手 等于 空 或者 文本 等于 空：
        返还 错误_无效参数。
    完。
    若 语速 大于 0：
        若 全局TTS 不等于 空：
            全局TTS 的 语速 为 语速。
        完。
    完。
    做 TTS_合成 用 文本。
    返还 0。
完。

TTS_设置音量 接受 音量：
    若 全局TTS 等于 空：
        返还 错误_未初始化。
    完。
    若 音量 小于 0：
        音量 为 0。
    完。
    若 音量 大于 1000：
        音量 为 1000。
    完。
    全局TTS 的 音量 为 音量。
    返还 0。
完。

TTS_设置音调 接受 音调：
    若 全局TTS 等于 空：
        返还 错误_未初始化。
    完。
    若 音调 小于 500：
        音调 为 500。
    完。
    若 音调 大于 2000：
        音调 为 2000。
    完。
    全局TTS 的 音调 为 音调。
    返还 0。
完。

TTS_停止 接受 零参：
    若 全局TTS 等于 空：
        返还 0。
    完。
    全局TTS 的 正在播放 为 假。
    全局TTS 的 当前位置 为 0。
    返还 0。
完。

TTS_是否播放中 接受 零参：
    若 全局TTS 等于 空：
        返还 假。
    完。
    返还 全局TTS 的 正在播放。
完。

TTS_更新 接受 助手：
    若 助手 等于 空：
        返还 错误_无效参数。
    完。
    若 全局TTS 等于 空 或者 全局TTS 的 正在播放 等于 假：
        返还 0。
    完。
    全局统计 的 TTS播放帧数 为 全局统计 的 TTS播放帧数 加 1。
    全局TTS 的 当前位置 为 全局TTS 的 当前位置 加 1。
    预计帧数 为 全局TTS 的 总长度 乘 2。
    若 全局TTS 的 当前位置 大于或等于 预计帧数：
        全局TTS 的 正在播放 为 假。
        返还 0。
    完。
    嘴型循环 为 全局TTS 的 当前位置 取余于 嘴型循环帧。
    若 嘴型循环 等于 0 或者 嘴型循环 等于 嘴型A帧数 或者 嘴型循环 等于 嘴型A帧数 加 嘴型O帧数：
        全局统计 的 嘴型切换次数 为 全局统计 的 嘴型切换次数 加 1。
    完。
    返还 0。
完。

获取骨骼位置 接受 骨骼编号：
    若 骨骼编号 小于 0 或者 骨骼编号 大于或等于 骨骼总数：
        返还 空。
    完。
    返还 骨骼列表 的 第 骨骼编号 项 的 本地位置。
完。

获取骨骼旋转 接受 骨骼编号：
    若 骨骼编号 小于 0 或者 骨骼编号 大于或等于 骨骼总数：
        返还 0。
    完。
    返还 骨骼列表 的 第 骨骼编号 项 的 本地旋转。
完。

获取骨骼父节点 接受 骨骼编号：
    若 骨骼编号 小于 0 或者 骨骼编号 大于或等于 骨骼总数：
        返还 0 减 1。
    完。
    返还 骨骼列表 的 第 骨骼编号 项 的 父骨骼。
完。

获取动画名称 接受 动画ID：
    若 动画ID 小于 0 或者 动画ID 大于或等于 做 计数 用 动画库：
        返还 「未知」。
    完。
    返还 动画库 的 第 动画ID 项 的 名称。
完。

获取当前动画 接受 零参：
    若 播放器 等于 空：
        返还 动画_待命呼吸。
    完。
    返还 播放器 的 当前动画。
完。

获取当前帧 接受 零参：
    若 播放器 等于 空：
        返还 0。
    完。
    返还 播放器 的 当前帧。
完。

骨骼动画_转储状态 接受 零参：
    做 打印 用 「[骨骼动画] ═══ 状态转储 ═══」。
    若 播放器 不等于 空：
        做 打印 用 做 字符串拼接 用 「  当前动画: 」、做 获取动画名称 用 播放器 的 当前动画。
        做 打印 用 做 字符串拼接 用 「  当前帧: 」、做 转字符串 用 播放器 的 当前帧。
        做 打印 用 做 字符串拼接 用 「  播放中: 」、做 转字符串 用 播放器 的 播放中。
        做 打印 用 做 字符串拼接 用 「  混合进度: 」、做 转字符串 用 播放器 的 混合进度。
    完。
    若 全局TTS 不等于 空：
        做 打印 用 做 字符串拼接 用 「  TTS播放中: 」、做 转字符串 用 全局TTS 的 正在播放。
        做 打印 用 做 字符串拼接 用 「  TTS合成次数: 」、做 转字符串 用 全局TTS 的 合成次数。
    完。
    返还 0。
完。

骨骼动画_打印统计 接受 零参：
    做 打印 用 「[骨骼动画] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  动画播放: 」、做 转字符串 用 全局统计 的 动画播放次数。
    做 打印 用 做 字符串拼接 用 「  动画切换: 」、做 转字符串 用 全局统计 的 动画切换次数。
    做 打印 用 做 字符串拼接 用 「  帧更新: 」、做 转字符串 用 全局统计 的 帧更新次数。
    做 打印 用 做 字符串拼接 用 「  插值计算: 」、做 转字符串 用 全局统计 的 插值计算次数。
    做 打印 用 做 字符串拼接 用 「  混合计算: 」、做 转字符串 用 全局统计 的 混合计算次数。
    做 打印 用 做 字符串拼接 用 「  呼吸循环: 」、做 转字符串 用 全局统计 的 呼吸循环次数。
    做 打印 用 做 字符串拼接 用 「  眨眼次数: 」、做 转字符串 用 全局统计 的 眨眼次数。
    做 打印 用 做 字符串拼接 用 「  TTS合成: 」、做 转字符串 用 全局统计 的 TTS合成次数。
    做 打印 用 做 字符串拼接 用 「  TTS播放帧: 」、做 转字符串 用 全局统计 的 TTS播放帧数。
    做 打印 用 做 字符串拼接 用 「  嘴型切换: 」、做 转字符串 用 全局统计 的 嘴型切换次数。
    做 打印 用 做 字符串拼接 用 「  AI手势选择: 」、做 转字符串 用 全局统计 的 AI手势选择次数。
    做 打印 用 做 字符串拼接 用 「  AI手势命中: 」、做 转字符串 用 全局统计 的 AI手势命中次数。
    做 打印 用 做 字符串拼接 用 「  骨骼更新: 」、做 转字符串 用 全局统计 的 骨骼更新次数。
    做 打印 用 做 字符串拼接 用 「  过渡完成: 」、做 转字符串 用 全局统计 的 过渡完成次数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    做 打印 用 做 字符串拼接 用 「  暂停次数: 」、做 转字符串 用 全局统计 的 暂停次数。
    返还 0。
完。

骨骼动画_注册到内核 接受 内核上下文：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    做 打印 用 「[骨骼动画] 已注册到内核上下文」。
    返还 0。
完。

骨骼动画_查询状态 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

骨骼动画_健康检查 接受 零参：
    若 已初始化 等于 假：
        返还 假。
    完。
    若 播放器 等于 空：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 帧更新次数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
