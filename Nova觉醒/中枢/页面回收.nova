《页面回收》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·感知·内核直连层》。

引用《.·内存初始化》。

引用《.·内容可见性》。

引用《.·物理页描述符》。

水位_高 为 0。

水位_低 为 1。

水位_极低 为 2。

衰减系数 为 922。

基数 为 1024。

老化轮次 为 4。

扫描批量 为 32。

压力历史深 为 16。

回收状态 是 一种 事物，含 唤醒次数、扫描总页、回收总页、直接回收次、老化扫描页、活跃页数、非活跃页数、脏页数、压力值、回收速率、上次唤醒、压力历史、历史写位、神经回收权重。

可变 守护状态 为 0。

可变 运行中 为 假。

回收_初始化 接受 零参：
    守护状态 为 做 堆_分配 用 回收状态。
    守护状态 的 唤醒次数 为 0。
    守护状态 的 扫描总页 为 0。
    守护状态 的 回收总页 为 0。
    守护状态 的 直接回收次 为 0。
    守护状态 的 老化扫描页 为 0。
    守护状态 的 活跃页数 为 0。
    守护状态 的 非活跃页数 为 0。
    守护状态 的 脏页数 为 0。
    守护状态 的 压力值 为 0。
    守护状态 的 回收速率 为 基数 除以 2。
    守护状态 的 上次唤醒 为 0。
    守护状态 的 压力历史 为 做 堆_分配 用 压力历史深 乘 4。
    守护状态 的 历史写位 为 0。
    守护状态 的 神经回收权重 为 512。
    运行中 为 假。
完。

回收_唤醒 接受 水位级别：
    若 守护状态 等于 0：
        返还。
    完。
    守护状态 的 唤醒次数 为 守护状态 的 唤醒次数 加 1。
    守护状态 的 上次唤醒 为 做 平台_获取高精度时间。
    运行中 为 真。
    目标页数 为 扫描批量。
    若 水位级别 等于 水位_极低：
        目标页数 为 扫描批量 乘 4。
    否则若 水位级别 等于 水位_低：
        目标页数 为 扫描批量 乘 2。
    完。
    做 回收_扫描周期 用 目标页数。
    运行中 为 假。
完。

回收_扫描周期 接受 目标：
    可变 已扫描 为 0。
    可变 已回收 为 0。
    可变 轮次 为 0。
    只要 已回收 小于 目标 并且 轮次 小于 老化轮次：
        本轮回收 为 做 回收_扫描非活跃 用 目标 减 已回收。
        已回收 为 已回收 加 本轮回收。
        若 本轮回收 等于 0：
            做 回收_老化活跃页。
        完。
        已扫描 为 已扫描 加 扫描批量。
        轮次 为 轮次 加 1。
    完。
    守护状态 的 扫描总页 为 守护状态 的 扫描总页 加 已扫描。
    守护状态 的 回收总页 为 守护状态 的 回收总页 加 已回收。
    做 回收_更新压力 用 已扫描、已回收。
    做 回收_神经调整 用 已扫描、已回收。
完。

回收_扫描非活跃 接受 目标：
    可变 回收数 为 0。
    可变 扫描数 为 0。
    只要 扫描数 小于 扫描批量 并且 回收数 小于 目标：
        页 为 做 非活跃链表_取头。
        若 页 等于 0：
            跳出。
        完。
        引用位 为 做 页_读引用位 用 页。
        若 引用位 等于 0：
            若 做 页_是脏 用 页：
                做 回写队列_添加 用 页。
                守护状态 的 脏页数 为 守护状态 的 脏页数 加 1。
            否则：
                做 页_释放 用 页。
                回收数 为 回收数 加 1。
                守护状态 的 非活跃页数 为 守护状态 的 非活跃页数 减 1。
            完。
        否则：
            做 页_清引用位 用 页。
            做 活跃链表_加尾 用 页。
            守护状态 的 活跃页数 为 守护状态 的 活跃页数 加 1。
            守护状态 的 非活跃页数 为 守护状态 的 非活跃页数 减 1。
        完。
        扫描数 为 扫描数 加 1。
    完。
    守护状态 的 老化扫描页 为 守护状态 的 老化扫描页 加 扫描数。
    返还 回收数。
完。

回收_老化活跃页 接受 零参：
    可变 降级数 为 0。
    可变 扫描数 为 0。
    只要 扫描数 小于 扫描批量：
        页 为 做 活跃链表_取头。
        若 页 等于 0：
            跳出。
        完。
        引用位 为 做 页_读引用位 用 页。
        若 引用位 等于 0：
            做 非活跃链表_加尾 用 页。
            守护状态 的 活跃页数 为 守护状态 的 活跃页数 减 1。
            守护状态 的 非活跃页数 为 守护状态 的 非活跃页数 加 1。
            降级数 为 降级数 加 1。
        否则：
            做 页_清引用位 用 页。
            做 活跃链表_加尾 用 页。
        完。
        扫描数 为 扫描数 加 1。
    完。
    返还 降级数。
完。

回收_直接回收 接受 需要页数：
    若 守护状态 等于 0：
        返还 0。
    完。
    守护状态 的 直接回收次 为 守护状态 的 直接回收次 加 1。
    可变 已回收 为 做 回收_扫描非活跃 用 需要页数。
    若 已回收 小于 需要页数：
        做 回收_老化活跃页。
        已回收 为 已回收 加 做 回收_扫描非活跃 用 需要页数 减 已回收。
    完。
    返还 已回收。
完。

回收_更新压力 接受 扫描数、回收数：
    若 守护状态 等于 0：
        返还。
    完。
    新压力 为 0。
    若 扫描数 大于 0：
        新压力 为 (扫描数 减 回收数) 乘 基数 除以 扫描数。
    完。
    守护状态 的 压力值 为 (守护状态 的 压力值 乘 衰减系数 加 新压力 乘 (基数 减 衰减系数)) 除以 基数。
    做 内存_写32 用 守护状态 的 压力历史 加 守护状态 的 历史写位 乘 4、守护状态 的 压力值。
    守护状态 的 历史写位 为 (守护状态 的 历史写位 加 1) 取余于 压力历史深。
完。

回收_神经调整 接受 扫描数、回收数：
    若 守护状态 等于 0：
        返还。
    完。
    若 扫描数 大于 0 并且 回收数 乘 4 小于 扫描数：
        守护状态 的 神经回收权重 为 守护状态 的 神经回收权重 乘 110 除以 100。
        若 守护状态 的 神经回收权重 大于 1024：
            守护状态 的 神经回收权重 为 1024。
        完。
    否则若 回收数 大于 扫描数 除以 2：
        守护状态 的 神经回收权重 为 守护状态 的 神经回收权重 乘 95 除以 100。
        若 守护状态 的 神经回收权重 小于 128：
            守护状态 的 神经回收权重 为 128。
        完。
    完。
完。

回收_预测压力趋势 接受 零参：
    若 守护状态 等于 0：
        返还 0。
    完。
    可变 前半 为 0。
    可变 后半 为 0。
    半深 为 压力历史深 除以 2。
    可变 序号 为 0。
    只要 序号 小于 半深：
        前半 为 前半 加 做 内存_读32 用 守护状态 的 压力历史 加 序号 乘 4。
        序号 为 序号 加 1。
    完。
    只要 序号 小于 压力历史深：
        后半 为 后半 加 做 内存_读32 用 守护状态 的 压力历史 加 序号 乘 4。
        序号 为 序号 加 1。
    完。
    若 前半 等于 0：
        返还 0。
    完。
    返还 (后半 减 前半) 乘 基数 除以 前半。
完。

回收_获取压力 接受 零参：
    若 守护状态 等于 0：
        返还 0。
    完。
    返还 守护状态 的 压力值。
完。

交换守护_完整统计 接受 零参：
    若 守护状态 等于 0：
        返还 一组：0、0、0、0、0、0、0。
    完。
    返还 一组：守护状态 的 唤醒次数、守护状态 的 扫描总页、守护状态 的 回收总页、守护状态 的 直接回收次、守护状态 的 压力值、守护状态 的 神经回收权重、做 回收_预测压力趋势。
完。

本章 收束。
