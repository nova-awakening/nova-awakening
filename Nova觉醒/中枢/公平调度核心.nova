《公平调度核心》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·公平调度周期》。

引用《.·红黑树创建》。

基础时间片 为 3000。

调度延迟 为 6000。

最小粒度 为 750。

唤醒粒度 为 1000。

Nova公平调度_带宽_周期 为 100000。

调度_最大延迟因子 为 8。

NICE_0_权重 为 1024。

NICE_权重表 为 一组：88761、71755、56483、46273、36291、29154、23254、18705、14949、11916、9548、7620、6100、4904、3906、3121、2501、1991、1586、1277、1024、820、655、526、423、335、272、215、172、137、110、87、70、56、45、36、29、23、18、15、12。

NICE_倒数表 为 一组：48388、59856、76040、92818、118348、147320、184698、229616、287308、360437、449829、563644、704093、875809、1099582、1376151、1717300、2157191、2708050、3363326、4194304、5237765、6557202、8165337、10153587、12820798、15790321、19976592、24970740、31350126、39045157、49367440、61356676、76695844、95443717、119304647、148102320、186737708、238609294、286331153、360437635。

可变 Nova公平调度运行队列 为 空。

可变 Nova公平调度进程数 为 0。

可变 最小虚拟运行时间 为 0。

可变 Nova公平调度总权重 为 0。

可变 Nova公平调度次数 为 0。

可变 Nova公平调度_当前执行时间 为 0。

可变 Nova公平调度_平均Nova虚拟运行时间加权和 为 0。

可变 Nova公平调度_当前实体 为 空。

RT_优先级数 为 100。

可变 RT_FIFO队列 为 一组：（空）。

可变 RT_进程数 为 0。

Nova公平调度_初始化 接受 零参：
    Nova公平调度运行队列 为 做 红黑树_创建。
    Nova公平调度进程数 为 0。
    Nova公平调度总权重 为 0。
    最小虚拟运行时间 为 0。
    Nova公平调度次数 为 0。
    Nova公平调度_当前执行时间 为 0。
    Nova公平调度_平均Nova虚拟运行时间加权和 为 0。
    Nova公平调度_当前实体 为 空。
    RT_FIFO队列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 RT_优先级数：
        做 追加 用 RT_FIFO队列、一组：（空）。
        i 为 i 加 1。
    完。
    RT_进程数 为 0。
完。

nice_到_权重 接受 nice值：
    索引 为 nice值 加 20。
    若 索引 小于 0：
        索引 为 0。
    完。
    若 索引 大于 39：
        索引 为 39。
    完。
    返还 NICE_权重表 的 第 索引 项。
完。

nice_到_倒数 接受 nice值：
    索引 为 nice值 加 20。
    若 索引 小于 0：
        索引 为 0。
    完。
    若 索引 大于 39：
        索引 为 39。
    完。
    返还 NICE_倒数表 的 第 索引 项。
完。

__calc_delta 接受 delta_exec、权重、倒数权重：
    若 权重 等于 NICE_0_权重：
        返还 delta_exec。
    完。
    若 权重 小于或等于 0：
        返还 delta_exec。
    完。
    返还 delta_exec 乘 NICE_0_权重 除以 权重。
完。

calc_delta_fair 接受 delta_exec、任务：
    权重 为 任务 的 第 T_权重 项。
    若 权重 等于 NICE_0_权重：
        返还 delta_exec。
    完。
    返还 做 __calc_delta 用 delta_exec、权重、做 nice_到_倒数 用 任务 的 第 T_nice值 项。
完。

Nova公平调度_update_curr 接受 当前任务、delta_us：
    若 当前任务 等于 空：
        返还。
    完。
    若 delta_us 小于或等于 0：
        返还。
    完。
    Nova虚拟运行时间增量 为 做 calc_delta_fair 用 delta_us、当前任务。
    做 设置 用 当前任务、T_虚拟运行时间、当前任务 的 第 T_虚拟运行时间 项 加 Nova虚拟运行时间增量。
    若 当前任务 的 第 T_虚拟运行时间 项 大于或等于 当前任务 的 第 T_deadline 项：
        虚拟片 为 做 calc_delta_fair 用 当前任务 的 第 T_时间片 项、当前任务。
        做 设置 用 当前任务、T_deadline、当前任务 的 第 T_虚拟运行时间 项 加 虚拟片。
    完。
    做 Nova公平调度_更新最小虚拟运行时间_带当前 用 当前任务。
    Nova公平调度_当前执行时间 为 Nova公平调度_当前执行时间 加 delta_us。
完。

本章 收束。
