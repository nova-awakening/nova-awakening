《虚分配故障》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·自动扩容》。

引用《.·字符串替换》。

引用《.·内核打印》。

引用《.·恐慌》。

引用《.·获取当前页》。

引用《.·伙伴系统释放》。

引用《.·映射页》。

vmalloc_fault 接受 地址：
    返还 成功。
完。

pte_保留位错误 接受 地址、错误码：
    做 内核打印 用 日志_错误、「BUG: PTE reserved bits set at 」 加 做 整数转十六进制 用 地址。
    做 内核恐慌 用 「Bad PTE」。
完。

发送SIGSEGV 接受 故障地址：
    做 内核调试 用 「SIGSEGV: 」 加 做 整数转十六进制 用 故障地址。
完。

缺页错误码描述 接受 错误码：
    可变 s 为 「」。
    若 错误码 位与 PF_存在 不等于 0：
        s 为 s 加 「P 」。
    否则：
        s 为 s 加 「!P 」。
    完。
    若 错误码 位与 PF_写 不等于 0：
        s 为 s 加 「W 」。
    否则：
        s 为 s 加 「R 」。
    完。
    若 错误码 位与 PF_用户 不等于 0：
        s 为 s 加 「U 」。
    否则：
        s 为 s 加 「K 」。
    完。
    若 错误码 位与 PF_取指 不等于 0：
        s 为 s 加 「I 」。
    完。
    返还 s。
完。

查找VMA 接受 mm、地址：
    若 mm 等于 空：
        返还 空。
    完。
    vma列表 为 做 mm_获取VMA列表 用 mm。
    若 vma列表 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 vma列表：
        vma 为 vma列表 的 第 i 项。
        若 地址 大于或等于 vma 的 第 VMA_起始 项 并且 地址 小于 vma 的 第 VMA_结束 项：
            返还 vma。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

栈扩展下限 接受 mm：
    返还 0x7FFF00000000 减 8388608。
完。

扩展栈 接受 mm、地址：
    vma列表 为 做 mm_获取VMA列表 用 mm。
    若 vma列表 等于 空：
        返还 空。
    完。
    可变 i 为 做 计数 用 vma列表 减 1。
    只要 i 大于或等于 0：
        vma 为 vma列表 的 第 i 项。
        若 vma 的 第 VMA_标志 项 位与 VM_GROWSDOWN：
            若 地址 小于 vma 的 第 VMA_起始 项 并且 地址 大于或等于 做 栈扩展下限 用 mm：
                vma 的 第 VMA_起始 项 为 地址 位与 按位取反 0xFFF。
                返还 vma。
            完。
        完。
        i 为 i 减 1。
    完。
    返还 空。
完。

VMA_可写 接受 vma：
    若 vma 等于 空：
        返还 假。
    完。
    返还 vma 的 第 VMA_标志 项 位与 VM_WRITE 不等于 0。
完。

VMA_可执行 接受 vma：
    若 vma 等于 空：
        返还 假。
    完。
    返还 vma 的 第 VMA_标志 项 位与 VM_EXEC 不等于 0。
完。

VMA_是COW 接受 vma、地址：
    若 vma 等于 空：
        返还 假。
    完。
    返还 vma 的 第 VMA_标志 项 位与 VM_WRITE 不等于 0 并且 vma 的 第 VMA_标志 项 位与 VM_SHARED 等于 0。
完。

VMA_是匿名 接受 vma：
    若 vma 等于 空：
        返还 真。
    完。
    返还 vma 的 第 VMA_文件 项 等于 空。
完。

VMA_保护 接受 vma：
    若 vma 等于 空：
        返还 PTE_存在 位或 PTE_用户。
    完。
    可变 保护 为 PTE_存在 位或 PTE_用户。
    若 vma 的 第 VMA_标志 项 位与 VM_WRITE：
        保护 为 保护 位或 PTE_可写。
    完。
    若 非 (vma 的 第 VMA_标志 项 位与 VM_EXEC)：
        保护 为 保护 位或 PTE_不可执行。
    完。
    返还 保护。
完。

mm_获取VMA列表 接受 mm：
    若 mm 等于 空：
        返还 空。
    完。
    返还 mm 的 第 MM_VMA列表 项。
完。

VM_READ 为 1 左移 0。

VM_WRITE 为 1 左移 1。

VM_EXEC 为 1 左移 2。

VM_SHARED 为 1 左移 3。

VM_GROWSDOWN 为 1 左移 6。

MM_VMA列表 为 0。

页表_查询 接受 mm、地址：
    若 mm 等于 空：
        返还 0。
    完。
    PML4 为 mm 的 第 1 项。
    若 PML4 等于 0：
        返还 0。
    完。
    返还 做 页表_遍历到PT 用 PML4、地址、假。
完。

页表_映射 接受 mm、虚拟地址、物理地址、保护：
    若 mm 等于 空：
        返还。
    完。
    PML4 为 mm 的 第 1 项。
    若 PML4 等于 0：
        返还。
    完。
    PT 为 做 页表_遍历到PT 用 PML4、虚拟地址、真。
    若 PT 等于 0：
        返还。
    完。
    索引 为 虚拟地址 右移 12 位与 0x1FF。
    做 页表_写条目 用 PT、索引、物理地址 位或 保护。
完。

分配页 接受 阶、gfp：
    返还 做 alloc_pages 用 阶、gfp。
完。

分配连续页 接受 页数：
    可变 阶 为 0。
    可变 大小 为 1。
    只要 大小 小于 页数：
        大小 为 大小 乘 2。
        阶 为 阶 加 1。
    完。
    返还 做 alloc_pages 用 阶、GFP_KERNEL。
完。

缺页_统计 接受 零参：
    返还 一组：缺页_总次数、缺页_次要次数、缺页_主要次数、缺页_COW次数、缺页_SIGSEGV次数。
完。

可变 vmalloc_同步次数 为 0。

可变 vmalloc_同步失败 为 0。

vmalloc_起始 为 0xFFFF888000000000。

vmalloc_结束 为 0xFFFFC87FFFFFFFFF。

是vmalloc地址 接受 地址：
    返还 地址 大于或等于 vmalloc_起始 并且 地址 小于或等于 vmalloc_结束。
完。

vmalloc_fault_完整 接受 地址：
    若 做 是vmalloc地址 用 地址 等于 假：
        返还 错误_无效参数。
    完。
    init_pgd 为 做 获取init_PGD。
    若 init_pgd 等于 0：
        vmalloc_同步失败 为 vmalloc_同步失败 加 1。
        返还 错误_不存在。
    完。
    当前pgd 为 做 当前PML4。
    若 当前pgd 等于 0：
        vmalloc_同步失败 为 vmalloc_同步失败 加 1。
        返还 错误_不存在。
    完。
    pgd索引 为 地址 右移 39 位与 0x1FF。
    init_pgd_条目 为 做 内存_读64 用 init_pgd 加 pgd索引 乘 8。
    若 init_pgd_条目 位与 1 等于 0：
        vmalloc_同步失败 为 vmalloc_同步失败 加 1。
        返还 错误_不存在。
    完。
    做 内存_写64 用 当前pgd 加 pgd索引 乘 8、init_pgd_条目。
    pud索引 为 地址 右移 30 位与 0x1FF。
    init_pud基 为 init_pgd_条目 位与 0xFFFFFFFFF000。
    init_pud_条目 为 做 内存_读64 用 init_pud基 加 pud索引 乘 8。
    若 init_pud_条目 位与 1 等于 0：
        vmalloc_同步失败 为 vmalloc_同步失败 加 1。
        返还 错误_不存在。
    完。
    pmd索引 为 地址 右移 21 位与 0x1FF。
    init_pmd基 为 init_pud_条目 位与 0xFFFFFFFFF000。
    init_pmd_条目 为 做 内存_读64 用 init_pmd基 加 pmd索引 乘 8。
    若 init_pmd_条目 位与 1 等于 0：
        vmalloc_同步失败 为 vmalloc_同步失败 加 1。
        返还 错误_不存在。
    完。
    vmalloc_同步次数 为 vmalloc_同步次数 加 1。
    返还 0。
完。

获取init_PGD 接受 零参：
    可变 cr3 为 0。
    汇编。
    块：
        「mov rax, cr3」。
        「mov [rbp-8], rax」。
    完。
    返还 cr3 位与 0xFFFFFFFFF000。
完。

当前PML4 接受 零参：
    可变 cr3 为 0。
    汇编。
    块：
        「mov rax, cr3」。
        「mov [rbp-8], rax」。
    完。
    返还 cr3 位与 0xFFFFFFFFF000。
完。

读CR2 接受 零参：
    可变 cr2 为 0。
    汇编。
    块：
        「mov rax, cr2」。
        「mov [rbp-8], rax」。
    完。
    返还 cr2。
完。

可变 vma_查找次数 为 0。

可变 vma_缓存命中 为 0。

可变 vma_缓存未命中 为 0。

查找VMA_带缓存 接受 mm、地址：
    vma_查找次数 为 vma_查找次数 加 1。
    若 mm 等于 空：
        返还 空。
    完。
    缓存vma 为 做 mm_获取VMA缓存 用 mm。
    若 缓存vma 不等于 空：
        若 地址 大于或等于 缓存vma 的 第 VMA_起始 项 并且 地址 小于 缓存vma 的 第 VMA_结束 项：
            vma_缓存命中 为 vma_缓存命中 加 1。
            返还 缓存vma。
        完。
    完。
    vma_缓存未命中 为 vma_缓存未命中 加 1。
    结果 为 做 查找VMA 用 mm、地址。
    若 结果 不等于 空：
        做 mm_设置VMA缓存 用 mm、结果。
    完。
    返还 结果。
完。

mm_获取VMA缓存 接受 mm：
    若 mm 等于 空：
        返还 空。
    完。
    返还 mm 的 第 2 项。
完。

mm_设置VMA缓存 接受 mm、vma：
    若 mm 等于 空：
        返还。
    完。
    mm 的 第 2 项 为 vma。
完。

VMA_遍历 接受 mm、回调：
    若 mm 等于 空 或者 回调 等于 空：
        返还 0。
    完。
    vma列表 为 做 mm_获取VMA列表 用 mm。
    若 vma列表 等于 空：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 vma列表：
        做 回调 用 vma列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 i。
完。

VMA_计数 接受 mm：
    若 mm 等于 空：
        返还 0。
    完。
    vma列表 为 做 mm_获取VMA列表 用 mm。
    若 vma列表 等于 空：
        返还 0。
    完。
    返还 做 计数 用 vma列表。
完。

VMA_获取文件 接受 vma：
    若 vma 等于 空：
        返还 空。
    完。
    返还 vma 的 第 VMA_文件 项。
完。

VMA_起始 为 0。

VMA_结束 为 1。

VMA_标志 为 2。

VMA_文件 为 3。

VMA_文件偏移 为 4。

可变 页表_映射次数 为 0。

可变 页表_取消映射次数 为 0。

页表_取消映射 接受 mm、虚拟地址：
    若 mm 等于 空：
        返还。
    完。
    PML4 为 mm 的 第 1 项。
    若 PML4 等于 0：
        返还。
    完。
    索引 为 虚拟地址 右移 12 位与 0x1FF。
    PT 为 做 页表_遍历到PT 用 PML4、虚拟地址、假。
    若 PT 不等于 0：
        做 页表_写条目 用 PT、索引、0。
        做 刷新TLB_单页 用 虚拟地址。
        页表_取消映射次数 为 页表_取消映射次数 加 1。
    完。
完。

页表_批量映射 接受 mm、起始虚拟、起始物理、页数、保护：
    可变 i 为 0。
    只要 i 小于 页数：
        做 页表_映射 用 mm、起始虚拟 加 i 乘 4096、起始物理 加 i 乘 4096、保护。
        页表_映射次数 为 页表_映射次数 加 1。
        i 为 i 加 1。
    完。
    返还 页数。
完。

页表_批量取消映射 接受 mm、起始虚拟、页数：
    可变 i 为 0。
    只要 i 小于 页数：
        做 页表_取消映射 用 mm、起始虚拟 加 i 乘 4096。
        i 为 i 加 1。
    完。
    返还 页数。
完。

刷新TLB_单页 接受 地址：
    汇编。
    块：
        「invlpg [rdi]」。
    完。
完。

刷新全部TLB 接受 零参：
    可变 cr3 为 0。
    汇编。
    块：
        「mov rax, cr3」。
        「mov cr3, rax」。
    完。
完。

页号到地址 接受 页号：
    返还 页号 乘 4096。
完。

页大小 为 4096。

GFP_KERNEL 为 0。

虚分配故障_完整统计 接受 零参：
    统计 为 做 字典。
    做 字典设 用 统计、「缺页总次数」、缺页_总次数。
    做 字典设 用 统计、「缺页次要」、缺页_次要次数。
    做 字典设 用 统计、「缺页主要」、缺页_主要次数。
    做 字典设 用 统计、「缺页COW」、缺页_COW次数。
    做 字典设 用 统计、「缺页SIGSEGV」、缺页_SIGSEGV次数。
    做 字典设 用 统计、「vmalloc同步次数」、vmalloc_同步次数。
    做 字典设 用 统计、「vmalloc同步失败」、vmalloc_同步失败。
    做 字典设 用 统计、「VMA查找次数」、vma_查找次数。
    做 字典设 用 统计、「VMA缓存命中」、vma_缓存命中。
    做 字典设 用 统计、「VMA缓存未命中」、vma_缓存未命中。
    做 字典设 用 统计、「VMA缓存命中率(%)」、若 vma_查找次数 大于 0 则 vma_缓存命中 乘 100 除以 vma_查找次数 否则 0。
    做 字典设 用 统计、「页表映射次数」、页表_映射次数。
    做 字典设 用 统计、「页表取消映射」、页表_取消映射次数。
    返还 统计。
完。

虚分配故障_打印统计 接受 零参：
    统计 为 做 虚分配故障_完整统计。
    做 打印 用 「[虚分配故障] ═══════ 完整统计 ═══════」。
    做 打印 用 「  缺页总次数:   」 加 做 转字符串 用 做 字典取 用 统计、「缺页总次数」。
    做 打印 用 「  缺页次要:     」 加 做 转字符串 用 做 字典取 用 统计、「缺页次要」。
    做 打印 用 「  缺页主要:     」 加 做 转字符串 用 做 字典取 用 统计、「缺页主要」。
    做 打印 用 「  缺页COW:      」 加 做 转字符串 用 做 字典取 用 统计、「缺页COW」。
    做 打印 用 「  SIGSEGV:      」 加 做 转字符串 用 做 字典取 用 统计、「缺页SIGSEGV」。
    做 打印 用 「  vmalloc同步:  」 加 做 转字符串 用 做 字典取 用 统计、「vmalloc同步次数」。
    做 打印 用 「  vmalloc失败:  」 加 做 转字符串 用 做 字典取 用 统计、「vmalloc同步失败」。
    做 打印 用 「  VMA查找:      」 加 做 转字符串 用 做 字典取 用 统计、「VMA查找次数」。
    做 打印 用 「  VMA缓存命中:  」 加 做 转字符串 用 做 字典取 用 统计、「VMA缓存命中」。
    做 打印 用 「  页表映射:     」 加 做 转字符串 用 做 字典取 用 统计、「页表映射次数」。
    做 打印 用 「  页表取消映射: 」 加 做 转字符串 用 做 字典取 用 统计、「页表取消映射」。
    做 打印 用 「[虚分配故障] ═════════════════════════」。
    返还 0。
完。

虚分配故障_dump 接受 零参：
    做 打印 用 「[虚分配故障 dump] ──────────────────」。
    做 打印 用 「  缺页总数: 」 加 做 转字符串 用 缺页_总次数。
    做 打印 用 「  vmalloc同步: 」 加 做 转字符串 用 vmalloc_同步次数。
    做 打印 用 「  VMA查找: 」 加 做 转字符串 用 vma_查找次数。
    做 打印 用 「  VMA缓存命中: 」 加 做 转字符串 用 vma_缓存命中。
    做 打印 用 「  页表映射: 」 加 做 转字符串 用 页表_映射次数。
    做 打印 用 「  当前CR2: 0x」 加 做 转字符串 用 做 读CR2。
    做 打印 用 「  当前CR3: 0x」 加 做 转字符串 用 做 当前PML4。
    做 打印 用 「[虚分配故障 dump] ──────────────────」。
完。

虚分配故障_销毁 接受 零参：
    vmalloc_同步次数 为 0。
    vmalloc_同步失败 为 0。
    vma_查找次数 为 0。
    vma_缓存命中 为 0。
    vma_缓存未命中 为 0。
    页表_映射次数 为 0。
    页表_取消映射次数 为 0。
    做 打印 用 「[虚分配故障] 已销毁」。
    返还 0。
完。

虚分配故障_为内存管理提供统计 接受 零参：
    返还 一组：缺页_总次数、缺页_次要次数、缺页_主要次数、vmalloc_同步次数、页表_映射次数。
完。

虚分配故障_为procfs提供信息 接受 零参：
    返还 一组：缺页_总次数、缺页_次要次数、缺页_主要次数、缺页_COW次数、缺页_SIGSEGV次数、vmalloc_同步次数、vma_查找次数、vma_缓存命中。
完。

虚分配故障_重置统计 接受 零参：
    缺页_总次数 为 0。
    缺页_次要次数 为 0。
    缺页_主要次数 为 0。
    缺页_COW次数 为 0。
    缺页_SIGSEGV次数 为 0。
    vmalloc_同步次数 为 0。
    vmalloc_同步失败 为 0。
    vma_查找次数 为 0。
    vma_缓存命中 为 0。
    vma_缓存未命中 为 0。
    页表_映射次数 为 0。
    页表_取消映射次数 为 0。
    做 打印 用 「[虚分配故障] 统计已重置」。
    返还 0。
完。

VMA_权限名 接受 标志：
    可变 名称 为 「」。
    若 标志 位与 VM_READ 不等于 0：
        名称 为 名称 加 「r」。
    否则：
        名称 为 名称 加 「-」。
    完。
    若 标志 位与 VM_WRITE 不等于 0：
        名称 为 名称 加 「w」。
    否则：
        名称 为 名称 加 「-」。
    完。
    若 标志 位与 VM_EXEC 不等于 0：
        名称 为 名称 加 「x」。
    否则：
        名称 为 名称 加 「-」。
    完。
    若 标志 位与 VM_SHARED 不等于 0：
        名称 为 名称 加 「s」。
    否则：
        名称 为 名称 加 「p」。
    完。
    返还 名称。
完。

VMA_dump 接受 mm：
    若 mm 等于 空：
        返还。
    完。
    vma列表 为 做 mm_获取VMA列表 用 mm。
    若 vma列表 等于 空：
        返还。
    完。
    做 打印 用 「[VMA dump] ──────────────────」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 vma列表：
        vma 为 vma列表 的 第 i 项。
        做 打印 用 「  0x」 加 做 转字符串 用 vma 的 第 VMA_起始 项 加 「-0x」 加 做 转字符串 用 vma 的 第 VMA_结束 项 加 「 」 加 做 VMA_权限名 用 vma 的 第 VMA_标志 项。
        i 为 i 加 1。
    完。
    做 打印 用 「[VMA dump] ──────────────────」。
完。

虚分配故障_为AI提供缺页模式 接受 零参：
    返还 一组：缺页_总次数、缺页_次要次数、缺页_主要次数、缺页_COW次数、vmalloc_同步次数、vma_缓存命中、vma_缓存未命中、页表_映射次数。
完。

本章 收束。
