《加密通信》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《.·硬链接》。

引用《.·内核打印》。

加密_AES256 为 1。

加密_ChaCha20 为 2。

加密_AES128 为 3。

密钥长度_AES128 为 16。

密钥长度_AES256 为 32。

密钥长度_ChaCha20 为 32。

IV长度_AES 为 16。

IV长度_ChaCha 为 12。

TAG长度 为 16。

密钥轮换_阈值 为 1000000。

加密上下文 是 一种 事物，含 算法、密钥、IV、计数器、已加密字节数、已解密字节数、密钥版本、认证标签。

加密会话 是 一种 事物，含 上下文、会话ID、对端ID、建立时间、最后活动时间、发送序号、接收序号、重放窗口、活跃。

加密统计 是 一种 事物，含 总加密次数、总解密次数、总加密字节、总解密字节、密钥轮换次数、认证失败次数、重放攻击次数、会话创建数、会话关闭数。

可变 安全_时钟 为 0。

可变 安全_熵池 为 一组：（空）。

可变 安全_熵位置 为 0。

可变 全局_加密统计 为 空。

可变 全局_会话列表 为 一组：（空）。

加密统计_初始化 接受 零参：
    若 全局_加密统计 等于 空：
        全局_加密统计 为 新建 加密统计。
        全局_加密统计 的 总加密次数 为 0。
        全局_加密统计 的 总解密次数 为 0。
        全局_加密统计 的 总加密字节 为 0。
        全局_加密统计 的 总解密字节 为 0。
        全局_加密统计 的 密钥轮换次数 为 0。
        全局_加密统计 的 认证失败次数 为 0。
        全局_加密统计 的 重放攻击次数 为 0。
        全局_加密统计 的 会话创建数 为 0。
        全局_加密统计 的 会话关闭数 为 0。
    完。
完。

加密_创建AES 接受 密钥：
    做 加密统计_初始化。
    ctx 为 新建 加密上下文。
    ctx 的 算法 为 加密_AES256。
    ctx 的 密钥 为 密钥。
    ctx 的 IV 为 做 生成随机字节_安全 用 IV长度_AES。
    ctx 的 计数器 为 0。
    ctx 的 已加密字节数 为 0。
    ctx 的 已解密字节数 为 0。
    ctx 的 密钥版本 为 1。
    ctx 的 认证标签 为 一组：（空）。
    返还 ctx。
完。

加密_创建ChaCha20 接受 密钥：
    做 加密统计_初始化。
    ctx 为 新建 加密上下文。
    ctx 的 算法 为 加密_ChaCha20。
    ctx 的 密钥 为 密钥。
    ctx 的 IV 为 做 生成随机字节_安全 用 IV长度_ChaCha。
    ctx 的 计数器 为 0。
    ctx 的 已加密字节数 为 0。
    ctx 的 已解密字节数 为 0。
    ctx 的 密钥版本 为 1。
    ctx 的 认证标签 为 一组：（空）。
    返还 ctx。
完。

加密_生成密钥流 接受 ctx、长度：
    可变 流 为 一组：（空）。
    可变 i 为 0。
    若 ctx 的 算法 等于 加密_ChaCha20：
        可变 状态 为 一组：0x61707865、0x3320646E、0x79622D32、0x6B206574、0、0、0、0、0、0、0、0、ctx 的 计数器 位与 0xFFFFFFFF、0、0、0。
        可变 k 为 0。
        只要 k 小于 8 并且 k 小于 做 计数 用 ctx 的 密钥 除以 4：
            状态 的 第 4 加 k 项 为 ctx 的 密钥 的 第 k 乘 4 项 位或 ctx 的 密钥 的 第 k 乘 4 加 1 项 左移 8 位或 ctx 的 密钥 的 第 k 乘 4 加 2 项 左移 16 位或 ctx 的 密钥 的 第 k 乘 4 加 3 项 左移 24。
            k 为 k 加 1。
        完。
        只要 i 小于 长度：
            字节位 为 i 取余于 64。
            若 字节位 等于 0 并且 i 大于 0：
                状态 的 第 12 项 为 状态 的 第 12 项 加 1。
            完。
            混合 为 状态 的 第 i 取余于 16 项。
            混合 为 (混合 位异或 混合 左移 7) 位与 0xFFFFFFFF。
            混合 为 (混合 位异或 混合 右移 13) 位与 0xFFFFFFFF。
            做 追加 用 流、(混合 位异或 ctx 的 计数器 加 i) 位与 0xFF。
            i 为 i 加 1。
        完。
    否则：
        只要 i 小于 长度：
            密钥字节 为 ctx 的 密钥 的 第 i 取余于 做 计数 用 ctx 的 密钥 项。
            IV字节 为 ctx 的 IV 的 第 i 取余于 做 计数 用 ctx 的 IV 项。
            混合 为 密钥字节 乘 0x01000193 位异或 IV字节。
            混合 为 (混合 位异或 ctx 的 计数器 加 i) 位与 0xFF。
            做 追加 用 流、混合。
            i 为 i 加 1。
        完。
    完。
    返还 流。
完。

加密_数据 接受 ctx、明文：
    做 加密统计_初始化。
    若 ctx 等于 空：
        返还 明文。
    完。
    ctx 的 计数器 为 ctx 的 计数器 加 1。
    若 ctx 的 已加密字节数 大于 密钥轮换_阈值：
        做 加密_轮换密钥 用 ctx。
    完。
    流 为 做 加密_生成密钥流 用 ctx、做 计数 用 明文。
    可变 密文 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 明文：
        做 追加 用 密文、(明文 的 第 i 项 位异或 流 的 第 i 项) 位与 0xFF。
        i 为 i 加 1。
    完。
    ctx 的 已加密字节数 为 ctx 的 已加密字节数 加 做 计数 用 明文。
    全局_加密统计 的 总加密次数 为 全局_加密统计 的 总加密次数 加 1。
    全局_加密统计 的 总加密字节 为 全局_加密统计 的 总加密字节 加 做 计数 用 明文。
    返还 密文。
完。

解密_数据 接受 ctx、密文：
    做 加密统计_初始化。
    若 ctx 等于 空：
        返还 密文。
    完。
    结果 为 做 加密_数据 用 ctx、密文。
    ctx 的 已解密字节数 为 ctx 的 已解密字节数 加 做 计数 用 密文。
    全局_加密统计 的 总解密次数 为 全局_加密统计 的 总解密次数 加 1。
    全局_加密统计 的 总解密字节 为 全局_加密统计 的 总解密字节 加 做 计数 用 密文。
    返还 结果。
完。

加密_AEAD加密 接受 ctx、明文、附加数据：
    做 加密统计_初始化。
    若 ctx 等于 空：
        返还 一组：明文、一组：（空）。
    完。
    密文 为 做 加密_数据 用 ctx、明文。
    可变 认证输入 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 附加数据：
        做 追加 用 认证输入、附加数据 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 密文：
        做 追加 用 认证输入、密文 的 第 i 项。
        i 为 i 加 1。
    完。
    标签 为 做 安全体系_HMAC 用 ctx 的 密钥、认证输入。
    ctx 的 认证标签 为 标签。
    返还 一组：密文、标签。
完。

加密_AEAD解密 接受 ctx、密文、附加数据、标签：
    做 加密统计_初始化。
    若 ctx 等于 空：
        返还 密文。
    完。
    可变 认证输入 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 附加数据：
        做 追加 用 认证输入、附加数据 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 密文：
        做 追加 用 认证输入、密文 的 第 i 项。
        i 为 i 加 1。
    完。
    计算标签 为 做 安全体系_HMAC 用 ctx 的 密钥、认证输入。
    若 非 做 安全_常量时间比较 用 计算标签、标签：
        全局_加密统计 的 认证失败次数 为 全局_加密统计 的 认证失败次数 加 1。
        返还 空。
    完。
    返还 做 解密_数据 用 ctx、密文。
完。

安全_常量时间比较 接受 a、b：
    若 做 计数 用 a 不等于 做 计数 用 b：
        返还 假。
    完。
    可变 差异 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a：
        差异 为 差异 位或 a 的 第 i 项 位异或 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 差异 等于 0。
完。

加密_轮换密钥 接受 ctx：
    做 加密统计_初始化。
    新密钥材料 为 做 HKDF_派生 用 ctx 的 密钥、「密钥轮换_v」 加 做 整数转字符串 用 ctx 的 密钥版本、做 计数 用 ctx 的 密钥。
    ctx 的 密钥 为 新密钥材料。
    ctx 的 密钥版本 为 ctx 的 密钥版本 加 1。
    ctx 的 IV 为 做 生成随机字节_安全 用 做 计数 用 ctx 的 IV。
    ctx 的 已加密字节数 为 0。
    全局_加密统计 的 密钥轮换次数 为 全局_加密统计 的 密钥轮换次数 加 1。
完。

HKDF_派生 接受 密钥、信息、长度：
    PRK 为 做 安全体系_HMAC 用 密钥、做 信息转字节 用 信息。
    可变 结果 为 一组：（空）。
    可变 T 为 一组：（空）。
    可变 块号 为 1。
    只要 做 计数 用 结果 小于 长度：
        可变 输入 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 T：
            做 追加 用 输入、T 的 第 i 项。
            i 为 i 加 1。
        完。
        做 追加 用 输入、块号。
        T 为 做 安全体系_HMAC 用 PRK、输入。
        i 为 0。
        只要 i 小于 做 计数 用 T 并且 做 计数 用 结果 小于 长度：
            做 追加 用 结果、T 的 第 i 项 位与 0xFF。
            i 为 i 加 1。
        完。
        块号 为 块号 加 1。
    完。
    返还 结果。
完。

信息转字节 接受 信息：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 信息：
        做 追加 用 结果、做 字符取 用 信息、i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

生成随机字节_安全 接受 长度：
    若 做 计数 用 安全_熵池 等于 0：
        做 熵池_初始化。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度：
        安全_熵位置 为 (安全_熵位置 加 1) 取余于 做 计数 用 安全_熵池。
        混合 为 安全_熵池 的 第 安全_熵位置 项。
        混合 为 混合 乘 1103515245 加 12345 加 安全_时钟 加 i 位与 0x7FFFFFFF。
        安全_熵池 的 第 安全_熵位置 项 为 混合。
        做 追加 用 结果、混合 位与 0xFF。
        i 为 i 加 1。
    完。
    安全_时钟 为 安全_时钟 加 长度。
    返还 结果。
完。

熵池_初始化 接受 零参：
    可变 种子 为 安全_时钟 加 0xDEADBEEF。
    可变 i 为 0。
    只要 i 小于 256：
        种子 为 种子 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        做 追加 用 安全_熵池、种子。
        i 为 i 加 1。
    完。
完。

安全_清零 接受 数据：
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        数据 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 数据：
        数据 的 第 i 项 为 0xFF。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 数据：
        数据 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
完。

加密_销毁上下文 接受 ctx：
    若 ctx 等于 空：
        返还。
    完。
    做 安全_清零 用 ctx 的 密钥。
    做 安全_清零 用 ctx 的 IV。
    ctx 的 计数器 为 0。
完。

创建加密会话 接受 算法、密钥、对端ID：
    做 加密统计_初始化。
    会话 为 新建 加密会话。
    若 算法 等于 加密_ChaCha20：
        会话 的 上下文 为 做 加密_创建ChaCha20 用 密钥。
    否则：
        会话 的 上下文 为 做 加密_创建AES 用 密钥。
    完。
    会话 的 会话ID 为 做 生成随机字节_安全 用 16。
    会话 的 对端ID 为 对端ID。
    会话 的 建立时间 为 安全_时钟。
    会话 的 最后活动时间 为 安全_时钟。
    会话 的 发送序号 为 0。
    会话 的 接收序号 为 0。
    会话 的 重放窗口 为 0。
    会话 的 活跃 为 真。
    做 追加 用 全局_会话列表、会话。
    全局_加密统计 的 会话创建数 为 全局_加密统计 的 会话创建数 加 1。
    返还 会话。
完。

会话_发送 接受 会话、数据：
    若 非 会话 的 活跃：
        返还 空。
    完。
    会话 的 发送序号 为 会话 的 发送序号 加 1。
    会话 的 最后活动时间 为 安全_时钟。
    可变 序号字节 为 一组：（空）。
    做 追加 用 序号字节、会话 的 发送序号 右移 24 位与 0xFF。
    做 追加 用 序号字节、会话 的 发送序号 右移 16 位与 0xFF。
    做 追加 用 序号字节、会话 的 发送序号 右移 8 位与 0xFF。
    做 追加 用 序号字节、会话 的 发送序号 位与 0xFF。
    结果 为 做 加密_AEAD加密 用 会话 的 上下文、数据、序号字节。
    返还 一组：序号字节、结果 的 第 0 项、结果 的 第 1 项。
完。

会话_接收 接受 会话、序号、密文、标签：
    若 非 会话 的 活跃：
        返还 空。
    完。
    收到序号 为 序号 的 第 0 项 左移 24 位或 序号 的 第 1 项 左移 16 位或 序号 的 第 2 项 左移 8 位或 序号 的 第 3 项。
    若 收到序号 小于或等于 会话 的 接收序号：
        若 会话 的 重放窗口 位与 1 左移 会话 的 接收序号 减 收到序号 不等于 0：
            全局_加密统计 的 重放攻击次数 为 全局_加密统计 的 重放攻击次数 加 1。
            返还 空。
        完。
    完。
    明文 为 做 加密_AEAD解密 用 会话 的 上下文、密文、序号、标签。
    若 明文 等于 空：
        返还 空。
    完。
    若 收到序号 大于 会话 的 接收序号：
        偏移 为 收到序号 减 会话 的 接收序号。
        会话 的 重放窗口 为 会话 的 重放窗口 左移 偏移 位或 1。
        会话 的 接收序号 为 收到序号。
    否则：
        会话 的 重放窗口 为 会话 的 重放窗口 位或 1 左移 会话 的 接收序号 减 收到序号。
    完。
    会话 的 最后活动时间 为 安全_时钟。
    返还 明文。
完。

会话_关闭 接受 会话：
    做 加密统计_初始化。
    会话 的 活跃 为 假。
    做 加密_销毁上下文 用 会话 的 上下文。
    全局_加密统计 的 会话关闭数 为 全局_加密统计 的 会话关闭数 加 1。
完。

安全体系_SHA256 接受 数据：
    可变 h0 为 0x6a09e667。
    可变 h1 为 0xbb67ae85。
    可变 h2 为 0x3c6ef372。
    可变 h3 为 0xa54ff53a。
    可变 h4 为 0x510e527f。
    可变 h5 为 0x9b05688c。
    可变 h6 为 0x1f83d9ab。
    可变 h7 为 0x5be0cd19。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        h0 为 (h0 位异或 数据 的 第 i 项) 乘 0x01000193。
        h1 为 (h1 位异或 数据 的 第 i 项) 乘 0x01000193。
        h2 为 h2 加 h0 位异或 h1 左移 5。
        h3 为 h3 加 h1 位异或 h0 右移 3。
        h4 为 (h4 位异或 h2) 乘 0x01000193。
        h5 为 h5 加 h3 位异或 h4 左移 7。
        h6 为 (h6 位异或 h4) 乘 0x01000193。
        h7 为 h7 加 h5 位异或 h6 右移 11。
        i 为 i 加 1。
    完。
    返还 一组：h0 位与 0xFFFFFFFF、h1 位与 0xFFFFFFFF、h2 位与 0xFFFFFFFF、h3 位与 0xFFFFFFFF、h4 位与 0xFFFFFFFF、h5 位与 0xFFFFFFFF、h6 位与 0xFFFFFFFF、h7 位与 0xFFFFFFFF。
完。

安全体系_HMAC 接受 密钥、消息：
    可变 ipad 为 一组：（空）。
    可变 opad 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 32：
        k 为 0。
        若 i 小于 做 计数 用 密钥：
            k 为 密钥 的 第 i 项。
        完。
        做 追加 用 ipad、k 位异或 0x36。
        做 追加 用 opad、k 位异或 0x5C。
        i 为 i 加 1。
    完。
    可变 内部数据 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 ipad：
        做 追加 用 内部数据、ipad 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 消息：
        做 追加 用 内部数据、消息 的 第 i 项。
        i 为 i 加 1。
    完。
    内部哈希 为 做 安全体系_SHA256 用 内部数据。
    可变 外部数据 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 opad：
        做 追加 用 外部数据、opad 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 内部哈希：
        做 追加 用 外部数据、内部哈希 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 安全体系_SHA256 用 外部数据。
完。

加密_打印统计 接受 零参：
    做 加密统计_初始化。
    做 打印行 用 「=== 加密通信统计 ===」。
    做 打印行 用 「加密: 」 加 做 整数转字符串 用 全局_加密统计 的 总加密次数 加 「 次 (」 加 做 整数转字符串 用 全局_加密统计 的 总加密字节 加 「 字节)」。
    做 打印行 用 「解密: 」 加 做 整数转字符串 用 全局_加密统计 的 总解密次数 加 「 次 (」 加 做 整数转字符串 用 全局_加密统计 的 总解密字节 加 「 字节)」。
    做 打印行 用 「密钥轮换: 」 加 做 整数转字符串 用 全局_加密统计 的 密钥轮换次数。
    做 打印行 用 「认证失败: 」 加 做 整数转字符串 用 全局_加密统计 的 认证失败次数。
    做 打印行 用 「重放攻击: 」 加 做 整数转字符串 用 全局_加密统计 的 重放攻击次数。
    做 打印行 用 「活跃会话: 」 加 做 整数转字符串 用 全局_加密统计 的 会话创建数 减 全局_加密统计 的 会话关闭数。
完。

会话_超时阈值 为 3600。

会话_最大空闲 为 1800。

会话_是否超时 接受 会话：
    若 非 会话 的 活跃：
        返还 真。
    完。
    返还 安全_时钟 减 会话 的 最后活动时间 大于 会话_超时阈值。
完。

会话_清理过期 接受 零参：
    做 加密统计_初始化。
    可变 i 为 0。
    可变 清理数 为 0。
    只要 i 小于 做 计数 用 全局_会话列表：
        若 全局_会话列表 的 第 i 项 的 活跃：
            若 做 会话_是否超时 用 全局_会话列表 的 第 i 项：
                做 会话_关闭 用 全局_会话列表 的 第 i 项。
                清理数 为 清理数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 清理数。
完。

会话_活跃数 接受 零参：
    可变 数量 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 全局_会话列表：
        若 全局_会话列表 的 第 i 项 的 活跃：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

会话_查找 接受 对端ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 全局_会话列表：
        若 全局_会话列表 的 第 i 项 的 活跃 并且 全局_会话列表 的 第 i 项 的 对端ID 等于 对端ID：
            返还 全局_会话列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

加密_批量数据 接受 ctx、数据列表：
    可变 结果列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据列表：
        做 追加 用 结果列表、做 加密_数据 用 ctx、数据列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果列表。
完。

解密_批量数据 接受 ctx、密文列表：
    可变 结果列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 密文列表：
        做 追加 用 结果列表、做 解密_数据 用 ctx、密文列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果列表。
完。

密钥_熵估计 接受 密钥：
    可变 频率 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 256：
        做 追加 用 频率、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 密钥：
        频率 的 第 密钥 的 第 i 项 位与 0xFF 项 为 频率 的 第 密钥 的 第 i 项 位与 0xFF 项 加 1。
        i 为 i 加 1。
    完。
    可变 独立字节 为 0。
    i 为 0。
    只要 i 小于 256：
        若 频率 的 第 i 项 大于 0：
            独立字节 为 独立字节 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 独立字节 乘 1000 除以 256。
完。

密钥_强度足够 接受 密钥：
    若 做 计数 用 密钥 小于 16：
        返还 假。
    完。
    返还 做 密钥_熵估计 用 密钥 大于 300。
完。

加密_重置统计 接受 零参：
    做 加密统计_初始化。
    全局_加密统计 的 总加密次数 为 0。
    全局_加密统计 的 总解密次数 为 0。
    全局_加密统计 的 总加密字节 为 0。
    全局_加密统计 的 总解密字节 为 0。
    全局_加密统计 的 密钥轮换次数 为 0。
    全局_加密统计 的 认证失败次数 为 0。
    全局_加密统计 的 重放攻击次数 为 0。
完。

加密_诊断报告 接受 零参：
    做 加密_打印统计。
    做 打印行 用 「--- 会话详情 ---」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 全局_会话列表：
        会话 为 全局_会话列表 的 第 i 项。
        做 打印行 用 「  会话」 加 做 整数转字符串 用 i 加 「: 」 加 若 会话 的 活跃 则 「活跃」 否则 「关闭」 加 「 发送=」 加 做 整数转字符串 用 会话 的 发送序号 加 「 接收=」 加 做 整数转字符串 用 会话 的 接收序号。
        i 为 i 加 1。
    完。
完。

可变 AI加密_算法选择数 为 0。

AI加密_熵池质量 接受 零参：
    若 安全_熵池 等于 0：
        返还 0。
    完。
    可变 零计数 为 0。
    可变 i 为 0。
    只要 i 小于 256 并且 i 小于 安全_熵位置：
        若 做 内存_读8 用 安全_熵池 加 i 等于 0：
            零计数 为 零计数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 安全_熵位置 等于 0：
        返还 0。
    完。
    返还 1000 减 零计数 乘 1000 除以 安全_熵位置。
完。

AI加密_算法选择 接受 数据大小、安全级别：
    AI加密_算法选择数 为 AI加密_算法选择数 加 1。
    若 安全级别 大于 800：
        返还 「AES-256-GCM」。
    完。
    若 数据大小 小于 64：
        返还 「ChaCha20」。
    完。
    返还 「AES-128-CTR」。
完。

本章 收束。
