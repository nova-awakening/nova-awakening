《红黑树后继》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·红黑树创建》。

引用《.·字符串替换》。

红 为 0。

黑 为 1。

RB_键 为 0。

RB_值 为 1。

RB_左 为 2。

RB_右 为 3。

RB_父 为 4。

RB_色 为 5。

AI热点窗口 为 16。

最大迭代深度 为 64。

导航删除统计 是 一种 事物，含 后继查找次数、前驱查找次数、移植次数、删除次数、删除修复次数、删除修复旋转、迭代器创建数、迭代器步进数、序列化次数、反序列化次数、批量删除次数、过滤删除次数、AI热点记录数、AI热点命中数、错误次数、空节点访问数。

可变 全局统计 为 新建 导航删除统计。

可变 已初始化 为 假。

可变 AI热点历史 为 一组：（空）。

可变 AI热点位置 为 0。

红黑树导航_初始化 接受 零参：
    若 已初始化 等于 真：
        返还 错误_已存在。
    完。
    AI热点历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI热点窗口：
        做 追加 用 AI热点历史、0。
        i 为 i 加 1。
    完。
    AI热点位置 为 0。
    已初始化 为 真。
    做 打印 用 「[红黑树导航] 初始化完成」。
    返还 0。
完。

红黑树导航_销毁 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    AI热点历史 为 一组：（空）。
    已初始化 为 假。
    做 打印 用 「[红黑树导航] 已销毁」。
    返还 0。
完。

红黑树_后继 接受 树、节点：
    若 节点 等于 空：
        全局统计 的 空节点访问数 为 全局统计 的 空节点访问数 加 1。
        返还 空。
    完。
    全局统计 的 后继查找次数 为 全局统计 的 后继查找次数 加 1。
    若 节点 的 第 RB_右 项 不等于 空：
        可变 n 为 节点 的 第 RB_右 项。
        只要 n 的 第 RB_左 项 不等于 空：
            n 为 n 的 第 RB_左 项。
        完。
        做 AI_记录热点 用 n 的 第 RB_键 项。
        返还 n。
    完。
    可变 当前 为 节点。
    可变 父 为 当前 的 第 RB_父 项。
    只要 父 不等于 空 并且 当前 等于 父 的 第 RB_右 项：
        当前 为 父。
        父 为 父 的 第 RB_父 项。
    完。
    若 父 不等于 空：
        做 AI_记录热点 用 父 的 第 RB_键 项。
    完。
    返还 父。
完。

红黑树_前驱 接受 树、节点：
    若 节点 等于 空：
        全局统计 的 空节点访问数 为 全局统计 的 空节点访问数 加 1。
        返还 空。
    完。
    全局统计 的 前驱查找次数 为 全局统计 的 前驱查找次数 加 1。
    若 节点 的 第 RB_左 项 不等于 空：
        可变 n 为 节点 的 第 RB_左 项。
        只要 n 的 第 RB_右 项 不等于 空：
            n 为 n 的 第 RB_右 项。
        完。
        返还 n。
    完。
    可变 当前 为 节点。
    可变 父 为 当前 的 第 RB_父 项。
    只要 父 不等于 空 并且 当前 等于 父 的 第 RB_左 项：
        当前 为 父。
        父 为 父 的 第 RB_父 项。
    完。
    返还 父。
完。

红黑树_移植 接受 树、u、v：
    若 树 等于 空 或者 u 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还。
    完。
    全局统计 的 移植次数 为 全局统计 的 移植次数 加 1。
    若 u 的 第 RB_父 项 等于 空：
        做 设置 用 树、0、v。
    否则若 u 等于 u 的 第 RB_父 项 的 第 RB_左 项：
        做 设置 用 u 的 第 RB_父 项、RB_左、v。
    否则：
        做 设置 用 u 的 第 RB_父 项、RB_右、v。
    完。
    若 v 不等于 空：
        做 设置 用 v、RB_父、u 的 第 RB_父 项。
    完。
完。

红黑树_删除 接受 树、节点：
    若 树 等于 空 或者 节点 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    全局统计 的 删除次数 为 全局统计 的 删除次数 加 1。
    可变 y 为 节点。
    可变 y原色 为 y 的 第 RB_色 项。
    可变 x 为 空。
    可变 x父 为 空。
    若 节点 的 第 RB_左 项 等于 空：
        x 为 节点 的 第 RB_右 项。
        x父 为 节点 的 第 RB_父 项。
        做 红黑树_移植 用 树、节点、节点 的 第 RB_右 项。
    否则若 节点 的 第 RB_右 项 等于 空：
        x 为 节点 的 第 RB_左 项。
        x父 为 节点 的 第 RB_父 项。
        做 红黑树_移植 用 树、节点、节点 的 第 RB_左 项。
    否则：
        y 为 节点 的 第 RB_右 项。
        只要 y 的 第 RB_左 项 不等于 空：
            y 为 y 的 第 RB_左 项。
        完。
        y原色 为 y 的 第 RB_色 项。
        x 为 y 的 第 RB_右 项。
        若 y 的 第 RB_父 项 等于 节点：
            x父 为 y。
        否则：
            x父 为 y 的 第 RB_父 项。
            做 红黑树_移植 用 树、y、y 的 第 RB_右 项。
            做 设置 用 y、RB_右、节点 的 第 RB_右 项。
            若 y 的 第 RB_右 项 不等于 空：
                做 设置 用 y 的 第 RB_右 项、RB_父、y。
            完。
        完。
        做 红黑树_移植 用 树、节点、y。
        做 设置 用 y、RB_左、节点 的 第 RB_左 项。
        若 y 的 第 RB_左 项 不等于 空：
            做 设置 用 y 的 第 RB_左 项、RB_父、y。
        完。
        做 设置 用 y、RB_色、节点 的 第 RB_色 项。
    完。
    做 设置 用 树、1、树 的 第 1 项 减 1。
    若 y原色 等于 黑：
        做 红黑树_删除修复 用 树、x、x父。
    完。
    返还 0。
完。

红黑树_按键删除 接受 树、键：
    若 树 等于 空：
        返还 错误_无效参数。
    完。
    可变 当前 为 树 的 第 0 项。
    只要 当前 不等于 空：
        若 键 小于 当前 的 第 RB_键 项：
            当前 为 当前 的 第 RB_左 项。
        否则若 键 大于 当前 的 第 RB_键 项：
            当前 为 当前 的 第 RB_右 项。
        否则：
            返还 做 红黑树_删除 用 树、当前。
        完。
    完。
    返还 错误_未找到。
完。

红黑树_删除修复 接受 树、x、x父：
    全局统计 的 删除修复次数 为 全局统计 的 删除修复次数 加 1。
    可变 节点 为 x。
    可变 父 为 x父。
    只要 (节点 等于 空 或者 节点 的 第 RB_色 项 等于 黑) 并且 节点 不等于 树 的 第 0 项：
        若 父 等于 空：
            止。
        完。
        若 节点 等于 父 的 第 RB_左 项 或者 节点 等于 空 并且 父 的 第 RB_左 项 等于 空：
            可变 兄 为 父 的 第 RB_右 项。
            若 兄 不等于 空 并且 兄 的 第 RB_色 项 等于 红：
                做 设置 用 兄、RB_色、黑。
                做 设置 用 父、RB_色、红。
                做 红黑树_左旋 用 树、父。
                全局统计 的 删除修复旋转 为 全局统计 的 删除修复旋转 加 1。
                兄 为 父 的 第 RB_右 项。
            完。
            兄左黑 为 兄 等于 空 或者 兄 的 第 RB_左 项 等于 空 或者 兄 的 第 RB_左 项 的 第 RB_色 项 等于 黑。
            兄右黑 为 兄 等于 空 或者 兄 的 第 RB_右 项 等于 空 或者 兄 的 第 RB_右 项 的 第 RB_色 项 等于 黑。
            若 兄左黑 并且 兄右黑：
                若 兄 不等于 空：
                    做 设置 用 兄、RB_色、红。
                完。
                节点 为 父。
                父 为 节点 的 第 RB_父 项。
            否则：
                若 兄右黑：
                    若 兄 不等于 空 并且 兄 的 第 RB_左 项 不等于 空：
                        做 设置 用 兄 的 第 RB_左 项、RB_色、黑。
                    完。
                    若 兄 不等于 空：
                        做 设置 用 兄、RB_色、红。
                    完。
                    做 红黑树_右旋 用 树、兄。
                    全局统计 的 删除修复旋转 为 全局统计 的 删除修复旋转 加 1。
                    兄 为 父 的 第 RB_右 项。
                完。
                若 兄 不等于 空：
                    做 设置 用 兄、RB_色、父 的 第 RB_色 项。
                完。
                做 设置 用 父、RB_色、黑。
                若 兄 不等于 空 并且 兄 的 第 RB_右 项 不等于 空：
                    做 设置 用 兄 的 第 RB_右 项、RB_色、黑。
                完。
                做 红黑树_左旋 用 树、父。
                全局统计 的 删除修复旋转 为 全局统计 的 删除修复旋转 加 1。
                节点 为 树 的 第 0 项。
                父 为 空。
            完。
        否则：
            可变 兄 为 父 的 第 RB_左 项。
            若 兄 不等于 空 并且 兄 的 第 RB_色 项 等于 红：
                做 设置 用 兄、RB_色、黑。
                做 设置 用 父、RB_色、红。
                做 红黑树_右旋 用 树、父。
                全局统计 的 删除修复旋转 为 全局统计 的 删除修复旋转 加 1。
                兄 为 父 的 第 RB_左 项。
            完。
            兄左黑 为 兄 等于 空 或者 兄 的 第 RB_左 项 等于 空 或者 兄 的 第 RB_左 项 的 第 RB_色 项 等于 黑。
            兄右黑 为 兄 等于 空 或者 兄 的 第 RB_右 项 等于 空 或者 兄 的 第 RB_右 项 的 第 RB_色 项 等于 黑。
            若 兄左黑 并且 兄右黑：
                若 兄 不等于 空：
                    做 设置 用 兄、RB_色、红。
                完。
                节点 为 父。
                父 为 节点 的 第 RB_父 项。
            否则：
                若 兄左黑：
                    若 兄 不等于 空 并且 兄 的 第 RB_右 项 不等于 空：
                        做 设置 用 兄 的 第 RB_右 项、RB_色、黑。
                    完。
                    若 兄 不等于 空：
                        做 设置 用 兄、RB_色、红。
                    完。
                    做 红黑树_左旋 用 树、兄。
                    全局统计 的 删除修复旋转 为 全局统计 的 删除修复旋转 加 1。
                    兄 为 父 的 第 RB_左 项。
                完。
                若 兄 不等于 空：
                    做 设置 用 兄、RB_色、父 的 第 RB_色 项。
                完。
                做 设置 用 父、RB_色、黑。
                若 兄 不等于 空 并且 兄 的 第 RB_左 项 不等于 空：
                    做 设置 用 兄 的 第 RB_左 项、RB_色、黑。
                完。
                做 红黑树_右旋 用 树、父。
                全局统计 的 删除修复旋转 为 全局统计 的 删除修复旋转 加 1。
                节点 为 树 的 第 0 项。
                父 为 空。
            完。
        完。
    完。
    若 节点 不等于 空：
        做 设置 用 节点、RB_色、黑。
    完。
完。

红黑树迭代器 是 一种 事物，含 树、当前节点、方向、已完成、步数。

迭代方向_正向 为 0。

迭代方向_反向 为 1。

红黑树_正向迭代器 接受 树：
    若 树 等于 空：
        返还 空。
    完。
    可变 迭代 为 新建 红黑树迭代器。
    迭代 的 树 为 树。
    迭代 的 方向 为 迭代方向_正向。
    迭代 的 已完成 为 假。
    迭代 的 步数 为 0。
    可变 节点 为 树 的 第 0 项。
    若 节点 不等于 空：
        只要 节点 的 第 RB_左 项 不等于 空：
            节点 为 节点 的 第 RB_左 项。
        完。
    完。
    迭代 的 当前节点 为 节点。
    全局统计 的 迭代器创建数 为 全局统计 的 迭代器创建数 加 1。
    返还 迭代。
完。

红黑树_反向迭代器 接受 树：
    若 树 等于 空：
        返还 空。
    完。
    可变 迭代 为 新建 红黑树迭代器。
    迭代 的 树 为 树。
    迭代 的 方向 为 迭代方向_反向。
    迭代 的 已完成 为 假。
    迭代 的 步数 为 0。
    可变 节点 为 树 的 第 0 项。
    若 节点 不等于 空：
        只要 节点 的 第 RB_右 项 不等于 空：
            节点 为 节点 的 第 RB_右 项。
        完。
    完。
    迭代 的 当前节点 为 节点。
    全局统计 的 迭代器创建数 为 全局统计 的 迭代器创建数 加 1。
    返还 迭代。
完。

迭代器_当前 接受 迭代：
    若 迭代 等于 空 或者 迭代 的 已完成 等于 真：
        返还 空。
    完。
    返还 迭代 的 当前节点。
完。

迭代器_下一个 接受 迭代：
    若 迭代 等于 空 或者 迭代 的 已完成 等于 真：
        返还 空。
    完。
    若 迭代 的 当前节点 等于 空：
        迭代 的 已完成 为 真。
        返还 空。
    完。
    全局统计 的 迭代器步进数 为 全局统计 的 迭代器步进数 加 1。
    迭代 的 步数 为 迭代 的 步数 加 1。
    若 迭代 的 方向 等于 迭代方向_正向：
        迭代 的 当前节点 为 做 红黑树_后继 用 迭代 的 树、迭代 的 当前节点。
    否则：
        迭代 的 当前节点 为 做 红黑树_前驱 用 迭代 的 树、迭代 的 当前节点。
    完。
    若 迭代 的 当前节点 等于 空：
        迭代 的 已完成 为 真。
    完。
    返还 迭代 的 当前节点。
完。

迭代器_是否结束 接受 迭代：
    若 迭代 等于 空：
        返还 真。
    完。
    返还 迭代 的 已完成。
完。

红黑树_序列化 接受 树：
    若 树 等于 空 或者 树 的 第 0 项 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 序列化次数 为 全局统计 的 序列化次数 加 1。
    可变 结果 为 一组：（空）。
    做 _序列化递归 用 树 的 第 0 项、结果。
    返还 结果。
完。

可变 _RB序列化深度 为 0。

_序列化递归 接受 节点、结果：
    若 节点 等于 空：
        返还。
    完。
    _RB序列化深度 为 _RB序列化深度 加 1。
    若 _RB序列化深度 大于 10000：
        _RB序列化深度 为 _RB序列化深度 减 1。
        返还。
    完。
    做 _序列化递归 用 节点 的 第 RB_左 项、结果。
    做 追加 用 结果、一组：节点 的 第 RB_键 项、节点 的 第 RB_值 项。
    做 _序列化递归 用 节点 的 第 RB_右 项、结果。
    _RB序列化深度 为 _RB序列化深度 减 1。
完。

红黑树_从有序列表 接受 有序列表：
    若 有序列表 等于 空 或者 做 计数 用 有序列表 等于 0：
        返还 空。
    完。
    全局统计 的 反序列化次数 为 全局统计 的 反序列化次数 加 1。
    可变 树 为 一组：空、0、空。
    可变 i 为 0。
    只要 i 小于 做 计数 用 有序列表：
        对 为 有序列表 的 第 i 项。
        做 红黑树_插入 用 树、对 的 第 0 项、对 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 树。
完。

红黑树_批量删除 接受 树、键列表：
    若 树 等于 空 或者 键列表 等于 空：
        返还 错误_无效参数。
    完。
    全局统计 的 批量删除次数 为 全局统计 的 批量删除次数 加 1。
    可变 已删除 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        结果 为 做 红黑树_按键删除 用 树、键列表 的 第 i 项。
        若 结果 等于 0：
            已删除 为 已删除 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 已删除。
完。

红黑树_过滤删除 接受 树、判断函数：
    若 树 等于 空 或者 判断函数 等于 空：
        返还 错误_无效参数。
    完。
    全局统计 的 过滤删除次数 为 全局统计 的 过滤删除次数 加 1。
    可变 待删除 为 一组：（空）。
    可变 迭代 为 做 红黑树_正向迭代器 用 树。
    只要 做 迭代器_是否结束 用 迭代 等于 假：
        当前 为 做 迭代器_当前 用 迭代。
        若 当前 不等于 空 并且 做 判断函数 用 当前 的 第 RB_键 项、当前 的 第 RB_值 项 等于 真：
            做 追加 用 待删除、当前 的 第 RB_键 项。
        完。
        做 迭代器_下一个 用 迭代。
    完。
    返还 做 红黑树_批量删除 用 树、待删除。
完。

红黑树_删除范围 接受 树、下界、上界：
    若 树 等于 空：
        返还 错误_无效参数。
    完。
    可变 待删除 为 一组：（空）。
    可变 当前 为 树 的 第 0 项。
    做 _收集范围键 用 当前、下界、上界、待删除。
    返还 做 红黑树_批量删除 用 树、待删除。
完。

_收集范围键 接受 节点、下界、上界、结果：
    若 节点 等于 空：
        返还。
    完。
    若 节点 的 第 RB_键 项 大于 下界：
        做 _收集范围键 用 节点 的 第 RB_左 项、下界、上界、结果。
    完。
    若 节点 的 第 RB_键 项 大于或等于 下界 并且 节点 的 第 RB_键 项 小于或等于 上界：
        做 追加 用 结果、节点 的 第 RB_键 项。
    完。
    若 节点 的 第 RB_键 项 小于 上界：
        做 _收集范围键 用 节点 的 第 RB_右 项、下界、上界、结果。
    完。
完。

AI_记录热点 接受 键：
    做 设置 用 AI热点历史、AI热点位置、键。
    AI热点位置 为 (AI热点位置 加 1) 取余于 AI热点窗口。
    全局统计 的 AI热点记录数 为 全局统计 的 AI热点记录数 加 1。
完。

AI_获取最热节点键 接受 零参：
    可变 最频键 为 0。
    可变 最频次 为 0。
    可变 i 为 0。
    只要 i 小于 AI热点窗口：
        若 AI热点历史 的 第 i 项 等于 0：
            i 为 i 加 1。
            继续。
        完。
        当前键 为 AI热点历史 的 第 i 项。
        可变 频次 为 0。
        可变 j 为 0。
        只要 j 小于 AI热点窗口：
            若 AI热点历史 的 第 j 项 等于 当前键：
                频次 为 频次 加 1。
            完。
            j 为 j 加 1。
        完。
        若 频次 大于 最频次：
            最频次 为 频次。
            最频键 为 当前键。
        完。
        i 为 i 加 1。
    完。
    若 最频次 大于 AI热点窗口 除以 3：
        全局统计 的 AI热点命中数 为 全局统计 的 AI热点命中数 加 1。
    完。
    返还 最频键。
完。

AI_建议缓存节点 接受 树：
    热键 为 做 AI_获取最热节点键。
    若 热键 等于 0：
        返还 一组：（空）。
    完。
    可变 建议 为 一组：（空）。
    做 追加 用 建议、热键。
    可变 当前 为 树 的 第 0 项。
    只要 当前 不等于 空：
        若 热键 小于 当前 的 第 RB_键 项：
            当前 为 当前 的 第 RB_左 项。
        否则若 热键 大于 当前 的 第 RB_键 项：
            当前 为 当前 的 第 RB_右 项。
        否则：
            后继 为 做 红黑树_后继 用 树、当前。
            前驱 为 做 红黑树_前驱 用 树、当前。
            若 后继 不等于 空：
                做 追加 用 建议、后继 的 第 RB_键 项。
            完。
            若 前驱 不等于 空：
                做 追加 用 建议、前驱 的 第 RB_键 项。
            完。
            返还 建议。
        完。
    完。
    返还 建议。
完。

红黑树导航_转储 接受 零参：
    做 打印 用 「[红黑树导航] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  后继查找: 」、做 转字符串 用 全局统计 的 后继查找次数。
    做 打印 用 做 字符串拼接 用 「  前驱查找: 」、做 转字符串 用 全局统计 的 前驱查找次数。
    做 打印 用 做 字符串拼接 用 「  删除次数: 」、做 转字符串 用 全局统计 的 删除次数。
    做 打印 用 做 字符串拼接 用 「  迭代器创建: 」、做 转字符串 用 全局统计 的 迭代器创建数。
    做 打印 用 做 字符串拼接 用 「  AI热点: 」、做 转字符串 用 做 AI_获取最热节点键。
    返还 0。
完。

红黑树导航_打印统计 接受 零参：
    做 打印 用 「[红黑树导航] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  后继查找: 」、做 转字符串 用 全局统计 的 后继查找次数。
    做 打印 用 做 字符串拼接 用 「  前驱查找: 」、做 转字符串 用 全局统计 的 前驱查找次数。
    做 打印 用 做 字符串拼接 用 「  移植次数: 」、做 转字符串 用 全局统计 的 移植次数。
    做 打印 用 做 字符串拼接 用 「  删除次数: 」、做 转字符串 用 全局统计 的 删除次数。
    做 打印 用 做 字符串拼接 用 「  删除修复: 」、做 转字符串 用 全局统计 的 删除修复次数。
    做 打印 用 做 字符串拼接 用 「  删除旋转: 」、做 转字符串 用 全局统计 的 删除修复旋转。
    做 打印 用 做 字符串拼接 用 「  迭代器创建: 」、做 转字符串 用 全局统计 的 迭代器创建数。
    做 打印 用 做 字符串拼接 用 「  迭代器步进: 」、做 转字符串 用 全局统计 的 迭代器步进数。
    做 打印 用 做 字符串拼接 用 「  序列化: 」、做 转字符串 用 全局统计 的 序列化次数。
    做 打印 用 做 字符串拼接 用 「  反序列化: 」、做 转字符串 用 全局统计 的 反序列化次数。
    做 打印 用 做 字符串拼接 用 「  批量删除: 」、做 转字符串 用 全局统计 的 批量删除次数。
    做 打印 用 做 字符串拼接 用 「  过滤删除: 」、做 转字符串 用 全局统计 的 过滤删除次数。
    做 打印 用 做 字符串拼接 用 「  AI热点记录: 」、做 转字符串 用 全局统计 的 AI热点记录数。
    做 打印 用 做 字符串拼接 用 「  AI热点命中: 」、做 转字符串 用 全局统计 的 AI热点命中数。
    做 打印 用 做 字符串拼接 用 「  空节点访问: 」、做 转字符串 用 全局统计 的 空节点访问数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

红黑树导航_注册到内核 接受 内核上下文：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    做 打印 用 「[红黑树导航] 已注册到内核上下文」。
    返还 0。
完。

红黑树导航_查询状态 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

红黑树导航_健康检查 接受 零参：
    若 已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 后继查找次数 加 全局统计 的 删除次数：
        返还 假。
    完。
    返还 真。
完。

错误_已存在 为 0 减 17。

错误_未初始化 为 0 减 1。

错误_无效参数 为 0 减 22。

错误_未找到 为 0 减 2。

本章 收束。
