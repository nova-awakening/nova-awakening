《输入系统调用》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·自动扩容》。

引用《..·标准·文件》。

引用《..·感知·内核直连层》。

引用《.·进程fork创建》。

引用《.·字符串替换》。

引用《.·内存初始化》。

引用《..·记忆·文件描述符》。

引用《.·释放文件表》。

引用《.·注册扩展系统》。

引用《.·网络系统调用》。

引用《.·每处理器》。

调用号_管道 为 22。

调用号_复制描述 为 32。

调用号_复制指定 为 33。

调用号_设备控制 为 16。

调用号_轮询 为 7。

调用号_选择 为 23。

调用号_非阻塞控制 为 72。

最大轮询项 为 256。

管道 是 一种 事物，含 读端、写端、缓冲区、缓冲大小、读位置、写位置、数据量、读者数、写者数。

轮询项 是 一种 事物，含 描述符、监听事件、触发事件。

可变 输入调用总数 为 0。

可变 管道创建数 为 0。

可变 复制描述数 为 0。

可变 设备控制数 为 0。

可变 轮询调用数 为 0。

注册输入系统调用 接受 零参：
    做 系统调用_注册 用 调用号_管道、系统_创建管道。
    做 系统调用_注册 用 调用号_复制描述、系统_复制描述。
    做 系统调用_注册 用 调用号_复制指定、系统_复制指定描述。
    做 系统调用_注册 用 调用号_设备控制、系统_设备控制。
    做 系统调用_注册 用 调用号_轮询、系统_轮询。
    做 系统调用_注册 用 调用号_选择、系统_选择。
完。

系统_创建管道 接受 文件描述对：
    输入调用总数 为 输入调用总数 加 1。
    管道创建数 为 管道创建数 加 1。
    管 为 做 堆_分配 用 管道。
    管 的 缓冲大小 为 4096。
    管 的 缓冲区 为 做 堆_分配 用 管 的 缓冲大小。
    管 的 读位置 为 0。
    管 的 写位置 为 0。
    管 的 数据量 为 0。
    管 的 读者数 为 1。
    管 的 写者数 为 1。
    读描述 为 做 文件表_分配 用 做 获取当前进程。
    写描述 为 做 文件表_分配 用 做 获取当前进程。
    若 读描述 小于 0 或者 写描述 小于 0：
        做 堆_释放 用 管 的 缓冲区。
        做 堆_释放 用 管。
        返还 错误_无资源。
    完。
    管 的 读端 为 读描述。
    管 的 写端 为 写描述。
    做 内存_写32 用 文件描述对、读描述。
    做 内存_写32 用 文件描述对 加 4、写描述。
    返还 0。
完。

系统_复制描述 接受 旧描述：
    输入调用总数 为 输入调用总数 加 1。
    复制描述数 为 复制描述数 加 1。
    进程 为 做 获取当前进程。
    新描述 为 做 文件表_分配 用 进程。
    若 新描述 小于 0：
        返还 错误_无资源。
    完。
    文件 为 做 文件表_获取 用 进程、旧描述。
    若 文件 等于 0：
        返还 错误_无效参数。
    完。
    做 文件表_设置 用 进程、新描述、文件。
    做 文件_增引用 用 文件。
    返还 新描述。
完。

系统_复制指定描述 接受 旧描述、新描述：
    输入调用总数 为 输入调用总数 加 1。
    复制描述数 为 复制描述数 加 1。
    若 旧描述 等于 新描述：
        返还 新描述。
    完。
    进程 为 做 获取当前进程。
    已有 为 做 文件表_获取 用 进程、新描述。
    若 已有 不等于 0：
        做 文件_关闭 用 已有。
    完。
    文件 为 做 文件表_获取 用 进程、旧描述。
    若 文件 等于 0：
        返还 错误_无效参数。
    完。
    做 文件表_设置 用 进程、新描述、文件。
    做 文件_增引用 用 文件。
    返还 新描述。
完。

系统_设备控制 接受 描述符、命令、参数：
    输入调用总数 为 输入调用总数 加 1。
    设备控制数 为 设备控制数 加 1。
    文件 为 做 文件表_获取 用 做 获取当前进程、描述符。
    若 文件 等于 0：
        返还 错误_无效参数。
    完。
    若 文件 的 设备控制 不等于 0：
        返还 做 文件 的 设备控制 用 文件、命令、参数。
    完。
    返还 错误_不支持。
完。

系统_轮询 接受 轮询数组、数量、超时毫秒：
    输入调用总数 为 输入调用总数 加 1。
    轮询调用数 为 轮询调用数 加 1。
    若 数量 大于 最大轮询项：
        返还 错误_无效参数。
    完。
    可变 就绪数 为 0。
    可变 序号 为 0。
    只要 序号 小于 数量：
        偏移 为 轮询数组 加 序号 乘 8。
        描述符 为 做 内存_读32 用 偏移。
        监听 为 做 内存_读16 用 偏移 加 4。
        可变 触发 为 0。
        文件 为 做 文件表_获取 用 做 获取当前进程、描述符。
        若 文件 不等于 0：
            若 监听 位与 1 不等于 0 并且 做 文件 的 可读 用 文件：
                触发 为 触发 位或 1。
            完。
            若 监听 位与 4 不等于 0 并且 做 文件 的 可写 用 文件：
                触发 为 触发 位或 4。
            完。
        否则：
            触发 为 32。
        完。
        做 内存_写16 用 偏移 加 6、触发。
        若 触发 不等于 0：
            就绪数 为 就绪数 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 就绪数。
完。

系统_选择 接受 最大描述、读集、写集、异常集、超时：
    输入调用总数 为 输入调用总数 加 1。
    可变 就绪数 为 0。
    可变 描述 为 0。
    只要 描述 小于 最大描述：
        文件 为 做 文件表_获取 用 做 获取当前进程、描述。
        若 文件 不等于 0：
            若 读集 不等于 0 并且 做 文件 的 可读 用 文件：
                就绪数 为 就绪数 加 1。
            完。
            若 写集 不等于 0 并且 做 文件 的 可写 用 文件：
                就绪数 为 就绪数 加 1。
            完。
        完。
        描述 为 描述 加 1。
    完。
    返还 就绪数。
完。

可变 输入_已初始化 为 假。

可变 pipe2_次数 为 0。

可变 ioctl_次数 为 0。

可变 splice_次数 为 0。

可变 tee_次数 为 0。

可变 sendfile_次数_IO 为 0。

可变 copy_range_次数 为 0。

可变 输入_错误数 为 0。

可变 输入_延迟总计 为 0。

可变 输入_延迟样本 为 0。

可变 输入_最大延迟 为 0。

可变 输入_速率 为 0。

可变 输入_速率窗口 为 0。

可变 输入_速率时刻 为 0。

可变 输入_峰值速率 为 0。

O_CLOEXEC_IO 为 0x80000。

O_NONBLOCK 为 0x800。

FIONREAD 为 0x541B。

FIONBIO 为 0x5421。

TIOCGWINSZ 为 0x5413。

TIOCSWINSZ 为 0x5414。

TCGETS 为 0x5401。

TCSETS 为 0x5402。

sys_pipe2 接受 pipefd、flags、_c、_d、_e、_f：
    若 pipefd 等于 空：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_无效参数_IO。
    完。
    读fd 为 做 分配FD_IO。
    写fd 为 做 分配FD_IO。
    若 读fd 小于 0 或者 写fd 小于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_内存不足_IO。
    完。
    做 设置 用 pipefd、0、读fd。
    做 设置 用 pipefd、1、写fd。
    pipe2_次数 为 pipe2_次数 加 1。
    管道创建数 为 管道创建数 加 1。
    输入_速率窗口 为 输入_速率窗口 加 1。
    返还 0。
完。

sys_ioctl_增强 接受 fd、cmd、arg、_d、_e、_f：
    若 fd 小于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_坏文件描述符_IO。
    完。
    ioctl_次数 为 ioctl_次数 加 1。
    设备控制数 为 设备控制数 加 1。
    输入_速率窗口 为 输入_速率窗口 加 1。
    若 cmd 等于 FIONREAD：
        若 arg 不等于 空：
            做 设置 用 arg、0、0。
        完。
        返还 0。
    完。
    若 cmd 等于 FIONBIO：
        返还 0。
    完。
    若 cmd 等于 TIOCGWINSZ：
        若 arg 不等于 空：
            做 设置 用 arg、0、25。
            做 设置 用 arg、1、80。
            做 设置 用 arg、2、0。
            做 设置 用 arg、3、0。
        完。
        返还 0。
    完。
    若 cmd 等于 TCGETS：
        返还 0。
    完。
    若 cmd 等于 TCSETS：
        返还 0。
    完。
    返还 错误_不支持操作_IO。
完。

sys_splice 接受 fd_in、off_in、fd_out、off_out、len、flags：
    若 fd_in 小于 0 或者 fd_out 小于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_坏文件描述符_IO。
    完。
    若 len 小于或等于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_无效参数_IO。
    完。
    splice_次数 为 splice_次数 加 1。
    输入_速率窗口 为 输入_速率窗口 加 1。
    返还 len。
完。

sys_tee 接受 fd_in、fd_out、len、flags、_e、_f：
    若 fd_in 小于 0 或者 fd_out 小于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_坏文件描述符_IO。
    完。
    tee_次数 为 tee_次数 加 1。
    输入_速率窗口 为 输入_速率窗口 加 1。
    返还 len。
完。

sys_copy_file_range 接受 fd_in、off_in、fd_out、off_out、len、flags：
    若 fd_in 小于 0 或者 fd_out 小于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_坏文件描述符_IO。
    完。
    copy_range_次数 为 copy_range_次数 加 1。
    输入_速率窗口 为 输入_速率窗口 加 1。
    返还 len。
完。

输入_记录延迟 接受 延迟：
    输入_延迟样本 为 输入_延迟样本 加 1。
    输入_延迟总计 为 输入_延迟总计 加 延迟。
    若 延迟 大于 输入_最大延迟：
        输入_最大延迟 为 延迟。
    完。
完。

输入_平均延迟 接受 零参：
    若 输入_延迟样本 等于 0：
        返还 0。
    完。
    返还 输入_延迟总计 除以 输入_延迟样本。
完。

输入_更新速率 接受 零参：
    现在 为 做 读取纳秒_IO。
    间隔 为 现在 减 输入_速率时刻。
    若 间隔 大于或等于 1000000000：
        输入_速率 为 输入_速率窗口 乘 1000000000 除以 间隔。
        若 输入_速率 大于 输入_峰值速率：
            输入_峰值速率 为 输入_速率。
        完。
        输入_速率窗口 为 0。
        输入_速率时刻 为 现在。
    完。
完。

输入_初始化 接受 零参：
    若 输入_已初始化 等于 真：
        返还 0 减 17。
    完。
    输入_已初始化 为 真。
    做 打印 用 「[输入系统调用] 初始化完成」。
    返还 0。
完。

输入系统调用_完整统计 接受 零参：
    总操作 为 输入调用总数 加 管道创建数 加 复制描述数 加 设备控制数 加 轮询调用数 加 pipe2_次数 加 ioctl_次数 加 splice_次数 加 tee_次数 加 copy_range_次数。
    统计 为 做 字典。
    做 字典设 用 统计、「已初始化」、输入_已初始化。
    做 字典设 用 统计、「总调用数」、输入调用总数。
    做 字典设 用 统计、「管道创建」、管道创建数。
    做 字典设 用 统计、「pipe2」、pipe2_次数。
    做 字典设 用 统计、「复制描述」、复制描述数。
    做 字典设 用 统计、「设备控制」、设备控制数。
    做 字典设 用 统计、「ioctl」、ioctl_次数。
    做 字典设 用 统计、「轮询调用」、轮询调用数。
    做 字典设 用 统计、「splice」、splice_次数。
    做 字典设 用 统计、「tee」、tee_次数。
    做 字典设 用 统计、「copy_range」、copy_range_次数。
    做 字典设 用 统计、「总操作」、总操作。
    做 字典设 用 统计、「平均延迟(ns)」、做 输入_平均延迟。
    做 字典设 用 统计、「最大延迟(ns)」、输入_最大延迟。
    做 字典设 用 统计、「速率」、输入_速率。
    做 字典设 用 统计、「峰值速率」、输入_峰值速率。
    做 字典设 用 统计、「错误数」、输入_错误数。
    返还 统计。
完。

输入系统调用_打印统计 接受 零参：
    统计 为 做 输入系统调用_完整统计。
    做 打印 用 「[输入系统调用] ═══════ 完整统计 ═══════」。
    做 打印 用 「  总调用:       」 加 做 转字符串 用 做 字典取 用 统计、「总调用数」。
    做 打印 用 「  管道创建:     」 加 做 转字符串 用 做 字典取 用 统计、「管道创建」。
    做 打印 用 「  pipe2:        」 加 做 转字符串 用 做 字典取 用 统计、「pipe2」。
    做 打印 用 「  设备控制:     」 加 做 转字符串 用 做 字典取 用 统计、「设备控制」。
    做 打印 用 「  ioctl:        」 加 做 转字符串 用 做 字典取 用 统计、「ioctl」。
    做 打印 用 「  轮询:         」 加 做 转字符串 用 做 字典取 用 统计、「轮询调用」。
    做 打印 用 「  splice:       」 加 做 转字符串 用 做 字典取 用 统计、「splice」。
    做 打印 用 「  tee:          」 加 做 转字符串 用 做 字典取 用 统计、「tee」。
    做 打印 用 「  copy_range:   」 加 做 转字符串 用 做 字典取 用 统计、「copy_range」。
    做 打印 用 「  平均延迟:     」 加 做 转字符串 用 做 字典取 用 统计、「平均延迟(ns)」 加 「ns」。
    做 打印 用 「  速率:         」 加 做 转字符串 用 做 字典取 用 统计、「速率」 加 「 次/秒」。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[输入系统调用] ═════════════════════════」。
    返还 0。
完。

输入系统调用_dump 接受 零参：
    做 打印 用 「[输入IO dump] ──────────────────」。
    做 打印 用 「  总调用: 」 加 做 转字符串 用 输入调用总数。
    做 打印 用 「  管道: 」 加 做 转字符串 用 管道创建数。
    做 打印 用 「  ioctl: 」 加 做 转字符串 用 ioctl_次数。
    做 打印 用 「  splice: 」 加 做 转字符串 用 splice_次数。
    做 打印 用 「  速率: 」 加 做 转字符串 用 输入_速率。
    做 打印 用 「  错误: 」 加 做 转字符串 用 输入_错误数。
    做 打印 用 「[输入IO dump] ──────────────────」。
完。

输入系统调用_销毁 接受 零参：
    输入_已初始化 为 假。
    返还 0。
完。

输入系统调用_为procfs提供统计 接受 零参：
    返还 一组：输入调用总数、管道创建数、复制描述数、设备控制数、轮询调用数、pipe2_次数、ioctl_次数、splice_次数、tee_次数、copy_range_次数。
完。

输入系统调用_为AI提供IO模式 接受 零参：
    返还 一组：输入调用总数、管道创建数、ioctl_次数、splice_次数、做 输入_平均延迟、输入_速率。
完。

输入系统调用_为调度器提供IO负载 接受 零参：
    返还 一组：输入_速率、输入_峰值速率、输入调用总数。
完。

输入系统调用_重置统计 接受 零参：
    输入调用总数 为 0。
    管道创建数 为 0。
    复制描述数 为 0。
    设备控制数 为 0。
    轮询调用数 为 0。
    pipe2_次数 为 0。
    ioctl_次数 为 0。
    splice_次数 为 0。
    tee_次数 为 0。
    copy_range_次数 为 0。
    输入_错误数 为 0。
    输入_延迟总计 为 0。
    输入_延迟样本 为 0。
    输入_最大延迟 为 0。
    输入_速率 为 0。
    输入_速率窗口 为 0。
    输入_峰值速率 为 0。
    做 打印 用 「[输入系统调用] 统计已重置」。
    返还 0。
完。

分配FD_IO 接受 零参：
    返还 3。
完。

错误_无效参数_IO 为 0 减 22。

错误_内存不足_IO 为 0 减 12。

错误_坏文件描述符_IO 为 0 减 9。

错误_不支持操作_IO 为 0 减 95。

读取纳秒_IO 接受 零参：
    可变 值 为 0。
    汇编。
    块：
        「rdtsc」。
        「shl rdx, 32」。
        「or rax, rdx」。
        「mov [rbp-8], rax」。
    完。
    返还 值。
完。

可变 fallocate_次数 为 0。

可变 fadvise_次数 为 0。

可变 readahead_次数 为 0。

可变 sync_次数 为 0。

可变 fsync_次数 为 0。

可变 fdatasync_次数 为 0。

FALLOC_FL_KEEP_SIZE 为 0x01。

FALLOC_FL_PUNCH_HOLE 为 0x02。

POSIX_FADV_NORMAL 为 0。

POSIX_FADV_SEQUENTIAL 为 2。

POSIX_FADV_RANDOM 为 1。

POSIX_FADV_WILLNEED 为 3。

POSIX_FADV_DONTNEED 为 4。

sys_fallocate 接受 fd、mode、offset、len、_e、_f：
    若 fd 小于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_坏文件描述符_IO。
    完。
    若 len 小于或等于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_无效参数_IO。
    完。
    fallocate_次数 为 fallocate_次数 加 1。
    输入_速率窗口 为 输入_速率窗口 加 1。
    返还 0。
完。

sys_fadvise64 接受 fd、offset、len、advice、_e、_f：
    若 fd 小于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_坏文件描述符_IO。
    完。
    fadvise_次数 为 fadvise_次数 加 1。
    输入_速率窗口 为 输入_速率窗口 加 1。
    返还 0。
完。

sys_readahead 接受 fd、offset、count、_d、_e、_f：
    若 fd 小于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_坏文件描述符_IO。
    完。
    若 count 小于或等于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_无效参数_IO。
    完。
    readahead_次数 为 readahead_次数 加 1。
    输入_速率窗口 为 输入_速率窗口 加 1。
    返还 0。
完。

sys_sync 接受 _a、_b、_c、_d、_e、_f：
    sync_次数 为 sync_次数 加 1。
    返还 0。
完。

sys_fsync 接受 fd、_b、_c、_d、_e、_f：
    若 fd 小于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_坏文件描述符_IO。
    完。
    fsync_次数 为 fsync_次数 加 1。
    输入_速率窗口 为 输入_速率窗口 加 1。
    返还 0。
完。

sys_fdatasync 接受 fd、_b、_c、_d、_e、_f：
    若 fd 小于 0：
        输入_错误数 为 输入_错误数 加 1。
        返还 错误_坏文件描述符_IO。
    完。
    fdatasync_次数 为 fdatasync_次数 加 1。
    输入_速率窗口 为 输入_速率窗口 加 1。
    返还 0。
完。

输入_为文件系统提供IO模式 接受 零参：
    返还 一组：readahead_次数、fadvise_次数、fsync_次数、fdatasync_次数、splice_次数。
完。

输入_为块设备提供同步请求 接受 零参：
    返还 一组：sync_次数、fsync_次数、fdatasync_次数。
完。

输入_为AI提供IO优化模式 接受 零参：
    总IO 为 输入调用总数 加 splice_次数 加 tee_次数 加 copy_range_次数。
    返还 一组：总IO、readahead_次数、fadvise_次数、splice_次数、做 输入_平均延迟。
完。

输入_为电源管理提供IO活跃度 接受 零参：
    返还 一组：输入_速率、输入_峰值速率、fsync_次数 加 fdatasync_次数。
完。

输入_获取同步IO比例 接受 零参：
    总 为 输入调用总数。
    若 总 等于 0：
        返还 0。
    完。
    返还 (fsync_次数 加 fdatasync_次数 加 sync_次数) 乘 100 除以 总。
完。

输入_获取零拷贝IO比例 接受 零参：
    总 为 输入调用总数。
    若 总 等于 0：
        返还 0。
    完。
    返还 (splice_次数 加 tee_次数 加 copy_range_次数) 乘 100 除以 总。
完。

输入_获取总操作数 接受 零参：
    返还 输入调用总数 加 管道创建数 加 复制描述数 加 设备控制数 加 轮询调用数 加 pipe2_次数 加 ioctl_次数 加 splice_次数 加 tee_次数 加 copy_range_次数 加 fallocate_次数 加 fadvise_次数 加 readahead_次数 加 sync_次数 加 fsync_次数 加 fdatasync_次数。
完。

输入_注册全部 接受 零参：
    返还 0。
    返还 0。
完。

输入_为调度器提供IO密度 接受 零参：
    返还 一组：输入_速率、fsync_次数 加 fdatasync_次数、splice_次数 加 tee_次数。
完。

本章 收束。
