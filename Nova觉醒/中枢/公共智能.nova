《公共智能》

引用《.·进程信号》。

引用《.·进程调度》。

引用《.·内存物理》。

引用《.·AI会话》。

引用《..·执行·内核智能》。

公智档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、灵敏数、睡眠数、调质数、稳态数、突触数、路由数、脉冲数、会话数、巡检数、成功数、失败数、最近通道、最近会话、最近信号、灵敏表、调质表、会话表。

公智请求 是 一种 事物，含 动作、目标、参数、选项。

公智回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

公智摘要 是 一种 事物，含 名称、灵敏数、睡眠数、调质数、稳态数、突触数、路由数、脉冲数、会话数、巡检数、成功数、失败数、健康。

灵敏项 是 一种 事物，含 名称、阈值、增益、噪声门、用途。

调质项 是 一种 事物，含 名称、值、范围、作用。

会话项 是 一种 事物，含 标识、底层会话ID、角色、状态、关注、记忆。

可变 公智账本 为 一组：（空）。

可变 公智构建数 为 0。

可变 公智总动作 为 0。

可变 公智总成功 为 0。

可变 公智总失败 为 0。

公共智能_建档 接受 名称：
    档 为 新建 公智档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 灵敏数 为 0。
    档 的 睡眠数 为 0。
    档 的 调质数 为 0。
    档 的 稳态数 为 0。
    档 的 突触数 为 0。
    档 的 路由数 为 0。
    档 的 脉冲数 为 0。
    档 的 会话数 为 0。
    档 的 巡检数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    档 的 最近通道 为 「」。
    档 的 最近会话 为 「」。
    档 的 最近信号 为 「」。
    档 的 灵敏表 为 一组：（空）。
    档 的 调质表 为 一组：（空）。
    档 的 会话表 为 一组：（空）。
    返还 档。
完。

公共智能_登记 接受 档：
    做 追加 用 公智账本、档。
    公智构建数 为 公智构建数 加 1。
    返还 档。
完。

公共智能_完整统计 接受 零参：
    返还 一组：公智构建数、公智总动作、公智总成功、公智总失败。
完。

公共智能_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 公智回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

公共智能_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    公智总动作 为 公智总动作 加 1。
完。

公共智能_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 会话数 等于 0 或者 档 的 调质数 等于 0：
        档 的 最近健康 为 「预热」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 0：
        档 的 最近健康 为 「监视」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「运行」。
    返还 档 的 最近健康。
完。

公共智能_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 档 的 名称 等于 「」：
        返还 假。
    完。
    返还 真。
完。

公共智能_成功 接受 档、来源、数据：
    档 的 最近结果 为 数据。
    档 的 成功数 为 档 的 成功数 加 1。
    公智总成功 为 公智总成功 加 1。
    档 的 最近统计 为 做 公共智能_完整统计。
    做 公共智能_刷新健康 用 档。
    返还 做 公共智能_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

公共智能_失败 接受 档、来源、错因：
    若 档 不等于 空：
        档 的 最近错误 为 错因。
        档 的 失败数 为 档 的 失败数 加 1。
        档 的 最近统计 为 做 公共智能_完整统计。
        做 公共智能_刷新健康 用 档。
    完。
    公智总失败 为 公智总失败 加 1。
    返还 做 公共智能_建回证 用 真、假、来源、错因、空、做 公共智能_完整统计。
完。

公共智能_加灵敏 接受 档、名称、阈值、增益、噪声门、用途：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「加灵敏」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「加灵敏」、一组：名称、阈值、增益、噪声门、用途、「公共智能」。
    项 为 新建 灵敏项。
    项 的 名称 为 名称。
    项 的 阈值 为 阈值。
    项 的 增益 为 增益。
    项 的 噪声门 为 噪声门。
    项 的 用途 为 用途。
    做 追加 用 档 的 灵敏表、项。
    档 的 灵敏数 为 档 的 灵敏数 加 1。
    档 的 最近通道 为 名称。
    返还 做 公共智能_成功 用 档、「加灵敏」、项。
完。

公共智能_调质 接受 档、名称、值、范围、作用：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「调质」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「调质」、一组：名称、值、范围、作用、「公共智能」。
    项 为 新建 调质项。
    项 的 名称 为 名称。
    项 的 值 为 值。
    项 的 范围 为 范围。
    项 的 作用 为 作用。
    做 追加 用 档 的 调质表、项。
    档 的 调质数 为 档 的 调质数 加 1。
    档 的 最近通道 为 名称。
    返还 做 公共智能_成功 用 档、「调质」、项。
完。

公共智能_建会话 接受 档、标识、角色、关注：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「建会话」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「建会话」、一组：标识、角色、关注、「公共智能」。
    做 会话_确保已初始化。
    项 为 新建 会话项。
    项 的 标识 为 标识。
    项 的 底层会话ID 为 做 会话_创建 用 0。
    项 的 角色 为 角色。
    项 的 状态 为 「活跃」。
    项 的 关注 为 关注。
    项 的 记忆 为 一组：（空）。
    做 追加 用 档 的 会话表、项。
    档 的 会话数 为 档 的 会话数 加 1。
    档 的 最近会话 为 标识。
    返还 做 公共智能_成功 用 档、「建会话」、项。
完。

公共智能_找会话 接受 档、标识：
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 会话表：
        项 为 档 的 会话表 的 第 索引 项。
        若 项 的 标识 等于 标识：
            返还 项。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

公共智能_睡眠巩固 接受 档、会话标识、记忆片段：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「睡眠巩固」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「睡眠巩固」、一组：会话标识、记忆片段、「公共智能」。
    会话 为 做 公共智能_找会话 用 档、会话标识。
    若 会话 等于 空：
        返还 做 公共智能_失败 用 档、「睡眠巩固」、「会话不存在」。
    完。
    做 追加 用 会话 的 记忆、记忆片段。
    若 会话 的 底层会话ID 大于 0：
        做 会话_记录对话 用 会话 的 底层会话ID。
        做 会话_保存快照 用 会话 的 底层会话ID、0、会话 的 记忆。
    完。
    档 的 睡眠数 为 档 的 睡眠数 加 1。
    档 的 最近会话 为 会话标识。
    返还 做 公共智能_成功 用 档、「睡眠巩固」、会话 的 记忆。
完。

公共智能_稳态 接受 档、能量、温度、压力：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「稳态」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「稳态」、一组：能量、温度、压力、「公共智能」。
    档 的 稳态数 为 档 的 稳态数 加 1。
    结论 为 「稳定」。
    若 能量 小于 40 或者 温度 大于 78 或者 压力 大于 75：
        结论 为 「调节」。
    完。
    返还 做 公共智能_成功 用 档、「稳态」、一组：能量、温度、压力、结论。
完。

公共智能_突触网格 接受 档、前区、后区、权重、塑性：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「突触网格」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「突触网格」、一组：前区、后区、权重、塑性、「公共智能」。
    档 的 突触数 为 档 的 突触数 加 1。
    返还 做 公共智能_成功 用 档、「突触网格」、一组：前区、后区、权重、塑性。
完。

公共智能_经络路由 接受 档、通道名、频段、目的、阈值：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「经络路由」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「经络路由」、一组：通道名、频段、目的、阈值、「公共智能」。
    档 的 路由数 为 档 的 路由数 加 1。
    档 的 最近通道 为 通道名。
    返还 做 公共智能_成功 用 档、「经络路由」、一组：通道名、频段、目的、阈值。
完。

公共智能_脉冲 接受 档、通道名、强度、时长、来源：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「脉冲」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「脉冲」、一组：通道名、强度、时长、来源、「公共智能」。
    档 的 脉冲数 为 档 的 脉冲数 加 1。
    档 的 最近通道 为 通道名。
    返还 做 公共智能_成功 用 档、「脉冲」、一组：通道名、强度、时长、来源。
完。

公共智能_信号巡检 接受 档、名称：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「信号巡检」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「信号巡检」、一组：名称、「公共智能」。
    信号档 为 做 进程信号_登记 用 做 进程信号_建档 用 名称 加 「信号」。
    做 进程信号_初始化 用 信号档。
    摘要 为 做 进程信号_摘要 用 信号档。
    档 的 巡检数 为 档 的 巡检数 加 1。
    档 的 最近信号 为 摘要 的 名称。
    返还 做 公共智能_成功 用 档、「信号巡检」、摘要。
完。

公共智能_智能巡检 接受 档、名称：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「智能巡检」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「智能巡检」、一组：名称、「公共智能」。
    智档 为 做 内核智能_登记 用 做 内核智能_建档 用 名称 加 「内智」。
    做 内核智能_初始化 用 智档。
    摘要 为 做 内核智能_摘要 用 智档。
    档 的 巡检数 为 档 的 巡检数 加 1。
    返还 做 公共智能_成功 用 档、「智能巡检」、摘要。
完。

公共智能_物页巡检 接受 档、名称：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「物页巡检」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「物页巡检」、一组：名称、「公共智能」。
    物档 为 做 内存物理_登记 用 做 内存物理_建档 用 名称 加 「物页」。
    做 内存物理_初始化 用 物档。
    摘要 为 做 内存物理_摘要 用 物档。
    档 的 巡检数 为 档 的 巡检数 加 1。
    返还 做 公共智能_成功 用 档、「物页巡检」、摘要。
完。

公共智能_调度巡检 接受 档、名称：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「调度巡检」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「调度巡检」、一组：名称、「公共智能」。
    调档 为 做 进程调度_创建 用 名称 加 「调度」、4。
    档 的 巡检数 为 档 的 巡检数 加 1。
    返还 做 公共智能_成功 用 档、「调度巡检」、一组：做 进程调度_名称 用 调档、做 进程调度_核数 用 调档、做 进程调度_模式 用 调档。
完。

公共智能_汇总巡检 接受 档、名称：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「汇总巡检」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「汇总巡检」、一组：名称、「公共智能」。
    报告 为 一组：（空）。
    做 追加 用 报告、做 公共智能_信号巡检 用 档、名称 的 数据。
    做 追加 用 报告、做 公共智能_智能巡检 用 档、名称 的 数据。
    做 追加 用 报告、做 公共智能_物页巡检 用 档、名称 的 数据。
    做 追加 用 报告、做 公共智能_调度巡检 用 档、名称 的 数据。
    档 的 巡检数 为 档 的 巡检数 加 1。
    返还 做 公共智能_成功 用 档、「汇总巡检」、报告。
完。

公共智能_摘要 接受 档：
    摘要 为 新建 公智摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 灵敏数 为 档 的 灵敏数。
    摘要 的 睡眠数 为 档 的 睡眠数。
    摘要 的 调质数 为 档 的 调质数。
    摘要 的 稳态数 为 档 的 稳态数。
    摘要 的 突触数 为 档 的 突触数。
    摘要 的 路由数 为 档 的 路由数。
    摘要 的 脉冲数 为 档 的 脉冲数。
    摘要 的 会话数 为 档 的 会话数。
    摘要 的 巡检数 为 档 的 巡检数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 公共智能_刷新健康 用 档。
    返还 摘要。
完。

公共智能_初始化 接受 档：
    若 非 做 公共智能_检查档 用 档：
        返还 做 公共智能_失败 用 档、「初始化」、「公智档不可用」。
    完。
    做 公共智能_记录 用 档、「初始化」、一组：（空）、「公共智能」。
    档 的 灵敏表 为 一组：（空）。
    档 的 调质表 为 一组：（空）。
    档 的 会话表 为 一组：（空）。
    档 的 灵敏数 为 0。
    档 的 调质数 为 0。
    档 的 会话数 为 0。
    档 的 是否初始化 为 真。
    做 公共智能_加灵敏 用 档、「视觉」、0.68、1.14、0.08、「界面」。
    做 公共智能_加灵敏 用 档、「动作」、0.72、1.20、0.06、「控制」。
    做 公共智能_调质 用 档、「多巴」、0.62、「0.0-1.0」、「强化」。
    做 公共智能_调质 用 档、「乙酰」、0.57、「0.0-1.0」、「注意」。
    做 公共智能_建会话 用 档、「主脑」、「中枢」、「系统状态」。
    返还 做 公共智能_成功 用 档、「初始化」、「已初始化」。
完。

公共智能_自检 接受 档：
    若 非 做 公共智能_检查档 用 档：
        返还 假。
    完。
    若 做 公共智能_初始化 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 公共智能_睡眠巩固 用 档、「主脑」、「将白璃布局固化到长期记忆」 的 已成 等于 假：
        返还 假。
    完。
    若 做 公共智能_稳态 用 档、83、56、32 的 已成 等于 假：
        返还 假。
    完。
    若 做 公共智能_突触网格 用 档、「感知」、「执行」、0.78、「可塑」 的 已成 等于 假：
        返还 假。
    完。
    若 做 公共智能_经络路由 用 档、「白璃路由」、「低频」、「桌面通道」、0.44 的 已成 等于 假：
        返还 假。
    完。
    若 做 公共智能_脉冲 用 档、「动作」、0.81、16、「指令面板」 的 已成 等于 假：
        返还 假。
    完。
    若 做 公共智能_汇总巡检 用 档、「主脑」 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

公共智能_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 公共智能_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        返还 做 公共智能_初始化 用 档。
    完。
    若 请求 的 动作 等于 「加灵敏」：
        返还 做 公共智能_加灵敏 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项、请求 的 参数 的 第 4 项。
    完。
    若 请求 的 动作 等于 「调质」：
        返还 做 公共智能_调质 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「建会话」：
        返还 做 公共智能_建会话 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「睡眠巩固」：
        返还 做 公共智能_睡眠巩固 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「稳态」：
        返还 做 公共智能_稳态 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「突触网格」：
        返还 做 公共智能_突触网格 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「经络路由」：
        返还 做 公共智能_经络路由 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「脉冲」：
        返还 做 公共智能_脉冲 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项。
    完。
    若 请求 的 动作 等于 「信号巡检」：
        返还 做 公共智能_信号巡检 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「智能巡检」：
        返还 做 公共智能_智能巡检 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「物页巡检」：
        返还 做 公共智能_物页巡检 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「调度巡检」：
        返还 做 公共智能_调度巡检 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「汇总巡检」：
        返还 做 公共智能_汇总巡检 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「摘要」：
        返还 做 公共智能_成功 用 档、「摘要」、做 公共智能_摘要 用 档。
    完。
    返还 做 公共智能_失败 用 档、「处理」、「未知动作」。
完。

STDP_A正 为 5。

STDP_A负 为 5。

STDP_TAU 为 20。

NIET_预测能力 接受 中断历史、窗口大小：
    若 中断历史 等于 0 或者 窗口大小 等于 0：
        返还 0。
    完。
    可变 max_freq 为 0。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 窗口大小：
        v 为 做 内存_读32有符号 用 中断历史 加 i 乘 4。
        若 v 大于 max_freq：
            max_freq 为 v。
        完。
        总 为 总 加 v。
        i 为 i 加 1。
    完。
    若 总 等于 0：
        返还 0。
    完。
    返还 max_freq 乘 Q_ONE 除以 总。
完。

STDP_A正 为 5。

STDP_A负 为 5。

STDP_TAU 为 20。

七态量化权重 接受 w：
    可变 idx 为 (w 加 3 乘 Q_ONE 除以 3) 乘 7 除以 (2 乘 Q_ONE)。
    若 idx 小于 0：
        idx 为 0。
    完。
    若 idx 大于 6：
        idx 为 6。
    完。
    返还 (idx 减 3) 乘 Q_ONE 除以 3。
完。

STDP_A正 为 5。

STDP_A负 为 5。

STDP_TAU 为 20。

本章 收束。
