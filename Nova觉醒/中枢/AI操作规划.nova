《AI操作规划》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·稳态·图层合成Alpha混合》。

引用《.·工具协议工》。

引用《.·内核打印》。

引用《.·整数转字符串》。

意图_点击按钮 为 1。

意图_填写表单 为 2。

意图_导航链接 为 3。

意图_读取内容 为 4。

意图_搜索 为 5。

AI操作计划 是 一种 事物，含 意图、步骤列表、当前步骤、已完成。

智能_规划操作 接受 意图、目标文本：
    计划 为 新建 AI操作计划。
    计划 的 意图 为 意图。
    计划 的 步骤列表 为 一组：（空）。
    计划 的 当前步骤 为 0。
    计划 的 已完成 为 假。
    若 意图 等于 意图_点击按钮：
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_浏览、一组：（空）。
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_点击、一组：目标文本。
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_浏览、一组：（空）。
    否则若 意图 等于 意图_填写表单：
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_浏览、一组：（空）。
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_输入、一组：目标文本。
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_浏览、一组：（空）。
    否则若 意图 等于 意图_导航链接：
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_浏览、一组：（空）。
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_点击、一组：目标文本。
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_等待、一组：目标文本。
    否则若 意图 等于 意图_读取内容：
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_读取、一组：（空）。
    否则若 意图 等于 意图_搜索：
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_浏览、一组：（空）。
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_输入、一组：目标文本。
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_点击、一组：「搜索」。
        做 追加 用 计划 的 步骤列表、做 MCP_创建请求 用 MCP_工具_等待、一组：「结果」。
    完。
    返还 计划。
完。

智能_执行计划 接受 计划：
    只要 计划 的 当前步骤 小于 做 计数 用 计划 的 步骤列表：
        请求 为 计划 的 步骤列表 的 第 计划 的 当前步骤 项。
        响应 为 做 MCP_处理请求 用 请求。
        若 非 响应 的 成功：
            做 内核调试 用 「AI操作计划: 步骤」 加 做 整数转字符串 用 计划 的 当前步骤 加 「失败: 」 加 响应 的 错误信息。
            返还 假。
        完。
        计划 的 当前步骤 为 计划 的 当前步骤 加 1。
    完。
    计划 的 已完成 为 真。
    返还 真。
完。

像素_验证区域颜色 接受 fb、x、y、w、h、期望颜色、容差：
    可变 匹配 为 0。
    可变 总 为 0。
    可变 dy 为 0。
    只要 dy 小于 h：
        可变 dx 为 0。
        只要 dx 小于 w：
            像素 为 做 像素_读点 用 fb、x 加 dx、y 加 dy。
            若 做 颜色_相近 用 像素、期望颜色、容差：
                匹配 为 匹配 加 1。
            完。
            总 为 总 加 1。
            dx 为 dx 加 4。
        完。
        dy 为 dy 加 4。
    完。
    若 总 等于 0：
        返还 假。
    完。
    返还 匹配 乘 100 除以 总 大于 80。
完。

颜色_相近 接受 c1、c2、容差：
    r差 为 (c1 右移 16 位与 0xFF) 减 (c2 右移 16 位与 0xFF)。
    g差 为 (c1 右移 8 位与 0xFF) 减 (c2 右移 8 位与 0xFF)。
    b差 为 (c1 位与 0xFF) 减 (c2 位与 0xFF)。
    若 r差 小于 0：
        r差 为 0 减 r差。
    完。
    若 g差 小于 0：
        g差 为 0 减 g差。
    完。
    若 b差 小于 0：
        b差 为 0 减 b差。
    完。
    返还 r差 加 g差 加 b差 小于或等于 容差。
完。

像素_区域截图 接受 fb、x、y、w、h：
    可变 像素数据 为 一组：（空）。
    可变 dy 为 0。
    只要 dy 小于 h：
        可变 dx 为 0。
        只要 dx 小于 w：
            做 追加 用 像素数据、做 像素_读点 用 fb、x 加 dx、y 加 dy。
            dx 为 dx 加 1。
        完。
        dy 为 dy 加 1。
    完。
    返还 像素数据。
完。

本章 收束。
