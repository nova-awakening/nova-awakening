《系统剖析器》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《.·内核打印》。

剖析条目 是 一种 事物，含 名称、调用次数、总耗时微秒、最大耗时微秒、最小耗时微秒、进入时间。

剖析_最大条目 为 1024。

可变 _剖析表 为 一组：（空）。

可变 _剖析启用 为 假。

可变 _采样总数 为 0。

剖析_启用 接受 零参：
    _剖析启用 为 真。
完。

剖析_禁用 接受 零参：
    _剖析启用 为 假。
完。

剖析_已启用 接受 零参：
    返还 _剖析启用。
完。

剖析_查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _剖析表：
        若 _剖析表 的 第 i 项 的 名称 等于 名称：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

剖析_进入 接受 名称：
    若 非 _剖析启用：
        返还。
    完。
    idx 为 做 剖析_查找 用 名称。
    若 idx 小于 0：
        若 做 计数 用 _剖析表 大于或等于 剖析_最大条目：
            返还。
        完。
        条目 为 新建 剖析条目。
        条目 的 名称 为 名称。
        条目 的 调用次数 为 0。
        条目 的 总耗时微秒 为 0。
        条目 的 最大耗时微秒 为 0。
        条目 的 最小耗时微秒 为 999999999。
        条目 的 进入时间 为 做 读取毫秒。
        做 追加 用 _剖析表、条目。
    否则：
        _剖析表 的 第 idx 项 的 进入时间 为 做 读取毫秒。
    完。
完。

剖析_退出 接受 名称：
    若 非 _剖析启用：
        返还。
    完。
    idx 为 做 剖析_查找 用 名称。
    若 idx 小于 0：
        返还。
    完。
    现在 为 做 读取毫秒。
    耗时 为 (现在 减 _剖析表 的 第 idx 项 的 进入时间) 乘 1000。
    _剖析表 的 第 idx 项 的 调用次数 为 _剖析表 的 第 idx 项 的 调用次数 加 1。
    _剖析表 的 第 idx 项 的 总耗时微秒 为 _剖析表 的 第 idx 项 的 总耗时微秒 加 耗时。
    若 耗时 大于 _剖析表 的 第 idx 项 的 最大耗时微秒：
        _剖析表 的 第 idx 项 的 最大耗时微秒 为 耗时。
    完。
    若 耗时 小于 _剖析表 的 第 idx 项 的 最小耗时微秒：
        _剖析表 的 第 idx 项 的 最小耗时微秒 为 耗时。
    完。
完。

剖析_采样 接受 当前函数名：
    若 非 _剖析启用：
        返还。
    完。
    _采样总数 为 _采样总数 加 1。
    idx 为 做 剖析_查找 用 当前函数名。
    若 idx 小于 0：
        若 做 计数 用 _剖析表 大于或等于 剖析_最大条目：
            返还。
        完。
        条目 为 新建 剖析条目。
        条目 的 名称 为 当前函数名。
        条目 的 调用次数 为 1。
        条目 的 总耗时微秒 为 0。
        条目 的 最大耗时微秒 为 0。
        条目 的 最小耗时微秒 为 0。
        条目 的 进入时间 为 0。
        做 追加 用 _剖析表、条目。
    否则：
        _剖析表 的 第 idx 项 的 调用次数 为 _剖析表 的 第 idx 项 的 调用次数 加 1。
    完。
完。

剖析_报告 接受 前N：
    可变 r 为 「═══ 系统剖析报告 ═══\n」。
    r 为 r 加 「条目数: 」 加 做 整数转字符串 用 做 计数 用 _剖析表 加 「\n」。
    r 为 r 加 「采样数: 」 加 做 整数转字符串 用 _采样总数 加 「\n」。
    可变 排序表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _剖析表：
        做 追加 用 排序表、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 排序表 减 1：
        可变 最大 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 排序表：
            若 _剖析表 的 第 排序表 的 第 j 项 项 的 总耗时微秒 大于 _剖析表 的 第 排序表 的 第 最大 项 项 的 总耗时微秒：
                最大 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最大 不等于 i：
            临时 为 排序表 的 第 i 项。
            排序表 的 第 i 项 为 排序表 的 第 最大 项。
            排序表 的 第 最大 项 为 临时。
        完。
        i 为 i 加 1。
    完。
    r 为 r 加 「热点 (按总耗时):\n」。
    i 为 0。
    只要 i 小于 做 计数 用 排序表 并且 i 小于 前N：
        e 为 _剖析表 的 第 排序表 的 第 i 项 项。
        平均 为 0。
        若 e 的 调用次数 大于 0：
            平均 为 e 的 总耗时微秒 除以 e 的 调用次数。
        完。
        r 为 r 加 「  」 加 做 整数转字符串 用 i 加 1 加 「. 」 加 e 的 名称。
        r 为 r 加 「 调用:」 加 做 整数转字符串 用 e 的 调用次数。
        r 为 r 加 「 总:」 加 做 整数转字符串 用 e 的 总耗时微秒 加 「us」。
        r 为 r 加 「 均:」 加 做 整数转字符串 用 平均 加 「us」。
        r 为 r 加 「 最大:」 加 做 整数转字符串 用 e 的 最大耗时微秒 加 「us」。
        r 为 r 加 「\n」。
        i 为 i 加 1。
    完。
    返还 r。
完。

剖析_重置 接受 零参：
    _剖析表 为 一组：（空）。
    _采样总数 为 0。
完。

本章 收束。
