《训练流程两》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·统计》。

引用《.·赫布学习更新》。

引用《.·整数转字符串》。

TNF58B_训练 接受 数据路径、批大小、学习率：
    做 内核信息 用 「TNF-58B训练: 开始 数据=」 加 数据路径 加 「 批大小=」 加 做 整数转字符串 用 批大小 加 「 学习率=」 加 做 整数转字符串 用 学习率。
    可变 全局步 为 0。
    可变 轮次 为 0。
    最大轮次 为 3。
    只要 轮次 小于 最大轮次：
        数据流 为 做 打开数据流 用 数据路径。
        若 数据流 等于 空：
            做 内核错误 用 「TNF-58B训练: 无法打开数据 」 加 数据路径。
            返还。
        完。
        可变 批计数 为 0。
        只要 非 做 数据流_结束 用 数据流：
            批数据 为 做 数据流_读批次 用 数据流、批大小。
            若 批数据 等于 空：
                中断。
            完。
            输出 为 做 TNF58B_前向 用 批数据。
            损失 为 做 TNF58B_计算损失 用 输出、批数据。
            做 TNF58B_赫布更新 用 批数据、输出、学习率。
            TNF58B_总损失 为 TNF58B_总损失 加 损失。
            TNF58B_已处理样本 为 TNF58B_已处理样本 加 批大小。
            全局步 为 全局步 加 1。
            批计数 为 批计数 加 1。
            若 全局步 取余于 1000 等于 0：
                平均损失 为 TNF58B_总损失 除以 全局步。
                做 内核信息 用 「TNF-58B训练: 轮次=」 加 做 整数转字符串 用 轮次 加 「 步=」 加 做 整数转字符串 用 全局步 加 「 损失=」 加 做 整数转字符串 用 平均损失 加 「 样本=」 加 做 整数转字符串 用 TNF58B_已处理样本。
            完。
            若 全局步 取余于 10000 等于 0：
                做 TNF58B_保存检查点 用 全局步。
            完。
        完。
        做 数据流_关闭 用 数据流。
        轮次 为 轮次 加 1。
        TNF58B_训练轮次 为 轮次。
        做 内核信息 用 「TNF-58B训练: 轮次」 加 做 整数转字符串 用 轮次 加 「完成 批=」 加 做 整数转字符串 用 批计数。
    完。
    做 内核信息 用 「TNF-58B训练: 完成! 总步=」 加 做 整数转字符串 用 全局步 加 「 总样本=」 加 做 整数转字符串 用 TNF58B_已处理样本。
完。

TNF58B_前向 接受 输入：
    可变 激活 为 输入。
    可变 i 为 0。
    只要 i 小于 做 计数 用 TNF58B_层：
        层 为 TNF58B_层 的 第 i 项。
        若 层 的 类型 等于 0：
            激活 为 做 联想层_前向 用 层、激活。
        否则若 层 的 类型 等于 1：
            激活 为 做 路由层_前向 用 层、激活。
        否则若 层 的 类型 等于 2：
            激活 为 做 记忆层_前向 用 层、激活。
        完。
        层 的 激活计数 为 层 的 激活计数 加 1。
        i 为 i 加 1。
    完。
    返还 激活。
完。

联想层_前向 接受 层、输入：
    维度 为 层 的 输入维度。
    可变 输出 为 一组：（空）。
    可变 活跃数 为 0。
    可变 j 为 0。
    只要 j 小于 维度：
        若 j 小于 做 计数 用 输入 并且 输入 的 第 j 项 不等于 0：
            可变 能量 为 0。
            若 j 小于 做 计数 用 层 的 偏置：
                能量 为 层 的 偏置 的 第 j 项。
            完。
            可变 k 为 0。
            只要 k 小于 做 计数 用 输入 并且 k 小于 维度：
                若 输入 的 第 k 项 不等于 0：
                    权重索引 为 j 乘 维度 加 k。
                    若 权重索引 小于 做 计数 用 层 的 权重：
                        能量 为 能量 加 层 的 权重 的 第 权重索引 项 乘 输入 的 第 k 项。
                    完。
                完。
                k 为 k 加 1。
            完。
            量化值 为 做 七态量化 用 能量。
            做 追加 用 输出、量化值。
            若 量化值 不等于 0：
                活跃数 为 活跃数 加 1。
            完。
        否则：
            做 追加 用 输出、0。
        完。
        j 为 j 加 1。
    完。
    返还 输出。
完。

路由层_前向 接受 层、输入：
    可变 输出 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 层 的 输出维度 并且 j 小于 做 计数 用 输入：
        路由值 为 输入 的 第 j 项。
        若 j 取余于 4 等于 0：
            路由值 为 路由值。
        否则若 j 取余于 4 等于 1：
            路由值 为 路由值 乘 3 除以 4。
        否则若 j 取余于 4 等于 2：
            路由值 为 路由值 除以 2。
        否则：
            路由值 为 路由值 除以 4。
        完。
        做 追加 用 输出、路由值。
        j 为 j 加 1。
    完。
    返还 输出。
完。

记忆层_前向 接受 层、输入：
    查询哈希 为 做 向量哈希 用 输入。
    记忆 为 做 稀疏KV_查找 用 查询哈希。
    若 记忆 不等于 空：
        返还 做 向量加权融合 用 输入、记忆、700。
    完。
    返还 输入。
完。

本章 收束。
