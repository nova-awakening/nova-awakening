《能力声明》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《.·内核打印》。

能力_文件读 为 0x0001。

能力_文件写 为 0x0002。

能力_网络出站 为 0x0004。

能力_网络入站 为 0x0008。

能力_进程创建 为 0x0010。

能力_进程信号 为 0x0020。

能力_设备访问 为 0x0040。

能力_GPU 为 0x0080。

能力_音频 为 0x0100。

能力_摄像头 为 0x0200。

能力_蓝牙 为 0x0400。

能力_USB 为 0x0800。

能力_系统配置 为 0x1000。

能力_内核模块 为 0x2000。

能力_加密存储 为 0x4000。

能力_AI推理 为 0x8000。

能力等级_必须 为 2。

能力等级_可选 为 1。

能力等级_禁止 为 0。

能力声明 是 一种 事物，含 能力ID、等级、理由、使用次数、最后使用时间。

可变 _已授权能力 为 一组：（空）。

可变 _能力审计日志 为 一组：（空）。

可变 _能力检查总数 为 0。

可变 _能力拒绝总数 为 0。

能力_注册 接受 能力ID、等级、理由：
    声明 为 新建 能力声明。
    声明 的 能力ID 为 能力ID。
    声明 的 等级 为 等级。
    声明 的 理由 为 理由。
    声明 的 使用次数 为 0。
    声明 的 最后使用时间 为 0。
    做 追加 用 _已授权能力、声明。
完。

能力_已授权 接受 能力ID：
    _能力检查总数 为 _能力检查总数 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _已授权能力：
        若 _已授权能力 的 第 i 项 的 能力ID 等于 能力ID 并且 _已授权能力 的 第 i 项 的 等级 大于 能力等级_禁止：
            _已授权能力 的 第 i 项 的 使用次数 为 _已授权能力 的 第 i 项 的 使用次数 加 1。
            _已授权能力 的 第 i 项 的 最后使用时间 为 做 读取毫秒 除以 1000。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    _能力拒绝总数 为 _能力拒绝总数 加 1。
    做 追加 用 _能力审计日志、一组：能力ID、「拒绝」、做 读取毫秒 除以 1000。
    返还 假。
完。

能力_检查并执行 接受 能力ID、操作名：
    若 做 能力_已授权 用 能力ID：
        做 追加 用 _能力审计日志、一组：能力ID、操作名、做 读取毫秒 除以 1000。
        返还 真。
    完。
    做 内核警告 用 「能力拒绝: 」 加 操作名 加 「 需要能力 」 加 做 整数转字符串 用 能力ID。
    返还 假。
完。

能力_撤销 接受 能力ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _已授权能力：
        若 _已授权能力 的 第 i 项 的 能力ID 等于 能力ID：
            _已授权能力 的 第 i 项 的 等级 为 能力等级_禁止。
            做 追加 用 _能力审计日志、一组：能力ID、「撤销」、做 读取毫秒 除以 1000。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

能力_重置 接受 零参：
    _已授权能力 为 一组：（空）。
    _能力审计日志 为 一组：（空）。
    _能力检查总数 为 0。
    _能力拒绝总数 为 0。
完。

能力_从Bundle加载 接受 能力ID列表：
    做 能力_重置。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力ID列表：
        做 能力_注册 用 能力ID列表 的 第 i 项、能力等级_必须、「Bundle声明」。
        i 为 i 加 1。
    完。
完。

能力_必须能力满足 接受 所需列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 所需列表：
        若 非 做 能力_已授权 用 所需列表 的 第 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

能力_已授权数 接受 零参：
    可变 n 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _已授权能力：
        若 _已授权能力 的 第 i 项 的 等级 大于 能力等级_禁止：
            n 为 n 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 n。
完。

能力_名称 接受 能力ID：
    若 能力ID 等于 能力_文件读：
        返还 「文件读取」。
    完。
    若 能力ID 等于 能力_文件写：
        返还 「文件写入」。
    完。
    若 能力ID 等于 能力_网络出站：
        返还 「网络出站」。
    完。
    若 能力ID 等于 能力_网络入站：
        返还 「网络入站」。
    完。
    若 能力ID 等于 能力_进程创建：
        返还 「进程创建」。
    完。
    若 能力ID 等于 能力_进程信号：
        返还 「进程信号」。
    完。
    若 能力ID 等于 能力_设备访问：
        返还 「设备访问」。
    完。
    若 能力ID 等于 能力_GPU：
        返还 「GPU计算」。
    完。
    若 能力ID 等于 能力_音频：
        返还 「音频」。
    完。
    若 能力ID 等于 能力_摄像头：
        返还 「摄像头」。
    完。
    若 能力ID 等于 能力_蓝牙：
        返还 「蓝牙」。
    完。
    若 能力ID 等于 能力_USB：
        返还 「USB」。
    完。
    若 能力ID 等于 能力_系统配置：
        返还 「系统配置」。
    完。
    若 能力ID 等于 能力_内核模块：
        返还 「内核模块」。
    完。
    若 能力ID 等于 能力_加密存储：
        返还 「加密存储」。
    完。
    若 能力ID 等于 能力_AI推理：
        返还 「AI推理」。
    完。
    返还 「未知能力」。
完。

能力_等级名 接受 等级：
    若 等级 等于 能力等级_必须：
        返还 「必须」。
    完。
    若 等级 等于 能力等级_可选：
        返还 「可选」。
    完。
    若 等级 等于 能力等级_禁止：
        返还 「禁止」。
    完。
    返还 「未知」。
完。

能力_审计报告 接受 零参：
    可变 r 为 「═══ 能力审计报告 ═══\n」。
    r 为 r 加 「已授权能力: 」 加 做 整数转字符串 用 做 能力_已授权数 加 「\n」。
    r 为 r 加 「检查总数: 」 加 做 整数转字符串 用 _能力检查总数 加 「\n」。
    r 为 r 加 「拒绝总数: 」 加 做 整数转字符串 用 _能力拒绝总数 加 「\n」。
    r 为 r 加 「审计日志: 」 加 做 整数转字符串 用 做 计数 用 _能力审计日志 加 「 条\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _已授权能力：
        cap 为 _已授权能力 的 第 i 项。
        若 cap 的 等级 大于 能力等级_禁止：
            r 为 r 加 「  」 加 做 能力_名称 用 cap 的 能力ID 加 「 [」 加 做 能力_等级名 用 cap 的 等级 加 「] 使用」 加 做 整数转字符串 用 cap 的 使用次数 加 「次\n」。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

本章 收束。
