《用户栈构建》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·内核打印》。

引用《.·辅助存根》。

引用《.·字符串替换》。

引用《..·引导·端口IO》。

引用《.·每处理器》。

引用《.·哈希函数》。

引用《.·整数转字符串》。

AT_NULL 为 0。

AT_PHDR 为 3。

AT_PHENT 为 4。

AT_PHNUM 为 5。

AT_PAGESZ 为 6。

AT_BASE 为 7。

AT_ENTRY 为 9。

AT_UID 为 11。

AT_EUID 为 12。

AT_GID 为 13。

AT_EGID 为 14。

AT_RANDOM 为 25。

AT_EXECFN 为 31。

AT_CLKTCK 为 17。

AT_HWCAP 为 16。

AT_SECURE 为 23。

栈默认基地址 为 0x7FFFFFFFE000。

栈最大参数数 为 256。

栈最大环境变量数 为 256。

栈最大字符串总长 为 131072。

最大预加载库 为 16。

最大库搜索路径 为 32。

可变 库搜索路径 为 一组：「/lib」、「/usr/lib」、「/usr/local/lib」。

可变 预加载列表 为 一组：（空）。

栈构建统计 是 一种 事物，含 栈构建次数、参数总数、环境变量总数、字符串总字节、auxv条目总数、库搜索次数、库找到次数、库未找到次数、预加载次数。

可变 全局_栈统计 为 空。

栈统计_初始化 接受 零参：
    若 全局_栈统计 等于 空：
        全局_栈统计 为 新建 栈构建统计。
        全局_栈统计 的 栈构建次数 为 0。
        全局_栈统计 的 参数总数 为 0。
        全局_栈统计 的 环境变量总数 为 0。
        全局_栈统计 的 字符串总字节 为 0。
        全局_栈统计 的 auxv条目总数 为 0。
        全局_栈统计 的 库搜索次数 为 0。
        全局_栈统计 的 库找到次数 为 0。
        全局_栈统计 的 库未找到次数 为 0。
        全局_栈统计 的 预加载次数 为 0。
    完。
完。

栈_写字符串 接受 sp、字符串：
    长度 为 做 字符串_长度 用 字符串。
    sp 为 sp 减 长度 减 1。
    可变 i 为 0。
    只要 i 小于 长度：
        做 内存写8 用 sp 加 i、字符串 的 第 i 项。
        i 为 i 加 1。
    完。
    做 内存写8 用 sp 加 长度、0。
    返还 sp。
完。

栈_对齐16 接受 sp：
    返还 sp 位与 按位取反 15。
完。

构建用户栈 接受 主对象、参数、环境变量：
    做 栈统计_初始化。
    全局_栈统计 的 栈构建次数 为 全局_栈统计 的 栈构建次数 加 1。
    可变 sp 为 栈默认基地址。
    argc 为 做 计数 用 参数。
    若 argc 大于 栈最大参数数：
        做 内核警告 用 「用户栈: 参数过多, 截断到 」 加 做 整数转字符串 用 栈最大参数数。
        argc 为 栈最大参数数。
    完。
    envc 为 做 计数 用 环境变量。
    若 envc 大于 栈最大环境变量数：
        envc 为 栈最大环境变量数。
    完。
    全局_栈统计 的 参数总数 为 全局_栈统计 的 参数总数 加 argc。
    全局_栈统计 的 环境变量总数 为 全局_栈统计 的 环境变量总数 加 envc。
    sp 为 sp 减 16。
    random_addr 为 sp。
    可变 ri 为 0。
    只要 ri 小于 16：
        做 内存写8 用 sp 加 ri、ri 乘 7 加 42 位与 0xFF。
        ri 为 ri 加 1。
    完。
    可变 execfn_addr 为 0。
    若 主对象 的 文件名 不等于 空：
        sp 为 做 栈_写字符串 用 sp、主对象 的 文件名。
        execfn_addr 为 sp。
        全局_栈统计 的 字符串总字节 为 全局_栈统计 的 字符串总字节 加 做 字符串_长度 用 主对象 的 文件名 加 1。
    完。
    可变 env_addrs 为 一组：（空）。
    可变 ei 为 0。
    只要 ei 小于 envc：
        sp 为 做 栈_写字符串 用 sp、环境变量 的 第 ei 项。
        做 追加 用 env_addrs、sp。
        全局_栈统计 的 字符串总字节 为 全局_栈统计 的 字符串总字节 加 做 字符串_长度 用 环境变量 的 第 ei 项 加 1。
        ei 为 ei 加 1。
    完。
    可变 arg_addrs 为 一组：（空）。
    可变 ai 为 0。
    只要 ai 小于 argc：
        sp 为 做 栈_写字符串 用 sp、参数 的 第 ai 项。
        做 追加 用 arg_addrs、sp。
        全局_栈统计 的 字符串总字节 为 全局_栈统计 的 字符串总字节 加 做 字符串_长度 用 参数 的 第 ai 项 加 1。
        ai 为 ai 加 1。
    完。
    sp 为 做 栈_对齐16 用 sp。
    可变 auxv_count 为 0。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_NULL。
    做 内存写64 用 sp 加 8、0。
    auxv_count 为 auxv_count 加 1。
    若 execfn_addr 不等于 0：
        sp 为 sp 减 16。
        做 内存写64 用 sp、AT_EXECFN。
        做 内存写64 用 sp 加 8、execfn_addr。
        auxv_count 为 auxv_count 加 1。
    完。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_RANDOM。
    做 内存写64 用 sp 加 8、random_addr。
    auxv_count 为 auxv_count 加 1。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_SECURE。
    做 内存写64 用 sp 加 8、0。
    auxv_count 为 auxv_count 加 1。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_CLKTCK。
    做 内存写64 用 sp 加 8、100。
    auxv_count 为 auxv_count 加 1。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_PAGESZ。
    做 内存写64 用 sp 加 8、4096。
    auxv_count 为 auxv_count 加 1。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_PHDR。
    做 内存写64 用 sp 加 8、主对象 的 PHDR地址。
    auxv_count 为 auxv_count 加 1。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_PHENT。
    做 内存写64 用 sp 加 8、56。
    auxv_count 为 auxv_count 加 1。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_PHNUM。
    做 内存写64 用 sp 加 8、主对象 的 PHDR数。
    auxv_count 为 auxv_count 加 1。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_ENTRY。
    做 内存写64 用 sp 加 8、主对象 的 入口点。
    auxv_count 为 auxv_count 加 1。
    若 主对象 的 解释器基地址 不等于 0：
        sp 为 sp 减 16。
        做 内存写64 用 sp、AT_BASE。
        做 内存写64 用 sp 加 8、主对象 的 解释器基地址。
        auxv_count 为 auxv_count 加 1。
    完。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_UID。
    做 内存写64 用 sp 加 8、做 获取当前进程 的 uid。
    auxv_count 为 auxv_count 加 1。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_EUID。
    做 内存写64 用 sp 加 8、做 获取当前进程 的 euid。
    auxv_count 为 auxv_count 加 1。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_GID。
    做 内存写64 用 sp 加 8、做 获取当前进程 的 gid。
    auxv_count 为 auxv_count 加 1。
    sp 为 sp 减 16。
    做 内存写64 用 sp、AT_EGID。
    做 内存写64 用 sp 加 8、做 获取当前进程 的 egid。
    auxv_count 为 auxv_count 加 1。
    全局_栈统计 的 auxv条目总数 为 全局_栈统计 的 auxv条目总数 加 auxv_count。
    sp 为 sp 减 8。
    做 内存写64 用 sp、0。
    ei 为 envc 减 1。
    只要 ei 大于或等于 0：
        sp 为 sp 减 8。
        做 内存写64 用 sp、env_addrs 的 第 ei 项。
        ei 为 ei 减 1。
    完。
    sp 为 sp 减 8。
    做 内存写64 用 sp、0。
    ai 为 argc 减 1。
    只要 ai 大于或等于 0：
        sp 为 sp 减 8。
        做 内存写64 用 sp、arg_addrs 的 第 ai 项。
        ai 为 ai 减 1。
    完。
    sp 为 sp 减 8。
    做 内存写64 用 sp、argc。
    返还 sp。
完。

查找共享库 接受 名称：
    做 栈统计_初始化。
    全局_栈统计 的 库搜索次数 为 全局_栈统计 的 库搜索次数 加 1。
    若 做 字符串_长度 用 名称 大于 0 并且 名称 的 第 0 项 等于 「/」：
        若 做 文件存在 用 名称：
            全局_栈统计 的 库找到次数 为 全局_栈统计 的 库找到次数 加 1。
            返还 名称。
        完。
        全局_栈统计 的 库未找到次数 为 全局_栈统计 的 库未找到次数 加 1。
        返还 「」。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 库搜索路径：
        全路径 为 库搜索路径 的 第 i 项 加 「/」 加 名称。
        若 做 文件存在 用 全路径：
            全局_栈统计 的 库找到次数 为 全局_栈统计 的 库找到次数 加 1。
            返还 全路径。
        完。
        i 为 i 加 1。
    完。
    全局_栈统计 的 库未找到次数 为 全局_栈统计 的 库未找到次数 加 1。
    返还 「」。
完。

已加载 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 已加载对象列表：
        若 已加载对象列表 的 第 i 项 的 名称 等于 名称：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

添加库搜索路径 接受 路径：
    若 做 计数 用 库搜索路径 大于或等于 最大库搜索路径：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 库搜索路径：
        若 库搜索路径 的 第 i 项 等于 路径：
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 库搜索路径、路径。
完。

解析LD_LIBRARY_PATH 接受 环境变量列表：
    前缀 为 「LD_LIBRARY_PATH=」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 环境变量列表：
        若 做 字符串开始于 用 环境变量列表 的 第 i 项、前缀：
            值 为 做 字符串_子串 用 环境变量列表 的 第 i 项、做 字符串_长度 用 前缀。
            路径列表 为 做 字符串分割 用 值、「:」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 路径列表：
                做 添加库搜索路径 用 路径列表 的 第 j 项。
                j 为 j 加 1。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

添加RPATH 接受 rpath_字符串：
    若 rpath_字符串 等于 空 或者 做 字符串_长度 用 rpath_字符串 等于 0：
        返还。
    完。
    路径列表 为 做 字符串分割 用 rpath_字符串、「:」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 路径列表：
        做 添加库搜索路径 用 路径列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

添加预加载 接受 库路径：
    做 栈统计_初始化。
    若 做 计数 用 预加载列表 大于或等于 最大预加载库：
        返还 错误_资源不足。
    完。
    做 追加 用 预加载列表、库路径。
    全局_栈统计 的 预加载次数 为 全局_栈统计 的 预加载次数 加 1。
    返还 成功。
完。

解析LD_PRELOAD 接受 环境变量列表：
    前缀 为 「LD_PRELOAD=」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 环境变量列表：
        若 做 字符串开始于 用 环境变量列表 的 第 i 项、前缀：
            值 为 做 字符串_子串 用 环境变量列表 的 第 i 项、做 字符串_长度 用 前缀。
            库列表 为 做 字符串分割 用 值、「:」。
            可变 j 为 0。
            只要 j 小于 做 计数 用 库列表：
                做 添加预加载 用 库列表 的 第 j 项。
                j 为 j 加 1。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

获取预加载列表 接受 零参：
    返还 预加载列表。
完。

环境变量_查找 接受 列表、键：
    前缀 为 键 加 「=」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 字符串开始于 用 列表 的 第 i 项、前缀：
            返还 做 字符串_子串 用 列表 的 第 i 项、做 字符串_长度 用 前缀。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

环境变量_设置 接受 列表、键、值：
    前缀 为 键 加 「=」。
    新条目 为 键 加 「=」 加 值。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 字符串开始于 用 列表 的 第 i 项、前缀：
            列表 的 第 i 项 为 新条目。
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 列表、新条目。
完。

栈构建_打印统计 接受 零参：
    做 栈统计_初始化。
    做 打印行 用 「=== 用户栈构建统计 ===」。
    做 打印行 用 「栈构建次数: 」 加 做 整数转字符串 用 全局_栈统计 的 栈构建次数。
    做 打印行 用 「参数总数: 」 加 做 整数转字符串 用 全局_栈统计 的 参数总数。
    做 打印行 用 「环境变量总数: 」 加 做 整数转字符串 用 全局_栈统计 的 环境变量总数。
    做 打印行 用 「字符串总字节: 」 加 做 整数转字符串 用 全局_栈统计 的 字符串总字节。
    做 打印行 用 「auxv条目总数: 」 加 做 整数转字符串 用 全局_栈统计 的 auxv条目总数。
    做 打印行 用 「库搜索次数: 」 加 做 整数转字符串 用 全局_栈统计 的 库搜索次数。
    做 打印行 用 「库找到: 」 加 做 整数转字符串 用 全局_栈统计 的 库找到次数。
    做 打印行 用 「库未找到: 」 加 做 整数转字符串 用 全局_栈统计 的 库未找到次数。
    做 打印行 用 「预加载: 」 加 做 整数转字符串 用 全局_栈统计 的 预加载次数。
    做 打印行 用 「搜索路径数: 」 加 做 整数转字符串 用 做 计数 用 库搜索路径。
    做 打印行 用 「预加载库数: 」 加 做 整数转字符串 用 做 计数 用 预加载列表。
    做 打印行 用 「保护页安装: 」 加 做 整数转字符串 用 保护页_安装次数。
    做 打印行 用 「保护页触发: 」 加 做 整数转字符串 用 保护页_触发次数。
完。

栈保护页大小 为 4096。

可变 保护页_安装次数 为 0。

可变 保护页_触发次数 为 0。

栈_安装保护页 接受 栈底地址：
    保护地址 为 栈底地址 减 栈保护页大小。
    结果 为 做 内存_设置权限 用 保护地址、栈保护页大小、0。
    若 结果 等于 成功：
        保护页_安装次数 为 保护页_安装次数 加 1。
    完。
    返还 结果。
完。

栈_检测溢出 接受 sp、栈底地址：
    距离 为 sp 减 (栈底地址 减 栈保护页大小)。
    若 距离 小于 栈保护页大小：
        保护页_触发次数 为 保护页_触发次数 加 1。
        做 内核警告 用 「栈溢出预警: SP距保护页仅」 加 做 整数转字符串 用 距离 加 「字节」。
        返还 真。
    完。
    返还 假。
完。

栈_默认大小 为 8388608。

栈_最小大小 为 65536。

栈_最大大小 为 67108864。

栈_估算所需大小 接受 argc、envc：
    基础 为 栈_默认大小。
    参数开销 为 argc 乘 256。
    环境开销 为 envc 乘 128。
    auxv开销 为 16 乘 16。
    总需求 为 基础 加 参数开销 加 环境开销 加 auxv开销。
    若 总需求 小于 栈_最小大小：
        返还 栈_最小大小。
    完。
    若 总需求 大于 栈_最大大小：
        返还 栈_最大大小。
    完。
    返还 总需求 加 4095 位与 按位取反 4095。
完。

栈_验证参数完整性 接受 参数、argc：
    可变 i 为 0。
    可变 总长 为 0。
    只要 i 小于 argc：
        若 参数 的 第 i 项 等于 空：
            做 内核警告 用 「栈构建: 参数[」 加 做 整数转字符串 用 i 加 「]为空指针」。
            返还 假。
        完。
        长度 为 做 字符串_长度 用 参数 的 第 i 项。
        总长 为 总长 加 长度。
        若 总长 大于 栈最大字符串总长：
            做 内核警告 用 「栈构建: 参数字符串总长超限 」 加 做 整数转字符串 用 总长。
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

可变 AI_用户栈构建_运行指标 为 0。

可变 AI_用户栈构建_异常指标 为 0。

AI_用户栈构建_自诊断 接受 零参：
    AI_用户栈构建_运行指标 为 保护页_安装次数。
    返还 AI_用户栈构建_运行指标。
完。

AI_用户栈构建_优化建议 接受 零参：
    指标 为 做 AI_用户栈构建_自诊断。
    若 AI_用户栈构建_运行指标 大于 10000：
        返还 「用户栈构建负载较高」。
    完。
    返还 「用户栈构建状态正常」。
完。

本章 收束。
