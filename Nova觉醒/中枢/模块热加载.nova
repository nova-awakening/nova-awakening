《模块热加载》

引用《.·类型定义》。

引用《.·错误码》。

外部。

智能_接口_推理。

模块状态_未加载 为 0。

模块状态_已加载 为 1。

模块状态_运行中 为 2。

模块状态_已停止 为 3。

模块状态_错误 为 4。

最大模块数 为 256。

可热更模块 是 一种 事物，含 模块ID、名称、版本、状态、代码哈希、加载时间、依赖列表、入口函数、大小_字节、调用次数、错误次数、上一版本。

可变 模块注册表 为 一组：（空）。

可变 下一个模块ID 为 1。

可变 热更新_总次数 为 0。

可变 热更新_成功次数 为 0。

可变 热更新_回滚次数 为 0。

可变 演化_时钟 为 0。

热更新_注册模块 接受 名称、版本、入口函数、依赖：
    若 做 计数 用 模块注册表 大于或等于 最大模块数：
        返还 负 1。
    完。
    m 为 新建 可热更模块。
    m 的 模块ID 为 下一个模块ID。
    下一个模块ID 为 下一个模块ID 加 1。
    m 的 名称 为 名称。
    m 的 版本 为 版本。
    m 的 状态 为 模块状态_未加载。
    m 的 加载时间 为 演化_时钟。
    m 的 依赖列表 为 依赖。
    m 的 入口函数 为 入口函数。
    m 的 调用次数 为 0。
    m 的 错误次数 为 0。
    m 的 上一版本 为 0。
    做 追加 用 模块注册表、m。
    返还 m 的 模块ID。
完。

热更新_加载 接受 模块ID：
    m 为 做 热更新_查找 用 模块ID。
    若 m 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 m 的 依赖列表：
        dep 为 做 热更新_查找 用 m 的 依赖列表 的 第 i 项。
        若 dep 等于 空 或者 dep 的 状态 小于 模块状态_已加载：
            做 内核错误 用 「热更新: 模块'」 加 m 的 名称 加 「'依赖未满足」。
            返还 假。
        完。
        i 为 i 加 1。
    完。
    m 的 状态 为 模块状态_已加载。
    m 的 加载时间 为 演化_时钟。
    做 内核信息 用 「热更新: 加载'」 加 m 的 名称 加 「' v」 加 做 版本转字符串 用 m 的 版本。
    返还 真。
完。

热更新_替换 接受 模块ID、新版本、新代码哈希：
    m 为 做 热更新_查找 用 模块ID。
    若 m 等于 空：
        返还 假。
    完。
    热更新_总次数 为 热更新_总次数 加 1。
    m 的 上一版本 为 m 的 版本。
    旧哈希 为 m 的 代码哈希。
    m 的 版本 为 新版本。
    m 的 代码哈希 为 新代码哈希。
    m 的 加载时间 为 演化_时钟。
    验证结果 为 做 热更新_验证 用 m。
    若 非 验证结果：
        m 的 版本 为 m 的 上一版本。
        m 的 代码哈希 为 旧哈希。
        热更新_回滚次数 为 热更新_回滚次数 加 1。
        做 内核错误 用 「热更新: '」 加 m 的 名称 加 「' 验证失败, 已回滚」。
        返还 假。
    完。
    热更新_成功次数 为 热更新_成功次数 加 1。
    做 进化日志_记录 用 「热更新」、m 的 名称、m 的 上一版本、新版本。
    做 内核信息 用 「热更新: '」 加 m 的 名称 加 「' v」 加 做 版本转字符串 用 m 的 上一版本 加 「 → v」 加 做 版本转字符串 用 新版本。
    返还 真。
完。

热更新_回滚 接受 模块ID：
    m 为 做 热更新_查找 用 模块ID。
    若 m 等于 空 或者 m 的 上一版本 等于 0：
        返还 假。
    完。
    当前版本 为 m 的 版本。
    m 的 版本 为 m 的 上一版本。
    m 的 上一版本 为 当前版本。
    热更新_回滚次数 为 热更新_回滚次数 加 1。
    做 进化日志_记录 用 「回滚」、m 的 名称、当前版本、m 的 版本。
    返还 真。
完。

热更新_验证 接受 m：
    若 m 的 代码哈希 等于 0：
        返还 假。
    完。
    若 m 的 入口函数 等于 「」：
        返还 假。
    完。
    返还 真。
完。

热更新_查找 接受 模块ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 模块注册表：
        若 模块注册表 的 第 i 项 的 模块ID 等于 模块ID：
            返还 模块注册表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

热更新_按名查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 模块注册表：
        若 模块注册表 的 第 i 项 的 名称 等于 名称：
            返还 模块注册表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

本章 收束。
