《表单智能发》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·稳态·绘制引擎》。

引用《.·内核打印》。

引用《..·执行·组件网化》。

引用《.·整数转字符串》。

外部。

元素_按类型查找。

外部。

元素_提取文本。

外部。

元素_获取属性。

外部。

验证_滑块。

外部。

验证_图片选择。

外部。

验证_reCAPTCHA_复选框。

外部。

验证_文字识别。

外部。

标签页_新建。

外部。

标签页_查找。

外部。

HTTP_创建请求。

外部。

HTTP_发送。

外部。

加密_创建AES。

外部。

生成随机字节_安全。

外部。

加密_数据。

外部。

仿生_打字。

外部。

仿生_鼠标点击_真人。

字段类型_用户名 为 1。

字段类型_邮箱 为 2。

字段类型_密码 为 3。

字段类型_确认密码 为 4。

字段类型_手机 为 5。

字段类型_姓名 为 6。

字段类型_验证码 为 7。

字段类型_同意条款 为 8。

字段类型_提交按钮 为 9。

字段类型_未知 为 0。

表单字段 是 一种 事物，含 元素ID、字段类型、名称、标签文本、默认值、类型属性、必填、x、y、宽、高。

发现的表单 是 一种 事物，含 表单元素ID、字段列表、提交按钮、表单类型、URL、置信度。

可变 已发现表单 为 一组：（空）。

可变 注册_总尝试 为 0。

可变 注册_总成功 为 0。

表单_智能发现 接受 DOM根：
    若 DOM根 等于 空：
        返还 一组：（空）。
    完。
    已发现表单 为 一组：（空）。
    所有表单 为 做 元素_按类型查找 用 DOM根、15。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有表单：
        表单 为 新建 发现的表单。
        表单 的 表单元素ID 为 所有表单 的 第 i 项 的 节点ID。
        表单 的 字段列表 为 一组：（空）。
        输入框 为 做 元素_按类型查找 用 所有表单 的 第 i 项、6。
        _文本域 为 做 元素_按类型查找 用 所有表单 的 第 i 项、16。
        按钮 为 做 元素_按类型查找 用 所有表单 的 第 i 项、5。
        可变 j 为 0。
        只要 j 小于 做 计数 用 输入框：
            字段 为 做 表单_分析字段 用 输入框 的 第 j 项。
            做 追加 用 表单 的 字段列表、字段。
            j 为 j 加 1。
        完。
        若 做 计数 用 按钮 大于 0：
            提交 为 新建 表单字段。
            提交 的 元素ID 为 按钮 的 第 0 项 的 节点ID。
            提交 的 字段类型 为 字段类型_提交按钮。
            提交 的 标签文本 为 做 元素_提取文本 用 按钮 的 第 0 项。
            提交 的 x 为 按钮 的 第 0 项 的 布局_x。
            提交 的 y 为 按钮 的 第 0 项 的 布局_y。
            表单 的 提交按钮 为 提交。
        完。
        表单 的 表单类型 为 做 表单_推断类型 用 表单。
        表单 的 置信度 为 700。
        做 追加 用 已发现表单、表单。
        i 为 i 加 1。
    完。
    做 内核调试 用 「表单发现: 找到」 加 做 整数转字符串 用 做 计数 用 已发现表单 加 「个表单」。
    返还 已发现表单。
完。

表单_分析字段 接受 输入元素：
    字段 为 新建 表单字段。
    字段 的 元素ID 为 输入元素 的 节点ID。
    字段 的 x 为 输入元素 的 布局_x。
    字段 的 y 为 输入元素 的 布局_y。
    字段 的 宽 为 输入元素 的 布局_宽。
    字段 的 高 为 输入元素 的 布局_高。
    类型 为 做 元素_获取属性 用 输入元素、做 哈希_注册 用 「type」。
    名称 为 做 元素_获取属性 用 输入元素、做 哈希_注册 用 「name」。
    占位 为 做 元素_获取属性 用 输入元素、做 哈希_注册 用 「placeholder」。
    _自动 为 做 元素_获取属性 用 输入元素、做 哈希_注册 用 「autocomplete」。
    字段 的 类型属性 为 类型。
    字段 的 名称 为 名称。
    字段 的 默认值 为 占位。
    若 类型 等于 「password」：
        若 做 包含_注册 用 名称、「confirm」 或者 做 包含_注册 用 名称、「repeat」 或者 做 包含_注册 用 占位、「确认」：
            字段 的 字段类型 为 字段类型_确认密码。
        否则：
            字段 的 字段类型 为 字段类型_密码。
        完。
    否则若 类型 等于 「email」 或者 做 包含_注册 用 名称、「email」 或者 做 包含_注册 用 名称、「mail」 或者 做 包含_注册 用 占位、「邮箱」：
        字段 的 字段类型 为 字段类型_邮箱。
    否则若 类型 等于 「tel」 或者 做 包含_注册 用 名称、「phone」 或者 做 包含_注册 用 名称、「mobile」 或者 做 包含_注册 用 占位、「手机」：
        字段 的 字段类型 为 字段类型_手机。
    否则若 做 包含_注册 用 名称、「user」 或者 做 包含_注册 用 名称、「login」 或者 做 包含_注册 用 名称、「account」 或者 做 包含_注册 用 占位、「用户名」：
        字段 的 字段类型 为 字段类型_用户名。
    否则若 做 包含_注册 用 名称、「name」 或者 做 包含_注册 用 占位、「姓名」 或者 做 包含_注册 用 占位、「昵称」：
        字段 的 字段类型 为 字段类型_姓名。
    否则若 做 包含_注册 用 名称、「captcha」 或者 做 包含_注册 用 名称、「verify」 或者 做 包含_注册 用 名称、「code」 或者 做 包含_注册 用 占位、「验证码」：
        字段 的 字段类型 为 字段类型_验证码。
    否则若 类型 等于 「checkbox」：
        字段 的 字段类型 为 字段类型_同意条款。
    否则：
        字段 的 字段类型 为 字段类型_未知。
    完。
    返还 字段。
完。

表单_推断类型 接受 表单：
    可变 有密码 为 假。
    可变 有邮箱 为 假。
    可变 有确认密码 为 假。
    可变 有用户名 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 表单 的 字段列表：
        若 表单 的 字段列表 的 第 i 项 的 字段类型 等于 字段类型_密码：
            有密码 为 真。
        完。
        若 表单 的 字段列表 的 第 i 项 的 字段类型 等于 字段类型_邮箱：
            有邮箱 为 真。
        完。
        若 表单 的 字段列表 的 第 i 项 的 字段类型 等于 字段类型_确认密码：
            有确认密码 为 真。
        完。
        若 表单 的 字段列表 的 第 i 项 的 字段类型 等于 字段类型_用户名：
            有用户名 为 真。
        完。
        i 为 i 加 1。
    完。
    若 有确认密码 或者 有密码 并且 有邮箱 并且 做 计数 用 表单 的 字段列表 大于或等于 3：
        返还 1。
    完。
    若 有密码 并且 (有用户名 或者 有邮箱) 并且 做 计数 用 表单 的 字段列表 小于或等于 3：
        返还 2。
    完。
    返还 0。
完。

本章 收束。
