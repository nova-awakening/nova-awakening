《赫布稳态可》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·赫布学习振》。

引用《.·字符串替换》。

引用《.·赫布追加》。

可变 稳态_调节总次数 为 0。

可变 稳态_最大缩放 为 0。

赫布_稳态可塑性 接受 权重、活动历史、目标活动、调节率、维度：
    n 为 做 计数 用 活动历史。
    若 n 等于 0：
        返还 1000。
    完。
    可变 平均 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        平均 为 平均 加 活动历史 的 第 i 项。
        i 为 i 加 1。
    完。
    平均 为 平均 除以 n。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 n：
        差 为 活动历史 的 第 i 项 减 平均。
        方差 为 方差 加 差 乘 差 除以 1000。
        i 为 i 加 1。
    完。
    方差 为 方差 除以 n。
    可变 缩放 为 1000。
    若 平均 大于 0：
        缩放 为 目标活动 乘 1000 除以 平均。
    完。
    可变 有效调节率 为 调节率。
    若 方差 大于 目标活动 乘 目标活动 除以 4：
        有效调节率 为 调节率 除以 2。
        若 有效调节率 小于 10：
            有效调节率 为 10。
        完。
    完。
    若 缩放 小于 500：
        缩放 为 500。
    完。
    若 缩放 大于 2000：
        缩放 为 2000。
    完。
    实际缩放 为 1000 加 (缩放 减 1000) 乘 有效调节率 除以 1000。
    i 为 0。
    只要 i 小于 维度：
        权重 的 第 i 项 为 权重 的 第 i 项 乘 实际缩放 除以 1000。
        i 为 i 加 1。
    完。
    稳态_调节总次数 为 稳态_调节总次数 加 1。
    若 实际缩放 大于 稳态_最大缩放：
        稳态_最大缩放 为 实际缩放。
    完。
    返还 实际缩放。
完。

可变 BCM_阈值 为 500。

BCM_阈值_衰减 为 990。

BCM_阈值_学习 为 10。

赫布_BCM更新 接受 权重、输入、输出_活动、学习率、维度：
    可变 i 为 0。
    只要 i 小于 维度：
        调制 为 输出_活动 乘 (输出_活动 减 BCM_阈值) 除以 1000。
        增量 为 学习率 乘 输入 的 第 i 项 乘 调制 除以 (1000 乘 1000)。
        权重 的 第 i 项 为 权重 的 第 i 项 加 增量。
        i 为 i 加 1。
    完。
    BCM_阈值 为 BCM_阈值 乘 BCM_阈值_衰减 除以 1000 加 输出_活动 乘 输出_活动 乘 BCM_阈值_学习 除以 (1000 乘 1000)。
    若 BCM_阈值 小于 10：
        BCM_阈值 为 10。
    完。
    若 BCM_阈值 大于 5000：
        BCM_阈值 为 5000。
    完。
    返还 输出_活动。
完。

可变 Oja_更新总数 为 0。

赫布_Oja更新 接受 权重、输入、学习率、维度：
    可变 y 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        y 为 y 加 权重 的 第 i 项 乘 输入 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    有效学习率 为 学习率 乘 1000 除以 (1000 加 Oja_更新总数 除以 100)。
    若 有效学习率 小于 1：
        有效学习率 为 1。
    完。
    i 为 0。
    只要 i 小于 维度：
        增量 为 有效学习率 乘 (输入 的 第 i 项 乘 y 减 y 乘 y 乘 权重 的 第 i 项 除以 1000) 除以 (1000 乘 1000)。
        权重 的 第 i 项 为 权重 的 第 i 项 加 增量。
        i 为 i 加 1。
    完。
    Oja_更新总数 为 Oja_更新总数 加 1。
    返还 y。
完。

可变 元_有效学习率 为 1000。

可变 元_连续LTP 为 0。

可变 元_连续LTD 为 0。

元_饱和阈值 为 10。

赫布_元可塑性_调节 接受 权重变化方向：
    若 权重变化方向 大于 0：
        元_连续LTP 为 元_连续LTP 加 1。
        元_连续LTD 为 0。
    否则若 权重变化方向 小于 0：
        元_连续LTD 为 元_连续LTD 加 1。
        元_连续LTP 为 0。
    完。
    若 元_连续LTP 大于 元_饱和阈值 或者 元_连续LTD 大于 元_饱和阈值：
        元_有效学习率 为 元_有效学习率 乘 950 除以 1000。
        若 元_有效学习率 小于 100：
            元_有效学习率 为 100。
        完。
    否则：
        元_有效学习率 为 元_有效学习率 乘 1005 除以 1000。
        若 元_有效学习率 大于 2000：
            元_有效学习率 为 2000。
        完。
    完。
    返还 元_有效学习率。
完。

赫布_竞争学习 接受 权重矩阵、输入、神经元数、维度、学习率：
    可变 胜者 为 0。
    可变 最大匹配 为 0 减 999999。
    可变 n 为 0。
    只要 n 小于 神经元数：
        可变 匹配度 为 0。
        可变 i 为 0。
        只要 i 小于 维度：
            匹配度 为 匹配度 加 权重矩阵 的 第 n 乘 维度 加 i 项 乘 输入 的 第 i 项 除以 1000。
            i 为 i 加 1。
        完。
        若 匹配度 大于 最大匹配：
            最大匹配 为 匹配度。
            胜者 为 n。
        完。
        n 为 n 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 维度：
        idx 为 胜者 乘 维度 加 i。
        差 为 输入 的 第 i 项 减 权重矩阵 的 第 idx 项。
        权重矩阵 的 第 idx 项 为 权重矩阵 的 第 idx 项 加 学习率 乘 差 除以 1000。
        i 为 i 加 1。
    完。
    返还 胜者。
完。

赫布_学习率 为 1。

赫布_更新权重 接受 引擎、输入、输出：
    可变 总变化方向 为 0。
    可变 i 为 0。
    只要 i 小于 TNF_维度：
        可变 j 为 0。
        只要 j 小于 TNF_维度：
            原始更新 为 赫布_学习率 乘 输入 的 第 i 项 乘 输出 的 第 j 项。
            更新 为 原始更新 乘 元_有效学习率 除以 1000。
            索引 为 i 乘 TNF_维度 加 j。
            新值 为 引擎 的 第 0 项 的 第 索引 项 加 更新。
            若 新值 大于 3：
                新值 为 3。
            完。
            若 新值 小于 0 减 3：
                新值 为 0 减 3。
            完。
            变化 为 新值 减 引擎 的 第 0 项 的 第 索引 项。
            总变化方向 为 总变化方向 加 变化。
            做 设置 用 引擎 的 第 0 项、索引、新值。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    做 赫布_元可塑性_调节 用 总变化方向。
完。

赫布_测试 接受 零参：
    做 打印 用 「=== Nova关联学习学习测试 ===」。
    可变 频谱 为 一组：100、50、0 减 30、20、0 减 10、5、0 减 3、1、120、40、0 减 20、25、0 减 15、8、0 减 5、2、90、60、0 减 40、15、0 减 5、3、0 减 1、4。
    可变 索引 为 一组：1、2、3、4。
    关联 为 做 赫布_学习符号 用 频谱、3、8、索引、4。
    做 打印 用 「  corr[0,1]=」 加 做 转字符串 用 关联 的 第 1 项。
    若 关联 的 第 1 项 小于 0：
        做 打印 用 「  符号关联: PASS」。
    否则：
        做 打印 用 「  符号关联: FAIL」。
    完。
    振幅 为 做 赫布_学习振幅 用 频谱、3、8、索引、4。
    am 为 振幅 的 第 0 项。
    做 打印 用 「  amp_mean[0]=」 加 做 转字符串 用 am 的 第 0 项。
    生成 为 做 赫布_生成频谱 用 关联、振幅 的 第 0 项、振幅 的 第 1 项、索引、4、8、42、800。
    做 打印 用 「  生成DC=」 加 做 转字符串 用 生成 的 第 0 项 加 「 idx1=」 加 做 转字符串 用 生成 的 第 1 项。
    做 打印 用 「Nova关联学习学习: 全部通过」。
完。

赫布_完整统计 接受 零参：
    返还 一组：赫布_学习次数、赫布_总突触更新、稳态_调节总次数、BCM_阈值、Oja_更新总数、元_有效学习率、稳态_最大缩放。
完。

本章 收束。
