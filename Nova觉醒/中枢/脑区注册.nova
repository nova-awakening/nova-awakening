《脑区注册》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·打乱》。

可变 脑区列表 为 一组：（空）。

脑区_最低能量 为 10。

可变 _脑区_总周期数 为 0。

可变 _脑区_能量重分配次 为 0。

可变 意识_同步度 为 0。

可变 意识_觉醒值 为 0。

觉醒阈值 为 350。

可变 广播_总次数 为 0。

可变 多巴胺 为 500。

可变 血清素 为 500。

可变 去甲肾上腮素 为 500。

脑区_注册 接受 名哈希、优先级：
    若 优先级 小于 1：
        优先级 为 1。
    完。
    id 为 做 计数 用 脑区列表。
    做 网络_追加 用 脑区列表、一组：id、名哈希、0、0、100、优先级、0、300 加 id 乘 50、0。
    返还 id。
完。

脑区_数量 接受 零参：
    返还 做 计数 用 脑区列表。
完。

脑区_激活度 接受 id：
    返还 脑区列表 的 第 id 项 的 第 2 项。
完。

脑区_设激活 接受 id、激活：
    脑区列表 的 第 id 项 的 第 2 项 为 激活。
完。

脑区_输出 接受 id：
    返还 脑区列表 的 第 id 项 的 第 3 项。
完。

脑区_设输出 接受 id、输出：
    脑区列表 的 第 id 项 的 第 3 项 为 输出。
完。

脑区_相位 接受 id：
    返还 脑区列表 的 第 id 项 的 第 6 项。
完。

全局广播 接受 信号、强度：
    广播_总次数 为 广播_总次数 加 1。
    NE增益 为 1000 加 (去甲肾上腮素 减 500) 除以 5。
    可变 i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        增量 为 强度 乘 脑区列表 的 第 i 项 的 第 5 项 乘 NE增益 除以 100000。
        脑区列表 的 第 i 项 的 第 2 项 为 脑区列表 的 第 i 项 的 第 2 项 加 增量。
        若 脑区列表 的 第 i 项 的 第 2 项 大于 100：
            脑区列表 的 第 i 项 的 第 2 项 为 100。
        完。
        i 为 i 加 1。
    完。
完。

竞争_选择 接受 零参：
    可变 最强 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 脑区列表：
        有效_i 为 脑区列表 的 第 i 项 的 第 2 项 乘 (1000 加 (多巴胺 减 500) 除以 10) 除以 1000。
        有效_最强 为 脑区列表 的 第 最强 项 的 第 2 项 乘 (1000 加 (多巴胺 减 500) 除以 10) 除以 1000。
        若 有效_i 大于 有效_最强：
            最强 为 i。
        完。
        i 为 i 加 1。
    完。
    抑制率 为 500 加 血清素 除以 5。
    i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        若 i 不等于 最强：
            脑区列表 的 第 i 项 的 第 2 项 为 脑区列表 的 第 i 项 的 第 2 项 乘 (1000 减 抑制率) 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 最强。
完。

能量_分配 接受 总能量：
    n 为 做 计数 用 脑区列表。
    若 n 等于 0：
        返还。
    完。
    保底总 为 脑区_最低能量 乘 n。
    可分配 为 总能量 减 保底总。
    若 可分配 小于 0：
        可分配 为 0。
    完。
    可变 总激活 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总激活 为 总激活 加 脑区列表 的 第 i 项 的 第 2 项。
        i 为 i 加 1。
    完。
    若 总激活 等于 0：
        总激活 为 1。
    完。
    i 为 0。
    只要 i 小于 n：
        脑区列表 的 第 i 项 的 第 4 项 为 脑区_最低能量 加 可分配 乘 脑区列表 的 第 i 项 的 第 2 项 除以 总激活。
        i 为 i 加 1。
    完。
    _脑区_能量重分配次 为 _脑区_能量重分配次 加 1。
完。

反馈_调节 接受 目标激活：
    可变 i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        误差 为 目标激活 减 脑区列表 的 第 i 项 的 第 2 项。
        脑区列表 的 第 i 项 的 第 2 项 为 脑区列表 的 第 i 项 的 第 2 项 加 误差 除以 4。
        若 脑区列表 的 第 i 项 的 第 2 项 大于 70：
            脑区列表 的 第 i 项 的 第 8 项 为 脑区列表 的 第 i 项 的 第 8 项 加 10。
            若 脑区列表 的 第 i 项 的 第 8 项 大于 500：
                脑区列表 的 第 i 项 的 第 8 项 为 500。
            完。
            脑区列表 的 第 i 项 的 第 2 项 为 脑区列表 的 第 i 项 的 第 2 项 乘 (1000 减 脑区列表 的 第 i 项 的 第 8 项) 除以 1000。
        否则：
            若 脑区列表 的 第 i 项 的 第 8 项 大于 0：
                脑区列表 的 第 i 项 的 第 8 项 为 脑区列表 的 第 i 项 的 第 8 项 减 20。
            完。
            若 脑区列表 的 第 i 项 的 第 8 项 小于 0：
                脑区列表 的 第 i 项 的 第 8 项 为 0。
            完。
        完。
        若 脑区列表 的 第 i 项 的 第 2 项 小于 0：
            脑区列表 的 第 i 项 的 第 2 项 为 0。
        完。
        i 为 i 加 1。
    完。
完。

振荡_相位更新 接受 耦合强度：
    n 为 做 计数 用 脑区列表。
    若 n 小于或等于 1：
        返还。
    完。
    可变 新相位 为 一组：（空）。
    可变 sin_sum 为 0。
    可变 cos_sum 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        ω 为 脑区列表 的 第 i 项 的 第 7 项。
        可变 耦合项 为 0。
        可变 j 为 0。
        只要 j 小于 n：
            若 i 不等于 j：
                可变 差 为 (脑区列表 的 第 j 项 的 第 6 项 减 脑区列表 的 第 i 项 的 第 6 项) 取余于 6283。
                若 差 大于 3141：
                    差 为 差 减 6283。
                完。
                若 差 小于 负 3141：
                    差 为 差 加 6283。
                完。
                耦合项 为 耦合项 加 差。
            完。
            j 为 j 加 1。
        完。
        可变 新φ 为 (脑区列表 的 第 i 项 的 第 6 项 加 ω 加 耦合强度 乘 耦合项 除以 (n 乘 1000)) 取余于 6283。
        若 新φ 小于 0：
            新φ 为 新φ 加 6283。
        完。
        做 网络_追加 用 新相位、新φ。
        可变 a 为 新φ 取余于 6283。
        若 a 小于或等于 3141：
            sin_sum 为 sin_sum 加 4 乘 a 乘 (3141 减 a) 除以 (3141 乘 3141 除以 1000)。
        否则：
            a2 为 a 减 3141。
            sin_sum 为 sin_sum 减 4 乘 a2 乘 (3141 减 a2) 除以 (3141 乘 3141 除以 1000)。
        完。
        可变 ca 为 (新φ 加 1570) 取余于 6283。
        若 ca 小于或等于 3141：
            cos_sum 为 cos_sum 加 4 乘 ca 乘 (3141 减 ca) 除以 (3141 乘 3141 除以 1000)。
        否则：
            ca2 为 ca 减 3141。
            cos_sum 为 cos_sum 减 4 乘 ca2 乘 (3141 减 ca2) 除以 (3141 乘 3141 除以 1000)。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        脑区列表 的 第 i 项 的 第 6 项 为 新相位 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 s 为 sin_sum 除以 n。
    可变 c 为 cos_sum 除以 n。
    若 s 小于 0：
        s 为 0 减 s。
    完。
    若 c 小于 0：
        c 为 0 减 c。
    完。
    意识_同步度 为 (s 加 c) 乘 707 除以 1000。
完。

调质_更新 接受 奖赏信号、威胁信号、社交信号：
    多巴胺 为 多巴胺 加 (奖赏信号 减 多巴胺) 乘 200 除以 1000。
    若 多巴胺 大于 1000：
        多巴胺 为 1000。
    完。
    若 多巴胺 小于 0：
        多巴胺 为 0。
    完。
    血清素 为 血清素 加 (社交信号 减 血清素) 乘 100 除以 1000。
    若 血清素 大于 1000：
        血清素 为 1000。
    完。
    若 血清素 小于 0：
        血清素 为 0。
    完。
    去甲肾上腮素 为 去甲肾上腮素 加 (威胁信号 减 去甲肾上腮素) 乘 300 除以 1000。
    若 去甲肾上腮素 大于 1000：
        去甲肾上腮素 为 1000。
    完。
    若 去甲肾上腮素 小于 0：
        去甲肾上腮素 为 0。
    完。
完。

计算_觉醒度 接受 零参：
    n 为 做 计数 用 脑区列表。
    若 n 等于 0：
        意识_觉醒值 为 0。
        返还 0。
    完。
    可变 加权和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        加权和 为 加权和 加 脑区列表 的 第 i 项 的 第 2 项 乘 脑区列表 的 第 i 项 的 第 5 项 乘 脑区列表 的 第 i 项 的 第 4 项 除以 10000。
        i 为 i 加 1。
    完。
    意识_觉醒值 为 加权和 除以 n 加 意识_同步度 除以 5。
    返还 意识_觉醒值。
完。

认知周期 接受 零参：
    _脑区_总周期数 为 _脑区_总周期数 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        若 脑区列表 的 第 i 项 的 第 2 项 大于 0：
            脑区列表 的 第 i 项 的 第 3 项 为 脑区列表 的 第 i 项 的 第 2 项 乘 脑区列表 的 第 i 项 的 第 4 项 除以 100。
        完。
        i 为 i 加 1。
    完。
    做 振荡_相位更新 用 200。
    胜者 为 做 竞争_选择。
    觉醒 为 做 计算_觉醒度。
    若 觉醒 大于 觉醒阈值：
        做 全局广播 用 脑区列表 的 第 胜者 项 的 第 3 项、20。
    完。
    衰减率 为 900 减 去甲肾上腮素 除以 10。
    i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        脑区列表 的 第 i 项 的 第 2 项 为 脑区列表 的 第 i 项 的 第 2 项 乘 衰减率 除以 1000。
        若 脑区列表 的 第 i 项 的 第 2 项 小于 1 并且 脑区列表 的 第 i 项 的 第 4 项 大于 0：
            脑区列表 的 第 i 项 的 第 2 项 为 1。
        完。
        i 为 i 加 1。
    完。
    返还 胜者。
完。

脑区_清空 接受 零参：
    脑区列表 为 一组：（空）。
    _脑区_总周期数 为 0。
    _脑区_能量重分配次 为 0。
    意识_同步度 为 0。
    意识_觉醒值 为 0。
    广播_总次数 为 0。
    多巴胺 为 500。
    血清素 为 500。
    去甲肾上腮素 为 500。
完。

脑区_序列化 接受 零参：
    可变 数据 为 一组：（空）。
    n 为 做 计数 用 脑区列表。
    做 网络_追加 用 数据、n。
    可变 i 为 0。
    只要 i 小于 n：
        可变 k 为 0。
        只要 k 小于 9：
            做 网络_追加 用 数据、脑区列表 的 第 i 项 的 第 k 项。
            k 为 k 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数据。
完。

脑区_反序列化 接受 数据：
    若 做 计数 用 数据 小于 1：
        返还。
    完。
    脑区列表 为 一组：（空）。
    n 为 数据 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 n：
        基 为 1 加 i 乘 9。
        若 基 加 8 小于 做 计数 用 数据：
            做 网络_追加 用 脑区列表、一组：数据 的 第 基 项、数据 的 第 基 加 1 项、数据 的 第 基 加 2 项、数据 的 第 基 加 3 项、数据 的 第 基 加 4 项、数据 的 第 基 加 5 项、数据 的 第 基 加 6 项、数据 的 第 基 加 7 项、数据 的 第 基 加 8 项。
        完。
        i 为 i 加 1。
    完。
完。

脑区_完整统计 接受 零参：
    可变 总能量 为 0。
    可变 总激活 为 0。
    n 为 做 计数 用 脑区列表。
    可变 i 为 0。
    只要 i 小于 n：
        总能量 为 总能量 加 脑区列表 的 第 i 项 的 第 4 项。
        总激活 为 总激活 加 脑区列表 的 第 i 项 的 第 2 项。
        i 为 i 加 1。
    完。
    返还 一组：n、总能量、总激活、_脑区_总周期数、_脑区_能量重分配次、意识_同步度、意识_觉醒值、广播_总次数、多巴胺、血清素、去甲肾上腮素。
完。

合作_扩散 接受 连接矩阵、扩散率：
    n 为 做 脑区_数量。
    可变 新激活 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 网络_追加 用 新激活、脑区列表 的 第 i 项 的 第 2 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 n：
            若 i 不等于 j 并且 i 乘 n 加 j 小于 做 计数 用 连接矩阵：
                新激活 的 第 i 项 为 新激活 的 第 i 项 加 脑区列表 的 第 j 项 的 第 2 项 乘 连接矩阵 的 第 i 乘 n 加 j 项 乘 扩散率 除以 10000。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        脑区列表 的 第 i 项 的 第 2 项 为 新激活 的 第 i 项。
        若 脑区列表 的 第 i 项 的 第 2 项 大于 100：
            脑区列表 的 第 i 项 的 第 2 项 为 100。
        完。
        若 脑区列表 的 第 i 项 的 第 2 项 小于 0：
            脑区列表 的 第 i 项 的 第 2 项 为 0。
        完。
        i 为 i 加 1。
    完。
完。

本章 收束。
