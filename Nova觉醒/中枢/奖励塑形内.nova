《奖励塑形内》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

引用《.·内核打印》。

引用《.·数学工具》。

好奇心_系数 为 100。

可变 状态访问计数 为 一组：（空）。

可变 好奇心_总奖励 为 0。

好奇心_奖励 接受 状态哈希：
    可变 次数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 状态访问计数：
        若 状态访问计数 的 第 i 项 等于 状态哈希：
            次数 为 次数 加 1。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 状态访问计数、状态哈希。
    内在奖励 为 好奇心_系数 乘 1000 除以 (做 整数开方 用 次数 加 1 乘 1000 除以 1000 加 1)。
    好奇心_总奖励 为 好奇心_总奖励 加 内在奖励。
    返还 内在奖励。
完。

奖励_塑形 接受 外在奖励、状态哈希：
    内在 为 做 好奇心_奖励 用 状态哈希。
    返还 外在奖励 加 内在。
完。

云端_最大端点 为 8。

云端状态_离线 为 0。

云端状态_在线 为 1。

云端状态_忙碌 为 2。

云端端点 是 一种 事物，含 地址哈希、模型名哈希、状态、请求次数、成功次数、平均延迟_ms、最大令牌、蒸馏_启用。

可变 云端_端点列表 为 一组：（空）。

可变 云端_请求总数 为 0。

可变 云端_成功总数 为 0。

可变 云端_蒸馏样本数 为 0。

云端_注册端点 接受 地址哈希、模型名哈希、最大令牌：
    若 做 计数 用 云端_端点列表 大于或等于 云端_最大端点：
        返还 负 1。
    完。
    端点 为 新建 云端端点。
    端点 的 地址哈希 为 地址哈希。
    端点 的 模型名哈希 为 模型名哈希。
    端点 的 状态 为 云端状态_离线。
    端点 的 请求次数 为 0。
    端点 的 成功次数 为 0。
    端点 的 平均延迟_ms 为 0。
    端点 的 最大令牌 为 最大令牌。
    端点 的 蒸馏_启用 为 真。
    做 追加 用 云端_端点列表、端点。
    做 内核信息 用 「云端模型: 注册端点 模型哈希=」 加 做 整数转字符串 用 模型名哈希。
    返还 做 计数 用 云端_端点列表 减 1。
完。

云端_选择最优端点 接受 零参：
    可变 最佳 为 负 1。
    可变 最低延迟 为 999999。
    可变 i 为 0。
    只要 i 小于 做 计数 用 云端_端点列表：
        若 云端_端点列表 的 第 i 项 的 状态 等于 云端状态_在线：
            若 云端_端点列表 的 第 i 项 的 平均延迟_ms 小于 最低延迟：
                最低延迟 为 云端_端点列表 的 第 i 项 的 平均延迟_ms。
                最佳 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

云端_推理请求 接受 输入数据、最大令牌：
    端点索引 为 做 云端_选择最优端点。
    若 端点索引 小于 0：
        做 内核调试 用 「云端推理: 无可用端点, 回退本地TNF」。
        返还 空。
    完。
    端点 为 云端_端点列表 的 第 端点索引 项。
    端点 的 请求次数 为 端点 的 请求次数 加 1。
    云端_请求总数 为 云端_请求总数 加 1。
    请求 为 做 构造推理请求 用 端点 的 地址哈希、输入数据、最大令牌。
    返还 请求。
完。

云端_处理响应 接受 端点索引、响应、延迟_ms：
    若 端点索引 大于或等于 做 计数 用 云端_端点列表：
        返还。
    完。
    端点 为 云端_端点列表 的 第 端点索引 项。
    端点 的 成功次数 为 端点 的 成功次数 加 1。
    云端_成功总数 为 云端_成功总数 加 1。
    端点 的 平均延迟_ms 为 (端点 的 平均延迟_ms 乘 900 加 延迟_ms 乘 100) 除以 1000。
    若 端点 的 蒸馏_启用 并且 响应 不等于 空：
        做 云端_知识蒸馏 用 响应。
    完。
完。

云端_知识蒸馏 接受 教师输出：
    云端_蒸馏样本数 为 云端_蒸馏样本数 加 1。
完。

云端_状态名 接受 状态：
    若 状态 等于 云端状态_离线：
        返还 「OFFLINE」。
    完。
    若 状态 等于 云端状态_在线：
        返还 「ONLINE」。
    完。
    若 状态 等于 云端状态_忙碌：
        返还 「BUSY」。
    完。
    返还 「UNKNOWN」。
完。

本章 收束。
