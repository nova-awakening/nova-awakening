《字符串替换》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·硬链接》。

引用《..·外联·URL解析辅助》。

引用《..·引导·端口IO》。

引用《.·整数转字符串》。

字符串_替换 接受 s、旧、新：
    可变 结果 为 「」。
    可变 位置 为 0。
    旧长 为 做 字符串长度 用 旧。
    s长 为 做 字符串长度 用 s。
    只要 位置 小于 s长：
        找到 为 做 字符串查找_从 用 s、旧、位置。
        若 找到 小于 0：
            结果 为 结果 加 做 字符串子串 用 s、位置、s长 减 位置。
            中断。
        完。
        结果 为 结果 加 做 字符串子串 用 s、位置、找到 减 位置 加 新。
        位置 为 找到 加 旧长。
    完。
    返还 结果。
完。

字符串_替换首个 接受 s、旧、新：
    位置 为 做 字符串查找 用 s、旧。
    若 位置 小于 0：
        返还 s。
    完。
    旧长 为 做 字符串长度 用 旧。
    返还 做 字符串子串 用 s、0、位置 加 新 加 做 字符串子串 用 s、位置 加 旧长、做 字符串长度 用 s 减 位置 减 旧长。
完。

是数字字符 接受 c：
    返还 c 大于或等于 48 并且 c 小于或等于 57。
完。

是字母字符 接受 c：
    返还 c 大于或等于 65 并且 c 小于或等于 90 或者 c 大于或等于 97 并且 c 小于或等于 122。
完。

是空白字符 接受 c：
    返还 c 等于 32 或者 c 等于 9 或者 c 等于 10 或者 c 等于 13。
完。

字符串_是数字 接受 s：
    长度 为 做 字符串长度 用 s。
    若 长度 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    若 做 编码 用 做 字符串取字符 用 s、0 等于 45：
        i 为 1。
    完。
    只要 i 小于 长度：
        若 非 做 是数字字符 用 做 编码 用 做 字符串取字符 用 s、i：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

内存比较 接受 地址1、地址2、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        a 为 做 内存读8 用 地址1 加 i。
        b 为 做 内存读8 用 地址2 加 i。
        若 a 不等于 b：
            返还 a 减 b。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

内存设置 接受 地址、值、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        做 内存写8 用 地址 加 i、值。
        i 为 i 加 1。
    完。
完。

内存复制 接受 目标、源、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        做 内存写8 用 目标 加 i、做 内存读8 用 源 加 i。
        i 为 i 加 1。
    完。
完。

十六进制转整数 接受 s：
    长度 为 做 字符串长度 用 s。
    可变 起始 为 0。
    若 长度 大于或等于 2 并且 做 字符串子串 用 s、0、2 等于 「0x」：
        起始 为 2。
    完。
    可变 结果 为 0。
    可变 i 为 起始。
    只要 i 小于 长度：
        c 为 做 编码 用 做 字符串取字符 用 s、i。
        结果 为 结果 乘 16。
        若 c 大于或等于 48 并且 c 小于或等于 57：
            结果 为 结果 加 (c 减 48)。
        否则若 c 大于或等于 65 并且 c 小于或等于 70：
            结果 为 结果 加 (c 减 55)。
        否则若 c 大于或等于 97 并且 c 小于或等于 102：
            结果 为 结果 加 (c 减 87)。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

转字符串 接受 n：
    返还 做 字符 用 48 加 n。
完。

内存清零 接受 地址、长度：
    做 内存设置 用 地址、0、长度。
完。

内存填充 接受 地址、值、长度：
    做 内存设置 用 地址、值、长度。
完。

字符串子串 接受 s、起始、长度：
    返还 做 子字符串 用 s、起始、长度。
完。

字符串取字符 接受 s、索引：
    返还 做 编码 用 s、索引。
完。

缓冲写 接受 buf、偏移、值：
    做 内存写8 用 buf 加 偏移、值 位与 0xFF。
完。

缓冲读 接受 buf、偏移：
    返还 做 内存读8 用 buf 加 偏移。
完。

缓冲写16 接受 buf、偏移、值：
    做 内存写8 用 buf 加 偏移、值 位与 0xFF。
    做 内存写8 用 buf 加 偏移 加 1、值 右移 8 位与 0xFF。
完。

缓冲读16 接受 buf、偏移：
    返还 做 内存读8 用 buf 加 偏移 位或 做 内存读8 用 buf 加 偏移 加 1 左移 8。
完。

缓冲写32 接受 buf、偏移、值：
    做 内存写8 用 buf 加 偏移、值 位与 0xFF。
    做 内存写8 用 buf 加 偏移 加 1、值 右移 8 位与 0xFF。
    做 内存写8 用 buf 加 偏移 加 2、值 右移 16 位与 0xFF。
    做 内存写8 用 buf 加 偏移 加 3、值 右移 24 位与 0xFF。
完。

缓冲读32 接受 buf、偏移：
    返还 做 内存读8 用 buf 加 偏移 位或 做 内存读8 用 buf 加 偏移 加 1 左移 8 位或 做 内存读8 用 buf 加 偏移 加 2 左移 16 位或 做 内存读8 用 buf 加 偏移 加 3 左移 24。
完。

字符串最后索引 接受 s、ch：
    可变 i 为 做 字符串长度 用 s 减 1。
    只要 i 大于或等于 0：
        若 做 编码 用 s、i 等于 ch：
            返还 i。
        完。
        i 为 i 减 1。
    完。
    返还 负 1。
完。

字符串分割 接受 s、分隔符：
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    长 为 做 字符串长度 用 s。
    只要 i 小于 长：
        若 做 编码 用 s、i 等于 分隔符：
            做 追加 用 结果、当前。
            当前 为 「」。
        否则：
            当前 为 当前 加 做 字符 用 做 编码 用 s、i。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 结果、当前。
    返还 结果。
完。

字符串格式 接受 格式、参数列表：
    可变 结果 为 「」。
    可变 i 为 0。
    可变 参数索引 为 0。
    长 为 做 字符串长度 用 格式。
    只要 i 小于 长：
        若 做 编码 用 格式、i 等于 37 并且 i 加 1 小于 长：
            下一个 为 做 编码 用 格式、i 加 1。
            若 下一个 等于 100 并且 参数索引 小于 做 计数 用 参数列表：
                结果 为 结果 加 做 整数转字符串 用 参数列表 的 第 参数索引 项。
                参数索引 为 参数索引 加 1。
                i 为 i 加 2。
            否则若 下一个 等于 115 并且 参数索引 小于 做 计数 用 参数列表：
                结果 为 结果 加 参数列表 的 第 参数索引 项。
                参数索引 为 参数索引 加 1。
                i 为 i 加 2。
            否则：
                结果 为 结果 加 做 字符 用 做 编码 用 格式、i。
                i 为 i 加 1。
            完。
        否则：
            结果 为 结果 加 做 字符 用 做 编码 用 格式、i。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

字符串_长度 接受 s：
    返还 做 字符串长度 用 s。
完。

字符串_截取 接受 s、起始、结束：
    返还 做 子字符串 用 s、起始、结束 减 起始。
完。

内存置零 接受 地址、长度：
    做 内存设置 用 地址、0、长度。
完。

读取字节 接受 地址、偏移：
    返还 做 内存读8 用 地址 加 偏移。
完。

内存写 接受 地址、值：
    做 内存写8 用 地址、值 位与 0xFF。
完。

缓冲写64 接受 buf、偏移、值：
    做 缓冲写32 用 buf、偏移、值 位与 0xFFFFFFFF。
    做 缓冲写32 用 buf、偏移 加 4、值 右移 32 位与 0xFFFFFFFF。
完。

缓冲_读u8 接受 buf、偏移：
    返还 做 内存读8 用 buf 加 偏移。
完。

缓冲_写u8 接受 buf、偏移、值：
    做 内存写8 用 buf 加 偏移、值 位与 0xFF。
完。

AI操作窗口 为 16。

操作类型_替换 为 1。

操作类型_查找 为 2。

操作类型_分割 为 3。

操作类型_格式化 为 4。

操作类型_比较 为 5。

操作类型_复制 为 6。

字符串统计 是 一种 事物，含 替换次数、替换首个次数、查找次数、分割次数、格式化次数、内存比较次数、内存复制次数、内存清零次数、十六进制转换次数、缓冲读写次数、字符判断次数、AI预测次数、AI预测命中、总处理字节、最长处理字符串、错误次数。

可变 全局统计 为 新建 字符串统计。

可变 字符串模块已初始化 为 假。

可变 AI操作历史 为 一组：（空）。

可变 AI操作位置 为 0。

字符串模块_初始化 接受 零参：
    若 字符串模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI操作历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI操作窗口：
        做 追加 用 AI操作历史、0。
        i 为 i 加 1。
    完。
    AI操作位置 为 0。
    字符串模块已初始化 为 真。
    做 打印 用 「[字符串] 初始化完成」。
    返还 0。
完。

字符串模块_销毁 接受 零参：
    若 字符串模块已初始化 等于 假：
        返还 0。
    完。
    AI操作历史 为 一组：（空）。
    字符串模块已初始化 为 假。
    做 打印 用 「[字符串] 已销毁」。
    返还 0。
完。

字符串_安全替换 接受 s、旧、新：
    若 s 等于 空 或者 旧 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 「」。
    完。
    全局统计 的 替换次数 为 全局统计 的 替换次数 加 1。
    做 AI_记录操作 用 操作类型_替换。
    返还 做 字符串_替换 用 s、旧、新。
完。

字符串_安全替换首个 接受 s、旧、新：
    若 s 等于 空 或者 旧 等于 空：
        返还 「」。
    完。
    全局统计 的 替换首个次数 为 全局统计 的 替换首个次数 加 1。
    返还 做 字符串_替换首个 用 s、旧、新。
完。

字符串_重复 接受 s、次数：
    若 s 等于 空 或者 次数 小于或等于 0：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 次数：
        结果 为 结果 加 s。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串_填充左 接受 s、目标长度、填充字符：
    当前长 为 做 字符串长度 用 s。
    若 当前长 大于或等于 目标长度：
        返还 s。
    完。
    可变 填充 为 「」。
    可变 i 为 0。
    只要 i 小于 目标长度 减 当前长：
        填充 为 填充 加 做 字符 用 填充字符。
        i 为 i 加 1。
    完。
    返还 填充 加 s。
完。

字符串_填充右 接受 s、目标长度、填充字符：
    当前长 为 做 字符串长度 用 s。
    若 当前长 大于或等于 目标长度：
        返还 s。
    完。
    可变 填充 为 s。
    可变 i 为 当前长。
    只要 i 小于 目标长度：
        填充 为 填充 加 做 字符 用 填充字符。
        i 为 i 加 1。
    完。
    返还 填充。
完。

字符串_包含 接受 s、子串：
    若 s 等于 空 或者 子串 等于 空：
        返还 假。
    完。
    位置 为 做 字符串查找 用 s、子串。
    返还 位置 大于或等于 0。
完。

字符串_开头是 接受 s、前缀：
    若 s 等于 空 或者 前缀 等于 空：
        返还 假。
    完。
    前缀长 为 做 字符串长度 用 前缀。
    若 做 字符串长度 用 s 小于 前缀长：
        返还 假。
    完。
    返还 做 字符串子串 用 s、0、前缀长 等于 前缀。
完。

字符串_结尾是 接受 s、后缀：
    若 s 等于 空 或者 后缀 等于 空：
        返还 假。
    完。
    s长 为 做 字符串长度 用 s。
    后缀长 为 做 字符串长度 用 后缀。
    若 s长 小于 后缀长：
        返还 假。
    完。
    返还 做 字符串子串 用 s、s长 减 后缀长、后缀长 等于 后缀。
完。

字符串_计数出现 接受 s、子串：
    若 s 等于 空 或者 子串 等于 空：
        返还 0。
    完。
    可变 计数 为 0。
    可变 位置 为 0。
    子串长 为 做 字符串长度 用 子串。
    只要 位置 小于 做 字符串长度 用 s：
        找到 为 做 字符串查找_从 用 s、子串、位置。
        若 找到 小于 0：
            返还 计数。
        完。
        计数 为 计数 加 1。
        位置 为 找到 加 子串长。
    完。
    返还 计数。
完。

字符串_转大写 接受 s：
    若 s 等于 空：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 s：
        c 为 做 编码 用 做 字符串取字符 用 s、i。
        若 c 大于或等于 97 并且 c 小于或等于 122：
            结果 为 结果 加 做 字符 用 c 减 32。
        否则：
            结果 为 结果 加 做 字符 用 c。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串_转小写 接受 s：
    若 s 等于 空：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 s：
        c 为 做 编码 用 做 字符串取字符 用 s、i。
        若 c 大于或等于 65 并且 c 小于或等于 90：
            结果 为 结果 加 做 字符 用 c 加 32。
        否则：
            结果 为 结果 加 做 字符 用 c。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串_去空白 接受 s：
    若 s 等于 空：
        返还 「」。
    完。
    可变 起始 为 0。
    可变 结束 为 做 字符串长度 用 s。
    只要 起始 小于 结束 并且 做 是空白字符 用 做 编码 用 做 字符串取字符 用 s、起始：
        起始 为 起始 加 1。
    完。
    只要 结束 大于 起始 并且 做 是空白字符 用 做 编码 用 做 字符串取字符 用 s、结束 减 1：
        结束 为 结束 减 1。
    完。
    返还 做 字符串子串 用 s、起始、结束 减 起始。
完。

字符串_连接列表 接受 列表、分隔符：
    若 列表 等于 空 或者 做 计数 用 列表 等于 0：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 i 大于 0 并且 分隔符 不等于 空：
            结果 为 结果 加 分隔符。
        完。
        结果 为 结果 加 做 转字符串 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

整数转十六进制 接受 值：
    若 值 等于 0：
        返还 「0」。
    完。
    可变 结果 为 「」。
    可变 v 为 值。
    若 v 小于 0：
        v 为 0 减 v。
    完。
    只要 v 大于 0：
        余 为 v 取余于 16。
        若 余 小于 10：
            结果 为 做 字符串拼接 用 做 字符 用 48 加 余、结果。
        否则：
            结果 为 做 字符串拼接 用 做 字符 用 55 加 余、结果。
        完。
        v 为 v 除以 16。
    完。
    若 值 小于 0：
        结果 为 做 字符串拼接 用 「-」、结果。
    完。
    返还 结果。
完。

AI_记录操作 接受 操作类型：
    做 设置 用 AI操作历史、AI操作位置、操作类型。
    AI操作位置 为 (AI操作位置 加 1) 取余于 AI操作窗口。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
完。

AI_预测下次操作 接受 零参：
    可变 最频操作 为 0。
    可变 最频次 为 0。
    可变 i 为 0。
    只要 i 小于 AI操作窗口：
        若 AI操作历史 的 第 i 项 等于 0：
            i 为 i 加 1。
            继续。
        完。
        当前 为 AI操作历史 的 第 i 项。
        可变 频 为 0。
        可变 j 为 0。
        只要 j 小于 AI操作窗口：
            若 AI操作历史 的 第 j 项 等于 当前：
                频 为 频 加 1。
            完。
            j 为 j 加 1。
        完。
        若 频 大于 最频次：
            最频次 为 频。
            最频操作 为 当前。
        完。
        i 为 i 加 1。
    完。
    若 最频次 大于 AI操作窗口 除以 3：
        全局统计 的 AI预测命中 为 全局统计 的 AI预测命中 加 1。
    完。
    返还 最频操作。
完。

AI_操作名 接受 类型：
    若 类型 等于 操作类型_替换：
        返还 「替换」。
    完。
    若 类型 等于 操作类型_查找：
        返还 「查找」。
    完。
    若 类型 等于 操作类型_分割：
        返还 「分割」。
    完。
    若 类型 等于 操作类型_格式化：
        返还 「格式化」。
    完。
    若 类型 等于 操作类型_比较：
        返还 「比较」。
    完。
    若 类型 等于 操作类型_复制：
        返还 「复制」。
    完。
    返还 「未知」。
完。

字符串模块_转储 接受 零参：
    做 打印 用 「[字符串] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  AI预测下次: 」、做 AI_操作名 用 做 AI_预测下次操作。
    返还 0。
完。

字符串模块_打印统计 接受 零参：
    做 打印 用 「[字符串] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  替换次数: 」、做 转字符串 用 全局统计 的 替换次数。
    做 打印 用 做 字符串拼接 用 「  替换首个: 」、做 转字符串 用 全局统计 的 替换首个次数。
    做 打印 用 做 字符串拼接 用 「  查找次数: 」、做 转字符串 用 全局统计 的 查找次数。
    做 打印 用 做 字符串拼接 用 「  分割次数: 」、做 转字符串 用 全局统计 的 分割次数。
    做 打印 用 做 字符串拼接 用 「  格式化: 」、做 转字符串 用 全局统计 的 格式化次数。
    做 打印 用 做 字符串拼接 用 「  内存比较: 」、做 转字符串 用 全局统计 的 内存比较次数。
    做 打印 用 做 字符串拼接 用 「  内存复制: 」、做 转字符串 用 全局统计 的 内存复制次数。
    做 打印 用 做 字符串拼接 用 「  内存清零: 」、做 转字符串 用 全局统计 的 内存清零次数。
    做 打印 用 做 字符串拼接 用 「  进制转换: 」、做 转字符串 用 全局统计 的 十六进制转换次数。
    做 打印 用 做 字符串拼接 用 「  缓冲读写: 」、做 转字符串 用 全局统计 的 缓冲读写次数。
    做 打印 用 做 字符串拼接 用 「  字符判断: 」、做 转字符串 用 全局统计 的 字符判断次数。
    做 打印 用 做 字符串拼接 用 「  AI预测: 」、做 转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI命中: 」、做 转字符串 用 全局统计 的 AI预测命中。
    做 打印 用 做 字符串拼接 用 「  总处理字节: 」、做 转字符串 用 全局统计 的 总处理字节。
    做 打印 用 做 字符串拼接 用 「  最长字符串: 」、做 转字符串 用 全局统计 的 最长处理字符串。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

字符串模块_注册到内核 接受 内核上下文：
    若 字符串模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[字符串] 已注册到内核上下文」。
    返还 0。
完。

字符串模块_查询状态 接受 零参：
    若 字符串模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

字符串模块_健康检查 接受 零参：
    若 字符串模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 替换次数 加 全局统计 的 查找次数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
