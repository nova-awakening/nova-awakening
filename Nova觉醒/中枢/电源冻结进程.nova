《电源冻结进程》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·引导·MSR模型寄存器》。

引用《.·内核打印》。

引用《..·引导·寄存器定义》。

引用《..·引导·特权指令》。

引用《.·页表重建》。

引用《.·整数转字符串》。

PM_状态_运行 为 0。

PM_状态_冻结中 为 1。

PM_状态_挂起中 为 2。

PM_状态_已挂起 为 3。

PM_状态_恢复中 为 4。

PM_状态_休眠中 为 5。

PM_冻结超时_ms 为 5000。

PM_最大唤醒锁 为 32。

PM_最大设备 为 128。

可变 pm_冻结计数 为 0。

可变 pm_当前状态 为 0。

电源管理统计 是 一种 事物，含 挂起次数、恢复次数、冻结次数、解冻次数、冻结失败次数、设备挂起失败次数、唤醒锁获取数、唤醒锁释放数、唤醒锁阻止挂起数、热限流次数。

可变 全局_pm统计 为 空。

pm统计_初始化 接受 零参：
    若 全局_pm统计 等于 空：
        全局_pm统计 为 新建 电源管理统计。
        全局_pm统计 的 挂起次数 为 0。
        全局_pm统计 的 恢复次数 为 0。
        全局_pm统计 的 冻结次数 为 0。
        全局_pm统计 的 解冻次数 为 0。
        全局_pm统计 的 冻结失败次数 为 0。
        全局_pm统计 的 设备挂起失败次数 为 0。
        全局_pm统计 的 唤醒锁获取数 为 0。
        全局_pm统计 的 唤醒锁释放数 为 0。
        全局_pm统计 的 唤醒锁阻止挂起数 为 0。
        全局_pm统计 的 热限流次数 为 0。
    完。
完。

pm_冻结进程 接受 零参：
    做 pm统计_初始化。
    做 内核信息 用 「PM: 冻结用户进程...」。
    pm_当前状态 为 PM_状态_冻结中。
    pm_冻结计数 为 0。
    可变 冻结失败 为 0。
    开始时间 为 做 系统时间_毫秒。
    可变 i 为 0。
    只要 i 小于 做 计数 用 进程链表：
        p 为 进程链表 的 第 i 项。
        若 p 的 pid 大于 1 并且 p 的 状态 不等于 进程_僵尸：
            已用时 为 做 系统时间_毫秒 减 开始时间。
            若 已用时 大于 PM_冻结超时_ms：
                做 内核警告 用 「PM: 冻结超时, 已冻结 」 加 做 整数转字符串 用 pm_冻结计数 加 「 个进程」。
                全局_pm统计 的 冻结失败次数 为 全局_pm统计 的 冻结失败次数 加 1。
                pm_当前状态 为 PM_状态_运行。
                返还 错误_超时。
            完。
            若 做 pm_进程持有唤醒锁 用 p 的 pid：
                做 内核信息 用 「PM: 进程 」 加 做 整数转字符串 用 p 的 pid 加 「 持有唤醒锁, 跳过」。
                冻结失败 为 冻结失败 加 1。
                i 为 i 加 1。
                继续。
            完。
            p 的 冻结标志 为 真。
            pm_冻结计数 为 pm_冻结计数 加 1。
        完。
        i 为 i 加 1。
    完。
    做 内核信息 用 「PM: 已冻结 」 加 做 整数转字符串 用 pm_冻结计数 加 「 个进程, 跳过 」 加 做 整数转字符串 用 冻结失败。
    全局_pm统计 的 冻结次数 为 全局_pm统计 的 冻结次数 加 1。
    返还 成功。
完。

pm_解冻进程 接受 零参：
    做 pm统计_初始化。
    做 内核信息 用 「PM: 解冻进程...」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 进程链表：
        进程链表 的 第 i 项 的 冻结标志 为 假。
        i 为 i 加 1。
    完。
    pm_冻结计数 为 0。
    pm_当前状态 为 PM_状态_运行。
    全局_pm统计 的 解冻次数 为 全局_pm统计 的 解冻次数 加 1。
完。

pm_冻结内核线程 接受 零参：
    做 内核信息 用 「PM: 冻结可冻结的内核线程...」。
    可变 冻结数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 进程链表：
        p 为 进程链表 的 第 i 项。
        若 p 的 pid 大于 0 并且 p 的 是内核线程 并且 p 的 可冻结 并且 p 的 状态 不等于 进程_僵尸：
            p 的 冻结标志 为 真。
            冻结数 为 冻结数 加 1。
        完。
        i 为 i 加 1。
    完。
    做 内核信息 用 「PM: 已冻结 」 加 做 整数转字符串 用 冻结数 加 「 个内核线程」。
    返还 成功。
完。

pm_解冻内核线程 接受 零参：
    做 内核信息 用 「PM: 解冻内核线程...」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 进程链表：
        若 进程链表 的 第 i 项 的 是内核线程：
            进程链表 的 第 i 项 的 冻结标志 为 假。
        完。
        i 为 i 加 1。
    完。
完。

唤醒锁 是 一种 事物，含 名称、持有者PID、获取时间、超时_ms、活跃。

可变 唤醒锁列表 为 一组：（空）。

pm_获取唤醒锁 接受 名称、pid、超时_ms：
    做 pm统计_初始化。
    若 做 计数 用 唤醒锁列表 大于或等于 PM_最大唤醒锁：
        做 内核警告 用 「PM: 唤醒锁已满」。
        返还 错误_资源不足。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 唤醒锁列表：
        若 唤醒锁列表 的 第 i 项 的 名称 等于 名称 并且 唤醒锁列表 的 第 i 项 的 持有者PID 等于 pid：
            唤醒锁列表 的 第 i 项 的 获取时间 为 做 系统时间_毫秒。
            唤醒锁列表 的 第 i 项 的 超时_ms 为 超时_ms。
            唤醒锁列表 的 第 i 项 的 活跃 为 真。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    锁 为 新建 唤醒锁。
    锁 的 名称 为 名称。
    锁 的 持有者PID 为 pid。
    锁 的 获取时间 为 做 系统时间_毫秒。
    锁 的 超时_ms 为 超时_ms。
    锁 的 活跃 为 真。
    做 追加 用 唤醒锁列表、锁。
    全局_pm统计 的 唤醒锁获取数 为 全局_pm统计 的 唤醒锁获取数 加 1。
    返还 成功。
完。

pm_释放唤醒锁 接受 名称、pid：
    做 pm统计_初始化。
    可变 i 为 0。
    只要 i 小于 做 计数 用 唤醒锁列表：
        若 唤醒锁列表 的 第 i 项 的 名称 等于 名称 并且 唤醒锁列表 的 第 i 项 的 持有者PID 等于 pid：
            唤醒锁列表 的 第 i 项 的 活跃 为 假。
            全局_pm统计 的 唤醒锁释放数 为 全局_pm统计 的 唤醒锁释放数 加 1。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    返还 错误_未找到。
完。

pm_清理过期唤醒锁 接受 零参：
    当前时间 为 做 系统时间_毫秒。
    可变 i 为 做 计数 用 唤醒锁列表 减 1。
    只要 i 大于或等于 0：
        锁 为 唤醒锁列表 的 第 i 项。
        若 锁 的 活跃 并且 锁 的 超时_ms 大于 0：
            若 当前时间 减 锁 的 获取时间 大于 锁 的 超时_ms：
                唤醒锁列表 的 第 i 项 的 活跃 为 假。
            完。
        完。
        i 为 i 减 1。
    完。
完。

pm_进程持有唤醒锁 接受 pid：
    做 pm_清理过期唤醒锁。
    可变 i 为 0。
    只要 i 小于 做 计数 用 唤醒锁列表：
        若 唤醒锁列表 的 第 i 项 的 持有者PID 等于 pid 并且 唤醒锁列表 的 第 i 项 的 活跃：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

pm_有活跃唤醒锁 接受 零参：
    做 pm_清理过期唤醒锁。
    可变 i 为 0。
    只要 i 小于 做 计数 用 唤醒锁列表：
        若 唤醒锁列表 的 第 i 项 的 活跃：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

可变 pm_设备列表 为 一组：（空）。

pm_注册设备 接受 设备、挂起回调、恢复回调：
    若 做 计数 用 pm_设备列表 大于或等于 PM_最大设备：
        做 内核警告 用 「PM: 设备PM列表已满」。
        返还 错误_资源不足。
    完。
    做 追加 用 pm_设备列表、一组：设备、挂起回调、恢复回调。
    返还 成功。
完。

pm_注销设备 接受 设备：
    可变 i 为 0。
    只要 i 小于 做 计数 用 pm_设备列表：
        若 pm_设备列表 的 第 i 项 的 第 0 项 等于 设备：
            做 删除 用 pm_设备列表、i。
            返还 成功。
        完。
        i 为 i 加 1。
    完。
    返还 错误_未找到。
完。

pm_挂起设备 接受 零参：
    做 pm统计_初始化。
    可变 i 为 做 计数 用 pm_设备列表 减 1。
    只要 i 大于或等于 0：
        设备 为 pm_设备列表 的 第 i 项。
        若 设备 的 第 1 项 不等于 空：
            结果 为 做 设备 的 第 1 项 用 设备 的 第 0 项。
            若 结果 不等于 成功：
                做 内核警告 用 「PM: 设备挂起失败, 索引=」 加 做 整数转字符串 用 i。
                全局_pm统计 的 设备挂起失败次数 为 全局_pm统计 的 设备挂起失败次数 加 1。
                做 pm_恢复设备_从 用 i 加 1。
                返还 结果。
            完。
        完。
        i 为 i 减 1。
    完。
    返还 成功。
完。

pm_恢复设备 接受 零参：
    做 pm_恢复设备_从 用 0。
完。

pm_恢复设备_从 接受 起始：
    可变 i 为 起始。
    只要 i 小于 做 计数 用 pm_设备列表：
        设备 为 pm_设备列表 的 第 i 项。
        若 设备 的 第 2 项 不等于 空：
            结果 为 做 设备 的 第 2 项 用 设备 的 第 0 项。
            若 结果 不等于 成功：
                做 内核警告 用 「PM: 设备恢复失败, 索引=」 加 做 整数转字符串 用 i。
            完。
        完。
        i 为 i 加 1。
    完。
完。

可变 pm_保存的CR3 为 0。

可变 pm_保存的EFER 为 0。

可变 pm_保存的RSP 为 0。

可变 pm_保存的RBP 为 0。

可变 pm_保存的IDT 为 0。

可变 pm_保存的GDT 为 0。

pm_保存CPU状态 接受 零参：
    pm_保存的CR3 为 做 读取CR3。
    pm_保存的EFER 为 做 读取MSR 用 0xC0000080。
    pm_保存的IDT 为 做 读取IDT。
    pm_保存的GDT 为 做 读取GDT。
    做 内核信息 用 「PM: CPU状态已保存」。
完。

pm_恢复CPU状态 接受 零参：
    做 写CR3 用 pm_保存的CR3。
    做 写MSR 用 0xC0000080、pm_保存的EFER。
    做 写IDT 用 pm_保存的IDT。
    做 写GDT 用 pm_保存的GDT。
    做 内核信息 用 「PM: CPU状态已恢复」。
完。

pm_禁用非启动CPU 接受 零参：
    做 内核信息 用 「PM: 禁用非启动CPU」。
    可变 i 为 1。
    只要 i 小于 CPU核心数：
        若 CPU状态 的 第 i 项 等于 CPU_在线：
            CPU状态 的 第 i 项 为 CPU_离线。
            做 发送IPI 用 i、IPI_停止。
        完。
        i 为 i 加 1。
    完。
完。

pm_启用非启动CPU 接受 零参：
    做 内核信息 用 「PM: 重新启用非启动CPU」。
    可变 i 为 1。
    只要 i 小于 CPU核心数：
        若 CPU状态 的 第 i 项 等于 CPU_离线：
            CPU状态 的 第 i 项 为 CPU_在线。
            做 发送IPI 用 i、IPI_启动。
        完。
        i 为 i 加 1。
    完。
完。

pm_系统挂起 接受 零参：
    做 pm统计_初始化。
    做 内核信息 用 「PM: 开始系统挂起流程」。
    若 做 pm_有活跃唤醒锁：
        做 内核警告 用 「PM: 有活跃唤醒锁, 阻止挂起」。
        全局_pm统计 的 唤醒锁阻止挂起数 为 全局_pm统计 的 唤醒锁阻止挂起数 加 1。
        返还 错误_忙。
    完。
    结果 为 做 pm_冻结进程。
    若 结果 不等于 成功：
        做 内核警告 用 「PM: 冻结进程失败, 中止挂起」。
        返还 结果。
    完。
    做 pm_冻结内核线程。
    结果 为 做 pm_挂起设备。
    若 结果 不等于 成功：
        做 内核警告 用 「PM: 设备挂起失败, 回滚」。
        做 pm_解冻内核线程。
        做 pm_解冻进程。
        返还 结果。
    完。
    做 pm_禁用非启动CPU。
    做 pm_保存CPU状态。
    pm_当前状态 为 PM_状态_已挂起。
    全局_pm统计 的 挂起次数 为 全局_pm统计 的 挂起次数 加 1。
    做 内核信息 用 「PM: 系统已挂起」。
    返还 成功。
完。

pm_系统恢复 接受 零参：
    做 pm统计_初始化。
    做 内核信息 用 「PM: 开始系统恢复流程」。
    pm_当前状态 为 PM_状态_恢复中。
    做 pm_恢复CPU状态。
    做 pm_启用非启动CPU。
    做 pm_恢复设备。
    做 pm_解冻内核线程。
    做 pm_解冻进程。
    pm_当前状态 为 PM_状态_运行。
    全局_pm统计 的 恢复次数 为 全局_pm统计 的 恢复次数 加 1。
    做 内核信息 用 「PM: 系统已恢复」。
    返还 成功。
完。

PM_热_警告温度 为 85。

PM_热_临界温度 为 100。

PM_热_目标温度 为 75。

可变 pm_当前温度 为 0。

可变 pm_热限流级别 为 0。

pm_热检查 接受 零参：
    做 pm统计_初始化。
    pm_当前温度 为 做 读取CPU温度。
    若 pm_当前温度 大于或等于 PM_热_临界温度：
        pm_热限流级别 为 3。
        做 内核警告 用 「PM: 临界温度! 」 加 做 整数转字符串 用 pm_当前温度 加 「°C - 紧急降频」。
        做 pm_设置频率_百分比 用 25。
        全局_pm统计 的 热限流次数 为 全局_pm统计 的 热限流次数 加 1。
    否则若 pm_当前温度 大于或等于 PM_热_警告温度：
        pm_热限流级别 为 2。
        做 内核信息 用 「PM: 高温警告 」 加 做 整数转字符串 用 pm_当前温度 加 「°C - 降频」。
        做 pm_设置频率_百分比 用 50。
        全局_pm统计 的 热限流次数 为 全局_pm统计 的 热限流次数 加 1。
    否则若 pm_当前温度 大于或等于 PM_热_目标温度：
        pm_热限流级别 为 1。
        做 pm_设置频率_百分比 用 75。
    否则：
        若 pm_热限流级别 大于 0：
            pm_热限流级别 为 0。
            做 pm_设置频率_百分比 用 100。
        完。
    完。
完。

pm_设置频率_百分比 接受 百分比：
    若 百分比 小于 10：
        百分比 为 10。
    完。
    若 百分比 大于 100：
        百分比 为 100。
    完。
    目标频率 为 CPU最大频率_MHz 乘 百分比 除以 100。
    CPU当前频率_MHz 为 目标频率。
    做 写MSR 用 MSR_PERF_CTL、目标频率。
完。

电源管理_统计 接受 零参：
    做 pm统计_初始化。
    返还 一组：pm_当前状态、pm_当前温度、CPU当前频率_MHz、全局_pm统计 的 挂起次数、全局_pm统计 的 恢复次数、做 计数 用 pm_设备列表。
完。

电源管理_打印统计 接受 零参：
    做 pm统计_初始化。
    做 打印行 用 「=== 电源管理统计 ===」。
    做 打印行 用 「当前状态: 」 加 做 整数转字符串 用 pm_当前状态。
    做 打印行 用 「挂起次数: 」 加 做 整数转字符串 用 全局_pm统计 的 挂起次数。
    做 打印行 用 「恢复次数: 」 加 做 整数转字符串 用 全局_pm统计 的 恢复次数。
    做 打印行 用 「冻结次数: 」 加 做 整数转字符串 用 全局_pm统计 的 冻结次数。
    做 打印行 用 「解冻次数: 」 加 做 整数转字符串 用 全局_pm统计 的 解冻次数。
    做 打印行 用 「冻结失败: 」 加 做 整数转字符串 用 全局_pm统计 的 冻结失败次数。
    做 打印行 用 「设备挂起失败: 」 加 做 整数转字符串 用 全局_pm统计 的 设备挂起失败次数。
    做 打印行 用 「唤醒锁获取: 」 加 做 整数转字符串 用 全局_pm统计 的 唤醒锁获取数。
    做 打印行 用 「唤醒锁释放: 」 加 做 整数转字符串 用 全局_pm统计 的 唤醒锁释放数。
    做 打印行 用 「唤醒锁阻止: 」 加 做 整数转字符串 用 全局_pm统计 的 唤醒锁阻止挂起数。
    做 打印行 用 「热限流次数: 」 加 做 整数转字符串 用 全局_pm统计 的 热限流次数。
    做 打印行 用 「当前温度: 」 加 做 整数转字符串 用 pm_当前温度 加 「°C」。
    做 打印行 用 「热限流级别: 」 加 做 整数转字符串 用 pm_热限流级别。
    做 打印行 用 「注册设备数: 」 加 做 整数转字符串 用 做 计数 用 pm_设备列表。
    做 打印行 用 「活跃唤醒锁: 」 加 做 整数转字符串 用 做 计数 用 唤醒锁列表。
完。

电源_重置统计 接受 零参：
    做 pm统计_初始化。
    全局_pm统计 的 挂起次数 为 0。
    全局_pm统计 的 恢复次数 为 0。
    全局_pm统计 的 冻结次数 为 0。
    全局_pm统计 的 解冻次数 为 0。
    全局_pm统计 的 冻结失败次数 为 0。
    全局_pm统计 的 设备挂起失败次数 为 0。
    全局_pm统计 的 唤醒锁获取数 为 0。
    全局_pm统计 的 唤醒锁释放数 为 0。
    全局_pm统计 的 唤醒锁阻止挂起数 为 0。
    全局_pm统计 的 热限流次数 为 0。
完。

可变 AI_电源冻结进_负载窗口 为 一组：（空）。

AI_电源冻结进_负载预测 接受 当前负载：
    做 追加 用 AI_电源冻结进_负载窗口、当前负载。
    若 做 计数 用 AI_电源冻结进_负载窗口 大于 20：
        做 列表_移除 用 AI_电源冻结进_负载窗口、0。
    完。
    若 做 计数 用 AI_电源冻结进_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_电源冻结进_负载窗口。
    趋势 为 AI_电源冻结进_负载窗口 的 第 n 减 1 项 减 AI_电源冻结进_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_电源冻结进_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
