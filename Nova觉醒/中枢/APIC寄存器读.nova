《APIC寄存器读》

引用《..·执行·端口》。

引用《.·中断管理系统》。

引用《.·自动扩容》。

引用《.·APIC任务优先级》。

引用《.·字符串替换》。

引用《..·引导·寄存器定义》。

接口C_ID 为 0x20。

接口C_版本 为 0x30。

接口C_TPR 为 0x80。

接口C_APR 为 0x90。

接口C_PPR 为 0xA0。

接口C_EOI 为 0xB0。

接口C_RRD 为 0xC0。

接口C_LDR 为 0xD0。

接口C_DFR 为 0xE0。

接口C_SVR 为 0xF0。

接口C_ISR_BASE 为 0x100。

接口C_TMR_BASE 为 0x180。

接口C_IRR_BASE 为 0x200。

接口C_ESR 为 0x280。

接口C_ICR低 为 0x300。

接口C_ICR高 为 0x310。

接口C_LVT_定时器 为 0x320。

接口C_LVT_温度 为 0x330。

接口C_LVT_性能计数 为 0x340。

接口C_LVT_LINT0 为 0x350。

接口C_LVT_LINT1 为 0x360。

接口C_LVT_错误 为 0x370。

接口C_定时器_初始计数 为 0x380。

接口C_定时器_当前计数 为 0x390。

接口C_定时器_分频 为 0x3E0。

接口C_SVR_使能 为 0x100。

接口C_SVR_焦点检查禁用 为 0x200。

接口C_SVR_EOI广播抑制 为 0x1000。

接口C_伪向量 为 0xFF。

接口C_LVT_屏蔽 为 0x10000。

接口C_LVT_固定 为 0x000。

接口C_LVT_SMI 为 0x200。

接口C_LVT_NMI 为 0x400。

接口C_LVT_INIT 为 0x500。

接口C_LVT_ExtINT 为 0x700。

接口C_定时器_单次 为 0x00000。

接口C_定时器_周期 为 0x20000。

接口C_定时器_TSC截止 为 0x40000。

接口C_ICR_固定 为 0x00000。

接口C_ICR_最低优先级 为 0x00100。

接口C_ICR_SMI 为 0x00200。

接口C_ICR_NMI 为 0x00400。

接口C_ICR_INIT 为 0x00500。

接口C_ICR_启动 为 0x00600。

接口C_ICR_物理目标 为 0x00000。

接口C_ICR_逻辑目标 为 0x00800。

接口C_ICR_空闲 为 0x00000。

接口C_ICR_发送中 为 0x01000。

接口C_ICR_无简写 为 0x00000。

接口C_ICR_自身 为 0x40000。

接口C_ICR_全部含自身 为 0x80000。

接口C_ICR_全部除自身 为 0xC0000。

接口C_ICR_去断言 为 0x00000。

接口C_ICR_断言 为 0x04000。

接口C_ICR_边沿触发 为 0x00000。

接口C_ICR_电平触发 为 0x08000。

接口C_分频_1 为 0x0B。

接口C_分频_2 为 0x00。

接口C_分频_4 为 0x01。

接口C_分频_8 为 0x02。

接口C_分频_16 为 0x03。

接口C_分频_32 为 0x08。

接口C_分频_64 为 0x09。

接口C_分频_128 为 0x0A。

MSR_接口C_BASE 为 0x1B。

MSR_接口C_BASE_使能 为 0x800。

MSR_接口C_BASE_BSP 为 0x100。

MSR_接口C_BASE_x2APIC 为 0x400。

ESR_发送校验和错误 为 0x01。

ESR_接收校验和错误 为 0x02。

ESR_发送接受错误 为 0x04。

ESR_接收接受错误 为 0x08。

ESR_可重定向IPI 为 0x10。

ESR_发送非法向量 为 0x20。

ESR_接收非法向量 为 0x40。

ESR_非法寄存器地址 为 0x80。

可变 接口C基址 为 0xFEE00000。

可变 接口C_已初始化_读 为 假。

可变 接口C_是x2APIC模式 为 假。

可变 读_读取次数 为 0。

可变 读_写入次数 为 0。

可变 读_EOI次数 为 0。

可变 读_ESR读取次数 为 0。

可变 读_ISR读取次数 为 0。

可变 读_IRR读取次数 为 0。

可变 读_ICR等待次数 为 0。

可变 读_ICR超时次数 为 0。

可变 读_错误数 为 0。

接口C_读 接受 偏移：
    读_读取次数 为 读_读取次数 加 1。
    返还 做 MMIO读32 用 接口C基址 加 偏移。
完。

接口C_写 接受 偏移、值：
    读_写入次数 为 读_写入次数 加 1。
    做 MMIO写32 用 接口C基址 加 偏移、值。
完。

接口C_获取ID 接受 零参：
    返还 做 接口C_读 用 接口C_ID 右移 24 位与 0xFF。
完。

接口C_获取版本 接受 零参：
    返还 做 接口C_读 用 接口C_版本 位与 0xFF。
完。

接口C_获取最大LVT 接受 零参：
    返还 (做 接口C_读 用 接口C_版本 右移 16 位与 0xFF) 加 1。
完。

接口C_获取逻辑ID 接受 零参：
    返还 做 接口C_读 用 接口C_LDR 右移 24 位与 0xFF。
完。

接口C_获取目标格式 接受 零参：
    返还 做 接口C_读 用 接口C_DFR。
完。

接口C_获取处理器优先级 接受 零参：
    返还 做 接口C_读 用 接口C_PPR 位与 0xFF。
完。

接口C_获取仲裁优先级 接受 零参：
    返还 做 接口C_读 用 接口C_APR 位与 0xFF。
完。

接口C_获取基址 接受 零参：
    返还 接口C基址。
完。

接口C_SVR已启用 接受 零参：
    返还 做 接口C_读 用 接口C_SVR 位与 接口C_SVR_使能 不等于 0。
完。

接口C_获取伪向量 接受 零参：
    返还 做 接口C_读 用 接口C_SVR 位与 0xFF。
完。

接口C_读ISR 接受 索引：
    若 索引 大于 7：
        返还 0。
    完。
    读_ISR读取次数 为 读_ISR读取次数 加 1。
    返还 做 接口C_读 用 接口C_ISR_BASE 加 索引 乘 0x10。
完。

接口C_读IRR 接受 索引：
    若 索引 大于 7：
        返还 0。
    完。
    读_IRR读取次数 为 读_IRR读取次数 加 1。
    返还 做 接口C_读 用 接口C_IRR_BASE 加 索引 乘 0x10。
完。

接口C_读TMR 接受 索引：
    若 索引 大于 7：
        返还 0。
    完。
    返还 做 接口C_读 用 接口C_TMR_BASE 加 索引 乘 0x10。
完。

接口C_向量在ISR中 接受 向量号：
    若 向量号 大于 255：
        返还 假。
    完。
    寄存器索引 为 向量号 除以 32。
    位偏移 为 向量号 取余于 32。
    返还 做 接口C_读ISR 用 寄存器索引 位与 1 左移 位偏移 不等于 0。
完。

接口C_向量在IRR中 接受 向量号：
    若 向量号 大于 255：
        返还 假。
    完。
    寄存器索引 为 向量号 除以 32。
    位偏移 为 向量号 取余于 32。
    返还 做 接口C_读IRR 用 寄存器索引 位与 1 左移 位偏移 不等于 0。
完。

接口C_向量是电平触发 接受 向量号：
    若 向量号 大于 255：
        返还 假。
    完。
    寄存器索引 为 向量号 除以 32。
    位偏移 为 向量号 取余于 32。
    返还 做 接口C_读TMR 用 寄存器索引 位与 1 左移 位偏移 不等于 0。
完。

接口C_获取最高ISR向量 接受 零参：
    可变 i 为 7。
    只要 i 大于或等于 0：
        值 为 做 接口C_读ISR 用 i。
        若 值 不等于 0：
            可变 位 为 31。
            只要 位 大于或等于 0：
                若 值 位与 1 左移 位 不等于 0：
                    返还 i 乘 32 加 位。
                完。
                位 为 位 减 1。
            完。
        完。
        i 为 i 减 1。
    完。
    返还 0 减 1。
完。

接口C_获取最高IRR向量 接受 零参：
    可变 i 为 7。
    只要 i 大于或等于 0：
        值 为 做 接口C_读IRR 用 i。
        若 值 不等于 0：
            可变 位 为 31。
            只要 位 大于或等于 0：
                若 值 位与 1 左移 位 不等于 0：
                    返还 i 乘 32 加 位。
                完。
                位 为 位 减 1。
            完。
        完。
        i 为 i 减 1。
    完。
    返还 0 减 1。
完。

接口C_统计ISR置位数 接受 零参：
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 8：
        值 为 做 接口C_读ISR 用 i。
        只要 值 不等于 0：
            计数 为 计数 加 (值 位与 1)。
            值 为 值 右移 1。
        完。
        i 为 i 加 1。
    完。
    返还 计数。
完。

接口C_统计IRR置位数 接受 零参：
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 8：
        值 为 做 接口C_读IRR 用 i。
        只要 值 不等于 0：
            计数 为 计数 加 (值 位与 1)。
            值 为 值 右移 1。
        完。
        i 为 i 加 1。
    完。
    返还 计数。
完。

接口C_读取错误 接受 零参：
    做 接口C_写 用 接口C_ESR、0。
    读_ESR读取次数 为 读_ESR读取次数 加 1。
    返还 做 接口C_读 用 接口C_ESR。
完。

接口C_解析错误 接受 错误码：
    可变 描述 为 「」。
    若 错误码 位与 ESR_发送校验和错误 不等于 0：
        描述 为 描述 加 「发送校验和错误 」。
    完。
    若 错误码 位与 ESR_接收校验和错误 不等于 0：
        描述 为 描述 加 「接收校验和错误 」。
    完。
    若 错误码 位与 ESR_发送接受错误 不等于 0：
        描述 为 描述 加 「发送接受错误 」。
    完。
    若 错误码 位与 ESR_接收接受错误 不等于 0：
        描述 为 描述 加 「接收接受错误 」。
    完。
    若 错误码 位与 ESR_发送非法向量 不等于 0：
        描述 为 描述 加 「发送非法向量 」。
    完。
    若 错误码 位与 ESR_接收非法向量 不等于 0：
        描述 为 描述 加 「接收非法向量 」。
    完。
    若 错误码 位与 ESR_非法寄存器地址 不等于 0：
        描述 为 描述 加 「非法寄存器地址 」。
    完。
    若 描述 等于 「」：
        描述 为 「无错误」。
    完。
    返还 描述。
完。

接口C_读LVT_定时器 接受 零参：
    返还 做 接口C_读 用 接口C_LVT_定时器。
完。

接口C_读LVT_温度 接受 零参：
    返还 做 接口C_读 用 接口C_LVT_温度。
完。

接口C_读LVT_性能 接受 零参：
    返还 做 接口C_读 用 接口C_LVT_性能计数。
完。

接口C_读LVT_LINT0 接受 零参：
    返还 做 接口C_读 用 接口C_LVT_LINT0。
完。

接口C_读LVT_LINT1 接受 零参：
    返还 做 接口C_读 用 接口C_LVT_LINT1。
完。

接口C_读LVT_错误 接受 零参：
    返还 做 接口C_读 用 接口C_LVT_错误。
完。

接口C_LVT已屏蔽 接受 寄存器偏移：
    返还 做 接口C_读 用 寄存器偏移 位与 接口C_LVT_屏蔽 不等于 0。
完。

接口C_LVT获取向量 接受 寄存器偏移：
    返还 做 接口C_读 用 寄存器偏移 位与 0xFF。
完。

接口C_LVT获取投递模式 接受 寄存器偏移：
    返还 做 接口C_读 用 寄存器偏移 右移 8 位与 0x07。
完。

接口C_LVT投递模式名 接受 模式：
    若 模式 等于 0：
        返还 「Fixed」。
    完。
    若 模式 等于 2：
        返还 「SMI」。
    完。
    若 模式 等于 4：
        返还 「NMI」。
    完。
    若 模式 等于 5：
        返还 「INIT」。
    完。
    若 模式 等于 7：
        返还 「ExtINT」。
    完。
    返还 「未知」。
完。

接口C_ICR空闲 接受 零参：
    返还 做 接口C_读 用 接口C_ICR低 位与 接口C_ICR_发送中 等于 0。
完。

接口C_等待ICR空闲 接受 零参：
    可变 等待 为 0。
    只要 等待 小于 100000：
        若 做 接口C_ICR空闲 等于 真：
            返还 0。
        完。
        等待 为 等待 加 1。
    完。
    读_ICR超时次数 为 读_ICR超时次数 加 1。
    读_错误数 为 读_错误数 加 1。
    返还 错误_超时。
完。

接口C_读ICR 接受 零参：
    低 为 做 接口C_读 用 接口C_ICR低。
    高 为 做 接口C_读 用 接口C_ICR高。
    返还 一组：低、高。
完。

接口C_读定时器_初始计数 接受 零参：
    返还 做 接口C_读 用 接口C_定时器_初始计数。
完。

接口C_读定时器_当前计数 接受 零参：
    返还 做 接口C_读 用 接口C_定时器_当前计数。
完。

接口C_读定时器_分频 接受 零参：
    返还 做 接口C_读 用 接口C_定时器_分频。
完。

接口C_定时器模式名 接受 零参：
    模式 为 做 接口C_读LVT_定时器 位与 0x60000。
    若 模式 等于 接口C_定时器_单次：
        返还 「单次」。
    完。
    若 模式 等于 接口C_定时器_周期：
        返还 「周期」。
    完。
    若 模式 等于 接口C_定时器_TSC截止：
        返还 「TSC截止」。
    完。
    返还 「未知」。
完。

接口C_分频值名 接受 分频：
    若 分频 等于 接口C_分频_1：
        返还 「÷1」。
    完。
    若 分频 等于 接口C_分频_2：
        返还 「÷2」。
    完。
    若 分频 等于 接口C_分频_4：
        返还 「÷4」。
    完。
    若 分频 等于 接口C_分频_8：
        返还 「÷8」。
    完。
    若 分频 等于 接口C_分频_16：
        返还 「÷16」。
    完。
    若 分频 等于 接口C_分频_32：
        返还 「÷32」。
    完。
    若 分频 等于 接口C_分频_64：
        返还 「÷64」。
    完。
    若 分频 等于 接口C_分频_128：
        返还 「÷128」。
    完。
    返还 「未知」。
完。

接口C_检测x2APIC 接受 零参：
    msr 为 做 读MSR 用 MSR_接口C_BASE。
    若 msr 位与 MSR_接口C_BASE_x2APIC 不等于 0：
        接口C_是x2APIC模式 为 真。
        返还 真。
    完。
    接口C_是x2APIC模式 为 假。
    返还 假。
完。

接口C_dump寄存器 接受 零参：
    做 打印 用 「[APIC dump] ═══════════════════════」。
    做 打印 用 「  基址:         0x」 加 做 转字符串 用 接口C基址。
    做 打印 用 「  ID:           」 加 做 转字符串 用 做 接口C_获取ID。
    做 打印 用 「  版本:         」 加 做 转字符串 用 做 接口C_获取版本。
    做 打印 用 「  最大LVT:      」 加 做 转字符串 用 做 接口C_获取最大LVT。
    做 打印 用 「  逻辑ID:       」 加 做 转字符串 用 做 接口C_获取逻辑ID。
    做 打印 用 「  SVR已启用:    」 加 做 转字符串 用 做 接口C_SVR已启用。
    做 打印 用 「  伪向量:       」 加 做 转字符串 用 做 接口C_获取伪向量。
    做 打印 用 「  TPR:          」 加 做 转字符串 用 做 接口C_读 用 接口C_TPR 位与 0xFF。
    做 打印 用 「  PPR:          」 加 做 转字符串 用 做 接口C_获取处理器优先级。
    做 打印 用 「  x2APIC:       」 加 做 转字符串 用 接口C_是x2APIC模式。
    做 打印 用 「  ── LVT ──」。
    做 打印 用 「  定时器:       0x」 加 做 转字符串 用 做 接口C_读LVT_定时器 加 「 模式=」 加 做 接口C_定时器模式名。
    做 打印 用 「  LINT0:        0x」 加 做 转字符串 用 做 接口C_读LVT_LINT0。
    做 打印 用 「  LINT1:        0x」 加 做 转字符串 用 做 接口C_读LVT_LINT1。
    做 打印 用 「  错误:         0x」 加 做 转字符串 用 做 接口C_读LVT_错误。
    做 打印 用 「  温度:         0x」 加 做 转字符串 用 做 接口C_读LVT_温度。
    做 打印 用 「  性能计数:     0x」 加 做 转字符串 用 做 接口C_读LVT_性能。
    做 打印 用 「  ── 定时器 ──」。
    做 打印 用 「  初始计数:     」 加 做 转字符串 用 做 接口C_读定时器_初始计数。
    做 打印 用 「  当前计数:     」 加 做 转字符串 用 做 接口C_读定时器_当前计数。
    做 打印 用 「  分频:         」 加 做 接口C_分频值名 用 做 接口C_读定时器_分频。
    错误 为 做 接口C_读取错误。
    做 打印 用 「  ESR:          0x」 加 做 转字符串 用 错误 加 「 」 加 做 接口C_解析错误 用 错误。
    做 打印 用 「  ISR置位数:    」 加 做 转字符串 用 做 接口C_统计ISR置位数。
    做 打印 用 「  IRR置位数:    」 加 做 转字符串 用 做 接口C_统计IRR置位数。
    做 打印 用 「  最高ISR向量:  」 加 做 转字符串 用 做 接口C_获取最高ISR向量。
    做 打印 用 「  最高IRR向量:  」 加 做 转字符串 用 做 接口C_获取最高IRR向量。
    做 打印 用 「[APIC dump] ═══════════════════════」。
完。

接口C_dump_ISR 接受 零参：
    做 打印 用 「[APIC ISR] ──────────────────」。
    可变 i 为 0。
    只要 i 小于 8：
        值 为 做 接口C_读ISR 用 i。
        若 值 不等于 0：
            做 打印 用 「  ISR[」 加 做 转字符串 用 i 加 「]: 0x」 加 做 转字符串 用 值 加 「 (向量」 加 做 转字符串 用 i 乘 32 加 「-」 加 做 转字符串 用 i 乘 32 加 31 加 「)」。
        完。
        i 为 i 加 1。
    完。
完。

接口C_dump_IRR 接受 零参：
    做 打印 用 「[APIC IRR] ──────────────────」。
    可变 i 为 0。
    只要 i 小于 8：
        值 为 做 接口C_读IRR 用 i。
        若 值 不等于 0：
            做 打印 用 「  IRR[」 加 做 转字符串 用 i 加 「]: 0x」 加 做 转字符串 用 值 加 「 (向量」 加 做 转字符串 用 i 乘 32 加 「-」 加 做 转字符串 用 i 乘 32 加 31 加 「)」。
        完。
        i 为 i 加 1。
    完。
完。

接口C_验证状态 接受 零参：
    可变 错误数 为 0。
    若 做 接口C_SVR已启用 等于 假：
        做 打印 用 「[APIC验证] 错误: SVR未启用」。
        错误数 为 错误数 加 1。
    完。
    错误码 为 做 接口C_读取错误。
    若 错误码 不等于 0：
        做 打印 用 「[APIC验证] 警告: ESR非零: 」 加 做 接口C_解析错误 用 错误码。
        错误数 为 错误数 加 1。
    完。
    若 做 接口C_ICR空闲 等于 假：
        做 打印 用 「[APIC验证] 警告: ICR非空闲(有未完成IPI)」。
        错误数 为 错误数 加 1。
    完。
    id 为 做 接口C_获取ID。
    若 id 大于 255：
        做 打印 用 「[APIC验证] 错误: ID超出范围: 」 加 做 转字符串 用 id。
        错误数 为 错误数 加 1。
    完。
    若 错误数 等于 0：
        做 打印 用 「[APIC验证] 全部通过」。
    完。
    返还 错误数。
完。

接口C_读_完整统计 接受 零参：
    统计 为 做 字典。
    做 字典设 用 统计、「基址」、接口C基址。
    做 字典设 用 统计、「ID」、做 接口C_获取ID。
    做 字典设 用 统计、「版本」、做 接口C_获取版本。
    做 字典设 用 统计、「最大LVT」、做 接口C_获取最大LVT。
    做 字典设 用 统计、「SVR已启用」、做 接口C_SVR已启用。
    做 字典设 用 统计、「x2APIC模式」、接口C_是x2APIC模式。
    做 字典设 用 统计、「TPR」、做 接口C_读 用 接口C_TPR 位与 0xFF。
    做 字典设 用 统计、「PPR」、做 接口C_获取处理器优先级。
    做 字典设 用 统计、「ISR置位数」、做 接口C_统计ISR置位数。
    做 字典设 用 统计、「IRR置位数」、做 接口C_统计IRR置位数。
    做 字典设 用 统计、「读取次数」、读_读取次数。
    做 字典设 用 统计、「写入次数」、读_写入次数。
    做 字典设 用 统计、「EOI次数」、读_EOI次数。
    做 字典设 用 统计、「ESR读取次数」、读_ESR读取次数。
    做 字典设 用 统计、「ISR读取次数」、读_ISR读取次数。
    做 字典设 用 统计、「IRR读取次数」、读_IRR读取次数。
    做 字典设 用 统计、「ICR等待次数」、读_ICR等待次数。
    做 字典设 用 统计、「ICR超时次数」、读_ICR超时次数。
    做 字典设 用 统计、「错误数」、读_错误数。
    做 字典设 用 统计、「定时器模式」、做 接口C_定时器模式名。
    做 字典设 用 统计、「定时器初始计数」、做 接口C_读定时器_初始计数。
    返还 统计。
完。

接口C_读_打印完整统计 接受 零参：
    统计 为 做 接口C_读_完整统计。
    做 打印 用 「[APIC读] ═══════ 完整统计 ═══════」。
    做 打印 用 「  ID:           」 加 做 转字符串 用 做 字典取 用 统计、「ID」。
    做 打印 用 「  版本:         」 加 做 转字符串 用 做 字典取 用 统计、「版本」。
    做 打印 用 「  最大LVT:      」 加 做 转字符串 用 做 字典取 用 统计、「最大LVT」。
    做 打印 用 「  SVR已启用:    」 加 做 转字符串 用 做 字典取 用 统计、「SVR已启用」。
    做 打印 用 「  TPR:          」 加 做 转字符串 用 做 字典取 用 统计、「TPR」。
    做 打印 用 「  ISR置位数:    」 加 做 转字符串 用 做 字典取 用 统计、「ISR置位数」。
    做 打印 用 「  IRR置位数:    」 加 做 转字符串 用 做 字典取 用 统计、「IRR置位数」。
    做 打印 用 「  总读取:       」 加 做 转字符串 用 做 字典取 用 统计、「读取次数」。
    做 打印 用 「  总写入:       」 加 做 转字符串 用 做 字典取 用 统计、「写入次数」。
    做 打印 用 「  ESR读取:      」 加 做 转字符串 用 做 字典取 用 统计、「ESR读取次数」。
    做 打印 用 「  ICR超时:      」 加 做 转字符串 用 做 字典取 用 统计、「ICR超时次数」。
    做 打印 用 「  定时器模式:   」 加 做 转字符串 用 做 字典取 用 统计、「定时器模式」。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[APIC读] ═════════════════════════」。
    返还 0。
完。

接口C_读_销毁 接受 零参：
    接口C_已初始化_读 为 假。
    读_读取次数 为 0。
    读_写入次数 为 0。
    读_EOI次数 为 0。
    读_ESR读取次数 为 0。
    读_ISR读取次数 为 0。
    读_IRR读取次数 为 0。
    读_ICR等待次数 为 0。
    读_ICR超时次数 为 0。
    读_错误数 为 0。
    做 打印 用 「[APIC读] 已销毁」。
    返还 0。
完。

接口C_为调度器提供当前CPU 接受 零参：
    返还 做 接口C_获取ID。
完。

接口C_为IRQ子系统提供状态 接受 零参：
    返还 一组：做 接口C_统计ISR置位数、做 接口C_统计IRR置位数、做 接口C_获取最高ISR向量、做 接口C_获取最高IRR向量。
完。

接口C_为电源管理提供定时器状态 接受 零参：
    返还 一组：做 接口C_读定时器_初始计数、做 接口C_读定时器_当前计数、做 接口C_读定时器_分频、做 接口C_定时器模式名。
完。

本章 收束。
