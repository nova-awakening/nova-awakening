《TNF推理引擎》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·感知·内核直连层》。

引用《.·内核打印》。

引用《..·推理·定点Q算术》。

引用《.·内存初始化》。

阴 为 0 减 1。

平 为 0。

阳 为 1。

TNF_维度 为 256。

TNF_量化级 为 7。

可变 TNF_阈值 为 300。

TNF_追加 接受 lst、val：
    做 插入 用 lst、做 计数 用 lst、val。
完。

可变 TNF_随机态 为 12345。

可变 TNF_权重矩阵 为 一组：（空）。

可变 TNF_推理次数 为 0。

可变 TNF_错误次数 为 0。

TNF_随机 接受 零参：
    TNF_随机态 为 TNF_随机态 位异或 TNF_随机态 左移 13。
    TNF_随机态 为 TNF_随机态 位异或 TNF_随机态 右移 7。
    TNF_随机态 为 TNF_随机态 位异或 TNF_随机态 左移 17。
    若 TNF_随机态 小于 0：
        TNF_随机态 为 0 减 TNF_随机态。
    完。
    返还 TNF_随机态。
完。

TNF_绝对值 接受 x：
    若 x 小于 0：
        返还 0 减 x。
    完。
    返还 x。
完。

TNF_整数sqrt 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 r 为 1。
    只要 r 乘 r 小于 x：
        r 为 r 加 1。
    完。
    返还 r。
完。

TNF_量化 接受 值：
    若 值 大于 TNF_阈值：
        返还 阳。
    完。
    若 值 小于 0 减 TNF_阈值：
        返还 阴。
    完。
    返还 平。
完。

TNF_反量化 接受 三态：
    若 三态 等于 阴：
        返还 0 减 1000。
    完。
    若 三态 等于 阳：
        返还 1000。
    完。
    返还 0。
完。

TNF_软量化 接受 值、温度：
    若 温度 等于 0：
        温度 为 1。
    完。
    若 值 大于 TNF_阈值：
        diff 为 (值 减 TNF_阈值) 乘 1000 除以 温度。
        若 diff 大于 1000：
            返还 1000。
        完。
        返还 diff。
    完。
    若 值 小于 0 减 TNF_阈值：
        diff 为 (做 TNF_绝对值 用 值 减 TNF_阈值) 乘 1000 除以 温度。
        若 diff 大于 1000：
            返还 0 减 1000。
        完。
        返还 0 减 diff。
    完。
    返还 0。
完。

TNF_乘 接受 a、b：
    r 为 a 乘 b。
    若 r 大于 0：
        返还 阳。
    完。
    若 r 小于 0：
        返还 阴。
    完。
    返还 平。
完。

TNF_加 接受 a、b：
    返还 a 加 b。
完。

TNF_取反 接受 a：
    返还 0 减 a。
完。

TNF_与 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

TNF_或 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

TNF_异或 接受 a、b：
    返还 做 TNF_或 用 做 TNF_与 用 a、做 TNF_取反 用 b、做 TNF_与 用 做 TNF_取反 用 a、b。
完。

TNF_多数表决 接受 a、b、c：
    和 为 a 加 b 加 c。
    若 和 大于 0：
        返还 阳。
    完。
    若 和 小于 0：
        返还 阴。
    完。
    返还 平。
完。

TNF_打包_创建 接受 维度：
    字节数 为 (维度 加 3) 除以 4。
    buf 为 做 堆_分配_数组 用 字节数。
    可变 i 为 0。
    只要 i 小于 字节数：
        做 内存_写8 用 buf 加 i、0。
        i 为 i 加 1。
    完。
    返还 buf。
完。

TNF_打包_写 接受 缓冲、索引、值：
    编码 为 0。
    若 值 大于 0：
        编码 为 1。
    否则若 值 小于 0：
        编码 为 2。
    完。
    字节位 为 索引 除以 4。
    位偏移 为 索引 取余于 4 乘 2。
    旧值 为 做 内存_读8 用 缓冲 加 字节位。
    掩码 为 0xFF 位异或 3 左移 位偏移。
    新值 为 旧值 位与 掩码 位或 编码 左移 位偏移。
    做 内存_写8 用 缓冲 加 字节位、新值。
完。

TNF_打包_读 接受 缓冲、索引：
    字节位 为 索引 除以 4。
    位偏移 为 索引 取余于 4 乘 2。
    值 为 做 内存_读8 用 缓冲 加 字节位 右移 位偏移 位与 3。
    若 值 等于 1：
        返还 阳。
    完。
    若 值 等于 2：
        返还 阴。
    完。
    返还 平。
完。

TNF向量 是 一种 事物，含 数据、维度、是打包。

TNF向量_创建 接受 维度：
    v 为 做 堆_分配 用 TNF向量。
    v 的 维度 为 维度。
    v 的 是打包 为 假。
    v 的 数据 为 做 堆_分配_数组 用 维度 乘 4。
    可变 i 为 0。
    只要 i 小于 维度：
        做 内存_写32 用 v 的 数据 加 i 乘 4、0。
        i 为 i 加 1。
    完。
    返还 v。
完。

TNF向量_创建_打包 接受 维度：
    v 为 做 堆_分配 用 TNF向量。
    v 的 维度 为 维度。
    v 的 是打包 为 真。
    v 的 数据 为 做 TNF_打包_创建 用 维度。
    返还 v。
完。

TNF向量_读 接受 v、i：
    若 v 等于 0 或者 i 大于或等于 v 的 维度：
        返还 0。
    完。
    若 v 的 是打包：
        返还 做 TNF_打包_读 用 v 的 数据、i。
    完。
    返还 做 内存_读32 用 v 的 数据 加 i 乘 4。
完。

TNF向量_写 接受 v、i、值：
    若 v 等于 0 或者 i 大于或等于 v 的 维度：
        返还。
    完。
    若 v 的 是打包：
        做 TNF_打包_写 用 v 的 数据、i、值。
    否则：
        做 内存_写32 用 v 的 数据 加 i 乘 4、值。
    完。
完。

TNF向量_释放 接受 v：
    若 v 等于 0：
        返还。
    完。
    若 v 的 数据 不等于 0：
        做 堆_释放 用 v 的 数据。
    完。
    做 堆_释放 用 v。
完。

TNF向量_点积 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 0。
    完。
    维 为 a 的 维度。
    若 b 的 维度 小于 维：
        维 为 b 的 维度。
    完。
    可变 累加 为 0。
    可变 i 为 0。
    只要 i 小于 维：
        累加 为 累加 加 做 TNF向量_读 用 a、i 乘 做 TNF向量_读 用 b、i。
        i 为 i 加 1。
    完。
    返还 累加。
完。

TNF向量_稀疏点积 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 0。
    完。
    维 为 a 的 维度。
    若 b 的 维度 小于 维：
        维 为 b 的 维度。
    完。
    可变 累加 为 0。
    可变 i 为 0。
    只要 i 小于 维：
        va 为 做 TNF向量_读 用 a、i。
        若 va 不等于 0：
            vb 为 做 TNF向量_读 用 b、i。
            若 vb 不等于 0：
                累加 为 累加 加 va 乘 vb。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 累加。
完。

TNF向量_汉明距离 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 0。
    完。
    维 为 a 的 维度。
    若 b 的 维度 小于 维：
        维 为 b 的 维度。
    完。
    可变 dist 为 0。
    可变 i 为 0。
    只要 i 小于 维：
        若 做 TNF向量_读 用 a、i 不等于 做 TNF向量_读 用 b、i：
            dist 为 dist 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 dist。
完。

TNF向量_量化 接受 源、目标：
    若 源 等于 0 或者 目标 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 源 的 维度 并且 i 小于 目标 的 维度：
        做 TNF向量_写 用 目标、i、做 TNF_量化 用 做 TNF向量_读 用 源、i。
        i 为 i 加 1。
    完。
完。

TNF层 是 一种 事物，含 输入维、输出维、权重、偏置。

TNF层_创建 接受 输入维、输出维：
    l 为 做 堆_分配 用 TNF层。
    l 的 输入维 为 输入维。
    l 的 输出维 为 输出维。
    l 的 权重 为 做 堆_分配_数组 用 输出维 乘 8。
    可变 i 为 0。
    只要 i 小于 输出维：
        w 为 做 TNF向量_创建 用 输入维。
        可变 j 为 0。
        只要 j 小于 输入维：
            r 为 做 TNF_随机 取余于 3 减 1。
            做 TNF向量_写 用 w、j、r。
            j 为 j 加 1。
        完。
        做 内存_写64 用 l 的 权重 加 i 乘 8、w。
        i 为 i 加 1。
    完。
    l 的 偏置 为 做 TNF向量_创建 用 输出维。
    返还 l。
完。

TNF层_前向 接受 层、输入：
    若 层 等于 0 或者 输入 等于 0：
        返还 0。
    完。
    TNF_推理次数 为 TNF_推理次数 加 1。
    输出 为 做 TNF向量_创建 用 层 的 输出维。
    可变 j 为 0。
    只要 j 小于 层 的 输出维：
        w 为 做 内存_读64 用 层 的 权重 加 j 乘 8。
        累加 为 做 TNF向量_稀疏点积 用 w、输入。
        偏 为 做 TNF向量_读 用 层 的 偏置、j。
        激活值 为 做 TNF_量化 用 累加 加 偏。
        做 TNF向量_写 用 输出、j、激活值。
        j 为 j 加 1。
    完。
    返还 输出。
完。

TNF_多层前向 接受 层数组、层数、输入：
    若 层数组 等于 0 或者 层数 等于 0 或者 输入 等于 0：
        返还 0。
    完。
    可变 当前 为 输入。
    可变 i 为 0。
    只要 i 小于 层数：
        层 为 做 内存_读64 用 层数组 加 i 乘 8。
        输出 为 做 TNF层_前向 用 层、当前。
        若 i 大于 0：
            做 TNF向量_释放 用 当前。
        完。
        当前 为 输出。
        i 为 i 加 1。
    完。
    返还 当前。
完。

TNF_Hopfield 是 一种 事物，含 维度、权重矩阵、模式数。

TNF_Hopfield_创建 接受 维度：
    h 为 做 堆_分配 用 TNF_Hopfield。
    h 的 维度 为 维度。
    h 的 模式数 为 0。
    h 的 权重矩阵 为 做 堆_分配_数组 用 维度 乘 维度 乘 4。
    可变 k 为 0。
    只要 k 小于 维度 乘 维度：
        做 内存_写32 用 h 的 权重矩阵 加 k 乘 4、0。
        k 为 k 加 1。
    完。
    返还 h。
完。

TNF_Hopfield_存储 接受 h、模式：
    若 h 等于 0 或者 模式 等于 0：
        返还。
    完。
    h 的 模式数 为 h 的 模式数 加 1。
    可变 i 为 0。
    只要 i 小于 h 的 维度：
        可变 j 为 0。
        只要 j 小于 h 的 维度：
            若 i 不等于 j：
                vi 为 做 TNF向量_读 用 模式、i。
                vj 为 做 TNF向量_读 用 模式、j。
                偏移 为 (i 乘 h 的 维度 加 j) 乘 4。
                旧 为 做 内存_读32 用 h 的 权重矩阵 加 偏移。
                做 内存_写32 用 h 的 权重矩阵 加 偏移、旧 加 vi 乘 vj。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

TNF_Hopfield_回忆 接受 h、状态、最大迭代：
    若 h 等于 0 或者 状态 等于 0：
        返还。
    完。
    可变 迭代 为 0。
    只要 迭代 小于 最大迭代：
        可变 变化 为 0。
        可变 i 为 0。
        只要 i 小于 h 的 维度：
            可变 累加 为 0。
            可变 j 为 0。
            只要 j 小于 h 的 维度：
                w 为 做 内存_读32 用 h 的 权重矩阵 加 (i 乘 h 的 维度 加 j) 乘 4。
                累加 为 累加 加 w 乘 做 TNF向量_读 用 状态、j。
                j 为 j 加 1。
            完。
            新值 为 做 TNF_量化 用 累加。
            若 新值 不等于 做 TNF向量_读 用 状态、i：
                变化 为 1。
            完。
            做 TNF向量_写 用 状态、i、新值。
            i 为 i 加 1。
        完。
        若 变化 等于 0：
            返还。
        完。
        迭代 为 迭代 加 1。
    完。
完。

TNF_Hopfield_能量 接受 h、状态：
    若 h 等于 0 或者 状态 等于 0：
        返还 0。
    完。
    可变 E 为 0。
    可变 i 为 0。
    只要 i 小于 h 的 维度：
        可变 j 为 i 加 1。
        只要 j 小于 h 的 维度：
            w 为 做 内存_读32 用 h 的 权重矩阵 加 (i 乘 h 的 维度 加 j) 乘 4。
            E 为 E 减 w 乘 做 TNF向量_读 用 状态、i 乘 做 TNF向量_读 用 状态、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 E。
完。

TNF_STDP更新 接受 权重、前时刻、后时刻、学习率：
    delta_t 为 后时刻 减 前时刻。
    可变 delta_w 为 0。
    若 delta_t 大于 0：
        delta_w 为 学习率 乘 1000 除以 (1000 加 delta_t)。
    否则若 delta_t 小于 0：
        delta_w 为 0 减 学习率 乘 1000 除以 (1000 减 delta_t)。
    完。
    返还 权重 加 delta_w。
完。

TNF_BCM更新 接受 权重、输入、输出、阈值、学习率：
    delta_w 为 学习率 乘 输出 乘 (输出 减 阈值) 乘 输入 除以 10000。
    返还 权重 加 delta_w。
完。

TNF_经验缓冲 是 一种 事物，含 数据、容量、当前数、写位置。

TNF_经验缓冲_创建 接受 容量：
    eb 为 做 堆_分配 用 TNF_经验缓冲。
    eb 的 容量 为 容量。
    eb 的 当前数 为 0。
    eb 的 写位置 为 0。
    eb 的 数据 为 做 堆_分配_数组 用 容量 乘 8。
    返还 eb。
完。

TNF_经验缓冲_存储 接受 eb、向量：
    若 eb 等于 0 或者 向量 等于 0：
        返还。
    完。
    做 内存_写64 用 eb 的 数据 加 eb 的 写位置 乘 8、向量。
    eb 的 写位置 为 (eb 的 写位置 加 1) 取余于 eb 的 容量。
    若 eb 的 当前数 小于 eb 的 容量：
        eb 的 当前数 为 eb 的 当前数 加 1。
    完。
完。

TNF_经验缓冲_随机取 接受 eb：
    若 eb 等于 0 或者 eb 的 当前数 等于 0：
        返还 0。
    完。
    idx 为 做 TNF_随机 取余于 eb 的 当前数。
    返还 做 内存_读64 用 eb 的 数据 加 idx 乘 8。
完。

TNF_统计_向量稀疏度 接受 v：
    若 v 等于 0 或者 v 的 维度 等于 0：
        返还 0。
    完。
    可变 零数 为 0。
    可变 i 为 0。
    只要 i 小于 v 的 维度：
        若 做 TNF向量_读 用 v、i 等于 0：
            零数 为 零数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 零数 乘 10000 除以 v 的 维度。
完。

TNF_统计_压缩比 接受 原始维度：
    原始字节 为 原始维度 乘 4。
    压缩字节 为 (原始维度 加 3) 除以 4。
    若 压缩字节 等于 0：
        返还 0。
    完。
    返还 原始字节 乘 100 除以 压缩字节。
完。

TNF_诊断打印 接受 零参：
    做 内核打印 用 「[TNF] 推理次数=」、TNF_推理次数、「 错误=」、TNF_错误次数。
    做 内核打印 用 「[TNF] 量化级=」、TNF_量化级、「 阈值=」、TNF_阈值。
    做 内核打印 用 「[TNF] 维度=」、TNF_维度。
完。

智能_TNF_AI初始化 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
    t 为 做 Tensor_创建 用 6。
    做 Tensor_写 用 t、0、TNF_维度。
    做 Tensor_写 用 t、1、TNF_量化级。
    做 Tensor_写 用 t、2、TNF_推理次数。
    做 Tensor_写 用 t、3、TNF_错误次数。
    做 Tensor_写 用 t、4、TNF_阈值。
    做 Tensor_写 用 t、5、TNF_随机态。
    做 AI总线_推理 用 bus、「NTNA_模块注册」、t。
    做 Tensor_释放 用 t。
完。

NTNA_AI自适应阈值 接受 推理输出、目标稀疏度：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 TNF_阈值。
    完。
    当前稀疏 为 做 TNF_统计_向量稀疏度 用 推理输出。
    t 为 做 Tensor_创建 用 3。
    做 Tensor_写 用 t、0、TNF_阈值。
    做 Tensor_写 用 t、1、当前稀疏。
    做 Tensor_写 用 t、2、目标稀疏度。
    新阈值 为 做 AI总线_推理 用 bus、「NTNA_阈值调整」、t。
    做 Tensor_释放 用 t。
    若 新阈值 大于 0：
        TNF_阈值 为 新阈值。
    完。
    返还 TNF_阈值。
完。

NTNA_AI权重剪枝 接受 层、剪枝比例：
    若 层 等于 0：
        返还 0。
    完。
    可变 剪枝数 为 0。
    可变 j 为 0。
    只要 j 小于 层 的 输出维：
        w 为 做 内存_读64 用 层 的 权重 加 j 乘 8。
        若 w 不等于 0：
            可变 i 为 0。
            只要 i 小于 层 的 输入维：
                值 为 做 TNF向量_读 用 w、i。
                若 值 等于 0：
                    剪枝数 为 剪枝数 加 1。
                完。
                i 为 i 加 1。
            完。
        完。
        j 为 j 加 1。
    完。
    返还 剪枝数。
完。

NTNA_AI异常检测 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 0。
    完。
    t 为 做 Tensor_创建 用 3。
    做 Tensor_写 用 t、0、TNF_推理次数。
    做 Tensor_写 用 t、1、TNF_错误次数。
    做 Tensor_写 用 t、2、TNF_随机态。
    风险 为 做 AI总线_风险评分 用 bus、「NTNA_异常」、t。
    做 Tensor_释放 用 t。
    返还 风险。
完。

可变 _谐振追加_历史 为 一组：（空）。

可变 _谐振追加_预测次数 为 0。

谐振追加_记录 接受 批量大小：
    做 追加 用 _谐振追加_历史、批量大小。
    若 做 计数 用 _谐振追加_历史 大于 32：
        可变 新历史 为 一组：（空）。
        可变 i 为 做 计数 用 _谐振追加_历史 减 32。
        只要 i 小于 做 计数 用 _谐振追加_历史：
            做 追加 用 新历史、_谐振追加_历史 的 第 i 项。
            i 为 i 加 1。
        完。
        _谐振追加_历史 为 新历史。
    完。
完。

谐振追加_预测容量 接受 零参：
    若 做 计数 用 _谐振追加_历史 等于 0：
        返还 16。
    完。
    可变 总和 为 0。
    可变 最大值 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _谐振追加_历史：
        总和 为 总和 加 _谐振追加_历史 的 第 i 项。
        若 _谐振追加_历史 的 第 i 项 大于 最大值：
            最大值 为 _谐振追加_历史 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    均值 为 总和 除以 做 计数 用 _谐振追加_历史。
    预测 为 均值 乘 3 除以 2。
    若 最大值 大于 预测：
        预测 为 最大值。
    完。
    _谐振追加_预测次数 为 _谐振追加_预测次数 加 1。
    返还 预测。
完。

神经追加_完整统计 接受 零参：
    返还 一组：「推理次数」、TNF_推理次数、「错误次数」、TNF_错误次数、「谐振预测次数」、_谐振追加_预测次数、「历史记录数」、做 计数 用 _谐振追加_历史。
完。

本章 收束。
