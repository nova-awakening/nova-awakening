《页表遍历》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·映射页》。

引用《..·引导·端口IO》。

引用《.·整数转字符串》。

回调_PML4 为 0。

回调_PDPT 为 1。

回调_PD 为 2。

回调_PT 为 3。

回调_大页 为 4。

遍历回调_创建 接受 零参：
    返还 一组：空、空、空、空、空。
完。

页表_查找PTE 接受 pml4_物理、虚拟地址：
    若 pml4_物理 等于 0：
        返还 0。
    完。
    pml4_虚拟 为 做 物理到虚拟 用 pml4_物理。
    pml4e 为 做 内存读64 用 pml4_虚拟 加 做 PML4索引 用 虚拟地址 乘 8。
    若 非 做 页表项_存在 用 pml4e：
        返还 0。
    完。
    pdpt_物理 为 做 页表项_地址 用 pml4e。
    pdpt_虚拟 为 做 物理到虚拟 用 pdpt_物理。
    pdpte 为 做 内存读64 用 pdpt_虚拟 加 做 PDPT索引 用 虚拟地址 乘 8。
    若 非 做 页表项_存在 用 pdpte：
        返还 0。
    完。
    若 做 页表项_是大页 用 pdpte：
        返还 pdpte。
    完。
    pd_物理 为 做 页表项_地址 用 pdpte。
    pd_虚拟 为 做 物理到虚拟 用 pd_物理。
    pde 为 做 内存读64 用 pd_虚拟 加 做 PD索引 用 虚拟地址 乘 8。
    若 非 做 页表项_存在 用 pde：
        返还 0。
    完。
    若 做 页表项_是大页 用 pde：
        返还 pde。
    完。
    pt_物理 为 做 页表项_地址 用 pde。
    pt_虚拟 为 做 物理到虚拟 用 pt_物理。
    pte 为 做 内存读64 用 pt_虚拟 加 做 PT索引 用 虚拟地址 乘 8。
    返还 pte。
完。

页表_查找PTE位置 接受 pml4_物理、虚拟地址：
    若 pml4_物理 等于 0：
        返还 0。
    完。
    pml4_虚拟 为 做 物理到虚拟 用 pml4_物理。
    pml4e 为 做 内存读64 用 pml4_虚拟 加 做 PML4索引 用 虚拟地址 乘 8。
    若 非 做 页表项_存在 用 pml4e：
        返还 0。
    完。
    pdpt_虚拟 为 做 物理到虚拟 用 做 页表项_地址 用 pml4e。
    pdpte 为 做 内存读64 用 pdpt_虚拟 加 做 PDPT索引 用 虚拟地址 乘 8。
    若 非 做 页表项_存在 用 pdpte 或者 做 页表项_是大页 用 pdpte：
        返还 0。
    完。
    pd_虚拟 为 做 物理到虚拟 用 做 页表项_地址 用 pdpte。
    pde 为 做 内存读64 用 pd_虚拟 加 做 PD索引 用 虚拟地址 乘 8。
    若 非 做 页表项_存在 用 pde 或者 做 页表项_是大页 用 pde：
        返还 0。
    完。
    pt_虚拟 为 做 物理到虚拟 用 做 页表项_地址 用 pde。
    返还 pt_虚拟 加 做 PT索引 用 虚拟地址 乘 8。
完。

页表_虚拟转物理 接受 pml4_物理、虚拟地址：
    pte 为 做 页表_查找PTE 用 pml4_物理、虚拟地址。
    若 非 做 页表项_存在 用 pte：
        返还 错误_未映射。
    完。
    返还 做 页表项_地址 用 pte 位或 虚拟地址 位与 0xFFF。
完。

页表_虚拟转物理_大页 接受 pml4_物理、虚拟地址：
    若 pml4_物理 等于 0：
        返还 错误_未映射。
    完。
    pml4_虚拟 为 做 物理到虚拟 用 pml4_物理。
    pml4e 为 做 内存读64 用 pml4_虚拟 加 做 PML4索引 用 虚拟地址 乘 8。
    若 非 做 页表项_存在 用 pml4e：
        返还 错误_未映射。
    完。
    pdpt_虚拟 为 做 物理到虚拟 用 做 页表项_地址 用 pml4e。
    pdpte 为 做 内存读64 用 pdpt_虚拟 加 做 PDPT索引 用 虚拟地址 乘 8。
    若 非 做 页表项_存在 用 pdpte：
        返还 错误_未映射。
    完。
    若 做 页表项_是大页 用 pdpte：
        返还 做 页表项_地址 用 pdpte 位或 虚拟地址 位与 0x3FFFFFFF。
    完。
    pd_虚拟 为 做 物理到虚拟 用 做 页表项_地址 用 pdpte。
    pde 为 做 内存读64 用 pd_虚拟 加 做 PD索引 用 虚拟地址 乘 8。
    若 非 做 页表项_存在 用 pde：
        返还 错误_未映射。
    完。
    若 做 页表项_是大页 用 pde：
        返还 做 页表项_地址 用 pde 位或 虚拟地址 位与 0x1FFFFF。
    完。
    pt_虚拟 为 做 物理到虚拟 用 做 页表项_地址 用 pde。
    pte 为 做 内存读64 用 pt_虚拟 加 做 PT索引 用 虚拟地址 乘 8。
    若 非 做 页表项_存在 用 pte：
        返还 错误_未映射。
    完。
    返还 做 页表项_地址 用 pte 位或 虚拟地址 位与 0xFFF。
完。

页表_遍历范围 接受 pml4_物理、起始、结束、回调、用户数据：
    若 pml4_物理 等于 0 或者 起始 大于或等于 结束：
        返还 0。
    完。
    pml4_虚拟 为 做 物理到虚拟 用 pml4_物理。
    可变 地址 为 起始 位与 按位取反 0xFFF。
    只要 地址 小于 结束：
        pml4e 为 做 内存读64 用 pml4_虚拟 加 做 PML4索引 用 地址 乘 8。
        若 非 做 页表项_存在 用 pml4e：
            地址 为 (地址 位或 0x7FFFFFFFFF) 加 1。
            继续。
        完。
        pdpt_虚拟 为 做 物理到虚拟 用 做 页表项_地址 用 pml4e。
        pdpte 为 做 内存读64 用 pdpt_虚拟 加 做 PDPT索引 用 地址 乘 8。
        若 非 做 页表项_存在 用 pdpte：
            地址 为 (地址 位或 0x3FFFFFFF) 加 1。
            继续。
        完。
        若 做 页表项_是大页 用 pdpte：
            结果 为 做 回调 用 地址、pdpte、用户数据。
            若 结果 不等于 0：
                返还 结果。
            完。
            地址 为 (地址 位或 0x3FFFFFFF) 加 1。
            继续。
        完。
        pd_虚拟 为 做 物理到虚拟 用 做 页表项_地址 用 pdpte。
        pde 为 做 内存读64 用 pd_虚拟 加 做 PD索引 用 地址 乘 8。
        若 非 做 页表项_存在 用 pde：
            地址 为 (地址 位或 0x1FFFFF) 加 1。
            继续。
        完。
        若 做 页表项_是大页 用 pde：
            结果 为 做 回调 用 地址、pde、用户数据。
            若 结果 不等于 0：
                返还 结果。
            完。
            地址 为 (地址 位或 0x1FFFFF) 加 1。
            继续。
        完。
        pt_虚拟 为 做 物理到虚拟 用 做 页表项_地址 用 pde。
        pte 为 做 内存读64 用 pt_虚拟 加 做 PT索引 用 地址 乘 8。
        若 做 页表项_存在 用 pte：
            结果 为 做 回调 用 地址、pte、用户数据。
            若 结果 不等于 0：
                返还 结果。
            完。
        完。
        地址 为 地址 加 页大小。
    完。
    返还 0。
完。

页表_统计用户空间 接受 pml4_物理：
    可变 页表页数 为 1。
    可变 映射页数 为 0。
    若 pml4_物理 等于 0：
        返还 一组：0、0。
    完。
    pml4_虚拟 为 做 物理到虚拟 用 pml4_物理。
    可变 pml4_i 为 0。
    只要 pml4_i 小于 256：
        pml4e 为 做 内存读64 用 pml4_虚拟 加 pml4_i 乘 8。
        若 做 页表项_存在 用 pml4e：
            页表页数 为 页表页数 加 1。
            pdpt_虚拟 为 做 物理到虚拟 用 做 页表项_地址 用 pml4e。
            可变 pdpt_i 为 0。
            只要 pdpt_i 小于 512：
                pdpte 为 做 内存读64 用 pdpt_虚拟 加 pdpt_i 乘 8。
                若 做 页表项_存在 用 pdpte：
                    若 做 页表项_是大页 用 pdpte：
                        映射页数 为 映射页数 加 (1 左移 18)。
                    否则：
                        页表页数 为 页表页数 加 1。
                        pd_虚拟 为 做 物理到虚拟 用 做 页表项_地址 用 pdpte。
                        可变 pd_i 为 0。
                        只要 pd_i 小于 512：
                            pde 为 做 内存读64 用 pd_虚拟 加 pd_i 乘 8。
                            若 做 页表项_存在 用 pde：
                                若 做 页表项_是大页 用 pde：
                                    映射页数 为 映射页数 加 512。
                                否则：
                                    页表页数 为 页表页数 加 1。
                                    pt_虚拟 为 做 物理到虚拟 用 做 页表项_地址 用 pde。
                                    可变 pt_i 为 0。
                                    只要 pt_i 小于 512：
                                        pte 为 做 内存读64 用 pt_虚拟 加 pt_i 乘 8。
                                        若 做 页表项_存在 用 pte：
                                            映射页数 为 映射页数 加 1。
                                        完。
                                        pt_i 为 pt_i 加 1。
                                    完。
                                完。
                            完。
                            pd_i 为 pd_i 加 1。
                        完。
                    完。
                完。
                pdpt_i 为 pdpt_i 加 1。
            完。
        完。
        pml4_i 为 pml4_i 加 1。
    完。
    返还 一组：页表页数、映射页数。
完。

页表_已映射 接受 pml4_物理、虚拟地址：
    pte 为 做 页表_查找PTE 用 pml4_物理、虚拟地址。
    返还 做 页表项_存在 用 pte。
完。

页表_可写 接受 pml4_物理、虚拟地址：
    pte 为 做 页表_查找PTE 用 pml4_物理、虚拟地址。
    若 非 做 页表项_存在 用 pte：
        返还 假。
    完。
    返还 做 页表项_可写 用 pte。
完。

页表_是用户页 接受 pml4_物理、虚拟地址：
    pte 为 做 页表_查找PTE 用 pml4_物理、虚拟地址。
    若 非 做 页表项_存在 用 pte：
        返还 假。
    完。
    返还 做 页表项_是用户 用 pte。
完。

页表_是COW页 接受 pml4_物理、虚拟地址：
    pte 为 做 页表_查找PTE 用 pml4_物理、虚拟地址。
    若 非 做 页表项_存在 用 pte：
        返还 假。
    完。
    返还 做 页表项_是COW 用 pte。
完。

页表遍历_完整统计 接受 零参：
    返还 一组：页表遍历_总次数。
完。

页表遍历_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 页表遍历_总次数 大于 10000000：
        做 追加 用 问题、「页表遍历次数过多，考虑使用大页」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「页表遍历健康」。
    完。
    返还 问题。
完。

页表遍历_打印条目 接受 虚拟地址、pml4、pdpt、pd、pt：
    可变 s 为 「VA=」 加 做 十六进制 用 虚拟地址。
    s 为 s 加 「 PML4[」 加 做 整数转字符串 用 pml4 加 「]」。
    s 为 s 加 「 PDPT[」 加 做 整数转字符串 用 pdpt 加 「]」。
    s 为 s 加 「 PD[」 加 做 整数转字符串 用 pd 加 「]」。
    s 为 s 加 「 PT[」 加 做 整数转字符串 用 pt 加 「]」。
    返还 s。
完。

可变 AI_页表遍历_运行指标 为 0。

可变 AI_页表遍历_异常指标 为 0。

AI_页表遍历_自诊断 接受 零参：
    AI_页表遍历_运行指标 为 0。
    返还 AI_页表遍历_运行指标。
完。

AI_页表遍历_优化建议 接受 零参：
    指标 为 做 AI_页表遍历_自诊断。
    返还 「页表遍历状态正常」。
完。

本章 收束。
