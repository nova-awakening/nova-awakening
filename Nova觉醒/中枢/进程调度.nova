《进程调度》

引用《..·标准·原生字符串》。

引用《.·调度带宽控制》。

引用《.·实时调度》。

引用《.·学习率预热》。

引用《.·公平调度时间片》。

引用《.·负载统计》。

引用《.·负载均衡》。

引用《.·调度时钟》。

引用《.·整数转字符串》。

引用《.·统一调度器核》。

引用《.·调度核心》。

引用《.·公平调度睡眠》。

引用《.·同步原语》。

调度模式_公平 为 「公平」。

调度模式_实时 为 「实时」。

调度模式_混合 为 「混合」。

调度档 是 一种 事物，含 名称、模式、核数、锁、当前任务、带宽组、滴答数、唤醒数、睡眠数、让出数、均衡数、实时数、公平数、采样数。

调度摘要 是 一种 事物，含 名称、模式、核数、当前任务、滴答数、唤醒数、睡眠数、让出数、均衡数、实时数、公平数、采样数、最闲核、最忙核、过载、健康。

可变 调度账本 为 一组：（空）。

可变 调度建数 为 0。

调度布尔串 接受 值：
    若 值：
        返还 「true」。
    完。
    返还 「false」。
完。

进程调度_模式名 接受 模式：
    若 模式 等于 调度模式_公平：
        返还 「公平」。
    完。
    若 模式 等于 调度模式_实时：
        返还 「实时」。
    完。
    若 模式 等于 调度模式_混合：
        返还 「混合」。
    完。
    返还 「未知」。
完。

进程调度_建档 接受 名称、模式、核数：
    做 调度_初始化。
    做 调度时钟_初始化。
    做 公平调度_初始化权重表。
    做 RT调度器_初始化。
    做 负载_初始化 用 核数。
    档 为 做 堆_分配 用 调度档。
    档 的 名称 为 名称。
    档 的 模式 为 模式。
    档 的 核数 为 核数。
    档 的 锁 为 做 同步原语_创建自旋 用 名称 加 「锁」。
    档 的 当前任务 为 0。
    档 的 带宽组 为 做 带宽组_不限制。
    档 的 滴答数 为 0。
    档 的 唤醒数 为 0。
    档 的 睡眠数 为 0。
    档 的 让出数 为 0。
    档 的 均衡数 为 0。
    档 的 实时数 为 0。
    档 的 公平数 为 0。
    档 的 采样数 为 0。
    返还 档。
完。

进程调度_创建 接受 名称、核数：
    档 为 做 进程调度_建档 用 名称、调度模式_混合、核数。
    做 追加 用 调度账本、档。
    调度建数 为 调度建数 加 1。
    返还 档。
完。

进程调度_创建公平 接受 名称、核数：
    档 为 做 进程调度_建档 用 名称、调度模式_公平、核数。
    做 追加 用 调度账本、档。
    调度建数 为 调度建数 加 1。
    返还 档。
完。

进程调度_创建实时 接受 名称、核数：
    档 为 做 进程调度_建档 用 名称、调度模式_实时、核数。
    做 追加 用 调度账本、档。
    调度建数 为 调度建数 加 1。
    返还 档。
完。

进程调度_名称 接受 档：
    返还 档 的 名称。
完。

进程调度_模式 接受 档：
    返还 档 的 模式。
完。

进程调度_核数 接受 档：
    返还 档 的 核数。
完。

进程调度_当前 接受 档：
    返还 档 的 当前任务。
完。

进程调度_设当前 接受 档、任务：
    档 的 当前任务 为 任务。
完。

进程调度_绑定带宽 接受 档、配额、周期：
    档 的 带宽组 为 做 带宽组_创建 用 配额、周期。
    返还 档 的 带宽组。
完。

进程调度_绑定带宽_含突发 接受 档、配额、周期、突发：
    档 的 带宽组 为 做 带宽组_创建_含突发 用 配额、周期、突发。
    返还 档 的 带宽组。
完。

进程调度_设模式 接受 档、模式：
    档 的 模式 为 模式。
完。

进程调度_初始化锁 接受 档：
    档 的 锁 为 做 同步原语_创建自旋 用 档 的 名称 加 「锁」。
完。

进程调度_锁定 接受 档：
    做 同步原语_获取 用 档 的 锁。
完。

进程调度_解锁 接受 档：
    做 同步原语_释放 用 档 的 锁。
完。

进程调度_推进滴答 接受 档、增量：
    做 进程调度_锁定 用 档。
    档 的 滴答数 为 档 的 滴答数 加 1。
    做 调度_tick 用 增量。
    做 调度时钟_更新。
    做 调度器_tick_时钟。
    档 的 采样数 为 档 的 采样数 加 1。
    做 进程调度_解锁 用 档。
完。

进程调度_采样核 接受 档、核号、任务数：
    做 进程调度_锁定 用 档。
    做 负载_采样 用 核号、任务数。
    档 的 采样数 为 档 的 采样数 加 1。
    做 进程调度_解锁 用 档。
完。

进程调度_记录切换 接受 档、核号：
    做 进程调度_锁定 用 档。
    做 负载_记录切换 用 核号。
    档 的 采样数 为 档 的 采样数 加 1。
    做 进程调度_解锁 用 档。
完。

进程调度_唤醒任务 接受 档、任务：
    做 进程调度_锁定 用 档。
    做 唤醒 用 任务。
    档 的 唤醒数 为 档 的 唤醒数 加 1。
    档 的 当前任务 为 任务。
    做 进程调度_解锁 用 档。
完。

进程调度_睡眠当前 接受 档：
    做 进程调度_锁定 用 档。
    做 睡眠。
    档 的 睡眠数 为 档 的 睡眠数 加 1。
    做 进程调度_解锁 用 档。
完。

进程调度_深睡当前 接受 档、通道：
    做 进程调度_锁定 用 档。
    做 深度睡眠 用 通道。
    档 的 睡眠数 为 档 的 睡眠数 加 1。
    做 进程调度_解锁 用 档。
完。

进程调度_让出当前 接受 档：
    做 进程调度_锁定 用 档。
    做 让出。
    档 的 让出数 为 档 的 让出数 加 1。
    做 进程调度_解锁 用 档。
完。

进程调度_执行一次 接受 档：
    做 进程调度_锁定 用 档。
    做 调度。
    档 的 滴答数 为 档 的 滴答数 加 1。
    做 进程调度_解锁 用 档。
完。

进程调度_设优先级 接受 档、任务、值：
    做 进程调度_锁定 用 档。
    做 设优先级 用 任务、值。
    做 进程调度_解锁 用 档。
完。

进程调度_设策略 接受 档、任务、策略、优先级：
    做 进程调度_锁定 用 档。
    做 设调度策略 用 任务、策略、优先级。
    做 进程调度_解锁 用 档。
完。

进程调度_公平入队 接受 档、实体：
    做 进程调度_锁定 用 档。
    做 公平调度_入队 用 实体。
    档 的 公平数 为 档 的 公平数 加 1。
    做 进程调度_解锁 用 档。
完。

进程调度_公平出队 接受 档：
    做 进程调度_锁定 用 档。
    实体 为 做 公平调度_出队。
    做 进程调度_解锁 用 档。
    返还 实体。
完。

进程调度_公平时间片 接受 档、实体：
    返还 做 公平调度_计算时间片 用 实体。
完。

进程调度_公平更新虚拟时 接受 档、实体、实际运行：
    做 公平调度_更新虚拟时 用 实体、实际运行。
    档 的 公平数 为 档 的 公平数 加 1。
完。

进程调度_公平需抢占 接受 档、当前实体、运行时长：
    返还 做 公平调度_需抢占 用 当前实体、运行时长。
完。

进程调度_公平快照 接受 档、缓冲、最大：
    返还 做 公平调度_快照 用 缓冲、最大。
完。

进程调度_公平恢复 接受 档、缓冲、数量：
    做 公平调度_恢复 用 缓冲、数量。
完。

进程调度_RT入队 接受 档、任务、优先级：
    做 进程调度_锁定 用 档。
    做 rt_入队_实时 用 任务、优先级。
    档 的 实时数 为 档 的 实时数 加 1。
    做 进程调度_解锁 用 档。
完。

进程调度_RT出队 接受 档、任务、优先级：
    做 进程调度_锁定 用 档。
    做 rt_出队_实时 用 任务、优先级。
    做 进程调度_解锁 用 档。
完。

进程调度_RT选择 接受 档：
    返还 做 rt_选择下一个_实时。
完。

进程调度_RT滴答 接受 档、当前任务：
    做 rt_tick_实时 用 当前任务。
    档 的 实时数 为 档 的 实时数 加 1。
完。

进程调度_RT有任务 接受 档：
    返还 做 RT_有任务。
完。

进程调度_RT任务数 接受 档：
    返还 做 RT_任务数。
完。

进程调度_消耗带宽 接受 档、delta_us：
    若 档 的 带宽组 等于 0：
        返还 0。
    完。
    返还 做 带宽_消耗时间 用 档 的 带宽组、delta_us。
完。

进程调度_检查带宽 接受 档：
    若 档 的 带宽组 等于 0：
        返还 0。
    完。
    做 带宽_检查周期 用 档 的 带宽组。
    返还 做 带宽_需要节流 用 档 的 带宽组。
完。

进程调度_解除带宽 接受 档：
    若 档 的 带宽组 等于 0：
        返还 0。
    完。
    做 带宽_解除节流 用 档 的 带宽组。
    返还 1。
完。

进程调度_带宽使用率 接受 档：
    若 档 的 带宽组 等于 0：
        返还 0。
    完。
    返还 做 带宽_获取使用率 用 档 的 带宽组。
完。

进程调度_找最闲 接受 档：
    返还 做 负载_找最闲。
完。

进程调度_找最忙 接受 档：
    返还 做 负载_找最忙。
完。

进程调度_需均衡 接受 档：
    返还 做 负载_需均衡。
完。

进程调度_执行均衡 接受 档：
    做 进程调度_锁定 用 档。
    若 做 负载_需均衡：
        做 触发负载均衡。
        档 的 均衡数 为 档 的 均衡数 加 1。
    完。
    做 进程调度_解锁 用 档。
完。

进程调度_idle均衡 接受 档、核号：
    做 idle均衡 用 核号。
    档 的 均衡数 为 档 的 均衡数 加 1。
完。

进程调度_迁移任务 接受 档、源CPU、目标CPU：
    结果 为 做 迁移任务 用 源CPU、目标CPU。
    档 的 均衡数 为 档 的 均衡数 加 1。
    返还 结果。
完。

进程调度_选唤醒CPU 接受 档、任务：
    返还 做 选择唤醒CPU 用 任务。
完。

进程调度_运行队列任务数 接受 档、核号：
    返还 做 运行队列_可运行数 用 核号。
完。

进程调度_利用率 接受 档、核号：
    返还 做 负载_获取利用率 用 核号。
完。

进程调度_平均利用率 接受 档：
    返还 做 负载_平均利用率。
完。

进程调度_过载 接受 档：
    返还 做 负载_是否过载。
完。

进程调度_趋势 接受 档：
    返还 做 负载_趋势。
完。

进程调度_空闲核数 接受 档：
    返还 做 负载_空闲核数。
完。

进程调度_总任务 接受 档：
    返还 做 负载_总任务。
完。

进程调度_总切换 接受 档：
    返还 做 负载_总切换。
完。

进程调度_总功耗 接受 档：
    返还 做 负载_总功耗。
完。

进程调度_摘要 接受 档：
    摘 为 做 堆_分配 用 调度摘要。
    摘 的 名称 为 档 的 名称。
    摘 的 模式 为 做 进程调度_模式名 用 档 的 模式。
    摘 的 核数 为 档 的 核数。
    摘 的 当前任务 为 档 的 当前任务。
    摘 的 滴答数 为 档 的 滴答数。
    摘 的 唤醒数 为 档 的 唤醒数。
    摘 的 睡眠数 为 档 的 睡眠数。
    摘 的 让出数 为 档 的 让出数。
    摘 的 均衡数 为 档 的 均衡数。
    摘 的 实时数 为 档 的 实时数。
    摘 的 公平数 为 档 的 公平数。
    摘 的 采样数 为 档 的 采样数。
    摘 的 最闲核 为 做 进程调度_找最闲 用 档。
    摘 的 最忙核 为 做 进程调度_找最忙 用 档。
    摘 的 过载 为 做 进程调度_过载 用 档。
    摘 的 健康 为 做 进程调度_健康检查 用 档。
    返还 摘。
完。

进程调度_摘要串 接受 档：
    摘 为 做 进程调度_摘要 用 档。
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"名称\":\"」。
    做 字符串构建器_追加 用 建、摘 的 名称。
    做 字符串构建器_追加 用 建、「\",\"模式\":\"」。
    做 字符串构建器_追加 用 建、摘 的 模式。
    做 字符串构建器_追加 用 建、「\",\"核数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 核数。
    做 字符串构建器_追加 用 建、「,\"滴答数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 滴答数。
    做 字符串构建器_追加 用 建、「,\"唤醒数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 唤醒数。
    做 字符串构建器_追加 用 建、「,\"睡眠数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 睡眠数。
    做 字符串构建器_追加 用 建、「,\"让出数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 让出数。
    做 字符串构建器_追加 用 建、「,\"均衡数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 均衡数。
    做 字符串构建器_追加 用 建、「,\"公平数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 公平数。
    做 字符串构建器_追加 用 建、「,\"实时数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 实时数。
    做 字符串构建器_追加 用 建、「,\"最闲核\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 最闲核。
    做 字符串构建器_追加 用 建、「,\"最忙核\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 最忙核。
    做 字符串构建器_追加 用 建、「,\"过载\":」。
    做 字符串构建器_追加 用 建、做 调度布尔串 用 摘 的 过载。
    做 字符串构建器_追加 用 建、「,\"健康\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 摘 的 健康。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

进程调度_批量摘要串 接受 列表：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「[」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 i 大于 0：
            做 字符串构建器_追加 用 建、「,」。
        完。
        做 字符串构建器_追加 用 建、做 进程调度_摘要串 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    做 字符串构建器_追加 用 建、「]」。
    返还 做 字符串构建器_构建 用 建。
完。

进程调度_统计串 接受 档：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"调度\":\"」。
    做 字符串构建器_追加 用 建、做 调度_统计。
    做 字符串构建器_追加 用 建、「\",\"时钟\":\"」。
    做 字符串构建器_追加 用 建、做 调度时钟_统计。
    做 字符串构建器_追加 用 建、「\",\"负载\":\"」。
    做 字符串构建器_追加 用 建、做 负载_完整统计。
    做 字符串构建器_追加 用 建、「\",\"公平\":\"」。
    做 字符串构建器_追加 用 建、做 公平调度_完整统计。
    做 字符串构建器_追加 用 建、「\",\"实时\":\"」。
    做 字符串构建器_追加 用 建、做 RT_完整统计。
    做 字符串构建器_追加 用 建、「\",\"带宽\":\"」。
    做 字符串构建器_追加 用 建、做 带宽_完整统计。
    做 字符串构建器_追加 用 建、「\"}」。
    返还 做 字符串构建器_构建 用 建。
完。

进程调度_健康检查 接受 档：
    可变 错数 为 0。
    若 档 等于 0：
        返还 1。
    完。
    若 档 的 锁 等于 0：
        错数 为 错数 加 1。
    完。
    若 做 同步原语_健康检查 用 档 的 锁 不等于 0：
        错数 为 错数 加 1。
    完。
    若 做 负载_健康检查 不等于 0：
        错数 为 错数 加 1。
    完。
    若 非 做 调度时钟_是稳定：
        错数 为 错数 加 1。
    完。
    若 做 带宽_当前节流组数 小于 0：
        错数 为 错数 加 1。
    完。
    返还 错数。
完。

进程调度_账本健康检查 接受 零参：
    可变 错数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度账本：
        错数 为 错数 加 做 进程调度_健康检查 用 调度账本 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 错数。
完。

进程调度_总览 接受 零参：
    建 为 做 字符串构建器_创建。
    做 字符串构建器_追加 用 建、「{」。
    做 字符串构建器_追加 用 建、「\"建数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 调度建数。
    做 字符串构建器_追加 用 建、「,\"账本数\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 计数 用 调度账本。
    做 字符串构建器_追加 用 建、「,\"健康错误\":」。
    做 字符串构建器_追加 用 建、做 整数转字符串 用 做 进程调度_账本健康检查。
    做 字符串构建器_追加 用 建、「,\"明细\":」。
    做 字符串构建器_追加 用 建、做 进程调度_批量摘要串 用 调度账本。
    做 字符串构建器_追加 用 建、「}」。
    返还 做 字符串构建器_构建 用 建。
完。

进程调度_示例流程 接受 零参：
    档 为 做 进程调度_创建 用 「主调度」、4。
    做 进程调度_绑定带宽_含突发 用 档、5000、10000、2000。
    做 进程调度_推进滴答 用 档、1。
    做 进程调度_采样核 用 档、0、3。
    做 进程调度_采样核 用 档、1、7。
    做 进程调度_记录切换 用 档、0。
    做 进程调度_执行均衡 用 档。
    做 进程调度_消耗带宽 用 档、120。
    做 进程调度_检查带宽 用 档。
    返还 做 进程调度_总览。
完。

可变 AI_进程调度_负载窗口 为 一组：（空）。

AI_进程调度_负载预测 接受 当前负载：
    做 追加 用 AI_进程调度_负载窗口、当前负载。
    若 做 计数 用 AI_进程调度_负载窗口 大于 20：
        做 列表_移除 用 AI_进程调度_负载窗口、0。
    完。
    若 做 计数 用 AI_进程调度_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_进程调度_负载窗口。
    趋势 为 AI_进程调度_负载窗口 的 第 n 减 1 项 减 AI_进程调度_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_进程调度_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
