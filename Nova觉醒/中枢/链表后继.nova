《链表后继》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·内核打印》。

引用《.·整数转字符串》。

链_前驱 为 0。

链_后继 为 1。

链_数据 为 2。

可变 链表_总创建 为 0。

可变 链表_总插入 为 0。

可变 链表_总删除 为 0。

可变 链表_总搜索 为 0。

可变 链表_总排序 为 0。

可变 链表_总拼接 为 0。

链表_后继 接受 节点：
    返还 节点 的 第 链_后继 项。
完。

链表_前驱 接受 节点：
    返还 节点 的 第 链_前驱 项。
完。

链表_首节点_或空 接受 头：
    若 做 链表_为空 用 头：
        返还 空。
    完。
    返还 头 的 第 链_后继 项。
完。

链表_创建 接受 零参：
    头 为 一组：0、0、空。
    做 设置 用 头、链_前驱、头。
    做 设置 用 头、链_后继、头。
    链表_总创建 为 链表_总创建 加 1。
    返还 头。
完。

链表_为空 接受 头：
    返还 头 的 第 链_后继 项 等于 头。
完。

链表_插入之间 接受 新节点、前节点、后节点：
    做 设置 用 新节点、链_前驱、前节点。
    做 设置 用 新节点、链_后继、后节点。
    做 设置 用 前节点、链_后继、新节点。
    做 设置 用 后节点、链_前驱、新节点。
完。

链表_头插 接受 头、数据：
    新节点 为 一组：空、空、数据。
    做 链表_插入之间 用 新节点、头、头 的 第 链_后继 项。
    链表_总插入 为 链表_总插入 加 1。
    返还 新节点。
完。

链表_尾插 接受 头、数据：
    新节点 为 一组：空、空、数据。
    做 链表_插入之间 用 新节点、头 的 第 链_前驱 项、头。
    链表_总插入 为 链表_总插入 加 1。
    返还 新节点。
完。

链表_插入后 接受 节点、数据：
    新节点 为 一组：空、空、数据。
    做 链表_插入之间 用 新节点、节点、节点 的 第 链_后继 项。
    链表_总插入 为 链表_总插入 加 1。
    返还 新节点。
完。

链表_插入前 接受 节点、数据：
    新节点 为 一组：空、空、数据。
    做 链表_插入之间 用 新节点、节点 的 第 链_前驱 项、节点。
    链表_总插入 为 链表_总插入 加 1。
    返还 新节点。
完。

链表_删除 接受 节点：
    前 为 节点 的 第 链_前驱 项。
    后 为 节点 的 第 链_后继 项。
    做 设置 用 前、链_后继、后。
    做 设置 用 后、链_前驱、前。
    做 设置 用 节点、链_前驱、节点。
    做 设置 用 节点、链_后继、节点。
    链表_总删除 为 链表_总删除 加 1。
完。

链表_弹出首 接受 头：
    若 做 链表_为空 用 头：
        返还 空。
    完。
    节点 为 头 的 第 链_后继 项。
    数据 为 节点 的 第 链_数据 项。
    做 链表_删除 用 节点。
    返还 数据。
完。

链表_弹出末 接受 头：
    若 做 链表_为空 用 头：
        返还 空。
    完。
    节点 为 头 的 第 链_前驱 项。
    数据 为 节点 的 第 链_数据 项。
    做 链表_删除 用 节点。
    返还 数据。
完。

链表_首节点 接受 头：
    若 做 链表_为空 用 头：
        返还 空。
    完。
    返还 头 的 第 链_后继 项。
完。

链表_末节点 接受 头：
    若 做 链表_为空 用 头：
        返还 空。
    完。
    返还 头 的 第 链_前驱 项。
完。

链表_数据 接受 节点：
    若 节点 等于 空：
        返还 空。
    完。
    返还 节点 的 第 链_数据 项。
完。

链表_设置数据 接受 节点、数据：
    若 节点 等于 空：
        返还。
    完。
    做 设置 用 节点、链_数据、数据。
完。

链表_第N个 接受 头、n：
    可变 当前 为 头 的 第 链_后继 项。
    可变 i 为 0。
    只要 当前 不等于 头 并且 i 小于 n：
        当前 为 当前 的 第 链_后继 项。
        i 为 i 加 1。
    完。
    若 当前 等于 头：
        返还 空。
    完。
    返还 当前。
完。

链表_第N个数据 接受 头、n：
    节点 为 做 链表_第N个 用 头、n。
    若 节点 等于 空：
        返还 空。
    完。
    返还 节点 的 第 链_数据 项。
完。

链表_长度 接受 头：
    可变 计数 为 0。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        计数 为 计数 加 1。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 计数。
完。

链表_到列表 接受 头：
    结果 为 一组：（空）。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        做 追加 用 结果、当前 的 第 链_数据 项。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 结果。
完。

链表_安全遍历 接受 头、回调：
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        下一个 为 当前 的 第 链_后继 项。
        做 回调 用 当前。
        当前 为 下一个。
    完。
完。

链表_反向遍历 接受 头、回调：
    可变 当前 为 头 的 第 链_前驱 项。
    只要 当前 不等于 头：
        上一个 为 当前 的 第 链_前驱 项。
        做 回调 用 当前。
        当前 为 上一个。
    完。
完。

链表_查找 接受 头、目标：
    链表_总搜索 为 链表_总搜索 加 1。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        若 当前 的 第 链_数据 项 等于 目标：
            返还 当前。
        完。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 空。
完。

链表_查找_条件 接受 头、谓词：
    链表_总搜索 为 链表_总搜索 加 1。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        若 做 谓词 用 当前 的 第 链_数据 项：
            返还 当前。
        完。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 空。
完。

链表_包含 接受 头、目标：
    返还 做 链表_查找 用 头、目标 不等于 空。
完。

链表_索引 接受 头、目标：
    可变 当前 为 头 的 第 链_后继 项。
    可变 idx 为 0。
    只要 当前 不等于 头：
        若 当前 的 第 链_数据 项 等于 目标：
            返还 idx。
        完。
        当前 为 当前 的 第 链_后继 项。
        idx 为 idx 加 1。
    完。
    返还 负 1。
完。

链表_排序 接受 头、比较函数：
    链表_总排序 为 链表_总排序 加 1。
    若 做 链表_为空 用 头：
        返还。
    完。
    长度 为 做 链表_长度 用 头。
    若 长度 小于或等于 1：
        返还。
    完。
    临时 为 做 链表_创建。
    只要 非 做 链表_为空 用 头：
        节点 为 头 的 第 链_后继 项。
        数据 为 节点 的 第 链_数据 项。
        做 链表_删除 用 节点。
        可变 插入点 为 临时 的 第 链_后继 项。
        只要 插入点 不等于 临时 并且 做 比较函数 用 插入点 的 第 链_数据 项、数据 小于或等于 0：
            插入点 为 插入点 的 第 链_后继 项。
        完。
        做 链表_插入之间 用 节点、插入点 的 第 链_前驱 项、插入点。
    完。
    若 非 做 链表_为空 用 临时：
        第一 为 临时 的 第 链_后继 项。
        最后 为 临时 的 第 链_前驱 项。
        做 设置 用 头、链_后继、第一。
        做 设置 用 第一、链_前驱、头。
        做 设置 用 头、链_前驱、最后。
        做 设置 用 最后、链_后继、头。
    完。
完。

链表_拼接 接受 目标头、源头：
    若 做 链表_为空 用 源头：
        返还。
    完。
    链表_总拼接 为 链表_总拼接 加 1。
    源首 为 源头 的 第 链_后继 项。
    源末 为 源头 的 第 链_前驱 项。
    目标末 为 目标头 的 第 链_前驱 项。
    做 设置 用 目标末、链_后继、源首。
    做 设置 用 源首、链_前驱、目标末。
    做 设置 用 源末、链_后继、目标头。
    做 设置 用 目标头、链_前驱、源末。
    做 设置 用 源头、链_后继、源头。
    做 设置 用 源头、链_前驱、源头。
完。

链表_移动到 接受 节点、目标头：
    做 链表_删除 用 节点。
    做 链表_插入之间 用 节点、目标头 的 第 链_前驱 项、目标头。
完。

链表_移动到头 接受 节点、目标头：
    做 链表_删除 用 节点。
    做 链表_插入之间 用 节点、目标头、目标头 的 第 链_后继 项。
完。

链表_交换数据 接受 节点A、节点B：
    若 节点A 等于 空 或者 节点B 等于 空 或者 节点A 等于 节点B：
        返还。
    完。
    临时 为 节点A 的 第 链_数据 项。
    做 设置 用 节点A、链_数据、节点B 的 第 链_数据 项。
    做 设置 用 节点B、链_数据、临时。
完。

链表_反转 接受 头：
    若 做 链表_为空 用 头：
        返还。
    完。
    可变 当前 为 头。
    只要 真：
        临时 为 当前 的 第 链_后继 项。
        做 设置 用 当前、链_后继、当前 的 第 链_前驱 项。
        做 设置 用 当前、链_前驱、临时。
        当前 为 临时。
        若 当前 等于 头：
            跳出。
        完。
    完。
完。

链表_旋转左 接受 头：
    若 做 链表_为空 用 头：
        返还。
    完。
    首 为 头 的 第 链_后继 项。
    做 链表_删除 用 首。
    做 链表_插入之间 用 首、头 的 第 链_前驱 项、头。
完。

链表_旋转右 接受 头：
    若 做 链表_为空 用 头：
        返还。
    完。
    末 为 头 的 第 链_前驱 项。
    做 链表_删除 用 末。
    做 链表_插入之间 用 末、头、头 的 第 链_后继 项。
完。

链表_过滤 接受 头、谓词：
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        下一个 为 当前 的 第 链_后继 项。
        若 非 做 谓词 用 当前 的 第 链_数据 项：
            做 链表_删除 用 当前。
        完。
        当前 为 下一个。
    完。
完。

链表_映射 接受 头、变换：
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        做 设置 用 当前、链_数据、做 变换 用 当前 的 第 链_数据 项。
        当前 为 当前 的 第 链_后继 项。
    完。
完。

链表_计数_条件 接受 头、谓词：
    可变 计数 为 0。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        若 做 谓词 用 当前 的 第 链_数据 项：
            计数 为 计数 加 1。
        完。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 计数。
完。

链表_从列表 接受 列表：
    头 为 做 链表_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 链表_尾插 用 头、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 头。
完。

链表_打印统计 接受 零参：
    做 打印行 用 「=== 链表统计 ===」。
    做 打印行 用 「创建: 」 加 做 整数转字符串 用 链表_总创建。
    做 打印行 用 「插入: 」 加 做 整数转字符串 用 链表_总插入。
    做 打印行 用 「删除: 」 加 做 整数转字符串 用 链表_总删除。
    做 打印行 用 「搜索: 」 加 做 整数转字符串 用 链表_总搜索。
    做 打印行 用 「排序: 」 加 做 整数转字符串 用 链表_总排序。
    做 打印行 用 「拼接: 」 加 做 整数转字符串 用 链表_总拼接。
完。

链表_获取统计 接受 零参：
    返还 一组：链表_总创建、链表_总插入、链表_总删除、链表_总搜索、链表_总排序、链表_总拼接。
完。

链表_去重_已排序 接受 头、相等判定：
    若 做 链表_为空 用 头：
        返还 0。
    完。
    可变 删除数 为 0。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        下一个 为 当前 的 第 链_后继 项。
        若 下一个 不等于 头 并且 做 相等判定 用 当前 的 第 链_数据 项、下一个 的 第 链_数据 项：
            做 链表_删除 用 下一个。
            删除数 为 删除数 加 1。
        否则：
            当前 为 下一个。
        完。
    完。
    返还 删除数。
完。

链表_去重 接受 头、相等判定：
    若 做 链表_为空 用 头：
        返还 0。
    完。
    可变 删除数 为 0。
    可变 外 为 头 的 第 链_后继 项。
    只要 外 不等于 头：
        可变 内 为 外 的 第 链_后继 项。
        只要 内 不等于 头：
            下一个 为 内 的 第 链_后继 项。
            若 做 相等判定 用 外 的 第 链_数据 项、内 的 第 链_数据 项：
                做 链表_删除 用 内。
                删除数 为 删除数 加 1。
            完。
            内 为 下一个。
        完。
        外 为 外 的 第 链_后继 项。
    完。
    返还 删除数。
完。

链表_分割 接受 头、位置：
    新头 为 做 链表_创建。
    长度 为 做 链表_长度 用 头。
    若 位置 大于或等于 长度 或者 位置 小于或等于 0：
        返还 新头。
    完。
    分割节点 为 做 链表_第N个 用 头、位置。
    若 分割节点 等于 空：
        返还 新头。
    完。
    原末 为 头 的 第 链_前驱 项。
    分割前 为 分割节点 的 第 链_前驱 项。
    做 设置 用 分割前、链_后继、头。
    做 设置 用 头、链_前驱、分割前。
    做 设置 用 新头、链_后继、分割节点。
    做 设置 用 分割节点、链_前驱、新头。
    做 设置 用 新头、链_前驱、原末。
    做 设置 用 原末、链_后继、新头。
    返还 新头。
完。

链表_折叠 接受 头、初始值、累积函数：
    可变 结果 为 初始值。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        结果 为 做 累积函数 用 结果、当前 的 第 链_数据 项。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 结果。
完。

链表_右折叠 接受 头、初始值、累积函数：
    可变 结果 为 初始值。
    可变 当前 为 头 的 第 链_前驱 项。
    只要 当前 不等于 头：
        结果 为 做 累积函数 用 当前 的 第 链_数据 项、结果。
        当前 为 当前 的 第 链_前驱 项。
    完。
    返还 结果。
完。

链表_完整性检查 接受 头：
    若 头 等于 空：
        返还 假。
    完。
    若 头 的 第 链_后继 项 等于 头：
        返还 头 的 第 链_前驱 项 等于 头。
    完。
    可变 当前 为 头 的 第 链_后继 项。
    可变 步数 为 0。
    最大步 为 100000。
    只要 当前 不等于 头 并且 步数 小于 最大步：
        后继 为 当前 的 第 链_后继 项。
        前驱 为 当前 的 第 链_前驱 项。
        若 后继 的 第 链_前驱 项 不等于 当前：
            返还 假。
        完。
        若 前驱 的 第 链_后继 项 不等于 当前：
            返还 假。
        完。
        当前 为 后继。
        步数 为 步数 加 1。
    完。
    返还 当前 等于 头。
完。

链表_重置统计 接受 零参：
    链表_总创建 为 0。
    链表_总插入 为 0。
    链表_总删除 为 0。
    链表_总搜索 为 0。
    链表_总排序 为 0。
    链表_总拼接 为 0。
完。

链表_诊断 接受 头、格式化：
    做 打印行 用 「=== 链表诊断 ===」。
    长度 为 做 链表_长度 用 头。
    做 打印行 用 「长度: 」 加 做 整数转字符串 用 长度。
    做 打印行 用 「完整性: 」 加 做 整数转字符串 用 做 链表_完整性检查 用 头。
    若 长度 小于或等于 0 或者 格式化 等于 空：
        返还。
    完。
    可变 当前 为 头 的 第 链_后继 项。
    可变 idx 为 0。
    只要 当前 不等于 头 并且 idx 小于 20：
        做 打印行 用 「  [」 加 做 整数转字符串 用 idx 加 「] 」 加 做 格式化 用 当前 的 第 链_数据 项。
        当前 为 当前 的 第 链_后继 项。
        idx 为 idx 加 1。
    完。
    若 idx 小于 长度：
        做 打印行 用 「  ... (省略 」 加 做 整数转字符串 用 长度 减 idx 加 「 项)」。
    完。
完。

本章 收束。
