《赫布学习振》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·赫布追加》。

赫布_学习振幅 接受 频谱列表、频谱数、维度、索引列表、k：
    可变 振幅均值 为 一组：（空）。
    可变 振幅标准差 为 一组：（空）。
    可变 ki 为 0。
    只要 ki 小于 k：
        idx 为 索引列表 的 第 ki 项。
        可变 绝对值和 为 0。
        可变 平方和 为 0。
        可变 si 为 0。
        只要 si 小于 频谱数：
            val 为 频谱列表 的 第 si 乘 维度 加 idx 项。
            av 为 做 赫布_绝对值 用 val。
            绝对值和 为 绝对值和 加 av。
            平方和 为 平方和 加 av 乘 av 除以 频谱数。
            si 为 si 加 1。
        完。
        均值 为 绝对值和 除以 频谱数。
        可变 方差 为 平方和 减 均值 乘 均值。
        若 方差 小于 0：
            方差 为 0。
        完。
        可变 标准差 为 0。
        只要 标准差 乘 标准差 小于 方差：
            标准差 为 标准差 加 1。
        完。
        做 赫布_追加 用 振幅均值、均值。
        做 赫布_追加 用 振幅标准差、标准差。
        ki 为 ki 加 1。
    完。
    返还 一组：振幅均值、振幅标准差。
完。

赫布_生成频谱 接受 关联、振幅均值、振幅标准差、索引列表、k、维度、种子、DC值：
    可变 频谱 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 赫布_追加 用 频谱、0。
        i 为 i 加 1。
    完。
    频谱 的 第 0 项 为 DC值。
    可变 已决定 为 一组：（空）。
    可变 当前种子 为 种子。
    可变 ki 为 0。
    只要 ki 小于 k：
        idx 为 索引列表 的 第 ki 项。
        可变 符号 为 1。
        若 ki 大于 0：
            符号 为 做 赫布_预测符号 用 关联、已决定、ki、k。
        否则：
            若 当前种子 小于 0：
                符号 为 0 减 1。
            完。
        完。
        可变 振幅 为 振幅均值 的 第 ki 项。
        可变 种子绝对值 为 做 赫布_绝对值 用 当前种子。
        振幅 为 振幅 加 种子绝对值 乘 振幅标准差 的 第 ki 项 除以 1000。
        频谱 的 第 idx 项 为 符号 乘 振幅。
        做 赫布_追加 用 已决定、符号。
        当前种子 为 当前种子 乘 1103 加 7919。
        若 当前种子 小于 0：
            当前种子 为 0 减 当前种子。
        完。
        当前种子 为 当前种子 取余于 2000 减 1000。
        ki 为 ki 加 1。
    完。
    返还 频谱。
完。

赫布_噪声修复 接受 权重矩阵、模式列表、模式数、维度、修复轮数：
    可变 w 为 一组：（空）。
    可变 ci 为 0。
    只要 ci 小于 做 计数 用 权重矩阵：
        做 赫布_追加 用 w、权重矩阵 的 第 ci 项。
        ci 为 ci 加 1。
    完。
    可变 轮次 为 0。
    只要 轮次 小于 修复轮数：
        可变 pi 为 0。
        只要 pi 小于 模式数：
            偏移 为 pi 乘 维度。
            可变 回忆 为 一组：（空）。
            可变 i 为 0。
            只要 i 小于 维度：
                可变 和 为 0。
                可变 j 为 0。
                只要 j 小于 维度：
                    和 为 和 加 w 的 第 i 乘 维度 加 j 项 乘 模式列表 的 第 偏移 加 j 项。
                    j 为 j 加 1。
                完。
                若 和 大于 0：
                    做 赫布_追加 用 回忆、1。
                否则：
                    做 赫布_追加 用 回忆、0 减 1。
                完。
                i 为 i 加 1。
            完。
            i 为 0。
            只要 i 小于 维度：
                若 回忆 的 第 i 项 不等于 做 赫布_符号 用 模式列表 的 第 偏移 加 i 项：
                    可变 j2 为 0。
                    只要 j2 小于 维度：
                        w 的 第 i 乘 维度 加 j2 项 为 w 的 第 i 乘 维度 加 j2 项 加 做 赫布_符号 用 模式列表 的 第 偏移 加 i 项 乘 做 赫布_符号 用 模式列表 的 第 偏移 加 j2 项。
                        j2 为 j2 加 1。
                    完。
                完。
                i 为 i 加 1。
            完。
            pi 为 pi 加 1。
        完。
        轮次 为 轮次 加 1。
    完。
    返还 w。
完。

本章 收束。
