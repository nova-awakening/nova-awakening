《遍历回调》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·双向链表操作》。

引用《.·链表后继》。

引用《.·字符串替换》。

引用《.·链表移到头部》。

链表_遍历 接受 头、回调：
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        做 回调 用 当前 的 第 链_数据 项。
        当前 为 当前 的 第 链_后继 项。
    完。
完。

链表_反向遍历 接受 头、回调：
    可变 当前 为 头 的 第 链_前驱 项。
    只要 当前 不等于 头：
        做 回调 用 当前 的 第 链_数据 项。
        当前 为 当前 的 第 链_前驱 项。
    完。
完。

链表_映射 接受 头、转换函数：
    可变 结果 为 一组：（空）。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        做 追加 用 结果、做 转换函数 用 当前 的 第 链_数据 项。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 结果。
完。

链表_归约 接受 头、初始值、归约函数：
    可变 累积 为 初始值。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        累积 为 做 归约函数 用 累积、当前 的 第 链_数据 项。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 累积。
完。

链表_弹出首 接受 头：
    若 做 链表_为空 用 头：
        返还 空。
    完。
    首 为 头 的 第 链_后继 项。
    做 链表_删除 用 首。
    返还 首 的 第 链_数据 项。
完。

链表_弹出末 接受 头：
    若 做 链表_为空 用 头：
        返还 空。
    完。
    末 为 头 的 第 链_前驱 项。
    做 链表_删除 用 末。
    返还 末 的 第 链_数据 项。
完。

链表_清空 接受 头：
    只要 非 做 链表_为空 用 头：
        做 链表_删除 用 头 的 第 链_后继 项。
    完。
完。

链表_第N个 接受 头、索引：
    可变 当前 为 头 的 第 链_后继 项。
    可变 i 为 0。
    只要 当前 不等于 头：
        若 i 等于 索引：
            返还 当前 的 第 链_数据 项。
        完。
        当前 为 当前 的 第 链_后继 项。
        i 为 i 加 1。
    完。
    返还 空。
完。

链表_替换 接受 旧节点、新节点：
    做 设置 用 新节点、链_前驱、旧节点 的 第 链_前驱 项。
    做 设置 用 新节点、链_后继、旧节点 的 第 链_后继 项。
    做 设置 用 旧节点 的 第 链_前驱 项、链_后继、新节点。
    做 设置 用 旧节点 的 第 链_后继 项、链_前驱、新节点。
完。

链表_旋转 接受 头：
    若 做 链表_为空 用 头：
        返还。
    完。
    首 为 头 的 第 链_后继 项。
    做 链表_移到尾部 用 头、首。
完。

H_下一个 为 0。

H_前驱指针 为 1。

H_数据 为 2。

hlist_创建 接受 零参：
    返还 一组：空。
完。

hlist_为空 接受 头：
    返还 头 的 第 0 项 等于 空。
完。

hlist_头插 接受 头、数据：
    新节点 为 一组：头 的 第 0 项、头、数据。
    若 头 的 第 0 项 不等于 空：
        做 设置 用 头 的 第 0 项、H_前驱指针、新节点。
    完。
    做 设置 用 头、0、新节点。
    返还 新节点。
完。

hlist_删除 接受 节点：
    下一个 为 节点 的 第 H_下一个 项。
    前驱 为 节点 的 第 H_前驱指针 项。
    若 前驱 不等于 空：
        做 设置 用 前驱、0、下一个。
    完。
    若 下一个 不等于 空：
        做 设置 用 下一个、H_前驱指针、前驱。
    完。
完。

hlist_遍历 接受 头、回调：
    可变 当前 为 头 的 第 0 项。
    只要 当前 不等于 空：
        下一个 为 当前 的 第 H_下一个 项。
        做 回调 用 当前 的 第 H_数据 项。
        当前 为 下一个。
    完。
完。

hlist_查找 接受 头、目标：
    可变 当前 为 头 的 第 0 项。
    只要 当前 不等于 空：
        若 当前 的 第 H_数据 项 等于 目标：
            返还 当前。
        完。
        当前 为 当前 的 第 H_下一个 项。
    完。
    返还 空。
完。

hlist_长度 接受 头：
    可变 数 为 0。
    可变 当前 为 头 的 第 0 项。
    只要 当前 不等于 空：
        数 为 数 加 1。
        当前 为 当前 的 第 H_下一个 项。
    完。
    返还 数。
完。

AI模式窗口 为 16。

遍历方向_正向 为 0。

遍历方向_反向 为 1。

链_后继 为 1。

链_前驱 为 0。

链_数据 为 2。

遍历统计 是 一种 事物，含 正向遍历次数、反向遍历次数、映射次数、归约次数、过滤次数、查找首次数、查找所有次数、弹出首次数、弹出末次数、清空次数、替换次数、旋转次数、安全遍历次数、hlist操作次数、AI模式记录数、AI模式命中数、回调执行总数、错误次数。

可变 全局统计 为 新建 遍历统计。

可变 遍历模块已初始化 为 假。

可变 AI模式历史 为 一组：（空）。

可变 AI模式位置 为 0。

遍历模块_初始化 接受 零参：
    若 遍历模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI模式历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI模式窗口：
        做 追加 用 AI模式历史、0。
        i 为 i 加 1。
    完。
    AI模式位置 为 0。
    遍历模块已初始化 为 真。
    做 打印 用 「[遍历回调] 初始化完成」。
    返还 0。
完。

遍历模块_销毁 接受 零参：
    若 遍历模块已初始化 等于 假：
        返还 0。
    完。
    AI模式历史 为 一组：（空）。
    遍历模块已初始化 为 假。
    做 打印 用 「[遍历回调] 已销毁」。
    返还 0。
完。

链表_安全遍历 接受 头、回调：
    若 头 等于 空 或者 回调 等于 空：
        返还。
    完。
    全局统计 的 安全遍历次数 为 全局统计 的 安全遍历次数 加 1。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        下一个 为 当前 的 第 链_后继 项。
        做 回调 用 当前 的 第 链_数据 项。
        全局统计 的 回调执行总数 为 全局统计 的 回调执行总数 加 1。
        当前 为 下一个。
    完。
完。

链表_安全反向遍历 接受 头、回调：
    若 头 等于 空 或者 回调 等于 空：
        返还。
    完。
    全局统计 的 安全遍历次数 为 全局统计 的 安全遍历次数 加 1。
    可变 当前 为 头 的 第 链_前驱 项。
    只要 当前 不等于 头：
        前一个 为 当前 的 第 链_前驱 项。
        做 回调 用 当前 的 第 链_数据 项。
        全局统计 的 回调执行总数 为 全局统计 的 回调执行总数 加 1。
        当前 为 前一个。
    完。
完。

链表_遍历_计数 接受 头、回调：
    若 头 等于 空 或者 回调 等于 空：
        返还 0。
    完。
    全局统计 的 正向遍历次数 为 全局统计 的 正向遍历次数 加 1。
    可变 计数 为 0。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        下一个 为 当前 的 第 链_后继 项。
        若 做 回调 用 当前 的 第 链_数据 项 等于 真：
            计数 为 计数 加 1。
        完。
        全局统计 的 回调执行总数 为 全局统计 的 回调执行总数 加 1。
        当前 为 下一个。
    完。
    返还 计数。
完。

链表_遍历_带索引 接受 头、回调：
    若 头 等于 空 或者 回调 等于 空：
        返还。
    完。
    全局统计 的 正向遍历次数 为 全局统计 的 正向遍历次数 加 1。
    可变 索引 为 0。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        做 回调 用 当前 的 第 链_数据 项、索引。
        全局统计 的 回调执行总数 为 全局统计 的 回调执行总数 加 1。
        当前 为 当前 的 第 链_后继 项。
        索引 为 索引 加 1。
    完。
完。

链表_遍历_直到 接受 头、回调：
    若 头 等于 空 或者 回调 等于 空：
        返还 空。
    完。
    全局统计 的 查找首次数 为 全局统计 的 查找首次数 加 1。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        若 做 回调 用 当前 的 第 链_数据 项 等于 真：
            返还 当前 的 第 链_数据 项。
        完。
        全局统计 的 回调执行总数 为 全局统计 的 回调执行总数 加 1。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 空。
完。

链表_查找所有 接受 头、条件：
    若 头 等于 空 或者 条件 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 查找所有次数 为 全局统计 的 查找所有次数 加 1。
    可变 结果 为 一组：（空）。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        若 做 条件 用 当前 的 第 链_数据 项 等于 真：
            做 追加 用 结果、当前 的 第 链_数据 项。
        完。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 结果。
完。

链表_映射_过滤 接受 头、转换函数、条件函数：
    若 头 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 映射次数 为 全局统计 的 映射次数 加 1。
    全局统计 的 过滤次数 为 全局统计 的 过滤次数 加 1。
    可变 结果 为 一组：（空）。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        若 条件函数 等于 空 或者 做 条件函数 用 当前 的 第 链_数据 项 等于 真：
            做 追加 用 结果、做 转换函数 用 当前 的 第 链_数据 项。
        完。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 结果。
完。

链表_扁平映射 接受 头、转换函数：
    若 头 等于 空 或者 转换函数 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 映射次数 为 全局统计 的 映射次数 加 1。
    可变 结果 为 一组：（空）。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        子结果 为 做 转换函数 用 当前 的 第 链_数据 项。
        若 子结果 不等于 空：
            可变 k 为 0。
            只要 k 小于 做 计数 用 子结果：
                做 追加 用 结果、子结果 的 第 k 项。
                k 为 k 加 1。
            完。
        完。
        当前 为 当前 的 第 链_后继 项。
    完。
    返还 结果。
完。

链表_求和 接受 头：
    可变 总和 为 0。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        总和 为 总和 加 当前 的 第 链_数据 项。
        当前 为 当前 的 第 链_后继 项。
    完。
    全局统计 的 归约次数 为 全局统计 的 归约次数 加 1。
    返还 总和。
完。

链表_最大值 接受 头：
    可变 最大 为 0。
    可变 已找到 为 假。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        若 已找到 等于 假 或者 当前 的 第 链_数据 项 大于 最大：
            最大 为 当前 的 第 链_数据 项。
            已找到 为 真。
        完。
        当前 为 当前 的 第 链_后继 项。
    完。
    全局统计 的 归约次数 为 全局统计 的 归约次数 加 1。
    返还 最大。
完。

链表_最小值 接受 头：
    可变 最小 为 0。
    可变 已找到 为 假。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        若 已找到 等于 假 或者 当前 的 第 链_数据 项 小于 最小：
            最小 为 当前 的 第 链_数据 项。
            已找到 为 真。
        完。
        当前 为 当前 的 第 链_后继 项。
    完。
    全局统计 的 归约次数 为 全局统计 的 归约次数 加 1。
    返还 最小。
完。

链表_平均值 接受 头：
    可变 总和 为 0。
    可变 计数 为 0。
    可变 当前 为 头 的 第 链_后继 项。
    只要 当前 不等于 头：
        总和 为 总和 加 当前 的 第 链_数据 项。
        计数 为 计数 加 1。
        当前 为 当前 的 第 链_后继 项。
    完。
    若 计数 等于 0：
        返还 0。
    完。
    返还 总和 除以 计数。
完。

hlist_安全遍历 接受 头、回调：
    若 头 等于 空 或者 回调 等于 空：
        返还。
    完。
    全局统计 的 hlist操作次数 为 全局统计 的 hlist操作次数 加 1。
    可变 当前 为 头 的 第 0 项。
    只要 当前 不等于 空：
        下一个 为 当前 的 第 H_下一个 项。
        做 回调 用 当前 的 第 H_数据 项。
        全局统计 的 回调执行总数 为 全局统计 的 回调执行总数 加 1。
        当前 为 下一个。
    完。
完。

hlist_映射 接受 头、转换函数：
    若 头 等于 空 或者 转换函数 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 hlist操作次数 为 全局统计 的 hlist操作次数 加 1。
    可变 结果 为 一组：（空）。
    可变 当前 为 头 的 第 0 项。
    只要 当前 不等于 空：
        做 追加 用 结果、做 转换函数 用 当前 的 第 H_数据 项。
        当前 为 当前 的 第 H_下一个 项。
    完。
    返还 结果。
完。

hlist_过滤查找 接受 头、条件：
    若 头 等于 空 或者 条件 等于 空：
        返还 一组：（空）。
    完。
    全局统计 的 hlist操作次数 为 全局统计 的 hlist操作次数 加 1。
    可变 结果 为 一组：（空）。
    可变 当前 为 头 的 第 0 项。
    只要 当前 不等于 空：
        若 做 条件 用 当前 的 第 H_数据 项 等于 真：
            做 追加 用 结果、当前 的 第 H_数据 项。
        完。
        当前 为 当前 的 第 H_下一个 项。
    完。
    返还 结果。
完。

hlist_包含 接受 头、目标：
    返还 做 hlist_查找 用 头、目标 不等于 空。
完。

hlist_清空 接受 头：
    若 头 等于 空：
        返还。
    完。
    只要 头 的 第 0 项 不等于 空：
        当前 为 头 的 第 0 项。
        做 设置 用 头、0、当前 的 第 H_下一个 项。
    完。
完。

AI_记录遍历方向 接受 方向：
    做 设置 用 AI模式历史、AI模式位置、方向。
    AI模式位置 为 (AI模式位置 加 1) 取余于 AI模式窗口。
    全局统计 的 AI模式记录数 为 全局统计 的 AI模式记录数 加 1。
完。

AI_预测遍历方向 接受 零参：
    可变 正向数 为 0。
    可变 反向数 为 0。
    可变 i 为 0。
    只要 i 小于 AI模式窗口：
        若 AI模式历史 的 第 i 项 等于 遍历方向_正向：
            正向数 为 正向数 加 1。
        否则若 AI模式历史 的 第 i 项 等于 遍历方向_反向：
            反向数 为 反向数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 正向数 大于 反向数 乘 2：
        全局统计 的 AI模式命中数 为 全局统计 的 AI模式命中数 加 1。
        返还 遍历方向_正向。
    完。
    若 反向数 大于 正向数 乘 2：
        全局统计 的 AI模式命中数 为 全局统计 的 AI模式命中数 加 1。
        返还 遍历方向_反向。
    完。
    返还 遍历方向_正向。
完。

AI_建议预取方向 接受 零参：
    方向 为 做 AI_预测遍历方向。
    若 方向 等于 遍历方向_正向：
        返还 「正向预取后继节点」。
    完。
    返还 「反向预取前驱节点」。
完。

遍历回调_转储 接受 零参：
    做 打印 用 「[遍历回调] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  正向遍历: 」、做 转字符串 用 全局统计 的 正向遍历次数。
    做 打印 用 做 字符串拼接 用 「  反向遍历: 」、做 转字符串 用 全局统计 的 反向遍历次数。
    做 打印 用 做 字符串拼接 用 「  安全遍历: 」、做 转字符串 用 全局统计 的 安全遍历次数。
    做 打印 用 做 字符串拼接 用 「  回调执行总数: 」、做 转字符串 用 全局统计 的 回调执行总数。
    做 打印 用 做 字符串拼接 用 「  AI建议: 」、做 AI_建议预取方向。
    返还 0。
完。

遍历回调_打印统计 接受 零参：
    做 打印 用 「[遍历回调] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  正向遍历: 」、做 转字符串 用 全局统计 的 正向遍历次数。
    做 打印 用 做 字符串拼接 用 「  反向遍历: 」、做 转字符串 用 全局统计 的 反向遍历次数。
    做 打印 用 做 字符串拼接 用 「  映射次数: 」、做 转字符串 用 全局统计 的 映射次数。
    做 打印 用 做 字符串拼接 用 「  归约次数: 」、做 转字符串 用 全局统计 的 归约次数。
    做 打印 用 做 字符串拼接 用 「  过滤次数: 」、做 转字符串 用 全局统计 的 过滤次数。
    做 打印 用 做 字符串拼接 用 「  查找首: 」、做 转字符串 用 全局统计 的 查找首次数。
    做 打印 用 做 字符串拼接 用 「  查找所有: 」、做 转字符串 用 全局统计 的 查找所有次数。
    做 打印 用 做 字符串拼接 用 「  弹出首: 」、做 转字符串 用 全局统计 的 弹出首次数。
    做 打印 用 做 字符串拼接 用 「  弹出末: 」、做 转字符串 用 全局统计 的 弹出末次数。
    做 打印 用 做 字符串拼接 用 「  替换次数: 」、做 转字符串 用 全局统计 的 替换次数。
    做 打印 用 做 字符串拼接 用 「  安全遍历: 」、做 转字符串 用 全局统计 的 安全遍历次数。
    做 打印 用 做 字符串拼接 用 「  hlist操作: 」、做 转字符串 用 全局统计 的 hlist操作次数。
    做 打印 用 做 字符串拼接 用 「  AI模式记录: 」、做 转字符串 用 全局统计 的 AI模式记录数。
    做 打印 用 做 字符串拼接 用 「  AI模式命中: 」、做 转字符串 用 全局统计 的 AI模式命中数。
    做 打印 用 做 字符串拼接 用 「  回调执行总数: 」、做 转字符串 用 全局统计 的 回调执行总数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

遍历回调_注册到内核 接受 内核上下文：
    若 遍历模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[遍历回调] 已注册到内核上下文」。
    返还 0。
完。

遍历回调_查询状态 接受 零参：
    若 遍历模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

遍历回调_健康检查 接受 零参：
    若 遍历模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 回调执行总数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
