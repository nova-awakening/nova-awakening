《中断请求描述》

引用《.·同步原语》。

引用《.·日志中枢》。

引用《.·自动扩容》。

引用《.·字符串替换》。

IRQ状态_禁用 为 0。

IRQ状态_启用 为 1。

IRQ状态_处理中 为 2。

IRQ状态_挂起 为 3。

IRQ状态_重放 为 4。

IRQ状态_等待线程 为 5。

IRQ状态_屏蔽 为 6。

IRQF_TRIGGER_RISING 为 0x01。

IRQF_TRIGGER_FALLING 为 0x02。

IRQF_TRIGGER_HIGH 为 0x04。

IRQF_TRIGGER_LOW 为 0x08。

IRQF_TRIGGER_NONE 为 0x00。

IRQF_TRIGGER_MASK 为 0x0F。

IRQF_SHARED 为 0x80。

IRQF_TIMER 为 0x200。

IRQF_NO_THREAD 为 0x400。

IRQF_ONESHOT 为 0x2000。

IRQF_NO_SUSPEND 为 0x4000。

IRQF_PERCPU 为 0x100。

IRQ_NONE 为 0。

IRQ_HANDLED 为 1。

IRQ_WAKE_THREAD 为 2。

芯片类型_未知 为 0。

芯片类型_PIC 为 1。

芯片类型_APIC 为 2。

芯片类型_MSI 为 3。

芯片类型_IOAPIC 为 4。

IRQ芯片 是 一种 事物，含 名称、类型、启用回调、禁用回调、应答回调、屏蔽回调、解屏蔽回调、重触发回调、设置亲和回调、设置类型回调、结束回调。

IRQ动作 是 一种 事物，含 处理函数、线程函数、线程ID、名称、设备ID、标志、下一个、触发次数、线程触发次数。

IRQ描述符 是 一种 事物，含 IRQ号、硬件IRQ号、动作链、状态、触发类型、触发计数、未处理计数、伪中断计数、线程处理计数、芯片、芯片数据、亲和CPU、有效亲和CPU、挂起亲和CPU、锁、深度、唤醒深度、域、最后触发时间、最大延迟、总延迟。

IRQ域 是 一种 事物，含 名称、映射表、反向映射表、大小、基址、控制器。

最大IRQ数 为 256。

可变 IRQ表 为 一组：（空）。

可变 IRQ_已初始化 为 假。

可变 统计_总触发数 为 0。

可变 统计_总处理数 为 0。

可变 统计_总未处理数 为 0。

可变 统计_总伪中断数 为 0。

可变 统计_总注册数 为 0。

可变 统计_总注销数 为 0。

可变 统计_总启用数 为 0。

可变 统计_总禁用数 为 0。

可变 统计_共享IRQ数 为 0。

可变 统计_线程化IRQ数 为 0。

可变 统计_亲和设置数 为 0。

可变 统计_触发类型变更数 为 0。

可变 统计_芯片注册数 为 0。

可变 统计_域映射数 为 0。

可变 统计_错误数 为 0。

最大域数 为 16。

可变 IRQ域表 为 一组：（空）。

可变 域数量 为 0。

IRQ芯片_创建 接受 名称、类型：
    若 名称 等于 空：
        返还 空。
    完。
    若 类型 小于 芯片类型_未知 或者 类型 大于 芯片类型_IOAPIC：
        统计_错误数 为 统计_错误数 加 1。
        返还 空。
    完。
    芯片 为 新建 IRQ芯片。
    芯片 的 名称 为 名称。
    芯片 的 类型 为 类型。
    芯片 的 启用回调 为 空。
    芯片 的 禁用回调 为 空。
    芯片 的 应答回调 为 空。
    芯片 的 屏蔽回调 为 空。
    芯片 的 解屏蔽回调 为 空。
    芯片 的 重触发回调 为 空。
    芯片 的 设置亲和回调 为 空。
    芯片 的 设置类型回调 为 空。
    芯片 的 结束回调 为 空。
    统计_芯片注册数 为 统计_芯片注册数 加 1。
    返还 芯片。
完。

IRQ芯片_设置回调 接受 芯片、类别、回调：
    若 芯片 等于 空 或者 回调 等于 空：
        返还 错误_无效参数。
    完。
    若 类别 等于 「启用」：
        芯片 的 启用回调 为 回调。
    否则若 类别 等于 「禁用」：
        芯片 的 禁用回调 为 回调。
    否则若 类别 等于 「应答」：
        芯片 的 应答回调 为 回调。
    否则若 类别 等于 「屏蔽」：
        芯片 的 屏蔽回调 为 回调。
    否则若 类别 等于 「解屏蔽」：
        芯片 的 解屏蔽回调 为 回调。
    否则若 类别 等于 「重触发」：
        芯片 的 重触发回调 为 回调。
    否则若 类别 等于 「设置亲和」：
        芯片 的 设置亲和回调 为 回调。
    否则若 类别 等于 「设置类型」：
        芯片 的 设置类型回调 为 回调。
    否则若 类别 等于 「结束」：
        芯片 的 结束回调 为 回调。
    否则：
        返还 错误_无效参数。
    完。
    返还 0。
完。

IRQ描述符_初始化全部 接受 零参：
    若 IRQ_已初始化 等于 真：
        做 内核警告 用 「[IRQ描述符] 警告: 重复初始化, 跳过」。
        返还 错误_已存在。
    完。
    IRQ表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大IRQ数：
        desc 为 新建 IRQ描述符。
        desc 的 IRQ号 为 i。
        desc 的 硬件IRQ号 为 i。
        desc 的 动作链 为 空。
        desc 的 状态 为 IRQ状态_禁用。
        desc 的 触发类型 为 IRQF_TRIGGER_NONE。
        desc 的 触发计数 为 0。
        desc 的 未处理计数 为 0。
        desc 的 伪中断计数 为 0。
        desc 的 线程处理计数 为 0。
        desc 的 芯片 为 空。
        desc 的 芯片数据 为 空。
        desc 的 亲和CPU 为 0xFFFFFFFFFFFFFFFF。
        desc 的 有效亲和CPU 为 0xFFFFFFFFFFFFFFFF。
        desc 的 挂起亲和CPU 为 0。
        desc 的 锁 为 做 自旋锁_创建。
        desc 的 深度 为 1。
        desc 的 唤醒深度 为 0。
        desc 的 域 为 空。
        desc 的 最后触发时间 为 0。
        desc 的 最大延迟 为 0。
        desc 的 总延迟 为 0。
        做 追加 用 IRQ表、desc。
        i 为 i 加 1。
    完。
    IRQ域表 为 一组：（空）。
    域数量 为 0。
    IRQ_已初始化 为 真。
    做 内核信息 用 「[IRQ描述符] 初始化完成, 向量数=」 加 做 转字符串 用 最大IRQ数。
    返还 0。
完。

IRQ域_创建 接受 名称、大小、基址、控制器：
    若 名称 等于 空：
        返还 空。
    完。
    若 域数量 大于或等于 最大域数：
        统计_错误数 为 统计_错误数 加 1。
        做 内核警告 用 「[IRQ域] 域表已满, 最大=」 加 做 转字符串 用 最大域数。
        返还 空。
    完。
    域 为 新建 IRQ域。
    域 的 名称 为 名称。
    域 的 映射表 为 一组：（空）。
    域 的 反向映射表 为 一组：（空）。
    域 的 大小 为 大小。
    域 的 基址 为 基址。
    域 的 控制器 为 控制器。
    可变 i 为 0。
    只要 i 小于 大小：
        做 追加 用 域 的 映射表、0 减 1。
        做 追加 用 域 的 反向映射表、0 减 1。
        i 为 i 加 1。
    完。
    做 追加 用 IRQ域表、域。
    域数量 为 域数量 加 1。
    做 内核信息 用 「[IRQ域] 创建: 」 加 名称 加 「 大小=」 加 做 转字符串 用 大小。
    返还 域。
完。

IRQ域_映射 接受 域、硬件IRQ、虚拟IRQ：
    若 域 等于 空：
        返还 错误_无效参数。
    完。
    若 硬件IRQ 大于或等于 域 的 大小：
        返还 错误_无效参数。
    完。
    若 虚拟IRQ 大于或等于 最大IRQ数：
        返还 错误_无效参数。
    完。
    做 设置 用 域 的 映射表、硬件IRQ、虚拟IRQ。
    若 虚拟IRQ 小于 域 的 大小：
        做 设置 用 域 的 反向映射表、虚拟IRQ、硬件IRQ。
    完。
    desc 为 IRQ表 的 第 虚拟IRQ 项。
    desc 的 域 为 域。
    desc 的 硬件IRQ号 为 硬件IRQ。
    统计_域映射数 为 统计_域映射数 加 1。
    返还 0。
完。

IRQ域_查找虚拟 接受 域、硬件IRQ：
    若 域 等于 空 或者 硬件IRQ 大于或等于 域 的 大小：
        返还 0 减 1。
    完。
    返还 域 的 映射表 的 第 硬件IRQ 项。
完。

IRQ域_查找硬件 接受 域、虚拟IRQ：
    若 域 等于 空 或者 虚拟IRQ 大于或等于 域 的 大小：
        返还 0 减 1。
    完。
    返还 域 的 反向映射表 的 第 虚拟IRQ 项。
完。

IRQ_设置触发类型 接受 IRQ号、类型：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 错误_无效参数。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    做 自旋锁_获取 用 desc 的 锁。
    旧类型 为 desc 的 触发类型。
    desc 的 触发类型 为 类型 位与 IRQF_TRIGGER_MASK。
    若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 设置类型回调 不等于 空：
        做 desc 的 芯片 的 设置类型回调 用 desc、类型 位与 IRQF_TRIGGER_MASK。
    完。
    做 自旋锁_释放 用 desc 的 锁。
    若 旧类型 不等于 desc 的 触发类型：
        统计_触发类型变更数 为 统计_触发类型变更数 加 1。
    完。
    返还 0。
完。

IRQ_获取触发类型 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 IRQF_TRIGGER_NONE。
    完。
    返还 IRQ表 的 第 IRQ号 项 的 触发类型。
完。

IRQ_触发类型名 接受 类型：
    若 类型 等于 IRQF_TRIGGER_NONE：
        返还 「none」。
    完。
    可变 名称 为 「」。
    若 类型 位与 IRQF_TRIGGER_RISING 不等于 0：
        名称 为 名称 加 「rising 」。
    完。
    若 类型 位与 IRQF_TRIGGER_FALLING 不等于 0：
        名称 为 名称 加 「falling 」。
    完。
    若 类型 位与 IRQF_TRIGGER_HIGH 不等于 0：
        名称 为 名称 加 「high 」。
    完。
    若 类型 位与 IRQF_TRIGGER_LOW 不等于 0：
        名称 为 名称 加 「low 」。
    完。
    若 名称 等于 「」：
        返还 「unknown」。
    完。
    返还 名称。
完。

IRQ_设置芯片 接受 IRQ号、芯片：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 错误_无效参数。
    完。
    若 芯片 等于 空：
        返还 错误_无效参数。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    做 自旋锁_获取 用 desc 的 锁。
    desc 的 芯片 为 芯片。
    做 自旋锁_释放 用 desc 的 锁。
    返还 0。
完。

IRQ_设置芯片数据 接受 IRQ号、数据：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 错误_无效参数。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    做 自旋锁_获取 用 desc 的 锁。
    desc 的 芯片数据 为 数据。
    做 自旋锁_释放 用 desc 的 锁。
    返还 0。
完。

IRQ_芯片_应答 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 应答回调 不等于 空：
        做 desc 的 芯片 的 应答回调 用 desc。
    完。
完。

IRQ_芯片_屏蔽 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 屏蔽回调 不等于 空：
        做 desc 的 芯片 的 屏蔽回调 用 desc。
    完。
完。

IRQ_芯片_解屏蔽 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 解屏蔽回调 不等于 空：
        做 desc 的 芯片 的 解屏蔽回调 用 desc。
    完。
完。

IRQ_芯片_结束 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 结束回调 不等于 空：
        做 desc 的 芯片 的 结束回调 用 desc。
    完。
完。

request_irq 接受 IRQ号、处理函数、标志、名称、设备ID：
    返还 做 request_threaded_irq 用 IRQ号、处理函数、空、标志、名称、设备ID。
完。

request_threaded_irq 接受 IRQ号、处理函数、线程函数、标志、名称、设备ID：
    若 IRQ_已初始化 等于 假：
        统计_错误数 为 统计_错误数 加 1。
        返还 错误_未初始化。
    完。
    若 IRQ号 大于或等于 最大IRQ数：
        统计_错误数 为 统计_错误数 加 1。
        返还 错误_无效参数。
    完。
    若 处理函数 等于 空 并且 线程函数 等于 空：
        统计_错误数 为 统计_错误数 加 1。
        返还 错误_无效参数。
    完。
    若 名称 等于 空 或者 名称 等于 「」：
        统计_错误数 为 统计_错误数 加 1。
        返还 错误_无效参数。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    动作 为 新建 IRQ动作。
    动作 的 处理函数 为 处理函数。
    动作 的 线程函数 为 线程函数。
    动作 的 线程ID 为 0。
    动作 的 名称 为 名称。
    动作 的 设备ID 为 设备ID。
    动作 的 标志 为 标志。
    动作 的 下一个 为 空。
    动作 的 触发次数 为 0。
    动作 的 线程触发次数 为 0。
    做 自旋锁_获取 用 desc 的 锁。
    若 desc 的 动作链 不等于 空：
        若 标志 位与 IRQF_SHARED 等于 0：
            做 自旋锁_释放 用 desc 的 锁。
            统计_错误数 为 统计_错误数 加 1。
            返还 错误_忙。
        完。
        若 desc 的 动作链 的 标志 位与 IRQF_SHARED 等于 0：
            做 自旋锁_释放 用 desc 的 锁。
            统计_错误数 为 统计_错误数 加 1。
            返还 错误_忙。
        完。
        新触发 为 标志 位与 IRQF_TRIGGER_MASK。
        若 新触发 不等于 IRQF_TRIGGER_NONE 并且 新触发 不等于 desc 的 触发类型：
            做 自旋锁_释放 用 desc 的 锁。
            统计_错误数 为 统计_错误数 加 1。
            返还 错误_无效参数。
        完。
    完。
    若 desc 的 动作链 等于 空：
        新触发 为 标志 位与 IRQF_TRIGGER_MASK。
        若 新触发 不等于 IRQF_TRIGGER_NONE：
            desc 的 触发类型 为 新触发。
        完。
    完。
    若 desc 的 动作链 等于 空：
        desc 的 动作链 为 动作。
    否则：
        可变 当前 为 desc 的 动作链。
        只要 当前 的 下一个 不等于 空：
            当前 为 当前 的 下一个。
        完。
        当前 的 下一个 为 动作。
        统计_共享IRQ数 为 统计_共享IRQ数 加 1。
    完。
    若 线程函数 不等于 空：
        统计_线程化IRQ数 为 统计_线程化IRQ数 加 1。
    完。
    desc 的 深度 为 0。
    desc 的 状态 为 IRQ状态_启用。
    若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 解屏蔽回调 不等于 空：
        做 desc 的 芯片 的 解屏蔽回调 用 desc。
    完。
    做 自旋锁_释放 用 desc 的 锁。
    统计_总注册数 为 统计_总注册数 加 1。
    做 内核信息 用 「[IRQ] 」 加 做 转字符串 用 IRQ号 加 「 注册: 」 加 名称。
    返还 0。
完。

free_irq 接受 IRQ号、设备ID：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 错误_无效参数。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    做 自旋锁_获取 用 desc 的 锁。
    可变 前一个 为 空。
    可变 当前 为 desc 的 动作链。
    可变 已找到 为 假。
    只要 当前 不等于 空：
        若 当前 的 设备ID 等于 设备ID：
            若 前一个 等于 空：
                desc 的 动作链 为 当前 的 下一个。
            否则：
                前一个 的 下一个 为 当前 的 下一个。
            完。
            已找到 为 真。
            中断。
        完。
        前一个 为 当前。
        当前 为 当前 的 下一个。
    完。
    若 已找到 等于 假：
        做 自旋锁_释放 用 desc 的 锁。
        统计_错误数 为 统计_错误数 加 1。
        返还 错误_不存在。
    完。
    若 desc 的 动作链 等于 空：
        desc 的 状态 为 IRQ状态_禁用。
        desc 的 深度 为 1。
        若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 屏蔽回调 不等于 空：
            做 desc 的 芯片 的 屏蔽回调 用 desc。
        完。
    完。
    做 自旋锁_释放 用 desc 的 锁。
    统计_总注销数 为 统计_总注销数 加 1。
    返还 0。
完。

IRQ_分发 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    统计_总触发数 为 统计_总触发数 加 1。
    desc 的 触发计数 为 desc 的 触发计数 加 1。
    若 desc 的 状态 等于 IRQ状态_禁用 或者 desc 的 状态 等于 IRQ状态_屏蔽：
        返还。
    完。
    若 desc 的 动作链 等于 空：
        desc 的 未处理计数 为 desc 的 未处理计数 加 1。
        统计_总未处理数 为 统计_总未处理数 加 1。
        返还。
    完。
    若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 应答回调 不等于 空：
        做 desc 的 芯片 的 应答回调 用 desc。
    完。
    desc 的 状态 为 IRQ状态_处理中。
    可变 当前 为 desc 的 动作链。
    可变 任一已处理 为 假。
    可变 需要线程 为 假。
    只要 当前 不等于 空：
        若 当前 的 处理函数 不等于 空：
            结果 为 做 当前 的 处理函数 用 IRQ号。
            当前 的 触发次数 为 当前 的 触发次数 加 1。
            若 结果 等于 IRQ_HANDLED：
                任一已处理 为 真。
            否则若 结果 等于 IRQ_WAKE_THREAD：
                任一已处理 为 真。
                需要线程 为 真。
                当前 的 线程触发次数 为 当前 的 线程触发次数 加 1。
            完。
        完。
        当前 为 当前 的 下一个。
    完。
    若 任一已处理 等于 假：
        desc 的 伪中断计数 为 desc 的 伪中断计数 加 1。
        统计_总伪中断数 为 统计_总伪中断数 加 1。
        若 desc 的 伪中断计数 大于 100000：
            desc 的 状态 为 IRQ状态_禁用。
            做 内核警告 用 「[IRQ] 」 加 做 转字符串 用 IRQ号 加 「 伪中断过多, 已自动禁用」。
        完。
    否则：
        统计_总处理数 为 统计_总处理数 加 1。
    完。
    若 需要线程 等于 真：
        desc 的 线程处理计数 为 desc 的 线程处理计数 加 1。
        desc 的 状态 为 IRQ状态_等待线程。
        可变 线程动作 为 desc 的 动作链。
        可变 已执行线程函数 为 假。
        只要 线程动作 不等于 空：
            若 线程动作 的 线程函数 不等于 空：
                做 线程动作 的 线程函数 用 IRQ号。
                线程动作 的 线程触发次数 为 线程动作 的 线程触发次数 加 1。
                已执行线程函数 为 真。
            完。
            线程动作 为 线程动作 的 下一个。
        完。
        desc 的 状态 为 IRQ状态_启用。
    否则：
        desc 的 状态 为 IRQ状态_启用。
    完。
    若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 结束回调 不等于 空：
        做 desc 的 芯片 的 结束回调 用 desc。
    完。
完。

IRQ_启用 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 错误_无效参数。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    做 自旋锁_获取 用 desc 的 锁。
    若 desc 的 深度 大于 0：
        desc 的 深度 为 desc 的 深度 减 1。
    完。
    若 desc 的 深度 等于 0：
        desc 的 状态 为 IRQ状态_启用。
        若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 解屏蔽回调 不等于 空：
            做 desc 的 芯片 的 解屏蔽回调 用 desc。
        完。
    完。
    做 自旋锁_释放 用 desc 的 锁。
    统计_总启用数 为 统计_总启用数 加 1。
    返还 0。
完。

IRQ_禁用_描述符 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 错误_无效参数。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    做 自旋锁_获取 用 desc 的 锁。
    desc 的 深度 为 desc 的 深度 加 1。
    若 desc 的 深度 等于 1：
        desc 的 状态 为 IRQ状态_禁用。
        若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 屏蔽回调 不等于 空：
            做 desc 的 芯片 的 屏蔽回调 用 desc。
        完。
    完。
    做 自旋锁_释放 用 desc 的 锁。
    统计_总禁用数 为 统计_总禁用数 加 1。
    返还 0。
完。

IRQ_禁用_同步 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 错误_无效参数。
    完。
    做 IRQ_禁用_描述符 用 IRQ号。
    desc 为 IRQ表 的 第 IRQ号 项。
    可变 等待次数 为 0。
    只要 desc 的 状态 等于 IRQ状态_处理中 并且 等待次数 小于 10000：
        等待次数 为 等待次数 加 1。
    完。
    若 等待次数 大于或等于 10000：
        做 内核警告 用 「[IRQ] 」 加 做 转字符串 用 IRQ号 加 「 同步禁用超时」。
    完。
    返还 0。
完。

IRQ_设置亲和 接受 IRQ号、CPU掩码：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 错误_无效参数。
    完。
    若 CPU掩码 等于 0：
        返还 错误_无效参数。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    做 自旋锁_获取 用 desc 的 锁。
    desc 的 亲和CPU 为 CPU掩码。
    若 desc 的 状态 等于 IRQ状态_处理中：
        desc 的 挂起亲和CPU 为 CPU掩码。
    否则：
        desc 的 有效亲和CPU 为 CPU掩码。
        若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 设置亲和回调 不等于 空：
            做 desc 的 芯片 的 设置亲和回调 用 desc、CPU掩码。
        完。
    完。
    做 自旋锁_释放 用 desc 的 锁。
    统计_亲和设置数 为 统计_亲和设置数 加 1。
    返还 0。
完。

IRQ_获取亲和 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 0。
    完。
    返还 IRQ表 的 第 IRQ号 项 的 有效亲和CPU。
完。

IRQ_应用挂起亲和 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    若 desc 的 挂起亲和CPU 不等于 0：
        desc 的 有效亲和CPU 为 desc 的 挂起亲和CPU。
        desc 的 挂起亲和CPU 为 0。
        若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 设置亲和回调 不等于 空：
            做 desc 的 芯片 的 设置亲和回调 用 desc、desc 的 有效亲和CPU。
        完。
    完。
完。

IRQ_设置唤醒 接受 IRQ号、启用：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 错误_无效参数。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    做 自旋锁_获取 用 desc 的 锁。
    若 启用 等于 真：
        desc 的 唤醒深度 为 desc 的 唤醒深度 加 1。
    否则：
        若 desc 的 唤醒深度 大于 0：
            desc 的 唤醒深度 为 desc 的 唤醒深度 减 1。
        完。
    完。
    做 自旋锁_释放 用 desc 的 锁。
    返还 0。
完。

IRQ_可唤醒 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 假。
    完。
    返还 IRQ表 的 第 IRQ号 项 的 唤醒深度 大于 0。
完。

IRQ_获取描述符 接受 IRQ号：
    若 IRQ_已初始化 等于 假：
        返还 空。
    完。
    若 IRQ号 大于或等于 最大IRQ数：
        返还 空。
    完。
    返还 IRQ表 的 第 IRQ号 项。
完。

IRQ_获取状态 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 IRQ状态_禁用。
    完。
    返还 IRQ表 的 第 IRQ号 项 的 状态。
完。

IRQ_获取芯片名 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 「none」。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    若 desc 的 芯片 等于 空：
        返还 「none」。
    完。
    返还 desc 的 芯片 的 名称。
完。

IRQ_动作链长度 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 0。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    可变 长度 为 0。
    可变 当前 为 desc 的 动作链。
    只要 当前 不等于 空：
        长度 为 长度 加 1。
        当前 为 当前 的 下一个。
    完。
    返还 长度。
完。

IRQ_状态名 接受 状态：
    若 状态 等于 IRQ状态_禁用：
        返还 「禁用」。
    完。
    若 状态 等于 IRQ状态_启用：
        返还 「启用」。
    完。
    若 状态 等于 IRQ状态_处理中：
        返还 「处理中」。
    完。
    若 状态 等于 IRQ状态_挂起：
        返还 「挂起」。
    完。
    若 状态 等于 IRQ状态_重放：
        返还 「重放」。
    完。
    若 状态 等于 IRQ状态_等待线程：
        返还 「等待线程」。
    完。
    若 状态 等于 IRQ状态_屏蔽：
        返还 「屏蔽」。
    完。
    返还 「未知」。
完。

IRQ_打印统计_描述符 接受 零参：
    可变 输出 为 「           CPU0       芯片          触发类型  名称\n」。
    可变 i 为 0。
    只要 i 小于 最大IRQ数：
        desc 为 IRQ表 的 第 i 项。
        若 desc 的 触发计数 大于 0 或者 desc 的 动作链 不等于 空：
            名称 为 「」。
            若 desc 的 动作链 不等于 空：
                名称 为 desc 的 动作链 的 名称。
            完。
            芯片名 为 「none」。
            若 desc 的 芯片 不等于 空：
                芯片名 为 desc 的 芯片 的 名称。
            完。
            输出 为 输出 加 「  」 加 做 转字符串 用 i 加 「:    」。
            输出 为 输出 加 做 转字符串 用 desc 的 触发计数 加 「    」。
            输出 为 输出 加 芯片名 加 「    」。
            输出 为 输出 加 做 IRQ_触发类型名 用 desc 的 触发类型 加 「    」。
            输出 为 输出 加 名称 加 「\n」。
        完。
        i 为 i 加 1。
    完。
    输出 为 输出 加 「合计触发: 」 加 做 转字符串 用 统计_总触发数 加 「\n」。
    输出 为 输出 加 「伪中断:   」 加 做 转字符串 用 统计_总伪中断数 加 「\n」。
    返还 输出。
完。

IRQ_打印统计_字符串 接受 零参：
    返还 做 IRQ_打印统计_描述符。
完。

IRQ描述符_完整统计 接受 零参：
    可变 已注册数 为 0。
    可变 已启用数 为 0。
    可变 已禁用数 为 0。
    可变 最热IRQ 为 0。
    可变 最热计数 为 0。
    可变 i 为 0。
    只要 i 小于 最大IRQ数：
        desc 为 IRQ表 的 第 i 项。
        若 desc 的 动作链 不等于 空：
            已注册数 为 已注册数 加 1。
        完。
        若 desc 的 状态 等于 IRQ状态_启用：
            已启用数 为 已启用数 加 1。
        完。
        若 desc 的 状态 等于 IRQ状态_禁用 并且 desc 的 深度 大于 0：
            已禁用数 为 已禁用数 加 1。
        完。
        若 desc 的 触发计数 大于 最热计数：
            最热计数 为 desc 的 触发计数。
            最热IRQ 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 做 字典_创建_统计 用 已注册数、已启用数、已禁用数、最热IRQ、最热计数。
完。

字典_创建_统计 接受 已注册数、已启用数、已禁用数、最热IRQ、最热计数：
    统计 为 做 字典。
    做 字典设 用 统计、「总触发数」、统计_总触发数。
    做 字典设 用 统计、「总处理数」、统计_总处理数。
    做 字典设 用 统计、「总未处理数」、统计_总未处理数。
    做 字典设 用 统计、「总伪中断数」、统计_总伪中断数。
    做 字典设 用 统计、「总注册数」、统计_总注册数。
    做 字典设 用 统计、「总注销数」、统计_总注销数。
    做 字典设 用 统计、「总启用操作」、统计_总启用数。
    做 字典设 用 统计、「总禁用操作」、统计_总禁用数。
    做 字典设 用 统计、「共享IRQ数」、统计_共享IRQ数。
    做 字典设 用 统计、「线程化IRQ数」、统计_线程化IRQ数。
    做 字典设 用 统计、「亲和设置数」、统计_亲和设置数。
    做 字典设 用 统计、「触发类型变更数」、统计_触发类型变更数。
    做 字典设 用 统计、「芯片注册数」、统计_芯片注册数。
    做 字典设 用 统计、「域映射数」、统计_域映射数。
    做 字典设 用 统计、「错误数」、统计_错误数。
    做 字典设 用 统计、「当前已注册」、已注册数。
    做 字典设 用 统计、「当前已启用」、已启用数。
    做 字典设 用 统计、「当前已禁用」、已禁用数。
    做 字典设 用 统计、「最热IRQ号」、最热IRQ。
    做 字典设 用 统计、「最热触发数」、最热计数。
    做 字典设 用 统计、「最大IRQ数」、最大IRQ数。
    做 字典设 用 统计、「域数量」、域数量。
    返还 统计。
完。

IRQ描述符_打印完整统计 接受 零参：
    统计 为 做 IRQ描述符_完整统计。
    做 打印 用 「[IRQ描述符] ═══════ 完整统计 ═══════」。
    做 打印 用 「  总触发数:       」 加 做 转字符串 用 做 字典取 用 统计、「总触发数」。
    做 打印 用 「  总处理数:       」 加 做 转字符串 用 做 字典取 用 统计、「总处理数」。
    做 打印 用 「  总未处理数:     」 加 做 转字符串 用 做 字典取 用 统计、「总未处理数」。
    做 打印 用 「  总伪中断数:     」 加 做 转字符串 用 做 字典取 用 统计、「总伪中断数」。
    做 打印 用 「  总注册数:       」 加 做 转字符串 用 做 字典取 用 统计、「总注册数」。
    做 打印 用 「  总注销数:       」 加 做 转字符串 用 做 字典取 用 统计、「总注销数」。
    做 打印 用 「  共享IRQ数:      」 加 做 转字符串 用 做 字典取 用 统计、「共享IRQ数」。
    做 打印 用 「  线程化IRQ数:    」 加 做 转字符串 用 做 字典取 用 统计、「线程化IRQ数」。
    做 打印 用 「  当前已注册:     」 加 做 转字符串 用 做 字典取 用 统计、「当前已注册」。
    做 打印 用 「  当前已启用:     」 加 做 转字符串 用 做 字典取 用 统计、「当前已启用」。
    做 打印 用 「  最热IRQ:        」 加 做 转字符串 用 做 字典取 用 统计、「最热IRQ号」。
    做 打印 用 「  最热触发数:     」 加 做 转字符串 用 做 字典取 用 统计、「最热触发数」。
    做 打印 用 「  错误数:         」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「  IRQ域数量:      」 加 做 转字符串 用 做 字典取 用 统计、「域数量」。
    做 打印 用 「[IRQ描述符] ═══════════════════════════」。
    返还 0。
完。

IRQ_验证描述符 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还 假。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    若 desc 的 深度 大于 0 并且 desc 的 状态 等于 IRQ状态_启用：
        做 内核警告 用 「[IRQ验证] 」 加 做 转字符串 用 IRQ号 加 「 深度>0但状态为启用」。
        返还 假。
    完。
    若 desc 的 深度 等于 0 并且 desc 的 状态 等于 IRQ状态_禁用 并且 desc 的 动作链 不等于 空：
        做 内核警告 用 「[IRQ验证] 」 加 做 转字符串 用 IRQ号 加 「 深度=0有handler但禁用」。
        返还 假。
    完。
    返还 真。
完。

IRQ_验证全部 接受 零参：
    可变 错误总数 为 0。
    可变 i 为 0。
    只要 i 小于 最大IRQ数：
        若 做 IRQ_验证描述符 用 i 等于 假：
            错误总数 为 错误总数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 错误总数 大于 0：
        做 内核警告 用 「[IRQ验证] 发现 」 加 做 转字符串 用 错误总数 加 「 个不一致」。
    否则：
        做 内核信息 用 「[IRQ验证] 全部通过」。
    完。
    返还 错误总数。
完。

IRQ_dump单个 接受 IRQ号：
    若 IRQ号 大于或等于 最大IRQ数：
        返还。
    完。
    desc 为 IRQ表 的 第 IRQ号 项。
    做 打印 用 「[IRQ dump] IRQ=」 加 做 转字符串 用 IRQ号。
    做 打印 用 「  硬件IRQ=」 加 做 转字符串 用 desc 的 硬件IRQ号。
    做 打印 用 「  状态=」 加 做 IRQ_状态名 用 desc 的 状态。
    做 打印 用 「  触发类型=」 加 做 IRQ_触发类型名 用 desc 的 触发类型。
    做 打印 用 「  触发计数=」 加 做 转字符串 用 desc 的 触发计数。
    做 打印 用 「  未处理=」 加 做 转字符串 用 desc 的 未处理计数。
    做 打印 用 「  伪中断=」 加 做 转字符串 用 desc 的 伪中断计数。
    做 打印 用 「  线程处理=」 加 做 转字符串 用 desc 的 线程处理计数。
    做 打印 用 「  深度=」 加 做 转字符串 用 desc 的 深度。
    做 打印 用 「  handler数=」 加 做 转字符串 用 做 IRQ_动作链长度 用 IRQ号。
    做 打印 用 「  芯片=」 加 做 IRQ_获取芯片名 用 IRQ号。
    做 打印 用 「  唤醒深度=」 加 做 转字符串 用 desc 的 唤醒深度。
完。

IRQ描述符_销毁全部 接受 零参：
    若 IRQ_已初始化 等于 假：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 最大IRQ数：
        desc 为 IRQ表 的 第 i 项。
        若 desc 的 动作链 不等于 空：
            desc 的 状态 为 IRQ状态_禁用。
            desc 的 动作链 为 空。
            若 desc 的 芯片 不等于 空 并且 desc 的 芯片 的 屏蔽回调 不等于 空：
                做 desc 的 芯片 的 屏蔽回调 用 desc。
            完。
        完。
        i 为 i 加 1。
    完。
    IRQ表 为 一组：（空）。
    IRQ域表 为 一组：（空）。
    域数量 为 0。
    IRQ_已初始化 为 假。
    做 内核信息 用 「[IRQ描述符] 已销毁」。
    返还 0。
完。

IRQ_活跃列表 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大IRQ数：
        若 IRQ表 的 第 i 项 的 触发计数 大于 0 或者 IRQ表 的 第 i 项 的 动作链 不等于 空：
            做 追加 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

IRQ_获取所有已注册名称 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大IRQ数：
        desc 为 IRQ表 的 第 i 项。
        若 desc 的 动作链 不等于 空：
            可变 当前 为 desc 的 动作链。
            只要 当前 不等于 空：
                做 追加 用 结果、一组：i、当前 的 名称。
                当前 为 当前 的 下一个。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

IRQ_为调度器提供负载信息 接受 零参：
    可变 负载表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大IRQ数：
        desc 为 IRQ表 的 第 i 项。
        若 desc 的 触发计数 大于 0 并且 desc 的 状态 等于 IRQ状态_启用：
            做 追加 用 负载表、一组：i、desc 的 触发计数、desc 的 有效亲和CPU。
        完。
        i 为 i 加 1。
    完。
    返还 负载表。
完。

IRQ_为电源管理提供唤醒列表 接受 零参：
    可变 唤醒列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大IRQ数：
        若 IRQ表 的 第 i 项 的 唤醒深度 大于 0：
            做 追加 用 唤醒列表、i。
        完。
        i 为 i 加 1。
    完。
    返还 唤醒列表。
完。

可变 AI_中断请求描_风暴阈值 为 10000。

可变 AI_中断请求描_预测IRQ 为 0。

AI_中断请求描_风暴检测 接受 触发频率：
    若 触发频率 大于 AI_中断请求描_风暴阈值：
        AI_中断请求描_风暴阈值 为 AI_中断请求描_风暴阈值 乘 3 除以 2。
        返还 真。
    完。
    返还 假。
完。

AI_中断请求描_自适应合并 接受 当前延迟：
    若 当前延迟 小于 100：
        返还 16。
    完。
    若 当前延迟 小于 500：
        返还 8。
    完。
    返还 1。
完。

本章 收束。
