《字符串工具》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·字符串替换》。

引用《..·感知·内核直连层》。

可变 操作总数 为 0。

可变 分配字节 为 0。

可变 释放字节 为 0。

可变 最大驻留 为 0。

字串_长度 接受 指针：
    若 指针 等于 0：
        返还 0。
    完。
    可变 计 为 0。
    只要 做 内存_读8 用 指针 加 计 不等于 0：
        计 为 计 加 1。
    完。
    操作总数 为 操作总数 加 1。
    返还 计。
完。

字串_比较 接受 甲、乙：
    若 甲 等于 乙：
        返还 0。
    完。
    若 甲 等于 0：
        返还 负 1。
    完。
    若 乙 等于 0：
        返还 1。
    完。
    可变 位 为 0。
    只要 做 内存_读8 用 甲 加 位 不等于 0 并且 做 内存_读8 用 乙 加 位 不等于 0：
        差 为 做 内存_读8 用 甲 加 位 减 做 内存_读8 用 乙 加 位。
        若 差 不等于 0：
            返还 差。
        完。
        位 为 位 加 1。
    完。
    操作总数 为 操作总数 加 1。
    返还 做 内存_读8 用 甲 加 位 减 做 内存_读8 用 乙 加 位。
完。

字串_复制 接受 目标、源、最大长：
    若 目标 等于 0 或者 源 等于 0：
        返还 0。
    完。
    可变 位 为 0。
    只要 位 小于 最大长 减 1 并且 做 内存_读8 用 源 加 位 不等于 0：
        做 内存_写8 用 目标 加 位、做 内存_读8 用 源 加 位。
        位 为 位 加 1。
    完。
    做 内存_写8 用 目标 加 位、0。
    操作总数 为 操作总数 加 1。
    返还 位。
完。

字串_查找字节 接受 指针、长度、目标字节：
    可变 位 为 0。
    只要 位 小于 长度：
        若 做 内存_读8 用 指针 加 位 等于 目标字节：
            返还 位。
        完。
        位 为 位 加 1。
    完。
    返还 负 1。
完。

字串_查找子串 接受 主串、主长、子串、子长：
    若 子长 等于 0：
        返还 0。
    完。
    若 子长 大于 主长：
        返还 负 1。
    完。
    可变 位 为 0。
    只要 位 小于或等于 主长 减 子长：
        可变 匹配 为 0。
        可变 全匹配 为 真。
        只要 匹配 小于 子长：
            若 做 内存_读8 用 主串 加 位 加 匹配 不等于 做 内存_读8 用 子串 加 匹配：
                全匹配 为 假。
                跳出。
            完。
            匹配 为 匹配 加 1。
        完。
        若 全匹配：
            操作总数 为 操作总数 加 1。
            返还 位。
        完。
        位 为 位 加 1。
    完。
    返还 负 1。
完。

字串_转大写 接受 指针、长度：
    可变 位 为 0。
    只要 位 小于 长度：
        字节 为 做 内存_读8 用 指针 加 位。
        若 字节 大于或等于 97 并且 字节 小于或等于 122：
            做 内存_写8 用 指针 加 位、字节 减 32。
        完。
        位 为 位 加 1。
    完。
完。

字串_转小写 接受 指针、长度：
    可变 位 为 0。
    只要 位 小于 长度：
        字节 为 做 内存_读8 用 指针 加 位。
        若 字节 大于或等于 65 并且 字节 小于或等于 90：
            做 内存_写8 用 指针 加 位、字节 加 32。
        完。
        位 为 位 加 1。
    完。
完。

字串_去首尾空白 接受 指针、长度：
    可变 起始 为 0。
    只要 起始 小于 长度：
        字节 为 做 内存_读8 用 指针 加 起始。
        若 字节 不等于 32 并且 字节 不等于 9 并且 字节 不等于 10 并且 字节 不等于 13：
            跳出。
        完。
        起始 为 起始 加 1。
    完。
    可变 结束 为 长度。
    只要 结束 大于 起始：
        字节 为 做 内存_读8 用 指针 加 结束 减 1。
        若 字节 不等于 32 并且 字节 不等于 9 并且 字节 不等于 10 并且 字节 不等于 13：
            跳出。
        完。
        结束 为 结束 减 1。
    完。
    返还 一组：起始、结束 减 起始。
完。

字串_是数字 接受 字节：
    返还 字节 大于或等于 48 并且 字节 小于或等于 57。
完。

字串_是字母 接受 字节：
    返还 字节 大于或等于 65 并且 字节 小于或等于 90 或者 字节 大于或等于 97 并且 字节 小于或等于 122。
完。

字串_是空白 接受 字节：
    返还 字节 等于 32 或者 字节 等于 9 或者 字节 等于 10 或者 字节 等于 13。
完。

字串_转整数 接受 指针、长度：
    可变 结果 为 0。
    可变 负号 为 假。
    可变 位 为 0。
    若 位 小于 长度 并且 做 内存_读8 用 指针 加 位 等于 45：
        负号 为 真。
        位 为 位 加 1。
    完。
    只要 位 小于 长度：
        字节 为 做 内存_读8 用 指针 加 位。
        若 非 做 字串_是数字 用 字节：
            跳出。
        完。
        结果 为 结果 乘 10 加 (字节 减 48)。
        位 为 位 加 1。
    完。
    若 负号：
        结果 为 0 减 结果。
    完。
    返还 结果。
完。

字串_哈希 接受 指针、长度：
    可变 哈希值 为 5381。
    可变 位 为 0。
    只要 位 小于 长度：
        哈希值 为 哈希值 乘 33 加 做 内存_读8 用 指针 加 位。
        位 为 位 加 1。
    完。
    返还 哈希值 位与 0x7FFFFFFF。
完。

字串_统计字节 接受 指针、长度、目标：
    可变 计 为 0。
    可变 位 为 0。
    只要 位 小于 长度：
        若 做 内存_读8 用 指针 加 位 等于 目标：
            计 为 计 加 1。
        完。
        位 为 位 加 1。
    完。
    返还 计。
完。

字串_反转 接受 指针、长度：
    可变 左 为 0。
    可变 右 为 长度 减 1。
    只要 左 小于 右：
        临时 为 做 内存_读8 用 指针 加 左。
        做 内存_写8 用 指针 加 左、做 内存_读8 用 指针 加 右。
        做 内存_写8 用 指针 加 右、临时。
        左 为 左 加 1。
        右 为 右 减 1。
    完。
完。

编码_ASCII 为 0。

编码_UTF8 为 1。

编码_GBK 为 2。

编码_未知 为 3。

AI分析窗口 为 16。

工具统计 是 一种 事物，含 长度调用数、比较调用数、复制调用数、查找字节数、查找子串数、转大写数、转小写数、去空白数、哈希计算数、转整数数、统计字节数、反转数、编码检测数、模式匹配数、AI分析次数、AI分析命中、错误次数。

可变 全局统计 为 新建 工具统计。

可变 工具模块已初始化 为 假。

可变 AI长度历史 为 一组：（空）。

可变 AI长度位置 为 0。

工具模块_初始化 接受 零参：
    若 工具模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI长度历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI分析窗口：
        做 追加 用 AI长度历史、0。
        i 为 i 加 1。
    完。
    AI长度位置 为 0。
    工具模块已初始化 为 真。
    做 打印 用 「[字符串工具] 初始化完成」。
    返还 0。
完。

工具模块_销毁 接受 零参：
    若 工具模块已初始化 等于 假：
        返还 0。
    完。
    AI长度历史 为 一组：（空）。
    工具模块已初始化 为 假。
    做 打印 用 「[字符串工具] 已销毁」。
    返还 0。
完。

字串_安全长度 接受 指针：
    全局统计 的 长度调用数 为 全局统计 的 长度调用数 加 1。
    长度 为 做 字串_长度 用 指针。
    做 AI_记录长度 用 长度。
    返还 长度。
完。

字串_安全比较 接受 甲、乙：
    全局统计 的 比较调用数 为 全局统计 的 比较调用数 加 1。
    返还 做 字串_比较 用 甲、乙。
完。

字串_安全复制 接受 目标、源、最大长：
    全局统计 的 复制调用数 为 全局统计 的 复制调用数 加 1。
    返还 做 字串_复制 用 目标、源、最大长。
完。

字串_检测编码 接受 指针、长度：
    若 指针 等于 0 或者 长度 等于 0：
        返还 编码_未知。
    完。
    全局统计 的 编码检测数 为 全局统计 的 编码检测数 加 1。
    可变 高位数 为 0。
    可变 utf8序列数 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        字节 为 做 内存_读8 用 指针 加 i。
        若 字节 大于 127：
            高位数 为 高位数 加 1。
            若 字节 位与 0xE0 等于 0xC0：
                若 i 加 1 小于 长度 并且 做 内存_读8 用 指针 加 i 加 1 位与 0xC0 等于 0x80：
                    utf8序列数 为 utf8序列数 加 1。
                    i 为 i 加 2。
                    继续。
                完。
            否则若 字节 位与 0xF0 等于 0xE0：
                若 i 加 2 小于 长度：
                    若 做 内存_读8 用 指针 加 i 加 1 位与 0xC0 等于 0x80 并且 做 内存_读8 用 指针 加 i 加 2 位与 0xC0 等于 0x80：
                        utf8序列数 为 utf8序列数 加 1。
                        i 为 i 加 3。
                        继续。
                    完。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    若 高位数 等于 0：
        返还 编码_ASCII。
    完。
    若 utf8序列数 大于 0 并且 utf8序列数 乘 3 大于或等于 高位数：
        返还 编码_UTF8。
    完。
    若 高位数 大于 0：
        返还 编码_GBK。
    完。
    返还 编码_未知。
完。

编码名称 接受 编码：
    若 编码 等于 编码_ASCII：
        返还 「ASCII」。
    完。
    若 编码 等于 编码_UTF8：
        返还 「UTF-8」。
    完。
    若 编码 等于 编码_GBK：
        返还 「GBK」。
    完。
    返还 「未知」。
完。

字串_通配符匹配 接受 文本指针、文本长、模式指针、模式长：
    全局统计 的 模式匹配数 为 全局统计 的 模式匹配数 加 1。
    返还 做 _通配符递归 用 文本指针、0、文本长、模式指针、0、模式长。
完。

_通配符递归 接受 文本、文位、文长、模式、模位、模长：
    若 模位 大于或等于 模长：
        返还 文位 大于或等于 文长。
    完。
    模字节 为 做 内存_读8 用 模式 加 模位。
    若 模字节 等于 42：
        可变 i 为 文位。
        只要 i 小于或等于 文长：
            若 做 _通配符递归 用 文本、i、文长、模式、模位 加 1、模长 等于 真：
                返还 真。
            完。
            i 为 i 加 1。
        完。
        返还 假。
    完。
    若 文位 大于或等于 文长：
        返还 假。
    完。
    若 模字节 等于 63 或者 模字节 等于 做 内存_读8 用 文本 加 文位：
        返还 做 _通配符递归 用 文本、文位 加 1、文长、模式、模位 加 1、模长。
    完。
    返还 假。
完。

字串_批量比较 接受 指针列表、目标：
    若 指针列表 等于 空 或者 目标 等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 指针列表：
        比较结果 为 做 字串_比较 用 指针列表 的 第 i 项、目标。
        做 追加 用 结果、一组：指针列表 的 第 i 项、比较结果。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字串_N比较 接受 甲、乙、n：
    若 甲 等于 0 或者 乙 等于 0：
        返还 0 减 1。
    完。
    可变 位 为 0。
    只要 位 小于 n：
        差 为 做 内存_读8 用 甲 加 位 减 做 内存_读8 用 乙 加 位。
        若 差 不等于 0：
            返还 差。
        完。
        若 做 内存_读8 用 甲 加 位 等于 0：
            返还 0。
        完。
        位 为 位 加 1。
    完。
    返还 0。
完。

字串_拼接 接受 目标、目标最大、源：
    若 目标 等于 0 或者 源 等于 0：
        返还 0。
    完。
    目标长 为 做 字串_长度 用 目标。
    源长 为 做 字串_长度 用 源。
    可变 复制长 为 源长。
    若 目标长 加 源长 大于或等于 目标最大：
        复制长 为 目标最大 减 目标长 减 1。
    完。
    若 复制长 小于或等于 0：
        返还 目标长。
    完。
    可变 i 为 0。
    只要 i 小于 复制长：
        做 内存_写8 用 目标 加 目标长 加 i、做 内存_读8 用 源 加 i。
        i 为 i 加 1。
    完。
    做 内存_写8 用 目标 加 目标长 加 复制长、0。
    返还 目标长 加 复制长。
完。

字串_重复字节 接受 目标、字节、次数：
    若 目标 等于 0 或者 次数 小于或等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 次数：
        做 内存_写8 用 目标 加 i、字节。
        i 为 i 加 1。
    完。
    做 内存_写8 用 目标 加 次数、0。
完。

AI_记录长度 接受 长度：
    做 设置 用 AI长度历史、AI长度位置、长度。
    AI长度位置 为 (AI长度位置 加 1) 取余于 AI分析窗口。
    全局统计 的 AI分析次数 为 全局统计 的 AI分析次数 加 1。
完。

AI_平均字符串长度 接受 零参：
    可变 总和 为 0。
    可变 计数 为 0。
    可变 i 为 0。
    只要 i 小于 AI分析窗口：
        若 AI长度历史 的 第 i 项 大于 0：
            总和 为 总和 加 AI长度历史 的 第 i 项。
            计数 为 计数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 计数 等于 0：
        返还 0。
    完。
    返还 总和 除以 计数。
完。

AI_最大字符串长度 接受 零参：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 AI分析窗口：
        若 AI长度历史 的 第 i 项 大于 最大：
            最大 为 AI长度历史 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

AI_建议缓冲区大小 接受 零参：
    均长 为 做 AI_平均字符串长度。
    最大 为 做 AI_最大字符串长度。
    建议1 为 均长 乘 2。
    建议2 为 最大 乘 150 除以 100。
    若 建议1 大于 建议2：
        返还 建议1。
    完。
    返还 建议2。
完。

字符串工具_转储 接受 零参：
    做 打印 用 「[字符串工具] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  操作总数: 」、做 转字符串 用 操作总数。
    做 打印 用 做 字符串拼接 用 「  AI平均长度: 」、做 转字符串 用 做 AI_平均字符串长度。
    做 打印 用 做 字符串拼接 用 「  AI最大长度: 」、做 转字符串 用 做 AI_最大字符串长度。
    做 打印 用 做 字符串拼接 用 「  AI建议缓冲: 」、做 转字符串 用 做 AI_建议缓冲区大小。
    返还 0。
完。

字符串工具_完整统计 接受 零参：
    返还 一组：操作总数、分配字节、释放字节、最大驻留。
完。

工具模块_打印统计 接受 零参：
    做 打印 用 「[字符串工具] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  长度调用: 」、做 转字符串 用 全局统计 的 长度调用数。
    做 打印 用 做 字符串拼接 用 「  比较调用: 」、做 转字符串 用 全局统计 的 比较调用数。
    做 打印 用 做 字符串拼接 用 「  复制调用: 」、做 转字符串 用 全局统计 的 复制调用数。
    做 打印 用 做 字符串拼接 用 「  查找字节: 」、做 转字符串 用 全局统计 的 查找字节数。
    做 打印 用 做 字符串拼接 用 「  查找子串: 」、做 转字符串 用 全局统计 的 查找子串数。
    做 打印 用 做 字符串拼接 用 「  转大写: 」、做 转字符串 用 全局统计 的 转大写数。
    做 打印 用 做 字符串拼接 用 「  转小写: 」、做 转字符串 用 全局统计 的 转小写数。
    做 打印 用 做 字符串拼接 用 「  去空白: 」、做 转字符串 用 全局统计 的 去空白数。
    做 打印 用 做 字符串拼接 用 「  哈希计算: 」、做 转字符串 用 全局统计 的 哈希计算数。
    做 打印 用 做 字符串拼接 用 「  转整数: 」、做 转字符串 用 全局统计 的 转整数数。
    做 打印 用 做 字符串拼接 用 「  统计字节: 」、做 转字符串 用 全局统计 的 统计字节数。
    做 打印 用 做 字符串拼接 用 「  反转: 」、做 转字符串 用 全局统计 的 反转数。
    做 打印 用 做 字符串拼接 用 「  编码检测: 」、做 转字符串 用 全局统计 的 编码检测数。
    做 打印 用 做 字符串拼接 用 「  模式匹配: 」、做 转字符串 用 全局统计 的 模式匹配数。
    做 打印 用 做 字符串拼接 用 「  AI分析: 」、做 转字符串 用 全局统计 的 AI分析次数。
    做 打印 用 做 字符串拼接 用 「  AI命中: 」、做 转字符串 用 全局统计 的 AI分析命中。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

工具模块_注册到内核 接受 内核上下文：
    若 工具模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[字符串工具] 已注册到内核上下文」。
    返还 0。
完。

工具模块_查询状态 接受 零参：
    若 工具模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

工具模块_健康检查 接受 零参：
    若 工具模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 操作总数：
        返还 假。
    完。
    返还 真。
完。

字串_是否为空 接受 指针：
    若 指针 等于 0：
        返还 真。
    完。
    返还 做 内存_读8 用 指针 等于 0。
完。

字串_相等 接受 甲、乙：
    返还 做 字串_比较 用 甲、乙 等于 0。
完。

字串_N相等 接受 甲、乙、n：
    返还 做 字串_N比较 用 甲、乙、n 等于 0。
完。

字串_填充零 接受 指针、长度：
    若 指针 等于 0 或者 长度 小于或等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 长度：
        做 内存_写8 用 指针 加 i、0。
        i 为 i 加 1。
    完。
完。

本章 收束。
