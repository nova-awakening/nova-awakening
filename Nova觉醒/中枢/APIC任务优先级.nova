《APIC任务优先级》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·检查点系统》。

引用《..·执行·内核界面》。

引用《.·APIC初始化》。

引用《.·字符串替换》。

引用《..·引导·端口IO》。

引用《.·时钟中枢》。

引用《.·安全窗口检》。

引用《.·APIC寄存器读》。

引用《.·APIC_IPI通讯》。

引用《..·引导·串口早期输》。

引用《.·自动扩容》。

引用《.·页表重建》。

TPR_接受全部 为 0x00。

TPR_阻断低于16 为 0x10。

TPR_阻断低于32 为 0x20。

TPR_阻断低于48 为 0x30。

TPR_阻断低于64 为 0x40。

TPR_阻断低于128 为 0x80。

TPR_阻断低于192 为 0xC0。

TPR_阻断全部 为 0xFF。

可变 接口C_定时器_已启动 为 假。

可变 定时器_间隔毫秒 为 10。

可变 定时器_间隔微秒 为 10000。

可变 TPR_保存栈 为 一组：（空）。

TPR_最大嵌套 为 16。

可变 TPR_设置次数 为 0。

可变 TPR_屏蔽次数 为 0。

可变 TPR_恢复次数 为 0。

可变 TPR_嵌套溢出次数 为 0。

可变 TPR_错误数 为 0。

接口C_设置任务优先级 接受 优先级：
    若 优先级 大于 255：
        TPR_错误数 为 TPR_错误数 加 1。
        返还 错误_无效参数。
    完。
    做 接口C_写 用 接口C_TPR、优先级 位与 0xFF。
    TPR_设置次数 为 TPR_设置次数 加 1。
    返还 0。
完。

接口C_获取任务优先级 接受 零参：
    返还 做 接口C_读 用 接口C_TPR 位与 0xFF。
完。

接口C_禁止所有中断 接受 零参：
    做 接口C_设置任务优先级 用 TPR_阻断全部。
    TPR_屏蔽次数 为 TPR_屏蔽次数 加 1。
完。

接口C_允许所有中断 接受 零参：
    做 接口C_设置任务优先级 用 TPR_接受全部。
    TPR_恢复次数 为 TPR_恢复次数 加 1。
完。

接口C_设置最低优先级 接受 级别：
    若 级别 大于 15：
        返还 错误_无效参数。
    完。
    返还 做 接口C_设置任务优先级 用 级别 左移 4。
完。

接口C_TPR_进入临界区 接受 新优先级：
    若 做 计数 用 TPR_保存栈 大于或等于 TPR_最大嵌套：
        TPR_嵌套溢出次数 为 TPR_嵌套溢出次数 加 1。
        TPR_错误数 为 TPR_错误数 加 1。
        返还 错误_溢出。
    完。
    旧TPR 为 做 接口C_获取任务优先级。
    做 追加 用 TPR_保存栈、旧TPR。
    若 新优先级 大于 旧TPR：
        做 接口C_设置任务优先级 用 新优先级。
    完。
    返还 0。
完。

接口C_TPR_退出临界区 接受 零参：
    若 做 计数 用 TPR_保存栈 等于 0：
        TPR_错误数 为 TPR_错误数 加 1。
        返还 错误_无效参数。
    完。
    栈顶 为 做 计数 用 TPR_保存栈 减 1。
    旧TPR 为 TPR_保存栈 的 第 栈顶 项。
    做 删除 用 TPR_保存栈、栈顶。
    做 接口C_设置任务优先级 用 旧TPR。
    返还 0。
完。

接口C_TPR名 接受 优先级：
    若 优先级 等于 TPR_接受全部：
        返还 「全部接受」。
    完。
    若 优先级 等于 TPR_阻断全部：
        返还 「全部阻断」。
    完。
    返还 「阻断<」 加 做 转字符串 用 优先级。
完。

接口C_定时器_初始化 接受 间隔毫秒：
    若 接口C_定时器_已启动 等于 真：
        做 打印 用 「[APIC定时器] 警告: 定时器已启动, 先停止」。
        做 接口C_停止定时器。
    完。
    若 间隔毫秒 小于或等于 0 或者 间隔毫秒 大于 1000：
        TPR_错误数 为 TPR_错误数 加 1。
        返还 错误_无效参数。
    完。
    做 接口C_校准定时器。
    做 接口C_设置周期定时器_毫秒 用 向量_接口C定时器、间隔毫秒。
    定时器_间隔毫秒 为 间隔毫秒。
    定时器_间隔微秒 为 间隔毫秒 乘 1000。
    接口C_定时器_已启动 为 真。
    定时器_tick计数 为 0。
    定时器_秒计数 为 0。
    做 早期打印OK 用 「[APIC定时器] 已启动」。
    做 早期打印数值 用 「  间隔(ms)」、间隔毫秒。
    返还 0。
完。

PIT_数据_通道2 为 0x42。

PIT_命令 为 0x43。

PIT_控制_通道2 为 0x61。

PIT_频率 为 1193182。

PIT_延迟_计数 接受 计数：
    控制旧 为 做 端口读8 用 PIT_控制_通道2。
    做 端口写8 用 PIT_控制_通道2、控制旧 位与 0xFD 位或 0x01。
    做 端口写8 用 PIT_命令、0xB0。
    做 端口写8 用 PIT_数据_通道2、计数 位与 0xFF。
    做 端口写8 用 PIT_数据_通道2、计数 右移 8 位与 0xFF。
    可变 等待 为 0。
    只要 做 端口读8 用 PIT_控制_通道2 位与 0x20 等于 0：
        做 暂停。
        等待 为 等待 加 1。
        若 等待 大于 10000000：
            做 打印 用 「[PIT] 警告: 延迟超时」。
            返还。
        完。
    完。
完。

PIT_延迟_10毫秒 接受 零参：
    做 PIT_延迟_计数 用 PIT_频率 除以 100。
完。

PIT_延迟_1毫秒 接受 零参：
    做 PIT_延迟_计数 用 PIT_频率 除以 1000。
完。

PIT_延迟_100毫秒 接受 零参：
    做 PIT_延迟_计数 用 PIT_频率 除以 10。
完。

PIT_延迟_微秒 接受 微秒：
    若 微秒 小于或等于 0：
        返还。
    完。
    计数 为 PIT_频率 乘 微秒 除以 1000000。
    若 计数 等于 0：
        计数 为 1。
    完。
    若 计数 大于 65535：
        计数 为 65535。
    完。
    做 PIT_延迟_计数 用 计数。
完。

暂停 接受 零参：
    汇编。
    块：
        「pause」。
    完。
完。

可变 定时器_tick计数 为 0。

可变 定时器_秒计数 为 0。

可变 定时器_uptime_ms 为 0。

可变 定时器_负载窗口tick 为 0。

可变 定时器_负载窗口开始 为 0。

可变 定时器_每秒tick 为 0。

可变 watchdog_最后tick 为 0。

可变 watchdog_超时tick 为 500。

可变 watchdog_触发次数 为 0。

可变 watchdog_已启用 为 假。

接口C定时器_处理 接受 零参：
    定时器_tick计数 为 定时器_tick计数 加 1。
    定时器_uptime_ms 为 定时器_uptime_ms 加 定时器_间隔毫秒。
    定时器_负载窗口tick 为 定时器_负载窗口tick 加 1。
    每秒tick数 为 1000 除以 定时器_间隔毫秒。
    若 每秒tick数 等于 0：
        每秒tick数 为 100。
    完。
    若 定时器_tick计数 取余于 每秒tick数 等于 0：
        定时器_秒计数 为 定时器_秒计数 加 1。
        定时器_每秒tick 为 定时器_负载窗口tick。
        定时器_负载窗口tick 为 0。
    完。
    做 定时器_滴答。
    做 高精度定时器_检查。
    若 watchdog_已启用 等于 真：
        做 watchdog_tick。
    完。
    做 热更新_watchdog_tick。
    做 调度器_tick 用 定时器_间隔微秒。
    软中断状态 的 挂起 为 软中断状态 的 挂起 位或 1 左移 软中断_定时器。
完。

接口C错误_处理 接受 零参：
    错误 为 做 接口C_读取错误。
    若 错误 不等于 0：
        做 内核警告 用 「[APIC] 错误: 0x」 加 做 转字符串 用 错误 加 「 」 加 做 接口C_解析错误 用 错误。
    完。
完。

watchdog_启用 接受 超时tick数：
    watchdog_超时tick 为 超时tick数。
    watchdog_最后tick 为 定时器_tick计数。
    watchdog_已启用 为 真。
    做 打印 用 「[Watchdog] 已启用, 超时=」 加 做 转字符串 用 超时tick数 加 「 tick」。
    返还 0。
完。

watchdog_禁用 接受 零参：
    watchdog_已启用 为 假。
    返还 0。
完。

watchdog_喂狗 接受 零参：
    watchdog_最后tick 为 定时器_tick计数。
完。

watchdog_tick 接受 零参：
    若 watchdog_已启用 等于 假：
        返还。
    完。
    差值 为 定时器_tick计数 减 watchdog_最后tick。
    若 差值 大于 watchdog_超时tick：
        watchdog_触发次数 为 watchdog_触发次数 加 1。
        做 内核警告 用 「[Watchdog] 软锁定检测! 上次喂狗距今=」 加 做 转字符串 用 差值 加 「 tick」。
        watchdog_最后tick 为 定时器_tick计数。
    完。
完。

接口C_获取tick计数 接受 零参：
    返还 定时器_tick计数。
完。

接口C_获取秒计数 接受 零参：
    返还 定时器_秒计数。
完。

接口C_获取uptime_ms 接受 零参：
    返还 定时器_uptime_ms。
完。

接口C_获取每秒tick 接受 零参：
    返还 定时器_每秒tick。
完。

接口C_定时器_已启动查询 接受 零参：
    返还 接口C_定时器_已启动。
完。

接口C_获取间隔毫秒 接受 零参：
    返还 定时器_间隔毫秒。
完。

接口C_获取uptime字符串 接受 零参：
    秒 为 定时器_秒计数。
    分 为 秒 除以 60。
    时 为 分 除以 60。
    天 为 时 除以 24。
    剩余秒 为 秒 取余于 60。
    剩余分 为 分 取余于 60。
    剩余时 为 时 取余于 24。
    返还 做 转字符串 用 天 加 「天」 加 做 转字符串 用 剩余时 加 「时」 加 做 转字符串 用 剩余分 加 「分」 加 做 转字符串 用 剩余秒 加 「秒」。
完。

接口C_MMIO读32 接受 地址：
    返还 做 volatile_读32 用 地址。
完。

接口C_MMIO写32 接受 地址、值：
    做 volatile_写32 用 地址、值。
完。

MMIO读32 接受 地址：
    返还 做 volatile_读32 用 地址。
完。

MMIO写32 接受 地址、值：
    做 volatile_写32 用 地址、值。
完。

volatile_读32 接受 地址：
    可变 值 为 0。
    汇编。
    块：
        「mov rax, [rbp+16]」。
        「mov eax, [rax]」。
        「mov [rbp-8], rax」。
    完。
    返还 值。
完。

volatile_写32 接受 地址、值：
    汇编。
    块：
        「mov rax, [rbp+24]」。
        「mov ecx, [rbp+16]」。
        「mov [rax], ecx」。
    完。
完。

volatile_读8 接受 地址：
    可变 值 为 0。
    汇编。
    块：
        「mov rax, [rbp+16]」。
        「movzx eax, byte [rax]」。
        「mov [rbp-8], rax」。
    完。
    返还 值。
完。

volatile_写8 接受 地址、值：
    汇编。
    块：
        「mov rax, [rbp+24]」。
        「mov cl, [rbp+16]」。
        「mov [rax], cl」。
    完。
完。

volatile_读16 接受 地址：
    可变 值 为 0。
    汇编。
    块：
        「mov rax, [rbp+16]」。
        「movzx eax, word [rax]」。
        「mov [rbp-8], rax」。
    完。
    返还 值。
完。

volatile_写16 接受 地址、值：
    汇编。
    块：
        「mov rax, [rbp+24]」。
        「mov cx, [rbp+16]」。
        「mov [rax], cx」。
    完。
完。

volatile_读64 接受 地址：
    可变 值 为 0。
    汇编。
    块：
        「mov rax, [rbp+16]」。
        「mov rax, [rax]」。
        「mov [rbp-8], rax」。
    完。
    返还 值。
完。

volatile_写64 接受 地址、值：
    汇编。
    块：
        「mov rax, [rbp+24]」。
        「mov rcx, [rbp+16]」。
        「mov [rax], rcx」。
    完。
完。

内存屏障 接受 零参：
    汇编。
    块：
        「mfence」。
    完。
完。

读内存屏障 接受 零参：
    汇编。
    块：
        「lfence」。
    完。
完。

写内存屏障 接受 零参：
    汇编。
    块：
        「sfence」。
    完。
完。

接口C_TPR定时器_完整统计 接受 零参：
    统计 为 做 字典。
    做 字典设 用 统计、「定时器已启动」、接口C_定时器_已启动。
    做 字典设 用 统计、「间隔(ms)」、定时器_间隔毫秒。
    做 字典设 用 统计、「tick总数」、定时器_tick计数。
    做 字典设 用 统计、「秒计数」、定时器_秒计数。
    做 字典设 用 统计、「uptime(ms)」、定时器_uptime_ms。
    做 字典设 用 统计、「每秒tick」、定时器_每秒tick。
    做 字典设 用 统计、「当前TPR」、做 接口C_获取任务优先级。
    做 字典设 用 统计、「TPR设置次数」、TPR_设置次数。
    做 字典设 用 统计、「TPR屏蔽次数」、TPR_屏蔽次数。
    做 字典设 用 统计、「TPR恢复次数」、TPR_恢复次数。
    做 字典设 用 统计、「TPR嵌套深度」、做 计数 用 TPR_保存栈。
    做 字典设 用 统计、「TPR嵌套溢出」、TPR_嵌套溢出次数。
    做 字典设 用 统计、「watchdog已启用」、watchdog_已启用。
    做 字典设 用 统计、「watchdog触发」、watchdog_触发次数。
    做 字典设 用 统计、「watchdog超时tick」、watchdog_超时tick。
    做 字典设 用 统计、「错误数」、TPR_错误数。
    做 字典设 用 统计、「uptime」、做 接口C_获取uptime字符串。
    返还 统计。
完。

接口C_TPR定时器_打印统计 接受 零参：
    统计 为 做 接口C_TPR定时器_完整统计。
    做 打印 用 「[TPR/定时器] ═══════ 完整统计 ═══════」。
    做 打印 用 「  uptime:       」 加 做 转字符串 用 做 字典取 用 统计、「uptime」。
    做 打印 用 「  tick总数:     」 加 做 转字符串 用 做 字典取 用 统计、「tick总数」。
    做 打印 用 「  秒计数:       」 加 做 转字符串 用 做 字典取 用 统计、「秒计数」。
    做 打印 用 「  间隔:         」 加 做 转字符串 用 做 字典取 用 统计、「间隔(ms)」 加 「ms」。
    做 打印 用 「  每秒tick:     」 加 做 转字符串 用 做 字典取 用 统计、「每秒tick」。
    做 打印 用 「  当前TPR:      」 加 做 转字符串 用 做 字典取 用 统计、「当前TPR」 加 「 (」 加 做 接口C_TPR名 用 做 字典取 用 统计、「当前TPR」 加 「)」。
    做 打印 用 「  TPR设置:      」 加 做 转字符串 用 做 字典取 用 统计、「TPR设置次数」。
    做 打印 用 「  TPR屏蔽:      」 加 做 转字符串 用 做 字典取 用 统计、「TPR屏蔽次数」。
    做 打印 用 「  TPR嵌套深度:  」 加 做 转字符串 用 做 字典取 用 统计、「TPR嵌套深度」。
    做 打印 用 「  watchdog:     」 加 做 转字符串 用 做 字典取 用 统计、「watchdog已启用」。
    做 打印 用 「  watchdog触发: 」 加 做 转字符串 用 做 字典取 用 统计、「watchdog触发」。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[TPR/定时器] ═════════════════════════」。
    返还 0。
完。

接口C_TPR定时器_dump 接受 零参：
    做 打印 用 「[TPR/定时器 dump] ──────────────────」。
    做 打印 用 「  定时器已启动: 」 加 做 转字符串 用 接口C_定时器_已启动。
    做 打印 用 「  间隔: 」 加 做 转字符串 用 定时器_间隔毫秒 加 「ms」。
    做 打印 用 「  tick计数: 」 加 做 转字符串 用 定时器_tick计数。
    做 打印 用 「  秒计数: 」 加 做 转字符串 用 定时器_秒计数。
    做 打印 用 「  uptime: 」 加 做 接口C_获取uptime字符串。
    做 打印 用 「  TPR: 」 加 做 转字符串 用 做 接口C_获取任务优先级。
    做 打印 用 「  TPR栈深度: 」 加 做 转字符串 用 做 计数 用 TPR_保存栈。
    做 打印 用 「  watchdog启用: 」 加 做 转字符串 用 watchdog_已启用。
    做 打印 用 「  watchdog最后tick: 」 加 做 转字符串 用 watchdog_最后tick。
    做 打印 用 「  watchdog触发: 」 加 做 转字符串 用 watchdog_触发次数。
    做 打印 用 「[TPR/定时器 dump] ──────────────────」。
完。

接口C_TPR定时器_销毁 接受 零参：
    若 接口C_定时器_已启动 等于 真：
        做 接口C_停止定时器。
    完。
    做 接口C_允许所有中断。
    TPR_保存栈 为 一组：（空）。
    watchdog_已启用 为 假。
    接口C_定时器_已启动 为 假。
    做 打印 用 「[TPR/定时器] 已销毁」。
    返还 0。
完。

接口C_为调度器提供tick信息 接受 零参：
    返还 一组：定时器_tick计数、定时器_间隔微秒、定时器_每秒tick。
完。

接口C_为电源管理提供uptime 接受 零参：
    返还 定时器_uptime_ms。
完。

接口C_为procfs提供uptime 接受 零参：
    返还 一组：定时器_秒计数、定时器_uptime_ms。
完。

可变 AI_高级中控设_风暴阈值 为 10000。

可变 AI_高级中控设_预测IRQ 为 0。

AI_高级中控设_风暴检测 接受 触发频率：
    若 触发频率 大于 AI_高级中控设_风暴阈值：
        AI_高级中控设_风暴阈值 为 AI_高级中控设_风暴阈值 乘 3 除以 2。
        返还 真。
    完。
    返还 假。
完。

AI_高级中控设_自适应合并 接受 当前延迟：
    若 当前延迟 小于 100：
        返还 16。
    完。
    若 当前延迟 小于 500：
        返还 8。
    完。
    返还 1。
完。

本章 收束。
