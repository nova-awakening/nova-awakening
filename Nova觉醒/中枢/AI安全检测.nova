《AI安全检测》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·链表后继》。

引用《.·时钟中枢》。

引用《.·整数转字符串》。

安全_正常 为 0。

安全_可疑 为 1。

安全_危险 为 2。

安全_攻击 为 3。

安全画像 是 一种 事物，含 pid、调用频率EMA、上次调用时间、窗口内调用数、窗口起始时间、失败计数、敏感调用计数、异常得分、信任等级。

可变 安全画像表 为 一组：（空）。

可变 异常事件计数 为 0。

可变 AI安全_已初始化 为 假。

可变 全局威胁等级 为 0。

敏感调用 为 一组：59、101、165、105、106、41、42、49。

AI安全_初始化 接受 零参：
    AI安全_已初始化 为 真。
    做 内核信息 用 「AI安全检测: 行为基线建模+异常偏离检测+自适应阈值」。
完。

AI安全_检查系统调用 接受 PID、调用号：
    若 非 AI安全_已初始化：
        返还 真。
    完。
    画像 为 做 AI安全_获取或创建画像 用 PID。
    现在 为 做 读取毫秒。
    画像 的 窗口内调用数 为 画像 的 窗口内调用数 加 1。
    若 现在 减 画像 的 窗口起始时间 大于 1000：
        频率 为 画像 的 窗口内调用数 乘 1024。
        画像 的 调用频率EMA 为 (画像 的 调用频率EMA 乘 922 加 频率 乘 102) 除以 1024。
        画像 的 窗口内调用数 为 0。
        画像 的 窗口起始时间 为 现在。
    完。
    画像 的 上次调用时间 为 现在。
    是敏感 为 做 AI安全_是敏感调用 用 调用号。
    若 是敏感：
        画像 的 敏感调用计数 为 画像 的 敏感调用计数 加 1。
    完。
    可变 得分 为 0。
    若 画像 的 调用频率EMA 大于 0：
        当前频率 为 画像 的 窗口内调用数 乘 1024。
        若 当前频率 大于 画像 的 调用频率EMA 乘 3：
            得分 为 得分 加 300。
        否则若 当前频率 大于 画像 的 调用频率EMA 乘 2：
            得分 为 得分 加 150。
        完。
    完。
    若 画像 的 敏感调用计数 大于 10：
        得分 为 得分 加 200。
    否则若 画像 的 敏感调用计数 大于 5：
        得分 为 得分 加 100。
    完。
    若 画像 的 失败计数 大于 10：
        得分 为 得分 加 400。
    否则若 画像 的 失败计数 大于 5：
        得分 为 得分 加 200。
    完。
    若 画像 的 信任等级 等于 0：
        得分 为 得分 加 50。
    完。
    画像 的 异常得分 为 得分。
    若 得分 大于或等于 800：
        异常事件计数 为 异常事件计数 加 1。
        做 内核警告 用 「AI安全: PID=」 加 做 整数转字符串 用 PID 加 「 攻击行为(得分=」 加 做 整数转字符串 用 得分 加 「)」。
        做 AI安全_更新全局威胁。
        返还 假。
    完。
    若 得分 大于或等于 500：
        做 内核警告 用 「AI安全: PID=」 加 做 整数转字符串 用 PID 加 「 可疑行为(得分=」 加 做 整数转字符串 用 得分 加 「)」。
    完。
    返还 真。
完。

AI安全_记录失败 接受 PID：
    画像 为 做 AI安全_查找画像 用 PID。
    若 画像 不等于 空：
        画像 的 失败计数 为 画像 的 失败计数 加 1。
    完。
完。

AI安全_记录成功 接受 PID：
    画像 为 做 AI安全_查找画像 用 PID。
    若 画像 不等于 空：
        画像 的 失败计数 为 0。
    完。
完。

AI安全_设置信任 接受 PID、等级：
    画像 为 做 AI安全_获取或创建画像 用 PID。
    画像 的 信任等级 为 等级。
完。

AI安全_是敏感调用 接受 调用号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 敏感调用：
        若 敏感调用 的 第 i 项 等于 调用号：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

AI安全_更新全局威胁 接受 零参：
    若 异常事件计数 大于 20：
        全局威胁等级 为 2。
    否则若 异常事件计数 大于 5：
        全局威胁等级 为 1。
    否则：
        全局威胁等级 为 0。
    完。
完。

AI安全_查找画像 接受 pid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 安全画像表：
        若 安全画像表 的 第 i 项 的 pid 等于 pid：
            返还 安全画像表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

AI安全_获取或创建画像 接受 pid：
    已有 为 做 AI安全_查找画像 用 pid。
    若 已有 不等于 空：
        返还 已有。
    完。
    新画像 为 新建 安全画像。
    新画像 的 pid 为 pid。
    新画像 的 窗口起始时间 为 做 读取毫秒。
    做 追加 用 安全画像表、新画像。
    返还 新画像。
完。

AI安全_威胁等级 接受 零参：
    返还 全局威胁等级。
完。

AI安全_异常总数 接受 零参：
    返还 异常事件计数。
完。

AI安全_威胁名 接受 等级：
    若 等级 等于 0：
        返还 「安全」。
    完。
    若 等级 等于 1：
        返还 「警戒」。
    完。
    若 等级 等于 2：
        返还 「危险」。
    完。
    返还 「未知」。
完。

AI安全_清除画像 接受 pid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 安全画像表：
        若 安全画像表 的 第 i 项 的 pid 等于 pid：
            做 链表_删除 用 安全画像表、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

AI安全_高危进程列表 接受 阈值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 安全画像表：
        若 安全画像表 的 第 i 项 的 异常得分 大于或等于 阈值：
            做 追加 用 结果、安全画像表 的 第 i 项 的 pid。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

AI安全_统计 接受 零参：
    返还 一组：做 计数 用 安全画像表、异常事件计数、全局威胁等级。
完。

AI安全_威胁级别名 接受 级别：
    若 级别 等于 0：
        返还 「normal」。
    完。
    若 级别 等于 1：
        返还 「elevated」。
    完。
    若 级别 等于 2：
        返还 「high」。
    完。
    若 级别 等于 3：
        返还 「critical」。
    完。
    返还 「unknown」。
完。

AI安全_完整统计 接受 零参：
    返还 一组：做 计数 用 安全画像表、异常事件计数、全局威胁等级、做 AI安全_威胁级别名 用 全局威胁等级。
完。

本章 收束。
