《信号正弦》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·张量数学引擎》。

信号精度 为 1000。

信号_sin 接受 x：
    可变 角 为 x 取余于 6283。
    若 角 小于 0：
        角 为 角 加 6283。
    完。
    若 角 小于 1571：
        返还 角 乘 信号精度 除以 1571。
    完。
    若 角 小于 3142：
        返还 (3142 减 角) 乘 信号精度 除以 1571。
    完。
    若 角 小于 4712：
        返还 0 减 (角 减 3142) 乘 信号精度 除以 1571。
    完。
    返还 0 减 (6283 减 角) 乘 信号精度 除以 1571。
完。

信号_cos 接受 x：
    返还 做 信号_sin 用 x 加 1571。
完。

信号_排序 接受 列表：
    n 为 做 计数 用 列表。
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 r、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 1。
    只要 i 小于 n：
        键 为 r 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 r 的 第 j 项 大于 键：
            r 的 第 j 加 1 项 为 r 的 第 j 项。
            j 为 j 减 1。
        完。
        r 的 第 j 加 1 项 为 键。
        i 为 i 加 1。
    完。
    返还 r。
完。

dft 接受 信号：
    n 为 做 计数 用 信号。
    可变 实部 为 一组：（空）。
    可变 虚部 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 n：
        可变 re 为 0。
        可变 im 为 0。
        可变 t 为 0。
        只要 t 小于 n：
            角度 为 2 乘 k 乘 t 乘 6283 除以 n。
            re 为 re 加 信号 的 第 t 项 乘 做 信号_cos 用 角度 除以 信号精度。
            im 为 im 减 信号 的 第 t 项 乘 做 信号_sin 用 角度 除以 信号精度。
            t 为 t 加 1。
        完。
        做 追加 用 实部、re。
        做 追加 用 虚部、im。
        k 为 k 加 1。
    完。
    返还 一组：实部、虚部。
完。

频谱幅度 接受 dft结果：
    实 为 dft结果 的 第 0 项。
    虚 为 dft结果 的 第 1 项。
    n 为 做 计数 用 实。
    可变 幅度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        mag_sq 为 实 的 第 i 项 乘 实 的 第 i 项 加 虚 的 第 i 项 乘 虚 的 第 i 项。
        做 追加 用 幅度、做 平方根 用 mag_sq。
        i 为 i 加 1。
    完。
    返还 幅度。
完。

低通滤波 接受 信号、窗口大小：
    n 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 和 为 0。
        可变 计数器 为 0。
        起 为 i 减 窗口大小 除以 2。
        终 为 i 加 窗口大小 除以 2。
        可变 j 为 起。
        只要 j 小于或等于 终：
            若 j 大于或等于 0 并且 j 小于 n：
                和 为 和 加 信号 的 第 j 项。
                计数器 为 计数器 加 1。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和 除以 计数器。
        i 为 i 加 1。
    完。
    返还 结果。
完。

高通滤波 接受 信号、窗口大小：
    低通 为 做 低通滤波 用 信号、窗口大小。
    n 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、信号 的 第 i 项 减 低通 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

中值滤波 接受 信号、窗口大小：
    n 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 窗口 为 一组：（空）。
        起 为 i 减 窗口大小 除以 2。
        终 为 i 加 窗口大小 除以 2。
        可变 j 为 起。
        只要 j 小于或等于 终：
            若 j 大于或等于 0 并且 j 小于 n：
                做 追加 用 窗口、信号 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        排序窗 为 做 信号_排序 用 窗口。
        做 追加 用 结果、排序窗 的 第 做 计数 用 排序窗 除以 2 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

指数移动平均 接受 信号、alpha：
    n 为 做 计数 用 信号。
    可变 结果 为 一组：信号 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        前 为 结果 的 第 i 减 1 项。
        新值 为 alpha 乘 信号 的 第 i 项 除以 信号精度 加 (信号精度 减 alpha) 乘 前 除以 信号精度。
        做 追加 用 结果、新值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

FIR_滤波 接受 信号、系数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 信号：
        可变 sum 为 0。
        可变 k 为 0。
        只要 k 小于 做 计数 用 系数：
            若 i 减 k 大于或等于 0：
                sum 为 sum 加 信号 的 第 i 减 k 项 乘 系数 的 第 k 项 除以 100。
            完。
            k 为 k 加 1。
        完。
        做 追加 用 结果、sum。
        i 为 i 加 1。
    完。
    返还 结果。
完。

IIR_一阶 接受 信号、a、b：
    可变 结果 为 一组：（空）。
    可变 prev 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 信号：
        y 为 a 乘 信号 的 第 i 项 除以 1000 加 b 乘 prev 除以 1000。
        做 追加 用 结果、y。
        prev 为 y。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 AI_信号正弦_操作热度 为 0。

AI_信号正弦_最优算法 接受 数据量、已排序：
    AI_信号正弦_操作热度 为 AI_信号正弦_操作热度 加 1。
    若 数据量 小于 16：
        返还 「插入排序」。
    完。
    若 已排序：
        返还 「二分查找」。
    完。
    若 数据量 小于 10000：
        返还 「快速排序」。
    完。
    返还 「归并排序」。
完。

若 象限 等于 1 或者 象限 等于 3：
    余 为 FP_HALF_PI 减 余。
完。

定点_cos_精确 接受 x：
    可变 角 为 x 取余于 FP_TWO_PI。
    若 角 小于 0：
        角 为 角 加 FP_TWO_PI。
    完。
    可变 象限 为 角 除以 FP_HALF_PI。
    可变 余 为 角 取余于 FP_HALF_PI。
    若 象限 大于 3：
        象限 为 3。
    完。
    若 象限 等于 1 或者 象限 等于 3：
        余 为 FP_HALF_PI 减 余。
    完。
    t 为 余。
    t2 为 t 乘 t 除以 FP。
    t4 为 t2 乘 t2 除以 FP。
    t6 为 t4 乘 t2 除以 FP。
    t8 为 t4 乘 t4 除以 FP。
    可变 结果 为 FP 减 t2 除以 2 加 t4 除以 24 减 t6 除以 720 加 t8 除以 40320。
    若 象限 等于 1 或者 象限 等于 2：
        结果 为 0 减 结果。
    完。
    返还 结果。
完。

定点_sin_精确 接受 x：
    返还 做 定点_cos_精确 用 x 减 FP_HALF_PI。
完。

定点_atan2 接受 y、x：
    若 x 等于 0 并且 y 等于 0：
        返还 0。
    完。
    若 x 等于 0：
        若 y 大于 0：
            返还 FP_HALF_PI。
        完。
        返还 0 减 FP_HALF_PI。
    完。
    可变 r 为 y 乘 FP 除以 x。
    若 r 大于 FP 或者 r 小于 负 FP：
        inv 为 x 乘 FP 除以 y。
        i2 为 inv 乘 inv 除以 FP。
        i3 为 i2 乘 inv 除以 FP。
        i5 为 i3 乘 i2 除以 FP。
        i7 为 i5 乘 i2 除以 FP。
        at 为 inv 减 i3 除以 3 加 i5 除以 5 减 i7 除以 7。
        若 y 大于 0：
            返还 FP_HALF_PI 减 at。
        完。
        返还 0 减 FP_HALF_PI 减 at。
    完。
    可变 结果 为 0。
    若 r 大于 8000：
        u 为 (r 减 FP) 乘 FP 除以 (r 加 FP)。
        u2 为 u 乘 u 除以 FP。
        u3 为 u2 乘 u 除以 FP。
        结果 为 FP_QRTR_PI 加 u 减 u3 除以 3。
    否则若 r 小于 负 8000：
        u 为 (r 加 FP) 乘 FP 除以 (0 减 r 加 FP)。
        u2 为 u 乘 u 除以 FP。
        u3 为 u2 乘 u 除以 FP。
        结果 为 0 减 FP_QRTR_PI 加 u 减 u3 除以 3。
    否则：
        r2 为 r 乘 r 除以 FP。
        r3 为 r2 乘 r 除以 FP。
        r5 为 r3 乘 r2 除以 FP。
        r7 为 r5 乘 r2 除以 FP。
        结果 为 r 减 r3 除以 3 加 r5 除以 5 减 r7 除以 7。
    完。
    若 x 小于 0：
        若 y 大于或等于 0：
            结果 为 结果 加 FP_PI。
        否则：
            结果 为 结果 减 FP_PI。
        完。
    完。
    返还 结果。
完。

本章 收束。
