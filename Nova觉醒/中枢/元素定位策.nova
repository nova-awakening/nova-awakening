《元素定位策》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·硬链接》。

引用《.·内核打印》。

引用《..·标准·原生字符串》。

引用《.·整数转字符串》。

UI_按文本查找 接受 文本：
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        若 已发现元素 的 第 i 项 的 名称 等于 文本：
            返还 已发现元素 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

UI_按文本模糊查找 接受 关键词：
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        若 做 字符串包含 用 已发现元素 的 第 i 项 的 名称、关键词：
            返还 已发现元素 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

UI_按角色查找 接受 角色：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        若 已发现元素 的 第 i 项 的 角色 等于 角色：
            做 追加 用 结果、已发现元素 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

UI_按坐标查找 接受 x、y：
    可变 最深 为 空。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        e 为 已发现元素 的 第 i 项。
        若 x 大于或等于 e 的 左上x 并且 x 小于 e 的 左上x 加 e 的 宽 并且 y 大于或等于 e 的 左上y 并且 y 小于 e 的 左上y 加 e 的 高：
            最深 为 e。
        完。
        i 为 i 加 1。
    完。
    返还 最深。
完。

UI_按元素选择器查找 接受 选择器：
    可变 i 为 0。
    只要 i 小于 做 计数 用 已发现元素：
        若 做 字符串结尾匹配 用 已发现元素 的 第 i 项 的 DOM路径、选择器：
            返还 已发现元素 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

操作_点击 为 1。

操作_双击 为 2。

操作_右击 为 3。

操作_输入 为 4。

操作_滚动 为 5。

操作_拖拽 为 6。

操作_悬停 为 7。

操作_按键 为 8。

UI操作记录 是 一种 事物，含 操作类型、目标元素ID、x、y、输入文本、时间戳、成功。

可变 操作历史 为 一组：（空）。

可变 操作_时钟 为 0。

UI_点击 接受 元素：
    若 元素 等于 空：
        UI_失败次数 为 UI_失败次数 加 1。
        返还 假。
    完。
    UI_操作次数 为 UI_操作次数 加 1。
    操作_时钟 为 操作_时钟 加 1。
    目标x 为 元素 的 中心x。
    目标y 为 元素 的 中心y。
    做 UI_发送鼠标移动 用 目标x、目标y。
    做 UI_发送鼠标按钮 用 目标x、目标y、1、真。
    做 UI_发送鼠标按钮 用 目标x、目标y、1、假。
    记录 为 新建 UI操作记录。
    记录 的 操作类型 为 操作_点击。
    记录 的 目标元素ID 为 元素 的 元素ID。
    记录 的 x 为 目标x。
    记录 的 y 为 目标y。
    记录 的 时间戳 为 操作_时钟。
    记录 的 成功 为 真。
    做 追加 用 操作历史、记录。
    UI_成功次数 为 UI_成功次数 加 1。
    做 内核调试 用 「UI操作: 点击 [」 加 元素 的 角色 加 「] '」 加 元素 的 名称 加 「' @(」 加 做 整数转字符串 用 目标x 加 「,」 加 做 整数转字符串 用 目标y 加 「)」。
    返还 真。
完。

UI_输入文本 接受 元素、文本：
    若 元素 等于 空 或者 非 元素 的 可输入：
        UI_失败次数 为 UI_失败次数 加 1。
        返还 假。
    完。
    UI_操作次数 为 UI_操作次数 加 1。
    操作_时钟 为 操作_时钟 加 1。
    做 UI_点击 用 元素。
    可变 i 为 0。
    只要 i 小于 做 字符串长度 用 文本：
        ch 为 做 字符串取字节 用 文本、i。
        做 UI_发送按键 用 ch、真。
        做 UI_发送按键 用 ch、假。
        i 为 i 加 1。
    完。
    记录 为 新建 UI操作记录。
    记录 的 操作类型 为 操作_输入。
    记录 的 目标元素ID 为 元素 的 元素ID。
    记录 的 输入文本 为 文本。
    记录 的 时间戳 为 操作_时钟。
    记录 的 成功 为 真。
    做 追加 用 操作历史、记录。
    UI_成功次数 为 UI_成功次数 加 1。
    做 内核调试 用 「UI操作: 输入 [」 加 元素 的 角色 加 「] 文本='」 加 文本 加 「'」。
    返还 真。
完。

UI_滚动 接受 x、y、方向、量：
    UI_操作次数 为 UI_操作次数 加 1。
    操作_时钟 为 操作_时钟 加 1。
    做 UI_发送滚轮 用 x、y、方向、量。
    UI_成功次数 为 UI_成功次数 加 1。
    返还 真。
完。

UI_拖拽 接受 源x、源y、目标x、目标y：
    UI_操作次数 为 UI_操作次数 加 1。
    操作_时钟 为 操作_时钟 加 1。
    做 UI_发送鼠标移动 用 源x、源y。
    做 UI_发送鼠标按钮 用 源x、源y、1、真。
    步数 为 10。
    可变 i 为 1。
    只要 i 小于或等于 步数：
        cx 为 源x 加 (目标x 减 源x) 乘 i 除以 步数。
        cy 为 源y 加 (目标y 减 源y) 乘 i 除以 步数。
        做 UI_发送鼠标移动 用 cx、cy。
        i 为 i 加 1。
    完。
    做 UI_发送鼠标按钮 用 目标x、目标y、1、假。
    UI_成功次数 为 UI_成功次数 加 1。
    返还 真。
完。

可变 AI_元素定位策_热节点缓存 为 0。

AI_元素定位策_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_元素定位策_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_元素定位策_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

本章 收束。
