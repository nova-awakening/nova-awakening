《突触网格》

引用《.·类型定义》。

引用《.·错误码》。

突触_模块数 为 30。

突触_连接类型 为 4。

突触_兴奋 为 0。

突触_抑制 为 1。

突触_调制 为 2。

突触_同步 为 3。

可变 突触_学习率 为 50。

可变 突触_衰减率 为 5。

可变 突触_最小强度 为 10。

可变 突触_最大强度 为 2000。

可变 突触_发芽阈值 为 300。

过滤_层数 为 5。

可变 过滤_保留率 为 700。

可变 过滤_强化率 为 200。

可变 EWC_强度 为 500。

可变 EWC_保护阈值 为 600。

可变 连接矩阵 为 一组：（空）。

可变 协同激活计数 为 一组：（空）。

可变 突触_已初始化 为 假。

可变 突触网格_已初始化 为 假。

可变 突触_总传播次数 为 0。

可变 突触_总发芽次数 为 0。

突触_初始化 接受 零参：
    连接矩阵 为 一组：（空）。
    协同激活计数 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 突触_模块数：
        可变 行 为 一组：（空）。
        可变 协同行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 突触_模块数：
            可变 类型组 为 一组：（空）。
            可变 t 为 0。
            只要 t 小于 突触_连接类型：
                初始强度 为 100。
                若 i 等于 j：
                    初始强度 为 0。
                完。
                做 追加 用 类型组、一组：初始强度、1、500、0、0。
                t 为 t 加 1。
            完。
            做 追加 用 行、类型组。
            做 追加 用 协同行、0。
            j 为 j 加 1。
        完。
        做 追加 用 连接矩阵、行。
        做 追加 用 协同激活计数、协同行。
        i 为 i 加 1。
    完。
    突触_已初始化 为 真。
    突触网格_已初始化 为 真。
    做 内核信息 用 「突触网格: 30×30×4=3600连接 EWC永不遗忘+突触发芽」。
完。

突触网格_获取连接数据 接受 零参：
    若 非 突触_已初始化：
        返还 一组：（空）。
    完。
    可变 连接数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 突触_模块数：
        可变 j 为 0。
        只要 j 小于 突触_模块数：
            可变 t 为 0。
            只要 t 小于 突触_连接类型：
                做 追加 用 连接数据、连接矩阵 的 第 i 项 的 第 j 项 的 第 t 项 的 第 0 项。
                t 为 t 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 连接数据。
完。

突触网格_应用连接数据 接受 连接数据：
    若 连接数据 等于 空：
        返还 0。
    完。
    若 非 突触_已初始化：
        做 突触_初始化。
    完。
    期望长度 为 突触_模块数 乘 突触_模块数 乘 突触_连接类型。
    若 做 计数 用 连接数据 小于 期望长度：
        返还 0。
    完。
    可变 索引 为 0。
    可变 i 为 0。
    只要 i 小于 突触_模块数：
        可变 j 为 0。
        只要 j 小于 突触_模块数：
            可变 t 为 0。
            只要 t 小于 突触_连接类型：
                可变 强度 为 连接数据 的 第 索引 项。
                若 强度 小于 0：
                    强度 为 0。
                完。
                若 强度 大于 突触_最大强度：
                    强度 为 突触_最大强度。
                完。
                连接矩阵 的 第 i 项 的 第 j 项 的 第 t 项 的 第 0 项 为 强度。
                索引 为 索引 加 1。
                t 为 t 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    突触_已初始化 为 真。
    突触网格_已初始化 为 真。
    返还 索引。
完。

突触_传播 接受 源模块、信号强度：
    若 非 突触_已初始化 或者 源模块 大于或等于 突触_模块数：
        返还 一组：（空）。
    完。
    突触_总传播次数 为 突触_总传播次数 加 1。
    可变 输出 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 突触_模块数：
        可变 净输入 为 0。
        兴奋 为 连接矩阵 的 第 源模块 项 的 第 j 项 的 第 突触_兴奋 项 的 第 0 项。
        抑制 为 连接矩阵 的 第 源模块 项 的 第 j 项 的 第 突触_抑制 项 的 第 0 项。
        调制 为 连接矩阵 的 第 源模块 项 的 第 j 项 的 第 突触_调制 项 的 第 0 项。
        净输入 为 (兴奋 减 抑制) 乘 (1000 加 调制) 除以 1000 乘 信号强度 除以 1000。
        可变 t 为 0。
        只要 t 小于 突触_连接类型：
            连接矩阵 的 第 源模块 项 的 第 j 项 的 第 t 项 的 第 3 项 为 连接矩阵 的 第 源模块 项 的 第 j 项 的 第 t 项 的 第 3 项 加 1。
            t 为 t 加 1。
        完。
        做 追加 用 输出、净输入。
        j 为 j 加 1。
    完。
    返还 输出。
完。

突触_赫布更新 接受 源模块、目标模块、源活性、目标活性：
    若 非 突触_已初始化：
        返还。
    完。
    增量 为 突触_学习率 乘 源活性 乘 目标活性 除以 (1000 乘 1000)。
    可变 新强度 为 连接矩阵 的 第 源模块 项 的 第 目标模块 项 的 第 突触_兴奋 项 的 第 0 项 加 增量。
    若 新强度 大于 突触_最大强度：
        新强度 为 突触_最大强度。
    完。
    若 新强度 小于 突触_最小强度：
        新强度 为 突触_最小强度。
    完。
    重要度 为 连接矩阵 的 第 源模块 项 的 第 目标模块 项 的 第 突触_兴奋 项 的 第 4 项。
    若 重要度 大于 EWC_保护阈值：
        保护因子 为 EWC_强度 乘 重要度 除以 1000。
        新强度 为 连接矩阵 的 第 源模块 项 的 第 目标模块 项 的 第 突触_兴奋 项 的 第 0 项 加 增量 乘 1000 除以 (1000 加 保护因子)。
    完。
    连接矩阵 的 第 源模块 项 的 第 目标模块 项 的 第 突触_兴奋 项 的 第 0 项 为 新强度。
    若 源活性 大于 500 并且 目标活性 大于 500：
        协同激活计数 的 第 源模块 项 的 第 目标模块 项 为 协同激活计数 的 第 源模块 项 的 第 目标模块 项 加 1。
    完。
完。

突触_检查发芽 接受 零参：
    可变 i 为 0。
    只要 i 小于 突触_模块数：
        可变 j 为 0。
        只要 j 小于 突触_模块数：
            若 协同激活计数 的 第 i 项 的 第 j 项 大于 突触_发芽阈值：
                连接矩阵 的 第 i 项 的 第 j 项 的 第 突触_调制 项 的 第 0 项 为 连接矩阵 的 第 i 项 的 第 j 项 的 第 突触_调制 项 的 第 0 项 加 100。
                若 连接矩阵 的 第 i 项 的 第 j 项 的 第 突触_调制 项 的 第 0 项 大于 突触_最大强度：
                    连接矩阵 的 第 i 项 的 第 j 项 的 第 突触_调制 项 的 第 0 项 为 突触_最大强度。
                完。
                协同激活计数 的 第 i 项 的 第 j 项 为 0。
                突触_总发芽次数 为 突触_总发芽次数 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

突触_衰减 接受 零参：
    可变 i 为 0。
    只要 i 小于 突触_模块数：
        可变 j 为 0。
        只要 j 小于 突触_模块数：
            可变 t 为 0。
            只要 t 小于 突触_连接类型：
                强度 为 连接矩阵 的 第 i 项 的 第 j 项 的 第 t 项 的 第 0 项。
                若 连接矩阵 的 第 i 项 的 第 j 项 的 第 t 项 的 第 3 项 等于 0：
                    强度 为 强度 减 突触_衰减率。
                    若 强度 小于 突触_最小强度：
                        强度 为 突触_最小强度。
                    完。
                    连接矩阵 的 第 i 项 的 第 j 项 的 第 t 项 的 第 0 项 为 强度。
                完。
                使用 为 连接矩阵 的 第 i 项 的 第 j 项 的 第 t 项 的 第 3 项。
                连接矩阵 的 第 i 项 的 第 j 项 的 第 t 项 的 第 4 项 为 (连接矩阵 的 第 i 项 的 第 j 项 的 第 t 项 的 第 4 项 乘 900 加 使用 乘 100) 除以 1000。
                连接矩阵 的 第 i 项 的 第 j 项 的 第 t 项 的 第 3 项 为 0。
                t 为 t 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

突触_深度过滤 接受 信号、信号长度：
    可变 当前信号 为 信号。
    可变 层 为 0。
    只要 层 小于 过滤_层数：
        可变 过滤后 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 信号长度：
            值 为 当前信号 的 第 i 项。
            若 值 大于 0：
                若 值 大于 500：
                    值 为 值 加 值 乘 过滤_强化率 除以 1000。
                否则：
                    值 为 值 乘 过滤_保留率 除以 1000。
                完。
            否则：
                若 值 小于 负 500：
                    值 为 值 减 负 值 乘 过滤_强化率 除以 1000。
                否则：
                    值 为 值 乘 过滤_保留率 除以 1000。
                完。
            完。
            做 追加 用 过滤后、值。
            i 为 i 加 1。
        完。
        当前信号 为 过滤后。
        层 为 层 加 1。
    完。
    返还 当前信号。
完。

突触_连接强度 接受 源、目标、类型：
    若 源 大于或等于 突触_模块数 或者 目标 大于或等于 突触_模块数 或者 类型 大于或等于 突触_连接类型：
        返还 0。
    完。
    返还 连接矩阵 的 第 源 项 的 第 目标 项 的 第 类型 项 的 第 0 项。
完。

突触_设连接强度 接受 源、目标、类型、强度：
    若 源 大于或等于 突触_模块数 或者 目标 大于或等于 突触_模块数 或者 类型 大于或等于 突触_连接类型：
        返还。
    完。
    可变 新值 为 强度。
    若 新值 大于 突触_最大强度：
        新值 为 突触_最大强度。
    完。
    若 新值 小于 突触_最小强度：
        新值 为 突触_最小强度。
    完。
    连接矩阵 的 第 源 项 的 第 目标 项 的 第 类型 项 的 第 0 项 为 新值。
完。

突触_统计 接受 零参：
    返还 一本字典：传播次数 对应 突触_总传播次数、发芽次数 对应 突触_总发芽次数。
完。

突触_类型名 接受 类型：
    若 类型 等于 突触_兴奋：
        返还 「excitatory」。
    完。
    若 类型 等于 突触_抑制：
        返还 「inhibitory」。
    完。
    若 类型 等于 突触_调制：
        返还 「modulatory」。
    完。
    若 类型 等于 突触_同步：
        返还 「gap_junction」。
    完。
    返还 「unknown」。
完。

突触_活跃连接数 接受 零参：
    可变 计 为 0。
    可变 i 为 0。
    只要 i 小于 突触_模块数：
        可变 j 为 0。
        只要 j 小于 突触_模块数：
            若 连接矩阵 的 第 i 项 的 第 j 项 的 第 突触_兴奋 项 的 第 0 项 大于 突触_最小强度：
                计 为 计 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计。
完。

突触_空闲修剪弱连接 接受 基础阈值：
    若 非 突触_已初始化：
        返还 0。
    完。
    可变 总强度 为 0。
    可变 活跃数 为 0。
    可变 i 为 0。
    只要 i 小于 突触_模块数：
        可变 j 为 0。
        只要 j 小于 突触_模块数：
            若 i 不等于 j：
                强度 为 连接矩阵 的 第 i 项 的 第 j 项 的 第 突触_兴奋 项 的 第 0 项。
                若 强度 大于 突触_最小强度：
                    总强度 为 总强度 加 强度。
                    活跃数 为 活跃数 加 1。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 活跃数 等于 0：
        返还 0。
    完。
    阈值 为 总强度 除以 活跃数 乘 307 除以 1024。
    若 阈值 小于 基础阈值：
        阈值 为 基础阈值。
    完。
    可变 修剪数 为 0。
    i 为 0。
    只要 i 小于 突触_模块数：
        可变 j 为 0。
        只要 j 小于 突触_模块数：
            若 i 不等于 j：
                可变 强度 为 连接矩阵 的 第 i 项 的 第 j 项 的 第 突触_兴奋 项 的 第 0 项。
                若 强度 大于 突触_最小强度：
                    强度 为 强度 乘 973 除以 1024。
                    若 强度 小于 阈值：
                        强度 为 突触_最小强度。
                        修剪数 为 修剪数 加 1。
                    完。
                    若 强度 小于 突触_最小强度：
                        强度 为 突触_最小强度。
                    完。
                    连接矩阵 的 第 i 项 的 第 j 项 的 第 突触_兴奋 项 的 第 0 项 为 强度。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 修剪数。
完。

突触_完整统计 接受 零参：
    返还 一组：突触_总传播次数、突触_总发芽次数、做 突触_活跃连接数。
完。

本章 收束。
