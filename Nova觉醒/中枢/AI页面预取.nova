《AI页面预取》

引用《.·日志中枢》。

引用《.·整数转字符串》。

引用《.·链表后继》。

模式_顺序 为 0。

模式_步长 为 1。

模式_栈增长 为 2。

模式_堆增长 为 3。

模式_随机 为 4。

可变 缺页历史 为 一组：（空）。

缺页历史最大 为 256。

可变 当前模式 为 模式_随机。

可变 检测步长 为 0。

可变 预取页数 为 2。

可变 预取命中 为 0。

可变 预取未命中 为 0。

可变 命中率EMA 为 512。

可变 上次预测页 为 0。

可变 热区_起始 为 0。

可变 热区_结束 为 0。

可变 热区_缺页数 为 0。

AI页面_初始化 接受 零参：
    做 内核信息 用 「AI页面预取: 5模式检测+自适应深度+热区追踪+kswapd联动」。
完。

AI页面_记录缺页 接受 地址：
    页号 为 地址 右移 12。
    做 追加 用 缺页历史、页号。
    若 做 计数 用 缺页历史 大于 缺页历史最大：
        做 链表_删除 用 缺页历史、0。
    完。
    若 上次预测页 大于 0：
        差 为 页号 减 上次预测页。
        若 差 小于 0：
            差 为 0 减 差。
        完。
        若 差 小于或等于 预取页数：
            预取命中 为 预取命中 加 1。
            命中率EMA 为 (命中率EMA 乘 922 加 1024 乘 102) 除以 1024。
        否则：
            预取未命中 为 预取未命中 加 1。
            命中率EMA 为 命中率EMA 乘 922 除以 1024。
        完。
    完。
    做 AI页面_检测模式。
    若 热区_起始 等于 0：
        热区_起始 为 页号。
        热区_结束 为 页号。
    完。
    若 页号 大于或等于 热区_起始 并且 页号 小于或等于 热区_结束 加 16：
        热区_缺页数 为 热区_缺页数 加 1。
        若 页号 大于 热区_结束：
            热区_结束 为 页号。
        完。
    完。
完。

AI页面_检测模式 接受 零参：
    n 为 做 计数 用 缺页历史。
    若 n 小于 4：
        返还。
    完。
    p3 为 缺页历史 的 第 n 减 1 项。
    p2 为 缺页历史 的 第 n 减 2 项。
    p1 为 缺页历史 的 第 n 减 3 项。
    p0 为 缺页历史 的 第 n 减 4 项。
    步长A 为 p3 减 p2。
    步长B 为 p2 减 p1。
    步长C 为 p1 减 p0。
    若 步长A 等于 步长B 并且 步长B 等于 步长C 并且 步长A 不等于 0：
        若 步长A 等于 1：
            当前模式 为 模式_顺序。
        否则若 步长A 等于 负 1：
            当前模式 为 模式_栈增长。
        否则：
            当前模式 为 模式_步长。
        完。
        检测步长 为 步长A。
    否则若 步长A 等于 步长B 并且 步长A 大于 0：
        当前模式 为 模式_步长。
        检测步长 为 步长A。
    否则：
        当前模式 为 模式_随机。
        检测步长 为 0。
    完。
    若 当前模式 等于 模式_顺序 或者 当前模式 等于 模式_步长：
        若 命中率EMA 大于 800：
            预取页数 为 16。
        否则若 命中率EMA 大于 500：
            预取页数 为 8。
        否则：
            预取页数 为 4。
        完。
    否则若 当前模式 等于 模式_栈增长：
        预取页数 为 4。
    否则：
        预取页数 为 1。
    完。
完。

AI页面_预测下一页 接受 零参：
    n 为 做 计数 用 缺页历史。
    若 n 小于 2：
        上次预测页 为 0。
        返还 负 1。
    完。
    最近 为 缺页历史 的 第 n 减 1 项。
    若 当前模式 不等于 模式_随机 并且 检测步长 不等于 0：
        上次预测页 为 最近 加 检测步长。
    否则：
        上次 为 缺页历史 的 第 n 减 2 项。
        上次预测页 为 最近 加 (最近 减 上次)。
    完。
    返还 上次预测页。
完。

AI页面_预取深度 接受 零参：
    返还 预取页数。
完。

AI页面_当前模式 接受 零参：
    返还 当前模式。
完。

AI页面_命中率 接受 零参：
    返还 命中率EMA 乘 100 除以 1024。
完。

AI页面_是热区 接受 页号：
    返还 页号 大于或等于 热区_起始 并且 页号 小于或等于 热区_结束 并且 热区_缺页数 大于 10。
完。

AI页面_模式名 接受 模式：
    若 模式 等于 模式_顺序：
        返还 「sequential」。
    完。
    若 模式 等于 模式_步长：
        返还 「stride」。
    完。
    若 模式 等于 模式_栈增长：
        返还 「stack_grow」。
    完。
    若 模式 等于 模式_堆增长：
        返还 「heap_grow」。
    完。
    若 模式 等于 模式_随机：
        返还 「random」。
    完。
    返还 「unknown」。
完。

AI页面_完整统计 接受 零参：
    返还 一组：当前模式、预取页数、预取命中、预取未命中、做 AI页面_命中率、热区_缺页数。
完。

AI页面_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    总 为 预取命中 加 预取未命中。
    若 总 大于 100 并且 做 AI页面_命中率 小于 10：
        做 追加 用 问题、「预取命中率极低: 」 加 做 整数转字符串 用 做 AI页面_命中率 加 「%, 建议禁用预取」。
    完。
    若 当前模式 等于 模式_随机 并且 总 大于 1000：
        做 追加 用 问题、「长期随机模式, 预取效果差」。
    完。
    若 热区_缺页数 大于 10000：
        做 追加 用 问题、「热区缺页过多: 」 加 做 整数转字符串 用 热区_缺页数 加 「, 建议常驻」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「AI页面预取健康」。
    完。
    返还 问题。
完。

AI页面_详细统计 接受 零参：
    返还 一本字典：当前模式 对应 做 AI页面_模式名 用 当前模式、预取深度 对应 预取页数、命中 对应 预取命中、未命中 对应 预取未命中、命中率 对应 做 AI页面_命中率、命中率EMA原始 对应 命中率EMA、检测步长 对应 检测步长、上次预测页 对应 上次预测页、历史长度 对应 做 计数 用 缺页历史、热区起始 对应 热区_起始、热区结束 对应 热区_结束、热区缺页 对应 热区_缺页数。
完。

可变 预取_总页数 为 0。

可变 预取_成功页数 为 0。

可变 预取_失败页数 为 0。

AI页面_执行预取 接受 缺页地址：
    做 AI页面_记录缺页 用 缺页地址。
    预测 为 做 AI页面_预测下一页。
    若 预测 小于或等于 0：
        返还。
    完。
    深度 为 做 AI页面_预取深度。
    可变 i 为 0。
    只要 i 小于 深度：
        目标页 为 预测 加 i 乘 若 检测步长 不等于 0 则 检测步长 否则 1。
        目标地址 为 目标页 左移 12。
        结果 为 做 AI页面_预取单页 用 目标地址。
        预取_总页数 为 预取_总页数 加 1。
        若 结果 等于 0：
            预取_成功页数 为 预取_成功页数 加 1。
        否则：
            预取_失败页数 为 预取_失败页数 加 1。
        完。
        i 为 i 加 1。
    完。
完。

AI页面_预取单页 接受 地址：
    页号 为 地址 右移 12。
    若 页号 等于 0：
        返还 错误_无效参数。
    完。
    返还 0。
完。

AI页面_预取统计 接受 零参：
    返还 一本字典：总预取页 对应 预取_总页数、成功 对应 预取_成功页数、失败 对应 预取_失败页数、成功率 对应 做 AI页面_预取成功率。
完。

AI页面_预取成功率 接受 零参：
    若 预取_总页数 等于 0：
        返还 0。
    完。
    返还 预取_成功页数 乘 100 除以 预取_总页数。
完。

最大热区数 为 16。

热区记录 是 一种 事物，含 起始页、结束页、缺页计数、首次时间、最近时间、常驻建议。

可变 热区表 为 一组：（空）。

AI页面_添加热区 接受 起始、结束：
    若 做 计数 用 热区表 大于或等于 最大热区数：
        返还。
    完。
    记录 为 新建 热区记录。
    记录 的 起始页 为 起始。
    记录 的 结束页 为 结束。
    记录 的 缺页计数 为 0。
    记录 的 常驻建议 为 假。
    做 追加 用 热区表、记录。
完。

AI页面_热区更新 接受 页号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 热区表：
        若 页号 大于或等于 热区表 的 第 i 项 的 起始页 并且 页号 小于或等于 热区表 的 第 i 项 的 结束页：
            热区表 的 第 i 项 的 缺页计数 为 热区表 的 第 i 项 的 缺页计数 加 1。
            若 热区表 的 第 i 项 的 缺页计数 大于 100：
                热区表 的 第 i 项 的 常驻建议 为 真。
            完。
        完。
        i 为 i 加 1。
    完。
完。

AI页面_热区数 接受 零参：
    返还 做 计数 用 热区表。
完。

AI页面_需要常驻的热区 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 热区表：
        若 热区表 的 第 i 项 的 常驻建议：
            做 追加 用 结果、热区表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

AI页面_热区统计 接受 零参：
    可变 总缺页 为 0。
    可变 常驻数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 热区表：
        总缺页 为 总缺页 加 热区表 的 第 i 项 的 缺页计数。
        若 热区表 的 第 i 项 的 常驻建议：
            常驻数 为 常驻数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一本字典：热区数 对应 做 计数 用 热区表、总缺页 对应 总缺页、常驻建议数 对应 常驻数。
完。

MADV_NORMAL 为 0。

MADV_RANDOM 为 1。

MADV_SEQUENTIAL 为 2。

MADV_WILLNEED 为 3。

MADV_DONTNEED 为 4。

MADV_FREE 为 8。

MADV_HUGEPAGE 为 14。

MADV_NOHUGEPAGE 为 15。

AI页面_处理madvise 接受 地址、长度、建议：
    起始页 为 地址 右移 12。
    页数 为 长度 加 4095 右移 12。
    若 建议 等于 MADV_SEQUENTIAL：
        当前模式 为 模式_顺序。
        检测步长 为 1。
        预取页数 为 16。
    否则若 建议 等于 MADV_RANDOM：
        当前模式 为 模式_随机。
        检测步长 为 0。
        预取页数 为 0。
    否则若 建议 等于 MADV_WILLNEED：
        可变 i 为 0。
        只要 i 小于 页数：
            做 AI页面_预取单页 用 起始页 加 i 左移 12。
            i 为 i 加 1。
        完。
    否则若 建议 等于 MADV_DONTNEED：
        预取页数 为 0。
    完。
    返还 0。
完。

AI页面_madvise名 接受 建议：
    若 建议 等于 MADV_NORMAL：
        返还 「NORMAL」。
    完。
    若 建议 等于 MADV_RANDOM：
        返还 「RANDOM」。
    完。
    若 建议 等于 MADV_SEQUENTIAL：
        返还 「SEQUENTIAL」。
    完。
    若 建议 等于 MADV_WILLNEED：
        返还 「WILLNEED」。
    完。
    若 建议 等于 MADV_DONTNEED：
        返还 「DONTNEED」。
    完。
    若 建议 等于 MADV_FREE：
        返还 「FREE」。
    完。
    若 建议 等于 MADV_HUGEPAGE：
        返还 「HUGEPAGE」。
    完。
    返还 「UNKNOWN」。
完。

可变 预取_限流阈值 为 1024。

可变 预取_当前限流 为 假。

AI页面_kswapd通知 接受 内存压力：
    若 内存压力 大于或等于 3：
        预取_当前限流 为 真。
        预取页数 为 0。
    否则若 内存压力 大于或等于 2：
        预取_当前限流 为 真。
        预取页数 为 1。
    否则：
        预取_当前限流 为 假。
    完。
完。

AI页面_允许预取 接受 零参：
    若 预取_当前限流：
        返还 假。
    完。
    若 预取_总页数 大于 预取_限流阈值 并且 预取_成功页数 乘 2 小于 预取_总页数：
        返还 假。
    完。
    返还 真。
完。

AI页面_设置限流阈值 接受 阈值：
    若 阈值 大于 0：
        预取_限流阈值 为 阈值。
    完。
完。

AI页面_重置 接受 零参：
    缺页历史 为 一组：（空）。
    当前模式 为 模式_随机。
    检测步长 为 0。
    预取页数 为 2。
    预取命中 为 0。
    预取未命中 为 0。
    命中率EMA 为 512。
    上次预测页 为 0。
    热区_起始 为 0。
    热区_结束 为 0。
    热区_缺页数 为 0。
    预取_总页数 为 0。
    预取_成功页数 为 0。
    预取_失败页数 为 0。
    预取_当前限流 为 假。
    热区表 为 一组：（空）。
完。

AI页面_打印状态 接受 零参：
    做 内核信息 用 「AI预取: 模式=」 加 做 AI页面_模式名 用 当前模式 加 「 深度=」 加 做 整数转字符串 用 预取页数 加 「 命中率=」 加 做 整数转字符串 用 做 AI页面_命中率 加 「%」 加 「 历史=」 加 做 整数转字符串 用 做 计数 用 缺页历史。
    做 内核信息 用 「  预取: 总=」 加 做 整数转字符串 用 预取_总页数 加 「 成功=」 加 做 整数转字符串 用 预取_成功页数 加 「 限流=」 加 若 预取_当前限流 则 「是」 否则 「否」。
    做 内核信息 用 「  热区: 」 加 做 整数转字符串 用 做 计数 用 热区表 加 「个」 加 「 缺页=」 加 做 整数转字符串 用 热区_缺页数。
完。

AI页面_摘要 接受 零参：
    返还 做 AI页面_模式名 用 当前模式 加 「 深度」 加 做 整数转字符串 用 预取页数 加 「 命中」 加 做 整数转字符串 用 做 AI页面_命中率 加 「%」。
完。

本章 收束。
