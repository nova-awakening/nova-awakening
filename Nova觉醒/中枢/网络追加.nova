《网络追加》

引用《.·类型定义》。

引用《.·错误码》。

网络精度 为 1000。

网络_追加 接受 lst、val：
    做 插入 用 lst、做 计数 用 lst、val。
完。

网络_开方 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 r 为 1。
    只要 r 乘 r 小于 n：
        r 为 r 加 1。
    完。
    返还 r。
完。

全连接_创建 接受 输入维、输出维：
    可变 权重 为 一组：输出维、输入维。
    可变 种子 为 12345。
    可变 i 为 0。
    只要 i 小于 输出维 乘 输入维：
        种子 为 种子 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        w 为 种子 取余于 200 减 100。
        做 网络_追加 用 权重、w。
        i 为 i 加 1。
    完。
    可变 偏置 为 一组：（空）。
    i 为 0。
    只要 i 小于 输出维：
        做 网络_追加 用 偏置、0。
        i 为 i 加 1。
    完。
    返还 一组：输入维、输出维、权重、偏置、一组：（空）、一组：（空）。
完。

全连接_前向 接受 层、输入：
    输出维 为 层 的 第 1 项。
    输入维 为 层 的 第 0 项。
    权重 为 层 的 第 2 项。
    偏置 为 层 的 第 3 项。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 输出维：
        可变 和 为 偏置 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 输入维：
            和 为 和 加 权重 的 第 2 加 i 乘 输入维 加 j 项 乘 输入 的 第 j 项 除以 网络精度。
            j 为 j 加 1。
        完。
        做 网络_追加 用 输出、和。
        i 为 i 加 1。
    完。
    层 的 第 4 项 为 输入。
    层 的 第 5 项 为 输出。
    返还 输出。
完。

全连接_反向 接受 层、输出梯度：
    输出维 为 层 的 第 1 项。
    输入维 为 层 的 第 0 项。
    权重 为 层 的 第 2 项。
    输入 为 层 的 第 4 项。
    可变 输入梯度 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 输入维：
        可变 和 为 0。
        可变 i 为 0。
        只要 i 小于 输出维：
            和 为 和 加 权重 的 第 2 加 i 乘 输入维 加 j 项 乘 输出梯度 的 第 i 项 除以 网络精度。
            i 为 i 加 1。
        完。
        做 网络_追加 用 输入梯度、和。
        j 为 j 加 1。
    完。
    可变 权重梯度 为 一组：（空）。
    可变 i2 为 0。
    只要 i2 小于 输出维：
        j 为 0。
        只要 j 小于 输入维：
            做 网络_追加 用 权重梯度、输出梯度 的 第 i2 项 乘 输入 的 第 j 项 除以 网络精度。
            j 为 j 加 1。
        完。
        i2 为 i2 加 1。
    完。
    返还 一组：输入梯度、权重梯度、输出梯度。
完。

全连接_更新 接受 层、权重梯度、偏置梯度、学习率：
    输出维 为 层 的 第 1 项。
    输入维 为 层 的 第 0 项。
    权重 为 层 的 第 2 项。
    偏置 为 层 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 输出维 乘 输入维：
        权重 的 第 2 加 i 项 为 权重 的 第 2 加 i 项 减 学习率 乘 权重梯度 的 第 i 项 除以 网络精度。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 输出维：
        偏置 的 第 i 项 为 偏置 的 第 i 项 减 学习率 乘 偏置梯度 的 第 i 项 除以 网络精度。
        i 为 i 加 1。
    完。
完。

本章 收束。
