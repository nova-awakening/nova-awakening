《辅助有符号读》

引用《..·中枢·系统调用表》。

引用《..·推理·通信渠道》。

引用《..·感知·内核直连层》。

引用《.·内核打印》。

引用《..·执行·平台时间》。

引用《..·推理·定点Q算术》。

引用《.·内存初始化》。

引用《..·推理·AI鼠标事件处理》。

引用《.·整数转字符串》。

可变 辅助_类型检查数 为 0。

可变 辅助_类型转换数 为 0。

可变 辅助_值创建数 为 0。

可变 辅助_内存读取数 为 0。

内存_读16_有符号 接受 地址：
    辅助_内存读取数 为 辅助_内存读取数 加 1。
    v 为 做 内存_读16 用 地址。
    若 v 大于或等于 0x8000：
        返还 v 减 0x10000。
    完。
    返还 v。
完。

内存_读8_有符号 接受 地址：
    辅助_内存读取数 为 辅助_内存读取数 加 1。
    v 为 做 内存_读8 用 地址。
    若 v 大于或等于 0x80：
        返还 v 减 0x100。
    完。
    返还 v。
完。

内存_读32_有符号 接受 地址：
    辅助_内存读取数 为 辅助_内存读取数 加 1。
    v 为 做 内存_读32 用 地址。
    若 v 大于或等于 0x80000000：
        返还 v 减 0x100000000。
    完。
    返还 v。
完。

内存_读LEB128 接受 地址：
    辅助_内存读取数 为 辅助_内存读取数 加 1。
    可变 结果 为 0。
    可变 移位 为 0。
    可变 pos 为 地址。
    可变 字节 为 0。
    只要 真：
        字节 为 做 内存_读8 用 pos。
        结果 为 结果 位或 (字节 位与 0x7F) 左移 移位。
        pos 为 pos 加 1。
        移位 为 移位 加 7。
        若 字节 位与 0x80 等于 0：
            跳出。
        完。
        若 移位 大于或等于 35：
            跳出。
        完。
    完。
    返还 一组：结果、pos 减 地址。
完。

内存_读SLEB128 接受 地址：
    辅助_内存读取数 为 辅助_内存读取数 加 1。
    可变 结果 为 0。
    可变 移位 为 0。
    可变 pos 为 地址。
    可变 字节 为 0。
    只要 真：
        字节 为 做 内存_读8 用 pos。
        结果 为 结果 位或 (字节 位与 0x7F) 左移 移位。
        pos 为 pos 加 1。
        移位 为 移位 加 7。
        若 字节 位与 0x80 等于 0：
            跳出。
        完。
        若 移位 大于或等于 35：
            跳出。
        完。
    完。
    若 移位 小于 64 并且 字节 位与 0x40 不等于 0：
        结果 为 结果 位或 0 减 (1 左移 移位)。
    完。
    返还 一组：结果、pos 减 地址。
完。

JSValue_获取类型 接受 v：
    若 v 等于 0：
        返还 JS类型_Undefined。
    完。
    返还 做 内存_读32 用 v。
完。

JSValue_获取字符串 接受 v：
    若 v 等于 0：
        返还 0。
    完。
    若 做 内存_读32 用 v 不等于 JS类型_String：
        返还 0。
    完。
    返还 做 内存_读64 用 v 加 24。
完。

JSValue_获取数值 接受 v：
    若 v 等于 0：
        返还 0。
    完。
    若 做 内存_读32 用 v 不等于 JS类型_Number：
        返还 0。
    完。
    返还 做 内存_读64 用 v 加 8。
完。

JSValue_获取布尔 接受 v：
    若 v 等于 0：
        返还 假。
    完。
    若 做 内存_读32 用 v 不等于 JS类型_Boolean：
        返还 假。
    完。
    返还 做 内存_读64 用 v 加 8 不等于 0。
完。

JSValue_IsNull 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 v 等于 0：
        返还 真。
    完。
    返还 做 内存_读32 用 v 等于 JS类型_Null。
完。

JSValue_IsUndefined 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 v 等于 0：
        返还 真。
    完。
    返还 做 内存_读32 用 v 等于 JS类型_Undefined。
完。

JSValue_IsNullOrUndefined 接受 v：
    返还 做 JSValue_IsNull 用 v 或者 做 JSValue_IsUndefined 用 v。
完。

JSValue_IsObject 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 v 等于 0：
        返还 假。
    完。
    类型 为 做 内存_读32 用 v。
    返还 类型 大于或等于 JS类型_Object。
完。

JSValue_IsBoolean 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 v 等于 0：
        返还 假。
    完。
    返还 做 内存_读32 用 v 等于 JS类型_Boolean。
完。

JSValue_IsNumber 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 v 等于 0：
        返还 假。
    完。
    返还 做 内存_读32 用 v 等于 JS类型_Number。
完。

JSValue_IsString 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 v 等于 0：
        返还 假。
    完。
    返还 做 内存_读32 用 v 等于 JS类型_String。
完。

JSValue_IsSymbol 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 v 等于 0：
        返还 假。
    完。
    返还 做 内存_读32 用 v 等于 JS类型_Symbol。
完。

JSValue_IsFunction 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 v 等于 0：
        返还 假。
    完。
    类型 为 做 内存_读32 用 v。
    返还 类型 等于 JS类型_Function。
完。

JSValue_IsArray 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 v 等于 0：
        返还 假。
    完。
    类型 为 做 内存_读32 用 v。
    返还 类型 等于 JS类型_Array。
完。

JSValue_ToBoolean 接受 v：
    辅助_类型转换数 为 辅助_类型转换数 加 1。
    若 v 等于 0：
        返还 假。
    完。
    类型 为 做 内存_读32 用 v。
    若 类型 等于 JS类型_Undefined 或者 类型 等于 JS类型_Null：
        返还 假。
    完。
    若 类型 等于 JS类型_Boolean：
        返还 做 内存_读64 用 v 加 8 不等于 0。
    完。
    若 类型 等于 JS类型_Number：
        n 为 做 内存_读64 用 v 加 8。
        返还 n 不等于 0。
    完。
    若 类型 等于 JS类型_String：
        len 为 做 内存_读32 用 v 加 16。
        返还 len 大于 0。
    完。
    返还 真。
完。

JSValue_ToNumber 接受 v：
    辅助_类型转换数 为 辅助_类型转换数 加 1。
    若 v 等于 0：
        返还 0。
    完。
    类型 为 做 内存_读32 用 v。
    若 类型 等于 JS类型_Number：
        返还 做 内存_读64 用 v 加 8。
    完。
    若 类型 等于 JS类型_Boolean：
        若 做 内存_读64 用 v 加 8 不等于 0：
            返还 1。
        完。
        返还 0。
    完。
    若 类型 等于 JS类型_Null：
        返还 0。
    完。
    若 类型 等于 JS类型_Undefined：
        返还 JS_NaN。
    完。
    返还 JS_NaN。
完。

JSValue_ToInt32 接受 v：
    辅助_类型转换数 为 辅助_类型转换数 加 1。
    n 为 做 JSValue_ToNumber 用 v。
    若 n 大于或等于 0x80000000：
        返还 n 减 0x100000000。
    完。
    返还 n 位与 0xFFFFFFFF。
完。

JSValue_ToUint32 接受 v：
    辅助_类型转换数 为 辅助_类型转换数 加 1。
    n 为 做 JSValue_ToNumber 用 v。
    返还 n 位与 0xFFFFFFFF。
完。

JSValue_Typeof 接受 v：
    若 v 等于 0：
        返还 「undefined」。
    完。
    类型 为 做 内存_读32 用 v。
    若 类型 等于 JS类型_Undefined：
        返还 「undefined」。
    完。
    若 类型 等于 JS类型_Null：
        返还 「object」。
    完。
    若 类型 等于 JS类型_Boolean：
        返还 「boolean」。
    完。
    若 类型 等于 JS类型_Number：
        返还 「number」。
    完。
    若 类型 等于 JS类型_String：
        返还 「string」。
    完。
    若 类型 等于 JS类型_Symbol：
        返还 「symbol」。
    完。
    若 类型 等于 JS类型_Function：
        返还 「function」。
    完。
    返还 「object」。
完。

JSValue_StrictEquals 接受 a、b：
    若 a 等于 0 并且 b 等于 0：
        返还 真。
    完。
    若 a 等于 0 或者 b 等于 0：
        返还 假。
    完。
    类型a 为 做 内存_读32 用 a。
    类型b 为 做 内存_读32 用 b。
    若 类型a 不等于 类型b：
        返还 假。
    完。
    若 类型a 等于 JS类型_Undefined 或者 类型a 等于 JS类型_Null：
        返还 真。
    完。
    若 类型a 等于 JS类型_Number：
        返还 做 内存_读64 用 a 加 8 等于 做 内存_读64 用 b 加 8。
    完。
    若 类型a 等于 JS类型_Boolean：
        返还 做 内存_读64 用 a 加 8 等于 做 内存_读64 用 b 加 8。
    完。
    若 类型a 等于 JS类型_String：
        返还 做 JSValue_字符串相等 用 a、b。
    完。
    返还 a 等于 b。
完。

JSValue_字符串相等 接受 a、b：
    len_a 为 做 内存_读32 用 a 加 16。
    len_b 为 做 内存_读32 用 b 加 16。
    若 len_a 不等于 len_b：
        返还 假。
    完。
    ptr_a 为 做 内存_读64 用 a 加 24。
    ptr_b 为 做 内存_读64 用 b 加 24。
    可变 i 为 0。
    只要 i 小于 len_a：
        若 做 内存_读8 用 ptr_a 加 i 不等于 做 内存_读8 用 ptr_b 加 i：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

JSValue_创建数值 接受 n：
    辅助_值创建数 为 辅助_值创建数 加 1。
    v 为 做 堆_分配 用 32。
    做 内存_写32 用 v、JS类型_Number。
    做 内存_写64 用 v 加 8、n。
    返还 v。
完。

JSValue_创建布尔 接受 b：
    辅助_值创建数 为 辅助_值创建数 加 1。
    v 为 做 堆_分配 用 32。
    做 内存_写32 用 v、JS类型_Boolean。
    做 内存_写64 用 v 加 8、若 b 则 1 否则 0。
    返还 v。
完。

JSValue_创建Null 接受 零参：
    辅助_值创建数 为 辅助_值创建数 加 1。
    v 为 做 堆_分配 用 32。
    做 内存_写32 用 v、JS类型_Null。
    返还 v。
完。

JSValue_创建Undefined 接受 零参：
    辅助_值创建数 为 辅助_值创建数 加 1。
    v 为 做 堆_分配 用 32。
    做 内存_写32 用 v、JS类型_Undefined。
    返还 v。
完。

平台_控制台输出 接受 数据、长度：
    做 系统调用 用 310、数据、长度、0、0、0。
完。

平台_控制台输出行 接受 数据、长度：
    做 平台_控制台输出 用 数据、长度。
    做 平台_控制台输出 用 「\n」、1。
完。

智能_Bytecode_AI初始化 接受 零参：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
完。

智能_Bytecode_AI热路径反馈 接受 函数哈希、执行时间：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还。
    完。
完。

智能_Bytecode_AI应该优化 接受 函数哈希、执行次数、平均时间：
    bus 为 全局AI总线。
    若 bus 等于 0：
        返还 假。
    完。
    特征 为 做 Tensor_创建 用 1、8。
    做 Tensor_写 用 特征、0、0、若 执行次数 大于 2000 则 Q_ONE 否则 执行次数 乘 Q_ONE 除以 2000。
    做 Tensor_写 用 特征、0、1、若 平均时间 大于 1000 则 Q_ONE 否则 平均时间 乘 Q_ONE 除以 1000。
    分数 为 做 AI总线_注意力权重 用 bus、特征。
    做 Tensor_释放 用 特征。
    返还 分数 大于 512。
完。

辅助_统计 接受 零参：
    返还 一组：辅助_类型检查数、辅助_类型转换数、辅助_值创建数、辅助_内存读取数。
完。

辅助_打印统计 接受 零参：
    做 打印行 用 「=== 字节码辅助统计 ===」。
    做 打印行 用 「类型检查: 」 加 做 整数转字符串 用 辅助_类型检查数。
    做 打印行 用 「类型转换: 」 加 做 整数转字符串 用 辅助_类型转换数。
    做 打印行 用 「值创建: 」 加 做 整数转字符串 用 辅助_值创建数。
    做 打印行 用 「内存读取: 」 加 做 整数转字符串 用 辅助_内存读取数。
完。

辅助_重置统计 接受 零参：
    辅助_类型检查数 为 0。
    辅助_类型转换数 为 0。
    辅助_值创建数 为 0。
    辅助_内存读取数 为 0。
完。

辅助_获取类型检查数 接受 零参：
    返还 辅助_类型检查数。
完。

辅助_获取类型转换数 接受 零参：
    返还 辅助_类型转换数。
完。

辅助_获取值创建数 接受 零参：
    返还 辅助_值创建数。
完。

辅助_获取内存读取数 接受 零参：
    返还 辅助_内存读取数。
完。

JSValue_IsCallable 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 v 等于 0：
        返还 假。
    完。
    类型 为 做 内存_读32 用 v。
    返还 类型 等于 JS类型_Function。
完。

JSValue_IsConstructor 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 v 等于 0：
        返还 假。
    完。
    类型 为 做 内存_读32 用 v。
    若 类型 不等于 JS类型_Function：
        返还 假。
    完。
    标志 为 做 内存_读32 用 v 加 4。
    返还 标志 位与 0x02 不等于 0。
完。

JSValue_IsNaN 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 v 等于 0：
        返还 假。
    完。
    若 做 内存_读32 用 v 不等于 JS类型_Number：
        返还 假。
    完。
    n 为 做 内存_读64 用 v 加 8。
    返还 n 等于 JS_NaN。
完。

JSValue_IsFinite 接受 v：
    辅助_类型检查数 为 辅助_类型检查数 加 1。
    若 非 做 JSValue_IsNumber 用 v：
        返还 假。
    完。
    n 为 做 内存_读64 用 v 加 8。
    返还 n 不等于 JS_NaN 并且 n 不等于 JS_Infinity 并且 n 不等于 JS_NegInfinity。
完。

内存_写16_大端 接受 addr、val：
    做 内存_写68 用 addr、val 右移 8 位与 0xFF。
    做 内存_写68 用 addr 加 1、val 位与 0xFF。
完。

内存_写32_大端 接受 addr、val：
    做 内存_写68 用 addr、val 右移 24 位与 0xFF。
    做 内存_写68 用 addr 加 1、val 右移 16 位与 0xFF。
    做 内存_写68 用 addr 加 2、val 右移 8 位与 0xFF。
    做 内存_写68 用 addr 加 3、val 位与 0xFF。
完。

内存_读16_大端 接受 addr：
    辅助_内存读取数 为 辅助_内存读取数 加 1。
    返还 做 内存_读8 用 addr 左移 8 位或 做 内存_读8 用 addr 加 1。
完。

内存_读32_大端 接受 addr：
    辅助_内存读取数 为 辅助_内存读取数 加 1。
    返还 做 内存_读8 用 addr 左移 24 位或 做 内存_读8 用 addr 加 1 左移 16 位或 做 内存_读8 用 addr 加 2 左移 8 位或 做 内存_读8 用 addr 加 3。
完。

JSValue_AbstractLessThan 接受 a、b：
    辅助_类型转换数 为 辅助_类型转换数 加 1。
    na 为 做 JSValue_ToNumber 用 a。
    nb 为 做 JSValue_ToNumber 用 b。
    若 na 等于 JS_NaN 或者 nb 等于 JS_NaN：
        返还 假。
    完。
    返还 na 小于 nb。
完。

JSValue_AbstractEquals 接受 a、b：
    辅助_类型转换数 为 辅助_类型转换数 加 1。
    若 做 JSValue_StrictEquals 用 a、b：
        返还 真。
    完。
    若 做 JSValue_IsNull 用 a 并且 做 JSValue_IsUndefined 用 b：
        返还 真。
    完。
    若 做 JSValue_IsUndefined 用 a 并且 做 JSValue_IsNull 用 b：
        返还 真。
    完。
    若 做 JSValue_IsNumber 用 a 并且 做 JSValue_IsNumber 用 b：
        返还 做 JSValue_ToNumber 用 a 等于 做 JSValue_ToNumber 用 b。
    完。
    返还 假。
完。

可变 _缓存_小整数 为 一组：（空）。

可变 _缓存_已初始化 为 假。

JSValue_缓存整数_初始化 接受 零参：
    若 _缓存_已初始化：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 256：
        做 追加 用 _缓存_小整数、做 JSValue_创建数值 用 i 减 128。
        i 为 i 加 1。
    完。
    _缓存_已初始化 为 真。
完。

JSValue_创建缓存数值 接受 n：
    做 JSValue_缓存整数_初始化。
    若 n 大于或等于 负 128 并且 n 小于或等于 127：
        返还 _缓存_小整数 的 第 n 加 128 项。
    完。
    返还 做 JSValue_创建数值 用 n。
完。

辅助_完整诊断 接受 零参：
    返还 一组：「类型检查」、辅助_类型检查数、「类型转换」、辅助_类型转换数、「值创建」、辅助_值创建数、「内存读取」、辅助_内存读取数、「NBCA字节码辅助」、1。
完。

本章 收束。
