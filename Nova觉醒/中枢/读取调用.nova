《读取调用》

引用《.·系统调用表》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·进程任务》。

引用《..·记忆·文件描述符》。

引用《..·记忆·文件文件》。

引用《..·记忆·路径查找》。

引用《.·硬链接》。

引用《.·字符串替换》。

引用《..·稳态·键盘》。

引用《..·记忆·文件节点创建》。

引用《..·记忆·ramfs文件创建》。

引用《..·记忆·目录项添加》。

引用《.·自动扩容》。

引用《..·记忆·VFS读写》。

引用《..·记忆·VFS文件操作》。

引用《.·释放文件表》。

引用《.·网络系统调用》。

引用《.·每处理器》。

引用《..·记忆·VFS文件读写》。

引用《.·内存系统调用》。

AT_FDCWD 为 负 100。

sys_read 接受 fd、缓冲、计数、_d、_e、_f：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    若 计数 等于 0：
        返还 0。
    完。
    若 fd 等于 0：
        返还 做 控制台_读取 用 缓冲、计数。
    完。
    返还 做 vfs_read 用 文件、缓冲、计数。
完。

sys_write 接受 fd、缓冲、计数、_d、_e、_f：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_坏文件描述符。
    完。
    若 计数 等于 0：
        返还 0。
    完。
    若 fd 等于 1 或者 fd 等于 2：
        做 控制台_写入 用 缓冲、计数。
        返还 计数。
    完。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    返还 做 vfs_write 用 文件、缓冲、计数。
完。

sys_open 接受 路径、标志、模式、_d、_e、_f：
    返还 做 do_sys_openat 用 AT_FDCWD、路径、标志、模式。
完。

sys_openat 接受 dirfd、路径、标志、模式、_e、_f：
    返还 做 do_sys_openat 用 dirfd、路径、标志、模式。
完。

sys_creat 接受 路径、模式、_c、_d、_e、_f：
    返还 做 do_sys_openat 用 AT_FDCWD、路径、O_CREAT 位或 O_WRONLY 位或 O_TRUNC、模式。
完。

do_sys_openat 接受 dirfd、路径、标志、模式：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件表 为 做 进程_文件表 用 当前。
    fd 为 做 文件表_分配 用 文件表。
    若 fd 小于 0：
        返还 错误_打开过多。
    完。
    可变 根 为 做 获取根目录项。
    若 dirfd 不等于 AT_FDCWD 并且 做 字符串取字符 用 路径、0 不等于 「/」：
        dir文件 为 做 进程_获取文件 用 当前、dirfd。
        若 dir文件 等于 空：
            返还 错误_坏文件描述符。
        完。
        根 为 做 文件_Nova路径项 用 dir文件。
    完。
    可变 查找标志 为 0。
    若 标志 位与 O_CREAT 不等于 0：
        查找标志 为 查找标志 位或 LOOKUP_CREATE。
    完。
    若 标志 位与 O_DIRECTORY 不等于 0：
        查找标志 为 查找标志 位或 LOOKUP_DIRECTORY。
    完。
    若 标志 位与 O_NOFOLLOW 等于 0：
        查找标志 为 查找标志 位或 LOOKUP_FOLLOW。
    完。
    目录项 为 做 路径查找_带标志 用 根、路径、查找标志。
    若 目录项 等于 空：
        若 标志 位与 O_CREAT 等于 0：
            返还 错误_不存在。
        完。
        创建结果 为 做 vfs_创建文件 用 路径、模式。
        若 创建结果 小于 0：
            返还 创建结果。
        完。
        目录项 为 做 路径查找 用 根、路径。
        若 目录项 等于 空：
            返还 错误_不存在。
        完。
    否则：
        若 标志 位与 O_CREAT 不等于 0 并且 标志 位与 O_EXCL 不等于 0：
            返还 错误_已存在。
        完。
    完。
    节点 为 做 目录项_Nova文件节点 用 目录项。
    若 节点 等于 空：
        返还 错误_不存在。
    完。
    若 标志 位与 O_DIRECTORY 不等于 0 并且 非 做 Nova节点_是目录 用 节点：
        返还 错误_非目录。
    完。
    若 标志 位与 O_TRUNC 不等于 0 并且 做 Nova节点_是文件 用 节点：
        做 Nova文件节点_设置大小 用 节点、0。
    完。
    文件 为 做 文件对象_创建 用 节点、目录项、标志。
    若 文件 等于 空：
        返还 错误_内存不足。
    完。
    操作 为 做 文件_操作表 用 文件。
    若 操作 不等于 空 并且 操作 的 打开 不等于 空：
        结果 为 做 操作 的 打开 用 节点、文件。
        若 结果 不等于 成功：
            返还 结果。
        完。
    完。
    做 文件表_设置 用 文件表、fd、文件。
    返还 fd。
完。

sys_close 接受 fd、_b、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_坏文件描述符。
    完。
    返还 做 系统_close 用 fd。
完。

sys_lseek 接受 fd、偏移、起点、_d、_e、_f：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    返还 做 vfs_lseek 用 文件、偏移、起点。
完。

sys_stat 接受 路径、statbuf、_c、_d、_e、_f：
    根 为 做 获取根目录项。
    目录项 为 做 路径查找_带标志 用 根、路径、LOOKUP_FOLLOW。
    若 目录项 等于 空：
        返还 错误_不存在。
    完。
    节点 为 做 目录项_Nova文件节点 用 目录项。
    若 节点 等于 空：
        返还 错误_不存在。
    完。
    返还 做 填充stat 用 节点、statbuf。
完。

sys_lstat 接受 路径、statbuf、_c、_d、_e、_f：
    根 为 做 获取根目录项。
    目录项 为 做 路径查找_带标志 用 根、路径、0。
    若 目录项 等于 空：
        返还 错误_不存在。
    完。
    节点 为 做 目录项_Nova文件节点 用 目录项。
    若 节点 等于 空：
        返还 错误_不存在。
    完。
    返还 做 填充stat 用 节点、statbuf。
完。

sys_fstat 接受 fd、statbuf、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    返还 做 填充stat 用 做 文件_Nova文件节点 用 文件、statbuf。
完。

填充stat 接受 节点、buf：
    若 节点 等于 空：
        返还 错误_无效参数。
    完。
    若 buf 不等于 空：
        做 设置 用 buf、0、0。
        做 设置 用 buf、1、做 Nova文件节点_编号 用 节点。
        做 设置 用 buf、2、做 Nova文件节点_模式 用 节点。
        做 设置 用 buf、3、做 Nova文件节点_链接数 用 节点。
        做 设置 用 buf、4、做 Nova文件节点_uid 用 节点。
        做 设置 用 buf、5、做 Nova文件节点_gid 用 节点。
        做 设置 用 buf、6、0。
        做 设置 用 buf、7、做 Nova文件节点_大小 用 节点。
        做 设置 用 buf、8、4096。
        做 设置 用 buf、9、做 Nova文件节点_大小 用 节点 除以 512。
        做 设置 用 buf、10、做 Nova文件节点_atime 用 节点。
        做 设置 用 buf、11、做 Nova文件节点_mtime 用 节点。
        做 设置 用 buf、12、做 Nova文件节点_ctime 用 节点。
    完。
    返还 成功。
完。

可变 pread_次数 为 0。

可变 pwrite_次数 为 0。

可变 readv_次数 为 0。

可变 writev_次数 为 0。

可变 lseek_次数 为 0。

可变 fcntl_次数 为 0。

可变 文件IO_已初始化 为 假。

可变 文件IO_错误数 为 0。

可变 文件IO_延迟总计 为 0。

可变 文件IO_延迟样本 为 0。

可变 文件IO_最大延迟 为 0。

可变 文件IO_速率 为 0。

可变 文件IO_速率窗口 为 0。

可变 文件IO_速率时刻 为 0。

可变 文件IO_峰值速率 为 0。

sys_pread64 接受 fd、buf、count、offset、_e、_f：
    若 fd 小于 0 或者 buf 等于 空：
        文件IO_错误数 为 文件IO_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取打开文件 用 当前、fd。
    若 文件 等于 空：
        文件IO_错误数 为 文件IO_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    结果 为 做 VFS_在偏移读 用 文件、buf、count、offset。
    pread_次数 为 pread_次数 加 1。
    文件IO_速率窗口 为 文件IO_速率窗口 加 1。
    做 文件IO_更新速率。
    返还 结果。
完。

sys_pwrite64 接受 fd、buf、count、offset、_e、_f：
    若 fd 小于 0 或者 buf 等于 空：
        文件IO_错误数 为 文件IO_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取打开文件 用 当前、fd。
    若 文件 等于 空：
        文件IO_错误数 为 文件IO_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    结果 为 做 VFS_在偏移写 用 文件、buf、count、offset。
    pwrite_次数 为 pwrite_次数 加 1。
    文件IO_速率窗口 为 文件IO_速率窗口 加 1。
    做 文件IO_更新速率。
    返还 结果。
完。

sys_readv 接受 fd、iov、iovcnt、_d、_e、_f：
    若 fd 小于 0 或者 iovcnt 小于或等于 0：
        文件IO_错误数 为 文件IO_错误数 加 1。
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取打开文件 用 当前、fd。
    若 文件 等于 空：
        文件IO_错误数 为 文件IO_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    可变 总读取 为 0。
    可变 i 为 0。
    只要 i 小于 iovcnt：
        base 为 做 内存_读64 用 iov 加 i 乘 16。
        len 为 做 内存_读64 用 iov 加 i 乘 16 加 8。
        若 base 不等于 0 并且 len 大于 0：
            已读 为 做 VFS_读取 用 文件、base、len。
            若 已读 大于 0：
                总读取 为 总读取 加 已读。
            完。
            若 已读 小于 len：
                i 为 iovcnt。
            完。
        完。
        i 为 i 加 1。
    完。
    readv_次数 为 readv_次数 加 1。
    文件IO_速率窗口 为 文件IO_速率窗口 加 1。
    做 文件IO_更新速率。
    返还 总读取。
完。

sys_writev 接受 fd、iov、iovcnt、_d、_e、_f：
    若 fd 小于 0 或者 iovcnt 小于或等于 0：
        文件IO_错误数 为 文件IO_错误数 加 1。
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取打开文件 用 当前、fd。
    若 文件 等于 空：
        文件IO_错误数 为 文件IO_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    可变 总写入 为 0。
    可变 i 为 0。
    只要 i 小于 iovcnt：
        base 为 做 内存_读64 用 iov 加 i 乘 16。
        len 为 做 内存_读64 用 iov 加 i 乘 16 加 8。
        若 base 不等于 0 并且 len 大于 0：
            已写 为 做 VFS_写入 用 文件、base、len。
            若 已写 大于 0：
                总写入 为 总写入 加 已写。
            完。
            若 已写 小于 len：
                i 为 iovcnt。
            完。
        完。
        i 为 i 加 1。
    完。
    writev_次数 为 writev_次数 加 1。
    文件IO_速率窗口 为 文件IO_速率窗口 加 1。
    做 文件IO_更新速率。
    返还 总写入。
完。

F_DUPFD 为 0。

F_GETFD 为 1。

F_SETFD 为 2。

F_GETFL 为 3。

F_SETFL 为 4。

F_DUPFD_CLOEXEC 为 1030。

FD_CLOEXEC 为 1。

sys_fcntl 接受 fd、cmd、arg、_d、_e、_f：
    若 fd 小于 0：
        文件IO_错误数 为 文件IO_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取打开文件 用 当前、fd。
    若 文件 等于 空：
        文件IO_错误数 为 文件IO_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    若 cmd 等于 F_DUPFD：
        返还 做 文件表_复制FD 用 当前、fd、arg。
    完。
    若 cmd 等于 F_DUPFD_CLOEXEC：
        新fd 为 做 文件表_复制FD 用 当前、fd、arg。
        若 新fd 大于或等于 0：
            做 文件表_设置cloexec 用 做 进程_文件表 用 当前、新fd。
        完。
        返还 新fd。
    完。
    若 cmd 等于 F_GETFD：
        返还 若 做 文件_cloexec 用 文件 则 FD_CLOEXEC 否则 0。
    完。
    若 cmd 等于 F_SETFD：
        做 文件_设置cloexec 用 文件、arg 位与 FD_CLOEXEC。
        返还 0。
    完。
    若 cmd 等于 F_GETFL：
        返还 做 文件_标志 用 文件。
    完。
    若 cmd 等于 F_SETFL：
        做 文件_设置标志 用 文件、arg。
        返还 0。
    完。
    fcntl_次数 为 fcntl_次数 加 1。
    做 文件IO_更新速率。
    返还 错误_无效参数。
完。

文件IO_记录延迟 接受 延迟：
    文件IO_延迟样本 为 文件IO_延迟样本 加 1。
    文件IO_延迟总计 为 文件IO_延迟总计 加 延迟。
    若 延迟 大于 文件IO_最大延迟：
        文件IO_最大延迟 为 延迟。
    完。
完。

文件IO_平均延迟 接受 零参：
    若 文件IO_延迟样本 等于 0：
        返还 0。
    完。
    返还 文件IO_延迟总计 除以 文件IO_延迟样本。
完。

文件IO_更新速率 接受 零参：
    现在 为 做 读取纳秒。
    间隔 为 现在 减 文件IO_速率时刻。
    若 间隔 大于或等于 1000000000：
        文件IO_速率 为 文件IO_速率窗口 乘 1000000000 除以 间隔。
        若 文件IO_速率 大于 文件IO_峰值速率：
            文件IO_峰值速率 为 文件IO_速率。
        完。
        文件IO_速率窗口 为 0。
        文件IO_速率时刻 为 现在。
    完。
完。

文件读取_完整统计 接受 零参：
    统计 为 做 字典。
    做 字典设 用 统计、「已初始化」、文件IO_已初始化。
    做 字典设 用 统计、「read次数」、read_总次数。
    做 字典设 用 统计、「write次数」、write_总次数。
    做 字典设 用 统计、「读取字节」、read_总字节。
    做 字典设 用 统计、「写入字节」、write_总字节。
    做 字典设 用 统计、「pread次数」、pread_次数。
    做 字典设 用 统计、「pwrite次数」、pwrite_次数。
    做 字典设 用 统计、「readv次数」、readv_次数。
    做 字典设 用 统计、「writev次数」、writev_次数。
    做 字典设 用 统计、「lseek次数」、lseek_次数。
    做 字典设 用 统计、「fcntl次数」、fcntl_次数。
    做 字典设 用 统计、「平均延迟(ns)」、做 文件IO_平均延迟。
    做 字典设 用 统计、「最大延迟(ns)」、文件IO_最大延迟。
    做 字典设 用 统计、「当前速率」、文件IO_速率。
    做 字典设 用 统计、「峰值速率」、文件IO_峰值速率。
    做 字典设 用 统计、「错误数」、文件IO_错误数。
    返还 统计。
完。

文件读取_打印统计 接受 零参：
    统计 为 做 文件读取_完整统计。
    做 打印 用 「[文件读写] ═══════ 完整统计 ═══════」。
    做 打印 用 「  read:         」 加 做 转字符串 用 做 字典取 用 统计、「read次数」。
    做 打印 用 「  write:        」 加 做 转字符串 用 做 字典取 用 统计、「write次数」。
    做 打印 用 「  读取字节:     」 加 做 转字符串 用 做 字典取 用 统计、「读取字节」。
    做 打印 用 「  写入字节:     」 加 做 转字符串 用 做 字典取 用 统计、「写入字节」。
    做 打印 用 「  pread:        」 加 做 转字符串 用 做 字典取 用 统计、「pread次数」。
    做 打印 用 「  pwrite:       」 加 做 转字符串 用 做 字典取 用 统计、「pwrite次数」。
    做 打印 用 「  readv:        」 加 做 转字符串 用 做 字典取 用 统计、「readv次数」。
    做 打印 用 「  writev:       」 加 做 转字符串 用 做 字典取 用 统计、「writev次数」。
    做 打印 用 「  lseek:        」 加 做 转字符串 用 做 字典取 用 统计、「lseek次数」。
    做 打印 用 「  fcntl:        」 加 做 转字符串 用 做 字典取 用 统计、「fcntl次数」。
    做 打印 用 「  平均延迟:     」 加 做 转字符串 用 做 字典取 用 统计、「平均延迟(ns)」 加 「ns」。
    做 打印 用 「  速率:         」 加 做 转字符串 用 做 字典取 用 统计、「当前速率」 加 「 次/秒」。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[文件读写] ═════════════════════════」。
    返还 0。
完。

文件读取_dump 接受 零参：
    做 打印 用 「[文件读写 dump] ──────────────────」。
    做 打印 用 「  read: 」 加 做 转字符串 用 read_总次数 加 「 (」 加 做 转字符串 用 read_总字节 加 「 字节)」。
    做 打印 用 「  write: 」 加 做 转字符串 用 write_总次数 加 「 (」 加 做 转字符串 用 write_总字节 加 「 字节)」。
    做 打印 用 「  pread: 」 加 做 转字符串 用 pread_次数。
    做 打印 用 「  pwrite: 」 加 做 转字符串 用 pwrite_次数。
    做 打印 用 「  readv: 」 加 做 转字符串 用 readv_次数。
    做 打印 用 「  writev: 」 加 做 转字符串 用 writev_次数。
    做 打印 用 「  速率: 」 加 做 转字符串 用 文件IO_速率。
    做 打印 用 「  错误: 」 加 做 转字符串 用 文件IO_错误数。
    做 打印 用 「[文件读写 dump] ──────────────────」。
完。

文件读取_销毁 接受 零参：
    文件IO_已初始化 为 假。
    返还 0。
完。

文件读取_为procfs提供统计 接受 零参：
    返还 一组：read_总次数、write_总次数、read_总字节、write_总字节、pread_次数、pwrite_次数、readv_次数、writev_次数。
完。

文件读取_为AI提供IO模式 接受 零参：
    返还 一组：read_总次数、write_总次数、read_总字节、write_总字节、做 文件IO_平均延迟、文件IO_速率。
完。

文件读取_重置统计 接受 零参：
    read_总次数 为 0。
    write_总次数 为 0。
    read_总字节 为 0。
    write_总字节 为 0。
    pread_次数 为 0。
    pwrite_次数 为 0。
    readv_次数 为 0。
    writev_次数 为 0。
    lseek_次数 为 0。
    fcntl_次数 为 0。
    文件IO_错误数 为 0。
    文件IO_延迟总计 为 0。
    文件IO_延迟样本 为 0。
    文件IO_最大延迟 为 0。
    文件IO_速率 为 0。
    文件IO_速率窗口 为 0。
    文件IO_峰值速率 为 0。
    做 打印 用 「[文件读写] 统计已重置」。
    返还 0。
完。

VFS_在偏移读 接受 文件、buf、count、offset：
    返还 做 vfs_pread 用 文件、buf、count、offset。
完。

VFS_在偏移写 接受 文件、buf、count、offset：
    返还 做 vfs_pwrite 用 文件、buf、count、offset。
完。

VFS_读取 接受 文件、buf、count：
    返还 做 vfs_read 用 文件、buf、count。
完。

VFS_写入 接受 文件、buf、count：
    返还 做 vfs_write 用 文件、buf、count。
完。

进程_获取打开文件 接受 进程、fd：
    返还 做 进程_获取文件 用 进程、fd。
完。

文件表_复制FD 接受 进程、旧fd、最小fd：
    若 进程 等于 空：
        返还 错误_坏文件描述符。
    完。
    若 最小fd 小于 0：
        返还 错误_无效参数。
    完。
    文件表 为 做 进程_文件表 用 进程。
    若 文件表 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件 为 做 文件表_获取 用 文件表、旧fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    新fd 为 做 文件表_分配_从 用 文件表、最小fd。
    若 新fd 小于 0：
        返还 新fd。
    完。
    做 文件_增引用 用 文件。
    做 文件表_设置 用 文件表、新fd、文件。
    返还 新fd。
完。

本章 收束。
