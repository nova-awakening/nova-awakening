《进程fork创建》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·感知·内核直连层》。

引用《.·硬链接》。

引用《.·内存初始化》。

引用《.·整数转字符串》。

引用《.·哈希函数》。

引用《.·内容可见性》。

引用《..·引导·端口IO》。

引用《.·获取当前页》。

最大资源限制 为 16。

创建速率窗口 为 32。

进程资源限 是 一种 事物，含 最大文件数、最大内存页、最大栈大小、最大子进程、处理器时限。

可变 总创建数 为 0。

可变 总失败数 为 0。

可变 下一标识 为 1。

可变 写时复制次数 为 0。

可变 创建速率历史 为 0。

可变 历史写位 为 0。

可变 上次速率时刻 为 0。

可变 当前速率 为 0。

进程_分叉 接受 父进程：
    若 父进程 等于 0：
        总失败数 为 总失败数 加 1。
        返还 错误_无效参数。
    完。
    子标识 为 下一标识。
    下一标识 为 下一标识 加 1。
    子进程 为 做 堆_分配 用 进程描述大小。
    若 子进程 等于 0：
        总失败数 为 总失败数 加 1。
        返还 错误_内存不足。
    完。
    做 内存_复制 用 子进程、父进程、进程描述大小。
    做 设置 用 子进程、T_标识、子标识。
    做 设置 用 子进程、T_父标识、父进程 的 第 T_标识 项。
    做 设置 用 子进程、T_状态、进程_就绪。
    做 进程_克隆页表 用 子进程、父进程。
    做 进程_继承资源限 用 子进程、父进程。
    做 进程_继承文件表 用 子进程、父进程。
    做 进程_继承信号表 用 子进程、父进程。
    总创建数 为 总创建数 加 1。
    做 进程_记录创建速率。
    返还 子标识。
完。

进程_克隆页表 接受 子、父：
    父页表基 为 父 的 第 T_页表基址 项。
    若 父页表基 等于 0：
        返还。
    完。
    子页表基 为 做 堆_分配 用 4096。
    做 内存_复制 用 子页表基、父页表基、4096。
    可变 页索引 为 0。
    只要 页索引 小于 512：
        页项 为 做 内存_读64 用 子页表基 加 页索引 乘 8。
        若 页项 不等于 0 并且 页项 位与 1 等于 1：
            新页项 为 页项 位与 按位取反 2 位或 1。
            做 内存_写64 用 子页表基 加 页索引 乘 8、新页项。
            做 内存_写64 用 父页表基 加 页索引 乘 8、新页项。
            写时复制次数 为 写时复制次数 加 1。
        完。
        页索引 为 页索引 加 1。
    完。
    做 设置 用 子、T_页表基址、子页表基。
完。

进程_继承资源限 接受 子、父：
    父限制 为 父 的 第 T_资源限制 项。
    若 父限制 等于 0：
        返还。
    完。
    子限制 为 做 堆_分配 用 进程资源限。
    子限制 的 最大文件数 为 做 内存_读32 用 父限制。
    子限制 的 最大内存页 为 做 内存_读32 用 父限制 加 4。
    子限制 的 最大栈大小 为 做 内存_读32 用 父限制 加 8。
    子限制 的 最大子进程 为 做 内存_读32 用 父限制 加 12。
    子限制 的 处理器时限 为 做 内存_读32 用 父限制 加 16。
    做 设置 用 子、T_资源限制、子限制。
完。

进程_继承文件表 接受 子、父：
    父文件表 为 父 的 第 T_文件表 项。
    若 父文件表 等于 0：
        返还。
    完。
    文件数 为 父 的 第 T_打开文件数 项。
    子文件表 为 做 堆_分配 用 文件数 乘 8。
    可变 序号 为 0。
    只要 序号 小于 文件数：
        文件项 为 做 内存_读64 用 父文件表 加 序号 乘 8。
        做 内存_写64 用 子文件表 加 序号 乘 8、文件项。
        若 文件项 不等于 0：
            做 文件_增引用 用 文件项。
        完。
        序号 为 序号 加 1。
    完。
    做 设置 用 子、T_文件表、子文件表。
    做 设置 用 子、T_打开文件数、文件数。
完。

进程_继承信号表 接受 子、父：
    父信号表 为 父 的 第 T_信号表 项。
    若 父信号表 等于 0：
        返还。
    完。
    子信号表 为 做 堆_分配 用 64 乘 8。
    做 内存_复制 用 子信号表、父信号表、64 乘 8。
    做 设置 用 子、T_信号表、子信号表。
完。

进程_记录创建速率 接受 零参：
    现在 为 做 平台_获取高精度时间。
    若 创建速率历史 等于 0：
        创建速率历史 为 做 堆_分配 用 创建速率窗口 乘 4。
        上次速率时刻 为 现在。
    完。
    做 内存_写32 用 创建速率历史 加 历史写位 乘 4、1。
    历史写位 为 (历史写位 加 1) 取余于 创建速率窗口。
    若 现在 减 上次速率时刻 大于或等于 1000000：
        可变 总 为 0。
        可变 序号 为 0。
        只要 序号 小于 创建速率窗口：
            总 为 总 加 做 内存_读32 用 创建速率历史 加 序号 乘 4。
            序号 为 序号 加 1。
        完。
        当前速率 为 总。
        上次速率时刻 为 现在。
    完。
完。

进程创建_完整统计 接受 零参：
    返还 一组：总创建数、总失败数、下一标识、写时复制次数、当前速率。
完。

可变 exec_调用数 为 0。

可变 exec_失败数 为 0。

进程描述大小 为 512。

T_标识 为 0。

T_父标识 为 1。

T_状态 为 2。

T_页表基址 为 3。

T_资源限制 为 4。

T_文件表 为 5。

T_打开文件数 为 6。

T_信号表 为 7。

进程_就绪 为 1。

进程_exec 接受 当前进程、路径、参数列表、环境列表：
    exec_调用数 为 exec_调用数 加 1。
    fd 为 做 sys_open 用 路径、0、0。
    若 fd 小于 0：
        exec_失败数 为 exec_失败数 加 1。
        返还 错误_文件不存在。
    完。
    头 为 做 分配内存 用 64。
    做 sys_read 用 fd、头、64。
    若 做 内存_读8 用 头 不等于 0x7F 或者 做 内存_读8 用 头 加 1 不等于 0x45 或者 做 内存_读8 用 头 加 2 不等于 0x4C 或者 做 内存_读8 用 头 加 3 不等于 0x46：
        做 释放内存 用 头。
        做 sys_close 用 fd。
        exec_失败数 为 exec_失败数 加 1。
        返还 错误_格式无效。
    完。
    程序头偏移 为 做 内存_读64 用 头 加 32。
    程序头大小 为 做 内存_读16 用 头 加 54。
    程序头数量 为 做 内存_读16 用 头 加 56。
    入口地址 为 做 内存_读64 用 头 加 24。
    做 进程_清除用户空间 用 当前进程。
    可变 i 为 0。
    只要 i 小于 程序头数量：
        ph缓冲 为 做 分配内存 用 程序头大小。
        做 sys_lseek 用 fd、程序头偏移 加 i 乘 程序头大小、0。
        做 sys_read 用 fd、ph缓冲、程序头大小。
        类型 为 做 内存_读32 用 ph缓冲。
        若 类型 等于 1：
            虚拟地址 为 做 内存_读64 用 ph缓冲 加 16。
            文件偏移 为 做 内存_读64 用 ph缓冲 加 8。
            文件大小 为 做 内存_读64 用 ph缓冲 加 32。
            内存大小 为 做 内存_读64 用 ph缓冲 加 40。
            对齐地址 为 虚拟地址 位与 按位取反 0xFFF。
            对齐大小 为 内存大小 加 (虚拟地址 减 对齐地址) 加 0xFFF 位与 按位取反 0xFFF。
            做 进程_映射段 用 当前进程、对齐地址、对齐大小。
            做 sys_lseek 用 fd、文件偏移、0。
            做 sys_read 用 fd、虚拟地址、文件大小。
            若 内存大小 大于 文件大小：
                做 内存_置零 用 虚拟地址 加 文件大小、内存大小 减 文件大小。
            完。
        完。
        做 释放内存 用 ph缓冲。
        i 为 i 加 1。
    完。
    栈顶 为 0x7FFFFFFFE000。
    栈大小 为 8 乘 1024 乘 1024。
    做 进程_映射段 用 当前进程、栈顶 减 栈大小、栈大小。
    可变 SP 为 栈顶 减 8。
    envp数 为 做 计数 用 环境列表。
    可变 ei 为 envp数 减 1。
    只要 ei 大于或等于 0：
        SP 为 SP 减 做 字符串长度 用 环境列表 的 第 ei 项 减 1。
        做 内存拷贝 用 SP、环境列表 的 第 ei 项、做 字符串长度 用 环境列表 的 第 ei 项 加 1。
        ei 为 ei 减 1。
    完。
    argc 为 做 计数 用 参数列表。
    可变 ai 为 argc 减 1。
    只要 ai 大于或等于 0：
        SP 为 SP 减 做 字符串长度 用 参数列表 的 第 ai 项 减 1。
        做 内存拷贝 用 SP、参数列表 的 第 ai 项、做 字符串长度 用 参数列表 的 第 ai 项 加 1。
        ai 为 ai 减 1。
    完。
    当前进程 的 入口地址 为 入口地址。
    当前进程 的 用户栈指针 为 SP。
    当前进程 的 寄存器 的 RIP 为 入口地址。
    当前进程 的 寄存器 的 RSP 为 SP。
    做 释放内存 用 头。
    做 sys_close 用 fd。
    返还 成功。
完。

进程_清除用户空间 接受 进程：
    若 进程 等于 0：
        返还。
    完。
    可变 vma 为 进程 的 vma头。
    只要 vma 不等于 0：
        下一个 为 做 内存_读64 用 vma 加 16。
        做 堆_释放 用 vma。
        vma 为 下一个。
    完。
    进程 的 vma头 为 0。
完。

进程_映射段 接受 进程、地址、大小：
    若 进程 等于 0 或者 大小 等于 0：
        返还 负 1。
    完。
    页数 为 (大小 加 4095) 除以 4096。
    可变 i 为 0。
    只要 i 小于 页数：
        物理页 为 做 物理页_分配 用 1。
        若 物理页 不等于 0：
            做 页表_映射 用 进程 的 PML4、地址 加 i 乘 4096、物理页、7。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

文件_增引用 接受 文件项：
    若 文件项 不等于 0：
        文件项 的 引用数 为 文件项 的 引用数 加 1。
    完。
完。

sys_open 接受 路径、标志、模式：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_无效参数。
    完。
    文件表 为 做 进程_文件表 用 当前。
    若 文件表 等于 空：
        返还 错误_打开过多。
    完。
    文件 为 做 vfs_open 用 路径、标志、模式。
    若 文件 等于 空 或者 文件 小于 0：
        返还 文件。
    完。
    fd 为 做 文件表_分配 用 文件表。
    若 fd 小于 0：
        返还 错误_打开过多。
    完。
    做 文件表_设置 用 文件表、fd、文件。
    返还 fd。
完。

sys_read 接受 fd、缓冲、大小：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    返还 做 vfs_read 用 文件、缓冲、大小。
完。

sys_close 接受 fd：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件表 为 做 进程_文件表 用 当前。
    若 文件表 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    做 文件表_关闭 用 文件表、fd。
    剩余 为 做 文件_减引用 用 文件。
    若 剩余 小于或等于 0：
        操作 为 文件 的 第 F_操作 项。
        若 操作 不等于 空 并且 操作 的 释放 不等于 空：
            做 操作 的 释放 用 文件 的 第 F_Nova文件节点 项、文件。
        完。
    完。
    返还 成功。
完。

sys_lseek 接受 fd、偏移、基准：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    返还 做 vfs_lseek 用 文件、偏移、基准。
完。

sys_write 接受 fd、数据、大小：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    返还 做 vfs_write 用 文件、数据、大小。
完。

错误_文件不存在 为 负 2。

错误_格式无效 为 负 8。

错误_无效参数 为 负 22。

错误_内存不足 为 负 12。

可变 AI_创建模式历史 为 一组：（空）。

AI_进程_记录创建 接受 PID、父PID：
    做 追加 用 AI_创建模式历史、一本字典：PID 对应 PID、父PID 对应 父PID、时间 对应 做 平台_获取高精度时间。
    若 做 计数 用 AI_创建模式历史 大于 128：
        AI_创建模式历史 为 做 子列表 用 AI_创建模式历史、64、128。
    完。
完。

AI_进程_检测fork炸弹 接受 零参：
    n 为 做 计数 用 AI_创建模式历史。
    若 n 小于 10：
        返还 假。
    完。
    可变 同父计数 为 0。
    最近父 为 AI_创建模式历史 的 第 n 减 1 项 的 父PID。
    可变 i 为 n 减 1。
    只要 i 大于或等于 0 并且 i 大于 n 减 10：
        若 AI_创建模式历史 的 第 i 项 的 父PID 等于 最近父：
            同父计数 为 同父计数 加 1。
        完。
        i 为 i 减 1。
    完。
    若 同父计数 大于 8：
        做 内核警告 用 「[AI] 疑似fork炸弹: 父PID=」 加 做 整数转字符串 用 最近父。
        返还 真。
    完。
    返还 假。
完。

进程创建_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 总失败数 大于 总创建数 除以 5 并且 总创建数 大于 20：
        做 追加 用 问题、「创建失败率高: 」 加 做 整数转字符串 用 总失败数 乘 100 除以 总创建数 加 「%」。
    完。
    若 当前速率 大于 100：
        做 追加 用 问题、「创建速率异常: 」 加 做 整数转字符串 用 当前速率 加 「/s」。
    完。
    若 做 AI_进程_检测fork炸弹：
        做 追加 用 问题、「疑似fork炸弹」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「进程创建健康」。
    完。
    返还 问题。
完。

本章 收束。
