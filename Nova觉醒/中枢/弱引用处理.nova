《弱引用处理》

引用《..·中枢·系统调用表》。

引用《..·推理·通信渠道》。

引用《..·感知·内核直连层》。

引用《.·字符串替换》。

引用《.·内存初始化》。

引用《.·内容可见性》。

引用《..·执行·底层内存操》。

标记_白色 为 0。

标记_灰色 为 1。

标记_黑色 为 2。

对象头_标记偏移 为 8。

对象头_大小偏移 为 10。

弱引用_初始容量 为 512。

弱引用_最大容量 为 65536。

终结_初始容量 为 128。

终结_最大容量 为 8192。

弱映射_桶数 为 256。

弱映射_最大载荷率 为 75。

弱集合_桶数 为 128。

终结_回调批量 为 32。

AI_存活预测_窗口 为 16。

弱引用状态_活跃 为 0。

弱引用状态_已清除 为 1。

弱引用状态_已销毁 为 2。

弱引用项 是 一种 事物，含 目标对象、回调函数、持有值、状态、创建时间、后继。

终结注册项 是 一种 事物，含 目标对象、回调函数、持有值、注销令牌、状态、注册时间。

弱映射桶 是 一种 事物，含 键对象、值对象、哈希值、活跃、后继。

弱集合桶 是 一种 事物，含 元素对象、哈希值、活跃、后继。

弱引用管理器 是 一种 事物，含 弱引用链头、弱引用数、弱引用活跃数、终结表基址、终结表大小、终结表容量、终结待执行队列、终结待执行数、弱映射表基址、弱映射桶数、弱映射元素数、弱集合表基址、弱集合桶数、弱集合元素数、AI存活历史、AI历史游标、AI预测存活率、AI批量化终结、统计_弱引用创建数、统计_弱引用清除数、统计_终结注册数、统计_终结执行数、统计_终结注销数、统计_弱映射设置数、统计_弱映射清除数、统计_弱集合添加数、统计_弱集合清除数、统计_处理轮数、统计_总处理时间。

弱引用管理器_创建 接受 零参：
    可变 管理器 为 做 堆_分配 用 弱引用管理器。
    若 管理器 等于 0：
        做 打印 用 「[弱引用] 错误: 管理器分配失败」。
        返还 空。
    完。
    管理器 的 弱引用链头 为 0。
    管理器 的 弱引用数 为 0。
    管理器 的 弱引用活跃数 为 0。
    管理器 的 终结表容量 为 终结_初始容量。
    管理器 的 终结表基址 为 做 平台_申请内存 用 管理器 的 终结表容量 乘 48。
    若 管理器 的 终结表基址 不等于 0：
        做 内存_清零 用 管理器 的 终结表基址、管理器 的 终结表容量 乘 48。
    完。
    管理器 的 终结表大小 为 0。
    管理器 的 终结待执行队列 为 0。
    管理器 的 终结待执行数 为 0。
    管理器 的 弱映射桶数 为 弱映射_桶数。
    管理器 的 弱映射表基址 为 做 平台_申请内存 用 管理器 的 弱映射桶数 乘 8。
    若 管理器 的 弱映射表基址 不等于 0：
        做 内存_清零 用 管理器 的 弱映射表基址、管理器 的 弱映射桶数 乘 8。
    完。
    管理器 的 弱映射元素数 为 0。
    管理器 的 弱集合桶数 为 弱集合_桶数。
    管理器 的 弱集合表基址 为 做 平台_申请内存 用 管理器 的 弱集合桶数 乘 8。
    若 管理器 的 弱集合表基址 不等于 0：
        做 内存_清零 用 管理器 的 弱集合表基址、管理器 的 弱集合桶数 乘 8。
    完。
    管理器 的 弱集合元素数 为 0。
    管理器 的 AI存活历史 为 做 平台_申请内存 用 AI_存活预测_窗口 乘 4。
    若 管理器 的 AI存活历史 不等于 0：
        做 内存_清零 用 管理器 的 AI存活历史、AI_存活预测_窗口 乘 4。
    完。
    管理器 的 AI历史游标 为 0。
    管理器 的 AI预测存活率 为 5000。
    管理器 的 AI批量化终结 为 假。
    管理器 的 统计_弱引用创建数 为 0。
    管理器 的 统计_弱引用清除数 为 0。
    管理器 的 统计_终结注册数 为 0。
    管理器 的 统计_终结执行数 为 0。
    管理器 的 统计_终结注销数 为 0。
    管理器 的 统计_弱映射设置数 为 0。
    管理器 的 统计_弱映射清除数 为 0。
    管理器 的 统计_弱集合添加数 为 0。
    管理器 的 统计_弱集合清除数 为 0。
    管理器 的 统计_处理轮数 为 0。
    管理器 的 统计_总处理时间 为 0。
    做 打印 用 「[弱引用] 管理器初始化完成」。
    返还 管理器。
完。

弱引用管理器_销毁 接受 管理器：
    若 管理器 等于 空：
        返还。
    完。
    可变 当前 为 管理器 的 弱引用链头。
    只要 当前 不等于 0：
        下一个 为 做 内存_读64 用 当前 加 40。
        做 堆_释放 用 当前。
        当前 为 下一个。
    完。
    若 管理器 的 终结表基址 不等于 0：
        做 平台_释放内存 用 管理器 的 终结表基址、管理器 的 终结表容量 乘 48。
    完。
    做 弱引用管理器_释放哈希表 用 管理器 的 弱映射表基址、管理器 的 弱映射桶数。
    做 弱引用管理器_释放哈希表 用 管理器 的 弱集合表基址、管理器 的 弱集合桶数。
    若 管理器 的 AI存活历史 不等于 0：
        做 平台_释放内存 用 管理器 的 AI存活历史、AI_存活预测_窗口 乘 4。
    完。
    做 打印 用 「[弱引用] 管理器已销毁」。
    做 堆_释放 用 管理器。
完。

弱引用管理器_释放哈希表 接受 表基址、桶数：
    若 表基址 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 桶数：
        可变 节点 为 做 内存_读64 用 表基址 加 i 乘 8。
        只要 节点 不等于 0：
            下一个 为 做 内存_读64 用 节点 加 32。
            做 堆_释放 用 节点。
            节点 为 下一个。
        完。
        i 为 i 加 1。
    完。
    做 平台_释放内存 用 表基址、桶数 乘 8。
完。

弱引用_创建 接受 管理器、目标、回调、持有值：
    若 管理器 等于 空 或者 目标 等于 0：
        返还 0。
    完。
    若 管理器 的 弱引用数 大于或等于 弱引用_最大容量：
        做 打印 用 「[弱引用] 警告: 弱引用数量达到上限」。
        返还 0。
    完。
    可变 引用 为 做 堆_分配 用 弱引用项。
    若 引用 等于 0：
        返还 0。
    完。
    引用 的 目标对象 为 目标。
    引用 的 回调函数 为 回调。
    引用 的 持有值 为 持有值。
    引用 的 状态 为 弱引用状态_活跃。
    引用 的 创建时间 为 做 平台_获取高精度时间。
    引用 的 后继 为 管理器 的 弱引用链头。
    管理器 的 弱引用链头 为 引用。
    管理器 的 弱引用数 为 管理器 的 弱引用数 加 1。
    管理器 的 弱引用活跃数 为 管理器 的 弱引用活跃数 加 1。
    管理器 的 统计_弱引用创建数 为 管理器 的 统计_弱引用创建数 加 1。
    返还 引用。
完。

弱引用_解引用 接受 管理器、引用：
    若 管理器 等于 空 或者 引用 等于 0：
        返还 0。
    完。
    若 引用 的 状态 不等于 弱引用状态_活跃：
        返还 0。
    完。
    若 引用 的 目标对象 等于 0：
        返还 0。
    完。
    标记 为 做 内存_读8 用 引用 的 目标对象 加 对象头_标记偏移。
    若 标记 等于 标记_白色：
    完。
    返还 引用 的 目标对象。
完。

弱引用_清除 接受 管理器、引用：
    若 管理器 等于 空 或者 引用 等于 0：
        返还。
    完。
    若 引用 的 状态 等于 弱引用状态_活跃：
        引用 的 状态 为 弱引用状态_已清除。
        引用 的 目标对象 为 0。
        管理器 的 弱引用活跃数 为 管理器 的 弱引用活跃数 减 1。
        管理器 的 统计_弱引用清除数 为 管理器 的 统计_弱引用清除数 加 1。
    完。
完。

终结_注册 接受 管理器、目标、回调、持有值、注销令牌：
    若 管理器 等于 空 或者 目标 等于 0 或者 回调 等于 0：
        返还 负 1。
    完。
    若 管理器 的 终结表大小 大于或等于 管理器 的 终结表容量：
        做 终结_扩容 用 管理器。
        若 管理器 的 终结表大小 大于或等于 管理器 的 终结表容量：
            做 打印 用 「[弱引用] 错误: 终结表已满」。
            返还 负 1。
        完。
    完。
    索引 为 管理器 的 终结表大小。
    基址 为 管理器 的 终结表基址 加 索引 乘 48。
    做 内存_写64 用 基址、目标。
    做 内存_写64 用 基址 加 8、回调。
    做 内存_写64 用 基址 加 16、持有值。
    做 内存_写64 用 基址 加 24、注销令牌。
    做 内存_写32 用 基址 加 32、0。
    做 内存_写64 用 基址 加 36、做 平台_获取高精度时间。
    管理器 的 终结表大小 为 管理器 的 终结表大小 加 1。
    管理器 的 统计_终结注册数 为 管理器 的 统计_终结注册数 加 1。
    返还 索引。
完。

终结_注销 接受 管理器、注销令牌：
    若 管理器 等于 空 或者 注销令牌 等于 0：
        返还 假。
    完。
    可变 序号 为 0。
    可变 注销数 为 0。
    只要 序号 小于 管理器 的 终结表大小：
        基址 为 管理器 的 终结表基址 加 序号 乘 48。
        令牌 为 做 内存_读64 用 基址 加 24。
        状态 为 做 内存_读32 用 基址 加 32。
        若 令牌 等于 注销令牌 并且 状态 等于 0：
            做 内存_写32 用 基址 加 32、3。
            注销数 为 注销数 加 1。
        完。
        序号 为 序号 加 1。
    完。
    管理器 的 统计_终结注销数 为 管理器 的 统计_终结注销数 加 注销数。
    返还 注销数 大于 0。
完。

终结_扩容 接受 管理器：
    若 管理器 的 终结表容量 大于或等于 终结_最大容量：
        返还。
    完。
    新容量 为 管理器 的 终结表容量 乘 2。
    若 新容量 大于 终结_最大容量：
        新容量 为 终结_最大容量。
    完。
    新内存 为 做 平台_申请内存 用 新容量 乘 48。
    若 新内存 等于 0：
        返还。
    完。
    做 内存_复制 用 新内存、管理器 的 终结表基址、管理器 的 终结表大小 乘 48。
    做 平台_释放内存 用 管理器 的 终结表基址、管理器 的 终结表容量 乘 48。
    管理器 的 终结表基址 为 新内存。
    管理器 的 终结表容量 为 新容量。
完。

弱映射_哈希 接受 键对象：
    可变 h 为 键对象。
    h 为 h 位异或 h 右移 16。
    h 为 h 乘 2654435761。
    h 为 h 位异或 h 右移 13。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    返还 h。
完。

弱映射_设 接受 管理器、键、值：
    若 管理器 等于 空 或者 键 等于 0 或者 管理器 的 弱映射表基址 等于 0：
        返还。
    完。
    哈希 为 做 弱映射_哈希 用 键。
    桶索引 为 哈希 取余于 管理器 的 弱映射桶数。
    桶基址 为 管理器 的 弱映射表基址 加 桶索引 乘 8。
    可变 节点 为 做 内存_读64 用 桶基址。
    只要 节点 不等于 0：
        节点键 为 做 内存_读64 用 节点。
        节点活跃 为 做 内存_读32 用 节点 加 24。
        若 节点键 等于 键 并且 节点活跃 等于 1：
            做 内存_写64 用 节点 加 8、值。
            返还。
        完。
        节点 为 做 内存_读64 用 节点 加 32。
    完。
    新节点 为 做 堆_分配 用 弱映射桶。
    若 新节点 等于 0：
        返还。
    完。
    做 内存_写64 用 新节点、键。
    做 内存_写64 用 新节点 加 8、值。
    做 内存_写32 用 新节点 加 16、哈希。
    做 内存_写32 用 新节点 加 24、1。
    做 内存_写64 用 新节点 加 32、做 内存_读64 用 桶基址。
    做 内存_写64 用 桶基址、新节点。
    管理器 的 弱映射元素数 为 管理器 的 弱映射元素数 加 1。
    管理器 的 统计_弱映射设置数 为 管理器 的 统计_弱映射设置数 加 1。
完。

弱映射_取 接受 管理器、键：
    若 管理器 等于 空 或者 键 等于 0 或者 管理器 的 弱映射表基址 等于 0：
        返还 0。
    完。
    哈希 为 做 弱映射_哈希 用 键。
    桶索引 为 哈希 取余于 管理器 的 弱映射桶数。
    可变 节点 为 做 内存_读64 用 管理器 的 弱映射表基址 加 桶索引 乘 8。
    只要 节点 不等于 0：
        若 做 内存_读64 用 节点 等于 键 并且 做 内存_读32 用 节点 加 24 等于 1：
            返还 做 内存_读64 用 节点 加 8。
        完。
        节点 为 做 内存_读64 用 节点 加 32。
    完。
    返还 0。
完。

弱映射_有 接受 管理器、键：
    返还 做 弱映射_取 用 管理器、键 不等于 0。
完。

弱映射_删 接受 管理器、键：
    若 管理器 等于 空 或者 键 等于 0 或者 管理器 的 弱映射表基址 等于 0：
        返还 假。
    完。
    哈希 为 做 弱映射_哈希 用 键。
    桶索引 为 哈希 取余于 管理器 的 弱映射桶数。
    可变 节点 为 做 内存_读64 用 管理器 的 弱映射表基址 加 桶索引 乘 8。
    只要 节点 不等于 0：
        若 做 内存_读64 用 节点 等于 键 并且 做 内存_读32 用 节点 加 24 等于 1：
            做 内存_写32 用 节点 加 24、0。
            管理器 的 弱映射元素数 为 管理器 的 弱映射元素数 减 1。
            返还 真。
        完。
        节点 为 做 内存_读64 用 节点 加 32。
    完。
    返还 假。
完。

弱集合_添加 接受 管理器、元素：
    若 管理器 等于 空 或者 元素 等于 0 或者 管理器 的 弱集合表基址 等于 0：
        返还。
    完。
    哈希 为 做 弱映射_哈希 用 元素。
    桶索引 为 哈希 取余于 管理器 的 弱集合桶数。
    桶基址 为 管理器 的 弱集合表基址 加 桶索引 乘 8。
    可变 节点 为 做 内存_读64 用 桶基址。
    只要 节点 不等于 0：
        若 做 内存_读64 用 节点 等于 元素 并且 做 内存_读32 用 节点 加 16 等于 1：
            返还。
        完。
        节点 为 做 内存_读64 用 节点 加 24。
    完。
    新节点 为 做 平台_申请内存 用 32。
    若 新节点 等于 0：
        返还。
    完。
    做 内存_写64 用 新节点、元素。
    做 内存_写32 用 新节点 加 8、哈希。
    做 内存_写32 用 新节点 加 16、1。
    做 内存_写64 用 新节点 加 24、做 内存_读64 用 桶基址。
    做 内存_写64 用 桶基址、新节点。
    管理器 的 弱集合元素数 为 管理器 的 弱集合元素数 加 1。
    管理器 的 统计_弱集合添加数 为 管理器 的 统计_弱集合添加数 加 1。
完。

弱集合_有 接受 管理器、元素：
    若 管理器 等于 空 或者 元素 等于 0 或者 管理器 的 弱集合表基址 等于 0：
        返还 假。
    完。
    哈希 为 做 弱映射_哈希 用 元素。
    桶索引 为 哈希 取余于 管理器 的 弱集合桶数。
    可变 节点 为 做 内存_读64 用 管理器 的 弱集合表基址 加 桶索引 乘 8。
    只要 节点 不等于 0：
        若 做 内存_读64 用 节点 等于 元素 并且 做 内存_读32 用 节点 加 16 等于 1：
            返还 真。
        完。
        节点 为 做 内存_读64 用 节点 加 24。
    完。
    返还 假。
完。

弱集合_删 接受 管理器、元素：
    若 管理器 等于 空 或者 元素 等于 0 或者 管理器 的 弱集合表基址 等于 0：
        返还 假。
    完。
    哈希 为 做 弱映射_哈希 用 元素。
    桶索引 为 哈希 取余于 管理器 的 弱集合桶数。
    可变 节点 为 做 内存_读64 用 管理器 的 弱集合表基址 加 桶索引 乘 8。
    只要 节点 不等于 0：
        若 做 内存_读64 用 节点 等于 元素 并且 做 内存_读32 用 节点 加 16 等于 1：
            做 内存_写32 用 节点 加 16、0。
            管理器 的 弱集合元素数 为 管理器 的 弱集合元素数 减 1。
            返还 真。
        完。
        节点 为 做 内存_读64 用 节点 加 24。
    完。
    返还 假。
完。

垃圾回收_处理弱引用 接受 管理器：
    若 管理器 等于 空：
        返还。
    完。
    开始 为 做 平台_获取高精度时间。
    管理器 的 统计_处理轮数 为 管理器 的 统计_处理轮数 加 1。
    可变 本轮清除数 为 0。
    可变 本轮存活数 为 0。
    可变 当前 为 管理器 的 弱引用链头。
    只要 当前 不等于 0：
        若 当前 的 状态 等于 弱引用状态_活跃 并且 当前 的 目标对象 不等于 0：
            颜色 为 做 内存_读8 用 当前 的 目标对象 加 对象头_标记偏移。
            若 颜色 等于 标记_白色：
                做 弱引用_清除 用 管理器、当前。
                本轮清除数 为 本轮清除数 加 1。
                若 当前 的 回调函数 不等于 0：
                完。
            否则：
                本轮存活数 为 本轮存活数 加 1。
            完。
        完。
        当前 为 当前 的 后继。
    完。
    做 垃圾回收_处理终结注册 用 管理器。
    做 垃圾回收_清理弱映射 用 管理器。
    做 垃圾回收_清理弱集合 用 管理器。
    做 垃圾回收_执行终结回调 用 管理器。
    做 垃圾回收_AI更新存活预测 用 管理器、本轮存活数、本轮清除数。
    耗时 为 做 平台_获取高精度时间 减 开始。
    管理器 的 统计_总处理时间 为 管理器 的 统计_总处理时间 加 耗时。
完。

垃圾回收_处理终结注册 接受 管理器：
    若 管理器 的 终结表基址 等于 0：
        返还。
    完。
    可变 序号 为 0。
    只要 序号 小于 管理器 的 终结表大小：
        基址 为 管理器 的 终结表基址 加 序号 乘 48。
        状态 为 做 内存_读32 用 基址 加 32。
        若 状态 等于 0：
            目标 为 做 内存_读64 用 基址。
            若 目标 不等于 0：
                颜色 为 做 内存_读8 用 目标 加 对象头_标记偏移。
                若 颜色 等于 标记_白色：
                    做 内存_写32 用 基址 加 32、1。
                    管理器 的 终结待执行数 为 管理器 的 终结待执行数 加 1。
                完。
            完。
        完。
        序号 为 序号 加 1。
    完。
完。

垃圾回收_清理弱映射 接受 管理器：
    若 管理器 的 弱映射表基址 等于 0：
        返还。
    完。
    可变 桶 为 0。
    只要 桶 小于 管理器 的 弱映射桶数：
        可变 节点 为 做 内存_读64 用 管理器 的 弱映射表基址 加 桶 乘 8。
        只要 节点 不等于 0：
            活跃 为 做 内存_读32 用 节点 加 24。
            若 活跃 等于 1：
                键 为 做 内存_读64 用 节点。
                若 键 不等于 0：
                    颜色 为 做 内存_读8 用 键 加 对象头_标记偏移。
                    若 颜色 等于 标记_白色：
                        做 内存_写32 用 节点 加 24、0。
                        管理器 的 弱映射元素数 为 管理器 的 弱映射元素数 减 1。
                        管理器 的 统计_弱映射清除数 为 管理器 的 统计_弱映射清除数 加 1。
                    完。
                完。
            完。
            节点 为 做 内存_读64 用 节点 加 32。
        完。
        桶 为 桶 加 1。
    完。
完。

垃圾回收_清理弱集合 接受 管理器：
    若 管理器 的 弱集合表基址 等于 0：
        返还。
    完。
    可变 桶 为 0。
    只要 桶 小于 管理器 的 弱集合桶数：
        可变 节点 为 做 内存_读64 用 管理器 的 弱集合表基址 加 桶 乘 8。
        只要 节点 不等于 0：
            活跃 为 做 内存_读32 用 节点 加 16。
            若 活跃 等于 1：
                元素 为 做 内存_读64 用 节点。
                若 元素 不等于 0：
                    颜色 为 做 内存_读8 用 元素 加 对象头_标记偏移。
                    若 颜色 等于 标记_白色：
                        做 内存_写32 用 节点 加 16、0。
                        管理器 的 弱集合元素数 为 管理器 的 弱集合元素数 减 1。
                        管理器 的 统计_弱集合清除数 为 管理器 的 统计_弱集合清除数 加 1。
                    完。
                完。
            完。
            节点 为 做 内存_读64 用 节点 加 24。
        完。
        桶 为 桶 加 1。
    完。
完。

垃圾回收_执行终结回调 接受 管理器：
    若 管理器 的 终结待执行数 等于 0：
        返还。
    完。
    可变 已执行 为 0。
    可变 序号 为 0。
    只要 序号 小于 管理器 的 终结表大小 并且 已执行 小于 终结_回调批量：
        基址 为 管理器 的 终结表基址 加 序号 乘 48。
        状态 为 做 内存_读32 用 基址 加 32。
        若 状态 等于 1：
            回调 为 做 内存_读64 用 基址 加 8。
            持有值 为 做 内存_读64 用 基址 加 16。
            若 回调 不等于 0：
                做 打印 用 「[弱引用] 执行终结回调」。
            完。
            做 内存_写32 用 基址 加 32、2。
            已执行 为 已执行 加 1。
            管理器 的 统计_终结执行数 为 管理器 的 统计_终结执行数 加 1。
        完。
        序号 为 序号 加 1。
    完。
    管理器 的 终结待执行数 为 管理器 的 终结待执行数 减 已执行。
完。

垃圾回收_AI更新存活预测 接受 管理器、存活数、清除数：
    若 管理器 等于 空 或者 管理器 的 AI存活历史 等于 0：
        返还。
    完。
    总数 为 存活数 加 清除数。
    若 总数 等于 0：
        返还。
    完。
    存活率 为 存活数 乘 10000 除以 总数。
    位置 为 管理器 的 AI历史游标 取余于 AI_存活预测_窗口。
    做 内存_写32 用 管理器 的 AI存活历史 加 位置 乘 4、存活率。
    管理器 的 AI历史游标 为 管理器 的 AI历史游标 加 1。
    可变 总和 为 0。
    可变 权重和 为 0。
    可变 i 为 0。
    只要 i 小于 AI_存活预测_窗口：
        历史值 为 做 内存_读32 用 管理器 的 AI存活历史 加 i 乘 4。
        若 历史值 大于 0：
            权重 为 i 加 1。
            总和 为 总和 加 历史值 乘 权重。
            权重和 为 权重和 加 权重。
        完。
        i 为 i 加 1。
    完。
    若 权重和 大于 0：
        管理器 的 AI预测存活率 为 总和 除以 权重和。
    完。
    若 管理器 的 AI预测存活率 小于 3000：
        管理器 的 AI批量化终结 为 真。
    否则：
        管理器 的 AI批量化终结 为 假。
    完。
完。

弱引用管理器_打印统计 接受 管理器：
    若 管理器 等于 空：
        返还。
    完。
    做 打印 用 「[弱引用] ═══════════ 弱引用统计 ═══════════」。
    做 打印 用 做 字符串拼接 用 「  弱引用总数:       」、做 转字符串 用 管理器 的 弱引用数。
    做 打印 用 做 字符串拼接 用 「  弱引用活跃数:     」、做 转字符串 用 管理器 的 弱引用活跃数。
    做 打印 用 做 字符串拼接 用 「  弱引用创建数:     」、做 转字符串 用 管理器 的 统计_弱引用创建数。
    做 打印 用 做 字符串拼接 用 「  弱引用清除数:     」、做 转字符串 用 管理器 的 统计_弱引用清除数。
    做 打印 用 「[弱引用] ── 终结注册 ──」。
    做 打印 用 做 字符串拼接 用 「  终结注册数:       」、做 转字符串 用 管理器 的 统计_终结注册数。
    做 打印 用 做 字符串拼接 用 「  终结执行数:       」、做 转字符串 用 管理器 的 统计_终结执行数。
    做 打印 用 做 字符串拼接 用 「  终结注销数:       」、做 转字符串 用 管理器 的 统计_终结注销数。
    做 打印 用 做 字符串拼接 用 「  终结待执行数:     」、做 转字符串 用 管理器 的 终结待执行数。
    做 打印 用 「[弱引用] ── WeakMap/WeakSet ──」。
    做 打印 用 做 字符串拼接 用 「  弱映射元素数:     」、做 转字符串 用 管理器 的 弱映射元素数。
    做 打印 用 做 字符串拼接 用 「  弱映射设置数:     」、做 转字符串 用 管理器 的 统计_弱映射设置数。
    做 打印 用 做 字符串拼接 用 「  弱映射清除数:     」、做 转字符串 用 管理器 的 统计_弱映射清除数。
    做 打印 用 做 字符串拼接 用 「  弱集合元素数:     」、做 转字符串 用 管理器 的 弱集合元素数。
    做 打印 用 做 字符串拼接 用 「  弱集合添加数:     」、做 转字符串 用 管理器 的 统计_弱集合添加数。
    做 打印 用 做 字符串拼接 用 「  弱集合清除数:     」、做 转字符串 用 管理器 的 统计_弱集合清除数。
    做 打印 用 「[弱引用] ── AI预测 ──」。
    做 打印 用 做 字符串拼接 用 「  AI预测存活率‱:   」、做 转字符串 用 管理器 的 AI预测存活率。
    做 打印 用 做 字符串拼接 用 「  AI批量化终结:     」、做 转字符串 用 管理器 的 AI批量化终结。
    做 打印 用 「[弱引用] ── 性能 ──」。
    做 打印 用 做 字符串拼接 用 「  处理轮数:         」、做 转字符串 用 管理器 的 统计_处理轮数。
    做 打印 用 做 字符串拼接 用 「  总处理时间(μs):   」、做 转字符串 用 管理器 的 统计_总处理时间。
    若 管理器 的 统计_处理轮数 大于 0：
        平均 为 管理器 的 统计_总处理时间 除以 管理器 的 统计_处理轮数。
        做 打印 用 做 字符串拼接 用 「  平均每轮(μs):     」、做 转字符串 用 平均。
    完。
    做 打印 用 「[弱引用] ═══════════════════════════════════════」。
完。

本章 收束。
