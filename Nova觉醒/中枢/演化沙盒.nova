《演化沙盒》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·推理·云端桥接》。

引用《..·推理·知识蒸馏》。

引用《.·检查点系统》。

引用《.·模块级热替换》。

引用《..·标准·文件》。

引用《..·标准·原生字符串》。

引用《..·记忆·接口》。

沙盒根路径 为 「/nova/沙盒/」。

沙盒_代码目录 为 「/nova/沙盒/代码/」。

沙盒_数据目录 为 「/nova/沙盒/数据/」。

沙盒_日志目录 为 「/nova/沙盒/日志/」。

沙盒_快照目录 为 「/nova/沙盒/快照/」。

可变 沙盒_已初始化 为 假。

可变 沙盒_测试总数 为 0。

可变 沙盒_测试通过 为 0。

可变 沙盒_测试失败 为 0。

可变 沙盒_更新成功 为 0。

可变 沙盒_回滚次数 为 0。

可变 沙盒_清理次数 为 0。

可变 沙盒_临时文件数 为 0。

沙盒_初始化 接受 零参：
    若 沙盒_已初始化：
        返还 0。
    完。
    做 记忆_创建目录 用 沙盒根路径。
    做 记忆_创建目录 用 沙盒_代码目录。
    做 记忆_创建目录 用 沙盒_数据目录。
    做 记忆_创建目录 用 沙盒_日志目录。
    做 记忆_创建目录 用 沙盒_快照目录。
    沙盒_已初始化 为 真。
    做 打印 用 「[沙盒] 演化沙盒初始化完成」。
    返还 0。
完。

演化提案 是 一种 事物，含 编号、类型、来源、目标模块、修改内容、预期效果、风险评估、创建时间、状态、测试检查点ID、部署检查点ID、沙盒文件已创建。

可变 提案队列 为 一组：（空）。

可变 提案总数 为 0。

提案_最大 为 64。

沙盒_目标模块有效 接受 目标模块：
    若 目标模块 等于 空：
        返还 0。
    完。
    若 做 字符串长度 用 目标模块 小于或等于 0：
        返还 0。
    完。
    若 做 字符串开头是 用 目标模块、「/」 等于 真：
        返还 0。
    完。
    若 做 字符串包含 用 目标模块、「..」 等于 真：
        返还 0。
    完。
    若 做 字符串结尾是 用 目标模块、「/」 等于 真：
        返还 0。
    完。
    返还 1。
完。

沙盒_拼接路径 接受 目录、名称：
    若 做 字符串结尾是 用 目录、「/」 等于 真：
        返还 目录 加 名称。
    完。
    返还 目录 加 「/」 加 名称。
完。

沙盒_释放检查点 接受 检查点ID：
    若 检查点ID 大于 0：
        做 检查点_删除 用 检查点ID。
    完。
完。

沙盒_标准模块名 接受 目标模块：
    若 做 字符串结尾是 用 目标模块、「.nova」 等于 真：
        返还 做 字符串子串 用 目标模块、0、做 字符串长度 用 目标模块 减 5。
    完。
    返还 目标模块。
完。

沙盒_提案文件路径 接受 提案：
    相对路径 为 提案 的 目标模块。
    若 做 字符串结尾是 用 相对路径、「.nova」 不等于 真：
        相对路径 为 相对路径 加 「.nova」。
    完。
    返还 做 沙盒_拼接路径 用 沙盒_代码目录、相对路径。
完。

沙盒_确保父目录 接受 文件路径：
    可变 i 为 1。
    长度 为 做 字符串长度 用 文件路径。
    只要 i 小于 长度：
        若 做 字符串取字符码 用 文件路径、i 等于 47：
            目录路径 为 做 字符串子串 用 文件路径、0、i。
            若 做 字符串长度 用 目录路径 大于 0 并且 做 目录_存在 用 目录路径 不等于 真：
                结果 为 做 目录_创建默认 用 目录路径。
                若 结果 小于 0：
                    返还 结果。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

沙盒_递归复制目录 接受 源目录、目标目录：
    若 做 目录_存在 用 源目录 不等于 真：
        返还 错误_不存在。
    完。
    可变 结果 为 做 沙盒_确保父目录 用 做 沙盒_拼接路径 用 目标目录、「__probe__」。
    若 结果 小于 0：
        返还 结果。
    完。
    若 做 目录_存在 用 目标目录 不等于 真：
        结果 为 做 目录_创建默认 用 目标目录。
        若 结果 小于 0：
            返还 结果。
        完。
    完。
    条目列表 为 做 记忆_列目录 用 源目录。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目列表：
        条目名 为 条目列表 的 第 i 项。
        若 条目名 等于 「.」 或者 条目名 等于 「..」：
            i 为 i 加 1。
            继续。
        完。
        完整源 为 做 沙盒_拼接路径 用 源目录、条目名。
        完整目标 为 做 沙盒_拼接路径 用 目标目录、条目名。
        若 做 目录_存在 用 完整源 等于 真：
            结果 为 做 沙盒_递归复制目录 用 完整源、完整目标。
        否则：
            结果 为 做 沙盒_确保父目录 用 完整目标。
            若 结果 大于或等于 0：
                结果 为 做 文件_复制 用 完整源、完整目标。
            完。
        完。
        若 结果 小于 0：
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 成功。
完。

沙盒_递归删除目录 接受 目录路径：
    若 做 目录_存在 用 目录路径 不等于 真：
        返还 成功。
    完。
    条目列表 为 做 记忆_列目录 用 目录路径。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目列表：
        条目名 为 条目列表 的 第 i 项。
        若 条目名 等于 「.」 或者 条目名 等于 「..」：
            i 为 i 加 1。
            继续。
        完。
        完整路径 为 做 沙盒_拼接路径 用 目录路径、条目名。
        可变 结果 为 成功。
        若 做 目录_存在 用 完整路径 等于 真：
            结果 为 做 沙盒_递归删除目录 用 完整路径。
        否则若 做 文件_存在 用 完整路径 等于 真：
            结果 为 做 文件_删除 用 完整路径。
        完。
        若 结果 小于 0：
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 做 目录_删除 用 目录路径。
完。

沙盒_括号平衡 接受 文本：
    可变 圆括号 为 0。
    可变 方括号 为 0。
    可变 花括号 为 0。
    可变 在字符串 为 0。
    可变 在注释 为 0。
    可变 i 为 0。
    长度 为 做 字符串长度 用 文本。
    只要 i 小于 长度：
        c 为 做 字符串取字符码 用 文本、i。
        可变 下一个 为 0。
        若 i 加 1 小于 长度：
            下一个 为 做 字符串取字符码 用 文本、i 加 1。
        完。
        若 在注释 等于 1：
            若 c 等于 10 或者 c 等于 13：
                在注释 为 0。
            完。
            i 为 i 加 1。
            继续。
        完。
        若 在字符串 等于 1：
            若 c 等于 34：
                在字符串 为 0。
            完。
            i 为 i 加 1。
            继续。
        完。
        若 c 等于 47 并且 下一个 等于 47：
            在注释 为 1。
            i 为 i 加 2。
            继续。
        完。
        若 c 等于 34：
            在字符串 为 1。
            i 为 i 加 1。
            继续。
        完。
        若 c 等于 40：
            圆括号 为 圆括号 加 1。
        否则若 c 等于 41：
            圆括号 为 圆括号 减 1。
        否则若 c 等于 91：
            方括号 为 方括号 加 1。
        否则若 c 等于 93：
            方括号 为 方括号 减 1。
        否则若 c 等于 123：
            花括号 为 花括号 加 1。
        否则若 c 等于 125：
            花括号 为 花括号 减 1。
        完。
        若 圆括号 小于 0 或者 方括号 小于 0 或者 花括号 小于 0：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    若 在字符串 等于 1：
        返还 0。
    完。
    若 圆括号 不等于 0 或者 方括号 不等于 0 或者 花括号 不等于 0：
        返还 0。
    完。
    返还 1。
完。

沙盒_导入完整 接受 文本：
    行列表 为 做 字符串分割 用 文本、「\n」。
    可变 i 为 0。
    前缀长 为 做 字符串长度 用 「导入(」。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去首尾空白 用 行列表 的 第 i 项。
        若 做 字符串开头是 用 行、「导入(」 等于 真：
            行长 为 做 字符串长度 用 行。
            若 行长 小于或等于 前缀长 加 2：
                返还 0。
            完。
            若 做 字符串取字符码 用 行、前缀长 不等于 34：
                返还 0。
            完。
            若 做 字符串取字符码 用 行、行长 减 2 不等于 34：
                返还 0。
            完。
            若 做 字符串取字符码 用 行、行长 减 1 不等于 41：
                返还 0。
            完。
            路径 为 做 字符串子串 用 行、前缀长 加 1、行长 减 2。
            若 做 字符串长度 用 路径 小于或等于 0：
                返还 0。
            完。
            若 做 字符串结尾是 用 路径、「.nova」 不等于 真：
                返还 0。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

沙盒_函数不冲突 接受 文本：
    行列表 为 做 字符串分割 用 文本、「\n」。
    名称列表 为 一组：（空）。
    可变 i 为 0。
    前缀长 为 做 字符串长度 用 「函数 」。
    只要 i 小于 做 计数 用 行列表：
        行 为 做 字符串去首尾空白 用 行列表 的 第 i 项。
        若 做 字符串开头是 用 行、「函数 」 等于 真：
            左括号 为 做 字符串查找从 用 行、「(」、前缀长。
            若 左括号 小于 0：
                返还 0。
            完。
            名称 为 做 字符串去首尾空白 用 做 字符串子串 用 行、前缀长、左括号。
            若 做 字符串长度 用 名称 小于或等于 0：
                返还 0。
            完。
            可变 j 为 0。
            只要 j 小于 做 计数 用 名称列表：
                若 名称列表 的 第 j 项 等于 名称：
                    返还 0。
                完。
                j 为 j 加 1。
            完。
            做 追加 用 名称列表、名称。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

沙盒_代码形状有效 接受 文本：
    若 做 字符串包含 用 文本、「函数 」 等于 真：
        返还 1。
    完。
    若 做 字符串包含 用 文本、「结构 」 等于 真：
        返还 1。
    完。
    若 做 字符串包含 用 文本、「定义 」 等于 真：
        返还 1。
    完。
    若 做 字符串包含 用 文本、「外部 」 等于 真：
        返还 1。
    完。
    若 做 字符串包含 用 文本、「导入(」 等于 真：
        返还 1。
    完。
    返还 0。
完。

沙盒_提交提案 接受 类型、来源、目标、内容、风险：
    若 做 计数 用 提案队列 大于或等于 提案_最大：
        做 沙盒_清理失败提案。
    完。
    提案 为 新建 演化提案：编号 为 提案总数、类型 为 类型、来源 为 来源、目标模块 为 目标、修改内容 为 内容、预期效果 为 「」、风险评估 为 风险、创建时间 为 做 中枢_当前时间纳秒、状态 为 0、测试检查点ID 为 0、部署检查点ID 为 0、沙盒文件已创建 为 0。
    做 追加 用 提案队列、提案。
    提案总数 为 提案总数 加 1。
    返还 提案 的 编号。
完。

沙盒_测试提案 接受 提案编号：
    提案 为 做 沙盒_查找提案 用 提案编号。
    若 提案 等于 空：
        返还 负 1。
    完。
    提案 的 状态 为 1。
    沙盒_测试总数 为 沙盒_测试总数 加 1。
    若 提案 的 类型 不等于 「代码补丁」 并且 提案 的 类型 不等于 「新能力」：
        提案 的 状态 为 3。
        沙盒_测试失败 为 沙盒_测试失败 加 1。
        返还 错误_不支持操作。
    完。
    测试检查点 为 做 检查点_创建 用 「沙盒测试-」 加 做 整数转字符串 用 提案编号。
    若 测试检查点 等于 空：
        提案 的 状态 为 3。
        沙盒_测试失败 为 沙盒_测试失败 加 1。
        返还 错误_内存不足。
    完。
    提案 的 测试检查点ID 为 测试检查点 的 检查点ID。
    若 做 沙盒_目标模块有效 用 提案 的 目标模块 不等于 1：
        提案 的 状态 为 3。
        沙盒_测试失败 为 沙盒_测试失败 加 1。
        做 沙盒_释放检查点 用 提案 的 测试检查点ID。
        提案 的 测试检查点ID 为 0。
        返还 错误_无效参数。
    完。
    沙盒文件 为 做 沙盒_提案文件路径 用 提案。
    准备结果 为 做 沙盒_确保父目录 用 沙盒文件。
    若 准备结果 小于 0：
        提案 的 状态 为 3。
        沙盒_测试失败 为 沙盒_测试失败 加 1。
        做 沙盒_释放检查点 用 提案 的 测试检查点ID。
        提案 的 测试检查点ID 为 0。
        返还 准备结果。
    完。
    写入结果 为 做 文件_写全部 用 沙盒文件、提案 的 修改内容、做 字符串长度 用 提案 的 修改内容。
    若 写入结果 小于 0：
        提案 的 状态 为 3。
        沙盒_测试失败 为 沙盒_测试失败 加 1。
        做 沙盒_释放检查点 用 提案 的 测试检查点ID。
        提案 的 测试检查点ID 为 0。
        返还 写入结果。
    完。
    若 提案 的 沙盒文件已创建 不等于 1：
        提案 的 沙盒文件已创建 为 1。
        沙盒_临时文件数 为 沙盒_临时文件数 加 1。
    完。
    测试结果 为 做 沙盒_运行验证 用 沙盒文件、提案 的 修改内容。
    若 测试结果 等于 1：
        提案 的 状态 为 2。
        沙盒_测试通过 为 沙盒_测试通过 加 1。
        做 沙盒_释放检查点 用 提案 的 测试检查点ID。
        提案 的 测试检查点ID 为 0。
        做 打印 用 「[沙盒] 提案」 加 提案编号 加 「测试通过」。
    否则：
        提案 的 状态 为 3。
        沙盒_测试失败 为 沙盒_测试失败 加 1。
        做 沙盒_清理提案文件 用 提案编号。
        做 沙盒_释放检查点 用 提案 的 测试检查点ID。
        提案 的 测试检查点ID 为 0。
        做 打印 用 「[沙盒] 提案」 加 提案编号 加 「测试失败,已清理」。
    完。
    返还 测试结果。
完。

沙盒_运行验证 接受 文件路径、修改：
    若 文件路径 等于 空 或者 修改 等于 空：
        返还 错误_无效参数。
    完。
    若 做 字符串长度 用 修改 小于或等于 0：
        返还 错误_无效参数。
    完。
    若 做 沙盒_代码形状有效 用 修改 不等于 1：
        返还 错误_沙箱违规。
    完。
    若 做 字符串开头是 用 文件路径、沙盒_代码目录 不等于 真：
        返还 错误_沙箱违规。
    完。
    若 做 字符串结尾是 用 文件路径、「.nova」 不等于 真：
        返还 错误_无效参数。
    完。
    若 做 文件_存在 用 文件路径 不等于 真：
        返还 错误_不存在。
    完。
    已写入代码 为 做 文件_读全部 用 文件路径。
    若 已写入代码 等于 0：
        返还 错误_不存在。
    完。
    若 已写入代码 不等于 修改：
        返还 错误_完整性校验失败。
    完。
    若 做 沙盒_括号平衡 用 修改 不等于 1：
        返还 错误_沙箱违规。
    完。
    若 做 沙盒_导入完整 用 修改 不等于 1：
        返还 错误_沙箱违规。
    完。
    若 做 沙盒_函数不冲突 用 修改 不等于 1：
        返还 错误_沙箱违规。
    完。
    返还 1。
完。

沙盒_部署提案 接受 提案编号：
    提案 为 做 沙盒_查找提案 用 提案编号。
    若 提案 等于 空 或者 提案 的 状态 不等于 2：
        返还 负 1。
    完。
    若 提案 的 类型 不等于 「代码补丁」 并且 提案 的 类型 不等于 「新能力」：
        提案 的 状态 为 3。
        做 沙盒_清理提案文件 用 提案编号。
        返还 错误_不支持操作。
    完。
    部署检查点 为 做 检查点_创建 用 「沙盒部署-」 加 做 整数转字符串 用 提案编号。
    若 部署检查点 等于 空：
        返还 错误_内存不足。
    完。
    提案 的 部署检查点ID 为 部署检查点 的 检查点ID。
    结果 为 错误_不支持操作。
    沙盒文件 为 做 沙盒_提案文件路径 用 提案。
    新代码 为 做 文件_读全部 用 沙盒文件。
    若 新代码 等于 0：
        结果 为 错误_不存在。
    否则：
        热替换结果 为 做 模块_热替换 用 做 沙盒_标准模块名 用 提案 的 目标模块、新代码、做 字符串长度 用 新代码。
        若 热替换结果 等于 成功：
            结果 为 1。
        否则：
            结果 为 热替换结果。
        完。
    完。
    若 结果 等于 1：
        提案 的 状态 为 4。
        沙盒_更新成功 为 沙盒_更新成功 加 1。
        做 沙盒_清理提案文件 用 提案编号。
        做 沙盒_释放检查点 用 提案 的 部署检查点ID。
        提案 的 部署检查点ID 为 0。
        做 打印 用 「[沙盒] 提案」 加 提案编号 加 「已部署到底层」。
    否则：
        做 沙盒_回滚 用 提案编号、提案 的 部署检查点ID。
    完。
    返还 结果。
完。

沙盒_回滚 接受 提案编号、检查点ID：
    提案 为 做 沙盒_查找提案 用 提案编号。
    若 提案 不等于 空：
        提案 的 状态 为 5。
    完。
    结果 为 错误_无效参数。
    若 检查点ID 大于 0：
        结果 为 做 检查点_恢复 用 检查点ID。
        做 沙盒_释放检查点 用 检查点ID。
    完。
    若 提案 不等于 空：
        提案 的 部署检查点ID 为 0。
        做 沙盒_清理提案文件 用 提案编号。
    完。
    若 结果 等于 成功：
        沙盒_回滚次数 为 沙盒_回滚次数 加 1。
        做 打印 用 「[沙盒] 提案」 加 提案编号 加 「已回滚」。
    完。
    返还 结果。
完。

沙盒_清理提案文件 接受 提案编号：
    提案 为 做 沙盒_查找提案 用 提案编号。
    若 提案 等于 空：
        返还。
    完。
    沙盒文件 为 做 沙盒_提案文件路径 用 提案。
    若 做 文件_存在 用 沙盒文件 等于 真：
        做 文件_删除 用 沙盒文件。
    完。
    若 提案 的 沙盒文件已创建 等于 1 并且 沙盒_临时文件数 大于 0：
        沙盒_临时文件数 为 沙盒_临时文件数 减 1。
    完。
    提案 的 沙盒文件已创建 为 0。
    做 沙盒_释放检查点 用 提案 的 测试检查点ID。
    做 沙盒_释放检查点 用 提案 的 部署检查点ID。
    提案 的 测试检查点ID 为 0。
    提案 的 部署检查点ID 为 0。
    沙盒_清理次数 为 沙盒_清理次数 加 1。
完。

沙盒_清理失败提案 接受 零参：
    可变 i 为 做 计数 用 提案队列 减 1。
    只要 i 大于或等于 0：
        若 提案队列 的 第 i 项 的 状态 等于 3：
            做 沙盒_清理提案文件 用 提案队列 的 第 i 项 的 编号。
            做 列表_移除 用 提案队列、i。
        完。
        i 为 i 减 1。
    完。
完。

沙盒_全量清理 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 提案队列：
        若 提案队列 的 第 i 项 的 状态 不等于 4：
            做 沙盒_清理提案文件 用 提案队列 的 第 i 项 的 编号。
        完。
        i 为 i 加 1。
    完。
    沙盒_临时文件数 为 0。
    做 打印 用 「[沙盒] 全量清理完成,零冗余」。
完。

沙盒_自动演化轮 接受 零参：
    若 累计奖励 大于 100 并且 经验总数 大于 50：
        编号 为 做 沙盒_提交提案 用 「权重更新」、「赫布学习」、「神经中枢」、「突触权重」、200。
        结果 为 做 沙盒_测试提案 用 编号。
        若 结果 大于 0：
            做 沙盒_部署提案 用 编号。
        完。
    完。
    若 回流_总样本 大于 100：
        编号 为 做 沙盒_提交提案 用 「权重更新」、「云端回流」、「推理/模型持久化」、「蒸馏权重」、300。
        结果 为 做 沙盒_测试提案 用 编号。
        若 结果 大于 0：
            做 沙盒_部署提案 用 编号。
        完。
    完。
    做 沙盒_清理失败提案。
完。

沙盒_查找提案 接受 编号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 提案队列：
        若 提案队列 的 第 i 项 的 编号 等于 编号：
            返还 提案队列 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

沙盒_用户目录 为 「/nova/沙盒/用户实验/」。

可变 用户实验_总数 为 0。

可变 用户实验_成功数 为 0。

可变 用户实验_当前项目 为 「」。

沙盒_创建实验 接受 项目名：
    项目路径 为 沙盒_用户目录 加 项目名 加 「/」。
    做 记忆_创建目录 用 项目路径。
    做 记忆_创建目录 用 项目路径 加 「代码/」。
    做 记忆_创建目录 用 项目路径 加 「数据/」。
    做 记忆_创建目录 用 项目路径 加 「输出/」。
    用户实验_当前项目 为 项目名。
    用户实验_总数 为 用户实验_总数 加 1。
    做 打印 用 「[沙盒] 实验项目 '」 加 项目名 加 「' 已创建」。
    返还 项目路径。
完。

沙盒_运行实验 接受 项目名、入口文件：
    项目路径 为 沙盒_用户目录 加 项目名 加 「/」。
    代码路径 为 项目路径 加 「代码/」 加 入口文件。
    限制 为 一本字典：最大内存 对应 1073741824、最大CPU秒 对应 30、最大文件数 对应 100、最大输出字节 对应 10485760、允许网络 对应 假、允许文件写 对应 真。
    做 打印 用 「[沙盒] 运行实验: 」 加 项目名 加 「/」 加 入口文件。
    结果 为 做 沙盒_隔离执行 用 代码路径、限制。
    若 结果 的 退出码 等于 0：
        用户实验_成功数 为 用户实验_成功数 加 1。
        做 打印 用 「[沙盒] 实验成功 (耗时」 加 结果 的 耗时毫秒 加 「ms)」。
    否则：
        做 打印 用 「[沙盒] 实验失败: 退出码」 加 结果 的 退出码。
    完。
    返还 结果。
完。

沙盒_隔离执行 接受 代码路径、限制：
    返还 一本字典：退出码 对应 0、耗时毫秒 对应 100、输出 对应 「」。
完。

沙盒_推送到系统 接受 项目名、目标路径：
    源 为 沙盒_用户目录 加 项目名 加 「/代码/」。
    做 打印 用 「[沙盒] 推送 '」 加 项目名 加 「' 到 」 加 目标路径。
    推送检查点 为 做 检查点_创建 用 「沙盒推送-」 加 项目名。
    若 推送检查点 等于 空：
        返还 错误_内存不足。
    完。
    结果 为 做 沙盒_递归复制目录 用 源、目标路径。
    若 结果 小于 0：
        做 检查点_恢复 用 推送检查点 的 检查点ID。
        做 沙盒_释放检查点 用 推送检查点 的 检查点ID。
        返还 结果。
    完。
    做 沙盒_释放检查点 用 推送检查点 的 检查点ID。
    返还 成功。
完。

沙盒_清理实验 接受 项目名：
    项目路径 为 沙盒_用户目录 加 项目名 加 「/」。
    结果 为 做 沙盒_递归删除目录 用 项目路径。
    若 结果 等于 成功：
        做 打印 用 「[沙盒] 实验 '」 加 项目名 加 「' 已清理」。
    完。
    返还 结果。
完。

沙盒_列出实验 接受 零参：
    返还 做 记忆_列目录 用 沙盒_用户目录。
完。

本章 收束。
