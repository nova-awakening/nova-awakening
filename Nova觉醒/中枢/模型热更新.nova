《模型热更新》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·哈希整数》。

引用《..·感知·内核直连层》。

引用《.·内容可见性》。

最大缓冲 为 32。

最大模块 为 64。

模块_加载中 为 0。

模块_活跃 为 1。

模块_卸载中 为 2。

模块_已卸载 为 3。

双缓冲项 是 一种 事物，含 名称哈希、活跃指针、备用指针、大小、切换次数、最近切换。

热更新模块 是 一种 事物，含 名称哈希、代码指针、代码大小、状态、加载时刻、引用计数。

可变 缓冲表 为 一组：（空）。

可变 缓冲数 为 0。

可变 模块表 为 一组：（空）。

可变 模块数 为 0。

可变 切换总数 为 0。

可变 加载总数 为 0。

可变 卸载总数 为 0。

可变 回滚总数 为 0。

热更新_初始化 接受 零参：
    缓冲表 为 一组：（空）。
    缓冲数 为 0。
    模块表 为 一组：（空）。
    模块数 为 0。
完。

热更新_注册缓冲 接受 名称、初始指针、大小：
    若 缓冲数 大于或等于 最大缓冲：
        返还 负 1。
    完。
    项 为 新建 双缓冲项。
    项 的 名称哈希 为 做 哈希_字符串 用 名称。
    项 的 活跃指针 为 初始指针。
    项 的 备用指针 为 做 堆_分配 用 大小。
    项 的 大小 为 大小。
    项 的 切换次数 为 0。
    项 的 最近切换 为 0。
    做 追加 用 缓冲表、项。
    索引 为 缓冲数。
    缓冲数 为 缓冲数 加 1。
    返还 索引。
完。

热更新_获取活跃 接受 名称：
    目标 为 做 哈希_字符串 用 名称。
    可变 序号 为 0。
    只要 序号 小于 缓冲数：
        若 缓冲表 的 第 序号 项 的 名称哈希 等于 目标：
            返还 缓冲表 的 第 序号 项 的 活跃指针。
        完。
        序号 为 序号 加 1。
    完。
    返还 0。
完。

热更新_获取备用 接受 名称：
    目标 为 做 哈希_字符串 用 名称。
    可变 序号 为 0。
    只要 序号 小于 缓冲数：
        若 缓冲表 的 第 序号 项 的 名称哈希 等于 目标：
            返还 缓冲表 的 第 序号 项 的 备用指针。
        完。
        序号 为 序号 加 1。
    完。
    返还 0。
完。

热更新_原子切换 接受 名称：
    目标 为 做 哈希_字符串 用 名称。
    可变 序号 为 0。
    只要 序号 小于 缓冲数：
        若 缓冲表 的 第 序号 项 的 名称哈希 等于 目标：
            切换总数 为 切换总数 加 1。
            临时 为 缓冲表 的 第 序号 项 的 活跃指针。
            缓冲表 的 第 序号 项 的 活跃指针 为 缓冲表 的 第 序号 项 的 备用指针。
            缓冲表 的 第 序号 项 的 备用指针 为 临时。
            缓冲表 的 第 序号 项 的 切换次数 为 缓冲表 的 第 序号 项 的 切换次数 加 1。
            缓冲表 的 第 序号 项 的 最近切换 为 做 平台_获取高精度时间。
            返还 0。
        完。
        序号 为 序号 加 1。
    完。
    返还 错误_不存在。
完。

热更新_加载模块 接受 名称、代码、大小：
    若 模块数 大于或等于 最大模块：
        返还 负 1。
    完。
    加载总数 为 加载总数 加 1。
    模块 为 新建 热更新模块。
    模块 的 名称哈希 为 做 哈希_字符串 用 名称。
    模块 的 代码指针 为 做 堆_分配 用 大小。
    若 大小 大于 104857600：
        返还 负 1。
    完。
    做 内存_复制 用 模块 的 代码指针、代码、大小。
    模块 的 代码大小 为 大小。
    模块 的 状态 为 模块_活跃。
    模块 的 加载时刻 为 做 平台_获取高精度时间。
    模块 的 引用计数 为 0。
    做 追加 用 模块表、模块。
    索引 为 模块数。
    模块数 为 模块数 加 1。
    返还 索引。
完。

热更新_卸载模块 接受 名称：
    目标 为 做 哈希_字符串 用 名称。
    可变 序号 为 0。
    只要 序号 小于 模块数：
        若 模块表 的 第 序号 项 的 名称哈希 等于 目标 并且 模块表 的 第 序号 项 的 状态 等于 模块_活跃：
            若 模块表 的 第 序号 项 的 引用计数 大于 0：
                返还 错误_忙。
            完。
            模块表 的 第 序号 项 的 状态 为 模块_已卸载。
            做 堆_释放 用 模块表 的 第 序号 项 的 代码指针。
            模块表 的 第 序号 项 的 代码指针 为 0。
            卸载总数 为 卸载总数 加 1。
            返还 0。
        完。
        序号 为 序号 加 1。
    完。
    返还 错误_不存在。
完。

热更新_替换模块 接受 名称、新代码、新大小：
    目标 为 做 哈希_字符串 用 名称。
    可变 序号 为 0。
    只要 序号 小于 模块数：
        若 模块表 的 第 序号 项 的 名称哈希 等于 目标 并且 模块表 的 第 序号 项 的 状态 等于 模块_活跃：
            旧指针 为 模块表 的 第 序号 项 的 代码指针。
            模块表 的 第 序号 项 的 代码指针 为 做 堆_分配 用 新大小。
            若 新大小 大于 104857600：
                返还 负 1。
            完。
            做 内存_复制 用 模块表 的 第 序号 项 的 代码指针、新代码、新大小。
            模块表 的 第 序号 项 的 代码大小 为 新大小。
            做 堆_释放 用 旧指针。
            返还 0。
        完。
        序号 为 序号 加 1。
    完。
    返还 错误_不存在。
完。

热更新_增加引用 接受 名称：
    目标 为 做 哈希_字符串 用 名称。
    可变 序号 为 0。
    只要 序号 小于 模块数：
        若 模块表 的 第 序号 项 的 名称哈希 等于 目标：
            模块表 的 第 序号 项 的 引用计数 为 模块表 的 第 序号 项 的 引用计数 加 1。
            返还。
        完。
        序号 为 序号 加 1。
    完。
完。

热更新_减少引用 接受 名称：
    目标 为 做 哈希_字符串 用 名称。
    可变 序号 为 0。
    只要 序号 小于 模块数：
        若 模块表 的 第 序号 项 的 名称哈希 等于 目标 并且 模块表 的 第 序号 项 的 引用计数 大于 0：
            模块表 的 第 序号 项 的 引用计数 为 模块表 的 第 序号 项 的 引用计数 减 1。
            返还。
        完。
        序号 为 序号 加 1。
    完。
完。

热更新_活跃模块数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 模块数：
        若 模块表 的 第 序号 项 的 状态 等于 模块_活跃：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

热更新_完整统计 接受 零参：
    返还 一组：缓冲数、模块数、做 热更新_活跃模块数、切换总数、加载总数、卸载总数、回滚总数。
完。

本章 收束。
