《自旋锁》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·原子操作》。

引用《.·APIC任务优先级》。

引用《.·统一调度器核》。

引用《.·整数转字符串》。

引用《.·公平等待队列》。

引用《.·内核打印》。

同步_最大自旋次数 为 1000。

同步_读写锁_写标志 为 0x80000000。

同步统计 是 一种 事物，含 自旋锁_获取次数、自旋锁_争用次数、互斥锁_获取次数、互斥锁_争用次数、读写锁_读获取次数、读写锁_写获取次数、信号量_获取次数、信号量_释放次数、完成量_等待次数、完成量_完成次数、屏障_等待次数、条件变量_等待次数、条件变量_通知次数。

可变 全局_同步统计 为 空。

同步统计_初始化 接受 零参：
    若 全局_同步统计 等于 空：
        全局_同步统计 为 新建 同步统计。
        全局_同步统计 的 自旋锁_获取次数 为 0。
        全局_同步统计 的 自旋锁_争用次数 为 0。
        全局_同步统计 的 互斥锁_获取次数 为 0。
        全局_同步统计 的 互斥锁_争用次数 为 0。
        全局_同步统计 的 读写锁_读获取次数 为 0。
        全局_同步统计 的 读写锁_写获取次数 为 0。
        全局_同步统计 的 信号量_获取次数 为 0。
        全局_同步统计 的 信号量_释放次数 为 0。
        全局_同步统计 的 完成量_等待次数 为 0。
        全局_同步统计 的 完成量_完成次数 为 0。
        全局_同步统计 的 屏障_等待次数 为 0。
        全局_同步统计 的 条件变量_等待次数 为 0。
        全局_同步统计 的 条件变量_通知次数 为 0。
    完。
完。

自旋锁 是 一种 事物，含 锁定值、持有者、获取计数。

初始化自旋锁 接受 锁：
    做 原子写入32位 用 & 锁 的 锁定值、0。
    锁 的 持有者 为 负 1。
    锁 的 获取计数 为 0。
完。

自旋锁加锁 接受 锁：
    做 同步统计_初始化。
    可变 自旋次数 为 0。
    只要 非 做 原子比较交换32位 用 & 锁 的 锁定值、0、1：
        只要 做 原子读取32位 用 & 锁 的 锁定值 不等于 0：
            做 CPU暂停。
            自旋次数 为 自旋次数 加 1。
        完。
    完。
    若 自旋次数 大于 0：
        全局_同步统计 的 自旋锁_争用次数 为 全局_同步统计 的 自旋锁_争用次数 加 1。
    完。
    锁 的 持有者 为 做 获取当前CPU编号。
    锁 的 获取计数 累加 1。
    全局_同步统计 的 自旋锁_获取次数 为 全局_同步统计 的 自旋锁_获取次数 加 1。
完。

自旋锁尝试加锁 接受 锁：
    若 做 原子比较交换32位 用 & 锁 的 锁定值、0、1：
        锁 的 持有者 为 做 获取当前CPU编号。
        锁 的 获取计数 累加 1。
        返还 真。
    完。
    返还 假。
完。

自旋锁解锁 接受 锁：
    锁 的 持有者 为 负 1。
    做 原子写入32位 用 & 锁 的 锁定值、0。
完。

自旋锁是否持有 接受 锁：
    返还 做 原子读取32位 用 & 锁 的 锁定值 不等于 0。
完。

自旋锁_IRQ 是 一种 事物，含 锁、标志。

自旋锁_IRQ加锁 接受 锁：
    锁 的 标志 为 做 保存标志并禁用中断。
    做 自旋锁加锁 用 & 锁 的 锁。
完。

自旋锁_IRQ解锁 接受 锁：
    做 自旋锁解锁 用 & 锁 的 锁。
    做 恢复标志 用 锁 的 标志。
完。

互斥锁 是 一种 事物，含 锁定值、等待队列、持有者、自旋锁、争用计数。

初始化互斥锁 接受 互斥锁：
    做 原子写入32位 用 & 互斥锁 的 锁定值、0。
    做 初始化等待队列头 用 & 互斥锁 的 等待队列。
    做 初始化自旋锁 用 & 互斥锁 的 自旋锁。
    互斥锁 的 持有者 为 空。
完。

互斥锁加锁 接受 互斥锁：
    做 同步统计_初始化。
    全局_同步统计 的 互斥锁_获取次数 为 全局_同步统计 的 互斥锁_获取次数 加 1。
    若 做 原子比较交换32位 用 & 互斥锁 的 锁定值、0、1：
        互斥锁 的 持有者 为 做 获取当前任务。
        返还。
    完。
    做 原子增加64位 用 & 互斥锁 的 争用计数、1。
    全局_同步统计 的 互斥锁_争用次数 为 全局_同步统计 的 互斥锁_争用次数 加 1。
    做 自旋锁加锁 用 & 互斥锁 的 自旋锁。
    只要 做 原子读取32位 用 & 互斥锁 的 锁定值 不等于 0：
        等待项 为 做 创建等待队列项。
        做 添加到等待队列 用 & 互斥锁 的 等待队列、等待项。
        做 自旋锁解锁 用 & 互斥锁 的 自旋锁。
        做 设置任务状态_不可中断睡眠。
        做 调度。
        做 自旋锁加锁 用 & 互斥锁 的 自旋锁。
    完。
    做 原子写入32位 用 & 互斥锁 的 锁定值、1。
    互斥锁 的 持有者 为 做 获取当前任务。
    做 自旋锁解锁 用 & 互斥锁 的 自旋锁。
完。

互斥锁解锁 接受 互斥锁：
    互斥锁 的 持有者 为 空。
    做 原子写入32位 用 & 互斥锁 的 锁定值、0。
    做 自旋锁加锁 用 & 互斥锁 的 自旋锁。
    若 做 等待队列非空 用 & 互斥锁 的 等待队列：
        等待项 为 做 移除等待队列首项 用 & 互斥锁 的 等待队列。
        做 唤醒任务 用 等待项。
    完。
    做 自旋锁解锁 用 & 互斥锁 的 自旋锁。
完。

互斥锁尝试加锁 接受 互斥锁：
    若 做 原子比较交换32位 用 & 互斥锁 的 锁定值、0、1：
        互斥锁 的 持有者 为 做 获取当前任务。
        返还 真。
    完。
    返还 假。
完。

互斥锁是否持有 接受 互斥锁：
    返还 做 原子读取32位 用 & 互斥锁 的 锁定值 不等于 0。
完。

读写锁 是 一种 事物，含 状态、等待写者队列、等待读者队列、自旋锁、读者计数。

初始化读写锁 接受 锁：
    做 原子写入32位 用 & 锁 的 状态、0。
    做 原子写入32位 用 & 锁 的 读者计数、0。
    做 初始化等待队列头 用 & 锁 的 等待写者队列。
    做 初始化等待队列头 用 & 锁 的 等待读者队列。
    做 初始化自旋锁 用 & 锁 的 自旋锁。
完。

读写锁_读加锁 接受 锁：
    做 同步统计_初始化。
    全局_同步统计 的 读写锁_读获取次数 为 全局_同步统计 的 读写锁_读获取次数 加 1。
    做 自旋锁加锁 用 & 锁 的 自旋锁。
    只要 做 原子读取32位 用 & 锁 的 状态 位与 同步_读写锁_写标志 不等于 0：
        等待项 为 做 创建等待队列项。
        做 添加到等待队列 用 & 锁 的 等待读者队列、等待项。
        做 自旋锁解锁 用 & 锁 的 自旋锁。
        做 设置任务状态_不可中断睡眠。
        做 调度。
        做 自旋锁加锁 用 & 锁 的 自旋锁。
    完。
    做 原子增加32位 用 & 锁 的 状态、1。
    做 原子增加32位 用 & 锁 的 读者计数、1。
    做 自旋锁解锁 用 & 锁 的 自旋锁。
完。

读写锁_读解锁 接受 锁：
    做 自旋锁加锁 用 & 锁 的 自旋锁。
    做 原子减少32位 用 & 锁 的 状态、1。
    做 原子减少32位 用 & 锁 的 读者计数、1。
    若 做 原子读取32位 用 & 锁 的 状态 等于 0：
        若 做 等待队列非空 用 & 锁 的 等待写者队列：
            等待项 为 做 移除等待队列首项 用 & 锁 的 等待写者队列。
            做 唤醒任务 用 等待项。
        完。
    完。
    做 自旋锁解锁 用 & 锁 的 自旋锁。
完。

读写锁_写加锁 接受 锁：
    做 同步统计_初始化。
    全局_同步统计 的 读写锁_写获取次数 为 全局_同步统计 的 读写锁_写获取次数 加 1。
    做 自旋锁加锁 用 & 锁 的 自旋锁。
    只要 做 原子读取32位 用 & 锁 的 状态 不等于 0：
        等待项 为 做 创建等待队列项。
        做 添加到等待队列 用 & 锁 的 等待写者队列、等待项。
        做 自旋锁解锁 用 & 锁 的 自旋锁。
        做 设置任务状态_不可中断睡眠。
        做 调度。
        做 自旋锁加锁 用 & 锁 的 自旋锁。
    完。
    做 原子写入32位 用 & 锁 的 状态、同步_读写锁_写标志。
    做 自旋锁解锁 用 & 锁 的 自旋锁。
完。

读写锁_写解锁 接受 锁：
    做 自旋锁加锁 用 & 锁 的 自旋锁。
    做 原子写入32位 用 & 锁 的 状态、0。
    只要 做 等待队列非空 用 & 锁 的 等待读者队列：
        等待项 为 做 移除等待队列首项 用 & 锁 的 等待读者队列。
        做 唤醒任务 用 等待项。
    完。
    若 做 等待队列非空 用 & 锁 的 等待写者队列：
        等待项 为 做 移除等待队列首项 用 & 锁 的 等待写者队列。
        做 唤醒任务 用 等待项。
    完。
    做 自旋锁解锁 用 & 锁 的 自旋锁。
完。

信号量 是 一种 事物，含 计数、等待队列、自旋锁。

信号量P 接受 信号量：
    做 同步统计_初始化。
    全局_同步统计 的 信号量_获取次数 为 全局_同步统计 的 信号量_获取次数 加 1。
    做 自旋锁加锁 用 & 信号量 的 自旋锁。
    只要 做 原子读取32位 用 & 信号量 的 计数 小于或等于 0：
        等待项 为 做 创建等待队列项。
        做 添加到等待队列 用 & 信号量 的 等待队列、等待项。
        做 自旋锁解锁 用 & 信号量 的 自旋锁。
        做 设置任务状态_不可中断睡眠。
        做 调度。
        做 自旋锁加锁 用 & 信号量 的 自旋锁。
    完。
    做 原子减少32位 用 & 信号量 的 计数、1。
    做 自旋锁解锁 用 & 信号量 的 自旋锁。
完。

信号量V 接受 信号量：
    做 同步统计_初始化。
    全局_同步统计 的 信号量_释放次数 为 全局_同步统计 的 信号量_释放次数 加 1。
    做 自旋锁加锁 用 & 信号量 的 自旋锁。
    做 原子增加32位 用 & 信号量 的 计数、1。
    若 做 等待队列非空 用 & 信号量 的 等待队列：
        等待项 为 做 移除等待队列首项 用 & 信号量 的 等待队列。
        做 唤醒任务 用 等待项。
    完。
    做 自旋锁解锁 用 & 信号量 的 自旋锁。
完。

信号量尝试P 接受 信号量：
    若 做 原子读取32位 用 & 信号量 的 计数 大于 0：
        做 自旋锁加锁 用 & 信号量 的 自旋锁。
        若 做 原子读取32位 用 & 信号量 的 计数 大于 0：
            做 原子减少32位 用 & 信号量 的 计数、1。
            做 自旋锁解锁 用 & 信号量 的 自旋锁。
            返还 真。
        完。
        做 自旋锁解锁 用 & 信号量 的 自旋锁。
    完。
    返还 假。
完。

信号量获取计数 接受 信号量：
    返还 做 原子读取32位 用 & 信号量 的 计数。
完。

完成量 是 一种 事物，含 完成标志、等待队列、自旋锁。

初始化完成量 接受 完成量：
    做 原子写入32位 用 & 完成量 的 完成标志、0。
    做 初始化等待队列头 用 & 完成量 的 等待队列。
    做 初始化自旋锁 用 & 完成量 的 自旋锁。
完。

等待完成 接受 完成量：
    做 同步统计_初始化。
    全局_同步统计 的 完成量_等待次数 为 全局_同步统计 的 完成量_等待次数 加 1。
    若 做 原子读取32位 用 & 完成量 的 完成标志 不等于 0：
        返还。
    完。
    做 自旋锁加锁 用 & 完成量 的 自旋锁。
    只要 做 原子读取32位 用 & 完成量 的 完成标志 等于 0：
        等待项 为 做 创建等待队列项。
        做 添加到等待队列 用 & 完成量 的 等待队列、等待项。
        做 自旋锁解锁 用 & 完成量 的 自旋锁。
        做 设置任务状态_不可中断睡眠。
        做 调度。
        做 自旋锁加锁 用 & 完成量 的 自旋锁。
    完。
    做 自旋锁解锁 用 & 完成量 的 自旋锁。
完。

通知完成 接受 完成量：
    做 同步统计_初始化。
    全局_同步统计 的 完成量_完成次数 为 全局_同步统计 的 完成量_完成次数 加 1。
    做 原子写入32位 用 & 完成量 的 完成标志、1。
    做 自旋锁加锁 用 & 完成量 的 自旋锁。
    只要 做 等待队列非空 用 & 完成量 的 等待队列：
        等待项 为 做 移除等待队列首项 用 & 完成量 的 等待队列。
        做 唤醒任务 用 等待项。
    完。
    做 自旋锁解锁 用 & 完成量 的 自旋锁。
完。

重置完成量 接受 完成量：
    做 原子写入32位 用 & 完成量 的 完成标志、0。
完。

屏障 是 一种 事物，含 总数、等待计数、代、等待队列、自旋锁。

初始化屏障 接受 屏障、总数：
    屏障 的 总数 为 总数。
    做 原子写入32位 用 & 屏障 的 等待计数、0。
    做 原子写入32位 用 & 屏障 的 代、0。
    做 初始化等待队列头 用 & 屏障 的 等待队列。
    做 初始化自旋锁 用 & 屏障 的 自旋锁。
完。

屏障等待 接受 屏障：
    做 同步统计_初始化。
    全局_同步统计 的 屏障_等待次数 为 全局_同步统计 的 屏障_等待次数 加 1。
    当前代 为 做 原子读取32位 用 & 屏障 的 代。
    新计数 为 做 原子增加32位 用 & 屏障 的 等待计数、1。
    若 新计数 大于或等于 屏障 的 总数：
        做 原子写入32位 用 & 屏障 的 等待计数、0。
        做 原子增加32位 用 & 屏障 的 代、1。
        做 自旋锁加锁 用 & 屏障 的 自旋锁。
        只要 做 等待队列非空 用 & 屏障 的 等待队列：
            等待项 为 做 移除等待队列首项 用 & 屏障 的 等待队列。
            做 唤醒任务 用 等待项。
        完。
        做 自旋锁解锁 用 & 屏障 的 自旋锁。
        返还。
    完。
    做 自旋锁加锁 用 & 屏障 的 自旋锁。
    只要 做 原子读取32位 用 & 屏障 的 代 等于 当前代：
        等待项 为 做 创建等待队列项。
        做 添加到等待队列 用 & 屏障 的 等待队列、等待项。
        做 自旋锁解锁 用 & 屏障 的 自旋锁。
        做 设置任务状态_不可中断睡眠。
        做 调度。
        做 自旋锁加锁 用 & 屏障 的 自旋锁。
    完。
    做 自旋锁解锁 用 & 屏障 的 自旋锁。
完。

条件变量 是 一种 事物，含 等待队列、自旋锁。

初始化条件变量 接受 条件：
    做 初始化等待队列头 用 & 条件 的 等待队列。
    做 初始化自旋锁 用 & 条件 的 自旋锁。
完。

条件变量_等待 接受 条件、互斥锁：
    做 同步统计_初始化。
    全局_同步统计 的 条件变量_等待次数 为 全局_同步统计 的 条件变量_等待次数 加 1。
    做 自旋锁加锁 用 & 条件 的 自旋锁。
    等待项 为 做 创建等待队列项。
    做 添加到等待队列 用 & 条件 的 等待队列、等待项。
    做 自旋锁解锁 用 & 条件 的 自旋锁。
    做 互斥锁解锁 用 互斥锁。
    做 设置任务状态_不可中断睡眠。
    做 调度。
    做 互斥锁加锁 用 互斥锁。
完。

条件变量_通知一个 接受 条件：
    做 同步统计_初始化。
    全局_同步统计 的 条件变量_通知次数 为 全局_同步统计 的 条件变量_通知次数 加 1。
    做 自旋锁加锁 用 & 条件 的 自旋锁。
    若 做 等待队列非空 用 & 条件 的 等待队列：
        等待项 为 做 移除等待队列首项 用 & 条件 的 等待队列。
        做 唤醒任务 用 等待项。
    完。
    做 自旋锁解锁 用 & 条件 的 自旋锁。
完。

条件变量_通知全部 接受 条件：
    做 同步统计_初始化。
    全局_同步统计 的 条件变量_通知次数 为 全局_同步统计 的 条件变量_通知次数 加 1。
    做 自旋锁加锁 用 & 条件 的 自旋锁。
    只要 做 等待队列非空 用 & 条件 的 等待队列：
        等待项 为 做 移除等待队列首项 用 & 条件 的 等待队列。
        做 唤醒任务 用 等待项。
    完。
    做 自旋锁解锁 用 & 条件 的 自旋锁。
完。

同步_打印统计 接受 零参：
    做 同步统计_初始化。
    做 打印行 用 「=== 同步原语统计 ===」。
    做 打印行 用 「自旋锁获取: 」 加 做 整数转字符串 用 全局_同步统计 的 自旋锁_获取次数。
    做 打印行 用 「自旋锁争用: 」 加 做 整数转字符串 用 全局_同步统计 的 自旋锁_争用次数。
    做 打印行 用 「互斥锁获取: 」 加 做 整数转字符串 用 全局_同步统计 的 互斥锁_获取次数。
    做 打印行 用 「互斥锁争用: 」 加 做 整数转字符串 用 全局_同步统计 的 互斥锁_争用次数。
    做 打印行 用 「读锁获取: 」 加 做 整数转字符串 用 全局_同步统计 的 读写锁_读获取次数。
    做 打印行 用 「写锁获取: 」 加 做 整数转字符串 用 全局_同步统计 的 读写锁_写获取次数。
    做 打印行 用 「信号量P: 」 加 做 整数转字符串 用 全局_同步统计 的 信号量_获取次数。
    做 打印行 用 「信号量V: 」 加 做 整数转字符串 用 全局_同步统计 的 信号量_释放次数。
    做 打印行 用 「完成量等待: 」 加 做 整数转字符串 用 全局_同步统计 的 完成量_等待次数。
    做 打印行 用 「完成量通知: 」 加 做 整数转字符串 用 全局_同步统计 的 完成量_完成次数。
    做 打印行 用 「屏障等待: 」 加 做 整数转字符串 用 全局_同步统计 的 屏障_等待次数。
    做 打印行 用 「条件等待: 」 加 做 整数转字符串 用 全局_同步统计 的 条件变量_等待次数。
    做 打印行 用 「条件通知: 」 加 做 整数转字符串 用 全局_同步统计 的 条件变量_通知次数。
    做 打印行 用 「序列锁读: 」 加 做 整数转字符串 用 全局_同步统计_序列锁_读次数。
    做 打印行 用 「序列锁写: 」 加 做 整数转字符串 用 全局_同步统计_序列锁_写次数。
完。

可变 全局_同步统计_序列锁_读次数 为 0。

可变 全局_同步统计_序列锁_写次数 为 0。

序列锁 是 一种 事物，含 序号、写锁。

初始化序列锁 接受 锁：
    做 原子写入32位 用 & 锁 的 序号、0。
    做 初始化自旋锁 用 & 锁 的 写锁。
完。

序列锁_写开始 接受 锁：
    全局_同步统计_序列锁_写次数 为 全局_同步统计_序列锁_写次数 加 1。
    做 自旋锁加锁 用 & 锁 的 写锁。
    做 原子增加32位 用 & 锁 的 序号、1。
    做 内存屏障。
完。

序列锁_写结束 接受 锁：
    做 内存屏障。
    做 原子增加32位 用 & 锁 的 序号、1。
    做 自旋锁解锁 用 & 锁 的 写锁。
完。

序列锁_读开始 接受 锁：
    全局_同步统计_序列锁_读次数 为 全局_同步统计_序列锁_读次数 加 1。
    可变 序号 为 做 原子读取32位 用 & 锁 的 序号。
    只要 序号 位与 1 不等于 0：
        做 CPU暂停。
        序号 为 做 原子读取32位 用 & 锁 的 序号。
    完。
    做 读内存屏障。
    返还 序号。
完。

序列锁_读重试 接受 锁、开始序号：
    做 读内存屏障。
    返还 做 原子读取32位 用 & 锁 的 序号 不等于 开始序号。
完。

锁顺序_最大深度 为 8。

每任务_锁记录 是 一种 事物，含 锁地址列表、锁顺序列表、深度。

死锁检测_记录获取 接受 记录、锁地址、顺序号：
    若 记录 等于 空：
        返还 真。
    完。
    若 记录 的 深度 大于或等于 锁顺序_最大深度：
        返还 真。
    完。
    可变 i 为 0。
    只要 i 小于 记录 的 深度：
        若 记录 的 锁顺序列表 的 第 i 项 大于或等于 顺序号：
            做 打印行 用 「[NSPT] 死锁风险: 顺序号 」 加 做 整数转字符串 用 顺序号 加 「 <= 已持有 」 加 做 整数转字符串 用 记录 的 锁顺序列表 的 第 i 项。
            返还 假。
        完。
        若 记录 的 锁地址列表 的 第 i 项 等于 锁地址：
            做 打印行 用 「[NSPT] 死锁: 同一锁重复获取」。
            返还 假。
        完。
        i 为 i 加 1。
    完。
    做 设置 用 记录 的 锁地址列表、记录 的 深度、锁地址。
    做 设置 用 记录 的 锁顺序列表、记录 的 深度、顺序号。
    记录 的 深度 为 记录 的 深度 加 1。
    返还 真。
完。

死锁检测_记录释放 接受 记录、锁地址：
    若 记录 等于 空 或者 记录 的 深度 小于或等于 0：
        返还。
    完。
    若 记录 的 锁地址列表 的 第 记录 的 深度 减 1 项 等于 锁地址：
        记录 的 深度 为 记录 的 深度 减 1。
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 记录 的 深度：
        若 记录 的 锁地址列表 的 第 i 项 等于 锁地址：
            可变 j 为 i。
            只要 j 小于 记录 的 深度 减 1：
                做 设置 用 记录 的 锁地址列表、j、记录 的 锁地址列表 的 第 j 加 1 项。
                做 设置 用 记录 的 锁顺序列表、j、记录 的 锁顺序列表 的 第 j 加 1 项。
                j 为 j 加 1。
            完。
            记录 的 深度 为 记录 的 深度 减 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

同步_重置统计 接受 零参：
    做 同步统计_初始化。
    全局_同步统计 的 自旋锁_获取次数 为 0。
    全局_同步统计 的 自旋锁_争用次数 为 0。
    全局_同步统计 的 互斥锁_获取次数 为 0。
    全局_同步统计 的 互斥锁_争用次数 为 0。
    全局_同步统计 的 读写锁_读获取次数 为 0。
    全局_同步统计 的 读写锁_写获取次数 为 0。
    全局_同步统计 的 信号量_获取次数 为 0。
    全局_同步统计 的 信号量_释放次数 为 0。
    全局_同步统计 的 完成量_等待次数 为 0。
    全局_同步统计 的 完成量_完成次数 为 0。
    全局_同步统计 的 屏障_等待次数 为 0。
    全局_同步统计 的 条件变量_等待次数 为 0。
    全局_同步统计 的 条件变量_通知次数 为 0。
    全局_同步统计_序列锁_读次数 为 0。
    全局_同步统计_序列锁_写次数 为 0。
完。

同步_获取争用率 接受 零参：
    做 同步统计_初始化。
    总获取 为 全局_同步统计 的 自旋锁_获取次数 加 全局_同步统计 的 互斥锁_获取次数。
    若 总获取 等于 0：
        返还 0。
    完。
    总争用 为 全局_同步统计 的 自旋锁_争用次数 加 全局_同步统计 的 互斥锁_争用次数。
    返还 总争用 乘 10000 除以 总获取。
完。

同步_诊断 接受 零参：
    做 同步_打印统计。
    争用率 为 做 同步_获取争用率。
    做 打印行 用 「争用率(万分): 」 加 做 整数转字符串 用 争用率。
    若 争用率 大于 5000：
        做 打印行 用 「[NSPT] 警告: 争用率超过50%, 建议优化锁粒度」。
    完。
完。

可变 AI_同步原语生_风暴阈值 为 10000。

可变 AI_同步原语生_预测IRQ 为 0。

AI_同步原语生_风暴检测 接受 触发频率：
    若 触发频率 大于 AI_同步原语生_风暴阈值：
        AI_同步原语生_风暴阈值 为 AI_同步原语生_风暴阈值 乘 3 除以 2。
        返还 真。
    完。
    返还 假。
完。

AI_同步原语生_自适应合并 接受 当前延迟：
    若 当前延迟 小于 100：
        返还 16。
    完。
    若 当前延迟 小于 500：
        返还 8。
    完。
    返还 1。
完。

本章 收束。
