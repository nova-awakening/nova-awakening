《红黑树删除》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·红黑树后继》。

引用《.·红黑树创建》。

可变 _RB删_总数 为 0。

可变 _RB删_惰性标记数 为 0。

可变 _RB删_批量次数 为 0。

可变 _RB删_最大批量 为 0。

可变 _RB删_清扫回收 为 0。

可变 _RB删_范围删除数 为 0。

可变 _惰性删除集 为 一组：（空）。

红黑树_删除 接受 树、键：
    z 为 做 红黑树_查找 用 树、键。
    若 z 等于 空：
        返还 错误_不存在。
    完。
    _RB删_总数 为 _RB删_总数 加 1。
    可变 y 为 z。
    y原色 为 y 的 第 RB_色 项。
    可变 x 为 空。
    可变 x父 为 空。
    若 z 的 第 RB_左 项 等于 空：
        x 为 z 的 第 RB_右 项。
        x父 为 z 的 第 RB_父 项。
        做 红黑树_移植 用 树、z、z 的 第 RB_右 项。
    否则若 z 的 第 RB_右 项 等于 空：
        x 为 z 的 第 RB_左 项。
        x父 为 z 的 第 RB_父 项。
        做 红黑树_移植 用 树、z、z 的 第 RB_左 项。
    否则：
        y 为 z 的 第 RB_右 项。
        只要 y 的 第 RB_左 项 不等于 空：
            y 为 y 的 第 RB_左 项。
        完。
        y原色 为 y 的 第 RB_色 项。
        x 为 y 的 第 RB_右 项。
        若 y 的 第 RB_父 项 等于 z：
            x父 为 y。
        否则：
            x父 为 y 的 第 RB_父 项。
            做 红黑树_移植 用 树、y、y 的 第 RB_右 项。
            做 设置 用 y、RB_右、z 的 第 RB_右 项。
            若 z 的 第 RB_右 项 不等于 空：
                做 设置 用 z 的 第 RB_右 项、RB_父、y。
            完。
        完。
        做 红黑树_移植 用 树、z、y。
        做 设置 用 y、RB_左、z 的 第 RB_左 项。
        若 z 的 第 RB_左 项 不等于 空：
            做 设置 用 z 的 第 RB_左 项、RB_父、y。
        完。
        做 设置 用 y、RB_色、z 的 第 RB_色 项。
    完。
    做 设置 用 树、1、树 的 第 1 项 减 1。
    若 y原色 等于 黑：
        做 红黑树_删除修复 用 树、x、x父。
    完。
    返还 成功。
完。

红黑树_删除最小 接受 树：
    最小 为 做 红黑树_最小 用 树。
    若 最小 等于 空：
        返还 空。
    完。
    键 为 最小 的 第 RB_键 项。
    值 为 最小 的 第 RB_值 项。
    做 红黑树_删除 用 树、键。
    返还 一组：键、值。
完。

红黑树_删除最大 接受 树：
    可变 节点 为 树 的 第 0 项。
    若 节点 等于 空：
        返还 空。
    完。
    只要 节点 的 第 RB_右 项 不等于 空：
        节点 为 节点 的 第 RB_右 项。
    完。
    键 为 节点 的 第 RB_键 项。
    值 为 节点 的 第 RB_值 项。
    做 红黑树_删除 用 树、键。
    返还 一组：键、值。
完。

红黑树_惰性删除 接受 树、键：
    节点 为 做 红黑树_查找 用 树、键。
    若 节点 等于 空：
        返还 错误_不存在。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _惰性删除集：
        若 _惰性删除集 的 第 i 项 等于 键：
            返还 错误_不存在。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 _惰性删除集、键。
    _RB删_惰性标记数 为 _RB删_惰性标记数 加 1。
    返还 成功。
完。

红黑树_是惰性删除 接受 键：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _惰性删除集：
        若 _惰性删除集 的 第 i 项 等于 键：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

红黑树_惰性查找 接受 树、键：
    若 做 红黑树_是惰性删除 用 键：
        返还 空。
    完。
    返还 做 红黑树_查找 用 树、键。
完。

红黑树_惰性清扫 接受 树：
    可变 清扫数 为 做 计数 用 _惰性删除集。
    可变 i 为 0。
    只要 i 小于 清扫数：
        做 红黑树_删除 用 树、_惰性删除集 的 第 i 项。
        i 为 i 加 1。
    完。
    _RB删_清扫回收 为 _RB删_清扫回收 加 清扫数。
    _惰性删除集 为 一组：（空）。
    返还 清扫数。
完。

红黑树_惰性待清数 接受 零参：
    返还 做 计数 用 _惰性删除集。
完。

红黑树_批量删除 接受 树、键列表：
    数量 为 做 计数 用 键列表。
    若 数量 等于 0：
        返还 0。
    完。
    可变 成功数 为 0。
    可变 i 为 0。
    只要 i 小于 数量：
        若 做 红黑树_删除 用 树、键列表 的 第 i 项 等于 成功：
            成功数 为 成功数 加 1。
        完。
        i 为 i 加 1。
    完。
    _RB删_批量次数 为 _RB删_批量次数 加 1。
    若 数量 大于 _RB删_最大批量：
        _RB删_最大批量 为 数量。
    完。
    返还 成功数。
完。

红黑树_范围删除 接受 树、低、高：
    键列表 为 一组：（空）。
    做 _红黑树_收集范围键 用 树 的 第 0 项、低、高、键列表。
    _RB删_范围删除数 为 _RB删_范围删除数 加 1。
    返还 做 红黑树_批量删除 用 树、键列表。
完。

_红黑树_收集范围键 接受 节点、低、高、结果：
    若 节点 等于 空：
        返还。
    完。
    若 节点 的 第 RB_键 项 大于 低：
        做 _红黑树_收集范围键 用 节点 的 第 RB_左 项、低、高、结果。
    完。
    若 节点 的 第 RB_键 项 大于或等于 低 并且 节点 的 第 RB_键 项 小于或等于 高：
        做 追加 用 结果、节点 的 第 RB_键 项。
    完。
    若 节点 的 第 RB_键 项 小于 高：
        做 _红黑树_收集范围键 用 节点 的 第 RB_右 项、低、高、结果。
    完。
完。

红黑树_条件删除 接受 树、值阈值：
    键列表 为 一组：（空）。
    做 _红黑树_收集条件键 用 树 的 第 0 项、值阈值、键列表。
    返还 做 红黑树_批量删除 用 树、键列表。
完。

_红黑树_收集条件键 接受 节点、阈值、结果：
    若 节点 等于 空：
        返还。
    完。
    做 _红黑树_收集条件键 用 节点 的 第 RB_左 项、阈值、结果。
    若 节点 的 第 RB_值 项 小于 阈值：
        做 追加 用 结果、节点 的 第 RB_键 项。
    完。
    做 _红黑树_收集条件键 用 节点 的 第 RB_右 项、阈值、结果。
完。

红黑树_安全遍历删除 接受 树、判断函数：
    待删除 为 一组：（空）。
    做 _红黑树_安全收集 用 树 的 第 0 项、判断函数、待删除。
    返还 做 红黑树_批量删除 用 树、待删除。
完。

_红黑树_安全收集 接受 节点、判断、结果：
    若 节点 等于 空：
        返还。
    完。
    做 _红黑树_安全收集 用 节点 的 第 RB_左 项、判断、结果。
    若 做 判断 用 节点 的 第 RB_键 项、节点 的 第 RB_值 项：
        做 追加 用 结果、节点 的 第 RB_键 项。
    完。
    做 _红黑树_安全收集 用 节点 的 第 RB_右 项、判断、结果。
完。

_红黑树_子树大小 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    返还 1 加 做 _红黑树_子树大小 用 节点 的 第 RB_左 项 加 做 _红黑树_子树大小 用 节点 的 第 RB_右 项。
完。

红黑树_第k小 接受 树、k：
    返还 做 _红黑树_第k小递归 用 树 的 第 0 项、k。
完。

_红黑树_第k小递归 接受 节点、k：
    若 节点 等于 空：
        返还 空。
    完。
    左大小 为 做 _红黑树_子树大小 用 节点 的 第 RB_左 项。
    若 k 小于 左大小：
        返还 做 _红黑树_第k小递归 用 节点 的 第 RB_左 项、k。
    完。
    若 k 等于 左大小：
        返还 一组：节点 的 第 RB_键 项、节点 的 第 RB_值 项。
    完。
    返还 做 _红黑树_第k小递归 用 节点 的 第 RB_右 项、k 减 左大小 减 1。
完。

红黑树_秩 接受 树、键：
    返还 做 _红黑树_秩递归 用 树 的 第 0 项、键。
完。

_红黑树_秩递归 接受 节点、键：
    若 节点 等于 空：
        返还 0。
    完。
    若 键 小于 节点 的 第 RB_键 项：
        返还 做 _红黑树_秩递归 用 节点 的 第 RB_左 项、键。
    完。
    若 键 等于 节点 的 第 RB_键 项：
        返还 做 _红黑树_子树大小 用 节点 的 第 RB_左 项。
    完。
    返还 1 加 做 _红黑树_子树大小 用 节点 的 第 RB_左 项 加 做 _红黑树_秩递归 用 节点 的 第 RB_右 项、键。
完。

红黑树_范围查询_递归 接受 节点、低、高、结果：
    若 节点 等于 空：
        返还。
    完。
    若 节点 的 第 RB_键 项 大于 低：
        做 红黑树_范围查询_递归 用 节点 的 第 RB_左 项、低、高、结果。
    完。
    若 节点 的 第 RB_键 项 大于或等于 低 并且 节点 的 第 RB_键 项 小于或等于 高：
        做 追加 用 结果、一组：节点 的 第 RB_键 项、节点 的 第 RB_值 项。
    完。
    若 节点 的 第 RB_键 项 小于 高：
        做 红黑树_范围查询_递归 用 节点 的 第 RB_右 项、低、高、结果。
    完。
完。

红黑树_范围查询 接受 树、低、高：
    结果 为 一组：（空）。
    做 红黑树_范围查询_递归 用 树 的 第 0 项、低、高、结果。
    返还 结果。
完。

红黑树_下界 接受 树、键：
    可变 节点 为 树 的 第 0 项。
    可变 最佳 为 空。
    只要 节点 不等于 空：
        若 节点 的 第 RB_键 项 大于或等于 键：
            最佳 为 节点。
            节点 为 节点 的 第 RB_左 项。
        否则：
            节点 为 节点 的 第 RB_右 项。
        完。
    完。
    返还 最佳。
完。

红黑树_上界 接受 树、键：
    可变 节点 为 树 的 第 0 项。
    可变 最佳 为 空。
    只要 节点 不等于 空：
        若 节点 的 第 RB_键 项 大于 键：
            最佳 为 节点。
            节点 为 节点 的 第 RB_左 项。
        否则：
            节点 为 节点 的 第 RB_右 项。
        完。
    完。
    返还 最佳。
完。

红黑树_前驱 接受 树、键：
    可变 节点 为 树 的 第 0 项。
    可变 最佳 为 空。
    只要 节点 不等于 空：
        若 节点 的 第 RB_键 项 小于 键：
            最佳 为 节点。
            节点 为 节点 的 第 RB_右 项。
        否则：
            节点 为 节点 的 第 RB_左 项。
        完。
    完。
    返还 最佳。
完。

红黑树_高度_递归 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    左高 为 做 红黑树_高度_递归 用 节点 的 第 RB_左 项。
    右高 为 做 红黑树_高度_递归 用 节点 的 第 RB_右 项。
    若 左高 大于 右高：
        返还 左高 加 1。
    完。
    返还 右高 加 1。
完。

红黑树_高度 接受 树：
    返还 做 红黑树_高度_递归 用 树 的 第 0 项。
完。

_红黑树_黑高验证 接受 节点：
    若 节点 等于 空：
        返还 1。
    完。
    左黑高 为 做 _红黑树_黑高验证 用 节点 的 第 RB_左 项。
    右黑高 为 做 _红黑树_黑高验证 用 节点 的 第 RB_右 项。
    若 左黑高 等于 负 1 或者 右黑高 等于 负 1 或者 左黑高 不等于 右黑高：
        返还 负 1。
    完。
    若 节点 的 第 RB_色 项 等于 黑：
        返还 左黑高 加 1。
    完。
    返还 左黑高。
完。

红黑树_验证 接受 树：
    返还 做 _红黑树_黑高验证 用 树 的 第 0 项 大于或等于 0。
完。

红黑树_中序遍历 接受 节点、结果：
    若 节点 等于 空：
        返还。
    完。
    做 红黑树_中序遍历 用 节点 的 第 RB_左 项、结果。
    做 追加 用 结果、一组：节点 的 第 RB_键 项、节点 的 第 RB_值 项。
    做 红黑树_中序遍历 用 节点 的 第 RB_右 项、结果。
完。

红黑树_到列表 接受 树：
    结果 为 一组：（空）。
    做 红黑树_中序遍历 用 树 的 第 0 项、结果。
    返还 结果。
完。

_红黑树_反序 接受 节点、结果：
    若 节点 等于 空：
        返还。
    完。
    做 _红黑树_反序 用 节点 的 第 RB_右 项、结果。
    做 追加 用 结果、一组：节点 的 第 RB_键 项、节点 的 第 RB_值 项。
    做 _红黑树_反序 用 节点 的 第 RB_左 项、结果。
完。

红黑树_到反序列表 接受 树：
    结果 为 一组：（空）。
    做 _红黑树_反序 用 树 的 第 0 项、结果。
    返还 结果。
完。

红黑树_前N最小 接受 树、n：
    结果 为 一组：（空）。
    做 _红黑树_前N 用 树 的 第 0 项、n、结果。
    返还 结果。
完。

_红黑树_前N 接受 节点、n、结果：
    若 节点 等于 空 或者 做 计数 用 结果 大于或等于 n：
        返还。
    完。
    做 _红黑树_前N 用 节点 的 第 RB_左 项、n、结果。
    若 做 计数 用 结果 小于 n：
        做 追加 用 结果、一组：节点 的 第 RB_键 项、节点 的 第 RB_值 项。
    完。
    若 做 计数 用 结果 小于 n：
        做 _红黑树_前N 用 节点 的 第 RB_右 项、n、结果。
    完。
完。

红黑树删除_完整统计 接受 树：
    返还 一组：一组：「元素数」、树 的 第 1 项、一组：「树高度」、做 红黑树_高度 用 树、一组：「总删除」、_RB删_总数、一组：「惰性待清」、做 计数 用 _惰性删除集、一组：「惰性总标记」、_RB删_惰性标记数、一组：「清扫回收」、_RB删_清扫回收、一组：「批量次数」、_RB删_批量次数、一组：「最大批量」、_RB删_最大批量、一组：「范围删除」、_RB删_范围删除数、一组：「结构有效」、做 红黑树_验证 用 树。
完。

红黑树_节点数_递归 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    返还 1 加 做 红黑树_节点数_递归 用 节点 的 第 RB_左 项 加 做 红黑树_节点数_递归 用 节点 的 第 RB_右 项。
完。

红黑树_红节点数_递归 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    可变 c 为 0。
    若 节点 的 第 RB_色 项 等于 红：
        c 为 1。
    完。
    返还 c 加 做 红黑树_红节点数_递归 用 节点 的 第 RB_左 项 加 做 红黑树_红节点数_递归 用 节点 的 第 RB_右 项。
完。

红黑树_黑高 接受 树：
    可变 节点 为 树 的 第 0 项。
    可变 h 为 0。
    只要 节点 不等于 空：
        若 节点 的 第 RB_色 项 等于 黑：
            h 为 h 加 1。
        完。
        节点 为 节点 的 第 RB_左 项。
    完。
    返还 h。
完。

红黑树_合并 接受 树a、树b：
    元素 为 一组：（空）。
    做 红黑树_中序遍历 用 树b 的 第 0 项、元素。
    可变 i 为 0。
    只要 i 小于 做 计数 用 元素：
        做 红黑树_插入 用 树a、元素 的 第 i 项 的 第 0 项、元素 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

红黑树_分裂 接受 树、枢轴键：
    所有 为 一组：（空）。
    做 红黑树_中序遍历 用 树 的 第 0 项、所有。
    左树 为 做 红黑树_创建。
    右树 为 做 红黑树_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有：
        若 所有 的 第 i 项 的 第 0 项 小于 枢轴键：
            做 红黑树_插入 用 左树、所有 的 第 i 项 的 第 0 项、所有 的 第 i 项 的 第 1 项。
        否则：
            做 红黑树_插入 用 右树、所有 的 第 i 项 的 第 0 项、所有 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：左树、右树。
完。

红黑树删除_重置统计 接受 零参：
    _RB删_总数 为 0。
    _RB删_惰性标记数 为 0。
    _RB删_清扫回收 为 0。
    _RB删_批量次数 为 0。
    _RB删_最大批量 为 0。
    _RB删_范围删除数 为 0。
    _惰性删除集 为 一组：（空）。
完。

红黑树_批量插入 接受 树、键值列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 键值列表：
        做 红黑树_插入 用 树、键值列表 的 第 i 项 的 第 0 项、键值列表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

红黑树_统计节点 接受 树：
    返还 一组：做 红黑树_节点数_递归 用 树 的 第 0 项、做 红黑树_红节点数_递归 用 树 的 第 0 项、做 红黑树_高度 用 树、做 红黑树_黑高 用 树。
完。

红黑树_完整诊断 接受 树：
    返还 一组：「元素数」、树 的 第 1 项、「树高」、做 红黑树_高度 用 树、「黑高」、做 红黑树_黑高 用 树、「红节点数」、做 红黑树_红节点数_递归 用 树 的 第 0 项、「删除总数」、_RB删_总数、「惰性待清」、做 计数 用 _惰性删除集、「批量次数」、_RB删_批量次数、「结构有效」、做 红黑树_验证 用 树、「NRDT+红黑树」、1。
完。

可变 AI_红黑树删除_热节点缓存 为 0。

AI_红黑树删除_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_红黑树删除_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_红黑树删除_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

本章 收束。
