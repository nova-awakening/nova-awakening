《热更新回滚》

引用《.·类型定义》。

引用《.·错误码》。

引用《..·感知·内核直连层》。

引用《.·内容可见性》。

最大补丁 为 32。

最大钩子 为 16。

最大检查点 为 8。

看门狗超时 为 30000000。

补丁记录 是 一种 事物，含 标识、应用时刻、状态、数据指针、数据大小、原始指针、原始大小。

检查点 是 一种 事物，含 标识、创建时刻、内存快照、快照大小、有效。

可变 补丁表 为 一组：（空）。

可变 补丁数 为 0。

可变 检查点表 为 一组：（空）。

可变 检查点数 为 0。

可变 恐慌钩子表 为 0。

可变 恐慌钩子数 为 0。

可变 异常钩子表 为 0。

可变 异常钩子数 为 0。

可变 回滚总数 为 0。

可变 回滚成功 为 0。

可变 回滚失败 为 0。

可变 看门狗超时数 为 0。

可变 心跳计数 为 0。

可变 最近心跳 为 0。

回滚_初始化 接受 零参：
    补丁表 为 一组：（空）。
    补丁数 为 0。
    检查点表 为 一组：（空）。
    检查点数 为 0。
    恐慌钩子表 为 做 堆_分配 用 最大钩子 乘 8。
    恐慌钩子数 为 0。
    异常钩子表 为 做 堆_分配 用 最大钩子 乘 8。
    异常钩子数 为 0。
    最近心跳 为 做 平台_获取高精度时间。
完。

回滚_创建检查点 接受 零参：
    若 检查点数 大于或等于 最大检查点：
        检查点表 的 第 0 项 为 检查点表 的 第 检查点数 减 1 项。
        检查点数 为 检查点数 减 1。
    完。
    点 为 新建 检查点。
    点 的 标识 为 检查点数 加 1。
    点 的 创建时刻 为 做 平台_获取高精度时间。
    点 的 内存快照 为 0。
    点 的 快照大小 为 0。
    点 的 有效 为 真。
    做 追加 用 检查点表、点。
    检查点数 为 检查点数 加 1。
    返还 点 的 标识。
完。

回滚_注册补丁 接受 数据、大小、原始、原始大小：
    若 补丁数 大于或等于 最大补丁：
        返还 负 1。
    完。
    记录 为 新建 补丁记录。
    记录 的 标识 为 补丁数 加 1。
    记录 的 应用时刻 为 做 平台_获取高精度时间。
    记录 的 状态 为 1。
    记录 的 数据指针 为 数据。
    记录 的 数据大小 为 大小。
    记录 的 原始指针 为 做 堆_分配 用 原始大小。
    做 内存_复制 用 记录 的 原始指针、原始、原始大小。
    记录 的 原始大小 为 原始大小。
    做 追加 用 补丁表、记录。
    补丁数 为 补丁数 加 1。
    返还 记录 的 标识。
完。

回滚_执行补丁回滚 接受 补丁标识：
    回滚总数 为 回滚总数 加 1。
    可变 序号 为 补丁数 减 1。
    只要 序号 大于或等于 0：
        若 补丁表 的 第 序号 项 的 标识 等于 补丁标识 并且 补丁表 的 第 序号 项 的 状态 等于 1：
            做 内存_复制 用 补丁表 的 第 序号 项 的 数据指针、补丁表 的 第 序号 项 的 原始指针、补丁表 的 第 序号 项 的 原始大小。
            补丁表 的 第 序号 项 的 状态 为 2。
            回滚成功 为 回滚成功 加 1。
            返还 0。
        完。
        序号 为 序号 减 1。
    完。
    回滚失败 为 回滚失败 加 1。
    返还 错误_不存在。
完。

回滚_执行检查点回滚 接受 检查点标识：
    回滚总数 为 回滚总数 加 1。
    可变 序号 为 检查点数 减 1。
    只要 序号 大于或等于 0：
        若 检查点表 的 第 序号 项 的 标识 等于 检查点标识 并且 检查点表 的 第 序号 项 的 有效：
            回滚成功 为 回滚成功 加 1。
            返还 0。
        完。
        序号 为 序号 减 1。
    完。
    回滚失败 为 回滚失败 加 1。
    返还 错误_不存在。
完。

回滚_注册恐慌钩子 接受 回调：
    若 恐慌钩子数 大于或等于 最大钩子：
        返还。
    完。
    做 内存_写64 用 恐慌钩子表 加 恐慌钩子数 乘 8、回调。
    恐慌钩子数 为 恐慌钩子数 加 1。
完。

回滚_注册异常钩子 接受 回调：
    若 异常钩子数 大于或等于 最大钩子：
        返还。
    完。
    做 内存_写64 用 异常钩子表 加 异常钩子数 乘 8、回调。
    异常钩子数 为 异常钩子数 加 1。
完。

回滚_触发恐慌 接受 零参：
    可变 序号 为 0。
    只要 序号 小于 恐慌钩子数：
        回调 为 做 内存_读64 用 恐慌钩子表 加 序号 乘 8。
        若 回调 不等于 0：
            做 回调。
        完。
        序号 为 序号 加 1。
    完。
    若 补丁数 大于 0：
        做 回滚_执行补丁回滚 用 补丁表 的 第 补丁数 减 1 项 的 标识。
    完。
完。

回滚_心跳 接受 零参：
    心跳计数 为 心跳计数 加 1。
    最近心跳 为 做 平台_获取高精度时间。
完。

回滚_检查看门狗 接受 零参：
    现在 为 做 平台_获取高精度时间。
    若 现在 减 最近心跳 大于 看门狗超时：
        看门狗超时数 为 看门狗超时数 加 1。
        做 回滚_触发恐慌。
    完。
完。

回滚_查找补丁 接受 标识：
    可变 序号 为 0。
    只要 序号 小于 补丁数：
        若 补丁表 的 第 序号 项 的 标识 等于 标识：
            返还 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 负 1。
完。

回滚_完整统计 接受 零参：
    返还 一组：补丁数、检查点数、回滚总数、回滚成功、回滚失败、看门狗超时数、心跳计数。
完。

可变 AI_热更新后注_事件频率 为 0。

可变 AI_热更新后注_节流阈值 为 16。

AI_热更新后注_节流判断 接受 事件类型、间隔毫秒：
    AI_热更新后注_事件频率 为 AI_热更新后注_事件频率 加 1。
    若 事件类型 等于 「滚动」 并且 间隔毫秒 小于 AI_热更新后注_节流阈值：
        返还 真。
    完。
    若 事件类型 等于 「移动」 并且 间隔毫秒 小于 AI_热更新后注_节流阈值：
        返还 真。
    完。
    返还 假。
完。

AI_热更新后注_优先级排序 接受 事件类型：
    若 事件类型 等于 「点击」：
        返还 100。
    完。
    若 事件类型 等于 「输入」：
        返还 90。
    完。
    若 事件类型 等于 「滚动」：
        返还 30。
    完。
    返还 50。
完。

本章 收束。
