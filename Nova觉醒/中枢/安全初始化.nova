《安全初始化》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·哈希整数》。

引用《.·内容可见性》。

权限_网络 为 1。

权限_读文件 为 2。

权限_写文件 为 4。

权限_执行 为 8。

权限_图处 为 16。

权限_设备 为 32。

权限_内核 为 64。

最大沙箱 为 32。

最大身份 为 16。

最大令牌 为 256。

审计容量 为 1024。

令牌有效期 为 3600000000。

沙箱 是 一种 事物，含 名称哈希、权限掩码、内存限制、已用内存、进程数、违规数、创建时刻、活跃。

安全身份 是 一种 事物，含 标识、名称哈希、权限级别、创建时刻、最近验证、验证次数。

安全令牌 是 一种 事物，含 令牌哈希、身份标识、过期时刻、使用次数、有效。

审计记录 是 一种 事物，含 时间戳、事件类型、主体标识、客体标识、结果。

可变 沙箱表 为 一组：（空）。

可变 沙箱数 为 0。

可变 身份表 为 一组：（空）。

可变 身份数 为 0。

可变 令牌表 为 一组：（空）。

可变 令牌数 为 0。

可变 审计环 为 0。

可变 审计写位 为 0。

可变 审计总数 为 0。

可变 告警数 为 0。

可变 拦截数 为 0。

可变 威胁分数 为 0。

安全_初始化 接受 零参：
    沙箱表 为 一组：（空）。
    沙箱数 为 0。
    身份表 为 一组：（空）。
    身份数 为 0。
    令牌表 为 一组：（空）。
    令牌数 为 0。
    审计环 为 做 堆_分配 用 审计容量 乘 40。
    审计写位 为 0。
    做 沙箱_创建 用 「浏览器」、权限_网络 位或 权限_读文件、536870912。
    做 沙箱_创建 用 「推理引擎」、权限_执行 位或 权限_图处、1073741824。
    做 沙箱_创建 用 「用户数据」、权限_读文件 位或 权限_写文件、268435456。
    做 身份_创建 用 「系统」、权限_内核 位或 权限_设备 位或 权限_执行。
    做 身份_创建 用 「用户」、权限_网络 位或 权限_读文件 位或 权限_写文件。
完。

沙箱_创建 接受 名称、权限、内存限：
    若 沙箱数 大于或等于 最大沙箱：
        返还 负 1。
    完。
    箱 为 新建 沙箱。
    箱 的 名称哈希 为 做 哈希_字符串 用 名称。
    箱 的 权限掩码 为 权限。
    箱 的 内存限制 为 内存限。
    箱 的 已用内存 为 0。
    箱 的 进程数 为 0。
    箱 的 违规数 为 0。
    箱 的 创建时刻 为 做 平台_获取高精度时间。
    箱 的 活跃 为 真。
    做 追加 用 沙箱表、箱。
    索引 为 沙箱数。
    沙箱数 为 沙箱数 加 1。
    返还 索引。
完。

沙箱_检查权限 接受 沙箱索引、请求权限：
    若 沙箱索引 大于或等于 沙箱数：
        拦截数 为 拦截数 加 1。
        返还 假。
    完。
    箱 为 沙箱表 的 第 沙箱索引 项。
    若 非 箱 的 活跃：
        拦截数 为 拦截数 加 1。
        返还 假。
    完。
    若 箱 的 权限掩码 位与 请求权限 不等于 请求权限：
        箱 的 违规数 为 箱 的 违规数 加 1。
        拦截数 为 拦截数 加 1。
        做 审计_记录 用 1、沙箱索引、请求权限、0。
        若 箱 的 违规数 大于 10：
            威胁分数 为 威胁分数 加 100。
        完。
        返还 假。
    完。
    返还 真。
完。

沙箱_检查内存 接受 沙箱索引、请求字节：
    若 沙箱索引 大于或等于 沙箱数：
        返还 假。
    完。
    箱 为 沙箱表 的 第 沙箱索引 项。
    若 箱 的 已用内存 加 请求字节 大于 箱 的 内存限制：
        箱 的 违规数 为 箱 的 违规数 加 1。
        做 审计_记录 用 2、沙箱索引、请求字节、0。
        返还 假。
    完。
    箱 的 已用内存 为 箱 的 已用内存 加 请求字节。
    返还 真。
完。

身份_创建 接受 名称、权限级别：
    若 身份数 大于或等于 最大身份：
        返还 负 1。
    完。
    身份 为 新建 安全身份。
    身份 的 标识 为 身份数。
    身份 的 名称哈希 为 做 哈希_字符串 用 名称。
    身份 的 权限级别 为 权限级别。
    身份 的 创建时刻 为 做 平台_获取高精度时间。
    身份 的 最近验证 为 0。
    身份 的 验证次数 为 0。
    做 追加 用 身份表、身份。
    索引 为 身份数。
    身份数 为 身份数 加 1。
    返还 索引。
完。

令牌_颁发 接受 身份标识：
    若 身份标识 大于或等于 身份数：
        返还 0。
    完。
    若 令牌数 大于或等于 最大令牌：
        做 令牌_清理过期。
        若 令牌数 大于或等于 最大令牌：
            返还 0。
        完。
    完。
    现在 为 做 平台_获取高精度时间。
    令牌 为 新建 安全令牌。
    令牌 的 令牌哈希 为 现在 位异或 身份标识 乘 2654435761。
    令牌 的 身份标识 为 身份标识。
    令牌 的 过期时刻 为 现在 加 令牌有效期。
    令牌 的 使用次数 为 0。
    令牌 的 有效 为 真。
    做 追加 用 令牌表、令牌。
    令牌数 为 令牌数 加 1。
    做 审计_记录 用 3、身份标识、令牌 的 令牌哈希、1。
    返还 令牌 的 令牌哈希。
完。

令牌_验证 接受 令牌哈希：
    现在 为 做 平台_获取高精度时间。
    可变 序号 为 0。
    只要 序号 小于 令牌数：
        若 令牌表 的 第 序号 项 的 令牌哈希 等于 令牌哈希 并且 令牌表 的 第 序号 项 的 有效：
            若 现在 大于 令牌表 的 第 序号 项 的 过期时刻：
                令牌表 的 第 序号 项 的 有效 为 假。
                做 审计_记录 用 4、令牌表 的 第 序号 项 的 身份标识、令牌哈希、0。
                返还 负 1。
            完。
            令牌表 的 第 序号 项 的 使用次数 为 令牌表 的 第 序号 项 的 使用次数 加 1。
            返还 令牌表 的 第 序号 项 的 身份标识。
        完。
        序号 为 序号 加 1。
    完。
    告警数 为 告警数 加 1。
    威胁分数 为 威胁分数 加 50。
    返还 负 1。
完。

令牌_清理过期 接受 零参：
    现在 为 做 平台_获取高精度时间。
    可变 序号 为 0。
    只要 序号 小于 令牌数：
        若 令牌表 的 第 序号 项 的 有效 并且 现在 大于 令牌表 的 第 序号 项 的 过期时刻：
            令牌表 的 第 序号 项 的 有效 为 假。
        完。
        序号 为 序号 加 1。
    完。
完。

审计_记录 接受 事件、主体、客体、结果：
    偏移 为 审计环 加 审计写位 乘 40。
    做 内存_写64 用 偏移、做 平台_获取高精度时间。
    做 内存_写64 用 偏移 加 8、事件。
    做 内存_写64 用 偏移 加 16、主体。
    做 内存_写64 用 偏移 加 24、客体。
    做 内存_写64 用 偏移 加 32、结果。
    审计写位 为 (审计写位 加 1) 取余于 审计容量。
    审计总数 为 审计总数 加 1。
完。

安全_获取威胁分 接受 零参：
    返还 威胁分数。
完。

安全_重置威胁 接受 零参：
    威胁分数 为 0。
完。

绝对安全_完整统计 接受 零参：
    返还 一组：沙箱数、身份数、令牌数、告警数、拦截数、审计总数、威胁分数。
完。

可变 AI_安全初始化_运行指标 为 0。

可变 AI_安全初始化_异常指标 为 0。

AI_安全初始化_自诊断 接受 零参：
    AI_安全初始化_运行指标 为 审计总数。
    返还 AI_安全初始化_运行指标。
完。

AI_安全初始化_优化建议 接受 零参：
    指标 为 做 AI_安全初始化_自诊断。
    若 AI_安全初始化_运行指标 大于 10000：
        返还 「安全初始化负载较高」。
    完。
    返还 「安全初始化状态正常」。
完。

本章 收束。
