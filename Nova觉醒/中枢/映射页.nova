《映射页》

引用《.·类型定义》。

引用《.·同步原语》。

引用《..·执行·内核智能》。

引用《.·页表索引》。

引用《.·获取当前页》。

引用《.·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·执行·底层内存操》。

引用《..·感知·内核直连层》。

引用《..·引导·特权指令》。

引用《.·字符串替换》。

引用《.·内存位图》。

引用《.·块缓分配器生》。

页大小_4KB 为 0x1000。

页大小_2MB 为 0x200000。

页大小_1GB 为 0x40000000。

页偏移位_4KB 为 12。

页偏移位_2MB 为 21。

页偏移位_1GB 为 30。

页表项数 为 512。

PTE_存在 为 1 左移 0。

PTE_可写 为 1 左移 1。

PTE_用户 为 1 左移 2。

PTE_写透 为 1 左移 3。

PTE_禁缓存 为 1 左移 4。

PTE_已访问 为 1 左移 5。

PTE_脏页 为 1 左移 6。

PTE_大页 为 1 左移 7。

PTE_全局 为 1 左移 8。

PTE_禁执行 为 1 左移 63。

PTE_COW 为 1 左移 9。

PTE_延迟 为 1 左移 10。

PTE_地址掩码 为 0x000FFFFFFFFFF000。

映射类型_4KB 为 0。

映射类型_2MB 为 1。

映射类型_1GB 为 2。

VMPT_特征维度 为 12。

VMPT_历史窗口 为 64。

VMPT_置信阈值 为 0.72。

映射统计 是 一种 事物，含 总映射次数、总取消映射次数、总查询次数、总4KB映射、总2MB映射、总1GB映射、总COW标记、总COW触发、总延迟分配、总延迟触发、总TLB刷新、总页表页分配、总页表页释放、总权限修改。

页表遍历结果 是 一种 事物，含 物理地址、页大小、映射类型、标志、存在、是COW、是延迟。

可变 全局映射统计 为 新建 映射统计。

可变 映射锁 为 做 自旋锁_创建。

可变 VMPT_访问历史 为 做 数组_创建 用 VMPT_历史窗口、0。

可变 VMPT_大小历史 为 做 数组_创建 用 VMPT_历史窗口、0。

可变 VMPT_历史写指针 为 0。

PML4索引 接受 虚拟地址：
    返还 虚拟地址 右移 39 位与 0x1FF。
完。

PDPT索引 接受 虚拟地址：
    返还 虚拟地址 右移 30 位与 0x1FF。
完。

PD索引 接受 虚拟地址：
    返还 虚拟地址 右移 21 位与 0x1FF。
完。

PT索引 接受 虚拟地址：
    返还 虚拟地址 右移 12 位与 0x1FF。
完。

页表项_存在 接受 条目：
    返还 条目 位与 PTE_存在 不等于 0。
完。

页表项_是大页 接受 条目：
    返还 条目 位与 PTE_大页 不等于 0。
完。

页表项_可写 接受 条目：
    返还 条目 位与 PTE_可写 不等于 0。
完。

页表项_是COW 接受 条目：
    返还 条目 位与 PTE_COW 不等于 0。
完。

页表项_是延迟 接受 条目：
    返还 条目 位与 PTE_延迟 不等于 0。
完。

页表项_地址 接受 条目：
    返还 条目 位与 PTE_地址掩码。
完。

页表项_标志 接受 条目：
    返还 条目 位与 0xFFF0000000000FFF。
完。

页表项_创建 接受 地址、标志：
    返还 地址 位与 PTE_地址掩码 位或 标志。
完。

页表_遍历到PT 接受 PML4物理、虚拟地址、创建：
    pml4_idx 为 做 PML4索引 用 虚拟地址。
    pml4_条目 为 做 页表_读条目 用 PML4物理、pml4_idx。
    可变 pdpt_物理 为 0。
    若 做 页表项_存在 用 pml4_条目：
        pdpt_物理 为 做 页表项_地址 用 pml4_条目。
    否则：
        若 非 创建：
            返还 0。
        完。
        pdpt_物理 为 做 页表_分配页。
        若 pdpt_物理 等于 0：
            返还 0。
        完。
        全局映射统计 的 总页表页分配 为 全局映射统计 的 总页表页分配 加 1。
        做 页表_写条目 用 PML4物理、pml4_idx、做 页表项_创建 用 pdpt_物理、PTE_存在 位或 PTE_可写 位或 PTE_用户。
    完。
    pdpt_idx 为 做 PDPT索引 用 虚拟地址。
    pdpt_条目 为 做 页表_读条目 用 pdpt_物理、pdpt_idx。
    可变 pd_物理 为 0。
    若 做 页表项_存在 用 pdpt_条目：
        若 做 页表项_是大页 用 pdpt_条目：
            返还 0。
        完。
        pd_物理 为 做 页表项_地址 用 pdpt_条目。
    否则：
        若 非 创建：
            返还 0。
        完。
        pd_物理 为 做 页表_分配页。
        若 pd_物理 等于 0：
            返还 0。
        完。
        全局映射统计 的 总页表页分配 为 全局映射统计 的 总页表页分配 加 1。
        做 页表_写条目 用 pdpt_物理、pdpt_idx、做 页表项_创建 用 pd_物理、PTE_存在 位或 PTE_可写 位或 PTE_用户。
    完。
    pd_idx 为 做 PD索引 用 虚拟地址。
    pd_条目 为 做 页表_读条目 用 pd_物理、pd_idx。
    可变 pt_物理 为 0。
    若 做 页表项_存在 用 pd_条目：
        若 做 页表项_是大页 用 pd_条目：
            返还 0。
        完。
        pt_物理 为 做 页表项_地址 用 pd_条目。
    否则：
        若 非 创建：
            返还 0。
        完。
        pt_物理 为 做 页表_分配页。
        若 pt_物理 等于 0：
            返还 0。
        完。
        全局映射统计 的 总页表页分配 为 全局映射统计 的 总页表页分配 加 1。
        做 页表_写条目 用 pd_物理、pd_idx、做 页表项_创建 用 pt_物理、PTE_存在 位或 PTE_可写 位或 PTE_用户。
    完。
    返还 pt_物理。
完。

映射页 接受 PML4物理、虚拟地址、物理地址、标志：
    做 自旋锁_加锁 用 映射锁。
    pt_物理 为 做 页表_遍历到PT 用 PML4物理、虚拟地址、真。
    若 pt_物理 等于 0：
        做 自旋锁_解锁 用 映射锁。
        做 内核错误 用 「映射页失败: 无法创建页表, VA=0x」 加 做 整数转十六进制 用 虚拟地址。
        返还 错误_无内存。
    完。
    pt_idx 为 做 PT索引 用 虚拟地址。
    旧条目 为 做 页表_读条目 用 pt_物理、pt_idx。
    若 做 页表项_存在 用 旧条目：
        全局映射统计 的 总权限修改 为 全局映射统计 的 总权限修改 加 1。
    完。
    做 页表_写条目 用 pt_物理、pt_idx、做 页表项_创建 用 物理地址、标志 位或 PTE_存在。
    做 刷新TLB 用 虚拟地址。
    全局映射统计 的 总映射次数 为 全局映射统计 的 总映射次数 加 1。
    全局映射统计 的 总4KB映射 为 全局映射统计 的 总4KB映射 加 1。
    全局映射统计 的 总TLB刷新 为 全局映射统计 的 总TLB刷新 加 1。
    做 自旋锁_解锁 用 映射锁。
    返还 成功。
完。

取消映射页 接受 PML4物理、虚拟地址：
    做 自旋锁_加锁 用 映射锁。
    pt_物理 为 做 页表_遍历到PT 用 PML4物理、虚拟地址、假。
    若 pt_物理 等于 0：
        做 自旋锁_解锁 用 映射锁。
        返还。
    完。
    pt_idx 为 做 PT索引 用 虚拟地址。
    做 页表_写条目 用 pt_物理、pt_idx、0。
    做 刷新TLB 用 虚拟地址。
    全局映射统计 的 总取消映射次数 为 全局映射统计 的 总取消映射次数 加 1。
    全局映射统计 的 总TLB刷新 为 全局映射统计 的 总TLB刷新 加 1。
    做 自旋锁_解锁 用 映射锁。
完。

查询映射 接受 PML4物理、虚拟地址：
    全局映射统计 的 总查询次数 为 全局映射统计 的 总查询次数 加 1。
    pt_物理 为 做 页表_遍历到PT 用 PML4物理、虚拟地址、假。
    若 pt_物理 等于 0：
        返还 0。
    完。
    pt_idx 为 做 PT索引 用 虚拟地址。
    条目 为 做 页表_读条目 用 pt_物理、pt_idx。
    若 非 做 页表项_存在 用 条目：
        返还 0。
    完。
    返还 做 页表项_地址 用 条目 位或 虚拟地址 位与 0xFFF。
完。

查询映射_详细 接受 PML4物理、虚拟地址：
    结果 为 新建 页表遍历结果。
    结果 的 存在 为 假。
    结果 的 物理地址 为 0。
    结果 的 页大小 为 页大小_4KB。
    结果 的 映射类型 为 映射类型_4KB。
    结果 的 标志 为 0。
    结果 的 是COW 为 假。
    结果 的 是延迟 为 假。
    pml4_条目 为 做 页表_读条目 用 PML4物理、做 PML4索引 用 虚拟地址。
    若 非 做 页表项_存在 用 pml4_条目：
        返还 结果。
    完。
    pdpt_物理 为 做 页表项_地址 用 pml4_条目。
    pdpt_条目 为 做 页表_读条目 用 pdpt_物理、做 PDPT索引 用 虚拟地址。
    若 非 做 页表项_存在 用 pdpt_条目：
        返还 结果。
    完。
    若 做 页表项_是大页 用 pdpt_条目：
        结果 的 存在 为 真。
        结果 的 物理地址 为 做 页表项_地址 用 pdpt_条目 位或 虚拟地址 位与 0x3FFFFFFF。
        结果 的 页大小 为 页大小_1GB。
        结果 的 映射类型 为 映射类型_1GB。
        结果 的 标志 为 做 页表项_标志 用 pdpt_条目。
        返还 结果。
    完。
    pd_物理 为 做 页表项_地址 用 pdpt_条目。
    pd_条目 为 做 页表_读条目 用 pd_物理、做 PD索引 用 虚拟地址。
    若 非 做 页表项_存在 用 pd_条目：
        返还 结果。
    完。
    若 做 页表项_是大页 用 pd_条目：
        结果 的 存在 为 真。
        结果 的 物理地址 为 做 页表项_地址 用 pd_条目 位或 虚拟地址 位与 0x1FFFFF。
        结果 的 页大小 为 页大小_2MB。
        结果 的 映射类型 为 映射类型_2MB。
        结果 的 标志 为 做 页表项_标志 用 pd_条目。
        返还 结果。
    完。
    pt_物理 为 做 页表项_地址 用 pd_条目。
    pt_条目 为 做 页表_读条目 用 pt_物理、做 PT索引 用 虚拟地址。
    若 非 做 页表项_存在 用 pt_条目：
        返还 结果。
    完。
    结果 的 存在 为 真。
    结果 的 物理地址 为 做 页表项_地址 用 pt_条目 位或 虚拟地址 位与 0xFFF。
    结果 的 标志 为 做 页表项_标志 用 pt_条目。
    结果 的 是COW 为 做 页表项_是COW 用 pt_条目。
    结果 的 是延迟 为 做 页表项_是延迟 用 pt_条目。
    返还 结果。
完。

映射范围 接受 PML4物理、虚拟起始、物理起始、大小、标志：
    页数 为 做 对齐到 用 大小、页大小_4KB 除以 页大小_4KB。
    可变 i 为 0。
    只要 i 小于 页数：
        结果 为 做 映射页 用 PML4物理、虚拟起始 加 i 乘 页大小_4KB、物理起始 加 i 乘 页大小_4KB、标志。
        若 结果 小于 0：
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 成功。
完。

取消映射范围 接受 PML4物理、虚拟起始、大小：
    页数 为 做 对齐到 用 大小、页大小_4KB 除以 页大小_4KB。
    可变 i 为 0。
    只要 i 小于 页数：
        做 取消映射页 用 PML4物理、虚拟起始 加 i 乘 页大小_4KB。
        i 为 i 加 1。
    完。
完。

映射大页_2MB 接受 PML4物理、虚拟地址、物理地址、标志：
    若 虚拟地址 位与 页大小_2MB 减 1 不等于 0：
        返还 错误_地址未对齐。
    完。
    若 物理地址 位与 页大小_2MB 减 1 不等于 0：
        返还 错误_地址未对齐。
    完。
    做 自旋锁_加锁 用 映射锁。
    pml4_条目 为 做 页表_读条目 用 PML4物理、做 PML4索引 用 虚拟地址。
    可变 pdpt_物理 为 0。
    若 做 页表项_存在 用 pml4_条目：
        pdpt_物理 为 做 页表项_地址 用 pml4_条目。
    否则：
        pdpt_物理 为 做 页表_分配页。
        若 pdpt_物理 等于 0：
            做 自旋锁_解锁 用 映射锁。
            返还 错误_无内存。
        完。
        全局映射统计 的 总页表页分配 为 全局映射统计 的 总页表页分配 加 1。
        做 页表_写条目 用 PML4物理、做 PML4索引 用 虚拟地址、做 页表项_创建 用 pdpt_物理、PTE_存在 位或 PTE_可写 位或 PTE_用户。
    完。
    pdpt_条目 为 做 页表_读条目 用 pdpt_物理、做 PDPT索引 用 虚拟地址。
    可变 pd_物理 为 0。
    若 做 页表项_存在 用 pdpt_条目：
        若 做 页表项_是大页 用 pdpt_条目：
            做 自旋锁_解锁 用 映射锁。
            返还 错误_地址冲突。
        完。
        pd_物理 为 做 页表项_地址 用 pdpt_条目。
    否则：
        pd_物理 为 做 页表_分配页。
        若 pd_物理 等于 0：
            做 自旋锁_解锁 用 映射锁。
            返还 错误_无内存。
        完。
        全局映射统计 的 总页表页分配 为 全局映射统计 的 总页表页分配 加 1。
        做 页表_写条目 用 pdpt_物理、做 PDPT索引 用 虚拟地址、做 页表项_创建 用 pd_物理、PTE_存在 位或 PTE_可写 位或 PTE_用户。
    完。
    做 页表_写条目 用 pd_物理、做 PD索引 用 虚拟地址、做 页表项_创建 用 物理地址、标志 位或 PTE_存在 位或 PTE_大页。
    做 刷新TLB 用 虚拟地址。
    全局映射统计 的 总映射次数 为 全局映射统计 的 总映射次数 加 1。
    全局映射统计 的 总2MB映射 为 全局映射统计 的 总2MB映射 加 1。
    全局映射统计 的 总TLB刷新 为 全局映射统计 的 总TLB刷新 加 1。
    做 自旋锁_解锁 用 映射锁。
    返还 成功。
完。

映射大页_1GB 接受 PML4物理、虚拟地址、物理地址、标志：
    若 虚拟地址 位与 页大小_1GB 减 1 不等于 0：
        返还 错误_地址未对齐。
    完。
    若 物理地址 位与 页大小_1GB 减 1 不等于 0：
        返还 错误_地址未对齐。
    完。
    做 自旋锁_加锁 用 映射锁。
    pml4_条目 为 做 页表_读条目 用 PML4物理、做 PML4索引 用 虚拟地址。
    可变 pdpt_物理 为 0。
    若 做 页表项_存在 用 pml4_条目：
        pdpt_物理 为 做 页表项_地址 用 pml4_条目。
    否则：
        pdpt_物理 为 做 页表_分配页。
        若 pdpt_物理 等于 0：
            做 自旋锁_解锁 用 映射锁。
            返还 错误_无内存。
        完。
        全局映射统计 的 总页表页分配 为 全局映射统计 的 总页表页分配 加 1。
        做 页表_写条目 用 PML4物理、做 PML4索引 用 虚拟地址、做 页表项_创建 用 pdpt_物理、PTE_存在 位或 PTE_可写 位或 PTE_用户。
    完。
    做 页表_写条目 用 pdpt_物理、做 PDPT索引 用 虚拟地址、做 页表项_创建 用 物理地址、标志 位或 PTE_存在 位或 PTE_大页。
    做 刷新TLB 用 虚拟地址。
    全局映射统计 的 总映射次数 为 全局映射统计 的 总映射次数 加 1。
    全局映射统计 的 总1GB映射 为 全局映射统计 的 总1GB映射 加 1。
    全局映射统计 的 总TLB刷新 为 全局映射统计 的 总TLB刷新 加 1。
    做 自旋锁_解锁 用 映射锁。
    返还 成功。
完。

标记COW_范围 接受 PML4物理、虚拟起始、大小：
    页数 为 做 对齐到 用 大小、页大小_4KB 除以 页大小_4KB。
    可变 标记数 为 0。
    可变 i 为 0。
    只要 i 小于 页数：
        va 为 虚拟起始 加 i 乘 页大小_4KB。
        pt_物理 为 做 页表_遍历到PT 用 PML4物理、va、假。
        若 pt_物理 不等于 0：
            pt_idx 为 做 PT索引 用 va。
            条目 为 做 页表_读条目 用 pt_物理、pt_idx。
            若 做 页表项_存在 用 条目 并且 做 页表项_可写 用 条目：
                新条目 为 条目 位与 按位取反 PTE_可写 位或 PTE_COW。
                做 页表_写条目 用 pt_物理、pt_idx、新条目。
                做 刷新TLB 用 va。
                标记数 为 标记数 加 1。
                全局映射统计 的 总COW标记 为 全局映射统计 的 总COW标记 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 标记数。
完。

处理COW缺页 接受 PML4物理、虚拟地址：
    pt_物理 为 做 页表_遍历到PT 用 PML4物理、虚拟地址、假。
    若 pt_物理 等于 0：
        返还 错误_无映射。
    完。
    pt_idx 为 做 PT索引 用 虚拟地址。
    条目 为 做 页表_读条目 用 pt_物理、pt_idx。
    若 非 做 页表项_存在 用 条目 或者 非 做 页表项_是COW 用 条目：
        返还 错误_非_COW页。
    完。
    旧物理 为 做 页表项_地址 用 条目。
    旧标志 为 做 页表项_标志 用 条目。
    新物理 为 做 页帧_分配。
    若 新物理 等于 0：
        返还 错误_无内存。
    完。
    做 内存_复制 用 新物理、旧物理、页大小_4KB。
    新标志 为 旧标志 位与 按位取反 PTE_COW 位或 PTE_可写。
    做 页表_写条目 用 pt_物理、pt_idx、做 页表项_创建 用 新物理、新标志。
    做 刷新TLB 用 虚拟地址。
    做 页帧_减少引用 用 旧物理。
    全局映射统计 的 总COW触发 为 全局映射统计 的 总COW触发 加 1。
    返还 成功。
完。

映射延迟页 接受 PML4物理、虚拟地址、标志：
    做 自旋锁_加锁 用 映射锁。
    pt_物理 为 做 页表_遍历到PT 用 PML4物理、虚拟地址、真。
    若 pt_物理 等于 0：
        做 自旋锁_解锁 用 映射锁。
        返还 错误_无内存。
    完。
    pt_idx 为 做 PT索引 用 虚拟地址。
    做 页表_写条目 用 pt_物理、pt_idx、PTE_延迟 位或 标志 位与 按位取反 PTE_存在。
    全局映射统计 的 总延迟分配 为 全局映射统计 的 总延迟分配 加 1。
    做 自旋锁_解锁 用 映射锁。
    返还 成功。
完。

处理延迟缺页 接受 PML4物理、虚拟地址：
    pt_物理 为 做 页表_遍历到PT 用 PML4物理、虚拟地址、假。
    若 pt_物理 等于 0：
        返还 错误_无映射。
    完。
    pt_idx 为 做 PT索引 用 虚拟地址。
    条目 为 做 页表_读条目 用 pt_物理、pt_idx。
    若 非 做 页表项_是延迟 用 条目：
        返还 错误_非延迟页。
    完。
    物理 为 做 页帧_分配。
    若 物理 等于 0：
        返还 错误_无内存。
    完。
    做 内存_清零 用 物理、页大小_4KB。
    标志 为 条目 位与 0xFFF 位或 PTE_存在。
    标志 为 标志 位与 按位取反 PTE_延迟。
    做 页表_写条目 用 pt_物理、pt_idx、做 页表项_创建 用 物理、标志。
    做 刷新TLB 用 虚拟地址。
    全局映射统计 的 总延迟触发 为 全局映射统计 的 总延迟触发 加 1。
    返还 成功。
完。

映射延迟范围 接受 PML4物理、虚拟起始、大小、标志：
    页数 为 做 对齐到 用 大小、页大小_4KB 除以 页大小_4KB。
    可变 i 为 0。
    只要 i 小于 页数：
        结果 为 做 映射延迟页 用 PML4物理、虚拟起始 加 i 乘 页大小_4KB、标志。
        若 结果 小于 0：
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 成功。
完。

修改页权限 接受 PML4物理、虚拟地址、新标志：
    pt_物理 为 做 页表_遍历到PT 用 PML4物理、虚拟地址、假。
    若 pt_物理 等于 0：
        返还 错误_无映射。
    完。
    pt_idx 为 做 PT索引 用 虚拟地址。
    条目 为 做 页表_读条目 用 pt_物理、pt_idx。
    若 非 做 页表项_存在 用 条目：
        返还 错误_无映射。
    完。
    地址 为 做 页表项_地址 用 条目。
    做 页表_写条目 用 pt_物理、pt_idx、做 页表项_创建 用 地址、新标志 位或 PTE_存在。
    做 刷新TLB 用 虚拟地址。
    全局映射统计 的 总权限修改 为 全局映射统计 的 总权限修改 加 1。
    全局映射统计 的 总TLB刷新 为 全局映射统计 的 总TLB刷新 加 1。
    返还 成功。
完。

修改范围权限 接受 PML4物理、虚拟起始、大小、新标志：
    页数 为 做 对齐到 用 大小、页大小_4KB 除以 页大小_4KB。
    可变 i 为 0。
    只要 i 小于 页数：
        做 修改页权限 用 PML4物理、虚拟起始 加 i 乘 页大小_4KB、新标志。
        i 为 i 加 1。
    完。
完。

TLB_批量刷新 接受 虚拟起始、大小：
    页数 为 做 对齐到 用 大小、页大小_4KB 除以 页大小_4KB。
    若 页数 大于 512：
        做 TLB_全局刷新。
        全局映射统计 的 总TLB刷新 为 全局映射统计 的 总TLB刷新 加 1。
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 页数：
        做 刷新TLB 用 虚拟起始 加 i 乘 页大小_4KB。
        i 为 i 加 1。
    完。
    全局映射统计 的 总TLB刷新 为 全局映射统计 的 总TLB刷新 加 页数。
完。

VMPT_记录映射 接受 虚拟地址、大小：
    idx 为 VMPT_历史写指针 取余于 VMPT_历史窗口。
    VMPT_访问历史 的 第 idx 项 为 虚拟地址。
    VMPT_大小历史 的 第 idx 项 为 大小。
    VMPT_历史写指针 为 VMPT_历史写指针 加 1。
完。

VMPT_构建特征 接受 虚拟地址、大小：
    可变 特征 为 做 数组_创建 用 VMPT_特征维度、0.0。
    特征 的 第 0 项 为 虚拟地址。
    特征 的 第 1 项 为 大小。
    特征 的 第 2 项 为 全局映射统计 的 总映射次数。
    特征 的 第 3 项 为 全局映射统计 的 总4KB映射。
    特征 的 第 4 项 为 全局映射统计 的 总2MB映射。
    特征 的 第 5 项 为 全局映射统计 的 总1GB映射。
    特征 的 第 6 项 为 全局映射统计 的 总COW触发。
    特征 的 第 7 项 为 全局映射统计 的 总延迟触发。
    特征 的 第 8 项 为 全局映射统计 的 总TLB刷新。
    特征 的 第 9 项 为 全局映射统计 的 总页表页分配。
    可变 样本 为 若 VMPT_历史写指针 小于 VMPT_历史窗口 则 VMPT_历史写指针 否则 VMPT_历史窗口。
    若 样本 大于 0：
        可变 大小和 为 0.0。
        可变 i 为 0。
        只要 i 小于 样本：
            大小和 为 大小和 加 VMPT_大小历史 的 第 i 项。
            i 为 i 加 1。
        完。
        特征 的 第 10 项 为 大小和 除以 样本。
    完。
    特征 的 第 11 项 为 样本。
    返还 特征。
完。

VMPT_建议映射策略 接受 虚拟地址、大小：
    bus 为 全局AI总线。
    若 bus 等于 0：
        若 大小 大于或等于 页大小_2MB 并且 虚拟地址 位与 页大小_2MB 减 1 等于 0：
            返还 一本字典：类型 对应 映射类型_2MB、延迟 对应 假。
        完。
        返还 一本字典：类型 对应 映射类型_4KB、延迟 对应 大小 大于 页大小_4KB 乘 64。
    完。
    特征 为 做 VMPT_构建特征 用 虚拟地址、大小。
    张量 为 做 Tensor_创建 用 1、VMPT_特征维度。
    可变 i 为 0。
    只要 i 小于 VMPT_特征维度：
        做 Tensor_写 用 张量、0、i、特征 的 第 i 项。
        i 为 i 加 1。
    完。
    预测 为 做 AI总线_推理 用 「VMPT_映射策略」、张量。
    做 Tensor_释放 用 张量。
    若 预测 的 置信度 小于 VMPT_置信阈值：
        返还 一本字典：类型 对应 映射类型_4KB、延迟 对应 假。
    完。
    类型 为 预测 的 输出向量 的 第 0 项。
    延迟 为 预测 的 输出向量 的 第 1 项 大于或等于 0.5。
    若 类型 小于 0.5：
        返还 一本字典：类型 对应 映射类型_4KB、延迟 对应 延迟。
    完。
    若 类型 小于 1.5：
        返还 一本字典：类型 对应 映射类型_2MB、延迟 对应 延迟。
    完。
    返还 一本字典：类型 对应 映射类型_1GB、延迟 对应 延迟。
完。

映射_获取统计 接受 零参：
    返还 一本字典：总映射 对应 全局映射统计 的 总映射次数、总取消 对应 全局映射统计 的 总取消映射次数、总查询 对应 全局映射统计 的 总查询次数、映射_4KB 对应 全局映射统计 的 总4KB映射、映射_2MB 对应 全局映射统计 的 总2MB映射、映射_1GB 对应 全局映射统计 的 总1GB映射、COW标记 对应 全局映射统计 的 总COW标记、COW触发 对应 全局映射统计 的 总COW触发、延迟分配 对应 全局映射统计 的 总延迟分配、延迟触发 对应 全局映射统计 的 总延迟触发、TLB刷新 对应 全局映射统计 的 总TLB刷新、页表页分配 对应 全局映射统计 的 总页表页分配、页表页释放 对应 全局映射统计 的 总页表页释放、权限修改 对应 全局映射统计 的 总权限修改。
完。

映射_打印统计 接受 零参：
    做 内核打印 用 「═══ 映射页统计 ═══」。
    做 内核打印 用 「映射: 总={} 取消={} 查询={}」、全局映射统计 的 总映射次数、全局映射统计 的 总取消映射次数、全局映射统计 的 总查询次数。
    做 内核打印 用 「页大小: 4KB={} 2MB={} 1GB={}」、全局映射统计 的 总4KB映射、全局映射统计 的 总2MB映射、全局映射统计 的 总1GB映射。
    做 内核打印 用 「COW: 标记={} 触发={} 延迟: 分配={} 触发={}」、全局映射统计 的 总COW标记、全局映射统计 的 总COW触发、全局映射统计 的 总延迟分配、全局映射统计 的 总延迟触发。
    做 内核打印 用 「TLB刷新={} 页表页分配={} 释放={} 权限修改={}」、全局映射统计 的 总TLB刷新、全局映射统计 的 总页表页分配、全局映射统计 的 总页表页释放、全局映射统计 的 总权限修改。
完。

映射_重置统计 接受 零参：
    全局映射统计 为 新建 映射统计。
完。

本章 收束。
