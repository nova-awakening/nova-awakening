《用户空间切》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·字符串替换》。

引用《..·引导·端口IO》。

引用《.·每处理器》。

引用《.·内核映射》。

内核代码段 为 0x08。

内核数据段 为 0x10。

用户代码段 为 0x1B。

用户数据段 为 0x23。

EFLAGS_IF 为 1 左移 9。

EFLAGS_IOPL0 为 0。

EFLAGS_IOPL3 为 3 左移 12。

可变 用户态进入次数 为 0。

可变 用户态返回次数 为 0。

进入用户态 接受 rip、rsp、rflags：
    用户态进入次数 为 用户态进入次数 加 1。
    做 交换GS。
    汇编。
    块：
        「push 0x23」。
        「push [rbp+32]」。
        「push [rbp+24]」。
        「push 0x1B」。
        「push [rbp+16]」。
        「iretq」。
    完。
完。

返回用户态前检查 接受 零参：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还。
    完。
    若 做 进程_有挂起信号 用 当前：
        做 处理挂起信号 用 当前。
    完。
    若 做 进程_需要调度标志 用 当前：
        做 调度。
    完。
    用户态返回次数 为 用户态返回次数 加 1。
完。

启动用户进程 接受 入口地址、用户栈顶：
    做 返回用户态前检查。
    rflags 为 EFLAGS_IF。
    做 进入用户态 用 入口地址、用户栈顶、rflags。
完。

启动用户进程_带参数 接受 入口地址、用户栈顶、argc、argv、envp：
    做 返回用户态前检查。
    rflags 为 EFLAGS_IF。
    做 进入用户态 用 入口地址、用户栈顶、rflags。
完。

fork返回 接受 零参：
    做 返回用户态前检查。
完。

交换GS 接受 零参：
    汇编。
    块：
        「swapgs」。
    完。
完。

允许用户内存访问 接受 零参：
    汇编。
    块：
        「stac」。
    完。
完。

禁止用户内存访问 接受 零参：
    汇编。
    块：
        「clac」。
    完。
完。

copy_from_user 接受 内核目标、用户源、长度：
    若 非 做 是用户地址 用 用户源：
        返还 错误_无效地址。
    完。
    若 非 做 是用户地址 用 用户源 加 长度 减 1：
        返还 错误_无效地址。
    完。
    做 允许用户内存访问。
    做 内存复制 用 内核目标、用户源、长度。
    做 禁止用户内存访问。
    返还 成功。
完。

copy_to_user 接受 用户目标、内核源、长度：
    若 非 做 是用户地址 用 用户目标：
        返还 错误_无效地址。
    完。
    若 非 做 是用户地址 用 用户目标 加 长度 减 1：
        返还 错误_无效地址。
    完。
    做 允许用户内存访问。
    做 内存复制 用 用户目标、内核源、长度。
    做 禁止用户内存访问。
    返还 成功。
完。

用户空间切换_统计 接受 零参：
    返还 一组：用户态进入次数、用户态返回次数。
完。

用户空间_上限 为 0x00007FFFFFFFFFFF。

进程切换_是用户地址 接受 地址：
    返还 地址 小于或等于 用户空间_上限。
完。

access_ok 接受 地址、大小：
    若 大小 等于 0：
        返还 真。
    完。
    若 地址 大于 用户空间_上限：
        返还 假。
    完。
    若 地址 加 大小 减 1 大于 用户空间_上限：
        返还 假。
    完。
    若 地址 加 大小 小于 地址：
        返还 假。
    完。
    返还 真。
完。

get_user_u64 接受 用户地址：
    若 非 做 是用户地址 用 用户地址：
        返还 一组：错误_无效地址、0。
    完。
    做 允许用户内存访问。
    值 为 做 内存读64 用 用户地址。
    做 禁止用户内存访问。
    返还 一组：成功、值。
完。

put_user_u64 接受 值、用户地址：
    若 非 做 是用户地址 用 用户地址：
        返还 错误_无效地址。
    完。
    做 允许用户内存访问。
    做 内存写64 用 用户地址、值。
    做 禁止用户内存访问。
    返还 成功。
完。

get_user_u32 接受 用户地址：
    若 非 做 是用户地址 用 用户地址：
        返还 一组：错误_无效地址、0。
    完。
    做 允许用户内存访问。
    值 为 做 内存读32 用 用户地址。
    做 禁止用户内存访问。
    返还 一组：成功、值。
完。

put_user_u32 接受 值、用户地址：
    若 非 做 是用户地址 用 用户地址：
        返还 错误_无效地址。
    完。
    做 允许用户内存访问。
    做 内存写32 用 用户地址、值。
    做 禁止用户内存访问。
    返还 成功。
完。

段选择子名 接受 选择子：
    若 选择子 等于 内核代码段：
        返还 「KERNEL_CS」。
    完。
    若 选择子 等于 内核数据段：
        返还 「KERNEL_DS」。
    完。
    若 选择子 等于 用户代码段：
        返还 「USER_CS」。
    完。
    若 选择子 等于 用户数据段：
        返还 「USER_DS」。
    完。
    返还 「UNKNOWN」。
完。

用户空间切换_完整统计 接受 零参：
    返还 一组：用户态进入次数、用户态返回次数。
完。

可变 AI_用户空间切_运行指标 为 0。

可变 AI_用户空间切_异常指标 为 0。

AI_用户空间切_自诊断 接受 零参：
    AI_用户空间切_运行指标 为 用户态进入次数。
    返还 AI_用户空间切_运行指标。
完。

AI_用户空间切_优化建议 接受 零参：
    指标 为 做 AI_用户空间切_自诊断。
    若 AI_用户空间切_运行指标 大于 10000：
        返还 「用户空间切负载较高」。
    完。
    返还 「用户空间切状态正常」。
完。

本章 收束。
