《AI代理》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·链表后继》。

任务_最大数 为 64。

步骤_最大数 为 32。

重试_最大次数 为 3。

任务状态_等待 为 0。

任务状态_执行中 为 1。

任务状态_完成 为 2。

任务状态_失败 为 3。

代理任务 是 一种 事物，含 任务ID、类型哈希、优先级、状态、步骤列表、当前步骤、重试次数、创建时间、完成时间。

可变 任务队列 为 一组：（空）。

可变 已完成队列 为 一组：（空）。

可变 代理_任务计数 为 0。

可变 代理_成功数 为 0。

可变 代理_失败数 为 0。

可变 代理_已初始化 为 假。

可变 代理_时钟 为 0。

工具_最大数 为 32。

工具描述 是 一种 事物，含 名称哈希、类型、调用次数、成功率EMA。

可变 工具表 为 一组：（空）。

代理_初始化 接受 零参：
    任务队列 为 一组：（空）。
    已完成队列 为 一组：（空）。
    工具表 为 一组：（空）。
    代理_任务计数 为 0。
    代理_成功数 为 0。
    代理_失败数 为 0。
    代理_已初始化 为 真。
    代理_时钟 为 0。
    做 内核信息 用 「AI代理: 任务队列+行动规划+工具选择+执行监控+反馈学习」。
完。

代理_提交任务 接受 类型哈希、优先级、步骤列表：
    若 做 计数 用 任务队列 大于或等于 任务_最大数：
        返还 负 1。
    完。
    代理_时钟 为 代理_时钟 加 1。
    代理_任务计数 为 代理_任务计数 加 1。
    t 为 新建 代理任务。
    t 的 任务ID 为 代理_任务计数。
    t 的 类型哈希 为 类型哈希。
    t 的 优先级 为 优先级。
    t 的 状态 为 任务状态_等待。
    t 的 步骤列表 为 步骤列表。
    t 的 当前步骤 为 0。
    t 的 重试次数 为 0。
    t 的 创建时间 为 代理_时钟。
    做 追加 用 任务队列、t。
    做 代理_排序任务。
    返还 t 的 任务ID。
完。

代理_排序任务 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务队列 减 1：
        可变 j 为 0。
        只要 j 小于 做 计数 用 任务队列 减 1 减 i：
            若 任务队列 的 第 j 项 的 优先级 小于 任务队列 的 第 j 加 1 项 的 优先级：
                tmp 为 任务队列 的 第 j 项。
                任务队列 的 第 j 项 为 任务队列 的 第 j 加 1 项。
                任务队列 的 第 j 加 1 项 为 tmp。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

代理_tick 接受 零参：
    若 非 代理_已初始化 或者 做 计数 用 任务队列 等于 0：
        返还。
    完。
    代理_时钟 为 代理_时钟 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务队列：
        若 任务队列 的 第 i 项 的 状态 等于 任务状态_等待：
            任务队列 的 第 i 项 的 状态 为 任务状态_执行中。
        完。
        若 任务队列 的 第 i 项 的 状态 等于 任务状态_执行中：
            做 代理_执行步骤 用 i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

代理_执行步骤 接受 任务索引：
    任务 为 任务队列 的 第 任务索引 项。
    若 任务 的 当前步骤 大于或等于 做 计数 用 任务 的 步骤列表：
        任务 的 状态 为 任务状态_完成。
        任务 的 完成时间 为 代理_时钟。
        代理_成功数 为 代理_成功数 加 1。
        做 代理_归档任务 用 任务索引。
        返还。
    完。
    步骤哈希 为 任务 的 步骤列表 的 第 任务 的 当前步骤 项。
    结果 为 做 代理_调用工具 用 步骤哈希。
    若 结果 大于或等于 0：
        任务 的 当前步骤 为 任务 的 当前步骤 加 1。
    否则：
        任务 的 重试次数 为 任务 的 重试次数 加 1。
        若 任务 的 重试次数 大于或等于 重试_最大次数：
            任务 的 状态 为 任务状态_失败。
            代理_失败数 为 代理_失败数 加 1。
            做 代理_归档任务 用 任务索引。
        完。
    完。
完。

代理_归档任务 接受 索引：
    若 做 计数 用 已完成队列 大于 32：
        做 链表_删除 用 已完成队列、0。
    完。
    做 追加 用 已完成队列、任务队列 的 第 索引 项。
    做 链表_删除 用 任务队列、索引。
完。

代理_注册工具 接受 名称哈希、类型：
    若 做 计数 用 工具表 大于或等于 工具_最大数：
        返还 负 1。
    完。
    工具 为 新建 工具描述。
    工具 的 名称哈希 为 名称哈希。
    工具 的 类型 为 类型。
    工具 的 调用次数 为 0。
    工具 的 成功率EMA 为 512。
    做 追加 用 工具表、工具。
    返还 做 计数 用 工具表 减 1。
完。

代理_调用工具 接受 步骤哈希：
    可变 最佳 为 负 1。
    可变 最佳率 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 工具表：
        若 工具表 的 第 i 项 的 成功率EMA 大于 最佳率：
            最佳率 为 工具表 的 第 i 项 的 成功率EMA。
            最佳 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最佳 大于或等于 0：
        工具表 的 第 最佳 项 的 调用次数 为 工具表 的 第 最佳 项 的 调用次数 加 1。
        返还 最佳。
    完。
    返还 负 1。
完。

代理_工具反馈 接受 工具索引、成功：
    若 工具索引 大于或等于 做 计数 用 工具表：
        返还。
    完。
    若 成功：
        工具表 的 第 工具索引 项 的 成功率EMA 为 (工具表 的 第 工具索引 项 的 成功率EMA 乘 922 加 1024 乘 102) 除以 1024。
    否则：
        工具表 的 第 工具索引 项 的 成功率EMA 为 工具表 的 第 工具索引 项 的 成功率EMA 乘 922 除以 1024。
    完。
完。

代理_待执行数 接受 零参：
    返还 做 计数 用 任务队列。
完。

代理_成功率 接受 零参：
    总 为 代理_成功数 加 代理_失败数。
    若 总 等于 0：
        返还 100。
    完。
    返还 代理_成功数 乘 100 除以 总。
完。

代理_统计 接受 零参：
    返还 一本字典：总任务 对应 代理_任务计数、成功 对应 代理_成功数、失败 对应 代理_失败数、待执行 对应 做 计数 用 任务队列、工具数 对应 做 计数 用 工具表。
完。

代理_任务状态名 接受 状态：
    若 状态 等于 任务状态_等待：
        返还 「waiting」。
    完。
    若 状态 等于 任务状态_执行中：
        返还 「running」。
    完。
    若 状态 等于 任务状态_完成：
        返还 「done」。
    完。
    若 状态 等于 任务状态_失败：
        返还 「failed」。
    完。
    返还 「unknown」。
完。

代理_工具类型名 接受 类型：
    若 类型 等于 0：
        返还 「syscmd」。
    完。
    若 类型 等于 1：
        返还 「file」。
    完。
    若 类型 等于 2：
        返还 「network」。
    完。
    若 类型 等于 3：
        返还 「inference」。
    完。
    若 类型 等于 4：
        返还 「custom」。
    完。
    返还 「unknown」。
完。

代理_完整统计 接受 零参：
    返还 一组：代理_任务计数、代理_成功数、代理_失败数、做 计数 用 任务队列、做 计数 用 工具表、做 代理_成功率、代理_时钟。
完。

本章 收束。
