《设置位》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·内核打印》。

引用《.·整数转字符串》。

位图_字大小 为 64。

设置位 接受 值、位：
    返还 值 位或 1 左移 位。
完。

清除位 接受 值、位：
    返还 值 位与 按位取反 (1 左移 位)。
完。

切换位 接受 值、位：
    返还 值 位异或 1 左移 位。
完。

测试位 接受 值、位：
    返还 值 右移 位 位与 1。
完。

设置位_范围 接受 值、起始、结束：
    可变 v 为 值。
    可变 i 为 起始。
    只要 i 小于或等于 结束 并且 i 小于 64：
        v 为 v 位或 1 左移 i。
        i 为 i 加 1。
    完。
    返还 v。
完。

清除位_范围 接受 值、起始、结束：
    可变 v 为 值。
    可变 i 为 起始。
    只要 i 小于或等于 结束 并且 i 小于 64：
        v 为 v 位与 按位取反 (1 左移 i)。
        i 为 i 加 1。
    完。
    返还 v。
完。

切换位_范围 接受 值、起始、结束：
    可变 v 为 值。
    可变 i 为 起始。
    只要 i 小于或等于 结束 并且 i 小于 64：
        v 为 v 位异或 1 左移 i。
        i 为 i 加 1。
    完。
    返还 v。
完。

位掩码 接受 高位、低位：
    宽度 为 高位 减 低位 加 1。
    若 宽度 大于或等于 64：
        返还 0 减 1。
    完。
    返还 (1 左移 宽度) 减 1 左移 低位。
完。

提取位域 接受 值、高位、低位：
    返还 值 右移 低位 位与 (1 左移 高位 减 低位 加 1) 减 1。
完。

插入位域 接受 原值、新值、高位、低位：
    掩码 为 做 位掩码 用 高位、低位。
    返还 原值 位与 按位取反 掩码 位或 新值 左移 低位 位与 掩码。
完。

位域_宽度 接受 高位、低位：
    返还 高位 减 低位 加 1。
完。

查找最低置位 接受 值：
    若 值 等于 0：
        返还 0 减 1。
    完。
    可变 位 为 0。
    可变 v 为 值。
    只要 v 位与 1 等于 0：
        v 为 v 右移 1。
        位 为 位 加 1。
    完。
    返还 位。
完。

查找最高置位 接受 值：
    若 值 等于 0：
        返还 0 减 1。
    完。
    可变 位 为 0。
    可变 v 为 值。
    只要 v 大于 1：
        v 为 v 右移 1。
        位 为 位 加 1。
    完。
    返还 位。
完。

查找第N个置位 接受 值、n：
    若 值 等于 0 或者 n 小于或等于 0：
        返还 负 1。
    完。
    可变 计 为 0。
    可变 i 为 0。
    只要 i 小于 64：
        若 值 右移 i 位与 1：
            计 为 计 加 1。
            若 计 等于 n：
                返还 i。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

查找第一个零位 接受 值：
    返还 做 查找最低置位 用 按位取反 值。
完。

计数置位 接受 值：
    可变 计数 为 0。
    可变 v 为 值。
    只要 v 不等于 0：
        v 为 v 位与 v 减 1。
        计数 为 计数 加 1。
    完。
    返还 计数。
完。

计数置位_快速 接受 值：
    可变 v 为 值。
    v 为 v 减 (v 右移 1 位与 0x5555555555555555)。
    v 为 (v 位与 0x3333333333333333) 加 (v 右移 2 位与 0x3333333333333333)。
    v 为 v 加 (v 右移 4) 位与 0x0F0F0F0F0F0F0F0F。
    返还 v 乘 0x0101010101010101 右移 56。
完。

计数零位 接受 值：
    返还 64 减 做 计数置位 用 值。
完。

是二的幂 接受 值：
    返还 值 大于 0 并且 值 位与 值 减 1 等于 0。
完。

向上取二的幂 接受 值：
    若 值 小于或等于 1：
        返还 1。
    完。
    可变 v 为 值 减 1。
    v 为 v 位或 v 右移 1。
    v 为 v 位或 v 右移 2。
    v 为 v 位或 v 右移 4。
    v 为 v 位或 v 右移 8。
    v 为 v 位或 v 右移 16。
    v 为 v 位或 v 右移 32。
    返还 v 加 1。
完。

向下取二的幂 接受 值：
    若 值 小于或等于 0：
        返还 0。
    完。
    位 为 做 查找最高置位 用 值。
    返还 1 左移 位。
完。

对齐 接受 值、对齐大小：
    若 对齐大小 小于或等于 0：
        返还 值。
    完。
    返还 值 加 对齐大小 减 1 位与 按位取反 (对齐大小 减 1)。
完。

是对齐的 接受 值、对齐大小：
    返还 值 位与 对齐大小 减 1 等于 0。
完。

交换字节16 接受 值：
    返还 (值 位与 0xFF) 左移 8 位或 值 右移 8 位与 0xFF。
完。

交换字节32 接受 值：
    b0 为 (值 位与 0xFF) 左移 24。
    b1 为 (值 右移 8 位与 0xFF) 左移 16。
    b2 为 (值 右移 16 位与 0xFF) 左移 8。
    b3 为 值 右移 24 位与 0xFF。
    返还 b0 位或 b1 位或 b2 位或 b3。
完。

交换字节64 接受 值：
    低32 为 做 交换字节32 用 值 位与 0xFFFFFFFF。
    高32 为 做 交换字节32 用 值 右移 32 位与 0xFFFFFFFF。
    返还 低32 左移 32 位或 高32。
完。

左旋转 接受 值、位数：
    n 为 位数 位与 63。
    返还 值 左移 n 位或 值 右移 64 减 n。
完。

右旋转 接受 值、位数：
    n 为 位数 位与 63。
    返还 值 右移 n 位或 值 左移 64 减 n。
完。

左旋转32 接受 值、位数：
    v 为 值 位与 0xFFFFFFFF。
    n 为 位数 位与 31。
    返还 (v 左移 n 位或 v 右移 32 减 n) 位与 0xFFFFFFFF。
完。

右旋转32 接受 值、位数：
    v 为 值 位与 0xFFFFFFFF。
    n 为 位数 位与 31。
    返还 (v 右移 n 位或 v 左移 32 减 n) 位与 0xFFFFFFFF。
完。

前导零 接受 值：
    若 值 等于 0：
        返还 64。
    完。
    可变 n 为 0。
    可变 v 为 值。
    若 v 位与 0xFFFFFFFF00000000 等于 0：
        n 为 n 加 32。
        v 为 v 左移 32。
    完。
    若 v 位与 0xFFFF000000000000 等于 0：
        n 为 n 加 16。
        v 为 v 左移 16。
    完。
    若 v 位与 0xFF00000000000000 等于 0：
        n 为 n 加 8。
        v 为 v 左移 8。
    完。
    若 v 位与 0xF000000000000000 等于 0：
        n 为 n 加 4。
        v 为 v 左移 4。
    完。
    若 v 位与 0xC000000000000000 等于 0：
        n 为 n 加 2。
        v 为 v 左移 2。
    完。
    若 v 位与 0x8000000000000000 等于 0：
        n 为 n 加 1。
    完。
    返还 n。
完。

后导零 接受 值：
    若 值 等于 0：
        返还 64。
    完。
    可变 n 为 0。
    可变 v 为 值。
    若 v 位与 0x00000000FFFFFFFF 等于 0：
        n 为 n 加 32。
        v 为 v 右移 32。
    完。
    若 v 位与 0x000000000000FFFF 等于 0：
        n 为 n 加 16。
        v 为 v 右移 16。
    完。
    若 v 位与 0x00000000000000FF 等于 0：
        n 为 n 加 8。
        v 为 v 右移 8。
    完。
    若 v 位与 0x000000000000000F 等于 0：
        n 为 n 加 4。
        v 为 v 右移 4。
    完。
    若 v 位与 0x0000000000000003 等于 0：
        n 为 n 加 2。
        v 为 v 右移 2。
    完。
    若 v 位与 0x0000000000000001 等于 0：
        n 为 n 加 1。
    完。
    返还 n。
完。

日志2 接受 值：
    若 值 小于或等于 0：
        返还 负 1。
    完。
    返还 63 减 做 前导零 用 值。
完。

反转位8 接受 值：
    可变 v 为 值 位与 0xFF。
    v 为 (v 位与 0xF0) 右移 4 位或 (v 位与 0x0F) 左移 4。
    v 为 (v 位与 0xCC) 右移 2 位或 (v 位与 0x33) 左移 2。
    v 为 (v 位与 0xAA) 右移 1 位或 (v 位与 0x55) 左移 1。
    返还 v。
完。

反转位32 接受 值：
    可变 v 为 值 位与 0xFFFFFFFF。
    v 为 v 右移 1 位与 0x55555555 位或 (v 位与 0x55555555) 左移 1。
    v 为 v 右移 2 位与 0x33333333 位或 (v 位与 0x33333333) 左移 2。
    v 为 v 右移 4 位与 0x0F0F0F0F 位或 (v 位与 0x0F0F0F0F) 左移 4。
    v 为 v 右移 8 位与 0x00FF00FF 位或 (v 位与 0x00FF00FF) 左移 8。
    v 为 v 右移 16 位或 v 左移 16。
    返还 v 位与 0xFFFFFFFF。
完。

奇偶校验 接受 值：
    可变 v 为 值。
    v 为 v 位异或 v 右移 32。
    v 为 v 位异或 v 右移 16。
    v 为 v 位异或 v 右移 8。
    v 为 v 位异或 v 右移 4。
    v 为 v 位异或 v 右移 2。
    v 为 v 位异或 v 右移 1。
    返还 v 位与 1。
完。

二进制转Gray 接受 值：
    返还 值 位异或 值 右移 1。
完。

Gray转二进制 接受 gray：
    可变 v 为 gray。
    可变 掩码 为 v 右移 1。
    只要 掩码 不等于 0：
        v 为 v 位异或 掩码。
        掩码 为 掩码 右移 1。
    完。
    返还 v。
完。

原子_设置位 接受 地址、位：
    旧值 为 做 原子_读 用 地址。
    做 原子_写 用 地址、旧值 位或 1 左移 位。
    返还 旧值 右移 位 位与 1。
完。

原子_清除位 接受 地址、位：
    旧值 为 做 原子_读 用 地址。
    做 原子_写 用 地址、旧值 位与 按位取反 (1 左移 位)。
    返还 旧值 右移 位 位与 1。
完。

原子_切换位 接受 地址、位：
    旧值 为 做 原子_读 用 地址。
    做 原子_写 用 地址、旧值 位异或 1 左移 位。
    返还 旧值 右移 位 位与 1。
完。

原子_测试并设置 接受 地址、位：
    旧值 为 做 原子_读 用 地址。
    若 旧值 右移 位 位与 1：
        返还 1。
    完。
    做 原子_写 用 地址、旧值 位或 1 左移 位。
    返还 0。
完。

位图_创建 接受 位数：
    字数 为 (位数 加 位图_字大小 减 1) 除以 位图_字大小。
    图 为 做 堆_分配 用 字数 乘 8。
    可变 i 为 0。
    只要 i 小于 字数：
        做 内存_写64 用 图 加 i 乘 8、0。
        i 为 i 加 1。
    完。
    返还 图。
完。

位图_设置 接受 图、位：
    字索引 为 位 除以 位图_字大小。
    位索引 为 位 取余于 位图_字大小。
    旧值 为 做 内存_读64 用 图 加 字索引 乘 8。
    做 内存_写64 用 图 加 字索引 乘 8、旧值 位或 1 左移 位索引。
完。

位图_清除 接受 图、位：
    字索引 为 位 除以 位图_字大小。
    位索引 为 位 取余于 位图_字大小。
    旧值 为 做 内存_读64 用 图 加 字索引 乘 8。
    做 内存_写64 用 图 加 字索引 乘 8、旧值 位与 按位取反 (1 左移 位索引)。
完。

位图_测试 接受 图、位：
    字索引 为 位 除以 位图_字大小。
    位索引 为 位 取余于 位图_字大小。
    字值 为 做 内存_读64 用 图 加 字索引 乘 8。
    返还 字值 右移 位索引 位与 1。
完。

位图_查找第一个零 接受 图、总位数：
    字数 为 (总位数 加 位图_字大小 减 1) 除以 位图_字大小。
    可变 i 为 0。
    只要 i 小于 字数：
        字值 为 做 内存_读64 用 图 加 i 乘 8。
        若 字值 不等于 0xFFFFFFFFFFFFFFFF：
            位内偏移 为 做 查找第一个零位 用 字值。
            结果位 为 i 乘 位图_字大小 加 位内偏移。
            若 结果位 小于 总位数：
                返还 结果位。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

位图_查找第一个置位 接受 图、总位数：
    字数 为 (总位数 加 位图_字大小 减 1) 除以 位图_字大小。
    可变 i 为 0。
    只要 i 小于 字数：
        字值 为 做 内存_读64 用 图 加 i 乘 8。
        若 字值 不等于 0：
            位内偏移 为 做 查找最低置位 用 字值。
            结果位 为 i 乘 位图_字大小 加 位内偏移。
            若 结果位 小于 总位数：
                返还 结果位。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

位图_计数置位 接受 图、总位数：
    字数 为 (总位数 加 位图_字大小 减 1) 除以 位图_字大小。
    可变 总计 为 0。
    可变 i 为 0。
    只要 i 小于 字数：
        总计 为 总计 加 做 计数置位 用 做 内存_读64 用 图 加 i 乘 8。
        i 为 i 加 1。
    完。
    返还 总计。
完。

位图_全部清零 接受 图、总位数：
    字数 为 (总位数 加 位图_字大小 减 1) 除以 位图_字大小。
    可变 i 为 0。
    只要 i 小于 字数：
        做 内存_写64 用 图 加 i 乘 8、0。
        i 为 i 加 1。
    完。
完。

位图_全部置一 接受 图、总位数：
    字数 为 (总位数 加 位图_字大小 减 1) 除以 位图_字大小。
    可变 i 为 0。
    只要 i 小于 字数：
        做 内存_写64 用 图 加 i 乘 8、0xFFFFFFFFFFFFFFFF。
        i 为 i 加 1。
    完。
完。

位图_或 接受 目标、源、总位数：
    字数 为 (总位数 加 位图_字大小 减 1) 除以 位图_字大小。
    可变 i 为 0。
    只要 i 小于 字数：
        a 为 做 内存_读64 用 目标 加 i 乘 8。
        b 为 做 内存_读64 用 源 加 i 乘 8。
        做 内存_写64 用 目标 加 i 乘 8、a 位或 b。
        i 为 i 加 1。
    完。
完。

位图_与 接受 目标、源、总位数：
    字数 为 (总位数 加 位图_字大小 减 1) 除以 位图_字大小。
    可变 i 为 0。
    只要 i 小于 字数：
        a 为 做 内存_读64 用 目标 加 i 乘 8。
        b 为 做 内存_读64 用 源 加 i 乘 8。
        做 内存_写64 用 目标 加 i 乘 8、a 位与 b。
        i 为 i 加 1。
    完。
完。

位图_异或 接受 目标、源、总位数：
    字数 为 (总位数 加 位图_字大小 减 1) 除以 位图_字大小。
    可变 i 为 0。
    只要 i 小于 字数：
        a 为 做 内存_读64 用 目标 加 i 乘 8。
        b 为 做 内存_读64 用 源 加 i 乘 8。
        做 内存_写64 用 目标 加 i 乘 8、a 位异或 b。
        i 为 i 加 1。
    完。
完。

位图_取反 接受 图、总位数：
    字数 为 (总位数 加 位图_字大小 减 1) 除以 位图_字大小。
    可变 i 为 0。
    只要 i 小于 字数：
        做 内存_円64 用 图 加 i 乘 8、按位取反 做 内存_读64 用 图 加 i 乘 8。
        i 为 i 加 1。
    完。
完。

位图_设置_范围 接受 图、起始位、结束位：
    可变 i 为 起始位。
    只要 i 小于或等于 结束位：
        做 位图_设置 用 图、i。
        i 为 i 加 1。
    完。
完。

位图_清除_范围 接受 图、起始位、结束位：
    可变 i 为 起始位。
    只要 i 小于或等于 结束位：
        做 位图_清除 用 图、i。
        i 为 i 加 1。
    完。
完。

位图_查找下一个零 接受 图、总位数、起始位：
    可变 i 为 起始位。
    只要 i 小于 总位数：
        若 做 位图_测试 用 图、i 等于 0：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

位图_查找下一个置位 接受 图、总位数、起始位：
    可变 i 为 起始位。
    只要 i 小于 总位数：
        若 做 位图_测试 用 图、i 不等于 0：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

位图_查找连续零 接受 图、总位数、需要数：
    可变 开始 为 负 1。
    可变 连续 为 0。
    可变 i 为 0。
    只要 i 小于 总位数：
        若 做 位图_测试 用 图、i 等于 0：
            若 连续 等于 0：
                开始 为 i。
            完。
            连续 为 连续 加 1。
            若 连续 大于或等于 需要数：
                返还 开始。
            完。
        否则：
            连续 为 0。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

位图_相等 接受 图A、图B、总位数：
    字数 为 (总位数 加 位图_字大小 减 1) 除以 位图_字大小。
    可变 i 为 0。
    只要 i 小于 字数：
        若 做 内存_读64 用 图A 加 i 乘 8 不等于 做 内存_读64 用 图B 加 i 乘 8：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

位图_是子集 接受 图A、图B、总位数：
    字数 为 (总位数 加 位图_字大小 减 1) 除以 位图_字大小。
    可变 i 为 0。
    只要 i 小于 字数：
        a 为 做 内存_读64 用 图A 加 i 乘 8。
        b 为 做 内存_读64 用 图B 加 i 乘 8。
        若 a 位与 b 不等于 a：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

位图_是空 接受 图、总位数：
    返还 做 位图_计数置位 用 图、总位数 等于 0。
完。

位图_是满 接受 图、总位数：
    返还 做 位图_计数置位 用 图、总位数 等于 总位数。
完。

可变 位操作_总次数 为 0。

可变 位图_创建数 为 0。

可变 位图_查找数 为 0。

位操作_打印统计 接受 零参：
    做 打印行 用 「=== 位操作统计 ===」。
    做 打印行 用 「总操作: 」 加 做 整数转字符串 用 位操作_总次数。
    做 打印行 用 「位图创建: 」 加 做 整数转字符串 用 位图_创建数。
    做 打印行 用 「位图查找: 」 加 做 整数转字符串 用 位图_查找数。
完。

位操作_重置统计 接受 零参：
    位操作_总次数 为 0。
    位图_创建数 为 0。
    位图_查找数 为 0。
完。

可变 AI_设置位_调用数 为 0。

可变 AI_设置位_异常数 为 0。

AI_设置位_监控 接受 零参：
    AI_设置位_调用数 为 AI_设置位_调用数 加 1。
    返还 AI_设置位_异常数 等于 0。
完。

AI_设置位_健康检查 接受 零参：
    若 AI_设置位_调用数 大于 0 并且 AI_设置位_异常数 乘 100 除以 AI_设置位_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
