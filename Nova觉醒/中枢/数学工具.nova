《数学工具》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·整数转字符串》。

可变 _数学_随机状态 为 0x12345678。

数学_随机 接受 零参：
    _数学_随机状态 为 _数学_随机状态 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    返还 _数学_随机状态。
完。

数学_随机_范围 接受 最小、最大：
    若 最大 小于或等于 最小：
        返还 最小。
    完。
    返还 最小 加 做 数学_随机 取余于 (最大 减 最小 加 1)。
完。

数学_随机_设种 接受 种子：
    _数学_随机状态 为 种子。
完。

数学_高斯随机 接受 均值、标准差：
    s 为 做 数学_随机 取余于 1000 加 做 数学_随机 取余于 1000 加 做 数学_随机 取余于 1000 减 1500。
    返还 均值 加 s 乘 标准差 除以 1500。
完。

数学_绝对值 接受 n：
    若 n 小于 0：
        返还 0 减 n。
    完。
    返还 n。
完。

数学_最大值 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

数学_最小值 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

数学_钳制 接受 值、下限、上限：
    若 值 小于 下限：
        返还 下限。
    完。
    若 值 大于 上限：
        返还 上限。
    完。
    返还 值。
完。

数学_开方 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    若 n 等于 1：
        返还 1。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

数学_正弦 接受 角_千：
    可变 a 为 角_千 取余于 6283。
    若 a 小于 0：
        a 为 a 加 6283。
    完。
    若 a 小于或等于 3141：
        p 为 a 乘 (3141 减 a) 除以 1000。
        返还 16 乘 p 乘 1000 除以 (49348 减 4 乘 p)。
    完。
    a 为 a 减 3141。
    p 为 a 乘 (3141 减 a) 除以 1000。
    返还 0 减 16 乘 p 乘 1000 除以 (49348 减 4 乘 p)。
完。

数学_余弦 接受 角_千：
    返还 做 数学_正弦 用 角_千 加 1570。
完。

数学_线性插值 接受 a、b、t：
    返还 a 加 (b 减 a) 乘 t 除以 1000。
完。

数学_双曲正切 接受 x_千：
    返还 2 乘 做 数学_sigmoid 用 2 乘 x_千 减 1000。
完。

前向_门控线性_层 接受 输入、门控张量、输出、n：
    若 输入 等于 0 或者 门控张量 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        x 为 做 内存_读32有符号 用 输入 加 i 乘 4。
        g 为 做 内存_读32有符号 用 门控张量 加 i 乘 4。
        做 内存_写32 用 输出 加 i 乘 4、做 激活_门控线性 用 x、g。
        i 为 i 加 1。
    完。
完。

数学_泄漏修正线性 接受 x_千：
    若 x_千 小于 0：
        返还 x_千 乘 100 除以 1000。
    完。
    返还 x_千。
完。

归一化指数_数值稳定 接受 scores、n：
    若 scores 等于 0 或者 n 等于 0：
        返还。
    完。
    可变 max_s 为 做 内存_读32有符号 用 scores。
    可变 i 为 1。
    只要 i 小于 n：
        v 为 做 内存_读32有符号 用 scores 加 i 乘 4。
        若 v 大于 max_s：
            max_s 为 v。
        完。
        i 为 i 加 1。
    完。
    可变 sum_exp 为 0。
    i 为 0。
    只要 i 小于 n：
        v 为 做 内存_读32有符号 用 scores 加 i 乘 4。
        e 为 做 Q_exp 用 v 减 max_s。
        做 内存_写32 用 scores 加 i 乘 4、e。
        sum_exp 为 sum_exp 加 e。
        i 为 i 加 1。
    完。
    若 sum_exp 等于 0：
        返还。
    完。
    i 为 0。
    只要 i 小于 n：
        e 为 做 内存_读32有符号 用 scores 加 i 乘 4。
        做 内存_写32 用 scores 加 i 乘 4、做 Q除 用 e 乘 Q_ONE、sum_exp。
        i 为 i 加 1。
    完。
完。

数学_千分位转小数字符串 接受 千分值：
    整数部分 为 千分值 除以 1000。
    小数部分 为 千分值 取余于 1000。
    若 千分值 小于 0：
        整数部分 为 (0 减 千分值) 除以 1000。
        小数部分 为 (0 减 千分值) 取余于 1000。
        返还 「-」 加 做 整数转字符串 用 整数部分 加 「.」 加 做 _数学_填零3 用 小数部分。
    完。
    返还 做 整数转字符串 用 整数部分 加 「.」 加 做 _数学_填零3 用 小数部分。
完。

_数学_填零3 接受 n：
    若 n 小于 10：
        返还 「00」 加 做 整数转字符串 用 n。
    完。
    若 n 小于 100：
        返还 「0」 加 做 整数转字符串 用 n。
    完。
    返还 做 整数转字符串 用 n。
完。

数学_快速幂 接受 底、指数：
    若 指数 小于 0：
        返还 0。
    完。
    若 指数 等于 0：
        返还 1。
    完。
    可变 结果 为 1。
    可变 基 为 底。
    可变 n 为 指数。
    只要 n 大于 0：
        若 n 位与 1：
            结果 为 结果 乘 基。
        完。
        基 为 基 乘 基。
        n 为 n 右移 1。
    完。
    返还 结果。
完。

数学_指数 接受 x_千：
    x 为 x_千。
    x2 为 x 乘 x 除以 1000。
    x3 为 x2 乘 x 除以 1000。
    x4 为 x3 乘 x 除以 1000。
    x5 为 x4 乘 x 除以 1000。
    返还 1000 加 x 加 x2 除以 2 加 x3 除以 6 加 x4 除以 24 加 x5 除以 120。
完。

数学_对数 接受 x_千：
    若 x_千 小于或等于 0：
        返还 0 减 99999。
    完。
    若 x_千 等于 1000：
        返还 0。
    完。
    可变 y 为 0。
    可变 tmp 为 x_千。
    只要 tmp 大于 2718：
        y 为 y 加 1000。
        tmp 为 tmp 乘 1000 除以 2718。
    完。
    只要 tmp 小于 368：
        y 为 y 减 1000。
        tmp 为 tmp 乘 2718 除以 1000。
    完。
    可变 迭代 为 0。
    只要 迭代 小于 8：
        ey 为 做 数学_指数 用 y。
        若 ey 小于或等于 0：
            ey 为 1。
        完。
        y 为 y 加 (x_千 减 ey) 乘 1000 除以 ey。
        迭代 为 迭代 加 1。
    完。
    返还 y。
完。

数学_反正切2 接受 y、x：
    若 x 等于 0 并且 y 等于 0：
        返还 0。
    完。
    若 x 等于 0：
        若 y 大于 0：
            返还 1570。
        完。
        返还 0 减 1570。
    完。
    可变 符号 为 1。
    可变 ay 为 y。
    可变 ax 为 x。
    若 ay 小于 0：
        ay 为 0 减 ay。
        符号 为 0 减 符号。
    完。
    若 ax 小于 0：
        ax 为 0 减 ax。
    完。
    可变 角度 为 0。
    若 ay 小于或等于 ax：
        z 为 ay 乘 1000 除以 ax。
        角度 为 z 乘 1000 除以 (1000 加 z 乘 z 乘 285 除以 1000000)。
    否则：
        z 为 ax 乘 1000 除以 ay。
        角度 为 1570 减 z 乘 1000 除以 (1000 加 z 乘 z 乘 285 除以 1000000)。
    完。
    若 x 小于 0：
        角度 为 3141 减 角度。
    完。
    若 符号 小于 0：
        角度 为 0 减 角度。
    完。
    返还 角度。
完。

数学_EMA更新 接受 旧值、新值、平滑因子：
    返还 (旧值 乘 (1000 减 平滑因子) 加 新值 乘 平滑因子) 除以 1000。
完。

数学_DEMA更新 接受 EMA1、EMA2、新值、平滑因子：
    新EMA1 为 做 数学_EMA更新 用 EMA1、新值、平滑因子。
    新EMA2 为 做 数学_EMA更新 用 EMA2、新EMA1、平滑因子。
    返还 一组：新EMA1、新EMA2、2 乘 新EMA1 减 新EMA2。
完。

对数近似_整数 接受 n：
    若 n 小于或等于 1：
        返还 0。
    完。
    返还 做 数学_对数 用 n 乘 1000。
完。

整数开方 接受 n：
    返还 做 数学_开方 用 n。
完。

数学_点积 接受 向量A、向量B、维度：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        和 为 和 加 向量A 的 第 i 项 乘 向量B 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 和。
完。

数学_L2范数 接受 向量、维度：
    可变 平方和 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        平方和 为 平方和 加 向量 的 第 i 项 乘 向量 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 做 数学_开方 用 平方和 乘 31。
完。

数学_向量加 接受 向量A、向量B、维度：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 结果、向量A 的 第 i 项 加 向量B 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学_向量减 接受 向量A、向量B、维度：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 结果、向量A 的 第 i 项 减 向量B 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学_向量数乘 接受 向量、标量_千、维度：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 结果、向量 的 第 i 项 乘 标量_千 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学_向量哈达玛积 接受 向量A、向量B、维度：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 结果、向量A 的 第 i 项 乘 向量B 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学_向量归一化 接受 向量、维度：
    范数 为 做 数学_L2范数 用 向量、维度。
    若 范数 等于 0：
        返还 向量。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 结果、向量 的 第 i 项 乘 1000 除以 范数。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学_余弦相似度 接受 向量A、向量B、维度：
    点 为 做 数学_点积 用 向量A、向量B、维度。
    范A 为 做 数学_L2范数 用 向量A、维度。
    范B 为 做 数学_L2范数 用 向量B、维度。
    若 范A 等于 0 或者 范B 等于 0：
        返还 0。
    完。
    返还 点 乘 1000 除以 范A 乘 1000 除以 范B。
完。

数学_向量距离 接受 向量A、向量B、维度：
    差 为 做 数学_向量减 用 向量A、向量B、维度。
    返还 做 数学_L2范数 用 差、维度。
完。

数学_曼哈顿距离 接受 向量A、向量B、维度：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        总和 为 总和 加 做 数学_绝对值 用 向量A 的 第 i 项 减 向量B 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总和。
完。

数学_闵可夫斯基距离 接受 向量A、向量B、维度、p：
    若 p 小于或等于 0：
        返还 0。
    完。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        d 为 做 数学_绝对值 用 向量A 的 第 i 项 减 向量B 的 第 i 项。
        总和 为 总和 加 做 数学_快速幂 用 d、p。
        i 为 i 加 1。
    完。
    返还 做 整数开方 用 总和。
完。

数学_矩阵2x2_创建 接受 a00、a01、a10、a11：
    返还 一组：一组：a00、a01、一组：a10、a11。
完。

数学_矩阵2x2_单位 接受 零参：
    返还 一组：一组：1000、0、一组：0、1000。
完。

数学_矩阵2x2_乘 接受 矩阵A、矩阵B：
    c00 为 (矩阵A 的 第 0 项 的 第 0 项 乘 矩阵B 的 第 0 项 的 第 0 项 加 矩阵A 的 第 0 项 的 第 1 项 乘 矩阵B 的 第 1 项 的 第 0 项) 除以 1000。
    c01 为 (矩阵A 的 第 0 项 的 第 0 项 乘 矩阵B 的 第 0 项 的 第 1 项 加 矩阵A 的 第 0 项 的 第 1 项 乘 矩阵B 的 第 1 项 的 第 1 项) 除以 1000。
    c10 为 (矩阵A 的 第 1 项 的 第 0 项 乘 矩阵B 的 第 0 项 的 第 0 项 加 矩阵A 的 第 1 项 的 第 1 项 乘 矩阵B 的 第 1 项 的 第 0 项) 除以 1000。
    c11 为 (矩阵A 的 第 1 项 的 第 0 项 乘 矩阵B 的 第 0 项 的 第 1 项 加 矩阵A 的 第 1 项 的 第 1 项 乘 矩阵B 的 第 1 项 的 第 1 项) 除以 1000。
    返还 一组：一组：c00、c01、一组：c10、c11。
完。

数学_矩阵2x2_行列式 接受 矩阵：
    返还 (矩阵 的 第 0 项 的 第 0 项 乘 矩阵 的 第 1 项 的 第 1 项 减 矩阵 的 第 0 项 的 第 1 项 乘 矩阵 的 第 1 项 的 第 0 项) 除以 1000。
完。

数学_矩阵2x2_逆 接受 矩阵：
    det 为 做 数学_矩阵2x2_行列式 用 矩阵。
    若 det 等于 0：
        返还 空。
    完。
    返还 一组：一组：矩阵 的 第 1 项 的 第 1 项 乘 1000 除以 det、0 减 矩阵 的 第 0 项 的 第 1 项 乘 1000 除以 det、一组：0 减 矩阵 的 第 1 项 的 第 0 项 乘 1000 除以 det、矩阵 的 第 0 项 的 第 0 项 乘 1000 除以 det。
完。

数学_矩阵2x2_向量乘 接受 矩阵、向量2：
    x 为 (矩阵 的 第 0 项 的 第 0 项 乘 向量2 的 第 0 项 加 矩阵 的 第 0 项 的 第 1 项 乘 向量2 的 第 1 项) 除以 1000。
    y 为 (矩阵 的 第 1 项 的 第 0 项 乘 向量2 的 第 0 项 加 矩阵 的 第 1 项 的 第 1 项 乘 向量2 的 第 1 项) 除以 1000。
    返还 一组：x、y。
完。

数学_矩阵2x2_旋转 接受 角_千：
    c 为 做 数学_余弦 用 角_千。
    s 为 做 数学_正弦 用 角_千。
    返还 一组：一组：c、0 减 s、一组：s、c。
完。

数学_矩阵2x2_缩放 接受 sx、sy：
    返还 一组：一组：sx、0、一组：0、sy。
完。

数学_矩阵2x2_转置 接受 矩阵：
    返还 一组：一组：矩阵 的 第 0 项 的 第 0 项、矩阵 的 第 1 项 的 第 0 项、一组：矩阵 的 第 0 项 的 第 1 项、矩阵 的 第 1 项 的 第 1 项。
完。

数学_矩阵3x3_单位 接受 零参：
    返还 一组：一组：1000、0、0、一组：0、1000、0、一组：0、0、1000。
完。

数学_矩阵3x3_向量乘 接受 矩阵、向量3：
    x 为 (矩阵 的 第 0 项 的 第 0 项 乘 向量3 的 第 0 项 加 矩阵 的 第 0 项 的 第 1 项 乘 向量3 的 第 1 项 加 矩阵 的 第 0 项 的 第 2 项 乘 向量3 的 第 2 项) 除以 1000。
    y 为 (矩阵 的 第 1 项 的 第 0 项 乘 向量3 的 第 0 项 加 矩阵 的 第 1 项 的 第 1 项 乘 向量3 的 第 1 项 加 矩阵 的 第 1 项 的 第 2 项 乘 向量3 的 第 2 项) 除以 1000。
    z 为 (矩阵 的 第 2 项 的 第 0 项 乘 向量3 的 第 0 项 加 矩阵 的 第 2 项 的 第 1 项 乘 向量3 的 第 1 项 加 矩阵 的 第 2 项 的 第 2 项 乘 向量3 的 第 2 项) 除以 1000。
    返还 一组：x、y、z。
完。

数学_均值 接受 数组、长度：
    若 长度 小于或等于 0：
        返还 0。
    完。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        和 为 和 加 数组 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和 除以 长度。
完。

数学_方差 接受 数组、长度：
    若 长度 小于或等于 1：
        返还 0。
    完。
    均值 为 做 数学_均值 用 数组、长度。
    可变 平方和 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        d 为 数组 的 第 i 项 减 均值。
        平方和 为 平方和 加 d 乘 d 除以 1000。
        i 为 i 加 1。
    完。
    返还 平方和 除以 (长度 减 1)。
完。

数学_标准差 接受 数组、长度：
    返还 做 数学_开方 用 做 数学_方差 用 数组、长度。
完。

数学_中位数 接受 数组、长度：
    若 长度 小于或等于 0：
        返还 0。
    完。
    可变 拷贝 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度：
        做 追加 用 拷贝、数组 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 1。
    只要 i 小于 长度：
        可变 key 为 拷贝 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 拷贝 的 第 j 项 大于 key：
            拷贝 的 第 j 加 1 项 为 拷贝 的 第 j 项。
            j 为 j 减 1。
        完。
        拷贝 的 第 j 加 1 项 为 key。
        i 为 i 加 1。
    完。
    若 长度 取余于 2 等于 1：
        返还 拷贝 的 第 长度 除以 2 项。
    完。
    返还 (拷贝 的 第 长度 除以 2 减 1 项 加 拷贝 的 第 长度 除以 2 项) 除以 2。
完。

数学_极差 接受 数组、长度：
    若 长度 小于或等于 0：
        返还 0。
    完。
    可变 最小 为 数组 的 第 0 项。
    可变 最大 为 数组 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 长度：
        若 数组 的 第 i 项 小于 最小：
            最小 为 数组 的 第 i 项。
        完。
        若 数组 的 第 i 项 大于 最大：
            最大 为 数组 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大 减 最小。
完。

数学_偏度近似 接受 数组、长度：
    若 长度 小于或等于 2：
        返还 0。
    完。
    均值 为 做 数学_均值 用 数组、长度。
    标准差 为 做 数学_标准差 用 数组、长度。
    若 标准差 等于 0：
        返还 0。
    完。
    可变 累积 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        z 为 (数组 的 第 i 项 减 均值) 乘 1000 除以 标准差。
        累积 为 累积 加 z 乘 z 除以 1000 乘 z 除以 1000。
        i 为 i 加 1。
    完。
    返还 累积 除以 长度。
完。

梯度累加器_创建 接受 参数维度、衰减率_千、学习率_千：
    可变 累积 为 一组：（空）。
    可变 平方 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数维度：
        做 追加 用 累积、0。
        做 追加 用 平方、0。
        i 为 i 加 1。
    完。
    若 衰减率_千 小于或等于 0：
        衰减率_千 为 900。
    完。
    若 学习率_千 小于或等于 0：
        学习率_千 为 10。
    完。
    返还 一组：累积、平方、0、衰减率_千、学习率_千。
完。

梯度累加器_更新 接受 累加器、梯度向量、维度：
    可变 i 为 0。
    只要 i 小于 维度：
        累加器 的 第 0 项 的 第 i 项 为 (累加器 的 第 0 项 的 第 i 项 乘 累加器 的 第 3 项 加 梯度向量 的 第 i 项 乘 (1000 减 累加器 的 第 3 项)) 除以 1000。
        g2 为 梯度向量 的 第 i 项 乘 梯度向量 的 第 i 项 除以 1000。
        累加器 的 第 1 项 的 第 i 项 为 (累加器 的 第 1 项 的 第 i 项 乘 累加器 的 第 3 项 加 g2 乘 (1000 减 累加器 的 第 3 项)) 除以 1000。
        i 为 i 加 1。
    完。
    累加器 的 第 2 项 为 累加器 的 第 2 项 加 1。
完。

梯度累加器_计算步长 接受 累加器、维度：
    可变 步长 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        denom 为 做 数学_开方 用 累加器 的 第 1 项 的 第 i 项 加 1。
        做 追加 用 步长、累加器 的 第 0 项 的 第 i 项 乘 累加器 的 第 4 项 除以 denom。
        i 为 i 加 1。
    完。
    返还 步长。
完。

梯度累加器_应用更新 接受 参数向量、步长向量、维度：
    可变 i 为 0。
    只要 i 小于 维度：
        参数向量 的 第 i 项 为 参数向量 的 第 i 项 减 步长向量 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 参数向量。
完。

梯度累加器_重置 接受 累加器、维度：
    可变 i 为 0。
    只要 i 小于 维度：
        累加器 的 第 0 项 的 第 i 项 为 0。
        累加器 的 第 1 项 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    累加器 的 第 2 项 为 0。
完。

梯度累加器_统计 接受 累加器：
    返还 一组：「步数」、累加器 的 第 2 项、「衰减率」、累加器 的 第 3 项、「学习率」、累加器 的 第 4 项。
完。

数学工具_完整统计 接受 零参：
    返还 一组：「随机种子」、_数学_随机状态、「FP基数」、1000、「二倍PI」、6283、「PI」、3141、「向量能力」、10、「矩阵能力」、11、「统计能力」、7、「AI激活能力」、5、「扩展能力」、1。
完。

本章 收束。
