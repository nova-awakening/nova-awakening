《获取进程》

引用《.·系统调用表》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·进程任务》。

引用《..·记忆·VFS文件读写》。

引用《.·字符串替换》。

引用《..·记忆·文件描述符》。

引用《.·执行分叉》。

引用《.·进程描述符》。

引用《..·记忆·目录项添加》。

引用《.·公平调度睡眠》。

引用《..·记忆·路径查找》。

引用《.·复制文件》。

引用《..·记忆·文件节点创建》。

引用《.·自动扩容》。

引用《.·网络系统调用》。

引用《.·每处理器》。

引用《.·信号获取默》。

引用《.·缺页类型名》。

WNOHANG 为 0x00000001。

WUNTRACED 为 0x00000002。

WCONTINUED 为 0x00000008。

CLONE_VM 为 0x00000100。

CLONE_FS 为 0x00000200。

CLONE_FILES 为 0x00000400。

CLONE_SIGHAND 为 0x00000800。

CLONE_THREAD 为 0x00010000。

CLONE_VFORK 为 0x00004000。

CLONE_PARENT 为 0x00008000。

CLONE_NEWNS 为 0x00020000。

SIGHUP 为 1。

SIGINT 为 2。

SIGQUIT 为 3。

SIGILL 为 4。

SIGABRT 为 6。

SIGFPE 为 8。

SIGKILL 为 9。

SIGSEGV 为 11。

SIGPIPE 为 13。

SIGALRM 为 14。

SIGTERM 为 15。

SIGCHLD 为 17。

SIGCONT 为 18。

SIGSTOP 为 19。

SIGTSTP 为 20。

SIGTTIN 为 21。

SIGTTOU 为 22。

SIGUSR1 为 10。

SIGUSR2 为 12。

最大信号 为 64。

sys_getpid 接受 _a、_b、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    返还 做 进程_PID 用 当前。
完。

sys_getppid 接受 _a、_b、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    父 为 做 进程_父 用 当前。
    若 父 等于 空：
        返还 0。
    完。
    返还 做 进程_PID 用 父。
完。

sys_getuid 接受 _a、_b、_c、_d、_e、_f：
    返还 做 进程_uid 用 做 获取当前进程。
完。

sys_getgid 接受 _a、_b、_c、_d、_e、_f：
    返还 做 进程_gid 用 做 获取当前进程。
完。

sys_geteuid 接受 _a、_b、_c、_d、_e、_f：
    返还 做 进程_euid 用 做 获取当前进程。
完。

sys_getegid 接受 _a、_b、_c、_d、_e、_f：
    返还 做 进程_egid 用 做 获取当前进程。
完。

sys_setuid 接受 uid、_b、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    若 做 进程_euid 用 当前 不等于 0 并且 uid 不等于 做 进程_uid 用 当前：
        返还 错误_权限。
    完。
    做 进程_设置uid 用 当前、uid。
    做 进程_设置euid 用 当前、uid。
    返还 成功。
完。

sys_setgid 接受 gid、_b、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    若 做 进程_euid 用 当前 不等于 0 并且 gid 不等于 做 进程_gid 用 当前：
        返还 错误_权限。
    完。
    做 进程_设置gid 用 当前、gid。
    做 进程_设置egid 用 当前、gid。
    返还 成功。
完。

sys_fork 接受 _a、_b、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    子进程 为 做 copy_process 用 当前、SIGCHLD、0、0。
    若 子进程 等于 空：
        返还 错误_内存不足。
    完。
    子pid 为 做 进程_PID 用 子进程。
    做 进程_设置状态 用 子进程、进程状态_就绪。
    做 调度_入队 用 子进程。
    返还 子pid。
完。

sys_vfork 接受 _a、_b、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    子进程 为 做 copy_process 用 当前、CLONE_VFORK 位或 CLONE_VM 位或 SIGCHLD、0、0。
    若 子进程 等于 空：
        返还 错误_内存不足。
    完。
    子pid 为 做 进程_PID 用 子进程。
    做 进程_设置状态 用 子进程、进程状态_就绪。
    做 调度_入队 用 子进程。
    若 子进程 不等于 空：
        做 进程_设置状态 用 当前、进程状态_等待。
        做 调度。
    完。
    返还 子pid。
完。

sys_clone 接受 标志、栈指针、parent_tidptr、child_tidptr、_e、_f：
    当前 为 做 获取当前进程。
    若 标志 位与 CLONE_SIGHAND 不等于 0 并且 标志 位与 CLONE_VM 等于 0：
        返还 错误_无效参数。
    完。
    若 标志 位与 CLONE_THREAD 不等于 0 并且 标志 位与 CLONE_SIGHAND 等于 0：
        返还 错误_无效参数。
    完。
    子进程 为 做 copy_process 用 当前、标志、栈指针、0。
    若 子进程 等于 空：
        返还 错误_内存不足。
    完。
    子pid 为 做 进程_PID 用 子进程。
    做 进程_设置状态 用 子进程、进程状态_就绪。
    做 调度_入队 用 子进程。
    返还 子pid。
完。

sys_execve 接受 路径、argv、envp、_d、_e、_f：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_无此进程。
    完。
    根 为 做 获取根目录项。
    目录项 为 做 路径查找 用 根、路径。
    若 目录项 等于 空：
        返还 错误_不存在。
    完。
    节点 为 做 目录项_Nova文件节点 用 目录项。
    若 节点 等于 空：
        返还 错误_不存在。
    完。
    若 非 做 Nova节点_是文件 用 节点：
        返还 错误_不可执行。
    完。
    文件表 为 做 进程_文件表 用 当前。
    做 exec_关闭cloexec 用 文件表。
    结果 为 做 do_exec 用 当前、路径、空、空。
    若 结果 小于 0：
        返还 结果。
    完。
    做 进程_重置信号处理器 用 当前。
    做 进程_设置参数 用 当前、argv、envp。
    返还 成功。
完。

exec_关闭cloexec 接受 文件表：
    可变 i 为 0。
    只要 i 小于 最大打开文件：
        文件 为 做 文件表_获取 用 文件表、i。
        若 文件 不等于 空 并且 做 文件_有cloexec 用 文件：
            做 文件表_关闭 用 文件表、i。
        完。
        i 为 i 加 1。
    完。
完。

可变 获取进程_已初始化 为 假。

可变 getpid_次数 为 0。

可变 getppid_次数 为 0。

可变 getuid_次数 为 0。

可变 getgid_次数 为 0。

可变 geteuid_次数 为 0。

可变 getegid_次数 为 0。

可变 getpgid_次数 为 0。

可变 setpgid_次数 为 0。

可变 getrusage_次数 为 0。

可变 times_次数 为 0。

可变 prctl_次数 为 0。

可变 获取_fork总数 为 0。

可变 获取_execve总数 为 0。

可变 获取_错误数 为 0。

可变 获取_延迟总计 为 0。

可变 获取_延迟样本 为 0。

可变 获取_最大延迟 为 0。

可变 获取_速率 为 0。

可变 获取_速率窗口 为 0。

可变 获取_速率时刻 为 0。

可变 获取_峰值速率 为 0。

进程_可dump标志 为 1 左移 20。

sys_getpgid 接受 pid、_b、_c、_d、_e、_f：
    若 pid 等于 0：
        当前 为 做 获取当前进程。
        若 当前 等于 空：
            返还 错误_不存在。
        完。
        getpgid_次数 为 getpgid_次数 加 1。
        返还 做 进程_组ID 用 当前。
    完。
    目标 为 做 通过PID查找进程 用 pid。
    若 目标 等于 空：
        获取_错误数 为 获取_错误数 加 1。
        返还 错误_不存在。
    完。
    getpgid_次数 为 getpgid_次数 加 1。
    返还 做 进程_组ID 用 目标。
完。

sys_setpgid 接受 pid、pgid、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        获取_错误数 为 获取_错误数 加 1。
        返还 错误_不存在。
    完。
    可变 目标 为 当前。
    若 pid 不等于 0：
        目标 为 做 通过PID查找进程 用 pid。
        若 目标 等于 空：
            获取_错误数 为 获取_错误数 加 1。
            返还 错误_不存在。
        完。
    完。
    若 pgid 等于 0：
        pgid 为 做 进程_PID 用 目标。
    完。
    做 进程_设置组ID 用 目标、pgid。
    setpgid_次数 为 setpgid_次数 加 1。
    返还 0。
完。

sys_getrusage 接受 who、usage、_c、_d、_e、_f：
    若 usage 等于 空：
        获取_错误数 为 获取_错误数 加 1。
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_不存在。
    完。
    做 设置 用 usage、0、做 进程_用户时间 用 当前。
    做 设置 用 usage、1、做 进程_系统时间 用 当前。
    做 设置 用 usage、2、做 进程_最大RSS 用 当前。
    做 设置 用 usage、3、做 进程_缺页次数 用 当前。
    做 设置 用 usage、4、做 进程_上下文切换 用 当前。
    getrusage_次数 为 getrusage_次数 加 1。
    返还 0。
完。

sys_times 接受 buf、_b、_c、_d、_e、_f：
    若 buf 等于 空：
        获取_错误数 为 获取_错误数 加 1。
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_不存在。
    完。
    做 设置 用 buf、0、做 进程_用户时间 用 当前。
    做 设置 用 buf、1、做 进程_系统时间 用 当前。
    做 设置 用 buf、2、0。
    做 设置 用 buf、3、0。
    times_次数 为 times_次数 加 1。
    返还 0。
完。

PR_SET_NAME 为 15。

PR_GET_NAME 为 16。

PR_SET_DUMPABLE 为 4。

PR_GET_DUMPABLE 为 3。

sys_prctl 接受 option、arg2、arg3、arg4、arg5、_f：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        获取_错误数 为 获取_错误数 加 1。
        返还 错误_不存在。
    完。
    若 option 等于 PR_SET_NAME：
        若 arg2 不等于 空：
            做 进程_设置名称 用 当前、arg2。
        完。
        prctl_次数 为 prctl_次数 加 1。
        返还 0。
    完。
    若 option 等于 PR_GET_NAME：
        若 arg2 不等于 空：
            做 进程_获取名称到 用 当前、arg2。
        完。
        prctl_次数 为 prctl_次数 加 1。
        返还 0。
    完。
    若 option 等于 PR_SET_DUMPABLE：
        做 进程_设置dumpable 用 当前、arg2。
        prctl_次数 为 prctl_次数 加 1。
        返还 0。
    完。
    若 option 等于 PR_GET_DUMPABLE：
        prctl_次数 为 prctl_次数 加 1。
        返还 做 进程_获取dumpable 用 当前。
    完。
    获取_错误数 为 获取_错误数 加 1。
    返还 错误_无效参数。
完。

获取_记录延迟 接受 延迟：
    获取_延迟样本 为 获取_延迟样本 加 1。
    获取_延迟总计 为 获取_延迟总计 加 延迟。
    若 延迟 大于 获取_最大延迟：
        获取_最大延迟 为 延迟。
    完。
完。

获取_平均延迟 接受 零参：
    若 获取_延迟样本 等于 0：
        返还 0。
    完。
    返还 获取_延迟总计 除以 获取_延迟样本。
完。

获取进程_完整统计 接受 零参：
    总操作 为 getpid_次数 加 getppid_次数 加 getuid_次数 加 getgid_次数 加 geteuid_次数 加 getegid_次数 加 getpgid_次数 加 setpgid_次数 加 getrusage_次数 加 times_次数 加 prctl_次数。
    统计 为 做 字典。
    做 字典设 用 统计、「已初始化」、获取进程_已初始化。
    做 字典设 用 统计、「getpid」、getpid_次数。
    做 字典设 用 统计、「getppid」、getppid_次数。
    做 字典设 用 统计、「getuid」、getuid_次数。
    做 字典设 用 统计、「getgid」、getgid_次数。
    做 字典设 用 统计、「geteuid」、geteuid_次数。
    做 字典设 用 统计、「getegid」、getegid_次数。
    做 字典设 用 统计、「getpgid」、getpgid_次数。
    做 字典设 用 统计、「setpgid」、setpgid_次数。
    做 字典设 用 统计、「getrusage」、getrusage_次数。
    做 字典设 用 统计、「times」、times_次数。
    做 字典设 用 统计、「prctl」、prctl_次数。
    做 字典设 用 统计、「总操作」、总操作。
    做 字典设 用 统计、「平均延迟(ns)」、做 获取_平均延迟。
    做 字典设 用 统计、「最大延迟(ns)」、获取_最大延迟。
    做 字典设 用 统计、「速率」、获取_速率。
    做 字典设 用 统计、「峰值速率」、获取_峰值速率。
    做 字典设 用 统计、「错误数」、获取_错误数。
    返还 统计。
完。

获取进程_打印统计 接受 零参：
    统计 为 做 获取进程_完整统计。
    做 打印 用 「[获取进程] ═══════ 完整统计 ═══════」。
    做 打印 用 「  getpid:       」 加 做 转字符串 用 做 字典取 用 统计、「getpid」。
    做 打印 用 「  getppid:      」 加 做 转字符串 用 做 字典取 用 统计、「getppid」。
    做 打印 用 「  getuid:       」 加 做 转字符串 用 做 字典取 用 统计、「getuid」。
    做 打印 用 「  getgid:       」 加 做 转字符串 用 做 字典取 用 统计、「getgid」。
    做 打印 用 「  getpgid:      」 加 做 转字符串 用 做 字典取 用 统计、「getpgid」。
    做 打印 用 「  setpgid:      」 加 做 转字符串 用 做 字典取 用 统计、「setpgid」。
    做 打印 用 「  getrusage:    」 加 做 转字符串 用 做 字典取 用 统计、「getrusage」。
    做 打印 用 「  times:        」 加 做 转字符串 用 做 字典取 用 统计、「times」。
    做 打印 用 「  prctl:        」 加 做 转字符串 用 做 字典取 用 统计、「prctl」。
    做 打印 用 「  总操作:       」 加 做 转字符串 用 做 字典取 用 统计、「总操作」。
    做 打印 用 「  平均延迟:     」 加 做 转字符串 用 做 字典取 用 统计、「平均延迟(ns)」 加 「ns」。
    做 打印 用 「  速率:         」 加 做 转字符串 用 做 字典取 用 统计、「速率」 加 「 次/秒」。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[获取进程] ═════════════════════════」。
    返还 0。
完。

获取进程_dump 接受 零参：
    做 打印 用 「[获取进程 dump] ──────────────────」。
    做 打印 用 「  getpid: 」 加 做 转字符串 用 getpid_次数。
    做 打印 用 「  getuid: 」 加 做 转字符串 用 getuid_次数。
    做 打印 用 「  getrusage: 」 加 做 转字符串 用 getrusage_次数。
    做 打印 用 「  prctl: 」 加 做 转字符串 用 prctl_次数。
    做 打印 用 「  速率: 」 加 做 转字符串 用 获取_速率。
    做 打印 用 「  错误: 」 加 做 转字符串 用 获取_错误数。
    做 打印 用 「[获取进程 dump] ──────────────────」。
完。

获取进程_初始化 接受 零参：
    若 获取进程_已初始化 等于 真：
        返还 错误_已存在。
    完。
    获取进程_已初始化 为 真。
    做 打印 用 「[获取进程] 初始化完成」。
    返还 0。
完。

获取进程_销毁 接受 零参：
    获取进程_已初始化 为 假。
    返还 0。
完。

获取进程_为procfs提供统计 接受 零参：
    返还 一组：getpid_次数、getppid_次数、getuid_次数、getgid_次数、getpgid_次数、getrusage_次数、times_次数、prctl_次数。
完。

获取进程_为AI提供使用模式 接受 零参：
    返还 一组：getpid_次数、getuid_次数、getrusage_次数、prctl_次数、做 获取_平均延迟。
完。

获取进程_重置统计 接受 零参：
    getpid_次数 为 0。
    getppid_次数 为 0。
    getuid_次数 为 0。
    getgid_次数 为 0。
    geteuid_次数 为 0。
    getegid_次数 为 0。
    getpgid_次数 为 0。
    setpgid_次数 为 0。
    getrusage_次数 为 0。
    times_次数 为 0。
    prctl_次数 为 0。
    获取_错误数 为 0。
    获取_延迟总计 为 0。
    获取_延迟样本 为 0。
    获取_最大延迟 为 0。
    获取_速率 为 0。
    获取_速率窗口 为 0。
    获取_峰值速率 为 0。
    做 打印 用 「[获取进程] 统计已重置」。
    返还 0。
完。

进程_uid 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    返还 做 进程_UID 用 进程。
完。

进程_gid 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    返还 做 进程_GID 用 进程。
完。

进程_euid 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    返还 做 进程_UID 用 进程。
完。

进程_egid 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    返还 做 进程_GID 用 进程。
完。

进程_用户时间 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    返还 进程 的 第 T_用户时间 项。
完。

进程_系统时间 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    返还 进程 的 第 T_内核时间 项。
完。

进程_最大RSS 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    mm 为 做 进程_mm 用 进程。
    若 mm 等于 空：
        返还 0。
    完。
    返还 mm 的 常驻页数 乘 4。
完。

进程_缺页次数 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    mm 为 做 进程_mm 用 进程。
    若 mm 等于 空：
        返还 做 缺页_获取总次数。
    完。
    返还 mm 的 缺页数。
完。

进程_上下文切换 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    返还 进程 的 第 T_总切换 项。
完。

通过PID查找进程 接受 pid：
    返还 做 进程表_查找 用 pid。
完。

进程_父PID 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    父 为 做 进程_父 用 进程。
    若 父 等于 空：
        返还 0。
    完。
    返还 做 进程_PID 用 父。
完。

进程_会话ID 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    返还 做 进程_SID 用 进程。
完。

进程_线程数 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    返还 1。
完。

进程_命令行 接受 进程：
    若 进程 等于 空：
        返还 「」。
    完。
    名称 为 做 进程_名称 用 进程。
    若 名称 等于 空：
        返还 「」。
    完。
    返还 名称。
完。

进程_自愿切换 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    返还 进程 的 第 T_自愿切换 项。
完。

进程_非自愿切换 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    返还 进程 的 第 T_非自愿切换 项。
完。

进程_设置名称 接受 进程、名称：
    若 进程 等于 空：
        返还。
    完。
    做 进程_设名称 用 进程、名称。
完。

进程_获取名称到 接受 进程、buf：
    若 进程 等于 空 或者 buf 等于 空：
        返还。
    完。
    做 设置 用 buf、0、做 进程_名称 用 进程。
完。

进程_设置组ID 接受 进程、pgid：
    若 进程 等于 空：
        返还。
    完。
    做 进程_设PGID 用 进程、pgid。
完。

进程_设置会话ID 接受 进程、sid：
    若 进程 等于 空：
        返还。
    完。
    做 进程_设SID 用 进程、sid。
完。

进程_设置dumpable 接受 进程、值：
    若 进程 等于 空：
        返还。
    完。
    若 值 不等于 0：
        做 进程_设标志 用 进程、进程_可dump标志。
    否则：
        做 进程_清标志 用 进程、进程_可dump标志。
    完。
完。

进程_获取dumpable 接受 进程：
    若 进程 等于 空：
        返还 0。
    完。
    若 做 进程_标志 用 进程 位与 进程_可dump标志 不等于 0：
        返还 1。
    完。
    返还 0。
完。

获取进程_为调度器提供进程查询负载 接受 零参：
    返还 一组：获取_速率、获取_峰值速率、getpid_次数 加 getppid_次数。
完。

获取进程_为电源管理挂起 接受 零参：
    返还 0。
完。

获取进程_获取总查询数 接受 零参：
    返还 getpid_次数 加 getppid_次数 加 getuid_次数 加 getgid_次数 加 geteuid_次数 加 getegid_次数 加 getpgid_次数 加 setpgid_次数 加 getrusage_次数 加 times_次数 加 prctl_次数。
完。

本章 收束。
