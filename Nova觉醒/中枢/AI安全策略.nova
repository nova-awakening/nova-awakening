《AI安全策略》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·时钟中枢》。

引用《.·链表后继》。

权限_系统 为 0。

权限_用户 为 1。

权限_访客 为 2。

过滤规则_最大 为 64。

审计日志_最大 为 512。

过滤规则 是 一种 事物，含 模式哈希、动作、触发次数。

审计条目 是 一种 事物，含 时间戳、操作类型、用户ID、结果、详情哈希。

可变 输入过滤规则 为 一组：（空）。

可变 输出过滤规则 为 一组：（空）。

可变 审计日志 为 一组：（空）。

可变 安全策略_已初始化 为 假。

可变 总拦截次数 为 0。

可变 总通过次数 为 0。

密钥_最大数 为 16。

加密密钥 是 一种 事物，含 名称哈希、加密数据、创建时间、最后访问。

可变 密钥存储 为 一组：（空）。

安全策略_初始化 接受 零参：
    输入过滤规则 为 一组：（空）。
    输出过滤规则 为 一组：（空）。
    审计日志 为 一组：（空）。
    密钥存储 为 一组：（空）。
    安全策略_已初始化 为 真。
    做 内核信息 用 「AI安全策略: 输入过滤+输出审查+权限分级+接口密钥加密+审计日志」。
完。

安全_添加输入规则 接受 模式哈希、动作：
    若 做 计数 用 输入过滤规则 大于或等于 过滤规则_最大：
        返还 负 1。
    完。
    r 为 新建 过滤规则。
    r 的 模式哈希 为 模式哈希。
    r 的 动作 为 动作。
    r 的 触发次数 为 0。
    做 追加 用 输入过滤规则、r。
    返还 0。
完。

安全_检查输入 接受 内容哈希：
    可变 i 为 0。
    只要 i 小于 做 计数 用 输入过滤规则：
        若 输入过滤规则 的 第 i 项 的 模式哈希 等于 内容哈希：
            输入过滤规则 的 第 i 项 的 触发次数 为 输入过滤规则 的 第 i 项 的 触发次数 加 1。
            若 输入过滤规则 的 第 i 项 的 动作 等于 1：
                总拦截次数 为 总拦截次数 加 1。
                做 安全_记录审计 用 1、0、1、内容哈希。
                返还 假。
            完。
        完。
        i 为 i 加 1。
    完。
    总通过次数 为 总通过次数 加 1。
    返还 真。
完。

安全_检查权限 接受 用户权限级别、需要权限级别：
    返还 用户权限级别 小于或等于 需要权限级别。
完。

安全_AI操作权限 接受 用户权限、操作类型：
    若 操作类型 等于 0 并且 用户权限 不等于 权限_系统：
        返还 假。
    完。
    若 操作类型 等于 1 并且 用户权限 大于 权限_用户：
        返还 假。
    完。
    返还 真。
完。

安全_存储密钥 接受 名称哈希、明文数据、加密密钥：
    若 做 计数 用 密钥存储 大于或等于 密钥_最大数：
        返还 负 1。
    完。
    k 为 新建 加密密钥。
    k 的 名称哈希 为 名称哈希。
    可变 加密后 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 明文数据：
        做 追加 用 加密后、明文数据 的 第 i 项 位异或 加密密钥 右移 i 取余于 8 位与 0xFF。
        i 为 i 加 1。
    完。
    k 的 加密数据 为 加密后。
    k 的 创建时间 为 做 读取毫秒。
    k 的 最后访问 为 k 的 创建时间。
    做 追加 用 密钥存储、k。
    返还 0。
完。

安全_读取密钥 接受 名称哈希、解密密钥：
    可变 i 为 0。
    只要 i 小于 做 计数 用 密钥存储：
        若 密钥存储 的 第 i 项 的 名称哈希 等于 名称哈希：
            密钥存储 的 第 i 项 的 最后访问 为 做 读取毫秒。
            可变 明文 为 一组：（空）。
            可变 j 为 0。
            只要 j 小于 做 计数 用 密钥存储 的 第 i 项 的 加密数据：
                做 追加 用 明文、密钥存储 的 第 i 项 的 加密数据 的 第 j 项 位异或 解密密钥 右移 j 取余于 8 位与 0xFF。
                j 为 j 加 1。
            完。
            返还 明文。
        完。
        i 为 i 加 1。
    完。
    返还 一组：（空）。
完。

安全_删除密钥 接受 名称哈希：
    可变 i 为 0。
    只要 i 小于 做 计数 用 密钥存储：
        若 密钥存储 的 第 i 项 的 名称哈希 等于 名称哈希：
            可变 j 为 0。
            只要 j 小于 做 计数 用 密钥存储 的 第 i 项 的 加密数据：
                密钥存储 的 第 i 项 的 加密数据 的 第 j 项 为 0。
                j 为 j 加 1。
            完。
            做 链表_删除 用 密钥存储、i。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

安全_记录审计 接受 操作类型、用户ID、结果、详情哈希：
    条目 为 新建 审计条目。
    条目 的 时间戳 为 做 读取毫秒。
    条目 的 操作类型 为 操作类型。
    条目 的 用户ID 为 用户ID。
    条目 的 结果 为 结果。
    条目 的 详情哈希 为 详情哈希。
    做 追加 用 审计日志、条目。
    若 做 计数 用 审计日志 大于 审计日志_最大：
        做 链表_删除 用 审计日志、0。
    完。
完。

安全_审计数 接受 零参：
    返还 做 计数 用 审计日志。
完。

安全_拦截率 接受 零参：
    总 为 总拦截次数 加 总通过次数。
    若 总 等于 0：
        返还 0。
    完。
    返还 总拦截次数 乘 100 除以 总。
完。

安全_权限名 接受 级别：
    若 级别 等于 权限_系统：
        返还 「system」。
    完。
    若 级别 等于 权限_用户：
        返还 「user」。
    完。
    若 级别 等于 权限_访客：
        返还 「guest」。
    完。
    返还 「unknown」。
完。

安全_动作名 接受 动作：
    若 动作 等于 0：
        返还 「allow」。
    完。
    若 动作 等于 1：
        返还 「block」。
    完。
    若 动作 等于 2：
        返还 「warn」。
    完。
    返还 「unknown」。
完。

安全策略_完整统计 接受 零参：
    返还 一组：做 计数 用 输入过滤规则、做 计数 用 输出过滤规则、做 计数 用 审计日志、总拦截次数、总通过次数、做 安全_拦截率、做 计数 用 密钥存储。
完。

本章 收束。
