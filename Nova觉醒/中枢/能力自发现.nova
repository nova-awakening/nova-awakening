《能力自发现》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·模块热加载》。

引用《..·外联·序列工具》。

引用《.·内核打印》。

引用《.·整数转字符串》。

成熟度_发现 为 1。

成熟度_原型 为 2。

成熟度_测试 为 3。

成熟度_稳定 为 4。

成熟度_核心 为 5。

能力需求 是 一种 事物，含 描述、优先级、发现时间、已满足、生成的模块、成熟度、失败次数、依赖列表。

可变 能力需求列表 为 一组：（空）。

可变 自生成_模块数 为 0。

可变 自生成_回滚数 为 0。

可变 自生成_失败数 为 0。

最大失败容忍 为 5。

成熟度_晋升_间隔 为 3000。

能力_发现需求 接受 描述、优先级：
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力需求列表：
        若 能力需求列表 的 第 i 项 的 描述 等于 描述：
            若 非 能力需求列表 的 第 i 项 的 已满足：
                若 优先级 大于 能力需求列表 的 第 i 项 的 优先级：
                    能力需求列表 的 第 i 项 的 优先级 为 优先级。
                    做 内核调试 用 「自演化: 提升已有需求优先级 '」 加 描述 加 「' → 」 加 做 整数转字符串 用 优先级。
                完。
            完。
            返还。
        完。
        i 为 i 加 1。
    完。
    需求 为 新建 能力需求。
    需求 的 描述 为 描述。
    需求 的 优先级 为 优先级。
    需求 的 发现时间 为 演化_时钟。
    需求 的 已满足 为 假。
    需求 的 成熟度 为 成熟度_发现。
    需求 的 失败次数 为 0。
    需求 的 依赖列表 为 一组：（空）。
    做 追加 用 能力需求列表、需求。
    做 内核信息 用 「自演化: 发现新能力需求 '」 加 描述 加 「' 优先级=」 加 做 整数转字符串 用 优先级。
完。

能力_添加依赖 接受 能力描述、依赖描述：
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力需求列表：
        若 能力需求列表 的 第 i 项 的 描述 等于 能力描述：
            做 追加 用 能力需求列表 的 第 i 项 的 依赖列表、依赖描述。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

能力_依赖已就绪 接受 需求索引：
    需求 为 能力需求列表 的 第 需求索引 项。
    可变 d 为 0。
    只要 d 小于 做 计数 用 需求 的 依赖列表：
        可变 找到 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 能力需求列表：
            若 能力需求列表 的 第 j 项 的 描述 等于 需求 的 依赖列表 的 第 d 项：
                若 能力需求列表 的 第 j 项 的 已满足：
                    找到 为 真。
                完。
            完。
            j 为 j 加 1。
        完。
        若 非 找到：
            返还 假。
        完。
        d 为 d 加 1。
    完。
    返还 真。
完。

能力_自动满足 接受 零参：
    可变 s 为 0。
    只要 s 小于 做 计数 用 能力需求列表：
        可变 t 为 s 加 1。
        只要 t 小于 做 计数 用 能力需求列表：
            若 能力需求列表 的 第 t 项 的 优先级 大于 能力需求列表 的 第 s 项 的 优先级：
                临时 为 能力需求列表 的 第 s 项。
                能力需求列表 的 第 s 项 为 能力需求列表 的 第 t 项。
                能力需求列表 的 第 t 项 为 临时。
            完。
            t 为 t 加 1。
        完。
        s 为 s 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力需求列表：
        若 非 能力需求列表 的 第 i 项 的 已满足：
            若 能力需求列表 的 第 i 项 的 失败次数 大于或等于 最大失败容忍：
                i 为 i 加 1。
                继续。
            完。
            若 非 做 能力_依赖已就绪 用 i：
                i 为 i 加 1。
                继续。
            完。
            回滚快照 为 做 热更新_创建快照。
            代码 为 做 智能_接口_推理 用 「生成NovaOS模块: 」 加 能力需求列表 的 第 i 项 的 描述。
            若 代码 不等于 空：
                模块ID 为 做 热更新_注册模块 用 能力需求列表 的 第 i 项 的 描述、10000、「自动入口」、一组：（空）。
                加载成功 为 做 热更新_加载 用 模块ID。
                若 加载成功：
                    验证结果 为 做 热更新_验证模块 用 模块ID。
                    若 验证结果：
                        能力需求列表 的 第 i 项 的 已满足 为 真。
                        能力需求列表 的 第 i 项 的 生成的模块 为 模块ID。
                        能力需求列表 的 第 i 项 的 成熟度 为 成熟度_原型。
                        自生成_模块数 为 自生成_模块数 加 1。
                        做 进化日志_记录 用 「自生成」、能力需求列表 的 第 i 项 的 描述、0、10000。
                    否则：
                        做 热更新_恢复快照 用 回滚快照。
                        能力需求列表 的 第 i 项 的 失败次数 为 能力需求列表 的 第 i 项 的 失败次数 加 1。
                        自生成_回滚数 为 自生成_回滚数 加 1。
                        做 内核警告 用 「自演化: 模块验证失败, 已回滚 '」 加 能力需求列表 的 第 i 项 的 描述 加 「'」。
                    完。
                否则：
                    做 热更新_恢复快照 用 回滚快照。
                    能力需求列表 的 第 i 项 的 失败次数 为 能力需求列表 的 第 i 项 的 失败次数 加 1。
                    自生成_失败数 为 自生成_失败数 加 1。
                完。
            否则：
                能力需求列表 的 第 i 项 的 失败次数 为 能力需求列表 的 第 i 项 的 失败次数 加 1。
                自生成_失败数 为 自生成_失败数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

能力_评估成熟度 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力需求列表：
        若 能力需求列表 的 第 i 项 的 已满足：
            存活时间 为 演化_时钟 减 能力需求列表 的 第 i 项 的 发现时间。
            当前阶段 为 能力需求列表 的 第 i 项 的 成熟度。
            若 当前阶段 等于 成熟度_原型：
                若 存活时间 大于或等于 成熟度_晋升_间隔：
                    能力需求列表 的 第 i 项 的 成熟度 为 成熟度_测试。
                    做 进化日志_记录 用 「成熟度晋升」、能力需求列表 的 第 i 项 的 描述 加 「→测试」、2、3。
                完。
            完。
            若 当前阶段 等于 成熟度_测试：
                若 存活时间 大于或等于 成熟度_晋升_间隔 乘 2：
                    若 能力需求列表 的 第 i 项 的 失败次数 等于 0：
                        能力需求列表 的 第 i 项 的 成熟度 为 成熟度_稳定。
                        做 进化日志_记录 用 「成熟度晋升」、能力需求列表 的 第 i 项 的 描述 加 「→稳定」、3、4。
                    完。
                完。
            完。
            若 当前阶段 等于 成熟度_稳定：
                若 存活时间 大于或等于 成熟度_晋升_间隔 乘 4：
                    能力需求列表 的 第 i 项 的 成熟度 为 成熟度_核心。
                    做 进化日志_记录 用 「成熟度晋升」、能力需求列表 的 第 i 项 的 描述 加 「→核心」、4、5。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

已学协议 是 一种 事物，含 协议名、版本、解析器哈希、学习时间、使用次数、匹配_magic、置信度。

可变 已学协议列表 为 一组：（空）。

可变 协议_未知遭遇数 为 0。

可变 协议_自动识别数 为 0。

协议_遇到未知 接受 数据、端口：
    协议_未知遭遇数 为 协议_未知遭遇数 加 1。
    做 内核调试 用 「自演化: 遇到未知协议 端口=」 加 做 整数转字符串 用 端口。
    可变 magic 为 0。
    若 做 计数 用 数据 大于或等于 4：
        magic 为 数据 的 第 0 项 乘 256 乘 256 乘 256 加 数据 的 第 1 项 乘 256 乘 256 加 数据 的 第 2 项 乘 256 加 数据 的 第 3 项。
        做 内核调试 用 「  magic=0x」 加 做 整数转字符串 用 magic。
    完。
    可变 k 为 0。
    只要 k 小于 做 计数 用 已学协议列表：
        若 已学协议列表 的 第 k 项 的 匹配_magic 等于 magic：
            若 magic 不等于 0：
                已学协议列表 的 第 k 项 的 使用次数 为 已学协议列表 的 第 k 项 的 使用次数 加 1。
                协议_自动识别数 为 协议_自动识别数 加 1。
                做 内核信息 用 「自演化: 自动匹配协议 '」 加 已学协议列表 的 第 k 项 的 协议名 加 「'」。
                返还。
            完。
        完。
        k 为 k 加 1。
    完。
    做 能力_发现需求 用 「协议解析器-端口」 加 做 整数转字符串 用 端口、500。
完。

协议_注册已学 接受 协议名、版本、magic值：
    可变 j 为 0。
    只要 j 小于 做 计数 用 已学协议列表：
        若 已学协议列表 的 第 j 项 的 协议名 等于 协议名：
            已学协议列表 的 第 j 项 的 版本 为 版本。
            已学协议列表 的 第 j 项 的 匹配_magic 为 magic值。
            返还。
        完。
        j 为 j 加 1。
    完。
    p 为 新建 已学协议。
    p 的 协议名 为 协议名。
    p 的 版本 为 版本。
    p 的 学习时间 为 演化_时钟。
    p 的 使用次数 为 0。
    p 的 匹配_magic 为 magic值。
    p 的 置信度 为 800。
    做 追加 用 已学协议列表、p。
    做 进化日志_记录 用 「学习协议」、协议名、0、0。
完。

缺陷报告 是 一种 事物，含 模块名、错误描述、严重度、发现时间、已修复、补丁版本、修复尝试数。

可变 缺陷列表 为 一组：（空）。

可变 自动补丁_总数 为 0。

可变 自动补丁_回滚数 为 0。

最大修复尝试 为 3。

缺陷_报告 接受 模块名、错误描述、严重度：
    可变 d 为 0。
    只要 d 小于 做 计数 用 缺陷列表：
        若 缺陷列表 的 第 d 项 的 模块名 等于 模块名：
            若 缺陷列表 的 第 d 项 的 错误描述 等于 错误描述：
                若 非 缺陷列表 的 第 d 项 的 已修复：
                    若 严重度 大于 缺陷列表 的 第 d 项 的 严重度：
                        缺陷列表 的 第 d 项 的 严重度 为 严重度。
                    完。
                    返还。
                完。
            完。
        完。
        d 为 d 加 1。
    完。
    报告 为 新建 缺陷报告。
    报告 的 模块名 为 模块名。
    报告 的 错误描述 为 错误描述。
    报告 的 严重度 为 严重度。
    报告 的 发现时间 为 演化_时钟。
    报告 的 已修复 为 假。
    报告 的 修复尝试数 为 0。
    做 追加 用 缺陷列表、报告。
    做 内核信息 用 「缺陷报告: [」 加 模块名 加 「] 」 加 错误描述 加 「 严重度=」 加 做 整数转字符串 用 严重度。
    若 严重度 大于或等于 3：
        做 缺陷_自动修复 用 做 计数 用 缺陷列表 减 1。
    完。
完。

缺陷_自动修复 接受 缺陷索引：
    若 缺陷索引 大于或等于 做 计数 用 缺陷列表：
        返还。
    完。
    报告 为 缺陷列表 的 第 缺陷索引 项。
    若 报告 的 已修复：
        返还。
    完。
    若 报告 的 修复尝试数 大于或等于 最大修复尝试：
        返还。
    完。
    报告 的 修复尝试数 为 报告 的 修复尝试数 加 1。
    模块 为 做 热更新_按名查找 用 报告 的 模块名。
    若 模块 等于 空：
        返还。
    完。
    快照 为 做 热更新_创建快照。
    补丁版本 为 模块 的 版本 加 1。
    修复 为 做 智能_接口_推理 用 「修复NovaOS缺陷: 」 加 报告 的 模块名 加 「 - 」 加 报告 的 错误描述。
    若 修复 不等于 空：
        成功 为 做 热更新_替换 用 模块 的 模块ID、补丁版本、演化_时钟。
        若 成功：
            验证 为 做 热更新_验证模块 用 模块 的 模块ID。
            若 验证：
                报告 的 已修复 为 真。
                报告 的 补丁版本 为 补丁版本。
                自动补丁_总数 为 自动补丁_总数 加 1。
                做 进化日志_记录 用 「自动补丁」、报告 的 模块名、模块 的 版本 减 1、补丁版本。
            否则：
                做 热更新_恢复快照 用 快照。
                自动补丁_回滚数 为 自动补丁_回滚数 加 1。
                做 内核警告 用 「自演化: 补丁验证失败, 回滚 '」 加 报告 的 模块名 加 「'」。
            完。
        完。
    完。
完。

能力自发现_获取统计 接受 零参：
    可变 已满足数 为 0。
    可变 未满足数 为 0。
    可变 核心级数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力需求列表：
        若 能力需求列表 的 第 i 项 的 已满足：
            已满足数 为 已满足数 加 1。
            若 能力需求列表 的 第 i 项 的 成熟度 等于 成熟度_核心：
                核心级数 为 核心级数 加 1。
            完。
        否则：
            未满足数 为 未满足数 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 未修复缺陷 为 0。
    可变 j 为 0。
    只要 j 小于 做 计数 用 缺陷列表：
        若 非 缺陷列表 的 第 j 项 的 已修复：
            未修复缺陷 为 未修复缺陷 加 1。
        完。
        j 为 j 加 1。
    完。
    返还 一本字典：需求总数 对应 做 计数 用 能力需求列表、已满足 对应 已满足数、未满足 对应 未满足数、核心级能力 对应 核心级数、自生成模块 对应 自生成_模块数、生成回滚 对应 自生成_回滚数、生成失败 对应 自生成_失败数、已学协议 对应 做 计数 用 已学协议列表、协议未知遭遇 对应 协议_未知遭遇数、协议自动识别 对应 协议_自动识别数、缺陷总数 对应 做 计数 用 缺陷列表、缺陷未修复 对应 未修复缺陷、自动补丁数 对应 自动补丁_总数、补丁回滚 对应 自动补丁_回滚数。
完。

能力_重置统计 接受 零参：
    自生成_模块数 为 0。
    自生成_回滚数 为 0。
    自生成_失败数 为 0。
    协议_未知遭遇数 为 0。
    协议_自动识别数 为 0。
    自动补丁_总数 为 0。
    自动补丁_回滚数 为 0。
完。

能力_打印统计 接受 零参：
    s 为 做 能力_统计。
    做 打印行 用 「=== 自演化能力统计 ===」。
    做 打印行 用 「需求总数: 」 加 做 整数转字符串 用 s 的 需求总数。
    做 打印行 用 「已满足: 」 加 做 整数转字符串 用 s 的 已满足。
    做 打印行 用 「未满足: 」 加 做 整数转字符串 用 s 的 未满足。
    做 打印行 用 「核心级: 」 加 做 整数转字符串 用 s 的 核心级能力。
    做 打印行 用 「自生成: 」 加 做 整数转字符串 用 s 的 自生成模块。
    做 打印行 用 「生成回滚: 」 加 做 整数转字符串 用 s 的 生成回滚。
    做 打印行 用 「已学协议: 」 加 做 整数转字符串 用 s 的 已学协议。
    做 打印行 用 「缺陷未修复: 」 加 做 整数转字符串 用 s 的 缺陷未修复。
    做 打印行 用 「自动补丁: 」 加 做 整数转字符串 用 s 的 自动补丁数。
完。

能力_健康度 接受 零参：
    s 为 做 能力_统计。
    若 s 的 需求总数 等于 0：
        返还 1000。
    完。
    满足率 为 s 的 已满足 乘 1000 除以 s 的 需求总数。
    缺陷扣分 为 s 的 缺陷未修复 乘 50。
    分数 为 满足率 减 缺陷扣分。
    若 分数 小于 0：
        分数 为 0。
    完。
    返还 分数。
完。

能力_获取满足率 接受 零参：
    s 为 做 能力_统计。
    若 s 的 需求总数 等于 0：
        返还 100。
    完。
    返还 s 的 已满足 乘 100 除以 s 的 需求总数。
完。

能力_获取最紧急缺陷 接受 零参：
    可变 最急 为 空。
    可变 最高级 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 缺陷列表：
        若 非 缺陷列表 的 第 i 项 的 已修复 并且 缺陷列表 的 第 i 项 的 优先级 大于 最高级：
            最高级 为 缺陷列表 的 第 i 项 的 优先级。
            最急 为 缺陷列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最急。
完。

能力_获取未满足核心需求 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力需求列表：
        若 非 能力需求列表 的 第 i 项 的 已满足 并且 能力需求列表 的 第 i 项 的 成熟度 等于 成熟度_核心：
            做 追加 用 结果、能力需求列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

能力_获取自生成数 接受 零参：
    返还 自生成_模块数。
完。

能力_获取补丁数 接受 零参：
    返还 自动补丁_总数。
完。

本章 收束。
