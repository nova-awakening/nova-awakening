《调度统计追踪》

※ NovaOS 调度统计与追踪系统 - 生产级实现
※ 提供详细的调度性能分析、延迟追踪、任务统计

引用《.·类型定义》。
引用《.·同步原语》。
引用《..·执行·内核智能》。

引用《.·内存物理》。
引用《.·进程任务》。
引用《.·进程调度》。
引用《.·时钟中枢》。
引用《.·数学工具》。
引用《.·同步原语》。
引用《.·字符串替换》。

※ 调度事件类型
枚举 调度事件类型 ：
    事件_任务唤醒 为 0,。
    事件_任务睡眠 为 1,。
    事件_任务切换 为 2,。
    事件_任务迁移 为 3,。
    事件_任务入队 为 4,。
    事件_任务出队 为 5,。
    事件_负载均衡 为 6,。
    事件_时钟滴答 为 7,。
完。

※ 调度事件记录
调度事件 是 一种 事物：
    时间戳。
    事件类型。
    CPU。
    任务PID。
    任务优先级。

    ※ 事件特定数据
    联合 ：
        切换数据: 结构体 ：
            前一个PID。
            下一个PID。
            延迟纳秒。
        },。
        迁移数据: 结构体 ：
            源CPU。
            目标CPU。
        },。
        负载数据: 结构体 ：
            源负载。
            目标负载。
            迁移任务数。
        },。
    },。
完。

※ 环形缓冲区用于事件追踪
事件缓冲区大小 为 65536

事件追踪器 是 一种 事物：
    锁。

    ※ 环形缓冲区
    事件缓冲区。
    写入位置。

    ※ 启用标志
    启用。

    ※ 事件计数器
    事件计数: [8]原子64位,

    ※ 丢失事件计数
    丢失事件。
完。

变量 全局事件追踪器: 事件追踪器。

※ 每任务调度统计
任务调度统计 是 一种 事物：
    ※ 运行时间统计
    总运行时间。
    总等待时间。
    总睡眠时间。

    ※ 调度次数
    被调度次数。
    主动让出次数。
    被抢占次数。

    ※ 唤醒统计
    唤醒次数。
    睡眠次数。

    ※ 迁移统计
    迁移次数。

    ※ 延迟统计
    最大调度延迟。
    平均调度延迟。

    ※ 时间戳
    上次运行时间。
    上次等待时间。
    上次睡眠时间。
完。

※ 每CPU调度统计
CPU调度统计 是 一种 事物：
    ※ 调度次数
    总调度次数。
    空闲调度次数。

    ※ 上下文切换
    上下文切换次数。

    ※ 负载均衡
    负载均衡次数。
    任务迁入数。
    任务迁出数。

    ※ 时钟滴答
    时钟滴答次数。

    ※ 各调度类统计
    Nova公平调度次数。
    实时调度次数。
    Deadline调度次数。

    ※ CPU时间分布
    用户态时间。
    内核态时间。
    空闲时间。
    中断时间。
    软中断时间。
完。

※ 全局调度统计
全局调度统计 是 一种 事物：
    CPU统计。
    ※ 系统级统计
    系统启动时间。
    总运行任务数。
    总创建任务数。
    总销毁任务数。
完。

变量 全局统计: 全局调度统计。

※ 初始化调度统计系统
初始化调度统计 接受 )：
    做 初始化自旋锁 用 &全局事件追踪器.锁。
    做 原子写入32位 用 &全局事件追踪器.启用、0。
    做 原子写入64位 用 &全局事件追踪器.写入位置、0。

    全局统计.系统启动时间 为 做 获取纳秒时间戳。

    做 打印 用 「调度统计系统初始化完成\n」。
完。

※ 启用事件追踪
启用事件追踪 接受 )：
    做 原子写入32位 用 &全局事件追踪器.启用、1。
    做 打印 用 「调度事件追踪已启用\n」。
完。

※ 禁用事件追踪
禁用事件追踪 接受 )：
    做 原子写入32位 用 &全局事件追踪器.启用、0。
    做 打印 用 「调度事件追踪已禁用\n」。
完。

※ 记录调度事件
记录调度事件 接受 事件)：
    若 做 原子读取32位 用 &全局事件追踪器.启用 等于 0：
        返还。
    完。

    ※ 获取写入位置
    位置 为 做 原子增加64位 用 &全局事件追踪器.写入位置、1。
    索引 为 位置 位与 (事件缓冲区大小 减 1)。

    ※ 写入事件
    事件.时间戳 为 做 获取纳秒时间戳。
    全局事件追踪器.事件缓冲区[索引] 为 *事件。

    ※ 更新计数器
    做 原子增加64位 用 &全局事件追踪器.事件计数[事件.事件类型]、1。
完。

※ 记录任务唤醒事件
追踪任务唤醒 接受 任务、CPU)：
    事件 为 调度事件：
        事件类型。
        CPU。
        任务PID: 任务.PID,。
        任务优先级: 任务.优先级,。
    完。
    做 记录调度事件 用 &事件。

    ※ 更新任务统计
    任务.调度统计.唤醒次数++。
完。

※ 记录任务睡眠事件
追踪任务睡眠 接受 任务、CPU)：
    事件 为 调度事件：
        事件类型。
        CPU。
        任务PID: 任务.PID,。
        任务优先级: 任务.优先级,。
    完。
    做 记录调度事件 用 &事件。

    ※ 更新任务统计
    任务.调度统计.睡眠次数++。
    任务.调度统计.上次睡眠时间 为 做 获取纳秒时间戳。
完。

※ 记录任务切换事件
追踪任务切换 接受 前一个、下一个、CPU、延迟)：
    事件 为 调度事件：
        事件类型。
        CPU。
        任务PID: 下一个.PID,。
        任务优先级: 下一个.优先级,。
    完。
    事件.切换数据.前一个PID 为 前一个.PID。
    事件.切换数据.下一个PID 为 下一个.PID。
    事件.切换数据.延迟纳秒 为 延迟。

    做 记录调度事件 用 &事件。

    ※ 更新任务统计
    下一个.调度统计.被调度次数++。
    下一个.调度统计.上次运行时间 为 做 获取纳秒时间戳。

    ※ 更新延迟统计
    若 延迟 大于 下一个.调度统计.最大调度延迟：
        下一个.调度统计.最大调度延迟 为 延迟。
    完。
    旧平均 为 下一个.调度统计.平均调度延迟。
    下一个.调度统计.平均调度延迟 为 (旧平均 乘 7 加 延迟) 除以 8。

    ※ 更新CPU统计
    全局统计.CPU统计[CPU].上下文切换次数++。
完。

※ 记录任务迁移事件
追踪任务迁移 接受 任务、源CPU、目标CPU)：
    事件 为 调度事件：
        事件类型。
        CPU。
        任务PID: 任务.PID,。
        任务优先级: 任务.优先级,。
    完。
    事件.迁移数据.源CPU 为 源CPU。
    事件.迁移数据.目标CPU 为 目标CPU。

    做 记录调度事件 用 &事件。

    ※ 更新任务统计
    任务.调度统计.迁移次数++。

    ※ 更新CPU统计
    全局统计.CPU统计[源CPU].任务迁出数++。
    全局统计.CPU统计[目标CPU].任务迁入数++。
完。

※ 记录负载均衡事件
追踪负载均衡 接受 源CPU、目标CPU、源负载、目标负载、迁移数)：
    事件 为 调度事件：
        事件类型。
        CPU。
        任务PID。
        任务优先级。
    完。
    事件.负载数据.源负载 为 源负载。
    事件.负载数据.目标负载 为 目标负载。
    事件.负载数据.迁移任务数 为 迁移数。

    做 记录调度事件 用 &事件。

    ※ 更新CPU统计
    全局统计.CPU统计[源CPU].负载均衡次数++。
完。

※ 更新CPU时间统计
更新CPU时间统计 接受 CPU、用户态、内核态)：
    统计 为 &全局统计.CPU统计[CPU]。
    统计.用户态时间 累加 用户态。
    统计.内核态时间 累加 内核态。
完。

※ 获取任务调度统计
获取任务统计 接受 任务)：
    返还 任务.调度统计。
完。

※ 获取CPU调度统计
获取CPU统计 接受 CPU)：
    若 CPU 小于 0 或 CPU 不小于 最大CPU数：
        返还 CPU调度统计{}。
    完。
    返还 全局统计.CPU统计[CPU]。
完。

※ 获取系统调度统计摘要
获取系统统计摘要 接受 )：
    摘要 为 系统统计摘要：
        系统运行时间: 做 获取纳秒时间戳 减 全局统计.系统启动时间,。
        总运行任务数: 做 原子读取64位 用 &全局统计.总运行任务数,。
        总创建任务数: 做 原子读取64位 用 &全局统计.总创建任务数,。
        总销毁任务数: 做 原子读取64位 用 &全局统计.总销毁任务数,。
    完。

    ※ 汇总所有CPU统计
    对于 i 为 0; i 小于 全局调度器.CPU数量; i++ ：
        CPU统计 为 &全局统计.CPU统计[i]。
        摘要.总调度次数 累加 CPU统计.总调度次数。
        摘要.总上下文切换 累加 CPU统计.上下文切换次数。
        摘要.总负载均衡次数 累加 CPU统计.负载均衡次数。
        摘要.总用户态时间 累加 CPU统计.用户态时间。
        摘要.总内核态时间 累加 CPU统计.内核态时间。
        摘要.总空闲时间 累加 CPU统计.空闲时间。
    完。

    ※ 计算CPU利用率
    总时间 为 摘要.总用户态时间 加 摘要.总内核态时间 加 摘要.总空闲时间。
    若 总时间 大于 0：
        摘要.CPU利用率 为 ((总时间 减 摘要.总空闲时间) 乘 100) 除以 总时间。
    完。

    返还 摘要。
完。

系统统计摘要 是 一种 事物：
    系统运行时间。
    总运行任务数。
    总创建任务数。
    总销毁任务数。

    总调度次数。
    总上下文切换。
    总负载均衡次数。

    总用户态时间。
    总内核态时间。
    总空闲时间。

    CPU利用率。 ※ 百分比。
完。

※ 导出事件追踪数据
导出事件追踪 接受 缓冲区、大小)：
    若 缓冲区 等于 空 或 大小 不大于 0：
        返还 -错误码_无效参数。
    完。

    写入位置 为 做 原子读取64位 用 &全局事件追踪器.写入位置。

    ※ 计算可读取的事件数量
    事件数 为 写入位置。
    若 事件数 大于 事件缓冲区大小：
        事件数 为 事件缓冲区大小。
    完。

    ※ 计算需要的缓冲区大小
    需要大小 为 事件数 乘 做 大小为 用 调度事件。
    若 需要大小 大于 大小：
        返还 -错误码_缓冲区太小。
    完。

    ※ 复制事件数据
    起始索引 为 (写入位置 减 事件数) 位与 (事件缓冲区大小 减 1)。

    对于 i 为 做 无符号长整数 用 0; i 小于 事件数; i++ ：
        索引 为 (起始索引 加 i) 位与 (事件缓冲区大小 减 1)。
        事件 为 &全局事件追踪器.事件缓冲区[索引]。

        ※ 复制到输出缓冲区
        目标 为 缓冲区 加 (i 乘 做 大小为 用 调度事件)。
        做 内存复制 用 目标、事件、做 大小为 用 调度事件。
    完。

    返还 事件数。
完。

※ 清空事件追踪缓冲区
清空事件追踪 接受 )：
    做 原子写入64位 用 &全局事件追踪器.写入位置、0。

    对于 i 为 0; i 小于 8; i++ ：
        做 原子写入64位 用 &全局事件追踪器.事件计数[i]、0。
    完。

    做 原子写入64位 用 &全局事件追踪器.丢失事件、0。
完。

※ 打印调度统计报告
打印调度统计报告 接受 )：
    摘要 为 做 获取系统统计摘要。

    做 打印 用 「\n========== NovaOS 调度统计报告 ==========\n」。
    做 打印 用 「系统运行时间: %llu 纳秒\n」、摘要.系统运行时间。
    做 打印 用 「CPU利用率: %llu%%\n」、摘要.CPU利用率。
    做 打印 用 「\n任务统计:\n」。
    做 打印 用 「  运行中任务: %llu\n」、摘要.总运行任务数。
    做 打印 用 「  已创建任务: %llu\n」、摘要.总创建任务数。
    做 打印 用 「  已销毁任务: %llu\n」、摘要.总销毁任务数。
    做 打印 用 「\n调度统计:\n」。
    做 打印 用 「  总调度次数: %llu\n」、摘要.总调度次数。
    做 打印 用 「  总上下文切换: %llu\n」、摘要.总上下文切换。
    做 打印 用 「  总负载均衡: %llu\n」、摘要.总负载均衡次数。
    做 打印 用 「\nCPU时间分布:\n」。
    做 打印 用 「  用户态: %llu 纳秒\n」、摘要.总用户态时间。
    做 打印 用 「  内核态: %llu 纳秒\n」、摘要.总内核态时间。
    做 打印 用 「  空闲: %llu 纳秒\n」、摘要.总空闲时间。

    做 打印 用 「\n每CPU统计:\n」。
    对于 i 为 0; i 小于 全局调度器.CPU数量; i++ ：
        CPU统计 为 做 获取CPU统计 用 i。
        做 打印 用 「  CPU %d:\n」、i。
        做 打印 用 「    调度次数: %llu\n」、CPU统计.总调度次数。
        做 打印 用 「    上下文切换: %llu\n」、CPU统计.上下文切换次数。
        做 打印 用 「    负载均衡: %llu\n」、CPU统计.负载均衡次数。
        做 打印 用 「    迁入/迁出: %llu 除以 %llu\n」、CPU统计.任务迁入数、CPU统计.任务迁出数。
    完。

    做 打印 用 「==========================================\n\n」。
完。

※ §7 延迟直方图

延迟桶数 为 16。

延迟直方图结构 是 一种 事物：
    桶: [16]原子64位,
    总采样数。
完。

变量 全局延迟直方图: 延迟直方图结构。

记录调度延迟 接受 延迟纳秒)：
    微秒 为 延迟纳秒 除以 1000。
    若 微秒 等于 0：

    ※ 确定桶索引: log2(微秒)
    桶索引 为 0。
    临时 为 微秒。
    只要 临时 大于 1 且 桶索引 小于 延迟桶数 减 1：
        临时 为 临时 除以 2。
        桶索引++。
    完。

    做 原子增加64位 用 &全局延迟直方图.桶[桶索引]、1。
    做 原子增加64位 用 &全局延迟直方图.总采样数、1。
完。

※ 获取P50/P90/P99尾延迟
获取尾延迟 接受 百分位)：
    总数 为 做 原子读取64位 用 &全局延迟直方图.总采样数。
    若 总数 等于 0：

    目标 为 总数 乘 百分位 除以 100。
    累计 为 做 无符号长整数 用 0。

    对于 i 为 0; i 小于 延迟桶数; i++ ：
        累计 累加 做 原子读取64位 用 &全局延迟直方图.桶[i]。
        若 累计 不小于 目标：
            ※ 返回该桶的上界 (2^i 微秒)
            返还 1 左移 i。
        完。
    完。
    返还 1 左移 (延迟桶数 减 1)。
完。

※ §8 调度器热点检测

热点记录 是 一种 事物：
    任务PID。
    调度次数。
    累计运行时间。
完。

最大热点任务数 为 32。
变量 热点表: [32]热点记录。
变量 热点表长度: 整数。

更新热点统计 接受 任务PID、运行时间)：
    ※ 查找或创建热点记录
    对于 i 为 0; i 小于 热点表长度; i++ ：
        若 热点表[i].任务PID 等于 任务PID：
            热点表[i].调度次数++。
            热点表[i].累计运行时间 累加 运行时间。
            返还。
        完。
    完。

    ※ 新任务: 尝试加入热点表
    若 热点表长度 小于 最大热点任务数：
        热点表[热点表长度] 为 热点记录：
            任务PID。
            调度次数。
            累计运行时间。
        完。
        热点表长度++。
    完。
完。

获取最热任务 接受 )：
    若 热点表长度 等于 0：

    最大索引 为 0。
    最大时间 为 热点表[0].累计运行时间。

    对于 i 为 1; i 小于 热点表长度; i++ ：
        若 热点表[i].累计运行时间 大于 最大时间：
            最大时间 为 热点表[i].累计运行时间。
            最大索引 为 i。
        完。
    完。

    返还 热点表[最大索引].任务PID。
完。

※ §9 CPU负载不均衡度分析

计算CPU负载不均衡度 接受 )：
    若 全局调度器.CPU数量 不大于 1：

    ※ 计算各CPU负载的变异系数
    总负载 为 做 无符号长整数 用 0。
    对于 i 为 0; i 小于 全局调度器.CPU数量; i++ ：
        总负载 累加 全局统计.CPU统计[i].总调度次数。
    完。
    平均负载 为 总负载 除以 做 无符号长整数 用 全局调度器.CPU数量。

    方差和 为 做 无符号长整数 用 0。
    对于 i 为 0; i 小于 全局调度器.CPU数量; i++ ：
        差值 为 全局统计.CPU统计[i].总调度次数。
        若 差值 大于 平均负载：
            偏差 为 差值 减 平均负载。
            方差和 累加 偏差 乘 偏差。
        否则：
            偏差 为 平均负载 减 差值。
            方差和 累加 偏差 乘 偏差。
        完。
    完。

    ※ 返回变异系数 (标准差/平均值 * 100)
    若 平均负载 等于 0：
    返还 做 整数开方 用 方差和 除以 做 无符号长整数 用 全局调度器.CPU数量 乘 100 除以 平均负载。
完。

打印延迟直方图 接受 )：
    做 打印 用 「\n调度延迟直方图:\n」。
    对于 i 为 0; i 小于 延迟桶数; i++ ：
        计数 为 做 原子读取64位 用 &全局延迟直方图.桶[i]。
        若 计数 大于 0：
            做 打印 用 「  <%d us: %llu\n」、1 左移 i、计数。
        完。
    完。
    做 打印 用 「  P50: %llu us\n」、做 获取尾延迟 用 50。
    做 打印 用 「  P90: %llu us\n」、做 获取尾延迟 用 90。
    做 打印 用 「  P99: %llu us\n」、做 获取尾延迟 用 99。
    做 打印 用 「  负载不均衡度: %llu%%\n」、做 计算CPU负载不均衡度。
完。

※ NSTS — Nova Scheduling Tracing & Statistics Theory
※ 公理1(无损追踪): 环形缓冲区保证最新事件不丢失
※ 公理2(原子一致): 所有计数器原子更新, 无撕裂读写
※ 公理3(低开销): 追踪开销 < 调度周期的1%
※ 定理1(尾延迟可观): P99延迟可通过直方图精确估算
※ 定理2(热点收敛): 热点检测在O(1)时间内更新

※ NSST — Nova Scheduling Statistics Theory
※ 公理1(事件完备): 所有调度决策点均生成事件, 无盲区
※ 公理2(时间守恒): 用户态+内核态+空闲 = 总运行时间
※ 公理3(环形缓冲无锁): 事件缓冲区无锁写入, 不影响调度延迟
※ 定理1(利用率可观测): CPU利用率可由时间比例精确计算
※ 定理2(追踪可导出): 任意时刻可导出事件缓冲区快照

统计_筛选事件 接受 类型、缓冲区、大小)：
    写入位置 为 做 原子读取64位 用 &全局事件追踪器.写入位置。
    事件数 为 写入位置。
    若 事件数 大于 事件缓冲区大小：

    可变 匹配数 = 做 无符号长整数 用 0。
    起始 为 (写入位置 减 事件数) 位与 (事件缓冲区大小 减 1)。

    对于 i 为 做 无符号长整数 用 0; i 小于 事件数 且 匹配数 小于 大小; i++ ：
        索引 为 (起始 加 i) 位与 (事件缓冲区大小 减 1)。
        事件 为 &全局事件追踪器.事件缓冲区[索引]。
        若 事件.类型 等于 类型：
            目标 为 缓冲区 加 (匹配数 乘 做 大小为 用 调度事件)。
            做 内存复制 用 目标、事件、做 大小为 用 调度事件。
            匹配数++。
        完。
    完。
    返还 匹配数。
完。

统计_打印事件计数 接受 )：
    做 打印 用 「=== 事件类型统计 ===\n」。
    对于 i 为 0; i 小于 8; i++ ：
        计数 为 做 原子读取64位 用 &全局事件追踪器.事件计数[i]。
        若 计数 大于 0：
            做 打印 用 「类型%d: %llu\n」、i、计数。
        完。
    完。
    做 打印 用 「丢失事件: %llu\n」、做 原子读取64位 用 &全局事件追踪器.丢失事件。
完。

统计_重置全部 接受 )：
    做 清空事件追踪。
    做 原子写入64位 用 &全局统计.总运行任务数、0。
    做 原子写入64位 用 &全局统计.总创建任务数、0。
    做 原子写入64位 用 &全局统计.总销毁任务数、0。
    对于 i 为 0; i 小于 全局调度器.CPU数量; i++ ：
        全局统计.CPU统计[i] 为 CPU调度统计{}。
    完。
完。

统计_计算CPU利用率 接受 cpu_id)：
    若 cpu_id 小于 0 或者 cpu_id 不小于 全局调度器.CPU数量：
    s 为 全局统计.CPU统计[cpu_id]。
    总时间 为 s.用户态时间 加 s.内核态时间 加 s.空闲时间。
    若 总时间 等于 0：
    返还 (s.用户态时间 加 s.内核态时间) 乘 1000 除以 总时间。
完。

统计_打印CPU利用率 接受 )：
    做 打印 用 「=== 做 CPU利用率 用 FP1000 ===\n」。
    对于 i 为 0; i 小于 全局调度器.CPU数量; i++ ：
        率 为 做 统计_计算CPU利用率 用 i。
        做 打印 用 「  CPU%d: %d.%d%%\n」、i、率 除以 10、率 取余于 10。
    完。
完。

统计_导出快照 接受 缓冲区、大小)：
    写入位置 为 做 原子读取64位 用 &全局事件追踪器.写入位置。
    实际数 为 写入位置。
    若 实际数 大于 事件缓冲区大小：
    若 实际数 大于 大小：
    起始 为 (写入位置 减 实际数) 位与 (事件缓冲区大小 减 1)。
    对于 i 为 做 无符号长整数 用 0; i 小于 实际数; i++ ：
        索引 为 (起始 加 i) 位与 (事件缓冲区大小 减 1)。
        目标 为 缓冲区 加 (i 乘 做 大小为 用 调度事件)。
        做 内存复制 用 目标、&全局事件追踪器.事件缓冲区[索引]、做 大小为 用 调度事件。
    完。
    返还 实际数。
完。

可变 AI_调度统计追_负载窗口 为 []。

AI_调度统计追_负载预测 接受 当前负载)：
    做 追加 用 AI_调度统计追_负载窗口、当前负载。
    若 做 计数 用 AI_调度统计追_负载窗口 大于 20：
    若 做 计数 用 AI_调度统计追_负载窗口 小于 3：
    n 为 做 计数 用 AI_调度统计追_负载窗口。
    趋势 为 AI_调度统计追_负载窗口[n-1] 减 AI_调度统计追_负载窗口[n-3]。
    返还 当前负载 加 趋势。
完。

AI_调度统计追_迁移建议 接受 源CPU负载、目标CPU负载)：
    若 源CPU负载 大于 目标CPU负载 乘 2：
    返还 假。
完。

本章 收束。
