《日志中枢》

引用《.·类型定义》。

引用《.·错误码》。

日志档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、级别、信息数、告警数、错误数、调试数、追踪数、恐慌数、订阅数、刷新数、导出数、成功数、失败数、最近级别、最近文本、日志表、订阅表。

日志请求 是 一种 事物，含 动作、目标、参数、选项。

日志回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

日志摘要 是 一种 事物，含 名称、级别、信息数、告警数、错误数、调试数、追踪数、恐慌数、订阅数、刷新数、导出数、成功数、失败数、健康。

日志项 是 一种 事物，含 级别、文本、来源、标签、时间。

日志级别_信息 为 「信息」。

日志级别_告警 为 「告警」。

日志级别_错误 为 「错误」。

日志级别_调试 为 「调试」。

日志级别_追踪 为 「追踪」。

日志级别_恐慌 为 「恐慌」。

可变 日志账本 为 一组：（空）。

可变 日志构建数 为 0。

可变 日志总动作 为 0。

可变 日志总成功 为 0。

可变 日志总失败 为 0。

日志中枢_建档 接受 名称：
    档 为 新建 日志档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 级别 为 日志级别_信息。
    档 的 信息数 为 0。
    档 的 告警数 为 0。
    档 的 错误数 为 0。
    档 的 调试数 为 0。
    档 的 追踪数 为 0。
    档 的 恐慌数 为 0。
    档 的 订阅数 为 0。
    档 的 刷新数 为 0。
    档 的 导出数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    档 的 最近级别 为 「」。
    档 的 最近文本 为 「」。
    档 的 日志表 为 一组：（空）。
    档 的 订阅表 为 一组：（空）。
    返还 档。
完。

日志中枢_登记 接受 档：
    做 追加 用 日志账本、档。
    日志构建数 为 日志构建数 加 1。
    返还 档。
完。

日志中枢_完整统计 接受 零参：
    返还 一组：日志构建数、日志总动作、日志总成功、日志总失败。
完。

日志中枢_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 日志回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

日志中枢_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    日志总动作 为 日志总动作 加 1。
完。

日志中枢_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 恐慌数 大于 0：
        档 的 最近健康 为 「恐慌」。
        返还 档 的 最近健康。
    完。
    若 档 的 错误数 大于 档 的 信息数 加 档 的 告警数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 错误数 大于 0 或者 档 的 告警数 大于 0：
        档 的 最近健康 为 「监视」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「运行」。
    返还 档 的 最近健康。
完。

日志中枢_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 档 的 名称 等于 「」：
        返还 假。
    完。
    返还 真。
完。

日志中枢_成功 接受 档、来源、数据：
    档 的 最近结果 为 数据。
    档 的 成功数 为 档 的 成功数 加 1。
    日志总成功 为 日志总成功 加 1。
    档 的 最近统计 为 做 日志中枢_完整统计。
    做 日志中枢_刷新健康 用 档。
    返还 做 日志中枢_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

日志中枢_失败 接受 档、来源、错因：
    若 档 不等于 空：
        档 的 最近错误 为 错因。
        档 的 失败数 为 档 的 失败数 加 1。
        档 的 最近统计 为 做 日志中枢_完整统计。
        做 日志中枢_刷新健康 用 档。
    完。
    日志总失败 为 日志总失败 加 1。
    返还 做 日志中枢_建回证 用 真、假、来源、错因、空、做 日志中枢_完整统计。
完。

日志中枢_时间戳 接受 零参：
    返还 「稳定时钟」。
完。

日志中枢_建项 接受 级别、文本、来源、标签：
    项 为 新建 日志项。
    项 的 级别 为 级别。
    项 的 文本 为 文本。
    项 的 来源 为 来源。
    项 的 标签 为 标签。
    项 的 时间 为 做 日志中枢_时间戳。
    返还 项。
完。

日志中枢_推送 接受 档、级别、文本、来源、标签：
    若 非 做 日志中枢_检查档 用 档：
        返还 做 日志中枢_失败 用 档、「推送」、「日志档不可用」。
    完。
    做 日志中枢_记录 用 档、「推送」、一组：级别、文本、来源、标签、「日志中枢」。
    项 为 做 日志中枢_建项 用 级别、文本、来源、标签。
    做 追加 用 档 的 日志表、项。
    档 的 最近级别 为 级别。
    档 的 最近文本 为 文本。
    若 级别 等于 日志级别_信息：
        档 的 信息数 为 档 的 信息数 加 1。
    完。
    若 级别 等于 日志级别_告警：
        档 的 告警数 为 档 的 告警数 加 1。
    完。
    若 级别 等于 日志级别_错误：
        档 的 错误数 为 档 的 错误数 加 1。
    完。
    若 级别 等于 日志级别_调试：
        档 的 调试数 为 档 的 调试数 加 1。
    完。
    若 级别 等于 日志级别_追踪：
        档 的 追踪数 为 档 的 追踪数 加 1。
    完。
    若 级别 等于 日志级别_恐慌：
        档 的 恐慌数 为 档 的 恐慌数 加 1。
    完。
    返还 做 日志中枢_成功 用 档、「推送」、项。
完。

日志中枢_设级别 接受 档、级别：
    若 非 做 日志中枢_检查档 用 档：
        返还 做 日志中枢_失败 用 档、「设级别」、「日志档不可用」。
    完。
    做 日志中枢_记录 用 档、「设级别」、一组：级别、「日志中枢」。
    档 的 级别 为 级别。
    档 的 最近级别 为 级别。
    返还 做 日志中枢_成功 用 档、「设级别」、级别。
完。

日志中枢_订阅 接受 档、名称、级别：
    若 非 做 日志中枢_检查档 用 档：
        返还 做 日志中枢_失败 用 档、「订阅」、「日志档不可用」。
    完。
    做 日志中枢_记录 用 档、「订阅」、一组：名称、级别、「日志中枢」。
    做 追加 用 档 的 订阅表、一组：名称、级别。
    档 的 订阅数 为 档 的 订阅数 加 1。
    返还 做 日志中枢_成功 用 档、「订阅」、一组：名称、级别。
完。

日志中枢_刷新 接受 档：
    若 非 做 日志中枢_检查档 用 档：
        返还 做 日志中枢_失败 用 档、「刷新」、「日志档不可用」。
    完。
    做 日志中枢_记录 用 档、「刷新」、一组：（空）、「日志中枢」。
    档 的 刷新数 为 档 的 刷新数 加 1。
    返还 做 日志中枢_成功 用 档、「刷新」、一组：做 计数 用 档 的 日志表、做 计数 用 档 的 订阅表。
完。

日志中枢_导出 接受 档、数量：
    若 非 做 日志中枢_检查档 用 档：
        返还 做 日志中枢_失败 用 档、「导出」、「日志档不可用」。
    完。
    做 日志中枢_记录 用 档、「导出」、一组：数量、「日志中枢」。
    档 的 导出数 为 档 的 导出数 加 1。
    若 数量 小于或等于 0 或者 数量 大于或等于 做 计数 用 档 的 日志表：
        返还 做 日志中枢_成功 用 档、「导出」、档 的 日志表。
    完。
    起 为 做 计数 用 档 的 日志表 减 数量。
    结果 为 一组：（空）。
    只要 起 小于 做 计数 用 档 的 日志表：
        做 追加 用 结果、档 的 日志表 的 第 起 项。
        起 为 起 加 1。
    完。
    返还 做 日志中枢_成功 用 档、「导出」、结果。
完。

日志中枢_信息 接受 档、文本、来源、标签：
    返还 做 日志中枢_推送 用 档、日志级别_信息、文本、来源、标签。
完。

日志中枢_告警 接受 档、文本、来源、标签：
    返还 做 日志中枢_推送 用 档、日志级别_告警、文本、来源、标签。
完。

日志中枢_错误 接受 档、文本、来源、标签：
    返还 做 日志中枢_推送 用 档、日志级别_错误、文本、来源、标签。
完。

日志中枢_调试 接受 档、文本、来源、标签：
    返还 做 日志中枢_推送 用 档、日志级别_调试、文本、来源、标签。
完。

日志中枢_追踪 接受 档、文本、来源、标签：
    返还 做 日志中枢_推送 用 档、日志级别_追踪、文本、来源、标签。
完。

日志中枢_恐慌 接受 档、文本、来源、标签：
    返还 做 日志中枢_推送 用 档、日志级别_恐慌、文本、来源、标签。
完。

日志中枢_最后项 接受 档：
    若 做 计数 用 档 的 日志表 等于 0：
        返还 空。
    完。
    返还 档 的 日志表 的 第 做 计数 用 档 的 日志表 减 1 项。
完。

日志中枢_查级别 接受 档、级别：
    若 非 做 日志中枢_检查档 用 档：
        返还 做 日志中枢_失败 用 档、「查级别」、「日志档不可用」。
    完。
    做 日志中枢_记录 用 档、「查级别」、一组：级别、「日志中枢」。
    结果 为 一组：（空）。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 日志表：
        项 为 档 的 日志表 的 第 索引 项。
        若 项 的 级别 等于 级别：
            做 追加 用 结果、项。
        完。
        索引 为 索引 加 1。
    完。
    返还 做 日志中枢_成功 用 档、「查级别」、结果。
完。

日志中枢_查标签 接受 档、标签：
    若 非 做 日志中枢_检查档 用 档：
        返还 做 日志中枢_失败 用 档、「查标签」、「日志档不可用」。
    完。
    做 日志中枢_记录 用 档、「查标签」、一组：标签、「日志中枢」。
    结果 为 一组：（空）。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 日志表：
        项 为 档 的 日志表 的 第 索引 项。
        若 项 的 标签 等于 标签：
            做 追加 用 结果、项。
        完。
        索引 为 索引 加 1。
    完。
    返还 做 日志中枢_成功 用 档、「查标签」、结果。
完。

日志中枢_摘要 接受 档：
    摘要 为 新建 日志摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 级别 为 档 的 级别。
    摘要 的 信息数 为 档 的 信息数。
    摘要 的 告警数 为 档 的 告警数。
    摘要 的 错误数 为 档 的 错误数。
    摘要 的 调试数 为 档 的 调试数。
    摘要 的 追踪数 为 档 的 追踪数。
    摘要 的 恐慌数 为 档 的 恐慌数。
    摘要 的 订阅数 为 档 的 订阅数。
    摘要 的 刷新数 为 档 的 刷新数。
    摘要 的 导出数 为 档 的 导出数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 日志中枢_刷新健康 用 档。
    返还 摘要。
完。

日志中枢_初始化 接受 档：
    若 非 做 日志中枢_检查档 用 档：
        返还 做 日志中枢_失败 用 档、「初始化」、「日志档不可用」。
    完。
    做 日志中枢_记录 用 档、「初始化」、一组：（空）、「日志中枢」。
    档 的 日志表 为 一组：（空）。
    档 的 订阅表 为 一组：（空）。
    档 的 信息数 为 0。
    档 的 告警数 为 0。
    档 的 错误数 为 0。
    档 的 调试数 为 0。
    档 的 追踪数 为 0。
    档 的 恐慌数 为 0。
    档 的 订阅数 为 0。
    档 的 刷新数 为 0。
    档 的 导出数 为 0。
    档 的 是否初始化 为 真。
    做 日志中枢_信息 用 档、「日志中枢已启动」、「日志中枢」、「启动」。
    做 日志中枢_订阅 用 档、「主循环」、日志级别_信息。
    做 日志中枢_订阅 用 档、「安全域」、日志级别_告警。
    做 日志中枢_订阅 用 档、「故障域」、日志级别_错误。
    返还 做 日志中枢_成功 用 档、「初始化」、「已初始化」。
完。

日志中枢_自检 接受 档：
    若 非 做 日志中枢_检查档 用 档：
        返还 假。
    完。
    若 做 日志中枢_初始化 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 日志中枢_调试 用 档、「白璃外衣开始刷新」、「界面」、「白璃」 的 已成 等于 假：
        返还 假。
    完。
    若 做 日志中枢_追踪 用 档、「控制协议已接入」、「控核」、「协议」 的 已成 等于 假：
        返还 假。
    完。
    若 做 日志中枢_告警 用 档、「检测到旧路径导入」、「导入修复」、「导入」 的 已成 等于 假：
        返还 假。
    完。
    若 做 日志中枢_错误 用 档、「断导入已转入修复队列」、「审计」、「修复」 的 已成 等于 假：
        返还 假。
    完。
    若 做 日志中枢_查级别 用 档、日志级别_错误 的 已成 等于 假：
        返还 假。
    完。
    若 做 日志中枢_查标签 用 档、「协议」 的 已成 等于 假：
        返还 假。
    完。
    若 做 日志中枢_导出 用 档、4 的 已成 等于 假：
        返还 假。
    完。
    若 做 日志中枢_刷新 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 日志中枢_恐慌 用 档、「自检恐慌路径演练」、「日志中枢」、「演练」 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

日志中枢_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 日志中枢_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        返还 做 日志中枢_初始化 用 档。
    完。
    若 请求 的 动作 等于 「设级别」：
        返还 做 日志中枢_设级别 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「信息」：
        返还 做 日志中枢_信息 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「告警」：
        返还 做 日志中枢_告警 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「错误」：
        返还 做 日志中枢_错误 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「调试」：
        返还 做 日志中枢_调试 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「追踪」：
        返还 做 日志中枢_追踪 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「恐慌」：
        返还 做 日志中枢_恐慌 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「订阅」：
        返还 做 日志中枢_订阅 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「刷新」：
        返还 做 日志中枢_刷新 用 档。
    完。
    若 请求 的 动作 等于 「导出」：
        返还 做 日志中枢_导出 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「查级别」：
        返还 做 日志中枢_查级别 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「查标签」：
        返还 做 日志中枢_查标签 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「摘要」：
        返还 做 日志中枢_成功 用 档、「摘要」、做 日志中枢_摘要 用 档。
    完。
    返还 做 日志中枢_失败 用 档、「处理」、「未知动作」。
完。

可变 AI_日志中枢_调用数 为 0。

可变 AI_日志中枢_异常数 为 0。

AI_日志中枢_监控 接受 零参：
    AI_日志中枢_调用数 为 AI_日志中枢_调用数 加 1。
    返还 AI_日志中枢_异常数 等于 0。
完。

AI_日志中枢_健康检查 接受 零参：
    若 AI_日志中枢_调用数 大于 0 并且 AI_日志中枢_异常数 乘 100 除以 AI_日志中枢_调用数 大于 10：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
