《注意力系统》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·链表后继》。

引用《.·整数转字符串》。

注意力_通道数 为 16。

注意力_焦点数 为 3。

注意力_衰减率 为 50。

注意力_新奇阈值 为 700。

注意力_疲劳阈值 为 100。

可变 显著性 为 一组：（空）。

可变 注意力焦点 为 一组：（空）。

可变 焦点持续时间 为 一组：（空）。

可变 注意力_已初始化 为 假。

可变 注意力_tick 为 0。

可变 注意力_切换次数 为 0。

注意力_初始化 接受 零参：
    显著性 为 一组：（空）。
    注意力焦点 为 一组：（空）。
    焦点持续时间 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 注意力_通道数：
        做 追加 用 显著性、0。
        i 为 i 加 1。
    完。
    注意力_已初始化 为 真。
    做 内核信息 用 「注意力系统: 」 加 做 整数转字符串 用 注意力_通道数 加 「通道 Top-」 加 做 整数转字符串 用 注意力_焦点数 加 「焦点+新奇抢占+疲劳切换」。
完。

注意力_输入 接受 通道、强度、新奇度：
    若 非 注意力_已初始化 或者 通道 大于或等于 注意力_通道数：
        返还。
    完。
    显著性 的 第 通道 项 为 强度 乘 (1000 加 新奇度) 除以 1000。
    若 新奇度 大于 注意力_新奇阈值：
        做 注意力_强制焦点 用 通道。
        注意力_切换次数 为 注意力_切换次数 加 1。
    完。
完。

注意力_tick 接受 零参：
    若 非 注意力_已初始化：
        返还。
    完。
    注意力_tick 为 注意力_tick 加 1。
    可变 i 为 0。
    只要 i 小于 注意力_通道数：
        显著性 的 第 i 项 为 显著性 的 第 i 项 减 注意力_衰减率。
        若 显著性 的 第 i 项 小于 0：
            显著性 的 第 i 项 为 0。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 焦点持续时间：
        焦点持续时间 的 第 i 项 为 焦点持续时间 的 第 i 项 加 1。
        i 为 i 加 1。
    完。
    做 注意力_更新焦点。
    i 为 0。
    只要 i 小于 做 计数 用 焦点持续时间：
        若 焦点持续时间 的 第 i 项 大于 注意力_疲劳阈值：
            若 i 小于 做 计数 用 注意力焦点：
                显著性 的 第 注意力焦点 的 第 i 项 项 为 显著性 的 第 注意力焦点 的 第 i 项 项 除以 2。
            完。
        完。
        i 为 i 加 1。
    完。
完。

注意力_更新焦点 接受 零参：
    可变 新焦点 为 一组：（空）。
    可变 新持续 为 一组：（空）。
    可变 n 为 0。
    只要 n 小于 注意力_焦点数：
        可变 最高 为 负 1。
        可变 最高值 为 0。
        可变 i 为 0。
        只要 i 小于 注意力_通道数：
            可变 已有 为 假。
            可变 j 为 0。
            只要 j 小于 做 计数 用 新焦点：
                若 新焦点 的 第 j 项 等于 i：
                    已有 为 真。
                完。
                j 为 j 加 1。
            完。
            若 非 已有 并且 显著性 的 第 i 项 大于 最高值：
                最高值 为 显著性 的 第 i 项。
                最高 为 i。
            完。
            i 为 i 加 1。
        完。
        若 最高 大于或等于 0 并且 最高值 大于 0：
            做 追加 用 新焦点、最高。
            可变 旧持续 为 0。
            可变 k 为 0。
            只要 k 小于 做 计数 用 注意力焦点：
                若 注意力焦点 的 第 k 项 等于 最高 并且 k 小于 做 计数 用 焦点持续时间：
                    旧持续 为 焦点持续时间 的 第 k 项。
                完。
                k 为 k 加 1。
            完。
            做 追加 用 新持续、旧持续。
        完。
        n 为 n 加 1。
    完。
    注意力焦点 为 新焦点。
    焦点持续时间 为 新持续。
完。

注意力_强制焦点 接受 通道：
    若 做 计数 用 注意力焦点 大于 0：
        注意力焦点 的 第 0 项 为 通道。
    否则：
        做 追加 用 注意力焦点、通道。
    完。
    若 做 计数 用 焦点持续时间 大于 0：
        焦点持续时间 的 第 0 项 为 0。
    否则：
        做 追加 用 焦点持续时间、0。
    完。
完。

注意力_是焦点 接受 通道：
    可变 i 为 0。
    只要 i 小于 做 计数 用 注意力焦点：
        若 注意力焦点 的 第 i 项 等于 通道：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

注意力_获取焦点 接受 零参：
    返还 注意力焦点。
完。

注意力_显著性 接受 通道：
    若 通道 小于 注意力_通道数：
        返还 显著性 的 第 通道 项。
    完。
    返还 0。
完。

注意力_权重 接受 零参：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 注意力_通道数：
        总和 为 总和 加 显著性 的 第 i 项。
        i 为 i 加 1。
    完。
    若 总和 等于 0：
        总和 为 1。
    完。
    可变 权重 为 一组：（空）。
    i 为 0。
    只要 i 小于 注意力_通道数：
        做 追加 用 权重、显著性 的 第 i 项 乘 1000 除以 总和。
        i 为 i 加 1。
    完。
    返还 权重。
完。

可变 ACh水平 为 500。

可变 NE水平 为 500。

注意力_设置调质 接受 ach、ne：
    ACh水平 为 ach。
    NE水平 为 ne。
完。

注意力_调制显著性 接受 通道、基础值：
    是焦点 为 做 注意力_是焦点 用 通道。
    可变 调制 为 基础值。
    若 是焦点：
        调制 为 调制 乘 (1000 加 ACh水平 除以 2) 除以 1000。
    否则：
        调制 为 调制 乘 (1000 减 ACh水平 除以 4) 除以 1000。
    完。
    调制 为 调制 乘 (1000 加 NE水平 除以 3) 除以 1000。
    若 调制 小于 0：
        调制 为 0。
    完。
    返还 调制。
完。

注意力_历史最大 为 64。

可变 注意力历史 为 一组：（空）。

注意力_记录焦点变化 接受 通道：
    做 追加 用 注意力历史、一组：注意力_tick、通道。
    若 做 计数 用 注意力历史 大于 注意力_历史最大：
        做 链表_删除 用 注意力历史、0。
    完。
完。

注意力_最近切换 接受 数量：
    n 为 做 计数 用 注意力历史。
    若 n 小于或等于 数量：
        返还 注意力历史。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 n 减 数量。
    只要 i 小于 n：
        做 追加 用 结果、注意力历史 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

注意力_统计 接受 零参：
    可变 最高通道 为 负 1。
    可变 最高值 为 0。
    可变 i 为 0。
    只要 i 小于 注意力_通道数：
        若 显著性 的 第 i 项 大于 最高值：
            最高值 为 显著性 的 第 i 项。
            最高通道 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 一组：注意力_tick、做 计数 用 注意力焦点、注意力_切换次数、最高通道、最高值、ACh水平、NE水平。
完。

注意力_显著性分布 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 注意力_通道数：
        做 追加 用 结果、一组：i、显著性 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

注意力_完整统计 接受 零参：
    返还 一组：注意力_tick、做 计数 用 注意力焦点、注意力_切换次数、注意力_通道数。
完。

本章 收束。
