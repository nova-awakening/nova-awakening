《最大池化与RNN》

引用《.·类型定义》。

引用《.·错误码》。

Nova最大池化1D 接受 输入、窗口、步幅：
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 窗口 小于或等于 做 计数 用 输入：
        可变 m 为 输入 的 第 i 项。
        可变 j 为 1。
        只要 j 小于 窗口：
            若 输入 的 第 i 加 j 项 大于 m：
                m 为 输入 的 第 i 加 j 项。
            完。
            j 为 j 加 1。
        完。
        做 网络_追加 用 输出、m。
        i 为 i 加 步幅。
    完。
    返还 输出。
完。

AvgPool1D 接受 输入、窗口、步幅：
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 窗口 小于或等于 做 计数 用 输入：
        可变 s 为 0。
        可变 j 为 0。
        只要 j 小于 窗口：
            s 为 s 加 输入 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        做 网络_追加 用 输出、s 除以 窗口。
        i 为 i 加 步幅。
    完。
    返还 输出。
完。

RNN_单步 接受 x、h_prev、Wx、Wh、b：
    可变 pre 为 Wx 乘 x 加 Wh 乘 h_prev 加 b。
    若 pre 大于 100：
        pre 为 100。
    完。
    若 pre 小于 0 减 100：
        pre 为 0 减 100。
    完。
    返还 pre。
完。

Nova激活函数_int 接受 x：
    若 x 大于 500：
        返还 100。
    完。
    若 x 小于 0 减 500：
        返还 0。
    完。
    返还 50 加 x 除以 10。
完。

长短记忆_单步 接受 x、h_prev、c_prev、Wf、Wi、Wo、Wc、bf、bi、bo、bc：
    f 为 做 Nova激活函数_int 用 Wf 乘 x 加 Wf 乘 h_prev 加 bf。
    ig 为 做 Nova激活函数_int 用 Wi 乘 x 加 Wi 乘 h_prev 加 bi。
    o 为 做 Nova激活函数_int 用 Wo 乘 x 加 Wo 乘 h_prev 加 bo。
    可变 c_hat 为 Wc 乘 x 加 Wc 乘 h_prev 加 bc。
    若 c_hat 大于 100：
        c_hat 为 100。
    完。
    若 c_hat 小于 0 减 100：
        c_hat 为 0 减 100。
    完。
    c 为 f 乘 c_prev 除以 100 加 ig 乘 c_hat 除以 100。
    h 为 o 乘 c 除以 100。
    返还 一组：h、c。
完。

嵌入_创建 接受 词表大小、维度：
    可变 表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 词表大小 乘 维度：
        做 网络_追加 用 表、(i 乘 7 加 13) 取余于 201 减 100。
        i 为 i 加 1。
    完。
    返还 一组：表、词表大小、维度。
完。

嵌入_查找 接受 嵌入表、令牌_id：
    dim 为 嵌入表 的 第 2 项。
    offset 为 令牌_id 乘 dim。
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 dim：
        做 网络_追加 用 v、嵌入表 的 第 0 项 的 第 offset 加 i 项。
        i 为 i 加 1。
    完。
    返还 v。
完。

位置编码_生成 接受 序列长度、维度：
    可变 编码 为 一组：（空）。
    可变 pos 为 0。
    只要 pos 小于 序列长度：
        可变 v 为 一组：（空）。
        可变 d 为 0。
        只要 d 小于 维度：
            angle 为 (pos 乘 127 加 d 乘 53 加 17) 取余于 201。
            val 为 angle 减 100。
            做 网络_追加 用 v、val。
            d 为 d 加 1。
        完。
        做 网络_追加 用 编码、v。
        pos 为 pos 加 1。
    完。
    返还 编码。
完。

注意力_点积 接受 Q、K、V：
    seq_len 为 做 计数 用 Q。
    dim 为 做 计数 用 Q 的 第 0 项。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 seq_len：
        可变 scores 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 seq_len：
            可变 dot 为 0。
            可变 d 为 0。
            只要 d 小于 dim：
                dot 为 dot 加 Q 的 第 i 项 的 第 d 项 乘 K 的 第 j 项 的 第 d 项。
                d 为 d 加 1。
            完。
            做 网络_追加 用 scores、dot。
            j 为 j 加 1。
        完。
        可变 max_s 为 scores 的 第 0 项。
        j 为 1。
        只要 j 小于 seq_len：
            若 scores 的 第 j 项 大于 max_s：
                max_s 为 scores 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        可变 exps 为 一组：（空）。
        可变 total 为 0。
        j 为 0。
        只要 j 小于 seq_len：
            可变 e 为 1000 加 (scores 的 第 j 项 减 max_s)。
            若 e 小于 1：
                e 为 1。
            完。
            做 网络_追加 用 exps、e。
            total 为 total 加 e。
            j 为 j 加 1。
        完。
        可变 out_v 为 一组：（空）。
        可变 d2 为 0。
        只要 d2 小于 dim：
            可变 weighted 为 0。
            j 为 0。
            只要 j 小于 seq_len：
                weighted 为 weighted 加 exps 的 第 j 项 乘 V 的 第 j 项 的 第 d2 项 除以 total。
                j 为 j 加 1。
            完。
            做 网络_追加 用 out_v、weighted。
            d2 为 d2 加 1。
        完。
        做 网络_追加 用 输出、out_v。
        i 为 i 加 1。
    完。
    返还 输出。
完。

多头注意力 接受 Q、K、V、头数：
    seq_len 为 做 计数 用 Q。
    若 seq_len 等于 0：
        返还 一组：（空）。
    完。
    dim 为 做 计数 用 Q 的 第 0 项。
    头维度 为 dim 除以 头数。
    可变 结果 为 一组：（空）。
    可变 h 为 0。
    只要 h 小于 头数：
        可变 Q_h 为 一组：（空）。
        可变 K_h 为 一组：（空）。
        可变 V_h 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 seq_len：
            可变 q_slice 为 一组：（空）。
            可变 k_slice 为 一组：（空）。
            可变 v_slice 为 一组：（空）。
            可变 d 为 h 乘 头维度。
            只要 d 小于 (h 加 1) 乘 头维度 并且 d 小于 dim：
                做 网络_追加 用 q_slice、Q 的 第 i 项 的 第 d 项。
                做 网络_追加 用 k_slice、K 的 第 i 项 的 第 d 项。
                做 网络_追加 用 v_slice、V 的 第 i 项 的 第 d 项。
                d 为 d 加 1。
            完。
            做 网络_追加 用 Q_h、q_slice。
            做 网络_追加 用 K_h、k_slice。
            做 网络_追加 用 V_h、v_slice。
            i 为 i 加 1。
        完。
        头输出 为 做 注意力_点积 用 Q_h、K_h、V_h。
        做 网络_追加 用 结果、头输出。
        h 为 h 加 1。
    完。
    返还 结果。
完。

前馈_层 接受 输入、W1、b1、W2、b2、隐藏维度：
    输入维度 为 做 计数 用 输入。
    可变 隐藏 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 隐藏维度：
        可变 sum 为 b1 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 输入维度：
            sum 为 sum 加 输入 的 第 j 项 乘 W1 的 第 i 乘 输入维度 加 j 项 除以 100。
            j 为 j 加 1。
        完。
        若 sum 小于 0：
            sum 为 0。
        完。
        做 网络_追加 用 隐藏、sum。
        i 为 i 加 1。
    完。
    可变 输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 输入维度：
        可变 sum 为 b2 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 隐藏维度：
            sum 为 sum 加 隐藏 的 第 j 项 乘 W2 的 第 i 乘 隐藏维度 加 j 项 除以 100。
            j 为 j 加 1。
        完。
        做 网络_追加 用 输出、sum。
        i 为 i 加 1。
    完。
    返还 输出。
完。

层归一化 接受 输入、gamma、beta：
    n 为 做 计数 用 输入。
    若 n 等于 0：
        返还 输入。
    完。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 输入 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 n：
        diff 为 输入 的 第 i 项 减 均值。
        方差 为 方差 加 diff 乘 diff 除以 100。
        i 为 i 加 1。
    完。
    方差 为 方差 除以 n。
    可变 std 为 1。
    若 方差 大于 0：
        std 为 做 整数_平方根 用 方差 乘 100。
    完。
    若 std 等于 0：
        std 为 1。
    完。
    可变 输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        normalized 为 (输入 的 第 i 项 减 均值) 乘 100 除以 std。
        scaled 为 normalized 乘 gamma 的 第 i 项 除以 100 加 beta 的 第 i 项。
        做 网络_追加 用 输出、scaled。
        i 为 i 加 1。
    完。
    返还 输出。
完。

批量归一化 接受 输入、运行均值、运行方差、gamma、beta：
    n 为 做 计数 用 输入。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        normalized 为 (输入 的 第 i 项 减 运行均值 的 第 i 项) 乘 100。
        std 为 做 整数_平方根 用 运行方差 的 第 i 项 乘 100 加 1。
        若 std 等于 0：
            std 为 1。
        完。
        normalized 为 normalized 除以 std。
        做 网络_追加 用 输出、normalized 乘 gamma 的 第 i 项 除以 100 加 beta 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 输出。
完。

随机丢弃 接受 输入、比率、训练中：
    若 非 训练中：
        返还 输入。
    完。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 输入：
        若 (i 乘 7 加 13) 取余于 100 小于 比率：
            做 网络_追加 用 输出、0。
        否则：
            做 网络_追加 用 输出、输入 的 第 i 项 乘 100 除以 (100 减 比率)。
        完。
        i 为 i 加 1。
    完。
    返还 输出。
完。

激活_SQNL 接受 t：
    若 t 等于 0：
        返还。
    完。
    总 为 t 的 行数 乘 t 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        x 为 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        可变 结果 为 0。
        若 x 大于 2 乘 Q_ONE：
            结果 为 Q_ONE。
        否则若 x 小于 负 2 乘 Q_ONE：
            结果 为 负 Q_ONE。
        否则若 x 大于或等于 0：
            结果 为 x 减 做 Q乘 用 x、x 除以 4。
        否则：
            结果 为 x 加 做 Q乘 用 x、x 除以 4。
        完。
        做 内存_写32 用 t 的 数据 加 i 乘 4、结果。
        i 为 i 加 1。
    完。
完。

GELU近似 接受 x：
    cubic 为 x 乘 x 乘 x 除以 10000。
    inner 为 x 加 cubic 乘 44 除以 1000。
    若 inner 大于 500：
        返还 x。
    完。
    若 inner 小于 负 500：
        返还 0。
    完。
    返还 x 乘 (50 加 inner) 除以 100。
完。

自门控激活 接受 x：
    sig 为 做 Nova激活函数_int 用 x。
    返还 x 乘 sig 除以 100。
完。

Tanh近似 接受 x：
    若 x 大于 300：
        返还 100。
    完。
    若 x 小于 负 300：
        返还 负 100。
    完。
    返还 x 乘 (300 减 x 乘 x 除以 300) 除以 300。
完。

MSE损失 接受 预测、目标：
    n 为 做 计数 用 预测。
    若 n 等于 0：
        返还 0。
    完。
    可变 sum 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        diff 为 预测 的 第 i 项 减 目标 的 第 i 项。
        sum 为 sum 加 diff 乘 diff 除以 100。
        i 为 i 加 1。
    完。
    返还 sum 除以 n。
完。

MAE损失 接受 预测、目标：
    n 为 做 计数 用 预测。
    若 n 等于 0：
        返还 0。
    完。
    可变 sum 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        diff 为 预测 的 第 i 项 减 目标 的 第 i 项。
        若 diff 小于 0：
            diff 为 0 减 diff。
        完。
        sum 为 sum 加 diff。
        i 为 i 加 1。
    完。
    返还 sum 除以 n。
完。

交叉熵损失 接受 预测概率、标签：
    若 预测概率 小于或等于 0：
        返还 10000。
    完。
    返还 0 减 做 整数_对数 用 预测概率。
完。

整数_平方根 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 r 为 x。
    可变 迭代 为 0。
    只要 迭代 小于 20：
        新r 为 (r 加 x 除以 r) 除以 2。
        若 新r 大于或等于 r：
            跳出。
        完。
        r 为 新r。
        迭代 为 迭代 加 1。
    完。
    返还 r。
完。

整数_对数 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 结果 为 0。
    可变 v 为 x。
    只要 v 大于 1：
        v 为 v 除以 2。
        结果 为 结果 加 69。
    完。
    返还 结果。
完。

网络_追加 接受 列表、值：
    做 追加 用 列表、值。
完。

向量_加 接受 a、b：
    n 为 做 计数 用 a。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 网络_追加 用 结果、a 的 第 i 项 加 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_减 接受 a、b：
    n 为 做 计数 用 a。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 网络_追加 用 结果、a 的 第 i 项 减 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_范数 接受 v：
    返还 做 整数_平方根 用 做 向量_点积 用 v、v 乘 100。
完。

向量_缩放 接受 v、因子：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        做 网络_追加 用 结果、v 的 第 i 项 乘 因子 除以 100。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 NN_前向总数 为 0。

可变 NN_注意力总数 为 0。

可变 NN_池化总数 为 0。

NN_统计 接受 零参：
    返还 一本字典：前向 对应 NN_前向总数、注意力 对应 NN_注意力总数、池化 对应 NN_池化总数。
完。

NN_健康检查 接受 零参：
    返还 「神经网络核心健康」。
完。

NN_重置统计 接受 零参：
    NN_前向总数 为 0。
    NN_注意力总数 为 0。
    NN_池化总数 为 0。
完。

NN_完整统计 接受 零参：
    返还 一组：NN_前向总数、NN_注意力总数、NN_池化总数。
完。

本章 收束。
