《AI调度增强》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·链表后继》。

引用《.·进程描述符》。

引用《..·推理·总线调度》。

智能_SCHED_历史深度 为 32。

AI调度画像 是 一种 事物，含 pid、cpu运行EMA、io等待EMA、行为类型、上次CPU、CPU迁移次数、缓存命中预测、建议时间片、马尔可夫状态。

可变 AI调度画像表 为 一组：（空）。

可变 AI调度_已初始化 为 假。

智能_EMA_衰减 为 922。

AI调度增强_初始化 接受 零参：
    AI调度_已初始化 为 真。
    做 内核信息 用 「AI调度增强: EMA行为分类+马尔可夫CPU预测+自适应时间片」。
完。

AI调度_记录运行 接受 pid、cpu运行时间、io等待时间、当前CPU：
    若 非 AI调度_已初始化：
        返还。
    完。
    画像 为 做 AI调度_获取或创建画像 用 pid。
    画像 的 cpu运行EMA 为 (智能_EMA_衰减 乘 画像 的 cpu运行EMA 加 (1024 减 智能_EMA_衰减) 乘 cpu运行时间) 除以 1024。
    画像 的 io等待EMA 为 (智能_EMA_衰减 乘 画像 的 io等待EMA 加 (1024 减 智能_EMA_衰减) 乘 io等待时间) 除以 1024。
    总时间 为 画像 的 cpu运行EMA 加 画像 的 io等待EMA。
    若 总时间 大于 0：
        cpu比率 为 画像 的 cpu运行EMA 乘 100 除以 总时间。
        若 cpu比率 大于 80：
            画像 的 行为类型 为 0。
        否则若 cpu比率 小于 20：
            画像 的 行为类型 为 1。
        否则：
            画像 的 行为类型 为 2。
        完。
    完。
    若 画像 的 上次CPU 不等于 当前CPU 并且 画像 的 上次CPU 小于 4：
        画像 的 CPU迁移次数 为 画像 的 CPU迁移次数 加 1。
    完。
    若 当前CPU 小于 4：
        可变 j 为 0。
        只要 j 小于 4：
            若 j 等于 当前CPU：
                画像 的 马尔可夫状态 的 第 j 项 为 (画像 的 马尔可夫状态 的 第 j 项 乘 900 加 1024 乘 100) 除以 1000。
            否则：
                画像 的 马尔可夫状态 的 第 j 项 为 画像 的 马尔可夫状态 的 第 j 项 乘 900 除以 1000。
            完。
            j 为 j 加 1。
        完。
    完。
    画像 的 上次CPU 为 当前CPU。
    若 画像 的 行为类型 等于 0：
        画像 的 建议时间片 为 20000000。
    否则若 画像 的 行为类型 等于 1：
        画像 的 建议时间片 为 4000000。
    否则：
        画像 的 建议时间片 为 10000000。
    完。
完。

AI调度_选择CPU 接受 pid、可用CPU数：
    画像 为 做 AI调度_查找画像 用 pid。
    若 画像 等于 空：
        返还 0。
    完。
    可变 最优CPU 为 0。
    可变 最高概率 为 0。
    可变 i 为 0。
    只要 i 小于 可用CPU数 并且 i 小于 4：
        若 画像 的 马尔可夫状态 的 第 i 项 大于 最高概率：
            最高概率 为 画像 的 马尔可夫状态 的 第 i 项。
            最优CPU 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最优CPU。
完。

AI调度_获取时间片 接受 pid：
    画像 为 做 AI调度_查找画像 用 pid。
    若 画像 等于 空：
        返还 10000000。
    完。
    返还 画像 的 建议时间片。
完。

AI调度_获取行为类型 接受 pid：
    画像 为 做 AI调度_查找画像 用 pid。
    若 画像 等于 空：
        返还 2。
    完。
    返还 画像 的 行为类型。
完。

AI调度增强_建议 接受 进程列表、CPU数量：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 进程列表：
        pid 为 做 进程_PID 用 进程列表 的 第 i 项。
        cpu 为 做 AI调度_选择CPU 用 pid、CPU数量。
        做 追加 用 结果、一组：pid、cpu。
        i 为 i 加 1。
    完。
    返还 结果。
完。

AI调度_查找画像 接受 pid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI调度画像表：
        若 AI调度画像表 的 第 i 项 的 pid 等于 pid：
            返还 AI调度画像表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

AI调度_获取或创建画像 接受 pid：
    已有 为 做 AI调度_查找画像 用 pid。
    若 已有 不等于 空：
        返还 已有。
    完。
    新画像 为 新建 AI调度画像。
    新画像 的 pid 为 pid。
    新画像 的 行为类型 为 2。
    新画像 的 建议时间片 为 10000000。
    新画像 的 马尔可夫状态 为 一组：256、256、256、256。
    做 追加 用 AI调度画像表、新画像。
    返还 新画像。
完。

AI调度_清除画像 接受 pid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI调度画像表：
        若 AI调度画像表 的 第 i 项 的 pid 等于 pid：
            做 链表_删除 用 AI调度画像表、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

可变 智能_全局负载EMA 为 0。

可变 智能_负载采样数 为 0。

AI调度_更新全局负载 接受 当前负载：
    智能_全局负载EMA 为 (智能_EMA_衰减 乘 智能_全局负载EMA 加 (1024 减 智能_EMA_衰减) 乘 当前负载 乘 1024) 除以 1024。
    智能_负载采样数 为 智能_负载采样数 加 1。
完。

AI调度_预测负载 接受 零参：
    返还 智能_全局负载EMA 除以 1024。
完。

AI调度_行为类型名 接受 类型：
    若 类型 等于 0：
        返还 「计算密集」。
    完。
    若 类型 等于 1：
        返还 「IO密集」。
    完。
    若 类型 等于 2：
        返还 「混合」。
    完。
    若 类型 等于 3：
        返还 「突发」。
    完。
    返还 「未知」。
完。

AI调度_统计 接受 零参：
    返还 一组：做 计数 用 AI调度画像表、智能_全局负载EMA 除以 1024、智能_负载采样数。
完。

AI调度_完整统计 接受 零参：
    返还 一组：做 计数 用 AI调度画像表、智能_全局负载EMA 除以 1024、智能_负载采样数、AI调度_已初始化。
完。

本章 收束。
