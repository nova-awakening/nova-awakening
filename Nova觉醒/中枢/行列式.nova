《行列式》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·欧几里得模》。

引用《.·整数转字符串》。

行列式2 接受 m：
    返还 做 矩阵_取 用 m、0、0 乘 做 矩阵_取 用 m、1、1 减 做 矩阵_取 用 m、0、1 乘 做 矩阵_取 用 m、1、0。
完。

行列式3 接受 m：
    a 为 做 矩阵_取 用 m、0、0。
    b 为 做 矩阵_取 用 m、0、1。
    c 为 做 矩阵_取 用 m、0、2。
    d 为 做 矩阵_取 用 m、1、0。
    e 为 做 矩阵_取 用 m、1、1。
    f 为 做 矩阵_取 用 m、1、2。
    g 为 做 矩阵_取 用 m、2、0。
    h 为 做 矩阵_取 用 m、2、1。
    kk 为 做 矩阵_取 用 m、2、2。
    返还 a 乘 (e 乘 kk 减 f 乘 h) 减 b 乘 (d 乘 kk 减 f 乘 g) 加 c 乘 (d 乘 h 减 e 乘 g)。
完。

矩阵_kronecker 接受 A、B：
    ra 为 A 的 第 0 项。
    ca 为 A 的 第 1 项。
    rb 为 B 的 第 0 项。
    cb 为 B 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 ra 乘 rb、ca 乘 cb。
    可变 i 为 0。
    只要 i 小于 ra：
        可变 j 为 0。
        只要 j 小于 ca：
            aij 为 做 矩阵_取 用 A、i、j。
            可变 p 为 0。
            只要 p 小于 rb：
                可变 q 为 0。
                只要 q 小于 cb：
                    做 矩阵_设 用 结果、i 乘 rb 加 p、j 乘 cb 加 q、aij 乘 做 矩阵_取 用 B、p、q。
                    q 为 q 加 1。
                完。
                p 为 p 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_幂 接受 m、n：
    大小 为 m 的 第 0 项。
    可变 结果 为 做 矩阵_单位 用 大小。
    可变 基 为 m。
    可变 指数 为 n。
    只要 指数 大于 0：
        若 指数 取余于 2 等于 1：
            结果 为 做 矩阵_乘 用 结果、基。
        完。
        基 为 做 矩阵_乘 用 基、基。
        指数 为 指数 除以 2。
    完。
    返还 结果。
完。

矩阵_取对角 接受 m：
    n 为 m 的 第 0 项。
    可变 对角 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 对角、做 矩阵_取 用 m、i、i。
        i 为 i 加 1。
    完。
    返还 对角。
完。

矩阵_无穷范数 接受 m：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 行：
        可变 行和 为 0。
        可变 j 为 0。
        只要 j 小于 列：
            可变 v 为 做 矩阵_取 用 m、i、j。
            若 v 小于 0：
                v 为 0 减 v。
            完。
            行和 为 行和 加 v。
            j 为 j 加 1。
        完。
        若 行和 大于 最大：
            最大 为 行和。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

矩阵_子矩阵 接受 m、行起、列起、行数、列数：
    可变 结果 为 做 矩阵_创建 用 行数、列数。
    可变 i 为 0。
    只要 i 小于 行数：
        可变 j 为 0。
        只要 j 小于 列数：
            做 矩阵_设 用 结果、i、j、做 矩阵_取 用 m、行起 加 i、列起 加 j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_展平 接受 m：
    n 为 m 的 第 0 项 乘 m 的 第 1 项。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、m 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_随机 接受 行、列、最小、最大、种子：
    可变 m 为 一组：行、列。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        做 追加 用 m、最小 加 s 取余于 (最大 减 最小 加 1)。
        i 为 i 加 1。
    完。
    返还 m。
完。

矩阵精度 为 1000。

AI矩阵窗口 为 16。

矩阵_Frobenius范数 接受 m：
    若 m 等于 空：
        返还 0。
    完。
    全局统计 的 范数计算数 为 全局统计 的 范数计算数 加 1。
    可变 平方和 为 0。
    可变 i 为 0。
    只要 i 小于 m 的 第 0 项 乘 m 的 第 1 项：
        v 为 m 的 第 2 加 i 项。
        平方和 为 平方和 加 v 乘 v 除以 矩阵精度。
        i 为 i 加 1。
    完。
    可变 x 为 平方和。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 平方和 除以 x) 除以 2。
    完。
    返还 x。
完。

矩阵_一范数 接受 m：
    若 m 等于 空：
        返还 0。
    完。
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 最大列和 为 0。
    可变 j 为 0。
    只要 j 小于 列：
        可变 列和 为 0。
        可变 i 为 0。
        只要 i 小于 行：
            可变 v 为 做 矩阵_取 用 m、i、j。
            若 v 小于 0：
                v 为 0 减 v。
            完。
            列和 为 列和 加 v。
            i 为 i 加 1。
        完。
        若 列和 大于 最大列和：
            最大列和 为 列和。
        完。
        j 为 j 加 1。
    完。
    返还 最大列和。
完。

矩阵_相等 接受 A、B：
    若 A 等于 空 或者 B 等于 空：
        返还 假。
    完。
    若 A 的 第 0 项 不等于 B 的 第 0 项 或者 A 的 第 1 项 不等于 B 的 第 1 项：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 A 的 第 0 项 乘 A 的 第 1 项：
        若 A 的 第 2 加 i 项 不等于 B 的 第 2 加 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

矩阵_是对称 接受 m：
    若 m 等于 空 或者 m 的 第 0 项 不等于 m 的 第 1 项：
        返还 假。
    完。
    n 为 m 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 做 矩阵_取 用 m、i、j 不等于 做 矩阵_取 用 m、j、i：
                返还 假。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

矩阵_是上三角 接受 m：
    若 m 等于 空 或者 m 的 第 0 项 不等于 m 的 第 1 项：
        返还 假。
    完。
    n 为 m 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 i：
            若 做 矩阵_取 用 m、i、j 不等于 0：
                返还 假。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

矩阵_是下三角 接受 m：
    若 m 等于 空 或者 m 的 第 0 项 不等于 m 的 第 1 项：
        返还 假。
    完。
    n 为 m 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 做 矩阵_取 用 m、i、j 不等于 0：
                返还 假。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

矩阵_条件数估算 接受 m：
    若 m 等于 空：
        返还 0。
    完。
    范数 为 做 矩阵_无穷范数 用 m。
    若 范数 等于 0：
        返还 0。
    完。
    迹 为 做 矩阵_迹_辅助 用 m。
    若 迹 等于 0：
        返还 范数。
    完。
    返还 范数 乘 范数 除以 迹。
完。

矩阵_迹_辅助 接受 m：
    若 m 等于 空 或者 m 的 第 0 项 不等于 m 的 第 1 项：
        返还 0。
    完。
    可变 迹 为 0。
    可变 i 为 0。
    只要 i 小于 m 的 第 0 项：
        迹 为 迹 加 做 矩阵_取 用 m、i、i。
        i 为 i 加 1。
    完。
    返还 迹。
完。

矩阵_稀疏率 接受 m：
    若 m 等于 空：
        返还 0。
    完。
    总元素 为 m 的 第 0 项 乘 m 的 第 1 项。
    若 总元素 等于 0：
        返还 0。
    完。
    可变 零数 为 0。
    可变 i 为 0。
    只要 i 小于 总元素：
        若 m 的 第 2 加 i 项 等于 0：
            零数 为 零数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 零数 乘 100 除以 总元素。
完。

矩阵_非零元素数 接受 m：
    若 m 等于 空：
        返还 0。
    完。
    可变 非零 为 0。
    可变 i 为 0。
    只要 i 小于 m 的 第 0 项 乘 m 的 第 1 项：
        若 m 的 第 2 加 i 项 不等于 0：
            非零 为 非零 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 非零。
完。

矩阵_水平拼接 接受 A、B：
    若 A 等于 空 或者 B 等于 空：
        返还 空。
    完。
    若 A 的 第 0 项 不等于 B 的 第 0 项：
        返还 空。
    完。
    行 为 A 的 第 0 项。
    列A 为 A 的 第 1 项。
    列B 为 B 的 第 1 项。
    可变 C 为 做 矩阵_创建 用 行、列A 加 列B。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列A：
            做 矩阵_设 用 C、i、j、做 矩阵_取 用 A、i、j。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 列B：
            做 矩阵_设 用 C、i、列A 加 j、做 矩阵_取 用 B、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 C。
完。

矩阵_垂直拼接 接受 A、B：
    若 A 等于 空 或者 B 等于 空：
        返还 空。
    完。
    若 A 的 第 1 项 不等于 B 的 第 1 项：
        返还 空。
    完。
    行A 为 A 的 第 0 项。
    行B 为 B 的 第 0 项。
    列 为 A 的 第 1 项。
    可变 C 为 做 矩阵_创建 用 行A 加 行B、列。
    可变 i 为 0。
    只要 i 小于 行A：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 C、i、j、做 矩阵_取 用 A、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 行B：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 C、行A 加 i、j、做 矩阵_取 用 B、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 C。
完。

可变 AI矩阵历史 为 一组：（空）。

可变 AI矩阵位置 为 0。

AI_记录矩阵运算 接受 维度、类型：
    若 做 计数 用 AI矩阵历史 小于 AI矩阵窗口：
        做 追加 用 AI矩阵历史、一组：维度、类型。
    否则：
        做 设置 用 AI矩阵历史、AI矩阵位置、一组：维度、类型。
    完。
    AI矩阵位置 为 (AI矩阵位置 加 1) 取余于 AI矩阵窗口。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
完。

AI_预测运算复杂度 接受 维度、类型：
    若 类型 等于 0：
        返还 维度 乘 维度。
    完。
    返还 维度 乘 维度 乘 维度。
完。

AI_建议是否并行 接受 维度：
    若 维度 大于 64：
        返还 真。
    完。
    返还 假。
完。

行列式统计 是 一种 事物，含 行列式2计算数、行列式3计算数、Kronecker积数、矩阵幂数、范数计算数、子矩阵提取数、随机矩阵数、拼接操作数、稀疏率查询数、比较操作数、AI预测次数、AI预测命中、最大维度、总运算量、错误次数。

可变 全局统计 为 新建 行列式统计。

可变 行列式模块已初始化 为 假。

行列式模块_初始化 接受 零参：
    若 行列式模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI矩阵历史 为 一组：（空）。
    AI矩阵位置 为 0。
    行列式模块已初始化 为 真。
    做 打印 用 「[行列式] 初始化完成」。
    返还 0。
完。

行列式模块_销毁 接受 零参：
    若 行列式模块已初始化 等于 假：
        返还 0。
    完。
    AI矩阵历史 为 一组：（空）。
    行列式模块已初始化 为 假。
    做 打印 用 「[行列式] 已销毁」。
    返还 0。
完。

行列式模块_转储 接受 零参：
    做 打印 用 「[行列式] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  行列式2: 」、做 整数转字符串 用 全局统计 的 行列式2计算数。
    做 打印 用 做 字符串拼接 用 「  行列式3: 」、做 整数转字符串 用 全局统计 的 行列式3计算数。
    做 打印 用 做 字符串拼接 用 「  Kronecker: 」、做 整数转字符串 用 全局统计 的 Kronecker积数。
    返还 0。
完。

行列式模块_打印统计 接受 零参：
    做 打印 用 「[行列式] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  行列式2: 」、做 整数转字符串 用 全局统计 的 行列式2计算数。
    做 打印 用 做 字符串拼接 用 「  行列式3: 」、做 整数转字符串 用 全局统计 的 行列式3计算数。
    做 打印 用 做 字符串拼接 用 「  Kronecker积: 」、做 整数转字符串 用 全局统计 的 Kronecker积数。
    做 打印 用 做 字符串拼接 用 「  矩阵幂: 」、做 整数转字符串 用 全局统计 的 矩阵幂数。
    做 打印 用 做 字符串拼接 用 「  范数计算: 」、做 整数转字符串 用 全局统计 的 范数计算数。
    做 打印 用 做 字符串拼接 用 「  子矩阵: 」、做 整数转字符串 用 全局统计 的 子矩阵提取数。
    做 打印 用 做 字符串拼接 用 「  随机矩阵: 」、做 整数转字符串 用 全局统计 的 随机矩阵数。
    做 打印 用 做 字符串拼接 用 「  拼接操作: 」、做 整数转字符串 用 全局统计 的 拼接操作数。
    做 打印 用 做 字符串拼接 用 「  稀疏率查询: 」、做 整数转字符串 用 全局统计 的 稀疏率查询数。
    做 打印 用 做 字符串拼接 用 「  比较操作: 」、做 整数转字符串 用 全局统计 的 比较操作数。
    做 打印 用 做 字符串拼接 用 「  AI预测: 」、做 整数转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI命中: 」、做 整数转字符串 用 全局统计 的 AI预测命中。
    做 打印 用 做 字符串拼接 用 「  最大维度: 」、做 整数转字符串 用 全局统计 的 最大维度。
    做 打印 用 做 字符串拼接 用 「  总运算量: 」、做 整数转字符串 用 全局统计 的 总运算量。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 整数转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

行列式模块_注册到内核 接受 内核上下文：
    若 行列式模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[行列式] 已注册到内核上下文」。
    返还 0。
完。

行列式模块_查询状态 接受 零参：
    若 行列式模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

行列式模块_健康检查 接受 零参：
    若 行列式模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 行列式2计算数 加 全局统计 的 Kronecker积数：
        返还 假。
    完。
    返还 真。
完。

矩阵_零矩阵 接受 行、列：
    返还 做 矩阵_创建 用 行、列。
完。

矩阵_是零矩阵 接受 m：
    若 m 等于 空：
        返还 真。
    完。
    可变 i 为 0。
    只要 i 小于 m 的 第 0 项 乘 m 的 第 1 项：
        若 m 的 第 2 加 i 项 不等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

矩阵_是单位矩阵 接受 m：
    若 m 等于 空 或者 m 的 第 0 项 不等于 m 的 第 1 项：
        返还 假。
    完。
    n 为 m 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 n：
            期望 为 0。
            若 i 等于 j：
                期望 为 矩阵精度。
            完。
            若 做 矩阵_取 用 m、i、j 不等于 期望：
                返还 假。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

矩阵_最大元素 接受 m：
    若 m 等于 空：
        返还 0。
    完。
    可变 最大 为 m 的 第 2 项。
    可变 i 为 1。
    只要 i 小于 m 的 第 0 项 乘 m 的 第 1 项：
        若 m 的 第 2 加 i 项 大于 最大：
            最大 为 m 的 第 2 加 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

矩阵_最小元素 接受 m：
    若 m 等于 空：
        返还 0。
    完。
    可变 最小 为 m 的 第 2 项。
    可变 i 为 1。
    只要 i 小于 m 的 第 0 项 乘 m 的 第 1 项：
        若 m 的 第 2 加 i 项 小于 最小：
            最小 为 m 的 第 2 加 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小。
完。

矩阵_元素和 接受 m：
    若 m 等于 空：
        返还 0。
    完。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 m 的 第 0 项 乘 m 的 第 1 项：
        和 为 和 加 m 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

矩阵_取行 接受 m、行号：
    若 m 等于 空 或者 行号 大于或等于 m 的 第 0 项：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 m 的 第 1 项：
        做 追加 用 结果、做 矩阵_取 用 m、行号、j。
        j 为 j 加 1。
    完。
    返还 结果。
完。

矩阵_取列 接受 m、列号：
    若 m 等于 空 或者 列号 大于或等于 m 的 第 1 项：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 m 的 第 0 项：
        做 追加 用 结果、做 矩阵_取 用 m、i、列号。
        i 为 i 加 1。
    完。
    返还 结果。
完。

本章 收束。
