《内存位图》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·字符串替换》。

位图最大字数 为 65536。

全一64 为 0xFFFFFFFFFFFFFFFF。

位图无效 为 0 减 1。

页大小 为 4096。

页帧位图最大页 为 1048576。

区域_DMA 为 0。

区域_Normal 为 1。

区域_HighMem 为 2。

区域数量 为 3。

DMA上限页 为 4096。

Normal上限页 为 262144。

AI预测窗口 为 16。

AI预测衰减 为 900。

AI预测缩放 为 1000。

碎片分析粒度 为 64。

位图描述符 是 一种 事物，含 数据、总位数、字数、已分配数、空闲数、最后分配位置。

页帧区域 是 一种 事物，含 名称、起始页、结束页、位图、总页数、空闲页数。

位图统计 是 一种 事物，含 创建次数、设置次数、清除次数、测试次数、查找次数、查找命中次数、查找未命中次数、分配次数、分配失败次数、释放次数、连续分配次数、对齐分配次数、范围操作次数、集合运算次数、AI预测次数、AI预测命中次数、碎片分析次数、错误次数。

可变 全局统计 为 新建 位图统计。

可变 已初始化 为 假。

可变 页帧区域列表 为 一组：（空）。

可变 AI预测历史 为 一组：（空）。

可变 AI预测位置 为 0。

内存位图_初始化 接受 零参：
    若 已初始化 等于 真：
        返还 错误_已存在。
    完。
    页帧区域列表 为 一组：（空）。
    AI预测历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI预测窗口：
        做 追加 用 AI预测历史、0。
        i 为 i 加 1。
    完。
    AI预测位置 为 0。
    已初始化 为 真。
    做 打印 用 「[内存位图] 初始化完成」。
    返还 0。
完。

内存位图_销毁 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    页帧区域列表 为 一组：（空）。
    AI预测历史 为 一组：（空）。
    已初始化 为 假。
    做 打印 用 「[内存位图] 已销毁」。
    返还 0。
完。

bitmap_创建 接受 位数：
    若 位数 小于或等于 0：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 空。
    完。
    字数 为 (位数 加 63) 除以 64。
    若 字数 大于 位图最大字数：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 空。
    完。
    可变 描述 为 新建 位图描述符。
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 字数：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    描述 的 数据 为 数据。
    描述 的 总位数 为 位数。
    描述 的 字数 为 字数。
    描述 的 已分配数 为 0。
    描述 的 空闲数 为 位数。
    描述 的 最后分配位置 为 0。
    全局统计 的 创建次数 为 全局统计 的 创建次数 加 1。
    返还 描述。
完。

bitmap_设置 接受 bm、位：
    若 bm 等于 空：
        返还 错误_无效参数。
    完。
    若 位 小于 0 或者 位 大于或等于 bm 的 总位数：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_越界。
    完。
    字 为 位 除以 64。
    偏移 为 位 取余于 64。
    旧值 为 bm 的 数据 的 第 字 项。
    bm 的 数据 的 第 字 项 为 bm 的 数据 的 第 字 项 位或 1 左移 偏移。
    若 旧值 位与 1 左移 偏移 等于 0：
        bm 的 已分配数 为 bm 的 已分配数 加 1。
        bm 的 空闲数 为 bm 的 空闲数 减 1。
    完。
    全局统计 的 设置次数 为 全局统计 的 设置次数 加 1。
    返还 0。
完。

bitmap_清除 接受 bm、位：
    若 bm 等于 空：
        返还 错误_无效参数。
    完。
    若 位 小于 0 或者 位 大于或等于 bm 的 总位数：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_越界。
    完。
    字 为 位 除以 64。
    偏移 为 位 取余于 64。
    旧值 为 bm 的 数据 的 第 字 项。
    bm 的 数据 的 第 字 项 为 bm 的 数据 的 第 字 项 位与 按位取反 (1 左移 偏移)。
    若 旧值 位与 1 左移 偏移 不等于 0：
        bm 的 已分配数 为 bm 的 已分配数 减 1。
        bm 的 空闲数 为 bm 的 空闲数 加 1。
    完。
    全局统计 的 清除次数 为 全局统计 的 清除次数 加 1。
    返还 0。
完。

bitmap_测试 接受 bm、位：
    若 bm 等于 空：
        返还 假。
    完。
    若 位 小于 0 或者 位 大于或等于 bm 的 总位数：
        返还 假。
    完。
    字 为 位 除以 64。
    偏移 为 位 取余于 64。
    全局统计 的 测试次数 为 全局统计 的 测试次数 加 1。
    返还 bm 的 数据 的 第 字 项 位与 1 左移 偏移 不等于 0。
完。

bitmap_翻转 接受 bm、位：
    若 bm 等于 空：
        返还 错误_无效参数。
    完。
    若 位 小于 0 或者 位 大于或等于 bm 的 总位数：
        返还 错误_越界。
    完。
    字 为 位 除以 64。
    偏移 为 位 取余于 64。
    旧值 为 bm 的 数据 的 第 字 项。
    bm 的 数据 的 第 字 项 为 bm 的 数据 的 第 字 项 位异或 1 左移 偏移。
    若 旧值 位与 1 左移 偏移 不等于 0：
        bm 的 已分配数 为 bm 的 已分配数 减 1。
        bm 的 空闲数 为 bm 的 空闲数 加 1。
    否则：
        bm 的 已分配数 为 bm 的 已分配数 加 1。
        bm 的 空闲数 为 bm 的 空闲数 减 1。
    完。
    返还 0。
完。

bitmap_找第一个设置位 接受 bm、总位数：
    返还 做 bitmap_找下一个设置位 用 bm、总位数、0。
完。

bitmap_找下一个设置位 接受 bm、总位数、起始：
    若 bm 等于 空：
        返还 总位数。
    完。
    全局统计 的 查找次数 为 全局统计 的 查找次数 加 1。
    可变 i 为 起始。
    只要 i 小于 总位数：
        字 为 i 除以 64。
        若 字 小于 bm 的 字数 并且 bm 的 数据 的 第 字 项 等于 0：
            i 为 (字 加 1) 乘 64。
            继续。
        完。
        若 做 bitmap_测试 用 bm、i：
            全局统计 的 查找命中次数 为 全局统计 的 查找命中次数 加 1。
            返还 i。
        完。
        i 为 i 加 1。
    完。
    全局统计 的 查找未命中次数 为 全局统计 的 查找未命中次数 加 1。
    返还 总位数。
完。

bitmap_找第一个清除位 接受 bm、总位数：
    返还 做 bitmap_找下一个清除位 用 bm、总位数、0。
完。

bitmap_找下一个清除位 接受 bm、总位数、起始：
    若 bm 等于 空：
        返还 总位数。
    完。
    全局统计 的 查找次数 为 全局统计 的 查找次数 加 1。
    可变 i 为 起始。
    只要 i 小于 总位数：
        字 为 i 除以 64。
        若 字 小于 bm 的 字数 并且 bm 的 数据 的 第 字 项 等于 全一64：
            i 为 (字 加 1) 乘 64。
            继续。
        完。
        若 做 bitmap_测试 用 bm、i 等于 假：
            全局统计 的 查找命中次数 为 全局统计 的 查找命中次数 加 1。
            返还 i。
        完。
        i 为 i 加 1。
    完。
    全局统计 的 查找未命中次数 为 全局统计 的 查找未命中次数 加 1。
    返还 总位数。
完。

bitmap_找连续清除位 接受 bm、总位数、需要数量：
    若 bm 等于 空 或者 需要数量 小于或等于 0：
        返还 位图无效。
    完。
    全局统计 的 查找次数 为 全局统计 的 查找次数 加 1。
    可变 起始 为 0。
    可变 连续 为 0。
    可变 i 为 0。
    只要 i 小于 总位数：
        若 做 bitmap_测试 用 bm、i 等于 假：
            若 连续 等于 0：
                起始 为 i。
            完。
            连续 为 连续 加 1。
            若 连续 大于或等于 需要数量：
                全局统计 的 查找命中次数 为 全局统计 的 查找命中次数 加 1。
                返还 起始。
            完。
        否则：
            连续 为 0。
        完。
        i 为 i 加 1。
    完。
    全局统计 的 查找未命中次数 为 全局统计 的 查找未命中次数 加 1。
    返还 位图无效。
完。

bitmap_全设置 接受 bm：
    若 bm 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 bm 的 字数：
        bm 的 数据 的 第 i 项 为 全一64。
        i 为 i 加 1。
    完。
    bm 的 已分配数 为 bm 的 总位数。
    bm 的 空闲数 为 0。
    返还 0。
完。

bitmap_全清除 接受 bm：
    若 bm 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 bm 的 字数：
        bm 的 数据 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    bm 的 已分配数 为 0。
    bm 的 空闲数 为 bm 的 总位数。
    返还 0。
完。

bitmap_权重 接受 bm、总位数：
    若 bm 等于 空：
        返还 0。
    完。
    返还 bm 的 已分配数。
完。

bitmap_为空 接受 bm：
    若 bm 等于 空：
        返还 真。
    完。
    返还 bm 的 已分配数 等于 0。
完。

bitmap_已满 接受 bm、总位数：
    若 bm 等于 空：
        返还 假。
    完。
    返还 bm 的 空闲数 等于 0。
完。

bitmap_使用率 接受 bm：
    若 bm 等于 空 或者 bm 的 总位数 等于 0：
        返还 0。
    完。
    返还 bm 的 已分配数 乘 100 除以 bm 的 总位数。
完。

bitmap_与 接受 dst、a、b：
    若 dst 等于 空 或者 a 等于 空 或者 b 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 a 的 字数 并且 i 小于 b 的 字数 并且 i 小于 dst 的 字数：
        dst 的 数据 的 第 i 项 为 a 的 数据 的 第 i 项 位与 b 的 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    全局统计 的 集合运算次数 为 全局统计 的 集合运算次数 加 1。
    返还 0。
完。

bitmap_或 接受 dst、a、b：
    若 dst 等于 空 或者 a 等于 空 或者 b 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 a 的 字数 并且 i 小于 b 的 字数 并且 i 小于 dst 的 字数：
        dst 的 数据 的 第 i 项 为 a 的 数据 的 第 i 项 位或 b 的 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    全局统计 的 集合运算次数 为 全局统计 的 集合运算次数 加 1。
    返还 0。
完。

bitmap_异或 接受 dst、a、b：
    若 dst 等于 空 或者 a 等于 空 或者 b 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 a 的 字数 并且 i 小于 b 的 字数 并且 i 小于 dst 的 字数：
        dst 的 数据 的 第 i 项 为 a 的 数据 的 第 i 项 位异或 b 的 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    全局统计 的 集合运算次数 为 全局统计 的 集合运算次数 加 1。
    返还 0。
完。

bitmap_取反 接受 dst、src：
    若 dst 等于 空 或者 src 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 src 的 字数 并且 i 小于 dst 的 字数：
        dst 的 数据 的 第 i 项 为 按位取反 src 的 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    全局统计 的 集合运算次数 为 全局统计 的 集合运算次数 加 1。
    返还 0。
完。

bitmap_相等 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 假。
    完。
    若 a 的 字数 不等于 b 的 字数：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 a 的 字数：
        若 a 的 数据 的 第 i 项 不等于 b 的 数据 的 第 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

bitmap_是子集 接受 子集、超集：
    若 子集 等于 空 或者 超集 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 子集 的 字数 并且 i 小于 超集 的 字数：
        若 子集 的 数据 的 第 i 项 位与 按位取反 超集 的 数据 的 第 i 项 不等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

bitmap_相交 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 a 的 字数 并且 i 小于 b 的 字数：
        若 a 的 数据 的 第 i 项 位与 b 的 数据 的 第 i 项 不等于 0：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

bitmap_设置范围 接受 bm、起始、结束：
    若 bm 等于 空：
        返还 错误_无效参数。
    完。
    若 起始 小于 0 或者 结束 大于 bm 的 总位数 或者 起始 大于或等于 结束：
        返还 错误_越界。
    完。
    可变 i 为 起始。
    只要 i 小于 结束：
        做 bitmap_设置 用 bm、i。
        i 为 i 加 1。
    完。
    全局统计 的 范围操作次数 为 全局统计 的 范围操作次数 加 1。
    返还 0。
完。

bitmap_清除范围 接受 bm、起始、结束：
    若 bm 等于 空：
        返还 错误_无效参数。
    完。
    若 起始 小于 0 或者 结束 大于 bm 的 总位数 或者 起始 大于或等于 结束：
        返还 错误_越界。
    完。
    可变 i 为 起始。
    只要 i 小于 结束：
        做 bitmap_清除 用 bm、i。
        i 为 i 加 1。
    完。
    全局统计 的 范围操作次数 为 全局统计 的 范围操作次数 加 1。
    返还 0。
完。

bitmap_分配一个 接受 bm：
    若 bm 等于 空：
        返还 位图无效。
    完。
    若 bm 的 空闲数 等于 0：
        全局统计 的 分配失败次数 为 全局统计 的 分配失败次数 加 1。
        返还 位图无效。
    完。
    可变 位 为 做 bitmap_找下一个清除位 用 bm、bm 的 总位数、bm 的 最后分配位置。
    若 位 大于或等于 bm 的 总位数：
        位 为 做 bitmap_找第一个清除位 用 bm、bm 的 总位数。
    完。
    若 位 小于 bm 的 总位数：
        做 bitmap_设置 用 bm、位。
        bm 的 最后分配位置 为 位 加 1。
        全局统计 的 分配次数 为 全局统计 的 分配次数 加 1。
        做 AI_记录分配 用 位。
        返还 位。
    完。
    全局统计 的 分配失败次数 为 全局统计 的 分配失败次数 加 1。
    返还 位图无效。
完。

bitmap_释放一个 接受 bm、位：
    若 bm 等于 空：
        返还 错误_无效参数。
    完。
    若 位 小于 0 或者 位 大于或等于 bm 的 总位数：
        返还 错误_越界。
    完。
    若 做 bitmap_测试 用 bm、位 等于 假：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    做 bitmap_清除 用 bm、位。
    全局统计 的 释放次数 为 全局统计 的 释放次数 加 1。
    返还 0。
完。

bitmap_分配连续 接受 bm、数量：
    若 bm 等于 空 或者 数量 小于或等于 0：
        返还 位图无效。
    完。
    若 bm 的 空闲数 小于 数量：
        全局统计 的 分配失败次数 为 全局统计 的 分配失败次数 加 1。
        返还 位图无效。
    完。
    起始 为 做 bitmap_找连续清除位 用 bm、bm 的 总位数、数量。
    若 起始 等于 位图无效：
        全局统计 的 分配失败次数 为 全局统计 的 分配失败次数 加 1。
        返还 位图无效。
    完。
    做 bitmap_设置范围 用 bm、起始、起始 加 数量。
    全局统计 的 连续分配次数 为 全局统计 的 连续分配次数 加 1。
    做 AI_记录分配 用 起始。
    返还 起始。
完。

bitmap_分配对齐 接受 bm、数量、对齐：
    若 bm 等于 空 或者 数量 小于或等于 0 或者 对齐 小于或等于 0：
        返还 位图无效。
    完。
    可变 起始 为 0。
    只要 起始 小于 bm 的 总位数：
        若 起始 取余于 对齐 不等于 0：
            起始 为 起始 加 对齐 减 起始 取余于 对齐。
        完。
        若 起始 加 数量 大于 bm 的 总位数：
            全局统计 的 分配失败次数 为 全局统计 的 分配失败次数 加 1。
            返还 位图无效。
        完。
        可变 全空 为 真。
        可变 j 为 0。
        只要 j 小于 数量：
            若 做 bitmap_测试 用 bm、起始 加 j 等于 真：
                全空 为 假。
                起始 为 起始 加 j 加 1。
                止。
            完。
            j 为 j 加 1。
        完。
        若 全空 等于 真：
            做 bitmap_设置范围 用 bm、起始、起始 加 数量。
            全局统计 的 对齐分配次数 为 全局统计 的 对齐分配次数 加 1。
            返还 起始。
        完。
    完。
    全局统计 的 分配失败次数 为 全局统计 的 分配失败次数 加 1。
    返还 位图无效。
完。

bitmap_释放范围 接受 bm、起始、数量：
    若 bm 等于 空：
        返还 错误_无效参数。
    完。
    若 起始 小于 0 或者 起始 加 数量 大于 bm 的 总位数：
        返还 错误_越界。
    完。
    做 bitmap_清除范围 用 bm、起始、起始 加 数量。
    全局统计 的 释放次数 为 全局统计 的 释放次数 加 数量。
    返还 0。
完。

bitmap_拷贝 接受 dst、src：
    若 dst 等于 空 或者 src 等于 空：
        返还 错误_无效参数。
    完。
    可变 i 为 0。
    只要 i 小于 src 的 字数 并且 i 小于 dst 的 字数：
        dst 的 数据 的 第 i 项 为 src 的 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    dst 的 已分配数 为 src 的 已分配数。
    dst 的 空闲数 为 src 的 空闲数。
    返还 0。
完。

页帧位图_初始化 接受 总页数：
    若 总页数 小于或等于 0 或者 总页数 大于 页帧位图最大页：
        返还 错误_无效参数。
    完。
    页帧区域列表 为 一组：（空）。
    可变 dma区 为 新建 页帧区域。
    dma区 的 名称 为 「DMA」。
    dma区 的 起始页 为 0。
    若 总页数 小于 DMA上限页：
        dma区 的 结束页 为 总页数。
    否则：
        dma区 的 结束页 为 DMA上限页。
    完。
    dma区 的 总页数 为 dma区 的 结束页 减 dma区 的 起始页。
    dma区 的 位图 为 做 bitmap_创建 用 dma区 的 总页数。
    dma区 的 空闲页数 为 dma区 的 总页数。
    做 追加 用 页帧区域列表、dma区。
    若 总页数 大于 DMA上限页：
        可变 normal区 为 新建 页帧区域。
        normal区 的 名称 为 「Normal」。
        normal区 的 起始页 为 DMA上限页。
        若 总页数 小于 Normal上限页：
            normal区 的 结束页 为 总页数。
        否则：
            normal区 的 结束页 为 Normal上限页。
        完。
        normal区 的 总页数 为 normal区 的 结束页 减 normal区 的 起始页。
        normal区 的 位图 为 做 bitmap_创建 用 normal区 的 总页数。
        normal区 的 空闲页数 为 normal区 的 总页数。
        做 追加 用 页帧区域列表、normal区。
    完。
    若 总页数 大于 Normal上限页：
        可变 high区 为 新建 页帧区域。
        high区 的 名称 为 「HighMem」。
        high区 的 起始页 为 Normal上限页。
        high区 的 结束页 为 总页数。
        high区 的 总页数 为 high区 的 结束页 减 high区 的 起始页。
        high区 的 位图 为 做 bitmap_创建 用 high区 的 总页数。
        high区 的 空闲页数 为 high区 的 总页数。
        做 追加 用 页帧区域列表、high区。
    完。
    做 打印 用 做 字符串拼接 用 「[页帧位图] 初始化, 总页数: 」、做 转字符串 用 总页数。
    返还 0。
完。

页帧_分配 接受 区域索引：
    若 区域索引 小于 0 或者 区域索引 大于或等于 做 计数 用 页帧区域列表：
        返还 位图无效。
    完。
    区域 为 页帧区域列表 的 第 区域索引 项。
    位 为 做 bitmap_分配一个 用 区域 的 位图。
    若 位 等于 位图无效：
        返还 位图无效。
    完。
    区域 的 空闲页数 为 区域 的 空闲页数 减 1。
    返还 区域 的 起始页 加 位。
完。

页帧_释放 接受 页号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 页帧区域列表：
        区域 为 页帧区域列表 的 第 i 项。
        若 页号 大于或等于 区域 的 起始页 并且 页号 小于 区域 的 结束页：
            本地位 为 页号 减 区域 的 起始页。
            结果 为 做 bitmap_释放一个 用 区域 的 位图、本地位。
            若 结果 等于 0：
                区域 的 空闲页数 为 区域 的 空闲页数 加 1。
            完。
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 错误_无效参数。
完。

页帧_获取空闲页数 接受 区域索引：
    若 区域索引 小于 0 或者 区域索引 大于或等于 做 计数 用 页帧区域列表：
        返还 0。
    完。
    返还 页帧区域列表 的 第 区域索引 项 的 空闲页数。
完。

AI_记录分配 接受 位置：
    若 AI预测位置 小于 AI预测窗口：
        做 设置 用 AI预测历史、AI预测位置、位置。
        AI预测位置 为 (AI预测位置 加 1) 取余于 AI预测窗口。
    完。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
完。

AI_预测下次分配位置 接受 零参：
    可变 加权和 为 0。
    可变 权重和 为 0。
    可变 权重 为 AI预测缩放。
    可变 i 为 0。
    只要 i 小于 AI预测窗口：
        索引 为 (AI预测位置 减 1 减 i 加 AI预测窗口) 取余于 AI预测窗口。
        加权和 为 加权和 加 AI预测历史 的 第 索引 项 乘 权重。
        权重和 为 权重和 加 权重。
        权重 为 权重 乘 AI预测衰减 除以 AI预测缩放。
        若 权重 小于或等于 0：
            权重 为 1。
        完。
        i 为 i 加 1。
    完。
    若 权重和 等于 0：
        返还 0。
    完。
    返还 加权和 除以 权重和。
完。

bitmap_碎片率 接受 bm：
    若 bm 等于 空 或者 bm 的 总位数 等于 0：
        返还 0。
    完。
    若 bm 的 空闲数 等于 0 或者 bm 的 空闲数 等于 bm 的 总位数：
        返还 0。
    完。
    可变 最大连续 为 0。
    可变 当前连续 为 0。
    可变 i 为 0。
    全局统计 的 碎片分析次数 为 全局统计 的 碎片分析次数 加 1。
    只要 i 小于 bm 的 总位数：
        若 做 bitmap_测试 用 bm、i 等于 假：
            当前连续 为 当前连续 加 1。
            若 当前连续 大于 最大连续：
                最大连续 为 当前连续。
            完。
        否则：
            当前连续 为 0。
        完。
        i 为 i 加 1。
    完。
    若 bm 的 空闲数 等于 0：
        返还 0。
    完。
    返还 100 减 最大连续 乘 100 除以 bm 的 空闲数。
完。

bitmap_空闲块数 接受 bm：
    若 bm 等于 空：
        返还 0。
    完。
    可变 块数 为 0。
    可变 在空闲中 为 假。
    可变 i 为 0。
    只要 i 小于 bm 的 总位数：
        若 做 bitmap_测试 用 bm、i 等于 假：
            若 在空闲中 等于 假：
                块数 为 块数 加 1。
                在空闲中 为 真。
            完。
        否则：
            在空闲中 为 假。
        完。
        i 为 i 加 1。
    完。
    返还 块数。
完。

bitmap_转储 接受 bm：
    若 bm 等于 空：
        做 打印 用 「[位图] 空位图」。
        返还 0。
    完。
    做 打印 用 做 字符串拼接 用 「[位图] 总位数: 」、做 转字符串 用 bm 的 总位数。
    做 打印 用 做 字符串拼接 用 「[位图] 已分配: 」、做 转字符串 用 bm 的 已分配数。
    做 打印 用 做 字符串拼接 用 「[位图] 空闲: 」、做 转字符串 用 bm 的 空闲数。
    做 打印 用 做 字符串拼接 用 「[位图] 使用率%: 」、做 转字符串 用 做 bitmap_使用率 用 bm。
    做 打印 用 做 字符串拼接 用 「[位图] 碎片率%: 」、做 转字符串 用 做 bitmap_碎片率 用 bm。
    做 打印 用 做 字符串拼接 用 「[位图] 空闲块数: 」、做 转字符串 用 做 bitmap_空闲块数 用 bm。
    返还 0。
完。

内存位图_打印统计 接受 零参：
    做 打印 用 「[内存位图] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  创建次数: 」、做 转字符串 用 全局统计 的 创建次数。
    做 打印 用 做 字符串拼接 用 「  设置次数: 」、做 转字符串 用 全局统计 的 设置次数。
    做 打印 用 做 字符串拼接 用 「  清除次数: 」、做 转字符串 用 全局统计 的 清除次数。
    做 打印 用 做 字符串拼接 用 「  测试次数: 」、做 转字符串 用 全局统计 的 测试次数。
    做 打印 用 做 字符串拼接 用 「  查找次数: 」、做 转字符串 用 全局统计 的 查找次数。
    做 打印 用 做 字符串拼接 用 「  查找命中: 」、做 转字符串 用 全局统计 的 查找命中次数。
    做 打印 用 做 字符串拼接 用 「  查找未命中: 」、做 转字符串 用 全局统计 的 查找未命中次数。
    做 打印 用 做 字符串拼接 用 「  分配次数: 」、做 转字符串 用 全局统计 的 分配次数。
    做 打印 用 做 字符串拼接 用 「  分配失败: 」、做 转字符串 用 全局统计 的 分配失败次数。
    做 打印 用 做 字符串拼接 用 「  释放次数: 」、做 转字符串 用 全局统计 的 释放次数。
    做 打印 用 做 字符串拼接 用 「  连续分配: 」、做 转字符串 用 全局统计 的 连续分配次数。
    做 打印 用 做 字符串拼接 用 「  对齐分配: 」、做 转字符串 用 全局统计 的 对齐分配次数。
    做 打印 用 做 字符串拼接 用 「  范围操作: 」、做 转字符串 用 全局统计 的 范围操作次数。
    做 打印 用 做 字符串拼接 用 「  集合运算: 」、做 转字符串 用 全局统计 的 集合运算次数。
    做 打印 用 做 字符串拼接 用 「  AI预测次数: 」、做 转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  碎片分析: 」、做 转字符串 用 全局统计 的 碎片分析次数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

内存位图_注册到内核 接受 内核上下文：
    若 已初始化 等于 假：
        返还 错误_未初始化。
    完。
    做 打印 用 「[内存位图] 已注册到内核上下文」。
    返还 0。
完。

内存位图_查询状态 接受 零参：
    若 已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

内存位图_健康检查 接受 零参：
    若 已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 分配次数 加 全局统计 的 释放次数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
