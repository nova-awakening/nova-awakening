《AI信号异常检测》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·链表后继》。

引用《.·文件完整性校验》。

引用《.·时钟中枢》。

引用《.·整数转字符串》。

外部。

进程_设置nice。

外部。

进程_发送信号。

信号_SEGV 为 11。

信号_BUS 为 7。

信号_FPE 为 8。

信号_ABRT 为 6。

信号_ILL 为 4。

信号画像 是 一种 事物，含 pid、致命信号EMA、总信号EMA、连续崩溃数、上次信号时间、窗口内信号数、窗口起始、不稳定、建议动作。

可变 信号画像表 为 一组：（空）。

可变 信号风暴_阈值 为 100。

可变 AI信号_已初始化 为 假。

AI信号_初始化 接受 零参：
    AI信号_已初始化 为 真。
    做 内核信息 用 「AI信号异常检测: 崩溃模式+信号风暴+自动处置」。
完。

AI信号_记录 接受 PID、信号号：
    若 非 AI信号_已初始化：
        返还。
    完。
    画像 为 做 AI信号_获取或创建 用 PID。
    现在 为 做 读取毫秒。
    画像 的 窗口内信号数 为 画像 的 窗口内信号数 加 1。
    若 现在 减 画像 的 窗口起始 大于 1000：
        画像 的 总信号EMA 为 (画像 的 总信号EMA 乘 922 加 画像 的 窗口内信号数 乘 1024 乘 102) 除以 (1024 乘 1024)。
        画像 的 窗口内信号数 为 0。
        画像 的 窗口起始 为 现在。
    完。
    画像 的 上次信号时间 为 现在。
    若 做 AI信号_是致命 用 信号号：
        画像 的 连续崩溃数 为 画像 的 连续崩溃数 加 1。
        画像 的 致命信号EMA 为 (画像 的 致命信号EMA 乘 922 加 1024 乘 102) 除以 1024。
        若 画像 的 连续崩溃数 大于或等于 5：
            画像 的 不稳定 为 1。
            画像 的 建议动作 为 2。
            做 内核警告 用 「AI信号: PID=」 加 做 整数转字符串 用 PID 加 「 连续崩溃」 加 做 整数转字符串 用 画像 的 连续崩溃数 加 「次, 建议杀死」。
        否则若 画像 的 连续崩溃数 大于或等于 3：
            画像 的 不稳定 为 1。
            画像 的 建议动作 为 1。
            做 内核警告 用 「AI信号: PID=」 加 做 整数转字符串 用 PID 加 「 频繁崩溃, 建议降优先级」。
        完。
    否则：
        画像 的 连续崩溃数 为 0。
    完。
    若 画像 的 窗口内信号数 大于 信号风暴_阈值：
        做 内核警告 用 「AI信号: PID=」 加 做 整数转字符串 用 PID 加 「 信号风暴! (」 加 做 整数转字符串 用 画像 的 窗口内信号数 加 「/秒)」。
    完。
完。

AI信号_是致命 接受 信号号：
    返还 信号号 等于 信号_SEGV 或者 信号号 等于 信号_BUS 或者 信号号 等于 信号_FPE 或者 信号号 等于 信号_ABRT 或者 信号号 等于 信号_ILL。
完。

AI信号_获取建议 接受 PID：
    画像 为 做 AI信号_查找 用 PID。
    若 画像 等于 空：
        返还 0。
    完。
    返还 画像 的 建议动作。
完。

AI信号_是不稳定 接受 PID：
    画像 为 做 AI信号_查找 用 PID。
    若 画像 等于 空：
        返还 假。
    完。
    返还 画像 的 不稳定 等于 1。
完。

AI信号_查找 接受 pid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 信号画像表：
        若 信号画像表 的 第 i 项 的 pid 等于 pid：
            返还 信号画像表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

AI信号_获取或创建 接受 pid：
    已有 为 做 AI信号_查找 用 pid。
    若 已有 不等于 空：
        返还 已有。
    完。
    新画像 为 新建 信号画像。
    新画像 的 pid 为 pid。
    新画像 的 窗口起始 为 做 读取毫秒。
    做 追加 用 信号画像表、新画像。
    返还 新画像。
完。

AI信号_进程退出 接受 pid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 信号画像表：
        若 信号画像表 的 第 i 项 的 pid 等于 pid：
            做 链表_删除 用 信号画像表、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

AI信号_执行建议 接受 pid：
    画像 为 做 AI信号_查找 用 pid。
    若 画像 等于 空：
        返还。
    完。
    若 画像 的 建议动作 等于 0：
        返还。
    完。
    若 画像 的 建议动作 等于 1：
        做 进程_设置nice 用 pid、19。
        做 内核信息 用 「AI信号: 已降低PID=」 加 做 整数转字符串 用 pid 加 「优先级」。
        画像 的 建议动作 为 0。
    否则若 画像 的 建议动作 等于 2：
        做 进程_发送信号 用 pid、9。
        做 内核警告 用 「AI信号: 已杀死不稳定进程PID=」 加 做 整数转字符串 用 pid。
        画像 的 建议动作 为 0。
    完。
完。

可变 AI信号_总记录数 为 0。

可变 AI信号_致命信号总数 为 0。

可变 AI信号_风暴检测数 为 0。

AI信号_全局统计 接受 零参：
    可变 不稳定数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 信号画像表：
        若 信号画像表 的 第 i 项 的 不稳定 等于 1：
            不稳定数 为 不稳定数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：做 计数 用 信号画像表、不稳定数、AI信号_总记录数、AI信号_致命信号总数、AI信号_风暴检测数。
完。

AI信号_不稳定进程列表 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 信号画像表：
        若 信号画像表 的 第 i 项 的 不稳定 等于 1：
            做 追加 用 结果、一组：信号画像表 的 第 i 项 的 pid、信号画像表 的 第 i 项 的 连续崩溃数、信号画像表 的 第 i 项 的 建议动作。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

AI信号_重置画像 接受 pid：
    画像 为 做 AI信号_查找 用 pid。
    若 画像 等于 空：
        返还。
    完。
    画像 的 连续崩溃数 为 0。
    画像 的 不稳定 为 0。
    画像 的 建议动作 为 0。
    画像 的 致命信号EMA 为 0。
    画像 的 总信号EMA 为 0。
完。

AI信号_动作名 接受 动作：
    若 动作 等于 0：
        返还 「none」。
    完。
    若 动作 等于 1：
        返还 「throttle」。
    完。
    若 动作 等于 2：
        返还 「Nova运行时实例」。
    完。
    若 动作 等于 3：
        返还 「kill」。
    完。
    返还 「unknown」。
完。

AI信号_完整统计 接受 零参：
    返还 一组：做 计数 用 信号画像表。
完。

本章 收束。
