《负载均衡》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·链表后继》。

引用《.·时钟中枢》。

引用《.·处理亲和性》。

引用《.·域内均衡》。

引用《.·运行队列初始化》。

均衡间隔_ms 为 4。

迁移冷却_ms 为 2。

可变 均衡_检查次数 为 0。

可变 均衡_迁移次数 为 0。

可变 均衡_失败次数 为 0。

可变 idle均衡_次数 为 0。

可变 上次均衡时间 为 0。

触发负载均衡 接受 零参：
    当前时间 为 做 读取毫秒。
    若 当前时间 减 上次均衡时间 小于 均衡间隔_ms：
        返还。
    完。
    上次均衡时间 为 当前时间。
    均衡_检查次数 为 均衡_检查次数 加 1。
    若 非 做 负载统计_需均衡：
        返还。
    完。
    最忙CPU 为 做 负载统计_找最忙CPU。
    最闲CPU 为 做 负载统计_找最闲CPU。
    若 最忙CPU 等于 最闲CPU：
        返还。
    完。
    不均衡 为 做 负载统计_不均衡量。
    若 不均衡 小于或等于 0：
        返还。
    完。
    结果 为 做 迁移任务 用 最忙CPU、最闲CPU。
    若 结果：
        均衡_迁移次数 为 均衡_迁移次数 加 1。
    否则：
        均衡_失败次数 为 均衡_失败次数 加 1。
    完。
完。

idle均衡 接受 idle_CPU：
    idle均衡_次数 为 idle均衡_次数 加 1。
    最忙CPU 为 做 负载统计_找最忙CPU。
    若 最忙CPU 等于 idle_CPU：
        返还 假。
    完。
    返还 做 迁移任务 用 最忙CPU、idle_CPU。
完。

迁移任务 接受 源CPU、目标CPU：
    rq 为 做 获取运行队列 用 源CPU。
    若 rq 等于 空：
        返还 假。
    完。
    可变 候选 为 空。
    可变 最低优先级 为 0。
    任务列表 为 做 运行队列_获取任务 用 rq。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务列表：
        任务 为 任务列表 的 第 i 项。
        若 任务 等于 做 运行队列_当前任务 用 rq：
            i 为 i 加 1。
            继续。
        完。
        若 非 做 可以迁移到 用 任务、目标CPU：
            i 为 i 加 1。
            继续。
        完。
        若 做 任务_最近迁移 用 任务：
            i 为 i 加 1。
            继续。
        完。
        若 做 任务_不可迁移 用 任务：
            i 为 i 加 1。
            继续。
        完。
        候选 为 任务。
        中断。
        i 为 i 加 1。
    完。
    若 候选 等于 空：
        返还 假。
    完。
    做 运行队列_移除任务 用 rq、候选。
    目标rq 为 做 获取运行队列 用 目标CPU。
    若 目标rq 不等于 空：
        做 运行队列_添加任务 用 目标rq、候选。
        做 任务_设置CPU 用 候选、目标CPU。
        做 任务_标记迁移时间 用 候选。
        返还 真。
    完。
    做 运行队列_添加任务 用 rq、候选。
    返还 假。
完。

任务_最近迁移 接受 任务：
    若 任务 等于 空：
        返还 假。
    完。
    当前时间 为 做 读取毫秒。
    上次迁移 为 任务 的 上次迁移时间。
    若 上次迁移 等于 0：
        返还 假。
    完。
    返还 当前时间 减 上次迁移 小于 迁移冷却_ms。
完。

任务_不可迁移 接受 任务：
    若 任务 等于 空：
        返还 真。
    完。
    若 任务 的 标志 不等于 空 并且 任务 的 标志 位与 0x200000 不等于 0：
        返还 真。
    完。
    返还 假。
完。

任务_设置CPU 接受 任务、cpu：
    若 任务 等于 空：
        返还。
    完。
    任务 的 当前CPU 为 cpu。
完。

任务_标记迁移时间 接受 任务：
    若 任务 等于 空：
        返还。
    完。
    任务 的 上次迁移时间 为 做 读取毫秒。
完。

外部。

所有运行队列。

获取运行队列_均衡 接受 cpu：
    若 所有运行队列 等于 空 或者 cpu 大于或等于 做 计数 用 所有运行队列：
        返还 空。
    完。
    返还 所有运行队列 的 第 cpu 项。
完。

运行队列_获取任务 接受 rq：
    若 rq 等于 空：
        返还 一组：（空）。
    完。
    若 rq 的 任务列表 等于 空：
        返还 一组：（空）。
    完。
    返还 rq 的 任务列表。
完。

运行队列_当前任务 接受 rq：
    若 rq 等于 空：
        返还 空。
    完。
    返还 rq 的 当前任务。
完。

运行队列_移除任务 接受 rq、任务：
    若 rq 等于 空 或者 任务 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 rq 的 任务列表：
        若 rq 的 任务列表 的 第 i 项 等于 任务：
            做 链表_删除 用 rq 的 任务列表、i。
            rq 的 任务数 为 rq 的 任务数 减 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

运行队列_添加任务 接受 rq、任务：
    若 rq 等于 空 或者 任务 等于 空：
        返还。
    完。
    做 追加 用 rq 的 任务列表、任务。
    rq 的 任务数 为 rq 的 任务数 加 1。
完。

选择唤醒CPU 接受 任务：
    上次CPU 为 做 进程_上次CPU 用 任务。
    最闲CPU 为 做 负载统计_找最闲CPU。
    上次负载 为 做 获取CPU利用率 用 上次CPU。
    若 上次负载 小于 50：
        返还 上次CPU。
    完。
    返还 最闲CPU。
完。

进程_上次CPU 接受 任务：
    若 任务 等于 空：
        返还 0。
    完。
    若 任务 的 当前CPU 不等于 空：
        返还 任务 的 当前CPU。
    完。
    返还 0。
完。

负载均衡_统计 接受 零参：
    返还 一组：均衡_检查次数、均衡_迁移次数、均衡_失败次数、idle均衡_次数。
完。

负载均衡_迁移成功率 接受 零参：
    总 为 均衡_迁移次数 加 均衡_失败次数。
    若 总 等于 0：
        返还 100。
    完。
    返还 均衡_迁移次数 乘 100 除以 总。
完。

负载均衡_设置间隔 接受 ms：
    若 ms 小于 1：
        返还。
    完。
完。

负载均衡_完整统计 接受 零参：
    返还 一组：均衡_检查次数、均衡_迁移次数、均衡_失败次数、idle均衡_次数、做 负载均衡_迁移成功率。
完。

负载均衡_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 均衡_失败次数 大于 100：
        做 追加 用 问题、「均衡_失败次数=」 加 均衡_失败次数。
    完。
    返还 做 计数 用 问题 等于 0。
完。

负载均衡_综合报告 接受 零参：
    做 打印 用 「[负载均衡] ═══ 综合报告 ═══」。
    做 打印 用 「  均衡_检查次数: 」 加 均衡_检查次数。
    做 打印 用 「  均衡_迁移次数: 」 加 均衡_迁移次数。
    做 打印 用 「  均衡_失败次数: 」 加 均衡_失败次数。
    做 打印 用 「  idle均衡_次数: 」 加 idle均衡_次数。
    做 打印 用 「  健康: 」 加 若 做 负载均衡_健康评估 则 「正常」 否则 「异常」。
完。

负载均衡_全局重置 接受 零参：
    均衡_检查次数 为 0。
    均衡_迁移次数 为 0。
    均衡_失败次数 为 0。
    idle均衡_次数 为 0。
    做 打印 用 「[负载均衡] 统计已重置」。
    返还 0。
完。

本章 收束。
