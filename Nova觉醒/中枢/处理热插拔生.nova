《处理热插拔生》

※ NovaOS CPU热插拔支持 - 生产级实现
※ 支持CPU动态上线/下线，任务迁移，负载重平衡

引用《.·类型定义》。
引用《.·同步原语》。
引用《..·执行·内核智能》。

引用《.·内存物理》。
引用《.·进程任务》。
引用《.·进程调度》。
引用《.·统一调度器核》。
引用《.·同步原语》。
引用《.·哈希函数》。
引用《.·整数转字符串》。
引用《.·域内均衡》。
引用《.·同步原语》。
引用《.·负载均衡》。
引用《.·每处理器》。

※ CPU状态
枚举 CPU状态 ：
    CPU离线 为 0,。
    CPU上线中 为 1,。
    CPU在线 为 2,。
    CPU下线中 为 3,。
完。

※ CPU热插拔事件
枚举 热插拔事件 ：
    事件_CPU上线前 为 0,。
    事件_CPU上线后 为 1,。
    事件_CPU下线前 为 2,。
    事件_CPU下线后 为 3,。
完。

※ 热插拔回调函数
类型 热插拔回调 = 做 函数 用 CPU: 整数、事件: 热插拔事件: 整数。

※ 热插拔通知链
热插拔通知节点 是 一种 事物：
    回调。
    优先级。
    下一个。
完。

※ CPU热插拔管理器
热插拔管理器 是 一种 事物：
    锁。

    ※ CPU状态数组
    CPU状态数组。
    ※ 在线CPU位图
    在线CPU位图。
    活跃CPU位图。

    ※ 通知链
    通知链头。
    ※ 统计
    上线次数。
    下线次数。
    迁移任务数。
完。

变量 全局热插拔管理器: 热插拔管理器。

※ 初始化CPU热插拔管理器
初始化CPU热插拔 接受 )：
    做 初始化读写锁 用 &全局热插拔管理器.锁。

    ※ 初始化所有CPU为离线状态
    对于 i 为 0; i 小于 最大CPU数; i++ ：
        全局热插拔管理器.CPU状态数组[i] 为 CPU离线。
    完。

    ※ 标记启动CPU为在线
    CPU数量 为 做 获取CPU数量。
    对于 i 为 0; i 小于 CPU数量; i++ ：
        全局热插拔管理器.CPU状态数组[i] 为 CPU在线。
        全局热插拔管理器.在线CPU位图 位或赋 (1 左移 i)。
        全局热插拔管理器.活跃CPU位图 位或赋 (1 左移 i)。
    完。

    做 打印 用 「CPU热插拔管理器初始化完成: %d个CPU在线\n」、CPU数量。
完。

※ 注册热插拔通知回调
注册热插拔通知 接受 回调、优先级)：
    节点 为 做 分配内存 用 做 大小为 用 热插拔通知节点。
    若 节点 等于 空：
        返还 -错误码_内存不足。
    完。

    节点.回调 为 回调。
    节点.优先级 为 优先级。

    做 写锁加锁 用 &全局热插拔管理器.锁。

    ※ 按优先级插入（高优先级在前）
    若 全局热插拔管理器.通知链头 等于 空 或：
       全局热插拔管理器.通知链头.优先级 小于 优先级 ：
        节点.下一个 为 全局热插拔管理器.通知链头。
        全局热插拔管理器.通知链头 为 节点。
    否则：
        当前 为 全局热插拔管理器.通知链头。
        只要 当前.下一个 不等于 空 且 当前.下一个.优先级 不小于 优先级：
            当前 = 当前.下一个。
        完。
        节点.下一个 为 当前.下一个。
        当前.下一个 = 节点。
    完。

    做 写锁解锁 用 &全局热插拔管理器.锁。
    返还 0。
完。

※ 通知热插拔事件
通知热插拔事件 接受 CPU、事件)：
    做 读锁加锁 用 &全局热插拔管理器.锁。

    当前 为 全局热插拔管理器.通知链头。
    只要 当前 不等于 空：
        结果 为 当前.做 回调 用 CPU、事件。
        若 结果 不等于 0：
            做 读锁解锁 用 &全局热插拔管理器.锁。
            返还 结果。
        完。
        当前 = 当前.下一个。
    完。

    做 读锁解锁 用 &全局热插拔管理器.锁。
    返还 0。
完。

※ CPU上线
CPU上线 接受 CPU)：
    若 CPU 小于 0 或 CPU 不小于 最大CPU数：
        返还 -错误码_无效参数。
    完。

    做 写锁加锁 用 &全局热插拔管理器.锁。

    ※ 检查CPU状态
    若 全局热插拔管理器.CPU状态数组[CPU] 不等于 CPU离线：
        做 写锁解锁 用 &全局热插拔管理器.锁。
        返还 -错误码_忙碌。
    完。

    全局热插拔管理器.CPU状态数组[CPU] 为 CPU上线中。
    做 写锁解锁 用 &全局热插拔管理器.锁。

    做 打印 用 「CPU %d 开始上线...\n」、CPU。

    ※ 通知上线前事件
    结果 为 做 通知热插拔事件 用 CPU、事件_CPU上线前。
    若 结果 不等于 0：
        全局热插拔管理器.CPU状态数组[CPU] 为 CPU离线。
        返还 结果。
    完。

    ※ 初始化CPU的运行队列
    运行队列 为 做 获取CPU运行队列 用 CPU。
    做 自旋锁加锁 用 &运行队列.锁。

    运行队列.在线 为 真。
    运行队列.活跃 为 真。
    运行队列.时钟滴答 为 0。
    运行队列.调度次数 为 0。
    运行队列.上下文切换次数 为 0。

    ※ 创建空闲任务
    若 运行队列.空闲任务 等于 空：
        运行队列.空闲任务 为 做 创建空闲任务 用 CPU。
    完。

    做 自旋锁解锁 用 &运行队列.锁。

    ※ 更新位图
    做 写锁加锁 用 &全局热插拔管理器.锁。
    全局热插拔管理器.在线CPU位图 位或赋 (1 左移 CPU)。
    全局热插拔管理器.活跃CPU位图 位或赋 (1 左移 CPU)。
    全局热插拔管理器.CPU状态数组[CPU] 为 CPU在线。
    全局热插拔管理器.上线次数[CPU]++。
    做 写锁解锁 用 &全局热插拔管理器.锁。

    ※ 通知上线后事件
    做 通知热插拔事件 用 CPU、事件_CPU上线后。

    ※ 触发负载均衡
    做 触发负载均衡 用 CPU。

    做 打印 用 「CPU %d 上线完成\n」、CPU。
    返还 0。
完。

※ CPU下线
CPU下线 接受 CPU)：
    若 CPU 小于 0 或 CPU 不小于 最大CPU数：
        返还 -错误码_无效参数。
    完。

    ※ 不能下线最后一个CPU
    若 做 统计在线CPU数 不大于 1：
        返还 -错误码_忙碌。
    完。

    做 写锁加锁 用 &全局热插拔管理器.锁。

    ※ 检查CPU状态
    若 全局热插拔管理器.CPU状态数组[CPU] 不等于 CPU在线：
        做 写锁解锁 用 &全局热插拔管理器.锁。
        返还 -错误码_无效状态。
    完。

    全局热插拔管理器.CPU状态数组[CPU] 为 CPU下线中。
    全局热插拔管理器.活跃CPU位图 位与赋 ~(1 左移 CPU)。
    做 写锁解锁 用 &全局热插拔管理器.锁。

    做 打印 用 「CPU %d 开始下线...\n」、CPU。

    ※ 通知下线前事件
    结果 为 做 通知热插拔事件 用 CPU、事件_CPU下线前。
    若 结果 不等于 0：
        全局热插拔管理器.CPU状态数组[CPU] 为 CPU在线。
        全局热插拔管理器.活跃CPU位图 位或赋 (1 左移 CPU)。
        返还 结果。
    完。

    ※ 迁移所有任务到其他CPU
    迁移数量 为 做 迁移CPU所有任务 用 CPU。
    全局热插拔管理器.迁移任务数[CPU] 累加 迁移数量。

    ※ 停止CPU的运行队列
    运行队列 为 做 获取CPU运行队列 用 CPU。
    做 自旋锁加锁 用 &运行队列.锁。
    运行队列.在线 为 假。
    运行队列.活跃 为 假。
    做 自旋锁解锁 用 &运行队列.锁。

    ※ 更新位图
    做 写锁加锁 用 &全局热插拔管理器.锁。
    全局热插拔管理器.在线CPU位图 位与赋 ~(1 左移 CPU)。
    全局热插拔管理器.CPU状态数组[CPU] 为 CPU离线。
    全局热插拔管理器.下线次数[CPU]++。
    做 写锁解锁 用 &全局热插拔管理器.锁。

    ※ 通知下线后事件
    做 通知热插拔事件 用 CPU、事件_CPU下线后。

    做 打印 用 「CPU %d 下线完成，迁移了 %d 个任务\n」、CPU、迁移数量。
    返还 0。
完。

※ 迁移CPU的所有任务
迁移CPU所有任务 接受 源CPU)：
    源队列 为 做 获取CPU运行队列 用 源CPU。
    迁移数量 为 做 无符号长整数 用 0。

    ※ 选择目标CPU（负载最轻的在线CPU）
    目标CPU 为 做 选择迁移目标CPU 用 源CPU。
    若 目标CPU 小于 0：
        做 打印 用 「错误：无法找到迁移目标CPU\n」。
        返还 0。
    完。

    目标队列 为 做 获取CPU运行队列 用 目标CPU。

    ※ 迁移Nova公平调度任务
    做 自旋锁加锁 用 &源队列.锁。
    Nova公平调度任务列表 为 做 收集Nova公平调度任务 用 &源队列.Nova公平调度队列。
    做 自旋锁解锁 用 &源队列.锁。

    对于 _, 任务 为 范围 Nova公平调度任务列表 ：
        若 做 迁移任务到CPU 用 任务、目标CPU 等于 0：
            迁移数量++。
        完。
    完。

    ※ 迁移实时任务
    做 自旋锁加锁 用 &源队列.锁。
    实时任务列表 为 做 收集实时任务 用 &源队列.实时队列。
    做 自旋锁解锁 用 &源队列.锁。

    对于 _, 任务 为 范围 实时任务列表 ：
        若 做 迁移任务到CPU 用 任务、目标CPU 等于 0：
            迁移数量++。
        完。
    完。

    ※ 迁移Deadline任务
    做 自旋锁加锁 用 &源队列.锁。
    Deadline任务列表 为 做 收集Deadline任务 用 &源队列.Deadline队列。
    做 自旋锁解锁 用 &源队列.锁。

    对于 _, 任务 为 范围 Deadline任务列表 ：
        若 做 迁移任务到CPU 用 任务、目标CPU 等于 0：
            迁移数量++。
        完。
    完。

    返还 迁移数量。
完。

※ 选择迁移目标CPU
选择迁移目标CPU 接受 排除CPU)：
    最小负载 为 无符号长整数最大值。
    最佳CPU 为 -1。

    对于 i 为 0; i 小于 全局调度器.CPU数量; i++ ：
        若 i 等于 排除CPU：
            继续。
        完。

        若 全局热插拔管理器.活跃CPU位图 位与 (1 左移 i)) 等于 0：
            继续。
        完。

        运行队列 为 做 获取CPU运行队列 用 i。
        若 运行队列.负载.权重 小于 最小负载：
            最小负载 为 运行队列.负载.权重。
            最佳CPU 为 i。
        完。
    完。

    返还 最佳CPU。
完。

※ 迁移任务到指定CPU
迁移任务到CPU 接受 任务、目标CPU)：
    若 任务 等于 空 或 目标CPU 小于 0：
        返还 -错误码_无效参数。
    完。

    源CPU 为 任务.CPU。
    若 源CPU 等于 目标CPU：
        返还 0。
    完。

    源队列 为 做 获取CPU运行队列 用 源CPU。
    目标队列 为 做 获取CPU运行队列 用 目标CPU。

    ※ 双锁保护（避免死锁：总是先锁编号小的CPU）
    若 源CPU 小于 目标CPU：
        做 自旋锁加锁 用 &源队列.锁。
        做 自旋锁加锁 用 &目标队列.锁。
    否则：
        做 自旋锁加锁 用 &目标队列.锁。
        做 自旋锁加锁 用 &源队列.锁。
    完。

    ※ 从源队列出队
    做 出队任务 用 任务、出队标志_迁移。

    ※ 更新任务CPU
    任务.CPU 为 目标CPU。
    任务.上次运行CPU 为 源CPU。

    ※ 入队到目标队列
    做 入队任务 用 任务、入队标志_迁移。

    做 自旋锁解锁 用 &目标队列.锁。
    做 自旋锁解锁 用 &源队列.锁。

    返还 0。
完。

※ 统计在线CPU数量
统计在线CPU数 接受 )：
    计数 为 0。
    位图 为 全局热插拔管理器.在线CPU位图。

    只要 位图 不等于 0：
        若 位图 位与 1) 不等于 0：
            计数++。
        完。
        位图 右移赋 1。
    完。

    返还 计数。
完。

※ 检查CPU是否在线
CPU是否在线 接受 CPU)：
    若 CPU 小于 0 或 CPU 不小于 最大CPU数：
        返还 假。
    完。
    返还 (全局热插拔管理器.在线CPU位图 位与 (1 左移 CPU)) 不等于 0。
完。

※ 检查CPU是否活跃
CPU是否活跃 接受 CPU)：
    若 CPU 小于 0 或 CPU 不小于 最大CPU数：
        返还 假。
    完。
    返还 (全局热插拔管理器.活跃CPU位图 位与 (1 左移 CPU)) 不等于 0。
完。

※ 获取在线CPU列表
获取在线CPU列表 接受 )：
    列表 为 做 创建整数数组 用 最大CPU数。
    计数 为 0。

    对于 i 为 0; i 小于 最大CPU数; i++ ：
        若 CPU是否在线(i：
            列表[计数] 为 i。
            计数++。
        完。
    完。

    返还 列表[:计数]。
完。

※ 获取热插拔统计信息
获取热插拔统计 接受 CPU)：
    若 CPU 小于 0 或 CPU 不小于 最大CPU数：
        返还 热插拔统计{}。
    完。

    返还 热插拔统计：
        CPU编号。
        状态: 全局热插拔管理器.CPU状态数组[CPU],。
        上线次数: 全局热插拔管理器.上线次数[CPU],。
        下线次数: 全局热插拔管理器.下线次数[CPU],。
        迁移任务数: 全局热插拔管理器.迁移任务数[CPU],。
    完。
完。

热插拔统计 是 一种 事物：
    CPU编号。
    状态。
    上线次数。
    下线次数。
    迁移任务数。
完。

※ 辅助函数：收集各类任务
收集Nova公平调度任务 接受 Nova公平调度队列)：
    ※ 实现略：遍历Nova公平调度红黑树收集所有任务
    返还 []*任务结构体{}。
完。

收集实时任务 接受 实时队列)：
    ※ 实现略：遍历实时优先级队列收集所有任务
    返还 []*任务结构体{}。
完。

收集Deadline任务 接受 Deadline队列)：
    ※ 实现略：遍历Deadline红黑树收集所有任务
    返还 []*任务结构体{}。
完。

※ NHPT — Nova Hotplug Processing Theory
※ 公理1(迁移守恒): 下线CPU上的所有任务必须无损迁移到在线CPU
※ 公理2(阶段原子): 每个热插拔阶段必须成功否则回滚全部
※ 公理3(位图一致): 在线/活跃位图必须与实际状态一致
※ 定理1(平衡保证): 迁移后CPU间负载不严重失衡
※ 定理2(最后一个保护): 系统必须始终保留至少一个在线CPU

热插拔_打印统计 接受 )：
    做 内核信息 用 「=== 热插拔统计 ===」。
    做 内核信息 用 「在线CPU数: 」 加 做 整数转字符串 用 做 统计在线CPU数。
    做 内核信息 用 「在线位图: 0x」 加 做 整数转十六 用 全局热插拔管理器.在线CPU位图。
    做 内核信息 用 「活跃位图: 0x」 加 做 整数转十六 用 全局热插拔管理器.活跃CPU位图。

    对于 i 为 0; i 小于 最大CPU数; i++ ：
        若 CPU是否在线(i：
            s 为 做 获取热插拔统计 用 i。
            内核信息(「  CPU」 加 整数转字符串(i)。
                + 「: 上线=」 加 做 整数转字符串 用 s.上线次数。
                + 「 下线=」 加 做 整数转字符串 用 s.下线次数。
                + 「 迁移=」 加 做 整数转字符串 用 s.迁移任务数)。
        完。
    完。
完。

热插拔_可否下线 接受 CPU)：
    ※ 必须保留至少一个在线CPU
    若 做 统计在线CPU数 不大于 1：
        做 内核警告 用 「热插拔: 不能下线最后一个CPU」。
        返还 假。
    完。
    若 !CPU是否在线(CPU：
        返还 假。
    完。
    返还 真。
完。

热插拔_重置统计 接受 )：
    对于 i 为 0; i 小于 最大CPU数; i++ ：
        全局热插拔管理器.上线次数[i] 为 0。
        全局热插拔管理器.下线次数[i] 为 0。
        全局热插拔管理器.迁移任务数[i] 为 0。
    完。
完。

※ 获取活跃CPU列表
获取活跃CPU列表 接受 )：
    列表 为 做 创建整数数组 用 最大CPU数。
    计数 为 0。
    对于 i 为 0; i 小于 最大CPU数; i++ ：
        若 CPU是否活跃(i：
            列表[计数] 为 i。
            计数++。
        完。
    完。
    返还 列表[:计数]。
完。

※ §11 谐振CPU热插拔负载迁移器
※ 在CPU下线前预测并迁移负载到最优目标CPU

可变 _谐振迁移_总次数 为 0。
可变 _谐振迁移_任务数 为 0。

谐振热插拔_选择目标CPU 接受 下线CPU、在线CPU列表、CPU数)：
    ※ 选择负载最低的在线CPU
    可变 最优CPU 为 -1。
    可变 最低负载 为 999999。
    可变 i 为 0。
    只要 i 小于 CPU数：
        若 在线CPU列表[i] 不等于 下线CPU：
            负载 为 做 获取CPU负载 用 在线CPU列表[i]。
            若 负载 小于 最低负载：
                最低负载 为 负载。
                最优CPU 为 在线CPU列表[i]。
            完。
        完。
        i 为 i 加 1。
    完。
    _谐振迁移_总次数 为 _谐振迁移_总次数 加 1。
    返还 最优CPU。
完。

热插拔_完整统计 接受 )：
    返还 [。
        「谐振迁移总次数」, _谐振迁移_总次数,。
        「谐振迁移任务数」, _谐振迁移_任务数。
    ]。
完。

本章 收束。
