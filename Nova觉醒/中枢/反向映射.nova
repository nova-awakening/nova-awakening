《反向映射》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·页表索引》。

引用《.·页表遍历》。

引用《.·链表后继》。

引用《..·引导·端口IO》。

引用《.·映射页》。

引用《.·双向链表操作》。

引用《.·虚拟内存区》。

引用《.·物理页描述符》。

AV_链表 为 0。

AV_引用 为 1。

AV_VMA数 为 2。

匿名VMA链_创建 接受 零参：
    返还 一组：做 链表_创建、1、0。
完。

匿名VMA链_添加 接受 av、vma：
    做 链表_尾插 用 av 的 第 AV_链表 项、vma。
    做 设置 用 av、AV_VMA数、av 的 第 AV_VMA数 项 加 1。
完。

匿名VMA链_移除 接受 av、vma：
    头 为 av 的 第 AV_链表 项。
    可变 当前 为 头 的 第 1 项。
    只要 当前 不等于 头：
        若 当前 的 第 2 项 等于 vma：
            做 链表_删除 用 当前。
            做 设置 用 av、AV_VMA数、av 的 第 AV_VMA数 项 减 1。
            返还。
        完。
        当前 为 当前 的 第 1 项。
    完。
完。

匿名VMA链_增引用 接受 av：
    做 设置 用 av、AV_引用、av 的 第 AV_引用 项 加 1。
完。

匿名VMA链_减引用 接受 av：
    做 设置 用 av、AV_引用、av 的 第 AV_引用 项 减 1。
    返还 av 的 第 AV_引用 项。
完。

rmap_添加匿名映射 接受 页号、vma、虚拟地址：
    页 为 做 页描述符_获取 用 页号。
    若 页 等于 空：
        返还。
    完。
    做 设置 用 页、PG_映射、vma。
    做 页描述符_增引用 用 页。
完。

rmap_添加文件映射 接受 页号、vma：
    页 为 做 页描述符_获取 用 页号。
    若 页 等于 空：
        返还。
    完。
    做 设置 用 页、PG_映射、vma。
    做 页描述符_增引用 用 页。
完。

rmap_移除映射 接受 页号：
    页 为 做 页描述符_获取 用 页号。
    若 页 等于 空：
        返还。
    完。
    剩余 为 做 页描述符_减引用 用 页。
    若 剩余 小于或等于 0：
        做 设置 用 页、PG_映射、0。
    完。
完。

rmap_遍历匿名 接受 页号、回调、用户数据：
    页 为 做 页描述符_获取 用 页号。
    若 页 等于 空 或者 页 的 第 PG_映射 项 等于 0：
        返还 0。
    完。
    vma 为 页 的 第 PG_映射 项。
    物理地址 为 页号 乘 页大小。
    mm 为 做 VMA_获取MM 用 vma。
    若 mm 等于 空：
        返还 0。
    完。
    pml4 为 mm 的 第 MM_PML4物理 项。
    可变 地址 为 vma 的 第 VMA_起始 项。
    只要 地址 小于 vma 的 第 VMA_结束 项：
        pte 为 做 页表_查找PTE 用 pml4、地址。
        若 做 页表项_存在 用 pte 并且 做 页表项_地址 用 pte 等于 物理地址：
            pte_位置 为 做 页表_查找PTE位置 用 pml4、地址。
            结果 为 做 回调 用 pte_位置、mm、vma、用户数据。
            若 结果 不等于 0：
                返还 结果。
            完。
        完。
        地址 为 地址 加 页大小。
    完。
    返还 0。
完。

rmap_取消映射回调 接受 pte_位置、mm、vma、用户数据：
    若 pte_位置 等于 0：
        返还 0。
    完。
    旧pte 为 做 内存读64 用 pte_位置。
    若 非 做 页表项_存在 用 旧pte：
        返还 0。
    完。
    做 内存写64 用 pte_位置、0。
    做 设置 用 mm、MM_RSS、mm 的 第 MM_RSS 项 减 1。
    返还 0。
完。

rmap_尝试取消全部映射 接受 页号：
    返还 做 rmap_遍历匿名 用 页号、rmap_取消映射回调、空。
完。

可变 引用检查_计数 为 0。

rmap_引用检查回调 接受 pte_位置、mm、vma、用户数据：
    若 pte_位置 等于 0：
        返还 0。
    完。
    pte 为 做 内存读64 用 pte_位置。
    若 做 页表项_存在 用 pte 并且 做 页表项_已访问 用 pte：
        引用检查_计数 为 引用检查_计数 加 1。
        新pte 为 pte 位与 按位取反 PTE_已访问。
        做 内存写64 用 pte_位置、新pte。
    完。
    返还 0。
完。

rmap_页面引用数 接受 页号：
    引用检查_计数 为 0。
    做 rmap_遍历匿名 用 页号、rmap_引用检查回调、空。
    返还 引用检查_计数。
完。

rmap_映射数量 接受 页号：
    页 为 做 页描述符_获取 用 页号。
    若 页 等于 空：
        返还 0。
    完。
    返还 页 的 第 PG_引用 项。
完。

rmap_迁移页 接受 旧页号、新页号：
    旧地址 为 旧页号 乘 页大小。
    新地址 为 新页号 乘 页大小。
    做 内存拷贝 用 新地址、旧地址、页大小。
    做 rmap_遍历匿名 用 旧页号、rmap_迁移回调、新页号。
    旧页 为 做 页描述符_获取 用 旧页号。
    新页 为 做 页描述符_获取 用 新页号。
    若 旧页 不等于 空 并且 新页 不等于 空：
        做 设置 用 新页、PG_映射、旧页 的 第 PG_映射 项。
        做 设置 用 旧页、PG_映射、0。
    完。
完。

rmap_迁移回调 接受 pte_位置、mm、vma、新页号：
    若 pte_位置 等于 0：
        返还 0。
    完。
    旧pte 为 做 内存读64 用 pte_位置。
    若 非 做 页表项_存在 用 旧pte：
        返还 0。
    完。
    标志 为 旧pte 位与 0xFFF。
    新pte 为 新页号 乘 页大小 位或 标志。
    做 内存写64 用 pte_位置、新pte。
    返还 0。
完。

rmap_统计 接受 零参：
    返还 一组：引用检查_计数。
完。

可变 rmap_添加匿名次数 为 0。

可变 rmap_添加文件次数 为 0。

可变 rmap_移除次数 为 0。

rmap_映射类型名 接受 页号：
    页 为 做 页描述符_获取 用 页号。
    若 页 等于 空：
        返还 「none」。
    完。
    若 页 的 第 PG_映射 项 等于 0：
        返还 「unmapped」。
    完。
    返还 「mapped」。
完。

rmap_完整统计 接受 零参：
    返还 一组：rmap_添加匿名次数、rmap_添加文件次数、rmap_移除次数、引用检查_计数。
完。

可变 AI_反向映射_运行指标 为 0。

可变 AI_反向映射_异常指标 为 0。

AI_反向映射_自诊断 接受 零参：
    AI_反向映射_运行指标 为 引用检查_计数。
    返还 AI_反向映射_运行指标。
完。

AI_反向映射_优化建议 接受 零参：
    指标 为 做 AI_反向映射_自诊断。
    若 AI_反向映射_运行指标 大于 10000：
        返还 「反向映射负载较高」。
    完。
    返还 「反向映射状态正常」。
完。

本章 收束。
