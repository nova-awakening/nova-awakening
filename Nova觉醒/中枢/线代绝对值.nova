《线代绝对值》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·日志中枢》。

引用《.·整数转字符串》。

线代精度 为 1000。

线代_绝对值 接受 x：
    若 x 小于 0：
        返还 0 减 x。
    完。
    返还 x。
完。

高斯消元 接受 A、b：
    n 为 A 的 第 0 项。
    可变 aug 为 一组：n、n 加 1。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 n：
            做 追加 用 aug、A 的 第 2 加 i 乘 n 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 aug、b 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 col 为 0。
    只要 col 小于 n：
        可变 主元行 为 col。
        可变 主元值 为 做 线代_绝对值 用 aug 的 第 2 加 col 乘 (n 加 1) 加 col 项。
        i 为 col 加 1。
        只要 i 小于 n：
            v 为 做 线代_绝对值 用 aug 的 第 2 加 i 乘 (n 加 1) 加 col 项。
            若 v 大于 主元值：
                主元值 为 v。
                主元行 为 i。
            完。
            i 为 i 加 1。
        完。
        若 主元值 等于 0：
            返还 空。
        完。
        若 主元行 不等于 col：
            可变 j2 为 0。
            只要 j2 小于或等于 n：
                临时 为 aug 的 第 2 加 col 乘 (n 加 1) 加 j2 项。
                aug 的 第 2 加 col 乘 (n 加 1) 加 j2 项 为 aug 的 第 2 加 主元行 乘 (n 加 1) 加 j2 项。
                aug 的 第 2 加 主元行 乘 (n 加 1) 加 j2 项 为 临时。
                j2 为 j2 加 1。
            完。
        完。
        i 为 col 加 1。
        只要 i 小于 n：
            因子 为 aug 的 第 2 加 i 乘 (n 加 1) 加 col 项 乘 线代精度 除以 aug 的 第 2 加 col 乘 (n 加 1) 加 col 项。
            可变 j3 为 col。
            只要 j3 小于或等于 n：
                aug 的 第 2 加 i 乘 (n 加 1) 加 j3 项 为 aug 的 第 2 加 i 乘 (n 加 1) 加 j3 项 减 因子 乘 aug 的 第 2 加 col 乘 (n 加 1) 加 j3 项 除以 线代精度。
                j3 为 j3 加 1。
            完。
            i 为 i 加 1。
        完。
        col 为 col 加 1。
    完。
    可变 x 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 x、0。
        i 为 i 加 1。
    完。
    i 为 n 减 1。
    只要 i 大于或等于 0：
        可变 和 为 aug 的 第 2 加 i 乘 (n 加 1) 加 n 项。
        可变 j4 为 i 加 1。
        只要 j4 小于 n：
            和 为 和 减 aug 的 第 2 加 i 乘 (n 加 1) 加 j4 项 乘 x 的 第 j4 项 除以 线代精度。
            j4 为 j4 加 1。
        完。
        若 aug 的 第 2 加 i 乘 (n 加 1) 加 i 项 等于 0：
            返还 空。
        完。
        x 的 第 i 项 为 和 乘 线代精度 除以 aug 的 第 2 加 i 乘 (n 加 1) 加 i 项。
        i 为 i 减 1。
    完。
    返还 x。
完。

lu分解 接受 A：
    n 为 A 的 第 0 项。
    可变 L 为 一组：n、n。
    可变 U 为 一组：n、n。
    可变 i 为 0。
    只要 i 小于 n 乘 n：
        做 追加 用 L、0。
        做 追加 用 U、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 j 为 i。
        只要 j 小于 n：
            可变 和 为 A 的 第 2 加 i 乘 n 加 j 项。
            可变 k 为 0。
            只要 k 小于 i：
                和 为 和 减 L 的 第 2 加 i 乘 n 加 k 项 乘 U 的 第 2 加 k 乘 n 加 j 项 除以 线代精度。
                k 为 k 加 1。
            完。
            U 的 第 2 加 i 乘 n 加 j 项 为 和。
            j 为 j 加 1。
        完。
        j 为 i。
        只要 j 小于 n：
            若 i 等于 j：
                L 的 第 2 加 i 乘 n 加 i 项 为 线代精度。
            否则：
                可变 和2 为 A 的 第 2 加 j 乘 n 加 i 项。
                可变 k2 为 0。
                只要 k2 小于 i：
                    和2 为 和2 减 L 的 第 2 加 j 乘 n 加 k2 项 乘 U 的 第 2 加 k2 乘 n 加 i 项 除以 线代精度。
                    k2 为 k2 加 1。
                完。
                若 U 的 第 2 加 i 乘 n 加 i 项 不等于 0：
                    L 的 第 2 加 j 乘 n 加 i 项 为 和2 乘 线代精度 除以 U 的 第 2 加 i 乘 n 加 i 项。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：L、U。
完。

线代_符号 接受 x：
    若 x 大于 0：
        返还 1。
    完。
    若 x 小于 0：
        返还 0 减 1。
    完。
    返还 0。
完。

线代_最大 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

线代_最小 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

线代_夹紧 接受 x、最小、最大：
    若 x 小于 最小：
        返还 最小。
    完。
    若 x 大于 最大：
        返还 最大。
    完。
    返还 x。
完。

线代_整数开方 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

矩阵_创建 接受 行、列：
    若 行 小于或等于 0 或者 列 小于或等于 0：
        返还 空。
    完。
    全局统计 的 矩阵创建数 为 全局统计 的 矩阵创建数 加 1。
    可变 m 为 一组：行、列。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        做 追加 用 m、0。
        i 为 i 加 1。
    完。
    返还 m。
完。

矩阵_获取 接受 m、行、列：
    若 m 等于 空：
        返还 0。
    完。
    返还 m 的 第 2 加 行 乘 m 的 第 1 项 加 列 项。
完。

矩阵_设置 接受 m、行、列、值：
    若 m 等于 空：
        返还。
    完。
    做 设置 用 m、2 加 行 乘 m 的 第 1 项 加 列、值。
完。

矩阵_加法 接受 A、B：
    若 A 等于 空 或者 B 等于 空：
        返还 空。
    完。
    若 A 的 第 0 项 不等于 B 的 第 0 项 或者 A 的 第 1 项 不等于 B 的 第 1 项：
        返还 空。
    完。
    全局统计 的 矩阵加法数 为 全局统计 的 矩阵加法数 加 1。
    可变 C 为 做 矩阵_创建 用 A 的 第 0 项、A 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 A 的 第 0 项 乘 A 的 第 1 项：
        做 设置 用 C、2 加 i、A 的 第 2 加 i 项 加 B 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    返还 C。
完。

矩阵_减法 接受 A、B：
    若 A 等于 空 或者 B 等于 空：
        返还 空。
    完。
    若 A 的 第 0 项 不等于 B 的 第 0 项 或者 A 的 第 1 项 不等于 B 的 第 1 项：
        返还 空。
    完。
    全局统计 的 矩阵减法数 为 全局统计 的 矩阵减法数 加 1。
    可变 C 为 做 矩阵_创建 用 A 的 第 0 项、A 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 A 的 第 0 项 乘 A 的 第 1 项：
        做 设置 用 C、2 加 i、A 的 第 2 加 i 项 减 B 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    返还 C。
完。

矩阵_标量乘 接受 A、标量：
    若 A 等于 空：
        返还 空。
    完。
    可变 C 为 做 矩阵_创建 用 A 的 第 0 项、A 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 A 的 第 0 项 乘 A 的 第 1 项：
        做 设置 用 C、2 加 i、A 的 第 2 加 i 项 乘 标量 除以 线代精度。
        i 为 i 加 1。
    完。
    返还 C。
完。

矩阵_转置 接受 A：
    若 A 等于 空：
        返还 空。
    完。
    全局统计 的 转置次数 为 全局统计 的 转置次数 加 1。
    可变 T 为 做 矩阵_创建 用 A 的 第 1 项、A 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 A 的 第 0 项：
        可变 j 为 0。
        只要 j 小于 A 的 第 1 项：
            做 矩阵_设置 用 T、j、i、做 矩阵_获取 用 A、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 T。
完。

矩阵_迹 接受 A：
    若 A 等于 空 或者 A 的 第 0 项 不等于 A 的 第 1 项：
        返还 0。
    完。
    可变 迹 为 0。
    可变 i 为 0。
    只要 i 小于 A 的 第 0 项：
        迹 为 迹 加 做 矩阵_获取 用 A、i、i。
        i 为 i 加 1。
    完。
    返还 迹。
完。

矩阵_单位 接受 n：
    可变 I 为 做 矩阵_创建 用 n、n。
    可变 i 为 0。
    只要 i 小于 n：
        做 矩阵_设置 用 I、i、i、线代精度。
        i 为 i 加 1。
    完。
    返还 I。
完。

向量_范数 接受 v：
    若 v 等于 空：
        返还 0。
    完。
    可变 平方和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        平方和 为 平方和 加 v 的 第 i 项 乘 v 的 第 i 项 除以 线代精度。
        i 为 i 加 1。
    完。
    返还 做 线代_整数开方 用 平方和。
完。

向量_加法 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 空。
    完。
    可变 结果 为 一组：（空）。
    长 为 做 计数 用 a。
    若 做 计数 用 b 小于 长：
        长 为 做 计数 用 b。
    完。
    可变 i 为 0。
    只要 i 小于 长：
        做 追加 用 结果、a 的 第 i 项 加 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_减法 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 空。
    完。
    可变 结果 为 一组：（空）。
    长 为 做 计数 用 a。
    若 做 计数 用 b 小于 长：
        长 为 做 计数 用 b。
    完。
    可变 i 为 0。
    只要 i 小于 长：
        做 追加 用 结果、a 的 第 i 项 减 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_标量乘 接受 v、标量：
    若 v 等于 空：
        返还 空。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        做 追加 用 结果、v 的 第 i 项 乘 标量 除以 线代精度。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_行列式_2x2 接受 A：
    若 A 等于 空 或者 A 的 第 0 项 不等于 2 或者 A 的 第 1 项 不等于 2：
        返还 0。
    完。
    返还 A 的 第 2 项 乘 A 的 第 5 项 除以 线代精度 减 A 的 第 3 项 乘 A 的 第 4 项 除以 线代精度。
完。

矩阵_行列式_3x3 接受 A：
    若 A 等于 空 或者 A 的 第 0 项 不等于 3 或者 A 的 第 1 项 不等于 3：
        返还 0。
    完。
    a 为 A 的 第 2 项。
    b 为 A 的 第 3 项。
    c 为 A 的 第 4 项。
    d 为 A 的 第 5 项。
    e 为 A 的 第 6 项。
    f 为 A 的 第 7 项。
    g 为 A 的 第 8 项。
    h 为 A 的 第 9 项。
    ii 为 A 的 第 10 项。
    返还 (a 乘 (e 乘 ii 减 f 乘 h) 减 b 乘 (d 乘 ii 减 f 乘 g) 加 c 乘 (d 乘 h 减 e 乘 g)) 除以 (线代精度 乘 线代精度)。
完。

AI计算窗口 为 16。

可变 AI计算历史 为 一组：（空）。

可变 AI计算位置 为 0。

AI_记录计算 接受 维度、耗时：
    若 做 计数 用 AI计算历史 小于 AI计算窗口：
        做 追加 用 AI计算历史、一组：维度、耗时。
    否则：
        做 设置 用 AI计算历史、AI计算位置、一组：维度、耗时。
    完。
    AI计算位置 为 (AI计算位置 加 1) 取余于 AI计算窗口。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
完。

AI_预测计算耗时 接受 维度：
    可变 总耗时 为 0。
    可变 总维度 为 0。
    可变 有效 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI计算历史：
        若 AI计算历史 的 第 i 项 的 第 0 项 大于 0：
            总耗时 为 总耗时 加 AI计算历史 的 第 i 项 的 第 1 项。
            总维度 为 总维度 加 AI计算历史 的 第 i 项 的 第 0 项。
            有效 为 有效 加 1。
        完。
        i 为 i 加 1。
    完。
    若 有效 等于 0 或者 总维度 等于 0：
        返还 维度 乘 维度 乘 维度。
    完。
    单位耗时 为 总耗时 除以 总维度。
    返还 单位耗时 乘 维度 乘 维度 乘 维度 除以 (总维度 乘 总维度)。
完。

线代统计 是 一种 事物，含 矩阵创建数、矩阵加法数、矩阵减法数、矩阵乘法数、转置次数、高斯消元数、LU分解数、向量运算数、行列式计算数、AI预测次数、AI预测命中、最大维度、总运算量、批量运算数、错误次数。

可变 全局统计 为 新建 线代统计。

可变 线代模块已初始化 为 假。

线代模块_初始化 接受 零参：
    若 线代模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI计算历史 为 一组：（空）。
    AI计算位置 为 0。
    线代模块已初始化 为 真。
    做 打印 用 「[线性代数] 初始化完成」。
    返还 0。
完。

线代模块_销毁 接受 零参：
    若 线代模块已初始化 等于 假：
        返还 0。
    完。
    AI计算历史 为 一组：（空）。
    线代模块已初始化 为 假。
    做 打印 用 「[线性代数] 已销毁」。
    返还 0。
完。

线代模块_转储 接受 零参：
    做 打印 用 「[线性代数] ═══ 状态转储 ═══」。
    做 打印 用 做 字符串拼接 用 「  矩阵乘法: 」、做 整数转字符串 用 全局统计 的 矩阵乘法数。
    做 打印 用 做 字符串拼接 用 「  高斯消元: 」、做 整数转字符串 用 全局统计 的 高斯消元数。
    做 打印 用 做 字符串拼接 用 「  向量运算: 」、做 整数转字符串 用 全局统计 的 向量运算数。
    返还 0。
完。

线代模块_打印统计 接受 零参：
    做 打印 用 「[线性代数] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  矩阵创建: 」、做 整数转字符串 用 全局统计 的 矩阵创建数。
    做 打印 用 做 字符串拼接 用 「  矩阵加法: 」、做 整数转字符串 用 全局统计 的 矩阵加法数。
    做 打印 用 做 字符串拼接 用 「  矩阵减法: 」、做 整数转字符串 用 全局统计 的 矩阵减法数。
    做 打印 用 做 字符串拼接 用 「  矩阵乘法: 」、做 整数转字符串 用 全局统计 的 矩阵乘法数。
    做 打印 用 做 字符串拼接 用 「  转置: 」、做 整数转字符串 用 全局统计 的 转置次数。
    做 打印 用 做 字符串拼接 用 「  高斯消元: 」、做 整数转字符串 用 全局统计 的 高斯消元数。
    做 打印 用 做 字符串拼接 用 「  LU分解: 」、做 整数转字符串 用 全局统计 的 LU分解数。
    做 打印 用 做 字符串拼接 用 「  向量运算: 」、做 整数转字符串 用 全局统计 的 向量运算数。
    做 打印 用 做 字符串拼接 用 「  行列式: 」、做 整数转字符串 用 全局统计 的 行列式计算数。
    做 打印 用 做 字符串拼接 用 「  AI预测: 」、做 整数转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI命中: 」、做 整数转字符串 用 全局统计 的 AI预测命中。
    做 打印 用 做 字符串拼接 用 「  最大维度: 」、做 整数转字符串 用 全局统计 的 最大维度。
    做 打印 用 做 字符串拼接 用 「  总运算量: 」、做 整数转字符串 用 全局统计 的 总运算量。
    做 打印 用 做 字符串拼接 用 「  批量运算: 」、做 整数转字符串 用 全局统计 的 批量运算数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 整数转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

线代模块_注册到内核 接受 内核上下文：
    若 线代模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[线性代数] 已注册到内核上下文」。
    返还 0。
完。

线代模块_查询状态 接受 零参：
    若 线代模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

线代模块_健康检查 接受 零参：
    若 线代模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 矩阵乘法数 加 全局统计 的 高斯消元数：
        返还 假。
    完。
    返还 真。
完。

矩阵_是方阵 接受 A：
    若 A 等于 空：
        返还 假。
    完。
    返还 A 的 第 0 项 等于 A 的 第 1 项。
完。

矩阵_维度 接受 A：
    若 A 等于 空：
        返还 一组：0、0。
    完。
    返还 一组：A 的 第 0 项、A 的 第 1 项。
完。

本章 收束。
