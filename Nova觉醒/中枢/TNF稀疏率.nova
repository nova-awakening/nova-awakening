《TNF稀疏率》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·内核打印》。

引用《.·TNF推理引擎》。

引用《.·整数转字符串》。

引用《.·打乱》。

可变 TNF_运算次数 为 0。

可变 TNF_量化次数 为 0。

可变 能量_计算次数 为 0。

可变 能量_更新次数 为 0。

TNF_稀疏率 接受 向量：
    TNF_运算次数 为 TNF_运算次数 加 1。
    若 做 计数 用 向量 等于 0：
        返还 0。
    完。
    可变 零数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 向量：
        若 向量 的 第 i 项 等于 平：
            零数 为 零数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 零数 乘 100 除以 做 计数 用 向量。
完。

TNF_分布 接受 向量：
    TNF_运算次数 为 TNF_运算次数 加 1。
    可变 阴数 为 0。
    可变 平数 为 0。
    可变 阳数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 向量：
        若 向量 的 第 i 项 等于 阴：
            阴数 为 阴数 加 1。
        否则若 向量 的 第 i 项 等于 平：
            平数 为 平数 加 1。
        否则：
            阳数 为 阳数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：阴数、平数、阳数。
完。

TNF_非零数 接受 向量：
    TNF_运算次数 为 TNF_运算次数 加 1。
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 向量：
        若 向量 的 第 i 项 不等于 平：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

TNF_卷积1D 接受 输入、核、步长：
    TNF_运算次数 为 TNF_运算次数 加 1。
    核长 为 做 计数 用 核。
    输入长 为 做 计数 用 输入。
    可变 结果 为 一组：（空）。
    可变 pos 为 0。
    只要 pos 加 核长 小于或等于 输入长：
        可变 sum 为 0。
        可变 k 为 0。
        只要 k 小于 核长：
            若 核 的 第 k 项 等于 阳：
                sum 为 sum 加 输入 的 第 pos 加 k 项。
            否则若 核 的 第 k 项 等于 阴：
                sum 为 sum 减 输入 的 第 pos 加 k 项。
            完。
            k 为 k 加 1。
        完。
        做 TNF_追加 用 结果、做 TNF_量化 用 sum。
        pos 为 pos 加 步长。
    完。
    返还 结果。
完。

TNF_最大池化 接受 输入、窗口、步长：
    TNF_运算次数 为 TNF_运算次数 加 1。
    n 为 做 计数 用 输入。
    可变 结果 为 一组：（空）。
    可变 pos 为 0。
    只要 pos 加 窗口 小于或等于 n：
        可变 最大 为 输入 的 第 pos 项。
        可变 最大绝对 为 若 最大 小于 0 则 0 减 最大 否则 最大。
        可变 k 为 1。
        只要 k 小于 窗口：
            v 为 输入 的 第 pos 加 k 项。
            绝对 为 若 v 小于 0 则 0 减 v 否则 v。
            若 绝对 大于 最大绝对：
                最大 为 v。
                最大绝对 为 绝对。
            完。
            k 为 k 加 1。
        完。
        做 TNF_追加 用 结果、最大。
        pos 为 pos 加 步长。
    完。
    返还 结果。
完。

TNF_平均池化 接受 输入、窗口、步长：
    TNF_运算次数 为 TNF_运算次数 加 1。
    n 为 做 计数 用 输入。
    可变 结果 为 一组：（空）。
    可变 pos 为 0。
    只要 pos 加 窗口 小于或等于 n：
        可变 sum 为 0。
        可变 k 为 0。
        只要 k 小于 窗口：
            sum 为 sum 加 输入 的 第 pos 加 k 项。
            k 为 k 加 1。
        完。
        做 TNF_追加 用 结果、做 TNF_量化 用 sum 除以 窗口。
        pos 为 pos 加 步长。
    完。
    返还 结果。
完。

TNF_汉明距离 接受 a、b：
    TNF_运算次数 为 TNF_运算次数 加 1。
    可变 diff 为 0。
    可变 i 为 0。
    na 为 做 计数 用 a。
    nb 为 做 计数 用 b。
    只要 i 小于 na 并且 i 小于 nb：
        若 a 的 第 i 项 不等于 b 的 第 i 项：
            diff 为 diff 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 diff。
完。

TNF_余弦相似度 接受 a、b：
    TNF_运算次数 为 TNF_运算次数 加 1。
    na 为 做 计数 用 a。
    nb 为 做 计数 用 b。
    n 为 若 na 小于 nb 则 na 否则 nb。
    可变 点积 为 0。
    可变 范数a 为 0。
    可变 范数b 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        点积 为 点积 加 a 的 第 i 项 乘 b 的 第 i 项。
        范数a 为 范数a 加 a 的 第 i 项 乘 a 的 第 i 项。
        范数b 为 范数b 加 b 的 第 i 项 乘 b 的 第 i 项。
        i 为 i 加 1。
    完。
    若 范数a 等于 0 或者 范数b 等于 0：
        返还 0。
    完。
    分母 为 做 网络_开方 用 范数a 乘 做 网络_开方 用 范数b 除以 1000。
    若 分母 等于 0：
        返还 0。
    完。
    返还 点积 乘 1000 除以 分母。
完。

TNF_匹配度 接受 a、b：
    TNF_运算次数 为 TNF_运算次数 加 1。
    na 为 做 计数 用 a。
    nb 为 做 计数 用 b。
    n 为 若 na 小于 nb 则 na 否则 nb。
    若 n 等于 0：
        返还 0。
    完。
    可变 匹配 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 a 的 第 i 项 等于 b 的 第 i 项：
            匹配 为 匹配 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 匹配 乘 100 除以 n。
完。

TNF_压缩比 接受 原始位宽、元素数：
    若 原始位宽 等于 0：
        返还 0。
    完。
    返还 原始位宽 乘 100 除以 2。
完。

TNF_打包 接受 向量：
    TNF_运算次数 为 TNF_运算次数 加 1。
    n 为 做 计数 用 向量。
    可变 打包结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 值 为 0。
        可变 乘子 为 1。
        可变 j 为 0。
        只要 j 小于 5 并且 i 加 j 小于 n：
            t 为 向量 的 第 i 加 j 项。
            编码 为 若 t 等于 阴 则 0 否则 若 t 等于 平 则 1 否则 2。
            值 为 值 加 编码 乘 乘子。
            乘子 为 乘子 乘 3。
            j 为 j 加 1。
        完。
        做 网络_追加 用 打包结果、值。
        i 为 i 加 5。
    完。
    返还 打包结果。
完。

TNF_解包 接受 打包数据、原始长度：
    TNF_运算次数 为 TNF_运算次数 加 1。
    可变 结果 为 一组：（空）。
    可变 已输出 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 打包数据 并且 已输出 小于 原始长度：
        可变 值 为 打包数据 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 5 并且 已输出 小于 原始长度：
            编码 为 值 取余于 3。
            值 为 值 除以 3。
            若 编码 等于 0：
                做 TNF_追加 用 结果、阴。
            否则若 编码 等于 1：
                做 TNF_追加 用 结果、平。
            否则：
                做 TNF_追加 用 结果、阳。
            完。
            已输出 为 已输出 加 1。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

TNF_熵 接受 向量：
    TNF_运算次数 为 TNF_运算次数 加 1。
    分布 为 做 TNF_分布 用 向量。
    n 为 做 计数 用 向量。
    若 n 等于 0：
        返还 0。
    完。
    可变 H 为 0。
    可变 i 为 0。
    只要 i 小于 3：
        p 为 分布 的 第 i 项 乘 1000 除以 n。
        若 p 大于 0 并且 p 小于 1000：
            H 为 H 加 p 乘 (1000 减 p) 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 H。
完。

TNF_topK 接受 向量、K：
    TNF_运算次数 为 TNF_运算次数 加 1。
    n 为 做 计数 用 向量。
    若 K 大于或等于 n：
        返还 向量。
    完。
    可变 阈值列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        绝对 为 若 向量 的 第 i 项 小于 0 则 0 减 向量 的 第 i 项 否则 向量 的 第 i 项。
        做 网络_追加 用 阈值列表、绝对。
        i 为 i 加 1。
    完。
    可变 k 为 0。
    只要 k 小于 K：
        可变 j 为 做 计数 用 阈值列表 减 1。
        只要 j 大于 k：
            若 阈值列表 的 第 j 项 大于 阈值列表 的 第 j 减 1 项：
                temp 为 阈值列表 的 第 j 项。
                阈值列表 的 第 j 项 为 阈值列表 的 第 j 减 1 项。
                阈值列表 的 第 j 减 1 项 为 temp。
            完。
            j 为 j 减 1。
        完。
        k 为 k 加 1。
    完。
    阈值 为 阈值列表 的 第 K 减 1 项。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        绝对 为 若 向量 的 第 i 项 小于 0 则 0 减 向量 的 第 i 项 否则 向量 的 第 i 项。
        若 绝对 大于或等于 阈值：
            做 TNF_追加 用 结果、向量 的 第 i 项。
        否则：
            做 TNF_追加 用 结果、平。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

TNF_随机投影 接受 输入、输出维、种子：
    TNF_运算次数 为 TNF_运算次数 加 1。
    输入维 为 做 计数 用 输入。
    可变 结果 为 一组：（空）。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 输出维：
        可变 sum 为 0。
        可变 j 为 0。
        只要 j 小于 输入维：
            s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
            r 为 s 取余于 3。
            若 r 等于 0：
                sum 为 sum 减 输入 的 第 j 项。
            否则若 r 等于 2：
                sum 为 sum 加 输入 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        做 TNF_追加 用 结果、做 TNF_量化 用 sum。
        i 为 i 加 1。
    完。
    返还 结果。
完。

能量_二次 接受 x、W、b：
    能量_计算次数 为 能量_计算次数 加 1。
    n 为 做 计数 用 x。
    可变 E 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 n：
            若 i 乘 n 加 j 小于 做 计数 用 W：
                E 为 E 减 x 的 第 i 项 乘 W 的 第 i 乘 n 加 j 项 乘 x 的 第 j 项 除以 (2 乘 1000)。
            完。
            j 为 j 加 1。
        完。
        若 i 小于 做 计数 用 b：
            E 为 E 减 b 的 第 i 项 乘 x 的 第 i 项 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 E。
完。

能量_线性 接受 x、w、b：
    能量_计算次数 为 能量_计算次数 加 1。
    n 为 做 计数 用 x。
    可变 E 为 b。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 做 计数 用 w：
            E 为 E 加 w 的 第 i 项 乘 x 的 第 i 项 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 E。
完。

能量_梯度 接受 x、W、b：
    能量_计算次数 为 能量_计算次数 加 1。
    n 为 做 计数 用 x。
    可变 梯度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 g 为 0。
        可变 j 为 0。
        只要 j 小于 n：
            若 i 乘 n 加 j 小于 做 计数 用 W：
                g 为 g 减 W 的 第 i 乘 n 加 j 项 乘 x 的 第 j 项 除以 1000。
            完。
            j 为 j 加 1。
        完。
        若 i 小于 做 计数 用 b：
            g 为 g 减 b 的 第 i 项。
        完。
        做 网络_追加 用 梯度、g。
        i 为 i 加 1。
    完。
    返还 梯度。
完。

能量_梯度下降 接受 x、W、b、学习率：
    能量_更新次数 为 能量_更新次数 加 1。
    梯度 为 做 能量_梯度 用 x、W、b。
    n 为 做 计数 用 x。
    可变 新x 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 网络_追加 用 新x、x 的 第 i 项 减 学习率 乘 梯度 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 新x。
完。

Hopfield_更新 接受 x、W、b、种子：
    能量_更新次数 为 能量_更新次数 加 1。
    n 为 做 计数 用 x。
    若 n 等于 0：
        返还 x。
    完。
    idx 为 种子 取余于 n。
    可变 h 为 0。
    可变 j 为 0。
    只要 j 小于 n：
        若 idx 乘 n 加 j 小于 做 计数 用 W：
            h 为 h 加 W 的 第 idx 乘 n 加 j 项 乘 x 的 第 j 项 除以 1000。
        完。
        j 为 j 加 1。
    完。
    若 idx 小于 做 计数 用 b：
        h 为 h 加 b 的 第 idx 项。
    完。
    可变 新x 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 等于 idx：
            若 h 大于 0：
                做 网络_追加 用 新x、1。
            否则若 h 小于 0：
                做 网络_追加 用 新x、0 减 1。
            否则：
                做 网络_追加 用 新x、x 的 第 i 项。
            完。
        否则：
            做 网络_追加 用 新x、x 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 新x。
完。

Hopfield_同步更新 接受 x、W、b：
    能量_更新次数 为 能量_更新次数 加 1。
    n 为 做 计数 用 x。
    可变 新x 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 h 为 0。
        可变 j 为 0。
        只要 j 小于 n：
            若 i 乘 n 加 j 小于 做 计数 用 W：
                h 为 h 加 W 的 第 i 乘 n 加 j 项 乘 x 的 第 j 项 除以 1000。
            完。
            j 为 j 加 1。
        完。
        若 i 小于 做 计数 用 b：
            h 为 h 加 b 的 第 i 项。
        完。
        若 h 大于 0：
            做 网络_追加 用 新x、1。
        否则若 h 小于 0：
            做 网络_追加 用 新x、0 减 1。
        否则：
            做 网络_追加 用 新x、x 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 新x。
完。

热力学_采样 接受 x、W、b、温度、种子：
    能量_更新次数 为 能量_更新次数 加 1。
    n 为 做 计数 用 x。
    若 n 等于 0：
        返还 x。
    完。
    idx 为 种子 取余于 n。
    可变 h 为 0。
    可变 j 为 0。
    只要 j 小于 n：
        若 idx 乘 n 加 j 小于 做 计数 用 W：
            h 为 h 加 W 的 第 idx 乘 n 加 j 项 乘 x 的 第 j 项 除以 1000。
        完。
        j 为 j 加 1。
    完。
    若 idx 小于 做 计数 用 b：
        h 为 h 加 b 的 第 idx 项。
    完。
    可变 p 为 500。
    若 温度 大于 0：
        arg 为 2 乘 h 乘 1000 除以 温度。
        若 arg 大于 4000：
            p 为 1000。
        否则若 arg 小于 0 减 4000：
            p 为 0。
        否则：
            p 为 500 加 arg 除以 8。
        完。
    完。
    s 为 种子 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    随机值 为 s 取余于 1000。
    可变 新x 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 等于 idx：
            若 随机值 小于 p：
                做 网络_追加 用 新x、1。
            否则：
                做 网络_追加 用 新x、0 减 1。
            完。
        否则：
            做 网络_追加 用 新x、x 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 新x。
完。

神经稀疏_统计 接受 零参：
    返还 一组：TNF_运算次数、TNF_量化次数、能量_计算次数、能量_更新次数。
完。

神经稀疏_打印统计 接受 零参：
    做 打印行 用 「=== TNF/能量模型统计 ===」。
    做 打印行 用 「TNF运算: 」 加 做 整数转字符串 用 TNF_运算次数。
    做 打印行 用 「TNF量化: 」 加 做 整数转字符串 用 TNF_量化次数。
    做 打印行 用 「能量计算: 」 加 做 整数转字符串 用 能量_计算次数。
    做 打印行 用 「能量更新: 」 加 做 整数转字符串 用 能量_更新次数。
完。

可变 _谐振稀疏_目标率 为 500。

可变 _谐振稀疏_调节次数 为 0。

谐振稀疏_调节 接受 当前精度、基准精度：
    精度差 为 当前精度 减 基准精度。
    若 精度差 小于 负 20：
        _谐振稀疏_目标率 为 _谐振稀疏_目标率 减 50。
        若 _谐振稀疏_目标率 小于 100：
            _谐振稀疏_目标率 为 100。
        完。
    完。
    做 否则如果 用 精度差 大于 负 5。
    块：
        _谐振稀疏_目标率 为 _谐振稀疏_目标率 加 30。
        若 _谐振稀疏_目标率 大于 900：
            _谐振稀疏_目标率 为 900。
        完。
    完。
    _谐振稀疏_调节次数 为 _谐振稀疏_调节次数 加 1。
完。

谐振稀疏_当前目标 接受 零参：
    返还 _谐振稀疏_目标率。
完。

神经稀疏_完整统计 接受 零参：
    返还 一组：「TNF运算次数」、TNF_运算次数、「TNF量化次数」、TNF_量化次数、「能量计算次数」、能量_计算次数、「能量更新次数」、能量_更新次数、「谐振稀疏目标」、_谐振稀疏_目标率、「谐振调节次数」、_谐振稀疏_调节次数。
完。

本章 收束。
