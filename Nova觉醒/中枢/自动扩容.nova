《自动扩容》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·哈希整数》。

引用《.·字符串替换》。

哈希表_插入_自动扩容 接受 表、键、值：
    若 做 哈希表_负载因子 用 表 大于 700：
        做 哈希表_扩容 用 表。
    完。
    返还 做 哈希表_插入 用 表、键、值。
完。

字典 接受 零参：
    返还 做 哈希表_创建 用 64。
完。

字典设 接受 表、字符串键、值：
    键哈希 为 做 哈希_字符串 用 字符串键。
    返还 做 哈希表_插入_自动扩容 用 表、键哈希、一组：字符串键、值。
完。

字典取 接受 表、字符串键：
    键哈希 为 做 哈希_字符串 用 字符串键。
    结果 为 做 哈希表_查找 用 表、键哈希。
    若 结果 等于 空：
        返还 空。
    完。
    若 结果 的 第 0 项 等于 字符串键：
        返还 结果 的 第 1 项。
    完。
    返还 空。
完。

字典删 接受 表、字符串键：
    键哈希 为 做 哈希_字符串 用 字符串键。
    返还 做 哈希表_删除 用 表、键哈希。
完。

字典_存在 接受 表、字符串键：
    返还 做 字典取 用 表、字符串键 不等于 空。
完。

哈希表_删除_自动缩容 接受 表、键：
    结果 为 做 哈希表_删除 用 表、键。
    若 表 的 第 1 项 大于 64 并且 做 哈希表_负载因子 用 表 小于 200：
        做 哈希表_缩容 用 表。
    完。
    返还 结果。
完。

哈希表_缩容 接受 表：
    旧桶 为 表 的 第 0 项。
    旧容量 为 表 的 第 1 项。
    新容量 为 旧容量 除以 2。
    若 新容量 小于 16：
        新容量 为 16。
    完。
    新桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 新容量：
        做 追加 用 新桶、一组：0、空、假。
        i 为 i 加 1。
    完。
    做 设置 用 表、0、新桶。
    做 设置 用 表、1、新容量。
    做 设置 用 表、2、0。
    i 为 0。
    只要 i 小于 旧容量：
        若 旧桶 的 第 i 项 的 第 2 项 等于 真：
            做 哈希表_插入 用 表、旧桶 的 第 i 项 的 第 0 项、旧桶 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
完。

可变 _潮汐_插入计数 为 0。

可变 _潮汐_删除计数 为 0。

可变 _潮汐_趋势 为 0。

潮汐_平滑因子 为 200。

潮汐_预扩容阈值 为 600。

潮汐_预缩容阈值 为 250。

潮汐_插入 接受 表、键、值：
    _潮汐_插入计数 为 _潮汐_插入计数 加 1。
    _潮汐_趋势 为 (_潮汐_趋势 乘 (1000 减 潮汐_平滑因子) 加 1000 乘 潮汐_平滑因子) 除以 1000。
    负载 为 做 哈希表_负载因子 用 表。
    若 负载 大于 潮汐_预扩容阈值 并且 _潮汐_趋势 大于 300：
        做 哈希表_扩容 用 表。
    否则若 负载 大于 700：
        做 哈希表_扩容 用 表。
    完。
    返还 做 哈希表_插入 用 表、键、值。
完。

潮汐_删除 接受 表、键：
    _潮汐_删除计数 为 _潮汐_删除计数 加 1。
    _潮汐_趋势 为 (_潮汐_趋势 乘 (1000 减 潮汐_平滑因子) 加 (0 减 1000) 乘 潮汐_平滑因子) 除以 1000。
    结果 为 做 哈希表_删除 用 表、键。
    负载 为 做 哈希表_负载因子 用 表。
    若 表 的 第 1 项 大于 64 并且 负载 小于 潮汐_预缩容阈值 并且 _潮汐_趋势 小于 负 300：
        做 哈希表_缩容 用 表。
    完。
    返还 结果。
完。

潮汐_统计 接受 零参：
    返还 一组：_潮汐_插入计数、_潮汐_删除计数、_潮汐_趋势。
完。

哈希表_所有键 接受 表：
    可变 结果 为 一组：（空）。
    桶 为 表 的 第 0 项。
    容量 为 表 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 容量：
        若 桶 的 第 i 项 的 第 2 项 等于 真：
            做 追加 用 结果、桶 的 第 i 项 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

哈希表_所有值 接受 表：
    可变 结果 为 一组：（空）。
    桶 为 表 的 第 0 项。
    容量 为 表 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 容量：
        若 桶 的 第 i 项 的 第 2 项 等于 真：
            做 追加 用 结果、桶 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

哈希表_清空 接受 表：
    桶 为 表 的 第 0 项。
    容量 为 表 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 容量：
        做 设置 用 桶 的 第 i 项、0、0。
        做 设置 用 桶 的 第 i 项、1、空。
        做 设置 用 桶 的 第 i 项、2、假。
        i 为 i 加 1。
    完。
    做 设置 用 表、2、0。
    _潮汐_插入计数 为 0。
    _潮汐_删除计数 为 0。
    _潮汐_趋势 为 0。
完。

可变 _增量_旧桶 为 空。

可变 _增量_旧容量 为 0。

可变 _增量_游标 为 0。

增量_每次迁移数 为 4。

哈希表_增量扩容_开始 接受 表：
    _增量_旧桶 为 表 的 第 0 项。
    _增量_旧容量 为 表 的 第 1 项。
    新容量 为 表 的 第 1 项 乘 2。
    新桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 新容量：
        做 追加 用 新桶、一组：0、空、假。
        i 为 i 加 1。
    完。
    做 设置 用 表、0、新桶。
    做 设置 用 表、1、新容量。
    做 设置 用 表、2、0。
    _增量_游标 为 0。
完。

哈希表_增量迁移_步进 接受 表：
    若 _增量_旧桶 等于 空：
        返还 假。
    完。
    可变 已迁移 为 0。
    只要 _增量_游标 小于 _增量_旧容量 并且 已迁移 小于 增量_每次迁移数：
        若 _增量_旧桶 的 第 _增量_游标 项 的 第 2 项 等于 真：
            做 哈希表_插入 用 表、_增量_旧桶 的 第 _增量_游标 项 的 第 0 项、_增量_旧桶 的 第 _增量_游标 项 的 第 1 项。
            已迁移 为 已迁移 加 1。
        完。
        _增量_游标 为 _增量_游标 加 1。
    完。
    若 _增量_游标 大于或等于 _增量_旧容量：
        _增量_旧桶 为 空。
        _增量_旧容量 为 0。
        返还 假。
    完。
    返还 真。
完。

哈希表_增量查找 接受 表、键：
    结果 为 做 哈希表_查找 用 表、键。
    若 结果 不等于 空：
        返还 结果。
    完。
    若 _增量_旧桶 不等于 空：
        哈希 为 键。
        若 哈希 小于 0：
            哈希 为 0 减 哈希。
        完。
        索引 为 哈希 取余于 _增量_旧容量。
        若 _增量_旧桶 的 第 索引 项 的 第 2 项 等于 真 并且 _增量_旧桶 的 第 索引 项 的 第 0 项 等于 键：
            返还 _增量_旧桶 的 第 索引 项 的 第 1 项。
        完。
    完。
    返还 空。
完。

成功 为 0。

错误_无效参数 为 0 减 22。

AI扩容窗口 为 16。

扩容统计 是 一种 事物，含 自动插入次数、自动删除次数、扩容次数、缩容次数、潮汐插入次数、潮汐删除次数、潮汐预测扩容、潮汐预测缩容、增量扩容开始数、增量迁移步数、增量完成数、字典操作次数、字典命中次数、AI预测次数、AI预测命中、清空次数、错误次数。

可变 全局统计 为 新建 扩容统计。

可变 扩容模块已初始化 为 假。

可变 AI负载历史 为 一组：（空）。

可变 AI负载位置 为 0。

扩容模块_初始化 接受 零参：
    若 扩容模块已初始化 等于 真：
        返还 0 减 17。
    完。
    AI负载历史 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 AI扩容窗口：
        做 追加 用 AI负载历史、0。
        i 为 i 加 1。
    完。
    AI负载位置 为 0。
    扩容模块已初始化 为 真。
    做 打印 用 「[自动扩容] 初始化完成」。
    返还 0。
完。

扩容模块_销毁 接受 零参：
    若 扩容模块已初始化 等于 假：
        返还 0。
    完。
    AI负载历史 为 一组：（空）。
    _增量_旧桶 为 空。
    _增量_旧容量 为 0。
    扩容模块已初始化 为 假。
    做 打印 用 「[自动扩容] 已销毁」。
    返还 0。
完。

安全_自动插入 接受 表、键、值：
    若 表 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    全局统计 的 自动插入次数 为 全局统计 的 自动插入次数 加 1。
    做 AI_记录负载 用 表。
    返还 做 哈希表_插入_自动扩容 用 表、键、值。
完。

安全_自动删除 接受 表、键：
    若 表 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    全局统计 的 自动删除次数 为 全局统计 的 自动删除次数 加 1。
    返还 做 哈希表_删除_自动缩容 用 表、键。
完。

安全_潮汐插入 接受 表、键、值：
    若 表 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    全局统计 的 潮汐插入次数 为 全局统计 的 潮汐插入次数 加 1。
    返还 做 潮汐_插入 用 表、键、值。
完。

安全_潮汐删除 接受 表、键：
    若 表 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    全局统计 的 潮汐删除次数 为 全局统计 的 潮汐删除次数 加 1。
    返还 做 潮汐_删除 用 表、键。
完。

安全_增量扩容_开始 接受 表：
    若 表 等于 空：
        返还 错误_无效参数。
    完。
    全局统计 的 增量扩容开始数 为 全局统计 的 增量扩容开始数 加 1。
    做 哈希表_增量扩容_开始 用 表。
    返还 0。
完。

安全_增量迁移 接受 表：
    若 表 等于 空：
        返还 假。
    完。
    全局统计 的 增量迁移步数 为 全局统计 的 增量迁移步数 加 1。
    结果 为 做 哈希表_增量迁移_步进 用 表。
    若 结果 等于 假：
        全局统计 的 增量完成数 为 全局统计 的 增量完成数 加 1。
    完。
    返还 结果。
完。

增量_是否进行中 接受 零参：
    返还 _增量_旧桶 不等于 空。
完。

增量_迁移进度 接受 零参：
    若 _增量_旧桶 等于 空 或者 _增量_旧容量 等于 0：
        返还 100。
    完。
    返还 _增量_游标 乘 100 除以 _增量_旧容量。
完。

字典_安全设 接受 表、字符串键、值：
    若 表 等于 空 或者 字符串键 等于 空：
        全局统计 的 错误次数 为 全局统计 的 错误次数 加 1。
        返还 错误_无效参数。
    完。
    全局统计 的 字典操作次数 为 全局统计 的 字典操作次数 加 1。
    返还 做 字典设 用 表、字符串键、值。
完。

字典_安全取 接受 表、字符串键：
    若 表 等于 空 或者 字符串键 等于 空：
        返还 空。
    完。
    全局统计 的 字典操作次数 为 全局统计 的 字典操作次数 加 1。
    结果 为 做 字典取 用 表、字符串键。
    若 结果 不等于 空：
        全局统计 的 字典命中次数 为 全局统计 的 字典命中次数 加 1。
    完。
    返还 结果。
完。

字典_批量设 接受 表、键值列表：
    若 表 等于 空 或者 键值列表 等于 空：
        返还 0。
    完。
    可变 已设 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键值列表：
        做 字典设 用 表、键值列表 的 第 i 项 的 第 0 项、键值列表 的 第 i 项 的 第 1 项。
        已设 为 已设 加 1。
        i 为 i 加 1。
    完。
    返还 已设。
完。

字典_大小 接受 表：
    若 表 等于 空：
        返还 0。
    完。
    返还 做 哈希表_大小 用 表。
完。

AI_记录负载 接受 表：
    若 表 等于 空：
        返还。
    完。
    负载 为 做 哈希表_负载因子 用 表。
    做 设置 用 AI负载历史、AI负载位置、负载。
    AI负载位置 为 (AI负载位置 加 1) 取余于 AI扩容窗口。
    全局统计 的 AI预测次数 为 全局统计 的 AI预测次数 加 1。
完。

AI_预测负载趋势 接受 零参：
    半 为 AI扩容窗口 除以 2。
    可变 前半和 为 0。
    可变 后半和 为 0。
    可变 i 为 0。
    只要 i 小于 半：
        前半和 为 前半和 加 AI负载历史 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 i 小于 AI扩容窗口：
        后半和 为 后半和 加 AI负载历史 的 第 i 项。
        i 为 i 加 1。
    完。
    前均 为 前半和 除以 半。
    后均 为 后半和 除以 半。
    趋势 为 后均 减 前均。
    若 趋势 大于 100：
        全局统计 的 AI预测命中 为 全局统计 的 AI预测命中 加 1。
        返还 「快速增长」。
    完。
    若 趋势 大于 20：
        返还 「缓慢增长」。
    完。
    若 趋势 大于 0 减 20：
        返还 「稳定」。
    完。
    若 趋势 大于 0 减 100：
        返还 「缓慢下降」。
    完。
    返还 「快速下降」。
完。

AI_建议扩缩容策略 接受 表：
    若 表 等于 空：
        返还 「无操作」。
    完。
    负载 为 做 哈希表_负载因子 用 表。
    趋势 为 做 AI_预测负载趋势。
    若 负载 大于 600 并且 趋势 等于 「快速增长」：
        返还 「立即扩容」。
    完。
    若 负载 大于 500 并且 趋势 等于 「缓慢增长」：
        返还 「预备扩容」。
    完。
    若 负载 小于 200 并且 趋势 等于 「快速下降」：
        返还 「建议缩容」。
    完。
    返还 「保持当前」。
完。

扩容模块_转储 接受 表：
    做 打印 用 「[自动扩容] ═══ 状态转储 ═══」。
    若 表 不等于 空：
        做 打印 用 做 字符串拼接 用 「  负载因子‰: 」、做 转字符串 用 做 哈希表_负载因子 用 表。
        做 打印 用 做 字符串拼接 用 「  元素数: 」、做 转字符串 用 做 哈希表_大小 用 表。
        做 打印 用 做 字符串拼接 用 「  容量: 」、做 转字符串 用 做 哈希表_容量 用 表。
    完。
    做 打印 用 做 字符串拼接 用 「  潮汐趋势: 」、做 转字符串 用 _潮汐_趋势。
    做 打印 用 做 字符串拼接 用 「  增量进行中: 」、做 转字符串 用 做 增量_是否进行中。
    若 做 增量_是否进行中 等于 真：
        做 打印 用 做 字符串拼接 用 「  迁移进度%: 」、做 转字符串 用 做 增量_迁移进度。
    完。
    做 打印 用 做 字符串拼接 用 「  AI趋势: 」、做 AI_预测负载趋势。
    若 表 不等于 空：
        做 打印 用 做 字符串拼接 用 「  AI建议: 」、做 AI_建议扩缩容策略 用 表。
    完。
    返还 0。
完。

自动扩容_完整统计 接受 零参：
    潮汐信息 为 做 潮汐_统计。
    增量进行中 为 _增量_旧桶 不等于 空。
    返还 一组：潮汐信息 的 第 0 项、潮汐信息 的 第 1 项、潮汐信息 的 第 2 项、增量进行中、_增量_游标。
完。

扩容模块_打印统计 接受 零参：
    做 打印 用 「[自动扩容] ═══ 运行统计 ═══」。
    做 打印 用 做 字符串拼接 用 「  自动插入: 」、做 转字符串 用 全局统计 的 自动插入次数。
    做 打印 用 做 字符串拼接 用 「  自动删除: 」、做 转字符串 用 全局统计 的 自动删除次数。
    做 打印 用 做 字符串拼接 用 「  扩容次数: 」、做 转字符串 用 全局统计 的 扩容次数。
    做 打印 用 做 字符串拼接 用 「  缩容次数: 」、做 转字符串 用 全局统计 的 缩容次数。
    做 打印 用 做 字符串拼接 用 「  潮汐插入: 」、做 转字符串 用 全局统计 的 潮汐插入次数。
    做 打印 用 做 字符串拼接 用 「  潮汐删除: 」、做 转字符串 用 全局统计 的 潮汐删除次数。
    做 打印 用 做 字符串拼接 用 「  潮汐预测扩容: 」、做 转字符串 用 全局统计 的 潮汐预测扩容。
    做 打印 用 做 字符串拼接 用 「  潮汐预测缩容: 」、做 转字符串 用 全局统计 的 潮汐预测缩容。
    做 打印 用 做 字符串拼接 用 「  增量扩容开始: 」、做 转字符串 用 全局统计 的 增量扩容开始数。
    做 打印 用 做 字符串拼接 用 「  增量迁移步: 」、做 转字符串 用 全局统计 的 增量迁移步数。
    做 打印 用 做 字符串拼接 用 「  增量完成: 」、做 转字符串 用 全局统计 的 增量完成数。
    做 打印 用 做 字符串拼接 用 「  字典操作: 」、做 转字符串 用 全局统计 的 字典操作次数。
    做 打印 用 做 字符串拼接 用 「  字典命中: 」、做 转字符串 用 全局统计 的 字典命中次数。
    做 打印 用 做 字符串拼接 用 「  AI预测: 」、做 转字符串 用 全局统计 的 AI预测次数。
    做 打印 用 做 字符串拼接 用 「  AI命中: 」、做 转字符串 用 全局统计 的 AI预测命中。
    做 打印 用 做 字符串拼接 用 「  清空次数: 」、做 转字符串 用 全局统计 的 清空次数。
    做 打印 用 做 字符串拼接 用 「  错误次数: 」、做 转字符串 用 全局统计 的 错误次数。
    返还 0。
完。

扩容模块_注册到内核 接受 内核上下文：
    若 扩容模块已初始化 等于 假：
        返还 0 减 1。
    完。
    做 打印 用 「[自动扩容] 已注册到内核上下文」。
    返还 0。
完。

扩容模块_查询状态 接受 零参：
    若 扩容模块已初始化 等于 假：
        返还 0。
    完。
    返还 1。
完。

扩容模块_健康检查 接受 零参：
    若 扩容模块已初始化 等于 假：
        返还 假。
    完。
    若 全局统计 的 错误次数 乘 10 大于 全局统计 的 自动插入次数 加 全局统计 的 自动删除次数：
        返还 假。
    完。
    返还 真。
完。

本章 收束。
