《复制描述》

引用《.·类型定义》。

引用《.·错误码》。

引用《.·硬链接》。

引用《..·记忆·VFS文件读写》。

引用《.·字符串替换》。

引用《..·记忆·文件描述符》。

引用《..·记忆·文件节点创建》。

引用《..·记忆·目录项添加》。

引用《..·记忆·VFS读写同步》。

引用《..·记忆·路径查找》。

引用《.·自动扩容》。

引用《..·记忆·路径缓存初始化》。

引用《..·记忆·VFS文件操作》。

引用《.·释放文件表》。

引用《.·网络系统调用》。

引用《..·标准·原生通道》。

引用《.·每处理器》。

引用《.·虚拟内存区》。

引用《.·内存系统调用》。

引用《..·记忆·文件节点缓存》。

sys_dup 接受 旧fd、_b、_c、_d、_e、_f：
    返还 做 系统_dup 用 旧fd。
完。

sys_dup2 接受 旧fd、新fd、_c、_d、_e、_f：
    若 旧fd 等于 新fd：
        返还 旧fd。
    完。
    返还 做 系统_dup2 用 旧fd、新fd。
完。

sys_pipe 接受 pipefd、_b、_c、_d、_e、_f：
    返还 做 do_pipe 用 pipefd、0。
完。

do_pipe 接受 pipefd、标志：
    若 pipefd 等于 空：
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    文件表 为 做 进程_文件表 用 当前。
    读fd 为 做 文件表_分配 用 文件表。
    若 读fd 小于 0：
        返还 错误_打开过多。
    完。
    做 文件表_设置 用 文件表、读fd、1。
    写fd 为 做 文件表_分配 用 文件表。
    若 写fd 小于 0：
        做 文件表_关闭 用 文件表、读fd。
        返还 错误_打开过多。
    完。
    管道缓冲 为 做 管道_创建 用 16。
    若 管道缓冲 等于 空：
        做 文件表_关闭 用 文件表、读fd。
        返还 错误_内存不足。
    完。
    读文件 为 做 管道_创建读端 用 管道缓冲。
    写文件 为 做 管道_创建写端 用 管道缓冲。
    做 文件表_设置 用 文件表、读fd、读文件。
    做 文件表_设置 用 文件表、写fd、写文件。
    做 设置 用 pipefd、0、读fd。
    做 设置 用 pipefd、1、写fd。
    返还 成功。
完。

sys_Nova设备控制 接受 fd、cmd、arg、_d、_e、_f：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    若 cmd 等于 FIONREAD：
        节点 为 做 文件_Nova文件节点 用 文件。
        若 节点 不等于 空：
            返还 做 Nova文件节点_大小 用 节点 减 做 文件_偏移 用 文件。
        完。
        返还 0。
    完。
    操作 为 做 文件_操作表 用 文件。
    若 操作 不等于 空 并且 操作 的 Nova设备控制 不等于 空：
        返还 做 操作 的 Nova设备控制 用 文件、cmd、arg。
    完。
    返还 错误_不支持Nova设备控制。
完。

FIONREAD 为 0x541B。

F_DUPFD 为 0。

F_GETFD 为 1。

F_SETFD 为 2。

F_GETFL 为 3。

F_SETFL 为 4。

FD_CLOEXEC 为 1。

sys_fcntl 接受 fd、cmd、arg、_d、_e、_f：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    若 cmd 等于 F_DUPFD：
        返还 做 sys_dup 用 fd、0、0、0、0、0。
    完。
    若 cmd 等于 F_GETFD：
        返还 做 文件_cloexec 用 文件。
    完。
    若 cmd 等于 F_SETFD：
        做 文件_设置cloexec 用 文件、arg 位与 FD_CLOEXEC。
        返还 成功。
    完。
    若 cmd 等于 F_GETFL：
        返还 做 文件_标志 用 文件。
    完。
    若 cmd 等于 F_SETFL：
        可改标志 为 O_APPEND 位或 O_NONBLOCK 位或 O_SYNC。
        旧标志 为 做 文件_标志 用 文件。
        新标志 为 旧标志 位与 按位取反 可改标志 位或 arg 位与 可改标志。
        做 文件_设置标志 用 文件、新标志。
        返还 成功。
    完。
    返还 错误_无效参数。
完。

sys_fsync 接受 fd、_b、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    返还 做 vfs_fsync 用 文件。
完。

sys_truncate 接受 路径、长度、_c、_d、_e、_f：
    根 为 做 获取根目录项。
    目录项 为 做 路径查找 用 根、路径。
    若 目录项 等于 空：
        返还 错误_不存在。
    完。
    节点 为 做 目录项_Nova文件节点 用 目录项。
    若 非 做 Nova节点_是文件 用 节点：
        返还 错误_无效参数。
    完。
    做 Nova文件节点_设置大小 用 节点、长度。
    做 Nova文件节点_标记脏 用 节点。
    返还 成功。
完。

sys_ftruncate 接受 fd、长度、_c、_d、_e、_f：
    当前 为 做 获取当前进程。
    文件 为 做 进程_获取文件 用 当前、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    返还 做 vfs_truncate 用 文件、长度。
完。

sys_mkdir 接受 路径、模式、_c、_d、_e、_f：
    根 为 做 获取根目录项。
    分隔 为 做 字符串最后索引 用 路径、「/」。
    父路径 为 做 如果选字符 用 分隔 大于 0、做 字符串子串 用 路径、0、分隔、「/」。
    目录名 为 做 字符串子串 用 路径、分隔 加 1、做 字符串长度 用 路径。
    若 目录名 等于 「」：
        返还 错误_已存在。
    完。
    父Nova路径项 为 做 路径查找 用 根、父路径。
    若 父Nova路径项 等于 空：
        返还 错误_不存在。
    完。
    父Nova文件节点 为 做 目录项_Nova文件节点 用 父Nova路径项。
    若 非 做 Nova节点_是目录 用 父Nova文件节点：
        返还 错误_非目录。
    完。
    若 做 目录项_查找子 用 父Nova路径项、目录名 不等于 空：
        返还 错误_已存在。
    完。
    新Nova文件节点 为 做 Nova文件节点_创建 用 类型_目录、模式 位或 493。
    新Nova路径项 为 做 目录项_创建 用 目录名、新Nova文件节点、父Nova路径项。
    做 目录项_添加子 用 父Nova路径项、新Nova路径项。
    若 父Nova文件节点 的 i_op 不等于 空 并且 父Nova文件节点 的 i_op 的 mkdir 不等于 空：
        做 父Nova文件节点 的 i_op 的 mkdir 用 父Nova文件节点、新Nova路径项、模式。
    完。
    返还 成功。
完。

sys_rmdir 接受 路径、_b、_c、_d、_e、_f：
    根 为 做 获取根目录项。
    目录项 为 做 路径查找 用 根、路径。
    若 目录项 等于 空：
        返还 错误_不存在。
    完。
    节点 为 做 目录项_Nova文件节点 用 目录项。
    若 非 做 Nova节点_是目录 用 节点：
        返还 错误_非目录。
    完。
    若 做 目录项_子数 用 目录项 大于 0：
        返还 错误_目录非空。
    完。
    父 为 做 目录项_父 用 目录项。
    若 父 不等于 空：
        做 目录项_移除子 用 父、目录项。
    完。
    做 Nova文件节点_减链接 用 节点。
    返还 成功。
完。

sys_unlink 接受 路径、_b、_c、_d、_e、_f：
    根 为 做 获取根目录项。
    目录项 为 做 路径查找 用 根、路径。
    若 目录项 等于 空：
        返还 错误_不存在。
    完。
    节点 为 做 目录项_Nova文件节点 用 目录项。
    若 做 Nova节点_是目录 用 节点：
        返还 错误_是目录。
    完。
    父 为 做 目录项_父 用 目录项。
    若 父 不等于 空：
        做 目录项_移除子 用 父、目录项。
    完。
    做 Nova文件节点_减链接 用 节点。
    若 做 Nova文件节点_链接数 用 节点 等于 0：
        做 Nova文件节点_释放 用 节点。
    完。
    unlink_总次数 为 unlink_总次数 加 1。
    返还 成功。
完。

可变 描述_已初始化 为 假。

可变 dup_次数 为 0。

可变 dup2_次数 为 0。

可变 dup3_次数 为 0。

可变 open_次数 为 0。

可变 close_次数 为 0。

可变 mkdir_次数 为 0。

可变 rmdir_次数 为 0。

可变 unlink_总次数 为 0。

可变 openat_次数 为 0。

可变 close_range_次数 为 0。

可变 描述_错误数 为 0。

可变 描述_延迟总计 为 0。

可变 描述_延迟样本 为 0。

可变 描述_最大延迟 为 0。

可变 描述_速率 为 0。

可变 描述_速率窗口 为 0。

可变 描述_速率时刻 为 0。

可变 描述_峰值速率 为 0。

O_CLOEXEC 为 0x80000。

AT_FDCWD 为 0 减 100。

sys_dup3 接受 oldfd、newfd、flags、_d、_e、_f：
    若 oldfd 小于 0：
        描述_错误数 为 描述_错误数 加 1。
        返还 错误_坏文件描述符。
    完。
    若 oldfd 等于 newfd：
        描述_错误数 为 描述_错误数 加 1。
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_内存不足。
    完。
    文件表 为 做 进程_文件表 用 当前。
    旧文件 为 做 进程_获取打开文件_描述 用 当前、newfd。
    若 旧文件 不等于 空：
        做 文件表_关闭_描述 用 当前、newfd。
    完。
    复制结果 为 做 文件表_复制到_描述 用 当前、oldfd、newfd。
    若 复制结果 小于 0：
        描述_错误数 为 描述_错误数 加 1。
        返还 复制结果。
    完。
    若 flags 位与 O_CLOEXEC 不等于 0：
        做 文件表_设置cloexec 用 文件表、newfd。
    完。
    dup3_次数 为 dup3_次数 加 1。
    描述_速率窗口 为 描述_速率窗口 加 1。
    返还 newfd。
完。

sys_openat 接受 dirfd、路径、flags、mode、_e、_f：
    若 路径 等于 空：
        描述_错误数 为 描述_错误数 加 1。
        返还 错误_无效参数。
    完。
    openat_次数 为 openat_次数 加 1。
    描述_速率窗口 为 描述_速率窗口 加 1。
    返还 做 do_sys_openat 用 dirfd、路径、flags、mode。
完。

sys_close_range 接受 first、last、flags、_d、_e、_f：
    若 first 大于 last：
        描述_错误数 为 描述_错误数 加 1。
        返还 错误_无效参数。
    完。
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_内存不足。
    完。
    可变 fd 为 first。
    只要 fd 小于或等于 last：
        文件 为 做 进程_获取打开文件_描述 用 当前、fd。
        若 文件 不等于 空：
            做 文件表_关闭_描述 用 当前、fd。
        完。
        fd 为 fd 加 1。
    完。
    close_range_次数 为 close_range_次数 加 1。
    描述_速率窗口 为 描述_速率窗口 加 1。
    返还 0。
完。

sys_unlinkat 接受 dirfd、路径、flags、_d、_e、_f：
    若 路径 等于 空：
        描述_错误数 为 描述_错误数 加 1。
        返还 错误_无效参数。
    完。
    unlink_总次数 为 unlink_总次数 加 1。
    描述_速率窗口 为 描述_速率窗口 加 1。
    返还 0。
完。

sys_mkdirat 接受 dirfd、路径、mode、_d、_e、_f：
    若 路径 等于 空：
        描述_错误数 为 描述_错误数 加 1。
        返还 错误_无效参数。
    完。
    mkdir_次数 为 mkdir_次数 加 1。
    描述_速率窗口 为 描述_速率窗口 加 1。
    返还 0。
完。

描述_记录延迟 接受 延迟：
    描述_延迟样本 为 描述_延迟样本 加 1。
    描述_延迟总计 为 描述_延迟总计 加 延迟。
    若 延迟 大于 描述_最大延迟：
        描述_最大延迟 为 延迟。
    完。
完。

描述_平均延迟 接受 零参：
    若 描述_延迟样本 等于 0：
        返还 0。
    完。
    返还 描述_延迟总计 除以 描述_延迟样本。
完。

描述_更新速率 接受 零参：
    现在 为 做 读取纳秒_描述。
    间隔 为 现在 减 描述_速率时刻。
    若 间隔 大于或等于 1000000000：
        描述_速率 为 描述_速率窗口 乘 1000000000 除以 间隔。
        若 描述_速率 大于 描述_峰值速率：
            描述_峰值速率 为 描述_速率。
        完。
        描述_速率窗口 为 0。
        描述_速率时刻 为 现在。
    完。
完。

复制描述_完整统计 接受 零参：
    总操作 为 dup_次数 加 dup2_次数 加 dup3_次数 加 open_次数 加 close_次数 加 mkdir_次数 加 rmdir_次数 加 unlink_总次数 加 openat_次数 加 close_range_次数。
    统计 为 做 字典。
    做 字典设 用 统计、「已初始化」、描述_已初始化。
    做 字典设 用 统计、「dup」、dup_次数。
    做 字典设 用 统计、「dup2」、dup2_次数。
    做 字典设 用 统计、「dup3」、dup3_次数。
    做 字典设 用 统计、「open」、open_次数。
    做 字典设 用 统计、「close」、close_次数。
    做 字典设 用 统计、「openat」、openat_次数。
    做 字典设 用 统计、「close_range」、close_range_次数。
    做 字典设 用 统计、「mkdir」、mkdir_次数。
    做 字典设 用 统计、「rmdir」、rmdir_次数。
    做 字典设 用 统计、「unlink」、unlink_总次数。
    做 字典设 用 统计、「总操作」、总操作。
    做 字典设 用 统计、「平均延迟(ns)」、做 描述_平均延迟。
    做 字典设 用 统计、「最大延迟(ns)」、描述_最大延迟。
    做 字典设 用 统计、「速率」、描述_速率。
    做 字典设 用 统计、「峰值速率」、描述_峰值速率。
    做 字典设 用 统计、「错误数」、描述_错误数。
    返还 统计。
完。

复制描述_打印统计 接受 零参：
    统计 为 做 复制描述_完整统计。
    做 打印 用 「[文件描述符] ═══════ 完整统计 ═══════」。
    做 打印 用 「  dup:          」 加 做 转字符串 用 做 字典取 用 统计、「dup」。
    做 打印 用 「  dup2:         」 加 做 转字符串 用 做 字典取 用 统计、「dup2」。
    做 打印 用 「  dup3:         」 加 做 转字符串 用 做 字典取 用 统计、「dup3」。
    做 打印 用 「  open:         」 加 做 转字符串 用 做 字典取 用 统计、「open」。
    做 打印 用 「  close:        」 加 做 转字符串 用 做 字典取 用 统计、「close」。
    做 打印 用 「  openat:       」 加 做 转字符串 用 做 字典取 用 统计、「openat」。
    做 打印 用 「  close_range:  」 加 做 转字符串 用 做 字典取 用 统计、「close_range」。
    做 打印 用 「  mkdir:        」 加 做 转字符串 用 做 字典取 用 统计、「mkdir」。
    做 打印 用 「  rmdir:        」 加 做 转字符串 用 做 字典取 用 统计、「rmdir」。
    做 打印 用 「  unlink:       」 加 做 转字符串 用 做 字典取 用 统计、「unlink」。
    做 打印 用 「  总操作:       」 加 做 转字符串 用 做 字典取 用 统计、「总操作」。
    做 打印 用 「  平均延迟:     」 加 做 转字符串 用 做 字典取 用 统计、「平均延迟(ns)」 加 「ns」。
    做 打印 用 「  速率:         」 加 做 转字符串 用 做 字典取 用 统计、「速率」 加 「 次/秒」。
    做 打印 用 「  错误数:       」 加 做 转字符串 用 做 字典取 用 统计、「错误数」。
    做 打印 用 「[文件描述符] ═════════════════════════」。
    返还 0。
完。

复制描述_dump 接受 零参：
    做 打印 用 「[文件描述符 dump] ──────────────────」。
    做 打印 用 「  dup: 」 加 做 转字符串 用 dup_次数。
    做 打印 用 「  dup2: 」 加 做 转字符串 用 dup2_次数。
    做 打印 用 「  dup3: 」 加 做 转字符串 用 dup3_次数。
    做 打印 用 「  open: 」 加 做 转字符串 用 open_次数。
    做 打印 用 「  close: 」 加 做 转字符串 用 close_次数。
    做 打印 用 「  openat: 」 加 做 转字符串 用 openat_次数。
    做 打印 用 「  mkdir: 」 加 做 转字符串 用 mkdir_次数。
    做 打印 用 「  unlink: 」 加 做 转字符串 用 unlink_总次数。
    做 打印 用 「  速率: 」 加 做 转字符串 用 描述_速率。
    做 打印 用 「  错误: 」 加 做 转字符串 用 描述_错误数。
    做 打印 用 「[文件描述符 dump] ──────────────────」。
完。

复制描述_销毁 接受 零参：
    描述_已初始化 为 假。
    返还 0。
完。

复制描述_初始化 接受 零参：
    若 描述_已初始化 等于 真：
        返还 错误_已存在。
    完。
    dup_次数 为 0。
    dup2_次数 为 0。
    dup3_次数 为 0。
    open_次数 为 0。
    close_次数 为 0。
    mkdir_次数 为 0。
    rmdir_次数 为 0。
    unlink_总次数 为 0。
    openat_次数 为 0。
    close_range_次数 为 0。
    描述_错误数 为 0。
    描述_已初始化 为 真。
    做 打印 用 「[文件描述符] 初始化完成」。
    返还 0。
完。

复制描述_为procfs提供统计 接受 零参：
    返还 一组：dup_次数、dup2_次数、dup3_次数、open_次数、close_次数、openat_次数、mkdir_次数、rmdir_次数、unlink_总次数。
完。

复制描述_为AI提供文件操作模式 接受 零参：
    返还 一组：open_次数、close_次数、dup_次数 加 dup2_次数 加 dup3_次数、mkdir_次数、unlink_总次数、做 描述_平均延迟。
完。

复制描述_重置统计 接受 零参：
    dup_次数 为 0。
    dup2_次数 为 0。
    dup3_次数 为 0。
    open_次数 为 0。
    close_次数 为 0。
    mkdir_次数 为 0。
    rmdir_次数 为 0。
    unlink_总次数 为 0。
    openat_次数 为 0。
    close_range_次数 为 0。
    描述_错误数 为 0。
    描述_延迟总计 为 0。
    描述_延迟样本 为 0。
    描述_最大延迟 为 0。
    描述_速率 为 0。
    描述_速率窗口 为 0。
    描述_峰值速率 为 0。
    做 打印 用 「[文件描述符] 统计已重置」。
    返还 0。
完。

进程_获取打开文件_描述 接受 进程、fd：
    若 进程 等于 空：
        返还 空。
    完。
    文件表 为 做 进程_文件表 用 进程。
    若 文件表 等于 空：
        返还 空。
    完。
    返还 做 文件表_获取 用 文件表、fd。
完。

文件表_关闭_描述 接受 进程、fd：
    若 进程 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件表 为 做 进程_文件表 用 进程。
    若 文件表 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件 为 做 文件表_获取 用 文件表、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    做 文件表_关闭 用 文件表、fd。
    剩余 为 做 文件_减引用 用 文件。
    若 剩余 小于或等于 0 并且 做 文件_操作表 用 文件 不等于 空 并且 做 文件_操作表 用 文件 的 release 不等于 空：
        做 做 文件_操作表 用 文件 的 release 用 文件 的 第 F_Nova文件节点 项、文件。
    完。
    返还 0。
完。

文件表_复制到_描述 接受 进程、旧fd、新fd：
    若 进程 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件表 为 做 进程_文件表 用 进程。
    若 文件表 等于 空：
        返还 错误_坏文件描述符。
    完。
    结果 为 做 文件表_dup2 用 文件表、旧fd、新fd。
    返还 结果。
完。

文件_设置cloexec_描述 接受 文件、值：
    若 文件 等于 空：
        返还。
    完。
    若 值 不等于 0：
        文件 的 f_flags 为 文件 的 f_flags 位或 O_CLOEXEC。
    否则：
        文件 的 f_flags 为 文件 的 f_flags 位与 按位取反 O_CLOEXEC。
    完。
完。

文件_目录项 接受 文件：
    若 文件 等于 空：
        返还 空。
    完。
    若 文件 的 f_Nova路径项 不等于 空：
        返还 文件 的 f_Nova路径项。
    完。
    若 文件 的 第 F_目录项 项 不等于 空：
        返还 文件 的 第 F_目录项 项。
    完。
    返还 空。
完。

获取当前工作目录 接受 零参：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 空。
    完。
    若 当前 的 cwd 不等于 空：
        返还 当前 的 cwd。
    完。
    返还 做 获取根目录项。
完。

读取纳秒_描述 接受 零参：
    可变 值 为 0。
    汇编。
    块：
        「rdtsc」。
        「shl rdx, 32」。
        「or rax, rdx」。
        「mov [rbp-8], rax」。
    完。
    返还 值。
完。

复制描述_为调度器提供IO负载 接受 零参：
    返还 一组：描述_速率、描述_峰值速率、做 描述_平均延迟。
完。

复制描述_为电源管理挂起 接受 零参：
    返还 0。
    返还 0。
完。

复制描述_获取总操作数 接受 零参：
    返还 dup_次数 加 dup2_次数 加 dup3_次数 加 open_次数 加 close_次数 加 mkdir_次数 加 rmdir_次数 加 unlink_总次数 加 openat_次数 加 close_range_次数。
完。

复制描述_获取打开关闭比 接受 零参：
    若 close_次数 等于 0：
        返还 0。
    完。
    返还 open_次数 乘 100 除以 close_次数。
完。

本章 收束。
