《读写调度器》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·整数转字符串》。

读截止 为 500000。

写截止 为 5000000。

批量大小 为 16。

饥饿阈值 为 4。

读写调度器 是 一种 事物，含 读队头、读队尾、读数量、写队头、写队尾、写数量、上次扇区、上次方向、总处理、总合并、读饥饿计数、写饥饿计数、当前批次、锁。

可变 全局调度 为 0。

读写调度_初始化 接受 零参：
    全局调度 为 做 堆_分配 用 读写调度器。
    全局调度 的 读队头 为 0。
    全局调度 的 读队尾 为 0。
    全局调度 的 读数量 为 0。
    全局调度 的 写队头 为 0。
    全局调度 的 写队尾 为 0。
    全局调度 的 写数量 为 0。
    全局调度 的 上次扇区 为 0。
    全局调度 的 上次方向 为 0。
    全局调度 的 总处理 为 0。
    全局调度 的 总合并 为 0。
    全局调度 的 读饥饿计数 为 0。
    全局调度 的 写饥饿计数 为 0。
    全局调度 的 当前批次 为 0。
    全局调度 的 锁 为 做 自旋锁_创建。
完。

读写调度_添加 接受 请求：
    若 全局调度 等于 0 或者 请求 等于 0：
        返还。
    完。
    做 自旋锁_获取 用 全局调度 的 锁。
    合并 为 做 读写调度_尝试合并 用 请求。
    若 合并：
        全局调度 的 总合并 为 全局调度 的 总合并 加 1。
        做 自旋锁_释放 用 全局调度 的 锁。
        返还。
    完。
    若 请求 的 类型 等于 0：
        请求 的 后继 为 0。
        若 全局调度 的 读队尾 不等于 0：
            全局调度 的 读队尾 的 后继 为 请求。
        否则：
            全局调度 的 读队头 为 请求。
        完。
        全局调度 的 读队尾 为 请求。
        全局调度 的 读数量 为 全局调度 的 读数量 加 1。
    否则：
        请求 的 后继 为 0。
        若 全局调度 的 写队尾 不等于 0：
            全局调度 的 写队尾 的 后继 为 请求。
        否则：
            全局调度 的 写队头 为 请求。
        完。
        全局调度 的 写队尾 为 请求。
        全局调度 的 写数量 为 全局调度 的 写数量 加 1。
    完。
    做 自旋锁_释放 用 全局调度 的 锁。
完。

读写调度_尝试合并 接受 请求：
    可变 当前 为 0。
    若 请求 的 类型 等于 0：
        当前 为 全局调度 的 读队头。
    否则：
        当前 为 全局调度 的 写队头。
    完。
    只要 当前 不等于 0：
        若 当前 的 设备号 等于 请求 的 设备号：
            若 当前 的 起始扇区 加 当前 的 扇区数 等于 请求 的 起始扇区：
                当前 的 扇区数 为 当前 的 扇区数 加 请求 的 扇区数。
                返还 真。
            完。
        完。
        当前 为 当前 的 后继。
    完。
    返还 假。
完。

读写调度_分发 接受 零参：
    若 全局调度 等于 0：
        返还 0。
    完。
    做 自旋锁_获取 用 全局调度 的 锁。
    可变 选中 为 0。
    若 全局调度 的 读数量 大于 0 并且 全局调度 的 写数量 大于 0：
        若 全局调度 的 写饥饿计数 大于或等于 饥饿阈值：
            选中 为 做 读写调度_取写。
            全局调度 的 写饥饿计数 为 0。
            全局调度 的 读饥饿计数 为 全局调度 的 读饥饿计数 加 1。
        否则：
            选中 为 做 读写调度_取读。
            全局调度 的 读饥饿计数 为 0。
            全局调度 的 写饥饿计数 为 全局调度 的 写饥饿计数 加 1。
        完。
    否则若 全局调度 的 读数量 大于 0：
        选中 为 做 读写调度_取读。
    否则若 全局调度 的 写数量 大于 0：
        选中 为 做 读写调度_取写。
    完。
    若 选中 不等于 0：
        全局调度 的 上次扇区 为 选中 的 起始扇区 加 选中 的 扇区数。
        全局调度 的 总处理 为 全局调度 的 总处理 加 1。
        全局调度 的 当前批次 为 全局调度 的 当前批次 加 1。
    完。
    做 自旋锁_释放 用 全局调度 的 锁。
    返还 选中。
完。

读写调度_取读 接受 零参：
    若 全局调度 的 读队头 等于 0：
        返还 0。
    完。
    请求 为 全局调度 的 读队头。
    全局调度 的 读队头 为 请求 的 后继。
    若 全局调度 的 读队头 等于 0：
        全局调度 的 读队尾 为 0。
    完。
    全局调度 的 读数量 为 全局调度 的 读数量 减 1。
    返还 请求。
完。

读写调度_取写 接受 零参：
    若 全局调度 的 写队头 等于 0：
        返还 0。
    完。
    请求 为 全局调度 的 写队头。
    全局调度 的 写队头 为 请求 的 后继。
    若 全局调度 的 写队头 等于 0：
        全局调度 的 写队尾 为 0。
    完。
    全局调度 的 写数量 为 全局调度 的 写数量 减 1。
    返还 请求。
完。

读写调度_队列深度 接受 零参：
    若 全局调度 等于 0：
        返还 0。
    完。
    返还 全局调度 的 读数量 加 全局调度 的 写数量。
完。

读写调度_完整统计 接受 零参：
    若 全局调度 等于 0：
        返还 一组：0、0、0、0、0。
    完。
    返还 一组：全局调度 的 读数量、全局调度 的 写数量、全局调度 的 总处理、全局调度 的 总合并、全局调度 的 当前批次。
完。

读写调度_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 全局调度 等于 0：
        做 追加 用 问题、「IO调度器未初始化」。
        返还 问题。
    完。
    若 全局调度 的 读数量 加 全局调度 的 写数量 大于 1000：
        做 追加 用 问题、「IO队列过长: 」 加 做 整数转字符串 用 全局调度 的 读数量 加 全局调度 的 写数量。
    完。
    若 全局调度 的 读饥饿计数 大于 饥饿阈值 乘 3：
        做 追加 用 问题、「读饥饿严重」。
    完。
    若 全局调度 的 写饥饿计数 大于 饥饿阈值 乘 3：
        做 追加 用 问题、「写饥饿严重」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「IO调度器健康」。
    完。
    返还 问题。
完。

读写调度_详细统计 接受 零参：
    若 全局调度 等于 0：
        返还 空。
    完。
    返还 一本字典：读队列 对应 全局调度 的 读数量、写队列 对应 全局调度 的 写数量、总处理 对应 全局调度 的 总处理、总合并 对应 全局调度 的 总合并、合并率 对应 做 读写调度_合并率、读饥饿 对应 全局调度 的 读饥饿计数、写饥饿 对应 全局调度 的 写饥饿计数、上次扇区 对应 全局调度 的 上次扇区、批次 对应 全局调度 的 当前批次。
完。

读写调度_合并率 接受 零参：
    若 全局调度 等于 0：
        返还 0。
    完。
    总 为 全局调度 的 总处理 加 全局调度 的 总合并。
    若 总 等于 0：
        返还 0。
    完。
    返还 全局调度 的 总合并 乘 100 除以 总。
完。

读写调度_重置统计 接受 零参：
    若 全局调度 等于 0：
        返还。
    完。
    全局调度 的 总处理 为 0。
    全局调度 的 总合并 为 0。
    全局调度 的 读饥饿计数 为 0。
    全局调度 的 写饥饿计数 为 0。
    全局调度 的 当前批次 为 0。
完。

读写调度_打印统计 接受 零参：
    若 全局调度 等于 0：
        做 内核信息 用 「IO调度器: 未初始化」。
        返还。
    完。
    做 内核信息 用 「IO调度器统计:」。
    做 内核信息 用 「  读队列: 」 加 做 整数转字符串 用 全局调度 的 读数量 加 「 写队列: 」 加 做 整数转字符串 用 全局调度 的 写数量。
    做 内核信息 用 「  总处理: 」 加 做 整数转字符串 用 全局调度 的 总处理 加 「 总合并: 」 加 做 整数转字符串 用 全局调度 的 总合并 加 「 合并率: 」 加 做 整数转字符串 用 做 读写调度_合并率 加 「%」。
完。

自旋锁_创建 接受 零参：
    返还 一本字典：值 对应 0、持有者 对应 0。
完。

自旋锁_获取 接受 锁：
    若 锁 等于 空 或者 锁 等于 0：
        返还。
    完。
    可变 自旋次数 为 0。
    最大自旋 为 10000000。
    只要 锁 的 值 不等于 0 并且 自旋次数 小于 最大自旋：
        汇编。
        块：
            「pause」。
        完。
        自旋次数 为 自旋次数 加 1。
    完。
    若 自旋次数 大于或等于 最大自旋：
        做 内核警告 用 「自旋锁超时: 可能死锁, 持有者PID=」 加 做 整数转字符串 用 锁 的 持有者。
    完。
    锁 的 值 为 1。
    锁 的 持有者 为 做 获取当前PID。
完。

自旋锁_释放 接受 锁：
    若 锁 等于 空 或者 锁 等于 0：
        返还。
    完。
    锁 的 持有者 为 0。
    锁 的 值 为 0。
完。

可变 AI_输入调度器_负载窗口 为 一组：（空）。

AI_输入调度器_负载预测 接受 当前负载：
    做 追加 用 AI_输入调度器_负载窗口、当前负载。
    若 做 计数 用 AI_输入调度器_负载窗口 大于 20：
        做 列表_移除 用 AI_输入调度器_负载窗口、0。
    完。
    若 做 计数 用 AI_输入调度器_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_输入调度器_负载窗口。
    趋势 为 AI_输入调度器_负载窗口 的 第 n 减 1 项 减 AI_输入调度器_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_输入调度器_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
