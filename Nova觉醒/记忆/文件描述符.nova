《文件描述符》

引用《..·中枢·错误码》。

引用《..·中枢·同步原语》。

引用《..·中枢·内存物理》。

引用《..·中枢·块内存分配器》。

引用《.·管道读端关》。

引用《..·中枢·每处理器》。

NR_OPEN_DEFAULT 为 64。

NR_OPEN_MAX 为 1048576。

最大打开文件 为 256。

文件表结构 是 一种 事物，含 count、fdt、open_fds、close_on_exec、next_fd、max_fds、锁。

文件对象 是 一种 事物，含 f_Nova文件节点、f_pos、f_flags、f_count、f_op、f_Nova路径项、f_vfsmnt、f_mode、f_cred。

FMODE_READ 为 0x1。

FMODE_WRITE 为 0x2。

FMODE_EXEC 为 0x20。

文件表_创建 接受 零参：
    fs 为 做 kmalloc 用 文件表结构。
    若 fs 等于 空：
        返还 空。
    完。
    fs 的 count 为 1。
    fs 的 fdt 为 一组：（空）。
    fs 的 open_fds 为 0。
    fs 的 close_on_exec 为 0。
    fs 的 next_fd 为 0。
    fs 的 max_fds 为 最大打开文件。
    fs 的 锁 为 0。
    可变 i 为 0。
    只要 i 小于 最大打开文件：
        做 追加 用 fs 的 fdt、空。
        i 为 i 加 1。
    完。
    返还 fs。
完。

文件表_分配 接受 表：
    返还 做 文件表_分配_从 用 表、0。
完。

文件表_分配_从 接受 表、起始fd：
    可变 fd 为 起始fd。
    若 fd 小于 表 的 next_fd：
        fd 为 表 的 next_fd。
    完。
    只要 fd 小于 表 的 max_fds：
        若 表 的 fdt 的 第 fd 项 等于 空：
            表 的 open_fds 为 表 的 open_fds 位或 1 左移 fd。
            表 的 next_fd 为 fd 加 1。
            返还 fd。
        完。
        fd 为 fd 加 1。
    完。
    旧max 为 表 的 max_fds。
    新max 为 旧max 乘 2。
    若 新max 大于 4096：
        新max 为 4096。
    完。
    若 新max 小于或等于 旧max：
        返还 错误_打开过多。
    完。
    可变 i 为 旧max。
    只要 i 小于 新max：
        做 追加 用 表 的 fdt、空。
        i 为 i 加 1。
    完。
    表 的 max_fds 为 新max。
    返还 做 文件表_分配fd 用 表。
完。

文件表_获取 接受 表、fd：
    若 fd 小于 0 或者 fd 大于或等于 表 的 max_fds：
        返还 空。
    完。
    返还 表 的 fdt 的 第 fd 项。
完。

文件表_设置 接受 表、fd、文件：
    若 fd 小于 0 或者 fd 大于或等于 表 的 max_fds：
        返还。
    完。
    做 设置 用 表 的 fdt、fd、文件。
    若 文件 不等于 空：
        表 的 open_fds 为 表 的 open_fds 位或 1 左移 fd。
    完。
完。

文件表_关闭 接受 表、fd：
    若 fd 小于 0 或者 fd 大于或等于 表 的 max_fds：
        返还。
    完。
    做 设置 用 表 的 fdt、fd、空。
    表 的 open_fds 为 表 的 open_fds 位与 按位取反 (1 左移 fd)。
    表 的 close_on_exec 为 表 的 close_on_exec 位与 按位取反 (1 左移 fd)。
    若 fd 小于 表 的 next_fd：
        表 的 next_fd 为 fd。
    完。
完。

文件表_设置cloexec 接受 表、fd：
    若 fd 大于或等于 0 并且 fd 小于 表 的 max_fds：
        表 的 close_on_exec 为 表 的 close_on_exec 位或 1 左移 fd。
    完。
完。

文件表_清除cloexec 接受 表、fd：
    若 fd 大于或等于 0 并且 fd 小于 表 的 max_fds：
        表 的 close_on_exec 为 表 的 close_on_exec 位与 按位取反 (1 左移 fd)。
    完。
完。

文件表_是cloexec 接受 表、fd：
    若 fd 大于或等于 0 并且 fd 小于 表 的 max_fds：
        返还 表 的 close_on_exec 右移 fd 位与 1。
    完。
    返还 0。
完。

文件表_exec关闭 接受 表：
    可变 fd 为 0。
    只要 fd 小于 表 的 max_fds：
        若 表 的 close_on_exec 右移 fd 位与 1：
            做 文件表_关闭 用 表、fd。
        完。
        fd 为 fd 加 1。
    完。
完。

文件表_复制 接受 源表：
    新表 为 做 文件表_创建。
    新表 的 open_fds 为 源表 的 open_fds。
    新表 的 close_on_exec 为 源表 的 close_on_exec。
    新表 的 next_fd 为 源表 的 next_fd。
    可变 fd 为 0。
    只要 fd 小于 源表 的 max_fds 并且 fd 小于 新表 的 max_fds：
        文件 为 源表 的 fdt 的 第 fd 项。
        若 文件 不等于 空：
            做 文件_增引用 用 文件。
            做 设置 用 新表 的 fdt、fd、文件。
        完。
        fd 为 fd 加 1。
    完。
    返还 新表。
完。

文件表_共享 接受 表：
    表 的 count 为 表 的 count 加 1。
    返还 表。
完。

文件表_减引用 接受 表：
    表 的 count 为 表 的 count 减 1。
    若 表 的 count 小于或等于 0：
        做 文件表_关闭全部 用 表。
    完。
完。

文件表_关闭全部 接受 表：
    可变 fd 为 0。
    只要 fd 小于 表 的 max_fds：
        若 表 的 fdt 的 第 fd 项 不等于 空：
            文件 为 表 的 fdt 的 第 fd 项。
            剩余 为 做 文件_减引用 用 文件。
            若 剩余 小于或等于 0：
            完。
            做 设置 用 表 的 fdt、fd、空。
        完。
        fd 为 fd 加 1。
    完。
完。

文件_创建 接受 Nova文件节点、标志：
    f 为 新建 文件对象。
    f 的 f_Nova文件节点 为 Nova文件节点。
    f 的 f_pos 为 0。
    f 的 f_flags 为 标志。
    f 的 f_count 为 1。
    f 的 f_op 为 空。
    f 的 f_Nova路径项 为 空。
    f 的 f_vfsmnt 为 空。
    f 的 f_mode 为 0。
    若 标志 位与 3 等于 0 或者 标志 位与 3 等于 2：
        f 的 f_mode 为 f 的 f_mode 位或 FMODE_READ。
    完。
    若 标志 位与 3 等于 1 或者 标志 位与 3 等于 2：
        f 的 f_mode 为 f 的 f_mode 位或 FMODE_WRITE。
    完。
    返还 f。
完。

文件_偏移 接受 文件：
    返还 文件 的 f_pos。
完。

文件_设置偏移 接受 文件、偏移：
    文件 的 f_pos 为 偏移。
完。

文件_增引用 接受 文件：
    文件 的 f_count 为 文件 的 f_count 加 1。
完。

文件_减引用 接受 文件：
    文件 的 f_count 为 文件 的 f_count 减 1。
    返还 文件 的 f_count。
完。

文件描述符_可读 接受 文件：
    返还 文件 的 f_mode 位与 FMODE_READ 不等于 0。
完。

文件描述符_可写 接受 文件：
    返还 文件 的 f_mode 位与 FMODE_WRITE 不等于 0。
完。

文件_有cloexec 接受 文件：
    返还 文件 的 f_flags 位与 O_CLOEXEC 不等于 0。
完。

O_CLOEXEC 为 0x80000。

文件表_已打开数 接受 表：
    可变 数 为 0。
    可变 fd 为 0。
    只要 fd 小于 表 的 max_fds：
        若 表 的 fdt 的 第 fd 项 不等于 空：
            数 为 数 加 1。
        完。
        fd 为 fd 加 1。
    完。
    返还 数。
完。

文件描述符_分配 接受 零参：
    当前 为 做 获取当前进程。
    返还 做 文件表_分配 用 当前 的 第 T_文件表 项。
完。

文件描述符_安装 接受 fd、文件：
    当前 为 做 获取当前进程。
    做 文件表_设置 用 当前 的 第 T_文件表 项、fd、文件。
完。

文件描述符_设置标志 接受 fd、标志：
    当前 为 做 获取当前进程。
    若 标志 位与 O_CLOEXEC：
        做 文件表_设置cloexec 用 当前 的 第 T_文件表 项、fd。
    完。
完。

文件描述符_获取 接受 fd：
    当前 为 做 获取当前进程。
    返还 做 文件表_获取 用 当前 的 第 T_文件表 项、fd。
完。

文件_poll 接受 文件、事件：
    若 文件 等于 空：
        返还 0。
    完。
    返还 0。
完。

可变 fd_总分配数 为 0。

可变 fd_总关闭数 为 0。

可变 fd_总复制数 为 0。

文件表_dup 接受 表、旧fd：
    文件 为 做 文件表_获取 用 表、旧fd。
    若 文件 等于 空：
        返还 负 1。
    完。
    新fd 为 做 文件表_分配 用 表。
    若 新fd 小于 0：
        返还 新fd。
    完。
    做 文件_增引用 用 文件。
    做 文件表_设置 用 表、新fd、文件。
    fd_总复制数 为 fd_总复制数 加 1。
    返还 新fd。
完。

文件表_dup2 接受 表、旧fd、新fd：
    若 旧fd 等于 新fd：
        返还 新fd。
    完。
    文件 为 做 文件表_获取 用 表、旧fd。
    若 文件 等于 空：
        返还 负 1。
    完。
    若 新fd 小于 0 或者 新fd 大于或等于 表 的 max_fds：
        返还 负 1。
    完。
    若 做 文件表_获取 用 表、新fd 不等于 空：
        做 文件表_关闭 用 表、新fd。
    完。
    做 文件_增引用 用 文件。
    做 文件表_设置 用 表、新fd、文件。
    fd_总复制数 为 fd_总复制数 加 1。
    返还 新fd。
完。

文件表_dup3 接受 表、旧fd、新fd、标志：
    若 旧fd 等于 新fd：
        返还 负 1。
    完。
    结果 为 做 文件表_dup2 用 表、旧fd、新fd。
    若 结果 大于或等于 0 并且 标志 位与 O_CLOEXEC：
        做 文件表_设置cloexec 用 表、新fd。
    完。
    返还 结果。
完。

F_GETFD 为 1。

F_SETFD 为 2。

F_GETFL 为 3。

F_SETFL 为 4。

F_DUPFD 为 5。

F_DUPFD_CLOEXEC 为 6。

FD_CLOEXEC 为 1。

O_APPEND 为 0x400。

O_NONBLOCK 为 0x800。

文件表_fcntl 接受 表、fd、命令、参数：
    文件 为 做 文件表_获取 用 表、fd。
    若 文件 等于 空：
        返还 负 1。
    完。
    若 命令 等于 F_GETFD：
        返还 做 文件表_是cloexec 用 表、fd。
    完。
    若 命令 等于 F_SETFD：
        若 参数 位与 FD_CLOEXEC：
            做 文件表_设置cloexec 用 表、fd。
        否则：
            做 文件表_清除cloexec 用 表、fd。
        完。
        返还 0。
    完。
    若 命令 等于 F_GETFL：
        返还 文件 的 f_flags。
    完。
    若 命令 等于 F_SETFL：
        文件 的 f_flags 为 文件 的 f_flags 位与 按位取反 (O_APPEND 位或 O_NONBLOCK) 位或 参数 位与 (O_APPEND 位或 O_NONBLOCK)。
        返还 0。
    完。
    若 命令 等于 F_DUPFD：
        返还 做 文件表_分配_从 用 表、参数。
    完。
    若 命令 等于 F_DUPFD_CLOEXEC：
        新fd 为 做 文件表_分配_从 用 表、参数。
        若 新fd 大于或等于 0：
            做 文件_增引用 用 文件。
            做 文件表_设置 用 表、新fd、文件。
            做 文件表_设置cloexec 用 表、新fd。
        完。
        返还 新fd。
    完。
    返还 负 1。
完。

SEEK_SET 为 0。

SEEK_CUR 为 1。

SEEK_END 为 2。

文件_lseek 接受 文件、偏移、基点：
    若 文件 等于 空：
        返还 负 1。
    完。
    可变 新位置 为 0。
    若 基点 等于 SEEK_SET：
        新位置 为 偏移。
    否则若 基点 等于 SEEK_CUR：
        新位置 为 文件 的 f_pos 加 偏移。
    否则若 基点 等于 SEEK_END：
        大小 为 做 Nova文件节点_获取大小 用 文件 的 f_Nova文件节点。
        新位置 为 大小 加 偏移。
    否则：
        返还 负 1。
    完。
    若 新位置 小于 0：
        返还 负 1。
    完。
    文件 的 f_pos 为 新位置。
    返还 新位置。
完。

文件表_遍历 接受 表、回调：
    可变 fd 为 0。
    只要 fd 小于 表 的 max_fds：
        若 表 的 fdt 的 第 fd 项 不等于 空：
            做 回调 用 fd、表 的 fdt 的 第 fd 项。
        完。
        fd 为 fd 加 1。
    完。
完。

文件描述符_dup 接受 旧fd：
    当前 为 做 获取当前进程。
    返还 做 文件表_dup 用 当前 的 第 T_文件表 项、旧fd。
完。

文件描述符_dup2 接受 旧fd、新fd：
    当前 为 做 获取当前进程。
    返还 做 文件表_dup2 用 当前 的 第 T_文件表 项、旧fd、新fd。
完。

文件描述符_fcntl 接受 fd、命令、参数：
    当前 为 做 获取当前进程。
    返还 做 文件表_fcntl 用 当前 的 第 T_文件表 项、fd、命令、参数。
完。

文件描述符_关闭 接受 fd：
    当前 为 做 获取当前进程。
    做 文件表_关闭 用 当前 的 第 T_文件表 项、fd。
    fd_总关闭数 为 fd_总关闭数 加 1。
完。

文件表_创建管道 接受 表、结果：
    读端 为 做 文件_创建 用 0、0。
    写端 为 做 文件_创建 用 0、1。
    若 读端 等于 空 或者 写端 等于 空：
        返还 负 1。
    完。
    读_fd 为 做 文件表_分配 用 表。
    若 读_fd 小于 0：
        返还 负 1。
    完。
    做 文件表_设置 用 表、读_fd、读端。
    写_fd 为 做 文件表_分配 用 表。
    若 写_fd 小于 0：
        做 文件表_关闭 用 表、读_fd。
        返还 负 1。
    完。
    做 文件表_设置 用 表、写_fd、写端。
    结果 的 第 0 项 为 读_fd。
    结果 的 第 1 项 为 写_fd。
    返还 0。
完。

文件描述符_fstat 接受 fd：
    当前 为 做 获取当前进程。
    文件 为 做 文件表_获取 用 当前 的 第 T_文件表 项、fd。
    若 文件 等于 空：
        返还 空。
    完。
    返还 一组：文件 的 f_Nova文件节点、文件 的 f_pos、文件 的 f_flags、文件 的 f_count、文件 的 f_mode。
完。

文件表_获取最大已用fd 接受 表：
    可变 最大 为 负 1。
    可变 fd 为 0。
    只要 fd 小于 表 的 max_fds：
        若 表 的 fdt 的 第 fd 项 不等于 空：
            最大 为 fd。
        完。
        fd 为 fd 加 1。
    完。
    返还 最大。
完。

文件描述符_完整统计 接受 零参：
    返还 一组：一组：「总分配」、fd_总分配数、一组：「总关闭」、fd_总关闭数、一组：「总复制」、fd_总复制数。
完。

可变 AI_文件描述符_运行指标 为 0。

可变 AI_文件描述符_异常指标 为 0。

AI_文件描述符_自诊断 接受 零参：
    AI_文件描述符_运行指标 为 0。
    返还 AI_文件描述符_运行指标。
完。

AI_文件描述符_优化建议 接受 零参：
    指标 为 做 AI_文件描述符_自诊断。
    返还 「文件描述符状态正常」。
完。

本章 收束。
