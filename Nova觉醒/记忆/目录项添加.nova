《目录项添加》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《.·路径缓存初始化》。

引用《..·中枢·链表后继》。

引用《.·文件节点创建》。

引用《..·中枢·同步原语》。

目录项_添加子 接受 父、子：
    做 自旋锁_获取 用 父 的 锁。
    做 链表_尾插 用 父 的 子链表、子。
    子 的 父 为 父。
    若 父 的 Nova文件节点 不等于 空 并且 做 Nova节点_是目录 用 子 的 Nova文件节点：
        做 Nova文件节点_增链接 用 父 的 Nova文件节点。
    完。
    做 自旋锁_释放 用 父 的 锁。
完。

目录项_移除子 接受 父、子：
    做 自旋锁_获取 用 父 的 锁。
    可变 节点 为 做 链表_首节点_或空 用 父 的 子链表。
    只要 节点 不等于 空 并且 节点 不等于 父 的 子链表：
        若 做 链表_数据 用 节点 等于 子：
            做 链表_删除 用 节点。
            若 父 的 Nova文件节点 不等于 空 并且 子 的 Nova文件节点 不等于 空 并且 做 Nova节点_是目录 用 子 的 Nova文件节点：
                做 Nova文件节点_减链接 用 父 的 Nova文件节点。
            完。
            做 自旋锁_释放 用 父 的 锁。
            返还 真。
        完。
        节点 为 做 链表_后继 用 节点。
    完。
    做 自旋锁_释放 用 父 的 锁。
    返还 假。
完。

目录项_查找子 接受 条目、名称：
    缓存结果 为 做 Nova路径缓存_查找 用 条目、名称。
    若 缓存结果 不等于 空：
        返还 缓存结果。
    完。
    做 自旋锁_获取 用 条目 的 锁。
    可变 节点 为 做 链表_首节点_或空 用 条目 的 子链表。
    只要 节点 不等于 空 并且 节点 不等于 条目 的 子链表：
        子 为 做 链表_数据 用 节点。
        若 子 的 名称 等于 名称：
            做 Nova路径项_增引用 用 子。
            做 自旋锁_释放 用 条目 的 锁。
            返还 子。
        完。
        节点 为 做 链表_后继 用 节点。
    完。
    做 自旋锁_释放 用 条目 的 锁。
    返还 空。
完。

Nova路径项_释放 接受 d：
    若 d 的 Nova文件节点 不等于 空：
        做 Nova文件节点_减引用 用 d 的 Nova文件节点。
        d 的 Nova文件节点 为 空。
    完。
    若 d 的 标志 位与 Nova路径缓存_MISS_TYPE 等于 Nova路径缓存_MISS_TYPE：
        Nova路径缓存负项数 为 Nova路径缓存负项数 减 1。
    完。
    Nova路径缓存总数 为 Nova路径缓存总数 减 1。
完。

Nova路径缓存_收缩 接受 需要释放数：
    可变 已释放 为 0。
    只要 已释放 小于 需要释放数 并且 Nova路径缓存_LRU数 大于 0：
        节点 为 做 链表_尾节点 用 Nova路径缓存_LRU。
        若 节点 等于 空：
            中断。
        完。
        d 为 做 链表_数据 用 节点。
        做 链表_删除 用 节点。
        Nova路径缓存_LRU数 为 Nova路径缓存_LRU数 减 1。
        做 Nova路径项_释放 用 d。
        已释放 为 已释放 加 1。
    完。
    返还 已释放。
完。

Nova路径项_删除 接受 d：
    做 自旋锁_获取 用 d 的 锁。
    若 d 的 引用计数 大于 1：
        若 d 的 Nova文件节点 不等于 空：
            做 Nova文件节点_减引用 用 d 的 Nova文件节点。
            d 的 Nova文件节点 为 空。
        完。
        d 的 标志 为 d 的 标志 位与 按位取反 Nova路径缓存_ENTRY_TYPE 位或 Nova路径缓存_MISS_TYPE。
        Nova路径缓存负项数 为 Nova路径缓存负项数 加 1。
        做 自旋锁_释放 用 d 的 锁。
    否则：
        做 自旋锁_释放 用 d 的 锁。
        做 Nova路径项_释放 用 d。
    完。
完。

目录项_名称 接受 条目：
    返还 条目 的 名称。
完。

目录项_Nova文件节点 接受 条目：
    返还 条目 的 Nova文件节点。
完。

目录项_父 接受 条目：
    返还 条目 的 父。
完。

目录项_子列表 接受 条目：
    返还 条目 的 子链表。
完。

目录项_是负项 接受 d：
    返还 d 的 Nova文件节点 等于 空。
完。

目录项_是目录 接受 d：
    返还 d 的 标志 位与 Nova路径缓存_ENTRY_TYPE 等于 Nova路径缓存_DIRECTORY_TYPE。
完。

目录项_是挂载点 接受 d：
    返还 d 的 标志 位与 Nova路径缓存_MOUNTED 不等于 0。
完。

Nova路径缓存_统计 接受 零参：
    返还 一组：Nova路径缓存总数、Nova路径缓存_LRU数、Nova路径缓存负项数。
完。

目录项_设置已挂载 接受 项：
    若 项 不等于 空：
        项 的 已挂载 为 真。
    完。
完。

本章 收束。
