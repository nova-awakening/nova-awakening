《挂载移动》

引用《..·中枢·类型定义》。
引用《..·中枢·错误码》。
引用《.·目录项添加》。
引用《..·中枢·链表后继》。
引用《..·中枢·内核打印》。
引用《.·超级块》。
引用《..·中枢·虚拟内存区》。
引用《.·VFS文件读写》。
引用《.·路径查找》。

do_move_mount 接受 源、目标：
    mnt 为 做 查找挂载_按目标 用 源。
    若 mnt 等于 空：

    根 为 做 获取根目录项。
    新目标 为 做 路径查找 用 根、目标。
    若 新目标 等于 空：

    ※ 清除旧挂载点标记, 设置新挂载点。
    做 目录项_清除已挂载 用 mnt.mnt_mountpoint。
    mnt.mnt_mountpoint 为 新目标。
    做 目录项_设置已挂载 用 新目标。

    做 内核信息 用 「move mount: 」 加 源 加 「 -> 」 加 目标。
    返还 成功。
完。

※ do_umount: 卸载。

do_umount 接受 目标、标志：
    mnt 为 做 查找挂载_按目标 用 目标。
    若 mnt 等于 空：

    ※ 检查是否有子挂载。
    若 做 计数 用 mnt.mnt_children 大于 0：
        返还 错误_忙
    完。

    ※ 同步文件系统。
    做 超级块_同步 用 mnt.mnt_sb。

    ※ 清除挂载点标记。
    做 目录项_清除已挂载 用 mnt.mnt_mountpoint。

    ※ 减少super_block引用。
    做 超级块_减引用 用 mnt.mnt_sb。

    ※ 从挂载表移除。
    可变 i 为 0。
    只要 i 小于 做 计数 用 挂载表：
        若 挂载表[i].mnt_id 等于 mnt.mnt_id：
            做 链表_删除 用 挂载表、i。
            挂载_总数 为 挂载_总数 减 1。
            中断。
        完。
        i 为 i 加 1。
    完。

    做 内核信息 用 「umount: 」 加 目标。
    返还 成功。
完。

※ pivot_root: 切换根文件系统。

pivot_root 接受 新根、旧根：
    ※ 将新根变成/, 旧的/移到旧根位置。
    若 根挂载 等于 空：
    ※ 查找新根和旧根的挂载点。
    新根Nova路径项 为 做 路径查找 用 做 获取根目录项 用 、0、新根。
    若 新根Nova路径项 等于 空：
    旧根Nova路径项 为 做 路径查找 用 做 获取根目录项 用 、0、旧根。
    若 旧根Nova路径项 等于 空：
    ※ 交换挂载树: 新根→/, 旧/→旧根位置。
    旧根挂载 为 根挂载。
    新根挂载 为 做 查找挂载 用 新根Nova路径项。
    若 新根挂载 等于 空：
    根挂载 为 新根挂载。
    ※ 将旧根挂载移到旧根路径下。
    旧根挂载.挂载点 为 旧根Nova路径项。
    做 内核信息 用 「pivot_root: 」 加 新根 加 「 -> 」 加 旧根。
    返还 成功。
完。

※ 列出挂载。

列出挂载 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 挂载表：
        mnt 为 挂载表[i]。
        只读 为 做 如果选字符 用 (mnt.mnt_flags 位与 MS_RDONLY) 不等于 0、「ro」、「rw」。
        打印行(mnt.mnt_devname 加 「 」 加 目录项_路径(mnt.mnt_mountpoint) 加 「 」 +。
               mnt.mnt_fstype 加 「 」 加 只读 加 「 0 0」)。
        i 为 i 加 1。
    完。
完。

※ 查找挂载。

查找挂载_按目标 接受 路径：
    根 为 做 获取根目录项。
    目标Nova路径项 为 做 路径查找 用 根、路径。
    若 目标Nova路径项 等于 空：
    返还 做 查找挂载_按Nova路径项 用 目标Nova路径项。
完。

查找挂载_按Nova路径项 接受 Nova路径项：
    可变 i 为 0。
    只要 i 小于 做 计数 用 挂载表：
        若 挂载表[i].mnt_mountpoint 等于 Nova路径项：
            返还 挂载表[i]。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

目录项_是挂载点_挂载 接受 Nova路径项：
    返还 做 查找挂载_按Nova路径项 用 Nova路径项 不等于 空。
完。

※ 统计。

挂载_统计 接受 零参：
    返还 挂载_总数。
完。

※ 错误_不支持设备 定义在 核心/公共/错误码.nova。

※ 挂载点辅助函数(路径查找需要)。

目录项_是挂载根 接受 Nova路径项：
    ※ 检查此Nova路径项是否是某个挂载的根目录项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 挂载表：
        若 挂载表[i].根Nova路径项 等于 Nova路径项：
        i 为 i 加 1。
    完。
    返还 假。
完。

挂载点_获取父Nova路径项 接受 Nova路径项：
    ※ 给定挂载根Nova路径项, 返回挂载点所在的父文件系统的Nova路径项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 挂载表：
        若 挂载表[i].根Nova路径项 等于 Nova路径项：
        i 为 i 加 1。
    完。
    返还 空。
完。

挂载点_获取根Nova路径项 接受 Nova路径项：
    ※ 给定挂载点Nova路径项, 返回挂载在此处的文件系统的根Nova路径项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 挂载表：
        若 挂载表[i].挂载点 等于 Nova路径项：
        i 为 i 加 1。
    完。
    返还 空。
完。

查找挂载 接受 Nova路径项：
    ※ 查找以此Nova路径项为根的挂载。
    可变 i 为 0。
    只要 i 小于 做 计数 用 挂载表：
        若 挂载表[i].根Nova路径项 等于 Nova路径项：
        i 为 i 加 1。
    完。
    返还 空。
完。

可变 AI_执行移挂载_热文件表 为 []。

AI_执行移挂载_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_执行移挂载_热文件表：
        若 AI_执行移挂载_热文件表[i].路径 等于 路径：
            AI_执行移挂载_热文件表[i].频率 为 AI_执行移挂载_热文件表[i].频率 加 1。
            返还 AI_执行移挂载_热文件表[i].频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_执行移挂载_热文件表 小于 128：
        做 追加 用 AI_执行移挂载_热文件表、{ 路径: 路径、频率: 1 }。
    完。
    返还 1。
完。

AI_执行移挂载_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
    返还 32768。
完。

本章 收束。

本章 收束。

本章 收束。
