《进程信息》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·进程标识分》。

引用《..·中枢·信号获取默》。

引用《..·中枢·内存系统调用》。

引用《..·中枢·时钟中枢》。

引用《..·中枢·进程描述符》。

引用《..·中枢·获取进程》。

引用《..·中枢·网络系统调用》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·字符串替换》。

引用《..·中枢·虚拟内存区》。

引用《..·中枢·虚分配故障》。

proc_进程状态 接受 PID：
    任务 为 做 进程表_查找 用 PID。
    若 任务 等于 空：
        返还 空。
    完。
    可变 输出 为 「」。
    输出 为 输出 加 「Name:\t」 加 做 进程_名称 用 任务 加 「\n」。
    输出 为 输出 加 「Umask:\t0022\n」。
    输出 为 输出 加 「State:\t」 加 做 进程_状态字符串 用 做 进程_状态 用 任务 加 「\n」。
    输出 为 输出 加 「Tgid:\t」 加 做 整数转字符串 用 PID 加 「\n」。
    输出 为 输出 加 「Ngid:\t0\n」。
    输出 为 输出 加 「Pid:\t」 加 做 整数转字符串 用 PID 加 「\n」。
    输出 为 输出 加 「PPid:\t」 加 做 整数转字符串 用 做 进程_父PID 用 任务 加 「\n」。
    输出 为 输出 加 「TracerPid:\t0\n」。
    输出 为 输出 加 「Uid:\t」 加 做 整数转字符串 用 做 进程_uid 用 任务 加 「\t」 加 做 整数转字符串 用 做 进程_euid 用 任务 加 「\t」 加 做 整数转字符串 用 做 进程_uid 用 任务 加 「\t」 加 做 整数转字符串 用 做 进程_uid 用 任务 加 「\n」。
    输出 为 输出 加 「Gid:\t」 加 做 整数转字符串 用 做 进程_gid 用 任务 加 「\t」 加 做 整数转字符串 用 做 进程_egid 用 任务 加 「\t」 加 做 整数转字符串 用 做 进程_gid 用 任务 加 「\t」 加 做 整数转字符串 用 做 进程_gid 用 任务 加 「\n」。
    输出 为 输出 加 「FDSize:\t256\n」。
    输出 为 输出 加 「Groups:\t\n」。
    输出 为 输出 加 「Threads:\t」 加 做 整数转字符串 用 做 进程_线程数 用 任务 加 「\n」。
    输出 为 输出 加 「SigPnd:\t」 加 做 整数转十六进制 用 做 进程_信号挂起 用 任务 加 「\n」。
    输出 为 输出 加 「SigBlk:\t」 加 做 整数转十六进制 用 做 进程_信号掩码 用 任务 加 「\n」。
    输出 为 输出 加 「SigIgn:\t0000000000000000\n」。
    输出 为 输出 加 「SigCgt:\t0000000000000000\n」。
    mm 为 做 进程_mm 用 任务。
    若 mm 不等于 空：
        输出 为 输出 加 「VmPeak:\t」 加 做 整数转字符串 用 做 mm_vm_peak 用 mm 除以 1024 加 「 kB\n」。
        输出 为 输出 加 「VmSize:\t」 加 做 整数转字符串 用 做 mm_vm_size 用 mm 除以 1024 加 「 kB\n」。
        输出 为 输出 加 「VmRSS:\t」 加 做 整数转字符串 用 做 mm_rss 用 mm 乘 4 加 「 kB\n」。
        输出 为 输出 加 「VmData:\t」 加 做 整数转字符串 用 做 mm_data_size 用 mm 除以 1024 加 「 kB\n」。
        输出 为 输出 加 「VmStk:\t」 加 做 整数转字符串 用 做 mm_stack_size 用 mm 除以 1024 加 「 kB\n」。
        输出 为 输出 加 「VmExe:\t」 加 做 整数转字符串 用 做 mm_text_size 用 mm 除以 1024 加 「 kB\n」。
    完。
    输出 为 输出 加 「voluntary_ctxt_switches:\t」 加 做 整数转字符串 用 做 进程_自愿切换 用 任务 加 「\n」。
    输出 为 输出 加 「nonvoluntary_ctxt_switches:\t」 加 做 整数转字符串 用 做 进程_非自愿切换 用 任务 加 「\n」。
    返还 输出。
完。

proc_进程stat 接受 PID：
    任务 为 做 进程表_查找 用 PID。
    若 任务 等于 空：
        返还 空。
    完。
    状态字符 为 做 进程_状态单字符 用 做 进程_状态 用 任务。
    ppid 为 做 进程_父PID 用 任务。
    pgrp 为 做 进程_组ID 用 任务。
    session 为 做 进程_会话ID 用 任务。
    utime 为 做 进程_用户时间 用 任务。
    stime 为 做 进程_内核时间 用 任务。
    优先级 为 做 进程_优先级 用 任务。
    nice 为 做 进程_nice 用 任务。
    线程数 为 做 进程_线程数 用 任务。
    vsize 为 0。
    rss 为 0。
    mm 为 做 进程_mm 用 任务。
    若 mm 不等于 空：
        vsize 为 做 mm_vm_size 用 mm。
        rss 为 做 mm_rss 用 mm。
    完。
    返还 做 整数转字符串 用 PID 加 「 (」 加 做 进程_名称 用 任务 加 「) 」 加 状态字符 加 「 」 加 做 整数转字符串 用 ppid 加 「 」 加 做 整数转字符串 用 pgrp 加 「 」 加 做 整数转字符串 用 session 加 「 0 0 0 0 0 0 0 」 加 做 整数转字符串 用 utime 加 「 」 加 做 整数转字符串 用 stime 加 「 0 0 」 加 做 整数转字符串 用 优先级 加 「 」 加 做 整数转字符串 用 nice 加 「 」 加 做 整数转字符串 用 线程数 加 「 0 0 」 加 做 整数转字符串 用 vsize 加 「 」 加 做 整数转字符串 用 rss 加 「 0\n」。
完。

proc_进程cmdline 接受 PID：
    任务 为 做 进程表_查找 用 PID。
    若 任务 等于 空：
        返还 空。
    完。
    返还 做 进程_命令行 用 任务。
完。

proc_进程maps 接受 PID：
    任务 为 做 进程表_查找 用 PID。
    若 任务 等于 空：
        返还 空。
    完。
    mm 为 做 进程_mm 用 任务。
    若 mm 等于 空：
        返还 「」。
    完。
    可变 输出 为 「」。
    vma列表 为 做 mm_vma列表 用 mm。
    可变 i 为 0。
    只要 i 小于 做 计数 用 vma列表：
        vma 为 vma列表 的 第 i 项。
        起始 为 做 VMA_起始 用 vma。
        结束 为 做 VMA_结束 用 vma。
        权限 为 做 VMA_权限字符串 用 vma。
        输出 为 输出 加 做 整数转十六进制 用 起始 加 「-」 加 做 整数转十六进制 用 结束 加 「 」 加 权限 加 「 00000000 00:00 0\n」。
        i 为 i 加 1。
    完。
    返还 输出。
完。

proc_进程fd列表 接受 PID：
    任务 为 做 进程表_查找 用 PID。
    若 任务 等于 空：
        返还 一组：（空）。
    完。
    文件表 为 做 进程_文件表 用 任务。
    若 文件表 等于 空：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 fd 为 0。
    只要 fd 小于 256：
        若 做 文件表_获取 用 文件表、fd 不等于 空：
            做 追加 用 结果、fd。
        完。
        fd 为 fd 加 1。
    完。
    返还 结果。
完。

proc_进程文件读 接受 PID、文件名：
    若 文件名 等于 「status」：
        返还 做 proc_进程状态 用 PID。
    完。
    若 文件名 等于 「stat」：
        返还 做 proc_进程stat 用 PID。
    完。
    若 文件名 等于 「cmdline」：
        返还 做 proc_进程cmdline 用 PID。
    完。
    若 文件名 等于 「maps」：
        返还 做 proc_进程maps 用 PID。
    完。
    若 文件名 等于 「comm」：
        任务 为 做 进程表_查找 用 PID。
        若 任务 不等于 空：
            返还 做 进程_名称 用 任务 加 「\n」。
        完。
    完。
    若 文件名 等于 「cwd」：
        返还 「/proc/」 加 做 整数转字符串 用 PID 加 「/cwd」。
    完。
    若 文件名 等于 「exe」：
        返还 「/proc/」 加 做 整数转字符串 用 PID 加 「/exe」。
    完。
    返还 空。
完。

进程_状态字符串 接受 状态：
    若 状态 等于 0：
        返还 「R (running)」。
    完。
    若 状态 等于 1：
        返还 「S (sleeping)」。
    完。
    若 状态 等于 2：
        返还 「D (disk sleep)」。
    完。
    若 状态 等于 3：
        返还 「T (stopped)」。
    完。
    若 状态 等于 4：
        返还 「Z (zombie)」。
    完。
    若 状态 等于 5：
        返还 「X (dead)」。
    完。
    返还 「? (unknown)」。
完。

进程_状态单字符 接受 状态：
    若 状态 等于 0：
        返还 「R」。
    完。
    若 状态 等于 1：
        返还 「S」。
    完。
    若 状态 等于 2：
        返还 「D」。
    完。
    若 状态 等于 3：
        返还 「T」。
    完。
    若 状态 等于 4：
        返还 「Z」。
    完。
    若 状态 等于 5：
        返还 「X」。
    完。
    返还 「?」。
完。

VMA_权限字符串_proc 接受 vma：
    可变 s 为 「」。
    s 为 s 加 做 如果选字符 用 做 VMA_保护 用 vma 位与 0x1 不等于 0、「r」、「-」。
    s 为 s 加 做 如果选字符 用 做 VMA_保护 用 vma 位与 0x2 不等于 0、「w」、「-」。
    s 为 s 加 做 如果选字符 用 做 VMA_保护 用 vma 位与 0x4 不等于 0、「x」、「-」。
    s 为 s 加 「p」。
    返还 s。
完。

整数转十六进制_proc 接受 值：
    若 值 等于 0：
        返还 「0」。
    完。
    可变 结果 为 「」。
    可变 n 为 值。
    只要 n 大于 0：
        位 为 n 取余于 16。
        若 位 小于 10：
            结果 为 做 字符 用 48 加 位 加 结果。
        否则：
            结果 为 做 字符 用 87 加 位 加 结果。
        完。
        n 为 n 除以 16。
    完。
    返还 结果。
完。

proc_进程io 接受 PID：
    任务 为 做 进程表_查找 用 PID。
    若 任务 等于 空：
        返还 空。
    完。
    可变 输出 为 「」。
    输出 为 输出 加 「rchar: 0\n」。
    输出 为 输出 加 「wchar: 0\n」。
    输出 为 输出 加 「syscr: 0\n」。
    输出 为 输出 加 「syscw: 0\n」。
    输出 为 输出 加 「read_bytes: 0\n」。
    输出 为 输出 加 「write_bytes: 0\n」。
    返还 输出。
完。

进程信息_完整统计 接受 零参：
    返还 一组：做 进程_总数。
完。

可变 AI_进程信息_负载窗口 为 一组：（空）。

AI_进程信息_负载预测 接受 当前负载：
    做 追加 用 AI_进程信息_负载窗口、当前负载。
    若 做 计数 用 AI_进程信息_负载窗口 大于 20：
        做 列表_移除 用 AI_进程信息_负载窗口、0。
    完。
    若 做 计数 用 AI_进程信息_负载窗口 小于 3：
        返还 当前负载。
    完。
    n 为 做 计数 用 AI_进程信息_负载窗口。
    趋势 为 AI_进程信息_负载窗口 的 第 n 减 1 项 减 AI_进程信息_负载窗口 的 第 n 减 3 项。
    返还 当前负载 加 趋势。
完。

AI_进程信息_迁移建议 接受 源CPU负载、目标CPU负载：
    若 源CPU负载 大于 目标CPU负载 乘 2：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
