《文件节点管理》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·内容可见性》。

最大缓存节点 为 1024。

哈希桶数 为 128。

节点_普通 为 1。

节点_目录 为 2。

节点_符号 为 3。

节点_块设备 为 4。

节点_字符设备 为 5。

文件节点 是 一种 事物，含 编号、类型、大小、权限、属主、属组、硬链接数、引用计数、创建时刻、修改时刻、访问时刻、脏标记、块列表、块数、设备号、哈希后继、淘汰前驱、淘汰后继。

可变 哈希表 为 0。

可变 缓存数 为 0。

可变 淘汰头 为 0。

可变 淘汰尾 为 0。

可变 下一编号 为 1。

可变 分配次数 为 0。

可变 释放次数 为 0。

可变 命中次数 为 0。

可变 未命中次数 为 0。

可变 回写次数 为 0。

可变 脏节点数 为 0。

文件节点_初始化 接受 零参：
    哈希表 为 做 堆_分配 用 哈希桶数 乘 8。
    可变 序号 为 0。
    只要 序号 小于 哈希桶数：
        做 内存_写64 用 哈希表 加 序号 乘 8、0。
        序号 为 序号 加 1。
    完。
    缓存数 为 0。
    淘汰头 为 0。
    淘汰尾 为 0。
完。

文件节点_分配 接受 类型、权限：
    若 缓存数 大于或等于 最大缓存节点：
        做 文件节点_淘汰。
    完。
    分配次数 为 分配次数 加 1。
    节点 为 做 堆_分配 用 文件节点。
    节点 的 编号 为 下一编号。
    下一编号 为 下一编号 加 1。
    节点 的 类型 为 类型。
    节点 的 大小 为 0。
    节点 的 权限 为 权限。
    节点 的 属主 为 0。
    节点 的 属组 为 0。
    节点 的 硬链接数 为 1。
    节点 的 引用计数 为 1。
    节点 的 创建时刻 为 做 平台_获取高精度时间。
    节点 的 修改时刻 为 节点 的 创建时刻。
    节点 的 访问时刻 为 节点 的 创建时刻。
    节点 的 脏标记 为 假。
    节点 的 块列表 为 0。
    节点 的 块数 为 0。
    节点 的 设备号 为 0。
    节点 的 哈希后继 为 0。
    节点 的 淘汰前驱 为 0。
    节点 的 淘汰后继 为 0。
    桶号 为 节点 的 编号 取余于 哈希桶数。
    节点 的 哈希后继 为 做 内存_读64 用 哈希表 加 桶号 乘 8。
    做 内存_写64 用 哈希表 加 桶号 乘 8、节点。
    缓存数 为 缓存数 加 1。
    做 文件节点_加淘汰头 用 节点。
    返还 节点。
完。

文件节点_查找 接受 编号：
    桶号 为 编号 取余于 哈希桶数。
    可变 当前 为 做 内存_读64 用 哈希表 加 桶号 乘 8。
    只要 当前 不等于 0：
        若 当前 的 编号 等于 编号：
            当前 的 引用计数 为 当前 的 引用计数 加 1。
            当前 的 访问时刻 为 做 平台_获取高精度时间。
            命中次数 为 命中次数 加 1。
            做 文件节点_移淘汰头 用 当前。
            返还 当前。
        完。
        当前 为 当前 的 哈希后继。
    完。
    未命中次数 为 未命中次数 加 1。
    返还 0。
完。

文件节点_释放 接受 节点：
    若 节点 等于 0：
        返还。
    完。
    节点 的 引用计数 为 节点 的 引用计数 减 1。
完。

文件节点_标脏 接受 节点：
    若 节点 等于 0：
        返还。
    完。
    若 非 节点 的 脏标记：
        节点 的 脏标记 为 真。
        脏节点数 为 脏节点数 加 1。
    完。
    节点 的 修改时刻 为 做 平台_获取高精度时间。
完。

文件节点_回写 接受 节点：
    若 节点 等于 0 或者 非 节点 的 脏标记：
        返还。
    完。
    节点 的 脏标记 为 假。
    脏节点数 为 脏节点数 减 1。
    回写次数 为 回写次数 加 1。
完。

文件节点_回写全部 接受 零参：
    可变 桶号 为 0。
    只要 桶号 小于 哈希桶数：
        可变 当前 为 做 内存_读64 用 哈希表 加 桶号 乘 8。
        只要 当前 不等于 0：
            若 当前 的 脏标记：
                做 文件节点_回写 用 当前。
            完。
            当前 为 当前 的 哈希后继。
        完。
        桶号 为 桶号 加 1。
    完。
完。

文件节点_淘汰 接受 零参：
    若 淘汰尾 等于 0：
        返还。
    完。
    可变 牺牲 为 淘汰尾。
    可变 搜索 为 0。
    只要 牺牲 不等于 0 并且 搜索 小于 16：
        若 牺牲 的 引用计数 小于或等于 0 并且 非 牺牲 的 脏标记：
            跳出。
        完。
        若 牺牲 的 脏标记：
            做 文件节点_回写 用 牺牲。
        完。
        牺牲 为 牺牲 的 淘汰前驱。
        搜索 为 搜索 加 1。
    完。
    若 牺牲 等于 0：
        返还。
    完。
    做 文件节点_从淘汰移除 用 牺牲。
    桶号 为 牺牲 的 编号 取余于 哈希桶数。
    可变 前驱 为 0。
    可变 当前 为 做 内存_读64 用 哈希表 加 桶号 乘 8。
    只要 当前 不等于 0：
        若 当前 等于 牺牲：
            若 前驱 等于 0：
                做 内存_写64 用 哈希表 加 桶号 乘 8、牺牲 的 哈希后继。
            否则：
                前驱 的 哈希后继 为 牺牲 的 哈希后继。
            完。
            跳出。
        完。
        前驱 为 当前。
        当前 为 当前 的 哈希后继。
    完。
    缓存数 为 缓存数 减 1。
    释放次数 为 释放次数 加 1。
    做 堆_释放 用 牺牲。
完。

文件节点_加淘汰头 接受 节点：
    节点 的 淘汰前驱 为 0。
    节点 的 淘汰后继 为 淘汰头。
    若 淘汰头 不等于 0：
        淘汰头 的 淘汰前驱 为 节点。
    完。
    淘汰头 为 节点。
    若 淘汰尾 等于 0：
        淘汰尾 为 节点。
    完。
完。

文件节点_移淘汰头 接受 节点：
    若 节点 等于 淘汰头：
        返还。
    完。
    做 文件节点_从淘汰移除 用 节点。
    做 文件节点_加淘汰头 用 节点。
完。

文件节点_从淘汰移除 接受 节点：
    若 节点 的 淘汰前驱 不等于 0：
        节点 的 淘汰前驱 的 淘汰后继 为 节点 的 淘汰后继。
    否则：
        淘汰头 为 节点 的 淘汰后继。
    完。
    若 节点 的 淘汰后继 不等于 0：
        节点 的 淘汰后继 的 淘汰前驱 为 节点 的 淘汰前驱。
    否则：
        淘汰尾 为 节点 的 淘汰前驱。
    完。
完。

文件节点_完整统计 接受 零参：
    返还 一组：缓存数、下一编号 减 1、脏节点数、分配次数、释放次数、命中次数、未命中次数、回写次数。
完。

可变 AI_文件节点完_运行指标 为 0。

可变 AI_文件节点完_异常指标 为 0。

AI_文件节点完_自诊断 接受 零参：
    AI_文件节点完_运行指标 为 分配次数。
    返还 AI_文件节点完_运行指标。
完。

AI_文件节点完_优化建议 接受 零参：
    指标 为 做 AI_文件节点完_自诊断。
    若 AI_文件节点完_运行指标 大于 10000：
        返还 「文件节点完负载较高」。
    完。
    返还 「文件节点完状态正常」。
完。

本章 收束。
