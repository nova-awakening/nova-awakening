《ext4目录遍历》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·同步原语》。

引用《.·ext4超级块读取》。

引用《.·文件文件》。

引用《..·中枢·日志中枢》。

引用《.·ext4目录项查找》。

引用《..·执行·控制台输出》。

引用《..·中枢·页缓存标记脏》。

引用《.·ext4块分配器》。

引用《..·中枢·字符串替换》。

引用《..·中枢·墙钟设置》。

引用《.·事务管理》。

引用《.·ext4目录删除》。

引用《.·ext4块组描述符》。

引用《..·稳态·AHCI驱动全局》。

EXT4_目录项_头大小 为 8。

EXT4_目录项_名称偏移 为 8。

EXT4_目录项_节点号偏移 为 0。

EXT4_目录项_记录长偏移 为 4。

EXT4_目录项_名称长偏移 为 6。

EXT4_目录项_类型偏移 为 7。

EXT4_最大名称长度 为 255。

EXT4_目录项类型_未知 为 0。

EXT4_目录项类型_文件 为 1。

EXT4_目录项类型_目录 为 2。

EXT4_目录项类型_字符设备 为 3。

EXT4_目录项类型_块设备 为 4。

EXT4_目录项类型_管道 为 5。

EXT4_目录项类型_套接字 为 6。

EXT4_目录项类型_符号链接 为 7。

EXT4_最大链接数 为 65000。

EXT4_最大符号链接长度 为 4096。

目录遍历上下文 是 一种 事物，含 文件节点、当前块、块内偏移、已返回条目数、缓冲区、缓冲区大小、已写入字节。

目录项解析结果 是 一种 事物，含 文件节点号、记录长度、名称长度、类型、名称、有效。

遍历统计 是 一种 事物，含 读目录次数、读目录返回条目总数、读目录空目录次数、mmap次数、fault次数、fault缓存命中、fault磁盘读取、fault空洞次数、删除文件次数、删除文件成功、删除文件失败、重命名次数、硬链接次数、符号链接次数、目录项解析次数、目录项解析错误。

可变 遍历统计数据 为 新建 遍历统计。

ext4_解析目录项 接受 块数据、偏移、块大小：
    遍历统计数据 的 目录项解析次数 为 遍历统计数据 的 目录项解析次数 加 1。
    若 块数据 等于 空 或者 偏移 大于或等于 块大小：
        遍历统计数据 的 目录项解析错误 为 遍历统计数据 的 目录项解析错误 加 1。
        返还 空。
    完。
    可变 结果 为 新建 目录项解析结果。
    结果 的 文件节点号 为 做 缓冲读32 用 块数据、偏移 加 EXT4_目录项_节点号偏移。
    结果 的 记录长度 为 做 缓冲读16 用 块数据、偏移 加 EXT4_目录项_记录长偏移。
    结果 的 名称长度 为 做 缓冲读8 用 块数据、偏移 加 EXT4_目录项_名称长偏移。
    结果 的 类型 为 做 缓冲读8 用 块数据、偏移 加 EXT4_目录项_类型偏移。
    若 结果 的 记录长度 小于 EXT4_目录项_头大小 或者 结果 的 记录长度 大于 块大小 减 偏移：
        遍历统计数据 的 目录项解析错误 为 遍历统计数据 的 目录项解析错误 加 1。
        结果 的 有效 为 假。
        返还 结果。
    完。
    若 结果 的 名称长度 大于 EXT4_最大名称长度 或者 结果 的 名称长度 大于 结果 的 记录长度 减 EXT4_目录项_头大小：
        遍历统计数据 的 目录项解析错误 为 遍历统计数据 的 目录项解析错误 加 1。
        结果 的 有效 为 假。
        返还 结果。
    完。
    若 结果 的 名称长度 大于 0 并且 结果 的 文件节点号 不等于 0：
        可变 名称 为 「」。
        可变 i 为 0。
        只要 i 小于 结果 的 名称长度：
            字节 为 做 缓冲读8 用 块数据、偏移 加 EXT4_目录项_名称偏移 加 i。
            名称 为 做 字符串拼接 用 名称、做 字符 用 字节。
            i 为 i 加 1。
        完。
        结果 的 名称 为 名称。
        结果 的 有效 为 真。
    否则：
        结果 的 有效 为 假。
    完。
    返还 结果。
完。

ext4_目录项类型转换 接受 ext4类型：
    做 匹配 用 ext4类型。
    返还 0。
完。

ext4_模式到目录项类型 接受 模式：
    类型位 为 模式 位与 0xF000。
    做 匹配 用 类型位。
    返还 EXT4_目录项类型_未知。
完。

ext4_读目录_完整 接受 文件、缓冲、大小：
    遍历统计数据 的 读目录次数 为 遍历统计数据 的 读目录次数 加 1。
    若 文件 等于 空 或者 文件 的 文件节点 等于 空：
        返还 错误码_无效参数。
    完。
    文件节点 为 文件 的 文件节点。
    sb 为 文件节点 的 超级块。
    信息 为 sb 的 私有信息。
    块大小 为 信息 的 块大小。
    若 文件节点 的 模式 位与 0xF000 不等于 0x4000：
        返还 错误码_不是目录。
    完。
    若 文件节点 的 大小 等于 0：
        遍历统计数据 的 读目录空目录次数 为 遍历统计数据 的 读目录空目录次数 加 1。
        返还 0。
    完。
    ext4信息 为 文件节点 的 私有信息。
    若 ext4信息 等于 空：
        返还 错误码_无效参数。
    完。
    可变 上下文 为 新建 目录遍历上下文。
    上下文 的 文件节点 为 文件节点。
    上下文 的 当前块 为 文件 的 偏移 除以 块大小。
    上下文 的 块内偏移 为 文件 的 偏移 取余于 块大小。
    上下文 的 缓冲区 为 缓冲。
    上下文 的 缓冲区大小 为 大小。
    上下文 的 已写入字节 为 0。
    上下文 的 已返回条目数 为 0。
    目录块数 为 (文件节点 的 大小 加 块大小 减 1) 除以 块大小。
    只要 上下文 的 当前块 小于 目录块数 并且 上下文 的 已写入字节 小于 上下文 的 缓冲区大小：
        物理块 为 做 ext4_extent_查找 用 sb、ext4信息、上下文 的 当前块。
        若 物理块 等于 0 减 1 或者 物理块 等于 0：
            上下文 的 当前块 为 上下文 的 当前块 加 1。
            上下文 的 块内偏移 为 0。
            继续。
        完。
        块数据 为 做 块设备_读 用 sb 的 设备、物理块 乘 块大小、块大小。
        若 块数据 等于 空：
            上下文 的 当前块 为 上下文 的 当前块 加 1。
            上下文 的 块内偏移 为 0。
            继续。
        完。
        只要 上下文 的 块内偏移 小于 块大小 并且 上下文 的 已写入字节 小于 上下文 的 缓冲区大小：
            项 为 做 ext4_解析目录项 用 块数据、上下文 的 块内偏移、块大小。
            若 项 等于 空：
                止。
            完。
            若 项 的 记录长度 等于 0：
                止。
            完。
            若 项 的 有效 并且 项 的 文件节点号 不等于 0：
                写入结果 为 做 ext4_填充getdents 用 上下文、项。
                若 写入结果 小于 0：
                    止。
                完。
                上下文 的 已返回条目数 为 上下文 的 已返回条目数 加 1。
            完。
            上下文 的 块内偏移 为 上下文 的 块内偏移 加 项 的 记录长度。
        完。
        上下文 的 当前块 为 上下文 的 当前块 加 1。
        上下文 的 块内偏移 为 0。
    完。
    文件 的 偏移 为 上下文 的 当前块 乘 块大小 加 上下文 的 块内偏移。
    遍历统计数据 的 读目录返回条目总数 为 遍历统计数据 的 读目录返回条目总数 加 上下文 的 已返回条目数。
    返还 上下文 的 已写入字节。
完。

ext4_填充getdents 接受 上下文、项：
    条目大小 为 8 加 8 加 2 加 1 加 项 的 名称长度 加 1。
    对齐大小 为 (条目大小 加 7) 除以 8 乘 8。
    若 上下文 的 已写入字节 加 对齐大小 大于 上下文 的 缓冲区大小：
        返还 负 1。
    完。
    偏移 为 上下文 的 已写入字节。
    做 缓冲写64 用 上下文 的 缓冲区、偏移、项 的 文件节点号。
    做 缓冲写64 用 上下文 的 缓冲区、偏移 加 8、上下文 的 当前块 乘 4096 加 上下文 的 块内偏移 加 项 的 记录长度。
    做 缓冲写16 用 上下文 的 缓冲区、偏移 加 16、对齐大小。
    做 缓冲写 用 上下文 的 缓冲区、偏移 加 18、项 的 类型。
    可变 i 为 0。
    只要 i 小于 项 的 名称长度：
        字节 为 做 编码 用 做 子串 用 项 的 名称、i、1。
        做 缓冲写 用 上下文 的 缓冲区、偏移 加 19 加 i、字节。
        i 为 i 加 1。
    完。
    做 缓冲写 用 上下文 的 缓冲区、偏移 加 19 加 项 的 名称长度、0。
    上下文 的 已写入字节 为 上下文 的 已写入字节 加 对齐大小。
    返还 0。
完。

ext4_内存映射 接受 文件、vma：
    遍历统计数据 的 mmap次数 为 遍历统计数据 的 mmap次数 加 1。
    若 文件 等于 空 或者 文件 的 文件节点 等于 空：
        返还 错误码_无效参数。
    完。
    若 文件 的 文件节点 的 模式 位与 0xF000 等于 0x4000：
        返还 错误码_不支持。
    完。
    vma 的 操作_fault 为 ext4_文件fault。
    vma 的 操作_map_pages 为 ext4_map_pages。
    vma 的 私有数据 为 文件 的 文件节点。
    返还 0。
完。

ext4_文件fault 接受 vma、地址：
    遍历统计数据 的 fault次数 为 遍历统计数据 的 fault次数 加 1。
    若 vma 等于 空 或者 vma 的 私有数据 等于 空：
        返还 空。
    完。
    文件节点 为 vma 的 私有数据。
    sb 为 文件节点 的 超级块。
    块大小 为 sb 的 私有信息 的 块大小。
    偏移 为 地址 减 vma 的 起始 加 vma 的 文件偏移。
    逻辑块 为 偏移 除以 块大小。
    若 偏移 大于或等于 文件节点 的 大小：
        返还 空。
    完。
    缓存页 为 做 页面缓存_查找 用 文件节点、逻辑块。
    若 缓存页 不等于 空：
        遍历统计数据 的 fault缓存命中 为 遍历统计数据 的 fault缓存命中 加 1。
        返还 缓存页。
    完。
    ext4信息 为 文件节点 的 私有信息。
    若 ext4信息 等于 空：
        返还 空。
    完。
    物理块 为 做 ext4_extent_查找 用 sb、ext4信息、逻辑块。
    若 物理块 等于 0 减 1 或者 物理块 等于 0：
        遍历统计数据 的 fault空洞次数 为 遍历统计数据 的 fault空洞次数 加 1。
        零页 为 做 创建缓冲 用 块大小。
        做 页面缓存_添加 用 文件节点、逻辑块、零页。
        返还 零页。
    完。
    遍历统计数据 的 fault磁盘读取 为 遍历统计数据 的 fault磁盘读取 加 1。
    块数据 为 做 块设备_读 用 sb 的 设备、物理块 乘 块大小、块大小。
    若 块数据 不等于 空：
        做 页面缓存_添加 用 文件节点、逻辑块、块数据。
    完。
    返还 块数据。
完。

ext4_map_pages 接受 vma、起始地址、页数：
    文件节点 为 vma 的 私有数据。
    若 文件节点 等于 空：
        返还 0。
    完。
    sb 为 文件节点 的 超级块。
    块大小 为 sb 的 私有信息 的 块大小。
    ext4信息 为 文件节点 的 私有信息。
    若 ext4信息 等于 空：
        返还 0。
    完。
    可变 已映射 为 0。
    可变 i 为 0。
    只要 i 小于 页数 并且 i 小于 16：
        偏移 为 起始地址 加 i 乘 块大小 减 vma 的 起始 加 vma 的 文件偏移。
        若 偏移 大于或等于 文件节点 的 大小：
            止。
        完。
        逻辑块 为 偏移 除以 块大小。
        缓存 为 做 页面缓存_查找 用 文件节点、逻辑块。
        若 缓存 等于 空：
            物理块 为 做 ext4_extent_查找 用 sb、ext4信息、逻辑块。
            若 物理块 不等于 0 减 1 并且 物理块 不等于 0：
                数据 为 做 块设备_读 用 sb 的 设备、物理块 乘 块大小、块大小。
                若 数据 不等于 空：
                    做 页面缓存_添加 用 文件节点、逻辑块、数据。
                    已映射 为 已映射 加 1。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 已映射。
完。

ext4_删除文件 接受 sb、父文件节点、名称：
    遍历统计数据 的 删除文件次数 为 遍历统计数据 的 删除文件次数 加 1。
    若 sb 等于 空 或者 父文件节点 等于 空 或者 名称 等于 空：
        遍历统计数据 的 删除文件失败 为 遍历统计数据 的 删除文件失败 加 1。
        返还 错误码_无效参数。
    完。
    若 做 字节长度 用 名称 等于 0 或者 做 字节长度 用 名称 大于 EXT4_最大名称长度：
        遍历统计数据 的 删除文件失败 为 遍历统计数据 的 删除文件失败 加 1。
        返还 错误码_名称过长。
    完。
    若 名称 等于 「.」 或者 名称 等于 「..」：
        遍历统计数据 的 删除文件失败 为 遍历统计数据 的 删除文件失败 加 1。
        返还 错误码_无效参数。
    完。
    项 为 做 ext4_查找目录项 用 sb、父文件节点、名称。
    若 项 等于 空：
        遍历统计数据 的 删除文件失败 为 遍历统计数据 的 删除文件失败 加 1。
        返还 错误码_不存在。
    完。
    子文件节点 为 做 ext4_读文件节点 用 sb、项 的 文件节点号。
    若 做 是错误 用 子文件节点：
        遍历统计数据 的 删除文件失败 为 遍历统计数据 的 删除文件失败 加 1。
        返还 子文件节点。
    完。
    若 子文件节点 的 模式 位与 0xF000 等于 0x4000：
        遍历统计数据 的 删除文件失败 为 遍历统计数据 的 删除文件失败 加 1。
        返还 错误码_是目录。
    完。
    事务 为 做 journal_开始事务。
    删除结果 为 做 ext4_删除目录项 用 sb、父文件节点、名称。
    若 删除结果 不等于 0：
        若 事务 不等于 空：
            做 journal_回滚事务 用 事务。
        完。
        遍历统计数据 的 删除文件失败 为 遍历统计数据 的 删除文件失败 加 1。
        返还 删除结果。
    完。
    若 事务 不等于 空：
        做 journal_记录修改 用 事务、父文件节点 的 编号、父文件节点。
    完。
    子文件节点 的 链接数 为 子文件节点 的 链接数 减 1。
    子文件节点 的 变更时间 为 做 当前时间_秒。
    若 子文件节点 的 链接数 等于 0：
        子文件节点 的 删除时间 为 做 当前时间_秒。
        做 打印 用 做 字符串拼接 用 「[ext4] 文件已标记删除: 」、名称。
    完。
    做 ext4_写文件节点 用 sb、子文件节点。
    若 事务 不等于 空：
        做 journal_记录修改 用 事务、子文件节点 的 编号、子文件节点。
    完。
    父文件节点 的 修改时间 为 做 当前时间_秒。
    父文件节点 的 变更时间 为 做 当前时间_秒。
    做 ext4_写文件节点 用 sb、父文件节点。
    若 事务 不等于 空：
        做 journal_提交事务 用 事务。
    完。
    遍历统计数据 的 删除文件成功 为 遍历统计数据 的 删除文件成功 加 1。
    返还 0。
完。

ext4_重命名 接受 sb、旧父节点、旧名称、新父节点、新名称：
    遍历统计数据 的 重命名次数 为 遍历统计数据 的 重命名次数 加 1。
    若 sb 等于 空 或者 旧父节点 等于 空 或者 新父节点 等于 空：
        返还 错误码_无效参数。
    完。
    若 旧名称 等于 空 或者 新名称 等于 空：
        返还 错误码_无效参数。
    完。
    旧项 为 做 ext4_查找目录项 用 sb、旧父节点、旧名称。
    若 旧项 等于 空：
        返还 错误码_不存在。
    完。
    移动节点号 为 旧项 的 文件节点号。
    事务 为 做 journal_开始事务。
    新项 为 做 ext4_查找目录项 用 sb、新父节点、新名称。
    若 新项 不等于 空：
        目标节点 为 做 ext4_读文件节点 用 sb、新项 的 文件节点号。
        若 非 做 是错误 用 目标节点：
            目标节点 的 链接数 为 目标节点 的 链接数 减 1。
            若 目标节点 的 链接数 等于 0：
                目标节点 的 删除时间 为 做 当前时间_秒。
            完。
            做 ext4_写文件节点 用 sb、目标节点。
        完。
        做 ext4_删除目录项 用 sb、新父节点、新名称。
    完。
    移动节点 为 做 ext4_读文件节点 用 sb、移动节点号。
    若 做 是错误 用 移动节点：
        若 事务 不等于 空：
            做 journal_回滚事务 用 事务。
        完。
        返还 移动节点。
    完。
    类型 为 做 ext4_模式到目录项类型 用 移动节点 的 模式。
    添加结果 为 做 ext4_添加目录项 用 sb、新父节点、新名称、移动节点号、类型。
    若 添加结果 不等于 0：
        若 事务 不等于 空：
            做 journal_回滚事务 用 事务。
        完。
        返还 添加结果。
    完。
    做 ext4_删除目录项 用 sb、旧父节点、旧名称。
    若 移动节点 的 模式 位与 0xF000 等于 0x4000 并且 旧父节点 的 编号 不等于 新父节点 的 编号：
        做 ext4_更新父目录项 用 sb、移动节点、新父节点 的 编号。
        旧父节点 的 链接数 为 旧父节点 的 链接数 减 1。
        新父节点 的 链接数 为 新父节点 的 链接数 加 1。
    完。
    旧父节点 的 修改时间 为 做 当前时间_秒。
    新父节点 的 修改时间 为 做 当前时间_秒。
    移动节点 的 变更时间 为 做 当前时间_秒。
    做 ext4_写文件节点 用 sb、旧父节点。
    做 ext4_写文件节点 用 sb、新父节点。
    做 ext4_写文件节点 用 sb、移动节点。
    若 事务 不等于 空：
        做 journal_提交事务 用 事务。
    完。
    返还 0。
完。

ext4_更新父目录项 接受 sb、目录节点、新父编号：
    信息 为 sb 的 私有信息。
    ext4信息 为 目录节点 的 私有信息。
    若 ext4信息 等于 空：
        返还。
    完。
    物理块 为 做 ext4_extent_查找 用 sb、ext4信息、0。
    若 物理块 等于 0 减 1：
        返还。
    完。
    块数据 为 做 块设备_读 用 sb 的 设备、物理块 乘 信息 的 块大小、信息 的 块大小。
    若 块数据 等于 空：
        返还。
    完。
    第一个长度 为 做 缓冲读16 用 块数据、EXT4_目录项_记录长偏移。
    做 缓冲写32 用 块数据、第一个长度 加 EXT4_目录项_节点号偏移、新父编号。
    做 块设备_写 用 sb 的 设备、物理块 乘 信息 的 块大小、块数据、信息 的 块大小。
完。

ext4_硬链接 接受 sb、源节点、目标父节点、新名称：
    遍历统计数据 的 硬链接次数 为 遍历统计数据 的 硬链接次数 加 1。
    若 源节点 等于 空 或者 目标父节点 等于 空 或者 新名称 等于 空：
        返还 错误码_无效参数。
    完。
    若 源节点 的 模式 位与 0xF000 等于 0x4000：
        返还 错误码_不允许。
    完。
    若 源节点 的 链接数 大于或等于 EXT4_最大链接数：
        返还 错误码_链接过多。
    完。
    事务 为 做 journal_开始事务。
    类型 为 做 ext4_模式到目录项类型 用 源节点 的 模式。
    结果 为 做 ext4_添加目录项 用 sb、目标父节点、新名称、源节点 的 编号、类型。
    若 结果 不等于 0：
        若 事务 不等于 空：
            做 journal_回滚事务 用 事务。
        完。
        返还 结果。
    完。
    源节点 的 链接数 为 源节点 的 链接数 加 1。
    源节点 的 变更时间 为 做 当前时间_秒。
    做 ext4_写文件节点 用 sb、源节点。
    目标父节点 的 修改时间 为 做 当前时间_秒。
    做 ext4_写文件节点 用 sb、目标父节点。
    若 事务 不等于 空：
        做 journal_提交事务 用 事务。
    完。
    返还 0。
完。

ext4_符号链接 接受 sb、父节点、名称、目标路径：
    遍历统计数据 的 符号链接次数 为 遍历统计数据 的 符号链接次数 加 1。
    若 父节点 等于 空 或者 名称 等于 空 或者 目标路径 等于 空：
        返还 错误码_无效参数。
    完。
    若 做 字节长度 用 目标路径 大于 EXT4_最大符号链接长度：
        返还 错误码_名称过长。
    完。
    事务 为 做 journal_开始事务。
    新节点号 为 做 ext4_分配文件节点号 用 sb。
    若 新节点号 小于或等于 0：
        若 事务 不等于 空：
            做 journal_回滚事务 用 事务。
        完。
        返还 错误码_无空间。
    完。
    可变 新节点 为 做 ext4_读文件节点 用 sb、新节点号。
    新节点 的 模式 为 0xA000 位或 0x01FF。
    新节点 的 链接数 为 1。
    新节点 的 大小 为 做 字节长度 用 目标路径。
    新节点 的 创建时间 为 做 当前时间_秒。
    新节点 的 修改时间 为 做 当前时间_秒。
    新节点 的 变更时间 为 做 当前时间_秒。
    若 做 字节长度 用 目标路径 小于或等于 60：
        新节点 的 符号链接数据 为 目标路径。
    否则：
        信息 为 sb 的 私有信息。
        请求 为 新建 Ext4分配请求。
        请求 的 文件节点号 为 新节点号。
        请求 的 请求块数 为 1。
        分配结果 为 做 ext4_分配块 用 sb、请求。
        若 做 是错误 用 分配结果：
            若 事务 不等于 空：
                做 journal_回滚事务 用 事务。
            完。
            返还 分配结果。
        完。
        数据块 为 做 创建缓冲 用 信息 的 块大小。
        可变 i 为 0。
        只要 i 小于 做 字节长度 用 目标路径：
            做 缓冲写 用 数据块、i、做 字节取 用 目标路径、i。
            i 为 i 加 1。
        完。
        做 块设备_写 用 sb 的 设备、分配结果 的 起始块 乘 信息 的 块大小、数据块、信息 的 块大小。
    完。
    做 ext4_写文件节点 用 sb、新节点。
    做 ext4_添加目录项 用 sb、父节点、名称、新节点号、EXT4_目录项类型_符号链接。
    父节点 的 修改时间 为 做 当前时间_秒。
    做 ext4_写文件节点 用 sb、父节点。
    若 事务 不等于 空：
        做 journal_提交事务 用 事务。
    完。
    返还 0。
完。

ext4_读目录_包装 接受 文件、缓冲、大小、偏移：
    返还 做 ext4_读目录_完整 用 文件、缓冲、大小。
完。

ext4_遍历_打印统计 接受 零参：
    做 打印 用 「[ext4遍历] ═══ 统计信息 ═══」。
    做 打印 用 做 字符串拼接 用 「  读目录次数:       」、做 转字符串 用 遍历统计数据 的 读目录次数。
    做 打印 用 做 字符串拼接 用 「  返回条目总数:     」、做 转字符串 用 遍历统计数据 的 读目录返回条目总数。
    做 打印 用 做 字符串拼接 用 「  空目录次数:       」、做 转字符串 用 遍历统计数据 的 读目录空目录次数。
    做 打印 用 做 字符串拼接 用 「  mmap次数:         」、做 转字符串 用 遍历统计数据 的 mmap次数。
    做 打印 用 做 字符串拼接 用 「  fault次数:        」、做 转字符串 用 遍历统计数据 的 fault次数。
    做 打印 用 做 字符串拼接 用 「  fault缓存命中:    」、做 转字符串 用 遍历统计数据 的 fault缓存命中。
    做 打印 用 做 字符串拼接 用 「  fault磁盘读取:    」、做 转字符串 用 遍历统计数据 的 fault磁盘读取。
    做 打印 用 做 字符串拼接 用 「  fault空洞次数:    」、做 转字符串 用 遍历统计数据 的 fault空洞次数。
    做 打印 用 做 字符串拼接 用 「  删除文件次数:     」、做 转字符串 用 遍历统计数据 的 删除文件次数。
    做 打印 用 做 字符串拼接 用 「  删除文件成功:     」、做 转字符串 用 遍历统计数据 的 删除文件成功。
    做 打印 用 做 字符串拼接 用 「  删除文件失败:     」、做 转字符串 用 遍历统计数据 的 删除文件失败。
    做 打印 用 做 字符串拼接 用 「  重命名次数:       」、做 转字符串 用 遍历统计数据 的 重命名次数。
    做 打印 用 做 字符串拼接 用 「  硬链接次数:       」、做 转字符串 用 遍历统计数据 的 硬链接次数。
    做 打印 用 做 字符串拼接 用 「  符号链接次数:     」、做 转字符串 用 遍历统计数据 的 符号链接次数。
    做 打印 用 做 字符串拼接 用 「  目录项解析次数:   」、做 转字符串 用 遍历统计数据 的 目录项解析次数。
    做 打印 用 做 字符串拼接 用 「  目录项解析错误:   」、做 转字符串 用 遍历统计数据 的 目录项解析错误。
    返还 0。
完。

ext4_遍历_获取统计 接受 零参：
    返还 遍历统计数据。
完。

ext4_获取目录操作表 接受 零参：
    返还 一本字典：读目录 对应 ext4_读目录_完整、查找 对应 ext4_查找目录项、创建 对应 ext4_添加目录项、删除 对应 ext4_删除文件、重命名 对应 ext4_重命名、链接 对应 ext4_硬链接、符号链接 对应 ext4_符号链接、内存映射 对应 ext4_内存映射。
完。

可变 AI_扩展列目录_运行指标 为 0。

可变 AI_扩展列目录_异常指标 为 0。

AI_扩展列目录_自诊断 接受 零参：
    AI_扩展列目录_运行指标 为 遍历统计数据。
    返还 AI_扩展列目录_运行指标。
完。

AI_扩展列目录_优化建议 接受 零参：
    指标 为 做 AI_扩展列目录_自诊断。
    若 AI_扩展列目录_运行指标 大于 10000：
        返还 「扩展列目录遍负载较高」。
    完。
    返还 「扩展列目录遍状态正常」。
完。

本章 收束。
