《扩展位图查找》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·同步原语》。

引用《.·ext4超级块读取》。

引用《.·ext4块组描述符》。

引用《.·ext4组分配器》。

引用《..·中枢·字符串替换》。

引用《..·稳态·AHCI驱动全局》。

位图查找结果 是 一种 事物，含 起始、长度。

字节_全零 为 0x00。

字节_全一 为 0xFF。

位图统计 是 一种 事物，含 查找连续次数、查找连续成功、查找连续失败、查找连续_精确匹配、查找连续_部分匹配、查找连续_扫描字节数、设置范围次数、设置范围位数、清除范围次数、清除范围位数、测试位次数、释放块次数、释放块成功、释放块失败、释放块_重复释放、释放块总数、统计空闲次数、碎片分析次数、最大空闲段查找次数。

可变 位图统计数据 为 新建 位图统计。

ext4_位图查找连续空闲 接受 位图、总位数、需要数：
    位图统计数据 的 查找连续次数 为 位图统计数据 的 查找连续次数 加 1。
    若 位图 等于 空 或者 总位数 等于 0 或者 需要数 等于 0：
        位图统计数据 的 查找连续失败 为 位图统计数据 的 查找连续失败 加 1。
        返还 新建 位图查找结果：起始 为 负 1、长度 为 0。
    完。
    可变 最佳起始 为 负 1。
    可变 最佳长度 为 0。
    可变 当前起始 为 负 1。
    可变 当前长度 为 0。
    总字节数 为 (总位数 加 7) 除以 8。
    可变 字节索引 为 0。
    只要 字节索引 小于 总字节数：
        位图统计数据 的 查找连续_扫描字节数 为 位图统计数据 的 查找连续_扫描字节数 加 1。
        字节值 为 做 缓冲读8 用 位图、字节索引。
        若 字节值 等于 字节_全一：
            若 当前长度 大于 最佳长度：
                最佳起始 为 当前起始。
                最佳长度 为 当前长度。
            完。
            当前起始 为 负 1。
            当前长度 为 0。
            字节索引 为 字节索引 加 1。
            继续。
        完。
        若 字节值 等于 字节_全零：
            位起始 为 字节索引 乘 8。
            若 当前起始 小于 0：
                当前起始 为 位起始。
                当前长度 为 0。
            完。
            可加位数 为 8。
            若 位起始 加 可加位数 大于 总位数：
                可加位数 为 总位数 减 位起始。
            完。
            当前长度 为 当前长度 加 可加位数。
            若 当前长度 大于或等于 需要数：
                位图统计数据 的 查找连续成功 为 位图统计数据 的 查找连续成功 加 1。
                位图统计数据 的 查找连续_精确匹配 为 位图统计数据 的 查找连续_精确匹配 加 1。
                返还 新建 位图查找结果：起始 为 当前起始、长度 为 需要数。
            完。
            字节索引 为 字节索引 加 1。
            继续。
        完。
        可变 位偏移 为 0。
        只要 位偏移 小于 8：
            位号 为 字节索引 乘 8 加 位偏移。
            若 位号 大于或等于 总位数：
                止。
            完。
            若 字节值 位与 1 左移 位偏移 等于 0：
                若 当前起始 小于 0：
                    当前起始 为 位号。
                    当前长度 为 1。
                否则：
                    当前长度 为 当前长度 加 1。
                完。
                若 当前长度 大于或等于 需要数：
                    位图统计数据 的 查找连续成功 为 位图统计数据 的 查找连续成功 加 1。
                    位图统计数据 的 查找连续_精确匹配 为 位图统计数据 的 查找连续_精确匹配 加 1。
                    返还 新建 位图查找结果：起始 为 当前起始、长度 为 需要数。
                完。
            否则：
                若 当前长度 大于 最佳长度：
                    最佳起始 为 当前起始。
                    最佳长度 为 当前长度。
                完。
                当前起始 为 负 1。
                当前长度 为 0。
            完。
            位偏移 为 位偏移 加 1。
        完。
        字节索引 为 字节索引 加 1。
    完。
    若 当前长度 大于 最佳长度：
        最佳起始 为 当前起始。
        最佳长度 为 当前长度。
    完。
    若 最佳长度 大于 0：
        若 最佳长度 大于 需要数：
            最佳长度 为 需要数。
        完。
        若 最佳长度 大于或等于 需要数：
            位图统计数据 的 查找连续成功 为 位图统计数据 的 查找连续成功 加 1。
        否则：
            位图统计数据 的 查找连续_部分匹配 为 位图统计数据 的 查找连续_部分匹配 加 1。
        完。
        返还 新建 位图查找结果：起始 为 最佳起始、长度 为 最佳长度。
    完。
    位图统计数据 的 查找连续失败 为 位图统计数据 的 查找连续失败 加 1。
    返还 新建 位图查找结果：起始 为 负 1、长度 为 0。
完。

ext4_位图设置范围 接受 位图、起始、数量：
    位图统计数据 的 设置范围次数 为 位图统计数据 的 设置范围次数 加 1。
    位图统计数据 的 设置范围位数 为 位图统计数据 的 设置范围位数 加 数量。
    若 位图 等于 空 或者 数量 等于 0：
        返还。
    完。
    可变 i 为 0。
    起始位偏移 为 起始 取余于 8。
    若 起始位偏移 等于 0 并且 数量 大于或等于 8：
        完整字节数 为 数量 除以 8。
        起始字节 为 起始 除以 8。
        可变 j 为 0。
        只要 j 小于 完整字节数：
            做 缓冲写 用 位图、起始字节 加 j、0xFF。
            j 为 j 加 1。
        完。
        i 为 完整字节数 乘 8。
    完。
    只要 i 小于 数量：
        位号 为 起始 加 i。
        字节索引 为 位号 除以 8。
        位偏移 为 位号 取余于 8。
        旧值 为 做 缓冲读8 用 位图、字节索引。
        做 缓冲写 用 位图、字节索引、旧值 位或 1 左移 位偏移。
        i 为 i 加 1。
    完。
完。

ext4_位图清除范围 接受 位图、起始、数量：
    位图统计数据 的 清除范围次数 为 位图统计数据 的 清除范围次数 加 1。
    位图统计数据 的 清除范围位数 为 位图统计数据 的 清除范围位数 加 数量。
    若 位图 等于 空 或者 数量 等于 0：
        返还。
    完。
    可变 i 为 0。
    起始位偏移 为 起始 取余于 8。
    若 起始位偏移 等于 0 并且 数量 大于或等于 8：
        完整字节数 为 数量 除以 8。
        起始字节 为 起始 除以 8。
        可变 j 为 0。
        只要 j 小于 完整字节数：
            做 缓冲写 用 位图、起始字节 加 j、0x00。
            j 为 j 加 1。
        完。
        i 为 完整字节数 乘 8。
    完。
    只要 i 小于 数量：
        位号 为 起始 加 i。
        字节索引 为 位号 除以 8。
        位偏移 为 位号 取余于 8。
        旧值 为 做 缓冲读8 用 位图、字节索引。
        做 缓冲写 用 位图、字节索引、旧值 位与 按位取反 (1 左移 位偏移)。
        i 为 i 加 1。
    完。
完。

ext4_位图测试位 接受 位图、位号：
    位图统计数据 的 测试位次数 为 位图统计数据 的 测试位次数 加 1。
    若 位图 等于 空：
        返还 假。
    完。
    字节索引 为 位号 除以 8。
    位偏移 为 位号 取余于 8。
    字节值 为 做 缓冲读8 用 位图、字节索引。
    返还 字节值 位与 1 左移 位偏移 不等于 0。
完。

ext4_位图设置位 接受 位图、位号：
    若 位图 等于 空：
        返还。
    完。
    字节索引 为 位号 除以 8。
    位偏移 为 位号 取余于 8。
    旧值 为 做 缓冲读8 用 位图、字节索引。
    做 缓冲写 用 位图、字节索引、旧值 位或 1 左移 位偏移。
完。

ext4_位图清除位 接受 位图、位号：
    若 位图 等于 空：
        返还。
    完。
    字节索引 为 位号 除以 8。
    位偏移 为 位号 取余于 8。
    旧值 为 做 缓冲读8 用 位图、字节索引。
    做 缓冲写 用 位图、字节索引、旧值 位与 按位取反 (1 左移 位偏移)。
完。

ext4_块所在块组 接受 信息、块号：
    若 信息 的 每组块数 等于 0：
        返还 0。
    完。
    返还 (块号 减 信息 的 磁盘超级块 的 首数据块) 除以 信息 的 每组块数。
完。

ext4_释放块 接受 sb、块号、数量：
    位图统计数据 的 释放块次数 为 位图统计数据 的 释放块次数 加 1。
    若 sb 等于 空 或者 sb 的 私有信息 等于 空：
        位图统计数据 的 释放块失败 为 位图统计数据 的 释放块失败 加 1。
        返还 错误码_无效参数。
    完。
    信息 为 sb 的 私有信息。
    若 块号 小于 信息 的 磁盘超级块 的 首数据块 或者 数量 等于 0：
        做 打印 用 做 字符串拼接 用 「[ext4] 错误: 无效释放参数 块号=」、做 转字符串 用 块号。
        位图统计数据 的 释放块失败 为 位图统计数据 的 释放块失败 加 1。
        返还 错误码_无效参数。
    完。
    若 块号 加 数量 大于 信息 的 块总数：
        做 打印 用 「[ext4] 错误: 释放范围超出文件系统」。
        位图统计数据 的 释放块失败 为 位图统计数据 的 释放块失败 加 1。
        返还 错误码_无效参数。
    完。
    组号 为 做 ext4_块所在块组 用 信息、块号。
    组内偏移 为 (块号 减 信息 的 磁盘超级块 的 首数据块) 取余于 信息 的 每组块数。
    若 组号 大于或等于 信息 的 块组数：
        位图统计数据 的 释放块失败 为 位图统计数据 的 释放块失败 加 1。
        返还 错误码_无效参数。
    完。
    若 组内偏移 加 数量 大于 信息 的 每组块数：
        做 打印 用 「[ext4] 警告: 释放跨块组边界,截断到当前块组」。
        数量 为 信息 的 每组块数 减 组内偏移。
    完。
    位图块号 为 做 ext4_获取块位图块 用 信息、组号。
    若 位图块号 等于 0：
        位图统计数据 的 释放块失败 为 位图统计数据 的 释放块失败 加 1。
        返还 错误码_IO。
    完。
    可变 位图 为 做 块设备_读 用 sb 的 设备、位图块号 乘 信息 的 块大小、信息 的 块大小。
    若 位图 等于 空：
        位图统计数据 的 释放块失败 为 位图统计数据 的 释放块失败 加 1。
        返还 错误码_IO。
    完。
    可变 重复计数 为 0。
    可变 i 为 0。
    只要 i 小于 数量：
        若 非 做 ext4_位图测试位 用 位图、组内偏移 加 i：
            做 打印 用 做 字符串拼接 用 「[ext4] 警告: 释放未分配块: 」、做 转字符串 用 块号 加 i。
            重复计数 为 重复计数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 重复计数 大于 0：
        位图统计数据 的 释放块_重复释放 为 位图统计数据 的 释放块_重复释放 加 重复计数。
    完。
    做 ext4_位图清除范围 用 位图、组内偏移、数量。
    做 块设备_写 用 sb 的 设备、位图块号 乘 信息 的 块大小、位图、信息 的 块大小。
    当前空闲 为 做 ext4_获取空闲块数 用 信息、组号。
    信息 的 组描述符 的 第 组号 项 的 空闲块数_低 为 当前空闲 加 数量 位与 0xFFFF。
    信息 的 空闲块数 为 信息 的 空闲块数 加 数量。
    位图统计数据 的 释放块成功 为 位图统计数据 的 释放块成功 加 1。
    位图统计数据 的 释放块总数 为 位图统计数据 的 释放块总数 加 数量。
    返还 0。
完。

ext4_统计位图空闲数 接受 位图、总位数：
    位图统计数据 的 统计空闲次数 为 位图统计数据 的 统计空闲次数 加 1。
    若 位图 等于 空 或者 总位数 等于 0：
        返还 0。
    完。
    可变 空闲数 为 0。
    总字节数 为 (总位数 加 7) 除以 8。
    可变 字节索引 为 0。
    只要 字节索引 小于 总字节数：
        字节值 为 做 缓冲读8 用 位图、字节索引。
        若 字节值 等于 0x00：
            位数 为 8。
            若 字节索引 乘 8 加 8 大于 总位数：
                位数 为 总位数 减 字节索引 乘 8。
            完。
            空闲数 为 空闲数 加 位数。
        否则若 字节值 不等于 0xFF：
            可变 位 为 0。
            只要 位 小于 8 并且 字节索引 乘 8 加 位 小于 总位数：
                若 字节值 位与 1 左移 位 等于 0：
                    空闲数 为 空闲数 加 1。
                完。
                位 为 位 加 1。
            完。
        完。
        字节索引 为 字节索引 加 1。
    完。
    返还 空闲数。
完。

ext4_分析位图碎片 接受 位图、总位数：
    位图统计数据 的 碎片分析次数 为 位图统计数据 的 碎片分析次数 加 1。
    若 位图 等于 空 或者 总位数 等于 0：
        返还 0。
    完。
    可变 碎片段数 为 0。
    可变 上一个是空闲 为 假。
    可变 位号 为 0。
    只要 位号 小于 总位数：
        当前是空闲 为 非 做 ext4_位图测试位 用 位图、位号。
        若 当前是空闲 并且 非 上一个是空闲：
            碎片段数 为 碎片段数 加 1。
        完。
        上一个是空闲 为 当前是空闲。
        位号 为 位号 加 1。
    完。
    返还 碎片段数。
完。

ext4_查找最大空闲段 接受 位图、总位数：
    位图统计数据 的 最大空闲段查找次数 为 位图统计数据 的 最大空闲段查找次数 加 1。
    若 位图 等于 空 或者 总位数 等于 0：
        返还 0。
    完。
    可变 最大长度 为 0。
    可变 当前长度 为 0。
    可变 位号 为 0。
    只要 位号 小于 总位数：
        若 非 做 ext4_位图测试位 用 位图、位号：
            当前长度 为 当前长度 加 1。
            若 当前长度 大于 最大长度：
                最大长度 为 当前长度。
            完。
        否则：
            当前长度 为 0。
        完。
        位号 为 位号 加 1。
    完。
    返还 最大长度。
完。

ext4_位图查找首个空闲 接受 位图、总位数、起始位：
    若 位图 等于 空：
        返还 负 1。
    完。
    可变 位号 为 起始位。
    只要 位号 小于 总位数：
        若 非 做 ext4_位图测试位 用 位图、位号：
            返还 位号。
        完。
        位号 为 位号 加 1。
    完。
    位号 为 0。
    只要 位号 小于 起始位：
        若 非 做 ext4_位图测试位 用 位图、位号：
            返还 位号。
        完。
        位号 为 位号 加 1。
    完。
    返还 负 1。
完。

ext4_位图查找首个占用 接受 位图、总位数、起始位：
    若 位图 等于 空：
        返还 负 1。
    完。
    可变 位号 为 起始位。
    只要 位号 小于 总位数：
        若 做 ext4_位图测试位 用 位图、位号：
            返还 位号。
        完。
        位号 为 位号 加 1。
    完。
    返还 负 1。
完。

ext4_位图_打印统计 接受 零参：
    做 打印 用 「[ext4位图] ═══ 统计信息 ═══」。
    做 打印 用 做 字符串拼接 用 「  查找连续次数:     」、做 转字符串 用 位图统计数据 的 查找连续次数。
    做 打印 用 做 字符串拼接 用 「  查找连续成功:     」、做 转字符串 用 位图统计数据 的 查找连续成功。
    做 打印 用 做 字符串拼接 用 「  查找连续失败:     」、做 转字符串 用 位图统计数据 的 查找连续失败。
    做 打印 用 做 字符串拼接 用 「  精确匹配:         」、做 转字符串 用 位图统计数据 的 查找连续_精确匹配。
    做 打印 用 做 字符串拼接 用 「  部分匹配:         」、做 转字符串 用 位图统计数据 的 查找连续_部分匹配。
    做 打印 用 做 字符串拼接 用 「  扫描字节数:       」、做 转字符串 用 位图统计数据 的 查找连续_扫描字节数。
    做 打印 用 做 字符串拼接 用 「  设置范围次数:     」、做 转字符串 用 位图统计数据 的 设置范围次数。
    做 打印 用 做 字符串拼接 用 「  设置范围位数:     」、做 转字符串 用 位图统计数据 的 设置范围位数。
    做 打印 用 做 字符串拼接 用 「  清除范围次数:     」、做 转字符串 用 位图统计数据 的 清除范围次数。
    做 打印 用 做 字符串拼接 用 「  测试位次数:       」、做 转字符串 用 位图统计数据 的 测试位次数。
    做 打印 用 做 字符串拼接 用 「  释放块次数:       」、做 转字符串 用 位图统计数据 的 释放块次数。
    做 打印 用 做 字符串拼接 用 「  释放块成功:       」、做 转字符串 用 位图统计数据 的 释放块成功。
    做 打印 用 做 字符串拼接 用 「  释放块失败:       」、做 转字符串 用 位图统计数据 的 释放块失败。
    做 打印 用 做 字符串拼接 用 「  重复释放:         」、做 转字符串 用 位图统计数据 的 释放块_重复释放。
    做 打印 用 做 字符串拼接 用 「  释放块总数:       」、做 转字符串 用 位图统计数据 的 释放块总数。
    做 打印 用 做 字符串拼接 用 「  统计空闲次数:     」、做 转字符串 用 位图统计数据 的 统计空闲次数。
    做 打印 用 做 字符串拼接 用 「  碎片分析次数:     」、做 转字符串 用 位图统计数据 的 碎片分析次数。
    返还 0。
完。

ext4_位图_获取统计 接受 零参：
    返还 位图统计数据。
完。

本章 收束。
