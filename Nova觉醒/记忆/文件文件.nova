《文件文件》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

文件档 是 一种 事物，含 名称、最近动作、最近参数、最近来源、最近结果、最近统计、最近错误、最近健康、是否初始化、系统类型数、挂载数、文件数、打开数、读取数、写入数、追加数、删除数、锁数、解锁数、成功数、失败数、最近路径、最近挂载、最近系统、系统类型表、挂载表、文件表、锁表。

文件请求 是 一种 事物，含 动作、目标、参数、选项。

文件回证 是 一种 事物，含 已收、已成、来源、错因、数据、统计。

文件摘要 是 一种 事物，含 名称、系统类型数、挂载数、文件数、打开数、读取数、写入数、追加数、删除数、锁数、解锁数、成功数、失败数、健康。

文件系统类型项 是 一种 事物，含 名称、块长、日志、只读、来源。

挂载项 是 一种 事物，含 路径、系统、设备、模式、来源。

文件项 是 一种 事物，含 路径、内容、大小、偏移、打开数、系统、模式、最近写入。

锁项 是 一种 事物，含 路径、模式、拥有者、次数。

可变 文件账本 为 一组：（空）。

可变 文件构建数 为 0。

可变 文件总动作 为 0。

可变 文件总成功 为 0。

可变 文件总失败 为 0。

文件文件_建档 接受 名称：
    档 为 新建 文件档。
    档 的 名称 为 名称。
    档 的 最近动作 为 「」。
    档 的 最近参数 为 一组：（空）。
    档 的 最近来源 为 「」。
    档 的 最近结果 为 0。
    档 的 最近统计 为 一组：（空）。
    档 的 最近错误 为 「」。
    档 的 最近健康 为 「未初始化」。
    档 的 是否初始化 为 假。
    档 的 系统类型数 为 0。
    档 的 挂载数 为 0。
    档 的 文件数 为 0。
    档 的 打开数 为 0。
    档 的 读取数 为 0。
    档 的 写入数 为 0。
    档 的 追加数 为 0。
    档 的 删除数 为 0。
    档 的 锁数 为 0。
    档 的 解锁数 为 0。
    档 的 成功数 为 0。
    档 的 失败数 为 0。
    档 的 最近路径 为 「」。
    档 的 最近挂载 为 「」。
    档 的 最近系统 为 「」。
    档 的 系统类型表 为 一组：（空）。
    档 的 挂载表 为 一组：（空）。
    档 的 文件表 为 一组：（空）。
    档 的 锁表 为 一组：（空）。
    返还 档。
完。

文件文件_登记 接受 档：
    做 追加 用 文件账本、档。
    文件构建数 为 文件构建数 加 1。
    返还 档。
完。

文件文件_完整统计 接受 零参：
    返还 一组：文件构建数、文件总动作、文件总成功、文件总失败。
完。

文件文件_建回证 接受 已收、已成、来源、错因、数据、统计：
    回证 为 新建 文件回证。
    回证 的 已收 为 已收。
    回证 的 已成 为 已成。
    回证 的 来源 为 来源。
    回证 的 错因 为 错因。
    回证 的 数据 为 数据。
    回证 的 统计 为 统计。
    返还 回证。
完。

文件文件_记录 接受 档、动作、参数、来源：
    档 的 最近动作 为 动作。
    档 的 最近参数 为 参数。
    档 的 最近来源 为 来源。
    文件总动作 为 文件总动作 加 1。
完。

文件文件_刷新健康 接受 档：
    若 非 档 的 是否初始化：
        档 的 最近健康 为 「未初始化」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 档 的 成功数：
        档 的 最近健康 为 「降级」。
        返还 档 的 最近健康。
    完。
    若 档 的 锁数 大于 档 的 解锁数 加 16：
        档 的 最近健康 为 「锁压」。
        返还 档 的 最近健康。
    完。
    若 档 的 失败数 大于 0：
        档 的 最近健康 为 「监视」。
        返还 档 的 最近健康。
    完。
    档 的 最近健康 为 「运行」。
    返还 档 的 最近健康。
完。

文件文件_检查档 接受 档：
    若 档 等于 空：
        返还 假。
    完。
    若 档 的 名称 等于 「」：
        返还 假。
    完。
    返还 真。
完。

文件文件_成功 接受 档、来源、数据：
    档 的 最近结果 为 数据。
    档 的 成功数 为 档 的 成功数 加 1。
    文件总成功 为 文件总成功 加 1。
    档 的 最近统计 为 做 文件文件_完整统计。
    做 文件文件_刷新健康 用 档。
    返还 做 文件文件_建回证 用 真、真、来源、「」、数据、档 的 最近统计。
完。

文件文件_失败 接受 档、来源、错因：
    若 档 不等于 空：
        档 的 最近错误 为 错因。
        档 的 失败数 为 档 的 失败数 加 1。
        档 的 最近统计 为 做 文件文件_完整统计。
        做 文件文件_刷新健康 用 档。
    完。
    文件总失败 为 文件总失败 加 1。
    返还 做 文件文件_建回证 用 真、假、来源、错因、空、做 文件文件_完整统计。
完。

文件文件_找系统类型 接受 档、名称：
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 系统类型表：
        项 为 档 的 系统类型表 的 第 索引 项。
        若 项 的 名称 等于 名称：
            返还 项。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

文件文件_找挂载 接受 档、路径：
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 挂载表：
        项 为 档 的 挂载表 的 第 索引 项。
        若 项 的 路径 等于 路径：
            返还 项。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

文件文件_规范路径 接受 路径：
    若 路径 等于 「」：
        返还 「/」。
    完。
    若 路径 的 第 0 项 不等于 「/」：
        返还 「/」 加 路径。
    完。
    返还 路径。
完。

文件文件_找文件 接受 档、路径：
    标准路径 为 做 文件文件_规范路径 用 路径。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 文件表：
        项 为 档 的 文件表 的 第 索引 项。
        若 项 的 路径 等于 标准路径：
            返还 项。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

文件文件_找锁 接受 档、路径、拥有者：
    标准路径 为 做 文件文件_规范路径 用 路径。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 锁表：
        项 为 档 的 锁表 的 第 索引 项。
        若 项 的 路径 等于 标准路径 并且 项 的 拥有者 等于 拥有者：
            返还 项。
        完。
        索引 为 索引 加 1。
    完。
    返还 空。
完。

文件文件_注册系统类型 接受 档、名称、块长、日志、只读、来源：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「注册系统类型」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「注册系统类型」、一组：名称、块长、日志、只读、来源、「文件文件」。
    若 做 文件文件_找系统类型 用 档、名称 不等于 空：
        返还 做 文件文件_失败 用 档、「注册系统类型」、「系统类型已存在」。
    完。
    项 为 新建 文件系统类型项。
    项 的 名称 为 名称。
    项 的 块长 为 块长。
    项 的 日志 为 日志。
    项 的 只读 为 只读。
    项 的 来源 为 来源。
    做 追加 用 档 的 系统类型表、项。
    档 的 系统类型数 为 档 的 系统类型数 加 1。
    档 的 最近系统 为 名称。
    返还 做 文件文件_成功 用 档、「注册系统类型」、项。
完。

文件文件_挂载 接受 档、路径、系统、设备、模式、来源：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「挂载」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「挂载」、一组：路径、系统、设备、模式、来源、「文件文件」。
    若 做 文件文件_找系统类型 用 档、系统 等于 空：
        返还 做 文件文件_失败 用 档、「挂载」、「文件系统类型不存在」。
    完。
    标准路径 为 做 文件文件_规范路径 用 路径。
    若 做 文件文件_找挂载 用 档、标准路径 不等于 空：
        返还 做 文件文件_失败 用 档、「挂载」、「挂载点已存在」。
    完。
    项 为 新建 挂载项。
    项 的 路径 为 标准路径。
    项 的 系统 为 系统。
    项 的 设备 为 设备。
    项 的 模式 为 模式。
    项 的 来源 为 来源。
    做 追加 用 档 的 挂载表、项。
    档 的 挂载数 为 档 的 挂载数 加 1。
    档 的 最近挂载 为 标准路径。
    档 的 最近系统 为 系统。
    返还 做 文件文件_成功 用 档、「挂载」、项。
完。

文件文件_卸载 接受 档、路径：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「卸载」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「卸载」、一组：路径、「文件文件」。
    标准路径 为 做 文件文件_规范路径 用 路径。
    新表 为 一组：（空）。
    索引 为 0。
    命中 为 假。
    只要 索引 小于 做 计数 用 档 的 挂载表：
        项 为 档 的 挂载表 的 第 索引 项。
        若 项 的 路径 等于 标准路径：
            命中 为 真。
        否则：
            做 追加 用 新表、项。
        完。
        索引 为 索引 加 1。
    完。
    档 的 挂载表 为 新表。
    档 的 挂载数 为 做 计数 用 档 的 挂载表。
    若 非 命中：
        返还 做 文件文件_失败 用 档、「卸载」、「挂载点不存在」。
    完。
    返还 做 文件文件_成功 用 档、「卸载」、标准路径。
完。

文件文件_建文件项 接受 路径、系统、模式：
    项 为 新建 文件项。
    项 的 路径 为 做 文件文件_规范路径 用 路径。
    项 的 内容 为 「」。
    项 的 大小 为 0。
    项 的 偏移 为 0。
    项 的 打开数 为 0。
    项 的 系统 为 系统。
    项 的 模式 为 模式。
    项 的 最近写入 为 「」。
    返还 项。
完。

文件文件_打开 接受 档、路径、系统、模式：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「打开」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「打开」、一组：路径、系统、模式、「文件文件」。
    项 为 做 文件文件_找文件 用 档、路径。
    若 项 等于 空：
        项 为 做 文件文件_建文件项 用 路径、系统、模式。
        做 追加 用 档 的 文件表、项。
        档 的 文件数 为 档 的 文件数 加 1。
    完。
    项 的 打开数 为 项 的 打开数 加 1。
    档 的 打开数 为 档 的 打开数 加 1。
    档 的 最近路径 为 项 的 路径。
    返还 做 文件文件_成功 用 档、「打开」、项。
完。

文件文件_关闭 接受 档、路径：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「关闭」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「关闭」、一组：路径、「文件文件」。
    项 为 做 文件文件_找文件 用 档、路径。
    若 项 等于 空：
        返还 做 文件文件_失败 用 档、「关闭」、「文件不存在」。
    完。
    若 项 的 打开数 大于 0：
        项 的 打开数 为 项 的 打开数 减 1。
    完。
    档 的 最近路径 为 项 的 路径。
    返还 做 文件文件_成功 用 档、「关闭」、项 的 打开数。
完。

文件文件_读取 接受 档、路径、长度：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「读取」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「读取」、一组：路径、长度、「文件文件」。
    项 为 做 文件文件_找文件 用 档、路径。
    若 项 等于 空：
        返还 做 文件文件_失败 用 档、「读取」、「文件不存在」。
    完。
    档 的 读取数 为 档 的 读取数 加 1。
    档 的 最近路径 为 项 的 路径。
    若 长度 小于或等于 0 或者 长度 大于或等于 项 的 大小：
        返还 做 文件文件_成功 用 档、「读取」、项 的 内容。
    完。
    返还 做 文件文件_成功 用 档、「读取」、做 子串 用 项 的 内容、0、长度。
完。

文件文件_写入 接受 档、路径、内容、模式：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「写入」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「写入」、一组：路径、内容、模式、「文件文件」。
    项 为 做 文件文件_找文件 用 档、路径。
    若 项 等于 空：
        项 为 做 文件文件_建文件项 用 路径、「临时」、模式。
        做 追加 用 档 的 文件表、项。
        档 的 文件数 为 档 的 文件数 加 1。
    完。
    项 的 内容 为 内容。
    项 的 大小 为 做 长度 用 内容。
    项 的 模式 为 模式。
    项 的 最近写入 为 内容。
    档 的 写入数 为 档 的 写入数 加 1。
    档 的 最近路径 为 项 的 路径。
    返还 做 文件文件_成功 用 档、「写入」、项。
完。

文件文件_追加 接受 档、路径、内容：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「追加」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「追加」、一组：路径、内容、「文件文件」。
    项 为 做 文件文件_找文件 用 档、路径。
    若 项 等于 空：
        项 为 做 文件文件_建文件项 用 路径、「临时」、「rw」。
        做 追加 用 档 的 文件表、项。
        档 的 文件数 为 档 的 文件数 加 1。
    完。
    项 的 内容 为 项 的 内容 加 内容。
    项 的 大小 为 做 长度 用 项 的 内容。
    项 的 最近写入 为 内容。
    档 的 追加数 为 档 的 追加数 加 1。
    档 的 最近路径 为 项 的 路径。
    返还 做 文件文件_成功 用 档、「追加」、项。
完。

文件文件_加锁 接受 档、路径、模式、拥有者：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「加锁」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「加锁」、一组：路径、模式、拥有者、「文件文件」。
    锁 为 做 文件文件_找锁 用 档、路径、拥有者。
    若 锁 等于 空：
        锁 为 新建 锁项。
        锁 的 路径 为 做 文件文件_规范路径 用 路径。
        锁 的 模式 为 模式。
        锁 的 拥有者 为 拥有者。
        锁 的 次数 为 0。
        做 追加 用 档 的 锁表、锁。
    完。
    锁 的 次数 为 锁 的 次数 加 1。
    档 的 锁数 为 档 的 锁数 加 1。
    档 的 最近路径 为 锁 的 路径。
    返还 做 文件文件_成功 用 档、「加锁」、锁。
完。

文件文件_解锁 接受 档、路径、拥有者：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「解锁」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「解锁」、一组：路径、拥有者、「文件文件」。
    锁 为 做 文件文件_找锁 用 档、路径、拥有者。
    若 锁 等于 空：
        返还 做 文件文件_失败 用 档、「解锁」、「锁不存在」。
    完。
    若 锁 的 次数 大于 0：
        锁 的 次数 为 锁 的 次数 减 1。
    完。
    档 的 解锁数 为 档 的 解锁数 加 1。
    档 的 最近路径 为 锁 的 路径。
    返还 做 文件文件_成功 用 档、「解锁」、锁 的 次数。
完。

文件文件_重命名 接受 档、旧路径、新路径：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「重命名」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「重命名」、一组：旧路径、新路径、「文件文件」。
    项 为 做 文件文件_找文件 用 档、旧路径。
    若 项 等于 空：
        返还 做 文件文件_失败 用 档、「重命名」、「文件不存在」。
    完。
    项 的 路径 为 做 文件文件_规范路径 用 新路径。
    档 的 最近路径 为 项 的 路径。
    返还 做 文件文件_成功 用 档、「重命名」、项。
完。

文件文件_删除 接受 档、路径：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「删除」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「删除」、一组：路径、「文件文件」。
    标准路径 为 做 文件文件_规范路径 用 路径。
    新表 为 一组：（空）。
    索引 为 0。
    命中 为 假。
    只要 索引 小于 做 计数 用 档 的 文件表：
        项 为 档 的 文件表 的 第 索引 项。
        若 项 的 路径 等于 标准路径：
            命中 为 真。
        否则：
            做 追加 用 新表、项。
        完。
        索引 为 索引 加 1。
    完。
    档 的 文件表 为 新表。
    档 的 文件数 为 做 计数 用 档 的 文件表。
    若 非 命中：
        返还 做 文件文件_失败 用 档、「删除」、「文件不存在」。
    完。
    档 的 删除数 为 档 的 删除数 加 1。
    档 的 最近路径 为 标准路径。
    返还 做 文件文件_成功 用 档、「删除」、标准路径。
完。

文件文件_挂载摘要 接受 档：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「挂载摘要」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「挂载摘要」、一组：（空）、「文件文件」。
    结果 为 一组：（空）。
    索引 为 0。
    只要 索引 小于 做 计数 用 档 的 挂载表：
        项 为 档 的 挂载表 的 第 索引 项。
        做 追加 用 结果、一组：项 的 路径、项 的 系统、项 的 设备、项 的 模式。
        索引 为 索引 加 1。
    完。
    返还 做 文件文件_成功 用 档、「挂载摘要」、结果。
完。

文件文件_摘要 接受 档：
    摘要 为 新建 文件摘要。
    摘要 的 名称 为 档 的 名称。
    摘要 的 系统类型数 为 档 的 系统类型数。
    摘要 的 挂载数 为 档 的 挂载数。
    摘要 的 文件数 为 档 的 文件数。
    摘要 的 打开数 为 档 的 打开数。
    摘要 的 读取数 为 档 的 读取数。
    摘要 的 写入数 为 档 的 写入数。
    摘要 的 追加数 为 档 的 追加数。
    摘要 的 删除数 为 档 的 删除数。
    摘要 的 锁数 为 档 的 锁数。
    摘要 的 解锁数 为 档 的 解锁数。
    摘要 的 成功数 为 档 的 成功数。
    摘要 的 失败数 为 档 的 失败数。
    摘要 的 健康 为 做 文件文件_刷新健康 用 档。
    返还 摘要。
完。

文件文件_初始化 接受 档：
    若 非 做 文件文件_检查档 用 档：
        返还 做 文件文件_失败 用 档、「初始化」、「文件档不可用」。
    完。
    做 文件文件_记录 用 档、「初始化」、一组：（空）、「文件文件」。
    档 的 系统类型表 为 一组：（空）。
    档 的 挂载表 为 一组：（空）。
    档 的 文件表 为 一组：（空）。
    档 的 锁表 为 一组：（空）。
    档 的 系统类型数 为 0。
    档 的 挂载数 为 0。
    档 的 文件数 为 0。
    档 的 是否初始化 为 真。
    做 文件文件_注册系统类型 用 档、「觉存」、4096、真、假、「初始化」。
    做 文件文件_注册系统类型 用 档、「白璃」、4096、假、假、「初始化」。
    做 文件文件_挂载 用 档、「/」、「觉存」、「根盘」、「rw」、「初始化」。
    做 文件文件_挂载 用 档、「/资源」、「白璃」、「资源盘」、「rw」、「初始化」。
    返还 做 文件文件_成功 用 档、「初始化」、「已初始化」。
完。

文件文件_自检 接受 档：
    若 非 做 文件文件_检查档 用 档：
        返还 假。
    完。
    若 做 文件文件_初始化 用 档 的 已成 等于 假：
        返还 假。
    完。
    若 做 文件文件_打开 用 档、「/资源/界面.css」、「白璃」、「rw」 的 已成 等于 假：
        返还 假。
    完。
    若 做 文件文件_写入 用 档、「/资源/界面.css」、「.白璃窗{backdrop-filter:blur(20px);}」、「rw」 的 已成 等于 假：
        返还 假。
    完。
    若 做 文件文件_追加 用 档、「/资源/界面.css」、「.白璃卡{border-radius:22px;}」 的 已成 等于 假：
        返还 假。
    完。
    若 做 文件文件_读取 用 档、「/资源/界面.css」、12 的 已成 等于 假：
        返还 假。
    完。
    若 做 文件文件_加锁 用 档、「/资源/界面.css」、「写锁」、「界面线程」 的 已成 等于 假：
        返还 假。
    完。
    若 做 文件文件_解锁 用 档、「/资源/界面.css」、「界面线程」 的 已成 等于 假：
        返还 假。
    完。
    若 做 文件文件_重命名 用 档、「/资源/界面.css」、「/资源/白璃.css」 的 已成 等于 假：
        返还 假。
    完。
    若 做 文件文件_关闭 用 档、「/资源/白璃.css」 的 已成 等于 假：
        返还 假。
    完。
    返还 真。
完。

文件文件_处理 接受 档、请求：
    若 请求 等于 空：
        返还 做 文件文件_失败 用 档、「处理」、「请求为空」。
    完。
    若 请求 的 动作 等于 「初始化」：
        返还 做 文件文件_初始化 用 档。
    完。
    若 请求 的 动作 等于 「注册系统类型」：
        返还 做 文件文件_注册系统类型 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项、请求 的 参数 的 第 4 项。
    完。
    若 请求 的 动作 等于 「挂载」：
        返还 做 文件文件_挂载 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项、请求 的 参数 的 第 3 项、请求 的 参数 的 第 4 项。
    完。
    若 请求 的 动作 等于 「卸载」：
        返还 做 文件文件_卸载 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「打开」：
        返还 做 文件文件_打开 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「关闭」：
        返还 做 文件文件_关闭 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「读取」：
        返还 做 文件文件_读取 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「写入」：
        返还 做 文件文件_写入 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「追加」：
        返还 做 文件文件_追加 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「加锁」：
        返还 做 文件文件_加锁 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项、请求 的 参数 的 第 2 项。
    完。
    若 请求 的 动作 等于 「解锁」：
        返还 做 文件文件_解锁 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「重命名」：
        返还 做 文件文件_重命名 用 档、请求 的 参数 的 第 0 项、请求 的 参数 的 第 1 项。
    完。
    若 请求 的 动作 等于 「删除」：
        返还 做 文件文件_删除 用 档、请求 的 参数 的 第 0 项。
    完。
    若 请求 的 动作 等于 「挂载摘要」：
        返还 做 文件文件_挂载摘要 用 档。
    完。
    若 请求 的 动作 等于 「摘要」：
        返还 做 文件文件_成功 用 档、「摘要」、做 文件文件_摘要 用 档。
    完。
    返还 做 文件文件_失败 用 档、「处理」、「未知动作」。
完。

可变 AI_文件文件_热文件表 为 一组：（空）。

AI_文件文件_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_文件文件_热文件表：
        若 AI_文件文件_热文件表 的 第 i 项 的 路径 等于 路径：
            AI_文件文件_热文件表 的 第 i 项 的 频率 为 AI_文件文件_热文件表 的 第 i 项 的 频率 加 1。
            返还 AI_文件文件_热文件表 的 第 i 项 的 频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_文件文件_热文件表 小于 128：
        做 追加 用 AI_文件文件_热文件表、一本字典：路径 对应 路径、频率 对应 1。
    完。
    返还 1。
完。

AI_文件文件_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
        返还 0。
    完。
    返还 32768。
完。

本章 收束。
