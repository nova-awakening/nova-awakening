《挂载统计》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·哈希整数》。

引用《..·中枢·内容可见性》。

最大挂载 为 64。

最大文件系统类型 为 16。

挂载_只读 为 1。

挂载_无执行 为 2。

挂载_无设备 为 4。

挂载_同步 为 8。

挂载项 是 一种 事物，含 挂载点哈希、设备哈希、文件系统类型、标志、根节点、父挂载、子挂载数、引用计数、挂载时刻、读操作数、写操作数。

文件系统类型 是 一种 事物，含 名称哈希、挂载回调、卸载回调、读超级块、同步回调。

可变 挂载表 为 一组：（空）。

可变 挂载数 为 0。

可变 类型表 为 一组：（空）。

可变 类型数 为 0。

可变 总挂载 为 0。

可变 总卸载 为 0。

挂载_初始化 接受 零参：
    挂载表 为 一组：（空）。
    挂载数 为 0。
    类型表 为 一组：（空）。
    类型数 为 0。
完。

文件系统_注册类型 接受 名称、挂载回调、卸载回调、读超级块：
    若 类型数 大于或等于 最大文件系统类型：
        返还 负 1。
    完。
    类型 为 新建 文件系统类型。
    类型 的 名称哈希 为 做 哈希_字符串 用 名称。
    类型 的 挂载回调 为 挂载回调。
    类型 的 卸载回调 为 卸载回调。
    类型 的 读超级块 为 读超级块。
    类型 的 同步回调 为 0。
    做 追加 用 类型表、类型。
    索引 为 类型数。
    类型数 为 类型数 加 1。
    返还 索引。
完。

挂载_执行 接受 挂载点、设备、文件系统名、标志：
    若 挂载数 大于或等于 最大挂载：
        返还 错误_无资源。
    完。
    类型索引 为 做 挂载_查找类型 用 文件系统名。
    若 类型索引 小于 0：
        返还 错误_不支持。
    完。
    项 为 新建 挂载项。
    项 的 挂载点哈希 为 做 哈希_字符串 用 挂载点。
    项 的 设备哈希 为 做 哈希_字符串 用 设备。
    项 的 文件系统类型 为 类型索引。
    项 的 标志 为 标志。
    项 的 根节点 为 0。
    项 的 父挂载 为 负 1。
    项 的 子挂载数 为 0。
    项 的 引用计数 为 1。
    项 的 挂载时刻 为 做 平台_获取高精度时间。
    项 的 读操作数 为 0。
    项 的 写操作数 为 0。
    若 类型表 的 第 类型索引 项 的 挂载回调 不等于 0：
        项 的 根节点 为 做 类型表 的 第 类型索引 项 的 挂载回调 用 设备。
    完。
    做 追加 用 挂载表、项。
    挂载数 为 挂载数 加 1。
    总挂载 为 总挂载 加 1。
    返还 挂载数 减 1。
完。

挂载_卸载 接受 挂载索引：
    若 挂载索引 大于或等于 挂载数：
        返还 错误_无效参数。
    完。
    项 为 挂载表 的 第 挂载索引 项。
    若 项 的 引用计数 大于 1：
        返还 错误_忙。
    完。
    若 项 的 子挂载数 大于 0：
        返还 错误_忙。
    完。
    类型索引 为 项 的 文件系统类型。
    若 类型索引 小于 类型数 并且 类型表 的 第 类型索引 项 的 卸载回调 不等于 0：
        做 类型表 的 第 类型索引 项 的 卸载回调 用 项 的 根节点。
    完。
    项 的 引用计数 为 0。
    总卸载 为 总卸载 加 1。
    返还 0。
完。

挂载_查找类型 接受 名称：
    目标 为 做 哈希_字符串 用 名称。
    可变 序号 为 0。
    只要 序号 小于 类型数：
        若 类型表 的 第 序号 项 的 名称哈希 等于 目标：
            返还 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 负 1。
完。

挂载_查找点 接受 挂载点：
    目标 为 做 哈希_字符串 用 挂载点。
    可变 序号 为 0。
    只要 序号 小于 挂载数：
        若 挂载表 的 第 序号 项 的 挂载点哈希 等于 目标 并且 挂载表 的 第 序号 项 的 引用计数 大于 0：
            返还 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 负 1。
完。

挂载_增引用 接受 索引：
    若 索引 小于 挂载数：
        挂载表 的 第 索引 项 的 引用计数 为 挂载表 的 第 索引 项 的 引用计数 加 1。
    完。
完。

挂载_减引用 接受 索引：
    若 索引 小于 挂载数 并且 挂载表 的 第 索引 项 的 引用计数 大于 0：
        挂载表 的 第 索引 项 的 引用计数 为 挂载表 的 第 索引 项 的 引用计数 减 1。
    完。
完。

挂载_记录读 接受 索引：
    若 索引 小于 挂载数：
        挂载表 的 第 索引 项 的 读操作数 为 挂载表 的 第 索引 项 的 读操作数 加 1。
    完。
完。

挂载_记录写 接受 索引：
    若 索引 小于 挂载数：
        挂载表 的 第 索引 项 的 写操作数 为 挂载表 的 第 索引 项 的 写操作数 加 1。
    完。
完。

挂载_同步全部 接受 零参：
    可变 序号 为 0。
    只要 序号 小于 挂载数：
        若 挂载表 的 第 序号 项 的 引用计数 大于 0：
            类型 为 挂载表 的 第 序号 项 的 文件系统类型。
            若 类型 小于 类型数 并且 类型表 的 第 类型 项 的 同步回调 不等于 0：
                做 类型表 的 第 类型 项 的 同步回调 用 挂载表 的 第 序号 项 的 根节点。
            完。
        完。
        序号 为 序号 加 1。
    完。
完。

挂载_活跃数 接受 零参：
    可变 数量 为 0。
    可变 序号 为 0。
    只要 序号 小于 挂载数：
        若 挂载表 的 第 序号 项 的 引用计数 大于 0：
            数量 为 数量 加 1。
        完。
        序号 为 序号 加 1。
    完。
    返还 数量。
完。

挂载_完整统计 接受 零参：
    返还 一组：挂载数、做 挂载_活跃数、类型数、总挂载、总卸载。
完。

可变 AI_挂载完整统_热文件表 为 一组：（空）。

AI_挂载完整统_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_挂载完整统_热文件表：
        若 AI_挂载完整统_热文件表 的 第 i 项 的 路径 等于 路径：
            AI_挂载完整统_热文件表 的 第 i 项 的 频率 为 AI_挂载完整统_热文件表 的 第 i 项 的 频率 加 1。
            返还 AI_挂载完整统_热文件表 的 第 i 项 的 频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_挂载完整统_热文件表 小于 128：
        做 追加 用 AI_挂载完整统_热文件表、一本字典：路径 对应 路径、频率 对应 1。
    完。
    返还 1。
完。

AI_挂载完整统_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
        返还 0。
    完。
    返还 32768。
完。

本章 收束。
