《proc挂载》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《.·文件系统类》。

引用《.·超级块》。

引用《..·中枢·哈希函数》。

引用《.·proc条目查找》。

引用《.·proc内置条目注册》。

PROC_魔数 为 0x9FA0。

PROC_最大条目数 为 256。

PROC_ROOT_Nova文件节点 为 1。

PROC_类型_目录 为 1。

PROC_类型_文件 为 2。

PROC_类型_符号链接 为 3。

Proc条目 是 一种 事物，含 名称、Nova文件节点号、类型、模式、读函数、写函数、数据、父目录、子条目、兄弟、大小、uid、gid。

可变 proc_根 为 空。

可变 proc_下一个Nova文件节点 为 2。

可变 proc_条目数 为 0。

proNova公平调度_初始化 接受 零参：
    proc_根 为 做 proNova公平调度_创建条目 用 「/」、PROC_类型_目录、0x41ED、空。
    proc_根 的 Nova文件节点号 为 PROC_ROOT_Nova文件节点。
    做 注册文件系统 用 「proc」、proNova公平调度_挂载。
    做 proNova公平调度_注册_内置条目。
    做 内核信息 用 「proNova公平调度: 初始化完成 (%d条目)\n」、proc_条目数。
完。

proNova公平调度_挂载 接受 零参：
    可变 sb 为 做 超级块_创建 用 PROC_魔数、4096、0、「proc」。
    sb 的 根Nova文件节点 为 proc_根 的 Nova文件节点号。
    返还 sb。
完。

proNova公平调度_创建条目 接受 名称、类型、模式、父：
    若 proc_条目数 大于或等于 PROC_最大条目数：
        返还 空。
    完。
    可变 条目 为 做 分配内存 用 做 大小 用 Proc条目。
    若 条目 等于 空：
        返还 空。
    完。
    条目 的 名称 为 名称。
    条目 的 Nova文件节点号 为 proc_下一个Nova文件节点。
    proc_下一个Nova文件节点 为 proc_下一个Nova文件节点 加 1。
    条目 的 类型 为 类型。
    条目 的 模式 为 模式。
    条目 的 读函数 为 空。
    条目 的 写函数 为 空。
    条目 的 数据 为 空。
    条目 的 父目录 为 父。
    条目 的 子条目 为 空。
    条目 的 兄弟 为 空。
    条目 的 大小 为 0。
    条目 的 uid 为 0。
    条目 的 gid 为 0。
    若 父 不等于 空：
        条目 的 兄弟 为 父 的 子条目。
        父 的 子条目 为 条目。
    完。
    proc_条目数 为 proc_条目数 加 1。
    返还 条目。
完。

proNova公平调度_注册 接受 名称、读函数：
    条目 为 做 proNova公平调度_创建条目 用 名称、PROC_类型_文件、0x8124、proc_根。
    若 条目 不等于 空：
        条目 的 读函数 为 读函数。
    完。
    返还 条目。
完。

proNova公平调度_注册_可写 接受 名称、读函数、写函数：
    条目 为 做 proNova公平调度_创建条目 用 名称、PROC_类型_文件、0x81A4、proc_根。
    若 条目 不等于 空：
        条目 的 读函数 为 读函数。
        条目 的 写函数 为 写函数。
    完。
    返还 条目。
完。

proNova公平调度_创建目录 接受 名称、父：
    若 父 等于 空：
        父 为 proc_根。
    完。
    返还 做 proNova公平调度_创建条目 用 名称、PROC_类型_目录、0x41ED、父。
完。

proNova公平调度_删除条目 接受 名称：
    条目 为 做 proNova公平调度_查找 用 名称。
    若 条目 等于 空：
        返还。
    完。
    若 条目 的 父目录 不等于 空：
        可变 前一个 为 空。
        可变 当前 为 条目 的 父目录 的 子条目。
        只要 当前 不等于 空：
            若 当前 等于 条目：
                若 前一个 等于 空：
                    条目 的 父目录 的 子条目 为 当前 的 兄弟。
                否则：
                    前一个 的 兄弟 为 当前 的 兄弟。
                完。
                跳出。
            完。
            前一个 为 当前。
            当前 为 当前 的 兄弟。
        完。
    完。
    做 释放内存 用 条目。
    proc_条目数 为 proc_条目数 减 1。
完。

本章 收束。
