《ext4_htree》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《.·ext4卸载超级块》。

引用《..·中枢·字符串替换》。

引用《.·ext4目录删除》。

引用《..·稳态·AHCI驱动全局》。

ext4_htree_查找 接受 sb、目录Nova文件节点、名称：
    哈希值 为 做 ext4_名称哈希 用 名称。
    返还 做 ext4_线性查找目录项 用 sb、目录Nova文件节点、名称。
完。

ext4_名称哈希 接受 名称：
    可变 哈希 为 0。
    长度 为 做 字符串_长度 用 名称。
    可变 i 为 0。
    只要 i 小于 长度：
        哈希 为 哈希 乘 31 加 名称 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 哈希 位与 0x7FFFFFFF。
完。

ext4_线性查找目录项 接受 sb、目录Nova文件节点、名称：
    信息 为 sb 的 私有信息。
    块大小 为 信息 的 块大小。
    ext4信息 为 目录Nova文件节点 的 私有信息。
    可变 偏移 为 0。
    只要 偏移 小于 目录Nova文件节点 的 大小：
        逻辑块 为 偏移 除以 块大小。
        物理块 为 做 ext4_extent_查找 用 sb、ext4信息、逻辑块。
        若 物理块 不等于 0 减 1：
            缓冲 为 做 块设备_读 用 sb 的 设备、物理块 乘 块大小、块大小。
            若 缓冲 不等于 空：
                可变 块内偏移 为 0。
                只要 块内偏移 小于 块大小：
                    项 为 做 ext4_解析目录项 用 缓冲、块内偏移。
                    若 项 的 记录长度 等于 0：
                        跳出。
                    完。
                    若 项 的 Nova文件节点号 不等于 0 并且 做 ext4_目录项名称匹配 用 项、名称：
                        做 缓冲_释放 用 缓冲。
                        返还 项。
                    完。
                    块内偏移 为 块内偏移 加 项 的 记录长度。
                完。
                做 缓冲_释放 用 缓冲。
            完。
        完。
        偏移 为 偏移 加 块大小。
    完。
    返还 空。
完。

ext4_解析目录项 接受 缓冲、偏移：
    可变 项 为 新建 Ext4目录项。
    项 的 Nova文件节点号 为 做 缓冲_读u32 用 缓冲、偏移。
    项 的 记录长度 为 做 缓冲_读u16 用 缓冲、偏移 加 4。
    项 的 名称长度 为 做 缓冲_读u8 用 缓冲、偏移 加 6。
    项 的 文件类型 为 做 缓冲_读u8 用 缓冲、偏移 加 7。
    可变 i 为 0。
    只要 i 小于 项 的 名称长度 并且 i 小于 EXT4_名称最大长度：
        项 的 名称 的 第 i 项 为 做 缓冲_读u8 用 缓冲、偏移 加 8 加 i。
        i 为 i 加 1。
    完。
    返还 项。
完。

ext4_写入目录项 接受 缓冲、偏移、Nova文件节点号、名称、文件类型、记录长度：
    名称长度 为 做 字符串_长度 用 名称。
    做 缓冲_写u32 用 缓冲、偏移、Nova文件节点号。
    做 缓冲_写u16 用 缓冲、偏移 加 4、记录长度。
    做 缓冲_写u8 用 缓冲、偏移 加 6、名称长度。
    做 缓冲_写u8 用 缓冲、偏移 加 7、文件类型。
    可变 i 为 0。
    只要 i 小于 名称长度：
        做 缓冲_写u8 用 缓冲、偏移 加 8 加 i、名称 的 第 i 项。
        i 为 i 加 1。
    完。
完。

ext4_写回目录项 接受 缓冲、偏移、项：
    做 缓冲_写u32 用 缓冲、偏移、项 的 Nova文件节点号。
    做 缓冲_写u16 用 缓冲、偏移 加 4、项 的 记录长度。
    做 缓冲_写u8 用 缓冲、偏移 加 6、项 的 名称长度。
    做 缓冲_写u8 用 缓冲、偏移 加 7、项 的 文件类型。
完。

ext4_目录项名称匹配 接受 项、名称：
    名称长度 为 做 字符串_长度 用 名称。
    若 项 的 名称长度 不等于 名称长度：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 名称长度：
        若 项 的 名称 的 第 i 项 不等于 名称 的 第 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

ext4_提取目录项名称 接受 项：
    返还 做 字节数组_转字符串 用 项 的 名称、项 的 名称长度。
完。

ext4_目录项实际长度 接受 名称长度：
    长度 为 EXT4_目录项_头大小 加 名称长度。
    返还 长度 加 3 位与 按位取反 3。
完。

本章 收束。
