《ext4_extent查找》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《.·ext4_extent插入》。

引用《.·ext4_extent删除》。

引用《.·ext4卸载超级块》。

引用《..·稳态·AHCI驱动全局》。

ext4_extent_查找 接受 sb、Nova文件节点信息、逻辑块：
    信息 为 sb 的 私有信息。
    可变 头 为 Nova文件节点信息 的 extent树。
    可变 深度 为 头 的 深度。
    可变 路径 为 做 数组 用 Ext4Extent路径。
    深度 加 1。
    若 深度 等于 0：
        extent 为 做 ext4_extent_二分查找_叶 用 头、逻辑块。
        若 extent 不等于 空：
            起始 为 extent 的 逻辑块。
            长度 为 做 ext4_extent_获取长度 用 extent。
            若 逻辑块 大于或等于 起始 并且 逻辑块 小于 起始 加 长度：
                偏移 为 逻辑块 减 起始。
                返还 做 ext4_extent_获取物理块 用 extent 加 偏移。
            完。
        完。
        返还 0 减 1。
    完。
    可变 当前头 为 头。
    可变 当前深度 为 深度。
    只要 当前深度 大于 0：
        索引 为 做 ext4_extent_二分查找_索引 用 当前头、逻辑块。
        若 索引 等于 空：
            返还 0 减 1。
        完。
        子块号 为 做 ext4_索引_获取叶块 用 索引。
        缓冲 为 做 块设备_读 用 sb 的 设备、子块号 乘 信息 的 块大小、信息 的 块大小。
        若 缓冲 等于 空：
            返还 0 减 1。
        完。
        当前头 为 做 ext4_extent_解析头 用 缓冲。
        若 当前头 的 魔数 不等于 EXT4_EXTENT_魔数：
            做 缓冲_释放 用 缓冲。
            返还 0 减 1。
        完。
        当前深度 为 当前深度 减 1。
    完。
    extent 为 做 ext4_extent_二分查找_叶 用 当前头、逻辑块。
    若 extent 不等于 空：
        起始 为 extent 的 逻辑块。
        长度 为 做 ext4_extent_获取长度 用 extent。
        若 逻辑块 大于或等于 起始 并且 逻辑块 小于 起始 加 长度：
            返还 做 ext4_extent_获取物理块 用 extent 加 (逻辑块 减 起始)。
        完。
    完。
    返还 0 减 1。
完。

ext4_extent_二分查找_叶 接受 头、逻辑块：
    数量 为 头 的 有效项数。
    若 数量 等于 0：
        返还 空。
    完。
    可变 低 为 0。
    可变 高 为 数量 减 1。
    可变 结果 为 空。
    只要 低 小于或等于 高：
        中 为 (低 加 高) 除以 2。
        项 为 做 ext4_extent_获取第N项 用 头、中。
        若 逻辑块 小于 项 的 逻辑块：
            若 中 等于 0：
                跳出。
            完。
            高 为 中 减 1。
        否则：
            结果 为 项。
            低 为 中 加 1。
        完。
    完。
    返还 结果。
完。

ext4_extent_二分查找_索引 接受 头、逻辑块：
    数量 为 头 的 有效项数。
    若 数量 等于 0：
        返还 空。
    完。
    可变 低 为 0。
    可变 高 为 数量 减 1。
    可变 结果 为 空。
    只要 低 小于或等于 高：
        中 为 (低 加 高) 除以 2。
        索引 为 做 ext4_extent_获取第N索引 用 头、中。
        若 逻辑块 小于 索引 的 逻辑块：
            若 中 等于 0：
                跳出。
            完。
            高 为 中 减 1。
        否则：
            结果 为 索引。
            低 为 中 加 1。
        完。
    完。
    返还 结果。
完。

本章 收束。
