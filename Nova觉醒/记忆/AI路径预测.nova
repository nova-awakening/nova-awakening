《AI路径预测》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·执行·控制台输出》。

引用《..·中枢·链表后继》。

引用《..·感知·内核直连层》。

引用《..·中枢·整数转字符串》。

路径转移 是 一种 事物，含 从路径、到路径、转移次数、频率EMA。

可变 路径访问历史 为 一组：（空）。

路径历史最大 为 64。

可变 路径转移表 为 一组：（空）。

可变 上次路径 为 「」。

可变 路径频率表 为 一组：（空）。

AI路径_初始化 接受 零参：
    做 内核信息 用 「AI路径预测: 2-gram转移矩阵+Nova路径缓存预热+工作集追踪」。
完。

AI路径_记录 接受 路径：
    做 追加 用 路径访问历史、路径。
    若 做 计数 用 路径访问历史 大于 路径历史最大：
        做 链表_删除 用 路径访问历史、0。
    完。
    可变 找到 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 路径频率表：
        若 路径频率表 的 第 i 项 的 第 0 项 等于 路径：
            做 设置 用 路径频率表 的 第 i 项、1、(路径频率表 的 第 i 项 的 第 1 项 乘 922 加 1024 乘 102) 除以 1024。
            找到 为 真。
            中断。
        完。
        i 为 i 加 1。
    完。
    若 非 找到：
        做 追加 用 路径频率表、一组：路径、102。
    完。
    若 上次路径 不等于 「」：
        转移 为 做 AI路径_查找转移 用 上次路径、路径。
        若 转移 不等于 空：
            转移 的 转移次数 为 转移 的 转移次数 加 1。
            转移 的 频率EMA 为 (转移 的 频率EMA 乘 922 加 1024 乘 102) 除以 1024。
        否则：
            新转移 为 新建 路径转移。
            新转移 的 从路径 为 上次路径。
            新转移 的 到路径 为 路径。
            新转移 的 转移次数 为 1。
            新转移 的 频率EMA 为 102。
            做 追加 用 路径转移表、新转移。
        完。
    完。
    上次路径 为 路径。
完。

AI路径_预测下一个 接受 零参：
    若 上次路径 等于 「」：
        返还 空。
    完。
    可变 最高频率 为 0。
    可变 最佳路径 为 空。
    可变 i 为 0。
    只要 i 小于 做 计数 用 路径转移表：
        若 路径转移表 的 第 i 项 的 从路径 等于 上次路径 并且 路径转移表 的 第 i 项 的 频率EMA 大于 最高频率：
            最高频率 为 路径转移表 的 第 i 项 的 频率EMA。
            最佳路径 为 路径转移表 的 第 i 项 的 到路径。
        完。
        i 为 i 加 1。
    完。
    返还 最佳路径。
完。

AI路径_预测概率 接受 当前路径、目标路径：
    转移 为 做 AI路径_查找转移 用 当前路径、目标路径。
    若 转移 等于 空：
        返还 0。
    完。
    返还 转移 的 频率EMA。
完。

AI路径_工作集 接受 前N个：
    可变 结果 为 一组：（空）。
    可变 n 为 0。
    只要 n 小于 前N个 并且 n 小于 做 计数 用 路径频率表：
        可变 最高频 为 0。
        可变 最高索引 为 负 1。
        可变 i 为 0。
        只要 i 小于 做 计数 用 路径频率表：
            若 路径频率表 的 第 i 项 的 第 1 项 大于 最高频：
                可变 已有 为 假。
                可变 j 为 0。
                只要 j 小于 做 计数 用 结果：
                    若 结果 的 第 j 项 等于 路径频率表 的 第 i 项 的 第 0 项：
                        已有 为 真。
                    完。
                    j 为 j 加 1。
                完。
                若 非 已有：
                    最高频 为 路径频率表 的 第 i 项 的 第 1 项。
                    最高索引 为 i。
                完。
            完。
            i 为 i 加 1。
        完。
        若 最高索引 大于或等于 0：
            做 追加 用 结果、路径频率表 的 第 最高索引 项 的 第 0 项。
        完。
        n 为 n 加 1。
    完。
    返还 结果。
完。

AI路径_查找转移 接受 从、到：
    可变 i 为 0。
    只要 i 小于 做 计数 用 路径转移表：
        若 路径转移表 的 第 i 项 的 从路径 等于 从 并且 路径转移表 的 第 i 项 的 到路径 等于 到：
            返还 路径转移表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

AI路径_完整统计 接受 零参：
    返还 一组：做 计数 用 路径访问历史、做 计数 用 路径转移表、做 计数 用 路径频率表。
完。

可变 路径_预测总数 为 0。

可变 路径_预测命中 为 0。

可变 路径_缓存预热数 为 0。

AI路径_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 做 计数 用 路径转移表 大于 10000：
        做 追加 用 问题、「路径转移表过大: 」 加 做 整数转字符串 用 做 计数 用 路径转移表。
    完。
    若 路径_预测总数 大于 100 并且 路径_预测命中 乘 100 除以 路径_预测总数 小于 10：
        做 追加 用 问题、「路径预测命中率极低」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「AI路径预测健康」。
    完。
    返还 问题。
完。

AI路径_详细统计 接受 零参：
    返还 一本字典：历史长度 对应 做 计数 用 路径访问历史、转移记录 对应 做 计数 用 路径转移表、频率记录 对应 做 计数 用 路径频率表、预测总数 对应 路径_预测总数、预测命中 对应 路径_预测命中、命中率 对应 做 AI路径_预测命中率、缓存预热 对应 路径_缓存预热数、上次路径 对应 上次路径。
完。

AI路径_预测命中率 接受 零参：
    若 路径_预测总数 等于 0：
        返还 0。
    完。
    返还 路径_预测命中 乘 100 除以 路径_预测总数。
完。

AI路径_重置统计 接受 零参：
    路径_预测总数 为 0。
    路径_预测命中 为 0。
    路径_缓存预热数 为 0。
完。

AI路径_重置全部 接受 零参：
    路径访问历史 为 一组：（空）。
    路径转移表 为 一组：（空）。
    路径频率表 为 一组：（空）。
    上次路径 为 「」。
    做 AI路径_重置统计。
完。

AI路径_提取目录 接受 路径：
    若 路径 等于 0：
        返还 「」。
    完。
    长度 为 做 字节长度 用 路径。
    可变 最后斜杠 为 负 1。
    可变 i 为 0。
    只要 i 小于 长度：
        若 做 字节取 用 路径、i 等于 47：
            最后斜杠 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最后斜杠 小于或等于 0：
        返还 「/」。
    完。
    返还 做 字符串_子串 用 路径、0、最后斜杠。
完。

AI路径_目录局部性 接受 零参：
    若 做 计数 用 路径访问历史 小于 2：
        返还 0。
    完。
    可变 连续同目录 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 路径访问历史：
        目录a 为 做 AI路径_提取目录 用 路径访问历史 的 第 i 减 1 项。
        目录b 为 做 AI路径_提取目录 用 路径访问历史 的 第 i 项。
        若 做 字符串_相等 用 目录a、目录b：
            连续同目录 为 连续同目录 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 连续同目录 乘 100 除以 (做 计数 用 路径访问历史 减 1)。
完。

AI路径_最活跃目录 接受 前N个：
    可变 目录计数 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 路径频率表：
        目录 为 做 AI路径_提取目录 用 路径频率表 的 第 i 项 的 第 0 项。
        可变 找到 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 目录计数：
            若 做 字符串_相等 用 目录计数 的 第 j 项 的 第 0 项、目录：
                目录计数 的 第 j 项 的 第 1 项 为 目录计数 的 第 j 项 的 第 1 项 加 路径频率表 的 第 i 项 的 第 1 项。
                找到 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 找到：
            做 追加 用 目录计数、一组：目录、路径频率表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    可变 n 为 0。
    只要 n 小于 前N个 并且 n 小于 做 计数 用 目录计数：
        可变 最高 为 0。
        可变 最高索引 为 负 1。
        可变 k 为 0。
        只要 k 小于 做 计数 用 目录计数：
            若 目录计数 的 第 k 项 的 第 1 项 大于 最高：
                可变 已有 为 假。
                可变 m 为 0。
                只要 m 小于 做 计数 用 结果：
                    若 做 字符串_相等 用 结果 的 第 m 项、目录计数 的 第 k 项 的 第 0 项：
                        已有 为 真。
                    完。
                    m 为 m 加 1。
                完。
                若 非 已有：
                    最高 为 目录计数 的 第 k 项 的 第 1 项。
                    最高索引 为 k。
                完。
            完。
            k 为 k 加 1。
        完。
        若 最高索引 大于或等于 0：
            做 追加 用 结果、目录计数 的 第 最高索引 项 的 第 0 项。
        完。
        n 为 n 加 1。
    完。
    返还 结果。
完。

AI路径_应该预热 接受 零参：
    预测 为 做 AI路径_预测下一个。
    若 预测 等于 空：
        返还 假。
    完。
    路径_预测总数 为 路径_预测总数 加 1。
    返还 真。
完。

AI路径_执行预热 接受 零参：
    路径_缓存预热数 为 路径_缓存预热数 加 1。
完。

AI路径_记录命中 接受 零参：
    路径_预测命中 为 路径_预测命中 加 1。
完。

字符串_子串 接受 s、起始、长度：
    若 s 等于 0：
        返还 「」。
    完。
    返还 s 加 起始。
完。

本章 收束。
