《ext4文件节点属性》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·同步原语》。

引用《..·推理·通信渠道》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

EXT4_S_IFMT 为 0xF000。

EXT4_S_IFREG 为 0x8000。

EXT4_S_IFDIR 为 0x4000。

EXT4_S_IFLNK 为 0xA000。

EXT4_S_IFBLK 为 0x6000。

EXT4_S_IFCHR 为 0x2000。

EXT4_S_IFIFO 为 0x1000。

EXT4_S_IFSOCK 为 0xC000。

EXT4_S_ISUID 为 0x0800。

EXT4_S_ISGID 为 0x0400。

EXT4_S_ISVTX 为 0x0200。

EXT4_S_IRUSR 为 0x0100。

EXT4_S_IWUSR 为 0x0080。

EXT4_S_IXUSR 为 0x0040。

EXT4_S_IRGRP 为 0x0020。

EXT4_S_IWGRP 为 0x0010。

EXT4_S_IXGRP 为 0x0008。

EXT4_S_IROTH 为 0x0004。

EXT4_S_IWOTH 为 0x0002。

EXT4_S_IXOTH 为 0x0001。

EXT4_SECRM_FL 为 0x00000001。

EXT4_UNRM_FL 为 0x00000002。

EXT4_COMPR_FL 为 0x00000004。

EXT4_SYNC_FL 为 0x00000008。

EXT4_IMMUTABLE_FL 为 0x00000010。

EXT4_APPEND_FL 为 0x00000020。

EXT4_NODUMP_FL 为 0x00000040。

EXT4_NOATIME_FL 为 0x00000080。

EXT4_INDEX_FL 为 0x00001000。

EXT4_EXTENTS_FL 为 0x00080000。

EXT4_INLINE_FL 为 0x10000000。

EXT4_ENCRYPT_FL 为 0x00000800。

EXT4_EPOCH_BITS 为 2。

EXT4_EPOCH_MASK 为 0x03。

EXT4_NSEC_MASK 为 0x3FFFFFFF。

可变 ext4_Nova文件节点读次数 为 0。

可变 ext4_Nova文件节点写次数 为 0。

可变 ext4_属性读总次数 为 0。

可变 ext4_属性写总次数 为 0。

可变 ext4_权限检查次数 为 0。

可变 ext4_权限拒绝次数 为 0。

可变 ext4_校验失败次数 为 0。

可变 ext4_AI文件分析次数 为 0。

可变 ext4_时间戳读次数 为 0。

可变 ext4_扩展属性读次数 为 0。

ext4_获取Nova文件节点大小 接受 磁盘in：
    若 磁盘in 等于 空：
        返还 0。
    完。
    ext4_属性读总次数 为 ext4_属性读总次数 加 1。
    ext4_Nova文件节点读次数 为 ext4_Nova文件节点读次数 加 1。
    可变 大小 为 磁盘in 的 大小_低 位与 0xFFFFFFFF。
    若 磁盘in 的 模式 位与 EXT4_S_IFMT 等于 EXT4_S_IFREG：
        大小 为 大小 位或 (磁盘in 的 大小_高 位与 0xFFFFFFFF) 左移 32。
    完。
    返还 大小。
完。

ext4_设置Nova文件节点大小 接受 磁盘in、大小：
    若 磁盘in 等于 空：
        返还。
    完。
    ext4_属性写总次数 为 ext4_属性写总次数 加 1。
    ext4_Nova文件节点写次数 为 ext4_Nova文件节点写次数 加 1。
    磁盘in 的 大小_低 为 大小 位与 0xFFFFFFFF。
    磁盘in 的 大小_高 为 大小 右移 32 位与 0xFFFFFFFF。
完。

ext4_获取Nova文件节点大小_安全 接受 磁盘in、最大允许：
    大小 为 做 ext4_获取Nova文件节点大小 用 磁盘in。
    若 大小 大于 最大允许：
        ext4_校验失败次数 为 ext4_校验失败次数 加 1。
        返还 最大允许。
    完。
    返还 大小。
完。

ext4_获取Nova文件节点_uid 接受 磁盘in：
    若 磁盘in 等于 空：
        返还 0。
    完。
    ext4_属性读总次数 为 ext4_属性读总次数 加 1。
    返还 磁盘in 的 uid_低 位与 0xFFFF 位或 (磁盘in 的 uid_高 位与 0xFFFF) 左移 16。
完。

ext4_设置Nova文件节点_uid 接受 磁盘in、uid：
    若 磁盘in 等于 空：
        返还。
    完。
    ext4_属性写总次数 为 ext4_属性写总次数 加 1。
    磁盘in 的 uid_低 为 uid 位与 0xFFFF。
    磁盘in 的 uid_高 为 uid 右移 16 位与 0xFFFF。
完。

ext4_获取Nova文件节点_gid 接受 磁盘in：
    若 磁盘in 等于 空：
        返还 0。
    完。
    ext4_属性读总次数 为 ext4_属性读总次数 加 1。
    返还 磁盘in 的 gid_低 位与 0xFFFF 位或 (磁盘in 的 gid_高 位与 0xFFFF) 左移 16。
完。

ext4_设置Nova文件节点_gid 接受 磁盘in、gid：
    若 磁盘in 等于 空：
        返还。
    完。
    ext4_属性写总次数 为 ext4_属性写总次数 加 1。
    磁盘in 的 gid_低 为 gid 位与 0xFFFF。
    磁盘in 的 gid_高 为 gid 右移 16 位与 0xFFFF。
完。

ext4_获取Nova文件节点块数 接受 磁盘in：
    若 磁盘in 等于 空：
        返还 0。
    完。
    ext4_属性读总次数 为 ext4_属性读总次数 加 1。
    可变 块数 为 磁盘in 的 块数_低 位与 0xFFFFFFFF。
    块数 为 块数 位或 (磁盘in 的 块数_高 位与 0xFFFF) 左移 32。
    返还 块数。
完。

ext4_设置Nova文件节点块数 接受 磁盘in、块数：
    若 磁盘in 等于 空：
        返还。
    完。
    ext4_属性写总次数 为 ext4_属性写总次数 加 1。
    磁盘in 的 块数_低 为 块数 位与 0xFFFFFFFF。
    磁盘in 的 块数_高 为 块数 右移 32 位与 0xFFFF。
完。

ext4_获取链接数 接受 磁盘in：
    若 磁盘in 等于 空：
        返还 0。
    完。
    返还 磁盘in 的 链接数 位与 0xFFFF。
完。

ext4_设置链接数 接受 磁盘in、链接数：
    若 磁盘in 等于 空：
        返还。
    完。
    磁盘in 的 链接数 为 链接数 位与 0xFFFF。
完。

ext4_增加链接数 接受 磁盘in：
    若 磁盘in 等于 空：
        返还 错误码_无效参数。
    完。
    当前 为 做 ext4_获取链接数 用 磁盘in。
    若 当前 大于或等于 0xFFFF：
        返还 错误码_超出范围。
    完。
    做 ext4_设置链接数 用 磁盘in、当前 加 1。
    返还 0。
完。

ext4_减少链接数 接受 磁盘in：
    若 磁盘in 等于 空：
        返还 错误码_无效参数。
    完。
    当前 为 做 ext4_获取链接数 用 磁盘in。
    若 当前 等于 0：
        ext4_校验失败次数 为 ext4_校验失败次数 加 1。
        返还 错误码_数据损坏。
    完。
    做 ext4_设置链接数 用 磁盘in、当前 减 1。
    返还 0。
完。

ext4_模式转类型 接受 模式：
    做 匹配 用 模式 位与 EXT4_S_IFMT。
完。

ext4_类型转模式 接受 类型：
    做 匹配 用 类型。
完。

ext4_是目录 接受 节点：
    若 节点 等于 空：
        返还 假。
    完。
    返还 节点 的 模式 位与 EXT4_S_IFMT 等于 EXT4_S_IFDIR。
完。

ext4_是普通文件 接受 节点：
    若 节点 等于 空：
        返还 假。
    完。
    返还 节点 的 模式 位与 EXT4_S_IFMT 等于 EXT4_S_IFREG。
完。

ext4_是符号链接 接受 节点：
    若 节点 等于 空：
        返还 假。
    完。
    返还 节点 的 模式 位与 EXT4_S_IFMT 等于 EXT4_S_IFLNK。
完。

ext4_是块设备 接受 节点：
    若 节点 等于 空：
        返还 假。
    完。
    返还 节点 的 模式 位与 EXT4_S_IFMT 等于 EXT4_S_IFBLK。
完。

ext4_是字符设备 接受 节点：
    若 节点 等于 空：
        返还 假。
    完。
    返还 节点 的 模式 位与 EXT4_S_IFMT 等于 EXT4_S_IFCHR。
完。

ext4_是管道 接受 节点：
    若 节点 等于 空：
        返还 假。
    完。
    返还 节点 的 模式 位与 EXT4_S_IFMT 等于 EXT4_S_IFIFO。
完。

ext4_是套接字 接受 节点：
    若 节点 等于 空：
        返还 假。
    完。
    返还 节点 的 模式 位与 EXT4_S_IFMT 等于 EXT4_S_IFSOCK。
完。

ext4_是特殊文件 接受 节点：
    若 节点 等于 空：
        返还 假。
    完。
    类型 为 节点 的 模式 位与 EXT4_S_IFMT。
    返还 类型 等于 EXT4_S_IFBLK 或者 类型 等于 EXT4_S_IFCHR 或者 类型 等于 EXT4_S_IFIFO 或者 类型 等于 EXT4_S_IFSOCK。
完。

ext4_获取权限位 接受 磁盘in：
    若 磁盘in 等于 空：
        返还 0。
    完。
    返还 磁盘in 的 模式 位与 0x0FFF。
完。

ext4_设置权限位 接受 磁盘in、权限：
    若 磁盘in 等于 空：
        返还。
    完。
    磁盘in 的 模式 为 磁盘in 的 模式 位与 0xF000 位或 权限 位与 0x0FFF。
完。

ext4_检查读权限 接受 磁盘in、uid、gid：
    ext4_权限检查次数 为 ext4_权限检查次数 加 1。
    若 磁盘in 等于 空：
        返还 假。
    完。
    模式 为 磁盘in 的 模式。
    若 uid 等于 0：
        返还 真。
    完。
    若 做 ext4_获取Nova文件节点_uid 用 磁盘in 等于 uid：
        返还 模式 位与 EXT4_S_IRUSR 不等于 0。
    完。
    若 做 ext4_获取Nova文件节点_gid 用 磁盘in 等于 gid：
        返还 模式 位与 EXT4_S_IRGRP 不等于 0。
    完。
    返还 模式 位与 EXT4_S_IROTH 不等于 0。
完。

ext4_检查写权限 接受 磁盘in、uid、gid：
    ext4_权限检查次数 为 ext4_权限检查次数 加 1。
    若 磁盘in 等于 空：
        返还 假。
    完。
    若 磁盘in 的 标志 位与 EXT4_IMMUTABLE_FL：
        ext4_权限拒绝次数 为 ext4_权限拒绝次数 加 1。
        返还 假。
    完。
    模式 为 磁盘in 的 模式。
    若 uid 等于 0：
        返还 真。
    完。
    若 做 ext4_获取Nova文件节点_uid 用 磁盘in 等于 uid：
        返还 模式 位与 EXT4_S_IWUSR 不等于 0。
    完。
    若 做 ext4_获取Nova文件节点_gid 用 磁盘in 等于 gid：
        返还 模式 位与 EXT4_S_IWGRP 不等于 0。
    完。
    返还 模式 位与 EXT4_S_IWOTH 不等于 0。
完。

ext4_检查执行权限 接受 磁盘in、uid、gid：
    ext4_权限检查次数 为 ext4_权限检查次数 加 1。
    若 磁盘in 等于 空：
        返还 假。
    完。
    模式 为 磁盘in 的 模式。
    若 uid 等于 0：
        返还 真。
    完。
    若 做 ext4_获取Nova文件节点_uid 用 磁盘in 等于 uid：
        返还 模式 位与 EXT4_S_IXUSR 不等于 0。
    完。
    若 做 ext4_获取Nova文件节点_gid 用 磁盘in 等于 gid：
        返还 模式 位与 EXT4_S_IXGRP 不等于 0。
    完。
    返还 模式 位与 EXT4_S_IXOTH 不等于 0。
完。

ext4_检查全权限 接受 磁盘in、uid、gid、请求权限：
    若 请求权限 位与 1：
        若 非 做 ext4_检查读权限 用 磁盘in、uid、gid：
            ext4_权限拒绝次数 为 ext4_权限拒绝次数 加 1。
            返还 假。
        完。
    完。
    若 请求权限 位与 2：
        若 非 做 ext4_检查写权限 用 磁盘in、uid、gid：
            ext4_权限拒绝次数 为 ext4_权限拒绝次数 加 1。
            返还 假。
        完。
    完。
    若 请求权限 位与 4：
        若 非 做 ext4_检查执行权限 用 磁盘in、uid、gid：
            ext4_权限拒绝次数 为 ext4_权限拒绝次数 加 1。
            返还 假。
        完。
    完。
    返还 真。
完。

ext4_获取时间戳_秒 接受 磁盘in、字段：
    ext4_时间戳读次数 为 ext4_时间戳读次数 加 1。
    若 磁盘in 等于 空：
        返还 0。
    完。
    做 匹配 用 字段。
完。

ext4_获取时间戳_纳秒 接受 磁盘in、字段：
    ext4_时间戳读次数 为 ext4_时间戳读次数 加 1。
    若 磁盘in 等于 空：
        返还 0。
    完。
    可变 额外 为 0。
    做 匹配 用 字段。
    返还 额外 位与 EXT4_NSEC_MASK。
完。

ext4_获取时间戳_epoch 接受 磁盘in、字段：
    若 磁盘in 等于 空：
        返还 0。
    完。
    可变 额外 为 0。
    做 匹配 用 字段。
    返还 额外 右移 30 位与 EXT4_EPOCH_MASK。
完。

ext4_设置时间戳 接受 磁盘in、字段、秒、纳秒：
    若 磁盘in 等于 空：
        返还。
    完。
    做 匹配 用 字段。
    额外值 为 纳秒 位与 EXT4_NSEC_MASK。
    做 匹配 用 字段。
完。

ext4_获取标志 接受 磁盘in：
    若 磁盘in 等于 空：
        返还 0。
    完。
    返还 磁盘in 的 标志。
完。

ext4_设置标志 接受 磁盘in、标志：
    若 磁盘in 等于 空：
        返还。
    完。
    磁盘in 的 标志 为 标志。
完。

ext4_添加标志 接受 磁盘in、新标志：
    若 磁盘in 等于 空：
        返还。
    完。
    磁盘in 的 标志 为 磁盘in 的 标志 位或 新标志。
完。

ext4_移除标志 接受 磁盘in、旧标志：
    若 磁盘in 等于 空：
        返还。
    完。
    磁盘in 的 标志 为 磁盘in 的 标志 位与 按位取反 旧标志。
完。

ext4_测试标志 接受 磁盘in、测试位：
    若 磁盘in 等于 空：
        返还 假。
    完。
    返还 磁盘in 的 标志 位与 测试位 不等于 0。
完。

ext4_是加密文件 接受 磁盘in：
    返还 做 ext4_测试标志 用 磁盘in、EXT4_ENCRYPT_FL。
完。

ext4_是extent文件 接受 磁盘in：
    返还 做 ext4_测试标志 用 磁盘in、EXT4_EXTENTS_FL。
完。

ext4_是内联数据文件 接受 磁盘in：
    返还 做 ext4_测试标志 用 磁盘in、EXT4_INLINE_FL。
完。

ext4_是不可变文件 接受 磁盘in：
    返还 做 ext4_测试标志 用 磁盘in、EXT4_IMMUTABLE_FL。
完。

ext4_是只追加文件 接受 磁盘in：
    返还 做 ext4_测试标志 用 磁盘in、EXT4_APPEND_FL。
完。

ext4_校验inode 接受 磁盘in：
    若 磁盘in 等于 空：
        返还 错误码_无效参数。
    完。
    类型 为 磁盘in 的 模式 位与 EXT4_S_IFMT。
    若 类型 不等于 EXT4_S_IFREG 并且 类型 不等于 EXT4_S_IFDIR 并且 类型 不等于 EXT4_S_IFLNK 并且 类型 不等于 EXT4_S_IFBLK 并且 类型 不等于 EXT4_S_IFCHR 并且 类型 不等于 EXT4_S_IFIFO 并且 类型 不等于 EXT4_S_IFSOCK：
        ext4_校验失败次数 为 ext4_校验失败次数 加 1。
        返还 错误码_数据损坏。
    完。
    链接 为 做 ext4_获取链接数 用 磁盘in。
    若 链接 等于 0 并且 类型 不等于 EXT4_S_IFREG：
        ext4_校验失败次数 为 ext4_校验失败次数 加 1。
        返还 错误码_数据损坏。
    完。
    大小 为 做 ext4_获取Nova文件节点大小 用 磁盘in。
    块数 为 做 ext4_获取Nova文件节点块数 用 磁盘in。
    若 大小 大于 0 并且 块数 等于 0 并且 类型 等于 EXT4_S_IFREG：
        若 非 做 ext4_是内联数据文件 用 磁盘in：
            ext4_校验失败次数 为 ext4_校验失败次数 加 1。
            返还 错误码_数据损坏。
        完。
    完。
    返还 0。
完。

ext4_修复inode_链接数 接受 磁盘in：
    若 磁盘in 等于 空：
        返还 错误码_无效参数。
    完。
    若 做 ext4_获取链接数 用 磁盘in 等于 0：
        做 ext4_设置链接数 用 磁盘in、1。
    完。
    返还 0。
完。

ext4_AI_分析文件行为 接受 磁盘in：
    ext4_AI文件分析次数 为 ext4_AI文件分析次数 加 1。
    bus 为 全局AI总线。
    若 bus 等于 空 或者 磁盘in 等于 空：
        返还 做 ext4_启发式文件分析 用 磁盘in。
    完。
    特征 为 做 Tensor_创建 用 12。
    若 特征 等于 空：
        返还 做 ext4_启发式文件分析 用 磁盘in。
    完。
    做 Tensor_清零 用 特征、12。
    做 Tensor_写 用 特征、0、做 ext4_模式转类型 用 磁盘in 的 模式。
    大小 为 做 ext4_获取Nova文件节点大小 用 磁盘in。
    做 Tensor_写 用 特征、1、做 ext4_安全log2 用 大小。
    做 Tensor_写 用 特征、2、做 ext4_获取权限位 用 磁盘in。
    做 Tensor_写 用 特征、3、做 ext4_获取链接数 用 磁盘in。
    做 Tensor_写 用 特征、4、做 ext4_获取Nova文件节点块数 用 磁盘in。
    做 Tensor_写 用 特征、5、做 ext4_获取Nova文件节点_uid 用 磁盘in。
    做 Tensor_写 用 特征、6、做 ext4_获取标志 用 磁盘in。
    做 Tensor_写 用 特征、7、做 ext4_获取时间戳_秒 用 磁盘in、2。
    异常分 为 做 AI总线_异常检测 用 bus、「ext4_inode_behavior」、特征。
    做 Tensor_释放 用 特征。
    若 异常分 大于或等于 7000：
        返还 2。
    完。
    若 异常分 大于或等于 4000：
        返还 1。
    完。
    返还 0。
完。

ext4_启发式文件分析 接受 磁盘in：
    若 磁盘in 等于 空：
        返还 2。
    完。
    可变 异常计数 为 0。
    权限 为 做 ext4_获取权限位 用 磁盘in。
    若 权限 等于 0x0FFF：
        异常计数 为 异常计数 加 1。
    完。
    若 权限 位与 EXT4_S_ISUID 并且 权限 位与 EXT4_S_IWOTH：
        异常计数 为 异常计数 加 1。
    完。
    大小 为 做 ext4_获取Nova文件节点大小 用 磁盘in。
    若 大小 大于 1099511627776：
        异常计数 为 异常计数 加 1。
    完。
    若 做 ext4_获取链接数 用 磁盘in 大于 1000：
        异常计数 为 异常计数 加 1。
    完。
    若 异常计数 大于或等于 2：
        返还 2。
    完。
    若 异常计数 大于或等于 1：
        返还 1。
    完。
    返还 0。
完。

ext4_安全log2 接受 值：
    若 值 小于或等于 0：
        返还 0。
    完。
    可变 结果 为 0。
    可变 n 为 值。
    只要 n 大于 1：
        n 为 n 右移 1。
        结果 为 结果 加 1。
    完。
    返还 结果。
完。

ext4Nova文件节点_完整统计 接受 零参：
    返还 一组：ext4_Nova文件节点读次数、ext4_Nova文件节点写次数、ext4_属性读总次数、ext4_属性写总次数、ext4_权限检查次数、ext4_权限拒绝次数、ext4_校验失败次数、ext4_AI文件分析次数、ext4_时间戳读次数、ext4_扩展属性读次数。
完。

ext4_打印文件节点诊断 接受 零参：
    做 内核打印 用 「═══ ext4文件节点属性诊断 ═══\n」。
    做 内核打印 用 「读=」、ext4_Nova文件节点读次数、「 写=」、ext4_Nova文件节点写次数、「\n」。
    做 内核打印 用 「属性读=」、ext4_属性读总次数、「 属性写=」、ext4_属性写总次数、「\n」。
    做 内核打印 用 「权限检查=」、ext4_权限检查次数、「 拒绝=」、ext4_权限拒绝次数、「\n」。
    做 内核打印 用 「校验失败=」、ext4_校验失败次数、「 AI分析=」、ext4_AI文件分析次数、「\n」。
完。

本章 收束。
