《临时文件》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·内核打印》。

引用《..·中枢·加密框架》。

引用《..·标准·文件》。

TMP_根目录 为 「/tmp」。

TMP_最大文件数 为 4096。

TMP_最大总字节 为 268435456。

TMP_超时秒 为 86400。

临时文件记录 是 一种 事物，含 路径、创建者PID、创建时间、大小、已清理。

可变 _临时文件表 为 一组：（空）。

可变 _临时总字节 为 0。

可变 _临时序号 为 0。

可变 _清理次数 为 0。

临时_创建 接受 前缀、PID：
    若 做 计数 用 _临时文件表 大于或等于 TMP_最大文件数：
        做 临时_清理超时 用 做 读取毫秒 除以 1000。
        若 做 计数 用 _临时文件表 大于或等于 TMP_最大文件数：
            返还 空。
        完。
    完。
    做 CSPRNG_确保初始化。
    _临时序号 为 _临时序号 加 1。
    随机 为 做 CSPRNG_生成。
    若 随机 小于 0：
        随机 为 0 减 随机。
    完。
    文件名 为 前缀 加 「_」 加 做 整数转字符串 用 PID 加 「_」 加 做 整数转字符串 用 _临时序号 加 「_」 加 做 整数转字符串 用 随机 取余于 100000。
    路径 为 TMP_根目录 加 「/」 加 文件名。
    记录 为 新建 临时文件记录。
    记录 的 路径 为 路径。
    记录 的 创建者PID 为 PID。
    记录 的 创建时间 为 做 读取毫秒 除以 1000。
    记录 的 大小 为 0。
    记录 的 已清理 为 假。
    做 追加 用 _临时文件表、记录。
    返还 路径。
完。

临时_创建目录 接受 前缀、PID：
    做 CSPRNG_确保初始化。
    _临时序号 为 _临时序号 加 1。
    随机 为 做 CSPRNG_生成。
    若 随机 小于 0：
        随机 为 0 减 随机。
    完。
    目录名 为 前缀 加 「_d_」 加 做 整数转字符串 用 PID 加 「_」 加 做 整数转字符串 用 随机 取余于 100000。
    路径 为 TMP_根目录 加 「/」 加 目录名。
    做 目录_创建 用 路径。
    记录 为 新建 临时文件记录。
    记录 的 路径 为 路径。
    记录 的 创建者PID 为 PID。
    记录 的 创建时间 为 做 读取毫秒 除以 1000。
    记录 的 大小 为 0。
    记录 的 已清理 为 假。
    做 追加 用 _临时文件表、记录。
    返还 路径。
完。

临时_删除 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _临时文件表：
        若 _临时文件表 的 第 i 项 的 路径 等于 路径 并且 非 _临时文件表 的 第 i 项 的 已清理：
            做 文件_删除 用 路径。
            _临时总字节 为 _临时总字节 减 _临时文件表 的 第 i 项 的 大小。
            _临时文件表 的 第 i 项 的 已清理 为 真。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

临时_清理PID 接受 PID：
    可变 已清理 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _临时文件表：
        若 _临时文件表 的 第 i 项 的 创建者PID 等于 PID 并且 非 _临时文件表 的 第 i 项 的 已清理：
            做 文件_删除 用 _临时文件表 的 第 i 项 的 路径。
            _临时总字节 为 _临时总字节 减 _临时文件表 的 第 i 项 的 大小。
            _临时文件表 的 第 i 项 的 已清理 为 真。
            已清理 为 已清理 加 1。
        完。
        i 为 i 加 1。
    完。
    若 已清理 大于 0：
        _清理次数 为 _清理次数 加 1。
    完。
    返还 已清理。
完。

临时_清理超时 接受 当前时间：
    可变 已清理 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _临时文件表：
        若 非 _临时文件表 的 第 i 项 的 已清理 并且 当前时间 减 _临时文件表 的 第 i 项 的 创建时间 大于 TMP_超时秒：
            做 文件_删除 用 _临时文件表 的 第 i 项 的 路径。
            _临时总字节 为 _临时总字节 减 _临时文件表 的 第 i 项 的 大小。
            _临时文件表 的 第 i 项 的 已清理 为 真。
            已清理 为 已清理 加 1。
        完。
        i 为 i 加 1。
    完。
    若 已清理 大于 0：
        _清理次数 为 _清理次数 加 1。
    完。
    返还 已清理。
完。

临时_清理全部 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _临时文件表：
        若 非 _临时文件表 的 第 i 项 的 已清理：
            做 文件_删除 用 _临时文件表 的 第 i 项 的 路径。
            _临时文件表 的 第 i 项 的 已清理 为 真。
        完。
        i 为 i 加 1。
    完。
    _临时总字节 为 0。
    _清理次数 为 _清理次数 加 1。
完。

临时_配额可用 接受 零参：
    返还 _临时总字节 小于 TMP_最大总字节。
完。

临时_登记大小 接受 路径、大小：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _临时文件表：
        若 _临时文件表 的 第 i 项 的 路径 等于 路径：
            _临时总字节 为 _临时总字节 减 _临时文件表 的 第 i 项 的 大小 加 大小。
            _临时文件表 的 第 i 项 的 大小 为 大小。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

临时_活跃数 接受 零参：
    可变 n 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _临时文件表：
        若 非 _临时文件表 的 第 i 项 的 已清理：
            n 为 n 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 n。
完。

临时_报告 接受 零参：
    可变 r 为 「═══ 临时文件管理 ═══\n」。
    r 为 r 加 「活跃: 」 加 做 整数转字符串 用 做 临时_活跃数 加 「 / 」 加 做 整数转字符串 用 TMP_最大文件数 加 「\n」。
    r 为 r 加 「占用: 」 加 做 整数转字符串 用 _临时总字节 除以 1024 加 「KB / 」 加 做 整数转字符串 用 TMP_最大总字节 除以 1048576 加 「MB\n」。
    r 为 r 加 「清理次数: 」 加 做 整数转字符串 用 _清理次数 加 「\n」。
    返还 r。
完。

本章 收束。
