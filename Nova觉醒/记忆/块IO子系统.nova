《块IO子系统》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·同步原语》。

引用《..·推理·定点Q算术》。

引用《..·中枢·内核打印》。

引用《..·感知·内核直连层》。

引用《..·中枢·伙伴系统释放》。

BIO_标志_读 为 0x01。

BIO_标志_写 为 0x02。

BIO_标志_同步 为 0x04。

BIO_标志_刷新 为 0x08。

BIO_标志_FUA 为 0x10。

BIO_标志_丢弃 为 0x20。

BIO_标志_预取 为 0x40。

BIO_标志_优先 为 0x80。

BIO_最大向量数 为 256。

BIO_池大小 为 128。

扇区大小_默认 为 512。

页面大小_默认 为 4096。

DMA_对齐掩码 为 0xFFF。

IO_合并门槛 为 8。

预取窗口大小 为 32。

预取历史大小 为 64。

外部。

全局AI总线。

BIO向量 是 一种 事物，含 页、偏移、长度。

BIO 是 一种 事物，含 设备、扇区、大小、标志、向量列表、向量数、最大向量数、当前向量、回调、私有数据、错误码、引用计数、提交时间、完成时间。

BIO池 是 一种 事物，含 空闲列表、总数、空闲数、分配次数、回收次数、池失败次数。

BIO统计 是 一种 事物，含 总读次数、总写次数、总读字节、总写字节、拆分次数、合并次数、DMA映射次数、预取触发数、预取命中数、平均IO延迟_微秒、最大IO延迟_微秒、AI优化次数。

预取记录 是 一种 事物，含 扇区号、访问时间、是读。

可变 bio_池 为 新建 BIO池。

可变 bio_统计 为 新建 BIO统计。

可变 bio_预取历史 为 一组：（空）。

可变 bio_已初始化 为 假。

bio_子系统_初始化 接受 零参：
    若 bio_已初始化：
        返还 真。
    完。
    bio_池 的 空闲列表 为 一组：（空）。
    bio_池 的 总数 为 BIO_池大小。
    bio_池 的 空闲数 为 BIO_池大小。
    bio_池 的 分配次数 为 0。
    bio_池 的 回收次数 为 0。
    bio_池 的 池失败次数 为 0。
    可变 i 为 0。
    只要 i 小于 BIO_池大小：
        b 为 新建 BIO。
        b 的 向量列表 为 一组：（空）。
        b 的 向量数 为 0。
        b 的 最大向量数 为 BIO_最大向量数。
        b 的 当前向量 为 0。
        b 的 引用计数 为 0。
        b 的 错误码 为 0。
        做 追加 用 bio_池 的 空闲列表、b。
        i 为 i 加 1。
    完。
    bio_统计 的 总读次数 为 0。
    bio_统计 的 总写次数 为 0。
    bio_统计 的 总读字节 为 0。
    bio_统计 的 总写字节 为 0。
    bio_统计 的 拆分次数 为 0。
    bio_统计 的 合并次数 为 0。
    bio_统计 的 DMA映射次数 为 0。
    bio_统计 的 预取触发数 为 0。
    bio_统计 的 预取命中数 为 0。
    bio_统计 的 平均IO延迟_微秒 为 0.0。
    bio_统计 的 最大IO延迟_微秒 为 0。
    bio_统计 的 AI优化次数 为 0。
    bio_预取历史 为 一组：（空）。
    bio_已初始化 为 真。
    返还 真。
完。

bio_分配 接受 最大向量：
    若 非 bio_已初始化：
        做 bio_子系统_初始化。
    完。
    若 bio_池 的 空闲数 大于 0：
        b 为 bio_池 的 空闲列表 的 第 bio_池 的 空闲数 减 1 项。
        bio_池 的 空闲数 为 bio_池 的 空闲数 减 1。
        bio_池 的 分配次数 为 bio_池 的 分配次数 加 1。
        b 的 向量列表 为 一组：（空）。
        b 的 向量数 为 0。
        b 的 当前向量 为 0。
        b 的 最大向量数 为 最大向量。
        b 的 大小 为 0。
        b 的 标志 为 0。
        b 的 引用计数 为 1。
        b 的 错误码 为 0。
        b 的 设备 为 空。
        b 的 回调 为 空。
        返还 b。
    完。
    bio_池 的 池失败次数 为 bio_池 的 池失败次数 加 1。
    b 为 新建 BIO。
    b 的 向量列表 为 一组：（空）。
    b 的 向量数 为 0。
    b 的 最大向量数 为 最大向量。
    b 的 当前向量 为 0。
    b 的 引用计数 为 1。
    b 的 错误码 为 0。
    返还 b。
完。

bio_释放 接受 bio：
    若 bio 等于 空：
        返还。
    完。
    bio 的 引用计数 为 bio 的 引用计数 减 1。
    若 bio 的 引用计数 大于 0：
        返还。
    完。
    若 bio_池 的 空闲数 小于 bio_池 的 总数：
        bio_池 的 空闲列表 的 第 bio_池 的 空闲数 项 为 bio。
        bio_池 的 空闲数 为 bio_池 的 空闲数 加 1。
        bio_池 的 回收次数 为 bio_池 的 回收次数 加 1。
    完。
完。

bio_增加引用 接受 bio：
    若 bio 等于 空：
        返还。
    完。
    bio 的 引用计数 为 bio 的 引用计数 加 1。
完。

bio_获取数据指针 接受 bio、向量索引：
    若 bio 等于 空：
        返还 空。
    完。
    若 向量索引 大于或等于 bio 的 向量数：
        返还 空。
    完。
    vec 为 bio 的 向量列表 的 第 向量索引 项。
    返还 做 页面地址 用 vec 的 页 加 vec 的 偏移。
完。

bio_获取DMA地址 接受 bio、向量索引：
    若 bio 等于 空 或者 向量索引 大于或等于 bio 的 向量数：
        返还 0。
    完。
    vec 为 bio 的 向量列表 的 第 向量索引 项。
    物理 为 做 页面_到_物理 用 vec 的 页 加 vec 的 偏移。
    若 物理 位与 DMA_对齐掩码 不等于 0：
        返还 做 bio_创建bounce 用 物理、vec 的 长度。
    完。
    bio_统计 的 DMA映射次数 为 bio_统计 的 DMA映射次数 加 1。
    返还 物理。
完。

bio_创建bounce 接受 物理地址、长度：
    对齐地址 为 物理地址 加 页面大小_默认 减 1 位与 按位取反 DMA_对齐掩码。
    bounce 为 做 内核_分配_对齐 用 长度、页面大小_默认。
    若 bounce 不等于 0：
        做 内存_复制 用 bounce、物理地址、长度。
    完。
    返还 bounce。
完。

bio_下一个向量 接受 bio：
    若 bio 等于 空：
        返还 空。
    完。
    若 bio 的 当前向量 大于或等于 bio 的 向量数：
        返还 空。
    完。
    vec 为 bio 的 向量列表 的 第 bio 的 当前向量 项。
    bio 的 当前向量 为 bio 的 当前向量 加 1。
    返还 vec。
完。

bio_重置遍历 接受 bio：
    若 bio 不等于 空：
        bio 的 当前向量 为 0。
    完。
完。

bio_总大小 接受 bio：
    若 bio 等于 空：
        返还 0。
    完。
    返还 bio 的 大小。
完。

bio_扇区数 接受 bio：
    若 bio 等于 空 或者 bio 的 设备 等于 空：
        返还 0。
    完。
    返还 bio 的 大小 除以 bio 的 设备 的 扇区大小。
完。

bio_添加页 接受 bio、页、长度、偏移：
    若 bio 等于 空：
        返还 假。
    完。
    若 bio 的 向量数 大于或等于 bio 的 最大向量数：
        返还 假。
    完。
    vec 为 新建 BIO向量。
    vec 的 页 为 页。
    vec 的 偏移 为 偏移。
    vec 的 长度 为 长度。
    做 追加 用 bio 的 向量列表、vec。
    bio 的 向量数 为 bio 的 向量数 加 1。
    bio 的 大小 为 bio 的 大小 加 长度。
    返还 真。
完。

bio_添加内核缓冲 接受 bio、缓冲、大小：
    若 bio 等于 空 或者 缓冲 等于 0：
        返还 假。
    完。
    页 为 做 虚拟_到_页 用 缓冲。
    偏移 为 缓冲 位与 DMA_对齐掩码。
    返还 做 bio_添加页 用 bio、页、大小、偏移。
完。

bio_拆分 接受 bio、扇区数：
    若 bio 等于 空 或者 bio 的 设备 等于 空：
        返还 空。
    完。
    拆分大小 为 扇区数 乘 bio 的 设备 的 扇区大小。
    若 拆分大小 大于或等于 bio 的 大小：
        返还 空。
    完。
    可变 新bio 为 做 bio_分配 用 bio 的 最大向量数。
    若 新bio 等于 空：
        返还 空。
    完。
    新bio 的 设备 为 bio 的 设备。
    新bio 的 扇区 为 bio 的 扇区 加 扇区数。
    新bio 的 标志 为 bio 的 标志。
    可变 累积大小 为 0。
    可变 i 为 0。
    只要 i 小于 bio 的 向量数：
        若 累积大小 大于或等于 拆分大小：
            做 bio_添加页 用 新bio、bio 的 向量列表 的 第 i 项 的 页、bio 的 向量列表 的 第 i 项 的 长度、bio 的 向量列表 的 第 i 项 的 偏移。
        完。
        累积大小 为 累积大小 加 bio 的 向量列表 的 第 i 项 的 长度。
        i 为 i 加 1。
    完。
    bio 的 大小 为 拆分大小。
    累积大小 为 0。
    bio 的 向量数 为 0。
    i 为 0。
    只要 累积大小 小于 拆分大小：
        累积大小 为 累积大小 加 bio 的 向量列表 的 第 i 项 的 长度。
        bio 的 向量数 为 bio 的 向量数 加 1。
        i 为 i 加 1。
    完。
    bio_统计 的 拆分次数 为 bio_统计 的 拆分次数 加 1。
    返还 新bio。
完。

bio_尝试合并 接受 bio1、bio2：
    若 bio1 等于 空 或者 bio2 等于 空：
        返还 假。
    完。
    若 bio1 的 设备 不等于 bio2 的 设备：
        返还 假。
    完。
    若 bio1 的 标志 位与 0x03 不等于 bio2 的 标志 位与 0x03：
        返还 假。
    完。
    bio1_结束扇区 为 bio1 的 扇区 加 做 bio_扇区数 用 bio1。
    若 bio1_结束扇区 不等于 bio2 的 扇区：
        返还 假。
    完。
    若 bio1 的 向量数 加 bio2 的 向量数 大于 bio1 的 最大向量数：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 bio2 的 向量数：
        做 bio_添加页 用 bio1、bio2 的 向量列表 的 第 i 项 的 页、bio2 的 向量列表 的 第 i 项 的 长度、bio2 的 向量列表 的 第 i 项 的 偏移。
        i 为 i 加 1。
    完。
    bio_统计 的 合并次数 为 bio_统计 的 合并次数 加 1。
    做 bio_释放 用 bio2。
    返还 真。
完。

bio_创建_读 接受 设备、扇区、缓冲、大小：
    可变 bio 为 做 bio_分配 用 1。
    若 bio 等于 空：
        返还 空。
    完。
    bio 的 设备 为 设备。
    bio 的 扇区 为 扇区。
    bio 的 标志 为 BIO_标志_读。
    做 bio_添加内核缓冲 用 bio、缓冲、大小。
    bio_统计 的 总读次数 为 bio_统计 的 总读次数 加 1。
    bio_统计 的 总读字节 为 bio_统计 的 总读字节 加 大小。
    返还 bio。
完。

bio_创建_写 接受 设备、扇区、缓冲、大小：
    可变 bio 为 做 bio_分配 用 1。
    若 bio 等于 空：
        返还 空。
    完。
    bio 的 设备 为 设备。
    bio 的 扇区 为 扇区。
    bio 的 标志 为 BIO_标志_写。
    做 bio_添加内核缓冲 用 bio、缓冲、大小。
    bio_统计 的 总写次数 为 bio_统计 的 总写次数 加 1。
    bio_统计 的 总写字节 为 bio_统计 的 总写字节 加 大小。
    返还 bio。
完。

bio_创建_批量读 接受 设备、扇区、页列表、页数：
    可变 bio 为 做 bio_分配 用 页数。
    若 bio 等于 空：
        返还 空。
    完。
    bio 的 设备 为 设备。
    bio 的 扇区 为 扇区。
    bio 的 标志 为 BIO_标志_读。
    可变 i 为 0。
    只要 i 小于 页数：
        做 bio_添加页 用 bio、页列表 的 第 i 项、页面大小_默认、0。
        i 为 i 加 1。
    完。
    bio_统计 的 总读次数 为 bio_统计 的 总读次数 加 1。
    bio_统计 的 总读字节 为 bio_统计 的 总读字节 加 页数 乘 页面大小_默认。
    返还 bio。
完。

bio_提交 接受 bio：
    若 bio 等于 空 或者 bio 的 设备 等于 空：
        返还 负 1。
    完。
    bio 的 提交时间 为 做 系统_获取时间戳。
    做 bio_记录访问 用 bio 的 扇区、bio 的 标志 位与 BIO_标志_读 不等于 0。
    结果 为 做 设备_提交请求 用 bio 的 设备、bio。
    返还 结果。
完。

bio_提交_同步 接受 bio：
    若 bio 等于 空：
        返还 负 1。
    完。
    bio 的 标志 为 bio 的 标志 位或 BIO_标志_同步。
    结果 为 做 bio_提交 用 bio。
    只要 bio 的 完成时间 等于 0：
        做 系统_延迟 用 1。
    完。
    延迟 为 bio 的 完成时间 减 bio 的 提交时间。
    若 延迟 大于 bio_统计 的 最大IO延迟_微秒：
        bio_统计 的 最大IO延迟_微秒 为 延迟。
    完。
    返还 bio 的 错误码。
完。

bio_完成回调 接受 bio、错误码：
    若 bio 等于 空：
        返还。
    完。
    bio 的 错误码 为 错误码。
    bio 的 完成时间 为 做 系统_获取时间戳。
    若 bio 的 回调 不等于 空：
        做 bio 的 回调 用 bio。
    完。
完。

bio_记录访问 接受 扇区、是读：
    记录 为 新建 预取记录。
    记录 的 扇区号 为 扇区。
    记录 的 访问时间 为 做 系统_获取时间戳。
    记录 的 是读 为 是读。
    做 追加 用 bio_预取历史、记录。
    若 做 计数 用 bio_预取历史 大于 预取历史大小：
        bio_预取历史 为 做 数组_切片 用 bio_预取历史、1、做 计数 用 bio_预取历史。
    完。
完。

bio_检测顺序模式 接受 零参：
    历史数 为 做 计数 用 bio_预取历史。
    若 历史数 小于 4：
        返还 假。
    完。
    可变 顺序计数 为 0。
    可变 i 为 历史数 减 3。
    只要 i 小于 历史数：
        若 bio_预取历史 的 第 i 项 的 扇区号 大于 bio_预取历史 的 第 i 减 1 项 的 扇区号：
            顺序计数 为 顺序计数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 顺序计数 大于或等于 2。
完。

bio_计算步长 接受 零参：
    历史数 为 做 计数 用 bio_预取历史。
    若 历史数 小于 2：
        返还 0。
    完。
    可变 总步长 为 0。
    可变 有效数 为 0。
    可变 i 为 历史数 减 3。
    若 i 小于 1：
        i 为 1。
    完。
    只要 i 小于 历史数：
        差 为 bio_预取历史 的 第 i 项 的 扇区号 减 bio_预取历史 的 第 i 减 1 项 的 扇区号。
        若 差 大于 0：
            总步长 为 总步长 加 差。
            有效数 为 有效数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 有效数 等于 0：
        返还 0。
    完。
    返还 总步长 除以 有效数。
完。

bio_AI预取 接受 设备：
    若 设备 等于 空：
        返还。
    完。
    若 非 做 bio_检测顺序模式：
        返还。
    完。
    历史数 为 做 计数 用 bio_预取历史。
    若 历史数 等于 0：
        返还。
    完。
    最近扇区 为 bio_预取历史 的 第 历史数 减 1 项 的 扇区号。
    步长 为 做 bio_计算步长。
    若 步长 等于 0：
        步长 为 8。
    完。
    可变 预取扇区数 为 预取窗口大小。
    若 全局AI总线 不等于 空：
        特征 为 做 Tensor_创建 用 8。
        做 Tensor_写 用 特征、0、最近扇区。
        做 Tensor_写 用 特征、1、步长。
        做 Tensor_写 用 特征、2、bio_统计 的 总读次数。
        做 Tensor_写 用 特征、3、bio_统计 的 预取命中数。
        做 Tensor_写 用 特征、4、bio_统计 的 预取触发数。
        做 Tensor_写 用 特征、5、历史数。
        做 Tensor_写 用 特征、6、bio_统计 的 总读字节。
        做 Tensor_写 用 特征、7、bio_统计 的 平均IO延迟_微秒。
        结果 为 做 AI总线_推理 用 全局AI总线、「块设备预取」、特征。
        若 结果 不等于 空：
            AI建议 为 做 Tensor_读 用 结果、0。
            若 AI建议 大于 0 并且 AI建议 小于或等于 256：
                预取扇区数 为 AI建议。
            完。
            bio_统计 的 AI优化次数 为 bio_统计 的 AI优化次数 加 1。
        完。
        做 Tensor_释放 用 特征。
    完。
    预取大小 为 预取扇区数 乘 扇区大小_默认。
    预取缓冲 为 做 内核_分配 用 预取大小。
    若 预取缓冲 不等于 0：
        bio 为 做 bio_创建_读 用 设备、最近扇区 加 步长、预取缓冲、预取大小。
        若 bio 不等于 空：
            bio 的 标志 为 bio 的 标志 位或 BIO_标志_预取。
            做 bio_提交 用 bio。
            bio_统计 的 预取触发数 为 bio_统计 的 预取触发数 加 1。
        完。
    完。
完。

散列聚合表项 是 一种 事物，含 物理地址、长度。

bio_构建SG表 接受 bio：
    若 bio 等于 空：
        返还 空。
    完。
    可变 sg表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 bio 的 向量数：
        vec 为 bio 的 向量列表 的 第 i 项。
        项 为 新建 散列聚合表项。
        项 的 物理地址 为 做 页面_到_物理 用 vec 的 页 加 vec 的 偏移。
        项 的 长度 为 vec 的 长度。
        若 做 计数 用 sg表 大于 0：
            上一项 为 sg表 的 第 做 计数 用 sg表 减 1 项。
            若 上一项 的 物理地址 加 上一项 的 长度 等于 项 的 物理地址：
                上一项 的 长度 为 上一项 的 长度 加 项 的 长度。
                i 为 i 加 1。
            否则：
                做 追加 用 sg表、项。
                i 为 i 加 1。
            完。
        否则：
            做 追加 用 sg表、项。
            i 为 i 加 1。
        完。
    完。
    返还 sg表。
完。

bio_DMA映射全部 接受 bio：
    若 bio 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 bio 的 向量数：
        dma地址 为 做 bio_获取DMA地址 用 bio、i。
        若 dma地址 等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

bio_获取统计 接受 零参：
    返还 bio_统计。
完。

bio_获取池统计 接受 零参：
    返还 bio_池。
完。

bio_打印统计 接受 零参：
    做 内核打印 用 「═══ NBDP 块IO统计 ═══」。
    做 内核打印 用 「总读: 」 加 做 转文本 用 bio_统计 的 总读次数 加 「次 」 加 做 转文本 用 bio_统计 的 总读字节 加 「字节」。
    做 内核打印 用 「总写: 」 加 做 转文本 用 bio_统计 的 总写次数 加 「次 」 加 做 转文本 用 bio_统计 的 总写字节 加 「字节」。
    做 内核打印 用 「拆分/合并: 」 加 做 转文本 用 bio_统计 的 拆分次数 加 「/」 加 做 转文本 用 bio_统计 的 合并次数。
    做 内核打印 用 「DMA映射: 」 加 做 转文本 用 bio_统计 的 DMA映射次数。
    做 内核打印 用 「预取触发/命中: 」 加 做 转文本 用 bio_统计 的 预取触发数 加 「/」 加 做 转文本 用 bio_统计 的 预取命中数。
    做 内核打印 用 「池分配/回收/失败: 」 加 做 转文本 用 bio_池 的 分配次数 加 「/」 加 做 转文本 用 bio_池 的 回收次数 加 「/」 加 做 转文本 用 bio_池 的 池失败次数。
    做 内核打印 用 「AI优化: 」 加 做 转文本 用 bio_统计 的 AI优化次数。
完。

本章 收束。
