《ext4组分配器》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·日志中枢》。

引用《..·中枢·同步原语》。

引用《..·中枢·进程调度》。

引用《..·中枢·内存虚拟》。

引用《..·中枢·进程信号》。

引用《..·感知·文档事件》。

引用《..·中枢·公共智能》。

引用《..·引导·引导加载器》。

引用《..·感知·内核直连层》。

引用《..·中枢·链表后继》。

引用《..·中枢·时钟中枢》。

引用《..·稳态·AHCI驱动全局》。

引用《..·中枢·整数转字符串》。

引用《.·ext4卸载超级块》。

EXT4_MB_组扫描上限 为 128。

EXT4_MB_好伙伴阈值 为 64。

EXT4_MB_预分配_文件 为 16。

EXT4_MB_预分配_目录 为 4。

EXT4_MB_最大连续分配 为 1024。

EXT4_MB_碎片_阈值 为 30。

位图_缓存最大 为 64。

Ext4分配请求 是 一种 事物，含 请求块数、目标组号、目标偏移、标志、PID、Nova文件节点号、是目录、是延迟。

Ext4分配结果 是 一种 事物，含 起始块、分配块数、组号、组内偏移。

位图缓存条目 是 一种 事物，含 组号、位图数据、脏、访问计数、上次访问。

可变 位图缓存 为 一组：（空）。

可变 分配_总请求 为 0。

可变 分配_总分配块 为 0。

可变 分配_精确命中 为 0。

可变 分配_好伙伴命中 为 0。

可变 分配_任意命中 为 0。

可变 分配_失败 为 0。

可变 分配_预分配命中 为 0。

可变 AI_预取命中 为 0。

ext4_尝试组分配 接受 sb、请求、组号：
    信息 为 sb 的 私有信息。
    空闲数 为 做 ext4_获取空闲块数 用 信息、组号。
    若 空闲数 小于 请求 的 请求块数：
        返还 错误码_无空间。
    完。
    位图块号 为 做 ext4_获取块位图块 用 信息、组号。
    可变 位图 为 做 块设备_读 用 sb 的 设备、位图块号 乘 信息 的 块大小、信息 的 块大小。
    若 位图 等于 空：
        返还 错误码_IO。
    完。
    结果 为 做 ext4_位图查找连续空闲 用 位图、信息 的 每组块数、请求 的 请求块数。
    若 结果 的 起始 小于 0：
        做 缓冲_释放 用 位图。
        返还 错误码_无空间。
    完。
    做 ext4_位图设置范围 用 位图、结果 的 起始、结果 的 长度。
    做 块设备_写 用 sb 的 设备、位图块号 乘 信息 的 块大小、位图、信息 的 块大小。
    做 缓冲_释放 用 位图。
    信息 的 组描述符 的 第 组号 项 的 空闲块数_低 为 信息 的 组描述符 的 第 组号 项 的 空闲块数_低 减 结果 的 长度。
    绝对块号 为 组号 乘 信息 的 每组块数 加 信息 的 磁盘超级块 的 首数据块 加 结果 的 起始。
    可变 分配结果 为 新建 Ext4分配结果。
    分配结果 的 起始块 为 绝对块号。
    分配结果 的 分配块数 为 结果 的 长度。
    分配结果 的 组号 为 组号。
    分配结果 的 组内偏移 为 结果 的 起始。
    返还 分配结果。
完。

ext4_扫描组_精确 接受 sb、请求：
    信息 为 sb 的 私有信息。
    可变 最佳组 为 负 1。
    可变 最佳空闲 为 0xFFFFFFFF。
    可变 i 为 0。
    只要 i 小于 信息 的 块组数 并且 i 小于 EXT4_MB_组扫描上限：
        空闲 为 做 ext4_获取空闲块数 用 信息、i。
        若 空闲 大于或等于 请求 的 请求块数 并且 空闲 小于 最佳空闲：
            最佳空闲 为 空闲。
            最佳组 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最佳组 大于或等于 0：
        返还 做 ext4_尝试组分配 用 sb、请求、最佳组。
    完。
    返还 错误码_无空间。
完。

ext4_扫描组_好伙伴 接受 sb、请求：
    信息 为 sb 的 私有信息。
    可变 i 为 0。
    只要 i 小于 信息 的 块组数：
        空闲 为 做 ext4_获取空闲块数 用 信息、i。
        若 空闲 大于或等于 EXT4_MB_好伙伴阈值：
            结果 为 做 ext4_尝试组分配 用 sb、请求、i。
            若 非 做 是错误 用 结果：
                返还 结果。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 错误码_无空间。
完。

ext4_扫描组_任意 接受 sb、请求：
    信息 为 sb 的 私有信息。
    可变 原始请求数 为 请求 的 请求块数。
    若 请求 的 请求块数 大于 1：
        请求 的 请求块数 为 1。
    完。
    可变 i 为 0。
    只要 i 小于 信息 的 块组数：
        空闲 为 做 ext4_获取空闲块数 用 信息、i。
        若 空闲 大于 0：
            结果 为 做 ext4_尝试组分配 用 sb、请求、i。
            若 非 做 是错误 用 结果：
                返还 结果。
            完。
        完。
        i 为 i 加 1。
    完。
    请求 的 请求块数 为 原始请求数。
    返还 错误码_无空间。
完。

ext4_mb分配块 接受 sb、请求：
    分配_总请求 为 分配_总请求 加 1。
    AI建议块数 为 做 AI_分配预测 用 请求。
    若 AI建议块数 大于 请求 的 请求块数 并且 AI建议块数 小于或等于 EXT4_MB_最大连续分配：
        请求 的 请求块数 为 AI建议块数。
    完。
    预分配结果 为 做 ext4_检查预分配 用 sb、请求。
    若 预分配结果 不等于 空：
        分配_预分配命中 为 分配_预分配命中 加 1。
        分配_总分配块 为 分配_总分配块 加 预分配结果 的 分配块数。
        返还 预分配结果。
    完。
    可变 结果 为 做 ext4_扫描组_精确 用 sb、请求。
    若 非 做 是错误 用 结果：
        分配_精确命中 为 分配_精确命中 加 1。
        分配_总分配块 为 分配_总分配块 加 结果 的 分配块数。
        做 ext4_建立预分配 用 sb、请求、结果。
        返还 结果。
    完。
    结果 为 做 ext4_扫描组_好伙伴 用 sb、请求。
    若 非 做 是错误 用 结果：
        分配_好伙伴命中 为 分配_好伙伴命中 加 1。
        分配_总分配块 为 分配_总分配块 加 结果 的 分配块数。
        返还 结果。
    完。
    结果 为 做 ext4_扫描组_任意 用 sb、请求。
    若 非 做 是错误 用 结果：
        分配_任意命中 为 分配_任意命中 加 1。
        分配_总分配块 为 分配_总分配块 加 结果 的 分配块数。
        返还 结果。
    完。
    分配_失败 为 分配_失败 加 1。
    做 内核警告 用 「[ext4] 块分配失败: 请求」 加 做 整数转字符串 用 请求 的 请求块数 加 「块, 无可用空间」。
    返还 错误码_无空间。
完。

ext4_释放块 接受 sb、起始块、块数：
    信息 为 sb 的 私有信息。
    若 块数 小于或等于 0：
        返还 错误码_无效参数。
    完。
    组号 为 (起始块 减 信息 的 磁盘超级块 的 首数据块) 除以 信息 的 每组块数。
    组内偏移 为 (起始块 减 信息 的 磁盘超级块 的 首数据块) 取余于 信息 的 每组块数。
    若 组号 大于或等于 信息 的 块组数：
        返还 错误码_无效参数。
    完。
    位图 为 做 ext4_加载位图_缓存 用 sb、组号。
    若 位图 等于 空：
        返还 错误码_IO。
    完。
    做 ext4_位图清除范围 用 位图、组内偏移、块数。
    做 ext4_位图缓存_标记脏 用 组号。
    信息 的 组描述符 的 第 组号 项 的 空闲块数_低 为 信息 的 组描述符 的 第 组号 项 的 空闲块数_低 加 块数。
    返还 成功。
完。

ext4_位图查找连续空闲 接受 位图、总位数、需要位数：
    可变 连续空闲 为 0。
    可变 起始位 为 负 1。
    可变 i 为 0。
    只要 i 小于 总位数：
        字节索引 为 i 除以 8。
        位索引 为 i 取余于 8。
        已使用 为 做 内存_读8 用 位图 加 字节索引 右移 位索引 位与 1。
        若 已使用 等于 0：
            若 连续空闲 等于 0：
                起始位 为 i。
            完。
            连续空闲 为 连续空闲 加 1。
            若 连续空闲 大于或等于 需要位数：
                返还 一本字典：起始 对应 起始位、长度 对应 需要位数。
            完。
        否则：
            连续空闲 为 0。
        完。
        i 为 i 加 1。
    完。
    返还 一本字典：起始 对应 负 1、长度 对应 0。
完。

ext4_位图设置范围 接受 位图、起始、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        位 为 起始 加 i。
        字节索引 为 位 除以 8。
        位索引 为 位 取余于 8。
        旧值 为 做 内存_读8 用 位图 加 字节索引。
        做 内存_写8 用 位图 加 字节索引、旧值 位或 1 左移 位索引。
        i 为 i 加 1。
    完。
完。

ext4_位图清除范围 接受 位图、起始、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        位 为 起始 加 i。
        字节索引 为 位 除以 8。
        位索引 为 位 取余于 8。
        旧值 为 做 内存_读8 用 位图 加 字节索引。
        做 内存_写8 用 位图 加 字节索引、旧值 位与 按位取反 (1 左移 位索引)。
        i 为 i 加 1。
    完。
完。

ext4_位图_统计空闲 接受 位图、总位数：
    可变 空闲 为 0。
    可变 i 为 0。
    只要 i 小于 总位数 除以 8：
        字节 为 做 内存_读8 用 位图 加 i。
        可变 b 为 字节。
        可变 已用 为 0。
        只要 b 大于 0：
            已用 为 已用 加 (b 位与 1)。
            b 为 b 右移 1。
        完。
        空闲 为 空闲 加 8 减 已用。
        i 为 i 加 1。
    完。
    返还 空闲。
完。

ext4_位图_碎片度 接受 位图、总位数：
    可变 段数 为 0。
    可变 空闲总 为 0。
    可变 在空闲段中 为 假。
    可变 i 为 0。
    只要 i 小于 总位数：
        字节索引 为 i 除以 8。
        位索引 为 i 取余于 8。
        已使用 为 做 内存_读8 用 位图 加 字节索引 右移 位索引 位与 1。
        若 已使用 等于 0：
            若 非 在空闲段中：
                段数 为 段数 加 1。
                在空闲段中 为 真。
            完。
            空闲总 为 空闲总 加 1。
        否则：
            在空闲段中 为 假。
        完。
        i 为 i 加 1。
    完。
    若 空闲总 等于 0：
        返还 100。
    完。
    返还 段数 乘 100 除以 空闲总。
完。

ext4_加载位图_缓存 接受 sb、组号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 位图缓存：
        若 位图缓存 的 第 i 项 的 组号 等于 组号：
            位图缓存 的 第 i 项 的 访问计数 为 位图缓存 的 第 i 项 的 访问计数 加 1。
            位图缓存 的 第 i 项 的 上次访问 为 做 读取毫秒。
            返还 位图缓存 的 第 i 项 的 位图数据。
        完。
        i 为 i 加 1。
    完。
    信息 为 sb 的 私有信息。
    位图块号 为 做 ext4_获取块位图块 用 信息、组号。
    位图 为 做 块设备_读 用 sb 的 设备、位图块号 乘 信息 的 块大小、信息 的 块大小。
    若 位图 等于 空：
        返还 空。
    完。
    若 做 计数 用 位图缓存 大于或等于 位图_缓存最大：
        做 ext4_位图缓存_淘汰。
    完。
    做 追加 用 位图缓存、新建 位图缓存条目：组号 为 组号、位图数据 为 位图、脏 为 假、访问计数 为 1、上次访问 为 做 读取毫秒。
    返还 位图。
完。

ext4_位图缓存_标记脏 接受 组号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 位图缓存：
        若 位图缓存 的 第 i 项 的 组号 等于 组号：
            位图缓存 的 第 i 项 的 脏 为 真。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

ext4_位图缓存_刷新 接受 sb：
    信息 为 sb 的 私有信息。
    可变 i 为 0。
    只要 i 小于 做 计数 用 位图缓存：
        若 位图缓存 的 第 i 项 的 脏：
            位图块号 为 做 ext4_获取块位图块 用 信息、位图缓存 的 第 i 项 的 组号。
            做 块设备_写 用 sb 的 设备、位图块号 乘 信息 的 块大小、位图缓存 的 第 i 项 的 位图数据、信息 的 块大小。
            位图缓存 的 第 i 项 的 脏 为 假。
        完。
        i 为 i 加 1。
    完。
完。

ext4_位图缓存_淘汰 接受 零参：
    若 做 计数 用 位图缓存 等于 0：
        返还。
    完。
    可变 最低 为 0。
    可变 最低访问 为 位图缓存 的 第 0 项 的 访问计数。
    可变 i 为 1。
    只要 i 小于 做 计数 用 位图缓存：
        若 位图缓存 的 第 i 项 的 访问计数 小于 最低访问：
            最低 为 i。
            最低访问 为 位图缓存 的 第 i 项 的 访问计数。
        完。
        i 为 i 加 1。
    完。
    若 位图缓存 的 第 最低 项 的 脏：
    完。
    做 缓冲_释放 用 位图缓存 的 第 最低 项 的 位图数据。
    做 链表_删除 用 位图缓存、最低。
完。

预分配窗口 是 一种 事物，含 Nova文件节点号、起始块、总块数、已使用块数、创建时间。

可变 预分配表 为 一组：（空）。

预分配_最大 为 256。

ext4_检查预分配 接受 sb、请求：
    可变 i 为 0。
    只要 i 小于 做 计数 用 预分配表：
        pa 为 预分配表 的 第 i 项。
        若 pa 的 Nova文件节点号 等于 请求 的 Nova文件节点号：
            剩余 为 pa 的 总块数 减 pa 的 已使用块数。
            若 剩余 大于或等于 请求 的 请求块数：
                结果 为 新建 Ext4分配结果：起始块 为 pa 的 起始块 加 pa 的 已使用块数、分配块数 为 请求 的 请求块数、组号 为 0、组内偏移 为 0。
                pa 的 已使用块数 为 pa 的 已使用块数 加 请求 的 请求块数。
                返还 结果。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

ext4_建立预分配 接受 sb、请求、已分配：
    若 做 计数 用 预分配表 大于或等于 预分配_最大：
        返还。
    完。
    预分配数 为 EXT4_MB_预分配_文件。
    若 请求 的 是目录：
        预分配数 为 EXT4_MB_预分配_目录。
    完。
    做 追加 用 预分配表、新建 预分配窗口：Nova文件节点号 为 请求 的 Nova文件节点号、起始块 为 已分配 的 起始块 加 已分配 的 分配块数、总块数 为 预分配数、已使用块数 为 0、创建时间 为 做 读取毫秒。
完。

ext4_清理预分配 接受 Nova文件节点号：
    可变 i 为 做 计数 用 预分配表 减 1。
    只要 i 大于或等于 0：
        若 预分配表 的 第 i 项 的 Nova文件节点号 等于 Nova文件节点号：
            未使用 为 预分配表 的 第 i 项 的 总块数 减 预分配表 的 第 i 项 的 已使用块数。
            若 未使用 大于 0：
            完。
            做 链表_删除 用 预分配表、i。
        完。
        i 为 i 减 1。
    完。
完。

AI文件增长记录 是 一种 事物，含 Nova文件节点号、历史分配、分配次数。

可变 AI文件增长表 为 一组：（空）。

AI_分配预测 接受 请求：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI文件增长表：
        若 AI文件增长表 的 第 i 项 的 Nova文件节点号 等于 请求 的 Nova文件节点号：
            记录 为 AI文件增长表 的 第 i 项。
            若 记录 的 分配次数 大于或等于 3：
                可变 总增长 为 0。
                可变 j 为 0。
                只要 j 小于 做 计数 用 记录 的 历史分配：
                    总增长 为 总增长 加 记录 的 历史分配 的 第 j 项。
                    j 为 j 加 1。
                完。
                平均 为 总增长 除以 记录 的 分配次数。
                预测 为 平均 乘 3 除以 2。
                若 预测 大于 EXT4_MB_最大连续分配：
                    预测 为 EXT4_MB_最大连续分配。
                完。
                若 预测 大于 请求 的 请求块数：
                    AI_预取命中 为 AI_预取命中 加 1。
                    返还 预测。
                完。
            完。
            做 追加 用 记录 的 历史分配、请求 的 请求块数。
            记录 的 分配次数 为 记录 的 分配次数 加 1。
            若 做 计数 用 记录 的 历史分配 大于 16：
                记录 的 历史分配 为 做 子列表 用 记录 的 历史分配、8、16。
            完。
            返还 请求 的 请求块数。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 AI文件增长表、新建 AI文件增长记录：Nova文件节点号 为 请求 的 Nova文件节点号、历史分配 为 一组：请求 的 请求块数、分配次数 为 1。
    若 做 计数 用 AI文件增长表 大于 1024：
        AI文件增长表 为 做 子列表 用 AI文件增长表、512、1024。
    完。
    返还 请求 的 请求块数。
完。

ext4_获取空闲块数 接受 信息、组号：
    若 组号 大于或等于 信息 的 块组数：
        返还 0。
    完。
    返还 信息 的 组描述符 的 第 组号 项 的 空闲块数_低。
完。

ext4_获取块位图块 接受 信息、组号：
    若 组号 大于或等于 信息 的 块组数：
        返还 0。
    完。
    返还 信息 的 组描述符 的 第 组号 项 的 块位图_低。
完。

ext4_分配统计 接受 零参：
    返还 一本字典：总请求 对应 分配_总请求、总分配块 对应 分配_总分配块、精确命中 对应 分配_精确命中、好伙伴命中 对应 分配_好伙伴命中、任意命中 对应 分配_任意命中、失败 对应 分配_失败、预分配命中 对应 分配_预分配命中、AI预取命中 对应 AI_预取命中、位图缓存大小 对应 做 计数 用 位图缓存、预分配窗口数 对应 做 计数 用 预分配表、AI增长记录 对应 做 计数 用 AI文件增长表。
完。

ext4_分配健康检查 接受 sb：
    信息 为 sb 的 私有信息。
    可变 问题 为 一组：（空）。
    可变 i 为 0。
    可变 高碎片组 为 0。
    只要 i 小于 信息 的 块组数 并且 i 小于 10：
        位图 为 做 ext4_加载位图_缓存 用 sb、i。
        若 位图 不等于 空：
            碎片 为 做 ext4_位图_碎片度 用 位图、信息 的 每组块数。
            若 碎片 大于 EXT4_MB_碎片_阈值：
                高碎片组 为 高碎片组 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    若 高碎片组 大于 5：
        做 追加 用 问题、「高碎片化块组: 」 加 做 整数转字符串 用 高碎片组。
    完。
    若 分配_失败 大于 10：
        做 追加 用 问题、「分配失败: 」 加 做 整数转字符串 用 分配_失败。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「ext4块分配健康」。
    完。
    返还 问题。
完。

本章 收束。
