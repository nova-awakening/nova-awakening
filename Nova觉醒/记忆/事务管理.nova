《事务管理》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·字符串替换》。

引用《.·日志初始化》。

引用《..·中枢·时钟中枢》。

引用《..·中枢·墙钟设置》。

引用《..·中枢·整数转字符串》。

引用《.·崩溃恢复》。

journal_开始事务 接受 零参：
    若 非 journal_已初始化：
        返还 空。
    完。
    tx 为 新建 事务。
    tx 的 事务ID 为 journal_序列号。
    journal_序列号 为 journal_序列号 加 1。
    tx 的 状态 为 事务_运行中。
    tx 的 开始时间 为 做 读取毫秒。
    tx 的 修改列表 为 一组：（空）。
    tx 的 修改块数 为 0。
    tx 的 journal起始块 为 journal_头指针。
    journal_当前事务 为 tx。
    journal_事务总数 为 journal_事务总数 加 1。
    返还 tx。
完。

journal_记录修改 接受 事务、块号、数据：
    若 事务 等于 空 或者 事务 的 状态 不等于 事务_运行中：
        返还。
    完。
    jbuf 为 新建 journal缓冲区。
    jbuf 的 块号 为 块号。
    jbuf 的 数据 为 数据。
    jbuf 的 已脏 为 真。
    jbuf 的 事务ID 为 事务 的 事务ID。
    做 追加 用 事务 的 修改列表、jbuf。
    事务 的 修改块数 为 事务 的 修改块数 加 1。
完。

journal_提交事务 接受 事务：
    若 事务 等于 空 或者 事务 的 状态 不等于 事务_运行中：
        返还 假。
    完。
    事务 的 状态 为 事务_锁定。
    描述符 为 做 创建缓冲 用 journal_块大小。
    做 缓冲写32 用 描述符、0、JBD2_MAGIC。
    做 缓冲写32 用 描述符、4、JBD2_描述符块。
    做 缓冲写32 用 描述符、8、事务 的 事务ID。
    可变 i 为 0。
    只要 i 小于 事务 的 修改块数 并且 i 小于 100：
        做 缓冲写64 用 描述符、16 加 i 乘 8、事务 的 修改列表 的 第 i 项 的 块号。
        i 为 i 加 1。
    完。
    做 journal_写入块 用 描述符。
    事务 的 状态 为 事务_刷写中。
    i 为 0。
    只要 i 小于 事务 的 修改块数：
        做 journal_写入块 用 事务 的 修改列表 的 第 i 项 的 数据。
        journal_写入块数 为 journal_写入块数 加 1。
        i 为 i 加 1。
    完。
    提交 为 做 创建缓冲 用 journal_块大小。
    做 缓冲写32 用 提交、0、JBD2_MAGIC。
    做 缓冲写32 用 提交、4、JBD2_提交块。
    做 缓冲写32 用 提交、8、事务 的 事务ID。
    做 缓冲写32 用 提交、12、做 journal_计算CRC 用 事务。
    做 journal_写入块 用 提交。
    事务 的 状态 为 事务_已提交。
    事务 的 提交时间 为 做 读取毫秒。
    做 追加 用 journal_事务列表、事务。
    若 journal_模式 等于 JOURNAL_MODE_ORDERED：
        做 journal_回写数据 用 事务。
    完。
    返还 真。
完。

journal_写入块 接受 数据：
    物理块 为 journal_超级块 的 起始块 加 1 加 journal_头指针。
    做 块设备_写入 用 journal_设备、物理块、数据。
    journal_头指针 为 (journal_头指针 加 1) 取余于 (journal_总块数 减 1)。
完。

journal_回写数据 接受 事务：
    可变 i 为 0。
    只要 i 小于 事务 的 修改块数：
        jbuf 为 事务 的 修改列表 的 第 i 项。
        做 块设备_写入 用 journal_设备、jbuf 的 块号、jbuf 的 数据。
        i 为 i 加 1。
    完。
完。

journal_checkpoint 接受 零参：
    可变 回收数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 journal_事务列表：
        tx 为 journal_事务列表 的 第 i 项。
        若 tx 的 状态 等于 事务_已提交：
            做 journal_回写数据 用 tx。
            tx 的 状态 为 事务_已检查点。
            回收数 为 回收数 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 新列表 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 journal_事务列表：
        若 journal_事务列表 的 第 i 项 的 状态 不等于 事务_已检查点：
            做 追加 用 新列表、journal_事务列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    journal_事务列表 为 新列表。
    若 做 计数 用 journal_事务列表 大于 0：
        journal_尾指针 为 journal_事务列表 的 第 0 项 的 journal起始块。
    否则：
        journal_尾指针 为 journal_头指针。
    完。
    若 回收数 大于 0：
        做 内核信息 用 「ext4 journal: checkpoint 」 加 做 整数转字符串 用 回收数 加 「 个事务」。
    完。
完。

journal_有序数据_提交前 接受 事务：
    若 事务 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 事务 的 有序数据列表：
        数据块 为 事务 的 有序数据列表 的 第 i 项。
        若 数据块 的 脏：
            做 块设备_同步写 用 journal_设备、数据块 的 块号、数据块 的 缓冲。
            数据块 的 脏 为 假。
        完。
        i 为 i 加 1。
    完。
完。

journal_有序数据_添加 接受 事务、块号、缓冲：
    若 事务 等于 空：
        返还。
    完。
    做 追加 用 事务 的 有序数据列表、一本字典：块号 对应 块号、缓冲 对应 缓冲、脏 对应 真。
完。

事务_超时_秒 为 5。

可变 journal_上次提交时间 为 0。

可变 journal_自动提交数 为 0。

journal_检查超时 接受 零参：
    若 journal_当前事务 等于 空：
        返还。
    完。
    现在 为 做 当前时间_纳秒 除以 1000000000。
    若 现在 减 journal_上次提交时间 大于 事务_超时_秒：
        做 journal_提交当前事务。
        journal_自动提交数 为 journal_自动提交数 加 1。
        journal_上次提交时间 为 现在。
    完。
完。

journal_提交当前事务 接受 零参：
    若 journal_当前事务 等于 空：
        返还。
    完。
    做 journal_有序数据_提交前 用 journal_当前事务。
    journal_提交总数 为 journal_提交总数 加 1。
完。

可变 journal_当前事务 为 空。

可变 journal_事务列表 为 一组：（空）。

可变 journal_序列号 为 1。

可变 journal_超级块 为 空。

可变 journal_头指针 为 0。

可变 journal_尾指针 为 0。

可变 journal_提交总数 为 0。

块设备_同步写 接受 设备、块号、数据：
    若 设备 等于 空 或者 数据 等于 空：
        返还。
    完。
    偏移 为 块号 乘 设备 的 块大小。
    做 设备 的 写操作 用 设备、偏移、数据、设备 的 块大小。
    做 设备 的 同步操作 用 设备。
完。

事务_统计 接受 零参：
    返还 一本字典：提交总数 对应 journal_提交总数、自动提交 对应 journal_自动提交数、当前事务 对应 journal_当前事务 不等于 空、事务列表 对应 做 计数 用 journal_事务列表。
完。

事务_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    若 journal_当前事务 不等于 空 并且 journal_自动提交数 大于 100：
        做 追加 用 问题、「自动提交过多」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「事务管理健康」。
    完。
    返还 问题。
完。

可变 AI_事务管理_故障预警数 为 0。

AI_事务管理_设备健康 接受 错误率、响应时间、温度：
    可变 分 为 1000。
    若 错误率 大于 10：
        分 为 分 减 错误率 乘 5。
    完。
    若 响应时间 大于 1000：
        分 为 分 减 200。
    完。
    若 温度 大于 80：
        分 为 分 减 300。
    完。
    若 分 小于 300：
        AI_事务管理_故障预警数 为 AI_事务管理_故障预警数 加 1。
    完。
    返还 分。
完。

本章 收束。
