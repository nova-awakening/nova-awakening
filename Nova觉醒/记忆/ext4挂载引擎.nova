《ext4挂载引擎》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·同步原语》。

引用《.·ext4超级块读取》。

引用《.·ext4超级块验证》。

引用《.·文件文件》。

引用《..·执行·控制台输出》。

引用《.·ext4卸载超级块》。

引用《..·中枢·字符串替换》。

引用《..·中枢·墙钟设置》。

引用《.·ext4块组描述符》。

引用《..·稳态·AHCI驱动全局》。

EXT4_魔数 为 0xEF53。

EXT4_超级块偏移 为 1024。

EXT4_超级块大小 为 1024。

EXT4_最小块大小 为 1024。

EXT4_最大块大小 为 65536。

EXT4_默认块大小 为 4096。

EXT4_默认文件节点大小 为 256。

EXT4_好旧REV_文件节点大小 为 128。

EXT4_根文件节点号 为 2。

EXT4_好旧REV 为 0。

EXT4_动态REV 为 1。

EXT4_特性_兼容_目录预分配 为 0x0001。

EXT4_特性_兼容_IMAGIC_INODES 为 0x0002。

EXT4_特性_兼容_HAS_JOURNAL 为 0x0004。

EXT4_特性_兼容_扩展属性 为 0x0008。

EXT4_特性_兼容_调整大小 为 0x0010。

EXT4_特性_兼容_目录索引 为 0x0020。

EXT4_特性_兼容_EXTENT树 为 0x0040。

EXT4_特性_兼容_FLEX_BG 为 0x0200。

EXT4_特性_兼容_稀疏超级块2 为 0x0200。

EXT4_特性_不兼容_FILETYPE 为 0x0002。

EXT4_特性_不兼容_RECOVER 为 0x0004。

EXT4_特性_不兼容_JOURNAL_DEV 为 0x0008。

EXT4_特性_不兼容_META_BG 为 0x0010。

EXT4_特性_不兼容_EXTENTS 为 0x0040。

EXT4_特性_不兼容_64位 为 0x0080。

EXT4_特性_不兼容_MMP 为 0x0100。

EXT4_特性_不兼容_FLEX_BG 为 0x0200。

EXT4_特性_不兼容_INLINE_DATA 为 0x8000。

EXT4_特性_不兼容_ENCRYPT 为 0x10000。

EXT4_特性_RO兼容_稀疏超级块 为 0x0001。

EXT4_特性_RO兼容_大文件 为 0x0002。

EXT4_特性_RO兼容_HUGE_FILE 为 0x0008。

EXT4_特性_RO兼容_GDT_CSUM 为 0x0010。

EXT4_特性_RO兼容_DIR_NLINK 为 0x0020。

EXT4_特性_RO兼容_EXTRA_ISIZE 为 0x0040。

EXT4_特性_RO兼容_QUOTA 为 0x0100。

EXT4_特性_RO兼容_BIGALLOC 为 0x0200。

EXT4_特性_RO兼容_METADATA_CSUM 为 0x0400。

EXT4_特性_RO兼容_READONLY 为 0x1000。

EXT4_特性_RO兼容_PROJECT 为 0x2000。

EXT4_有效_FS 为 0x0001。

EXT4_错误_FS 为 0x0002。

EXT4_孤儿_FS 为 0x0004。

EXT4_错误_继续 为 1。

EXT4_错误_只读 为 2。

EXT4_错误_恐慌 为 3。

EXT4_节点标志_SECRM 为 0x00000001。

EXT4_节点标志_UNRM 为 0x00000002。

EXT4_节点标志_COMPR 为 0x00000004。

EXT4_节点标志_SYNC 为 0x00000008。

EXT4_节点标志_IMMUTABLE 为 0x00000010。

EXT4_节点标志_APPEND 为 0x00000020。

EXT4_节点标志_NODUMP 为 0x00000040。

EXT4_节点标志_NOATIME 为 0x00000080。

EXT4_节点标志_INDEX 为 0x00001000。

EXT4_节点标志_JOURNAL_DATA 为 0x00004000。

EXT4_节点标志_DIRSYNC 为 0x00010000。

EXT4_节点标志_TOPDIR 为 0x00020000。

EXT4_节点标志_HUGE_FILE 为 0x00040000。

EXT4_节点标志_EXTENTS 为 0x00080000。

EXT4_节点标志_INLINE_DATA 为 0x10000000。

EXT4_节点标志_ENCRYPT 为 0x00000800。

EXT4_FT_未知 为 0。

EXT4_FT_普通 为 1。

EXT4_FT_目录 为 2。

EXT4_FT_字符设备 为 3。

EXT4_FT_块设备 为 4。

EXT4_FT_管道 为 5。

EXT4_FT_套接字 为 6。

EXT4_FT_符号链接 为 7。

Ext4挂载选项 是 一种 事物，含 数据模式、是否只读、是否无屏障、是否DISCARD、是否无ACLS、是否调试、是否无atime、是否数据校验和、日志提交间隔、最大批量时间、条带宽度、错误行为。

EXT4_数据模式_日志 为 1。

EXT4_数据模式_有序 为 2。

EXT4_数据模式_回写 为 3。

ext4_默认挂载选项 接受 零参：
    可变 选项 为 新建 Ext4挂载选项。
    选项 的 数据模式 为 EXT4_数据模式_有序。
    选项 的 是否只读 为 假。
    选项 的 是否无屏障 为 假。
    选项 的 是否DISCARD 为 假。
    选项 的 是否无ACLS 为 假。
    选项 的 是否调试 为 假。
    选项 的 是否无atime 为 假。
    选项 的 是否数据校验和 为 假。
    选项 的 日志提交间隔 为 5。
    选项 的 最大批量时间 为 15000。
    选项 的 条带宽度 为 0。
    选项 的 错误行为 为 EXT4_错误_继续。
    返还 选项。
完。

ext4_解析挂载选项 接受 选项字符串：
    可变 选项 为 做 ext4_默认挂载选项。
    若 选项字符串 等于 空 或者 做 字节长度 用 选项字符串 等于 0：
        返还 选项。
    完。
    若 做 包含 用 选项字符串、「ro」：
        选项 的 是否只读 为 真。
    完。
    若 做 包含 用 选项字符串、「nobarrier」：
        选项 的 是否无屏障 为 真。
    完。
    若 做 包含 用 选项字符串、「discard」：
        选项 的 是否DISCARD 为 真。
    完。
    若 做 包含 用 选项字符串、「noatime」：
        选项 的 是否无atime 为 真。
    完。
    若 做 包含 用 选项字符串、「data=journal」：
        选项 的 数据模式 为 EXT4_数据模式_日志。
    完。
    若 做 包含 用 选项字符串、「data=writeback」：
        选项 的 数据模式 为 EXT4_数据模式_回写。
    完。
    若 做 包含 用 选项字符串、「errors=remount-ro」：
        选项 的 错误行为 为 EXT4_错误_只读。
    完。
    若 做 包含 用 选项字符串、「errors=panic」：
        选项 的 错误行为 为 EXT4_错误_恐慌。
    完。
    返还 选项。
完。

文件系统类型 是 一种 事物，含 名称、标志、挂载函数、卸载函数、下一个。

可变 ext4文件系统类型 为 新建 文件系统类型。

可变 ext4_已注册 为 假。

ext4_注册文件系统 接受 零参：
    若 ext4_已注册：
        返还 0。
    完。
    ext4文件系统类型 的 名称 为 「ext4」。
    ext4文件系统类型 的 标志 为 0x01。
    ext4文件系统类型 的 挂载函数 为 ext4_挂载。
    ext4文件系统类型 的 卸载函数 为 ext4_卸载超级块。
    结果 为 做 VFS_注册文件系统类型 用 ext4文件系统类型。
    若 结果 等于 0：
        ext4_已注册 为 真。
        做 打印 用 「[ext4] 文件系统类型已注册到VFS」。
    完。
    返还 结果。
完。

ext4_注销文件系统 接受 零参：
    若 非 ext4_已注册：
        返还 0。
    完。
    做 VFS_注销文件系统类型 用 ext4文件系统类型。
    ext4_已注册 为 假。
    做 打印 用 「[ext4] 文件系统类型已注销」。
    返还 0。
完。

ext4_挂载 接受 设备名、挂载点、选项字符串：
    做 打印 用 做 字符串拼接 用 「[ext4] 挂载: 设备=」、设备名。
    做 打印 用 做 字符串拼接 用 「[ext4]   挂载点=」、挂载点。
    选项 为 做 ext4_解析挂载选项 用 选项字符串。
    设备 为 做 块设备_打开 用 设备名。
    若 设备 等于 空：
        做 打印 用 「[ext4] 错误: 无法打开块设备」。
        返还 错误码_设备不存在。
    完。
    可变 sb 为 做 VFS_分配超级块。
    若 sb 等于 空：
        返还 错误码_内存不足。
    完。
    挂载选项标志 为 做 ext4_选项到标志 用 选项。
    结果 为 做 ext4_填充超级块 用 sb、设备、挂载选项标志。
    若 结果 不等于 0：
        做 VFS_释放超级块 用 sb。
        返还 结果。
    完。
    信息 为 sb 的 私有信息。
    若 选项 的 是否只读：
        信息 的 只读 为 真。
    完。
    根节点 为 做 ext4_读文件节点 用 sb、EXT4_根文件节点号。
    若 做 是错误 用 根节点：
        做 ext4_卸载超级块 用 sb。
        返还 根节点。
    完。
    sb 的 根节点 为 根节点。
    做 VFS_添加挂载点 用 sb、挂载点。
    做 打印 用 做 字符串拼接 用 「[ext4] 挂载完成: 」、挂载点。
    返还 0。
完。

ext4_选项到标志 接受 选项：
    可变 标志 为 0。
    若 选项 的 数据模式 等于 EXT4_数据模式_日志：
        标志 为 标志 位或 0x0004。
    否则若 选项 的 数据模式 等于 EXT4_数据模式_回写：
        标志 为 标志 位或 0x0010。
    否则：
        标志 为 标志 位或 0x0008。
    完。
    若 选项 的 是否无屏障：
        标志 为 标志 位或 0x0020。
    完。
    若 选项 的 是否DISCARD：
        标志 为 标志 位或 0x0040。
    完。
    若 选项 的 是否调试：
        标志 为 标志 位或 0x0001。
    完。
    返还 标志。
完。

ext4_重挂载 接受 sb、新选项字符串：
    若 sb 等于 空 或者 sb 的 私有信息 等于 空：
        返还 错误码_无效参数。
    完。
    信息 为 sb 的 私有信息。
    新选项 为 做 ext4_解析挂载选项 用 新选项字符串。
    若 新选项 的 是否只读 并且 非 信息 的 只读：
        做 ext4_同步文件系统 用 sb。
        信息 的 只读 为 真。
        做 打印 用 「[ext4] 重挂载为只读」。
    完。
    若 非 新选项 的 是否只读 并且 信息 的 只读：
        若 信息 的 状态 等于 EXT4_错误_FS：
            做 打印 用 「[ext4] 错误: 文件系统有错误,无法切换到读写模式」。
            返还 错误码_无效参数。
        完。
        若 做 ext4_需要只读挂载 用 信息 的 磁盘超级块：
            做 打印 用 「[ext4] 错误: 不支持的RO兼容特性,无法读写挂载」。
            返还 错误码_无效参数。
        完。
        信息 的 只读 为 假。
        信息 的 状态 为 EXT4_有效_FS。
        做 ext4_写超级块 用 sb 的 设备、信息。
        做 打印 用 「[ext4] 重挂载为读写」。
    完。
    信息 的 挂载选项 为 做 ext4_选项到标志 用 新选项。
    返还 0。
完。

ext4_读备份超级块 接受 设备、块组号、块大小：
    若 非 做 ext4_是备份组 用 块组号：
        返还 错误码_不存在。
    完。
    备份偏移 为 块组号 乘 块大小 乘 8 乘 块大小。
    若 块组号 等于 0：
        备份偏移 为 EXT4_超级块偏移。
    完。
    缓冲 为 做 块设备_读 用 设备、备份偏移、EXT4_超级块大小。
    若 缓冲 等于 空：
        返还 错误码_IO。
    完。
    魔数 为 做 缓冲读16 用 缓冲、0x38。
    若 魔数 不等于 EXT4_魔数：
        返还 错误码_无效超级块。
    完。
    做 打印 用 做 字符串拼接 用 「[ext4] 读取备份超级块: 块组」、做 转字符串 用 块组号。
    返还 缓冲。
完。

ext4_是备份组 接受 块组号：
    若 块组号 等于 0：
        返还 真。
    完。
    若 块组号 等于 1：
        返还 真。
    完。
    可变 n 为 块组号。
    只要 n 大于 1 并且 n 取余于 3 等于 0：
        n 为 n 除以 3。
    完。
    若 n 等于 1：
        返还 真。
    完。
    n 为 块组号。
    只要 n 大于 1 并且 n 取余于 5 等于 0：
        n 为 n 除以 5。
    完。
    若 n 等于 1：
        返还 真。
    完。
    n 为 块组号。
    只要 n 大于 1 并且 n 取余于 7 等于 0：
        n 为 n 除以 7。
    完。
    若 n 等于 1：
        返还 真。
    完。
    返还 假。
完。

原生解析统计 是 一种 事物，含 注册次数、注销次数、挂载次数、挂载成功、挂载失败、重挂载次数、备份超级块读取次数、备份超级块成功、选项解析次数。

可变 原生解析统计数据 为 新建 原生解析统计。

ext4_原生_打印统计 接受 零参：
    做 打印 用 「[ext4原生] ═══ 统计信息 ═══」。
    做 打印 用 做 字符串拼接 用 「  注册次数:         」、做 转字符串 用 原生解析统计数据 的 注册次数。
    做 打印 用 做 字符串拼接 用 「  挂载次数:         」、做 转字符串 用 原生解析统计数据 的 挂载次数。
    做 打印 用 做 字符串拼接 用 「  挂载成功:         」、做 转字符串 用 原生解析统计数据 的 挂载成功。
    做 打印 用 做 字符串拼接 用 「  挂载失败:         」、做 转字符串 用 原生解析统计数据 的 挂载失败。
    做 打印 用 做 字符串拼接 用 「  重挂载次数:       」、做 转字符串 用 原生解析统计数据 的 重挂载次数。
    做 打印 用 做 字符串拼接 用 「  备份超级块读取:   」、做 转字符串 用 原生解析统计数据 的 备份超级块读取次数。
    做 打印 用 做 字符串拼接 用 「  选项解析次数:     」、做 转字符串 用 原生解析统计数据 的 选项解析次数。
    返还 0。
完。

ext4_原生_获取统计 接受 零参：
    返还 原生解析统计数据。
完。

ext4_模块初始化 接受 零参：
    做 打印 用 「[ext4] ═══ ext4文件系统模块初始化 ═══」。
    做 打印 用 「[ext4]   版本: NovaOS ext4 1.0」。
    做 打印 用 「[ext4]   支持特性: extents/64bit/flex_bg/filetype/recover」。
    结果 为 做 ext4_注册文件系统。
    若 结果 等于 0：
        做 打印 用 「[ext4]   文件系统类型注册成功」。
    否则：
        做 打印 用 「[ext4]   警告: 文件系统类型注册失败」。
    完。
    返还 结果。
完。

ext4_模块清理 接受 零参：
    做 打印 用 「[ext4] ext4文件系统模块清理」。
    做 ext4_注销文件系统。
    返还 0。
完。

ext4_写备份超级块 接受 sb：
    若 sb 等于 空 或者 sb 的 私有信息 等于 空：
        返还 错误码_无效参数。
    完。
    信息 为 sb 的 私有信息。
    若 信息 的 只读：
        返还 0。
    完。
    可变 已写 为 0。
    可变 组号 为 1。
    只要 组号 小于 信息 的 块组数 并且 已写 小于 10：
        若 做 ext4_是备份组 用 组号：
            备份偏移 为 组号 乘 信息 的 每组块数 乘 信息 的 块大小。
            结果 为 做 ext4_写超级块_到偏移 用 sb 的 设备、信息、备份偏移。
            若 结果 等于 0：
                已写 为 已写 加 1。
            完。
        完。
        组号 为 组号 加 1。
    完。
    做 打印 用 做 字符串拼接 用 「[ext4] 备份超级块已写入: 」、做 转字符串 用 已写。
    返还 0。
完。

ext4_写超级块_到偏移 接受 设备、信息、偏移：
    可变 缓冲 为 做 创建缓冲 用 EXT4_超级块大小。
    做 缓冲写32 用 缓冲、0x00、信息 的 磁盘超级块 的 文件节点总数。
    做 缓冲写32 用 缓冲、0x04、信息 的 块总数 位与 0xFFFFFFFF。
    做 缓冲写32 用 缓冲、0x0C、信息 的 空闲块数 位与 0xFFFFFFFF。
    做 缓冲写32 用 缓冲、0x10、信息 的 空闲文件节点数。
    做 缓冲写16 用 缓冲、0x38、EXT4_魔数。
    做 缓冲写16 用 缓冲、0x3A、信息 的 状态。
    做 缓冲写32 用 缓冲、0x30、做 当前时间_秒。
    返还 做 块设备_写 用 设备、偏移、缓冲、EXT4_超级块大小。
完。

ext4_在线调整大小 接受 sb、新块数：
    若 sb 等于 空 或者 sb 的 私有信息 等于 空：
        返还 错误码_无效参数。
    完。
    信息 为 sb 的 私有信息。
    若 信息 的 只读：
        做 打印 用 「[ext4] 错误: 只读模式下无法调整大小」。
        返还 错误码_只读文件系统。
    完。
    若 新块数 小于或等于 信息 的 块总数：
        做 打印 用 「[ext4] 错误: 收缩文件系统暂不支持」。
        返还 错误码_不支持。
    完。
    当前块组数 为 信息 的 块组数。
    新块组数 为 (新块数 减 信息 的 磁盘超级块 的 首数据块 加 信息 的 每组块数 减 1) 除以 信息 的 每组块数。
    若 新块组数 小于或等于 当前块组数：
        信息 的 块总数 为 新块数。
        信息 的 空闲块数 为 信息 的 空闲块数 加 (新块数 减 信息 的 块总数)。
        做 ext4_写超级块 用 sb 的 设备、信息。
        做 打印 用 做 字符串拼接 用 「[ext4] 扩展最后块组, 新总块数: 」、做 转字符串 用 新块数。
        返还 0。
    完。
    做 打印 用 做 字符串拼接 用 「[ext4] 需要添加块组: 」、做 转字符串 用 新块组数 减 当前块组数。
    做 打印 用 「[ext4] 在线resize扩展块组: 暂不支持完整实现」。
    返还 错误码_不支持。
完。

ext4_获取文件系统信息字符串 接受 sb：
    若 sb 等于 空 或者 sb 的 私有信息 等于 空：
        返还 「无效」。
    完。
    信息 为 sb 的 私有信息。
    可变 结果 为 「ext4: 」。
    结果 为 做 字符串拼接 用 结果、做 字符串拼接 用 「块大小=」、做 转字符串 用 信息 的 块大小。
    结果 为 做 字符串拼接 用 结果、做 字符串拼接 用 「 块组=」、做 转字符串 用 信息 的 块组数。
    结果 为 做 字符串拼接 用 结果、做 字符串拼接 用 「 总块=」、做 转字符串 用 信息 的 块总数。
    结果 为 做 字符串拼接 用 结果、做 字符串拼接 用 「 空闲=」、做 转字符串 用 信息 的 空闲块数。
    若 信息 的 只读：
        结果 为 做 字符串拼接 用 结果、「 [只读]」。
    完。
    返还 结果。
完。

ext4_获取使用率百分比 接受 sb：
    若 sb 等于 空 或者 sb 的 私有信息 等于 空：
        返还 0。
    完。
    信息 为 sb 的 私有信息。
    若 信息 的 块总数 等于 0：
        返还 0。
    完。
    已用 为 信息 的 块总数 减 信息 的 空闲块数。
    返还 已用 乘 100 除以 信息 的 块总数。
完。

可变 AI_原生系统扩_热文件表 为 一组：（空）。

AI_原生系统扩_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_原生系统扩_热文件表：
        若 AI_原生系统扩_热文件表 的 第 i 项 的 路径 等于 路径：
            AI_原生系统扩_热文件表 的 第 i 项 的 频率 为 AI_原生系统扩_热文件表 的 第 i 项 的 频率 加 1。
            返还 AI_原生系统扩_热文件表 的 第 i 项 的 频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_原生系统扩_热文件表 小于 128：
        做 追加 用 AI_原生系统扩_热文件表、一本字典：路径 对应 路径、频率 对应 1。
    完。
    返还 1。
完。

AI_原生系统扩_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
        返还 0。
    完。
    返还 32768。
完。

原生系统扩展_统计 接受 零参：
    返还 一本字典：模块 对应 「原生系统扩展」、ext4文件系统类型 对应 ext4文件系统类型、ext4_已注册 对应 ext4_已注册、原生解析统计数据 对应 原生解析统计数据、AI_原生系统扩_热文件表 对应 做 计数 用 AI_原生系统扩_热文件表。
完。

原生系统扩展_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 原生解析统计数据 大于 100000：
        做 追加 用 问题、「原生解析统计数据异常增长」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

原生系统扩展_综合报告 接受 零参：
    做 打印 用 「[原生系统扩展] ═══ 综合报告 ═══」。
    做 打印 用 「  ext4文件系统类型: 」 加 若 ext4文件系统类型 不等于 空 则 「已初始化」 否则 「未初始化」。
    做 打印 用 「  ext4_已注册: 」 加 若 ext4_已注册 则 「是」 否则 「否」。
    做 打印 用 「  原生解析统计数据: 」 加 若 原生解析统计数据 不等于 空 则 「已初始化」 否则 「未初始化」。
    做 打印 用 「  AI_原生系统扩_热文件表: 」 加 做 整数转字符串 用 做 计数 用 AI_原生系统扩_热文件表。
    做 打印 用 「  健康: 」 加 若 做 原生系统扩展_健康评估 则 「正常」 否则 「异常」。
完。

原生系统扩展_全局重置 接受 零参：
    ext4_已注册 为 假。
    AI_原生系统扩_热文件表 为 一组：（空）。
    做 打印 用 「[原生系统扩展] 全局统计已重置」。
    返还 0。
完。

本章 收束。
