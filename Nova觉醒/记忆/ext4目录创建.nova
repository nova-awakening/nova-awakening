《ext4目录创建》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《.·ext4目录项查找》。

引用《.·ext4块分配器》。

引用《.·ext4_extent插入》。

引用《.·ext4目录删除》。

引用《.·ext4读节点》。

引用《..·中枢·哈希函数》。

引用《.·ext4卸载超级块》。

引用《..·稳态·AHCI驱动全局》。

引用《.·ext4目录遍历》。

引用《.·ext4_htree》。

引用《..·中枢·字符串替换》。

ext4_创建目录 接受 sb、父Nova文件节点、名称、模式：
    新Nova文件节点 为 做 ext4_分配Nova文件节点 用 sb、父Nova文件节点、EXT4_S_IFDIR 位或 模式 位与 0x0FFF。
    若 做 是错误 用 新Nova文件节点：
        返还 新Nova文件节点。
    完。
    新Nova文件节点 的 链接数 为 2。
    做 ext4_初始化目录块 用 sb、新Nova文件节点、父Nova文件节点 的 编号。
    结果 为 做 ext4_添加目录项 用 sb、父Nova文件节点、名称、新Nova文件节点 的 编号、EXT4_FT_目录。
    若 结果 不等于 0：
        做 ext4_释放Nova文件节点 用 sb、新Nova文件节点 的 编号。
        返还 结果。
    完。
    父Nova文件节点 的 链接数 为 父Nova文件节点 的 链接数 加 1。
    返还 新Nova文件节点。
完。

ext4_初始化目录块 接受 sb、目录Nova文件节点、父Nova文件节点号：
    信息 为 sb 的 私有信息。
    块大小 为 信息 的 块大小。
    ext4信息 为 目录Nova文件节点 的 私有信息。
    物理块 为 做 ext4_分配块 用 sb、1。
    若 物理块 等于 0：
        返还。
    完。
    做 ext4_extent_插入 用 sb、ext4信息、0、物理块、1。
    目录Nova文件节点 的 大小 为 块大小。
    可变 缓冲 为 做 分配内存 用 块大小。
    做 内存置零 用 缓冲、块大小。
    点记录长度 为 12。
    做 ext4_写入目录项 用 缓冲、0、目录Nova文件节点 的 编号、「.」、EXT4_FT_目录、点记录长度。
    点点记录长度 为 块大小 减 点记录长度。
    做 ext4_写入目录项 用 缓冲、点记录长度、父Nova文件节点号、「..」、EXT4_FT_目录、点点记录长度。
    做 块设备_写 用 sb 的 设备、物理块 乘 块大小、缓冲、块大小。
    做 释放内存 用 缓冲。
完。

ext4_删除目录 接受 sb、父Nova文件节点、名称：
    项 为 做 ext4_查找目录项 用 sb、父Nova文件节点、名称。
    若 项 等于 空：
        返还 错误码_不存在。
    完。
    子Nova文件节点 为 做 ext4_读Nova文件节点 用 sb、项 的 Nova文件节点号。
    若 做 是错误 用 子Nova文件节点：
        返还 子Nova文件节点。
    完。
    若 非 做 ext4_目录为空 用 sb、子Nova文件节点：
        返还 错误码_目录非空。
    完。
    做 ext4_删除目录项 用 sb、父Nova文件节点、名称。
    子Nova文件节点 的 链接数 为 0。
    父Nova文件节点 的 链接数 为 父Nova文件节点 的 链接数 减 1。
    返还 0。
完。

ext4_目录为空 接受 sb、目录Nova文件节点：
    信息 为 sb 的 私有信息。
    块大小 为 信息 的 块大小。
    ext4信息 为 目录Nova文件节点 的 私有信息。
    可变 偏移 为 0。
    只要 偏移 小于 目录Nova文件节点 的 大小：
        逻辑块 为 偏移 除以 块大小。
        物理块 为 做 ext4_extent_查找 用 sb、ext4信息、逻辑块。
        若 物理块 等于 0 减 1：
            偏移 为 偏移 加 块大小。
            继续。
        完。
        缓冲 为 做 块设备_读 用 sb 的 设备、物理块 乘 块大小、块大小。
        若 缓冲 等于 空：
            偏移 为 偏移 加 块大小。
            继续。
        完。
        可变 块内偏移 为 0。
        只要 块内偏移 小于 块大小：
            项 为 做 ext4_解析目录项 用 缓冲、块内偏移。
            若 项 的 记录长度 等于 0：
                跳出。
            完。
            若 项 的 Nova文件节点号 不等于 0：
                名 为 做 ext4_提取目录项名称 用 项。
                若 名 不等于 「.」 并且 名 不等于 「..」：
                    做 缓冲_释放 用 缓冲。
                    返还 假。
                完。
            完。
            块内偏移 为 块内偏移 加 项 的 记录长度。
        完。
        做 缓冲_释放 用 缓冲。
        偏移 为 偏移 加 块大小。
    完。
    返还 真。
完。

本章 收束。
