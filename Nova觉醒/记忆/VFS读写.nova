《VFS读写》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·内存回收》。

引用《.·VFS读写同步》。

引用《.·文件节点创建》。

SEEK_SET 为 0。

SEEK_CUR 为 1。

SEEK_END 为 2。

可变 VFS_读次数 为 0。

可变 VFS_写次数 为 0。

可变 VFS_读字节 为 0。

可变 VFS_写字节 为 0。

vfs_read 接受 文件、缓冲区、大小：
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    若 大小 等于 0：
        返还 0。
    完。
    标志 为 文件 的 第 F_标志 项。
    若 标志 位与 O_WRONLY 不等于 0 并且 标志 位与 O_RDWR 等于 0：
        返还 错误_坏文件描述符。
    完。
    节点 为 文件 的 第 F_Nova文件节点 项。
    若 节点 等于 空：
        返还 错误_坏文件描述符。
    完。
    操作 为 文件 的 第 F_操作 项。
    若 操作 等于 空 或者 操作 的 读 等于 空：
        返还 错误_无效参数。
    完。
    位置 为 文件 的 第 F_偏移 项。
    文件大小 为 节点 的 第 I_大小 项。
    若 位置 大于或等于 文件大小：
        返还 0。
    完。
    若 位置 加 大小 大于 文件大小：
        大小 为 文件大小 减 位置。
    完。
    已读 为 做 操作 的 读 用 文件、缓冲区、大小、位置。
    若 已读 大于 0：
        做 设置 用 文件、F_偏移、位置 加 已读。
        做 Nova文件节点_更新访问时间 用 节点。
        VFS_读次数 为 VFS_读次数 加 1。
        VFS_读字节 为 VFS_读字节 加 已读。
    完。
    返还 已读。
完。

vfs_write 接受 文件、数据、大小：
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    若 大小 等于 0：
        返还 0。
    完。
    标志 为 文件 的 第 F_标志 项。
    若 标志 位与 O_WRONLY 等于 0 并且 标志 位与 O_RDWR 等于 0：
        返还 错误_坏文件描述符。
    完。
    节点 为 文件 的 第 F_Nova文件节点 项。
    若 节点 等于 空：
        返还 错误_坏文件描述符。
    完。
    操作 为 文件 的 第 F_操作 项。
    若 操作 等于 空 或者 操作 的 写 等于 空：
        返还 错误_无效参数。
    完。
    可变 位置 为 文件 的 第 F_偏移 项。
    若 标志 位与 O_APPEND 不等于 0：
        位置 为 节点 的 第 I_大小 项。
    完。
    已写 为 做 操作 的 写 用 文件、数据、大小、位置。
    若 已写 大于 0：
        做 设置 用 文件、F_偏移、位置 加 已写。
        若 位置 加 已写 大于 节点 的 第 I_大小 项：
            做 Nova文件节点_设置大小 用 节点、位置 加 已写。
        完。
        做 Nova文件节点_标记脏 用 节点。
        做 Nova文件节点_更新修改时间 用 节点。
        VFS_写次数 为 VFS_写次数 加 1。
        VFS_写字节 为 VFS_写字节 加 已写。
        做 回写_平衡脏页。
    完。
    返还 已写。
完。

vfs_lseek 接受 文件、偏移、起点：
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    节点 为 文件 的 第 F_Nova文件节点 项。
    可变 新位置 为 0。
    若 起点 等于 SEEK_SET：
        新位置 为 偏移。
    否则若 起点 等于 SEEK_CUR：
        新位置 为 文件 的 第 F_偏移 项 加 偏移。
    否则若 起点 等于 SEEK_END：
        新位置 为 节点 的 第 I_大小 项 加 偏移。
    否则：
        返还 错误_无效参数。
    完。
    若 新位置 小于 0：
        返还 错误_无效参数。
    完。
    做 设置 用 文件、F_偏移、新位置。
    返还 新位置。
完。

vfs_pread 接受 文件、缓冲区、大小、位置：
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    操作 为 文件 的 第 F_操作 项。
    若 操作 等于 空 或者 操作 的 读 等于 空：
        返还 错误_无效参数。
    完。
    节点 为 文件 的 第 F_Nova文件节点 项。
    若 位置 大于或等于 节点 的 第 I_大小 项：
        返还 0。
    完。
    若 位置 加 大小 大于 节点 的 第 I_大小 项：
        大小 为 节点 的 第 I_大小 项 减 位置。
    完。
    返还 做 操作 的 读 用 文件、缓冲区、大小、位置。
完。

vfs_pwrite 接受 文件、数据、大小、位置：
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    操作 为 文件 的 第 F_操作 项。
    若 操作 等于 空 或者 操作 的 写 等于 空：
        返还 错误_无效参数。
    完。
    已写 为 做 操作 的 写 用 文件、数据、大小、位置。
    若 已写 大于 0：
        节点 为 文件 的 第 F_Nova文件节点 项。
        若 位置 加 已写 大于 节点 的 第 I_大小 项：
            做 Nova文件节点_设置大小 用 节点、位置 加 已写。
        完。
        做 Nova文件节点_标记脏 用 节点。
    完。
    返还 已写。
完。

vfs_readv 接受 文件、iov列表：
    可变 总读 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 iov列表：
        缓冲 为 iov列表 的 第 i 项 的 第 0 项。
        大小 为 iov列表 的 第 i 项 的 第 1 项。
        已读 为 做 vfs_read 用 文件、缓冲、大小。
        若 已读 小于 0：
            若 总读 大于 0：
                返还 总读。
            完。
            返还 已读。
        完。
        总读 为 总读 加 已读。
        若 已读 小于 大小：
            中断。
        完。
        i 为 i 加 1。
    完。
    返还 总读。
完。

vfs_writev 接受 文件、iov列表：
    可变 总写 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 iov列表：
        数据 为 iov列表 的 第 i 项 的 第 0 项。
        大小 为 iov列表 的 第 i 项 的 第 1 项。
        已写 为 做 vfs_write 用 文件、数据、大小。
        若 已写 小于 0：
            若 总写 大于 0：
                返还 总写。
            完。
            返还 已写。
        完。
        总写 为 总写 加 已写。
        若 已写 小于 大小：
            中断。
        完。
        i 为 i 加 1。
    完。
    返还 总写。
完。

可变 AI_虚文件信号_热文件表 为 一组：（空）。

AI_虚文件信号_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_虚文件信号_热文件表：
        若 AI_虚文件信号_热文件表 的 第 i 项 的 路径 等于 路径：
            AI_虚文件信号_热文件表 的 第 i 项 的 频率 为 AI_虚文件信号_热文件表 的 第 i 项 的 频率 加 1。
            返还 AI_虚文件信号_热文件表 的 第 i 项 的 频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_虚文件信号_热文件表 小于 128：
        做 追加 用 AI_虚文件信号_热文件表、一本字典：路径 对应 路径、频率 对应 1。
    完。
    返还 1。
完。

AI_虚文件信号_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
        返还 0。
    完。
    返还 32768。
完。

本章 收束。
