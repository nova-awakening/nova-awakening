《文件节点缓存》

引用《..·中枢·类型定义》。
引用《..·中枢·错误码》。
引用《..·中枢·时钟中枢》。
引用《..·中枢·同步原语》。
引用《..·中枢·双向链表操作》。
引用《..·中枢·哈希整数》。

    编号。 ※ i_ino: Nova文件节点号。
    模式。 ※ i_mode: 做 类型 用 高4位 + 做 权限 用 低12位。
    uid。 ※ i_uid。
    gid。 ※ i_gid。
    大小。 ※ i_size: 做 文件大小 用 字节。
    块数。 ※ i_blocks: 512字节块数。
    块位移。 ※ i_blkbits: 做 块大小位移 用 通常12=4KB。
    链接数。 ※ i_nlink: 硬链接数。
    访问时间秒。 ※ i_atime_sec。
    修改时间秒。 ※ i_mtime_sec (数据修改)。
    变更时间秒。 ※ i_ctime_sec (元数据变更)。
    访问时间纳秒。
    修改时间纳秒。
    变更时间纳秒。
    设备号。 ※ i_rdev。
    i_op。 ※ 做 Nova文件节点操作 用 lookup/mkdir/unlink...。
    i_fop。 ※ 做 文件操作 用 read/write/llseek...。
    超级块。 ※ i_sb。
    引用计数。 ※ i_count: 打开引用。
    脏。 ※ I_DIRTY。
    正在写回。 ※ I_WRITEBACK。
    已释放。 ※ I_FREEING。
    锁。 ※ i_lock。
    哈希链。 ※ i_hash: Nova文件节点缓存哈希链。
    LRU链。 ※ i_lru: LRU淘汰链。
    超级块链。 ※ i_sb_list。
    映射。 ※ i_mapping → address_space。
    私有数据。 ※ i_private: FS专用数据。
完。

※ Nova文件节点缓存。
※ 用(超级块, ino)做哈希键快速查找。

Nova文件节点_HASH_BITS 为 12。
Nova文件节点_HASH_SIZE 为 4096
可变 Nova文件节点哈希表 为 空。
可变 Nova文件节点_LRU 为 空
可变 Nova文件节点_LRU数 为 0。
可变 下一个Nova文件节点号 为 1。
可变 Nova文件节点总数 为 0。
可变 Nova文件节点_Nova块缓存缓存 为 空

Nova文件节点缓存_初始化 接受 零参：
    Nova文件节点哈希表 为 做 哈希表_创建 用 Nova文件节点_HASH_SIZE。
    Nova文件节点_LRU 为 做 链表_创建。
    ※ Nova文件节点_Nova块缓存缓存 = kmem_cache_create(「Nova文件节点_cache」, Nova文件节点结构大小, 64, Nova块缓存_HWCACHE_ALIGN)。
    做 内核信息 用 「Nova文件节点缓存初始化」。
完。

Nova文件节点哈希键 接受 超级块、编号：
    返还 (超级块 乘 31 加 编号) 取余于 Nova文件节点_HASH_SIZE。
完。

※ Nova文件节点创建与分配。

Nova文件节点_分配 接受 零参：
    ※ 从Nova块缓存缓存分配Nova文件节点结构。
    可变 节点 为 Nova文件节点 {}。
    节点.编号 为 下一个Nova文件节点号。
    下一个Nova文件节点号 为 下一个Nova文件节点号 加 1。
    节点.引用计数 为 1。
    节点.链接数 为 0。
    节点.脏 为 假。
    节点.正在写回 为 假。
    节点.已释放 为 假。
    节点.锁 为 做 自旋锁_创建。
    节点.块位移 为 12
    Nova文件节点总数 为 Nova文件节点总数 加 1。
    返还 节点。
完。

Nova文件节点_创建 接受 类型_和_权限、额外权限：
    可变 节点 为 做 Nova文件节点_分配。
    节点.模式 为 类型_和_权限 位或 额外权限。
    节点.链接数 为 1。
    ※ 设置时间戳为当前时间。
    当前时间 为 做 系统时间_获取秒。
    节点.访问时间秒 为 当前时间。
    节点.修改时间秒 为 当前时间。
    节点.变更时间秒 为 当前时间。
    返还 节点。

本章 收束。

本章 收束。

本章 收束。
