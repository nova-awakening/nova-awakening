《VFS读写同步》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·日志中枢》。

引用《..·中枢·同步原语》。

引用《..·中枢·进程调度》。

引用《..·中枢·内存虚拟》。

引用《..·感知·文档事件》。

引用《.·文件文件》。

引用《..·中枢·公共智能》。

引用《..·引导·引导加载器》。

引用《..·中枢·页缓存标记脏》。

引用《..·中枢·墙钟设置》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·块内存分配器》。

引用《..·中枢·读取调用》。

引用《..·引导·端口IO》。

引用《..·中枢·时钟中枢》。

引用《..·中枢·页缓存与地址空间》。

F_Nova文件节点 为 0。

F_偏移 为 1。

F_标志 为 2。

F_引用 为 3。

F_操作 为 4。

F_目录项 为 5。

F_挂载点 为 6。

F_预读状态 为 7。

I_大小 为 0。

I_模式 为 1。

I_UID 为 2。

I_GID 为 3。

I_链接数 为 4。

I_操作 为 5。

I_块数 为 6。

I_时间 为 7。

I_地址空间 为 8。

I_脏标志 为 9。

I_设备号 为 10。

O_RDONLY 为 0。

O_WRONLY 为 1。

O_RDWR 为 2。

O_CREAT 为 0x40。

O_TRUNC 为 0x200。

O_APPEND 为 0x400。

O_NONBLOCK 为 0x800。

O_SYNC 为 0x101000。

O_DIRECT 为 0x4000。

页大小 为 4096。

预读_最小窗口 为 4。

预读_最大窗口 为 128。

回写_间隔_毫秒 为 5000。

回写_脏页阈值 为 1024。

可变 VFS_读次数 为 0。

可变 VFS_写次数 为 0。

可变 VFS_读字节 为 0。

可变 VFS_写字节 为 0。

可变 VFS_fsync次数 为 0。

可变 VFS_truncate次数 为 0。

可变 VFS_页缓存命中 为 0。

可变 VFS_页缓存未命中 为 0。

可变 VFS_预读触发 为 0。

可变 VFS_回写页数 为 0。

可变 VFS_直接IO次数 为 0。

预读状态 是 一种 事物，含 上次偏移、当前窗口、连续计数、是顺序、预取页数。

预读状态_创建 接受 零参：
    返还 新建 预读状态：上次偏移 为 0、当前窗口 为 预读_最小窗口、连续计数 为 0、是顺序 为 假、预取页数 为 0。
完。

vfs_read 接受 文件、用户缓冲、大小：
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    若 大小 小于或等于 0：
        返还 0。
    完。
    节点 为 文件 的 第 F_Nova文件节点 项。
    若 节点 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件大小 为 节点 的 第 I_大小 项。
    偏移 为 文件 的 第 F_偏移 项。
    若 偏移 大于或等于 文件大小：
        返还 0。
    完。
    可变 实际大小 为 大小。
    若 偏移 加 实际大小 大于 文件大小：
        实际大小 为 文件大小 减 偏移。
    完。
    若 文件 的 第 F_标志 项 位与 O_DIRECT：
        结果 为 做 vfs_直接读 用 文件、用户缓冲、实际大小、偏移。
        VFS_直接IO次数 为 VFS_直接IO次数 加 1。
        返还 结果。
    完。
    地址空间 为 节点 的 第 I_地址空间 项。
    可变 已读 为 0。
    只要 已读 小于 实际大小：
        页索引 为 (偏移 加 已读) 除以 页大小。
        页内偏移 为 (偏移 加 已读) 取余于 页大小。
        本次大小 为 页大小 减 页内偏移。
        若 本次大小 大于 实际大小 减 已读：
            本次大小 为 实际大小 减 已读。
        完。
        页 为 做 页缓存_查找 用 地址空间、页索引。
        若 页 不等于 空：
            VFS_页缓存命中 为 VFS_页缓存命中 加 1。
        否则：
            VFS_页缓存未命中 为 VFS_页缓存未命中 加 1。
            页 为 做 页缓存_创建并读入 用 地址空间、页索引、节点。
            若 页 等于 空：
                跳出。
            完。
            做 vfs_预读检测 用 文件、偏移 加 已读、地址空间、节点。
        完。
        做 内存拷贝 用 用户缓冲 加 已读、页 加 页内偏移、本次大小。
        已读 为 已读 加 本次大小。
    完。
    文件 的 第 F_偏移 项 为 偏移 加 已读。
    做 Nova文件节点_更新访问时间 用 节点。
    VFS_读次数 为 VFS_读次数 加 1。
    VFS_读字节 为 VFS_读字节 加 已读。
    返还 已读。
完。

vfs_write 接受 文件、用户缓冲、大小：
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    若 大小 小于或等于 0：
        返还 0。
    完。
    节点 为 文件 的 第 F_Nova文件节点 项。
    若 节点 等于 空：
        返还 错误_坏文件描述符。
    完。
    可变 偏移 为 文件 的 第 F_偏移 项。
    若 文件 的 第 F_标志 项 位与 O_APPEND：
        偏移 为 节点 的 第 I_大小 项。
    完。
    若 文件 的 第 F_标志 项 位与 O_DIRECT：
        结果 为 做 vfs_直接写 用 文件、用户缓冲、大小、偏移。
        VFS_直接IO次数 为 VFS_直接IO次数 加 1。
        返还 结果。
    完。
    地址空间 为 节点 的 第 I_地址空间 项。
    可变 已写 为 0。
    只要 已写 小于 大小：
        页索引 为 (偏移 加 已写) 除以 页大小。
        页内偏移 为 (偏移 加 已写) 取余于 页大小。
        本次大小 为 页大小 减 页内偏移。
        若 本次大小 大于 大小 减 已写：
            本次大小 为 大小 减 已写。
        完。
        可变 页 为 做 页缓存_查找 用 地址空间、页索引。
        若 页 等于 空：
            页 为 做 页缓存_分配 用 地址空间、页索引。
            若 页 等于 空：
                跳出。
            完。
            若 页内偏移 大于 0 或者 本次大小 小于 页大小：
                做 页缓存_读入 用 地址空间、页索引、节点、页。
            完。
        完。
        做 内存拷贝 用 页 加 页内偏移、用户缓冲 加 已写、本次大小。
        做 页缓存_标记脏 用 地址空间、页索引。
        已写 为 已写 加 本次大小。
    完。
    若 偏移 加 已写 大于 节点 的 第 I_大小 项：
        做 Nova文件节点_设置大小 用 节点、偏移 加 已写。
    完。
    文件 的 第 F_偏移 项 为 偏移 加 已写。
    做 Nova文件节点_更新修改时间 用 节点。
    做 Nova文件节点_标记脏 用 节点。
    若 文件 的 第 F_标志 项 位与 O_SYNC：
        做 vfs_fsync 用 文件。
    完。
    VFS_写次数 为 VFS_写次数 加 1。
    VFS_写字节 为 VFS_写字节 加 已写。
    返还 已写。
完。

vfs_fsync 接受 文件：
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    VFS_fsync次数 为 VFS_fsync次数 加 1。
    操作 为 文件 的 第 F_操作 项。
    若 操作 不等于 空 并且 操作 的 fsync 不等于 空：
        返还 做 操作 的 fsync 用 文件。
    完。
    节点 为 文件 的 第 F_Nova文件节点 项。
    若 节点 不等于 空 并且 节点 的 第 I_地址空间 项 不等于 空：
        做 页缓存_同步 用 节点 的 第 I_地址空间 项。
    完。
    返还 成功。
完。

vfs_fdatasync 接受 文件：
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    节点 为 文件 的 第 F_Nova文件节点 项。
    若 节点 不等于 空 并且 节点 的 第 I_地址空间 项 不等于 空：
        做 页缓存_同步 用 节点 的 第 I_地址空间 项。
    完。
    返还 成功。
完。

vfs_truncate 接受 文件、新大小：
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    节点 为 文件 的 第 F_Nova文件节点 项。
    若 节点 等于 空：
        返还 错误_坏文件描述符。
    完。
    VFS_truncate次数 为 VFS_truncate次数 加 1。
    旧大小 为 节点 的 第 I_大小 项。
    做 Nova文件节点_设置大小 用 节点、新大小。
    若 新大小 小于 旧大小：
        操作 为 节点 的 第 I_操作 项。
        若 操作 不等于 空 并且 操作 的 截断 不等于 空：
            做 操作 的 截断 用 节点、新大小。
        完。
        若 节点 的 第 I_地址空间 项 不等于 空：
            做 页缓存_截断 用 节点 的 第 I_地址空间 项、新大小。
        完。
    否则若 新大小 大于 旧大小：
    完。
    做 Nova文件节点_标记脏 用 节点。
    返还 成功。
完。

vfs_预读检测 接受 文件、当前偏移、地址空间、节点：
    若 文件 的 第 F_预读状态 项 等于 空：
        文件 的 第 F_预读状态 项 为 做 预读状态_创建。
    完。
    ra 为 文件 的 第 F_预读状态 项。
    期望偏移 为 ra 的 上次偏移 加 页大小。
    若 当前偏移 大于或等于 期望偏移 减 页大小 并且 当前偏移 小于或等于 期望偏移 加 页大小：
        ra 的 连续计数 为 ra 的 连续计数 加 1。
        ra 的 是顺序 为 ra 的 连续计数 大于 2。
    否则：
        ra 的 连续计数 为 0。
        ra 的 是顺序 为 假。
        ra 的 当前窗口 为 预读_最小窗口。
    完。
    ra 的 上次偏移 为 当前偏移。
    若 ra 的 是顺序：
        VFS_预读触发 为 VFS_预读触发 加 1。
        预读起始 为 当前偏移 除以 页大小 加 1。
        可变 i 为 0。
        只要 i 小于 ra 的 当前窗口：
            页索引 为 预读起始 加 i。
            若 做 页缓存_查找 用 地址空间、页索引 等于 空：
                做 页缓存_创建并读入 用 地址空间、页索引、节点。
                ra 的 预取页数 为 ra 的 预取页数 加 1。
            完。
            i 为 i 加 1。
        完。
        ra 的 当前窗口 为 ra 的 当前窗口 乘 2。
        若 ra 的 当前窗口 大于 预读_最大窗口：
            ra 的 当前窗口 为 预读_最大窗口。
        完。
    完。
完。

vfs_直接读 接受 文件、缓冲、大小、偏移：
    节点 为 文件 的 第 F_Nova文件节点 项。
    操作 为 节点 的 第 I_操作 项。
    若 操作 不等于 空 并且 操作 的 直接读 不等于 空：
        返还 做 操作 的 直接读 用 节点、缓冲、大小、偏移。
    完。
    返还 错误_不支持。
完。

vfs_直接写 接受 文件、缓冲、大小、偏移：
    节点 为 文件 的 第 F_Nova文件节点 项。
    操作 为 节点 的 第 I_操作 项。
    若 操作 不等于 空 并且 操作 的 直接写 不等于 空：
        返还 做 操作 的 直接写 用 节点、缓冲、大小、偏移。
    完。
    返还 错误_不支持。
完。

Nova文件节点_设置大小 接受 节点、大小：
    若 节点 不等于 空：
        做 设置 用 节点、I_大小、大小。
    完。
完。

Nova文件节点_标记脏 接受 节点：
    若 节点 不等于 空：
        做 设置 用 节点、I_脏标志、1。
    完。
完。

Nova文件节点_更新访问时间 接受 节点：
    做 设置 用 节点、I_时间、做 当前时间_秒。
完。

Nova文件节点_更新修改时间 接受 节点：
    做 设置 用 节点、I_时间、做 当前时间_秒。
完。

Nova文件节点_标记脏_读写 接受 节点：
    若 节点 不等于 空：
        做 Nova文件节点_标记脏 用 节点。
    完。
完。

可变 回写_上次时间 为 0。

回写_检查 接受 零参：
    现在 为 做 读取毫秒。
    若 现在 减 回写_上次时间 小于 回写_间隔_毫秒：
        返还。
    完。
    回写_上次时间 为 现在。
    做 回写_刷新全部。
完。

回写_刷新全部 接受 零参：
    脏列表 为 做 获取脏inode列表。
    若 脏列表 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 脏列表：
        节点 为 脏列表 的 第 i 项。
        若 节点 的 第 I_地址空间 项 不等于 空：
            刷新数 为 做 页缓存_同步 用 节点 的 第 I_地址空间 项。
            VFS_回写页数 为 VFS_回写页数 加 刷新数。
        完。
        节点 的 第 I_脏标志 项 为 0。
        i 为 i 加 1。
    完。
完。

文件_读取全部 接受 路径：
    fd 为 做 sys_open 用 路径、0、0。
    若 fd 小于 0：
        返还 空。
    完。
    缓冲 为 做 kmalloc 用 65536。
    可变 总读取 为 0。
    可变 本次 为 做 sys_read 用 fd、缓冲 加 总读取、65536 减 总读取。
    只要 本次 大于 0：
        总读取 为 总读取 加 本次。
        若 总读取 大于或等于 65536：
            跳出。
        完。
        本次 为 做 sys_read 用 fd、缓冲 加 总读取、65536 减 总读取。
    完。
    做 sys_close 用 fd。
    返还 一组：缓冲、总读取。
完。

文件_写入全部 接受 路径、数据、大小：
    fd 为 做 sys_open 用 路径、O_WRONLY 位或 O_CREAT 位或 O_TRUNC、0x1B6。
    若 fd 小于 0：
        返还 fd。
    完。
    可变 已写 为 0。
    只要 已写 小于 大小：
        本次 为 做 sys_write 用 fd、数据 加 已写、大小 减 已写。
        若 本次 小于或等于 0：
            跳出。
        完。
        已写 为 已写 加 本次。
    完。
    做 sys_close 用 fd。
    返还 已写。
完。

文件_存在 接受 路径：
    fd 为 做 sys_open 用 路径、O_RDONLY、0。
    若 fd 小于 0：
        返还 假。
    完。
    做 sys_close 用 fd。
    返还 真。
完。

获取文件大小_路径 接受 路径：
    fd 为 做 sys_open 用 路径、O_RDONLY、0。
    若 fd 小于 0：
        返还 负 1。
    完。
    大小 为 做 sys_lseek 用 fd、0、2。
    做 sys_close 用 fd。
    返还 大小。
完。

获取路径大小 接受 路径：
    返还 做 获取文件大小_路径 用 路径。
完。

获取文件权限 接受 路径：
    返还 0x1B6。
完。

VFS_读写统计 接受 零参：
    返还 一本字典：读次数 对应 VFS_读次数、写次数 对应 VFS_写次数、读字节 对应 VFS_读字节、写字节 对应 VFS_写字节、fsync 对应 VFS_fsync次数、truncate 对应 VFS_truncate次数、缓存命中 对应 VFS_页缓存命中、缓存未命中 对应 VFS_页缓存未命中、命中率 对应 VFS_页缓存命中 加 VFS_页缓存未命中 大于 0 并且 VFS_页缓存命中 乘 100 除以 (VFS_页缓存命中 加 VFS_页缓存未命中)、预读触发 对应 VFS_预读触发、回写页数 对应 VFS_回写页数、直接IO 对应 VFS_直接IO次数。
完。

VFS读写_完整统计 接受 零参：
    返还 做 VFS_读写统计。
完。

VFS_IO健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    总请求 为 VFS_页缓存命中 加 VFS_页缓存未命中。
    若 总请求 大于 100 并且 VFS_页缓存命中 乘 100 除以 总请求 小于 50：
        做 追加 用 问题、「页缓存命中率过低: 」 加 做 整数转字符串 用 VFS_页缓存命中 乘 100 除以 总请求 加 「%」。
    完。
    若 VFS_回写页数 等于 0 并且 VFS_写次数 大于 1000：
        做 追加 用 问题、「回写未工作: 大量写操作但无回写」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「VFS I/O健康」。
    完。
    返还 问题。
完。

本章 收束。
