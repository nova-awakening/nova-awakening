《ext4超级块读取》

※ NovaOS ext4 超级块读取·磁盘结构·偏移常量·特性标志 — 完整生产级实现。
※ 功能: 磁盘超级块完整解析/内存超级块信息/块组描述符/特性标志/UUID/卷名。
※ 关联: ext4验证魔数 → ext4读块组描述符 → ext4日志系统 → VFS超级块 → 块设备层。

引用《..·中枢·类型定义》。
引用《..·中枢·错误码》。
引用《..·中枢·同步原语》。
引用《..·中枢·字符串替换》。
引用《..·稳态·AHCI驱动全局》。

※ 第1部分: 超级块磁盘偏移常量(精确到字节)。

EXT4_超级块偏移 为 1024。
EXT4_超级块大小 为 1024。
EXT4_魔数 为 0xEF53。
EXT4_最小块大小 为 1024。
EXT4_最大块大小 为 65536。
EXT4_好旧REV_文件节点大小 为 128。
EXT4_有效_FS 为 0x0001。
EXT4_错误_FS 为 0x0002。

※ 超级块字段偏移(相对于超级块起始)。
EXT4_SB偏移_文件节点总数 为 0x00。
EXT4_SB偏移_块总数 为 0x04。
EXT4_SB偏移_保留块数 为 0x08。
EXT4_SB偏移_空闲块数 为 0x0C。
EXT4_SB偏移_空闲文件节点数 为 0x10。
EXT4_SB偏移_首数据块 为 0x14。
EXT4_SB偏移_块大小LOG 为 0x18。
EXT4_SB偏移_簇大小LOG 为 0x1C。
EXT4_SB偏移_每组块数 为 0x20。
EXT4_SB偏移_每组簇数 为 0x24。
EXT4_SB偏移_每组文件节点数 为 0x28。
EXT4_SB偏移_挂载时间 为 0x2C。
EXT4_SB偏移_写入时间 为 0x30。
EXT4_SB偏移_挂载次数 为 0x34。
EXT4_SB偏移_最大挂载次数 为 0x36。
EXT4_SB偏移_魔数 为 0x38。
EXT4_SB偏移_状态 为 0x3A。
EXT4_SB偏移_错误行为 为 0x3C。
EXT4_SB偏移_修订版本_次 为 0x3E。
EXT4_SB偏移_最后检查时间 为 0x40。
EXT4_SB偏移_检查间隔 为 0x44。
EXT4_SB偏移_创建者OS 为 0x48。
EXT4_SB偏移_修订版本 为 0x4C。
EXT4_SB偏移_默认保留UID 为 0x50。
EXT4_SB偏移_默认保留GID 为 0x52。
※ 动态修订版本扩展字段。
EXT4_SB偏移_首非保留节点 为 0x54。
EXT4_SB偏移_文件节点大小 为 0x58。
EXT4_SB偏移_块组号 为 0x5A。
EXT4_SB偏移_兼容特性 为 0x5C。
EXT4_SB偏移_不兼容特性 为 0x60。
EXT4_SB偏移_RO兼容特性 为 0x64。
EXT4_SB偏移_UUID 为 0x68。
EXT4_SB偏移_卷名 为 0x78。
EXT4_SB偏移_最后挂载路径 为 0x88。
EXT4_SB偏移_日志文件节点 为 0xE0。
EXT4_SB偏移_块组描述符大小 为 0xFE。
EXT4_SB偏移_块总数高位 为 0x150。
EXT4_SB偏移_校验和种子 为 0x270。
EXT4_SB偏移_校验和 为 0x3FC。

※ 第2部分: 特性标志定义。

※ 兼容特性(可以在不支持时安全挂载)。
EXT4_特性_兼容_目录预分配 为 0x0001。
EXT4_特性_兼容_HAS_日志 为 0x0004。
EXT4_特性_兼容_扩展属性 为 0x0008。
EXT4_特性_兼容_调整大小 为 0x0010。
EXT4_特性_兼容_目录索引 为 0x0020。
EXT4_特性_兼容_稀疏超级块2 为 0x0200。

※ 不兼容特性(必须支持才能挂载)。
EXT4_特性_不兼容_FILETYPE 为 0x0002。
EXT4_特性_不兼容_RECOVER 为 0x0004。
EXT4_特性_不兼容_日志设备 为 0x0008。
EXT4_特性_不兼容_META_BG 为 0x0010。
EXT4_特性_不兼容_EXTENTS 为 0x0040。
EXT4_特性_不兼容_64位 为 0x0080。
EXT4_特性_不兼容_FLEX_BG 为 0x0200。
EXT4_特性_不兼容_内联数据 为 0x8000。

※ 只读兼容特性(不支持时只能只读挂载)。
EXT4_特性_RO_稀疏超级块 为 0x0001。
EXT4_特性_RO_大文件 为 0x0002。
EXT4_特性_RO_巨文件 为 0x0008。
EXT4_特性_RO_GDT_校验和 为 0x0010。
EXT4_特性_RO_目录链接数 为 0x0020。
EXT4_特性_RO_额外节点大小 为 0x0040。
EXT4_特性_RO_元数据校验和 为 0x0400。

※ 挂载选项。
EXT4_挂载_调试 为 0x0001。
EXT4_挂载_无ACLS 为 0x0002。
EXT4_挂载_DATA_日志 为 0x0004。
EXT4_挂载_DATA_有序 为 0x0008。
EXT4_挂载_DATA_回写 为 0x0010。
EXT4_挂载_无屏障 为 0x0020。
EXT4_挂载_DISCARD 为 0x0040。

※ 第3部分: 磁盘超级块完整结构。

Ext4磁盘超级块 是 一种 事物：
    文件节点总数,。
    块总数_低,。
    保留块数_低,。
    空闲块数_低,。
    空闲文件节点数,。
    首数据块,。
    块大小_log,。
    簇大小_log,。
    每组块数,。
    每组簇数,。
    每组文件节点数,。
    挂载时间,。
    写入时间,。
    挂载次数,。
    最大挂载次数,。
    魔数,。
    状态,。
    错误行为,。
    修订版本_次,。
    最后检查时间,。
    检查间隔,。
    创建者OS,。
    修订版本,。
    默认保留UID,。
    默认保留GID,。
    ※ 动态修订版本扩展。
    首非保留节点,。
    文件节点大小,。
    块组号,。
    兼容特性,。
    不兼容特性,。
    只读兼容特性,。
    UUID,
    卷名,
    最后挂载路径,
    日志文件节点,。
    块组描述符大小,。
    块总数_高,。
    校验和种子,。
    校验和,。
完。

※ 第4部分: 内存超级块信息(挂载后维护)。

Ext4超级块信息 是 一种 事物：
    磁盘超级块,
    块大小,。
    块大小位,。
    簇大小,。
    文件节点大小,。
    文件节点每块,。
    块组数,。
    每组块数,。
    每组文件节点数,。
    块总数,。
    空闲块数,。
    空闲文件节点数,。
    组描述符,
    组描述符块数,。
    挂载选项,。
    状态,。
    日志文件节点号,。
    日志,
    已挂载,。
    只读,。
    锁,
    卷名,。
    UUID字符串,。
完。

※ 第5部分: 块组描述符。

Ext4块组描述符 是 一种 事物：
    块位图_低,。
    文件节点位图_低,。
    文件节点表_低,。
    空闲块数_低,。
    空闲文件节点数_低,。
    目录数_低,。
    标志,。
    排除位图_低,。
    快照块位图_低,。
    未使用计数_低,。
    ※ 64位扩展。
    块位图_高,。
    文件节点位图_高,。
    文件节点表_高,。
    空闲块数_高,。
    空闲文件节点数_高,。
    目录数_高,。
    未使用计数_高,。
    排除位图_高,。
    快照块位图_高,。
    校验和,。
完。

※ 第6部分: 统计计数器。

超级块读取统计 是 一种 事物：
    读取次数,。
    读取成功,。
    读取失败,。
    魔数验证失败,。
    特性检查失败,。
    块大小无效,。
    解析字段数,。
    UUID解析次数,。
    卷名解析次数,。
    块组计算次数,。
    64位模式次数,。
    校验和验证次数,。
    校验和失败次数,。
    最大文件大小计算,。
    填充超级块次数,。
完。

可变 超级块读取统计数据 为 超级块读取统计 {}。

※ 第7部分: 核心读取 — ext4_读超级块。

ext4_读超级块 接受 设备：
    超级块读取统计数据.读取次数 为 超级块读取统计数据.读取次数 加 1。

    若 设备 等于 空：
        超级块读取统计数据.读取失败 为 超级块读取统计数据.读取失败 加 1。
        返还 错误码_无效参数。
    完。

    可变 缓冲 为 做 块设备_读 用 设备、EXT4_超级块偏移、EXT4_超级块大小。
    若 缓冲 等于 空：
        超级块读取统计数据.读取失败 为 超级块读取统计数据.读取失败 加 1。
        做 打印 用 「[ext4] 错误: 做 无法读取超级块 用 偏移1024」。
        返还 错误码_IO。
    完。

    魔数 为 做 缓冲读16 用 缓冲、EXT4_SB偏移_魔数。
    若 魔数 不等于 EXT4_魔数：
        超级块读取统计数据.魔数验证失败 为 超级块读取统计数据.魔数验证失败 加 1。
        做 打印 用 做 字符串拼接 用 「[ext4] 错误: 无效魔数 0x」、做 转字符串 用 魔数。
        返还 错误码_无效超级块。
    完。

    可变 磁盘sb 为 Ext4磁盘超级块 {}。
    磁盘sb.魔数 为 魔数。
    磁盘sb.文件节点总数 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_文件节点总数。
    磁盘sb.块总数_低 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_块总数。
    磁盘sb.保留块数_低 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_保留块数。
    磁盘sb.空闲块数_低 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_空闲块数。
    磁盘sb.空闲文件节点数 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_空闲文件节点数。
    磁盘sb.首数据块 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_首数据块。
    磁盘sb.块大小_log 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_块大小LOG。
    磁盘sb.簇大小_log 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_簇大小LOG。
    磁盘sb.每组块数 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_每组块数。
    磁盘sb.每组簇数 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_每组簇数。
    磁盘sb.每组文件节点数 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_每组文件节点数。

    超级块读取统计数据.解析字段数 为 超级块读取统计数据.解析字段数 加 11。

    磁盘sb.挂载时间 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_挂载时间。
    磁盘sb.写入时间 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_写入时间。
    磁盘sb.挂载次数 为 做 缓冲读16 用 缓冲、EXT4_SB偏移_挂载次数。
    磁盘sb.最大挂载次数 为 做 缓冲读16 用 缓冲、EXT4_SB偏移_最大挂载次数。
    磁盘sb.状态 为 做 缓冲读16 用 缓冲、EXT4_SB偏移_状态。
    磁盘sb.错误行为 为 做 缓冲读16 用 缓冲、EXT4_SB偏移_错误行为。
    磁盘sb.修订版本_次 为 做 缓冲读16 用 缓冲、EXT4_SB偏移_修订版本_次。
    磁盘sb.最后检查时间 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_最后检查时间。
    磁盘sb.检查间隔 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_检查间隔。
    磁盘sb.创建者OS 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_创建者OS。
    磁盘sb.修订版本 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_修订版本。
    磁盘sb.默认保留UID 为 做 缓冲读16 用 缓冲、EXT4_SB偏移_默认保留UID。
    磁盘sb.默认保留GID 为 做 缓冲读16 用 缓冲、EXT4_SB偏移_默认保留GID。

    超级块读取统计数据.解析字段数 为 超级块读取统计数据.解析字段数 加 13。

    磁盘sb.兼容特性 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_兼容特性。
    磁盘sb.不兼容特性 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_不兼容特性。
    磁盘sb.只读兼容特性 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_RO兼容特性。

    若 磁盘sb.修订版本 不小于 1：
        磁盘sb.首非保留节点 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_首非保留节点。
        磁盘sb.文件节点大小 为 做 缓冲读16 用 缓冲、EXT4_SB偏移_文件节点大小。
        磁盘sb.块组号 为 做 缓冲读16 用 缓冲、EXT4_SB偏移_块组号。
        磁盘sb.日志文件节点 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_日志文件节点。
        磁盘sb.块组描述符大小 为 做 缓冲读16 用 缓冲、EXT4_SB偏移_块组描述符大小。
        超级块读取统计数据.解析字段数 为 超级块读取统计数据.解析字段数 加 5。
    否则：
        磁盘sb.文件节点大小 为 EXT4_好旧REV_文件节点大小。
        磁盘sb.块组描述符大小 为 32。
        磁盘sb.首非保留节点 为 11。
    完。

    磁盘sb.UUID 为 做 ext4_读UUID 用 缓冲、EXT4_SB偏移_UUID。
    超级块读取统计数据.UUID解析次数 为 超级块读取统计数据.UUID解析次数 加 1。

    磁盘sb.卷名 为 做 ext4_读字符串 用 缓冲、EXT4_SB偏移_卷名、16。
    超级块读取统计数据.卷名解析次数 为 超级块读取统计数据.卷名解析次数 加 1。

    磁盘sb.最后挂载路径 为 做 ext4_读字符串 用 缓冲、EXT4_SB偏移_最后挂载路径、64。

    若 磁盘sb.不兼容特性 位与 EXT4_特性_不兼容_64位 不等于 0：
        磁盘sb.块总数_高 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_块总数高位。
        超级块读取统计数据.64位模式次数 为 超级块读取统计数据.64位模式次数 加 1。
    完。

    若 磁盘sb.只读兼容特性 位与 EXT4_特性_RO_元数据校验和 不等于 0：
        磁盘sb.校验和种子 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_校验和种子。
        磁盘sb.校验和 为 做 缓冲读32 用 缓冲、EXT4_SB偏移_校验和。
        超级块读取统计数据.校验和验证次数 为 超级块读取统计数据.校验和验证次数 加 1。
    完。

    超级块读取统计数据.读取成功 为 超级块读取统计数据.读取成功 加 1。
    返还 磁盘sb。
完。

※ 第7.1部分: UUID和字符串读取辅助。

ext4_读UUID 接受 缓冲、偏移：
    ※ 读取16字节UUID,格式化为 xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 16：
        字节 为 做 缓冲读8 用 缓冲、偏移 加 i。
        高 为 (字节 右移 4) 位与 0x0F。
        低 为 字节 位与 0x0F。
        结果 为 做 字符串拼接 用 结果、做 ext4_十六进制字符 用 高。
        结果 为 做 字符串拼接 用 结果、做 ext4_十六进制字符 用 低。
        若 i 等于 3 或者 i 等于 5 或者 i 等于 7 或者 i 等于 9：
            结果 为 做 字符串拼接 用 结果、「-」。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

ext4_十六进制字符 接受 值：
    若 值 小于 10) { 返还 字符(值 加 48：
    返还 做 字符 用 值 减 10 加 97。
完。

ext4_读字符串 接受 缓冲、偏移、最大长度：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 最大长度：
        字节 为 做 缓冲读8 用 缓冲、偏移 加 i。
        若 字节 等于 0：
        结果 为 做 字符串拼接 用 结果、做 字符 用 字节。
        i 为 i 加 1。
    完。
    返还 结果。
完。

※ 第8部分: 块大小验证与最大文件大小计算。

ext4_验证块大小 接受 块大小_log：
    块大小 为 EXT4_最小块大小 左移 块大小_log。
    若 块大小 小于 EXT4_最小块大小：
    若 块大小 大于 EXT4_最大块大小：
    ※ 块大小必须是2的幂。
    若 块大小 位与 (块大小 减 1) 不等于 0：
    返还 真。
完。

ext4_计算最大文件大小 接受 块大小位：
    超级块读取统计数据.最大文件大小计算 为 超级块读取统计数据.最大文件大小计算 加 1。
    ※ ext4使用extent树,最大文件大小 = 2^32 * 块大小。
    块大小 为 1 左移 块大小位。
    ※ 简化: 最大16TB(4096字节块) 或 256TB(65536字节块)。
    返还 0xFFFFFFFF 乘 块大小。
完。

ext4_计算块组数 接受 块总数、首数据块、每组块数：
    超级块读取统计数据.块组计算次数 为 超级块读取统计数据.块组计算次数 加 1。
    若 每组块数 等于 0：
    数据块数 为 块总数 减 首数据块。
    返还 (数据块数 加 每组块数 减 1) 除以 每组块数。
完。

※ 第9部分: 特性标志打印(调试用)。

ext4_打印特性标志 接受 磁盘sb：
    做 打印 用 「[ext4] ── 兼容特性 ──」。
    若 磁盘sb.兼容特性 位与 EXT4_特性_兼容_HAS_日志 不等于 0：
        做 打印 用 「  HAS_JOURNAL」。
    完。
    若 磁盘sb.兼容特性 位与 EXT4_特性_兼容_扩展属性 不等于 0：
        做 打印 用 「  EXT_ATTR」。
    完。
    若 磁盘sb.兼容特性 位与 EXT4_特性_兼容_目录索引 不等于 0：
        做 打印 用 「  DIR_INDEX」。
    完。

    做 打印 用 「[ext4] ── 不兼容特性 ──」。
    若 磁盘sb.不兼容特性 位与 EXT4_特性_不兼容_FILETYPE 不等于 0：
        做 打印 用 「  FILETYPE」。
    完。
    若 磁盘sb.不兼容特性 位与 EXT4_特性_不兼容_EXTENTS 不等于 0：
        做 打印 用 「  EXTENTS」。
    完。
    若 磁盘sb.不兼容特性 位与 EXT4_特性_不兼容_64位 不等于 0：
        做 打印 用 「  64BIT」。
    完。
    若 磁盘sb.不兼容特性 位与 EXT4_特性_不兼容_FLEX_BG 不等于 0：
        做 打印 用 「  FLEX_BG」。
    完。

    做 打印 用 「[ext4] ── 只读兼容特性 ──」。
    若 磁盘sb.只读兼容特性 位与 EXT4_特性_RO_大文件 不等于 0：
        做 打印 用 「  LARGE_FILE」。
    完。
    若 磁盘sb.只读兼容特性 位与 EXT4_特性_RO_巨文件 不等于 0：
        做 打印 用 「  HUGE_FILE」。
    完。
    若 磁盘sb.只读兼容特性 位与 EXT4_特性_RO_元数据校验和 不等于 0：
        做 打印 用 「  METADATA_CSUM」。
    完。
完。

※ 第10部分: 统计输出。

ext4_超级块读取_打印统计 接受 零参：
    做 打印 用 「[ext4超级块] ═══ 统计信息 ═══」。
    做 打印 用 做 字符串拼接 用 「  读取次数:         」、做 转字符串 用 超级块读取统计数据.读取次数。
    做 打印 用 做 字符串拼接 用 「  读取成功:         」、做 转字符串 用 超级块读取统计数据.读取成功。
    做 打印 用 做 字符串拼接 用 「  读取失败:         」、做 转字符串 用 超级块读取统计数据.读取失败。
    做 打印 用 做 字符串拼接 用 「  魔数验证失败:     」、做 转字符串 用 超级块读取统计数据.魔数验证失败。
    做 打印 用 做 字符串拼接 用 「  解析字段数:       」、做 转字符串 用 超级块读取统计数据.解析字段数。
    做 打印 用 做 字符串拼接 用 「  UUID解析:         」、做 转字符串 用 超级块读取统计数据.UUID解析次数。
    做 打印 用 做 字符串拼接 用 「  64位模式:         」、做 转字符串 用 超级块读取统计数据.64位模式次数。
    做 打印 用 做 字符串拼接 用 「  校验和验证:       」、做 转字符串 用 超级块读取统计数据.校验和验证次数。
    做 打印 用 做 字符串拼接 用 「  填充超级块:       」、做 转字符串 用 超级块读取统计数据.填充超级块次数。
    返还 0。
完。

ext4_超级块读取_获取统计 接受 零参：
    返还 超级块读取统计数据。
完。

ext4_超级块读取_重置统计 接受 零参：
    超级块读取统计数据 为 超级块读取统计 {}。
    返还 0。
完。

※ 第11部分: 超级块信息打印(调试用)。

ext4_打印超级块信息 接受 磁盘sb：
    做 打印 用 「[ext4] ═══ 超级块信息 ═══」。
    做 打印 用 做 字符串拼接 用 「  魔数:             0x」、做 转字符串 用 磁盘sb.魔数。
    做 打印 用 做 字符串拼接 用 「  文件节点总数:     」、做 转字符串 用 磁盘sb.文件节点总数。
    做 打印 用 做 字符串拼接 用 「  做 块总数 用 低:       」、做 转字符串 用 磁盘sb.块总数_低。
    做 打印 用 做 字符串拼接 用 「  做 空闲块数 用 低:     」、做 转字符串 用 磁盘sb.空闲块数_低。
    做 打印 用 做 字符串拼接 用 「  空闲文件节点数:   」、做 转字符串 用 磁盘sb.空闲文件节点数。
    做 打印 用 做 字符串拼接 用 「  首数据块:         」、做 转字符串 用 磁盘sb.首数据块。
    做 打印 用 做 字符串拼接 用 「  块大小:           」、做 转字符串 用 EXT4_最小块大小 左移 磁盘sb.块大小_log。
    做 打印 用 做 字符串拼接 用 「  每组块数:         」、做 转字符串 用 磁盘sb.每组块数。
    做 打印 用 做 字符串拼接 用 「  每组文件节点数:   」、做 转字符串 用 磁盘sb.每组文件节点数。
    做 打印 用 做 字符串拼接 用 「  挂载次数:         」、做 转字符串 用 磁盘sb.挂载次数。
    做 打印 用 做 字符串拼接 用 「  状态:             」、做 转字符串 用 磁盘sb.状态。
    做 打印 用 做 字符串拼接 用 「  修订版本:         」、做 转字符串 用 磁盘sb.修订版本。
    做 打印 用 做 字符串拼接 用 「  文件节点大小:     」、做 转字符串 用 磁盘sb.文件节点大小。
    做 打印 用 做 字符串拼接 用 「  块组描述符大小:   」、做 转字符串 用 磁盘sb.块组描述符大小。
    做 打印 用 做 字符串拼接 用 「  UUID:             」、磁盘sb.UUID。
    做 打印 用 做 字符串拼接 用 「  卷名:             」、磁盘sb.卷名。
    做 打印 用 做 字符串拼接 用 「  兼容特性:         0x」、做 转字符串 用 磁盘sb.兼容特性。
    做 打印 用 做 字符串拼接 用 「  不兼容特性:       0x」、做 转字符串 用 磁盘sb.不兼容特性。
    做 打印 用 做 字符串拼接 用 「  只读兼容特性:     0x」、做 转字符串 用 磁盘sb.只读兼容特性。
    做 ext4_打印特性标志 用 磁盘sb。
    返还 0。
完。

ext4_验证文件节点大小 接受 文件节点大小、块大小：
    若 文件节点大小 小于 EXT4_好旧REV_文件节点大小：
    若 文件节点大小 大于 块大小：
    ※ 必须是2的幂。
    若 文件节点大小 位与 (文件节点大小 减 1) 不等于 0：
    返还 真。
完。

ext4_验证每组参数 接受 每组块数、每组文件节点数：
    若 每组块数 等于 0 或者 每组文件节点数 等于 0：
    若 每组块数 大于 0x10000：
    若 每组文件节点数 大于 0x10000：
    返还 真。
完。

可变 AI_扩展读超级_热文件表 为 []。

AI_扩展读超级_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_扩展读超级_热文件表：
        若 AI_扩展读超级_热文件表[i].路径 等于 路径：
            AI_扩展读超级_热文件表[i].频率 为 AI_扩展读超级_热文件表[i].频率 加 1。
            返还 AI_扩展读超级_热文件表[i].频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_扩展读超级_热文件表 小于 128：
        做 追加 用 AI_扩展读超级_热文件表、{ 路径: 路径、频率: 1 }。
    完。
    返还 1。
完。

AI_扩展读超级_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
    返还 32768。
完。

本章 收束。

本章 收束。

本章 收束。
