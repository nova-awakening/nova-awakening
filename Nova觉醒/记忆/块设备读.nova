《块设备读》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·同步原语》。

引用《..·中枢·信号量创建》。

引用《..·中枢·字符串替换》。

引用《..·引导·寄存器定义》。

引用《..·中枢·内核打印》。

引用《..·推理·定点Q算术》。

引用《..·中枢·哈希函数》。

块设备_读 接受 设备号、偏移、大小：
    dev 为 做 块设备_获取 用 设备号。
    若 dev 等于 空：
        返还 错误码_无设备。
    完。
    若 dev 的 操作表 的 读 等于 空：
        返还 错误码_IO。
    完。
    扇区 为 偏移 除以 dev 的 扇区大小。
    扇区数 为 (大小 加 dev 的 扇区大小 减 1) 除以 dev 的 扇区大小。
    可变 缓冲 为 做 分配内存 用 扇区数 乘 dev 的 扇区大小。
    若 缓冲 等于 空：
        返还 错误码_内存不足。
    完。
    结果 为 做 dev 的 操作表 的 读 用 dev、扇区、缓冲、扇区数。
    若 结果 不等于 0：
        做 释放内存 用 缓冲。
        返还 结果。
    完。
    返还 缓冲。
完。

块设备_写 接受 设备号、偏移、数据、大小：
    dev 为 做 块设备_获取 用 设备号。
    若 dev 等于 空：
        返还 错误码_无设备。
    完。
    若 dev 的 操作表 的 写 等于 空：
        返还 错误码_IO。
    完。
    若 dev 的 标志 位与 块设备_标志_只读：
        返还 错误码_只读。
    完。
    扇区 为 偏移 除以 dev 的 扇区大小。
    扇区数 为 (大小 加 dev 的 扇区大小 减 1) 除以 dev 的 扇区大小。
    返还 做 dev 的 操作表 的 写 用 dev、扇区、数据、扇区数。
完。

块设备_刷新 接受 设备号：
    dev 为 做 块设备_获取 用 设备号。
    若 dev 等于 空：
        返还 错误码_无设备。
    完。
    若 dev 的 操作表 的 刷新 不等于 空：
        返还 做 dev 的 操作表 的 刷新 用 dev。
    完。
    返还 0。
完。

块请求队列_创建 接受 dev：
    可变 q 为 做 分配内存 用 做 大小 用 块请求队列。
    q 的 请求链表 为 空。
    q 的 请求数 为 0。
    q 的 最大请求数 为 块设备_最大请求队列。
    q 的 设备 为 dev。
    q 的 已停止 为 假。
    返还 q。
完。

块请求队列_销毁 接受 q：
    若 q 等于 空：
        返还。
    完。
    可变 req 为 q 的 请求链表。
    只要 req 不等于 空：
        下一个 为 req 的 下一个。
        req 的 错误 为 错误码_IO。
        做 完成量_完成 用 req 的 完成量。
        做 释放内存 用 req。
        req 为 下一个。
    完。
    做 释放内存 用 q。
完。

块请求队列_提交 接受 q、req：
    做 自旋锁_获取 用 q 的 锁。
    若 q 的 已停止 或者 q 的 请求数 大于或等于 q 的 最大请求数：
        做 自旋锁_释放 用 q 的 锁。
        返还 错误码_再试。
    完。
    req 的 下一个 为 空。
    若 q 的 请求链表 等于 空：
        q 的 请求链表 为 req。
    否则：
        可变 当前 为 q 的 请求链表。
        只要 当前 的 下一个 不等于 空：
            当前 为 当前 的 下一个。
        完。
        当前 的 下一个 为 req。
    完。
    q 的 请求数 为 q 的 请求数 加 1。
    做 自旋锁_释放 用 q 的 锁。
    返还 0。
完。

块请求队列_取出 接受 q：
    做 自旋锁_获取 用 q 的 锁。
    若 q 的 请求链表 等于 空：
        做 自旋锁_释放 用 q 的 锁。
        返还 空。
    完。
    req 为 q 的 请求链表。
    q 的 请求链表 为 req 的 下一个。
    q 的 请求数 为 q 的 请求数 减 1。
    做 自旋锁_释放 用 q 的 锁。
    返还 req。
完。

块请求队列_停止 接受 q：
    q 的 已停止 为 真。
完。

块请求队列_恢复 接受 q：
    q 的 已停止 为 假。
完。

NBIO_最大合并 为 256。

NBIO_预读窗口 为 32。

NBIO_写回延迟 为 5000。

NBIO_写回最大脑 为 128。

块设备_最大请求队列 为 256。

块设备_标志_只读 为 0x01。

AI_NBIO_特征维度 为 10。

可变 NBIO_总读数 为 0。

可变 NBIO_总写数 为 0。

可变 NBIO_合并数 为 0。

可变 NBIO_预读命中 为 0。

可变 NBIO_预读未中 为 0。

可变 NBIO_写回数 为 0。

可变 NBIO_总字节读 为 0。

可变 NBIO_总字节写 为 0。

块请求 是 一种 事物，含 扁区、扁区数、缓冲、读写标志、错误、完成量、下一个、优先级。

块请求队列 是 一种 事物，含 请求链表、请求数、最大请求数、设备、已停止、锁。

散列元素 是 一种 事物，含 物理地址、长度。

块设备_散列读 接受 设备号、扁区、散列表、元素数：
    dev 为 做 块设备_获取 用 设备号。
    若 dev 等于 空：
        返还 错误码_无设备。
    完。
    若 dev 的 操作表 的 散列读 等于 空：
        返还 做 块设备_散列读_回退 用 dev、扁区、散列表、元素数。
    完。
    返还 做 dev 的 操作表 的 散列读 用 dev、扁区、散列表、元素数。
完。

块设备_散列读_回退 接受 dev、扁区、散列表、元素数：
    可变 总长 为 0。
    可变 i 为 0。
    只要 i 小于 元素数：
        总长 为 总长 加 散列表 的 第 i 项 的 长度。
        i 为 i 加 1。
    完。
    扁区数 为 (总长 加 dev 的 扁区大小 减 1) 除以 dev 的 扁区大小。
    缓冲 为 做 分配内存 用 扁区数 乘 dev 的 扁区大小。
    若 缓冲 等于 空：
        返还 错误码_内存不足。
    完。
    结果 为 做 dev 的 操作表 的 读 用 dev、扁区、缓冲、扁区数。
    若 结果 不等于 0：
        做 释放内存 用 缓冲。
        返还 结果。
    完。
    可变 偏移 为 0。
    i 为 0。
    只要 i 小于 元素数：
        做 内存复制 用 散列表 的 第 i 项 的 物理地址、缓冲 加 偏移、散列表 的 第 i 项 的 长度。
        偏移 为 偏移 加 散列表 的 第 i 项 的 长度。
        i 为 i 加 1。
    完。
    做 释放内存 用 缓冲。
    返还 0。
完。

预读状态 是 一种 事物，含 最后扁区、预读扁区、窗口大小、连续命中、已启用。

NBIO_预读初始化 接受 dev：
    dev 的 预读 为 新建 预读状态：最后扁区 为 0、预读扁区 为 0、窗口大小 为 4、连续命中 为 0、已启用 为 真。
完。

NBIO_预读检查 接受 dev、当前扁区：
    若 非 dev 的 预读 的 已启用：
        返还。
    完。
    若 当前扁区 等于 dev 的 预读 的 最后扁区 加 1 或者 当前扁区 等于 dev 的 预读 的 预读扁区：
        dev 的 预读 的 连续命中 为 dev 的 预读 的 连续命中 加 1。
        NBIO_预读命中 为 NBIO_预读命中 加 1。
        若 dev 的 预读 的 连续命中 大于 4 并且 dev 的 预读 的 窗口大小 小于 NBIO_预读窗口：
            dev 的 预读 的 窗口大小 为 dev 的 预读 的 窗口大小 乘 2。
            若 dev 的 预读 的 窗口大小 大于 NBIO_预读窗口：
                dev 的 预读 的 窗口大小 为 NBIO_预读窗口。
            完。
        完。
        做 NBIO_提交预读 用 dev、当前扁区 加 1、dev 的 预读 的 窗口大小。
    否则：
        dev 的 预读 的 连续命中 为 0。
        dev 的 预读 的 窗口大小 为 4。
        NBIO_预读未中 为 NBIO_预读未中 加 1。
    完。
    dev 的 预读 的 最后扁区 为 当前扁区。
完。

NBIO_提交预读 接受 dev、起始扁区、扁区数：
    缓冲 为 做 分配内存 用 扁区数 乘 dev 的 扁区大小。
    若 缓冲 等于 空：
        返还。
    完。
    req 为 做 分配内存 用 做 大小 用 块请求。
    req 的 扁区 为 起始扁区。
    req 的 扁区数 为 扁区数。
    req 的 缓冲 为 缓冲。
    req 的 读写标志 为 0。
    req 的 优先级 为 0。
    做 块请求队列_提交 用 dev 的 队列、req。
    dev 的 预读 的 预读扁区 为 起始扁区 加 扁区数。
完。

脏页记录 是 一种 事物，含 扁区、缓冲、扁区数、时间戳、下一个。

可变 NBIO_脏页链表 为 空。

可变 NBIO_脏页数 为 0。

NBIO_延迟写 接受 设备号、扁区、数据、大小：
    dev 为 做 块设备_获取 用 设备号。
    若 dev 等于 空：
        返还 错误码_无设备。
    完。
    若 dev 的 标志 位与 块设备_标志_只读：
        返还 错误码_只读。
    完。
    扁区数 为 (大小 加 dev 的 扁区大小 减 1) 除以 dev 的 扁区大小。
    缓冲 为 做 分配内存 用 扁区数 乘 dev 的 扁区大小。
    若 缓冲 等于 空：
        返还 错误码_内存不足。
    完。
    做 内存复制 用 缓冲、数据、大小。
    记录 为 做 分配内存 用 做 大小 用 脏页记录。
    记录 的 扁区 为 扁区 除以 dev 的 扁区大小。
    记录 的 缓冲 为 缓冲。
    记录 的 扁区数 为 扁区数。
    记录 的 时间戳 为 做 读TSC。
    记录 的 下一个 为 NBIO_脏页链表。
    NBIO_脏页链表 为 记录。
    NBIO_脏页数 为 NBIO_脏页数 加 1。
    若 NBIO_脏页数 大于或等于 NBIO_写回最大脑：
        做 NBIO_强制写回 用 设备号。
    完。
    NBIO_总写数 为 NBIO_总写数 加 1。
    NBIO_总字节写 为 NBIO_总字节写 加 大小。
    返还 0。
完。

NBIO_强制写回 接受 设备号：
    dev 为 做 块设备_获取 用 设备号。
    若 dev 等于 空：
        返还。
    完。
    可变 当前 为 NBIO_脏页链表。
    只要 当前 不等于 空：
        下一个 为 当前 的 下一个。
        做 dev 的 操作表 的 写 用 dev、当前 的 扁区、当前 的 缓冲、当前 的 扁区数。
        做 释放内存 用 当前 的 缓冲。
        做 释放内存 用 当前。
        当前 为 下一个。
        NBIO_写回数 为 NBIO_写回数 加 1。
    完。
    NBIO_脏页链表 为 空。
    NBIO_脏页数 为 0。
    做 块设备_刷新 用 设备号。
完。

NBIO_尝试合并 接受 q、req：
    若 q 的 请求链表 等于 空：
        返还 假。
    完。
    可变 当前 为 q 的 请求链表。
    只要 当前 不等于 空：
        若 当前 的 扁区 加 当前 的 扁区数 等于 req 的 扁区：
            若 当前 的 扁区数 加 req 的 扁区数 小于或等于 NBIO_最大合并：
                当前 的 扁区数 为 当前 的 扁区数 加 req 的 扁区数。
                NBIO_合并数 为 NBIO_合并数 加 1。
                返还 真。
            完。
        完。
        若 req 的 扁区 加 req 的 扁区数 等于 当前 的 扁区：
            若 当前 的 扁区数 加 req 的 扁区数 小于或等于 NBIO_最大合并：
                当前 的 扁区 为 req 的 扁区。
                当前 的 扁区数 为 当前 的 扁区数 加 req 的 扁区数。
                NBIO_合并数 为 NBIO_合并数 加 1。
                返还 真。
            完。
        完。
        当前 为 当前 的 下一个。
    完。
    返还 假。
完。

NBIO_AI分析 接受 零参：
    特征 为 做 Tensor_创建 用 AI_NBIO_特征维度。
    若 特征 等于 空：
        返还。
    完。
    做 Tensor_写 用 特征、0、NBIO_总读数。
    做 Tensor_写 用 特征、1、NBIO_总写数。
    做 Tensor_写 用 特征、2、NBIO_合并数。
    做 Tensor_写 用 特征、3、NBIO_预读命中。
    做 Tensor_写 用 特征、4、NBIO_预读未中。
    做 Tensor_写 用 特征、5、NBIO_写回数。
    做 Tensor_写 用 特征、6、NBIO_总字节读。
    做 Tensor_写 用 特征、7、NBIO_总字节写。
    做 Tensor_写 用 特征、8、NBIO_脏页数。
    命中率 为 0。
    若 NBIO_预读命中 加 NBIO_预读未中 大于 0：
        命中率 为 NBIO_预读命中 乘 1000 除以 (NBIO_预读命中 加 NBIO_预读未中)。
    完。
    做 Tensor_写 用 特征、9、命中率。
    结果 为 做 AI总线_推理 用 「IO调度」、特征。
    做 Tensor_释放 用 特征。
完。

NBIO_诊断报告 接受 零参：
    做 内核打印 用 「═══ NBIO 块设备IO统计 ═══\n」。
    做 内核打印 用 「总读: %d  总写: %d  合并: %d\n」、NBIO_总读数、NBIO_总写数、NBIO_合并数。
    做 内核打印 用 「预读命中: %d  未中: %d\n」、NBIO_预读命中、NBIO_预读未中。
    做 内核打印 用 「写回: %d  脏页: %d\n」、NBIO_写回数、NBIO_脏页数。
    做 内核打印 用 「字节读: %d  字节写: %d\n」、NBIO_总字节读、NBIO_总字节写。
完。

NBIO_最大设备 为 64。

可变 NBIO_设备表 为 一组：（空）。

可变 NBIO_设备数 为 0。

块设备信息 是 一种 事物，含 设备号、名称、扁区大小、总扁区数、操作表、队列、预读、标志、分区表、分区数。

分区信息 是 一种 事物，含 起始扁区、扁区数、类型、可启动。

块设备_注册 接受 设备号、名称、扁区大小、总扁区、操作表：
    若 NBIO_设备数 大于或等于 NBIO_最大设备：
        返还 错误码_满。
    完。
    dev 为 新建 块设备信息：设备号 为 设备号、名称 为 名称、扁区大小 为 扁区大小、总扁区数 为 总扁区、操作表 为 操作表、队列 为 空、预读 为 空、标志 为 0、分区表 为 一组：（空）、分区数 为 0。
    做 追加 用 NBIO_设备表、dev。
    NBIO_设备数 为 NBIO_设备数 加 1。
    dev 的 队列 为 做 块请求队列_创建 用 dev。
    做 NBIO_预读初始化 用 dev。
    做 内核信息 用 「NBIO: 注册块设备 %s 设备号=%d 扁区=%d 总量=%d\n」、名称、设备号、扁区大小、总扁区。
    返还 0。
完。

块设备_获取 接受 设备号：
    可变 i 为 0。
    只要 i 小于 NBIO_设备数：
        若 NBIO_设备表 的 第 i 项 的 设备号 等于 设备号：
            返还 NBIO_设备表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

块设备_注销 接受 设备号：
    做 NBIO_强制写回 用 设备号。
    dev 为 做 块设备_获取 用 设备号。
    若 dev 不等于 空：
        做 块请求队列_销毁 用 dev 的 队列。
        做 内核信息 用 「NBIO: 注销块设备 %s\n」、dev 的 名称。
    完。
完。

MBR_签名 为 0xAA55。

MBR_分区表偏移 为 446。

MBR_分区条目大小 为 16。

NBIO_解析MBR 接受 设备号：
    mbr 为 做 块设备_读 用 设备号、0、512。
    若 mbr 等于 空 或者 做 计数 用 mbr 小于 512：
        返还 负 1。
    完。
    签名 为 mbr 的 第 510 项 位或 mbr 的 第 511 项 左移 8。
    若 签名 不等于 MBR_签名：
        做 内核警告 用 「NBIO: 无效MBR签名 %04x\n」、签名。
        返还 负 1。
    完。
    dev 为 做 块设备_获取 用 设备号。
    若 dev 等于 空：
        返还 负 1。
    完。
    可变 i 为 0。
    只要 i 小于 4：
        偏移 为 MBR_分区表偏移 加 i 乘 MBR_分区条目大小。
        状态 为 mbr 的 第 偏移 项。
        类型 为 mbr 的 第 偏移 加 4 项。
        起始LBA 为 mbr 的 第 偏移 加 8 项 位或 mbr 的 第 偏移 加 9 项 左移 8 位或 mbr 的 第 偏移 加 10 项 左移 16 位或 mbr 的 第 偏移 加 11 项 左移 24。
        扁区数 为 mbr 的 第 偏移 加 12 项 位或 mbr 的 第 偏移 加 13 项 左移 8 位或 mbr 的 第 偏移 加 14 项 左移 16 位或 mbr 的 第 偏移 加 15 项 左移 24。
        若 类型 不等于 0 并且 扁区数 大于 0：
            做 追加 用 dev 的 分区表、新建 分区信息：起始扁区 为 起始LBA、扁区数 为 扁区数、类型 为 类型、可启动 为 状态 等于 0x80。
            dev 的 分区数 为 dev 的 分区数 加 1。
            做 内核信息 用 「  分区%d: LBA=%d 大小=%d 类型=0x%02x\n」、i、起始LBA、扁区数、类型。
        完。
        i 为 i 加 1。
    完。
    做 释放内存 用 mbr。
    返还 dev 的 分区数。
完。

块设备_同步读 接受 设备号、偏移、大小：
    NBIO_总读数 为 NBIO_总读数 加 1。
    NBIO_总字节读 为 NBIO_总字节读 加 大小。
    dev 为 做 块设备_获取 用 设备号。
    若 dev 等于 空：
        返还 错误码_无设备。
    完。
    扁区 为 偏移 除以 dev 的 扁区大小。
    结果 为 做 块设备_读 用 设备号、偏移、大小。
    做 NBIO_预读检查 用 dev、扁区。
    返还 结果。
完。

块设备_同步写 接受 设备号、偏移、数据、大小：
    NBIO_总写数 为 NBIO_总写数 加 1。
    NBIO_总字节写 为 NBIO_总字节写 加 大小。
    返还 做 块设备_写 用 设备号、偏移、数据、大小。
完。

NBIO_初始化 接受 零参：
    NBIO_总读数 为 0。
    NBIO_总写数 为 0。
    NBIO_合并数 为 0。
    NBIO_预读命中 为 0。
    NBIO_预读未中 为 0。
    NBIO_写回数 为 0。
    NBIO_总字节读 为 0。
    NBIO_总字节写 为 0。
    NBIO_设备数 为 0。
    做 内核信息 用 「NBIO: 块设备IO子系统已初始化」。
完。

NBIO_遍历设备 接受 回调：
    可变 i 为 0。
    只要 i 小于 NBIO_设备数：
        做 回调 用 NBIO_设备表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

NBIO_获取设备数 接受 零参：
    返还 NBIO_设备数。
完。

NBIO_重置统计 接受 零参：
    NBIO_总读数 为 0。
    NBIO_总写数 为 0。
    NBIO_合并数 为 0。
    NBIO_预读命中 为 0。
    NBIO_预读未中 为 0。
    NBIO_写回数 为 0。
    NBIO_总字节读 为 0。
    NBIO_总字节写 为 0。
完。

本章 收束。
