《epoll插入与查找》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·同步原语》。

引用《..·中枢·哈希函数》。

引用《.·文件描述符》。

引用《..·中枢·整数转字符串》。

Nova事件轮询_插入 接受 ep、fd、文件、事件：
    可变 epi 为 做 分配内存 用 做 大小 用 Nova事件轮询监控项。
    epi 的 fd 为 fd。
    epi 的 文件 为 文件。
    epi 的 事件 为 事件。
    epi 的 Nova事件轮询实例 为 ep。
    epi 的 已就绪 为 假。
    epi 的 已禁用 为 假。
    做 红黑树_插入 用 ep 的 监控树、fd、epi。
    ep 的 监控项数 为 ep 的 监控项数 加 1。
    做 文件_注册poll回调 用 文件、Nova事件轮询_唤醒回调、epi。
    当前事件 为 做 文件_poll 用 文件、0。
    若 当前事件 位与 epi 的 事件 的 事件：
        做 Nova事件轮询_标记就绪 用 ep、epi、当前事件。
    完。
    返还 0。
完。

Nova事件轮询_移除 接受 ep、epi：
    做 红黑树_删除 用 ep 的 监控树、epi 的 fd。
    若 epi 的 已就绪：
        做 Nova事件轮询_从就绪链表移除 用 ep、epi。
    完。
    做 文件_注销poll回调 用 epi 的 文件、epi。
    ep 的 监控项数 为 ep 的 监控项数 减 1。
    做 释放内存 用 epi。
完。

Nova事件轮询_查找监控项 接受 ep、fd：
    返还 做 红黑树_查找 用 ep 的 监控树、fd。
完。

Nova事件轮询_标记就绪 接受 ep、epi、事件：
    做 自旋锁_获取 用 ep 的 就绪锁。
    若 非 epi 的 已就绪：
        epi 的 已就绪 为 真。
        epi 的 就绪链接 为 ep 的 就绪链表。
        ep 的 就绪链表 为 epi。
        ep 的 就绪数 为 ep 的 就绪数 加 1。
    完。
    做 自旋锁_释放 用 ep 的 就绪锁。
    做 等待队列_唤醒一个 用 ep 的 等待队列。
完。

Nova事件轮询_从就绪链表移除 接受 ep、epi：
    做 自旋锁_获取 用 ep 的 就绪锁。
    若 非 epi 的 已就绪：
        做 自旋锁_释放 用 ep 的 就绪锁。
        返还。
    完。
    可变 前一个 为 空。
    可变 当前 为 ep 的 就绪链表。
    只要 当前 不等于 空：
        若 当前 等于 epi：
            若 前一个 等于 空：
                ep 的 就绪链表 为 当前 的 就绪链接。
            否则：
                前一个 的 就绪链接 为 当前 的 就绪链接。
            完。
            ep 的 就绪数 为 ep 的 就绪数 减 1。
            epi 的 已就绪 为 假。
            跳出。
        完。
        前一个 为 当前。
        当前 为 当前 的 就绪链接。
    完。
    做 自旋锁_释放 用 ep 的 就绪锁。
完。

Nova事件轮询_收集就绪 接受 ep、事件数组、最大数：
    做 自旋锁_获取 用 ep 的 就绪锁。
    可变 收集数 为 0。
    可变 当前 为 ep 的 就绪链表。
    只要 当前 不等于 空 并且 收集数 小于 最大数：
        下一个 为 当前 的 就绪链接。
        当前事件 为 做 文件_poll 用 当前 的 文件、0。
        关心事件 为 当前事件 位与 当前 的 事件 的 事件。
        若 关心事件 不等于 0：
            事件数组 的 第 收集数 项 的 事件 为 关心事件。
            事件数组 的 第 收集数 项 的 数据 为 当前 的 事件 的 数据。
            收集数 为 收集数 加 1。
            若 当前 的 事件 的 事件 位与 Nova事件轮询ET：
                当前 的 已就绪 为 假。
            完。
            若 当前 的 事件 的 事件 位与 Nova事件轮询ONESHOT：
                当前 的 已禁用 为 真。
                当前 的 已就绪 为 假。
            完。
        否则：
            当前 的 已就绪 为 假。
        完。
        当前 为 下一个。
    完。
    ep 的 就绪链表 为 空。
    ep 的 就绪数 为 0。
    做 自旋锁_释放 用 ep 的 就绪锁。
    返还 收集数。
完。

Nova事件轮询_修改 接受 ep、fd、新事件：
    epi 为 做 Nova事件轮询_查找监控项 用 ep、fd。
    若 epi 等于 空：
        返还 错误_不存在。
    完。
    做 自旋锁_获取 用 ep 的 就绪锁。
    epi 的 事件 为 新事件。
    epi 的 已禁用 为 假。
    做 自旋锁_释放 用 ep 的 就绪锁。
    当前事件 为 做 文件_poll 用 epi 的 文件、0。
    若 当前事件 位与 新事件 的 事件：
        做 Nova事件轮询_标记就绪 用 ep、epi、当前事件。
    完。
    返还 0。
完。

Nova事件轮询_删除 接受 ep、fd：
    epi 为 做 Nova事件轮询_查找监控项 用 ep、fd。
    若 epi 等于 空：
        返还 错误_不存在。
    完。
    做 Nova事件轮询_移除 用 ep、epi。
    返还 0。
完。

Nova事件轮询_禁用监控 接受 ep、fd：
    epi 为 做 Nova事件轮询_查找监控项 用 ep、fd。
    若 epi 等于 空：
        返还 错误_不存在。
    完。
    epi 的 已禁用 为 真。
    若 epi 的 已就绪：
        做 Nova事件轮询_从就绪链表移除 用 ep、epi。
    完。
    返还 0。
完。

Nova事件轮询_启用监控 接受 ep、fd：
    epi 为 做 Nova事件轮询_查找监控项 用 ep、fd。
    若 epi 等于 空：
        返还 错误_不存在。
    完。
    epi 的 已禁用 为 假。
    当前事件 为 做 文件_poll 用 epi 的 文件、0。
    若 当前事件 位与 epi 的 事件 的 事件：
        做 Nova事件轮询_标记就绪 用 ep、epi、当前事件。
    完。
    返还 0。
完。

Nova事件轮询IN 为 0x001。

Nova事件轮询PRI 为 0x002。

Nova事件轮询OUT 为 0x004。

Nova事件轮询RDNORM 为 0x040。

Nova事件轮询RDBAND 为 0x080。

Nova事件轮询WRNORM 为 0x100。

Nova事件轮询WRBAND 为 0x200。

Nova事件轮询MSG 为 0x400。

Nova事件轮询ERR 为 0x008。

Nova事件轮询HUP 为 0x010。

Nova事件轮询RDHUP 为 0x2000。

Nova事件轮询ET 为 0x80000000。

Nova事件轮询ONESHOT 为 0x40000000。

Nova事件轮询EXCLUSIVE 为 0x10000000。

Nova事件轮询_事件名 接受 事件：
    可变 名称 为 「」。
    若 事件 位与 Nova事件轮询IN：
        名称 为 名称 加 「IN 」。
    完。
    若 事件 位与 Nova事件轮询OUT：
        名称 为 名称 加 「OUT 」。
    完。
    若 事件 位与 Nova事件轮询ERR：
        名称 为 名称 加 「ERR 」。
    完。
    若 事件 位与 Nova事件轮询HUP：
        名称 为 名称 加 「HUP 」。
    完。
    若 事件 位与 Nova事件轮询RDHUP：
        名称 为 名称 加 「RDHUP 」。
    完。
    若 事件 位与 Nova事件轮询PRI：
        名称 为 名称 加 「PRI 」。
    完。
    若 事件 位与 Nova事件轮询ET：
        名称 为 名称 加 「ET 」。
    完。
    若 事件 位与 Nova事件轮询ONESHOT：
        名称 为 名称 加 「ONESHOT 」。
    完。
    若 名称 等于 「」：
        返还 「NONE」。
    完。
    返还 名称。
完。

Nova事件轮询_是边沿触发 接受 事件：
    返还 事件 位与 Nova事件轮询ET 不等于 0。
完。

Nova事件轮询_是单次触发 接受 事件：
    返还 事件 位与 Nova事件轮询ONESHOT 不等于 0。
完。

Nova事件轮询_是可读事件 接受 事件：
    返还 事件 位与 (Nova事件轮询IN 位或 Nova事件轮询RDNORM 位或 Nova事件轮询PRI) 不等于 0。
完。

Nova事件轮询_是可写事件 接受 事件：
    返还 事件 位与 (Nova事件轮询OUT 位或 Nova事件轮询WRNORM) 不等于 0。
完。

Nova事件轮询_是错误事件 接受 事件：
    返还 事件 位与 (Nova事件轮询ERR 位或 Nova事件轮询HUP) 不等于 0。
完。

红黑树节点 是 一种 事物，含 键、值、左子、右子、父节点、颜色。

红色 为 0。

黑色 为 1。

红黑树_创建 接受 零参：
    返还 空。
完。

红黑树_查找 接受 根、键：
    可变 当前 为 根。
    只要 当前 不等于 空：
        若 键 小于 当前 的 键：
            当前 为 当前 的 左子。
        否则若 键 大于 当前 的 键：
            当前 为 当前 的 右子。
        否则：
            返还 当前 的 值。
        完。
    完。
    返还 空。
完。

红黑树_插入 接受 根、键、值：
    节点 为 做 分配内存 用 做 大小 用 红黑树节点。
    节点 的 键 为 键。
    节点 的 值 为 值。
    节点 的 左子 为 空。
    节点 的 右子 为 空。
    节点 的 颜色 为 红色。
    若 根 等于 空：
        节点 的 颜色 为 黑色。
        返还 节点。
    完。
    可变 当前 为 根。
    可变 父 为 空。
    只要 当前 不等于 空：
        父 为 当前。
        若 键 小于 当前 的 键：
            当前 为 当前 的 左子。
        否则若 键 大于 当前 的 键：
            当前 为 当前 的 右子。
        否则：
            当前 的 值 为 值。
            做 释放内存 用 节点。
            返还 根。
        完。
    完。
    节点 的 父节点 为 父。
    若 键 小于 父 的 键：
        父 的 左子 为 节点。
    否则：
        父 的 右子 为 节点。
    完。
    返还 根。
完。

红黑树_删除 接受 根、键：
    可变 当前 为 根。
    只要 当前 不等于 空：
        若 键 小于 当前 的 键：
            当前 为 当前 的 左子。
        否则若 键 大于 当前 的 键：
            当前 为 当前 的 右子。
        否则：
            做 释放内存 用 当前。
            返还 根。
        完。
    完。
    返还 根。
完。

红黑树_节点数 接受 根：
    若 根 等于 空：
        返还 0。
    完。
    返还 1 加 做 红黑树_节点数 用 根 的 左子 加 做 红黑树_节点数 用 根 的 右子。
完。

红黑树_最小值 接受 根：
    若 根 等于 空：
        返还 空。
    完。
    可变 当前 为 根。
    只要 当前 的 左子 不等于 空：
        当前 为 当前 的 左子。
    完。
    返还 当前 的 值。
完。

红黑树_最大值 接受 根：
    若 根 等于 空：
        返还 空。
    完。
    可变 当前 为 根。
    只要 当前 的 右子 不等于 空：
        当前 为 当前 的 右子。
    完。
    返还 当前 的 值。
完。

Nova事件轮询实例 是 一种 事物，含 监控树、监控项数、就绪链表、就绪数、就绪锁、等待队列、已关闭。

Nova事件轮询监控项 是 一种 事物，含 fd、文件、事件、Nova事件轮询实例、已就绪、已禁用、就绪链接。

Nova事件轮询事件 是 一种 事物，含 事件、数据。

Nova事件轮询_创建 接受 零参：
    ep 为 做 分配内存 用 做 大小 用 Nova事件轮询实例。
    ep 的 监控树 为 做 红黑树_创建。
    ep 的 监控项数 为 0。
    ep 的 就绪链表 为 空。
    ep 的 就绪数 为 0。
    ep 的 就绪锁 为 做 自旋锁_创建。
    ep 的 等待队列 为 做 等待队列_创建。
    ep 的 已关闭 为 假。
    返还 ep。
完。

Nova事件轮询_销毁 接受 ep：
    若 ep 等于 空：
        返还。
    完。
    ep 的 已关闭 为 真。
    ep 的 监控树 为 空。
    ep 的 就绪链表 为 空。
    ep 的 监控项数 为 0。
    ep 的 就绪数 为 0。
    做 释放内存 用 ep。
完。

可变 epoll_创建总数 为 0。

可变 epoll_销毁总数 为 0。

可变 epoll_插入总数 为 0。

可变 epoll_移除总数 为 0。

可变 epoll_修改总数 为 0。

可变 epoll_就绪收集总数 为 0。

可变 epoll_唤醒总数 为 0。

Nova事件轮询_统计 接受 零参：
    返还 一本字典：创建 对应 epoll_创建总数、销毁 对应 epoll_销毁总数、插入 对应 epoll_插入总数、移除 对应 epoll_移除总数、修改 对应 epoll_修改总数、就绪收集 对应 epoll_就绪收集总数、唤醒 对应 epoll_唤醒总数。
完。

Nova事件轮询_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    活跃 为 epoll_创建总数 减 epoll_销毁总数。
    若 活跃 大于 1000：
        做 追加 用 问题、「事件轮询实例过多: 」 加 做 整数转字符串 用 活跃。
    完。
    若 epoll_插入总数 大于 epoll_移除总数 乘 3 并且 epoll_移除总数 大于 100：
        做 追加 用 问题、「监控项泄漏: 插入远多于移除」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「事件轮询健康」。
    完。
    返还 问题。
完。

Nova事件轮询_重置统计 接受 零参：
    epoll_创建总数 为 0。
    epoll_销毁总数 为 0。
    epoll_插入总数 为 0。
    epoll_移除总数 为 0。
    epoll_修改总数 为 0。
    epoll_就绪收集总数 为 0。
    epoll_唤醒总数 为 0。
完。

Nova事件轮询_实例诊断 接受 ep：
    若 ep 等于 空：
        返还 「实例为空」。
    完。
    返还 一本字典：监控项数 对应 ep 的 监控项数、就绪数 对应 ep 的 就绪数、已关闭 对应 ep 的 已关闭。
完。

Nova事件轮询_完整统计 接受 零参：
    返还 一组：epoll_创建总数、epoll_销毁总数、epoll_插入总数、epoll_移除总数、epoll_修改总数、epoll_就绪收集总数、epoll_唤醒总数。
完。

可变 AI_事件轮插入_热节点缓存 为 0。

AI_事件轮插入_节点热度 接受 节点、操作类型：
    若 操作类型 等于 「查询」：
        返还 「缓存」。
    完。
    若 操作类型 等于 「修改」：
        AI_事件轮插入_热节点缓存 为 0。
        返还 「失效」。
    完。
    返还 「普通」。
完。

AI_事件轮插入_批量DOM优化 接受 操作数：
    若 操作数 大于 100：
        返还 「延迟重排」。
    完。
    若 操作数 大于 10：
        返还 「批量重排」。
    完。
    返还 「即时重排」。
完。

事件轮插入_统计 接受 零参：
    返还 一本字典：epoll创建总数 对应 epoll_创建总数、epoll销毁总数 对应 epoll_销毁总数、epoll插入总数 对应 epoll_插入总数、epoll移除总数 对应 epoll_移除总数、epoll修改总数 对应 epoll_修改总数、epoll就绪收集总数 对应 epoll_就绪收集总数、epoll唤醒总数 对应 epoll_唤醒总数。
完。

事件轮插入_健康评估 接受 零参：
    可变 问题 为 一组：（空）。
    若 epoll_创建总数 大于 10 并且 epoll_创建总数 减 epoll_销毁总数 大于 100：
        做 追加 用 问题、「epoll实例泄漏」。
    完。
    若 epoll_插入总数 大于 100 并且 epoll_插入总数 减 epoll_移除总数 大于 1000：
        做 追加 用 问题、「epoll监控项泄漏」。
    完。
    返还 做 计数 用 问题 等于 0。
完。

事件轮插入_综合报告 接受 零参：
    做 打印 用 「[事件轮插入] ═══ 综合报告 ═══」。
    做 打印 用 「  创建=」 加 做 整数转字符串 用 epoll_创建总数 加 「 销毁=」 加 做 整数转字符串 用 epoll_销毁总数 加 「 插入=」 加 做 整数转字符串 用 epoll_插入总数 加 「 移除=」 加 做 整数转字符串 用 epoll_移除总数。
    做 打印 用 「  修改=」 加 做 整数转字符串 用 epoll_修改总数 加 「 就绪=」 加 做 整数转字符串 用 epoll_就绪收集总数 加 「 唤醒=」 加 做 整数转字符串 用 epoll_唤醒总数。
    做 打印 用 「  健康: 」 加 若 做 事件轮插入_健康评估 则 「正常」 否则 「异常」。
完。

事件轮插入_全局重置 接受 零参：
    epoll_创建总数 为 0。
    epoll_销毁总数 为 0。
    epoll_插入总数 为 0。
    epoll_移除总数 为 0。
    epoll_修改总数 为 0。
    epoll_就绪收集总数 为 0。
    epoll_唤醒总数 为 0。
    做 打印 用 「[事件轮插入] 统计已重置」。
    返还 0。
完。

本章 收束。
