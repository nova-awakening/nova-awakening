《ext4_extent树分裂》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《.·ext4_extent删除》。

引用《.·ext4块分配器》。

引用《..·中枢·哈希函数》。

引用《..·稳态·AHCI驱动全局》。

ext4_extent_分裂根 接受 sb、Nova文件节点信息：
    信息 为 sb 的 私有信息。
    可变 旧头 为 Nova文件节点信息 的 extent树。
    新块 为 做 ext4_分配块 用 sb、1。
    若 新块 等于 0：
        返还 错误码_无空间。
    完。
    可变 叶缓冲 为 做 分配内存 用 信息 的 块大小。
    可变 叶头 为 新建 Ext4Extent树头。
    叶头 的 魔数 为 EXT4_EXTENT_魔数。
    叶头 的 有效项数 为 旧头 的 有效项数。
    叶头 的 最大项数 为 做 ext4_extent_计算叶最大项数 用 信息 的 块大小。
    叶头 的 深度 为 0。
    叶头 的 代数 为 旧头 的 代数。
    可变 i 为 0。
    只要 i 小于 旧头 的 有效项数：
        项 为 做 ext4_extent_获取第N项 用 旧头、i。
        做 ext4_extent_设置第N项 用 叶头、i、项。
        i 为 i 加 1。
    完。
    做 ext4_extent_写回头 用 叶缓冲、叶头。
    做 块设备_写 用 sb 的 设备、新块 乘 信息 的 块大小、叶缓冲、信息 的 块大小。
    做 释放内存 用 叶缓冲。
    首逻辑块 为 做 ext4_extent_获取第N项 用 旧头、0 的 逻辑块。
    旧头 的 有效项数 为 1。
    旧头 的 深度 为 旧头 的 深度 加 1。
    旧头 的 最大项数 为 (60 减 EXT4_EXTENT头_大小) 除以 EXT4_EXTENT索引_大小。
    可变 新索引 为 新建 Ext4Extent索引。
    新索引 的 逻辑块 为 首逻辑块。
    做 ext4_索引_设置叶块 用 新索引、新块。
    做 ext4_extent_设置第N索引 用 旧头、0、新索引。
    旧头 的 代数 为 旧头 的 代数 加 1。
    Nova文件节点信息 的 extent树 为 旧头。
    返还 0。
完。

ext4_extent_分裂叶 接受 sb、Nova文件节点信息、叶头、缓冲、旧块号：
    信息 为 sb 的 私有信息。
    新块 为 做 ext4_分配块 用 sb、1。
    若 新块 等于 0：
        返还 错误码_无空间。
    完。
    分裂点 为 叶头 的 有效项数 除以 2。
    可变 新叶缓冲 为 做 分配内存 用 信息 的 块大小。
    可变 新叶头 为 新建 Ext4Extent树头。
    新叶头 的 魔数 为 EXT4_EXTENT_魔数。
    新叶头 的 有效项数 为 叶头 的 有效项数 减 分裂点。
    新叶头 的 最大项数 为 做 ext4_extent_计算叶最大项数 用 信息 的 块大小。
    新叶头 的 深度 为 0。
    可变 i 为 0。
    只要 i 小于 新叶头 的 有效项数：
        项 为 做 ext4_extent_获取第N项 用 叶头、分裂点 加 i。
        做 ext4_extent_设置第N项 用 新叶头、i、项。
        i 为 i 加 1。
    完。
    叶头 的 有效项数 为 分裂点。
    做 ext4_extent_写回头 用 新叶缓冲、新叶头。
    做 块设备_写 用 sb 的 设备、新块 乘 信息 的 块大小、新叶缓冲、信息 的 块大小。
    做 释放内存 用 新叶缓冲。
    新首逻辑块 为 做 ext4_extent_获取第N项 用 新叶头、0 的 逻辑块。
    返还 做 ext4_extent_索引插入 用 sb、Nova文件节点信息、新首逻辑块、新块。
完。

ext4_extent_索引插入 接受 sb、Nova文件节点信息、逻辑块、子块号：
    可变 头 为 Nova文件节点信息 的 extent树。
    若 头 的 有效项数 大于或等于 头 的 最大项数：
        返还 错误码_无空间。
    完。
    可变 新索引 为 新建 Ext4Extent索引。
    新索引 的 逻辑块 为 逻辑块。
    做 ext4_索引_设置叶块 用 新索引、子块号。
    可变 插入位 为 头 的 有效项数。
    可变 i 为 0。
    只要 i 小于 头 的 有效项数：
        索引 为 做 ext4_extent_获取第N索引 用 头、i。
        若 逻辑块 小于 索引 的 逻辑块：
            插入位 为 i。
            跳出。
        完。
        i 为 i 加 1。
    完。
    可变 j 为 头 的 有效项数。
    只要 j 大于 插入位：
        源 为 做 ext4_extent_获取第N索引 用 头、j 减 1。
        做 ext4_extent_设置第N索引 用 头、j、源。
        j 为 j 减 1。
    完。
    做 ext4_extent_设置第N索引 用 头、插入位、新索引。
    头 的 有效项数 为 头 的 有效项数 加 1。
    Nova文件节点信息 的 extent树 为 头。
    返还 0。
完。

本章 收束。
