《VFS文件操作》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·硬链接》。

引用《.·文件节点创建》。

引用《.·文件描述符》。

引用《.·目录项添加》。

引用《.·路径查找》。

引用《..·中枢·字符串替换》。

引用《.·路径缓存初始化》。

引用《..·中枢·释放文件表》。

引用《..·中枢·整数转字符串》。

引用《..·中枢·进程fork创建》。

引用《..·中枢·网络系统调用》。

引用《..·中枢·每处理器》。

引用《..·中枢·虚拟内存区》。

引用《.·VFS文件读写》。

引用《.·文件节点缓存》。

O_RDONLY 为 0x0000。

O_WRONLY 为 0x0001。

O_RDWR 为 0x0002。

O_CREAT 为 0x0040。

O_EXCL 为 0x0080。

O_NOCTTY 为 0x0100。

O_TRUNC 为 0x0200。

O_APPEND 为 0x0400。

O_NONBLOCK 为 0x0800。

O_SYNC 为 0x101000。

O_DIRECTORY 为 0x10000。

O_NOFOLLOW 为 0x20000。

O_CLOEXEC 为 0x80000。

S_IRWXU 为 448。

S_IRUSR 为 256。

S_IWUSR 为 128。

S_IXUSR 为 64。

S_IRWXG 为 56。

S_IRWXO 为 7。

可变 打开_总次数 为 0。

可变 关闭_总次数 为 0。

可变 dup_总次数 为 0。

系统_open 接受 路径、标志、模式：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件表 为 当前 的 第 T_文件表 项。
    若 文件表 等于 空：
        返还 错误_坏文件描述符。
    完。
    fd 为 做 文件表_分配 用 文件表。
    若 fd 小于 0：
        返还 fd。
    完。
    根 为 做 获取根目录项。
    目录项 为 做 路径查找 用 根、路径。
    若 目录项 等于 空：
        若 标志 位与 O_CREAT 等于 0：
            返还 错误_不存在。
        完。
        创建结果 为 做 vfs_创建文件 用 路径、模式。
        若 创建结果 小于 0：
            返还 创建结果。
        完。
        目录项 为 做 路径查找 用 根、路径。
        若 目录项 等于 空：
            返还 错误_不存在。
        完。
    否则：
        若 标志 位与 O_CREAT 不等于 0 并且 标志 位与 O_EXCL 不等于 0：
            返还 错误_已存在。
        完。
    完。
    节点 为 做 目录项_Nova文件节点 用 目录项。
    若 标志 位与 O_DIRECTORY 不等于 0 并且 非 做 Nova节点_是目录 用 节点：
        返还 错误_不是目录。
    完。
    权限结果 为 做 检查访问权限 用 节点、标志。
    若 权限结果 不等于 成功：
        返还 权限结果。
    完。
    若 标志 位与 O_TRUNC 不等于 0 并且 做 Nova节点_是文件 用 节点：
        做 Nova文件节点_设置大小 用 节点、0。
    完。
    文件 为 做 文件对象_创建 用 节点、目录项、标志。
    若 文件 等于 空：
        返还 错误_内存不足。
    完。
    操作 为 文件 的 第 F_操作 项。
    若 操作 不等于 空 并且 操作 的 打开 不等于 空：
        打开结果 为 做 操作 的 打开 用 节点、文件。
        若 打开结果 不等于 成功：
            返还 打开结果。
        完。
    完。
    做 文件表_设置 用 文件表、fd、文件。
    打开_总次数 为 打开_总次数 加 1。
    返还 fd。
完。

系统_close 接受 fd：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 错误_坏文件描述符。
    完。
    文件表 为 当前 的 第 T_文件表 项。
    文件 为 做 文件表_获取 用 文件表、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    做 文件表_关闭 用 文件表、fd。
    剩余 为 做 文件_减引用 用 文件。
    若 剩余 小于或等于 0：
        操作 为 文件 的 第 F_操作 项。
        若 操作 不等于 空 并且 操作 的 释放 不等于 空：
            做 操作 的 释放 用 文件 的 第 F_Nova文件节点 项、文件。
        完。
    完。
    关闭_总次数 为 关闭_总次数 加 1。
    返还 成功。
完。

系统_dup 接受 旧fd：
    当前 为 做 获取当前进程。
    文件表 为 当前 的 第 T_文件表 项。
    文件 为 做 文件表_获取 用 文件表、旧fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    新fd 为 做 文件表_分配 用 文件表。
    若 新fd 小于 0：
        返还 新fd。
    完。
    做 文件_增引用 用 文件。
    做 文件表_设置 用 文件表、新fd、文件。
    dup_总次数 为 dup_总次数 加 1。
    返还 新fd。
完。

系统_dup2 接受 旧fd、新fd：
    当前 为 做 获取当前进程。
    文件表 为 当前 的 第 T_文件表 项。
    文件 为 做 文件表_获取 用 文件表、旧fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    若 新fd 小于 0 或者 新fd 大于或等于 最大打开文件：
        返还 错误_坏文件描述符。
    完。
    若 做 文件表_获取 用 文件表、新fd 不等于 空：
        做 系统_close 用 新fd。
    完。
    做 文件_增引用 用 文件。
    做 文件表_设置 用 文件表、新fd、文件。
    dup_总次数 为 dup_总次数 加 1。
    返还 新fd。
完。

ST_设备 为 0。

ST_Nova文件节点 为 1。

ST_模式 为 2。

ST_链接 为 3。

ST_UID 为 4。

ST_GID 为 5。

ST_大小 为 6。

ST_块数 为 7。

ST_访问时间 为 8。

ST_修改时间 为 9。

系统_stat 接受 路径：
    根 为 做 获取根目录项。
    目录项 为 做 路径查找 用 根、路径。
    若 目录项 等于 空：
        返还 错误_不存在。
    完。
    返还 做 Nova文件节点到stat 用 做 目录项_Nova文件节点 用 目录项。
完。

系统_fstat 接受 fd：
    当前 为 做 获取当前进程。
    文件 为 做 文件表_获取 用 当前 的 第 T_文件表 项、fd。
    若 文件 等于 空：
        返还 错误_坏文件描述符。
    完。
    返还 做 Nova文件节点到stat 用 文件 的 第 F_Nova文件节点 项。
完。

Nova文件节点到stat 接受 节点：
    若 节点 等于 空：
        返还 错误_无效参数。
    完。
    返还 一组：0、节点 的 第 I_编号 项、节点 的 第 I_权限 项 位或 节点 的 第 I_类型 项 左移 12、节点 的 第 I_链接 项、0、0、节点 的 第 I_大小 项、节点 的 第 I_大小 项 除以 512、节点 的 第 I_时间 项、节点 的 第 I_时间 项。
完。

文件对象_创建 接受 节点、目录项、标志：
    操作 为 空。
    若 节点 的 第 I_操作 项 不等于 空：
        操作 为 节点 的 第 I_操作 项 的 文件操作。
    完。
    返还 一组：节点、0、标志、1、操作、目录项、空。
完。

检查访问权限 接受 节点、打开标志：
    若 节点 等于 空：
        返还 错误_不存在。
    完。
    模式 为 做 Nova文件节点_权限 用 节点、4。
    若 打开标志 位与 O_RDONLY 不等于 0 或者 打开标志 位与 O_RDWR 不等于 0：
        若 模式 位与 292 等于 0：
            返还 错误_权限。
        完。
    完。
    若 打开标志 位与 O_WRONLY 不等于 0 或者 打开标志 位与 O_RDWR 不等于 0：
        若 模式 位与 146 等于 0：
            返还 错误_权限。
        完。
    完。
    返还 成功。
完。

vfs_创建文件 接受 路径、模式：
    分隔位 为 做 字符串最后索引 用 路径、「/」。
    父路径 为 做 如果选字符 用 分隔位 大于 0、做 字符串子串 用 路径、0、分隔位、「/」。
    文件名 为 做 字符串子串 用 路径、分隔位 加 1、做 字符串长度 用 路径。
    根 为 做 获取根目录项。
    父目录项 为 做 路径查找 用 根、父路径。
    若 父目录项 等于 空：
        返还 错误_不存在。
    完。
    父节点 为 做 目录项_Nova文件节点 用 父目录项。
    若 非 做 Nova节点_是目录 用 父节点：
        返还 错误_不是目录。
    完。
    新节点 为 做 Nova文件节点_创建 用 类型_普通文件、模式。
    新目录项 为 做 目录项_创建 用 文件名、新节点、父目录项。
    做 目录项_添加子 用 父目录项、新目录项。
    操作 为 父节点 的 第 I_操作 项。
    若 操作 不等于 空 并且 操作 的 创建 不等于 空：
        做 操作 的 创建 用 父节点、新目录项、模式。
    完。
    返还 成功。
完。

VFS_打开关闭统计 接受 零参：
    返还 一组：打开_总次数、关闭_总次数、dup_总次数。
完。

VFS打开关闭_完整统计 接受 零参：
    返还 一组：打开_总次数、关闭_总次数、dup_总次数。
完。

VFS打开关闭_健康检查 接受 零参：
    可变 问题 为 一组：（空）。
    未关闭 为 打开_总次数 减 关闭_总次数。
    若 未关闭 大于 1000：
        做 追加 用 问题、「文件描述符泄漏: 」 加 做 整数转字符串 用 未关闭 加 「个未关闭」。
    完。
    若 做 计数 用 问题 等于 0：
        返还 「VFS打开关闭健康」。
    完。
    返还 问题。
完。

VFS_获取打开文件数 接受 零参：
    返还 打开_总次数 减 关闭_总次数。
完。

VFS_文件泄漏检查 接受 零参：
    返还 打开_总次数 减 关闭_总次数 大于 1000。
完。

可变 AI_文件层打开_热文件表 为 一组：（空）。

AI_文件层打开_访问模式 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 AI_文件层打开_热文件表：
        若 AI_文件层打开_热文件表 的 第 i 项 的 路径 等于 路径：
            AI_文件层打开_热文件表 的 第 i 项 的 频率 为 AI_文件层打开_热文件表 的 第 i 项 的 频率 加 1。
            返还 AI_文件层打开_热文件表 的 第 i 项 的 频率。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 AI_文件层打开_热文件表 小于 128：
        做 追加 用 AI_文件层打开_热文件表、一本字典：路径 对应 路径、频率 对应 1。
    完。
    返还 1。
完。

AI_文件层打开_预读建议 接受 文件大小、已读偏移：
    若 已读偏移 大于 文件大小 乘 80 除以 100：
        返还 0。
    完。
    返还 32768。
完。

本章 收束。
