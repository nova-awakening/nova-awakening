《proc格式化写》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·负载统计》。

引用《..·中枢·哈希整数》。

引用《..·中枢·退出调用》。

引用《..·中枢·内容可见性》。

引用《..·中枢·整数转字符串》。

最大条目 为 128。

条目_目录 为 0。

条目_文件 为 1。

进程文件条目 是 一种 事物，含 名称哈希、类型、读回调、写回调、父条目、数据指针。

可变 条目表 为 一组：（空）。

可变 条目数 为 0。

可变 读次数 为 0。

可变 写次数 为 0。

格式化写 接受 缓冲、文本：
    若 缓冲 等于 0 或者 文本 等于 空：
        返还 0。
    完。
    可变 i 为 0。
    长 为 做 字符串长度 用 文本。
    只要 i 小于 长：
        做 内存_写8 用 缓冲 加 i、做 字符编码 用 文本、i。
        i 为 i 加 1。
    完。
    返还 i。
完。

整数写 接受 缓冲、值：
    返还 做 格式化写 用 缓冲、做 整数转字符串 用 值。
完。

进程文件_初始化 接受 零参：
    条目表 为 一组：（空）。
    条目数 为 0。
    做 进程文件_注册目录 用 「」、0。
    做 进程文件_注册文件 用 「内存信息」、0、进程文件_读内存、0。
    做 进程文件_注册文件 用 「处理器信息」、0、进程文件_读处理器、0。
    做 进程文件_注册文件 用 「版本」、0、进程文件_读版本、0。
    做 进程文件_注册文件 用 「运行时间」、0、进程文件_读运行时间、0。
    做 进程文件_注册文件 用 「负载均值」、0、进程文件_读负载、0。
    做 进程文件_注册文件 用 「中断」、0、进程文件_读中断、0。
完。

进程文件_注册目录 接受 名称、父：
    若 条目数 大于或等于 最大条目：
        返还 负 1。
    完。
    条目 为 新建 进程文件条目。
    条目 的 名称哈希 为 做 哈希_字符串 用 名称。
    条目 的 类型 为 条目_目录。
    条目 的 读回调 为 0。
    条目 的 写回调 为 0。
    条目 的 父条目 为 父。
    条目 的 数据指针 为 0。
    做 追加 用 条目表、条目。
    索引 为 条目数。
    条目数 为 条目数 加 1。
    返还 索引。
完。

进程文件_注册文件 接受 名称、父、读回调、数据：
    若 条目数 大于或等于 最大条目：
        返还 负 1。
    完。
    条目 为 新建 进程文件条目。
    条目 的 名称哈希 为 做 哈希_字符串 用 名称。
    条目 的 类型 为 条目_文件。
    条目 的 读回调 为 读回调。
    条目 的 写回调 为 0。
    条目 的 父条目 为 父。
    条目 的 数据指针 为 数据。
    做 追加 用 条目表、条目。
    索引 为 条目数。
    条目数 为 条目数 加 1。
    返还 索引。
完。

进程文件_读 接受 条目索引、缓冲、最大长：
    若 条目索引 大于或等于 条目数：
        返还 错误_无效参数。
    完。
    读次数 为 读次数 加 1。
    条目 为 条目表 的 第 条目索引 项。
    若 条目 的 读回调 等于 0：
        返还 0。
    完。
    返还 做 条目 的 读回调 用 缓冲、最大长、条目 的 数据指针。
完。

进程文件_查找 接受 名称、父：
    目标 为 做 哈希_字符串 用 名称。
    可变 序号 为 0。
    只要 序号 小于 条目数：
        若 条目表 的 第 序号 项 的 名称哈希 等于 目标 并且 条目表 的 第 序号 项 的 父条目 等于 父：
            返还 序号。
        完。
        序号 为 序号 加 1。
    完。
    返还 负 1。
完。

进程文件_注册进程 接受 进程标识：
    进程目录 为 做 进程文件_注册目录 用 「」、0。
    若 进程目录 小于 0：
        返还。
    完。
    做 进程文件_注册文件 用 「状态」、进程目录、进程文件_读进程状态、进程标识。
    做 进程文件_注册文件 用 「命令行」、进程目录、进程文件_读命令行、进程标识。
    做 进程文件_注册文件 用 「映射」、进程目录、进程文件_读映射、进程标识。
完。

进程文件_读内存 接受 缓冲、最大长、数据：
    可变 写位 为 0。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「总内存: 」。
    写位 为 写位 加 做 整数写 用 缓冲 加 写位、做 获取总内存。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「\n空闲: 」。
    写位 为 写位 加 做 整数写 用 缓冲 加 写位、做 获取空闲内存。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「\n已用: 」。
    写位 为 写位 加 做 整数写 用 缓冲 加 写位、做 获取已用内存。
    返还 写位。
完。

进程文件_读处理器 接受 缓冲、最大长、数据：
    可变 写位 为 0。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「核心数: 」。
    写位 为 写位 加 做 整数写 用 缓冲 加 写位、做 获取核心数。
    返还 写位。
完。

进程文件_读版本 接受 缓冲、最大长、数据：
    返还 做 格式化写 用 缓冲、「觉醒 1.0.0」。
完。

进程文件_读运行时间 接受 缓冲、最大长、数据：
    可变 写位 为 0。
    写位 为 写位 加 做 整数写 用 缓冲、做 平台_获取高精度时间 除以 1000000。
    返还 写位。
完。

进程文件_读负载 接受 缓冲、最大长、数据：
    均值 为 做 负载_获取均值。
    可变 写位 为 0。
    写位 为 写位 加 做 整数写 用 缓冲、均值 的 第 0 项。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「 」。
    写位 为 写位 加 做 整数写 用 缓冲 加 写位、均值 的 第 1 项。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「 」。
    写位 为 写位 加 做 整数写 用 缓冲 加 写位、均值 的 第 2 项。
    返还 写位。
完。

进程文件_读中断 接受 缓冲、最大长、数据：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 做 格式化写 用 缓冲、「no-process\n」。
    完。
    可变 写位 为 0。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「voluntary 」。
    写位 为 写位 加 做 整数写 用 缓冲 加 写位、当前 的 第 T_自愿切换 项。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「\nnonvoluntary 」。
    写位 为 写位 加 做 整数写 用 缓冲 加 写位、当前 的 第 T_非自愿切换 项。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「\ncontext 」。
    写位 为 写位 加 做 整数写 用 缓冲 加 写位、当前 的 第 T_总切换 项。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「\n」。
    返还 写位。
完。

进程文件_读进程状态 接受 缓冲、最大长、数据：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 做 格式化写 用 缓冲、「unknown\n」。
    完。
    状态名 为 做 进程_状态名称 用 做 进程_状态 用 当前。
    可变 写位 为 0。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「state 」。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、状态名。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「\npid 」。
    写位 为 写位 加 做 整数写 用 缓冲 加 写位、做 进程_进程号 用 当前。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「\n」。
    返还 写位。
完。

进程文件_读命令行 接受 缓冲、最大长、数据：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 做 格式化写 用 缓冲、「\n」。
    完。
    名称 为 做 进程_名称 用 当前。
    若 名称 等于 空：
        名称 为 「」。
    完。
    可变 写位 为 0。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、名称。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「\n」。
    返还 写位。
完。

进程文件_读映射 接受 缓冲、最大长、数据：
    当前 为 做 获取当前进程。
    若 当前 等于 空：
        返还 做 格式化写 用 缓冲、「maps unavailable\n」。
    完。
    mm 为 做 进程_mm 用 当前。
    若 mm 等于 空：
        返还 做 格式化写 用 缓冲、「maps none\n」。
    完。
    可变 写位 为 0。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「vmas 」。
    写位 为 写位 加 做 整数写 用 缓冲 加 写位、做 计数 用 mm。
    写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「\n」。
    若 做 计数 用 mm 大于 0：
        第一个 为 mm 的 第 0 项。
        写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「first 」。
        写位 为 写位 加 做 整数写 用 缓冲 加 写位、第一个 的 第 0 项。
        写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「-」。
        写位 为 写位 加 做 整数写 用 缓冲 加 写位、第一个 的 第 1 项。
        写位 为 写位 加 做 格式化写 用 缓冲 加 写位、「\n」。
    完。
    返还 写位。
完。

进程文件_完整统计 接受 零参：
    返还 一组：条目数、读次数、写次数。
完。

本章 收束。
