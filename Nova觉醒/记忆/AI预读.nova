《AI预读》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·链表后继》。

文件读取画像 是 一种 事物，含 Nova文件节点号、上次偏移、上上次偏移、模式、预读窗口、预读命中、预读未命中、命中率EMA、步长。

可变 文件画像表 为 一组：（空）。

可变 读取模式 为 一组：（空）。

预读_最小 为 4。

预读_最大 为 128。

AI预读_初始化 接受 零参：
    做 内核信息 用 「AI预读: 每Nova文件节点模式检测+自适应窗口+步长预测」。
完。

AI预读_记录 接受 Nova文件节点号、偏移、大小：
    做 追加 用 读取模式、一组：Nova文件节点号、偏移、大小。
    若 做 计数 用 读取模式 大于 64：
        做 链表_删除 用 读取模式、0。
    完。
    画像 为 做 AI预读_获取或创建 用 Nova文件节点号。
    若 画像 的 上次偏移 大于 0：
        当前步长 为 偏移 减 画像 的 上次偏移。
        若 当前步长 大于 0 并且 当前步长 小于或等于 大小 乘 2：
            画像 的 模式 为 0。
            画像 的 步长 为 当前步长。
        否则若 画像 的 上上次偏移 大于 0：
            上次步长 为 画像 的 上次偏移 减 画像 的 上上次偏移。
            若 当前步长 等于 上次步长 并且 当前步长 不等于 0：
                画像 的 模式 为 2。
                画像 的 步长 为 当前步长。
            否则：
                画像 的 模式 为 1。
            完。
        否则：
            画像 的 模式 为 1。
        完。
    完。
    画像 的 上上次偏移 为 画像 的 上次偏移。
    画像 的 上次偏移 为 偏移。
    若 画像 的 模式 等于 0 或者 画像 的 模式 等于 2：
        若 画像 的 命中率EMA 大于 800：
            画像 的 预读窗口 为 画像 的 预读窗口 乘 3 除以 2。
        否则若 画像 的 命中率EMA 大于 500：
            画像 的 预读窗口 为 画像 的 预读窗口 加 2。
        完。
    否则：
        画像 的 预读窗口 为 画像 的 预读窗口 乘 2 除以 3。
    完。
    若 画像 的 预读窗口 小于 预读_最小：
        画像 的 预读窗口 为 预读_最小。
    完。
    若 画像 的 预读窗口 大于 预读_最大：
        画像 的 预读窗口 为 预读_最大。
    完。
完。

AI预读_记录命中 接受 Nova文件节点号：
    画像 为 做 AI预读_查找 用 Nova文件节点号。
    若 画像 不等于 空：
        画像 的 预读命中 为 画像 的 预读命中 加 1。
        画像 的 命中率EMA 为 (画像 的 命中率EMA 乘 922 加 1024 乘 102) 除以 1024。
    完。
完。

AI预读_记录未命中 接受 Nova文件节点号：
    画像 为 做 AI预读_查找 用 Nova文件节点号。
    若 画像 不等于 空：
        画像 的 预读未命中 为 画像 的 预读未命中 加 1。
        画像 的 命中率EMA 为 画像 的 命中率EMA 乘 922 除以 1024。
    完。
完。

AI预读_建议大小 接受 零参：
    若 做 计数 用 读取模式 小于 2：
        返还 预读_最小。
    完。
    最近 为 读取模式 的 第 做 计数 用 读取模式 减 1 项。
    画像 为 做 AI预读_查找 用 最近 的 第 0 项。
    若 画像 不等于 空：
        返还 画像 的 预读窗口。
    完。
    返还 预读_最小。
完。

AI预读_建议大小_Nova文件节点 接受 Nova文件节点号：
    画像 为 做 AI预读_查找 用 Nova文件节点号。
    若 画像 不等于 空：
        返还 画像 的 预读窗口。
    完。
    返还 预读_最小。
完。

AI预读_预测下次偏移 接受 Nova文件节点号：
    画像 为 做 AI预读_查找 用 Nova文件节点号。
    若 画像 等于 空 或者 画像 的 步长 等于 0：
        返还 负 1。
    完。
    返还 画像 的 上次偏移 加 画像 的 步长。
完。

AI预读_查找 接受 Nova文件节点号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 文件画像表：
        若 文件画像表 的 第 i 项 的 Nova文件节点号 等于 Nova文件节点号：
            返还 文件画像表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

AI预读_获取或创建 接受 Nova文件节点号：
    已有 为 做 AI预读_查找 用 Nova文件节点号。
    若 已有 不等于 空：
        返还 已有。
    完。
    新画像 为 新建 文件读取画像。
    新画像 的 Nova文件节点号 为 Nova文件节点号。
    新画像 的 预读窗口 为 预读_最小。
    新画像 的 命中率EMA 为 512。
    做 追加 用 文件画像表、新画像。
    返还 新画像。
完。

AI预读_模式名 接受 模式：
    若 模式 等于 0：
        返还 「sequential」。
    完。
    若 模式 等于 1：
        返还 「random」。
    完。
    若 模式 等于 2：
        返还 「strided」。
    完。
    返还 「unknown」。
完。

AI预读_完整统计 接受 零参：
    返还 一组：做 计数 用 文件画像表、做 计数 用 读取模式。
完。

本章 收束。
