《ext4块分配器》

引用《..·中枢·类型定义》。

引用《..·中枢·错误码》。

引用《..·中枢·同步原语》。

引用《.·ext4超级块读取》。

引用《.·ext4块组描述符》。

引用《.·文件文件》。

引用《.·ext4组分配器》。

引用《.·扩展位图查找》。

引用《..·中枢·字符串替换》。

引用《.·ext4分配统计》。

引用《..·中枢·墙钟设置》。

EXT4_MB_最小分配大小 为 1。

EXT4_MB_最大分配大小 为 2048。

EXT4_MB_预分配_文件 为 8。

EXT4_MB_预分配_目录 为 16。

EXT4_MB_流式分配阈值 为 16。

EXT4_MB_组扫描上限 为 10。

EXT4_MB_好伙伴阈值 为 128。

EXT4_MB_CR0_最大扫描组 为 4。

EXT4_MB_CR1_最大扫描组 为 32。

EXT4_MB_保留块比例 为 20。

EXT4_MB_标志_正常 为 0x0000。

EXT4_MB_标志_元数据 为 0x0001。

EXT4_MB_标志_精确 为 0x0002。

EXT4_MB_标志_流式 为 0x0004。

EXT4_MB_标志_延迟 为 0x0008。

EXT4_MB_标志_预分配 为 0x0010。

EXT4_MB_标志_强制单块 为 0x0020。

EXT4_MB_CR0_精确匹配 为 0。

EXT4_MB_CR1_好伙伴 为 1。

EXT4_MB_CR2_任意空闲 为 2。

EXT4_MB_CR3_强制单块 为 3。

Ext4分配请求 是 一种 事物，含 文件节点号、逻辑块、目标组、目标偏移、请求块数、标志。

Ext4分配结果 是 一种 事物，含 起始块、分配块数、组号、组内偏移。

Ext4组伙伴 是 一种 事物，含 组号、空闲块数、最大空闲段、碎片数、位图、位图已加载、锁、最后访问时间。

分配统计 是 一种 事物，含 总分配次数、总分配成功、总分配失败、总分配块数、CR0_精确匹配次数、CR0_精确匹配成功、CR1_好伙伴次数、CR1_好伙伴成功、CR2_任意空闲次数、CR2_任意空闲成功、CR3_强制单块次数、CR3_强制单块成功、目标组命中次数、目标组命中成功、预分配命中次数、扫描组总数、空间不足次数、保留块拒绝次数、简化分配次数。

可变 分配统计数据 为 新建 分配统计。

可变 伙伴缓存 为 一组：（空）。

伙伴缓存_最大数量 为 16。

ext4_分配块 接受 sb、请求：
    分配统计数据 的 简化分配次数 为 分配统计数据 的 简化分配次数 加 1。
    若 做 是整数 用 请求：
        可变 分配请求 为 新建 Ext4分配请求。
        分配请求 的 请求块数 为 请求。
        分配请求 的 标志 为 EXT4_MB_标志_正常。
        分配请求 的 目标组 为 0。
        返还 做 ext4_多块分配 用 sb、分配请求。
    完。
    返还 做 ext4_多块分配 用 sb、请求。
完。

ext4_多块分配 接受 sb、请求：
    分配统计数据 的 总分配次数 为 分配统计数据 的 总分配次数 加 1。
    若 sb 等于 空 或者 sb 的 私有信息 等于 空：
        分配统计数据 的 总分配失败 为 分配统计数据 的 总分配失败 加 1。
        返还 错误码_无效参数。
    完。
    信息 为 sb 的 私有信息。
    若 请求 的 请求块数 等于 0：
        分配统计数据 的 总分配失败 为 分配统计数据 的 总分配失败 加 1。
        返还 错误码_无效参数。
    完。
    若 请求 的 请求块数 大于 EXT4_MB_最大分配大小：
        请求 的 请求块数 为 EXT4_MB_最大分配大小。
    完。
    保留块数 为 信息 的 块总数 除以 EXT4_MB_保留块比例。
    若 信息 的 空闲块数 小于或等于 保留块数：
        若 请求 的 标志 位与 EXT4_MB_标志_元数据 等于 0：
            分配统计数据 的 保留块拒绝次数 为 分配统计数据 的 保留块拒绝次数 加 1。
            分配统计数据 的 总分配失败 为 分配统计数据 的 总分配失败 加 1。
            返还 错误码_无空间。
        完。
    完。
    若 信息 的 空闲块数 小于 请求 的 请求块数：
        分配统计数据 的 空间不足次数 为 分配统计数据 的 空间不足次数 加 1。
        分配统计数据 的 总分配失败 为 分配统计数据 的 总分配失败 加 1。
        做 打印 用 做 字符串拼接 用 「[ext4] 空闲块不足: 需要=」、做 转字符串 用 请求 的 请求块数。
        返还 错误码_无空间。
    完。
    若 请求 的 目标组 等于 0 并且 请求 的 文件节点号 大于 0：
        请求 的 目标组 为 做 ext4_计算目标组 用 信息、请求 的 文件节点号、请求 的 逻辑块。
    完。
    若 请求 的 目标组 小于 信息 的 块组数：
        分配统计数据 的 目标组命中次数 为 分配统计数据 的 目标组命中次数 加 1。
        空闲数 为 做 ext4_获取空闲块数 用 信息、请求 的 目标组。
        若 空闲数 大于或等于 请求 的 请求块数：
            结果 为 做 ext4_尝试组分配 用 sb、请求、请求 的 目标组。
            若 非 做 是错误 用 结果：
                分配统计数据 的 目标组命中成功 为 分配统计数据 的 目标组命中成功 加 1。
                做 ext4_更新分配统计 用 信息、结果。
                做 ext4_记录分配成功 用 结果。
                返还 结果。
            完。
        完。
    完。
    分配统计数据 的 CR0_精确匹配次数 为 分配统计数据 的 CR0_精确匹配次数 加 1。
    结果0 为 做 ext4_扫描组_精确 用 sb、请求。
    若 非 做 是错误 用 结果0：
        分配统计数据 的 CR0_精确匹配成功 为 分配统计数据 的 CR0_精确匹配成功 加 1。
        做 ext4_更新分配统计 用 信息、结果0。
        做 ext4_记录分配成功 用 结果0。
        返还 结果0。
    完。
    分配统计数据 的 CR1_好伙伴次数 为 分配统计数据 的 CR1_好伙伴次数 加 1。
    结果1 为 做 ext4_扫描组_好伙伴 用 sb、请求。
    若 非 做 是错误 用 结果1：
        分配统计数据 的 CR1_好伙伴成功 为 分配统计数据 的 CR1_好伙伴成功 加 1。
        做 ext4_更新分配统计 用 信息、结果1。
        做 ext4_记录分配成功 用 结果1。
        返还 结果1。
    完。
    分配统计数据 的 CR2_任意空闲次数 为 分配统计数据 的 CR2_任意空闲次数 加 1。
    结果2 为 做 ext4_扫描组_任意 用 sb、请求。
    若 非 做 是错误 用 结果2：
        分配统计数据 的 CR2_任意空闲成功 为 分配统计数据 的 CR2_任意空闲成功 加 1。
        做 ext4_更新分配统计 用 信息、结果2。
        做 ext4_记录分配成功 用 结果2。
        返还 结果2。
    完。
    若 请求 的 请求块数 大于 1：
        分配统计数据 的 CR3_强制单块次数 为 分配统计数据 的 CR3_强制单块次数 加 1。
        可变 单块请求 为 请求。
        单块请求 的 请求块数 为 1。
        结果3 为 做 ext4_扫描组_任意 用 sb、单块请求。
        若 非 做 是错误 用 结果3：
            分配统计数据 的 CR3_强制单块成功 为 分配统计数据 的 CR3_强制单块成功 加 1。
            做 ext4_更新分配统计 用 信息、结果3。
            做 ext4_记录分配成功 用 结果3。
            返还 结果3。
        完。
    完。
    分配统计数据 的 总分配失败 为 分配统计数据 的 总分配失败 加 1。
    做 打印 用 「[ext4] 块分配失败: 所有阶段均未成功」。
    返还 错误码_无空间。
完。

ext4_计算目标组 接受 信息、文件节点号、逻辑块：
    若 信息 的 块组数 等于 0：
        返还 0。
    完。
    节点所在组 为 (文件节点号 减 1) 除以 信息 的 每组文件节点数。
    若 节点所在组 小于 信息 的 块组数：
        空闲 为 做 ext4_获取空闲块数 用 信息、节点所在组。
        若 空闲 大于 0：
            返还 节点所在组。
        完。
    完。
    若 逻辑块 大于 0：
        估计组 为 逻辑块 除以 信息 的 每组块数。
        若 估计组 小于 信息 的 块组数：
            空闲2 为 做 ext4_获取空闲块数 用 信息、估计组。
            若 空闲2 大于 0：
                返还 估计组。
            完。
        完。
    完。
    可变 组号 为 节点所在组。
    若 组号 大于或等于 信息 的 块组数：
        组号 为 0。
    完。
    可变 已检查 为 0。
    只要 已检查 小于 信息 的 块组数：
        空闲3 为 做 ext4_获取空闲块数 用 信息、组号。
        若 空闲3 大于 0：
            返还 组号。
        完。
        组号 为 (组号 加 1) 取余于 信息 的 块组数。
        已检查 为 已检查 加 1。
    完。
    返还 0。
完。

ext4_记录分配成功 接受 结果：
    分配统计数据 的 总分配成功 为 分配统计数据 的 总分配成功 加 1。
    分配统计数据 的 总分配块数 为 分配统计数据 的 总分配块数 加 结果 的 分配块数。
完。

ext4_获取分配请求描述 接受 请求：
    可变 描述 为 「分配请求: 」。
    描述 为 做 字符串拼接 用 描述、做 字符串拼接 用 「块数=」、做 转字符串 用 请求 的 请求块数。
    描述 为 做 字符串拼接 用 描述、做 字符串拼接 用 「 目标组=」、做 转字符串 用 请求 的 目标组。
    描述 为 做 字符串拼接 用 描述、做 字符串拼接 用 「 标志=0x」、做 转字符串 用 请求 的 标志。
    返还 描述。
完。

ext4_伙伴缓存_查找 接受 组号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 伙伴缓存：
        若 伙伴缓存 的 第 i 项 的 组号 等于 组号 并且 伙伴缓存 的 第 i 项 的 位图已加载：
            伙伴缓存 的 第 i 项 的 最后访问时间 为 做 当前时间_秒。
            返还 伙伴缓存 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

ext4_伙伴缓存_添加 接受 伙伴：
    若 做 计数 用 伙伴缓存 大于或等于 伙伴缓存_最大数量：
        可变 最早索引 为 0。
        可变 最早时间 为 伙伴缓存 的 第 0 项 的 最后访问时间。
        可变 i 为 1。
        只要 i 小于 做 计数 用 伙伴缓存：
            若 伙伴缓存 的 第 i 项 的 最后访问时间 小于 最早时间：
                最早时间 为 伙伴缓存 的 第 i 项 的 最后访问时间。
                最早索引 为 i。
            完。
            i 为 i 加 1。
        完。
        做 设置 用 伙伴缓存、最早索引、伙伴。
    否则：
        做 追加 用 伙伴缓存、伙伴。
    完。
完。

ext4_伙伴缓存_失效 接受 组号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 伙伴缓存：
        若 伙伴缓存 的 第 i 项 的 组号 等于 组号：
            伙伴缓存 的 第 i 项 的 位图已加载 为 假。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

ext4_计算预分配大小 接受 请求：
    若 请求 的 标志 位与 EXT4_MB_标志_元数据 不等于 0：
        返还 请求 的 请求块数。
    完。
    若 请求 的 标志 位与 EXT4_MB_标志_流式 不等于 0：
        返还 请求 的 请求块数 加 4。
    完。
    若 请求 的 逻辑块 小于 EXT4_MB_流式分配阈值：
        返还 请求 的 请求块数 加 EXT4_MB_预分配_文件 乘 2。
    完。
    返还 请求 的 请求块数 加 EXT4_MB_预分配_文件。
完。

ext4_调整请求大小 接受 sb、请求：
    信息 为 sb 的 私有信息。
    若 请求 的 标志 位与 EXT4_MB_标志_强制单块 不等于 0：
        返还。
    完。
    预分配大小 为 做 ext4_计算预分配大小 用 请求。
    若 预分配大小 大于 请求 的 请求块数 并且 预分配大小 小于或等于 EXT4_MB_最大分配大小：
        若 信息 的 空闲块数 大于 预分配大小 乘 2：
            请求 的 请求块数 为 预分配大小。
            请求 的 标志 为 请求 的 标志 位或 EXT4_MB_标志_预分配。
        完。
    完。
完。

ext4_分配_打印统计 接受 零参：
    做 打印 用 「[ext4分配] ═══ 统计信息 ═══」。
    做 打印 用 做 字符串拼接 用 「  总分配次数:       」、做 转字符串 用 分配统计数据 的 总分配次数。
    做 打印 用 做 字符串拼接 用 「  总分配成功:       」、做 转字符串 用 分配统计数据 的 总分配成功。
    做 打印 用 做 字符串拼接 用 「  总分配失败:       」、做 转字符串 用 分配统计数据 的 总分配失败。
    做 打印 用 做 字符串拼接 用 「  总分配块数:       」、做 转字符串 用 分配统计数据 的 总分配块数。
    做 打印 用 做 字符串拼接 用 「  CR0精确匹配:      」、做 转字符串 用 分配统计数据 的 CR0_精确匹配成功。
    做 打印 用 做 字符串拼接 用 「  CR1好伙伴:        」、做 转字符串 用 分配统计数据 的 CR1_好伙伴成功。
    做 打印 用 做 字符串拼接 用 「  CR2任意空闲:      」、做 转字符串 用 分配统计数据 的 CR2_任意空闲成功。
    做 打印 用 做 字符串拼接 用 「  CR3强制单块:      」、做 转字符串 用 分配统计数据 的 CR3_强制单块成功。
    做 打印 用 做 字符串拼接 用 「  目标组命中:       」、做 转字符串 用 分配统计数据 的 目标组命中成功。
    做 打印 用 做 字符串拼接 用 「  空间不足次数:     」、做 转字符串 用 分配统计数据 的 空间不足次数。
    做 打印 用 做 字符串拼接 用 「  保留块拒绝:       」、做 转字符串 用 分配统计数据 的 保留块拒绝次数。
    若 分配统计数据 的 总分配成功 大于 0：
        CR0率 为 分配统计数据 的 CR0_精确匹配成功 乘 100 除以 分配统计数据 的 总分配成功。
        目标率 为 分配统计数据 的 目标组命中成功 乘 100 除以 分配统计数据 的 总分配成功。
        做 打印 用 做 字符串拼接 用 「  CR0命中率:        」、做 字符串拼接 用 做 转字符串 用 CR0率、「%」。
        做 打印 用 做 字符串拼接 用 「  目标组命中率:     」、做 字符串拼接 用 做 转字符串 用 目标率、「%」。
    完。
    返还 0。
完。

ext4_分配_获取统计 接受 零参：
    返还 分配统计数据。
完。

ext4_分配_重置统计 接受 零参：
    分配统计数据 为 新建 分配统计。
    返还 0。
完。

ext4_mballoc_初始化 接受 sb：
    若 sb 等于 空 或者 sb 的 私有信息 等于 空：
        返还 错误码_无效参数。
    完。
    信息 为 sb 的 私有信息。
    伙伴缓存 为 一组：（空）。
    分配统计数据 为 新建 分配统计。
    做 打印 用 做 字符串拼接 用 「[ext4mballoc] 初始化完成: 块组数=」、做 转字符串 用 信息 的 块组数。
    做 打印 用 做 字符串拼接 用 「[ext4mballoc]   空闲块数=」、做 转字符串 用 信息 的 空闲块数。
    做 打印 用 做 字符串拼接 用 「[ext4mballoc]   每组块数=」、做 转字符串 用 信息 的 每组块数。
    返还 0。
完。

ext4_mballoc_销毁 接受 零参：
    伙伴缓存 为 一组：（空）。
    做 打印 用 「[ext4mballoc] 已销毁」。
    返还 0。
完。

ext4_mballoc_dump_块组 接受 sb：
    若 sb 等于 空 或者 sb 的 私有信息 等于 空：
        返还。
    完。
    信息 为 sb 的 私有信息。
    做 打印 用 「[ext4mballoc] ═══ 块组空闲摘要 ═══」。
    可变 总空闲 为 0。
    可变 满组数 为 0。
    可变 空组数 为 0。
    可变 组号 为 0。
    只要 组号 小于 信息 的 块组数 并且 组号 小于 64：
        空闲 为 做 ext4_获取空闲块数 用 信息、组号。
        总空闲 为 总空闲 加 空闲。
        若 空闲 等于 0：
            满组数 为 满组数 加 1。
        完。
        若 空闲 等于 信息 的 每组块数：
            空组数 为 空组数 加 1。
        完。
        若 组号 小于 16：
            做 打印 用 做 字符串拼接 用 「  组」、做 字符串拼接 用 做 转字符串 用 组号、做 字符串拼接 用 「: 空闲=」、做 转字符串 用 空闲。
        完。
        组号 为 组号 加 1。
    完。
    做 打印 用 做 字符串拼接 用 「  总空闲块(扫描): 」、做 转字符串 用 总空闲。
    做 打印 用 做 字符串拼接 用 「  已满块组数:     」、做 转字符串 用 满组数。
    做 打印 用 做 字符串拼接 用 「  全空块组数:     」、做 转字符串 用 空组数。
    若 信息 的 块总数 大于 0：
        使用率 为 (信息 的 块总数 减 信息 的 空闲块数) 乘 100 除以 信息 的 块总数。
        做 打印 用 做 字符串拼接 用 「  磁盘使用率:     」、做 字符串拼接 用 做 转字符串 用 使用率、「%」。
    完。
完。

ext4_mballoc_dump_伙伴缓存 接受 零参：
    做 打印 用 「[ext4mballoc] ═══ 伙伴缓存状态 ═══」。
    做 打印 用 做 字符串拼接 用 「  缓存条目数: 」、做 转字符串 用 做 计数 用 伙伴缓存。
    做 打印 用 做 字符串拼接 用 「  最大容量:   」、做 转字符串 用 伙伴缓存_最大数量。
    可变 i 为 0。
    只要 i 小于 做 计数 用 伙伴缓存 并且 i 小于 8：
        条目 为 伙伴缓存 的 第 i 项。
        做 打印 用 做 字符串拼接 用 「  [」、做 字符串拼接 用 做 转字符串 用 i、做 字符串拼接 用 「] 组=」、做 字符串拼接 用 做 转字符串 用 条目 的 组号、做 字符串拼接 用 「 空闲=」、做 字符串拼接 用 做 转字符串 用 条目 的 空闲块数、做 字符串拼接 用 「 已加载=」、做 转字符串 用 条目 的 位图已加载。
        i 为 i 加 1。
    完。
完。

ext4_批量分配 接受 sb、请求列表：
    若 sb 等于 空 或者 请求列表 等于 空：
        返还 错误码_无效参数。
    完。
    可变 结果列表 为 一组：（空）。
    可变 i 为 0。
    可变 成功数 为 0。
    可变 失败数 为 0。
    只要 i 小于 做 计数 用 请求列表：
        结果 为 做 ext4_分配块 用 sb、请求列表 的 第 i 项。
        若 非 做 是错误 用 结果：
            做 追加 用 结果列表、结果。
            成功数 为 成功数 加 1。
        否则：
            失败数 为 失败数 加 1。
            若 失败数 大于 0 并且 成功数 大于 0：
                可变 j 为 0。
                只要 j 小于 做 计数 用 结果列表：
                    做 ext4_释放块 用 sb、结果列表 的 第 j 项 的 起始块、结果列表 的 第 j 项 的 分配块数。
                    j 为 j 加 1。
                完。
                返还 错误码_无空间。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果列表。
完。

可变 AI_扩展分配块_运行指标 为 0。

可变 AI_扩展分配块_异常指标 为 0。

AI_扩展分配块_自诊断 接受 零参：
    AI_扩展分配块_运行指标 为 分配统计数据。
    返还 AI_扩展分配块_运行指标。
完。

AI_扩展分配块_优化建议 接受 零参：
    指标 为 做 AI_扩展分配块_自诊断。
    若 AI_扩展分配块_运行指标 大于 10000：
        返还 「扩展分配块负载较高」。
    完。
    返还 「扩展分配块状态正常」。
完。

本章 收束。
