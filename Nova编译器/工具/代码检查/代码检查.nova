《代码检查》

引用《..·..·标准库·基础》。

级别_错误 为 3。

级别_警告 为 2。

级别_提示 为 1。

级别_信息 为 0。

代码检查_诊断_创建 接受 规则ID、级别、行、列、消息：
    返还 一组：规则ID、级别、行、列、消息、「」。
完。

检查_诊断_设修复 接受 诊断、修复：
    诊断 的 第 5 项 为 修复。
完。

诊断_级别名 接受 级别：
    若 级别 等于 级别_错误：
        返还 「error」。
    完。
    若 级别 等于 级别_警告：
        返还 「warning」。
    完。
    若 级别 等于 级别_提示：
        返还 「提示」。
    完。
    返还 「信息」。
完。

诊断_格式化 接受 诊断：
    可变 r 为 做 诊断_级别名 用 诊断 的 第 1 项。
    r 为 做 字符串拼接 用 r、「 」。
    r 为 做 字符串拼接 用 r、「[」。
    r 为 做 字符串拼接 用 r、诊断 的 第 0 项。
    r 为 做 字符串拼接 用 r、「] 行」。
    r 为 做 字符串拼接 用 r、做 检查_数到文 用 诊断 的 第 2 项。
    r 为 做 字符串拼接 用 r、「: 」。
    r 为 做 字符串拼接 用 r、诊断 的 第 4 项。
    返还 r。
完。

规则_创建 接受 id、名称、级别、检查函数：
    返还 一组：id、名称、级别、真、检查函数。
完。

检查器_创建 接受 零参：
    检查器 为 一组：一组：（空）、一组：（空）、一组：（空）、做 字典。
    做 检查器_注册内置规则 用 检查器。
    返还 检查器。
完。

检查器_添加规则 接受 检查器、规则：
    做 追加 用 检查器 的 第 0 项、规则。
完。

检查器_忽略 接受 检查器、规则ID：
    做 追加 用 检查器 的 第 2 项、规则ID。
完。

检查器_注册内置规则 接受 检查器：
    做 检查器_添加规则 用 检查器、做 规则_创建 用 「W001」、「行长度过长」、级别_警告、空。
    做 检查器_添加规则 用 检查器、做 规则_创建 用 「W002」、「函数过长」、级别_警告、空。
    做 检查器_添加规则 用 检查器、做 规则_创建 用 「W003」、「嵌套过深」、级别_警告、空。
    做 检查器_添加规则 用 检查器、做 规则_创建 用 「E001」、「未使用变量」、级别_错误、空。
    做 检查器_添加规则 用 检查器、做 规则_创建 用 「E002」、「未定义引用」、级别_错误、空。
    做 检查器_添加规则 用 检查器、做 规则_创建 用 「S001」、「缺少文档注释」、级别_提示、空。
    做 检查器_添加规则 用 检查器、做 规则_创建 用 「S002」、「命名不规范」、级别_提示、空。
    做 检查器_添加规则 用 检查器、做 规则_创建 用 「C001」、「圈复杂度过高」、级别_警告、空。
完。

检查器_检查源码 接受 检查器、源码行列表：
    检查器 的 第 1 项 为 一组：（空）。
    最大行长 为 120。
    最大函数行 为 100。
    最大嵌套 为 5。
    可变 i 为 0。
    可变 函数开始 为 负 1。
    可变 当前嵌套 为 0。
    可变 最大观察嵌套 为 0。
    可变 分支计数 为 0。
    只要 i 小于 做 计数 用 源码行列表：
        行 为 源码行列表 的 第 i 项。
        行号 为 i 加 1。
        行长 为 做 计数 用 行。
        若 行长 大于 最大行长：
            d 为 做 代码检查_诊断_创建 用 「W001」、级别_警告、行号、最大行长、「行长度超过120字符」。
            做 检查_诊断_设修复 用 d、「拆分为多行」。
            做 追加 用 检查器 的 第 1 项、d。
        完。
        若 做 检查_包含 用 行、「函数 」 或者 做 检查_包含 用 行、「函数(」 或者 做 检查_包含 用 行、「法 」 或者 做 检查_包含 用 行、「法(」：
            若 函数开始 大于或等于 0：
                函数行数 为 i 减 函数开始。
                若 函数行数 大于 最大函数行：
                    做 追加 用 检查器 的 第 1 项、做 代码检查_诊断_创建 用 「W002」、级别_警告、函数开始 加 1、0、做 字符串拼接 用 「函数过长(」、做 字符串拼接 用 做 检查_数到文 用 函数行数、「行)」。
                完。
                若 最大观察嵌套 大于 最大嵌套：
                    做 追加 用 检查器 的 第 1 项、做 代码检查_诊断_创建 用 「W003」、级别_警告、函数开始 加 1、0、做 字符串拼接 用 「嵌套深度过大: 」、做 检查_数到文 用 最大观察嵌套。
                完。
                若 分支计数 大于 10：
                    做 追加 用 检查器 的 第 1 项、做 代码检查_诊断_创建 用 「C001」、级别_警告、函数开始 加 1、0、做 字符串拼接 用 「圈复杂度过高: 」、做 检查_数到文 用 分支计数 加 1。
                完。
            完。
            函数开始 为 i。
            当前嵌套 为 0。
            最大观察嵌套 为 0。
            分支计数 为 0。
        完。
        可变 j 为 0。
        只要 j 小于 行长：
            c 为 做 子串 用 行、j、1。
            若 做 字符串等于 用 c、「{」：
                当前嵌套 为 当前嵌套 加 1。
            完。
            若 做 字符串等于 用 c、「}」：
                当前嵌套 为 当前嵌套 减 1。
            完。
            j 为 j 加 1。
        完。
        若 当前嵌套 大于 最大观察嵌套：
            最大观察嵌套 为 当前嵌套。
        完。
        若 做 检查_包含 用 行、「如果 」 或者 做 检查_包含 用 行、「如果(」 或者 做 检查_包含 用 行、「若 」 或者 做 检查_包含 用 行、「若(」：
            分支计数 为 分支计数 加 1。
        完。
        若 做 检查_包含 用 行、「当 」 或者 做 检查_包含 用 行、「当(」：
            分支计数 为 分支计数 加 1。
        完。
        若 做 检查_包含 用 行、「否则 如果」 或者 做 检查_包含 用 行、「否 若」：
            分支计数 为 分支计数 加 1。
        完。
        若 做 检查_包含 用 行、「辨」：
            分支计数 为 分支计数 加 1。
        完。
        若 (做 检查_包含 用 行、「函数 」 或者 做 检查_包含 用 行、「法 」) 并且 函数开始 等于 i：
            若 i 等于 0 或者 做 检查_包含 用 源码行列表 的 第 i 减 1 项、「//」 等于 假：
                做 追加 用 检查器 的 第 1 项、做 代码检查_诊断_创建 用 「S001」、级别_提示、行号、0、「公开函数缺少文档注释」。
            完。
        完。
        i 为 i 加 1。
    完。
    若 函数开始 大于或等于 0：
        函数行数 为 做 计数 用 源码行列表 减 函数开始。
        若 函数行数 大于 最大函数行：
            做 追加 用 检查器 的 第 1 项、做 代码检查_诊断_创建 用 「W002」、级别_警告、函数开始 加 1、0、做 字符串拼接 用 「函数过长(」、做 字符串拼接 用 做 检查_数到文 用 函数行数、「行)」。
        完。
    完。
    返还 检查器 的 第 1 项。
完。

检查器_报告 接受 检查器：
    可变 警告数 为 0。
    可变 提示数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 检查器 的 第 1 项：
        若 检查器 的 第 1 项 的 第 i 项 的 第 1 项 等于 级别_错误：
            错误数 为 错误数 加 1。
        完。
        若 检查器 的 第 1 项 的 第 i 项 的 第 1 项 等于 级别_警告：
            警告数 为 警告数 加 1。
        完。
        若 检查器 的 第 1 项 的 第 i 项 的 第 1 项 等于 级别_提示：
            提示数 为 提示数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：做 计数 用 检查器 的 第 1 项、错误数、警告数、提示数。
完。

检查器_格式化报告 接受 检查器：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 检查器 的 第 1 项：
        结果 为 做 字符串拼接 用 结果、做 诊断_格式化 用 检查器 的 第 1 项 的 第 i 项。
        结果 为 做 字符串拼接 用 结果、「\n」。
        i 为 i 加 1。
    完。
    统计 为 做 检查器_报告 用 检查器。
    结果 为 做 字符串拼接 用 结果、「\n总计: 」。
    结果 为 做 字符串拼接 用 结果、做 检查_数到文 用 统计 的 第 0 项。
    结果 为 做 字符串拼接 用 结果、「 问题 (」。
    结果 为 做 字符串拼接 用 结果、做 检查_数到文 用 统计 的 第 1 项。
    结果 为 做 字符串拼接 用 结果、「 错误,  」。
    结果 为 做 字符串拼接 用 结果、做 检查_数到文 用 统计 的 第 2 项。
    结果 为 做 字符串拼接 用 结果、「 警告,  」。
    结果 为 做 字符串拼接 用 结果、做 检查_数到文 用 统计 的 第 3 项。
    结果 为 做 字符串拼接 用 结果、「 提示)」。
    返还 结果。
完。

检查器_检查AST 接受 检查器、函数列表、主体：
    可变 诊断列 为 检查器 的 第 1 项。
    若 函数列表 不等于 空 并且 做 是列表 用 函数列表：
        可变 fi 为 0。
        只要 fi 小于 做 计数 用 函数列表：
            若 做 是列表 用 函数项 并且 做 计数 用 函数项 大于或等于 4：
                函数名 为 函数项 的 第 1 项。
                参数列 为 函数项 的 第 2 项。
                函数体 为 函数项 的 第 3 项。
                若 做 是列表 用 参数列 并且 做 计数 用 参数列 大于 7：
                    做 追加 用 诊断列、做 代码检查_诊断_创建 用 「E003」、级别_警告、fi、0、做 字符串拼接 用 「函数参数过多: 」、做 字符串拼接 用 做 转字符串 用 函数名、做 字符串拼接 用 「(」、做 字符串拼接 用 做 检查_数到文 用 做 计数 用 参数列、「)」。
                完。
                若 做 是列表 用 函数体：
                    做 _检查_AST_语句列表 用 诊断列、函数体、0、做 转字符串 用 函数名。
                完。
            完。
            fi 为 fi 加 1。
        完。
    完。
    若 主体 不等于 空 并且 做 是列表 用 主体：
        做 _检查_AST_语句列表 用 诊断列、主体、0、「__主程序__」。
    完。
    返还 诊断列。
完。

_检查_AST_语句列表 接受 诊断列、语句列表、深度、函数名：
    若 深度 大于 8：
        返还 空。
    完。
    可变 si 为 0。
    只要 si 小于 做 计数 用 语句列表：
        语句 为 语句列表 的 第 si 项。
        若 做 是列表 用 语句 并且 做 计数 用 语句 大于或等于 2：
            类型 为 语句 的 第 0 项。
            若 类型 等于 0 并且 si 小于 做 计数 用 语句列表 减 1：
                做 追加 用 诊断列、做 代码检查_诊断_创建 用 「W004」、级别_提示、si、0、做 字符串拼接 用 「空语句 在 」、函数名。
            完。
            若 类型 等于 115 并且 做 计数 用 语句 大于或等于 3 并且 做 是列表 用 语句 的 第 2 项：
                做 _检查_AST_语句列表 用 诊断列、语句 的 第 2 项、深度 加 1、函数名。
                若 做 计数 用 语句 大于或等于 4 并且 做 是列表 用 语句 的 第 3 项：
                    做 _检查_AST_语句列表 用 诊断列、语句 的 第 3 项、深度 加 1、函数名。
                完。
            完。
            若 类型 等于 116 并且 做 计数 用 语句 大于或等于 3 并且 做 是列表 用 语句 的 第 2 项：
                做 _检查_AST_语句列表 用 诊断列、语句 的 第 2 项、深度 加 1、函数名。
            完。
        完。
        si 为 si 加 1。
    完。
完。

检查_包含 接受 文本、子串文本：
    n 为 做 计数 用 文本。
    m 为 做 计数 用 子串文本。
    若 m 大于 n：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于或等于 n 减 m：
        若 做 字符串等于 用 做 子串 用 文本、i、m、子串文本：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

检查_数到文 接受 n：
    若 n 等于 0：
        返还 「0」。
    完。
    可变 v 为 n。
    可变 负 为 假。
    若 v 小于 0：
        负 为 真。
        v 为 0 减 v。
    完。
    可变 r 为 「」。
    只要 v 大于 0：
        r 为 做 字符串拼接 用 做 子串 用 「0123456789」、v 取余于 10、1、r。
        v 为 v 除以 10。
    完。
    若 负：
        r 为 做 字符串拼接 用 「-」、r。
    完。
    返还 r。
完。

检查_蛇形命名 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 名称：
        c 为 做 字节取 用 名称、i。
        若 c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

检查_驼峰命名 接受 名称：
    若 做 字节长度 用 名称 等于 0：
        返还 假。
    完。
    首字 为 做 字节取 用 名称、0。
    返还 首字 大于或等于 65 并且 首字 小于或等于 90。
完。

检查_是魔数 接受 值：
    若 值 等于 0 或者 值 等于 1 或者 值 等于 负 1 或者 值 等于 2 或者 值 等于 10 或者 值 等于 100 或者 值 等于 1000：
        返还 假。
    完。
    返还 真。
完。

检查_参数过多 接受 参数数：
    若 参数数 大于 7：
        返还 「参数过多(>7)-建议使用结构体」。
    完。
    若 参数数 大于 5：
        返还 「参数较多(>5)-注意可读性」。
    完。
    返还 「ok」。
完。

检查_嵌套深度 接受 最大深度：
    若 最大深度 大于 5：
        返还 「嵌套过深(>5)-建议提取函数」。
    完。
    若 最大深度 大于 3：
        返还 「嵌套较深(>3)-注意可读性」。
    完。
    返还 「ok」。
完。

检查_重复行 接受 行列表、窗口大小：
    n 为 做 计数 用 行列表。
    可变 重复数 为 0。
    可变 i 为 0。
    只要 i 小于或等于 n 减 窗口大小 乘 2：
        可变 j 为 i 加 窗口大小。
        只要 j 小于或等于 n 减 窗口大小：
            可变 匹配 为 真。
            可变 k 为 0。
            只要 k 小于 窗口大小：
                若 行列表 的 第 i 加 k 项 不等于 行列表 的 第 j 加 k 项：
                    匹配 为 假。
                完。
                k 为 k 加 1。
            完。
            若 匹配：
                重复数 为 重复数 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 重复数。
完。

检查_待办标记 接受 行列表：
    可变 待办 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        若 做 检查_包含 用 行列表 的 第 i 项、「TODO」 或者 做 检查_包含 用 行列表 的 第 i 项、「FIXME」 或者 做 检查_包含 用 行列表 的 第 i 项、「HACK」：
            待办 为 待办 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 待办。
完。

检查_汇总报告 接受 文件名、行数、函数数、最大嵌套、待办数、重复数：
    可变 问题 为 0。
    若 行数 大于 500：
        问题 为 问题 加 1。
    完。
    若 最大嵌套 大于 5：
        问题 为 问题 加 1。
    完。
    若 待办数 大于 5：
        问题 为 问题 加 1。
    完。
    若 重复数 大于 3：
        问题 为 问题 加 1。
    完。
    返还 一组：文件名、行数、函数数、问题。
完。

本章 收束。
