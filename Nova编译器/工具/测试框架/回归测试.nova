《回归测试》

引用《.·断言》。

引用《..·..·标准库·基础》。

引用《..·..·自举兼容桥》。

引用《..·..·系统扩展·共享辅助》。

测试_字符串基础 接受 零参：
    做 测试组 用 「字符串基础」。
    做 断言字符串相等 用 做 字符串拼接 用 「hello」、「 world」、「hello world」、「字符串拼接-基本」。
    做 断言字符串相等 用 做 字符串拼接 用 「」、「abc」、「abc」、「字符串拼接-左空」。
    做 断言字符串相等 用 做 字符串拼接 用 「abc」、「」、「abc」、「字符串拼接-右空」。
    做 断言字符串相等 用 做 字符串拼接 用 「」、「」、「」、「字符串拼接-双空」。
    做 断言字符串相等 用 做 子串 用 「hello」、0、5、「hello」、「子串-完整」。
    做 断言字符串相等 用 做 子串 用 「hello」、1、3、「ell」、「子串-中间」。
    做 断言字符串相等 用 做 子串 用 「hello」、「」、「子串-零长度」。
    做 断言相等 用 做 计数 用 「abc」、3、「字符串长度-ascii」。
    做 断言相等 用 做 计数 用 「」、0、「字符串长度-空」。
    做 断言相等 用 做 字符串查找 用 「hello world」、「world」、6、「字符串查找-存在」。
    做 断言相等 用 做 字符串查找 用 「hello」、「xyz」、负 1、「字符串查找-不存在」。
    做 断言相等 用 做 字符串查找 用 「aaa」、「a」、0、「字符串查找-首字符」。
    做 断言相等 用 做 字符串查找 用 「」、「a」、负 1、「字符串查找-空串」。
完。

测试_列表基础 接受 零参：
    做 测试组 用 「列表基础」。
    可变 a 为 一组：（空）。
    做 追加 用 a、10。
    做 追加 用 a、20。
    做 追加 用 a、30。
    做 断言相等 用 做 计数 用 a、3、「列表追加-长度」。
    做 断言相等 用 a 的 第 0 项、10、「列表索引-0」。
    做 断言相等 用 a 的 第 1 项、20、「列表索引-1」。
    做 断言相等 用 a 的 第 2 项、30、「列表索引-2」。
    a 的 第 1 项 为 99。
    做 断言相等 用 a 的 第 1 项、99、「列表赋值-修改」。
    可变 b 为 一组：一组：1、2、一组：3、4。
    做 断言相等 用 b 的 第 0 项 的 第 0 项、1、「嵌套列表-[0][0]」。
    做 断言相等 用 b 的 第 1 项 的 第 1 项、4、「嵌套列表-[1][1]」。
    做 断言相等 用 做 计数 用 b、2、「嵌套列表-外层长度」。
    做 断言相等 用 做 计数 用 b 的 第 0 项、2、「嵌套列表-内层长度」。
    可变 c 为 一组：（空）。
    做 断言相等 用 做 计数 用 c、0、「空列表-长度」。
完。

测试_值比较 接受 零参：
    做 测试组 用 「值比较」。
    做 断言为真 用 1 等于 1、「整数相等」。
    做 断言为真 用 1 不等于 2、「整数不等」。
    做 断言为真 用 0 等于 0、「零相等」。
    做 断言为真 用 负 1 等于 负 1、「负数相等」。
    做 断言为真 用 「abc」 等于 「abc」、「字符串相等」。
    做 断言为真 用 「abc」 不等于 「def」、「字符串不等」。
    做 断言为真 用 「」 等于 「」、「空串相等」。
    做 断言为真 用 空 等于 空、「空值相等」。
    做 断言为真 用 真 等于 真、「真==真」。
    做 断言为真 用 假 等于 假、「假==假」。
    做 断言为真 用 真 不等于 假、「真!=假」。
    做 断言为真 用 1 不等于 「1」、「整数!=字符串」。
    做 断言为真 用 0 不等于 空、「零!=空」。
    做 断言为真 用 1 不等于 假、「一!=假」。
完。

测试_算术 接受 零参：
    做 测试组 用 「算术与位运算」。
    做 断言相等 用 1 加 2、3、「加法」。
    做 断言相等 用 10 减 7、3、「减法」。
    做 断言相等 用 3 乘 4、12、「乘法」。
    做 断言相等 用 10 除以 3、3、「整除」。
    做 断言相等 用 10 取余于 3、1、「取模」。
    做 断言相等 用 0 减 5、负 5、「负数」。
    做 断言相等 用 0xFF 位与 0x0F、0x0F、「位与」。
    做 断言相等 用 0xF0 位或 0x0F、0xFF、「位或」。
    做 断言相等 用 0xFF 位异或 0x0F、0xF0、「位异或」。
    做 断言相等 用 1 左移 3、8、「左移」。
    做 断言相等 用 16 右移 2、4、「右移」。
    做 断言相等 用 0 乘 12345、0、「零乘」。
    做 断言相等 用 1 乘 12345、12345、「一乘」。
完。

测试_字典 接受 零参：
    做 测试组 用 「字典操作」。
    d 为 做 字典。
    做 字典设 用 d、「名」、「Nova」。
    做 字典设 用 d、「版本」、「2」。
    做 字典设 用 d、「年」、「2026」。
    做 断言相等 用 做 字典取 用 d、「名」、「Nova」、「字典取-存在」。
    做 断言相等 用 做 字典取 用 d、「版本」、「2」、「字典取-第二键」。
    做 断言相等 用 做 字典取 用 d、「年」、「2026」、「字典取-第三键」。
    做 字典设 用 d、「版本」、「3」。
    做 断言相等 用 做 字典取 用 d、「版本」、「3」、「字典覆写」。
    键 为 做 字典键列表 用 d。
    做 断言大于 用 做 计数 用 键、0、「字典键列表-非空」。
完。

测试_控制流 接受 零参：
    做 测试组 用 「控制流」。
    可变 x 为 0。
    若 真：
        x 为 1。
    完。
    做 断言相等 用 x、1、「如果-真」。
    若 假：
        x 为 99。
    否则：
        x 为 2。
    完。
    做 断言相等 用 x、2、「如果-假-否则」。
    可变 sum 为 0。
    可变 i 为 1。
    只要 i 小于或等于 10：
        sum 为 sum 加 i。
        i 为 i 加 1。
    完。
    做 断言相等 用 sum、55、「当循环-1到10求和」。
    可变 匹配 为 假。
    匹配 为 真。
    做 断言为真 用 匹配、「匹配-作为变量名赋值」。
完。

_辅助_加法 接受 a、b：
    返还 a 加 b。
完。

_辅助_阶乘 接受 n：
    若 n 小于或等于 1：
        返还 1。
    完。
    返还 n 乘 做 _辅助_阶乘 用 n 减 1。
完。

测试_函数 接受 零参：
    做 测试组 用 「函数与递归」。
    做 断言相等 用 做 _辅助_加法 用 3、4、7、「函数调用-加法」。
    做 断言相等 用 做 _辅助_阶乘 用 5、120、「递归-阶乘5」。
    做 断言相等 用 做 _辅助_阶乘 用 1、1、「递归-阶乘1」。
    做 断言相等 用 做 _辅助_阶乘 用 0、1、「递归-阶乘0」。
完。

测试_缓冲区 接受 零参：
    做 测试组 用 「缓冲区操作」。
    buf 为 做 缓冲区 用 16。
    做 断言相等 用 做 计数 用 buf、16、「缓冲区-创建大小」。
    做 断言相等 用 buf 的 第 0 项、0、「缓冲区-零初始化」。
    做 断言相等 用 buf 的 第 15 项、0、「缓冲区-末尾零」。
    做 缓冲写 用 buf、0、0xAB。
    做 断言相等 用 buf 的 第 0 项、0xAB、「缓冲写-单字节」。
    做 内存写16 用 buf、2、0x1234。
    做 断言相等 用 buf 的 第 2 项、0x34、「内存写16-低字节」。
    做 断言相等 用 buf 的 第 3 项、0x12、「内存写16-高字节」。
    做 内存写32 用 buf、4、0x12345678。
    做 断言相等 用 buf 的 第 4 项、0x78、「内存写32-字节0」。
    做 断言相等 用 buf 的 第 5 项、0x56、「内存写32-字节1」。
    做 断言相等 用 buf 的 第 6 项、0x34、「内存写32-字节2」。
    做 断言相等 用 buf 的 第 7 项、0x12、「内存写32-字节3」。
    做 内存写64 用 buf、8、256。
    做 断言相等 用 buf 的 第 8 项、0、「内存写64-字节0(256)」。
    做 断言相等 用 buf 的 第 9 项、1、「内存写64-字节1(256)」。
完。

测试_作用域 接受 零参：
    做 测试组 用 「作用域管理」。
    深度基 为 做 作用域_深度。
    做 作用域_进入。
    深度前 为 做 作用域_深度。
    做 断言相等 用 深度前、深度基 加 1、「作用域进入-深度加1」。
    做 作用域_退出。
    深度后 为 做 作用域_深度。
    做 断言相等 用 深度后、深度基、「作用域退出-深度恢复」。
    做 作用域_进入。
    做 作用域_进入。
    做 断言相等 用 做 作用域_深度、深度基 加 2、「嵌套作用域-深度+2」。
    做 作用域_退出。
    做 作用域_退出。
    做 断言为真 用 真、「嵌套作用域-无死循环」。
完。

测试_数据结构 接受 零参：
    做 测试组 用 「数据结构完整性」。
    src 为 一组：10、20、30、40、50。
    s 为 做 切片 用 src、1、4。
    做 断言相等 用 做 计数 用 s、3、「切片-长度」。
    做 断言相等 用 s 的 第 0 项、20、「切片-首元素」。
    做 断言相等 用 s 的 第 2 项、40、「切片-末元素」。
    做 断言相等 用 做 求和 用 一组：1、2、3、4、5、15、「求和-1到5」。
    做 断言相等 用 做 求和 用 一组：（空）、0、「求和-空列表」。
    做 断言相等 用 做 位计数 用 0、0、「位计数-0」。
    做 断言相等 用 做 位计数 用 1、1、「位计数-1」。
    做 断言相等 用 做 位计数 用 7、3、「位计数-7(111)」。
    做 断言相等 用 做 位计数 用 255、8、「位计数-255」。
完。

做 测试套件 用 「Nova编译器回归测试」。

做 测试_字符串基础。

做 测试_列表基础。

做 测试_值比较。

做 测试_算术。

做 测试_字典。

做 测试_控制流。

做 测试_函数。

做 测试_缓冲区。

做 测试_作用域。

做 测试_数据结构。

失败数 为 做 测试报告。

若 失败数 大于 0：
    做 打印 用 「」。
    做 打印 用 「回归测试发现失败! 退出码: 1」。
    做 打印 用 「失败模式: 」 加 做 测试_失败模式分析。
完。

若 失败数 等于 0：
    做 打印 用 「」。
    做 打印 用 「全部回归测试通过!」。
完。

本章 收束。
