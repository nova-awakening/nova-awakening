《断言》

引用《..·..·标准库·基础》。

可变 __测试通过 为 0。

可变 __测试失败 为 0。

可变 __测试跳过 为 0。

可变 __测试名 为 「」。

可变 __测试套件名 为 「」。

可变 __测试总时间 为 0。

可变 __测试详情 为 一组：（空）。

可变 __测试失败详情 为 一组：（空）。

可变 __测试套件列表 为 一组：（空）。

可变 __当前套件通过 为 0。

可变 __当前套件失败 为 0。

测试组 接受 名称：
    若 __测试名 不等于 「」：
        做 追加 用 __测试套件列表、一组：__测试名、__当前套件通过、__当前套件失败。
    完。
    __测试名 为 名称。
    __当前套件通过 为 0。
    __当前套件失败 为 0。
    做 打印 用 「 」 加 名称 加 「 」。
完。

测试套件 接受 名称：
    __测试套件名 为 名称。
    __测试通过 为 0。
    __测试失败 为 0。
    __测试跳过 为 0。
    __测试详情 为 一组：（空）。
    __测试失败详情 为 一组：（空）。
    __测试套件列表 为 一组：（空）。
    __当前套件通过 为 0。
    __当前套件失败 为 0。
    做 打印 用 「」。
    做 打印 用 「 测试套件: 」 加 名称。
    做 打印 用 「」。
完。

断言相等 接受 实际、期望、说明：
    若 做 转字符串 用 实际 等于 做 转字符串 用 期望：
        __测试通过 为 __测试通过 加 1。
        __当前套件通过 为 __当前套件通过 加 1。
        做 打印 用 「 」 加 说明。
        做 追加 用 __测试详情、一组：说明、「pass」、「」。
    否则：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 」 加 说明。
        做 打印 用 「 期望: 」 加 做 转字符串 用 期望。
        做 打印 用 「 实际: 」 加 做 转字符串 用 实际。
        做 追加 用 __测试详情、一组：说明、「failed」、「期望」 加 做 转字符串 用 期望 加 「实际」 加 做 转字符串 用 实际。
        做 追加 用 __测试失败详情、一组：__测试名、说明、做 转字符串 用 期望、做 转字符串 用 实际。
    完。
完。

断言不等 接受 实际、非期望、说明：
    若 做 转字符串 用 实际 不等于 做 转字符串 用 非期望：
        __测试通过 为 __测试通过 加 1。
        __当前套件通过 为 __当前套件通过 加 1。
        做 打印 用 「 」 加 说明。
    否则：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 」 加 说明 加 「 (值不应等于 」 加 做 转字符串 用 非期望 加 「)」。
        做 追加 用 __测试失败详情、一组：__测试名、说明、「不等于」 加 做 转字符串 用 非期望、做 转字符串 用 实际。
    完。
完。

断言为真 接受 条件、说明：
    若 条件：
        __测试通过 为 __测试通过 加 1。
        __当前套件通过 为 __当前套件通过 加 1。
        做 打印 用 「 」 加 说明。
    否则：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 」 加 说明。
        做 追加 用 __测试失败详情、一组：__测试名、说明、「真」、「假」。
    完。
完。

断言为假 接受 条件、说明：
    若 条件 等于 假：
        __测试通过 为 __测试通过 加 1。
        __当前套件通过 为 __当前套件通过 加 1。
        做 打印 用 「 」 加 说明。
    否则：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 」 加 说明。
        做 追加 用 __测试失败详情、一组：__测试名、说明、「假」、「真」。
    完。
完。

断言为空 接受 值、说明：
    若 值 等于 空：
        __测试通过 为 __测试通过 加 1。
        __当前套件通过 为 __当前套件通过 加 1。
        做 打印 用 「 」 加 说明。
    否则：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 」 加 说明 加 「 (期望空,实际: 」 加 做 转字符串 用 值 加 「)」。
        做 追加 用 __测试失败详情、一组：__测试名、说明、「空」、做 转字符串 用 值。
    完。
完。

断言非空 接受 值、说明：
    若 值 不等于 空：
        __测试通过 为 __测试通过 加 1。
        __当前套件通过 为 __当前套件通过 加 1。
        做 打印 用 「 」 加 说明。
    否则：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 」 加 说明 加 「 (值不应为空)」。
        做 追加 用 __测试失败详情、一组：__测试名、说明、「非空」、「空」。
    完。
完。

断言大于 接受 实际、阈值、说明：
    若 实际 大于 阈值：
        __测试通过 为 __测试通过 加 1。
        __当前套件通过 为 __当前套件通过 加 1。
        做 打印 用 「 」 加 说明。
    否则：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 」 加 说明 加 「 (」 加 做 转字符串 用 实际 加 「 不大于 」 加 做 转字符串 用 阈值 加 「)」。
        做 追加 用 __测试失败详情、一组：__测试名、说明、「>」 加 做 转字符串 用 阈值、做 转字符串 用 实际。
    完。
完。

断言小于 接受 实际、阈值、说明：
    若 实际 小于 阈值：
        __测试通过 为 __测试通过 加 1。
        __当前套件通过 为 __当前套件通过 加 1。
        做 打印 用 「 」 加 说明。
    否则：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 」 加 说明 加 「 (」 加 做 转字符串 用 实际 加 「 不小于 」 加 做 转字符串 用 阈值 加 「)」。
    完。
完。

断言范围 接受 实际、最小、最大、说明：
    若 实际 大于或等于 最小 并且 实际 小于或等于 最大：
        __测试通过 为 __测试通过 加 1。
        __当前套件通过 为 __当前套件通过 加 1。
        做 打印 用 「 」 加 说明。
    否则：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 」 加 说明 加 「 (」 加 做 转字符串 用 实际 加 「 不在 [」 加 做 转字符串 用 最小 加 「,」 加 做 转字符串 用 最大 加 「])」。
    完。
完。

断言近似 接受 实际、期望、容差、说明：
    可变 差 为 实际 减 期望。
    若 差 小于 0：
        差 为 0 减 差。
    完。
    若 差 小于或等于 容差：
        __测试通过 为 __测试通过 加 1。
        __当前套件通过 为 __当前套件通过 加 1。
        做 打印 用 「 」 加 说明。
    否则：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 」 加 说明 加 「 (差值」 加 做 转字符串 用 差 加 「超过容差」 加 做 转字符串 用 容差 加 「)」。
    完。
完。

断言列表相等 接受 实际、期望、说明：
    若 做 计数 用 实际 不等于 做 计数 用 期望：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 」 加 说明 加 「 (长度不同: 」 加 做 转字符串 用 做 计数 用 实际 加 「 vs 」 加 做 转字符串 用 做 计数 用 期望 加 「)」。
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 实际：
        若 实际 的 第 i 项 不等于 期望 的 第 i 项：
            __测试失败 为 __测试失败 加 1。
            __当前套件失败 为 __当前套件失败 加 1。
            做 打印 用 「 」 加 说明 加 「 (索引」 加 做 转字符串 用 i 加 「: 」 加 做 转字符串 用 实际 的 第 i 项 加 「 != 」 加 做 转字符串 用 期望 的 第 i 项 加 「)」。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    __测试通过 为 __测试通过 加 1。
    __当前套件通过 为 __当前套件通过 加 1。
    做 打印 用 「 」 加 说明。
完。

断言列表包含 接受 列表、值、说明：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 等于 值：
            __测试通过 为 __测试通过 加 1。
            __当前套件通过 为 __当前套件通过 加 1。
            做 打印 用 「 」 加 说明。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    __测试失败 为 __测试失败 加 1。
    __当前套件失败 为 __当前套件失败 加 1。
    做 打印 用 「 」 加 说明 加 「 (列表不包含 」 加 做 转字符串 用 值 加 「)」。
完。

断言列表长度 接受 列表、期望长度、说明：
    做 断言相等 用 做 计数 用 列表、期望长度、说明。
完。

测试跳过 接受 说明：
    __测试跳过 为 __测试跳过 加 1。
    做 打印 用 「 跳过: 」 加 说明。
完。

测试报告 接受 零参：
    若 __测试名 不等于 「」：
        做 追加 用 __测试套件列表、一组：__测试名、__当前套件通过、__当前套件失败。
    完。
    总数 为 __测试通过 加 __测试失败。
    做 打印 用 「」。
    做 打印 用 「」。
    做 打印 用 「 测试报告 」。
    做 打印 用 「」。
    做 打印 用 「 通过: 」 加 做 转字符串 用 __测试通过 加 「/」 加 做 转字符串 用 总数。
    若 __测试跳过 大于 0：
        做 打印 用 「 跳过: 」 加 做 转字符串 用 __测试跳过。
    完。
    若 __测试失败 等于 0：
        做 打印 用 「 状态: 全部通过!」。
    否则：
        做 打印 用 「 失败: 」 加 做 转字符串 用 __测试失败。
        做 打印 用 「」。
        可变 i 为 0。
        只要 i 小于 做 计数 用 __测试失败详情：
            详 为 __测试失败详情 的 第 i 项。
            做 打印 用 「 [」 加 详 的 第 0 项 加 「] 」 加 详 的 第 1 项。
            做 打印 用 「 期望: 」 加 详 的 第 2 项 加 「 实际: 」 加 详 的 第 3 项。
            i 为 i 加 1。
        完。
    完。
    做 打印 用 「」。
    若 做 计数 用 __测试套件列表 大于 0：
        做 打印 用 「」。
        做 打印 用 「 各测试组统计 」。
        可变 i 为 0。
        只要 i 小于 做 计数 用 __测试套件列表：
            套 为 __测试套件列表 的 第 i 项。
            可变 状态 为 「」。
            若 套 的 第 2 项 大于 0：
                状态 为 「」。
            完。
            做 打印 用 「 」 加 状态 加 「 」 加 套 的 第 0 项 加 「: 」 加 做 转字符串 用 套 的 第 1 项 加 「通过 」 加 做 转字符串 用 套 的 第 2 项 加 「failed」。
            i 为 i 加 1。
        完。
    完。
    返还 __测试失败。
完。

可变 __测试计时_开始 为 0。

可变 __基准测试结果 为 一组：（空）。

测试计时开始 接受 零参：
    __测试计时_开始 为 做 时间。
完。

测试计时结束 接受 说明：
    耗时 为 做 时间 减 __测试计时_开始。
    做 打印 用 「 」 加 说明 加 「: 」 加 做 转字符串 用 耗时 加 「ms」。
完。

基准测试 接受 名称、迭代数、测试函数：
    做 打印 用 「 基准: 」 加 名称 加 「 (」 加 做 转字符串 用 迭代数 加 「次)」。
    开始时刻 为 做 时间。
    可变 i 为 0。
    只要 i 小于 迭代数：
        做 测试函数。
        i 为 i 加 1。
    完。
    总耗时 为 做 时间 减 开始时刻。
    平均耗时 为 总耗时 除以 迭代数。
    做 追加 用 __基准测试结果、一组：名称、迭代数、总耗时、平均耗时。
    做 打印 用 「 完成 」 加 做 转字符串 用 迭代数 加 「 次迭代, 总耗时 」 加 做 转字符串 用 总耗时 加 「ms, 平均 」 加 做 转字符串 用 平均耗时 加 「ms」。
完。

基准报告 接受 零参：
    做 打印 用 「」。
    做 打印 用 「 基准测试报告 」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 __基准测试结果：
        项 为 __基准测试结果 的 第 i 项。
        若 做 计数 用 项 大于或等于 4：
            做 打印 用 「 」 加 项 的 第 0 项 加 「: 」 加 做 转字符串 用 项 的 第 1 项 加 「次, 总耗时 」 加 做 转字符串 用 项 的 第 2 项 加 「ms, 平均 」 加 做 转字符串 用 项 的 第 3 项 加 「ms」。
        否则：
            做 打印 用 「 」 加 项 的 第 0 项 加 「: 」 加 做 转字符串 用 项 的 第 1 项 加 「次」。
        完。
        i 为 i 加 1。
    完。
完。

测试_失败模式分析 接受 零参：
    若 做 计数 用 __测试失败详情 等于 0：
        返还 「无失败」。
    完。
    可变 同组失败 为 0。
    可变 上一组 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 __测试失败详情：
        若 __测试失败详情 的 第 i 项 的 第 0 项 等于 上一组：
            同组失败 为 同组失败 加 1。
        完。
        上一组 为 __测试失败详情 的 第 i 项 的 第 0 项。
        i 为 i 加 1。
    完。
    若 同组失败 大于 做 计数 用 __测试失败详情 除以 2：
        返还 「集中失败: 多个失败集中在同一测试组,可能是基础功能缺陷」。
    完。
    返还 「分散失败: 失败分布在多个测试组,可能是边界条件问题」。
完。

测试_覆盖率估算 接受 函数总数：
    已测试 为 __测试通过 加 __测试失败。
    若 函数总数 等于 0：
        返还 0。
    完。
    返还 已测试 乘 100 除以 函数总数。
完。

测试_健康评分 接受 零参：
    总 为 __测试通过 加 __测试失败。
    若 总 等于 0：
        返还 0。
    完。
    可变 评分 为 __测试通过 乘 100 除以 总。
    若 __测试跳过 大于 总 除以 10：
        评分 为 评分 减 10。
    完。
    若 评分 小于 0：
        评分 为 0。
    完。
    返还 评分。
完。

测试_推荐 接受 零参：
    若 __测试失败 大于 __测试通过：
        返还 「大量失败: 建议先修复核心功能再扩展测试」。
    完。
    若 __测试跳过 大于 5：
        返还 「跳过过多: 建议补全跳过的测试用例」。
    完。
    若 __测试通过 加 __测试失败 小于 10：
        返还 「用例不足: 建议增加边界条件和异常测试」。
    完。
    若 __测试失败 等于 0：
        返还 「全部通过: 建议增加压力测试和随机测试」。
    完。
    返还 「正常: 优先修复失败用例」。
完。

__测试_断言 接受 零参：
    __测试通过 为 0。
    __测试失败 为 0。
    __测试跳过 为 0。
    __测试名 为 「」。
完。

断言字符串相等 接受 实际、期望、说明：
    实长 为 做 计数 用 实际。
    期长 为 做 计数 用 期望。
    可变 匹配 为 假。
    若 实长 等于 期长：
        若 期长 等于 0：
            匹配 为 真。
        否则：
            若 做 字符串查找 用 实际、期望 等于 0：
                匹配 为 真。
            完。
        完。
    完。
    若 匹配：
        __测试通过 为 __测试通过 加 1。
        __当前套件通过 为 __当前套件通过 加 1。
        做 打印 用 「 」 加 说明。
        做 追加 用 __测试详情、一组：说明、「pass」、「」。
    否则：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 」 加 说明。
        做 打印 用 「  期望: 」 加 期望。
        做 打印 用 「  实际: 」 加 实际。
        做 追加 用 __测试失败详情、一组：__测试名、说明、期望、实际。
    完。
完。

断言字符串包含 接受 文本、子串文本、说明：
    可变 i 为 0。
    子长 为 做 计数 用 子串文本。
    文长 为 做 计数 用 文本。
    只要 i 加 子长 小于或等于 文长：
        若 做 子串 用 文本、i、子长 等于 子串文本：
            __测试通过 为 __测试通过 加 1。
            __当前套件通过 为 __当前套件通过 加 1。
            做 打印 用 「 ✓ 」 加 说明。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    __测试失败 为 __测试失败 加 1。
    __当前套件失败 为 __当前套件失败 加 1。
    做 打印 用 「 ✗ 」 加 说明 加 「 (不包含 '」 加 子串文本 加 「')」。
完。

断言字符串前缀 接受 文本、前缀、说明：
    前长 为 做 计数 用 前缀。
    若 做 计数 用 文本 大于或等于 前长 并且 做 子串 用 文本、0、前长 等于 前缀：
        __测试通过 为 __测试通过 加 1。
        __当前套件通过 为 __当前套件通过 加 1。
        做 打印 用 「 ✓ 」 加 说明。
    否则：
        __测试失败 为 __测试失败 加 1。
        __当前套件失败 为 __当前套件失败 加 1。
        做 打印 用 「 ✗ 」 加 说明 加 「 (不以 '」 加 前缀 加 「' 开头)」。
    完。
完。

断言字符串长度 接受 文本、期望、说明：
    做 断言相等 用 做 计数 用 文本、期望、说明。
完。

参数化测试 接受 名称、参数列表、测试函数：
    做 打印 用 「 ── 参数化: 」 加 名称 加 「 (」 加 做 转字符串 用 做 计数 用 参数列表 加 「组) ──」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数列表：
        做 测试函数 用 参数列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

重复测试 接受 名称、次数、测试函数：
    做 打印 用 「 ── 重复: 」 加 名称 加 「 (」 加 做 转字符串 用 次数 加 「次) ──」。
    可变 失败轮 为 0。
    可变 i 为 0。
    只要 i 小于 次数：
        前失败 为 __测试失败。
        做 测试函数。
        若 __测试失败 大于 前失败：
            失败轮 为 失败轮 加 1。
        完。
        i 为 i 加 1。
    完。
    若 失败轮 大于 0：
        做 打印 用 「 警告: 」 加 做 转字符串 用 失败轮 加 「/」 加 做 转字符串 用 次数 加 「 轮有失败」。
    完。
完。

测试夹具_创建 接受 设置函数、清理函数：
    返还 一组：设置函数、清理函数、一组：（空）。
完。

测试夹具_运行 接受 夹具、测试函数：
    做 夹具 的 第 0 项。
    做 测试函数。
    做 夹具 的 第 1 项。
完。

可变 __上次测试结果 为 一组：（空）。

测试_保存结果 接受 零参：
    __上次测试结果 为 一组：__测试通过、__测试失败、__测试跳过。
完。

测试_回归检测 接受 零参：
    若 做 计数 用 __上次测试结果 小于 3：
        返还 「无历史数据」。
    完。
    上次失败 为 __上次测试结果 的 第 1 项。
    若 __测试失败 大于 上次失败：
        返还 「回归: 失败数增加 」 加 做 转字符串 用 __测试失败 减 上次失败。
    完。
    若 __测试失败 小于 上次失败：
        返还 「改善: 失败数减少 」 加 做 转字符串 用 上次失败 减 __测试失败。
    完。
    返还 「稳定: 无变化」。
完。

测试_摘要 接受 零参：
    总 为 __测试通过 加 __测试失败。
    若 总 等于 0：
        返还 「无测试执行」。
    完。
    通过率 为 __测试通过 乘 100 除以 总。
    可变 等级 为 「A」。
    若 通过率 小于 90：
        等级 为 「B」。
    完。
    若 通过率 小于 70：
        等级 为 「C」。
    完。
    若 通过率 小于 50：
        等级 为 「D」。
    完。
    若 通过率 小于 30：
        等级 为 「F」。
    完。
    返还 等级 加 「级 (」 加 做 转字符串 用 通过率 加 「% 通过)」。
完。

测试_运行策略 接受 _总用例数、可用时间：
    若 可用时间 小于 10：
        返还 「烟雾测试: 只运行核心用例」。
    完。
    若 可用时间 小于 60：
        返还 「快速测试: 运行高优先级用例」。
    完。
    若 可用时间 小于 300：
        返还 「标准测试: 运行全部功能测试」。
    完。
    返还 「完整测试: 功能+性能+压力测试」。
完。

断言_总通过 接受 零参：
    返还 __测试通过。
完。

断言_总失败 接受 零参：
    返还 __测试失败。
完。

断言_总跳过 接受 零参：
    返还 __测试跳过。
完。

断言_总数 接受 零参：
    返还 __测试通过 加 __测试失败 加 __测试跳过。
完。

断言_通过率 接受 零参：
    总 为 做 断言_总数。
    若 总 等于 0：
        返还 0。
    完。
    返还 __测试通过 乘 100 除以 总。
完。

断言_质量评分 接受 零参：
    率 为 做 断言_通过率。
    若 率 大于或等于 95：
        返还 一组：率、「优秀」。
    完。
    若 率 大于或等于 80：
        返还 一组：率、「良好」。
    完。
    若 率 大于或等于 60：
        返还 一组：率、「及格」。
    完。
    返还 一组：率、「需要修复」。
完。

断言_摘要 接受 零参：
    质量 为 做 断言_质量评分。
    返还 一组：__测试通过、__测试失败、__测试跳过、质量 的 第 0 项、质量 的 第 1 项。
完。

本章 收束。
