《模块基线测试》

引用《..·..·标准库·系统·测试框架》。

引用《..·..·编译器·词法·词法分析》。

引用《..·..·编译器·语法·解析器》。

引用《..·..·编译器·语义·符号表》。

引用《..·..·编译器·标记常量》。

测试_词法_空源码 接受 零参：
    词法 为 做 词法器_创建 用 「」。
    做 断言_等于 用 做 扫描器_结束 用 词法、1。
    返还 真。
完。

测试_词法_单字符源码 接受 零参：
    词法 为 做 词法器_创建 用 「a」。
    做 断言_等于 用 做 扫描器_结束 用 词法、0。
    做 断言_等于 用 做 扫描器_当前 用 词法、97。
    返还 真。
完。

测试_词法_前进行号追踪 接受 零参：
    词法 为 做 词法器_创建 用 「a\nb」。
    做 扫描器_前进 用 词法。
    做 扫描器_前进 用 词法。
    做 断言_等于 用 做 扫描器_行号 用 词法、2。
    做 断言_等于 用 做 扫描器_列号 用 词法、1。
    返还 真。
完。

测试_词法_越界保护 接受 零参：
    词法 为 做 词法器_创建 用 「x」。
    做 扫描器_前进 用 词法。
    c 为 做 扫描器_当前 用 词法。
    做 断言_等于 用 c、0。
    返还 真。
完。

测试_解析器_空标记列表 接受 零参：
    p 为 做 解析器_创建 用 一组：（空）。
    做 断言_等于 用 做 解析器_结束 用 p、1。
    做 断言_等于 用 做 解析器_错误数 用 p、0。
    返还 真。
完。

测试_解析器_EOF哨兵 接受 零参：
    p 为 做 解析器_创建 用 一组：（空）。
    t 为 做 解析器_当前 用 p。
    做 断言_等于 用 t 的 第 0 项、0。
    返还 真。
完。

测试_解析器_前进 接受 零参：
    标记1 为 一组：标_整数、42、1。
    标记2 为 一组：标_加、「+」、1。
    p 为 做 解析器_创建 用 一组：标记1、标记2。
    t1 为 做 解析器_前进 用 p。
    做 断言_等于 用 t1 的 第 0 项、标_整数。
    做 断言_等于 用 t1 的 第 1 项、42。
    做 断言_等于 用 做 解析器_位置 用 p、1。
    返还 真。
完。

测试_解析器_窥视 接受 零参：
    标记1 为 一组：标_整数、1、1。
    标记2 为 一组：标_加、「+」、1。
    标记3 为 一组：标_整数、2、1。
    p 为 做 解析器_创建 用 一组：标记1、标记2、标记3。
    t2 为 做 解析器_窥视 用 p、1。
    做 断言_等于 用 t2 的 第 0 项、标_加。
    做 断言_等于 用 做 解析器_位置 用 p、0。
    返还 真。
完。

测试_解析器_错误记录 接受 零参：
    p 为 做 解析器_创建 用 一组：（空）。
    做 解析器_报错 用 p、1000、5。
    做 断言_等于 用 做 解析器_错误数 用 p、1。
    返还 真。
完。

测试_符号表_创建为空 接受 零参：
    表 为 做 符号表_创建 用 100。
    做 断言_等于 用 做 符号表_符号表_大小 用 表、0。
    返还 真。
完。

测试_符号表_追加_查找 接受 零参：
    表 为 做 符号表_创建 用 100。
    r 为 做 符号表_追加 用 表、42、符号表_符号_变量、0、1。
    做 断言_等于 用 r、1。
    做 断言_等于 用 做 符号表_符号表_大小 用 表、1。
    s 为 做 符号表_符号表_查找 用 表、42。
    做 断言_非空 用 s。
    做 断言_等于 用 做 符号_名称 用 s、42。
    返还 真。
完。

测试_符号表_查找不存在 接受 零参：
    表 为 做 符号表_创建 用 100。
    s 为 做 符号表_符号表_查找 用 表、999。
    做 断言_等于 用 s、0。
    返还 真。
完。

测试_符号表_容量上限 接受 零参：
    表 为 做 符号表_创建 用 2。
    做 符号表_追加 用 表、1、符号表_符号_变量、0、1。
    做 符号表_追加 用 表、2、符号表_符号_变量、0、1。
    r3 为 做 符号表_追加 用 表、3、符号表_符号_变量、0、1。
    做 断言_等于 用 r3、0。
    返还 真。
完。

测试_符号_偏移 接受 零参：
    s 为 做 符号_创建 用 10、符号表_符号_变量、1、0。
    做 符号_设偏移 用 s、16。
    做 断言_等于 用 做 符号_偏移 用 s、16。
    返还 真。
完。

测试_基础设施_基础_拼接 接受 零参：
    s 为 做 字符串拼接 用 「a」、「b」。
    做 断言_等于 用 做 计数 用 s、2。
    返还 真。
完。

测试_基础设施_列表追加 接受 零参：
    可变 lst 为 一组：（空）。
    做 追加 用 lst、1。
    做 追加 用 lst、2。
    做 追加 用 lst、3。
    做 断言_等于 用 做 计数 用 lst、3。
    做 断言_等于 用 lst 的 第 0 项、1。
    做 断言_等于 用 lst 的 第 2 项、3。
    返还 真。
完。

测试_基础设施_字典 接受 零参：
    d 为 做 字典。
    做 字典设 用 d、「key1」、100。
    做 断言_等于 用 做 字典取 用 d、「key1」、100。
    返还 真。
完。

模块基线_运行所有 接受 零参：
    套件id 为 做 测试框架_套件_创建 用 「Nova 模块基线 A.3」。
    做 套件_添加测试 用 套件id、「词法_空源码」、测试_词法_空源码。
    做 套件_添加测试 用 套件id、「词法_单字符」、测试_词法_单字符源码。
    做 套件_添加测试 用 套件id、「词法_行号追踪」、测试_词法_前进行号追踪。
    做 套件_添加测试 用 套件id、「词法_越界保护」、测试_词法_越界保护。
    做 套件_添加测试 用 套件id、「解析器_空」、测试_解析器_空标记列表。
    做 套件_添加测试 用 套件id、「解析器_EOF」、测试_解析器_EOF哨兵。
    做 套件_添加测试 用 套件id、「解析器_前进」、测试_解析器_前进。
    做 套件_添加测试 用 套件id、「解析器_窥视」、测试_解析器_窥视。
    做 套件_添加测试 用 套件id、「解析器_错误」、测试_解析器_错误记录。
    做 套件_添加测试 用 套件id、「符号表_创建」、测试_符号表_创建为空。
    做 套件_添加测试 用 套件id、「符号表_追加查找」、测试_符号表_追加_查找。
    做 套件_添加测试 用 套件id、「符号表_未找到」、测试_符号表_查找不存在。
    做 套件_添加测试 用 套件id、「符号表_容量」、测试_符号表_容量上限。
    做 套件_添加测试 用 套件id、「符号表_偏移」、测试_符号_偏移。
    做 套件_添加测试 用 套件id、「基础_字符串拼接」、测试_基础设施_基础_拼接。
    做 套件_添加测试 用 套件id、「基础_列表追加」、测试_基础设施_列表追加。
    做 套件_添加测试 用 套件id、「基础_字典」、测试_基础设施_字典。
    结果 为 做 套件_运行 用 套件id。
    通过 为 结果 的 第 0 项。
    失败 为 结果 的 第 1 项。
    做 打印文本 用 「═══════════════════════════」。
    做 打印文本 用 「Nova 模块基线测试 A.3 结果:」。
    做 打印文本 用 做 字符串拼接 用 「  通过: 」、做 转字符串 用 通过。
    做 打印文本 用 做 字符串拼接 用 「  失败: 」、做 转字符串 用 失败。
    做 打印文本 用 做 字符串拼接 用 「  总计: 」、做 转字符串 用 通过 加 失败。
    做 打印文本 用 「═══════════════════════════」。
    返还 失败 等于 0。
完。

做 模块基线_运行所有。

本章 收束。
