《格式化器》

引用《..·..·标准库·基础》。

格式配置_默认 接受 零参：
    返还 一组：4、真、100、假、1、0、真。
完。

格式配置_紧凑 接受 零参：
    返还 一组：2、真、120、假、0、真。
完。

格式化器_创建 接受 配置：
    返还 一组：配置、0、一组：（空）、「」、0。
完。

格式化器_格式化 接受 源码：
    配置 为 做 格式配置_默认。
    fmt 为 做 格式化器_创建 用 配置。
    行列表 为 做 格式_分割行 用 源码。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        做 格式化器_处理行 用 fmt、行列表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 做 计数 用 fmt 的 第 3 项 大于 0：
        做 追加 用 fmt 的 第 2 项、fmt 的 第 3 项。
    完。
    返还 做 格式_合并行 用 fmt 的 第 2 项。
完。

格式化器_处理行 接受 fmt、原始行：
    去空白 为 做 格式_去首尾空白 用 原始行。
    若 做 计数 用 去空白 等于 0：
        若 fmt 的 第 4 项 不等于 0：
            做 格式化器_输出行 用 fmt、「」。
            fmt 的 第 4 项 为 0。
        完。
        返还。
    完。
    若 做 格式_开头是 用 去空白、「//」：
        做 格式化器_输出行 用 fmt、做 字符串拼接 用 做 格式_缩进 用 fmt、去空白。
        fmt 的 第 4 项 为 1。
        返还。
    完。
    若 做 格式_开头是 用 去空白、「}」：
        fmt 的 第 1 项 为 fmt 的 第 1 项 减 1。
        若 fmt 的 第 1 项 小于 0：
            fmt 的 第 1 项 为 0。
        完。
    完。
    可变 格式行 为 做 格式_缩进 用 fmt。
    格式化内容 为 做 格式_规范化空白 用 去空白、fmt 的 第 0 项 的 第 6 项。
    格式行 为 做 字符串拼接 用 格式行、格式化内容。
    若 做 计数 用 格式行 大于 fmt 的 第 0 项 的 第 2 项：
        断行结果 为 做 格式_断行 用 格式行、fmt 的 第 0 项 的 第 2 项、fmt。
        可变 j 为 0。
        只要 j 小于 做 计数 用 断行结果：
            做 格式化器_输出行 用 fmt、断行结果 的 第 j 项。
            j 为 j 加 1。
        完。
    否则：
        做 格式化器_输出行 用 fmt、格式行。
    完。
    可变 k 为 0。
    只要 k 小于 做 计数 用 去空白：
        c 为 做 子串 用 去空白、k、1。
        若 做 字符串等于 用 c、「{」：
            fmt 的 第 1 项 为 fmt 的 第 1 项 加 1。
        完。
        k 为 k 加 1。
    完。
    若 做 格式_包含 用 去空白、「函数 」 或者 做 格式_包含 用 去空白、「法 」：
        fmt 的 第 4 项 为 3。
    否则若 做 格式_开头是 用 去空白、「}」：
        fmt 的 第 4 项 为 4。
    否则：
        fmt 的 第 4 项 为 2。
    完。
    若 fmt 的 第 4 项 等于 3 并且 做 计数 用 fmt 的 第 2 项 大于 0：
        可变 空行数 为 0。
        只要 空行数 小于 fmt 的 第 0 项 的 第 4 项：
            最后 为 fmt 的 第 2 项 的 第 做 计数 用 fmt 的 第 2 项 减 1 项。
            若 做 计数 用 做 格式_去首尾空白 用 最后 等于 0：
                空行数 为 fmt 的 第 0 项 的 第 4 项。
            否则：
                可变 新列表 为 一组：（空）。
                可变 m 为 0。
                只要 m 小于 做 计数 用 fmt 的 第 2 项：
                    做 追加 用 新列表、fmt 的 第 2 项 的 第 m 项。
                    m 为 m 加 1。
                完。
                做 追加 用 新列表、「」。
                fmt 的 第 2 项 为 新列表。
                空行数 为 空行数 加 1。
            完。
        完。
    完。
完。

格式化器_输出行 接受 fmt、行：
    做 追加 用 fmt 的 第 2 项、行。
完。

格式_缩进 接受 fmt：
    宽度 为 fmt 的 第 0 项 的 第 0 项 乘 fmt 的 第 1 项。
    可变 r 为 「」。
    可变 i 为 0。
    若 fmt 的 第 0 项 的 第 1 项：
        只要 i 小于 宽度：
            r 为 做 字符串拼接 用 r、「 」。
            i 为 i 加 1。
        完。
    否则：
        只要 i 小于 fmt 的 第 1 项：
            r 为 做 字符串拼接 用 r、「\t」。
            i 为 i 加 1。
        完。
    完。
    返还 r。
完。

格式_规范化空白 接受 行、操作符空格：
    n 为 做 计数 用 行。
    可变 结果 为 「」。
    可变 i 为 0。
    可变 在字符串 为 假。
    可变 上一空白 为 假。
    只要 i 小于 n：
        c 为 做 子串 用 行、i、1。
        若 做 字符串等于 用 c、「\"」：
            在字符串 为 在字符串 等于 假。
        完。
        若 在字符串：
            结果 为 做 字符串拼接 用 结果、c。
            i 为 i 加 1。
            续。
        完。
        若 做 字符串等于 用 c、「 」 或者 做 字符串等于 用 c、「\t」：
            若 上一空白 等于 假 并且 做 计数 用 结果 大于 0：
                结果 为 做 字符串拼接 用 结果、「 」。
            完。
            上一空白 为 真。
            i 为 i 加 1。
            续。
        完。
        上一空白 为 假。
        若 操作符空格：
            若 做 字符串等于 用 c、「=」 或者 做 字符串等于 用 c、「+」 或者 做 字符串等于 用 c、「-」 或者 做 字符串等于 用 c、「*」 或者 做 字符串等于 用 c、「/」：
                可变 双字符 为 假。
                若 i 加 1 小于 n：
                    下一 为 做 子串 用 行、i 加 1、1。
                    若 做 字符串等于 用 下一、「=」：
                        双字符 为 真。
                    完。
                完。
                若 双字符：
                    若 做 计数 用 结果 大于 0 并且 做 字符串等于 用 做 子串 用 结果、做 计数 用 结果 减 1、1、「 」 等于 假：
                        结果 为 做 字符串拼接 用 结果、「 」。
                    完。
                    结果 为 做 字符串拼接 用 结果、c。
                    结果 为 做 字符串拼接 用 结果、做 子串 用 行、i 加 1、1。
                    结果 为 做 字符串拼接 用 结果、「 」。
                    i 为 i 加 2。
                    续。
                完。
            完。
        完。
        结果 为 做 字符串拼接 用 结果、c。
        i 为 i 加 1。
    完。
    只要 做 计数 用 结果 大于 0 并且 做 字符串等于 用 做 子串 用 结果、做 计数 用 结果 减 1、1、「 」：
        结果 为 做 子串 用 结果、0、做 计数 用 结果 减 1。
    完。
    返还 结果。
完。

格式_断行 接受 行、最大宽、fmt：
    可变 结果 为 一组：（空）。
    n 为 做 计数 用 行。
    可变 当前 为 「」。
    可变 i 为 0。
    可变 在字符串 为 假。
    只要 i 小于 n：
        c 为 做 子串 用 行、i、1。
        若 做 字符串等于 用 c、「\"」：
            在字符串 为 在字符串 等于 假。
        完。
        当前 为 做 字符串拼接 用 当前、c。
        若 在字符串 等于 假 并且 做 字符串等于 用 c、「,」 并且 做 计数 用 当前 大于 最大宽 除以 2：
            做 追加 用 结果、当前。
            当前 为 做 格式_缩进 用 fmt。
            当前 为 做 字符串拼接 用 当前、「    」。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前 大于 0：
        做 追加 用 结果、当前。
    完。
    若 做 计数 用 结果 等于 0：
        做 追加 用 结果、行。
    完。
    返还 结果。
完。

格式_分割行 接受 文本：
    n 为 做 计数 用 文本。
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 文本、i、1。
        若 做 字符串等于 用 c、「\n」：
            做 追加 用 结果、当前。
            当前 为 「」。
        否则若 做 字符串等于 用 c、「\r」 等于 假：
            当前 为 做 字符串拼接 用 当前、c。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前 大于 0：
        做 追加 用 结果、当前。
    完。
    返还 结果。
完。

格式_合并行 接受 行列表：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        若 i 大于 0：
            结果 为 做 字符串拼接 用 结果、「\n」。
        完。
        结果 为 做 字符串拼接 用 结果、行列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

格式_去首尾空白 接受 s：
    n 为 做 计数 用 s。
    可变 开始 为 0。
    只要 开始 小于 n 并且 (做 字符串等于 用 做 子串 用 s、开始、1、「 」 或者 做 字符串等于 用 做 子串 用 s、开始、1、「\t」)：
        开始 为 开始 加 1。
    完。
    可变 结束 为 n。
    只要 结束 大于 开始 并且 (做 字符串等于 用 做 子串 用 s、结束 减 1、1、「 」 或者 做 字符串等于 用 做 子串 用 s、结束 减 1、1、「\t」)：
        结束 为 结束 减 1。
    完。
    若 开始 大于或等于 结束：
        返还 「」。
    完。
    返还 做 子串 用 s、开始、结束 减 开始。
完。

格式_开头是 接受 s、前缀：
    若 做 计数 用 s 小于 做 计数 用 前缀：
        返还 假。
    完。
    返还 做 字符串等于 用 做 子串 用 s、0、做 计数 用 前缀、前缀。
完。

格式_包含 接受 文本、子串文本：
    n 为 做 计数 用 文本。
    m 为 做 计数 用 子串文本。
    若 m 大于 n：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于或等于 n 减 m：
        若 做 字符串等于 用 做 子串 用 文本、i、m、子串文本：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

本章 收束。
