《性能分析器》

引用《..·..·标准库·基础》。

调用记录_创建 接受 函数名、开始tick、调用者ID、深度：
    返还 一组：函数名、开始tick、0、调用者ID、深度。
完。

调用记录_结束 接受 记录、结束tick：
    记录 的 第 2 项 为 结束tick。
完。

调用记录_耗时 接受 记录：
    返还 记录 的 第 2 项 减 记录 的 第 1 项。
完。

函数统计_创建 接受 函数名：
    返还 一组：函数名、0、x7FFFFFFF、0、一组：（空）。
完。

函数统计_更新 接受 统计、耗时：
    统计 的 第 1 项 为 统计 的 第 1 项 加 1。
    统计 的 第 2 项 为 统计 的 第 2 项 加 耗时。
    若 耗时 小于 统计 的 第 4 项：
        统计 的 第 4 项 为 耗时。
    完。
    若 耗时 大于 统计 的 第 5 项：
        统计 的 第 5 项 为 耗时。
    完。
完。

函数统计_平均 接受 统计：
    若 统计 的 第 1 项 等于 0：
        返还 0。
    完。
    返还 统计 的 第 2 项 除以 统计 的 第 1 项。
完。

分析器_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、做 字典、0、0、假、一组：（空）。
完。

分析器_开始 接受 分析器、当前tick：
    分析器 的 第 5 项 为 当前tick。
    分析器 的 第 3 项 为 当前tick。
    分析器 的 第 6 项 为 真。
完。

分析器_停止 接受 分析器、当前tick：
    分析器 的 第 3 项 为 当前tick。
    分析器 的 第 6 项 为 假。
    只要 做 计数 用 分析器 的 第 1 项 大于 0：
        做 分析器_函数退出 用 分析器、当前tick。
    完。
完。

分析器_函数进入 接受 分析器、函数名、当前tick：
    若 分析器 的 第 6 项 等于 假：
        返还。
    完。
    分析器 的 第 3 项 为 当前tick。
    可变 调用者ID 为 负 1。
    若 做 计数 用 分析器 的 第 1 项 大于 0：
        调用者ID 为 分析器 的 第 1 项 的 第 做 计数 用 分析器 的 第 1 项 减 1 项。
    完。
    可变 记录 为 做 调用记录_创建 用 函数名、当前tick、调用者ID、做 计数 用 分析器 的 第 1 项。
    可变 记录ID 为 做 计数 用 分析器 的 第 0 项。
    做 追加 用 分析器 的 第 0 项、记录。
    做 追加 用 分析器 的 第 1 项、记录ID。
    分析器 的 第 4 项 为 分析器 的 第 4 项 加 1。
完。

分析器_函数退出 接受 分析器、当前tick：
    若 做 计数 用 分析器 的 第 1 项 等于 0：
        返还。
    完。
    分析器 的 第 3 项 为 当前tick。
    记录ID 为 分析器 的 第 1 项 的 第 做 计数 用 分析器 的 第 1 项 减 1 项。
    可变 新栈 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分析器 的 第 1 项 减 1：
        做 追加 用 新栈、分析器 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    分析器 的 第 1 项 为 新栈。
    可变 记录 为 分析器 的 第 0 项 的 第 记录ID 项。
    做 调用记录_结束 用 记录、当前tick。
    函数名 为 记录 的 第 0 项。
    可变 统计 为 做 字典取 用 分析器 的 第 2 项、函数名。
    若 统计 等于 空：
        统计 为 做 函数统计_创建 用 函数名。
        做 字典设 用 分析器 的 第 2 项、函数名、统计。
    完。
    做 函数统计_更新 用 统计、做 调用记录_耗时 用 记录。
完。

分析器_内存快照 接受 分析器、当前tick、总分配、当前使用、峰值、分配次数、释放次数：
    做 追加 用 分析器 的 第 7 项、一组：当前tick、总分配、当前使用、峰值、分配次数、释放次数。
完。

分析器_热点 接受 分析器、前N个：
    可变 列表 为 一组：（空）。
    可变 已添加 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分析器 的 第 0 项：
        名 为 分析器 的 第 0 项 的 第 i 项 的 第 0 项。
        若 做 字典取 用 已添加、名 等于 空：
            统计 为 做 字典取 用 分析器 的 第 2 项、名。
            若 统计 不等于 空：
                做 追加 用 列表、统计。
                做 字典设 用 已添加、名、真。
            完。
        完。
        i 为 i 加 1。
    完。
    i 为 1。
    只要 i 小于 做 计数 用 列表：
        当前 为 列表 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 列表 的 第 j 项 的 第 2 项 小于 当前 的 第 2 项：
            列表 的 第 j 加 1 项 为 列表 的 第 j 项。
            j 为 j 减 1。
        完。
        列表 的 第 j 加 1 项 为 当前。
        i 为 i 加 1。
    完。
    若 前N个 大于 做 计数 用 列表：
        前N个 为 做 计数 用 列表。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 前N个：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

分析器_调用图 接受 分析器：
    可变 边列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分析器 的 第 0 项：
        可变 记录 为 分析器 的 第 0 项 的 第 i 项。
        若 记录 的 第 3 项 大于或等于 0：
            调用者 为 分析器 的 第 0 项 的 第 记录 的 第 3 项 项 的 第 0 项。
            做 追加 用 边列表、一组：调用者、记录 的 第 0 项、做 调用记录_耗时 用 记录。
        完。
        i 为 i 加 1。
    完。
    返还 边列表。
完。

分析器_火焰图数据 接受 分析器：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分析器 的 第 0 项：
        记录 为 分析器 的 第 0 项 的 第 i 项。
        若 记录 的 第 2 项 等于 0：
            i 为 i 加 1。
            续。
        完。
        可变 路径 为 记录 的 第 0 项。
        可变 父ID 为 记录 的 第 3 项。
        只要 父ID 大于或等于 0：
            路径 为 做 字符串拼接 用 分析器 的 第 0 项 的 第 父ID 项 的 第 0 项、做 字符串拼接 用 「;」、路径。
            父ID 为 分析器 的 第 0 项 的 第 父ID 项 的 第 3 项。
        完。
        做 追加 用 结果、一组：路径、做 调用记录_耗时 用 记录。
        i 为 i 加 1。
    完。
    返还 结果。
完。

分析器_文本报告 接受 分析器：
    总时间 为 分析器 的 第 3 项 减 分析器 的 第 5 项。
    可变 报告 为 「═══ 性能分析报告 ═══\n」。
    报告 为 做 字符串拼接 用 报告、「总运行时间: 」。
    报告 为 做 字符串拼接 用 报告、做 分析_数到文 用 总时间。
    报告 为 做 字符串拼接 用 报告、「 ticks\n」。
    报告 为 做 字符串拼接 用 报告、「总采样数: 」。
    报告 为 做 字符串拼接 用 报告、做 分析_数到文 用 分析器 的 第 4 项。
    报告 为 做 字符串拼接 用 报告、「\n\n--- 热点函数 (Top 10) ---\n」。
    热点 为 做 分析器_热点 用 分析器、10。
    可变 i 为 0。
    只要 i 小于 做 计数 用 热点：
        s 为 热点 的 第 i 项。
        报告 为 做 字符串拼接 用 报告、做 分析_数到文 用 i 加 1。
        报告 为 做 字符串拼接 用 报告、「. 」。
        报告 为 做 字符串拼接 用 报告、s 的 第 0 项。
        报告 为 做 字符串拼接 用 报告、「 调用:」。
        报告 为 做 字符串拼接 用 报告、做 分析_数到文 用 s 的 第 1 项。
        报告 为 做 字符串拼接 用 报告、「 总耗时:」。
        报告 为 做 字符串拼接 用 报告、做 分析_数到文 用 s 的 第 2 项。
        报告 为 做 字符串拼接 用 报告、「 平均:」。
        报告 为 做 字符串拼接 用 报告、做 分析_数到文 用 做 函数统计_平均 用 s。
        报告 为 做 字符串拼接 用 报告、「\n」。
        i 为 i 加 1。
    完。
    若 做 计数 用 分析器 的 第 7 项 大于 0：
        最后 为 分析器 的 第 7 项 的 第 做 计数 用 分析器 的 第 7 项 减 1 项。
        报告 为 做 字符串拼接 用 报告、「\n--- 内存 ---\n」。
        报告 为 做 字符串拼接 用 报告、「当前使用: 」。
        报告 为 做 字符串拼接 用 报告、做 分析_数到文 用 最后 的 第 2 项。
        报告 为 做 字符串拼接 用 报告、「 峰值: 」。
        报告 为 做 字符串拼接 用 报告、做 分析_数到文 用 最后 的 第 3 项。
        报告 为 做 字符串拼接 用 报告、「\n」。
    完。
    返还 报告。
完。

分析器_自动开始 接受 分析器：
    做 分析器_开始 用 分析器、做 时间。
完。

分析器_自动停止 接受 分析器：
    做 分析器_停止 用 分析器、做 时间。
完。

分析器_自动进入 接受 分析器、函数名：
    做 分析器_函数进入 用 分析器、函数名、做 时间。
完。

分析器_自动退出 接受 分析器：
    做 分析器_函数退出 用 分析器、做 时间。
完。

分析器_自动内存快照 接受 分析器、总分配、当前使用、峰值、分配次数、释放次数：
    做 分析器_内存快照 用 分析器、做 时间、总分配、当前使用、峰值、分配次数、释放次数。
完。

分析器_测量 接受 函数引用、参数：
    开始 为 做 时间。
    结果 为 做 函数引用 用 参数。
    耗时 为 做 时间 减 开始。
    返还 一组：结果、耗时。
完。

分析_数到文 接受 n：
    若 n 等于 0：
        返还 「0」。
    完。
    可变 v 为 n。
    可变 负 为 假。
    若 v 小于 0：
        负 为 真。
        v 为 0 减 v。
    完。
    可变 r 为 「」。
    只要 v 大于 0：
        r 为 做 字符串拼接 用 做 子串 用 「0123456789」、v 取余于 10、1、r。
        v 为 v 除以 10。
    完。
    若 负：
        r 为 做 字符串拼接 用 「-」、r。
    完。
    返还 r。
完。

本章 收束。
