《帮助系统》

帮助_概述 接受 零参：
    做 打印 用 「================================================================================」。
    做 打印 用 「                    龙 Nova Nova编译器 v3.1.0」。
    做 打印 用 「              纯中文 -> x86-64 原生机器码 (Linux ELF)」。
    做 打印 用 「================================================================================」。
    做 打印 用 「」。
    做 打印 用 「用法:」。
    做 打印 用 「  nova <源文件.nova>        编译Nova源文件为x86-64原生可执行文件」。
    做 打印 用 「  nova 帮助                 显示此帮助信息」。
    做 打印 用 「  nova 帮助 语法            Nova语言语法参考」。
    做 打印 用 「  nova 帮助 内建            内建函数完整列表(65个)」。
    做 打印 用 「  nova 帮助 标准库          标准库模块概览(237个模块, 5800+法)」。
    做 打印 用 「  nova 帮助 架构            编译器架构与x86-64指令集」。
    做 打印 用 「  nova 帮助 示例            代码示例」。
    做 打印 用 「  nova 帮助 ai              AI精准开发参考(语法规范/限制/模式)」。
    做 打印 用 「  nova 帮助 全部            输出全部帮助(供AI参考)」。
    做 打印 用 「」。
    做 打印 用 「概述:」。
    做 打印 用 「  Nova编译器将Nova语言(.nova)源代码直接编译为x86-64原生可执行文件(ELF64)。」。
    做 打印 用 「  无需任何外部工具链/链接器/运行时。纯中文关键字,零英文依赖。」。
    做 打印 用 「  内含完整标准库: AI/神经网络/数学/系统/网络/图形/加密等237个模块。」。
    做 打印 用 「  支持自举: 编译器可编译自身,Stage2==Stage3二进制完全一致。」。
    做 打印 用 「」。
    做 打印 用 「核心特性:」。
    做 打印 用 「  [原生编译] 直接生成x86-64机器码,不经过中间语言,不依赖外部编译器」。
    做 打印 用 「  [纯中文]   关键字/标识符/错误信息全部中文,中文是直击0/1的原生语言」。
    做 打印 用 「  [单文件]   生成独立PE可执行文件,无外部依赖,可直接运行」。
    做 打印 用 「  [自举]     编译器可编译自身,输出二进制SHA256一致」。
    做 打印 用 「  [5800+法] 覆盖AI/数学/系统/网络/图形/加密/数据结构等全领域」。
    做 打印 用 「  [闭包]     匿名函数自动捕获外层变量」。
    做 打印 用 「  [结构体]   用户定义复合数据类型,点号访问/赋值」。
    做 打印 用 「  [模块导入] 多文件项目,入/引用两种方式」。
    做 打印 用 「  [辨]     模式匹配(switch/match)语句」。
    做 打印 用 「  [异常]     试/得/抛 异常处理」。
    做 打印 用 「  [每个循环] 每 x 在 列表 < > 遍历语法」。
    做 打印 用 「」。
    做 打印 用 「编译示例:」。
    做 打印 用 「  nova hello.nova           -> 生成 hello」。
    做 打印 用 「  nova 自举入口.nova        -> 自举编译,生成新编译器」。
完。

帮助_语法 接受 零参：
    做 打印 用 「================================================================================」。
    做 打印 用 「                         Nova 语言语法参考」。
    做 打印 用 「================================================================================」。
    做 打印 用 「」。
    做 打印 用 「■ 关键字 (22个单字原创关键字):」。
    做 打印 用 「  定义      不可变绑定        定义 x = 10」。
    做 打印 用 「  定义 可变 可变绑定          定义 可变 x = 0」。
    做 打印 用 「  函数 定义函数          函数 加法(a, b) ：返还 a + b }」。
    做 打印 用 「  返回      返回值            返回 结果」。
    做 打印 用 「  如果 (     条件判断          如果 (x > 0) { ... }」。
    做 打印 用 「  则      条件引导          如果 (...) { }」。
    做 打印 用 「  否      条件否则          否则 { ... } 或 否则 如果 (...) { ... }」。
    做 打印 用 「  当 (     条件循环          当 i < 10) { i = i + 1 }」。
    做 打印 用 「  时      循环引导          当 (...) { }」。
    做 打印 用 「  永      无限循环          永 ：若 (条件) { _止 } }」。
    做 打印 用 「  每      遍历              每 项 在 列表 { 打印(项) }」。
    做 打印 用 「  在      遍历介词          每 X 在 Y { }」。
    做 打印 用 「  _止      跳出循环          止」。
    做 打印 用 「  续      跳过本次          续」。
    做 打印 用 「  入      导入模块          导入('模块.nova')」。
    做 打印 用 「  用      引用模块          用 '../模块.nova'」。
    做 打印 用 「  结构 定义结构体        结构 点 { x, y }」。
    做 打印 用 「  建      创建结构实例      定义 p = 建 点 { x: 1, y: 2 }」。
    做 打印 用 「  辨      模式辨别          辨 值 { 为 1 { ... } 为 2 { ... } 余 { ... } }」。
    做 打印 用 「  为      辨别分支          为 模式值 { }」。
    做 打印 用 「  余      辨别默认          余 { }」。
    做 打印 用 「  试      异常处理          试 { ... } 得 错误 { ... }」。
    做 打印 用 「  得      捕获异常          得 错误名 { 处理代码 }」。
    做 打印 用 「  抛      抛出异常          抛('错误信息')」。
    做 打印 用 「  空      空值              如果 (x == 空) { ... }」。
    做 打印 用 「  真/假   布尔值            定义 标志 = 真」。
    做 打印 用 「」。
    做 打印 用 「■ 数据类型:」。
    做 打印 用 「  整数      64位有符号        定义 x = 42」。
    做 打印 用 「  浮点      64位双精度        定义 pi = 3.14159」。
    做 打印 用 「  字符串    UTF-8             定义 名字 = '你好世界'」。
    做 打印 用 「  列表      动态数组          定义 表 = [1, 2, 3]」。
    做 打印 用 「  字典      哈希映射          定义 映射 = 字典()」。
    做 打印 用 「  结构体    复合类型          定义 点 = 建 坐标 { x: 1, y: 2 }」。
    做 打印 用 「  函数 一等公民          定义 回调 = 法(x) ：返还 x + 1 }」。
    做 打印 用 「  空      空值              空」。
    做 打印 用 「」。
    做 打印 用 「■ 运算符:」。
    做 打印 用 「  算术    + - * / %           加减乘除取模」。
    做 打印 用 「  比较    == != < > <= >=     等于/不等/小于/大于/小等/大等」。
    做 打印 用 「  逻辑    && || !             与/或/非」。
    做 打印 用 「  位运算  & | ^ ~ << >>       按位与/或/异或/取反/左移/右移」。
    做 打印 用 「  赋值    =                   赋值」。
    做 打印 用 「  索引    a[i]                列表/字典索引」。
    做 打印 用 「  点号    a.字段              结构体字段访问」。
    做 打印 用 「  字符串  +                   字符串拼接: '你' + '好' -> '你好'」。
    做 打印 用 「」。
    做 打印 用 「■ 函数定义:」。
    做 打印 用 「  函数 名称(参数1, 参数2) {」。
    做 打印 用 「      // 函数体」。
    做 打印 用 「      返回 结果」。
    做 打印 用 「  }」。
    做 打印 用 「」。
    做 打印 用 「■ 条件控制(中文自然语法, 无C强制括号):」。
    做 打印 用 「  如果 (x > 0) {」。
    做 打印 用 「      打印(x)」。
    做 打印 用 「  否则若 (x == 0) {」。
    做 打印 用 「      打印(0)」。
    做 打印 用 「  否则：」。
    做 打印 用 「      打印(-1)」。
    做 打印 用 「  }」。
    做 打印 用 「」。
    做 打印 用 「■ 循环(中文自然语法):」。
    做 打印 用 「  当 (i < 10) { i = i + 1 }       // 条件循环」。
    做 打印 用 「  永 ：若 (条件) { _止 } }          // 无限循环」。
    做 打印 用 「  每 项 在 列表 { 打印(项) }        // 遍历」。
    做 打印 用 「」。
    做 打印 用 「■ 模式辨别(原创语法):」。
    做 打印 用 「  辨 值 {」。
    做 打印 用 「      为 1 { 打印(1) }」。
    做 打印 用 「      为 2 { 打印(2) }」。
    做 打印 用 「      余 { 打印(0) }」。
    做 打印 用 「  }」。
    做 打印 用 「」。
    做 打印 用 「■ 匿名函数/闭包:」。
    做 打印 用 「  定义 平方 = 法(x) ：返还 x * x }」。
    做 打印 用 「  定义 加n = 法(n) ：返还 法(x) ：返还 x + n } }  // 闭包」。
    做 打印 用 「  定义 箭头 = (x) => x * 2                      // 箭头函数」。
    做 打印 用 「」。
    做 打印 用 「■ 结构体:」。
    做 打印 用 「  结构 向量 { x, y, z }」。
    做 打印 用 「  定义 v = 建 向量 { x: 1, y: 2, z: 3 }」。
    做 打印 用 「  打印(v.x)                    // 访问字段」。
    做 打印 用 「  v.x = 10                     // 修改字段」。
    做 打印 用 「」。
    做 打印 用 「■ 模块导入:」。
    做 打印 用 「  导入('标准库/数学/数学.nova')      // 导入文件中所有函数」。
    做 打印 用 「  用 '../模块.nova'                // 引用方式导入」。
    做 打印 用 「」。
    做 打印 用 「■ 异常处理:」。
    做 打印 用 「  试 {」。
    做 打印 用 「      定义 结果 = 危险操作()」。
    做 打印 用 「  } 得 错误 {」。
    做 打印 用 「      打印文本(字符串拼接('错误: ', 转字符串(错误)))」。
    做 打印 用 「  }」。
    做 打印 用 「」。
    做 打印 用 「■ 自然语言编程(像写文章一样开发):」。
    做 打印 用 「  中文运算:  3 加 5    10 减 3    4 乘 5    15 除 3」。
    做 打印 用 「  中文比较:  x 大于 0  x 小于 10  x 等于 y  x 不等于 y」。
    做 打印 用 「  中文范围:  x 不小于 60  x 不大于 100」。
    做 打印 用 「  中文逻辑:  A 并且 B  A 或者 B  非 条件」。
    做 打印 用 「  中文赋值:  定义 名字 是 '小明'    定义 可变 计数 是 0」。
    做 打印 用 「  中文访问:  学生 的 姓名         列表 的 长度」。
    做 打印 用 「  自然输出:  说('你好世界')       显示(变量)」。
    做 打印 用 「  自然循环:  重复 5 次 { ... }    从 1 到 10 { ... }」。
    做 打印 用 「  所有写法直接编译为x86-64机器码, 无中间语言, 不套壳」。
完。

帮助_内建 接受 零参：
    可变 列表 为 一组：（空）。
    做 打印 用 「================================================================================」。
    做 打印 用 「                       内建函数完整列表 (78个)」。
    做 打印 用 「================================================================================」。
    做 打印 用 「」。
    做 打印 用 「■ 输出 (3个):」。
    做 打印 用 「  打印(字符串字面量)          输出字符串字面量+换行」。
    做 打印 用 「  打印文本(字符串表达式)      输出字符串表达式+换行」。
    做 打印 用 「  打印值(任意值)              输出任意值的调试表示」。
    做 打印 用 「」。
    做 打印 用 「■ 字符串操作 (15个):」。
    做 打印 用 「  字符串拼接(a, b)            拼接两个字符串 -> 新字符串」。
    做 打印 用 「  字符串等于(a, b)            比较两个字符串是否相等 -> 布尔」。
    做 打印 用 「  子串(字符串, 起始, 长度)     提取子字符串」。
    做 打印 用 「  字节长度(字符串)            返回字符串的字节长度」。
    做 打印 用 「  字节取(字符串, 索引)        取第n个字节的值」。
    做 打印 用 「  编码(字符串)                返回首字符的Unicode编码值」。
    做 打印 用 「  字符(编码值)                Unicode编码值转字符」。
    做 打印 用 「  替换(字符串, 旧, 新)        替换子串」。
    做 打印 用 「  查找(字符串, 子串)          查找子串位置, -1=未找到」。
    做 打印 用 「  包含(字符串, 子串)          是否包含子串 -> 布尔」。
    做 打印 用 「  以开头(字符串, 前缀)        是否以前缀开头 -> 布尔」。
    做 打印 用 「  以结尾(字符串, 后缀)        是否以后缀结尾 -> 布尔」。
    做 打印 用 「  大写化(字符串)              转大写」。
    做 打印 用 「  小写化(字符串)              转小写」。
    做 打印 用 「  分割(字符串, 分隔符)        按分隔符分割字符串 -> 列表」。
    做 打印 用 「」。
    做 打印 用 「■ 类型转换 (4个):」。
    做 打印 用 「  转字符串(值)                任意值转字符串表示」。
    做 打印 用 「  整数(字符串)                字符串转整数」。
    做 打印 用 「  类型(值)                    返回值的类型名: '整数'/'字符串'/'列表'/'字典'」。
    做 打印 用 「  拼接(a, b)                  通用拼接(字符串或列表)」。
    做 打印 用 「」。
    做 打印 用 「■ 列表操作 (8个):」。
    做 打印 用 「  计数(列表或字符串)          返回元素数量或字符串长度」。
    做 打印 用 「  追加(列表, 值)              在列表末尾添加元素」。
    做 打印 用 「  插入(列表, 索引, 值)        在指定位置插入元素」。
    做 打印 用 「  删除(列表, 索引)            删除指定位置的元素, 返回被删值」。
    做 打印 用 「  设置(列表, 索引, 值)        设置指定位置的值」。
    做 打印 用 「  范围(起, _止, 步)            生成整数范围列表」。
    做 打印 用 「  是列表(值)                  判断是否为列表 -> 布尔」。
    做 打印 用 「  是整数(值)                  判断是否为整数 -> 布尔」。
    做 打印 用 「」。
    做 打印 用 「■ 字典操作 (7个):」。
    做 打印 用 「  字典()                      创建空字典」。
    做 打印 用 「  字典取(字典, 键)            获取值, 键不存在返回空」。
    做 打印 用 「  字典设(字典, 键, 值)        设置键值对」。
    做 打印 用 「  字典有(字典, 键)            键是否存在 -> 布尔」。
    做 打印 用 「  字典键(字典)                返回所有键的列表」。
    做 打印 用 「  字典值(字典)                返回所有值的列表」。
    做 打印 用 「  是字典(值)                  判断是否为字典 -> 布尔」。
    做 打印 用 「  是字符串(值)                判断是否为字符串 -> 布尔」。
    做 打印 用 「」。
    做 打印 用 「■ 数学 (5个):」。
    做 打印 用 「  绝对值(数)                  返回绝对值」。
    做 打印 用 「  最大值(a, b)                返回较大值」。
    做 打印 用 「  最小值(a, b)                返回较小值」。
    做 打印 用 「  幂(底数, 指数)              整数幂运算」。
    做 打印 用 「  随机数(0)                    返回伪随机整数」。
    做 打印 用 「」。
    做 打印 用 「■ 文件操作 (7个):」。
    做 打印 用 「  读文件(路径)                读取文件全部内容为字符串, 失败返回空」。
    做 打印 用 「  写文件(路径,  内容, 0)          写入字符串到文件」。
    做 打印 用 「  文件存在(路径)              检查文件是否存在 -> 布尔」。
    做 打印 用 「  读字节(路径)                读取文件为字节缓冲区」。
    做 打印 用 「  写字节(路径, 缓冲区, 大小)  写入字节缓冲区到文件」。
    做 打印 用 「  写16(路径, 缓冲区, 大小)    以16位格式写入文件」。
    做 打印 用 「  写32(路径, 缓冲区, 大小)    以32位格式写入文件」。
    做 打印 用 「」。
    做 打印 用 「■ 缓冲区操作 (6个):」。
    做 打印 用 「  缓冲区(大小)                分配指定大小的字节缓冲区」。
    做 打印 用 「  缓冲写(缓冲区, 偏移, 字节值)   写入1字节」。
    做 打印 用 「  缓冲写16(缓冲区, 偏移, 值)     写入2字节(小端)」。
    做 打印 用 「  缓冲写32(缓冲区, 偏移, 值)     写入4字节(小端)」。
    做 打印 用 「  缓冲读(缓冲区, 偏移)           读取1字节」。
    做 打印 用 「  分配(大小)                     分配堆内存」。
    做 打印 用 「」。
    做 打印 用 「■ 内存操作 (8个):」。
    做 打印 用 「  内存读(地址)                读取8字节(64位)」。
    做 打印 用 「  内存读32(地址)              读取4字节(32位)」。
    做 打印 用 「  内存读16(地址)              读取2字节(16位)」。
    做 打印 用 「  内存读8(地址)               读取1字节(8位)」。
    做 打印 用 「  内存写(地址, 值)            写入8字节」。
    做 打印 用 「  内存写32(地址,  值, 0)          写入4字节」。
    做 打印 用 「  内存写16(地址,  值, 0)          写入2字节」。
    做 打印 用 「  内存写8(地址, 值)           写入1字节」。
    做 打印 用 「  内存复制(目标, 源, 长度)    内存块复制」。
    做 打印 用 「  内存填充(地址, 值, 长度)    内存块填充」。
    做 打印 用 「」。
    做 打印 用 「■ 系统 (10个):」。
    做 打印 用 「  退出(代码)                  退出程序,返回退出码」。
    做 打印 用 「  参数()                      获取命令行参数列表」。
    做 打印 用 「  加载库(库名)                加载DLL动态库 -> 库句柄」。
    做 打印 用 「  获取函数(库句柄, 函数名)    获取DLL函数地址」。
    做 打印 用 「  调用指针(函数地址,  参数..., 0, 0, 0, 0)  调用函数指针(最多3个参数)」。
    做 打印 用 「  调用指针4(函数地址,  a1..a4, 0 , 0, 0)  调用函数指针(4个参数)」。
    做 打印 用 「  调用指针5(函数地址,  a1..a5, 0 , 0 , 0, 0)  调用函数指针(5个参数)」。
    做 打印 用 「  时间戳()                    返回系统毫秒时间戳」。
    做 打印 用 「  延时(毫秒)                  线程休眠指定毫秒数」。
    做 打印 用 「」。
    做 打印 用 「■ 硬件信息 (3个):」。
    做 打印 用 「  处理器名()                  返回CPU品牌字符串」。
    做 打印 用 「  总内存()                    返回系统总物理内存(字节)」。
    做 打印 用 「  可用内存()                  返回系统可用物理内存(字节)」。
完。

帮助_标准库 接受 零参：
    做 打印 用 「================================================================================」。
    做 打印 用 「                   标准库模块概览 (237模块, 5800+法)」。
    做 打印 用 「================================================================================」。
    做 打印 用 「」。
    做 打印 用 「■ 核心模块 (12个):」。
    做 打印 用 「  基础            追加/字符码/验证/映射操作」。
    做 打印 用 「  数据结构        列表/栈/队列/链表/二叉堆/红黑树/B树」。
    做 打印 用 「  排序            快速排序/归并排序/堆排序/计数排序/桶排序」。
    做 打印 用 「  集合运算        交集/并集/差集/子集判断」。
    做 打印 用 「  哈希表          开放寻址哈希表/链式哈希表」。
    做 打印 用 「  堆队列          最小堆/最大堆/优先队列」。
    做 打印 用 「  缓存            LRU缓存/TTL缓存/分层缓存」。
    做 打印 用 「  字符串          分割/修剪/填充/替换/大小写转换/分词」。
    做 打印 用 「  位运算          位操作/位域/位图/位计数」。
    做 打印 用 「  IO              文件读写/流操作/缓冲IO」。
    做 打印 用 「  JSON            解析/生成/查询/格式化」。
    做 打印 用 「  OOP             类/继承/多态/接口模拟」。
    做 打印 用 「」。
    做 打印 用 「■ 数学模块 (14个):」。
    做 打印 用 「  数学            阶乘/GCD/LCM/素数/组合/斐波那契/三角函数」。
    做 打印 用 「  矩阵运算        向量点积/范数/矩阵乘法/转置/行列式」。
    做 打印 用 「  线性代数        线性方程组/特征值/SVD/LU分解」。
    做 打印 用 「  稀疏运算        稀疏矩阵/压缩存储/稀疏乘法」。
    做 打印 用 「  张量            多维张量/广播/切片/变形」。
    做 打印 用 「  概率分布        正态/泊松/二项/均匀/伯努利分布」。
    做 打印 用 「  统计            均值/中位数/方差/标准差/相关系数」。
    做 打印 用 「  信号处理        FFT/离散傅里叶变换/滤波器/窗函数/卷积」。
    做 打印 用 「  浮点模拟        IEEE754位操作/浮点分类/定点运算」。
    做 打印 用 「  图算法          BFS/DFS/Dijkstra/拓扑排序/最小生成树」。
    做 打印 用 「  自动微分        前向/反向求导/计算图/梯度」。
    做 打印 用 「  梯度运算        梯度计算/雅可比/海森矩阵」。
    做 打印 用 「  梯度裁剪        范数裁剪/值裁剪/自适应裁剪」。
    做 打印 用 「  正则表达式      模式匹配/捕获组/替换/分割」。
    做 打印 用 「」。
    做 打印 用 「■ AI/机器学习模块 (38个):」。
    做 打印 用 「  神经网络        全连接层/前向传播/反向传播/Softmax/随机丢弃」。
    做 打印 用 「  高级神经网络    ResNet/DenseNet/残差连接」。
    做 打印 用 「  激活函数        ReLU/Sigmoid/Tanh/门控线性单元/GELU/Mish」。
    做 打印 用 「  归一化层        BatchNorm/层归一化/GroupNorm/RMSNorm」。
    做 打印 用 「  池化层          最大池化/平均池化/全局池化/自适应池化」。
    做 打印 用 「  循环网络        RNN/LSTM/GRU/双向RNN/序列到序列」。
    做 打印 用 「  注意力机制      多头注意力/自注意力/交叉注意力/Flash注意力」。
    做 打印 用 「  位置编码        正弦/旋转(RoPE)/相对位置/ALiBi」。
    做 打印 用 「  高级损失函数    MSE/交叉熵/Huber/Focal/对比损失」。
    做 打印 用 「  权重初始化      Xavier/He/正交/均匀/正态初始化」。
    做 打印 用 「  正则化          L1/L2/随机丢弃/权重衰减/谱归一化」。
    做 打印 用 「  高级优化器      SGD/Adam/AdaGrad/RMSProp/LAMB/AdaFactor」。
    做 打印 用 「  混合精度        FP16/BF16自动混合精度训练」。
    做 打印 用 「  量化            INT8/INT4/动态量化/量化感知训练」。
    做 打印 用 「  数据增强        翻转/旋转/裁剪/颜色扰动/Mixup/CutMix」。
    做 打印 用 「  数据管道        数据加载/预处理/批次/多线程」。
    做 打印 用 「  采样策略        TopK/TopP/温度/束搜索/核采样」。
    做 打印 用 「  评估指标        准确率/精确率/召回率/F1/AUC/BLEU」。
    做 打印 用 「  评估基准        标准基准/速度测试/精度测试」。
    做 打印 用 「  训练管线        训练循环/验证/早停/学习率调度」。
    做 打印 用 「  训练回调        日志/检查点/早停/学习率回调」。
    做 打印 用 「  检查点管理      保存/加载/版本管理/自动恢复」。
    做 打印 用 「  模型架构        GPT/BERT/LLaMA/T5/ViT/Mamba」。
    做 打印 用 「  高级模型架构    改进版模型架构/门控线性单元/RoPE集成」。
    做 打印 用 「  混合专家        MoE路由/专家网络/负载均衡/TopK门控」。
    做 打印 用 「  模型服务        推理服务/批量推理/KV缓存」。
    做 打印 用 「  模型仓库        模型注册/版本管理/部署」。
    做 打印 用 「  模型动物园      预定义模型集合/快速构建」。
    做 打印 用 「  微调            LoRA/QLoRA/适配器/Prefix调优」。
    做 打印 用 「  RLHF            人类反馈强化学习/PPO/奖励模型」。
    做 打印 用 「  强化学习        Q学习/SARSA/DQN/PPO/A3C/经验回放」。
    做 打印 用 「  持续学习        增量学习/知识蒸馏/弹性权重巩固」。
    做 打印 用 「  推理            推理引擎/逻辑推理/因果推理」。
    做 打印 用 「  推理优化        KV缓存/投机解码/批量推理/算子融合」。
    做 打印 用 「  文本生成        自回归生成/条件生成/可控生成」。
    做 打印 用 「  嵌入存储        向量存储/相似搜索/HNSW索引」。
    做 打印 用 「  检索增强        RAG/文档检索/上下文注入/重排序」。
    做 打印 用 「  进化算法        遗传算法/粒子群/差分进化/进化策略」。
    做 打印 用 「」。
    做 打印 用 「■ 系统模块 (20个):」。
    做 打印 用 「  系统调用        Linux/Windows系统调用号/POSIX错误码」。
    做 打印 用 「  系统配置        特性标志/配置管理/运行时选项」。
    做 打印 用 「  系统启动        引导序列/初始化/自检」。
    做 打印 用 「  系统韧性        故障恢复/熔断器/重试策略/降级」。
    做 打印 用 「  内存管理        堆分配/释放/内存池/垃圾回收」。
    做 打印 用 「  硬件抽象        设备管理/驱动接口/HAL层」。
    做 打印 用 「  并发            线程/协程/锁/信号量/屏障」。
    做 打印 用 「  并行训练        数据并行/模型并行/流水线并行」。
    做 打印 用 「  分布式          分布式通信/AllReduce/参数服务器」。
    做 打印 用 「  调度器          任务调度/优先级/抢占/时间片」。
    做 打印 用 「  任务调度        异步任务/定时器/工作窃取」。
    做 打印 用 「  日志调试        日志级别/断言/调试输出/性能计时」。
    做 打印 用 「  状态机          有限状态机/层次状态机/状态转换」。
    做 打印 用 「  工作流          工作流引擎/DAG调度/条件分支」。
    做 打印 用 「  终端界面          颜色输出/进度条/表格/菜单」。
    做 打印 用 「  协议栈          TCP/UDP/ICMP/ARP/DNS协议实现」。
    做 打印 用 「  网络            Socket/HTTP/WebSocket/TLS」。
    做 打印 用 「  配置解析        INI/TOML/YAML配置解析」。
    做 打印 用 「  序列化          二进制序列化/反序列化/版本兼容」。
    做 打印 用 「  压缩            LZ77/RLE/哈夫曼/DEFLATE压缩」。
    做 打印 用 「」。
    做 打印 用 「■ 文本/编码模块 (8个):」。
    做 打印 用 「  分词器          BPE/WordPiece/SentencePiece分词」。
    做 打印 用 「  编解码          Base64/Hex/URL编码解码」。
    做 打印 用 「  文本生成        文本处理/模式匹配/自然语言处理」。
    做 打印 用 「  对话管理        对话状态/意图识别/槽填充」。
    做 打印 用 「  语法解析        LL/LR语法解析/AST构建」。
    做 打印 用 「  知识图谱        实体/关系/三元组/图查询/推理」。
    做 打印 用 「  视觉处理        图像变换/特征提取/目标检测」。
    做 打印 用 「  音频处理        波形/频谱/MFCC/语音特征」。
    做 打印 用 「」。
    做 打印 用 「■ 安全模块 (4个):」。
    做 打印 用 「  安全            加密/签名/证书/密钥管理」。
    做 打印 用 「  安全沙箱        资源限制/权限控制/隔离执行」。
    做 打印 用 「  智能安全          模型安全/对抗检测/公平性/可解释性」。
    做 打印 用 「  类型系统        类型检查/泛型/接口/枚举/代数类型」。
    做 打印 用 「」。
    做 打印 用 「■ 图形/IO模块 (7个):」。
    做 打印 用 「  画布            2D绘图/像素操作/形状/文本渲染」。
    做 打印 用 「  窗口            Win32窗口创建/事件循环/消息处理」。
    做 打印 用 「  终端            ANSI终端/颜色/光标/键盘输入」。
    做 打印 用 「  BMP格式         灰度/RGB位图生成与保存」。
    做 打印 用 「  文件读写          文件读写/路径操作/目录遍历」。
    做 打印 用 「  文件监控        文件变化监控/事件通知」。
    做 打印 用 「  数据库          关系数据库/键值存储/SQL查询」。
    做 打印 用 「」。
    做 打印 用 「■ 高级AI模块 (9个):」。
    做 打印 用 「  经络网络        生物启发神经网络/脉冲/自组织」。
    做 打印 用 「  记忆系统        工作记忆/长期记忆/记忆巩固」。
    做 打印 用 「  意识            注意力控制/元认知/自我监控」。
    做 打印 用 「  元认知          学习策略/自我评估/策略选择」。
    做 打印 用 「  全脑整合        多模块协同/信息整合/决策融合」。
    做 打印 用 「  智能体          Agent框架/工具调用/规划/执行」。
    做 打印 用 「  多模态          视觉-语言/音频-文本/跨模态融合」。
    做 打印 用 「  时间序列        预测/异常检测/趋势分析/季节分解」。
    做 打印 用 「  张量核心         张量网络框架核心/稀疏张量」。
    做 打印 用 「」。
    做 打印 用 「■ AI前沿扩展模块 (20个):」。
    做 打印 用 「  扩散模型        DDPM/DDIM/CFG/噪声调度/VLB损失/EMA」。
    做 打印 用 「  状态空间        S4/Mamba选择扫描/RWKV/RetNet/门控SSM」。
    做 打印 用 「  图神经网络      GCN/GAT/GraphSAGE/MPNN/GIN/异构图」。
    做 打印 用 「  对比学习        InfoNCE/NT-Xent/CLIP/MoCo/BYOL/BarlowTwins/VICReg」。
    做 打印 用 「  DPO             直接偏好优化/IPO/KTO/ORPO/SimPO/CPO/拒绝采样」。
    做 打印 用 「  思维链          CoT/ToT/ReAct/Reflexion/STaR/MCTS/过程奖励」。
    做 打印 用 「  提示工程        模板/少样本/对话管理/工具调用/RAG/安全过滤」。
    做 打印 用 「  模型压缩        剪枝/蒸馏/SVD低秩/聚类/层融合/早退」。
    做 打印 用 「  元学习          MAML/Reptile/MetaSGD/原型网络/少样本/任务采样」。
    做 打印 用 「  自监督学习      MLM/MAE/DINO/JEPA/CPC/去噪/旋转预测」。
    做 打印 用 「  联邦学习        FedAvg/FedProx/差分隐私/安全聚合/通信压缩」。
    做 打印 用 「  神经架构搜索    DARTS/进化搜索/性能预测/Pareto/超参搜索」。
    做 打印 用 「  世界模型        潜在动力学/想象rollout/CEM规划/奖励预测」。
    做 打印 用 「  稀疏注意力      滑动窗口/BigBird/块稀疏/线性注意力/膨胀」。
    做 打印 用 「  因果推理        因果图/do-calculus/ATE/PSM/IPW/格兰杰/反事实/中介」。
    做 打印 用 「  贝叶斯学习      贝叶斯回归/变分推断/MC随机丢弃/BO/深度集成/ECE」。
    做 打印 用 「  能量模型        Hopfield/RBM/对比散度/朗之万/分数匹配」。
    做 打印 用 「  流模型          RealNVP/Glow/流匹配/ODE采样/Sinkhorn」。
    做 打印 用 「  多智能体        QMIX/VDN/CommNet/任务委派/对话编排/角色分配」。
    做 打印 用 「  缩放定律        Chinchilla/Kaplan/μP/涌现阈值/训练成本估算」。
    做 打印 用 「」。
    做 打印 用 「■ 系统基础扩展模块 (4个, 唯一无重复):」。
    做 打印 用 「  迭代器          映射/过滤/归约/zip/枚举/分块/窗口/扫描/去重」。
    做 打印 用 「  结果类型        Option/Result/错误码/错误链/安全除法/安全索引」。
    做 打印 用 「  命令行          参数解析/子命令/选项/标志/帮助生成/验证」。
    做 打印 用 「  基准测试        性能测量/统计/比较/回归检测/吞吐量/套件」。
    做 打印 用 「」。
    做 打印 用 「■ 编译器管线模块 (23个):」。
    做 打印 用 「  词法/            标记定义/字符工具/数字扫描/字符串扫描/注释/词法分析」。
    做 打印 用 「  语法/            节点定义/表达式/语句/解析器」。
    做 打印 用 「  语义/            符号表/作用域/可变性/未使用/生命期/类型推断」。
    做 打印 用 「  运行时/          内存分配/垃圾回收/列表/字典/字符串/协程/异常」。
    做 打印 用 「  宏系统           宏定义/展开/卫生宏」。
    做 打印 用 「  转换器           Forth转换器/转换验证」。
完。

帮助_架构 接受 零参：
    做 打印 用 「================================================================================」。
    做 打印 用 「                       编译器架构与x86-64指令集」。
    做 打印 用 「================================================================================」。
    做 打印 用 「」。
    做 打印 用 「■ 编译流水线:」。
    做 打印 用 「  源代码(.nova) -> 词法分析 -> 语法分析(AST) -> 语义分析 -> x86-64代码生成 -> PE链接」。
    做 打印 用 「」。
    做 打印 用 「  1. 词法分析: Unicode感知,支持中文标识符,21个关键字」。
    做 打印 用 「  2. 语法分析: 递归下降解析器,生成AST(抽象语法树)」。
    做 打印 用 「  3. 语义分析: 变量解析,闭包检测,类型推断」。
    做 打印 用 「  4. 代码生成: 直接生成x86-64机器码(无中间表示)」。
    做 打印 用 「  5. PE链接:   生成Windows PE可执行文件(含IAT/重定位)」。
    做 打印 用 「」。
    做 打印 用 「■ 目标架构:」。
    做 打印 用 「  ISA:        x86-64 (AMD64)」。
    做 打印 用 「  OS:         Windows (PE32+ 格式)」。
    做 打印 用 「  ABI:        Windows x64调用约定 (rcx,rdx,r8,r9,栈)」。
    做 打印 用 「  字长:       64位」。
    做 打印 用 「  浮点:       SSE2 (xmm0-xmm15)」。
    做 打印 用 「  对齐:       16字节栈对齐」。
    做 打印 用 「」。
    做 打印 用 「■ 使用的x86-64指令集:」。
    做 打印 用 「  数据移动:   mov, lea, push, pop, movzx, movsx」。
    做 打印 用 「  算术:       add, sub, imul, idiv, neg, inc, dec, cqo」。
    做 打印 用 「  逻辑:       and, or, xor, not, shl, shr, sar」。
    做 打印 用 「  比较:       cmp, test, sete, setne, setl, setg, setle, setge」。
    做 打印 用 「  跳转:       jmp, je, jne, jl, jg, jle, jge, call, ret」。
    做 打印 用 「  浮点SSE2:   movsd, addsd, subsd, mulsd, divsd, cvtsi2sd, cvttsd2si」。
    做 打印 用 「  浮点比较:   ucomisd, comisd」。
    做 打印 用 「  系统:       syscall(通过IAT间接调用)」。
    做 打印 用 「」。
    做 打印 用 「■ 寄存器使用:」。
    做 打印 用 「  rax         返回值/暂存计算」。
    做 打印 用 「  rcx         第1参数」。
    做 打印 用 「  rdx         第2参数/除法高位」。
    做 打印 用 「  r8          第3参数」。
    做 打印 用 「  r9          第4参数」。
    做 打印 用 「  rsp         栈指针」。
    做 打印 用 「  rbp         帧指针(未使用,用rsp寻址)」。
    做 打印 用 「  r10-r15     暂存寄存器」。
    做 打印 用 「  xmm0-xmm3   浮点参数/返回值」。
    做 打印 用 「」。
    做 打印 用 「■ 内存模型:」。
    做 打印 用 「  局部变量:   栈分配 [rsp+偏移]」。
    做 打印 用 「  全局变量:   堆分配数组,通过全局指针+索引访问」。
    做 打印 用 「  字符串:     编译期嵌入代码段尾部,运行时rip相对寻址」。
    做 打印 用 「  列表:       运行时堆分配,引用计数管理」。
    做 打印 用 「  字典:       运行时堆分配,线性探测哈希表」。
    做 打印 用 「  闭包变量:   全局槽位分配,编译期捕获」。
    做 打印 用 「」。
    做 打印 用 「■ PE文件结构:」。
    做 打印 用 「  DOS头 + PE签名 + COFF头 + Optional头」。
    做 打印 用 「  .text段: 机器码+字符串数据」。
    做 打印 用 「  .rdata段: 导入目录+IAT」。
    做 打印 用 「  导入DLL: kernel32.dll (GetStdHandle/WriteConsoleA/CreateFileA/...)」。
    做 打印 用 「            msvcrt.dll  (malloc/free/memcpy/...)」。
    做 打印 用 「」。
    做 打印 用 「■ 运行时特性:」。
    做 打印 用 「  堆分配器:   1GB VirtualAlloc堆,bump分配器,按需分页」。
    做 打印 用 「  字符串:     不可变,拼接产生新串,UTF-8编码」。
    做 打印 用 「  列表:       动态数组,自动扩容(初始容量n+8)」。
    做 打印 用 「  字典:       键值对,自适应增长(初始256,满时2x,无上限),线性搜索」。
    做 打印 用 「  异常:       setjmp/longjmp实现尝试/得」。
    做 打印 用 「  闭包:       编译期AST分析,全局槽位存储捕获变量」。
    做 打印 用 「」。
    做 打印 用 「■ 编译器容量:」。
    做 打印 用 「  函数表:     自适应增长(无硬性上限)」。
    做 打印 用 「  全局变量:   无硬性上限(堆分配)」。
    做 打印 用 「  字符串常量: 无硬性上限(动态数组)」。
    做 打印 用 「  闭包:       动态检测(编译期AST分析)」。
    做 打印 用 「  代码缓冲:   64MB(充足容量)」。
    做 打印 用 「  运行时堆:   1GB(VirtualAlloc按需分页)」。
完。

帮助_示例 接受 零参：
    做 打印 用 「================================================================================」。
    做 打印 用 「                            代码示例」。
    做 打印 用 「================================================================================」。
    做 打印 用 「」。
    做 打印 用 「■ Hello World:」。
    做 打印 用 「  打印('你好世界!')」。
    做 打印 用 「」。
    做 打印 用 「■ 变量与运算:」。
    做 打印 用 「  定义 x = 42」。
    做 打印 用 「  定义 可变 y = 0」。
    做 打印 用 「  y = x * 2 + 1」。
    做 打印 用 「  打印文本(字符串拼接('结果: ', 转字符串(y)))」。
    做 打印 用 「」。
    做 打印 用 「■ 法:」。
    做 打印 用 「  函数 阶乘(n) <」。
    做 打印 用 「      如果 (n <= 1) < 返回 1 >」。
    做 打印 用 「      返回 n * 阶乘(n - 1)」。
    做 打印 用 「  >」。
    做 打印 用 「  打印文本(字符串拼接('10! = ', 转字符串(阶乘(10))))」。
    做 打印 用 「」。
    做 打印 用 「■ 列表操作:」。
    做 打印 用 「  定义 可变 数据 = [3, 1, 4, 1, 5, 9]」。
    做 打印 用 「  追加(数据, 2)」。
    做 打印 用 「  定义 可变 i = 0」。
    做 打印 用 「  当 (i < 计数(数据)) <」。
    做 打印 用 「      打印文本(转字符串(数据[i]))」。
    做 打印 用 「      i = i + 1」。
    做 打印 用 「  >」。
    做 打印 用 「」。
    做 打印 用 「■ 结构体:」。
    做 打印 用 「  结构 向量3D < x, y, z >」。
    做 打印 用 「  函数 向量_加(a, b) <」。
    做 打印 用 「      返回 建 向量3D(a.x + b.x, a.y + b.y, a.z + b.z)」。
    做 打印 用 「  >」。
    做 打印 用 「  定义 v1 = 建 向量3D(1, 2, 3)」。
    做 打印 用 「  定义 v2 = 建 向量3D(4, 5, 6)」。
    做 打印 用 「  定义 v3 = 向量_加(v1, v2)」。
    做 打印 用 「」。
    做 打印 用 「■ 闭包:」。
    做 打印 用 「  函数 创建计数器() <」。
    做 打印 用 「      定义 可变 n = 0」。
    做 打印 用 「      返回 法() <」。
    做 打印 用 「          n = n + 1」。
    做 打印 用 「          返回 n」。
    做 打印 用 「      >」。
    做 打印 用 「  >」。
    做 打印 用 「」。
    做 打印 用 「■ 异常处理:」。
    做 打印 用 「  试 <」。
    做 打印 用 「      定义 结果 = 某函数()」。
    做 打印 用 「  > 得 错误 <」。
    做 打印 用 「      打印文本(字符串拼接('捕获异常: ', 转字符串(错误)))」。
    做 打印 用 「  >」。
    做 打印 用 「」。
    做 打印 用 「■ 模块化项目:」。
    做 打印 用 「  // 文件: 数学工具.nova」。
    做 打印 用 「  函数 平方(x) < 返回 x * x >」。
    做 打印 用 「  函数 立方(x) < 返回 x * x * x >」。
    做 打印 用 「」。
    做 打印 用 「  // 文件: 主程序.nova」。
    做 打印 用 「  导入('数学工具.nova')」。
    做 打印 用 「  打印文本(转字符串(平方(5)))    // 输出: 25」。
    做 打印 用 「」。
    做 打印 用 「■ BMP图像生成:」。
    做 打印 用 「  定义 宽 = 256」。
    做 打印 用 「  定义 高 = 256」。
    做 打印 用 「  bmp_写灰度('output.bmp', 像素数据, 宽, 高)」。
    做 打印 用 「  bmp_写RGB('color.bmp', RGB数据, 宽, 高)」。
    做 打印 用 「」。
    做 打印 用 「■ 神经网络(标准库):」。
    做 打印 用 「  定义 可变 权重 = 矩阵_随机(784,  128, 0 , 0, 0)」。
    做 打印 用 「  定义 可变 输出 = 全连接_前向(输入, 权重)」。
    做 打印 用 「  输出 = ReLU(输出)」。
    做 打印 用 「  定义 损失 = 交叉熵(输出, 标签)」。
完。

帮助_AI 接受 零参：
    做 打印 用 「================================================================================」。
    做 打印 用 「                    AI精准开发参考 (机器可读规范)」。
    做 打印 用 「================================================================================」。
    做 打印 用 「」。
    做 打印 用 「■ 编译命令:」。
    做 打印 用 「  nova <源文件.nova>            -> 生成 <源文件>」。
    做 打印 用 「  输出文件名 = 输入文件名去掉.nova后缀」。
    做 打印 用 「  退出码: 0=成功, 非0=失败」。
    做 打印 用 「」。
    做 打印 用 「■ 导入路径解析规则:」。
    做 打印 用 「  导入('相对路径.nova')            相对于当前编译的.nova文件所在目录」。
    做 打印 用 「  导入('标准库/模块.nova')         相对路径,从编译器所在目录查找」。
    做 打印 用 「  导入('../../目录/文件.nova')     支持父目录引用」。
    做 打印 用 「  导入只提取: 函数定义 + 结构体定义 + 顶层语句」。
    做 打印 用 「  导入不递归: 被导入文件内的导入语句会被跳过」。
    做 打印 用 「  用 '路径.nova'                与导入相同,另一种语法」。
    做 打印 用 「」。
    做 打印 用 「■ 语法精确规则 (AI必读):」。
    做 打印 用 「  块定界符: { } (花括号)」。
    做 打印 用 「  语句无分号: 每行一条语句,无需分号结尾」。
    做 打印 用 「  字符串: 单引号或双引号均可: '你好' 或 \"你好\"」。
    做 打印 用 「  注释: // 单行注释」。
    做 打印 用 「  变量声明: 定义 x = 值  或  定义 可变 x = 值」。
    做 打印 用 「  不可变变量: 定义 x = 10 (之后不能 x = 20)」。
    做 打印 用 「  可变变量: 定义 可变 x = 0 (之后可以 x = 20)」。
    做 打印 用 「  函数定义: 函数 名称(参数列表) { 函数体 }」。
    做 打印 用 「  箭头函数: 参数 => 表达式  (自动生成匿名函数)」。
    做 打印 用 「  条件: 如果 (条件) { 否则若 (条件) { 否则： }」。
    做 打印 用 「  永: 当 (条件) { }  或  每 变量 在 列表 { }」。
    做 打印 用 「  辨: 辨 值 { 模式1 { } 模式2 { 否则： } }」。
    做 打印 用 「」。
    做 打印 用 「■ 类型系统:」。
    做 打印 用 「  动态类型,运行时检查,值以64位表示」。
    做 打印 用 「  整数: 64位有符号 (-2^63 ~ 2^63-1)」。
    做 打印 用 「  浮点: 64位IEEE754双精度」。
    做 打印 用 「  字符串: 不可变UTF-8,堆分配」。
    做 打印 用 「  列表: 动态数组,元素为任意类型,自动扩容」。
    做 打印 用 「  字典: 字符串键→任意值,自适应增长(256→512→1024→...无上限)」。
    做 打印 用 「  结构体: 编译期定义字段,运行时作为列表存储」。
    做 打印 用 「  空: 空值(null),函数无返回值时返回空」。
    做 打印 用 「  布尔: 真=1, 假=0 (整数语义)」。
    做 打印 用 「」。
    做 打印 用 「■ 函数调用约定:」。
    做 打印 用 「  最多支持6个参数: rcx, rdx, r8, r9, [rsp+0x20], [rsp+0x28]」。
    做 打印 用 「  返回值在rax」。
    做 打印 用 「  可变参数: 不支持」。
    做 打印 用 「  递归: 支持」。
    做 打印 用 「  闭包: 支持(编译期捕获外层变量到全局槽位)」。
    做 打印 用 「」。
    做 打印 用 「■ 常见模式 (AI直接复用):」。
    做 打印 用 「  // 字符串拼接多段」。
    做 打印 用 「  定义 结果 = 字符串拼接(字符串拼接(a, b), c), c)」。
    做 打印 用 「  // 列表遍历」。
    做 打印 用 「  定义 可变 i = 0」。
    做 打印 用 「  当 (i < 计数(列表)) { 处理(列表[i])  i = i + 1 }」。
    做 打印 用 「  // 字典使用」。
    做 打印 用 「  定义 可变 表 = 字典()」。
    做 打印 用 「  字典设(表, '键', 值)」。
    做 打印 用 「  定义 v = 字典取(表, '键')」。
    做 打印 用 「  // 错误处理」。
    做 打印 用 「  试 { 危险代码() } 得 错误 { 打印文本(转字符串(错误)) }」。
    做 打印 用 「  // 文件读写」。
    做 打印 用 「  定义 内容 = 读文件('路径.txt')」。
    做 打印 用 「  如果 (内容 != 空) { 处理(内容) }」。
    做 打印 用 「  写文件('输出.txt',  数据字符串, 0)」。
    做 打印 用 「」。
    做 打印 用 「■ 已知限制 (AI必须避免):」。
    做 打印 用 「  1. 不支持字典字面量 {key:val} 语法,必须用 字典()+字典设(0, 0 , 0)」。
    做 打印 用 「  2. 不支持可变参数函数」。
    做 打印 用 「  3. 不支持多返回值(用列表包装)」。
    做 打印 用 「  4. 字符串不可变,修改必须创建新串」。
    做 打印 用 「  5. 浮点数不支持全部数学函数(用标准库整数模拟)」。
    做 打印 用 「  6. 主要支持Linux/Windows x86-64目标平台」。
    做 打印 用 「  7. 中文关键字不能用作标识符名称」。
    做 打印 用 「  8. 导入不递归(被导入文件内的导入被跳过)」。
    做 打印 用 「  9. 每个源文件最大4MB(文件读取缓冲区)」。
    做 打印 用 「」。
    做 打印 用 「■ 自举验证命令:」。
    做 打印 用 「  nova 自举入口.nova           生成 stage2 (Stage2)」。
    做 打印 用 「  ./nova 自举入口.nova     生成 stage3 (Stage3)」。
    做 打印 用 「  比较 stage2 stage3  SHA256必须一致」。
    做 打印 用 「」。
    做 打印 用 「■ 编译器版本历史:」。
    做 打印 用 「  v1.0.0  自举成功,基础语法,72个函数」。
    做 打印 用 「  v2.0.0  UTF-8打印,常量折叠,文件缓冲4MB」。
    做 打印 用 「  v3.0.0  自适应字典(无上限),64MB代码缓冲,1GB堆,213模块集成」。
    做 打印 用 「  v3.1.0  安全加固: 插入位置/间接调用/除零保护/越界保护/删除」。
完。

帮助_全部 接受 零参：
    做 帮助_概述。
    做 打印 用 「」。
    做 帮助_语法。
    做 打印 用 「」。
    做 帮助_内建。
    做 打印 用 「」。
    做 帮助_标准库。
    做 打印 用 「」。
    做 帮助_架构。
    做 打印 用 「」。
    做 帮助_示例。
    做 打印 用 「」。
    做 帮助_AI。
完。

本章 收束。
