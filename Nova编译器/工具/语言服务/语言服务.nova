《语言服务》

引用《..·..·标准库·基础》。

符号_函数 为 1。

符号_变量 为 2。

符号_常量 为 3。

符号_参数 为 4。

符号_结构 为 5。

符号_模块 为 6。

服务_符号_创建 接受 名称、类型、文件、行、列：
    返还 一组：名称、类型、文件、行、列、行、「」、一组：（空）。
完。

符号_设范围结束 接受 符号、结束行：
    符号 的 第 5 项 为 结束行。
完。

符号_设详情 接受 符号、详情：
    符号 的 第 6 项 为 详情。
完。

符号_添加子符号 接受 符号、子：
    做 追加 用 符号 的 第 7 项、子。
完。

符号_类型名 接受 类型：
    若 类型 等于 符号_函数：
        返还 「法」。
    完。
    若 类型 等于 符号_变量：
        返还 「变量」。
    完。
    若 类型 等于 符号_常量：
        返还 「常量」。
    完。
    若 类型 等于 符号_参数：
        返还 「参数」。
    完。
    若 类型 等于 符号_结构：
        返还 「型」。
    完。
    若 类型 等于 符号_模块：
        返还 「模块」。
    完。
    返还 「unknown」。
完。

服务_符号表_创建 接受 零参：
    返还 一组：一组：（空）、做 字典、做 字典。
完。

服务_符号表_追加 接受 表、符号：
    idx 为 做 计数 用 表 的 第 0 项。
    做 追加 用 表 的 第 0 项、符号。
    现有 为 做 字典取 用 表 的 第 1 项、符号 的 第 0 项。
    若 现有 等于 空：
        做 字典设 用 表 的 第 1 项、符号 的 第 0 项、一组：idx。
    否则：
        做 追加 用 现有、idx。
    完。
    文件列表 为 做 字典取 用 表 的 第 2 项、符号 的 第 2 项。
    若 文件列表 等于 空：
        做 字典设 用 表 的 第 2 项、符号 的 第 2 项、一组：idx。
    否则：
        做 追加 用 文件列表、idx。
    完。
    返还 idx。
完。

服务_符号表_查找 接受 表、名称：
    可变 索引列表 为 做 字典取 用 表 的 第 1 项、名称。
    若 索引列表 等于 空：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 索引列表：
        做 追加 用 结果、表 的 第 0 项 的 第 索引列表 的 第 i 项 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

符号表_文件符号 接受 表、文件路径：
    索引列表 为 做 字典取 用 表 的 第 2 项、文件路径。
    若 索引列表 等于 空：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 索引列表：
        做 追加 用 结果、表 的 第 0 项 的 第 索引列表 的 第 i 项 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

索引_源码 接受 源码、文件路径、符号表：
    可变 行列表 为 做 lsp_分割行 用 源码。
    可变 n 为 做 计数 用 行列表。
    可变 i 为 0。
    只要 i 小于 n：
        行 为 做 lsp_去空白 用 行列表 的 第 i 项。
        行号 为 i 加 1。
        若 做 lsp_开头是 用 行、「函数 」 或者 做 lsp_开头是 用 行、「法 」：
            签名 为 做 lsp_解析函数名 用 行。
            若 签名 不等于 空：
                可变 符号 为 做 服务_符号_创建 用 签名 的 第 0 项、符号_函数、文件路径、行号、1。
                做 符号_设详情 用 符号、行。
                可变 p 为 0。
                只要 p 小于 做 计数 用 签名 的 第 1 项：
                    参数符号 为 做 服务_符号_创建 用 签名 的 第 1 项 的 第 p 项、符号_参数、文件路径、行号、0。
                    做 符号_添加子符号 用 符号、参数符号。
                    p 为 p 加 1。
                完。
                函数结束行 为 做 语言服务_查找函数结束 用 行列表、i、n。
                若 函数结束行 大于 i：
                    做 符号_设范围结束 用 符号、函数结束行 加 1。
                完。
                做 服务_符号表_追加 用 符号表、符号。
            完。
        否则若 (做 lsp_开头是 用 行、「定义 」 或者 做 lsp_开头是 用 行、「令 」) 并且 做 lsp_包含 用 行、「=」：
            可变 名称 为 做 lsp_解析定义名 用 行。
            若 做 计数 用 名称 大于 0：
                可变 类型 为 符号_常量。
                若 做 lsp_包含 用 行、「可变」：
                    类型 为 符号_变量。
                完。
                符号 为 做 服务_符号_创建 用 名称、类型、文件路径、行号、1。
                做 符号_设详情 用 符号、行。
                做 服务_符号表_追加 用 符号表、符号。
            完。
        完。
        i 为 i 加 1。
    完。
完。

lsp_补全 接受 符号表、前缀、_当前文件：
    可变 建议 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 符号表 的 第 0 项：
        可变 s 为 符号表 的 第 0 项 的 第 i 项。
        若 做 lsp_开头是 用 s 的 第 0 项、前缀：
            做 追加 用 建议、一组：s 的 第 0 项、做 符号_类型名 用 s 的 第 1 项、s 的 第 6 项。
        完。
        i 为 i 加 1。
    完。
    可变 j 为 1。
    只要 j 小于 做 计数 用 建议：
        可变 当前 为 建议 的 第 j 项。
        可变 k 为 j 减 1。
        只要 k 大于或等于 0 并且 做 计数 用 建议 的 第 k 项 的 第 0 项 大于 做 计数 用 当前 的 第 0 项：
            建议 的 第 k 加 1 项 为 建议 的 第 k 项。
            k 为 k 减 1。
        完。
        建议 的 第 k 加 1 项 为 当前。
        j 为 j 加 1。
    完。
    返还 建议。
完。

lsp_跳转定义 接受 符号表、名称：
    可变 结果 为 做 服务_符号表_查找 用 符号表、名称。
    若 做 计数 用 结果 等于 0：
        返还 空。
    完。
    返还 一组：结果 的 第 0 项 的 第 2 项、结果 的 第 0 项 的 第 3 项、结果 的 第 0 项 的 第 4 项。
完。

lsp_悬停 接受 符号表、名称：
    可变 结果 为 做 服务_符号表_查找 用 符号表、名称。
    若 做 计数 用 结果 等于 0：
        返还 「」。
    完。
    可变 s 为 结果 的 第 0 项。
    可变 提示 为 做 字符串拼接 用 「**」、做 符号_类型名 用 s 的 第 1 项。
    提示 为 做 字符串拼接 用 提示、「** `」。
    提示 为 做 字符串拼接 用 提示、s 的 第 0 项。
    提示 为 做 字符串拼接 用 提示、「`\n\n」。
    若 做 计数 用 s 的 第 6 项 大于 0：
        提示 为 做 字符串拼接 用 提示、s 的 第 6 项。
    完。
    若 s 的 第 1 项 等于 符号_函数 并且 做 计数 用 s 的 第 7 项 大于 0：
        提示 为 做 字符串拼接 用 提示、「\n\n参数: 」。
        可变 i 为 0。
        只要 i 小于 做 计数 用 s 的 第 7 项：
            若 i 大于 0：
                提示 为 做 字符串拼接 用 提示、「, 」。
            完。
            提示 为 做 字符串拼接 用 提示、s 的 第 7 项 的 第 i 项 的 第 0 项。
            i 为 i 加 1。
        完。
    完。
    返还 提示。
完。

lsp_大纲 接受 符号表、文件路径：
    文件符号 为 做 符号表_文件符号 用 符号表、文件路径。
    可变 大纲 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文件符号：
        s 为 文件符号 的 第 i 项。
        做 追加 用 大纲、一组：s 的 第 0 项、做 符号_类型名 用 s 的 第 1 项、s 的 第 3 项、s 的 第 5 项。
        i 为 i 加 1。
    完。
    返还 大纲。
完。

lsp_查找引用 接受 源码列表、名称：
    可变 引用列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 源码列表：
        文件 为 源码列表 的 第 i 项 的 第 0 项。
        行列表 为 源码列表 的 第 i 项 的 第 1 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 行列表：
            若 做 lsp_包含 用 行列表 的 第 j 项、名称：
                做 追加 用 引用列表、一组：文件、j 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 引用列表。
完。

lsp_解析函数名 接受 行：
    可变 n 为 做 计数 用 行。
    可变 i 为 3。
    只要 i 小于 n 并且 做 字符串等于 用 做 子串 用 行、i、1、「 」：
        i 为 i 加 1。
    完。
    可变 名称 为 「」。
    只要 i 小于 n 并且 做 字符串等于 用 做 子串 用 行、i、1、「(」 等于 假：
        名称 为 做 字符串拼接 用 名称、做 子串 用 行、i、1。
        i 为 i 加 1。
    完。
    若 i 大于或等于 n：
        返还 空。
    完。
    可变 参数 为 一组：（空）。
    i 为 i 加 1。
    可变 当前 为 「」。
    只要 i 小于 n 并且 _参完 等于 假：
        可变 c 为 做 子串 用 行、i、1。
        若 c 等于 「)」：
            _参完 为 真。
        否则：
            若 做 字符串等于 用 c、「,」：
                p 为 做 lsp_去空白 用 当前。
                若 做 计数 用 p 大于 0：
                    做 追加 用 参数、p。
                完。
                当前 为 「」。
            否则：
                当前 为 做 字符串拼接 用 当前、c。
            完。
            i 为 i 加 1。
        完。
    完。
    最后 为 做 lsp_去空白 用 当前。
    若 做 计数 用 最后 大于 0：
        做 追加 用 参数、最后。
    完。
    返还 一组：名称、参数。
完。

语言服务_查找函数结束 接受 行列表、起始、n：
    可变 深度 为 0。
    可变 j 为 起始。
    只要 j 小于 n：
        可变 k 为 0。
        只要 k 小于 做 计数 用 行列表 的 第 j 项：
            若 做 字符串等于 用 做 子串 用 行列表 的 第 j 项、k、1、「{」：
                深度 为 深度 加 1。
            完。
            若 做 字符串等于 用 做 子串 用 行列表 的 第 j 项、k、1、「}」：
                深度 为 深度 减 1。
            完。
            k 为 k 加 1。
        完。
        若 深度 小于或等于 0 并且 j 大于 起始：
            返还 j。
        完。
        j 为 j 加 1。
    完。
    返还 起始。
完。

lsp_解析定义名 接受 行：
    可变 i 为 3。
    只要 i 小于 做 计数 用 行 并且 做 字符串等于 用 做 子串 用 行、i、1、「 」：
        i 为 i 加 1。
    完。
    若 i 加 3 小于 做 计数 用 行 并且 做 字符串等于 用 做 子串 用 行、i、3、「可变」：
        i 为 i 加 4。
    完。
    可变 名称 为 「」。
    只要 i 小于 做 计数 用 行 并且 _解析中：
        可变 c 为 做 子串 用 行、i、1。
        若 做 字符串等于 用 c、「 」 或者 做 字符串等于 用 c、「=」：
            _解析中 为 假。
        否则：
            名称 为 做 字符串拼接 用 名称、c。
            i 为 i 加 1。
        完。
    完。
    返还 名称。
完。

lsp_分割行 接受 文本：
    可变 n 为 做 计数 用 文本。
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 文本、i、1。
        若 做 字符串等于 用 c、「\n」：
            做 追加 用 结果、当前。
            当前 为 「」。
        否则若 做 字符串等于 用 c、「\r」 等于 假：
            当前 为 做 字符串拼接 用 当前、c。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前 大于 0：
        做 追加 用 结果、当前。
    完。
    返还 结果。
完。

lsp_去空白 接受 s：
    可变 n 为 做 计数 用 s。
    可变 开始 为 0。
    只要 开始 小于 n 并且 (做 字符串等于 用 做 子串 用 s、开始、1、「 」 或者 做 字符串等于 用 做 子串 用 s、开始、1、「\t」)：
        开始 为 开始 加 1。
    完。
    可变 结束 为 n。
    只要 结束 大于 开始 并且 (做 字符串等于 用 做 子串 用 s、结束 减 1、1、「 」 或者 做 字符串等于 用 做 子串 用 s、结束 减 1、1、「\t」)：
        结束 为 结束 减 1。
    完。
    若 开始 大于或等于 结束：
        返还 「」。
    完。
    返还 做 子串 用 s、开始、结束 减 开始。
完。

lsp_开头是 接受 s、前缀：
    若 做 计数 用 s 小于 做 计数 用 前缀：
        返还 假。
    完。
    返还 做 字符串等于 用 做 子串 用 s、0、做 计数 用 前缀、前缀。
完。

lsp_包含 接受 文本、子串文本：
    n 为 做 计数 用 文本。
    m 为 做 计数 用 子串文本。
    若 m 大于 n：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于或等于 n 减 m：
        若 做 字符串等于 用 做 子串 用 文本、i、m、子串文本：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

本章 收束。
