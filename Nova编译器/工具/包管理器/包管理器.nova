《包管理器》

引用《..·..·标准库·基础》。

版本_创建 接受 主、次、补丁：
    返还 一组：主、次、补丁、「」。
完。

版本_解析 接受 文本：
    n 为 做 计数 用 文本。
    可变 部分 为 一组：0。
    可变 当前 为 0。
    可变 部分号 为 0。
    可变 i 为 0。
    可变 _解析中 为 真。
    只要 i 小于 n 并且 部分号 小于 3 并且 _解析中：
        c 为 做 编码 用 做 子串 用 文本、i、1。
        若 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
            当前 为 当前 乘 10 加 (c 减 48)。
        否则若 做 字符串等于 用 做 子串 用 文本、i、1、「.」：
            部分 的 第 部分号 项 为 当前。
            当前 为 0。
            部分号 为 部分号 加 1。
        否则：
            _解析中 为 假。
        完。
        i 为 i 加 1。
    完。
    若 部分号 小于 3：
        部分 的 第 部分号 项 为 当前。
    完。
    返还 一组：部分 的 第 0 项、部分 的 第 1 项、部分 的 第 2 项、「」。
完。

版本_格式化 接受 v：
    可变 r 为 做 包_数到文 用 v 的 第 0 项。
    r 为 做 字符串拼接 用 r、「.」。
    r 为 做 字符串拼接 用 r、做 包_数到文 用 v 的 第 1 项。
    r 为 做 字符串拼接 用 r、「.」。
    r 为 做 字符串拼接 用 r、做 包_数到文 用 v 的 第 2 项。
    若 做 计数 用 v 的 第 3 项 大于 0：
        r 为 做 字符串拼接 用 r、「-」。
        r 为 做 字符串拼接 用 r、v 的 第 3 项。
    完。
    返还 r。
完。

版本_比较 接受 a、b：
    若 a 的 第 0 项 不等于 b 的 第 0 项：
        返还 a 的 第 0 项 减 b 的 第 0 项。
    完。
    若 a 的 第 1 项 不等于 b 的 第 1 项：
        返还 a 的 第 1 项 减 b 的 第 1 项。
    完。
    返还 a 的 第 2 项 减 b 的 第 2 项。
完。

版本_兼容 接受 版本、约束类型、约束版本：
    若 做 字符串等于 用 约束类型、「=」：
        返还 做 版本_比较 用 版本、约束版本 等于 0。
    完。
    若 做 字符串等于 用 约束类型、「>=」：
        返还 做 版本_比较 用 版本、约束版本 大于或等于 0。
    完。
    若 做 字符串等于 用 约束类型、「<」：
        返还 做 版本_比较 用 版本、约束版本 小于 0。
    完。
    若 做 字符串等于 用 约束类型、「^」：
        返还 版本 的 第 0 项 等于 约束版本 的 第 0 项 并且 做 版本_比较 用 版本、约束版本 大于或等于 0。
    完。
    若 做 字符串等于 用 约束类型、「~」：
        返还 版本 的 第 0 项 等于 约束版本 的 第 0 项 并且 版本 的 第 1 项 等于 约束版本 的 第 1 项 并且 做 版本_比较 用 版本、约束版本 大于或等于 0。
    完。
    返还 假。
完。

包_创建 接受 名称、版本：
    返还 一组：名称、版本、一组：（空）、「」、「」、「」、「」。
完。

包_添加依赖 接受 包、依赖名、约束类型、约束版本：
    做 追加 用 包 的 第 2 项、一组：依赖名、约束类型、约束版本。
完。

包_设描述 接受 包、描述：
    包 的 第 3 项 为 描述。
完。

包_设作者 接受 包、作者：
    包 的 第 4 项 为 作者。
完。

包_设许可证 接受 包、许可证：
    包 的 第 5 项 为 许可证。
完。

包_设入口 接受 包、入口：
    包 的 第 6 项 为 入口。
完。

注册表_创建 接受 零参：
    返还 一组：做 字典、0。
完。

注册表_发布 接受 注册表、包：
    名称 为 包 的 第 0 项。
    现有 为 做 字典取 用 注册表 的 第 0 项、名称。
    若 现有 等于 空：
        做 字典设 用 注册表 的 第 0 项、名称、一组：包。
    否则：
        可变 i 为 0。
        只要 i 小于 做 计数 用 现有：
            若 做 版本_比较 用 现有 的 第 i 项 的 第 1 项、包 的 第 1 项 等于 0：
                返还 一组：「error」、「版本已存在」。
            完。
            i 为 i 加 1。
        完。
        做 追加 用 现有、包。
    完。
    注册表 的 第 1 项 为 注册表 的 第 1 项 加 1。
    返还 一组：「success」、包 的 第 0 项。
完。

注册表_查找 接受 注册表、名称、约束类型、约束版本：
    版本列表 为 做 字典取 用 注册表 的 第 0 项、名称。
    若 版本列表 等于 空：
        返还 空。
    完。
    可变 最佳 为 空。
    可变 i 为 0。
    只要 i 小于 做 计数 用 版本列表：
        若 做 版本_兼容 用 版本列表 的 第 i 项 的 第 1 项、约束类型、约束版本：
            若 最佳 等于 空 或者 做 版本_比较 用 版本列表 的 第 i 项 的 第 1 项、最佳 的 第 1 项 大于 0：
                最佳 为 版本列表 的 第 i 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

依赖_解析 接受 注册表、根包：
    可变 已解析 为 一组：（空）。
    可变 待处理 为 一组：根包。
    可变 已访问 为 做 字典。
    可变 冲突列表 为 一组：（空）。
    只要 做 计数 用 待处理 大于 0：
        当前 为 待处理 的 第 0 项。
        可变 新待处理 为 一组：（空）。
        可变 i 为 1。
        只要 i 小于 做 计数 用 待处理：
            做 追加 用 新待处理、待处理 的 第 i 项。
            i 为 i 加 1。
        完。
        待处理 为 新待处理。
        名称 为 当前 的 第 0 项。
        若 做 字典取 用 已访问、名称 不等于 空：
            续。
        完。
        做 字典设 用 已访问、名称、当前 的 第 1 项。
        做 追加 用 已解析、当前。
        i 为 0。
        只要 i 小于 做 计数 用 当前 的 第 2 项：
            依赖 为 当前 的 第 2 项 的 第 i 项。
            解析包 为 做 注册表_查找 用 注册表、依赖 的 第 0 项、依赖 的 第 1 项、依赖 的 第 2 项。
            若 解析包 等于 空：
                做 追加 用 冲突列表、一组：「not found」、依赖 的 第 0 项、依赖 的 第 1 项、做 版本_格式化 用 依赖 的 第 2 项。
            否则：
                已解析版本 为 做 字典取 用 已访问、依赖 的 第 0 项。
                若 已解析版本 不等于 空 并且 做 版本_比较 用 已解析版本、解析包 的 第 1 项 不等于 0：
                    做 追加 用 冲突列表、一组：「版本冲突」、依赖 的 第 0 项、做 版本_格式化 用 已解析版本、做 版本_格式化 用 解析包 的 第 1 项。
                否则：
                    做 追加 用 待处理、解析包。
                完。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 一组：已解析、冲突列表。
完。

锁文件_创建 接受 已解析列表：
    可变 条目 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已解析列表：
        包 为 已解析列表 的 第 i 项。
        做 追加 用 条目、一组：包 的 第 0 项、做 版本_格式化 用 包 的 第 1 项、0。
        i 为 i 加 1。
    完。
    返还 条目。
完。

锁文件_格式化 接受 锁文件：
    可变 结果 为 「# nova.lock\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 锁文件：
        结果 为 做 字符串拼接 用 结果、锁文件 的 第 i 项 的 第 0 项。
        结果 为 做 字符串拼接 用 结果、「 = 」。
        结果 为 做 字符串拼接 用 结果、锁文件 的 第 i 项 的 第 1 项。
        结果 为 做 字符串拼接 用 结果、「\n」。
        i 为 i 加 1。
    完。
    返还 结果。
完。

包_数到文 接受 n：
    若 n 等于 0：
        返还 「0」。
    完。
    可变 v 为 n。
    可变 r 为 「」。
    只要 v 大于 0：
        r 为 做 字符串拼接 用 做 子串 用 「0123456789」、v 取余于 10、1、r。
        v 为 v 除以 10。
    完。
    返还 r。
完。

依赖_检测冲突 接受 依赖列表：
    可变 冲突 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 依赖列表：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 依赖列表：
            若 依赖列表 的 第 i 项 的 第 0 项 等于 依赖列表 的 第 j 项 的 第 0 项：
                va 为 做 版本_解析 用 依赖列表 的 第 i 项 的 第 1 项。
                vb 为 做 版本_解析 用 依赖列表 的 第 j 项 的 第 1 项。
                若 va 的 第 0 项 不等于 vb 的 第 0 项：
                    做 追加 用 冲突、一组：依赖列表 的 第 i 项 的 第 0 项、依赖列表 的 第 i 项 的 第 1 项、依赖列表 的 第 j 项 的 第 1 项。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 冲突。
完。

可变 _包缓存_名 为 一组：（空）。

可变 _包缓存_版本 为 一组：（空）。

可变 _包缓存_路径 为 一组：（空）。

包缓存_添加 接受 名、版本、路径：
    做 追加 用 _包缓存_名、名。
    做 追加 用 _包缓存_版本、版本。
    做 追加 用 _包缓存_路径、路径。
完。

包缓存_查找 接受 名、版本：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _包缓存_名：
        若 _包缓存_名 的 第 i 项 等于 名 并且 _包缓存_版本 的 第 i 项 等于 版本：
            返还 _包缓存_路径 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

包缓存_大小 接受 零参：
    返还 做 计数 用 _包缓存_名。
完。

包缓存_清空 接受 零参：
    _包缓存_名 为 一组：（空）。
    _包缓存_版本 为 一组：（空）。
    _包缓存_路径 为 一组：（空）。
完。

本章 收束。
