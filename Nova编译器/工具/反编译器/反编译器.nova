《反编译器》

引用《..·..·标准库·基础》。

指令_数据 为 0。

指令_算术 为 1。

反编译_指令_跳转 为 2。

反编译_指令_调用 为 3。

反编译_指令_返回 为 4。

指令_加载 为 5。

指令_存储 为 6。

指令_比较 为 7。

指令_系统 为 8。

反汇编指令_创建 接受 地址、字节、助记符、操作数：
    返还 一组：地址、字节、助记符、操作数、指令_数据、「」。
完。

反汇编指令_设类型 接受 指令、类型：
    指令 的 第 4 项 为 类型。
完。

反汇编指令_设注释 接受 指令、注释：
    指令 的 第 5 项 为 注释。
完。

反汇编指令_格式化 接受 指令：
    可变 r 为 做 反编译_十六进制 用 指令 的 第 0 项、8。
    r 为 做 字符串拼接 用 r、「: 」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 指令 的 第 1 项 并且 i 小于 8：
        r 为 做 字符串拼接 用 r、做 反编译_十六进制 用 指令 的 第 1 项 的 第 i 项、2。
        r 为 做 字符串拼接 用 r、「 」。
        i 为 i 加 1。
    完。
    只要 做 计数 用 r 小于 36：
        r 为 做 字符串拼接 用 r、「 」。
    完。
    r 为 做 字符串拼接 用 r、指令 的 第 2 项。
    若 做 计数 用 指令 的 第 3 项 大于 0：
        r 为 做 字符串拼接 用 r、「 」。
        i 为 0。
        只要 i 小于 做 计数 用 指令 的 第 3 项：
            若 i 大于 0：
                r 为 做 字符串拼接 用 r、「, 」。
            完。
            r 为 做 字符串拼接 用 r、指令 的 第 3 项 的 第 i 项。
            i 为 i 加 1。
        完。
    完。
    若 做 计数 用 指令 的 第 5 项 大于 0：
        r 为 做 字符串拼接 用 r、「 ; 」。
        r 为 做 字符串拼接 用 r、指令 的 第 5 项。
    完。
    返还 r。
完。

x86_解码 接受 字节列表、起始、基址：
    可变 指令列表 为 一组：（空）。
    可变 偏移 为 起始。
    n 为 做 计数 用 字节列表。
    只要 偏移 小于 n：
        可变 地址 为 基址 加 偏移 减 起始。
        可变 b 为 字节列表 的 第 偏移 项。
        可变 指令 为 空。
        可变 rex 为 0。
        若 b 大于或等于 64 并且 b 小于或等于 79：
            rex 为 b。
            偏移 为 偏移 加 1。
            若 偏移 大于或等于 n：
                偏移 为 n。
            完。
            b 为 字节列表 的 第 偏移 项。
        完。
        若 b 等于 144：
            指令 为 做 反汇编指令_创建 用 地址、一组：144、「nop」、一组：（空）。
            偏移 为 偏移 加 1。
        否则若 b 等于 195：
            指令 为 做 反汇编指令_创建 用 地址、一组：195、「ret」、一组：（空）。
            做 反汇编指令_设类型 用 指令、反编译_指令_返回。
            偏移 为 偏移 加 1。
        否则若 b 大于或等于 80 并且 b 小于或等于 87：
            可变 寄存器 为 做 x86_寄存器名 用 b 减 80、rex。
            指令 为 做 反汇编指令_创建 用 地址、一组：b、「push」、一组：寄存器。
            做 反汇编指令_设类型 用 指令、指令_存储。
            偏移 为 偏移 加 1。
        否则若 b 大于或等于 88 并且 b 小于或等于 字符_下划线：
            可变 寄存器 为 做 x86_寄存器名 用 b 减 88、rex。
            指令 为 做 反汇编指令_创建 用 地址、一组：b、「pop」、一组：寄存器。
            做 反汇编指令_设类型 用 指令、指令_加载。
            偏移 为 偏移 加 1。
        否则若 b 大于或等于 184 并且 b 小于或等于 191：
            寄存器 为 做 x86_寄存器名 用 b 减 184、rex。
            可变 立即数 为 0。
            可变 字节数 为 4。
            若 rex 大于或等于 72：
                字节数 为 8。
            完。
            若 偏移 加 字节数 小于 n：
                可变 j 为 0。
                可变 倍数 为 1。
                只要 j 小于 字节数：
                    立即数 为 立即数 加 字节列表 的 第 偏移 加 1 加 j 项 乘 倍数。
                    倍数 为 倍数 乘 256。
                    j 为 j 加 1。
                完。
            完。
            可变 bytes 为 一组：b。
            可变 k 为 0。
            只要 k 小于 字节数：
                做 追加 用 bytes、字节列表 的 第 偏移 加 1 加 k 项。
                k 为 k 加 1。
            完。
            指令 为 做 反汇编指令_创建 用 地址、bytes、「mov」、一组：寄存器、做 反编译_十六进制前缀 用 立即数。
            做 反汇编指令_设类型 用 指令、指令_数据。
            偏移 为 偏移 加 1 加 字节数。
        否则若 b 等于 232：
            若 偏移 加 4 小于 n：
                可变 rel 为 字节列表 的 第 偏移 加 1 项 加 字节列表 的 第 偏移 加 2 项 乘 256 加 字节列表 的 第 偏移 加 3 项 乘 65536 加 字节列表 的 第 偏移 加 4 项 乘 16777216。
                若 rel 大于或等于 2147483648：
                    rel 为 rel 减 4294967296。
                完。
                可变 目标 为 地址 加 5 加 rel。
                指令 为 做 反汇编指令_创建 用 地址、一组：b、字节列表 的 第 偏移 加 1 项、字节列表 的 第 偏移 加 2 项、字节列表 的 第 偏移 加 3 项、字节列表 的 第 偏移 加 4 项、「call」、一组：做 反编译_十六进制前缀 用 目标。
                做 反汇编指令_设类型 用 指令、反编译_指令_调用。
            完。
            偏移 为 偏移 加 5。
        否则若 b 等于 233：
            若 偏移 加 4 小于 n：
                可变 rel2 为 字节列表 的 第 偏移 加 1 项 加 字节列表 的 第 偏移 加 2 项 乘 256 加 字节列表 的 第 偏移 加 3 项 乘 65536 加 字节列表 的 第 偏移 加 4 项 乘 16777216。
                若 rel2 大于或等于 2147483648：
                    rel2 为 rel2 减 4294967296。
                完。
                可变 目标2 为 地址 加 5 加 rel2。
                指令 为 做 反汇编指令_创建 用 地址、一组：b、「jmp」、一组：做 反编译_十六进制前缀 用 目标2。
                做 反汇编指令_设类型 用 指令、反编译_指令_跳转。
            完。
            偏移 为 偏移 加 5。
        否则若 b 等于 235：
            若 偏移 加 1 小于 n：
                可变 rel3 为 字节列表 的 第 偏移 加 1 项。
                若 rel3 大于或等于 128：
                    rel3 为 rel3 减 256。
                完。
                目标3 为 地址 加 2 加 rel3。
                指令 为 做 反汇编指令_创建 用 地址、一组：b、字节列表 的 第 偏移 加 1 项、「jmp short」、一组：做 反编译_十六进制前缀 用 目标3。
                做 反汇编指令_设类型 用 指令、反编译_指令_跳转。
            完。
            偏移 为 偏移 加 2。
        否则若 b 等于 204：
            指令 为 做 反汇编指令_创建 用 地址、一组：204、「int3」、一组：（空）。
            做 反汇编指令_设类型 用 指令、指令_系统。
            偏移 为 偏移 加 1。
        否则若 b 等于 15 并且 偏移 加 1 小于 n 并且 字节列表 的 第 偏移 加 1 项 等于 5：
            指令 为 做 反汇编指令_创建 用 地址、一组：15、5、「syscall」、一组：（空）。
            做 反汇编指令_设类型 用 指令、指令_系统。
            偏移 为 偏移 加 2。
        否则：
            指令 为 做 反汇编指令_创建 用 地址、一组：b、「db」、一组：做 反编译_十六进制前缀 用 b。
            偏移 为 偏移 加 1。
        完。
        若 指令 不等于 空：
            做 追加 用 指令列表、指令。
        完。
    完。
    返还 指令列表。
完。

控制流_构建 接受 指令列表：
    可变 领导者 为 做 字典。
    若 做 计数 用 指令列表 大于 0：
        做 字典设 用 领导者、指令列表 的 第 0 项 的 第 0 项、真。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 指令列表：
        指令 为 指令列表 的 第 i 项。
        若 指令 的 第 4 项 等于 反编译_指令_跳转 或者 指令 的 第 4 项 等于 反编译_指令_调用：
            若 i 加 1 小于 做 计数 用 指令列表：
                做 字典设 用 领导者、指令列表 的 第 i 加 1 项 的 第 0 项、真。
            完。
        完。
        若 指令 的 第 4 项 等于 反编译_指令_返回：
            若 i 加 1 小于 做 计数 用 指令列表：
                做 字典设 用 领导者、指令列表 的 第 i 加 1 项 的 第 0 项、真。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 块列表 为 一组：（空）。
    可变 当前块指令 为 一组：（空）。
    可变 当前开始 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 指令列表：
        地址 为 指令列表 的 第 i 项 的 第 0 项。
        若 做 计数 用 当前块指令 大于 0 并且 做 字典取 用 领导者、地址 不等于 空：
            做 追加 用 块列表、一组：当前开始、指令列表 的 第 i 减 1 项 的 第 0 项、当前块指令、一组：（空）、一组：（空）。
            当前块指令 为 一组：（空）。
            当前开始 为 地址。
        完。
        若 做 计数 用 当前块指令 等于 0：
            当前开始 为 地址。
        完。
        做 追加 用 当前块指令、指令列表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前块指令 大于 0：
        做 追加 用 块列表、一组：当前开始、指令列表 的 第 做 计数 用 指令列表 减 1 项 的 第 0 项、当前块指令、一组：（空）、一组：（空）。
    完。
    返还 块列表。
完。

x86_寄存器名 接受 编号、rex：
    名64 为 一组：「rax」、「rcx」、「rdx」、「rbx」、「rsp」、「rbp」、「rsi」、「rdi」。
    名32 为 一组：「eax」、「ecx」、「edx」、「ebx」、「esp」、「ebp」、「esi」、「edi」。
    若 编号 小于 0 或者 编号 大于 7：
        返还 「?」。
    完。
    若 rex 大于或等于 72：
        返还 名64 的 第 编号 项。
    完。
    返还 名32 的 第 编号 项。
完。

PE_解析头 接受 字节列表：
    若 做 计数 用 字节列表 小于 64：
        返还 空。
    完。
    若 字节列表 的 第 0 项 不等于 77 或者 字节列表 的 第 1 项 不等于 90：
        返还 空。
    完。
    pe偏移 为 字节列表 的 第 60 项 加 字节列表 的 第 61 项 乘 256。
    若 pe偏移 加 24 大于 做 计数 用 字节列表：
        返还 空。
    完。
    若 字节列表 的 第 pe偏移 项 不等于 80 或者 字节列表 的 第 pe偏移 加 1 项 不等于 69：
        返还 空。
    完。
    节数 为 字节列表 的 第 pe偏移 加 6 项 加 字节列表 的 第 pe偏移 加 7 项 乘 256。
    可选头大小 为 字节列表 的 第 pe偏移 加 20 项 加 字节列表 的 第 pe偏移 加 21 项 乘 256。
    opt偏移 为 pe偏移 加 24。
    可变 魔数 为 0。
    若 opt偏移 加 2 小于或等于 做 计数 用 字节列表：
        魔数 为 字节列表 的 第 opt偏移 项 加 字节列表 的 第 opt偏移 加 1 项 乘 256。
    完。
    可变 入口点 为 0。
    若 opt偏移 加 20 小于或等于 做 计数 用 字节列表：
        入口点 为 字节列表 的 第 opt偏移 加 16 项 加 字节列表 的 第 opt偏移 加 17 项 乘 256 加 字节列表 的 第 opt偏移 加 18 项 乘 65536 加 字节列表 的 第 opt偏移 加 19 项 乘 16777216。
    完。
    返还 一组：pe偏移、节数、可选头大小、魔数、入口点。
完。

反编译_十六进制 接受 n、位数：
    表 为 「0123456789abcdef」。
    可变 v 为 n。
    若 v 小于 0：
        v 为 0 减 v。
    完。
    可变 r 为 「」。
    可变 i 为 0。
    只要 i 小于 位数：
        r 为 做 字符串拼接 用 做 子串 用 表、v 取余于 16、1、r。
        v 为 v 除以 16。
        i 为 i 加 1。
    完。
    返还 r。
完。

反编译_十六进制前缀 接受 n：
    返还 做 字符串拼接 用 「0x」、做 反编译_十六进制 用 n、8。
完。

本章 收束。
