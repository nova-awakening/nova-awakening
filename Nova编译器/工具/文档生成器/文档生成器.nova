《文档生成器》

引用《..·..·标准库·基础》。

文档_模块 为 1。

文档_函数 为 2。

文档_常量 为 3。

文档_结构 为 4。

文档_示例 为 5。

文档条目_创建 接受 类型、名称、描述：
    返还 一组：类型、名称、描述、一组：（空）、「」、「」、「」、0。
完。

文档条目_添加参数 接受 条目、参数名、参数描述：
    做 追加 用 条目 的 第 3 项、一组：参数名、参数描述。
完。

文档条目_设返回 接受 条目、描述：
    条目 的 第 4 项 为 描述。
完。

文档条目_设示例 接受 条目、代码：
    条目 的 第 5 项 为 代码。
完。

文档条目_设模块 接受 条目、模块名：
    条目 的 第 6 项 为 模块名。
完。

文档条目_设行号 接受 条目、行号：
    条目 的 第 7 项 为 行号。
完。

文档生成器_创建 接受 零参：
    返还 一组：一组：（空）、做 字典、做 字典。
完。

文档_解析源码 接受 源码、模块名：
    行列表 为 做 文档_分割行 用 源码。
    n 为 做 计数 用 行列表。
    可变 条目列表 为 一组：（空）。
    可变 i 为 0。
    可变 当前注释 为 一组：（空）。
    只要 i 小于 n：
        行 为 做 文档_去空白 用 行列表 的 第 i 项。
        若 做 文档_开头是 用 行、「//」：
            注释文本 为 做 文档_去空白 用 做 子串 用 行、2、做 计数 用 行 减 2。
            做 追加 用 当前注释、注释文本。
            i 为 i 加 1。
            续。
        完。
        若 做 文档_开头是 用 行、「函数 」 或者 做 文档_开头是 用 行、「法 」：
            签名 为 做 文档_解析函数签名 用 行。
            若 签名 不等于 空：
                条目 为 做 文档条目_创建 用 文档_函数、签名 的 第 0 项、「」。
                做 文档条目_设模块 用 条目、模块名。
                做 文档条目_设行号 用 条目、i 加 1。
                做 文档_处理注释 用 条目、当前注释。
                可变 p 为 0。
                只要 p 小于 做 计数 用 签名 的 第 1 项：
                    可变 已有描述 为 假。
                    可变 q 为 0。
                    只要 q 小于 做 计数 用 条目 的 第 3 项：
                        若 做 字符串等于 用 条目 的 第 3 项 的 第 q 项 的 第 0 项、签名 的 第 1 项 的 第 p 项：
                            已有描述 为 真。
                        完。
                        q 为 q 加 1。
                    完。
                    若 已有描述 等于 假：
                        做 文档条目_添加参数 用 条目、签名 的 第 1 项 的 第 p 项、「」。
                    完。
                    p 为 p 加 1。
                完。
                做 追加 用 条目列表、条目。
            完。
            当前注释 为 一组：（空）。
            i 为 i 加 1。
            续。
        完。
        若 做 文档_开头是 用 行、「定义 」 或者 做 文档_开头是 用 行、「令 」：
            常量名 为 做 文档_解析常量名 用 行。
            若 做 计数 用 常量名 大于 0：
                条目 为 做 文档条目_创建 用 文档_常量、常量名、「」。
                做 文档条目_设模块 用 条目、模块名。
                做 文档条目_设行号 用 条目、i 加 1。
                做 文档_处理注释 用 条目、当前注释。
                做 追加 用 条目列表、条目。
            完。
            当前注释 为 一组：（空）。
            i 为 i 加 1。
            续。
        完。
        若 做 计数 用 当前注释 大于 0 并且 做 文档_包含 用 当前注释 的 第 0 项、「═══」：
            标题 为 做 文档_去空白 用 当前注释 的 第 0 项。
            可变 干净标题 为 「」。
            可变 k 为 0。
            只要 k 小于 做 计数 用 标题：
                c 为 做 子串 用 标题、k、1。
                若 做 字符串等于 用 c、「═」 等于 假：
                    干净标题 为 做 字符串拼接 用 干净标题、c。
                完。
                k 为 k 加 1。
            完。
            干净标题 为 做 文档_去空白 用 干净标题。
            若 做 计数 用 干净标题 大于 0：
                段落条目 为 做 文档条目_创建 用 文档_模块、干净标题、「」。
                做 文档条目_设模块 用 段落条目、模块名。
                若 做 计数 用 当前注释 大于 1：
                    段落条目 的 第 2 项 为 做 文档_拼接描述 用 当前注释。
                完。
                做 追加 用 条目列表、段落条目。
            完。
        完。
        当前注释 为 一组：（空）。
        i 为 i 加 1。
    完。
    返还 条目列表。
完。

文档_处理注释 接受 条目、注释列表：
    可变 描述 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 注释列表：
        行 为 注释列表 的 第 i 项。
        若 做 文档_开头是 用 行、「@参数 」 或者 做 文档_开头是 用 行、「@param 」：
            剩余 为 做 文档_去空白 用 做 子串 用 行、4、做 计数 用 行 减 4。
            空格位 为 做 文档_查找字符 用 剩余、「 」。
            若 空格位 大于 0：
                做 文档条目_添加参数 用 条目、做 子串 用 剩余、0、空格位、做 文档_去空白 用 做 子串 用 剩余、空格位、做 计数 用 剩余 减 空格位。
            完。
        否则若 做 文档_开头是 用 行、「@回 」 或者 做 文档_开头是 用 行、「@return 」：
            做 文档条目_设返回 用 条目、做 文档_去空白 用 做 子串 用 行、4、做 计数 用 行 减 4。
        否则若 做 文档_开头是 用 行、「@示例」：
            可变 示例 为 「」。
            i 为 i 加 1。
            只要 i 小于 做 计数 用 注释列表 并且 做 文档_开头是 用 注释列表 的 第 i 项、「@」 等于 假：
                若 做 计数 用 示例 大于 0：
                    示例 为 做 字符串拼接 用 示例、「\n」。
                完。
                示例 为 做 字符串拼接 用 示例、注释列表 的 第 i 项。
                i 为 i 加 1。
            完。
            做 文档条目_设示例 用 条目、示例。
            续。
        否则若 做 文档_包含 用 行、「═══」 等于 假：
            若 做 计数 用 描述 大于 0：
                描述 为 做 字符串拼接 用 描述、「 」。
            完。
            描述 为 做 字符串拼接 用 描述、行。
        完。
        i 为 i 加 1。
    完。
    条目 的 第 2 项 为 描述。
完。

文档_生成Markdown 接受 条目列表、标题：
    可变 md 为 「# 」。
    md 为 做 字符串拼接 用 md、标题。
    md 为 做 字符串拼接 用 md、「\n\n」。
    可变 当前模块 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目列表：
        条目 为 条目列表 的 第 i 项。
        若 条目 的 第 0 项 等于 文档_模块：
            md 为 做 字符串拼接 用 md、「\n## 」。
            md 为 做 字符串拼接 用 md、条目 的 第 1 项。
            md 为 做 字符串拼接 用 md、「\n\n」。
            若 做 计数 用 条目 的 第 2 项 大于 0：
                md 为 做 字符串拼接 用 md、条目 的 第 2 项。
                md 为 做 字符串拼接 用 md、「\n\n」。
            完。
            当前模块 为 条目 的 第 1 项。
        完。
        若 条目 的 第 0 项 等于 文档_函数：
            md 为 做 字符串拼接 用 md、「### `」。
            md 为 做 字符串拼接 用 md、条目 的 第 1 项。
            md 为 做 字符串拼接 用 md、「`\n\n」。
            若 做 计数 用 条目 的 第 2 项 大于 0：
                md 为 做 字符串拼接 用 md、条目 的 第 2 项。
                md 为 做 字符串拼接 用 md、「\n\n」。
            完。
            若 做 计数 用 条目 的 第 3 项 大于 0：
                md 为 做 字符串拼接 用 md、「**参数:**\n\n」。
                可变 p 为 0。
                只要 p 小于 做 计数 用 条目 的 第 3 项：
                    md 为 做 字符串拼接 用 md、「- `」。
                    md 为 做 字符串拼接 用 md、条目 的 第 3 项 的 第 p 项 的 第 0 项。
                    md 为 做 字符串拼接 用 md、「`」。
                    若 做 计数 用 条目 的 第 3 项 的 第 p 项 的 第 1 项 大于 0：
                        md 为 做 字符串拼接 用 md、「 — 」。
                        md 为 做 字符串拼接 用 md、条目 的 第 3 项 的 第 p 项 的 第 1 项。
                    完。
                    md 为 做 字符串拼接 用 md、「\n」。
                    p 为 p 加 1。
                完。
                md 为 做 字符串拼接 用 md、「\n」。
            完。
            若 做 计数 用 条目 的 第 4 项 大于 0：
                md 为 做 字符串拼接 用 md、「**回:** 」。
                md 为 做 字符串拼接 用 md、条目 的 第 4 项。
                md 为 做 字符串拼接 用 md、「\n\n」。
            完。
            若 做 计数 用 条目 的 第 5 项 大于 0：
                md 为 做 字符串拼接 用 md、「**示例:**\n\n```nova\n」。
                md 为 做 字符串拼接 用 md、条目 的 第 5 项。
                md 为 做 字符串拼接 用 md、「\n```\n\n」。
            完。
        完。
        若 条目 的 第 0 项 等于 文档_常量：
            md 为 做 字符串拼接 用 md、「- **`」。
            md 为 做 字符串拼接 用 md、条目 的 第 1 项。
            md 为 做 字符串拼接 用 md、「`**」。
            若 做 计数 用 条目 的 第 2 项 大于 0：
                md 为 做 字符串拼接 用 md、「 — 」。
                md 为 做 字符串拼接 用 md、条目 的 第 2 项。
            完。
            md 为 做 字符串拼接 用 md、「\n」。
        完。
        i 为 i 加 1。
    完。
    返还 md。
完。

文档_解析函数签名 接受 行：
    n 为 做 计数 用 行。
    可变 名称开始 为 3。
    只要 名称开始 小于 n 并且 做 字符串等于 用 做 子串 用 行、名称开始、1、「 」：
        名称开始 为 名称开始 加 1。
    完。
    可变 括号位 为 名称开始。
    只要 括号位 小于 n 并且 做 字符串等于 用 做 子串 用 行、括号位、1、「(」 等于 假：
        括号位 为 括号位 加 1。
    完。
    若 括号位 大于或等于 n：
        返还 空。
    完。
    名称 为 做 子串 用 行、名称开始、括号位 减 名称开始。
    可变 参数列表 为 一组：（空）。
    可变 i 为 括号位 加 1。
    可变 当前参数 为 「」。
    可变 _参完 为 假。
    只要 i 小于 n 并且 _参完 等于 假：
        c 为 做 子串 用 行、i、1。
        若 c 等于 「)」：
            _参完 为 真。
        否则：
            若 做 字符串等于 用 c、「,」：
                参数 为 做 文档_去空白 用 当前参数。
                若 做 计数 用 参数 大于 0：
                    做 追加 用 参数列表、参数。
                完。
                当前参数 为 「」。
            否则：
                当前参数 为 做 字符串拼接 用 当前参数、c。
            完。
            i 为 i 加 1。
        完。
    完。
    最后参数 为 做 文档_去空白 用 当前参数。
    若 做 计数 用 最后参数 大于 0：
        做 追加 用 参数列表、最后参数。
    完。
    返还 一组：名称、参数列表。
完。

文档_解析常量名 接受 行：
    可变 i 为 3。
    只要 i 小于 做 计数 用 行 并且 做 字符串等于 用 做 子串 用 行、i、1、「 」：
        i 为 i 加 1。
    完。
    可变 名称 为 「」。
    可变 _名完 为 假。
    只要 i 小于 做 计数 用 行 并且 _名完 等于 假：
        c 为 做 子串 用 行、i、1。
        若 c 等于 「 」 或者 c 等于 「=」：
            _名完 为 真。
        否则：
            名称 为 做 字符串拼接 用 名称、c。
            i 为 i 加 1。
        完。
    完。
    返还 名称。
完。

文档_分割行 接受 文本：
    n 为 做 计数 用 文本。
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 文本、i、1。
        若 做 字符串等于 用 c、「\n」：
            做 追加 用 结果、当前。
            当前 为 「」。
        否则若 做 字符串等于 用 c、「\r」 等于 假：
            当前 为 做 字符串拼接 用 当前、c。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前 大于 0：
        做 追加 用 结果、当前。
    完。
    返还 结果。
完。

文档_拼接描述 接受 注释列表：
    可变 描述 为 「」。
    可变 j 为 1。
    只要 j 小于 做 计数 用 注释列表：
        若 做 文档_包含 用 注释列表 的 第 j 项、「═══」 等于 假：
            若 做 计数 用 描述 大于 0：
                描述 为 做 字符串拼接 用 描述、「 」。
            完。
            描述 为 做 字符串拼接 用 描述、注释列表 的 第 j 项。
        完。
        j 为 j 加 1。
    完。
    返还 描述。
完。

文档_去空白 接受 s：
    n 为 做 计数 用 s。
    可变 开始 为 0。
    只要 开始 小于 n 并且 (做 字符串等于 用 做 子串 用 s、开始、1、「 」 或者 做 字符串等于 用 做 子串 用 s、开始、1、「\t」)：
        开始 为 开始 加 1。
    完。
    可变 结束 为 n。
    只要 结束 大于 开始 并且 (做 字符串等于 用 做 子串 用 s、结束 减 1、1、「 」 或者 做 字符串等于 用 做 子串 用 s、结束 减 1、1、「\t」)：
        结束 为 结束 减 1。
    完。
    若 开始 大于或等于 结束：
        返还 「」。
    完。
    返还 做 子串 用 s、开始、结束 减 开始。
完。

文档_开头是 接受 s、前缀：
    若 做 计数 用 s 小于 做 计数 用 前缀：
        返还 假。
    完。
    返还 做 字符串等于 用 做 子串 用 s、0、做 计数 用 前缀、前缀。
完。

文档_包含 接受 文本、子串文本：
    n 为 做 计数 用 文本。
    m 为 做 计数 用 子串文本。
    若 m 大于 n：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于或等于 n 减 m：
        若 做 字符串等于 用 做 子串 用 文本、i、m、子串文本：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

文档_查找字符 接受 s、目标：
    可变 i 为 0。
    只要 i 小于 做 计数 用 s：
        若 做 字符串等于 用 做 子串 用 s、i、1、目标：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

文档_生成标题 接受 级别、文本：
    可变 前缀 为 「」。
    可变 i 为 0。
    只要 i 小于 级别：
        前缀 为 做 字符串拼接 用 前缀、「#」。
        i 为 i 加 1。
    完。
    返还 做 字符串拼接 用 前缀、做 字符串拼接 用 「 」、文本。
完。

文档_生成列表项 接受 缩进、文本：
    可变 前缀 为 「」。
    可变 i 为 0。
    只要 i 小于 缩进：
        前缀 为 做 字符串拼接 用 前缀、「 」。
        i 为 i 加 1。
    完。
    返还 做 字符串拼接 用 前缀、做 字符串拼接 用 「- 」、文本。
完。

文档_生成代码块 接受 语言、代码：
    返还 做 字符串拼接 用 「```」、做 字符串拼接 用 语言、做 字符串拼接 用 「\n」、做 字符串拼接 用 代码、「\n```」。
完。

文档_生成表格头 接受 列名列表：
    可变 头 为 「|」。
    可变 分隔 为 「|」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列名列表：
        头 为 做 字符串拼接 用 头、做 字符串拼接 用 「 」、做 字符串拼接 用 列名列表 的 第 i 项、「 |」。
        分隔 为 做 字符串拼接 用 分隔、「 --- |」。
        i 为 i 加 1。
    完。
    返还 做 字符串拼接 用 头、做 字符串拼接 用 「\n」、分隔。
完。

文档_生成表格行 接受 值列表：
    可变 行 为 「|」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 值列表：
        行 为 做 字符串拼接 用 行、做 字符串拼接 用 「 」、做 字符串拼接 用 值列表 的 第 i 项、「 |」。
        i 为 i 加 1。
    完。
    返还 行。
完。

文档_解析函数签名_简表 接受 行：
    可变 名称 为 「」。
    可变 参数 为 「」。
    可变 阶段 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行：
        c 为 做 字符码 用 行、i。
        若 阶段 等于 0 并且 c 等于 字符_空格：
            阶段 为 1。
        否则若 阶段 等于 1：
            若 c 等于 字符_左圆括：
                阶段 为 2。
            否则：
                名称 为 做 字符串拼接 用 名称、做 字符 用 c。
            完。
        否则若 阶段 等于 2：
            若 c 等于 字符_右圆括：
                阶段 为 3。
            否则：
                参数 为 做 字符串拼接 用 参数、做 字符 用 c。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 一组：名称、参数。
完。

文档_生成API 接受 函数列表：
    可变 结果 为 做 文档_生成标题 用 1、「API参考」。
    结果 为 做 字符串拼接 用 结果、「\n\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 函数列表：
        签名 为 做 文档_解析函数签名_简表 用 函数列表 的 第 i 项。
        结果 为 做 字符串拼接 用 结果、做 文档_生成标题 用 2、签名 的 第 0 项。
        结果 为 做 字符串拼接 用 结果、做 字符串拼接 用 「\n\n参数: `」、做 字符串拼接 用 签名 的 第 1 项、「`\n\n」。
        i 为 i 加 1。
    完。
    返还 结果。
完。

文档_生成目录 接受 标题列表：
    可变 目录 为 「## 目录\n\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 标题列表：
        目录 为 做 字符串拼接 用 目录、做 字符串拼接 用 「- 」、做 字符串拼接 用 标题列表 的 第 i 项、「\n」。
        i 为 i 加 1。
    完。
    返还 目录。
完。

可变 _文档_生成次数 为 0。

可变 _文档_总字数 为 0。

可变 _文档_函数数 为 0。

文档_记录生成 接受 字数：
    _文档_生成次数 为 _文档_生成次数 加 1。
    _文档_总字数 为 _文档_总字数 加 字数。
完。

文档_记录函数 接受 零参：
    _文档_函数数 为 _文档_函数数 加 1。
完。

文档_质量评估 接受 零参：
    若 _文档_函数数 大于 0 并且 _文档_总字数 除以 _文档_函数数 小于 10：
        返还 「文档过简(补充描述)」。
    完。
    若 _文档_总字数 大于 100000：
        返还 「文档量大」。
    完。
    返还 「文档正常」。
完。

文档_重置 接受 零参：
    _文档_生成次数 为 0。
    _文档_总字数 为 0。
    _文档_函数数 为 0。
完。

本章 收束。
