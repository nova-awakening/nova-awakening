《前端入口》

引用《..·标准库·基础》。

引用《..·标准库·系统·硬件库》。

引用《标记常量》。

引用《规划·核心·常量》。

引用《词法·注释处理》。

引用《词法·数字扫描》。

引用《词法·字符串扫描》。

可变 词法状态 为 一组：0、0、0、0、0。

可变 语法状态 为 一组：0、0、0、0、0、0、一组：（空）。

可变 行号状态 为 一组：一组：（空）、一组：（空）、一组：（空）、一组：（空）、一组：（空）、0、0。

词法引擎_全局重置 接受 零参：
    词法状态 的 第 0 项 为 0。
    词法状态 的 第 1 项 为 0。
    词法状态 的 第 2 项 为 0。
    词法状态 的 第 3 项 为 0。
    词法状态 的 第 4 项 为 0。
完。

语法引擎_全局重置 接受 零参：
    语法状态 的 第 0 项 为 0。
    语法状态 的 第 1 项 为 0。
    语法状态 的 第 2 项 为 0。
    语法状态 的 第 3 项 为 0。
    语法状态 的 第 4 项 为 0。
    语法状态 的 第 5 项 为 0。
    语法状态 的 第 6 项 为 一组：（空）。
完。

行号追踪_全局重置 接受 零参：
    行号状态 的 第 0 项 为 一组：（空）。
    行号状态 的 第 1 项 为 一组：（空）。
    行号状态 的 第 2 项 为 一组：（空）。
    行号状态 的 第 3 项 为 一组：（空）。
    行号状态 的 第 4 项 为 一组：（空）。
    行号状态 的 第 5 项 为 0。
    行号状态 的 第 6 项 为 0。
完。

前端_查关键字 接受 名称：
    若 名称 等于 「当」：
        返还 标记_当。
    完。
    若 名称 等于 「定义」：
        返还 标记_定义。
    完。
    若 名称 等于 「可变」：
        返还 标记_可变。
    完。
    若 名称 等于 「函数」：
        返还 标记_函数。
    完。
    若 名称 等于 「返回」：
        返还 标记_返回。
    完。
    若 名称 等于 「如果」：
        返还 标记_如果。
    完。
    若 名称 等于 「若」：
        返还 标记_如果。
    完。
    若 名称 等于 「假如」：
        返还 标记_如果。
    完。
    若 名称 等于 「假设」：
        返还 标记_如果。
    完。
    若 名称 等于 「倘若」：
        返还 标记_如果。
    完。
    若 名称 等于 「要是」：
        返还 标记_如果。
    完。
    若 名称 等于 「因为」：
        返还 标记_如果。
    完。
    若 名称 等于 「虽然」：
        返还 标记_如果。
    完。
    若 名称 等于 「否则」：
        返还 标记_否则。
    完。
    若 名称 等于 「不然」：
        返还 标记_否则。
    完。
    若 名称 等于 「不然的话」：
        返还 标记_否则。
    完。
    若 名称 等于 「但是」：
        返还 标记_连接词。
    完。
    若 名称 等于 「可是」：
        返还 标记_连接词。
    完。
    若 名称 等于 「然而」：
        返还 标记_连接词。
    完。
    若 名称 等于 「循环」：
        返还 标记_循环。
    完。
    若 名称 等于 「导入」：
        返还 标记_导入。
    完。
    若 名称 等于 「结构」：
        返还 标记_结构。
    完。
    若 名称 等于 「建」：
        返还 标记_建。
    完。
    若 名称 等于 「匹配」：
        返还 标记_匹配。
    完。
    若 名称 等于 「默认」：
        返还 标记_默认。
    完。
    若 名称 等于 「中断」：
        返还 标记_中断。
    完。
    若 名称 等于 「继续」：
        返还 标记_继续。
    完。
    若 名称 等于 「真」：
        返还 标记_真。
    完。
    若 名称 等于 「假」：
        返还 标记_假。
    完。
    若 名称 等于 「空」：
        返还 标记_空。
    完。
    若 名称 等于 「尝试」：
        返还 标记_尝试。
    完。
    若 名称 等于 「得」：
        返还 标记_得。
    完。
    若 名称 等于 「抛出」：
        返还 标记_抛出。
    完。
    若 名称 等于 「每」：
        返还 标记_循环。
    完。
    若 名称 等于 「每个」：
        返还 标记_循环。
    完。
    若 名称 等于 「入」：
        返还 标记_导入。
    完。
    若 名称 等于 「止」：
        返还 标记_中断。
    完。
    若 名称 等于 「续」：
        返还 标记_继续。
    完。
    若 名称 等于 「试」：
        返还 标记_尝试。
    完。
    若 名称 等于 「抛」：
        返还 标记_抛出。
    完。
    若 名称 等于 「所以」：
        返还 标记_连接词。
    完。
    若 名称 等于 「?」：
        返还 标记_连接词。
    完。
    若 名称 等于 「则」：
        返还 标记_连接词。
    完。
    若 名称 等于 「首先」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「然后」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「接着」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「随后」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「最后」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「此时」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「于是」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「继而」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「其次」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「再者」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「另外」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「汇编」：
        返还 标记_汇编。
    完。
    若 名称 等于 「必须」：
        返还 标记_如果。
    完。
    若 名称 等于 「一定」：
        返还 标记_如果。
    完。
    若 名称 等于 「若是」：
        返还 标记_如果。
    完。
    若 名称 等于 「只要」：
        返还 标记_如果。
    完。
    若 名称 等于 「每当」：
        返还 标记_如果。
    完。
    若 名称 等于 「一旦」：
        返还 标记_如果。
    完。
    若 名称 等于 「凡是」：
        返还 标记_如果。
    完。
    若 名称 等于 「倘」：
        返还 标记_如果。
    完。
    若 名称 等于 「否则的话」：
        返还 标记_否则。
    完。
    若 名称 等于 「反之」：
        返还 标记_否则。
    完。
    若 名称 等于 「除此之外」：
        返还 标记_否则。
    完。
    若 名称 等于 「直到」：
        返还 标记_当。
    完。
    若 名称 等于 「重复」：
        返还 标记_当。
    完。
    若 名称 等于 「持续」：
        返还 标记_当。
    完。
    若 名称 等于 「法」：
        返还 标记_函数。
    完。
    若 名称 等于 「做法」：
        返还 标记_函数。
    完。
    若 名称 等于 「称为」：
        返还 标记_定义。
    完。
    若 名称 等于 「叫做」：
        返还 标记_定义。
    完。
    若 名称 等于 「给出」：
        返还 标记_返回。
    完。
    若 名称 等于 「大于等于」：
        返还 标记_大等。
    完。
    若 名称 等于 「小于等于」：
        返还 标记_小等。
    完。
    若 名称 等于 「不等于」：
        返还 标记_不等。
    完。
    若 名称 等于 「大于」：
        返还 标记_大于。
    完。
    若 名称 等于 「小于」：
        返还 标记_小于。
    完。
    若 名称 等于 「等于」：
        返还 标记_等等。
    完。
    若 名称 等于 「加等」：
        返还 标记_加等。
    完。
    若 名称 等于 「减等」：
        返还 标记_减等。
    完。
    若 名称 等于 「且」：
        返还 标记_且。
    完。
    若 名称 等于 「并且」：
        返还 标记_且。
    完。
    若 名称 等于 「而且」：
        返还 标记_且。
    完。
    若 名称 等于 「或」：
        返还 标记_或。
    完。
    若 名称 等于 「或者」：
        返还 标记_或。
    完。
    若 名称 等于 「非」：
        返还 标记_非。
    完。
    若 名称 等于 「知晓」：
        返还 标记_标识符。
    完。
    若 名称 等于 「推断」：
        返还 标记_标识符。
    完。
    若 名称 等于 「记住」：
        返还 标记_标识符。
    完。
    若 名称 等于 「回想」：
        返还 标记_标识符。
    完。
    若 名称 等于 「关注」：
        返还 标记_标识符。
    完。
    若 名称 等于 「学会」：
        返还 标记_标识符。
    完。
    若 名称 等于 「理解」：
        返还 标记_标识符。
    完。
    若 名称 等于 「领悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「认知」：
        返还 标记_标识符。
    完。
    若 名称 等于 「感知」：
        返还 标记_标识符。
    完。
    若 名称 等于 「说」：
        返还 标记_标识符。
    完。
    若 名称 等于 「打印」：
        返还 标记_标识符。
    完。
    若 名称 等于 「显示」：
        返还 标记_标识符。
    完。
    若 名称 等于 「告诉」：
        返还 标记_标识符。
    完。
    若 名称 等于 「报告」：
        返还 标记_标识符。
    完。
    若 名称 等于 「不同」：
        返还 标记_不等。
    完。
    若 名称 等于 「不一样」：
        返还 标记_不等。
    完。
    若 名称 等于 「不一致」：
        返还 标记_不等。
    完。
    若 名称 等于 「有别」：
        返还 标记_不等。
    完。
    若 名称 等于 「有异」：
        返还 标记_不等。
    完。
    若 名称 等于 「迥异」：
        返还 标记_不等。
    完。
    若 名称 等于 「殊异」：
        返还 标记_不等。
    完。
    若 名称 等于 「各异」：
        返还 标记_不等。
    完。
    若 名称 等于 「相异」：
        返还 标记_不等。
    完。
    若 名称 等于 「不似」：
        返还 标记_不等。
    完。
    若 名称 等于 「以及」：
        返还 标记_且。
    完。
    若 名称 等于 「兼」：
        返还 标记_且。
    完。
    若 名称 等于 「同时」：
        返还 标记_且。
    完。
    若 名称 等于 「亦」：
        返还 标记_且。
    完。
    若 名称 等于 「与」：
        返还 标记_且。
    完。
    若 名称 等于 「连同」：
        返还 标记_且。
    完。
    若 名称 等于 「加之」：
        返还 标记_且。
    完。
    若 名称 等于 「况且」：
        返还 标记_且。
    完。
    若 名称 等于 「何况」：
        返还 标记_且。
    完。
    若 名称 等于 「不但」：
        返还 标记_且。
    完。
    若 名称 等于 「不仅」：
        返还 标记_且。
    完。
    若 名称 等于 「既」：
        返还 标记_且。
    完。
    若 名称 等于 「又」：
        返还 标记_且。
    完。
    若 名称 等于 「打断」：
        返还 标记_中断。
    完。
    若 名称 等于 「跳出」：
        返还 标记_中断。
    完。
    若 名称 等于 「脱离」：
        返还 标记_中断。
    完。
    若 名称 等于 「终了」：
        返还 标记_中断。
    完。
    若 名称 等于 「就此打住」：
        返还 标记_中断。
    完。
    若 名称 等于 「到此为止」：
        返还 标记_中断。
    完。
    若 名称 等于 「戛然而止」：
        返还 标记_中断。
    完。
    若 名称 等于 「招式」：
        返还 标记_函数。
    完。
    若 名称 等于 「技法」：
        返还 标记_函数。
    完。
    若 名称 等于 「程式」：
        返还 标记_函数。
    完。
    若 名称 等于 「诀」：
        返还 标记_函数。
    完。
    若 名称 等于 「式」：
        返还 标记_函数。
    完。
    若 名称 等于 「道」：
        返还 标记_函数。
    完。
    若 名称 等于 「手法」：
        返还 标记_函数。
    完。
    若 名称 等于 「笔法」：
        返还 标记_函数。
    完。
    若 名称 等于 「章法」：
        返还 标记_函数。
    完。
    若 名称 等于 「心法」：
        返还 标记_函数。
    完。
    若 名称 等于 「口诀」：
        返还 标记_函数。
    完。
    若 名称 等于 「秘诀」：
        返还 标记_函数。
    完。
    若 名称 等于 「妙法」：
        返还 标记_函数。
    完。
    若 名称 等于 「法门」：
        返还 标记_函数。
    完。
    若 名称 等于 「要诀」：
        返还 标记_函数。
    完。
    若 名称 等于 「窍门」：
        返还 标记_函数。
    完。
    若 名称 等于 「法则」：
        返还 标记_函数。
    完。
    若 名称 等于 「定式」：
        返还 标记_函数。
    完。
    若 名称 等于 「要不」：
        返还 标记_否则。
    完。
    若 名称 等于 「要不然」：
        返还 标记_否则。
    完。
    若 名称 等于 「再不然」：
        返还 标记_否则。
    完。
    若 名称 等于 「若非如此」：
        返还 标记_否则。
    完。
    若 名称 等于 「与此相反」：
        返还 标记_否则。
    完。
    若 名称 等于 「恰恰相反」：
        返还 标记_否则。
    完。
    若 名称 等于 「另则」：
        返还 标记_否则。
    完。
    若 名称 等于 「再不」：
        返还 标记_否则。
    完。
    若 名称 等于 「亦非」：
        返还 标记_否则。
    完。
    若 名称 等于 「不则」：
        返还 标记_否则。
    完。
    若 名称 等于 「否则就」：
        返还 标记_否则。
    完。
    若 名称 等于 「否则便」：
        返还 标记_否则。
    完。
    若 名称 等于 「若不然」：
        返还 标记_否则。
    完。
    若 名称 等于 「如若不然」：
        返还 标记_否则。
    完。
    若 名称 等于 「不然就」：
        返还 标记_否则。
    完。
    若 名称 等于 「超过」：
        返还 标记_大于。
    完。
    若 名称 等于 「多于」：
        返还 标记_大于。
    完。
    若 名称 等于 「高于」：
        返还 标记_大于。
    完。
    若 名称 等于 「胜过」：
        返还 标记_大于。
    完。
    若 名称 等于 「优于」：
        返还 标记_大于。
    完。
    若 名称 等于 「逾」：
        返还 标记_大于。
    完。
    若 名称 等于 「过」：
        返还 标记_大于。
    完。
    若 名称 等于 「越」：
        返还 标记_大于。
    完。
    若 名称 等于 「甚于」：
        返还 标记_大于。
    完。
    若 名称 等于 「至少」：
        返还 标记_大等。
    完。
    若 名称 等于 「不少于」：
        返还 标记_大等。
    完。
    若 名称 等于 「不低于」：
        返还 标记_大等。
    完。
    若 名称 等于 「起码」：
        返还 标记_大等。
    完。
    若 名称 等于 「不亚于」：
        返还 标记_大等。
    完。
    若 名称 等于 「不下于」：
        返还 标记_大等。
    完。
    若 名称 等于 「设若」：
        返还 标记_如果。
    完。
    若 名称 等于 「设使」：
        返还 标记_如果。
    完。
    若 名称 等于 「万一」：
        返还 标记_如果。
    完。
    若 名称 等于 「即使」：
        返还 标记_如果。
    完。
    若 名称 等于 「哪怕」：
        返还 标记_如果。
    完。
    若 名称 等于 「就算」：
        返还 标记_如果。
    完。
    若 名称 等于 「但凡」：
        返还 标记_如果。
    完。
    若 名称 等于 「既然」：
        返还 标记_如果。
    完。
    若 名称 等于 「鉴于」：
        返还 标记_如果。
    完。
    若 名称 等于 「如若」：
        返还 标记_如果。
    完。
    若 名称 等于 「假若」：
        返还 标记_如果。
    完。
    若 名称 等于 「诚然」：
        返还 标记_如果。
    完。
    若 名称 等于 「纵然」：
        返还 标记_如果。
    完。
    若 名称 等于 「纵使」：
        返还 标记_如果。
    完。
    若 名称 等于 「除非」：
        返还 标记_如果。
    完。
    若 名称 等于 「万若」：
        返还 标记_如果。
    完。
    若 名称 等于 「要不是」：
        返还 标记_如果。
    完。
    若 名称 等于 「苟」：
        返还 标记_如果。
    完。
    若 名称 等于 「若夫」：
        返还 标记_如果。
    完。
    若 名称 等于 「设令」：
        返还 标记_如果。
    完。
    若 名称 等于 「如其」：
        返还 标记_如果。
    完。
    若 名称 等于 「若使」：
        返还 标记_如果。
    完。
    若 名称 等于 「若令」：
        返还 标记_如果。
    完。
    若 名称 等于 「设或」：
        返还 标记_如果。
    完。
    若 名称 等于 「傥」：
        返还 标记_如果。
    完。
    若 名称 等于 「诚如」：
        返还 标记_如果。
    完。
    若 名称 等于 「脱若」：
        返还 标记_如果。
    完。
    若 名称 等于 「向使」：
        返还 标记_如果。
    完。
    若 名称 等于 「使」：
        返还 标记_如果。
    完。
    若 名称 等于 「藉」：
        返还 标记_如果。
    完。
    若 名称 等于 「若果」：
        返还 标记_如果。
    完。
    若 名称 等于 「果若」：
        返还 标记_如果。
    完。
    若 名称 等于 「果然」：
        返还 标记_如果。
    完。
    若 名称 等于 「当真」：
        返还 标记_如果。
    完。
    若 名称 等于 「确实」：
        返还 标记_如果。
    完。
    若 名称 等于 「如果说」：
        返还 标记_如果。
    完。
    若 名称 等于 「倘或」：
        返还 标记_如果。
    完。
    若 名称 等于 「倘使」：
        返还 标记_如果。
    完。
    若 名称 等于 「或若」：
        返还 标记_如果。
    完。
    若 名称 等于 「或者说」：
        返还 标记_如果。
    完。
    若 名称 等于 「赋名」：
        返还 标记_定义。
    完。
    若 名称 等于 「规定」：
        返还 标记_定义。
    完。
    若 名称 等于 「指定」：
        返还 标记_定义。
    完。
    若 名称 等于 「设定」：
        返还 标记_定义。
    完。
    若 名称 等于 「记为」：
        返还 标记_定义。
    完。
    若 名称 等于 「名曰」：
        返还 标记_定义。
    完。
    若 名称 等于 「谓之」：
        返还 标记_定义。
    完。
    若 名称 等于 「号为」：
        返还 标记_定义。
    完。
    若 名称 等于 「名为」：
        返还 标记_定义。
    完。
    若 名称 等于 「定为」：
        返还 标记_定义。
    完。
    若 名称 等于 「立为」：
        返还 标记_定义。
    完。
    若 名称 等于 「封为」：
        返还 标记_定义。
    完。
    若 名称 等于 「册为」：
        返还 标记_定义。
    完。
    若 名称 等于 「题为」：
        返还 标记_定义。
    完。
    若 名称 等于 「署为」：
        返还 标记_定义。
    完。
    若 名称 等于 「冠以」：
        返还 标记_定义。
    完。
    若 名称 等于 「拟为」：
        返还 标记_定义。
    完。
    若 名称 等于 「引入」：
        返还 标记_导入。
    完。
    若 名称 等于 「纳入」：
        返还 标记_导入。
    完。
    若 名称 等于 「援引」：
        返还 标记_导入。
    完。
    若 名称 等于 「征引」：
        返还 标记_导入。
    完。
    若 名称 等于 「引」：
        返还 标记_导入。
    完。
    若 名称 等于 「不足」：
        返还 标记_小于。
    完。
    若 名称 等于 「少于」：
        返还 标记_小于。
    完。
    若 名称 等于 「低于」：
        返还 标记_小于。
    完。
    若 名称 等于 「劣于」：
        返还 标记_小于。
    完。
    若 名称 等于 「逊于」：
        返还 标记_小于。
    完。
    若 名称 等于 「亚于」：
        返还 标记_小于。
    完。
    若 名称 等于 「次于」：
        返还 标记_小于。
    完。
    若 名称 等于 「弱于」：
        返还 标记_小于。
    完。
    若 名称 等于 「欠于」：
        返还 标记_小于。
    完。
    若 名称 等于 「至多」：
        返还 标记_小等。
    完。
    若 名称 等于 「不超过」：
        返还 标记_小等。
    完。
    若 名称 等于 「不多于」：
        返还 标记_小等。
    完。
    若 名称 等于 「不高于」：
        返还 标记_小等。
    完。
    若 名称 等于 「顶多」：
        返还 标记_小等。
    完。
    若 名称 等于 「充其量」：
        返还 标记_小等。
    完。
    若 名称 等于 「试探」：
        返还 标记_尝试。
    完。
    若 名称 等于 「试验」：
        返还 标记_尝试。
    完。
    若 名称 等于 「试图」：
        返还 标记_尝试。
    完。
    若 名称 等于 「姑且」：
        返还 标记_尝试。
    完。
    若 名称 等于 「且试」：
        返还 标记_尝试。
    完。
    若 名称 等于 「反复」：
        返还 标记_当。
    完。
    若 名称 等于 「始终」：
        返还 标记_当。
    完。
    若 名称 等于 「一直」：
        返还 标记_当。
    完。
    若 名称 等于 「不断」：
        返还 标记_当。
    完。
    若 名称 等于 「不停」：
        返还 标记_当。
    完。
    若 名称 等于 「周而复始」：
        返还 标记_当。
    完。
    若 名称 等于 「循而往复」：
        返还 标记_当。
    完。
    若 名称 等于 「往复」：
        返还 标记_当。
    完。
    若 名称 等于 「盘桓」：
        返还 标记_当。
    完。
    若 名称 等于 「徘徊」：
        返还 标记_当。
    完。
    若 名称 等于 「逐个」：
        返还 标记_循环。
    完。
    若 名称 等于 「逐项」：
        返还 标记_循环。
    完。
    若 名称 等于 「轮流」：
        返还 标记_循环。
    完。
    若 名称 等于 「历遍」：
        返还 标记_循环。
    完。
    若 名称 等于 「依次」：
        返还 标记_循环。
    完。
    若 名称 等于 「逐一」：
        返还 标记_循环。
    完。
    若 名称 等于 「挨个」：
        返还 标记_循环。
    完。
    若 名称 等于 「逐次」：
        返还 标记_循环。
    完。
    若 名称 等于 「逐件」：
        返还 标记_循环。
    完。
    若 名称 等于 「一一」：
        返还 标记_循环。
    完。
    若 名称 等于 「挨次」：
        返还 标记_循环。
    完。
    若 名称 等于 「抑或」：
        返还 标记_或。
    完。
    若 名称 等于 「亦或」：
        返还 标记_或。
    完。
    若 名称 等于 「还是」：
        返还 标记_或。
    完。
    若 名称 等于 「要么」：
        返还 标记_或。
    完。
    若 名称 等于 「或许」：
        返还 标记_或。
    完。
    若 名称 等于 「也许」：
        返还 标记_或。
    完。
    若 名称 等于 「丢出」：
        返还 标记_抛出。
    完。
    若 名称 等于 「告警」：
        返还 标记_抛出。
    完。
    若 名称 等于 「掷出」：
        返还 标记_抛出。
    完。
    若 名称 等于 「投出」：
        返还 标记_抛出。
    完。
    若 名称 等于 「甩出」：
        返还 标记_抛出。
    完。
    若 名称 等于 「思考」：
        返还 标记_标识符。
    完。
    若 名称 等于 「判断」：
        返还 标记_标识符。
    完。
    若 名称 等于 「分析」：
        返还 标记_标识符。
    完。
    若 名称 等于 「演绎」：
        返还 标记_标识符。
    完。
    若 名称 等于 「归纳」：
        返还 标记_标识符。
    完。
    若 名称 等于 「类比」：
        返还 标记_标识符。
    完。
    若 名称 等于 「反思」：
        返还 标记_标识符。
    完。
    若 名称 等于 「总结」：
        返还 标记_标识符。
    完。
    若 名称 等于 「权衡」：
        返还 标记_标识符。
    完。
    若 名称 等于 「审视」：
        返还 标记_标识符。
    完。
    若 名称 等于 「洞察」：
        返还 标记_标识符。
    完。
    若 名称 等于 「发现」：
        返还 标记_标识符。
    完。
    若 名称 等于 「搜索」：
        返还 标记_标识符。
    完。
    若 名称 等于 「寻找」：
        返还 标记_标识符。
    完。
    若 名称 等于 「定位」：
        返还 标记_标识符。
    完。
    若 名称 等于 「追踪」：
        返还 标记_标识符。
    完。
    若 名称 等于 「监控」：
        返还 标记_标识符。
    完。
    若 名称 等于 「检测」：
        返还 标记_标识符。
    完。
    若 名称 等于 「确认」：
        返还 标记_标识符。
    完。
    若 名称 等于 「保障」：
        返还 标记_标识符。
    完。
    若 名称 等于 「维护」：
        返还 标记_标识符。
    完。
    若 名称 等于 「改进」：
        返还 标记_标识符。
    完。
    若 名称 等于 「进化」：
        返还 标记_标识符。
    完。
    若 名称 等于 「记忆」：
        返还 标记_标识符。
    完。
    若 名称 等于 「触发」：
        返还 标记_标识符。
    完。
    若 名称 等于 「转换」：
        返还 标记_标识符。
    完。
    若 名称 等于 「揣摩」：
        返还 标记_标识符。
    完。
    若 名称 等于 「忖度」：
        返还 标记_标识符。
    完。
    若 名称 等于 「揣度」：
        返还 标记_标识符。
    完。
    若 名称 等于 「估量」：
        返还 标记_标识符。
    完。
    若 名称 等于 「斟酌」：
        返还 标记_标识符。
    完。
    若 名称 等于 「取舍」：
        返还 标记_标识符。
    完。
    若 名称 等于 「抉择」：
        返还 标记_标识符。
    完。
    若 名称 等于 「甄别」：
        返还 标记_标识符。
    完。
    若 名称 等于 「辨识」：
        返还 标记_标识符。
    完。
    若 名称 等于 「辨析」：
        返还 标记_标识符。
    完。
    若 名称 等于 「洞悉」：
        返还 标记_标识符。
    完。
    若 名称 等于 「参透」：
        返还 标记_标识符。
    完。
    若 名称 等于 「勘破」：
        返还 标记_标识符。
    完。
    若 名称 等于 「通晓」：
        返还 标记_标识符。
    完。
    若 名称 等于 「谙熟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「精通」：
        返还 标记_标识符。
    完。
    若 名称 等于 「熟稔」：
        返还 标记_标识符。
    完。
    若 名称 等于 「觉察」：
        返还 标记_标识符。
    完。
    若 名称 等于 「察觉」：
        返还 标记_标识符。
    完。
    若 名称 等于 「体悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「顿悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「醒悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「省悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「彻悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「大悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「开悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「了悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「沉思」：
        返还 标记_标识符。
    完。
    若 名称 等于 「深思」：
        返还 标记_标识符。
    完。
    若 名称 等于 「冥想」：
        返还 标记_标识符。
    完。
    若 名称 等于 「静思」：
        返还 标记_标识符。
    完。
    若 名称 等于 「默想」：
        返还 标记_标识符。
    完。
    若 名称 等于 「凝视」：
        返还 标记_标识符。
    完。
    若 名称 等于 「注视」：
        返还 标记_标识符。
    完。
    若 名称 等于 「端详」：
        返还 标记_标识符。
    完。
    若 名称 等于 「审阅」：
        返还 标记_标识符。
    完。
    若 名称 等于 「研读」：
        返还 标记_标识符。
    完。
    若 名称 等于 「品鉴」：
        返还 标记_标识符。
    完。
    若 名称 等于 「鉴赏」：
        返还 标记_标识符。
    完。
    若 名称 等于 「赏析」：
        返还 标记_标识符。
    完。
    若 名称 等于 「剖析」：
        返还 标记_标识符。
    完。
    若 名称 等于 「解构」：
        返还 标记_标识符。
    完。
    若 名称 等于 「重构」：
        返还 标记_标识符。
    完。
    若 名称 等于 「整合」：
        返还 标记_标识符。
    完。
    若 名称 等于 「汇聚」：
        返还 标记_标识符。
    完。
    若 名称 等于 「凝聚」：
        返还 标记_标识符。
    完。
    若 名称 等于 「萃取」：
        返还 标记_标识符。
    完。
    若 名称 等于 「提炼」：
        返还 标记_标识符。
    完。
    若 名称 等于 「锤炼」：
        返还 标记_标识符。
    完。
    若 名称 等于 「淬炼」：
        返还 标记_标识符。
    完。
    若 名称 等于 「磨砺」：
        返还 标记_标识符。
    完。
    若 名称 等于 「雕琢」：
        返还 标记_标识符。
    完。
    若 名称 等于 「打磨」：
        返还 标记_标识符。
    完。
    若 名称 等于 「铸造」：
        返还 标记_标识符。
    完。
    若 名称 等于 「锻造」：
        返还 标记_标识符。
    完。
    若 名称 等于 「熔铸」：
        返还 标记_标识符。
    完。
    若 名称 等于 「相等」：
        返还 标记_等等。
    完。
    若 名称 等于 「一样」：
        返还 标记_等等。
    完。
    若 名称 等于 「同为」：
        返还 标记_等等。
    完。
    若 名称 等于 「恰为」：
        返还 标记_等等。
    完。
    若 名称 等于 「恰是」：
        返还 标记_等等。
    完。
    若 名称 等于 「正是」：
        返还 标记_等等。
    完。
    若 名称 等于 「便是」：
        返还 标记_等等。
    完。
    若 名称 等于 「即是」：
        返还 标记_等等。
    完。
    若 名称 等于 「乃是」：
        返还 标记_等等。
    完。
    若 名称 等于 「搭建」：
        返还 标记_结构。
    完。
    若 名称 等于 「组建」：
        返还 标记_结构。
    完。
    若 名称 等于 「体制」：
        返还 标记_结构。
    完。
    若 名称 等于 「格局」：
        返还 标记_结构。
    完。
    若 名称 等于 「越过」：
        返还 标记_继续。
    完。
    若 名称 等于 「略过」：
        返还 标记_继续。
    完。
    若 名称 等于 「接续」：
        返还 标记_继续。
    完。
    若 名称 等于 「延续」：
        返还 标记_继续。
    完。
    若 名称 等于 「续行」：
        返还 标记_继续。
    完。
    若 名称 等于 「继之」：
        返还 标记_继续。
    完。
    若 名称 等于 「续之」：
        返还 标记_继续。
    完。
    若 名称 等于 「得出」：
        返还 标记_返回。
    完。
    若 名称 等于 「传回」：
        返还 标记_返回。
    完。
    若 名称 等于 「回传」：
        返还 标记_返回。
    完。
    若 名称 等于 「带回」：
        返还 标记_返回。
    完。
    若 名称 等于 「送回」：
        返还 标记_返回。
    完。
    若 名称 等于 「呈递」：
        返还 标记_返回。
    完。
    若 名称 等于 「交还」：
        返还 标记_返回。
    完。
    若 名称 等于 「归还」：
        返还 标记_返回。
    完。
    若 名称 等于 「奉还」：
        返还 标记_返回。
    完。
    若 名称 等于 「缴回」：
        返还 标记_返回。
    完。
    若 名称 等于 「递回」：
        返还 标记_返回。
    完。
    若 名称 等于 「回禀」：
        返还 标记_返回。
    完。
    若 名称 等于 「呈报」：
        返还 标记_返回。
    完。
    若 名称 等于 「禀报」：
        返还 标记_返回。
    完。
    若 名称 等于 「上呈」：
        返还 标记_返回。
    完。
    若 名称 等于 「献上」：
        返还 标记_返回。
    完。
    若 名称 等于 「产生」：
        返还 标记_返回。
    完。
    若 名称 等于 「因此」：
        返还 标记_连接词。
    完。
    若 名称 等于 「故而」：
        返还 标记_连接词。
    完。
    若 名称 等于 「故此」：
        返还 标记_连接词。
    完。
    若 名称 等于 「因而」：
        返还 标记_连接词。
    完。
    若 名称 等于 「以致」：
        返还 标记_连接词。
    完。
    若 名称 等于 「换言之」：
        返还 标记_连接词。
    完。
    若 名称 等于 「总之」：
        返还 标记_连接词。
    完。
    若 名称 等于 「综上」：
        返还 标记_连接词。
    完。
    若 名称 等于 「不过」：
        返还 标记_连接词。
    完。
    若 名称 等于 「奈何」：
        返还 标记_连接词。
    完。
    若 名称 等于 「岂料」：
        返还 标记_连接词。
    完。
    若 名称 等于 「谁知」：
        返还 标记_连接词。
    完。
    若 名称 等于 「哪知」：
        返还 标记_连接词。
    完。
    若 名称 等于 「谁想」：
        返还 标记_连接词。
    完。
    若 名称 等于 「不料」：
        返还 标记_连接词。
    完。
    若 名称 等于 「不曾想」：
        返还 标记_连接词。
    完。
    若 名称 等于 「殊不知」：
        返还 标记_连接词。
    完。
    若 名称 等于 「孰料」：
        返还 标记_连接词。
    完。
    若 名称 等于 「讵料」：
        返还 标记_连接词。
    完。
    若 名称 等于 「果不其然」：
        返还 标记_连接词。
    完。
    若 名称 等于 「却说」：
        返还 标记_连接词。
    完。
    若 名称 等于 「话说」：
        返还 标记_连接词。
    完。
    若 名称 等于 「且说」：
        返还 标记_连接词。
    完。
    若 名称 等于 「再说」：
        返还 标记_连接词。
    完。
    若 名称 等于 「又说」：
        返还 标记_连接词。
    完。
    若 名称 等于 「如此说来」：
        返还 标记_连接词。
    完。
    若 名称 等于 「由此可见」：
        返还 标记_连接词。
    完。
    若 名称 等于 「据此」：
        返还 标记_连接词。
    完。
    若 名称 等于 「有鉴于此」：
        返还 标记_连接词。
    完。
    若 名称 等于 「那么」：
        返还 标记_连接词。
    完。
    若 名称 等于 「如此」：
        返还 标记_连接词。
    完。
    若 名称 等于 「这般」：
        返还 标记_连接词。
    完。
    若 名称 等于 「那般」：
        返还 标记_连接词。
    完。
    若 名称 等于 「如是」：
        返还 标记_连接词。
    完。
    若 名称 等于 「并非」：
        返还 标记_非。
    完。
    若 名称 等于 「绝非」：
        返还 标记_非。
    完。
    若 名称 等于 「非是」：
        返还 标记_非。
    完。
    若 名称 等于 「断非」：
        返还 标记_非。
    完。
    若 名称 等于 「决非」：
        返还 标记_非。
    完。
    若 名称 等于 「未必」：
        返还 标记_非。
    完。
    若 名称 等于 「莫」：
        返还 标记_非。
    完。
    若 名称 等于 「勿」：
        返还 标记_非。
    完。
    若 名称 等于 「毋」：
        返还 标记_非。
    完。
    若 名称 等于 「休」：
        返还 标记_非。
    完。
    若 名称 等于 「起初」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「紧接着」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「随即」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「之后」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「后来」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「终于」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「末了」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「与此同时」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「进而」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「从而」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「直至」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「乃至」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「待到」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「旋即」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「既而」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「尔后」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「俄而」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「忽而」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「转而」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「至此」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「话分两头」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「且不说」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「单说」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「先说」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「先且不表」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「暂且不提」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「容后再叙」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「下回分解」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「言归正传」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「闲话少叙」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「长话短说」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「书归正传」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「方才」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「适才」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「彼时」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「此刻」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「当下」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「未几」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「须臾」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「顷刻」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「刹那」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「霎时」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「翌日」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「次日」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「后日」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「即刻」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「立时」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「不等於」：
        返还 标记_不等。
    完。
    若 名称 等于 「各異」：
        返还 标记_不等。
    完。
    若 名称 等于 「有別」：
        返还 标记_不等。
    完。
    若 名称 等于 「有異」：
        返还 标记_不等。
    完。
    若 名称 等于 「殊異」：
        返还 标记_不等。
    完。
    若 名称 等于 「相異」：
        返还 标记_不等。
    完。
    若 名称 等于 「迥異」：
        返还 标记_不等。
    完。
    若 名称 等于 「不僅」：
        返还 标记_且。
    完。
    若 名称 等于 「並且」：
        返还 标记_且。
    完。
    若 名称 等于 「何況」：
        返还 标记_且。
    完。
    若 名称 等于 「況且」：
        返还 标记_且。
    完。
    若 名称 等于 「與」：
        返还 标记_且。
    完。
    若 名称 等于 「連同」：
        返还 标记_且。
    完。
    若 名称 等于 「中斷」：
        返还 标记_中断。
    完。
    若 名称 等于 「打斷」：
        返还 标记_中断。
    完。
    若 名称 等于 「終了」：
        返还 标记_中断。
    完。
    若 名称 等于 「脫離」：
        返还 标记_中断。
    完。
    若 名称 等于 「函數」：
        返还 标记_函数。
    完。
    若 名称 等于 「口訣」：
        返还 标记_函数。
    完。
    若 名称 等于 「法則」：
        返还 标记_函数。
    完。
    若 名称 等于 「法門」：
        返还 标记_函数。
    完。
    若 名称 等于 「秘訣」：
        返还 标记_函数。
    完。
    若 名称 等于 「竅門」：
        返还 标记_函数。
    完。
    若 名称 等于 「筆法」：
        返还 标记_函数。
    完。
    若 名称 等于 「要訣」：
        返还 标记_函数。
    完。
    若 名称 等于 「訣」：
        返还 标记_函数。
    完。
    若 名称 等于 「可變」：
        返还 标记_可变。
    完。
    若 名称 等于 「不則」：
        返还 标记_否则。
    完。
    若 名称 等于 「不然的話」：
        返还 标记_否则。
    完。
    若 名称 等于 「另則」：
        返还 标记_否则。
    完。
    若 名称 等于 「否則」：
        返还 标记_否则。
    完。
    若 名称 等于 「否則便」：
        返还 标记_否则。
    完。
    若 名称 等于 「否則就」：
        返还 标记_否则。
    完。
    若 名称 等于 「否則的話」：
        返还 标记_否则。
    完。
    若 名称 等于 「與此相反」：
        返还 标记_否则。
    完。
    若 名称 等于 「優於」：
        返还 标记_大于。
    完。
    若 名称 等于 「勝過」：
        返还 标记_大于。
    完。
    若 名称 等于 「多於」：
        返还 标记_大于。
    完。
    若 名称 等于 「大於」：
        返还 标记_大于。
    完。
    若 名称 等于 「甚於」：
        返还 标记_大于。
    完。
    若 名称 等于 「超過」：
        返还 标记_大于。
    完。
    若 名称 等于 「過」：
        返还 标记_大于。
    完。
    若 名称 等于 「高於」：
        返还 标记_大于。
    完。
    若 名称 等于 「不下於」：
        返还 标记_大等。
    完。
    若 名称 等于 「不亞於」：
        返还 标记_大等。
    完。
    若 名称 等于 「不低於」：
        返还 标记_大等。
    完。
    若 名称 等于 「不少於」：
        返还 标记_大等。
    完。
    若 名称 等于 「大於等於」：
        返还 标记_大等。
    完。
    若 名称 等于 「起碼」：
        返还 标记_大等。
    完。
    若 名称 等于 「假設」：
        返还 标记_如果。
    完。
    若 名称 等于 「必須」：
        返还 标记_如果。
    完。
    若 名称 等于 「每當」：
        返还 标记_如果。
    完。
    若 名称 等于 「當真」：
        返还 标记_如果。
    完。
    若 名称 等于 「確實」：
        返还 标记_如果。
    完。
    若 名称 等于 「縱使」：
        返还 标记_如果。
    完。
    若 名称 等于 「縱然」：
        返还 标记_如果。
    完。
    若 名称 等于 「脫若」：
        返还 标记_如果。
    完。
    若 名称 等于 「萬一」：
        返还 标记_如果。
    完。
    若 名称 等于 「萬若」：
        返还 标记_如果。
    完。
    若 名称 等于 「設令」：
        返还 标记_如果。
    完。
    若 名称 等于 「設使」：
        返还 标记_如果。
    完。
    若 名称 等于 「設或」：
        返还 标记_如果。
    完。
    若 名称 等于 「設若」：
        返还 标记_如果。
    完。
    若 名称 等于 「誠如」：
        返还 标记_如果。
    完。
    若 名称 等于 「誠然」：
        返还 标记_如果。
    完。
    若 名称 等于 「鑒於」：
        返还 标记_如果。
    完。
    若 名称 等于 「雖然」：
        返还 标记_如果。
    完。
    若 名称 等于 「定義」：
        返还 标记_定义。
    完。
    若 名称 等于 「稱为」：
        返还 标记_定义。
    完。
    若 名称 等于 「規定」：
        返还 标记_定义。
    完。
    若 名称 等于 「記为」：
        返还 标记_定义。
    完。
    若 名称 等于 「設定」：
        返还 标记_定义。
    完。
    若 名称 等于 「賦名」：
        返还 标记_定义。
    完。
    若 名称 等于 「題为」：
        返还 标记_定义。
    完。
    若 名称 等于 「導入」：
        返还 标记_导入。
    完。
    若 名称 等于 「亞於」：
        返还 标记_小于。
    完。
    若 名称 等于 「低於」：
        返还 标记_小于。
    完。
    若 名称 等于 「劣於」：
        返还 标记_小于。
    完。
    若 名称 等于 「小於」：
        返还 标记_小于。
    完。
    若 名称 等于 「少於」：
        返还 标记_小于。
    完。
    若 名称 等于 「弱於」：
        返还 标记_小于。
    完。
    若 名称 等于 「欠於」：
        返还 标记_小于。
    完。
    若 名称 等于 「次於」：
        返还 标记_小于。
    完。
    若 名称 等于 「逊於」：
        返还 标记_小于。
    完。
    若 名称 等于 「不多於」：
        返还 标记_小等。
    完。
    若 名称 等于 「不超過」：
        返还 标记_小等。
    完。
    若 名称 等于 「不高於」：
        返还 标记_小等。
    完。
    若 名称 等于 「小於等於」：
        返还 标记_小等。
    完。
    若 名称 等于 「且試」：
        返还 标记_尝试。
    完。
    若 名称 等于 「嘗試」：
        返还 标记_尝试。
    完。
    若 名称 等于 「試」：
        返还 标记_尝试。
    完。
    若 名称 等于 「試圖」：
        返还 标记_尝试。
    完。
    若 名称 等于 「試探」：
        返还 标记_尝试。
    完。
    若 名称 等于 「試驗」：
        返还 标记_尝试。
    完。
    若 名称 等于 「不斷」：
        返还 标记_当。
    完。
    若 名称 等于 「反複」：
        返还 标记_当。
    完。
    若 名称 等于 「周而複始」：
        返还 标记_当。
    完。
    若 名称 等于 「始終」：
        返还 标记_当。
    完。
    若 名称 等于 「往複」：
        返还 标记_当。
    完。
    若 名称 等于 「循而往複」：
        返还 标记_当。
    完。
    若 名称 等于 「持續」：
        返还 标记_当。
    完。
    若 名称 等于 「當」：
        返还 标记_当。
    完。
    若 名称 等于 「盤桓」：
        返还 标记_当。
    完。
    若 名称 等于 「重複」：
        返还 标记_当。
    完。
    若 名称 等于 「循環」：
        返还 标记_循环。
    完。
    若 名称 等于 「挨個」：
        返还 标记_循环。
    完。
    若 名称 等于 「歷遍」：
        返还 标记_循环。
    完。
    若 名称 等于 「每個」：
        返还 标记_循环。
    完。
    若 名称 等于 「輪流」：
        返还 标记_循环。
    完。
    若 名称 等于 「逐個」：
        返还 标记_循环。
    完。
    若 名称 等于 「逐項」：
        返还 标记_循环。
    完。
    若 名称 等于 「拋」：
        返还 标记_抛出。
    完。
    若 名称 等于 「拋出」：
        返还 标记_抛出。
    完。
    若 名称 等于 「判斷」：
        返还 标记_标识符。
    完。
    若 名称 等于 「匯聚」：
        返还 标记_标识符。
    完。
    若 名称 等于 「參透」：
        返还 标记_标识符。
    完。
    若 名称 等于 「品鑒」：
        返还 标记_标识符。
    完。
    若 名称 等于 「察覺」：
        返还 标记_标识符。
    完。
    若 名称 等于 「審视」：
        返还 标记_标识符。
    完。
    若 名称 等于 「審阅」：
        返还 标记_标识符。
    完。
    若 名称 等于 「推斷」：
        返还 标记_标识符。
    完。
    若 名称 等于 「改進」：
        返还 标记_标识符。
    完。
    若 名称 等于 「權衡」：
        返还 标记_标识符。
    完。
    若 名称 等于 「歸纳」：
        返还 标记_标识符。
    完。
    若 名称 等于 「熔鑄」：
        返还 标记_标识符。
    完。
    若 名称 等于 「甄別」：
        返还 标记_标识符。
    完。
    若 名称 等于 「發現」：
        返还 标记_标识符。
    完。
    若 名称 等于 「研讀」：
        返还 标记_标识符。
    完。
    若 名称 等于 「確認」：
        返还 标记_标识符。
    完。
    若 名称 等于 「維护」：
        返还 标记_标识符。
    完。
    若 名称 等于 「總結」：
        返还 标记_标识符。
    完。
    若 名称 等于 「覺察」：
        返还 标记_标识符。
    完。
    若 名称 等于 「解構」：
        返还 标记_标识符。
    完。
    若 名称 等于 「觸發」：
        返还 标记_标识符。
    完。
    若 名称 等于 「記住」：
        返还 标记_标识符。
    完。
    若 名称 等于 「記忆」：
        返还 标记_标识符。
    完。
    若 名称 等于 「認知」：
        返还 标记_标识符。
    完。
    若 名称 等于 「諳熟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「賞析」：
        返还 标记_标识符。
    完。
    若 名称 等于 「轉換」：
        返还 标记_标识符。
    完。
    若 名称 等于 「辨識」：
        返还 标记_标识符。
    完。
    若 名称 等于 「進化」：
        返还 标记_标识符。
    完。
    若 名称 等于 「重構」：
        返还 标记_标识符。
    完。
    若 名称 等于 「錘炼」：
        返还 标记_标识符。
    完。
    若 名称 等于 「鍛造」：
        返还 标记_标识符。
    完。
    若 名称 等于 「鑄造」：
        返还 标记_标识符。
    完。
    若 名称 等于 「鑒賞」：
        返还 标记_标识符。
    完。
    若 名称 等于 「開悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「關注」：
        返还 标记_标识符。
    完。
    若 名称 等于 「靜思」：
        返还 标记_标识符。
    完。
    若 名称 等于 「頓悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「類比」：
        返还 标记_标识符。
    完。
    若 名称 等于 「顯示」：
        返还 标记_标识符。
    完。
    若 名称 等于 「體悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「匯編」：
        返还 标记_汇编。
    完。
    若 名称 等于 「等於」：
        返还 标记_等等。
    完。
    若 名称 等于 「組建」：
        返还 标记_结构。
    完。
    若 名称 等于 「結構」：
        返还 标记_结构。
    完。
    若 名称 等于 「體制」：
        返还 标记_结构。
    完。
    若 名称 等于 「延續」：
        返还 标记_继续。
    完。
    若 名称 等于 「接續」：
        返还 标记_继续。
    完。
    若 名称 等于 「略過」：
        返还 标记_继续。
    完。
    若 名称 等于 「續」：
        返还 标记_继续。
    完。
    若 名称 等于 「續之」：
        返还 标记_继续。
    完。
    若 名称 等于 「續行」：
        返还 标记_继续。
    完。
    若 名称 等于 「继續」：
        返还 标记_继续。
    完。
    若 名称 等于 「越過」：
        返还 标记_继续。
    完。
    若 名称 等于 「傳回」：
        返还 标记_返回。
    完。
    若 名称 等于 「呈遞」：
        返还 标记_返回。
    完。
    若 名称 等于 「回傳」：
        返还 标记_返回。
    完。
    若 名称 等于 「回稟」：
        返还 标记_返回。
    完。
    若 名称 等于 「帶回」：
        返还 标记_返回。
    完。
    若 名称 等于 「歸还」：
        返还 标记_返回。
    完。
    若 名称 等于 「獻上」：
        返还 标记_返回。
    完。
    若 名称 等于 「產生」：
        返还 标记_返回。
    完。
    若 名称 等于 「稟报」：
        返还 标记_返回。
    完。
    若 名称 等于 「繳回」：
        返还 标记_返回。
    完。
    若 名称 等于 「遞回」：
        返还 标记_返回。
    完。
    若 名称 等于 「不過」：
        返还 标记_连接词。
    完。
    若 名称 等于 「則」：
        返还 标记_连接词。
    完。
    若 名称 等于 「如此说來」：
        返还 标记_连接词。
    完。
    若 名称 等于 「換言之」：
        返还 标记_连接词。
    完。
    若 名称 等于 「據此」：
        返还 标记_连接词。
    完。
    若 名称 等于 「有鑒於此」：
        返还 标记_连接词。
    完。
    若 名称 等于 「綜上」：
        返还 标记_连接词。
    完。
    若 名称 等于 「總之」：
        返还 标记_连接词。
    完。
    若 名称 等于 「詎料」：
        返还 标记_连接词。
    完。
    若 名称 等于 「話说」：
        返还 标记_连接词。
    完。
    若 名称 等于 「這般」：
        返还 标记_连接词。
    完。
    若 名称 等于 「並非」：
        返还 标记_非。
    完。
    若 名称 等于 「斷非」：
        返还 标记_非。
    完。
    若 名称 等于 「決非」：
        返还 标记_非。
    完。
    若 名称 等于 「絕非」：
        返还 标记_非。
    完。
    若 名称 等于 「后來」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「單说」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「容后再敘」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「從而」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「於是」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「暫且不提」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「書歸正傳」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「未幾」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「爾后」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「當下」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「終於」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「緊接着」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「與此同时」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「言歸正傳」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「話分兩头」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「轉而」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「進而」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「長話短说」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「閒話少敘」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「隨即」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「隨后」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「頃刻」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「須臾」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「默認」：
        返还 标记_默认。
    完。
    若 名称 等于 「不若」：
        返还 标记_不等。
    完。
    若 名称 等于 「异于」：
        返还 标记_不等。
    完。
    若 名称 等于 「非同」：
        返还 标记_不等。
    完。
    若 名称 等于 「兼之」：
        返还 标记_且。
    完。
    若 名称 等于 「及」：
        返还 标记_且。
    完。
    若 名称 等于 「并与」：
        返还 标记_且。
    完。
    若 名称 等于 「休矣」：
        返还 标记_中断。
    完。
    若 名称 等于 「免」：
        返还 标记_中断。
    完。
    若 名称 等于 「已矣」：
        返还 标记_中断。
    完。
    若 名称 等于 「罢」：
        返还 标记_中断。
    完。
    若 名称 等于 「技」：
        返还 标记_函数。
    完。
    若 名称 等于 「术」：
        返还 标记_函数。
    完。
    若 名称 等于 「机」：
        返还 标记_函数。
    完。
    若 名称 等于 「略」：
        返还 标记_函数。
    完。
    若 名称 等于 「策」：
        返还 标记_函数。
    完。
    若 名称 等于 「谋」：
        返还 标记_函数。
    完。
    若 名称 等于 「不尔」：
        返还 标记_否则。
    完。
    若 名称 等于 「不然则」：
        返还 标记_否则。
    完。
    若 名称 等于 「不然者」：
        返还 标记_否则。
    完。
    若 名称 等于 「其不然」：
        返还 标记_否则。
    完。
    若 名称 等于 「否者」：
        返还 标记_否则。
    完。
    若 名称 等于 「异则」：
        返还 标记_否则。
    完。
    若 名称 等于 「若不尔者」：
        返还 标记_否则。
    完。
    若 名称 等于 「过于」：
        返还 标记_大于。
    完。
    若 名称 等于 「逾于」：
        返还 标记_大于。
    完。
    若 名称 等于 「令」：
        返还 标记_如果。
    完。
    若 名称 等于 「使然」：
        返还 标记_如果。
    完。
    若 名称 等于 「倘如」：
        返还 标记_如果。
    完。
    若 名称 等于 「借如」：
        返还 标记_如果。
    完。
    若 名称 等于 「假使」：
        返还 标记_如果。
    完。
    若 名称 等于 「其若」：
        返还 标记_如果。
    完。
    若 名称 等于 「如使」：
        返还 标记_如果。
    完。
    若 名称 等于 「比如」：
        返还 标记_如果。
    完。
    若 名称 等于 「第若」：
        返还 标记_如果。
    完。
    若 名称 等于 「脱」：
        返还 标记_如果。
    完。
    若 名称 等于 「若乃」：
        返还 标记_如果。
    完。
    若 名称 等于 「若其」：
        返还 标记_如果。
    完。
    若 名称 等于 「若复」：
        返还 标记_如果。
    完。
    若 名称 等于 「设如」：
        返还 标记_如果。
    完。
    若 名称 等于 「设或者」：
        返还 标记_如果。
    完。
    若 名称 等于 「号之」：
        返还 标记_定义。
    完。
    若 名称 等于 「号之曰」：
        返还 标记_定义。
    完。
    若 名称 等于 「名之」：
        返还 标记_定义。
    完。
    若 名称 等于 「名之曰」：
        返还 标记_定义。
    完。
    若 名称 等于 「呼之」：
        返还 标记_定义。
    完。
    若 名称 等于 「命之」：
        返还 标记_定义。
    完。
    若 名称 等于 「字之」：
        返还 标记_定义。
    完。
    若 名称 等于 「封之」：
        返还 标记_定义。
    完。
    若 名称 等于 「曰」：
        返还 标记_定义。
    完。
    若 名称 等于 「称之」：
        返还 标记_定义。
    完。
    若 名称 等于 「谓」：
        返还 标记_定义。
    完。
    若 名称 等于 「用」：
        返还 标记_导入。
    完。
    若 名称 等于 「采」：
        返还 标记_导入。
    完。
    若 名称 等于 「不及」：
        返还 标记_小于。
    完。
    若 名称 等于 「不逮」：
        返还 标记_小于。
    完。
    若 名称 等于 「弗如」：
        返还 标记_小于。
    完。
    若 名称 等于 「姑」：
        返还 标记_尝试。
    完。
    若 名称 等于 「聊」：
        返还 标记_尝试。
    完。
    若 名称 等于 「三复」：
        返还 标记_当。
    完。
    若 名称 等于 「再三」：
        返还 标记_当。
    完。
    若 名称 等于 「屡」：
        返还 标记_当。
    完。
    若 名称 等于 「每每」：
        返还 标记_当。
    完。
    若 名称 等于 「历」：
        返还 标记_循环。
    完。
    若 名称 等于 「周」：
        返还 标记_循环。
    完。
    若 名称 等于 「遍」：
        返还 标记_循环。
    完。
    若 名称 等于 「其或」：
        返还 标记_或。
    完。
    若 名称 等于 「将」：
        返还 标记_或。
    完。
    若 名称 等于 「抑」：
        返还 标记_或。
    完。
    若 名称 等于 「投」：
        返还 标记_抛出。
    完。
    若 名称 等于 「掷」：
        返还 标记_抛出。
    完。
    若 名称 等于 「决」：
        返还 标记_标识符。
    完。
    若 名称 等于 「划」：
        返还 标记_标识符。
    完。
    若 名称 等于 「叩」：
        返还 标记_标识符。
    完。
    若 名称 等于 「定」：
        返还 标记_标识符。
    完。
    若 名称 等于 「审」：
        返还 标记_标识符。
    完。
    若 名称 等于 「察」：
        返还 标记_标识符。
    完。
    若 名称 等于 「寻」：
        返还 标记_标识符。
    完。
    若 名称 等于 「彻」：
        返还 标记_标识符。
    完。
    若 名称 等于 「忖」：
        返还 标记_标识符。
    完。
    若 名称 等于 「悉」：
        返还 标记_标识符。
    完。
    若 名称 等于 「悟」：
        返还 标记_标识符。
    完。
    若 名称 等于 「探」：
        返还 标记_标识符。
    完。
    若 名称 等于 「揣」：
        返还 标记_标识符。
    完。
    若 名称 等于 「断」：
        返还 标记_标识符。
    完。
    若 名称 等于 「明」：
        返还 标记_标识符。
    完。
    若 名称 等于 「晓」：
        返还 标记_标识符。
    完。
    若 名称 等于 「晰」：
        返还 标记_标识符。
    完。
    若 名称 等于 「榷」：
        返还 标记_标识符。
    完。
    若 名称 等于 「求」：
        返还 标记_标识符。
    完。
    若 名称 等于 「炼」：
        返还 标记_标识符。
    完。
    若 名称 等于 「琢」：
        返还 标记_标识符。
    完。
    若 名称 等于 「砺」：
        返还 标记_标识符。
    完。
    若 名称 等于 「磨」：
        返还 标记_标识符。
    完。
    若 名称 等于 「究」：
        返还 标记_标识符。
    完。
    若 名称 等于 「穷」：
        返还 标记_标识符。
    完。
    若 名称 等于 「筹」：
        返还 标记_标识符。
    完。
    若 名称 等于 「索」：
        返还 标记_标识符。
    完。
    若 名称 等于 「衡」：
        返还 标记_标识符。
    完。
    若 名称 等于 「裁」：
        返还 标记_标识符。
    完。
    若 名称 等于 「觅」：
        返还 标记_标识符。
    完。
    若 名称 等于 「议」：
        返还 标记_标识符。
    完。
    若 名称 等于 「论」：
        返还 标记_标识符。
    完。
    若 名称 等于 「访」：
        返还 标记_标识符。
    完。
    若 名称 等于 「诘」：
        返还 标记_标识符。
    完。
    若 名称 等于 「询」：
        返还 标记_标识符。
    完。
    若 名称 等于 「谕」：
        返还 标记_标识符。
    完。
    若 名称 等于 「谙」：
        返还 标记_标识符。
    完。
    若 名称 等于 「质」：
        返还 标记_标识符。
    完。
    若 名称 等于 「辨」：
        返还 标记_标识符。
    完。
    若 名称 等于 「辩」：
        返还 标记_标识符。
    完。
    若 名称 等于 「达」：
        返还 标记_标识符。
    完。
    若 名称 等于 「透」：
        返还 标记_标识符。
    完。
    若 名称 等于 「通」：
        返还 标记_标识符。
    完。
    若 名称 等于 「酌」：
        返还 标记_标识符。
    完。
    若 名称 等于 「量」：
        返还 标记_标识符。
    完。
    若 名称 等于 「鉴」：
        返还 标记_标识符。
    完。
    若 名称 等于 「铸」：
        返还 标记_标识符。
    完。
    若 名称 等于 「锻」：
        返还 标记_标识符。
    完。
    若 名称 等于 「问」：
        返还 标记_标识符。
    完。
    若 名称 等于 「同于」：
        返还 标记_等等。
    完。
    若 名称 等于 「如一」：
        返还 标记_等等。
    完。
    若 名称 等于 「无异」：
        返还 标记_等等。
    完。
    若 名称 等于 「相若」：
        返还 标记_等等。
    完。
    若 名称 等于 「若等」：
        返还 标记_等等。
    完。
    若 名称 等于 「再」：
        返还 标记_继续。
    完。
    若 名称 等于 「复」：
        返还 标记_继续。
    完。
    若 名称 等于 「因得」：
        返还 标记_返回。
    完。
    若 名称 等于 「复之」：
        返还 标记_返回。
    完。
    若 名称 等于 「归之」：
        返还 标记_返回。
    完。
    若 名称 等于 「得之」：
        返还 标记_返回。
    完。
    若 名称 等于 「报之」：
        返还 标记_返回。
    完。
    若 名称 等于 「是以得」：
        返还 标记_返回。
    完。
    若 名称 等于 「纳之」：
        返还 标记_返回。
    完。
    若 名称 等于 「致之」：
        返还 标记_返回。
    完。
    若 名称 等于 「获之」：
        返还 标记_返回。
    完。
    若 名称 等于 「还之」：
        返还 标记_返回。
    完。
    若 名称 等于 「不意」：
        返还 标记_连接词。
    完。
    若 名称 等于 「且夫」：
        返还 标记_连接词。
    完。
    若 名称 等于 「乃」：
        返还 标记_连接词。
    完。
    若 名称 等于 「以故」：
        返还 标记_连接词。
    完。
    若 名称 等于 「以是」：
        返还 标记_连接词。
    完。
    若 名称 等于 「卒然」：
        返还 标记_连接词。
    完。
    若 名称 等于 「夫」：
        返还 标记_连接词。
    完。
    若 名称 等于 「孰知」：
        返还 标记_连接词。
    完。
    若 名称 等于 「惟」：
        返还 标记_连接词。
    完。
    若 名称 等于 「故」：
        返还 标记_连接词。
    完。
    若 名称 等于 「是以」：
        返还 标记_连接词。
    完。
    若 名称 等于 「是故」：
        返还 标记_连接词。
    完。
    若 名称 等于 「然」：
        返还 标记_连接词。
    完。
    若 名称 等于 「然则」：
        返还 标记_连接词。
    完。
    若 名称 等于 「特」：
        返还 标记_连接词。
    完。
    若 名称 等于 「独」：
        返还 标记_连接词。
    完。
    若 名称 等于 「由是」：
        返还 标记_连接词。
    完。
    若 名称 等于 「盖」：
        返还 标记_连接词。
    完。
    若 名称 等于 「竟而」：
        返还 标记_连接词。
    完。
    若 名称 等于 「缘此」：
        返还 标记_连接词。
    完。
    若 名称 等于 「遽而」：
        返还 标记_连接词。
    完。
    若 名称 等于 「岂非」：
        返还 标记_非。
    完。
    若 名称 等于 「弗」：
        返还 标记_非。
    完。
    若 名称 等于 「无乃」：
        返还 标记_非。
    完。
    若 名称 等于 「未尝」：
        返还 标记_非。
    完。
    若 名称 等于 「莫非」：
        返还 标记_非。
    完。
    若 名称 等于 「非乃」：
        返还 标记_非。
    完。
    若 名称 等于 「乃复」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「俄」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「先是」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「初」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「卒」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「始」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「少焉」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「少顷」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「已」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「已而」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「旋」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「有顷」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「比及」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「毕」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「甫」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「竟」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「讫」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「迄」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「适」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「遂」：
        返还 标记_顺序词。
    完。
    若 名称 等于 「顷之」：
        返还 标记_顺序词。
    完。
    返还 0。
完。

_UTF8码点 接受 串、偏移：
    b0 为 做 字节取 用 串、偏移。
    若 b0 小于 128：
        返还 b0 乘 65536 加 1。
    完。
    若 b0 小于 224：
        b1 为 做 字节取 用 串、偏移 加 1。
        返还 ((b0 减 192) 乘 64 加 (b1 减 128)) 乘 65536 加 2。
    完。
    若 b0 小于 240：
        b1 为 做 字节取 用 串、偏移 加 1。
        b2 为 做 字节取 用 串、偏移 加 2。
        返还 ((b0 减 224) 乘 4096 加 (b1 减 128) 乘 64 加 (b2 减 128)) 乘 65536 加 3。
    完。
    b1 为 做 字节取 用 串、偏移 加 1。
    b2 为 做 字节取 用 串、偏移 加 2。
    b3 为 做 字节取 用 串、偏移 加 3。
    返还 ((b0 减 240) 乘 262144 加 (b1 减 128) 乘 4096 加 (b2 减 128) 乘 64 加 (b3 减 128)) 乘 65536 加 4。
完。

_UTF8取码点 接受 编码值：
    返还 编码值 除以 65536。
完。

_UTF8取字节数 接受 编码值：
    返还 编码值 减 编码值 除以 65536 乘 65536。
完。

_是中文数字码点 接受 cp：
    返还 cp 等于 中文码_一 或者 cp 等于 中文码_二 或者 cp 等于 中文码_三 或者 cp 等于 中文码_四 或者 cp 等于 中文码_五 或者 cp 等于 中文码_六 或者 cp 等于 中文码_七 或者 cp 等于 中文码_八 或者 cp 等于 中文码_九 或者 cp 等于 中文码_零 或者 cp 等于 中文码_零 或者 cp 等于 中文码_两 或者 cp 等于 中文码_十 或者 cp 等于 中文码_百 或者 cp 等于 中文码_千 或者 cp 等于 中文码_万 或者 cp 等于 中文码_亿。
完。

_中文数字码点值 接受 cp：
    若 cp 等于 中文码_零 或者 cp 等于 中文码_零：
        返还 0。
    完。
    若 cp 等于 中文码_一：
        返还 1。
    完。
    若 cp 等于 中文码_二 或者 cp 等于 中文码_两：
        返还 2。
    完。
    若 cp 等于 中文码_三：
        返还 3。
    完。
    若 cp 等于 中文码_四：
        返还 4。
    完。
    若 cp 等于 中文码_五：
        返还 5。
    完。
    若 cp 等于 中文码_六：
        返还 6。
    完。
    若 cp 等于 中文码_七：
        返还 7。
    完。
    若 cp 等于 中文码_八：
        返还 8。
    完。
    若 cp 等于 中文码_九：
        返还 9。
    完。
    返还 0 减 1。
完。

_是中文数字单位码点 接受 cp：
    返还 cp 等于 中文码_十 或者 cp 等于 中文码_百 或者 cp 等于 中文码_千 或者 cp 等于 中文码_万 或者 cp 等于 中文码_亿。
完。

_解析中文数字 接受 名称：
    字节长 为 做 字节长度 用 名称。
    若 字节长 等于 0：
        返还 0 减 1。
    完。
    首编码 为 做 _UTF8码点 用 名称、0。
    首码点 为 做 _UTF8取码点 用 首编码。
    若 做 _UTF8取字节数 用 首编码 等于 字节长 并且 做 _是中文数字单位码点 用 首码点：
        返还 0 减 1。
    完。
    可变 偏移 为 0。
    只要 偏移 小于 字节长：
        编码值 为 做 _UTF8码点 用 名称、偏移。
        cp 为 做 _UTF8取码点 用 编码值。
        步长 为 做 _UTF8取字节数 用 编码值。
        若 做 _是中文数字码点 用 cp 等于 假：
            返还 0 减 1。
        完。
        偏移 为 偏移 加 步长。
    完。
    可变 结果值 为 0。
    可变 当前值 为 0。
    可变 段值 为 0。
    可变 首字符 为 1。
    偏移 为 0。
    只要 偏移 小于 字节长：
        编码值 为 做 _UTF8码点 用 名称、偏移。
        cp 为 做 _UTF8取码点 用 编码值。
        步长 为 做 _UTF8取字节数 用 编码值。
        d 为 做 _中文数字码点值 用 cp。
        若 d 大于或等于 0：
            当前值 为 d。
        否则若 cp 等于 中文码_十：
            若 当前值 等于 0 并且 段值 等于 0 并且 首字符 等于 1：
                当前值 为 1。
            完。
            段值 为 段值 加 当前值 乘 10。
            当前值 为 0。
        否则若 cp 等于 中文码_百：
            若 当前值 等于 0 并且 段值 等于 0：
                当前值 为 1。
            完。
            段值 为 段值 加 当前值 乘 100。
            当前值 为 0。
        否则若 cp 等于 中文码_千：
            若 当前值 等于 0 并且 段值 等于 0：
                当前值 为 1。
            完。
            段值 为 段值 加 当前值 乘 1000。
            当前值 为 0。
        否则若 cp 等于 中文码_万：
            段值 为 段值 加 当前值。
            结果值 为 (结果值 加 段值) 乘 10000。
            段值 为 0。
            当前值 为 0。
        否则若 cp 等于 中文码_亿：
            段值 为 段值 加 当前值。
            结果值 为 (结果值 加 段值) 乘 100000000。
            段值 为 0。
            当前值 为 0。
        完。
        首字符 为 0。
        偏移 为 偏移 加 步长。
    完。
    返还 结果值 加 段值 加 当前值。
完。

_词法_运算符 接受 标记列、c、c2、行、列：
    若 c 等于 字符_等号 并且 c2 等于 字符_等号：
        做 追加 用 标记列、一组：标记_等等、0、行、列。
        返还 2。
    完。
    若 c 等于 字符_叹号 并且 c2 等于 字符_等号：
        做 追加 用 标记列、一组：标记_不等、0、行、列。
        返还 2。
    完。
    若 c 等于 字符_小于号 并且 c2 等于 字符_等号：
        做 追加 用 标记列、一组：标记_小等、0、行、列。
        返还 2。
    完。
    若 c 等于 字符_大于号 并且 c2 等于 字符_等号：
        做 追加 用 标记列、一组：标记_大等、0、行、列。
        返还 2。
    完。
    若 c 等于 字符_小于号 并且 c2 等于 字符_小于号：
        做 追加 用 标记列、一组：标记_左移、0、行、列。
        返还 2。
    完。
    若 c 等于 字符_大于号 并且 c2 等于 字符_大于号：
        做 追加 用 标记列、一组：标记_右移、0、行、列。
        返还 2。
    完。
    若 c 等于 字符_与号 并且 c2 等于 字符_与号：
        做 追加 用 标记列、一组：标记_且、0、行、列。
        返还 2。
    完。
    若 c 等于 字符_竖线 并且 c2 等于 字符_竖线：
        做 追加 用 标记列、一组：标记_或、0、行、列。
        返还 2。
    完。
    若 c 等于 字符_加号 并且 c2 等于 字符_等号：
        做 追加 用 标记列、一组：标记_加等、0、行、列。
        返还 2。
    完。
    若 c 等于 字符_减号 并且 c2 等于 字符_等号：
        做 追加 用 标记列、一组：标记_减等、0、行、列。
        返还 2。
    完。
    若 c 等于 字符_加号：
        做 追加 用 标记列、一组：标记_加、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_减号：
        做 追加 用 标记列、一组：标记_减、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_星号：
        做 追加 用 标记列、一组：标记_乘、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_斜杠：
        做 追加 用 标记列、一组：标记_除、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_百分号：
        做 追加 用 标记列、一组：标记_余、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_等号：
        做 追加 用 标记列、一组：标记_赋值、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_小于号：
        做 追加 用 标记列、一组：标记_小于、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_大于号：
        做 追加 用 标记列、一组：标记_大于、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_叹号：
        做 追加 用 标记列、一组：标记_非、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_与号：
        做 追加 用 标记列、一组：标记_位与、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_竖线：
        做 追加 用 标记列、一组：标记_位或、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_异或号：
        做 追加 用 标记列、一组：标记_位异或、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_波浪号：
        做 追加 用 标记列、一组：标记_位取反、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_左圆括：
        做 追加 用 标记列、一组：标记_左括、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_右圆括：
        做 追加 用 标记列、一组：标记_右括、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_左花括：
        做 追加 用 标记列、一组：标记_左花、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_右花括：
        做 追加 用 标记列、一组：标记_右花、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_左方括：
        做 追加 用 标记列、一组：标记_左方、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_右方括：
        做 追加 用 标记列、一组：标记_右方、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_逗号：
        做 追加 用 标记列、一组：标记_逗号、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_句点：
        做 追加 用 标记列、一组：标记_点号、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_冒号：
        做 追加 用 标记列、一组：标记_冒号、0、行、列。
        返还 1。
    完。
    若 c 等于 字符_分号：
        做 追加 用 标记列、一组：标记_分号、0、行、列。
        返还 1。
    完。
    返还 0。
完。

_词法_全角码点 接受 标记列、cp、行、列：
    若 cp 等于 中文码_左括：
        做 追加 用 标记列、一组：标记_左括、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_右括：
        做 追加 用 标记列、一组：标记_右括、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_左花：
        做 追加 用 标记列、一组：标记_左花、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_右花：
        做 追加 用 标记列、一组：标记_右花、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_左方：
        做 追加 用 标记列、一组：标记_左方、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_右方：
        做 追加 用 标记列、一组：标记_右方、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_逗号 或者 cp 等于 12289：
        做 追加 用 标记列、一组：标记_逗号、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_冒号：
        做 追加 用 标记列、一组：标记_冒号、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_左书：
        做 追加 用 标记列、一组：标记_左花、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_右书：
        做 追加 用 标记列、一组：标记_右花、0、行、列。
        返还 1。
    完。
    若 cp 等于 12302：
        做 追加 用 标记列、一组：标记_左括、0、行、列。
        返还 1。
    完。
    若 cp 等于 12303：
        做 追加 用 标记列、一组：标记_右括、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_分号：
        做 追加 用 标记列、一组：标记_分号、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_句号：
        做 追加 用 标记列、一组：标记_分号、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_等号：
        做 追加 用 标记列、一组：标记_赋值、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_加号：
        做 追加 用 标记列、一组：标记_加、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_减号：
        做 追加 用 标记列、一组：标记_减、0、行、列。
        返还 1。
    完。
    若 cp 等于 中文码_叹号：
        做 追加 用 标记列、一组：标记_非、0、行、列。
        返还 1。
    完。
    返还 0。
完。

前端_词法分析 接受 源码：
    若 源码 等于 空：
        返还 一组：（空）。
    完。
    可变 标记列 为 一组：（空）。
    长 为 做 计数 用 源码。
    可变 位置 为 0。
    可变 行 为 1。
    可变 列 为 1。
    若 长 大于或等于 3：
        若 做 字节取 用 源码、0 等于 239：
            若 做 字节取 用 源码、1 等于 187：
                若 做 字节取 用 源码、2 等于 191：
                    位置 为 3。
                完。
            完。
        完。
    完。
    只要 位置 小于 长：
        c 为 做 字节取 用 源码、位置。
        可变 已处理 为 0。
        可变 _解码码点 为 0。
        可变 _解码步长 为 1。
        若 已处理 等于 0 并且 (c 等于 字符_空格 或者 c 等于 字符_制表 或者 c 等于 字符_回车)：
            位置 为 位置 加 1。
            列 为 列 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 c 等于 字符_换行：
            位置 为 位置 加 1。
            行 为 行 加 1。
            列 为 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 c 等于 字符_斜杠 并且 位置 加 1 小于 长：
            cn 为 做 字节取 用 源码、位置 加 1。
            若 cn 等于 字符_斜杠：
                位置 为 位置 加 2。
                只要 位置 小于 长 并且 做 字节取 用 源码、位置 不等于 字符_换行：
                    位置 为 位置 加 1。
                完。
                已处理 为 1。
            完。
            若 已处理 等于 0 并且 cn 等于 字符_星号：
                位置 为 位置 加 2。
                可变 深 为 1。
                只要 位置 加 1 小于 长 并且 深 大于 0：
                    bc 为 做 字节取 用 源码、位置。
                    bn 为 做 字节取 用 源码、位置 加 1。
                    若 bc 等于 字符_星号 并且 bn 等于 字符_斜杠：
                        深 为 深 减 1。
                        位置 为 位置 加 2。
                    否则若 bc 等于 字符_斜杠 并且 bn 等于 字符_星号：
                        深 为 深 加 1。
                        位置 为 位置 加 2。
                    否则：
                        若 bc 等于 字符_换行：
                            行 为 行 加 1。
                            列 为 0。
                        完。
                        位置 为 位置 加 1。
                        列 为 列 加 1。
                    完。
                完。
                已处理 为 1。
            完。
        完。
        若 已处理 等于 0 并且 c 大于 127：
            _解码 为 做 _UTF8码点 用 源码、位置。
            _cp 为 做 _UTF8取码点 用 _解码。
            _步 为 做 _UTF8取字节数 用 _解码。
            _解码码点 为 _cp。
            _解码步长 为 _步。
            若 _cp 等于 12288 或者 _cp 等于 8194：
                位置 为 位置 加 _步。
                列 为 列 加 1。
                已处理 为 1。
            完。
            若 已处理 等于 0 并且 _cp 等于 27880 并且 位置 加 _步 小于 长：
                _下解码 为 做 _UTF8码点 用 源码、位置 加 _步。
                _下cp 为 做 _UTF8取码点 用 _下解码。
                若 _下cp 等于 中文码_冒号 或者 _下cp 等于 58：
                    位置 为 位置 加 _步 加 做 _UTF8取字节数 用 _下解码。
                    只要 位置 小于 长 并且 做 字节取 用 源码、位置 不等于 字符_换行：
                        位置 为 位置 加 1。
                    完。
                    已处理 为 1。
                完。
            完。
            若 已处理 等于 0：
                _标点结果 为 做 _词法_全角码点 用 标记列、_cp、行、列。
                若 _标点结果 大于 0：
                    位置 为 位置 加 _步。
                    列 为 列 加 1。
                    已处理 为 1。
                完。
            完。
            若 已处理 等于 0 并且 (_cp 等于 8220 或者 _cp 等于 8216)：
            完。
        完。
        若 已处理 等于 0 并且 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
            数起行 为 行。
            数起列 为 列。
            可变 值 为 0。
            可变 是十六 为 0。
            若 c 等于 字符_零 并且 位置 加 1 小于 长：
                nx 为 做 字节取 用 源码、位置 加 1。
                若 nx 等于 字符_小写x 或者 nx 等于 字符_大写X：
                    是十六 为 1。
                    位置 为 位置 加 2。
                    列 为 列 加 2。
                    可变 扫hex 为 1。
                    只要 位置 小于 长 并且 扫hex 等于 1：
                        h 为 做 字节取 用 源码、位置。
                        若 h 大于或等于 字符_零 并且 h 小于或等于 字符_九：
                            值 为 值 乘 16 加 h 减 字符_零。
                            位置 为 位置 加 1。
                            列 为 列 加 1。
                        否则若 h 大于或等于 字符_小写a 并且 h 小于或等于 字符_小写f：
                            值 为 值 乘 16 加 h 减 87。
                            位置 为 位置 加 1。
                            列 为 列 加 1。
                        否则若 h 大于或等于 字符_大写A 并且 h 小于或等于 字符_大写F：
                            值 为 值 乘 16 加 h 减 55。
                            位置 为 位置 加 1。
                            列 为 列 加 1。
                        否则：
                            扫hex 为 0。
                        完。
                    完。
                    做 追加 用 标记列、一组：词法_浮点、值、数起行、数起列。
                完。
            完。
            若 是十六 等于 0：
                可变 扫dec 为 1。
                只要 位置 小于 长 并且 扫dec 等于 1：
                    d 为 做 字节取 用 源码、位置。
                    若 d 大于或等于 字符_零 并且 d 小于或等于 字符_九：
                        值 为 值 乘 10 加 d 减 字符_零。
                        位置 为 位置 加 1。
                        列 为 列 加 1。
                    否则：
                        扫dec 为 0。
                    完。
                完。
                若 位置 小于 长 并且 做 字节取 用 源码、位置 等于 字符_句点 并且 位置 加 1 小于 长 并且 做 字节取 用 源码、位置 加 1 大于或等于 字符_零 并且 做 字节取 用 源码、位置 加 1 小于或等于 字符_九：
                    位置 为 位置 加 1。
                    列 为 列 加 1。
                    可变 小数值 为 0。
                    可变 小数位 为 0。
                    可变 扫frac 为 1。
                    只要 位置 小于 长 并且 扫frac 等于 1：
                        fd 为 做 字节取 用 源码、位置。
                        若 fd 大于或等于 字符_零 并且 fd 小于或等于 字符_九：
                            小数值 为 小数值 乘 10 加 fd 减 字符_零。
                            小数位 为 小数位 加 1。
                            位置 为 位置 加 1。
                            列 为 列 加 1。
                        否则：
                            扫frac 为 0。
                        完。
                    完。
                    做 追加 用 标记列、一组：词法_插值串、值、数起行、数起列、小数值、小数位。
                否则：
                    做 追加 用 标记列、一组：词法_整数、值、数起行、数起列。
                完。
            完。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 (c 等于 字符_双引号 或者 _解码码点 等于 8220)：
            串起行 为 行。
            串起列 为 列。
            可变 结束引号 为 34。
            若 _解码码点 等于 8220：
                结束引号 为 8221。
                位置 为 位置 加 _解码步长。
            否则：
                位置 为 位置 加 1。
            完。
            列 为 列 加 1。
            可变 内容 为 「」。
            可变 扫串 为 1。
            只要 位置 小于 长 并且 扫串 等于 1：
                sc 为 做 字节取 用 源码、位置。
                若 结束引号 大于 127 并且 sc 大于 127：
                    _sc解 为 做 _UTF8码点 用 源码、位置。
                    若 做 _UTF8取码点 用 _sc解 等于 结束引号：
                        位置 为 位置 加 做 _UTF8取字节数 用 _sc解。
                        列 为 列 加 1。
                        扫串 为 0。
                    否则：
                        内容 为 内容 加 做 子串 用 源码、位置、1。
                        位置 为 位置 加 1。
                    完。
                否则若 sc 等于 结束引号：
                    位置 为 位置 加 1。
                    列 为 列 加 1。
                    扫串 为 0。
                否则若 sc 等于 字符_反斜杠 并且 位置 加 1 小于 长：
                    转 为 做 字节取 用 源码、位置 加 1。
                    若 转 等于 字符_小写n：
                        内容 为 内容 加 「\n」。
                    否则若 转 等于 字符_小写t：
                        内容 为 内容 加 「\t」。
                    否则若 转 等于 字符_小写r：
                        内容 为 内容 加 「\r」。
                    否则若 转 等于 字符_反斜杠：
                        内容 为 内容 加 「\\」。
                    否则若 转 等于 字符_双引号：
                        内容 为 内容 加 「\"」。
                    否则若 转 等于 字符_零：
                        内容 为 内容 加 「\0」。
                    否则：
                        内容 为 内容 加 做 子串 用 源码、位置 加 1、1。
                    完。
                    位置 为 位置 加 2。
                    列 为 列 加 2。
                否则：
                    内容 为 内容 加 做 子串 用 源码、位置、1。
                    若 sc 等于 字符_换行：
                        行 为 行 加 1。
                        列 为 1。
                    否则：
                        列 为 列 加 1。
                    完。
                    位置 为 位置 加 1。
                完。
            完。
            可变 _稳定内容 为 「」。
            可变 _稳定i 为 0。
            _稳定n 为 做 字节长度 用 内容。
            只要 _稳定i 小于 _稳定n：
                _稳定内容 为 做 字符串拼接 用 _稳定内容、做 字符 用 做 字节取 用 内容、_稳定i。
                _稳定i 为 _稳定i 加 1。
            完。
            做 追加 用 标记列、一组：词法_字串、_稳定内容、串起行、串起列。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 (c 等于 字符_单引号 或者 _解码码点 等于 8216)：
            串起行 为 行。
            串起列 为 列。
            可变 结束引号 为 39。
            若 _解码码点 等于 8216：
                结束引号 为 8217。
                位置 为 位置 加 _解码步长。
            否则：
                位置 为 位置 加 1。
            完。
            列 为 列 加 1。
            可变 内容 为 「」。
            可变 扫串 为 1。
            只要 位置 小于 长 并且 扫串 等于 1：
                sc 为 做 字节取 用 源码、位置。
                若 结束引号 大于 127 并且 sc 大于 127：
                    _sc解 为 做 _UTF8码点 用 源码、位置。
                    若 做 _UTF8取码点 用 _sc解 等于 结束引号：
                        位置 为 位置 加 做 _UTF8取字节数 用 _sc解。
                        列 为 列 加 1。
                        扫串 为 0。
                    否则：
                        内容 为 内容 加 做 子串 用 源码、位置、1。
                        位置 为 位置 加 1。
                    完。
                否则若 sc 等于 结束引号：
                    位置 为 位置 加 1。
                    列 为 列 加 1。
                    扫串 为 0。
                否则若 sc 等于 字符_反斜杠 并且 位置 加 1 小于 长：
                    转 为 做 字节取 用 源码、位置 加 1。
                    若 转 等于 字符_小写n：
                        内容 为 内容 加 「\n」。
                    否则若 转 等于 字符_小写t：
                        内容 为 内容 加 「\t」。
                    否则若 转 等于 字符_反斜杠：
                        内容 为 内容 加 「\\」。
                    否则若 转 等于 字符_单引号：
                        内容 为 内容 加 「\'」。
                    否则：
                        内容 为 内容 加 做 子串 用 源码、位置 加 1、1。
                    完。
                    位置 为 位置 加 2。
                    列 为 列 加 2。
                否则：
                    内容 为 内容 加 做 子串 用 源码、位置、1。
                    若 sc 等于 字符_换行：
                        行 为 行 加 1。
                        列 为 1。
                    否则：
                        列 为 列 加 1。
                    完。
                    位置 为 位置 加 1。
                完。
            完。
            可变 _稳定内容2 为 「」。
            可变 _稳定i2 为 0。
            _稳定n2 为 做 字节长度 用 内容。
            只要 _稳定i2 小于 _稳定n2：
                _稳定内容2 为 做 字符串拼接 用 _稳定内容2、做 字符 用 做 字节取 用 内容、_稳定i2。
                _稳定i2 为 _稳定i2 加 1。
            完。
            做 追加 用 标记列、一组：词法_字串、_稳定内容2、串起行、串起列。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 (c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z 或者 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z 或者 c 等于 字符_下划线 或者 c 大于 127)：
            标起行 为 行。
            标起列 为 列。
            起始 为 位置。
            可变 扫标 为 1。
            只要 位置 小于 长 并且 扫标 等于 1：
                ic 为 做 字节取 用 源码、位置。
                若 ic 大于或等于 字符_大写A 并且 ic 小于或等于 字符_大写Z 或者 ic 大于或等于 字符_小写a 并且 ic 小于或等于 字符_小写z 或者 ic 大于或等于 字符_零 并且 ic 小于或等于 字符_九 或者 ic 等于 字符_下划线 或者 ic 大于 127：
                    位置 为 位置 加 1。
                    列 为 列 加 1。
                否则：
                    扫标 为 0。
                完。
            完。
            名称 为 做 子串 用 源码、起始、位置 减 起始。
            中数 为 做 _解析中文数字 用 名称。
            若 中数 大于或等于 0：
                做 追加 用 标记列、一组：词法_整数、中数、标起行、标起列。
            否则：
                kw 为 做 前端_查关键字 用 名称。
                若 kw 大于 0：
                    做 追加 用 标记列、一组：kw、名称、标起行、标起列。
                否则：
                    做 追加 用 标记列、一组：20、名称、标起行、标起列。
                完。
            完。
            已处理 为 1。
        完。
        若 已处理 等于 0：
            可变 c2 为 0。
            若 位置 加 1 小于 长：
                c2 为 做 字节取 用 源码、位置 加 1。
            完。
            已消费 为 做 _词法_运算符 用 标记列、c、c2、行、列。
            若 已消费 大于 0：
                位置 为 位置 加 已消费。
                列 为 列 加 已消费。
            否则：
                位置 为 位置 加 1。
                列 为 列 加 1。
            完。
        完。
    完。
    做 追加 用 标记列、一组：0、0、行、列。
    返还 标记列。
完。

可变 _解析_标记组 为 一组：（空）。

可变 _解析_位置 为 0。

可变 _解析_长度 为 0。

_语法_结束 接受 零参：
    返还 _解析_位置 大于或等于 _解析_长度。
完。

_语法_当前 接受 零参：
    若 _解析_位置 大于或等于 _解析_长度：
        返还 一组：0、0、0。
    完。
    返还 _解析_标记组 的 第 _解析_位置 项。
完。

_语法_推进 接受 零参：
    标记 为 做 _语法_当前。
    若 _解析_位置 小于 _解析_长度：
        _解析_位置 为 _解析_位置 加 1。
    完。
    返还 标记。
完。

_语法_类型 接受 零参：
    返还 做 _语法_当前 的 第 0 项。
完。

_语法_预览类型 接受 偏移：
    索引 为 _解析_位置 加 偏移。
    若 索引 小于 0 或者 索引 大于或等于 _解析_长度：
        返还 0。
    完。
    返还 _解析_标记组 的 第 索引 项 的 第 0 项。
完。

_语法_期望 接受 期望：
    若 做 _语法_当前 的 第 0 项 等于 期望：
        _解析_位置 为 _解析_位置 加 1。
        返还 1。
    完。
    返还 0。
完。

_标记转运算 接受 t：
    若 t 等于 标记_加：
        返还 「+」。
    完。
    若 t 等于 标记_减：
        返还 「-」。
    完。
    若 t 等于 标记_乘：
        返还 「*」。
    完。
    若 t 等于 标记_除：
        返还 「/」。
    完。
    若 t 等于 标记_余：
        返还 「%」。
    完。
    若 t 等于 标记_等等：
        返还 「==」。
    完。
    若 t 等于 标记_不等：
        返还 「!=」。
    完。
    若 t 等于 标记_小于：
        返还 「<」。
    完。
    若 t 等于 标记_大于：
        返还 「>」。
    完。
    若 t 等于 标记_小等：
        返还 「<=」。
    完。
    若 t 等于 标记_大等：
        返还 「>=」。
    完。
    若 t 等于 标记_且：
        返还 「&&」。
    完。
    若 t 等于 标记_或：
        返还 「||」。
    完。
    若 t 等于 标记_位与：
        返还 「&」。
    完。
    若 t 等于 标记_位或：
        返还 「|」。
    完。
    若 t 等于 标记_位异或：
        返还 「^」。
    完。
    若 t 等于 标记_左移：
        返还 「<<」。
    完。
    若 t 等于 标记_右移：
        返还 「>>」。
    完。
    返还 「+」。
完。

_p_中文算术类型 接受 零参：
    若 做 _语法_类型 不等于 标记_标识符：
        返还 0。
    完。
    v 为 做 _语法_当前 的 第 1 项。
    若 v 等于 「加」：
        返还 标记_加。
    完。
    若 v 等于 「减」：
        返还 标记_减。
    完。
    若 v 等于 「乘」：
        返还 标记_乘。
    完。
    若 v 等于 「除」：
        返还 标记_除。
    完。
    若 v 等于 「余」：
        返还 标记_余。
    完。
    若 v 等于 「等于」：
        返还 标记_等等。
    完。
    若 v 等于 「不等于」：
        返还 标记_不等。
    完。
    若 v 等于 「小于」：
        返还 标记_小于。
    完。
    若 v 等于 「大于」：
        返还 标记_大于。
    完。
    若 v 等于 「小于等于」：
        返还 标记_小等。
    完。
    若 v 等于 「大于等于」：
        返还 标记_大等。
    完。
    若 v 等于 「不同」：
        返还 标记_不等。
    完。
    若 v 等于 「不一样」：
        返还 标记_不等。
    完。
    若 v 等于 「不一致」：
        返还 标记_不等。
    完。
    若 v 等于 「有别」：
        返还 标记_不等。
    完。
    若 v 等于 「有异」：
        返还 标记_不等。
    完。
    若 v 等于 「迥异」：
        返还 标记_不等。
    完。
    若 v 等于 「殊异」：
        返还 标记_不等。
    完。
    若 v 等于 「相异」：
        返还 标记_不等。
    完。
    若 v 等于 「倍」：
        返还 标记_乘。
    完。
    若 v 等于 「翻」：
        返还 标记_乘。
    完。
    若 v 等于 「相乘」：
        返还 标记_乘。
    完。
    若 v 等于 「乘以」：
        返还 标记_乘。
    完。
    若 v 等于 「取余」：
        返还 标记_余。
    完。
    若 v 等于 「扣」：
        返还 标记_减。
    完。
    若 v 等于 「削」：
        返还 标记_减。
    完。
    若 v 等于 「裁」：
        返还 标记_减。
    完。
    若 v 等于 「减去」：
        返还 标记_减。
    完。
    若 v 等于 「扣除」：
        返还 标记_减。
    完。
    若 v 等于 「相减」：
        返还 标记_减。
    完。
    若 v 等于 「添」：
        返还 标记_加。
    完。
    若 v 等于 「添加」：
        返还 标记_加。
    完。
    若 v 等于 「相加」：
        返还 标记_加。
    完。
    若 v 等于 「超过」：
        返还 标记_大于。
    完。
    若 v 等于 「多于」：
        返还 标记_大于。
    完。
    若 v 等于 「高于」：
        返还 标记_大于。
    完。
    若 v 等于 「胜过」：
        返还 标记_大于。
    完。
    若 v 等于 「优于」：
        返还 标记_大于。
    完。
    若 v 等于 「逾」：
        返还 标记_大于。
    完。
    若 v 等于 「甚于」：
        返还 标记_大于。
    完。
    若 v 等于 「至少」：
        返还 标记_大等。
    完。
    若 v 等于 「不少于」：
        返还 标记_大等。
    完。
    若 v 等于 「不低于」：
        返还 标记_大等。
    完。
    若 v 等于 「起码」：
        返还 标记_大等。
    完。
    若 v 等于 「不亚于」：
        返还 标记_大等。
    完。
    若 v 等于 「不足」：
        返还 标记_小于。
    完。
    若 v 等于 「少于」：
        返还 标记_小于。
    完。
    若 v 等于 「低于」：
        返还 标记_小于。
    完。
    若 v 等于 「劣于」：
        返还 标记_小于。
    完。
    若 v 等于 「逊于」：
        返还 标记_小于。
    完。
    若 v 等于 「弱于」：
        返还 标记_小于。
    完。
    若 v 等于 「至多」：
        返还 标记_小等。
    完。
    若 v 等于 「不超过」：
        返还 标记_小等。
    完。
    若 v 等于 「不多于」：
        返还 标记_小等。
    完。
    若 v 等于 「不高于」：
        返还 标记_小等。
    完。
    若 v 等于 「顶多」：
        返还 标记_小等。
    完。
    若 v 等于 「相等」：
        返还 标记_等等。
    完。
    若 v 等于 「一样」：
        返还 标记_等等。
    完。
    若 v 等于 「同为」：
        返还 标记_等等。
    完。
    若 v 等于 「恰为」：
        返还 标记_等等。
    完。
    若 v 等于 「恰是」：
        返还 标记_等等。
    完。
    若 v 等于 「正是」：
        返还 标记_等等。
    完。
    若 v 等于 「便是」：
        返还 标记_等等。
    完。
    若 v 等于 「即是」：
        返还 标记_等等。
    完。
    若 v 等于 「乃是」：
        返还 标记_等等。
    完。
    若 v 等于 「均分」：
        返还 标记_除。
    完。
    若 v 等于 「相除」：
        返还 标记_除。
    完。
    若 v 等于 「除以」：
        返还 标记_除。
    完。
    返还 0。
完。

_语法_二元优先级 接受 t：
    若 t 等于 标记_或：
        返还 1。
    完。
    若 t 等于 标记_且：
        返还 2。
    完。
    若 t 等于 标记_位或：
        返还 3。
    完。
    若 t 等于 标记_位异或：
        返还 4。
    完。
    若 t 等于 标记_位与：
        返还 5。
    完。
    若 t 等于 标记_等等 或者 t 等于 标记_不等：
        返还 6。
    完。
    若 t 等于 标记_小于 或者 t 等于 标记_大于 或者 t 等于 标记_小等 或者 t 等于 标记_大等：
        返还 7。
    完。
    若 t 等于 标记_左移 或者 t 等于 标记_右移：
        返还 8。
    完。
    若 t 等于 标记_加 或者 t 等于 标记_减：
        返还 9。
    完。
    若 t 等于 标记_乘 或者 t 等于 标记_除 或者 t 等于 标记_余：
        返还 10。
    完。
    返还 0。
完。

_语法_表达式 接受 零参：
    返还 做 _语法_二元 用 0。
完。

_语法_二元 接受 最小：
    可变 左 为 做 _语法_一元。
    只要 做 _语法_结束 等于 0：
        可变 t 为 做 _语法_类型。
        中t 为 做 _p_中文算术类型。
        若 中t 大于 0：
            t 为 中t。
        完。
        优先级 为 做 _语法_二元优先级 用 t。
        若 优先级 等于 0 或者 优先级 小于 最小：
            返还 左。
        完。
        做 _语法_推进。
        右 为 做 _语法_二元 用 优先级 加 1。
        左 为 一组：节点_二元、做 _标记转运算 用 t、左、右。
    完。
    返还 左。
完。

_语法_一元 接受 零参：
    t 为 做 _语法_类型。
    若 t 等于 标记_减：
        做 _语法_推进。
        返还 一组：节点_二元、「-」、一组：标记_数字、0、做 _语法_一元。
    完。
    若 t 等于 标记_非：
        做 _语法_推进。
        返还 一组：节点_一元正、做 _语法_一元。
    完。
    若 t 等于 标记_位取反：
        做 _语法_推进。
        返还 一组：节点_一元负、做 _语法_一元。
    完。
    返还 做 _语法_后缀。
完。

_语法_后缀 接受 零参：
    可变 表达式 为 做 _语法_基元。
    只要 做 _语法_结束 等于 0：
        t 为 做 _语法_类型。
        若 t 等于 标记_左括：
            可变 名称 为 「」。
            若 表达式 的 第 0 项 等于 标记_名称：
                名称 为 表达式 的 第 1 项。
            否则若 表达式 的 第 0 项 等于 标记_连接词：
                名称 为 表达式 的 第 2 项。
            完。
            做 _语法_推进。
            可变 参数 为 一组：（空）。
            只要 做 _语法_类型 不等于 标记_右括 并且 做 _语法_结束 等于 0：
                做 追加 用 参数、做 _语法_表达式。
                做 _语法_期望 用 标记_逗号。
            完。
            做 _语法_期望 用 标记_右括。
            表达式 为 一组：节点_调用、名称、参数。
        否则若 t 等于 标记_左方：
            做 _语法_推进。
            索引 为 做 _语法_表达式。
            做 _语法_期望 用 标记_右方。
            表达式 为 一组：节点_索引、表达式、索引。
        否则若 t 等于 标记_点号：
            做 _语法_推进。
            标记 为 做 _语法_推进。
            若 标记 的 第 0 项 等于 标记_标识符 或者 标记 的 第 0 项 大于或等于 标记_定义：
                表达式 为 一组：131、表达式、标记 的 第 1 项。
            完。
        否则若 t 等于 标记_左花 并且 表达式 的 第 0 项 等于 标记_名称：
            节名 为 表达式 的 第 1 项。
            做 _语法_推进。
            可变 字段列 为 一组：（空）。
            只要 做 _语法_类型 不等于 标记_右花 并且 做 _语法_结束 等于 0：
                首标记 为 做 _语法_推进。
                可变 函数名 为 「」。
                若 首标记 的 第 0 项 等于 标记_标识符 或者 首标记 的 第 0 项 大于或等于 标记_定义：
                    函数名 为 首标记 的 第 1 项。
                完。
                做 _语法_期望 用 标记_冒号。
                做 追加 用 字段列、一组：函数名、做 _语法_表达式。
                做 _语法_期望 用 标记_逗号。
            完。
            做 _语法_期望 用 标记_右花。
            表达式 为 一组：130、节名、字段列。
        否则：
            返还 表达式。
        完。
    完。
    返还 表达式。
完。

_语法_基元 接受 零参：
    标记 为 做 _语法_推进。
    t 为 标记 的 第 0 项。
    v 为 标记 的 第 1 项。
    若 t 等于 词法_整数 或者 t 等于 词法_浮点：
        返还 一组：标记_数字、v。
    完。
    若 t 等于 词法_插值串：
        返还 一组：节_浮点、v、标记 的 第 4 项、标记 的 第 5 项。
    完。
    若 t 等于 词法_字串：
        返还 一组：标记_字串、v。
    完。
    若 t 等于 标记_真：
        返还 一组：标记_数字、1。
    完。
    若 t 等于 标记_假：
        返还 一组：标记_数字、0。
    完。
    若 t 等于 标记_空：
        返还 一组：节点_空字面量。
    完。
    若 t 等于 标记_标识符：
        返还 一组：标记_名称、v。
    完。
    若 t 等于 标记_如果：
        _解析_位置 为 _解析_位置 减 1。
        返还 做 _语法_如果。
    完。
    若 t 大于或等于 标记_定义 并且 t 小于或等于 标记_抛出 并且 t 不等于 标记_建 并且 t 不等于 标记_函数：
        返还 一组：标记_名称、v。
    完。
    若 t 等于 标记_左括：
        e 为 做 _语法_表达式。
        做 _语法_期望 用 标记_右括。
        返还 e。
    完。
    若 t 等于 标记_左方：
        可变 项 为 一组：（空）。
        只要 做 _语法_类型 不等于 标记_右方 并且 做 _语法_结束 等于 0：
            做 追加 用 项、做 _语法_表达式。
            做 _语法_期望 用 标记_逗号。
        完。
        做 _语法_期望 用 标记_右方。
        返还 一组：节点_赋值、项。
    完。
    若 t 等于 标记_建：
        下标记 为 做 _语法_推进。
        可变 节名 为 「」。
        若 下标记 的 第 0 项 等于 标记_标识符 或者 下标记 的 第 0 项 大于或等于 标记_定义：
            节名 为 下标记 的 第 1 项。
        完。
        可变 字段列 为 一组：（空）。
        若 做 _语法_类型 等于 标记_左括：
            做 _语法_推进。
            只要 做 _语法_类型 不等于 标记_右括 并且 做 _语法_结束 等于 0：
                做 追加 用 字段列、一组：「」、做 _语法_表达式。
                做 _语法_期望 用 标记_逗号。
            完。
            做 _语法_期望 用 标记_右括。
        否则若 做 _语法_类型 等于 标记_左花：
            做 _语法_推进。
            只要 做 _语法_类型 不等于 标记_右花 并且 做 _语法_结束 等于 0：
                首标记 为 做 _语法_推进。
                可变 备选函数名 为 「」。
                若 首标记 的 第 0 项 等于 标记_标识符 或者 首标记 的 第 0 项 大于或等于 标记_定义：
                    备选函数名 为 首标记 的 第 1 项。
                完。
                做 _语法_期望 用 标记_冒号。
                做 追加 用 字段列、一组：备选函数名、做 _语法_表达式。
                做 _语法_期望 用 标记_逗号。
            完。
            做 _语法_期望 用 标记_右花。
        完。
        返还 一组：130、节名、字段列。
    完。
    若 t 等于 标记_函数：
        若 做 _语法_类型 等于 标记_左括：
            做 _语法_推进。
            可变 参数 为 一组：（空）。
            只要 做 _语法_类型 不等于 标记_右括 并且 做 _语法_结束 等于 0：
                前标记 为 做 _语法_推进。
                若 前标记 的 第 0 项 等于 标记_标识符 或者 前标记 的 第 0 项 大于或等于 标记_定义：
                    做 追加 用 参数、前标记 的 第 1 项。
                完。
                做 _语法_期望 用 标记_逗号。
            完。
            做 _语法_期望 用 标记_右括。
            体 为 做 _语法_块。
            可变 名 为 做 字符串拼接 用 「__匿名函数_」、做 转字符串 用 做 计数 用 语法状态 的 第 6 项。
            做 追加 用 语法状态 的 第 6 项、一组：120、名、参数、体、标记 的 第 2 项。
            返还 一组：标记_名称、名。
        完。
    完。
    返还 一组：标记_数字、0。
完。

_语法_块 接受 零参：
    若 做 _语法_期望 用 标记_左花 等于 0：
        返还 一组：（空）。
    完。
    可变 语句列 为 一组：（空）。
    只要 做 _语法_类型 不等于 标记_右花 并且 做 _语法_结束 等于 0：
        做 _p_跳过文章分隔。
        若 做 _语法_类型 等于 标记_右花 或者 做 _语法_结束 不等于 0：
            继续。
        完。
        s 为 做 _语法_语句。
        若 s 不等于 空：
            做 追加 用 语句列、s。
        完。
    完。
    做 _语法_期望 用 标记_右花。
    返还 语句列。
完。

_p_block或单语句 接受 零参：
    若 做 _语法_类型 等于 标记_左花：
        返还 做 _语法_块。
    完。
    若 做 _语法_类型 等于 标记_冒号：
        做 _语法_推进。
        做 _p_跳过文章分隔。
        可变 语句列 为 一组：（空）。
        s 为 做 _语法_语句。
        若 s 不等于 空：
            做 追加 用 语句列、s。
        完。
        返还 语句列。
    完。
    单句 为 做 _语法_语句。
    若 单句 等于 空：
        返还 一组：（空）。
    完。
    返还 一组：单句。
完。

_p_是文章分隔 接受 t：
    返还 t 等于 标记_逗号 或者 t 等于 标记_分号。
完。

_p_跳过文章分隔 接受 零参：
    只要 做 _p_是文章分隔 用 做 _语法_类型：
        做 _语法_推进。
    完。
完。

_p_跳过顺承前缀 接受 零参：
    只要 做 _语法_类型 等于 标记_顺序词：
        做 _语法_推进。
        做 _p_跳过文章分隔。
    完。
完。

_p_跳过后件连接 接受 零参：
    做 _p_跳过文章分隔。
    若 做 _语法_类型 等于 标记_连接词：
        做 _语法_推进。
    完。
    做 _p_跳过文章分隔。
完。

_语法_语句 接受 零参：
    做 _p_跳过文章分隔。
    做 _p_跳过顺承前缀。
    做 _p_跳过文章分隔。
    t 为 做 _语法_类型。
    若 t 等于 标记_函数：
        返还 做 _语法_函数。
    完。
    若 t 等于 标记_定义：
        返还 做 _语法_变量。
    完。
    若 t 等于 标记_返回：
        返还 做 _语法_返回。
    完。
    若 t 等于 标记_如果：
        返还 做 _语法_如果。
    完。
    若 t 等于 标记_当：
        返还 做 _语法_循环。
    完。
    若 t 等于 标记_结构：
        返还 做 _语法_结构。
    完。
    若 t 等于 标记_中断：
        _行 为 做 _语法_当前 的 第 2 项。
        做 _语法_推进。
        返还 一组：136、_行。
    完。
    若 t 等于 标记_继续：
        _行 为 做 _语法_当前 的 第 2 项。
        做 _语法_推进。
        返还 一组：137、_行。
    完。
    若 t 等于 标记_导入：
        做 _语法_推进。
        做 _语法_推进。
        返还 空。
    完。
    若 t 等于 标记_尝试：
        返还 做 _语法_尝试。
    完。
    若 t 等于 标记_汇编：
        返还 做 _语法_汇编。
    完。
    若 t 等于 标记_抛出：
        _行 为 做 _语法_当前 的 第 2 项。
        做 _语法_推进。
        返还 一组：162、做 _语法_表达式、_行。
    完。
    若 t 等于 标记_循环：
        返还 做 _语法_遍历。
    完。
    若 t 等于 标记_匹配 并且 做 _语法_预览类型 用 1 等于 标记_左括：
        返还 做 _语法_匹配。
    完。
    若 t 等于 标记_右花 或者 t 等于 0：
        做 _语法_推进。
        返还 空。
    完。
    返还 做 _语法_表达式语句。
完。

_语法_函数 接受 零参：
    _行 为 做 _语法_当前 的 第 2 项。
    做 _语法_推进。
    下标记 为 做 _语法_推进。
    可变 名称 为 「」。
    若 下标记 的 第 0 项 等于 标记_标识符 或者 下标记 的 第 0 项 大于或等于 标记_定义：
        名称 为 下标记 的 第 1 项。
    完。
    做 _语法_期望 用 标记_左括。
    可变 参数 为 一组：（空）。
    只要 做 _语法_类型 不等于 标记_右括 并且 做 _语法_结束 等于 0：
        前标记 为 做 _语法_推进。
        若 前标记 的 第 0 项 等于 标记_标识符 或者 前标记 的 第 0 项 大于或等于 标记_定义：
            做 追加 用 参数、前标记 的 第 1 项。
        完。
        做 _语法_期望 用 标记_逗号。
    完。
    做 _语法_期望 用 标记_右括。
    返还 一组：120、名称、参数、做 _语法_块、_行。
完。

_语法_变量 接受 零参：
    _行 为 做 _语法_当前 的 第 2 项。
    做 _语法_推进。
    可变 是可变 为 0。
    若 做 _语法_类型 等于 标记_可变：
        做 _语法_推进。
        是可变 为 1。
    完。
    下标记 为 做 _语法_推进。
    可变 名称 为 「」。
    若 下标记 的 第 0 项 等于 标记_标识符 或者 下标记 的 第 0 项 大于或等于 标记_定义：
        名称 为 下标记 的 第 1 项。
    完。
    若 名称 等于 「」：
        _行号文本 为 做 转字符串 用 _行。
        做 打印文本 用 做 字符串拼接 用 「错误: 变量名冲突保留字 (行 」、做 字符串拼接 用 _行号文本、做 字符串拼接 用 「): 不能使用 '」、做 字符串拼接 用 下标记 的 第 1 项、「' 作为变量名. 考虑用下划线前缀如 _或 _与 _非」。
    完。
    可变 初值 为 一组：标记_数字、0。
    若 做 _语法_期望 用 标记_赋值 等于 1 或者 做 _语法_类型 等于 标记_标识符 并且 做 _语法_当前 的 第 1 项 等于 「为」 并且 做 _语法_推进：
        初值 为 做 _语法_表达式。
    完。
    返还 一组：110、名称、初值、是可变、_行。
完。

_语法_返回 接受 零参：
    _行 为 做 _语法_当前 的 第 2 项。
    做 _语法_推进。
    若 做 _语法_类型 等于 标记_右花 或者 做 _语法_类型 等于 0：
        返还 一组：114、一组：标记_数字、0、_行。
    完。
    返还 一组：114、做 _语法_表达式、_行。
完。

_语法_如果 接受 零参：
    _行 为 做 _语法_当前 的 第 2 项。
    做 _语法_推进。
    可变 条件 为 一组：标记_数字、0。
    若 做 _语法_期望 用 标记_左括 等于 1：
        条件 为 做 _语法_表达式。
        做 _语法_期望 用 标记_右括。
    否则：
        条件 为 做 _语法_表达式。
    完。
    做 _p_跳过后件连接。
    真支 为 做 _p_block或单语句。
    可变 假支 为 一组：（空）。
    若 做 _语法_类型 等于 标记_否则：
        做 _语法_推进。
        做 _p_跳过文章分隔。
        若 做 _语法_类型 等于 标记_如果：
            假支 为 一组：做 _语法_如果。
        否则：
            假支 为 做 _p_block或单语句。
        完。
    完。
    返还 一组：112、条件、真支、假支、_行。
完。

_语法_循环 接受 零参：
    _行 为 做 _语法_当前 的 第 2 项。
    做 _语法_推进。
    可变 条件 为 一组：标记_数字、0。
    若 做 _语法_期望 用 标记_左括 等于 1：
        条件 为 做 _语法_表达式。
        做 _语法_期望 用 标记_右括。
    否则：
        条件 为 做 _语法_表达式。
    完。
    做 _p_跳过后件连接。
    返还 一组：113、条件、做 _p_block或单语句、_行。
完。

_语法_结构 接受 零参：
    做 _语法_推进。
    下标记 为 做 _语法_推进。
    可变 名称 为 「」。
    若 下标记 的 第 0 项 等于 标记_标识符 或者 下标记 的 第 0 项 大于或等于 标记_定义：
        名称 为 下标记 的 第 1 项。
    完。
    可变 字段 为 一组：（空）。
    若 做 _语法_期望 用 标记_左括 等于 1：
        只要 做 _语法_类型 不等于 标记_右括 并且 做 _语法_结束 等于 0：
            首标记 为 做 _语法_推进。
            若 首标记 的 第 0 项 等于 标记_标识符 或者 首标记 的 第 0 项 大于或等于 标记_定义：
                做 追加 用 字段、首标记 的 第 1 项。
            完。
            做 _语法_期望 用 标记_逗号。
        完。
        做 _语法_期望 用 标记_右括。
    否则若 做 _语法_期望 用 标记_左花 等于 1：
        只要 做 _语法_类型 不等于 标记_右花 并且 做 _语法_结束 等于 0：
            首标记 为 做 _语法_推进。
            若 首标记 的 第 0 项 等于 标记_标识符 或者 首标记 的 第 0 项 大于或等于 标记_定义：
                做 追加 用 字段、首标记 的 第 1 项。
            完。
            做 _语法_期望 用 标记_逗号。
        完。
        做 _语法_期望 用 标记_右花。
    完。
    若 名称 不等于 「」：
        做 字典设 用 结构体表、名称、字段。
        可变 _函返索引 为 0。
        只要 _函返索引 小于 做 计数 用 字段：
            若 做 字典有 用 字段注册表、字段 的 第 _函返索引 项 等于 假：
                做 字典设 用 字段注册表、字段 的 第 _函返索引 项、_函返索引。
            完。
            _函返索引 为 _函返索引 加 1。
        完。
    完。
    返还 空。
完。

_语法_尝试 接受 零参：
    _行 为 做 _语法_当前 的 第 2 项。
    做 _语法_推进。
    试体 为 做 _语法_块。
    可变 捕名 为 「__错误」。
    可变 得体 为 一组：（空）。
    若 做 _语法_类型 等于 标记_得：
        做 _语法_推进。
        若 做 _语法_类型 等于 标记_左括：
            做 _语法_推进。
            值标记 为 做 _语法_推进。
            若 值标记 的 第 0 项 等于 标记_标识符 或者 值标记 的 第 0 项 大于或等于 标记_定义：
                捕名 为 值标记 的 第 1 项。
            完。
            做 _语法_期望 用 标记_右括。
        完。
        得体 为 做 _语法_块。
    完。
    返还 一组：160、试体、捕名、得体、_行。
完。

_语法_汇编 接受 零参：
    _行 为 做 _语法_当前 的 第 2 项。
    做 _语法_推进。
    若 做 _语法_期望 用 标记_左花 等于 0：
        返还 一组：164、一组：（空）、_行。
    完。
    可变 文本列 为 一组：（空）。
    只要 做 _语法_类型 不等于 标记_右花 并且 做 _语法_结束 等于 0：
        做 _p_跳过文章分隔。
        若 做 _语法_类型 等于 标记_右花 或者 做 _语法_结束 不等于 0：
            续。
        完。
        标记 为 做 _语法_推进。
        若 标记 的 第 0 项 等于 词法_字串：
            _原文 为 标记 的 第 1 项。
            可变 _稳定文 为 「」。
            可变 _i 为 0。
            _n 为 做 字节长度 用 _原文。
            只要 _i 小于 _n：
                _稳定文 为 做 字符串拼接 用 _稳定文、做 字符 用 做 字节取 用 _原文、_i。
                _i 为 _i 加 1。
            完。
            做 追加 用 文本列、_稳定文。
        完。
        做 _p_跳过文章分隔。
    完。
    做 _语法_期望 用 标记_右花。
    返还 一组：164、文本列、_行。
完。

_语法_遍历 接受 零参：
    _行 为 做 _语法_当前 的 第 2 项。
    做 _语法_推进。
    做 _语法_期望 用 标记_左括。
    若 做 _语法_类型 等于 标记_标识符：
        可能变量 为 做 _语法_当前 的 第 1 项。
        保存位 为 _解析_位置。
        做 _语法_推进。
        若 做 _语法_类型 等于 标记_标识符 并且 做 字符串相等 用 做 _语法_当前 的 第 1 项、「在」：
            做 _语法_推进。
            集合 为 做 _语法_表达式。
            做 _语法_期望 用 标记_右括。
            返还 一组：134、可能变量、集合、做 _语法_块、_行。
        完。
        _解析_位置 为 保存位。
    完。
    条件 为 做 _语法_表达式。
    做 _语法_期望 用 标记_右括。
    返还 一组：113、条件、做 _语法_块、_行。
完。

_语法_匹配 接受 零参：
    _行 为 做 _语法_当前 的 第 2 项。
    做 _语法_推进。
    做 _语法_期望 用 标记_左括。
    目标 为 做 _语法_表达式。
    做 _语法_期望 用 标记_右括。
    做 _语法_期望 用 标记_左花。
    可变 分支列 为 一组：（空）。
    只要 做 _语法_类型 不等于 标记_右花 并且 做 _语法_结束 等于 0：
        若 做 _语法_类型 等于 标记_默认：
            做 _语法_推进。
            做 _语法_期望 用 标记_冒号。
            做 追加 用 分支列、一组：空、做 _语法_块。
        否则：
            模式 为 做 _语法_表达式。
            做 _语法_期望 用 标记_冒号。
            做 追加 用 分支列、一组：模式、做 _语法_块。
        完。
    完。
    做 _语法_期望 用 标记_右花。
    返还 一组：135、目标、分支列、_行。
完。

_语法_表达式语句 接受 零参：
    _行 为 做 _语法_当前 的 第 2 项。
    表达式 为 做 _语法_表达式。
    若 做 _语法_类型 等于 标记_赋值 或者 做 _语法_类型 等于 标记_标识符 并且 做 _语法_当前 的 第 1 项 等于 「为」：
        做 _语法_推进。
        值 为 做 _语法_表达式。
        若 表达式 的 第 0 项 等于 标记_名称：
            返还 一组：111、表达式 的 第 1 项、值、_行。
        完。
        若 表达式 的 第 0 项 等于 节点_索引：
            基址 为 表达式 的 第 1 项。
            若 基址 的 第 0 项 等于 标记_名称：
                返还 一组：116、基址 的 第 1 项、表达式 的 第 2 项、值、_行。
            完。
            返还 一组：141、基址、表达式 的 第 2 项、值、_行。
        完。
        若 表达式 的 第 0 项 等于 标记_连接词：
            返还 一组：132、表达式 的 第 1 项、表达式 的 第 2 项、值、_行。
        完。
        返还 一组：115、表达式、_行。
    完。
    若 做 _语法_类型 等于 标记_加等：
        做 _语法_推进。
        值 为 做 _语法_表达式。
        若 表达式 的 第 0 项 等于 标记_名称：
            返还 一组：111、表达式 的 第 1 项、一组：节点_二元、「+」、表达式、值、_行。
        完。
        返还 一组：115、表达式、_行。
    完。
    若 做 _语法_类型 等于 标记_减等：
        做 _语法_推进。
        值 为 做 _语法_表达式。
        若 表达式 的 第 0 项 等于 标记_名称：
            返还 一组：111、表达式 的 第 1 项、一组：节点_二元、「-」、表达式、值、_行。
        完。
        返还 一组：115、表达式、_行。
    完。
    返还 一组：115、表达式、_行。
完。

前端_解析_程序 接受 词表：
    若 词表 等于 空 或者 做 计数 用 词表 等于 0：
        返还 一组：一组：（空）、一组：（空）。
    完。
    _解析_标记组 为 词表。
    _解析_位置 为 0。
    _解析_长度 为 做 计数 用 词表。
    语法状态 的 第 5 项 为 0。
    可变 函数列 为 一组：（空）。
    可变 主体 为 一组：（空）。
    只要 做 _语法_结束 等于 0 并且 做 _语法_类型 不等于 0：
        做 _p_跳过文章分隔。
        若 做 _语法_结束 不等于 0 或者 做 _语法_类型 等于 0：
            继续。
        完。
        t 为 做 _语法_类型。
        若 t 等于 标记_函数：
            f 为 做 _语法_函数。
            若 f 不等于 空：
                做 追加 用 函数列、f。
            完。
        否则：
            s 为 做 _语法_语句。
            若 s 不等于 空：
                做 追加 用 主体、s。
            完。
        完。
    完。
    可变 li 为 0。
    只要 li 小于 做 计数 用 语法状态 的 第 6 项：
        做 追加 用 函数列、语法状态 的 第 6 项 的 第 li 项。
        li 为 li 加 1。
    完。
    返还 一组：函数列、主体。
完。

发射_权威wrappers 接受 零参：
    可变 包装列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 11：
        做 追加 用 包装列表、0。
        i 为 i 加 1。
    完。
    返还 包装列表。
完。

_elf写 接受 buf、位置、val：
    buf 的 第 位置 项 为 val 位与 255。
完。

_elf写16 接受 buf、位置、val：
    可变 v 为 val。
    若 v 小于 0：
        v 为 v 加 65536。
    完。
    buf 的 第 位置 项 为 v 位与 255。
    buf 的 第 位置 加 1 项 为 v 除以 256 位与 255。
完。

_elf写32 接受 buf、位置、val：
    可变 v 为 val。
    若 v 小于 0：
        v 为 v 加 4294967296。
    完。
    buf 的 第 位置 项 为 v 位与 255。
    buf 的 第 位置 加 1 项 为 v 除以 256 位与 255。
    buf 的 第 位置 加 2 项 为 v 除以 65536 位与 255。
    buf 的 第 位置 加 3 项 为 v 除以 16777216 位与 255。
完。

_elf写64 接受 buf、位置、val：
    可变 v 为 val。
    可变 i 为 0。
    只要 i 小于 8：
        b 为 v 位与 255。
        buf 的 第 位置 加 i 项 为 b。
        v 为 (v 减 b) 除以 256。
        i 为 i 加 1。
    完。
完。

写二进制 接受 文件名、数据、大小：
    返还 做 写文件 用 文件名、做 列表转字节 用 数据、大小。
完。

本章 收束。
