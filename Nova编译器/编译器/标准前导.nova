《标准前导》

引用《..·标准库·基础》。

引用《..·标准库·数学·基础·数学库》。

引用《..·标准库·数学·数论》。

标准前导_连接 接受 列表、分隔：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 i 大于 0：
            结果 为 做 字符串拼接 用 结果、分隔。
        完。
        结果 为 做 字符串拼接 用 结果、做 转字符串 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

反转 接受 列表：
    可变 结果 为 一组：（空）。
    可变 i 为 做 计数 用 列表 减 1。
    只要 i 大于或等于 0：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 结果。
完。

排序 接受 列表、_扩2：
    可变 结果 为 一组：（空）。
    可变 ci 为 0。
    只要 ci 小于 做 计数 用 列表：
        做 追加 用 结果、列表 的 第 ci 项。
        ci 为 ci 加 1。
    完。
    可变 已换 为 1。
    只要 已换 等于 1：
        已换 为 0。
        ci 为 0。
        只要 ci 小于 做 计数 用 结果 减 1：
            若 结果 的 第 ci 项 大于 结果 的 第 ci 加 1 项：
                临 为 结果 的 第 ci 项。
                做 设置 用 结果、ci、结果 的 第 ci 加 1 项。
                做 设置 用 结果、ci 加 1、临。
                已换 为 1。
            完。
            ci 为 ci 加 1。
        完。
    完。
    返还 结果。
完。

累加 接受 列表：
    可变 结果 为 一组：（空）。
    可变 累 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        累 为 累 加 列表 的 第 i 项。
        做 追加 用 结果、累。
        i 为 i 加 1。
    完。
    返还 结果。
完。

标准前导_跳过 接受 列表、数量：
    可变 结果 为 一组：（空）。
    可变 i 为 数量。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

复制 接受 列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

标准前导_去空白 接受 s：
    可变 起 为 0。
    可变 末 为 做 字节长度 用 s。
    只要 起 小于 末：
        c 为 做 字节取 用 s、起。
        若 c 不等于 字符_空格 并且 c 不等于 字符_制表 并且 c 不等于 字符_换行 并且 c 不等于 字符_回车：
            只要 末 大于 起：
                ce 为 做 字节取 用 s、末 减 1。
                若 ce 不等于 32 并且 ce 不等于 9 并且 ce 不等于 10 并且 ce 不等于 13：
                    返还 做 子串 用 s、起、末 减 起。
                完。
                末 为 末 减 1。
            完。
            返还 做 子串 用 s、起、末 减 起。
        完。
        起 为 起 加 1。
    完。
    返还 「」。
完。

标准前导_重复 接受 s、次数：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 次数：
        结果 为 做 字符串拼接 用 结果、s。
        i 为 i 加 1。
    完。
    返还 结果。
完。

浮点 接受 值：
    返还 值 乘 1。
完。

填充 接受 列表、值、数量：
    可变 i 为 0。
    只要 i 小于 数量：
        做 设置 用 列表、i、值。
        i 为 i 加 1。
    完。
完。

重载 接受 路径：
    内容 为 做 读文件 用 路径。
    若 内容 等于 空：
        返还 0。
    完。
    返还 1。
完。

标准前导_绝对值 接受 x：
    若 x 小于 0：
        返还 0 减 x。
    完。
    返还 x。
完。

标准前导_最大值 接受 a、b：
    若 a 大于或等于 b：
        返还 a。
    完。
    返还 b。
完。

标准前导_最小值 接受 a、b：
    若 a 小于或等于 b：
        返还 a。
    完。
    返还 b。
完。

夹紧 接受 值、最小、最大：
    若 值 小于 最小：
        返还 最小。
    完。
    若 值 大于 最大：
        返还 最大。
    完。
    返还 值。
完。

标准前导_整数sqrt 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

标准前导_幂 接受 底、指数：
    若 指数 等于 0：
        返还 1。
    完。
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 指数：
        结果 为 结果 乘 底。
        i 为 i 加 1。
    完。
    返还 结果。
完。

标准前导_最大公约数 接受 a、b：
    只要 b 不等于 0：
        t 为 b。
        b 为 a 取余于 b。
        a 为 t。
    完。
    返还 a。
完。

标准前导_最小公倍数 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 0。
    完。
    返还 做 绝对值 用 a 乘 b 除以 做 最大公约数 用 a、b。
完。

标准前导_列表_反转 接受 列表：
    可变 结果 为 一组：（空）。
    可变 i 为 做 计数 用 列表 减 1。
    只要 i 大于或等于 0：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 结果。
完。

前导_列表_去重 接受 列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        可变 已有 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 结果：
            若 结果 的 第 j 项 等于 列表 的 第 i 项：
                已有 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 已有：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表_展平 接受 嵌套列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 嵌套列表：
        可变 j 为 0。
        只要 j 小于 做 计数 用 嵌套列表 的 第 i 项：
            做 追加 用 结果、嵌套列表 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

标准前导_列表_求和 接受 列表：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        总 为 总 加 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

标准前导_列表_最大 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 m 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 大于 m：
            m 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 m。
完。

标准前导_列表_最小 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 m 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 m：
            m 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 m。
完。

标准前导_字符串_重复 接受 s、次数：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 次数：
        结果 为 结果 加 s。
        i 为 i 加 1。
    完。
    返还 结果。
完。

标准前导_字符串_左填充 接受 s、目标长度、填充字符：
    只要 做 计数 用 s 小于 目标长度：
        s 为 填充字符 加 s。
    完。
    返还 s。
完。

标准前导_字符串_右填充 接受 s、目标长度、填充字符：
    只要 做 计数 用 s 小于 目标长度：
        s 为 s 加 填充字符。
    完。
    返还 s。
完。

可变 _前导_调用数 为 0。

前导_记录 接受 零参：
    _前导_调用数 为 _前导_调用数 加 1。
完。

本章 收束。
