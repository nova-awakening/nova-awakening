《等价验证器》

引用《..·..·..·标准库·基础》。

验证_接口 为 0。

验证_行为 为 1。

验证_资源 为 2。

验证_ABI 为 3。

验证_性能 为 4。

结果_通过 为 0。

结果_警告 为 1。

结果_失败 为 2。

结果_阻塞 为 3。

验证项_创建 接受 级别、名称：
    返还 一组：级别、名称、结果_通过、「」、「」、「」。
完。

验证项_级别 接受 v：
    返还 v 的 第 0 项。
完。

验证项_名称 接受 v：
    返还 v 的 第 1 项。
完。

验证项_结果 接受 v：
    返还 v 的 第 2 项。
完。

验证项_描述 接受 v：
    返还 v 的 第 3 项。
完。

验证项_设结果 接受 v、结果、描述：
    v 的 第 2 项 为 结果。
    v 的 第 3 项 为 描述。
完。

验证项_设证据 接受 v、源、目标：
    v 的 第 4 项 为 源。
    v 的 第 5 项 为 目标。
完。

等价报告_创建 接受 零参：
    返还 一组：一组：（空）、0、0、「」。
完。

等价报告_添加 接受 报告、验证项：
    做 追加 用 报告 的 第 0 项、验证项。
    若 验证项 的 第 2 项 等于 结果_通过：
        报告 的 第 1 项 为 报告 的 第 1 项 加 1。
    完。
    若 验证项 的 第 2 项 等于 结果_警告：
        报告 的 第 2 项 为 报告 的 第 2 项 加 1。
    完。
    若 验证项 的 第 2 项 等于 结果_失败：
        报告 的 第 3 项 为 报告 的 第 3 项 加 1。
    完。
    若 验证项 的 第 2 项 等于 结果_阻塞：
        报告 的 第 4 项 为 报告 的 第 4 项 加 1。
    完。
完。

等价报告_是否通过 接受 报告：
    返还 报告 的 第 3 项 等于 0 并且 报告 的 第 4 项 等于 0。
完。

验证_接口等价 接受 源接口列表、目标接口列表：
    项 为 做 验证项_创建 用 验证_接口、「公共API完整性」。
    源数 为 做 计数 用 源接口列表。
    目标数 为 做 计数 用 目标接口列表。
    若 源数 等于 0 并且 目标数 等于 0：
        做 验证项_设结果 用 项、结果_通过、「无公共API」。
        返还 项。
    完。
    可变 缺失数 为 0。
    可变 i 为 0。
    只要 i 小于 源数：
        可变 找到 为 假。
        可变 j 为 0。
        只要 j 小于 目标数：
            若 源接口列表 的 第 i 项 等于 目标接口列表 的 第 j 项：
                找到 为 真。
            完。
            j 为 j 加 1。
        完。
        若 找到 等于 假：
            缺失数 为 缺失数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 缺失数 等于 0：
        做 验证项_设结果 用 项、结果_通过、做 字符串拼接 用 「全部」、做 字符串拼接 用 做 转字符串 用 源数、「个接口已覆盖」。
    否则：
        做 验证项_设结果 用 项、结果_失败、做 字符串拼接 用 做 转字符串 用 缺失数、「个公共API缺失」。
    完。
    返还 项。
完。

验证_行为等价 接受 测试结果列表：
    项 为 做 验证项_创建 用 验证_行为、「行为等价(测试)」。
    总数 为 做 计数 用 测试结果列表。
    若 总数 等于 0：
        做 验证项_设结果 用 项、结果_警告、「无测试用例」。
        返还 项。
    完。
    可变 i 为 0。
    只要 i 小于 总数：
        若 测试结果列表 的 第 i 项 等于 真：
            通过数 为 通过数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 通过数 等于 总数：
        做 验证项_设结果 用 项、结果_通过、做 字符串拼接 用 做 转字符串 用 总数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 总数、「 测试通过」。
    否则：
        做 验证项_设结果 用 项、结果_失败、做 字符串拼接 用 做 转字符串 用 通过数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 总数、「 测试通过」。
    完。
    返还 项。
完。

验证_性能预算 接受 源时间、目标时间、容忍倍数：
    项 为 做 验证项_创建 用 验证_性能、「性能预算」。
    若 源时间 小于或等于 0：
        做 验证项_设结果 用 项、结果_警告、「无性能基线」。
        返还 项。
    完。
    比率 为 目标时间 乘 100 除以 源时间。
    若 比率 小于或等于 容忍倍数 乘 100：
        做 验证项_设结果 用 项、结果_通过、做 字符串拼接 用 「目标/源=」、做 字符串拼接 用 做 转字符串 用 比率、「%」。
    否则：
        做 验证项_设结果 用 项、结果_失败、做 字符串拼接 用 「超预算: 目标/源=」、做 字符串拼接 用 做 转字符串 用 比率、「%」。
    完。
    返还 项。
完。

等价报告_一票否决检查 接受 报告：
    可变 否决项 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 报告 的 第 0 项：
        项 为 报告 的 第 0 项 的 第 i 项。
        若 项 的 第 0 项 等于 验证_接口 并且 项 的 第 2 项 等于 结果_失败：
            做 追加 用 否决项、做 字符串拼接 用 「公共API缺失: 」、项 的 第 3 项。
        完。
        若 项 的 第 0 项 等于 验证_行为 并且 项 的 第 2 项 等于 结果_失败：
            做 追加 用 否决项、做 字符串拼接 用 「行为差分: 」、项 的 第 3 项。
        完。
        i 为 i 加 1。
    完。
    返还 否决项。
完。

等价报告_打印 接受 报告：
    做 打印 用 「═══ 等价验证报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  通过: 」、做 转字符串 用 报告 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  警告: 」、做 转字符串 用 报告 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  失败: 」、做 转字符串 用 报告 的 第 3 项。
    做 打印文本 用 做 字符串拼接 用 「  阻塞: 」、做 转字符串 用 报告 的 第 4 项。
    若 做 等价报告_是否通过 用 报告：
        做 打印 用 「  结论: 等价验证通过 ✓」。
    否则：
        做 打印 用 「  结论: 等价验证未通过 ✗」。
        否决 为 做 等价报告_一票否决检查 用 报告。
        可变 i 为 0。
        只要 i 小于 做 计数 用 否决：
            做 打印文本 用 做 字符串拼接 用 「  [否决] 」、否决 的 第 i 项。
            i 为 i 加 1。
        完。
    完。
完。

本章 收束。
