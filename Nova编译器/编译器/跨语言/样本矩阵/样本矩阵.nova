《样本矩阵》

引用《..·..·..·标准库·基础》。

规模_微型 为 0。

规模_小型 为 1。

规模_中型 为 2。

规模_大型 为 3。

规模_超大型 为 4。

复杂度_简单 为 0。

复杂度_标准 为 1。

复杂度_复杂 为 2。

复杂度_极端 为 3。

样本_待测 为 0。

样本_探测通过 为 1。

样本_语义通过 为 2。

样本_重建通过 为 3。

样本_等价通过 为 4。

样本_合规通过 为 5。

样本_全通过 为 6。

样本_失败 为 7。

样本_暂缓 为 8。

样本_创建 接受 名称、语言、规模、复杂度：
    返还 一组：名称、语言、规模、复杂度、样本_待测、0、0、一组：（空）。
完。

样本_名称 接受 s：
    返还 s 的 第 0 项。
完。

样本_语言 接受 s：
    返还 s 的 第 1 项。
完。

样本_规模 接受 s：
    返还 s 的 第 2 项。
完。

样本_复杂度 接受 s：
    返还 s 的 第 3 项。
完。

样本_状态 接受 s：
    返还 s 的 第 4 项。
完。

样本_行数 接受 s：
    返还 s 的 第 5 项。
完。

样本_函数数 接受 s：
    返还 s 的 第 6 项。
完。

样本_设状态 接受 s、状态：
    s 的 第 4 项 为 状态。
完。

样本_设行数 接受 s、n：
    s 的 第 5 项 为 n。
完。

样本_设函数数 接受 s、n：
    s 的 第 6 项 为 n。
完。

样本_设公共API数 接受 s、n：
    s 的 第 7 项 为 n。
完。

样本_添加诊断 接受 s、消息：
    做 追加 用 s 的 第 8 项、消息。
完。

样本矩阵_创建 接受 零参：
    返还 一组：一组：（空）、做 字典、一组：0、0、0。
完。

矩阵_样本 接受 m：
    返还 m 的 第 0 项。
完。

矩阵_语言覆盖 接受 m：
    返还 m 的 第 1 项。
完。

矩阵_添加样本 接受 m、样本：
    做 追加 用 m 的 第 0 项、样本。
    m 的 第 3 项 的 第 0 项 为 m 的 第 3 项 的 第 0 项 加 1。
完。

矩阵_更新统计 接受 m：
    m 的 第 3 项 的 第 1 项 为 0。
    m 的 第 3 项 的 第 2 项 为 0。
    m 的 第 3 项 的 第 3 项 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 m 的 第 0 项：
        若 m 的 第 0 项 的 第 i 项 的 第 4 项 等于 样本_全通过：
            m 的 第 3 项 的 第 1 项 为 m 的 第 3 项 的 第 1 项 加 1。
        完。
        若 m 的 第 0 项 的 第 i 项 的 第 4 项 等于 样本_失败：
            m 的 第 3 项 的 第 2 项 为 m 的 第 3 项 的 第 2 项 加 1。
        完。
        若 m 的 第 0 项 的 第 i 项 的 第 4 项 等于 样本_暂缓：
            m 的 第 3 项 的 第 3 项 为 m 的 第 3 项 的 第 3 项 加 1。
        完。
        i 为 i 加 1。
    完。
完。

矩阵_加载第一波 接受 m：
    做 矩阵_添加样本 用 m、做 样本_创建 用 「rust-cli-simple」、1、规模_微型、复杂度_简单。
    做 矩阵_添加样本 用 m、做 样本_创建 用 「rust-lib-no-macro」、1、规模_小型、复杂度_标准。
    做 矩阵_添加样本 用 m、做 样本_创建 用 「rust-web-actix」、1、规模_中型、复杂度_复杂。
    做 矩阵_添加样本 用 m、做 样本_创建 用 「go-cli-cobra」、2、规模_小型、复杂度_简单。
    做 矩阵_添加样本 用 m、做 样本_创建 用 「go-service-gin」、2、规模_中型、复杂度_标准。
    做 矩阵_添加样本 用 m、做 样本_创建 用 「go-grpc-service」、2、规模_中型、复杂度_复杂。
    做 矩阵_添加样本 用 m、做 样本_创建 用 「c-lib-abi-clean」、3、规模_小型、复杂度_简单。
    做 矩阵_添加样本 用 m、做 样本_创建 用 「c-embedded-hal」、3、规模_中型、复杂度_标准。
    做 矩阵_添加样本 用 m、做 样本_创建 用 「java-spring-minimal」、6、规模_中型、复杂度_复杂。
    做 矩阵_添加样本 用 m、做 样本_创建 用 「java-cli-picocli」、6、规模_小型、复杂度_简单。
    做 矩阵_添加样本 用 m、做 样本_创建 用 「python-fastapi」、5、规模_小型、复杂度_标准。
    做 矩阵_添加样本 用 m、做 样本_创建 用 「python-cli-click」、5、规模_微型、复杂度_简单。
    s1 为 做 样本_创建 用 「rust-proc-macro-heavy」、1、规模_中型、复杂度_极端。
    做 样本_设状态 用 s1、样本_暂缓。
    做 样本_添加诊断 用 s1、「proc-macro需要宏展开器支持」。
    做 矩阵_添加样本 用 m、s1。
    s2 为 做 样本_创建 用 「cpp-template-meta」、4、规模_中型、复杂度_极端。
    做 样本_设状态 用 s2、样本_暂缓。
    做 样本_添加诊断 用 s2、「C++模板元编程超出受控子集」。
    做 矩阵_添加样本 用 m、s2。
    s3 为 做 样本_创建 用 「python-django-full」、5、规模_大型、复杂度_极端。
    做 样本_设状态 用 s3、样本_暂缓。
    做 样本_添加诊断 用 s3、「Django ORM/反射/monkey-patch超出受控范围」。
    做 矩阵_添加样本 用 m、s3。
完。

矩阵_打印报告 接受 m：
    做 矩阵_更新统计 用 m。
    做 打印 用 「═══ 工业样本矩阵报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  总样本数: 」、做 转字符串 用 m 的 第 3 项 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  全通过: 」、做 转字符串 用 m 的 第 3 项 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  失败: 」、做 转字符串 用 m 的 第 3 项 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  暂缓: 」、做 转字符串 用 m 的 第 3 项 的 第 3 项。
    若 m 的 第 3 项 的 第 0 项 大于 0：
        做 打印文本 用 做 字符串拼接 用 「  通过率: 」、做 字符串拼接 用 做 转字符串 用 m 的 第 3 项 的 第 1 项 乘 100 除以 m 的 第 3 项 的 第 0 项、「%」。
    完。
    做 打印 用 「\n  语言覆盖:」。
    可变 语言统计 为 一组：0、0、0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 m 的 第 0 项：
        lang 为 m 的 第 0 项 的 第 i 项 的 第 1 项。
        若 lang 大于或等于 0 并且 lang 小于或等于 6：
            语言统计 的 第 lang 项 为 语言统计 的 第 lang 项 加 1。
        完。
        i 为 i 加 1。
    完。
    若 语言统计 的 第 1 项 大于 0：
        做 打印文本 用 做 字符串拼接 用 「    Rust: 」、做 转字符串 用 语言统计 的 第 1 项。
    完。
    若 语言统计 的 第 2 项 大于 0：
        做 打印文本 用 做 字符串拼接 用 「    Go: 」、做 转字符串 用 语言统计 的 第 2 项。
    完。
    若 语言统计 的 第 3 项 大于 0：
        做 打印文本 用 做 字符串拼接 用 「    C: 」、做 转字符串 用 语言统计 的 第 3 项。
    完。
    若 语言统计 的 第 4 项 大于 0：
        做 打印文本 用 做 字符串拼接 用 「    C++: 」、做 转字符串 用 语言统计 的 第 4 项。
    完。
    若 语言统计 的 第 5 项 大于 0：
        做 打印文本 用 做 字符串拼接 用 「    Python: 」、做 转字符串 用 语言统计 的 第 5 项。
    完。
    若 语言统计 的 第 6 项 大于 0：
        做 打印文本 用 做 字符串拼接 用 「    Java: 」、做 转字符串 用 语言统计 的 第 6 项。
    完。
完。

矩阵_是否合格 接受 m：
    做 矩阵_更新统计 用 m。
    若 m 的 第 3 项 的 第 0 项 等于 0：
        返还 假。
    完。
    有效数 为 m 的 第 3 项 的 第 0 项 减 m 的 第 3 项 的 第 3 项。
    若 有效数 等于 0：
        返还 假。
    完。
    返还 m 的 第 3 项 的 第 1 项 乘 100 除以 有效数 大于或等于 60。
完。

矩阵_打印详情 接受 m：
    做 打印 用 「═══ 样本矩阵详情 ═══」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 m 的 第 0 项：
        s 为 m 的 第 0 项 的 第 i 项。
        可变 状态名 为 「待测」。
        若 s 的 第 4 项 等于 样本_全通过：
            状态名 为 「✅全通过」。
        完。
        若 s 的 第 4 项 等于 样本_失败：
            状态名 为 「❌失败」。
        完。
        若 s 的 第 4 项 等于 样本_暂缓：
            状态名 为 「⏸暂缓」。
        完。
        若 s 的 第 4 项 等于 样本_探测通过：
            状态名 为 「探测✓」。
        完。
        若 s 的 第 4 项 等于 样本_语义通过：
            状态名 为 「语义✓」。
        完。
        若 s 的 第 4 项 等于 样本_重建通过：
            状态名 为 「重建✓」。
        完。
        做 打印文本 用 做 字符串拼接 用 「  」、做 字符串拼接 用 s 的 第 0 项、做 字符串拼接 用 「 → 」、状态名。
        i 为 i 加 1。
    完。
完。

本章 收束。
