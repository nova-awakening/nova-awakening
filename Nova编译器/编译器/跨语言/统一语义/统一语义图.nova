《统一语义图》

引用《..·..·..·标准库·基础》。

引用《..·工程摄取·目录架构扫描》。

引用《..·工程摄取·构建图解析》。

引用《..·合规·克隆扫描》。

引用《..·验证·等价验证器》。

引用《..·样本矩阵·样本矩阵》。

引用《..·原生重建·模块重建器》。

引用《..·原生重建·项目生成器》。

引用《..·智能协作·补丁评审》。

引用《..·智能协作·工具审计》。

引用《..·语言前端·C·C前端》。

引用《..·语言前端·Cpp·Cpp前端》。

引用《..·语言前端·Go·Go前端》。

引用《..·语言前端·Java·Java前端》。

引用《..·语言前端·Python·Python前端》。

引用《..·语言前端·Rust·Rust前端》。

语义_类型 为 0。

语义_所有权 为 1。

语义_并发 为 2。

语义_错误 为 3。

语义_资源 为 4。

语义_ABI 为 5。

语义_元编程 为 6。

语义_构建条件 为 7。

类型节点_基本 为 0。

类型节点_结构 为 1。

类型节点_枚举 为 2。

类型节点_接口 为 3。

类型节点_泛型 为 4。

类型节点_函数签名 为 5。

类型节点_别名 为 6。

所有权_值传递 为 0。

所有权_不可变借用 为 1。

所有权_可变借用 为 2。

所有权_转移 为 3。

所有权_GC管理 为 4。

所有权_手动 为 5。

所有权_引用计数 为 6。

并发_线程 为 0。

并发_协程 为 1。

并发_通道 为 2。

并发_异步 为 3。

并发_事件循环 为 4。

并发_无 为 5。

错误_Result 为 0。

错误_异常 为 1。

错误_Panic 为 2。

错误_errno 为 3。

错误_混合 为 4。

语义节点_创建 接受 类别、子类型、名称：
    返还 一组：类别、子类型、名称、「」、一组：（空）、一组：（空）、0。
完。

语义节点_类别 接受 n：
    返还 n 的 第 0 项。
完。

语义节点_子类型 接受 n：
    返还 n 的 第 1 项。
完。

语义节点_名称 接受 n：
    返还 n 的 第 2 项。
完。

语义节点_源位置 接受 n：
    返还 n 的 第 3 项。
完。

语义节点_属性 接受 n：
    返还 n 的 第 4 项。
完。

语义节点_子节点 接受 n：
    返还 n 的 第 5 项。
完。

语义节点_风险 接受 n：
    返还 n 的 第 6 项。
完。

语义节点_设源位置 接受 n、位置：
    n 的 第 3 项 为 位置。
完。

语义节点_设风险 接受 n、等级：
    n 的 第 6 项 为 等级。
完。

语义节点_添加属性 接受 n、键、值：
    做 追加 用 n 的 第 4 项、一组：键、值。
完。

语义节点_添加子节点 接受 n、子：
    做 追加 用 n 的 第 5 项、子。
完。

语义图_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、一组：（空）、一组：（空）、一组：（空）。
完。

语义图_节点 接受 g：
    返还 g 的 第 0 项。
完。

语义图_类型映射 接受 g：
    返还 g 的 第 1 项。
完。

语义图_接口列表 接受 g：
    返还 g 的 第 2 项。
完。

语义图_风险项 接受 g：
    返还 g 的 第 3 项。
完。

语义图_阻塞项 接受 g：
    返还 g 的 第 4 项。
完。

语义图_添加节点 接受 g、节点：
    做 追加 用 g 的 第 0 项、节点。
完。

语义图_添加类型映射 接受 g、源类型、目标类型：
    做 追加 用 g 的 第 1 项、一组：源类型、目标类型。
完。

语义图_添加接口 接受 g、接口名、方法列表：
    做 追加 用 g 的 第 2 项、一组：接口名、方法列表。
完。

语义图_标记风险 接受 g、描述、等级：
    做 追加 用 g 的 第 3 项、一组：描述、等级。
完。

语义图_标记阻塞 接受 g、描述、原因：
    做 追加 用 g 的 第 4 项、一组：描述、原因。
完。

类型映射_Rust到Nova 接受 rust类型：
    若 rust类型 等于 「i8」 或者 rust类型 等于 「i16」 或者 rust类型 等于 「i32」 或者 rust类型 等于 「i64」 或者 rust类型 等于 「isize」：
        返还 「整数」。
    完。
    若 rust类型 等于 「u8」 或者 rust类型 等于 「u16」 或者 rust类型 等于 「u32」 或者 rust类型 等于 「u64」 或者 rust类型 等于 「usize」：
        返还 「整数」。
    完。
    若 rust类型 等于 「f32」 或者 rust类型 等于 「f64」：
        返还 「浮点」。
    完。
    若 rust类型 等于 「bool」：
        返还 「布尔」。
    完。
    若 rust类型 等于 「String」 或者 rust类型 等于 「&str」：
        返还 「字符串」。
    完。
    若 rust类型 等于 「Vec」：
        返还 「列表」。
    完。
    若 rust类型 等于 「HashMap」：
        返还 「字典」。
    完。
    若 rust类型 等于 「Option」：
        返还 「可空」。
    完。
    若 rust类型 等于 「Result」：
        返还 「结果」。
    完。
    若 rust类型 等于 「()」：
        返还 「空」。
    完。
    返还 做 字符串拼接 用 「结构_」、rust类型。
完。

类型映射_Go到Nova 接受 go类型：
    若 go类型 等于 「int」 或者 go类型 等于 「int8」 或者 go类型 等于 「int16」 或者 go类型 等于 「int32」 或者 go类型 等于 「int64」：
        返还 「整数」。
    完。
    若 go类型 等于 「uint」 或者 go类型 等于 「uint8」 或者 go类型 等于 「uint16」 或者 go类型 等于 「uint32」 或者 go类型 等于 「uint64」：
        返还 「整数」。
    完。
    若 go类型 等于 「float32」 或者 go类型 等于 「float64」：
        返还 「浮点」。
    完。
    若 go类型 等于 「bool」：
        返还 「布尔」。
    完。
    若 go类型 等于 「string」：
        返还 「字符串」。
    完。
    若 go类型 等于 「error」：
        返还 「error」。
    完。
    返还 做 字符串拼接 用 「结构_」、go类型。
完。

类型映射_C到Nova 接受 c类型：
    若 c类型 等于 「int」 或者 c类型 等于 「long」 或者 c类型 等于 「short」 或者 c类型 等于 「char」：
        返还 「整数」。
    完。
    若 c类型 等于 「float」 或者 c类型 等于 「double」：
        返还 「浮点」。
    完。
    若 c类型 等于 「void」：
        返还 「空」。
    完。
    若 c类型 等于 「char*」 或者 c类型 等于 「const char*」：
        返还 「字符串」。
    完。
    返还 做 字符串拼接 用 「结构_」、c类型。
完。

类型映射_Python到Nova 接受 py类型：
    若 py类型 等于 「int」：
        返还 「整数」。
    完。
    若 py类型 等于 「float」：
        返还 「浮点」。
    完。
    若 py类型 等于 「bool」：
        返还 「布尔」。
    完。
    若 py类型 等于 「str」：
        返还 「字符串」。
    完。
    若 py类型 等于 「list」：
        返还 「列表」。
    完。
    若 py类型 等于 「dict」：
        返还 「字典」。
    完。
    若 py类型 等于 「无」：
        返还 「空」。
    完。
    返还 做 字符串拼接 用 「结构_」、py类型。
完。

类型映射_Java到Nova 接受 java类型：
    若 java类型 等于 「int」 或者 java类型 等于 「long」 或者 java类型 等于 「short」 或者 java类型 等于 「byte」：
        返还 「整数」。
    完。
    若 java类型 等于 「float」 或者 java类型 等于 「double」：
        返还 「浮点」。
    完。
    若 java类型 等于 「boolean」：
        返还 「布尔」。
    完。
    若 java类型 等于 「String」：
        返还 「字符串」。
    完。
    若 java类型 等于 「List」 或者 java类型 等于 「ArrayList」：
        返还 「列表」。
    完。
    若 java类型 等于 「Map」 或者 java类型 等于 「HashMap」：
        返还 「字典」。
    完。
    若 java类型 等于 「void」：
        返还 「空」。
    完。
    返还 做 字符串拼接 用 「结构_」、java类型。
完。

语义图_打印风险报告 接受 g：
    做 打印 用 「═══ 语义风险报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  类型映射数: 」、做 转字符串 用 做 计数 用 g 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  接口数: 」、做 转字符串 用 做 计数 用 g 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  风险项数: 」、做 转字符串 用 做 计数 用 g 的 第 3 项。
    做 打印文本 用 做 字符串拼接 用 「  阻塞项数: 」、做 转字符串 用 做 计数 用 g 的 第 4 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 g 的 第 3 项：
        做 打印文本 用 做 字符串拼接 用 「  [风险] 」、g 的 第 3 项 的 第 i 项 的 第 0 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 g 的 第 4 项：
        做 打印文本 用 做 字符串拼接 用 「  [阻塞] 」、做 字符串拼接 用 g 的 第 4 项 的 第 i 项 的 第 0 项、做 字符串拼接 用 「 — 」、g 的 第 4 项 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

本章 收束。
