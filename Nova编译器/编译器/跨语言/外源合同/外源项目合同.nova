《外源项目合同》

引用《..·..·..·标准库·基础》。

引用《..·工程摄取·工程探测器》。

内存模型_所有权 为 0。

内存模型_GC 为 1。

内存模型_手动 为 2。

内存模型_混合 为 3。

并发模型_线程 为 0。

并发模型_协程 为 1。

并发模型_异步IO 为 2。

并发模型_JVM线程 为 3。

并发模型_事件循环 为 4。

并发模型_无 为 5。

错误模型_Result 为 0。

错误模型_Panic 为 1。

错误模型_异常 为 2。

错误模型_errno 为 3。

错误模型_混合 为 4。

重置Profile_严格原生 为 0。

重置Profile_宿主兼容 为 1。

重置Profile_混合桥接 为 2。

外源合同_创建 接受 零参：
    返还 一组：「」、0、0、「」、一组：（空）、一组：（空）、一组：（空）、空、一组：（空）、内存模型_GC、并发模型_无、错误模型_混合、一组：（空）、一组：（空）、一组：（空）、重置Profile_宿主兼容、一组：（空）、一组：（空）。
完。

外源合同_名称 接受 c：
    返还 c 的 第 0 项。
完。

外源合同_源语言 接受 c：
    返还 c 的 第 1 项。
完。

外源合同_构建系统 接受 c：
    返还 c 的 第 2 项。
完。

外源合同_工作区根 接受 c：
    返还 c 的 第 3 项。
完。

外源合同_包根 接受 c：
    返还 c 的 第 4 项。
完。

外源合同_入口 接受 c：
    返还 c 的 第 5 项。
完。

外源合同_公共API 接受 c：
    返还 c 的 第 8 项。
完。

外源合同_内存模型 接受 c：
    返还 c 的 第 9 项。
完。

外源合同_并发模型 接受 c：
    返还 c 的 第 10 项。
完。

外源合同_错误模型 接受 c：
    返还 c 的 第 11 项。
完。

外源合同_FFI边界 接受 c：
    返还 c 的 第 12 项。
完。

外源合同_高风险 接受 c：
    返还 c 的 第 13 项。
完。

外源合同_许可证 接受 c：
    返还 c 的 第 14 项。
完。

外源合同_重置Profile 接受 c：
    返还 c 的 第 15 项。
完。

外源合同_不支持项 接受 c：
    返还 c 的 第 16 项。
完。

外源合同_诊断 接受 c：
    返还 c 的 第 17 项。
完。

外源合同_设名称 接受 c、v：
    c 的 第 0 项 为 v。
完。

外源合同_设源语言 接受 c、v：
    c 的 第 1 项 为 v。
完。

外源合同_设构建系统 接受 c、v：
    c 的 第 2 项 为 v。
完。

外源合同_设工作区根 接受 c、v：
    c 的 第 3 项 为 v。
完。

外源合同_设内存模型 接受 c、v：
    c 的 第 9 项 为 v。
完。

外源合同_设并发模型 接受 c、v：
    c 的 第 10 项 为 v。
完。

外源合同_设错误模型 接受 c、v：
    c 的 第 11 项 为 v。
完。

外源合同_设重置Profile 接受 c、v：
    c 的 第 15 项 为 v。
完。

外源合同_添加包根 接受 c、路径：
    做 追加 用 c 的 第 4 项、路径。
完。

外源合同_添加入口 接受 c、路径：
    做 追加 用 c 的 第 5 项、路径。
完。

外源合同_添加公共API 接受 c、名称：
    做 追加 用 c 的 第 8 项、名称。
完。

外源合同_添加FFI边界 接受 c、边界：
    做 追加 用 c 的 第 12 项、边界。
完。

外源合同_添加高风险 接受 c、项：
    做 追加 用 c 的 第 13 项、项。
完。

外源合同_添加许可证 接受 c、名称、类型：
    做 追加 用 c 的 第 14 项、一组：名称、类型。
完。

外源合同_添加不支持项 接受 c、描述：
    做 追加 用 c 的 第 16 项、描述。
完。

外源合同_添加诊断 接受 c、级别、消息：
    做 追加 用 c 的 第 17 项、一组：级别、消息。
完。

外源合同_从探测结果 接受 探测结果：
    合同 为 做 外源合同_创建。
    做 外源合同_设源语言 用 合同、做 探测结果_语言 用 探测结果。
    做 外源合同_设构建系统 用 合同、做 探测结果_构建系统 用 探测结果。
    做 外源合同_设工作区根 用 合同、做 探测结果_工作区根 用 探测结果。
    语言 为 做 探测结果_语言 用 探测结果。
    若 语言 等于 1：
        做 外源合同_设内存模型 用 合同、内存模型_所有权。
        做 外源合同_设并发模型 用 合同、并发模型_异步IO。
        做 外源合同_设错误模型 用 合同、错误模型_Result。
    完。
    若 语言 等于 2：
        做 外源合同_设内存模型 用 合同、内存模型_GC。
        做 外源合同_设并发模型 用 合同、并发模型_协程。
        做 外源合同_设错误模型 用 合同、错误模型_Panic。
    完。
    若 语言 等于 3：
        做 外源合同_设内存模型 用 合同、内存模型_手动。
        做 外源合同_设并发模型 用 合同、并发模型_线程。
        做 外源合同_设错误模型 用 合同、错误模型_errno。
    完。
    若 语言 等于 4：
        做 外源合同_设内存模型 用 合同、内存模型_混合。
        做 外源合同_设并发模型 用 合同、并发模型_线程。
        做 外源合同_设错误模型 用 合同、错误模型_异常。
    完。
    若 语言 等于 5：
        做 外源合同_设内存模型 用 合同、内存模型_GC。
        做 外源合同_设并发模型 用 合同、并发模型_异步IO。
        做 外源合同_设错误模型 用 合同、错误模型_异常。
    完。
    若 语言 等于 6：
        做 外源合同_设内存模型 用 合同、内存模型_GC。
        做 外源合同_设并发模型 用 合同、并发模型_JVM线程。
        做 外源合同_设错误模型 用 合同、错误模型_异常。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 做 探测结果_包根列表 用 探测结果：
        做 外源合同_添加包根 用 合同、做 探测结果_包根列表 用 探测结果 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 做 探测结果_入口列表 用 探测结果：
        做 外源合同_添加入口 用 合同、做 探测结果_入口列表 用 探测结果 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 合同。
完。

内存模型_名称 接受 id：
    若 id 等于 内存模型_所有权：
        返还 「ownership」。
    完。
    若 id 等于 内存模型_GC：
        返还 「gc」。
    完。
    若 id 等于 内存模型_手动：
        返还 「manual」。
    完。
    若 id 等于 内存模型_混合：
        返还 「hybrid」。
    完。
    返还 「unknown」。
完。

并发模型_名称 接受 id：
    若 id 等于 并发模型_线程：
        返还 「threads」。
    完。
    若 id 等于 并发模型_协程：
        返还 「goroutine」。
    完。
    若 id 等于 并发模型_异步IO：
        返还 「asyncio」。
    完。
    若 id 等于 并发模型_JVM线程：
        返还 「jvm-thread」。
    完。
    若 id 等于 并发模型_事件循环：
        返还 「event-loop」。
    完。
    返还 「none」。
完。

外源合同_打印报告 接受 c：
    做 打印 用 「═══ 外源项目合同报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  名称: 」、c 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  源语言: 」、做 源语言_名称 用 c 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  构建系统: 」、做 构建系统_名称 用 c 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  工作区根: 」、c 的 第 3 项。
    做 打印文本 用 做 字符串拼接 用 「  包根数: 」、做 转字符串 用 做 计数 用 c 的 第 4 项。
    做 打印文本 用 做 字符串拼接 用 「  入口数: 」、做 转字符串 用 做 计数 用 c 的 第 5 项。
    做 打印文本 用 做 字符串拼接 用 「  公共API数: 」、做 转字符串 用 做 计数 用 c 的 第 8 项。
    做 打印文本 用 做 字符串拼接 用 「  内存模型: 」、做 内存模型_名称 用 c 的 第 9 项。
    做 打印文本 用 做 字符串拼接 用 「  并发模型: 」、做 并发模型_名称 用 c 的 第 10 项。
    做 打印文本 用 做 字符串拼接 用 「  FFI边界数: 」、做 转字符串 用 做 计数 用 c 的 第 12 项。
    做 打印文本 用 做 字符串拼接 用 「  高风险项数: 」、做 转字符串 用 做 计数 用 c 的 第 13 项。
    做 打印文本 用 做 字符串拼接 用 「  许可证数: 」、做 转字符串 用 做 计数 用 c 的 第 14 项。
    做 打印文本 用 做 字符串拼接 用 「  不支持项数: 」、做 转字符串 用 做 计数 用 c 的 第 16 项。
    若 做 计数 用 c 的 第 17 项 大于 0：
        做 打印文本 用 做 字符串拼接 用 「  诊断数: 」、做 转字符串 用 做 计数 用 c 的 第 17 项。
    完。
完。

本章 收束。
