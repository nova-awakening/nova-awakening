《Go前端》

引用《..·..·..·..·标准库·基础》。

引用《..·..·统一语义·统一语义图》。

Go声明_函数 为 0。

Go声明_结构 为 1。

Go声明_接口 为 2。

Go声明_类型别名 为 3。

Go声明_常量 为 4。

Go声明_变量 为 5。

Go声明_创建 接受 类型、名称、可见性：
    返还 一组：类型、名称、可见性、一组：（空）、0。
完。

Go模块_创建 接受 路径、包名：
    返还 一组：路径、包名、一组：（空）、一组：（空）、一组：（空）。
完。

Go模块_包名 接受 m：
    返还 m 的 第 1 项。
完。

Go模块_声明 接受 m：
    返还 m 的 第 2 项。
完。

Go模块_导入 接受 m：
    返还 m 的 第 3 项。
完。

Go前端_解析骨架 接受 源码、文件路径：
    模块 为 做 Go模块_创建 用 文件路径、「」。
    可变 位置 为 0。
    总长 为 做 字节长度 用 源码。
    可变 行号 为 1。
    只要 位置 小于 总长：
        只要 行尾 小于 总长 并且 做 字节取 用 源码、行尾 不等于 字符_换行：
            行尾 为 行尾 加 1。
        完。
        行 为 做 子串 用 源码、位置、行尾 减 位置。
        位置 为 行尾 加 1。
        已裁剪 为 做 _Go_去首空白 用 行。
        若 做 _Go_行开头 用 已裁剪、「package 」：
            模块 的 第 1 项 为 做 子串 用 已裁剪、8、做 字节长度 用 已裁剪 减 8。
        完。
        若 做 _Go_行开头 用 已裁剪、「func 」：
            名 为 做 _Go_提取函数名 用 已裁剪。
            可见 为 做 _Go_是否导出 用 名。
            声明 为 做 Go声明_创建 用 Go声明_函数、名、可见。
            声明 的 第 4 项 为 行号。
            做 追加 用 模块 的 第 2 项、声明。
        完。
        若 做 _Go_行开头 用 已裁剪、「type 」：
            名 为 做 _Go_提取类型名 用 已裁剪。
            可见 为 做 _Go_是否导出 用 名。
            可变 类型 为 Go声明_结构。
            若 做 _Go_含 用 已裁剪、「interface」：
                类型 为 Go声明_接口。
            完。
            声明 为 做 Go声明_创建 用 类型、名、可见。
            声明 的 第 4 项 为 行号。
            做 追加 用 模块 的 第 2 项、声明。
        完。
        若 做 _Go_行开头 用 已裁剪、「import 」 或者 已裁剪 等于 「import (」：
            做 追加 用 模块 的 第 3 项、已裁剪。
        完。
        行号 为 行号 加 1。
    完。
    返还 模块。
完。

Go前端_到语义图 接受 模块、语义图：
    声明列表 为 模块 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 声明列表：
        d 为 声明列表 的 第 i 项。
        节点 为 做 语义节点_创建 用 0、d 的 第 0 项、d 的 第 1 项。
        做 语义节点_设源位置 用 节点、模块 的 第 0 项。
        若 d 的 第 2 项 等于 「exported」：
            做 语义节点_添加属性 用 节点、「可见性」、「公共」。
        完。
        做 语义图_添加节点 用 语义图、节点。
        i 为 i 加 1。
    完。
完。

Go模块_打印 接受 模块：
    做 打印文本 用 做 字符串拼接 用 「═══ Go模块: 」、做 字符串拼接 用 模块 的 第 0 项、做 字符串拼接 用 「 (」、做 字符串拼接 用 模块 的 第 1 项、「)」。
    做 打印文本 用 做 字符串拼接 用 「  声明数: 」、做 转字符串 用 做 计数 用 模块 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  导入数: 」、做 转字符串 用 做 计数 用 模块 的 第 3 项。
完。

_Go_去首空白 接受 s：
    可变 i 为 0。
    l 为 做 字节长度 用 s。
    只要 i 小于 l 并且 (做 字节取 用 s、i 等于 字符_空格 或者 做 字节取 用 s、i 等于 字符_制表)：
        i 为 i 加 1。
    完。
    若 i 大于或等于 l：
        返还 「」。
    完。
    返还 做 子串 用 s、i、l 减 i。
完。

_Go_行开头 接受 行、前缀：
    pl 为 做 字节长度 用 前缀。
    若 做 字节长度 用 行 小于 pl：
        返还 假。
    完。
    返还 做 子串 用 行、0、pl 等于 前缀。
完。

_Go_是否导出 接受 名称：
    若 做 字节长度 用 名称 等于 0：
        返还 「」。
    完。
    首字节 为 做 字节取 用 名称、0。
    若 首字节 大于或等于 字符_大写A 并且 首字节 小于或等于 字符_大写Z：
        返还 「exported」。
    完。
    返还 「」。
完。

_Go_提取函数名 接受 行：
    可变 位置 为 5。
    l 为 做 字节长度 用 行。
    若 位置 小于 l 并且 做 字节取 用 行、位置 等于 字符_左圆括：
        只要 位置 小于 l 并且 做 字节取 用 行、位置 不等于 字符_右圆括：
            位置 为 位置 加 1。
        完。
        位置 为 位置 加 1。
        只要 位置 小于 l 并且 做 字节取 用 行、位置 等于 字符_空格：
            位置 为 位置 加 1。
        完。
    完。
    起 为 位置。
    只要 位置 小于 l 并且 做 字节取 用 行、位置 不等于 字符_左圆括 并且 做 字节取 用 行、位置 不等于 字符_空格：
        位置 为 位置 加 1。
    完。
    若 位置 大于 起：
        返还 做 子串 用 行、起、位置 减 起。
    完。
    返还 「anonymous」。
完。

_Go_提取类型名 接受 行：
    可变 位置 为 5。
    l 为 做 字节长度 用 行。
    只要 位置 小于 l 并且 做 字节取 用 行、位置 等于 字符_空格：
        位置 为 位置 加 1。
    完。
    起 为 位置。
    只要 位置 小于 l 并且 做 字节取 用 行、位置 不等于 字符_空格 并且 做 字节取 用 行、位置 不等于 字符_左花括：
        位置 为 位置 加 1。
    完。
    若 位置 大于 起：
        返还 做 子串 用 行、起、位置 减 起。
    完。
    返还 「unknown」。
完。

_Go_含 接受 s、子串：
    sl 为 做 字节长度 用 s。
    pl 为 做 字节长度 用 子串。
    若 pl 大于 sl：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于或等于 sl 减 pl：
        若 做 子串 用 s、i、pl 等于 子串：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

本章 收束。
