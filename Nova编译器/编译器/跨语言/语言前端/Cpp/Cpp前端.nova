《Cpp前端》

引用《..·..·..·..·标准库·基础》。

引用《..·..·统一语义·统一语义图》。

Cpp声明_函数 为 0。

Cpp声明_类 为 1。

Cpp声明_结构 为 2。

Cpp声明_命名空间 为 3。

Cpp声明_模板 为 4。

Cpp声明_枚举 为 5。

Cpp声明_Typedef 为 6。

Cpp声明_Using 为 7。

Cpp声明_Include 为 8。

Cpp声明_创建 接受 类型、名称、可见性：
    返还 一组：类型、名称、可见性、0。
完。

Cpp模块_创建 接受 路径：
    返还 一组：路径、一组：（空）、一组：（空）、一组：（空）、一组：（空）。
完。

Cpp模块_声明 接受 m：
    返还 m 的 第 1 项。
完。

Cpp模块_Include 接受 m：
    返还 m 的 第 2 项。
完。

Cpp模块_命名空间 接受 m：
    返还 m 的 第 3 项。
完。

Cpp前端_解析骨架 接受 源码、文件路径：
    模块 为 做 Cpp模块_创建 用 文件路径。
    可变 位置 为 0。
    总长 为 做 字节长度 用 源码。
    可变 行号 为 1。
    只要 位置 小于 总长：
        只要 行尾 小于 总长 并且 做 字节取 用 源码、行尾 不等于 字符_换行：
            行尾 为 行尾 加 1。
        完。
        行 为 做 子串 用 源码、位置、行尾 减 位置。
        位置 为 行尾 加 1。
        已裁剪 为 做 _Cpp_去首空白 用 行。
        若 做 _Cpp_行开头 用 已裁剪、「#include」：
            做 追加 用 模块 的 第 2 项、已裁剪。
        完。
        若 做 _Cpp_行开头 用 已裁剪、「class 」：
            名 为 做 _Cpp_提取词 用 已裁剪、6。
            做 追加 用 模块 的 第 1 项、做 Cpp声明_创建 用 Cpp声明_类、名、「」。
        完。
        若 做 _Cpp_行开头 用 已裁剪、「struct 」：
            名 为 做 _Cpp_提取词 用 已裁剪、7。
            做 追加 用 模块 的 第 1 项、做 Cpp声明_创建 用 Cpp声明_结构、名、「」。
        完。
        若 做 _Cpp_行开头 用 已裁剪、「namespace 」：
            名 为 做 _Cpp_提取词 用 已裁剪、10。
            做 追加 用 模块 的 第 3 项、名。
        完。
        若 做 _Cpp_行开头 用 已裁剪、「template」：
            做 追加 用 模块 的 第 1 项、做 Cpp声明_创建 用 Cpp声明_模板、已裁剪、「」。
        完。
        若 做 _Cpp_行开头 用 已裁剪、「enum 」：
            名 为 做 _Cpp_提取词 用 已裁剪、5。
            做 追加 用 模块 的 第 1 项、做 Cpp声明_创建 用 Cpp声明_枚举、名、「」。
        完。
        行号 为 行号 加 1。
    完。
    返还 模块。
完。

Cpp前端_到语义图 接受 模块、语义图：
    可变 i 为 0。
    只要 i 小于 做 计数 用 模块 的 第 1 项：
        d 为 模块 的 第 1 项 的 第 i 项。
        节点 为 做 语义节点_创建 用 0、d 的 第 0 项、d 的 第 1 项。
        做 语义节点_设源位置 用 节点、模块 的 第 0 项。
        做 语义图_添加节点 用 语义图、节点。
        i 为 i 加 1。
    完。
完。

Cpp模块_打印 接受 模块：
    做 打印文本 用 做 字符串拼接 用 「═══ C++模块: 」、模块 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  声明数: 」、做 转字符串 用 做 计数 用 模块 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  命名空间数: 」、做 转字符串 用 做 计数 用 模块 的 第 3 项。
完。

_Cpp_去首空白 接受 s：
    可变 i 为 0。
    l 为 做 字节长度 用 s。
    只要 i 小于 l 并且 (做 字节取 用 s、i 等于 字符_空格 或者 做 字节取 用 s、i 等于 字符_制表)：
        i 为 i 加 1。
    完。
    若 i 大于或等于 l：
        返还 「」。
    完。
    返还 做 子串 用 s、i、l 减 i。
完。

_Cpp_行开头 接受 行、前缀：
    pl 为 做 字节长度 用 前缀。
    若 做 字节长度 用 行 小于 pl：
        返还 假。
    完。
    返还 做 子串 用 行、0、pl 等于 前缀。
完。

_Cpp_提取词 接受 行、起始：
    可变 位置 为 起始。
    l 为 做 字节长度 用 行。
    只要 位置 小于 l 并且 做 字节取 用 行、位置 等于 字符_空格：
        位置 为 位置 加 1。
    完。
    起 为 位置。
    只要 位置 小于 l 并且 做 字节取 用 行、位置 不等于 字符_空格 并且 做 字节取 用 行、位置 不等于 字符_左花括 并且 做 字节取 用 行、位置 不等于 字符_分号 并且 做 字节取 用 行、位置 不等于 字符_冒号 并且 做 字节取 用 行、位置 不等于 字符_小于号：
        位置 为 位置 加 1。
    完。
    若 位置 大于 起：
        返还 做 子串 用 行、起、位置 减 起。
    完。
    返还 「unknown」。
完。

本章 收束。
