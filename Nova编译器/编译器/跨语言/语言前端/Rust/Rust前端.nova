《Rust前端》

引用《..·..·..·..·标准库·基础》。

引用《..·..·统一语义·统一语义图》。

Rust声明_函数 为 0。

Rust声明_结构 为 1。

Rust声明_枚举 为 2。

Rust声明_Trait 为 3。

Rust声明_Impl 为 4。

Rust声明_模块 为 5。

Rust声明_类型别名 为 6。

Rust声明_常量 为 7。

Rust声明_Use 为 8。

Rust声明_创建 接受 类型、名称、可见性：
    返还 一组：类型、名称、可见性、一组：（空）、一组：（空）、一组：（空）、0。
完。

Rust声明_类型 接受 d：
    返还 d 的 第 0 项。
完。

Rust声明_名称 接受 d：
    返还 d 的 第 1 项。
完。

Rust声明_可见性 接受 d：
    返还 d 的 第 2 项。
完。

Rust声明_泛型 接受 d：
    返还 d 的 第 3 项。
完。

Rust声明_生命周期 接受 d：
    返还 d 的 第 4 项。
完。

Rust模块_创建 接受 路径：
    返还 一组：路径、一组：（空）、一组：（空）、一组：（空）、一组：（空）。
完。

Rust模块_声明 接受 m：
    返还 m 的 第 1 项。
完。

Rust模块_Use 接受 m：
    返还 m 的 第 2 项。
完。

Rust模块_子模块 接受 m：
    返还 m 的 第 3 项。
完。

Rust前端_解析骨架 接受 源码、文件路径：
    模块 为 做 Rust模块_创建 用 文件路径。
    可变 位置 为 0。
    总长 为 做 字节长度 用 源码。
    可变 行号 为 1。
    只要 位置 小于 总长：
        只要 行尾 小于 总长 并且 做 字节取 用 源码、行尾 不等于 字符_换行：
            行尾 为 行尾 加 1。
        完。
        行 为 做 子串 用 源码、位置、行尾 减 位置。
        位置 为 行尾 加 1。
        已裁剪 为 做 _Rust_去首空白 用 行。
        可见 为 做 _Rust_检查可见性 用 已裁剪。
        若 做 _Rust_行开头 用 已裁剪、「fn 」 或者 做 _Rust_行开头 用 已裁剪、「pub fn 」 或者 做 _Rust_行开头 用 已裁剪、「pub(crate) fn 」：
            名 为 做 _Rust_提取函数名 用 已裁剪。
            声明 为 做 Rust声明_创建 用 Rust声明_函数、名、可见。
            声明 的 第 6 项 为 行号。
            做 追加 用 模块 的 第 1 项、声明。
        完。
        若 做 _Rust_行开头 用 已裁剪、「struct 」 或者 做 _Rust_行开头 用 已裁剪、「pub struct 」：
            名 为 做 _Rust_提取类型名 用 已裁剪、「struct 」。
            声明 为 做 Rust声明_创建 用 Rust声明_结构、名、可见。
            声明 的 第 6 项 为 行号。
            做 追加 用 模块 的 第 1 项、声明。
        完。
        若 做 _Rust_行开头 用 已裁剪、「enum 」 或者 做 _Rust_行开头 用 已裁剪、「pub enum 」：
            名 为 做 _Rust_提取类型名 用 已裁剪、「enum 」。
            声明 为 做 Rust声明_创建 用 Rust声明_枚举、名、可见。
            声明 的 第 6 项 为 行号。
            做 追加 用 模块 的 第 1 项、声明。
        完。
        若 做 _Rust_行开头 用 已裁剪、「trait 」 或者 做 _Rust_行开头 用 已裁剪、「pub trait 」：
            名 为 做 _Rust_提取类型名 用 已裁剪、「trait 」。
            声明 为 做 Rust声明_创建 用 Rust声明_Trait、名、可见。
            声明 的 第 6 项 为 行号。
            做 追加 用 模块 的 第 1 项、声明。
        完。
        若 做 _Rust_行开头 用 已裁剪、「impl 」 或者 做 _Rust_行开头 用 已裁剪、「impl<」：
            名 为 做 _Rust_提取impl名 用 已裁剪。
            声明 为 做 Rust声明_创建 用 Rust声明_Impl、名、「」。
            声明 的 第 6 项 为 行号。
            做 追加 用 模块 的 第 1 项、声明。
        完。
        若 做 _Rust_行开头 用 已裁剪、「mod 」 或者 做 _Rust_行开头 用 已裁剪、「pub mod 」：
            名 为 做 _Rust_提取类型名 用 已裁剪、「mod 」。
            做 追加 用 模块 的 第 3 项、名。
        完。
        若 做 _Rust_行开头 用 已裁剪、「use 」 或者 做 _Rust_行开头 用 已裁剪、「pub use 」：
            做 追加 用 模块 的 第 2 项、已裁剪。
        完。
        行号 为 行号 加 1。
    完。
    返还 模块。
完。

Rust前端_到语义图 接受 模块、语义图：
    声明列表 为 模块 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 声明列表：
        d 为 声明列表 的 第 i 项。
        可变 类别 为 0。
        若 d 的 第 0 项 等于 Rust声明_函数：
            类别 为 4。
        完。
        节点 为 做 语义节点_创建 用 类别、d 的 第 0 项、d 的 第 1 项。
        做 语义节点_设源位置 用 节点、模块 的 第 0 项。
        若 d 的 第 2 项 等于 「pub」：
            做 语义节点_添加属性 用 节点、「可见性」、「公共」。
        完。
        做 语义图_添加节点 用 语义图、节点。
        i 为 i 加 1。
    完。
完。

Rust模块_打印 接受 模块：
    做 打印文本 用 做 字符串拼接 用 「═══ Rust模块: 」、模块 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  声明数: 」、做 转字符串 用 做 计数 用 模块 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  use数: 」、做 转字符串 用 做 计数 用 模块 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  子模块数: 」、做 转字符串 用 做 计数 用 模块 的 第 3 项。
完。

_Rust_去首空白 接受 s：
    可变 i 为 0。
    l 为 做 字节长度 用 s。
    只要 i 小于 l 并且 (做 字节取 用 s、i 等于 字符_空格 或者 做 字节取 用 s、i 等于 字符_制表)：
        i 为 i 加 1。
    完。
    若 i 大于或等于 l：
        返还 「」。
    完。
    返还 做 子串 用 s、i、l 减 i。
完。

_Rust_检查可见性 接受 行：
    若 做 _Rust_行开头 用 行、「pub 」 或者 做 _Rust_行开头 用 行、「pub(」：
        返还 「pub」。
    完。
    返还 「」。
完。

_Rust_行开头 接受 行、前缀：
    pl 为 做 字节长度 用 前缀。
    若 做 字节长度 用 行 小于 pl：
        返还 假。
    完。
    返还 做 子串 用 行、0、pl 等于 前缀。
完。

_Rust_提取函数名 接受 行：
    可变 位置 为 0。
    l 为 做 字节长度 用 行。
    只要 位置 小于 l 减 2：
        若 做 字节取 用 行、位置 等于 字符_小写f 并且 做 字节取 用 行、位置 加 1 等于 字符_小写n 并且 做 字节取 用 行、位置 加 2 等于 字符_空格：
            位置 为 位置 加 3。
            起 为 位置。
            只要 位置 小于 l 并且 做 字节取 用 行、位置 不等于 字符_左圆括 并且 做 字节取 用 行、位置 不等于 字符_小于号 并且 做 字节取 用 行、位置 不等于 字符_空格：
                位置 为 位置 加 1。
            完。
            返还 做 子串 用 行、起、位置 减 起。
        完。
        位置 为 位置 加 1。
    完。
    返还 「anonymous」。
完。

_Rust_提取类型名 接受 行、关键字：
    可变 位置 为 0。
    l 为 做 字节长度 用 行。
    kl 为 做 字节长度 用 关键字。
    只要 位置 小于 l 减 kl：
        若 做 子串 用 行、位置、kl 等于 关键字：
            位置 为 位置 加 kl。
            起 为 位置。
            只要 位置 小于 l 并且 做 字节取 用 行、位置 不等于 字符_空格 并且 做 字节取 用 行、位置 不等于 字符_小于号 并且 做 字节取 用 行、位置 不等于 字符_左圆括 并且 做 字节取 用 行、位置 不等于 字符_左花括 并且 做 字节取 用 行、位置 不等于 字符_分号：
                位置 为 位置 加 1。
            完。
            返还 做 子串 用 行、起、位置 减 起。
        完。
        位置 为 位置 加 1。
    完。
    返还 「unknown」。
完。

_Rust_提取impl名 接受 行：
    l 为 做 字节长度 用 行。
    可变 位置 为 5。
    若 做 字节取 用 行、4 等于 字符_小于号：
        位置 为 4。
    完。
    只要 位置 小于 l 并且 做 字节取 用 行、位置 等于 字符_空格：
        位置 为 位置 加 1。
    完。
    起 为 位置。
    只要 位置 小于 l 并且 做 字节取 用 行、位置 不等于 字符_空格 并且 做 字节取 用 行、位置 不等于 字符_左花括 并且 做 字节取 用 行、位置 不等于 字符_小于号：
        位置 为 位置 加 1。
    完。
    返还 做 子串 用 行、起、位置 减 起。
完。

本章 收束。
