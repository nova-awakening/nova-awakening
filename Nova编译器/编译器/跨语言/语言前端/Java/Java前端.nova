《Java前端》

引用《..·..·..·..·标准库·基础》。

引用《..·..·统一语义·统一语义图》。

Java声明_类 为 0。

Java声明_接口 为 1。

Java声明_方法 为 2。

Java声明_枚举 为 3。

Java声明_注解 为 4。

Java声明_包 为 5。

Java声明_导入 为 6。

Java声明_创建 接受 类型、名称、修饰符：
    返还 一组：类型、名称、修饰符、0。
完。

Java模块_创建 接受 路径：
    返还 一组：路径、「」、一组：（空）、一组：（空）、一组：（空）、一组：（空）。
完。

Java模块_包名 接受 m：
    返还 m 的 第 1 项。
完。

Java模块_声明 接受 m：
    返还 m 的 第 2 项。
完。

Java模块_导入 接受 m：
    返还 m 的 第 3 项。
完。

Java模块_注解 接受 m：
    返还 m 的 第 4 项。
完。

Java模块_风险 接受 m：
    返还 m 的 第 5 项。
完。

Java前端_解析骨架 接受 源码、文件路径：
    模块 为 做 Java模块_创建 用 文件路径。
    可变 位置 为 0。
    总长 为 做 字节长度 用 源码。
    可变 行号 为 1。
    只要 位置 小于 总长：
        只要 行尾 小于 总长 并且 做 字节取 用 源码、行尾 不等于 字符_换行：
            行尾 为 行尾 加 1。
        完。
        行 为 做 子串 用 源码、位置、行尾 减 位置。
        位置 为 行尾 加 1。
        已裁剪 为 做 _Java_去首空白 用 行。
        若 做 _Java_行开头 用 已裁剪、「package 」：
            模块 的 第 1 项 为 做 _Java_提取词 用 已裁剪、8。
        完。
        若 做 _Java_行开头 用 已裁剪、「import 」：
            做 追加 用 模块 的 第 3 项、已裁剪。
        完。
        若 做 _Java_含 用 已裁剪、「class 」：
            名 为 做 _Java_提取类名 用 已裁剪、「class 」。
            修饰 为 做 _Java_提取修饰符 用 已裁剪。
            做 追加 用 模块 的 第 2 项、做 Java声明_创建 用 Java声明_类、名、修饰。
        完。
        若 做 _Java_含 用 已裁剪、「interface 」：
            名 为 做 _Java_提取类名 用 已裁剪、「interface 」。
            做 追加 用 模块 的 第 2 项、做 Java声明_创建 用 Java声明_接口、名、「」。
        完。
        若 做 _Java_含 用 已裁剪、「enum 」：
            名 为 做 _Java_提取类名 用 已裁剪、「enum 」。
            做 追加 用 模块 的 第 2 项、做 Java声明_创建 用 Java声明_枚举、名、「」。
        完。
        若 做 _Java_行开头 用 已裁剪、「@」：
            做 追加 用 模块 的 第 4 项、一组：行号、已裁剪。
        完。
        若 做 _Java_含 用 已裁剪、「Class.forName」 或者 做 _Java_含 用 已裁剪、「getMethod」 或者 做 _Java_含 用 已裁剪、「Proxy.newProxyInstance」 或者 做 _Java_含 用 已裁剪、「ClassLoader」：
            做 追加 用 模块 的 第 5 项、一组：行号、已裁剪。
        完。
        行号 为 行号 加 1。
    完。
    返还 模块。
完。

Java前端_到语义图 接受 模块、语义图：
    可变 i 为 0。
    只要 i 小于 做 计数 用 模块 的 第 2 项：
        d 为 模块 的 第 2 项 的 第 i 项。
        节点 为 做 语义节点_创建 用 0、d 的 第 0 项、d 的 第 1 项。
        做 语义节点_设源位置 用 节点、模块 的 第 0 项。
        做 语义图_添加节点 用 语义图、节点。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 模块 的 第 5 项：
        做 语义图_标记风险 用 语义图、做 字符串拼接 用 「Java反射高风险: 行」、做 转字符串 用 模块 的 第 5 项 的 第 i 项 的 第 0 项、2。
        i 为 i 加 1。
    完。
完。

Java模块_打印 接受 模块：
    做 打印文本 用 做 字符串拼接 用 「═══ Java模块: 」、做 字符串拼接 用 模块 的 第 0 项、做 字符串拼接 用 「 (」、做 字符串拼接 用 模块 的 第 1 项、「)」。
    做 打印文本 用 做 字符串拼接 用 「  声明数: 」、做 转字符串 用 做 计数 用 模块 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  导入数: 」、做 转字符串 用 做 计数 用 模块 的 第 3 项。
    做 打印文本 用 做 字符串拼接 用 「  注解数: 」、做 转字符串 用 做 计数 用 模块 的 第 4 项。
    做 打印文本 用 做 字符串拼接 用 「  高风险项: 」、做 转字符串 用 做 计数 用 模块 的 第 5 项。
完。

_Java_去首空白 接受 s：
    可变 i 为 0。
    l 为 做 字节长度 用 s。
    只要 i 小于 l 并且 (做 字节取 用 s、i 等于 字符_空格 或者 做 字节取 用 s、i 等于 字符_制表)：
        i 为 i 加 1。
    完。
    若 i 大于或等于 l：
        返还 「」。
    完。
    返还 做 子串 用 s、i、l 减 i。
完。

_Java_行开头 接受 行、前缀：
    pl 为 做 字节长度 用 前缀。
    若 做 字节长度 用 行 小于 pl：
        返还 假。
    完。
    返还 做 子串 用 行、0、pl 等于 前缀。
完。

_Java_含 接受 s、子串：
    sl 为 做 字节长度 用 s。
    pl 为 做 字节长度 用 子串。
    若 pl 大于 sl：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于或等于 sl 减 pl：
        若 做 子串 用 s、i、pl 等于 子串：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

_Java_提取词 接受 行、起始：
    可变 位置 为 起始。
    l 为 做 字节长度 用 行。
    起 为 位置。
    只要 位置 小于 l 并且 做 字节取 用 行、位置 不等于 字符_分号 并且 做 字节取 用 行、位置 不等于 字符_空格：
        位置 为 位置 加 1。
    完。
    若 位置 大于 起：
        返还 做 子串 用 行、起、位置 减 起。
    完。
    返还 「」。
完。

_Java_提取类名 接受 行、关键字：
    kl 为 做 字节长度 用 关键字。
    l 为 做 字节长度 用 行。
    可变 位置 为 0。
    只要 位置 小于 l 减 kl：
        若 做 子串 用 行、位置、kl 等于 关键字：
            位置 为 位置 加 kl。
            起 为 位置。
            只要 位置 小于 l 并且 做 字节取 用 行、位置 不等于 字符_空格 并且 做 字节取 用 行、位置 不等于 字符_左花括 并且 做 字节取 用 行、位置 不等于 字符_小于号：
                位置 为 位置 加 1。
            完。
            返还 做 子串 用 行、起、位置 减 起。
        完。
        位置 为 位置 加 1。
    完。
    返还 「unknown」。
完。

_Java_提取修饰符 接受 行：
    若 做 _Java_行开头 用 行、「public 」：
        返还 「public」。
    完。
    若 做 _Java_行开头 用 行、「private 」：
        返还 「private」。
    完。
    若 做 _Java_行开头 用 行、「protected 」：
        返还 「protected」。
    完。
    返还 「」。
完。

本章 收束。
