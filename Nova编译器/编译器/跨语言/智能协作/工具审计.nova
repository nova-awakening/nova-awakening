《工具审计》

引用《..·..·..·标准库·基础》。

工具_文件读取 为 0。

工具_文件写入 为 1。

工具_编译执行 为 2。

工具_网络访问 为 3。

工具_系统命令 为 4。

审计条目_创建 接受 工具类型、目标、参数：
    返还 一组：0、工具类型、目标、参数、「」、「」。
完。

审计条目_设结果 接受 e、结果：
    e 的 第 4 项 为 结果。
完。

审计条目_设批准者 接受 e、批准者：
    e 的 第 5 项 为 批准者。
完。

可变 _工具白名单 为 一组：（空）。

审计_初始化白名单 接受 零参：
    _工具白名单 为 一组：一组：工具_文件读取、「允许」、一组：工具_编译执行、「允许」、一组：工具_文件写入、「需审批」、一组：工具_网络访问、「禁止」、一组：工具_系统命令、「需审批」。
完。

审计_检查白名单 接受 工具类型：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _工具白名单：
        若 _工具白名单 的 第 i 项 的 第 0 项 等于 工具类型：
            返还 _工具白名单 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 「禁止」。
完。

可变 智能协作__审计日志 为 一组：一组：（空）、0、0、0。

工具审计_审计_记录 接受 工具类型、目标、参数：
    权限 为 做 审计_检查白名单 用 工具类型。
    条目 为 做 审计条目_创建 用 工具类型、目标、参数。
    若 权限 等于 「禁止」：
        做 审计条目_设结果 用 条目、「拒绝:白名单禁止」。
        智能协作__审计日志 的 第 1 项 为 智能协作__审计日志 的 第 1 项 加 1。
    否则若 权限 等于 「需审批」：
        做 审计条目_设结果 用 条目、「等待审批」。
        智能协作__审计日志 的 第 2 项 为 智能协作__审计日志 的 第 2 项 加 1。
    否则：
        做 审计条目_设结果 用 条目、「允许」。
    完。
    做 追加 用 智能协作__审计日志 的 第 0 项、条目。
    返还 条目。
完。

审计_批准 接受 条目索引、批准者：
    若 条目索引 小于 做 计数 用 智能协作__审计日志 的 第 0 项：
        智能协作__审计日志 的 第 0 项 的 第 条目索引 项 的 第 5 项 为 批准者。
        智能协作__审计日志 的 第 0 项 的 第 条目索引 项 的 第 4 项 为 「已批准」。
        智能协作__审计日志 的 第 3 项 为 智能协作__审计日志 的 第 3 项 加 1。
    完。
完。

可变 _快照列表 为 一组：（空）。

审计_创建快照 接受 标签：
    做 追加 用 _快照列表、一组：标签、做 计数 用 智能协作__审计日志 的 第 0 项。
    返还 做 计数 用 _快照列表 减 1。
完。

审计_回滚到快照 接受 快照索引：
    若 快照索引 大于或等于 做 计数 用 _快照列表：
        返还 假。
    完。
    目标数 为 _快照列表 的 第 快照索引 项 的 第 1 项。
    只要 做 计数 用 智能协作__审计日志 的 第 0 项 大于 目标数：
        智能协作__审计日志 的 第 0 项 为 智能协作__审计日志 的 第 0 项。
    完。
    返还 真。
完。

审计_打印报告 接受 零参：
    做 打印 用 「═══ 工具审计报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  总调用数: 」、做 转字符串 用 做 计数 用 智能协作__审计日志 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  拒绝数: 」、做 转字符串 用 智能协作__审计日志 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  需审批: 」、做 转字符串 用 智能协作__审计日志 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  已批准: 」、做 转字符串 用 智能协作__审计日志 的 第 3 项。
    做 打印文本 用 做 字符串拼接 用 「  快照数: 」、做 转字符串 用 做 计数 用 _快照列表。
    做 打印 用 「\n  白名单:」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _工具白名单：
        可变 名称 为 「unknown」。
        若 _工具白名单 的 第 i 项 的 第 0 项 等于 工具_文件读取：
            名称 为 「文件读取」。
        完。
        若 _工具白名单 的 第 i 项 的 第 0 项 等于 工具_文件写入：
            名称 为 「文件写入」。
        完。
        若 _工具白名单 的 第 i 项 的 第 0 项 等于 工具_编译执行：
            名称 为 「编译执行」。
        完。
        若 _工具白名单 的 第 i 项 的 第 0 项 等于 工具_网络访问：
            名称 为 「网络访问」。
        完。
        若 _工具白名单 的 第 i 项 的 第 0 项 等于 工具_系统命令：
            名称 为 「系统命令」。
        完。
        做 打印文本 用 做 字符串拼接 用 「    」、做 字符串拼接 用 名称、做 字符串拼接 用 「: 」、_工具白名单 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

本章 收束。
