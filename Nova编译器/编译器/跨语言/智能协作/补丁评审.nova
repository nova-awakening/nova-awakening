《补丁评审》

引用《..·..·..·标准库·基础》。

补丁_暂存 为 0。

补丁_审中 为 1。

补丁_通过 为 2。

补丁_拒绝 为 3。

补丁_回滚 为 4。

补丁_创建 接受 id、目标文件、操作、旧内容、新内容：
    返还 一组：id、目标文件、操作、旧内容、新内容、补丁_暂存、一组：（空）。
完。

补丁_ID 接受 p：
    返还 p 的 第 0 项。
完。

补丁_目标 接受 p：
    返还 p 的 第 1 项。
完。

补丁_操作 接受 p：
    返还 p 的 第 2 项。
完。

补丁_状态 接受 p：
    返还 p 的 第 5 项。
完。

补丁_审查 接受 p：
    返还 p 的 第 6 项。
完。

审查_语义完整 为 0。

审查_安全门控 为 1。

审查_类型正确 为 2。

审查_风格合规 为 3。

审查_克隆安全 为 4。

审查结果_创建 接受 维度、通过、描述：
    返还 一组：维度、通过、描述。
完。

暂存区_创建 接受 零参：
    返还 一组：一组：（空）、0、0。
完。

暂存区_添加 接受 暂存、补丁：
    做 追加 用 暂存 的 第 0 项、补丁。
    暂存 的 第 1 项 为 暂存 的 第 1 项 加 1。
完。

补丁_执行审查 接受 补丁：
    补丁 的 第 5 项 为 补丁_审中。
    可变 语义通过 为 真。
    若 补丁 的 第 4 项 等于 「」 或者 补丁 的 第 4 项 等于 空：
        语义通过 为 假。
    完。
    做 追加 用 补丁 的 第 6 项、做 审查结果_创建 用 审查_语义完整、语义通过、「新内容非空」。
    安全通过 为 做 _检查安全模式 用 补丁 的 第 4 项。
    做 追加 用 补丁 的 第 6 项、做 审查结果_创建 用 审查_安全门控、安全通过、「安全模式检查」。
    类型通过 为 做 _检查Nova风格 用 补丁 的 第 4 项。
    做 追加 用 补丁 的 第 6 项、做 审查结果_创建 用 审查_类型正确、类型通过、「Nova风格检查」。
    克隆通过 为 做 _检查无外源注释 用 补丁 的 第 4 项。
    做 追加 用 补丁 的 第 6 项、做 审查结果_创建 用 审查_克隆安全、克隆通过、「无外源注释复制」。
    若 语义通过 并且 安全通过 并且 类型通过 并且 克隆通过：
        补丁 的 第 5 项 为 补丁_通过。
    否则：
        补丁 的 第 5 项 为 补丁_拒绝。
    完。
    返还 补丁。
完。

暂存区_批量审查 接受 暂存：
    可变 i 为 0。
    只要 i 小于 做 计数 用 暂存 的 第 0 项：
        做 补丁_执行审查 用 暂存 的 第 0 项 的 第 i 项。
        若 暂存 的 第 0 项 的 第 i 项 的 第 5 项 等于 补丁_通过：
            暂存 的 第 2 项 为 暂存 的 第 2 项 加 1。
        完。
        若 暂存 的 第 0 项 的 第 i 项 的 第 5 项 等于 补丁_拒绝：
            暂存 的 第 3 项 为 暂存 的 第 3 项 加 1。
        完。
        i 为 i 加 1。
    完。
完。

补丁_回滚 接受 补丁：
    补丁 的 第 5 项 为 补丁_回滚。
完。

暂存区_打印 接受 暂存：
    做 打印 用 「═══ 补丁评审报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  总补丁数: 」、做 转字符串 用 暂存 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  通过: 」、做 转字符串 用 暂存 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  拒绝: 」、做 转字符串 用 暂存 的 第 3 项。
    做 打印文本 用 做 字符串拼接 用 「  通过率: 」、做 字符串拼接 用 做 转字符串 用 暂存 的 第 2 项 乘 100 除以 (暂存 的 第 1 项 加 1)、「%」。
完。

_检查安全模式 接受 内容：
    若 内容 等于 空：
        返还 假。
    完。
    若 做 _含子串 用 内容、「系统调用(」：
        返还 假。
    完。
    若 做 _含子串 用 内容、「裸指针」：
        返还 假。
    完。
    返还 真。
完。

_检查Nova风格 接受 内容：
    若 内容 等于 空：
        返还 假。
    完。
    若 做 _含子串 用 内容、「函数 」 或者 做 _含子串 用 内容、「定义 」 或者 做 _含子串 用 内容、「如果 」 或者 做 _含子串 用 内容、「当 」：
        返还 真。
    完。
    若 做 _含子串 用 内容、「//」：
        返还 真。
    完。
    返还 假。
完。

_检查无外源注释 接受 内容：
    若 内容 等于 空：
        返还 真。
    完。
    若 做 _含子串 用 内容、「Copyright」 或者 做 _含子串 用 内容、「LICENSE」 或者 做 _含子串 用 内容、「All rights reserved」：
        返还 假。
    完。
    返还 真。
完。

_含子串 接受 s、子串：
    sl 为 做 字节长度 用 s。
    pl 为 做 字节长度 用 子串。
    若 pl 大于 sl：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于或等于 sl 减 pl：
        若 做 子串 用 s、i、pl 等于 子串：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

本章 收束。
