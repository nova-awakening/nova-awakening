《项目生成器》

引用《..·..·..·标准库·基础》。

引用《..·外源合同·外源项目合同》。

重置_严格原生 为 0。

重置_宿主兼容 为 1。

重置_混合桥接 为 2。

重置计划_创建 接受 源合同、语义图、profile：
    返还 一组：源合同、语义图、profile、一组：（空）、一组：（空）、一组：（空）。
完。

重置计划_源合同 接受 p：
    返还 p 的 第 0 项。
完。

重置计划_语义图 接受 p：
    返还 p 的 第 1 项。
完。

重置计划_Profile 接受 p：
    返还 p 的 第 2 项。
完。

重置计划_模块映射 接受 p：
    返还 p 的 第 3 项。
完。

重置计划_边界 接受 p：
    返还 p 的 第 4 项。
完。

重置计划_报告 接受 p：
    返还 p 的 第 5 项。
完。

模块映射_创建 接受 源路径、目标路径：
    返还 一组：源路径、目标路径、「待生成」、0。
完。

边界_FFI 为 0。

边界_网络 为 1。

边界_进程 为 2。

边界_文件 为 3。

边界声明_创建 接受 类型、名称、描述：
    返还 一组：类型、名称、描述。
完。

项目生成_执行 接受 计划、输出路径：
    可变 报告 为 一组：（空）。
    做 _生成项目描述 用 计划。
    做 追加 用 报告、一组：「project.nova」、「已生成」。
    做 _生成清单 用 计划。
    做 追加 用 报告、一组：「_manifest.txt」、「已生成」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 计划 的 第 3 项：
        映射 为 计划 的 第 3 项 的 第 i 项。
        做 追加 用 报告、一组：映射 的 第 1 项、映射 的 第 2 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 计划 的 第 4 项：
        边界 为 计划 的 第 4 项 的 第 i 项。
        做 追加 用 报告、一组：做 字符串拼接 用 「边界/」、边界 的 第 1 项、「已生成」。
        i 为 i 加 1。
    完。
    返还 报告。
完。

_生成项目描述 接受 计划：
    可变 内容 为 「# 原生重置项目\n」。
    内容 为 做 字符串拼接 用 内容、「# 由Nova跨语言原生重置工具生成\n\n」。
    源 为 计划 的 第 0 项。
    若 源 不等于 空：
        内容 为 做 字符串拼接 用 内容、做 字符串拼接 用 「项目名称: 」、做 字符串拼接 用 做 外源合同_名称 用 源、「_nova\n」。
        内容 为 做 字符串拼接 用 内容、「项目类型: hosted_app\n」。
    完。
    若 计划 的 第 2 项 等于 重置_严格原生：
        内容 为 做 字符串拼接 用 内容、「默认Profile: strict-native\n」。
    否则若 计划 的 第 2 项 等于 重置_宿主兼容：
        内容 为 做 字符串拼接 用 内容、「默认Profile: hosted-compatible\n」。
    否则：
        内容 为 做 字符串拼接 用 内容、「默认Profile: hybrid-bridge\n」。
    完。
    内容 为 做 字符串拼接 用 内容、「镜像类型: elf\n」。
    返还 内容。
完。

_生成清单 接受 计划：
    可变 清单 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 计划 的 第 3 项：
        清单 为 做 字符串拼接 用 清单、做 字符串拼接 用 计划 的 第 3 项 的 第 i 项 的 第 1 项、「\n」。
        i 为 i 加 1。
    完。
    返还 清单。
完。

项目生成_打印报告 接受 报告：
    做 打印 用 「═══ 项目生成报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  生成文件数: 」、做 转字符串 用 做 计数 用 报告。
    可变 i 为 0。
    只要 i 小于 做 计数 用 报告：
        做 打印文本 用 做 字符串拼接 用 「  」、做 字符串拼接 用 报告 的 第 i 项 的 第 0 项、做 字符串拼接 用 「 → 」、报告 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

本章 收束。
