《目录架构扫描》

引用《..·..·..·标准库·基础》。

节点_目录 为 0。

节点_源文件 为 1。

节点_配置 为 2。

节点_资源 为 3。

节点_测试 为 4。

节点_文档 为 5。

节点_忽略 为 6。

目录节点_创建 接受 类型、路径：
    返还 一组：类型、路径、一组：（空）、「」、0。
完。

目录节点_类型 接受 n：
    返还 n 的 第 0 项。
完。

目录节点_路径 接受 n：
    返还 n 的 第 1 项。
完。

目录节点_子节点 接受 n：
    返还 n 的 第 2 项。
完。

目录节点_扩展名 接受 n：
    返还 n 的 第 3 项。
完。

目录节点_行数 接受 n：
    返还 n 的 第 4 项。
完。

_推断文件类型 接受 路径、语言：
    若 做 _字符串含 用 路径、「test」 或者 做 _字符串含 用 路径、「spec」 或者 做 _字符串含 用 路径、「_test.go」：
        返还 节点_测试。
    完。
    若 做 _字符串尾 用 路径、「.toml」 或者 做 _字符串尾 用 路径、「.yaml」 或者 做 _字符串尾 用 路径、「.yml」 或者 做 _字符串尾 用 路径、「.json」 或者 做 _字符串尾 用 路径、「.xml」 或者 做 _字符串尾 用 路径、「.ini」：
        返还 节点_配置。
    完。
    若 做 _字符串尾 用 路径、「.md」 或者 做 _字符串尾 用 路径、「.txt」 或者 做 _字符串尾 用 路径、「.rst」：
        返还 节点_文档。
    完。
    若 语言 等于 1：
        若 做 _字符串尾 用 路径、「.rs」：
            返还 节点_源文件。
        完。
    完。
    若 语言 等于 2：
        若 做 _字符串尾 用 路径、「.go」：
            返还 节点_源文件。
        完。
    完。
    若 语言 等于 3 或者 语言 等于 4：
        若 做 _字符串尾 用 路径、「.c」 或者 做 _字符串尾 用 路径、「.h」 或者 做 _字符串尾 用 路径、「.cpp」 或者 做 _字符串尾 用 路径、「.hpp」 或者 做 _字符串尾 用 路径、「.cc」：
            返还 节点_源文件。
        完。
    完。
    若 语言 等于 5：
        若 做 _字符串尾 用 路径、「.py」：
            返还 节点_源文件。
        完。
    完。
    若 语言 等于 6：
        若 做 _字符串尾 用 路径、「.java」：
            返还 节点_源文件。
        完。
    完。
    若 做 _字符串尾 用 路径、「.png」 或者 做 _字符串尾 用 路径、「.jpg」 或者 做 _字符串尾 用 路径、「.svg」 或者 做 _字符串尾 用 路径、「.ico」 或者 做 _字符串尾 用 路径、「.wasm」 或者 做 _字符串尾 用 路径、「.bin」：
        返还 节点_资源。
    完。
    返还 节点_忽略。
完。

目录架构_统计 接受 根节点：
    可变 源文件数 为 0。
    可变 测试数 为 0。
    可变 配置数 为 0。
    可变 资源数 为 0。
    可变 总行数 为 0。
    做 _统计递归 用 根节点、源文件数、测试数、配置数、资源数、总行数。
    返还 一组：源文件数、测试数、配置数、资源数、总行数。
完。

_统计递归 接受 节点、源文件数、测试数、配置数、资源数、总行数：
    若 节点 的 第 0 项 等于 节点_源文件：
        源文件数 为 源文件数 加 1。
        总行数 为 总行数 加 节点 的 第 4 项。
    完。
    若 节点 的 第 0 项 等于 节点_测试：
        测试数 为 测试数 加 1。
    完。
    若 节点 的 第 0 项 等于 节点_配置：
        配置数 为 配置数 加 1。
    完。
    若 节点 的 第 0 项 等于 节点_资源：
        资源数 为 资源数 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点 的 第 2 项：
        做 _统计递归 用 节点 的 第 2 项 的 第 i 项、源文件数、测试数、配置数、资源数、总行数。
        i 为 i 加 1。
    完。
完。

目录架构_打印报告 接受 统计：
    做 打印 用 「═══ 目录架构报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  源文件数: 」、做 转字符串 用 统计 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  测试文件数: 」、做 转字符串 用 统计 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  配置文件数: 」、做 转字符串 用 统计 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  资源文件数: 」、做 转字符串 用 统计 的 第 3 项。
    做 打印文本 用 做 字符串拼接 用 「  估计总行数: 」、做 转字符串 用 统计 的 第 4 项。
完。

_字符串含 接受 s、子串：
    sl 为 做 字节长度 用 s。
    pl 为 做 字节长度 用 子串。
    若 pl 大于 sl：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于或等于 sl 减 pl：
        若 做 子串 用 s、i、pl 等于 子串：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

_字符串尾 接受 s、后缀：
    sl 为 做 字节长度 用 s。
    pl 为 做 字节长度 用 后缀。
    若 pl 大于 sl：
        返还 假。
    完。
    返还 做 子串 用 s、sl 减 pl、pl 等于 后缀。
完。

本章 收束。
