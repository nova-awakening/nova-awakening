《工程探测器》

引用《..·..·..·标准库·基础》。

源语言_未知 为 0。

源语言_Rust 为 1。

源语言_Go 为 2。

源语言_C 为 3。

源语言_Cpp 为 4。

源语言_Python 为 5。

源语言_Java 为 6。

构建系统_未知 为 0。

构建系统_Cargo 为 1。

构建系统_GoMod 为 2。

构建系统_CMake 为 3。

构建系统_Make 为 4。

构建系统_PyProject 为 5。

构建系统_Maven 为 6。

构建系统_Gradle 为 7。

探测结果_创建 接受 零参：
    返还 一组：源语言_未知、构建系统_未知、「」、一组：（空）、一组：（空）、一组：（空）。
完。

探测结果_语言 接受 r：
    返还 r 的 第 0 项。
完。

探测结果_构建系统 接受 r：
    返还 r 的 第 1 项。
完。

探测结果_工作区根 接受 r：
    返还 r 的 第 2 项。
完。

探测结果_包根列表 接受 r：
    返还 r 的 第 3 项。
完。

探测结果_入口列表 接受 r：
    返还 r 的 第 4 项。
完。

探测结果_诊断 接受 r：
    返还 r 的 第 5 项。
完。

源语言_名称 接受 id：
    若 id 等于 源语言_Rust：
        返还 「rust」。
    完。
    若 id 等于 源语言_Go：
        返还 「go」。
    完。
    若 id 等于 源语言_C：
        返还 「c」。
    完。
    若 id 等于 源语言_Cpp：
        返还 「cpp」。
    完。
    若 id 等于 源语言_Python：
        返还 「python」。
    完。
    若 id 等于 源语言_Java：
        返还 「java」。
    完。
    返还 「unknown」。
完。

构建系统_名称 接受 id：
    若 id 等于 构建系统_Cargo：
        返还 「cargo」。
    完。
    若 id 等于 构建系统_GoMod：
        返还 「go」。
    完。
    若 id 等于 构建系统_CMake：
        返还 「cmake」。
    完。
    若 id 等于 构建系统_Make：
        返还 「make」。
    完。
    若 id 等于 构建系统_PyProject：
        返还 「pyproject」。
    完。
    若 id 等于 构建系统_Maven：
        返还 「maven」。
    完。
    若 id 等于 构建系统_Gradle：
        返还 「gradle」。
    完。
    返还 「unknown」。
完。

工程探测 接受 项目路径：
    结果 为 做 探测结果_创建。
    结果 的 第 2 项 为 项目路径。
    _cargo 为 做 读文件 用 做 字符串拼接 用 项目路径、「/Cargo.toml」。
    若 _cargo 不等于 空：
        结果 的 第 0 项 为 源语言_Rust。
        结果 的 第 1 项 为 构建系统_Cargo。
        做 _探测_Rust入口 用 项目路径、结果。
        返还 结果。
    完。
    _gomod 为 做 读文件 用 做 字符串拼接 用 项目路径、「/go.mod」。
    若 _gomod 不等于 空：
        结果 的 第 0 项 为 源语言_Go。
        结果 的 第 1 项 为 构建系统_GoMod。
        做 _探测_Go入口 用 项目路径、结果。
        返还 结果。
    完。
    _pyproj 为 做 读文件 用 做 字符串拼接 用 项目路径、「/pyproject.toml」。
    若 _pyproj 不等于 空：
        结果 的 第 0 项 为 源语言_Python。
        结果 的 第 1 项 为 构建系统_PyProject。
        返还 结果。
    完。
    _pom 为 做 读文件 用 做 字符串拼接 用 项目路径、「/pom.xml」。
    若 _pom 不等于 空：
        结果 的 第 0 项 为 源语言_Java。
        结果 的 第 1 项 为 构建系统_Maven。
        返还 结果。
    完。
    _gradle 为 做 读文件 用 做 字符串拼接 用 项目路径、「/build.gradle」。
    若 _gradle 不等于 空：
        结果 的 第 0 项 为 源语言_Java。
        结果 的 第 1 项 为 构建系统_Gradle。
        返还 结果。
    完。
    _cmake 为 做 读文件 用 做 字符串拼接 用 项目路径、「/CMakeLists.txt」。
    若 _cmake 不等于 空：
        结果 的 第 0 项 为 源语言_C。
        结果 的 第 1 项 为 构建系统_CMake。
        返还 结果。
    完。
    _makefile 为 做 读文件 用 做 字符串拼接 用 项目路径、「/Makefile」。
    若 _makefile 不等于 空：
        结果 的 第 0 项 为 源语言_C。
        结果 的 第 1 项 为 构建系统_Make。
        返还 结果。
    完。
    做 追加 用 结果 的 第 5 项、一组：「error」、「无法识别工程语言类型」。
    返还 结果。
完。

_探测_Rust入口 接受 根、结果：
    _主入口 为 做 读文件 用 做 字符串拼接 用 根、「/src/main.rs」。
    若 _主入口 不等于 空：
        做 追加 用 结果 的 第 4 项、「src/main.rs」。
    完。
    _lib 为 做 读文件 用 做 字符串拼接 用 根、「/src/lib.rs」。
    若 _lib 不等于 空：
        做 追加 用 结果 的 第 4 项、「src/lib.rs」。
    完。
    做 追加 用 结果 的 第 3 项、根。
完。

_探测_Go入口 接受 根、结果：
    _主入口 为 做 读文件 用 做 字符串拼接 用 根、「/main.go」。
    若 _主入口 不等于 空：
        做 追加 用 结果 的 第 4 项、「main.go」。
    完。
    _cmd 为 做 读文件 用 做 字符串拼接 用 根、「/cmd/main.go」。
    若 _cmd 不等于 空：
        做 追加 用 结果 的 第 4 项、「cmd/main.go」。
    完。
    做 追加 用 结果 的 第 3 项、根。
完。

探测结果_打印 接受 r：
    做 打印 用 「═══ 工程探测报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  语言: 」、做 源语言_名称 用 r 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  构建系统: 」、做 构建系统_名称 用 r 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  工作区根: 」、r 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  包根数: 」、做 转字符串 用 做 计数 用 r 的 第 3 项。
    做 打印文本 用 做 字符串拼接 用 「  入口数: 」、做 转字符串 用 做 计数 用 r 的 第 4 项。
    若 做 计数 用 r 的 第 5 项 大于 0：
        做 打印文本 用 做 字符串拼接 用 「  诊断数: 」、做 转字符串 用 做 计数 用 r 的 第 5 项。
    完。
完。

本章 收束。
