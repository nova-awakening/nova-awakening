《智能寄存器分配》

引用《..·..·标准库·基础》。

RA_精度 为 10000。

RA_寄存器数 为 16。

RA_可分配 为 13。

RA_溢出代价_基础 为 100。

RA_循环加权 为 10。

可变 移除数 为 0。

可变 找到 为 0。

活跃区间_创建 接受 变量名、起始、结束：
    返还 一组：变量名、起始、结束、负 1、0、0、0、一组：（空）、0、一组：（空）。
完。

活跃区间_添加使用 接受 区间、点、循环深度：
    做 追加 用 区间 的 第 7 项、点。
    区间 的 第 5 项 为 区间 的 第 5 项 加 1。
    若 循环深度 大于 区间 的 第 6 项：
        区间 的 第 6 项 为 循环深度。
    完。
    若 点 大于 区间 的 第 2 项：
        区间 的 第 2 项 为 点。
    完。
完。

区间_重叠 接受 A、B：
    若 A 的 第 2 项 小于 B 的 第 1 项 或者 B 的 第 2 项 小于 A 的 第 1 项：
        返还 0。
    完。
    返还 1。
完。

计算溢出代价 接受 区间：
    基础 为 区间 的 第 5 项 乘 RA_溢出代价_基础。
    可变 循环因子 为 1。
    可变 d 为 0。
    只要 d 小于 区间 的 第 6 项：
        循环因子 为 循环因子 乘 RA_循环加权。
        d 为 d 加 1。
    完。
    代价 为 基础 乘 循环因子。
    区间 的 第 8 项 为 代价。
    返还 代价。
完。

干涉图_构建 接受 区间列表：
    n 为 做 计数 用 区间列表。
    可变 邻接 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 n：
            做 追加 用 行、0。
            j 为 j 加 1。
        完。
        做 追加 用 邻接、行。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 做 区间_重叠 用 区间列表 的 第 i 项、区间列表 的 第 j 项：
                邻接 的 第 i 项 的 第 j 项 为 1。
                邻接 的 第 j 项 的 第 i 项 为 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 邻接。
完。

干涉图_度数 接受 邻接、节点、n：
    可变 度 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 邻接 的 第 节点 项 的 第 i 项 大于 0：
            度 为 度 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 度。
完。

图着色_分配 接受 区间列表、邻接：
    n 为 做 计数 用 区间列表。
    可变 栈 为 一组：（空）。
    可变 已移除 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 已移除、0。
        i 为 i 加 1。
    完。
    移除数 为 0。
    只要 移除数 小于 n：
        找到 为 0。
        i 为 0。
        只要 i 小于 n：
            若 已移除 的 第 i 项 等于 0：
                可变 有效度 为 0。
                可变 j 为 0。
                只要 j 小于 n：
                    若 邻接 的 第 i 项 的 第 j 项 大于 0 并且 已移除 的 第 j 项 等于 0：
                        有效度 为 有效度 加 1。
                    完。
                    j 为 j 加 1。
                完。
                若 有效度 小于 RA_可分配：
                    做 追加 用 栈、i。
                    已移除 的 第 i 项 为 1。
                    移除数 为 移除数 加 1。
                    找到 为 1。
                完。
            完。
            i 为 i 加 1。
        完。
        若 找到 等于 0：
            可变 最低代价 为 999999999。
            可变 溢出节点 为 负 1。
            可变 si 为 0。
            只要 si 小于 n：
                若 已移除 的 第 si 项 等于 0：
                    代价 为 做 计算溢出代价 用 区间列表 的 第 si 项。
                    若 代价 小于 最低代价：
                        最低代价 为 代价。
                        溢出节点 为 si。
                    完。
                完。
                si 为 si 加 1。
            完。
            若 溢出节点 大于或等于 0：
                做 追加 用 栈、溢出节点。
                已移除 的 第 溢出节点 项 为 1。
                移除数 为 移除数 加 1。
                区间列表 的 第 溢出节点 项 的 第 4 项 为 1。
            完。
        完。
    完。
    可变 颜色 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 颜色、负 1。
        i 为 i 加 1。
    完。
    可变 s 为 做 计数 用 栈 减 1。
    只要 s 大于或等于 0：
        节点 为 栈 的 第 s 项。
        可变 已用 为 一组：（空）。
        可变 k 为 0。
        只要 k 小于 RA_可分配：
            做 追加 用 已用、0。
            k 为 k 加 1。
        完。
        可变 j 为 0。
        只要 j 小于 n：
            若 邻接 的 第 节点 项 的 第 j 项 大于 0 并且 颜色 的 第 j 项 大于或等于 0 并且 颜色 的 第 j 项 小于 RA_可分配：
                已用 的 第 颜色 的 第 j 项 项 为 1。
            完。
            j 为 j 加 1。
        完。
        可变 选色 为 负 1。
        k 为 0。
        只要 k 小于 RA_可分配：
            若 已用 的 第 k 项 等于 0：
                选色 为 k。
                k 为 RA_可分配。
            完。
            k 为 k 加 1。
        完。
        若 选色 大于或等于 0：
            颜色 的 第 节点 项 为 选色。
            区间列表 的 第 节点 项 的 第 3 项 为 选色。
            区间列表 的 第 节点 项 的 第 4 项 为 0。
        否则：
            区间列表 的 第 节点 项 的 第 4 项 为 1。
        完。
        s 为 s 减 1。
    完。
    返还 区间列表。
完。

线性扫描_分配 接受 区间列表：
    n 为 做 计数 用 区间列表。
    可变 新池 为 一组：（空）。
    可变 排序索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 排序索引、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n 减 1：
        可变 最小 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 区间列表 的 第 排序索引 的 第 j 项 项 的 第 1 项 小于 区间列表 的 第 排序索引 的 第 最小 项 项 的 第 1 项：
                最小 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最小 不等于 i：
            暂存 为 排序索引 的 第 i 项。
            排序索引 的 第 i 项 为 排序索引 的 第 最小 项。
            排序索引 的 第 最小 项 为 暂存。
        完。
        i 为 i 加 1。
    完。
    可变 活跃集 为 一组：（空）。
    可变 空闲池 为 一组：（空）。
    i 为 0。
    只要 i 小于 RA_可分配：
        做 追加 用 空闲池、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        索引 为 排序索引 的 第 i 项。
        区间 为 区间列表 的 第 索引 项。
        起始 为 区间 的 第 1 项。
        可变 k 为 0。
        只要 k 小于 做 计数 用 活跃集：
            活跃idx 为 活跃集 的 第 k 项。
            若 区间列表 的 第 活跃idx 项 的 第 2 项 小于 起始：
                释放寄存器 为 区间列表 的 第 活跃idx 项 的 第 3 项。
                若 释放寄存器 大于或等于 0：
                    做 追加 用 空闲池、释放寄存器。
                完。
                最后 为 做 计数 用 活跃集 减 1。
                若 k 小于 最后：
                    活跃集 的 第 k 项 为 活跃集 的 第 最后 项。
                完。
                可变 新活跃 为 一组：（空）。
                可变 m 为 0。
                只要 m 小于 做 计数 用 活跃集 减 1：
                    做 追加 用 新活跃、活跃集 的 第 m 项。
                    m 为 m 加 1。
                完。
                活跃集 为 新活跃。
            否则：
                k 为 k 加 1。
            完。
        完。
        若 做 计数 用 空闲池 大于 0：
            寄存器 为 空闲池 的 第 做 计数 用 空闲池 减 1 项。
            可变 p 为 0。
            只要 p 小于 做 计数 用 空闲池 减 1：
                做 追加 用 新池、空闲池 的 第 p 项。
                p 为 p 加 1。
            完。
            空闲池 为 新池。
            区间 的 第 3 项 为 寄存器。
            做 追加 用 活跃集、索引。
        否则：
            可变 最晚idx 为 负 1。
            可变 最晚结束 为 负 1。
            k 为 0。
            只要 k 小于 做 计数 用 活跃集：
                aidx 为 活跃集 的 第 k 项。
                若 区间列表 的 第 aidx 项 的 第 2 项 大于 最晚结束：
                    最晚结束 为 区间列表 的 第 aidx 项 的 第 2 项。
                    最晚idx 为 k。
                完。
                k 为 k 加 1。
            完。
            若 最晚idx 大于或等于 0 并且 最晚结束 大于 区间 的 第 2 项：
                溢出区间 为 区间列表 的 第 活跃集 的 第 最晚idx 项 项。
                区间 的 第 3 项 为 溢出区间 的 第 3 项。
                溢出区间 的 第 3 项 为 负 1。
                溢出区间 的 第 4 项 为 1。
                活跃集 的 第 最晚idx 项 为 索引。
            否则：
                区间 的 第 4 项 为 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 区间列表。
完。

评估分配质量 接受 区间列表：
    n 为 做 计数 用 区间列表。
    可变 总变量 为 n。
    可变 已分配 为 0。
    可变 溢出数 为 0。
    可变 总溢出代价 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 区间列表 的 第 i 项 的 第 3 项 大于或等于 0：
            已分配 为 已分配 加 1。
        完。
        若 区间列表 的 第 i 项 的 第 4 项 大于 0：
            溢出数 为 溢出数 加 1。
            总溢出代价 为 总溢出代价 加 区间列表 的 第 i 项 的 第 8 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：总变量、已分配、溢出数、总溢出代价、已分配 乘 RA_精度 除以 (总变量 加 1)。
完。

RA_线性扫描 接受 活跃区间、寄存器数：
    可变 分配表 为 一组：（空）。
    可变 活跃寄存器 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 活跃区间：
        可变 新活跃 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 做 计数 用 活跃寄存器：
            若 活跃寄存器 的 第 j 项 的 第 1 项 大于 活跃区间 的 第 i 项 的 第 0 项：
                做 追加 用 新活跃、活跃寄存器 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        活跃寄存器 为 新活跃。
        若 做 计数 用 活跃寄存器 小于 寄存器数：
            做 追加 用 分配表、一组：i、做 计数 用 活跃寄存器。
            做 追加 用 活跃寄存器、活跃区间 的 第 i 项。
        否则：
            做 追加 用 分配表、一组：i、负 1。
        完。
        i 为 i 加 1。
    完。
    返还 分配表。
完。

RA_溢出代价 接受 _变量、循环深度：
    返还 (循环深度 加 1) 乘 10。
完。

RA_合并候选 接受 变量A、变量B、冲突图：
    可变 i 为 0。
    只要 i 小于 做 计数 用 冲突图：
        若 冲突图 的 第 i 项 的 第 0 项 等于 变量A 并且 冲突图 的 第 i 项 的 第 1 项 等于 变量B：
            返还 假。
        完。
        若 冲突图 的 第 i 项 的 第 0 项 等于 变量B 并且 冲突图 的 第 i 项 的 第 1 项 等于 变量A：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

RA_着色阶数 接受 _变量、冲突图：
    可变 度 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 冲突图：
        若 冲突图 的 第 i 项 的 第 0 项 等于 _变量 或者 冲突图 的 第 i 项 的 第 1 项 等于 _变量：
            度 为 度 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 度。
完。

可变 _RA_分配次数 为 0。

可变 _RA_溢出次数 为 0。

可变 _RA_合并次数 为 0。

RA_记录分配 接受 零参：
    _RA_分配次数 为 _RA_分配次数 加 1。
完。

RA_记录溢出 接受 零参：
    _RA_溢出次数 为 _RA_溢出次数 加 1。
完。

RA_记录合并 接受 零参：
    _RA_合并次数 为 _RA_合并次数 加 1。
完。

RA_溢出率 接受 零参：
    若 _RA_分配次数 等于 0：
        返还 0。
    完。
    返还 _RA_溢出次数 乘 100 除以 _RA_分配次数。
完。

RA_质量评估 接受 零参：
    率 为 做 RA_溢出率。
    若 率 大于 30：
        返还 「溢出严重(增加寄存器或优化活跃范围)」。
    完。
    若 率 大于 10：
        返还 「溢出偏多」。
    完。
    若 率 大于 0：
        返还 「少量溢出」。
    完。
    返还 「零溢出(最优)」。
完。

RA_重置 接受 零参：
    _RA_分配次数 为 0。
    _RA_溢出次数 为 0。
    _RA_合并次数 为 0。
完。

RA_重命名检测 接受 指令列表：
    可变 可重命名 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 指令列表：
        若 指令列表 的 第 i 项 的 第 0 项 等于 指令列表 的 第 i 减 1 项 的 第 0 项：
            可重命名 为 可重命名 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 可重命名。
完。

RA_活跃范围 接受 定义点、使用点列表：
    可变 最远 为 定义点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 使用点列表：
        若 使用点列表 的 第 i 项 大于 最远：
            最远 为 使用点列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：定义点、最远。
完。

RA_推荐寄存器数 接受 变量数、循环深度：
    基础 为 8。
    若 变量数 大于 30：
        返还 基础 加 8。
    完。
    若 循环深度 大于 3：
        返还 基础 加 4。
    完。
    返还 基础。
完。

RA_是否最优 接受 零参：
    返还 做 RA_溢出率 等于 0。
完。

RA_效率分 接受 零参：
    返还 100 减 做 RA_溢出率。
完。

RA_精确溢出代价 接受 区间：
    使用次数 为 区间 的 第 5 项。
    循环深度 为 区间 的 第 6 项。
    区间长度 为 区间 的 第 2 项 减 区间 的 第 1 项 加 1。
    基础代价 为 使用次数 乘 2。
    可变 循环权重 为 1。
    可变 i 为 0。
    只要 i 小于 循环深度：
        循环权重 为 循环权重 乘 10。
        若 循环权重 大于 10000：
            循环权重 为 10000。
        完。
        i 为 i 加 1。
    完。
    可变 长度惩罚 为 1。
    若 区间长度 大于 100：
        长度惩罚 为 2。
    完。
    精确代价 为 基础代价 乘 循环权重 乘 长度惩罚。
    区间 的 第 8 项 为 精确代价。
    返还 精确代价。
完。

RA_选择最优溢出目标 接受 活跃区间列表：
    若 做 计数 用 活跃区间列表 等于 0：
        返还 负 1。
    完。
    可变 最小代价 为 负 1。
    可变 最优索引 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 活跃区间列表：
        代价 为 做 RA_精确溢出代价 用 活跃区间列表 的 第 i 项。
        若 最小代价 等于 负 1 或者 代价 小于 最小代价：
            最小代价 为 代价。
            最优索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最优索引。
完。

RA_应该保护不溢出 接受 区间、临界循环深度：
    返还 区间 的 第 6 项 大于或等于 临界循环深度。
完。

RA_溢出决策报告 接受 区间列表：
    做 打印 用 「[寄存器分配] 溢出代价分析:」。
    可变 热点数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 区间列表：
        代价 为 做 RA_精确溢出代价 用 区间列表 的 第 i 项。
        若 做 RA_应该保护不溢出 用 区间列表 的 第 i 项、2：
            热点数 为 热点数 加 1。
            做 打印 用 「  热点变量 」 加 做 转字符串 用 区间列表 的 第 i 项 的 第 0 项 加 「 循环深度=」 加 做 转字符串 用 区间列表 的 第 i 项 的 第 6 项 加 「 代价=」 加 做 转字符串 用 代价 加 「 [保护]」。
        完。
        i 为 i 加 1。
    完。
    做 打印 用 「  热点变量: 」 加 做 转字符串 用 热点数 加 「 个（优先分配寄存器）」。
完。

本章 收束。
