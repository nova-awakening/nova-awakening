《自适应优化器》

引用《..·..·标准库·基础》。

引用《..·..·编译器·标记常量》。

OPT_精度 为 10000。

特征_循环深度 为 0。

特征_分支密度 为 1。

特征_内存访问 为 2。

特征_函数调用 为 3。

特征_数据依赖 为 4。

特征_并行度 为 5。

特征_代码大小 为 6。

特征_寄存器压力 为 7。

特征_指令混合 为 8。

特征_跳转密度 为 9。

特征维度 为 10。

特征向量_创建 接受 零参：
    可变 向量 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 特征维度：
        做 追加 用 向量、0。
        i 为 i 加 1。
    完。
    返还 向量。
完。

提取程序特征 接受 节点列表：
    可变 特征 为 做 特征向量_创建。
    可变 循环深度 为 0。
    可变 最大循环深度 为 0。
    可变 分支数 为 0。
    可变 内存操作 为 0。
    可变 调用数 为 0。
    可变 总节点 为 做 计数 用 节点列表。
    可变 i 为 0。
    只要 i 小于 总节点：
        节点 为 节点列表 的 第 i 项。
        类型 为 节点 的 第 0 项。
        若 类型 等于 标记_标识符：
            循环深度 为 循环深度 加 1。
            若 循环深度 大于 最大循环深度：
                最大循环深度 为 循环深度。
            完。
        完。
        若 类型 等于 21：
            循环深度 为 循环深度 减 1。
        完。
        若 类型 等于 10 或者 类型 等于 11：
            分支数 为 分支数 加 1。
        完。
        若 类型 等于 标记_加 或者 类型 等于 标记_减：
            内存操作 为 内存操作 加 1。
        完。
        若 类型 等于 标记_等等：
            调用数 为 调用数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 总节点 大于 0：
        特征 的 第 特征_循环深度 项 为 最大循环深度 乘 OPT_精度 除以 10。
        特征 的 第 特征_分支密度 项 为 分支数 乘 OPT_精度 除以 总节点。
        特征 的 第 特征_内存访问 项 为 内存操作 乘 OPT_精度 除以 总节点。
        特征 的 第 特征_函数调用 项 为 调用数 乘 OPT_精度 除以 总节点。
        特征 的 第 特征_代码大小 项 为 总节点。
    完。
    返还 特征。
完。

策略_无优化 为 0。

策略_常量折叠 为 1。

策略_死代码消除 为 2。

策略_循环展开 为 3。

策略_循环向量化 为 4。

策略_函数内联 为 5。

策略_尾调用优化 为 6。

策略_强度削减 为 7。

策略_寄存器分配 为 8。

策略_指令调度 为 9。

策略数 为 10。

代价模型_创建 接受 零参：
    可变 权重 为 一组：（空）。
    可变 s 为 0。
    只要 s 小于 策略数：
        可变 行 为 一组：（空）。
        可变 f 为 0。
        只要 f 小于 特征维度：
            可变 w 为 0。
            若 s 等于 策略_循环展开 并且 f 等于 特征_循环深度：
                w 为 8000。
            完。
            若 s 等于 策略_循环向量化 并且 f 等于 特征_循环深度：
                w 为 9000。
            完。
            若 s 等于 策略_死代码消除 并且 f 等于 特征_分支密度：
                w 为 7000。
            完。
            若 s 等于 策略_函数内联 并且 f 等于 特征_函数调用：
                w 为 8500。
            完。
            若 s 等于 策略_尾调用优化 并且 f 等于 特征_函数调用：
                w 为 7500。
            完。
            若 s 等于 策略_寄存器分配 并且 f 等于 特征_内存访问：
                w 为 9000。
            完。
            若 s 等于 策略_常量折叠：
                w 为 w 加 3000。
            完。
            做 追加 用 行、w。
            f 为 f 加 1。
        完。
        做 追加 用 权重、行。
        s 为 s 加 1。
    完。
    返还 一组：权重、一组：（空）、0、0。
完。

代价模型_预测 接受 模型、特征、策略：
    权重 为 模型 的 第 0 项。
    行 为 权重 的 第 策略 项。
    可变 分数 为 0。
    可变 f 为 0。
    只要 f 小于 特征维度：
        分数 为 分数 加 行 的 第 f 项 乘 特征 的 第 f 项 除以 OPT_精度。
        f 为 f 加 1。
    完。
    返还 分数。
完。

代价模型_选择策略 接受 模型、特征：
    可变 策略排名 为 一组：（空）。
    可变 s 为 0。
    只要 s 小于 策略数：
        分数 为 做 代价模型_预测 用 模型、特征、s。
        做 追加 用 策略排名、一组：s、分数。
        s 为 s 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 策略数 减 1：
        可变 最大idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 策略数：
            若 策略排名 的 第 j 项 的 第 1 项 大于 策略排名 的 第 最大idx 项 的 第 1 项：
                最大idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最大idx 不等于 i：
            暂存 为 策略排名 的 第 i 项。
            策略排名 的 第 i 项 为 策略排名 的 第 最大idx 项。
            策略排名 的 第 最大idx 项 为 暂存。
        完。
        i 为 i 加 1。
    完。
    可变 选中 为 一组：（空）。
    i 为 0。
    只要 i 小于 5 并且 i 小于 策略数：
        若 策略排名 的 第 i 项 的 第 1 项 大于 0：
            做 追加 用 选中、策略排名 的 第 i 项 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 选中。
完。

代价模型_反馈 接受 模型、特征、策略、实际收益：
    预测收益 为 做 代价模型_预测 用 模型、特征、策略。
    误差 为 实际收益 减 预测收益。
    做 追加 用 模型 的 第 1 项、一组：特征、策略、实际收益。
    模型 的 第 2 项 为 模型 的 第 2 项 加 1。
    可变 绝对误差 为 误差。
    若 绝对误差 小于 0：
        绝对误差 为 0 减 绝对误差。
    完。
    模型 的 第 3 项 为 模型 的 第 3 项 加 绝对误差。
    学习率 为 100。
    权重 为 模型 的 第 0 项。
    可变 f 为 0。
    只要 f 小于 特征维度：
        梯度 为 误差 乘 特征 的 第 f 项 除以 OPT_精度。
        权重 的 第 策略 项 的 第 f 项 为 权重 的 第 策略 项 的 第 f 项 加 学习率 乘 梯度 除以 OPT_精度。
        f 为 f 加 1。
    完。
    返还 误差。
完。

代价模型_准确度 接受 模型：
    若 模型 的 第 2 项 等于 0：
        返还 OPT_精度。
    完。
    平均误差 为 模型 的 第 3 项 除以 模型 的 第 2 项。
    若 平均误差 大于 OPT_精度：
        返还 0。
    完。
    返还 OPT_精度 减 平均误差。
完。

评估策略序列 接受 模型、特征、策略序列：
    可变 总收益 为 0。
    可变 当前特征 为 特征。
    可变 i 为 0。
    只要 i 小于 做 计数 用 策略序列：
        收益 为 做 代价模型_预测 用 模型、当前特征、策略序列 的 第 i 项。
        总收益 为 总收益 加 收益。
        若 策略序列 的 第 i 项 等于 策略_死代码消除：
            当前特征 的 第 特征_代码大小 项 为 当前特征 的 第 特征_代码大小 项 乘 8500 除以 OPT_精度。
        完。
        若 策略序列 的 第 i 项 等于 策略_函数内联：
            当前特征 的 第 特征_代码大小 项 为 当前特征 的 第 特征_代码大小 项 乘 12000 除以 OPT_精度。
            当前特征 的 第 特征_函数调用 项 为 当前特征 的 第 特征_函数调用 项 乘 3000 除以 OPT_精度。
        完。
        若 策略序列 的 第 i 项 等于 策略_循环展开：
            当前特征 的 第 特征_代码大小 项 为 当前特征 的 第 特征_代码大小 项 乘 14000 除以 OPT_精度。
            当前特征 的 第 特征_循环深度 项 为 当前特征 的 第 特征_循环深度 项 乘 5000 除以 OPT_精度。
        完。
        i 为 i 加 1。
    完。
    返还 总收益。
完。

贪心策略搜索 接受 模型、特征、最大步数：
    可变 序列 为 一组：（空）。
    可变 已用 为 一组：（空）。
    可变 步 为 0。
    只要 步 小于 最大步数：
        可变 最优策略 为 负 1。
        可变 最优收益 为 0。
        可变 s 为 0。
        只要 s 小于 策略数：
            可变 已用标 为 0。
            可变 k 为 0。
            只要 k 小于 做 计数 用 已用：
                若 已用 的 第 k 项 等于 s：
                    已用标 为 1。
                完。
                k 为 k 加 1。
            完。
            若 已用标 等于 0：
                试序列 为 一组：（空）。
                可变 j 为 0。
                只要 j 小于 做 计数 用 序列：
                    做 追加 用 试序列、序列 的 第 j 项。
                    j 为 j 加 1。
                完。
                做 追加 用 试序列、s。
                收益 为 做 评估策略序列 用 模型、特征、试序列。
                若 收益 大于 最优收益：
                    最优收益 为 收益。
                    最优策略 为 s。
                完。
            完。
            s 为 s 加 1。
        完。
        若 最优策略 大于或等于 0 并且 最优收益 大于 0：
            做 追加 用 序列、最优策略。
            做 追加 用 已用、最优策略。
        否则：
            返还 序列。
        完。
        步 为 步 加 1。
    完。
    返还 序列。
完。

热点分析 接受 函数列表：
    可变 热点 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 函数列表：
        函数信息 为 函数列表 的 第 i 项。
        特征 为 做 提取程序特征 用 函数信息。
        可变 热度 为 特征 的 第 特征_循环深度 项 乘 特征 的 第 特征_代码大小 项 除以 OPT_精度。
        热度 为 热度 加 特征 的 第 特征_函数调用 项 乘 2。
        做 追加 用 热点、一组：i、热度。
        i 为 i 加 1。
    完。
    可变 a 为 0。
    只要 a 小于 做 计数 用 热点 减 1：
        可变 最大 为 a。
        可变 b 为 a 加 1。
        只要 b 小于 做 计数 用 热点：
            若 热点 的 第 b 项 的 第 1 项 大于 热点 的 第 最大 项 的 第 1 项：
                最大 为 b。
            完。
            b 为 b 加 1。
        完。
        若 最大 不等于 a：
            暂存 为 热点 的 第 a 项。
            热点 的 第 a 项 为 热点 的 第 最大 项。
            热点 的 第 最大 项 为 暂存。
        完。
        a 为 a 加 1。
    完。
    返还 热点。
完。

分配优化预算 接受 热点列表、总预算：
    若 做 计数 用 热点列表 等于 0：
        返还 一组：（空）。
    完。
    可变 总热度 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 热点列表：
        总热度 为 总热度 加 热点列表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    若 总热度 等于 0：
        总热度 为 1。
    完。
    可变 预算分配 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 热点列表：
        可变 份额 为 热点列表 的 第 i 项 的 第 1 项 乘 总预算 除以 总热度。
        最小预算 为 总预算 除以 (做 计数 用 热点列表 乘 10)。
        若 份额 小于 最小预算：
            份额 为 最小预算。
        完。
        做 追加 用 预算分配、一组：热点列表 的 第 i 项 的 第 0 项、份额。
        i 为 i 加 1。
    完。
    返还 预算分配。
完。

优化_策略推荐 接受 代码大小、循环深度、函数调用数：
    若 循环深度 大于 5：
        返还 「循环展开+向量化」。
    完。
    若 函数调用数 大于 100：
        返还 「内联优先」。
    完。
    若 代码大小 大于 10000：
        返还 「分阶段优化」。
    完。
    返还 「标准优化」。
完。

优化_内联决策 接受 函数大小、调用次数、热度：
    若 函数大小 小于 10 并且 调用次数 大于 5：
        返还 真。
    完。
    若 函数大小 小于 50 并且 热度 大于 80：
        返还 真。
    完。
    若 函数大小 小于 5：
        返还 真。
    完。
    返还 假。
完。

优化_循环展开决策 接受 迭代次数、循环体大小：
    若 迭代次数 小于或等于 4：
        返还 迭代次数。
    完。
    若 循环体大小 小于 10：
        返还 4。
    完。
    若 循环体大小 小于 50：
        返还 2。
    完。
    返还 1。
完。

优化_向量化可行性 接受 迭代次数、依赖深度：
    若 依赖深度 大于 1：
        返还 假。
    完。
    若 迭代次数 小于 4：
        返还 假。
    完。
    返还 真。
完。

优化_寄存器压力 接受 变量数、可用寄存器：
    若 变量数 大于 可用寄存器 乘 2：
        返还 「严重溢出」。
    完。
    若 变量数 大于 可用寄存器：
        返还 「轻微溢出」。
    完。
    返还 「无压力」。
完。

可变 _自适应_优化轮次 为 0。

可变 _自适应_内联数 为 0。

可变 _自适应_展开数 为 0。

自适应_记录优化 接受 零参：
    _自适应_优化轮次 为 _自适应_优化轮次 加 1。
完。

自适应_记录内联 接受 零参：
    _自适应_内联数 为 _自适应_内联数 加 1。
完。

自适应_记录展开 接受 零参：
    _自适应_展开数 为 _自适应_展开数 加 1。
完。

自适应_效率评估 接受 零参：
    若 _自适应_优化轮次 等于 0：
        返还 「未执行优化」。
    完。
    总操作 为 _自适应_内联数 加 _自适应_展开数。
    若 总操作 等于 0：
        返还 「优化无效果」。
    完。
    返还 「已执行」 加 做 转字符串 用 总操作 加 「次优化操作」。
完。

自适应_重置 接受 零参：
    _自适应_优化轮次 为 0。
    _自适应_内联数 为 0。
    _自适应_展开数 为 0。
完。

成本_指令 接受 操作类型：
    若 操作类型 等于 0：
        返还 1。
    完。
    若 操作类型 等于 1：
        返还 3。
    完。
    若 操作类型 等于 2：
        返还 4。
    完。
    若 操作类型 等于 3：
        返还 5。
    完。
    若 操作类型 等于 4：
        返还 10。
    完。
    若 操作类型 等于 5：
        返还 20。
    完。
    返还 1。
完。

成本_循环 接受 迭代、体成本：
    返还 迭代 乘 体成本 加 迭代 乘 2。
完。

成本_函数 接受 参数数、体成本：
    返还 参数数 乘 2 加 体成本 加 10。
完。

成本_估算 接受 指令列表：
    可变 总成本 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 指令列表：
        总成本 为 总成本 加 做 成本_指令 用 指令列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总成本。
完。

成本_优化收益 接受 原始成本、优化成本：
    若 原始成本 等于 0：
        返还 0。
    完。
    返还 (原始成本 减 优化成本) 乘 100 除以 原始成本。
完。

成本_是否值得优化 接受 原始成本、优化编译成本：
    若 优化编译成本 大于 原始成本 乘 10：
        返还 假。
    完。
    返还 真。
完。

成本_推荐优化级别 接受 代码复杂度：
    若 代码复杂度 大于 10000：
        返还 3。
    完。
    若 代码复杂度 大于 1000：
        返还 2。
    完。
    返还 1。
完。

成本_总评估 接受 指令列表：
    成本 为 做 成本_估算 用 指令列表。
    若 成本 大于 10000：
        返还 「重度计算(必须优化)」。
    完。
    若 成本 大于 1000：
        返还 「中等计算」。
    完。
    返还 「轻量计算」。
完。

循环标记_可展开 为 1。

循环标记_可向量化 为 2。

循环标记_有不变量 为 4。

循环标记_可并行 为 8。

循环标记_内存密集 为 16。

可变 _循环_列表 为 一组：（空）。

循环_注册 接受 循环ID、深度、迭代数、体指令数、归纳变量数：
    可变 标记 为 0。
    若 迭代数 大于 0 并且 迭代数 小于或等于 16 并且 体指令数 小于或等于 20：
        标记 为 标记 位或 循环标记_可展开。
    完。
    若 归纳变量数 小于或等于 2 并且 体指令数 小于或等于 50：
        标记 为 标记 位或 循环标记_可向量化。
    完。
    做 追加 用 _循环_列表、一组：循环ID、深度、迭代数、体指令数、归纳变量数、标记。
    返还 标记。
完。

循环_设置标记 接受 循环ID、新标记：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _循环_列表：
        若 _循环_列表 的 第 i 项 的 第 0 项 等于 循环ID：
            _循环_列表 的 第 i 项 的 第 5 项 为 _循环_列表 的 第 i 项 的 第 5 项 位或 新标记。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

循环_有标记 接受 循环ID、标记：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _循环_列表：
        若 _循环_列表 的 第 i 项 的 第 0 项 等于 循环ID：
            返还 _循环_列表 的 第 i 项 的 第 5 项 位与 标记 不等于 0。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

循环_展开因子 接受 迭代数、体指令数：
    若 体指令数 大于 30：
        返还 0。
    完。
    若 迭代数 小于或等于 4：
        返还 迭代数。
    完。
    若 迭代数 小于或等于 8 并且 体指令数 小于或等于 10：
        返还 4。
    完。
    若 迭代数 小于或等于 16 并且 体指令数 小于或等于 5：
        返还 8。
    完。
    若 体指令数 小于或等于 8：
        返还 4。
    完。
    返还 2。
完。

循环_不变量外提 接受 循环体指令、归纳变量列表：
    可变 可外提 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 循环体指令：
        instr 为 循环体指令 的 第 i 项。
        可变 依赖归纳 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 归纳变量列表：
            若 instr 等于 归纳变量列表 的 第 j 项：
                依赖归纳 为 真。
            完。
            j 为 j 加 1。
        完。
        若 依赖归纳 等于 假：
            做 追加 用 可外提、i。
        完。
        i 为 i 加 1。
    完。
    返还 可外提。
完。

可变 PGO_计数器 为 一组：（空）。

PGO_注册函数 接受 函数名：
    做 追加 用 PGO_计数器、一组：函数名、0、0。
完。

PGO_记录执行 接受 函数名、耗时：
    可变 i 为 0。
    只要 i 小于 做 计数 用 PGO_计数器：
        若 PGO_计数器 的 第 i 项 的 第 0 项 等于 函数名：
            PGO_计数器 的 第 i 项 的 第 1 项 为 PGO_计数器 的 第 i 项 的 第 1 项 加 1。
            PGO_计数器 的 第 i 项 的 第 2 项 为 PGO_计数器 的 第 i 项 的 第 2 项 加 耗时。
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 PGO_计数器、一组：函数名、1、耗时。
完。

PGO_热度 接受 函数名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 PGO_计数器：
        若 PGO_计数器 的 第 i 项 的 第 0 项 等于 函数名：
            若 PGO_计数器 的 第 i 项 的 第 1 项 大于 1000：
                返还 「热」。
            完。
            若 PGO_计数器 的 第 i 项 的 第 1 项 大于 10：
                返还 「温」。
            完。
            返还 「冷」。
        完。
        i 为 i 加 1。
    完。
    返还 「未知」。
完。

PGO_优化级别 接受 函数名：
    热度 为 做 PGO_热度 用 函数名。
    若 热度 等于 「热」：
        返还 3。
    完。
    若 热度 等于 「温」：
        返还 2。
    完。
    返还 1。
完。

可变 PGO_分支统计 为 一组：（空）。

PGO_记录分支 接受 位置、走真、走假：
    做 追加 用 PGO_分支统计、一组：位置、走真、走假。
完。

PGO_分支概率 接受 位置：
    可变 i 为 0。
    只要 i 小于 做 计数 用 PGO_分支统计：
        若 PGO_分支统计 的 第 i 项 的 第 0 项 等于 位置：
            总 为 PGO_分支统计 的 第 i 项 的 第 1 项 加 PGO_分支统计 的 第 i 项 的 第 2 项。
            若 总 等于 0：
                返还 50。
            完。
            返还 PGO_分支统计 的 第 i 项 的 第 1 项 乘 100 除以 总。
        完。
        i 为 i 加 1。
    完。
    返还 50。
完。

可变 编译缓存 为 一组：（空）。

缓存_查找 接受 模块名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 编译缓存：
        若 编译缓存 的 第 i 项 的 第 0 项 等于 模块名：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

缓存_更新 接受 模块名、源码哈希、编译哈希、依赖列表：
    idx 为 做 缓存_查找 用 模块名。
    若 idx 大于或等于 0：
        编译缓存 的 第 idx 项 的 第 1 项 为 源码哈希。
        编译缓存 的 第 idx 项 的 第 2 项 为 编译哈希。
        编译缓存 的 第 idx 项 的 第 3 项 为 依赖列表。
        编译缓存 的 第 idx 项 的 第 4 项 为 0。
    否则：
        做 追加 用 编译缓存、一组：模块名、源码哈希、编译哈希、依赖列表、0。
    完。
完。

缓存_有效 接受 模块名、当前源码哈希：
    idx 为 做 缓存_查找 用 模块名。
    若 idx 小于 0：
        返还 假。
    完。
    若 编译缓存 的 第 idx 项 的 第 1 项 不等于 当前源码哈希：
        返还 假。
    完。
    依赖 为 编译缓存 的 第 idx 项 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 依赖：
        dep_idx 为 做 缓存_查找 用 依赖 的 第 i 项。
        若 dep_idx 小于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

缓存_计算重编列表 接受 所有模块、哈希表：
    可变 需重编 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有模块：
        模块名 为 所有模块 的 第 i 项。
        可变 哈希 为 0。
        若 i 小于 做 计数 用 哈希表：
            哈希 为 哈希表 的 第 i 项。
        完。
        若 做 缓存_有效 用 模块名、哈希 等于 假：
            做 追加 用 需重编、模块名。
        完。
        i 为 i 加 1。
    完。
    返还 需重编。
完。

缓存_命中率 接受 零参：
    总 为 做 计数 用 编译缓存。
    若 总 等于 0：
        返还 0。
    完。
    可变 有效数 为 0。
    可变 i 为 0。
    只要 i 小于 总：
        若 编译缓存 的 第 i 项 的 第 1 项 不等于 0：
            有效数 为 有效数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 有效数 乘 100 除以 总。
完。

本章 收束。
