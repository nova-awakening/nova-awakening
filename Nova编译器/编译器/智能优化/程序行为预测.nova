《程序行为预测》

引用《..·..·标准库·基础》。

引用《..·..·编译器·规划·核心·常量》。

预测精度 为 10000。

启发_循环回边 为 9000。

启发_错误检查 为 1000。

启发_空值检查 为 500。

启发_边界检查 为 200。

启发_比较零 为 5000。

启发_正数 为 7000。

启发_相等 为 3000。

静态分支预测 接受 分支类型、比较运算符、操作数信息：
    若 分支类型 等于 1：
        返还 启发_循环回边。
    完。
    若 分支类型 等于 2：
        返还 启发_错误检查。
    完。
    若 比较运算符 等于 0：
        返还 启发_相等。
    完。
    若 比较运算符 等于 1：
        返还 预测精度 减 启发_相等。
    完。
    若 比较运算符 等于 2：
        返还 启发_正数。
    完。
    若 比较运算符 等于 3：
        返还 预测精度 减 启发_正数。
    完。
    若 操作数信息 等于 1：
        返还 启发_空值检查。
    完。
    若 操作数信息 等于 2：
        返还 启发_边界检查。
    完。
    返还 5000。
完。

预测循环次数 接受 初始值、终止条件、步长、循环类型：
    若 循环类型 等于 0 并且 步长 大于 0：
        若 终止条件 大于 初始值：
            若 步长 等于 0：
                返还 0。
            完。
            返还 (终止条件 减 初始值 加 步长 减 1) 除以 步长。
        完。
        返还 0。
    完。
    若 循环类型 等于 1：
        返还 10。
    完。
    若 循环类型 等于 2：
        返还 100。
    完。
    返还 10。
完。

模式_顺序 为 0。

模式_步进 为 1。

模式_随机 为 2。

模式_时间局部 为 3。

模式_空间局部 为 4。

预测内存模式 接受 访问序列：
    长度 为 做 计数 用 访问序列。
    若 长度 小于 2：
        返还 一组：模式_顺序、预测精度。
    完。
    可变 步长统计 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度 减 1：
        步长 为 访问序列 的 第 i 加 1 项 减 访问序列 的 第 i 项。
        做 追加 用 步长统计、步长。
        i 为 i 加 1。
    完。
    可变 顺序计数 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 步长统计：
        若 步长统计 的 第 i 项 等于 1 或者 步长统计 的 第 i 项 等于 8：
            顺序计数 为 顺序计数 加 1。
        完。
        i 为 i 加 1。
    完。
    顺序率 为 顺序计数 乘 预测精度 除以 做 计数 用 步长统计。
    若 顺序率 大于 8000：
        返还 一组：模式_顺序、顺序率。
    完。
    首步 为 步长统计 的 第 0 项。
    可变 固定计数 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 步长统计：
        若 步长统计 的 第 i 项 等于 首步：
            固定计数 为 固定计数 加 1。
        完。
        i 为 i 加 1。
    完。
    固定率 为 固定计数 乘 预测精度 除以 做 计数 用 步长统计。
    若 固定率 大于 7000：
        返还 一组：模式_步进、固定率。
    完。
    可变 重复计数 为 0。
    i 为 0。
    只要 i 小于 长度：
        可变 j 为 i 加 1。
        只要 j 小于 长度 并且 j 小于 i 加 10：
            若 访问序列 的 第 j 项 等于 访问序列 的 第 i 项：
                重复计数 为 重复计数 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    重复率 为 重复计数 乘 预测精度 除以 长度。
    若 重复率 大于 3000：
        返还 一组：模式_时间局部、重复率。
    完。
    返还 一组：模式_随机、预测精度 减 顺序率。
完。

预测调用频率 接受 函数信息：
    可变 热度 为 预测精度 除以 2。
    是递归 为 函数信息 的 第 0 项。
    若 是递归：
        热度 为 热度 乘 3。
    完。
    循环内 为 函数信息 的 第 1 项。
    若 循环内 大于 0：
        可变 d 为 0。
        只要 d 小于 循环内：
            热度 为 热度 乘 10。
            d 为 d 加 1。
        完。
    完。
    是库函数 为 函数信息 的 第 2 项。
    若 是库函数：
        热度 为 热度 除以 5。
    完。
    若 热度 大于 预测精度 乘 100：
        热度 为 预测精度 乘 100。
    完。
    返还 热度。
完。

预测数据大小 接受 分配信息：
    分配类型 为 分配信息 的 第 0 项。
    已知大小 为 分配信息 的 第 1 项。
    若 已知大小 大于 0：
        返还 已知大小。
    完。
    若 分配类型 等于 1：
        返还 100。
    完。
    若 分配类型 等于 2：
        返还 50。
    完。
    若 分配类型 等于 3：
        返还 64。
    完。
    返还 32。
完。

预测模型_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、0、0、5000。
完。

预测模型_记录 接受 模型、类型、位置、预测值、实际值：
    若 类型 等于 0：
        做 追加 用 模型 的 第 0 项、一组：位置、预测值、实际值。
    否则：
        做 追加 用 模型 的 第 1 项、一组：位置、预测值、实际值。
    完。
    模型 的 第 2 项 为 模型 的 第 2 项 加 1。
    可变 误差 为 预测值 减 实际值。
    若 误差 小于 0：
        误差 为 0 减 误差。
    完。
    若 误差 小于 预测精度 除以 10：
        模型 的 第 3 项 为 模型 的 第 3 项 加 1。
    完。
完。

预测模型_准确率 接受 模型：
    若 模型 的 第 2 项 等于 0：
        返还 预测精度。
    完。
    返还 模型 的 第 3 项 乘 预测精度 除以 模型 的 第 2 项。
完。

预测模型_修正 接受 模型、位置、原始预测：
    可变 匹配数 为 0。
    历史 为 模型 的 第 0 项。
    可变 实际总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 历史：
        若 历史 的 第 i 项 的 第 0 项 等于 位置：
            实际总和 为 实际总和 加 历史 的 第 i 项 的 第 2 项。
            匹配数 为 匹配数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 匹配数 大于或等于 3：
        历史平均 为 实际总和 除以 匹配数。
        若 预测精度 等于 0：
            返还 0。
        完。
        返还 历史平均 乘 7000 除以 预测精度 加 原始预测 乘 3000 除以 预测精度。
    完。
    返还 原始预测。
完。

预测_是库函数名 接受 函数名：
    若 函数名 等于 空：
        返还 假。
    完。
    若 做 字节长度 用 函数名 大于或等于 2 并且 做 子串 用 函数名、0、2 等于 「__」：
        返还 真。
    完。
    若 做 字符串相等 用 函数名、「打印」 或者 做 字符串相等 用 函数名、「print」：
        返还 真。
    完。
    返还 假。
完。

预测_扫描函数节点 接受 节点、函数名、循环深度、统计：
    若 节点 等于 空 或者 做 是列表 用 节点 等于 假 或者 做 计数 用 节点 等于 0：
        返还 空。
    完。
    若 节点 的 第 0 项 等于 节_调用 并且 做 计数 用 节点 大于或等于 2：
        若 做 字符串相等 用 节点 的 第 1 项、函数名：
            统计 的 第 0 项 为 真。
        完。
    完。
    若 节点 的 第 0 项 等于 节_当：
        新深度 为 循环深度 加 1。
        若 新深度 大于 统计 的 第 1 项：
            统计 的 第 1 项 为 新深度。
        完。
        可变 j 为 1。
        只要 j 小于 做 计数 用 节点：
            若 做 是列表 用 节点 的 第 j 项：
                做 预测_扫描函数节点 用 节点 的 第 j 项、函数名、新深度、统计。
            完。
            j 为 j 加 1。
        完。
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点：
        若 做 是列表 用 节点 的 第 i 项：
            做 预测_扫描函数节点 用 节点 的 第 i 项、函数名、循环深度、统计。
        完。
        i 为 i 加 1。
    完。
完。

预测_函数信息_从AST 接受 函数项：
    若 函数项 等于 空 或者 做 是列表 用 函数项 等于 假 或者 做 计数 用 函数项 小于 4：
        返还 一组：假、0、假、0。
    完。
    函数名 为 函数项 的 第 1 项。
    函数体 为 函数项 的 第 3 项。
    统计 为 一组：假、0。
    可变 代码大小 为 0。
    若 做 是列表 用 函数体：
        代码大小 为 做 计数 用 函数体。
        做 预测_扫描函数节点 用 函数体、函数名、0、统计。
    完。
    返还 一组：统计 的 第 0 项、统计 的 第 1 项、做 预测_是库函数名 用 函数名、代码大小。
完。

生成预测报告 接受 函数列表、_模型：
    可变 报告 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 函数列表：
        函数信息 为 做 预测_函数信息_从AST 用 函数列表 的 第 i 项。
        可变 频率 为 做 预测调用频率 用 函数信息。
        若 _模型 不等于 空：
            频率 为 做 预测模型_修正 用 _模型、i、频率。
        完。
        可变 函数报告 为 一组：i、频率、函数信息 的 第 3 项、0。
        函数报告 的 第 3 项 为 函数报告 的 第 1 项 乘 函数报告 的 第 2 项 除以 预测精度。
        做 追加 用 报告、函数报告。
        i 为 i 加 1。
    完。
    返还 报告。
完。

程序行为预测_横幅 接受 零参：
    做 打印 用 「═══ 程序行为预测器 v1.0 — 编译期运行时行为智能预测 ═══」。
完。

本章 收束。
