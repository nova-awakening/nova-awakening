《并行编译》

引用《..·..·标准库·基础》。

可变 最大并行数 为 1。

可变 并行详细模式 为 假。

并行_配置 接受 并行度：
    若 并行度 大于 0：
        最大并行数 为 并行度。
    完。
完。

并行_重置运行状态 接受 零参：
    最大并行数 为 1。
完。

并行_设置详细模式 接受 启用：
    并行详细模式 为 启用。
完。

任务_等待 为 0。

任务_运行中 为 1。

编译任务_创建 接受 模块名：
    返还 一组：模块名、任务_等待、空、「」。
完。

并行_执行批次 接受 任务列表：
    可变 完成数 为 0。
    可变 失败数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务列表：
        任务 为 任务列表 的 第 i 项。
        若 做 模块_编译单个 用 任务 的 第 0 项：
            完成数 为 完成数 加 1。
        否则：
            失败数 为 失败数 加 1。
            返还 一组：完成数、失败数。
        完。
        i 为 i 加 1。
    完。
    返还 一组：完成数、失败数。
完。

并行_报告未满足依赖 接受 名、已分配：
    若 并行详细模式：
        做 打印文本 用 做 字符串拼接 用 「[并行] 等待依赖: 」、做 转字符串 用 名。
    完。
    返还 做 计数 用 已分配。
完。

计算编译层级 接受 编译顺序、_模块表参、_模块索引参：
    n 为 做 计数 用 编译顺序。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、一组：编译顺序 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

并行_按层级分组 接受 编译顺序：
    n 为 做 计数 用 编译顺序。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 层级映射 为 做 字典。
    可变 i 为 0。
    只要 i 小于 n：
        名 为 编译顺序 的 第 i 项。
        可变 本层 为 0。
        若 做 字典有 用 模块索引、名：
            位置 为 做 字典取 用 模块索引、名。
            信息 为 模块表 的 第 位置 项。
            依赖列表 为 信息 的 依赖列表。
            若 做 是列表 用 依赖列表：
                可变 j 为 0。
                只要 j 小于 做 计数 用 依赖列表：
                    依赖层 为 做 字典取 用 层级映射、依赖列表 的 第 j 项。
                    若 依赖层 不等于 空 并且 依赖层 加 1 大于 本层：
                        本层 为 依赖层 加 1。
                    完。
                    j 为 j 加 1。
                完。
            完。
        完。
        做 字典设 用 层级映射、名、本层。
        若 本层 大于 最大层：
            最大层 为 本层。
        完。
        i 为 i 加 1。
    完。
    可变 分组 为 一组：（空）。
    i 为 0。
    只要 i 小于或等于 最大层：
        做 追加 用 分组、一组：（空）。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        层 为 做 字典取 用 层级映射、编译顺序 的 第 i 项。
        若 层 不等于 空 并且 层 小于 做 计数 用 分组：
            做 追加 用 分组 的 第 层 项、编译顺序 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 分组。
完。

并行_编译全部 接受 模块列表：
    可变 总失败 为 0。
    可变 总编译 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 模块列表：
        可变 模块名 为 模块列表 的 第 i 项。
        若 做 是列表 用 模块列表 的 第 i 项：
            模块名 为 模块列表 的 第 i 项 的 第 0 项。
        完。
        做 打印文本 用 做 字符串拼接 用 「[编译] 」、做 字符串拼接 用 做 转字符串 用 i、做 字符串拼接 用 「 」、模块名。
        若 做 模块_编译单个 用 模块名：
            总编译 为 总编译 加 1。
            _并行_编译计数 为 _并行_编译计数 加 1。
        否则：
            总失败 为 总失败 加 1。
            若 总失败 大于 0：
                返还 总编译。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 总编译。
完。

并行_统计 接受 层级分组：
    若 层级分组 等于 空：
        返还 一组：0。
    完。
    总层数 为 做 计数 用 层级分组。
    可变 总模块 为 0。
    可变 i 为 0。
    只要 i 小于 总层数：
        总模块 为 总模块 加 做 计数 用 层级分组 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 一组：总层数、总模块、_并行_编译计数。
完。

可变 _并行_编译计数 为 0。

可变 _并行_缓存命中数 为 0。

并行_记录编译 接受 零参：
    _并行_编译计数 为 _并行_编译计数 加 1。
完。

并行_记录缓存命中 接受 零参：
    _并行_缓存命中数 为 _并行_缓存命中数 加 1。
完。

并行_编译计数 接受 零参：
    返还 _并行_编译计数。
完。

并行_缓存命中率 接受 零参：
    总 为 _并行_编译计数 加 _并行_缓存命中数。
    若 总 等于 0：
        返还 0。
    完。
    返还 _并行_缓存命中数 乘 100 除以 总。
完。

本章 收束。
