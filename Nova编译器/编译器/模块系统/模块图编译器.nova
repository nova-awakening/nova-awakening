《模块图编译器》

引用《..·..·工具·帮助系统》。

引用《..·..·编译器·标记常量》。

引用《..·..·标准库·文件·路径·路径》。

引用《..·..·IR·优化器·优化管线》。

引用《..·..·IR·优化器·尾调用优化》。

引用《..·..·IR·优化器·窥孔优化》。

引用《..·..·系统扩展·P0-裸机·裸机ELF生成器》。

引用《..·..·系统扩展·P2-改进·生命周期》。

引用《..·..·平台·代码生成·elf·ELF生成器》。

引用《..·..·平台·代码生成·x86_64·PE生成器》。

引用《..·..·平台·代码生成·x86_64·代码生成器》。

引用《..·..·平台·目标选择》。

引用《..·..·平台·系统调用·系统调用封装》。

引用《..·..·标准库·基础》。

引用《..·..·标准库·文本·文本处理》。

引用《..·..·标准库·系统·硬件库》。

引用《..·..·标准库·缓存·缓存》。

引用《..·..·标准库·转换器·转换验证》。

引用《..·分析·编译流水线》。

引用《..·分析·错误诊断》。

引用《..·前端入口》。

引用《..·前端入口_运行时种子》。

引用《..·安全·效果系统》。

引用《..·安全·能力系统》。

引用《..·标准前导》。

引用《增量缓存》。

引用《并行编译》。

引用《模块接口契约》。

引用《..·规划·代码生成·目标选择》。

引用《..·规划·核心·发射器》。

引用《..·规划·核心·常量》。

引用《..·语义·作用域》。

引用《..·语义·可变性检查》。

引用《..·语义·未使用检查》。

引用《..·语义·语义预检诊断》。

引用《..·语义·行号追踪》。

引用《..·..·自举兼容桥》。

引用《..·..·运行时·内存·内存Profile测量》。

引用《..·..·运行时·原语桩》。

引用《..·..·运行时·垃圾回收·垃圾回收》。

引用《..·..·运行时·字典引擎·字典引擎》。

引用《..·规划·链接器·PE格式·PE链接器》。

引用《..·规划·类型系统·泛型·泛型》。

引用《..·规划·错误处理·错误处理》。

引用《..·..·链接器·ELF链接器》。

引用《..·..·链接器·MachO链接器》。

引用《..·..·链接器·Wasm链接器》。

引用《..·..·标准库·索引》。

引用《..·..·标准库·AI索引》。

引用《..·..·标准库·类型系统·类型系统》。

引用《..·语义·生命期分析》。

引用《..·语法·语句》。

模块信息 是 一种 事物，含 名称、路径、哈希、依赖列表、导出符号、编译状态、目标码、目标码大小、上次编译时间、版本号、修正表数据、函数表数据、入口偏移、profile标签、子系统归属、模块标签列表、允许热更、仅裸机、仅宿主、保留符号。

可变 模块表 为 一组：（空）。

可变 模块索引 为 做 字典。

可变 编译顺序 为 一组：（空）。

可变 模块入口模块名 为 「」。

可变 _模块_项目合同 为 空。

可变 _模块行号映射 为 做 字典。

可变 _链接_全局函数表 为 一组：0。

可变 模块图详细模式 为 一组：假。

可变 模块图扫描读根 为 「」。

可变 模块图编译器_目标ELF 为 0。

可变 _进度_总数 为 0。

可变 _进度_当前 为 0。

可变 _进度_成功 为 0。

可变 _进度_失败 为 0。

可变 _进度_跳过 为 0。

可变 _进度_警告 为 0。

可变 _进度_解析错误 为 0。

可变 _进度_语义错误 为 0。

可变 _进度_能力违规 为 0。

可变 _进度_错误列表 为 一组：（空）。

可变 _进度_阶段 为 「初始化」。

可变 _进度_最后模块 为 「」。

_打印行 接受 文本：
    做 打印文本 用 文本。
    做 打印文本 用 「\n」。
完。

_进度条表 为 一组：「....................」、「=...................」、「==..................」、「===.................」、「====................」、「=====...............」、「======..............」、「=======.............」、「========............」、「=========...........」、「==========..........」、「===========.........」、「============........」、「=============.......」、「==============......」、「===============.....」、「================....」、「=================...」、「==================..」、「===================.」、「====================」。

_进度_条 接受 当前、总数：
    若 总数 小于或等于 0：
        返还 _进度条表 的 第 0 项。
    完。
    级 为 当前 乘 20 除以 总数。
    若 级 大于 20：
        返还 _进度条表 的 第 20 项。
    完。
    若 级 小于 0：
        返还 _进度条表 的 第 0 项。
    完。
    返还 _进度条表 的 第 级 项。
完。

_进度_填充数字 接受 数值、最大值：
    可变 结果 为 做 转字符串 用 数值。
    目标宽 为 做 字节长度 用 做 转字符串 用 最大值。
    只要 做 字节长度 用 结果 小于 目标宽：
        结果 为 做 字符串拼接 用 「 」、结果。
    完。
    返还 结果。
完。

_进度_显示编译 接受 序号、总数、模块名：
    条 为 做 _进度_条 用 序号、总数。
    百分比 为 做 转字符串 用 序号 乘 100 除以 总数。
    序 为 做 _进度_填充数字 用 序号、总数。
    总 为 做 转字符串 用 总数。
    做 打印文本 用 「  」。
    做 打印文本 用 条。
    做 打印文本 用 「 」。
    做 打印文本 用 百分比。
    做 打印文本 用 「% [」。
    做 打印文本 用 序。
    做 打印文本 用 「/」。
    做 打印文本 用 总。
    做 打印文本 用 「]  」。
    做 打印文本 用 模块名。
    做 打印文本 用 「\n」。
    _进度_最后模块 为 模块名。
    若 序号 取余于 50 等于 0 或者 序号 等于 总数：
        做 _进度_心跳 用 模块名。
    完。
完。

_进度_显示预注册 接受 序号、总数：
    若 序号 等于 1 或者 序号 等于 总数 或者 序号 取余于 100 等于 0：
        条 为 做 _进度_条 用 序号、总数。
        百分比 为 做 转字符串 用 序号 乘 100 除以 总数。
        序 为 做 _进度_填充数字 用 序号、总数。
        做 打印文本 用 「  」。
        做 打印文本 用 条。
        做 打印文本 用 「 」。
        做 打印文本 用 百分比。
        做 打印文本 用 「% [」。
        做 打印文本 用 序。
        做 打印文本 用 「/」。
        做 打印文本 用 做 转字符串 用 总数。
        做 打印文本 用 「]  预注册」。
        做 打印文本 用 「\n」。
    完。
完。

_进度_记录错误 接受 类型、文件、行号、描述：
    位置串 为 做 字符串拼接 用 文件、做 字符串拼接 用 「:」、做 转字符串 用 行号。
    条目 为 做 字符串拼接 用 类型、做 字符串拼接 用 「 │ 」、做 字符串拼接 用 位置串、做 字符串拼接 用 「 │ 」、描述。
    做 追加 用 _进度_错误列表、条目。
    若 做 字符串相等 用 类型、「解析」：
        _进度_解析错误 为 _进度_解析错误 加 1。
    完。
    若 做 字符串相等 用 类型、「语义」：
        _进度_语义错误 为 _进度_语义错误 加 1。
    完。
    若 做 字符串相等 用 类型、「能力」：
        _进度_能力违规 为 _进度_能力违规 加 1。
    完。
    做 _打印行 用 做 字符串拼接 用 「    ✗ [」、做 字符串拼接 用 类型、做 字符串拼接 用 「] 」、做 字符串拼接 用 位置串、做 字符串拼接 用 「  」、描述。
完。

_进度_心跳 接受 模块名：
    _进度_最后模块 为 模块名。
    行1 为 做 字符串拼接 用 「phase=」、做 字符串拼接 用 _进度_阶段、做 字符串拼接 用 「\ncurrent=」、做 转字符串 用 _进度_当前。
    行2 为 做 字符串拼接 用 「\ntotal=」、做 字符串拼接 用 做 转字符串 用 _进度_总数、做 字符串拼接 用 「\nmodule=」、模块名。
    行3 为 做 字符串拼接 用 「\nsuccess=」、做 字符串拼接 用 做 转字符串 用 _进度_成功、做 字符串拼接 用 「\nfail=」、做 转字符串 用 _进度_失败。
    行4 为 做 字符串拼接 用 「\nparse_err=」、做 字符串拼接 用 做 转字符串 用 _进度_解析错误、「\n」。
    做 写文件 用 「/tmp/nova_compile_progress.txt」、做 字符串拼接 用 行1、做 字符串拼接 用 行2、做 字符串拼接 用 行3、行4、0。
完。

_进度_写IDE诊断 接受 零参：
    行1 为 做 字符串拼接 用 「NOVA_DIAG\nphase=」、做 字符串拼接 用 _进度_阶段、做 字符串拼接 用 「\ncurrent=」、做 转字符串 用 _进度_当前。
    行2 为 做 字符串拼接 用 「\ntotal=」、做 字符串拼接 用 做 转字符串 用 _进度_总数、做 字符串拼接 用 「\nsuccess=」、做 转字符串 用 _进度_成功。
    行3 为 做 字符串拼接 用 「\nfail=」、做 字符串拼接 用 做 转字符串 用 _进度_失败、做 字符串拼接 用 「\nskip=」、做 转字符串 用 _进度_跳过。
    行4 为 做 字符串拼接 用 「\nparse_err=」、做 字符串拼接 用 做 转字符串 用 _进度_解析错误、做 字符串拼接 用 「\nsem_err=」、做 转字符串 用 _进度_语义错误。
    行5 为 做 字符串拼接 用 「\ncap_err=」、做 字符串拼接 用 做 转字符串 用 _进度_能力违规、做 字符串拼接 用 「\nlast=」、_进度_最后模块。
    行6 为 做 字符串拼接 用 「\nerrors=」、做 字符串拼接 用 做 转字符串 用 做 计数 用 _进度_错误列表、「\n」。
    做 写文件 用 「/tmp/nova_compile_diag.txt」、做 字符串拼接 用 行1、做 字符串拼接 用 行2、做 字符串拼接 用 行3、做 字符串拼接 用 行4、做 字符串拼接 用 行5、行6、0。
完。

_进度_汇总 接受 总数、成功、失败、跳过、警告、解析错误：
    做 _打印行 用 「」。
    做 _打印行 用 「╔══════════════════════════════════════════╗」。
    做 _打印行 用 做 字符串拼接 用 「║  编译完成  共 」、做 字符串拼接 用 做 转字符串 用 总数、「 个模块」。
    做 _打印行 用 「╠══════════════════════════════════════════╣」。
    若 成功 大于 0：
        做 _打印行 用 做 字符串拼接 用 「║  ✓ 成功:     」、做 转字符串 用 成功。
    完。
    若 失败 大于 0：
        做 _打印行 用 做 字符串拼接 用 「║  ✗ 失败:     」、做 转字符串 用 失败。
    完。
    若 跳过 大于 0：
        做 _打印行 用 做 字符串拼接 用 「║  ○ 跳过:     」、做 转字符串 用 跳过。
    完。
    若 _进度_解析错误 大于 0：
        做 _打印行 用 做 字符串拼接 用 「║  ✗ 解析错误: 」、做 转字符串 用 _进度_解析错误。
    完。
    若 _进度_语义错误 大于 0：
        做 _打印行 用 做 字符串拼接 用 「║  △ 语义错误: 」、做 转字符串 用 _进度_语义错误。
    完。
    若 _进度_能力违规 大于 0：
        做 _打印行 用 做 字符串拼接 用 「║  ⊘ 能力违规: 」、做 转字符串 用 _进度_能力违规。
    完。
    若 警告 大于 0：
        做 _打印行 用 做 字符串拼接 用 「║  △ 警告:     」、做 转字符串 用 警告。
    完。
    做 _打印行 用 「╚══════════════════════════════════════════╝」。
    若 做 计数 用 _进度_错误列表 大于 0：
        做 _打印行 用 做 字符串拼接 用 「\n  错误详情 (共 」、做 字符串拼接 用 做 转字符串 用 做 计数 用 _进度_错误列表、「 条):」。
        做 _打印行 用 「  ─────────────────────────────────────」。
        可变 ei 为 0。
        只要 ei 小于 做 计数 用 _进度_错误列表 并且 ei 小于 30：
            做 _打印行 用 做 字符串拼接 用 「  」、_进度_错误列表 的 第 ei 项。
            ei 为 ei 加 1。
        完。
        若 做 计数 用 _进度_错误列表 大于 30：
            做 _打印行 用 做 字符串拼接 用 「  ... 还有 」、做 字符串拼接 用 做 转字符串 用 做 计数 用 _进度_错误列表 减 30、「 条未显示」。
        完。
        做 _打印行 用 「  ─────────────────────────────────────」。
    完。
    做 _打印行 用 「」。
    做 _进度_写IDE诊断。
完。

模块图_设置详细模式 接受 启用：
    模块图详细模式 的 第 0 项 为 启用。
    做 增量缓存_设置详细模式 用 启用。
    做 并行_设置详细模式 用 启用。
完。

模块图_打印详细 接受 文本：
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 文本。
    完。
完。

模块图_写崩溃诊断 接受 阶段、模块名：
    做 写文件 用 「/临时/nova_stage2_last_module.txt」、做 字符串拼接 用 阶段、做 字符串拼接 用 「\n」、模块名、0。
完。

模块图_阶段标记 接受 阶段、模块名：
    若 模块图详细模式 的 第 0 项：
        做 模块图_写崩溃诊断 用 阶段、模块名。
        做 模块图_打印详细 用 做 字符串拼接 用 「[阶段] 」、做 字符串拼接 用 阶段、做 字符串拼接 用 「 :: 」、模块名。
    完。
完。

模块图_重置运行状态 接受 零参：
    若 模块表 不等于 空 并且 做 是列表 用 模块表：
        做 模块图_清空列表 用 模块表。
    否则：
        模块表 为 一组：（空）。
    完。
    若 模块索引 不等于 空 并且 做 是字典 用 模块索引：
        做 字典_哈希表_清空 用 模块索引。
    否则：
        模块索引 为 做 字典。
    完。
    若 编译顺序 不等于 空 并且 做 是列表 用 编译顺序：
        做 模块图_清空列表 用 编译顺序。
    否则：
        编译顺序 为 一组：（空）。
    完。
    模块入口模块名 为 「」。
    _模块_项目合同 为 空。
    若 _模块行号映射 不等于 空 并且 做 是字典 用 _模块行号映射：
        做 字典_哈希表_清空 用 _模块行号映射。
    否则：
        _模块行号映射 为 做 字典。
    完。
    若 _链接_全局函数表 不等于 空 并且 做 是列表 用 _链接_全局函数表：
        若 做 计数 用 _链接_全局函数表 等于 0：
            做 追加 用 _链接_全局函数表、0。
        完。
        _链接_全局函数表 的 第 0 项 为 0。
        只要 做 计数 用 _链接_全局函数表 大于 1：
            做 删除 用 _链接_全局函数表、做 计数 用 _链接_全局函数表 减 1。
        完。
    否则：
        _链接_全局函数表 为 一组：0。
    完。
    若 模块图详细模式 不等于 空 并且 做 是列表 用 模块图详细模式：
        若 做 计数 用 模块图详细模式 等于 0：
            做 追加 用 模块图详细模式、假。
        完。
        模块图详细模式 的 第 0 项 为 假。
        只要 做 计数 用 模块图详细模式 大于 1：
            做 删除 用 模块图详细模式、做 计数 用 模块图详细模式 减 1。
        完。
    否则：
        模块图详细模式 为 一组：假。
    完。
    模块图扫描读根 为 「」。
    模块图编译器_目标ELF 为 0。
    _进度_总数 为 0。
    _进度_当前 为 0。
    _进度_成功 为 0。
    _进度_失败 为 0。
    _进度_跳过 为 0。
    _进度_警告 为 0。
    _进度_解析错误 为 0。
    _进度_语义错误 为 0。
    _进度_能力违规 为 0。
    若 _进度_错误列表 不等于 空 并且 做 是列表 用 _进度_错误列表：
        做 模块图_清空列表 用 _进度_错误列表。
    否则：
        _进度_错误列表 为 一组：（空）。
    完。
    _进度_阶段 为 「初始化」。
    _进度_最后模块 为 「」。
    若 _跳板表 不等于 空 并且 做 是字典 用 _跳板表：
        做 字典_哈希表_清空 用 _跳板表。
    否则：
        _跳板表 为 做 字典。
    完。
    若 _旧代码池 不等于 空 并且 做 是列表 用 _旧代码池：
        做 模块图_清空列表 用 _旧代码池。
    否则：
        _旧代码池 为 一组：（空）。
    完。
完。

模块图_清空列表 接受 列表：
    可变 i 为 做 计数 用 列表。
    只要 i 大于 0：
        做 删除 用 列表、i 减 1。
        i 为 i 减 1。
    完。
完。

模块图_安全取字节 接受 字节列、偏移：
    若 字节列 等于 空 或者 偏移 小于 0 或者 偏移 大于或等于 做 计数 用 字节列：
        返还 0 减 1。
    完。
    返还 字节列 的 第 偏移 项。
完。

模块图_入口字节摘要 接受 字节列、入口偏移：
    b0 为 做 模块图_安全取字节 用 字节列、入口偏移。
    b1 为 做 模块图_安全取字节 用 字节列、入口偏移 加 1。
    b2 为 做 模块图_安全取字节 用 字节列、入口偏移 加 2。
    b3 为 做 模块图_安全取字节 用 字节列、入口偏移 加 3。
    返还 做 字符串拼接 用 「b0=」、做 字符串拼接 用 做 转字符串 用 b0、做 字符串拼接 用 「 b1=」、做 字符串拼接 用 做 转字符串 用 b1、做 字符串拼接 用 「 b2=」、做 字符串拼接 用 做 转字符串 用 b2、做 字符串拼接 用 「 b3=」、做 转字符串 用 b3。
完。

模块图_当前目标是裸机 接受 零参：
    返还 做 字符串相等 用 当前目标名称、「裸机」 或者 做 字符串相等 用 当前目标名称、「kernel」。
完。

模块图_当前目标是Linux 接受 零参：
    返还 做 字符串相等 用 当前目标名称、「linux」 或者 做 字符串相等 用 当前目标名称、「elf」。
完。

模块图_当前目标格式 接受 零参：
    若 做 模块图_当前目标是Linux：
        返还 1。
    完。
    若 做 模块图_当前目标是裸机：
        返还 2。
    完。
    若 做 字符串相等 用 当前目标名称、「windows」 或者 做 字符串相等 用 当前目标名称、「pe」：
        返还 0。
    完。
    返还 0。
完。

模块_注册 接受 名称、路径：
    若 做 字典有 用 模块索引、名称：
        返还 做 字典取 用 模块索引、名称。
    完。
    可变 信息 为 建。
    新建 模块信息：名称 为 名称、路径 为 路径、哈希 为 0、依赖列表 为 一组：（空）、导出符号 为 一组：（空）、编译状态 为 0、目标码 为 空、目标码大小 为 0、上次编译时间 为 0、版本号 为 0、修正表数据 为 一组：（空）、函数表数据 为 做 字典、入口偏移 为 0、profile标签 为 一组：（空）、子系统归属 为 「」、模块标签列表 为 一组：（空）、允许热更 为 真、仅裸机 为 假、仅宿主 为 假、保留符号 为 假。
    位置 为 做 计数 用 模块表。
    做 追加 用 模块表、信息。
    做 字典设 用 模块索引、名称、位置。
    返还 位置。
完。

模块_写回信息 接受 位置、信息：
    模块表 的 第 位置 项 为 信息。
完。

模块_推断标签 接受 信息：
    路径 为 信息 的 路径。
    若 路径 等于 空：
        返还 信息。
    完。
    若 做 字符串包含 用 路径、「引导」 或者 做 字符串包含 用 路径、「boot」：
        信息 的 子系统归属 为 「引导」。
        做 追加 用 信息 的 模块标签列表、「启动」。
        信息 的 允许热更 为 假。
        信息 的 仅裸机 为 真。
    完。
    若 做 字符串包含 用 路径、「中枢」 或者 做 字符串包含 用 路径、「神经」：
        信息 的 子系统归属 为 「中枢」。
        做 追加 用 信息 的 模块标签列表、「AI原生」。
    完。
    若 做 字符串包含 用 路径、「外联」 或者 做 字符串包含 用 路径、「网络」：
        信息 的 子系统归属 为 「外联」。
        做 追加 用 信息 的 模块标签列表、「可热更」。
    完。
    若 做 字符串包含 用 路径、「推理」 或者 做 字符串包含 用 路径、「注意力」 或者 做 字符串包含 用 路径、「量化」：
        做 追加 用 信息 的 模块标签列表、「AI原生」。
    完。
    若 做 字符串包含 用 路径、「记忆」 或者 做 字符串包含 用 路径、「memory」：
        做 追加 用 信息 的 模块标签列表、「记忆层」。
    完。
    若 做 字符串包含 用 路径、「学习」 或者 做 字符串包含 用 路径、「赫布」 或者 做 字符串包含 用 路径、「强化」：
        做 追加 用 信息 的 模块标签列表、「学习层」。
    完。
    若 做 字符串包含 用 路径、「P0-裸机」 或者 做 字符串包含 用 路径、「特权」 或者 做 字符串包含 用 路径、「页表」：
        信息 的 仅裸机 为 真。
        信息 的 允许热更 为 假。
        做 追加 用 信息 的 模块标签列表、「内核态」。
    完。
    若 做 字符串包含 用 路径、「调试」 或者 做 字符串包含 用 路径、「DWARF」 或者 做 字符串包含 用 路径、「debug」：
        信息 的 保留符号 为 真。
    完。
    返还 信息。
完。

模块_profile可编译 接受 信息、活跃profile：
    若 活跃profile 等于 空 或者 做 字节长度 用 活跃profile 等于 0：
        返还 真。
    完。
    若 做 字符串包含 用 活跃profile、「kernel」：
        若 信息 的 仅宿主：
            返还 假。
        完。
    完。
    若 做 字符串包含 用 活跃profile、「analysis」 或者 做 字符串包含 用 活跃profile、「linux-user」：
        若 信息 的 仅裸机：
            返还 假。
        完。
    完。
    若 做 计数 用 信息 的 profile标签 大于 0：
        可变 _找到 为 假。
        可变 _pi 为 0。
        只要 _pi 小于 做 计数 用 信息 的 profile标签：
            若 做 字符串相等 用 信息 的 profile标签 的 第 _pi 项、活跃profile：
                _找到 为 真。
            完。
            _pi 为 _pi 加 1。
        完。
        若 _找到 等于 假：
            返还 假。
        完。
    完。
    返还 真。
完。

模块_批量推断标签 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 模块表：
        模块表 的 第 i 项 为 做 模块_推断标签 用 模块表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

模块_标签统计 接受 零参：
    可变 AI数 为 0。
    可变 启动数 为 0。
    可变 可热更数 为 0。
    可变 裸机数 为 0。
    可变 记忆数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 模块表：
        _信息 为 模块表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 _信息 的 模块标签列表：
            _t 为 _信息 的 模块标签列表 的 第 j 项。
            若 做 字符串相等 用 _t、「AI原生」：
                AI数 为 AI数 加 1。
            完。
            若 做 字符串相等 用 _t、「启动」：
                启动数 为 启动数 加 1。
            完。
            若 做 字符串相等 用 _t、「可热更」：
                可热更数 为 可热更数 加 1。
            完。
            若 做 字符串相等 用 _t、「内核态」：
                裸机数 为 裸机数 加 1。
            完。
            若 做 字符串相等 用 _t、「记忆层」：
                记忆数 为 记忆数 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：AI数、启动数、可热更数、裸机数、记忆数。
完。

模块_同步目标格式 接受 零参：
    模块图编译器_目标ELF 为 做 模块图_当前目标格式。
    做 编译_设置目标ELF 用 模块图编译器_目标ELF。
    做 管线_设置裸机模式 用 模块图编译器_目标ELF 等于 2。
完。

模块_保存代码生成状态 接受 零参：
    返还 一组：代码、代码位、修正表、函数表、变量表、变量数、字串表、字串偏移、全局变量表、全局变量数、主体模式、当前函数名、帧大小位、变量类型表、模块图编译器_目标ELF。
完。

模块_重置模块代码生成状态 接受 零参：
    做 编译_重置代码生成状态 用 假、假。
    做 编译_设置目标ELF 用 模块图编译器_目标ELF。
完。

模块_恢复代码生成状态 接受 状态、恢复全局变量：
    代码 为 状态 的 第 0 项。
    代码位 为 状态 的 第 1 项。
    修正表 为 状态 的 第 2 项。
    函数表 为 状态 的 第 3 项。
    变量表 为 状态 的 第 4 项。
    变量数 为 状态 的 第 5 项。
    字串表 为 状态 的 第 6 项。
    字串偏移 为 状态 的 第 7 项。
    若 恢复全局变量：
        全局变量表 为 状态 的 第 8 项。
        全局变量数 为 状态 的 第 9 项。
    完。
    主体模式 为 状态 的 第 10 项。
    当前函数名 为 状态 的 第 11 项。
    帧大小位 为 状态 的 第 12 项。
    变量类型表 为 状态 的 第 13 项。
    模块图编译器_目标ELF 为 状态 的 第 14 项。
    做 编译_设置目标ELF 用 模块图编译器_目标ELF。
完。

模块_初始化链接代码生成状态 接受 零参：
    做 编译_重置代码生成状态 用 真、假。
    做 编译_设置目标ELF 用 模块图编译器_目标ELF。
完。

模块图_规范路径 接受 路径：
    若 路径 等于 空：
        返还 「」。
    完。
    干净路径 为 做 替换 用 做 替换 用 路径、「\r」、「」、「\n」、「」。
    若 做 计数 用 干净路径 等于 0：
        返还 「」。
    完。
    可变 结果 为 做 替换 用 干净路径、「\\」、「/」。
    只要 做 以开头 用 结果、「./」：
        结果 为 做 子串 用 结果、2、做 计数 用 结果 减 2。
    完。
    只要 做 字符串包含 用 结果、「//」：
        结果 为 做 替换 用 结果、「//」、「/」。
    完。
    返还 结果。
完。

模块图_父目录 接受 路径：
    标准路径 为 做 模块图_规范路径 用 路径。
    可变 i 为 做 计数 用 标准路径 减 1。
    只要 i 大于或等于 0：
        若 做 字节取 用 标准路径、i 等于 字符_斜杠：
            若 i 等于 0：
                返还 「/」。
            完。
            若 i 等于 2 并且 做 计数 用 标准路径 大于或等于 3 并且 做 字节取 用 标准路径、1 等于 字符_冒号：
                返还 做 子串 用 标准路径、0、3。
            完。
            返还 做 子串 用 标准路径、0、i。
        完。
        i 为 i 减 1。
    完。
    返还 「」。
完。

模块图_拼路径 接受 基础、子路径：
    可变 基 为 基础。
    可变 子 为 子路径。
    若 基 等于 空：
        基 为 「」。
    完。
    若 子 等于 空：
        子 为 「」。
    完。
    基长 为 做 字节长度 用 基。
    子长 为 做 字节长度 用 子。
    可变 结果 为 「」。
    若 基长 等于 0：
        结果 为 子。
    否则若 子长 等于 0：
        结果 为 基。
    否则若 做 字节取 用 子、0 等于 字符_斜杠 或者 做 字节取 用 子、0 等于 字符_反斜杠：
        结果 为 子。
    否则若 子长 大于或等于 2 并且 做 字节取 用 子、1 等于 字符_冒号：
        结果 为 子。
    否则若 做 字节取 用 基、基长 减 1 等于 字符_斜杠 或者 做 字节取 用 基、基长 减 1 等于 字符_反斜杠：
        结果 为 做 字符串拼接 用 基、子。
    否则：
        结果 为 做 字符串拼接 用 基、做 字符串拼接 用 「/」、子。
    完。
    返还 结果。
完。

模块图_条目是目录 接受 条目：
    若 做 字节长度 用 条目 等于 0：
        返还 假。
    完。
    长度 为 做 字节长度 用 条目。
    若 长度 小于或等于 0：
        返还 假。
    完。
    末字 为 做 字节取 用 条目、长度 减 1。
    若 末字 等于 47：
        返还 真。
    完。
    若 末字 等于 标记_左花：
        返还 真。
    完。
    返还 假。
完。

模块图_条目名称 接受 条目：
    可变 名称 为 做 替换 用 做 替换 用 条目、「\r」、「」、「\n」、「」。
    若 做 模块图_条目是目录 用 名称：
        返还 做 子串 用 名称、0、做 计数 用 名称 减 1。
    完。
    返还 名称。
完。

模块图_应跳过条目 接受 条目：
    名称 为 做 模块图_条目名称 用 条目。
    若 做 字节长度 用 名称 等于 0 或者 做 字符串相等 用 名称、「.」 或者 做 字符串相等 用 名称、「..」：
        返还 真。
    完。
    若 做 字符串相等 用 名称、「.nova_cache」 或者 做 字符串相等 用 名称、「_bootstrap_out」 或者 做 字符串相等 用 名称、「.git」 或者 做 字符串相等 用 名称、「.vs」：
        返还 真。
    完。
    若 做 以开头 用 名称、「_」：
        返还 真。
    完。
    若 做 以开头 用 名称、「.__nova_dir_」：
        返还 真。
    完。
    返还 假。
完。

模块图_应跳过文件路径 接受 路径：
    标准路径 为 做 模块图_规范路径 用 路径。
    若 做 字节长度 用 标准路径 等于 0：
        返还 真。
    完。
    若 做 包含 用 标准路径、「/.nova_cache/」 或者 做 包含 用 标准路径、「/_bootstrap_out/」：
        返还 真。
    完。
    若 做 包含 用 标准路径、「/.git/」 或者 做 包含 用 标准路径、「/.vs/」：
        返还 真。
    完。
    若 做 包含 用 标准路径、「/_」：
        返还 真。
    完。
    若 做 包含 用 标准路径、「/.__nova_dir_」：
        返还 真。
    完。
    返还 假。
完。

模块图_递归枚举Nova文件 接受 目录：
    标准目录 为 做 模块图_规范路径 用 目录。
    可变 结果 为 一组：（空）。
    若 做 字节长度 用 标准目录 等于 0：
        返还 结果。
    完。
    条目列表 为 做 列目录 用 标准目录。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目列表：
        原始条目 为 条目列表 的 第 i 项。
        条目名 为 做 模块图_条目名称 用 原始条目。
        若 做 模块图_应跳过条目 用 条目名：
            i 为 i 加 1。
            继续。
        完。
        可变 条目路径 为 条目名。
        若 做 字节长度 用 标准目录 不等于 1 或者 做 字节取 用 标准目录、0 不等于 字符_句点：
            条目路径 为 做 字符串拼接 用 标准目录、做 字符串拼接 用 「/」、条目名。
        完。
        若 做 模块图_条目是目录 用 原始条目：
            子结果 为 做 模块图_递归枚举Nova文件 用 条目路径。
            可变 si 为 0。
            只要 si 小于 做 计数 用 子结果：
                做 追加 用 结果、子结果 的 第 si 项。
                si 为 si 加 1。
            完。
        否则若 做 模块图_是Nova文件路径 用 条目路径：
            若 非 做 模块图_应跳过文件路径 用 条目路径：
                做 追加 用 结果、条目路径。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模块图_设置项目合同 接受 合同：
    _模块_项目合同 为 合同。
完。

模块_推断入口模块 接受 项目根：
    根目录 为 做 模块图_项目根目录 用 项目根。
    若 _模块_项目合同 不等于 空 并且 做 是列表 用 _模块_项目合同 并且 做 计数 用 _模块_项目合同 大于或等于 3：
        _合同入口 为 _模块_项目合同 的 第 2 项。
        若 _合同入口 不等于 空 并且 做 字节长度 用 _合同入口 大于 0：
            _合同路径 为 做 模块图_规范路径 用 做 字符串拼接 用 根目录、做 字符串拼接 用 「/」、_合同入口。
            若 做 字典有 用 模块索引、_合同路径：
                _入口位2 为 做 字典取 用 模块索引、_合同路径。
                做 模块图_打印详细 用 做 字符串拼接 用 「[入口] 项目合同指定: 」、_合同路径。
                返还 模块表 的 第 _入口位2 项 的 名称。
            完。
            做 模块图_打印详细 用 做 字符串拼接 用 「[入口] 项目合同指定的入口未找到: 」、_合同入口。
        完。
    完。
    候选 为 做 模块图_规范路径 用 做 字符串拼接 用 根目录、「/Nova.nova」。
    若 做 字典有 用 模块索引、候选：
        _入口位 为 做 字典取 用 模块索引、候选。
        返还 模块表 的 第 _入口位 项 的 名称。
    完。
    可变 mi 为 0。
    只要 mi 小于 做 计数 用 模块表：
        名 为 模块表 的 第 mi 项 的 名称。
        若 做 字符串相等 用 名、「Nova.nova」 或者 做 以结尾 用 名、「/Nova.nova」：
            返还 名。
        完。
        mi 为 mi 加 1。
    完。
    若 做 计数 用 模块表 大于 0：
        首模块 为 模块表 的 第 0 项 的 名称。
        做 模块图_打印详细 用 做 字符串拼接 用 「[入口] 回退到编译模块表首项: 」、首模块。
        返还 首模块。
    完。
    返还 候选。
完。

模块_添加依赖 接受 模块名、依赖名：
    若 做 字典有 用 模块索引、模块名：
        位置 为 做 字典取 用 模块索引、模块名。
        可变 信息 为 模块表 的 第 位置 项。
        若 非 做 是列表 用 信息 的 依赖列表：
            信息 的 依赖列表 为 一组：（空）。
        完。
        可变 已有 为 假。
        可变 i 为 0。
        _dep数 为 做 计数 用 信息 的 依赖列表。
        只要 i 小于 _dep数：
            若 做 字符串相等 用 信息 的 依赖列表 的 第 i 项、依赖名：
                已有 为 真。
                i 为 _dep数。
            完。
            i 为 i 加 1。
        完。
        若 非 已有：
            做 追加 用 信息 的 依赖列表、依赖名。
            做 模块_写回信息 用 位置、信息。
        完。
    完。
完。

模块_添加导出 接受 模块名、符号名：
    若 做 字典有 用 模块索引、模块名：
        位置 为 做 字典取 用 模块索引、模块名。
        可变 信息 为 模块表 的 第 位置 项。
        做 追加 用 信息 的 导出符号、符号名。
        做 模块_写回信息 用 位置、信息。
    完。
完。

模块_统计依赖 接受 零参：
    可变 总边数 为 0。
    可变 有依赖模块数 为 0。
    可变 最大依赖数 为 0。
    可变 最大依赖模块 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 模块表：
        依赖列表 为 模块表 的 第 i 项 的 依赖列表。
        若 非 做 是列表 用 依赖列表：
            依赖列表 为 一组：（空）。
        完。
        依赖数 为 做 计数 用 依赖列表。
        总边数 为 总边数 加 依赖数。
        若 依赖数 大于 0：
            有依赖模块数 为 有依赖模块数 加 1。
        完。
        若 依赖数 大于 最大依赖数：
            最大依赖数 为 依赖数。
            最大依赖模块 为 模块表 的 第 i 项 的 名称。
        完。
        i 为 i 加 1。
    完。
    返还 一组：总边数、有依赖模块数、最大依赖数、最大依赖模块。
完。

模块_拓扑排序 接受 _扩1：
    模块数 为 做 计数 用 模块表。
    可变 入度 为 做 字典。
    可变 被依赖者 为 做 字典。
    可变 i 为 0。
    只要 i 小于 模块数：
        名 为 模块表 的 第 i 项 的 名称。
        做 字典设 用 入度、名、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 模块数：
        当前名 为 模块表 的 第 i 项 的 名称。
        依赖列表 为 模块表 的 第 i 项 的 依赖列表。
        若 做 是列表 用 依赖列表 等于 假：
            依赖列表 为 一组：（空）。
        完。
        可变 j 为 0。
        只要 j 小于 做 计数 用 依赖列表：
            依赖 为 依赖列表 的 第 j 项。
            若 做 字典有 用 入度、依赖：
                做 字典设 用 入度、当前名、做 字典取 用 入度、当前名 加 1。
                反列 为 做 字典取 用 被依赖者、依赖。
                若 反列 等于 空：
                    反列 为 一组：（空）。
                    做 字典设 用 被依赖者、依赖、反列。
                完。
                做 追加 用 反列、当前名。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    做 模块图_清空列表 用 编译顺序。
    可变 队列 为 一组：（空）。
    i 为 0。
    只要 i 小于 模块数：
        若 做 字典取 用 入度、模块表 的 第 i 项 的 名称 等于 0：
            做 追加 用 队列、模块表 的 第 i 项 的 名称。
        完。
        i 为 i 加 1。
    完。
    做 排序 用 队列、0。
    可变 队头 为 0。
    只要 队头 小于 做 计数 用 队列：
        当前 为 队列 的 第 队头 项。
        队头 为 队头 加 1。
        做 追加 用 编译顺序、当前。
        依赖者列表 为 做 字典取 用 被依赖者、当前。
        若 依赖者列表 不等于 空 并且 做 是列表 用 依赖者列表：
            可变 新零度 为 一组：（空）。
            可变 k 为 0。
            只要 k 小于 做 计数 用 依赖者列表：
                依赖者 为 依赖者列表 的 第 k 项。
                做 字典设 用 入度、依赖者、做 字典取 用 入度、依赖者 减 1。
                若 做 字典取 用 入度、依赖者 等于 0：
                    做 追加 用 新零度、依赖者。
                完。
                k 为 k 加 1。
            完。
            若 做 计数 用 新零度 大于 0：
                做 排序 用 新零度、0。
                可变 ni 为 0。
                只要 ni 小于 做 计数 用 新零度：
                    做 追加 用 队列、新零度 的 第 ni 项。
                    ni 为 ni 加 1。
                完。
            完。
        完。
    完。
    若 做 计数 用 编译顺序 小于 模块数：
        可变 环数 为 模块数 减 做 计数 用 编译顺序。
        做 模块图_打印详细 用 做 字符串拼接 用 「[拓扑] 检测到循环依赖, 环中模块=」、做 转字符串 用 环数。
        可变 _环模块 为 一组：（空）。
        可变 ci 为 0。
        只要 ci 小于 模块数：
            若 做 字典取 用 入度、模块表 的 第 ci 项 的 名称 大于 0：
                做 追加 用 _环模块、模块表 的 第 ci 项 的 名称。
            完。
            ci 为 ci 加 1。
        完。
        做 排序 用 _环模块、0。
        ci 为 0。
        只要 ci 小于 做 计数 用 _环模块：
            做 追加 用 编译顺序、_环模块 的 第 ci 项。
            ci 为 ci 加 1。
        完。
    完。
    返还 编译顺序。
完。

模块_计算哈希 接受 源码：
    可变 哈希值 为 14695981039346656037。
    长度 为 做 字节长度 用 源码。
    可变 i 为 0。
    只要 i 小于 长度：
        哈希值 为 哈希值 位异或 做 字节取 用 源码、i。
        哈希值 为 哈希值 乘 1099511628211。
        哈希值 为 哈希值 位与 0x7FFFFFFFFFFFFFFF。
        i 为 i 加 1。
    完。
    返还 哈希值。
完。

可变 _重编状态 为 一组：做 字典、做 字典。

模块_重编检查重置 接受 零参：
    _重编状态 的 第 0 项 为 做 字典。
    _重编状态 的 第 1 项 为 做 字典。
完。

模块_依赖是否变更 接受 依赖列表：
    可变 j 为 0。
    只要 j 小于 做 计数 用 依赖列表：
        若 做 模块_需要重编 用 依赖列表 的 第 j 项：
            返还 真。
        完。
        j 为 j 加 1。
    完。
    返还 假。
完。

模块_需要重编 接受 模块名：
    已缓存 为 做 字典取 用 _重编状态 的 第 0 项、模块名。
    若 已缓存 不等于 空：
        返还 已缓存。
    完。
    若 做 字典取 用 _重编状态 的 第 1 项、模块名 不等于 空：
        返还 假。
    完。
    做 字典设 用 _重编状态 的 第 1 项、模块名、真。
    若 非 做 字典有 用 模块索引、模块名：
        做 字典设 用 _重编状态 的 第 0 项、模块名、真。
        返还 真。
    完。
    位置 为 做 字典取 用 模块索引、模块名。
    信息 为 模块表 的 第 位置 项。
    若 信息 的 目标码 等于 空 或者 信息 的 编译状态 不等于 2：
        做 字典设 用 _重编状态 的 第 0 项、模块名、真。
        返还 真。
    完。
    源码 为 做 读文件 用 信息 的 路径。
    若 源码 等于 空：
        做 字典设 用 _重编状态 的 第 0 项、模块名、真。
        返还 真。
    完。
    若 当前哈希 不等于 信息 的 哈希 或者 信息 的 哈希 等于 0：
        做 字典设 用 _重编状态 的 第 0 项、模块名、真。
        返还 真。
    完。
    依赖组 为 信息 的 依赖列表。
    若 依赖组 不等于 空 并且 做 是列表 用 依赖组：
        若 做 模块_依赖是否变更 用 依赖组：
            做 字典设 用 _重编状态 的 第 0 项、模块名、真。
            返还 真。
        完。
    完。
    做 字典设 用 _重编状态 的 第 0 项、模块名、假。
    返还 假。
完。

_模块_增量编译_处理单个 接受 名、统计：
    做 模块图_阶段标记 用 「增量循环/模块前」、名。
    需要重编 为 做 模块_需要重编 用 名。
    若 需要重编：
        做 模块图_阶段标记 用 「增量循环/重编前」、名。
        编译成功 为 做 模块_编译单个 用 名。
        若 编译成功：
            做 模块图_阶段标记 用 「增量循环/重编成功」、名。
            统计 的 第 0 项 为 统计 的 第 0 项 加 1。
        否则：
            做 模块图_阶段标记 用 「增量循环/重编失败」、名。
            统计 的 第 2 项 为 统计 的 第 2 项 加 1。
            做 模块_回滚 用 名。
        完。
    否则：
        做 模块图_阶段标记 用 「增量循环/跳过」、名。
        统计 的 第 1 项 为 统计 的 第 1 项 加 1。
    完。
    做 模块图_阶段标记 用 「增量循环/模块后」、名。
完。

模块_增量编译 接受 零参：
    做 模块_重编检查重置。
    做 模块_拓扑排序 用 0。
    顺序 为 编译顺序。
    可变 统计 为 一组：0、0、0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 顺序：
        做 _模块_增量编译_处理单个 用 顺序 的 第 i 项、统计。
        i 为 i 加 1。
    完。
    做 模块图_打印详细 用 做 字符串拼接 用 「增量编译: 」、做 字符串拼接 用 做 转字符串 用 统计 的 第 0 项、做 字符串拼接 用 「编译 」、做 字符串拼接 用 做 转字符串 用 统计 的 第 1 项、做 字符串拼接 用 「跳过 」、做 字符串拼接 用 做 转字符串 用 统计 的 第 2 项、「failed」。
    返还 统计 的 第 2 项 等于 0。
完。

模块_处理_节_定义 接受 预扫句：
    预扫名 为 预扫句 的 第 1 项。
    若 做 字典取 用 全局变量表、预扫名 等于 空：
        做 字典设 用 全局变量表、预扫名、全局变量数 加 1。
        全局变量数 为 全局变量数 加 1。
    完。
完。

模块_处理_2 接受 _vm：
    做 发射 用 0xFA。
    做 发射 用 0xF4。
    做 发射组 用 一组：0xEB、0xFC。
完。

模块_检查括号匹配 接受 源码、文件名：
    可变 深度 为 0。
    可变 行号 为 1。
    可变 最后开括行 为 0。
    可变 i 为 0。
    长度 为 做 字节长度 用 源码。
    只要 i 小于 长度：
        字节 为 做 字节取 用 源码、i。
        若 字节 等于 字符_换行：
            行号 为 行号 加 1。
            i 为 i 加 1。
            继续。
        完。
        若 字节 等于 字符_斜杠 并且 i 加 1 小于 长度 并且 做 字节取 用 源码、i 加 1 等于 字符_斜杠：
            只要 i 小于 长度 并且 做 字节取 用 源码、i 不等于 字符_换行：
                i 为 i 加 1。
            完。
            继续。
        完。
        若 字节 等于 标记_余 或者 字节 等于 39：
            引号 为 字节。
            i 为 i 加 1。
            可变 _扫串 为 真。
            只要 i 小于 长度 并且 _扫串：
                sc 为 做 字节取 用 源码、i。
                若 sc 等于 字符_换行：
                    行号 为 行号 加 1。
                完。
                若 sc 等于 标记_左花：
                    i 为 i 加 2。
                    继续。
                完。
                若 sc 等于 引号：
                    i 为 i 加 1。
                    _扫串 为 假。
                    继续。
                完。
                i 为 i 加 1。
            完。
            继续。
        完。
        若 字节 等于 标记_中断：
            深度 为 深度 加 1。
            最后开括行 为 行号。
        完。
        若 字节 等于 标记_真：
            深度 为 深度 减 1。
        完。
        若 深度 小于 0：
            做 _进度_记录错误 用 「解析」、文件名、行号、「多余的 '}'」。
            返还 假。
        完。
        i 为 i 加 1。
    完。
    若 深度 大于 0：
        做 _进度_记录错误 用 「解析」、文件名、最后开括行、做 字符串拼接 用 「未闭合的 '{' (缺少 」、做 字符串拼接 用 做 转字符串 用 深度、「 个 '}')」。
        返还 假。
    完。
    返还 真。
完。

模块_检查主程序存在 接受 函数列、文件名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 函数列：
        若 做 是列表 用 函数列 的 第 i 项 并且 做 计数 用 函数列 的 第 i 项 大于或等于 2：
            若 做 字符串相等 用 函数列 的 第 i 项 的 第 1 项、「主程序」：
                返还 真。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

模块_编译单个 接受 模块名：
    可变 _ei 为 0。
    可变 _优fi 为 0。
    可变 预函i 为 0。
    可变 预扫i 为 0。
    若 非 做 字典有 用 模块索引、模块名：
        返还 假。
    完。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [单模块] 」 加 模块名 加 「\n」。
    完。
    位置 为 做 字典取 用 模块索引、模块名。
    可变 信息 为 模块表 的 第 位置 项。
    信息 的 编译状态 为 1。
    做 模块_写回信息 用 位置、信息。
    若 模块图详细模式 的 第 0 项：
        做 模块图_打印详细 用 做 字符串拼接 用 「[编译开始] 」、模块名。
    完。
    做 模块图_阶段标记 用 「保存状态」、模块名。
    做 词法引擎_全局重置。
    做 语法引擎_全局重置。
    做 行号追踪_全局重置。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 做 字符串拼接 用 「  [阶段] 读源码 」、模块名。
    完。
    源码 为 做 读文件 用 信息 的 路径。
    若 源码 等于 空：
        做 打印文本 用 做 字符串拼接 用 「读文件失败: 」、信息 的 路径。
        信息 的 编译状态 为 3。
        做 模块_写回信息 用 位置、信息。
        返还 假。
    完。
    旧版本号 为 信息 的 版本号。
    做 模块图_阶段标记 用 「保存状态完成」、模块名。
    若 非 做 模块_检查括号匹配 用 源码、信息 的 路径：
        信息 的 编译状态 为 3。
        做 模块_写回信息 用 位置、信息。
        返还 假。
    完。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 做 字符串拼接 用 「  [阶段] 词法 」、模块名。
    完。
    做 位置_注册文件 用 信息 的 路径。
    做 模块图_清空列表 用 词行号。
    词表 为 做 前端_词法分析 用 源码。
    若 词表 等于 空 或者 做 计数 用 词表 等于 0：
        做 打印文本 用 做 字符串拼接 用 「词法失败: 」、模块名。
        信息 的 编译状态 为 3。
        做 模块_写回信息 用 位置、信息。
        返还 假。
    完。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 做 字符串拼接 用 「  [阶段] 语法 」、模块名。
    完。
    做 模块图_清空列表 用 匿名函数列。
    程序 为 做 前端_解析_程序 用 词表。
    _解析错误数 为 语法状态 的 第 5 项。
    若 做 是列表 用 程序 等于 假 或者 做 计数 用 程序 小于 2：
        做 打印文本 用 做 字符串拼接 用 「错误: 」、做 字符串拼接 用 信息 的 路径、「 语法分析失败」。
        信息 的 编译状态 为 3。
        做 模块_写回信息 用 位置、信息。
        返还 假。
    完。
    函数列 为 程序 的 第 0 项。
    主体 为 程序 的 第 1 项。
    可变 li 为 0。
    只要 li 小于 做 计数 用 匿名函数列：
        做 追加 用 函数列、匿名函数列 的 第 li 项。
        li 为 li 加 1。
    完。
    若 _解析错误数 大于 0：
        做 打印文本 用 做 字符串拼接 用 「错误: 」、做 字符串拼接 用 信息 的 路径、做 字符串拼接 用 「 语法错误 (」、做 字符串拼接 用 做 转字符串 用 _解析错误数、「个)」。
        信息 的 编译状态 为 3。
        做 模块_写回信息 用 位置、信息。
        返还 假。
    完。
    若 做 计数 用 函数列 等于 0 并且 做 计数 用 主体 等于 0：
        做 模块图_打印详细 用 做 字符串拼接 用 「[跳过] 」、做 字符串拼接 用 信息 的 路径、「 (无函数和语句)」。
        信息 的 编译状态 为 2。
        信息 的 目标码 为 空。
        信息 的 目标码大小 为 0。
        做 模块_写回信息 用 位置、信息。
        返还 真。
    完。
    可变 _语义错误数 为 0。
    可变 _语义严格度 为 0。
    可变 _当前profile 为 「」。
    若 _模块_项目合同 不等于 空 并且 做 是列表 用 _模块_项目合同 并且 做 计数 用 _模块_项目合同 大于或等于 5：
        _当前profile 为 _模块_项目合同 的 第 4 项。
        若 做 字符串相等 用 _当前profile、「analysis-host」：
            _语义严格度 为 2。
        完。
        若 做 字符串相等 用 _当前profile、「kernel-qemu」 或者 做 字符串相等 用 _当前profile、「kernel-physical」：
            _语义严格度 为 0。
        完。
        若 做 字符串相等 用 _当前profile、「linux-user」：
            _语义严格度 为 0。
        完。
    完。
    _语义结果 为 做 语义_最小预检查 用 函数列、主体。
    _语义警告 为 _语义结果 的 第 4 项。
    _语义错误数 为 _语义结果 的 第 5 项。
    _进度_警告 为 _进度_警告 加 _语义警告。
    _进度_语义错误 为 _进度_语义错误 加 _语义错误数。
    _语义_作用域栈 为 _语义结果 的 第 1 项。
    _作用域诊断 为 做 作用域_诊断 用 _语义_作用域栈。
    若 模块图详细模式 的 第 0 项 并且 _作用域诊断 不等于 空：
        做 打印文本 用 做 字符串拼接 用 「  [作用域] 」、做 字符串拼接 用 模块名、做 字符串拼接 用 「 总变量=」、做 字符串拼接 用 做 转字符串 用 _作用域诊断 的 第 1 项、做 字符串拼接 用 「 未使用=」、做 字符串拼接 用 做 转字符串 用 _作用域诊断 的 第 2 项、做 字符串拼接 用 「 最大深度=」、做 转字符串 用 _作用域诊断 的 第 3 项。
    完。
    可变 _借用违规数 为 0。
    可变 _生命周期违规 为 0。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 借用进入\n」。
    完。
    做 借用_进入作用域。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 约束检查\n」。
    完。
    做 约束_检查一致性。
    _生命周期违规 为 _生命周期_违规数。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 借用退出\n」。
    完。
    做 借用_退出作用域。
    _借用违规数 为 _生命周期违规。
    可变 _AI约束违规 为 0。
    若 _语义严格度 等于 2：
        _语义错误数 为 _语义错误数 加 _语义警告 加 _借用违规数 加 _AI约束违规。
    完。
    若 _语义严格度 等于 1：
        _语义错误数 为 _语义错误数 加 _借用违规数。
    完。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 优化准备\n」。
    完。
    若 做 字符串相等 用 _当前profile、「analysis-host」：
        做 设置优化等级 用 3。
    否则若 做 字符串相等 用 _当前profile、「kernel-qemu」 或者 做 字符串相等 用 _当前profile、「kernel-physical」：
        做 设置优化等级 用 2。
    否则：
        做 设置优化等级 用 2。
    完。
    _优化报告 为 一组：0、0、0、0。
    可变 _尾调数 为 0。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 尾调扫描\n」。
    完。
    只要 _优fi 小于 做 计数 用 函数列：
        若 做 是列表 用 函数列 的 第 _优fi 项 并且 做 计数 用 函数列 的 第 _优fi 项 大于或等于 4：
            _优函体 为 函数列 的 第 _优fi 项 的 第 3 项。
            若 做 是列表 用 _优函体 并且 做 计数 用 _优函体 大于 0：
                _尾类型 为 做 尾调用优_分析尾部 用 _优函体、函数列 的 第 _优fi 项 的 第 1 项、空。
                若 _尾类型 大于 0：
                    _尾调数 为 _尾调数 加 1。
                完。
            完。
        完。
        _优fi 为 _优fi 加 1。
    完。
    可变 _ai路径函数数 为 0。
    可变 _裸机禁内联数 为 0。
    可变 _pfi 为 0。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] AI路径扫描\n」。
    完。
    只要 _pfi 小于 做 计数 用 函数列：
        若 做 是列表 用 函数列 的 第 _pfi 项 并且 做 计数 用 函数列 的 第 _pfi 项 大于或等于 4：
            _pf名 为 函数列 的 第 _pfi 项 的 第 1 项。
            _算子标签 为 做 IR标签_从函数名推断 用 _pf名。
            _模态标签 为 做 IR标签_从函数名推断模态 用 _pf名。
            做 IR标签表_设置 用 _pfi、_算子标签、_模态标签、0、0。
            若 _算子标签 不等于 IR标签_普通：
                _ai路径函数数 为 _ai路径函数数 加 1。
                做 管线_记录AI节点。
            完。
            若 非 做 管线_函数可内联 用 _pf名：
                _裸机禁内联数 为 _裸机禁内联数 加 1。
                做 管线_记录裸机禁内联。
            完。
        完。
        _pfi 为 _pfi 加 1。
    完。
    做 管线_记录运行。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 效果能力\n」。
    完。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 效果聚合\n」。
    完。
    _模块效果 为 做 效果_模块聚合 用 模块名、函数列。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 能力推断\n」。
    完。
    _模块能力推断 为 做 能力_从模块名推断 用 模块名。
    可变 _efi 为 0。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 能力授权\n」。
    完。
    只要 _efi 小于 做 计数 用 _模块能力推断：
        做 能力_授权 用 模块名、_模块能力推断 的 第 _efi 项、1、假。
        _efi 为 _efi 加 1。
    完。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 效果违规检查\n」。
    完。
    可变 _效果违规总数 为 做 效果_批量检查违规 用 _当前profile、函数列。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 能力违规检查\n」。
    完。
    可变 _能力违规总数 为 做 能力_批量检查profile 用 _当前profile、模块名、_模块效果。
    若 _语义严格度 大于或等于 1：
        _语义错误数 为 _语义错误数 加 _效果违规总数 加 _能力违规总数。
    完。
    若 做 计数 用 _模块效果 大于 0 或者 _效果违规总数 大于 0 或者 _能力违规总数 大于 0：
        若 模块图详细模式 的 第 0 项：
            可变 _效果文本 为 「」。
            只要 _ei 小于 做 计数 用 _模块效果：
                若 _ei 大于 0：
                    _效果文本 为 _效果文本 加 「,」。
                完。
                _效果文本 为 _效果文本 加 做 效果名 用 _模块效果 的 第 _ei 项。
                _ei 为 _ei 加 1。
            完。
            做 模块图_打印详细 用 做 字符串拼接 用 「[效果] 」、做 字符串拼接 用 模块名、做 字符串拼接 用 「 效果=[」、做 字符串拼接 用 _效果文本、做 字符串拼接 用 「] 效果违规=」、做 字符串拼接 用 做 转字符串 用 _效果违规总数、做 字符串拼接 用 「 能力违规=」、做 转字符串 用 _能力违规总数。
        完。
        若 _效果违规总数 大于 0：
            做 效果_输出违规详情。
        完。
        若 _能力违规总数 大于 0：
            做 能力_输出违规详情。
        完。
    完。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 效果能力收尾\n」。
    完。
    可变 _管线优化级 为 2。
    若 做 字符串相等 用 _当前profile、「analysis-host」：
        _管线优化级 为 3。
    完。
    可变 _模块标签列 为 空。
    若 _模块_项目合同 不等于 空 并且 做 是列表 用 _模块_项目合同 并且 做 计数 用 _模块_项目合同 大于或等于 7：
        _子列 为 _模块_项目合同 的 第 6 项。
        若 做 是列表 用 _子列：
            可变 _tsi 为 0。
            只要 _tsi 小于 做 计数 用 _子列：
                若 做 是列表 用 _子列 的 第 _tsi 项 并且 做 计数 用 _子列 的 第 _tsi 项 大于或等于 2：
                    若 做 字符串包含 用 模块名、_子列 的 第 _tsi 项 的 第 0 项：
                        _模块标签列 为 一组：「内核态」。
                    完。
                完。
                _tsi 为 _tsi 加 1。
            完。
        完。
    完。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 流水线开始\n」。
    完。
    _管线结果 为 做 流水线_执行_模块级 用 模块名、信息 的 路径、_管线优化级、_当前profile、_模块标签列、函数列、主体。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 「  [阶段] 流水线完成\n」。
    完。
    _管线报告 为 _管线结果 的 第 1 项。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 做 字符串拼接 用 「  [流水线] 」、做 字符串拼接 用 模块名、做 字符串拼接 用 「 阶段数=」、做 转字符串 用 做 计数 用 _管线报告。
    完。
    若 非 做 流水线_逃逸门控通过：
        _语义错误数 为 _语义错误数 加 1。
        做 打印文本 用 做 字符串拼接 用 「  [逃逸阻塞] 」、做 字符串拼接 用 模块名、做 字符串拼接 用 「 逃逸数=」、做 字符串拼接 用 做 转字符串 用 _语义_动态逃逸数、做 字符串拼接 用 「 阈值=」、做 转字符串 用 _语义_逃逸阈值。
    完。
    若 _语义错误数 大于 0 并且 _语义严格度 大于或等于 1：
        做 打印文本 用 做 字符串拼接 用 「  [阻塞] 」、做 字符串拼接 用 模块名、做 字符串拼接 用 「 profile=」、做 字符串拼接 用 _当前profile、做 字符串拼接 用 「 语义错误=」、做 字符串拼接 用 做 转字符串 用 _语义错误数、「 → 跳过代码生成」。
        信息 的 目标码 为 空。
        信息 的 目标码大小 为 0。
        做 模块_写回信息 用 位置、信息。
        返还 真。
    完。
    若 模块图详细模式 的 第 0 项：
        做 打印文本 用 做 字符串拼接 用 「  [阶段] 代码生成 」、模块名。
    完。
    做 模块图_阶段标记 用 「代码生成前」、模块名。
    保存_代码生成状态 为 做 模块_保存代码生成状态。
    若 模块图详细模式 的 第 0 项：
        做 模块图_打印详细 用 做 字符串拼接 用 「[代码生成前] 」、模块名。
    完。
    做 模块_同步目标格式。
    做 模块图_阶段标记 用 「重置代码生成状态」、模块名。
    做 模块_重置模块代码生成状态。
    做 模块图_阶段标记 用 「运行时注册」、模块名。
    做 发射_权威运行时 用 函数表。
    代码 为 一组：（空）。
    代码位 为 0。
    修正表 为 一组：（空）。
    函数表 为 做 字典。
    做 发射 用 0x90。
    做 编译_设置当前模块名 用 模块名。
    做 编译_设置源文件名 用 信息 的 路径。
    若 模块图详细模式 的 第 0 项：
        做 模块图_打印详细 用 做 字符串拼接 用 「[代码生成中] 」、模块名。
    完。
    做 模块图_阶段标记 用 「闭包检测」、模块名。
    做 模块图_阶段标记 用 「全局变量预扫」、模块名。
    只要 预扫i 小于 做 计数 用 主体：
        预扫句 为 主体 的 第 预扫i 项。
        若 做 是列表 用 预扫句 等于 假 或者 做 计数 用 预扫句 小于 2：
            预扫i 为 预扫i 加 1。
            继续。
        完。
        若 预扫句 的 第 0 项 等于 节_定义：
            做 模块_处理_节_定义 用 预扫句。
        完。
        预扫i 为 预扫i 加 1。
    完。
    做 模块图_阶段标记 用 「函数预注册」、模块名。
    若 做 字典取 用 全局变量表、「__错误_标志」 等于 空：
        做 字典设 用 全局变量表、「__错误_标志」、全局变量数 加 1。
        全局变量数 为 全局变量数 加 1。
    完。
    若 做 字典取 用 全局变量表、「__错误_值」 等于 空：
        做 字典设 用 全局变量表、「__错误_值」、全局变量数 加 1。
        全局变量数 为 全局变量数 加 1。
    完。
    若 做 字典取 用 全局变量表、「__错误_基指针」 等于 空：
        做 字典设 用 全局变量表、「__错误_基指针」、全局变量数 加 1。
        全局变量数 为 全局变量数 加 1。
    完。
    若 做 字典取 用 全局变量表、「__错误_栈指针」 等于 空：
        做 字典设 用 全局变量表、「__错误_栈指针」、全局变量数 加 1。
        全局变量数 为 全局变量数 加 1。
    完。
    若 做 字典取 用 全局变量表、「__错误_跳转」 等于 空：
        做 字典设 用 全局变量表、「__错误_跳转」、全局变量数 加 1。
        全局变量数 为 全局变量数 加 1。
    完。
    若 模块图详细模式 的 第 0 项：
        做 模块图_打印详细 用 做 字符串拼接 用 「[函数预注册] 」、模块名。
    完。
    只要 预函i 小于 做 计数 用 函数列：
        若 做 是列表 用 函数列 的 第 预函i 项 并且 做 计数 用 函数列 的 第 预函i 项 大于或等于 2：
            预函名 为 函数列 的 第 预函i 项 的 第 1 项。
            若 做 字典取 用 函数表、预函名 等于 空：
                做 字典设 用 函数表、预函名、0 减 1。
            完。
        完。
        预函i 为 预函i 加 1。
    完。
    若 模块图详细模式 的 第 0 项：
        做 模块图_打印详细 用 做 字符串拼接 用 「[函数预注册完成] 」、模块名。
    完。
    做 闭包分析_函数列 用 函数列。
    做 模块图_阶段标记 用 「函数编译循环」、模块名。
    可变 编译失败数 为 0。
    _编译前代码位 为 代码位。
    可变 _pass0_i 为 0。
    只要 _pass0_i 小于 做 计数 用 函数列：
        若 做 是列表 用 函数列 的 第 _pass0_i 项 并且 做 计数 用 函数列 的 第 _pass0_i 项 大于或等于 4：
            _fn名_p0 为 函数列 的 第 _pass0_i 项 的 第 1 项。
            _fn体_p0 为 函数列 的 第 _pass0_i 项 的 第 3 项。
            做 _调用点扫描_填参数类型 用 _fn体_p0。
            做 _返回类型扫描_填函数返回类型 用 _fn名_p0、_fn体_p0。
        完。
        _pass0_i 为 _pass0_i 加 1。
    完。
    可变 _pass0_bi 为 0。
    只要 _pass0_bi 小于 做 计数 用 主体：
        做 _调用点扫描_填参数类型 用 主体 的 第 _pass0_bi 项。
        _pass0_bi 为 _pass0_bi 加 1。
    完。
    可变 fi 为 0。
    只要 fi 小于 做 计数 用 函数列：
        若 做 是列表 用 函数列 的 第 fi 项 等于 假 或者 做 计数 用 函数列 的 第 fi 项 小于 4：
            fi 为 fi 加 1。
            继续。
        完。
        _p1名 为 函数列 的 第 fi 项 的 第 1 项。
        若 做 字节长度 用 _p1名 大于或等于 10 并且 做 字节取 用 _p1名、0 等于 字符_下划线 并且 做 字节取 用 _p1名、1 等于 字符_下划线：
            fi 为 fi 加 1。
            继续。
        完。
        若 模块图详细模式 的 第 0 项：
            做 模块图_打印详细 用 做 字符串拼接 用 「[编译函数] 」、_p1名。
        完。
        函数前位 为 代码位。
        做 编译_函数 用 函数列 的 第 fi 项、空。
        若 代码位 等于 函数前位 并且 做 计数 用 函数列 的 第 fi 项 大于或等于 4：
            编译失败数 为 编译失败数 加 1。
        完。
        fi 为 fi 加 1。
    完。
    fi 为 0。
    只要 fi 小于 做 计数 用 函数列：
        若 做 是列表 用 函数列 的 第 fi 项 等于 假 或者 做 计数 用 函数列 的 第 fi 项 小于 4：
            fi 为 fi 加 1。
            继续。
        完。
        _p2名 为 函数列 的 第 fi 项 的 第 1 项。
        若 做 字节长度 用 _p2名 小于 10 或者 做 字节取 用 _p2名、0 不等于 字符_下划线 或者 做 字节取 用 _p2名、1 不等于 字符_下划线：
            fi 为 fi 加 1。
            继续。
        完。
        若 模块图详细模式 的 第 0 项：
            做 模块图_打印详细 用 做 字符串拼接 用 「[编译lambda] 」、_p2名。
        完。
        函数前位2 为 代码位。
        _fn闭包 为 做 字典取 用 闭包表、_p2名。
        做 编译_函数 用 函数列 的 第 fi 项、_fn闭包。
        若 代码位 等于 函数前位2 并且 做 计数 用 函数列 的 第 fi 项 大于或等于 4：
            编译失败数 为 编译失败数 加 1。
        完。
        fi 为 fi 加 1。
    完。
    做 模块图_阶段标记 用 「主体编译」、模块名。
    入口 为 代码位。
    当前函数名 为 「__主程序__」。
    变量表 为 做 字典。
    变量数 为 0。
    主体模式 为 1。
    做 发射_序言。
    若 做 计数 用 主体 大于 0：
        可变 si 为 0。
        只要 si 小于 做 计数 用 主体：
            做 编译_语句 用 主体 的 第 si 项。
            si 为 si 加 1。
        完。
    完。
    若 模块图编译器_目标ELF 等于 2：
        做 模块_处理_2 用 空。
    完。
    做 发射_尾声。
    做 发射 用 0xCC。
    对齐帧 为 (变量数 加 8) 乘 8。
    对齐帧2 为 (对齐帧 加 8 加 15) 除以 16 乘 16 减 8。
    若 对齐帧2 小于 4096：
        对齐帧2 为 4096。
    完。
    做 内存写32 用 代码、帧大小位、对齐帧2。
    信息 的 入口偏移 为 入口。
    若 编译失败数 大于 0：
        做 打印文本 用 做 字符串拼接 用 「代码生成警告: 」、做 字符串拼接 用 模块名、做 字符串拼接 用 「 (」、做 字符串拼接 用 做 转字符串 用 编译失败数、「个函数未产出代码)」。
    完。
    若 _编译前代码位 小于 代码位：
        _窥孔数 为 做 窥孔_后处理_字节 用 _编译前代码位、代码位。
        若 _窥孔数 大于 0：
            做 模块图_打印详细 用 做 字符串拼接 用 「[窥孔] 」、做 字符串拼接 用 模块名、做 字符串拼接 用 「 优化=」、做 转字符串 用 _窥孔数。
        完。
    完。
    可变 _嵌入字串i 为 0。
    只要 _嵌入字串i 小于 做 计数 用 字串表：
        做 追加 用 字串偏移、代码位。
        _嵌入字串 为 字串表 的 第 _嵌入字串i 项。
        可变 _嵌入字节i 为 0。
        只要 _嵌入字节i 小于 做 字节长度 用 _嵌入字串：
            做 发射 用 做 字节取 用 _嵌入字串、_嵌入字节i。
            _嵌入字节i 为 _嵌入字节i 加 1。
        完。
        做 发射 用 0。
        _嵌入字串i 为 _嵌入字串i 加 1。
    完。
    可变 _未决修正 为 一组：（空）。
    可变 _修正i 为 0。
    只要 _修正i 小于 做 计数 用 修正表：
        _修类 为 做 修正表_取类型 用 修正表、_修正i。
        _修位 为 做 修正表_取位置 用 修正表、_修正i。
        _修附 为 做 修正表_取附加 用 修正表、_修正i。
        _修步长 为 做 修正表_项步长 用 修正表、_修正i。
        若 做 字符串相等 用 _修类、「str」：
            _str索引 为 _修附。
            若 做 是整数 用 _str索引 并且 _str索引 大于或等于 0 并且 _str索引 小于 做 计数 用 字串偏移：
                _字串位 为 字串偏移 的 第 _str索引 项。
                做 内存写32 用 代码、_修位、_字串位 减 (_修位 加 4)。
            否则：
                做 修正表_追加现成 用 _未决修正、修正表、_修正i。
            完。
        否则：
            做 修正表_追加现成 用 _未决修正、修正表、_修正i。
        完。
        _修正i 为 _修正i 加 _修步长。
    完。
    本模块代码大小 为 代码位。
    做 模块图_打印详细 用 做 字符串拼接 用 「[模块代码容器] 」、做 字符串拼接 用 模块名、做 字符串拼接 用 「 list=」、做 字符串拼接 用 做 转字符串 用 做 是列表 用 代码、做 字符串拼接 用 「 size=」、做 转字符串 用 本模块代码大小。
    若 本模块代码大小 大于 0：
        信息 的 目标码 为 代码。
        信息 的 目标码大小 为 本模块代码大小。
        做 模块图_打印详细 用 做 字符串拼接 用 「[模块入口] 」、做 字符串拼接 用 模块名、做 字符串拼接 用 「 entry=」、做 字符串拼接 用 做 转字符串 用 信息 的 入口偏移、做 字符串拼接 用 「 size=」、做 字符串拼接 用 做 转字符串 用 信息 的 目标码大小、做 字符串拼接 用 「 」、做 模块图_入口字节摘要 用 代码、信息 的 入口偏移。
    否则：
        信息 的 目标码 为 空。
        信息 的 目标码大小 为 0。
    完。
    若 模块图详细模式 的 第 0 项：
        可变 _后处理_注册 为 0。
        可变 _后处理_索引 为 0。
        只要 _后处理_索引 小于 做 计数 用 _未决修正：
            _后处理_步长 为 做 修正表_项步长 用 _未决修正、_后处理_索引。
            若 做 字符串相等 用 做 修正表_取类型 用 _未决修正、_后处理_索引、「rt_globals」：
                _后处理_注册 为 _后处理_注册 加 1。
            完。
            _后处理_索引 为 _后处理_索引 加 _后处理_步长。
        完。
        做 模块图_打印详细 用 做 字符串拼接 用 「[修正表] 」、做 字符串拼接 用 模块名、做 字符串拼接 用 「 未决=」、做 字符串拼接 用 做 转字符串 用 做 计数 用 _未决修正、做 字符串拼接 用 「 rt_globals=」、做 转字符串 用 _后处理_注册。
    完。
    信息 的 修正表数据 为 _未决修正。
    信息 的 函数表数据 为 函数表。
    做 字典设 用 _模块行号映射、模块名、做 编译_行号映射表。
    做 编译_重置行号映射。
    做 模块图_阶段标记 用 「恢复状态」、模块名。
    做 模块_恢复代码生成状态 用 保存_代码生成状态、假。
    做 编译_设置当前模块名 用 「」。
    若 模块图详细模式 的 第 0 项：
        做 模块图_打印详细 用 做 字符串拼接 用 「[代码生成后] 」、模块名。
    完。
    做 模块图_阶段标记 用 「导出符号」、模块名。
    信息 的 导出符号 为 一组：（空）。
    fi 为 0。
    只要 fi 小于 做 计数 用 函数列：
        若 做 是列表 用 函数列 的 第 fi 项 并且 做 计数 用 函数列 的 第 fi 项 大于或等于 2：
            做 追加 用 信息 的 导出符号、函数列 的 第 fi 项 的 第 1 项。
        完。
        fi 为 fi 加 1。
    完。
    做 模块图_阶段标记 用 「缓存保存」、模块名。
    做 模块图_阶段标记 用 「缓存保存/哈希前」、模块名。
    新哈希 为 做 模块_计算哈希 用 源码。
    做 模块图_阶段标记 用 「缓存保存/哈希后」、模块名。
    信息 的 哈希 为 新哈希。
    做 模块图_阶段标记 用 「缓存保存/状态前」、模块名。
    信息 的 编译状态 为 2。
    做 模块图_阶段标记 用 「缓存保存/版本前」、模块名。
    信息 的 版本号 为 旧版本号 加 1。
    做 模块图_阶段标记 用 「缓存保存/版本后」、模块名。
    信息 的 上次编译时间 为 做 时间。
    做 模块_写回信息 用 位置、信息。
    做 模块图_阶段标记 用 「缓存保存/写回后」、模块名。
    若 模块图详细模式 的 第 0 项：
        做 模块图_打印详细 用 做 字符串拼接 用 「[编译完成] 」、模块名。
    完。
    若 模块图详细模式 的 第 0 项：
        做 打印 用 「.」。
    完。
    做 模块图_阶段标记 用 「缓存保存/返回前」、模块名。
    返还 真。
完。

模块_回滚 接受 模块名：
    若 做 字典有 用 模块索引、模块名：
        位置 为 做 字典取 用 模块索引、模块名。
        可变 信息 为 模块表 的 第 位置 项。
        若 信息 的 版本号 大于 0：
            缓存结果 为 做 缓存_加载 用 模块名。
            若 缓存结果 不等于 空：
                信息 的 目标码 为 缓存结果。
                信息 的 目标码大小 为 做 计数 用 缓存结果。
            完。
            信息 的 编译状态 为 2。
            做 模块_写回信息 用 位置、信息。
            做 打印文本 用 做 字符串拼接 用 「回滚模块: 」、做 字符串拼接 用 模块名、做 字符串拼接 用 「 → 版本」、做 转字符串 用 信息 的 版本号。
        否则：
            做 模块_写回信息 用 位置、信息。
            做 打印文本 用 做 字符串拼接 用 「回滚失败(无历史版本): 」、模块名。
        完。
    完。
完。

可变 _跳板表 为 做 字典。

可变 _旧代码池 为 一组：（空）。

跳板_注册 接受 函数名、代码地址：
    做 字典设 用 _跳板表、函数名、代码地址。
完。

跳板_查询 接受 函数名：
    若 做 字典有 用 _跳板表、函数名：
        返还 做 字典取 用 _跳板表、函数名。
    完。
    返还 空。
完。

模块_热更新 接受 模块名：
    可变 替换数 为 0。
    若 非 做 字典有 用 模块索引、模块名：
        返还 假。
    完。
    位置 为 做 字典取 用 模块索引、模块名。
    旧信息目标码 为 模块表 的 第 位置 项 的 目标码。
    编译成功 为 做 模块_编译单个 用 模块名。
    若 非 编译成功：
        做 打印文本 用 做 字符串拼接 用 「热更新失败, 保持旧版: 」、模块名。
        做 模块_回滚 用 模块名。
        返还 假。
    完。
    新信息 为 模块表 的 第 位置 项。
    新函数键 为 做 字典键 用 新信息 的 函数表数据。
    做 排序 用 新函数键、0。
    可变 ki 为 0。
    只要 ki 小于 做 计数 用 新函数键：
        函数名 为 新函数键 的 第 ki 项。
        新偏移 为 做 字典取 用 新信息 的 函数表数据、函数名。
        做 跳板_注册 用 函数名、新偏移。
        替换数 为 替换数 加 1。
        ki 为 ki 加 1。
    完。
    若 旧信息目标码 不等于 空：
        做 追加 用 _旧代码池、旧信息目标码。
    完。
    只要 做 计数 用 _旧代码池 大于 3：
        做 删除 用 _旧代码池。
    完。
    做 打印文本 用 做 字符串拼接 用 「热更新成功: 」、做 字符串拼接 用 模块名、做 字符串拼接 用 「 v」、做 字符串拼接 用 做 转字符串 用 新信息 的 版本号、做 字符串拼接 用 「 (」、做 字符串拼接 用 做 转字符串 用 替换数、「个函数替换)」。
    返还 真。
完。

_map字节_追加整数 接受 n、buf：
    若 n 等于 0：
        做 追加 用 buf、48。
        返还 0。
    完。
    可变 临时 为 一组：（空）。
    可变 v 为 n。
    只要 v 大于 0：
        做 追加 用 临时、48 加 v 取余于 10。
        v 为 v 除以 10。
    完。
    可变 i 为 做 计数 用 临时 减 1。
    只要 i 大于或等于 0：
        做 追加 用 buf、临时 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 0。
完。

模块_处理_格式_PE 接受 vm：
    pe结果 为 做 构建PE 用 入口偏移、代码位。
    做 写二进制 用 输出路径、pe结果 的 第 0 项、pe结果 的 第 1 项。
    做 模块图_打印详细 用 做 字符串拼接 用 「PE写入成功: 」、输出路径。
完。

模块_分块写文件 接受 路径、数据、大小、清空：
    临时路径 为 做 字符串拼接 用 路径、「.__chunk」。
    若 清空 不等于 0：
        做 __系统命令 用 做 字符串拼接 用 「rm -f '」、做 字符串拼接 用 路径、做 字符串拼接 用 「' '」、做 字符串拼接 用 临时路径、「'」。
    否则：
        做 __系统命令 用 做 字符串拼接 用 「rm -f '」、做 字符串拼接 用 临时路径、「'」。
    完。
    可变 偏移 为 0。
    只要 偏移 小于 大小：
        可变 块长 为 65536。
        若 大小 小于或等于 4096：
            块长 为 16。
        完。
        若 偏移 加 块长 大于 大小：
            块长 为 大小 减 偏移。
        完。
        可变 块 为 一组：（空）。
        可变 bi 为 0。
        只要 bi 小于 块长：
            做 追加 用 块、数据 的 第 偏移 加 bi 项。
            bi 为 bi 加 1。
        完。
        做 写二进制 用 临时路径、块、块长。
        做 __系统命令 用 做 字符串拼接 用 「cat '」、做 字符串拼接 用 临时路径、做 字符串拼接 用 「' >> '」、做 字符串拼接 用 路径、「'」。
        偏移 为 偏移 加 块长。
    完。
    做 __系统命令 用 做 字符串拼接 用 「rm -f '」、做 字符串拼接 用 临时路径、「'」。
    返还 1。
完。

模块_追加16LE 接受 buf、值：
    做 追加 用 buf、值 位与 255。
    做 追加 用 buf、值 右移 8 位与 255。
完。

模块_追加32LE 接受 buf、值：
    做 追加 用 buf、值 位与 255。
    做 追加 用 buf、值 右移 8 位与 255。
    做 追加 用 buf、值 右移 16 位与 255。
    做 追加 用 buf、值 右移 24 位与 255。
完。

模块_追加64LE 接受 buf、值：
    做 追加 用 buf、值 位与 255。
    做 追加 用 buf、值 右移 8 位与 255。
    做 追加 用 buf、值 右移 16 位与 255。
    做 追加 用 buf、值 右移 24 位与 255。
    做 追加 用 buf、值 右移 32 位与 255。
    做 追加 用 buf、值 右移 40 位与 255。
    做 追加 用 buf、值 右移 48 位与 255。
    做 追加 用 buf、值 右移 56 位与 255。
完。

模块_写LinuxELF 接受 输出路径、入口偏移、代码大小：
    代码段文件偏移 为 0x1000。
    基址 为 0x400000。
    代码段虚址 为 基址 加 代码段文件偏移。
    对齐代码大小 为 (代码大小 加 0xFFF) 除以 0x1000 乘 0x1000。
    数据段文件偏移 为 代码段文件偏移 加 对齐代码大小。
    数据段虚址 为 基址 加 数据段文件偏移。
    数据段文件大小 为 0x1000。
    数据段内存大小 为 数据段文件大小 加 0x10000000。
    填充大小 为 数据段文件偏移 减 代码段文件偏移 减 代码大小。
    入口虚址 为 代码段虚址 加 入口偏移。
    代码文件大小 为 代码段文件偏移 加 代码大小。
    可变 首页 为 一组：（空）。
    做 追加 用 首页、0x7F。
    做 追加 用 首页、0x45。
    做 追加 用 首页、0x4C。
    做 追加 用 首页、0x46。
    做 追加 用 首页、2。
    做 追加 用 首页、1。
    做 追加 用 首页、1。
    做 追加 用 首页、0。
    可变 _hzi 为 0。
    只要 _hzi 小于 8：
        做 追加 用 首页、0。
        _hzi 为 _hzi 加 1。
    完。
    做 模块_追加16LE 用 首页、2。
    做 模块_追加16LE 用 首页、62。
    做 模块_追加32LE 用 首页、1。
    做 模块_追加64LE 用 首页、入口虚址。
    做 模块_追加64LE 用 首页、64。
    做 模块_追加64LE 用 首页、0。
    做 模块_追加32LE 用 首页、0。
    做 模块_追加16LE 用 首页、64。
    做 模块_追加16LE 用 首页、56。
    做 模块_追加16LE 用 首页、2。
    做 模块_追加16LE 用 首页、0。
    做 模块_追加16LE 用 首页、0。
    做 模块_追加16LE 用 首页、0。
    做 模块_追加32LE 用 首页、1。
    做 模块_追加32LE 用 首页、7。
    做 模块_追加64LE 用 首页、0。
    做 模块_追加64LE 用 首页、基址。
    做 模块_追加64LE 用 首页、基址。
    做 模块_追加64LE 用 首页、代码文件大小。
    做 模块_追加64LE 用 首页、代码文件大小。
    做 模块_追加64LE 用 首页、0x1000。
    做 模块_追加32LE 用 首页、1。
    做 模块_追加32LE 用 首页、6。
    做 模块_追加64LE 用 首页、数据段文件偏移。
    做 模块_追加64LE 用 首页、数据段虚址。
    做 模块_追加64LE 用 首页、数据段虚址。
    做 模块_追加64LE 用 首页、数据段文件大小。
    做 模块_追加64LE 用 首页、数据段内存大小。
    做 模块_追加64LE 用 首页、0x1000。
    只要 做 计数 用 首页 小于 代码段文件偏移：
        做 追加 用 首页、0。
    完。
    做 打印文本 用 做 字符串拼接 用 「[ELF-CHK] b0=」、做 转字符串 用 首页 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「[ELF-PH64] 」、做 转字符串 用 首页 的 第 64 项。
    做 打印文本 用 做 字符串拼接 用 「[ELF-PH67] 」、做 转字符串 用 首页 的 第 67 项。
    做 打印文本 用 做 字符串拼接 用 「[ELF-PH68] 」、做 转字符串 用 首页 的 第 68 项。
    做 打印文本 用 做 字符串拼接 用 「[ELF-PH71] 」、做 转字符串 用 首页 的 第 71 项。
    可变 _elf整体 为 首页。
    可变 _ci 为 0。
    只要 _ci 小于 代码大小：
        做 追加 用 _elf整体、代码 的 第 _ci 项。
        _ci 为 _ci 加 1。
    完。
    可变 _pi 为 0。
    只要 _pi 小于 填充大小：
        做 追加 用 _elf整体、0。
        _pi 为 _pi 加 1。
    完。
    可变 _di 为 0。
    只要 _di 小于 数据段文件大小：
        做 追加 用 _elf整体、0。
        _di 为 _di 加 1。
    完。
    做 写二进制 用 输出路径、_elf整体、做 计数 用 _elf整体。
    做 __系统命令 用 做 字符串拼接 用 「chmod +x '」、做 字符串拼接 用 输出路径、「'」。
    返还 数据段文件偏移 加 数据段文件大小。
完。

模块图_解析依赖路径 接受 依赖路径、当前目录：
    可变 路径 为 依赖路径。
    只要 做 以开头 用 路径、「./」：
        路径 为 做 子串 用 路径、2、做 计数 用 路径 减 2。
    完。
    可变 目录 为 当前目录。
    只要 做 以开头 用 路径、「../」：
        路径 为 做 子串 用 路径、3、做 计数 用 路径 减 3。
        目录 为 做 模块图_父目录 用 目录。
    完。
    若 做 字节长度 用 目录 大于 0：
        返还 做 模块图_规范路径 用 做 字符串拼接 用 目录、做 字符串拼接 用 「/」、路径。
    完。
    返还 做 模块图_规范路径 用 路径。
完。

可变 _fixup_基址 为 0。

可变 _fixup_fallback偏移 为 0。

可变 _fixup_当前模块名 为 「」。

模块_应用call修正 接受 修正表数据、修索引、全局函数表、合并代码：
    目标函数 为 做 修正表_取附加 用 修正表数据、修索引。
    修位 为 做 修正表_取位置 用 修正表数据、修索引。
    全局目标 为 做 字典取 用 全局函数表、目标函数。
    若 全局目标 等于 空：
        做 打印文本 用 做 字符串拼接 用 「[UNRESOLVED] 」、目标函数。
        修正绝对位2 为 _fixup_基址 加 修位。
        重定位32_后备 为 _fixup_fallback偏移 减 (修正绝对位2 加 4)。
        做 内存写32 用 合并代码、修正绝对位2、重定位32_后备。
        返还 0。
    完。
    修正绝对位 为 _fixup_基址 加 修位。
    重定位32 为 全局目标 减 (修正绝对位 加 4)。
    若 重定位32 小于 0：
        若 重定位32 大于 0 减 200：
            若 模块图详细模式 的 第 0 项：
                做 打印文本 用 做 字符串拼接 用 「[SELF-CALL-G] fn=」、做 字符串拼接 用 目标函数、做 字符串拼接 用 「 重定位32=」、做 字符串拼接 用 做 转字符串 用 重定位32、做 字符串拼接 用 「 abspos=」、做 转字符串 用 修正绝对位。
            完。
        完。
    完。
    做 内存写32 用 合并代码、修正绝对位、重定位32。
    返还 1。
完。

模块_应用call修正_本地 接受 修正表数据、修索引、全局函数表、目标码：
    目标函数 为 做 修正表_取附加 用 修正表数据、修索引。
    修位 为 做 修正表_取位置 用 修正表数据、修索引。
    全局目标 为 做 字典取 用 全局函数表、目标函数。
    若 全局目标 等于 空：
        做 打印文本 用 做 字符串拼接 用 「[UNR] 」、做 字符串拼接 用 _fixup_当前模块名、做 字符串拼接 用 「 -> 」、目标函数。
        修正绝对位 为 _fixup_基址 加 修位。
        重定位32_后备 为 _fixup_fallback偏移 减 (修正绝对位 加 4)。
        做 内存写32 用 目标码、修位、重定位32_后备。
        返还 0。
    完。
    修正绝对位 为 _fixup_基址 加 修位。
    重定位32 为 全局目标 减 (修正绝对位 加 4)。
    若 重定位32 小于 0：
        若 重定位32 大于 0 减 200：
            若 模块图详细模式 的 第 0 项：
                做 打印文本 用 做 字符串拼接 用 「[SELF-CALL] fn=」、做 字符串拼接 用 目标函数、做 字符串拼接 用 「 重定位32=」、做 字符串拼接 用 做 转字符串 用 重定位32、做 字符串拼接 用 「 base=」、做 字符串拼接 用 做 转字符串 用 _fixup_基址、做 字符串拼接 用 「 pos=」、做 转字符串 用 修位。
            完。
        完。
    完。
    做 内存写32 用 目标码、修位、重定位32。
    返还 1。
完。

模块_链接全部 接受 输出路径：
    可变 _mi 为 0。
    可变 _si 为 0。
    可变 ri 为 0。
    做 模块图_阶段标记 用 「链接/开始」、「<bootstrap>」。
    做 模块_拓扑排序 用 0。
    顺序 为 编译顺序。
    做 模块_同步目标格式。
    总全局变量数 为 全局变量数。
    做 模块_初始化链接代码生成状态。
    做 发射_权威运行时 用 函数表。
    _fallback偏移 为 代码位。
    做 发射 用 191。
    做 发射 用 99。
    做 发射 用 0。
    做 发射 用 0。
    做 发射 用 0。
    做 发射 用 184。
    做 发射 用 60。
    做 发射 用 0。
    做 发射 用 0。
    做 发射 用 0。
    做 发射 用 15。
    做 发射 用 5。
    运行时前缀大小 为 代码位。
    运行时前缀修正表 为 修正表。
    运行时前缀函数表 为 函数表。
    做 模块图_打印详细 用 做 字符串拼接 用 「[链接前缀] 运行时前缀大小: 」、做 转字符串 用 运行时前缀大小。
    可变 全部目标码 为 一组：（空）。
    可变 模块基地址 为 一组：（空）。
    可变 全局函数表 为 做 字典。
    可变 模块init列表 为 一组：（空）。
    可变 _模块名列表 为 一组：（空）。
    可变 当前基地址 为 运行时前缀大小。
    可变 首入口偏移 为 0。
    可变 首入口已定 为 假。
    可变 入口偏移 为 0。
    可变 入口已定 为 假。
    可变 运行时函i 为 0。
    运行时函数键 为 做 字典键 用 运行时前缀函数表。
    做 排序 用 运行时函数键、0。
    只要 运行时函i 小于 做 计数 用 运行时函数键：
        运行时函数名 为 运行时函数键 的 第 运行时函i 项。
        做 字典设 用 全局函数表、运行时函数名、做 字典取 用 运行时前缀函数表、运行时函数名。
        运行时函i 为 运行时函i 加 1。
    完。
    做 模块图_打印详细 用 做 字符串拼接 用 「[ENTRY-DIAG] 模块入口模块名=[」、做 字符串拼接 用 模块入口模块名、「]」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 顺序：
        名 为 顺序 的 第 i 项。
        若 做 字典有 用 模块索引、名：
            位置 为 做 字典取 用 模块索引、名。
            信息 为 模块表 的 第 位置 项。
            若 信息 的 编译状态 等于 2 并且 信息 的 目标码 不等于 空：
                若 做 计数 用 全部目标码 小于 3：
                    做 打印文本 用 做 字符串拼接 用 「[链接聚合样本] #」、做 字符串拼接 用 做 转字符串 用 做 计数 用 全部目标码、做 字符串拼接 用 「 」、名。
                完。
                做 __运行时_列表追加 用 全部目标码、信息 的 目标码。
                做 __运行时_列表追加 用 模块基地址、当前基地址。
                若 非 首入口已定：
                    首入口偏移 为 当前基地址 加 信息 的 入口偏移。
                    首入口已定 为 真。
                完。
                若 做 字节长度 用 模块入口模块名 大于 0 并且 做 字符串相等 用 名、模块入口模块名：
                    入口偏移 为 当前基地址 加 信息 的 入口偏移。
                    入口已定 为 真。
                    做 模块图_打印详细 用 做 字符串拼接 用 「[ENTRY-MATCH] 」、做 字符串拼接 用 名、做 字符串拼接 用 「 base=」、做 字符串拼接 用 做 转字符串 用 当前基地址、做 字符串拼接 用 「 entry=」、做 转字符串 用 入口偏移。
                完。
                模块函数键 为 做 字典键 用 信息 的 函数表数据。
                可变 j 为 0。
                做 排序 用 模块函数键、0。
                只要 j 小于 做 计数 用 模块函数键：
                    函数名 为 模块函数键 的 第 j 项。
                    模块内偏移 为 做 字典取 用 信息 的 函数表数据、函数名。
                    若 模块内偏移 大于或等于 0：
                        做 字典设 用 全局函数表、函数名、当前基地址 加 模块内偏移。
                    完。
                    j 为 j 加 1。
                完。
                做 __运行时_列表追加 用 模块init列表、当前基地址 加 信息 的 入口偏移。
                做 __运行时_列表追加 用 _模块名列表、名。
                _实际大小 为 做 计数 用 信息 的 目标码。
                若 信息 的 目标码大小 不等于 _实际大小：
                    做 打印文本 用 做 字符串拼接 用 「[SIZE-MISMATCH] 」、做 字符串拼接 用 名、做 字符串拼接 用 「 声明=」、做 字符串拼接 用 做 转字符串 用 信息 的 目标码大小、做 字符串拼接 用 「 实际=」、做 转字符串 用 _实际大小。
                完。
                若 做 字典有 用 _模块行号映射、名：
                    _lm数据 为 做 字典取 用 _模块行号映射、名。
                    若 _lm数据 不等于 空：
                        可变 _lmi 为 0。
                        只要 _lmi 小于 做 计数 用 _lm数据：
                            _lm条目 为 _lm数据 的 第 _lmi 项。
                            做 追加 用 做 编译_行号映射表、一组：_lm条目 的 第 0 项 加 当前基地址、_lm条目 的 第 1 项。
                            _lmi 为 _lmi 加 1。
                        完。
                    完。
                完。
                当前基地址 为 当前基地址 加 信息 的 目标码大小。
            完。
        完。
        i 为 i 加 1。
    完。
    做 模块图_阶段标记 用 「链接/聚合后」、「<bootstrap>」。
    可变 _rt重写i 为 0。
    只要 _rt重写i 小于 做 计数 用 运行时函数键：
        _rt重写名 为 运行时函数键 的 第 _rt重写i 项。
        做 字典设 用 全局函数表、_rt重写名、做 字典取 用 运行时前缀函数表、_rt重写名。
        _rt重写i 为 _rt重写i 加 1。
    完。
    若 非 入口已定：
        做 打印文本 用 做 字符串拼接 用 「链接失败: 入口模块 [」、做 字符串拼接 用 模块入口模块名、「] 未在已编译模块中找到」。
        返还 假。
    完。
    做 打印文本 用 做 字符串拼接 用 「[链接诊断] 顺序=」、做 字符串拼接 用 做 转字符串 用 做 计数 用 顺序、做 字符串拼接 用 「 前缀=」、做 字符串拼接 用 做 转字符串 用 运行时前缀大小、做 字符串拼接 用 「 当前基址=」、做 字符串拼接 用 做 转字符串 用 当前基地址、做 字符串拼接 用 「 入口已定=」、做 转字符串 用 入口已定。
    做 模块图_打印详细 用 做 字符串拼接 用 「[ENTRY-FINAL] 入口偏移=」、做 字符串拼接 用 做 转字符串 用 入口偏移、做 字符串拼接 用 「 入口已定=」、做 字符串拼接 用 做 转字符串 用 入口已定、做 字符串拼接 用 「 首入口=」、做 转字符串 用 首入口偏移。
    可变 总代码大小 为 当前基地址。
    若 总代码大小 等于 0：
        做 打印文本 用 「链接失败: 无有效目标码」。
        返还 假。
    完。
    _链接_全局函数表 的 第 0 项 为 全局函数表。
    可变 rtfi 为 0。
    只要 rtfi 小于 做 计数 用 运行时前缀修正表：
        修rt类 为 做 修正表_取类型 用 运行时前缀修正表、rtfi。
        修rt步长 为 做 修正表_项步长 用 运行时前缀修正表、rtfi。
        若 做 字符串相等 用 修rt类、「call」：
            _fixup_基址 为 0。
            _fixup_fallback偏移 为 _fallback偏移。
            _rt修 为 做 模块_应用call修正 用 运行时前缀修正表、rtfi、全局函数表、代码。
            若 _rt修 小于 0：
                返还 假。
            完。
        完。
        rtfi 为 rtfi 加 修rt步长。
    完。
    做 符号驻留_重置。
    可变 全局修正表 为 一组：（空）。
    可变 前缀修i 为 0。
    只要 前缀修i 小于 做 计数 用 运行时前缀修正表：
        前修类 为 做 修正表_取类型 用 运行时前缀修正表、前缀修i。
        前修步 为 做 修正表_项步长 用 运行时前缀修正表、前缀修i。
        若 非 做 字符串相等 用 前修类、「call」 并且 前修类 不等于 空：
            前修位 为 做 修正表_取位置 用 运行时前缀修正表、前缀修i。
            前修附 为 做 修正表_取附加 用 运行时前缀修正表、前缀修i。
            做 修正表_追加记录 用 全局修正表、前修类、前修位、前修附。
        完。
        前缀修i 为 前缀修i 加 前修步。
    完。
    可变 修正数 为 0。
    可变 模块序号 为 0。
    可变 _总call数 为 0。
    可变 _call成功 为 0。
    可变 _call回退 为 0。
    可变 _未解析函数名 为 做 字典。
    i 为 0。
    只要 i 小于 做 计数 用 顺序：
        名 为 顺序 的 第 i 项。
        若 做 字典有 用 模块索引、名：
            位置 为 做 字典取 用 模块索引、名。
            信息 为 模块表 的 第 位置 项。
            若 信息 的 编译状态 等于 2 并且 信息 的 目标码 不等于 空：
                基址 为 模块基地址 的 第 模块序号 项。
                ri 为 0。
                只要 ri 小于 做 计数 用 信息 的 修正表数据：
                    修类 为 做 修正表_取类型 用 信息 的 修正表数据、ri。
                    修步长 为 做 修正表_项步长 用 信息 的 修正表数据、ri。
                    若 做 字符串相等 用 修类、「call」：
                        _总call数 为 _总call数 加 1。
                        _fixup_基址 为 基址。
                        _fixup_fallback偏移 为 _fallback偏移。
                        _fixup_当前模块名 为 名。
                        _r 为 做 模块_应用call修正_本地 用 信息 的 修正表数据、ri、全局函数表、信息 的 目标码。
                        若 _r 小于 0：
                            返还 假。
                        完。
                        若 _r 大于 0：
                            _call成功 为 _call成功 加 1。
                        否则：
                            _call回退 为 _call回退 加 1。
                            _fb函名 为 做 修正表_取附加 用 信息 的 修正表数据、ri。
                            若 _fb函名 不等于 空：
                                做 字典设 用 _未解析函数名、_fb函名、名。
                            完。
                        完。
                        修正数 为 修正数 加 _r。
                    否则若 修类 不等于 空：
                        修位 为 做 修正表_取位置 用 信息 的 修正表数据、ri。
                        修附 为 做 修正表_取附加 用 信息 的 修正表数据、ri。
                        做 修正表_追加记录 用 全局修正表、修类、基址 加 修位、修附。
                    完。
                    ri 为 ri 加 修步长。
                完。
                模块序号 为 模块序号 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    做 模块图_打印详细 用 做 字符串拼接 用 「[STEP2] total_call=」、做 字符串拼接 用 做 转字符串 用 _总call数、做 字符串拼接 用 「 ok=」、做 字符串拼接 用 做 转字符串 用 _call成功、做 字符串拼接 用 「 fb=」、做 转字符串 用 _call回退。
    _驻留统计 为 做 符号驻留_统计。
    做 模块图_打印详细 用 做 字符串拼接 用 「[符号去重] 总查询=」、做 字符串拼接 用 做 转字符串 用 _驻留统计 的 第 0 项、做 字符串拼接 用 「 命中=」、做 字符串拼接 用 做 转字符串 用 _驻留统计 的 第 1 项、做 字符串拼接 用 「 唯一符号=」、做 转字符串 用 _驻留统计 的 第 2 项。
    _未解析键 为 做 字典键 用 _未解析函数名。
    若 做 计数 用 _未解析键 大于 0 并且 做 计数 用 模块表 小于或等于 3：
        做 排序 用 _未解析键、0。
        做 打印文本 用 做 字符串拼接 用 「\n警告: 」、做 字符串拼接 用 做 转字符串 用 做 计数 用 _未解析键、「 个未定义的函数调用:」。
        _已定义函数名列表 为 做 字典键 用 全局函数表。
        可变 _ui 为 0。
        _上限 为 10。
        只要 _ui 小于 做 计数 用 _未解析键 并且 _ui 小于 _上限：
            _uf 为 _未解析键 的 第 _ui 项。
            _um 为 做 字典取 用 _未解析函数名、_uf。
            做 打印文本 用 做 字符串拼接 用 「  → 」、做 字符串拼接 用 _uf、做 字符串拼接 用 「()  (在 」、做 字符串拼接 用 _um、「 中)」。
            _相似 为 做 诊断_查找相似符号 用 _uf、_已定义函数名列表、3。
            可变 _si 为 0。
            只要 _si 小于 做 计数 用 _相似：
                做 打印文本 用 做 字符串拼接 用 「      您是否想用: 」、做 字符串拼接 用 _相似 的 第 _si 项、「?」。
                _si 为 _si 加 1。
            完。
            _ui 为 _ui 加 1。
        完。
        若 做 计数 用 _未解析键 大于 _上限：
            做 打印文本 用 做 字符串拼接 用 「  ... 还有 」、做 字符串拼接 用 做 转字符串 用 做 计数 用 _未解析键 减 _上限、「 个」。
        完。
    完。
    若 做 计数 用 _未解析键 大于 0 并且 模块图详细模式 的 第 0 项：
        做 打印文本 用 做 字符串拼接 用 「[链接] 未解析函数数: 」、做 转字符串 用 做 计数 用 _未解析键。
    完。
    运行时前缀代码 为 代码。
    代码 为 一组：（空）。
    代码位 为 0。
    可变 _前缀i 为 0。
    只要 _前缀i 小于 运行时前缀大小：
        做 发射 用 运行时前缀代码 的 第 _前缀i 项。
        _前缀i 为 _前缀i 加 1。
    完。
    可变 k 为 0。
    只要 k 小于 做 计数 用 全部目标码：
        片段 为 全部目标码 的 第 k 项。
        可变 bi 为 0。
        只要 bi 小于 做 计数 用 片段：
            做 发射 用 片段 的 第 bi 项。
            bi 为 bi 加 1。
        完。
        k 为 k 加 1。
    完。
    代码位 为 总代码大小。
    做 模块图_打印详细 用 做 字符串拼接 用 「链接 」、做 字符串拼接 用 做 转字符串 用 做 计数 用 顺序、做 字符串拼接 用 「 模块 (」、做 字符串拼接 用 做 转字符串 用 总代码大小、做 字符串拼接 用 「字节) → 」、输出路径。
    若 模块图详细模式 的 第 0 项：
        可变 _dbi 为 0。
        只要 _dbi 小于 做 计数 用 模块基地址：
            _mn 为 「」。
            若 _dbi 小于 做 计数 用 _模块名列表：
                _mn 为 _模块名列表 的 第 _dbi 项。
            完。
            若 模块基地址 的 第 _dbi 项 大于或等于 5850000 并且 模块基地址 的 第 _dbi 项 小于或等于 5870000：
                做 打印文本 用 做 字符串拼接 用 「[CRASH-MOD] #」、做 字符串拼接 用 做 转字符串 用 _dbi、做 字符串拼接 用 「 addr=」、做 字符串拼接 用 做 转字符串 用 模块基地址 的 第 _dbi 项、做 字符串拼接 用 「 」、_mn。
            完。
            若 _dbi 小于 15 或者 _dbi 大于或等于 94：
                做 打印文本 用 做 字符串拼接 用 「[BASE] #」、做 字符串拼接 用 做 转字符串 用 _dbi、做 字符串拼接 用 「 addr=」、做 字符串拼接 用 做 转字符串 用 模块基地址 的 第 _dbi 项、做 字符串拼接 用 「 」、_mn。
            完。
            _dbi 为 _dbi 加 1。
        完。
    完。
    做 模块图_打印详细 用 做 字符串拼接 用 「[INIT-DIAG] 代码位=」、做 字符串拼接 用 做 转字符串 用 代码位、做 字符串拼接 用 「 总代码大小=」、做 字符串拼接 用 做 转字符串 用 总代码大小、做 字符串拼接 用 「 diff=」、做 字符串拼接 用 做 转字符串 用 代码位 减 总代码大小、做 字符串拼接 用 「 init0=」、做 转字符串 用 模块init列表 的 第 0 项。
    合并init偏移 为 总代码大小。
    做 发射 用 0x55。
    做 发射 用 0x48。
    做 发射 用 0x89。
    做 发射 用 0xE5。
    做 发射 用 0x48。
    做 发射 用 0x83。
    做 发射 用 0xEC。
    做 发射 用 0x28。
    若 总全局变量数 大于 0：
        全局大小 为 总全局变量数 乘 8。
        做 发射 用 0xB9。
        做 发射 用 全局大小 位与 255。
        做 发射 用 全局大小 右移 8 位与 255。
        做 发射 用 全局大小 右移 16 位与 255。
        做 发射 用 全局大小 右移 24 位与 255。
        alloc偏移 为 做 字典取 用 全局函数表、「__运行时_分配」。
        alloc_call位 为 代码位。
        做 发射 用 0xE8。
        分配重定位 为 alloc偏移 减 (alloc_call位 加 5)。
        做 发射 用 分配重定位 位与 255。
        做 发射 用 分配重定位 右移 8 位与 255。
        做 发射 用 分配重定位 右移 16 位与 255。
        做 发射 用 分配重定位 右移 24 位与 255。
        做 发射 用 0x48。
        做 发射 用 0x89。
        做 发射 用 0x05。
        做 修正表_追加记录 用 全局修正表、「rt_globals」、代码位、空。
        做 发射 用 0x00。
        做 发射 用 0x00。
        做 发射 用 0x00。
        做 发射 用 0x00。
    完。
    可变 _initI 为 0。
    可变 _initPos 为 代码位。
    只要 _initI 小于 做 计数 用 模块init列表：
        _目标 为 模块init列表 的 第 _initI 项。
        _相对偏移 为 _目标 减 (_initPos 加 5)。
        做 发射 用 0xE8。
        做 发射 用 _相对偏移 位与 255。
        做 发射 用 _相对偏移 右移 8 位与 255。
        做 发射 用 _相对偏移 右移 16 位与 255。
        做 发射 用 _相对偏移 右移 24 位与 255。
        _initPos 为 _initPos 加 5。
        _initI 为 _initI 加 1。
    完。
    做 发射 用 0xC9。
    做 发射 用 0xC3。
    做 字典设 用 全局函数表、「__初始化全局__」、合并init偏移。
    总代码大小 为 代码位。
    代码位 为 总代码大小。
    修正表 为 全局修正表。
    函数表 为 全局函数表。
    字串表 为 一组：（空）。
    字串偏移 为 一组：（空）。
    若 做 字符串相等 用 当前目标名称、「linux」：
        _start偏移 为 总代码大小。
        可变 _start代码 为 一组：（空）。
        可变 _主程序偏移 为 做 字典取 用 全局函数表、「主程序」。
        可变 _有主程序 为 假。
        若 _主程序偏移 不等于 空 并且 _主程序偏移 大于 0：
            _有主程序 为 真。
        完。
        若 _有主程序：
            _start代码 为 一组：0x49、0x89、0xE5、0x48、0x81、0xEC、0x00、0x00、0x04、0x00、0x4C、0x8D、0x7C、0x24、0x28、0xE8、0x00、0x00、0x00、0x00、0x49、0x89、0xC6、0xE8、0x00、0x00、0x00、0x00、0xE8、0x00、0x00、0x00、0x00、0x48、0x89、0xC7、0x48、0xC7、0xC0、0x3C、0x00、0x00、0x00、0x0F、0x05。
        否则：
            _start代码 为 一组：0x49、0x89、0xE5、0x48、0x81、0xEC、0x00、0x00、0x04、0x00、0x4C、0x8D、0x7C、0x24、0x28、0xE8、0x00、0x00、0x00、0x00、0x49、0x89、0xC6、0xE8、0x00、0x00、0x00、0x00、0x48、0x31、0xFF、0x48、0xC7、0xC0、0x3C、0x00、0x00、0x00、0x0F、0x05。
        完。
        _堆初始化偏移 为 做 字典取 用 全局函数表、「__运行时_堆初始化」。
        若 _堆初始化偏移 等于 空：
            _堆初始化偏移 为 0。
        完。
        _堆重定位 为 _堆初始化偏移 减 (_start偏移 加 20)。
        _start代码 的 第 16 项 为 _堆重定位 位与 255。
        _start代码 的 第 17 项 为 _堆重定位 右移 8 位与 255。
        _start代码 的 第 18 项 为 _堆重定位 右移 16 位与 255。
        _start代码 的 第 19 项 为 _堆重定位 右移 24 位与 255。
        _初始化全局偏移 为 做 字典取 用 全局函数表、「__初始化全局__」。
        若 _初始化全局偏移 等于 空：
            _初始化全局偏移 为 0。
        完。
        _初始化重定位 为 _初始化全局偏移 减 (_start偏移 加 28)。
        _start代码 的 第 24 项 为 _初始化重定位 位与 255。
        _start代码 的 第 25 项 为 _初始化重定位 右移 8 位与 255。
        _start代码 的 第 26 项 为 _初始化重定位 右移 16 位与 255。
        _start代码 的 第 27 项 为 _初始化重定位 右移 24 位与 255。
        若 _有主程序：
            _入口重定位 为 _主程序偏移 减 (_start偏移 加 33)。
            _start代码 的 第 29 项 为 _入口重定位 位与 255。
            _start代码 的 第 30 项 为 _入口重定位 右移 8 位与 255。
            _start代码 的 第 31 项 为 _入口重定位 右移 16 位与 255。
            _start代码 的 第 32 项 为 _入口重定位 右移 24 位与 255。
        完。
        _si 为 0。
        只要 _si 小于 做 计数 用 _start代码：
            做 发射 用 _start代码 的 第 _si 项。
            _si 为 _si 加 1。
        完。
        做 打印文本 用 做 字符串拼接 用 「[START-OFF] 」、做 转字符串 用 _start偏移。
        做 打印文本 用 做 字符串拼接 用 「[START-LEN] 」、做 转字符串 用 做 计数 用 _start代码。
        做 打印文本 用 做 字符串拼接 用 「[CODE-WPOS] 」、做 转字符串 用 代码位。
        做 打印文本 用 做 字符串拼接 用 「[CODE-COUNT] 」、做 转字符串 用 做 计数 用 代码。
        做 打印文本 用 做 字符串拼接 用 「[START-B0] 」、做 转字符串 用 代码 的 第 _start偏移 项。
        做 打印文本 用 做 字符串拼接 用 「[START-B1] 」、做 转字符串 用 代码 的 第 _start偏移 加 1 项。
        做 打印文本 用 做 字符串拼接 用 「[START-B2] 」、做 转字符串 用 代码 的 第 _start偏移 加 2 项。
        做 打印文本 用 做 字符串拼接 用 「[START-B3] 」、做 转字符串 用 代码 的 第 _start偏移 加 3 项。
        总代码大小 为 代码位。
        _数据段文件偏移 为 0x1000 加 (总代码大小 加 0xFFF) 除以 0x1000 乘 0x1000。
        _数据段虚地址 为 0x400000 加 _数据段文件偏移。
        可变 _rg计数 为 0。
        可变 _v未修 为 0。
        可变 _gfi 为 0。
        只要 _gfi 小于 做 计数 用 全局修正表：
            _gft 为 做 修正表_取类型 用 全局修正表、_gfi。
            _gfp 为 做 修正表_取位置 用 全局修正表、_gfi。
            _gf步长 为 做 修正表_项步长 用 全局修正表、_gfi。
            _下一指令指针 为 0x401000 加 _gfp 加 4。
            若 做 字符串相等 用 _gft、「rt_globals」：
                _rg计数 为 _rg计数 加 1。
                若 _gfp 大于或等于 14300 并且 _gfp 小于或等于 14368 并且 模块图详细模式 的 第 0 项：
                    做 打印文本 用 做 字符串拼接 用 「[RGCHK] gfp=」、做 转字符串 用 _gfp。
                完。
                做 内存写32 用 代码、_gfp、_数据段虚地址 加 24 减 _下一指令指针。
                若 _gfp 大于或等于 0 并且 _gfp 加 3 小于 做 计数 用 代码：
                    若 代码 的 第 _gfp 项 等于 0 并且 代码 的 第 _gfp 加 1 项 等于 0 并且 代码 的 第 _gfp 加 2 项 等于 0 并且 代码 的 第 _gfp 加 3 项 等于 0：
                        若 _v未修 小于 10 并且 模块图详细模式 的 第 0 项：
                            做 打印文本 用 做 字符串拼接 用 「[FIXUP-ZERO] pos=」、做 转字符串 用 _gfp。
                        完。
                        _v未修 为 _v未修 加 1。
                    完。
                完。
            否则若 做 字符串相等 用 _gft、「rt_heap」：
                做 内存写32 用 代码、_gfp、_数据段虚地址 减 _下一指令指针。
            否则若 做 字符串相等 用 _gft、「rt_saved_rsp」：
                做 内存写32 用 代码、_gfp、_数据段虚地址 加 16 减 _下一指令指针。
            完。
            _gfi 为 _gfi 加 _gf步长。
        完。
        做 模块图_打印详细 用 做 字符串拼接 用 「[RGCHK] total=」、做 转字符串 用 _rg计数。
        做 模块图_打印详细 用 做 字符串拼接 用 「[VERIFY] fixup_zero=」、做 转字符串 用 _v未修。
        若 模块图详细模式 的 第 0 项：
            可变 _uc 为 0。
            可变 _uci 为 0。
            只要 _uci 小于 做 计数 用 代码 减 4：
                若 代码 的 第 _uci 项 等于 232 并且 代码 的 第 _uci 加 1 项 等于 0 并且 代码 的 第 _uci 加 2 项 等于 0 并且 代码 的 第 _uci 加 3 项 等于 0 并且 代码 的 第 _uci 加 4 项 等于 0：
                    做 打印文本 用 做 字符串拼接 用 「[UE8] pos=」、做 转字符串 用 _uci。
                    _uc 为 _uc 加 1。
                完。
                _uci 为 _uci 加 1。
            完。
            做 模块图_打印详细 用 做 字符串拼接 用 「[CALL-CHK] unpatched_E8=」、做 转字符串 用 _uc。
        完。
        做 mem_记录分配 用 做 计数 用 代码。
        做 mem_记录分配 用 总字串大小。
        做 mem_记录分配 用 做 计数 用 修正表 乘 16。
        做 模块图_打印详细 用 做 字符串拼接 用 「[M9] 代码段=」、做 字符串拼接 用 做 转字符串 用 做 计数 用 代码、做 字符串拼接 用 「 字串表=」、做 字符串拼接 用 做 转字符串 用 总字串大小、做 字符串拼接 用 「 修正表=」、做 转字符串 用 做 计数 用 修正表 乘 16。
        做 模块图_打印详细 用 做 字符串拼接 用 「[M9] 峰值=」、做 字符串拼接 用 做 转字符串 用 _mem_峰值字节、做 字符串拼接 用 「 当前=」、做 字符串拼接 用 做 转字符串 用 _mem_当前字节、做 字符串拼接 用 「 碎片率=」、做 字符串拼接 用 做 转字符串 用 做 mem_碎片率、「%」。
        若 做 mem_是极低内存profile：
            做 模块图_打印详细 用 「[M9] ✓ 极低内存profile合规」。
        否则：
            做 模块图_打印详细 用 「[M9] ✗ 未达极低内存标准」。
        完。
        做 模块图_打印详细 用 做 字符串拼接 用 「[ELF-BUILD] start=」、做 字符串拼接 用 做 转字符串 用 _start偏移、做 字符串拼接 用 「 code=」、做 转字符串 用 总代码大小。
        elf大小 为 做 模块_写LinuxELF 用 输出路径、_start偏移、总代码大小。
        做 模块图_打印详细 用 做 字符串拼接 用 「ELF写入成功: 」、输出路径。
        _lm表 为 做 编译_行号映射表。
        _lm数 为 做 计数 用 _lm表。
        可变 _调试级 为 「line」。
        若 _模块_项目合同 不等于 空 并且 做 是列表 用 _模块_项目合同 并且 做 计数 用 _模块_项目合同 大于或等于 5：
            _调试配置 为 _模块_项目合同 的 第 4 项。
            若 做 字符串相等 用 _调试配置、「analysis-host」：
                _调试级 为 「full」。
            完。
            若 做 字符串相等 用 _调试配置、「kernel-qemu」：
                _调试级 为 「symbol」。
            完。
        完。
        做 模块图_打印详细 用 做 字符串拼接 用 「[DWARF] 行号映射条目数: 」、做 字符串拼接 用 做 转字符串 用 _lm数、做 字符串拼接 用 「 调试级=」、_调试级。
        若 _lm数 大于 0：
            可变 _map字节 为 一组：（空）。
            只要 _mi 小于 _lm数：
                _me 为 _lm表 的 第 _mi 项。
                做 _map字节_追加整数 用 _me 的 第 0 项、_map字节。
                做 追加 用 _map字节、32。
                做 _map字节_追加整数 用 _me 的 第 1 项、_map字节。
                做 追加 用 _map字节、10。
                _mi 为 _mi 加 1。
            完。
            做 写列表文件 用 做 字符串拼接 用 输出路径、「.map」、_map字节。
        完。
        若 做 字符串相等 用 _调试级、「symbol」 或者 做 字符串相等 用 _调试级、「full」：
            可变 _sym字节 为 一组：（空）。
            _函数键 为 做 字典键 用 函数表。
            可变 _sym_i 为 0。
            只要 _sym_i 小于 做 计数 用 _函数键：
                _符号名 为 _函数键 的 第 _sym_i 项。
                _符号偏移 为 做 字典取 用 函数表、_符号名。
                若 _符号偏移 不等于 空：
                    做 _map字节_追加整数 用 _符号偏移、_sym字节。
                    做 追加 用 _sym字节、32。
                    可变 _ni 为 0。
                    只要 _ni 小于 做 字节长度 用 _符号名：
                        做 追加 用 _sym字节、做 字节取 用 _符号名、_ni。
                        _ni 为 _ni 加 1。
                    完。
                    做 追加 用 _sym字节、10。
                完。
                _sym_i 为 _sym_i 加 1。
            完。
            做 写列表文件 用 做 字符串拼接 用 输出路径、「.sym」、_sym字节。
            做 模块图_打印详细 用 做 字符串拼接 用 「[DWARF] 符号表条目数: 」、做 转字符串 用 做 计数 用 _函数键。
        完。
        若 做 字符串相等 用 _调试级、「full」：
            可变 _rpt字节 为 一组：（空）。
            可变 _rpt行列表 为 一组：「# Nova编译诊断报告」、做 字符串拼接 用 「# 模块数: 」、做 转字符串 用 做 计数 用 模块表、做 字符串拼接 用 「# 代码大小: 」、做 转字符串 用 代码位、做 字符串拼接 用 「# 函数数: 」、做 转字符串 用 做 计数 用 _函数键。
            可变 _rli 为 0。
            只要 _rli 小于 做 计数 用 _rpt行列表：
                可变 _rpt行 为 _rpt行列表 的 第 _rli 项。
                可变 _ri 为 0。
                只要 _ri 小于 做 字节长度 用 _rpt行：
                    做 追加 用 _rpt字节、做 字节取 用 _rpt行、_ri。
                    _ri 为 _ri 加 1。
                完。
                做 追加 用 _rpt字节、10。
                _rli 为 _rli 加 1。
            完。
            做 写列表文件 用 做 字符串拼接 用 输出路径、「.report」、_rpt字节。
            做 模块图_打印详细 用 「[DWARF] 完整诊断报告已生成」。
        完。
    否则若 做 字符串相等 用 当前目标名称、「裸机」 或者 做 字符串相等 用 当前目标名称、「kernel」：
        做 写代码为ELF 用 输出路径、代码、入口偏移。
        _内核物理地址 为 0x100000。
        _代码段对齐 为 (代码位 加 0xFFF) 除以 0x1000 乘 0x1000。
        _数据文件偏移 为 0x1000 加 _代码段对齐。
        _MB2偏移 为 232。
        _入口低32 为 0x80101000 加 入口偏移 位与 0xFFFFFFFF。
        _入口高32 为 0xFFFFFFFF。
        _内核基低 为 0x80100000。
        _内核基高 为 0xFFFFFFFF。
        _总段大小 为 0x1000 加 代码位。
        可变 _p 为 一组：（空）。
        可变 _pi 为 0。
        只要 _pi 小于 280：
            做 追加 用 _p、0。
            _pi 为 _pi 加 1。
        完。
        _p 的 第 0 项 为 0x7F。
        _p 的 第 1 项 为 0x45。
        _p 的 第 2 项 为 0x4C。
        _p 的 第 3 项 为 0x46。
        _p 的 第 4 项 为 2。
        _p 的 第 5 项 为 1。
        _p 的 第 6 项 为 1。
        做 内存写32 用 _p、16、2 加 62 乘 65536。
        做 内存写32 用 _p、20、1。
        做 内存写32 用 _p、24、_入口低32。
        做 内存写32 用 _p、28、_入口高32。
        做 内存写32 用 _p、32、64。
        做 内存写32 用 _p、36、0。
        做 内存写32 用 _p、52、64 加 56 乘 65536。
        做 内存写32 用 _p、56、3。
        做 内存写32 用 _p、64、1。
        做 内存写32 用 _p、68、7。
        做 内存写32 用 _p、80、_内核基低。
        做 内存写32 用 _p、84、_内核基高。
        做 内存写32 用 _p、88、_内核物理地址。
        做 内存写32 用 _p、92、0。
        做 内存写32 用 _p、96、_总段大小。
        做 内存写32 用 _p、100、0。
        做 内存写32 用 _p、104、_总段大小。
        做 内存写32 用 _p、108、0。
        做 内存写32 用 _p、112、0x1000。
        做 内存写32 用 _p、116、0。
        做 内存写32 用 _p、120、1。
        做 内存写32 用 _p、124、6。
        做 内存写32 用 _p、128、_数据文件偏移。
        做 内存写32 用 _p、132、0。
        做 内存写32 用 _p、136、_内核基低 加 _数据文件偏移 位与 0xFFFFFFFF。
        做 内存写32 用 _p、140、_内核基高。
        做 内存写32 用 _p、144、_内核物理地址 加 _数据文件偏移。
        做 内存写32 用 _p、148、0。
        做 内存写32 用 _p、152、0x1000。
        做 内存写32 用 _p、156、0。
        做 内存写32 用 _p、160、0x1000。
        做 内存写32 用 _p、164、0。
        做 内存写32 用 _p、168、0x1000。
        做 内存写32 用 _p、172、0。
        做 内存写32 用 _p、176、4。
        做 内存写32 用 _p、180、4。
        做 内存写32 用 _p、184、_MB2偏移。
        做 内存写32 用 _p、188、0。
        做 内存写32 用 _p、192、_内核基低 加 _MB2偏移。
        做 内存写32 用 _p、196、_内核基高。
        做 内存写32 用 _p、200、_内核物理地址 加 _MB2偏移。
        做 内存写32 用 _p、204、0。
        做 内存写32 用 _p、208、48。
        做 内存写32 用 _p、212、0。
        做 内存写32 用 _p、216、48。
        做 内存写32 用 _p、220、0。
        做 内存写32 用 _p、224、8。
        做 内存写32 用 _p、228、0。
        做 内存写32 用 _p、232、0xE85250D6。
        做 内存写32 用 _p、236、0。
        做 内存写32 用 _p、240、48。
        做 内存写32 用 _p、244、0 减 (0xE85250D6 加 0 加 48)。
        做 内存写32 用 _p、248、5。
        做 内存写32 用 _p、252、20。
        做 内存写32 用 _p、256、80。
        做 内存写32 用 _p、260、25。
        做 内存写32 用 _p、264、0。
        做 内存写32 用 _p、268、0。
        做 内存写32 用 _p、272、8。
        做 内存写32 用 _p、276、0。
        _原elf 为 做 读文件 用 输出路径。
        若 _原elf 不等于 空：
            _原大小 为 做 字节长度 用 _原elf。
            可变 _新elf 为 一组：（空）。
            可变 _hi 为 0。
            只要 _hi 小于 280：
                做 追加 用 _新elf、_p 的 第 _hi 项。
                _hi 为 _hi 加 1。
            完。
            可变 _ri 为 280。
            只要 _ri 小于 _原大小：
                做 追加 用 _新elf、做 字节取 用 _原elf、_ri。
                _ri 为 _ri 加 1。
            完。
            做 写二进制 用 输出路径、_新elf、做 计数 用 _新elf。
        完。
        做 QEMU_写出Bundle工件 用 输出路径。
        做 模块图_打印详细 用 「裸机ELF写入成功」。
        做 模块图_打印详细 用 「QEMU bundle写入成功」。
    否则：
        做 模块_处理_格式_PE 用 0。
    完。
    返还 真。
完。

模块图_转义路径 接受 路径：
    长度 为 做 字节长度 用 路径。
    若 长度 等于 0：
        返还 路径。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    可变 有引号 为 假。
    只要 i 小于 长度：
        若 做 字节取 用 路径、i 等于 字符_单引号：
            有引号 为 真。
            i 为 长度。
        完。
        i 为 i 加 1。
    完。
    若 非 有引号：
        返还 路径。
    完。
    i 为 0。
    只要 i 小于 长度：
        b 为 做 字节取 用 路径、i。
        若 b 等于 字符_单引号：
            结果 为 做 字符串拼接 用 结果、「'\\''」。
        否则：
            结果 为 做 字符串拼接 用 结果、做 字符 用 b。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

是目录 接受 路径：
    可变 标准路径 为 做 模块图_规范路径 用 路径。
    若 做 字节长度 用 标准路径 等于 0：
        标准路径 为 「.」。
    完。
    返还 做 __运行时_是目录 用 标准路径。
完。

模块图_是Nova文件路径 接受 路径：
    标准路径 为 做 模块图_规范路径 用 路径。
    若 做 字节长度 用 标准路径 等于 0：
        返还 假。
    完。
    长度 为 做 字节长度 用 标准路径。
    若 长度 小于 5：
        返还 假。
    完。
    若 做 字节取 用 标准路径、长度 减 5 等于 字符_句点 并且 做 字节取 用 标准路径、长度 减 4 等于 110 并且 做 字节取 用 标准路径、长度 减 3 等于 111 并且 做 字节取 用 标准路径、长度 减 2 等于 118 并且 做 字节取 用 标准路径、长度 减 1 等于 字符_小写a：
        返还 真。
    完。
    返还 假。
完。

模块图_项目根目录 接受 路径：
    可变 标准路径 为 路径。
    若 标准路径 等于 空：
        标准路径 为 「」。
    完。
    标准路径 为 做 替换 用 做 替换 用 标准路径、「\r」、「」、「\n」、「」。
    若 做 字节长度 用 标准路径 等于 0：
        返还 「.」。
    完。
    若 做 模块图_是Nova文件路径 用 标准路径：
        父目录 为 做 模块图_父目录 用 标准路径。
        若 做 字节长度 用 父目录 大于 0：
            返还 父目录。
        完。
        返还 「.」。
    完。
    返还 标准路径。
完。

列目录 接受 路径：
    可变 标准路径 为 做 模块图_规范路径 用 路径。
    若 做 字节长度 用 标准路径 等于 0：
        标准路径 为 「.」。
    完。
    可变 枚举缓存 为 「.__nova_dir_tmp.txt」。
    可变 _目录是点 为 0。
    若 做 字节长度 用 标准路径 等于 1：
        若 做 字节取 用 标准路径、0 等于 字符_句点：
            _目录是点 为 1。
        完。
    完。
    若 _目录是点 等于 0：
        枚举缓存 为 做 字符串拼接 用 标准路径、「/.__nova_dir_tmp.txt」。
    完。
    命令 为 「ls -1pA '」 加 做 模块图_转义路径 用 标准路径 加 「' > '」 加 做 模块图_转义路径 用 枚举缓存 加 「'」。
    执行结果 为 做 __系统命令 用 命令。
    若 执行结果 不等于 0：
        返还 一组：（空）。
    完。
    文本 为 做 读文件 用 枚举缓存。
    做 __系统命令 用 「rm -f '」 加 做 模块图_转义路径 用 枚举缓存 加 「'」。
    可变 结果 为 一组：（空）。
    若 文本 不等于 空 并且 做 字节长度 用 文本 大于 0：
        行列表 为 做 模块图_文本分行 用 文本。
        可变 已见 为 做 字典。
        可变 i 为 0。
        可变 _mfg样本 为 0。
        只要 i 小于 做 计数 用 行列表：
            原行 为 做 替换 用 做 替换 用 行列表 的 第 i 项、「\r」、「」、「\n」、「」。
            行 为 做 模块图_规范路径 用 原行。
            是目录条目 为 做 模块图_条目是目录 用 原行。
            _mfg是Nova 为 做 模块图_是Nova文件路径 用 行。
            _mfg跳过 为 做 模块图_应跳过文件路径 用 行。
            若 模块图详细模式 的 第 0 项 并且 _mfg样本 小于 3：
                做 模块图_打印详细 用 做 字符串拼接 用 「[清单] 行=」、行。
                做 模块图_打印详细 用 做 字符串拼接 用 「[清单] 是Nova=」、做 转字符串 用 _mfg是Nova。
                做 模块图_打印详细 用 做 字符串拼接 用 「[清单] 跳过=」、做 转字符串 用 _mfg跳过。
                _mfg样本 为 _mfg样本 加 1。
            完。
            若 做 字节长度 用 行 大于 0：
                可变 结果项 为 行。
                若 是目录条目：
                    结果项 为 做 字符串拼接 用 结果项、「/」。
                完。
                若 做 字典取 用 已见、结果项 等于 空：
                    做 字典设 用 已见、结果项、真。
                    做 追加 用 结果、结果项。
                完。
            完。
            i 为 i 加 1。
        完。
    完。
    做 模块图_打印详细 用 做 字符串拼接 用 「[清单] 结果数=」、做 转字符串 用 做 计数 用 结果。
    返还 结果。
完。

模块_扫描文件 接受 文件路径：
    可变 标准路径 为 文件路径。
    若 做 字节长度 用 标准路径 等于 0：
        返还 空。
    完。
    可变 读路径 为 标准路径。
    若 做 路径_是相对 用 读路径：
        若 做 字节长度 用 模块图扫描读根 大于 0：
            读路径 为 做 字符串拼接 用 模块图扫描读根、做 字符串拼接 用 「/」、读路径。
        完。
    完。
    源码 为 做 读文件 用 读路径。
    若 源码 等于 空：
        做 打印文本 用 做 字符串拼接 用 「读文件失败: 」、读路径。
        返还 空。
    完。
    可变 模块名扫 为 标准路径。
    若 做 字节长度 用 模块图扫描读根 大于 0：
        _读根前缀 为 做 字符串拼接 用 模块图扫描读根、「/」。
        若 做 以开头 用 模块名扫、_读根前缀：
            模块名扫 为 做 子串 用 模块名扫、做 计数 用 _读根前缀、做 计数 用 模块名扫 减 做 计数 用 _读根前缀。
        完。
    完。
    若 做 以开头 用 模块名扫、「./」：
        模块名扫 为 做 子串 用 模块名扫、2、做 计数 用 模块名扫 减 2。
    完。
    做 模块_注册 用 模块名扫、读路径。
    当前目录 为 做 模块图_父目录 用 模块名扫。
    可变 行列表 为 做 模块图_文本分行 用 源码。
    可变 行号 为 0。
    只要 行号 小于 做 计数 用 行列表：
        可变 行 为 行列表 的 第 行号 项。
        可变 行前 为 0。
        只要 行前 小于 做 计数 用 行 并且 (做 字节取 用 行、行前 等于 字符_空格 或者 做 字节取 用 行、行前 等于 字符_制表)：
            行前 为 行前 加 1。
        完。
        有效行 为 做 子串 用 行、行前、做 计数 用 行 减 行前。
        若 做 计数 用 有效行 大于或等于 2 并且 做 字节取 用 有效行、0 等于 字符_斜杠 并且 做 字节取 用 有效行、1 等于 字符_斜杠：
        否则：
            可变 导入偏移 为 0。
            可变 括号位 为 0。
            只要 括号位 小于 做 计数 用 有效行 并且 做 字节取 用 有效行、括号位 不等于 字符_左圆括：
                括号位 为 括号位 加 1。
            完。
            若 括号位 小于 做 计数 用 有效行：
                可变 导入前缀 为 做 子串 用 有效行、0、括号位。
                只要 做 计数 用 导入前缀 大于 0 并且 (做 字节取 用 导入前缀、做 计数 用 导入前缀 减 1 等于 字符_空格 或者 做 字节取 用 导入前缀、做 计数 用 导入前缀 减 1 等于 字符_制表)：
                    导入前缀 为 做 子串 用 导入前缀、0、做 计数 用 导入前缀 减 1。
                完。
                若 做 字符串相等 用 导入前缀、「入」 或者 做 字符串相等 用 导入前缀、「导入」：
                    导入偏移 为 括号位 加 1。
                完。
            完。
            若 导入偏移 大于 0：
                若 导入偏移 小于 做 计数 用 有效行 并且 (做 字节取 用 有效行、导入偏移 等于 字符_双引号 或者 做 字节取 用 有效行、导入偏移 等于 字符_单引号)：
                    引号字节 为 做 字节取 用 有效行、导入偏移。
                    导入偏移 为 导入偏移 加 1。
                    路径起始 为 导入偏移。
                    只要 导入偏移 小于 做 计数 用 有效行 并且 做 字节取 用 有效行、导入偏移 不等于 引号字节：
                        导入偏移 为 导入偏移 加 1。
                    完。
                    若 导入偏移 大于 路径起始：
                        依赖路径 为 做 子串 用 有效行、路径起始、导入偏移 减 路径起始。
                        依赖名 为 做 模块图_解析依赖路径 用 依赖路径、当前目录。
                        做 模块_添加依赖 用 模块名扫、依赖名。
                    完。
                完。
            完。
        完。
        行号 为 行号 加 1。
    完。
    返还 模块名扫。
完。

模块图_文本分行 接受 文本：
    返还 做 分行 用 文本。
完。

模块图_追加清单文件 接受 清单路径、原结果：
    可变 结果 为 原结果。
    若 非 做 是列表 用 结果：
        结果 为 一组：（空）。
    完。
    内容 为 做 读文件 用 清单路径。
    若 内容：
        若 做 字节长度 用 内容 大于 0：
            行列表 为 做 模块图_文本分行 用 内容。
            结果 为 做 模块图_追加清单行递归 用 行列表、0、做 计数 用 行列表、结果。
        完。
    完。
    返还 结果。
完。

模块图_追加清单行递归 接受 行列表、索引、剩余、结果：
    可变 i 为 索引。
    可变 剩 为 剩余。
    只要 剩 大于 0：
        可变 行 为 做 替换 用 做 替换 用 行列表 的 第 i 项、「\r」、「」、「\n」、「」。
        若 做 以开头 用 行、「./」：
            行 为 做 子串 用 行、2、做 计数 用 行 减 2。
        完。
        若 做 字节长度 用 行 大于 0：
            做 追加 用 结果、行。
        完。
        i 为 i 加 1。
        剩 为 剩 减 1。
    完。
    返还 结果。
完。

模块图_扫描文件列表递归 接受 文件列表、索引、剩余、扫描根、扫描根是点：
    可变 i 为 索引。
    可变 剩 为 剩余。
    只要 剩 大于 0：
        可变 文件路径 为 做 字符串拼接 用 文件列表 的 第 i 项、「」。
        若 做 路径_是相对 用 文件路径：
            若 非 扫描根是点：
                文件路径 为 做 字符串拼接 用 扫描根、做 字符串拼接 用 「/」、文件路径。
            完。
        完。
        做 模块_扫描文件 用 文件路径。
        i 为 i 加 1。
        剩 为 剩 减 1。
    完。
    返还 0。
完。

_扫描_尝试清单 接受 文件名、扫描根：
    可变 清单内容 为 做 读文件 用 文件名。
    若 非 清单内容：
        若 做 字节长度 用 扫描根 大于 0：
            清单内容 为 做 读文件 用 做 字符串拼接 用 扫描根、做 字符串拼接 用 「/」、文件名。
        完。
    完。
    若 清单内容：
        若 做 字节长度 用 清单内容 大于 0：
            行列表 为 做 模块图_文本分行 用 清单内容。
            返还 做 模块图_追加清单行递归 用 行列表、0、做 计数 用 行列表、一组：（空）。
        完。
    完。
    返还 一组：（空）。
完。

模块_扫描项目 接受 根目录：
    可变 _sfi 为 0。
    可变 _si 为 0。
    模块图扫描读根 为 「」。
    做 模块图_打印详细 用 做 字符串拼接 用 「扫描项目: 」、根目录。
    可变 扫描根 为 根目录。
    若 做 字节长度 用 扫描根 等于 0：
        扫描根 为 「.」。
    完。
    扫描根 为 做 模块图_项目根目录 用 扫描根。
    若 做 字节长度 用 扫描根 等于 0：
        扫描根 为 「.」。
    完。
    可变 文件列表 为 一组：（空）。
    可变 _扫描根是点 为 0。
    若 做 字节长度 用 扫描根 等于 1 并且 做 字节取 用 扫描根、0 等于 字符_句点：
        _扫描根是点 为 1。
    完。
    文件列表 为 做 _扫描_尝试清单 用 「_manifest.txt」、扫描根。
    若 非 做 计数 用 文件列表：
        文件列表 为 做 _扫描_尝试清单 用 「nova_manifest.txt」、扫描根。
    完。
    若 非 做 计数 用 文件列表：
        文件列表 为 做 _扫描_尝试清单 用 「.__nova_files_tmp.txt」、扫描根。
    完。
    若 非 做 计数 用 文件列表：
        文件列表 为 做 _扫描_尝试清单 用 「nova_files_tmp.txt」、扫描根。
    完。
    若 非 做 计数 用 文件列表：
        文件列表 为 做 模块图_递归枚举Nova文件 用 扫描根。
        做 模块图_打印详细 用 做 字符串拼接 用 「[扫描] 回退到递归枚举 root=」、扫描根。
    否则：
        做 模块图_打印详细 用 做 字符串拼接 用 「[扫描] 使用显式清单 root=」、扫描根。
    完。
    若 _模块_项目合同：
        若 做 是列表 用 _模块_项目合同 并且 做 计数 用 _模块_项目合同 大于或等于 7：
            _子系统列 为 _模块_项目合同 的 第 6 项。
            若 做 是列表 用 _子系统列 并且 做 计数 用 _子系统列 大于 0：
                做 模块图_打印详细 用 做 字符串拼接 用 「[项目系统] 聚合子系统manifest: 」、做 转字符串 用 做 计数 用 _子系统列。
                只要 _si 小于 做 计数 用 _子系统列：
                    _子系统 为 _子系统列 的 第 _si 项。
                    若 做 是列表 用 _子系统 并且 做 计数 用 _子系统 大于或等于 2：
                        _子名 为 _子系统 的 第 0 项。
                        _子清单 为 _子系统 的 第 1 项。
                        可变 _子路径 为 _子清单。
                        若 非 _扫描根是点：
                            _子路径 为 做 字符串拼接 用 扫描根、做 字符串拼接 用 「/」、_子清单。
                        完。
                        _子文件 为 做 模块图_追加清单文件 用 _子路径、一组：（空）。
                        若 做 计数 用 _子文件 大于 0：
                            做 模块图_打印详细 用 做 字符串拼接 用 「  [子系统] 」、做 字符串拼接 用 _子名、做 字符串拼接 用 「 → 」、做 字符串拼接 用 做 转字符串 用 做 计数 用 _子文件、「 个文件」。
                            _子目录 为 做 模块图_父目录 用 _子清单。
                            _sfi 为 0。
                            只要 _sfi 小于 做 计数 用 _子文件：
                                若 做 字节长度 用 _子目录 大于 0：
                                    做 追加 用 文件列表、做 字符串拼接 用 _子目录、做 字符串拼接 用 「/」、_子文件 的 第 _sfi 项。
                                否则：
                                    做 追加 用 文件列表、_子文件 的 第 _sfi 项。
                                完。
                                _sfi 为 _sfi 加 1。
                            完。
                        完。
                    完。
                    _si 为 _si 加 1。
                完。
            完。
        完。
    完。
    做 模块图_扫描文件列表递归 用 文件列表、0、做 计数 用 文件列表、扫描根、_扫描根是点。
    做 模块图_打印详细 用 做 字符串拼接 用 「发现 」、做 字符串拼接 用 做 转字符串 用 做 计数 用 模块表、「 个模块」。
    返还 做 计数 用 模块表。
完。

_模块_手动编译_处理单个 接受 模块名、位置、统计：
    当前序号 为 位置 加 1。
    _进度_当前 为 当前序号。
    做 模块图_阶段标记 用 「手动编译/模块前」、模块名。
    若 做 字典有 用 模块索引、模块名：
        _过滤位 为 做 字典取 用 模块索引、模块名。
        _过滤信息 为 模块表 的 第 _过滤位 项。
        _活跃p 为 「」。
        若 _模块_项目合同 不等于 空 并且 做 是列表 用 _模块_项目合同 并且 做 计数 用 _模块_项目合同 大于或等于 5：
            _活跃p 为 _模块_项目合同 的 第 4 项。
        完。
        若 非 做 模块_profile可编译 用 _过滤信息、_活跃p：
            _进度_跳过 为 _进度_跳过 加 1。
            做 _进度_显示编译 用 当前序号、_进度_总数、做 字符串拼接 用 模块名、「 (跳过)」。
            返还。
        完。
    完。
    做 _进度_显示编译 用 当前序号、_进度_总数、模块名。
    编译成功 为 做 模块_编译单个 用 模块名。
    若 编译成功：
        _进度_成功 为 _进度_成功 加 1。
        统计 的 第 0 项 为 统计 的 第 0 项 加 1。
        做 模块图_阶段标记 用 「手动编译/成功」、模块名。
    否则：
        _进度_失败 为 _进度_失败 加 1。
        做 模块图_阶段标记 用 「手动编译/失败」、模块名。
    完。
完。

_模块_手动编译_遍历 接受 层级、位置、统计：
    可变 i 为 位置。
    层级数 为 做 计数 用 层级。
    只要 i 小于 层级数：
        模块名 为 层级 的 第 i 项。
        做 _模块_手动编译_处理单个 用 模块名、i、统计。
        i 为 i 加 1。
    完。
完。

模块_自举 接受 项目根、输出路径：
    规范项目根 为 做 模块图_项目根目录 用 项目根。
    做 模块图_打印详细 用 「模块_自举: begin」。
    做 缓存_配置项目根 用 规范项目根。
    做 模块图_打印详细 用 「模块_自举: cache project root ok」。
    做 模块图_打印详细 用 「模块_自举: target parsed ok」。
    做 模块图_打印详细 用 做 字符串拼接 用 「[目标] 」、当前目标名称。
    可变 _m8_三元组 为 空。
    若 做 字符串相等 用 当前目标名称、「linux」 或者 做 字符串相等 用 当前目标名称、「elf」 或者 做 字符串相等 用 当前目标名称、「x86-64」：
        _m8_三元组 为 做 三元组_创建 用 目标_X86_64、系统_Linux、「SysV」。
        做 设置目标架构 用 架构_x86_64。
    否则若 做 字符串相等 用 当前目标名称、「裸机」 或者 做 字符串相等 用 当前目标名称、「kernel」：
        _m8_三元组 为 做 三元组_创建 用 目标_X86_64、系统_裸机、「SysV」。
        做 设置目标架构 用 架构_x86_64。
    否则若 做 字符串相等 用 当前目标名称、「arm64」：
        _m8_三元组 为 做 三元组_创建 用 目标_ARM64、系统_Linux、「AAPCS64」。
        做 设置目标架构 用 架构_ARM64。
    否则若 做 字符串相等 用 当前目标名称、「riscv64」：
        _m8_三元组 为 做 三元组_创建 用 目标_RISCV64、系统_Linux、「LP64D」。
        做 设置目标架构 用 架构_RISC_V。
    否则若 做 字符串相等 用 当前目标名称、「wasm」 或者 做 字符串相等 用 当前目标名称、「wasm32」：
        _m8_三元组 为 做 三元组_创建 用 目标_WASM32、系统_WASI、「WASM」。
        做 设置目标架构 用 架构_WASM。
    否则：
        _m8_三元组 为 做 三元组_创建 用 目标_X86_64、系统_Linux、「SysV」。
        做 设置目标架构 用 架构_x86_64。
    完。
    做 目标_设置 用 _m8_三元组。
    若 _模块_项目合同 不等于 空 并且 做 是列表 用 _模块_项目合同 并且 做 计数 用 _模块_项目合同 大于或等于 5：
        做 目标_设置profile 用 _模块_项目合同 的 第 4 项。
    完。
    _M8配置 为 做 目标_获取。
    若 _M8配置 不等于 空：
        做 模块图_打印详细 用 做 字符串拼接 用 「[M8] 目标三元组: 」、做 三元组_描述 用 _m8_三元组。
        做 模块图_打印详细 用 做 字符串拼接 用 「[M8] 指针宽度=」、做 字符串拼接 用 做 转字符串 用 做 目标_指针宽度 用 _M8配置、做 字符串拼接 用 「 栈对齐=」、做 字符串拼接 用 做 转字符串 用 做 目标_栈对齐 用 _M8配置、做 字符串拼接 用 「 参数寄存器=」、做 转字符串 用 做 目标_参数寄存器数 用 _M8配置。
        做 模块图_打印详细 用 做 字符串拼接 用 「[M8] ELF机器号=」、做 字符串拼接 用 做 转字符串 用 做 目标_ELF机器号 用 做 三元组_架构 用 _m8_三元组、做 字符串拼接 用 「 syscall=」、做 目标_syscall指令 用 做 三元组_架构 用 _m8_三元组。
    完。
    做 模块图_打印详细 用 做 字符串拼接 用 「[ABI] 」、做 字符串拼接 用 做 目标_名称、做 字符串拼接 用 「 cc=」、做 目标_调用约定名。
    做 缓存_初始化 用 0。
    做 模块图_打印详细 用 「模块_自举: cache init ok」。
    做 模块图_打印详细 用 「[增量缓存] 就绪」。
    可变 _rt_jit门控 为 2。
    可变 _rt_热更允许 为 0。
    可变 _rt_gc策略 为 「分代」。
    若 _模块_项目合同 不等于 空 并且 做 是列表 用 _模块_项目合同 并且 做 计数 用 _模块_项目合同 大于或等于 5：
        _运行时配置 为 _模块_项目合同 的 第 4 项。
        若 做 字符串相等 用 _运行时配置、「analysis-host」：
            _rt_jit门控 为 3。
            _rt_热更允许 为 0。
            _rt_gc策略 为 「关闭」。
        完。
        若 做 字符串相等 用 _运行时配置、「linux-user」：
            _rt_jit门控 为 2。
            _rt_热更允许 为 1。
            _rt_gc策略 为 「分代」。
        完。
        若 做 字符串相等 用 _运行时配置、「kernel-qemu」：
            _rt_jit门控 为 1。
            _rt_热更允许 为 1。
            _rt_gc策略 为 「手动」。
        完。
        若 做 字符串相等 用 _运行时配置、「kernel-physical」：
            _rt_jit门控 为 0。
            _rt_热更允许 为 1。
            _rt_gc策略 为 「手动」。
        完。
        做 模块图_打印详细 用 做 字符串拼接 用 「[运行时] JIT门控=」、做 字符串拼接 用 做 转字符串 用 _rt_jit门控、做 字符串拼接 用 「 热更=」、做 字符串拼接 用 做 转字符串 用 _rt_热更允许、做 字符串拼接 用 「 GC=」、_rt_gc策略。
    完。
    做 内存基准_标记冷启动。
    模块数 为 做 模块_扫描项目 用 规范项目根。
    做 模块图_打印详细 用 做 字符串拼接 用 「[扫描] 发现 」、做 字符串拼接 用 做 转字符串 用 模块数、「 个模块」。
    做 模块_批量推断标签。
    缓存命中 为 做 缓存_恢复模块状态 用 模块表、模块索引。
    若 缓存命中 大于 0：
        做 模块图_打印详细 用 做 字符串拼接 用 「[缓存] 命中 」、做 字符串拼接 用 做 转字符串 用 缓存命中、「 个模块 (跳过重编)」。
    完。
    新注册 为 做 契约_自动发现 用 规范项目根。
    做 模块图_打印详细 用 做 字符串拼接 用 「[契约] 」、做 字符串拼接 用 做 转字符串 用 新注册、「 个新模块注册契约」。
    循环结果 为 做 模块图_循环检测。
    若 循环结果 的 第 0 项：
        做 模块图_打印详细 用 「[循环检测] 存在循环依赖, 拓扑排序将自动容忍」。
    完。
    做 模块图_阶段标记 用 「自举/循环检测后」、「<bootstrap>」。
    做 模块图_阶段标记 用 「自举/拓扑前」、「<bootstrap>」。
    做 模块_拓扑排序 用 0。
    顺序 为 编译顺序。
    做 模块图_阶段标记 用 「自举/拓扑后」、「<bootstrap>」。
    做 模块图_打印详细 用 做 字符串拼接 用 「[拓扑] 编译顺序: 」、做 转字符串 用 做 计数 用 顺序。
    做 模块图_打印详细 用 做 字符串拼接 用 「[诊断] 拓扑排序数=」、做 转字符串 用 做 计数 用 顺序。
    层级 为 顺序。
    做 模块图_阶段标记 用 「自举/预注册前」、「<bootstrap>」。
    全局变量表 为 做 字典。
    全局变量数 为 0。
    可变 预注册i 为 0。
    可变 _预注册_跳过数 为 0。
    可变 _预注册_读失败 为 0。
    可变 _预注册_词失败 为 0。
    _进度_阶段 为 「预注册」。
    _进度_总数 为 做 计数 用 顺序。
    _进度_当前 为 0。
    做 _打印行 用 做 字符串拼接 用 「\n  ─── 预注册阶段 ─── 扫描 」、做 字符串拼接 用 做 转字符串 用 做 计数 用 顺序、「 个模块的全局变量...」。
    只要 预注册i 小于 做 计数 用 顺序：
        预注册名 为 顺序 的 第 预注册i 项。
        做 模块图_阶段标记 用 「预注册/模块前」、预注册名。
        做 _进度_显示预注册 用 预注册i 加 1、做 计数 用 顺序。
        若 非 做 字典有 用 模块索引、预注册名：
            _预注册_跳过数 为 _预注册_跳过数 加 1。
            预注册i 为 预注册i 加 1。
            继续。
        完。
        预注册位置 为 做 字典取 用 模块索引、预注册名。
        预注册信息 为 模块表 的 第 预注册位置 项。
        做 模块图_阶段标记 用 「预注册/读前」、预注册名。
        预注册源码 为 做 读文件 用 预注册信息 的 路径。
        做 模块图_阶段标记 用 「预注册/读后」、预注册名。
        若 预注册源码 等于 空 或者 做 计数 用 预注册源码 等于 0：
            _预注册_读失败 为 _预注册_读失败 加 1。
            预注册i 为 预注册i 加 1。
            继续。
        完。
        做 词法引擎_全局重置。
        做 语法引擎_全局重置。
        做 行号追踪_全局重置。
        做 位置_注册文件 用 预注册信息 的 路径。
        做 模块图_清空列表 用 词行号。
        做 模块图_阶段标记 用 「预注册/词法前」、预注册名。
        预注册词表 为 做 前端_词法分析 用 预注册源码。
        做 模块图_阶段标记 用 「预注册/词法后」、预注册名。
        若 预注册词表 不等于 空 并且 做 计数 用 预注册词表 大于 0：
            做 模块图_清空列表 用 匿名函数列。
            做 模块图_阶段标记 用 「预注册/解析前」、预注册名。
            预注册程序 为 做 前端_解析_程序 用 预注册词表。
            做 模块图_阶段标记 用 「预注册/解析后」、预注册名。
            若 做 是列表 用 预注册程序 等于 假 或者 做 计数 用 预注册程序 小于 2：
                _预注册_词失败 为 _预注册_词失败 加 1。
                预注册i 为 预注册i 加 1。
                继续。
            完。
            预注册主体 为 预注册程序 的 第 1 项。
            做 模块图_阶段标记 用 「预注册/变量扫描前」、预注册名。
            可变 预注册si 为 0。
            只要 预注册si 小于 做 计数 用 预注册主体：
                若 做 是列表 用 预注册主体 的 第 预注册si 项 并且 做 计数 用 预注册主体 的 第 预注册si 项 大于或等于 2 并且 预注册主体 的 第 预注册si 项 的 第 0 项 等于 节_定义：
                    预注册变量名 为 预注册主体 的 第 预注册si 项 的 第 1 项。
                    若 做 字典取 用 全局变量表、预注册变量名 等于 空：
                        做 字典设 用 全局变量表、预注册变量名、全局变量数 加 1。
                        全局变量数 为 全局变量数 加 1。
                    完。
                完。
                预注册si 为 预注册si 加 1。
            完。
            做 模块图_阶段标记 用 「预注册/变量扫描后」、预注册名。
        完。
        做 模块图_阶段标记 用 「预注册/模块后」、预注册名。
        预注册i 为 预注册i 加 1。
    完。
    做 模块图_阶段标记 用 「自举/预注册后」、「<bootstrap>」。
    做 模块图_打印详细 用 做 字符串拼接 用 「[预注册] 全局变量: 」、做 字符串拼接 用 做 转字符串 用 全局变量数、做 字符串拼接 用 「 跳过=」、做 字符串拼接 用 做 转字符串 用 _预注册_跳过数、做 字符串拼接 用 「 读失败=」、做 转字符串 用 _预注册_读失败。
    做 模块图_打印详细 用 做 字符串拼接 用 「[诊断] 预注册完成 全局变量数=」、做 转字符串 用 全局变量数。
    _进度_阶段 为 「编译」。
    _进度_总数 为 做 计数 用 层级。
    _进度_当前 为 0。
    _进度_成功 为 0。
    _进度_失败 为 0。
    _进度_跳过 为 0。
    _进度_警告 为 0。
    _进度_解析错误 为 0。
    _进度_语义错误 为 0。
    _进度_能力违规 为 0。
    _进度_错误列表 为 一组：（空）。
    做 _打印行 用 做 字符串拼接 用 「\n  ─── 编译阶段 ─── 开始编译 」、做 字符串拼接 用 做 转字符串 用 _进度_总数、「 个模块...」。
    做 效果_初始化默认profile。
    做 能力_初始化默认profile。
    可变 手动统计 为 一组：0。
    做 模块图_阶段标记 用 「自举/手动编译前」、「<bootstrap>」。
    做 _模块_手动编译_遍历 用 层级、0、手动统计。
    做 模块图_阶段标记 用 「自举/手动编译后」、「<bootstrap>」。
    编译结果 为 手动统计 的 第 0 项。
    若 编译结果 等于 0 并且 做 计数 用 模块表 大于 0：
        若 模块图详细模式 的 第 0 项：
            做 打印 用 「[编译] 全部命中缓存, 无需重编」。
        完。
    完。
    可变 失败数 为 0。
    可变 已编译数 为 0。
    可变 有码段数 为 0。
    可变 ci 为 0。
    只要 ci 小于 做 计数 用 模块表：
        若 模块表 的 第 ci 项 的 编译状态 等于 2：
            已编译数 为 已编译数 加 1。
        完。
        若 模块表 的 第 ci 项 的 编译状态 等于 3：
            失败数 为 失败数 加 1。
        完。
        若 模块表 的 第 ci 项 的 目标码 不等于 空 并且 模块表 的 第 ci 项 的 目标码大小 大于 0：
            有码段数 为 有码段数 加 1。
        完。
        ci 为 ci 加 1。
    完。
    做 _进度_汇总 用 做 计数 用 模块表、_进度_成功、_进度_失败、_进度_跳过、_进度_警告、失败数。
    做 打印文本 用 做 字符串拼接 用 「[自举诊断] 已编译=」、做 字符串拼接 用 做 转字符串 用 已编译数、做 字符串拼接 用 「 失败=」、做 字符串拼接 用 做 转字符串 用 失败数、做 字符串拼接 用 「 有码段=」、做 转字符串 用 有码段数。
    做 模块图_打印详细 用 做 字符串拼接 用 「[诊断] 已编译模块数=」、做 转字符串 用 已编译数。
    若 模块图详细模式 的 第 0 项：
        _标签统 为 做 模块_标签统计。
        做 打印文本 用 做 字符串拼接 用 「[标签统计] AI=」、做 字符串拼接 用 做 转字符串 用 _标签统 的 第 0 项、做 字符串拼接 用 「 启动=」、做 字符串拼接 用 做 转字符串 用 _标签统 的 第 1 项、做 字符串拼接 用 「 可热更=」、做 字符串拼接 用 做 转字符串 用 _标签统 的 第 2 项、做 字符串拼接 用 「 内核态=」、做 字符串拼接 用 做 转字符串 用 _标签统 的 第 3 项、做 字符串拼接 用 「 记忆=」、做 转字符串 用 _标签统 的 第 4 项。
        _依赖统 为 做 模块_统计依赖。
        做 打印文本 用 做 字符串拼接 用 「[依赖统计] 总边=」、做 字符串拼接 用 做 转字符串 用 _依赖统 的 第 0 项、做 字符串拼接 用 「 有依赖=」、做 字符串拼接 用 做 转字符串 用 _依赖统 的 第 1 项、做 字符串拼接 用 「 最大依赖=」、做 字符串拼接 用 做 转字符串 用 _依赖统 的 第 2 项、做 字符串拼接 用 「 by 」、_依赖统 的 第 3 项。
        做 优化管线_打印结构化报告。
    完。
    若 失败数 大于 0：
        做 打印文本 用 做 字符串拼接 用 「\n编译失败: 」、做 字符串拼接 用 做 转字符串 用 失败数、做 字符串拼接 用 「 个模块有错误 (共 」、做 字符串拼接 用 做 转字符串 用 做 计数 用 模块表、「 个模块)」。
        可变 _fi 为 0。
        只要 _fi 小于 做 计数 用 模块表：
            若 模块表 的 第 _fi 项 的 编译状态 等于 3：
                做 打印文本 用 做 字符串拼接 用 「  ✗ 」、模块表 的 第 _fi 项 的 路径。
            完。
            _fi 为 _fi 加 1。
        完。
        返还 假。
    完。
    做 内存基准_自动采集。
    _进度_阶段 为 「链接」。
    做 _打印行 用 做 字符串拼接 用 「\n  ─── 链接阶段 ─── 目标: 」、输出路径。
    做 _进度_心跳 用 「链接中...」。
    模块入口模块名 为 做 模块_推断入口模块 用 规范项目根。
    做 模块图_打印详细 用 做 字符串拼接 用 「[链接] 目标: 」、输出路径。
    链接成功 为 做 模块_链接全部 用 输出路径。
    若 非 链接成功：
        做 打印 用 「自举失败: 链接错误」。
        返还 假。
    完。
    做 缓存_保存manifest。
    若 模块图详细模式 的 第 0 项：
        做 缓存_统计。
    完。
    做 流水线_记录编译。
    做 流水线_记录优化 用 已编译数。
    若 模块图详细模式 的 第 0 项：
        _管线统计 为 做 流水线_总统计。
        做 打印文本 用 做 字符串拼接 用 「  [流水线] 编译次数=」、做 字符串拼接 用 做 转字符串 用 _管线统计 的 第 0 项、做 字符串拼接 用 「 优化轮次=」、做 转字符串 用 _管线统计 的 第 1 项。
        做 内存profile_打印报告。
        做 效果_打印报告。
        做 能力_打印报告。
        _ai统计 为 做 AI后端_统计报告。
        做 打印文本 用 做 字符串拼接 用 「[AI后端] SIMD=」、做 字符串拼接 用 做 字典取 用 _ai统计、「SIMD发射」、做 字符串拼接 用 「 低比特=」、做 字符串拼接 用 做 字典取 用 _ai统计、「低比特发射」、做 字符串拼接 用 「 GQA=」、做 字符串拼接 用 做 字典取 用 _ai统计、「GQA发射」、做 字符串拼接 用 「 裸机=」、做 字典取 用 _ai统计、「裸机目标」。
    完。
    做 模块图_打印详细 用 「═══ 自举完成 ═══」。
    返还 真。
完。

模块图编_自举_验证 接受 编译器A路径、编译器B路径：
    数据A 为 做 读文件 用 编译器A路径。
    数据B 为 做 读文件 用 编译器B路径。
    若 数据A 等于 空 或者 数据B 等于 空：
        做 打印文本 用 「验证失败: 无法读取编译器文件」。
        返还 假。
    完。
    大小A 为 做 字节长度 用 数据A。
    大小B 为 做 字节长度 用 数据B。
    若 大小A 不等于 大小B：
        做 打印文本 用 做 字符串拼接 用 「验证失败: 大小不同 (」、做 字符串拼接 用 做 转字符串 用 大小A、做 字符串拼接 用 「 vs 」、做 转字符串 用 大小B。
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 大小A：
        若 做 字节取 用 数据A、i 不等于 做 字节取 用 数据B、i：
            做 打印文本 用 做 字符串拼接 用 「验证失败: 偏移 」、做 字符串拼接 用 做 转字符串 用 i、「 处不同」。
            返还 假。
        完。
        i 为 i 加 1。
    完。
    做 打印文本 用 做 字符串拼接 用 「验证成功: 」、做 字符串拼接 用 做 转字符串 用 大小A、「 字节完全一致」。
    返还 真。
完。

_系统执行_转义参数 接受 参数：
    长度 为 做 字节长度 用 参数。
    若 长度 等于 0：
        返还 参数。
    完。
    可变 需转义 为 假。
    可变 i 为 0。
    只要 i 小于 长度：
        b 为 做 字节取 用 参数、i。
        若 b 等于 标记_余 或者 b 等于 标记_左花 或者 b 等于 标记_逗号 或者 b 等于 36：
            需转义 为 真。
            i 为 长度。
        完。
        i 为 i 加 1。
    完。
    若 非 需转义：
        返还 参数。
    完。
    可变 结果 为 「」。
    i 为 0。
    只要 i 小于 长度：
        b 为 做 字节取 用 参数、i。
        若 b 等于 标记_余 或者 b 等于 标记_左花 或者 b 等于 标记_逗号 或者 b 等于 36：
            结果 为 做 字符串拼接 用 结果、做 字符串拼接 用 「\\」、做 字符 用 b。
        否则：
            结果 为 做 字符串拼接 用 结果、做 字符 用 b。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

系统执行 接受 程序、参数列表：
    可变 命令行 为 「\"」 加 做 _系统执行_转义参数 用 程序 加 「\"」。
    可变 ai 为 0。
    只要 ai 小于 做 计数 用 参数列表：
        命令行 为 命令行 加 「 」 加 「\"」 加 做 _系统执行_转义参数 用 参数列表 的 第 ai 项 加 「\"」。
        ai 为 ai 加 1。
    完。
    做 打印 用 「[系统执行] 」 加 命令行。
    退出码 为 做 __系统命令 用 命令行。
    做 打印 用 「[系统执行] 退出码: 」 加 做 转字符串 用 退出码。
    返还 退出码。
完。

完整自举 接受 项目根、编译器路径：
    做 打印 用 「═══ Nova 完整分离式链式自举 ═══」。
    做 打印 用 「[Stage-0] Rust引导编译器 → Stage-1」。
    规范项目根 为 做 模块图_项目根目录 用 项目根。
    编译器源 为 做 模块图_拼路径 用 规范项目根、「Nova.nova」。
    可变 stage1名 为 做 模块图_拼路径 用 规范项目根、「stage1_编译器」。
    可变 stage2名 为 做 模块图_拼路径 用 规范项目根、「stage2_编译器」。
    可变 stage3名 为 做 模块图_拼路径 用 规范项目根、「stage3_编译器」。
    可变 stage1程序 为 编译器路径。
    可变 stage2程序 为 stage2名。
    若 做 字符串相等 用 当前目标名称、「windows」 或者 做 字符串相等 用 当前目标名称、「pe」：
        stage1名 为 做 模块图_拼路径 用 规范项目根、「stage1_编译器.exe」。
        stage2名 为 做 模块图_拼路径 用 规范项目根、「stage2_编译器.exe」。
        stage3名 为 做 模块图_拼路径 用 规范项目根、「stage3_编译器.exe」。
        stage2程序 为 stage2名。
    完。
    做 打印 用 「[自举目标] 」 加 编译器源。
    做 打印 用 「[Stage-1 输出] 」 加 stage1名。
    做 打印 用 「[Stage-2 输出] 」 加 stage2名。
    做 打印 用 「[Stage-3 输出] 」 加 stage3名。
    s1成功 为 做 模块_自举 用 项目根、stage1名。
    若 非 s1成功：
        做 打印 用 「Stage-1 失败」。
        返还 假。
    完。
    若 非 做 文件存在 用 stage1名：
        做 打印 用 「Stage-1 失败: 未生成产物 」 加 stage1名。
        返还 假。
    完。
    做 打印 用 「[Stage-1] 完成 → 」 加 stage1名。
    做 打印 用 「[Stage-2] stage1 模块图编译 」 加 项目根 加 「 → 」 加 stage2名。
    s2退出码 为 做 系统执行 用 stage1程序、一组：项目根、「--compile」、「--module-graph」、「--target」、当前目标名称、「-o」、stage2名。
    若 s2退出码 不等于 0：
        做 打印 用 「Stage-2 失败: stage1编译器执行返回 」 加 做 转字符串 用 s2退出码。
        返还 假。
    完。
    若 非 做 文件存在 用 stage2名：
        做 打印 用 「Stage-2 失败: 未生成产物 」 加 stage2名。
        返还 假。
    完。
    做 打印 用 「[Stage-2] 完成 → 」 加 stage2名。
    做 打印 用 「[Stage-3] stage2 模块图编译 」 加 项目根 加 「 → 」 加 stage3名。
    s3退出码 为 做 系统执行 用 stage2程序、一组：项目根、「--compile」、「--module-graph」、「--target」、当前目标名称、「-o」、stage3名。
    若 s3退出码 不等于 0：
        做 打印 用 「Stage-3 失败: stage2编译器执行返回 」 加 做 转字符串 用 s3退出码。
        返还 假。
    完。
    若 非 做 文件存在 用 stage3名：
        做 打印 用 「Stage-3 失败: 未生成产物 」 加 stage3名。
        返还 假。
    完。
    做 打印 用 「[Stage-3] 完成 → 」 加 stage3名。
    做 打印 用 「[验证] 对比 stage2 与 stage3...」。
    验证结果 为 做 模块图编_自举_验证 用 stage2名、stage3名。
    若 验证结果：
        做 打印 用 「═══ 自举验证通过: 编译器可完美复制自身 ═══」。
    否则：
        做 打印 用 「═══ 自举验证失败: stage2 与 stage3 不一致 ═══」。
    完。
    返还 验证结果。
完。

_K_后序DFS_迭代 接受 邻接表、起始id、已访问、后序栈：
    可变 dfs栈 为 一组：（空）。
    起始键 为 做 转字符串 用 起始id。
    若 做 字典取 用 已访问、起始键 不等于 空：
        返还 空。
    完。
    做 追加 用 dfs栈、一组：起始id、0。
    做 字典设 用 已访问、起始键、真。
    只要 做 计数 用 dfs栈 大于 0：
        栈顶 为 dfs栈 的 第 做 计数 用 dfs栈 减 1 项。
        当前id 为 栈顶 的 第 0 项。
        可变 邻居idx 为 栈顶 的 第 1 项。
        邻居 为 做 字典取 用 邻接表、做 转字符串 用 当前id。
        可变 推进 为 假。
        若 邻居 不等于 空：
            只要 邻居idx 小于 做 计数 用 邻居 并且 非 推进：
                下一个 为 邻居 的 第 邻居idx 项。
                邻居idx 为 邻居idx 加 1。
                栈顶 的 第 1 项 为 邻居idx。
                下一键 为 做 转字符串 用 下一个。
                若 做 字典取 用 已访问、下一键 等于 空：
                    做 字典设 用 已访问、下一键、真。
                    做 追加 用 dfs栈、一组：下一个、0。
                    推进 为 真。
                完。
            完。
        完。
        若 非 推进：
            做 追加 用 后序栈、当前id。
            做 删除 用 dfs栈、做 计数 用 dfs栈 减 1。
        完。
    完。
完。

_K_反向DFS_迭代 接受 反向邻接表、起始id、已访问、当前SCC：
    可变 dfs栈 为 一组：起始id。
    起始键 为 做 转字符串 用 起始id。
    若 做 字典取 用 已访问、起始键 不等于 空：
        返还 空。
    完。
    做 字典设 用 已访问、起始键、真。
    做 追加 用 当前SCC、起始id。
    只要 做 计数 用 dfs栈 大于 0：
        当前 为 dfs栈 的 第 做 计数 用 dfs栈 减 1 项。
        做 删除 用 dfs栈、做 计数 用 dfs栈 减 1。
        邻居 为 做 字典取 用 反向邻接表、做 转字符串 用 当前。
        若 邻居 不等于 空：
            可变 k 为 0。
            只要 k 小于 做 计数 用 邻居：
                下一个 为 邻居 的 第 k 项。
                下一键 为 做 转字符串 用 下一个。
                若 做 字典取 用 已访问、下一键 等于 空：
                    做 字典设 用 已访问、下一键、真。
                    做 追加 用 当前SCC、下一个。
                    做 追加 用 dfs栈、下一个。
                完。
                k 为 k 加 1。
            完。
        完。
    完。
完。

模块图_Kosaraju_SCC 接受 零参：
    模块数 为 做 计数 用 模块表。
    可变 名到id 为 做 字典。
    可变 id到名 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 模块数：
        做 字典设 用 名到id、模块表 的 第 i 项 的 名称、i。
        做 追加 用 id到名、模块表 的 第 i 项 的 名称。
        i 为 i 加 1。
    完。
    可变 邻接表 为 做 字典。
    可变 反向邻接表 为 做 字典。
    i 为 0。
    只要 i 小于 模块数：
        可变 依赖组 为 模块表 的 第 i 项 的 依赖列表。
        若 依赖组 等于 空：
            依赖组 为 一组：（空）。
        完。
        可变 邻居ids 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 做 计数 用 依赖组：
            依赖号 为 做 字典取 用 名到id、依赖组 的 第 j 项。
            若 依赖号 不等于 空：
                做 追加 用 邻居ids、依赖号。
                反键 为 做 转字符串 用 依赖号。
                若 做 字典取 用 反向邻接表、反键 等于 空：
                    做 字典设 用 反向邻接表、反键、一组：（空）。
                完。
                做 追加 用 做 字典取 用 反向邻接表、反键、i。
            完。
            j 为 j 加 1。
        完。
        做 字典设 用 邻接表、做 转字符串 用 i、邻居ids。
        i 为 i 加 1。
    完。
    可变 已访问1 为 做 字典。
    可变 后序栈 为 一组：（空）。
    i 为 0。
    只要 i 小于 模块数：
        做 _K_后序DFS_迭代 用 邻接表、i、已访问1、后序栈。
        i 为 i 加 1。
    完。
    可变 已访问2 为 做 字典。
    可变 所有SCC 为 一组：（空）。
    i 为 做 计数 用 后序栈 减 1。
    只要 i 大于或等于 0：
        v 为 后序栈 的 第 i 项。
        若 做 字典取 用 已访问2、做 转字符串 用 v 等于 空：
            可变 当前SCC 为 一组：（空）。
            做 _K_反向DFS_迭代 用 反向邻接表、v、已访问2、当前SCC。
            做 追加 用 所有SCC、当前SCC。
        完。
        i 为 i 减 1。
    完。
    返还 一组：所有SCC、id到名。
完。

模块图_循环检测 接受 零参：
    模块数 为 做 计数 用 模块表。
    若 模块数 等于 0：
        返还 一组：假、一组：（空）。
    完。
    可变 入度 为 做 字典。
    可变 被依赖者 为 做 字典。
    可变 i 为 0。
    只要 i 小于 模块数：
        做 字典设 用 入度、模块表 的 第 i 项 的 名称、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 模块数：
        当前名 为 模块表 的 第 i 项 的 名称。
        依赖列表 为 模块表 的 第 i 项 的 依赖列表。
        若 做 是列表 用 依赖列表 等于 假：
            依赖列表 为 一组：（空）。
        完。
        可变 j 为 0。
        只要 j 小于 做 计数 用 依赖列表：
            依赖 为 依赖列表 的 第 j 项。
            若 做 字典有 用 入度、依赖：
                做 字典设 用 入度、当前名、做 字典取 用 入度、当前名 加 1。
                反列 为 做 字典取 用 被依赖者、依赖。
                若 反列 等于 空：
                    反列 为 一组：（空）。
                    做 字典设 用 被依赖者、依赖、反列。
                完。
                做 追加 用 反列、当前名。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 队列 为 一组：（空）。
    i 为 0。
    只要 i 小于 模块数：
        若 做 字典取 用 入度、模块表 的 第 i 项 的 名称 等于 0：
            做 追加 用 队列、模块表 的 第 i 项 的 名称。
        完。
        i 为 i 加 1。
    完。
    可变 已处理数 为 0。
    可变 qi 为 0。
    只要 qi 小于 做 计数 用 队列：
        当前 为 队列 的 第 qi 项。
        已处理数 为 已处理数 加 1。
        依赖者列表 为 做 字典取 用 被依赖者、当前。
        若 依赖者列表 不等于 空 并且 做 是列表 用 依赖者列表：
            可变 k 为 0。
            只要 k 小于 做 计数 用 依赖者列表：
                名 为 依赖者列表 的 第 k 项。
                做 字典设 用 入度、名、做 字典取 用 入度、名 减 1。
                若 做 字典取 用 入度、名 等于 0：
                    做 追加 用 队列、名。
                完。
                k 为 k 加 1。
            完。
        完。
        qi 为 qi 加 1。
    完。
    若 已处理数 大于或等于 模块数：
        做 模块图_打印详细 用 「[OK] 模块依赖图无循环 (拓扑验证)」。
        返还 一组：假、一组：（空）。
    完。
    scc结果 为 做 模块图_Kosaraju_SCC。
    所有SCC 为 scc结果 的 第 0 项。
    id到名 为 scc结果 的 第 1 项。
    可变 循环模块 为 一组：（空）。
    可变 循环组数 为 0。
    可变 si 为 0。
    只要 si 小于 做 计数 用 所有SCC：
        当前SCC 为 所有SCC 的 第 si 项。
        若 做 计数 用 当前SCC 大于 1：
            循环组数 为 循环组数 加 1。
            可变 组摘要 为 「」。
            可变 ki 为 0。
            组上限 为 5。
            只要 ki 小于 做 计数 用 当前SCC 并且 ki 小于 组上限：
                若 ki 大于 0：
                    组摘要 为 做 字符串拼接 用 组摘要、「 → 」。
                完。
                若 当前SCC 的 第 ki 项 小于 做 计数 用 id到名：
                    组摘要 为 做 字符串拼接 用 组摘要、id到名 的 第 当前SCC 的 第 ki 项 项。
                完。
                ki 为 ki 加 1。
            完。
            若 做 计数 用 当前SCC 大于 组上限：
                组摘要 为 做 字符串拼接 用 组摘要、做 字符串拼接 用 「 ... 等」、做 字符串拼接 用 做 转字符串 用 做 计数 用 当前SCC 减 组上限、「个」。
            完。
            做 模块图_打印详细 用 做 字符串拼接 用 「[循环组」、做 字符串拼接 用 做 转字符串 用 循环组数、做 字符串拼接 用 「] 」、做 字符串拼接 用 做 转字符串 用 做 计数 用 当前SCC、做 字符串拼接 用 「个模块: 」、组摘要。
            ki 为 0。
            只要 ki 小于 做 计数 用 当前SCC：
                若 当前SCC 的 第 ki 项 小于 做 计数 用 id到名：
                    做 追加 用 循环模块、id到名 的 第 当前SCC 的 第 ki 项 项。
                完。
                ki 为 ki 加 1。
            完。
        完。
        si 为 si 加 1。
    完。
    做 模块图_打印详细 用 做 字符串拼接 用 「[信息] 模块循环依赖已容忍 (」、做 字符串拼接 用 做 转字符串 用 做 计数 用 循环模块、做 字符串拼接 用 「个模块,  」、做 字符串拼接 用 做 转字符串 用 循环组数、「个循环组)」。
    返还 一组：真、循环模块。
完。

本章 收束。
