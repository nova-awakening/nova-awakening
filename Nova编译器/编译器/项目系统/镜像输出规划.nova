《镜像输出规划》

引用《..·..·标准库·基础》。

引用《..·..·运行时·字典引擎·字典引擎》。

镜像规划_创建 接受 合同、配置、输出路径：
    镜像类型 为 配置 的 第 3 项。
    可变 基址 为 0。
    可变 段列 为 一组：「.text」、「.rodata」、「.data」、「.bss」。
    可变 需要MB 为 0。
    可变 需要stub 为 0。
    若 做 字符串相等 用 镜像类型、「kernel-elf」 或者 做 字符串相等 用 镜像类型、「multiboot2」：
        基址 为 1048576。
        做 追加 用 段列、「.init」。
        做 追加 用 段列、「.note」。
        需要MB 为 1。
        需要stub 为 1。
    完。
    若 做 字符串相等 用 镜像类型、「disk」：
        基址 为 1048576。
        做 追加 用 段列、「.init」。
        做 追加 用 段列、「.note」。
        需要MB 为 1。
        需要stub 为 1。
    完。
    若 做 字符串相等 用 镜像类型、「elf」：
        基址 为 4194304。
    完。
    入口 为 合同 的 第 3 项。
    若 做 字符串相等 用 入口、「none」 或者 做 字节长度 用 入口 等于 0：
        若 需要stub 等于 1：
            入口 为 「入口64」。
        否则：
            入口 为 「__主程序__」。
        完。
    完。
    可变 输出 为 输出路径。
    若 做 字节长度 用 输出 等于 0：
        若 做 字符串相等 用 镜像类型、「elf」：
            输出 为 「nova_output」。
        完。
        若 做 字符串相等 用 镜像类型、「kernel-elf」：
            输出 为 「nova_kernel.elf」。
        完。
        若 做 字符串相等 用 镜像类型、「multiboot2」：
            输出 为 「nova_kernel.mb2」。
        完。
        若 做 字符串相等 用 镜像类型、「disk」：
            输出 为 「nova_kernel.img」。
        完。
    完。
    返还 一组：镜像类型、入口、基址、段列、需要MB、需要stub、配置 的 第 11 项、输出。
完。

镜像规划_类型 接受 规划：
    返还 规划 的 第 0 项。
完。

镜像规划_入口符号 接受 规划：
    返还 规划 的 第 1 项。
完。

镜像规划_基址 接受 规划：
    返还 规划 的 第 2 项。
完。

镜像规划_段列表 接受 规划：
    返还 规划 的 第 3 项。
完。

镜像规划_需要Multiboot 接受 规划：
    返还 规划 的 第 4 项。
完。

镜像规划_需要启动stub 接受 规划：
    返还 规划 的 第 5 项。
完。

镜像规划_链接布局 接受 规划：
    返还 规划 的 第 6 项。
完。

镜像规划_输出路径 接受 规划：
    返还 规划 的 第 7 项。
完。

镜像规划_打印 接受 规划：
    做 打印 用 「镜像输出规划:」。
    做 打印 用 「  类型: 」 加 规划 的 第 0 项。
    做 打印 用 「  入口: 」 加 规划 的 第 1 项。
    做 打印 用 「  基址: 」 加 做 转字符串 用 规划 的 第 2 项。
    做 打印 用 「  段: 」 加 做 转字符串 用 做 计数 用 规划 的 第 3 项 加 「个」。
    做 打印 用 「  输出: 」 加 规划 的 第 7 项。
完。

本章 收束。
