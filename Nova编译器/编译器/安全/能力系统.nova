《能力系统》

引用《..·..·标准库·基础》。

引用《..·..·标准库·转换器·转换验证》。

引用《.·效果系统》。

能力_无 为 0。

能力_设备 为 1。

能力_显存 为 2。

能力系统_能力_文件 为 3。

能力_系统调用 为 4。

能力_中断 为 5。

能力_特权 为 6。

能力系统_能力_网络 为 7。

能力_DMA 为 8。

能力_裸指针 为 9。

能力_汇编 为 10。

能力名 接受 c：
    若 c 等于 能力_无：
        返还 「none」。
    完。
    若 c 等于 能力_设备：
        返还 「设备」。
    完。
    若 c 等于 能力_显存：
        返还 「显存」。
    完。
    若 c 等于 能力系统_能力_文件：
        返还 「文件」。
    完。
    若 c 等于 能力_系统调用：
        返还 「系统调用」。
    完。
    若 c 等于 能力_中断：
        返还 「中断」。
    完。
    若 c 等于 能力_特权：
        返还 「特权」。
    完。
    若 c 等于 能力系统_能力_网络：
        返还 「网络」。
    完。
    若 c 等于 能力_DMA：
        返还 「DMA」。
    完。
    若 c 等于 能力_裸指针：
        返还 「裸指针」。
    完。
    若 c 等于 能力_汇编：
        返还 「汇编」。
    完。
    返还 「未知能力」。
完。

能力_创建令牌 接受 类型、模块、级别、可传递：
    返还 一组：类型、模块、级别、可传递。
完。

能力_令牌类型 接受 令牌：
    返还 令牌 的 第 0 项。
完。

能力_令牌模块 接受 令牌：
    返还 令牌 的 第 1 项。
完。

能力_令牌级别 接受 令牌：
    返还 令牌 的 第 2 项。
完。

能力_令牌可传递 接受 令牌：
    返还 令牌 的 第 3 项。
完。

可变 _能力表 为 空。

能力_初始化 接受 零参：
    _能力表 为 做 字典。
完。

能力_授权 接受 模块名、能力类型、级别、可传递：
    若 _能力表 等于 空：
        做 能力_初始化。
    完。
    现有 为 做 字典取 用 _能力表、模块名。
    可变 列表 为 一组：（空）。
    若 现有 不等于 空：
        列表 为 现有。
    完。
    做 追加 用 列表、做 能力_创建令牌 用 能力类型、模块名、级别、可传递。
    做 字典设 用 _能力表、模块名、列表。
完。

能力_检查 接受 模块名、能力类型：
    若 _能力表 等于 空：
        返还 假。
    完。
    列表 为 做 字典取 用 _能力表、模块名。
    若 列表 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 能力_令牌类型 用 列表 的 第 i 项 等于 能力类型：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

能力_获取所有 接受 模块名：
    若 _能力表 等于 空：
        返还 一组：（空）。
    完。
    列表 为 做 字典取 用 _能力表、模块名。
    若 列表 等于 空：
        返还 一组：（空）。
    完。
    返还 列表。
完。

能力_从模块名推断 接受 模块名：
    可变 结果 为 一组：（空）。
    若 做 字符串包含 用 模块名、「GPU」 或者 做 字符串包含 用 模块名、「gpu」：
        做 追加 用 结果、能力_设备。
        做 追加 用 结果、能力_显存。
    完。
    若 做 字符串包含 用 模块名、「文件」 或者 做 字符串包含 用 模块名、「IO」 或者 做 字符串包含 用 模块名、「磁盘」：
        做 追加 用 结果、能力系统_能力_文件。
    完。
    若 做 字符串包含 用 模块名、「网络」 或者 做 字符串包含 用 模块名、「套接字」 或者 做 字符串包含 用 模块名、「HTTP」：
        做 追加 用 结果、能力系统_能力_网络。
    完。
    若 做 字符串包含 用 模块名、「裸机」 或者 做 字符串包含 用 模块名、「内核」 或者 做 字符串包含 用 模块名、「启动」：
        做 追加 用 结果、能力_特权。
        做 追加 用 结果、能力_系统调用。
        做 追加 用 结果、能力_中断。
    完。
    若 做 字符串包含 用 模块名、「汇编」 或者 做 字符串包含 用 模块名、「ASM」：
        做 追加 用 结果、能力_汇编。
    完。
    若 做 字符串包含 用 模块名、「指针」 或者 做 字符串包含 用 模块名、「内存」：
        做 追加 用 结果、能力_裸指针。
    完。
    返还 结果。
完。

可变 _能力违规 为 一组：（空）。

能力_检查违规 接受 模块名、需要能力：
    若 做 能力_检查 用 模块名、需要能力 等于 假：
        做 追加 用 _能力违规、一组：模块名、需要能力。
        返还 假。
    完。
    返还 真。
完。

能力_违规数 接受 零参：
    返还 做 计数 用 _能力违规。
完。

能力_重置违规 接受 零参：
    _能力违规 为 一组：（空）。
完。

可变 _profile能力门控 为 空。

能力_设置profile门控 接受 profile名、允许能力列表：
    若 _profile能力门控 等于 空：
        _profile能力门控 为 做 字典。
    完。
    做 字典设 用 _profile能力门控、profile名、允许能力列表。
完。

能力_profile允许 接受 profile名、能力类型：
    若 _profile能力门控 等于 空：
        返还 真。
    完。
    白名单 为 做 字典取 用 _profile能力门控、profile名。
    若 白名单 等于 空：
        返还 真。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 白名单：
        若 白名单 的 第 i 项 等于 能力类型：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

能力_初始化默认profile 接受 零参：
    做 能力_设置profile门控 用 「linux-user」、一组：能力系统_能力_文件、能力系统_能力_网络、能力_系统调用。
    做 能力_设置profile门控 用 「kernel-qemu」、一组：能力_设备、能力_显存、能力系统_能力_文件、能力_系统调用、能力_中断、能力_特权、能力系统_能力_网络、能力_DMA、能力_裸指针、能力_汇编。
    做 能力_设置profile门控 用 「analysis-host」、一组：能力系统_能力_文件。
完。

能力_报告 接受 零参：
    可变 r 为 做 字典。
    可变 模块数 为 0。
    若 _能力表 不等于 空：
        模块数 为 做 计数 用 做 字典键 用 _能力表。
    完。
    做 字典设 用 r、「授权模块数」、做 转字符串 用 模块数。
    做 字典设 用 r、「能力违规数」、做 转字符串 用 做 计数 用 _能力违规。
    返还 r。
完。

能力_打印报告 接受 零参：
    r 为 做 能力_报告。
    做 打印文本 用 做 字符串拼接 用 「[CAPABILITY-REPORT] 授权模块=」、做 字符串拼接 用 做 字典取 用 r、「授权模块数」、做 字符串拼接 用 「 违规=」、做 字典取 用 r、「能力违规数」。
完。

能力_输出违规详情 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _能力违规：
        项 为 _能力违规 的 第 i 项。
        做 打印文本 用 做 字符串拼接 用 「  [能力违规] 模块=」、做 字符串拼接 用 项 的 第 0 项、做 字符串拼接 用 「 需要能力=」、做 能力名 用 项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

能力_批量检查profile 接受 profile名、模块名、效果列表：
    可变 违规数 为 0。
    若 profile名 等于 「」 或者 profile名 等于 空：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 效果列表：
        需要 为 做 效果_到能力 用 效果列表 的 第 i 项。
        若 需要 不等于 0：
            若 做 能力_profile允许 用 profile名、需要 等于 假：
                做 追加 用 _能力违规、一组：模块名、需要。
                违规数 为 违规数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 违规数。
完。

能力系统_报告 接受 零参：
    返还 做 能力_报告。
完。

本章 收束。
