《效果系统》

引用《..·..·标准库·基础》。

引用《..·..·标准库·转换器·转换验证》。

效果_无 为 0。

效果_IO 为 1。

效果_FFI 为 2。

效果_SYSCALL 为 3。

效果_GPU 为 4。

效果_ASM 为 5。

效果_RAW_PTR 为 6。

效果_UNSAFE_LAYOUT 为 7。

效果_INTERRUPT 为 8。

效果_DMA 为 9。

效果_PRIVILEGE 为 10。

效果名 接受 e：
    若 e 等于 效果_无：
        返还 「none」。
    完。
    若 e 等于 效果_IO：
        返还 「IO」。
    完。
    若 e 等于 效果_FFI：
        返还 「FFI」。
    完。
    若 e 等于 效果_SYSCALL：
        返还 「SYSCALL」。
    完。
    若 e 等于 效果_GPU：
        返还 「GPU」。
    完。
    若 e 等于 效果_ASM：
        返还 「ASM」。
    完。
    若 e 等于 效果_RAW_PTR：
        返还 「RAW_PTR」。
    完。
    若 e 等于 效果_UNSAFE_LAYOUT：
        返还 「UNSAFE_LAYOUT」。
    完。
    若 e 等于 效果_INTERRUPT：
        返还 「INTERRUPT」。
    完。
    若 e 等于 效果_DMA：
        返还 「DMA」。
    完。
    若 e 等于 效果_PRIVILEGE：
        返还 「PRIVILEGE」。
    完。
    返还 「未知效果」。
完。

可变 _效果签名表 为 空。

效果_初始化 接受 零参：
    _效果签名表 为 做 字典。
完。

效果_声明 接受 函数名、效果列表：
    若 _效果签名表 等于 空：
        做 效果_初始化。
    完。
    做 字典设 用 _效果签名表、函数名、效果列表。
完。

效果_查询 接受 函数名：
    若 _效果签名表 等于 空：
        返还 空。
    完。
    返还 做 字典取 用 _效果签名表、函数名。
完。

效果_有效果 接受 函数名：
    列表 为 做 效果_查询 用 函数名。
    若 列表 等于 空：
        返还 假。
    完。
    返还 做 计数 用 列表 大于 0。
完。

效果_包含 接受 函数名、效果类型：
    列表 为 做 效果_查询 用 函数名。
    若 列表 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 等于 效果类型：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

效果_从函数名推断 接受 名称：
    若 名称 等于 空：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    若 做 字符串包含 用 名称、「打印」 或者 做 字符串包含 用 名称、「写文件」 或者 做 字符串包含 用 名称、「读文件」 或者 做 字符串包含 用 名称、「print」：
        做 追加 用 结果、效果_IO。
    完。
    若 做 字符串包含 用 名称、「ffi」 或者 做 字符串包含 用 名称、「FFI」 或者 做 字符串包含 用 名称、「外部调用」：
        做 追加 用 结果、效果_FFI。
    完。
    若 做 字符串包含 用 名称、「syscall」 或者 做 字符串包含 用 名称、「系统调用」 或者 做 字符串包含 用 名称、「平台_」：
        做 追加 用 结果、效果_SYSCALL。
    完。
    若 做 字符串包含 用 名称、「gpu」 或者 做 字符串包含 用 名称、「GPU」 或者 做 字符串包含 用 名称、「显存」：
        做 追加 用 结果、效果_GPU。
    完。
    若 做 字符串包含 用 名称、「汇编」 或者 做 字符串包含 用 名称、「asm」 或者 做 字符串包含 用 名称、「内联汇编」：
        做 追加 用 结果、效果_ASM。
    完。
    若 做 字符串包含 用 名称、「指针」 或者 做 字符串包含 用 名称、「volatile」 或者 做 字符串包含 用 名称、「raw_ptr」：
        做 追加 用 结果、效果_RAW_PTR。
    完。
    若 做 字符串包含 用 名称、「中断」 或者 做 字符串包含 用 名称、「interrupt」 或者 做 字符串包含 用 名称、「IDT」：
        做 追加 用 结果、效果_INTERRUPT。
    完。
    若 做 字符串包含 用 名称、「特权」 或者 做 字符串包含 用 名称、「privilege」 或者 做 字符串包含 用 名称、「ring0」：
        做 追加 用 结果、效果_PRIVILEGE。
    完。
    返还 结果。
完。

可变 _模块效果表 为 空。

效果_模块聚合 接受 模块名、函数列表：
    若 _模块效果表 等于 空：
        _模块效果表 为 做 字典。
    完。
    可变 模块效果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 函数列表：
        若 做 是列表 用 函数列表 的 第 i 项 并且 做 计数 用 函数列表 的 第 i 项 大于或等于 4：
            函数名 为 函数列表 的 第 i 项 的 第 1 项。
            推断 为 做 效果_从函数名推断 用 函数名。
            若 做 计数 用 推断 大于 0：
                做 效果_声明 用 函数名、推断。
                可变 j 为 0。
                只要 j 小于 做 计数 用 推断：
                    可变 已有 为 假。
                    可变 k 为 0。
                    只要 k 小于 做 计数 用 模块效果：
                        若 模块效果 的 第 k 项 等于 推断 的 第 j 项：
                            已有 为 真。
                        完。
                        k 为 k 加 1。
                    完。
                    若 已有 等于 假：
                        做 追加 用 模块效果、推断 的 第 j 项。
                    完。
                    j 为 j 加 1。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    做 字典设 用 _模块效果表、模块名、模块效果。
    返还 模块效果。
完。

可变 _profile效果白名单 为 空。

效果_设置profile白名单 接受 profile名、允许效果列表：
    若 _profile效果白名单 等于 空：
        _profile效果白名单 为 做 字典。
    完。
    做 字典设 用 _profile效果白名单、profile名、允许效果列表。
完。

效果_profile允许 接受 profile名、效果类型：
    若 _profile效果白名单 等于 空：
        返还 真。
    完。
    白名单 为 做 字典取 用 _profile效果白名单、profile名。
    若 白名单 等于 空：
        返还 真。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 白名单：
        若 白名单 的 第 i 项 等于 效果类型：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

效果_初始化默认profile 接受 零参：
    做 效果_设置profile白名单 用 「linux-user」、一组：效果_IO、效果_FFI、效果_SYSCALL。
    做 效果_设置profile白名单 用 「kernel-qemu」、一组：效果_IO、效果_FFI、效果_SYSCALL、效果_GPU、效果_ASM、效果_RAW_PTR、效果_UNSAFE_LAYOUT、效果_INTERRUPT、效果_DMA、效果_PRIVILEGE。
    做 效果_设置profile白名单 用 「analysis-host」、一组：效果_IO。
完。

可变 _效果违规 为 一组：（空）。

效果_检查违规 接受 profile名、函数名：
    效果列表 为 做 效果_查询 用 函数名。
    若 效果列表 等于 空：
        返还 0。
    完。
    可变 违规数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 效果列表：
        若 做 效果_profile允许 用 profile名、效果列表 的 第 i 项 等于 假：
            做 追加 用 _效果违规、一组：profile名、函数名、效果列表 的 第 i 项。
            违规数 为 违规数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 违规数。
完。

效果_违规数 接受 零参：
    返还 做 计数 用 _效果违规。
完。

效果_重置违规 接受 零参：
    _效果违规 为 一组：（空）。
完。

效果_报告 接受 零参：
    可变 r 为 做 字典。
    可变 函数总数 为 0。
    若 _效果签名表 不等于 空：
        函数总数 为 做 计数 用 做 字典键 用 _效果签名表。
    完。
    可变 模块总数 为 0。
    若 _模块效果表 不等于 空：
        模块总数 为 做 计数 用 做 字典键 用 _模块效果表。
    完。
    做 字典设 用 r、「效果函数数」、做 转字符串 用 函数总数。
    做 字典设 用 r、「效果模块数」、做 转字符串 用 模块总数。
    做 字典设 用 r、「效果违规数」、做 转字符串 用 做 计数 用 _效果违规。
    返还 r。
完。

效果_打印报告 接受 零参：
    r 为 做 效果_报告。
    做 打印文本 用 做 字符串拼接 用 「[EFFECT-REPORT] 函数=」、做 字符串拼接 用 做 字典取 用 r、「效果函数数」、做 字符串拼接 用 「 模块=」、做 字符串拼接 用 做 字典取 用 r、「效果模块数」、做 字符串拼接 用 「 违规=」、做 字典取 用 r、「效果违规数」。
完。

效果_到能力 接受 效果类型：
    若 效果类型 等于 效果_IO：
        返还 3。
    完。
    若 效果类型 等于 效果_FFI：
        返还 0。
    完。
    若 效果类型 等于 效果_SYSCALL：
        返还 4。
    完。
    若 效果类型 等于 效果_GPU：
        返还 1。
    完。
    若 效果类型 等于 效果_ASM：
        返还 10。
    完。
    若 效果类型 等于 效果_RAW_PTR：
        返还 9。
    完。
    若 效果类型 等于 效果_INTERRUPT：
        返还 5。
    完。
    若 效果类型 等于 效果_DMA：
        返还 8。
    完。
    若 效果类型 等于 效果_PRIVILEGE：
        返还 6。
    完。
    返还 0。
完。

效果_批量检查违规 接受 profile名、函数列表：
    若 profile名 等于 「」 或者 profile名 等于 空：
        返还 0。
    完。
    可变 总违规 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 函数列表：
        若 做 是列表 用 函数列表 的 第 i 项 并且 做 计数 用 函数列表 的 第 i 项 大于或等于 4：
            函数名 为 函数列表 的 第 i 项 的 第 1 项。
            总违规 为 总违规 加 做 效果_检查违规 用 profile名、函数名。
        完。
        i 为 i 加 1。
    完。
    返还 总违规。
完。

效果_输出违规详情 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _效果违规：
        项 为 _效果违规 的 第 i 项。
        做 打印文本 用 做 字符串拼接 用 「  [效果违规] profile=」、做 字符串拼接 用 项 的 第 0 项、做 字符串拼接 用 「 函数=」、做 字符串拼接 用 项 的 第 1 项、做 字符串拼接 用 「 效果=」、做 效果名 用 项 的 第 2 项。
        i 为 i 加 1。
    完。
完。

效果系统_报告 接受 零参：
    返还 做 效果_报告。
完。

外源效果_UNSAFE_RUST 为 100。

外源效果_CGO 为 101。

外源效果_JNI 为 102。

外源效果_CTYPES 为 103。

外源效果_INLINE_ASM 为 104。

外源效果_ALLOCATOR 为 105。

外源效果_REFLECTION 为 106。

外源效果_THREAD_SPAWN 为 107。

效果_从外源映射 接受 外源效果：
    若 外源效果 等于 外源效果_UNSAFE_RUST：
        返还 一组：效果_RAW_PTR、效果_UNSAFE_LAYOUT。
    完。
    若 外源效果 等于 外源效果_CGO：
        返还 一组：效果_FFI。
    完。
    若 外源效果 等于 外源效果_JNI：
        返还 一组：效果_FFI。
    完。
    若 外源效果 等于 外源效果_CTYPES：
        返还 一组：效果_FFI。
    完。
    若 外源效果 等于 外源效果_INLINE_ASM：
        返还 一组：效果_ASM。
    完。
    若 外源效果 等于 外源效果_ALLOCATOR：
        返还 一组：效果_RAW_PTR。
    完。
    若 外源效果 等于 外源效果_REFLECTION：
        返还 一组：效果_UNSAFE_LAYOUT。
    完。
    若 外源效果 等于 外源效果_THREAD_SPAWN：
        返还 一组：效果_SYSCALL。
    完。
    返还 一组：（空）。
完。

效果_外源名 接受 e：
    若 e 等于 外源效果_UNSAFE_RUST：
        返还 「unsafe_rust」。
    完。
    若 e 等于 外源效果_CGO：
        返还 「cgo」。
    完。
    若 e 等于 外源效果_JNI：
        返还 「jni」。
    完。
    若 e 等于 外源效果_CTYPES：
        返还 「ctypes」。
    完。
    若 e 等于 外源效果_INLINE_ASM：
        返还 「inline_asm」。
    完。
    若 e 等于 外源效果_ALLOCATOR：
        返还 「allocator」。
    完。
    若 e 等于 外源效果_REFLECTION：
        返还 「reflection」。
    完。
    若 e 等于 外源效果_THREAD_SPAWN：
        返还 「thread_spawn」。
    完。
    返还 「unknown」。
完。

效果_初始化重置profile 接受 零参：
    做 效果_设置profile白名单 用 「strict-native」、一组：效果_IO。
    做 效果_设置profile白名单 用 「hosted-compatible」、一组：效果_IO、效果_FFI、效果_SYSCALL。
    做 效果_设置profile白名单 用 「hybrid-bridge」、一组：效果_IO、效果_FFI、效果_SYSCALL、效果_ASM、效果_RAW_PTR。
完。

可变 _外源效果审计 为 一组：（空）。

效果_审计外源边界 接受 profile名、外源边界列表：
    _外源效果审计 为 一组：（空）。
    可变 违规总数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 外源边界列表：
        边界 为 外源边界列表 的 第 i 项。
        映射效果 为 做 效果_从外源映射 用 边界 的 第 1 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 映射效果：
            若 做 效果_profile允许 用 profile名、映射效果 的 第 j 项 等于 假：
                做 追加 用 _外源效果审计、一组：边界 的 第 0 项、边界 的 第 1 项、映射效果 的 第 j 项、「违规」。
                违规总数 为 违规总数 加 1。
            否则：
                做 追加 用 _外源效果审计、一组：边界 的 第 0 项、边界 的 第 1 项、映射效果 的 第 j 项、「合规」。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 违规总数。
完。

效果_外源审计报告 接受 零参：
    r 为 做 字典。
    可变 合规数 为 0。
    可变 违规数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _外源效果审计：
        若 _外源效果审计 的 第 i 项 的 第 3 项 等于 「合规」：
            合规数 为 合规数 加 1。
        否则：
            违规数 为 违规数 加 1。
        完。
        i 为 i 加 1。
    完。
    做 字典设 用 r、「外源边界总数」、做 转字符串 用 做 计数 用 _外源效果审计。
    做 字典设 用 r、「合规数」、做 转字符串 用 合规数。
    做 字典设 用 r、「违规数」、做 转字符串 用 违规数。
    返还 r。
完。

效果_打印外源审计 接受 零参：
    做 打印 用 「═══ 外源边界效果审计 ═══」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _外源效果审计：
        项 为 _外源效果审计 的 第 i 项。
        i 为 i 加 1。
    完。
完。

本章 收束。
