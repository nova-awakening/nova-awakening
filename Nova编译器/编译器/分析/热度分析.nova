《热度分析》

引用《..·..·标准库·基础》。

引用《..·..·编译器·标记常量》。

热度_冷 为 0。

热度_暖 为 1。

热度_热 为 2。

热度_极热 为 3。

权重_循环体 为 100。

权重_函数调用 为 10。

权重_分支真 为 7。

权重_分支假 为 3。

权重_错误处理 为 0。

权重_顶层 为 1。

热度来源_静态 为 0。

热度来源_profile 为 1。

热度来源_循环 为 2。

热度来源_标注 为 3。

热度记录_创建 接受 位置、估算值、来源：
    返还 一组：位置、估算值、1、来源。
完。

热度记录_位置 接受 r：
    返还 r 的 第 0 项。
完。

热度记录_估算值 接受 r：
    返还 r 的 第 1 项。
完。

热度记录_执行次数 接受 r：
    返还 r 的 第 2 项。
完。

热度记录_来源 接受 r：
    返还 r 的 第 3 项。
完。

热度记录_更新执行次数 接受 r、次数：
    r 的 第 2 项 为 次数。
完。

热度_从循环深度 接受 循环深度：
    若 循环深度 等于 0：
        返还 权重_顶层。
    完。
    可变 热度 为 权重_顶层。
    可变 i 为 0。
    只要 i 小于 循环深度：
        热度 为 热度 乘 权重_循环体。
        若 热度 大于 1000000：
            热度 为 1000000。
        完。
        i 为 i 加 1。
    完。
    返还 热度。
完。

热度_分析语句列表 接受 语句列表、当前循环深度：
    可变 热度值 为 做 热度_从循环深度 用 当前循环深度。
    可变 函数调用数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列表：
        语句 为 语句列表 的 第 i 项。
        若 做 是列表 用 语句 并且 做 计数 用 语句 大于 0：
            若 语句 的 第 0 项 等于 节点_调用：
                函数调用数 为 函数调用数 加 1。
            完。
            若 语句 的 第 0 项 等于 标记_返回：
                热度值 为 热度值 乘 权重_循环体。
            完。
        完。
        i 为 i 加 1。
    完。
    热度值 为 热度值 加 函数调用数 乘 权重_函数调用。
    返还 热度值。
完。

热度_分析函数列表 接受 函数列表：
    可变 热度表 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 函数列表：
        函数信息 为 函数列表 的 第 i 项。
        若 做 是列表 用 函数信息 并且 做 计数 用 函数信息 大于或等于 4：
            函数名 为 函数信息 的 第 1 项。
            函数体 为 函数信息 的 第 3 项。
            可变 热度值 为 权重_顶层。
            若 做 是列表 用 函数体 并且 做 计数 用 函数体 大于 0：
                热度值 为 做 热度_分析语句列表 用 函数体、0。
            完。
            做 字典设 用 热度表、函数名、热度值。
        完。
        i 为 i 加 1。
    完。
    返还 热度表。
完。

热度_估算函数 接受 函数信息、调用次数、最大循环深度：
    可变 热度 为 调用次数 乘 权重_函数调用。
    热度 为 热度 加 做 热度_从循环深度 用 最大循环深度。
    若 热度 大于 1000000：
        热度 为 1000000。
    完。
    返还 热度。
完。

热度_级别 接受 热度值：
    若 热度值 大于或等于 10000：
        返还 热度_极热。
    完。
    若 热度值 大于或等于 100：
        返还 热度_热。
    完。
    若 热度值 大于或等于 10：
        返还 热度_暖。
    完。
    返还 热度_冷。
完。

热度_级别名 接受 级别：
    若 级别 等于 热度_极热：
        返还 「极热(必须优化)」。
    完。
    若 级别 等于 热度_热：
        返还 「热(建议优化)」。
    完。
    若 级别 等于 热度_暖：
        返还 「暖(可选优化)」。
    完。
    返还 「冷(无需优化)」。
完。

热度_应优先内联 接受 函数热度、函数节点数：
    若 做 热度_级别 用 函数热度 等于 热度_极热：
        返还 函数节点数 小于或等于 200。
    完。
    若 做 热度_级别 用 函数热度 等于 热度_热：
        返还 函数节点数 小于或等于 64。
    完。
    返还 函数节点数 小于或等于 20。
完。

热度_推荐展开因子 接受 循环热度、循环体大小：
    级别 为 做 热度_级别 用 循环热度。
    若 级别 等于 热度_极热 并且 循环体大小 小于或等于 4：
        返还 8。
    完。
    若 级别 等于 热度_极热 并且 循环体大小 小于或等于 8：
        返还 4。
    完。
    若 级别 等于 热度_热 并且 循环体大小 小于或等于 4：
        返还 4。
    完。
    若 级别 等于 热度_热 并且 循环体大小 小于或等于 8：
        返还 2。
    完。
    若 级别 等于 热度_暖 并且 循环体大小 小于或等于 2：
        返还 2。
    完。
    返还 1。
完。

热度_代码布局优先级 接受 热度值：
    返还 热度值。
完。

可变 _热度_profile数据 为 做 字典。

热度_记录profile 接受 位置ID、执行次数：
    做 字典设 用 _热度_profile数据、做 转字符串 用 位置ID、执行次数。
完。

热度_查询profile 接受 位置ID：
    v 为 做 字典取 用 _热度_profile数据、做 转字符串 用 位置ID。
    若 v 等于 空：
        返还 0 减 1。
    完。
    返还 v。
完。

热度_混合估算 接受 位置ID、静态估算：
    profile 为 做 热度_查询profile 用 位置ID。
    若 profile 大于或等于 0：
        返还 profile 乘 7 除以 10 加 静态估算 乘 3 除以 10。
    完。
    返还 静态估算。
完。

热度_排行TopN 接受 热度表、n：
    m 为 做 计数 用 热度表。
    可变 sorted_idx 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 m：
        做 追加 用 sorted_idx、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n 并且 i 小于 m：
        可变 最大idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 m：
            若 热度表 的 第 sorted_idx 的 第 j 项 项 的 第 1 项 大于 热度表 的 第 sorted_idx 的 第 最大idx 项 项 的 第 1 项：
                最大idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最大idx 不等于 i：
            暂存 为 sorted_idx 的 第 i 项。
            sorted_idx 的 第 i 项 为 sorted_idx 的 第 最大idx 项。
            sorted_idx 的 第 最大idx 项 为 暂存。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n 并且 i 小于 m：
        做 追加 用 结果、热度表 的 第 sorted_idx 的 第 i 项 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _热度_分析次数 为 0。

可变 _热度_热函数数 为 0。

热度_记录分析 接受 是热路径：
    _热度_分析次数 为 _热度_分析次数 加 1。
    若 是热路径：
        _热度_热函数数 为 _热度_热函数数 加 1。
    完。
完。

热度_报告 接受 零参：
    返还 一组：_热度_分析次数、_热度_热函数数、若 _热度_分析次数 大于 0 则 _热度_热函数数 乘 100 除以 _热度_分析次数 否则 0。
完。

本章 收束。
