《数据流分析》

引用《..·..·标准库·基础》。

引用《..·..·编译器·标记常量》。

数流_前向 为 0。

数流_后向 为 1。

数流_AST_定义 为 110。

数流_AST_赋值 为 111。

数流_AST_如果 为 112。

数流_AST_循环 为 113。

数流_AST_返回 为 114。

数流_AST_表达式语句 为 115。

数流_AST_索引赋值 为 116。

数流_AST_函数 为 120。

数流_AST_成员访问 为 131。

数流_AST_成员赋值 为 132。

数流_AST_遍历 为 134。

数流_AST_匹配 为 135。

数流_AST_中断 为 136。

数流_AST_继续 为 137。

数流_AST_尝试 为 160。

数流_AST_抛出 为 162。

数流_交集 为 0。

数流_并集 为 1。

数流_块_创建 接受 id、语句列表：
    返还 一组：id、语句列表、一组：（空）、一组：（空）、一组：（空）、一组：（空）、一组：（空）、一组：（空）。
完。

数流_块_ID 接受 块：
    返还 块 的 第 0 项。
完。

数流_块_语句 接受 块：
    返还 块 的 第 1 项。
完。

数流_块_前驱 接受 块：
    返还 块 的 第 2 项。
完。

数流_块_后继 接受 块：
    返还 块 的 第 3 项。
完。

数流_块_IN 接受 块：
    返还 块 的 第 4 项。
完。

数流_块_OUT 接受 块：
    返还 块 的 第 5 项。
完。

数流_块_GEN 接受 块：
    返还 块 的 第 6 项。
完。

数流_块_KILL 接受 块：
    返还 块 的 第 7 项。
完。

数流_块_添加前驱 接受 块、前驱ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 块 的 第 2 项：
        若 块 的 第 2 项 的 第 i 项 等于 前驱ID：
            返还 空。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 块 的 第 2 项、前驱ID。
完。

数流_块_添加后继 接受 块、后继ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 块 的 第 3 项：
        若 块 的 第 3 项 的 第 i 项 等于 后继ID：
            返还 空。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 块 的 第 3 项、后继ID。
完。

数流_集_元素键 接受 元素：
    返还 做 转字符串 用 元素。
完。

数流_集_索引表 接受 集合：
    可变 索引表 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合：
        做 字典设 用 索引表、做 数流_集_元素键 用 集合 的 第 i 项、i 加 1。
        i 为 i 加 1。
    完。
    返还 索引表。
完。

数流_集_索引含 接受 索引表、元素：
    返还 做 字典取 用 索引表、做 数流_集_元素键 用 元素 不等于 空。
完。

数流_集_追加唯一 接受 集合、索引表、元素：
    若 元素 等于 空 或者 元素 等于 「」：
        返还 假。
    完。
    若 做 数流_集_索引含 用 索引表、元素：
        返还 假。
    完。
    做 追加 用 集合、元素。
    做 字典设 用 索引表、做 数流_集_元素键 用 元素、做 计数 用 集合。
    返还 真。
完。

数流_ID列表_追加唯一 接受 列表、id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 等于 id：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 列表、id。
    返还 真。
完。

数流_ID列表_包含 接受 列表、id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 等于 id：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

数流_集_并 接受 A、B：
    若 做 计数 用 A 等于 0：
        返还 一组：做 数流_集_复制 用 B、做 计数 用 B 大于 0。
    完。
    若 做 计数 用 B 等于 0：
        返还 一组：做 数流_集_复制 用 A、假。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 A：
        做 追加 用 结果、A 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 变化 为 假。
    索引表 为 做 数流_集_索引表 用 结果。
    i 为 0。
    只要 i 小于 做 计数 用 B：
        元素 为 B 的 第 i 项。
        若 做 数流_集_索引含 用 索引表、元素 等于 假：
            做 追加 用 结果、元素。
            做 字典设 用 索引表、做 数流_集_元素键 用 元素、做 计数 用 结果。
            变化 为 真。
        完。
        i 为 i 加 1。
    完。
    返还 一组：结果、变化。
完。

数流_集_交 接受 A、B：
    若 做 计数 用 A 等于 0 或者 做 计数 用 B 等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    索引表 为 做 数流_集_索引表 用 B。
    可变 i 为 0。
    只要 i 小于 做 计数 用 A：
        元素 为 A 的 第 i 项。
        若 做 数流_集_索引含 用 索引表、元素：
            做 追加 用 结果、元素。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数流_集_差 接受 A、B：
    若 做 计数 用 A 等于 0：
        返还 一组：（空）。
    完。
    若 做 计数 用 B 等于 0：
        返还 做 数流_集_复制 用 A。
    完。
    可变 结果 为 一组：（空）。
    索引表 为 做 数流_集_索引表 用 B。
    可变 i 为 0。
    只要 i 小于 做 计数 用 A：
        元素 为 A 的 第 i 项。
        若 做 数流_集_索引含 用 索引表、元素 等于 假：
            做 追加 用 结果、元素。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数流_集_相等 接受 A、B：
    若 做 计数 用 A 不等于 做 计数 用 B：
        返还 假。
    完。
    若 做 计数 用 A 等于 0：
        返还 真。
    完。
    索引表 为 做 数流_集_索引表 用 B。
    可变 i 为 0。
    只要 i 小于 做 计数 用 A：
        若 做 数流_集_索引含 用 索引表、A 的 第 i 项 等于 假：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

数流_集_复制 接受 A：
    若 做 计数 用 A 等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 A：
        做 追加 用 结果、A 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数流_CFG_创建 接受 入口ID：
    返还 一组：一组：（空）、入口ID。
完。

数流_CFG_块列表 接受 cfg：
    返还 cfg 的 第 0 项。
完。

数流_CFG_入口 接受 cfg：
    返还 cfg 的 第 1 项。
完。

数流_CFG_添加块 接受 cfg、块：
    做 追加 用 cfg 的 第 0 项、块。
完。

数流_CFG_查找块 接受 cfg、id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 cfg 的 第 0 项：
        若 做 数流_块_ID 用 cfg 的 第 0 项 的 第 i 项 等于 id：
            返还 cfg 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

数流_CFG_块索引表 接受 cfg：
    块列表 为 做 数流_CFG_块列表 用 cfg。
    可变 索引表 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 块列表：
        做 字典设 用 索引表、做 转字符串 用 做 数流_块_ID 用 块列表 的 第 i 项、i 加 1。
        i 为 i 加 1。
    完。
    返还 索引表。
完。

数流_CFG_块索引 接受 索引表、块ID：
    编码索引 为 做 字典取 用 索引表、做 转字符串 用 块ID。
    若 编码索引 等于 空：
        返还 0 减 1。
    完。
    返还 编码索引 减 1。
完。

数流_CFG_添加边 接受 cfg、从ID、到ID：
    从块 为 做 数流_CFG_查找块 用 cfg、从ID。
    到块 为 做 数流_CFG_查找块 用 cfg、到ID。
    若 从块 不等于 空 并且 到块 不等于 空：
        做 数流_块_添加后继 用 从块、到ID。
        做 数流_块_添加前驱 用 到块、从ID。
    完。
完。

数流_变量_追加 接受 结果、索引表、变量名：
    若 变量名 等于 空 或者 变量名 等于 「」：
        返还 假。
    完。
    返还 做 数流_集_追加唯一 用 结果、索引表、变量名。
完。

数流_列表_追加唯一 接受 列表、元素：
    若 元素 等于 空 或者 元素 等于 「」：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 等于 元素：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 列表、元素。
    返还 真。
完。

数流_表达式_直接名称 接受 表达式：
    若 做 是列表 用 表达式 并且 做 计数 用 表达式 大于或等于 2 并且 表达式 的 第 0 项 等于 标记_名称：
        返还 表达式 的 第 1 项。
    完。
    返还 空。
完。

数流_表达式_收集变量 接受 节点、结果、索引表：
    若 做 是列表 用 节点 等于 假 或者 做 计数 用 节点 等于 0：
        返还 空。
    完。
    若 做 是整数 用 节点 的 第 0 项 并且 节点 的 第 0 项 等于 标记_名称 并且 做 计数 用 节点 大于或等于 2：
        做 数流_变量_追加 用 结果、索引表、节点 的 第 1 项。
        返还 空。
    完。
    可变 起始 为 0。
    若 做 是整数 用 节点 的 第 0 项：
        起始 为 1。
    完。
    可变 i 为 起始。
    只要 i 小于 做 计数 用 节点：
        若 做 是列表 用 节点 的 第 i 项：
            做 数流_表达式_收集变量 用 节点 的 第 i 项、结果、索引表。
        完。
        i 为 i 加 1。
    完。
完。

数流_语句_局部使用变量 接受 语句：
    可变 结果 为 一组：（空）。
    索引表 为 做 字典。
    若 做 是列表 用 语句 等于 假 或者 做 计数 用 语句 等于 0：
        返还 结果。
    完。
    类型 为 语句 的 第 0 项。
    若 (类型 等于 数流_AST_定义 或者 类型 等于 数流_AST_赋值) 并且 做 计数 用 语句 大于或等于 3：
        做 数流_表达式_收集变量 用 语句 的 第 2 项、结果、索引表。
        返还 结果。
    完。
    若 (类型 等于 数流_AST_如果 或者 类型 等于 数流_AST_循环 或者 类型 等于 数流_AST_返回 或者 类型 等于 数流_AST_表达式语句 或者 类型 等于 数流_AST_抛出) 并且 做 计数 用 语句 大于或等于 2：
        做 数流_表达式_收集变量 用 语句 的 第 1 项、结果、索引表。
        返还 结果。
    完。
    若 类型 等于 数流_AST_索引赋值 并且 做 计数 用 语句 大于或等于 4：
        做 数流_变量_追加 用 结果、索引表、语句 的 第 1 项。
        做 数流_表达式_收集变量 用 语句 的 第 2 项、结果、索引表。
        做 数流_表达式_收集变量 用 语句 的 第 3 项、结果、索引表。
        返还 结果。
    完。
    若 类型 等于 数流_AST_成员赋值 并且 做 计数 用 语句 大于或等于 4：
        若 做 是列表 用 语句 的 第 1 项：
            做 数流_表达式_收集变量 用 语句 的 第 1 项、结果、索引表。
        否则：
            做 数流_变量_追加 用 结果、索引表、语句 的 第 1 项。
        完。
        做 数流_表达式_收集变量 用 语句 的 第 3 项、结果、索引表。
        返还 结果。
    完。
    若 类型 等于 数流_AST_遍历 并且 做 计数 用 语句 大于或等于 3：
        做 数流_表达式_收集变量 用 语句 的 第 2 项、结果、索引表。
        返还 结果。
    完。
    若 类型 等于 数流_AST_匹配 并且 做 计数 用 语句 大于或等于 2：
        做 数流_表达式_收集变量 用 语句 的 第 1 项、结果、索引表。
        返还 结果。
    完。
    返还 结果。
完。

数流_语句_局部定义变量 接受 语句：
    可变 结果 为 一组：（空）。
    索引表 为 做 字典。
    若 做 是列表 用 语句 等于 假 或者 做 计数 用 语句 等于 0：
        返还 结果。
    完。
    类型 为 语句 的 第 0 项。
    若 (类型 等于 数流_AST_定义 或者 类型 等于 数流_AST_赋值 或者 类型 等于 数流_AST_索引赋值 或者 类型 等于 数流_AST_遍历) 并且 做 计数 用 语句 大于或等于 2：
        做 数流_变量_追加 用 结果、索引表、语句 的 第 1 项。
        返还 结果。
    完。
    若 类型 等于 数流_AST_成员赋值 并且 做 计数 用 语句 大于或等于 2：
        若 做 是列表 用 语句 的 第 1 项 并且 做 计数 用 语句 的 第 1 项 大于或等于 2 并且 语句 的 第 1 项 的 第 0 项 等于 标记_名称：
            做 数流_变量_追加 用 结果、索引表、语句 的 第 1 项 的 第 1 项。
        否则若 做 是列表 用 语句 的 第 1 项 等于 假：
            做 数流_变量_追加 用 结果、索引表、语句 的 第 1 项。
        完。
        返还 结果。
    完。
    若 类型 等于 数流_AST_函数 并且 做 计数 用 语句 大于或等于 3 并且 做 是列表 用 语句 的 第 2 项：
        可变 i 为 0。
        只要 i 小于 做 计数 用 语句 的 第 2 项：
            做 数流_变量_追加 用 结果、索引表、语句 的 第 2 项 的 第 i 项。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

数流_块_填充默认摘要 接受 块：
    块 的 第 6 项 为 一组：（空）。
    块 的 第 7 项 为 一组：（空）。
    语句列 为 做 数流_块_语句 用 块。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列：
        语句 为 语句列 的 第 i 项。
        若 做 是列表 用 语句 并且 做 计数 用 语句 大于 0：
            类型 为 语句 的 第 0 项。
            若 类型 等于 数流_AST_函数 并且 做 计数 用 语句 大于或等于 3 并且 做 是列表 用 语句 的 第 2 项：
                可变 j 为 0。
                只要 j 小于 做 计数 用 语句 的 第 2 项：
                    做 数流_列表_追加唯一 用 块 的 第 7 项、语句 的 第 2 项 的 第 j 项。
                    j 为 j 加 1。
                完。
            完。
            若 (类型 等于 数流_AST_定义 或者 类型 等于 数流_AST_赋值) 并且 做 计数 用 语句 大于或等于 3：
                做 数流_列表_追加唯一 用 块 的 第 7 项、语句 的 第 1 项。
                做 数流_列表_追加唯一 用 块 的 第 6 项、做 数流_表达式_直接名称 用 语句 的 第 2 项。
            否则若 (类型 等于 数流_AST_返回 或者 类型 等于 数流_AST_表达式语句 或者 类型 等于 数流_AST_如果 或者 类型 等于 数流_AST_循环 或者 类型 等于 数流_AST_抛出) 并且 做 计数 用 语句 大于或等于 2：
                做 数流_列表_追加唯一 用 块 的 第 6 项、做 数流_表达式_直接名称 用 语句 的 第 1 项。
            否则若 类型 等于 数流_AST_索引赋值 并且 做 计数 用 语句 大于或等于 4：
                做 数流_列表_追加唯一 用 块 的 第 7 项、语句 的 第 1 项。
                做 数流_列表_追加唯一 用 块 的 第 6 项、语句 的 第 1 项。
                做 数流_列表_追加唯一 用 块 的 第 6 项、做 数流_表达式_直接名称 用 语句 的 第 2 项。
                做 数流_列表_追加唯一 用 块 的 第 6 项、做 数流_表达式_直接名称 用 语句 的 第 3 项。
            否则若 类型 等于 数流_AST_遍历 并且 做 计数 用 语句 大于或等于 3：
                做 数流_列表_追加唯一 用 块 的 第 7 项、语句 的 第 1 项。
                做 数流_列表_追加唯一 用 块 的 第 6 项、做 数流_表达式_直接名称 用 语句 的 第 2 项。
            否则若 类型 等于 数流_AST_成员赋值 并且 做 计数 用 语句 大于或等于 4：
                做 数流_列表_追加唯一 用 块 的 第 6 项、做 数流_表达式_直接名称 用 语句 的 第 1 项。
                做 数流_列表_追加唯一 用 块 的 第 6 项、做 数流_表达式_直接名称 用 语句 的 第 3 项。
            完。
        完。
        i 为 i 加 1。
    完。
完。

数流_CFG_重置分析 接受 cfg：
    块列表 为 做 数流_CFG_块列表 用 cfg。
    可变 i 为 0。
    只要 i 小于 做 计数 用 块列表：
        块列表 的 第 i 项 的 第 4 项 为 一组：（空）。
        块列表 的 第 i 项 的 第 5 项 为 一组：（空）。
        i 为 i 加 1。
    完。
完。

数流_CFG_填充默认摘要 接受 cfg：
    块列表 为 做 数流_CFG_块列表 用 cfg。
    可变 i 为 0。
    只要 i 小于 做 计数 用 块列表：
        做 数流_块_填充默认摘要 用 块列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

数流_CFG_清空语句 接受 cfg：
    块列表 为 做 数流_CFG_块列表 用 cfg。
    可变 i 为 0。
    只要 i 小于 做 计数 用 块列表：
        块列表 的 第 i 项 的 第 1 项 为 一组：（空）。
        i 为 i 加 1。
    完。
完。

数流_CFG_新块 接受 cfg、状态、语句列表：
    块ID 为 状态 的 第 0 项。
    状态 的 第 0 项 为 状态 的 第 0 项 加 1。
    做 数流_CFG_添加块 用 cfg、做 数流_块_创建 用 块ID、语句列表。
    返还 块ID。
完。

数流_CFG_连接出口 接受 cfg、出口列表、入口ID：
    若 入口ID 小于 0：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 出口列表：
        做 数流_CFG_添加边 用 cfg、出口列表 的 第 i 项、入口ID。
        i 为 i 加 1。
    完。
完。

数流_函数入口语句 接受 函数语句：
    可变 行号 为 0。
    若 做 计数 用 函数语句 大于或等于 5：
        行号 为 函数语句 的 第 4 项。
    完。
    返还 一组：数流_AST_函数、函数语句 的 第 1 项、函数语句 的 第 2 项、一组：（空）、行号。
完。

数流_CFG_构建语句列 接受 cfg、状态、语句列、继续目标、中断目标：
    若 做 是列表 用 语句列 等于 假 或者 做 计数 用 语句列 等于 0：
        返还 一组：负 1、一组：（空）。
    完。
    可变 首块ID 为 0 减 1。
    可变 出口列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列：
        片段 为 做 数流_CFG_构建单语句 用 cfg、状态、语句列 的 第 i 项、继续目标、中断目标。
        若 片段 的 第 0 项 大于或等于 0：
            若 首块ID 小于 0：
                首块ID 为 片段 的 第 0 项。
            完。
            若 做 计数 用 出口列表 大于 0：
                做 数流_CFG_连接出口 用 cfg、出口列表、片段 的 第 0 项。
            完。
            出口列表 为 片段 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    若 首块ID 小于 0：
        返还 一组：负 1、一组：（空）。
    完。
    返还 一组：首块ID、出口列表。
完。

数流_CFG_构建单语句 接受 cfg、状态、语句、继续目标、中断目标：
    若 做 是列表 用 语句 等于 假 或者 做 计数 用 语句 等于 0：
        返还 一组：负 1、一组：（空）。
    完。
    类型 为 语句 的 第 0 项。
    若 类型 等于 数流_AST_如果：
        条件块ID 为 做 数流_CFG_新块 用 cfg、状态、一组：语句。
        可变 出口列表 为 一组：（空）。
        真支 为 一组：（空）。
        假支 为 一组：（空）。
        若 做 计数 用 语句 大于或等于 3 并且 做 是列表 用 语句 的 第 2 项：
            真支 为 语句 的 第 2 项。
        完。
        若 做 计数 用 语句 大于或等于 4 并且 做 是列表 用 语句 的 第 3 项：
            假支 为 语句 的 第 3 项。
        完。
        真片段 为 做 数流_CFG_构建语句列 用 cfg、状态、真支、继续目标、中断目标。
        若 真片段 的 第 0 项 大于或等于 0：
            做 数流_CFG_添加边 用 cfg、条件块ID、真片段 的 第 0 项。
            可变 i 为 0。
            只要 i 小于 做 计数 用 真片段 的 第 1 项：
                做 数流_ID列表_追加唯一 用 出口列表、真片段 的 第 1 项 的 第 i 项。
                i 为 i 加 1。
            完。
        否则：
            做 数流_ID列表_追加唯一 用 出口列表、条件块ID。
        完。
        假片段 为 做 数流_CFG_构建语句列 用 cfg、状态、假支、继续目标、中断目标。
        若 假片段 的 第 0 项 大于或等于 0：
            做 数流_CFG_添加边 用 cfg、条件块ID、假片段 的 第 0 项。
            可变 j 为 0。
            只要 j 小于 做 计数 用 假片段 的 第 1 项：
                做 数流_ID列表_追加唯一 用 出口列表、假片段 的 第 1 项 的 第 j 项。
                j 为 j 加 1。
            完。
        否则：
            做 数流_ID列表_追加唯一 用 出口列表、条件块ID。
        完。
        返还 一组：条件块ID、出口列表。
    完。
    若 类型 等于 数流_AST_循环 或者 类型 等于 数流_AST_遍历：
        条件块ID 为 做 数流_CFG_新块 用 cfg、状态、一组：语句。
        后继块ID 为 做 数流_CFG_新块 用 cfg、状态、一组：（空）。
        做 数流_CFG_添加边 用 cfg、条件块ID、后继块ID。
        体语句列 为 一组：（空）。
        若 类型 等于 数流_AST_循环 并且 做 计数 用 语句 大于或等于 3 并且 做 是列表 用 语句 的 第 2 项：
            体语句列 为 语句 的 第 2 项。
        完。
        若 类型 等于 数流_AST_遍历 并且 做 计数 用 语句 大于或等于 4 并且 做 是列表 用 语句 的 第 3 项：
            体语句列 为 语句 的 第 3 项。
        完。
        体片段 为 做 数流_CFG_构建语句列 用 cfg、状态、体语句列、条件块ID、后继块ID。
        若 体片段 的 第 0 项 大于或等于 0：
            做 数流_CFG_添加边 用 cfg、条件块ID、体片段 的 第 0 项。
            做 数流_CFG_连接出口 用 cfg、体片段 的 第 1 项、条件块ID。
        否则：
            做 数流_CFG_添加边 用 cfg、条件块ID、条件块ID。
        完。
        返还 一组：条件块ID、一组：后继块ID。
    完。
    若 类型 等于 数流_AST_匹配：
        条件块ID 为 做 数流_CFG_新块 用 cfg、状态、一组：语句。
        可变 出口列表 为 一组：（空）。
        可变 有默认 为 假。
        若 做 计数 用 语句 大于或等于 3 并且 做 是列表 用 语句 的 第 2 项：
            可变 i 为 0。
            只要 i 小于 做 计数 用 语句 的 第 2 项：
                分支 为 语句 的 第 2 项 的 第 i 项。
                若 做 是列表 用 分支 并且 做 计数 用 分支 大于或等于 2：
                    若 分支 的 第 0 项 等于 空：
                        有默认 为 真。
                    完。
                    分支片段 为 做 数流_CFG_构建语句列 用 cfg、状态、分支 的 第 1 项、继续目标、中断目标。
                    若 分支片段 的 第 0 项 大于或等于 0：
                        做 数流_CFG_添加边 用 cfg、条件块ID、分支片段 的 第 0 项。
                        可变 j 为 0。
                        只要 j 小于 做 计数 用 分支片段 的 第 1 项：
                            做 数流_ID列表_追加唯一 用 出口列表、分支片段 的 第 1 项 的 第 j 项。
                            j 为 j 加 1。
                        完。
                    否则：
                        做 数流_ID列表_追加唯一 用 出口列表、条件块ID。
                    完。
                完。
                i 为 i 加 1。
            完。
        完。
        若 有默认 等于 假：
            做 数流_ID列表_追加唯一 用 出口列表、条件块ID。
        完。
        返还 一组：条件块ID、出口列表。
    完。
    若 类型 等于 数流_AST_尝试：
        尝试块ID 为 做 数流_CFG_新块 用 cfg、状态、一组：语句。
        可变 出口列表 为 一组：（空）。
        试片段 为 一组：负 1、一组：（空）。
        得片段 为 一组：负 1、一组：（空）。
        若 做 计数 用 语句 大于或等于 2：
            试片段 为 做 数流_CFG_构建语句列 用 cfg、状态、语句 的 第 1 项、继续目标、中断目标。
        完。
        若 做 计数 用 语句 大于或等于 4：
            得片段 为 做 数流_CFG_构建语句列 用 cfg、状态、语句 的 第 3 项、继续目标、中断目标。
        完。
        若 试片段 的 第 0 项 大于或等于 0：
            做 数流_CFG_添加边 用 cfg、尝试块ID、试片段 的 第 0 项。
            可变 i 为 0。
            只要 i 小于 做 计数 用 试片段 的 第 1 项：
                做 数流_ID列表_追加唯一 用 出口列表、试片段 的 第 1 项 的 第 i 项。
                i 为 i 加 1。
            完。
        否则：
            做 数流_ID列表_追加唯一 用 出口列表、尝试块ID。
        完。
        若 得片段 的 第 0 项 大于或等于 0：
            做 数流_CFG_添加边 用 cfg、尝试块ID、得片段 的 第 0 项。
            可变 j 为 0。
            只要 j 小于 做 计数 用 得片段 的 第 1 项：
                做 数流_ID列表_追加唯一 用 出口列表、得片段 的 第 1 项 的 第 j 项。
                j 为 j 加 1。
            完。
        完。
        返还 一组：尝试块ID、出口列表。
    完。
    块ID 为 做 数流_CFG_新块 用 cfg、状态、一组：语句。
    若 类型 等于 数流_AST_中断：
        若 中断目标 大于或等于 0：
            做 数流_CFG_添加边 用 cfg、块ID、中断目标。
        完。
        返还 一组：块ID、一组：（空）。
    完。
    若 类型 等于 数流_AST_继续：
        若 继续目标 大于或等于 0：
            做 数流_CFG_添加边 用 cfg、块ID、继续目标。
        完。
        返还 一组：块ID、一组：（空）。
    完。
    若 类型 等于 数流_AST_返回 或者 类型 等于 数流_AST_抛出：
        返还 一组：块ID、一组：（空）。
    完。
    返还 一组：块ID、一组：块ID。
完。

数流_CFG_从函数列与主体构建 接受 函数列、主体：
    cfg 为 做 数流_CFG_创建 用 0。
    构建状态 为 一组：1。
    做 数流_CFG_添加块 用 cfg、做 数流_块_创建 用 0、一组：（空）。
    若 主体 不等于 空 并且 做 是列表 用 主体 并且 做 计数 用 主体 大于 0：
        主体片段 为 做 数流_CFG_构建语句列 用 cfg、构建状态、主体、0 减 1、0 减 1。
        若 主体片段 的 第 0 项 大于或等于 0：
            做 数流_CFG_添加边 用 cfg、0、主体片段 的 第 0 项。
        完。
    完。
    若 函数列 不等于 空 并且 做 是列表 用 函数列：
        可变 i 为 0。
        只要 i 小于 做 计数 用 函数列：
            函数语句 为 函数列 的 第 i 项。
            若 做 是列表 用 函数语句 并且 做 计数 用 函数语句 大于或等于 4：
                函数入口块ID 为 做 数流_CFG_新块 用 cfg、构建状态、一组：做 数流_函数入口语句 用 函数语句。
                函数出口块ID 为 做 数流_CFG_新块 用 cfg、构建状态、一组：（空）。
                若 做 是列表 用 函数语句 的 第 3 项 并且 做 计数 用 函数语句 的 第 3 项 大于 0：
                    函数片段 为 做 数流_CFG_构建语句列 用 cfg、构建状态、函数语句 的 第 3 项、0 减 1、0 减 1。
                    若 函数片段 的 第 0 项 大于或等于 0：
                        做 数流_CFG_添加边 用 cfg、函数入口块ID、函数片段 的 第 0 项。
                        做 数流_CFG_连接出口 用 cfg、函数片段 的 第 1 项、函数出口块ID。
                    否则：
                        做 数流_CFG_添加边 用 cfg、函数入口块ID、函数出口块ID。
                    完。
                否则：
                    做 数流_CFG_添加边 用 cfg、函数入口块ID、函数出口块ID。
                完。
            完。
            i 为 i 加 1。
        完。
    完。
    做 数流_CFG_填充默认摘要 用 cfg。
    做 数流_CFG_清空语句 用 cfg。
    做 数流_CFG_重置分析 用 cfg。
    返还 cfg。
完。

数流_工作列表_创建 接受 块数：
    可变 工作列表 为 一组：（空）。
    可变 在列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 块数：
        做 追加 用 工作列表、i。
        做 追加 用 在列、真。
        i 为 i 加 1。
    完。
    返还 一组：工作列表、在列、0。
完。

数流_工作列表_从初始索引创建 接受 块数、初始索引列表：
    可变 工作列表 为 一组：（空）。
    可变 在列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 块数：
        做 追加 用 在列、假。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 初始索引列表：
        索引 为 初始索引列表 的 第 i 项。
        若 索引 大于或等于 0 并且 索引 小于 块数 并且 在列 的 第 索引 项 等于 假：
            做 追加 用 工作列表、索引。
            在列 的 第 索引 项 为 真。
        完。
        i 为 i 加 1。
    完。
    返还 一组：工作列表、在列、0。
完。

数流_CFG_指定入口可达索引列表 接受 cfg、入口块ID：
    块列表 为 做 数流_CFG_块列表 用 cfg。
    块数 为 做 计数 用 块列表。
    索引表 为 做 数流_CFG_块索引表 用 cfg。
    可变 结果 为 一组：（空）。
    可变 已见ID 为 一组：（空）。
    可变 队列 为 一组：入口块ID。
    可变 前沿 为 0。
    只要 前沿 小于 做 计数 用 队列：
        块ID 为 队列 的 第 前沿 项。
        前沿 为 前沿 加 1。
        若 做 数流_ID列表_追加唯一 用 已见ID、块ID：
            索引 为 做 数流_CFG_块索引 用 索引表、块ID。
            若 索引 大于或等于 0：
                做 追加 用 结果、索引。
                后继列表 为 做 数流_块_后继 用 块列表 的 第 索引 项。
                可变 i 为 0。
                只要 i 小于 做 计数 用 后继列表：
                    做 追加 用 队列、后继列表 的 第 i 项。
                    i 为 i 加 1。
                完。
            完。
        完。
    完。
    若 做 计数 用 结果 等于 0：
        入口索引 为 做 数流_CFG_块索引 用 索引表、入口块ID。
        若 入口索引 大于或等于 0：
            返还 一组：入口索引。
        完。
        若 块数 大于 0 并且 入口块ID 等于 做 数流_CFG_入口 用 cfg：
            返还 一组：0。
        完。
    完。
    返还 结果。
完。

数流_CFG_入口可达索引列表 接受 cfg：
    返还 做 数流_CFG_指定入口可达索引列表 用 cfg、做 数流_CFG_入口 用 cfg。
完。

数流_CFG_入口可达子图 接受 cfg：
    原块列表 为 做 数流_CFG_块列表 用 cfg。
    可达索引列表 为 做 数流_CFG_入口可达索引列表 用 cfg。
    可变 可达ID列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 可达索引列表：
        做 追加 用 可达ID列表、做 数流_块_ID 用 原块列表 的 第 可达索引列表 的 第 i 项 项。
        i 为 i 加 1。
    完。
    子图 为 做 数流_CFG_创建 用 做 数流_CFG_入口 用 cfg。
    i 为 0。
    只要 i 小于 做 计数 用 可达索引列表：
        原块 为 原块列表 的 第 可达索引列表 的 第 i 项 项。
        做 数流_CFG_添加块 用 子图、做 数流_块_创建 用 做 数流_块_ID 用 原块、一组：（空）。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 可达索引列表：
        原块 为 原块列表 的 第 可达索引列表 的 第 i 项 项。
        新块 为 做 数流_CFG_查找块 用 子图、做 数流_块_ID 用 原块。
        若 新块 不等于 空：
            可变 j 为 0。
            只要 j 小于 做 计数 用 做 数流_块_前驱 用 原块：
                前驱ID 为 做 数流_块_前驱 用 原块 的 第 j 项。
                若 做 数流_ID列表_包含 用 可达ID列表、前驱ID：
                    做 数流_块_添加前驱 用 新块、前驱ID。
                完。
                j 为 j 加 1。
            完。
            j 为 0。
            只要 j 小于 做 计数 用 做 数流_块_后继 用 原块：
                后继ID 为 做 数流_块_后继 用 原块 的 第 j 项。
                若 做 数流_ID列表_包含 用 可达ID列表、后继ID：
                    做 数流_块_添加后继 用 新块、后继ID。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 子图。
完。

数流_工作列表_加入 接受 工作列表、在列、块索引：
    若 块索引 小于 0 或者 块索引 大于或等于 做 计数 用 在列：
        返还 假。
    完。
    若 在列 的 第 块索引 项：
        返还 假。
    完。
    做 追加 用 工作列表、块索引。
    在列 的 第 块索引 项 为 真。
    返还 真。
完。

数流_meet 接受 A、B、操作：
    若 操作 等于 数流_并集：
        返还 做 数流_集_并 用 A、B 的 第 0 项。
    完。
    返还 做 数流_集_交 用 A、B。
完。

数流_前向求解 接受 cfg、meet操作：
    返还 做 数流_前向求解_从入口 用 cfg、meet操作、做 数流_CFG_入口 用 cfg。
完。

数流_前向求解_从入口 接受 cfg、meet操作、入口块ID：
    可变 迭代数 为 0。
    块列表 为 做 数流_CFG_块列表 用 cfg。
    n 为 做 计数 用 块列表。
    最大迭代 为 n 乘 10。
    块索引表 为 做 数流_CFG_块索引表 用 cfg。
    队列状态 为 做 数流_工作列表_从初始索引创建 用 n、做 数流_CFG_指定入口可达索引列表 用 cfg、入口块ID。
    工作列表 为 队列状态 的 第 0 项。
    在列 为 队列状态 的 第 1 项。
    可变 前沿 为 队列状态 的 第 2 项。
    只要 前沿 小于 做 计数 用 工作列表 并且 迭代数 小于 最大迭代：
        块索引 为 工作列表 的 第 前沿 项。
        前沿 为 前沿 加 1。
        在列 的 第 块索引 项 为 假。
        块 为 块列表 的 第 块索引 项。
        前驱列表 为 做 数流_块_前驱 用 块。
        可变 新IN 为 一组：（空）。
        若 做 计数 用 前驱列表 大于 0：
            p0索引 为 做 数流_CFG_块索引 用 块索引表、前驱列表 的 第 0 项。
            若 p0索引 大于或等于 0：
                新IN 为 做 数流_集_复制 用 做 数流_块_OUT 用 块列表 的 第 p0索引 项。
            完。
            可变 i 为 1。
            只要 i 小于 做 计数 用 前驱列表：
                p索引 为 做 数流_CFG_块索引 用 块索引表、前驱列表 的 第 i 项。
                若 p索引 大于或等于 0：
                    新IN 为 做 数流_meet 用 新IN、做 数流_块_OUT 用 块列表 的 第 p索引 项、meet操作。
                完。
                i 为 i 加 1。
            完。
        完。
        in减kill 为 做 数流_集_差 用 新IN、做 数流_块_KILL 用 块。
        新OUT结果 为 做 数流_集_并 用 做 数流_块_GEN 用 块、in减kill。
        新OUT 为 新OUT结果 的 第 0 项。
        若 做 数流_集_相等 用 新OUT、做 数流_块_OUT 用 块 等于 假：
            块 的 第 4 项 为 新IN。
            块 的 第 5 项 为 新OUT。
            后继列表 为 做 数流_块_后继 用 块。
            可变 i 为 0。
            只要 i 小于 做 计数 用 后继列表：
                后继索引 为 做 数流_CFG_块索引 用 块索引表、后继列表 的 第 i 项。
                若 后继索引 大于或等于 0：
                    做 数流_工作列表_加入 用 工作列表、在列、后继索引。
                完。
                i 为 i 加 1。
            完。
        否则：
            块 的 第 4 项 为 新IN。
        完。
        迭代数 为 迭代数 加 1。
    完。
    返还 迭代数。
完。

数流_后向求解 接受 cfg、meet操作：
    返还 做 数流_后向求解_从入口 用 cfg、meet操作、做 数流_CFG_入口 用 cfg。
完。

数流_后向求解_从入口 接受 cfg、meet操作、入口块ID：
    可变 迭代数 为 0。
    块列表 为 做 数流_CFG_块列表 用 cfg。
    n 为 做 计数 用 块列表。
    最大迭代 为 n 乘 10。
    块索引表 为 做 数流_CFG_块索引表 用 cfg。
    队列状态 为 做 数流_工作列表_从初始索引创建 用 n、做 数流_CFG_指定入口可达索引列表 用 cfg、入口块ID。
    工作列表 为 队列状态 的 第 0 项。
    在列 为 队列状态 的 第 1 项。
    可变 前沿 为 队列状态 的 第 2 项。
    只要 前沿 小于 做 计数 用 工作列表 并且 迭代数 小于 最大迭代：
        块索引 为 工作列表 的 第 前沿 项。
        前沿 为 前沿 加 1。
        在列 的 第 块索引 项 为 假。
        块 为 块列表 的 第 块索引 项。
        后继列表 为 做 数流_块_后继 用 块。
        可变 新OUT 为 一组：（空）。
        若 做 计数 用 后继列表 大于 0：
            s0索引 为 做 数流_CFG_块索引 用 块索引表、后继列表 的 第 0 项。
            若 s0索引 大于或等于 0：
                新OUT 为 做 数流_集_复制 用 做 数流_块_IN 用 块列表 的 第 s0索引 项。
            完。
            可变 i 为 1。
            只要 i 小于 做 计数 用 后继列表：
                s索引 为 做 数流_CFG_块索引 用 块索引表、后继列表 的 第 i 项。
                若 s索引 大于或等于 0：
                    新OUT 为 做 数流_meet 用 新OUT、做 数流_块_IN 用 块列表 的 第 s索引 项、meet操作。
                完。
                i 为 i 加 1。
            完。
        完。
        out减kill 为 做 数流_集_差 用 新OUT、做 数流_块_KILL 用 块。
        新IN结果 为 做 数流_集_并 用 做 数流_块_GEN 用 块、out减kill。
        新IN 为 新IN结果 的 第 0 项。
        若 做 数流_集_相等 用 新IN、做 数流_块_IN 用 块 等于 假：
            块 的 第 4 项 为 新IN。
            块 的 第 5 项 为 新OUT。
            前驱列表 为 做 数流_块_前驱 用 块。
            可变 i 为 0。
            只要 i 小于 做 计数 用 前驱列表：
                前驱索引 为 做 数流_CFG_块索引 用 块索引表、前驱列表 的 第 i 项。
                若 前驱索引 大于或等于 0：
                    做 数流_工作列表_加入 用 工作列表、在列、前驱索引。
                完。
                i 为 i 加 1。
            完。
        否则：
            块 的 第 5 项 为 新OUT。
        完。
        迭代数 为 迭代数 加 1。
    完。
    返还 迭代数。
完。

活跃变量_分析 接受 cfg：
    返还 做 活跃变量_精准分析 用 cfg。
完。

活跃变量_是活跃 接受 块、变量：
    OUT 为 做 数流_块_OUT 用 块。
    可变 i 为 0。
    只要 i 小于 做 计数 用 OUT：
        若 OUT 的 第 i 项 等于 变量：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

到达定义_分析 接受 cfg：
    返还 做 到达定义_精准分析 用 cfg。
完。

精准Worklist迭代 接受 cfg、方向、meet操作：
    若 方向 等于 数流_后向：
        返还 做 数流_后向求解 用 cfg、meet操作。
    完。
    返还 做 数流_前向求解 用 cfg、meet操作。
完。

精准Worklist迭代_从入口 接受 cfg、方向、meet操作、入口块ID：
    若 方向 等于 数流_后向：
        返还 做 数流_后向求解_从入口 用 cfg、meet操作、入口块ID。
    完。
    返还 做 数流_前向求解_从入口 用 cfg、meet操作、入口块ID。
完。

活跃变量_精准分析 接受 cfg：
    返还 做 精准Worklist迭代 用 cfg、数流_后向、数流_并集。
完。

活跃变量_精准分析_从入口 接受 cfg、入口块ID：
    返还 做 精准Worklist迭代_从入口 用 cfg、数流_后向、数流_并集、入口块ID。
完。

到达定义_精准分析 接受 cfg：
    返还 做 精准Worklist迭代 用 cfg、数流_前向、数流_并集。
完。

到达定义_精准分析_从入口 接受 cfg、入口块ID：
    返还 做 精准Worklist迭代_从入口 用 cfg、数流_前向、数流_并集、入口块ID。
完。

本章 收束。
