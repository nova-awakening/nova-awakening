《编译流水线》

引用《..·..·IR·优化器·优化管线》。

引用《..·..·编译器·标记常量》。

引用《..·..·系统扩展·P2-改进·生命周期》。

引用《..·..·平台·目标选择》。

引用《..·..·标准库·基础》。

引用《..·..·标准库·转换器·转换验证》。

引用《内存安全分析》。

引用《别名分析》。

引用《向量化分析》。

引用《常量传播》。

引用《循环不变量提升》。

引用《循环分析》。

引用《指令调度》。

引用《支配树》。

引用《数据流分析》。

引用《热度分析》。

引用《过程间优化》。

引用《过程间分析》。

引用《值域分析》。

引用《并发分析》。

引用《指针分析》。

引用《逃逸分析增强》。

引用《DWARF调试信息》。

引用《..·智能优化·智能寄存器分配》。

引用《..·智能优化·程序行为预测》。

引用《..·前端入口》。

引用《..·宏系统·宏系统》。

引用《..·智能优化·自适应优化器》。

引用《..·语义·类型推断》。

引用《类型推断》。

引用《..·跨语言·外源合同·外源项目合同》。

引用《..·跨语言·工程摄取·工程探测器》。

引用《..·跨语言·统一语义·统一语义图》。

引用《..·验证·形式化验证引擎》。

引用《..·..·运行时·原语桩》。

引用《..·..·系统扩展·P2-改进·观测与诊断》。

流水线_调试 为 0。

流水线_优化O1 为 1。

流水线_优化O2 为 2。

流水线_优化O3 为 3。

流水线_优化Os 为 4。

可变 _流水线_当前配置 为 0。

可变 _流水线_目标平台 为 1。

可变 _流水线_宏注册表 为 空。

可变 _流水线_宏模板 为 一组：（空）。

可变 _流水线_阶段 为 一组：（空）。

流水线_设置配置 接受 配置：
    _流水线_当前配置 为 配置。
完。

流水线_设置目标 接受 目标：
    _流水线_目标平台 为 目标。
完。

流水线_当前配置 接受 零参：
    返还 _流水线_当前配置。
完。

流水线状态_创建 接受 阶段名：
    返还 一组：阶段名、假、0、0、0。
完。

流水线状态_阶段 接受 s：
    返还 s 的 第 0 项。
完。

流水线状态_完成 接受 s：
    返还 s 的 第 1 项。
完。

流水线状态_标记完成 接受 s：
    s 的 第 1 项 为 真。
完。

流水线状态_增项目 接受 s：
    s 的 第 2 项 为 s 的 第 2 项 加 1。
完。

流水线状态_增警告 接受 s：
    s 的 第 3 项 为 s 的 第 3 项 加 1。
完。

流水线状态_增优化 接受 s：
    s 的 第 4 项 为 s 的 第 4 项 加 1。
完。

编译上下文_创建 接受 源文件：
    返还 一组：源文件、空、空、空、空、空、空、空、一组：（空）、一组：（空）、0、做 字典、0、空、空、做 字典、做 字典。
完。

上下文_源文件 接受 ctx：
    返还 ctx 的 第 0 项。
完。

上下文_AST 接受 ctx：
    返还 ctx 的 第 1 项。
完。

上下文_语义 接受 ctx：
    返还 ctx 的 第 2 项。
完。

上下文_CFG 接受 ctx：
    返还 ctx 的 第 3 项。
完。

上下文_支配树 接受 ctx：
    返还 ctx 的 第 4 项。
完。

上下文_循环 接受 ctx：
    返还 ctx 的 第 5 项。
完。

上下文_别名 接受 ctx：
    返还 ctx 的 第 6 项。
完。

上下文_热度 接受 ctx：
    返还 ctx 的 第 7 项。
完。

上下文_诊断 接受 ctx：
    返还 ctx 的 第 8 项。
完。

上下文_已完成阶段 接受 ctx：
    返还 ctx 的 第 9 项。
完。

上下文_设AST 接受 ctx、ast：
    ctx 的 第 1 项 为 ast。
完。

上下文_设语义 接受 ctx、s：
    ctx 的 第 2 项 为 s。
完。

上下文_设CFG 接受 ctx、控制流图：
    ctx 的 第 3 项 为 控制流图。
完。

上下文_设支配树 接受 ctx、dt：
    ctx 的 第 4 项 为 dt。
完。

上下文_设循环 接受 ctx、loops：
    ctx 的 第 5 项 为 loops。
完。

上下文_设别名 接受 ctx、aa：
    ctx 的 第 6 项 为 aa。
完。

上下文_设热度 接受 ctx、h：
    ctx 的 第 7 项 为 h。
完。

上下文_添加诊断 接受 ctx、d：
    做 追加 用 ctx 的 第 8 项、d。
完。

上下文_标记阶段完成 接受 ctx、阶段名：
    做 追加 用 ctx 的 第 9 项、阶段名。
完。

上下文_增优化次数 接受 ctx：
    ctx 的 第 10 项 为 ctx 的 第 10 项 加 1。
完。

上下文_性能数据 接受 ctx：
    返还 ctx 的 第 11 项。
完。

上下文_设代码大小 接受 ctx、n：
    ctx 的 第 12 项 为 n。
完。

上下文_函数列 接受 ctx：
    返还 ctx 的 第 13 项。
完。

上下文_主体 接受 ctx：
    返还 ctx 的 第 14 项。
完。

上下文_函数结构分析 接受 ctx：
    返还 ctx 的 第 15 项。
完。

上下文_函数数据流分析 接受 ctx：
    返还 ctx 的 第 16 项。
完。

上下文_设函数列 接受 ctx、fl：
    ctx 的 第 13 项 为 fl。
完。

上下文_设主体 接受 ctx、body：
    ctx 的 第 14 项 为 body。
完。

上下文_清空函数结构分析 接受 ctx：
    ctx 的 第 15 项 为 做 字典。
完。

上下文_记录函数结构分析 接受 ctx、入口块ID、函数名、支配树、循环：
    做 字典设 用 ctx 的 第 15 项、做 转字符串 用 入口块ID、一组：函数名、入口块ID、支配树、循环。
完。

上下文_清空函数数据流分析 接受 ctx：
    ctx 的 第 16 项 为 做 字典。
完。

上下文_记录函数数据流分析 接受 ctx、入口块ID、函数名、活跃块表、到达定义块表：
    做 字典设 用 ctx 的 第 16 项、做 转字符串 用 入口块ID、一组：函数名、入口块ID、活跃块表、到达定义块表。
完。

_流水线_快照_数据流块表 接受 cfg、入口块ID：
    块列表 为 做 数流_CFG_块列表 用 cfg。
    可达索引列表 为 做 数流_CFG_指定入口可达索引列表 用 cfg、入口块ID。
    可变 结果 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 可达索引列表：
        _块 为 块列表 的 第 可达索引列表 的 第 i 项 项。
        _块ID 为 做 数流_块_ID 用 _块。
        做 字典设 用 结果、做 转字符串 用 _块ID、一组：_块ID、做 数流_集_复制 用 做 数流_块_IN 用 _块、做 数流_集_复制 用 做 数流_块_OUT 用 _块。
        i 为 i 加 1。
    完。
    返还 结果。
完。

_流水线_列表全在索引表 接受 元素列表、索引表：
    若 元素列表 等于 空：
        返还 真。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 元素列表：
        若 做 数流_集_索引含 用 索引表、元素列表 的 第 i 项 等于 假：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

_流水线_分析表转函数名字典 接受 分析表：
    可变 结果 为 做 字典。
    若 分析表 等于 空：
        返还 结果。
    完。
    键列表 为 做 字典_键排序 用 做 字典键 用 分析表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        条目 为 做 字典取 用 分析表、键列表 的 第 i 项。
        若 做 是列表 用 条目 并且 做 计数 用 条目 大于或等于 1：
            做 字典设 用 结果、条目 的 第 0 项、条目。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

_流水线_最大循环深度 接受 循环列表：
    若 循环列表 等于 空：
        返还 0。
    完。
    可变 最大深度 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 循环列表：
        循环 为 循环列表 的 第 i 项。
        若 做 是列表 用 循环 并且 做 计数 用 循环 大于或等于 6 并且 做 循环_深度 用 循环 大于 最大深度：
            最大深度 为 做 循环_深度 用 循环。
        完。
        i 为 i 加 1。
    完。
    返还 最大深度。
完。

_流水线_块表最大集合大小 接受 块表、集合索引：
    若 块表 等于 空：
        返还 0。
    完。
    可变 最大大小 为 0。
    键列表 为 做 字典_键排序 用 做 字典键 用 块表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        条目 为 做 字典取 用 块表、键列表 的 第 i 项。
        若 做 是列表 用 条目 并且 做 计数 用 条目 大于 集合索引 并且 做 是列表 用 条目 的 第 集合索引 项 并且 做 计数 用 条目 的 第 集合索引 项 大于 最大大小：
            最大大小 为 做 计数 用 条目 的 第 集合索引 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大大小。
完。

_流水线_热度表级别计数 接受 热度表、最低级别：
    若 热度表 等于 空：
        返还 0。
    完。
    可变 数量 为 0。
    键列表 为 做 字典_键排序 用 做 字典键 用 热度表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        热度值 为 做 字典取 用 热度表、键列表 的 第 i 项。
        若 热度值 不等于 空 并且 做 热度_级别 用 热度值 大于或等于 最低级别：
            数量 为 数量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数量。
完。

_流水线_循环语句列表 接受 控制流图、循环：
    可变 结果 为 一组：（空）。
    若 控制流图 等于 空 或者 循环 等于 空：
        返还 结果。
    完。
    _头块ID 为 做 循环_头块 用 循环。
    _头块 为 做 数流_CFG_查找块 用 控制流图、_头块ID。
    若 _头块 不等于 空：
        _头块语句 为 做 数流_块_语句 用 _头块。
        若 做 是列表 用 _头块语句：
            可变 _hsi 为 0。
            只要 _hsi 小于 做 计数 用 _头块语句：
                做 追加 用 结果、_头块语句 的 第 _hsi 项。
                _hsi 为 _hsi 加 1。
            完。
        完。
    完。
    _体块ID列表 为 做 循环_体 用 循环。
    若 做 是列表 用 _体块ID列表：
        可变 _bi 为 0。
        只要 _bi 小于 做 计数 用 _体块ID列表：
            若 _体块ID列表 的 第 _bi 项 不等于 _头块ID：
                _体块 为 做 数流_CFG_查找块 用 控制流图、_体块ID列表 的 第 _bi 项。
                若 _体块 不等于 空：
                    _体块语句 为 做 数流_块_语句 用 _体块。
                    若 做 是列表 用 _体块语句：
                        可变 _bsi 为 0。
                        只要 _bsi 小于 做 计数 用 _体块语句：
                            做 追加 用 结果、_体块语句 的 第 _bsi 项。
                            _bsi 为 _bsi 加 1。
                        完。
                    完。
                完。
            完。
            _bi 为 _bi 加 1。
        完。
    完。
    返还 结果。
完。

_流水线_语句列表局部使用全在索引表 接受 语句列表、索引表：
    若 语句列表 等于 空：
        返还 假。
    完。
    可变 有语句 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列表：
        _语句 为 语句列表 的 第 i 项。
        若 做 是列表 用 _语句 并且 做 计数 用 _语句 大于 0：
            有语句 为 真。
            若 做 _流水线_列表全在索引表 用 做 数流_语句_局部使用变量 用 _语句、索引表 等于 假：
                返还 假。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 有语句。
完。

流水线_阶段1_前端 接受 ctx：
    状态 为 做 流水线状态_创建 用 「前端分析」。
    若 做 上下文_源文件 用 ctx 不等于 空：
        源码 为 做 读文件 用 做 上下文_源文件 用 ctx。
        若 源码 不等于 空：
            做 前端_词法分析 用 源码。
            做 上下文_设AST 用 ctx、程序。
            若 做 是列表 用 程序 并且 做 计数 用 程序 大于或等于 2：
                做 上下文_设函数列 用 ctx、程序 的 第 0 项。
                做 上下文_设主体 用 ctx、程序 的 第 1 项。
            完。
            做 流水线状态_增项目 用 状态。
        完。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「前端分析」。
    返还 状态。
完。

流水线_阶段1_5_宏展开 接受 ctx：
    状态 为 做 流水线状态_创建 用 「宏展开」。
    若 做 上下文_AST 用 ctx 不等于 空：
        _宏_展开数 为 0。
        _宏_当前递归 为 0。
        reg 为 _流水线_宏注册表。
        模板列表 为 _流水线_宏模板。
        若 reg 不等于 空 并且 做 宏注册表_大小 用 reg 大于 0：
            函数列 为 做 上下文_函数列 用 ctx。
            若 函数列 不等于 空 并且 做 是列表 用 函数列：
                可变 fi 为 0。
                只要 fi 小于 做 计数 用 函数列：
                    若 做 是列表 用 函数列 的 第 fi 项 并且 做 计数 用 函数列 的 第 fi 项 大于或等于 4 并且 做 是列表 用 函数列 的 第 fi 项 的 第 3 项：
                        做 _宏_展开语句列表 用 reg、模板列表、函数列 的 第 fi 项 的 第 3 项。
                    完。
                    fi 为 fi 加 1。
                完。
            完。
            主体 为 做 上下文_主体 用 ctx。
            若 主体 不等于 空 并且 做 是列表 用 主体：
                做 _宏_展开语句列表 用 reg、模板列表、主体。
            完。
        完。
        做 流水线状态_增项目 用 状态。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「宏展开」。
    返还 状态。
完。

_宏_展开语句列表 接受 reg、模板列表、语句列表：
    可变 si 为 0。
    只要 si 小于 做 计数 用 语句列表：
        语句 为 语句列表 的 第 si 项。
        若 做 是列表 用 语句 并且 做 计数 用 语句 大于或等于 3 并且 语句 的 第 0 项 等于 节点_变量声明：
            若 做 宏_进入展开：
                结果 为 做 宏_展开一层 用 reg、语句 的 第 1 项、语句 的 第 2 项、模板列表。
                若 结果 的 第 0 项 等于 真：
                    做 宏_记录展开。
                    _宏_展开数 为 _宏_展开数 加 1。
                完。
                做 宏_离开展开。
            完。
        完。
        若 做 是列表 用 语句 并且 做 计数 用 语句 大于或等于 3：
            若 语句 的 第 0 项 等于 标记_否则 并且 做 是列表 用 语句 的 第 2 项：
                做 _宏_展开语句列表 用 reg、模板列表、语句 的 第 2 项。
            完。
            若 语句 的 第 0 项 等于 标记_否则 并且 做 计数 用 语句 大于或等于 4 并且 做 是列表 用 语句 的 第 3 项：
                做 _宏_展开语句列表 用 reg、模板列表、语句 的 第 3 项。
            完。
            若 语句 的 第 0 项 等于 标记_当 并且 做 是列表 用 语句 的 第 2 项：
                做 _宏_展开语句列表 用 reg、模板列表、语句 的 第 2 项。
            完。
        完。
        si 为 si 加 1。
    完。
完。

可变 _语义_推断总数 为 0。

可变 _语义_AI违规数 为 0。

可变 _语义_动态逃逸数 为 0。

可变 _语义_函数类型数 为 0。

可变 _语义_逃逸详情 为 一组：（空）。

可变 _语义_逃逸阈值 为 负 1。

可变 _语义_函数返回型表 为 空。

可变 _语义_函数定义表 为 空。

可变 _语义_函数推断中 为 空。

流水线_阶段1_8_语义检查 接受 ctx：
    状态 为 做 流水线状态_创建 用 「语义检查」。
    _语义_推断总数 为 0。
    _语义_AI违规数 为 0。
    _语义_动态逃逸数 为 0。
    _语义_函数类型数 为 0。
    _语义_逃逸详情 为 一组：（空）。
    _语义_函数返回型表 为 做 字典。
    _语义_函数定义表 为 做 字典。
    _语义_函数推断中 为 做 字典。
    函数列 为 做 上下文_函数列 用 ctx。
    若 函数列 不等于 空 并且 做 是列表 用 函数列：
        可变 _fi定义 为 0。
        只要 _fi定义 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _fi定义 项 并且 做 计数 用 函数列 的 第 _fi定义 项 大于或等于 4：
                做 字典设 用 _语义_函数定义表、函数列 的 第 _fi定义 项 的 第 1 项、函数列 的 第 _fi定义 项 的 第 3 项。
            完。
            _fi定义 为 _fi定义 加 1。
        完。
        可变 _fi推断 为 0。
        只要 _fi推断 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _fi推断 项 并且 做 计数 用 函数列 的 第 _fi推断 项 大于或等于 4：
                做 _语义_推断函数返回类型 用 函数列 的 第 _fi推断 项 的 第 1 项。
            完。
            _fi推断 为 _fi推断 加 1。
        完。
        可变 fi 为 0。
        只要 fi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 fi 项 并且 做 计数 用 函数列 的 第 fi 项 大于或等于 4：
                _语义_函数类型数 为 _语义_函数类型数 加 1。
            完。
            fi 为 fi 加 1。
        完。
        做 流水线状态_增项目 用 状态。
    完。
    主体 为 做 上下文_主体 用 ctx。
    若 主体 不等于 空 并且 做 是列表 用 主体：
        可变 si 为 0。
        只要 si 小于 做 计数 用 主体：
            做 _语义_流水线_推断语句 用 主体 的 第 si 项、「__主程序__」。
            si 为 si 加 1。
        完。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「语义检查」。
    返还 状态。
完。

_语义_合并候选类型 接受 当前、候选：
    若 候选 等于 型_未知：
        返还 当前。
    完。
    若 当前 等于 型_未知：
        返还 候选。
    完。
    返还 做 类型推断_合并类型 用 当前、候选。
完。

_语义_是返回语句 接受 头：
    若 头 等于 节点_成员：
        返还 真。
    完。
    若 头 等于 节点_调用：
        返还 真。
    完。
    若 头 等于 标记_如果：
        返还 真。
    完。
    返还 假。
完。

_语义_是条件语句 接受 头：
    若 头 等于 节点_赋值：
        返还 真。
    完。
    若 头 等于 标记_定义：
        返还 真。
    完。
    若 头 等于 标记_除：
        返还 真。
    完。
    返还 假。
完。

_语义_是循环语句 接受 头：
    若 头 等于 节点_索引：
        返还 真。
    完。
    若 头 等于 标记_建：
        返还 真。
    完。
    若 头 等于 标记_余：
        返还 真。
    完。
    返还 假。
完。

_语义_是调用表达式 接受 头：
    若 头 等于 节点_列表：
        返还 真。
    完。
    若 头 等于 标记_标识符：
        返还 真。
    完。
    若 头 等于 节点_调用：
        返还 真。
    完。
    返还 假。
完。

_语义_提取调用名 接受 表达式：
    若 做 是列表 用 表达式 等于 假 或者 做 计数 用 表达式 小于 2：
        返还 空。
    完。
    若 做 _语义_是调用表达式 用 表达式 的 第 0 项 等于 假：
        返还 空。
    完。
    若 做 是字符串 用 表达式 的 第 1 项：
        返还 表达式 的 第 1 项。
    完。
    返还 空。
完。

_语义_推断语句列表返回类型 接受 语句列：
    若 做 是列表 用 语句列 等于 假：
        返还 型_未知。
    完。
    可变 结果 为 型_未知。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列：
        结果 为 做 _语义_合并候选类型 用 结果、做 _语义_推断语句返回类型 用 语句列 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

_语义_推断语句返回类型 接受 语句：
    若 做 是列表 用 语句 等于 假 或者 做 计数 用 语句 小于 1：
        返还 型_未知。
    完。
    头 为 语句 的 第 0 项。
    若 做 _语义_是返回语句 用 头：
        若 做 计数 用 语句 大于或等于 2：
            返还 做 _语义_推断表达式类型 用 语句 的 第 1 项。
        完。
        返还 型_无値。
    完。
    可变 结果 为 型_未知。
    若 做 _语义_是条件语句 用 头：
        若 做 计数 用 语句 大于或等于 3 并且 做 是列表 用 语句 的 第 2 项：
            结果 为 做 _语义_合并候选类型 用 结果、做 _语义_推断语句列表返回类型 用 语句 的 第 2 项。
        完。
        若 做 计数 用 语句 大于或等于 4 并且 做 是列表 用 语句 的 第 3 项：
            结果 为 做 _语义_合并候选类型 用 结果、做 _语义_推断语句列表返回类型 用 语句 的 第 3 项。
        完。
        返还 结果。
    完。
    若 做 _语义_是循环语句 用 头：
        若 做 计数 用 语句 大于或等于 3 并且 做 是列表 用 语句 的 第 2 项：
            返还 做 _语义_推断语句列表返回类型 用 语句 的 第 2 项。
        完。
    完。
    返还 型_未知。
完。

_语义_推断函数返回类型 接受 函数名：
    若 _语义_函数返回型表 不等于 空 并且 做 字典有 用 _语义_函数返回型表、函数名：
        返还 做 字典取 用 _语义_函数返回型表、函数名。
    完。
    若 _语义_函数定义表 等于 空 或者 做 字典有 用 _语义_函数定义表、函数名 等于 假：
        返还 型_任意。
    完。
    若 _语义_函数推断中 不等于 空 并且 做 字典有 用 _语义_函数推断中、函数名：
        返还 型_任意。
    完。
    做 字典设 用 _语义_函数推断中、函数名、真。
    结果 为 做 _语义_推断语句列表返回类型 用 做 字典取 用 _语义_函数定义表、函数名。
    _语义_函数推断中 为 做 字典删 用 _语义_函数推断中、函数名。
    若 结果 等于 型_未知：
        结果 为 型_无値。
    完。
    做 字典设 用 _语义_函数返回型表、函数名、结果。
    返还 结果。
完。

_语义_流水线_推断语句 接受 语句、函数名：
    若 做 是列表 用 语句 等于 假 或者 做 计数 用 语句 小于 1：
        返还 空。
    完。
    头 为 语句 的 第 0 项。
    _语义_推断总数 为 _语义_推断总数 加 1。
    做 类型推断_记录。
    若 (头 等于 节点_二元 或者 头 等于 标记_可变) 并且 做 计数 用 语句 大于或等于 3：
        _右类型 为 做 _语义_推断表达式类型 用 语句 的 第 2 项。
        若 _右类型 等于 型_任意：
            _语义_动态逃逸数 为 _语义_动态逃逸数 加 1。
            若 做 计数 用 _语义_逃逸详情 小于 100：
                做 追加 用 _语义_逃逸详情、一组：函数名、「赋值」、_语义_推断总数。
            完。
        完。
        返还 空。
    完。
    若 做 _语义_是返回语句 用 头 并且 做 计数 用 语句 大于或等于 2：
        _ret类型 为 做 _语义_推断表达式类型 用 语句 的 第 1 项。
        若 _ret类型 等于 型_任意：
            _语义_动态逃逸数 为 _语义_动态逃逸数 加 1。
            若 做 计数 用 _语义_逃逸详情 小于 100：
                做 追加 用 _语义_逃逸详情、一组：函数名、「返回」、_语义_推断总数。
            完。
        完。
        返还 空。
    完。
    若 做 _语义_是条件语句 用 头 并且 做 计数 用 语句 大于或等于 3：
        若 做 是列表 用 语句 的 第 2 项：
            可变 bi 为 0。
            只要 bi 小于 做 计数 用 语句 的 第 2 项：
                做 _语义_流水线_推断语句 用 语句 的 第 2 项 的 第 bi 项、函数名。
                bi 为 bi 加 1。
            完。
        完。
        若 做 计数 用 语句 大于或等于 4 并且 做 是列表 用 语句 的 第 3 项：
            可变 bi2 为 0。
            只要 bi2 小于 做 计数 用 语句 的 第 3 项：
                做 _语义_流水线_推断语句 用 语句 的 第 3 项 的 第 bi2 项、函数名。
                bi2 为 bi2 加 1。
            完。
        完。
        返还 空。
    完。
    若 做 _语义_是循环语句 用 头 并且 做 计数 用 语句 大于或等于 3：
        若 做 是列表 用 语句 的 第 2 项：
            可变 li 为 0。
            只要 li 小于 做 计数 用 语句 的 第 2 项：
                做 _语义_流水线_推断语句 用 语句 的 第 2 项 的 第 li 项、函数名。
                li 为 li 加 1。
            完。
        完。
        返还 空。
    完。
    返还 空。
完。

_语义_推断表达式类型 接受 表达式：
    若 做 是列表 用 表达式 等于 假：
        若 做 是整数 用 表达式：
            返还 型_整数。
        完。
        若 表达式 等于 空：
            返还 型_空値。
        完。
        返还 型_字符串。
    完。
    若 做 计数 用 表达式 小于 1：
        返还 型_未知。
    完。
    头 为 表达式 的 第 0 项。
    若 头 等于 标记_数字 或者 头 等于 1：
        返还 型_整数。
    完。
    若 头 等于 2：
        返还 型_浮点。
    完。
    若 头 等于 标记_字串 或者 头 等于 3：
        返还 型_字符串。
    完。
    若 头 等于 5：
        返还 型_布尔。
    完。
    若 头 等于 0：
        返还 型_空値。
    完。
    若 (头 等于 节点_字典 或者 头 等于 10) 并且 做 计数 用 表达式 大于或等于 4：
        _l 为 做 _语义_推断表达式类型 用 表达式 的 第 2 项。
        _r 为 做 _语义_推断表达式类型 用 表达式 的 第 3 项。
        返还 做 类型推断_二元结果类型 用 表达式 的 第 1 项、_l、_r。
    完。
    若 做 _语义_是调用表达式 用 头：
        _被调 为 做 _语义_提取调用名 用 表达式。
        若 _被调 不等于 空：
            返还 做 _语义_推断函数返回类型 用 _被调。
        完。
        返还 型_任意。
    完。
    若 头 等于 标记_定义：
        返还 型_列表。
    完。
    若 头 等于 标记_可变：
        返还 型_字典。
    完。
    若 头 等于 标记_名称 或者 头 等于 4：
        返还 型_未知。
    完。
    返还 型_未知。
完。

流水线_阶段2_CFG构建 接受 ctx：
    状态 为 做 流水线状态_创建 用 「CFG构建」。
    若 做 上下文_AST 用 ctx 不等于 空：
        函数列 为 做 上下文_函数列 用 ctx。
        主体 为 做 上下文_主体 用 ctx。
        若 (函数列 等于 空 或者 主体 等于 空) 并且 做 是列表 用 做 上下文_AST 用 ctx 并且 做 计数 用 做 上下文_AST 用 ctx 大于或等于 2：
            函数列 为 做 上下文_AST 用 ctx 的 第 0 项。
            主体 为 做 上下文_AST 用 ctx 的 第 1 项。
            做 上下文_设函数列 用 ctx、函数列。
            做 上下文_设主体 用 ctx、主体。
        完。
        cfg 为 做 数流_CFG_从函数列与主体构建 用 函数列、主体。
        做 上下文_设CFG 用 ctx、cfg。
        做 流水线状态_增项目 用 状态。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「CFG构建」。
    返还 状态。
完。

流水线_阶段3_结构分析 接受 ctx：
    状态 为 做 流水线状态_创建 用 「结构分析」。
    控制流图 为 做 上下文_CFG 用 ctx。
    做 上下文_清空函数结构分析 用 ctx。
    若 控制流图 不等于 空 并且 做 计数 用 做 数流_CFG_块列表 用 控制流图 大于 0：
        可达子图 为 做 数流_CFG_入口可达子图 用 控制流图。
        dt 为 做 支配树_构建 用 可达子图、做 数流_CFG_入口 用 可达子图。
        做 上下文_设支配树 用 ctx、dt。
        loops 为 做 循环分析_分析 用 可达子图、dt。
        做 上下文_设循环 用 ctx、loops。
        _块列表 为 做 数流_CFG_块列表 用 控制流图。
        可变 _fbi 为 0。
        只要 _fbi 小于 做 计数 用 _块列表：
            _块 为 _块列表 的 第 _fbi 项。
            _语句列 为 做 数流_块_语句 用 _块。
            若 做 数流_块_ID 用 _块 不等于 做 数流_CFG_入口 用 控制流图 并且 做 是列表 用 _语句列 并且 做 计数 用 _语句列 大于 0：
                _首语句 为 _语句列 的 第 0 项。
                若 做 是列表 用 _首语句 并且 做 计数 用 _首语句 大于 0 并且 _首语句 的 第 0 项 等于 数流_AST_函数：
                    _函数支配树 为 做 支配树_构建 用 控制流图、做 数流_块_ID 用 _块。
                    _函数循环 为 做 循环分析_分析 用 控制流图、_函数支配树。
                    做 上下文_记录函数结构分析 用 ctx、做 数流_块_ID 用 _块、_首语句 的 第 1 项、_函数支配树、_函数循环。
                完。
            完。
            _fbi 为 _fbi 加 1。
        完。
        做 流水线状态_增项目 用 状态。
        做 流水线状态_增优化 用 状态。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「结构分析」。
    返还 状态。
完。

流水线_阶段4_数据流 接受 ctx：
    状态 为 做 流水线状态_创建 用 「数据流分析」。
    控制流图 为 做 上下文_CFG 用 ctx。
    做 上下文_清空函数数据流分析 用 ctx。
    若 控制流图 不等于 空：
        做 数流_CFG_重置分析 用 控制流图。
        做 活跃变量_精准分析 用 控制流图。
        做 数流_CFG_重置分析 用 控制流图。
        做 到达定义_精准分析 用 控制流图。
        _块列表 为 做 数流_CFG_块列表 用 控制流图。
        可变 _fbi 为 0。
        只要 _fbi 小于 做 计数 用 _块列表：
            _块 为 _块列表 的 第 _fbi 项。
            _语句列 为 做 数流_块_语句 用 _块。
            若 做 数流_块_ID 用 _块 不等于 做 数流_CFG_入口 用 控制流图 并且 做 是列表 用 _语句列 并且 做 计数 用 _语句列 大于 0：
                _首语句 为 _语句列 的 第 0 项。
                若 做 是列表 用 _首语句 并且 做 计数 用 _首语句 大于 0 并且 _首语句 的 第 0 项 等于 数流_AST_函数：
                    做 数流_CFG_重置分析 用 控制流图。
                    做 活跃变量_精准分析_从入口 用 控制流图、做 数流_块_ID 用 _块。
                    _函数活跃块表 为 做 _流水线_快照_数据流块表 用 控制流图、做 数流_块_ID 用 _块。
                    做 数流_CFG_重置分析 用 控制流图。
                    做 到达定义_精准分析_从入口 用 控制流图、做 数流_块_ID 用 _块。
                    _函数到达定义块表 为 做 _流水线_快照_数据流块表 用 控制流图、做 数流_块_ID 用 _块。
                    做 上下文_记录函数数据流分析 用 ctx、做 数流_块_ID 用 _块、_首语句 的 第 1 项、_函数活跃块表、_函数到达定义块表。
                完。
            完。
            _fbi 为 _fbi 加 1。
        完。
        做 流水线状态_增优化 用 状态。
    完。
    函数列 为 做 上下文_函数列 用 ctx。
    若 函数列 不等于 空：
        _热度表 为 做 热度_分析函数列表 用 函数列。
        _函数结构名表 为 做 _流水线_分析表转函数名字典 用 做 上下文_函数结构分析 用 ctx。
        _函数数据流名表 为 做 _流水线_分析表转函数名字典 用 做 上下文_函数数据流分析 用 ctx。
        可变 _热度分析函数数 为 0。
        可变 _热度结构命中数 为 0。
        可变 _热度数据流命中数 为 0。
        可变 _热度热函数数 为 0。
        可变 _热度极热函数数 为 0。
        可变 _hfi 为 0。
        只要 _hfi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _hfi 项 并且 做 计数 用 函数列 的 第 _hfi 项 大于或等于 4：
                _h名 为 函数列 的 第 _hfi 项 的 第 1 项。
                _h体 为 函数列 的 第 _hfi 项 的 第 3 项。
                可变 _h值 为 做 字典取 用 _热度表、_h名。
                _结构项 为 做 字典取 用 _函数结构名表、_h名。
                _数据项 为 做 字典取 用 _函数数据流名表、_h名。
                _热度分析函数数 为 _热度分析函数数 加 1。
                若 _h值 等于 空：
                    _h值 为 权重_顶层。
                完。
                若 做 是列表 用 _h体 并且 做 计数 用 _h体 大于 0：
                    _h值 为 做 热度_分析语句列表 用 _h体、0。
                完。
                若 做 是列表 用 _结构项 并且 做 计数 用 _结构项 大于或等于 4：
                    _最大循环深度 为 做 _流水线_最大循环深度 用 _结构项 的 第 3 项。
                    _热度结构命中数 为 _热度结构命中数 加 1。
                    若 _最大循环深度 大于 0：
                        _h值 为 _h值 加 做 热度_从循环深度 用 _最大循环深度。
                    完。
                完。
                若 做 是列表 用 _数据项 并且 做 计数 用 _数据项 大于或等于 4：
                    _最大活跃OUT 为 做 _流水线_块表最大集合大小 用 _数据项 的 第 2 项、2。
                    _最大到达IN 为 做 _流水线_块表最大集合大小 用 _数据项 的 第 3 项、1。
                    _热度数据流命中数 为 _热度数据流命中数 加 1。
                    _h值 为 _h值 加 _最大活跃OUT 加 _最大到达IN。
                完。
                _h值 为 做 热度_混合估算 用 _h名、_h值。
                若 _h值 大于 1000000：
                    _h值 为 1000000。
                完。
                _h级别 为 做 热度_级别 用 _h值。
                若 _h级别 大于或等于 热度_热：
                    _热度热函数数 为 _热度热函数数 加 1。
                完。
                若 _h级别 等于 热度_极热：
                    _热度极热函数数 为 _热度极热函数数 加 1。
                完。
                做 字典设 用 _热度表、_h名、_h值。
            完。
            _hfi 为 _hfi 加 1。
        完。
        做 上下文_设热度 用 ctx、_热度表。
        做 字典设 用 做 上下文_性能数据 用 ctx、「阶段4_热度_分析函数数」、做 转字符串 用 _热度分析函数数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「阶段4_热度_结构命中数」、做 转字符串 用 _热度结构命中数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「阶段4_热度_数据流命中数」、做 转字符串 用 _热度数据流命中数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「阶段4_热度_热函数数」、做 转字符串 用 _热度热函数数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「阶段4_热度_极热函数数」、做 转字符串 用 _热度极热函数数。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「数据流分析」。
    返还 状态。
完。

流水线_阶段4_5_借用分析 接受 ctx：
    状态 为 做 流水线状态_创建 用 「借用/生命周期」。
    做 借用_进入作用域。
    做 约束_检查一致性。
    做 借用_退出作用域。
    做 流水线状态_增项目 用 状态。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「借用/生命周期」。
    返还 状态。
完。

流水线_阶段5_别名分析 接受 ctx：
    状态 为 做 流水线状态_创建 用 「别名分析」。
    做 AA_重置指针集。
    aa_ctx 为 做 BCE_上下文_创建。
    函数列 为 做 上下文_函数列 用 ctx。
    若 函数列 不等于 空 并且 做 是列表 用 函数列：
        可变 _afi 为 0。
        只要 _afi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _afi 项 并且 做 计数 用 函数列 的 第 _afi 项 大于或等于 4：
                _a体 为 函数列 的 第 _afi 项 的 第 3 项。
                若 做 是列表 用 _a体：
                    可变 _ai 为 0。
                    只要 _ai 小于 做 计数 用 _a体：
                        若 做 是列表 用 _a体 的 第 _ai 项 并且 做 计数 用 _a体 的 第 _ai 项 大于或等于 3：
                            若 _a体 的 第 _ai 项 的 第 0 项 等于 标记_定义：
                                _av名 为 _a体 的 第 _ai 项 的 第 1 项。
                                _av表 为 _a体 的 第 _ai 项 的 第 2 项。
                                若 做 是列表 用 _av表 并且 做 计数 用 _av表 大于或等于 2 并且 _av表 的 第 0 项 等于 标记_名称：
                                    做 AA_记录指向 用 _av名、_av表 的 第 1 项。
                                完。
                                做 流水线状态_增项目 用 状态。
                            完。
                            若 _a体 的 第 _ai 项 的 第 0 项 等于 标记_可变：
                                _av名2 为 _a体 的 第 _ai 项 的 第 1 项。
                                _av表2 为 _a体 的 第 _ai 项 的 第 2 项。
                                若 做 是列表 用 _av表2 并且 做 计数 用 _av表2 大于或等于 2 并且 _av表2 的 第 0 项 等于 标记_名称：
                                    做 AA_记录指向 用 _av名2、_av表2 的 第 1 项。
                                完。
                            完。
                        完。
                        _ai 为 _ai 加 1。
                    完。
                完。
            完。
            _afi 为 _afi 加 1。
        完。
    完。
    做 上下文_设别名 用 ctx、aa_ctx。
    做 PTA_重置。
    若 函数列 不等于 空 并且 做 是列表 用 函数列：
        可变 _ptafi 为 0。
        只要 _ptafi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _ptafi 项 并且 做 计数 用 函数列 的 第 _ptafi 项 大于或等于 4：
                _pta体 为 函数列 的 第 _ptafi 项 的 第 3 项。
                若 做 是列表 用 _pta体：
                    可变 _pi 为 0。
                    只要 _pi 小于 做 计数 用 _pta体：
                        若 做 是列表 用 _pta体 的 第 _pi 项 并且 做 计数 用 _pta体 的 第 _pi 项 大于或等于 3：
                            若 _pta体 的 第 _pi 项 的 第 0 项 等于 标记_定义 并且 做 是列表 用 _pta体 的 第 _pi 项 的 第 2 项 并且 做 计数 用 _pta体 的 第 _pi 项 的 第 2 项 大于或等于 2：
                                若 _pta体 的 第 _pi 项 的 第 2 项 的 第 0 项 等于 标记_名称：
                                    做 PTA_添加约束 用 0、_pta体 的 第 _pi 项 的 第 1 项、_pta体 的 第 _pi 项 的 第 2 项 的 第 1 项、空。
                                完。
                            完。
                        完。
                        _pi 为 _pi 加 1。
                    完。
                完。
            完。
            _ptafi 为 _ptafi 加 1。
        完。
    完。
    做 EE_重置。
    若 函数列 不等于 空 并且 做 是列表 用 函数列：
        可变 _eefi 为 0。
        只要 _eefi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _eefi 项 并且 做 计数 用 函数列 的 第 _eefi 项 大于或等于 4：
                做 EE_进入函数 用 函数列 的 第 _eefi 项 的 第 1 项。
            完。
            _eefi 为 _eefi 加 1。
        完。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「别名分析」。
    返还 状态。
完。

流水线_阶段6_优化 接受 ctx：
    可变 _si 为 0。
    状态 为 做 流水线状态_创建 用 「优化」。
    配置 为 做 流水线_当前配置。
    若 配置 等于 流水线_优化O1：
        做 优化管线_预设O1。
    否则若 配置 等于 流水线_优化O2：
        做 优化管线_预设O2。
    否则若 配置 等于 流水线_优化O3：
        做 优化管线_预设O3。
    否则若 配置 等于 流水线_优化Os：
        做 优化管线_预设Os。
    否则：
        做 优化管线_预设O0。
    完。
    函数列 为 做 上下文_函数列 用 ctx。
    主体列 为 做 上下文_主体 用 ctx。
    若 配置 大于或等于 流水线_优化O1 并且 函数列 不等于 空：
        做 打印文本 用 「    [优化] O1开始\n」。
        报告 为 做 优化管线_执行 用 函数列、主体列。
        可变 总优化 为 0。
        可变 ri 为 0。
        只要 ri 小于 做 计数 用 报告：
            总优化 为 总优化 加 报告 的 第 ri 项。
            ri 为 ri 加 1。
        完。
        状态 的 第 4 项 为 总优化。
        做 打印文本 用 「    [优化] O1结束\n」。
    完。
    若 配置 大于或等于 流水线_优化O2 并且 函数列 不等于 空：
        做 LICM_重置。
        可变 _cp总变化 为 0。
        可变 _licm总提升 为 0。
        可变 _licm分析函数数 为 0。
        可变 _licm循环数 为 0。
        可变 _licm候选数 为 0。
        可变 _licm活跃候选数 为 0。
        可变 _licm到达就绪候选数 为 0。
        可变 _cpfi 为 0。
        做 打印文本 用 「    [优化] O2常量传播开始\n」。
        只要 _cpfi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _cpfi 项 并且 做 计数 用 函数列 的 第 _cpfi 项 大于或等于 4：
                _cp函体 为 函数列 的 第 _cpfi 项 的 第 3 项。
                若 做 是列表 用 _cp函体 并且 做 计数 用 _cp函体 大于 0：
                    cp结果 为 做 CP_传播函数 用 _cp函体。
                    _cp总变化 为 _cp总变化 加 cp结果 的 第 2 项。
                完。
            完。
            _cpfi 为 _cpfi 加 1。
        完。
        若 _cp总变化 大于 0：
            做 CP_记录传播。
        完。
        做 打印文本 用 「    [优化] O2常量传播结束\n」。
        可变 _vrfi 为 0。
        做 打印文本 用 「    [优化] O2值域开始\n」。
        只要 _vrfi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _vrfi 项 并且 做 计数 用 函数列 的 第 _vrfi 项 大于或等于 4：
                _vr函体 为 函数列 的 第 _vrfi 项 的 第 3 项。
                若 做 是列表 用 _vr函体 并且 做 计数 用 _vr函体 大于 0：
                    做 VR_记录推断。
                完。
            完。
            _vrfi 为 _vrfi 加 1。
        完。
        做 打印文本 用 「    [优化] O2值域结束\n」。
        做 打印文本 用 「    [优化] O2循环开始\n」。
        控制流图 为 做 上下文_CFG 用 ctx。
        _函数结构表 为 做 上下文_函数结构分析 用 ctx。
        _函数数据流表 为 做 上下文_函数数据流分析 用 ctx。
        若 控制流图 不等于 空 并且 _函数结构表 不等于 空 并且 _函数数据流表 不等于 空：
            _分析键 为 做 字典_键排序 用 做 字典键 用 _函数结构表。
            可变 _aki 为 0。
            只要 _aki 小于 做 计数 用 _分析键：
                _键 为 _分析键 的 第 _aki 项。
                _结构项 为 做 字典取 用 _函数结构表、_键。
                _数据项 为 做 字典取 用 _函数数据流表、_键。
                若 做 是列表 用 _结构项 并且 做 计数 用 _结构项 大于或等于 4 并且 做 是列表 用 _数据项 并且 做 计数 用 _数据项 大于或等于 4：
                    _循环列表 为 _结构项 的 第 3 项。
                    _活跃块表 为 _数据项 的 第 2 项。
                    _到达定义块表 为 _数据项 的 第 3 项。
                    若 做 是列表 用 _循环列表 并且 做 计数 用 _循环列表 大于 0：
                        _licm分析函数数 为 _licm分析函数数 加 1。
                        可变 _li 为 0。
                        只要 _li 小于 做 计数 用 _循环列表：
                            _循环 为 _循环列表 的 第 _li 项。
                            _头块ID 为 做 循环_头块 用 _循环。
                            _头块 为 做 数流_CFG_查找块 用 控制流图、_头块ID。
                            做 LICM_记录循环。
                            _licm循环数 为 _licm循环数 加 1。
                            若 _头块 不等于 空：
                                _头块语句 为 做 数流_块_语句 用 _头块。
                                _不变量索引 为 做 LICM_扫描不变量 用 _头块语句、做 循环_体 用 _循环、控制流图。
                                _头块活跃 为 做 字典取 用 _活跃块表、做 转字符串 用 _头块ID。
                                _头块到达 为 做 字典取 用 _到达定义块表、做 转字符串 用 _头块ID。
                                _活跃OUT索引 为 做 字典。
                                _到达IN索引 为 做 字典。
                                若 做 是列表 用 _头块活跃 并且 做 计数 用 _头块活跃 大于或等于 3：
                                    _活跃OUT索引 为 做 数流_集_索引表 用 _头块活跃 的 第 2 项。
                                完。
                                若 做 是列表 用 _头块到达 并且 做 计数 用 _头块到达 大于或等于 2：
                                    _到达IN索引 为 做 数流_集_索引表 用 _头块到达 的 第 1 项。
                                完。
                                可变 _ii 为 0。
                                只要 _ii 小于 做 计数 用 _不变量索引：
                                    _idx 为 _不变量索引 的 第 _ii 项。
                                    若 _idx 大于或等于 0 并且 _idx 小于 做 计数 用 _头块语句：
                                        _候选语句 为 _头块语句 的 第 _idx 项。
                                        若 做 LICM_提升安全 用 _候选语句、_头块ID、控制流图：
                                            _licm候选数 为 _licm候选数 加 1。
                                            若 做 是列表 用 _候选语句 并且 做 计数 用 _候选语句 大于或等于 2 并且 做 数流_集_索引含 用 _活跃OUT索引、_候选语句 的 第 1 项：
                                                _licm活跃候选数 为 _licm活跃候选数 加 1。
                                            完。
                                            若 做 _流水线_列表全在索引表 用 做 数流_语句_局部使用变量 用 _候选语句、_到达IN索引：
                                                _licm到达就绪候选数 为 _licm到达就绪候选数 加 1。
                                            完。
                                        完。
                                    完。
                                    _ii 为 _ii 加 1。
                                完。
                            完。
                            _li 为 _li 加 1。
                        完。
                    完。
                完。
                _aki 为 _aki 加 1。
            完。
        完。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O2_LICM_分析函数数」、做 转字符串 用 _licm分析函数数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O2_LICM_循环数」、做 转字符串 用 _licm循环数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O2_LICM_候选数」、做 转字符串 用 _licm候选数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O2_LICM_活跃候选数」、做 转字符串 用 _licm活跃候选数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O2_LICM_到达就绪候选数」、做 转字符串 用 _licm到达就绪候选数。
        状态 的 第 4 项 为 状态 的 第 4 项 加 _cp总变化 加 _licm总提升。
        做 打印文本 用 「    [优化] O2结束\n」。
    完。
    若 配置 大于或等于 流水线_优化O3 并且 函数列 不等于 空：
        做 打印文本 用 「    [优化] O3开始\n」。
        做 IPA_重置摘要库。
        _o3控制流图 为 做 上下文_CFG 用 ctx。
        _o3函数结构名表 为 做 _流水线_分析表转函数名字典 用 做 上下文_函数结构分析 用 ctx。
        _o3函数数据流名表 为 做 _流水线_分析表转函数名字典 用 做 上下文_函数数据流分析 用 ctx。
        可变 _ipa_fi 为 0。
        做 打印文本 用 「    [优化] O3-IPA开始\n」。
        只要 _ipa_fi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _ipa_fi 项 并且 做 计数 用 函数列 的 第 _ipa_fi 项 大于或等于 4：
                _ipa_名 为 函数列 的 第 _ipa_fi 项 的 第 1 项。
                _ipa_体 为 函数列 的 第 _ipa_fi 项 的 第 3 项。
                _ipa_摘要 为 做 IPA_摘要_创建 用 _ipa_名。
                若 做 是列表 用 _ipa_体：
                    _si 为 0。
                    只要 _si 小于 做 计数 用 _ipa_体：
                        若 做 是列表 用 _ipa_体 的 第 _si 项 并且 做 计数 用 _ipa_体 的 第 _si 项 大于 0：
                            若 _ipa_体 的 第 _si 项 的 第 0 项 等于 节点_调用：
                                _ipa_摘要 的 第 4 项 为 真。
                            完。
                            若 (_ipa_体 的 第 _si 项 的 第 0 项 等于 标记_定义 或者 _ipa_体 的 第 _si 项 的 第 0 项 等于 标记_可变) 并且 做 计数 用 _ipa_体 的 第 _si 项 大于或等于 3：
                                若 做 是列表 用 _ipa_体 的 第 _si 项 的 第 2 项 并且 做 计数 用 _ipa_体 的 第 _si 项 的 第 2 项 大于 0 并且 _ipa_体 的 第 _si 项 的 第 2 项 的 第 0 项 等于 节点_调用：
                                    _ipa_摘要 的 第 4 项 为 真。
                                完。
                            完。
                        完。
                        _si 为 _si 加 1。
                    完。
                完。
                做 IPA_注册摘要 用 _ipa_摘要。
                做 IPA_记录分析。
            完。
            _ipa_fi 为 _ipa_fi 加 1。
        完。
        做 打印文本 用 「    [优化] O3-IPA结束\n」。
        做 IPO_重置。
        _o3热度表 为 做 上下文_热度 用 ctx。
        可变 _ipo分析函数数 为 0。
        可变 _ipo结构命中数 为 0。
        可变 _ipo数据流命中数 为 0。
        可变 _ipo热度命中数 为 0。
        可变 _ipo低环深函数数 为 0。
        可变 _ipo低压力函数数 为 0。
        可变 _ipo小函数数 为 0。
        可变 _ipo热点数 为 0。
        可变 _ipo热点小函数数 为 0。
        可变 _ipo_fi 为 0。
        做 打印文本 用 「    [优化] O3-IPO开始\n」。
        只要 _ipo_fi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _ipo_fi 项 并且 做 计数 用 函数列 的 第 _ipo_fi 项 大于或等于 4：
                _ipo_名 为 函数列 的 第 _ipo_fi 项 的 第 1 项。
                _ipo_体 为 函数列 的 第 _ipo_fi 项 的 第 3 项。
                _ipo_结构项 为 做 字典取 用 _o3函数结构名表、_ipo_名。
                _ipo_数据项 为 做 字典取 用 _o3函数数据流名表、_ipo_名。
                可变 _ipo_热度值 为 空。
                可变 _ipo_初始性质 为 0。
                可变 _ipo_最大循环深度 为 0。
                可变 _ipo_最大活跃OUT 为 0。
                可变 _ipo_最大到达IN 为 0。
                可变 _ipo_低压力 为 假。
                可变 _ipo_小函数 为 假。
                可变 _ipo_热点 为 假。
                _ipo分析函数数 为 _ipo分析函数数 加 1。
                若 做 是列表 用 _ipo_结构项 并且 做 计数 用 _ipo_结构项 大于或等于 4：
                    _ipo结构命中数 为 _ipo结构命中数 加 1。
                    _ipo_最大循环深度 为 做 _流水线_最大循环深度 用 _ipo_结构项 的 第 3 项。
                    若 _ipo_最大循环深度 小于或等于 1：
                        _ipo低环深函数数 为 _ipo低环深函数数 加 1。
                    完。
                完。
                若 做 是列表 用 _ipo_数据项 并且 做 计数 用 _ipo_数据项 大于或等于 4：
                    _ipo数据流命中数 为 _ipo数据流命中数 加 1。
                    _ipo_最大活跃OUT 为 做 _流水线_块表最大集合大小 用 _ipo_数据项 的 第 2 项、2。
                    _ipo_最大到达IN 为 做 _流水线_块表最大集合大小 用 _ipo_数据项 的 第 3 项、1。
                    若 _ipo_最大活跃OUT 小于或等于 8 并且 _ipo_最大到达IN 小于或等于 16：
                        _ipo_低压力 为 真。
                        _ipo低压力函数数 为 _ipo低压力函数数 加 1。
                    完。
                完。
                若 _o3热度表 不等于 空：
                    _ipo_热度值 为 做 字典取 用 _o3热度表、_ipo_名。
                    若 _ipo_热度值 不等于 空：
                        _ipo热度命中数 为 _ipo热度命中数 加 1。
                        若 做 热度_级别 用 _ipo_热度值 大于或等于 热度_热：
                            _ipo_热点 为 真。
                            _ipo_初始性质 为 _ipo_初始性质 位或 FP_热点。
                            _ipo热点数 为 _ipo热点数 加 1。
                        完。
                    完。
                完。
                若 做 是列表 用 _ipo_体 并且 做 计数 用 _ipo_体 小于或等于 8 并且 做 是列表 用 _ipo_结构项 并且 做 计数 用 _ipo_结构项 大于或等于 4 并且 做 是列表 用 _ipo_数据项 并且 做 计数 用 _ipo_数据项 大于或等于 4 并且 _ipo_最大循环深度 小于或等于 1 并且 _ipo_低压力：
                    _ipo_小函数 为 真。
                    _ipo_初始性质 为 _ipo_初始性质 位或 FP_小函数。
                    _ipo小函数数 为 _ipo小函数数 加 1。
                完。
                若 _ipo_热点 并且 _ipo_小函数：
                    _ipo热点小函数数 为 _ipo热点小函数数 加 1。
                完。
                做 IPO_注册函数 用 _ipo_名、_ipo_初始性质。
                可变 _有调用 为 假。
                若 做 是列表 用 _ipo_体：
                    可变 _ci 为 0。
                    只要 _ci 小于 做 计数 用 _ipo_体：
                        若 做 是列表 用 _ipo_体 的 第 _ci 项 并且 做 计数 用 _ipo_体 的 第 _ci 项 大于 0：
                            若 _ipo_体 的 第 _ci 项 的 第 0 项 等于 节点_调用 并且 做 计数 用 _ipo_体 的 第 _ci 项 大于或等于 2：
                                做 IPO_添加调用边 用 _ipo_名、_ipo_体 的 第 _ci 项 的 第 1 项。
                                _有调用 为 真。
                            完。
                            若 (_ipo_体 的 第 _ci 项 的 第 0 项 等于 标记_定义 或者 _ipo_体 的 第 _ci 项 的 第 0 项 等于 标记_可变) 并且 做 计数 用 _ipo_体 的 第 _ci 项 大于或等于 3：
                                若 做 是列表 用 _ipo_体 的 第 _ci 项 的 第 2 项 并且 做 计数 用 _ipo_体 的 第 _ci 项 的 第 2 项 大于或等于 2 并且 _ipo_体 的 第 _ci 项 的 第 2 项 的 第 0 项 等于 节点_调用：
                                    做 IPO_添加调用边 用 _ipo_名、_ipo_体 的 第 _ci 项 的 第 2 项 的 第 1 项。
                                    _有调用 为 真。
                                完。
                            完。
                        完。
                        _ci 为 _ci 加 1。
                    完。
                完。
                若 非 _有调用：
                    做 IPO_分析纯度 用 _ipo_名、假、假、假。
                完。
            完。
            _ipo_fi 为 _ipo_fi 加 1。
        完。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_IPO_分析函数数」、做 转字符串 用 _ipo分析函数数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_IPO_结构命中数」、做 转字符串 用 _ipo结构命中数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_IPO_数据流命中数」、做 转字符串 用 _ipo数据流命中数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_IPO_热度命中数」、做 转字符串 用 _ipo热度命中数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_IPO_低环深函数数」、做 转字符串 用 _ipo低环深函数数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_IPO_低压力函数数」、做 转字符串 用 _ipo低压力函数数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_IPO_小函数数」、做 转字符串 用 _ipo小函数数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_IPO_热点数」、做 转字符串 用 _ipo热点数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_IPO_热点小函数数」、做 转字符串 用 _ipo热点小函数数。
        做 打印文本 用 「    [优化] O3-IPO结束\n」。
        做 自适应_重置。
        可变 _aoe_fi 为 0。
        做 打印文本 用 「    [优化] O3-自适应开始\n」。
        只要 _aoe_fi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _aoe_fi 项 并且 做 计数 用 函数列 的 第 _aoe_fi 项 大于或等于 4：
                _aoe_体 为 函数列 的 第 _aoe_fi 项 的 第 3 项。
                若 做 是列表 用 _aoe_体 并且 做 计数 用 _aoe_体 大于 0：
                    做 提取程序特征 用 _aoe_体。
                    做 自适应_记录优化。
                完。
            完。
            _aoe_fi 为 _aoe_fi 加 1。
        完。
        做 打印文本 用 「    [优化] O3-自适应结束\n」。
        可变 _vz分析函数数 为 0。
        可变 _vz结构命中数 为 0。
        可变 _vz数据流命中数 为 0。
        可变 _vz循环数 为 0。
        可变 _vz头块活跃OUT峰值 为 0。
        可变 _vz头块到达IN峰值 为 0。
        可变 _vz头块到达就绪循环数 为 0。
        可变 _vz可向量化循环数 为 0。
        可变 _vz可向量化且就绪循环数 为 0。
        _vz函数键列表 为 做 字典_键排序 用 做 字典键 用 _o3函数结构名表。
        可变 _vz_fi 为 0。
        做 打印文本 用 「    [优化] O3-向量化开始\n」。
        只要 _vz_fi 小于 做 计数 用 _vz函数键列表：
            _vz函数名 为 _vz函数键列表 的 第 _vz_fi 项。
            _vz结构项 为 做 字典取 用 _o3函数结构名表、_vz函数名。
            _vz数据项 为 做 字典取 用 _o3函数数据流名表、_vz函数名。
            _vz分析函数数 为 _vz分析函数数 加 1。
            若 做 是列表 用 _vz结构项 并且 做 计数 用 _vz结构项 大于或等于 4：
                _vz结构命中数 为 _vz结构命中数 加 1。
                _vz循环列表 为 _vz结构项 的 第 3 项。
                可变 _vz活跃块表 为 空。
                可变 _vz到达块表 为 空。
                若 做 是列表 用 _vz数据项 并且 做 计数 用 _vz数据项 大于或等于 4：
                    _vz数据流命中数 为 _vz数据流命中数 加 1。
                    _vz活跃块表 为 _vz数据项 的 第 2 项。
                    _vz到达块表 为 _vz数据项 的 第 3 项。
                完。
                若 做 是列表 用 _vz循环列表 并且 _o3控制流图 不等于 空：
                    可变 _vz_li 为 0。
                    只要 _vz_li 小于 做 计数 用 _vz循环列表：
                        _vz循环 为 _vz循环列表 的 第 _vz_li 项。
                        若 做 是列表 用 _vz循环 并且 做 计数 用 _vz循环 大于或等于 3：
                            _vz循环数 为 _vz循环数 加 1。
                            _vz头块ID 为 做 循环_头块 用 _vz循环。
                            _vz语句列表 为 做 _流水线_循环语句列表 用 _o3控制流图、_vz循环。
                            可变 _vz头块就绪 为 假。
                            若 _vz活跃块表 不等于 空 并且 _vz到达块表 不等于 空：
                                _vz头块活跃 为 做 字典取 用 _vz活跃块表、做 转字符串 用 _vz头块ID。
                                _vz头块到达 为 做 字典取 用 _vz到达块表、做 转字符串 用 _vz头块ID。
                                若 做 是列表 用 _vz头块活跃 并且 做 计数 用 _vz头块活跃 大于或等于 3 并且 做 是列表 用 _vz头块活跃 的 第 2 项：
                                    若 做 计数 用 _vz头块活跃 的 第 2 项 大于 _vz头块活跃OUT峰值：
                                        _vz头块活跃OUT峰值 为 做 计数 用 _vz头块活跃 的 第 2 项。
                                    完。
                                完。
                                若 做 是列表 用 _vz头块到达 并且 做 计数 用 _vz头块到达 大于或等于 2 并且 做 是列表 用 _vz头块到达 的 第 1 项：
                                    若 做 计数 用 _vz头块到达 的 第 1 项 大于 _vz头块到达IN峰值：
                                        _vz头块到达IN峰值 为 做 计数 用 _vz头块到达 的 第 1 项。
                                    完。
                                    _vz头块到达IN索引 为 做 数流_集_索引表 用 _vz头块到达 的 第 1 项。
                                    _vz头块 为 做 数流_CFG_查找块 用 _o3控制流图、_vz头块ID。
                                    若 _vz头块 不等于 空：
                                        _vz头块就绪 为 做 _流水线_语句列表局部使用全在索引表 用 做 数流_块_语句 用 _vz头块、_vz头块到达IN索引。
                                        若 _vz头块就绪：
                                            _vz头块到达就绪循环数 为 _vz头块到达就绪循环数 加 1。
                                        完。
                                    完。
                                完。
                            完。
                            _vz报告 为 做 VZ_分析循环 用 _vz循环、_vz语句列表、「i」、_o3控制流图。
                            若 做 向量化报告_可向量化 用 _vz报告：
                                _vz可向量化循环数 为 _vz可向量化循环数 加 1。
                                若 _vz头块就绪：
                                    _vz可向量化且就绪循环数 为 _vz可向量化且就绪循环数 加 1。
                                完。
                            完。
                        完。
                        _vz_li 为 _vz_li 加 1。
                    完。
                完。
            完。
            _vz_fi 为 _vz_fi 加 1。
        完。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_VZ_分析函数数」、做 转字符串 用 _vz分析函数数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_VZ_结构命中数」、做 转字符串 用 _vz结构命中数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_VZ_数据流命中数」、做 转字符串 用 _vz数据流命中数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_VZ_循环数」、做 转字符串 用 _vz循环数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_VZ_头块活跃OUT峰值」、做 转字符串 用 _vz头块活跃OUT峰值。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_VZ_头块到达IN峰值」、做 转字符串 用 _vz头块到达IN峰值。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_VZ_头块到达就绪循环数」、做 转字符串 用 _vz头块到达就绪循环数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_VZ_可向量化循环数」、做 转字符串 用 _vz可向量化循环数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「O3_VZ_可向量化且就绪循环数」、做 转字符串 用 _vz可向量化且就绪循环数。
        做 打印文本 用 「    [优化] O3-向量化结束\n」。
        做 打印文本 用 「    [优化] O3-预测开始\n」。
        _预测模型 为 做 预测模型_创建。
        做 生成预测报告 用 函数列、_预测模型。
        做 打印文本 用 「    [优化] O3-预测结束\n」。
        做 RA_重置。
        可变 _rafi 为 0。
        做 打印文本 用 「    [优化] O3-寄存器开始\n」。
        只要 _rafi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _rafi 项 并且 做 计数 用 函数列 的 第 _rafi 项 大于或等于 4：
                _ra体 为 函数列 的 第 _rafi 项 的 第 3 项。
                若 做 是列表 用 _ra体 并且 做 计数 用 _ra体 大于 0：
                    做 RA_记录分配。
                完。
            完。
            _rafi 为 _rafi 加 1。
        完。
        做 打印文本 用 「    [优化] O3-寄存器结束\n」。
        做 打印文本 用 「    [优化] O3结束\n」。
    完。
    ctx 的 第 10 项 为 ctx 的 第 10 项 加 状态 的 第 4 项。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「优化」。
    返还 状态。
完。

流水线_阶段7_安全分析 接受 ctx：
    可变 _sfi 为 0。
    可变 _si 为 0。
    状态 为 做 流水线状态_创建 用 「安全分析」。
    bce_ctx 为 做 BCE_上下文_创建。
    做 UAF_重置。
    函数列 为 做 上下文_函数列 用 ctx。
    若 函数列 不等于 空 并且 做 是列表 用 函数列：
        只要 _sfi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _sfi 项 并且 做 计数 用 函数列 的 第 _sfi 项 大于或等于 4：
                _s体 为 函数列 的 第 _sfi 项 的 第 3 项。
                若 做 是列表 用 _s体：
                    只要 _si 小于 做 计数 用 _s体：
                        若 做 是列表 用 _s体 的 第 _si 项 并且 做 计数 用 _s体 的 第 _si 项 大于 0：
                            若 (_s体 的 第 _si 项 的 第 0 项 等于 标记_定义 或者 _s体 的 第 _si 项 的 第 0 项 等于 标记_可变) 并且 做 计数 用 _s体 的 第 _si 项 大于或等于 3：
                                若 做 是列表 用 _s体 的 第 _si 项 的 第 2 项 并且 做 计数 用 _s体 的 第 _si 项 的 第 2 项 大于或等于 3 并且 _s体 的 第 _si 项 的 第 2 项 的 第 0 项 等于 节点_索引：
                                    _bce结果 为 做 BCE_分析访问 用 bce_ctx、_s体 的 第 _si 项 的 第 2 项 的 第 1 项、_s体 的 第 _si 项 的 第 2 项 的 第 2 项。
                                    若 _bce结果 等于 1：
                                        做 BCE_记录消除。
                                    否则：
                                        做 BCE_记录保留。
                                    完。
                                    做 流水线状态_增项目 用 状态。
                                完。
                            完。
                        完。
                        _si 为 _si 加 1。
                    完。
                完。
            完。
            _sfi 为 _sfi 加 1。
        完。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「安全分析」。
    返还 状态。
完。

可变 _污点_源数 为 0。

可变 _污点_汇点数 为 0。

可变 _污点_路径数 为 0。

流水线_阶段7_5_污点分析 接受 ctx、profile：
    状态 为 做 流水线状态_创建 用 「污点分析」。
    可变 _污点启用 为 假。
    若 profile 不等于 空：
        若 做 字符串包含 用 profile、「analysis」 或者 做 字符串包含 用 profile、「kernel」：
            _污点启用 为 真。
        完。
    完。
    若 _污点启用：
        _污点_源数 为 0。
        _污点_汇点数 为 0。
        _污点_路径数 为 0。
        函数列 为 做 上下文_函数列 用 ctx。
        若 函数列 不等于 空 并且 做 是列表 用 函数列：
            可变 _tfi 为 0。
            只要 _tfi 小于 做 计数 用 函数列：
                若 做 是列表 用 函数列 的 第 _tfi 项 并且 做 计数 用 函数列 的 第 _tfi 项 大于或等于 4：
                    _t体 为 函数列 的 第 _tfi 项 的 第 3 项。
                    若 做 是列表 用 _t体：
                        可变 _ti 为 0。
                        只要 _ti 小于 做 计数 用 _t体：
                            若 做 是列表 用 _t体 的 第 _ti 项 并且 做 计数 用 _t体 的 第 _ti 项 大于 0：
                                若 _t体 的 第 _ti 项 的 第 0 项 等于 节点_调用 并且 做 计数 用 _t体 的 第 _ti 项 大于或等于 2：
                                    _调用名 为 _t体 的 第 _ti 项 的 第 1 项。
                                    若 做 是列表 用 _调用名 等于 假：
                                        _污点_源数 为 _污点_源数 加 1。
                                        _污点_汇点数 为 _污点_汇点数 加 1。
                                        _污点_路径数 为 _污点_路径数 加 1。
                                    完。
                                完。
                            完。
                            _ti 为 _ti 加 1。
                        完。
                    完。
                完。
                _tfi 为 _tfi 加 1。
            完。
        完。
        做 流水线状态_增项目 用 状态。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「污点分析」。
    返还 状态。
完。

可变 _形式化_检查数 为 0。

可变 _形式化_通过数 为 0。

可变 _形式化_失败数 为 0。

流水线_阶段7_8_形式化验证 接受 ctx、模块标签：
    状态 为 做 流水线状态_创建 用 「形式化验证」。
    可变 _需要验证 为 假。
    若 模块标签 不等于 空 并且 做 是列表 用 模块标签：
        可变 _ti 为 0。
        只要 _ti 小于 做 计数 用 模块标签：
            若 模块标签 的 第 _ti 项 等于 「启动」 或者 模块标签 的 第 _ti 项 等于 「内核态」：
                _需要验证 为 真。
            完。
            _ti 为 _ti 加 1。
        完。
    完。
    若 _需要验证：
        做 验证_重置。
        函数列 为 做 上下文_函数列 用 ctx。
        若 函数列 不等于 空 并且 做 是列表 用 函数列：
            可变 _fvi 为 0。
            只要 _fvi 小于 做 计数 用 函数列：
                若 做 是列表 用 函数列 的 第 _fvi 项 并且 做 计数 用 函数列 的 第 _fvi 项 大于或等于 4：
                    _fv体 为 函数列 的 第 _fvi 项 的 第 3 项。
                    若 做 是列表 用 _fv体：
                        可变 _vi 为 0。
                        只要 _vi 小于 做 计数 用 _fv体：
                            若 做 是列表 用 _fv体 的 第 _vi 项 并且 做 计数 用 _fv体 的 第 _vi 项 大于 0：
                                若 _fv体 的 第 _vi 项 的 第 0 项 等于 节点_二元 并且 做 计数 用 _fv体 的 第 _vi 项 大于或等于 4 并且 _fv体 的 第 _vi 项 的 第 1 项 等于 4：
                                    _除数节点 为 _fv体 的 第 _vi 项 的 第 3 项。
                                    若 做 是列表 用 _除数节点 并且 做 计数 用 _除数节点 大于或等于 2 并且 _除数节点 的 第 0 项 等于 标记_数字：
                                        _dz 为 做 验证_除零 用 _除数节点 的 第 1 项。
                                        做 验证_记录 用 _dz 的 第 0 项。
                                    否则：
                                        做 验证_记录 用 假。
                                    完。
                                    _形式化_检查数 为 _形式化_检查数 加 1。
                                完。
                                若 (_fv体 的 第 _vi 项 的 第 0 项 等于 标记_定义 或者 _fv体 的 第 _vi 项 的 第 0 项 等于 标记_可变) 并且 做 计数 用 _fv体 的 第 _vi 项 大于或等于 3：
                                    若 做 是列表 用 _fv体 的 第 _vi 项 的 第 2 项 并且 做 计数 用 _fv体 的 第 _vi 项 的 第 2 项 大于或等于 4：
                                        若 _fv体 的 第 _vi 项 的 第 2 项 的 第 0 项 等于 节点_二元 并且 _fv体 的 第 _vi 项 的 第 2 项 的 第 1 项 等于 4：
                                            _d2 为 _fv体 的 第 _vi 项 的 第 2 项 的 第 3 项。
                                            若 做 是列表 用 _d2 并且 做 计数 用 _d2 大于或等于 2 并且 _d2 的 第 0 项 等于 标记_数字：
                                                做 验证_记录 用 做 验证_除零 用 _d2 的 第 1 项 的 第 0 项。
                                            否则：
                                                做 验证_记录 用 假。
                                            完。
                                            _形式化_检查数 为 _形式化_检查数 加 1。
                                        完。
                                    完。
                                完。
                            完。
                            _vi 为 _vi 加 1。
                        完。
                    完。
                完。
                _fvi 为 _fvi 加 1。
            完。
        完。
        _形式化_通过数 为 _形式化_通过数 加 _验证_通过数。
        _形式化_失败数 为 _形式化_失败数 加 _验证_失败数。
        做 流水线状态_增项目 用 状态。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「形式化验证」。
    返还 状态。
完。

可变 _约束域_函数数 为 0。

可变 _约束域_安全数 为 0。

可变 _约束域_警告数 为 0。

流水线_阶段7_9_约束域 接受 ctx：
    状态 为 做 流水线状态_创建 用 「约束域」。
    _约束域_函数数 为 0。
    _约束域_安全数 为 0。
    _约束域_警告数 为 0。
    函数列 为 做 上下文_函数列 用 ctx。
    若 函数列 不等于 空 并且 做 是列表 用 函数列：
        可变 fi 为 0。
        只要 fi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 fi 项 并且 做 计数 用 函数列 的 第 fi 项 大于或等于 4：
                _约束域_函数数 为 _约束域_函数数 加 1。
                _fn参 为 函数列 的 第 fi 项 的 第 2 项。
                _fn体 为 函数列 的 第 fi 项 的 第 3 项。
                若 做 是列表 用 _fn参：
                    _参数数 为 做 计数 用 _fn参。
                完。
                _槽数 为 _参数数 加 4。
                若 _槽数 大于 8：
                    _槽数 为 8。
                完。
                初始 为 做 抽象状态_创建 用 _槽数。
                可变 vi 为 0。
                只要 vi 小于 _槽数：
                    初始 的 第 vi 项 为 做 区间_创建 用 0 减 域_顶、域_顶。
                    vi 为 vi 加 1。
                完。
                若 做 是列表 用 _fn体：
                    可变 si 为 0。
                    只要 si 小于 做 计数 用 _fn体：
                        做 _约束域_分析语句 用 _fn体 的 第 si 项、初始。
                        si 为 si 加 1。
                    完。
                完。
                结果 为 做 不动点_迭代 用 初始、0、10。
                可变 已知数 为 0。
                vi 为 0。
                只要 vi 小于 做 计数 用 结果：
                    若 做 区间_是底 用 结果 的 第 vi 项 等于 假 并且 做 区间_是顶 用 结果 的 第 vi 项 等于 假：
                        已知数 为 已知数 加 1。
                    完。
                    vi 为 vi 加 1。
                完。
                若 已知数 大于 0：
                    _约束域_安全数 为 _约束域_安全数 加 1。
                完。
            完。
            fi 为 fi 加 1。
        完。
    完。
    做 流水线状态_增项目 用 状态。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「约束域」。
    返还 状态。
完。

_约束域_分析语句 接受 语句、抽象状态：
    若 做 是列表 用 语句 等于 假 或者 做 计数 用 语句 小于 1：
        返还 空。
    完。
    头 为 语句 的 第 0 项。
    若 头 等于 节点_二元 并且 做 计数 用 语句 大于或等于 3：
        _区间 为 做 _约束域_推断表达式区间 用 语句 的 第 2 项。
        若 _区间 不等于 空 并且 做 区间_是顶 用 _区间 等于 假：
            _约束域_安全数 为 _约束域_安全数 加 1。
        完。
        返还 空。
    完。
    若 头 等于 节点_赋值 并且 做 计数 用 语句 大于或等于 3：
        若 做 是列表 用 语句 的 第 2 项：
            可变 bi 为 0。
            只要 bi 小于 做 计数 用 语句 的 第 2 项：
                做 _约束域_分析语句 用 语句 的 第 2 项 的 第 bi 项、抽象状态。
                bi 为 bi 加 1。
            完。
        完。
        若 做 计数 用 语句 大于或等于 4 并且 做 是列表 用 语句 的 第 3 项：
            可变 bi2 为 0。
            只要 bi2 小于 做 计数 用 语句 的 第 3 项：
                做 _约束域_分析语句 用 语句 的 第 3 项 的 第 bi2 项、抽象状态。
                bi2 为 bi2 加 1。
            完。
        完。
        返还 空。
    完。
    若 头 等于 节点_索引 并且 做 计数 用 语句 大于或等于 3 并且 做 是列表 用 语句 的 第 2 项：
        可变 li 为 0。
        只要 li 小于 做 计数 用 语句 的 第 2 项：
            做 _约束域_分析语句 用 语句 的 第 2 项 的 第 li 项、抽象状态。
            li 为 li 加 1。
        完。
    完。
    返还 空。
完。

_约束域_推断表达式区间 接受 表达式：
    若 做 是列表 用 表达式 等于 假：
        若 做 是整数 用 表达式：
            返还 做 区间_创建 用 表达式、表达式。
        完。
        返还 空。
    完。
    若 做 计数 用 表达式 小于 1：
        返还 空。
    完。
    头 为 表达式 的 第 0 项。
    若 头 等于 标记_数字 并且 做 计数 用 表达式 大于或等于 2 并且 做 是整数 用 表达式 的 第 1 项：
        返还 做 区间_创建 用 表达式 的 第 1 项、表达式 的 第 1 项。
    完。
    若 头 等于 节点_字典 并且 做 计数 用 表达式 大于或等于 4：
        _la 为 做 _约束域_推断表达式区间 用 表达式 的 第 2 项。
        _ra 为 做 _约束域_推断表达式区间 用 表达式 的 第 3 项。
        若 _la 不等于 空 并且 _ra 不等于 空：
            返还 做 区间_并 用 _la、_ra。
        完。
    完。
    若 头 等于 节点_列表：
        _约束域_警告数 为 _约束域_警告数 加 1。
        返还 做 区间_创建 用 0 减 域_顶、域_顶。
    完。
    返还 空。
完。

可变 _降级_结构体展开数 为 0。

可变 _降级_调用ABI化数 为 0。

可变 _降级_闭包平坦数 为 0。

流水线_阶段7_95_IR降级 接受 ctx、目标：
    状态 为 做 流水线状态_创建 用 「IR降级」。
    _降级_结构体展开数 为 0。
    _降级_调用ABI化数 为 0。
    _降级_闭包平坦数 为 0。
    函数列 为 做 上下文_函数列 用 ctx。
    若 函数列 不等于 空 并且 做 是列表 用 函数列：
        可变 fi 为 0。
        只要 fi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 fi 项 并且 做 计数 用 函数列 的 第 fi 项 大于或等于 4：
                体 为 函数列 的 第 fi 项 的 第 3 项。
                若 做 是列表 用 体：
                    做 _降级_递归扫描 用 体。
                完。
            完。
            fi 为 fi 加 1。
        完。
    完。
    主体 为 做 上下文_主体 用 ctx。
    若 主体 不等于 空 并且 做 是列表 用 主体：
        做 _降级_递归扫描 用 主体。
    完。
    做 流水线状态_增项目 用 状态。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「IR降级」。
    返还 状态。
完。

_降级_递归扫描 接受 语句列：
    若 做 是列表 用 语句列 等于 假：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列：
        s 为 语句列 的 第 i 项。
        若 做 是列表 用 s 并且 做 计数 用 s 大于或等于 1：
            头 为 s 的 第 0 项。
            若 头 等于 标记_否则：
                _降级_结构体展开数 为 _降级_结构体展开数 加 1。
            完。
            若 头 等于 节点_列表：
                _降级_调用ABI化数 为 _降级_调用ABI化数 加 1。
            完。
            若 头 等于 标记_建：
                _降级_闭包平坦数 为 _降级_闭包平坦数 加 1。
            完。
            若 头 等于 节点_赋值 并且 做 计数 用 s 大于或等于 3 并且 做 是列表 用 s 的 第 2 项：
                做 _降级_递归扫描 用 s 的 第 2 项。
                若 做 计数 用 s 大于或等于 4 并且 做 是列表 用 s 的 第 3 项：
                    做 _降级_递归扫描 用 s 的 第 3 项。
                完。
            完。
            若 头 等于 节点_索引 并且 做 计数 用 s 大于或等于 3 并且 做 是列表 用 s 的 第 2 项：
                做 _降级_递归扫描 用 s 的 第 2 项。
            完。
            若 头 等于 节点_二元 并且 做 计数 用 s 大于或等于 3：
                做 _降级_递归扫描表达式 用 s 的 第 2 项。
            完。
            若 头 等于 节点_成员 并且 做 计数 用 s 大于或等于 2：
                做 _降级_递归扫描表达式 用 s 的 第 1 项。
            完。
        完。
        i 为 i 加 1。
    完。
完。

_降级_递归扫描表达式 接受 表达式：
    若 做 是列表 用 表达式 等于 假 或者 做 计数 用 表达式 小于 1：
        返还 空。
    完。
    头 为 表达式 的 第 0 项。
    若 头 等于 标记_否则：
        _降级_结构体展开数 为 _降级_结构体展开数 加 1。
    完。
    若 头 等于 节点_列表：
        _降级_调用ABI化数 为 _降级_调用ABI化数 加 1。
        若 做 计数 用 表达式 大于或等于 3 并且 做 是列表 用 表达式 的 第 2 项：
            可变 ai 为 0。
            只要 ai 小于 做 计数 用 表达式 的 第 2 项：
                做 _降级_递归扫描表达式 用 表达式 的 第 2 项 的 第 ai 项。
                ai 为 ai 加 1。
            完。
        完。
    完。
    若 头 等于 标记_建：
        _降级_闭包平坦数 为 _降级_闭包平坦数 加 1。
    完。
    若 头 等于 节点_字典 并且 做 计数 用 表达式 大于或等于 4：
        做 _降级_递归扫描表达式 用 表达式 的 第 2 项。
        做 _降级_递归扫描表达式 用 表达式 的 第 3 项。
    完。
    返还 空。
完。

流水线_阶段8_代码生成 接受 ctx、目标：
    状态 为 做 流水线状态_创建 用 「代码生成」。
    若 做 上下文_热度 用 ctx 不等于 空：
        _布局热函数数 为 做 _流水线_热度表级别计数 用 做 上下文_热度 用 ctx、热度_热。
        _布局极热函数数 为 做 _流水线_热度表级别计数 用 做 上下文_热度 用 ctx、热度_极热。
        做 字典设 用 做 上下文_性能数据 用 ctx、「阶段8_布局热函数数」、做 转字符串 用 _布局热函数数。
        做 字典设 用 做 上下文_性能数据 用 ctx、「阶段8_布局极热函数数」、做 转字符串 用 _布局极热函数数。
        做 流水线状态_增优化 用 状态。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「代码生成」。
    返还 状态。
完。

流水线_阶段9_指令调度 接受 ctx：
    状态 为 做 流水线状态_创建 用 「指令调度」。
    若 做 流水线_当前配置 大于或等于 流水线_优化O2：
        做 IS_调度次数_重置。
        做 流水线状态_增优化 用 状态。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「指令调度」。
    返还 状态。
完。

流水线_执行 接受 源文件、优化级别、目标平台：
    做 流水线_设置配置 用 优化级别。
    做 流水线_设置目标 用 目标平台。
    ctx 为 做 编译上下文_创建 用 源文件。
    可变 阶段报告 为 一组：（空）。
    做 追加 用 阶段报告、做 流水线_阶段1_前端 用 ctx。
    做 追加 用 阶段报告、做 流水线_阶段1_5_宏展开 用 ctx。
    做 追加 用 阶段报告、做 流水线_阶段1_8_语义检查 用 ctx。
    若 做 计数 用 做 上下文_诊断 用 ctx 等于 0：
        做 追加 用 阶段报告、做 流水线_阶段2_CFG构建 用 ctx。
        做 追加 用 阶段报告、做 流水线_阶段3_结构分析 用 ctx。
        做 追加 用 阶段报告、做 流水线_阶段4_数据流 用 ctx。
        做 追加 用 阶段报告、做 流水线_阶段4_5_借用分析 用 ctx。
        做 追加 用 阶段报告、做 流水线_阶段5_别名分析 用 ctx。
        做 追加 用 阶段报告、做 流水线_阶段6_优化 用 ctx。
        做 追加 用 阶段报告、做 流水线_阶段7_安全分析 用 ctx。
        做 追加 用 阶段报告、做 流水线_阶段7_5_污点分析 用 ctx、空。
        做 追加 用 阶段报告、做 流水线_阶段7_8_形式化验证 用 ctx、空。
        做 追加 用 阶段报告、做 流水线_阶段7_9_约束域 用 ctx。
        做 追加 用 阶段报告、做 流水线_阶段7_3_并发分析 用 ctx。
        做 追加 用 阶段报告、做 流水线_阶段7_95_IR降级 用 ctx、目标平台。
        做 追加 用 阶段报告、做 流水线_阶段8_代码生成 用 ctx、目标平台。
        做 追加 用 阶段报告、做 流水线_阶段8_5_DWARF调试信息 用 ctx、源文件。
        做 追加 用 阶段报告、做 流水线_阶段9_指令调度 用 ctx。
    完。
    做 流水线_记录编译。
    返还 一组：ctx、阶段报告。
完。

流水线_执行_模块级 接受 模块名、模块路径、优化级别、profile、模块标签、函数列、主体：
    做 流水线_设置配置 用 优化级别。
    若 profile 等于 「kernel-physical」：
        做 流水线_设置逃逸阈值 用 0。
    否则若 profile 等于 「kernel-qemu」：
        做 流水线_设置逃逸阈值 用 2。
    否则：
        做 流水线_设置逃逸阈值 用 负 1。
    完。
    做 打印文本 用 「    [流水线] 配置完成\n」。
    ctx 为 做 编译上下文_创建 用 模块路径。
    做 打印文本 用 「    [流水线] 上下文创建\n」。
    若 函数列 不等于 空：
        做 上下文_设函数列 用 ctx、函数列。
    完。
    若 主体 不等于 空：
        做 上下文_设主体 用 ctx、主体。
    完。
    做 上下文_设AST 用 ctx、一组：函数列、主体。
    做 打印文本 用 「    [流水线] AST注入\n」。
    可变 阶段报告 为 一组：（空）。
    做 打印文本 用 「    [流水线] 宏展开前\n」。
    做 追加 用 阶段报告、做 流水线_阶段1_5_宏展开 用 ctx。
    做 打印文本 用 「    [流水线] 语义检查前\n」。
    做 追加 用 阶段报告、做 流水线_阶段1_8_语义检查 用 ctx。
    做 打印文本 用 「    [流水线] CFG前\n」。
    做 追加 用 阶段报告、做 流水线_阶段2_CFG构建 用 ctx。
    做 打印文本 用 「    [流水线] 结构前\n」。
    做 追加 用 阶段报告、做 流水线_阶段3_结构分析 用 ctx。
    做 打印文本 用 「    [流水线] 数据流前\n」。
    做 追加 用 阶段报告、做 流水线_阶段4_数据流 用 ctx。
    做 打印文本 用 「    [流水线] 借用前\n」。
    做 追加 用 阶段报告、做 流水线_阶段4_5_借用分析 用 ctx。
    做 打印文本 用 「    [流水线] 别名前\n」。
    做 追加 用 阶段报告、做 流水线_阶段5_别名分析 用 ctx。
    做 打印文本 用 「    [流水线] 优化前\n」。
    做 追加 用 阶段报告、做 流水线_阶段6_优化 用 ctx。
    做 打印文本 用 「    [流水线] 安全前\n」。
    做 追加 用 阶段报告、做 流水线_阶段7_安全分析 用 ctx。
    做 打印文本 用 「    [流水线] 污点前\n」。
    做 追加 用 阶段报告、做 流水线_阶段7_5_污点分析 用 ctx、profile。
    做 打印文本 用 「    [流水线] 形式化前\n」。
    做 追加 用 阶段报告、做 流水线_阶段7_8_形式化验证 用 ctx、模块标签。
    做 打印文本 用 「    [流水线] 约束域前\n」。
    做 追加 用 阶段报告、做 流水线_阶段7_9_约束域 用 ctx。
    做 打印文本 用 「    [流水线] 并发前\n」。
    做 追加 用 阶段报告、做 流水线_阶段7_3_并发分析 用 ctx。
    做 打印文本 用 「    [流水线] IR降级前\n」。
    做 追加 用 阶段报告、做 流水线_阶段7_95_IR降级 用 ctx、_流水线_目标平台。
    做 打印文本 用 「    [流水线] DWARF前\n」。
    做 追加 用 阶段报告、做 流水线_阶段8_5_DWARF调试信息 用 ctx、模块路径。
    做 打印文本 用 「    [流水线] 指令调度前\n」。
    做 追加 用 阶段报告、做 流水线_阶段9_指令调度 用 ctx。
    做 打印文本 用 「    [流水线] 返回前\n」。
    做 流水线_记录编译。
    返还 一组：ctx、阶段报告。
完。

流水线_打印报告 接受 阶段报告：
    可变 i 为 0。
    只要 i 小于 做 计数 用 阶段报告：
        s 为 阶段报告 的 第 i 项。
        可变 状态 为 「✗」。
        若 做 流水线状态_完成 用 s：
            状态 为 「✓」。
        完。
        做 打印文本 用 做 字符串拼接 用 「  [」、做 字符串拼接 用 状态、做 字符串拼接 用 「] 」、做 字符串拼接 用 做 流水线状态_阶段 用 s、做 字符串拼接 用 「 优化:」、做 转字符串 用 s 的 第 4 项。
        i 为 i 加 1。
    完。
完。

流水线_结构化报告 接受 阶段报告：
    可变 完成数 为 0。
    可变 报告 为 做 字典。
    做 字典设 用 报告、「总阶段数」、做 转字符串 用 做 计数 用 阶段报告。
    可变 总优化 为 0。
    可变 已执行 为 一组：（空）。
    可变 已跳过 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 阶段报告：
        s 为 阶段报告 的 第 i 项。
        若 做 流水线状态_完成 用 s：
            完成数 为 完成数 加 1。
            做 追加 用 已执行、做 流水线状态_阶段 用 s。
        否则：
            做 追加 用 已跳过、做 流水线状态_阶段 用 s。
        完。
        总优化 为 总优化 加 s 的 第 4 项。
        i 为 i 加 1。
    完。
    做 字典设 用 报告、「完成阶段数」、做 转字符串 用 完成数。
    做 字典设 用 报告、「总优化次数」、做 转字符串 用 总优化。
    做 字典设 用 报告、「污点源数」、做 转字符串 用 _污点_源数。
    做 字典设 用 报告、「形式化检查数」、做 转字符串 用 _形式化_检查数。
    做 字典设 用 报告、「形式化通过数」、做 转字符串 用 _形式化_通过数。
    做 字典设 用 报告、「编译总数」、做 转字符串 用 _流水线_总编译次数。
    做 字典设 用 报告、「语义_推断总数」、做 转字符串 用 _语义_推断总数。
    做 字典设 用 报告、「语义_函数类型数」、做 转字符串 用 _语义_函数类型数。
    做 字典设 用 报告、「语义_动态逃逸数」、做 转字符串 用 _语义_动态逃逸数。
    做 字典设 用 报告、「语义_逃逸详情数」、做 转字符串 用 做 计数 用 _语义_逃逸详情。
    做 字典设 用 报告、「语义_逃逸门控」、做 转字符串 用 做 流水线_逃逸门控通过。
    做 字典设 用 报告、「语义_AI违规数」、做 转字符串 用 _语义_AI违规数。
    做 字典设 用 报告、「约束域_函数数」、做 转字符串 用 _约束域_函数数。
    做 字典设 用 报告、「约束域_安全数」、做 转字符串 用 _约束域_安全数。
    做 字典设 用 报告、「降级_结构体」、做 转字符串 用 _降级_结构体展开数。
    做 字典设 用 报告、「降级_调用ABI」、做 转字符串 用 _降级_调用ABI化数。
    做 字典设 用 报告、「降级_闭包」、做 转字符串 用 _降级_闭包平坦数。
    返还 报告。
完。

流水线_设置逃逸阈值 接受 阈值：
    _语义_逃逸阈值 为 阈值。
完。

流水线_逃逸门控通过 接受 零参：
    若 _语义_逃逸阈值 小于 0：
        返还 真。
    完。
    返还 _语义_动态逃逸数 小于或等于 _语义_逃逸阈值。
完。

流水线_逃逸详情 接受 零参：
    返还 _语义_逃逸详情。
完。

可变 _流水线_总编译次数 为 0。

可变 _流水线_总优化次数 为 0。

流水线_记录编译 接受 零参：
    _流水线_总编译次数 为 _流水线_总编译次数 加 1。
完。

流水线_记录优化 接受 n：
    _流水线_总优化次数 为 _流水线_总优化次数 加 n。
完。

流水线_总统计 接受 零参：
    返还 一组：_流水线_总编译次数、_流水线_总优化次数。
完。

安全验证_汇总报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「污点_源数」、做 转字符串 用 _污点_源数。
    做 字典设 用 r、「污点_汇点数」、做 转字符串 用 _污点_汇点数。
    做 字典设 用 r、「污点_路径数」、做 转字符串 用 _污点_路径数。
    做 字典设 用 r、「形式化_检查数」、做 转字符串 用 _形式化_检查数。
    做 字典设 用 r、「形式化_通过数」、做 转字符串 用 _形式化_通过数。
    做 字典设 用 r、「形式化_失败数」、做 转字符串 用 _形式化_失败数。
    做 字典设 用 r、「约束域_函数数」、做 转字符串 用 _约束域_函数数。
    做 字典设 用 r、「约束域_安全数」、做 转字符串 用 _约束域_安全数。
    做 字典设 用 r、「约束域_警告数」、做 转字符串 用 _约束域_警告数。
    做 字典设 用 r、「逃逸_总数」、做 转字符串 用 _语义_动态逃逸数。
    做 字典设 用 r、「逃逸_详情数」、做 转字符串 用 做 计数 用 _语义_逃逸详情。
    做 字典设 用 r、「逃逸_门控通过」、做 转字符串 用 做 流水线_逃逸门控通过。
    可变 评级 为 「安全」。
    若 _形式化_失败数 大于 0：
        评级 为 「warning」。
    完。
    若 _约束域_警告数 大于 5：
        评级 为 「warning」。
    完。
    若 做 流水线_逃逸门控通过 等于 假：
        评级 为 「阻塞」。
    完。
    若 _污点_路径数 大于 0 并且 _形式化_失败数 大于 0：
        评级 为 「危险」。
    完。
    做 字典设 用 r、「综合评级」、评级。
    若 _形式化_检查数 大于 0：
        做 字典设 用 r、「形式化通过率%」、做 转字符串 用 _形式化_通过数 乘 100 除以 _形式化_检查数。
    完。
    若 _约束域_函数数 大于 0：
        做 字典设 用 r、「约束域安全率%」、做 转字符串 用 _约束域_安全数 乘 100 除以 _约束域_函数数。
    完。
    返还 r。
完。

可变 _并发_竞争数 为 0。

可变 _并发_死锁风险数 为 0。

流水线_阶段7_3_并发分析 接受 ctx：
    状态 为 做 流水线状态_创建 用 「并发分析」。
    _并发_竞争数 为 0。
    _并发_死锁风险数 为 0。
    做 并发_重置等待图。
    函数列 为 做 上下文_函数列 用 ctx。
    若 函数列 不等于 空 并且 做 是列表 用 函数列：
        可变 _cfi 为 0。
        只要 _cfi 小于 做 计数 用 函数列：
            若 做 是列表 用 函数列 的 第 _cfi 项 并且 做 计数 用 函数列 的 第 _cfi 项 大于或等于 4：
                _c体 为 函数列 的 第 _cfi 项 的 第 3 项。
                若 做 是列表 用 _c体：
                    可变 _ci 为 0。
                    只要 _ci 小于 做 计数 用 _c体：
                        若 做 是列表 用 _c体 的 第 _ci 项 并且 做 计数 用 _c体 的 第 _ci 项 大于或等于 3：
                            若 _c体 的 第 _ci 项 的 第 0 项 等于 节点_调用 并且 做 计数 用 _c体 的 第 _ci 项 大于或等于 2：
                                做 并发_记录分析 用 0。
                            完。
                        完。
                        _ci 为 _ci 加 1。
                    完。
                完。
            完。
            _cfi 为 _cfi 加 1。
        完。
        做 流水线状态_增项目 用 状态。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「并发分析」。
    返还 状态。
完。

流水线_阶段8_5_DWARF调试信息 接受 ctx、源文件：
    状态 为 做 流水线状态_创建 用 「DWARF调试信息」。
    若 _流水线_当前配置 等于 流水线_调试：
        cu 为 做 DWARF_创建编译单元 用 源文件、「.」。
        函数列 为 做 上下文_函数列 用 ctx。
        若 函数列 不等于 空 并且 做 是列表 用 函数列：
            可变 _di 为 0。
            只要 _di 小于 做 计数 用 函数列：
                若 做 是列表 用 函数列 的 第 _di 项 并且 做 计数 用 函数列 的 第 _di 项 大于或等于 4：
                    _d函名 为 函数列 的 第 _di 项 的 第 1 项。
                    _ddie 为 做 DWARF_创建函数 用 _d函名、0、0、源文件、_di。
                    做 DWARF_DIE_添加子节点 用 cu、_ddie。
                完。
                _di 为 _di 加 1。
            完。
        完。
        做 流水线状态_增项目 用 状态。
    完。
    做 流水线状态_标记完成 用 状态。
    做 上下文_标记阶段完成 用 ctx、「DWARF调试信息」。
    返还 状态。
完。

宏展开_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「宏定义数」、做 转字符串 用 _流水线_阶段 的 第 2 项 的 第 3 项。
    做 字典设 用 r、「宏展开已执行」、做 转字符串 用 _流水线_阶段 的 第 2 项 的 第 2 项。
    做 字典设 用 r、「宏展开警告」、做 转字符串 用 _流水线_阶段 的 第 2 项 的 第 4 项。
    返还 r。
完。

语义阶段_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「语义阶段已执行」、做 转字符串 用 _流水线_阶段 的 第 3 项 的 第 2 项。
    做 字典设 用 r、「类型推断数」、做 转字符串 用 _语义_推断总数。
    做 字典设 用 r、「函数类型数」、做 转字符串 用 _语义_函数类型数。
    做 字典设 用 r、「AI违规数」、做 转字符串 用 _语义_AI违规数。
    做 字典设 用 r、「逃逸门控通过」、做 转字符串 用 做 流水线_逃逸门控通过。
    返还 r。
完。

控制流图_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「CFG已执行」、做 转字符串 用 _流水线_阶段 的 第 1 项 的 第 2 项。
    做 字典设 用 r、「CFG处理项」、做 转字符串 用 _流水线_阶段 的 第 1 项 的 第 3 项。
    做 字典设 用 r、「CFG警告」、做 转字符串 用 _流水线_阶段 的 第 1 项 的 第 4 项。
    返还 r。
完。

数据流_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「数据流已执行」、做 转字符串 用 _流水线_阶段 的 第 4 项 的 第 2 项。
    做 字典设 用 r、「数据流处理项」、做 转字符串 用 _流水线_阶段 的 第 4 项 的 第 3 项。
    做 字典设 用 r、「数据流警告」、做 转字符串 用 _流水线_阶段 的 第 4 项 的 第 4 项。
    返还 r。
完。

别名分析_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「别名分析已执行」、做 转字符串 用 _流水线_阶段 的 第 5 项 的 第 2 项。
    做 字典设 用 r、「别名分析处理项」、做 转字符串 用 _流水线_阶段 的 第 5 项 的 第 3 项。
    做 字典设 用 r、「别名分析警告」、做 转字符串 用 _流水线_阶段 的 第 5 项 的 第 4 项。
    返还 r。
完。

安全性_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「安全分析已执行」、做 转字符串 用 _流水线_阶段 的 第 6 项 的 第 2 项。
    做 字典设 用 r、「污点_源数」、做 转字符串 用 _污点_源数。
    做 字典设 用 r、「污点_汇点数」、做 转字符串 用 _污点_汇点数。
    做 字典设 用 r、「污点_路径数」、做 转字符串 用 _污点_路径数。
    做 字典设 用 r、「安全评级」、做 字典取 用 做 安全验证_汇总报告、「综合评级」。
    返还 r。
完。

形式化_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「形式化验证已执行」、做 转字符串 用 _流水线_阶段 的 第 7 项 的 第 2 项。
    做 字典设 用 r、「检查数」、做 转字符串 用 _形式化_检查数。
    做 字典设 用 r、「通过数」、做 转字符串 用 _形式化_通过数。
    做 字典设 用 r、「失败数」、做 转字符串 用 _形式化_失败数。
    返还 r。
完。

优化_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「优化已执行」、做 转字符串 用 _流水线_阶段 的 第 8 项 的 第 2 项。
    做 字典设 用 r、「优化次数」、做 转字符串 用 _流水线_阶段 的 第 8 项 的 第 5 项。
    做 字典设 用 r、「优化警告」、做 转字符串 用 _流水线_阶段 的 第 8 项 的 第 4 项。
    返还 r。
完。

语义_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「推断总数」、做 转字符串 用 _语义_推断总数。
    做 字典设 用 r、「函数类型数」、做 转字符串 用 _语义_函数类型数。
    做 字典设 用 r、「AI违规数」、做 转字符串 用 _语义_AI违规数。
    做 字典设 用 r、「动态逃逸数」、做 转字符串 用 _语义_动态逃逸数。
    做 字典设 用 r、「逃逸阈值」、做 转字符串 用 _语义_逃逸阈值。
    做 字典设 用 r、「逃逸门控通过」、做 转字符串 用 做 流水线_逃逸门控通过。
    返还 r。
完。

动态逃逸_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「逃逸总数」、做 转字符串 用 _语义_动态逃逸数。
    做 字典设 用 r、「逃逸详情数」、做 转字符串 用 做 计数 用 _语义_逃逸详情。
    做 字典设 用 r、「阈值」、做 转字符串 用 _语义_逃逸阈值。
    做 字典设 用 r、「门控通过」、做 转字符串 用 做 流水线_逃逸门控通过。
    可变 escape_i 为 0。
    只要 escape_i 小于 做 计数 用 _语义_逃逸详情 并且 escape_i 小于 10：
        d 为 _语义_逃逸详情 的 第 escape_i 项。
        做 字典设 用 r、做 字符串拼接 用 「逃逸_」、做 转字符串 用 escape_i、做 字符串拼接 用 d 的 第 0 项、做 字符串拼接 用 「:」、d 的 第 1 项。
        escape_i 为 escape_i 加 1。
    完。
    返还 r。
完。

约束冲突_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「约束域_函数数」、做 转字符串 用 _约束域_函数数。
    做 字典设 用 r、「约束域_安全数」、做 转字符串 用 _约束域_安全数。
    做 字典设 用 r、「约束域_警告数」、做 转字符串 用 _约束域_警告数。
    做 字典设 用 r、「AI违规数」、做 转字符串 用 _语义_AI违规数。
    做 字典设 用 r、「逃逸门控通过」、做 转字符串 用 做 流水线_逃逸门控通过。
    可变 冲突总 为 _约束域_警告数 加 _语义_AI违规数。
    做 字典设 用 r、「冲突总数」、做 转字符串 用 冲突总。
    返还 r。
完。

语义设备_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「目标设备」、做 目标_名称。
    做 字典设 用 r、「设备类型」、「CPU」。
    做 字典设 用 r、「指令宽度」、做 转字符串 用 做 目标_指令宽度。
    做 字典设 用 r、「SIMD支持」、做 转字符串 用 做 目标_支持SIMD。
    做 字典设 用 r、「浮点寄存器」、做 转字符串 用 做 目标_浮点寄存器数。
    做 字典设 用 r、「设备约束违规」、「0」。
    返还 r。
完。

语义内存_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「内存模型」、「GC管理」。
    做 字典设 用 r、「逃逸数」、做 转字符串 用 _语义_动态逃逸数。
    做 字典设 用 r、「逃逸阈值」、做 转字符串 用 _语义_逃逸阈值。
    做 字典设 用 r、「内存约束违规」、「0」。
    返还 r。
完。

语义生命周期_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「生命周期检查」、「借用系统」。
    做 字典设 用 r、「约束域_函数数」、做 转字符串 用 _约束域_函数数。
    做 字典设 用 r、「约束域_安全数」、做 转字符串 用 _约束域_安全数。
    做 字典设 用 r、「约束域_警告数」、做 转字符串 用 _约束域_警告数。
    返还 r。
完。

语义合同_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「推断总数」、做 转字符串 用 _语义_推断总数。
    做 字典设 用 r、「函数类型数」、做 转字符串 用 _语义_函数类型数。
    做 字典设 用 r、「AI违规数」、做 转字符串 用 _语义_AI违规数。
    做 字典设 用 r、「效果门控通过」、做 转字符串 用 做 流水线_逃逸门控通过。
    返还 r。
完。

流水线_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「profile」、做 目标_获取profile。
    做 字典设 用 r、「target」、做 目标_名称。
    可变 已执行数 为 0。
    可变 跳过数 为 0。
    可变 总优化 为 0。
    可变 pi 为 0。
    只要 pi 小于 做 计数 用 _流水线_阶段：
        若 _流水线_阶段 的 第 pi 项 的 第 2 项 大于 0：
            已执行数 为 已执行数 加 1。
            总优化 为 总优化 加 _流水线_阶段 的 第 pi 项 的 第 5 项。
        否则：
            跳过数 为 跳过数 加 1。
        完。
        pi 为 pi 加 1。
    完。
    做 字典设 用 r、「已执行阶段数」、做 转字符串 用 已执行数。
    做 字典设 用 r、「跳过阶段数」、做 转字符串 用 跳过数。
    做 字典设 用 r、「总优化次数」、做 转字符串 用 总优化。
    做 字典设 用 r、「安全评级」、做 字典取 用 做 安全验证_汇总报告、「综合评级」。
    做 字典设 用 r、「逃逸门控」、做 转字符串 用 做 流水线_逃逸门控通过。
    做 字典设 用 r、「形式化_检查数」、做 转字符串 用 _形式化_检查数。
    做 字典设 用 r、「形式化_失败数」、做 转字符串 用 _形式化_失败数。
    返还 r。
完。

布局映射 接受 镜像类型、目标名、入口符号、基址、代码大小、运行时大小：
    r 为 做 字典。
    做 字典设 用 r、「image_kind」、镜像类型。
    做 字典设 用 r、「target」、目标名。
    做 字典设 用 r、「entry_symbol」、入口符号。
    做 字典设 用 r、「base_address」、做 转字符串 用 基址。
    做 字典设 用 r、「code_size」、做 转字符串 用 代码大小。
    做 字典设 用 r、「runtime_size」、做 转字符串 用 运行时大小。
    做 字典设 用 r、「total_size」、做 转字符串 用 代码大小 加 运行时大小。
    返还 r。
完。

流水线_重置计划报告 接受 源合同、目标合同、语义图：
    r 为 做 字典。
    做 字典设 用 r、「报告类型」、「reset_plan_report」。
    若 源合同 不等于 空：
        做 字典设 用 r、「源语言」、做 源语言_名称 用 做 外源合同_源语言 用 源合同。
        做 字典设 用 r、「源工作区」、做 外源合同_工作区根 用 源合同。
        做 字典设 用 r、「公共API数」、做 转字符串 用 做 计数 用 做 外源合同_公共API 用 源合同。
        做 字典设 用 r、「FFI边界数」、做 转字符串 用 做 计数 用 做 外源合同_FFI边界 用 源合同。
        做 字典设 用 r、「高风险项数」、做 转字符串 用 做 计数 用 做 外源合同_高风险 用 源合同。
        做 字典设 用 r、「不支持项数」、做 转字符串 用 做 计数 用 做 外源合同_不支持项 用 源合同。
    完。
    若 语义图 不等于 空：
        做 字典设 用 r、「语义节点数」、做 转字符串 用 做 计数 用 做 语义图_节点 用 语义图。
        做 字典设 用 r、「类型映射数」、做 转字符串 用 做 计数 用 做 语义图_类型映射 用 语义图。
        做 字典设 用 r、「风险项数」、做 转字符串 用 做 计数 用 做 语义图_风险项 用 语义图。
        做 字典设 用 r、「阻塞项数」、做 转字符串 用 做 计数 用 做 语义图_阻塞项 用 语义图。
    完。
    可变 可继续 为 真。
    若 源合同 不等于 空 并且 做 计数 用 做 外源合同_不支持项 用 源合同 大于 0：
        可继续 为 假。
    完。
    若 语义图 不等于 空 并且 做 计数 用 做 语义图_阻塞项 用 语义图 大于 0：
        可继续 为 假。
    完。
    做 字典设 用 r、「可继续重置」、做 转字符串 用 可继续。
    返还 r。
完。

本章 收束。
