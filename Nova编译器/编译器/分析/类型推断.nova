《类型推断》

引用《..·..·标准库·基础》。

引用《..·..·编译器·标记常量》。

类型推断_类型_空值 为 5。

类型推断_类型_列表 为 6。

类型推断_类型_字典 为 7。

类型推断_类型_函数 为 8。

类型_动态 为 9。

类型_永不 为 10。

可变 _类型变量计数 为 0。

分析_新类型变量 接受 零参：
    _类型变量计数 为 _类型变量计数 加 1。
    返还 一组：0 减 _类型变量计数。
完。

是类型变量 接受 t：
    若 非 做 是列表 用 t 或者 做 计数 用 t 等于 0：
        返还 假。
    完。
    返还 t 的 第 0 项 小于 0。
完。

类型变量ID 接受 t：
    返还 0 减 t 的 第 0 项。
完。

类型_整数值 接受 零参：
    返还 一组：类型_整数。
完。

类型_浮点值 接受 零参：
    返还 一组：类型_浮点。
完。

类型_字符串值 接受 零参：
    返还 一组：类型_字符串。
完。

类型_布尔值 接受 零参：
    返还 一组：类型_布尔。
完。

类型推断_类型_空值 接受 零参：
    返还 一组：类型推断_类型_空值。
完。

类型_动态值 接受 零参：
    返还 一组：类型_动态。
完。

类型_永不值 接受 零参：
    返还 一组：类型_永不。
完。

类型_列表值 接受 元素类型：
    返还 一组：类型推断_类型_列表、元素类型。
完。

类型_字典值 接受 键类型、值类型：
    返还 一组：类型推断_类型_字典、键类型、值类型。
完。

类型_函数值 接受 参数类型列表、返回类型：
    返还 一组：类型推断_类型_函数、参数类型列表、返回类型。
完。

类型_标记 接受 t：
    若 非 做 是列表 用 t 或者 做 计数 用 t 等于 0：
        返还 类型_未知。
    完。
    返还 t 的 第 0 项。
完。

类型推断_类型_是数值 接受 t：
    若 非 做 是列表 用 t 或者 做 计数 用 t 等于 0：
        返还 假。
    完。
    返还 t 的 第 0 项 等于 类型_整数 或者 t 的 第 0 项 等于 类型_浮点。
完。

类型_是可比较 接受 t：
    若 非 做 是列表 用 t 或者 做 计数 用 t 等于 0：
        返还 假。
    完。
    返还 t 的 第 0 项 等于 类型_整数 或者 t 的 第 0 项 等于 类型_浮点 或者 t 的 第 0 项 等于 类型_字符串 或者 t 的 第 0 项 等于 类型_布尔。
完。

可变 _类型替换表 为 做 字典。

推断_重置 接受 零参：
    _类型替换表 为 做 字典。
    _类型变量计数 为 0。
完。

推断_查找 接受 t：
    若 非 做 是类型变量 用 t：
        返还 t。
    完。
    id 为 做 类型变量ID 用 t。
    绑定 为 做 字典取 用 _类型替换表、做 转字符串 用 id。
    若 绑定 等于 空：
        返还 t。
    完。
    最终 为 做 推断_查找 用 绑定。
    若 最终 不等于 绑定：
        做 字典设 用 _类型替换表、做 转字符串 用 id、最终。
    完。
    返还 最终。
完。

推断_应用替换 接受 t：
    若 非 做 是列表 用 t 或者 做 计数 用 t 等于 0：
        返还 t。
    完。
    t 为 做 推断_查找 用 t。
    若 非 做 是列表 用 t 或者 做 计数 用 t 等于 0：
        返还 t。
    完。
    若 做 是类型变量 用 t：
        返还 t。
    完。
    标记 为 t 的 第 0 项。
    若 标记 等于 类型推断_类型_列表 并且 做 计数 用 t 大于或等于 2：
        返还 一组：类型推断_类型_列表、做 推断_应用替换 用 t 的 第 1 项。
    完。
    若 标记 等于 类型推断_类型_字典 并且 做 计数 用 t 大于或等于 3：
        返还 一组：类型推断_类型_字典、做 推断_应用替换 用 t 的 第 1 项、做 推断_应用替换 用 t 的 第 2 项。
    完。
    返还 t。
完。

推断_occurs_check 接受 var_id、t：
    若 非 做 是列表 用 t 或者 做 计数 用 t 等于 0：
        返还 假。
    完。
    若 做 是类型变量 用 t：
        返还 做 类型变量ID 用 t 等于 var_id。
    完。
    可变 i 为 1。
    只要 i 小于 做 计数 用 t：
        若 做 推断_occurs_check 用 var_id、t 的 第 i 项：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

推断_合一 接受 t1、t2：
    t1 为 做 推断_查找 用 t1。
    t2 为 做 推断_查找 用 t2。
    若 做 是列表 用 t1 并且 做 是列表 用 t2 并且 做 计数 用 t1 大于 0 并且 做 计数 用 t2 大于 0 并且 t1 的 第 0 项 等于 t2 的 第 0 项：
        若 做 计数 用 t1 等于 1 并且 做 计数 用 t2 等于 1：
            返还 真。
        完。
        若 做 计数 用 t1 大于或等于 2 并且 做 计数 用 t2 大于或等于 2：
            返还 做 推断_合一 用 t1 的 第 1 项、t2 的 第 1 项 并且 (做 计数 用 t1 小于 3 或者 做 计数 用 t2 小于 3 或者 做 推断_合一 用 t1 的 第 2 项、t2 的 第 2 项)。
        完。
        返还 真。
    完。
    若 做 是类型变量 用 t1：
        若 做 推断_occurs_check 用 做 类型变量ID 用 t1、t2：
            返还 假。
        完。
        做 字典设 用 _类型替换表、做 转字符串 用 做 类型变量ID 用 t1、t2。
        返还 真。
    完。
    若 做 是类型变量 用 t2：
        若 做 推断_occurs_check 用 做 类型变量ID 用 t2、t1：
            返还 假。
        完。
        做 字典设 用 _类型替换表、做 转字符串 用 做 类型变量ID 用 t2、t1。
        返还 真。
    完。
    若 做 是列表 用 t1 并且 t1 的 第 0 项 等于 类型_动态：
        返还 真。
    完。
    若 做 是列表 用 t2 并且 t2 的 第 0 项 等于 类型_动态：
        返还 真。
    完。
    若 做 是列表 用 t1 并且 t1 的 第 0 项 等于 类型_永不：
        返还 真。
    完。
    若 做 是列表 用 t2 并且 t2 的 第 0 项 等于 类型_永不：
        返还 真。
    完。
    返还 假。
完。

推断_表达式类型 接受 节点、类型环境：
    若 节点 等于 空 或者 非 做 是列表 用 节点 或者 做 计数 用 节点 等于 0：
        返还 做 类型_动态值。
    完。
    节点类型 为 节点 的 第 0 项。
    若 节点类型 等于 标记_数字 并且 做 计数 用 节点 大于或等于 2：
        返还 做 类型_整数值。
    完。
    若 节点类型 等于 节点_空字面量：
        返还 做 类型推断_类型_空值。
    完。
    若 节点类型 等于 标记_字串 并且 做 计数 用 节点 大于或等于 2：
        返还 做 类型_浮点值。
    完。
    若 节点类型 等于 节点_字典 并且 做 计数 用 节点 大于或等于 2：
        返还 做 类型_字符串值。
    完。
    若 节点类型 等于 节点_成员 或者 节点类型 等于 节点_列表：
        返还 做 类型_布尔值。
    完。
    若 节点类型 等于 标记_定义：
        返还 做 类型推断_类型_空值。
    完。
    若 节点类型 等于 标记_名称 并且 做 计数 用 节点 大于或等于 2：
        名称 为 节点 的 第 1 项。
        类型 为 做 字典取 用 类型环境、名称。
        若 类型 等于 空：
            返还 做 类型_动态值。
        完。
        返还 类型。
    完。
    若 节点类型 等于 节点_二元 并且 做 计数 用 节点 大于或等于 4：
        运算符 为 节点 的 第 1 项。
        左类型 为 做 推断_表达式类型 用 节点 的 第 2 项、类型环境。
        右类型 为 做 推断_表达式类型 用 节点 的 第 3 项、类型环境。
        若 运算符 大于或等于 1 并且 运算符 小于或等于 5：
            若 做 类型_标记 用 左类型 等于 类型_浮点 或者 做 类型_标记 用 右类型 等于 类型_浮点：
                返还 做 类型_浮点值。
            完。
            若 运算符 等于 1 并且 做 类型_标记 用 左类型 等于 类型_字符串：
                返还 做 类型_字符串值。
            完。
            若 做 类型_标记 用 左类型 等于 类型_整数 并且 做 类型_标记 用 右类型 等于 类型_整数：
                返还 做 类型_整数值。
            完。
        完。
        若 运算符 大于或等于 6 并且 运算符 小于或等于 11：
            返还 做 类型_布尔值。
        完。
        若 运算符 等于 12 或者 运算符 等于 13：
            返还 做 类型_布尔值。
        完。
        返还 做 类型_动态值。
    完。
    若 节点类型 等于 节点_调用 并且 做 计数 用 节点 大于或等于 2：
        函数名 为 节点 的 第 1 项。
        函数类型 为 做 字典取 用 类型环境、函数名。
        若 做 是列表 用 函数类型 并且 做 计数 用 函数类型 大于或等于 3 并且 函数类型 的 第 0 项 等于 类型推断_类型_函数：
            返还 函数类型 的 第 2 项。
        完。
        返还 做 类型_动态值。
    完。
    若 节点类型 等于 节点_赋值：
        若 做 计数 用 节点 小于或等于 1：
            返还 做 类型_列表值 用 做 类型_动态值。
        完。
        元素类型 为 做 推断_表达式类型 用 节点 的 第 1 项、类型环境。
        返还 做 类型_列表值 用 元素类型。
    完。
    若 节点类型 等于 节点_索引 并且 做 计数 用 节点 大于或等于 3：
        基类型 为 做 推断_表达式类型 用 节点 的 第 1 项、类型环境。
        若 做 是列表 用 基类型 并且 基类型 的 第 0 项 等于 类型推断_类型_列表 并且 做 计数 用 基类型 大于或等于 2：
            返还 基类型 的 第 1 项。
        完。
        若 做 是列表 用 基类型 并且 基类型 的 第 0 项 等于 类型推断_类型_字典 并且 做 计数 用 基类型 大于或等于 3：
            返还 基类型 的 第 2 项。
        完。
        返还 做 类型_动态值。
    完。
    返还 做 类型_动态值。
完。

推断_语句检查 接受 语句、类型环境：
    若 语句 等于 空 或者 非 做 是列表 用 语句 或者 做 计数 用 语句 等于 0：
        返还 空。
    完。
    节点类型 为 语句 的 第 0 项。
    若 节点类型 等于 标记_可变 并且 做 计数 用 语句 大于或等于 3：
        变量名 为 语句 的 第 1 项。
        初值类型 为 做 推断_表达式类型 用 语句 的 第 2 项、类型环境。
        做 字典设 用 类型环境、变量名、初值类型。
    完。
    若 节点类型 等于 标记_函数 并且 做 计数 用 语句 大于或等于 2：
        条件类型 为 做 推断_表达式类型 用 语句 的 第 1 项、类型环境。
        做 推断_合一 用 条件类型、做 类型_布尔值。
    完。
完。

可变 _推断_合一次数 为 0。

可变 _推断_失败次数 为 0。

推断_记录合一 接受 成功：
    _推断_合一次数 为 _推断_合一次数 加 1。
    若 非 成功：
        _推断_失败次数 为 _推断_失败次数 加 1。
    完。
完。

推断_统计 接受 零参：
    返还 一组：_推断_合一次数、_推断_失败次数、做 字典取 用 _类型替换表、「计数」。
完。

本章 收束。
