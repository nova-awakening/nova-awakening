《并发分析》

引用《..·..·标准库·基础》。

访问_原子读写 为 2。

锁_互斥 为 0。

锁_读写 为 1。

锁_自旋 为 2。

锁_无锁 为 3。

锁状态_创建 接受 ID、类型：
    返还 一组：ID、类型、0 减 1、假。
完。

锁状态_ID 接受 s：
    返还 s 的 第 0 项。
完。

锁状态_类型 接受 s：
    返还 s 的 第 1 项。
完。

锁状态_持有者 接受 s：
    返还 s 的 第 2 项。
完。

锁状态_已锁定 接受 s：
    返还 s 的 第 3 项。
完。

锁状态_获取 接受 s、线程ID：
    s 的 第 2 项 为 线程ID。
    s 的 第 3 项 为 真。
完。

锁状态_释放 接受 s：
    s 的 第 2 项 为 0 减 1。
    s 的 第 3 项 为 假。
完。

访问记录_创建 接受 变量名、访问类型、线程ID、位置、有锁：
    返还 一组：变量名、访问类型、线程ID、位置、有锁。
完。

访问记录_变量 接受 r：
    返还 r 的 第 0 项。
完。

访问记录_类型 接受 r：
    返还 r 的 第 1 项。
完。

访问记录_线程 接受 r：
    返还 r 的 第 2 项。
完。

访问记录_有锁 接受 r：
    返还 r 的 第 4 项。
完。

并发_是数据竞争 接受 访问A、访问B：
    若 做 访问记录_变量 用 访问A 不等于 做 访问记录_变量 用 访问B：
        返还 假。
    完。
    若 做 访问记录_线程 用 访问A 等于 做 访问记录_线程 用 访问B：
        返还 假。
    完。
    若 做 访问记录_类型 用 访问A 等于 访问_读 并且 做 访问记录_类型 用 访问B 等于 访问_读：
        返还 假。
    完。
    若 非 做 访问记录_有锁 用 访问A 或者 非 做 访问记录_有锁 用 访问B：
        返还 真。
    完。
    返还 假。
完。

并发_扫描数据竞争 接受 访问列表：
    可变 竞争列表 为 一组：（空）。
    n 为 做 计数 用 访问列表。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 做 并发_是数据竞争 用 访问列表 的 第 i 项、访问列表 的 第 j 项：
                做 追加 用 竞争列表、一组：访问列表 的 第 i 项、访问列表 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 竞争列表。
完。

可变 _锁等待图 为 一组：（空）。

并发_记录等待 接受 等待线程、锁ID、持有线程：
    做 追加 用 _锁等待图、一组：等待线程、锁ID、持有线程。
完。

并发_重置等待图 接受 零参：
    _锁等待图 为 一组：（空）。
完。

并发_检测死锁 接受 零参：
    可变 死锁风险 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _锁等待图：
        等待者 为 _锁等待图 的 第 i 项 的 第 0 项。
        持有者 为 _锁等待图 的 第 i 项 的 第 2 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 _锁等待图：
            若 i 不等于 j：
                若 _锁等待图 的 第 j 项 的 第 0 项 等于 持有者：
                    可变 k 为 0。
                    只要 k 小于 做 计数 用 _锁等待图：
                        若 _锁等待图 的 第 k 项 的 第 0 项 等于 等待者 并且 _锁等待图 的 第 k 项 的 第 2 项 等于 持有者：
                            做 追加 用 死锁风险、一组：等待者、持有者、_锁等待图 的 第 i 项 的 第 1 项。
                        完。
                        k 为 k 加 1。
                    完。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 死锁风险。
完。

可变 _原子性_检查序列 为 一组：（空）。

原子性_记录检查 接受 位置、变量名、线程ID：
    做 追加 用 _原子性_检查序列、一组：位置、0 减 1、变量名、线程ID。
完。

原子性_记录操作 接受 位置、变量名、线程ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _原子性_检查序列：
        记录 为 _原子性_检查序列 的 第 i 项。
        若 记录 的 第 2 项 等于 变量名 并且 记录 的 第 3 项 等于 线程ID 并且 记录 的 第 1 项 等于 0 减 1：
            记录 的 第 1 项 为 位置。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 _原子性_检查序列、一组：0 减 1、位置、变量名、线程ID。
完。

原子性_扫描TOCTOU 接受 访问列表：
    可变 风险 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _原子性_检查序列：
        记录 为 _原子性_检查序列 的 第 i 项。
        若 记录 的 第 0 项 大于或等于 0 并且 记录 的 第 1 项 大于或等于 0：
            变量名 为 记录 的 第 2 项。
            线程ID 为 记录 的 第 3 项。
            可变 j 为 0。
            只要 j 小于 做 计数 用 访问列表：
                访问 为 访问列表 的 第 j 项。
                若 做 访问记录_变量 用 访问 等于 变量名 并且 做 访问记录_线程 用 访问 不等于 线程ID 并且 做 访问记录_类型 用 访问 等于 访问_写：
                    做 追加 用 风险、一组：变量名、线程ID、「TOCTOU时间窗口漏洞」。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 风险。
完。

线程安全_否 为 0。

线程安全_Send 为 1。

线程安全_Sync 为 2。

分析类型线程安全 接受 类型：
    若 类型 等于 1 或者 类型 等于 2：
        返还 线程安全_Sync。
    完。
    若 类型 等于 3：
        返还 线程安全_Sync。
    完。
    若 类型 等于 4：
        返还 线程安全_Sync。
    完。
    若 类型 等于 5：
        返还 线程安全_Send。
    完。
    若 类型 等于 6：
        返还 线程安全_Send。
    完。
    若 类型 等于 7：
        返还 线程安全_Send。
    完。
    返还 线程安全_否。
完。

并发分析_报告 接受 访问列表：
    竞争 为 做 并发_扫描数据竞争 用 访问列表。
    死锁 为 做 并发_检测死锁。
    toctou 为 做 原子性_扫描TOCTOU 用 访问列表。
    可变 报告 为 一组：（空）。
    做 追加 用 报告、一组：「数据竞争」、做 计数 用 竞争。
    做 追加 用 报告、一组：「死锁风险」、做 计数 用 死锁。
    做 追加 用 报告、一组：「原子性违规」、做 计数 用 toctou。
    返还 报告。
完。

可变 _并发_分析次数 为 0。

可变 _并发_竞争数 为 0。

并发_记录分析 接受 竞争数：
    _并发_分析次数 为 _并发_分析次数 加 1。
    _并发_竞争数 为 _并发_竞争数 加 竞争数。
完。

并发_统计报告 接受 零参：
    返还 一组：_并发_分析次数、_并发_竞争数。
完。

本章 收束。
