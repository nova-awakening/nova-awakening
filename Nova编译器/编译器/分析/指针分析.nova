《指针分析》

引用《..·..·标准库·基础》。

最大紧凑对象数 为 64。

PTA_位集_创建 接受 零参：
    返还 一组：0、0。
完。

PTA_位集_设置 接受 s、id：
    若 id 小于 64：
        s 的 第 0 项 为 s 的 第 0 项 位或 1 左移 id。
    完。
    返还 s。
完。

PTA_位集_检查 接受 s、id：
    若 id 小于 64：
        返还 s 的 第 0 项 右移 id 位与 1 等于 1。
    完。
    返还 假。
完。

PTA_位集_合并 接受 s1、s2：
    返还 一组：s1 的 第 0 项 位或 s2 的 第 0 项、s1 的 第 1 项 位或 s2 的 第 1 项。
完。

PTA_位集_为空 接受 s：
    返还 s 的 第 0 项 等于 0 并且 s 的 第 1 项 等于 0。
完。

PTA_位集_相等 接受 s1、s2：
    返还 s1 的 第 0 项 等于 s2 的 第 0 项 并且 s1 的 第 1 项 等于 s2 的 第 1 项。
完。

可变 _PTA_对象表 为 一组：（空）。

可变 _PTA_对象计数 为 0。

PTA_OBJ_堆 为 0。

PTA_OBJ_栈 为 1。

PTA_OBJ_全局 为 2。

PTA_OBJ_参数 为 3。

PTA_OBJ_返回 为 4。

PTA_OBJ_未知 为 5。

PTA_新对象 接受 类型、位置：
    id 为 _PTA_对象计数。
    做 追加 用 _PTA_对象表、一组：id、类型、位置。
    _PTA_对象计数 为 _PTA_对象计数 加 1。
    返还 id。
完。

指针分析_分配 为 0。

指针分析_复制 为 1。

指针分析_加载 为 2。

指针分析_存储 为 3。

指针分析_字段 为 4。

可变 _PTA_约束列表 为 一组：（空）。

PTA_重置 接受 零参：
    _PTA_对象表 为 一组：（空）。
    _PTA_对象计数 为 0。
    _PTA_约束列表 为 一组：（空）。
完。

PTA_添加约束 接受 类型、目标、源、字段名：
    做 追加 用 _PTA_约束列表、一组：类型、目标、源、字段名。
完。

PTA_求解 接受 pts_map：
    若 pts_map 等于 空：
        pts_map 为 做 字典。
    完。
    可变 已改变 为 真。
    可变 迭代次数 为 0。
    最大迭代 为 做 计数 用 _PTA_约束列表 乘 3 加 10。
    只要 已改变 并且 迭代次数 小于 最大迭代：
        已改变 为 假。
        迭代次数 为 迭代次数 加 1。
        可变 i 为 0。
        只要 i 小于 做 计数 用 _PTA_约束列表：
            约束 为 _PTA_约束列表 的 第 i 项。
            类型 为 约束 的 第 0 项。
            目标 为 约束 的 第 1 项。
            源 为 约束 的 第 2 项。
            dst_pts 为 做 字典取 用 pts_map、目标。
            若 dst_pts 等于 空：
                dst_pts 为 做 PTA_位集_创建。
            完。
            若 类型 等于 指针分析_分配：
                新pts 为 做 PTA_位集_设置 用 dst_pts、源。
                若 非 做 PTA_位集_相等 用 新pts、dst_pts：
                    做 字典设 用 pts_map、目标、新pts。
                    已改变 为 真。
                完。
            完。
            若 类型 等于 指针分析_复制：
                src_pts 为 做 字典取 用 pts_map、源。
                若 src_pts 不等于 空 并且 非 做 PTA_位集_为空 用 src_pts：
                    新pts 为 做 PTA_位集_合并 用 dst_pts、src_pts。
                    若 非 做 PTA_位集_相等 用 新pts、dst_pts：
                        做 字典设 用 pts_map、目标、新pts。
                        已改变 为 真。
                    完。
                完。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 一组：pts_map、迭代次数。
完。

PTA_可能别名 接受 pts_map、变量A、变量B：
    pts_A 为 做 字典取 用 pts_map、变量A。
    pts_B 为 做 字典取 用 pts_map、变量B。
    若 pts_A 等于 空 或者 pts_B 等于 空：
        返还 真。
    完。
    若 做 PTA_位集_为空 用 pts_A 或者 做 PTA_位集_为空 用 pts_B：
        返还 假。
    完。
    交集 为 一组：pts_A 的 第 0 项 位与 pts_B 的 第 0 项、pts_A 的 第 1 项 位与 pts_B 的 第 1 项。
    返还 非 做 PTA_位集_为空 用 交集。
完。

PTA_必须别名 接受 pts_map、变量A、变量B：
    pts_A 为 做 字典取 用 pts_map、变量A。
    pts_B 为 做 字典取 用 pts_map、变量B。
    若 pts_A 等于 空 或者 pts_B 等于 空：
        返还 假。
    完。
    若 非 做 PTA_位集_相等 用 pts_A、pts_B：
        返还 假。
    完。
    返还 pts_A 的 第 0 项 不等于 0 并且 pts_A 的 第 0 项 位与 pts_A 的 第 0 项 减 1 等于 0。
完。

PTA_得到指向集 接受 pts_map、变量名：
    pts 为 做 字典取 用 pts_map、变量名。
    若 pts 等于 空：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 bits 为 pts 的 第 0 项。
    可变 id 为 0。
    只要 bits 不等于 0：
        若 bits 位与 1 等于 1：
            做 追加 用 结果、id。
        完。
        bits 为 bits 右移 1。
        id 为 id 加 1。
    完。
    返还 结果。
完。

可变 _PTA_查询次数 为 0。

可变 _PTA_别名否定次数 为 0。

PTA_记录查询 接受 结果_为别名：
    _PTA_查询次数 为 _PTA_查询次数 加 1。
    若 非 结果_为别名：
        _PTA_别名否定次数 为 _PTA_别名否定次数 加 1。
    完。
完。

PTA_统计 接受 零参：
    返还 一组：_PTA_查询次数、_PTA_别名否定次数、_PTA_对象计数、若 _PTA_查询次数 大于 0 则 _PTA_别名否定次数 乘 100 除以 _PTA_查询次数 否则 0。
完。

本章 收束。
