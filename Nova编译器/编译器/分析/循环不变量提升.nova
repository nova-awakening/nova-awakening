《循环不变量提升》

引用《..·..·标准库·基础》。

引用《..·..·编译器·标记常量》。

引用《.·循环分析》。

引用《.·数据流分析》。

不变量_创建 接受 哈希、节点、前置块：
    返还 一组：哈希、节点、前置块、空。
完。

不变量_哈希 接受 inv：
    返还 inv 的 第 0 项。
完。

不变量_节点 接受 inv：
    返还 inv 的 第 1 项。
完。

不变量_前置块 接受 inv：
    返还 inv 的 第 2 项。
完。

不变量_临时变量 接受 inv：
    返还 inv 的 第 3 项。
完。

不变量_设临时变量 接受 inv、名：
    inv 的 第 3 项 为 名。
完。

LICM_变量在循环内被定义 接受 循环体块ID列表、cfg、变量名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 循环体块ID列表：
        块 为 做 数流_CFG_查找块 用 cfg、循环体块ID列表 的 第 i 项。
        若 块 不等于 空：
            kill集 为 做 数流_块_KILL 用 块。
            可变 j 为 0。
            只要 j 小于 做 计数 用 kill集：
                若 kill集 的 第 j 项 等于 变量名：
                    返还 真。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

LICM_节点是纯的 接受 节点：
    若 节点 等于 空 或者 做 计数 用 节点 等于 0：
        返还 假。
    完。
    类型 为 节点 的 第 0 项。
    若 类型 等于 标记_数字 或者 类型 等于 节点_空字面量 或者 类型 等于 标记_名称：
        返还 真。
    完。
    若 类型 等于 节点_二元 并且 做 计数 用 节点 大于或等于 4：
        返还 做 LICM_节点是纯的 用 节点 的 第 2 项 并且 做 LICM_节点是纯的 用 节点 的 第 3 项。
    完。
    若 (类型 等于 节点_一元正 或者 类型 等于 节点_一元负) 并且 做 计数 用 节点 大于或等于 2：
        返还 做 LICM_节点是纯的 用 节点 的 第 1 项。
    完。
    若 类型 等于 节点_调用：
        返还 假。
    完。
    若 类型 等于 标记_当 或者 类型 等于 标记_顺序词：
        返还 假。
    完。
    返还 假。
完。

LICM_表达式是不变量 接受 节点、循环体块ID列表、cfg：
    若 非 做 LICM_节点是纯的 用 节点：
        返还 假。
    完。
    若 节点 的 第 0 项 等于 标记_数字 或者 节点 的 第 0 项 等于 节点_空字面量：
        返还 真。
    完。
    若 节点 的 第 0 项 等于 标记_名称 并且 做 计数 用 节点 大于或等于 2：
        返还 非 做 LICM_变量在循环内被定义 用 循环体块ID列表、cfg、节点 的 第 1 项。
    完。
    若 节点 的 第 0 项 等于 节点_二元 并且 做 计数 用 节点 大于或等于 4：
        返还 做 LICM_表达式是不变量 用 节点 的 第 2 项、循环体块ID列表、cfg 并且 做 LICM_表达式是不变量 用 节点 的 第 3 项、循环体块ID列表、cfg。
    完。
    若 (节点 的 第 0 项 等于 节点_一元正 或者 节点 的 第 0 项 等于 节点_一元负) 并且 做 计数 用 节点 大于或等于 2：
        返还 做 LICM_表达式是不变量 用 节点 的 第 1 项、循环体块ID列表、cfg。
    完。
    返还 假。
完。

LICM_扫描不变量 接受 语句列表、循环体块ID列表、cfg：
    可变 不变量索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列表：
        语句 为 语句列表 的 第 i 项。
        若 做 是列表 用 语句 并且 做 计数 用 语句 大于或等于 3：
            类型 为 语句 的 第 0 项。
            若 类型 等于 标记_定义：
                表达式 为 语句 的 第 2 项。
                若 做 LICM_表达式是不变量 用 表达式、循环体块ID列表、cfg：
                    做 追加 用 不变量索引、i。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 不变量索引。
完。

LICM_提升安全 接受 语句、循环头块ID、cfg：
    头块 为 做 数流_CFG_查找块 用 cfg、循环头块ID。
    若 头块 等于 空：
        返还 假。
    完。
    后继数 为 做 计数 用 做 数流_块_后继 用 头块。
    返还 后继数 小于或等于 2。
完。

可变 _LICM_临时计数 为 0。

LICM_新临时变量 接受 零参：
    _LICM_临时计数 为 _LICM_临时计数 加 1。
    返还 做 字符串拼接 用 「__循环外提_临时」、做 转字符串 用 _LICM_临时计数。
完。

LICM_执行提升 接受 语句列表、不变量索引列表：
    可变 前置语句 为 一组：（空）。
    可变 新语句列表 为 一组：（空）。
    可变 提升映射 为 一组：一组：（空）、一组：（空）。
    可变 提升集 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 不变量索引列表：
        做 字典设 用 提升集、做 转字符串 用 不变量索引列表 的 第 i 项、真。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 语句列表：
        语句 为 语句列表 的 第 i 项。
        若 做 字典取 用 提升集、做 转字符串 用 i 不等于 空：
            临时名 为 做 LICM_新临时变量。
            做 追加 用 前置语句、语句。
            做 追加 用 提升映射 的 第 0 项、语句 的 第 1 项。
            做 追加 用 提升映射 的 第 1 项、临时名。
        否则：
            做 追加 用 新语句列表、语句。
        完。
        i 为 i 加 1。
    完。
    返还 一组：前置语句、新语句列表、提升映射、做 计数 用 前置语句。
完。

LICM_检测归纳变量 接受 语句列表、循环变量：
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列表：
        语句 为 语句列表 的 第 i 项。
        若 做 是列表 用 语句 并且 做 计数 用 语句 大于或等于 3 并且 语句 的 第 0 项 等于 标记_定义：
            表达式 为 语句 的 第 2 项。
            若 做 是列表 用 表达式 并且 表达式 的 第 0 项 等于 节点_二元 并且 做 计数 用 表达式 大于或等于 4：
                若 表达式 的 第 1 项 等于 3：
                    若 做 是列表 用 表达式 的 第 2 项 并且 表达式 的 第 2 项 的 第 0 项 等于 标记_名称 并且 表达式 的 第 2 项 的 第 1 项 等于 循环变量：
                        若 做 是列表 用 表达式 的 第 3 项 并且 表达式 的 第 3 项 的 第 0 项 等于 标记_数字：
                            返还 一组：真、语句 的 第 1 项、表达式 的 第 3 项 的 第 1 项。
                        完。
                    完。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 一组：假、空、0。
完。

LICM_决策展开 接受 循环、语句列表：
    循环深度 为 做 循环_深度 用 循环。
    循环体大小 为 做 计数 用 语句列表。
    若 循环深度 大于 2：
        返还 0。
    完。
    若 循环体大小 大于 8：
        返还 1。
    完。
    若 循环体大小 小于或等于 2：
        返还 4。
    完。
    若 循环体大小 小于或等于 4：
        返还 2。
    完。
    返还 1。
完。

可变 _LICM_提升次数 为 0。

可变 _LICM_删除语句数 为 0。

可变 _LICM_循环数 为 0。

LICM_记录提升 接受 n：
    _LICM_提升次数 为 _LICM_提升次数 加 n。
    _LICM_删除语句数 为 _LICM_删除语句数 加 n。
完。

LICM_记录循环 接受 零参：
    _LICM_循环数 为 _LICM_循环数 加 1。
完。

LICM_重置 接受 零参：
    _LICM_临时计数 为 0。
    _LICM_提升次数 为 0。
    _LICM_删除语句数 为 0。
    _LICM_循环数 为 0。
完。

LICM_报告 接受 零参：
    返还 一组：_LICM_提升次数、_LICM_删除语句数、_LICM_循环数。
完。

强度削减_是2的幂 接受 n：
    若 n 小于或等于 0：
        返还 负 1。
    完。
    可变 v 为 n。
    可变 k 为 0。
    只要 v 取余于 2 等于 0：
        v 为 v 除以 2。
        k 为 k 加 1。
    完。
    若 v 等于 1：
        返还 k。
    完。
    返还 负 1。
完。

强度削减_可削减乘法 接受 乘法表达式、被乘数是循环变量：
    若 非 被乘数是循环变量：
        返还 假。
    完。
    常量 为 乘法表达式。
    返还 做 强度削减_是2的幂 用 常量 大于或等于 0。
完。

强度削减_是线性归纳变量 接受 变量名、循环体块列表：
    可变 赋值次数 为 0。
    可变 是线性 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 循环体块列表：
        语句列表 为 做 数流_块_语句 用 循环体块列表 的 第 i 项。
        若 语句列表 不等于 空：
            可变 j 为 0。
            只要 j 小于 做 计数 用 语句列表：
                语句 为 语句列表 的 第 j 项。
                若 做 是列表 用 语句 并且 做 计数 用 语句 大于或等于 3 并且 语句 的 第 0 项 等于 标记_可变 并且 语句 的 第 1 项 等于 变量名：
                    赋值次数 为 赋值次数 加 1。
                    表达式 为 语句 的 第 2 项。
                    若 做 是列表 用 表达式 并且 做 计数 用 表达式 大于或等于 4 并且 表达式 的 第 0 项 等于 节点_二元：
                        op 为 表达式 的 第 1 项。
                        若 (op 等于 「+」 或者 op 等于 「-」) 并且 做 是列表 用 表达式 的 第 2 项 并且 表达式 的 第 2 项 的 第 0 项 等于 标记_名称 并且 表达式 的 第 2 项 的 第 1 项 等于 变量名 并且 做 是列表 用 表达式 的 第 3 项 并且 表达式 的 第 3 项 的 第 0 项 等于 标记_数字：
                            是线性 为 真。
                        完。
                    完。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 赋值次数 等于 1 并且 是线性。
完。

LICM_效率评分 接受 零参：
    若 _LICM_循环数 等于 0：
        返还 0。
    完。
    每循环提升 为 _LICM_提升次数 乘 10 除以 _LICM_循环数。
    若 每循环提升 大于 100：
        返还 100。
    完。
    返还 每循环提升。
完。

本章 收束。
