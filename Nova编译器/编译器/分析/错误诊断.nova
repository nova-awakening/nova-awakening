《错误诊断》

引用《..·..·标准库·基础》。

诊断_错误 为 0。

诊断_警告 为 1。

诊断_提示 为 2。

诊断_建议 为 3。

错误码_语法 为 1000。

错误码_类型 为 2000。

错误码_未定义 为 3000。

错误码_重定义 为 3001。

错误码_可变性 为 4000。

错误码_生命期 为 5000。

错误码_模块 为 6000。

错误码_溢出 为 7000。

错误码_除零 为 7001。

错误码_越界 为 7002。

跨度_创建 接受 文件、起始行、起始列、结束行、结束列：
    返还 一组：文件、起始行、起始列、结束行、结束列。
完。

跨度_文件 接受 s：
    返还 s 的 第 0 项。
完。

跨度_起始行 接受 s：
    返还 s 的 第 1 项。
完。

跨度_起始列 接受 s：
    返还 s 的 第 2 项。
完。

跨度_结束行 接受 s：
    返还 s 的 第 3 项。
完。

跨度_结束列 接受 s：
    返还 s 的 第 4 项。
完。

跨度_是单行 接受 s：
    返还 s 的 第 1 项 等于 s 的 第 3 项。
完。

跨度_合并 接受 a、b：
    若 a 的 第 0 项 不等于 b 的 第 0 项：
        返还 a。
    完。
    可变 起始行 为 b 的 第 1 项。
    若 a 的 第 1 项 小于 b 的 第 1 项：
        起始行 为 a 的 第 1 项。
    完。
    可变 起始列 为 b 的 第 2 项。
    若 a 的 第 1 项 小于 b 的 第 1 项：
        起始列 为 a 的 第 2 项。
    否则若 a 的 第 1 项 等于 b 的 第 1 项 并且 a 的 第 2 项 小于 b 的 第 2 项：
        起始列 为 a 的 第 2 项。
    完。
    可变 结束行 为 b 的 第 3 项。
    若 a 的 第 3 项 大于 b 的 第 3 项：
        结束行 为 a 的 第 3 项。
    完。
    可变 结束列 为 b 的 第 4 项。
    若 a 的 第 3 项 大于 b 的 第 3 项：
        结束列 为 a 的 第 4 项。
    否则若 a 的 第 3 项 等于 b 的 第 3 项 并且 a 的 第 4 项 大于 b 的 第 4 项：
        结束列 为 a 的 第 4 项。
    完。
    返还 一组：a 的 第 0 项、起始行、起始列、结束行、结束列。
完。

标注_创建 接受 跨度、文本、是主要：
    返还 一组：跨度、文本、是主要。
完。

标注_主要 接受 跨度、文本：
    返还 做 标注_创建 用 跨度、文本、真。
完。

标注_次要 接受 跨度、文本：
    返还 做 标注_创建 用 跨度、文本、假。
完。

修复_替换 为 0。

修复_插入 为 1。

修复_删除 为 2。

修复_仅提示 为 3。

修复_替换文本 接受 跨度、新文本、说明：
    返还 一组：修复_替换、跨度、新文本、说明。
完。

修复_插入文本 接受 跨度、新文本、说明：
    返还 一组：修复_插入、跨度、新文本、说明。
完。

修复_删除区间 接受 跨度、说明：
    返还 一组：修复_删除、跨度、「」、说明。
完。

修复_提示 接受 跨度、新文本、说明：
    返还 一组：修复_仅提示、跨度、新文本、说明。
完。

诊断_创建 接受 级别、错误码、消息、_扩4、_扩5、_扩6：
    返还 一组：级别、错误码、消息、一组：（空）、一组：（空）、一组：（空）。
完。

诊断_添加标注 接受 d、标注：
    做 追加 用 d 的 第 3 项、标注。
完。

诊断_添加修复 接受 d、修复：
    做 追加 用 d 的 第 4 项、修复。
完。

诊断_添加说明 接受 d、文本：
    做 追加 用 d 的 第 5 项、文本。
完。

诊断_级别 接受 d：
    返还 d 的 第 0 项。
完。

诊断_错误码 接受 d：
    返还 d 的 第 1 项。
完。

诊断_消息 接受 d：
    返还 d 的 第 2 项。
完。

诊断_标注列表 接受 d：
    返还 d 的 第 3 项。
完。

诊断_修复列表 接受 d：
    返还 d 的 第 4 项。
完。

诊断_说明列表 接受 d：
    返还 d 的 第 5 项。
完。

诊断_未定义符号 接受 跨度、符号名：
    d 为 做 诊断_创建 用 诊断_错误、错误码_未定义、做 字符串拼接 用 「未定义的符号: `」、做 字符串拼接 用 符号名、「`」、0、0、0。
    做 诊断_添加标注 用 d、做 标注_主要 用 跨度、「在此使用了未定义符号」。
    做 诊断_添加说明 用 d、做 字符串拼接 用 「提示: 确认 `」、做 字符串拼接 用 符号名、「` 是否已定义，或是否存在拼写错误」。
    返还 d。
完。

诊断_类型不匹配 接受 跨度、期望类型、实际类型：
    d 为 做 诊断_创建 用 诊断_错误、错误码_类型、做 字符串拼接 用 「类型不匹配: 期望 `」、做 字符串拼接 用 期望类型、做 字符串拼接 用 「` 但得到 `」、做 字符串拼接 用 实际类型、「`」、0、0、0。
    做 诊断_添加标注 用 d、做 标注_主要 用 跨度、做 字符串拼接 用 「此处类型为 `」、做 字符串拼接 用 实际类型、「`」。
    返还 d。
完。

诊断_不可变变量 接受 跨度、变量名：
    d 为 做 诊断_创建 用 诊断_错误、错误码_可变性、做 字符串拼接 用 「不可变变量 `」、做 字符串拼接 用 变量名、「` 不能被赋值」、0、0、0。
    做 诊断_添加标注 用 d、做 标注_主要 用 跨度、「尝试修改不可变变量」。
    做 诊断_添加修复 用 d、做 修复_提示 用 空、0、做 字符串拼接 用 「将声明改为 `定义 可变 」、做 字符串拼接 用 变量名、「 = ...`」。
    返还 d。
完。

诊断_符号重定义 接受 新跨度、旧跨度、符号名：
    d 为 做 诊断_创建 用 诊断_错误、错误码_重定义、做 字符串拼接 用 「符号 `」、做 字符串拼接 用 符号名、「` 在同一作用域内重复定义」、0、0、0。
    做 诊断_添加标注 用 d、做 标注_主要 用 新跨度、「第二次定义」。
    做 诊断_添加标注 用 d、做 标注_次要 用 旧跨度、「首次定义在此」。
    返还 d。
完。

诊断_越界访问 接受 跨度、索引、长度：
    d 为 做 诊断_创建 用 诊断_错误、错误码_越界、做 字符串拼接 用 「索引越界: 索引 」、做 字符串拼接 用 做 转字符串 用 索引、做 字符串拼接 用 「 超出长度 」、做 转字符串 用 长度、0、0、0。
    做 诊断_添加标注 用 d、做 标注_主要 用 跨度、「此处发生越界访问」。
    返还 d。
完。

诊断_语法错误 接受 跨度、描述：
    d 为 做 诊断_创建 用 诊断_错误、错误码_语法、描述、0、0、0。
    做 诊断_添加标注 用 d、做 标注_主要 用 跨度、「语法错误」。
    返还 d。
完。

诊断收集器_创建 接受 零参：
    返还 一组：一组：（空）、0、0、一组：（空）。
完。

诊断收集器_注册文件 接受 收集器、文件名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 收集器 的 第 3 项：
        若 收集器 的 第 3 项 的 第 i 项 等于 文件名：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 收集器 的 第 3 项、文件名。
    返还 做 计数 用 收集器 的 第 3 项 减 1。
完。

诊断收集器_添加 接受 收集器、诊断：
    做 追加 用 收集器 的 第 0 项、诊断。
    若 做 诊断_级别 用 诊断 等于 诊断_错误：
        收集器 的 第 1 项 为 收集器 的 第 1 项 加 1。
    否则若 做 诊断_级别 用 诊断 等于 诊断_警告：
        收集器 的 第 2 项 为 收集器 的 第 2 项 加 1。
    完。
完。

诊断收集器_错误数 接受 收集器：
    返还 收集器 的 第 1 项。
完。

诊断收集器_警告数 接受 收集器：
    返还 收集器 的 第 2 项。
完。

诊断收集器_有错误 接受 收集器：
    返还 收集器 的 第 1 项 大于 0。
完。

诊断收集器_按行排序 接受 收集器：
    列表 为 收集器 的 第 0 项。
    n 为 做 计数 用 列表。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            ai 为 做 诊断_标注列表 用 列表 的 第 i 项。
            aj 为 做 诊断_标注列表 用 列表 的 第 j 项。
            若 做 计数 用 ai 大于 0 并且 做 计数 用 aj 大于 0：
                若 做 跨度_起始行 用 ai 的 第 0 项 的 第 0 项 大于 做 跨度_起始行 用 aj 的 第 0 项 的 第 0 项：
                    暂存 为 列表 的 第 i 项。
                    列表 的 第 i 项 为 列表 的 第 j 项。
                    列表 的 第 j 项 为 暂存。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

级别名 为 一组：「error」、「warning」、「提示」、「建议」。

级别前缀 为 一组：「[错误]」、「[警告]」、「[提示]」、「[建议]」。

诊断_格式化单条 接受 收集器、诊断：
    级别 为 做 诊断_级别 用 诊断。
    前缀 为 级别前缀 的 第 级别 项。
    行 为 做 字符串拼接 用 前缀、做 字符串拼接 用 「 」、做 诊断_消息 用 诊断。
    标注列表 为 做 诊断_标注列表 用 诊断。
    可变 结果 为 一组：行。
    可变 i 为 0。
    只要 i 小于 做 计数 用 标注列表：
        标注 为 标注列表 的 第 i 项。
        跨度 为 标注 的 第 0 项。
        文本 为 标注 的 第 1 项。
        若 跨度 不等于 空 并且 跨度 不等于 空：
            可变 文件 为 「?」。
            若 做 跨度_文件 用 跨度 小于 做 计数 用 收集器 的 第 3 项：
                文件 为 收集器 的 第 3 项 的 第 做 跨度_文件 用 跨度 项。
            完。
            位置 为 做 字符串拼接 用 文件、做 字符串拼接 用 「:」、做 字符串拼接 用 做 转字符串 用 做 跨度_起始行 用 跨度、做 字符串拼接 用 「:」、做 转字符串 用 做 跨度_起始列 用 跨度。
            做 追加 用 结果、做 字符串拼接 用 「  --> 」、位置。
            做 追加 用 结果、做 字符串拼接 用 「  | 」、文本。
        完。
        i 为 i 加 1。
    完。
    修复列表 为 做 诊断_修复列表 用 诊断。
    i 为 0。
    只要 i 小于 做 计数 用 修复列表：
        修复 为 修复列表 的 第 i 项。
        做 追加 用 结果、做 字符串拼接 用 「  = 」、修复 的 第 3 项。
        i 为 i 加 1。
    完。
    说明列表 为 做 诊断_说明列表 用 诊断。
    i 为 0。
    只要 i 小于 做 计数 用 说明列表：
        做 追加 用 结果、做 字符串拼接 用 「  = 说明: 」、说明列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

诊断_打印全部 接受 收集器：
    列表 为 收集器 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        行列表 为 做 诊断_格式化单条 用 收集器、列表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 行列表：
            做 打印文本 用 行列表 的 第 j 项。
            j 为 j 加 1。
        完。
        做 打印文本 用 「」。
        i 为 i 加 1。
    完。
    若 收集器 的 第 1 项 大于 0 或者 收集器 的 第 2 项 大于 0：
        做 打印文本 用 做 字符串拼接 用 「编译结果: 」、做 字符串拼接 用 做 转字符串 用 收集器 的 第 1 项、做 字符串拼接 用 「 个错误, 」、做 字符串拼接 用 做 转字符串 用 收集器 的 第 2 项、「 个警告」。
    完。
完。

诊断_未使用变量 接受 跨度、变量名：
    d 为 做 诊断_创建 用 诊断_警告、3100、做 字符串拼接 用 「变量 `」、做 字符串拼接 用 变量名、「` 未使用」。
    做 诊断_添加标注 用 d、做 标注_主要 用 跨度、「此变量被声明但从未使用」。
    做 诊断_添加修复 用 d、做 修复_提示 用 空、0、做 字符串拼接 用 「如果有意忽略，将变量名改为 `_」、做 字符串拼接 用 变量名、「`」。
    返还 d。
完。

诊断_影子变量 接受 新跨度、旧跨度、变量名：
    d 为 做 诊断_创建 用 诊断_提示、3200、做 字符串拼接 用 「变量 `」、做 字符串拼接 用 变量名、「` 遮蔽了外层同名变量」。
    做 诊断_添加标注 用 d、做 标注_主要 用 新跨度、「内层声明」。
    做 诊断_添加标注 用 d、做 标注_次要 用 旧跨度、「外层声明在此」。
    返还 d。
完。

诊断_潜在除零 接受 跨度：
    d 为 做 诊断_创建 用 诊断_警告、错误码_除零、「潜在的除零风险」、0、0、0。
    做 诊断_添加标注 用 d、做 标注_主要 用 跨度、「除数可能为零」。
    做 诊断_添加修复 用 d、做 修复_提示 用 空、0、「建议在除法前检查除数是否为零」。
    返还 d。
完。

可变 _诊断_总发出 为 0。

可变 _诊断_总错误 为 0。

可变 _诊断_总警告 为 0。

诊断_记录 接受 级别：
    _诊断_总发出 为 _诊断_总发出 加 1。
    若 级别 等于 诊断_错误：
        _诊断_总错误 为 _诊断_总错误 加 1。
    完。
    若 级别 等于 诊断_警告：
        _诊断_总警告 为 _诊断_总警告 加 1。
    完。
完。

诊断_全局统计 接受 零参：
    返还 一组：_诊断_总发出、_诊断_总错误、_诊断_总警告。
完。

诊断_最小3 接受 a、b、c：
    可变 m 为 a。
    若 b 小于 m：
        m 为 b。
    完。
    若 c 小于 m：
        m 为 c。
    完。
    返还 m。
完。

诊断_编辑距离 接受 a、b：
    m 为 做 字节长度 用 a。
    n 为 做 字节长度 用 b。
    若 m 等于 0：
        返还 n。
    完。
    若 n 等于 0：
        返还 m。
    完。
    可变 dp 为 一组：（空）。
    可变 初始化i 为 0。
    总长 为 (m 加 1) 乘 (n 加 1)。
    只要 初始化i 小于 总长：
        做 追加 用 dp、0。
        初始化i 为 初始化i 加 1。
    完。
    可变 bi 为 0。
    只要 bi 小于或等于 m：
        dp 的 第 bi 乘 (n 加 1) 项 为 bi。
        bi 为 bi 加 1。
    完。
    可变 bj 为 0。
    只要 bj 小于或等于 n：
        dp 的 第 bj 项 为 bj。
        bj 为 bj 加 1。
    完。
    可变 i 为 1。
    只要 i 小于或等于 m：
        可变 j 为 1。
        只要 j 小于或等于 n：
            可变 cost 为 1。
            若 做 字节取 用 a、i 减 1 等于 做 字节取 用 b、j 减 1：
                cost 为 0。
            完。
            删除代价 为 dp 的 第 (i 减 1) 乘 (n 加 1) 加 j 项 加 1。
            插入代价 为 dp 的 第 i 乘 (n 加 1) 加 (j 减 1) 项 加 1。
            替换代价 为 dp 的 第 (i 减 1) 乘 (n 加 1) 加 (j 减 1) 项 加 cost。
            dp 的 第 i 乘 (n 加 1) 加 j 项 为 做 诊断_最小3 用 删除代价、插入代价、替换代价。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 dp 的 第 m 乘 (n 加 1) 加 n 项。
完。

诊断_查找相似符号 接受 目标名、符号列表、前K：
    可变 候选 为 一组：（空）。
    目标长 为 做 字节长度 用 目标名。
    阈值基础 为 目标长 除以 2。
    可变 有效阈值 为 阈值基础。
    若 有效阈值 小于 2：
        有效阈值 为 2。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 符号列表：
        名 为 符号列表 的 第 i 项。
        名长 为 做 字节长度 用 名。
        可变 长差 为 名长 减 目标长。
        若 长差 小于 0：
            长差 为 目标长 减 名长。
        完。
        若 长差 小于或等于 有效阈值：
            距离 为 做 诊断_编辑距离 用 目标名、名。
            若 距离 大于 0 并且 距离 小于或等于 有效阈值：
                做 追加 用 候选、一组：名、距离。
            完。
        完。
        i 为 i 加 1。
    完。
    n 为 做 计数 用 候选。
    可变 ii 为 0。
    只要 ii 小于 n 减 1：
        可变 jj 为 0。
        只要 jj 小于 n 减 ii 减 1：
            若 候选 的 第 jj 项 的 第 1 项 大于 候选 的 第 jj 加 1 项 的 第 1 项：
                暂存 为 候选 的 第 jj 项。
                候选 的 第 jj 项 为 候选 的 第 jj 加 1 项。
                候选 的 第 jj 加 1 项 为 暂存。
            完。
            jj 为 jj 加 1。
        完。
        ii 为 ii 加 1。
    完。
    可变 结果 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 前K 并且 k 小于 做 计数 用 候选：
        做 追加 用 结果、候选 的 第 k 项 的 第 0 项。
        k 为 k 加 1。
    完。
    返还 结果。
完。

诊断_未定义符号_带推荐 接受 跨度、符号名、可用符号列表：
    d 为 做 诊断_未定义符号 用 跨度、符号名。
    相似 为 做 诊断_查找相似符号 用 符号名、可用符号列表、3。
    可变 i 为 0。
    只要 i 小于 做 计数 用 相似：
        建议 为 做 字符串拼接 用 「您是否想用 `」、做 字符串拼接 用 相似 的 第 i 项、「`?」。
        做 诊断_添加修复 用 d、做 修复_提示 用 空、0、建议。
        i 为 i 加 1。
    完。
    返还 d。
完。

诊断_提取行 接受 源码、行号：
    长 为 做 字节长度 用 源码。
    可变 当前行 为 1。
    可变 行开始 为 0。
    可变 i 为 0。
    只要 i 小于 长 并且 当前行 小于 行号：
        若 做 字节取 用 源码、i 等于 10：
            当前行 为 当前行 加 1。
            行开始 为 i 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 行结束 为 行开始。
    只要 行结束 小于 长 并且 做 字节取 用 源码、行结束 不等于 10：
        行结束 为 行结束 加 1。
    完。
    返还 做 子串 用 源码、行开始、行结束 减 行开始。
完。

诊断_生成箭头 接受 起始列、错误长度：
    可变 空格 为 「」。
    可变 sp 为 1。
    只要 sp 小于 起始列：
        空格 为 做 字符串拼接 用 空格、「 」。
        sp 为 sp 加 1。
    完。
    可变 箭头 为 「」。
    可变 ar 为 0。
    可变 长 为 错误长度。
    若 长 小于或等于 0：
        长 为 1。
    完。
    只要 ar 小于 长：
        箭头 为 做 字符串拼接 用 箭头、「^」。
        ar 为 ar 加 1。
    完。
    返还 做 字符串拼接 用 空格、箭头。
完。

诊断_格式化源码片段 接受 源码、行号、起始列、错误长度、消息：
    源码行 为 做 诊断_提取行 用 源码、行号。
    行号字符串 为 做 转字符串 用 行号。
    可变 空白前缀 为 「」。
    可变 spi 为 0。
    只要 spi 小于或等于 做 字节长度 用 行号字符串：
        空白前缀 为 做 字符串拼接 用 空白前缀、「 」。
        spi 为 spi 加 1。
    完。
    箭头 为 做 诊断_生成箭头 用 起始列、错误长度。
    可变 结果 为 一组：（空）。
    做 追加 用 结果、做 字符串拼接 用 行号字符串、做 字符串拼接 用 「 | 」、源码行。
    做 追加 用 结果、做 字符串拼接 用 空白前缀、做 字符串拼接 用 「| 」、做 字符串拼接 用 箭头、做 字符串拼接 用 「 」、消息。
    返还 结果。
完。

可变 诊断_使用颜色 为 1。

诊断_颜色_重置 为 「\x1b[0m」。

诊断_颜色_红 为 「\x1b[31m」。

诊断_颜色_黄 为 「\x1b[33m」。

诊断_颜色_蓝 为 「\x1b[34m」。

诊断_颜色_绿 为 「\x1b[32m」。

诊断_颜色_青 为 「\x1b[36m」。

诊断_颜色_粗体 为 「\x1b[1m」。

诊断_关闭颜色 接受 零参：
    诊断_使用颜色 为 0。
完。

诊断_开启颜色 接受 零参：
    诊断_使用颜色 为 1。
完。

诊断_上色 接受 颜色代码、文本：
    若 诊断_使用颜色 等于 0：
        返还 文本。
    完。
    返还 做 字符串拼接 用 颜色代码、做 字符串拼接 用 文本、诊断_颜色_重置。
完。

诊断_着色_错误 接受 文本：
    返还 做 诊断_上色 用 诊断_颜色_红、文本。
完。

诊断_着色_警告 接受 文本：
    返还 做 诊断_上色 用 诊断_颜色_黄、文本。
完。

诊断_着色_提示 接受 文本：
    返还 做 诊断_上色 用 诊断_颜色_蓝、文本。
完。

诊断_着色_成功 接受 文本：
    返还 做 诊断_上色 用 诊断_颜色_绿、文本。
完。

诊断_着色_粗体 接受 文本：
    返还 做 诊断_上色 用 诊断_颜色_粗体、文本。
完。

本章 收束。
