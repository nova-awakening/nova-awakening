《支配树》

引用《..·..·标准库·基础》。

引用《数据流分析》。

支配_节点_创建 接受 块ID：
    返还 一组：块ID、负 1、一组：（空）、一组：（空）。
完。

支配_节点_ID 接受 n：
    返还 n 的 第 0 项。
完。

支配_节点_IDom 接受 n：
    返还 n 的 第 1 项。
完。

支配_节点_子节点 接受 n：
    返还 n 的 第 2 项。
完。

支配_节点_支配前沿 接受 n：
    返还 n 的 第 3 项。
完。

支配_节点_设IDom 接受 n、直接支配：
    n 的 第 1 项 为 直接支配。
完。

支配树_创建 接受 入口ID：
    返还 一组：一组：（空）、入口ID、一组：（空）。
完。

支配树_节点列表 接受 树：
    返还 树 的 第 0 项。
完。

支配树_入口 接受 树：
    返还 树 的 第 1 项。
完。

支配树_查找 接受 树、块ID：
    可变 i 为 0。
    节点列表 为 树 的 第 0 项。
    只要 i 小于 做 计数 用 节点列表：
        若 做 支配_节点_ID 用 节点列表 的 第 i 项 等于 块ID：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 0 减 1。
完。

支配树_DFS后序 接受 cfg、块ID、后序列表、访问列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 访问列表：
        若 访问列表 的 第 i 项 等于 块ID：
            返还 空。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 访问列表、块ID。
    块 为 做 数流_CFG_查找块 用 cfg、块ID。
    若 块 等于 空：
        返还 空。
    完。
    后继列表 为 做 数流_块_后继 用 块。
    i 为 0。
    只要 i 小于 做 计数 用 后继列表：
        做 支配树_DFS后序 用 cfg、后继列表 的 第 i 项、后序列表、访问列表。
        i 为 i 加 1。
    完。
    做 追加 用 后序列表、块ID。
完。

支配树_生成后序 接受 cfg、入口ID：
    后序 为 一组：（空）。
    访问 为 一组：（空）。
    做 支配树_DFS后序 用 cfg、入口ID、后序、访问。
    返还 后序。
完。

支配_公共 接受 后序索引、idoms、a、b：
    可变 finger1 为 a。
    可变 finger2 为 b。
    只要 finger1 不等于 finger2：
        只要 后序索引 的 第 finger1 项 小于 后序索引 的 第 finger2 项：
            finger1 为 idoms 的 第 finger1 项。
        完。
        只要 后序索引 的 第 finger2 项 小于 后序索引 的 第 finger1 项：
            finger2 为 idoms 的 第 finger2 项。
        完。
    完。
    返还 finger1。
完。

支配树_计算idom 接受 cfg、入口ID：
    块列表 为 做 数流_CFG_块列表 用 cfg。
    n 为 做 计数 用 块列表。
    后序 为 做 支配树_生成后序 用 cfg、入口ID。
    后序数 为 做 计数 用 后序。
    可变 后序索引 为 做 字典。
    可变 i 为 0。
    只要 i 小于 后序数：
        做 字典设 用 后序索引、做 转字符串 用 后序 的 第 i 项、i。
        i 为 i 加 1。
    完。
    可变 直接支配 为 做 字典。
    做 字典设 用 直接支配、做 转字符串 用 入口ID、入口ID。
    可变 变化 为 真。
    可变 迭代数 为 0。
    最大迭代 为 n 乘 2。
    只要 变化 并且 迭代数 小于 最大迭代：
        变化 为 假。
        i 为 后序数 减 1。
        只要 i 大于或等于 0：
            块ID 为 后序 的 第 i 项。
            若 块ID 等于 入口ID：
                i 为 i 减 1。
                继续。
            完。
            块 为 做 数流_CFG_查找块 用 cfg、块ID。
            若 块 等于 空：
                i 为 i 减 1。
                继续。
            完。
            前驱列表 为 做 数流_块_前驱 用 块。
            若 做 计数 用 前驱列表 等于 0：
                i 为 i 减 1。
                继续。
            完。
            可变 new_idom 为 0 减 1。
            可变 j 为 0。
            只要 j 小于 做 计数 用 前驱列表：
                p 为 前驱列表 的 第 j 项。
                若 做 字典取 用 直接支配、做 转字符串 用 p 不等于 空：
                    new_idom 为 p。
                完。
                j 为 j 加 1。
            完。
            若 new_idom 小于 0：
                i 为 i 减 1。
                继续。
            完。
            j 为 0。
            只要 j 小于 做 计数 用 前驱列表：
                p 为 前驱列表 的 第 j 项。
                若 p 不等于 new_idom 并且 做 字典取 用 直接支配、做 转字符串 用 p 不等于 空：
                    可变 f1 为 new_idom。
                    可变 f2 为 p。
                    可变 f1序 为 做 字典取 用 后序索引、做 转字符串 用 f1。
                    可变 f2序 为 做 字典取 用 后序索引、做 转字符串 用 f2。
                    若 f1序 不等于 空 并且 f2序 不等于 空：
                        只要 f1 不等于 f2：
                            只要 f1序 小于 f2序：
                                上 为 做 字典取 用 直接支配、做 转字符串 用 f1。
                                若 上 等于 空：
                                    f1 为 f2。
                                否则：
                                    f1 为 上。
                                    f1序 为 做 字典取 用 后序索引、做 转字符串 用 f1。
                                    若 f1序 等于 空：
                                        f1 为 f2。
                                    完。
                                完。
                            完。
                            只要 f2序 小于 f1序：
                                上 为 做 字典取 用 直接支配、做 转字符串 用 f2。
                                若 上 等于 空：
                                    f2 为 f1。
                                否则：
                                    f2 为 上。
                                    f2序 为 做 字典取 用 后序索引、做 转字符串 用 f2。
                                    若 f2序 等于 空：
                                        f2 为 f1。
                                    完。
                                完。
                            完。
                        完。
                        new_idom 为 f1。
                    完。
                完。
                j 为 j 加 1。
            完。
            旧idom 为 做 字典取 用 直接支配、做 转字符串 用 块ID。
            若 旧idom 等于 空 或者 旧idom 不等于 new_idom：
                做 字典设 用 直接支配、做 转字符串 用 块ID、new_idom。
                变化 为 真。
            完。
            i 为 i 减 1。
        完。
        迭代数 为 迭代数 加 1。
    完。
    返还 一组：直接支配、后序索引、迭代数。
完。

支配树_构建 接受 cfg、入口ID：
    块列表 为 做 数流_CFG_块列表 用 cfg。
    n 为 做 计数 用 块列表。
    结果 为 做 支配树_计算idom 用 cfg、入口ID。
    直接支配 为 结果 的 第 0 项。
    可变 节点列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 节点列表、做 支配_节点_创建 用 做 数流_块_ID 用 块列表 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        块ID 为 做 数流_块_ID 用 块列表 的 第 i 项。
        直接支配者 为 做 字典取 用 直接支配、做 转字符串 用 块ID。
        若 直接支配者 不等于 空 并且 直接支配者 不等于 块ID：
            节点列表 的 第 i 项 的 第 1 项 为 直接支配者。
            可变 j 为 0。
            只要 j 小于 n：
                若 做 数流_块_ID 用 块列表 的 第 j 项 等于 直接支配者：
                    做 追加 用 节点列表 的 第 j 项 的 第 2 项、块ID。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    树 为 做 支配树_创建 用 入口ID。
    树 的 第 0 项 为 节点列表。
    返还 树。
完。

支配树_a支配b 接受 树、a_ID、b_ID：
    若 a_ID 等于 b_ID：
        返还 真。
    完。
    节点列表 为 做 支配树_节点列表 用 树。
    可变 当前 为 b_ID。
    可变 步数 为 0。
    最大步 为 做 计数 用 节点列表 加 1。
    只要 当前 不等于 做 支配树_入口 用 树 并且 步数 小于 最大步：
        索引 为 做 支配树_查找 用 树、当前。
        若 索引 小于 0：
            返还 假。
        完。
        直接支配 为 做 支配_节点_IDom 用 节点列表 的 第 索引 项。
        若 直接支配 等于 a_ID：
            返还 真。
        完。
        若 直接支配 小于 0 或者 直接支配 等于 当前：
            返还 假。
        完。
        当前 为 直接支配。
        步数 为 步数 加 1。
    完。
    返还 当前 等于 a_ID。
完。

支配树_支配者链 接受 树、块ID：
    可变 链 为 一组：块ID。
    节点列表 为 做 支配树_节点列表 用 树。
    可变 当前 为 块ID。
    入口 为 做 支配树_入口 用 树。
    可变 步数 为 0。
    最大步 为 做 计数 用 节点列表 加 1。
    只要 当前 不等于 入口 并且 步数 小于 最大步：
        索引 为 做 支配树_查找 用 树、当前。
        若 索引 小于 0：
            返还 链。
        完。
        直接支配 为 做 支配_节点_IDom 用 节点列表 的 第 索引 项。
        若 直接支配 小于 0：
            返还 链。
        完。
        做 追加 用 链、直接支配。
        当前 为 直接支配。
        步数 为 步数 加 1。
    完。
    返还 链。
完。

支配树_计算支配前沿 接受 树、cfg：
    块列表 为 做 数流_CFG_块列表 用 cfg。
    n 为 做 计数 用 块列表。
    节点列表 为 做 支配树_节点列表 用 树。
    可变 i 为 0。
    只要 i 小于 n：
        块 为 块列表 的 第 i 项。
        前驱列表 为 做 数流_块_前驱 用 块。
        若 做 计数 用 前驱列表 大于或等于 2：
            块ID 为 做 数流_块_ID 用 块。
            可变 j 为 0。
            只要 j 小于 做 计数 用 前驱列表：
                前驱ID 为 前驱列表 的 第 j 项。
                可变 当前 为 前驱ID。
                块idx 为 做 支配树_查找 用 树、块ID。
                可变 块idom 为 0 减 1。
                若 块idx 大于或等于 0：
                    块idom 为 做 支配_节点_IDom 用 节点列表 的 第 块idx 项。
                完。
                可变 步 为 0。
                只要 当前 不等于 块idom 并且 步 小于 n：
                    cur_idx 为 做 支配树_查找 用 树、当前。
                    若 cur_idx 小于 0：
                        步 为 n。
                    否则：
                        可变 已有 为 假。
                        可变 k 为 0。
                        只要 k 小于 做 计数 用 节点列表 的 第 cur_idx 项 的 第 3 项：
                            若 节点列表 的 第 cur_idx 项 的 第 3 项 的 第 k 项 等于 块ID：
                                已有 为 真。
                            完。
                            k 为 k 加 1。
                        完。
                        若 已有 等于 假：
                            做 追加 用 节点列表 的 第 cur_idx 项 的 第 3 项、块ID。
                        完。
                        cur_idom 为 做 支配_节点_IDom 用 节点列表 的 第 cur_idx 项。
                        若 cur_idom 小于 0：
                            步 为 n。
                        否则：
                            当前 为 cur_idom。
                        完。
                    完。
                    步 为 步 加 1。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

支配树_DFS遍历 接受 树、块ID、回调结果列表：
    做 追加 用 回调结果列表、块ID。
    索引 为 做 支配树_查找 用 树、块ID。
    若 索引 小于 0：
        返还 空。
    完。
    子节点 为 做 支配_节点_子节点 用 做 支配树_节点列表 用 树 的 第 索引 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子节点：
        做 支配树_DFS遍历 用 树、子节点 的 第 i 项、回调结果列表。
        i 为 i 加 1。
    完。
完。

本章 收束。
