《指令调度》

引用《..·..·标准库·基础》。

延迟_整数加减 为 1。

延迟_整数乘 为 3。

延迟_整数除 为 20。

延迟_内存读 为 4。

延迟_内存写 为 1。

延迟_跳转 为 0。

延迟_比较 为 1。

延迟_移位 为 1。

延迟_位运算 为 1。

延迟_条件移动 为 1。

依赖_数据 为 0。

依赖_反依 为 1。

依赖_输出 为 2。

依赖_控制 为 3。

依赖_内存 为 4。

IS_节点_创建 接受 操作码、操作数、延迟：
    返还 一组：操作码、操作数、延迟、0、假、一组：（空）、一组：（空）。
完。

IS_节点_操作码 接受 n：
    返还 n 的 第 0 项。
完。

IS_节点_延迟 接受 n：
    返还 n 的 第 2 项。
完。

IS_节点_最早发射 接受 n：
    返还 n 的 第 3 项。
完。

IS_节点_已调度 接受 n：
    返还 n 的 第 4 项。
完。

IS_节点_前驱 接受 n：
    返还 n 的 第 5 项。
完。

IS_节点_后继 接受 n：
    返还 n 的 第 6 项。
完。

IS_节点_设最早发射 接受 n、t：
    n 的 第 3 项 为 t。
完。

IS_节点_标记已调度 接受 n：
    n 的 第 4 项 为 真。
完。

IS_添加依赖 接受 节点列表、from_idx、to_idx、依赖类型：
    做 追加 用 节点列表 的 第 from_idx 项 的 第 6 项、一组：to_idx、依赖类型。
    做 追加 用 节点列表 的 第 to_idx 项 的 第 5 项、一组：from_idx、依赖类型。
完。

IS_构建依赖图 接受 指令列表：
    n 为 做 计数 用 指令列表。
    可变 节点列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        指令 为 指令列表 的 第 i 项。
        延迟 为 做 IS_操作码延迟 用 指令 的 第 0 项。
        做 追加 用 节点列表、做 IS_节点_创建 用 指令 的 第 0 项、指令、延迟。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        当前写寄存器 为 做 IS_指令写寄存器 用 指令列表 的 第 i 项。
        当前读寄存器 为 做 IS_指令读寄存器 用 指令列表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 i：
            前写 为 做 IS_指令写寄存器 用 指令列表 的 第 j 项。
            前读 为 做 IS_指令读寄存器 用 指令列表 的 第 j 项。
            若 做 IS_寄存器集合交叉 用 前写、当前读寄存器：
                做 IS_添加依赖 用 节点列表、j、i、依赖_数据。
            完。
            若 做 IS_寄存器集合交叉 用 前读、当前写寄存器：
                做 IS_添加依赖 用 节点列表、j、i、依赖_反依。
            完。
            若 做 IS_寄存器集合交叉 用 前写、当前写寄存器：
                做 IS_添加依赖 用 节点列表、j、i、依赖_输出。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 节点列表。
完。

IS_操作码延迟 接受 操作码：
    若 操作码 等于 0x48 或者 操作码 等于 0x4c：
        返还 延迟_内存读。
    完。
    若 操作码 等于 0xF7：
        返还 延迟_整数除。
    完。
    若 操作码 等于 0x0F：
        返还 延迟_比较。
    完。
    若 操作码 等于 0x89 或者 操作码 等于 0x88：
        返还 延迟_内存写。
    完。
    返还 延迟_整数加减。
完。

IS_指令写寄存器 接受 指令：
    若 指令 等于 空 或者 做 计数 用 指令 小于 2：
        返还 一组：（空）。
    完。
    返还 一组：指令 的 第 1 项。
完。

IS_指令读寄存器 接受 指令：
    若 指令 等于 空 或者 做 计数 用 指令 小于 3：
        返还 一组：（空）。
    完。
    返还 一组：指令 的 第 2 项。
完。

IS_寄存器集合交叉 接受 集A、集B：
    可变 i 为 0。
    只要 i 小于 做 计数 用 集A：
        可变 j 为 0。
        只要 j 小于 做 计数 用 集B：
            若 集A 的 第 i 项 等于 集B 的 第 j 项：
                返还 真。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

IS_列表调度 接受 节点列表：
    n 为 做 计数 用 节点列表。
    可变 调度结果 为 一组：（空）。
    可变 当前时间 为 0。
    CPL 为 做 IS_计算关键路径 用 节点列表。
    只要 做 计数 用 调度结果 小于 n：
        可变 就绪集 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 n：
            若 非 做 IS_节点_已调度 用 节点列表 的 第 i 项：
                若 做 IS_所有前驱已调度 用 节点列表、i：
                    最早发射 为 做 IS_计算最早发射时间 用 节点列表、i。
                    若 最早发射 小于或等于 当前时间：
                        做 追加 用 就绪集、i。
                    完。
                完。
            完。
            i 为 i 加 1。
        完。
        若 做 计数 用 就绪集 等于 0：
            当前时间 为 当前时间 加 1。
            继续。
        完。
        可变 最优idx 为 就绪集 的 第 0 项。
        可变 最优CPL 为 CPL 的 第 就绪集 的 第 0 项 项。
        i 为 1。
        只要 i 小于 做 计数 用 就绪集：
            若 CPL 的 第 就绪集 的 第 i 项 项 大于 最优CPL：
                最优CPL 为 CPL 的 第 就绪集 的 第 i 项 项。
                最优idx 为 就绪集 的 第 i 项。
            完。
            i 为 i 加 1。
        完。
        做 IS_节点_标记已调度 用 节点列表 的 第 最优idx 项。
        做 追加 用 调度结果、最优idx。
        当前时间 为 当前时间 加 做 IS_节点_延迟 用 节点列表 的 第 最优idx 项。
    完。
    返还 调度结果。
完。

IS_计算关键路径 接受 节点列表：
    n 为 做 计数 用 节点列表。
    可变 CPL 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 CPL、0。
        i 为 i 加 1。
    完。
    i 为 n 减 1。
    只要 i 大于或等于 0：
        后继列表 为 做 IS_节点_后继 用 节点列表 的 第 i 项。
        可变 最大后继CPL 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 后继列表：
            后继idx 为 后继列表 的 第 j 项 的 第 0 项。
            候选 为 做 IS_节点_延迟 用 节点列表 的 第 后继idx 项 加 CPL 的 第 后继idx 项。
            若 候选 大于 最大后继CPL：
                最大后继CPL 为 候选。
            完。
            j 为 j 加 1。
        完。
        CPL 的 第 i 项 为 做 IS_节点_延迟 用 节点列表 的 第 i 项 加 最大后继CPL。
        i 为 i 减 1。
    完。
    返还 CPL。
完。

IS_所有前驱已调度 接受 节点列表、节点idx：
    前驱列表 为 做 IS_节点_前驱 用 节点列表 的 第 节点idx 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 前驱列表：
        若 非 做 IS_节点_已调度 用 节点列表 的 第 前驱列表 的 第 i 项 的 第 0 项 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

IS_计算最早发射时间 接受 节点列表、节点idx：
    前驱列表 为 做 IS_节点_前驱 用 节点列表 的 第 节点idx 项。
    可变 最早 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 前驱列表：
        前驱idx 为 前驱列表 的 第 i 项 的 第 0 项。
        前驱完成 为 做 IS_节点_最早发射 用 节点列表 的 第 前驱idx 项 加 做 IS_节点_延迟 用 节点列表 的 第 前驱idx 项。
        若 前驱完成 大于 最早：
            最早 为 前驱完成。
        完。
        i 为 i 加 1。
    完。
    返还 最早。
完。

IS_优化 接受 指令列表：
    若 做 计数 用 指令列表 小于或等于 1：
        返还 指令列表。
    完。
    节点列表 为 做 IS_构建依赖图 用 指令列表。
    调度顺序 为 做 IS_列表调度 用 节点列表。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度顺序：
        做 追加 用 结果、指令列表 的 第 调度顺序 的 第 i 项 项。
        i 为 i 加 1。
    完。
    _IS_调度次数 为 _IS_调度次数 加 1。
    返还 结果。
完。

可变 _IS_调度次数 为 0。

可变 _IS_减少停顿数 为 0。

IS_调度次数_重置 接受 零参：
    _IS_调度次数 为 0。
    _IS_减少停顿数 为 0。
完。

IS_记录停顿减少 接受 n：
    _IS_减少停顿数 为 _IS_减少停顿数 加 n。
完。

IS_报告 接受 零参：
    返还 一组：_IS_调度次数、_IS_减少停顿数。
完。

本章 收束。
