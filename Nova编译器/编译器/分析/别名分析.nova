《别名分析》

引用《..·..·标准库·基础》。

引用《..·..·编译器·标记常量》。

别名_肯定 为 0。

别名_可能 为 1。

别名_否定 为 2。

访问_读 为 0。

访问_写 为 1。

访问_读写 为 2。

来源_栈 为 0。

来源_堆 为 1。

来源_全局 为 2。

来源_参数 为 3。

来源_常量 为 4。

AA_指针_创建 接受 来源、基础变量、偏移、大小、类型ID：
    返还 一组：来源、基础变量、偏移、大小、类型ID。
完。

AA_指针_来源 接受 p：
    返还 p 的 第 0 项。
完。

AA_指针_基础 接受 p：
    返还 p 的 第 1 项。
完。

AA_指针_偏移 接受 p：
    返还 p 的 第 2 项。
完。

AA_指针_大小 接受 p：
    返还 p 的 第 3 项。
完。

AA_指针_类型 接受 p：
    返还 p 的 第 4 项。
完。

AA_查询 接受 p1、p2：
    若 做 AA_指针_来源 用 p1 等于 做 AA_指针_来源 用 p2 并且 做 AA_指针_基础 用 p1 等于 做 AA_指针_基础 用 p2 并且 做 AA_指针_偏移 用 p1 等于 做 AA_指针_偏移 用 p2：
        返还 别名_肯定。
    完。
    若 做 AA_指针_来源 用 p1 等于 来源_栈 并且 做 AA_指针_来源 用 p2 等于 来源_堆：
        返还 别名_否定。
    完。
    若 做 AA_指针_来源 用 p1 等于 来源_堆 并且 做 AA_指针_来源 用 p2 等于 来源_栈：
        返还 别名_否定。
    完。
    若 做 AA_指针_来源 用 p1 等于 来源_常量 或者 做 AA_指针_来源 用 p2 等于 来源_常量：
        若 做 AA_指针_来源 用 p1 不等于 做 AA_指针_来源 用 p2：
            返还 别名_否定。
        完。
    完。
    若 做 AA_指针_基础 用 p1 等于 做 AA_指针_基础 用 p2：
        off1 为 做 AA_指针_偏移 用 p1。
        off2 为 做 AA_指针_偏移 用 p2。
        sz1 为 做 AA_指针_大小 用 p1。
        sz2 为 做 AA_指针_大小 用 p2。
        若 sz1 大于 0 并且 sz2 大于 0：
            若 off1 加 sz1 小于或等于 off2 或者 off2 加 sz2 小于或等于 off1：
                返还 别名_否定。
            完。
        完。
    完。
    t1 为 做 AA_指针_类型 用 p1。
    t2 为 做 AA_指针_类型 用 p2。
    若 t1 大于 0 并且 t2 大于 0 并且 t1 不等于 t2：
        若 做 AA_TBAA_无关 用 t1、t2：
            返还 别名_否定。
        完。
    完。
    返还 别名_可能。
完。

AA_TBAA_无关 接受 类型A、类型B：
    数值类型 为 一组：1、2。
    引用类型 为 一组：3、5、6、7。
    可变 A是数值 为 假。
    可变 B是数值 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数值类型：
        若 数值类型 的 第 i 项 等于 类型A：
            A是数值 为 真。
        完。
        若 数值类型 的 第 i 项 等于 类型B：
            B是数值 为 真。
        完。
        i 为 i 加 1。
    完。
    若 A是数值 并且 B是数值 等于 假：
        返还 真。
    完。
    若 B是数值 并且 A是数值 等于 假：
        返还 真。
    完。
    返还 假。
完。

AA_写读别名 接受 写指针、读指针列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 读指针列表：
        若 做 AA_查询 用 写指针、读指针列表 的 第 i 项 不等于 别名_否定：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

AA_对象逃逸 接受 对象ID、逃逸集：
    可变 i 为 0。
    只要 i 小于 做 计数 用 逃逸集：
        若 逃逸集 的 第 i 项 等于 对象ID：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

AA_简单逃逸分析 接受 语句列表、变量名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列表：
        语句 为 语句列表 的 第 i 项。
        若 做 是列表 用 语句 并且 做 计数 用 语句 大于或等于 2：
            若 语句 的 第 0 项 等于 3：
                若 做 AA_表达式含变量 用 语句 的 第 1 项、变量名：
                    返还 真。
                完。
            完。
            若 语句 的 第 0 项 等于 标记_可变 并且 做 计数 用 语句 大于或等于 3：
                若 做 AA_表达式含变量 用 语句 的 第 2 项、变量名：
                    返还 真。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

AA_表达式含变量 接受 节点、变量名：
    若 节点 等于 空：
        返还 假。
    完。
    若 做 类型 用 节点 不等于 做 类型 用 一组：（空）：
        返还 假。
    完。
    若 做 计数 用 节点 等于 0：
        返还 假。
    完。
    若 节点 的 第 0 项 等于 标记_名称 并且 做 计数 用 节点 大于或等于 2：
        返还 节点 的 第 1 项 等于 变量名。
    完。
    可变 i 为 1。
    只要 i 小于 做 计数 用 节点：
        若 做 是列表 用 节点 的 第 i 项：
            若 做 AA_表达式含变量 用 节点 的 第 i 项、变量名：
                返还 真。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

可变 _AA_指针集 为 做 字典。

AA_重置指针集 接受 零参：
    _AA_指针集 为 做 字典。
完。

AA_记录指向 接受 变量名、对象ID：
    键 为 变量名。
    已有 为 做 字典取 用 _AA_指针集、键。
    若 已有 等于 空：
        做 字典设 用 _AA_指针集、键、一组：对象ID。
    否则：
        可变 found 为 假。
        可变 i 为 0。
        只要 i 小于 做 计数 用 已有：
            若 已有 的 第 i 项 等于 对象ID：
                found 为 真。
            完。
            i 为 i 加 1。
        完。
        若 非 found：
            做 追加 用 已有、对象ID。
        完。
    完。
完。

AA_查询指向 接受 变量名：
    v 为 做 字典取 用 _AA_指针集、变量名。
    若 v 等于 空：
        返还 一组：（空）。
    完。
    返还 v。
完。

AA_可能别名 接受 变量A、变量B：
    A指向 为 做 AA_查询指向 用 变量A。
    B指向 为 做 AA_查询指向 用 变量B。
    若 做 计数 用 A指向 等于 0 或者 做 计数 用 B指向 等于 0：
        返还 真。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 A指向：
        可变 j 为 0。
        只要 j 小于 做 计数 用 B指向：
            若 A指向 的 第 i 项 等于 B指向 的 第 j 项：
                返还 真。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

可变 _AA_查询次数 为 0。

可变 _AA_否定次数 为 0。

可变 _AA_肯定次数 为 0。

AA_记录查询 接受 结果：
    _AA_查询次数 为 _AA_查询次数 加 1。
    若 结果 等于 别名_否定：
        _AA_否定次数 为 _AA_否定次数 加 1。
    完。
    若 结果 等于 别名_肯定：
        _AA_肯定次数 为 _AA_肯定次数 加 1。
    完。
完。

AA_精确率 接受 零参：
    若 _AA_查询次数 等于 0：
        返还 0。
    完。
    返还 (_AA_否定次数 加 _AA_肯定次数) 乘 100 除以 _AA_查询次数。
完。

AA_报告 接受 零参：
    返还 一组：_AA_查询次数、_AA_否定次数、_AA_肯定次数、做 AA_精确率。
完。

本章 收束。
