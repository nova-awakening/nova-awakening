《节点定义》

引用《..·..·标准库·基础》。

节_布尔 为 4。

节_空值 为 5。

节_标识 为 6。

节_一元 为 11。

节_成员 为 14。

节_声明变量 为 30。

节_声明函数 为 31。

节_条件 为 33。

节_循环 为 34。

节_遍历 为 35。

节_代码块 为 36。

节_导入 为 40。

节_结构定义 为 41。

节_异常捕获 为 42。

节_异常抛出 为 43。

节_表达式句 为 44。

节_程序 为 50。

创建节点 接受 类型、数据、行号：
    _节点_创建数 为 _节点_创建数 加 1。
    返还 一组：类型、数据、行号、一组：（空）。
完。

创建叶节点 接受 类型、值、行号：
    _节点_创建数 为 _节点_创建数 加 1。
    返还 一组：类型、值、行号、一组：（空）。
完。

创建二元 接受 运算符、左、右、行号：
    返还 一组：节_二元、运算符、行号、一组：左、右。
完。

创建一元 接受 运算符、操作数、行号：
    返还 一组：节_一元、运算符、行号、一组：操作数。
完。

创建调用 接受 函数名、参数列表、行号：
    返还 一组：节_调用、函数名、行号、参数列表。
完。

创建赋值 接受 目标、值、行号：
    返还 一组：节_赋值、0、行号、一组：目标、值。
完。

创建条件 接受 条件、真支、假支、行号：
    返还 一组：节_条件、0、行号、一组：条件、真支、假支。
完。

创建循环 接受 条件、体、行号：
    返还 一组：节_循环、0、行号、一组：条件、体。
完。

创建块 接受 语句列表、行号：
    返还 一组：节_代码块、0、行号、语句列表。
完。

创建变量 接受 名称、是可变、初值、行号：
    返还 一组：节_声明变量、一组：名称、是可变、行号、一组：初值。
完。

创建函数 接受 名称、参数、体、行号：
    返还 一组：节_声明函数、一组：名称、参数、行号、一组：体。
完。

创建程序 接受 语句列表：
    返还 一组：节_程序、0、语句列表。
完。

节点_类型 接受 n：
    返还 n 的 第 0 项。
完。

节点_数据 接受 n：
    返还 n 的 第 1 项。
完。

节点_行号 接受 n：
    返还 n 的 第 2 项。
完。

节点_子节点 接受 n：
    返还 n 的 第 3 项。
完。

节点_子数 接受 n：
    返还 做 计数 用 n 的 第 3 项。
完。

AST_类型 接受 n：
    返还 做 节点_类型 用 n。
完。

AST_行号 接受 n：
    返还 做 节点_行号 用 n。
完。

AST_子节点 接受 n：
    返还 做 节点_子节点 用 n。
完。

AST_子节点数 接受 n：
    返还 做 节点_子数 用 n。
完。

节点_左 接受 n：
    返还 n 的 第 3 项 的 第 0 项。
完。

节点_右 接受 n：
    返还 n 的 第 3 项 的 第 1 项。
完。

是表达式 接受 t：
    返还 t 大于或等于 1 并且 t 小于或等于 19。
完。

是语句 接受 t：
    返还 t 大于或等于 30 并且 t 小于或等于 44。
完。

是字面量 接受 t：
    返还 t 大于或等于 1 并且 t 小于或等于 5。
完。

有副作用 接受 t：
    若 t 等于 节_赋値：
        返还 1。
    完。
    若 t 等于 节_调用：
        返还 1。
    完。
    返还 0。
完。

统计节点 接受 节点：
    可变 总 为 1。
    子 为 做 节点_子节点 用 节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子：
        总 为 总 加 做 统计节点 用 子 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

节点深度 接受 节点：
    子 为 做 节点_子节点 用 节点。
    若 做 计数 用 子 等于 0：
        返还 1。
    完。
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子：
        d 为 做 节点深度 用 子 的 第 i 项。
        若 d 大于 最大：
            最大 为 d。
        完。
        i 为 i 加 1。
    完。
    返还 最大 加 1。
完。

前序遍历 接受 节点、访问函数：
    _节点_遍历数 为 _节点_遍历数 加 1。
    做 访问函数 用 节点。
    子 为 做 节点_子节点 用 节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子：
        做 前序遍历 用 子 的 第 i 项、访问函数。
        i 为 i 加 1。
    完。
完。

后序遍历 接受 节点、访问函数：
    子 为 做 节点_子节点 用 节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子：
        做 后序遍历 用 子 的 第 i 项、访问函数。
        i 为 i 加 1。
    完。
    做 访问函数 用 节点。
完。

收集叶节点 接受 节点：
    子 为 做 节点_子节点 用 节点。
    若 做 计数 用 子 等于 0：
        返还 一组：节点。
    完。
    可变 叶列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子：
        子叶 为 做 收集叶节点 用 子 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 子叶：
            做 追加 用 叶列表、子叶 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 叶列表。
完。

查找节点_按类型 接受 节点、类型：
    可变 结果 为 一组：（空）。
    若 做 节点_类型 用 节点 等于 类型：
        做 追加 用 结果、节点。
    完。
    子 为 做 节点_子节点 用 节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子：
        子结果 为 做 查找节点_按类型 用 子 的 第 i 项、类型。
        可变 j 为 0。
        只要 j 小于 做 计数 用 子结果：
            做 追加 用 结果、子结果 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

查找首个 接受 节点、类型：
    若 做 节点_类型 用 节点 等于 类型：
        返还 节点。
    完。
    子 为 做 节点_子节点 用 节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子：
        找到 为 做 查找首个 用 子 的 第 i 项、类型。
        若 找到 不等于 空：
            返还 找到。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

替换节点 接受 节点、旧类型、替换函数：
    _节点_替换数 为 _节点_替换数 加 1。
    若 做 节点_类型 用 节点 等于 旧类型：
        返还 做 替换函数 用 节点。
    完。
    子 为 做 节点_子节点 用 节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子：
        子 的 第 i 项 为 做 替换节点 用 子 的 第 i 项、旧类型、替换函数。
        i 为 i 加 1。
    完。
    返还 节点。
完。

节点宽度 接受 节点：
    子 为 做 节点_子节点 用 节点。
    若 做 计数 用 子 等于 0：
        返还 1。
    完。
    可变 宽度 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子：
        宽度 为 宽度 加 做 节点宽度 用 子 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 宽度。
完。

节点路径 接受 根、目标、路径：
    做 追加 用 路径、根。
    若 根 等于 目标：
        返还 真。
    完。
    子 为 做 节点_子节点 用 根。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子：
        若 做 节点路径 用 子 的 第 i 项、目标、路径：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    做 删除 用 路径、做 计数 用 路径 减 1。
    返还 假。
完。

节点转字符串 接受 节点、缩进：
    可变 前缀 为 「」。
    可变 i 为 0。
    只要 i 小于 缩进：
        前缀 为 前缀 加 「  」。
        i 为 i 加 1。
    完。
    可变 结果 为 前缀 加 做 转字符串 用 做 节点_类型 用 节点。
    子 为 做 节点_子节点 用 节点。
    若 做 计数 用 子 大于 0：
        结果 为 结果 加 「 {\n」。
        i 为 0。
        只要 i 小于 做 计数 用 子：
            结果 为 结果 加 做 节点转字符串 用 子 的 第 i 项、缩进 加 1 加 「\n」。
            i 为 i 加 1。
        完。
        结果 为 结果 加 前缀 加 「}」。
    完。
    返还 结果。
完。

可变 _节点_创建数 为 0。

可变 _节点_遍历数 为 0。

可变 _节点_替换数 为 0。

节点_记录创建 接受 零参：
    _节点_创建数 为 _节点_创建数 加 1。
完。

节点_记录遍历 接受 零参：
    _节点_遍历数 为 _节点_遍历数 加 1。
完。

节点_记录替换 接受 零参：
    _节点_替换数 为 _节点_替换数 加 1。
完。

本章 收束。
