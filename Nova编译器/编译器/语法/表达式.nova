《表达式》

标_余 为 34。

标_模 为 34。

标_与 为 50。

二元优先级 接受 op：
    若 op 等于 标记_或：
        返还 1。
    完。
    若 op 等于 标记_且：
        返还 2。
    完。
    若 op 等于 标记_位或：
        返还 3。
    完。
    若 op 等于 标记_位异或：
        返还 4。
    完。
    若 op 等于 标记_位与：
        返还 5。
    完。
    若 op 等于 标_等于：
        返还 6。
    完。
    若 op 等于 标_不等：
        返还 6。
    完。
    若 op 等于 标_小于：
        返还 7。
    完。
    若 op 等于 标_大于：
        返还 7。
    完。
    若 op 等于 标_小等：
        返还 7。
    完。
    若 op 等于 标_大等：
        返还 7。
    完。
    若 op 等于 标记_左移：
        返还 8。
    完。
    若 op 等于 标记_右移：
        返还 8。
    完。
    若 op 等于 标_加：
        返还 9。
    完。
    若 op 等于 标_减：
        返还 9。
    完。
    若 op 等于 标_乘：
        返还 10。
    完。
    若 op 等于 标_除：
        返还 10。
    完。
    若 op 等于 标_余：
        返还 10。
    完。
    返还 0。
完。

一元优先级 接受 op：
    若 op 等于 标_减：
        返还 11。
    完。
    若 op 等于 标_非：
        返还 11。
    完。
    若 op 等于 标_位非：
        返还 11。
    完。
    返还 0。
完。

是右结合 接受 op：
    返还 op 大于或等于 标记_赋值 并且 op 小于或等于 80。
完。

优先级更高 接受 a、b：
    pa 为 做 二元优先级 用 a。
    pb 为 做 二元优先级 用 b。
    若 pa 大于 pb：
        返还 1。
    完。
    返还 0。
完。

同优先级 接受 a、b：
    返还 做 二元优先级 用 a 等于 做 二元优先级 用 b。
完。

反转比较 接受 op：
    若 op 等于 标_等于：
        返还 标_不等。
    完。
    若 op 等于 标_不等：
        返还 标_等于。
    完。
    若 op 等于 标_小于：
        返还 标_大等。
    完。
    若 op 等于 标_大于：
        返还 标_小等。
    完。
    若 op 等于 标_小等：
        返还 标_大于。
    完。
    若 op 等于 标_大等：
        返还 标_小于。
    完。
    返还 op。
完。

交换比较 接受 op：
    若 op 等于 标_小于：
        返还 标_大于。
    完。
    若 op 等于 标_大于：
        返还 标_小于。
    完。
    若 op 等于 标_小等：
        返还 标_大等。
    完。
    若 op 等于 标_大等：
        返还 标_小等。
    完。
    返还 op。
完。

可交换 接受 op：
    若 op 等于 标_加：
        返还 1。
    完。
    若 op 等于 标_乘：
        返还 1。
    完。
    若 op 等于 标_等于：
        返还 1。
    完。
    若 op 等于 标_不等：
        返还 1。
    完。
    若 op 等于 标记_位与：
        返还 1。
    完。
    若 op 等于 标记_位或：
        返还 1。
    完。
    若 op 等于 标记_位异或：
        返还 1。
    完。
    返还 0。
完。

最大优先级 为 12。

是算术运算 接受 op：
    返还 op 等于 标_加 或者 op 等于 标_减 或者 op 等于 标_乘 或者 op 等于 标_除 或者 op 等于 标_模。
完。

是比较运算 接受 op：
    返还 op 等于 标_等于 或者 op 等于 标_不等 或者 op 等于 标_小于 或者 op 等于 标_大于 或者 op 等于 标_小等 或者 op 等于 标_大等。
完。

是逻辑运算 接受 op：
    返还 op 等于 标_与 或者 op 等于 标_或。
完。

是位运算 接受 op：
    返还 op 等于 标记_位与 或者 op 等于 标记_位或 或者 op 等于 标记_位异或。
完。

运算符_类别 接受 op：
    若 做 是算术运算 用 op：
        返还 「算术」。
    完。
    若 做 是比较运算 用 op：
        返还 「比较」。
    完。
    若 做 是逻辑运算 用 op：
        返还 「逻辑」。
    完。
    若 做 是位运算 用 op：
        返还 「位运算」。
    完。
    返还 「其他」。
完。

常量折叠_二元 接受 op、左值、右值：
    若 op 等于 标_加：
        返还 左值 加 右值。
    完。
    若 op 等于 标_减：
        返还 左值 减 右值。
    完。
    若 op 等于 标_乘：
        返还 左值 乘 右值。
    完。
    若 op 等于 标_除：
        若 右值 等于 0：
            返还 空。
        完。
        返还 左值 除以 右值。
    完。
    若 op 等于 标_模：
        若 右值 等于 0：
            返还 空。
        完。
        返还 左值 取余于 右值。
    完。
    若 op 等于 标_等于：
        返还 左值 等于 右值。
    完。
    若 op 等于 标_不等：
        返还 左值 不等于 右值。
    完。
    若 op 等于 标_小于：
        返还 左值 小于 右值。
    完。
    若 op 等于 标_大于：
        返还 左值 大于 右值。
    完。
    若 op 等于 标_小等：
        返还 左值 小于或等于 右值。
    完。
    若 op 等于 标_大等：
        返还 左值 大于或等于 右值。
    完。
    返还 空。
完。

常量折叠_一元 接受 op、值：
    若 op 等于 标_减：
        返还 0 减 值。
    完。
    若 op 等于 标_非：
        返还 非 值。
    完。
    返还 空。
完。

表达式_深度估算 接受 节点数：
    若 节点数 小于或等于 3：
        返还 1。
    完。
    若 节点数 小于或等于 10：
        返还 2。
    完。
    若 节点数 小于或等于 30：
        返还 3。
    完。
    返还 4。
完。

表达式_过于复杂 接受 节点数：
    返还 节点数 大于 50。
完。

可变 _表达式_解析数 为 0。

可变 _表达式_折叠数 为 0。

表达式_记录解析 接受 零参：
    _表达式_解析数 为 _表达式_解析数 加 1。
完。

表达式_记录折叠 接受 零参：
    _表达式_折叠数 为 _表达式_折叠数 加 1。
完。

本章 收束。
