《解析器》

引用《..·..·标准库·基础》。

引用《..·..·编译器·标记常量》。

骨_节_整数 为 100。

骨_节_字符串 为 101。

骨_节_标识符 为 102。

骨_节_调用 为 103。

骨_节_二元 为 104。

骨_节_列表 为 105。

骨_节_索引 为 106。

骨_节_定义 为 110。

骨_节_赋值 为 111。

骨_节_如果 为 112。

骨_节_当 为 113。

骨_节_返回 为 114。

骨_节_表达式 为 115。

骨_节_索引赋值 为 116。

骨_节_函数 为 120。

骨_节_新建 为 130。

骨_节_点访问 为 131。

骨_节_点赋值 为 132。

骨_节_匿名函数 为 133。

骨_节_每个 为 134。

骨_节_匹配 为 135。

骨_节_中断 为 136。

骨_节_继续 为 137。

骨_节_逻辑非 为 138。

骨_节_位非 为 139。

骨_节_间接调用 为 140。

骨_节_浮点 为 150。

骨_节_字典 为 151。

骨_节_尝试 为 160。

骨_节_抛出 为 162。

骨_节_空 为 165。

EOF标记 为 一组：0、0、一组：（空）。

解析器_创建 接受 标记列表：
    返还 一组：标记列表、0、一组：（空）。
完。

解析器_位置 接受 p：
    返还 p 的 第 1 项。
完。

解析器_错误数 接受 p：
    返还 做 计数 用 p 的 第 2 项。
完。

解析器_结束 接受 p：
    若 p 的 第 1 项 大于或等于 做 计数 用 p 的 第 0 项：
        返还 1。
    完。
    返还 0。
完。

解析器_当前 接受 p：
    若 做 解析器_结束 用 p 等于 1：
        返还 EOF标记。
    完。
    返还 p 的 第 0 项 的 第 p 的 第 1 项 项。
完。

解析器_前进 接受 p：
    标记 为 做 解析器_当前 用 p。
    p 的 第 1 项 为 p 的 第 1 项 加 1。
    返还 标记。
完。

解析器_窥视 接受 p、n：
    位置 为 p 的 第 1 项 加 n。
    若 位置 大于或等于 做 计数 用 p 的 第 0 项：
        返还 EOF标记。
    完。
    返还 p 的 第 0 项 的 第 位置 项。
完。

解析器_期望 接受 p、期望类型：
    标记 为 做 解析器_当前 用 p。
    若 标记 的 第 0 项 等于 期望类型：
        p 的 第 1 项 为 p 的 第 1 项 加 1。
        返还 1。
    完。
    做 追加 用 p 的 第 2 项、一组：期望类型、标记 的 第 0 项、标记 的 第 2 项。
    返还 0。
完。

解析器_报错 接受 p、消息码、行号：
    做 追加 用 p 的 第 2 项、一组：消息码、0、行号。
完。

解析器_进入循环 接受 p：
    p 的 第 3 项 为 p 的 第 3 项 加 1。
完。

解析器_离开循环 接受 p：
    p 的 第 3 项 为 p 的 第 3 项 减 1。
完。

解析器_在循环中 接受 p：
    返还 p 的 第 3 项 大于 0。
完。

解析器_进入函数 接受 p：
    p 的 第 4 项 为 p 的 第 4 项 加 1。
完。

解析器_离开函数 接受 p：
    p 的 第 4 项 为 p 的 第 4 项 减 1。
完。

解析器_在函数中 接受 p：
    返还 p 的 第 4 项 大于 0。
完。

解析器_同步 接受 p：
    只要 做 解析器_结束 用 p 等于 0：
        标记 为 做 解析器_当前 用 p。
        t 为 标记 的 第 0 项。
        若 t 等于 标记_定义：
            返还 0。
        完。
        若 t 等于 标记_函数：
            返还 0。
        完。
        若 t 等于 标记_返回：
            返还 0。
        完。
        若 t 等于 标记_如果：
            返还 0。
        完。
        若 t 等于 标记_当：
            返还 0。
        完。
        若 t 等于 标记_右花：
            返还 0。
        完。
        p 的 第 1 项 为 p 的 第 1 项 加 1。
    完。
    返还 0。
完。

析错_意外 为 1。

析错_期望 为 2。

析错_未终止 为 3。

析错_止越界 为 4。

析错_回越界 为 5。

析错_重复 为 6。

析错_实参多 为 7。

析错_形参多 为 8。

上限_形参 为 255。

上限_实参 为 255。

上限_局部 为 65536。

上限_深度 为 256。

可变 _解析_深度 为 0。

可变 _解析_最大深度 为 0。

解析_进入 接受 零参：
    _解析_深度 为 _解析_深度 加 1。
    若 _解析_深度 大于 _解析_最大深度：
        _解析_最大深度 为 _解析_深度。
    完。
完。

解析_退出 接受 零参：
    若 _解析_深度 大于 0：
        _解析_深度 为 _解析_深度 减 1。
    完。
完。

解析_深度 接受 零参：
    返还 _解析_深度。
完。

解析_过深 接受 零参：
    返还 _解析_深度 大于 上限_深度。
完。

解析_跳过直到 接受 p、目标类型列表：
    可变 跳过数 为 0。
    只要 p 的 第 1 项 小于 做 计数 用 p 的 第 0 项：
        当前 为 p 的 第 0 项 的 第 p 的 第 1 项 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 目标类型列表：
            若 当前 等于 目标类型列表 的 第 i 项：
                返还 跳过数。
            完。
            i 为 i 加 1。
        完。
        p 的 第 1 项 为 p 的 第 1 项 加 1。
        跳过数 为 跳过数 加 1。
    完。
    返还 跳过数。
完。

解析_同步到语句 接受 p：
    返还 做 解析_跳过直到 用 p、一组：112、113、114、116、93。
完。

解析_期望 接受 p、期望类型：
    若 p 的 第 1 项 大于或等于 做 计数 用 p 的 第 0 项：
        返还 假。
    完。
    若 p 的 第 0 项 的 第 p 的 第 1 项 项 等于 期望类型：
        p 的 第 1 项 为 p 的 第 1 项 加 1。
        返还 真。
    完。
    返还 假。
完。

可变 _上下文栈 为 一组：（空）。

上下文_进入 接受 类型：
    做 追加 用 _上下文栈、类型。
完。

上下文_退出 接受 零参：
    可变 新栈 为 一组：（空）。
    若 做 计数 用 _上下文栈 大于 0：
        可变 i 为 0。
        只要 i 小于 做 计数 用 _上下文栈 减 1：
            做 追加 用 新栈、_上下文栈 的 第 i 项。
            i 为 i 加 1。
        完。
        _上下文栈 为 新栈。
    完。
完。

上下文_在循环中 接受 零参：
    可变 i 为 做 计数 用 _上下文栈 减 1。
    只要 i 大于或等于 0：
        若 _上下文栈 的 第 i 项 等于 「循环」：
            返还 真。
        完。
        i 为 i 减 1。
    完。
    返还 假。
完。

上下文_在函数中 接受 零参：
    可变 i 为 做 计数 用 _上下文栈 减 1。
    只要 i 大于或等于 0：
        若 _上下文栈 的 第 i 项 等于 「函数」：
            返还 真。
        完。
        i 为 i 减 1。
    完。
    返还 假。
完。

上下文_当前 接受 零参：
    若 做 计数 用 _上下文栈 等于 0：
        返还 「顶层」。
    完。
    返还 _上下文栈 的 第 做 计数 用 _上下文栈 减 1 项。
完。

可变 _解析_节点数 为 0。

可变 _解析_错误恢复数 为 0。

解析器_记录节点 接受 零参：
    _解析_节点数 为 _解析_节点数 加 1。
完。

解析器_记录恢复 接受 零参：
    _解析_错误恢复数 为 _解析_错误恢复数 加 1。
完。

解析器_解析 接受 p：
    可变 ast 为 一组：（空）。
    只要 做 解析器_结束 用 p 等于 0：
        标记 为 做 解析器_当前 用 p。
        t 为 标记 的 第 0 项。
        行 为 标记 的 第 2 项。
        若 t 等于 0：
            返还 ast。
        完。
        若 t 等于 标记_函数：
            做 解析器_前进 用 p。
            名tok 为 做 解析器_前进 用 p。
            名称 为 名tok 的 第 1 项。
            做 解析器_期望 用 p、标记_左括。
            可变 参数 为 一组：（空）。
            只要 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 不等于 标记_右括：
                前标记 为 做 解析器_前进 用 p。
                做 追加 用 参数、前标记 的 第 1 项。
                若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_逗号：
                    做 解析器_前进 用 p。
                完。
            完。
            若 做 解析器_结束 用 p 等于 0：
                做 解析器_前进 用 p。
            完。
            体 为 做 解析器_解析块 用 p。
            做 追加 用 ast、一组：骨_节_函数、名称、参数、体、行。
            做 解析器_记录节点。
        否则若 t 等于 标记_定义：
            做 解析器_前进 用 p。
            可变 是可变 为 0。
            若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_可变：
                做 解析器_前进 用 p。
                是可变 为 1。
            完。
            名tok 为 做 解析器_前进 用 p。
            可变 初值 为 空。
            若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_赋值：
                做 解析器_前进 用 p。
                初值 为 做 解析器_解析表达式 用 p。
            完。
            做 追加 用 ast、一组：骨_节_定义、名tok 的 第 1 项、初值、是可变、行。
            做 解析器_记录节点。
        否则若 t 等于 标记_如果：
            节点 为 做 解析器_解析如果 用 p。
            做 追加 用 ast、节点。
        否则若 t 等于 标记_当：
            节点 为 做 解析器_解析当 用 p。
            做 追加 用 ast、节点。
        否则若 t 等于 标记_返回：
            做 解析器_前进 用 p。
            可变 值 为 空。
            若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 不等于 标记_右花：
                值 为 做 解析器_解析表达式 用 p。
            完。
            做 追加 用 ast、一组：骨_节_返回、值、行。
            做 解析器_记录节点。
        否则若 t 等于 标记_中断：
            做 解析器_前进 用 p。
            做 追加 用 ast、一组：骨_节_中断、行。
            做 解析器_记录节点。
        否则若 t 等于 标记_继续：
            做 解析器_前进 用 p。
            做 追加 用 ast、一组：骨_节_继续、行。
            做 解析器_记录节点。
        否则若 t 等于 标记_抛出：
            做 解析器_前进 用 p。
            可变 值 为 空。
            若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 不等于 标记_右花：
                值 为 做 解析器_解析表达式 用 p。
            完。
            做 追加 用 ast、一组：骨_节_抛出、值、行。
            做 解析器_记录节点。
        否则：
            表达式 为 做 解析器_解析表达式 用 p。
            若 表达式 不等于 空：
                做 追加 用 ast、一组：骨_节_表达式、表达式、行。
                做 解析器_记录节点。
            否则：
                做 解析器_前进 用 p。
                做 解析器_记录恢复。
            完。
        完。
    完。
    返还 ast。
完。

解析器_解析块 接受 p：
    可变 语句列 为 一组：（空）。
    若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_左花：
        做 解析器_前进 用 p。
    完。
    只要 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 不等于 标记_右花：
        子ast 为 做 解析器_解析 用 p。
        可变 j 为 0。
        只要 j 小于 做 计数 用 子ast：
            做 追加 用 语句列、子ast 的 第 j 项。
            j 为 j 加 1。
        完。
        若 做 计数 用 子ast 等于 0：
            做 解析器_前进 用 p。
        完。
    完。
    若 做 解析器_结束 用 p 等于 0：
        做 解析器_前进 用 p。
    完。
    返还 语句列。
完。

解析器_解析如果 接受 p：
    行 为 做 解析器_当前 用 p 的 第 2 项。
    做 解析器_前进 用 p。
    若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_左括：
        做 解析器_前进 用 p。
    完。
    条件 为 做 解析器_解析表达式 用 p。
    若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_右括：
        做 解析器_前进 用 p。
    完。
    then体 为 做 解析器_解析块 用 p。
    可变 else体 为 一组：（空）。
    若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_否则：
        做 解析器_前进 用 p。
        若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_如果：
            else体 为 一组：做 解析器_解析如果 用 p。
        否则：
            else体 为 做 解析器_解析块 用 p。
        完。
    完。
    做 解析器_记录节点。
    返还 一组：骨_节_如果、条件、then体、else体、行。
完。

解析器_解析当 接受 p：
    行 为 做 解析器_当前 用 p 的 第 2 项。
    做 解析器_前进 用 p。
    若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_左括：
        做 解析器_前进 用 p。
    完。
    条件 为 做 解析器_解析表达式 用 p。
    若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_右括：
        做 解析器_前进 用 p。
    完。
    循环体 为 做 解析器_解析块 用 p。
    做 解析器_记录节点。
    返还 一组：骨_节_当、条件、循环体、行。
完。

解析器_解析表达式 接受 p：
    若 做 解析器_结束 用 p 等于 1：
        返还 空。
    完。
    若 做 解析器_当前 用 p 的 第 0 项 等于 标记_非：
        做 解析器_前进 用 p。
        操作数 为 做 解析器_解析原子 用 p。
        返还 一组：骨_节_逻辑非、操作数。
    完。
    若 做 解析器_当前 用 p 的 第 0 项 等于 标记_位取反：
        做 解析器_前进 用 p。
        操作数 为 做 解析器_解析原子 用 p。
        返还 一组：骨_节_位非、操作数。
    完。
    左 为 做 解析器_解析原子 用 p。
    若 做 解析器_结束 用 p 等于 0：
        op_tok 为 做 解析器_当前 用 p。
        ot 为 op_tok 的 第 0 项。
        若 ot 大于或等于 标记_加 并且 ot 小于或等于 标记_右移 或者 ot 大于或等于 标记_等等 并且 ot 小于或等于 标记_大等：
            做 解析器_前进 用 p。
            右 为 做 解析器_解析原子 用 p。
            返还 一组：骨_节_二元、op_tok 的 第 1 项、左、右。
        完。
    完。
    返还 左。
完。

解析器_解析原子 接受 p：
    若 做 解析器_结束 用 p 等于 1：
        返还 空。
    完。
    标记 为 做 解析器_当前 用 p。
    t 为 标记 的 第 0 项。
    行 为 标记 的 第 2 项。
    若 t 等于 标记_数字：
        做 解析器_前进 用 p。
        返还 一组：骨_节_整数、标记 的 第 1 项。
    完。
    若 t 等于 标记_浮点：
        做 解析器_前进 用 p。
        返还 一组：骨_节_浮点、标记 的 第 1 项。
    完。
    若 t 等于 标记_字串：
        做 解析器_前进 用 p。
        返还 一组：骨_节_字符串、标记 的 第 1 项。
    完。
    若 t 等于 标记_标识符：
        做 解析器_前进 用 p。
        若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_左括：
            做 解析器_前进 用 p。
            可变 参数 为 一组：（空）。
            只要 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 不等于 标记_右括：
                arg 为 做 解析器_解析表达式 用 p。
                做 追加 用 参数、arg。
                若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_逗号：
                    做 解析器_前进 用 p。
                完。
            完。
            若 做 解析器_结束 用 p 等于 0：
                做 解析器_前进 用 p。
            完。
            返还 一组：骨_节_调用、标记 的 第 1 项、参数、行。
        完。
        返还 一组：骨_节_标识符、标记 的 第 1 项。
    完。
    若 t 等于 标记_左括：
        做 解析器_前进 用 p。
        表达式 为 做 解析器_解析表达式 用 p。
        若 做 解析器_结束 用 p 等于 0 并且 做 解析器_当前 用 p 的 第 0 项 等于 标记_右括：
            做 解析器_前进 用 p。
        完。
        返还 表达式。
    完。
    若 t 等于 标记_真：
        做 解析器_前进 用 p。
        返还 一组：骨_节_整数、1。
    完。
    若 t 等于 标记_假：
        做 解析器_前进 用 p。
        返还 一组：骨_节_整数、0。
    完。
    若 t 等于 标记_空：
        做 解析器_前进 用 p。
        返还 一组：骨_节_空、一组：（空）。
    完。
    做 解析器_前进 用 p。
    返还 空。
完。

本章 收束。
