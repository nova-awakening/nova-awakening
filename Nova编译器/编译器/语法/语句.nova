《语句》

引用《..·..·标准库·基础》。

引用《..·..·编译器·标记常量》。

语句_变量 为 1。

语句_函数 为 2。

语句_如果 为 4。

语句_当 为 5。

语句_每个 为 6。

语句_匹配 为 7。

语句_中断 为 8。

语句_继续 为 9。

语句_导入 为 10。

语句_结构 为 11。

语句_抛出 为 13。

语句_表达式 为 14。

语句_块 为 15。

语句_空 为 16。

可变 _语句_错误数 为 0。

是声明语句 接受 t：
    若 t 等于 语句_变量：
        返还 1。
    完。
    若 t 等于 语句_函数：
        返还 1。
    完。
    若 t 等于 语句_结构：
        返还 1。
    完。
    若 t 等于 语句_导入：
        返还 1。
    完。
    返还 0。
完。

是控制流 接受 t：
    若 t 等于 语句_如果：
        返还 1。
    完。
    若 t 等于 语句_当：
        返还 1。
    完。
    若 t 等于 语句_每个：
        返还 1。
    完。
    若 t 等于 语句_匹配：
        返还 1。
    完。
    若 t 等于 语句_尝试：
        返还 1。
    完。
    返还 0。
完。

是跳转语句 接受 t：
    若 t 等于 语句_返回：
        返还 1。
    完。
    若 t 等于 语句_中断：
        返还 1。
    完。
    若 t 等于 语句_继续：
        返还 1。
    完。
    若 t 等于 语句_抛出：
        返还 1。
    完。
    返还 0。
完。

需要循环上下文 接受 t：
    若 t 等于 语句_中断：
        返还 1。
    完。
    若 t 等于 语句_继续：
        返还 1。
    完。
    返还 0。
完。

需要函数上下文 接受 t：
    若 t 等于 语句_返回：
        返还 1。
    完。
    返还 0。
完。

语句信息 接受 类型：
    若 类型 等于 语句_变量：
        返还 一组：类型、0、1、0。
    完。
    若 类型 等于 语句_函数：
        返还 一组：类型、0、2、0。
    完。
    若 类型 等于 语句_返回：
        返还 一组：类型、0、1、0。
    完。
    若 类型 等于 语句_如果：
        返还 一组：类型、0、3、0。
    完。
    若 类型 等于 语句_当：
        返还 一组：类型、0、2、0。
    完。
    若 类型 等于 语句_每个：
        返还 一组：类型、0、3、0。
    完。
    若 类型 等于 语句_匹配：
        返还 一组：类型、0、2、0。
    完。
    若 类型 等于 语句_中断：
        返还 一组：类型、0、0。
    完。
    若 类型 等于 语句_继续：
        返还 一组：类型、0、0。
    完。
    若 类型 等于 语句_导入：
        返还 一组：类型、0、1、0。
    完。
    若 类型 等于 语句_结构：
        返还 一组：类型、0、1、0。
    完。
    若 类型 等于 语句_尝试：
        返还 一组：类型、0、3、0。
    完。
    若 类型 等于 语句_抛出：
        返还 一组：类型、0、1、0。
    完。
    若 类型 等于 语句_表达式：
        返还 一组：类型、0、1、0。
    完。
    若 类型 等于 语句_块：
        返还 一组：类型、0、0。
    完。
    返还 一组：语句_空、0、0。
完。

关键字到语句 接受 kw：
    若 kw 等于 标记_定义：
        返还 语句_变量。
    完。
    若 kw 等于 标记_函数：
        返还 语句_函数。
    完。
    若 kw 等于 标记_返回：
        返还 语句_返回。
    完。
    若 kw 等于 标记_如果：
        返还 语句_如果。
    完。
    若 kw 等于 标记_当：
        返还 语句_当。
    完。
    若 kw 等于 标记_循环：
        返还 语句_每个。
    完。
    若 kw 等于 标记_导入：
        返还 语句_导入。
    完。
    若 kw 等于 标记_结构：
        返还 语句_结构。
    完。
    若 kw 等于 标记_匹配：
        返还 语句_匹配。
    完。
    若 kw 等于 标记_中断：
        返还 语句_中断。
    完。
    若 kw 等于 标记_继续：
        返还 语句_继续。
    完。
    若 kw 等于 标记_尝试：
        返还 语句_尝试。
    完。
    若 kw 等于 标记_抛出：
        返还 语句_抛出。
    完。
    返还 0。
完。

是终结语句 接受 t：
    若 t 等于 语句_返回：
        返还 1。
    完。
    若 t 等于 语句_中断：
        返还 1。
    完。
    若 t 等于 语句_继续：
        返还 1。
    完。
    若 t 等于 语句_抛出：
        返还 1。
    完。
    返还 0。
完。

变量分析 接受 有可变标记、有赋值：
    返还 一组：1、有可变标记、有赋值。
完。

是赋值语句 接受 类型：
    返还 类型 等于 语句_变量 或者 类型 等于 语句_表达式。
完。

是循环语句 接受 类型：
    返还 类型 等于 语句_当 或者 类型 等于 语句_每个。
完。

是条件语句 接受 类型：
    返还 类型 等于 语句_如果 或者 类型 等于 语句_匹配。
完。

是函数定义 接受 类型：
    返还 类型 等于 语句_函数。
完。

是跳转 接受 类型：
    返还 做 是跳转语句 用 类型。
完。

语句块_创建 接受 零参：
    返还 一组：一组：（空）、负 1、0、一组：（空）。
完。

语句块_添加语句 接受 块、语句类型：
    做 追加 用 块 的 第 0 项、语句类型。
完。

语句块_语句数 接受 块：
    返还 做 计数 用 块 的 第 0 项。
完。

语句块_统计类型 接受 块：
    可变 赋值 为 0。
    可变 循环 为 0。
    可变 条件 为 0。
    可变 跳转 为 0。
    可变 其他 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 块 的 第 0 项：
        t 为 块 的 第 0 项 的 第 i 项。
        若 做 是赋值语句 用 t：
            赋值 为 赋值 加 1。
        否则若 做 是循环语句 用 t：
            循环 为 循环 加 1。
        否则若 做 是条件语句 用 t：
            条件 为 条件 加 1。
        否则若 做 是跳转 用 t：
            跳转 为 跳转 加 1。
        否则：
            其他 为 其他 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：赋值、循环、条件、跳转、其他。
完。

控制流_有返回 接受 语句列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列表：
        若 语句列表 的 第 i 项 等于 语句_返回：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

控制流_有中断 接受 语句列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列表：
        若 语句列表 的 第 i 项 等于 语句_中断 或者 语句列表 的 第 i 项 等于 语句_继续：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

控制流_不可达检测 接受 语句列表：
    可变 不可达 为 一组：（空）。
    可变 已终止 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列表：
        若 已终止：
            做 追加 用 不可达、i。
        完。
        若 做 是跳转 用 语句列表 的 第 i 项：
            已终止 为 真。
        完。
        i 为 i 加 1。
    完。
    返还 不可达。
完。

可变 _作用域栈 为 一组：（空）。

语句_作用域_进入 接受 零参：
    做 追加 用 _作用域栈、一组：（空）。
完。

语句_作用域_退出 接受 零参：
    可变 新栈 为 一组：（空）。
    若 做 计数 用 _作用域栈 大于 0：
        n 为 做 计数 用 _作用域栈。
        可变 i 为 0。
        只要 i 小于 n 减 1：
            做 追加 用 新栈、_作用域栈 的 第 i 项。
            i 为 i 加 1。
        完。
        _作用域栈 为 新栈。
    完。
完。

语句_作用域_添加变量 接受 名：
    若 做 计数 用 _作用域栈 大于 0：
        做 追加 用 _作用域栈 的 第 做 计数 用 _作用域栈 减 1 项、名。
    完。
完。

作用域_查找 接受 名：
    可变 i 为 做 计数 用 _作用域栈 减 1。
    只要 i 大于或等于 0：
        可变 j 为 0。
        只要 j 小于 做 计数 用 _作用域栈 的 第 i 项：
            若 _作用域栈 的 第 i 项 的 第 j 项 等于 名：
                返还 真。
            完。
            j 为 j 加 1。
        完。
        i 为 i 减 1。
    完。
    返还 假。
完。

语句_作用域_深度 接受 零参：
    返还 做 计数 用 _作用域栈。
完。

可变 _语句_总数 为 0。

语句_记录 接受 零参：
    _语句_总数 为 _语句_总数 加 1。
完。

语句_记录错误 接受 零参：
    _语句_错误数 为 _语句_错误数 加 1。
完。

本章 收束。
