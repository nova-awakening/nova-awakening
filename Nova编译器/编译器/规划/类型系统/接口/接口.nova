《接口》

引用《..·..·..·..·标准库·基础》。

接口_创建 接受 iid：
    返还 一组：iid、一组：（空）、一组：（空）。
完。

接口_加方法 接受 iface、方法id、参数数：
    做 追加 用 iface 的 第 1 项、一组：方法id、参数数。
完。

接口_方法数 接受 iface：
    返还 做 计数 用 iface 的 第 1 项。
完。

接口_加父 接受 iface、父id：
    做 追加 用 iface 的 第 2 项、父id。
完。

虚表_创建 接受 iface：
    n 为 做 接口_方法数 用 iface。
    可变 槽 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 槽、0。
        i 为 i 加 1。
    完。
    返还 一组：iface 的 第 0 项、槽。
完。

虚表_绑定 接受 vt、方法idx、函数标记：
    若 方法idx 大于或等于 0 并且 方法idx 小于 做 计数 用 vt 的 第 1 项：
        vt 的 第 1 项 的 第 方法idx 项 为 函数标记。
    完。
完。

虚表_查 接受 vt、方法idx：
    若 方法idx 大于或等于 0 并且 方法idx 小于 做 计数 用 vt 的 第 1 项：
        返还 vt 的 第 1 项 的 第 方法idx 项。
    完。
    返还 0。
完。

虚表_已满 接受 vt：
    可变 i 为 0。
    只要 i 小于 做 计数 用 vt 的 第 1 项：
        若 vt 的 第 1 项 的 第 i 项 等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

接口_满足 接受 iface、vt：
    若 做 接口_方法数 用 iface 不等于 做 计数 用 vt 的 第 1 项：
        返还 假。
    完。
    返还 做 虚表_已满 用 vt。
完。

结构类型_创建 接受 tid：
    返还 一组：tid、一组：（空）。
完。

结构类型_加方法 接受 stype、方法id、参数数、函数标记：
    做 追加 用 stype 的 第 1 项、一组：方法id、参数数、函数标记。
完。

结构满足接口 接受 stype、iface：
    可变 i 为 0。
    只要 i 小于 做 计数 用 iface 的 第 1 项：
        需要方法 为 iface 的 第 1 项 的 第 i 项 的 第 0 项。
        需要参数 为 iface 的 第 1 项 的 第 i 项 的 第 1 项。
        可变 ok 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 stype 的 第 1 项：
            若 stype 的 第 1 项 的 第 j 项 的 第 0 项 等于 需要方法 并且 stype 的 第 1 项 的 第 j 项 的 第 1 项 等于 需要参数：
                ok 为 真。
            完。
            j 为 j 加 1。
        完。
        若 ok 等于 假：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

自动虚表 接受 stype、iface：
    可变 vt 为 做 虚表_创建 用 iface。
    可变 i 为 0。
    只要 i 小于 做 计数 用 iface 的 第 1 项：
        需要方法 为 iface 的 第 1 项 的 第 i 项 的 第 0 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 stype 的 第 1 项：
            若 stype 的 第 1 项 的 第 j 项 的 第 0 项 等于 需要方法：
                做 虚表_绑定 用 vt、i、stype 的 第 1 项 的 第 j 项 的 第 2 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 vt。
完。

接口_加默认方法 接受 iface、方法id、参数数、默认标记：
    做 追加 用 iface 的 第 1 项、一组：方法id、参数数、默认标记。
完。

自动虚表_默认 接受 stype、iface：
    可变 vt 为 做 虚表_创建 用 iface。
    可变 i 为 0。
    只要 i 小于 做 计数 用 iface 的 第 1 项：
        需要方法 为 iface 的 第 1 项 的 第 i 项 的 第 0 项。
        可变 已绑 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 stype 的 第 1 项：
            若 stype 的 第 1 项 的 第 j 项 的 第 0 项 等于 需要方法：
                做 虚表_绑定 用 vt、i、stype 的 第 1 项 的 第 j 项 的 第 2 项。
                已绑 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已绑 等于 假 并且 做 计数 用 iface 的 第 1 项 的 第 i 项 大于或等于 3 并且 iface 的 第 1 项 的 第 i 项 的 第 2 项 大于 0：
            做 虚表_绑定 用 vt、i、iface 的 第 1 项 的 第 i 项 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
    返还 vt。
完。

接口_继承自 接受 接口表、iface_id、target_id：
    若 iface_id 等于 target_id：
        返还 真。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 接口表：
        若 接口表 的 第 i 项 的 第 0 项 等于 iface_id：
            父列表 为 接口表 的 第 i 项 的 第 2 项。
            可变 j 为 0。
            只要 j 小于 做 计数 用 父列表：
                若 父列表 的 第 j 项 等于 target_id：
                    返还 真。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

动态分发 接受 vt、方法idx：
    若 vt 等于 空：
        返还 负 1。
    完。
    标记 为 做 虚表_查 用 vt、方法idx。
    若 标记 等于 0：
        返还 负 1。
    完。
    返还 标记。
完。

安全_动态分发 接受 vt、方法idx：
    若 vt 等于 空：
        返还 一组：假、负 1。
    完。
    标记 为 做 虚表_查 用 vt、方法idx。
    若 标记 等于 0：
        返还 一组：假、负 1。
    完。
    返还 一组：真、标记。
完。

接口_方法列表 接受 iface：
    可变 ids 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 iface 的 第 1 项：
        做 追加 用 ids、iface 的 第 1 项 的 第 i 项 的 第 0 项。
        i 为 i 加 1。
    完。
    返还 ids。
完。

接口_合并 接受 id、接口a、接口b：
    可变 合并 为 做 接口_创建 用 id。
    可变 i 为 0。
    只要 i 小于 做 计数 用 接口a 的 第 1 项：
        做 接口_加方法 用 合并、接口a 的 第 1 项 的 第 i 项 的 第 0 项、接口a 的 第 1 项 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 接口b 的 第 1 项：
        模块号 为 接口b 的 第 1 项 的 第 i 项 的 第 0 项。
        可变 dup 为 假。
        可变 k 为 0。
        只要 k 小于 做 计数 用 合并 的 第 1 项：
            若 合并 的 第 1 项 的 第 k 项 的 第 0 项 等于 模块号：
                dup 为 真。
            完。
            k 为 k 加 1。
        完。
        若 dup 等于 假：
            做 接口_加方法 用 合并、模块号、接口b 的 第 1 项 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 合并。
完。

接口表_方法数 接受 接口名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 接口表：
        若 接口表 的 第 i 项 的 第 0 项 等于 接口名：
            返还 做 计数 用 接口表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

接口_有方法 接受 接口名、方法名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 接口表：
        若 接口表 的 第 i 项 的 第 0 项 等于 接口名：
            可变 j 为 0。
            只要 j 小于 做 计数 用 接口表 的 第 i 项 的 第 1 项：
                若 接口表 的 第 i 项 的 第 1 项 的 第 j 项 等于 方法名：
                    返还 真。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

本章 收束。
