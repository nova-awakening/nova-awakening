《枚举》

引用《..·..·..·..·标准库·基础》。

枚举_定义 接受 名id：
    返还 一组：名id、一组：（空）。
完。

枚举_添加变体 接受 edef、标签、字段数：
    做 追加 用 edef 的 第 1 项、一组：标签、字段数。
完。

枚举_变体数 接受 edef：
    返还 做 计数 用 edef 的 第 1 项。
完。

枚举_变体字段数 接受 edef、标签：
    可变 i 为 0。
    只要 i 小于 做 计数 用 edef 的 第 1 项：
        若 edef 的 第 1 项 的 第 i 项 的 第 0 项 等于 标签：
            返还 edef 的 第 1 项 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

枚举_构造0 接受 标签：
    返还 一组：标签。
完。

枚举_构造1 接受 标签、a：
    返还 一组：标签、a。
完。

枚举_构造2 接受 标签、a、b：
    返还 一组：标签、a、b。
完。

枚举_构造3 接受 标签、a、b、c：
    返还 一组：标签、a、b、c。
完。

枚举_标签 接受 e：
    返还 e 的 第 0 项。
完。

枚举_字段 接受 e、idx：
    返还 e 的 第 idx 加 1 项。
完。

枚举_字段数 接受 e：
    返还 做 计数 用 e 减 1。
完。

匹配_创建 接受 零参：
    返还 一组：（空）。
完。

匹配_添加分支 接受 分支表、标签、结果值：
    做 追加 用 分支表、一组：标签、结果值。
完。

匹配_执行 接受 分支表、枚举值：
    标签 为 枚举值 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分支表：
        若 分支表 的 第 i 项 的 第 0 项 等于 标签：
            返还 分支表 的 第 i 项 的 第 1 项。
        完。
        若 分支表 的 第 i 项 的 第 0 项 等于 负 1：
            返还 分支表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

匹配_穷举 接受 edef、分支表：
    可变 有万能 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分支表：
        若 分支表 的 第 i 项 的 第 0 项 等于 负 1：
            有万能 为 真。
        完。
        i 为 i 加 1。
    完。
    若 有万能 等于 真：
        返还 真。
    完。
    可变 j 为 0。
    只要 j 小于 做 计数 用 edef 的 第 1 项：
        标签 为 edef 的 第 1 项 的 第 j 项 的 第 0 项。
        i 为 0。
        只要 i 小于 做 计数 用 分支表：
            若 分支表 的 第 i 项 的 第 0 项 等于 标签：
                已找 为 真。
            完。
            i 为 i 加 1。
        完。
        若 已找 等于 假：
            返还 假。
        完。
        j 为 j 加 1。
    完。
    返还 真。
完。

选项_有 为 1。

可选_有 接受 值：
    返还 一组：选项_有、值。
完。

可选_无 接受 零参：
    返还 一组：优化_无、0。
完。

可选_是有 接受 opt：
    返还 opt 的 第 0 项 等于 选项_有。
完。

可选_取值 接受 opt：
    若 opt 的 第 0 项 等于 选项_有：
        返还 opt 的 第 1 项。
    完。
    返还 0。
完。

可选_取或 接受 opt、缺省：
    若 opt 的 第 0 项 等于 选项_有：
        返还 opt 的 第 1 项。
    完。
    返还 缺省。
完。

结果_成功 接受 值：
    返还 一组：结果_成功、值。
完。

结果_失败 接受 错误：
    返还 一组：结果_错误、错误。
完。

结果_是成功 接受 r：
    返还 r 的 第 0 项 等于 结果_成功。
完。

结果_取值 接受 r：
    返还 r 的 第 1 项。
完。

形状_圆 接受 半径：
    返还 做 枚举_构造1 用 0、半径。
完。

形状_矩形 接受 宽、高：
    返还 做 枚举_构造2 用 1、宽、高。
完。

形状_三角 接受 a、b、c：
    返还 做 枚举_构造3 用 2、a、b、c。
完。

形状_面积 接受 形状：
    标签 为 形状 的 第 0 项。
    若 标签 等于 0：
        返还 3142 乘 形状 的 第 1 项 乘 形状 的 第 1 项 除以 1000。
    完。
    若 标签 等于 1：
        返还 形状 的 第 1 项 乘 形状 的 第 2 项。
    完。
    若 标签 等于 2：
        s2 为 形状 的 第 1 项 加 形状 的 第 2 项 加 形状 的 第 3 项。
        返还 s2 乘 s2 除以 16。
    完。
    返还 0。
完。

可选_映射 接受 opt、变换、参数：
    若 opt 的 第 0 项 不等于 选项_有：
        返还 做 可选_无。
    完。
    v 为 opt 的 第 1 项。
    若 变换 等于 1：
        返还 做 可选_有 用 v 加 参数。
    完。
    若 变换 等于 2：
        返还 做 可选_有 用 v 乘 参数。
    完。
    若 变换 等于 3：
        返还 做 可选_有 用 v 减 参数。
    完。
    返还 opt。
完。

可选_过滤 接受 opt、阈值：
    若 opt 的 第 0 项 不等于 选项_有：
        返还 做 可选_无。
    完。
    若 opt 的 第 1 项 大于 阈值：
        返还 opt。
    完。
    返还 做 可选_无。
完。

可选_展平 接受 opt：
    若 opt 的 第 0 项 不等于 选项_有：
        返还 做 可选_无。
    完。
    inner 为 opt 的 第 1 项。
    若 做 计数 用 inner 大于或等于 2 并且 inner 的 第 0 项 等于 选项_有：
        返还 inner。
    完。
    返还 opt。
完。

结果_映射 接受 r、变换、参数：
    若 r 的 第 0 项 不等于 结果_成功：
        返还 r。
    完。
    v 为 r 的 第 1 项。
    若 变换 等于 1：
        返还 做 结果_成功 用 v 加 参数。
    完。
    若 变换 等于 2：
        返还 做 结果_成功 用 v 乘 参数。
    完。
    返还 r。
完。

结果_或 接受 r、默认值：
    若 r 的 第 0 项 等于 结果_成功：
        返还 r 的 第 1 项。
    完。
    返还 默认值。
完。

枚举_变体名 接受 edef、标签：
    可变 i 为 0。
    只要 i 小于 做 计数 用 edef 的 第 1 项：
        若 edef 的 第 1 项 的 第 i 项 的 第 0 项 等于 标签：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

匹配_缺失 接受 edef、分支表：
    可变 缺失 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 做 计数 用 edef 的 第 1 项：
        标签 为 edef 的 第 1 项 的 第 j 项 的 第 0 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 分支表：
            若 分支表 的 第 i 项 的 第 0 项 等于 标签 或者 分支表 的 第 i 项 的 第 0 项 等于 负 1：
                已找 为 真。
            完。
            i 为 i 加 1。
        完。
        若 已找 等于 假：
            做 追加 用 缺失、标签。
        完。
        j 为 j 加 1。
    完。
    返还 缺失。
完。

匹配_穷尽 接受 edef、分支表：
    缺失 为 做 匹配_缺失 用 edef、分支表。
    返还 做 计数 用 缺失 等于 0。
完。

匹配_冗余 接受 分支表：
    可变 冗余 为 一组：（空）。
    可变 已见标签 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分支表：
        标签 为 分支表 的 第 i 项 的 第 0 项。
        若 标签 等于 负 1：
            可变 j 为 i 加 1。
            只要 j 小于 做 计数 用 分支表：
                做 追加 用 冗余、j。
                j 为 j 加 1。
            完。
            返还 冗余。
        完。
        可变 k 为 0。
        只要 k 小于 做 计数 用 已见标签：
            若 已见标签 的 第 k 项 等于 标签：
                做 追加 用 冗余、i。
            完。
            k 为 k 加 1。
        完。
        做 追加 用 已见标签、标签。
        i 为 i 加 1。
    完。
    返还 冗余。
完。

枚举注册表_创建 接受 零参：
    返还 一组：一组：（空）、0。
完。

枚举注册表_注册 接受 注册表、名称、变体列表：
    edef 为 做 枚举_定义 用 名称。
    可变 vi 为 0。
    只要 vi 小于 做 计数 用 变体列表：
        做 枚举_添加变体 用 edef、变体列表 的 第 vi 项 的 第 0 项、变体列表 的 第 vi 项 的 第 1 项。
        vi 为 vi 加 1。
    完。
    做 追加 用 注册表 的 第 0 项、edef。
    注册表 的 第 1 项 为 注册表 的 第 1 项 加 1。
    返还 注册表 的 第 1 项 减 1。
完。

枚举注册表_查找 接受 注册表、名称：
    可变 i 为 0。
    只要 i 小于 注册表 的 第 1 项：
        若 注册表 的 第 0 项 的 第 i 项 的 第 0 项 等于 名称：
            返还 注册表 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

枚举注册表_大小 接受 注册表：
    返还 注册表 的 第 1 项。
完。

枚举值_相等 接受 a、b：
    若 a 的 第 0 项 不等于 b 的 第 0 项：
        返还 0。
    完。
    arity_a 为 做 计数 用 a 减 1。
    arity_b 为 做 计数 用 b 减 1。
    若 arity_a 不等于 arity_b：
        返还 0。
    完。
    可变 i 为 1。
    只要 i 小于或等于 arity_a：
        若 a 的 第 i 项 不等于 b 的 第 i 项：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

枚举值_序列化 接受 值：
    可变 结果 为 一组：值 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 值：
        做 追加 用 结果、值 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

本章 收束。
