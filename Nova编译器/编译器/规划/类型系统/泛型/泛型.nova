《泛型》

引用《..·..·..·..·标准库·基础》。

泛型_参数 为 1。

泛型_约束 为 2。

泛型_具体 为 3。

泛型_推断 为 4。

约束_可比较 为 1。

约束_可哈希 为 2。

约束_可打印 为 3。

约束_可复制 为 4。

约束_可迭代 为 5。

类型参数 接受 pid、种类：
    返还 一组：pid、种类、一组：（空）。
完。

类型参数_加约束 接受 tp、约束id：
    做 追加 用 tp 的 第 2 项、约束id。
完。

类型参数_有约束 接受 tp、约束id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 tp 的 第 2 项：
        若 tp 的 第 2 项 的 第 i 项 等于 约束id：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

泛型签名 接受 fid、参数列表：
    返还 一组：fid、参数列表、一组：（空）。
完。

泛型签名_加形参 接受 sig、类型id：
    做 追加 用 sig 的 第 2 项、类型id。
完。

替换表_创建 接受 零参：
    返还 一组：（空）。
完。

替换表_绑定 接受 表、pid、具体：
    做 追加 用 表、一组：pid、具体。
完。

替换表_查 接受 表、pid：
    可变 i 为 0。
    只要 i 小于 做 计数 用 表：
        若 表 的 第 i 项 的 第 0 项 等于 pid：
            返还 表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

统一化 接受 sig、实参类型：
    可变 表 为 做 替换表_创建。
    若 做 计数 用 sig 的 第 2 项 不等于 做 计数 用 实参类型：
        返还 一组：假、表。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 sig 的 第 2 项：
        形参 为 sig 的 第 2 项 的 第 i 项。
        实参 为 实参类型 的 第 i 项。
        若 形参 小于 0：
            pid 为 0 减 形参。
            已有 为 做 替换表_查 用 表、pid。
            若 已有 等于 负 1：
                做 替换表_绑定 用 表、pid、实参。
            否则：
                若 已有 不等于 实参：
                    返还 一组：假、表。
                完。
            完。
        否则：
            若 形参 不等于 实参：
                返还 一组：假、表。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 一组：真、表。
完。

单态缓存_创建 接受 零参：
    返还 一组：（空）。
完。

单态缓存_查 接受 缓存、fid、类型列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 缓存：
        若 缓存 的 第 i 项 的 第 0 项 等于 fid：
            cached 为 缓存 的 第 i 项 的 第 1 项。
            若 做 计数 用 cached 等于 做 计数 用 类型列表：
                可变 相同 为 真。
                可变 j 为 0。
                只要 j 小于 做 计数 用 cached：
                    若 cached 的 第 j 项 不等于 类型列表 的 第 j 项：
                        相同 为 假。
                    完。
                    j 为 j 加 1。
                完。
                若 相同 等于 真：
                    返还 缓存 的 第 i 项 的 第 2 项。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

单态缓存_加 接受 缓存、fid、类型列表、名id：
    做 追加 用 缓存、一组：fid、类型列表、名id。
完。

类型满足约束 接受 类型id、约束id：
    若 类型id 等于 2：
        返还 约束id 小于或等于 4。
    完。
    若 类型id 等于 3：
        返还 约束id 等于 1 或者 约束id 等于 3 或者 约束id 等于 4。
    完。
    若 类型id 等于 4：
        返还 约束id 小于或等于 4。
    完。
    若 类型id 等于 5：
        返还 约束id 等于 5 或者 约束id 等于 3。
    完。
    返还 假。
完。

泛型_交换 接受 lst、i、j：
    临时 为 lst 的 第 i 项。
    lst 的 第 i 项 为 lst 的 第 j 项。
    lst 的 第 j 项 为 临时。
完。

泛型_最大 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

泛型_最小 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

泛型_映射 接受 lst、off：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 lst：
        做 追加 用 结果、lst 的 第 i 项 加 off。
        i 为 i 加 1。
    完。
    返还 结果。
完。

泛型_过滤大于 接受 lst、阈值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 lst：
        若 lst 的 第 i 项 大于 阈值：
            做 追加 用 结果、lst 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

泛型_归约求和 接受 lst：
    可变 s 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 lst：
        s 为 s 加 lst 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 s。
完。

约束全检 接受 sig、替换表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 sig 的 第 1 项：
        tp 为 sig 的 第 1 项 的 第 i 项。
        具体 为 做 替换表_查 用 替换表、tp 的 第 0 项。
        若 具体 不等于 负 1：
            可变 j 为 0。
            只要 j 小于 做 计数 用 tp 的 第 2 项：
                若 做 类型满足约束 用 具体、tp 的 第 2 项 的 第 j 项 等于 假：
                    返还 假。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

型变_不变 为 0。

型变_协变 为 1。

型变_逆变 为 2。

型变兼容 接受 型变、源、目标：
    若 源 等于 目标：
        返还 真。
    完。
    若 型变 等于 型变_协变 并且 源 等于 2 并且 目标 等于 3：
        返还 真。
    完。
    若 型变 等于 型变_逆变 并且 源 等于 3 并且 目标 等于 2：
        返还 真。
    完。
    返还 假。
完。

泛型_实例化 接受 形参列表、替换表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 形参列表：
        形参 为 形参列表 的 第 i 项。
        若 形参 小于 0：
            具体 为 做 替换表_查 用 替换表、0 减 形参。
            若 具体 不等于 负 1：
                做 追加 用 结果、具体。
            否则：
                做 追加 用 结果、形参。
            完。
        否则：
            做 追加 用 结果、形参。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

泛型_配对 接受 a、b：
    可变 结果 为 一组：（空）。
    可变 n 为 做 计数 用 a。
    若 做 计数 用 b 小于 n：
        n 为 做 计数 用 b。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、一组：a 的 第 i 项、b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

泛型_任一大于 接受 lst、阈值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 lst：
        若 lst 的 第 i 项 大于 阈值：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

泛型_全部大于 接受 lst、阈值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 lst：
        若 lst 的 第 i 项 小于或等于 阈值：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

本章 收束。
