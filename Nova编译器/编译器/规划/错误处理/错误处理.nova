《错误处理》

引用《..·..·..·标准库·基础》。

引用《..·..·..·标准库·类型系统·类型系统》。

规划错误_级别_警告 为 1。

规划错误_级别_错误 为 2。

级别_致命 为 3。

错误_词法 为 100。

错误_非法字符 为 101。

错误_未闭合字串 为 102。

错误_非法十六进 为 103。

错误_语法 为 200。

错误_期望标记 为 201。

错误_非法表达式 为 202。

错误_非法语句 为 203。

错误_未闭合括号 为 204。

错误_语义 为 300。

错误_未定义变量 为 301。

错误_重复定义 为 302。

错误_参数不匹配 为 303。

错误_不可变赋值 为 304。

错误_类型不匹配 为 305。

错误_代码生成 为 400。

错误_栈溢出 为 401。

错误_标签未定义 为 402。

错误_寄存器溢出 为 403。

错误处理_诊断器_创建 接受 零参：
    返还 一组：一组：（空）、0、0。
完。

错误处理_诊断器_设级别 接受 d、最小级别：
    d 的 第 3 项 为 最小级别。
完。

错误处理_诊断器_报告 接受 d、级别、代码、行号、列号：
    若 级别 小于 d 的 第 3 项：
        返还。
    完。
    做 追加 用 d 的 第 0 项、一组：级别、代码、行号、列号。
    若 级别 大于或等于 规划错误_级别_错误：
        d 的 第 1 项 为 d 的 第 1 项 加 1。
    完。
    若 级别 等于 规划错误_级别_警告：
        d 的 第 2 项 为 d 的 第 2 项 加 1。
    完。
完。

错误处理_诊断器_错误 接受 d、代码、行号：
    做 诊断器_报告 用 d、规划错误_级别_错误、代码、行号、0。
完。

错误处理_诊断器_警告 接受 d、代码、行号：
    做 诊断器_报告 用 d、规划错误_级别_警告、代码、行号、0。
完。

诊断器_致命 接受 d、代码、行号：
    做 诊断器_报告 用 d、级别_致命、代码、行号、0。
完。

错误处理_诊断器_错误数 接受 d：
    返还 d 的 第 1 项。
完。

错误处理_诊断器_警告数 接受 d：
    返还 d 的 第 2 项。
完。

错误处理_诊断器_总数 接受 d：
    返还 做 计数 用 d 的 第 0 项。
完。

错误处理_诊断器_有错误 接受 d：
    返还 d 的 第 1 项 大于 0。
完。

级别名 接受 lv：
    若 lv 等于 0：
        返还 「info」。
    完。
    若 lv 等于 1：
        返还 「warn」。
    完。
    若 lv 等于 2：
        返还 「error」。
    完。
    返还 「fatal」。
完。

错误阶段 接受 代码：
    若 代码 大于或等于 100 并且 代码 小于 200：
        返还 1。
    完。
    若 代码 大于或等于 200 并且 代码 小于 300：
        返还 2。
    完。
    若 代码 大于或等于 300 并且 代码 小于 400：
        返还 3。
    完。
    若 代码 大于或等于 400：
        返还 4。
    完。
    返还 0。
完。

生成建议 接受 代码：
    若 代码 等于 错误_未闭合字串：
        返还 1。
    完。
    若 代码 等于 错误_未闭合括号：
        返还 2。
    完。
    若 代码 等于 错误_期望标记：
        返还 3。
    完。
    若 代码 等于 错误_未定义变量：
        返还 4。
    完。
    若 代码 等于 错误_不可变赋值：
        返还 5。
    完。
    若 代码 等于 错误_参数不匹配：
        返还 6。
    完。
    若 代码 等于 错误_类型不匹配：
        返还 7。
    完。
    返还 0。
完。

诊断器_过滤 接受 d、最小级别：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 d 的 第 0 项：
        若 d 的 第 0 项 的 第 i 项 的 第 0 项 大于或等于 最小级别：
            做 追加 用 结果、d 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

诊断器_首错 接受 d：
    可变 i 为 0。
    只要 i 小于 做 计数 用 d 的 第 0 项：
        若 d 的 第 0 项 的 第 i 项 的 第 0 项 大于或等于 规划错误_级别_错误：
            返还 d 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

诊断器_去重 接受 d：
    可变 唯一 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 d 的 第 0 项：
        条目 为 d 的 第 0 项 的 第 i 项。
        可变 是重复 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 唯一：
            若 唯一 的 第 j 项 的 第 1 项 等于 条目 的 第 1 项 并且 唯一 的 第 j 项 的 第 2 项 等于 条目 的 第 2 项：
                是重复 为 真。
            完。
            j 为 j 加 1。
        完。
        若 是重复 等于 假：
            做 追加 用 唯一、条目。
        完。
        i 为 i 加 1。
    完。
    返还 唯一。
完。

诊断器_按行排序 接受 诊断列表：
    n 为 做 计数 用 诊断列表。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 n 减 i 减 1：
            若 诊断列表 的 第 j 项 的 第 2 项 大于 诊断列表 的 第 j 加 1 项 的 第 2 项：
                临时 为 诊断列表 的 第 j 项。
                诊断列表 的 第 j 项 为 诊断列表 的 第 j 加 1 项。
                诊断列表 的 第 j 加 1 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

恢复_跳过 为 1。

恢复_插入 为 2。

恢复_同步 为 3。

恢复策略 接受 代码：
    若 代码 大于或等于 100 并且 代码 小于 200：
        返还 恢复_跳过。
    完。
    若 代码 大于或等于 200 并且 代码 小于 300：
        返还 恢复_同步。
    完。
    若 代码 大于或等于 300 并且 代码 小于 400：
        返还 恢复_跳过。
    完。
    返还 恢复_同步。
完。

格式化诊断 接受 条目：
    级 为 做 级别名 用 条目 的 第 0 项。
    行 为 做 转字符串 用 条目 的 第 2 项。
    码 为 做 转字符串 用 条目 的 第 1 项。
    返还 做 字符串拼接 用 级、做 字符串拼接 用 「[」、做 字符串拼接 用 码、做 字符串拼接 用 「] 行:」、行。
完。

诊断器_摘要 接受 d：
    返还 做 字符串拼接 用 做 转字符串 用 d 的 第 1 项、做 字符串拼接 用 「 错误, 」、做 字符串拼接 用 做 转字符串 用 d 的 第 2 项、「 警告」。
完。

诊断器_建议恢复 接受 条目：
    码 为 条目 的 第 1 项。
    若 码 大于或等于 1000 并且 码 小于 2000：
        返还 「语法错误: 检查括号和分号」。
    完。
    若 码 大于或等于 2000 并且 码 小于 3000：
        返还 「类型错误: 检查类型注解」。
    完。
    若 码 大于或等于 3000 并且 码 小于 4000：
        返还 「语义错误: 检查变量声明和作用域」。
    完。
    若 码 大于或等于 4000 并且 码 小于 5000：
        返还 「链接错误: 检查导入和模块依赖」。
    完。
    返还 「未知错误类型」。
完。

诊断器_是致命 接受 条目：
    返还 条目 的 第 0 项 等于 0。
完。

诊断器_致命数 接受 d：
    可变 n 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 d 的 第 0 项：
        若 做 诊断器_是致命 用 d 的 第 0 项 的 第 i 项：
            n 为 n 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 n。
完。

诊断器_按行分组 接受 d：
    可变 行号列表 为 一组：（空）。
    可变 条目组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 d 的 第 0 项：
        行 为 d 的 第 0 项 的 第 i 项 的 第 2 项。
        可变 已有 为 负 1。
        可变 j 为 0。
        只要 j 小于 做 计数 用 行号列表：
            若 行号列表 的 第 j 项 等于 行：
                已有 为 j。
            完。
            j 为 j 加 1。
        完。
        若 已有 大于或等于 0：
            做 追加 用 条目组 的 第 已有 项、d 的 第 0 项 的 第 i 项。
        否则：
            做 追加 用 行号列表、行。
            做 追加 用 条目组、一组：d 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：行号列表、条目组。
完。

诊断器_按级别分组 接受 d：
    可变 错误列表 为 一组：（空）。
    可变 警告列表 为 一组：（空）。
    可变 提示列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 d 的 第 0 项：
        级 为 d 的 第 0 项 的 第 i 项 的 第 0 项。
        若 级 等于 0：
            做 追加 用 错误列表、d 的 第 0 项 的 第 i 项。
        否则若 级 等于 1：
            做 追加 用 警告列表、d 的 第 0 项 的 第 i 项。
        否则：
            做 追加 用 提示列表、d 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：错误列表、警告列表、提示列表。
完。

可变 _最大错误数 为 100。

诊断器_设最大错误 接受 n：
    _最大错误数 为 n。
完。

诊断器_已达上限 接受 d：
    返还 d 的 第 1 项 大于或等于 _最大错误数。
完。

诊断器_格式化全部 接受 d：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 d 的 第 0 项：
        做 追加 用 结果、做 格式化诊断 用 d 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

诊断器_格式化带建议 接受 d：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 d 的 第 0 项：
        格式 为 做 格式化诊断 用 d 的 第 0 项 的 第 i 项。
        建议 为 做 诊断器_建议恢复 用 d 的 第 0 项 的 第 i 项。
        做 追加 用 结果、一组：格式、建议。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _诊断_总数 为 0。

可变 _诊断_去重数 为 0。

错误处理_诊断_记录 接受 零参：
    _诊断_总数 为 _诊断_总数 加 1。
完。

诊断_记录去重 接受 零参：
    _诊断_去重数 为 _诊断_去重数 加 1。
完。

本章 收束。
