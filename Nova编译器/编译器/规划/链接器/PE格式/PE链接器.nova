《PE链接器》

引用《..·..·..·..·标准库·基础》。

引用《..·..·..·..·自举兼容桥》。

PE链接器_PE_魔数 为 0x00004550。

PE32P_魔数 为 0x020B。

映像基址 为 0x00400000。

节对齐 为 0x1000。

头部大小 为 0x200。

节_包含代码 为 0x20。

节_可执行 为 0x20000000。

节_可读 为 0x40000000。

节_可写 为 0x80000000。

节_已初始化数据 为 0x40。

机器_AMD64 为 0x8664。

PE_对齐 接受 值、边界：
    余 为 值 取余于 边界。
    若 余 等于 0：
        返还 值。
    完。
    返还 值 加 边界 减 余。
完。

PE_段_创建 接受 名称、rva、虚拟大小、文件偏移、文件大小、属性：
    返还 一组：名称、rva、虚拟大小、文件偏移、文件大小、属性。
完。

PE_导入_创建 接受 dll名、函数列表：
    返还 一组：dll名、函数列表、0、0。
完。

PE_计算导入表 接受 导入列表：
    可变 总函数 为 0。
    可变 名称大小 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 导入列表：
        条目 为 导入列表 的 第 i 项。
        fn数 为 做 计数 用 条目 的 第 1 项。
        总函数 为 总函数 加 fn数。
        名称大小 为 名称大小 加 16。
        名称大小 为 名称大小 加 fn数 乘 24。
        i 为 i 加 1。
    完。
    dll数 为 做 计数 用 导入列表。
    idt大小 为 (dll数 加 1) 乘 20。
    ilt大小 为 (总函数 加 dll数) 乘 8。
    iat大小 为 ilt大小。
    返还 一组：idt大小、ilt大小、iat大小、名称大小、总函数、idt大小 加 ilt大小 加 iat大小 加 名称大小。
完。

PE_计算布局 接受 代码大小、导入列表：
    导入项 为 做 PE_计算导入表 用 导入列表。
    代码段_相对虚地址 为 节对齐。
    代码段_偏移 为 头部大小。
    代码段_虚大小 为 代码大小。
    代码段_文件大小 为 做 PE_对齐 用 代码大小、文件对齐。
    导入段_相对虚地址 为 代码段_相对虚地址 加 做 PE_对齐 用 代码段_虚大小、节对齐。
    导入段_偏移 为 代码段_偏移 加 代码段_文件大小。
    导入段_虚大小 为 导入项 的 第 5 项。
    导入段_文件大小 为 做 PE_对齐 用 导入段_虚大小、文件对齐。
    总大小 为 导入段_偏移 加 导入段_文件大小。
    入口 为 代码段_相对虚地址。
    返还 一组：代码段_相对虚地址、代码段_偏移、代码段_文件大小、导入段_相对虚地址、导入段_偏移、导入段_文件大小、总大小、入口。
完。

PE_导入表_相对虚地址 接受 导入段_相对虚地址、导入列表、函数索引：
    导入项 为 做 PE_计算导入表 用 导入列表。
    返还 导入段_相对虚地址 加 导入项 的 第 0 项 加 导入项 的 第 1 项 加 函数索引 乘 8。
完。

PE_计算重定位 接受 指令rva、iat_rva：
    返还 iat_rva 减 (指令rva 加 6)。
完。

PE_计算RIP相对 接受 指令rva、目标rva、指令长度：
    返还 目标rva 减 (指令rva 加 指令长度)。
完。

PE_DOS头大小 接受 零参：
    返还 64。
完。

PE_COFF头 接受 机器、段数、可选头大小：
    返还 一组：机器、段数、0、0、可选头大小、0x22。
完。

PE_可选头大小 为 112 加 8 乘 16。

映像子系统_控制台 为 3。

映像子系统_窗口 为 2。

DLL_NX保护 为 0x100。

DLL_地址随机 为 0x40。

DLL_终端服务 为 0x400。

DLL_控制流保护 为 0x4000。

DLL_默认 为 0x8160。

PE_镜像大小 接受 布局：
    返还 布局 的 第 3 项 加 做 PE_对齐 用 布局 的 第 5 项、节对齐。
完。

PE_扩展布局 接受 代码大小、数据大小、_bss大小、导入列表：
    导入项 为 做 PE_计算导入表 用 导入列表。
    代码段_相对虚地址 为 节对齐。
    代码段_偏移 为 头部大小。
    代码段_虚大小 为 代码大小。
    代码段_文件大小 为 做 PE_对齐 用 代码大小、文件对齐。
    导入段_相对虚地址 为 代码段_相对虚地址 加 做 PE_对齐 用 代码段_虚大小、节对齐。
    导入段_偏移 为 代码段_偏移 加 代码段_文件大小。
    导入段_虚大小 为 导入项 的 第 5 项。
    导入段_文件大小 为 做 PE_对齐 用 导入段_虚大小、文件对齐。
    数据段_相对虚地址 为 导入段_相对虚地址 加 做 PE_对齐 用 导入段_虚大小、节对齐。
    数据段_偏移 为 导入段_偏移 加 导入段_文件大小。
    数据段_文件大小 为 做 PE_对齐 用 数据大小、文件对齐。
    总大小 为 数据段_偏移 加 数据段_文件大小。
    入口 为 代码段_相对虚地址。
    返还 一组：代码段_相对虚地址、代码段_偏移、代码段_文件大小、导入段_相对虚地址、导入段_偏移、导入段_文件大小、数据段_相对虚地址、数据段_偏移、数据段_文件大小、总大小、入口。
完。

PE_段数 接受 有数据、有bss：
    可变 n 为 2。
    若 有数据：
        n 为 n 加 1。
    完。
    若 有bss：
        n 为 n 加 1。
    完。
    返还 n。
完。

PE_校验和 接受 文件数据、文件大小：
    可变 校验 为 0。
    校验偏移 为 0x58。
    可变 i 为 0。
    只要 i 加 1 小于 文件大小：
        若 i 不等于 校验偏移 并且 i 不等于 校验偏移 加 2：
            字 为 文件数据 的 第 i 项 加 文件数据 的 第 i 加 1 项 乘 256。
            校验 为 校验 加 字。
            若 校验 大于 0xFFFF：
                校验 为 (校验 位与 0xFFFF) 加 (校验 右移 16)。
            完。
        完。
        i 为 i 加 2。
    完。
    若 文件大小 取余于 2 不等于 0：
        校验 为 校验 加 文件数据 的 第 文件大小 减 1 项。
        若 校验 大于 0xFFFF：
            校验 为 (校验 位与 0xFFFF) 加 (校验 右移 16)。
        完。
    完。
    返还 (校验 位与 0xFFFF) 加 文件大小。
完。

机器_ARM64 为 0xAA64。

机器_I386 为 0x014C。

机器_RISCV64 为 0x5064。

目录_EXPORT 为 0。

目录_IMPORT 为 1。

目录_RESOURCE 为 2。

目录_EXCEPTION 为 3。

目录_SECURITY 为 4。

目录_BASERELOC 为 5。

目录_DEBUG 为 6。

目录_TLS 为 9。

目录_IAT 为 12。

目录_DELAY_IMPORT 为 13。

目录_CLR_HEADER 为 14。

DIR_数量 为 16。

映像重定位_绝对 为 0。

映像重定位_高位 为 1。

映像重定位_低位 为 2。

映像重定位_高低位 为 3。

映像重定位_目录64 为 10。

PE_安全对齐 接受 值、边界：
    若 边界 小于或等于 0：
        返还 值。
    完。
    余 为 值 取余于 边界。
    若 余 等于 0：
        返还 值。
    完。
    返还 值 加 边界 减 余。
完。

PE_有效机器 接受 m：
    若 m 等于 机器_AMD64：
        返还 1。
    完。
    若 m 等于 机器_ARM64：
        返还 1。
    完。
    若 m 等于 机器_I386：
        返还 1。
    完。
    若 m 等于 机器_RISCV64：
        返还 1。
    完。
    返还 0。
完。

PE_机器名 接受 m：
    若 m 等于 机器_AMD64：
        返还 「AMD64」。
    完。
    若 m 等于 机器_ARM64：
        返还 「ARM64」。
    完。
    若 m 等于 机器_I386：
        返还 「i386」。
    完。
    若 m 等于 机器_RISCV64：
        返还 「RISC-V64」。
    完。
    返还 「unknown」。
完。

PE_段可执行 接受 属性：
    返还 属性 位与 节_可执行 不等于 0。
完。

PE_段可写 接受 属性：
    返还 属性 位与 节_可写 不等于 0。
完。

PE_段可读 接受 属性：
    返还 属性 位与 节_可读 不等于 0。
完。

PE_重定位条目 接受 类型、偏移：
    返还 类型 左移 12 位或 偏移 位与 0xFFF。
完。

PE_重定位块大小 接受 条目数：
    原始 为 8 加 条目数 乘 2。
    返还 做 PE_安全对齐 用 原始、4。
完。

可变 pe缓冲 为 做 缓冲区 用 0x20000。

可变 pe位 为 0。

pe写 接受 b：
    做 缓冲写 用 pe缓冲、pe位、b。
    pe位 为 pe位 加 1。
完。

pe写16 接受 v：
    做 缓冲写16 用 pe缓冲、pe位、v。
    pe位 为 pe位 加 2。
完。

pe写32 接受 v：
    做 缓冲写32 用 pe缓冲、pe位、v。
    pe位 为 pe位 加 4。
完。

pe写64 接受 v：
    做 缓冲写64 用 pe缓冲、pe位、v。
    pe位 为 pe位 加 8。
完。

pe写组 接受 列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 pe写 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

pe写串 接受 s：
    可变 i 为 0。
    只要 i 小于 做 长度 用 s：
        做 pe写 用 做 字节值 用 s、i。
        i 为 i 加 1。
    完。
完。

pe写C串 接受 s：
    做 pe写串 用 s。
    做 pe写 用 0。
完。

pe填零到 接受 目标位：
    只要 pe位 小于 目标位：
        做 pe写 用 0。
    完。
完。

pe修正32 接受 偏移、值：
    做 缓冲写32 用 pe缓冲、偏移、值。
完。

pe修正16 接受 偏移、值：
    做 缓冲写16 用 pe缓冲、偏移、值。
完。

pe修正64 接受 偏移、值：
    做 缓冲写64 用 pe缓冲、偏移、值。
完。

pe写代码 接受 代码源、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        做 pe写 用 做 缓冲读 用 代码源、i。
        i 为 i 加 1。
    完。
完。

PE_写DOS头 接受 零参：
    起始 为 pe位。
    做 pe写16 用 0x5A4D。
    做 pe写16 用 0x0090。
    做 pe写16 用 0x0003。
    做 pe写16 用 0。
    做 pe写16 用 0x0004。
    做 pe写16 用 0。
    做 pe写16 用 0xFFFF。
    做 pe写16 用 0。
    做 pe写16 用 0x00B8。
    做 pe写16 用 0。
    做 pe写16 用 0。
    做 pe写16 用 0。
    做 pe写16 用 0x0040。
    做 pe写16 用 0。
    可变 j 为 0。
    只要 j 小于 36：
        做 pe写 用 0。
        j 为 j 加 1。
    完。
    做 pe写32 用 0x00000080。
    做 pe填零到 用 起始 加 128。
完。

PE_写签名 接受 零参：
    做 pe写32 用 PE链接器_PE_魔数。
完。

PE_写COFF头 接受 机器、段数、时间戳、可选头大小、特征：
    做 pe写16 用 机器。
    做 pe写16 用 段数。
    做 pe写32 用 时间戳。
    做 pe写32 用 0。
    做 pe写32 用 0。
    做 pe写16 用 可选头大小。
    做 pe写16 用 特征。
完。

PE_写可选头 接受 入口RVA、代码大小、代码段_相对虚地址、镜像大小、头大小、子系统、导入段_相对虚地址、imp_size、iat_rva、iat_size：
    可选头起始 为 pe位。
    做 pe写16 用 PE32P_魔数。
    做 pe写 用 0x0E。
    做 pe写 用 0x00。
    做 pe写32 用 代码大小。
    做 pe写32 用 0。
    做 pe写32 用 0。
    做 pe写32 用 入口RVA。
    做 pe写32 用 代码段_相对虚地址。
    做 pe写64 用 映像基址。
    做 pe写32 用 节对齐。
    做 pe写32 用 文件对齐。
    做 pe写16 用 6。
    做 pe写16 用 0。
    做 pe写16 用 0。
    做 pe写16 用 0。
    做 pe写16 用 6。
    做 pe写16 用 0。
    做 pe写32 用 0。
    做 pe写32 用 镜像大小。
    做 pe写32 用 头大小。
    做 pe写32 用 0。
    做 pe写16 用 子系统。
    做 pe写16 用 DLL_默认。
    做 pe写64 用 0x100000。
    做 pe写64 用 0x1000。
    做 pe写64 用 0x100000。
    做 pe写64 用 0x1000。
    做 pe写32 用 0。
    做 pe写32 用 DIR_数量。
    可变 d 为 0。
    只要 d 小于 DIR_数量：
        若 d 等于 目录_IMPORT：
            做 pe写32 用 导入段_相对虚地址。
            做 pe写32 用 imp_size。
        否则若 d 等于 目录_IAT：
            做 pe写32 用 iat_rva。
            做 pe写32 用 iat_size。
        否则：
            做 pe写32 用 0。
            做 pe写32 用 0。
        完。
        d 为 d 加 1。
    完。
完。

PE_写段头 接受 名称、虚拟大小、rva、文件大小、文件偏移、属性：
    可变 i 为 0。
    只要 i 小于 8：
        若 i 小于 做 长度 用 名称：
            做 pe写 用 做 字节值 用 名称、i。
        否则：
            做 pe写 用 0。
        完。
        i 为 i 加 1。
    完。
    做 pe写32 用 虚拟大小。
    做 pe写32 用 rva。
    做 pe写32 用 文件大小。
    做 pe写32 用 文件偏移。
    做 pe写32 用 0。
    做 pe写32 用 0。
    做 pe写16 用 0。
    做 pe写16 用 0。
    做 pe写32 用 属性。
完。

PE_写IDT 接受 导入列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 导入列表：
        条目 为 导入列表 的 第 i 项。
        做 pe写32 用 条目 的 第 2 项。
        做 pe写32 用 0。
        做 pe写32 用 0。
        做 pe写32 用 条目 的 第 4 项。
        做 pe写32 用 条目 的 第 3 项。
        i 为 i 加 1。
    完。
    可变 z 为 0。
    只要 z 小于 20：
        做 pe写 用 0。
        z 为 z 加 1。
    完。
完。

PE_写ILT 接受 提示_相对虚地址组：
    可变 i 为 0。
    只要 i 小于 做 计数 用 提示_相对虚地址组：
        做 pe写64 用 提示_相对虚地址组 的 第 i 项。
        i 为 i 加 1。
    完。
    做 pe写64 用 0。
完。

PE_写IAT 接受 提示_相对虚地址组：
    可变 i 为 0。
    只要 i 小于 做 计数 用 提示_相对虚地址组：
        做 pe写64 用 提示_相对虚地址组 的 第 i 项。
        i 为 i 加 1。
    完。
    做 pe写64 用 0。
完。

PE_写HintName 接受 序号、函数名：
    做 pe写16 用 序号。
    做 pe写C串 用 函数名。
    若 pe位 取余于 2 不等于 0：
        做 pe写 用 0。
    完。
完。

PE_写DLL名 接受 名称：
    做 pe写C串 用 名称。
    若 pe位 取余于 2 不等于 0：
        做 pe写 用 0。
    完。
完。

PE_写重定位块 接受 page_rva、条目列表：
    块大小 为 做 PE_重定位块大小 用 做 计数 用 条目列表。
    做 pe写32 用 page_rva。
    做 pe写32 用 块大小。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目列表：
        做 pe写16 用 条目列表 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 pe位 取余于 4 不等于 0：
        做 pe写 用 0。
    完。
完。

PE_写数据串 接受 s：
    起始 为 pe位。
    做 pe写C串 用 s。
    返还 起始。
完。

PE_写数据组 接受 列表：
    做 pe写组 用 列表。
完。

PE_组装最小 接受 代码源、代码长、导入列表、子系统：
    pe位 为 0。
    布局 为 做 PE_计算布局 用 代码长、导入列表。
    代码段_相对虚地址 为 布局 的 第 0 项。
    代码段_偏移 为 布局 的 第 1 项。
    代码段_文件大小 为 布局 的 第 2 项。
    导入段_相对虚地址 为 布局 的 第 3 项。
    导入段_偏移 为 布局 的 第 4 项。
    导入段_文件大小 为 布局 的 第 5 项。
    总大小 为 布局 的 第 6 项。
    入口RVA 为 布局 的 第 7 项。
    镜像大小 为 做 PE_镜像大小 用 布局。
    导入项 为 做 PE_计算导入表 用 导入列表。
    导入描述_相对虚地址 为 导入段_相对虚地址。
    ilt_rva 为 导入段_相对虚地址 加 导入项 的 第 0 项。
    iat_rva 为 ilt_rva 加 导入项 的 第 1 项。
    做 PE_写DOS头。
    做 PE_写签名。
    做 PE_写COFF头 用 机器_AMD64、2、0、PE_可选头大小、0x22。
    做 PE_写可选头 用 入口RVA、代码长、代码段_相对虚地址、镜像大小、头部大小、子系统、导入描述_相对虚地址、导入项 的 第 0 项 加 导入项 的 第 1 项、iat_rva、导入项 的 第 2 项。
    做 PE_写段头 用 「.text」、代码长、代码段_相对虚地址、代码段_文件大小、代码段_偏移、节_包含代码 位或 节_可执行 位或 节_可读。
    做 PE_写段头 用 「.idata」、导入项 的 第 5 项、导入段_相对虚地址、导入段_文件大小、导入段_偏移、节_已初始化数据 位或 节_可读 位或 节_可写。
    做 pe填零到 用 代码段_偏移。
    做 pe写代码 用 代码源、代码长。
    做 pe填零到 用 代码段_偏移 加 代码段_文件大小。
    可变 dll_i 为 0。
    可变 当前ilt 为 ilt_rva。
    可变 当前iat 为 iat_rva。
    可变 名称区位 为 iat_rva 加 导入项 的 第 2 项。
    只要 dll_i 小于 做 计数 用 导入列表：
        可变 条目 为 导入列表 的 第 dll_i 项。
        fn数 为 做 计数 用 条目 的 第 1 项。
        条目 的 第 2 项 为 当前ilt。
        条目 的 第 3 项 为 当前iat。
        当前ilt 为 当前ilt 加 (fn数 加 1) 乘 8。
        当前iat 为 当前iat 加 (fn数 加 1) 乘 8。
        dll_i 为 dll_i 加 1。
    完。
    做 PE_写IDT 用 导入列表。
    dll_i 为 0。
    可变 hint_rvas_all 为 一组：（空）。
    只要 dll_i 小于 做 计数 用 导入列表：
        条目 为 导入列表 的 第 dll_i 项。
        fn数 为 做 计数 用 条目 的 第 1 项。
        可变 提示_相对虚地址组 为 一组：（空）。
        可变 fi 为 0。
        只要 fi 小于 fn数：
            提示_相对虚地址组 为 做 追加 用 提示_相对虚地址组、名称区位。
            hint_rvas_all 为 做 追加 用 hint_rvas_all、名称区位。
            fn名 为 条目 的 第 1 项 的 第 fi 项。
            hn大小 为 2 加 做 长度 用 fn名 加 1。
            若 hn大小 取余于 2 不等于 0：
                hn大小 为 hn大小 加 1。
            完。
            名称区位 为 名称区位 加 hn大小。
            fi 为 fi 加 1。
        完。
        做 PE_写ILT 用 提示_相对虚地址组。
        条目 的 第 4 项 为 名称区位。
        dll名大小 为 做 长度 用 条目 的 第 0 项 加 1。
        若 dll名大小 取余于 2 不等于 0：
            dll名大小 为 dll名大小 加 1。
        完。
        名称区位 为 名称区位 加 dll名大小。
        dll_i 为 dll_i 加 1。
    完。
    dll_i 为 0。
    可变 all_idx 为 0。
    只要 dll_i 小于 做 计数 用 导入列表：
        条目 为 导入列表 的 第 dll_i 项。
        fn数 为 做 计数 用 条目 的 第 1 项。
        可变 提示_相对虚地址组 为 一组：（空）。
        可变 fi 为 0。
        只要 fi 小于 fn数：
            提示_相对虚地址组 为 做 追加 用 提示_相对虚地址组、hint_rvas_all 的 第 all_idx 项。
            all_idx 为 all_idx 加 1。
            fi 为 fi 加 1。
        完。
        做 PE_写IAT 用 提示_相对虚地址组。
        dll_i 为 dll_i 加 1。
    完。
    dll_i 为 0。
    只要 dll_i 小于 做 计数 用 导入列表：
        条目 为 导入列表 的 第 dll_i 项。
        可变 fi 为 0。
        只要 fi 小于 做 计数 用 条目 的 第 1 项：
            做 PE_写HintName 用 0、条目 的 第 1 项 的 第 fi 项。
            fi 为 fi 加 1。
        完。
        dll_i 为 dll_i 加 1。
    完。
    dll_i 为 0。
    只要 dll_i 小于 做 计数 用 导入列表：
        做 PE_写DLL名 用 导入列表 的 第 dll_i 项 的 第 0 项。
        dll_i 为 dll_i 加 1。
    完。
    dll_i 为 0。
    只要 dll_i 小于 做 计数 用 导入列表：
        idt_条目偏移 为 导入段_偏移 加 dll_i 乘 20。
        做 pe修正32 用 idt_条目偏移 加 12、导入列表 的 第 dll_i 项 的 第 4 项。
        dll_i 为 dll_i 加 1。
    完。
    做 pe填零到 用 总大小。
    返还 一组：pe缓冲、总大小、iat_rva。
完。

PE_组装扩展 接受 代码源、代码长、数据源、数据长、导入列表、子系统：
    pe位 为 0。
    布局 为 做 PE_扩展布局 用 代码长、数据长、0、导入列表。
    代码段_相对虚地址 为 布局 的 第 0 项。
    代码段_偏移 为 布局 的 第 1 项。
    代码段_文件大小 为 布局 的 第 2 项。
    导入段_相对虚地址 为 布局 的 第 3 项。
    导入段_偏移 为 布局 的 第 4 项。
    导入段_文件大小 为 布局 的 第 5 项。
    数据段_相对虚地址 为 布局 的 第 6 项。
    数据段_偏移 为 布局 的 第 7 项。
    数据段_文件大小 为 布局 的 第 8 项。
    总大小 为 布局 的 第 9 项。
    入口RVA 为 布局 的 第 10 项。
    镜像大小 为 数据段_相对虚地址 加 做 PE_对齐 用 数据长、节对齐。
    导入项 为 做 PE_计算导入表 用 导入列表。
    导入描述_相对虚地址 为 导入段_相对虚地址。
    ilt_rva 为 导入段_相对虚地址 加 导入项 的 第 0 项。
    iat_rva 为 ilt_rva 加 导入项 的 第 1 项。
    做 PE_写DOS头。
    做 PE_写签名。
    做 PE_写COFF头 用 机器_AMD64、3、0、PE_可选头大小、0x22。
    做 PE_写可选头 用 入口RVA、代码长、代码段_相对虚地址、镜像大小、头部大小、子系统、导入描述_相对虚地址、导入项 的 第 0 项 加 导入项 的 第 1 项、iat_rva、导入项 的 第 2 项。
    做 PE_写段头 用 「.text」、代码长、代码段_相对虚地址、代码段_文件大小、代码段_偏移、节_包含代码 位或 节_可执行 位或 节_可读。
    做 PE_写段头 用 「.idata」、导入项 的 第 5 项、导入段_相对虚地址、导入段_文件大小、导入段_偏移、节_已初始化数据 位或 节_可读 位或 节_可写。
    做 PE_写段头 用 「.data」、数据长、数据段_相对虚地址、数据段_文件大小、数据段_偏移、节_已初始化数据 位或 节_可读 位或 节_可写。
    做 pe填零到 用 代码段_偏移。
    做 pe写代码 用 代码源、代码长。
    做 pe填零到 用 代码段_偏移 加 代码段_文件大小。
    可变 dll_i 为 0。
    可变 当前ilt 为 ilt_rva。
    可变 当前iat 为 iat_rva。
    可变 名称区位 为 iat_rva 加 导入项 的 第 2 项。
    只要 dll_i 小于 做 计数 用 导入列表：
        可变 条目 为 导入列表 的 第 dll_i 项。
        fn数 为 做 计数 用 条目 的 第 1 项。
        条目 的 第 2 项 为 当前ilt。
        条目 的 第 3 项 为 当前iat。
        当前ilt 为 当前ilt 加 (fn数 加 1) 乘 8。
        当前iat 为 当前iat 加 (fn数 加 1) 乘 8。
        dll_i 为 dll_i 加 1。
    完。
    做 PE_写IDT 用 导入列表。
    dll_i 为 0。
    可变 hint_rvas_all 为 一组：（空）。
    只要 dll_i 小于 做 计数 用 导入列表：
        条目 为 导入列表 的 第 dll_i 项。
        fn数 为 做 计数 用 条目 的 第 1 项。
        可变 提示_相对虚地址组 为 一组：（空）。
        可变 fi 为 0。
        只要 fi 小于 fn数：
            提示_相对虚地址组 为 做 追加 用 提示_相对虚地址组、名称区位。
            hint_rvas_all 为 做 追加 用 hint_rvas_all、名称区位。
            fn名 为 条目 的 第 1 项 的 第 fi 项。
            hn大小 为 2 加 做 长度 用 fn名 加 1。
            若 hn大小 取余于 2 不等于 0：
                hn大小 为 hn大小 加 1。
            完。
            名称区位 为 名称区位 加 hn大小。
            fi 为 fi 加 1。
        完。
        做 PE_写ILT 用 提示_相对虚地址组。
        条目 的 第 4 项 为 名称区位。
        dll名大小 为 做 长度 用 条目 的 第 0 项 加 1。
        若 dll名大小 取余于 2 不等于 0：
            dll名大小 为 dll名大小 加 1。
        完。
        名称区位 为 名称区位 加 dll名大小。
        dll_i 为 dll_i 加 1。
    完。
    dll_i 为 0。
    可变 all_idx 为 0。
    只要 dll_i 小于 做 计数 用 导入列表：
        条目 为 导入列表 的 第 dll_i 项。
        fn数 为 做 计数 用 条目 的 第 1 项。
        可变 提示_相对虚地址组 为 一组：（空）。
        可变 fi 为 0。
        只要 fi 小于 fn数：
            提示_相对虚地址组 为 做 追加 用 提示_相对虚地址组、hint_rvas_all 的 第 all_idx 项。
            all_idx 为 all_idx 加 1。
            fi 为 fi 加 1。
        完。
        做 PE_写IAT 用 提示_相对虚地址组。
        dll_i 为 dll_i 加 1。
    完。
    dll_i 为 0。
    只要 dll_i 小于 做 计数 用 导入列表：
        条目 为 导入列表 的 第 dll_i 项。
        可变 fi 为 0。
        只要 fi 小于 做 计数 用 条目 的 第 1 项：
            做 PE_写HintName 用 0、条目 的 第 1 项 的 第 fi 项。
            fi 为 fi 加 1。
        完。
        dll_i 为 dll_i 加 1。
    完。
    dll_i 为 0。
    只要 dll_i 小于 做 计数 用 导入列表：
        做 PE_写DLL名 用 导入列表 的 第 dll_i 项 的 第 0 项。
        dll_i 为 dll_i 加 1。
    完。
    dll_i 为 0。
    只要 dll_i 小于 做 计数 用 导入列表：
        idt_条目偏移 为 导入段_偏移 加 dll_i 乘 20。
        做 pe修正32 用 idt_条目偏移 加 12、导入列表 的 第 dll_i 项 的 第 4 项。
        dll_i 为 dll_i 加 1。
    完。
    做 pe填零到 用 导入段_偏移 加 导入段_文件大小。
    做 pe写代码 用 数据源、数据长。
    做 pe填零到 用 总大小。
    返还 一组：pe缓冲、总大小、iat_rva、数据段_相对虚地址。
完。

PE_修正IAT调用 接受 代码源、代码偏移、代码段_相对虚地址、iat_rva、函数索引：
    指令rva 为 代码段_相对虚地址 加 代码偏移。
    目标iat 为 iat_rva 加 函数索引 乘 8。
    偏移量 为 目标iat 减 (指令rva 加 6)。
    做 缓冲写32 用 代码源、代码偏移 加 2、偏移量。
完。

PE_修正RIP引用 接受 代码源、代码偏移、代码段_相对虚地址、目标rva：
    指令rva 为 代码段_相对虚地址 加 代码偏移。
    偏移量 为 目标rva 减 (指令rva 加 7)。
    做 缓冲写32 用 代码源、代码偏移 加 3、偏移量。
完。

PE_批量修正IAT 接受 代码源、代码段_相对虚地址、iat_rva、修正列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 修正列表：
        项 为 修正列表 的 第 i 项。
        做 PE_修正IAT调用 用 代码源、项 的 第 0 项、代码段_相对虚地址、iat_rva、项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

PE_批量修正RIP 接受 代码源、代码段_相对虚地址、修正列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 修正列表：
        项 为 修正列表 的 第 i 项。
        做 PE_修正RIP引用 用 代码源、项 的 第 0 项、代码段_相对虚地址、项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

PE_获取缓冲 接受 零参：
    返还 pe缓冲。
完。

PE_获取大小 接受 零参：
    返还 pe位。
完。

PE_重置 接受 零参：
    pe位 为 0。
完。

本章 收束。
