《wasm编码》

※ 线性内存(阴平坦) ←→ 栈(阳结构化)
※ push = 气入栈, pop = 气出栈, call = 气跨函数流动
※ i32/i64/f32/f64: 四种基本"数"量词

引用《..·..·..·..·平台·代码生成·wasm·wasm编码器》。
引用《..·..·..·..·标准库·基础》。

※ Wasm 魔数与版本
※ 'a'
※ 's'
※ 'm'
WASM小写_WASM_版本 为 1。

※ 段ID
段_TYPE 为 1。
段_IMPORT 为 2。
段_FUNC 为 3。
段_TABLE 为 4。
段_MEMORY 为 5。
段_GLOBAL 为 6。
段_EXPORT 为 7。
段_START 为 8。
段_ELEM 为 9。
段_CODE 为 10。
WASM小写_段_DATA 为 11。

※ 值类型
类型_i32 为 0x7F。
类型_i64 为 0x7E。
类型_f32 为 0x7D。
类型_f64 为 0x7C。
类型_void 为 0x40

※ 导出类型
导出_函数 为 0x00。
导出_表 为 0x01。
导出_内存 为 0x02。
导出_全局 为 0x03。

※ 操作码 — 控制流
操作码_unreachable 为 0x00。
操作码_nop 为 0x01。
操作码_block 为 0x02。
操作码_loop 为 0x03。
操作码_if 为 0x04。
操作码_else 为 0x05。
操作码_end 为 0x0B。
操作码_br 为 0x0C。
操作码_br_if 为 0x0D。
操作码_return 为 0x0F。
操作码_call 为 0x10。

※ 操作码 — 变量
操作码_local_get 为 0x20。
操作码_local_set 为 0x21。
操作码_local_tee 为 0x22。
操作码_global_get 为 0x23。
操作码_global_set 为 0x24。

※ 操作码 — 内存
操作码_i32_load 为 0x28。
操作码_i64_load 为 0x29。
操作码_i32_store 为 0x36。
操作码_i64_store 为 0x37。
操作码_mem_size 为 0x3F。
操作码_mem_grow 为 0x40。

※ 操作码 — 常量
操作码_i32_const 为 0x41。
操作码_i64_const 为 0x42。
操作码_f32_const 为 0x43。
操作码_f64_const 为 0x44。

※ 操作码 — i32比较
操作码_i32_eqz 为 0x45。
操作码_i32_eq 为 0x46。
操作码_i32_ne 为 0x47。
操作码_i32_lt_s 为 0x48。
操作码_i32_gt_s 为 0x4A。
操作码_i32_le_s 为 0x4C。
操作码_i32_ge_s 为 0x4E。

※ 操作码 — i64比较
操作码_i64_eqz 为 0x50。
操作码_i64_eq 为 0x51。
操作码_i64_ne 为 0x52。
操作码_i64_lt_s 为 0x53。
操作码_i64_gt_s 为 0x55。
操作码_i64_le_s 为 0x57。
操作码_i64_ge_s 为 0x59。

※ 操作码 — i32算术
操作码_i32_add 为 0x6A。
操作码_i32_sub 为 0x6B。
操作码_i32_mul 为 0x6C。
操作码_i32_div_s 为 0x6D。
操作码_i32_rem_s 为 0x6F。
操作码_i32_and 为 0x71。
操作码_i32_or 为 0x72。
操作码_i32_xor 为 0x73。
操作码_i32_shl 为 0x74。
操作码_i32_shr_s 为 0x75。

※ 操作码 — i64算术
操作码_i64_add 为 0x7C。
操作码_i64_sub 为 0x7D。
操作码_i64_mul 为 0x7E。
操作码_i64_div_s 为 0x7F。
操作码_i64_rem_s 为 0x81。
操作码_i64_and 为 0x83。
操作码_i64_or 为 0x84。
操作码_i64_xor 为 0x85。
操作码_i64_shl 为 0x86。
操作码_i64_shr_s 为 0x87。

※ 操作码 — i32无符号比较
操作码_i32_lt_u 为 0x49。
操作码_i32_gt_u 为 0x4B。
操作码_i32_le_u 为 0x4D。
操作码_i32_ge_u 为 0x4F。

※ 操作码 — i64无符号比较
操作码_i64_lt_u 为 0x54。
操作码_i64_gt_u 为 0x56。
操作码_i64_le_u 为 0x58。
操作码_i64_ge_u 为 0x5A。

※ 操作码 — i32无符号算术
操作码_i32_div_u 为 0x6E。
操作码_i32_rem_u 为 0x70。
操作码_i32_shr_u 为 0x76。
操作码_i32_rotl 为 0x77。
操作码_i32_rotr 为 0x78。
操作码_i32_clz 为 0x67。
操作码_i32_ctz 为 0x68。
操作码_i32_位计数 为 0x69。

※ 操作码 — i64无符号算术
操作码_i64_div_u 为 0x80。
操作码_i64_rem_u 为 0x82。
操作码_i64_shr_u 为 0x88。
操作码_i64_rotl 为 0x89。
操作码_i64_rotr 为 0x8A。
操作码_i64_clz 为 0x79。
操作码_i64_ctz 为 0x7A。
操作码_i64_位计数 为 0x7B。

※ 操作码 — f32
操作码_f32_abs 为 0x8B。
操作码_f32_neg 为 0x8C。
操作码_f32_ceil 为 0x8D。
操作码_f32_floor 为 0x8E。
操作码_f32_sqrt 为 0x91。
操作码_f32_add 为 0x92。
操作码_f32_sub 为 0x93。
操作码_f32_mul 为 0x94。
操作码_f32_div 为 0x95。
操作码_f32_min 为 0x96。
操作码_f32_max 为 0x97。
操作码_f32_eq 为 0x5B。
操作码_f32_ne 为 0x5C。
操作码_f32_lt 为 0x5D。
操作码_f32_gt 为 0x5E。
操作码_f32_le 为 0x5F。
操作码_f32_ge 为 0x60。

※ 操作码 — f64
操作码_f64_abs 为 0x99。
操作码_f64_neg 为 0x9A。
操作码_f64_ceil 为 0x9B。
操作码_f64_floor 为 0x9C。
操作码_f64_sqrt 为 0x9F。
操作码_f64_add 为 0xA0。
操作码_f64_sub 为 0xA1。
操作码_f64_mul 为 0xA2。
操作码_f64_div 为 0xA3。
操作码_f64_min 为 0xA4。
操作码_f64_max 为 0xA5。
操作码_f64_eq 为 0x61。
操作码_f64_ne 为 0x62。
操作码_f64_lt 为 0x63。
操作码_f64_gt 为 0x64。
操作码_f64_le 为 0x65。
操作码_f64_ge 为 0x66。

※ 操作码 — 更多转换
操作码_i32_wrap_i64 为 0xA7。
操作码_i64_extend_i32s 为 0xAC。
操作码_i64_extend_i32u 为 0xAD。
操作码_i32_trunc_f32_s 为 0xA8。
操作码_i32_trunc_f64_s 为 0xAA。
操作码_f32_convert_i32_s 为 0xB2。
操作码_f64_convert_i32_s 为 0xB7。
操作码_f32_demote_f64 为 0xB6。
操作码_f64_promote_f32 为 0xBB。
操作码_i32_reinterpret_f32 为 0xBC。
操作码_f32_reinterpret_i32 为 0xBE。

※ 操作码 — 扩展控制流
操作码_br_table 为 0x0E。
操作码_call_indirect 为 0x11。
操作码_drop 为 0x1A。
操作码_select 为 0x1B。

※ 操作码 — 扩展内存
操作码_i32_load8_s 为 0x2C。
操作码_i32_load8_u 为 0x2D。
操作码_i32_load16_s 为 0x2E。
操作码_i32_load16_u 为 0x2F。
操作码_i64_load8_s 为 0x30。
操作码_i64_load8_u 为 0x31。
操作码_i64_load16_s 为 0x32。
操作码_i64_load16_u 为 0x33。
操作码_i64_load32_s 为 0x34。
操作码_i64_load32_u 为 0x35。
操作码_i32_store8 为 0x3A。
操作码_i32_store16 为 0x3B。
操作码_i64_store8 为 0x3C。
操作码_i64_store16 为 0x3D。
操作码_i64_store32 为 0x3E。

※ 操作码 — f32更多
操作码_f32_trunc 为 0x8F。
操作码_f32_nearest 为 0x90。
操作码_f32_copysign 为 0x98。

※ 操作码 — f64更多
操作码_f64_trunc 为 0x9D。
操作码_f64_nearest 为 0x9E。
操作码_f64_copysign 为 0xA6。

※ 操作码 — 更多转换 (补全)
操作码_i32_trunc_f32_u 为 0xA9。
操作码_i32_trunc_f64_u 为 0xAB。
操作码_i64_trunc_f32_s 为 0xAE。
操作码_i64_trunc_f32_u 为 0xAF。
操作码_i64_trunc_f64_s 为 0xB0。
操作码_i64_trunc_f64_u 为 0xB1。
操作码_f32_convert_i32_u 为 0xB3。
操作码_f32_convert_i64_s 为 0xB4。
操作码_f32_convert_i64_u 为 0xB5。
操作码_f64_convert_i32_u 为 0xB8。
操作码_f64_convert_i64_s 为 0xB9。
操作码_f64_convert_i64_u 为 0xBA。
操作码_i64_reinterpret_f64 为 0xBD。
操作码_f64_reinterpret_i64 为 0xBF。

※ 操作码 — i32扩展 (sign-extension)
操作码_i32_extend8_s 为 0xC0。
操作码_i32_extend16_s 为 0xC1。
操作码_i64_extend8_s 为 0xC2。
操作码_i64_extend16_s 为 0xC3。
操作码_i64_extend32_s 为 0xC4。

※ 操作码 — 引用类型
操作码_ref_null 为 0xD0。
操作码_ref_is_null 为 0xD1。
操作码_ref_func 为 0xD2。

※ 操作码 — 多值select
操作码_select_t 为 0x1C。

※ 表操作 (0xFC前缀子操作码)
操作码_prefix_fc 为 0xFC。
流控_memory_copy 为 10。
流控_memory_fill 为 11。
流控_table_init 为 12。
流控_elem_drop 为 13。
流控_table_copy 为 14。
流控_table_grow 为 15。
流控_table_size 为 16。
流控_table_fill 为 17。

※ 非陷阱转换 (0xFC前缀)
流控_i32_trunc_sat_f32_s 为 0。
流控_i32_trunc_sat_f32_u 为 1。
流控_i32_trunc_sat_f64_s 为 2。
流控_i32_trunc_sat_f64_u 为 3。
流控_i64_trunc_sat_f32_s 为 4。
流控_i64_trunc_sat_f32_u 为 5。
流控_i64_trunc_sat_f64_s 为 6。
流控_i64_trunc_sat_f64_u 为 7。

※ SIMD前缀
操作码_prefix_simd 为 0xFD。

※ 值类型扩展
类型_funcref 为 0x70。
类型_externref 为 0x6F。
类型_v128 为 0x7B。

※ 限定类型(limits)
限定_无上限 为 0x00。
限定_有上限 为 0x01。

※ LEB128 编码 (Wasm核心编码格式)

※ 无符号 LEB128
wasm_编码_uleb128 接受 值) { 定义 结果：
 可变 v 为 值。
 可变 已完成 为 0。
 只要 已完成 等于 0：
 v 为 v 右移 7。
 若 v 等于 0：
 做 追加 用 结果、字节。
 已完成 为 1。
 否则：
 做 追加 用 结果、字节 位或 0x80。
 完。
 完。
 返还 结果。
完。

※ 有符号 LEB128
wasm_编码_sleb128 接受 值) { 定义 结果：
 可变 v 为 值。
 可变 已完成 为 0。
 只要 已完成 等于 0：
 v 为 v 右移 7。
 若 v 等于 0：
 做 追加 用 结果、字节。
 已完成 为 1。
 否则：
 做 追加 用 结果、字节 位或 0x80。
 完。
 否则：
 做 追加 用 结果、字节。
 已完成 为 1。
 否则：
 做 追加 用 结果、字节 位或 0x80。
 完。
 否则：
 做 追加 用 结果、字节 位或 0x80。
 完。
 完。
 完。
 返还 结果。
完。

※ 模块构建辅助

※ Wasm模块头 (8字节: magic+version)
模块头 接受 ) ：返还 [WASM_魔数_0、WASM_魔数_1、WASM_魔数_2、WASM_魔数_3：
 WASM小写_WASM_版本, 0x00, 0x00, 0x00]。
完。

※ 函数类型: (参数类型列表) -> (返回类型列表)
wasm_编码_函数类型 接受 参数列表、返回列表) { 定义 结果：
 ※ 参数
 plen 为 做 编码_uleb128 用 做 计数 用 参数列表。
 可变 pi 为 0。
 只要 pi 小于 做 计数 用 plen：
 pi 为 pi 加 1 }。
 pi 为 0。
 只要 pi 小于 做 计数 用 参数列表：
 pi 为 pi 加 1 }。
 ※ 回
 rlen 为 做 编码_uleb128 用 做 计数 用 返回列表。
 可变 ri 为 0。
 只要 ri 小于 做 计数 用 rlen：
 ri 为 ri 加 1 }。
 ri 为 0。
 只要 ri 小于 做 计数 用 返回列表：
 ri 为 ri 加 1 }。
 返还 结果。
完。

※ 段封装: 段ID + LEB128(内容长度) + 内容
封装段 接受 段id、内容) { 定义 结果：
 len 为 做 编码_uleb128 用 做 计数 用 内容。
 可变 i 为 0。
 只要 i 小于 做 计数 用 len：
 i 为 i 加 1 }。
 i 为 0。
 只要 i 小于 做 计数 用 内容：
 i 为 i 加 1 }。
 返还 结果。
完。

※ 导出条目
wasm_编码_导出 接受 名称字节、类型、索引) { 定义 结果：
 名长 为 做 编码_uleb128 用 做 计数 用 名称字节。
 可变 i 为 0。
 只要 i 小于 做 计数 用 名长：
 i 为 i 加 1 }。
 i 为 0。
 只要 i 小于 做 计数 用 名称字节：
 i 为 i 加 1 }。
 做 追加 用 结果、类型。
 索引 为 做 编码_uleb128 用 索引。
 i 为 0。
 只要 i 小于 做 计数 用 索引：
 i 为 i 加 1 }。
 返还 结果。
完。

※ 内存段: limits (min_pages, max_pages可选)
wasm_编码_内存 接受 最小页、0) { 定义 结果：
 最短编码 为 做 编码_uleb128 用 最小页。
 可变 i 为 0。
 只要 i 小于 做 计数 用 最短编码：
 i 为 i 加 1 }。
 返还 结果。
完。

※ WASM指令编码

WASM_不可达 为 0x00。
WASM_空操作 为 0x01。
WASM_块 为 0x02。
WASM_循环 为 0x03。
WASM_如果 为 0x04。
WASM_否则 为 0x05。
WASM_结束 为 0x0B。
WASM_跳转 为 0x0C。
WASM_条件跳 为 0x0D。
WASM_返回 为 0x0F。
WASM_调用 为 0x10。
WASM_丢弃 为 0x1A。
WASM_本地读 为 0x20。
WASM_本地写 为 0x21。
WASM_全局读 为 0x23。
WASM_全局写 为 0x24。

wasm_编码_调用 接受 函数索引) { 定义 可变 结果：
 编码值 为 做 编码_uleb128 用 函数索引。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

wasm_编码_本地读 接受 索引) { 定义 可变 结果：
 编码值 为 做 编码_uleb128 用 索引。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

wasm_编码_本地写 接受 索引) { 定义 可变 结果：
 编码值 为 做 编码_uleb128 用 索引。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

※ WASM算术指令

WASM_i32_加 为 0x6A。
WASM_i32_减 为 0x6B。
WASM_i32_乘 为 0x6C。
WASM_i32_除s 为 0x6D。
WASM_i32_余s 为 0x6F。
WASM_i32_与 为 0x71。
WASM_i32_或 为 0x72。
WASM_i32_异或 为 0x73。
WASM_i32_左移 为 0x74。
WASM_i32_右移s 为 0x75。
WASM_i32_等 为 0x46。
WASM_i32_不等 为 0x47。
WASM_i32_小于s 为 0x48。
WASM_i32_大于s 为 0x4A。

wasm_编码_i32常量 接受 值) { 定义 可变 结果：
 编码值 为 做 编码_sleb128 用 值。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

※ WASM内存指令

wasm_编码_i32加载 接受 对齐、偏移) { 定义 可变 结果：
 a 为 做 编码_uleb128 用 对齐。
 可变 i 为 0。
 只要 i 小于 做 计数 用 a：
 o 为 做 编码_uleb128 用 偏移。
 i 为 0。
 只要 i 小于 做 计数 用 o：
 返还 结果。
完。

wasm_编码_i32存储 接受 对齐、偏移) { 定义 可变 结果：
 a 为 做 编码_uleb128 用 对齐。
 可变 i 为 0。
 只要 i 小于 做 计数 用 a：
 o 为 做 编码_uleb128 用 偏移。
 i 为 0。
 只要 i 小于 做 计数 用 o：
 返还 结果。
完。

※ WASM i64常量/加载/存储

wasm_编码_i64常量 接受 值) { 定义 可变 结果：
 编码值 为 做 wasm_编码_sleb128 用 值。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

wasm_编码_i64加载 接受 对齐、偏移) { 定义 可变 结果：
 a 为 做 wasm_编码_uleb128 用 对齐。
 可变 i 为 0。
 只要 i 小于 做 计数 用 a：
 o 为 做 wasm_编码_uleb128 用 偏移。
 i 为 0。
 只要 i 小于 做 计数 用 o：
 返还 结果。
完。

wasm_编码_i64存储 接受 对齐、偏移) { 定义 可变 结果：
 a 为 做 wasm_编码_uleb128 用 对齐。
 可变 i 为 0。
 只要 i 小于 做 计数 用 a：
 o 为 做 wasm_编码_uleb128 用 偏移。
 i 为 0。
 只要 i 小于 做 计数 用 o：
 返还 结果。
完。

※ WASM f32/f64常量

wasm_编码_f32常量 接受 b0、b1、b2、b3) ：返还 [0x43、b0&0xFF、b1&0xFF、b2&0xFF、b3&0xFF]：
完。

wasm_编码_f64常量 接受 b0、b1、b2、b3、b4、b5、b6、b7) ：返还 [0x44、b0&0xFF、b1&0xFF、b2&0xFF、b3&0xFF、b4&0xFF、b5&0xFF、b6&0xFF、b7&0xFF]：
完。

※ WASM f32/f64加载/存储

wasm_编码_f32加载 接受 对齐、偏移) { 定义 可变 结果：
 a 为 做 wasm_编码_uleb128 用 对齐。
 可变 i 为 0。
 只要 i 小于 做 计数 用 a：
 o 为 做 wasm_编码_uleb128 用 偏移。
 i 为 0。
 只要 i 小于 做 计数 用 o：
 返还 结果。
完。

wasm_编码_f32存储 接受 对齐、偏移) { 定义 可变 结果：
 a 为 做 wasm_编码_uleb128 用 对齐。
 可变 i 为 0。
 只要 i 小于 做 计数 用 a：
 o 为 做 wasm_编码_uleb128 用 偏移。
 i 为 0。
 只要 i 小于 做 计数 用 o：
 返还 结果。
完。

wasm_编码_f64加载 接受 对齐、偏移) { 定义 可变 结果：
 a 为 做 wasm_编码_uleb128 用 对齐。
 可变 i 为 0。
 只要 i 小于 做 计数 用 a：
 o 为 做 wasm_编码_uleb128 用 偏移。
 i 为 0。
 只要 i 小于 做 计数 用 o：
 返还 结果。
完。

wasm_编码_f64存储 接受 对齐、偏移) { 定义 可变 结果：
 a 为 做 wasm_编码_uleb128 用 对齐。
 可变 i 为 0。
 只要 i 小于 做 计数 用 a：
 o 为 做 wasm_编码_uleb128 用 偏移。
 i 为 0。
 只要 i 小于 做 计数 用 o：
 返还 结果。
完。

※ WASM 子字节内存操作

wasm_编码_内存操作 接受 操作码、对齐、偏移) { 定义 可变 结果：
 a 为 做 wasm_编码_uleb128 用 对齐。
 可变 i 为 0。
 只要 i 小于 做 计数 用 a：
 o 为 做 wasm_编码_uleb128 用 偏移。
 i 为 0。
 只要 i 小于 做 计数 用 o：
 返还 结果。
完。

※ i32子字节加载
wasm_编码_i32加载8s 接受 对齐、偏移) ：返还 做 wasm_编码_内存操作 用 0x2C、对齐、偏移 }：
wasm_编码_i32加载8u 接受 对齐、偏移) ：返还 做 wasm_编码_内存操作 用 0x2D、对齐、偏移 }：
wasm_编码_i32加载16s 接受 对齐、偏移)：返还 做 wasm_编码_内存操作 用 0x2E、对齐、偏移 }：
wasm_编码_i32加载16u 接受 对齐、偏移)：返还 做 wasm_编码_内存操作 用 0x2F、对齐、偏移 }：
※ i64子字节加载
wasm_编码_i64加载8s 接受 对齐、偏移) ：返还 做 wasm_编码_内存操作 用 0x30、对齐、偏移 }：
wasm_编码_i64加载8u 接受 对齐、偏移) ：返还 做 wasm_编码_内存操作 用 0x31、对齐、偏移 }：
wasm_编码_i64加载16s 接受 对齐、偏移)：返还 做 wasm_编码_内存操作 用 0x32、对齐、偏移 }：
wasm_编码_i64加载16u 接受 对齐、偏移)：返还 做 wasm_编码_内存操作 用 0x33、对齐、偏移 }：
wasm_编码_i64加载32s 接受 对齐、偏移)：返还 做 wasm_编码_内存操作 用 0x34、对齐、偏移 }：
wasm_编码_i64加载32u 接受 对齐、偏移)：返还 做 wasm_编码_内存操作 用 0x35、对齐、偏移 }：
※ i32子字节存储
wasm_编码_i32存储8 接受 对齐、偏移) ：返还 做 wasm_编码_内存操作 用 0x3A、对齐、偏移 }：
wasm_编码_i32存储16 接受 对齐、偏移) ：返还 做 wasm_编码_内存操作 用 0x3B、对齐、偏移 }：
※ i64子字节存储
wasm_编码_i64存储8 接受 对齐、偏移) ：返还 做 wasm_编码_内存操作 用 0x3C、对齐、偏移 }：
wasm_编码_i64存储16 接受 对齐、偏移) ：返还 做 wasm_编码_内存操作 用 0x3D、对齐、偏移 }：
wasm_编码_i64存储32 接受 对齐、偏移) ：返还 做 wasm_编码_内存操作 用 0x3E、对齐、偏移 }：

※ WASM 全局读写编码

wasm_编码_全局读 接受 索引) { 定义 可变 结果：
 编码值 为 做 wasm_编码_uleb128 用 索引。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

wasm_编码_全局写 接受 索引) { 定义 可变 结果：
 编码值 为 做 wasm_编码_uleb128 用 索引。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

wasm_编码_本地暂存 接受 索引) { 定义 可变 结果：
 编码值 为 做 wasm_编码_uleb128 用 索引。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

※ WASM 控制流编码

wasm_编码_块 接受 块类型) ：返还 [0x02、块类型]：
完。

wasm_编码_循环 接受 块类型) ：返还 [0x03、块类型]：
完。

wasm_编码_如果 接受 块类型) ：返还 [0x04、块类型]：
完。

wasm_编码_否则 接受 ) ：返还 [0x05] }：
wasm_编码_结束 接受 ) ：返还 [0x0B] }：
wasm_编码_返回 接受 ) ：返还 [0x0F] }：
wasm_编码_不可达 接受 ) ：返还 [0x00] }：
wasm_编码_空操作 接受 ) ：返还 [0x01] }：
wasm_编码_丢弃 接受 ) ：返还 [0x1A] }：
wasm_编码_选择 接受 ) ：返还 [0x1B] }：

wasm_编码_跳转 接受 深度) { 定义 可变 结果：
 编码值 为 做 wasm_编码_uleb128 用 深度。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

wasm_编码_条件跳 接受 深度) { 定义 可变 结果：
 编码值 为 做 wasm_编码_uleb128 用 深度。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

※ br_table: 目标列表 + 默认目标
wasm_编码_跳转表 接受 目标列表、默认目标) { 定义 可变 结果：
 数量enc 为 做 wasm_编码_uleb128 用 做 计数 用 目标列表。
 可变 i 为 0。
 只要 i 小于 做 计数 用 数量enc：
 i 为 0。
 只要 i 小于 做 计数 用 目标列表：
 可变 j 为 0。
 只要 j 小于 做 计数 用 t：
 i 为 i 加 1。
 完。
 默认enc 为 做 wasm_编码_uleb128 用 默认目标。
 i 为 0。
 只要 i 小于 做 计数 用 默认enc：
 返还 结果。
完。

※ call_indirect: 类型索引 + 表索引(通常0)
wasm_编码_间接调用 接受 类型索引、表索引) { 定义 可变 结果：
 ty 为 做 wasm_编码_uleb128 用 类型索引。
 可变 i 为 0。
 只要 i 小于 做 计数 用 ty：
 tb 为 做 wasm_编码_uleb128 用 表索引。
 i 为 0。
 只要 i 小于 做 计数 用 tb：
 返还 结果。
完。

※ WASM 内存大小/增长

wasm_编码_内存大小 接受 ) ：返还 [0x3F、0x00] }：
wasm_编码_内存增长 接受 ) ：返还 [0x40、0x00] }：

※ WASM 0xFC前缀指令

wasm_编码_fc指令 接受 子操作码) { 定义 可变 结果：
 编码值 为 做 wasm_编码_uleb128 用 子操作码。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

※ memory.copy: 0xFC 10 0x00 0x00
wasm_编码_内存复制 接受 ) ：返还 [0xFC、10、0x00、0x00] }：
※ memory.fill: 0xFC 11 0x00
wasm_编码_内存填充 接受 ) ：返还 [0xFC、11、0x00] }：

※ 非陷阱截断转换
wasm_编码_i32截断饱和_f32s 接受 ) ：返还 做 wasm_编码_fc指令 用 0 }：
wasm_编码_i32截断饱和_f32u 接受 ) ：返还 做 wasm_编码_fc指令 用 1 }：
wasm_编码_i32截断饱和_f64s 接受 ) ：返还 做 wasm_编码_fc指令 用 2 }：
wasm_编码_i32截断饱和_f64u 接受 ) ：返还 做 wasm_编码_fc指令 用 3 }：
wasm_编码_i64截断饱和_f32s 接受 ) ：返还 做 wasm_编码_fc指令 用 4 }：
wasm_编码_i64截断饱和_f32u 接受 ) ：返还 做 wasm_编码_fc指令 用 5 }：
wasm_编码_i64截断饱和_f64s 接受 ) ：返还 做 wasm_编码_fc指令 用 6 }：
wasm_编码_i64截断饱和_f64u 接受 ) ：返还 做 wasm_编码_fc指令 用 7 }：

※ WASM 单字节指令编码(算术/比较/转换)

wasm_编码_单字节 接受 操作码) ：返还 [操作码] }：

※ i32 算术
wasm_编码_i32加 接受 ) ：返还 做 wasm_编码_单字节 用 0x6A }：
wasm_编码_i32减 接受 ) ：返还 做 wasm_编码_单字节 用 0x6B }：
wasm_编码_i32乘 接受 ) ：返还 做 wasm_编码_单字节 用 0x6C }：
wasm_编码_i32除s 接受 ) ：返还 做 wasm_编码_单字节 用 0x6D }：
wasm_编码_i32除u 接受 ) ：返还 做 wasm_编码_单字节 用 0x6E }：
wasm_编码_i32余s 接受 ) ：返还 做 wasm_编码_单字节 用 0x6F }：
wasm_编码_i32余u 接受 ) ：返还 做 wasm_编码_单字节 用 0x70 }：
wasm_编码_i32与 接受 ) ：返还 做 wasm_编码_单字节 用 0x71 }：
wasm_编码_i32或 接受 ) ：返还 做 wasm_编码_单字节 用 0x72 }：
wasm_编码_i32异或 接受 ) ：返还 做 wasm_编码_单字节 用 0x73 }：
wasm_编码_i32左移 接受 ) ：返还 做 wasm_编码_单字节 用 0x74 }：
wasm_编码_i32右移s 接受 )：返还 做 wasm_编码_单字节 用 0x75 }：
wasm_编码_i32右移u 接受 )：返还 做 wasm_编码_单字节 用 0x76 }：
wasm_编码_i32左旋 接受 ) ：返还 做 wasm_编码_单字节 用 0x77 }：
wasm_编码_i32右旋 接受 ) ：返还 做 wasm_编码_单字节 用 0x78 }：
wasm_编码_i32前导零 接受 )：返还 做 wasm_编码_单字节 用 0x67 }：
wasm_编码_i32尾零 接受 ) ：返还 做 wasm_编码_单字节 用 0x68 }：
wasm_编码_i32弹数 接受 ) ：返还 做 wasm_编码_单字节 用 0x69 }：

※ i32 比较
wasm_编码_i32为零 接受 ) ：返还 做 wasm_编码_单字节 用 0x45 }：
wasm_编码_i32等 接受 ) ：返还 做 wasm_编码_单字节 用 0x46 }：
wasm_编码_i32不等 接受 ) ：返还 做 wasm_编码_单字节 用 0x47 }：
wasm_编码_i32小于s 接受 )：返还 做 wasm_编码_单字节 用 0x48 }：
wasm_编码_i32小于u 接受 )：返还 做 wasm_编码_单字节 用 0x49 }：
wasm_编码_i32大于s 接受 )：返还 做 wasm_编码_单字节 用 0x4A }：
wasm_编码_i32大于u 接受 )：返还 做 wasm_编码_单字节 用 0x4B }：
wasm_编码_i32小等s 接受 )：返还 做 wasm_编码_单字节 用 0x4C }：
wasm_编码_i32小等u 接受 )：返还 做 wasm_编码_单字节 用 0x4D }：
wasm_编码_i32大等s 接受 )：返还 做 wasm_编码_单字节 用 0x4E }：
wasm_编码_i32大等u 接受 )：返还 做 wasm_编码_单字节 用 0x4F }：

※ i64 算术
wasm_编码_i64加 接受 ) ：返还 做 wasm_编码_单字节 用 0x7C }：
wasm_编码_i64减 接受 ) ：返还 做 wasm_编码_单字节 用 0x7D }：
wasm_编码_i64乘 接受 ) ：返还 做 wasm_编码_单字节 用 0x7E }：
wasm_编码_i64除s 接受 ) ：返还 做 wasm_编码_单字节 用 0x7F }：
wasm_编码_i64除u 接受 ) ：返还 做 wasm_编码_单字节 用 0x80 }：
wasm_编码_i64余s 接受 ) ：返还 做 wasm_编码_单字节 用 0x81 }：
wasm_编码_i64余u 接受 ) ：返还 做 wasm_编码_单字节 用 0x82 }：
wasm_编码_i64与 接受 ) ：返还 做 wasm_编码_单字节 用 0x83 }：
wasm_编码_i64或 接受 ) ：返还 做 wasm_编码_单字节 用 0x84 }：
wasm_编码_i64异或 接受 ) ：返还 做 wasm_编码_单字节 用 0x85 }：
wasm_编码_i64左移 接受 ) ：返还 做 wasm_编码_单字节 用 0x86 }：
wasm_编码_i64右移s 接受 )：返还 做 wasm_编码_单字节 用 0x87 }：
wasm_编码_i64右移u 接受 )：返还 做 wasm_编码_单字节 用 0x88 }：
wasm_编码_i64左旋 接受 ) ：返还 做 wasm_编码_单字节 用 0x89 }：
wasm_编码_i64右旋 接受 ) ：返还 做 wasm_编码_单字节 用 0x8A }：
wasm_编码_i64前导零 接受 )：返还 做 wasm_编码_单字节 用 0x79 }：
wasm_编码_i64尾零 接受 ) ：返还 做 wasm_编码_单字节 用 0x7A }：
wasm_编码_i64弹数 接受 ) ：返还 做 wasm_编码_单字节 用 0x7B }：

※ i64 比较
wasm_编码_i64为零 接受 ) ：返还 做 wasm_编码_单字节 用 0x50 }：
wasm_编码_i64等 接受 ) ：返还 做 wasm_编码_单字节 用 0x51 }：
wasm_编码_i64不等 接受 ) ：返还 做 wasm_编码_单字节 用 0x52 }：
wasm_编码_i64小于s 接受 )：返还 做 wasm_编码_单字节 用 0x53 }：
wasm_编码_i64小于u 接受 )：返还 做 wasm_编码_单字节 用 0x54 }：
wasm_编码_i64大于s 接受 )：返还 做 wasm_编码_单字节 用 0x55 }：
wasm_编码_i64大于u 接受 )：返还 做 wasm_编码_单字节 用 0x56 }：
wasm_编码_i64小等s 接受 )：返还 做 wasm_编码_单字节 用 0x57 }：
wasm_编码_i64小等u 接受 )：返还 做 wasm_编码_单字节 用 0x58 }：
wasm_编码_i64大等s 接受 )：返还 做 wasm_编码_单字节 用 0x59 }：
wasm_编码_i64大等u 接受 )：返还 做 wasm_编码_单字节 用 0x5A }：

※ f32 算术
wasm_编码_f32绝对值 接受 )：返还 做 wasm_编码_单字节 用 0x8B }：
wasm_编码_f32取反 接受 ) ：返还 做 wasm_编码_单字节 用 0x8C }：
wasm_编码_f32向上 接受 ) ：返还 做 wasm_编码_单字节 用 0x8D }：
wasm_编码_f32向下 接受 ) ：返还 做 wasm_编码_单字节 用 0x8E }：
wasm_编码_f32截断 接受 ) ：返还 做 wasm_编码_单字节 用 0x8F }：
wasm_编码_f32四舍 接受 ) ：返还 做 wasm_编码_单字节 用 0x90 }：
wasm_编码_f32平方根 接受 )：返还 做 wasm_编码_单字节 用 0x91 }：
wasm_编码_f32加 接受 ) ：返还 做 wasm_编码_单字节 用 0x92 }：
wasm_编码_f32减 接受 ) ：返还 做 wasm_编码_单字节 用 0x93 }：
wasm_编码_f32乘 接受 ) ：返还 做 wasm_编码_单字节 用 0x94 }：
wasm_编码_f32除 接受 ) ：返还 做 wasm_编码_单字节 用 0x95 }：
wasm_编码_f32最小 接受 ) ：返还 做 wasm_编码_单字节 用 0x96 }：
wasm_编码_f32最大 接受 ) ：返还 做 wasm_编码_单字节 用 0x97 }：
wasm_编码_f32符号复制 接受 )：返还 做 wasm_编码_单字节 用 0x98 }：

※ f32 比较
wasm_编码_f32等 接受 ) ：返还 做 wasm_编码_单字节 用 0x5B }：
wasm_编码_f32不等 接受 ) ：返还 做 wasm_编码_单字节 用 0x5C }：
wasm_编码_f32小于 接受 ) ：返还 做 wasm_编码_单字节 用 0x5D }：
wasm_编码_f32大于 接受 ) ：返还 做 wasm_编码_单字节 用 0x5E }：
wasm_编码_f32小等 接受 ) ：返还 做 wasm_编码_单字节 用 0x5F }：
wasm_编码_f32大等 接受 ) ：返还 做 wasm_编码_单字节 用 0x60 }：

※ f64 算术
wasm_编码_f64绝对值 接受 )：返还 做 wasm_编码_单字节 用 0x99 }：
wasm_编码_f64取反 接受 ) ：返还 做 wasm_编码_单字节 用 0x9A }：
wasm_编码_f64向上 接受 ) ：返还 做 wasm_编码_单字节 用 0x9B }：
wasm_编码_f64向下 接受 ) ：返还 做 wasm_编码_单字节 用 0x9C }：
wasm_编码_f64截断 接受 ) ：返还 做 wasm_编码_单字节 用 0x9D }：
wasm_编码_f64四舍 接受 ) ：返还 做 wasm_编码_单字节 用 0x9E }：
wasm_编码_f64平方根 接受 )：返还 做 wasm_编码_单字节 用 0x9F }：
wasm_编码_f64加 接受 ) ：返还 做 wasm_编码_单字节 用 0xA0 }：
wasm_编码_f64减 接受 ) ：返还 做 wasm_编码_单字节 用 0xA1 }：
wasm_编码_f64乘 接受 ) ：返还 做 wasm_编码_单字节 用 0xA2 }：
wasm_编码_f64除 接受 ) ：返还 做 wasm_编码_单字节 用 0xA3 }：
wasm_编码_f64最小 接受 ) ：返还 做 wasm_编码_单字节 用 0xA4 }：
wasm_编码_f64最大 接受 ) ：返还 做 wasm_编码_单字节 用 0xA5 }：
wasm_编码_f64符号复制 接受 )：返还 做 wasm_编码_单字节 用 0xA6 }：

※ f64 比较
wasm_编码_f64等 接受 ) ：返还 做 wasm_编码_单字节 用 0x61 }：
wasm_编码_f64不等 接受 ) ：返还 做 wasm_编码_单字节 用 0x62 }：
wasm_编码_f64小于 接受 ) ：返还 做 wasm_编码_单字节 用 0x63 }：
wasm_编码_f64大于 接受 ) ：返还 做 wasm_编码_单字节 用 0x64 }：
wasm_编码_f64小等 接受 ) ：返还 做 wasm_编码_单字节 用 0x65 }：
wasm_编码_f64大等 接受 ) ：返还 做 wasm_编码_单字节 用 0x66 }：

※ 类型转换
wasm_编码_i32截i64 接受 ) ：返还 做 wasm_编码_单字节 用 0xA7 }：
wasm_编码_i64扩i32s 接受 ) ：返还 做 wasm_编码_单字节 用 0xAC }：
wasm_编码_i64扩i32u 接受 ) ：返还 做 wasm_编码_单字节 用 0xAD }：
wasm_编码_i32截f32s 接受 ) ：返还 做 wasm_编码_单字节 用 0xA8 }：
wasm_编码_i32截f32u 接受 ) ：返还 做 wasm_编码_单字节 用 0xA9 }：
wasm_编码_i32截f64s 接受 ) ：返还 做 wasm_编码_单字节 用 0xAA }：
wasm_编码_i32截f64u 接受 ) ：返还 做 wasm_编码_单字节 用 0xAB }：
wasm_编码_i64截f32s 接受 ) ：返还 做 wasm_编码_单字节 用 0xAE }：
wasm_编码_i64截f32u 接受 ) ：返还 做 wasm_编码_单字节 用 0xAF }：
wasm_编码_i64截f64s 接受 ) ：返还 做 wasm_编码_单字节 用 0xB0 }：
wasm_编码_i64截f64u 接受 ) ：返还 做 wasm_编码_单字节 用 0xB1 }：
wasm_编码_f32转i32s 接受 ) ：返还 做 wasm_编码_单字节 用 0xB2 }：
wasm_编码_f32转i32u 接受 ) ：返还 做 wasm_编码_单字节 用 0xB3 }：
wasm_编码_f32转i64s 接受 ) ：返还 做 wasm_编码_单字节 用 0xB4 }：
wasm_编码_f32转i64u 接受 ) ：返还 做 wasm_编码_单字节 用 0xB5 }：
wasm_编码_f32降f64 接受 ) ：返还 做 wasm_编码_单字节 用 0xB6 }：
wasm_编码_f64转i32s 接受 ) ：返还 做 wasm_编码_单字节 用 0xB7 }：
wasm_编码_f64转i32u 接受 ) ：返还 做 wasm_编码_单字节 用 0xB8 }：
wasm_编码_f64转i64s 接受 ) ：返还 做 wasm_编码_单字节 用 0xB9 }：
wasm_编码_f64转i64u 接受 ) ：返还 做 wasm_编码_单字节 用 0xBA }：
wasm_编码_f64升f32 接受 ) ：返还 做 wasm_编码_单字节 用 0xBB }：
wasm_编码_i32重释f32 接受 )：返还 做 wasm_编码_单字节 用 0xBC }：
wasm_编码_i64重释f64 接受 )：返还 做 wasm_编码_单字节 用 0xBD }：
wasm_编码_f32重释i32 接受 )：返还 做 wasm_编码_单字节 用 0xBE }：
wasm_编码_f64重释i64 接受 )：返还 做 wasm_编码_单字节 用 0xBF }：

※ sign-extension
wasm_编码_i32扩8s 接受 ) ：返还 做 wasm_编码_单字节 用 0xC0 }：
wasm_编码_i32扩16s 接受 ) ：返还 做 wasm_编码_单字节 用 0xC1 }：
wasm_编码_i64扩8s 接受 ) ：返还 做 wasm_编码_单字节 用 0xC2 }：
wasm_编码_i64扩16s 接受 ) ：返还 做 wasm_编码_单字节 用 0xC3 }：
wasm_编码_i64扩32s 接受 ) ：返还 做 wasm_编码_单字节 用 0xC4 }：

※ WASM 数据段编码

※ 主动数据段: 0x00 + offset_expr + 数据字节
wasm_编码_数据段_主动 接受 偏移值、数据) { 定义 可变 结果：
 ※ offset表达式: i32.const + 偏移 + end
 做 追加 用 结果、0x41
 off_enc 为 做 wasm_编码_sleb128 用 偏移值。
 可变 i 为 0。
 只要 i 小于 做 计数 用 off_enc：
 做 追加 用 结果、0x0B
 ※ 数据长度 + 数据
 len_enc 为 做 wasm_编码_uleb128 用 做 计数 用 数据。
 i 为 0。
 只要 i 小于 做 计数 用 len_enc：
 i 为 0。
 只要 i 小于 做 计数 用 数据：
 返还 结果。
完。

※ 被动数据段: 0x01 + 数据
wasm_编码_数据段_被动 接受 数据) { 定义 可变 结果：
 len_enc 为 做 wasm_编码_uleb128 用 做 计数 用 数据。
 可变 i 为 0。
 只要 i 小于 做 计数 用 len_enc：
 i 为 0。
 只要 i 小于 做 计数 用 数据：
 返还 结果。
完。

※ WASM 导入段编码

wasm_编码_导入_函数 接受 模块名字节、名称字节、类型索引) { 定义 可变 结果：
 ※ 模块名
 mlen 为 做 wasm_编码_uleb128 用 做 计数 用 模块名字节。
 可变 i 为 0。
 只要 i 小于 做 计数 用 mlen：
 i 为 0。
 只要 i 小于 做 计数 用 模块名字节：
 ※ 名称
 名长 为 做 wasm_编码_uleb128 用 做 计数 用 名称字节。
 i 为 0。
 只要 i 小于 做 计数 用 名长：
 i 为 0。
 只要 i 小于 做 计数 用 名称字节：
 ※ 导入类型: 0x00=函数
 做 追加 用 结果、0x00。
 tidx 为 做 wasm_编码_uleb128 用 类型索引。
 i 为 0。
 只要 i 小于 做 计数 用 tidx：
 返还 结果。
完。

wasm_编码_导入_内存 接受 模块名字节、名称字节、最小页、最大页) { 定义 可变 结果：
 ※ 模块名
 mlen 为 做 wasm_编码_uleb128 用 做 计数 用 模块名字节。
 可变 i 为 0。
 只要 i 小于 做 计数 用 mlen：
 i 为 0。
 只要 i 小于 做 计数 用 模块名字节：
 ※ 名称
 名长 为 做 wasm_编码_uleb128 用 做 计数 用 名称字节。
 i 为 0。
 只要 i 小于 做 计数 用 名长：
 i 为 0。
 只要 i 小于 做 计数 用 名称字节：
 ※ 导入类型: 0x02=内存
 做 追加 用 结果、0x02。
 若 最大页 大于 0：
 做 追加 用 结果、0x01
 mn 为 做 wasm_编码_uleb128 用 最小页。
 i 为 0。
 只要 i 小于 做 计数 用 mn：
 mx 为 做 wasm_编码_uleb128 用 最大页。
 i 为 0。
 只要 i 小于 做 计数 用 mx：
 否则：
 做 追加 用 结果、0x00
 mn 为 做 wasm_编码_uleb128 用 最小页。
 i 为 0。
 只要 i 小于 做 计数 用 mn：
 完。
 返还 结果。
完。

※ WASM 全局段编码

wasm_编码_全局声明 接受 值类型、可变标志、初始值) { 定义 可变 结果、可变标志]：
 ※ 初始值表达式
 若 值类型 等于 类型_i32：
 做 追加 用 结果、0x41。
 编码值 为 做 wasm_编码_sleb128 用 初始值。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 否则：
 做 追加 用 结果、0x42。
 编码值 为 做 wasm_编码_sleb128 用 初始值。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 完。
 完。
 做 追加 用 结果、0x0B
 返还 结果。
完。

※ WASM 有上限内存段编码

wasm_编码_内存_有上限 接受 最小页、最大页) { 定义 可变 结果：
 最短编码 为 做 wasm_编码_uleb128 用 最小页。
 可变 i 为 0。
 只要 i 小于 做 计数 用 最短编码：
 max_enc 为 做 wasm_编码_uleb128 用 最大页。
 i 为 0。
 只要 i 小于 做 计数 用 max_enc：
 返还 结果。
完。

※ WASM 表段编码

wasm_编码_表 接受 引用类型、最小、最大) { 定义 可变 结果：
 若 最大 大于 0：
 做 追加 用 结果、0x01。
 mn 为 做 wasm_编码_uleb128 用 最小。
 可变 i 为 0。
 只要 i 小于 做 计数 用 mn：
 mx 为 做 wasm_编码_uleb128 用 最大。
 i 为 0。
 只要 i 小于 做 计数 用 mx：
 否则：
 做 追加 用 结果、0x00。
 mn 为 做 wasm_编码_uleb128 用 最小。
 可变 i 为 0。
 只要 i 小于 做 计数 用 mn：
 完。
 返还 结果。
完。

※ WASM 起始段编码

wasm_编码_起始段 接受 函数索引) ：返还 wasm_编码_uleb128(函数索引：
完。

※ WASM 字符串→UTF8字节 辅助

wasm_字符串转字节 接受 文本) { 定义 可变 结果：
 可变 i 为 0。
 只要 i 小于 做 长度 用 文本：
 做 追加 用 结果、做 字节 用 文本、i。
 i 为 i 加 1。
 完。
 返还 结果。
完。

※ WASM 完整模块组装

wasm_组装模块 接受 类型段、导入段、函数段、表段、内存段、全局段、导出段、起始段、元素段、代码段、数据段) { 定义 可变 结果：
 ※ 按段ID顺序依次追加非空段
 若 做 计数 用 类型段 大于 0：
     s 为 做 封装段 用 段_TYPE、类型段。
     可变 i 为 0。
     只要 i 小于 做 计数 用 s：
 完。
 若 做 计数 用 导入段 大于 0：
     s 为 做 封装段 用 段_IMPORT、导入段。
     可变 i 为 0。
     只要 i 小于 做 计数 用 s：
 完。
 若 做 计数 用 函数段 大于 0：
     s 为 做 封装段 用 段_FUNC、函数段。
     可变 i 为 0。
     只要 i 小于 做 计数 用 s：
 完。
 若 做 计数 用 表段 大于 0：
     s 为 做 封装段 用 段_TABLE、表段。
     可变 i 为 0。
     只要 i 小于 做 计数 用 s：
 完。
 若 做 计数 用 内存段 大于 0：
     s 为 做 封装段 用 段_MEMORY、内存段。
     可变 i 为 0。
     只要 i 小于 做 计数 用 s：
 完。
 若 做 计数 用 全局段 大于 0：
     s 为 做 封装段 用 段_GLOBAL、全局段。
     可变 i 为 0。
     只要 i 小于 做 计数 用 s：
 完。
 若 做 计数 用 导出段 大于 0：
     s 为 做 封装段 用 段_EXPORT、导出段。
     可变 i 为 0。
     只要 i 小于 做 计数 用 s：
 完。
 若 做 计数 用 起始段 大于 0：
     s 为 做 封装段 用 段_START、起始段。
     可变 i 为 0。
     只要 i 小于 做 计数 用 s：
 完。
 若 做 计数 用 元素段 大于 0：
     s 为 做 封装段 用 段_ELEM、元素段。
     可变 i 为 0。
     只要 i 小于 做 计数 用 s：
 完。
 若 做 计数 用 代码段 大于 0：
     s 为 做 封装段 用 段_CODE、代码段。
     可变 i 为 0。
     只要 i 小于 做 计数 用 s：
 完。
 若 做 计数 用 数据段 大于 0：
     s 为 做 封装段 用 WASM小写_段_DATA、数据段。
     可变 i 为 0。
     只要 i 小于 做 计数 用 s：
 完。
 返还 结果。
完。

可变 _wasm_编码次数 为 0。
可变 _wasm_总字节 为 0。
可变 _wasm_函数数 为 0。

wasm_记录编码 接受 字节数) { _wasm_编码次数：
 _wasm_总字节 为 _wasm_总字节 加 字节数。
完。

wasm_记录函数 接受 ) { _wasm_函数数：

wasm_密度评估 接受 )：
    若 _wasm_编码次数 等于 0：
完。
 平均 为 _wasm_总字节 乘 10 除以 _wasm_编码次数。
 若 平均 大于 50：
 返还 「WASM编码紧凑」。
完。

wasm_重置 接受 ) { _wasm_编码次数：

※ WASM函数体编码

wasm_编码_函数体 接受 本地变量、指令列表) { 定义 可变 体：
 ※ 本地变量声明
 本地数enc 为 做 编码_uleb128 用 做 计数 用 本地变量。
 可变 i 为 0。
 只要 i 小于 做 计数 用 本地数enc：
 i 为 0。
 只要 i 小于 做 计数 用 本地变量：
 可变 j 为 0。
 只要 j 小于 做 计数 用 cnt：
 做 追加 用 体、本地变量[i]。
 i 为 i 加 1。
 完。
 ※ 指令
 i 为 0。
 只要 i 小于 做 计数 用 指令列表：
 做 追加 用 体、WASM_结束。
 ※ 长度前缀
 长度enc 为 做 编码_uleb128 用 做 计数 用 体。
 可变 结果 为 []。
 i 为 0。
 只要 i 小于 做 计数 用 长度enc：
 i 为 0。
 只要 i 小于 做 计数 用 体：
 返还 结果。
完。

※ WASM SIMD v128 指令 (128位向量, 操作码前缀0xFD)

※ SIMD辅助: 发射SIMD操作码(前缀 + LEB128操作码)
wasm_SIMD指令集 接受 操作码) { 定义 结果：
 编码值 为 做 wasm_编码_uleb128 用 操作码。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

※ v128.load — 加载128位向量
wasm_向量128_加载 接受 对齐、偏移) { 定义 r：
※ v128.store
wasm_向量128_存储 接受 对齐、偏移) { 定义 r：
※ v128.const — 128位常量
wasm_向量128_常量 接受 字节16) { 定义 r：

※ i8x16 操作
wasm_整8x16_广播 接受 ) ：返还 做 wasm_SIMD指令集 用 15 }：
wasm_整8x16_提取通道有符号 接受 l) { 定义 r、l) 返回 r }：
wasm_整8x16_提取通道无符号 接受 l) { 定义 r、l) 返回 r }：
wasm_整8x16_替换通道 接受 l) { 定义 r、l) 返回 r }：
wasm_整8x16_等于 接受 ) ：返还 做 wasm_SIMD指令集 用 35 }：
wasm_整8x16_不等 接受 ) ：返还 做 wasm_SIMD指令集 用 36 }：
wasm_整8x16_有符号小于 接受 ) ：返还 做 wasm_SIMD指令集 用 37 }：
wasm_整8x16_无符号小于 接受 ) ：返还 做 wasm_SIMD指令集 用 38 }：
wasm_整8x16_有符号大于 接受 ) ：返还 做 wasm_SIMD指令集 用 39 }：
wasm_整8x16_无符号大于 接受 ) ：返还 做 wasm_SIMD指令集 用 40 }：
wasm_整8x16_有符号小于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 41 }：
wasm_整8x16_无符号小于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 42 }：
wasm_整8x16_有符号大于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 43 }：
wasm_整8x16_无符号大于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 44 }：
wasm_整8x16_取负 接受 ) ：返还 做 wasm_SIMD指令集 用 97 }：
wasm_整8x16_绝对值 接受 ) ：返还 做 wasm_SIMD指令集 用 96 }：
wasm_整8x16_加 接受 ) ：返还 做 wasm_SIMD指令集 用 110 }：
wasm_整8x16_减 接受 ) ：返还 做 wasm_SIMD指令集 用 113 }：
wasm_整8x16_add_sat_s 接受 ) ：返还 做 wasm_SIMD指令集 用 111 }：
wasm_整8x16_add_sat_u 接受 ) ：返还 做 wasm_SIMD指令集 用 112 }：
wasm_整8x16_sub_sat_s 接受 ) ：返还 做 wasm_SIMD指令集 用 114 }：
wasm_整8x16_sub_sat_u 接受 ) ：返还 做 wasm_SIMD指令集 用 115 }：
wasm_整8x16_min_s 接受 ) ：返还 做 wasm_SIMD指令集 用 118 }：
wasm_整8x16_min_u 接受 ) ：返还 做 wasm_SIMD指令集 用 119 }：
wasm_整8x16_max_s 接受 ) ：返还 做 wasm_SIMD指令集 用 120 }：
wasm_整8x16_max_u 接受 ) ：返还 做 wasm_SIMD指令集 用 121 }：
wasm_整8x16_无符号平均 接受 ) ：返还 做 wasm_SIMD指令集 用 123 }：
wasm_整8x16_位计数 接受 ) ：返还 做 wasm_SIMD指令集 用 98 }：
wasm_整8x16_全真 接受 ) ：返还 做 wasm_SIMD指令集 用 99 }：
wasm_整8x16_位掩码 接受 ) ：返还 做 wasm_SIMD指令集 用 100 }：
wasm_整8x16_窄化i16x8有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 101 }：
wasm_整8x16_窄化i16x8无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 102 }：
wasm_整8x16_混洗 接受 索引16) { 定义 r：
wasm_整8x16_重排 接受 ) ：返还 做 wasm_SIMD指令集 用 14 }：

※ i16x8 操作
wasm_整16x8_广播 接受 ) ：返还 做 wasm_SIMD指令集 用 16 }：
wasm_整16x8_提取通道有符号 接受 l) { 定义 r、l) 返回 r }：
wasm_整16x8_提取通道无符号 接受 l) { 定义 r、l) 返回 r }：
wasm_整16x8_替换通道 接受 l) { 定义 r、l) 返回 r }：
wasm_整16x8_等于 接受 ) ：返还 做 wasm_SIMD指令集 用 45 }：
wasm_整16x8_不等 接受 ) ：返还 做 wasm_SIMD指令集 用 46 }：
wasm_整16x8_有符号小于 接受 ) ：返还 做 wasm_SIMD指令集 用 47 }：
wasm_整16x8_无符号小于 接受 ) ：返还 做 wasm_SIMD指令集 用 48 }：
wasm_整16x8_有符号大于 接受 ) ：返还 做 wasm_SIMD指令集 用 49 }：
wasm_整16x8_无符号大于 接受 ) ：返还 做 wasm_SIMD指令集 用 50 }：
wasm_整16x8_有符号小于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 51 }：
wasm_整16x8_无符号小于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 52 }：
wasm_整16x8_有符号大于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 53 }：
wasm_整16x8_无符号大于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 54 }：
wasm_整16x8_取负 接受 ) ：返还 做 wasm_SIMD指令集 用 129 }：
wasm_整16x8_绝对值 接受 ) ：返还 做 wasm_SIMD指令集 用 128 }：
wasm_整16x8_加 接受 ) ：返还 做 wasm_SIMD指令集 用 142 }：
wasm_整16x8_减 接受 ) ：返还 做 wasm_SIMD指令集 用 145 }：
wasm_整16x8_乘 接受 ) ：返还 做 wasm_SIMD指令集 用 149 }：
wasm_整16x8_add_sat_s 接受 ) ：返还 做 wasm_SIMD指令集 用 143 }：
wasm_整16x8_add_sat_u 接受 ) ：返还 做 wasm_SIMD指令集 用 144 }：
wasm_整16x8_sub_sat_s 接受 ) ：返还 做 wasm_SIMD指令集 用 146 }：
wasm_整16x8_sub_sat_u 接受 ) ：返还 做 wasm_SIMD指令集 用 147 }：
wasm_整16x8_min_s 接受 ) ：返还 做 wasm_SIMD指令集 用 150 }：
wasm_整16x8_min_u 接受 ) ：返还 做 wasm_SIMD指令集 用 151 }：
wasm_整16x8_max_s 接受 ) ：返还 做 wasm_SIMD指令集 用 152 }：
wasm_整16x8_max_u 接受 ) ：返还 做 wasm_SIMD指令集 用 153 }：
wasm_整16x8_无符号平均 接受 ) ：返还 做 wasm_SIMD指令集 用 155 }：
wasm_整16x8_全真 接受 ) ：返还 做 wasm_SIMD指令集 用 131 }：
wasm_整16x8_位掩码 接受 ) ：返还 做 wasm_SIMD指令集 用 132 }：
wasm_整16x8_左移 接受 ) ：返还 做 wasm_SIMD指令集 用 139 }：
wasm_整16x8_有符号右移 接受 ) ：返还 做 wasm_SIMD指令集 用 140 }：
wasm_整16x8_无符号右移 接受 ) ：返还 做 wasm_SIMD指令集 用 141 }：
wasm_整16x8_扩展乘低i8x16有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 156 }：
wasm_整16x8_扩展乘高i8x16有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 157 }：
wasm_整16x8_扩展乘低i8x16无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 158 }：
wasm_整16x8_扩展乘高i8x16无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 159 }：
wasm_整16x8_Q15饱和乘有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 148 }：
wasm_整16x8_扩展对加i8x16有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 124 }：
wasm_整16x8_扩展对加i8x16无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 125 }：

※ i32x4 操作
wasm_整32x4_广播 接受 ) ：返还 做 wasm_SIMD指令集 用 17 }：
wasm_整32x4_提取通道 接受 l) { 定义 r、l) 返回 r }：
wasm_整32x4_替换通道 接受 l) { 定义 r、l) 返回 r }：
wasm_整32x4_等于 接受 ) ：返还 做 wasm_SIMD指令集 用 55 }：
wasm_整32x4_不等 接受 ) ：返还 做 wasm_SIMD指令集 用 56 }：
wasm_整32x4_有符号小于 接受 ) ：返还 做 wasm_SIMD指令集 用 57 }：
wasm_整32x4_无符号小于 接受 ) ：返还 做 wasm_SIMD指令集 用 58 }：
wasm_整32x4_有符号大于 接受 ) ：返还 做 wasm_SIMD指令集 用 59 }：
wasm_整32x4_无符号大于 接受 ) ：返还 做 wasm_SIMD指令集 用 60 }：
wasm_整32x4_有符号小于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 61 }：
wasm_整32x4_无符号小于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 62 }：
wasm_整32x4_有符号大于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 63 }：
wasm_整32x4_无符号大于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 64 }：
wasm_整32x4_取负 接受 ) ：返还 做 wasm_SIMD指令集 用 161 }：
wasm_整32x4_绝对值 接受 ) ：返还 做 wasm_SIMD指令集 用 160 }：
wasm_整32x4_加 接受 ) ：返还 做 wasm_SIMD指令集 用 174 }：
wasm_整32x4_减 接受 ) ：返还 做 wasm_SIMD指令集 用 177 }：
wasm_整32x4_乘 接受 ) ：返还 做 wasm_SIMD指令集 用 181 }：
wasm_整32x4_min_s 接受 ) ：返还 做 wasm_SIMD指令集 用 182 }：
wasm_整32x4_min_u 接受 ) ：返还 做 wasm_SIMD指令集 用 183 }：
wasm_整32x4_max_s 接受 ) ：返还 做 wasm_SIMD指令集 用 184 }：
wasm_整32x4_max_u 接受 ) ：返还 做 wasm_SIMD指令集 用 185 }：
wasm_整32x4_全真 接受 ) ：返还 做 wasm_SIMD指令集 用 163 }：
wasm_整32x4_位掩码 接受 ) ：返还 做 wasm_SIMD指令集 用 164 }：
wasm_整32x4_左移 接受 ) ：返还 做 wasm_SIMD指令集 用 171 }：
wasm_整32x4_有符号右移 接受 ) ：返还 做 wasm_SIMD指令集 用 172 }：
wasm_整32x4_无符号右移 接受 ) ：返还 做 wasm_SIMD指令集 用 173 }：
wasm_整32x4_点积i16x8有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 186 }：
wasm_整32x4_扩展乘低i16x8有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 188 }：
wasm_整32x4_扩展乘高i16x8有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 189 }：
wasm_整32x4_扩展乘低i16x8无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 190 }：
wasm_整32x4_扩展乘高i16x8无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 191 }：
wasm_整32x4_扩展对加i16x8有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 126 }：
wasm_整32x4_扩展对加i16x8无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 127 }：
wasm_整32x4_截断饱和f32x4有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 248 }：
wasm_整32x4_截断饱和f32x4无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 249 }：
wasm_整32x4_trunc_sat_f64x2_s_zero 接受 ) ：返还 做 wasm_SIMD指令集 用 252 }：
wasm_整32x4_trunc_sat_f64x2_u_zero 接受 ) ：返还 做 wasm_SIMD指令集 用 253 }：

※ i64x2 操作
wasm_整64x2_广播 接受 ) ：返还 做 wasm_SIMD指令集 用 18 }：
wasm_整64x2_提取通道 接受 l) { 定义 r、l) 返回 r }：
wasm_整64x2_替换通道 接受 l) { 定义 r、l) 返回 r }：
wasm_整64x2_等于 接受 ) ：返还 做 wasm_SIMD指令集 用 214 }：
wasm_整64x2_不等 接受 ) ：返还 做 wasm_SIMD指令集 用 215 }：
wasm_整64x2_有符号小于 接受 ) ：返还 做 wasm_SIMD指令集 用 216 }：
wasm_整64x2_有符号大于 接受 ) ：返还 做 wasm_SIMD指令集 用 217 }：
wasm_整64x2_有符号小于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 218 }：
wasm_整64x2_有符号大于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 219 }：
wasm_整64x2_取负 接受 ) ：返还 做 wasm_SIMD指令集 用 193 }：
wasm_整64x2_绝对值 接受 ) ：返还 做 wasm_SIMD指令集 用 192 }：
wasm_整64x2_加 接受 ) ：返还 做 wasm_SIMD指令集 用 206 }：
wasm_整64x2_减 接受 ) ：返还 做 wasm_SIMD指令集 用 209 }：
wasm_整64x2_乘 接受 ) ：返还 做 wasm_SIMD指令集 用 213 }：
wasm_整64x2_全真 接受 ) ：返还 做 wasm_SIMD指令集 用 195 }：
wasm_整64x2_位掩码 接受 ) ：返还 做 wasm_SIMD指令集 用 196 }：
wasm_整64x2_左移 接受 ) ：返还 做 wasm_SIMD指令集 用 203 }：
wasm_整64x2_有符号右移 接受 ) ：返还 做 wasm_SIMD指令集 用 204 }：
wasm_整64x2_无符号右移 接受 ) ：返还 做 wasm_SIMD指令集 用 205 }：
wasm_整64x2_扩展乘低i32x4有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 220 }：
wasm_整64x2_扩展乘高i32x4有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 221 }：
wasm_整64x2_扩展乘低i32x4无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 222 }：
wasm_整64x2_扩展乘高i32x4无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 223 }：

※ f32x4 浮点操作
wasm_浮32x4_广播 接受 ) ：返还 做 wasm_SIMD指令集 用 19 }：
wasm_浮32x4_提取通道 接受 l) { 定义 r、l) 返回 r }：
wasm_浮32x4_替换通道 接受 l) { 定义 r、l) 返回 r }：
wasm_浮32x4_等于 接受 ) ：返还 做 wasm_SIMD指令集 用 65 }：
wasm_浮32x4_不等 接受 ) ：返还 做 wasm_SIMD指令集 用 66 }：
wasm_浮32x4_小于 接受 ) ：返还 做 wasm_SIMD指令集 用 67 }：
wasm_浮32x4_大于 接受 ) ：返还 做 wasm_SIMD指令集 用 68 }：
wasm_浮32x4_小于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 69 }：
wasm_浮32x4_大于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 70 }：
wasm_浮32x4_绝对值 接受 ) ：返还 做 wasm_SIMD指令集 用 224 }：
wasm_浮32x4_取负 接受 ) ：返还 做 wasm_SIMD指令集 用 225 }：
wasm_浮32x4_平方根 接受 ) ：返还 做 wasm_SIMD指令集 用 227 }：
wasm_浮32x4_加 接受 ) ：返还 做 wasm_SIMD指令集 用 228 }：
wasm_浮32x4_减 接受 ) ：返还 做 wasm_SIMD指令集 用 229 }：
wasm_浮32x4_乘 接受 ) ：返还 做 wasm_SIMD指令集 用 230 }：
wasm_浮32x4_除 接受 ) ：返还 做 wasm_SIMD指令集 用 231 }：
wasm_浮32x4_最小 接受 ) ：返还 做 wasm_SIMD指令集 用 232 }：
wasm_浮32x4_最大 接受 ) ：返还 做 wasm_SIMD指令集 用 233 }：
wasm_浮32x4_伪最小 接受 ) ：返还 做 wasm_SIMD指令集 用 234 }：
wasm_浮32x4_伪最大 接受 ) ：返还 做 wasm_SIMD指令集 用 235 }：
wasm_浮32x4_向上取整 接受 ) ：返还 做 wasm_SIMD指令集 用 103 }：
wasm_浮32x4_向下取整 接受 ) ：返还 做 wasm_SIMD指令集 用 104 }：
wasm_浮32x4_截断 接受 ) ：返还 做 wasm_SIMD指令集 用 105 }：
wasm_浮32x4_最近取整 接受 ) ：返还 做 wasm_SIMD指令集 用 106 }：
wasm_浮32x4_convert_i32x4_s 接受 ) ：返还 做 wasm_SIMD指令集 用 250 }：
wasm_浮32x4_convert_i32x4_u 接受 ) ：返还 做 wasm_SIMD指令集 用 251 }：
wasm_浮32x4_降精度f64x2零扩展 接受 ) ：返还 做 wasm_SIMD指令集 用 94 }：

※ f64x2 浮点操作
wasm_浮64x2_广播 接受 ) ：返还 做 wasm_SIMD指令集 用 20 }：
wasm_浮64x2_提取通道 接受 l) { 定义 r、l) 返回 r }：
wasm_浮64x2_替换通道 接受 l) { 定义 r、l) 返回 r }：
wasm_浮64x2_等于 接受 ) ：返还 做 wasm_SIMD指令集 用 71 }：
wasm_浮64x2_不等 接受 ) ：返还 做 wasm_SIMD指令集 用 72 }：
wasm_浮64x2_小于 接受 ) ：返还 做 wasm_SIMD指令集 用 73 }：
wasm_浮64x2_大于 接受 ) ：返还 做 wasm_SIMD指令集 用 74 }：
wasm_浮64x2_小于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 75 }：
wasm_浮64x2_大于等于 接受 ) ：返还 做 wasm_SIMD指令集 用 76 }：
wasm_浮64x2_绝对值 接受 ) ：返还 做 wasm_SIMD指令集 用 236 }：
wasm_浮64x2_取负 接受 ) ：返还 做 wasm_SIMD指令集 用 237 }：
wasm_浮64x2_平方根 接受 ) ：返还 做 wasm_SIMD指令集 用 239 }：
wasm_浮64x2_加 接受 ) ：返还 做 wasm_SIMD指令集 用 240 }：
wasm_浮64x2_减 接受 ) ：返还 做 wasm_SIMD指令集 用 241 }：
wasm_浮64x2_乘 接受 ) ：返还 做 wasm_SIMD指令集 用 242 }：
wasm_浮64x2_除 接受 ) ：返还 做 wasm_SIMD指令集 用 243 }：
wasm_浮64x2_最小 接受 ) ：返还 做 wasm_SIMD指令集 用 244 }：
wasm_浮64x2_最大 接受 ) ：返还 做 wasm_SIMD指令集 用 245 }：
wasm_浮64x2_伪最小 接受 ) ：返还 做 wasm_SIMD指令集 用 246 }：
wasm_浮64x2_伪最大 接受 ) ：返还 做 wasm_SIMD指令集 用 247 }：
wasm_浮64x2_向上取整 接受 ) ：返还 做 wasm_SIMD指令集 用 116 }：
wasm_浮64x2_向下取整 接受 ) ：返还 做 wasm_SIMD指令集 用 117 }：
wasm_浮64x2_截断 接受 ) ：返还 做 wasm_SIMD指令集 用 122 }：
wasm_浮64x2_最近取整 接受 ) ：返还 做 wasm_SIMD指令集 用 148 }：
wasm_浮64x2_转换低i32x4有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 254 }：
wasm_浮64x2_转换低i32x4无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 255 }：
wasm_浮64x2_升精度低f32x4 接受 ) ：返还 做 wasm_SIMD指令集 用 95 }：

※ v128 逻辑/位操作
wasm_向量128_取反 接受 ) ：返还 做 wasm_SIMD指令集 用 77 }：
wasm_向量128_与 接受 ) ：返还 做 wasm_SIMD指令集 用 78 }：
wasm_向量128_与非 接受 ) ：返还 做 wasm_SIMD指令集 用 79 }：
wasm_向量128_或 接受 ) ：返还 做 wasm_SIMD指令集 用 80 }：
wasm_向量128_异或 接受 ) ：返还 做 wasm_SIMD指令集 用 81 }：
wasm_向量128_位选择 接受 ) ：返还 做 wasm_SIMD指令集 用 82 }：
wasm_向量128_任真 接受 ) ：返还 做 wasm_SIMD指令集 用 83 }：

※ v128 加载变体
wasm_向量128_加载8x8有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 1 }：
wasm_向量128_加载8x8无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 2 }：
wasm_向量128_加载16x4有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 3 }：
wasm_向量128_加载16x4无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 4 }：
wasm_向量128_加载32x2有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 5 }：
wasm_向量128_加载32x2无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 6 }：
wasm_向量128_加载8广播 接受 ) ：返还 做 wasm_SIMD指令集 用 7 }：
wasm_向量128_加载16广播 接受 ) ：返还 做 wasm_SIMD指令集 用 8 }：
wasm_向量128_加载32广播 接受 ) ：返还 做 wasm_SIMD指令集 用 9 }：
wasm_向量128_加载64广播 接受 ) ：返还 做 wasm_SIMD指令集 用 10 }：
wasm_向量128_加载32零 接受 ) ：返还 做 wasm_SIMD指令集 用 92 }：
wasm_向量128_加载64零 接受 ) ：返还 做 wasm_SIMD指令集 用 93 }：
wasm_向量128_加载8通道 接受 l) { 定义 r、l) 返回 r }：
wasm_向量128_加载16通道 接受 l) { 定义 r、l) 返回 r }：
wasm_向量128_加载32通道 接受 l) { 定义 r、l) 返回 r }：
wasm_向量128_加载64通道 接受 l) { 定义 r、l) 返回 r }：
wasm_向量128_存储8通道 接受 l) { 定义 r、l) 返回 r }：
wasm_向量128_存储16通道 接受 l) { 定义 r、l) 返回 r }：
wasm_向量128_存储32通道 接受 l) { 定义 r、l) 返回 r }：
wasm_向量128_存储64通道 接受 l) { 定义 r、l) 返回 r }：

※ WASM 线程与原子操作 (前缀 0xFE)
操作码_prefix_atomic 为 0xFE。

wasm_原子操作 接受 操作码) { 定义 结果：
 编码值 为 做 wasm_编码_uleb128 用 操作码。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。

※ memory.atomic.notify — 唤醒等待线程
wasm_原子_通知 接受 ) ：返还 做 wasm_原子操作 用 0 }：
※ memory.atomic.wait32 — 等待32位值变化
wasm_原子_等待32 接受 ) ：返还 做 wasm_原子操作 用 1 }：
※ memory.atomic.wait64
wasm_原子_等待64 接受 ) ：返还 做 wasm_原子操作 用 2 }：
※ atomic.fence — 原子屏障
wasm_原子_栅栏 接受 ) ：返还 做 wasm_原子操作 用 3 }：
※ i32.atomic.load
wasm_整32_atomic_load 接受 ) ：返还 做 wasm_原子操作 用 16 }：
※ i64.atomic.load
wasm_整64_atomic_load 接受 ) ：返还 做 wasm_原子操作 用 17 }：
※ i32.atomic.load8_u / load16_u
wasm_整32_atomic_load8_u 接受 ) ：返还 做 wasm_原子操作 用 18 }：
wasm_整32_atomic_load16_u 接受 ) ：返还 做 wasm_原子操作 用 19 }：
※ i32.atomic.store
wasm_整32_atomic_store 接受 ) ：返还 做 wasm_原子操作 用 23 }：
※ i64.atomic.store
wasm_整64_atomic_store 接受 ) ：返还 做 wasm_原子操作 用 24 }：
※ i32.atomic.store8 / store16
wasm_整32_atomic_store8 接受 ) ：返还 做 wasm_原子操作 用 25 }：
wasm_整32_atomic_store16 接受 ) ：返还 做 wasm_原子操作 用 26 }：
※ i32.atomic.rmw.add — 原子读-修改-写加
wasm_整32_atomic_rmw_add 接受 ) ：返还 做 wasm_原子操作 用 30 }：
※ i64.atomic.rmw.add
wasm_整64_atomic_rmw_add 接受 ) ：返还 做 wasm_原子操作 用 31 }：
※ i32.atomic.rmw.sub
wasm_整32_atomic_rmw_sub 接受 ) ：返还 做 wasm_原子操作 用 37 }：
※ i32.atomic.rmw.and
wasm_整32_atomic_rmw_and 接受 ) ：返还 做 wasm_原子操作 用 44 }：
※ i32.atomic.rmw.or
wasm_整32_atomic_rmw_or 接受 ) ：返还 做 wasm_原子操作 用 51 }：
※ i32.atomic.rmw.xor
wasm_整32_atomic_rmw_xor 接受 ) ：返还 做 wasm_原子操作 用 58 }：
※ i32.atomic.rmw.xchg — 原子交换
wasm_整32_atomic_rmw_xchg 接受 ) ：返还 做 wasm_原子操作 用 65 }：
※ i32.atomic.rmw.cmpxchg — 原子比较并交换
wasm_整32_atomic_rmw_cmpxchg 接受 ) ：返还 做 wasm_原子操作 用 72 }：
※ i64 原子RMW
wasm_整64_atomic_rmw_sub 接受 ) ：返还 做 wasm_原子操作 用 38 }：
wasm_整64_atomic_rmw_and 接受 ) ：返还 做 wasm_原子操作 用 45 }：
wasm_整64_atomic_rmw_or 接受 ) ：返还 做 wasm_原子操作 用 52 }：
wasm_整64_atomic_rmw_xor 接受 ) ：返还 做 wasm_原子操作 用 59 }：
wasm_整64_atomic_rmw_xchg 接受 ) ：返还 做 wasm_原子操作 用 66 }：
wasm_整64_atomic_rmw_cmpxchg 接受 ) ：返还 做 wasm_原子操作 用 73 }：

※ WASM 引用类型 / 异常处理 / 尾调用

※ ref.null — 空引用
wasm_引用_空 接受 类型) ：返还 [0xD0、类型]：
完。
※ ref.is_null — 引用判空
wasm_引用_是否空 接受 ) ：返还 [0xD1] }：
※ ref.func — 函数引用
wasm_引用_函数 接受 索引) { 定义 结果：
 编码值 为 做 wasm_编码_uleb128 用 索引。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。
※ table.get / table.set
wasm_表_获取 接受 表索引) { 定义 结果：
 编码值 为 做 wasm_编码_uleb128 用 表索引。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。
wasm_表_设置 接受 表索引) { 定义 结果：
 编码值 为 做 wasm_编码_uleb128 用 表索引。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。
※ 异常处理/throw (异常处理提案)
wasm_尝试 接受 bt) ：返还 [0x06、bt] }：
wasm_捕获 接受 标签) { 定义 结果：
 编码值 为 做 wasm_编码_uleb128 用 标签。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。
wasm_抛出 接受 标签) { 定义 结果：
 编码值 为 做 wasm_编码_uleb128 用 标签。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。
wasm_重抛 接受 深度) ：返还 [0x09、深度] }：
※ return_call / return_call_indirect (尾调用)
wasm_返回调用 接受 索引) { 定义 结果：
 编码值 为 做 wasm_编码_uleb128 用 索引。
 可变 i 为 0。
 只要 i 小于 做 计数 用 编码值：
 返还 结果。
完。
wasm_返回间接调用 接受 类型索引、表索引) { 定义 结果：
 enc1 为 做 wasm_编码_uleb128 用 类型索引。
 enc2 为 做 wasm_编码_uleb128 用 表索引。
 可变 i 为 0。
 只要 i 小于 做 计数 用 enc1：
 i 为 0。
 只要 i 小于 做 计数 用 enc2：
 返还 结果。
完。

※ WASM GC 提案 (prefix 0xFB)
※ struct.new type_idx
wasm_结构_新建 接受 类型) { 定义 r、0x00] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
※ struct.new_default type_idx
wasm_结构_新建默认 接受 类型) { 定义 r、0x01] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
※ struct.get type_idx field_idx
wasm_结构_获取 接受 类型、字段) { 定义 r、0x02]：
 e1 为 做 wasm_编码_uleb128 用 类型 e2 = 做 wasm_编码_uleb128 用 字段。
 可变 i 为 0 只要 (i 小于 做 计数 用 e1) { 做 追加 用 r、e1[i] i = i 加 1 }。
 i 为 0 只要 (i 小于 做 计数 用 e2) { 做 追加 用 r、e2[i] i = i 加 1 } 返还 r }。
※ struct.get_s / struct.get_u
wasm_结构_获取有符号 接受 类型、字段) { 定义 r、0x03]：
 e1 为 做 wasm_编码_uleb128 用 类型 e2 = 做 wasm_编码_uleb128 用 字段。
 可变 i 为 0 只要 (i 小于 做 计数 用 e1) { 做 追加 用 r、e1[i] i = i 加 1 }。
 i 为 0 只要 (i 小于 做 计数 用 e2) { 做 追加 用 r、e2[i] i = i 加 1 } 返还 r }。
wasm_结构_获取无符号 接受 类型、字段) { 定义 r、0x04]：
 e1 为 做 wasm_编码_uleb128 用 类型 e2 = 做 wasm_编码_uleb128 用 字段。
 可变 i 为 0 只要 (i 小于 做 计数 用 e1) { 做 追加 用 r、e1[i] i = i 加 1 }。
 i 为 0 只要 (i 小于 做 计数 用 e2) { 做 追加 用 r、e2[i] i = i 加 1 } 返还 r }。
※ struct.set type_idx field_idx
wasm_结构_设置 接受 类型、字段) { 定义 r、0x05]：
 e1 为 做 wasm_编码_uleb128 用 类型 e2 = 做 wasm_编码_uleb128 用 字段。
 可变 i 为 0 只要 (i 小于 做 计数 用 e1) { 做 追加 用 r、e1[i] i = i 加 1 }。
 i 为 0 只要 (i 小于 做 计数 用 e2) { 做 追加 用 r、e2[i] i = i 加 1 } 返还 r }。
※ array.new / array.new_default / array.new_fixed
wasm_数组_新建 接受 类型) { 定义 r、0x06] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_数组_新建默认 接受 类型) { 定义 r、0x07] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_数组_新建固定 接受 类型、长度) { 定义 r、0x08]：
 e1 为 做 wasm_编码_uleb128 用 类型 e2 = 做 wasm_编码_uleb128 用 长度。
 可变 i 为 0 只要 (i 小于 做 计数 用 e1) { 做 追加 用 r、e1[i] i = i 加 1 }。
 i 为 0 只要 (i 小于 做 计数 用 e2) { 做 追加 用 r、e2[i] i = i 加 1 } 返还 r }。
※ array.new_data / array.new_elem
wasm_数组_新建数据 接受 类型、数据) { 定义 r、0x09]：
 e1 为 做 wasm_编码_uleb128 用 类型 e2 = 做 wasm_编码_uleb128 用 数据。
 可变 i 为 0 只要 (i 小于 做 计数 用 e1) { 做 追加 用 r、e1[i] i = i 加 1 }。
 i 为 0 只要 (i 小于 做 计数 用 e2) { 做 追加 用 r、e2[i] i = i 加 1 } 返还 r }。
wasm_数组_新建元素 接受 类型、元素) { 定义 r、0x0A]：
 e1 为 做 wasm_编码_uleb128 用 类型 e2 = 做 wasm_编码_uleb128 用 元素。
 可变 i 为 0 只要 (i 小于 做 计数 用 e1) { 做 追加 用 r、e1[i] i = i 加 1 }。
 i 为 0 只要 (i 小于 做 计数 用 e2) { 做 追加 用 r、e2[i] i = i 加 1 } 返还 r }。
※ array.get / array.get_s / array.get_u
wasm_数组_获取 接受 类型) { 定义 r、0x0B] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_数组_获取有符号 接受 类型) { 定义 r、0x0C] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_数组_获取无符号 接受 类型) { 定义 r、0x0D] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
※ array.set / array.len / array.fill / array.copy
wasm_数组_设置 接受 类型) { 定义 r、0x0E] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_数组_长度 接受 ) ：返还 [0xFB、0x0F] }：
wasm_数组_填充 接受 类型) { 定义 r、0x10] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_数组_复制 接受 d类型、s类型) { 定义 r、0x11]：
 e1 为 做 wasm_编码_uleb128 用 d类型 e2 = 做 wasm_编码_uleb128 用 s类型。
 可变 i 为 0 只要 (i 小于 做 计数 用 e1) { 做 追加 用 r、e1[i] i = i 加 1 }。
 i 为 0 只要 (i 小于 做 计数 用 e2) { 做 追加 用 r、e2[i] i = i 加 1 } 返还 r }。
※ array.init_data / array.init_elem
wasm_数组_初始化数据 接受 类型、数据) { 定义 r、0x12]：
 e1 为 做 wasm_编码_uleb128 用 类型 e2 = 做 wasm_编码_uleb128 用 数据。
 可变 i 为 0 只要 (i 小于 做 计数 用 e1) { 做 追加 用 r、e1[i] i = i 加 1 }。
 i 为 0 只要 (i 小于 做 计数 用 e2) { 做 追加 用 r、e2[i] i = i 加 1 } 返还 r }。
wasm_数组_初始化元素 接受 类型、元素) { 定义 r、0x13]：
 e1 为 做 wasm_编码_uleb128 用 类型 e2 = 做 wasm_编码_uleb128 用 元素。
 可变 i 为 0 只要 (i 小于 做 计数 用 e1) { 做 追加 用 r、e1[i] i = i 加 1 }。
 i 为 0 只要 (i 小于 做 计数 用 e2) { 做 追加 用 r、e2[i] i = i 加 1 } 返还 r }。
※ ref.test / ref.cast (non-null variants)
wasm_引用_测试 接受 类型) { 定义 r、0x14] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_引用_测试空 接受 类型) { 定义 r、0x15] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_引用_转换 接受 类型) { 定义 r、0x16] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_引用_转换空 接受 类型) { 定义 r、0x17] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
※ br_on_cast / br_on_cast_fail
wasm_转换时分支 接受 标志、标签、ht1、ht2) ：返还 [0xFB、0x18、标志、标签、ht1、ht2] }：
wasm_转换失败分支 接受 标志、标签、ht1、ht2) ：返还 [0xFB、0x19、标志、标签、ht1、ht2] }：
※ any.convert_extern / extern.convert_any
wasm_任意转外部 接受 ) ：返还 [0xFB、0x1A] }：
wasm_外部转任意 接受 ) ：返还 [0xFB、0x1B] }：
※ ref.i31 / i31.get_s / i31.get_u
wasm_引用_i31 接受 ) ：返还 [0xFB、0x1C] }：
wasm_整31_get_s 接受 ) ：返还 [0xFB、0x1D] }：
wasm_整31_get_u 接受 ) ：返还 [0xFB、0x1E] }：

※ WASM Relaxed SIMD (prefix 0xFD, 高编号)
※ relaxed i8x16.swizzle
wasm_整8x16_松弛重排 接受 ) ：返还 [0xFD、0x80、0x02] }：
※ relaxed i32x4.trunc_f32x4_s/u
wasm_整32x4_relaxed_trunc_f32x4_s 接受 ) ：返还 [0xFD、0x81、0x02] }：
wasm_整32x4_relaxed_trunc_f32x4_u 接受 ) ：返还 [0xFD、0x82、0x02] }：
※ relaxed i32x4.trunc_f64x2_s/u_zero
wasm_整32x4_relaxed_trunc_f64x2_s_zero 接受 ) ：返还 [0xFD、0x83、0x02] }：
wasm_整32x4_relaxed_trunc_f64x2_u_zero 接受 ) ：返还 [0xFD、0x84、0x02] }：
※ relaxed f32x4/f64x2 madd/nmadd (FMA)
wasm_浮32x4_松弛乘加 接受 ) ：返还 [0xFD、0x85、0x02] }：
wasm_浮32x4_松弛负乘加 接受 ) ：返还 [0xFD、0x86、0x02] }：
wasm_浮64x2_松弛乘加 接受 ) ：返还 [0xFD、0x87、0x02] }：
wasm_浮64x2_松弛负乘加 接受 ) ：返还 [0xFD、0x88、0x02] }：
※ relaxed i8x16.laneselect / i16x8.laneselect / i32x4.laneselect / i64x2.laneselect
wasm_整8x16_松弛通道选择 接受 ) ：返还 [0xFD、0x89、0x02] }：
wasm_整16x8_松弛通道选择 接受 ) ：返还 [0xFD、0x8A、0x02] }：
wasm_整32x4_松弛通道选择 接受 ) ：返还 [0xFD、0x8B、0x02] }：
wasm_整64x2_松弛通道选择 接受 ) ：返还 [0xFD、0x8C、0x02] }：
※ relaxed f32x4/f64x2 min/max
wasm_浮32x4_松弛最小 接受 ) ：返还 [0xFD、0x8D、0x02] }：
wasm_浮32x4_松弛最大 接受 ) ：返还 [0xFD、0x8E、0x02] }：
wasm_浮64x2_松弛最小 接受 ) ：返还 [0xFD、0x8F、0x02] }：
wasm_浮64x2_松弛最大 接受 ) ：返还 [0xFD、0x90、0x02] }：
※ relaxed i16x8.q15mulr_s
wasm_整16x8_relaxed_q15mulr_s 接受 ) ：返还 [0xFD、0x91、0x02] }：
※ relaxed i16x8.dot_i8x16_i7x16_s
wasm_整16x8_松弛点积i8x16 接受 ) ：返还 [0xFD、0x92、0x02] }：
※ relaxed i32x4.dot_i8x16_i7x16_add_s
wasm_整32x4_松弛点积加i8x16 接受 ) ：返还 [0xFD、0x93、0x02] }：

※ WASM 多表/多内存/杂项补全
※ table.init elem_idx table_idx
wasm_表_初始化 接受 元素、表) { 定义 r、0x0C]：
 e1 为 做 wasm_编码_uleb128 用 元素 e2 = 做 wasm_编码_uleb128 用 表。
 可变 i 为 0 只要 (i 小于 做 计数 用 e1) { 做 追加 用 r、e1[i] i = i 加 1 }。
 i 为 0 只要 (i 小于 做 计数 用 e2) { 做 追加 用 r、e2[i] i = i 加 1 } 返还 r }。
※ elem.drop seg_idx
wasm_元素_丢弃 接受 段) { 定义 r、0x0D] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
※ table.copy dst_table src_table
wasm_表_复制 接受 目标、源) { 定义 r、0x0E]：
 e1 为 做 wasm_编码_uleb128 用 目标 e2 = 做 wasm_编码_uleb128 用 源。
 可变 i 为 0 只要 (i 小于 做 计数 用 e1) { 做 追加 用 r、e1[i] i = i 加 1 }。
 i 为 0 只要 (i 小于 做 计数 用 e2) { 做 追加 用 r、e2[i] i = i 加 1 } 返还 r }。
※ table.grow / table.size / table.fill
wasm_表_增长 接受 表) { 定义 r、0x0F] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_表_大小 接受 表) { 定义 r、0x10] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_表_填充 接受 表) { 定义 r、0x11] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
※ select with type
wasm_类型选择 接受 类型数、类型) ：返还 [0x1C、类型数、类型] }：
※ memory.init / data.drop / memory.copy / memory.fill (bulk memory)
wasm_内存_初始化 接受 数据) { 定义 r、0x08] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 做 追加 用 r、0 返还 r }。
wasm_数据_丢弃 接受 数据) { 定义 r、0x09] 定义 e：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_内存_复制 接受 ) ：返还 [0xFC、0x0A、0x00、0x00] }：
wasm_内存_填充 接受 ) ：返还 [0xFC、0x0B、0x00] }：
※ i32.trunc_sat_f32_s/u / i32.trunc_sat_f64_s/u (非陷阱转换)
wasm_整32_trunc_sat_f32_s 接受 ) ：返还 [0xFC、0x00] }：
wasm_整32_trunc_sat_f32_u 接受 ) ：返还 [0xFC、0x01] }：
wasm_整32_trunc_sat_f64_s 接受 ) ：返还 [0xFC、0x02] }：
wasm_整32_trunc_sat_f64_u 接受 ) ：返还 [0xFC、0x03] }：
wasm_整64_trunc_sat_f32_s 接受 ) ：返还 [0xFC、0x04] }：
wasm_整64_trunc_sat_f32_u 接受 ) ：返还 [0xFC、0x05] }：
wasm_整64_trunc_sat_f64_s 接受 ) ：返还 [0xFC、0x06] }：
wasm_整64_trunc_sat_f64_u 接受 ) ：返还 [0xFC、0x07] }：
※ ref.eq — 引用相等比较
wasm_引用_等于 接受 ) ：返还 [0xD3] }：
※ ref.as_non_null
wasm_引用_转非空 接受 ) ：返还 [0xD4] }：
※ br_on_null / br_on_non_null
wasm_空时分支 接受 标签) { 定义 r：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_非空时分支 接受 标签) { 定义 r：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
※ call_ref / return_call_ref
wasm_引用调用 接受 类型) { 定义 r：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
wasm_返回引用调用 接受 类型) { 定义 r：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。

※ catch_all
wasm_捕获全部 接受 ) ：返还 [0x19] }：
※ delegate label
wasm_委托 接受 标签) { 定义 r：
 可变 i 为 0 只要 (i 小于 做 计数 用 e) { 做 追加 用 r、e[i] i = i 加 1 } 返还 r }。
※ throw_ref (exnref提案)
wasm_抛出引用 接受 ) ：返还 [0x0A] }：
※ try_table (新异常处理)
wasm_尝试表 接受 块类型) ：返还 [0x1F、块类型] }：

※ i64.atomic.load8_u
wasm_整64_atomic_load8_u 接受 对齐、偏移) { 定义 r、0x14] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
※ i64.atomic.load16_u
wasm_整64_atomic_load16_u 接受 对齐、偏移) { 定义 r、0x15] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
※ i64.atomic.load32_u
wasm_整64_atomic_load32_u 接受 对齐、偏移) { 定义 r、0x16] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
※ i64.atomic.store8
wasm_整64_atomic_store8 接受 对齐、偏移) { 定义 r、0x1B] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
※ i64.atomic.store16
wasm_整64_atomic_store16 接受 对齐、偏移) { 定义 r、0x1C] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
※ i64.atomic.store32
wasm_整64_atomic_store32 接受 对齐、偏移) { 定义 r、0x1D] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
※ i32.atomic.rmw8.add_u / sub_u / and_u / or_u / xor_u / xchg_u / cmpxchg_u
wasm_整32_atomic_rmw8_add_u 接受 对齐、偏移) { 定义 r、0x2A] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
wasm_整32_atomic_rmw8_sub_u 接受 对齐、偏移) { 定义 r、0x2B] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
wasm_整32_atomic_rmw8_and_u 接受 对齐、偏移) { 定义 r、0x2C] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
wasm_整32_atomic_rmw8_or_u 接受 对齐、偏移) { 定义 r、0x2D] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
wasm_整32_atomic_rmw8_xor_u 接受 对齐、偏移) { 定义 r、0x2E] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
wasm_整32_atomic_rmw8_xchg_u 接受 对齐、偏移) { 定义 r、0x2F] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
wasm_整32_atomic_rmw8_cmpxchg_u 接受 对齐、偏移) { 定义 r、0x4A] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
※ i32.atomic.rmw16.add_u / sub_u / and_u / or_u / xor_u / xchg_u / cmpxchg_u
wasm_整32_atomic_rmw16_add_u 接受 对齐、偏移) { 定义 r、0x30] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
wasm_整32_atomic_rmw16_sub_u 接受 对齐、偏移) { 定义 r、0x31] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
wasm_整32_atomic_rmw16_and_u 接受 对齐、偏移) { 定义 r、0x32] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
wasm_整32_atomic_rmw16_or_u 接受 对齐、偏移) { 定义 r、0x33] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
wasm_整32_atomic_rmw16_xor_u 接受 对齐、偏移) { 定义 r、0x34] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
wasm_整32_atomic_rmw16_xchg_u 接受 对齐、偏移) { 定义 r、0x35] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：
wasm_整32_atomic_rmw16_cmpxchg_u 接受 对齐、偏移) { 定义 r、0x4B] 做 追加 用 r、对齐 做 追加 用 r、偏移 返回 r }：

wasm_整8x16_左移 接受 ) ：返还 做 wasm_SIMD指令集 用 107 }：
wasm_整8x16_有符号右移 接受 ) ：返还 做 wasm_SIMD指令集 用 108 }：
wasm_整8x16_无符号右移 接受 ) ：返还 做 wasm_SIMD指令集 用 109 }：
wasm_整16x8_窄化i32x4有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 133 }：
wasm_整16x8_窄化i32x4无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 134 }：
wasm_整16x8_低半扩展i8x16有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 135 }：
wasm_整16x8_高半扩展i8x16有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 136 }：
wasm_整16x8_低半扩展i8x16无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 137 }：
wasm_整16x8_高半扩展i8x16无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 138 }：
wasm_整32x4_低半扩展i16x8有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 167 }：
wasm_整32x4_高半扩展i16x8有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 168 }：
wasm_整32x4_低半扩展i16x8无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 169 }：
wasm_整32x4_高半扩展i16x8无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 170 }：
wasm_整64x2_低半扩展i32x4有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 199 }：
wasm_整64x2_高半扩展i32x4有符号 接受 ) ：返还 做 wasm_SIMD指令集 用 200 }：
wasm_整64x2_低半扩展i32x4无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 201 }：
wasm_整64x2_高半扩展i32x4无符号 接受 ) ：返还 做 wasm_SIMD指令集 用 202 }：

※ 更多数值转换/位操作
※ i32.extend8_s / i32.extend16_s
wasm_整32_有符号扩展8 接受 ) ：返还 0xC0 }：
wasm_整32_有符号扩展16 接受 ) ：返还 0xC1 }：
※ i64.extend8_s / i64.extend16_s / i64.extend32_s
wasm_整64_有符号扩展8 接受 ) ：返还 0xC2 }：
wasm_整64_有符号扩展16 接受 ) ：返还 0xC3 }：
wasm_整64_有符号扩展32 接受 ) ：返还 0xC4 }：

本章 收束。
