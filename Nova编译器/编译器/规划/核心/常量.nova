《常量》

引用《..·..·..·标准库·基础》。

引用《.·发射器》。

版本_主 为 3。

版本_次 为 1。

版本_补 为 0。

版本字符串 接受 零参：
    返还 做 字符串拼接 用 做 转字符串 用 版本_主、做 字符串拼接 用 「.」、做 字符串拼接 用 做 转字符串 用 版本_次、做 字符串拼接 用 「.」、做 转字符串 用 版本_补。
完。

词_结束 为 0。

词_整数 为 1。

词_浮点 为 2。

词_字符串 为 3。

词_标识符 为 4。

词_函数 为 5。

词_定义 为 6。

词_可变 为 7。

词_返回 为 8。

词_如果 为 9。

词_否则 为 10。

词_当 为 11。

词_真 为 12。

词_假 为 13。

词_空 为 14。

词_结构 为 15。

词_新建 为 16。

词_导入 为 17。

词_每个 为 18。

词_在 为 19。

词_左括号 为 20。

词_右括号 为 21。

词_左花括 为 22。

词_右花括 为 23。

词_左方括 为 24。

词_右方括 为 25。

词_逗号 为 26。

词_点 为 27。

词_冒号 为 28。

词_感叹 为 29。

词_加 为 30。

词_减 为 31。

词_乘 为 32。

词_除 为 33。

词_取模 为 34。

词_赋值 为 35。

词_等于 为 36。

词_不等 为 37。

词_小于 为 38。

词_大于 为 39。

词_小等 为 40。

词_大等 为 41。

词_与 为 42。

词_或 为 43。

词_位与 为 54。

词_位或 为 55。

词_位异或 为 56。

词_左移 为 57。

词_右移 为 58。

词_位非 为 59。

词_匹配 为 70。

词_中断 为 71。

词_继续 为 72。

词_尝试 为 73。

词_捕获 为 74。

词_抛出 为 75。

词_最终 为 76。

词_箭头 为 80。

词_分号 为 81。

词_双冒号 为 82。

词_问号 为 83。

词_加等 为 84。

词_减等 为 85。

词_乘等 为 86。

词_除等 为 87。

词_取模等 为 88。

词_位与等 为 89。

词_位或等 为 90。

词_位异或等 为 91。

词_左移等 为 92。

词_右移等 为 93。

词_接口 为 94。

词_实现 为 95。

词_枚举 为 96。

词_类型 为 97。

词_异步 为 98。

词_等待 为 99。

可变 节_整数 为 100。

可变 节_字符串 为 101。

可变 节_标识符 为 102。

可变 节_调用 为 103。

可变 节_二元 为 104。

可变 节_列表 为 105。

可变 节_索引 为 106。

可变 节_定义 为 110。

可变 节_赋值 为 111。

可变 节_如果 为 112。

可变 节_当 为 113。

可变 节_返回 为 114。

可变 节_表达式 为 115。

可变 节_索引赋值 为 116。

可变 节_函数 为 120。

可变 节_新建 为 130。

可变 节_点访问 为 131。

可变 节_点赋值 为 132。

可变 节_匿名函数 为 133。

可变 节_每个 为 134。

可变 节_匹配 为 135。

可变 节_中断 为 136。

可变 节_继续 为 137。

可变 节_逻辑非 为 138。

可变 节_位非 为 139。

可变 节_间接调用 为 140。

可变 节_链式索引赋值 为 141。

可变 节_浮点 为 150。

可变 节_字典 为 151。

可变 节_尝试 为 160。

可变 节_捕获 为 161。

可变 节_抛出 为 162。

可变 节_最终 为 163。

可变 节_复合赋值 为 170。

可变 节_三元 为 171。

可变 节_接口 为 180。

可变 节_实现 为 181。

可变 节_枚举 为 182。

可变 节_类型别名 为 183。

可变 节_异步 为 190。

可变 节_等待 为 191。

可变 节_管道 为 192。

可变 节_展开 为 193。

可变 节_解构 为 194。

编译_初始化AST常量 接受 零参：
    节_整数 为 100。
    节_字符串 为 101。
    节_标识符 为 102。
    节_调用 为 103。
    节_二元 为 104。
    节_列表 为 105。
    节_索引 为 106。
    节_定义 为 110。
    节_赋值 为 111。
    节_如果 为 112。
    节_当 为 113。
    节_返回 为 114。
    节_表达式 为 115。
    节_索引赋值 为 116。
    节_函数 为 120。
    节_新建 为 130。
    节_点访问 为 131。
    节_点赋值 为 132。
    节_匿名函数 为 133。
    节_每个 为 134。
    节_匹配 为 135。
    节_中断 为 136。
    节_继续 为 137。
    节_逻辑非 为 138。
    节_位非 为 139。
    节_间接调用 为 140。
    节_链式索引赋值 为 141。
    节_浮点 为 150。
    节_字典 为 151。
    节_尝试 为 160。
    节_捕获 为 161。
    节_抛出 为 162。
    节_最终 为 163。
    节_复合赋值 为 170。
    节_三元 为 171。
    节_接口 为 180。
    节_实现 为 181。
    节_枚举 为 182。
    节_类型别名 为 183。
    节_异步 为 190。
    节_等待 为 191。
    节_管道 为 192。
    节_展开 为 193。
    节_解构 为 194。
完。

寄_RAX 为 0。

寄_RCX 为 1。

寄_RDX 为 2。

寄_RBX 为 3。

寄_RSP 为 4。

寄_RBP 为 5。

寄_RSI 为 6。

寄_RDI 为 7。

寄_R8 为 8。

寄_R9 为 9。

寄_R10 为 10。

寄_R11 为 11。

寄_R12 为 12。

寄_R13 为 13。

寄_R14 为 14。

寄_R15 为 15。

寄_XMM0 为 0。

寄_XMM1 为 1。

寄_XMM2 为 2。

寄_XMM3 为 3。

寄_XMM4 为 4。

寄_XMM5 为 5。

寄_XMM6 为 6。

寄_XMM7 为 7。

架构_X86_64 为 1。

架构_ARM64 为 2。

架构_RISCV64 为 3。

架构_WASM32 为 4。

架构_WASM64 为 5。

系统_WINDOWS 为 1。

系统_LINUX 为 2。

系统_MACOS 为 3。

系统_FREESTANDING 为 0。

调用_WINDOWS_X64 为 1。

调用_SYSTEM_V 为 2。

调用_AAPCS64 为 3。

调用_RISCV 为 4。

参数寄1_WIN 为 1。

参数寄2_WIN 为 2。

参数寄3_WIN 为 8。

参数寄4_WIN 为 9。

参数寄1_SV 为 7。

参数寄2_SV 为 6。

参数寄3_SV 为 2。

参数寄4_SV 为 1。

参数寄5_SV 为 8。

参数寄6_SV 为 9。

类_空 为 0。

类_整数 为 1。

类_浮点 为 2。

类_布尔 为 3。

类_字符串 为 4。

类_列表 为 5。

类_字典 为 6。

类_函数 为 7。

类_结构 为 8。

类_指针 为 9。

类_数组 为 10。

类_枚举 为 11。

类_接口 为 12。

类_任意 为 99。

宽_8 为 1。

宽_16 为 2。

宽_32 为 4。

宽_64 为 8。

宽_128 为 16。

优化_O0 为 0。

优化_O1 为 1。

优化_O2 为 2。

优化_O3 为 3。

优化_Os 为 4。

错_无 为 0。

错_语法 为 1。

错_类型 为 2。

错_未定义 为 3。

错_重定义 为 4。

错_参数数量 为 5。

错_类型不匹配 为 6。

错_除零 为 7。

错_越界 为 8。

错_空引用 为 9。

错_栈溢出 为 10。

错_内存不足 为 11。

错_IO 为 12。

错_断言失败 为 13。

错_内部 为 99。

动态精度 为 10000。

动态零 为 0。

动态半 为 5000。

动态一 为 10000。

动态极小 为 1。

动态_乘 接受 a、b：
    返还 a 乘 b 除以 动态精度。
完。

动态_加 接受 a、b：
    和 为 a 加 b。
    若 和 大于 动态一：
        返还 动态一。
    完。
    返还 和。
完。

动态_减 接受 a、b：
    差 为 a 减 b。
    若 差 小于 动态零：
        返还 动态零。
    完。
    返还 差。
完。

动态_反 接受 a：
    返还 动态一 减 a。
完。

动态_钳位 接受 v、最小、最大：
    若 v 小于 最小：
        返还 最小。
    完。
    若 v 大于 最大：
        返还 最大。
    完。
    返还 v。
完。

动态_线性插值 接受 a、b、t：
    返还 a 加 做 动态_乘 用 b 减 a、t。
完。

动态_从整数 接受 分子、分母：
    若 分母 等于 0：
        返还 0。
    完。
    返还 分子 乘 动态精度 除以 分母。
完。

动态_到整数 接受 v、范围：
    若 动态精度 等于 0：
        返还 0。
    完。
    返还 v 乘 范围 除以 动态精度。
完。

动态_sigmoid 接受 x：
    可变 归一 为 x。
    若 归一 小于 0：
        归一 为 0 减 归一。
    完。
    分母 为 动态精度 加 归一。
    若 分母 等于 0：
        返还 动态半。
    完。
    若 x 大于或等于 0：
        返还 动态半 加 x 乘 动态半 除以 分母。
    完。
    返还 动态半 减 (0 减 x) 乘 动态半 除以 分母。
完。

动态_relu 接受 x：
    若 x 小于 0：
        返还 0。
    完。
    返还 x。
完。

动态_softmax 接受 值列表：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 值列表：
        总和 为 总和 加 值列表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 总和 等于 0：
        返还 值列表。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 值列表：
        做 追加 用 结果、值列表 的 第 i 项 乘 动态精度 除以 总和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

PE_文件对齐 为 0x200。

PE_段对齐 为 0x1000。

PE_代码段RVA 为 0x1000。

PE_基址 为 0x400000。

PE_魔数 为 0x5A4D。

PE_签名 为 0x4550。

PE_可选头魔数 为 0x020B。

PE_机器_X64 为 0x8664。

PE_机器_ARM64 为 0xAA64。

PE_机器_RISCV64 为 0x5064。

PE_机器_I386 为 0x014C。

PE_子系统_控制台 为 3。

PE_子系统_窗口 为 2。

PE_子系统_EFI应用 为 10。

PE_DLL_高熵VA 为 0x0020。

PE_DLL_动态基址 为 0x0040。

PE_DLL_NX兼容 为 0x0100。

PE_DLL_终端服务器 为 0x8000。

PE_段_代码 为 0x00000020。

PE_段_已初始化 为 0x00000040。

PE_段_未初始化 为 0x00000080。

PE_段_可执行 为 0x20000000。

PE_段_可读 为 0x40000000。

PE_段_可写 为 0x80000000。

PE_目录_导出 为 0。

PE_目录_导入 为 1。

PE_目录_资源 为 2。

PE_目录_异常 为 3。

PE_目录_安全 为 4。

PE_目录_基址重定位 为 5。

PE_目录_调试 为 6。

PE_目录_TLS 为 9。

PE_目录_IAT 为 12。

PE_目录_数量 为 16。

PE_特征_可执行 为 0x0002。

PE_特征_大地址感知 为 0x0020。

PE_特征_无重定位 为 0x0001。

ELF_魔数 为 0x464C457F。

ELF_类_64 为 2。

ELF_数据_小端 为 1。

ELF_类型_可执行 为 2。

ELF_类型_共享 为 3。

ELF_机器_X86_64 为 62。

ELF_机器_AARCH64 为 183。

ELF_机器_RISCV 为 243。

MACHO_魔数_64 为 0xFEEDFACF。

MachO_处理器_X86_64 为 0x01000007。

MachO_处理器_ARM64 为 0x0100000C。

MACHO_文件_可执行 为 2。

MACHO_文件_动态库 为 6。

词类型名 接受 类型：
    若 类型 等于 词_结束：
        返还 「结束」。
    完。
    若 类型 等于 词_整数：
        返还 「整数」。
    完。
    若 类型 等于 词_浮点：
        返还 「浮点」。
    完。
    若 类型 等于 词_字符串：
        返还 「字符串」。
    完。
    若 类型 等于 词_标识符：
        返还 「标识符」。
    完。
    若 类型 等于 词_函数：
        返还 「法」。
    完。
    若 类型 等于 词_定义：
        返还 「令」。
    完。
    若 类型 等于 词_返回：
        返还 「回」。
    完。
    若 类型 等于 词_如果：
        返还 「若」。
    完。
    若 类型 等于 词_当：
        返还 「当」。
    完。
    若 类型 等于 词_加：
        返还 「+」。
    完。
    若 类型 等于 词_减：
        返还 「-」。
    完。
    若 类型 等于 词_乘：
        返还 「*」。
    完。
    若 类型 等于 词_除：
        返还 「/」。
    完。
    若 类型 等于 词_等于：
        返还 「==」。
    完。
    若 类型 等于 词_赋值：
        返还 「=」。
    完。
    若 类型 等于 词_不等：
        返还 「!=」。
    完。
    若 类型 等于 词_小于：
        返还 「<」。
    完。
    若 类型 等于 词_大于：
        返还 「>」。
    完。
    若 类型 等于 词_小等：
        返还 「<=」。
    完。
    若 类型 等于 词_大等：
        返还 「>=」。
    完。
    若 类型 等于 词_与：
        返还 「&&」。
    完。
    若 类型 等于 词_或：
        返还 「||」。
    完。
    若 类型 等于 词_取模：
        返还 「%」。
    完。
    若 类型 等于 词_位与：
        返还 「&」。
    完。
    若 类型 等于 词_位或：
        返还 「|」。
    完。
    若 类型 等于 词_位异或：
        返还 「^」。
    完。
    若 类型 等于 词_左移：
        返还 「<<」。
    完。
    若 类型 等于 词_右移：
        返还 「>>」。
    完。
    若 类型 等于 词_位非：
        返还 「~」。
    完。
    若 类型 等于 词_感叹：
        返还 「!」。
    完。
    若 类型 等于 词_左括号：
        返还 「(」。
    完。
    若 类型 等于 词_右括号：
        返还 「)」。
    完。
    若 类型 等于 词_左花括：
        返还 「{」。
    完。
    若 类型 等于 词_右花括：
        返还 「}」。
    完。
    若 类型 等于 词_左方括：
        返还 「[」。
    完。
    若 类型 等于 词_右方括：
        返还 「]」。
    完。
    若 类型 等于 词_逗号：
        返还 「,」。
    完。
    若 类型 等于 词_点：
        返还 「.」。
    完。
    若 类型 等于 词_冒号：
        返还 「:」。
    完。
    若 类型 等于 词_可变：
        返还 「变」。
    完。
    若 类型 等于 词_否则：
        返还 「否则」。
    完。
    若 类型 等于 词_真：
        返还 「真」。
    完。
    若 类型 等于 词_假：
        返还 「假」。
    完。
    若 类型 等于 词_空：
        返还 「空」。
    完。
    若 类型 等于 词_结构：
        返还 「构」。
    完。
    若 类型 等于 词_新建：
        返还 「新」。
    完。
    若 类型 等于 词_导入：
        返还 「导入」。
    完。
    若 类型 等于 词_每个：
        返还 「每个」。
    完。
    若 类型 等于 词_在：
        返还 「在」。
    完。
    若 类型 等于 词_匹配：
        返还 「匹配」。
    完。
    若 类型 等于 词_中断：
        返还 「中断」。
    完。
    若 类型 等于 词_继续：
        返还 「继续」。
    完。
    若 类型 等于 词_尝试：
        返还 「尝试」。
    完。
    若 类型 等于 词_捕获：
        返还 「捕获」。
    完。
    若 类型 等于 词_抛出：
        返还 「抛出」。
    完。
    若 类型 等于 词_箭头：
        返还 「=>」。
    完。
    若 类型 等于 词_分号：
        返还 「;」。
    完。
    若 类型 等于 词_接口：
        返还 「接口」。
    完。
    若 类型 等于 词_实现：
        返还 「实现」。
    完。
    若 类型 等于 词_枚举：
        返还 「枚举」。
    完。
    若 类型 等于 词_异步：
        返还 「异步」。
    完。
    若 类型 等于 词_等待：
        返还 「等待」。
    完。
    返还 「unknown」。
完。

节点类型名 接受 类型：
    若 类型 等于 节_整数：
        返还 「整数」。
    完。
    若 类型 等于 节_浮点：
        返还 「浮点」。
    完。
    若 类型 等于 节_字符串：
        返还 「字符串」。
    完。
    若 类型 等于 节_标识符：
        返还 「标识符」。
    完。
    若 类型 等于 节_调用：
        返还 「调用」。
    完。
    若 类型 等于 节_二元：
        返还 「二元」。
    完。
    若 类型 等于 节_定义：
        返还 「令」。
    完。
    若 类型 等于 节_赋值：
        返还 「赋值」。
    完。
    若 类型 等于 节_如果：
        返还 「若」。
    完。
    若 类型 等于 节_当：
        返还 「当」。
    完。
    若 类型 等于 节_返回：
        返还 「回」。
    完。
    若 类型 等于 节_函数：
        返还 「法」。
    完。
    若 类型 等于 节_列表：
        返还 「列表」。
    完。
    若 类型 等于 节_索引：
        返还 「索引」。
    完。
    若 类型 等于 节_表达式：
        返还 「表达式」。
    完。
    若 类型 等于 节_索引赋值：
        返还 「索引赋值」。
    完。
    若 类型 等于 节_新建：
        返还 「新建」。
    完。
    若 类型 等于 节_点访问：
        返还 「点访问」。
    完。
    若 类型 等于 节_点赋值：
        返还 「点赋值」。
    完。
    若 类型 等于 节_匿名函数：
        返还 「匿名函数」。
    完。
    若 类型 等于 节_每个：
        返还 「每个」。
    完。
    若 类型 等于 节_匹配：
        返还 「匹配」。
    完。
    若 类型 等于 节_中断：
        返还 「中断」。
    完。
    若 类型 等于 节_继续：
        返还 「继续」。
    完。
    若 类型 等于 节_逻辑非：
        返还 「逻辑非」。
    完。
    若 类型 等于 节_位非：
        返还 「位非」。
    完。
    若 类型 等于 节_字典：
        返还 「字典」。
    完。
    若 类型 等于 节_尝试：
        返还 「尝试」。
    完。
    若 类型 等于 节_捕获：
        返还 「捕获」。
    完。
    若 类型 等于 节_抛出：
        返还 「抛出」。
    完。
    若 类型 等于 节_复合赋值：
        返还 「复合赋值」。
    完。
    若 类型 等于 节_三元：
        返还 「三元」。
    完。
    若 类型 等于 节_接口：
        返还 「接口」。
    完。
    若 类型 等于 节_实现：
        返还 「实现」。
    完。
    若 类型 等于 节_枚举：
        返还 「枚举」。
    完。
    若 类型 等于 节_异步：
        返还 「异步」。
    完。
    若 类型 等于 节_等待：
        返还 「等待」。
    完。
    返还 「unknown」。
完。

架构名 接受 架构：
    若 架构 等于 架构_X86_64：
        返还 「x86-64」。
    完。
    若 架构 等于 架构_ARM64：
        返还 「ARM64」。
    完。
    若 架构 等于 架构_RISCV64：
        返还 「RISC-V64」。
    完。
    若 架构 等于 架构_WASM32：
        返还 「WASM32」。
    完。
    若 架构 等于 架构_WASM64：
        返还 「WASM64」。
    完。
    返还 「未知架构」。
完。

系统名 接受 系统：
    若 系统 等于 系统_WINDOWS：
        返还 「Windows」。
    完。
    若 系统 等于 系统_LINUX：
        返还 「Linux」。
    完。
    若 系统 等于 系统_MACOS：
        返还 「macOS」。
    完。
    若 系统 等于 系统_FREESTANDING：
        返还 「裸机」。
    完。
    返还 「未知系统」。
完。

类型名 接受 类型：
    若 类型 等于 类_空：
        返还 「空」。
    完。
    若 类型 等于 类_整数：
        返还 「整数」。
    完。
    若 类型 等于 类_浮点：
        返还 「浮点」。
    完。
    若 类型 等于 类_布尔：
        返还 「布尔」。
    完。
    若 类型 等于 类_字符串：
        返还 「字符串」。
    完。
    若 类型 等于 类_列表：
        返还 「列表」。
    完。
    若 类型 等于 类_字典：
        返还 「字典」。
    完。
    若 类型 等于 类_函数：
        返还 「函数」。
    完。
    若 类型 等于 类_结构：
        返还 「结构」。
    完。
    若 类型 等于 类_指针：
        返还 「指针」。
    完。
    若 类型 等于 类_数组：
        返还 「数组」。
    完。
    若 类型 等于 类_枚举：
        返还 「枚举」。
    完。
    若 类型 等于 类_接口：
        返还 「接口」。
    完。
    若 类型 等于 类_任意：
        返还 「任意」。
    完。
    返还 「未知类型」。
完。

错误名 接受 码：
    若 码 等于 错_无：
        返还 「无错误」。
    完。
    若 码 等于 错_语法：
        返还 「语法错误」。
    完。
    若 码 等于 错_类型：
        返还 「类型错误」。
    完。
    若 码 等于 错_未定义：
        返还 「undefined」。
    完。
    若 码 等于 错_重定义：
        返还 「重定义」。
    完。
    若 码 等于 错_参数数量：
        返还 「参数数量错误」。
    完。
    若 码 等于 错_类型不匹配：
        返还 「type mismatch」。
    完。
    若 码 等于 错_除零：
        返还 「除零」。
    完。
    若 码 等于 错_越界：
        返还 「out of range」。
    完。
    若 码 等于 错_空引用：
        返还 「空引用」。
    完。
    若 码 等于 错_栈溢出：
        返还 「stack overflow」。
    完。
    若 码 等于 错_内存不足：
        返还 「out of memory」。
    完。
    若 码 等于 错_IO：
        返还 「IO错误」。
    完。
    若 码 等于 错_断言失败：
        返还 「assertion failed」。
    完。
    若 码 等于 错_内部：
        返还 「internal error」。
    完。
    返还 「未知错误」。
完。

x86寄存器名 接受 编号：
    若 编号 等于 寄_RAX：
        返还 「rax」。
    完。
    若 编号 等于 寄_RCX：
        返还 「rcx」。
    完。
    若 编号 等于 寄_RDX：
        返还 「rdx」。
    完。
    若 编号 等于 寄_RBX：
        返还 「rbx」。
    完。
    若 编号 等于 寄_RSP：
        返还 「rsp」。
    完。
    若 编号 等于 寄_RBP：
        返还 「rbp」。
    完。
    若 编号 等于 寄_RSI：
        返还 「rsi」。
    完。
    若 编号 等于 寄_RDI：
        返还 「rdi」。
    完。
    若 编号 等于 寄_R8：
        返还 「r8」。
    完。
    若 编号 等于 寄_R9：
        返还 「r9」。
    完。
    若 编号 等于 寄_R10：
        返还 「r10」。
    完。
    若 编号 等于 寄_R11：
        返还 「r11」。
    完。
    若 编号 等于 寄_R12：
        返还 「r12」。
    完。
    若 编号 等于 寄_R13：
        返还 「r13」。
    完。
    若 编号 等于 寄_R14：
        返还 「r14」。
    完。
    若 编号 等于 寄_R15：
        返还 「r15」。
    完。
    返还 「未知寄存器」。
完。

可变 节点 为 空。

可变 结构体表 为 做 字典。

可变 字段注册表 为 做 字典。

可变 当前函数名 为 「__主程序__」。

可变 主体模式 为 0。

可变 修正表 为 一组：（空）。

可变 函数表 为 做 字典。

可变 变量表 为 做 字典。

可变 变量数 为 0。

可变 字串表 为 一组：（空）。

可变 字串偏移 为 一组：（空）。

可变 全局变量表 为 做 字典。

修正表_追加记录 接受 表、类型、位置、附加：
    做 追加 用 表、类型。
    做 追加 用 表、位置。
    做 追加 用 表、附加。
完。

修正表_项步长 接受 表、索引：
    若 做 是列表 用 表 等于 假 或者 索引 小于 0 或者 索引 大于或等于 做 计数 用 表：
        返还 1。
    完。
    若 做 类型 用 表 的 第 索引 项 等于 「列表」：
        返还 1。
    完。
    返还 3。
完。

修正表_取字段 接受 表、索引、字段偏移、默认值：
    若 做 是列表 用 表 等于 假 或者 索引 小于 0 或者 索引 大于或等于 做 计数 用 表：
        返还 默认值。
    完。
    若 做 类型 用 表 的 第 索引 项 等于 「列表」：
        若 做 计数 用 表 的 第 索引 项 大于 字段偏移：
            返还 表 的 第 索引 项 的 第 字段偏移 项。
        完。
        返还 默认值。
    完。
    实际索引 为 索引 加 字段偏移。
    若 实际索引 大于或等于 做 计数 用 表：
        返还 默认值。
    完。
    返还 表 的 第 实际索引 项。
完。

修正表_取类型 接受 表、索引：
    返还 做 修正表_取字段 用 表、索引、0、空。
完。

修正表_取位置 接受 表、索引：
    返还 做 修正表_取字段 用 表、索引、1、0。
完。

修正表_取附加 接受 表、索引：
    返还 做 修正表_取字段 用 表、索引、2、空。
完。

修正表_追加现成 接受 目标表、源表、索引：
    做 修正表_追加记录 用 目标表、做 修正表_取类型 用 源表、索引、做 修正表_取位置 用 源表、索引、做 修正表_取附加 用 源表、索引。
完。

可变 全局变量数 为 0。

可变 帧大小位 为 0。

可变 循环深度 为 0。

可变 循环头栈 为 一组：（空）。

可变 中断跳列栈 为 一组：（空）。

可变 继续目标栈 为 一组：（空）。

可变 未定义符号数 为 0。

可变 未定义符号表 为 做 字典。

可变 变量类型表 为 做 字典。

可变 函数参数类型表 为 做 字典。

可变 函数返回类型表 为 做 字典。

可变 _字符串驻留表 为 做 字典。

编译_重置代码生成状态 接受 重置全局变量、重置结构状态：
    做 代码_重置。
    修正表 为 一组：（空）。
    函数表 为 做 字典。
    变量表 为 做 字典。
    变量数 为 0。
    字串表 为 一组：（空）。
    字串偏移 为 一组：（空）。
    主体模式 为 0。
    当前函数名 为 「__主程序__」。
    帧大小位 为 0。
    循环深度 为 0。
    循环头栈 为 一组：（空）。
    中断跳列栈 为 一组：（空）。
    继续目标栈 为 一组：（空）。
    未定义符号数 为 0。
    未定义符号表 为 做 字典。
    变量类型表 为 做 字典。
    函数参数类型表 为 做 字典。
    函数返回类型表 为 做 字典。
    _字符串驻留表 为 做 字典。
    节点 为 空。
    若 重置全局变量：
        全局变量表 为 做 字典。
        全局变量数 为 0。
    完。
    若 重置结构状态：
        结构体表 为 做 字典。
        字段注册表 为 做 字典。
    完。
完。

作用域_全局 为 0。

作用域_函数 为 1。

作用域_块 为 2。

作用域_循环 为 3。

作用域_匹配 为 4。

可变性_不可变 为 0。

可变性_可变 为 1。

可变性_常量 为 2。

可变检_通过 为 0。

可变检_赋不可变 为 1。

可变检_赋常量 为 2。

可变检_改参数 为 3。

可变检_改字段 为 4。

可变检_未初始化 为 5。

使用类_变量 为 1。

使用类_函数 为 2。

使用类_参数 为 3。

使用类_导入 为 4。

警告级_信息 为 0。

警告级_警告 为 1。

警告级_错误 为 2。

类型_未知 为 0。

类型_整数 为 1。

类型_浮点 为 2。

类型_字符串 为 3。

类型_布尔 为 4。

类型_列表 为 5。

类型_字典 为 6。

类型_函数 为 7。

类型_空值 为 8。

类型_任意 为 9。

类型_无值 为 10。

类型_错误 为 11。

生命_未生 为 0。

生命_活跃 为 1。

生命_消亡 为 2。

生命_逃逸 为 3。

逃逸_无 为 0。

逃逸_返回值 为 1。

逃逸_闭包 为 2。

逃逸_全局 为 3。

逃逸_参数 为 4。

数制_十进制 为 0。

数制_十六进制 为 1。

数制_八进制 为 2。

数制_二进制 为 3。

数制_浮点 为 4。

数制_错误 为 5。

注释类_无 为 0。

注释类_单行 为 1。

注释类_多行 为 2。

注释类_文档 为 3。

串状_正常 为 0。

串状_未终止 为 1。

串状_无效转义 为 2。

串状_无效十六 为 3。

解析上限_形参 为 255。

解析上限_实参 为 255。

解析上限_局部 为 65536。

解析上限_深度 为 256。

可变 _常量_注册数 为 0。

可变 _常量_冲突数 为 0。

可变 _符号驻留池 为 做 字典。

可变 _符号驻留命中 为 0。

可变 _符号驻留总数 为 0。

符号驻留 接受 名：
    _符号驻留总数 为 _符号驻留总数 加 1。
    已有 为 做 字典取 用 _符号驻留池、名。
    若 已有 不等于 空：
        _符号驻留命中 为 _符号驻留命中 加 1。
        返还 已有。
    完。
    做 字典设 用 _符号驻留池、名、名。
    返还 名。
完。

符号驻留_重置 接受 零参：
    _符号驻留池 为 做 字典。
    _符号驻留命中 为 0。
    _符号驻留总数 为 0。
完。

符号驻留_统计 接受 零参：
    返还 一组：_符号驻留总数、_符号驻留命中、做 计数 用 做 字典键 用 _符号驻留池。
完。

修正表_符号去重 接受 表：
    可变 i 为 0。
    可变 去重数 为 0。
    只要 i 小于 做 计数 用 表：
        步长 为 做 修正表_项步长 用 表、i。
        若 步长 等于 3 并且 i 加 2 小于 做 计数 用 表：
            附加 为 表 的 第 i 加 2 项。
            若 附加 不等于 空 并且 做 是字符串 用 附加：
                表 的 第 i 加 2 项 为 做 符号驻留 用 附加。
                去重数 为 去重数 加 1。
            完。
        完。
        i 为 i 加 步长。
    完。
    返还 去重数。
完。

模块内容哈希 接受 代码列表：
    可变 哈希 为 5381。
    n 为 做 计数 用 代码列表。
    可变 i 为 0。
    只要 i 小于 n：
        哈希 为 (哈希 左移 5) 加 哈希 加 代码列表 的 第 i 项。
        哈希 为 哈希 位与 0x7FFFFFFF。
        i 为 i 加 1。
    完。
    返还 哈希。
完。

十六进制S形_1000 接受 x_1000：
    若 x_1000 小于 负 4000：
        返还 18。
    完。
    若 x_1000 小于 负 2000：
        返还 119。
    完。
    若 x_1000 小于 0：
        返还 270 加 (x_1000 加 2000) 乘 230 除以 2000。
    完。
    若 x_1000 小于 2000：
        返还 500 加 x_1000 乘 230 除以 2000。
    完。
    若 x_1000 小于 4000：
        返还 881 加 (x_1000 减 2000) 乘 101 除以 2000。
    完。
    返还 982。
完。

本章 收束。
