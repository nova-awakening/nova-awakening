《SSA构建》

引用《..·..·..·标准库·基础》。

SSA_整数常量 为 1。

SSA_字符串常量 为 2。

SSA_参数 为 3。

SSA_指令结果 为 4。

SSA_φ函数 为 5。

SSA_加 为 10。

SSA_减 为 11。

SSA_乘 为 12。

SSA_除 为 13。

SSA_余 为 14。

SSA_等于 为 20。

SSA_不等 为 21。

SSA_小于 为 22。

SSA_大等 为 23。

SSA_且 为 30。

SSA_或 为 31。

SSA_非 为 32。

SSA_左移 为 40。

SSA_右移 为 41。

SSA_位与 为 42。

SSA_位或 为 43。

SSA_载入 为 50。

SSA_存储 为 51。

SSA_调用 为 60。

SSA_返回 为 61。

SSA_跳转 为 70。

SSA_条件跳 为 71。

SSA_φ 为 80。

SSA_创建值 接受 类型、数据、版本：
    返还 一组：类型、数据、版本。
完。

SSA_创建指令 接受 操作码、结果、操作数、块号：
    返还 一组：操作码、结果、操作数、块号。
完。

SSA_创建块 接受 标签：
    返还 一组：标签、一组：（空）、一组：（空）、一组：（空）。
完。

SSA_块_添加指令 接受 块、指令：
    做 追加 用 块 的 第 1 项、指令。
完。

SSA_块_添加前驱 接受 块、前驱标签：
    做 追加 用 块 的 第 2 项、前驱标签。
完。

SSA_块_添加后继 接受 块、后继标签：
    做 追加 用 块 的 第 3 项、后继标签。
完。

可变 SSA_块表 为 一组：（空）。

可变 SSA_块索引 为 做 字典。

可变 SSA_下一版本 为 做 字典。

SSA_注册块 接受 标签：
    块 为 做 SSA_创建块 用 标签。
    位置 为 做 计数 用 SSA_块表。
    做 追加 用 SSA_块表、块。
    做 字典设 用 SSA_块索引、标签、位置。
    返还 位置。
完。

SSA_新版本_命名 接受 变量名：
    若 做 字典有 用 SSA_下一版本、变量名 等于 假：
        做 字典设 用 SSA_下一版本、变量名、0。
    完。
    版本 为 做 字典取 用 SSA_下一版本、变量名。
    做 字典设 用 SSA_下一版本、变量名、版本 加 1。
    返还 版本。
完。

SSA_从AST构建 接受 _函数节点：
    SSA_块表 为 一组：（空）。
    SSA_块索引 为 做 字典。
    SSA_下一版本 为 做 字典。
    做 SSA_注册块 用 「入口」。
    做 打印 用 「SSA构建: 」 加 做 转字符串 用 做 计数 用 SSA_块表 加 「 个基本块\n」。
    返还 SSA_块表。
完。

SSA_插入φ函数 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 SSA_块表：
        块 为 SSA_块表 的 第 i 项。
        若 做 计数 用 块 的 第 2 项 大于 1：
            做 打印 用 「  块」 加 做 转字符串 用 i 加 「: 」 加 做 转字符串 用 做 计数 用 块 的 第 2 项 加 「个前驱 → φ候选\n」。
        完。
        i 为 i 加 1。
    完。
完。

SSA_常量传播 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 SSA_块表：
        指令列 为 SSA_块表 的 第 i 项 的 第 1 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 指令列：
            指令 为 指令列 的 第 j 项。
            若 指令 的 第 0 项 等于 SSA_φ：
                输入 为 指令 的 第 2 项。
                若 做 计数 用 输入 大于 0：
                    可变 全相同 为 真。
                    首值 为 输入 的 第 0 项。
                    可变 k 为 1。
                    只要 k 小于 做 计数 用 输入：
                        若 输入 的 第 k 项 不等于 首值：
                            全相同 为 假。
                        完。
                        k 为 k 加 1。
                    完。
                    若 全相同：
                        替换数 为 替换数 加 1。
                    完。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    做 打印 用 「常量传播: 替换 」 加 做 转字符串 用 替换数 加 「 个φ函数\n」。
    返还 替换数。
完。

SSA_死代码消除 接受 零参：
    做 打印 用 「死代码消除: 移除 」 加 做 转字符串 用 移除数 加 「 条指令\n」。
    返还 移除数。
完。

可变 _支配_idom 为 一组：（空）。

支配树_初始化 接受 块数：
    _支配_idom 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 块数：
        做 追加 用 _支配_idom、负 1。
        i 为 i 加 1。
    完。
    _支配_idom 的 第 0 项 为 0。
完。

支配树_设 接受 块、支配者：
    若 块 大于或等于 0 并且 块 小于 做 计数 用 _支配_idom：
        _支配_idom 的 第 块 项 为 支配者。
    完。
完。

支配树_查 接受 块：
    若 块 大于或等于 0 并且 块 小于 做 计数 用 _支配_idom：
        返还 _支配_idom 的 第 块 项。
    完。
    返还 负 1。
完。

支配_是否支配 接受 a、b：
    可变 当前 为 b。
    只要 当前 大于或等于 0：
        若 当前 等于 a：
            返还 真。
        完。
        若 _支配_idom 的 第 当前 项 等于 当前：
            返还 假。
        完。
        当前 为 _支配_idom 的 第 当前 项。
    完。
    返还 假。
完。

支配_求交 接受 a、b：
    可变 f1 为 a。
    可变 f2 为 b。
    只要 f1 不等于 f2：
        只要 f1 小于 f2：
            f1 为 _支配_idom 的 第 f1 项。
        完。
        只要 f2 小于 f1：
            f2 为 _支配_idom 的 第 f2 项。
        完。
    完。
    返还 f1。
完。

SSA_更新支配边界 接受 边界列表、运行者、目标：
    只要 运行者 不等于 _支配_idom 的 第 目标 项：
        可变 已有 为 假。
        可变 k 为 0。
        只要 k 小于 做 计数 用 边界列表 的 第 运行者 项：
            若 边界列表 的 第 运行者 项 的 第 k 项 等于 目标：
                已有 为 真。
            完。
            k 为 k 加 1。
        完。
        若 非 已有：
            做 追加 用 边界列表 的 第 运行者 项、目标。
        完。
        运行者 为 _支配_idom 的 第 运行者 项。
    完。
完。

支配边界_计算 接受 块数：
    可变 边界列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 块数：
        做 追加 用 边界列表、一组：（空）。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 块数：
        若 i 大于或等于 做 计数 用 SSA_块表：
            i 为 i 加 1。
            续。
        完。
        前驱 为 SSA_块表 的 第 i 项 的 第 2 项。
        若 做 计数 用 前驱 大于或等于 2：
            可变 j 为 0。
            只要 j 小于 做 计数 用 前驱：
                做 SSA_更新支配边界 用 边界列表、前驱 的 第 j 项、i。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 边界列表。
完。

SSA_活跃变量 接受 块索引：
    若 块索引 小于 0 或者 块索引 大于或等于 做 计数 用 SSA_块表：
        返还 一组：（空）。
    完。
    可变 活跃 为 一组：（空）。
    指令列 为 SSA_块表 的 第 块索引 项 的 第 1 项。
    可变 i 为 做 计数 用 指令列 减 1。
    只要 i 大于或等于 0：
        指令 为 指令列 的 第 i 项。
        若 做 计数 用 指令 大于 2：
            操作数 为 指令 的 第 2 项。
            可变 j 为 0。
            只要 j 小于 做 计数 用 操作数：
                可变 已有 为 假。
                可变 k 为 0。
                只要 k 小于 做 计数 用 活跃：
                    若 活跃 的 第 k 项 等于 操作数 的 第 j 项：
                        已有 为 真。
                    完。
                    k 为 k 加 1。
                完。
                若 非 已有：
                    做 追加 用 活跃、操作数 的 第 j 项。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 减 1。
    完。
    返还 活跃。
完。

SSA_常量折叠 接受 零参：
    可变 折叠数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 SSA_块表：
        指令列 为 SSA_块表 的 第 i 项 的 第 1 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 指令列：
            指令 为 指令列 的 第 j 项。
            op 为 指令 的 第 0 项。
            若 做 计数 用 指令 大于 2 并且 做 计数 用 指令 的 第 2 项 大于或等于 2：
                操作数 为 指令 的 第 2 项。
                左 为 操作数 的 第 0 项。
                右 为 操作数 的 第 1 项。
                若 做 计数 用 左 大于或等于 2 并且 左 的 第 0 项 等于 SSA_整数常量 并且 做 计数 用 右 大于或等于 2 并且 右 的 第 0 项 等于 SSA_整数常量：
                    左值 为 左 的 第 1 项。
                    右值 为 右 的 第 1 项。
                    可变 结果 为 负 1。
                    可变 可折叠 为 真。
                    若 op 等于 SSA_加：
                        结果 为 左值 加 右值。
                    否则若 op 等于 SSA_减：
                        结果 为 左值 减 右值。
                    否则若 op 等于 SSA_乘：
                        结果 为 左值 乘 右值。
                    否则若 op 等于 SSA_除 并且 右值 不等于 0：
                        结果 为 左值 除以 右值。
                    否则若 op 等于 SSA_余 并且 右值 不等于 0：
                        结果 为 左值 取余于 右值。
                    否则若 op 等于 SSA_左移：
                        结果 为 左值 乘 (1 左移 右值)。
                    否则若 op 等于 SSA_位与：
                        结果 为 左值 位与 右值。
                    否则若 op 等于 SSA_位或：
                        结果 为 左值 位或 右值。
                    否则若 op 等于 SSA_等于：
                        若 左值 等于 右值：
                            结果 为 1。
                        否则：
                            结果 为 0。
                        完。
                    否则若 op 等于 SSA_不等：
                        若 左值 不等于 右值：
                            结果 为 1。
                        否则：
                            结果 为 0。
                        完。
                    否则若 op 等于 SSA_小于：
                        若 左值 小于 右值：
                            结果 为 1。
                        否则：
                            结果 为 0。
                        完。
                    否则：
                        可折叠 为 假。
                    完。
                    若 可折叠：
                        指令列 的 第 j 项 为 做 SSA_创建指令 用 SSA_加、做 SSA_创建值 用 SSA_整数常量、结果、0、一组：一组：SSA_整数常量、结果、一组：SSA_整数常量、0、i。
                        折叠数 为 折叠数 加 1。
                    完。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 折叠数。
完。

SSA_强度削弱 接受 零参：
    可变 削弱数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 SSA_块表：
        指令列 为 SSA_块表 的 第 i 项 的 第 1 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 指令列：
            指令 为 指令列 的 第 j 项。
            若 做 计数 用 指令 大于 2 并且 做 计数 用 指令 的 第 2 项 大于或等于 2：
                op 为 指令 的 第 0 项。
                右 为 指令 的 第 2 项 的 第 1 项。
                若 op 等于 SSA_乘 并且 做 计数 用 右 大于或等于 2 并且 右 的 第 0 项 等于 SSA_整数常量：
                    值 为 右 的 第 1 项。
                    若 值 等于 2：
                        指令 的 第 0 项 为 SSA_左移。
                        指令 的 第 2 项 的 第 1 项 为 一组：SSA_整数常量、1。
                        削弱数 为 削弱数 加 1。
                    否则若 值 等于 4：
                        指令 的 第 0 项 为 SSA_左移。
                        指令 的 第 2 项 的 第 1 项 为 一组：SSA_整数常量、2。
                        削弱数 为 削弱数 加 1。
                    否则若 值 等于 8：
                        指令 的 第 0 项 为 SSA_左移。
                        指令 的 第 2 项 的 第 1 项 为 一组：SSA_整数常量、3。
                        削弱数 为 削弱数 加 1。
                    否则若 值 等于 16：
                        指令 的 第 0 项 为 SSA_左移。
                        指令 的 第 2 项 的 第 1 项 为 一组：SSA_整数常量、4。
                        削弱数 为 削弱数 加 1。
                    完。
                完。
                若 op 等于 SSA_除 并且 做 计数 用 右 大于或等于 2 并且 右 的 第 0 项 等于 SSA_整数常量：
                    值d 为 右 的 第 1 项。
                    若 值d 等于 2：
                        指令 的 第 0 项 为 SSA_右移。
                        指令 的 第 2 项 的 第 1 项 为 一组：SSA_整数常量、1。
                        削弱数 为 削弱数 加 1。
                    否则若 值d 等于 4：
                        指令 的 第 0 项 为 SSA_右移。
                        指令 的 第 2 项 的 第 1 项 为 一组：SSA_整数常量、2。
                        削弱数 为 削弱数 加 1。
                    否则若 值d 等于 8：
                        指令 的 第 0 项 为 SSA_右移。
                        指令 的 第 2 项 的 第 1 项 为 一组：SSA_整数常量、3。
                        削弱数 为 削弱数 加 1。
                    完。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 削弱数。
完。

可变 _拷贝源 为 一组：（空）。

可变 _拷贝目标 为 一组：（空）。

SSA_拷贝传播_记录 接受 原始、等价：
    做 追加 用 _拷贝源、原始。
    做 追加 用 _拷贝目标、等价。
完。

SSA_拷贝传播_查 接受 名称：
    可变 当前 为 名称。
    可变 深度 为 0。
    只要 深度 小于 100：
        可变 找到 为 假。
        可变 i 为 0。
        只要 i 小于 做 计数 用 _拷贝源：
            若 _拷贝源 的 第 i 项 等于 当前：
                当前 为 _拷贝目标 的 第 i 项。
                找到 为 真。
            完。
            i 为 i 加 1。
        完。
        若 非 找到：
            返还 当前。
        完。
        深度 为 深度 加 1。
    完。
    返还 当前。
完。

SSA_拷贝传播 接受 零参：
    可变 传播数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 SSA_块表：
        指令列 为 SSA_块表 的 第 i 项 的 第 1 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 指令列：
            指令 为 指令列 的 第 j 项。
            若 指令 的 第 0 项 等于 SSA_φ 并且 做 计数 用 指令 的 第 2 项 大于或等于 2：
                输入 为 指令 的 第 2 项。
                可变 k 为 1。
                只要 k 小于 做 计数 用 输入：
                    若 输入 的 第 k 项 不等于 输入 的 第 0 项：
                        全同 为 假。
                    完。
                    k 为 k 加 1。
                完。
                若 全同：
                    做 SSA_拷贝传播_记录 用 指令 的 第 1 项、输入 的 第 0 项。
                    传播数 为 传播数 加 1。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 传播数。
完。

可变 _CSE_表达式 为 一组：（空）。

_CSE_哈希 接受 操作码、操作数：
    可变 h 为 操作码 乘 10007。
    可变 i 为 0。
    只要 i 小于 做 计数 用 操作数：
        若 做 计数 用 操作数 的 第 i 项 大于或等于 2：
            h 为 h 加 (i 加 1) 乘 31 乘 操作数 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 h。
完。

SSA_CSE_查 接受 操作码、操作数：
    h 为 做 _CSE_哈希 用 操作码、操作数。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _CSE_表达式：
        若 _CSE_表达式 的 第 i 项 的 第 0 项 等于 操作码 并且 _CSE_表达式 的 第 i 项 的 第 1 项 等于 h：
            返还 _CSE_表达式 的 第 i 项 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

SSA_CSE_注册 接受 操作码、操作数、结果：
    h 为 做 _CSE_哈希 用 操作码、操作数。
    做 追加 用 _CSE_表达式、一组：操作码、h、结果。
完。

SSA_公共子表达式消除 接受 零参：
    可变 消除数 为 0。
    _CSE_表达式 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 SSA_块表：
        指令列 为 SSA_块表 的 第 i 项 的 第 1 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 指令列：
            指令 为 指令列 的 第 j 项。
            op 为 指令 的 第 0 项。
            若 op 大于或等于 SSA_加 并且 op 小于或等于 SSA_位或：
                已有 为 做 SSA_CSE_查 用 op、指令 的 第 2 项。
                若 已有 不等于 空：
                    做 SSA_拷贝传播_记录 用 指令 的 第 1 项、已有。
                    消除数 为 消除数 加 1。
                否则：
                    做 SSA_CSE_注册 用 op、指令 的 第 2 项、指令 的 第 1 项。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 消除数。
完。

SSA_检测循环 接受 块数：
    可变 回边列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 块数 并且 i 小于 做 计数 用 SSA_块表：
        后继 为 SSA_块表 的 第 i 项 的 第 3 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 后继：
            目标 为 后继 的 第 j 项。
            若 目标 小于或等于 i：
                做 追加 用 回边列表、一组：i、目标。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 回边列表。
完。

SSA_是循环不变 接受 指令、循环块集：
    若 指令 的 第 0 项 等于 SSA_载入 或者 指令 的 第 0 项 等于 SSA_存储 或者 指令 的 第 0 项 等于 SSA_调用：
        返还 假。
    完。
    若 做 计数 用 指令 小于或等于 2：
        返还 假。
    完。
    操作数 为 指令 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 操作数：
        op 为 操作数 的 第 i 项。
        若 做 计数 用 op 大于或等于 3：
            定义块 为 op 的 第 2 项。
            可变 j 为 0。
            只要 j 小于 做 计数 用 循环块集：
                若 循环块集 的 第 j 项 等于 定义块：
                    返还 假。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

SSA_循环不变外提 接受 零参：
    回边 为 做 SSA_检测循环 用 做 计数 用 SSA_块表。
    可变 外提数 为 0。
    可变 r 为 0。
    只要 r 小于 做 计数 用 回边：
        循环头 为 回边 的 第 r 项 的 第 1 项。
        循环尾 为 回边 的 第 r 项 的 第 0 项。
        可变 循环块 为 一组：（空）。
        可变 b 为 循环头。
        只要 b 小于或等于 循环尾：
            做 追加 用 循环块、b。
            b 为 b 加 1。
        完。
        可变 bi 为 0。
        只要 bi 小于 做 计数 用 循环块：
            块号 为 循环块 的 第 bi 项。
            若 块号 小于 做 计数 用 SSA_块表：
                指令列 为 SSA_块表 的 第 块号 项 的 第 1 项。
                可变 j 为 0。
                只要 j 小于 做 计数 用 指令列：
                    若 做 SSA_是循环不变 用 指令列 的 第 j 项、循环块：
                        外提数 为 外提数 加 1。
                    完。
                    j 为 j 加 1。
                完。
            完。
            bi 为 bi 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 外提数。
完。

SSA_归纳变量检测 接受 循环头、循环尾：
    可变 归纳列表 为 一组：（空）。
    可变 b 为 循环头。
    只要 b 小于或等于 循环尾 并且 b 小于 做 计数 用 SSA_块表：
        指令列 为 SSA_块表 的 第 b 项 的 第 1 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 指令列：
            指令 为 指令列 的 第 j 项。
            若 指令 的 第 0 项 等于 SSA_加 并且 做 计数 用 指令 的 第 2 项 大于或等于 2：
                右 为 指令 的 第 2 项 的 第 1 项。
                若 做 计数 用 右 大于或等于 2 并且 右 的 第 0 项 等于 SSA_整数常量：
                    做 追加 用 归纳列表、一组：指令 的 第 1 项、右 的 第 1 项、b。
                完。
            完。
            j 为 j 加 1。
        完。
        b 为 b 加 1。
    完。
    返还 归纳列表。
完。

SSA_X86_寄存器数 为 14。

SSA_ARM64_寄存器数 为 28。

SSA_RISCV_寄存器数 为 28。

SSA_活跃区间 接受 值ID、块列表：
    可变 起始 为 999999。
    可变 结束 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 块列表：
        块 为 块列表 的 第 i 项。
        若 块 小于 做 计数 用 SSA_块表：
            指令列 为 SSA_块表 的 第 块 项 的 第 1 项。
            可变 j 为 0。
            只要 j 小于 做 计数 用 指令列：
                指令 为 指令列 的 第 j 项。
                若 指令 的 第 1 项 等于 值ID：
                    若 块 小于 起始：
                        起始 为 块。
                    完。
                完。
                若 做 计数 用 指令 大于 2：
                    可变 k 为 0。
                    只要 k 小于 做 计数 用 指令 的 第 2 项：
                        若 指令 的 第 2 项 的 第 k 项 等于 值ID：
                            若 块 大于 结束：
                                结束 为 块。
                            完。
                        完。
                        k 为 k 加 1。
                    完。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 一组：起始、结束。
完。

SSA_线性扫描分配 接受 活跃区间列表、寄存器数：
    可变 新活跃 为 一组：（空）。
    可变 溢出数 为 0。
    可变 分配 为 一组：（空）。
    可变 活跃 为 一组：（空）。
    可变 下一寄存器 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 活跃区间列表：
        区间 为 活跃区间列表 的 第 i 项。
        起始 为 区间 的 第 0 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 活跃：
            若 活跃 的 第 j 项 的 第 2 项 大于或等于 起始：
                做 追加 用 新活跃、活跃 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        活跃 为 新活跃。
        若 做 计数 用 活跃 小于 寄存器数：
            寄存器 为 下一寄存器 取余于 寄存器数。
            下一寄存器 为 下一寄存器 加 1。
            做 追加 用 分配、一组：i、寄存器。
            做 追加 用 活跃、一组：i、寄存器、区间 的 第 1 项。
        否则：
            做 追加 用 分配、一组：i、负 1。
            溢出数 为 溢出数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：分配、溢出数。
完。

SSA_验证 接受 零参：
    可变 定义集名 为 一组：（空）。
    可变 定义集块 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 SSA_块表：
        指令列 为 SSA_块表 的 第 i 项 的 第 1 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 指令列：
            指令 为 指令列 的 第 j 项。
            结果 为 指令 的 第 1 项。
            可变 k 为 0。
            只要 k 小于 做 计数 用 定义集名：
                若 定义集名 的 第 k 项 等于 结果：
                    错误数 为 错误数 加 1。
                完。
                k 为 k 加 1。
            完。
            做 追加 用 定义集名、结果。
            做 追加 用 定义集块、i。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 SSA_块表：
        块 为 SSA_块表 的 第 i 项。
        前驱数 为 做 计数 用 块 的 第 2 项。
        指令列2 为 块 的 第 1 项。
        可变 j2 为 0。
        只要 j2 小于 做 计数 用 指令列2：
            若 指令列2 的 第 j2 项 的 第 0 项 等于 SSA_φ：
                若 做 计数 用 指令列2 的 第 j2 项 的 第 2 项 不等于 前驱数：
                    错误数 为 错误数 加 1。
                完。
            完。
            j2 为 j2 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 错误数。
完。

SSA_优化流水线 接受 遍数：
    可变 总折叠 为 0。
    可变 总传播 为 0。
    可变 总消除 为 0。
    可变 总削弱 为 0。
    可变 总外提 为 0。
    可变 总CSE 为 0。
    可变 p 为 0。
    只要 p 小于 遍数：
        总折叠 为 总折叠 加 做 SSA_常量折叠。
        总传播 为 总传播 加 做 SSA_常量传播。
        总消除 为 总消除 加 做 SSA_死代码消除。
        总削弱 为 总削弱 加 做 SSA_强度削弱。
        总传播 为 总传播 加 做 SSA_拷贝传播。
        总CSE 为 总CSE 加 做 SSA_公共子表达式消除。
        总外提 为 总外提 加 做 SSA_循环不变外提。
        p 为 p 加 1。
    完。
    返还 一组：总折叠、总传播、总消除、总削弱、总外提、总CSE。
完。

SSA_低化_X86 为 1。

SSA_低化_ARM64 为 2。

SSA_低化_RISCV 为 3。

SSA_低化 接受 目标架构：
    可变 伪指令 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 SSA_块表：
        指令列 为 SSA_块表 的 第 i 项 的 第 1 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 指令列：
            指令 为 指令列 的 第 j 项。
            做 追加 用 伪指令、一组：目标架构、指令 的 第 0 项、指令 的 第 1 项、指令 的 第 2 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 伪指令。
完。

SSA_打印块 接受 块索引：
    若 块索引 小于 0 或者 块索引 大于或等于 做 计数 用 SSA_块表：
        返还 空。
    完。
    块 为 SSA_块表 的 第 块索引 项。
    做 打印 用 「块 」 加 块 的 第 0 项 加 「 (前驱:」 加 做 转字符串 用 做 计数 用 块 的 第 2 项 加 「 后继:」 加 做 转字符串 用 做 计数 用 块 的 第 3 项 加 「)\n」。
    可变 j 为 0。
    只要 j 小于 做 计数 用 块 的 第 1 项：
        指令 为 块 的 第 1 项 的 第 j 项。
        做 打印 用 「  」 加 做 转字符串 用 指令 的 第 0 项 加 「 → 」 加 做 转字符串 用 指令 的 第 1 项 加 「\n」。
        j 为 j 加 1。
    完。
完。

SSA_打印全部 接受 零参：
    做 打印 用 「═══ SSA IR ═══ 」 加 做 转字符串 用 做 计数 用 SSA_块表 加 「 个基本块\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 SSA_块表：
        做 SSA_打印块 用 i。
        i 为 i 加 1。
    完。
完。

SSA_统计信息 接受 零参：
    可变 总指令 为 0。
    可变 φ数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 SSA_块表：
        指令列 为 SSA_块表 的 第 i 项 的 第 1 项。
        总指令 为 总指令 加 做 计数 用 指令列。
        可变 j 为 0。
        只要 j 小于 做 计数 用 指令列：
            若 指令列 的 第 j 项 的 第 0 项 等于 SSA_φ：
                φ数 为 φ数 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：做 计数 用 SSA_块表、总指令、φ数。
完。

可变 _SSA_φ插入数 为 0。

可变 _SSA_版本数 为 0。

可变 _SSA_消除数 为 0。

SSA_记录φ 接受 零参：
    _SSA_φ插入数 为 _SSA_φ插入数 加 1。
完。

SSA_记录版本 接受 零参：
    _SSA_版本数 为 _SSA_版本数 加 1。
完。

SSA_记录消除 接受 零参：
    _SSA_消除数 为 _SSA_消除数 加 1。
完。

本章 收束。
