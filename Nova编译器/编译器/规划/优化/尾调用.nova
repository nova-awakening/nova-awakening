《尾调用》

引用《..·..·..·标准库·基础》。

尾调_互递归 为 3。

在尾位置 接受 _语句类型、是最后、在返回中：
    若 在返回中 等于 1：
        若 是最后 等于 1：
            返还 1。
        完。
    完。
    返还 0。
完。

检测尾调用 接受 当前函数、被调函数、尾位置：
    若 尾位置 等于 0：
        返还 尾调_无。
    完。
    若 当前函数 等于 被调函数：
        返还 尾调_自递归。
    完。
    返还 尾调_其他。
完。

尾递归信息 接受 函数ID、参数数：
    返还 一组：函数ID、参数数。
完。

尾递归_标记可转 接受 信息：
    信息 的 第 2 项 为 1。
完。

尾递归_增计数 接受 信息：
    信息 的 第 3 项 为 信息 的 第 3 项 加 1。
完。

尾递归_可转换 接受 信息：
    返还 信息 的 第 2 项。
完。

尾递归_次数 接受 信息：
    返还 信息 的 第 3 项。
完。

分析尾部 接受 语句列表、当前函数、最后调用目标：
    长 为 做 计数 用 语句列表。
    若 长 等于 0：
        返还 尾调_无。
    完。
    最后 为 语句列表 的 第 长 减 1 项。
    若 最后 等于 3：
        若 最后调用目标 大于 0：
            若 最后调用目标 等于 当前函数：
                返还 尾调_自递归。
            完。
            返还 尾调_其他。
        完。
    完。
    返还 尾调_无。
完。

TCO统计_创建 接受 零参：
    返还 一组：0。
完。

TCO_添加递归 接受 stats：
    stats 的 第 0 项 为 stats 的 第 0 项 加 1。
完。

TCO_添加调用 接受 stats：
    stats 的 第 1 项 为 stats 的 第 1 项 加 1。
完。

TCO_尾递归数 接受 stats：
    返还 stats 的 第 0 项。
完。

TCO_尾调用数 接受 stats：
    返还 stats 的 第 1 项。
完。

栈帧节省 接受 尾递归数、平均调用深度：
    返还 尾递归数 乘 平均调用深度 乘 8。
完。

TCO安全 接受 有异常处理、有闭包：
    若 有异常处理 等于 1：
        返还 0。
    完。
    若 有闭包 等于 1：
        返还 0。
    完。
    返还 1。
完。

检测互递归_规划 接受 调用图、函数数：
    可变 组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 函数数：
        可变 j 为 i 加 1。
        只要 j 小于 函数数：
            若 调用图 的 第 i 项 的 第 j 项 大于 0 并且 调用图 的 第 j 项 的 第 i 项 大于 0：
                做 追加 用 组、一组：i、j。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 组。
完。

TCO转换_创建 接受 函数ID、参数数、位置列表：
    返还 一组：函数ID、参数数、位置列表。
完。

TCO转换_执行 接受 信息：
    可变 转换数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 信息 的 第 2 项：
        转换数 为 转换数 加 1。
        i 为 i 加 1。
    完。
    信息 的 第 3 项 为 转换数。
    信息 的 第 4 项 为 转换数 乘 64。
    返还 转换数。
完。

检测累积器 接受 参数变化：
    n 为 做 计数 用 参数变化。
    若 n 小于 2：
        返还 0。
    完。
    可变 递减 为 负 1。
    可变 累积 为 负 1。
    可变 i 为 0。
    只要 i 小于 n：
        若 参数变化 的 第 i 项 等于 负 1：
            递减 为 i。
        完。
        若 参数变化 的 第 i 项 大于 0：
            累积 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 递减 大于或等于 0 并且 累积 大于或等于 0。
完。

TCO报告 接受 stats、转换列表：
    可变 总转换 为 0。
    可变 总节省 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 转换列表：
        总转换 为 总转换 加 转换列表 的 第 i 项 的 第 3 项。
        总节省 为 总节省 加 转换列表 的 第 i 项 的 第 4 项。
        i 为 i 加 1。
    完。
    返还 一组：做 TCO_尾递归数 用 stats、做 TCO_尾调用数 用 stats、总转换、总节省。
完。

TCO_是尾位置 接受 指令列表、位置：
    若 位置 大于或等于 做 计数 用 指令列表 减 1：
        返还 假。
    完。
    下一条 为 指令列表 的 第 位置 加 1 项。
    若 下一条 的 第 0 项 等于 0x0F：
        返还 真。
    完。
    返还 假。
完。

TCO_扫描尾调用 接受 指令列表：
    可变 尾调用列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 指令列表：
        指令 为 指令列表 的 第 i 项。
        若 指令 的 第 0 项 等于 0x10：
            若 做 TCO_是尾位置 用 指令列表、i：
                做 追加 用 尾调用列表、一组：i、指令 的 第 1 项、指令 的 第 2 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 尾调用列表。
完。

TCO_是自递归 接受 函数名、调用目标：
    返还 函数名 等于 调用目标。
完。

TCO_扫描自递归 接受 函数名、指令列表：
    可变 自递归列表 为 一组：（空）。
    尾调用 为 做 TCO_扫描尾调用 用 指令列表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 尾调用：
        若 做 TCO_是自递归 用 函数名、尾调用 的 第 i 项 的 第 1 项：
            做 追加 用 自递归列表、尾调用 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 自递归列表。
完。

TCO_递归转循环 接受 参数列表、_函数体长度：
    可变 转换指令 为 一组：（空）。
    做 追加 用 转换指令、一组：0xF0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数列表：
        做 追加 用 转换指令、一组：0x20、i、参数列表 的 第 i 项。
        i 为 i 加 1。
    完。
    做 追加 用 转换指令、一组：0xF1。
    返还 转换指令。
完。

可变 _TCO_调用图 为 一组：（空）。

TCO_记录调用 接受 调用者、被调用者：
    做 追加 用 _TCO_调用图、一组：调用者、被调用者。
完。

TCO_获取被调用 接受 函数名：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _TCO_调用图：
        若 _TCO_调用图 的 第 i 项 的 第 0 项 等于 函数名：
            做 追加 用 结果、_TCO_调用图 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

TCO_检测互递归 接受 函数A、函数B：
    A调用 为 做 TCO_获取被调用 用 函数A。
    可变 i 为 0。
    只要 i 小于 做 计数 用 A调用：
        若 A调用 的 第 i 项 等于 函数B：
            B调用 为 做 TCO_获取被调用 用 函数B。
            可变 j 为 0。
            只要 j 小于 做 计数 用 B调用：
                若 B调用 的 第 j 项 等于 函数A：
                    返还 真。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

TCO_蹦床_包装 接受 函数ID、参数：
    返还 一组：1、函数ID、参数。
完。

TCO_蹦床_是延迟 接受 值：
    若 做 计数 用 值 大于或等于 1 并且 值 的 第 0 项 等于 1：
        返还 真。
    完。
    返还 假。
完。

TCO_蹦床_执行 接受 初始值、最大轮数：
    可变 当前 为 初始值。
    可变 轮 为 0。
    只要 做 TCO_蹦床_是延迟 用 当前 并且 轮 小于 最大轮数：
        当前 为 一组：0、当前 的 第 1 项、当前 的 第 2 项。
        轮 为 轮 加 1。
    完。
    返还 一组：当前、轮。
完。

可变 _TCO_栈深度历史 为 一组：（空）。

TCO_记录栈深度 接受 深度：
    可变 新历史 为 一组：（空）。
    做 追加 用 _TCO_栈深度历史、深度。
    若 做 计数 用 _TCO_栈深度历史 大于 1000：
        可变 i 为 500。
        只要 i 小于 做 计数 用 _TCO_栈深度历史：
            做 追加 用 新历史、_TCO_栈深度历史 的 第 i 项。
            i 为 i 加 1。
        完。
        _TCO_栈深度历史 为 新历史。
    完。
完。

TCO_最大栈深度 接受 零参：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _TCO_栈深度历史：
        若 _TCO_栈深度历史 的 第 i 项 大于 最大：
            最大 为 _TCO_栈深度历史 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

TCO_平均栈深度 接受 零参：
    若 做 计数 用 _TCO_栈深度历史 等于 0：
        返还 0。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _TCO_栈深度历史：
        总 为 总 加 _TCO_栈深度历史 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总 除以 做 计数 用 _TCO_栈深度历史。
完。

可变 _TCO_检测次数 为 0。

可变 _TCO_转换次数 为 0。

可变 _TCO_节省帧数 为 0。

TCO_记录检测 接受 零参：
    _TCO_检测次数 为 _TCO_检测次数 加 1。
完。

TCO_记录转换 接受 零参：
    _TCO_转换次数 为 _TCO_转换次数 加 1。
完。

TCO_记录节省 接受 帧数：
    _TCO_节省帧数 为 _TCO_节省帧数 加 帧数。
完。

本章 收束。
