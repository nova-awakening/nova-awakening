《类型推断》

引用《..·..·标准库·基础》。

引用《..·..·编译器·标记常量》。

型_未知 为 0。

型_整数 为 1。

型_浮点 为 2。

型_字符串 为 3。

型_布尔 为 4。

型_列表 为 5。

型_字典 为 6。

型_函数 为 7。

型_空値 为 8。

型_任意 为 9。

型_无値 为 10。

型_错误 为 11。

类型推断_类型宽度 接受 t：
    若 t 等于 型_整数：
        返还 8。
    完。
    若 t 等于 型_浮点：
        返还 8。
    完。
    若 t 等于 型_布尔：
        返还 1。
    完。
    若 t 等于 型_字符串：
        返还 8。
    完。
    若 t 等于 型_列表：
        返还 8。
    完。
    若 t 等于 型_字典：
        返还 8。
    完。
    若 t 等于 型_空値：
        返还 0。
    完。
    返还 8。
完。

类型推断_类型兼容 接受 a、b：
    若 a 等于 b：
        返还 1。
    完。
    若 a 等于 型_任意：
        返还 1。
    完。
    若 b 等于 型_任意：
        返还 1。
    完。
    若 a 等于 型_未知：
        返还 1。
    完。
    若 b 等于 型_未知：
        返还 1。
    完。
    若 a 等于 型_整数：
        若 b 等于 型_浮点：
            返还 1。
        完。
    完。
    若 a 等于 型_浮点：
        若 b 等于 型_整数：
            返还 1。
        完。
    完。
    若 b 等于 型_空値：
        若 a 等于 型_字符串：
            返还 1。
        完。
        若 a 等于 型_列表：
            返还 1。
        完。
        若 a 等于 型_字典：
            返还 1。
        完。
        若 a 等于 型_函数：
            返还 1。
        完。
    完。
    返还 0。
完。

类型推断_二元结果类型 接受 运算符、左类型、右类型：
    若 运算符 大于或等于 30：
        若 运算符 小于或等于 34：
            若 运算符 等于 标记_加：
                若 左类型 等于 型_字符串：
                    返还 型_字符串。
                完。
            完。
            若 左类型 等于 型_浮点：
                返还 型_浮点。
            完。
            若 右类型 等于 型_浮点：
                返还 型_浮点。
            完。
            返还 型_整数。
        完。
    完。
    若 运算符 大于或等于 40：
        若 运算符 小于或等于 45：
            返还 型_布尔。
        完。
    完。
    若 运算符 等于 标记_且：
        返还 型_布尔。
    完。
    若 运算符 等于 标记_或：
        返还 型_布尔。
    完。
    若 运算符 大于或等于 标记_位与：
        若 运算符 小于或等于 标记_右移：
            返还 型_整数。
        完。
    完。
    返还 型_未知。
完。

类型推断_一元结果类型 接受 运算符、操作数类型：
    若 运算符 等于 标记_减：
        返还 操作数类型。
    完。
    若 运算符 等于 标记_非：
        返还 型_布尔。
    完。
    若 运算符 等于 标记_位取反：
        返还 型_整数。
    完。
    返还 型_未知。
完。

类型推断_可赋値 接受 目标、源：
    返还 做 类型推断_类型兼容 用 目标、源。
完。

类型推断_是数値 接受 t：
    若 t 等于 型_整数：
        返还 1。
    完。
    若 t 等于 型_浮点：
        返还 1。
    完。
    返还 0。
完。

类型推断_是引用 接受 t：
    若 t 等于 型_字符串：
        返还 1。
    完。
    若 t 等于 型_列表：
        返还 1。
    完。
    若 t 等于 型_字典：
        返还 1。
    完。
    若 t 等于 型_函数：
        返还 1。
    完。
    返还 0。
完。

类型推断_合并类型 接受 a、b：
    若 a 等于 b：
        返还 a。
    完。
    若 a 等于 型_未知：
        返还 b。
    完。
    若 b 等于 型_未知：
        返还 a。
    完。
    若 做 类型推断_是数値 用 a 等于 1 并且 做 类型推断_是数値 用 b 等于 1：
        返还 型_浮点。
    完。
    返还 型_任意。
完。

可变 _约束_左 为 一组：（空）。

可变 _约束_右 为 一组：（空）。

可变 _约束_种类 为 一组：（空）。

约束_添加相等 接受 类型A、类型B：
    做 追加 用 _约束_左、类型A。
    做 追加 用 _约束_右、类型B。
    做 追加 用 _约束_种类、0。
完。

约束_添加子类型 接受 子类型、父类型：
    做 追加 用 _约束_左、子类型。
    做 追加 用 _约束_右、父类型。
    做 追加 用 _约束_种类、1。
完。

约束_数量 接受 零参：
    返还 做 计数 用 _约束_左。
完。

约束_清空 接受 零参：
    _约束_左 为 一组：（空）。
    _约束_右 为 一组：（空）。
    _约束_种类 为 一组：（空）。
完。

可变 _类型变量_名 为 一组：（空）。

可变 _类型变量_绑定 为 一组：（空）。

可变 _下一类型变量 为 0。

新类型变量 接受 零参：
    名 为 「T」 加 做 转字符串 用 _下一类型变量。
    做 追加 用 _类型变量_名、名。
    做 追加 用 _类型变量_绑定、型_未知。
    _下一类型变量 为 _下一类型变量 加 1。
    返还 做 计数 用 _类型变量_名 减 1。
完。

类型变量_绑定 接受 idx：
    返还 _类型变量_绑定 的 第 idx 项。
完。

类型变量_设绑定 接受 idx、类型：
    _类型变量_绑定 的 第 idx 项 为 类型。
完。

类型变量_已绑定 接受 idx：
    返还 _类型变量_绑定 的 第 idx 项 不等于 型_未知。
完。

类型变量_解析 接受 idx：
    可变 当前 为 idx。
    只要 当前 小于 做 计数 用 _类型变量_绑定 并且 _类型变量_绑定 的 第 当前 项 不等于 型_未知：
        若 _类型变量_绑定 的 第 当前 项 小于 做 计数 用 _类型变量_绑定：
            当前 为 _类型变量_绑定 的 第 当前 项。
        否则：
            返还 _类型变量_绑定 的 第 当前 项。
        完。
    完。
    返还 型_未知。
完。

类型_统一 接受 A型、B型：
    若 A型 等于 B型：
        返还 真。
    完。
    若 A型 等于 型_未知：
        返还 真。
    完。
    若 B型 等于 型_未知：
        返还 真。
    完。
    若 A型 等于 型_任意 或者 B型 等于 型_任意：
        返还 真。
    完。
    若 做 类型推断_是数値 用 A型 等于 1 并且 做 类型推断_是数値 用 B型 等于 1：
        返还 真。
    完。
    返还 假。
完。

约束_求解 接受 零参：
    可变 变化 为 真。
    可变 迭代 为 0。
    只要 变化 并且 迭代 小于 100：
        变化 为 假。
        可变 i 为 0。
        只要 i 小于 做 计数 用 _约束_左：
            左 为 _约束_左 的 第 i 项。
            右 为 _约束_右 的 第 i 项。
            若 _约束_种类 的 第 i 项 等于 0：
                若 左 小于 做 计数 用 _类型变量_绑定 并且 非 做 类型变量_已绑定 用 左：
                    做 类型变量_设绑定 用 左、右。
                    变化 为 真。
                完。
                若 右 小于 做 计数 用 _类型变量_绑定 并且 非 做 类型变量_已绑定 用 右：
                    做 类型变量_设绑定 用 右、左。
                    变化 为 真。
                完。
            完。
            i 为 i 加 1。
        完。
        迭代 为 迭代 加 1。
    完。
    返还 迭代 小于 100。
完。

推断_赋値类型 接受 左类型、右表达式类型：
    若 左类型 等于 型_未知：
        返还 右表达式类型。
    完。
    若 做 类型_统一 用 左类型、右表达式类型：
        返还 左类型。
    完。
    返还 型_任意。
完。

推断_返回类型 接受 分支类型列表：
    若 做 计数 用 分支类型列表 等于 0：
        返还 型_未知。
    完。
    可变 结果 为 分支类型列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 分支类型列表：
        结果 为 做 类型推断_合并类型 用 结果、分支类型列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

推断_条件类型 接受 _条件类型、真分支类型、假分支类型：
    返还 做 类型推断_合并类型 用 真分支类型、假分支类型。
完。

类型推断_泛型_实例化 接受 模板参数列表、实际参数列表：
    可变 映射 为 一组：一组：（空）、一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 模板参数列表 并且 i 小于 做 计数 用 实际参数列表：
        做 追加 用 映射 的 第 0 项、模板参数列表 的 第 i 项。
        做 追加 用 映射 的 第 1 项、实际参数列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 映射。
完。

泛型_替换 接受 类型、映射：
    可变 i 为 0。
    只要 i 小于 做 计数 用 映射 的 第 0 项：
        若 类型 等于 映射 的 第 0 项 的 第 i 项：
            返还 映射 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 类型。
完。

可变 _推断_总数 为 0。

可变 _推断_失败数 为 0。

可变 _推断_约束数 为 0。

类型推断_记录 接受 零参：
    _推断_总数 为 _推断_总数 加 1。
完。

类型推断_记录失败 接受 零参：
    _推断_失败数 为 _推断_失败数 加 1。
完。

类型推断_记录约束 接受 零参：
    _推断_约束数 为 _推断_约束数 加 1。
完。

型_联合 为 20。

型_可空 为 21。

型_泛型参 为 22。

型_别名 为 23。

型_约束泛 为 24。

型_元组 为 25。

型_结果 为 26。

联合类型_创建 接受 类型列表：
    可变 扁平 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 类型列表：
        t 为 类型列表 的 第 i 项。
        若 做 是列表 用 t 并且 做 计数 用 t 大于 0 并且 t 的 第 0 项 等于 型_联合：
            可变 j 为 0。
            只要 j 小于 做 计数 用 t 的 第 1 项：
                可变 已有 为 假。
                可变 k 为 0。
                只要 k 小于 做 计数 用 扁平：
                    若 扁平 的 第 k 项 等于 t 的 第 1 项 的 第 j 项：
                        已有 为 真。
                    完。
                    k 为 k 加 1。
                完。
                若 已有 等于 假：
                    做 追加 用 扁平、t 的 第 1 项 的 第 j 项。
                完。
                j 为 j 加 1。
            完。
        否则：
            可变 已有 为 假。
            可变 k 为 0。
            只要 k 小于 做 计数 用 扁平：
                若 扁平 的 第 k 项 等于 t：
                    已有 为 真。
                完。
                k 为 k 加 1。
            完。
            若 已有 等于 假：
                做 追加 用 扁平、t。
            完。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 扁平 等于 1：
        返还 扁平 的 第 0 项。
    完。
    返还 一组：型_联合、扁平。
完。

联合类型_包含 接受 联合、目标类型：
    若 做 是列表 用 联合 等于 假 或者 做 计数 用 联合 小于 2：
        返还 联合 等于 目标类型。
    完。
    若 联合 的 第 0 项 不等于 型_联合：
        返还 联合 等于 目标类型。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 联合 的 第 1 项：
        若 联合 的 第 1 项 的 第 i 项 等于 目标类型：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

联合类型_兼容 接受 子类型、超类型：
    若 做 是列表 用 子类型 等于 假 或者 子类型 的 第 0 项 不等于 型_联合：
        返还 做 联合类型_包含 用 超类型、子类型。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子类型 的 第 1 项：
        若 做 联合类型_包含 用 超类型、子类型 的 第 1 项 的 第 i 项 等于 假：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

可空类型_创建 接受 基础类型：
    返还 做 联合类型_创建 用 一组：基础类型、型_空値。
完。

可空类型_是可空 接受 类型：
    返还 做 联合类型_包含 用 类型、型_空値。
完。

空安全_安全 为 0。

空安全_未检查 为 1。

空安全_已窄化 为 2。

可空类型_窄化 接受 联合类型：
    若 做 是列表 用 联合类型 等于 假 或者 联合类型 的 第 0 项 不等于 型_联合：
        若 联合类型 等于 型_空値：
            返还 型_无値。
        完。
        返还 联合类型。
    完。
    可变 非空 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 联合类型 的 第 1 项：
        若 联合类型 的 第 1 项 的 第 i 项 不等于 型_空値：
            做 追加 用 非空、联合类型 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 非空 等于 0：
        返还 型_无値。
    完。
    若 做 计数 用 非空 等于 1：
        返还 非空 的 第 0 项。
    完。
    返还 一组：型_联合、非空。
完。

约束_无 为 0。

约束_数值 为 1。

约束_可迭代 为 2。

约束_可比较 为 3。

约束_可哈希 为 4。

约束_具体类 为 5。

泛型约束_创建 接受 种类：
    返还 一组：种类、型_任意。
完。

泛型约束_具体 接受 类型：
    返还 一组：约束_具体类、类型。
完。

泛型约束_满足 接受 约束、实际类型：
    种类 为 约束 的 第 0 项。
    若 种类 等于 约束_无：
        返还 真。
    完。
    若 种类 等于 约束_数值：
        返还 实际类型 等于 型_整数 或者 实际类型 等于 型_浮点。
    完。
    若 种类 等于 约束_可迭代：
        返还 实际类型 等于 型_列表 或者 实际类型 等于 型_字符串 或者 实际类型 等于 型_字典。
    完。
    若 种类 等于 约束_可比较：
        返还 实际类型 等于 型_整数 或者 实际类型 等于 型_浮点 或者 实际类型 等于 型_字符串 或者 实际类型 等于 型_布尔。
    完。
    若 种类 等于 约束_可哈希：
        返还 实际类型 等于 型_整数 或者 实际类型 等于 型_字符串 或者 实际类型 等于 型_布尔。
    完。
    若 种类 等于 约束_具体类：
        返还 做 类型推断_类型兼容 用 实际类型、约束 的 第 1 项。
    完。
    返还 假。
完。

可变 泛型参数表 为 一组：（空）。

泛型_注册参数 接受 名称、约束：
    做 追加 用 泛型参数表、一组：名称、约束。
完。

泛型_查找约束 接受 名称：
    可变 i 为 做 计数 用 泛型参数表 减 1。
    只要 i 大于或等于 0：
        若 泛型参数表 的 第 i 项 的 第 0 项 等于 名称：
            返还 泛型参数表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 减 1。
    完。
    返还 做 泛型约束_创建 用 约束_无。
完。

泛型_实例化验证 接受 名称、实际类型：
    约束 为 做 泛型_查找约束 用 名称。
    返还 做 泛型约束_满足 用 约束、实际类型。
完。

可变 类型别名表 为 一组：（空）。

类型别名_注册 接受 别名、原类型：
    做 追加 用 类型别名表、一组：别名、原类型。
完。

类型别名_解析 接受 名称：
    可变 i 为 0。
    可变 深度 为 0。
    只要 深度 小于 10：
        可变 找到 为 假。
        i 为 0。
        只要 i 小于 做 计数 用 类型别名表：
            若 类型别名表 的 第 i 项 的 第 0 项 等于 名称：
                名称 为 类型别名表 的 第 i 项 的 第 1 项。
                找到 为 真。
            完。
            i 为 i 加 1。
        完。
        若 找到 等于 假：
            返还 名称。
        完。
        深度 为 深度 加 1。
    完。
    返还 名称。
完。

INT64_上界 为 9223372036854775807。

溢出检测_加法 接受 a、b：
    若 b 大于 0 并且 a 大于 INT64_上界 减 b：
        返还 真。
    完。
    若 b 小于 0 并且 a 小于 0 减 INT64_上界 减 1 减 b：
        返还 真。
    完。
    返还 假。
完。

溢出检测_乘法 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 假。
    完。
    若 a 大于 0 并且 b 大于 0 并且 a 大于 INT64_上界 除以 b：
        返还 真。
    完。
    若 a 小于 0 并且 b 小于 0 并且 a 小于 INT64_上界 除以 b：
        返还 真。
    完。
    若 a 大于 0 并且 b 小于 0 并且 b 小于 (0 减 INT64_上界 减 1) 除以 a：
        返还 真。
    完。
    若 a 小于 0 并且 b 大于 0 并且 a 小于 (0 减 INT64_上界 减 1) 除以 b：
        返还 真。
    完。
    返还 假。
完。

溢出检测_左移 接受 值、位数：
    若 位数 小于 0 或者 位数 大于或等于 63：
        返还 真。
    完。
    若 值 大于 0 并且 值 大于 INT64_上界 右移 位数：
        返还 真。
    完。
    返还 假。
完。

可变 溢出警告列表 为 一组：（空）。

溢出警告_添加 接受 行号、运算、左值、右值：
    做 追加 用 溢出警告列表、一组：行号、运算、左值、右值。
完。

溢出警告_报告 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 溢出警告列表：
        w 为 溢出警告列表 的 第 i 项。
        做 打印 用 「[溢出警告] 行」 加 做 转字符串 用 w 的 第 0 项 加 「: 」 加 w 的 第 1 项 加 「运算可能溢出」。
        i 为 i 加 1。
    完。
    返还 做 计数 用 溢出警告列表。
完。

结果类型_创建 接受 成功类型、错误类型：
    返还 一组：型_结果、成功类型、错误类型。
完。

结果类型_成功类型 接受 rt：
    返还 rt 的 第 1 项。
完。

结果类型_错误类型 接受 rt：
    返还 rt 的 第 2 项。
完。

本章 收束。
