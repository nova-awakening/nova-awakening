《可变性检查》

引用《..·..·标准库·基础》。

性_不可变 为 0。

性_可变 为 1。

性_常量 为 2。

检_通过 为 0。

检_赋不可变 为 1。

检_赋常量 为 2。

检_改参数 为 3。

检_改字段 为 4。

检_未初始化 为 5。

可变记录 接受 名称ID、可变性：
    返还 一组：名称ID、可变性。
完。

记录_可变性 接受 r：
    返还 r 的 第 1 项。
完。

记录_已初始化 接受 r：
    返还 r 的 第 2 项。
完。

记录_赋值次数 接受 r：
    返还 r 的 第 3 项。
完。

记录_初始化 接受 r：
    r 的 第 2 项 为 1。
    r 的 第 3 项 为 r 的 第 3 项 加 1。
完。

可变性检_记录_赋值 接受 r：
    r 的 第 3 项 为 r 的 第 3 项 加 1。
完。

检查赋值 接受 记录：
    _可变性_检查数 为 _可变性_检查数 加 1。
    若 记录 的 第 1 项 等于 性_常量：
        _可变性_违规数 为 _可变性_违规数 加 1。
        返还 检_赋常量。
    完。
    若 记录 的 第 1 项 等于 性_不可变：
        若 记录 的 第 2 项 等于 1：
            返还 检_赋不可变。
        完。
    完。
    返还 检_通过。
完。

检查使用 接受 记录：
    若 记录 的 第 2 项 等于 0：
        返还 检_未初始化。
    完。
    返还 检_通过。
完。

可变性检_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、做 字典。
完。

可变性检_注册 接受 检查器、名称ID、可变性：
    索引 为 做 计数 用 检查器 的 第 0 项。
    做 追加 用 检查器 的 第 0 项、做 可变记录 用 名称ID、可变性。
    做 字典设置 用 检查器 的 第 2 项、名称ID、索引。
完。

可变性检_查找 接受 检查器、名称ID：
    索引 为 做 字典获取 用 检查器 的 第 2 项、名称ID。
    若 索引 不等于 空：
        返还 检查器 的 第 0 项 的 第 索引 项。
    完。
    返还 0。
完。

可变性检_赋值 接受 检查器、名称ID、行号：
    记录 为 做 可变性检_查找 用 检查器、名称ID。
    若 记录 等于 0：
        返还 检_通过。
    完。
    结果 为 做 检查赋值 用 记录。
    若 结果 不等于 检_通过：
        做 追加 用 检查器 的 第 1 项、一组：结果、名称ID、行号。
    否则：
        若 记录 的 第 2 项 等于 0：
            做 记录_初始化 用 记录。
        否则：
            做 可变性检_记录_赋值 用 记录。
        完。
    完。
    返还 结果。
完。

可变性检_使用 接受 检查器、名称ID、行号：
    记录 为 做 可变性检_查找 用 检查器、名称ID。
    若 记录 等于 0：
        返还 检_通过。
    完。
    结果 为 做 检查使用 用 记录。
    若 结果 不等于 检_通过：
        做 追加 用 检查器 的 第 1 项、一组：结果、名称ID、行号。
    完。
    返还 结果。
完。

可变性检_错误数 接受 检查器：
    返还 做 计数 用 检查器 的 第 1 项。
完。

可变性检_错误列表 接受 检查器：
    返还 检查器 的 第 1 项。
完。

可变性检_错误描述 接受 错误码：
    若 错误码 等于 检_赋不可变：
        返还 「赋値给不可变变量」。
    完。
    若 错误码 等于 检_赋常量：
        返还 「赋値给常量」。
    完。
    若 错误码 等于 检_改参数：
        返还 「修改不可变参数」。
    完。
    若 错误码 等于 检_改字段：
        返还 「修改不可变结构字段」。
    完。
    若 错误码 等于 检_未初始化：
        返还 「使用未初始化变量」。
    完。
    返还 「未知可变性错误」。
完。

可变性检_多次赋値警告 接受 检查器：
    可变 警告 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 检查器 的 第 0 项：
        记录 为 检查器 的 第 0 项 的 第 i 项。
        若 记录 的 第 1 项 等于 性_不可变 并且 记录 的 第 3 项 大于 1：
            做 追加 用 警告、一组：记录 的 第 0 项、记录 的 第 3 项。
        完。
        i 为 i 加 1。
    完。
    返还 警告。
完。

可变检_不必要可变 接受 检查器：
    可变 建议 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 检查器 的 第 0 项：
        记录 为 检查器 的 第 0 项 的 第 i 项。
        若 记录 的 第 1 项 等于 性_可变 并且 记录 的 第 3 项 小于或等于 1：
            做 追加 用 建议、记录 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 建议。
完。

可变检_安全度 接受 检查器：
    总 为 做 计数 用 检查器 的 第 0 项。
    若 总 等于 0：
        返还 10000。
    完。
    可变 不可变数 为 0。
    可变 i 为 0。
    只要 i 小于 总：
        若 检查器 的 第 0 项 的 第 i 项 的 第 1 项 不等于 性_可变：
            不可变数 为 不可变数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 不可变数 乘 10000 除以 总。
完。

可变 _可变性_检查数 为 0。

可变 _可变性_违规数 为 0。

本章 收束。
