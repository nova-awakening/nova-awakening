《语义预检诊断》

引用《..·..·标准库·基础》。

引用《..·分析·错误诊断》。

引用《.·作用域》。

引用《.·符号表》。

引用《.·可变性检查》。

引用《.·未使用检查》。

语义预检_未使用类别名 接受 类别：
    若 类别 等于 类_变量：
        返还 「变量」。
    完。
    若 类别 等于 未使用检查_类_函数：
        返还 「函数」。
    完。
    若 类别 等于 类_参数：
        返还 「参数」。
    完。
    若 类别 等于 类_导入：
        返还 「导入」。
    完。
    返还 「符号」。
完。

语义预检_追加候选符号 接受 列表、名称：
    若 名称 等于 空 或者 名称 等于 「」：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 等于 名称：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 列表、名称。
    返还 1。
完。

语义预检_可用符号列表 接受 语义结果：
    可变 结果 为 一组：（空）。
    若 做 是列表 用 语义结果 等于 假：
        返还 结果。
    完。
    若 做 计数 用 语义结果 大于或等于 1：
        符号表 为 语义结果 的 第 0 项。
        若 做 是列表 用 符号表 并且 做 计数 用 符号表 大于或等于 3：
            可变 i 为 0。
            只要 i 小于 做 符号表_符号表_大小 用 符号表：
                做 语义预检_追加候选符号 用 结果、符号表 的 第 0 项 的 第 i 项 的 第 0 项。
                i 为 i 加 1。
            完。
        完。
    完。
    若 做 计数 用 语义结果 大于或等于 2：
        作用域栈 为 语义结果 的 第 1 项。
        若 做 是列表 用 作用域栈 并且 做 计数 用 作用域栈 大于或等于 1 并且 做 是列表 用 作用域栈 的 第 0 项：
            可变 si 为 0。
            只要 si 小于 做 计数 用 作用域栈 的 第 0 项：
                域 为 作用域栈 的 第 0 项 的 第 si 项。
                变量表 为 做 作用域_变量 用 域。
                可变 vi 为 0。
                只要 vi 小于 做 计数 用 变量表：
                    做 语义预检_追加候选符号 用 结果、做 变量_名称 用 变量表 的 第 vi 项。
                    vi 为 vi 加 1。
                完。
                si 为 si 加 1。
            完。
        完。
    完。
    返还 结果。
完。

语义预检_跨度 接受 文件索引、行号：
    可变 行 为 行号。
    若 做 是整数 用 行 等于 假 或者 行 小于或等于 0：
        行 为 1。
    完。
    返还 做 跨度_创建 用 文件索引、行、1、行、1。
完。

语义预检_创建预检问题诊断 接受 文件索引、问题、可用符号列：
    类型 为 问题 的 第 0 项。
    名称 为 问题 的 第 1 项。
    行号 为 问题 的 第 2 项。
    参考行号 为 问题 的 第 3 项。
    新跨度 为 做 语义预检_跨度 用 文件索引、行号。
    若 类型 等于 语义问题_重定义：
        若 参考行号 大于 0：
            返还 做 诊断_符号重定义 用 新跨度、做 语义预检_跨度 用 文件索引、参考行号、名称。
        完。
        d 为 做 诊断_创建 用 诊断_错误、错误码_重定义、做 字符串拼接 用 「符号 `」、做 字符串拼接 用 名称、「` 在同一作用域内重复定义」、0、0、0。
        做 诊断_添加标注 用 d、做 标注_主要 用 新跨度、「重复定义」。
        返还 d。
    完。
    若 类型 等于 语义问题_未定义赋值：
        d 为 做 诊断_未定义符号_带推荐 用 新跨度、名称、可用符号列。
        做 诊断_添加说明 用 d、「提示: 此处是赋值目标，请先定义该变量」。
        返还 d。
    完。
    若 类型 等于 语义问题_未定义引用：
        返还 做 诊断_未定义符号_带推荐 用 新跨度、名称、可用符号列。
    完。
    返还 空。
完。

语义预检_创建可变性诊断 接受 文件索引、错误项：
    错误码 为 错误项 的 第 0 项。
    名称 为 错误项 的 第 1 项。
    跨度 为 做 语义预检_跨度 用 文件索引、错误项 的 第 2 项。
    若 错误码 等于 检_赋不可变：
        返还 做 诊断_不可变变量 用 跨度、名称。
    完。
    描述 为 做 可变性检_错误描述 用 错误码。
    d 为 做 诊断_创建 用 诊断_错误、错误码_可变性、做 字符串拼接 用 描述、做 字符串拼接 用 「: `」、做 字符串拼接 用 名称、「`」、0、0、0。
    做 诊断_添加标注 用 d、做 标注_主要 用 跨度、描述。
    返还 d。
完。

语义预检_创建未使用诊断 接受 文件索引、警告项：
    名称 为 警告项 的 第 1 项。
    类别 为 警告项 的 第 2 项。
    跨度 为 做 语义预检_跨度 用 文件索引、警告项 的 第 3 项。
    若 类别 等于 类_变量：
        返还 做 诊断_未使用变量 用 跨度、名称。
    完。
    类别名 为 做 语义预检_未使用类别名 用 类别。
    d 为 做 诊断_创建 用 诊断_警告、3100、做 字符串拼接 用 类别名、做 字符串拼接 用 「 `」、做 字符串拼接 用 名称、「` 未使用」、0、0、0。
    标注说明 为 做 字符串拼接 用 「此」、做 字符串拼接 用 类别名、「被声明但从未使用」。
    做 诊断_添加标注 用 d、做 标注_主要 用 跨度、标注说明。
    返还 d。
完。

语义预检_生成诊断收集器 接受 文件路径、语义结果：
    收集器 为 做 诊断收集器_创建。
    可变 问题列表 为 一组：（空）。
    若 做 是列表 用 语义结果 并且 做 计数 用 语义结果 大于或等于 7 并且 做 是列表 用 语义结果 的 第 6 项：
        问题列表 为 语义结果 的 第 6 项。
    完。
    可变 可变错误列 为 一组：（空）。
    若 做 是列表 用 语义结果 并且 做 计数 用 语义结果 大于或等于 3：
        可变错误列 为 做 可变性检_错误列表 用 语义结果 的 第 2 项。
    完。
    可变 未用警告列 为 一组：（空）。
    若 做 是列表 用 语义结果 并且 做 计数 用 语义结果 大于或等于 4：
        未用警告列 为 做 未用检_警告列表 用 语义结果 的 第 3 项。
    完。
    若 做 计数 用 问题列表 等于 0 并且 做 计数 用 可变错误列 等于 0 并且 做 计数 用 未用警告列 等于 0：
        返还 收集器。
    完。
    文件索引 为 做 诊断收集器_注册文件 用 收集器、文件路径。
    可变 可用符号列 为 一组：（空）。
    若 做 计数 用 问题列表 大于 0：
        可用符号列 为 做 语义预检_可用符号列表 用 语义结果。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 问题列表：
        d 为 做 语义预检_创建预检问题诊断 用 文件索引、问题列表 的 第 i 项、可用符号列。
        若 d 不等于 空：
            做 诊断收集器_添加 用 收集器、d。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 可变错误列：
        做 诊断收集器_添加 用 收集器、做 语义预检_创建可变性诊断 用 文件索引、可变错误列 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 未用警告列：
        做 诊断收集器_添加 用 收集器、做 语义预检_创建未使用诊断 用 文件索引、未用警告列 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 收集器。
完。

本章 收束。
