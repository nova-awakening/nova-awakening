《作用域》

引用《..·..·标准库·基础》。

引用《..·..·编译器·标记常量》。

引用《.·可变性检查》。

引用《.·未使用检查》。

引用《.·符号表》。

域_全局 为 0。

域_函数 为 1。

域_块 为 2。

域_循环 为 3。

域_匹配 为 4。

变量_创建 接受 名称ID、是可变、行号：
    返还 一组：名称ID、0、是可变、0、0、行号。
完。

变量_名称 接受 v：
    返还 v 的 第 0 项。
完。

变量_可变 接受 v：
    返还 v 的 第 2 项。
完。

变量_已赋值 接受 v：
    返还 v 的 第 3 项。
完。

变量_被引用 接受 v：
    返还 v 的 第 4 项。
完。

变量_行号 接受 v：
    返还 v 的 第 5 项。
完。

变量_标记赋值 接受 v：
    v 的 第 3 项 为 1。
完。

变量_标记引用 接受 v：
    v 的 第 4 项 为 1。
完。

作用域_创建 接受 类型、父索引、深度：
    返还 一组：类型、父索引、一组：（空）、深度。
完。

作用域_类型 接受 s：
    返还 s 的 第 0 项。
完。

作用域_父 接受 s：
    返还 s 的 第 1 项。
完。

作用域_变量 接受 s：
    返还 s 的 第 2 项。
完。

作用域_作用域_深度 接受 s：
    返还 s 的 第 3 项。
完。

作用域_变量数 接受 s：
    返还 做 计数 用 s 的 第 2 项。
完。

作用域栈_创建 接受 零参：
    全局 为 做 作用域_创建 用 域_全局、0 减 1、0。
    返还 一组：一组：全局、0。
完。

作用域栈_推入 接受 栈、类型：
    当前 为 栈 的 第 1 项。
    深度 为 栈 的 第 0 项 的 第 当前 项 的 第 3 项 加 1。
    新作用域 为 做 作用域_创建 用 类型、当前、深度。
    做 追加 用 栈 的 第 0 项、新作用域。
    栈 的 第 1 项 为 做 计数 用 栈 的 第 0 项 减 1。
    返还 栈 的 第 1 项。
完。

作用域栈_弹出 接受 栈：
    当前 为 栈 的 第 0 项 的 第 栈 的 第 1 项 项。
    栈 的 第 1 项 为 当前 的 第 1 项。
    返还 当前。
完。

作用域栈_当前 接受 栈：
    返还 栈 的 第 0 项 的 第 栈 的 第 1 项 项。
完。

作用域栈_深度 接受 栈：
    返还 栈 的 第 0 项 的 第 栈 的 第 1 项 项 的 第 3 项。
完。

声明变量 接受 栈、名称ID、是可变、行号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 栈 的 第 0 项 的 第 栈 的 第 1 项 项 的 第 2 项：
        若 做 字符串相等 用 栈 的 第 0 项 的 第 栈 的 第 1 项 项 的 第 2 项 的 第 i 项 的 第 0 项、名称ID：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 栈 的 第 0 项 的 第 栈 的 第 1 项 项 的 第 2 项、做 变量_创建 用 名称ID、是可变、行号。
    返还 1。
完。

当前作用域_查找变量 接受 栈、名称ID：
    变量表 为 栈 的 第 0 项 的 第 栈 的 第 1 项 项 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 变量表：
        若 做 字符串相等 用 变量表 的 第 i 项 的 第 0 项、名称ID：
            返还 变量表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

查找变量 接受 栈、名称ID：
    可变 索引 为 栈 的 第 1 项。
    只要 索引 大于或等于 0：
        可变 i 为 0。
        只要 i 小于 做 计数 用 栈 的 第 0 项 的 第 索引 项 的 第 2 项：
            若 做 字符串相等 用 栈 的 第 0 项 的 第 索引 项 的 第 2 项 的 第 i 项 的 第 0 项、名称ID：
                返还 栈 的 第 0 项 的 第 索引 项 的 第 2 项 的 第 i 项。
            完。
            i 为 i 加 1。
        完。
        索引 为 栈 的 第 0 项 的 第 索引 项 的 第 1 项。
    完。
    返还 0。
完。

在循环作用域 接受 栈：
    可变 索引 为 栈 的 第 1 项。
    只要 索引 大于或等于 0：
        若 栈 的 第 0 项 的 第 索引 项 的 第 0 项 等于 域_循环：
            返还 1。
        完。
        若 栈 的 第 0 项 的 第 索引 项 的 第 0 项 等于 域_函数：
            返还 0。
        完。
        索引 为 栈 的 第 0 项 的 第 索引 项 的 第 1 项。
    完。
    返还 0。
完。

在函数作用域 接受 栈：
    可变 索引 为 栈 的 第 1 项。
    只要 索引 大于或等于 0：
        若 栈 的 第 0 项 的 第 索引 项 的 第 0 项 等于 域_函数：
            返还 1。
        完。
        索引 为 栈 的 第 0 项 的 第 索引 项 的 第 1 项。
    完。
    返还 0。
完。

检查遮蔽 接受 栈、名称ID：
    可变 索引 为 栈 的 第 0 项 的 第 栈 的 第 1 项 项 的 第 1 项。
    只要 索引 大于或等于 0：
        作用域 为 栈 的 第 0 项 的 第 索引 项。
        变量表 为 作用域 的 第 2 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 变量表：
            若 做 字符串相等 用 变量表 的 第 i 项 的 第 0 项、名称ID：
                返还 一组：1、作用域 的 第 3 项、变量表 的 第 i 项 的 第 5 项。
            完。
            i 为 i 加 1。
        完。
        索引 为 作用域 的 第 1 项。
    完。
    返还 一组：0。
完。

检测未使用变量 接受 栈、作用域索引：
    作用域 为 栈 的 第 0 项 的 第 作用域索引 项。
    变量表 为 作用域 的 第 2 项。
    可变 未使用 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 变量表：
        若 变量表 的 第 i 项 的 第 4 项 等于 0 并且 变量表 的 第 i 项 的 第 3 项 等于 1：
            做 追加 用 未使用、一组：变量表 的 第 i 项 的 第 0 项、变量表 的 第 i 项 的 第 5 项。
        完。
        i 为 i 加 1。
    完。
    返还 未使用。
完。

检测未初始化引用 接受 栈、作用域索引：
    作用域 为 栈 的 第 0 项 的 第 作用域索引 项。
    变量表 为 作用域 的 第 2 项。
    可变 问题 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 变量表：
        若 变量表 的 第 i 项 的 第 4 项 等于 1 并且 变量表 的 第 i 项 的 第 3 项 等于 0：
            做 追加 用 问题、一组：变量表 的 第 i 项 的 第 0 项、变量表 的 第 i 项 的 第 5 项。
        完。
        i 为 i 加 1。
    完。
    返还 问题。
完。

分析闭包捕获 接受 栈：
    可变 捕获列表 为 一组：（空）。
    可变 函数深度 为 负 1。
    可变 索引 为 栈 的 第 1 项。
    只要 索引 大于或等于 0：
        若 栈 的 第 0 项 的 第 索引 项 的 第 0 项 等于 域_函数：
            函数深度 为 栈 的 第 0 项 的 第 索引 项 的 第 3 项。
            索引 为 负 1。
        否则：
            索引 为 栈 的 第 0 项 的 第 索引 项 的 第 1 项。
        完。
    完。
    若 函数深度 小于 0：
        返还 捕获列表。
    完。
    索引 为 栈 的 第 1 项。
    只要 索引 大于或等于 0 并且 栈 的 第 0 项 的 第 索引 项 的 第 3 项 大于或等于 函数深度：
        变量表 为 栈 的 第 0 项 的 第 索引 项 的 第 2 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 变量表：
            若 变量表 的 第 i 项 的 第 4 项 等于 1：
                定义结果 为 做 查找变量定义深度 用 栈、变量表 的 第 i 项 的 第 0 项。
                若 定义结果 大于或等于 0 并且 定义结果 小于 函数深度：
                    做 追加 用 捕获列表、一组：变量表 的 第 i 项 的 第 0 项、定义结果、变量表 的 第 i 项 的 第 2 项。
                完。
            完。
            i 为 i 加 1。
        完。
        索引 为 栈 的 第 0 项 的 第 索引 项 的 第 1 项。
    完。
    返还 捕获列表。
完。

查找变量定义深度 接受 栈、名称ID：
    可变 索引 为 栈 的 第 1 项。
    只要 索引 大于或等于 0：
        变量表 为 栈 的 第 0 项 的 第 索引 项 的 第 2 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 变量表：
            若 做 字符串相等 用 变量表 的 第 i 项 的 第 0 项、名称ID 并且 变量表 的 第 i 项 的 第 3 项 等于 1：
                返还 栈 的 第 0 项 的 第 索引 项 的 第 3 项。
            完。
            i 为 i 加 1。
        完。
        索引 为 栈 的 第 0 项 的 第 索引 项 的 第 1 项。
    完。
    返还 负 1。
完。

作用域_诊断 接受 栈：
    总作用域 为 做 计数 用 栈 的 第 0 项。
    可变 总变量 为 0。
    可变 总未使用 为 0。
    可变 总遮蔽 为 0。
    可变 最大深度 为 0。
    可变 s 为 0。
    只要 s 小于 总作用域：
        作用域 为 栈 的 第 0 项 的 第 s 项。
        总变量 为 总变量 加 做 计数 用 作用域 的 第 2 项。
        若 作用域 的 第 3 项 大于 最大深度：
            最大深度 为 作用域 的 第 3 项。
        完。
        未使用 为 做 检测未使用变量 用 栈、s。
        总未使用 为 总未使用 加 做 计数 用 未使用。
        s 为 s 加 1。
    完。
    返还 一组：总作用域、总变量、总未使用、最大深度、总遮蔽。
完。

作用域_统计可变率 接受 栈、索引：
    变量表 为 栈 的 第 0 项 的 第 索引 项 的 第 2 项。
    总数 为 做 计数 用 变量表。
    若 总数 等于 0：
        返还 0。
    完。
    可变 可变数 为 0。
    可变 i 为 0。
    只要 i 小于 总数：
        若 变量表 的 第 i 项 的 第 2 项 等于 1：
            可变数 为 可变数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 可变数 乘 10000 除以 总数。
完。

在匹配作用域 接受 栈：
    可变 索引 为 栈 的 第 1 项。
    只要 索引 大于或等于 0：
        若 栈 的 第 0 项 的 第 索引 项 的 第 0 项 等于 域_匹配：
            返还 1。
        完。
        若 栈 的 第 0 项 的 第 索引 项 的 第 0 项 等于 域_函数：
            返还 0。
        完。
        索引 为 栈 的 第 0 项 的 第 索引 项 的 第 1 项。
    完。
    返还 0。
完。

统计函数局部变量 接受 栈：
    可变 总数 为 0。
    可变 索引 为 栈 的 第 1 项。
    只要 索引 大于或等于 0：
        作用域 为 栈 的 第 0 项 的 第 索引 项。
        总数 为 总数 加 做 计数 用 作用域 的 第 2 项。
        若 作用域 的 第 0 项 等于 域_函数：
            返还 总数。
        完。
        索引 为 作用域 的 第 1 项。
    完。
    返还 总数。
完。

可变 _作用域_创建数 为 0。

可变 _作用域_最大深度 为 0。

语义问题_重定义 为 1。

语义问题_未定义赋值 为 2。

语义问题_未定义引用 为 3。

语义_预检_追加问题 接受 问题列表、类型、名称ID、行号、参考行号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 问题列表：
        项 为 问题列表 的 第 i 项。
        若 项 的 第 0 项 等于 类型 并且 做 字符串相等 用 项 的 第 1 项、名称ID 并且 项 的 第 2 项 等于 行号：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 问题列表、一组：类型、名称ID、行号、参考行号。
    返还 1。
完。

语义_预检_取语句行号 接受 语句：
    若 做 是列表 用 语句 等于 假：
        返还 0。
    完。
    若 做 计数 用 语句 等于 0：
        返还 0。
    完。
    候选 为 语句 的 第 做 计数 用 语句 减 1 项。
    若 做 是整数 用 候选：
        返还 候选。
    完。
    返还 0。
完。

语义_预检_记录赋值 接受 作用域栈、可变检查器、未用检查器、问题列表、名称ID、行号：
    若 名称ID 等于 空 或者 名称ID 等于 「」：
        返还 0。
    完。
    若 做 查找变量 用 作用域栈、名称ID 等于 0：
        做 语义_预检_追加问题 用 问题列表、语义问题_未定义赋值、名称ID、行号、0。
    完。
    做 可变性检_赋值 用 可变检查器、名称ID、行号。
    做 未用检_赋值记录 用 未用检查器、名称ID。
    返还 1。
完。

语义_预检_注册变量 接受 作用域栈、可变检查器、未用检查器、问题列表、名称ID、类别、是可变、行号：
    若 名称ID 等于 空 或者 名称ID 等于 「」：
        返还 0。
    完。
    已有 为 做 当前作用域_查找变量 用 作用域栈、名称ID。
    若 已有 不等于 0：
        做 语义_预检_追加问题 用 问题列表、语义问题_重定义、名称ID、行号、做 变量_行号 用 已有。
        返还 0。
    完。
    做 声明变量 用 作用域栈、名称ID、是可变、行号。
    可变 可变性 为 性_不可变。
    若 是可变：
        可变性 为 性_可变。
    完。
    做 可变性检_注册 用 可变检查器、名称ID、可变性。
    做 未用检_声明 用 未用检查器、名称ID、类别、行号。
    返还 1。
完。

语义_预检_记录引用 接受 作用域栈、符号表、可变检查器、未用检查器、问题列表、名称ID、行号、检查未定义：
    若 名称ID 等于 空 或者 名称ID 等于 「」：
        返还 0。
    完。
    做 未用检_引用 用 未用检查器、名称ID。
    做 可变性检_使用 用 可变检查器、名称ID、行号。
    若 检查未定义：
        若 做 查找变量 用 作用域栈、名称ID 等于 0 并且 做 符号表_符号表_查找 用 符号表、名称ID 等于 0：
            做 语义_预检_追加问题 用 问题列表、语义问题_未定义引用、名称ID、行号、0。
        完。
    完。
    返还 1。
完。

语义_预检_变量是否可变 接受 语句：
    若 做 是列表 用 语句 等于 假：
        返还 0。
    完。
    若 做 计数 用 语句 小于 4：
        返还 0。
    完。
    若 语句 的 第 3 项：
        返还 1。
    完。
    返还 0。
完。

语义_预检_遍历表达式 接受 表达式、作用域栈、符号表、可变检查器、未用检查器、问题列表、当前行：
    若 做 是列表 用 表达式 等于 假 或者 做 计数 用 表达式 等于 0：
        返还 0。
    完。
    类型 为 表达式 的 第 0 项。
    若 类型 等于 标记_名称：
        做 语义_预检_记录引用 用 作用域栈、符号表、可变检查器、未用检查器、问题列表、表达式 的 第 1 项、当前行、1。
        返还 1。
    完。
    若 类型 等于 节点_调用：
        做 语义_预检_记录引用 用 作用域栈、符号表、可变检查器、未用检查器、问题列表、表达式 的 第 1 项、当前行、0。
        若 做 计数 用 表达式 大于或等于 3 并且 做 是列表 用 表达式 的 第 2 项：
            可变 i 为 0。
            只要 i 小于 做 计数 用 表达式 的 第 2 项：
                做 语义_预检_遍历表达式 用 表达式 的 第 2 项 的 第 i 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、当前行。
                i 为 i 加 1。
            完。
        完。
        返还 1。
    完。
    若 类型 等于 节点_二元：
        若 做 计数 用 表达式 大于或等于 3：
            做 语义_预检_遍历表达式 用 表达式 的 第 2 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、当前行。
        完。
        若 做 计数 用 表达式 大于或等于 4：
            做 语义_预检_遍历表达式 用 表达式 的 第 3 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、当前行。
        完。
        返还 1。
    完。
    若 类型 等于 节点_赋值：
        若 做 计数 用 表达式 大于或等于 2 并且 做 是列表 用 表达式 的 第 1 项：
            可变 i 为 0。
            只要 i 小于 做 计数 用 表达式 的 第 1 项：
                做 语义_预检_遍历表达式 用 表达式 的 第 1 项 的 第 i 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、当前行。
                i 为 i 加 1。
            完。
        完。
        返还 1。
    完。
    若 类型 等于 节点_索引：
        若 做 计数 用 表达式 大于或等于 2：
            做 语义_预检_遍历表达式 用 表达式 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、当前行。
        完。
        若 做 计数 用 表达式 大于或等于 3：
            做 语义_预检_遍历表达式 用 表达式 的 第 2 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、当前行。
        完。
        返还 1。
    完。
    若 类型 等于 标记_抛出：
        若 做 计数 用 表达式 大于或等于 3 并且 做 是列表 用 表达式 的 第 2 项：
            可变 i 为 0。
            只要 i 小于 做 计数 用 表达式 的 第 2 项：
                字段 为 表达式 的 第 2 项 的 第 i 项。
                若 做 是列表 用 字段 并且 做 计数 用 字段 大于或等于 2：
                    做 语义_预检_遍历表达式 用 字段 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、当前行。
                完。
                i 为 i 加 1。
            完。
        完。
        返还 1。
    完。
    若 类型 等于 标记_连接词：
        若 做 计数 用 表达式 大于或等于 2：
            做 语义_预检_遍历表达式 用 表达式 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、当前行。
        完。
        返还 1。
    完。
    若 类型 等于 节点_一元正 或者 类型 等于 节点_一元负：
        若 做 计数 用 表达式 大于或等于 2：
            做 语义_预检_遍历表达式 用 表达式 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、当前行。
        完。
        返还 1。
    完。
    返还 0。
完。

语义_预检_遍历语句列 接受 语句列、作用域栈、符号表、可变检查器、未用检查器、问题列表：
    若 做 是列表 用 语句列 等于 假：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列：
        做 语义_预检_遍历语句 用 语句列 的 第 i 项、作用域栈、符号表、可变检查器、未用检查器、问题列表。
        i 为 i 加 1。
    完。
    返还 1。
完。

语义_预检_遍历语句 接受 语句、作用域栈、符号表、可变检查器、未用检查器、问题列表：
    若 做 是列表 用 语句 等于 假 或者 做 计数 用 语句 等于 0：
        返还 0。
    完。
    类型 为 语句 的 第 0 项。
    _行 为 做 语义_预检_取语句行号 用 语句。
    若 类型 等于 标记_定义：
        可变 是可变 为 做 语义_预检_变量是否可变 用 语句。
        做 语义_预检_注册变量 用 作用域栈、可变检查器、未用检查器、问题列表、语句 的 第 1 项、类_变量、是可变、_行。
        若 做 计数 用 语句 大于或等于 3：
            做 语义_预检_遍历表达式 用 语句 的 第 2 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
            做 语义_预检_记录赋值 用 作用域栈、可变检查器、未用检查器、问题列表、语句 的 第 1 项、_行。
        完。
        返还 1。
    完。
    若 类型 等于 标记_可变：
        若 做 计数 用 语句 大于或等于 3：
            做 语义_预检_遍历表达式 用 语句 的 第 2 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
        完。
        做 语义_预检_记录赋值 用 作用域栈、可变检查器、未用检查器、问题列表、语句 的 第 1 项、_行。
        返还 1。
    完。
    若 类型 等于 标记_函数：
        若 做 计数 用 语句 大于或等于 2：
            做 语义_预检_遍历表达式 用 语句 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
        完。
        做 作用域栈_推入 用 作用域栈、域_块。
        若 做 计数 用 语句 大于或等于 3：
            做 语义_预检_遍历语句列 用 语句 的 第 2 项、作用域栈、符号表、可变检查器、未用检查器、问题列表。
        完。
        做 作用域栈_弹出 用 作用域栈。
        若 做 计数 用 语句 大于或等于 4 并且 做 是列表 用 语句 的 第 3 项：
            做 作用域栈_推入 用 作用域栈、域_块。
            做 语义_预检_遍历语句列 用 语句 的 第 3 项、作用域栈、符号表、可变检查器、未用检查器、问题列表。
            做 作用域栈_弹出 用 作用域栈。
        完。
        返还 1。
    完。
    若 类型 等于 标记_返回：
        若 做 计数 用 语句 大于或等于 2：
            做 语义_预检_遍历表达式 用 语句 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
        完。
        做 作用域栈_推入 用 作用域栈、域_循环。
        若 做 计数 用 语句 大于或等于 3：
            做 语义_预检_遍历语句列 用 语句 的 第 2 项、作用域栈、符号表、可变检查器、未用检查器、问题列表。
        完。
        做 作用域栈_弹出 用 作用域栈。
        返还 1。
    完。
    若 类型 等于 标记_如果：
        若 做 计数 用 语句 大于或等于 2：
            做 语义_预检_遍历表达式 用 语句 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
        完。
        返还 1。
    完。
    若 类型 等于 标记_否则：
        若 做 计数 用 语句 大于或等于 2：
            做 语义_预检_遍历表达式 用 语句 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
        完。
        返还 1。
    完。
    若 类型 等于 标记_当：
        若 做 计数 用 语句 大于或等于 2：
            若 做 是列表 用 语句 的 第 1 项：
                做 语义_预检_遍历表达式 用 语句 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
            否则：
                做 语义_预检_记录引用 用 作用域栈、符号表、可变检查器、未用检查器、问题列表、语句 的 第 1 项、_行、1。
            完。
        完。
        若 做 计数 用 语句 大于或等于 3：
            做 语义_预检_遍历表达式 用 语句 的 第 2 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
        完。
        若 做 计数 用 语句 大于或等于 4：
            做 语义_预检_遍历表达式 用 语句 的 第 3 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
        完。
        返还 1。
    完。
    若 类型 等于 标记_建：
        若 做 计数 用 语句 大于或等于 2：
            做 未用检_声明 用 未用检查器、语句 的 第 1 项、未使用检查_类_函数、_行。
        完。
        做 作用域栈_推入 用 作用域栈、域_函数。
        若 做 计数 用 语句 大于或等于 3 并且 做 是列表 用 语句 的 第 2 项：
            可变 pi 为 0。
            只要 pi 小于 做 计数 用 语句 的 第 2 项：
                做 语义_预检_注册变量 用 作用域栈、可变检查器、未用检查器、问题列表、语句 的 第 2 项 的 第 pi 项、类_参数、0、_行。
                做 语义_预检_记录赋值 用 作用域栈、可变检查器、未用检查器、问题列表、语句 的 第 2 项 的 第 pi 项、_行。
                pi 为 pi 加 1。
            完。
        完。
        若 做 计数 用 语句 大于或等于 4：
            做 语义_预检_遍历语句列 用 语句 的 第 3 项、作用域栈、符号表、可变检查器、未用检查器、问题列表。
        完。
        做 作用域栈_弹出 用 作用域栈。
        返还 1。
    完。
    若 类型 等于 标记_顺序词：
        若 做 计数 用 语句 大于或等于 2：
            做 语义_预检_遍历表达式 用 语句 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
        完。
        若 做 计数 用 语句 大于或等于 4：
            做 语义_预检_遍历表达式 用 语句 的 第 3 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
        完。
        返还 1。
    完。
    若 类型 等于 标记_关键135：
        若 做 计数 用 语句 大于或等于 2：
            做 语义_预检_遍历表达式 用 语句 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
        完。
        若 做 计数 用 语句 大于或等于 3 并且 做 是列表 用 语句 的 第 2 项：
            可变 bi 为 0。
            只要 bi 小于 做 计数 用 语句 的 第 2 项：
                分支 为 语句 的 第 2 项 的 第 bi 项。
                若 做 是列表 用 分支 并且 做 计数 用 分支 大于或等于 2：
                    做 语义_预检_遍历表达式 用 分支 的 第 0 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
                    做 作用域栈_推入 用 作用域栈、域_匹配。
                    做 语义_预检_遍历语句列 用 分支 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表。
                    做 作用域栈_弹出 用 作用域栈。
                完。
                bi 为 bi 加 1。
            完。
        完。
        返还 1。
    完。
    若 类型 等于 节点_变量声明：
        若 做 计数 用 语句 大于或等于 2：
            做 作用域栈_推入 用 作用域栈、域_块。
            做 语义_预检_遍历语句列 用 语句 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表。
            做 作用域栈_弹出 用 作用域栈。
        完。
        若 做 计数 用 语句 大于或等于 3：
            做 作用域栈_推入 用 作用域栈、域_块。
            做 语义_预检_遍历语句列 用 语句 的 第 2 项、作用域栈、符号表、可变检查器、未用检查器、问题列表。
            做 作用域栈_弹出 用 作用域栈。
        完。
        返还 1。
    完。
    若 类型 等于 节点_赋值语句：
        若 做 计数 用 语句 大于或等于 2：
            做 语义_预检_遍历表达式 用 语句 的 第 1 项、作用域栈、符号表、可变检查器、未用检查器、问题列表、_行。
        完。
        返还 1。
    完。
    返还 0。
完。

语义_最小预检查 接受 函数列、主体：
    符号表 为 做 符号表_创建 用 做 计数 用 函数列 加 64。
    作用域栈 为 做 作用域栈_创建。
    可变检查器 为 做 可变性检_创建。
    未用检查器 为 做 未用检_创建。
    问题列表 为 一组：（空）。
    可变 fi 为 0。
    只要 fi 小于 做 计数 用 函数列：
        函数节点 为 函数列 的 第 fi 项。
        若 做 是列表 用 函数节点 并且 做 计数 用 函数节点 大于或等于 2：
            函数行 为 做 语义_预检_取语句行号 用 函数节点。
            若 做 符号表_查找类型 用 符号表、函数节点 的 第 1 项、符号表_符号_函数 等于 0：
                做 符号表_追加 用 符号表、函数节点 的 第 1 项、符号表_符号_函数、0、0。
            否则：
                做 语义_预检_追加问题 用 问题列表、语义问题_重定义、函数节点 的 第 1 项、函数行、0。
            完。
            做 未用检_声明 用 未用检查器、函数节点 的 第 1 项、未使用检查_类_函数、函数行。
        完。
        fi 为 fi 加 1。
    完。
    做 语义_预检_遍历语句列 用 主体、作用域栈、符号表、可变检查器、未用检查器、问题列表。
    fi 为 0。
    只要 fi 小于 做 计数 用 函数列：
        函数节点 为 函数列 的 第 fi 项。
        若 做 是列表 用 函数节点 并且 做 计数 用 函数节点 大于或等于 4：
            函数行 为 做 语义_预检_取语句行号 用 函数节点。
            做 作用域栈_推入 用 作用域栈、域_函数。
            若 做 是列表 用 函数节点 的 第 2 项：
                可变 pi 为 0。
                只要 pi 小于 做 计数 用 函数节点 的 第 2 项：
                    做 语义_预检_注册变量 用 作用域栈、可变检查器、未用检查器、问题列表、函数节点 的 第 2 项 的 第 pi 项、类_参数、0、函数行。
                    做 语义_预检_记录赋值 用 作用域栈、可变检查器、未用检查器、问题列表、函数节点 的 第 2 项 的 第 pi 项、函数行。
                    pi 为 pi 加 1。
                完。
            完。
            做 语义_预检_遍历语句列 用 函数节点 的 第 3 项、作用域栈、符号表、可变检查器、未用检查器、问题列表。
            做 作用域栈_弹出 用 作用域栈。
        完。
        fi 为 fi 加 1。
    完。
    未用警告数 为 做 未用检_执行 用 未用检查器。
    可变错误数 为 做 可变性检_错误数 用 可变检查器 加 做 计数 用 问题列表。
    返还 一组：符号表、作用域栈、可变检查器、未用检查器、未用警告数、可变错误数、问题列表。
完。

本章 收束。
