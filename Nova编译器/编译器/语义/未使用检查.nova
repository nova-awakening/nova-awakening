《未使用检查》

引用《..·..·标准库·基础》。

引用《..·..·运行时·字典引擎·字典引擎》。

类_变量 为 1。

未使用检查_类_函数 为 2。

类_参数 为 3。

类_导入 为 4。

级_信息 为 0。

级_警告 为 1。

级_错误 为 2。

可变 _未使用检查_扫描数 为 0。

使用记录 接受 名称ID、类别、行号：
    返还 一组：名称ID、类别、行号。
完。

未用记_标记引用 接受 r：
    r 的 第 3 项 为 r 的 第 3 项 加 1。
完。

未用记_标记赋值 接受 r：
    r 的 第 4 项 为 r 的 第 4 项 加 1。
完。

未用记_引用数 接受 r：
    返还 r 的 第 3 项。
完。

未用记_赋值数 接受 r：
    返还 r 的 第 4 项。
完。

未用检_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、做 字典。
完。

未用检_声明 接受 ck、名称ID、类别、行号：
    索引 为 做 计数 用 ck 的 第 0 项。
    做 追加 用 ck 的 第 0 项、做 使用记录 用 名称ID、类别、行号。
    做 字典设置 用 ck 的 第 2 项、名称ID、索引。
完。

未用检_引用 接受 ck、名称ID：
    索引 为 做 字典获取 用 ck 的 第 2 项、名称ID。
    若 索引 不等于 空：
        做 未用记_标记引用 用 ck 的 第 0 项 的 第 索引 项。
        返还 1。
    完。
    返还 0。
完。

未用检_赋值记录 接受 ck、名称ID：
    索引 为 做 字典获取 用 ck 的 第 2 项、名称ID。
    若 索引 不等于 空：
        做 未用记_标记赋值 用 ck 的 第 0 项 的 第 索引 项。
        返还 1。
    完。
    返还 0。
完。

未用检_执行 接受 ck：
    可变 警告数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 ck 的 第 0 项：
        记录 为 ck 的 第 0 项 的 第 i 项。
        若 记录 的 第 3 项 等于 0：
            若 记录 的 第 4 项 等于 0 并且 记录 的 第 1 项 不等于 类_参数：
                做 追加 用 ck 的 第 1 项、一组：级_警告、记录 的 第 0 项、记录 的 第 1 项、记录 的 第 2 项、0。
                警告数 为 警告数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 警告数。
完。

未用检_警告数 接受 ck：
    返还 做 计数 用 ck 的 第 1 项。
完。

未用检_警告列表 接受 ck：
    返还 ck 的 第 1 项。
完。

未使用_声明 接受 ck、名称ID、类别：
    返还 做 未用检_声明 用 ck、名称ID、类别、0。
完。

未使用_执行 接受 ck：
    返还 做 未用检_执行 用 ck。
完。

应忽略 接受 名称ID：
    若 名称ID 小于 0：
        返还 1。
    完。
    返还 0。
完。

是只写 接受 记录：
    若 记录 的 第 4 项 大于 0：
        若 记录 的 第 3 项 等于 0：
            返还 1。
        完。
    完。
    返还 0。
完。

使用率 接受 ck：
    总 为 做 计数 用 ck 的 第 0 项。
    若 总 等于 0：
        返还 100。
    完。
    可变 已用 为 0。
    可变 i 为 0。
    只要 i 小于 总：
        若 ck 的 第 0 项 的 第 i 项 的 第 3 项 大于 0：
            已用 为 已用 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 已用 乘 100 除以 总。
完。

收集未使用 接受 ck：
    可变 列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 ck 的 第 0 项：
        若 ck 的 第 0 项 的 第 i 项 的 第 3 项 等于 0 并且 非 做 应忽略 用 ck 的 第 0 项 的 第 i 项 的 第 0 项：
            做 追加 用 列表、ck 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 列表。
完。

收集只写 接受 ck：
    可变 列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 ck 的 第 0 项：
        若 做 是只写 用 ck 的 第 0 项 的 第 i 项：
            做 追加 用 列表、ck 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 列表。
完。

可变 _导入_名 为 一组：（空）。

可变 _导入_已用 为 一组：（空）。

导入检查_注册 接受 名：
    做 追加 用 _导入_名、名。
    做 追加 用 _导入_已用、假。
完。

导入检查_标记使用 接受 名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _导入_名：
        若 做 字符串相等 用 _导入_名 的 第 i 项、名：
            _导入_已用 的 第 i 项 为 真。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

导入检查_未使用列表 接受 零参：
    可变 列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _导入_名：
        若 非 _导入_已用 的 第 i 项：
            做 追加 用 列表、_导入_名 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 列表。
完。

可变 _函数声明 为 一组：（空）。

可变 _函数引用 为 一组：（空）。

函数检查_注册 接受 名：
    做 追加 用 _函数声明、名。
    做 追加 用 _函数引用、0。
完。

函数检查_记录引用 接受 名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _函数声明：
        若 做 字符串相等 用 _函数声明 的 第 i 项、名：
            _函数引用 的 第 i 项 为 _函数引用 的 第 i 项 加 1。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

函数检查_未调用列表 接受 零参：
    可变 列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _函数声明：
        若 _函数引用 的 第 i 项 等于 0：
            做 追加 用 列表、_函数声明 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 列表。
完。

未使用_总计 接受 报告：
    返还 做 计数 用 报告 的 第 0 项 加 做 计数 用 报告 的 第 1 项 加 做 计数 用 报告 的 第 2 项 加 做 计数 用 报告 的 第 3 项。
完。

检查_未使用参数 接受 参数列表、函数体引用列表：
    可变 未使用 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数列表：
        可变 已用 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 函数体引用列表：
            若 函数体引用列表 的 第 j 项 等于 参数列表 的 第 i 项：
                已用 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已用 等于 假：
            做 追加 用 未使用、参数列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 未使用。
完。

检查_未使用导入 接受 导入列表、代码引用列表：
    可变 未使用 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 导入列表：
        可变 已用 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 代码引用列表：
            若 代码引用列表 的 第 j 项 等于 导入列表 的 第 i 项：
                已用 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已用 等于 假：
            做 追加 用 未使用、导入列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 未使用。
完。

可变 _未使用检查_发现数 为 0。

未使用_记录扫描 接受 零参：
    _未使用检查_扫描数 为 _未使用检查_扫描数 加 1。
完。

未使用_记录发现 接受 零参：
    _未使用检查_发现数 为 _未使用检查_发现数 加 1。
完。

本章 收束。
