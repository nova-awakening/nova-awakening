《生命期分析》

引用《..·..·标准库·基础》。

期_未生 为 0。

期_活跃 为 1。

期_消亡 为 2。

期_逃逸 为 3。

逃_无 为 0。

逃_返回値 为 1。

逃_闭包 为 2。

逃_全局 为 3。

逃_参数 为 4。

活跃区间 接受 变量ID、起始：
    返还 一组：变量ID、起始、起始、逃_无。
完。

区间_变量 接受 r：
    返还 r 的 第 0 项。
完。

区间_起始 接受 r：
    返还 r 的 第 1 项。
完。

区间_结束 接受 r：
    返还 r 的 第 2 项。
完。

区间_逃逸 接受 r：
    返还 r 的 第 3 项。
完。

区间_延伸 接受 r、位置：
    若 位置 大于 r 的 第 2 项：
        r 的 第 2 项 为 位置。
    完。
完。

区间_标记逃逸 接受 r、类型：
    r 的 第 3 项 为 类型。
完。

区间_长度 接受 r：
    返还 r 的 第 2 项 减 r 的 第 1 项 加 1。
完。

区间重叠 接受 a、b：
    若 a 的 第 2 项 小于 b 的 第 1 项：
        返还 0。
    完。
    若 b 的 第 2 项 小于 a 的 第 1 项：
        返还 0。
    完。
    返还 1。
完。

生命期分_分析器_创建 接受 零参：
    返还 一组：一组：（空）、0。
完。

分析器_声明 接受 分析器、变量ID：
    r 为 做 活跃区间 用 变量ID、分析器 的 第 1 项。
    做 追加 用 分析器 的 第 0 项、r。
    返还 r。
完。

分析器_使用 接受 分析器、变量ID：
    可变 i 为 做 计数 用 分析器 的 第 0 项 减 1。
    只要 i 大于或等于 0：
        若 分析器 的 第 0 项 的 第 i 项 的 第 0 项 等于 变量ID：
            做 区间_延伸 用 分析器 的 第 0 项 的 第 i 项、分析器 的 第 1 项。
            返还 1。
        完。
        i 为 i 减 1。
    完。
    返还 0。
完。

分析器_前进 接受 分析器：
    分析器 的 第 1 项 为 分析器 的 第 1 项 加 1。
完。

分析器_位置 接受 分析器：
    返还 分析器 的 第 1 项。
完。

可共享 接受 分析器、id_a、id_b：
    可变 ra 为 0。
    可变 rb 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分析器 的 第 0 项：
        若 分析器 的 第 0 项 的 第 i 项 的 第 0 项 等于 id_a：
            ra 为 分析器 的 第 0 项 的 第 i 项。
        完。
        若 分析器 的 第 0 项 的 第 i 项 的 第 0 项 等于 id_b：
            rb 为 分析器 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 ra 等于 0：
        返还 1。
    完。
    若 rb 等于 0：
        返还 1。
    完。
    若 做 区间重叠 用 ra、rb 等于 1：
        返还 0。
    完。
    返还 1。
完。

活跃数 接受 分析器、位置：
    可变 数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分析器 的 第 0 项：
        r 为 分析器 的 第 0 项 的 第 i 项。
        若 位置 大于或等于 r 的 第 1 项：
            若 位置 小于或等于 r 的 第 2 项：
                数 为 数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 数。
完。

最大活跃 接受 分析器：
    可变 最大 为 0。
    可变 p 为 0。
    只要 p 小于或等于 分析器 的 第 1 项：
        n 为 做 活跃数 用 分析器、p。
        若 n 大于 最大：
            最大 为 n。
        完。
        p 为 p 加 1。
    完。
    返还 最大。
完。

逃逸列表 接受 分析器：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分析器 的 第 0 项：
        若 分析器 的 第 0 项 的 第 i 项 的 第 3 项 不等于 逃_无：
            做 追加 用 结果、分析器 的 第 0 项 的 第 i 项 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

借用_共享 为 0。

可变 _借用_活跃表 为 一组：（空）。

借用_注册 接受 变量ID、类型、起始：
    做 追加 用 _借用_活跃表、一组：变量ID、类型、起始、负 1。
    返还 做 计数 用 _借用_活跃表 减 1。
完。

借用_释放 接受 借用ID、结束：
    若 借用ID 大于或等于 0 并且 借用ID 小于 做 计数 用 _借用_活跃表：
        _借用_活跃表 的 第 借用ID 项 的 第 3 项 为 结束。
    完。
完。

借用_检查冲突 接受 变量ID、新类型、位置：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _借用_活跃表：
        借用 为 _借用_活跃表 的 第 i 项。
        若 借用 的 第 0 项 等于 变量ID 并且 (借用 的 第 3 项 等于 负 1 或者 借用 的 第 3 项 大于 位置)：
            若 新类型 等于 借用_可变：
                返还 「不可变借用时不能可变借用」。
            完。
            若 借用 的 第 1 项 等于 借用_可变：
                返还 「可变借用时不能再借用」。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

借用_活跃数 接受 变量ID、位置：
    可变 数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _借用_活跃表：
        若 _借用_活跃表 的 第 i 项 的 第 0 项 等于 变量ID：
            若 _借用_活跃表 的 第 i 项 的 第 3 项 等于 负 1 或者 _借用_活跃表 的 第 i 项 的 第 3 项 大于 位置：
                数 为 数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 数。
完。

可变 _生命期_约束 为 一组：（空）。

生命期_添加约束 接受 a、b、关系：
    做 追加 用 _生命期_约束、一组：a、b、关系。
完。

生命期_求解 接受 约束列表：
    可变 范围 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 约束列表：
        约束 为 约束列表 的 第 i 项。
        可变 找到A 为 假。
        可变 找到B 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 范围：
            若 范围 的 第 j 项 的 第 0 项 等于 约束 的 第 0 项：
                找到A 为 真。
            完。
            若 范围 的 第 j 项 的 第 0 项 等于 约束 的 第 1 项：
                找到B 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 找到A：
            做 追加 用 范围、一组：约束 的 第 0 项、0、999999。
        完。
        若 非 找到B：
            做 追加 用 范围、一组：约束 的 第 1 项、0、999999。
        完。
        i 为 i 加 1。
    完。
    返还 范围。
完。

可变 _区域_栈 为 一组：（空）。

区域_进入 接受 区域ID：
    做 追加 用 _区域_栈、一组：区域ID、一组：（空）。
完。

区域_分配 接受 变量ID：
    若 做 计数 用 _区域_栈 等于 0：
        返还。
    完。
    顶 为 _区域_栈 的 第 做 计数 用 _区域_栈 减 1 项。
    做 追加 用 顶 的 第 1 项、变量ID。
完。

区域_退出 接受 零参：
    可变 新栈 为 一组：（空）。
    若 做 计数 用 _区域_栈 等于 0：
        返还 一组：（空）。
    完。
    顶 为 _区域_栈 的 第 做 计数 用 _区域_栈 减 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _区域_栈 减 1：
        做 追加 用 新栈、_区域_栈 的 第 i 项。
        i 为 i 加 1。
    完。
    _区域_栈 为 新栈。
    返还 顶 的 第 1 项。
完。

区域_当前深度 接受 零参：
    返还 做 计数 用 _区域_栈。
完。

可变 _移动_状态 为 一组：（空）。

移动_标记 接受 变量ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _移动_状态：
        若 _移动_状态 的 第 i 项 的 第 0 项 等于 变量ID：
            _移动_状态 的 第 i 项 的 第 1 项 为 真。
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 _移动_状态、一组：变量ID、真。
完。

移动_检查 接受 变量ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _移动_状态：
        若 _移动_状态 的 第 i 项 的 第 0 项 等于 变量ID：
            返还 _移动_状态 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

移动_使用后检查 接受 变量ID：
    若 做 移动_检查 用 变量ID：
        返还 「使用已移动的值」。
    完。
    返还 空。
完。

析构_计算顺序 接受 变量列表：
    可变 顺序 为 一组：（空）。
    可变 i 为 做 计数 用 变量列表 减 1。
    只要 i 大于或等于 0：
        做 追加 用 顺序、变量列表 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 顺序。
完。

可变 _生命期_借用检查数 为 0。

可变 _生命期_冲突数 为 0。

可变 _生命期_移动数 为 0。

生命期_记录借用 接受 零参：
    _生命期_借用检查数 为 _生命期_借用检查数 加 1。
完。

生命期_记录冲突 接受 零参：
    _生命期_冲突数 为 _生命期_冲突数 加 1。
完。

生命期_记录移动 接受 零参：
    _生命期_移动数 为 _生命期_移动数 加 1。
完。

借用_不可变 为 0。

借用_可变 为 1。

可变 _借用_记录列表 为 一组：（空）。

可变 _借用_下一ID 为 0。

借用_创建 接受 源变量、类型、位置：
    id 为 _借用_下一ID。
    _借用_下一ID 为 _借用_下一ID 加 1。
    记录 为 一组：id、源变量、类型、位置、位置、真。
    做 追加 用 _借用_记录列表、记录。
    做 生命期_记录借用。
    返还 id。
完。

借用_结束 接受 借用ID、位置：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _借用_记录列表：
        若 _借用_记录列表 的 第 i 项 的 第 0 项 等于 借用ID：
            _借用_记录列表 的 第 i 项 的 第 4 项 为 位置。
            _借用_记录列表 的 第 i 项 的 第 5 项 为 假。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

借用_是活跃 接受 借用ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _借用_记录列表：
        若 _借用_记录列表 的 第 i 项 的 第 0 项 等于 借用ID：
            返还 _借用_记录列表 的 第 i 项 的 第 5 项。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

借用检查_新借用 接受 源变量、新借用类型、位置：
    若 做 移动_检查 用 源变量：
        做 生命期_记录冲突。
        返还 3。
    完。
    可变 活跃不可变数 为 0。
    可变 活跃可变数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _借用_记录列表：
        若 _借用_记录列表 的 第 i 项 的 第 1 项 等于 源变量 并且 _借用_记录列表 的 第 i 项 的 第 5 项：
            若 _借用_记录列表 的 第 i 项 的 第 2 项 等于 借用_不可变：
                活跃不可变数 为 活跃不可变数 加 1。
            完。
            若 _借用_记录列表 的 第 i 项 的 第 2 项 等于 借用_可变：
                活跃可变数 为 活跃可变数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    若 新借用类型 等于 借用_可变：
        若 活跃可变数 大于 0 或者 活跃不可变数 大于 0：
            做 生命期_记录冲突。
            若 活跃可变数 大于 0：
                返还 1。
            完。
            返还 2。
        完。
    完。
    若 新借用类型 等于 借用_不可变 并且 活跃可变数 大于 0：
        做 生命期_记录冲突。
        返还 2。
    完。
    返还 0。
完。

所有权_可转移 接受 变量ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _借用_记录列表：
        若 _借用_记录列表 的 第 i 项 的 第 1 项 等于 变量ID 并且 _借用_记录列表 的 第 i 项 的 第 5 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

所有权_转移 接受 源、目标、位置：
    若 做 所有权_可转移 用 源 等于 假：
        返还 「不能转移: 存在活跃借用」。
    完。
    做 移动_标记 用 源。
    做 生命期_记录移动。
    返还 空。
完。

可变 _借用_错误列表 为 一组：（空）。

借用错误_添加 接受 行号、错误码、变量名、描述：
    做 追加 用 _借用_错误列表、一组：行号、错误码、变量名、描述。
完。

借用错误_报告 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _借用_错误列表：
        e 为 _借用_错误列表 的 第 i 项。
        做 打印 用 「[借用错误] 行」 加 做 转字符串 用 e 的 第 0 项 加 「 E」 加 做 转字符串 用 e 的 第 1 项 加 「: 」 加 e 的 第 3 项 加 「 (变量: 」 加 e 的 第 2 项 加 「)」。
        i 为 i 加 1。
    完。
    返还 做 计数 用 _借用_错误列表。
完。

借用_重置 接受 零参：
    _借用_记录列表 为 一组：（空）。
    _借用_下一ID 为 0。
    _借用_错误列表 为 一组：（空）。
完。

可变 _并发_共享变量 为 一组：（空）。

并发_标记共享 接受 变量ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _并发_共享变量：
        若 _并发_共享变量 的 第 i 项 等于 变量ID：
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 _并发_共享变量、变量ID。
完。

并发_是共享 接受 变量ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _并发_共享变量：
        若 _并发_共享变量 的 第 i 项 等于 变量ID：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

并发_借用检查 接受 变量ID、借用类型：
    若 做 并发_是共享 用 变量ID 并且 借用类型 等于 借用_可变：
        返还 「数据竞争风险: 共享变量的可变借用需同步保护」。
    完。
    返还 空。
完。

本章 收束。
