《符号表》

引用《..·..·系统扩展·共享辅助》。

引用《..·..·标准库·基础》。

符号表_符号_变量 为 1。

符号表_符号_函数 为 2。

符号表_符号_参数 为 3。

符号表_符号_结构 为 4。

符号_字段 为 5。

符号表_符号_常量 为 6。

符号_内建 为 7。

符号_创建 接受 名称ID、符号类型、数据类型、深度：
    返还 一组：名称ID、符号类型、数据类型、深度。
完。

符号_名称 接受 s：
    返还 s 的 第 0 项。
完。

符号_类型 接受 s：
    返还 s 的 第 1 项。
完。

符号_数据类型 接受 s：
    返还 s 的 第 2 项。
完。

符号_深度 接受 s：
    返还 s 的 第 3 项。
完。

符号_偏移 接受 s：
    返还 s 的 第 4 项。
完。

符号_设偏移 接受 s、off：
    s 的 第 4 项 为 off。
完。

符号表_创建 接受 容量：
    返还 一组：一组：（空）、容量、0。
完。

符号表_符号表_大小 接受 t：
    返还 t 的 第 2 项。
完。

符号表_追加 接受 表、名称ID、符号类型、数据类型、深度：
    若 表 的 第 2 项 大于或等于 表 的 第 1 项：
        返还 0。
    完。
    符号 为 做 符号_创建 用 名称ID、符号类型、数据类型、深度。
    做 追加 用 表 的 第 0 项、符号。
    表 的 第 2 项 为 表 的 第 2 项 加 1。
    返还 1。
完。

符号表_符号表_查找 接受 表、名称ID：
    可变 i 为 表 的 第 2 项 减 1。
    只要 i 大于或等于 0：
        若 做 字符串相等 用 表 的 第 0 项 的 第 i 项 的 第 0 项、名称ID：
            返还 表 的 第 0 项 的 第 i 项。
        完。
        i 为 i 减 1。
    完。
    返还 0。
完。

符号表_查找类型 接受 表、名称ID、符号类型：
    可变 i 为 表 的 第 2 项 减 1。
    只要 i 大于或等于 0：
        符号 为 表 的 第 0 项 的 第 i 项。
        若 做 字符串相等 用 符号 的 第 0 项、名称ID：
            若 符号 的 第 1 项 等于 符号类型：
                返还 符号。
            完。
        完。
        i 为 i 减 1。
    完。
    返还 0。
完。

符号表_清除深度 接受 表、深度：
    可变 已删 为 0。
    只要 表 的 第 2 项 大于 0：
        last 为 表 的 第 0 项 的 第 表 的 第 2 项 减 1 项。
        若 last 的 第 3 项 大于或等于 深度：
            表 的 第 2 项 为 表 的 第 2 项 减 1。
            做 删除 用 表 的 第 0 项、表 的 第 2 项。
            已删 为 已删 加 1。
        否则：
            返还 已删。
        完。
    完。
    返还 已删。
完。

函数签名 接受 名称ID、参数数、返回类型：
    返还 一组：名称ID、参数数、一组：（空）、返回类型。
完。

符号表_签名_参数数 接受 sig：
    返还 sig 的 第 1 项。
完。

符号表_签名_返回类型 接受 sig：
    返还 sig 的 第 3 项。
完。

注册内建 接受 表：
    做 符号表_追加 用 表、1、符号_内建、0、0。
    做 符号表_追加 用 表、2、符号_内建、0、0。
    做 符号表_追加 用 表、3、符号_内建、0、0。
    返还 3。
完。

统计符号 接受 表、符号类型：
    可变 数 为 0。
    可变 i 为 0。
    只要 i 小于 表 的 第 2 项：
        若 表 的 第 0 项 的 第 i 项 的 第 1 项 等于 符号类型：
            数 为 数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数。
完。

符号哈希_桶数 为 256。

符号_哈希 接受 名称ID：
    可变 h 为 (名称ID 乘 2654435761 右移 16) 取余于 符号哈希_桶数。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    返还 h。
完。

哈希符号表_创建 接受 零参：
    可变 桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 符号哈希_桶数：
        做 追加 用 桶、一组：（空）。
        i 为 i 加 1。
    完。
    返还 一组：桶、0。
完。

哈希符号表_追加 接受 表、符号：
    可变 h 为 做 符号_哈希 用 符号 的 第 0 项。
    做 追加 用 表 的 第 0 项 的 第 h 项、符号。
    表 的 第 1 项 为 表 的 第 1 项 加 1。
完。

哈希符号表_查找 接受 表、名称ID：
    可变 h 为 做 符号_哈希 用 名称ID。
    桶 为 表 的 第 0 项 的 第 h 项。
    可变 i 为 做 计数 用 桶 减 1。
    只要 i 大于或等于 0：
        若 做 字符串相等 用 桶 的 第 i 项 的 第 0 项、名称ID：
            返还 桶 的 第 i 项。
        完。
        i 为 i 减 1。
    完。
    返还 0。
完。

哈希符号表_查找函数 接受 表、名称ID：
    可变 h 为 做 符号_哈希 用 名称ID。
    桶 为 表 的 第 0 项 的 第 h 项。
    可变 i 为 做 计数 用 桶 减 1。
    只要 i 大于或等于 0：
        若 做 字符串相等 用 桶 的 第 i 项 的 第 0 项、名称ID 并且 桶 的 第 i 项 的 第 1 项 等于 符号表_符号_函数：
            返还 桶 的 第 i 项。
        完。
        i 为 i 减 1。
    完。
    返还 0。
完。

哈希符号表_大小 接受 表：
    返还 表 的 第 1 项。
完。

符号表_快照 接受 表：
    返还 表 的 第 2 项。
完。

符号表_恢复 接受 表、快照：
    只要 表 的 第 2 项 大于 快照：
        表 的 第 2 项 为 表 的 第 2 项 减 1。
        若 表 的 第 2 项 小于 做 计数 用 表 的 第 0 项：
            做 删除 用 表 的 第 0 项、表 的 第 2 项。
        完。
    完。
完。

符号表_存在于深度 接受 表、名称ID、深度：
    可变 i 为 表 的 第 2 项 减 1。
    只要 i 大于或等于 0：
        符号 为 表 的 第 0 项 的 第 i 项。
        若 符号 的 第 3 项 小于 深度：
            返还 0。
        完。
        若 做 字符串相等 用 符号 的 第 0 项、名称ID 并且 符号 的 第 3 项 等于 深度：
            返还 1。
        完。
        i 为 i 减 1。
    完。
    返还 0。
完。

符号表_列出深度 接受 表、深度：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 表 的 第 2 项：
        若 表 的 第 0 项 的 第 i 项 的 第 3 项 等于 深度：
            做 追加 用 结果、表 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

符号表_导出 接受 表：
    可变 导出列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 表 的 第 2 项：
        符号 为 表 的 第 0 项 的 第 i 项。
        若 符号 的 第 3 项 等于 0：
            做 追加 用 导出列表、一组：符号 的 第 0 项、符号 的 第 1 项、符号 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
    返还 导出列表。
完。

符号表_导入 接受 表、导出列表：
    可变 导入数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 导出列表：
        项 为 导出列表 的 第 i 项。
        若 做 符号表_查找 用 表、项 的 第 0 项 等于 0：
            做 符号表_追加 用 表、项 的 第 0 项、项 的 第 1 项、项 的 第 2 项、0。
            导入数 为 导入数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 导入数。
完。

符号表_报告 接受 表：
    变量数 为 做 统计符号 用 表、符号表_符号_变量。
    函数数 为 做 统计符号 用 表、符号表_符号_函数。
    参数数 为 做 统计符号 用 表、符号表_符号_参数。
    结构数 为 做 统计符号 用 表、符号表_符号_结构。
    常量数 为 做 统计符号 用 表、符号表_符号_常量。
    内建数 为 做 统计符号 用 表、符号_内建。
    返还 一组：表 的 第 2 项、变量数、函数数、参数数、结构数、常量数、内建数。
完。

符号表_检查冲突 接受 表、名称ID、_新类型、深度：
    现有 为 做 符号表_查找 用 表、名称ID。
    若 现有 等于 0：
        返还 0。
    完。
    若 现有 的 第 3 项 等于 深度：
        返还 1。
    完。
    返还 2。
完。

符号表_查找重载 接受 表、名称ID：
    可变 重载列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 表 的 第 2 项：
        符号 为 表 的 第 0 项 的 第 i 项。
        若 做 字符串相等 用 符号 的 第 0 项、名称ID 并且 符号 的 第 1 项 等于 符号表_符号_函数：
            做 追加 用 重载列表、符号。
        完。
        i 为 i 加 1。
    完。
    返还 重载列表。
完。

可变 _符号表_注册数 为 0。

可变 _符号表_查找数 为 0。

可变 _符号表_冲突数 为 0。

符号表_哈希_笔画加权 接受 码点、容量：
    可变 stroke 为 码点 取余于 256。
    若 码点 大于或等于 19968 并且 码点 小于或等于 40959：
        stroke 为 4 加 (码点 减 19968) 乘 13 除以 20991。
    完。
    rot 为 stroke 取余于 23 加 1。
    h 为 (0x9E3779B9 乘 rot 加 码点) 取余于 容量。
    若 h 小于 0：
        返还 0 减 h。
    完。
    返还 h。
完。

本章 收束。
