《形式化验证引擎》

引用《..·..·标准库·基础》。

引用《..·..·编译器·标记常量》。

验证精度 为 10000。

域_底 为 负 99999999。

域_顶 为 99999999。

区间_创建 接受 最小、最大：
    若 最小 大于 最大：
        返还 一组：域_底、域_底。
    完。
    返还 一组：最小、最大。
完。

区间_是底 接受 区间：
    返还 区间 的 第 0 项 等于 域_底 并且 区间 的 第 1 项 等于 域_底。
完。

区间_是顶 接受 区间：
    返还 区间 的 第 0 项 等于 0 减 域_顶 并且 区间 的 第 1 项 等于 域_顶。
完。

区间_并 接受 A、B：
    若 做 区间_是底 用 A：
        返还 B。
    完。
    若 做 区间_是底 用 B：
        返还 A。
    完。
    可变 最小 为 A 的 第 0 项。
    若 B 的 第 0 项 小于 最小：
        最小 为 B 的 第 0 项。
    完。
    可变 最大 为 A 的 第 1 项。
    若 B 的 第 1 项 大于 最大：
        最大 为 B 的 第 1 项。
    完。
    返还 一组：最小、最大。
完。

区间_交 接受 A、B：
    可变 最小 为 A 的 第 0 项。
    若 B 的 第 0 项 大于 最小：
        最小 为 B 的 第 0 项。
    完。
    可变 最大 为 A 的 第 1 项。
    若 B 的 第 1 项 小于 最大：
        最大 为 B 的 第 1 项。
    完。
    若 最小 大于 最大：
        返还 一组：域_底、域_底。
    完。
    返还 一组：最小、最大。
完。

区间_加 接受 A、B：
    若 做 区间_是底 用 A 或者 做 区间_是底 用 B：
        返还 一组：域_底、域_底。
    完。
    返还 一组：A 的 第 0 项 加 B 的 第 0 项、A 的 第 1 项 加 B 的 第 1 项。
完。

区间_乘 接受 A、B：
    若 做 区间_是底 用 A 或者 做 区间_是底 用 B：
        返还 一组：域_底、域_底。
    完。
    p1 为 A 的 第 0 项 乘 B 的 第 0 项。
    p2 为 A 的 第 0 项 乘 B 的 第 1 项。
    p3 为 A 的 第 1 项 乘 B 的 第 0 项。
    p4 为 A 的 第 1 项 乘 B 的 第 1 项。
    可变 最小 为 p1。
    若 p2 小于 最小：
        最小 为 p2。
    完。
    若 p3 小于 最小：
        最小 为 p3。
    完。
    若 p4 小于 最小：
        最小 为 p4。
    完。
    可变 最大 为 p1。
    若 p2 大于 最大：
        最大 为 p2。
    完。
    若 p3 大于 最大：
        最大 为 p3。
    完。
    若 p4 大于 最大：
        最大 为 p4。
    完。
    返还 一组：最小、最大。
完。

区间_加宽 接受 旧、新：
    若 做 区间_是底 用 旧：
        返还 新。
    完。
    可变 下 为 旧 的 第 0 项。
    可变 上 为 旧 的 第 1 项。
    若 新 的 第 0 项 小于 旧 的 第 0 项：
        下 为 0 减 域_顶。
    完。
    若 新 的 第 1 项 大于 旧 的 第 1 项：
        上 为 域_顶。
    完。
    返还 一组：下、上。
完。

区间_收窄 接受 旧、新：
    可变 下 为 旧 的 第 0 项。
    可变 上 为 旧 的 第 1 项。
    若 旧 的 第 0 项 等于 0 减 域_顶：
        下 为 新 的 第 0 项。
    完。
    若 旧 的 第 1 项 等于 域_顶：
        上 为 新 的 第 1 项。
    完。
    返还 一组：下、上。
完。

抽象状态_创建 接受 变量数：
    可变 状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 变量数：
        做 追加 用 状态、一组：域_底、域_底。
        i 为 i 加 1。
    完。
    返还 状态。
完。

抽象状态_合并 接受 状态A、状态B：
    大小 为 做 计数 用 状态A。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        做 追加 用 结果、做 区间_并 用 状态A 的 第 i 项、状态B 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

抽象_赋值 接受 状态、变量索引、值区间：
    状态 的 第 变量索引 项 为 值区间。
    返还 状态。
完。

抽象_条件_大于 接受 状态、变量索引、_常量值：
    可变 新状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 状态：
        若 i 等于 变量索引：
            旧 为 状态 的 第 i 项。
            新最小 为 常量 加 1。
            若 旧 的 第 0 项 大于 新最小：
                新最小 为 旧 的 第 0 项。
            完。
            若 新最小 大于 旧 的 第 1 项：
                做 追加 用 新状态、一组：域_底、域_底。
            否则：
                做 追加 用 新状态、一组：新最小、旧 的 第 1 项。
            完。
        否则：
            做 追加 用 新状态、状态 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 新状态。
完。

抽象_条件_小于 接受 状态、变量索引、_常量值：
    可变 新状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 状态：
        若 i 等于 变量索引：
            旧 为 状态 的 第 i 项。
            新最大 为 常量 减 1。
            若 旧 的 第 1 项 小于 新最大：
                新最大 为 旧 的 第 1 项。
            完。
            若 旧 的 第 0 项 大于 新最大：
                做 追加 用 新状态、一组：域_底、域_底。
            否则：
                做 追加 用 新状态、一组：旧 的 第 0 项、新最大。
            完。
        否则：
            做 追加 用 新状态、状态 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 新状态。
完。

不动点_相等 接受 状态A、状态B：
    可变 i 为 0。
    只要 i 小于 做 计数 用 状态A：
        若 状态A 的 第 i 项 的 第 0 项 不等于 状态B 的 第 i 项 的 第 0 项：
            返还 0。
        完。
        若 状态A 的 第 i 项 的 第 1 项 不等于 状态B 的 第 i 项 的 第 1 项：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

不动点_迭代 接受 初始状态、_循环体函数标识、最大迭代：
    可变 当前 为 初始状态。
    可变 迭代 为 0。
    只要 迭代 小于 最大迭代：
        可变 新状态 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 当前：
            做 追加 用 新状态、当前 的 第 i 项。
            i 为 i 加 1。
        完。
        合并 为 做 抽象状态_合并 用 当前、新状态。
        若 做 不动点_相等 用 当前、合并：
            返还 当前。
        完。
        若 迭代 大于 3：
            i 为 0。
            只要 i 小于 做 计数 用 合并：
                合并 的 第 i 项 为 做 区间_加宽 用 当前 的 第 i 项、合并 的 第 i 项。
                i 为 i 加 1。
            完。
        完。
        当前 为 合并。
        迭代 为 迭代 加 1。
    完。
    返还 当前。
完。

验证_成功 为 1。

验证_失败 为 0。

验证_不确定 为 2。

验证_范围 接受 状态、变量索引、安全最小、安全最大：
    区间 为 状态 的 第 变量索引 项。
    若 做 区间_是底 用 区间：
        返还 验证_成功。
    完。
    若 区间 的 第 0 项 大于或等于 安全最小 并且 区间 的 第 1 项 小于或等于 安全最大：
        返还 验证_成功。
    完。
    若 区间 的 第 0 项 大于 安全最大 或者 区间 的 第 1 项 小于 安全最小：
        返还 验证_失败。
    完。
    返还 验证_不确定。
完。

验证_非零 接受 状态、变量索引：
    区间 为 状态 的 第 变量索引 项。
    若 区间 的 第 0 项 大于 0 或者 区间 的 第 1 项 小于 0：
        返还 验证_成功。
    完。
    若 区间 的 第 0 项 等于 0 并且 区间 的 第 1 项 等于 0：
        返还 验证_失败。
    完。
    返还 验证_不确定。
完。

验证_数组越界 接受 状态、索引变量、数组大小：
    区间 为 状态 的 第 索引变量 项。
    若 区间 的 第 0 项 大于或等于 0 并且 区间 的 第 1 项 小于 数组大小：
        返还 验证_成功。
    完。
    若 区间 的 第 0 项 大于或等于 数组大小 或者 区间 的 第 1 项 小于 0：
        返还 验证_失败。
    完。
    返还 验证_不确定。
完。

霍尔三元组_创建 接受 前置条件、语句、后置条件：
    返还 一组：前置条件、语句、后置条件。
完。

最弱前置条件_赋值 接受 后置条件、_变量、_表达式值：
    返还 后置条件。
完。

最弱前置条件_条件 接受 _条件、wp1、wp2：
    返还 做 抽象状态_合并 用 wp1、wp2。
完。

符号_具体 为 0。

验证_符号_变量 为 1。

符号_运算 为 2。

符号值_具体 接受 值：
    返还 一组：符号_具体、值。
完。

符号值_变量 接受 名称：
    返还 一组：验证_符号_变量、名称。
完。

符号值_运算 接受 运算符、左、右：
    若 左 的 第 0 项 等于 符号_具体 并且 右 的 第 0 项 等于 符号_具体：
        若 运算符 等于 0：
            返还 做 符号值_具体 用 左 的 第 1 项 加 右 的 第 1 项。
        完。
        若 运算符 等于 1：
            返还 做 符号值_具体 用 左 的 第 1 项 减 右 的 第 1 项。
        完。
        若 运算符 等于 2：
            返还 做 符号值_具体 用 左 的 第 1 项 乘 右 的 第 1 项。
        完。
        若 运算符 等于 3 并且 右 的 第 1 项 不等于 0：
            返还 做 符号值_具体 用 左 的 第 1 项 除以 右 的 第 1 项。
        完。
    完。
    返还 一组：符号_运算、一组：运算符、左、右。
完。

路径条件_创建 接受 零参：
    返还 一组：（空）。
完。

路径条件_追加 接受 路径、约束：
    做 追加 用 路径、约束。
    返还 路径。
完。

路径条件_可满足 接受 路径：
    可变 i 为 0。
    只要 i 小于 做 计数 用 路径：
        约束 为 路径 的 第 i 项。
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 路径：
            若 约束 的 第 1 项 等于 路径 的 第 j 项 的 第 1 项：
                若 约束 的 第 0 项 等于 1 并且 路径 的 第 j 项 的 第 0 项 等于 2：
                    若 约束 的 第 2 项 大于或等于 路径 的 第 j 项 的 第 2 项：
                        返还 0。
                    完。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

验证报告_创建 接受 零参：
    返还 一组：0、0、0、一组：（空）、一组：（空）。
完。

验证报告_记录 接受 报告、类型、位置、描述：
    若 类型 等于 验证_成功：
        报告 的 第 0 项 为 报告 的 第 0 项 加 1。
        做 追加 用 报告 的 第 4 项、一组：位置、描述。
    否则若 类型 等于 验证_失败：
        报告 的 第 1 项 为 报告 的 第 1 项 加 1。
        做 追加 用 报告 的 第 3 项、一组：位置、「error」、描述。
    否则：
        报告 的 第 2 项 为 报告 的 第 2 项 加 1。
        做 追加 用 报告 的 第 3 项、一组：位置、「warning」、描述。
    完。
完。

验证报告_安全度 接受 报告：
    总数 为 报告 的 第 0 项 加 报告 的 第 1 项 加 报告 的 第 2 项。
    若 总数 等于 0：
        返还 验证精度。
    完。
    返还 报告 的 第 0 项 乘 验证精度 除以 总数。
完。

验证报告_打印 接受 报告：
    做 打印 用 「═══ 形式化验证报告 ═══」。
    做 打印 用 「 已证明安全: 」。
    做 打印 用 报告 的 第 0 项。
    做 打印 用 「 证明失败: 」。
    做 打印 用 报告 的 第 1 项。
    做 打印 用 「 无法确定: 」。
    做 打印 用 报告 的 第 2 项。
    安全度 为 做 验证报告_安全度 用 报告。
    做 打印 用 「 安全度: 」。
    做 打印 用 安全度 乘 100 除以 验证精度。
完。

验证_循环不变式 接受 初始值、步进函数、不变式、最大迭代：
    可变 当前 为 初始值。
    可变 i 为 0。
    只要 i 小于 最大迭代：
        若 非 做 不变式 用 当前：
            返还 一组：假、「不变式在第」 加 做 转字符串 用 i 加 「次迭代违反」、当前。
        完。
        当前 为 做 步进函数 用 当前。
        i 为 i 加 1。
    完。
    返还 一组：真、「不变式在」 加 做 转字符串 用 最大迭代 加 「次迭代中保持」、当前。
完。

验证_范围检查 接受 值、最小、最大：
    若 值 小于 最小：
        返还 一组：假、「下溢: 」 加 做 转字符串 用 值 加 「<」 加 做 转字符串 用 最小。
    完。
    若 值 大于 最大：
        返还 一组：假、「上溢: 」 加 做 转字符串 用 值 加 「>」 加 做 转字符串 用 最大。
    完。
    返还 一组：真、「范围内」。
完。

验证_除零 接受 除数：
    若 除数 等于 0：
        返还 一组：假、「division by zero」。
    完。
    返还 一组：真、「安全」。
完。

验证_数组越界_运行时 接受 索引、长度：
    若 索引 小于 0：
        返还 一组：假、「负索引」。
    完。
    若 索引 大于或等于 长度：
        返还 一组：假、「越界: 」 加 做 转字符串 用 索引 加 「>=」 加 做 转字符串 用 长度。
    完。
    返还 一组：真、「安全」。
完。

验证_空指针 接受 指针：
    若 指针 等于 空 或者 指针 等于 0：
        返还 一组：假、「空指针」。
    完。
    返还 一组：真、「安全」。
完。

契约_前置条件 接受 条件、描述：
    若 非 条件：
        返还 一组：假、「前置条件违反: 」 加 描述。
    完。
    返还 一组：真、「前置条件满足」。
完。

契约_后置条件 接受 条件、描述：
    若 非 条件：
        返还 一组：假、「后置条件违反: 」 加 描述。
    完。
    返还 一组：真、「后置条件满足」。
完。

契约_不变量 接受 条件、描述：
    若 非 条件：
        返还 一组：假、「不变量违反: 」 加 描述。
    完。
    返还 一组：真、「不变量保持」。
完。

可变 _验证_总检查数 为 0。

可变 _验证_通过数 为 0。

可变 _验证_失败数 为 0。

验证_记录 接受 通过：
    _验证_总检查数 为 _验证_总检查数 加 1。
    若 通过：
        _验证_通过数 为 _验证_通过数 加 1。
    否则：
        _验证_失败数 为 _验证_失败数 加 1。
    完。
完。

验证_通过率 接受 零参：
    若 _验证_总检查数 等于 0：
        返还 0。
    完。
    返还 _验证_通过数 乘 100 除以 _验证_总检查数。
完。

验证_安全等级 接受 零参：
    率 为 做 验证_通过率。
    若 率 等于 标记_数字：
        返还 「完全验证(形式化安全)」。
    完。
    若 率 大于 95：
        返还 「高安全」。
    完。
    若 率 大于 80：
        返还 「中等安全」。
    完。
    返还 「低安全(需修复)」。
完。

验证_总数 接受 零参：
    返还 _验证_总检查数。
完。

验证_通过数 接受 零参：
    返还 _验证_通过数。
完。

验证_失败数 接受 零参：
    返还 _验证_失败数。
完。

验证_重置 接受 零参：
    _验证_总检查数 为 0。
    _验证_通过数 为 0。
    _验证_失败数 为 0。
完。

验证_批量检查 接受 条件列表：
    可变 全通过 为 假。
    可变 失败项 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条件列表：
        若 非 条件列表 的 第 i 项 的 第 0 项：
            全通过 为 假。
            做 追加 用 失败项、条件列表 的 第 i 项 的 第 1 项。
        完。
        做 验证_记录 用 条件列表 的 第 i 项 的 第 0 项。
        i 为 i 加 1。
    完。
    返还 一组：全通过、失败项。
完。

验证_断言相等 接受 实际、期望、描述：
    若 实际 不等于 期望：
        返还 一组：假、描述 加 「: 期望」 加 做 转字符串 用 期望 加 「实际」 加 做 转字符串 用 实际。
    完。
    返还 一组：真、描述 加 「: 通过」。
完。

验证_是否安全 接受 零参：
    返还 做 验证_通过率 等于 标记_数字。
完。

形式化验证_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「验证总数」、做 转字符串 用 做 验证_总数。
    做 字典设 用 r、「通过数」、做 转字符串 用 做 验证_通过数。
    做 字典设 用 r、「失败数」、做 转字符串 用 做 验证_失败数。
    做 字典设 用 r、「通过率%」、做 转字符串 用 做 验证_通过率。
    做 字典设 用 r、「全部安全」、做 转字符串 用 做 验证_是否安全。
    返还 r。
完。

可变 _验证白名单 为 做 字典。

可变 _白名单_默认策略 为 「阻塞」。

验证白名单_添加 接受 模块名、策略：
    做 字典设 用 _验证白名单、模块名、策略。
完。

验证白名单_查询 接受 模块名：
    策略 为 做 字典取 用 _验证白名单、模块名。
    若 策略 不等于 空：
        返还 策略。
    完。
    返还 _白名单_默认策略。
完。

验证白名单_设默认 接受 策略：
    _白名单_默认策略 为 策略。
完。

验证白名单_是否放行 接受 模块名：
    策略 为 做 验证白名单_查询 用 模块名。
    返还 策略 等于 「放行」。
完。

验证白名单_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「白名单模块数」、做 转字符串 用 做 计数 用 做 字典键 用 _验证白名单。
    做 字典设 用 r、「默认策略」、_白名单_默认策略。
    返还 r。
完。

安全级_严格 为 0。

安全级_标准 为 1。

安全级_宽松 为 2。

可变 _当前安全级 为 安全级_标准。

安全门控_设级别 接受 级别：
    _当前安全级 为 级别。
完。

安全门控_获取级别 接受 零参：
    返还 _当前安全级。
完。

安全门控_应阻塞构建 接受 验证通过率、效果违规数、能力违规数：
    若 _当前安全级 等于 安全级_严格：
        返还 验证通过率 小于 100 或者 效果违规数 大于 0 或者 能力违规数 大于 0。
    完。
    若 _当前安全级 等于 安全级_标准：
        返还 效果违规数 大于 0 或者 能力违规数 大于 0。
    完。
    返还 假。
完。

安全门控_profile映射 接受 profile名：
    若 profile名 等于 「kernel-qemu」 或者 profile名 等于 「kernel-physical」：
        返还 安全级_严格。
    完。
    若 profile名 等于 「linux-user」：
        返还 安全级_标准。
    完。
    返还 安全级_宽松。
完。

安全门_报告 接受 验证通过率、效果违规数、能力违规数：
    r 为 做 字典。
    做 字典设 用 r、「安全级别」、做 转字符串 用 _当前安全级。
    做 字典设 用 r、「验证通过率%」、做 转字符串 用 验证通过率。
    做 字典设 用 r、「效果违规数」、做 转字符串 用 效果违规数。
    做 字典设 用 r、「能力违规数」、做 转字符串 用 能力违规数。
    做 字典设 用 r、「应阻塞构建」、做 转字符串 用 做 安全门控_应阻塞构建 用 验证通过率、效果违规数、能力违规数。
    做 字典设 用 r、「白名单」、做 验证白名单_报告。
    返还 r。
完。

内存安全_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「验证」、做 形式化验证_报告。
    做 字典设 用 r、「门控级别」、做 转字符串 用 _当前安全级。
    做 字典设 用 r、「白名单模块数」、做 转字符串 用 做 计数 用 做 字典键 用 _验证白名单。
    返还 r。
完。

本章 收束。
