《数字扫描》

数字结果 接受 类型、值、小数位、错误：
    返还 一组：类型、值、小数位、错误。
完。

检测前缀 接受 首字符、次字符：
    若 首字符 不等于 48：
        返还 数制_十进制。
    完。
    若 次字符 等于 标记_建：
        返还 数制_十六进制。
    完。
    若 次字符 等于 字符_大写X：
        返还 数制_十六进制。
    完。
    若 次字符 等于 标记_可变：
        返还 数制_八进制。
    完。
    若 次字符 等于 字符_大写O：
        返还 数制_八进制。
    完。
    若 次字符 等于 标记_冒号：
        返还 数制_二进制。
    完。
    若 次字符 等于 字符_大写B：
        返还 数制_二进制。
    完。
    返还 数制_十进制。
完。

解析十进制 接受 数字列表：
    _数扫_整数数 为 _数扫_整数数 加 1。
    长 为 做 计数 用 数字列表。
    可变 值 为 0。
    可变 i 为 0。
    只要 i 小于 长：
        d 为 数字列表 的 第 i 项。
        若 d 小于 0：
            返还 做 数字结果 用 数制_错误、0、1、0。
        完。
        若 d 大于 9：
            返还 做 数字结果 用 数制_错误、0、1、0。
        完。
        值 为 值 乘 10 加 d。
        i 为 i 加 1。
    完。
    返还 做 数字结果 用 数制_十进制、值、0、0。
完。

数字扫描_解析十六进制 接受 数字列表：
    _数扫_整数数 为 _数扫_整数数 加 1。
    长 为 做 计数 用 数字列表。
    若 长 等于 0：
        _数扫_错误数 为 _数扫_错误数 加 1。
        返还 做 数字结果 用 数制_错误、0、2、0。
    完。
    可变 值 为 0。
    可变 i 为 0。
    只要 i 小于 长：
        d 为 数字列表 的 第 i 项。
        若 d 小于 0：
            返还 做 数字结果 用 数制_错误、0、2、0。
        完。
        若 d 大于 15：
            返还 做 数字结果 用 数制_错误、0、2、0。
        完。
        值 为 值 乘 16 加 d。
        i 为 i 加 1。
    完。
    返还 做 数字结果 用 数制_十六进制、值、0、0。
完。

数字扫描_解析八进制 接受 数字列表：
    长 为 做 计数 用 数字列表。
    若 长 等于 0：
        返还 做 数字结果 用 数制_错误、0、3、0。
    完。
    可变 值 为 0。
    可变 i 为 0。
    只要 i 小于 长：
        d 为 数字列表 的 第 i 项。
        若 d 小于 0：
            返还 做 数字结果 用 数制_错误、0、3、0。
        完。
        若 d 大于 7：
            返还 做 数字结果 用 数制_错误、0、3、0。
        完。
        值 为 值 乘 8 加 d。
        i 为 i 加 1。
    完。
    返还 做 数字结果 用 数制_八进制、值、0、0。
完。

数字扫描_解析二进制 接受 数字列表：
    长 为 做 计数 用 数字列表。
    若 长 等于 0：
        返还 做 数字结果 用 数制_错误、0、4、0。
    完。
    可变 值 为 0。
    可变 i 为 0。
    只要 i 小于 长：
        d 为 数字列表 的 第 i 项。
        若 d 不等于 0 并且 d 不等于 1：
            返还 做 数字结果 用 数制_错误、0、4、0。
        完。
        值 为 值 乘 2 加 d。
        i 为 i 加 1。
    完。
    返还 做 数字结果 用 数制_二进制、值、0、0。
完。

是数字分隔符 接受 c：
    若 c 等于 标记_右方：
        返还 1。
    完。
    返还 0。
完。

十进制位数 接受 值：
    若 值 等于 0：
        返还 1。
    完。
    可变 n 为 0。
    可变 v 为 值。
    只要 v 大于 0：
        v 为 v 除以 10。
        n 为 n 加 1。
    完。
    返还 n。
完。

十六进制位数 接受 值：
    若 值 等于 0：
        返还 1。
    完。
    可变 n 为 0。
    可变 v 为 值。
    只要 v 大于 0：
        v 为 v 除以 16。
        n 为 n 加 1。
    完。
    返还 n。
完。

在范围内 接受 值、最小、最大：
    若 值 小于 最小：
        返还 0。
    完。
    若 值 大于 最大：
        返还 0。
    完。
    返还 1。
完。

扫描_浮点数 接受 源、起始：
    _数扫_浮点数 为 _数扫_浮点数 加 1。
    可变 位置 为 起始。
    可变 整数部 为 0。
    若 位置 小于 做 计数 用 源 并且 做 编码 用 做 子串 用 源、位置、1 等于 字符_减号：
        负号 为 真。
        位置 为 位置 加 1。
    完。
    可变 继续整数 为 真。
    只要 位置 小于 做 计数 用 源 并且 继续整数：
        c 为 做 编码 用 做 子串 用 源、位置、1。
        若 c 大于或等于 48 并且 c 小于或等于 57：
            整数部 为 整数部 乘 10 加 (c 减 48)。
            位置 为 位置 加 1。
        否则：
            继续整数 为 假。
        完。
    完。
    可变 小数部 为 0。
    可变 小数位 为 0。
    若 位置 小于 做 计数 用 源 并且 做 编码 用 做 子串 用 源、位置、1 等于 46：
        位置 为 位置 加 1。
        可变 继续小数 为 真。
        只要 位置 小于 做 计数 用 源 并且 继续小数：
            c 为 做 编码 用 做 子串 用 源、位置、1。
            若 c 大于或等于 48 并且 c 小于或等于 57：
                小数部 为 小数部 乘 10 加 (c 减 48)。
                小数位 为 小数位 加 1。
                位置 为 位置 加 1。
            否则：
                继续小数 为 假。
            完。
        完。
    完。
    可变 指数 为 0。
    可变 指数负 为 假。
    若 位置 小于 做 计数 用 源：
        ec 为 做 编码 用 做 子串 用 源、位置、1。
        若 ec 等于 标记_字串 或者 ec 等于 69：
            位置 为 位置 加 1。
            若 位置 小于 做 计数 用 源 并且 做 编码 用 做 子串 用 源、位置、1 等于 字符_减号：
                指数负 为 真。
                位置 为 位置 加 1。
            否则若 位置 小于 做 计数 用 源 并且 做 编码 用 做 子串 用 源、位置、1 等于 43：
                位置 为 位置 加 1。
            完。
            可变 继续指数 为 真。
            只要 位置 小于 做 计数 用 源 并且 继续指数：
                c 为 做 编码 用 做 子串 用 源、位置、1。
                若 c 大于或等于 48 并且 c 小于或等于 57：
                    指数 为 指数 乘 10 加 (c 减 48)。
                    位置 为 位置 加 1。
                否则：
                    继续指数 为 假。
                完。
            完。
        完。
    完。
    若 指数负：
        指数 为 0 减 指数。
    完。
    返还 一组：整数部、小数部、小数位、指数、负号、位置。
完。

数字_十转二 接受 值：
    若 值 等于 0：
        返还 「0」。
    完。
    可变 结果 为 「」。
    可变 n 为 值。
    若 n 小于 0：
        n 为 0 减 n。
    完。
    只要 n 大于 0：
        若 n 取余于 2 等于 1：
            结果 为 「1」 加 结果。
        否则：
            结果 为 「0」 加 结果。
        完。
        n 为 n 除以 2。
    完。
    若 值 小于 0：
        结果 为 「-」 加 结果。
    完。
    返还 结果。
完。

数字_十转八 接受 值：
    若 值 等于 0：
        返还 「0」。
    完。
    可变 结果 为 「」。
    可变 n 为 值。
    若 n 小于 0：
        n 为 0 减 n。
    完。
    只要 n 大于 0：
        结果 为 做 转字符串 用 n 取余于 8 加 结果。
        n 为 n 除以 8。
    完。
    若 值 小于 0：
        结果 为 「-」 加 结果。
    完。
    返还 结果。
完。

数字_十转十六 接受 值：
    若 值 等于 0：
        返还 「0」。
    完。
    可变 结果 为 「」。
    可变 hex表 为 「0123456789ABCDEF」。
    可变 n 为 值。
    若 n 小于 0：
        n 为 0 减 n。
    完。
    只要 n 大于 0：
        结果 为 做 子串 用 hex表、n 取余于 16、1 加 结果。
        n 为 n 除以 16。
    完。
    若 值 小于 0：
        结果 为 「-」 加 结果。
    完。
    返还 结果。
完。

是有效整数 接受 源、起始、长度：
    可变 i 为 起始。
    若 i 小于 起始 加 长度 并且 做 编码 用 做 子串 用 源、i、1 等于 字符_减号：
        i 为 i 加 1。
    完。
    若 i 大于或等于 起始 加 长度：
        返还 0。
    完。
    只要 i 小于 起始 加 长度：
        c 为 做 编码 用 做 子串 用 源、i、1。
        若 c 小于 48 或者 c 大于 57：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

是有效浮点 接受 源、起始、长度：
    可变 有点 为 假。
    可变 有数字 为 假。
    可变 i 为 起始。
    若 i 小于 起始 加 长度 并且 做 编码 用 做 子串 用 源、i、1 等于 字符_减号：
        i 为 i 加 1。
    完。
    只要 i 小于 起始 加 长度：
        c 为 做 编码 用 做 子串 用 源、i、1。
        若 c 等于 46：
            若 有点：
                返还 0。
            完。
            有点 为 真。
        否则若 c 大于或等于 48 并且 c 小于或等于 57：
            有数字 为 真。
        否则若 c 等于 标记_字串 或者 c 等于 69：
            若 非 有数字：
                返还 0。
            完。
            i 为 i 加 1。
            若 i 小于 起始 加 长度：
                ec 为 做 编码 用 做 子串 用 源、i、1。
                若 ec 等于 标记_大于 或者 ec 等于 标记_大等：
                    i 为 i 加 1。
                完。
            完。
            只要 i 小于 起始 加 长度：
                c2 为 做 编码 用 做 子串 用 源、i、1。
                若 c2 小于 48 或者 c2 大于 57：
                    返还 0。
                完。
                i 为 i 加 1。
            完。
            返还 1。
        否则：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 有数字。
完。

数字扫描_数字_千分位 接受 值：
    s 为 做 转字符串 用 值。
    可变 结果 为 「」。
    可变 计 为 0。
    可变 i 为 做 计数 用 s 减 1。
    只要 i 大于或等于 0：
        若 计 大于 0 并且 计 取余于 3 等于 0：
            结果 为 「,」 加 结果。
        完。
        结果 为 做 子串 用 s、i、1 加 结果。
        计 为 计 加 1。
        i 为 i 减 1。
    完。
    返还 结果。
完。

可变 _数扫_整数数 为 0。

可变 _数扫_浮点数 为 0。

可变 _数扫_错误数 为 0。

数扫_记录整数 接受 零参：
    _数扫_整数数 为 _数扫_整数数 加 1。
完。

数扫_记录浮点 接受 零参：
    _数扫_浮点数 为 _数扫_浮点数 加 1。
完。

数扫_记录错误 接受 零参：
    _数扫_错误数 为 _数扫_错误数 加 1。
完。

本章 收束。
