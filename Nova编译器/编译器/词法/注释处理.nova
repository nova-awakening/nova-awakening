《注释处理》

引用《..·..·标准库·基础》。

引用《..·..·编译器·标记常量》。

注类_无 为 0。

注类_单行 为 1。

注类_多行 为 2。

注类_文档 为 3。

注状_正常 为 0。

注状_未终止 为 1。

注状_过深 为 2。

检测注释 接受 c1、c2、c3：
    若 c1 等于 47：
        若 c2 等于 47：
            若 c3 等于 47：
                返还 注类_文档。
            完。
            返还 注类_单行。
        完。
        若 c2 等于 标记_小于：
            返还 注类_多行。
        完。
    完。
    返还 注类_无。
完。

单行注释长度 接受 源码、起始：
    长 为 做 计数 用 源码。
    可变 i 为 起始。
    只要 i 小于 长：
        c 为 做 编码 用 做 子串 用 源码、i、1。
        若 c 等于 字符_换行：
            返还 i 减 起始。
        完。
        若 c 等于 字符_回车：
            返还 i 减 起始。
        完。
        i 为 i 加 1。
    完。
    返还 i 减 起始。
完。

多行注释长度 接受 源码、起始：
    长 为 做 计数 用 源码。
    可变 i 为 起始 加 2。
    可变 深度 为 1。
    可变 最大深度 为 1。
    只要 i 小于 长 减 1：
        c 为 做 编码 用 做 子串 用 源码、i、1。
        n 为 做 编码 用 做 子串 用 源码、i 加 1、1。
        若 c 等于 字符_斜杠：
            若 n 等于 标记_小于：
                深度 为 深度 加 1。
                若 深度 大于 最大深度：
                    最大深度 为 深度。
                完。
                若 深度 大于 16：
                    返还 一组：i 减 起始、注状_过深、深度。
                完。
                i 为 i 加 2。
            否则：
                i 为 i 加 1。
            完。
        否则：
            若 c 等于 标记_小于：
                若 n 等于 47：
                    深度 为 深度 减 1。
                    若 深度 等于 0：
                        返还 一组：i 加 2 减 起始、注状_正常、最大深度。
                    完。
                    i 为 i 加 2。
                否则：
                    i 为 i 加 1。
                完。
            否则：
                i 为 i 加 1。
            完。
        完。
    完。
    返还 一组：长 减 起始、注状_未终止、深度。
完。

注释行数 接受 源码、起始、长度：
    可变 行 为 0。
    可变 i 为 起始。
    终止 为 起始 加 长度。
    只要 i 小于 终止：
        若 i 小于 做 计数 用 源码：
            若 做 编码 用 做 子串 用 源码、i、1 等于 字符_换行：
                行 为 行 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 行。
完。

是空行 接受 源码、起始、长度：
    可变 i 为 起始。
    终止 为 起始 加 长度。
    只要 i 小于 终止：
        若 i 小于 做 计数 用 源码：
            c 为 做 编码 用 做 子串 用 源码、i、1。
            若 c 不等于 字符_空格 并且 c 不等于 字符_制表 并且 c 不等于 字符_换行 并且 c 不等于 字符_回车：
                返还 0。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

文档内容偏移 接受 源码、起始：
    可变 i 为 起始 加 3。
    长 为 做 计数 用 源码。
    只要 i 小于 长：
        若 做 编码 用 做 子串 用 源码、i、1 等于 字符_空格：
            i 为 i 加 1。
        否则：
            返还 i。
        完。
    完。
    返还 i。
完。

注释_提取文档块 接受 源码、起始：
    可变 行列表 为 一组：（空）。
    可变 位置 为 起始。
    长 为 做 计数 用 源码。
    只要 位置 小于 长：
        若 位置 加 2 小于 长 并且 做 子串 用 源码、位置、3 等于 「///」：
            可变 行起始 为 位置 加 3。
            只要 行起始 小于 长 并且 做 编码 用 做 子串 用 源码、行起始、1 等于 字符_空格：
                行起始 为 行起始 加 1。
            完。
            可变 行结束 为 行起始。
            只要 行结束 小于 长 并且 做 编码 用 做 子串 用 源码、行结束、1 不等于 10：
                行结束 为 行结束 加 1。
            完。
            做 追加 用 行列表、做 子串 用 源码、行起始、行结束 减 行起始。
            位置 为 行结束 加 1。
        否则：
            返还 行列表。
        完。
    完。
    返还 行列表。
完。

注释_提取参数说明 接受 文档行列表：
    可变 参数列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档行列表：
        行 为 文档行列表 的 第 i 项。
        若 做 计数 用 行 大于 3 并且 做 子串 用 行、0、3 等于 「@参数」：
            做 追加 用 参数列表、做 子串 用 行、3、做 计数 用 行 减 3。
        完。
        若 做 计数 用 行 大于 6 并且 做 子串 用 行、0、6 等于 「@param」：
            做 追加 用 参数列表、做 子串 用 行、6、做 计数 用 行 减 6。
        完。
        i 为 i 加 1。
    完。
    返还 参数列表。
完。

注释_提取返回说明 接受 文档行列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档行列表：
        行 为 文档行列表 的 第 i 项。
        若 做 计数 用 行 大于 3 并且 做 子串 用 行、0、3 等于 「@返回」：
            返还 做 子串 用 行、3、做 计数 用 行 减 3。
        完。
        若 做 计数 用 行 大于 7 并且 做 子串 用 行、0、7 等于 「@return」：
            返还 做 子串 用 行、7、做 计数 用 行 减 7。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

注释_提取标记 接受 源码、标记、标记长、类型名：
    可变 结果 为 一组：（空）。
    可变 位置 为 0。
    长 为 做 计数 用 源码。
    可变 行号 为 1。
    只要 位置 小于 长：
        若 位置 加 标记长 小于 长 并且 做 子串 用 源码、位置、标记长 等于 标记：
            可变 内容起始 为 位置 加 标记长。
            若 内容起始 小于 长 并且 做 编码 用 做 子串 用 源码、内容起始、1 等于 58：
                内容起始 为 内容起始 加 1。
            完。
            只要 内容起始 小于 长 并且 做 编码 用 做 子串 用 源码、内容起始、1 等于 字符_空格：
                内容起始 为 内容起始 加 1。
            完。
            可变 内容结束 为 内容起始。
            只要 内容结束 小于 长 并且 做 编码 用 做 子串 用 源码、内容结束、1 不等于 10：
                内容结束 为 内容结束 加 1。
            完。
            做 追加 用 结果、一组：行号、类型名、做 子串 用 源码、内容起始、内容结束 减 内容起始。
        完。
        若 做 编码 用 做 子串 用 源码、位置、1 等于 字符_换行：
            行号 为 行号 加 1。
        完。
        位置 为 位置 加 1。
    完。
    返还 结果。
完。

注释_提取待办 接受 源码：
    返还 做 注释_提取标记 用 源码、「TODO」、4、「待办」。
完。

注释_提取待修 接受 源码：
    返还 做 注释_提取标记 用 源码、「FIXME」、5、「待修」。
完。

注释_统计行数 接受 源码：
    可变 总行 为 0。
    可变 注释行 为 0。
    可变 空行 为 0。
    可变 代码行 为 0。
    可变 位置 为 0。
    长 为 做 计数 用 源码。
    只要 位置 小于 长：
        总行 为 总行 加 1。
        只要 位置 小于 长 并且 做 编码 用 做 子串 用 源码、位置、1 等于 字符_空格：
            位置 为 位置 加 1。
        完。
        若 位置 大于或等于 长 或者 做 编码 用 做 子串 用 源码、位置、1 等于 字符_换行：
            空行 为 空行 加 1。
        否则若 位置 加 1 小于 长 并且 做 子串 用 源码、位置、2 等于 「//」：
            注释行 为 注释行 加 1。
        否则：
            代码行 为 代码行 加 1。
        完。
        只要 位置 小于 长 并且 做 编码 用 做 子串 用 源码、位置、1 不等于 10：
            位置 为 位置 加 1。
        完。
        位置 为 位置 加 1。
    完。
    返还 一组：总行、代码行、注释行、空行。
完。

注释_覆盖率 接受 源码：
    统计 为 做 注释_统计行数 用 源码。
    若 统计 的 第 0 项 等于 0：
        返还 0。
    完。
    返还 统计 的 第 2 项 乘 100 除以 统计 的 第 0 项。
完。

注释_规范化 接受 注释文本：
    可变 结果 为 「」。
    可变 i 为 0。
    长 为 做 计数 用 注释文本。
    可变 上一空格 为 假。
    只要 i 小于 长：
        c 为 做 编码 用 做 子串 用 注释文本、i、1。
        若 c 等于 标记_乘：
            若 非 上一空格：
                结果 为 结果 加 「 」。
            完。
            上一空格 为 真。
        否则：
            结果 为 结果 加 做 子串 用 注释文本、i、1。
            上一空格 为 假。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

注释_缩进对齐 接受 源码、缩进级：
    可变 前缀 为 「」。
    可变 i 为 0。
    只要 i 小于 缩进级：
        前缀 为 前缀 加 「    」。
        i 为 i 加 1。
    完。
    返还 前缀 加 「// 」 加 源码。
完。

可变 _注释_处理数 为 0。

可变 _注释_待办数 为 0。

可变 _注释_待修数 为 0。

注释处理_记录 接受 零参：
    _注释_处理数 为 _注释_处理数 加 1。
完。

注释处理_记录待办 接受 零参：
    _注释_待办数 为 _注释_待办数 加 1。
完。

注释处理_记录待修 接受 零参：
    _注释_待修数 为 _注释_待修数 加 1。
完。

本章 收束。
