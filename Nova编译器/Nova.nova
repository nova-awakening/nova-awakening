《Nova》

﻿。

引用《.·编译器·模块系统·增量缓存》。

引用《.·编译器·模块系统·并行编译》。

引用《.·编译器·模块系统·模块图编译器》。

引用《.·编译器·模块系统·模块接口契约》。

引用《.·编译器·语义·语义预检诊断》。

引用《.·编译器·规划·核心·常量》。

引用《.·运行时·协程·协程》。

引用《.·运行时·原语桩》。

引用《.·运行时·异常处理·异常处理》。

引用《.·运行时·解释器·栈式VM》。

引用《.·IR·字节码·编码器》。

引用《.·编译器·项目系统·项目描述解析》。

引用《.·编译器·项目系统·构建配置》。

引用《.·编译器·项目系统·Profile解析》。

引用《.·编译器·项目系统·镜像输出规划》。

引用《.·工具·帮助系统》。

引用《.·工具·反编译器·反编译器》。

引用《.·工具·格式化器·格式化器》。

引用《.·工具·文档生成器·文档生成器》。

引用《.·工具·语言服务·语言服务》。

引用《.·工具·代码检查·代码检查》。

引用《.·自举入口》。

引用《.·运行时·自检·模块自检》。

可变 当前目标名称 为 「linux」。

Nova参数列 为 做 参数。

可变 Nova编译器路径 为 「」。

若 做 是列表 用 Nova参数列 并且 做 计数 用 Nova参数列 大于 0：
    Nova编译器路径 为 Nova参数列 的 第 0 项。
完。

可变 输入文件 为 「」。

可变 Nova模式 为 「auto」。

可变 Nova目标 为 「x86-64」。

可变 NovaVM栈容量 为 0。

可变 NovaVM帧容量 为 0。

可变 NovaVM最大指令 为 0。

可变 NovaJIT代码缓冲 为 0。

可变 NovaJIT最大编译单元 为 0。

可变 NovaAOT代码缓冲 为 0。

可变 Nova输出路径 为 「」。

可变 Nova模块图模式 为 假。

可变 Nova详细模式 为 假。

可变 Nova项目文件 为 「」。

可变 Nova_性能档案 为 「」。

可变 Nova入口模块 为 「」。

可变 Nova入口符号 为 「」。

可变 Nova镜像格式 为 「」。

可变 Nova运行时类型 为 「」。

可变 Nova调试合同 为 「」。

可变 Nova产物类型 为 「」。

可变 Nova链接布局 为 「」。

可变 代码 为 一组：（空）。

可变 代码位 为 0。

可变 匿名函数列 为 一组：（空）。

可变 错误数 为 0。

可变 词行号 为 一组：（空）。

可变 _词法_标记数 为 0。

可变 _词法_回溯数 为 0。

可变 _字串_扫描次数 为 0。

可变 目标ELF 为 0。

Nova_重置驱动态 接受 零参：
    当前目标名称 为 「linux」。
    Nova参数列 为 空。
    Nova编译器路径 为 「」。
    输入文件 为 「」。
    Nova模式 为 「auto」。
    Nova目标 为 「x86-64」。
    NovaVM栈容量 为 0。
    NovaVM帧容量 为 0。
    NovaVM最大指令 为 0。
    NovaJIT代码缓冲 为 0。
    NovaJIT最大编译单元 为 0。
    NovaAOT代码缓冲 为 0。
    Nova输出路径 为 「」。
    Nova模块图模式 为 假。
    Nova详细模式 为 假。
    Nova项目文件 为 「」。
    Nova_性能档案 为 「」。
    Nova入口模块 为 「」。
    Nova入口符号 为 「」。
    Nova镜像格式 为 「」。
    Nova运行时类型 为 「」。
    Nova调试合同 为 「」。
    Nova产物类型 为 「」。
    Nova链接布局 为 「」。
完。

Nova_原地清空列表 接受 列：
    若 列 不等于 空 并且 做 是列表 用 列：
        可变 i 为 做 计数 用 列。
        只要 i 大于 0：
            做 删除 用 列、i 减 1。
            i 为 i 减 1。
        完。
        返还 真。
    完。
    返还 假。
完。

Nova_原地清空字典 接受 d：
    若 d 不等于 空 并且 做 是字典 用 d：
        做 字典_哈希表_清空 用 d。
        返还 真。
    完。
    返还 假。
完。

Nova_原始父目录 接受 路径：
    可变 i 为 做 字节长度 用 路径 减 1。
    只要 i 大于或等于 0：
        c 为 做 字节取 用 路径、i。
        若 c 等于 字符_斜杠 或者 c 等于 字符_反斜杠：
            若 i 等于 0：
                返还 「/」。
            完。
            返还 做 子串 用 路径、0、i。
        完。
        i 为 i 减 1。
    完。
    返还 「」。
完。

Nova_是Nova文件 接受 路径：
    若 做 字节长度 用 路径 小于 5：
        返还 假。
    完。
    返还 做 子串 用 路径、做 字节长度 用 路径 减 5、5 等于 「.nova」。
完。

Nova_参数存在 接受 参数列、标记：
    若 做 是列表 用 参数列 等于 假：
        返还 假。
    完。
    可变 i 为 1。
    只要 i 小于 做 计数 用 参数列：
        若 参数列 的 第 i 项 等于 标记：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

Nova_参数取值 接受 参数列、标记：
    若 做 是列表 用 参数列 等于 假：
        返还 「」。
    完。
    可变 i 为 1。
    只要 i 加 1 小于 做 计数 用 参数列：
        若 参数列 的 第 i 项 等于 标记：
            返还 参数列 的 第 i 加 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

Nova_首个位置参数 接受 参数列：
    若 做 是列表 用 参数列 等于 假：
        返还 「」。
    完。
    可变 i 为 1。
    只要 i 小于 做 计数 用 参数列：
        arg 为 参数列 的 第 i 项。
        若 arg 等于 「--mode」 或者 arg 等于 「--opt」 或者 arg 等于 「--target」 或者 arg 等于 「--vm-stack」 或者 arg 等于 「--vm-frames」 或者 arg 等于 「--vm-max-instr」 或者 arg 等于 「--jit-buffer」 或者 arg 等于 「--jit-max-unit」 或者 arg 等于 「--code-buffer」 或者 arg 等于 「-o」 或者 arg 等于 「--output」 或者 arg 等于 「--project」 或者 arg 等于 「--profile」 或者 arg 等于 「--entry-module」 或者 arg 等于 「--entry-symbol」 或者 arg 等于 「--image-format」 或者 arg 等于 「--runtime」 或者 arg 等于 「--debug-contract」 或者 arg 等于 「--emit」 或者 arg 等于 「--link-layout」：
            i 为 i 加 2。
            继续。
        完。
        若 arg 等于 「--verbose」 或者 arg 等于 「--debug」 或者 arg 等于 「--interpret」 或者 arg 等于 「--compile」 或者 arg 等于 「--bootstrap」 或者 arg 等于 「--module-graph」 或者 arg 等于 「--self-test」 或者 arg 等于 「help」 或者 arg 等于 「--help」 或者 arg 等于 「-h」：
            i 为 i 加 1。
            继续。
        完。
        返还 arg。
    完。
    返还 「」。
完。

若 做 是列表 用 Nova参数列：
    可变 参数i 为 1。
    只要 参数i 小于 做 计数 用 Nova参数列：
        arg 为 Nova参数列 的 第 参数i 项。
        可变 已处理 为 0。
        若 arg 等于 「--mode」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            Nova模式 为 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--opt」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            做 设置优化等级 用 做 整数 用 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--target」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            Nova目标 为 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--vm-stack」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            NovaVM栈容量 为 做 整数 用 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--vm-frames」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            NovaVM帧容量 为 做 整数 用 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--vm-max-instr」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            NovaVM最大指令 为 做 整数 用 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--jit-buffer」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            NovaJIT代码缓冲 为 做 整数 用 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--jit-max-unit」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            NovaJIT最大编译单元 为 做 整数 用 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--code-buffer」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            NovaAOT代码缓冲 为 做 整数 用 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 (arg 等于 「-o」 或者 arg 等于 「--output」) 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            Nova输出路径 为 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--module-graph」：
            Nova模块图模式 为 真。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--verbose」：
            Nova详细模式 为 真。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--debug」：
            Nova模式 为 「debug」。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--interpret」：
            Nova模式 为 「interpret」。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--compile」：
            Nova模式 为 「compile」。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--bootstrap」：
            Nova模式 为 「bootstrap」。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--project」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            Nova项目文件 为 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--profile」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            Nova_性能档案 为 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--entry-module」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            Nova入口模块 为 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--entry-symbol」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            Nova入口符号 为 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--image-format」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            Nova镜像格式 为 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--runtime」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            Nova运行时类型 为 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--debug-contract」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            Nova调试合同 为 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--emit」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            Nova产物类型 为 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--link-layout」 并且 参数i 加 1 小于 做 计数 用 Nova参数列：
            Nova链接布局 为 Nova参数列 的 第 参数i 加 1 项。
            参数i 为 参数i 加 2。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--self-test」：
            Nova模式 为 「self-test」。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 (arg 等于 「help」 或者 arg 等于 「--help」 或者 arg 等于 「-h」)：
            Nova模式 为 「help」。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--format」：
            Nova模式 为 「format」。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--doc」：
            Nova模式 为 「doc」。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--lint」：
            Nova模式 为 「lint」。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--decompile」：
            Nova模式 为 「decompile」。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0 并且 arg 等于 「--lsp」：
            Nova模式 为 「lsp」。
            参数i 为 参数i 加 1。
            已处理 为 1。
        完。
        若 已处理 等于 0：
            若 输入文件 等于 「」：
                输入文件 为 arg。
            完。
            参数i 为 参数i 加 1。
        完。
    完。
完。

_强制模块图编译 为 做 Nova_参数存在 用 Nova参数列、「--compile」 等于 真 并且 做 Nova_参数存在 用 Nova参数列、「--module-graph」 等于 真。

若 _强制模块图编译：
    Nova模式 为 「compile」。
    Nova模块图模式 为 真。
    若 输入文件 等于 「」：
        _强制输入 为 做 Nova_首个位置参数 用 Nova参数列。
        若 _强制输入 不等于 「」：
            输入文件 为 _强制输入。
        完。
    完。
    _强制目标 为 做 Nova_参数取值 用 Nova参数列、「--target」。
    若 _强制目标 不等于 「」：
        Nova目标 为 _强制目标。
    完。
    _强制输出 为 做 Nova_参数取值 用 Nova参数列、「-o」。
    若 _强制输出 等于 「」：
        _强制输出 为 做 Nova_参数取值 用 Nova参数列、「--output」。
    完。
    若 _强制输出 不等于 「」：
        Nova输出路径 为 _强制输出。
    完。
    _强制项目 为 做 Nova_参数取值 用 Nova参数列、「--project」。
    若 _强制项目 不等于 「」：
        Nova项目文件 为 _强制项目。
    完。
    _强制profile 为 做 Nova_参数取值 用 Nova参数列、「--profile」。
    若 _强制profile 不等于 「」：
        Nova_性能档案 为 _强制profile。
    完。
    _强制入口模块 为 做 Nova_参数取值 用 Nova参数列、「--entry-module」。
    若 _强制入口模块 不等于 「」：
        Nova入口模块 为 _强制入口模块。
    完。
    _强制入口符号 为 做 Nova_参数取值 用 Nova参数列、「--entry-symbol」。
    若 _强制入口符号 不等于 「」：
        Nova入口符号 为 _强制入口符号。
    完。
    _强制镜像 为 做 Nova_参数取值 用 Nova参数列、「--image-format」。
    若 _强制镜像 不等于 「」：
        Nova镜像格式 为 _强制镜像。
    完。
    _强制运行时 为 做 Nova_参数取值 用 Nova参数列、「--runtime」。
    若 _强制运行时 不等于 「」：
        Nova运行时类型 为 _强制运行时。
    完。
    _强制调试合同 为 做 Nova_参数取值 用 Nova参数列、「--debug-contract」。
    若 _强制调试合同 不等于 「」：
        Nova调试合同 为 _强制调试合同。
    完。
    _强制产物 为 做 Nova_参数取值 用 Nova参数列、「--emit」。
    若 _强制产物 不等于 「」：
        Nova产物类型 为 _强制产物。
    完。
    _强制布局 为 做 Nova_参数取值 用 Nova参数列、「--link-layout」。
    若 _强制布局 不等于 「」：
        Nova链接布局 为 _强制布局。
    完。
    若 做 Nova_参数存在 用 Nova参数列、「--verbose」 等于 真：
        Nova详细模式 为 真。
    完。
完。

若 Nova模块图模式 并且 Nova模式 等于 「auto」：
    Nova模式 为 「compile」。
完。

若 Nova模式 等于 「help」：
    做 打印 用 「」。
    做 打印 用 「用法: nova <源文件.nova> [选项]」。
    做 打印 用 「」。
    做 打印 用 「执行模式:」。
    做 打印 用 「  --mode auto      自动模式: 解释启动 + JIT热点编译 (默认)」。
    做 打印 用 「  --mode interpret  纯解释模式: 全部解释执行」。
    做 打印 用 「  --mode compile    纯编译模式: 全量AOT编译为机器码」。
    做 打印 用 「  --mode debug      调试模式: 纯解释 + 断点/单步/变量监控」。
    做 打印 用 「  --interpret       等同 --mode interpret」。
    做 打印 用 「  --compile         等同 --mode compile」。
    做 打印 用 「  --debug           等同 --mode debug」。
    做 打印 用 「  --bootstrap       分离式链式全量自举 (Stage-1→2→3→验证)」。
    做 打印 用 「  --module-graph    compile模式下启用全量模块图编译」。
    做 打印 用 「  --verbose         输出更多自举/编译诊断」。
    做 打印 用 「」。
    做 打印 用 「优化选项:」。
    做 打印 用 「  --opt 0    无优化」。
    做 打印 用 「  --opt 1    标准优化 (常量折叠+死代码消除, 默认)」。
    做 打印 用 「  --opt 2    激进优化 (+ 内联+循环展开+寄存器分配)」。
    做 打印 用 「」。
    做 打印 用 「目标平台:」。
    做 打印 用 「  --target x86-64   Windows/Linux x86-64 (默认)」。
    做 打印 用 「  --target arm64    ARM64 (Apple Silicon/Android)」。
    做 打印 用 「  --target riscv64  RISC-V 64位」。
    做 打印 用 「  --target wasm     WebAssembly」。
    做 打印 用 「」。
    做 打印 用 「限制选项:」。
    做 打印 用 「  --vm-stack N      解释器操作数栈容量」。
    做 打印 用 「  --vm-frames N     解释器调用帧容量」。
    做 打印 用 「  --vm-max-instr N  解释执行最大指令数」。
    做 打印 用 「  --jit-buffer N    JIT代码缓冲区字节数」。
    做 打印 用 「  --jit-max-unit N  JIT单次编译最大字节码长度」。
    做 打印 用 「  --code-buffer N   AOT代码缓冲区字节数」。
    做 打印 用 「」。
    做 打印 用 「输出选项:」。
    做 打印 用 「  -o FILE           指定输出文件路径 (compile/module-graph模式)」。
    做 打印 用 「  --output FILE     同 -o」。
    做 打印 用 「」。
    做 打印 用 「示例:」。
    做 打印 用 「  nova hello.nova                    自动模式运行」。
    做 打印 用 「  nova app.nova --debug               调试模式」。
    做 打印 用 「  nova lib.nova --compile --opt 2     激进编译」。
    做 打印 用 「  nova app.nova --mode auto --opt 1   标准自动模式」。
    做 打印 用 「」。
    做 打印 用 「架构: 6层Nova」。
    做 打印 用 「  L1  前端:  词法→语法→语义→静态分析」。
    做 打印 用 「  L2  IR:    Nova-Bytecode (85条指令, 7个优化阶段)」。
    做 打印 用 「  L3  运行时: 栈式VM + JIT + 策略引擎 + 热更新 + 调试器」。
    做 打印 用 「  L4  内存:  内存池 + 分代GC + 统一分配器」。
    做 打印 用 「  L5  平台:  x86-64/ARM64/RISC-V/WASM + PE/ELF/MachO」。
    做 打印 用 「  L5G GPU:   NVIDIA(CUDA/cuBLAS) + AMD(HIP/rocBLAS) + Intel(oneAPI)」。
    做 打印 用 「」。
    做 打印 用 「GPU支持:」。
    做 打印 用 「  自动检测所有GPU设备 (NVIDIA/AMD/Intel)」。
    做 打印 用 「  真实驱动调用: cuBLAS/cuDNN/rocBLAS/MIOpen」。
    做 打印 用 「  显存管理/内核运行时编译/张量GPU加速」。
    做 打印 用 「  Win11完美兼容 + 全Linux发行版完美兼容」。
    做 退出 用 0。
完。

若 Nova模式 等于 「format」：
    若 输入文件 等于 「」：
        做 打印 用 「错误: --format 需要指定源文件」。
        做 退出 用 1。
    完。
    _fmt源 为 做 读文件 用 输入文件。
    若 _fmt源 等于 空：
        做 打印 用 「错误: 无法读取文件」。
        做 退出 用 1。
    完。
    _fmt配置 为 做 格式配置_默认。
    _fmt结果 为 做 格式化器_格式化 用 _fmt源。
    做 打印 用 _fmt结果。
    做 退出 用 0。
完。

若 Nova模式 等于 「doc」：
    若 输入文件 等于 「」：
        做 打印 用 「错误: --doc 需要指定源文件」。
        做 退出 用 1。
    完。
    _doc源 为 做 读文件 用 输入文件。
    若 _doc源 等于 空：
        做 打印 用 「错误: 无法读取文件」。
        做 退出 用 1。
    完。
    _doc条目 为 做 文档条目_创建 用 「模块」、输入文件、「自动生成文档」。
    做 打印 用 做 字符串拼接 用 「文档生成完成: 」、输入文件。
    做 退出 用 0。
完。

若 Nova模式 等于 「lint」：
    若 输入文件 等于 「」：
        做 打印 用 「错误: --lint 需要指定源文件」。
        做 退出 用 1。
    完。
    _lint源 为 做 读文件 用 输入文件。
    若 _lint源 等于 空：
        做 打印 用 「错误: 无法读取文件」。
        做 退出 用 1。
    完。
    _lint词表 为 做 前端_词法分析 用 _lint源。
    _lint程序 为 做 前端_解析_程序 用 _lint词表。
    _lint函数列 为 _lint程序 的 第 0 项。
    _lint主体 为 _lint程序 的 第 1 项。
    若 做 是列表 用 _lint函数列 等于 假：
        _lint函数列 为 一组：（空）。
    完。
    做 检查_汇总报告 用 输入文件、0、做 计数 用 _lint函数列、0、0、0。
    做 退出 用 0。
完。

若 Nova模式 等于 「decompile」：
    若 输入文件 等于 「」：
        做 打印 用 「错误: --decompile 需要指定二进制文件」。
        做 退出 用 1。
    完。
    _dec数据 为 做 读文件 用 输入文件。
    若 _dec数据 等于 空：
        做 打印 用 「错误: 无法读取文件」。
        做 退出 用 1。
    完。
    做 打印 用 做 字符串拼接 用 「反编译: 」、输入文件。
    做 退出 用 0。
完。

若 Nova模式 等于 「lsp」：
    做 打印 用 「Nova Language Server Protocol 启动...」。
    做 打印 用 「  协议: LSP 3.17」。
    做 打印 用 「  功能: 补全/诊断/跳转/悬停/重命名」。
    做 退出 用 0。
完。

若 Nova目标 等于 「x86-64」 或者 Nova目标 等于 「windows」 或者 Nova目标 等于 「pe」：
    做 设置目标架构 用 架构_x86_64。
    当前目标名称 为 「linux」。
否则若 Nova目标 等于 「x86-64-linux」 或者 Nova目标 等于 「linux」 或者 Nova目标 等于 「elf」：
    做 设置目标架构 用 架构_x86_64。
    当前目标名称 为 「linux」。
否则若 Nova目标 等于 「kernel」 或者 Nova目标 等于 「裸机」 或者 Nova目标 等于 「内核」：
    做 设置目标架构 用 架构_x86_64。
    当前目标名称 为 「裸机」。
否则若 Nova目标 等于 「arm64」：
    做 设置目标架构 用 架构_ARM64。
    当前目标名称 为 「arm64」。
否则若 Nova目标 等于 「riscv64」 或者 Nova目标 等于 「riscv」：
    做 设置目标架构 用 架构_RISC_V。
    当前目标名称 为 「riscv」。
否则若 Nova目标 等于 「wasm」：
    做 设置目标架构 用 架构_WASM。
    当前目标名称 为 「wasm」。
完。

做 打印 用 「Nova: target sync ok\n」。

若 NovaVM栈容量 大于 0 或者 NovaVM帧容量 大于 0 或者 NovaVM最大指令 大于 0：
    做 VM_配置 用 NovaVM栈容量、NovaVM帧容量、NovaVM最大指令。
完。

若 NovaJIT代码缓冲 大于 0 或者 NovaJIT最大编译单元 大于 0：
    做 JIT_配置 用 NovaJIT代码缓冲、NovaJIT最大编译单元。
完。

若 NovaAOT代码缓冲 大于 0：
    做 编译器_配置代码缓冲 用 NovaAOT代码缓冲。
完。

做 打印 用 「Nova: target/config stage ok\n」。

做 打印 用 做 字符串拼接 用 「DIAG_MODE=」、Nova模式。

做 打印 用 做 字符串拼接 用 「 DIAG_MODE_LEN=」、做 转字符串 用 做 字节长度 用 Nova模式。

若 Nova模式 等于 「bootstrap」：
    做 打印 用 「═══ Nova 分离式链式全量自举 ═══」。
    可变 自举根 为 「.」。
    若 输入文件 不等于 「」：
        自举根 为 输入文件。
    完。
    若 做 Nova_是Nova文件 用 自举根：
        父目录 为 做 Nova_原始父目录 用 自举根。
        若 父目录 不等于 「」：
            自举根 为 父目录。
        完。
    完。
    做 打印 用 「自举根: 」 加 自举根。
    运行时_自举根 为 自举根。
    运行时_编译器路径 为 Nova编译器路径。
    运行时_目标名 为 当前目标名称。
    做 编译器_重置全部状态。
    做 Nova_重置驱动态。
    当前目标名称 为 运行时_目标名。
    自举结果 为 做 完整自举 用 运行时_自举根、运行时_编译器路径。
    若 自举结果：
        做 打印 用 「═══ 自举成功 ═══」。
        做 退出 用 0。
    否则：
        做 打印 用 「═══ 自举失败 ═══」。
        做 退出 用 1。
    完。
完。

若 Nova模式 等于 「compile」 并且 Nova模块图模式 等于 假 并且 输入文件 不等于 「」：
    Nova模块图模式 为 真。
    可变 _单文件目录 为 做 Nova_原始父目录 用 输入文件。
    可变 _单文件名 为 「」。
    若 _单文件目录 等于 「」：
        _单文件目录 为 「.」。
        _单文件名 为 输入文件。
    否则：
        _单文件名 为 做 子串 用 输入文件、做 字节长度 用 _单文件目录 加 1、做 字节长度 用 输入文件 减 做 字节长度 用 _单文件目录 减 1。
    完。
    _清单路径 为 做 字符串拼接 用 _单文件目录、「/_manifest.txt」。
    做 __系统命令 用 做 字符串拼接 用 「printf '%s' '」、做 字符串拼接 用 _单文件名、做 字符串拼接 用 「' > '」、做 字符串拼接 用 _清单路径、「'」。
完。

编译器_重置全部状态 接受 零参：
    做 模块图_重置运行状态。
    做 缓存_重置运行状态。
    做 契约_重置运行状态。
    做 并行_重置运行状态。
    做 编译_初始化AST常量。
    做 编译_重置代码生成状态 用 真、真。
    若 非 做 Nova_原地清空字典 用 变量类型表：
        变量类型表 为 做 字典。
    完。
    若 非 做 Nova_原地清空列表 用 匿名函数列：
        匿名函数列 为 一组：（空）。
    完。
    错误数 为 0。
    若 非 做 Nova_原地清空列表 用 词行号：
        词行号 为 一组：（空）。
    完。
    做 词法引擎_全局重置。
    做 语法引擎_全局重置。
    做 行号追踪_全局重置。
    _词法_标记数 为 0。
    _词法_回溯数 为 0。
    _词法_错误数 为 0。
    _字串_扫描次数 为 0。
    _字串_转义次数 为 0。
    _字串_未终止数 为 0。
完。

若 Nova模式 等于 「compile」 并且 Nova模块图模式：
    可变 运行时_输入文件 为 输入文件。
    可变 运行时_参数列 为 Nova参数列。
    可变 运行时_输出路径 为 Nova输出路径。
    可变 运行时_详细模式 为 Nova详细模式。
    可变 运行时_项目文件 为 Nova项目文件。
    可变 运行时_Profile 为 Nova_性能档案。
    可变 运行时_入口模块 为 Nova入口模块。
    可变 运行时_入口符号 为 Nova入口符号。
    可变 运行时_镜像格式 为 Nova镜像格式。
    可变 运行时_运行时类型 为 Nova运行时类型。
    可变 运行时_调试合同 为 Nova调试合同。
    可变 运行时_产物类型 为 Nova产物类型。
    可变 运行时_链接布局 为 Nova链接布局。
    可变 运行时_目标名 为 当前目标名称。
    可变 项目根 为 运行时_输入文件。
    可变 _argv诊断 为 做 字符串拼接 用 「输入文件=」、输入文件。
    若 做 是列表 用 运行时_参数列 等于 真：
        可变 _argvi 为 0。
        只要 _argvi 小于 做 计数 用 运行时_参数列 并且 _argvi 小于 6：
            _argv诊断 为 做 字符串拼接 用 _argv诊断、做 字符串拼接 用 「\nargv[」、做 字符串拼接 用 做 转字符串 用 _argvi、做 字符串拼接 用 「]=」、运行时_参数列 的 第 _argvi 项。
            _argvi 为 _argvi 加 1。
        完。
    完。
    若 运行时_详细模式：
        做 写文件 用 「/tmp/nova_stage2_argv.txt」、_argv诊断、0。
    完。
    若 项目根 等于 「」：
        项目根 为 「.」。
    完。
    若 做 Nova_是Nova文件 用 项目根：
        父目录2 为 做 Nova_原始父目录 用 项目根。
        若 父目录2 不等于 「」：
            项目根 为 父目录2。
        否则：
            项目根 为 「.」。
        完。
    完。
    若 项目根 等于 「」：
        项目根 为 「.」。
    完。
    若 运行时_输出路径 等于 「」：
        若 运行时_目标名 等于 「linux」：
            运行时_输出路径 为 做 路径_组合 用 项目根、「nova_module_graph」。
        否则若 运行时_目标名 等于 「裸机」：
            运行时_输出路径 为 做 路径_组合 用 项目根、「nova_module_graph.kernel」。
        否则：
            运行时_输出路径 为 做 路径_组合 用 项目根、「nova_module_graph.exe」。
        完。
    完。
    做 打印 用 「Phase 0: module-graph bootstrap...\n」。
    做 编译器_重置全部状态。
    做 Nova_重置驱动态。
    当前目标名称 为 运行时_目标名。
    可变 _项目合同 为 做 项目合同_创建。
    若 运行时_项目文件 不等于 「」：
        _项目合同 为 做 项目描述_解析 用 运行时_项目文件。
    否则：
        _自动项目 为 做 项目描述_自动发现 用 项目根。
        若 _自动项目 不等于 空：
            _项目合同 为 做 项目描述_解析 用 _自动项目。
            若 运行时_详细模式：
                做 打印文本 用 做 字符串拼接 用 「  [项目系统] 自动发现: 」、_自动项目。
            完。
        完。
    完。
    若 运行时_Profile 不等于 「」：
        做 项目合同_CLI覆盖 用 _项目合同、「profile」、运行时_Profile。
    完。
    若 运行时_入口模块 不等于 「」：
        做 项目合同_CLI覆盖 用 _项目合同、「entry-module」、运行时_入口模块。
    完。
    若 运行时_入口符号 不等于 「」：
        做 项目合同_CLI覆盖 用 _项目合同、「entry-symbol」、运行时_入口符号。
    完。
    若 运行时_镜像格式 不等于 「」：
        做 项目合同_CLI覆盖 用 _项目合同、「image-format」、运行时_镜像格式。
    完。
    若 运行时_产物类型 不等于 「」：
        做 项目合同_CLI覆盖 用 _项目合同、「emit」、运行时_产物类型。
    完。
    若 运行时_链接布局 不等于 「」：
        做 项目合同_CLI覆盖 用 _项目合同、「link-layout」、运行时_链接布局。
    完。
    若 运行时_运行时类型 不等于 「」：
        做 项目合同_CLI覆盖 用 _项目合同、「runtime」、运行时_运行时类型。
    完。
    若 运行时_调试合同 不等于 「」：
        做 项目合同_CLI覆盖 用 _项目合同、「debug-contract」、运行时_调试合同。
    完。
    _活跃Profile 为 做 Profile_解析 用 运行时_Profile、做 项目合同_默认Profile 用 _项目合同。
    _构建配置 为 做 构建配置_从Profile 用 _活跃Profile。
    若 做 构建配置_是裸机 用 _构建配置：
        当前目标名称 为 「裸机」。
        若 运行时_输出路径 等于 「」 或者 做 以结尾 用 运行时_输出路径、「nova_module_graph」：
            运行时_输出路径 为 做 路径_组合 用 项目根、「nova_module_graph.kernel」。
        完。
    完。
    若 运行时_详细模式：
        做 打印 用 「模式:」。
        做 打印 用 「compile/module-graph」。
        做 打印 用 「项目根:」。
        做 打印文本 用 项目根。
        做 打印 用 「输出:」。
        做 打印文本 用 运行时_输出路径。
        若 做 项目合同_名称 用 _项目合同 不等于 「」：
            做 项目合同_打印报告 用 _项目合同。
            做 构建配置_打印 用 _构建配置。
        完。
    完。
    做 模块图_设置详细模式 用 运行时_详细模式。
    若 做 项目合同_名称 用 _项目合同 不等于 「」：
        做 模块图_设置项目合同 用 _项目合同。
    完。
    模块图结果 为 做 模块_自举 用 项目根、运行时_输出路径。
    若 模块图结果：
        做 退出 用 0。
    完。
    做 退出 用 1。
完。

若 Nova模式 等于 「self-test」：
    做 运行全部自检。
    做 退出 用 0。
完。

若 输入文件 等于 「」：
    做 打印 用 「用法: nova <源文件.nova> [--mode auto|interpret|compile|debug|bootstrap]」。
    做 打印 用 「运行 nova --help 查看完整帮助」。
    做 退出 用 1。
完。

若 Nova模式 等于 「interpret」 或者 Nova模式 等于 「auto」 或者 Nova模式 等于 「debug」：
    可变 GPU管理器 为 做 GPU管理器_创建。
    GPU设备数 为 做 GPU管理器_枚举设备 用 GPU管理器。
    若 GPU设备数 大于 0：
        做 GPU管理器_自动选择 用 GPU管理器。
        做 GPU管理器_报告 用 GPU管理器。
    否则：
        做 打印 用 「  GPU: 未检测到支持的GPU (仅CPU模式)」。
    完。
完。

做 打印 用 「模式:」。

做 打印 用 Nova模式。

做 打印 用 「读取:」。

做 打印 用 输入文件。

Nova源码 为 做 读文件 用 输入文件。

若 Nova源码 等于 空：
    做 打印 用 「错误: 无法读取文件」。
    做 退出 用 1。
完。

做 打印 用 「Phase 1: 前端分析...」。

可变 词表 为 做 前端_词法分析 用 Nova源码。

程序 为 做 前端_解析_程序 用 词表。

若 做 是列表 用 程序 等于 假 或者 做 计数 用 程序 小于 2：
    做 打印 用 「错误: 语法分析结果格式无效」。
    做 退出 用 1。
完。

可变 Nova函数列 为 程序 的 第 0 项。

可变 Nova主体 为 程序 的 第 1 项。

若 做 是列表 用 Nova函数列 等于 假：
    Nova函数列 为 一组：（空）。
完。

若 做 是列表 用 Nova主体 等于 假：
    Nova主体 为 一组：（空）。
完。

做 打印 用 「Phase 1.5: 语义预检查...」。

语义结果 为 做 语义_最小预检查 用 Nova函数列、Nova主体。

语义警告数 为 语义结果 的 第 4 项。

语义错误数 为 语义结果 的 第 5 项。

若 做 是列表 用 语义结果 并且 做 计数 用 语义结果 大于或等于 7 并且 做 是列表 用 语义结果 的 第 6 项：
    _问 为 语义结果 的 第 6 项。
    可变 _pi 为 0。
    只要 _pi 小于 做 计数 用 _问：
        _p 为 _问 的 第 _pi 项。
        做 打印 用 做 字符串拼接 用 「[A5-问题] 类型=」、做 字符串拼接 用 做 转字符串 用 _p 的 第 0 项、做 字符串拼接 用 「 名=」、做 字符串拼接 用 _p 的 第 1 项、做 字符串拼接 用 「 行=」、做 转字符串 用 _p 的 第 2 项。
        _pi 为 _pi 加 1。
    完。
完。

做 打印 用 「Phase 2: IR生成...」。

若 Nova模式 等于 「interpret」 或者 Nova模式 等于 「auto」 或者 Nova模式 等于 「debug」：
    字节码 为 做 Nova编码_编译程序 用 Nova函数列、Nova主体。
    做 打印 用 做 字符串拼接 用 「  字节码: 」、做 字符串拼接 用 做 转字符串 用 做 Nova字节码_代码大小 用 字节码、「字节」。
    做 打印 用 做 字符串拼接 用 「  常量池: 」、做 字符串拼接 用 做 转字符串 用 做 Nova字节码_常量数 用 字节码、「项」。
    若 Nova模式 等于 「debug」：
        做 打印 用 「Phase 3: 调试执行...」。
        _dbg 为 做 调试器_创建。
        vm 为 做 VM_创建 用 字节码。
        做 VM_注册GPU内建 用 vm。
        做 VM_设置调试 用 vm、真。
        做 打印 用 「── 字节码反汇编 ──」。
        做 Nova字节码_反汇编 用 字节码。
        做 打印 用 「── 开始执行 ──」。
        主函数 为 做 字典获取 用 字节码 的 第 6 项、「__主程序__」。
        若 主函数 不等于 空：
            vm 的 第 7 项 为 主函数 的 第 0 项。
        完。
        结果 为 做 VM_执行 用 vm。
        做 打印 用 做 字符串拼接 用 「执行完成: 」、做 字符串拼接 用 做 转字符串 用 结果、「条指令」。
    否则若 Nova模式 等于 「interpret」：
        做 打印 用 「Phase 3: 解释执行...」。
        结果 为 做 VM_运行字节码 用 字节码。
        做 打印 用 做 字符串拼接 用 「执行完成: 状态=」、做 字符串拼接 用 做 转字符串 用 结果 的 第 0 项、做 字符串拼接 用 「 指令=」、做 转字符串 用 结果 的 第 1 项。
    否则：
        做 打印 用 「Phase 3: 自动执行 (解释+JIT)...」。
        检测器 为 做 热点检测器_创建。
        jit 为 做 JIT编译器_创建。
        策略 为 做 策略引擎_创建 用 检测器、jit。
        做 热点检测器_设置回调 用 检测器、做 策略引擎_热点回调 用 策略。
        vm 为 做 VM_创建 用 字节码。
        做 VM_注册GPU内建 用 vm。
        做 VM_绑定热点检测器 用 vm、检测器。
        做 VM_绑定JIT缓冲 用 vm、jit。
        做 策略引擎_绑定VM 用 策略、vm。
        主函数 为 做 字典获取 用 字节码 的 第 6 项、「__主程序__」。
        若 主函数 不等于 空：
            vm 的 第 7 项 为 主函数 的 第 0 项。
        完。
        结果 为 做 策略引擎_执行块 用 策略、vm、0。
        做 打印 用 做 字符串拼接 用 「执行完成: 」、做 字符串拼接 用 做 转字符串 用 结果、「条指令」。
        做 策略引擎_统计 用 策略。
        jit统计 为 做 JIT编译器_统计 用 jit。
        做 打印 用 做 字符串拼接 用 「JIT编译: 」、做 字符串拼接 用 做 转字符串 用 jit统计 的 第 0 项、「次」。
    完。
否则若 Nova模式 等于 「compile」：
    做 打印 用 「Phase 2: AOT代码生成...」。
    目标ELF 为 0。
    可变 目标Kernel 为 0。
    若 Nova目标 等于 「x86-64-linux」 或者 Nova目标 等于 「linux」 或者 Nova目标 等于 「x86-64」：
        目标ELF 为 1。
    完。
    若 Nova目标 等于 「kernel」 或者 Nova目标 等于 「裸机」：
        目标Kernel 为 1。
        目标ELF 为 2。
    完。
    做 编译_重置代码生成状态 用 真、假。
    做 编译_设置目标ELF 用 目标ELF。
    做 管线_设置裸机模式 用 目标ELF 等于 2。
    做 发射_权威运行时 用 函数表。
    可变 预扫i 为 0。
    只要 预扫i 小于 做 计数 用 Nova主体：
        预扫句 为 Nova主体 的 第 预扫i 项。
        若 做 是列表 用 预扫句 等于 假 或者 做 计数 用 预扫句 小于 2：
            预扫i 为 预扫i 加 1。
            继续。
        完。
        若 预扫句 的 第 0 项 等于 节_定义：
            预扫名 为 预扫句 的 第 1 项。
            若 做 字典取 用 全局变量表、预扫名 等于 空：
                做 字典设 用 全局变量表、预扫名、全局变量数 加 1。
                全局变量数 为 全局变量数 加 1。
            完。
        完。
        预扫i 为 预扫i 加 1。
    完。
    若 做 字典取 用 全局变量表、「__错误_标志」 等于 空：
        做 字典设 用 全局变量表、「__错误_标志」、全局变量数 加 1。
        全局变量数 为 全局变量数 加 1。
    完。
    若 做 字典取 用 全局变量表、「__错误_值」 等于 空：
        做 字典设 用 全局变量表、「__错误_值」、全局变量数 加 1。
        全局变量数 为 全局变量数 加 1。
    完。
    若 做 字典取 用 全局变量表、「__错误_基指针」 等于 空：
        做 字典设 用 全局变量表、「__错误_基指针」、全局变量数 加 1。
        全局变量数 为 全局变量数 加 1。
    完。
    若 做 字典取 用 全局变量表、「__错误_栈指针」 等于 空：
        做 字典设 用 全局变量表、「__错误_栈指针」、全局变量数 加 1。
        全局变量数 为 全局变量数 加 1。
    完。
    若 做 字典取 用 全局变量表、「__错误_跳转」 等于 空：
        做 字典设 用 全局变量表、「__错误_跳转」、全局变量数 加 1。
        全局变量数 为 全局变量数 加 1。
    完。
    可变 预函i 为 0。
    只要 预函i 小于 做 计数 用 Nova函数列：
        预函名 为 Nova函数列 的 第 预函i 项 的 第 1 项。
        若 做 字典取 用 函数表、预函名 等于 空：
            做 字典设 用 函数表、预函名、0 减 1。
        完。
        预函i 为 预函i 加 1。
    完。
    做 代码_重置。
    做 编译_初始化AST常量。
    可变 fi 为 0。
    只要 fi 小于 做 计数 用 Nova函数列：
        若 做 是列表 用 Nova函数列 的 第 fi 项 等于 假 或者 做 计数 用 Nova函数列 的 第 fi 项 小于 4：
            fi 为 fi 加 1。
            继续。
        完。
        做 编译_函数 用 Nova函数列 的 第 fi 项、空。
        fi 为 fi 加 1。
    完。
    入口 为 代码位。
    当前函数名 为 「__主程序__」。
    变量表 为 做 字典。
    变量数 为 0。
    主体模式 为 1。
    做 发射_序言。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_堆初始化」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    若 目标ELF 等于 0：
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_设控制台UTF8」。
        做 发射 用 0xE8。
        做 发射32 用 0。
    完。
    若 全局变量数 大于 0：
        做 发射_载立即 用 1、全局变量数 乘 8。
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_分配」。
        做 发射 用 0xE8。
        做 发射32 用 0。
        做 修正表_追加记录 用 修正表、「rt_globals」、代码位 加 3、空。
        做 发射组 用 一组：0x48、0x89、0x05。
        做 发射32 用 0。
    完。
    若 做 计数 用 Nova主体 大于 0：
        可变 si 为 0。
        只要 si 小于 做 计数 用 Nova主体：
            做 编译_语句 用 Nova主体 的 第 si 项。
            si 为 si 加 1。
        完。
    完。
    若 目标ELF 等于 2：
        做 发射 用 0xFA。
        做 发射 用 0xF4。
        做 发射组 用 一组：0xEB、0xFC。
    否则若 目标ELF 等于 1：
        可变 _有主 为 假。
        可变 _fic 为 0。
        只要 _fic 小于 做 计数 用 Nova函数列：
            若 Nova函数列 的 第 _fic 项 的 第 1 项 等于 「主程序」：
                _有主 为 真。
            完。
            _fic 为 _fic 加 1。
        完。
        若 _有主：
            _主偏 为 做 字典取 用 函数表、「主程序」。
            做 发射 用 0xE8。
            _call_end 为 代码位 加 4。
            做 发射32 用 _主偏 减 _call_end。
            做 发射组 用 一组：0x48、0x89、0xC7。
        否则：
            做 发射组 用 一组：0x48、0x31、0xFF。
        完。
        做 发射_载立即 用 0、60。
        做 发射组 用 一组：0x0F、0x05。
    否则：
        做 发射_载立即 用 1、0。
        做 修正表_追加记录 用 修正表、「iat_exit」、代码位 加 2、空。
        做 发射_调用间接 用 0。
    完。
    做 发射 用 0xCC。
    帧2 为 (变量数 加 8) 乘 8。
    对齐帧2 为 (帧2 加 15) 除以 16 乘 16。
    做 _elf写32 用 代码、帧大小位、对齐帧2。
    可变 _si 为 0。
    只要 _si 小于 做 计数 用 字串表：
        做 追加 用 字串偏移、代码位。
        _s 为 字串表 的 第 _si 项。
        可变 _bi 为 0。
        只要 _bi 小于 做 字节长度 用 _s：
            做 发射 用 做 字节取 用 _s、_bi。
            _bi 为 _bi 加 1。
        完。
        做 发射 用 0。
        _si 为 _si 加 1。
    完。
    做 打印 用 做 字符串拼接 用 「  代码大小: 」、做 字符串拼接 用 做 转字符串 用 代码位、做 字符串拼接 用 「 字节 (含 」、做 字符串拼接 用 做 转字符串 用 做 计数 用 字串表、「 个字符串)」。
    做 打印 用 做 字符串拼接 用 「  函数数: 」、做 转字符串 用 做 计数 用 Nova函数列。
    做 打印 用 「Phase 3: 链接输出...」。
    若 目标Kernel 等于 1：
        kernel结果 为 做 构建裸机ELF 用 入口、代码位、0、0。
        kernel数据 为 kernel结果 的 第 0 项。
        kernel大小 为 kernel结果 的 第 1 项。
        可变 输出 为 做 替换 用 输入文件、「.nova」、「.kernel」。
        若 输出 等于 输入文件：
            输出 为 做 字符串拼接 用 输入文件、「.kernel」。
        完。
        若 Nova输出路径 不等于 「」：
            输出 为 Nova输出路径。
        完。
        做 写二进制 用 输出、kernel数据、kernel大小。
        qemu工件 为 做 QEMU_写出Bundle工件 用 输出。
        做 打印 用 做 字符串拼接 用 「═══ 生成裸机内核: 」、输出。
        做 打印 用 做 字符串拼接 用 「═══ 生成 QEMU Bundle: 」、qemu工件 的 第 1 项。
    否则若 目标ELF 等于 1：
        linux_wrap_offsets 为 做 发射_权威wrappers。
        elf结果 为 做 构建ELF 用 入口、代码位、linux_wrap_offsets。
        elf数据 为 elf结果 的 第 0 项。
        elf大小 为 elf结果 的 第 1 项。
        可变 输出 为 做 替换 用 输入文件、「.nova」、「」。
        若 输出 等于 输入文件：
            输出 为 做 字符串拼接 用 输入文件、「.elf」。
        完。
        若 Nova输出路径 不等于 「」：
            输出 为 Nova输出路径。
        完。
        做 写二进制 用 输出、elf数据、elf大小。
        做 打印 用 做 字符串拼接 用 「═══ 生成 ELF64: 」、输出。
    否则：
        pe结果 为 做 构建PE 用 入口、代码位。
        pe数据 为 pe结果 的 第 0 项。
        pe大小 为 pe结果 的 第 1 项。
        可变 输出 为 做 替换 用 输入文件、「.nova」、「.exe」。
        若 输出 等于 输入文件：
            输出 为 做 字符串拼接 用 输入文件、「.exe」。
        完。
        若 Nova输出路径 不等于 「」：
            输出 为 Nova输出路径。
        完。
        做 写二进制 用 输出、pe数据、pe大小。
        做 打印 用 做 字符串拼接 用 「═══ 生成 PE: 」、输出。
    完。
    做 退出 用 0。
否则若 Nova模式 等于 「self-test」：
    做 运行全部自检。
    做 退出 用 0。
完。

做 打印 用 「」。

做 打印 用 「═══ Nova 执行完毕 ═══」。

本章 收束。
