《自举入口》

引用《.·标准库·缓存·缓存》。

引用《.·编译器·模块系统·并行编译》。

引用《.·编译器·模块系统·模块图编译器》。

引用《.·编译器·模块系统·模块接口契约》。

自举版本 为 「4.1.0」。

自举_主入口 接受 零参：
    做 打印 用 「═══ Nova 分离式链式全量自举 v」 加 自举版本 加 「 ═══\n」。
    做 打印 用 「[阶段0] 初始化...\n」。
    可变 自举参数列 为 做 参数。
    可变 编译器路径 为 「」。
    若 做 是列表 用 自举参数列 并且 做 计数 用 自举参数列 大于 0：
        编译器路径 为 自举参数列 的 第 0 项。
    完。
    做 打印 用 「[阶段0] 参数OK\n」。
    若 编译器路径 等于 「」：
        做 打印 用 「  编译器: <未提供>\n」。
    否则：
        做 打印 用 「  编译器: 」 加 编译器路径 加 「\n」。
    完。
    做 打印 用 「[阶段0] 路径OK\n」。
    做 打印 用 「  目标: 」 加 当前目标名称 加 「\n」。
    做 打印 用 「[阶段0] 目标名OK\n」。
    做 缓存_初始化 用 0。
    做 打印 用 「[阶段0] 缓存OK\n」。
    做 打印 用 「  增量缓存: 就绪\n」。
    做 打印 用 「[阶段1] 扫描模块依赖图...\n」。
    模块数 为 做 模块_扫描项目 用 「.」。
    做 打印 用 「  发现 」 加 做 转字符串 用 模块数 加 「 个模块\n」。
    新契约 为 做 契约_自动发现 用 「.」。
    做 打印 用 「[阶段2] 契约注册: 」 加 做 转字符串 用 新契约 加 「 个\n」。
    做 打印 用 「[阶段3] 拓扑排序...\n」。
    顺序 为 做 模块_拓扑排序 用 0。
    层级 为 做 计算编译层级 用 顺序、模块表、模块索引。
    做 并行_统计 用 层级。
    做 打印 用 「[阶段4] 并行增量编译...\n」。
    做 并行_编译全部 用 层级。
    可变 失败数 为 0。
    可变 ci 为 0。
    只要 ci 小于 做 计数 用 模块表：
        若 模块表 的 第 ci 项 的 编译状态 等于 3：
            失败数 为 失败数 加 1。
        完。
        ci 为 ci 加 1。
    完。
    若 失败数 大于 0：
        做 打印 用 「自举失败: 」 加 做 转字符串 用 失败数 加 「 个模块编译错误\n」。
        做 退出 用 1。
    完。
    做 打印 用 「[阶段5] 链接...\n」。
    链接ok 为 做 模块_链接全部 用 「stage_next」。
    若 链接ok 等于 假：
        做 打印 用 「自举失败: 链接错误\n」。
        做 退出 用 1。
    完。
    做 打印 用 「  产出: stage_next\n」。
    做 打印 用 「[阶段6] 自举验证...\n」。
    做 缓存_统计。
    做 打印 用 「═══ 自举完成 ═══\n」。
完。

本章 收束。
