《内存池》

引用《..·..·标准库·基础》。

Slab_创建 接受 块大小、总块数：
    可变 空闲 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 总块数：
        做 追加 用 空闲、总块数 减 1 减 i。
        i 为 i 加 1。
    完。
    返还 一组：块大小、总块数、空闲、0、0。
完。

Slab_块大小 接受 s：
    返还 s 的 第 0 项。
完。

Slab_总块数 接受 s：
    返还 s 的 第 1 项。
完。

Slab_已分配 接受 s：
    返还 s 的 第 3 项。
完。

Slab_空闲数 接受 s：
    返还 做 计数 用 s 的 第 2 项。
完。

Slab_水位线 接受 s：
    返还 s 的 第 4 项。
完。

Slab_分配 接受 s：
    可变 新空闲 为 一组：（空）。
    若 做 计数 用 s 的 第 2 项 等于 0：
        返还 负 1。
    完。
    块号 为 s 的 第 2 项 的 第 做 计数 用 s 的 第 2 项 减 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 s 的 第 2 项 减 1：
        做 追加 用 新空闲、s 的 第 2 项 的 第 i 项。
        i 为 i 加 1。
    完。
    s 的 第 2 项 为 新空闲。
    s 的 第 3 项 为 s 的 第 3 项 加 1。
    若 s 的 第 3 项 大于 s 的 第 4 项：
        s 的 第 4 项 为 s 的 第 3 项。
    完。
    返还 块号。
完。

Slab_释放 接受 s、块号：
    若 块号 小于 0 或者 块号 大于或等于 s 的 第 1 项：
        返还 假。
    完。
    做 追加 用 s 的 第 2 项、块号。
    s 的 第 3 项 为 s 的 第 3 项 减 1。
    返还 真。
完。

Slab_利用率 接受 s：
    若 s 的 第 1 项 等于 0：
        返还 0。
    完。
    返还 s 的 第 3 项 乘 100 除以 s 的 第 1 项。
完。

内存池_创建 接受 零参：
    返还 一组：做 Slab_创建 用 64、256、做 Slab_创建 用 256、128、做 Slab_创建 用 1024、64、做 Slab_创建 用 4096、16。
完。

_内存池_选级 接受 大小：
    若 大小 小于或等于 64：
        返还 0。
    完。
    若 大小 小于或等于 256：
        返还 1。
    完。
    若 大小 小于或等于 1024：
        返还 2。
    完。
    若 大小 小于或等于 4096：
        返还 3。
    完。
    返还 负 1。
完。

内存池_分配 接受 池、大小：
    级 为 做 _内存池_选级 用 大小。
    若 级 小于 0：
        返还 一组：负 1、负 1。
    完。
    块号 为 做 Slab_分配 用 池 的 第 级 项。
    若 块号 小于 0：
        返还 一组：负 1、级。
    完。
    返还 一组：块号、级。
完。

内存池_释放 接受 池、块号、级：
    若 级 小于 0 或者 级 大于或等于 4：
        返还 假。
    完。
    返还 做 Slab_释放 用 池 的 第 级 项、块号。
完。

内存池_总容量 接受 池：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 4：
        总 为 总 加 做 Slab_块大小 用 池 的 第 i 项 乘 做 Slab_总块数 用 池 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

内存池_总已用 接受 池：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 4：
        总 为 总 加 做 Slab_块大小 用 池 的 第 i 项 乘 做 Slab_已分配 用 池 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

内存池_利用率 接受 池：
    容量 为 做 内存池_总容量 用 池。
    若 容量 等于 0：
        返还 0。
    完。
    返还 做 内存池_总已用 用 池 乘 100 除以 容量。
完。

内存池_重置 接受 池：
    可变 i 为 0。
    只要 i 小于 4：
        池 的 第 i 项 的 第 3 项 为 0。
        池 的 第 i 项 的 第 2 项 为 一组：（空）。
        总块 为 池 的 第 i 项 的 第 1 项。
        可变 j 为 0。
        只要 j 小于 总块：
            做 追加 用 池 的 第 i 项 的 第 2 项、总块 减 1 减 j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

内存池_打印报告 接受 池：
    做 打印 用 「═══ 内存池报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  总容量: 」、做 字符串拼接 用 做 转字符串 用 做 内存池_总容量 用 池、「 bytes」。
    做 打印文本 用 做 字符串拼接 用 「  总已用: 」、做 字符串拼接 用 做 转字符串 用 做 内存池_总已用 用 池、「 bytes」。
    做 打印文本 用 做 字符串拼接 用 「  利用率: 」、做 字符串拼接 用 做 转字符串 用 做 内存池_利用率 用 池、「%」。
    做 打印 用 「  各级Slab:」。
    可变 i 为 0。
    只要 i 小于 4：
        s 为 池 的 第 i 项。
        i 为 i 加 1。
    完。
完。

本章 收束。
