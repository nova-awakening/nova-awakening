《宿主协议》

引用《..·..·标准库·基础》。

引用《..·..·标准库·文本·文本处理》。

引用《..·..·标准库·系统·硬件库》。

引用《..·..·编译器·跨语言·智能协作·重置协调器》。

引用《..·..·编译器·跨语言·工程摄取·工程探测器》。

消息_请求 为 0。

消息_响应 为 1。

消息_通知 为 2。

消息_诊断 为 3。

方法_编译 为 0。

方法_诊断 为 1。

方法_补全 为 2。

方法_跳转定义 为 3。

方法_查找引用 为 4。

方法_格式化 为 5。

方法_重命名 为 6。

方法_悬停 为 7。

方法_符号列表 为 8。

方法_工作区符号 为 9。

方法_重置探测 为 10。

方法_重置执行 为 11。

可变 _协议_下个ID 为 1。

协议消息_创建 接受 类型、方法、参数：
    id 为 _协议_下个ID。
    _协议_下个ID 为 _协议_下个ID 加 1。
    返还 一组：id、类型、方法、参数、空、空。
完。

消息_ID 接受 m：
    返还 m 的 第 0 项。
完。

消息_类型 接受 m：
    返还 m 的 第 1 项。
完。

消息_方法 接受 m：
    返还 m 的 第 2 项。
完。

消息_参数 接受 m：
    返还 m 的 第 3 项。
完。

消息_结果 接受 m：
    返还 m 的 第 4 项。
完。

消息_错误 接受 m：
    返还 m 的 第 5 项。
完。

消息_设结果 接受 m、结果：
    m 的 第 4 项 为 结果。
完。

消息_设错误 接受 m、错误：
    m 的 第 5 项 为 错误。
完。

严重_错误 为 0。

严重_警告 为 1。

严重_信息 为 2。

严重_提示 为 3。

宿主协议_诊断_创建 接受 行、列、级别、消息、来源：
    返还 一组：行、列、级别、消息、来源。
完。

符号种类_函数 为 0。

符号种类_变量 为 1。

符号种类_结构 为 2。

符号种类_常量 为 3。

符号种类_模块 为 4。

符号信息_创建 接受 名称、种类、文件、行：
    返还 一组：名称、种类、文件、行、「」。
完。

宿主服务_创建 接受 名称：
    返还 一组：名称、一组：（空）、一组：（空）、一组：（空）、做 字典。
完。

宿主_名称 接受 h：
    返还 h 的 第 0 项。
完。

宿主_能力 接受 h：
    返还 h 的 第 1 项。
完。

宿主_消息队列 接受 h：
    返还 h 的 第 2 项。
完。

宿主_诊断缓存 接受 h：
    返还 h 的 第 3 项。
完。

宿主_符号缓存 接受 h：
    返还 h 的 第 4 项。
完。

宿主_注册能力 接受 h、方法：
    做 追加 用 h 的 第 1 项、方法。
完。

宿主_推送消息 接受 h、消息：
    做 追加 用 h 的 第 2 项、消息。
完。

宿主_缓存诊断 接受 h、诊断：
    做 追加 用 h 的 第 3 项、诊断。
完。

_宿主_符号键 接受 符号：
    返还 做 字符串拼接 用 符号 的 第 2 项、做 字符串拼接 用 「#」、做 字符串拼接 用 做 转字符串 用 符号 的 第 3 项、做 字符串拼接 用 「#」、符号 的 第 0 项。
完。

宿主_缓存符号 接受 h、符号：
    做 字典设 用 h 的 第 4 项、做 _宿主_符号键 用 符号、符号。
完。

_宿主_参数第 接受 参数、索引：
    若 做 是列表 用 参数 等于 真：
        若 索引 小于 做 计数 用 参数：
            返还 参数 的 第 索引 项。
        完。
        返还 空。
    完。
    若 索引 等于 0：
        返还 参数。
    完。
    返还 空。
完。

_宿主_参数文本 接受 参数、索引：
    值 为 做 _宿主_参数第 用 参数、索引。
    若 值 等于 空：
        返还 「」。
    完。
    返还 值。
完。

_宿主_符号种类名 接受 种类：
    若 种类 等于 符号种类_函数：
        返还 「函数」。
    完。
    若 种类 等于 符号种类_变量：
        返还 「变量」。
    完。
    若 种类 等于 符号种类_结构：
        返还 「结构」。
    完。
    若 种类 等于 符号种类_常量：
        返还 「常量」。
    完。
    若 种类 等于 符号种类_模块：
        返还 「模块」。
    完。
    返还 「unknown」。
完。

宿主_全部符号 接受 h：
    可变 结果 为 一组：（空）。
    键列表 为 做 字典键 用 h 的 第 4 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        符号 为 做 字典取 用 h 的 第 4 项、键列表 的 第 i 项。
        若 符号 不等于 空：
            做 追加 用 结果、符号。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

_宿主_筛符号 接受 h、查询、文件、仅前缀、仅精确：
    可变 结果 为 一组：（空）。
    全部 为 做 宿主_全部符号 用 h。
    可变 i 为 0。
    只要 i 小于 做 计数 用 全部：
        符号 为 全部 的 第 i 项。
        可变 匹配文件 为 真。
        可变 匹配查询 为 真。
        若 文件 不等于 「」 并且 符号 的 第 2 项 不等于 文件：
            匹配文件 为 假。
        完。
        若 查询 不等于 「」：
            若 仅精确：
                若 符号 的 第 0 项 不等于 查询：
                    匹配查询 为 假。
                完。
            否则若 仅前缀：
                若 做 以开头 用 符号 的 第 0 项、查询 等于 假：
                    匹配查询 为 假。
                完。
            否则：
                若 做 包含 用 符号 的 第 0 项、查询 等于 假 并且 做 包含 用 符号 的 第 2 项、查询 等于 假：
                    匹配查询 为 假。
                完。
            完。
        完。
        若 匹配文件 并且 匹配查询：
            做 追加 用 结果、符号。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

_宿主_格式化文本 接受 文本：
    若 文本 等于 空：
        返还 「」。
    完。
    行列表 为 做 分割 用 文本、「\n」。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        可变 行 为 做 替换 用 行列表 的 第 i 项、「\t」、「    」。
        可变 末 为 做 计数 用 行。
        只要 末 大于 0 并且 (做 子串 用 行、末 减 1、1 等于 「 」 或者 做 子串 用 行、末 减 1、1 等于 「\t」 或者 做 子串 用 行、末 减 1、1 等于 「\r」)：
            末 为 末 减 1。
        完。
        行 为 做 子串 用 行、0、末。
        若 i 大于 0：
            结果 为 做 字符串拼接 用 结果、「\n」。
        完。
        结果 为 做 字符串拼接 用 结果、行。
        i 为 i 加 1。
    完。
    返还 结果。
完。

_宿主_符号摘要 接受 符号：
    返还 一组：符号 的 第 0 项、做 _宿主_符号种类名 用 符号 的 第 1 项、符号 的 第 2 项、符号 的 第 3 项、符号 的 第 4 项。
完。

_宿主_重置阶段名 接受 id：
    若 id 等于 0：
        返还 「待命」。
    完。
    若 id 等于 1：
        返还 「探测」。
    完。
    若 id 等于 2：
        返还 「建模」。
    完。
    若 id 等于 3：
        返还 「骨架解析」。
    完。
    若 id 等于 4：
        返还 「语义抽取」。
    完。
    若 id 等于 5：
        返还 「风险评审」。
    完。
    若 id 等于 6：
        返还 「重建计划」。
    完。
    若 id 等于 7：
        返还 「模块生成」。
    完。
    若 id 等于 8：
        返还 「等价验证」。
    完。
    若 id 等于 9：
        返还 「合规审查」。
    完。
    若 id 等于 10：
        返还 「报告输出」。
    完。
    若 id 等于 11：
        返还 「complete」。
    完。
    返还 「unknown」。
完。

宿主_有能力 接受 h、方法：
    可变 i 为 0。
    只要 i 小于 做 计数 用 h 的 第 1 项：
        若 h 的 第 1 项 的 第 i 项 等于 方法：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

宿主_处理请求 接受 h、消息：
    方法 为 做 消息_方法 用 消息。
    若 做 宿主_有能力 用 h、方法 等于 假：
        做 消息_设错误 用 消息、「方法未注册」。
        返还 消息。
    完。
    若 方法 等于 方法_编译：
        返还 做 _宿主_处理编译 用 h、消息。
    完。
    若 方法 等于 方法_诊断：
        返还 做 _宿主_处理诊断 用 h、消息。
    完。
    若 方法 等于 方法_补全：
        返还 做 _宿主_处理补全 用 h、消息。
    完。
    若 方法 等于 方法_跳转定义：
        返还 做 _宿主_处理跳转定义 用 h、消息。
    完。
    若 方法 等于 方法_查找引用：
        返还 做 _宿主_处理查找引用 用 h、消息。
    完。
    若 方法 等于 方法_格式化：
        返还 做 _宿主_处理格式化 用 h、消息。
    完。
    若 方法 等于 方法_重命名：
        返还 做 _宿主_处理重命名 用 h、消息。
    完。
    若 方法 等于 方法_悬停：
        返还 做 _宿主_处理悬停 用 h、消息。
    完。
    若 方法 等于 方法_符号列表：
        返还 做 _宿主_处理符号列表 用 h、消息。
    完。
    若 方法 等于 方法_工作区符号：
        返还 做 _宿主_处理工作区符号 用 h、消息。
    完。
    若 方法 等于 方法_重置探测：
        返还 做 _宿主_处理重置探测 用 h、消息。
    完。
    若 方法 等于 方法_重置执行：
        返还 做 _宿主_处理重置执行 用 h、消息。
    完。
    做 消息_设错误 用 消息、「方法无处理器」。
    返还 消息。
完。

_宿主_处理编译 接受 h、消息：
    参数 为 做 消息_参数 用 消息。
    源文件 为 做 _宿主_参数文本 用 参数、0。
    优化级别 为 做 _宿主_参数第 用 参数、1。
    若 源文件 等于 「」：
        做 消息_设错误 用 消息、「缺少源文件路径」。
        返还 消息。
    完。
    做 宿主_推送消息 用 h、消息。
    做 消息_设结果 用 消息、一组：「已入队」、源文件、优化级别、做 计数 用 h 的 第 2 项。
    返还 消息。
完。

_宿主_处理诊断 接受 h、消息：
    查询 为 做 _宿主_参数文本 用 做 消息_参数 用 消息、0。
    若 查询 等于 「」：
        做 消息_设结果 用 消息、h 的 第 3 项。
        返还 消息。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 h 的 第 3 项：
        d 为 h 的 第 3 项 的 第 i 项。
        若 做 是列表 用 d 等于 真 并且 做 计数 用 d 大于或等于 5：
            若 做 包含 用 做 转字符串 用 d 的 第 3 项、查询 或者 做 包含 用 做 转字符串 用 d 的 第 4 项、查询：
                做 追加 用 结果、d。
            完。
        完。
        i 为 i 加 1。
    完。
    做 消息_设结果 用 消息、结果。
    返还 消息。
完。

_宿主_处理补全 接受 h、消息：
    参数 为 做 消息_参数 用 消息。
    查询 为 做 _宿主_参数文本 用 参数、0。
    文件 为 做 _宿主_参数文本 用 参数、1。
    做 消息_设结果 用 消息、做 _宿主_筛符号 用 h、查询、文件、真、假。
    返还 消息。
完。

_宿主_处理跳转定义 接受 h、消息：
    参数 为 做 消息_参数 用 消息。
    名称 为 做 _宿主_参数文本 用 参数、0。
    文件 为 做 _宿主_参数文本 用 参数、1。
    结果 为 做 _宿主_筛符号 用 h、名称、文件、假、真。
    若 做 计数 用 结果 等于 0：
        做 消息_设错误 用 消息、「未找到定义」。
        返还 消息。
    完。
    做 消息_设结果 用 消息、结果 的 第 0 项。
    返还 消息。
完。

_宿主_处理查找引用 接受 h、消息：
    参数 为 做 消息_参数 用 消息。
    名称 为 做 _宿主_参数文本 用 参数、0。
    文件 为 做 _宿主_参数文本 用 参数、1。
    做 消息_设结果 用 消息、做 _宿主_筛符号 用 h、名称、文件、假、真。
    返还 消息。
完。

_宿主_处理格式化 接受 h、消息：
    参数 为 做 消息_参数 用 消息。
    文本 为 做 _宿主_参数文本 用 参数、0。
    若 文本 等于 「」 并且 做 是列表 用 参数 等于 真 并且 做 计数 用 参数 大于 1：
        文本 为 参数 的 第 1 项。
    完。
    做 消息_设结果 用 消息、做 _宿主_格式化文本 用 文本。
    返还 消息。
完。

_宿主_处理重命名 接受 h、消息：
    可变 命中数 为 0。
    参数 为 做 消息_参数 用 消息。
    旧名 为 做 _宿主_参数文本 用 参数、0。
    新名 为 做 _宿主_参数文本 用 参数、1。
    文本 为 做 _宿主_参数文本 用 参数、2。
    若 旧名 等于 「」 或者 新名 等于 「」：
        做 消息_设错误 用 消息、「重命名参数不足」。
        返还 消息。
    完。
    全部 为 做 宿主_全部符号 用 h。
    可变 新缓存 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 全部：
        符号 为 全部 的 第 i 项。
        若 符号 的 第 0 项 等于 旧名：
            符号 的 第 0 项 为 新名。
            命中数 为 命中数 加 1。
        完。
        做 字典设 用 新缓存、做 _宿主_符号键 用 符号、符号。
        i 为 i 加 1。
    完。
    h 的 第 4 项 为 新缓存。
    若 文本 不等于 「」：
        文本 为 做 替换 用 文本、旧名、新名。
    完。
    做 消息_设结果 用 消息、一组：命中数、文本。
    返还 消息。
完。

_宿主_处理悬停 接受 h、消息：
    参数 为 做 消息_参数 用 消息。
    名称 为 做 _宿主_参数文本 用 参数、0。
    文件 为 做 _宿主_参数文本 用 参数、1。
    结果 为 做 _宿主_筛符号 用 h、名称、文件、假、真。
    若 做 计数 用 结果 等于 0：
        做 消息_设错误 用 消息、「未找到符号」。
        返还 消息。
    完。
    做 消息_设结果 用 消息、做 _宿主_符号摘要 用 结果 的 第 0 项。
    返还 消息。
完。

_宿主_处理符号列表 接受 h、消息：
    参数 为 做 消息_参数 用 消息。
    可变 查询 为 做 _宿主_参数文本 用 参数、0。
    可变 文件 为 「」。
    若 做 包含 用 查询、「/」 或者 做 以结尾 用 查询、「.nova」：
        文件 为 查询。
        查询 为 「」。
    否则：
        文件 为 做 _宿主_参数文本 用 参数、1。
    完。
    做 消息_设结果 用 消息、做 _宿主_筛符号 用 h、查询、文件、假、假。
    返还 消息。
完。

_宿主_处理工作区符号 接受 h、消息：
    查询 为 做 _宿主_参数文本 用 做 消息_参数 用 消息、0。
    做 消息_设结果 用 消息、做 _宿主_筛符号 用 h、查询、「」、假、假。
    返还 消息。
完。

_宿主_处理重置探测 接受 h、消息：
    路径 为 做 _宿主_参数文本 用 做 消息_参数 用 消息、0。
    若 路径 等于 「」：
        做 消息_设错误 用 消息、「缺少工程路径」。
        返还 消息。
    完。
    探测 为 做 工程探测 用 路径。
    做 消息_设结果 用 消息、一组：做 源语言_名称 用 探测 的 第 0 项、做 构建系统_名称 用 探测 的 第 1 项、探测 的 第 2 项、探测 的 第 3 项、探测 的 第 4 项、探测 的 第 5 项。
    返还 消息。
完。

_宿主_处理重置执行 接受 h、消息：
    路径 为 做 _宿主_参数文本 用 做 消息_参数 用 消息、0。
    若 路径 等于 「」：
        做 消息_设错误 用 消息、「缺少工程路径」。
        返还 消息。
    完。
    ctx 为 做 协调上下文_创建。
    做 协调_执行 用 ctx、路径。
    做 消息_设结果 用 消息、一组：做 _宿主_重置阶段名 用 做 协调_阶段 用 ctx、做 协调_建议列表 用 ctx、做 协调_审批记录 用 ctx、做 协调_报告 用 ctx、做 协调_诊断 用 ctx。
    返还 消息。
完。

宿主_初始化IDE模式 接受 h：
    做 宿主_注册能力 用 h、方法_编译。
    做 宿主_注册能力 用 h、方法_诊断。
    做 宿主_注册能力 用 h、方法_补全。
    做 宿主_注册能力 用 h、方法_跳转定义。
    做 宿主_注册能力 用 h、方法_查找引用。
    做 宿主_注册能力 用 h、方法_格式化。
    做 宿主_注册能力 用 h、方法_重命名。
    做 宿主_注册能力 用 h、方法_悬停。
    做 宿主_注册能力 用 h、方法_符号列表。
    做 宿主_注册能力 用 h、方法_工作区符号。
    做 宿主_注册能力 用 h、方法_重置探测。
    做 宿主_注册能力 用 h、方法_重置执行。
完。

宿主_初始化CLI模式 接受 h：
    做 宿主_注册能力 用 h、方法_编译。
    做 宿主_注册能力 用 h、方法_诊断。
    做 宿主_注册能力 用 h、方法_重置探测。
    做 宿主_注册能力 用 h、方法_重置执行。
完。

宿主_打印报告 接受 h：
    做 打印 用 「═══ 宿主服务报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  名称: 」、h 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  注册能力: 」、做 转字符串 用 做 计数 用 h 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  消息队列: 」、做 转字符串 用 做 计数 用 h 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  诊断缓存: 」、做 转字符串 用 做 计数 用 h 的 第 3 项。
    做 打印文本 用 做 字符串拼接 用 「  符号缓存: 」、做 转字符串 用 做 计数 用 做 宿主_全部符号 用 h。
完。

本章 收束。
