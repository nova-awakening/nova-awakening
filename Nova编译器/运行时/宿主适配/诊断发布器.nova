《诊断发布器》

引用《..·..·标准库·基础》。

来源_词法 为 0。

来源_语法 为 1。

来源_语义 为 2。

来源_类型 为 3。

来源_借用 为 4。

来源_效果 为 5。

来源_优化 为 6。

来源_链接 为 7。

来源名 接受 s：
    若 s 等于 来源_词法：
        返还 「词法」。
    完。
    若 s 等于 来源_语法：
        返还 「语法」。
    完。
    若 s 等于 来源_语义：
        返还 「语义」。
    完。
    若 s 等于 来源_类型：
        返还 「类型」。
    完。
    若 s 等于 来源_借用：
        返还 「借用」。
    完。
    若 s 等于 来源_效果：
        返还 「效果」。
    完。
    若 s 等于 来源_优化：
        返还 「优化」。
    完。
    若 s 等于 来源_链接：
        返还 「链接」。
    完。
    返还 「unknown」。
完。

诊断发布器_诊断_创建 接受 文件、行、列、级别、消息、来源：
    返还 一组：文件、行、列、级别、消息、来源、「」、「」。
完。

诊断_文件 接受 d：
    返还 d 的 第 0 项。
完。

诊断_行 接受 d：
    返还 d 的 第 1 项。
完。

诊断_列 接受 d：
    返还 d 的 第 2 项。
完。

诊断发布器_诊断_级别 接受 d：
    返还 d 的 第 3 项。
完。

诊断发布器_诊断_消息 接受 d：
    返还 d 的 第 4 项。
完。

诊断_来源 接受 d：
    返还 d 的 第 5 项。
完。

诊断_代码 接受 d：
    返还 d 的 第 6 项。
完。

诊断_修复 接受 d：
    返还 d 的 第 7 项。
完。

诊断_设代码 接受 d、c：
    d 的 第 6 项 为 c。
完。

诊断_设修复 接受 d、f：
    d 的 第 7 项 为 f。
完。

可变 _发布器_诊断 为 一组：（空）。

可变 _发布器_按文件 为 空。

可变 _发布器_统计 为 一组：0、0、0。

发布器_重置 接受 零参：
    _发布器_诊断 为 一组：（空）。
    _发布器_按文件 为 做 字典。
    _发布器_统计 为 一组：0、0、0。
完。

发布器_初始化 接受 零参：
    若 _发布器_按文件 等于 空：
        _发布器_按文件 为 做 字典。
    完。
完。

发布器_接收 接受 诊断：
    做 发布器_初始化。
    做 追加 用 _发布器_诊断、诊断。
    文件 为 做 诊断_文件 用 诊断。
    现有 为 做 字典取 用 _发布器_按文件、文件。
    若 现有 等于 空：
        做 字典设 用 _发布器_按文件、文件、一组：诊断。
    否则：
        做 追加 用 现有、诊断。
    完。
    级别 为 做 诊断发布器_诊断_级别 用 诊断。
    若 级别 大于或等于 0 并且 级别 小于 4：
        _发布器_统计 的 第 级别 项 为 _发布器_统计 的 第 级别 项 加 1。
    完。
完。

发布器_从编译器错误 接受 文件、行、消息：
    d 为 做 诊断发布器_诊断_创建 用 文件、行、0、消息、来源_语义、0。
    做 发布器_接收 用 d。
    返还 d。
完。

发布器_从编译器警告 接受 文件、行、消息：
    d 为 做 诊断发布器_诊断_创建 用 文件、行、0、1、消息、来源_语义。
    做 发布器_接收 用 d。
    返还 d。
完。

发布器_从词法错误 接受 文件、行、列、消息：
    d 为 做 诊断发布器_诊断_创建 用 文件、行、列、0、消息、来源_词法。
    做 诊断_设代码 用 d、「E001」。
    做 发布器_接收 用 d。
    返还 d。
完。

发布器_从语法错误 接受 文件、行、列、消息：
    d 为 做 诊断发布器_诊断_创建 用 文件、行、列、0、消息、来源_语法。
    做 诊断_设代码 用 d、「E002」。
    做 发布器_接收 用 d。
    返还 d。
完。

发布器_从借用违规 接受 文件、行、消息：
    d 为 做 诊断发布器_诊断_创建 用 文件、行、0、消息、来源_借用、0。
    做 诊断_设代码 用 d、「E003」。
    做 发布器_接收 用 d。
    返还 d。
完。

发布器_从效果违规 接受 文件、行、消息：
    d 为 做 诊断发布器_诊断_创建 用 文件、行、0、消息、来源_效果、0。
    做 诊断_设代码 用 d、「E004」。
    做 发布器_接收 用 d。
    返还 d。
完。

发布器_转宿主消息 接受 诊断：
    返还 一组：做 诊断_行 用 诊断、做 诊断_列 用 诊断、做 诊断发布器_诊断_级别 用 诊断、做 字符串拼接 用 「[」、做 字符串拼接 用 做 诊断_代码 用 诊断、做 字符串拼接 用 「] 」、做 诊断发布器_诊断_消息 用 诊断、做 来源名 用 做 诊断_来源 用 诊断。
完。

发布器_文件诊断 接受 文件名：
    做 发布器_初始化。
    列表 为 做 字典取 用 _发布器_按文件、文件名。
    若 列表 等于 空：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、做 发布器_转宿主消息 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

发布器_错误数 接受 零参：
    返还 _发布器_统计 的 第 0 项。
完。

发布器_警告数 接受 零参：
    返还 _发布器_统计 的 第 1 项。
完。

发布器_总数 接受 零参：
    返还 做 计数 用 _发布器_诊断。
完。

发布器_有错误 接受 零参：
    返还 _发布器_统计 的 第 0 项 大于 0。
完。

发布器_打印报告 接受 零参：
    做 打印 用 「═══ 诊断发布器报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  总诊断: 」、做 转字符串 用 做 计数 用 _发布器_诊断。
    做 打印文本 用 做 字符串拼接 用 「  错误: 」、做 转字符串 用 _发布器_统计 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  警告: 」、做 转字符串 用 _发布器_统计 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  信息: 」、做 转字符串 用 _发布器_统计 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  提示: 」、做 转字符串 用 _发布器_统计 的 第 3 项。
    可变 来源计数 为 一组：0、0、0、0、0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _发布器_诊断：
        可变 s 为 做 诊断_来源 用 _发布器_诊断 的 第 i 项。
        若 s 大于或等于 0 并且 s 小于 8：
            来源计数 的 第 s 项 为 来源计数 的 第 s 项 加 1。
        完。
        i 为 i 加 1。
    完。
    做 打印 用 「  按来源:」。
    可变 s 为 0。
    只要 s 小于 8：
        若 来源计数 的 第 s 项 大于 0：
            做 打印文本 用 做 字符串拼接 用 「    」、做 字符串拼接 用 做 来源名 用 s、做 字符串拼接 用 「: 」、做 转字符串 用 来源计数 的 第 s 项。
        完。
        s 为 s 加 1。
    完。
完。

本章 收束。
