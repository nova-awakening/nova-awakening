《热点检测》

引用《..·..·标准库·基础》。

JIT_热点阈值 为 10。

JIT_超热阈值 为 100。

JIT_冷却阈值 为 10000。

热点_冷 为 0。

热点_温 为 1。

热点_热 为 2。

热点_超热 为 3。

热点_冷却中 为 4。

热点_创建 接受 块ID：
    返还 一组：块ID、0、热点_冷、0、负 1、0。
完。

热点_块ID 接受 记录：
    返还 记录 的 第 0 项。
完。

热点_计数 接受 记录：
    返还 记录 的 第 1 项。
完。

热点_状态 接受 记录：
    返还 记录 的 第 2 项。
完。

热点_已编译 接受 记录：
    返还 记录 的 第 4 项 大于或等于 0。
完。

热点检测器_创建 接受 零参：
    返还 一组：做 字典、一组：（空）、0、空。
完。

热点检测器_设置回调 接受 检测器、回调：
    检测器 的 第 4 项 为 回调。
完。

热点检测器_计数 接受 检测器、块ID、时间戳：
    键 为 做 转字符串 用 块ID。
    可变 记录 为 做 字典获取 用 检测器 的 第 0 项、键。
    若 记录 等于 空：
        记录 为 做 热点_创建 用 块ID。
        做 字典设置 用 检测器 的 第 0 项、键、记录。
    完。
    记录 的 第 1 项 为 记录 的 第 1 项 加 1。
    记录 的 第 3 项 为 时间戳。
    若 记录 的 第 2 项 等于 热点_冷 并且 记录 的 第 1 项 大于或等于 JIT_热点阈值：
        记录 的 第 2 项 为 热点_温。
        检测器 的 第 2 项 为 检测器 的 第 2 项 加 1。
        做 追加 用 检测器 的 第 1 项、块ID。
        若 检测器 的 第 4 项 不等于 空：
            做 检测器 的 第 4 项 用 块ID、记录 的 第 1 项。
        完。
    完。
    若 记录 的 第 2 项 等于 热点_热 并且 记录 的 第 1 项 大于或等于 JIT_超热阈值：
        记录 的 第 2 项 为 热点_超热。
        记录 的 第 5 项 为 2。
    完。
    返还 记录 的 第 2 项。
完。

热点检测器_标记编译完成 接受 检测器、块ID、机器码偏移：
    键 为 做 转字符串 用 块ID。
    可变 记录 为 做 字典获取 用 检测器 的 第 0 项、键。
    若 记录 不等于 空：
        记录 的 第 2 项 为 热点_热。
        记录 的 第 4 项 为 机器码偏移。
        检测器 的 第 3 项 为 检测器 的 第 3 项 加 1。
    完。
完。

热点检测器_取编译任务 接受 检测器：
    可变 新队列 为 一组：（空）。
    若 做 计数 用 检测器 的 第 1 项 等于 0：
        返还 负 1。
    完。
    块ID 为 检测器 的 第 1 项 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 检测器 的 第 1 项：
        做 追加 用 新队列、检测器 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    检测器 的 第 1 项 为 新队列。
    返还 块ID。
完。

热点检测器_冷却扫描 接受 检测器、当前时间：
    可变 回收数 为 0。
    键列表 为 做 字典键 用 检测器 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        可变 记录 为 做 字典获取 用 检测器 的 第 0 项、键列表 的 第 i 项。
        若 记录 不等于 空 并且 记录 的 第 2 项 大于或等于 热点_热：
            空闲时间 为 当前时间 减 记录 的 第 3 项。
            若 空闲时间 大于 JIT_冷却阈值：
                记录 的 第 2 项 为 热点_冷却中。
                记录 的 第 4 项 为 负 1。
                记录 的 第 1 项 为 0。
                回收数 为 回收数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 回收数。
完。

热点检测器_查询 接受 检测器、块ID：
    键 为 做 转字符串 用 块ID。
    返还 做 字典获取 用 检测器 的 第 0 项、键。
完。

热点检测器_应JIT执行 接受 检测器、块ID：
    可变 记录 为 做 热点检测器_查询 用 检测器、块ID。
    若 记录 等于 空：
        返还 假。
    完。
    返还 记录 的 第 2 项 大于或等于 热点_热 并且 记录 的 第 4 项 大于或等于 0。
完。

可变 热点统计 为 一组：100、一组：（空）、0、0、0。

热点检测器_调整阈值 接受 检测器：
    键列表 为 做 字典键 用 检测器 的 第 0 项。
    可变 块数 为 做 计数 用 键列表。
    若 块数 等于 0：
        返还。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 块数：
        记录 为 做 字典获取 用 检测器 的 第 0 项、键列表 的 第 i 项。
        若 记录 不等于 空：
            总 为 总 加 记录 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    平均 为 总 除以 块数。
    热点统计 的 第 0 项 为 平均 乘 2。
    若 热点统计 的 第 0 项 小于 50：
        热点统计 的 第 0 项 为 50。
    完。
    若 热点统计 的 第 0 项 大于 10000：
        热点统计 的 第 0 项 为 10000。
    完。
    做 追加 用 热点统计 的 第 1 项、热点统计 的 第 0 项。
完。

热点_获取阈值 接受 零参：
    返还 热点统计 的 第 0 项。
完。

热点检测器_衰减 接受 检测器、因子：
    键列表 为 做 字典键 用 检测器 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        可变 记录 为 做 字典获取 用 检测器 的 第 0 项、键列表 的 第 i 项。
        若 记录 不等于 空：
            记录 的 第 1 项 为 记录 的 第 1 项 乘 因子 除以 100。
        完。
        i 为 i 加 1。
    完。
完。

热点检测器_TopN 接受 检测器、n：
    键列表 为 做 字典键 用 检测器 的 第 0 项。
    可变 块列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        记录 为 做 字典获取 用 检测器 的 第 0 项、键列表 的 第 i 项。
        若 记录 不等于 空：
            做 追加 用 块列表、一组：记录 的 第 0 项、记录 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    可变 k 为 0。
    只要 k 小于 n 并且 k 小于 做 计数 用 块列表：
        可变 j 为 k 加 1。
        只要 j 小于 做 计数 用 块列表：
            若 块列表 的 第 j 项 的 第 1 项 大于 块列表 的 第 k 项 的 第 1 项：
                临 为 块列表 的 第 k 项。
                块列表 的 第 k 项 为 块列表 的 第 j 项。
                块列表 的 第 j 项 为 临。
            完。
            j 为 j 加 1。
        完。
        k 为 k 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n 并且 i 小于 做 计数 用 块列表：
        做 追加 用 结果、块列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

热点_记录检测 接受 零参：
    热点统计 的 第 2 项 为 热点统计 的 第 2 项 加 1。
完。

热点_记录命中 接受 零参：
    热点统计 的 第 3 项 为 热点统计 的 第 3 项 加 1。
完。

热点_记录编译 接受 零参：
    热点统计 的 第 4 项 为 热点统计 的 第 4 项 加 1。
完。

热点_命中率 接受 零参：
    若 热点统计 的 第 2 项 等于 0：
        返还 0。
    完。
    返还 热点统计 的 第 3 项 乘 100 除以 热点统计 的 第 2 项。
完。

热点_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「检测次数」、做 转字符串 用 热点统计 的 第 2 项。
    做 字典设 用 r、「命中次数」、做 转字符串 用 热点统计 的 第 3 项。
    做 字典设 用 r、「编译次数」、做 转字符串 用 热点统计 的 第 4 项。
    做 字典设 用 r、「命中率%」、做 转字符串 用 做 热点_命中率。
    做 字典设 用 r、「热点阈值」、做 转字符串 用 JIT_热点阈值。
    返还 r。
完。

本章 收束。
