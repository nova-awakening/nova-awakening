《注意力机制》

引用《..·..·标准库·基础》。

注意力机制_向量_点积 接受 a、b：
    可变 n 为 做 计数 用 a。
    blen 为 做 计数 用 b。
    若 blen 小于 n：
        n 为 blen。
    完。
    可变 累加 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        ai 为 a 的 第 i 项。
        bi 为 b 的 第 i 项。
        积 为 ai 乘 bi。
        累加 为 累加 加 积。
        i 为 i 加 1。
    完。
    返还 累加。
完。

向量_缩放 接受 v、s：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        vi 为 v 的 第 i 项。
        scaled 为 vi 乘 s。
        做 追加 用 结果、scaled。
        i 为 i 加 1。
    完。
    返还 结果。
完。

注意力机制_向量_加 接受 a、b：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a：
        做 追加 用 结果、a 的 第 i 项 加 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

softmax_近似 接受 分数列表：
    可变 最大 为 分数列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 分数列表：
        若 分数列表 的 第 i 项 大于 最大：
            最大 为 分数列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 exp值 为 一组：（空）。
    可变 总和 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 分数列表：
        x 为 分数列表 的 第 i 项 减 最大。
        可变 ex 为 1000 加 x 加 x 乘 x 除以 2000。
        若 ex 小于 1：
            ex 为 1。
        完。
        做 追加 用 exp值、ex。
        总和 为 总和 加 ex。
        i 为 i 加 1。
    完。
    可变 概率 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 exp值：
        做 追加 用 概率、exp值 的 第 i 项 乘 1000 除以 总和。
        i 为 i 加 1。
    完。
    返还 概率。
完。

注意力机制_注意力_单头 接受 Q列表、K列表、V列表、d_k：
    seq_len 为 做 计数 用 Q列表。
    可变 输出 为 一组：（空）。
    缩放 为 d_k。
    可变 qi 为 0。
    只要 qi 小于 seq_len：
        可变 分数 为 一组：（空）。
        可变 kj 为 0。
        只要 kj 小于 seq_len：
            点 为 做 注意力机制_向量_点积 用 Q列表 的 第 qi 项、K列表 的 第 kj 项。
            做 追加 用 分数、点 乘 1000 除以 缩放。
            kj 为 kj 加 1。
        完。
        权重 为 做 softmax_近似 用 分数。
        d_v 为 做 计数 用 V列表 的 第 0 项。
        可变 加权V 为 一组：（空）。
        可变 d 为 0。
        只要 d 小于 d_v：
            做 追加 用 加权V、0。
            d 为 d 加 1。
        完。
        可变 vj 为 0。
        只要 vj 小于 seq_len：
            d 为 0。
            只要 d 小于 d_v：
                加权V 的 第 d 项 为 加权V 的 第 d 项 加 V列表 的 第 vj 项 的 第 d 项 乘 权重 的 第 vj 项 除以 1000。
                d 为 d 加 1。
            完。
            vj 为 vj 加 1。
        完。
        做 追加 用 输出、加权V。
        qi 为 qi 加 1。
    完。
    返还 输出。
完。

注意力_多头 接受 Q、K、V、num_heads、d_model：
    可变 拼接输出 为 一组：（空）。
    d_k 为 d_model 除以 num_heads。
    seq_len 为 做 计数 用 Q。
    可变 所有头输出 为 一组：（空）。
    可变 h 为 0。
    只要 h 小于 num_heads：
        起始 为 h 乘 d_k。
        可变 头Q 为 一组：（空）。
        可变 头K 为 一组：（空）。
        可变 头V 为 一组：（空）。
        可变 s 为 0。
        只要 s 小于 seq_len：
            可变 qslice 为 一组：（空）。
            可变 kslice 为 一组：（空）。
            可变 vslice 为 一组：（空）。
            可变 d 为 0。
            只要 d 小于 d_k：
                做 追加 用 qslice、Q 的 第 s 项 的 第 起始 加 d 项。
                做 追加 用 kslice、K 的 第 s 项 的 第 起始 加 d 项。
                做 追加 用 vslice、V 的 第 s 项 的 第 起始 加 d 项。
                d 为 d 加 1。
            完。
            做 追加 用 头Q、qslice。
            做 追加 用 头K、kslice。
            做 追加 用 头V、vslice。
            s 为 s 加 1。
        完。
        头输出 为 做 注意力机制_注意力_单头 用 头Q、头K、头V、d_k。
        做 追加 用 所有头输出、头输出。
        h 为 h 加 1。
    完。
    可变 si 为 0。
    只要 si 小于 seq_len：
        可变 行 为 一组：（空）。
        h 为 0。
        只要 h 小于 num_heads：
            可变 d 为 0。
            只要 d 小于 d_k：
                做 追加 用 行、所有头输出 的 第 h 项 的 第 si 项 的 第 d 项。
                d 为 d 加 1。
            完。
            h 为 h 加 1。
        完。
        做 追加 用 拼接输出、行。
        si 为 si 加 1。
    完。
    返还 拼接输出。
完。

注意力机制_KV缓存_创建 接受 最大长度：
    返还 一组：一组：（空）、一组：（空）、0、最大长度。
完。

注意力机制_KV缓存_追加 接受 缓存、k向量、v向量：
    若 缓存 的 第 2 项 小于 缓存 的 第 3 项：
        做 追加 用 缓存 的 第 0 项、k向量。
        做 追加 用 缓存 的 第 1 项、v向量。
        缓存 的 第 2 项 为 缓存 的 第 2 项 加 1。
    完。
完。

KV缓存_K列表 接受 缓存：
    返还 缓存 的 第 0 项。
完。

KV缓存_V列表 接受 缓存：
    返还 缓存 的 第 1 项。
完。

注意力机制_KV缓存_长度 接受 缓存：
    返还 缓存 的 第 2 项。
完。

注意力机制_位置编码_生成 接受 seq_len、d_model：
    可变 编码 为 一组：（空）。
    可变 pos 为 0。
    只要 pos 小于 seq_len：
        可变 行 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 d_model：
            可变 值 为 0。
            若 i 取余于 2 等于 0：
                值 为 pos 乘 1000 除以 (1 加 i 乘 i)。
            否则：
                值 为 1000 减 pos 乘 1000 除以 (1 加 i 乘 i)。
            完。
            做 追加 用 行、值。
            i 为 i 加 1。
        完。
        做 追加 用 编码、行。
        pos 为 pos 加 1。
    完。
    返还 编码。
完。

注意力_打印报告 接受 seq_len、d_model、num_heads：
    做 打印 用 「═══ 注意力机制报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  序列长度: 」、做 转字符串 用 seq_len。
    做 打印文本 用 做 字符串拼接 用 「  模型维度: 」、做 转字符串 用 d_model。
    做 打印文本 用 做 字符串拼接 用 「  注意力头: 」、做 转字符串 用 num_heads。
    做 打印文本 用 做 字符串拼接 用 「  每头维度: 」、做 转字符串 用 d_model 除以 num_heads。
    做 打印文本 用 做 字符串拼接 用 「  注意力矩阵: 」、做 字符串拼接 用 做 转字符串 用 seq_len、做 字符串拼接 用 「×」、做 转字符串 用 seq_len。
完。

本章 收束。
