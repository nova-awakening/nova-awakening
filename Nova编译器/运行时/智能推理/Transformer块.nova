《Transformer块》

引用《..·..·标准库·基础》。

引用《.·注意力机制》。

Transformer_层归一化 接受 向量、gamma、beta：
    可变 方差和 为 0。
    d 为 做 计数 用 向量。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 d：
        和 为 和 加 向量 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 和 除以 d。
    i 为 0。
    只要 i 小于 d：
        差 为 向量 的 第 i 项 减 均值。
        方差和 为 方差和 加 差 乘 差。
        i 为 i 加 1。
    完。
    方差 为 方差和 除以 d。
    可变 标准差 为 方差 除以 2 加 1。
    若 标准差 大于 0：
        标准差 为 (标准差 加 方差 除以 标准差) 除以 2。
    完。
    若 标准差 小于 1：
        标准差 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 d：
        归一 为 (向量 的 第 i 项 减 均值) 乘 1000 除以 标准差。
        缩放 为 归一 乘 gamma 的 第 i 项 除以 1000 加 beta 的 第 i 项。
        做 追加 用 结果、缩放。
        i 为 i 加 1。
    完。
    返还 结果。
完。

激活_修正线性 接受 向量：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 向量：
        若 向量 的 第 i 项 大于 0：
            做 追加 用 结果、向量 的 第 i 项。
        否则：
            做 追加 用 结果、0。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

激活_GELU 接受 向量：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 向量：
        x 为 向量 的 第 i 项。
        若 abs_x 小于 0：
            abs_x 为 0 减 abs_x。
        完。
        sig 为 (x 乘 1000 加 abs_x 乘 1000) 除以 (2 乘 abs_x 加 2)。
        做 追加 用 结果、x 乘 sig 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

线性变换 接受 输入、权重矩阵、偏置：
    输出维 为 做 计数 用 偏置。
    输入维 为 做 计数 用 输入。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 输出维：
        可变 和 为 偏置 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 输入维：
            和 为 和 加 输入 的 第 j 项 乘 权重矩阵 的 第 i 项 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

Transformer_FFN_前向 接受 输入、W1、b1、W2、b2：
    隐藏 为 做 线性变换 用 输入、W1、b1。
    激活 为 做 激活_GELU 用 隐藏。
    返还 做 线性变换 用 激活、W2、b2。
完。

残差_加 接受 输入、子层输出：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 输入：
        做 追加 用 结果、输入 的 第 i 项 加 子层输出 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

Transformer块_前向 接受 输入序列、参数、num_heads、d_model：
    gamma1 为 参数 的 第 0 项。
    beta1 为 参数 的 第 1 项。
    gamma2 为 参数 的 第 2 项。
    beta2 为 参数 的 第 3 项。
    seq_len 为 做 计数 用 输入序列。
    可变 归一1 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 seq_len：
        做 追加 用 归一1、做 Transformer_层归一化 用 输入序列 的 第 i 项、gamma1、beta1。
        i 为 i 加 1。
    完。
    注意力输出 为 做 注意力_多头 用 归一1、归一1、归一1、num_heads、d_model。
    可变 残差1 为 一组：（空）。
    i 为 0。
    只要 i 小于 seq_len：
        做 追加 用 残差1、做 残差_加 用 输入序列 的 第 i 项、注意力输出 的 第 i 项。
        i 为 i 加 1。
    完。
    W1 为 参数 的 第 4 项。
    b1 为 参数 的 第 5 项。
    W2 为 参数 的 第 6 项。
    b2 为 参数 的 第 7 项。
    可变 输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 seq_len：
        归一2 为 做 Transformer_层归一化 用 残差1 的 第 i 项、gamma2、beta2。
        ffn输出 为 做 Transformer_FFN_前向 用 归一2、W1、b1、W2、b2。
        做 追加 用 输出、做 残差_加 用 残差1 的 第 i 项、ffn输出。
        i 为 i 加 1。
    完。
    返还 输出。
完。

Transformer_Transformer配置_创建 接受 层数、d_model、num_heads、d_ff：
    返还 一组：层数、d_model、num_heads、d_ff。
完。

Transformer_打印报告 接受 配置：
    做 打印 用 「═══ Transformer配置报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  层数: 」、做 转字符串 用 配置 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  模型维度: 」、做 转字符串 用 配置 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  注意力头: 」、做 转字符串 用 配置 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  FFN维度: 」、做 转字符串 用 配置 的 第 3 项。
    做 打印文本 用 做 字符串拼接 用 「  每头维度: 」、做 转字符串 用 配置 的 第 1 项 除以 配置 的 第 2 项。
    参数估计 为 配置 的 第 0 项 乘 (4 乘 配置 的 第 1 项 乘 配置 的 第 1 项 加 2 乘 配置 的 第 1 项 乘 配置 的 第 3 项)。
    做 打印文本 用 做 字符串拼接 用 「  参数估计: ~」、做 字符串拼接 用 做 转字符串 用 参数估计 除以 1000000、「M」。
完。

本章 收束。
