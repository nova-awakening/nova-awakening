《内存分配》

引用《..·..·标准库·基础》。

列表_移除 接受 列表、索引：
    可变 新 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 i 不等于 索引：
            做 追加 用 新、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 新。
完。

对齐8 接受 n：
    返还 n 加 7 位与 0 减 8。
完。

log2_上取整 接受 n：
    可变 k 为 0。
    可变 v 为 1。
    只要 v 小于 n：
        v 为 v 乘 2。
        k 为 k 加 1。
    完。
    返还 k。
完。

幂2 接受 k：
    可变 v 为 1。
    可变 i 为 0。
    只要 i 小于 k：
        v 为 v 乘 2。
        i 为 i 加 1。
    完。
    返还 v。
完。

二次幂 接受 k：
    返还 做 幂2 用 k。
完。

伙伴_创建 接受 最大阶、最小阶：
    层数 为 最大阶 减 最小阶 加 1。
    可变 空闲表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 层数：
        做 追加 用 空闲表、一组：（空）。
        i 为 i 加 1。
    完。
    做 追加 用 空闲表 的 第 层数 减 1 项、0。
    可变 块表 为 一组：（空）。
    做 追加 用 块表、一组：0、最大阶、0。
    返还 一组：空闲表、最小阶、最大阶、层数、块表。
完。

伙伴_移除空闲 接受 分配器、层idx、地址：
    可变 新 为 一组：（空）。
    可变 找到 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分配器 的 第 0 项 的 第 层idx 项：
        若 分配器 的 第 0 项 的 第 层idx 项 的 第 i 项 等于 地址 并且 找到 等于 假：
            找到 为 真。
        否则：
            做 追加 用 新、分配器 的 第 0 项 的 第 层idx 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    分配器 的 第 0 项 的 第 层idx 项 为 新。
    返还 找到。
完。

伙伴_查块 接受 块表、地址：
    可变 i 为 0。
    只要 i 小于 做 计数 用 块表：
        若 块表 的 第 i 项 的 第 0 项 等于 地址：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

伙伴_分配 接受 分配器、大小：
    可变 需要阶 为 做 log2_上取整 用 大小。
    若 需要阶 小于 分配器 的 第 1 项：
        需要阶 为 分配器 的 第 1 项。
    完。
    若 需要阶 大于 分配器 的 第 2 项：
        返还 负 1。
    完。
    层偏移 为 需要阶 减 分配器 的 第 1 项。
    可变 找到阶 为 负 1。
    可变 k 为 层偏移。
    只要 k 小于 分配器 的 第 3 项：
        若 做 计数 用 分配器 的 第 0 项 的 第 k 项 大于 0：
            找到阶 为 k。
            k 为 分配器 的 第 3 项。
        否则：
            k 为 k 加 1。
        完。
    完。
    若 找到阶 小于 0：
        返还 负 1。
    完。
    实际阶 为 找到阶 加 分配器 的 第 1 项。
    可变 地址 为 分配器 的 第 0 项 的 第 找到阶 项 的 第 做 计数 用 分配器 的 第 0 项 的 第 找到阶 项 减 1 项。
    分配器 的 第 0 项 的 第 找到阶 项 为 做 列表_移除 用 分配器 的 第 0 项 的 第 找到阶 项、做 计数 用 分配器 的 第 0 项 的 第 找到阶 项 减 1。
    可变 当前阶 为 实际阶。
    可变 当前地址 为 地址。
    只要 当前阶 大于 需要阶：
        当前阶 为 当前阶 减 1。
        伙伴地址 为 当前地址 加 做 二次幂 用 当前阶。
        做 追加 用 分配器 的 第 0 项 的 第 当前阶 减 分配器 的 第 1 项 项、伙伴地址。
        做 追加 用 分配器 的 第 4 项、一组：伙伴地址、当前阶、0。
    完。
    可变 bi 为 做 伙伴_查块 用 分配器 的 第 4 项、当前地址。
    若 bi 大于或等于 0：
        分配器 的 第 4 项 的 第 bi 项 的 第 1 项 为 需要阶。
        分配器 的 第 4 项 的 第 bi 项 的 第 2 项 为 1。
    否则：
        做 追加 用 分配器 的 第 4 项、一组：当前地址、需要阶、1。
    完。
    分配器 的 第 5 项 为 分配器 的 第 5 项 加 1。
    返还 当前地址。
完。

伙伴_地址 接受 地址、阶：
    可变 块大小 为 做 二次幂 用 阶。
    返还 地址 位异或 块大小。
完。

伙伴_释放 接受 分配器、地址：
    bi 为 做 伙伴_查块 用 分配器 的 第 4 项、地址。
    若 bi 小于 0：
        返还 假。
    完。
    若 分配器 的 第 4 项 的 第 bi 项 的 第 2 项 等于 0：
        返还 假。
    完。
    可变 当前阶 为 分配器 的 第 4 项 的 第 bi 项 的 第 1 项。
    可变 当前地址 为 地址。
    分配器 的 第 4 项 的 第 bi 项 的 第 2 项 为 0。
    可变 合并成功 为 真。
    只要 合并成功 并且 当前阶 小于 分配器 的 第 2 项：
        伙伴addr 为 做 伙伴_地址 用 当前地址、当前阶。
        pi 为 做 伙伴_查块 用 分配器 的 第 4 项、伙伴addr。
        若 pi 大于或等于 0 并且 分配器 的 第 4 项 的 第 pi 项 的 第 2 项 等于 0 并且 分配器 的 第 4 项 的 第 pi 项 的 第 1 项 等于 当前阶：
            做 伙伴_移除空闲 用 分配器、当前阶 减 分配器 的 第 1 项、伙伴addr。
            若 伙伴addr 小于 当前地址：
                当前地址 为 伙伴addr。
            完。
            当前阶 为 当前阶 加 1。
            ui 为 做 伙伴_查块 用 分配器 的 第 4 项、当前地址。
            若 ui 大于或等于 0：
                分配器 的 第 4 项 的 第 ui 项 的 第 1 项 为 当前阶。
            完。
        否则：
            合并成功 为 假。
        完。
    完。
    做 追加 用 分配器 的 第 0 项 的 第 当前阶 减 分配器 的 第 1 项 项、当前地址。
    分配器 的 第 6 项 为 分配器 的 第 6 项 加 1。
    返还 真。
完。

伙伴_分配数 接受 分配器：
    返还 分配器 的 第 5 项。
完。

伙伴_释放数 接受 分配器：
    返还 分配器 的 第 6 项。
完。

伙伴_空闲块数 接受 分配器：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 分配器 的 第 3 项：
        总 为 总 加 做 计数 用 分配器 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

空闲链_最佳分配 接受 分配器、大小：
    可变 对齐大小 为 做 对齐8 用 大小。
    若 对齐大小 小于 8：
        对齐大小 为 8。
    完。
    可变 最佳 为 负 1。
    可变 最小差 为 0x7FFFFFFF。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分配器 的 第 0 项：
        若 分配器 的 第 0 项 的 第 i 项 的 第 2 项 等于 0 并且 分配器 的 第 0 项 的 第 i 项 的 第 1 项 大于或等于 对齐大小：
            差 为 分配器 的 第 0 项 的 第 i 项 的 第 1 项 减 对齐大小。
            若 差 小于 最小差：
                最小差 为 差。
                最佳 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最佳 小于 0：
        返还 负 1。
    完。
    可变 起始 为 分配器 的 第 0 项 的 第 最佳 项 的 第 0 项。
    可变 块大小 为 分配器 的 第 0 项 的 第 最佳 项 的 第 1 项。
    若 块大小 大于 对齐大小 加 8：
        可变 新块 为 一组：（空）。
        i 为 0。
        只要 i 小于 做 计数 用 分配器 的 第 0 项：
            若 i 等于 最佳：
                做 追加 用 新块、一组：起始、对齐大小、1。
                做 追加 用 新块、一组：起始 加 对齐大小、块大小 减 对齐大小、0。
            否则：
                做 追加 用 新块、分配器 的 第 0 项 的 第 i 项。
            完。
            i 为 i 加 1。
        完。
        分配器 的 第 0 项 为 新块。
    否则：
        分配器 的 第 0 项 的 第 最佳 项 的 第 2 项 为 1。
    完。
    分配器 的 第 2 项 为 分配器 的 第 2 项 加 1。
    返还 起始。
完。

空闲链_利用率 接受 分配器：
    可变 已用 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分配器 的 第 0 项：
        若 分配器 的 第 0 项 的 第 i 项 的 第 2 项 等于 1：
            已用 为 已用 加 分配器 的 第 0 项 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    若 分配器 的 第 1 项 等于 0：
        返还 0。
    完。
    返还 已用 乘 100 除以 分配器 的 第 1 项。
完。

空闲链_创建 接受 容量：
    可变 块列表 为 一组：（空）。
    做 追加 用 块列表、一组：0、容量、0。
    返还 一组：块列表、容量、0。
完。

空闲链_分配 接受 分配器、大小：
    可变 对齐大小 为 做 对齐8 用 大小。
    若 对齐大小 小于 8：
        对齐大小 为 8。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分配器 的 第 0 项：
        若 分配器 的 第 0 项 的 第 i 项 的 第 2 项 等于 0 并且 分配器 的 第 0 项 的 第 i 项 的 第 1 项 大于或等于 对齐大小：
            起始 为 分配器 的 第 0 项 的 第 i 项 的 第 0 项。
            块大小 为 分配器 的 第 0 项 的 第 i 项 的 第 1 项。
            若 块大小 大于 对齐大小 加 8：
                可变 新块 为 一组：（空）。
                可变 j 为 0。
                只要 j 小于 做 计数 用 分配器 的 第 0 项：
                    若 j 等于 i：
                        做 追加 用 新块、一组：起始、对齐大小、1。
                        做 追加 用 新块、一组：起始 加 对齐大小、块大小 减 对齐大小、0。
                    否则：
                        做 追加 用 新块、分配器 的 第 0 项 的 第 j 项。
                    完。
                    j 为 j 加 1。
                完。
                分配器 的 第 0 项 为 新块。
            否则：
                分配器 的 第 0 项 的 第 i 项 的 第 2 项 为 1。
            完。
            分配器 的 第 2 项 为 分配器 的 第 2 项 加 1。
            返还 起始。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

空闲链_释放 接受 分配器、地址：
    可变 找到 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分配器 的 第 0 项：
        若 分配器 的 第 0 项 的 第 i 项 的 第 0 项 等于 地址 并且 分配器 的 第 0 项 的 第 i 项 的 第 2 项 等于 1：
            分配器 的 第 0 项 的 第 i 项 的 第 2 项 为 0。
            找到 为 i。
        完。
        i 为 i 加 1。
    完。
    若 找到 小于 0：
        返还 假。
    完。
    可变 新块 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 分配器 的 第 0 项：
        当前 为 分配器 的 第 0 项 的 第 i 项。
        若 当前 的 第 2 项 等于 0 并且 做 计数 用 新块 大于 0 并且 新块 的 第 做 计数 用 新块 减 1 项 的 第 2 项 等于 0：
            新块 的 第 做 计数 用 新块 减 1 项 的 第 1 项 为 新块 的 第 做 计数 用 新块 减 1 项 的 第 1 项 加 当前 的 第 1 项。
        否则：
            做 追加 用 新块、一组：当前 的 第 0 项、当前 的 第 1 项、当前 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
    分配器 的 第 0 项 为 新块。
    返还 真。
完。

空闲链_空闲总量 接受 分配器：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分配器 的 第 0 项：
        若 分配器 的 第 0 项 的 第 i 项 的 第 2 项 等于 0：
            总 为 总 加 分配器 的 第 0 项 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 总。
完。

空闲链_碎片数 接受 分配器：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分配器 的 第 0 项：
        若 分配器 的 第 0 项 的 第 i 项 的 第 2 项 等于 0：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

内存分配_竞技场_创建 接受 容量：
    返还 一组：容量、0、一组：（空）。
完。

竞技场_分配 接受 arena、大小：
    对齐后 为 做 对齐8 用 大小。
    若 arena 的 第 1 项 加 对齐后 大于 arena 的 第 0 项：
        返还 负 1。
    完。
    地址 为 arena 的 第 1 项。
    arena 的 第 1 项 为 arena 的 第 1 项 加 对齐后。
    arena 的 第 2 项 为 arena 的 第 2 项 加 1。
    返还 地址。
完。

竞技场_保存 接受 arena：
    做 追加 用 arena 的 第 3 项、arena 的 第 1 项。
完。

竞技场_恢复 接受 arena：
    若 做 计数 用 arena 的 第 3 项 大于 0：
        上次 为 arena 的 第 3 项 的 第 做 计数 用 arena 的 第 3 项 减 1 项。
        arena 的 第 3 项 为 做 列表_移除 用 arena 的 第 3 项、做 计数 用 arena 的 第 3 项 减 1。
        arena 的 第 1 项 为 上次。
    完。
完。

竞技场_重置 接受 arena：
    arena 的 第 1 项 为 0。
    arena 的 第 2 项 为 0。
    arena 的 第 3 项 为 一组：（空）。
完。

竞技场_已用 接受 arena：
    返还 arena 的 第 1 项。
完。

竞技场_剩余 接受 arena：
    返还 arena 的 第 0 项 减 arena 的 第 1 项。
完。

内存分配_slab_创建 接受 对象大小、初始数量：
    可变 空闲 为 一组：（空）。
    可变 i 为 初始数量 减 1。
    只要 i 大于或等于 0：
        做 追加 用 空闲、i。
        i 为 i 减 1。
    完。
    返还 一组：对象大小、空闲、初始数量、0。
完。

内存分配_slab_分配 接受 slab：
    若 做 计数 用 slab 的 第 1 项 等于 0：
        返还 负 1。
    完。
    可变 idx 为 slab 的 第 1 项 的 第 做 计数 用 slab 的 第 1 项 减 1 项。
    slab 的 第 1 项 为 做 列表_移除 用 slab 的 第 1 项、做 计数 用 slab 的 第 1 项 减 1。
    slab 的 第 3 项 为 slab 的 第 3 项 加 1。
    返还 idx 乘 slab 的 第 0 项。
完。

内存分配_slab_释放 接受 slab、地址：
    idx 为 地址 除以 slab 的 第 0 项。
    做 追加 用 slab 的 第 1 项、idx。
    slab 的 第 3 项 为 slab 的 第 3 项 减 1。
完。

slab_已用 接受 slab：
    返还 slab 的 第 3 项。
完。

slab_空闲 接受 slab：
    返还 做 计数 用 slab 的 第 1 项。
完。

分配器_内存池_创建 接受 块大小、块数：
    可变 池 为 一组：块大小、块数、一组：（空）、0。
    可变 i 为 0。
    只要 i 小于 块数：
        做 追加 用 池 的 第 2 项、i。
        i 为 i 加 1。
    完。
    返还 池。
完。

分配器_内存池_分配 接受 池：
    可变 新空闲 为 一组：（空）。
    若 做 计数 用 池 的 第 2 项 等于 0：
        返还 负 1。
    完。
    n 为 做 计数 用 池 的 第 2 项。
    idx 为 池 的 第 2 项 的 第 n 减 1 项。
    可变 k 为 0。
    只要 k 小于 n 减 1：
        做 追加 用 新空闲、池 的 第 2 项 的 第 k 项。
        k 为 k 加 1。
    完。
    池 的 第 2 项 为 新空闲。
    池 的 第 3 项 为 池 的 第 3 项 加 1。
    返还 idx 乘 池 的 第 0 项。
完。

简易池_释放 接受 池、地址：
    idx 为 地址 除以 池 的 第 0 项。
    做 追加 用 池 的 第 2 项、idx。
    池 的 第 3 项 为 池 的 第 3 项 减 1。
完。

简易池_利用率 接受 池：
    若 池 的 第 1 项 等于 0：
        返还 0。
    完。
    返还 池 的 第 3 项 乘 100 除以 池 的 第 1 项。
完。

内存分配_伙伴_阶 接受 大小：
    可变 阶 为 0。
    可变 s 为 1。
    只要 s 小于 大小：
        s 为 s 乘 2。
        阶 为 阶 加 1。
    完。
    返还 阶。
完。

伙伴_对齐 接受 大小：
    可变 s 为 1。
    只要 s 小于 大小：
        s 为 s 乘 2。
    完。
    返还 s。
完。

可变 内存统计 为 一组：0、0、0、0、0。

内存分配_内存_记录分配 接受 大小：
    内存统计 的 第 0 项 为 内存统计 的 第 0 项 加 1。
    内存统计 的 第 2 项 为 内存统计 的 第 2 项 加 大小。
    内存统计 的 第 4 项 为 内存统计 的 第 4 项 加 大小。
    若 内存统计 的 第 4 项 大于 内存统计 的 第 3 项：
        内存统计 的 第 3 项 为 内存统计 的 第 4 项。
    完。
完。

内存分配_内存_记录释放 接受 大小：
    内存统计 的 第 1 项 为 内存统计 的 第 1 项 加 1。
    内存统计 的 第 4 项 为 内存统计 的 第 4 项 减 大小。
完。

内存_泄漏检测 接受 零参：
    差 为 内存统计 的 第 0 项 减 内存统计 的 第 1 项。
    若 差 大于 100：
        返还 「严重泄漏: 未释放」 加 做 转字符串 用 差 加 「块」。
    完。
    若 差 大于 10：
        返还 「可能泄漏: 」 加 做 转字符串 用 差 加 「块未释放」。
    完。
    若 差 大于 0：
        返还 「轻微泄漏」。
    完。
    返还 「无泄漏」。
完。

内存_碎片评估 接受 零参：
    若 内存统计 的 第 3 项 等于 0：
        返还 「无数据」。
    完。
    利用率 为 内存统计 的 第 4 项 乘 100 除以 内存统计 的 第 3 项。
    若 利用率 小于 30：
        返还 「碎片严重(考虑压缩)」。
    完。
    若 利用率 小于 60：
        返还 「中等碎片」。
    完。
    返还 「碎片率低」。
完。

内存_重置 接受 零参：
    内存统计 的 第 0 项 为 0。
    内存统计 的 第 1 项 为 0。
    内存统计 的 第 2 项 为 0。
    内存统计 的 第 3 项 为 0。
    内存统计 的 第 4 项 为 0。
完。

内存_平均分配大小 接受 零参：
    若 内存统计 的 第 0 项 等于 0：
        返还 0。
    完。
    返还 内存统计 的 第 2 项 除以 内存统计 的 第 0 项。
完。

内存_推荐池大小 接受 零参：
    平均 为 做 内存_平均分配大小。
    若 平均 大于 4096：
        返还 「大对象池(>4KB)」。
    完。
    若 平均 大于 256：
        返还 「中等对象池」。
    完。
    返还 「小对象Slab分配」。
完。

本章 收束。
