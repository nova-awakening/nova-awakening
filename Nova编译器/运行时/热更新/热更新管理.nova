《热更新管理》

引用《..·..·标准库·基础》。

引用《..·..·编译器·词法·词法分析》。

引用《..·..·IR·字节码·指令集》。

引用《..·..·IR·字节码·编码器》。

引用《..·解释器·栈式VM》。

引用《..·..·编译器·语法·解析器》。

引用《..·..·标准库·系统·硬件库》。

引用《..·原语桩》。

热更_空闲 为 0。

热更_检测中 为 1。

热更_编译中 为 2。

热更_替换中 为 3。

热更_回滚中 为 4。

可变 _热更_版本历史 为 一组：（空）。

可变 _热更_回滚栈 为 一组：（空）。

可变 _热更_监控列表 为 一组：（空）。

可变 _热更_文件哈希 为 一组：（空）。

可变 _热更_模块表 为 一组：（空）。

可变 _热更_依赖图 为 一组：（空）。

可变 _热更_统计 为 一组：0、0、0。

热更_创建快照 接受 版本号、字节码块、全局状态：
    可变 状态副本 为 做 字典。
    键列表 为 做 字典键 用 全局状态。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        键 为 键列表 的 第 i 项。
        值 为 做 字典获取 用 全局状态、键。
        若 做 是列表 用 值：
            可变 副本列表 为 一组：（空）。
            可变 j 为 0。
            只要 j 小于 做 计数 用 值：
                做 追加 用 副本列表、值 的 第 j 项。
                j 为 j 加 1。
            完。
            做 字典设置 用 状态副本、键、副本列表。
        否则：
            做 字典设置 用 状态副本、键、值。
        完。
        i 为 i 加 1。
    完。
    返还 一组：版本号、字节码块、做 字典、状态副本、做 时间。
完。

热更_快照版本 接受 快照：
    返还 快照 的 第 0 项。
完。

热更管理器_创建 接受 零参：
    返还 一组：0、热更_空闲、一组：（空）、5、做 字典、空。
完。

热更管理器_版本 接受 管理器：
    返还 管理器 的 第 0 项。
完。

热更管理器_状态 接受 管理器：
    返还 管理器 的 第 1 项。
完。

热更管理器_监控文件 接受 管理器、路径：
    若 做 文件存在 用 路径：
        做 字典设置 用 管理器 的 第 4 项、路径。
        返还 真。
    完。
    返还 假。
完。

热更_内容哈希 接受 内容：
    若 内容 等于 空 或者 内容 等于 「」：
        返还 0。
    完。
    可变 h 为 5381。
    长 为 做 字节长度 用 内容。
    可变 i 为 0。
    只要 i 小于 长：
        h 为 h 乘 33 加 做 编码 用 做 子串 用 内容、i、1 位与 0x7FFFFFFF。
        i 为 i 加 1。
    完。
    返还 h。
完。

热更管理器_检查变更 接受 管理器：
    可变 变更列表 为 一组：（空）。
    监控键 为 做 字典键 用 管理器 的 第 4 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 监控键：
        路径 为 监控键 的 第 i 项。
        若 做 文件存在 用 路径：
            内容 为 做 读文件 用 路径。
            新哈希 为 做 热更_内容哈希 用 内容。
            旧哈希 为 做 字典获取 用 管理器 的 第 4 项、路径。
            若 新哈希 不等于 旧哈希：
                做 字典设置 用 管理器 的 第 4 项、路径、新哈希。
                做 追加 用 变更列表、路径。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 变更列表。
完。

热更管理器_更新 接受 管理器、新源码、vm：
    可变 新列表 为 一组：（空）。
    管理器 的 第 1 项 为 热更_编译中。
    快照 为 做 热更_创建快照 用 管理器 的 第 0 项、做 VM_字节码 用 vm、做 VM_全局表 用 vm。
    做 追加 用 管理器 的 第 2 项、快照。
    只要 做 计数 用 管理器 的 第 2 项 大于 管理器 的 第 3 项：
        可变 i 为 1。
        只要 i 小于 做 计数 用 管理器 的 第 2 项：
            做 追加 用 新列表、管理器 的 第 2 项 的 第 i 项。
            i 为 i 加 1。
        完。
        管理器 的 第 2 项 为 新列表。
    完。
    可变 新字节码 为 空。
    若 新源码 不等于 空 并且 做 字节长度 用 新源码 大于 0：
        lex 为 做 词法器_创建 用 新源码。
        做 词法器_扫描全部 用 lex。
        标记列 为 做 词法器_标记 用 lex。
        若 做 计数 用 标记列 大于 0：
            ast 为 做 解析器_解析 用 解析器。
            若 ast 不等于 空 并且 做 是列表 用 ast：
                块 为 做 Nova字节码_创建块。
                编码ctx 为 做 Nova编码_创建上下文 用 块。
                可变 si 为 0。
                只要 si 小于 做 计数 用 ast：
                    若 做 是列表 用 ast 的 第 si 项 并且 做 计数 用 ast 的 第 si 项 大于或等于 1：
                        做 Nova编码_编译语句 用 编码ctx、ast 的 第 si 项。
                    完。
                    si 为 si 加 1。
                完。
                做 Nova字节码_发射 用 块、指令_停机。
                新字节码 为 块。
            完。
        完。
    完。
    管理器 的 第 1 项 为 热更_替换中。
    若 新字节码 不等于 空：
        旧字节码 为 做 VM_字节码 用 vm。
        变更 为 做 热更_差异检测 用 旧字节码、新字节码。
        vm 的 第 6 项 为 新字节码。
        可变 ci 为 0。
        只要 ci 小于 做 计数 用 变更：
            做 热更_标记JIT失效 用 变更 的 第 ci 项。
            ci 为 ci 加 1。
        完。
    完。
    管理器 的 第 0 项 为 管理器 的 第 0 项 加 1。
    管理器 的 第 1 项 为 热更_空闲。
    做 热更_记录更新。
    做 打印 用 做 字符串拼接 用 「热更新完成: v」、做 转字符串 用 管理器 的 第 0 项。
    返还 真。
完。

热更管理器_回滚 接受 管理器、vm：
    若 做 计数 用 管理器 的 第 2 项 等于 0：
        做 打印 用 「错误: 没有可回滚的版本」。
        返还 假。
    完。
    管理器 的 第 1 项 为 热更_回滚中。
    快照 为 管理器 的 第 2 项 的 第 做 计数 用 管理器 的 第 2 项 减 1 项。
    快照字节码 为 快照 的 第 1 项。
    若 快照字节码 不等于 空：
        vm 的 第 6 项 为 快照字节码。
    完。
    快照状态 为 快照 的 第 3 项。
    若 快照状态 不等于 空：
        键列表 为 做 字典键 用 快照状态。
        可变 i 为 0。
        只要 i 小于 做 计数 用 键列表：
            键 为 键列表 的 第 i 项。
            做 字典设置 用 vm 的 第 4 项、键、做 字典获取 用 快照状态、键。
            i 为 i 加 1。
        完。
    完。
    做 热更_清除JIT失效。
    可变 新快照列 为 一组：（空）。
    可变 si 为 0。
    只要 si 小于 做 计数 用 管理器 的 第 2 项 减 1：
        做 追加 用 新快照列、管理器 的 第 2 项 的 第 si 项。
        si 为 si 加 1。
    完。
    管理器 的 第 2 项 为 新快照列。
    管理器 的 第 0 项 为 做 热更_快照版本 用 快照。
    管理器 的 第 1 项 为 热更_空闲。
    做 热更_记录回滚。
    做 打印 用 做 字符串拼接 用 「回滚到版本: v」、做 转字符串 用 管理器 的 第 0 项。
    返还 真。
完。

热更_差异检测 接受 旧块、新块：
    可变 变更 为 一组：（空）。
    若 旧块 等于 空 或者 新块 等于 空：
        返还 变更。
    完。
    可变 旧函数表 为 空。
    可变 新函数表 为 空。
    若 做 是列表 用 旧块 并且 做 计数 用 旧块 大于或等于 7：
        旧函数表 为 旧块 的 第 6 项。
    完。
    若 做 是列表 用 新块 并且 做 计数 用 新块 大于或等于 7：
        新函数表 为 新块 的 第 6 项。
    完。
    若 旧函数表 等于 空 或者 新函数表 等于 空：
        返还 变更。
    完。
    新键 为 做 字典键 用 新函数表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 新键：
        名 为 新键 的 第 i 项。
        旧信息 为 做 字典获取 用 旧函数表、名。
        若 旧信息 等于 空：
            做 追加 用 变更、名。
        否则：
            新信息 为 做 字典获取 用 新函数表、名。
            若 做 是列表 用 旧信息 并且 做 是列表 用 新信息 并且 做 计数 用 旧信息 大于或等于 2 并且 做 计数 用 新信息 大于或等于 2：
                若 旧信息 的 第 0 项 不等于 新信息 的 第 0 项 或者 旧信息 的 第 1 项 不等于 新信息 的 第 1 项：
                    做 追加 用 变更、名。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    旧键 为 做 字典键 用 旧函数表。
    可变 j 为 0。
    只要 j 小于 做 计数 用 旧键：
        若 做 字典获取 用 新函数表、旧键 的 第 j 项 等于 空：
            做 追加 用 变更、旧键 的 第 j 项。
        完。
        j 为 j 加 1。
    完。
    返还 变更。
完。

热更_记录版本 接受 管理器、描述：
    可变 版本项 为 一组：0、空、0。
    版本项 的 第 0 项 为 管理器 的 第 0 项。
    版本项 的 第 1 项 为 描述。
    做 追加 用 _热更_版本历史、版本项。
    做 追加 用 _热更_回滚栈、管理器 的 第 0 项。
完。

热更_回滚 接受 管理器：
    可变 新栈 为 一组：（空）。
    若 做 计数 用 _热更_回滚栈 小于 2：
        返还 假。
    完。
    当前数 为 做 计数 用 _热更_回滚栈。
    可变 i 为 0。
    只要 i 小于 当前数 减 1：
        做 追加 用 新栈、_热更_回滚栈 的 第 i 项。
        i 为 i 加 1。
    完。
    _热更_回滚栈 为 新栈。
    管理器 的 第 0 项 为 _热更_回滚栈 的 第 做 计数 用 _热更_回滚栈 减 1 项。
    返还 真。
完。

热更_获取版本 接受 管理器：
    返还 管理器 的 第 0 项。
完。

热更_版本数 接受 零参：
    返还 做 计数 用 _热更_版本历史。
完。

热更_添加监控 接受 文件路径：
    做 追加 用 _热更_监控列表、文件路径。
    做 追加 用 _热更_文件哈希、0。
完。

热更_移除监控 接受 文件路径：
    可变 新列表 为 一组：（空）。
    可变 新哈希 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _热更_监控列表：
        若 _热更_监控列表 的 第 i 项 不等于 文件路径：
            做 追加 用 新列表、_热更_监控列表 的 第 i 项。
            做 追加 用 新哈希、_热更_文件哈希 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    _热更_监控列表 为 新列表。
    _热更_文件哈希 为 新哈希。
完。

热更_监控数 接受 零参：
    返还 做 计数 用 _热更_监控列表。
完。

热更_检测变更 接受 文件索引、新哈希：
    若 文件索引 大于或等于 做 计数 用 _热更_文件哈希：
        返还 假。
    完。
    若 _热更_文件哈希 的 第 文件索引 项 不等于 新哈希：
        _热更_文件哈希 的 第 文件索引 项 为 新哈希。
        返还 真。
    完。
    返还 假。
完。

热更_注册模块 接受 名称、地址、大小：
    idx 为 做 热更_查找模块 用 名称。
    若 idx 大于或等于 0：
        返还 假。
    完。
    可变 模块项 为 一组：空、0、0。
    模块项 的 第 0 项 为 名称。
    模块项 的 第 1 项 为 地址。
    模块项 的 第 2 项 为 大小。
    做 追加 用 _热更_模块表、模块项。
    返还 真。
完。

热更_查找模块 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _热更_模块表：
        若 做 字符串等于 用 _热更_模块表 的 第 i 项 的 第 0 项、名称：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

热更_替换模块 接受 名称、新地址、新大小：
    idx 为 做 热更_查找模块 用 名称。
    若 idx 小于 0：
        返还 假。
    完。
    _热更_模块表 的 第 idx 项 的 第 1 项 为 新地址。
    _热更_模块表 的 第 idx 项 的 第 2 项 为 新大小。
    _热更_模块表 的 第 idx 项 的 第 3 项 为 _热更_模块表 的 第 idx 项 的 第 3 项 加 1。
    返还 真。
完。

热更_模块数 接受 零参：
    返还 做 计数 用 _热更_模块表。
完。

热更_获取模块信息 接受 名称：
    idx 为 做 热更_查找模块 用 名称。
    若 idx 小于 0：
        返还 空。
    完。
    返还 _热更_模块表 的 第 idx 项。
完。

热更_模块快照 接受 管理器：
    可变 快照 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _热更_模块表：
        做 追加 用 快照、一组：_热更_模块表 的 第 i 项 的 第 0 项、_热更_模块表 的 第 i 项 的 第 1 项、_热更_模块表 的 第 i 项 的 第 2 项。
        i 为 i 加 1。
    完。
    做 追加 用 管理器 的 第 2 项、一组：管理器 的 第 0 项、快照。
    返还 做 计数 用 管理器 的 第 2 项 减 1。
完。

热更_恢复快照 接受 管理器、快照索引：
    若 快照索引 大于或等于 做 计数 用 管理器 的 第 2 项：
        返还 假。
    完。
    快照 为 管理器 的 第 2 项 的 第 快照索引 项。
    管理器 的 第 0 项 为 快照 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 快照 的 第 1 项：
        做 热更_替换模块 用 快照 的 第 1 项 的 第 i 项 的 第 0 项、快照 的 第 1 项 的 第 i 项 的 第 1 项、快照 的 第 1 项 的 第 i 项 的 第 2 项。
        i 为 i 加 1。
    完。
    返还 真。
完。

热更_添加依赖 接受 模块A、模块B：
    可变 依赖项 为 一组：空、空。
    依赖项 的 第 0 项 为 模块A。
    依赖项 的 第 1 项 为 模块B。
    做 追加 用 _热更_依赖图、依赖项。
完。

热更_获取依赖 接受 模块名：
    可变 依赖 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _热更_依赖图：
        若 做 字符串等于 用 _热更_依赖图 的 第 i 项 的 第 0 项、模块名：
            做 追加 用 依赖、_热更_依赖图 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 依赖。
完。

热更_影响分析 接受 模块名：
    可变 受影响 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _热更_依赖图：
        若 做 字符串等于 用 _热更_依赖图 的 第 i 项 的 第 1 项、模块名：
            做 追加 用 受影响、_热更_依赖图 的 第 i 项 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 受影响。
完。

热更_安全检查 接受 模块名：
    依赖 为 做 热更_获取依赖 用 模块名。
    若 做 计数 用 依赖 大于 10：
        返还 「依赖过多(风险高)」。
    完。
    影响 为 做 热更_影响分析 用 模块名。
    若 做 计数 用 影响 大于 5：
        返还 「影响范围大(谨慎替换)」。
    完。
    返还 「安全」。
完。

热更_记录更新 接受 零参：
    _热更_统计 的 第 0 项 为 _热更_统计 的 第 0 项 加 1。
完。

热更_记录回滚 接受 零参：
    _热更_统计 的 第 1 项 为 _热更_统计 的 第 1 项 加 1。
完。

热更_记录失败 接受 零参：
    _热更_统计 的 第 2 项 为 _热更_统计 的 第 2 项 加 1。
完。

可变 _热更_JIT失效列表 为 一组：（空）。

热更_标记JIT失效 接受 函数名：
    做 追加 用 _热更_JIT失效列表、函数名。
完。

热更_JIT是否失效 接受 函数名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _热更_JIT失效列表：
        若 做 字符串等于 用 _热更_JIT失效列表 的 第 i 项、函数名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

热更_清除JIT失效 接受 零参：
    _热更_JIT失效列表 为 一组：（空）。
完。

热更_执行完整管线 接受 管理器、vm、变更模块名、新源码路径：
    若 做 热更_白名单数 大于 0：
        守卫结果 为 做 热更_白名单守卫 用 变更模块名。
        若 做 字符串等于 用 守卫结果、「pass」 等于 假：
            做 打印 用 做 字符串拼接 用 「热更拒绝: 」、做 字符串拼接 用 变更模块名、做 字符串拼接 用 「 → 」、守卫结果。
            做 热更_记录失败。
            返还 假。
        完。
    完。
    做 热更_安全检查 用 变更模块名。
    做 热更_模块快照 用 管理器。
    做 热更_记录版本 用 管理器、做 字符串拼接 用 「热更: 」、变更模块名。
    影响列表 为 做 热更_影响分析 用 变更模块名。
    可变 i 为 0。
    只要 i 小于 做 计数 用 影响列表：
        做 热更_标记JIT失效 用 影响列表 的 第 i 项。
        i 为 i 加 1。
    完。
    做 热更_标记JIT失效 用 变更模块名。
    管理器 的 第 1 项 为 热更_编译中。
    可变 新源码 为 空。
    若 新源码路径 不等于 空 并且 做 文件存在 用 新源码路径：
        新源码 为 做 读文件 用 新源码路径。
    完。
    若 新源码 等于 空 或者 做 字节长度 用 新源码 等于 0：
        管理器 的 第 1 项 为 热更_空闲。
        做 热更_记录失败。
        返还 假。
    完。
    可变 新字节码 为 空。
    lex 为 做 词法器_创建 用 新源码。
    做 词法器_扫描全部 用 lex。
    标记列 为 做 词法器_标记 用 lex。
    若 做 计数 用 标记列 大于 0：
        ast 为 做 解析器_解析 用 解析器。
        若 ast 不等于 空 并且 做 是列表 用 ast：
            块 为 做 Nova字节码_创建块。
            编码ctx 为 做 Nova编码_创建上下文 用 块。
            可变 si 为 0。
            只要 si 小于 做 计数 用 ast：
                若 做 是列表 用 ast 的 第 si 项 并且 做 计数 用 ast 的 第 si 项 大于或等于 1：
                    做 Nova编码_编译语句 用 编码ctx、ast 的 第 si 项。
                完。
                si 为 si 加 1。
            完。
            做 Nova字节码_发射 用 块、指令_停机。
            新字节码 为 块。
        完。
    完。
    管理器 的 第 1 项 为 热更_替换中。
    若 新字节码 不等于 空：
        旧字节码 为 做 VM_字节码 用 vm。
        变更 为 做 热更_差异检测 用 旧字节码、新字节码。
        vm 的 第 6 项 为 新字节码。
        可变 ci 为 0。
        只要 ci 小于 做 计数 用 变更：
            做 热更_标记JIT失效 用 变更 的 第 ci 项。
            ci 为 ci 加 1。
        完。
    否则：
        管理器 的 第 1 项 为 热更_空闲。
        做 热更_记录失败。
        返还 假。
    完。
    合同 为 做 热更_获取状态合同 用 变更模块名。
    若 合同 不等于 空：
        策略 为 合同 的 第 2 项。
        若 策略 等于 迁移_重置学习：
            做 字典设置 用 做 VM_全局表 用 vm、做 字符串拼接 用 「__学习状态_」、变更模块名、空。
        完。
        若 策略 等于 迁移_完整快照：
            做 热更_记录版本 用 管理器、做 字符串拼接 用 「迁移快照: 」、变更模块名。
        完。
    完。
    若 新字节码 不等于 空：
        新大小 为 做 计数 用 新字节码 的 第 4 项。
        若 做 热更_查找模块 用 变更模块名 小于 0：
            做 热更_注册模块 用 变更模块名、0、新大小。
        否则：
            做 热更_替换模块 用 变更模块名、0、新大小。
        完。
    完。
    管理器 的 第 0 项 为 管理器 的 第 0 项 加 1。
    管理器 的 第 1 项 为 热更_空闲。
    做 热更_记录更新。
    返还 真。
完。

热更_统计报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「更新次数」、做 转字符串 用 _热更_统计 的 第 0 项。
    做 字典设 用 r、「回滚次数」、做 转字符串 用 _热更_统计 的 第 1 项。
    做 字典设 用 r、「失败次数」、做 转字符串 用 _热更_统计 的 第 2 项。
    做 字典设 用 r、「监控文件数」、做 转字符串 用 做 计数 用 _热更_监控列表。
    做 字典设 用 r、「模块数」、做 转字符串 用 做 计数 用 _热更_模块表。
    做 字典设 用 r、「JIT失效数」、做 转字符串 用 做 计数 用 _热更_JIT失效列表。
    做 字典设 用 r、「版本历史数」、做 转字符串 用 做 计数 用 _热更_版本历史。
    返还 r。
完。

可变 _调试_断点表 为 做 字典。

可变 _调试_启用 为 假。

可变 _调试_单步模式 为 假。

可变 _调试_断点数值 为 0。

可变 _调试_当前行 为 0。

可变 _调试_当前函数 为 「」。

可变 _调试_调用栈 为 一组：（空）。

调试器_启用 接受 零参：
    _调试_启用 为 真。
完。

调试器_禁用 接受 零参：
    _调试_启用 为 假。
完。

调试器_是否启用 接受 零参：
    返还 _调试_启用。
完。

_调试_断点键 接受 文件、行号：
    返还 做 字符串拼接 用 文件、做 字符串拼接 用 「:」、做 转字符串 用 行号。
完。

调试器_重置 接受 零参：
    _调试_断点表 为 做 字典。
    _调试_断点数值 为 0。
    _调试_启用 为 假。
    _调试_单步模式 为 假。
    _调试_当前行 为 0。
    _调试_当前函数 为 「」。
    _调试_调用栈 为 一组：（空）。
完。

调试器_添加断点 接受 文件、行号：
    键 为 做 _调试_断点键 用 文件、行号。
    若 做 字典取 用 _调试_断点表、键 等于 空：
        _调试_断点数值 为 _调试_断点数值 加 1。
    完。
    做 字典设 用 _调试_断点表、键、真。
完。

调试器_移除断点 接受 文件、行号：
    键 为 做 _调试_断点键 用 文件、行号。
    若 做 字典取 用 _调试_断点表、键 不等于 空：
        _调试_断点表 为 做 字典删 用 _调试_断点表、键。
        若 _调试_断点数值 大于 0：
            _调试_断点数值 为 _调试_断点数值 减 1。
        完。
    完。
完。

调试器_命中断点 接受 文件、行号：
    若 _调试_启用 等于 假：
        返还 假。
    完。
    若 _调试_单步模式：
        返还 真。
    完。
    返还 做 字典取 用 _调试_断点表、做 _调试_断点键 用 文件、行号 等于 真。
完。

调试器_设置单步 接受 启用：
    _调试_单步模式 为 启用。
完。

调试器_更新位置 接受 行号、函数名：
    _调试_当前行 为 行号。
    _调试_当前函数 为 函数名。
完。

调试器_压入调用 接受 函数名：
    做 追加 用 _调试_调用栈、函数名。
完。

调试器_弹出调用 接受 零参：
    可变 新栈 为 一组：（空）。
    若 做 计数 用 _调试_调用栈 大于 0：
        可变 i 为 0。
        只要 i 小于 做 计数 用 _调试_调用栈 减 1：
            做 追加 用 新栈、_调试_调用栈 的 第 i 项。
            i 为 i 加 1。
        完。
        _调试_调用栈 为 新栈。
    完。
完。

调试器_调用栈 接受 零参：
    返还 _调试_调用栈。
完。

调试器_断点数 接受 零参：
    返还 _调试_断点数值。
完。

调试器_报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「启用」、做 转字符串 用 _调试_启用。
    做 字典设 用 r、「单步」、做 转字符串 用 _调试_单步模式。
    做 字典设 用 r、「当前行」、做 转字符串 用 _调试_当前行。
    做 字典设 用 r、「当前函数」、_调试_当前函数。
    做 字典设 用 r、「断点数」、做 转字符串 用 _调试_断点数值。
    做 字典设 用 r、「调用栈深度」、做 转字符串 用 做 计数 用 _调试_调用栈。
    返还 r。
完。

可变 _热更_白名单 为 一组：（空）。

可变 _热更_状态合同 为 一组：（空）。

热更_添加白名单 接受 模块名、状态类型、迁移策略：
    做 追加 用 _热更_白名单、模块名。
    可变 合同项 为 一组：空、空、空。
    合同项 的 第 0 项 为 模块名。
    合同项 的 第 1 项 为 状态类型。
    合同项 的 第 2 项 为 迁移策略。
    做 追加 用 _热更_状态合同、合同项。
完。

热更_在白名单中 接受 模块名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _热更_白名单：
        若 做 字符串等于 用 _热更_白名单 的 第 i 项、模块名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

热更_获取状态合同 接受 模块名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _热更_状态合同：
        若 做 字符串等于 用 _热更_状态合同 的 第 i 项 的 第 0 项、模块名：
            返还 _热更_状态合同 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

热更_白名单数 接受 零参：
    返还 做 计数 用 _热更_白名单。
完。

热更_白名单守卫 接受 模块名：
    若 做 热更_在白名单中 用 模块名 等于 假：
        返还 「拒绝: 模块不在热更白名单中」。
    完。
    合同 为 做 热更_获取状态合同 用 模块名。
    若 合同 等于 空：
        返还 「拒绝: 模块无状态迁移合同」。
    完。
    返还 「pass」。
完。

迁移_无状态 为 「无状态」。

迁移_保留记忆 为 「保留记忆」。

迁移_重置学习 为 「重置学习」。

迁移_完整快照 为 「完整快照」。

热更_白名单报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「白名单模块数」、做 转字符串 用 做 计数 用 _热更_白名单。
    做 字典设 用 r、「状态合同数」、做 转字符串 用 做 计数 用 _热更_状态合同。
    返还 r。
完。

本章 收束。
