《字典引擎》

引用《..·..·标准库·哈希表·哈希表》。

引用《..·..·标准库·基础》。

引用《..·..·标准库·集合·字典·有序字典》。

字符串相等 接受 a、b：
    若 做 计数 用 a 不等于 做 计数 用 b：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a：
        若 做 编码 用 做 子串 用 a、i、1 不等于 做 编码 用 做 子串 用 b、i、1：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

djb2_哈希 接受 键：
    可变 h 为 5381。
    可变 n 为 做 计数 用 键。
    可变 i 为 0。
    只要 i 小于 n：
        h 为 h 乘 33 加 做 编码 用 做 子串 用 键、i、1。
        i 为 i 加 1。
    完。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    返还 h。
完。

整数_哈希 接受 n：
    可变 x 为 n。
    若 x 小于 0：
        x 为 0 减 x。
    完。
    x 为 x 乘 2654435761。
    若 x 小于 0：
        x 为 0 减 x。
    完。
    返还 x。
完。

字典_哈希表_创建 接受 初始容量：
    可变 cap 为 16。
    只要 cap 小于 初始容量：
        cap 为 cap 乘 2。
    完。
    可变 状态 为 一组：（空）。
    可变 键 为 一组：（空）。
    可变 值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 cap：
        做 追加 用 状态、0。
        做 追加 用 键、「」。
        做 追加 用 值、0。
        i 为 i 加 1。
    完。
    返还 一组：状态、键、值、cap。
完。

哈希表_探测 接受 表、查键：
    cap 为 表 的 第 3 项。
    可变 h 为 做 djb2_哈希 用 查键。
    可变 idx 为 h 取余于 cap。
    可变 首墓碑 为 负 1。
    可变 步 为 0。
    只要 步 小于 cap：
        s 为 表 的 第 0 项 的 第 idx 项。
        若 s 等于 0：
            若 首墓碑 大于或等于 0：
                返还 0 减 首墓碑 减 1。
            完。
            返还 0 减 idx 减 1。
        完。
        若 s 等于 2 并且 首墓碑 小于 0：
            首墓碑 为 idx。
        完。
        若 s 等于 1 并且 做 字符串相等 用 表 的 第 1 项 的 第 idx 项、查键：
            返还 idx。
        完。
        idx 为 (idx 加 1) 取余于 cap。
        步 为 步 加 1。
    完。
    若 首墓碑 大于或等于 0：
        返还 0 减 首墓碑 减 1。
    完。
    返还 0 减 idx 减 1。
完。

哈希表_扩容 接受 表：
    旧cap 为 表 的 第 3 项。
    新cap 为 旧cap 乘 2。
    可变 新状态 为 一组：（空）。
    可变 新键 为 一组：（空）。
    可变 新值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 新cap：
        做 追加 用 新状态、0。
        做 追加 用 新键、「」。
        做 追加 用 新值、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 旧cap：
        若 表 的 第 0 项 的 第 i 项 等于 1：
            k 为 表 的 第 1 项 的 第 i 项。
            v 为 表 的 第 2 项 的 第 i 项。
            h 为 做 djb2_哈希 用 k。
            可变 ni 为 h 取余于 新cap。
            只要 新状态 的 第 ni 项 不等于 0：
                ni 为 (ni 加 1) 取余于 新cap。
            完。
            新状态 的 第 ni 项 为 1。
            新键 的 第 ni 项 为 k。
            新值 的 第 ni 项 为 v。
        完。
        i 为 i 加 1。
    完。
    表 的 第 0 项 为 新状态。
    表 的 第 1 项 为 新键。
    表 的 第 2 项 为 新值。
    表 的 第 3 项 为 新cap。
    表 的 第 5 项 为 0。
完。

字典引擎_哈希表_设 接受 表、键、值：
    若 (表 的 第 4 项 加 表 的 第 5 项) 乘 4 大于 表 的 第 3 项 乘 3：
        做 哈希表_扩容 用 表。
    完。
    可变 pos 为 做 哈希表_探测 用 表、键。
    若 pos 大于或等于 0：
        表 的 第 2 项 的 第 pos 项 为 值。
    否则：
        ins 为 0 减 pos 减 1。
        若 表 的 第 0 项 的 第 ins 项 等于 2：
            表 的 第 5 项 为 表 的 第 5 项 减 1。
        完。
        表 的 第 0 项 的 第 ins 项 为 1。
        表 的 第 1 项 的 第 ins 项 为 键。
        表 的 第 2 项 的 第 ins 项 为 值。
        表 的 第 4 项 为 表 的 第 4 项 加 1。
    完。
完。

字典引擎_哈希表_取 接受 表、键：
    可变 pos 为 做 哈希表_探测 用 表、键。
    若 pos 大于或等于 0：
        返还 表 的 第 2 项 的 第 pos 项。
    完。
    返还 空。
完。

哈希表_有 接受 表、键：
    返还 做 哈希表_探测 用 表、键 大于或等于 0。
完。

哈希表_删 接受 表、键：
    可变 pos 为 做 哈希表_探测 用 表、键。
    若 pos 大于或等于 0：
        表 的 第 0 项 的 第 pos 项 为 2。
        表 的 第 1 项 的 第 pos 项 为 「」。
        表 的 第 2 项 的 第 pos 项 为 0。
        表 的 第 4 项 为 表 的 第 4 项 减 1。
        表 的 第 5 项 为 表 的 第 5 项 加 1。
        返还 真。
    完。
    返还 假。
完。

哈希表_键 接受 表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 表 的 第 3 项：
        若 表 的 第 0 项 的 第 i 项 等于 1：
            做 追加 用 结果、表 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

哈希表_值 接受 表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 表 的 第 3 项：
        若 表 的 第 0 项 的 第 i 项 等于 1：
            做 追加 用 结果、表 的 第 2 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

哈希表_计数 接受 表：
    返还 表 的 第 4 项。
完。

哈希表_容量 接受 表：
    返还 表 的 第 3 项。
完。

哈希表_负载 接受 表：
    返还 (表 的 第 4 项 加 表 的 第 5 项) 乘 100 除以 表 的 第 3 项。
完。

字典引擎_哈希表_合并 接受 a、b：
    可变 结果 为 做 哈希表_创建 用 a 的 第 3 项 加 b 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 a 的 第 3 项：
        若 a 的 第 0 项 的 第 i 项 等于 1：
            做 哈希表_设 用 结果、a 的 第 1 项 的 第 i 项、a 的 第 2 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 b 的 第 3 项：
        若 b 的 第 0 项 的 第 i 项 等于 1：
            做 哈希表_设 用 结果、b 的 第 1 项 的 第 i 项、b 的 第 2 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串_小于 接受 a、b：
    la 为 做 计数 用 a。
    lb 为 做 计数 用 b。
    可变 小 为 la。
    若 lb 小于 小：
        小 为 lb。
    完。
    可变 i 为 0。
    只要 i 小于 小：
        ca 为 做 编码 用 做 子串 用 a、i、1。
        cb 为 做 编码 用 做 子串 用 b、i、1。
        若 ca 小于 cb：
            返还 真。
        完。
        若 ca 大于 cb：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 la 小于 lb。
完。

字典_键排序 接受 键列表：
    n 为 做 计数 用 键列表。
    可变 i 为 1。
    只要 i 小于 n：
        当前 为 键列表 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 做 字符串_小于 用 当前、键列表 的 第 j 项：
            键列表 的 第 j 加 1 项 为 键列表 的 第 j 项。
            j 为 j 减 1。
        完。
        键列表 的 第 j 加 1 项 为 当前。
        i 为 i 加 1。
    完。
完。

哈希表_计数满足 接受 表、阈值：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 表 的 第 3 项：
        若 表 的 第 0 项 的 第 i 项 等于 1 并且 表 的 第 2 项 的 第 i 项 大于或等于 阈值：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

字典_哈希表_清空 接受 表：
    可变 i 为 0。
    只要 i 小于 表 的 第 3 项：
        表 的 第 0 项 的 第 i 项 为 0。
        表 的 第 1 项 的 第 i 项 为 「」。
        表 的 第 2 项 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    表 的 第 4 项 为 0。
    表 的 第 5 项 为 0。
完。

哈希表_克隆 接受 表：
    新表 为 做 哈希表_创建 用 表 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 表 的 第 3 项：
        若 表 的 第 0 项 的 第 i 项 等于 1：
            做 哈希表_设 用 新表、表 的 第 1 项 的 第 i 项、表 的 第 2 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 新表。
完。

哈希表_条目 接受 表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 表 的 第 3 项：
        若 表 的 第 0 项 的 第 i 项 等于 1：
            做 追加 用 结果、一组：表 的 第 1 项 的 第 i 项、表 的 第 2 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

哈希表_取默认 接受 表、键、默认值：
    pos 为 做 哈希表_探测 用 表、键。
    若 pos 大于或等于 0：
        返还 表 的 第 2 项 的 第 pos 项。
    完。
    返还 默认值。
完。

本章 收束。
