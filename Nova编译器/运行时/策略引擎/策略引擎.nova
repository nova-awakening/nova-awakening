《策略引擎》

引用《..·..·标准库·基础》。

引用《..·JIT·热点检测》。

引用《..·JIT·编译引擎》。

引用《..·解释器·栈式VM》。

模式_解释 为 0。

模式_JIT 为 1。

模式_AOT 为 2。

模式_调试 为 3。

JIT门控_禁用 为 0。

JIT门控_白名单 为 1。

JIT门控_标准 为 2。

JIT门控_最大化 为 3。

可变 _JIT门控等级 为 JIT门控_标准。

可变 _JIT白名单 为 一组：（空）。

可变 _JIT最大编译单元 为 4096。

JIT门控_设置profile 接受 profile名：
    若 profile名 等于 「analysis-host」：
        _JIT门控等级 为 JIT门控_最大化。
        _JIT最大编译单元 为 16384。
    完。
    若 profile名 等于 「linux-user」：
        _JIT门控等级 为 JIT门控_标准。
        _JIT最大编译单元 为 4096。
    完。
    若 profile名 等于 「kernel-qemu」：
        _JIT门控等级 为 JIT门控_白名单。
        _JIT最大编译单元 为 1024。
    完。
    若 profile名 等于 「kernel-physical」：
        _JIT门控等级 为 JIT门控_禁用。
        _JIT最大编译单元 为 0。
    完。
完。

JIT门控_添加白名单 接受 块ID：
    做 追加 用 _JIT白名单、块ID。
完。

JIT门控_允许 接受 块ID：
    若 _JIT门控等级 等于 JIT门控_禁用：
        返还 假。
    完。
    若 _JIT门控等级 等于 JIT门控_最大化：
        返还 真。
    完。
    若 _JIT门控等级 等于 JIT门控_标准：
        返还 真。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _JIT白名单：
        若 _JIT白名单 的 第 i 项 等于 块ID：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

JIT门控_报告 接受 零参：
    可变 r 为 做 字典。
    可变 等级名 为 「标准」。
    若 _JIT门控等级 等于 JIT门控_禁用：
        等级名 为 「禁用」。
    完。
    若 _JIT门控等级 等于 JIT门控_白名单：
        等级名 为 「白名单」。
    完。
    若 _JIT门控等级 等于 JIT门控_最大化：
        等级名 为 「最大化」。
    完。
    做 字典设 用 r、「JIT门控等级」、等级名。
    做 字典设 用 r、「白名单数」、做 转字符串 用 做 计数 用 _JIT白名单。
    做 字典设 用 r、「最大编译单元」、做 转字符串 用 _JIT最大编译单元。
    返还 r。
完。

策略_自动 为 0。

策略_纯解释 为 1。

策略_纯编译 为 2。

策略_混合 为 3。

可变 _策略_统计 为 一组：0、0。

策略引擎_创建 接受 热点检测器、JIT编译器：
    返还 一组：策略_自动、热点检测器、JIT编译器、做 字典、一组：0、0、0、0、0、假、空。
完。

策略引擎_绑定VM 接受 引擎、vm：
    引擎 的 第 6 项 为 vm。
完。

策略引擎_设置策略 接受 引擎、策略：
    引擎 的 第 0 项 为 策略。
完。

策略引擎_当前策略 接受 引擎：
    返还 引擎 的 第 0 项。
完。

策略引擎_决策 接受 引擎、块ID：
    若 引擎 的 第 5 项：
        返还 模式_调试。
    完。
    若 引擎 的 第 0 项 等于 策略_纯解释：
        返还 模式_解释。
    完。
    若 引擎 的 第 0 项 等于 策略_纯编译：
        返还 模式_AOT。
    完。
    若 做 JIT门控_允许 用 块ID 并且 做 热点检测器_应JIT执行 用 引擎 的 第 1 项、块ID：
        引擎 的 第 4 项 的 第 1 项 为 引擎 的 第 4 项 的 第 1 项 加 1。
        返还 模式_JIT。
    完。
    引擎 的 第 4 项 的 第 0 项 为 引擎 的 第 4 项 的 第 0 项 加 1。
    返还 模式_解释。
完。

策略引擎_执行块 接受 引擎、vm、块ID：
    模式 为 做 策略引擎_决策 用 引擎、块ID。
    引擎 的 第 4 项 的 第 4 项 为 引擎 的 第 4 项 的 第 4 项 加 1。
    若 模式 等于 模式_解释 或者 模式 等于 模式_调试：
        返还 做 VM_执行 用 vm。
    完。
    若 模式 等于 模式_JIT：
        记录 为 做 热点检测器_查询 用 引擎 的 第 1 项、块ID。
        若 记录 不等于 空 并且 做 热点_已编译 用 记录：
            引擎 的 第 4 项 的 第 2 项 为 引擎 的 第 4 项 的 第 2 项 加 1。
            jit 为 引擎 的 第 2 项。
            入口 为 记录 的 第 4 项。
            结果 为 做 执行缓冲码 用 jit 的 第 0 项、入口。
            返还 结果。
        完。
        返还 做 VM_执行 用 vm。
    完。
    若 做 AOT_已编译 等于 假：
        aot_jit 为 引擎 的 第 2 项。
        aot_vm 为 引擎 的 第 6 项。
        若 aot_jit 不等于 空 并且 aot_vm 不等于 空：
            函数表 为 做 VM_函数表 用 aot_vm。
            字节码表 为 做 VM_字节码 用 aot_vm。
            若 函数表 不等于 空 并且 字节码表 不等于 空：
                做 AOT_编译全部 用 aot_jit、函数表、字节码表。
                做 执行路径_设置 用 执行路径_AOT。
            完。
        完。
    完。
    若 做 AOT_已编译：
        aot_jit 为 引擎 的 第 2 项。
        aot_结果 为 做 AOT_查找并执行 用 aot_jit、做 转字符串 用 块ID。
        若 aot_结果 不等于 空：
            做 执行路径_记录调用 用 执行路径_AOT。
            返还 aot_结果。
        完。
        记录 为 做 热点检测器_查询 用 引擎 的 第 1 项、块ID。
        若 记录 不等于 空 并且 做 热点_已编译 用 记录：
            jit 为 引擎 的 第 2 项。
            入口 为 记录 的 第 4 项。
            做 执行路径_记录调用 用 执行路径_JIT。
            返还 做 执行缓冲码 用 jit 的 第 0 项、入口。
        完。
    完。
    做 执行路径_记录调用 用 执行路径_解释。
    返还 做 VM_执行 用 vm。
完。

策略引擎_热点回调 接受 引擎：
    返还 做 法 用 参数。
    块：
        块ID 为 参数 的 第 0 项。
        jit 为 引擎 的 第 2 项。
        vm 为 引擎 的 第 6 项。
        引擎 的 第 4 项 的 第 3 项 为 引擎 的 第 4 项 的 第 3 项 加 1。
        块信息 为 做 VM_获取块 用 vm、块ID。
        若 块信息 不等于 空：
            字节码 为 块信息 的 第 0 项。
            起始 为 块信息 的 第 1 项。
            长度 为 块信息 的 第 2 项。
            已有 为 做 字典取 用 jit 的 第 2 项、做 转字符串 用 块ID。
            若 已有 等于 空：
                结果 为 做 JIT编译器_编译块 用 jit、字节码、起始、长度。
                若 结果 不等于 空：
                    做 字典设 用 jit 的 第 2 项、做 转字符串 用 块ID、结果。
                    若 做 计数 用 结果 大于或等于 3 并且 结果 的 第 2 项 等于 0：
                        做 热点检测器_标记编译完成 用 引擎 的 第 1 项、块ID、结果 的 第 0 项。
                    否则：
                    完。
                完。
            完。
        完。
    完。
完。

策略引擎_进入调试 接受 引擎：
    引擎 的 第 5 项 为 真。
    做 打印 用 「策略引擎: 切换为调试模式 (纯解释)」。
完。

策略引擎_退出调试 接受 引擎：
    引擎 的 第 5 项 为 假。
    做 打印 用 「策略引擎: 恢复自动模式 (解释+JIT)」。
完。

策略引擎_生产模式 接受 引擎：
    引擎 的 第 0 项 为 策略_纯编译。
    做 打印 用 「策略引擎: 生产模式 (预编译所有代码)」。
完。

策略引擎_GC机器码 接受 引擎：
    回收数 为 做 热点检测器_冷却扫描 用 引擎 的 第 1 项、0。
    若 回收数 大于 0：
        做 打印 用 做 字符串拼接 用 「策略引擎: 回收」、做 字符串拼接 用 做 转字符串 用 回收数、「个冷代码块的机器码」。
    完。
    返还 回收数。
完。

可变 _采样_块ID 为 一组：（空）。

可变 _采样_解释耗时 为 一组：（空）。

可变 _采样_JIT耗时 为 一组：（空）。

策略_记录解释耗时 接受 块ID、耗时：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _采样_块ID：
        若 _采样_块ID 的 第 i 项 等于 块ID：
            _采样_解释耗时 的 第 i 项 为 _采样_解释耗时 的 第 i 项 加 耗时。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 _采样_块ID、块ID。
    做 追加 用 _采样_解释耗时、耗时。
    做 追加 用 _采样_JIT耗时、0。
完。

策略_记录JIT耗时 接受 块ID、耗时：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _采样_块ID：
        若 _采样_块ID 的 第 i 项 等于 块ID：
            _采样_JIT耗时 的 第 i 项 为 _采样_JIT耗时 的 第 i 项 加 耗时。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 _采样_块ID、块ID。
    做 追加 用 _采样_解释耗时、0。
    做 追加 用 _采样_JIT耗时、耗时。
完。

策略_应编译 接受 块ID、编译开销：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _采样_块ID：
        若 _采样_块ID 的 第 i 项 等于 块ID：
            解释总 为 _采样_解释耗时 的 第 i 项。
            JIT总 为 _采样_JIT耗时 的 第 i 项。
            若 JIT总 大于 0：
                返还 假。
            完。
            返还 解释总 大于 编译开销。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

可变 _编译队列_块ID 为 一组：（空）。

可变 _编译队列_优先级 为 一组：（空）。

编译队列_入队 接受 块ID、优先级：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _编译队列_块ID：
        若 _编译队列_块ID 的 第 i 项 等于 块ID：
            若 优先级 大于 _编译队列_优先级 的 第 i 项：
                _编译队列_优先级 的 第 i 项 为 优先级。
            完。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 _编译队列_块ID、块ID。
    做 追加 用 _编译队列_优先级、优先级。
完。

编译队列_出队 接受 零参：
    若 做 计数 用 _编译队列_块ID 等于 0：
        返还 负 1。
    完。
    可变 最高idx 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 _编译队列_优先级：
        若 _编译队列_优先级 的 第 i 项 大于 _编译队列_优先级 的 第 最高idx 项：
            最高idx 为 i。
        完。
        i 为 i 加 1。
    完。
    块ID 为 _编译队列_块ID 的 第 最高idx 项。
    做 删除 用 _编译队列_块ID、最高idx。
    做 删除 用 _编译队列_优先级、最高idx。
    返还 块ID。
完。

编译队列_大小 接受 零参：
    返还 做 计数 用 _编译队列_块ID。
完。

可变 _策略历史_时间 为 一组：（空）。

可变 _策略历史_决策 为 一组：（空）。

策略_记录决策 接受 时间、决策：
    做 追加 用 _策略历史_时间、时间。
    做 追加 用 _策略历史_决策、决策。
完。

策略_最近决策 接受 数量：
    可变 结果 为 一组：（空）。
    总 为 做 计数 用 _策略历史_决策。
    可变 起始 为 总 减 数量。
    若 起始 小于 0：
        起始 为 0。
    完。
    可变 i 为 起始。
    只要 i 小于 总：
        做 追加 用 结果、一组：_策略历史_时间 的 第 i 项、_策略历史_决策 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

策略_JIT比率 接受 零参：
    可变 JIT数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _策略历史_决策：
        若 _策略历史_决策 的 第 i 项 等于 「JIT」：
            JIT数 为 JIT数 加 1。
        完。
        i 为 i 加 1。
    完。
    总 为 做 计数 用 _策略历史_决策。
    若 总 等于 0：
        返还 0。
    完。
    返还 JIT数 乘 100 除以 总。
完。

策略_记录调度 接受 零参：
    _策略_统计 的 第 0 项 为 _策略_统计 的 第 0 项 加 1。
完。

策略_记录切换 接受 零参：
    _策略_统计 的 第 1 项 为 _策略_统计 的 第 1 项 加 1。
完。

本章 收束。
