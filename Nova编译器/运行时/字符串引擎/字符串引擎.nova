《字符串引擎》

引用《..·..·标准库·基础》。

引用《..·..·标准库·算法·字符串算法》。

引用《..·字典引擎·字典引擎》。

字符串引_utf8_编码 接受 码点：
    若 码点 小于 128：
        返还 一组：码点。
    完。
    若 码点 小于 2048：
        返还 一组：192 加 码点 除以 64、128 加 码点 取余于 64。
    完。
    若 码点 小于 65536：
        返还 一组：224 加 码点 除以 4096、128 加 码点 除以 64 取余于 64、128 加 码点 取余于 64。
    完。
    返还 一组：240 加 码点 除以 262144、128 加 码点 除以 4096 取余于 64、128 加 码点 除以 64 取余于 64、128 加 码点 取余于 64。
完。

utf8_字节数 接受 首字节：
    若 首字节 小于 128：
        返还 1。
    完。
    若 首字节 小于 224：
        返还 2。
    完。
    若 首字节 小于 240：
        返还 3。
    完。
    返还 4。
完。

字符串_码点列表 接受 s：
    可变 结果 为 一组：（空）。
    n 为 做 计数 用 s。
    可变 i 为 0。
    只要 i 小于 n：
        b 为 做 编码 用 做 子串 用 s、i、1。
        可变 字符长 为 1。
        若 b 大于或等于 0xF0：
            字符长 为 4。
        否则：
            若 b 大于或等于 0xE0：
                字符长 为 3。
            否则：
                若 b 大于或等于 0xC0：
                    字符长 为 2。
                完。
            完。
        完。
        可变 cp 为 0。
        若 字符长 等于 1：
            cp 为 b。
        否则：
            若 字符长 等于 2：
                cp 为 (b 减 0xC0) 乘 64 加 (做 编码 用 做 子串 用 s、i 加 1、1 减 0x80)。
            否则：
                若 字符长 等于 3：
                    cp 为 (b 减 0xE0) 乘 4096 加 (做 编码 用 做 子串 用 s、i 加 1、1 减 0x80) 乘 64 加 (做 编码 用 做 子串 用 s、i 加 2、1 减 0x80)。
                否则：
                    cp 为 (b 减 0xF0) 乘 262144 加 (做 编码 用 做 子串 用 s、i 加 1、1 减 0x80) 乘 4096 加 (做 编码 用 做 子串 用 s、i 加 2、1 减 0x80) 乘 64 加 (做 编码 用 做 子串 用 s、i 加 3、1 减 0x80)。
                完。
            完。
        完。
        做 追加 用 结果、cp。
        i 为 i 加 字符长。
    完。
    返还 结果。
完。

码点列表_字符串 接受 列表：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        结果 为 做 字符串拼接 用 结果、做 字符 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串引_kmp_失败表 接受 模式：
    m 为 做 计数 用 模式。
    可变 表 为 一组：0。
    可变 i 为 1。
    只要 i 小于 m：
        做 追加 用 表、0。
        i 为 i 加 1。
    完。
    可变 k 为 0。
    i 为 1。
    只要 i 小于 m：
        只要 k 大于 0 并且 做 编码 用 做 子串 用 模式、k、1 不等于 做 编码 用 做 子串 用 模式、i、1：
            k 为 表 的 第 k 减 1 项。
        完。
        若 做 编码 用 做 子串 用 模式、k、1 等于 做 编码 用 做 子串 用 模式、i、1：
            k 为 k 加 1。
        完。
        表 的 第 i 项 为 k。
        i 为 i 加 1。
    完。
    返还 表。
完。

字符串引_kmp_搜索 接受 文本、模式：
    n 为 做 计数 用 文本。
    m 为 做 计数 用 模式。
    若 m 等于 0：
        返还 0。
    完。
    若 m 大于 n：
        返还 负 1。
    完。
    失败 为 做 kmp_失败表 用 模式。
    可变 j 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        只要 j 大于 0 并且 做 编码 用 做 子串 用 模式、j、1 不等于 做 编码 用 做 子串 用 文本、i、1：
            j 为 失败 的 第 j 减 1 项。
        完。
        若 做 编码 用 做 子串 用 模式、j、1 等于 做 编码 用 做 子串 用 文本、i、1：
            j 为 j 加 1。
        完。
        若 j 等于 m：
            返还 i 减 m 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

kmp_全部搜索 接受 文本、模式：
    n 为 做 计数 用 文本。
    m 为 做 计数 用 模式。
    可变 结果 为 一组：（空）。
    若 m 等于 0 或者 m 大于 n：
        返还 结果。
    完。
    失败 为 做 kmp_失败表 用 模式。
    可变 j 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        只要 j 大于 0 并且 做 编码 用 做 子串 用 模式、j、1 不等于 做 编码 用 做 子串 用 文本、i、1：
            j 为 失败 的 第 j 减 1 项。
        完。
        若 做 编码 用 做 子串 用 模式、j、1 等于 做 编码 用 做 子串 用 文本、i、1：
            j 为 j 加 1。
        完。
        若 j 等于 m：
            做 追加 用 结果、i 减 m 加 1。
            j 为 失败 的 第 j 减 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串引_字符串_重复 接受 s、n：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        结果 为 做 字符串拼接 用 结果、s。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串引_字符串_反转 接受 s：
    n 为 做 计数 用 s。
    可变 结果 为 「」。
    可变 i 为 n 减 1。
    只要 i 大于或等于 0：
        结果 为 做 字符串拼接 用 结果、做 子串 用 s、i、1。
        i 为 i 减 1。
    完。
    返还 结果。
完。

字符串_开头是 接受 s、前缀：
    pl 为 做 计数 用 前缀。
    若 pl 大于 做 计数 用 s：
        返还 假。
    完。
    返还 做 字符串相等 用 做 子串 用 s、0、pl、前缀。
完。

字符串_结尾是 接受 s、后缀：
    sl 为 做 计数 用 s。
    后l 为 做 计数 用 后缀。
    若 后l 大于 sl：
        返还 假。
    完。
    返还 做 字符串相等 用 做 子串 用 s、sl 减 后l、后l、后缀。
完。

字符串_大写 接受 s：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 s：
        c 为 做 编码 用 做 子串 用 s、i、1。
        若 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z：
            结果 为 做 字符串拼接 用 结果、做 字符 用 c 减 32。
        否则：
            结果 为 做 字符串拼接 用 结果、做 子串 用 s、i、1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串_小写 接受 s：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 s：
        c 为 做 编码 用 做 子串 用 s、i、1。
        若 c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z：
            结果 为 做 字符串拼接 用 结果、做 字符 用 c 加 32。
        否则：
            结果 为 做 字符串拼接 用 结果、做 子串 用 s、i、1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串_修剪 接受 s：
    n 为 做 计数 用 s。
    可变 左 为 0。
    只要 左 小于 n 并且 做 编码 用 做 子串 用 s、左、1 小于或等于 32：
        左 为 左 加 1。
    完。
    可变 右 为 n 减 1。
    只要 右 大于或等于 左 并且 做 编码 用 做 子串 用 s、右、1 小于或等于 32：
        右 为 右 减 1。
    完。
    若 左 大于 右：
        返还 「」。
    完。
    返还 做 子串 用 s、左、右 减 左 加 1。
完。

字符串引_字符串_左填充 接受 s、目标长、填充字符：
    可变 结果 为 s。
    只要 做 计数 用 结果 小于 目标长：
        结果 为 做 字符串拼接 用 填充字符、结果。
    完。
    返还 结果。
完。

字符串引_字符串_右填充 接受 s、目标长、填充字符：
    可变 结果 为 s。
    只要 做 计数 用 结果 小于 目标长：
        结果 为 做 字符串拼接 用 结果、填充字符。
    完。
    返还 结果。
完。

字符串引_字符串_分割 接受 s、分隔符：
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    sl 为 做 计数 用 s。
    dl 为 做 计数 用 分隔符。
    若 dl 等于 0：
        做 追加 用 结果、s。
        返还 结果。
    完。
    可变 i 为 0。
    只要 i 小于 sl：
        若 i 加 dl 小于或等于 sl 并且 做 子串 用 s、i、dl 等于 分隔符：
            做 追加 用 结果、当前。
            当前 为 「」。
            i 为 i 加 dl。
        否则：
            当前 为 做 字符串拼接 用 当前、做 子串 用 s、i、1。
            i 为 i 加 1。
        完。
    完。
    做 追加 用 结果、当前。
    返还 结果。
完。

字符串_合并 接受 列表、分隔符：
    n 为 做 计数 用 列表。
    若 n 等于 0：
        返还 「」。
    完。
    可变 结果 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        结果 为 做 字符串拼接 用 做 字符串拼接 用 结果、分隔符、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串_替换全部 接受 s、旧、新：
    位置 为 做 kmp_全部搜索 用 s、旧。
    若 做 计数 用 位置 等于 0：
        返还 s。
    完。
    旧长 为 做 计数 用 旧。
    可变 结果 为 「」。
    可变 上次 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 位置：
        若 位置 的 第 i 项 大于 上次：
            结果 为 做 字符串拼接 用 结果、做 子串 用 s、上次、位置 的 第 i 项 减 上次。
        完。
        结果 为 做 字符串拼接 用 结果、新。
        上次 为 位置 的 第 i 项 加 旧长。
        i 为 i 加 1。
    完。
    若 上次 小于 做 计数 用 s：
        结果 为 做 字符串拼接 用 结果、做 子串 用 s、上次、做 计数 用 s 减 上次。
    完。
    返还 结果。
完。

字符串引_字符串_是回文 接受 s：
    n 为 做 计数 用 s。
    可变 i 为 0。
    只要 i 小于 n 除以 2：
        若 做 编码 用 做 子串 用 s、i、1 不等于 做 编码 用 做 子串 用 s、n 减 1 减 i、1：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

字符串引擎_包含 接受 s、子串目标：
    返还 做 kmp_搜索 用 s、子串目标 大于或等于 0。
完。

字符串_索引 接受 s、子串目标：
    返还 做 kmp_搜索 用 s、子串目标。
完。

字符串_替换首个 接受 s、旧、新：
    pos 为 做 kmp_搜索 用 s、旧。
    若 pos 小于 0：
        返还 s。
    完。
    旧长 为 做 计数 用 旧。
    可变 结果 为 「」。
    若 pos 大于 0：
        结果 为 做 子串 用 s、0、pos。
    完。
    结果 为 做 字符串拼接 用 结果、新。
    若 pos 加 旧长 小于 做 计数 用 s：
        结果 为 做 字符串拼接 用 结果、做 子串 用 s、pos 加 旧长、做 计数 用 s 减 pos 减 旧长。
    完。
    返还 结果。
完。

字符串_转整数 接受 s：
    可变 结果 为 0。
    可变 负 为 假。
    可变 i 为 0。
    若 做 计数 用 s 大于 0 并且 做 编码 用 做 子串 用 s、0、1 等于 45：
        负 为 真。
        i 为 1。
    完。
    只要 i 小于 做 计数 用 s：
        c 为 做 编码 用 做 子串 用 s、i、1 减 48。
        若 c 大于或等于 0 并且 c 小于或等于 字符_制表：
            结果 为 结果 乘 10 加 c。
        否则：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    若 负：
        返还 0 减 结果。
    完。
    返还 结果。
完。

字符串_全数字 接受 s：
    若 做 计数 用 s 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 s：
        c 为 做 编码 用 做 子串 用 s、i、1。
        若 c 小于 48 或者 c 大于 57：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

字符串_全字母 接受 s：
    若 做 计数 用 s 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 s：
        c 为 做 编码 用 做 子串 用 s、i、1。
        若 非 (c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z 或者 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z)：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

本章 收束。
