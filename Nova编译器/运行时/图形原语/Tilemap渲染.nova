《Tilemap渲染》

引用《..·..·标准库·基础》。

引用《.·精灵批渲染》。

图块集_创建 接受 纹理、图块宽、图块高：
    列数 为 做 纹理_宽 用 纹理 除以 图块宽。
    行数 为 做 纹理_高 用 纹理 除以 图块高。
    返还 一组：纹理、图块宽、图块高、列数、行数。
完。

图块集_图块宽 接受 ts：
    返还 ts 的 第 1 项。
完。

图块集_图块高 接受 ts：
    返还 ts 的 第 2 项。
完。

图块集_列数 接受 ts：
    返还 ts 的 第 3 项。
完。

图块集_图块矩形 接受 ts、图块ID：
    若 图块ID 小于 0：
        返还 一组：0、0、0、0。
    完。
    列 为 图块ID 取余于 ts 的 第 3 项。
    行 为 图块ID 除以 ts 的 第 3 项。
    返还 一组：列 乘 ts 的 第 1 项、行 乘 ts 的 第 2 项、ts 的 第 1 项、ts 的 第 2 项。
完。

图层_创建 接受 名称、宽、高：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 宽 乘 高：
        做 追加 用 数据、负 1。
        i 为 i 加 1。
    完。
    返还 一组：名称、宽、高、数据、真、1000。
完。

图层_名称 接受 l：
    返还 l 的 第 0 项。
完。

图层_宽 接受 l：
    返还 l 的 第 1 项。
完。

图层_高 接受 l：
    返还 l 的 第 2 项。
完。

图层_可见 接受 l：
    返还 l 的 第 4 项。
完。

图层_设可见 接受 l、v：
    l 的 第 4 项 为 v。
完。

图层_设图块 接受 l、x、y、图块ID：
    若 x 大于或等于 0 并且 x 小于 l 的 第 1 项 并且 y 大于或等于 0 并且 y 小于 l 的 第 2 项：
        l 的 第 3 项 的 第 y 乘 l 的 第 1 项 加 x 项 为 图块ID。
    完。
完。

图层_取图块 接受 l、x、y：
    若 x 小于 0 或者 x 大于或等于 l 的 第 1 项 或者 y 小于 0 或者 y 大于或等于 l 的 第 2 项：
        返还 负 1。
    完。
    返还 l 的 第 3 项 的 第 y 乘 l 的 第 1 项 加 x 项。
完。

瓦片地图_创建 接受 图块集：
    返还 一组：图块集、一组：（空）。
完。

瓦片地图_图块集 接受 m：
    返还 m 的 第 0 项。
完。

瓦片地图_图层 接受 m：
    返还 m 的 第 1 项。
完。

瓦片地图_相机x 接受 m：
    返还 m 的 第 2 项。
完。

瓦片地图_相机y 接受 m：
    返还 m 的 第 3 项。
完。

瓦片地图_添加图层 接受 m、图层：
    做 追加 用 m 的 第 1 项、图层。
完。

瓦片地图_设相机 接受 m、x、y：
    m 的 第 2 项 为 x。
    m 的 第 3 项 为 y。
完。

瓦片地图_移动相机 接受 m、dx、dy：
    m 的 第 2 项 为 m 的 第 2 项 加 dx。
    m 的 第 3 项 为 m 的 第 3 项 加 dy。
完。

瓦片地图_渲染 接受 m、帧缓冲、屏宽、屏高：
    ts 为 m 的 第 0 项。
    图块w 为 做 图块集_图块宽 用 ts。
    图块h 为 做 图块集_图块高 用 ts。
    纹理 为 ts 的 第 0 项。
    camx 为 m 的 第 2 项。
    camy 为 m 的 第 3 项。
    起始列 为 camx 除以 图块w。
    起始行 为 camy 除以 图块h。
    可见列 为 屏宽 除以 图块w 加 2。
    可见行 为 屏高 除以 图块h 加 2。
    偏移x 为 camx 取余于 图块w。
    偏移y 为 camy 取余于 图块h。
    可变 li 为 0。
    只要 li 小于 做 计数 用 m 的 第 1 项：
        图层 为 m 的 第 1 项 的 第 li 项。
        若 做 图层_可见 用 图层：
            可变 行 为 0。
            只要 行 小于 可见行：
                可变 列 为 0。
                只要 列 小于 可见列：
                    图块ID 为 做 图层_取图块 用 图层、起始列 加 列、起始行 加 行。
                    若 图块ID 大于或等于 0：
                        源 为 做 图块集_图块矩形 用 ts、图块ID。
                        目标x 为 列 乘 图块w 减 偏移x。
                        目标y 为 行 乘 图块h 减 偏移y。
                        可变 py 为 0。
                        只要 py 小于 图块h：
                            可变 px 为 0。
                            只要 px 小于 图块w：
                                dx 为 目标x 加 px。
                                dy 为 目标y 加 py。
                                若 dx 大于或等于 0 并且 dx 小于 屏宽 并且 dy 大于或等于 0 并且 dy 小于 屏高：
                                    颜色 为 做 纹理_取像素 用 纹理、源 的 第 0 项 加 px、源 的 第 1 项 加 py。
                                    若 颜色 不等于 纹理 的 第 3 项：
                                        帧缓冲 的 第 dy 乘 屏宽 加 dx 项 为 颜色。
                                    完。
                                完。
                                px 为 px 加 1。
                            完。
                            py 为 py 加 1。
                        完。
                    完。
                    列 为 列 加 1。
                完。
                行 为 行 加 1。
            完。
        完。
        li 为 li 加 1。
    完。
完。

瓦片地图_图块是实心 接受 m、图层索引、世界x、世界y：
    ts 为 m 的 第 0 项。
    列 为 世界x 除以 做 图块集_图块宽 用 ts。
    行 为 世界y 除以 做 图块集_图块高 用 ts。
    若 图层索引 小于 做 计数 用 m 的 第 1 项：
        id 为 做 图层_取图块 用 m 的 第 1 项 的 第 图层索引 项、列、行。
        返还 id 大于或等于 0。
    完。
    返还 假。
完。

瓦片地图_打印报告 接受 m：
    做 打印 用 「═══ 瓦片地图报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  图块大小: 」、做 字符串拼接 用 做 转字符串 用 做 图块集_图块宽 用 m 的 第 0 项、做 字符串拼接 用 「×」、做 转字符串 用 做 图块集_图块高 用 m 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  图层数: 」、做 转字符串 用 做 计数 用 m 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  相机: (」、做 字符串拼接 用 做 转字符串 用 m 的 第 2 项、做 字符串拼接 用 「,」、做 字符串拼接 用 做 转字符串 用 m 的 第 3 项、「)」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 m 的 第 1 项：
        l 为 m 的 第 1 项 的 第 i 项。
        做 打印文本 用 做 字符串拼接 用 「  [」、做 字符串拼接 用 做 转字符串 用 i、做 字符串拼接 用 「] 」、做 字符串拼接 用 做 图层_名称 用 l、做 字符串拼接 用 「 」、做 字符串拼接 用 做 转字符串 用 做 图层_宽 用 l、做 字符串拼接 用 「×」、做 转字符串 用 做 图层_高 用 l。
        i 为 i 加 1。
    完。
完。

本章 收束。
