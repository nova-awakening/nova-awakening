《粒子系统》

引用《..·..·标准库·基础》。

粒子_创建 接受 x、y、vx、vy、生命、颜色、大小：
    返还 一组：x、y、vx、vy、生命、生命、颜色、大小、1000。
完。

粒子_x 接受 p：
    返还 p 的 第 0 项。
完。

粒子_y 接受 p：
    返还 p 的 第 1 项。
完。

粒子_存活 接受 p：
    返还 p 的 第 4 项 大于 0。
完。

粒子_颜色 接受 p：
    返还 p 的 第 6 项。
完。

粒子_大小 接受 p：
    返还 p 的 第 7 项。
完。

粒子_透明度 接受 p：
    返还 p 的 第 8 项。
完。

粒子_更新 接受 p、重力y：
    若 p 的 第 4 项 小于或等于 0：
        返还 空。
    完。
    p 的 第 0 项 为 p 的 第 0 项 加 p 的 第 2 项。
    p 的 第 1 项 为 p 的 第 1 项 加 p 的 第 3 项。
    p 的 第 3 项 为 p 的 第 3 项 加 重力y。
    p 的 第 4 项 为 p 的 第 4 项 减 1。
    若 p 的 第 5 项 大于 0：
        p 的 第 8 项 为 p 的 第 4 项 乘 1000 除以 p 的 第 5 项。
    完。
完。

发射器_创建 接受 x、y：
    返还 一组：x、y、一组：100、300、一组：0、360、一组：30、60、0xFFFFFF、一组：2、5、10、5。
完。

发射器_设位置 接受 e、x、y：
    e 的 第 0 项 为 x。
    e 的 第 1 项 为 y。
完。

发射器_设速度 接受 e、最小、最大：
    e 的 第 2 项 为 一组：最小、最大。
完。

发射器_设角度 接受 e、最小、最大：
    e 的 第 3 项 为 一组：最小、最大。
完。

发射器_设生命 接受 e、最小、最大：
    e 的 第 4 项 为 一组：最小、最大。
完。

发射器_设颜色 接受 e、c：
    e 的 第 5 项 为 c。
完。

发射器_设大小 接受 e、最小、最大：
    e 的 第 6 项 为 一组：最小、最大。
完。

发射器_设重力 接受 e、g：
    e 的 第 7 项 为 g。
完。

发射器_设发射率 接受 e、r：
    e 的 第 8 项 为 r。
完。

可变 _粒子_随机种子 为 12345。

_粒子_随机 接受 零参：
    _粒子_随机种子 为 _粒子_随机种子 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    返还 _粒子_随机种子。
完。

_粒子_随机范围 接受 最小、最大：
    若 最大 小于或等于 最小：
        返还 最小。
    完。
    返还 最小 加 做 _粒子_随机 取余于 (最大 减 最小 加 1)。
完。

粒子系统_创建 接受 发射器、最大数：
    返还 一组：发射器、一组：（空）、最大数。
完。

粒子系统_活跃数 接受 ps：
    返还 ps 的 第 3 项。
完。

粒子系统_总发射 接受 ps：
    返还 ps 的 第 4 项。
完。

粒子系统_发射 接受 ps：
    e 为 ps 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 e 的 第 8 项 并且 ps 的 第 3 项 小于 ps 的 第 2 项：
        速度 为 做 _粒子_随机范围 用 e 的 第 2 项 的 第 0 项、e 的 第 2 项 的 第 1 项。
        角度 为 做 _粒子_随机范围 用 e 的 第 3 项 的 第 0 项、e 的 第 3 项 的 第 1 项。
        生命 为 做 _粒子_随机范围 用 e 的 第 4 项 的 第 0 项、e 的 第 4 项 的 第 1 项。
        大小 为 做 _粒子_随机范围 用 e 的 第 6 项 的 第 0 项、e 的 第 6 项 的 第 1 项。
        可变 vx 为 速度 乘 (50 减 角度 取余于 100) 除以 100。
        可变 vy 为 0 减 速度 乘 (50 减 (角度 加 25) 取余于 100) 除以 100。
        p 为 做 粒子_创建 用 e 的 第 0 项、e 的 第 1 项、vx、vy、生命、e 的 第 5 项、大小。
        做 追加 用 ps 的 第 1 项、p。
        ps 的 第 3 项 为 ps 的 第 3 项 加 1。
        ps 的 第 4 项 为 ps 的 第 4 项 加 1。
        i 为 i 加 1。
    完。
完。

粒子系统_更新 接受 ps：
    可变 新列表 为 一组：（空）。
    重力 为 ps 的 第 0 项 的 第 7 项。
    可变 活跃 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 ps 的 第 1 项：
        p 为 ps 的 第 1 项 的 第 i 项。
        做 粒子_更新 用 p、重力。
        若 做 粒子_存活 用 p：
            做 追加 用 新列表、p。
            活跃 为 活跃 加 1。
        完。
        i 为 i 加 1。
    完。
    ps 的 第 1 项 为 新列表。
    ps 的 第 3 项 为 活跃。
完。

粒子系统_渲染 接受 ps、帧缓冲、屏宽、屏高：
    可变 i 为 0。
    只要 i 小于 做 计数 用 ps 的 第 1 项：
        p 为 ps 的 第 1 项 的 第 i 项。
        若 做 粒子_存活 用 p：
            px 为 做 粒子_x 用 p 除以 100。
            py 为 做 粒子_y 用 p 除以 100。
            大小 为 做 粒子_大小 用 p。
            颜色 为 做 粒子_颜色 用 p。
            可变 dy 为 0。
            只要 dy 小于 大小：
                可变 dx 为 0。
                只要 dx 小于 大小：
                    sx 为 px 加 dx。
                    sy 为 py 加 dy。
                    若 sx 大于或等于 0 并且 sx 小于 屏宽 并且 sy 大于或等于 0 并且 sy 小于 屏高：
                        帧缓冲 的 第 sy 乘 屏宽 加 sx 项 为 颜色。
                    完。
                    dx 为 dx 加 1。
                完。
                dy 为 dy 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
完。

粒子系统_打印报告 接受 ps：
    做 打印 用 「═══ 粒子系统报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  活跃粒子: 」、做 字符串拼接 用 做 转字符串 用 ps 的 第 3 项、做 字符串拼接 用 「/」、做 转字符串 用 ps 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  总发射: 」、做 转字符串 用 ps 的 第 4 项。
    做 打印文本 用 做 字符串拼接 用 「  发射率: 」、做 字符串拼接 用 做 转字符串 用 ps 的 第 0 项 的 第 8 项、「/帧」。
    做 打印文本 用 做 字符串拼接 用 「  重力: 」、做 转字符串 用 ps 的 第 0 项 的 第 7 项。
完。

本章 收束。
