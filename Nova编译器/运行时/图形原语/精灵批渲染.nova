《精灵批渲染》

引用《..·..·标准库·基础》。

纹理_创建 接受 宽、高：
    可变 像素 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 宽 乘 高：
        做 追加 用 像素、0。
        i 为 i 加 1。
    完。
    返还 一组：宽、高、像素、负 1。
完。

纹理_宽 接受 t：
    返还 t 的 第 0 项。
完。

纹理_高 接受 t：
    返还 t 的 第 1 项。
完。

纹理_设透明色 接受 t、c：
    t 的 第 3 项 为 c。
完。

纹理_取像素 接受 t、x、y：
    若 x 小于 0 或者 x 大于或等于 t 的 第 0 项 或者 y 小于 0 或者 y 大于或等于 t 的 第 1 项：
        返还 0。
    完。
    返还 t 的 第 2 项 的 第 y 乘 t 的 第 0 项 加 x 项。
完。

纹理_设像素 接受 t、x、y、颜色：
    若 x 大于或等于 0 并且 x 小于 t 的 第 0 项 并且 y 大于或等于 0 并且 y 小于 t 的 第 1 项：
        t 的 第 2 项 的 第 y 乘 t 的 第 0 项 加 x 项 为 颜色。
    完。
完。

精灵_创建 接受 纹理、x、y：
    返还 一组：纹理、x、y、做 纹理_宽 用 纹理、做 纹理_高 用 纹理、0、0、做 纹理_宽 用 纹理、做 纹理_高 用 纹理、0、真、假、假。
完。

精灵_x 接受 s：
    返还 s 的 第 1 项。
完。

精灵_y 接受 s：
    返还 s 的 第 2 项。
完。

精灵_图层 接受 s：
    返还 s 的 第 9 项。
完。

精灵_可见 接受 s：
    返还 s 的 第 10 项。
完。

精灵_设位置 接受 s、x、y：
    s 的 第 1 项 为 x。
    s 的 第 2 项 为 y。
完。

精灵_设源矩形 接受 s、sx、sy、sw、sh：
    s 的 第 5 项 为 sx。
    s 的 第 6 项 为 sy。
    s 的 第 7 项 为 sw。
    s 的 第 8 项 为 sh。
完。

精灵_设图层 接受 s、z：
    s 的 第 9 项 为 z。
完。

精灵_设可见 接受 s、v：
    s 的 第 10 项 为 v。
完。

精灵_设翻转 接受 s、fx、fy：
    s 的 第 11 项 为 fx。
    s 的 第 12 项 为 fy。
完。

视口_创建 接受 x、y、宽、高：
    返还 一组：x、y、宽、高。
完。

视口_包含 接受 v、x、y：
    返还 x 大于或等于 v 的 第 0 项 并且 x 小于 v 的 第 0 项 加 v 的 第 2 项 并且 y 大于或等于 v 的 第 1 项 并且 y 小于 v 的 第 1 项 加 v 的 第 3 项。
完。

批渲染器_创建 接受 宽、高：
    可变 缓冲 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 宽 乘 高：
        做 追加 用 缓冲、0。
        i 为 i 加 1。
    完。
    返还 一组：一组：（空）、缓冲、宽、高、做 视口_创建 用 0、0、宽、高、0。
完。

批渲染器_添加精灵 接受 r、精灵：
    做 追加 用 r 的 第 0 项、精灵。
完。

批渲染器_精灵数 接受 r：
    返还 做 计数 用 r 的 第 0 项。
完。

批渲染器_绘制调用数 接受 r：
    返还 r 的 第 5 项。
完。

批渲染器_清屏 接受 r、颜色：
    可变 i 为 0。
    只要 i 小于 r 的 第 2 项 乘 r 的 第 3 项：
        r 的 第 1 项 的 第 i 项 为 颜色。
        i 为 i 加 1。
    完。
完。

_批渲染器_排序图层 接受 列表：
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        当前 为 列表 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 做 精灵_图层 用 列表 的 第 j 项 大于 做 精灵_图层 用 当前：
            列表 的 第 j 加 1 项 为 列表 的 第 j 项。
            j 为 j 减 1。
        完。
        列表 的 第 j 加 1 项 为 当前。
        i 为 i 加 1。
    完。
完。

批渲染器_渲染 接受 r：
    r 的 第 5 项 为 0。
    做 _批渲染器_排序图层 用 r 的 第 0 项。
    缓宽 为 r 的 第 2 项。
    缓高 为 r 的 第 3 项。
    可变 si 为 0。
    只要 si 小于 做 计数 用 r 的 第 0 项：
        s 为 r 的 第 0 项 的 第 si 项。
        若 做 精灵_可见 用 s：
            纹理 为 s 的 第 0 项。
            dx 为 s 的 第 1 项。
            dy 为 s 的 第 2 项。
            sx 为 s 的 第 5 项。
            sy 为 s 的 第 6 项。
            sw 为 s 的 第 7 项。
            sh 为 s 的 第 8 项。
            翻转x 为 s 的 第 11 项。
            翻转y 为 s 的 第 12 项。
            透明 为 纹理 的 第 3 项。
            可变 py 为 0。
            只要 py 小于 sh：
                可变 px 为 0。
                只要 px 小于 sw：
                    可变 采样x 为 sx 加 px。
                    可变 采样y 为 sy 加 py。
                    若 翻转x：
                        采样x 为 sx 加 sw 减 1 减 px。
                    完。
                    若 翻转y：
                        采样y 为 sy 加 sh 减 1 减 py。
                    完。
                    颜色 为 做 纹理_取像素 用 纹理、采样x、采样y。
                    若 颜色 不等于 透明：
                        目标x 为 dx 加 px。
                        目标y 为 dy 加 py。
                        若 目标x 大于或等于 0 并且 目标x 小于 缓宽 并且 目标y 大于或等于 0 并且 目标y 小于 缓高：
                            r 的 第 1 项 的 第 目标y 乘 缓宽 加 目标x 项 为 颜色。
                        完。
                    完。
                    px 为 px 加 1。
                完。
                py 为 py 加 1。
            完。
            r 的 第 5 项 为 r 的 第 5 项 加 1。
        完。
        si 为 si 加 1。
    完。
完。

批渲染器_取像素 接受 r、x、y：
    若 x 小于 0 或者 x 大于或等于 r 的 第 2 项 或者 y 小于 0 或者 y 大于或等于 r 的 第 3 项：
        返还 0。
    完。
    返还 r 的 第 1 项 的 第 y 乘 r 的 第 2 项 加 x 项。
完。

批渲染器_打印报告 接受 r：
    做 打印 用 「═══ 精灵批渲染报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  精灵数: 」、做 转字符串 用 做 计数 用 r 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  缓冲: 」、做 字符串拼接 用 做 转字符串 用 r 的 第 2 项、做 字符串拼接 用 「×」、做 转字符串 用 r 的 第 3 项。
    做 打印文本 用 做 字符串拼接 用 「  绘制调用: 」、做 转字符串 用 r 的 第 5 项。
完。

本章 收束。
