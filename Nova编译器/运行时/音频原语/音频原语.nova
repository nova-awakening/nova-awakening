《音频原语》

引用《..·..·标准库·基础》。

引用《..·..·标准库·数学·图像处理》。

引用《..·视觉原语·视觉原语》。

音频_8位 为 1。

音频_16位 为 2。

音频_24位 为 3。

音频_32位 为 4。

音频_采样率_8K 为 8000。

音频_采样率_16K 为 16000。

音频_采样率_44K 为 44100。

音频_采样率_48K 为 48000。

音频_采样率_96K 为 96000。

音频_创建 接受 采样率、位深、通道数、采样数：
    可变 数据 为 一组：（空）。
    总采样 为 采样数 乘 通道数。
    可变 i 为 0。
    只要 i 小于 总采样：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 一组：采样率、位深、通道数、采样数、数据。
完。

音频_采样率 接受 buf：
    返还 buf 的 第 0 项。
完。

音频_位深 接受 buf：
    返还 buf 的 第 1 项。
完。

音频_通道数 接受 buf：
    返还 buf 的 第 2 项。
完。

音频_采样数 接受 buf：
    返还 buf 的 第 3 项。
完。

采样_读 接受 buf、通道、位置：
    若 位置 小于 0 或者 位置 大于或等于 buf 的 第 3 项：
        返还 0。
    完。
    偏移 为 位置 乘 buf 的 第 2 项 加 通道。
    若 偏移 大于或等于 做 计数 用 buf 的 第 4 项：
        返还 0。
    完。
    返还 buf 的 第 4 项 的 第 偏移 项。
完。

采样_写 接受 buf、通道、位置、值：
    若 位置 小于 0 或者 位置 大于或等于 buf 的 第 3 项：
        返还 空。
    完。
    偏移 为 位置 乘 buf 的 第 2 项 加 通道。
    若 偏移 大于或等于 做 计数 用 buf 的 第 4 项：
        返还 空。
    完。
    可变 钳值 为 值。
    若 buf 的 第 1 项 等于 音频_16位：
        若 钳值 大于 32767：
            钳值 为 32767。
        完。
        若 钳值 小于 负 32768：
            钳值 为 负 32768。
        完。
    完。
    buf 的 第 4 项 的 第 偏移 项 为 钳值。
完。

波形_正弦 接受 相位、振幅：
    可变 规范相位 为 相位 取余于 36000。
    若 规范相位 小于 0：
        规范相位 为 规范相位 加 36000。
    完。
    可变 符号 为 1。
    可变 角 为 规范相位。
    若 角 大于或等于 18000：
        符号 为 0 减 1。
        角 为 角 减 18000。
    完。
    若 角 大于 9000：
        角 为 18000 减 角。
    完。
    弧度 为 角 乘 31416 除以 18000。
    x 为 弧度。
    x3 为 x 乘 x 除以 10000 乘 x 除以 10000。
    结果 为 x 减 x3 除以 6。
    返还 符号 乘 结果 乘 振幅 除以 10000。
完。

波形_方波 接受 相位、振幅：
    可变 规范 为 相位 取余于 36000。
    若 规范 小于 0：
        规范 为 规范 加 36000。
    完。
    若 规范 小于 18000：
        返还 振幅。
    完。
    返还 0 减 振幅。
完。

波形_锯齿 接受 相位、振幅：
    可变 规范 为 相位 取余于 36000。
    若 规范 小于 0：
        规范 为 规范 加 36000。
    完。
    返还 振幅 乘 (规范 乘 2 减 36000) 除以 36000。
完。

波形_三角 接受 相位、振幅：
    可变 规范 为 相位 取余于 36000。
    若 规范 小于 0：
        规范 为 规范 加 36000。
    完。
    若 规范 小于 9000：
        返还 振幅 乘 规范 除以 9000。
    完。
    若 规范 小于 27000：
        返还 振幅 乘 (18000 减 规范) 除以 9000。
    完。
    返还 振幅 乘 (规范 减 36000) 除以 9000。
完。

音频_生成正弦 接受 buf、频率、振幅：
    可变 i 为 0。
    只要 i 小于 buf 的 第 3 项：
        相位 为 i 乘 频率 乘 36000 除以 buf 的 第 0 项。
        值 为 做 波形_正弦 用 相位、振幅。
        可变 ch 为 0。
        只要 ch 小于 buf 的 第 2 项：
            做 采样_写 用 buf、ch、i、值。
            ch 为 ch 加 1。
        完。
        i 为 i 加 1。
    完。
完。

音频_生成方波 接受 buf、频率、振幅：
    可变 i 为 0。
    只要 i 小于 buf 的 第 3 项：
        相位 为 i 乘 频率 乘 36000 除以 buf 的 第 0 项。
        值 为 做 波形_方波 用 相位、振幅。
        可变 ch 为 0。
        只要 ch 小于 buf 的 第 2 项：
            做 采样_写 用 buf、ch、i、值。
            ch 为 ch 加 1。
        完。
        i 为 i 加 1。
    完。
完。

音频_调音量 接受 buf、音量：
    可变 i 为 0。
    只要 i 小于 做 计数 用 buf 的 第 4 项：
        buf 的 第 4 项 的 第 i 项 为 buf 的 第 4 项 的 第 i 项 乘 音量 除以 10000。
        i 为 i 加 1。
    完。
完。

音频_静音 接受 buf：
    可变 i 为 0。
    只要 i 小于 做 计数 用 buf 的 第 4 项：
        buf 的 第 4 项 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
完。

音频_混合 接受 a、b：
    若 a 的 第 0 项 不等于 b 的 第 0 项 或者 a 的 第 2 项 不等于 b 的 第 2 项：
        返还 a。
    完。
    可变 结果 为 做 音频_创建 用 a 的 第 0 项、a 的 第 1 项、a 的 第 2 项、a 的 第 3 项。
    可变 i 为 0。
    长 为 做 计数 用 a 的 第 4 项。
    若 做 计数 用 b 的 第 4 项 小于 长：
        长 为 做 计数 用 b 的 第 4 项。
    完。
    只要 i 小于 长：
        可变 混合值 为 a 的 第 4 项 的 第 i 项 加 b 的 第 4 项 的 第 i 项。
        若 混合值 大于 32767：
            混合值 为 32767。
        完。
        若 混合值 小于 负 32768：
            混合值 为 负 32768。
        完。
        结果 的 第 4 项 的 第 i 项 为 混合值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

音频_峰值 接受 buf：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 buf 的 第 4 项：
        可变 绝对值 为 buf 的 第 4 项 的 第 i 项。
        若 绝对值 小于 0：
            绝对值 为 0 减 绝对值。
        完。
        若 绝对值 大于 最大：
            最大 为 绝对值。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

音频_RMS 接受 buf：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 buf 的 第 4 项：
        总和 为 总和 加 buf 的 第 4 项 的 第 i 项 乘 buf 的 第 4 项 的 第 i 项 除以 做 计数 用 buf 的 第 4 项。
        i 为 i 加 1。
    完。
    若 总和 小于或等于 0：
        返还 0。
    完。
    可变 x 为 总和。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 总和 除以 x) 除以 2。
    完。
    返还 x。
完。

音频原语_ADSR_创建 接受 攻击、衰减、持续级别、释放：
    返还 一组：攻击、衰减、持续级别、释放。
完。

音频原语_ADSR_值 接受 包络、位置、总长：
    A 为 包络 的 第 0 项。
    D 为 包络 的 第 1 项。
    S 为 包络 的 第 2 项。
    R 为 包络 的 第 3 项。
    释放起点 为 总长 减 R。
    若 位置 小于 A：
        若 A 等于 0：
            返还 10000。
        完。
        返还 位置 乘 10000 除以 A。
    完。
    若 位置 小于 A 加 D：
        若 D 等于 0：
            返还 S。
        完。
        返还 10000 减 (10000 减 S) 乘 (位置 减 A) 除以 D。
    完。
    若 位置 小于 释放起点：
        返还 S。
    完。
    若 位置 大于或等于 总长：
        返还 0。
    完。
    若 R 等于 0：
        返还 0。
    完。
    返还 S 乘 (总长 减 位置) 除以 R。
完。

效果_低通 接受 buf、alpha：
    可变 前值 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 buf 的 第 4 项：
        前值 为 前值 加 alpha 乘 (buf 的 第 4 项 的 第 i 项 减 前值) 除以 10000。
        buf 的 第 4 项 的 第 i 项 为 前值。
        i 为 i 加 1。
    完。
完。

效果_高通 接受 buf、alpha：
    可变 前输入 为 0。
    可变 前输出 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 buf 的 第 4 项：
        当前 为 buf 的 第 4 项 的 第 i 项。
        前输出 为 alpha 乘 (前输出 加 当前 减 前输入) 除以 10000。
        前输入 为 当前。
        buf 的 第 4 项 的 第 i 项 为 前输出。
        i 为 i 加 1。
    完。
完。

效果_延迟 接受 buf、延迟采样数、衰减：
    可变 i 为 延迟采样数。
    只要 i 小于 做 计数 用 buf 的 第 4 项：
        buf 的 第 4 项 的 第 i 项 为 buf 的 第 4 项 的 第 i 项 加 buf 的 第 4 项 的 第 i 减 延迟采样数 项 乘 衰减 除以 10000。
        若 buf 的 第 4 项 的 第 i 项 大于 32767：
            buf 的 第 4 项 的 第 i 项 为 32767。
        完。
        若 buf 的 第 4 项 的 第 i 项 小于 负 32768：
            buf 的 第 4 项 的 第 i 项 为 负 32768。
        完。
        i 为 i 加 1。
    完。
完。

效果_淡入 接受 buf、采样数：
    可变 i 为 0。
    只要 i 小于 采样数 并且 i 小于 做 计数 用 buf 的 第 4 项：
        buf 的 第 4 项 的 第 i 项 为 buf 的 第 4 项 的 第 i 项 乘 i 除以 采样数。
        i 为 i 加 1。
    完。
完。

效果_淡出 接受 buf、采样数：
    总 为 做 计数 用 buf 的 第 4 项。
    可变 i 为 0。
    只要 i 小于 采样数 并且 总 减 1 减 i 大于或等于 0：
        buf 的 第 4 项 的 第 总 减 1 减 i 项 为 buf 的 第 4 项 的 第 总 减 1 减 i 项 乘 i 除以 采样数。
        i 为 i 加 1。
    完。
完。

DFT_正变换 接受 信号、N：
    可变 实部 为 一组：（空）。
    可变 虚部 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 N：
        可变 实和 为 0。
        可变 虚和 为 0。
        可变 n 为 0。
        只要 n 小于 N：
            角度 为 k 乘 n 乘 36000 除以 N。
            余弦 为 做 波形_正弦 用 角度 加 9000、10000。
            正弦 为 做 波形_正弦 用 角度、10000。
            实和 为 实和 加 信号 的 第 n 项 乘 余弦 除以 10000。
            虚和 为 虚和 减 信号 的 第 n 项 乘 正弦 除以 10000。
            n 为 n 加 1。
        完。
        做 追加 用 实部、实和。
        做 追加 用 虚部、虚和。
        k 为 k 加 1。
    完。
    返还 一组：实部、虚部。
完。

DFT_幅度 接受 实部、虚部、k：
    r 为 实部 的 第 k 项。
    i 为 虚部 的 第 k 项。
    平方和 为 r 乘 r 除以 10000 加 i 乘 i 除以 10000。
    若 平方和 小于或等于 0：
        返还 0。
    完。
    可变 x 为 平方和。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 平方和 除以 x) 除以 2。
    完。
    返还 x。
完。

WAV_生成头 接受 采样率、位深、通道数、数据字节数：
    可变 头 为 一组：（空）。
    做 追加 用 头、82。
    做 追加 用 头、73。
    做 追加 用 头、70。
    做 追加 用 头、70。
    文件大小 为 数据字节数 加 36。
    做 追加 用 头、文件大小 位与 255。
    做 追加 用 头、文件大小 除以 256 位与 255。
    做 追加 用 头、文件大小 除以 65536 位与 255。
    做 追加 用 头、文件大小 除以 16777216 位与 255。
    做 追加 用 头、87。
    做 追加 用 头、65。
    做 追加 用 头、86。
    做 追加 用 头、69。
    做 追加 用 头、102。
    做 追加 用 头、109。
    做 追加 用 头、116。
    做 追加 用 头、32。
    做 追加 用 头、16。
    做 追加 用 头、0。
    做 追加 用 头、0。
    做 追加 用 头、0。
    做 追加 用 头、1。
    做 追加 用 头、0。
    做 追加 用 头、通道数 位与 255。
    做 追加 用 头、0。
    做 追加 用 头、采样率 位与 255。
    做 追加 用 头、采样率 除以 256 位与 255。
    做 追加 用 头、采样率 除以 65536 位与 255。
    做 追加 用 头、0。
    字节率 为 采样率 乘 通道数 乘 位深 除以 8。
    做 追加 用 头、字节率 位与 255。
    做 追加 用 头、字节率 除以 256 位与 255。
    做 追加 用 头、字节率 除以 65536 位与 255。
    做 追加 用 头、字节率 除以 16777216 位与 255。
    块对齐 为 通道数 乘 位深 除以 8。
    做 追加 用 头、块对齐 位与 255。
    做 追加 用 头、0。
    做 追加 用 头、位深 位与 255。
    做 追加 用 头、0。
    做 追加 用 头、100。
    做 追加 用 头、97。
    做 追加 用 头、116。
    做 追加 用 头、97。
    做 追加 用 头、数据字节数 位与 255。
    做 追加 用 头、数据字节数 除以 256 位与 255。
    做 追加 用 头、数据字节数 除以 65536 位与 255。
    做 追加 用 头、数据字节数 除以 16777216 位与 255。
    返还 头。
完。

可变 _音频_采样操作数 为 0。

可变 _音频_波形生成数 为 0。

PCM缓冲_创建 接受 采样数、采样率、位深、通道数：
    返还 做 音频_创建 用 采样率、位深、通道数、采样数。
完。

PCM缓冲_峰值 接受 buf：
    返还 做 音频_峰值 用 buf。
完。

PCM缓冲_RMS 接受 buf：
    返还 做 音频_RMS 用 buf。
完。

正弦波_生成 接受 buf、频率、振幅、起始、长度：
    可变 i 为 起始。
    只要 i 小于 起始 加 长度 并且 i 小于 buf 的 第 3 项：
        相位 为 i 乘 频率 乘 36000 除以 buf 的 第 0 项。
        值 为 做 波形_正弦 用 相位、振幅。
        可变 ch 为 0。
        只要 ch 小于 buf 的 第 2 项：
            做 采样_写 用 buf、ch、i、值。
            ch 为 ch 加 1。
        完。
        i 为 i 加 1。
    完。
完。

ADSR_应用 接受 buf、A、D、S起、S止、S级、总长：
    包络 为 做 音频原语_ADSR_创建 用 A、D、S级、总长 减 S止。
    可变 i 为 0。
    只要 i 小于 做 计数 用 buf 的 第 4 项：
        env 为 做 音频原语_ADSR_值 用 包络、i、总长。
        buf 的 第 4 项 的 第 i 项 为 buf 的 第 4 项 的 第 i 项 乘 env 除以 10000。
        i 为 i 加 1。
    完。
完。

低通滤波_应用 接受 buf、alpha：
    做 效果_低通 用 buf、alpha。
完。

音频环形_创建 接受 容量：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 一组：容量、0、0、数据、0。
完。

音频环形_可写 接受 ring：
    若 ring 的 第 4 项 不等于 0：
        返还 0。
    完。
    若 ring 的 第 2 项 大于或等于 ring 的 第 1 项：
        返还 ring 的 第 0 项 减 ring 的 第 2 项 加 ring 的 第 1 项。
    完。
    返还 ring 的 第 1 项 减 ring 的 第 2 项。
完。

音频环形_可读 接受 ring：
    若 ring 的 第 4 项 不等于 0：
        返还 ring 的 第 0 项。
    完。
    若 ring 的 第 2 项 大于或等于 ring 的 第 1 项：
        返还 ring 的 第 2 项 减 ring 的 第 1 项。
    完。
    返还 ring 的 第 0 项 减 ring 的 第 1 项 加 ring 的 第 2 项。
完。

音频环形_写 接受 ring、值：
    若 ring 的 第 4 项 不等于 0：
        返还 假。
    完。
    ring 的 第 3 项 的 第 ring 的 第 2 项 项 为 值。
    ring 的 第 2 项 为 (ring 的 第 2 项 加 1) 取余于 ring 的 第 0 项。
    若 ring 的 第 2 项 等于 ring 的 第 1 项：
        ring 的 第 4 项 为 1。
    完。
    返还 真。
完。

音频环形_读 接受 ring：
    若 ring 的 第 1 项 等于 ring 的 第 2 项 并且 ring 的 第 4 项 等于 0：
        返还 0。
    完。
    值 为 ring 的 第 3 项 的 第 ring 的 第 1 项 项。
    ring 的 第 1 项 为 (ring 的 第 1 项 加 1) 取余于 ring 的 第 0 项。
    ring 的 第 4 项 为 0。
    返还 值。
完。

音频环形_批量写 接受 ring、buf、起始、数量：
    可变 写入数 为 0。
    可变 i 为 起始。
    只要 i 小于 起始 加 数量 并且 做 音频环形_可写 用 ring 大于 0：
        若 i 小于 做 计数 用 buf 的 第 4 项：
            做 音频环形_写 用 ring、buf 的 第 4 项 的 第 i 项。
            写入数 为 写入数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 写入数。
完。

音频设备_创建 接受 采样率、位深、通道数、缓冲大小：
    可变 实际大小 为 缓冲大小。
    若 实际大小 等于 空 或者 实际大小 小于或等于 0：
        实际大小 为 采样率 除以 10。
    完。
    ring 为 做 音频环形_创建 用 实际大小 乘 通道数。
    返还 一组：「软件混音」、ring、采样率、位深、通道数、0。
完。

音频设备_打开 接受 dev：
    dev 的 第 5 项 为 1。
完。

音频设备_播放 接受 dev：
    dev 的 第 5 项 为 2。
完。

音频设备_暂停 接受 dev：
    dev 的 第 5 项 为 3。
完。

音频设备_关闭 接受 dev：
    dev 的 第 5 项 为 0。
完。

音频设备_提交 接受 dev、buf：
    若 dev 的 第 5 项 小于 1：
        返还 0。
    完。
    返还 做 音频环形_批量写 用 dev 的 第 1 项、buf、0、buf 的 第 3 项 乘 buf 的 第 2 项。
完。

音频设备_写入 接受 dev、buf、起始、数量：
    若 dev 的 第 5 项 小于 1：
        返还 0。
    完。
    返还 做 音频环形_批量写 用 dev 的 第 1 项、buf、起始、数量。
完。

BMP_编码 接受 img：
    宽 为 img 的 第 0 项。
    高 为 img 的 第 1 项。
    行字节 为 宽 乘 3。
    填充 为 (4 减 行字节 取余于 4) 取余于 4。
    行步长 为 行字节 加 填充。
    像素数据大小 为 行步长 乘 高。
    文件大小 为 54 加 像素数据大小。
    可变 字节 为 一组：（空）。
    做 追加 用 字节、66。
    做 追加 用 字节、77。
    做 追加 用 字节、文件大小 位与 255。
    做 追加 用 字节、文件大小 除以 256 位与 255。
    做 追加 用 字节、文件大小 除以 65536 位与 255。
    做 追加 用 字节、文件大小 除以 16777216 位与 255。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、54。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、40。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、宽 位与 255。
    做 追加 用 字节、宽 除以 256 位与 255。
    做 追加 用 字节、宽 除以 65536 位与 255。
    做 追加 用 字节、0。
    做 追加 用 字节、高 位与 255。
    做 追加 用 字节、高 除以 256 位与 255。
    做 追加 用 字节、高 除以 65536 位与 255。
    做 追加 用 字节、0。
    做 追加 用 字节、1。
    做 追加 用 字节、0。
    做 追加 用 字节、24。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、像素数据大小 位与 255。
    做 追加 用 字节、像素数据大小 除以 256 位与 255。
    做 追加 用 字节、像素数据大小 除以 65536 位与 255。
    做 追加 用 字节、像素数据大小 除以 16777216 位与 255。
    做 追加 用 字节、19。
    做 追加 用 字节、11。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、19。
    做 追加 用 字节、11。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    做 追加 用 字节、0。
    可变 y 为 高 减 1。
    只要 y 大于或等于 0：
        可变 x 为 0。
        只要 x 小于 宽：
            像素 为 做 像素_读 用 img、x、y。
            做 追加 用 字节、像素 的 第 2 项 位与 255。
            做 追加 用 字节、像素 的 第 1 项 位与 255。
            做 追加 用 字节、像素 的 第 0 项 位与 255。
            x 为 x 加 1。
        完。
        可变 pi 为 0。
        只要 pi 小于 填充：
            做 追加 用 字节、0。
            pi 为 pi 加 1。
        完。
        y 为 y 减 1。
    完。
    返还 字节。
完。

BMP_解码 接受 字节：
    若 做 计数 用 字节 小于 54：
        返还 空。
    完。
    若 字节 的 第 0 项 不等于 66 或者 字节 的 第 1 项 不等于 77：
        返还 空。
    完。
    宽 为 字节 的 第 18 项 加 字节 的 第 19 项 乘 256 加 字节 的 第 20 项 乘 65536。
    高 为 字节 的 第 22 项 加 字节 的 第 23 项 乘 256 加 字节 的 第 24 项 乘 65536。
    位深 为 字节 的 第 28 项 加 字节 的 第 29 项 乘 256。
    若 位深 不等于 24：
        返还 空。
    完。
    偏移 为 字节 的 第 10 项 加 字节 的 第 11 项 乘 256 加 字节 的 第 12 项 乘 65536 加 字节 的 第 13 项 乘 16777216。
    行字节 为 宽 乘 3。
    填充 为 (4 减 行字节 取余于 4) 取余于 4。
    行步长 为 行字节 加 填充。
    img 为 做 图像_创建 用 宽、高、4。
    可变 y 为 高 减 1。
    只要 y 大于或等于 0：
        行偏移 为 偏移 加 (高 减 1 减 y) 乘 行步长。
        可变 x 为 0。
        只要 x 小于 宽：
            bi 为 行偏移 加 x 乘 3。
            若 bi 加 2 小于 做 计数 用 字节：
                做 像素_写 用 img、x、y、字节 的 第 bi 加 2 项、字节 的 第 bi 加 1 项、字节 的 第 bi 项、255。
            完。
            x 为 x 加 1。
        完。
        y 为 y 减 1。
    完。
    返还 img。
完。

视频帧队列_创建 接受 最大深度：
    返还 一组：最大深度、一组：（空）、0、0、0。
完。

视频帧队列_推入 接受 队列、帧：
    若 队列 的 第 4 项 大于或等于 队列 的 第 0 项：
        返还 假。
    完。
    做 追加 用 队列 的 第 1 项、帧。
    队列 的 第 4 项 为 队列 的 第 4 项 加 1。
    返还 真。
完。

视频帧队列_弹出 接受 队列：
    若 队列 的 第 4 项 等于 0：
        返还 空。
    完。
    帧 为 队列 的 第 1 项 的 第 队列 的 第 2 项 项。
    队列 的 第 2 项 为 队列 的 第 2 项 加 1。
    队列 的 第 4 项 为 队列 的 第 4 项 减 1。
    返还 帧。
完。

视频帧队列_深度 接受 队列：
    返还 队列 的 第 4 项。
完。

可变 _音频合同_采样率 为 44100。

可变 _音频合同_位深 为 16。

可变 _音频合同_通道数 为 1。

可变 _音频合同_缓冲长度 为 4096。

可变 _音频合同_延迟预算 为 20。

可变 _音频合同_设备类型 为 「软件」。

音频合同_设置 接受 采样率、位深、通道、缓冲、延迟、设备：
    _音频合同_采样率 为 采样率。
    _音频合同_位深 为 位深。
    _音频合同_通道数 为 通道。
    _音频合同_缓冲长度 为 缓冲。
    _音频合同_延迟预算 为 延迟。
    _音频合同_设备类型 为 设备。
完。

音频管线_报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「采样率」、做 转字符串 用 _音频合同_采样率。
    做 字典设 用 r、「位深」、做 转字符串 用 _音频合同_位深。
    做 字典设 用 r、「通道数」、做 转字符串 用 _音频合同_通道数。
    做 字典设 用 r、「缓冲长度」、做 转字符串 用 _音频合同_缓冲长度。
    做 字典设 用 r、「延迟预算ms」、做 转字符串 用 _音频合同_延迟预算。
    做 字典设 用 r、「设备类型」、_音频合同_设备类型。
    做 字典设 用 r、「采样操作数」、做 转字符串 用 _音频_采样操作数。
    做 字典设 用 r、「波形生成数」、做 转字符串 用 _音频_波形生成数。
    返还 r。
完。

可变 _图像合同_格式 为 「RGBA8」。

可变 _图像合同_色彩空间 为 「sRGB」。

可变 _图像合同_最大宽 为 4096。

可变 _图像合同_最大高 为 4096。

图像合同_设置 接受 格式、色彩空间、最大宽、最大高：
    _图像合同_格式 为 格式。
    _图像合同_色彩空间 为 色彩空间。
    _图像合同_最大宽 为 最大宽。
    _图像合同_最大高 为 最大高。
完。

图像管线_报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「格式」、_图像合同_格式。
    做 字典设 用 r、「色彩空间」、_图像合同_色彩空间。
    做 字典设 用 r、「最大宽」、做 转字符串 用 _图像合同_最大宽。
    做 字典设 用 r、「最大高」、做 转字符串 用 _图像合同_最大高。
    返还 r。
完。

可变 _视频合同_帧率 为 30。

可变 _视频合同_分辨率宽 为 1920。

可变 _视频合同_分辨率高 为 1080。

可变 _视频合同_编码 为 「raw」。

可变 _视频合同_帧队列深度 为 3。

可变 _视频合同_AV同步模式 为 「audio_master」。

视频合同_设置 接受 帧率、宽、高、编码、队列深、同步：
    _视频合同_帧率 为 帧率。
    _视频合同_分辨率宽 为 宽。
    _视频合同_分辨率高 为 高。
    _视频合同_编码 为 编码。
    _视频合同_帧队列深度 为 队列深。
    _视频合同_AV同步模式 为 同步。
完。

视频管线_报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「帧率」、做 转字符串 用 _视频合同_帧率。
    做 字典设 用 r、「分辨率」、做 字符串拼接 用 做 转字符串 用 _视频合同_分辨率宽、做 字符串拼接 用 「x」、做 转字符串 用 _视频合同_分辨率高。
    做 字典设 用 r、「编码」、_视频合同_编码。
    做 字典设 用 r、「帧队列深度」、做 转字符串 用 _视频合同_帧队列深度。
    做 字典设 用 r、「AV同步」、_视频合同_AV同步模式。
    返还 r。
完。

可变 _媒体_音频缓冲字节 为 0。

可变 _媒体_图像缓冲字节 为 0。

可变 _媒体_视频缓冲字节 为 0。

媒体内存_记录 接受 音频、图像、视频：
    _媒体_音频缓冲字节 为 音频。
    _媒体_图像缓冲字节 为 图像。
    _媒体_视频缓冲字节 为 视频。
完。

媒体内存_报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「音频缓冲」、做 转字符串 用 _媒体_音频缓冲字节。
    做 字典设 用 r、「图像缓冲」、做 转字符串 用 _媒体_图像缓冲字节。
    做 字典设 用 r、「视频缓冲」、做 转字符串 用 _媒体_视频缓冲字节。
    做 字典设 用 r、「总计」、做 转字符串 用 _媒体_音频缓冲字节 加 _媒体_图像缓冲字节 加 _媒体_视频缓冲字节。
    返还 r。
完。

AV同步_创建 接受 采样率：
    返还 一组：采样率、0、0、0、0、0、0。
完。

AV同步_音频推进 接受 同步、采样数：
    同步 的 第 1 项 为 同步 的 第 1 项 加 采样数。
    同步 的 第 2 项 为 同步 的 第 1 项 乘 1000 除以 同步 的 第 0 项。
完。

AV同步_音频时间 接受 同步：
    返还 同步 的 第 2 项。
完。

AV同步_视频决策 接受 同步、视频PTS：
    同步 的 第 3 项 为 视频PTS。
    同步 的 第 4 项 为 视频PTS 减 同步 的 第 2 项。
    若 同步 的 第 4 项 小于 负 40：
        同步 的 第 5 项 为 同步 的 第 5 项 加 1。
        返还 0。
    完。
    若 同步 的 第 4 项 大于 20：
        同步 的 第 6 项 为 同步 的 第 6 项 加 1。
        返还 2。
    完。
    返还 1。
完。

AV同步_漂移 接受 同步：
    返还 同步 的 第 4 项。
完。

AV同步_报告 接受 同步：
    可变 r 为 做 字典。
    做 字典设 用 r、「采样率」、做 转字符串 用 同步 的 第 0 项。
    做 字典设 用 r、「已播放采样」、做 转字符串 用 同步 的 第 1 项。
    做 字典设 用 r、「音频时间ms」、做 转字符串 用 同步 的 第 2 项。
    做 字典设 用 r、「视频PTS」、做 转字符串 用 同步 的 第 3 项。
    做 字典设 用 r、「漂移ms」、做 转字符串 用 同步 的 第 4 项。
    做 字典设 用 r、「丢帧数」、做 转字符串 用 同步 的 第 5 项。
    做 字典设 用 r、「重复帧数」、做 转字符串 用 同步 的 第 6 项。
    返还 r。
完。

媒体_真实smoke 接受 零参：
    可变 结果 为 做 字典。
    buf 为 做 PCM缓冲_创建 用 1024、44100、16、1。
    做 正弦波_生成 用 buf、440、800、0、1024。
    做 ADSR_应用 用 buf、50、100、700、800、174、1024。
    做 低通滤波_应用 用 buf、200。
    峰值 为 做 PCM缓冲_峰值 用 buf。
    rms 为 做 PCM缓冲_RMS 用 buf。
    设备 为 做 音频设备_创建 用 44100、16、1、4096。
    做 音频设备_打开 用 设备。
    写入数 为 做 音频设备_写入 用 设备、buf、0、512。
    _媒体_音频缓冲字节 为 1024 乘 2。
    音频报告 为 做 字典。
    做 字典设 用 音频报告、「采样率」、「44100」。
    做 字典设 用 音频报告、「位深」、「16」。
    做 字典设 用 音频报告、「通道」、「1」。
    做 字典设 用 音频报告、「缓冲长度」、「1024」。
    做 字典设 用 音频报告、「峰值」、做 转字符串 用 峰值。
    做 字典设 用 音频报告、「RMS」、做 转字符串 用 rms。
    做 字典设 用 音频报告、「环形写入」、做 转字符串 用 写入数。
    做 字典设 用 结果、「audio_pipeline」、音频报告。
    宽 为 64。
    高 为 64。
    可变 图像 为 一组：（空）。
    可变 pi 为 0。
    只要 pi 小于 宽 乘 高 乘 4：
        若 pi 除以 4 取余于 宽 小于 宽 除以 2：
            做 追加 用 图像、255。
            做 追加 用 图像、0。
            做 追加 用 图像、0。
            做 追加 用 图像、255。
        否则：
            做 追加 用 图像、0。
            做 追加 用 图像、0。
            做 追加 用 图像、255。
            做 追加 用 图像、255。
        完。
        pi 为 pi 加 4。
    完。
    _媒体_图像缓冲字节 为 宽 乘 高 乘 4。
    图像报告 为 做 字典。
    做 字典设 用 图像报告、「宽」、做 转字符串 用 宽。
    做 字典设 用 图像报告、「高」、做 转字符串 用 高。
    做 字典设 用 图像报告、「色彩空间」、「RGBA8」。
    做 字典设 用 图像报告、「像素数」、做 转字符串 用 宽 乘 高。
    做 字典设 用 图像报告、「字节数」、做 转字符串 用 宽 乘 高 乘 4。
    做 字典设 用 结果、「image_pipeline」、图像报告。
    同步 为 做 AV同步_创建 用 44100。
    可变 跳过数 为 0。
    可变 重复数 为 0。
    做 AV同步_音频推进 用 同步、44100。
    可变 显示数 为 0。
    可变 fi 为 0。
    只要 fi 小于 30：
        pts 为 fi 乘 33。
        决策 为 做 AV同步_视频决策 用 同步、pts。
        若 决策 等于 1：
            显示数 为 显示数 加 1。
        完。
        若 决策 等于 0：
            跳过数 为 跳过数 加 1。
        完。
        若 决策 等于 2：
            重复数 为 重复数 加 1。
        完。
        fi 为 fi 加 1。
    完。
    _媒体_视频缓冲字节 为 30 乘 宽 乘 高 乘 4。
    视频报告 为 做 字典。
    做 字典设 用 视频报告、「帧数」、「30」。
    做 字典设 用 视频报告、「显示」、做 转字符串 用 显示数。
    做 字典设 用 视频报告、「跳过」、做 转字符串 用 跳过数。
    做 字典设 用 视频报告、「重复」、做 转字符串 用 重复数。
    做 字典设 用 结果、「video_pipeline」、视频报告。
    做 字典设 用 结果、「av_sync」、做 AV同步_报告 用 同步。
    做 字典设 用 结果、「media_memory」、做 媒体内存_报告。
    返还 结果。
完。

音视同步_报告 接受 同步：
    返还 做 AV同步_报告 用 同步。
完。

本章 收束。
