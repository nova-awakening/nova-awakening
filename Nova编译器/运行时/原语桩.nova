《原语桩》

引用《..·标准库·基础》。

可变 __原语桩_活跃 为 真。

可变 __原语桩_调用计数 为 0。

原语桩_是活跃 接受 零参：
    返还 __原语桩_活跃。
完。

原语桩_警告 接受 名称：
    __原语桩_调用计数 为 __原语桩_调用计数 加 1。
    若 __原语桩_调用计数 小于或等于 3：
        做 打印 用 「[原语桩警告] 非S0环境调用了底层原语: 」 加 名称 加 「 — 返回默认值0」。
    完。
完。

调用指针 接受 _指针、_扩2、_扩3、_扩4、_扩5、_扩6：
    做 原语桩_警告 用 「调用指针」。
    返还 0。
完。

调用指针1 接受 _指针、_a：
    做 原语桩_警告 用 「调用指针1」。
    返还 0。
完。

调用指针2 接受 _指针、_a、_b：
    做 原语桩_警告 用 「调用指针2」。
    返还 0。
完。

调用指针3 接受 _指针、_a、_b、_c：
    做 原语桩_警告 用 「调用指针3」。
    返还 0。
完。

调用指针4 接受 _指针、_a、_b、_c、_d：
    做 原语桩_警告 用 「调用指针4」。
    返还 0。
完。

调用指针6 接受 _指针、_a、_b、_c、_d、_e、_f：
    做 原语桩_警告 用 「调用指针6」。
    返还 0。
完。

调用指针5 接受 _指针、_a、_b、_c、_d、_e：
    做 原语桩_警告 用 「调用指针5」。
    返还 0。
完。

系统调用 接受 _号、_a、_b、_c、_扩5：
    做 原语桩_警告 用 「系统调用」。
    返还 0。
完。

系统调用_原生 接受 _号、_a、_b、_c、_d、_e：
    做 原语桩_警告 用 「系统调用_原生」。
    返还 0。
完。

系统调用6 接受 _号、_a、_b、_c、_d、_e、_f：
    做 原语桩_警告 用 「系统调用6」。
    返还 0。
完。

开方 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

抛 接受 _码：
    做 退出 用 _码。
完。

加载库 接受 _名称：
    返还 0。
完。

内存读 接受 _地址：
    返还 0。
完。

内存填充 接受 _地址、_值、_大小：
    返还 0。
完。

__系统命令 接受 _命令：
    返还 做 __运行时_执行命令 用 _命令。
完。

__运行时_C串转Nova 接受 _指针：
    返还 「」。
完。

__运行时_文件存在 接受 _路径：
    返还 0。
完。

__即时编译_执行 接受 _块、_偏移：
    返还 0。
完。

外部调用0 接受 _地址、_扩2：
    返还 0。
完。

是浮点 接受 _v：
    返还 0。
完。

字典删 接受 _d、_键：
    所有键 为 做 字典键 用 _d。
    可变 新 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有键：
        若 所有键 的 第 i 项 不等于 _键：
            做 字典设 用 新、所有键 的 第 i 项、做 字典取 用 _d、所有键 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 新。
完。

类型判断 接受 _v：
    返还 做 类型 用 _v。
完。

代码生成_偏移读取 接受 _偏移、_大小、_扩3：
    返还 0。
完。

代码生成_立即数赋值 接受 _寄存器、_值、_扩3：
    返还 0。
完。

代码生成_cmp_立即数 接受 _寄存器、_值：
    返还 0。
完。

代码生成_分配 接受 _大小、_扩2：
    返还 0。
完。

代码生成_函数尾 接受 _帧大小：
    返还 0。
完。

代码生成_函数头 接受 _名称、_参数数、_扩3：
    返还 0。
完。

代码生成_零填充 接受 _大小、_扩2：
    返还 0。
完。

代码生成_函数指针 接受 _名称、_扩2：
    返还 0。
完。

代码生成_调用 接受 _名称、_扩2：
    返还 0。
完。

代码生成_参数复制 接受 _源、_目标、_扩3：
    返还 0。
完。

代码生成_sub 接受 _寄存器、_值：
    返还 0。
完。

代码生成_mov_dword_store 接受 _基址、_偏移、_值：
    返还 0。
完。

代码生成_mov_word_store 接受 _基址、_偏移、_值：
    返还 0。
完。

代码生成_mov_byte_store 接受 _基址、_偏移、_值：
    返还 0。
完。

代码生成_mov_qword_store 接受 _基址、_偏移、_值：
    返还 0。
完。

代码生成_test_rax 接受 _标志：
    返还 0。
完。

代码生成_add 接受 _寄存器、_值：
    返还 0。
完。

代码生成_imul_立即数 接受 _寄存器、_值：
    返还 0。
完。

代码生成_mov_qword 接受 _目标、_源：
    返还 0。
完。

代码生成_lea_rbp 接受 _偏移、_扩2：
    返还 0。
完。

代码生成_mov_dword 接受 _目标、_源：
    返还 0。
完。

代码生成_movzx_word 接受 _目标、_源：
    返还 0。
完。

代码生成_movzx_byte 接受 _目标、_源：
    返还 0。
完。

代码生成_jz 接受 _标签、_目标：
    返还 0。
完。

代码生成_jae 接受 _标签、_目标：
    返还 0。
完。

代码生成_偏移写入 接受 _偏移、_值、_扩3：
    返还 0。
完。

代码生成_数据段 接受 _数据、_扩2：
    返还 0。
完。

编译器_配置代码缓冲 接受 _大小：
    返还 0。
完。

GPU初始化 接受 _设备：
    返还 0。
完。

GPU分配 接受 _大小：
    返还 0。
完。

GPU上传 接受 _设备、_主机、_大小：
    返还 0。
完。

GPU下载 接受 _主机、_设备、_大小：
    返还 0。
完。

GPU释放 接受 _设备内存：
    返还 0。
完。

GPU关闭 接受 零参：
    返还 0。
完。

GPU_快速沃尔什变换 接受 _数据、_大小：
    返还 0。
完。

__内建_向量载入 接受 _地址、_扩2：
    返还 0。
完。

__内建_向量存储 接受 _地址、_值、_扩3：
    返还 0。
完。

__内建_向量加32x8 接受 _a、_b：
    返还 0。
完。

__内建_向量加64x4 接受 _a、_b：
    返还 0。
完。

__内建_向量乘32x8 接受 _a、_b：
    返还 0。
完。

__内建_向量宽乘32 接受 _a、_b：
    返还 0。
完。

__内建_向量水平加 接受 _v：
    返还 0。
完。

__内建_向量除64 接受 _a、_b：
    返还 0。
完。

__内建_向量最大值32 接受 _a、_b：
    返还 0。
完。

__内建_内存屏障 接受 零参：
    返还 0。
完。

__内建_原子比较交换 接受 _地址、_期望、_新值：
    返还 0。
完。

__内建_原子加 接受 _地址、_值：
    返还 0。
完。

__ff指令_调用1 接受 _函数、_a：
    返还 做 调用指针1 用 _函数、_a。
完。

__ff指令_调用2 接受 _函数、_a、_b：
    返还 做 调用指针2 用 _函数、_a、_b。
完。

__ff指令_调用3 接受 _函数、_a、_b、_c：
    返还 做 调用指针3 用 _函数、_a、_b、_c。
完。

__ff指令_调用4 接受 _函数、_a、_b、_c、_d：
    返还 做 调用指针4 用 _函数、_a、_b、_c、_d。
完。

__ff指令_调用5 接受 _函数、_a、_b、_c、_d、_e：
    返还 做 调用指针5 用 _函数、_a、_b、_c、_d、_e。
完。

__ff指令_调用6 接受 _函数、_a、_b、_c、_d、_e、_f：
    返还 做 调用指针6 用 _函数、_a、_b、_c、_d、_e、_f。
完。

__ff指令_调用N 接受 _函数、_参数列表：
    返还 做 调用指针 用 _函数、0、0、0、0、0。
完。

常量表_注册 接受 _名称、_值：
    返还 0。
完。

本章 收束。
