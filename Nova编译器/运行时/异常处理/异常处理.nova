《异常处理》

引用《..·..·标准库·基础》。

引用《..·..·编译器·规划·类型系统·枚举·枚举》。

错误_空引用 为 3。

错误_类型 为 12。

异常处理_错误_栈溢出 为 10。

错误_断言 为 11。

错误_自定义 为 100。

异常处理_结果_成功 接受 值：
    返还 一组：1、值、「」。
完。

异常处理_结果_失败 接受 码、消息：
    返还 一组：0、码、消息。
完。

异常处理_结果_是成功 接受 r：
    返还 r 的 第 0 项 等于 1。
完。

结果_是失败 接受 r：
    返还 r 的 第 0 项 等于 0。
完。

异常_结果_成功 接受 值：
    返还 做 异常处理_结果_成功 用 值。
完。

异常_结果_失败 接受 码、消息：
    返还 做 异常处理_结果_失败 用 码、消息。
完。

异常_结果_是成功 接受 r：
    返还 做 异常处理_结果_是成功 用 r。
完。

异常处理_结果_取值 接受 r：
    返还 r 的 第 1 项。
完。

结果_错误码 接受 r：
    返还 r 的 第 1 项。
完。

结果_消息 接受 r：
    返还 r 的 第 2 项。
完。

异常处理_安全除 接受 a、b：
    若 b 等于 0：
        返还 做 异常_结果_失败 用 错误_除零、「除以零」。
    完。
    返还 做 异常_结果_成功 用 a 除以 b。
完。

异常处理_安全索引 接受 列表、i：
    若 i 小于 0 或者 i 大于或等于 做 计数 用 列表：
        返还 做 异常_结果_失败 用 错误_越界、「index out of bounds」。
    完。
    返还 做 异常_结果_成功 用 列表 的 第 i 项。
完。

安全_取非空 接受 值：
    若 值 等于 空：
        返还 做 异常_结果_失败 用 错误_空引用、「空引用」。
    完。
    返还 做 异常_结果_成功 用 值。
完。

异常栈_创建 接受 零参：
    返还 一组：（空）。
完。

异常栈_推 接受 栈、函数名、码、消息：
    做 追加 用 栈、一组：函数名、码、消息、做 计数 用 栈。
完。

异常栈_深度 接受 栈：
    返还 做 计数 用 栈。
完。

异常栈_顶 接受 栈：
    若 做 计数 用 栈 等于 0：
        返还 空。
    完。
    返还 栈 的 第 做 计数 用 栈 减 1 项。
完。

异常栈_跟踪 接受 栈：
    可变 结果 为 「」。
    可变 i 为 做 计数 用 栈 减 1。
    只要 i 大于或等于 0：
        帧 为 栈 的 第 i 项。
        结果 为 结果 加 「 在 」 加 帧 的 第 0 项 加 「: 」 加 帧 的 第 2 项。
        若 i 大于 0：
            结果 为 结果 加 「 | 」。
        完。
        i 为 i 减 1。
    完。
    返还 结果。
完。

异常处理_错误描述 接受 码：
    若 码 等于 错误_除零：
        返还 「除以零」。
    完。
    若 码 等于 错误_越界：
        返还 「index out of bounds」。
    完。
    若 码 等于 错误_空引用：
        返还 「空引用」。
    完。
    若 码 等于 错误_类型：
        返还 「类型错误」。
    完。
    若 码 等于 异常处理_错误_栈溢出：
        返还 「stack overflow」。
    完。
    若 码 等于 错误_断言：
        返还 「assertion failed」。
    完。
    若 码 等于 错误_溢出：
        返还 「数值溢出」。
    完。
    若 码 等于 错误_IO：
        返还 「IO错误」。
    完。
    返还 「未知错误」。
完。

异常_断言 接受 栈、条件、消息：
    若 条件 等于 假：
        做 异常栈_推 用 栈、「断言」、错误_断言、消息。
        返还 假。
    完。
    返还 真。
完。

可变 崩溃状态 为 一组：0、一组：一组：0、0、「」、一组：（空）、0、「」。

崩溃_设协程 接受 id：
    崩溃状态 的 第 0 项 为 id。
完。

崩溃_获取协程 接受 零参：
    返还 崩溃状态 的 第 0 项。
完。

崩溃_查上下文 接受 id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 崩溃状态 的 第 1 项：
        若 崩溃状态 的 第 1 项 的 第 i 项 的 第 0 项 等于 id：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    做 插入 用 崩溃状态 的 第 1 项、做 计数 用 崩溃状态 的 第 1 项、一组：id、0、「」。
    返还 做 计数 用 崩溃状态 的 第 1 项 减 1。
完。

崩溃 接受 消息：
    idx 为 做 崩溃_查上下文 用 崩溃状态 的 第 0 项。
    崩溃状态 的 第 1 项 的 第 idx 项 的 第 1 项 为 1。
    崩溃状态 的 第 1 项 的 第 idx 项 的 第 2 项 为 消息。
完。

恢复 接受 _扩1：
    idx 为 做 崩溃_查上下文 用 崩溃状态 的 第 0 项。
    若 崩溃状态 的 第 1 项 的 第 idx 项 的 第 1 项 等于 1：
        崩溃状态 的 第 1 项 的 第 idx 项 的 第 1 项 为 0。
        msg 为 崩溃状态 的 第 1 项 的 第 idx 项 的 第 2 项。
        崩溃状态 的 第 1 项 的 第 idx 项 的 第 2 项 为 「」。
        返还 msg。
    完。
    返还 空。
完。

是崩溃 接受 零参：
    idx 为 做 崩溃_查上下文 用 崩溃状态 的 第 0 项。
    返还 崩溃状态 的 第 1 项 的 第 idx 项 的 第 1 项 等于 1。
完。

恐慌 接受 消息：
    做 崩溃 用 消息。
完。

异常处理_错误_包装 接受 外码、外消息、内结果：
    链消息 为 外消息 加 「 <- 」 加 做 结果_消息 用 内结果。
    返还 做 异常_结果_失败 用 外码、链消息。
完。

异常_结果_映射值 接受 r、变换类型、参数：
    若 做 结果_是失败 用 r：
        返还 r。
    完。
    v 为 做 结果_取值 用 r。
    若 变换类型 等于 1：
        返还 做 异常_结果_成功 用 v 加 参数。
    完。
    若 变换类型 等于 2：
        返还 做 异常_结果_成功 用 v 乘 参数。
    完。
    若 变换类型 等于 3：
        返还 做 异常_结果_成功 用 v 减 参数。
    完。
    返还 r。
完。

异常处理_安全乘 接受 a、b：
    若 b 等于 0：
        返还 做 异常_结果_成功 用 0。
    完。
    结果 为 a 乘 b。
    若 结果 除以 b 不等于 a：
        返还 做 异常_结果_失败 用 错误_溢出、「整数溢出」。
    完。
    返还 做 异常_结果_成功 用 结果。
完。

安全_类型 接受 值、期望类型：
    若 期望类型 等于 1 并且 值 等于 空：
        返还 做 异常_结果_失败 用 错误_类型、「期望非空」。
    完。
    返还 做 异常_结果_成功 用 值。
完。

异常栈_过滤 接受 栈、最小码：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 栈：
        若 栈 的 第 i 项 的 第 1 项 大于或等于 最小码：
            做 追加 用 结果、栈 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

异常栈_清空重建 接受 零参：
    返还 一组：（空）。
完。

panic嵌套 接受 消息：
    做 追加 用 崩溃状态 的 第 2 项、消息。
    崩溃状态 的 第 3 项 为 1。
    崩溃状态 的 第 4 项 为 消息。
完。

recover嵌套 接受 零参：
    若 做 计数 用 崩溃状态 的 第 2 项 大于 0：
        msg 为 崩溃状态 的 第 2 项 的 第 做 计数 用 崩溃状态 的 第 2 项 减 1 项。
        做 删除 用 崩溃状态 的 第 2 项、做 计数 用 崩溃状态 的 第 2 项 减 1。
        崩溃状态 的 第 3 项 为 0。
        崩溃状态 的 第 4 项 为 「」。
        返还 msg。
    完。
    返还 空。
完。

panic嵌套深度 接受 零参：
    返还 做 计数 用 崩溃状态 的 第 2 项。
完。

可变 __异常桥_VM异常值 为 空。

可变 __异常桥_VM异常栈 为 空。

异常桥_VM抛出回调 接受 异常值：
    __异常桥_VM异常值 为 异常值。
    做 崩溃 用 做 转字符串 用 异常值。
完。

异常桥_VM异常转Result 接受 零参：
    若 __异常桥_VM异常值 不等于 空：
        值 为 __异常桥_VM异常值。
        __异常桥_VM异常值 为 空。
        返还 做 异常_结果_失败 用 错误_自定义、做 转字符串 用 值。
    完。
    返还 空。
完。

异常桥_语言panic到VM 接受 vm、消息：
    若 vm 不等于 空 并且 做 计数 用 vm 大于 5：
        vm 的 第 5 项 为 3。
    完。
    做 崩溃 用 消息。
完。

异常桥_Result升级throw 接受 r：
    若 做 结果_是失败 用 r：
        做 崩溃 用 「不可恢复: 」 加 做 结果_消息 用 r。
        返还 真。
    完。
    返还 假。
完。

异常桥_安全调用 接受 函数引用、参数：
    旧状态 为 崩溃状态 的 第 3 项。
    崩溃状态 的 第 3 项 为 0。
    结果值 为 做 函数引用 用 参数。
    若 崩溃状态 的 第 3 项 等于 1：
        msg 为 做 恢复 用 0。
        返还 做 异常_结果_失败 用 错误_自定义、msg。
    完。
    崩溃状态 的 第 3 项 为 旧状态。
    返还 做 异常_结果_成功 用 结果值。
完。

异常桥_状态报告 接受 零参：
    返还 一组：崩溃状态 的 第 3 项、__异常桥_VM异常值 不等于 空、做 panic嵌套深度。
完。

本章 收束。
