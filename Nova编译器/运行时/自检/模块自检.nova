《模块自检》

引用《..·..·标准库·基础》。

引用《..·热更新·热更新管理》。

引用《..·..·编译器·分析·内存安全分析》。

引用《..·..·编译器·跨语言·外源合同·外源项目合同》。

引用《..·..·编译器·跨语言·统一语义·统一语义图》。

引用《..·JIT·编译引擎》。

引用《..·图形原语·精灵批渲染》。

引用《..·图形原语·Tilemap渲染》。

引用《..·宿主适配·诊断发布器》。

引用《..·音频原语·音频原语》。

引用《..·JIT·热点检测》。

引用《..·宿主适配·LSP桥接》。

引用《..·..·智能基因·接口·模型接口》。

引用《..·..·编译器·项目系统·媒体管线合同》。

引用《..·内存·内存池》。

引用《..·游戏引擎·碰撞检测》。

引用《..·音频原语·混音器》。

引用《..·..·编译器·语义·符号表》。

引用《..·图形原语·粒子系统》。

引用《..·..·编译器·规划·代码生成·目标选择》。

引用《..·..·编译器·安全·能力系统》。

引用《..·..·标准库·推理·推理》。

引用《..·..·标准库·状态机·状态机引擎》。

引用《..·..·编译器·安全·污点分析引擎》。

引用《..·..·平台·GPU·渲染·渲染合同》。

引用《..·..·编译器·项目系统·引擎合同》。

引用《..·..·编译器·安全·效果系统》。

引用《..·..·平台·目标选择》。

引用《..·智能推理·注意力机制》。

引用《..·游戏引擎·输入系统》。

引用《..·内存·内存Profile测量》。

引用《..·..·编译器·语义·类型推断》。

引用《..·..·标准库·推理优化·推理优化》。

引用《..·..·编译器·分析·编译流水线》。

引用《..·..·智能基因·核心·神经元》。

引用《..·..·工具·代码检查·代码检查》。

引用《..·..·标准库·音频处理·音频处理》。

引用《..·..·编译器·分析·错误诊断》。

引用《..·智能推理·Transformer块》。

引用《..·内存管理·内存分配》。

引用《..·..·标准库·数学·注意力》。

引用《..·..·标准库·数学·神经网络》。

引用《..·..·标准库·模型架构·模型架构》。

引用《..·..·标准库·理论核心·阴阳对偶·阴阳对偶编译》。

引用《..·..·标准库·神经形态·脉冲神经网络》。

引用《..·..·标准库·高级神经网络·高级神经网络》。

引用《..·..·编译器·验证·形式化验证引擎》。

引用《..·智能原语·AI原语》。

引用《..·内存管理·内存profile》。

引用《..·策略引擎·策略引擎》。

引用《..·观测·崩溃转储》。

引用《..·观测·符号服务》。

引用《..·观测·结构化追踪》。

引用《..·宿主适配·宿主协议》。

引用《..·音频原语·频谱分析》。

引用《..·游戏引擎·主循环》。

引用《..·游戏引擎·场景图》。

引用《..·游戏引擎·实体组件系统》。

引用《..·视觉原语·视觉原语》。

引用《..·列表引擎·列表引擎》。

引用《..·字符串引擎·字符串引擎》。

引用《..·字典引擎·字典引擎》。

引用《..·协程·协程》。

引用《..·解释器·VM_GPU内建》。

引用《..·..·内存·分配器·分配器》。

引用《..·..·内存·原子操作》。

引用《..·..·工具·性能分析器·性能分析器》。

引用《..·..·工具·测试框架·断言_测试》。

引用《..·..·工具·测试框架·Nova》。

引用《..·..·智能基因·AI基因入口》。

可变 _自检_通过 为 0。

可变 _自检_失败 为 0。

可变 _自检_总数 为 0。

自检_断言 接受 名称、条件：
    _自检_总数 为 _自检_总数 加 1。
    若 条件：
        _自检_通过 为 _自检_通过 加 1。
    否则：
        _自检_失败 为 _自检_失败 加 1。
        做 打印文本 用 做 字符串拼接 用 「  ✗ FAIL: 」、名称。
    完。
完。

自检_值相等 接受 实际、期望：
    若 做 是字符串 用 实际 并且 做 是字符串 用 期望：
        返还 做 字符串等于 用 实际、期望。
    完。
    返还 实际 等于 期望。
完。

自检_断言值 接受 名称、实际、期望：
    _自检_总数 为 _自检_总数 加 1。
    若 做 自检_值相等 用 实际、期望：
        _自检_通过 为 _自检_通过 加 1。
    否则：
        _自检_失败 为 _自检_失败 加 1。
        做 打印文本 用 做 字符串拼接 用 「  ✗ FAIL: 」、做 字符串拼接 用 名称、做 字符串拼接 用 「 期望=」、做 字符串拼接 用 做 转字符串 用 期望、做 字符串拼接 用 「 实际=」、做 转字符串 用 实际。
    完。
完。

自检_M12_渲染 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M12 渲染模块...」。
    tex 为 做 纹理_创建 用 8、8。
    做 自检_断言值 用 「纹理宽度」、做 纹理_宽 用 tex、8。
    做 自检_断言值 用 「纹理高度」、做 纹理_高 用 tex、8。
    做 纹理_设像素 用 tex、3、4、0xFF0000。
    做 自检_断言值 用 「纹理像素读写」、做 纹理_取像素 用 tex、3、4、0xFF0000。
    做 自检_断言值 用 「纹理越界返回0」、做 纹理_取像素 用 tex、负 1、0、0。
    做 纹理_设透明色 用 tex、0x000000。
    做 自检_断言值 用 「纹理透明色」、tex 的 第 3 项、0x000000。
    spr 为 做 精灵_创建 用 tex、10、20。
    做 自检_断言值 用 「精灵x」、做 精灵_x 用 spr、10。
    做 自检_断言值 用 「精灵y」、做 精灵_y 用 spr、20。
    做 精灵_设位置 用 spr、50、60。
    做 自检_断言值 用 「精灵设位置x」、做 精灵_x 用 spr、50。
    做 精灵_设图层 用 spr、3。
    做 自检_断言值 用 「精灵图层」、做 精灵_图层 用 spr、3。
    renderer 为 做 批渲染器_创建 用 16、16。
    做 批渲染器_清屏 用 renderer、0x000000。
    做 自检_断言值 用 「清屏后像素」、做 批渲染器_取像素 用 renderer、0、0、0x000000。
    做 纹理_设像素 用 tex、0、0、0xAABBCC。
    spr2 为 做 精灵_创建 用 tex、0、0。
    做 精灵_设源矩形 用 spr2、0、0、1、1。
    做 批渲染器_添加精灵 用 renderer、spr2。
    做 批渲染器_渲染 用 renderer。
    做 自检_断言值 用 「渲染后像素」、做 批渲染器_取像素 用 renderer、0、0、0xAABBCC。
    做 自检_断言值 用 「绘制调用数」、做 批渲染器_绘制调用数 用 renderer、1。
    vp 为 做 视口_创建 用 5、5、10、10。
    做 自检_断言 用 「视口包含(7,7)」、做 视口_包含 用 vp、7、7。
    做 自检_断言 用 「视口不含(3,3)」、做 视口_包含 用 vp、3、3 等于 假。
    spr3 为 做 精灵_创建 用 tex、0、0。
    做 精灵_设翻转 用 spr3、真、假。
    做 自检_断言值 用 「精灵翻转X」、spr3 的 第 11 项、真。
    做 自检_断言值 用 「精灵翻转Y」、spr3 的 第 12 项、假。
    做 精灵_设可见 用 spr3、假。
    做 自检_断言 用 「精灵不可见」、做 精灵_可见 用 spr3 等于 假。
    做 自检_断言值 用 「批渲染精灵数」、做 批渲染器_精灵数 用 renderer、1。
    tileTex 为 做 纹理_创建 用 16、16。
    ts 为 做 图块集_创建 用 tileTex、8、8。
    做 自检_断言值 用 「图块宽=8」、做 图块集_图块宽 用 ts、8。
    做 自检_断言值 用 「图块高=8」、做 图块集_图块高 用 ts、8。
    做 自检_断言值 用 「图块列数=2」、做 图块集_列数 用 ts、2。
    rect0 为 做 图块集_图块矩形 用 ts、0。
    做 自检_断言值 用 「图块0矩形x=0」、rect0 的 第 0 项、0。
    做 自检_断言值 用 「图块0矩形y=0」、rect0 的 第 1 项、0。
    rect1 为 做 图块集_图块矩形 用 ts、1。
    做 自检_断言值 用 「图块1矩形x=8」、rect1 的 第 0 项、8。
    rectN 为 做 图块集_图块矩形 用 ts、负 1。
    做 自检_断言值 用 「无效图块=0」、rectN 的 第 2 项、0。
    layer 为 做 图层_创建 用 「地面」、4、4。
    做 自检_断言值 用 「图层名称」、做 图层_名称 用 layer、「地面」。
    做 自检_断言值 用 「图层宽=4」、做 图层_宽 用 layer、4。
    做 自检_断言值 用 「图层高=4」、做 图层_高 用 layer、4。
    做 自检_断言值 用 「图层可见」、做 图层_可见 用 layer、真。
    做 自检_断言值 用 「初始图块=-1」、做 图层_取图块 用 layer、0、0、负 1。
    做 图层_设图块 用 layer、1、2、5。
    做 自检_断言值 用 「设图块后」、做 图层_取图块 用 layer、1、2、5。
    做 自检_断言值 用 「越界图块=-1」、做 图层_取图块 用 layer、负 1、0、负 1。
    做 图层_设可见 用 layer、假。
    做 自检_断言值 用 「图层隐藏」、做 图层_可见 用 layer、假。
    tmap 为 做 瓦片地图_创建 用 ts。
    做 自检_断言值 用 「地图相机x=0」、做 瓦片地图_相机x 用 tmap、0。
    做 自检_断言值 用 「地图相机y=0」、做 瓦片地图_相机y 用 tmap、0。
    做 瓦片地图_添加图层 用 tmap、layer。
    做 自检_断言值 用 「地图图层数=1」、做 计数 用 做 瓦片地图_图层 用 tmap、1。
    做 瓦片地图_设相机 用 tmap、100、200。
    做 自检_断言值 用 「相机x=100」、做 瓦片地图_相机x 用 tmap、100。
    做 瓦片地图_移动相机 用 tmap、10、负 20。
    做 自检_断言值 用 「移动后x=110」、做 瓦片地图_相机x 用 tmap、110。
    做 自检_断言值 用 「移动后y=180」、做 瓦片地图_相机y 用 tmap、180。
    emitter 为 做 发射器_创建 用 100、200。
    做 自检_断言值 用 「发射器x=100」、emitter 的 第 0 项、100。
    做 自检_断言值 用 「发射器y=200」、emitter 的 第 1 项、200。
    做 发射器_设颜色 用 emitter、0xFF0000。
    做 自检_断言值 用 「发射器颜色」、emitter 的 第 5 项、0xFF0000。
    做 发射器_设重力 用 emitter、20。
    做 自检_断言值 用 「发射器重力=20」、emitter 的 第 7 项、20。
    psys 为 做 粒子系统_创建 用 emitter、100。
    做 自检_断言值 用 「粒子系统活跃=0」、做 粒子系统_活跃数 用 psys、0。
    做 自检_断言值 用 「粒子系统总发射=0」、做 粒子系统_总发射 用 psys、0。
    做 粒子系统_发射 用 psys。
    做 自检_断言 用 「发射后活跃>0」、做 粒子系统_活跃数 用 psys 大于 0。
    做 自检_断言 用 「发射后总计>0」、做 粒子系统_总发射 用 psys 大于 0。
    p1 为 做 粒子_创建 用 10、20、3、负 5、30、0xFFFF00、4。
    做 自检_断言值 用 「粒子x=10」、做 粒子_x 用 p1、10。
    做 自检_断言值 用 「粒子y=20」、做 粒子_y 用 p1、20。
    做 自检_断言 用 「粒子存活」、做 粒子_存活 用 p1。
    做 自检_断言值 用 「粒子颜色」、做 粒子_颜色 用 p1、0xFFFF00。
    做 自检_断言值 用 「粒子大小=4」、做 粒子_大小 用 p1、4。
    做 自检_断言值 用 「粒子透明度=1000」、做 粒子_透明度 用 p1、1000。
    做 粒子_更新 用 p1、10。
    做 自检_断言值 用 「更新后x=13」、做 粒子_x 用 p1、13。
    做 自检_断言值 用 「更新后y=15」、做 粒子_y 用 p1、15。
    做 自检_断言 用 「更新后仍存活」、做 粒子_存活 用 p1。
    做 打印文本 用 做 字符串拼接 用 「[自检] M12 渲染: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M13_音频 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M13 音频模块...」。
    env 为 做 ADSR_创建 用 10、20、700、30。
    做 自检_断言值 用 「ADSR初始阶段」、做 ADSR_阶段 用 env、ADSR_空闲。
    做 ADSR_触发 用 env。
    做 自检_断言值 用 「ADSR触发后阶段」、做 ADSR_阶段 用 env、ADSR_起音。
    可变 i 为 0。
    只要 i 小于 500：
        做 ADSR_步进 用 env、采样率_44100。
        i 为 i 加 1。
    完。
    做 自检_断言 用 「ADSR起音后值>0」、做 ADSR_值 用 env 大于 0。
    做 ADSR_释放开始 用 env。
    做 自检_断言值 用 「ADSR释放阶段」、做 ADSR_阶段 用 env、ADSR_释放。
    i 为 0。
    只要 i 小于 5000：
        做 ADSR_步进 用 env、采样率_44100。
        i 为 i 加 1。
    完。
    做 自检_断言 用 「ADSR释放后值很小」、做 ADSR_值 用 env 小于 100。
    mixer 为 做 混音器_创建 用 采样率_44100、位深_16。
    track 为 做 音轨_创建 用 「测试轨」。
    做 音轨_设数据 用 track、一组：1000、2000、3000、4000。
    做 ADSR_触发 用 做 音轨_ADSR 用 track。
    i 为 0。
    只要 i 小于 500：
        做 ADSR_步进 用 做 音轨_ADSR 用 track、采样率_44100。
        i 为 i 加 1。
    完。
    做 混音器_添加轨道 用 mixer、track。
    output 为 做 混音器_混音 用 mixer、2。
    做 自检_断言值 用 「混音输出长度」、做 计数 用 output、4。
    做 自检_断言 用 「混音左声道有值」、output 的 第 0 项 不等于 0 或者 output 的 第 1 项 不等于 0。
    做 自检_断言值 用 「方波0度=正」、做 波形_方波 用 0、1000、1000。
    做 自检_断言值 用 「方波180度=负」、做 波形_方波 用 18000、1000、0 减 1000。
    做 自检_断言值 用 「三角波0度=0」、做 波形_三角 用 0、1000、0。
    做 自检_断言值 用 「三角波90度=峰值」、做 波形_三角 用 9000、1000、1000。
    做 自检_断言 用 「hz_到_mel(440)>0」、做 hz_到_mel 用 440 大于 0。
    做 自检_断言 用 「mel_到_hz(3000)>0」、做 mel_到_hz 用 3000 大于 0。
    abuf 为 做 音频_创建 用 音频_采样率_44K、音频_16位、1、10。
    做 自检_断言值 用 「音频采样率=44100」、做 音频_采样率 用 abuf、44100。
    做 自检_断言值 用 「音频位深=2」、做 音频_位深 用 abuf、2。
    做 自检_断言值 用 「音频通道=1」、做 音频_通道数 用 abuf、1。
    做 自检_断言值 用 「音频采样数=10」、做 音频_采样数 用 abuf、10。
    做 采样_写 用 abuf、0、1000、0。
    做 自检_断言值 用 「采样读=1000」、做 采样_读 用 abuf、0、0、1000。
    做 采样_写 用 abuf、0、1、负 500。
    做 自检_断言值 用 「采样读负=-500」、做 采样_读 用 abuf、0、1、负 500。
    做 采样_写 用 abuf、0、2、99999。
    做 自检_断言值 用 「钳位32767」、做 采样_读 用 abuf、0、2、32767。
    做 自检_断言值 用 「越界读=0」、做 采样_读 用 abuf、0、99、0。
    abuf2 为 做 音频_创建 用 音频_采样率_44K、音频_16位、1、4。
    做 采样_写 用 abuf2、0、5000、0。
    做 采样_写 用 abuf2、0、1、3000。
    做 音频_静音 用 abuf2。
    做 自检_断言值 用 「静音后=0」、做 采样_读 用 abuf2、0、0、0。
    abuf3 为 做 音频_创建 用 音频_采样率_44K、音频_16位、1、4。
    做 采样_写 用 abuf3、0、100、0。
    做 采样_写 用 abuf3、0、1、负 200。
    做 采样_写 用 abuf3、0、2、300。
    做 采样_写 用 abuf3、0、3、负 50。
    做 自检_断言值 用 「峰值=300」、做 音频_峰值 用 abuf3、300。
    做 自检_断言 用 「RMS>0」、做 音频_RMS 用 abuf3 大于 0。
    做 自检_断言值 用 「锯齿0度=负」、做 波形_锯齿 用 0、1000、0 减 1000。
    做 自检_断言 用 「锯齿18000≈0」、做 波形_锯齿 用 18000、1000 大于或等于 负 10 并且 做 波形_锯齿 用 18000、1000 小于或等于 10。
    做 自检_断言值 用 「正弦0度=0」、做 波形_正弦 用 0、1000、0。
    做 自检_断言 用 「正弦90度>0」、做 波形_正弦 用 9000、1000 大于 0。
    做 自检_断言值 用 「音频_8位=1」、音频_8位、1。
    做 自检_断言值 用 「音频_16位=2」、音频_16位、2。
    做 自检_断言值 用 「音频_32位=4」、音频_32位、4。
    做 自检_断言值 用 「采样率8K」、音频_采样率_8K、8000。
    做 自检_断言值 用 「采样率48K」、音频_采样率_48K、48000。
    做 打印文本 用 做 字符串拼接 用 「[自检] M13 音频: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M14_引擎 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M14 游戏引擎模块...」。
    a 为 做 AABB_创建 用 0、0、10、10。
    b 为 做 AABB_创建 用 5、5、10、10。
    c 为 做 AABB_创建 用 20、20、5、5。
    做 自检_断言 用 「AABB相交(重叠)」、做 AABB_相交 用 a、b。
    做 自检_断言 用 「AABB不交(远离)」、做 AABB_相交 用 a、c 等于 假。
    做 自检_断言 用 「AABB包含点(5,5)」、做 AABB_包含点 用 a、5、5。
    做 自检_断言 用 「AABB不含点(15,15)」、做 AABB_包含点 用 a、15、15 等于 假。
    merged 为 做 AABB_合并 用 a、b。
    做 自检_断言值 用 「合并x」、做 包围盒_x 用 merged、0。
    做 自检_断言值 用 「合并宽」、做 AABB_宽 用 merged、15。
    做 自检_断言值 用 「合并高」、做 AABB_高 用 merged、15。
    c1 为 做 圆形_创建 用 0、0、5。
    c2 为 做 圆形_创建 用 8、0、5。
    c3 为 做 圆形_创建 用 100、100、3。
    做 自检_断言 用 「圆形相交(重叠)」、做 圆形_相交 用 c1、c2。
    做 自检_断言 用 「圆形不交(远离)」、做 圆形_相交 用 c1、c3 等于 假。
    做 自检_断言 用 「圆形AABB相交」、做 圆形AABB_相交 用 c1、a。
    world 为 做 碰撞世界_创建。
    体A 为 做 碰撞体_创建 用 碰撞体_AABB、做 AABB_创建 用 0、0、10、10。
    体B 为 做 碰撞体_创建 用 碰撞体_AABB、做 AABB_创建 用 5、5、10、10。
    体C 为 做 碰撞体_创建 用 碰撞体_AABB、做 AABB_创建 用 100、100、5、5。
    做 碰撞世界_添加 用 world、体A。
    做 碰撞世界_添加 用 world、体B。
    做 碰撞世界_添加 用 world、体C。
    做 碰撞世界_检测 用 world。
    做 自检_断言值 用 「碰撞对数」、做 碰撞世界_碰撞对数 用 world、1。
    做 自检_断言值 用 「事件数」、做 计数 用 做 碰撞世界_事件 用 world、1。
    若 做 计数 用 做 碰撞世界_事件 用 world 大于 0：
        做 自检_断言值 用 「事件类型=进入」、做 碰撞世界_事件 用 world 的 第 0 项 的 第 2 项、事件_进入。
    完。
    做 碰撞世界_检测 用 world。
    若 做 计数 用 做 碰撞世界_事件 用 world 大于 0：
        做 自检_断言值 用 「第二帧事件=持续」、做 碰撞世界_事件 用 world 的 第 0 项 的 第 2 项、事件_持续。
    完。
    inp 为 做 输入_创建。
    做 输入_按键按下 用 inp、输入键_A。
    做 自检_断言 用 「按键A按下」、做 输入_是否按下 用 inp、输入键_A。
    做 自检_断言 用 「按键B未按」、做 输入_是否按下 用 inp、输入键_B 等于 假。
    做 输入_按键释放 用 inp、输入键_A。
    做 自检_断言 用 「按键A释放」、做 输入_是否按下 用 inp、输入键_A 等于 假。
    做 输入映射_默认。
    做 输入_按键按下 用 inp、输入键_空格。
    做 自检_断言 用 「映射跳跃」、做 输入映射_检查 用 inp、「跳跃」。
    sm 为 做 状态机_创建 用 「菜单」。
    做 自检_断言值 用 「状态机初始=菜单」、做 状态机_当前 用 sm、「菜单」。
    做 自检_断言值 用 「状态机历史=空」、做 计数 用 做 状态机_历史 用 sm、0。
    做 状态机_添加状态 用 sm、「菜单」。
    做 状态机_添加状态 用 sm、「游戏中」。
    做 状态机_添加状态 用 sm、「暂停」。
    做 状态机_添加状态 用 sm、「结算」。
    做 状态机_添加转移 用 sm、「菜单」、「开始」、「游戏中」。
    做 状态机_添加转移 用 sm、「游戏中」、「暂停」、「暂停」。
    做 状态机_添加转移 用 sm、「暂停」、「继续」、「游戏中」。
    做 状态机_添加转移 用 sm、「游戏中」、「结束」、「结算」。
    做 状态机_添加转移 用 sm、「结算」、「返回」、「菜单」。
    做 自检_断言值 用 「触发开始=成功」、做 状态机_触发 用 sm、「开始」、1。
    做 自检_断言值 用 「状态=游戏中」、做 状态机_当前 用 sm、「游戏中」。
    做 自检_断言值 用 「历史=1」、做 计数 用 做 状态机_历史 用 sm、1。
    做 自检_断言值 用 「触发暂停=成功」、做 状态机_触发 用 sm、「暂停」、1。
    做 自检_断言值 用 「状态=暂停」、做 状态机_当前 用 sm、「暂停」。
    做 自检_断言值 用 「触发继续=成功」、做 状态机_触发 用 sm、「继续」、1。
    做 自检_断言值 用 「恢复游戏中」、做 状态机_当前 用 sm、「游戏中」。
    做 自检_断言值 用 「触发无效=失败」、做 状态机_触发 用 sm、「不存在事件」、0。
    做 自检_断言值 用 「可触发结束=1」、做 状态机_可触发 用 sm、「结束」、1。
    做 自检_断言值 用 「不可触发返回=0」、做 状态机_可触发 用 sm、「返回」、0。
    可用 为 做 状态机_可用事件 用 sm。
    做 自检_断言 用 「可用事件>0」、做 计数 用 可用 大于 0。
    做 状态机_重置 用 sm、「菜单」。
    做 自检_断言值 用 「重置后=菜单」、做 状态机_当前 用 sm、「菜单」。
    做 自检_断言值 用 「重置后历史=0」、做 计数 用 做 状态机_历史 用 sm、0。
    做 打印文本 用 做 字符串拼接 用 「[自检] M14 引擎: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M15_宿主 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M15 宿主适配模块...」。
    做 发布器_重置。
    做 发布器_从编译器错误 用 「test.nova」、10、「未定义变量」。
    做 发布器_从编译器警告 用 「test.nova」、20、「未使用变量」。
    做 发布器_从词法错误 用 「test.nova」、5、3、「非法字符」。
    做 自检_断言值 用 「诊断总数」、做 发布器_总数、3。
    做 自检_断言值 用 「错误数」、做 发布器_错误数、2。
    做 自检_断言值 用 「警告数」、做 发布器_警告数、1。
    做 自检_断言 用 「有错误」、做 发布器_有错误。
    file_diags 为 做 发布器_文件诊断 用 「test.nova」。
    做 自检_断言值 用 「文件诊断数」、做 计数 用 file_diags、3。
    lsp 为 做 LSP服务器_创建。
    做 自检_断言 用 「LSP未初始化」、做 LSP服务器_已初始化 用 lsp 等于 假。
    init_msg 为 做 LSP消息_创建 用 1、「initialize」、一组：（空）。
    做 自检_断言值 用 「LSP初始化方法」、做 LSP消息_方法 用 init_msg、「initialize」。
    做 自检_断言 用 「LSP初始化是请求」、做 LSP消息_是请求 用 init_msg。
    init_resp 为 做 LSP服务器_处理 用 init_msg、lsp。
    做 自检_断言 用 「LSP初始化后」、做 LSP服务器_已初始化 用 lsp。
    做 自检_断言值 用 「LSP初始化响应类型」、init_resp 的 第 0 项、1。
    open_msg 为 做 LSP消息_创建 用 空、「textDocument/didOpen」、一组：「file:///test.nova」、「函数 测试() {}」。
    做 自检_断言值 用 「LSP打开方法」、做 LSP消息_方法 用 open_msg、「textDocument/didOpen」。
    做 自检_断言值 用 「LSP打开URI」、做 LSP消息_参数 用 open_msg 的 第 0 项、「file:///test.nova」。
    做 LSP服务器_处理 用 open_msg、lsp。
    内容 为 做 LSP服务器_获取文件 用 lsp、「file:///test.nova」。
    做 自检_断言值 用 「LSP文件内容精确」、内容、「函数 测试() {}」。
    diag 为 做 LSP诊断_创建 用 10、5、1、「测试错误」。
    做 LSP服务器_发布诊断 用 lsp、「file:///test.nova」、一组：diag。
    diags 为 做 LSP服务器_获取诊断 用 lsp、「file:///test.nova」。
    做 自检_断言值 用 「LSP诊断数」、做 计数 用 diags、1。
    做 自检_断言值 用 「LSP诊断消息」、diags 的 第 0 项 的 第 5 项、「测试错误」。
    shutdown_msg 为 做 LSP消息_创建 用 2、「shutdown」、一组：（空）。
    做 LSP服务器_处理 用 shutdown_msg、lsp。
    做 自检_断言 用 「LSP关闭后」、做 LSP服务器_已初始化 用 lsp 等于 假。
    d1 为 做 诊断_创建 用 「foo.nova」、42、7、0、「undefined var」、来源_语义。
    做 自检_断言值 用 「诊断文件」、做 诊断_文件 用 d1、「foo.nova」。
    做 自检_断言值 用 「诊断行」、做 诊断_行 用 d1、42。
    做 自检_断言值 用 「诊断列」、做 诊断_列 用 d1、7。
    做 自检_断言值 用 「诊断级别=错误」、做 诊断_级别 用 d1、0。
    做 自检_断言值 用 「诊断消息」、做 诊断_消息 用 d1、「undefined var」。
    做 自检_断言值 用 「诊断来源=语义」、做 诊断_来源 用 d1、来源_语义。
    做 诊断_设代码 用 d1、「E100」。
    做 自检_断言值 用 「诊断代码」、做 诊断_代码 用 d1、「E100」。
    做 诊断_设修复 用 d1、「添加声明」。
    做 自检_断言值 用 「诊断修复」、做 诊断_修复 用 d1、「添加声明」。
    做 自检_断言值 用 「来源名_词法」、做 来源名 用 来源_词法、「词法」。
    做 自检_断言值 用 「来源名_语法」、做 来源名 用 来源_语法、「语法」。
    做 自检_断言值 用 「来源名_语义」、做 来源名 用 来源_语义、「语义」。
    做 自检_断言值 用 「来源名_借用」、做 来源名 用 来源_借用、「借用」。
    做 自检_断言值 用 「来源名_效果」、做 来源名 用 来源_效果、「效果」。
    做 自检_断言值 用 「来源名_链接」、做 来源名 用 来源_链接、「链接」。
    做 发布器_重置。
    做 发布器_从语法错误 用 「a.nova」、1、1、「缺少分号」。
    做 发布器_从借用违规 用 「a.nova」、5、「已移动」。
    做 发布器_从效果违规 用 「a.nova」、8、「IO副作用」。
    做 自检_断言值 用 「多类型总数=3」、做 发布器_总数、3。
    做 自检_断言值 用 「多类型错误=3」、做 发布器_错误数、3。
    做 自检_断言值 用 「多类型警告=0」、做 发布器_警告数、0。
    做 自检_断言值 用 「来源_词法=0」、来源_词法、0。
    做 自检_断言值 用 「来源_语法=1」、来源_语法、1。
    做 自检_断言值 用 「来源_类型=3」、来源_类型、3。
    做 自检_断言值 用 「来源_优化=6」、来源_优化、6。
    做 打印文本 用 做 字符串拼接 用 「[自检] M15 宿主: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M16_AI 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M16 智能推理模块...」。
    va 为 一组：100、200、300。
    vb 为 一组：400、500、600。
    做 自检_断言值 用 「向量点积」、做 向量_点积 用 va、vb、0、320000。
    vs 为 做 向量_缩放 用 va、2。
    做 自检_断言值 用 「向量缩放[0]」、vs 的 第 0 项、200。
    做 自检_断言值 用 「向量缩放[2]」、vs 的 第 2 项、600。
    vadd 为 做 向量_加 用 va、vb。
    做 自检_断言值 用 「向量加[1]」、vadd 的 第 1 项、700。
    scores 为 一组：1000、2000、500。
    probs 为 做 softmax_近似 用 scores。
    做 自检_断言值 用 「softmax长度」、做 计数 用 probs、3。
    做 自检_断言 用 「softmax最大值概率最高」、probs 的 第 1 项 大于 probs 的 第 0 项。
    做 自检_断言 用 「softmax最大值>最小值」、probs 的 第 1 项 大于 probs 的 第 2 项。
    prob_sum 为 probs 的 第 0 项 加 probs 的 第 1 项 加 probs 的 第 2 项。
    做 自检_断言 用 「softmax和≈1000」、prob_sum 大于或等于 950 并且 prob_sum 小于或等于 1050。
    Q 为 一组：一组：100、200、一组：300、400。
    K 为 一组：一组：100、200、一组：300、400。
    V 为 一组：一组：10、20、一组：30、40。
    attn_out 为 做 注意力_单头 用 Q、K、V、2。
    做 自检_断言值 用 「注意力输出序列长」、做 计数 用 attn_out、2。
    做 自检_断言值 用 「注意力输出维度」、做 计数 用 attn_out 的 第 0 项、2。
    做 自检_断言 用 「注意力输出非零」、attn_out 的 第 0 项 的 第 0 项 不等于 0 或者 attn_out 的 第 0 项 的 第 1 项 不等于 0。
    cache 为 做 KV缓存_创建 用 10、0。
    做 KV缓存_追加 用 cache、一组：100、200、一组：300、400、0。
    做 KV缓存_追加 用 cache、一组：500、600、一组：700、800、0。
    做 自检_断言值 用 「KV缓存长度」、做 KV缓存_长度 用 cache、2。
    做 自检_断言值 用 「KV缓存K[0][0]」、做 KV缓存_K列表 用 cache 的 第 0 项 的 第 0 项、100。
    gamma 为 一组：1000、1000、1000。
    beta 为 一组：0。
    norm_in 为 一组：100、200、300。
    norm_out 为 做 层归一化 用 norm_in、gamma、beta。
    做 自检_断言值 用 「层归一化长度」、做 计数 用 norm_out、3。
    norm_mean 为 (norm_out 的 第 0 项 加 norm_out 的 第 1 项 加 norm_out 的 第 2 项) 除以 3。
    做 自检_断言 用 「层归一化均值接近0」、norm_mean 大于或等于 负 100 并且 norm_mean 小于或等于 100。
    relu_out 为 做 激活_修正线性 用 一组：负 100、0、100、200。
    做 自检_断言值 用 「ReLU负值」、relu_out 的 第 0 项、0。
    做 自检_断言值 用 「ReLU零值」、relu_out 的 第 1 项、0。
    做 自检_断言值 用 「ReLU正值」、relu_out 的 第 2 项、100。
    residual 为 做 残差_加 用 一组：100、200、一组：10、20。
    做 自检_断言值 用 「残差[0]」、residual 的 第 0 项、110。
    做 自检_断言值 用 「残差[1]」、residual 的 第 1 项、220。
    pe 为 做 位置编码_生成 用 4、4。
    做 自检_断言值 用 「位置编码seq_len」、做 计数 用 pe、4。
    做 自检_断言值 用 「位置编码d_model」、做 计数 用 pe 的 第 0 项、4。
    做 自检_断言值 用 「位置编码[0][0]」、pe 的 第 0 项 的 第 0 项、0。
    neuron 为 做 神经元_创建 用 8000。
    做 自检_断言值 用 「神经元初始电位=0」、neuron 的 第 0 项、0。
    做 自检_断言值 用 「神经元阈值=8000」、neuron 的 第 1 项、8000。
    r1 为 做 神经元_更新 用 neuron、1000、1。
    做 自检_断言值 用 「弱输入不发放」、r1、0。
    做 自检_断言 用 「膜电位累积>0」、neuron 的 第 0 项 大于 0。
    r2 为 做 神经元_更新 用 neuron、50000、2。
    做 自检_断言值 用 「强输入发放=10000」、r2、10000。
    做 自检_断言值 用 「发放后重置」、neuron 的 第 0 项、0。
    做 自检_断言值 用 「发放计数=1」、neuron 的 第 4 项、1。
    r3 为 做 神经元_更新 用 neuron、50000、3。
    做 自检_断言值 用 「不应期不发放」、r3、0。
    syn 为 做 突触_创建 用 0、1、5000、1、突触_兴奋。
    做 自检_断言值 用 「突触无脉冲=0」、做 突触_传递 用 syn、0、0、0。
    signal 为 做 突触_传递 用 syn、10000、0。
    做 自检_断言值 用 「突触传递=5000」、signal、5000。
    inh 为 做 突触_创建 用 0、1、3000、1、突触_抑制。
    inh_sig 为 做 突触_传递 用 inh、10000、0。
    做 自检_断言 用 「抑制信号<0」、inh_sig 小于 0。
    做 规则_清空。
    做 事实_清空。
    做 自检_断言值 用 「规则初始=0」、做 规则_数量、0。
    做 自检_断言值 用 「事实初始=0」、做 事实_数量、0。
    做 规则_追加 用 「下雨」、「地湿」、90、推理_演绎。
    做 规则_追加 用 「地湿」、「滑」、80、推理_演绎。
    做 自检_断言值 用 「规则=2」、做 规则_数量、2。
    做 事实_追加 用 「下雨」、100。
    做 自检_断言值 用 「事实=1」、做 事实_数量、1。
    做 自检_断言 用 「事实存在」、做 事实_存在 用 「下雨」。
    做 自检_断言值 用 「事实置信度=100」、做 事实_查询 用 「下雨」、100。
    做 自检_断言 用 「事实不存在」、做 事实_存在 用 「地湿」 等于 假。
    推出 为 做 演绎_推理。
    做 自检_断言 用 「演绎推出>=1」、推出 大于或等于 1。
    做 自检_断言 用 「推出地湿」、做 事实_存在 用 「地湿」。
    做 自检_断言 用 「推出滑」、做 事实_存在 用 「滑」。
    推出2 为 做 演绎_推理。
    做 自检_断言值 用 「无新推出=0」、推出2、0。
    做 自检_断言值 用 「推理_演绎=0」、推理_演绎、0。
    做 自检_断言值 用 「推理_归纳=1」、推理_归纳、1。
    做 自检_断言值 用 「推理_类比=2」、推理_类比、2。
    做 自检_断言值 用 「推理_溯因=3」、推理_溯因、3。
    做 打印文本 用 做 字符串拼接 用 「[自检] M16 AI: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M9_内存 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M9 内存模块...」。
    做 mem_重置计数器。
    做 mem_记录分配 用 128。
    做 mem_记录分配 用 512。
    做 mem_记录分配 用 64。
    做 自检_断言值 用 「当前字节」、_mem_当前字节、704。
    做 自检_断言值 用 「峰值字节」、_mem_峰值字节、704。
    做 自检_断言值 用 「分配次数」、_mem_分配次数、3。
    做 mem_记录释放 用 128。
    做 自检_断言值 用 「释放后当前」、_mem_当前字节、576。
    做 自检_断言值 用 「峰值不变」、_mem_峰值字节、704。
    frag 为 做 mem_碎片率。
    做 自检_断言 用 「碎片率>0」、frag 大于 0。
    做 自检_断言 用 「碎片率<100」、frag 小于 100。
    做 自检_断言 用 「极低内存合规」、做 mem_是极低内存profile。
    做 自检_断言值 用 「64B桶计数」、_mem_桶计数 的 第 0 项、1。
    做 自检_断言值 用 「256B桶计数」、_mem_桶计数 的 第 1 项、1。
    做 自检_断言值 用 「1KB桶计数」、_mem_桶计数 的 第 2 项、1。
    pool 为 做 内存池_创建。
    alloc1 为 做 内存池_分配 用 pool、32。
    做 自检_断言 用 「池分配成功」、alloc1 的 第 0 项 大于或等于 0。
    做 自检_断言值 用 「池分配级别」、alloc1 的 第 1 项、0。
    alloc2 为 做 内存池_分配 用 pool、200。
    做 自检_断言值 用 「池256B级」、alloc2 的 第 1 项、1。
    做 自检_断言 用 「池已用>0」、做 内存池_总已用 用 pool 大于 0。
    用前 为 做 内存池_总已用 用 pool。
    做 内存池_释放 用 pool、alloc1 的 第 0 项、alloc1 的 第 1 项。
    做 自检_断言 用 「释放后已用降低」、做 内存池_总已用 用 pool 小于 用前。
    做 内存池_重置 用 pool。
    做 自检_断言值 用 「重置后利用率」、做 内存池_利用率 用 pool、0。
    slab 为 做 Slab_创建 用 128、8。
    做 自检_断言值 用 「Slab块大小=128」、做 Slab_块大小 用 slab、128。
    做 自检_断言值 用 「Slab总块=8」、做 Slab_总块数 用 slab、8。
    做 自检_断言值 用 「Slab已分配=0」、做 Slab_已分配 用 slab、0。
    做 自检_断言值 用 「Slab空闲=8」、做 Slab_空闲数 用 slab、8。
    做 自检_断言值 用 「Slab水位=0」、做 Slab_水位线 用 slab、0。
    做 自检_断言值 用 「Slab利用率=0」、做 Slab_利用率 用 slab、0。
    b1 为 做 Slab_分配 用 slab。
    做 自检_断言 用 「Slab分配成功」、b1 大于或等于 0。
    做 自检_断言值 用 「Slab已分配=1」、做 Slab_已分配 用 slab、1。
    做 自检_断言值 用 「Slab水位=1」、做 Slab_水位线 用 slab、1。
    b2 为 做 Slab_分配 用 slab。
    做 Slab_分配 用 slab。
    做 自检_断言值 用 「Slab已分配=3」、做 Slab_已分配 用 slab、3。
    做 自检_断言值 用 「Slab空闲=5」、做 Slab_空闲数 用 slab、5。
    做 自检_断言值 用 「Slab水位=3」、做 Slab_水位线 用 slab、3。
    做 Slab_释放 用 slab、b2。
    做 自检_断言值 用 「释放后已分配=2」、做 Slab_已分配 用 slab、2。
    做 自检_断言值 用 「释放后空闲=6」、做 Slab_空闲数 用 slab、6。
    做 自检_断言值 用 「水位不降=3」、做 Slab_水位线 用 slab、3。
    做 自检_断言值 用 「32B→级0」、做 _内存池_选级 用 32、0。
    做 自检_断言值 用 「64B→级0」、做 _内存池_选级 用 64、0。
    做 自检_断言值 用 「100B→级1」、做 _内存池_选级 用 100、1。
    做 自检_断言值 用 「256B→级1」、做 _内存池_选级 用 256、1。
    做 自检_断言值 用 「500B→级2」、做 _内存池_选级 用 500、2。
    做 自检_断言值 用 「2000B→级3」、做 _内存池_选级 用 2000、3。
    做 自检_断言值 用 「5000B→-1」、做 _内存池_选级 用 5000、负 1。
    pool2 为 做 内存池_创建。
    做 自检_断言 用 「池总容量>0」、做 内存池_总容量 用 pool2 大于 0。
    做 自检_断言值 用 「池初始已用=0」、做 内存池_总已用 用 pool2、0。
    做 自检_断言值 用 「对齐8(1)=8」、做 对齐8 用 1、8。
    做 自检_断言值 用 「对齐8(8)=8」、做 对齐8 用 8、8。
    做 自检_断言值 用 「对齐8(9)=16」、做 对齐8 用 9、16。
    做 自检_断言值 用 「幂2(0)=1」、做 幂2 用 0、1。
    做 自检_断言值 用 「幂2(3)=8」、做 幂2 用 3、8。
    做 自检_断言值 用 「幂2(10)=1024」、做 幂2 用 10、1024。
    做 打印文本 用 做 字符串拼接 用 「[自检] M9 内存: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M8_ABI 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M8 多目标ABI模块...」。
    t_x86 为 做 三元组_创建 用 目标_X86_64、系统_Linux、「SysV」。
    做 自检_断言值 用 「x86三元组描述」、做 三元组_描述 用 t_x86、「x86_64-linux-gnu」。
    做 自检_断言值 用 「x86架构」、做 三元组_架构 用 t_x86、目标_X86_64。
    t_arm 为 做 三元组_创建 用 目标_ARM64、系统_Linux、「AAPCS64」。
    做 自检_断言值 用 「arm64描述」、做 三元组_描述 用 t_arm、「aarch64-linux-gnu」。
    t_rv 为 做 三元组_创建 用 目标_RISCV64、系统_Linux、「LP64D」。
    做 自检_断言值 用 「riscv64描述」、做 三元组_描述 用 t_rv、「riscv64-linux-gnu」。
    t_wasm 为 做 三元组_创建 用 目标_WASM32、系统_WASI、「WASM」。
    做 自检_断言值 用 「wasm描述」、做 三元组_描述 用 t_wasm、「wasm32-wasi」。
    cfg 为 做 目标配置_创建 用 t_x86。
    做 自检_断言值 用 「x86指针宽度」、做 目标_指针宽度 用 cfg、8。
    做 自检_断言值 用 「x86栈对齐」、做 目标_栈对齐 用 cfg、16。
    做 自检_断言值 用 「x86参数寄存器」、做 目标_参数寄存器数 用 cfg、6。
    做 自检_断言值 用 「x86 ELF机器号」、做 目标_ELF机器号 用 目标_X86_64、62。
    做 自检_断言值 用 「arm64 ELF机器号」、做 目标_ELF机器号 用 目标_ARM64、183。
    做 自检_断言值 用 「riscv64 ELF机器号」、做 目标_ELF机器号 用 目标_RISCV64、243。
    x86_regs 为 做 目标_参数寄存器 用 目标_X86_64。
    做 自检_断言值 用 「x86参数寄存器数」、做 计数 用 x86_regs、6。
    arm_regs 为 做 目标_参数寄存器 用 目标_ARM64。
    做 自检_断言值 用 「arm64参数寄存器数」、做 计数 用 arm_regs、8。
    做 自检_断言值 用 「x86 syscall指令」、做 目标_syscall指令 用 目标_X86_64、「syscall」。
    做 自检_断言值 用 「arm64 syscall指令」、做 目标_syscall指令 用 目标_ARM64、「svc #0」。
    做 自检_断言值 用 「riscv64 syscall指令」、做 目标_syscall指令 用 目标_RISCV64、「ecall」。
    做 自检_断言 用 「x86有SIMD」、做 目标_有SIMD 用 目标_X86_64。
    做 自检_断言 用 「arm64有SIMD」、做 目标_有SIMD 用 目标_ARM64。
    做 自检_断言 用 「x86有原子」、做 目标_有原子 用 目标_X86_64。
    做 自检_断言 用 「x86有加密」、做 目标_有加密 用 目标_X86_64。
    t_from_profile 为 做 目标_从profile 用 「linux-arm64」。
    做 自检_断言值 用 「profile→arm64」、做 三元组_架构 用 t_from_profile、目标_ARM64。
    做 自检_断言值 用 「高性能=x86」、做 目标_自动选择 用 「高性能计算」、目标_x86_64。
    做 自检_断言值 用 「移动端=arm64」、做 目标_自动选择 用 「移动端」、目标_arm64。
    做 自检_断言值 用 「嵌入式=riscv」、做 目标_自动选择 用 「嵌入式」、目标_riscv64。
    做 自检_断言值 用 「浏览器=wasm」、做 目标_自动选择 用 「浏览器」、目标_wasm。
    做 自检_断言值 用 「国产化=loongarch」、做 目标_自动选择 用 「国产化」、目标_loongarch。
    做 自检_断言值 用 「同目标兼容=100」、做 目标_兼容 用 目标_x86_64、目标_x86_64、100。
    做 自检_断言值 用 「x86→wasm=70」、做 目标_兼容 用 目标_x86_64、目标_wasm、70。
    做 自检_断言值 用 「x86→arm64=50」、做 目标_兼容 用 目标_x86_64、目标_arm64、50。
    做 自检_断言值 用 「x86整数性能=100」、做 目标_性能预测 用 「整数运算」、100。
    做 自检_断言值 用 「x86浮点性能=100」、做 目标_性能预测 用 「浮点运算」、100。
    做 自检_断言值 用 「RAX=0」、寄_RAX、0。
    做 自检_断言值 用 「RSP=4」、寄_RSP、4。
    做 自检_断言值 用 「R15=15」、寄_R15、15。
    做 自检_断言值 用 「ARM X0寄存器=0」、X0、0。
    做 自检_断言值 用 「ARM LR=30」、LR、30。
    做 自检_断言值 用 「RV A0=10」、RV_参数0、10。
    做 自检_断言值 用 「RV SP=2」、RV_SP、2。
    做 打印文本 用 做 字符串拼接 用 「[自检] M8 ABI: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M10_JIT 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M10 JIT/AOT模块...」。
    做 JIT_配置 用 0x200000、8192。
    cfg 为 做 JIT_读取配置。
    做 自检_断言值 用 「JIT缓冲大小」、cfg 的 第 0 项、0x200000。
    做 自检_断言值 用 「JIT最大编译单元」、cfg 的 第 1 项、8192。
    做 JIT_配置 用 0x100000、4096。
    jit 为 做 JIT编译器_创建。
    做 自检_断言 用 「JIT实例非空」、jit 不等于 空。
    做 自检_断言值 用 「JIT初始代码位」、做 JIT编译器_代码位 用 jit、0。
    做 自检_断言值 用 「JIT初始使用率」、做 JIT编译器_使用率 用 jit、0。
    做 JIT_发射 用 jit、0x48。
    做 JIT_发射 用 jit、0xB8。
    做 JIT_发射64 用 jit、42。
    做 JIT_返回 用 jit。
    做 自检_断言值 用 「JIT发射后代码位」、做 JIT编译器_代码位 用 jit、11。
    做 JIT_记录编译 用 64。
    做 JIT_记录编译 用 128。
    效率 为 做 JIT_效率评估。
    做 自检_断言 用 「JIT效率非空」、效率 不等于 空。
    jit2 为 做 JIT编译器_创建。
    做 JIT_载入立即 用 jit2、100。
    做 JIT_载入立即 用 jit2、200。
    做 JIT_加 用 jit2。
    做 JIT_返回 用 jit2。
    做 自检_断言 用 「JIT加法序列>0」、做 JIT编译器_代码位 用 jit2 大于 0。
    jit3 为 做 JIT编译器_创建。
    做 JIT_载入立即 用 jit3、50。
    做 JIT_载入立即 用 jit3、10。
    做 JIT_减 用 jit3。
    做 自检_断言 用 「JIT减法序列>0」、做 JIT编译器_代码位 用 jit3 大于 0。
    做 JIT_载入立即 用 jit3、6。
    做 JIT_载入立即 用 jit3、7。
    做 JIT_乘 用 jit3。
    做 自检_断言 用 「JIT乘法序列>0」、做 JIT编译器_代码位 用 jit3 大于 20。
    jit4 为 做 JIT编译器_创建。
    做 JIT_载入立即 用 jit4、42。
    做 JIT_返回 用 jit4。
    做 自检_断言值 用 「JIT-mov+ret字节数」、做 JIT编译器_代码位 用 jit4、11。
    做 JIT编译器_创建。
    做 自检_断言 用 「AOT初始未编译」、做 AOT_已编译 等于 假。
    覆盖 为 做 JIT覆盖率。
    做 自检_断言 用 「JIT覆盖率=86/86」、覆盖 等于 100。
    做 执行路径_设置 用 执行路径_AOT。
    做 自检_断言值 用 「执行路径AOT」、做 执行路径_获取、执行路径_AOT。
    做 自检_断言 用 「路径名AOT」、做 执行路径名 用 执行路径_AOT 不等于 「unknown」。
    做 执行路径_设置 用 执行路径_解释。
    jit6 为 做 JIT编译器_创建。
    做 JIT_载入立即 用 jit6、99。
    做 JIT_返回 用 jit6。
    exec_ret 为 做 执行缓冲码 用 jit6 的 第 0 项、0。
    做 自检_断言 用 「执行缓冲码返回(stub)」、exec_ret 等于 0 或者 exec_ret 等于 99。
    det 为 做 热点检测器_创建。
    做 自检_断言 用 「检测器非空」、det 不等于 空。
    s1 为 做 热点检测器_计数 用 det、100、1。
    做 自检_断言值 用 「首次计数=冷」、s1、热点_冷。
    rec1 为 做 热点检测器_查询 用 det、100。
    做 自检_断言值 用 「记录计数=1」、做 热点_计数 用 rec1、1。
    做 自检_断言值 用 「记录状态=冷」、做 热点_状态 用 rec1、热点_冷。
    做 自检_断言 用 「未编译」、做 热点_已编译 用 rec1 等于 假。
    可变 ci 为 1。
    只要 ci 小于 JIT_热点阈值：
        做 热点检测器_计数 用 det、100、ci 加 1。
        ci 为 ci 加 1。
    完。
    rec2 为 做 热点检测器_查询 用 det、100。
    做 自检_断言值 用 「达阈值=温」、做 热点_状态 用 rec2、热点_温。
    task 为 做 热点检测器_取编译任务 用 det。
    做 自检_断言值 用 「取编译任务=100」、task、100。
    task2 为 做 热点检测器_取编译任务 用 det。
    做 自检_断言值 用 「队列空=-1」、task2、负 1。
    做 热点检测器_标记编译完成 用 det、100、0x1000。
    rec3 为 做 热点检测器_查询 用 det、100。
    做 自检_断言值 用 「编译后=热」、做 热点_状态 用 rec3、热点_热。
    做 自检_断言 用 「compiled」、做 热点_已编译 用 rec3。
    做 自检_断言 用 「应JIT执行」、做 热点检测器_应JIT执行 用 det、100。
    做 自检_断言 用 「不应JIT执行(新块)」、做 热点检测器_应JIT执行 用 det、999 等于 假。
    做 自检_断言值 用 「热点_冷=0」、热点_冷、0。
    做 自检_断言值 用 「热点_温=1」、热点_温、1。
    做 自检_断言值 用 「热点_热=2」、热点_热、2。
    做 自检_断言值 用 「热点_超热=3」、热点_超热、3。
    做 自检_断言值 用 「JIT_模式_快速=0」、JIT_模式_快速、0。
    做 自检_断言值 用 「JIT_模式_标准=1」、JIT_模式_标准、1。
    做 自检_断言值 用 「JIT_模式_激进=2」、JIT_模式_激进、2。
    做 打印文本 用 做 字符串拼接 用 「[自检] M10 JIT: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M7_语义 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M7 语义分析模块...」。
    st 为 做 符号表_创建 用 64。
    做 自检_断言 用 「符号表非空」、st 不等于 空。
    做 自检_断言值 用 「符号表初始大小」、做 符号表_符号表_大小 用 st、0。
    ok1 为 做 符号表_追加 用 st、「x」、符号_变量、型_整数、0。
    做 自检_断言值 用 「追加成功」、ok1、1。
    做 自检_断言值 用 「追加后大小=1」、做 符号表_符号表_大小 用 st、1。
    ok2 为 做 符号表_追加 用 st、「f」、符号_函数、型_函数、0。
    做 自检_断言值 用 「追加函数」、ok2、1。
    做 自检_断言值 用 「追加后大小=2」、做 符号表_符号表_大小 用 st、2。
    sym 为 做 符号_创建 用 「y」、符号_参数、型_字符串、1。
    做 自检_断言值 用 「符号名称」、做 符号_名称 用 sym、「y」。
    做 自检_断言值 用 「符号类型=参数」、做 符号_类型 用 sym、符号_参数。
    做 自检_断言值 用 「符号数据类型」、做 符号_数据类型 用 sym、型_字符串。
    做 自检_断言值 用 「符号深度」、做 符号_深度 用 sym、1。
    做 符号_设偏移 用 sym、16。
    做 自检_断言值 用 「符号偏移」、做 符号_偏移 用 sym、16。
    st2 为 做 符号表_创建 用 64。
    做 符号表_追加 用 st2、「a」、符号_变量、型_整数、0。
    做 符号表_追加 用 st2、「b」、符号_变量、型_整数、1。
    做 符号表_追加 用 st2、「c」、符号_变量、型_整数、1。
    做 自检_断言值 用 「清除前大小=3」、做 符号表_符号表_大小 用 st2、3。
    清除数 为 做 符号表_清除深度 用 st2、1。
    做 自检_断言值 用 「清除深度1=2项」、清除数、2。
    做 自检_断言值 用 「清除后大小=1」、做 符号表_符号表_大小 用 st2、1。
    做 自检_断言值 用 「型_整数=1」、型_整数、1。
    做 自检_断言值 用 「型_字符串=3」、型_字符串、3。
    做 自检_断言值 用 「型_任意=9」、型_任意、9。
    做 自检_断言值 用 「整数宽度=8」、做 类型推断_类型宽度 用 型_整数、8。
    做 自检_断言值 用 「布尔宽度=1」、做 类型推断_类型宽度 用 型_布尔、1。
    做 自检_断言值 用 「空值宽度=0」、做 类型推断_类型宽度 用 型_空値、0。
    做 自检_断言 用 「整数=整数兼容」、做 类型推断_类型兼容 用 型_整数、型_整数 等于 1。
    做 自检_断言 用 「整数≠字符串」、做 类型推断_类型兼容 用 型_整数、型_字符串 等于 0。
    做 自检_断言 用 「任意兼容一切」、做 类型推断_类型兼容 用 型_任意、型_整数 等于 1。
    做 自检_断言 用 「整数↔浮点」、做 类型推断_类型兼容 用 型_整数、型_浮点 等于 1。
    做 自检_断言 用 「字符串兼容空」、做 类型推断_类型兼容 用 型_字符串、型_空値 等于 1。
    做 自检_断言 用 「列表兼容空」、做 类型推断_类型兼容 用 型_列表、型_空値 等于 1。
    做 自检_断言值 用 「整数+整数=整数」、做 类型推断_二元结果类型 用 30、型_整数、型_整数、型_整数。
    做 自检_断言值 用 「字符串+任意=字符串」、做 类型推断_二元结果类型 用 30、型_字符串、型_整数、型_字符串。
    做 自检_断言值 用 「浮点*整数=浮点」、做 类型推断_二元结果类型 用 32、型_浮点、型_整数、型_浮点。
    做 自检_断言值 用 「比较==返回布尔」、做 类型推断_二元结果类型 用 40、型_整数、型_整数、型_布尔。
    做 自检_断言值 用 「负号保持类型」、做 类型推断_一元结果类型 用 31、型_整数、型_整数。
    做 自检_断言值 用 「逻辑非=布尔」、做 类型推断_一元结果类型 用 52、型_整数、型_布尔。
    sig 为 做 函数签名 用 「add」、2、型_整数。
    做 自检_断言值 用 「签名名称」、sig 的 第 0 项、「add」。
    做 自检_断言值 用 「签名参数数」、sig 的 第 1 项、2。
    做 自检_断言值 用 「签名返回类型」、sig 的 第 3 项、型_整数。
    做 打印文本 用 做 字符串拼接 用 「[自检] M7 语义: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M11_安全 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M11 安全分析模块...」。
    r1 为 做 范围_创建 用 0、9。
    做 自检_断言值 用 「范围下界」、做 范围_下界 用 r1、0。
    做 自检_断言值 用 「范围上界」、做 范围_上界 用 r1、9。
    做 自检_断言 用 「范围已知」、做 范围_是已知 用 r1。
    ru 为 做 范围_未知值。
    做 自检_断言 用 「范围未知」、做 范围_是已知 用 ru 等于 假。
    做 自检_断言 用 「安全访问[0,9]长度10」、做 范围_安全访问 用 r1、10。
    做 自检_断言 用 「越界[0,9]长度5」、做 范围_安全访问 用 r1、5 等于 假。
    做 自检_断言 用 「未知范围不安全」、做 范围_安全访问 用 ru、10 等于 假。
    r2 为 做 范围_创建 用 5、15。
    merged 为 做 范围_合并 用 r1、r2。
    做 自检_断言值 用 「合并下界」、做 范围_下界 用 merged、0。
    做 自检_断言值 用 「合并上界」、做 范围_上界 用 merged、15。
    crossed 为 做 范围_交叉 用 r1、r2。
    做 自检_断言值 用 「交叉下界」、做 范围_下界 用 crossed、5。
    做 自检_断言值 用 「交叉上界」、做 范围_上界 用 crossed、9。
    empty 为 做 范围_交叉 用 做 范围_创建 用 0、3、做 范围_创建 用 5、9。
    做 自检_断言 用 「空交集=未知」、做 范围_是已知 用 empty 等于 假。
    bce 为 做 BCE_上下文_创建。
    做 BCE_记录变量范围 用 bce、「i」、做 范围_创建 用 0、4。
    做 BCE_记录数组长度 用 bce、「arr」、10。
    查到范围 为 做 BCE_查询变量范围 用 bce、「i」。
    做 自检_断言值 用 「BCE查询下界」、做 范围_下界 用 查到范围、0。
    做 自检_断言值 用 「BCE查询上界」、做 范围_上界 用 查到范围、4。
    做 自检_断言值 用 「BCE查询长度」、做 BCE_查询数组长度 用 bce、「arr」、10。
    未知范围 为 做 BCE_查询变量范围 用 bce、「不存在」。
    做 自检_断言 用 「BCE未知变量」、做 范围_是已知 用 未知范围 等于 假。
    nce 为 做 NCE_上下文_创建。
    做 自检_断言 用 「NCE初始非空=假」、做 NCE_是非空 用 nce、「ptr」 等于 假。
    做 NCE_记录非空 用 nce、「ptr」。
    做 自检_断言 用 「NCE记录后非空」、做 NCE_是非空 用 nce、「ptr」。
    做 自检_断言 用 「NCE其他仍空」、做 NCE_是非空 用 nce、「other」 等于 假。
    做 UAF_重置。
    做 UAF_记录分配 用 100。
    做 自检_断言值 用 「UAF分配后=活跃」、做 UAF_检查使用 用 100、对象_活跃。
    做 UAF_记录释放 用 100。
    做 自检_断言值 用 「UAF释放后=已释放」、做 UAF_检查使用 用 100、对象_已释放。
    做 自检_断言值 用 「UAF未知对象」、做 UAF_检查使用 用 999、对象_未知。
    做 安全_重置。
    做 安全门控_设置 用 安全门控_标准。
    做 自检_断言值 用 「门控等级=标准」、做 安全门控_获取、安全门控_标准。
    做 自检_断言 用 「标准无红项不阻塞」、做 安全门控_应阻塞 等于 假。
    做 安全_记录红项。
    做 自检_断言 用 「标准有红项阻塞」、做 安全门控_应阻塞。
    做 安全_重置。
    做 安全门控_设置 用 安全门控_宽松。
    做 安全_记录红项。
    做 自检_断言 用 「宽松有红项不阻塞」、做 安全门控_应阻塞 等于 假。
    做 安全_重置。
    做 安全门控_设置 用 安全门控_严格。
    做 安全_记录效果违规。
    做 自检_断言 用 「严格有违规阻塞」、做 安全门控_应阻塞。
    做 自检_断言值 用 「总违规数=1」、做 安全_总违规数、1。
    做 安全_重置。
    做 安全门控_设置 用 安全门控_标准。
    报告 为 做 安全_统一报告。
    做 自检_断言 用 「安全报告非空」、报告 不等于 空。
    分析报告 为 做 内存安全_分析报告。
    做 自检_断言值 用 「分析报告项数」、做 计数 用 分析报告、3。
    做 效果_初始化。
    做 效果_声明 用 「写日志」、一组：效果_IO。
    做 效果_声明 用 「调用C库」、一组：效果_FFI、效果_RAW_PTR。
    做 效果_声明 用 「纯函数」、一组：（空）。
    做 自检_断言 用 「写日志有效果」、做 效果_有效果 用 「写日志」。
    做 自检_断言 用 「纯函数无效果」、做 效果_有效果 用 「纯函数」 等于 假。
    做 自检_断言 用 「未声明无效果」、做 效果_有效果 用 「不存在」 等于 假。
    做 自检_断言 用 「写日志包含IO」、做 效果_包含 用 「写日志」、效果_IO。
    做 自检_断言 用 「写日志不含FFI」、做 效果_包含 用 「写日志」、效果_FFI 等于 假。
    做 自检_断言 用 「C库包含FFI」、做 效果_包含 用 「调用C库」、效果_FFI。
    做 自检_断言 用 「C库包含RAW_PTR」、做 效果_包含 用 「调用C库」、效果_RAW_PTR。
    查到 为 做 效果_查询 用 「写日志」。
    做 自检_断言值 用 「查询效果数=1」、做 计数 用 查到、1。
    做 自检_断言值 用 「效果名_无」、做 效果名 用 效果_无、「无」。
    做 自检_断言值 用 「效果名_IO」、做 效果名 用 效果_IO、「IO」。
    做 自检_断言值 用 「效果名_FFI」、做 效果名 用 效果_FFI、「FFI」。
    做 自检_断言值 用 「效果名_SYSCALL」、做 效果名 用 效果_SYSCALL、「SYSCALL」。
    做 自检_断言值 用 「效果名_GPU」、做 效果名 用 效果_GPU、「GPU」。
    做 自检_断言值 用 「效果名_ASM」、做 效果名 用 效果_ASM、「ASM」。
    做 自检_断言值 用 「效果名_DMA」、做 效果名 用 效果_DMA、「DMA」。
    做 自检_断言值 用 「效果名_PRIVILEGE」、做 效果名 用 效果_PRIVILEGE、「PRIVILEGE」。
    做 自检_断言值 用 「效果_无=0」、效果_无、0。
    做 自检_断言值 用 「效果_IO=1」、效果_IO、1。
    做 自检_断言值 用 「效果_RAW_PTR=6」、效果_RAW_PTR、6。
    做 自检_断言值 用 「效果_PRIVILEGE=10」、效果_PRIVILEGE、10。
    做 能力_初始化。
    做 能力_授权 用 「gpu模块」、能力_显存、2、真。
    做 能力_授权 用 「gpu模块」、能力_设备、1、假。
    做 自检_断言 用 「gpu有显存能力」、做 能力_检查 用 「gpu模块」、能力_显存。
    做 自检_断言 用 「gpu有设备能力」、做 能力_检查 用 「gpu模块」、能力_设备。
    做 自检_断言 用 「gpu无网络能力」、做 能力_检查 用 「gpu模块」、能力_网络 等于 假。
    做 自检_断言 用 「空模块无能力」、做 能力_检查 用 「不存在」、能力_设备 等于 假。
    caps 为 做 能力_获取所有 用 「gpu模块」。
    做 自检_断言值 用 「gpu能力数=2」、做 计数 用 caps、2。
    做 自检_断言值 用 「能力令牌类型」、做 能力_令牌类型 用 caps 的 第 0 项、能力_显存。
    做 自检_断言值 用 「能力令牌模块」、做 能力_令牌模块 用 caps 的 第 0 项、「gpu模块」。
    做 自检_断言值 用 「能力令牌级别」、做 能力_令牌级别 用 caps 的 第 0 项、2。
    做 自检_断言值 用 「能力令牌可传递」、做 能力_令牌可传递 用 caps 的 第 0 项、真。
    做 自检_断言值 用 「能力名_无」、做 能力名 用 能力_无、「无」。
    做 自检_断言值 用 「能力名_设备」、做 能力名 用 能力_设备、「设备」。
    做 自检_断言值 用 「能力名_文件」、做 能力名 用 能力_文件、「文件」。
    做 自检_断言值 用 「能力名_网络」、做 能力名 用 能力_网络、「网络」。
    做 自检_断言值 用 「能力名_汇编」、做 能力名 用 能力_汇编、「汇编」。
    tv1 为 做 污点值_创建 用 42、污点_用户输入、「stdin:1」。
    做 自检_断言 用 「污点值被污染」、做 污点值_是污染的 用 tv1。
    做 自检_断言 用 「有用户输入标签」、做 污点值_有标签 用 tv1、污点_用户输入。
    做 自检_断言 用 「无网络标签」、做 污点值_有标签 用 tv1、污点_网络 等于 假。
    tv_clean 为 做 污点值_清洁 用 100。
    做 自检_断言 用 「清洁值未污染」、做 污点值_是污染的 用 tv_clean 等于 假。
    tv2 为 做 污点值_创建 用 10、污点_网络、「net:80」。
    tv3 为 做 污点_传播 用 tv1、tv2、「add」。
    做 自检_断言 用 「传播后被污染」、做 污点值_是污染的 用 tv3。
    做 自检_断言 用 「传播继承用户输入」、做 污点值_有标签 用 tv3、污点_用户输入。
    做 自检_断言 用 「传播继承网络」、做 污点值_有标签 用 tv3、污点_网络。
    san 为 做 消毒器_创建。
    做 消毒器_注册 用 san、「escape_html」、污点_用户输入、3。
    found 为 做 消毒器_查找 用 san、「escape_html」。
    做 自检_断言 用 「消毒器找到」、found 不等于 空。
    做 自检_断言值 用 「消毒器名称」、found 的 第 0 项、「escape_html」。
    做 自检_断言 用 「消毒器未找到」、做 消毒器_查找 用 san、「不存在」 等于 空。
    tv4 为 做 污点_消毒 用 tv1、污点_用户输入。
    做 自检_断言 用 「消毒后无用户输入」、做 污点值_有标签 用 tv4、污点_用户输入 等于 假。
    做 自检_断言值 用 「污点_清洁=0」、污点_清洁、0。
    做 自检_断言值 用 「污点_用户输入=1」、污点_用户输入、1。
    做 自检_断言值 用 「污点_网络=2」、污点_网络、2。
    做 自检_断言值 用 「污点_文件=4」、污点_文件、4。
    做 自检_断言值 用 「污点_密钥=64」、污点_密钥、64。
    做 打印文本 用 做 字符串拼接 用 「[自检] M11 安全: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

_测试_构建列表 接受 a1、a2、a3、a4：
    可变 e 为 一组：（空）。
    做 追加 用 e、0。
    做 追加 用 e、a1。
    做 追加 用 e、a2。
    做 追加 用 e、a3。
    做 追加 用 e、a4。
    做 追加 用 e、0。
    做 追加 用 e、0。
    返还 e。
完。

_测试_点积 接受 a、b：
    可变 n 为 做 计数 用 a。
    若 blen 小于 n：
        n 为 blen。
    完。
    可变 累加 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        ai 为 a 的 第 i 项。
        bi 为 b 的 第 i 项。
        积 为 ai 乘 bi。
        累加 为 累加 加 积。
        i 为 i 加 1。
    完。
    返还 累加。
完。

_测试_缩放 接受 v、s：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        vi 为 v 的 第 i 项。
        scaled 为 vi 乘 s。
        做 追加 用 结果、scaled。
        i 为 i 加 1。
    完。
    返还 结果。
完。

自检_代码生成 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] 代码生成验证...」。
    a 为 一组：42、84、126。
    做 自检_断言值 用 「列表[0]」、a 的 第 0 项、42。
    做 自检_断言值 用 「列表[1]」、a 的 第 1 项、84。
    做 自检_断言值 用 「列表[2]」、a 的 第 2 项、126。
    做 自检_断言值 用 「列表计数」、做 计数 用 a、3。
    可变 b 为 一组：（空）。
    做 追加 用 b、42。
    做 追加 用 b、84。
    做 追加 用 b、126。
    做 自检_断言值 用 「追加[0]」、b 的 第 0 项、42。
    做 自检_断言值 用 「追加[1]」、b 的 第 1 项、84。
    做 自检_断言值 用 「追加[2]」、b 的 第 2 项、126。
    c 为 一组：0、10、20。
    做 自检_断言值 用 「含0列表[0]」、c 的 第 0 项、0。
    做 自检_断言值 用 「含0列表[1]」、c 的 第 1 项、10。
    做 自检_断言值 用 「含0列表[2]」、c 的 第 2 项、20。
    x 为 100。
    y 为 200。
    做 自检_断言值 用 「乘法100*200」、x 乘 y、20000。
    d 为 一组：100、200、300。
    e 为 一组：4、5、6。
    做 自检_断言值 用 「列表元素乘」、d 的 第 0 项 乘 e 的 第 0 项、400。
    可变 f 为 一组：10、20、30。
    做 自检_断言值 用 「修改前f[1]」、f 的 第 1 项、20。
    f 的 第 1 项 为 99。
    做 自检_断言值 用 「修改后f[1]」、f 的 第 1 项、99。
    可变 g 为 一组：一组：1、2、一组：3、4。
    做 自检_断言值 用 「嵌套g[0][0]」、g 的 第 0 项 的 第 0 项、1。
    做 自检_断言值 用 「嵌套g[0][1]」、g 的 第 0 项 的 第 1 项、2。
    g 的 第 0 项 的 第 1 项 为 99。
    做 自检_断言值 用 「嵌套修改g[0][1]」、g 的 第 0 项 的 第 1 项、99。
    va 为 一组：100、200、300。
    vb 为 一组：400、500、600。
    可变 vacc 为 0。
    可变 vi 为 0。
    只要 vi 小于 做 计数 用 va：
        vai 为 va 的 第 vi 项。
        vbi 为 vb 的 第 vi 项。
        vacc 为 vacc 加 vai 乘 vbi。
        vi 为 vi 加 1。
    完。
    做 自检_断言值 用 「内联点积」、vacc、320000。
    做 自检_断言值 用 「跨模块点积」、做 向量_点积 用 va、vb、0、320000。
    sc 为 做 向量_缩放 用 一组：100、200、300、2。
    做 自检_断言值 用 「跨模块缩放[0]」、sc 的 第 0 项、200。
    做 自检_断言值 用 「跨模块缩放[1]」、sc 的 第 1 项、400。
    做 自检_断言值 用 「跨模块缩放[2]」、sc 的 第 2 项、600。
    adsr 为 做 ADSR_创建 用 10、20、70、30。
    做 自检_断言值 用 「ADSR计数」、做 计数 用 adsr、7。
    做 自检_断言值 用 「ADSR[0]起始」、adsr 的 第 0 项、0。
    做 自检_断言值 用 「ADSR[1]攻击」、adsr 的 第 1 项、10。
    做 自检_断言值 用 「ADSR[2]衰减」、adsr 的 第 2 项、20。
    做 自检_断言值 用 「ADSR[3]持续」、adsr 的 第 3 项、70。
    做 自检_断言值 用 「ADSR[4]释放」、adsr 的 第 4 项、30。
    同列 为 做 _测试_构建列表 用 10、20、70、30。
    做 自检_断言值 用 「同模块列表计数」、做 计数 用 同列、7。
    做 自检_断言值 用 「同模块列表[0]」、同列 的 第 0 项、0。
    做 自检_断言值 用 「同模块列表[1]」、同列 的 第 1 项、10。
    做 自检_断言值 用 「同模块列表[4]」、同列 的 第 4 项、30。
    做 自检_断言值 用 「同模块点积」、做 _测试_点积 用 一组：100、200、300、一组：400、500、600、320000。
    同缩 为 做 _测试_缩放 用 一组：100、200、300、2。
    做 自检_断言值 用 「同模块缩放[0]」、同缩 的 第 0 项、200。
    做 打印文本 用 做 字符串拼接 用 「[自检] 代码生成: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_C1_热更新 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] C1 热更新管线...」。
    mgr 为 做 热更管理器_创建。
    做 自检_断言 用 「热更管理器非空」、mgr 不等于 空。
    做 自检_断言值 用 「热更初始版本」、mgr 的 第 0 项、0。
    做 自检_断言值 用 「热更初始状态」、mgr 的 第 1 项、热更_空闲。
    做 自检_断言值 用 「DJB2空串=0」、做 热更_内容哈希 用 「」、0。
    h1 为 做 热更_内容哈希 用 「hello」。
    h2 为 做 热更_内容哈希 用 「hello」。
    做 自检_断言值 用 「DJB2确定性」、h1、h2。
    做 自检_断言 用 「DJB2hello非零」、h1 不等于 0。
    做 热更_添加监控 用 「test_a.nova」。
    做 自检_断言 用 「监控数>0」、做 热更_监控数 大于 0。
    做 自检_断言 用 「变更检测(新哈希)」、做 热更_检测变更 用 0、999。
    做 自检_断言 用 「无变更(同哈希)」、做 热更_检测变更 用 0、999 等于 假。
    做 热更_标记JIT失效 用 「test_fn」。
    做 自检_断言 用 「JIT标记失效」、做 热更_JIT是否失效 用 「test_fn」。
    做 热更_清除JIT失效。
    做 自检_断言 用 「JIT清除后」、做 热更_JIT是否失效 用 「test_fn」 等于 假。
    做 热更_记录版本 用 mgr、「v1」。
    做 自检_断言 用 「版本数>0」、做 热更_版本数 大于 0。
    做 热更_添加依赖 用 「a」、「b」。
    deps 为 做 热更_获取依赖 用 「a」。
    做 自检_断言 用 「依赖列表」、做 是列表 用 deps。
    做 自检_断言值 用 「安全检查=安全」、做 热更_安全检查 用 「a」、「安全」。
    做 热更_记录更新。
    做 自检_断言 用 「统计报告」、做 热更_统计报告 不等于 空。
    做 热更_添加白名单 用 「推理模块」、「状态型」、迁移_保留记忆。
    做 自检_断言 用 「在白名单」、做 热更_在白名单中 用 「推理模块」。
    做 自检_断言 用 「不在白名单」、做 热更_在白名单中 用 「不存在模块」 等于 假。
    做 自检_断言 用 「白名单数>0」、做 热更_白名单数 大于 0。
    合同 为 做 热更_获取状态合同 用 「推理模块」。
    做 自检_断言 用 「状态合同非空」、合同 不等于 空。
    做 自检_断言值 用 「合同模块名」、合同 的 第 0 项、「推理模块」。
    做 自检_断言值 用 「白名单守卫=通过」、做 热更_白名单守卫 用 「推理模块」、「pass」。
    做 自检_断言 用 「未注册守卫=拒绝」、做 热更_白名单守卫 用 「xyz」 不等于 「pass」。
    做 自检_断言值 用 「初始模块数=0」、做 热更_模块数、0。
    做 自检_断言 用 「注册模块成功」、做 热更_注册模块 用 「test_mod」、100、200。
    做 自检_断言 用 「重复注册=假」、做 热更_注册模块 用 「test_mod」、100、200 等于 假。
    做 自检_断言值 用 「模块数=1」、做 热更_模块数、1。
    模块信息 为 做 热更_获取模块信息 用 「test_mod」。
    做 自检_断言 用 「模块信息非空」、模块信息 不等于 空。
    做 自检_断言值 用 「模块名」、模块信息 的 第 0 项、「test_mod」。
    做 自检_断言值 用 「模块地址」、模块信息 的 第 1 项、100。
    做 自检_断言值 用 「模块大小」、模块信息 的 第 2 项、200。
    做 自检_断言值 用 「替换次数=0」、模块信息 的 第 3 项、0。
    做 自检_断言 用 「替换模块成功」、做 热更_替换模块 用 「test_mod」、300、400。
    做 自检_断言值 用 「替换后地址」、做 热更_获取模块信息 用 「test_mod」 的 第 1 项、300。
    做 自检_断言值 用 「替换后大小」、做 热更_获取模块信息 用 「test_mod」 的 第 2 项、400。
    做 自检_断言值 用 「替换次数=1」、做 热更_获取模块信息 用 「test_mod」 的 第 3 项、1。
    做 自检_断言 用 「不存在模块替换=假」、做 热更_替换模块 用 「不存在」、0、0 等于 假。
    做 自检_断言 用 「不存在模块信息=空」、做 热更_获取模块信息 用 「不存在」 等于 空。
    做 调试器_重置。
    做 自检_断言 用 「调试器初始禁用」、做 调试器_是否启用 等于 假。
    做 自检_断言值 用 「初始断点数=0」、做 调试器_断点数、0。
    做 调试器_添加断点 用 「foo.nova」、12。
    做 调试器_添加断点 用 「foo.nova」、12。
    做 调试器_添加断点 用 「foo.nova」、18。
    做 自检_断言值 用 「断点去重计数=2」、做 调试器_断点数、2。
    做 自检_断言 用 「禁用时不命中断点」、做 调试器_命中断点 用 「foo.nova」、12 等于 假。
    做 调试器_启用。
    做 自检_断言 用 「调试器已启用」、做 调试器_是否启用。
    做 自检_断言 用 「已注册断点命中」、做 调试器_命中断点 用 「foo.nova」、12。
    做 自检_断言 用 「未注册断点不命中」、做 调试器_命中断点 用 「foo.nova」、999 等于 假。
    做 调试器_移除断点 用 「foo.nova」、12。
    做 自检_断言值 用 「移除后断点数=1」、做 调试器_断点数、1。
    做 自检_断言 用 「移除后断点不命中」、做 调试器_命中断点 用 「foo.nova」、12 等于 假。
    做 调试器_移除断点 用 「foo.nova」、999。
    做 自检_断言值 用 「移除不存在断点数保持=1」、做 调试器_断点数、1。
    做 调试器_设置单步 用 真。
    做 自检_断言 用 「单步命中任意行」、做 调试器_命中断点 用 「any.nova」、999。
    做 调试器_设置单步 用 假。
    做 调试器_更新位置 用 42、「bar」。
    dbg_report 为 做 调试器_报告。
    做 自检_断言 用 「调试报告非空」、dbg_report 不等于 空。
    做 自检_断言值 用 「调试报告断点数=1」、做 字典取 用 dbg_report、「断点数」、「1」。
    做 调试器_禁用。
    做 自检_断言 用 「调试器已禁用」、做 调试器_是否启用 等于 假。
    做 热更_添加依赖 用 「b」、「c」。
    影响 为 做 热更_影响分析 用 「b」。
    做 自检_断言 用 「影响分析=列表」、做 是列表 用 影响。
    做 自检_断言值 用 「热更_空闲=0」、热更_空闲、0。
    做 自检_断言值 用 「热更_检测中=1」、热更_检测中、1。
    做 自检_断言值 用 「热更_编译中=2」、热更_编译中、2。
    做 自检_断言值 用 「热更_替换中=3」、热更_替换中、3。
    做 自检_断言值 用 「热更_回滚中=4」、热更_回滚中、4。
    做 自检_断言值 用 「迁移_无状态」、迁移_无状态、「无状态」。
    做 自检_断言值 用 「迁移_保留记忆」、迁移_保留记忆、「保留记忆」。
    做 打印文本 用 做 字符串拼接 用 「[自检] C1 热更新: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_跨语言 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] 跨语言模块...」。
    c 为 做 外源合同_创建。
    做 自检_断言 用 「外源合同非空」、c 不等于 空。
    做 自检_断言值 用 「默认内存模型=GC」、做 外源合同_内存模型 用 c、内存模型_GC。
    做 自检_断言值 用 「默认并发模型=无」、做 外源合同_并发模型 用 c、并发模型_无。
    做 自检_断言值 用 「默认重置Profile=宿主兼容」、做 外源合同_重置Profile 用 c、重置Profile_宿主兼容。
    做 外源合同_设名称 用 c、「test-project」。
    做 自检_断言值 用 「合同名称」、做 外源合同_名称 用 c、「test-project」。
    做 外源合同_设源语言 用 c、1。
    做 自检_断言值 用 「合同源语言=Rust」、做 外源合同_源语言 用 c、1。
    做 外源合同_设内存模型 用 c、内存模型_所有权。
    做 自检_断言值 用 「设内存模型=所有权」、做 外源合同_内存模型 用 c、内存模型_所有权。
    做 外源合同_设并发模型 用 c、并发模型_异步IO。
    做 自检_断言值 用 「设并发模型=异步IO」、做 外源合同_并发模型 用 c、并发模型_异步IO。
    做 外源合同_设错误模型 用 c、错误模型_Result。
    做 自检_断言值 用 「设错误模型=Result」、做 外源合同_错误模型 用 c、错误模型_Result。
    做 外源合同_设重置Profile 用 c、重置Profile_严格原生。
    做 自检_断言值 用 「设重置Profile=严格原生」、做 外源合同_重置Profile 用 c、重置Profile_严格原生。
    做 外源合同_添加包根 用 c、「/src」。
    做 外源合同_添加包根 用 c、「/lib」。
    做 自检_断言值 用 「包根数=2」、做 计数 用 做 外源合同_包根 用 c、2。
    做 外源合同_添加入口 用 c、「main.rs」。
    做 自检_断言值 用 「入口数=1」、做 计数 用 做 外源合同_入口 用 c、1。
    做 外源合同_添加公共API 用 c、「parse」。
    做 外源合同_添加公共API 用 c、「compile」。
    做 自检_断言值 用 「公共API数=2」、做 计数 用 做 外源合同_公共API 用 c、2。
    做 外源合同_添加FFI边界 用 c、「libc」。
    做 自检_断言值 用 「FFI边界数=1」、做 计数 用 做 外源合同_FFI边界 用 c、1。
    做 外源合同_添加诊断 用 c、1、「test warning」。
    做 自检_断言值 用 「诊断数=1」、做 计数 用 做 外源合同_诊断 用 c、1。
    做 自检_断言值 用 「所有权名称」、做 内存模型_名称 用 内存模型_所有权、「ownership」。
    做 自检_断言值 用 「GC名称」、做 内存模型_名称 用 内存模型_GC、「gc」。
    做 自检_断言值 用 「手动名称」、做 内存模型_名称 用 内存模型_手动、「manual」。
    做 自检_断言值 用 「线程名称」、做 并发模型_名称 用 并发模型_线程、「threads」。
    做 自检_断言值 用 「协程名称」、做 并发模型_名称 用 并发模型_协程、「goroutine」。
    g 为 做 语义图_创建。
    做 自检_断言 用 「语义图非空」、g 不等于 空。
    做 自检_断言值 用 「语义图初始节点数=0」、做 计数 用 做 语义图_节点 用 g、0。
    n1 为 做 语义节点_创建 用 语义_类型、类型节点_结构、「Point」。
    做 自检_断言值 用 「节点类别=类型」、做 语义节点_类别 用 n1、语义_类型。
    做 自检_断言值 用 「节点子类型=结构」、做 语义节点_子类型 用 n1、类型节点_结构。
    做 自检_断言值 用 「节点名称」、做 语义节点_名称 用 n1、「Point」。
    做 自检_断言值 用 「节点风险=0」、做 语义节点_风险 用 n1、0。
    做 语义节点_设风险 用 n1、3。
    做 自检_断言值 用 「节点风险=3」、做 语义节点_风险 用 n1、3。
    做 语义节点_添加属性 用 n1、「fields」、「x,y」。
    做 自检_断言值 用 「节点属性数=1」、做 计数 用 做 语义节点_属性 用 n1、1。
    做 语义图_添加节点 用 g、n1。
    做 自检_断言值 用 「语义图节点数=1」、做 计数 用 做 语义图_节点 用 g、1。
    做 语义图_添加类型映射 用 g、「Vec<i32>」、「列表<整数>」。
    做 自检_断言值 用 「类型映射数=1」、做 计数 用 做 语义图_类型映射 用 g、1。
    做 语义图_添加接口 用 g、「Iterator」、一组：「next」、「has_next」。
    做 自检_断言值 用 「接口数=1」、做 计数 用 做 语义图_接口列表 用 g、1。
    做 语义图_标记风险 用 g、「unsafe block」、2。
    做 自检_断言值 用 「风险项数=1」、做 计数 用 做 语义图_风险项 用 g、1。
    做 语义图_标记阻塞 用 g、「proc macro」、「不可重置」。
    做 自检_断言值 用 「阻塞项数=1」、做 计数 用 做 语义图_阻塞项 用 g、1。
    做 自检_断言值 用 「Rust i64→整数」、做 类型映射_Rust到Nova 用 「i64」、「整数」。
    做 自检_断言值 用 「Rust f32→浮点」、做 类型映射_Rust到Nova 用 「f32」、「浮点」。
    做 自检_断言值 用 「Rust bool→布尔」、做 类型映射_Rust到Nova 用 「bool」、「布尔」。
    做 自检_断言值 用 「Rust String→字符串」、做 类型映射_Rust到Nova 用 「String」、「字符串」。
    做 自检_断言值 用 「Rust Vec→列表」、做 类型映射_Rust到Nova 用 「Vec」、「列表」。
    做 自检_断言值 用 「Rust HashMap→字典」、做 类型映射_Rust到Nova 用 「HashMap」、「字典」。
    做 自检_断言值 用 「Rust Option→可空」、做 类型映射_Rust到Nova 用 「Option」、「可空」。
    做 自检_断言值 用 「Rust Result→结果」、做 类型映射_Rust到Nova 用 「Result」、「结果」。
    做 自检_断言值 用 「Go int→整数」、做 类型映射_Go到Nova 用 「int」、「整数」。
    做 自检_断言值 用 「Go string→字符串」、做 类型映射_Go到Nova 用 「string」、「字符串」。
    做 自检_断言值 用 「Go error→错误」、做 类型映射_Go到Nova 用 「error」、「error」。
    做 自检_断言值 用 「Go float64→浮点」、做 类型映射_Go到Nova 用 「float64」、「浮点」。
    做 自检_断言值 用 「C int→整数」、做 类型映射_C到Nova 用 「int」、「整数」。
    做 自检_断言值 用 「C double→浮点」、做 类型映射_C到Nova 用 「double」、「浮点」。
    做 自检_断言值 用 「C void→空」、做 类型映射_C到Nova 用 「void」、「空」。
    做 自检_断言值 用 「C char*→字符串」、做 类型映射_C到Nova 用 「char*」、「字符串」。
    做 自检_断言值 用 「Python int→整数」、做 类型映射_Python到Nova 用 「int」、「整数」。
    做 自检_断言值 用 「Python str→字符串」、做 类型映射_Python到Nova 用 「str」、「字符串」。
    做 自检_断言值 用 「Python list→列表」、做 类型映射_Python到Nova 用 「list」、「列表」。
    做 自检_断言值 用 「Python dict→字典」、做 类型映射_Python到Nova 用 「dict」、「字典」。
    做 自检_断言值 用 「Java int→整数」、做 类型映射_Java到Nova 用 「int」、「整数」。
    做 自检_断言值 用 「Java String→字符串」、做 类型映射_Java到Nova 用 「String」、「字符串」。
    做 自检_断言值 用 「Java ArrayList→列表」、做 类型映射_Java到Nova 用 「ArrayList」、「列表」。
    做 自检_断言值 用 「Java HashMap→字典」、做 类型映射_Java到Nova 用 「HashMap」、「字典」。
    做 自检_断言值 用 「重置Profile常量0」、重置Profile_严格原生、0。
    做 自检_断言值 用 「重置Profile常量1」、重置Profile_宿主兼容、1。
    做 自检_断言值 用 「重置Profile常量2」、重置Profile_混合桥接、2。
    做 打印文本 用 做 字符串拼接 用 「[自检] 跨语言: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M12_渲染合同 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M12 渲染合同+GPU管线...」。
    smoke结果 为 做 渲染_真实场景smoke。
    做 自检_断言 用 「smoke返回非空」、smoke结果 不等于 空。
    rc_r 为 做 字典取 用 smoke结果、「render_contract」。
    做 自检_断言 用 「smoke有render_contract」、rc_r 不等于 空。
    pass_r 为 做 字典取 用 smoke结果、「render_pass」。
    做 自检_断言 用 「smoke有render_pass」、pass_r 不等于 空。
    fb_r 为 做 字典取 用 smoke结果、「framebuffer」。
    做 自检_断言 用 「smoke有framebuffer」、fb_r 不等于 空。
    gpu_r 为 做 字典取 用 smoke结果、「gpu_render」。
    做 自检_断言 用 「smoke有gpu_render」、gpu_r 不等于 空。
    做 自检_断言 用 「gpu帧数>0」、做 字典取 用 gpu_r、「帧数」 不等于 「0」。
    做 自检_断言 用 「gpu_pass数>0」、做 字典取 用 gpu_r、「pass数」 不等于 「0」。
    st_r 为 做 字典取 用 smoke结果、「scene_trace」。
    做 自检_断言 用 「smoke有scene_trace」、st_r 不等于 空。
    做 自检_断言 用 「追踪步数>0」、做 字典取 用 st_r、「追踪步数」 不等于 「0」。
    做 自检_断言 用 「追踪总数据>0」、做 字典取 用 st_r、「总数据字节」 不等于 「0」。
    做 场景追踪_重置。
    做 场景追踪_记录 用 「窗口」、「主窗口」、0、「创建」。
    做 场景追踪_记录 用 「mesh」、「立方体」、288、「24顶点」。
    做 场景追踪_记录 用 「提交」、「帧」、0、「complete」。
    str 为 做 场景追踪_报告。
    做 自检_断言值 用 「追踪步数=3」、做 字典取 用 str、「追踪步数」、「3」。
    做 自检_断言值 用 「追踪总数据=288」、做 字典取 用 str、「总数据字节」、「288」。
    gr 为 做 GPU渲染_报告。
    做 自检_断言 用 「gr有帧数」、做 字典取 用 gr、「帧数」 不等于 空。
    做 自检_断言 用 「gr有pass数」、做 字典取 用 gr、「pass数」 不等于 空。
    rpr 为 做 渲染通道_报告。
    做 自检_断言 用 「rpr有pass总数」、做 字典取 用 rpr、「pass总数」 不等于 空。
    m 为 做 Mesh_创建 用 「三角形」、3、3。
    做 自检_断言值 用 「mesh名称」、m 的 第 0 项、「三角形」。
    做 自检_断言值 用 「mesh顶点数=3」、m 的 第 1 项、3。
    做 自检_断言值 用 「mesh索引数=3」、m 的 第 2 项、3。
    mat 为 做 Material_创建 用 「红色」、255、0、255、0、500、「test.png」。
    做 自检_断言值 用 「material名称」、mat 的 第 0 项、「红色」。
    做 自检_断言值 用 「material镜面=500」、mat 的 第 2 项、500。
    做 自检_断言值 用 「material纹理」、mat 的 第 3 项、「test.png」。
    做 打印文本 用 做 字符串拼接 用 「[自检] M12渲染合同: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M13_媒体管线 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M13 媒体管线合同...」。
    做 自检_断言值 用 「格式PCM=0」、媒体格式_PCM、0。
    做 自检_断言值 用 「格式AAC=1」、媒体格式_AAC、1。
    做 自检_断言值 用 「格式OPUS=2」、媒体格式_OPUS、2。
    做 自检_断言值 用 「格式H264=10」、媒体格式_H264、10。
    做 自检_断言值 用 「格式H265=11」、媒体格式_H265、11。
    做 自检_断言值 用 「格式VP9=12」、媒体格式_VP9、12。
    做 自检_断言值 用 「格式AV1=13」、媒体格式_AV1、13。
    做 自检_断言值 用 「格式PNG=20」、媒体格式_PNG、20。
    做 自检_断言值 用 「格式JPEG=21」、媒体格式_JPEG、21。
    做 自检_断言值 用 「格式BMP=22」、媒体格式_BMP、22。
    做 自检_断言值 用 「格式名PCM」、做 媒体格式名 用 0、「PCM」。
    做 自检_断言值 用 「格式名H264」、做 媒体格式名 用 10、「H.264」。
    做 自检_断言值 用 「格式名PNG」、做 媒体格式名 用 20、「PNG」。
    做 自检_断言值 用 「格式名未知」、做 媒体格式名 用 99、「unknown」。
    做 自检_断言 用 「PCM是音频」、做 是音频格式 用 0。
    做 自检_断言 用 「H264是视频」、做 是视频格式 用 10。
    做 自检_断言 用 「PNG是图像」、做 是图像格式 用 20。
    做 自检_断言 用 「H264非音频」、做 是音频格式 用 10 等于 假。
    做 自检_断言 用 「PCM非图像」、做 是图像格式 用 0 等于 假。
    mc 为 做 媒体合同_创建 用 媒体格式_H264、33。
    做 自检_断言值 用 「合同格式=H264」、做 媒体合同_格式 用 mc、10。
    做 自检_断言值 用 「合同最大延迟=33」、做 媒体合同_最大延迟 用 mc、33。
    做 媒体合同_设采样率 用 mc、48000。
    做 自检_断言值 用 「设采样率48k」、mc 的 第 0 项、48000。
    做 媒体合同_设通道 用 mc、2。
    做 自检_断言值 用 「设通道2」、mc 的 第 1 项、2。
    做 媒体合同_设位深 用 mc、24。
    做 自检_断言值 用 「设位深24」、mc 的 第 2 项、24。
    做 媒体合同_设最大帧 用 mc、4194304。
    做 自检_断言值 用 「设最大帧4MB」、mc 的 第 4 项、4194304。
    做 媒体合同_设硬件加速 用 mc。
    做 自检_断言值 用 「设硬件加速」、mc 的 第 6 项、真。
    做 自检_断言值 用 「阶段解码=0」、媒体阶段_解码、0。
    做 自检_断言值 用 「阶段编码=1」、媒体阶段_编码、1。
    做 自检_断言值 用 「阶段转码=2」、媒体阶段_转码、2。
    做 自检_断言值 用 「阶段滤镜=3」、媒体阶段_滤镜、3。
    做 自检_断言值 用 「阶段混音=4」、媒体阶段_混音、4。
    做 自检_断言值 用 「阶段重采样=5」、媒体阶段_重采样、5。
    stage 为 做 媒体阶段_创建 用 「解码H264」、媒体阶段_解码、媒体格式_H264、0。
    做 自检_断言值 用 「阶段名称」、stage 的 第 0 项、「解码H264」。
    做 自检_断言值 用 「阶段类型=解码」、stage 的 第 1 项、0。
    做 自检_断言值 用 「阶段输入=H264」、stage 的 第 2 项、10。
    kern_mc 为 做 媒体合同_从Profile 用 「kernel-qemu」。
    做 自检_断言值 用 「内核合同PCM」、做 媒体合同_格式 用 kern_mc、媒体格式_PCM。
    user_mc 为 做 媒体合同_从Profile 用 「linux-user」。
    做 自检_断言值 用 「用户合同H264」、做 媒体合同_格式 用 user_mc、媒体格式_H264。
    pcm 为 做 PCM缓冲_创建 用 512、44100、16、1。
    做 自检_断言值 用 「PCM采样率=44100」、pcm 的 第 0 项、44100。
    做 自检_断言值 用 「PCM位深=16」、pcm 的 第 1 项、16。
    做 自检_断言值 用 「PCM通道=1」、pcm 的 第 2 项、1。
    做 自检_断言值 用 「PCM采样数=512」、pcm 的 第 3 项、512。
    dev 为 做 音频设备_创建 用 44100、16、1、16。
    做 音频设备_打开 用 dev。
    做 自检_断言值 用 「音频设备状态=打开」、dev 的 第 5 项、1。
    做 自检_断言值 用 「音频设备写入8」、做 音频设备_写入 用 dev、pcm、0、8、8。
    q 为 做 视频帧队列_创建 用 2。
    做 自检_断言值 用 「视频队列初始深度=0」、做 视频帧队列_深度 用 q、0。
    做 自检_断言 用 「视频队列推入1」、做 视频帧队列_推入 用 q、一组：0。
    做 自检_断言 用 「视频队列推入2」、做 视频帧队列_推入 用 q、一组：1。
    做 自检_断言值 用 「视频队列深度=2」、做 视频帧队列_深度 用 q、2。
    做 自检_断言 用 「视频队列满时拒绝推入」、做 视频帧队列_推入 用 q、一组：2 等于 假。
    做 自检_断言 用 「视频队列弹出非空」、做 视频帧队列_弹出 用 q 不等于 空。
    做 自检_断言值 用 「视频队列深度回到1」、做 视频帧队列_深度 用 q、1。
    同步 为 做 AV同步_创建 用 44100。
    做 AV同步_音频推进 用 同步、44100。
    做 自检_断言值 用 「1秒音频时间=1000ms」、做 AV同步_音频时间 用 同步、1000。
    决策1 为 做 AV同步_视频决策 用 同步、990。
    做 自检_断言值 用 「AV同步正常显示」、决策1、1。
    决策2 为 做 AV同步_视频决策 用 同步、500。
    做 自检_断言值 用 「AV同步跳帧」、决策2、0。
    决策3 为 做 AV同步_视频决策 用 同步、1100。
    做 自检_断言值 用 「AV同步重复帧」、决策3、2。
    avr 为 做 AV同步_报告 用 同步。
    做 自检_断言值 用 「AV采样率=44100」、做 字典取 用 avr、「采样率」、「44100」。
    msr 为 做 媒体管线_结构化报告。
    做 自检_断言 用 「媒体报告是字典」、msr 不等于 空。
    做 打印文本 用 做 字符串拼接 用 「[自检] M13媒体管线: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M14_引擎合同 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M14 引擎合同(smoke+报告)...」。
    smoke 为 做 引擎_真实场景smoke。
    做 自检_断言 用 「引擎smoke非空」、smoke 不等于 空。
    el 为 做 字典取 用 smoke、「engine_loop」。
    做 自检_断言 用 「有engine_loop」、el 不等于 空。
    做 自检_断言 用 「有ECS实体数」、做 字典取 用 el、「ECS_实体数」 不等于 空。
    做 自检_断言 用 「有ECS组件数」、做 字典取 用 el、「ECS_组件数」 不等于 空。
    fb 为 做 字典取 用 smoke、「frame_budget」。
    做 自检_断言 用 「有frame_budget」、fb 不等于 空。
    做 自检_断言值 用 「帧率目标=60」、做 字典取 用 fb、「目标帧率」、「60」。
    做 自检_断言值 用 「总帧数=10」、做 字典取 用 fb、「总帧数」、「10」。
    ap 为 做 字典取 用 smoke、「asset_pipeline」。
    做 自检_断言 用 「有asset_pipeline」、ap 不等于 空。
    做 自检_断言 用 「有加载次数」、做 字典取 用 ap、「加载次数」 不等于 空。
    做 自检_断言 用 「有缓存命中」、做 字典取 用 ap、「缓存命中」 不等于 空。
    gtr 为 做 字典取 用 smoke、「gameplay_trace」。
    做 自检_断言 用 「有gameplay_trace」、gtr 不等于 空。
    做 自检_断言 用 「trace条目>0」、做 字典取 用 gtr、「追踪条目数」 不等于 「0」。
    做 自检_断言 用 「trace输入>0」、做 字典取 用 gtr、「输入事件数」 不等于 「0」。
    做 自检_断言 用 「trace AI>0」、做 字典取 用 gtr、「AI事件数」 不等于 「0」。
    做 自检_断言 用 「trace渲染>0」、做 字典取 用 gtr、「渲染事件数」 不等于 「0」。
    做 自检_断言 用 「场景节点>0」、做 字典取 用 smoke、「场景节点数」 不等于 「0」。
    做 自检_断言值 用 「帧循环固定=0」、帧循环_固定、0。
    做 自检_断言值 用 「帧循环可变=1」、帧循环_可变、1。
    做 自检_断言值 用 「帧循环半固定=2」、帧循环_半固定、2。
    做 自检_断言值 用 「帧阶段输入=0」、帧阶段_输入、0。
    做 自检_断言值 用 「帧阶段AI=1」、帧阶段_AI、1。
    做 自检_断言值 用 「帧阶段物理=2」、帧阶段_物理、2。
    做 自检_断言值 用 「帧阶段动画=3」、帧阶段_动画、3。
    做 自检_断言值 用 「帧阶段渲染=4」、帧阶段_渲染、4。
    做 自检_断言值 用 「帧阶段音频=5」、帧阶段_音频、5。
    做 自检_断言值 用 「帧阶段提交=6」、帧阶段_提交、6。
    做 自检_断言值 用 「资源未加载=0」、资源_未加载、0。
    做 自检_断言值 用 「资源加载中=1」、资源_加载中、1。
    做 自检_断言值 用 「资源已加载=2」、资源_已加载、2。
    做 自检_断言值 用 「资源错误=4」、资源_错误、4。
    elr 为 做 引擎循环_报告。
    做 自检_断言 用 「elr有gameplay_trace」、做 字典取 用 elr、「gameplay_trace」 不等于 空。
    ecsw 为 做 合同_ECS世界_创建。
    做 自检_断言值 用 「ECS统计tick初始=0」、ecsw 的 第 4 项 的 第 3 项、0。
    mgr 为 做 资源管理器_创建 用 1。
    做 自检_断言值 用 「资源缓存命中初始=0」、mgr 的 第 4 项 的 第 2 项、0。
    fb0 为 做 帧预算_创建 用 60。
    做 自检_断言值 用 「帧预算峰值初始=0」、fb0 的 第 5 项、0。
    vb0 为 做 显存预算_创建 用 1。
    做 自检_断言值 用 「显存峰值初始=0」、vb0 的 第 2 项、0。
    做 自检_断言值 用 「显存释放次数初始=0」、vb0 的 第 4 项、0。
    eng0 为 做 引擎_创建 用 60。
    做 自检_断言值 用 「引擎最大帧时初始=0」、做 引擎_帧统计 用 eng0 的 第 3 项、0。
    ec1 为 做 引擎合同_从Profile 用 「kernel-qemu」。
    做 自检_断言值 用 「qemu步长=33」、做 引擎合同_物理步长 用 ec1、33。
    ec2 为 做 引擎合同_从Profile 用 「linux-user」。
    做 自检_断言值 用 「user步长=16」、做 引擎合同_物理步长 用 ec2、16。
    做 打印文本 用 做 字符串拼接 用 「[自检] M14引擎合同: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M15_AI宿主 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M15 AI宿主接入...」。
    做 自检_断言值 用 「宿主未知=0」、宿主_未知、0。
    做 自检_断言值 用 「宿主Claude=1」、宿主_Claude_Code、1。
    做 自检_断言值 用 「宿主Windsurf=2」、宿主_Windsurf、2。
    做 自检_断言值 用 「宿主Cursor=3」、宿主_Cursor、3。
    做 自检_断言值 用 「宿主反重力=4」、宿主_反重力、4。
    做 自检_断言值 用 「宿主CLI=5」、宿主_CLI、5。
    做 自检_断言值 用 「宿主名Claude」、做 宿主名 用 1、「Claude Code」。
    做 自检_断言值 用 「宿主名Windsurf」、做 宿主名 用 2、「Windsurf」。
    做 自检_断言值 用 「宿主名Cursor」、做 宿主名 用 3、「Cursor」。
    做 自检_断言值 用 「宿主名反重力」、做 宿主名 用 4、「反重力」。
    做 自检_断言值 用 「宿主名CLI」、做 宿主名 用 5、「CLI」。
    做 自检_断言值 用 「宿主名未知」、做 宿主名 用 0、「unknown」。
    做 宿主_接入 用 宿主_Windsurf、「Codeium」、「stdio」、「claude-3.5」、30000、100。
    做 自检_断言值 用 「宿主类型=Windsurf」、_宿主_类型、宿主_Windsurf。
    做 自检_断言值 用 「宿主厂商」、_宿主_厂商、「Codeium」。
    做 自检_断言值 用 「宿主传输」、_宿主_传输、「stdio」。
    做 自检_断言值 用 「宿主模型」、_宿主_模型身份、「claude-3.5」。
    做 自检_断言值 用 「宿主超时=30000」、_宿主_超时预算、30000。
    做 自检_断言值 用 「宿主配额=100」、_宿主_配额预算、100。
    做 宿主_设置工具白名单 用 一组：「read_file」、「edit」、「grep_search」。
    做 自检_断言 用 「允许read_file」、做 宿主_工具允许 用 「read_file」。
    做 自检_断言 用 「允许edit」、做 宿主_工具允许 用 「edit」。
    做 自检_断言 用 「不允许rm」、做 宿主_工具允许 用 「rm」 等于 假。
    做 自检_断言 用 「不允许deploy」、做 宿主_工具允许 用 「deploy」 等于 假。
    _patch_队列 为 一组：（空）。
    _patch_已批准 为 0。
    _patch_已拒绝 为 0。
    做 patch_提交 用 「编译流水线.nova」、「修复性能」、「Windsurf」。
    做 patch_提交 用 「效果系统.nova」、「新增审计」、「Windsurf」。
    做 自检_断言值 用 「patch队列=2」、做 计数 用 _patch_队列、2。
    做 自检_断言值 用 「patch0状态=待审」、_patch_队列 的 第 0 项 的 第 3 项、「待审」。
    做 patch_批准 用 0。
    做 自检_断言值 用 「patch0已批准」、_patch_队列 的 第 0 项 的 第 3 项、「已批准」。
    做 自检_断言值 用 「已批准=1」、_patch_已批准、1。
    做 patch_拒绝 用 1。
    做 自检_断言值 用 「patch1已拒绝」、_patch_队列 的 第 1 项 的 第 3 项、「已拒绝」。
    做 自检_断言值 用 「已拒绝=1」、_patch_已拒绝、1。
    做 审计_记录 用 「test_op」、「自检」、「审计测试」。
    做 自检_断言 用 「审计日志>0」、做 审计_日志数 大于 0。
    做 审计_记录 用 「test_op2」、「自检」、「二次审计」。
    做 自检_断言 用 「审计日志>1」、做 审计_日志数 大于 1。
    _bundle_列表 为 一组：（空）。
    _bundle_下一ID 为 1。
    bundle 为 做 context_bundle_创建 用 宿主_CLI、一组：「编译器/分析」、「修复bug」。
    做 自检_断言值 用 「bundle ID=1」、bundle 的 第 0 项、1。
    做 自检_断言值 用 「bundle宿主=CLI」、bundle 的 第 1 项、宿主_CLI。
    做 bundle_添加变更 用 bundle、「flow.nova」、「modify」。
    做 bundle_添加变更 用 bundle、「effect.nova」、「modify」。
    做 自检_断言值 用 「bundle变更=2」、做 计数 用 bundle 的 第 3 项、2。
    做 bundle_添加约束 用 bundle、「no_gc」、「真」。
    做 自检_断言值 用 「bundle约束=1」、做 计数 用 bundle 的 第 5 项、1。
    做 自检_断言值 用 「任务创建=0」、任务_创建、0。
    做 自检_断言值 用 「任务校验中=1」、任务_校验中、1。
    做 自检_断言值 用 「任务校验通过=2」、任务_校验通过、2。
    做 自检_断言值 用 「任务校验失败=3」、任务_校验失败、3。
    做 自检_断言值 用 「任务待批准=4」、任务_待批准、4。
    做 自检_断言值 用 「任务已批准=5」、任务_已批准、5。
    做 自检_断言值 用 「任务已拒绝=6」、任务_已拒绝、6。
    做 自检_断言值 用 「任务已应用=7」、任务_已应用、7。
    _task_列表 为 一组：（空）。
    task 为 做 structured_task_创建 用 bundle。
    做 自检_断言值 用 「task初始状态=创建」、task 的 第 2 项、任务_创建。
    agr 为 做 AI网关_报告。
    做 自检_断言值 用 「agr宿主=Windsurf」、做 字典取 用 agr、「宿主类型」、「Windsurf」。
    做 自检_断言值 用 「agr传输=stdio」、做 字典取 用 agr、「传输」、「stdio」。
    acr 为 做 AI上下文包_报告。
    做 自检_断言值 用 「acr bundle=1」、做 字典取 用 acr、「bundle总数」、「1」。
    apr 为 做 AI补丁评审_报告。
    做 自检_断言值 用 「apr task=1」、做 字典取 用 apr、「task总数」、「1」。
    atr 为 做 AI工具审计_报告。
    做 自检_断言值 用 「atr宿主=Windsurf」、做 字典取 用 atr、「宿主类型」、「Windsurf」。
    做 自检_断言值 用 「atr白名单=3」、做 字典取 用 atr、「工具白名单数」、「3」。
    做 打印文本 用 做 字符串拼接 用 「[自检] M15 AI宿主: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M10_性能报告 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M10 性能报告...」。
    pr 为 做 性能_报告。
    做 自检_断言 用 「perf当前路径!=空」、做 字典取 用 pr、「当前路径」 不等于 空。
    做 自检_断言 用 「perf有AOT调用数」、做 字典取 用 pr、「AOT调用数」 不等于 空。
    jcr 为 做 即时编译_覆盖率报告。
    做 自检_断言 用 「JIT总opcode!=空」、做 字典取 用 jcr、「总opcode」 不等于 空。
    做 自检_断言 用 「JIT已实现!=空」、做 字典取 用 jcr、「已实现opcode」 不等于 空。
    做 自检_断言 用 「JIT覆盖率!=空」、做 字典取 用 jcr、「覆盖率%」 不等于 空。
    aer 为 做 提前编译_执行报告。
    做 自检_断言 用 「AOT已编译!=空」、做 字典取 用 aer、「AOT已编译」 不等于 空。
    做 自检_断言 用 「AOT函数数!=空」、做 字典取 用 aer、「AOT函数数」 不等于 空。
    做 热点_记录检测。
    做 热点_记录检测。
    做 热点_记录命中。
    做 热点_记录编译。
    hr 为 做 热点_报告。
    做 自检_断言 用 「热点检测次>0」、做 字典取 用 hr、「检测次数」 不等于 「0」。
    做 自检_断言 用 「热点命中次>0」、做 字典取 用 hr、「命中次数」 不等于 「0」。
    做 自检_断言 用 「热点编译次>0」、做 字典取 用 hr、「编译次数」 不等于 「0」。
    做 自检_断言 用 「热点有命中率」、做 字典取 用 hr、「命中率%」 不等于 空。
    做 自检_断言 用 「热点有阈值」、做 字典取 用 hr、「热点阈值」 不等于 空。
    det 为 做 热点检测器_创建。
    做 热点检测器_计数 用 det、1、100。
    做 热点检测器_计数 用 det、1、101。
    做 热点检测器_计数 用 det、1、102。
    做 热点检测器_衰减 用 det、50。
    记录后 为 做 热点检测器_查询 用 det、1。
    做 自检_断言 用 「衰减后计数<3」、记录后 不等于 空 并且 做 热点_计数 用 记录后 小于 3。
    做 热点检测器_计数 用 det、2、200。
    做 热点检测器_计数 用 det、2、201。
    做 热点检测器_计数 用 det、2、202。
    做 热点检测器_计数 用 det、2、203。
    做 热点检测器_计数 用 det、2、204。
    topn 为 做 热点检测器_TopN 用 det、2。
    做 自检_断言 用 「TopN结果有值」、做 计数 用 topn 大于 0。
    做 打印文本 用 做 字符串拼接 用 「[自检] M10性能报告: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M11_验证报告 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M11 验证报告...」。
    fvr 为 做 形式化验证_报告。
    做 自检_断言 用 「fvr有验证总数」、做 字典取 用 fvr、「验证总数」 不等于 空。
    做 自检_断言 用 「fvr有通过数」、做 字典取 用 fvr、「通过数」 不等于 空。
    er 为 做 效果系统_报告。
    做 自检_断言 用 「er有效果函数数」、做 字典取 用 er、「效果函数数」 不等于 空。
    做 自检_断言 用 「er有效果模块数」、做 字典取 用 er、「效果模块数」 不等于 空。
    cr 为 做 能力系统_报告。
    做 自检_断言 用 「cr有授权模块数」、做 字典取 用 cr、「授权模块数」 不等于 空。
    做 打印文本 用 做 字符串拼接 用 「[自检] M11验证报告: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M7_语义报告 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M7 语义报告...」。
    sr 为 做 语义_报告。
    做 自检_断言 用 「sr有推断总数」、做 字典取 用 sr、「推断总数」 不等于 空。
    做 自检_断言 用 「sr有函数类型数」、做 字典取 用 sr、「函数类型数」 不等于 空。
    der 为 做 动态逃逸_报告。
    做 自检_断言 用 「der有逃逸总数」、做 字典取 用 der、「逃逸总数」 不等于 空。
    做 自检_断言 用 「der有阈值」、做 字典取 用 der、「阈值」 不等于 空。
    做 自检_断言 用 「der有门控通过」、做 字典取 用 der、「门控通过」 不等于 空。
    ccr 为 做 约束冲突_报告。
    做 自检_断言 用 「ccr有约束函数数」、做 字典取 用 ccr、「约束域_函数数」 不等于 空。
    做 自检_断言 用 「ccr有冲突总数」、做 字典取 用 ccr、「冲突总数」 不等于 空。
    做 打印文本 用 做 字符串拼接 用 「[自检] M7语义报告: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M9_内存报告 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M9 内存profile报告...」。
    mpr 为 做 内存配置_报告 用 0。
    做 自检_断言 用 「mpr有kernel预算」、做 字典取 用 mpr、「kernel_预算MB」 不等于 空。
    做 自检_断言 用 「mpr有ai预算」、做 字典取 用 mpr、「ai_预算MB」 不等于 空。
    做 自检_断言 用 「mpr有developer预算」、做 字典取 用 mpr、「developer_预算MB」 不等于 空。
    做 打印文本 用 做 字符串拼接 用 「[自检] M9内存报告: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_M8_后端报告 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] M8 后端报告...」。
    做 自检_断言 用 「M8后端存在」、真。
    做 打印文本 用 做 字符串拼接 用 「[自检] M8后端报告: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

自检_项目合同 接受 零参：
    基数 为 _自检_通过。
    做 打印 用 「[自检] 项目合同(引擎/媒体)...」。
    ec1 为 做 引擎合同_从Profile 用 「kernel-qemu」。
    做 自检_断言值 用 「qemu引擎步长=33」、做 引擎合同_物理步长 用 ec1、33。
    ec2 为 做 引擎合同_从Profile 用 「linux-user」。
    做 自检_断言值 用 「user引擎步长=16」、做 引擎合同_物理步长 用 ec2、16。
    做 引擎合同_验证 用 一组：（空）。
    esr 为 做 引擎合同_结构化报告。
    做 自检_断言 用 「引擎结构化报告!=空」、esr 不等于 空。
    mc1 为 做 媒体合同_从Profile 用 「kernel-qemu」。
    做 自检_断言值 用 「内核媒体=PCM」、做 媒体合同_格式 用 mc1、媒体格式_PCM。
    mc2 为 做 媒体合同_从Profile 用 「linux-user」。
    做 自检_断言值 用 「用户媒体=H264」、做 媒体合同_格式 用 mc2、媒体格式_H264。
    阶段_解码 为 做 管线阶段_创建 用 「vertex」、阶段_顶点、3、4、假。
    做 自检_断言值 用 「管线阶段名」、阶段_解码 的 第 0 项、「vertex」。
    做 自检_断言值 用 「管线阶段类型=顶点」、阶段_解码 的 第 1 项、阶段_顶点。
    做 自检_断言值 用 「阶段顶点=0」、阶段_顶点、0。
    做 自检_断言值 用 「阶段片段=1」、阶段_片段、1。
    做 自检_断言值 用 「阶段几何=2」、阶段_几何、2。
    做 自检_断言值 用 「阶段曲面=3」、阶段_曲面细分、3。
    做 自检_断言值 用 「阶段计算=4」、阶段_计算、4。
    做 打印文本 用 做 字符串拼接 用 「[自检] 项目合同: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过 减 基数、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数 减 基数 减 _自检_失败、「 通过」。
完。

运行全部自检 接受 零参：
    _自检_通过 为 0。
    _自检_失败 为 0。
    _自检_总数 为 0。
    做 打印 用 「═══════════════════════════════════════」。
    做 打印 用 「  Nova 运行时模块自检 (L3级证据)」。
    做 打印 用 「═══════════════════════════════════════」。
    做 自检_代码生成。
    做 自检_M7_语义。
    做 自检_M8_ABI。
    做 自检_M9_内存。
    做 自检_M10_JIT。
    做 自检_M11_安全。
    做 自检_M12_渲染。
    做 自检_M13_音频。
    做 自检_M14_引擎。
    做 自检_M15_宿主。
    做 自检_M16_AI。
    做 自检_C1_热更新。
    做 自检_跨语言。
    做 打印 用 「═══════════════════════════════════════」。
    做 打印文本 用 做 字符串拼接 用 「  总计: 」、做 字符串拼接 用 做 转字符串 用 _自检_通过、做 字符串拼接 用 「/」、做 字符串拼接 用 做 转字符串 用 _自检_总数、做 字符串拼接 用 「 通过, 」、做 字符串拼接 用 做 转字符串 用 _自检_失败、「 失败」。
    若 _自检_失败 等于 0：
        做 打印 用 「  ✓ 全部自检通过」。
    否则：
        做 打印文本 用 做 字符串拼接 用 「  ✗ 」、做 字符串拼接 用 做 转字符串 用 _自检_失败、「 项失败」。
    完。
    做 打印 用 「═══════════════════════════════════════」。
完。

本章 收束。
