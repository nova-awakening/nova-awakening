《调试器》

引用《..·..·标准库·基础》。

引用《..·..·自举兼容桥》。

引用《..·热更新·热更新管理》。

引用《..·解释器·栈式VM》。

调试_继续 为 0。

调试_单步 为 1。

调试_步过 为 2。

调试_步出 为 3。

调试_查看变量 为 4。

调试_设断点 为 5。

调试_删断点 为 6。

调试_查看栈 为 7。

调试_修改变量 为 8。

断点_行 为 0。

断点_函数 为 1。

断点_条件 为 2。

断点_一次性 为 3。

断点_创建 接受 ID、类型、位置：
    返还 一组：ID、类型、位置、空、真、0。
完。

断点_ID 接受 bp：
    返还 bp 的 第 0 项。
完。

调试器_断点_启用 接受 bp：
    返还 bp 的 第 4 项。
完。

调试器_断点_命中 接受 bp：
    bp 的 第 5 项 为 bp 的 第 5 项 加 1。
完。

调试器_调试器_创建 接受 零参：
    返还 一组：一组：（空）、1、假、0、「」、空、一组：（空）、一组：（空）、做 字典、一组：（空）。
完。

调试器_调试器_添加断点 接受 调试器、类型、位置：
    bp 为 做 断点_创建 用 调试器 的 第 1 项、类型、位置。
    做 追加 用 调试器 的 第 0 项、bp。
    调试器 的 第 1 项 为 调试器 的 第 1 项 加 1。
    做 打印 用 做 字符串拼接 用 「断点 #」、做 字符串拼接 用 做 转字符串 用 bp 的 第 0 项、做 字符串拼接 用 「 设置于 」、做 转字符串 用 位置。
    返还 bp 的 第 0 项。
完。

调试器_调试器_删除断点 接受 调试器、ID：
    可变 新列表 为 一组：（空）。
    可变 i 为 0。
    可变 已删 为 假。
    只要 i 小于 做 计数 用 调试器 的 第 0 项：
        若 做 断点_ID 用 调试器 的 第 0 项 的 第 i 项 不等于 ID：
            做 追加 用 新列表、调试器 的 第 0 项 的 第 i 项。
        否则：
            已删 为 真。
        完。
        i 为 i 加 1。
    完。
    调试器 的 第 0 项 为 新列表。
    返还 已删。
完。

调试器_启用断点 接受 调试器、ID、启用：
    可变 i 为 0。
    只要 i 小于 做 计数 用 调试器 的 第 0 项：
        若 做 断点_ID 用 调试器 的 第 0 项 的 第 i 项 等于 ID：
            调试器 的 第 0 项 的 第 i 项 的 第 4 项 为 启用。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

调试器_列出断点 接受 调试器：
    做 打印 用 做 字符串拼接 用 「═══ 断点列表 (」、做 字符串拼接 用 做 转字符串 用 做 计数 用 调试器 的 第 0 项、「个) ═══」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调试器 的 第 0 项：
        bp 为 调试器 的 第 0 项 的 第 i 项。
        可变 状态 为 「启用」。
        若 非 bp 的 第 4 项：
            状态 为 「禁用」。
        完。
        做 打印 用 做 字符串拼接 用 「 #」、做 字符串拼接 用 做 转字符串 用 bp 的 第 0 项、做 字符串拼接 用 「 行」、做 字符串拼接 用 做 转字符串 用 bp 的 第 2 项、做 字符串拼接 用 「 [」、做 字符串拼接 用 状态、做 字符串拼接 用 「] 命中」、做 字符串拼接 用 做 转字符串 用 bp 的 第 5 项、「次」。
        i 为 i 加 1。
    完。
完。

调试器_检查 接受 调试器、行号、函数名：
    调试器 的 第 3 项 为 行号。
    调试器 的 第 4 项 为 函数名。
    若 调试器 的 第 2 项：
        返还 真。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调试器 的 第 0 项：
        bp 为 调试器 的 第 0 项 的 第 i 项。
        若 做 断点_启用 用 bp：
            若 bp 的 第 1 项 等于 断点_行 并且 bp 的 第 2 项 等于 行号：
                做 断点_命中 用 bp。
                若 bp 的 第 1 项 等于 断点_一次性：
                    做 调试器_删除断点 用 调试器、bp 的 第 0 项。
                完。
                返还 真。
            完。
            若 bp 的 第 1 项 等于 断点_函数 并且 bp 的 第 2 项 等于 函数名：
                做 断点_命中 用 bp。
                返还 真。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

调试器_查看变量 接受 _调试器、vm、名称：
    帧 为 做 VM_当前帧 用 vm。
    若 帧 不等于 空：
        局部表 为 做 字典获取 用 做 VM_全局表 用 vm、「__调试局部变量__」。
        若 局部表 不等于 空：
            局部值 为 做 字典获取 用 局部表、名称。
            若 局部值 不等于 空：
                做 打印 用 做 字符串拼接 用 「 」、做 字符串拼接 用 名称、做 字符串拼接 用 「 = 」、做 转字符串 用 局部值。
                返还 局部值。
            完。
        完。
    完。
    值 为 做 字典获取 用 做 VM_全局表 用 vm、名称。
    若 值 不等于 空：
        做 打印 用 做 字符串拼接 用 「 」、做 字符串拼接 用 名称、做 字符串拼接 用 「 = 」、做 转字符串 用 值。
        返还 值。
    完。
    做 打印 用 做 字符串拼接 用 「 未找到变量: 」、名称。
    返还 空。
完。

调试器_调试器_调用栈 接受 _调试器、vm：
    做 打印 用 「═══ 调用栈 ═══」。
    可变 i 为 vm 的 第 3 项 减 1。
    可变 层 为 0。
    只要 i 大于或等于 0：
        帧 为 vm 的 第 2 项 的 第 i 项。
        做 打印 用 做 字符串拼接 用 「 #」、做 字符串拼接 用 做 转字符串 用 层、做 字符串拼接 用 「 」、做 字符串拼接 用 帧 的 第 3 项、做 字符串拼接 用 「 (PC=」、做 字符串拼接 用 做 转字符串 用 帧 的 第 0 项、「)」。
        i 为 i 减 1。
        层 为 层 加 1。
    完。
    做 打印 用 做 字符串拼接 用 「 #」、做 字符串拼接 用 做 转字符串 用 层、「 <__主程序__>」。
完。

调试器_调试器_添加监视 接受 调试器、名称：
    做 追加 用 调试器 的 第 6 项、名称。
完。

调试器_显示监视 接受 调试器、vm：
    若 做 计数 用 调试器 的 第 6 项 等于 0：
        返还。
    完。
    做 打印 用 「── 监视变量 ──」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调试器 的 第 6 项：
        做 调试器_查看变量 用 调试器、vm、调试器 的 第 6 项 的 第 i 项。
        i 为 i 加 1。
    完。
完。

调试器_注册JIT映射 接受 调试器、块ID、机器码偏移、行号：
    键 为 做 字符串拼接 用 做 转字符串 用 块ID、做 字符串拼接 用 「:」、做 转字符串 用 机器码偏移。
    做 字典设置 用 调试器 的 第 8 项、键、行号。
完。

调试器_查JIT映射 接受 调试器、块ID、机器码偏移：
    键 为 做 字符串拼接 用 做 转字符串 用 块ID、做 字符串拼接 用 「:」、做 转字符串 用 机器码偏移。
    返还 做 字典获取 用 调试器 的 第 8 项、键。
完。

调试器_开始单步 接受 调试器：
    调试器 的 第 2 项 为 真。
完。

调试器_停止单步 接受 调试器：
    调试器 的 第 2 项 为 假。
完。

调试器_实时修改 接受 _调试器、热更管理器、新源码、vm：
    做 打印 用 「调试器: 实时修改代码...」。
    vm 的 第 5 项 为 VM_状态_暂停。
    做 热更管理器_更新 用 热更管理器、新源码、vm。
    vm 的 第 5 项 为 VM_状态_运行。
    做 打印 用 「调试器: 代码已更新，继续执行」。
完。

可变 _条件断点_行 为 一组：（空）。

可变 _条件断点_条件 为 一组：（空）。

条件断点_添加 接受 行号、条件表达式：
    做 追加 用 _条件断点_行、行号。
    做 追加 用 _条件断点_条件、条件表达式。
完。

条件断点_检查 接受 行号、变量值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _条件断点_行：
        若 _条件断点_行 的 第 i 项 等于 行号：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

可变 _监视_名 为 一组：（空）。

可变 _监视_值 为 一组：（空）。

监视_添加 接受 变量名：
    做 追加 用 _监视_名、变量名。
    做 追加 用 _监视_值、0。
完。

监视_更新 接受 变量名、值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _监视_名：
        若 _监视_名 的 第 i 项 等于 变量名：
            _监视_值 的 第 i 项 为 值。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

监视_列表 接受 零参：
    返还 一组：_监视_名、_监视_值。
完。

本章 收束。
