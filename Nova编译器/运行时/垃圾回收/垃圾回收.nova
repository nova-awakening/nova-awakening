《垃圾回收》

引用《..·..·标准库·基础》。

引用《..·内存管理·内存profile》。

GC_白 为 0。

GC_灰 为 1。

GC_黑 为 2。

GC_创建 接受 容量、阈值：
    若 容量 小于或等于 0：
        容量 为 64。
    完。
    若 阈值 小于或等于 0：
        阈值 为 容量 乘 8。
    完。
    可变 对象表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 对象表、一组：GC_白、0、一组：（空）。
        i 为 i 加 1。
    完。
    返还 一组：对象表、一组：（空）、一组：（空）、0、阈值。
完。

GC_分配 接受 gc、大小：
    若 大小 小于或等于 0：
        大小 为 1。
    完。
    若 gc 的 第 3 项 加 大小 大于 gc 的 第 4 项：
        做 GC_收集 用 gc。
    完。
    表 为 gc 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 表：
        若 表 的 第 i 项 的 第 4 项 等于 0：
            表 的 第 i 项 的 第 0 项 为 GC_白。
            表 的 第 i 项 的 第 1 项 为 大小。
            表 的 第 i 项 的 第 2 项 为 一组：（空）。
            表 的 第 i 项 的 第 3 项 为 0。
            表 的 第 i 项 的 第 4 项 为 1。
            gc 的 第 3 项 为 gc 的 第 3 项 加 大小。
            若 做 内存profile_当前 不等于 空：
                做 内存度量_记录分配 用 做 内存profile_当前、大小。
            完。
            返还 i。
        完。
        i 为 i 加 1。
    完。
    做 GC_收集 用 gc。
    i 为 0。
    只要 i 小于 做 计数 用 表：
        若 表 的 第 i 项 的 第 4 项 等于 0：
            表 的 第 i 项 的 第 0 项 为 GC_白。
            表 的 第 i 项 的 第 1 项 为 大小。
            表 的 第 i 项 的 第 2 项 为 一组：（空）。
            表 的 第 i 项 的 第 3 项 为 0。
            表 的 第 i 项 的 第 4 项 为 1。
            gc 的 第 3 项 为 gc 的 第 3 项 加 大小。
            若 做 内存profile_当前 不等于 空：
                做 内存度量_记录分配 用 做 内存profile_当前、大小。
            完。
            返还 i。
        完。
        i 为 i 加 1。
    完。
    若 做 内存profile_当前 不等于 空：
        做 内存度量_记录OOM 用 做 内存profile_当前。
    完。
    返还 负 1。
完。

GC_添加引用 接受 gc、来源、目标：
    若 来源 大于或等于 0 并且 来源 小于 做 计数 用 gc 的 第 0 项 并且 gc 的 第 0 项 的 第 来源 项 的 第 4 项 等于 1 并且 目标 大于或等于 0 并且 目标 小于 做 计数 用 gc 的 第 0 项：
        做 追加 用 gc 的 第 0 项 的 第 来源 项 的 第 2 项、目标。
    完。
完。

GC_设根 接受 gc、根列表：
    gc 的 第 1 项 为 根列表。
完。

GC_加根 接受 gc、对象id：
    做 追加 用 gc 的 第 1 项、对象id。
完。

GC_标记阶段 接受 gc：
    可变 i 为 0。
    只要 i 小于 做 计数 用 gc 的 第 0 项：
        若 gc 的 第 0 项 的 第 i 项 的 第 4 项 等于 1：
            gc 的 第 0 项 的 第 i 项 的 第 0 项 为 GC_白。
        完。
        i 为 i 加 1。
    完。
    gc 的 第 2 项 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 gc 的 第 1 项：
        可变 rid 为 gc 的 第 1 项 的 第 i 项。
        若 rid 大于或等于 0 并且 rid 小于 做 计数 用 gc 的 第 0 项 并且 gc 的 第 0 项 的 第 rid 项 的 第 4 项 等于 1 并且 gc 的 第 0 项 的 第 rid 项 的 第 0 项 等于 GC_白：
            gc 的 第 0 项 的 第 rid 项 的 第 0 项 为 GC_灰。
            做 追加 用 gc 的 第 2 项、rid。
        完。
        i 为 i 加 1。
    完。
    可变 头 为 0。
    只要 头 小于 做 计数 用 gc 的 第 2 项：
        可变 当前id 为 gc 的 第 2 项 的 第 头 项。
        头 为 头 加 1。
        可变 引用列表 为 gc 的 第 0 项 的 第 当前id 项 的 第 2 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 引用列表：
            可变 ref 为 引用列表 的 第 j 项。
            若 ref 大于或等于 0 并且 ref 小于 做 计数 用 gc 的 第 0 项 并且 gc 的 第 0 项 的 第 ref 项 的 第 4 项 等于 1 并且 gc 的 第 0 项 的 第 ref 项 的 第 0 项 等于 GC_白：
                gc 的 第 0 项 的 第 ref 项 的 第 0 项 为 GC_灰。
                做 追加 用 gc 的 第 2 项、ref。
            完。
            j 为 j 加 1。
        完。
        gc 的 第 0 项 的 第 当前id 项 的 第 0 项 为 GC_黑。
    完。
完。

GC_增量标记 接受 gc、步数：
    可变 已处理 为 0。
    可变 头 为 0。
    只要 头 小于 做 计数 用 gc 的 第 2 项 并且 已处理 小于 步数：
        可变 当前id 为 gc 的 第 2 项 的 第 头 项。
        头 为 头 加 1。
        若 gc 的 第 0 项 的 第 当前id 项 的 第 0 项 等于 GC_灰：
            可变 引用列表 为 gc 的 第 0 项 的 第 当前id 项 的 第 2 项。
            可变 j 为 0。
            只要 j 小于 做 计数 用 引用列表：
                可变 ref 为 引用列表 的 第 j 项。
                若 ref 大于或等于 0 并且 ref 小于 做 计数 用 gc 的 第 0 项 并且 gc 的 第 0 项 的 第 ref 项 的 第 4 项 等于 1 并且 gc 的 第 0 项 的 第 ref 项 的 第 0 项 等于 GC_白：
                    gc 的 第 0 项 的 第 ref 项 的 第 0 项 为 GC_灰。
                    做 追加 用 gc 的 第 2 项、ref。
                完。
                j 为 j 加 1。
            完。
            gc 的 第 0 项 的 第 当前id 项 的 第 0 项 为 GC_黑。
            已处理 为 已处理 加 1。
        完。
    完。
    返还 已处理。
完。

GC_清除阶段 接受 gc：
    可变 释放数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 gc 的 第 0 项：
        若 gc 的 第 0 项 的 第 i 项 的 第 4 项 等于 1：
            若 gc 的 第 0 项 的 第 i 项 的 第 0 项 等于 GC_白：
                _释放字节 为 gc 的 第 0 项 的 第 i 项 的 第 1 项。
                gc 的 第 3 项 为 gc 的 第 3 项 减 _释放字节。
                若 做 内存profile_当前 不等于 空：
                    做 内存度量_记录释放 用 做 内存profile_当前、_释放字节。
                完。
                gc 的 第 0 项 的 第 i 项 的 第 1 项 为 0。
                gc 的 第 0 项 的 第 i 项 的 第 2 项 为 一组：（空）。
                gc 的 第 0 项 的 第 i 项 的 第 4 项 为 0。
                释放数 为 释放数 加 1。
            否则：
                gc 的 第 0 项 的 第 i 项 的 第 3 项 为 gc 的 第 0 项 的 第 i 项 的 第 3 项 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    gc 的 第 5 项 为 gc 的 第 5 项 加 1。
    gc 的 第 6 项 为 gc 的 第 6 项 加 释放数。
    若 gc 的 第 3 项 乘 2 大于 gc 的 第 4 项：
        gc 的 第 4 项 为 gc 的 第 3 项 乘 2。
    完。
    返还 释放数。
完。

GC_收集 接受 gc：
    若 做 内存profile_当前 不等于 空：
        做 内存度量_记录GC 用 做 内存profile_当前。
    完。
    做 GC_标记阶段 用 gc。
    返还 做 GC_清除阶段 用 gc。
完。

GC_存活数 接受 gc：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 gc 的 第 0 项：
        若 gc 的 第 0 项 的 第 i 项 的 第 4 项 等于 1：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

GC_总字节 接受 gc：
    返还 gc 的 第 3 项。
完。

GC_阈值 接受 gc：
    返还 gc 的 第 4 项。
完。

GC_次数 接受 gc：
    返还 gc 的 第 5 项。
完。

GC_总释放 接受 gc：
    返还 gc 的 第 6 项。
完。

GC_对象代龄 接受 gc、id：
    若 id 大于或等于 0 并且 id 小于 做 计数 用 gc 的 第 0 项 并且 gc 的 第 0 项 的 第 id 项 的 第 4 项 等于 1：
        返还 gc 的 第 0 项 的 第 id 项 的 第 3 项。
    完。
    返还 负 1。
完。

GC_需要收集 接受 gc：
    返还 gc 的 第 3 项 大于或等于 gc 的 第 4 项。
完。

GC_添加弱引用 接受 _gc、弱表、源、目标：
    做 追加 用 弱表、一组：源、目标。
完。

GC_清理弱引用 接受 gc、弱表：
    可变 新表 为 一组：（空）。
    可变 清除数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 弱表：
        目标 为 弱表 的 第 i 项 的 第 1 项。
        若 目标 大于或等于 0 并且 目标 小于 做 计数 用 gc 的 第 0 项 并且 gc 的 第 0 项 的 第 目标 项 的 第 4 项 等于 1：
            做 追加 用 新表、弱表 的 第 i 项。
        否则：
            清除数 为 清除数 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 j 为 0。
    只要 j 小于 做 计数 用 新表：
        弱表 的 第 j 项 为 新表 的 第 j 项。
        j 为 j 加 1。
    完。
    只要 j 小于 做 计数 用 弱表：
        弱表 的 第 j 项 为 一组：负 1、负 1。
        j 为 j 加 1。
    完。
    返还 一组：做 计数 用 新表、清除数。
完。

GC_注册终结器 接受 终结表、对象id：
    做 追加 用 终结表、一组：对象id、0。
完。

GC_执行终结 接受 gc、终结表：
    可变 触发数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 终结表：
        oid 为 终结表 的 第 i 项 的 第 0 项。
        若 oid 大于或等于 0 并且 oid 小于 做 计数 用 gc 的 第 0 项：
            若 gc 的 第 0 项 的 第 oid 项 的 第 4 项 等于 0 并且 终结表 的 第 i 项 的 第 1 项 等于 0：
                终结表 的 第 i 项 的 第 1 项 为 1。
                触发数 为 触发数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 触发数。
完。

GC_分代收集 接受 gc、代龄阈值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 gc 的 第 0 项：
        若 gc 的 第 0 项 的 第 i 项 的 第 4 项 等于 1 并且 gc 的 第 0 项 的 第 i 项 的 第 3 项 小于 代龄阈值：
            gc 的 第 0 项 的 第 i 项 的 第 0 项 为 GC_白。
        完。
        i 为 i 加 1。
    完。
    gc 的 第 2 项 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 gc 的 第 1 项：
        rid 为 gc 的 第 1 项 的 第 i 项。
        若 rid 大于或等于 0 并且 rid 小于 做 计数 用 gc 的 第 0 项 并且 gc 的 第 0 项 的 第 rid 项 的 第 4 项 等于 1：
            若 gc 的 第 0 项 的 第 rid 项 的 第 0 项 等于 GC_白：
                gc 的 第 0 项 的 第 rid 项 的 第 0 项 为 GC_灰。
                做 追加 用 gc 的 第 2 项、rid。
            否则若 gc 的 第 0 项 的 第 rid 项 的 第 3 项 大于或等于 代龄阈值：
                做 追加 用 gc 的 第 2 项、rid。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 头 为 0。
    只要 头 小于 做 计数 用 gc 的 第 2 项：
        当前id 为 gc 的 第 2 项 的 第 头 项。
        头 为 头 加 1。
        引用列表 为 gc 的 第 0 项 的 第 当前id 项 的 第 2 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 引用列表：
            ref 为 引用列表 的 第 j 项。
            若 ref 大于或等于 0 并且 ref 小于 做 计数 用 gc 的 第 0 项 并且 gc 的 第 0 项 的 第 ref 项 的 第 4 项 等于 1 并且 gc 的 第 0 项 的 第 ref 项 的 第 0 项 等于 GC_白 并且 gc 的 第 0 项 的 第 ref 项 的 第 3 项 小于 代龄阈值：
                gc 的 第 0 项 的 第 ref 项 的 第 0 项 为 GC_灰。
                做 追加 用 gc 的 第 2 项、ref。
            完。
            j 为 j 加 1。
        完。
        若 gc 的 第 0 项 的 第 当前id 项 的 第 0 项 等于 GC_灰：
            gc 的 第 0 项 的 第 当前id 项 的 第 0 项 为 GC_黑。
        完。
    完。
    可变 释放数 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 gc 的 第 0 项：
        若 gc 的 第 0 项 的 第 i 项 的 第 4 项 等于 1 并且 gc 的 第 0 项 的 第 i 项 的 第 3 项 小于 代龄阈值 并且 gc 的 第 0 项 的 第 i 项 的 第 0 项 等于 GC_白：
            gc 的 第 3 项 为 gc 的 第 3 项 减 gc 的 第 0 项 的 第 i 项 的 第 1 项。
            gc 的 第 0 项 的 第 i 项 的 第 1 项 为 0。
            gc 的 第 0 项 的 第 i 项 的 第 2 项 为 一组：（空）。
            gc 的 第 0 项 的 第 i 项 的 第 4 项 为 0。
            释放数 为 释放数 加 1。
        否则若 gc 的 第 0 项 的 第 i 项 的 第 4 项 等于 1：
            gc 的 第 0 项 的 第 i 项 的 第 3 项 为 gc 的 第 0 项 的 第 i 项 的 第 3 项 加 1。
        完。
        i 为 i 加 1。
    完。
    gc 的 第 5 项 为 gc 的 第 5 项 加 1。
    gc 的 第 6 项 为 gc 的 第 6 项 加 释放数。
    返还 释放数。
完。

可变 _GC_裸机模式 为 假。

可变 _GC_bump_当前 为 0。

可变 _GC_bump_上限 为 0。

可变 _GC_bump_分配数 为 0。

GC_设置裸机模式 接受 启用、堆上限：
    _GC_裸机模式 为 启用。
    _GC_bump_当前 为 0。
    _GC_bump_上限 为 堆上限。
    _GC_bump_分配数 为 0。
完。

GC_是裸机模式 接受 零参：
    返还 _GC_裸机模式。
完。

GC_裸机分配 接受 大小：
    可变 对齐大小 为 大小 加 7 位与 0 减 8。
    若 对齐大小 小于 8：
        对齐大小 为 8。
    完。
    若 _GC_bump_当前 加 对齐大小 大于 _GC_bump_上限：
        返还 0 减 1。
    完。
    地址 为 _GC_bump_当前。
    _GC_bump_当前 为 _GC_bump_当前 加 对齐大小。
    _GC_bump_分配数 为 _GC_bump_分配数 加 1。
    返还 地址。
完。

GC_裸机_已用 接受 零参：
    返还 _GC_bump_当前。
完。

GC_裸机_剩余 接受 零参：
    返还 _GC_bump_上限 减 _GC_bump_当前。
完。

GC_裸机_分配数 接受 零参：
    返还 _GC_bump_分配数。
完。

GC_统一分配 接受 gc、大小：
    若 _GC_裸机模式：
        返还 做 GC_裸机分配 用 大小。
    完。
    返还 做 GC_分配 用 gc、大小。
完。

GC_统一收集 接受 gc：
    若 _GC_裸机模式：
        返还 0。
    完。
    返还 做 GC_收集 用 gc。
完。

GC_裸机报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「裸机模式」、做 转字符串 用 _GC_裸机模式。
    做 字典设 用 r、「bump已用」、做 转字符串 用 _GC_bump_当前。
    做 字典设 用 r、「bump上限」、做 转字符串 用 _GC_bump_上限。
    做 字典设 用 r、「bump分配数」、做 转字符串 用 _GC_bump_分配数。
    返还 r。
完。

内存profile_no_gc_kernel 为 1。

内存profile_bounded_ai 为 2。

内存profile_developer_host 为 3。

可变 _当前内存profile 为 内存profile_developer_host。

内存profile_设置 接受 p：
    _当前内存profile 为 p。
完。

内存profile_获取 接受 零参：
    返还 _当前内存profile。
完。

内存profile名 接受 p：
    若 p 等于 内存profile_no_gc_kernel：
        返还 「no_gc_kernel」。
    完。
    若 p 等于 内存profile_bounded_ai：
        返还 「bounded_ai_runtime」。
    完。
    若 p 等于 内存profile_developer_host：
        返还 「developer_host」。
    完。
    返还 「unknown」。
完。

内存profile_允许GC 接受 零参：
    返还 _当前内存profile 不等于 内存profile_no_gc_kernel。
完。

内存profile_允许全堆扩张 接受 零参：
    返还 _当前内存profile 等于 内存profile_developer_host。
完。

内存profile_允许诊断缓冲 接受 零参：
    返还 _当前内存profile 等于 内存profile_developer_host。
完。

可变 _内存预算_最大堆 为 0。

可变 _内存预算_最大常驻 为 0。

可变 _内存预算_启动 为 0。

可变 _内存预算_每帧 为 0。

可变 _内存预算_显存 为 0。

内存预算_设置 接受 最大堆、最大常驻、启动、每帧、显存：
    _内存预算_最大堆 为 最大堆。
    _内存预算_最大常驻 为 最大常驻。
    _内存预算_启动 为 启动。
    _内存预算_每帧 为 每帧。
    _内存预算_显存 为 显存。
完。

可变 _内存基准_冷启动 为 0。

可变 _内存基准_峰值RSS 为 0。

可变 _内存基准_稳态RSS 为 0。

可变 _内存基准_临时分配 为 0。

可变 _内存基准_碎片率 为 0。

内存基准_记录冷启动 接受 值：
    _内存基准_冷启动 为 值。
完。

内存基准_记录峰值 接受 值：
    _内存基准_峰值RSS 为 值。
完。

内存基准_记录稳态 接受 值：
    _内存基准_稳态RSS 为 值。
完。

内存基准_记录临时分配 接受 值：
    _内存基准_临时分配 为 值。
完。

内存基准_记录碎片率 接受 值：
    _内存基准_碎片率 为 值。
完。

内存基准_自动采集 接受 零参：
    _内存基准_峰值RSS 为 内存统计 的 第 3 项。
    _内存基准_稳态RSS 为 内存统计 的 第 4 项。
    _内存基准_临时分配 为 内存统计 的 第 2 项。
    若 内存统计 的 第 3 项 大于 0：
        _内存基准_碎片率 为 (内存统计 的 第 3 项 减 内存统计 的 第 4 项) 乘 100 除以 内存统计 的 第 3 项。
    完。
完。

内存基准_标记冷启动 接受 零参：
    _内存基准_冷启动 为 内存统计 的 第 4 项。
完。

内存profile_报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「profile」、做 内存profile名 用 _当前内存profile。
    做 字典设 用 r、「允许GC」、做 转字符串 用 做 内存profile_允许GC。
    做 字典设 用 r、「允许全堆扩张」、做 转字符串 用 做 内存profile_允许全堆扩张。
    做 字典设 用 r、「预算_最大堆」、做 转字符串 用 _内存预算_最大堆。
    做 字典设 用 r、「预算_最大常驻」、做 转字符串 用 _内存预算_最大常驻。
    做 字典设 用 r、「预算_启动」、做 转字符串 用 _内存预算_启动。
    做 字典设 用 r、「预算_每帧」、做 转字符串 用 _内存预算_每帧。
    做 字典设 用 r、「预算_显存」、做 转字符串 用 _内存预算_显存。
    做 字典设 用 r、「基准_冷启动」、做 转字符串 用 _内存基准_冷启动。
    做 字典设 用 r、「基准_峰值RSS」、做 转字符串 用 _内存基准_峰值RSS。
    做 字典设 用 r、「基准_稳态RSS」、做 转字符串 用 _内存基准_稳态RSS。
    做 字典设 用 r、「基准_临时分配」、做 转字符串 用 _内存基准_临时分配。
    做 字典设 用 r、「基准_碎片率」、做 转字符串 用 _内存基准_碎片率。
    返还 r。
完。

内存profile_打印报告 接受 零参：
    r 为 做 内存profile_报告。
    做 打印文本 用 做 字符串拼接 用 「[MEM-PROFILE] 」、做 字符串拼接 用 做 字典取 用 r、「profile」、做 字符串拼接 用 「 GC=」、做 字符串拼接 用 做 字典取 用 r、「允许GC」、做 字符串拼接 用 「 峰值=」、做 字符串拼接 用 做 字典取 用 r、「基准_峰值RSS」、做 字符串拼接 用 「 稳态=」、做 字典取 用 r、「基准_稳态RSS」。
完。

本章 收束。
