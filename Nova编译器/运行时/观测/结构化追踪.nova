《结构化追踪》

引用《..·..·标准库·基础》。

追踪_函数入 为 1。

追踪_函数出 为 2。

追踪_系统调用 为 3。

追踪_中断 为 4。

追踪_异常 为 5。

追踪_热更新 为 6。

追踪_内存分配 为 7。

追踪_内存释放 为 8。

追踪_调度切换 为 9。

追踪_设备事件 为 10。

追踪_模态切换 为 11。

可变 _追踪_缓冲 为 一组：（空）。

可变 _追踪_最大容量 为 4096。

可变 _追踪_启用 为 真。

可变 _追踪_总计数 为 0。

可变 _追踪_丢弃数 为 0。

追踪_设置容量 接受 容量：
    _追踪_最大容量 为 容量。
完。

追踪_启用 接受 零参：
    _追踪_启用 为 真。
完。

追踪_禁用 接受 零参：
    _追踪_启用 为 假。
完。

追踪_记录 接受 类型、CPU线程、模块、函数名、行号、数据：
    若 _追踪_启用 等于 假：
        返还 空。
    完。
    _追踪_总计数 为 _追踪_总计数 加 1。
    若 做 计数 用 _追踪_缓冲 大于或等于 _追踪_最大容量：
        _追踪_缓冲 的 第 0 项 为 _追踪_缓冲 的 第 做 计数 用 _追踪_缓冲 减 1 项。
        _追踪_丢弃数 为 _追踪_丢弃数 加 1。
        返还 空。
    完。
    做 追加 用 _追踪_缓冲、一组：做 时间、类型、CPU线程、模块、函数名、行号、数据。
完。

追踪_函数进入 接受 模块、函数名：
    做 追踪_记录 用 追踪_函数入、0、模块、函数名、0、空。
完。

追踪_函数退出 接受 模块、函数名：
    做 追踪_记录 用 追踪_函数出、0、模块、函数名、0、空。
完。

追踪_系统调用事件 接受 调用号、参数：
    做 追踪_记录 用 追踪_系统调用、0、「内核」、「syscall」、0、一组：调用号、参数。
完。

追踪_中断事件 接受 向量号、来源：
    做 追踪_记录 用 追踪_中断、0、「内核」、「中断」、0、一组：向量号、来源。
完。

追踪_异常事件 接受 异常类型、错误码、地址：
    做 追踪_记录 用 追踪_异常、0、「内核」、「异常」、0、一组：异常类型、错误码、地址。
完。

追踪_热更新事件 接受 模块名、版本、成功：
    做 追踪_记录 用 追踪_热更新、0、模块名、「热更新」、0、一组：版本、成功。
完。

追踪_内存事件 接受 操作、地址、大小：
    做 追踪_记录 用 做 如果操作 用 操作、追踪_内存分配、追踪_内存释放、0、「运行时」、「内存」、0、一组：地址、大小。
完。

如果操作 接受 op、a、b：
    若 op 等于 0：
        返还 a。
    完。
    返还 b。
完。

追踪_调度事件 接受 旧任务、新任务：
    做 追踪_记录 用 追踪_调度切换、0、「调度器」、「切换」、0、一组：旧任务、新任务。
完。

追踪_最近N条 接受 n：
    总数 为 做 计数 用 _追踪_缓冲。
    若 n 大于或等于 总数：
        返还 _追踪_缓冲。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 总数 减 n。
    只要 i 小于 总数：
        做 追加 用 结果、_追踪_缓冲 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

追踪_按类型过滤 接受 类型：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _追踪_缓冲：
        若 _追踪_缓冲 的 第 i 项 的 第 1 项 等于 类型：
            做 追加 用 结果、_追踪_缓冲 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

追踪_按模块过滤 接受 模块名：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _追踪_缓冲：
        若 _追踪_缓冲 的 第 i 项 的 第 3 项 等于 模块名：
            做 追加 用 结果、_追踪_缓冲 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

追踪_打印报告 接受 零参：
    做 打印文本 用 「═══ 结构化追踪报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  总记录: 」、做 转字符串 用 _追踪_总计数。
    做 打印文本 用 做 字符串拼接 用 「  缓冲条目: 」、做 转字符串 用 做 计数 用 _追踪_缓冲。
    做 打印文本 用 做 字符串拼接 用 「  丢弃数: 」、做 转字符串 用 _追踪_丢弃数。
    可变 类型统计 为 一组：0、0、0、0、0、0、0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _追踪_缓冲：
        t 为 _追踪_缓冲 的 第 i 项 的 第 1 项。
        若 t 大于或等于 1 并且 t 小于或等于 11：
            类型统计 的 第 t 项 为 类型统计 的 第 t 项 加 1。
        完。
        i 为 i 加 1。
    完。
完。

追踪_清空 接受 零参：
    _追踪_缓冲 为 一组：（空）。
    _追踪_总计数 为 0。
    _追踪_丢弃数 为 0。
完。

本章 收束。
