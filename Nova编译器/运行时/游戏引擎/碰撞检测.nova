《碰撞检测》

引用《..·..·标准库·基础》。

碰撞体_AABB 为 0。

碰撞体_圆形 为 1。

碰撞体_射线 为 2。

AABB_创建 接受 x、y、宽、高：
    返还 一组：x、y、宽、高。
完。

包围盒_x 接受 a：
    返还 a 的 第 0 项。
完。

包围盒_y 接受 a：
    返还 a 的 第 1 项。
完。

AABB_宽 接受 a：
    返还 a 的 第 2 项。
完。

AABB_高 接受 a：
    返还 a 的 第 3 项。
完。

AABB_右 接受 a：
    返还 a 的 第 0 项 加 a 的 第 2 项。
完。

AABB_下 接受 a：
    返还 a 的 第 1 项 加 a 的 第 3 项。
完。

AABB_中心x 接受 a：
    返还 a 的 第 0 项 加 a 的 第 2 项 除以 2。
完。

AABB_中心y 接受 a：
    返还 a 的 第 1 项 加 a 的 第 3 项 除以 2。
完。

AABB_相交 接受 a、b：
    若 a 的 第 0 项 大于或等于 b 的 第 0 项 加 b 的 第 2 项：
        返还 假。
    完。
    若 a 的 第 0 项 加 a 的 第 2 项 小于或等于 b 的 第 0 项：
        返还 假。
    完。
    若 a 的 第 1 项 大于或等于 b 的 第 1 项 加 b 的 第 3 项：
        返还 假。
    完。
    若 a 的 第 1 项 加 a 的 第 3 项 小于或等于 b 的 第 1 项：
        返还 假。
    完。
    返还 真。
完。

AABB_包含点 接受 a、px、py：
    返还 px 大于或等于 a 的 第 0 项 并且 px 小于 a 的 第 0 项 加 a 的 第 2 项 并且 py 大于或等于 a 的 第 1 项 并且 py 小于 a 的 第 1 项 加 a 的 第 3 项。
完。

AABB_合并 接受 a、b：
    x 为 a 的 第 0 项。
    若 b 的 第 0 项 小于 x：
        x 为 b 的 第 0 项。
    完。
    y 为 a 的 第 1 项。
    若 b 的 第 1 项 小于 y：
        y 为 b 的 第 1 项。
    完。
    可变 右 为 a 的 第 0 项 加 a 的 第 2 项。
    若 b 的 第 0 项 加 b 的 第 2 项 大于 右：
        右 为 b 的 第 0 项 加 b 的 第 2 项。
    完。
    可变 下 为 a 的 第 1 项 加 a 的 第 3 项。
    若 b 的 第 1 项 加 b 的 第 3 项 大于 下：
        下 为 b 的 第 1 项 加 b 的 第 3 项。
    完。
    返还 做 AABB_创建 用 x、y、右 减 x、下 减 y。
完。

圆形_创建 接受 cx、cy、r：
    返还 一组：cx、cy、r。
完。

圆形_x 接受 c：
    返还 c 的 第 0 项。
完。

圆形_y 接受 c：
    返还 c 的 第 1 项。
完。

圆形_半径 接受 c：
    返还 c 的 第 2 项。
完。

圆形_相交 接受 a、b：
    dx 为 a 的 第 0 项 减 b 的 第 0 项。
    dy 为 a 的 第 1 项 减 b 的 第 1 项。
    距离平方 为 dx 乘 dx 加 dy 乘 dy。
    半径和 为 a 的 第 2 项 加 b 的 第 2 项。
    返还 距离平方 小于或等于 半径和 乘 半径和。
完。

圆形AABB_相交 接受 圆、矩形：
    可变 最近x 为 圆 的 第 0 项。
    若 最近x 小于 矩形 的 第 0 项：
        最近x 为 矩形 的 第 0 项。
    完。
    若 最近x 大于 矩形 的 第 0 项 加 矩形 的 第 2 项：
        最近x 为 矩形 的 第 0 项 加 矩形 的 第 2 项。
    完。
    可变 最近y 为 圆 的 第 1 项。
    若 最近y 小于 矩形 的 第 1 项：
        最近y 为 矩形 的 第 1 项。
    完。
    若 最近y 大于 矩形 的 第 1 项 加 矩形 的 第 3 项：
        最近y 为 矩形 的 第 1 项 加 矩形 的 第 3 项。
    完。
    dx 为 圆 的 第 0 项 减 最近x。
    dy 为 圆 的 第 1 项 减 最近y。
    返还 dx 乘 dx 加 dy 乘 dy 小于或等于 圆 的 第 2 项 乘 圆 的 第 2 项。
完。

可变 _碰撞_下个ID 为 1。

碰撞体_创建 接受 类型、形状：
    id 为 _碰撞_下个ID。
    _碰撞_下个ID 为 _碰撞_下个ID 加 1。
    返还 一组：id、类型、形状、1、0xFFFF、假。
完。

碰撞体_ID 接受 c：
    返还 c 的 第 0 项。
完。

碰撞体_类型 接受 c：
    返还 c 的 第 1 项。
完。

碰撞体_形状 接受 c：
    返还 c 的 第 2 项。
完。

碰撞体_层 接受 c：
    返还 c 的 第 3 项。
完。

碰撞体_掩码 接受 c：
    返还 c 的 第 4 项。
完。

碰撞体_设层 接受 c、l：
    c 的 第 3 项 为 l。
完。

碰撞体_设掩码 接受 c、m：
    c 的 第 4 项 为 m。
完。

碰撞体_设触发器 接受 c、t：
    c 的 第 5 项 为 t。
完。

碰撞体_层可交互 接受 a、b：
    返还 a 的 第 4 项 位与 b 的 第 3 项 不等于 0 并且 b 的 第 4 项 位与 a 的 第 3 项 不等于 0。
完。

碰撞_检测对 接受 a、b：
    若 做 碰撞体_层可交互 用 a、b 等于 假：
        返还 假。
    完。
    若 a 的 第 1 项 等于 碰撞体_AABB 并且 b 的 第 1 项 等于 碰撞体_AABB：
        返还 做 AABB_相交 用 a 的 第 2 项、b 的 第 2 项。
    完。
    若 a 的 第 1 项 等于 碰撞体_圆形 并且 b 的 第 1 项 等于 碰撞体_圆形：
        返还 做 圆形_相交 用 a 的 第 2 项、b 的 第 2 项。
    完。
    若 a 的 第 1 项 等于 碰撞体_AABB 并且 b 的 第 1 项 等于 碰撞体_圆形：
        返还 做 圆形AABB_相交 用 b 的 第 2 项、a 的 第 2 项。
    完。
    若 a 的 第 1 项 等于 碰撞体_圆形 并且 b 的 第 1 项 等于 碰撞体_AABB：
        返还 做 圆形AABB_相交 用 a 的 第 2 项、b 的 第 2 项。
    完。
    返还 假。
完。

事件_进入 为 0。

事件_持续 为 1。

事件_离开 为 2。

碰撞世界_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、一组：（空）、一组：（空）。
完。

碰撞世界_添加 接受 w、碰撞体：
    做 追加 用 w 的 第 0 项、碰撞体。
完。

碰撞世界_检测 接受 w：
    w 的 第 1 项 为 w 的 第 2 项。
    w 的 第 2 项 为 一组：（空）。
    w 的 第 3 项 为 一组：（空）。
    n 为 做 计数 用 w 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 做 碰撞_检测对 用 w 的 第 0 项 的 第 i 项、w 的 第 0 项 的 第 j 项：
                对 为 一组：做 碰撞体_ID 用 w 的 第 0 项 的 第 i 项、做 碰撞体_ID 用 w 的 第 0 项 的 第 j 项。
                做 追加 用 w 的 第 2 项、对。
                若 做 _碰撞对_在上帧 用 w 的 第 1 项、对 的 第 0 项、对 的 第 1 项：
                    做 追加 用 w 的 第 3 项、一组：对 的 第 0 项、对 的 第 1 项、事件_持续。
                否则：
                    做 追加 用 w 的 第 3 项、一组：对 的 第 0 项、对 的 第 1 项、事件_进入。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 k 为 0。
    只要 k 小于 做 计数 用 w 的 第 1 项：
        旧对 为 w 的 第 1 项 的 第 k 项。
        若 做 _碰撞对_在当前帧 用 w 的 第 2 项、旧对 的 第 0 项、旧对 的 第 1 项 等于 假：
            做 追加 用 w 的 第 3 项、一组：旧对 的 第 0 项、旧对 的 第 1 项、事件_离开。
        完。
        k 为 k 加 1。
    完。
完。

_碰撞对_在上帧 接受 列表、id1、id2：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 的 第 0 项 等于 id1 并且 列表 的 第 i 项 的 第 1 项 等于 id2：
            返还 真。
        完。
        若 列表 的 第 i 项 的 第 0 项 等于 id2 并且 列表 的 第 i 项 的 第 1 项 等于 id1：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

_碰撞对_在当前帧 接受 列表、id1、id2：
    返还 做 _碰撞对_在上帧 用 列表、id1、id2。
完。

碰撞世界_网格检测 接受 w、格宽、格高、世界宽、世界高：
    w 的 第 1 项 为 w 的 第 2 项。
    w 的 第 2 项 为 一组：（空）。
    w 的 第 3 项 为 一组：（空）。
    可变 桶 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 w 的 第 0 项：
        体 为 w 的 第 0 项 的 第 i 项。
        形 为 做 碰撞体_形状 用 体。
        可变 cx 为 0。
        可变 cy 为 0。
        若 做 碰撞体_类型 用 体 等于 碰撞体_AABB：
            cx 为 做 AABB_中心x 用 形 除以 格宽。
            cy 为 做 AABB_中心y 用 形 除以 格高。
        否则若 做 碰撞体_类型 用 体 等于 碰撞体_圆形：
            cx 为 做 圆形_x 用 形 除以 格宽。
            cy 为 做 圆形_y 用 形 除以 格高。
        完。
        键 为 做 字符串拼接 用 做 转字符串 用 cx、做 字符串拼接 用 「,」、做 转字符串 用 cy。
        现有 为 做 字典取 用 桶、键。
        若 现有 等于 空：
            做 字典设 用 桶、键、一组：i。
        否则：
            做 追加 用 现有、i。
        完。
        i 为 i 加 1。
    完。
    所有键 为 做 字典键 用 桶。
    可变 ki 为 0。
    只要 ki 小于 做 计数 用 所有键：
        格内 为 做 字典取 用 桶、所有键 的 第 ki 项。
        可变 a 为 0。
        只要 a 小于 做 计数 用 格内：
            可变 b 为 a 加 1。
            只要 b 小于 做 计数 用 格内：
                体A 为 w 的 第 0 项 的 第 格内 的 第 a 项 项。
                体B 为 w 的 第 0 项 的 第 格内 的 第 b 项 项。
                若 做 碰撞_检测对 用 体A、体B：
                    对 为 一组：做 碰撞体_ID 用 体A、做 碰撞体_ID 用 体B。
                    做 追加 用 w 的 第 2 项、对。
                    若 做 _碰撞对_在上帧 用 w 的 第 1 项、对 的 第 0 项、对 的 第 1 项：
                        做 追加 用 w 的 第 3 项、一组：对 的 第 0 项、对 的 第 1 项、事件_持续。
                    否则：
                        做 追加 用 w 的 第 3 项、一组：对 的 第 0 项、对 的 第 1 项、事件_进入。
                    完。
                完。
                b 为 b 加 1。
            完。
            a 为 a 加 1。
        完。
        ki 为 ki 加 1。
    完。
完。

碰撞世界_事件 接受 w：
    返还 w 的 第 3 项。
完。

碰撞世界_碰撞对数 接受 w：
    返还 做 计数 用 w 的 第 2 项。
完。

碰撞世界_打印报告 接受 w：
    做 打印 用 「═══ 碰撞检测报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  碰撞体数: 」、做 转字符串 用 做 计数 用 w 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  碰撞对数: 」、做 转字符串 用 做 计数 用 w 的 第 2 项。
    做 打印文本 用 做 字符串拼接 用 「  事件数: 」、做 转字符串 用 做 计数 用 w 的 第 3 项。
    可变 进入 为 0。
    可变 持续 为 0。
    可变 离开 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 w 的 第 3 项：
        若 w 的 第 3 项 的 第 i 项 的 第 2 项 等于 事件_进入：
            进入 为 进入 加 1。
        完。
        若 w 的 第 3 项 的 第 i 项 的 第 2 项 等于 事件_持续：
            持续 为 持续 加 1。
        完。
        若 w 的 第 3 项 的 第 i 项 的 第 2 项 等于 事件_离开：
            离开 为 离开 加 1。
        完。
        i 为 i 加 1。
    完。
    做 打印文本 用 做 字符串拼接 用 「    进入: 」、做 字符串拼接 用 做 转字符串 用 进入、做 字符串拼接 用 「 持续: 」、做 字符串拼接 用 做 转字符串 用 持续、做 字符串拼接 用 「 离开: 」、做 转字符串 用 离开。
完。

本章 收束。
