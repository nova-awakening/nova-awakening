《输入系统》

引用《..·..·标准库·基础》。

键_释放 为 0。

键_按下 为 1。

键_持续 为 2。

输入键_上 为 0。

输入键_下 为 1。

输入键_左 为 2。

输入键_右 为 3。

输入键_A 为 4。

输入键_B 为 5。

输入系统_键_X 为 6。

输入系统_键_Y 为 7。

键_开始 为 8。

键_选择 为 9。

输入键_空格 为 10。

输入键_回车 为 11。

输入键_ESC 为 12。

键_总数 为 16。

鼠标_左键 为 0。

鼠标_右键 为 1。

鼠标_中键 为 2。

事件_按键按下 为 0。

事件_按键释放 为 1。

输入系统_事件_鼠标按下 为 2。

输入系统_事件_鼠标释放 为 3。

输入系统_事件_鼠标移动 为 4。

事件_触摸开始 为 5。

事件_触摸结束 为 6。

输入事件_创建 接受 类型、码、值：
    返还 一组：类型、码、值、0。
完。

输入_创建 接受 零参：
    可变 当前 为 一组：（空）。
    可变 上帧 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 键_总数：
        做 追加 用 当前、键_释放。
        做 追加 用 上帧、键_释放。
        i 为 i 加 1。
    完。
    返还 一组：当前、上帧、0、一组：键_释放、键_释放、键_释放、一组：（空）。
完。

输入_帧开始 接受 inp：
    可变 i 为 0。
    只要 i 小于 键_总数：
        inp 的 第 1 项 的 第 i 项 为 inp 的 第 0 项 的 第 i 项。
        若 inp 的 第 0 项 的 第 i 项 等于 键_按下：
            inp 的 第 0 项 的 第 i 项 为 键_持续。
        完。
        i 为 i 加 1。
    完。
    inp 的 第 5 项 为 一组：（空）。
完。

输入_按键按下 接受 inp、键码：
    若 键码 大于或等于 0 并且 键码 小于 键_总数：
        inp 的 第 0 项 的 第 键码 项 为 键_按下。
    完。
    做 追加 用 inp 的 第 5 项、做 输入事件_创建 用 事件_按键按下、键码、1。
完。

输入_按键释放 接受 inp、键码：
    若 键码 大于或等于 0 并且 键码 小于 键_总数：
        inp 的 第 0 项 的 第 键码 项 为 键_释放。
    完。
    做 追加 用 inp 的 第 5 项、做 输入事件_创建 用 事件_按键释放、键码、0。
完。

输入_鼠标移动 接受 inp、x、y：
    inp 的 第 2 项 为 x。
    inp 的 第 3 项 为 y。
    做 追加 用 inp 的 第 5 项、做 输入事件_创建 用 输入系统_事件_鼠标移动、0、0。
完。

输入_鼠标按下 接受 inp、按钮：
    若 按钮 大于或等于 0 并且 按钮 小于 3：
        inp 的 第 4 项 的 第 按钮 项 为 键_按下。
    完。
    做 追加 用 inp 的 第 5 项、做 输入事件_创建 用 输入系统_事件_鼠标按下、按钮、1。
完。

输入_鼠标释放 接受 inp、按钮：
    若 按钮 大于或等于 0 并且 按钮 小于 3：
        inp 的 第 4 项 的 第 按钮 项 为 键_释放。
    完。
    做 追加 用 inp 的 第 5 项、做 输入事件_创建 用 输入系统_事件_鼠标释放、按钮、0。
完。

输入_是否按下 接受 inp、键码：
    若 键码 小于 0 或者 键码 大于或等于 键_总数：
        返还 假。
    完。
    返还 inp 的 第 0 项 的 第 键码 项 等于 键_按下。
完。

输入_是否持续 接受 inp、键码：
    若 键码 小于 0 或者 键码 大于或等于 键_总数：
        返还 假。
    完。
    返还 inp 的 第 0 项 的 第 键码 项 等于 键_持续 或者 inp 的 第 0 项 的 第 键码 项 等于 键_按下。
完。

输入_刚按下 接受 inp、键码：
    若 键码 小于 0 或者 键码 大于或等于 键_总数：
        返还 假。
    完。
    返还 inp 的 第 0 项 的 第 键码 项 等于 键_按下 并且 inp 的 第 1 项 的 第 键码 项 等于 键_释放。
完。

输入_刚释放 接受 inp、键码：
    若 键码 小于 0 或者 键码 大于或等于 键_总数：
        返还 假。
    完。
    返还 inp 的 第 0 项 的 第 键码 项 等于 键_释放 并且 inp 的 第 1 项 的 第 键码 项 不等于 键_释放。
完。

输入_鼠标x 接受 inp：
    返还 inp 的 第 2 项。
完。

输入_鼠标y 接受 inp：
    返还 inp 的 第 3 项。
完。

输入_鼠标按钮 接受 inp、按钮：
    若 按钮 小于 0 或者 按钮 大于或等于 3：
        返还 假。
    完。
    返还 inp 的 第 4 项 的 第 按钮 项 不等于 键_释放。
完。

可变 _输入映射 为 空。

输入映射_初始化 接受 零参：
    _输入映射 为 做 字典。
完。

输入映射_绑定 接受 动作、键码：
    若 _输入映射 等于 空：
        做 输入映射_初始化。
    完。
    现有 为 做 字典取 用 _输入映射、动作。
    若 现有 等于 空：
        做 字典设 用 _输入映射、动作、一组：键码。
    否则：
        做 追加 用 现有、键码。
    完。
完。

输入映射_检查 接受 inp、动作：
    若 _输入映射 等于 空：
        返还 假。
    完。
    键列表 为 做 字典取 用 _输入映射、动作。
    若 键列表 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        若 做 输入_是否持续 用 inp、键列表 的 第 i 项：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

输入映射_默认 接受 零参：
    做 输入映射_初始化。
    做 输入映射_绑定 用 「移动上」、输入键_上。
    做 输入映射_绑定 用 「移动下」、输入键_下。
    做 输入映射_绑定 用 「移动左」、输入键_左。
    做 输入映射_绑定 用 「移动右」、输入键_右。
    做 输入映射_绑定 用 「跳跃」、输入键_空格。
    做 输入映射_绑定 用 「攻击」、输入键_A。
    做 输入映射_绑定 用 「交互」、输入键_B。
    做 输入映射_绑定 用 「暂停」、输入键_ESC。
    做 输入映射_绑定 用 「确认」、输入键_回车。
完。

输入_打印报告 接受 inp：
    做 打印 用 「═══ 输入系统报告 ═══」。
    可变 按下数 为 0。
    可变 i 为 0。
    只要 i 小于 键_总数：
        若 inp 的 第 0 项 的 第 i 项 不等于 键_释放：
            按下数 为 按下数 加 1。
        完。
        i 为 i 加 1。
    完。
    做 打印文本 用 做 字符串拼接 用 「  当前按下: 」、做 转字符串 用 按下数。
    做 打印文本 用 做 字符串拼接 用 「  鼠标: (」、做 字符串拼接 用 做 转字符串 用 inp 的 第 2 项、做 字符串拼接 用 「,」、做 字符串拼接 用 做 转字符串 用 inp 的 第 3 项、「)」。
    做 打印文本 用 做 字符串拼接 用 「  事件队列: 」、做 转字符串 用 做 计数 用 inp 的 第 5 项。
完。

本章 收束。
