《场景图》

引用《..·..·标准库·基础》。

节点_空 为 0。

节点_精灵 为 1。

节点_网格 为 2。

节点_相机 为 3。

节点_光源 为 4。

节点_碰撞体 为 5。

节点_音频源 为 6。

节点_UI元素 为 7。

可变 _场景_下个ID 为 1。

场景图_场景节点_创建 接受 名称、类型：
    id 为 _场景_下个ID。
    _场景_下个ID 为 _场景_下个ID 加 1。
    返还 一组：id、名称、类型、负 1、一组：（空）、一组：0、0、1、1、1、0、真、真、一组：（空）。
完。

节点_ID 接受 n：
    返还 n 的 第 0 项。
完。

节点_名称 接受 n：
    返还 n 的 第 1 项。
完。

场景_节点_类型 接受 n：
    返还 n 的 第 2 项。
完。

节点_父ID 接受 n：
    返还 n 的 第 3 项。
完。

节点_子列表 接受 n：
    返还 n 的 第 4 项。
完。

节点_变换 接受 n：
    返还 n 的 第 5 项。
完。

节点_可见 接受 n：
    返还 n 的 第 6 项。
完。

节点_激活 接受 n：
    返还 n 的 第 7 项。
完。

节点_组件 接受 n：
    返还 n 的 第 8 项。
完。

变换_位置 接受 t：
    返还 一组：t 的 第 0 项、t 的 第 1 项、t 的 第 2 项。
完。

场景图_变换_缩放 接受 t：
    返还 一组：t 的 第 3 项、t 的 第 4 项、t 的 第 5 项。
完。

变换_旋转 接受 t：
    返还 t 的 第 6 项。
完。

变换_设位置 接受 t、x、y、z：
    t 的 第 0 项 为 x。
    t 的 第 1 项 为 y。
    t 的 第 2 项 为 z。
完。

变换_设缩放 接受 t、sx、sy、sz：
    t 的 第 3 项 为 sx。
    t 的 第 4 项 为 sy。
    t 的 第 5 项 为 sz。
完。

变换_设旋转 接受 t、角：
    t 的 第 6 项 为 角。
完。

变换_移动 接受 t、dx、dy、dz：
    t 的 第 0 项 为 t 的 第 0 项 加 dx。
    t 的 第 1 项 为 t 的 第 1 项 加 dy。
    t 的 第 2 项 为 t 的 第 2 项 加 dz。
完。

场景图_创建 接受 零参：
    根 为 做 场景图_场景节点_创建 用 「根」、节点_空。
    字典 为 做 字典。
    做 字典设 用 字典、做 转字符串 用 做 节点_ID 用 根、根。
    返还 一组：根、字典、1。
完。

场景图_根 接受 g：
    返还 g 的 第 0 项。
完。

场景图_节点数 接受 g：
    返还 g 的 第 2 项。
完。

场景图_添加子节点 接受 g、父节点、子节点：
    子节点 的 第 3 项 为 做 节点_ID 用 父节点。
    做 追加 用 父节点 的 第 4 项、做 节点_ID 用 子节点。
    做 字典设 用 g 的 第 1 项、做 转字符串 用 做 节点_ID 用 子节点、子节点。
    g 的 第 2 项 为 g 的 第 2 项 加 1。
完。

场景图_查找 接受 g、id：
    返还 做 字典取 用 g 的 第 1 项、做 转字符串 用 id。
完。

场景图_DFS 接受 g、节点、回调：
    做 回调 用 节点。
    子列 为 做 节点_子列表 用 节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子列：
        子 为 做 场景图_查找 用 g、子列 的 第 i 项。
        若 子 不等于 空：
            做 场景图_DFS 用 g、子、回调。
        完。
        i 为 i 加 1。
    完。
完。

场景图_传播变换 接受 g、节点、父变换：
    t 为 做 节点_变换 用 节点。
    世界x 为 父变换 的 第 0 项 加 t 的 第 0 项 乘 父变换 的 第 3 项。
    世界y 为 父变换 的 第 1 项 加 t 的 第 1 项 乘 父变换 的 第 4 项。
    世界z 为 父变换 的 第 2 项 加 t 的 第 2 项 乘 父变换 的 第 5 项。
    世界sx 为 父变换 的 第 3 项 乘 t 的 第 3 项。
    世界sy 为 父变换 的 第 4 项 乘 t 的 第 4 项。
    世界sz 为 父变换 的 第 5 项 乘 t 的 第 5 项。
    世界变换 为 一组：世界x、世界y、世界z、世界sx、世界sy、世界sz、t 的 第 6 项 加 父变换 的 第 6 项。
    子列 为 做 节点_子列表 用 节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子列：
        子 为 做 场景图_查找 用 g、子列 的 第 i 项。
        若 子 不等于 空：
            做 场景图_传播变换 用 g、子、世界变换。
        完。
        i 为 i 加 1。
    完。
完。

场景图_删除节点 接受 g、id：
    节点 为 做 场景图_查找 用 g、id。
    若 节点 等于 空：
        返还 0。
    完。
    子列 为 做 节点_子列表 用 节点。
    可变 i 为 做 计数 用 子列 减 1。
    只要 i 大于或等于 0：
        做 场景图_删除节点 用 g、子列 的 第 i 项。
        i 为 i 减 1。
    完。
    父id 为 做 节点_父ID 用 节点。
    若 父id 大于或等于 0：
        父 为 做 场景图_查找 用 g、父id。
        若 父 不等于 空：
            父子列 为 做 节点_子列表 用 父。
            可变 j 为 0。
            只要 j 小于 做 计数 用 父子列：
                若 父子列 的 第 j 项 等于 id：
                    做 删除 用 父子列、j。
                    止。
                完。
                j 为 j 加 1。
            完。
        完。
    完。
    做 字典设 用 g 的 第 1 项、做 转字符串 用 id、空。
    g 的 第 2 项 为 g 的 第 2 项 减 1。
    返还 1。
完。

场景图_移动节点 接受 g、节点id、新父id：
    节点 为 做 场景图_查找 用 g、节点id。
    若 节点 等于 空：
        返还 0。
    完。
    新父 为 做 场景图_查找 用 g、新父id。
    若 新父 等于 空：
        返还 0。
    完。
    若 做 _是后代 用 g、新父id、节点id：
        返还 0。
    完。
    旧父id 为 做 节点_父ID 用 节点。
    若 旧父id 大于或等于 0：
        旧父 为 做 场景图_查找 用 g、旧父id。
        若 旧父 不等于 空：
            旧子列 为 做 节点_子列表 用 旧父。
            可变 i 为 0。
            只要 i 小于 做 计数 用 旧子列：
                若 旧子列 的 第 i 项 等于 节点id：
                    做 删除 用 旧子列、i。
                    止。
                完。
                i 为 i 加 1。
            完。
        完。
    完。
    节点 的 第 3 项 为 新父id。
    做 追加 用 新父 的 第 4 项、节点id。
    返还 1。
完。

_是后代 接受 g、查询id、祖先id：
    若 查询id 等于 祖先id：
        返还 真。
    完。
    祖先 为 做 场景图_查找 用 g、祖先id。
    若 祖先 等于 空：
        返还 假。
    完。
    子列 为 做 节点_子列表 用 祖先。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子列：
        若 做 _是后代 用 g、查询id、子列 的 第 i 项：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

场景图_BFS 接受 g、起始节点、回调：
    可变 队列 为 一组：起始节点。
    可变 头 为 0。
    只要 头 小于 做 计数 用 队列：
        当前 为 队列 的 第 头 项。
        头 为 头 加 1。
        做 回调 用 当前。
        子列 为 做 节点_子列表 用 当前。
        可变 i 为 0。
        只要 i 小于 做 计数 用 子列：
            子 为 做 场景图_查找 用 g、子列 的 第 i 项。
            若 子 不等于 空：
                做 追加 用 队列、子。
            完。
            i 为 i 加 1。
        完。
    完。
完。

场景图_按名称查找 接受 g、名称：
    可变 结果 为 一组：（空）。
    键列 为 做 字典键 用 g 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        节点 为 做 字典取 用 g 的 第 1 项、键列 的 第 i 项。
        若 节点 不等于 空 并且 做 字符串相等 用 做 节点_名称 用 节点、名称：
            做 追加 用 结果、节点。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

场景图_按类型查找 接受 g、类型：
    可变 结果 为 一组：（空）。
    键列 为 做 字典键 用 g 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        节点 为 做 字典取 用 g 的 第 1 项、键列 的 第 i 项。
        若 节点 不等于 空 并且 做 场景_节点_类型 用 节点 等于 类型：
            做 追加 用 结果、节点。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

节点_层掩码 接受 n：
    若 做 计数 用 n 大于 9：
        返还 n 的 第 9 项。
    完。
    返还 0xFFFF。
完。

节点_设层掩码 接受 n、掩码：
    只要 做 计数 用 n 小于或等于 9：
        做 追加 用 n、0。
    完。
    n 的 第 9 项 为 掩码。
完。

场景图_可见过滤 接受 g、层掩码：
    可变 结果 为 一组：（空）。
    键列 为 做 字典键 用 g 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        节点 为 做 字典取 用 g 的 第 1 项、键列 的 第 i 项。
        若 节点 不等于 空 并且 做 节点_可见 用 节点 并且 做 节点_层掩码 用 节点 位与 层掩码 不等于 0：
            做 追加 用 结果、节点。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

节点_标脏 接受 n：
    只要 做 计数 用 n 小于或等于 10：
        做 追加 用 n、0。
    完。
    n 的 第 10 项 为 1。
完。

节点_是脏 接受 n：
    若 做 计数 用 n 大于 10：
        返还 n 的 第 10 项 等于 1。
    完。
    返还 假。
完。

节点_清脏 接受 n：
    若 做 计数 用 n 大于 10：
        n 的 第 10 项 为 0。
    完。
完。

场景图_脏传播 接受 g、节点、父变换：
    若 做 节点_是脏 用 节点 等于 假：
        返还 0。
    完。
    t 为 做 节点_变换 用 节点。
    世界x 为 父变换 的 第 0 项 加 t 的 第 0 项 乘 父变换 的 第 3 项。
    世界y 为 父变换 的 第 1 项 加 t 的 第 1 项 乘 父变换 的 第 4 项。
    世界z 为 父变换 的 第 2 项 加 t 的 第 2 项 乘 父变换 的 第 5 项。
    世界sx 为 父变换 的 第 3 项 乘 t 的 第 3 项。
    世界sy 为 父变换 的 第 4 项 乘 t 的 第 4 项。
    世界sz 为 父变换 的 第 5 项 乘 t 的 第 5 项。
    世界变换 为 一组：世界x、世界y、世界z、世界sx、世界sy、世界sz、t 的 第 6 项 加 父变换 的 第 6 项。
    做 节点_清脏 用 节点。
    子列 为 做 节点_子列表 用 节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子列：
        子 为 做 场景图_查找 用 g、子列 的 第 i 项。
        若 子 不等于 空：
            做 节点_标脏 用 子。
            做 场景图_脏传播 用 g、子、世界变换。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

场景图_统计 接受 g：
    可变 统计 为 一组：0、0、0、0、0、0、0、0。
    键列 为 做 字典键 用 g 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        节点 为 做 字典取 用 g 的 第 1 项、键列 的 第 i 项。
        若 节点 不等于 空：
            t 为 做 场景_节点_类型 用 节点。
            若 t 大于或等于 0 并且 t 小于 8：
                统计 的 第 t 项 为 统计 的 第 t 项 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 统计。
完。

场景图_打印 接受 g：
    做 打印 用 「═══ 场景图报告 ═══」。
    做 打印文本 用 做 字符串拼接 用 「  节点总数: 」、做 转字符串 用 g 的 第 2 项。
    做 _打印节点树 用 g、g 的 第 0 项、0。
完。

_打印节点树 接受 g、节点、深度：
    可变 前缀 为 「」。
    可变 d 为 0。
    只要 d 小于 深度：
        前缀 为 做 字符串拼接 用 前缀、「  」。
        d 为 d 加 1。
    完。
    做 打印文本 用 做 字符串拼接 用 前缀、做 字符串拼接 用 做 节点_名称 用 节点、做 字符串拼接 用 「 (」、做 字符串拼接 用 做 转字符串 用 做 场景_节点_类型 用 节点、「)」。
    子列 为 做 节点_子列表 用 节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子列：
        子 为 做 场景图_查找 用 g、子列 的 第 i 项。
        若 子 不等于 空：
            做 _打印节点树 用 g、子、深度 加 1。
        完。
        i 为 i 加 1。
    完。
完。

本章 收束。
