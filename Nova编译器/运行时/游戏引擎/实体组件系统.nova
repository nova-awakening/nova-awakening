《实体组件系统》

引用《..·..·标准库·基础》。

组件_变换 为 0。

组件_精灵 为 1。

组件_刚体 为 2。

组件_碰撞器 为 3。

组件_脚本 为 4。

组件_动画 为 5。

组件_音频 为 6。

组件_粒子 为 7。

可变 _ECS_下个实体ID 为 1。

实体_创建 接受 零参：
    id 为 _ECS_下个实体ID。
    _ECS_下个实体ID 为 _ECS_下个实体ID 加 1。
    返还 一组：id、做 字典、真。
完。

实体_ID 接受 e：
    返还 e 的 第 0 项。
完。

实体_激活 接受 e：
    返还 e 的 第 2 项。
完。

实体_设激活 接受 e、v：
    e 的 第 2 项 为 v。
完。

实体_添加组件 接受 e、类型、数据：
    做 字典设 用 e 的 第 1 项、做 转字符串 用 类型、数据。
完。

实体_获取组件 接受 e、类型：
    返还 做 字典取 用 e 的 第 1 项、做 转字符串 用 类型。
完。

实体_有组件 接受 e、类型：
    返还 做 字典有 用 e 的 第 1 项、做 转字符串 用 类型。
完。

ECS世界_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、0。
完。

ECS世界_实体 接受 w：
    返还 w 的 第 0 项。
完。

ECS世界_系统 接受 w：
    返还 w 的 第 1 项。
完。

ECS世界_帧数 接受 w：
    返还 w 的 第 2 项。
完。

ECS世界_添加实体 接受 w、e：
    做 追加 用 w 的 第 0 项、e。
完。

ECS世界_添加系统 接受 w、系统：
    做 追加 用 w 的 第 1 项、系统。
完。

系统_创建 接受 名称、需要组件、更新函数：
    返还 一组：名称、需要组件、更新函数。
完。

ECS世界_执行系统 接受 w、系统、dt：
    需要 为 系统 的 第 1 项。
    更新 为 系统 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 w 的 第 0 项：
        e 为 w 的 第 0 项 的 第 i 项。
        若 做 实体_激活 用 e：
            可变 匹配 为 真。
            可变 j 为 0。
            只要 j 小于 做 计数 用 需要：
                若 做 实体_有组件 用 e、需要 的 第 j 项 等于 假：
                    匹配 为 假。
                完。
                j 为 j 加 1。
            完。
            若 匹配：
                做 更新 用 e、dt。
            完。
        完。
        i 为 i 加 1。
    完。
完。

ECS世界_帧更新 接受 w、dt：
    可变 i 为 0。
    只要 i 小于 做 计数 用 w 的 第 1 项：
        做 ECS世界_执行系统 用 w、w 的 第 1 项 的 第 i 项、dt。
        i 为 i 加 1。
    完。
    w 的 第 2 项 为 w 的 第 2 项 加 1。
完。

_变换系统_更新 接受 e、dt：
    t 为 做 实体_获取组件 用 e、组件_变换。
    若 t 等于 空：
        返还 空。
    完。
完。

_刚体系统_更新 接受 e、dt：
    rb 为 做 实体_获取组件 用 e、组件_刚体。
    t 为 做 实体_获取组件 用 e、组件_变换。
    若 rb 等于 空 或者 t 等于 空：
        返还 空。
    完。
    t 的 第 0 项 为 t 的 第 0 项 加 rb 的 第 0 项 乘 dt。
    t 的 第 1 项 为 t 的 第 1 项 加 rb 的 第 1 项 乘 dt。
    t 的 第 2 项 为 t 的 第 2 项 加 rb 的 第 2 项 乘 dt。
    若 rb 的 第 4 项 不等于 0：
        rb 的 第 1 项 为 rb 的 第 1 项 减 rb 的 第 4 项 乘 dt。
    完。
完。

ECS世界_删除实体 接受 w、实体id：
    列表 为 w 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 实体_ID 用 列表 的 第 i 项 等于 实体id：
            做 删除 用 列表、i。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

实体_移除组件 接受 e、类型：
    键 为 做 转字符串 用 类型。
    若 做 字典有 用 e 的 第 1 项、键：
        做 字典设 用 e 的 第 1 项、键、空。
        返还 真。
    完。
    返还 假。
完。

ECS世界_查找实体 接受 w、实体id：
    列表 为 w 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 实体_ID 用 列表 的 第 i 项 等于 实体id：
            返还 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

ECS世界_查询 接受 w、需要组件列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 w 的 第 0 项：
        e 为 w 的 第 0 项 的 第 i 项。
        若 做 实体_激活 用 e：
            可变 匹配 为 真。
            可变 j 为 0。
            只要 j 小于 做 计数 用 需要组件列表：
                若 做 实体_有组件 用 e、需要组件列表 的 第 j 项 等于 假：
                    匹配 为 假。
                    j 为 做 计数 用 需要组件列表。
                完。
                j 为 j 加 1。
            完。
            若 匹配：
                做 追加 用 结果、e。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

系统_创建_带优先级 接受 名称、需要组件、更新函数、优先级：
    返还 一组：名称、需要组件、更新函数、优先级。
完。

系统_优先级 接受 系统：
    若 做 计数 用 系统 大于 3：
        返还 系统 的 第 3 项。
    完。
    返还 0。
完。

ECS世界_排序系统 接受 w：
    列表 为 w 的 第 1 项。
    n 为 做 计数 用 列表。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 0。
        只要 j 小于 n 减 1 减 i：
            若 做 系统_优先级 用 列表 的 第 j 项 小于 做 系统_优先级 用 列表 的 第 j 加 1 项：
                临时 为 列表 的 第 j 项。
                列表 的 第 j 项 为 列表 的 第 j 加 1 项。
                列表 的 第 j 加 1 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

ECS世界_对每个 接受 w、需要组件列表、操作函数：
    可变 执行数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 w 的 第 0 项：
        e 为 w 的 第 0 项 的 第 i 项。
        若 做 实体_激活 用 e：
            可变 匹配 为 真。
            可变 j 为 0。
            只要 j 小于 做 计数 用 需要组件列表：
                若 做 实体_有组件 用 e、需要组件列表 的 第 j 项 等于 假：
                    匹配 为 假。
                    j 为 做 计数 用 需要组件列表。
                完。
                j 为 j 加 1。
            完。
            若 匹配：
                做 操作函数 用 e。
                执行数 为 执行数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 执行数。
完。

ECS世界_组件统计 接受 w：
    可变 统计 为 一组：0、0、0、0、0、0、0、0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 w 的 第 0 项：
        e 为 w 的 第 0 项 的 第 i 项。
        可变 t 为 0。
        只要 t 小于 8：
            若 做 实体_有组件 用 e、t：
                统计 的 第 t 项 为 统计 的 第 t 项 加 1。
            完。
            t 为 t 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 统计。
完。

ECS世界_注册默认系统 接受 w：
    做 ECS世界_添加系统 用 w、做 系统_创建 用 「变换」、一组：组件_变换、_变换系统_更新。
    做 ECS世界_添加系统 用 w、做 系统_创建 用 「刚体」、一组：组件_刚体、组件_变换、_刚体系统_更新。
完。

ECS世界_打印 接受 w：
    做 打印 用 「═══ ECS世界报告 ═══」。
    活跃 为 做 计数 用 做 ECS世界_查询 用 w、一组：（空）。
    做 打印文本 用 做 字符串拼接 用 「  实体总数: 」、做 转字符串 用 做 计数 用 w 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  活跃实体: 」、做 转字符串 用 活跃。
    做 打印文本 用 做 字符串拼接 用 「  系统数: 」、做 转字符串 用 做 计数 用 w 的 第 1 项。
    做 打印文本 用 做 字符串拼接 用 「  帧计数: 」、做 转字符串 用 w 的 第 2 项。
完。

本章 收束。
