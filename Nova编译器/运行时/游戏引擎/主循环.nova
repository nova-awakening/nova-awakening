《主循环》

引用《..·..·标准库·基础》。

引用《..·..·编译器·语法·节点定义》。

引用《实体组件系统》。

引用《场景图》。

引擎_未启动 为 0。

引擎_运行中 为 1。

引擎_暂停 为 2。

引擎_退出 为 3。

引擎_创建 接受 目标帧率：
    步长 为 1000 除以 目标帧率。
    返还 一组：目标帧率、步长、5、引擎_未启动、空、空、一组：0、0、0、0。
完。

引擎_帧率 接受 e：
    返还 e 的 第 0 项。
完。

引擎_步长 接受 e：
    返还 e 的 第 1 项。
完。

引擎_状态 接受 e：
    返还 e 的 第 3 项。
完。

引擎_场景 接受 e：
    返还 e 的 第 4 项。
完。

引擎_ECS 接受 e：
    返还 e 的 第 5 项。
完。

引擎_设场景 接受 e、场景：
    e 的 第 4 项 为 场景。
完。

引擎_设ECS 接受 e、世界：
    e 的 第 5 项 为 世界。
完。

引擎_帧统计 接受 e：
    返还 e 的 第 6 项。
完。

引擎_更新帧统计 接受 e、帧时：
    e 的 第 6 项 的 第 0 项 为 e 的 第 6 项 的 第 0 项 加 1。
    e 的 第 6 项 的 第 1 项 为 e 的 第 6 项 的 第 1 项 加 帧时。
    若 e 的 第 6 项 的 第 2 项 等于 0 或者 帧时 小于 e 的 第 6 项 的 第 2 项：
        e 的 第 6 项 的 第 2 项 为 帧时。
    完。
    若 帧时 大于 e 的 第 6 项 的 第 3 项：
        e 的 第 6 项 的 第 3 项 为 帧时。
    完。
完。

引擎_初始化 接受 e：
    若 e 的 第 4 项 等于 空：
        e 的 第 4 项 为 做 场景图_创建。
    完。
    若 e 的 第 5 项 等于 空：
        e 的 第 5 项 为 做 ECS世界_创建。
        做 ECS世界_注册默认系统 用 e 的 第 5 项。
    完。
    e 的 第 3 项 为 引擎_运行中。
完。

引擎_模拟帧 接受 e、当前时间、上次时间：
    若 e 的 第 3 项 不等于 引擎_运行中：
        返还 空。
    完。
    dt 为 当前时间 减 上次时间。
    步长 为 e 的 第 1 项。
    可变 累积 为 dt。
    可变 步数 为 0。
    只要 累积 大于或等于 步长 并且 步数 小于 e 的 第 2 项：
        做 _引擎_固定更新 用 e、步长。
        累积 为 累积 减 步长。
        步数 为 步数 加 1。
    完。
    做 _引擎_渲染 用 e。
    做 引擎_更新帧统计 用 e、dt。
完。

_引擎_固定更新 接受 e、dt：
    若 e 的 第 5 项 不等于 空：
        做 ECS世界_帧更新 用 e 的 第 5 项、dt。
    完。
    若 e 的 第 4 项 不等于 空：
        根 为 做 场景图_根 用 e 的 第 4 项。
        做 场景图_传播变换 用 e 的 第 4 项、根、一组：0、0、1、1、1、0。
    完。
完。

_引擎_渲染 接受 e：
    可变 ri 为 0。
    若 e 的 第 4 项 不等于 空：
        根 为 做 场景图_根 用 e 的 第 4 项。
        做 _渲染_遍历节点 用 e 的 第 4 项、根、0。
    完。
    若 e 的 第 5 项 不等于 空：
        实体列表 为 做 ECS世界_实体 用 e 的 第 5 项。
        只要 ri 小于 做 计数 用 实体列表：
            实体 为 实体列表 的 第 ri 项。
            渲染数据 为 做 实体_获取组件 用 实体、组件_精灵。
            若 渲染数据 不等于 空：
                变换 为 做 实体_获取组件 用 实体、组件_变换。
                若 变换 不等于 空：
                    做 _渲染_提交实体 用 做 实体_ID 用 实体、变换、渲染数据。
                完。
            完。
            ri 为 ri 加 1。
        完。
    完。
完。

_渲染_遍历节点 接受 场景、节点、深度：
    若 节点 等于 空：
        返还。
    完。
    若 做 节点_可见 用 节点：
        做 _渲染_提交节点 用 节点。
    完。
    子ID列表 为 做 节点_子列表 用 节点。
    若 子ID列表 不等于 空：
        可变 ci 为 0。
        只要 ci 小于 做 计数 用 子ID列表：
            子节点 为 做 场景图_查找 用 场景、子ID列表 的 第 ci 项。
            若 子节点 不等于 空：
                做 _渲染_遍历节点 用 场景、子节点、深度 加 1。
            完。
            ci 为 ci 加 1。
        完。
    完。
完。

_渲染_提交节点 接受 节点：
    组件 为 做 节点_组件 用 节点。
    若 组件 等于 空 或者 做 计数 用 组件 等于 0：
        返还。
    完。
    做 节点_变换 用 节点。
    做 节点_类型 用 节点。
    _引擎_渲染计数 为 _引擎_渲染计数 加 1。
完。

_渲染_提交实体 接受 eid、变换、渲染数据：
    _引擎_渲染计数 为 _引擎_渲染计数 加 1。
完。

可变 _引擎_渲染计数 为 0。

引擎_渲染计数 接受 零参：
    返还 _引擎_渲染计数。
完。

引擎_重置渲染计数 接受 零参：
    _引擎_渲染计数 为 0。
完。

引擎_暂停 接受 e：
    e 的 第 3 项 为 引擎_暂停。
完。

引擎_恢复 接受 e：
    e 的 第 3 项 为 引擎_运行中。
完。

引擎_退出 接受 e：
    e 的 第 3 项 为 引擎_退出。
完。

引擎_打印报告 接受 e：
    做 打印 用 「═══ 游戏引擎报告 ═══」。
    可变 状态名 为 「unknown」。
    若 e 的 第 3 项 等于 引擎_未启动：
        状态名 为 「未启动」。
    完。
    若 e 的 第 3 项 等于 引擎_运行中：
        状态名 为 「running」。
    完。
    若 e 的 第 3 项 等于 引擎_暂停：
        状态名 为 「暂停」。
    完。
    若 e 的 第 3 项 等于 引擎_退出：
        状态名 为 「退出」。
    完。
    做 打印文本 用 做 字符串拼接 用 「  状态: 」、状态名。
    做 打印文本 用 做 字符串拼接 用 「  目标帧率: 」、做 转字符串 用 e 的 第 0 项。
    做 打印文本 用 做 字符串拼接 用 「  固定步长: 」、做 字符串拼接 用 做 转字符串 用 e 的 第 1 项、「ms」。
    做 打印文本 用 做 字符串拼接 用 「  总帧数: 」、做 转字符串 用 e 的 第 6 项 的 第 0 项。
    若 e 的 第 6 项 的 第 0 项 大于 0：
        做 打印文本 用 做 字符串拼接 用 「  平均帧时: 」、做 字符串拼接 用 做 转字符串 用 e 的 第 6 项 的 第 1 项 除以 e 的 第 6 项 的 第 0 项、「ms」。
    完。
    若 e 的 第 4 项 不等于 空：
        做 打印文本 用 做 字符串拼接 用 「  场景节点: 」、做 转字符串 用 做 场景图_节点数 用 e 的 第 4 项。
    完。
    若 e 的 第 5 项 不等于 空：
        做 打印文本 用 做 字符串拼接 用 「  ECS实体: 」、做 转字符串 用 做 计数 用 做 ECS世界_实体 用 e 的 第 5 项。
    完。
完。

本章 收束。
