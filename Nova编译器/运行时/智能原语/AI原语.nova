《AI原语》

引用《..·..·标准库·基础》。

神经元_静息电位 为 0。

神经元_默认阈值 为 8000。

神经元_默认衰减 为 9500。

神经元_不应期长 为 3。

神经元_创建 接受 阈值：
    返还 一组：神经元_静息电位、阈值、神经元_默认衰减、0、负 100。
完。

神经元_创建默认 接受 零参：
    返还 做 神经元_创建 用 神经元_默认阈值。
完。

神经元_更新 接受 神经元、输入电流、当前时间：
    若 神经元 的 第 3 项 大于 0：
        神经元 的 第 3 项 为 神经元 的 第 3 项 减 1。
        神经元 的 第 0 项 为 神经元_静息电位。
        返还 0。
    完。
    神经元 的 第 0 项 为 神经元 的 第 0 项 乘 神经元 的 第 2 项 除以 10000 加 输入电流。
    若 神经元 的 第 0 项 大于或等于 神经元 的 第 1 项：
        神经元 的 第 0 项 为 神经元_静息电位。
        神经元 的 第 3 项 为 神经元_不应期长。
        神经元 的 第 4 项 为 神经元 的 第 4 项 加 1。
        神经元 的 第 5 项 为 当前时间。
        返还 10000。
    完。
    返还 0。
完。

突触_兴奋 为 0。

突触_抑制 为 1。

智能原语_突触_创建 接受 源、目标、权重、延迟、类型：
    返还 一组：源、目标、权重、延迟、类型。
完。

智能原语_突触_传递 接受 突触、脉冲：
    若 脉冲 等于 0：
        返还 0。
    完。
    信号 为 脉冲 乘 突触 的 第 2 项 除以 10000。
    若 突触 的 第 4 项 等于 突触_抑制：
        返还 0 减 信号。
    完。
    返还 信号。
完。

赫布_STDP 接受 突触、前发放时间、后发放时间、学习率：
    时间差 为 后发放时间 减 前发放时间。
    可变 权重变化 为 0。
    若 时间差 大于 0：
        权重变化 为 学习率 乘 10000 除以 (10000 加 时间差 乘 1000)。
    否则若 时间差 小于 0：
        绝对差 为 0 减 时间差。
        权重变化 为 0 减 学习率 乘 10000 除以 (10000 加 绝对差 乘 1000)。
    完。
    突触 的 第 2 项 为 突触 的 第 2 项 加 权重变化。
    若 突触 的 第 4 项 等于 突触_兴奋 并且 突触 的 第 2 项 小于 0：
        突触 的 第 2 项 为 0。
    完。
    若 突触 的 第 4 项 等于 突触_抑制 并且 突触 的 第 2 项 大于 0：
        突触 的 第 2 项 为 0。
    完。
    返还 权重变化。
完。

SNN_创建 接受 神经元数：
    可变 神经元列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 神经元数：
        做 追加 用 神经元列表、做 神经元_创建默认。
        i 为 i 加 1。
    完。
    返还 一组：神经元列表、一组：（空）、0、一组：（空）。
完。

SNN_添加突触 接受 网络、源、目标、权重、延迟、类型：
    做 追加 用 网络 的 第 1 项、做 智能原语_突触_创建 用 源、目标、权重、延迟、类型。
完。

智能原语_SNN_步进 接受 网络、外部输入：
    可变 新队列 为 一组：（空）。
    可变 脉冲输出 为 一组：（空）。
    时间 为 网络 的 第 2 项。
    可变 i 为 0。
    可变 输入 为 一组：（空）。
    只要 i 小于 做 计数 用 网络 的 第 0 项：
        做 追加 用 输入、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 外部输入 并且 i 小于 做 计数 用 输入：
        输入 的 第 i 项 为 输入 的 第 i 项 加 外部输入 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 网络 的 第 3 项：
        事件 为 网络 的 第 3 项 的 第 i 项。
        若 事件 的 第 0 项 小于或等于 时间：
            若 事件 的 第 1 项 大于或等于 0 并且 事件 的 第 1 项 小于 做 计数 用 输入：
                输入 的 第 事件 的 第 1 项 项 为 输入 的 第 事件 的 第 1 项 项 加 事件 的 第 2 项。
            完。
        否则：
            做 追加 用 新队列、事件。
        完。
        i 为 i 加 1。
    完。
    网络 的 第 3 项 为 新队列。
    i 为 0。
    只要 i 小于 做 计数 用 网络 的 第 0 项：
        脉冲 为 做 神经元_更新 用 网络 的 第 0 项 的 第 i 项、输入 的 第 i 项、时间。
        若 脉冲 大于 0：
            做 追加 用 脉冲输出、i。
            可变 j 为 0。
            只要 j 小于 做 计数 用 网络 的 第 1 项：
                突 为 网络 的 第 1 项 的 第 j 项。
                若 突 的 第 0 项 等于 i：
                    信号 为 做 智能原语_突触_传递 用 突、脉冲。
                    做 追加 用 网络 的 第 3 项、一组：时间 加 突 的 第 3 项、突 的 第 1 项、信号。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    网络 的 第 2 项 为 时间 加 1。
    返还 脉冲输出。
完。

稀疏_编码 接受 输入向量、字典矩阵、稀疏度：
    可变 激活值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字典矩阵：
        可变 点积 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 输入向量 并且 j 小于 做 计数 用 字典矩阵 的 第 i 项：
            点积 为 点积 加 输入向量 的 第 j 项 乘 字典矩阵 的 第 i 项 的 第 j 项 除以 10000。
            j 为 j 加 1。
        完。
        做 追加 用 激活值、一组：点积、i。
        i 为 i 加 1。
    完。
    可变 n 为 做 计数 用 激活值。
    可变 p 为 0。
    只要 p 小于 n 减 1：
        可变 q 为 0。
        只要 q 小于 n 减 1 减 p：
            若 激活值 的 第 q 项 的 第 0 项 小于 激活值 的 第 q 加 1 项 的 第 0 项：
                暂 为 激活值 的 第 q 项。
                激活值 的 第 q 项 为 激活值 的 第 q 加 1 项。
                激活值 的 第 q 加 1 项 为 暂。
            完。
            q 为 q 加 1。
        完。
        p 为 p 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 稀疏度 并且 i 小于 n：
        做 追加 用 结果、激活值 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

局部连接_创建 接受 网络、层大小、感受野、权重初始：
    可变 i 为 0。
    只要 i 小于 层大小：
        可变 j 为 i 减 感受野。
        只要 j 小于或等于 i 加 感受野：
            若 j 大于或等于 0 并且 j 小于 层大小 并且 j 不等于 i：
                做 SNN_添加突触 用 网络、j、i、权重初始、1、突触_兴奋。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

侧抑制 接受 激活列表、抑制强度：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 激活列表：
        若 激活列表 的 第 i 项 大于 最大：
            最大 为 激活列表 的 第 i 项。
            最大索引 为 i。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 激活列表：
        若 i 等于 最大索引：
            做 追加 用 结果、激活列表 的 第 i 项。
        否则：
            做 追加 用 结果、激活列表 的 第 i 项 乘 (10000 减 抑制强度) 除以 10000。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

自适应阈值_更新 接受 神经元、目标发放率、调节速率：
    当前率 为 神经元 的 第 4 项。
    若 当前率 大于 目标发放率：
        神经元 的 第 1 项 为 神经元 的 第 1 项 加 调节速率。
    否则若 当前率 小于 目标发放率：
        神经元 的 第 1 项 为 神经元 的 第 1 项 减 调节速率。
        若 神经元 的 第 1 项 小于 1000：
            神经元 的 第 1 项 为 1000。
        完。
    完。
完。

可变 _AI_神经元更新数 为 0。

可变 _AI_突触传递数 为 0。

可变 _AI_学习事件数 为 0。

本章 收束。
