《MachO链接器》

引用《..·标准库·基础》。

MachO_对齐 接受 值、边界：
    可变 余 为 值 取余于 边界。
    若 余 等于 0：
        返还 值。
    完。
    返还 值 加 边界 减 余。
完。

MachO_页大小 接受 cpu类型：
    若 cpu类型 等于 CPU_TYPE_ARM64：
        返还 16384。
    完。
    返还 4096。
完。

MachO_段命令 接受 名、vmaddr、vmsize、fileoff、filesize、maxprot、initprot、nsects：
    返还 一组：LC_SEGMENT_64、LC_SEG64_大小 加 SECTION64_大小 乘 nsects、名、vmaddr、vmsize、fileoff、filesize、maxprot、initprot、nsects。
完。

MachO_节 接受 名、段名、addr、大小、offset、对齐log2、类型：
    返还 一组：名、段名、addr、大小、offset、对齐log2、0、类型。
完。

MachO_入口命令 接受 入口偏移：
    返还 一组：LC_MAIN、24、入口偏移、0。
完。

MachO_符号表命令 接受 symoff、nsyms、stroff、strsize：
    返还 一组：LC_SYMTAB、24、symoff、nsyms、stroff、strsize。
完。

MachO_动态库命令 接受 名：
    返还 一组：LC_LOAD_DYLIB、56、名、x10000、0x10000。
完。

符号_外部 为 1。

符号_节 为 0x0E。

可变 _MachO_统计 为 一组：0、0。

MachO_符号 接受 名偏移、节idx、值：
    返还 一组：名偏移、符号_外部 加 符号_节、节idx、0、值。
完。

MachO_字符串表 接受 零参：
    返还 一组：一组：「」、一组：0、1。
完。

MachO_strtab_加 接受 st、名：
    off 为 st 的 第 2 项。
    做 追加 用 st 的 第 0 项、名。
    做 追加 用 st 的 第 1 项、off。
    st 的 第 2 项 为 off 加 做 计数 用 名 加 1。
    返还 off。
完。

MachO_计算布局 接受 代码大小、数据大小、符号数、cpu类型：
    可变 页 为 做 MachO_页大小 用 cpu类型。
    ncmds 为 7。
    sizeofcmds 为 LC_SEG64_大小 乘 4 加 SECTION64_大小 乘 3 加 24 加 24 加 56。
    头总 为 MACHO头_大小 加 sizeofcmds。
    text_off 为 做 MachO_对齐 用 头总、页。
    text_vsz 为 做 MachO_对齐 用 代码大小、页。
    data_off 为 text_off 加 text_vsz。
    data_vsz 为 做 MachO_对齐 用 数据大小、页。
    linkedit_off 为 data_off 加 data_vsz。
    symtab_sz 为 NLIST64_大小 乘 (符号数 加 1)。
    strtab_sz 为 符号数 乘 16 加 32。
    linkedit_sz 为 做 MachO_对齐 用 symtab_sz 加 strtab_sz、页。
    可变 总大小 为 linkedit_off 加 linkedit_sz。
    入口偏移 为 text_off。
    返还 一组：text_off、text_vsz、data_off、data_vsz、linkedit_off、总大小、页、入口偏移、ncmds、sizeofcmds。
完。

MachO_Fat头大小 接受 架构数：
    返还 8 加 20 乘 架构数。
完。

MachO_Fat_计算 接受 x86大小、arm大小：
    头大小 为 做 MachO_Fat头大小 用 2。
    x86偏移 为 做 MachO_对齐 用 头大小、0x1000。
    arm偏移 为 做 MachO_对齐 用 x86偏移 加 x86大小、0x4000。
    总大小 为 arm偏移 加 arm大小。
    返还 一组：x86偏移、arm偏移、总大小。
完。

MachO_CPU名 接受 t：
    若 t 等于 CPU_TYPE_X86_64：
        返还 「x86_64」。
    完。
    若 t 等于 CPU_TYPE_ARM64：
        返还 「arm64」。
    完。
    返还 「unknown」。
完。

MachO_代码签名大小 接受 文件大小、页大小：
    可变 页数 为 文件大小 除以 页大小。
    若 文件大小 取余于 页大小 大于 0：
        页数 为 页数 加 1。
    完。
    估算 为 800 加 页数 乘 32。
    返还 做 MachO_对齐 用 估算、16。
完。

MachO_代码签名命令 接受 数据偏移、数据大小：
    返还 一组：LC_CODE_SIGNATURE、16、数据偏移、数据大小。
完。

MachO_rebase大小 接受 指针数：
    返还 做 MachO_对齐 用 指针数 乘 5 加 8、8。
完。

MachO_bind大小 接受 导入数：
    返还 做 MachO_对齐 用 导入数 乘 20 加 8、8。
完。

MachO_ADRP_页 接受 pc、目标：
    pc页 为 pc 减 pc 取余于 4096。
    目标页 为 目标 减 目标 取余于 4096。
    返还 (目标页 减 pc页) 除以 4096。
完。

MachO_ADD_偏移 接受 目标：
    返还 目标 取余于 4096。
完。

MachO_签名布局 接受 代码大小、数据大小、符号数、cpu类型：
    基础 为 做 MachO_计算布局 用 代码大小、数据大小、符号数、cpu类型。
    页 为 基础 的 第 6 项。
    签名大小 为 做 MachO_代码签名大小 用 基础 的 第 5 项、页。
    签名偏移 为 基础 的 第 5 项。
    新总大小 为 基础 的 第 5 项 加 做 MachO_对齐 用 签名大小、页。
    返还 一组：基础 的 第 0 项、基础 的 第 1 项、基础 的 第 2 项、基础 的 第 3 项、基础 的 第 4 项、新总大小、页、基础 的 第 7 项、签名偏移、签名大小。
完。

MachO_最低OS版本 接受 主版、次版、补丁：
    返还 主版 乘 65536 加 次版 乘 256 加 补丁。
完。

加载命令_UUID 为 0x1B。

加载命令_最低macOS版本 为 0x24。

加载命令_源版本 为 0x2A。

加载命令_构建版本 为 0x32。

加载命令_仅动态链接信息 为 0x80000022。

加载命令_函数起始 为 0x26。

加载命令_代码中数据 为 0x29。

平台_macOS 为 1。

平台_iOS 为 2。

平台_tvOS 为 3。

平台_watchOS 为 4。

MachO_安全对齐 接受 值、边界：
    若 边界 小于或等于 0：
        返还 值。
    完。
    余 为 值 取余于 边界。
    若 余 等于 0：
        返还 值。
    完。
    返还 值 加 边界 减 余。
完。

MachO_有效CPU 接受 cpu：
    若 cpu 等于 CPU_TYPE_X86_64：
        返还 1。
    完。
    若 cpu 等于 CPU_TYPE_ARM64：
        返还 1。
    完。
    返还 0。
完。

MachO_构建版本命令 接受 平台、最低版本、sdk版本：
    返还 一组：加载命令_构建版本、32、平台、最低版本、sdk版本、0。
完。

MachO_UUID命令 接受 零参：
    返还 一组：加载命令_UUID、24。
完。

MachO_保护 接受 读、写、执行：
    可变 p 为 0。
    若 读：
        p 为 p 位或 VM_PROT_READ。
    完。
    若 写：
        p 为 p 位或 VM_PROT_WRITE。
    完。
    若 执行：
        p 为 p 位或 VM_PROT_EXECUTE。
    完。
    返还 p。
完。

MachO_有效段名ID 接受 id：
    若 id 大于或等于 0：
        若 id 小于或等于 5：
            返还 1。
        完。
    完。
    返还 0。
完。

MachO_stub大小 接受 cpu类型：
    若 cpu类型 等于 CPU_TYPE_ARM64：
        返还 12。
    完。
    返还 6。
完。

间接符号大小 为 4。

可变 _MachO_段名 为 一组：（空）。

可变 _MachO_段偏移 为 一组：（空）。

可变 _MachO_段大小 为 一组：（空）。

可变 _MachO_段保护 为 一组：（空）。

MachO_添加段 接受 名称、偏移、大小、保护：
    做 追加 用 _MachO_段名、名称。
    做 追加 用 _MachO_段偏移、偏移。
    做 追加 用 _MachO_段大小、大小。
    做 追加 用 _MachO_段保护、保护。
完。

MachO_查段 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _MachO_段名：
        若 _MachO_段名 的 第 i 项 等于 名称：
            返还 一组：_MachO_段偏移 的 第 i 项、_MachO_段大小 的 第 i 项、_MachO_段保护 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

MachO_段总大小 接受 零参：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _MachO_段大小：
        总 为 总 加 _MachO_段大小 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

可变 _MachO_符号名 为 一组：（空）。

可变 _MachO_符号值 为 一组：（空）。

可变 _MachO_符号类型 为 一组：（空）。

MachO_添加符号 接受 名称、值、类型：
    做 追加 用 _MachO_符号名、名称。
    做 追加 用 _MachO_符号值、值。
    做 追加 用 _MachO_符号类型、类型。
完。

MachO_查符号 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _MachO_符号名：
        若 _MachO_符号名 的 第 i 项 等于 名称：
            返还 _MachO_符号值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

MachO_全局符号 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _MachO_符号名：
        若 _MachO_符号类型 的 第 i 项 等于 1：
            做 追加 用 结果、一组：_MachO_符号名 的 第 i 项、_MachO_符号值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

MachO_外部符号 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _MachO_符号名：
        若 _MachO_符号类型 的 第 i 项 等于 2：
            做 追加 用 结果、_MachO_符号名 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _MachO_重定位_偏移 为 一组：（空）。

可变 _MachO_重定位_符号 为 一组：（空）。

可变 _MachO_重定位_类型 为 一组：（空）。

MachO_添加重定位 接受 偏移、符号名、类型：
    做 追加 用 _MachO_重定位_偏移、偏移。
    做 追加 用 _MachO_重定位_符号、符号名。
    做 追加 用 _MachO_重定位_类型、类型。
完。

MachO_应用重定位 接受 代码、基址：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _MachO_重定位_偏移：
        符号值 为 做 MachO_查符号 用 _MachO_重定位_符号 的 第 i 项。
        若 符号值 大于或等于 0：
            偏移 为 _MachO_重定位_偏移 的 第 i 项。
            代码 的 第 偏移 项 为 (符号值 加 基址) 取余于 256。
            代码 的 第 偏移 加 1 项 为 (符号值 加 基址) 除以 256 取余于 256。
            代码 的 第 偏移 加 2 项 为 (符号值 加 基址) 除以 65536 取余于 256。
            代码 的 第 偏移 加 3 项 为 (符号值 加 基址) 除以 16777216 取余于 256。
        完。
        i 为 i 加 1。
    完。
完。

MachO_胖头 接受 架构列表：
    可变 头 为 一组：0xCA、0xFE、0xBA、0xBE。
    n 为 做 计数 用 架构列表。
    做 追加 用 头、n 除以 16777216 取余于 256。
    做 追加 用 头、n 除以 65536 取余于 256。
    做 追加 用 头、n 除以 256 取余于 256。
    做 追加 用 头、n 取余于 256。
    返还 头。
完。

MachO_记录链接 接受 零参：
    _MachO_统计 的 第 0 项 为 _MachO_统计 的 第 0 项 加 1。
完。

MachO_记录重定位 接受 零参：
    _MachO_统计 的 第 1 项 为 _MachO_统计 的 第 1 项 加 1。
完。

本章 收束。
