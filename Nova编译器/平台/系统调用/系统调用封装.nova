《系统调用封装》

引用《..·..·运行时·原语桩》。

引用《..·目标选择》。

引用《..·..·标准库·基础》。

引用《..·..·运行时·JIT·编译引擎》。

系统调用_读 为 0。

系统调用_写 为 1。

系统调用_打开 为 2。

系统调用_关闭 为 3。

系统调用_内存映射 为 9。

系统调用_取消映射 为 11。

系统调用_内存保护 为 10。

系统调用_堆扩展 为 12。

系统调用_退出 为 60。

系统调用_取进程号 为 39。

系统调用_时钟 为 228。

系统调用_纳秒睡眠 为 35。

系统调用_分叉 为 57。

系统调用_执行 为 59。

Windows_标准输出 为 负 11。

Windows_标准输入 为 负 10。

窗口_内存提交 为 0x1000。

窗口_内存保留 为 0x2000。

Windows_内存释放 为 0x8000。

Windows_页_读写 为 0x04。

Windows_页_读写执行 为 0x40。

平台_打开文件 接受 路径、模式：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」：
        返还 做 系统调用 用 「CreateFileA」、路径、模式、0、3。
    完。
    返还 做 系统调用_原生 用 系统调用_打开、路径、模式、644、0、0。
完。

平台_读文件 接受 句柄、缓冲、大小：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」：
        返还 做 系统调用 用 「ReadFile」、句柄、缓冲、大小、0。
    完。
    返还 做 系统调用_原生 用 系统调用_读、句柄、缓冲、大小、0、0。
完。

平台_写文件 接受 句柄、数据、大小：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」：
        返还 做 系统调用 用 「WriteFile」、句柄、数据、大小、0。
    完。
    返还 做 系统调用_原生 用 系统调用_写、句柄、数据、大小、0、0。
完。

平台_关闭文件 接受 句柄：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」：
        返还 做 系统调用 用 「CloseHandle」、句柄、0、0、0。
    完。
    返还 做 系统调用_原生 用 系统调用_关闭、句柄、0、0、0、0。
完。

平台_分配内存 接受 大小：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」：
        返还 做 系统调用 用 「VirtualAlloc」、0、大小、窗口_内存提交 位或 窗口_内存保留、Windows_页_读写。
    完。
    返还 做 系统调用_原生 用 系统调用_内存映射、0、大小、3、0x22、负 1。
完。

平台_释放内存 接受 地址、大小：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」：
        返还 做 系统调用 用 「VirtualFree」、地址、0、Windows_内存释放、0。
    完。
    返还 做 系统调用_原生 用 系统调用_取消映射、地址、大小、0、0、0。
完。

平台_可执行内存 接受 _地址、大小：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」：
        返还 做 系统调用 用 「VirtualAlloc」、0、大小、窗口_内存提交 位或 窗口_内存保留、Windows_页_读写执行。
    完。
    返还 做 系统调用_原生 用 系统调用_内存映射、0、大小、7、0x22、负 1。
完。

平台_退出 接受 代码：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」：
        返还 做 系统调用 用 「ExitProcess」、代码、0、0、0。
    完。
    返还 做 系统调用_原生 用 系统调用_退出、代码、0、0、0、0。
完。

平台_获取PID 接受 零参：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」：
        返还 做 系统调用 用 「GetCurrentProcessId」、0、0、0、0。
    完。
    返还 做 系统调用_原生 用 系统调用_取进程号、0、0、0、0、0。
完。

平台_毫秒时间 接受 零参：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」：
        返还 做 系统调用 用 「GetTickCount64」、0、0、0、0。
    完。
    返还 做 系统调用_原生 用 系统调用_时钟、1、0、0、0、0。
完。

平台_睡眠 接受 毫秒：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」：
        返还 做 系统调用 用 「Sleep」、毫秒、0、0、0。
    完。
    秒 为 毫秒 除以 1000。
    纳秒 为 毫秒 取余于 1000 乘 1000000。
    返还 做 系统调用_原生 用 系统调用_纳秒睡眠、一组：秒、纳秒、0、0、0、0。
完。

平台_输出 接受 文本：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」：
        句柄 为 做 系统调用 用 「GetStdHandle」、Windows_标准输出、0、0、0。
        返还 做 系统调用 用 「WriteConsoleA」、句柄、文本、做 计数 用 文本、0。
    完。
    返还 做 系统调用_原生 用 系统调用_写、1、文本、做 计数 用 文本、0、0。
完。

RV_系统调用_读 为 63。

RV_系统调用_写 为 64。

RV_系统调用_打开位于 为 56。

RV_系统调用_关闭 为 57。

RV_系统调用_内存映射 为 222。

RV_系统调用_解除映射 为 215。

RV_系统调用_MPROTECT 为 226。

RV_系统调用_堆断点 为 214。

RV_系统调用_退出 为 93。

RV_系统调用_GETPID 为 172。

RV_系统调用_CLOCK 为 113。

RV_系统调用_NANOSLEEP 为 101。

RV_当前工作目录 为 负 100。

ARM64_系统调用_读 为 63。

ARM64_系统调用_写 为 64。

ARM64_系统调用_打开AT 为 56。

ARM64_系统调用_关闭 为 57。

ARM64_系统调用_内存映射 为 222。

ARM64_系统调用_取消映射 为 215。

ARM64_系统调用_内存保护 为 226。

ARM64_系统调用_堆扩展 为 214。

ARM64_系统调用_退出 为 93。

ARM64_系统调用_取进程号 为 172。

ARM64_系统调用_时钟 为 113。

syscall号_write 接受 架构：
    若 架构 等于 「riscv64」：
        返还 RV_系统调用_写。
    完。
    若 架构 等于 「arm64」：
        返还 ARM64_系统调用_写。
    完。
    返还 系统调用_写。
完。

syscall号_read 接受 架构：
    若 架构 等于 「riscv64」：
        返还 RV_系统调用_读。
    完。
    若 架构 等于 「arm64」：
        返还 ARM64_系统调用_读。
    完。
    返还 系统调用_读。
完。

syscall号_exit 接受 架构：
    若 架构 等于 「riscv64」：
        返还 RV_系统调用_退出。
    完。
    若 架构 等于 「arm64」：
        返还 ARM64_系统调用_退出。
    完。
    返还 系统调用_退出。
完。

syscall号_mmap 接受 架构：
    若 架构 等于 「riscv64」：
        返还 RV_系统调用_内存映射。
    完。
    若 架构 等于 「arm64」：
        返还 ARM64_系统调用_内存映射。
    完。
    返还 系统调用_内存映射。
完。

平台_发射系统调用 接受 jit、调用号：
    做 JIT_发射 用 jit、0x48。
    做 JIT_发射 用 jit、0xB8。
    做 JIT_发射64 用 jit、调用号。
    做 JIT_发射 用 jit、0x0F。
    做 JIT_发射 用 jit、0x05。
完。

平台_发射系统调用_riscv 接受 发射器、调用号：
    做 发射器 用 0x13 位或 17 左移 7 位或 0 左移 15 位或 (调用号 位与 0xFFF) 左移 20。
    做 发射器 用 0x00000073。
完。

平台_发射系统调用_arm64 接受 发射器、调用号：
    做 发射器 用 0xD2800000 位或 8 位与 31 位或 (调用号 位与 0xFFFF) 左移 5。
    做 发射器 用 0xD4000001。
完。

可变 _当前平台 为 0。

平台_WINDOWS 为 1。

平台_LINUX 为 2。

平台_MACOS 为 3。

平台_设置 接受 平台：
    _当前平台 为 平台。
完。

系统调用封装_平台_是Windows 接受 零参：
    返还 _当前平台 等于 平台_WINDOWS。
完。

系统调用封装_平台_是Linux 接受 零参：
    返还 _当前平台 等于 平台_LINUX。
完。

平台_名称 接受 零参：
    若 _当前平台 等于 平台_WINDOWS：
        返还 「Windows」。
    完。
    若 _当前平台 等于 平台_LINUX：
        返还 「Linux」。
    完。
    若 _当前平台 等于 平台_MACOS：
        返还 「macOS」。
    完。
    返还 「unknown」。
完。

可变 _syscall_调用数 为 0。

可变 _syscall_失败数 为 0。

syscall_记录调用 接受 零参：
    _syscall_调用数 为 _syscall_调用数 加 1。
完。

syscall_记录失败 接受 零参：
    _syscall_失败数 为 _syscall_失败数 加 1。
完。

可变 _宿主 为 一组：0、0、0、一组：0。

宿主_初始化 接受 零参：
    _宿主 的 第 0 项 为 平台_LINUX。
    _宿主 的 第 1 项 为 1 加 4 加 8 加 16。
    _宿主 的 第 2 项 为 做 系统调用_原生 用 39、0、0、0、0、0。
    _宿主 的 第 3 项 为 做 平台_毫秒时间。
    _宿主 的 第 4 项 为 一组：0。
完。

系统调用封装_宿主_有能力 接受 能力：
    返还 _宿主 的 第 1 项 位与 能力 不等于 0。
完。

宿主_读文件 接受 路径：
    若 做 系统调用封装_宿主_有能力 用 1 等于 假：
        返还 空。
    完。
    fd 为 做 平台_打开文件 用 路径、0。
    若 fd 小于 0：
        返还 空。
    完。
    可变 内容 为 「」。
    可变 buf 为 「                                                                」。
    可变 读了 为 做 平台_读文件 用 fd、buf、64。
    只要 读了 大于 0：
        内容 为 做 字符串拼接 用 内容、做 子串 用 buf、0、读了。
        _宿主 的 第 4 项 的 第 0 项 为 _宿主 的 第 4 项 的 第 0 项 加 读了。
        读了 为 做 平台_读文件 用 fd、buf、64。
    完。
    做 平台_关闭文件 用 fd。
    _宿主 的 第 4 项 的 第 2 项 为 _宿主 的 第 4 项 的 第 2 项 加 3。
    返还 内容。
完。

宿主_写文件 接受 路径、内容：
    若 做 系统调用封装_宿主_有能力 用 1 等于 假：
        返还 假。
    完。
    fd 为 做 平台_打开文件 用 路径、1 加 0x40 加 0x200。
    若 fd 小于 0：
        返还 假。
    完。
    写了 为 做 平台_写文件 用 fd、内容、做 字节长度 用 内容。
    做 平台_关闭文件 用 fd。
    _宿主 的 第 4 项 的 第 1 项 为 _宿主 的 第 4 项 的 第 1 项 加 写了。
    _宿主 的 第 4 项 的 第 2 项 为 _宿主 的 第 4 项 的 第 2 项 加 3。
    返还 写了 大于 0。
完。

宿主_运行时间 接受 零参：
    返还 做 平台_毫秒时间 减 _宿主 的 第 3 项。
完。

宿主_创建bundle 接受 零参：
    返还 一组：1、_宿主 的 第 0 项、_宿主 的 第 1 项、_宿主 的 第 2 项、_宿主 的 第 3 项、_宿主 的 第 4 项、做 宿主_运行时间、0、做 平台_毫秒时间。
完。

宿主_bundle_版本 接受 b：
    返还 b 的 第 0 项。
完。

宿主_bundle_平台 接受 b：
    返还 b 的 第 1 项。
完。

宿主_bundle_能力 接受 b：
    返还 b 的 第 2 项。
完。

宿主_bundle_运行时间 接受 b：
    返还 b 的 第 6 项。
完。

宿主_bundle_时间戳 接受 b：
    返还 b 的 第 8 项。
完。

宿主_bundle_有效 接受 b：
    若 b 等于 空 或者 做 是列表 用 b 等于 假 或者 做 计数 用 b 小于 9：
        返还 假。
    完。
    若 b 的 第 0 项 不等于 1：
        返还 假。
    完。
    返还 真。
完。

可变 _patch_暂存 为 一组：（空）。

可变 _patch_计数 为 0。

patch_暂存 接受 模块名、旧哈希、新目标码、新哈希：
    做 追加 用 _patch_暂存、一组：模块名、旧哈希、新目标码、新哈希、0。
    _patch_计数 为 _patch_计数 加 1。
    返还 _patch_计数 减 1。
完。

patch_验证 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 做 计数 用 _patch_暂存：
        返还 假。
    完。
    p 为 _patch_暂存 的 第 索引 项。
    若 p 的 第 4 项 不等于 0：
        返还 假。
    完。
    若 p 的 第 2 项 等于 空：
        返还 假。
    完。
    若 p 的 第 1 项 等于 p 的 第 3 项：
        返还 假。
    完。
    p 的 第 4 项 为 1。
    返还 真。
完。

patch_应用 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 做 计数 用 _patch_暂存：
        返还 假。
    完。
    p 为 _patch_暂存 的 第 索引 项。
    若 p 的 第 4 项 不等于 1：
        返还 假。
    完。
    p 的 第 4 项 为 2。
    返还 真。
完。

patch_回滚 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 做 计数 用 _patch_暂存：
        返还 假。
    完。
    p 为 _patch_暂存 的 第 索引 项。
    若 p 的 第 4 项 不等于 2：
        返还 假。
    完。
    p 的 第 4 项 为 3。
    返还 真。
完。

patch_暂存数 接受 零参：
    返还 做 计数 用 _patch_暂存。
完。

patch_待验证数 接受 零参：
    可变 n 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _patch_暂存：
        若 _patch_暂存 的 第 i 项 的 第 4 项 等于 0：
            n 为 n 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 n。
完。

patch_已应用数 接受 零参：
    可变 n 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _patch_暂存：
        若 _patch_暂存 的 第 i 项 的 第 4 项 等于 2：
            n 为 n 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 n。
完。

宿主_报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「平台」、做 平台_名称。
    做 字典设 用 r、「pid」、做 转字符串 用 _宿主 的 第 2 项。
    做 字典设 用 r、「运行时间ms」、做 转字符串 用 做 宿主_运行时间。
    做 字典设 用 r、「文件IO」、做 转字符串 用 做 系统调用封装_宿主_有能力 用 1。
    做 字典设 用 r、「进程」、做 转字符串 用 做 系统调用封装_宿主_有能力 用 4。
    做 字典设 用 r、「mmap」、做 转字符串 用 做 系统调用封装_宿主_有能力 用 8。
    做 字典设 用 r、「时间」、做 转字符串 用 做 系统调用封装_宿主_有能力 用 16。
    做 字典设 用 r、「读字节」、做 转字符串 用 _宿主 的 第 4 项 的 第 0 项。
    做 字典设 用 r、「写字节」、做 转字符串 用 _宿主 的 第 4 项 的 第 1 项。
    做 字典设 用 r、「syscall数」、做 转字符串 用 _宿主 的 第 4 项 的 第 2 项。
    返还 r。
完。

本章 收束。
