《wasm编码器》

引用《..·..·..·标准库·基础》。

引用《..·..·..·编译器·规划·类型系统·基础类型·基础类型》。

WASM_魔数_0 为 0x00。

WASM_魔数_1 为 0x61。

WASM_魔数_2 为 0x73。

WASM_魔数_3 为 0x6D。

WASM_版本 为 1。

WASM编码器_类型_列表 为 5。

WASM编码器_类型_字典 为 6。

WASM编码器_类型_函数 为 7。

WASM编码器_类型_任意 为 9。

编码_uleb128 接受 值：
    可变 结果 为 一组：（空）。
    可变 v 为 值。
    可变 完成 为 假。
    只要 完成 等于 假：
        字节 为 v 位与 0x7F。
        v 为 v 右移 7。
        若 v 等于 0：
            做 追加 用 结果、字节。
            完成 为 真。
        否则：
            做 追加 用 结果、字节 位或 0x80。
        完。
    完。
    返还 结果。
完。

编码_sleb128 接受 值：
    可变 结果 为 一组：（空）。
    可变 v 为 值。
    可变 完成 为 假。
    只要 完成 等于 假：
        字节 为 v 位与 0x7F。
        符号位 为 字节 位与 0x40。
        v 为 v 右移 7。
        若 v 等于 0：
            若 符号位 等于 0：
                做 追加 用 结果、字节。
                完成 为 真。
            否则：
                做 追加 用 结果、字节 位或 0x80。
            完。
        否则：
            若 v 等于 0 减 1：
                若 符号位 等于 0：
                    做 追加 用 结果、字节 位或 0x80。
                否则：
                    做 追加 用 结果、字节。
                    完成 为 真。
                完。
            否则：
                做 追加 用 结果、字节 位或 0x80。
            完。
        完。
    完。
    返还 结果。
完。

WASM_模块头 接受 零参：
    返还 一组：WASM_魔数_0、WASM_魔数_1、WASM_魔数_2、WASM_魔数_3、WASM_版本、0x00、0x00、0x00。
完。

编码_函数类型 接受 参数列表、返回列表：
    可变 结果 为 一组：0x60。
    参数数编码 为 做 编码_uleb128 用 做 计数 用 参数列表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数数编码：
        做 追加 用 结果、参数数编码 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 参数列表：
        做 追加 用 结果、参数列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返回数编码 为 做 编码_uleb128 用 做 计数 用 返回列表。
    i 为 0。
    只要 i 小于 做 计数 用 返回数编码：
        做 追加 用 结果、返回数编码 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 返回列表：
        做 追加 用 结果、返回列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

WASM_封装段 接受 段id、内容：
    可变 结果 为 一组：段id。
    长度编码 为 做 编码_uleb128 用 做 计数 用 内容。
    可变 i 为 0。
    只要 i 小于 做 计数 用 长度编码：
        做 追加 用 结果、长度编码 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 内容：
        做 追加 用 结果、内容 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

编码_导出 接受 名称字节、类型、索引：
    可变 结果 为 一组：（空）。
    名称长度编码 为 做 编码_uleb128 用 做 计数 用 名称字节。
    可变 i 为 0。
    只要 i 小于 做 计数 用 名称长度编码：
        做 追加 用 结果、名称长度编码 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 名称字节：
        做 追加 用 结果、名称字节 的 第 i 项。
        i 为 i 加 1。
    完。
    做 追加 用 结果、类型。
    索引编码 为 做 编码_uleb128 用 索引。
    i 为 0。
    只要 i 小于 做 计数 用 索引编码：
        做 追加 用 结果、索引编码 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

编码_内存 接受 最小页：
    可变 结果 为 一组：0x00。
    编码 为 做 编码_uleb128 用 最小页。
    可变 i 为 0。
    只要 i 小于 做 计数 用 编码：
        做 追加 用 结果、编码 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

wasm_类型名 接受 id：
    若 id 等于 类型_空：
        返还 「空」。
    完。
    若 id 等于 类型_布尔：
        返还 「布尔」。
    完。
    若 id 等于 类型_整数：
        返还 「整数」。
    完。
    若 id 等于 类型_浮点：
        返还 「浮点」。
    完。
    若 id 等于 类型_字符串：
        返还 「字符串」。
    完。
    若 id 等于 WASM编码器_类型_列表：
        返还 「列表」。
    完。
    若 id 等于 WASM编码器_类型_字典：
        返还 「字典」。
    完。
    若 id 等于 WASM编码器_类型_函数：
        返还 「函数」。
    完。
    若 id 等于 类型_结构：
        返还 「结构」。
    完。
    若 id 等于 类型_枚举：
        返还 「枚举」。
    完。
    若 id 等于 类型_接口：
        返还 「接口」。
    完。
    若 id 等于 类型_泛型：
        返还 「泛型」。
    完。
    若 id 等于 WASM编码器_类型_任意：
        返还 「任意」。
    完。
    返还 「unknown」。
完。

BT_是数值 接受 t：
    若 t 等于 类型_整数：
        返还 真。
    完。
    若 t 等于 类型_浮点：
        返还 真。
    完。
    返还 假。
完。

wasm_是集合 接受 t：
    若 t 等于 WASM编码器_类型_列表：
        返还 真。
    完。
    若 t 等于 WASM编码器_类型_字典：
        返还 真。
    完。
    返还 假。
完。

wasm_是可索引 接受 t：
    若 t 等于 类型_字符串：
        返还 真。
    完。
    若 做 是集合 用 t：
        返还 真。
    完。
    返还 假。
完。

wasm_是可迭代 接受 t：
    返还 做 是可索引 用 t。
完。

wasm_是可调用 接受 t：
    返还 t 等于 WASM编码器_类型_函数。
完。

wasm_是引用类型 接受 t：
    若 t 等于 类型_字符串：
        返还 真。
    完。
    若 t 等于 WASM编码器_类型_列表：
        返还 真。
    完。
    若 t 等于 WASM编码器_类型_字典：
        返还 真。
    完。
    若 t 等于 WASM编码器_类型_函数：
        返还 真。
    完。
    若 t 等于 类型_结构：
        返还 真。
    完。
    若 t 等于 类型_接口：
        返还 真。
    完。
    若 t 等于 类型_泛型：
        返还 真。
    完。
    返还 假。
完。

BT_二元结果类型 接受 op、左、右：
    若 op 等于 「+」：
        若 左 等于 类型_字符串：
            若 右 等于 类型_字符串：
                返还 类型_字符串。
            完。
        完。
    完。
    若 做 BT_是数值 用 左：
        若 做 BT_是数值 用 右：
            若 左 等于 类型_浮点：
                返还 类型_浮点。
            完。
            若 右 等于 类型_浮点：
                返还 类型_浮点。
            完。
            返还 类型_整数。
        完。
    完。
    若 op 等于 「==」：
        返还 类型_布尔。
    完。
    若 op 等于 「!=」：
        返还 类型_布尔。
    完。
    若 op 等于 「<」：
        返还 类型_布尔。
    完。
    若 op 等于 「>」：
        返还 类型_布尔。
    完。
    若 op 等于 「<=」：
        返还 类型_布尔。
    完。
    若 op 等于 「>=」：
        返还 类型_布尔。
    完。
    返还 WASM编码器_类型_任意。
完。

BT_一元结果类型 接受 op、操作数：
    若 op 等于 「!」：
        返还 类型_布尔。
    完。
    若 op 等于 「-」：
        若 做 BT_是数值 用 操作数：
            返还 操作数。
        完。
    完。
    返还 操作数。
完。

wasm_类型兼容 接受 期望、实际：
    若 期望 等于 WASM编码器_类型_任意：
        返还 真。
    完。
    若 期望 等于 实际：
        返还 真。
    完。
    若 做 BT_是数值 用 期望：
        若 做 BT_是数值 用 实际：
            返还 真。
        完。
    完。
    返还 假。
完。

wasm_可强转 接受 源、目标：
    若 源 等于 目标：
        返还 真。
    完。
    若 做 BT_是数值 用 源：
        若 做 BT_是数值 用 目标：
            返还 真。
        完。
    完。
    若 目标 等于 类型_字符串：
        返还 真。
    完。
    若 目标 等于 类型_布尔：
        返还 真。
    完。
    返还 假。
完。

wasm_类型大小 接受 t：
    若 t 等于 类型_空：
        返还 0。
    完。
    若 t 等于 类型_布尔：
        返还 1。
    完。
    若 t 等于 类型_整数：
        返还 8。
    完。
    若 t 等于 类型_浮点：
        返还 8。
    完。
    若 t 等于 类型_字符串：
        返还 16。
    完。
    若 t 等于 WASM编码器_类型_列表：
        返还 16。
    完。
    若 t 等于 WASM编码器_类型_字典：
        返还 16。
    完。
    若 t 等于 WASM编码器_类型_函数：
        返还 8。
    完。
    若 t 等于 类型_结构：
        返还 16。
    完。
    若 t 等于 类型_枚举：
        返还 8。
    完。
    若 t 等于 类型_接口：
        返还 16。
    完。
    若 t 等于 类型_泛型：
        返还 8。
    完。
    返还 8。
完。

wasm_类型对齐 接受 t：
    若 t 等于 类型_布尔：
        返还 1。
    完。
    返还 8。
完。

wasm_别名表_创建 接受 零参：
    返还 一组：（空）。
完。

wasm_别名表_注册 接受 表、别名、原类型：
    做 追加 用 表、一组：别名、原类型。
完。

wasm_别名表_解析 接受 表、别名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 表：
        若 表 的 第 i 项 的 第 0 项 等于 别名：
            返还 表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 别名。
完。

wasm_类型环境_创建 接受 零参：
    返还 一组：一组：（空）、空。
完。

wasm_类型环境_子 接受 父：
    返还 一组：一组：（空）、父。
完。

wasm_类型环境_绑定 接受 env、名id、类型id：
    做 追加 用 env 的 第 0 项、一组：名id、类型id。
完。

本章 收束。
