《代码生成器》

引用《..·..·..·系统扩展·P0-裸机·内联汇编编译》。

引用《..·..·..·编译器·标记常量》。

引用《..·..·..·系统扩展·P0-裸机·原子操作编码》。

引用《..·..·..·系统扩展·P0-裸机·无GC运行时》。

引用《..·..·..·系统扩展·P0-裸机·易变访问》。

引用《..·..·..·系统扩展·P0-裸机·特权指令编码》。

引用《..·..·..·系统扩展·P0-裸机·链接脚本支持》。

引用《..·..·..·系统扩展·P0-裸机·紧凑结构体》。

引用《x86编码器》。

引用《..·..·..·标准库·基础》。

引用《..·..·..·标准库·系统·平台·平台》。

引用《..·..·..·IR·优化器·优化管线》。

引用《..·..·..·编译器·前端入口》。

引用《..·..·..·编译器·分析·错误诊断》。

引用《..·..·..·编译器·规划·核心·发射器》。

引用《..·..·..·编译器·规划·核心·常量》。

引用《..·..·..·运行时·字典引擎·字典引擎》。

可变 代码生成器_目标ELF 为 1。

可变 当前编译模块名 为 「」。

可变 闭包表 为 做 字典。

可变 当前函数列 为 空。

可变 当前函数名表 为 做 字典。

可变 _尾调用模式 为 0。

可变 _行号映射表 为 一组：（空）。

可变 _编译源文件名 为 「」。

_记录行号 接受 行号：
    若 行号 大于 0：
        做 追加 用 _行号映射表、一组：代码位、行号。
    完。
完。

_取节点行号 接受 n：
    若 n 等于 空：
        返还 0。
    完。
    长度 为 做 计数 用 n。
    若 长度 小于 2：
        返还 0。
    完。
    t 为 n 的 第 0 项。
    若 t 等于 标记_定义 并且 长度 大于或等于 5：
        返还 n 的 第 4 项。
    完。
    若 t 等于 标记_可变 并且 长度 大于或等于 4：
        返还 n 的 第 3 项。
    完。
    若 t 等于 标记_函数 并且 长度 大于或等于 5：
        返还 n 的 第 4 项。
    完。
    若 t 等于 标记_返回 并且 长度 大于或等于 4：
        返还 n 的 第 3 项。
    完。
    若 t 等于 标记_如果 并且 长度 大于或等于 3：
        返还 n 的 第 2 项。
    完。
    若 t 等于 标记_否则 并且 长度 大于或等于 3：
        返还 n 的 第 2 项。
    完。
    若 t 等于 标记_当 并且 长度 大于或等于 5：
        返还 n 的 第 4 项。
    完。
    若 t 等于 标记_建 并且 长度 大于或等于 5：
        返还 n 的 第 4 项。
    完。
    若 t 等于 标记_顺序词 并且 长度 大于或等于 5：
        返还 n 的 第 4 项。
    完。
    若 t 等于 标记_关键135 并且 长度 大于或等于 4：
        返还 n 的 第 3 项。
    完。
    若 t 等于 标记_关键136 并且 长度 大于或等于 2：
        返还 n 的 第 1 项。
    完。
    若 t 等于 标记_关键137 并且 长度 大于或等于 2：
        返还 n 的 第 1 项。
    完。
    若 t 等于 节点_变量声明 并且 长度 大于或等于 5：
        返还 n 的 第 4 项。
    完。
    若 t 等于 节点_赋值语句 并且 长度 大于或等于 3：
        返还 n 的 第 2 项。
    完。
    返还 0。
完。

编译_行号映射表 接受 零参：
    返还 _行号映射表。
完。

编译_重置行号映射 接受 零参：
    _行号映射表 为 一组：（空）。
完。

编译_设置源文件名 接受 名：
    _编译源文件名 为 名。
完。

编译_设置当前模块名 接受 名称：
    当前编译模块名 为 名称。
完。

编译_设置目标ELF 接受 值：
    代码生成器_目标ELF 为 值。
完。

编译_当前函数列 接受 零参：
    返还 当前函数列。
完。

编译_当前模块是 接受 名称：
    若 当前编译模块名 等于 空 或者 名称 等于 空：
        返还 假。
    完。
    返还 做 编译_字符串安全相等 用 当前编译模块名、名称。
完。

查字段偏移 接受 类型名、字段名：
    可变 字段列 为 做 字典取 用 结构体表、类型名。
    若 字段列 等于 0 或者 字段列 等于 空：
        返还 0 减 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字段列：
        若 做 编译_字符串安全相等 用 字段列 的 第 i 项、字段名：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 0 减 1。
完。

_字符串比较 接受 a、b：
    la 为 做 字节长度 用 a。
    lb 为 做 字节长度 用 b。
    可变 最小 为 la。
    若 lb 小于 最小：
        最小 为 lb。
    完。
    可变 i 为 0。
    只要 i 小于 最小：
        若 做 字节取 用 a、i 小于 做 字节取 用 b、i：
            返还 真。
        完。
        若 做 字节取 用 a、i 大于 做 字节取 用 b、i：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 la 小于 lb。
完。

编译_是列表值 接受 v：
    若 做 是列表 用 v：
        返还 真。
    完。
    返还 假。
完。

编译_是字符串值 接受 v：
    若 做 是字符串 用 v：
        返还 真。
    完。
    返还 假。
完。

编译_字符串安全相等 接受 a、b：
    若 a 等于 空 或者 b 等于 空：
        返还 假。
    完。
    返还 做 字符串相等 用 a、b。
完。

编译_规范化二元操作 接受 op：
    若 op 等于 空：
        返还 空。
    完。
    若 op 大于或等于 1 并且 op 小于或等于 18：
        返还 op。
    完。
    若 做 编译_字符串安全相等 用 op、「+」：
        返还 1。
    完。
    若 做 编译_字符串安全相等 用 op、「-」：
        返还 2。
    完。
    若 做 编译_字符串安全相等 用 op、「*」：
        返还 3。
    完。
    若 做 编译_字符串安全相等 用 op、「/」：
        返还 4。
    完。
    若 做 编译_字符串安全相等 用 op、「%」：
        返还 5。
    完。
    若 做 编译_字符串安全相等 用 op、「==」：
        返还 6。
    完。
    若 做 编译_字符串安全相等 用 op、「!=」：
        返还 7。
    完。
    若 做 编译_字符串安全相等 用 op、「<」：
        返还 8。
    完。
    若 做 编译_字符串安全相等 用 op、「>」：
        返还 9。
    完。
    若 做 编译_字符串安全相等 用 op、「<=」：
        返还 10。
    完。
    若 做 编译_字符串安全相等 用 op、「>=」：
        返还 11。
    完。
    若 做 编译_字符串安全相等 用 op、「&」：
        返还 12。
    完。
    若 做 编译_字符串安全相等 用 op、「|」：
        返还 13。
    完。
    若 做 编译_字符串安全相等 用 op、「^」：
        返还 14。
    完。
    若 做 编译_字符串安全相等 用 op、「<<」：
        返还 15。
    完。
    若 做 编译_字符串安全相等 用 op、「>>」：
        返还 16。
    完。
    若 做 编译_字符串安全相等 用 op、「&&」：
        返还 17。
    完。
    若 做 编译_字符串安全相等 用 op、「||」：
        返还 18。
    完。
    若 op 等于 标记_加：
        返还 1。
    完。
    若 op 等于 标记_减：
        返还 2。
    完。
    若 op 等于 标记_乘：
        返还 3。
    完。
    若 op 等于 标记_除：
        返还 4。
    完。
    若 op 等于 标记_余：
        返还 5。
    完。
    若 op 等于 标记_等等：
        返还 6。
    完。
    若 op 等于 标记_不等：
        返还 7。
    完。
    若 op 等于 标记_小于：
        返还 8。
    完。
    若 op 等于 标记_大于：
        返还 9。
    完。
    若 op 等于 标记_小等：
        返还 10。
    完。
    若 op 等于 标记_大等：
        返还 11。
    完。
    若 op 等于 标记_位与：
        返还 12。
    完。
    若 op 等于 标记_位或：
        返还 13。
    完。
    若 op 等于 标记_位异或：
        返还 14。
    完。
    若 op 等于 标记_左移：
        返还 15。
    完。
    若 op 等于 标记_右移：
        返还 16。
    完。
    若 op 等于 标记_且 或者 op 等于 标记_标识符：
        返还 17。
    完。
    若 op 等于 标记_或 或者 op 等于 21：
        返还 18。
    完。
    返还 空。
完。

编译_当前函数是 接受 名称：
    返还 做 编译_字符串安全相等 用 当前函数名、名称。
完。

_查字段偏移_全局 接受 字段名：
    所有键 为 做 字典键 用 结构体表。
    可变 最佳名 为 空。
    可变 最佳idx 为 0 减 1。
    可变 si 为 0。
    只要 si 小于 做 计数 用 所有键：
        sn 为 所有键 的 第 si 项。
        sf 为 做 字典取 用 结构体表、sn。
        可变 fi 为 0。
        只要 fi 小于 做 计数 用 sf：
            若 做 编译_字符串安全相等 用 sf 的 第 fi 项、字段名：
                若 最佳名 等于 空 或者 做 _字符串比较 用 sn、最佳名：
                    最佳名 为 sn。
                    最佳idx 为 fi。
                完。
            完。
            fi 为 fi 加 1。
        完。
        si 为 si 加 1。
    完。
    返还 最佳idx。
完。

推断结构类型名 接受 _expr：
    若 _expr 等于 空：
        返还 空。
    完。
    若 _expr 的 第 0 项 等于 标记_抛出：
        返还 _expr 的 第 1 项。
    完。
    若 _expr 的 第 0 项 等于 标记_名称：
        可变 _vt 为 做 字典取 用 变量类型表、_expr 的 第 1 项。
        若 _vt 等于 0 或者 _vt 等于 空：
            返还 空。
        完。
        若 _vt 等于 「__转文本__」 或者 _vt 等于 「__转浮点__」：
            返还 空。
        完。
        返还 _vt。
    完。
    若 _expr 的 第 0 项 等于 节点_索引：
        可变 _base 为 _expr 的 第 1 项。
        若 _base 的 第 0 项 等于 标记_名称：
            可变 _容器名 为 _base 的 第 1 项。
            若 _容器名 等于 「模块表」：
                返还 「模块信息」。
            完。
            若 _容器名 等于 「契约注册表」：
                返还 「模块契约」。
            完。
        完。
    完。
    返还 空。
完。

编译_条件融合跳转 接受 _cf节：
    若 _cf节 等于 空 或者 做 编译_是列表值 用 _cf节 等于 假 或者 做 计数 用 _cf节 小于 4：
        返还 空。
    完。
    若 _cf节 的 第 0 项 不等于 节点_二元：
        返还 空。
    完。
    _cf_op 为 做 编译_规范化二元操作 用 _cf节 的 第 1 项。
    若 _cf_op 小于 6 或者 _cf_op 大于 11：
        返还 空。
    完。
    若 做 是浮点节点 用 _cf节 的 第 2 项 或者 做 是浮点节点 用 _cf节 的 第 3 项：
        返还 空。
    完。
    若 做 是字符串节点 用 _cf节 的 第 2 项 或者 做 是字符串节点 用 _cf节 的 第 3 项：
        返还 空。
    完。
    _cfl 为 _cf节 的 第 2 项。
    _cfr 为 _cf节 的 第 3 项。
    可变 _用立即数 为 假。
    可变 _imm 为 0。
    若 做 是列表 用 _cfr 并且 做 计数 用 _cfr 大于或等于 2 并且 _cfr 的 第 0 项 等于 标记_数字 并且 做 类型 用 _cfr 的 第 1 项 等于 「整数」：
        _v 为 _cfr 的 第 1 项。
        若 _v 大于或等于 0 减 2147483648 并且 _v 小于或等于 2147483647：
            _用立即数 为 真。
            _imm 为 _v。
        完。
    完。
    若 _用立即数：
        做 编译_表达式 用 _cfl。
        做 发射组 用 一组：0x48、0x3D。
        做 发射32 用 _imm。
    否则若 做 是简单 用 _cfr：
        做 编译_表达式 用 _cfl。
        做 发射_mov_rcx_rax。
        做 编译_表达式 用 _cfr。
        做 发射组 用 一组：0x48、0x87、0xC1。
        做 发射组 用 一组：0x48、0x39、0xC8。
    否则：
        做 编译_表达式 用 _cfl。
        做 发射 用 0x50。
        做 编译_表达式 用 _cfr。
        做 发射_mov_rcx_rax。
        做 发射 用 0x58。
        做 发射组 用 一组：0x48、0x39、0xC8。
    完。
    可变 _cf_op_byte 为 0x85。
    若 _cf_op 等于 6：
        _cf_op_byte 为 0x85。
    完。
    若 _cf_op 等于 7：
        _cf_op_byte 为 0x84。
    完。
    若 _cf_op 等于 8：
        _cf_op_byte 为 0x8D。
    完。
    若 _cf_op 等于 9：
        _cf_op_byte 为 0x8E。
    完。
    若 _cf_op 等于 10：
        _cf_op_byte 为 0x8F。
    完。
    若 _cf_op 等于 11：
        _cf_op_byte 为 0x8C。
    完。
    做 发射组 用 一组：0x0F、_cf_op_byte。
    _跳位 为 代码位。
    做 发射32 用 0。
    返还 _跳位。
完。

表达式_已是布尔 接受 节点：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 2：
        返还 假。
    完。
    头 为 节点 的 第 0 项。
    若 头 等于 节点_二元 并且 做 计数 用 节点 大于或等于 4：
        _op 为 做 编译_规范化二元操作 用 节点 的 第 1 项。
        若 _op 大于或等于 6 并且 _op 小于或等于 11：
            返还 真。
        完。
        若 _op 等于 17 或者 _op 等于 18：
            返还 真。
        完。
    完。
    若 头 等于 节点_一元正：
        返还 真。
    完。
    若 头 等于 标记_数字 并且 做 计数 用 节点 大于或等于 2：
        若 节点 的 第 1 项 等于 0 或者 节点 的 第 1 项 等于 1：
            返还 真。
        完。
    完。
    返还 假。
完。

_是单节点 接受 v：
    若 做 编译_是列表值 用 v 等于 假 或者 做 计数 用 v 小于 1：
        返还 假。
    完。
    _h 为 v 的 第 0 项。
    若 做 编译_是列表值 用 _h：
        返还 假。
    完。
    若 _h 等于 空：
        返还 假。
    完。
    返还 真。
完。

_收集局部变量名 接受 节点、集合：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 2：
        返还 空。
    完。
    头 为 节点 的 第 0 项。
    若 头 等于 节_定义 并且 做 计数 用 节点 大于或等于 2：
        做 字典设 用 集合、节点 的 第 1 项、1。
    完。
    可变 ci 为 1。
    只要 ci 小于 做 计数 用 节点：
        _sub 为 节点 的 第 ci 项。
        若 做 编译_是列表值 用 _sub：
            做 _收集局部变量名 用 _sub、集合。
            可变 li 为 0。
            只要 li 小于 做 计数 用 _sub：
                若 做 编译_是列表值 用 _sub 的 第 li 项：
                    做 _收集局部变量名 用 _sub 的 第 li 项、集合。
                完。
                li 为 li 加 1。
            完。
        完。
        ci 为 ci 加 1。
    完。
完。

_浮点局部变量_预标记 接受 节点：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 2：
        返还 假。
    完。
    可变 有新 为 假。
    头 为 节点 的 第 0 项。
    若 头 等于 节_定义 并且 做 计数 用 节点 大于或等于 3：
        _def值 为 节点 的 第 2 项。
        _def名 为 节点 的 第 1 项。
        若 做 是浮点节点 用 _def值：
            _旧 为 做 字典取 用 变量类型表、_def名。
            若 _旧 不等于 「__转浮点__」 并且 _旧 不等于 「__浮点列表__」：
                做 字典设 用 变量类型表、_def名、「__转浮点__」。
                有新 为 真。
            完。
        完。
        若 做 是浮点列表字面量 用 _def值：
            _旧2 为 做 字典取 用 变量类型表、_def名。
            若 _旧2 不等于 「__浮点列表__」：
                做 字典设 用 变量类型表、_def名、「__浮点列表__」。
                有新 为 真。
            完。
        完。
        若 做 编译_是列表值 用 _def值 并且 做 计数 用 _def值 大于或等于 3 并且 _def值 的 第 0 项 等于 节点_调用：
            _cargs 为 _def值 的 第 2 项。
            若 做 编译_是列表值 用 _cargs：
                可变 _有列表参数 为 假。
                可变 _cai 为 0。
                只要 _cai 小于 做 计数 用 _cargs：
                    _ca 为 _cargs 的 第 _cai 项。
                    若 做 编译_是列表值 用 _ca 并且 做 计数 用 _ca 大于或等于 2 并且 _ca 的 第 0 项 等于 标记_名称：
                        _cavt 为 做 字典取 用 变量类型表、_ca 的 第 1 项。
                        若 _cavt 等于 「__浮点列表__」：
                            _有列表参数 为 真。
                        完。
                    完。
                    若 做 是浮点列表字面量 用 _ca：
                        _有列表参数 为 真。
                    完。
                    _cai 为 _cai 加 1。
                完。
                若 _有列表参数：
                    _旧3 为 做 字典取 用 变量类型表、_def名。
                    若 _旧3 不等于 「__浮点列表__」：
                        做 字典设 用 变量类型表、_def名、「__浮点列表__」。
                        有新 为 真。
                    完。
                完。
            完。
            _f名节点 为 _def值 的 第 1 项。
            可变 _f名 为 空。
            若 做 是字符串 用 _f名节点：
                _f名 为 _f名节点。
            否则若 做 编译_是列表值 用 _f名节点 并且 做 计数 用 _f名节点 大于或等于 2 并且 _f名节点 的 第 0 项 等于 标记_名称：
                _f名 为 _f名节点 的 第 1 项。
            完。
            若 _f名 不等于 空 并且 做 是字符串 用 _f名：
                _rt 为 做 字典取 用 函数返回类型表、_f名。
                若 _rt 等于 「__浮点列表__」：
                    _旧r1 为 做 字典取 用 变量类型表、_def名。
                    若 _旧r1 不等于 「__浮点列表__」：
                        做 字典设 用 变量类型表、_def名、「__浮点列表__」。
                        有新 为 真。
                    完。
                否则若 _rt 等于 「__转浮点__」：
                    _旧r2 为 做 字典取 用 变量类型表、_def名。
                    若 _旧r2 不等于 「__转浮点__」 并且 _旧r2 不等于 「__浮点列表__」：
                        做 字典设 用 变量类型表、_def名、「__转浮点__」。
                        有新 为 真。
                    完。
                完。
            完。
        完。
    完。
    若 头 等于 节点_调用 并且 做 计数 用 节点 大于或等于 3 并且 做 编译_字符串安全相等 用 节点 的 第 1 项、「追加」：
        _aargs 为 节点 的 第 2 项。
        若 做 编译_是列表值 用 _aargs 并且 做 计数 用 _aargs 大于或等于 2：
            _tgt 为 _aargs 的 第 0 项。
            _val 为 _aargs 的 第 1 项。
            若 做 编译_是列表值 用 _tgt 并且 做 计数 用 _tgt 大于或等于 2 并且 _tgt 的 第 0 项 等于 标记_名称 并且 做 是浮点节点 用 _val：
                _mn 为 _tgt 的 第 1 项。
                _旧m 为 做 字典取 用 变量类型表、_mn。
                若 _旧m 不等于 「__浮点列表__」：
                    做 字典设 用 变量类型表、_mn、「__浮点列表__」。
                    有新 为 真。
                完。
            完。
        完。
    完。
    可变 ci 为 1。
    只要 ci 小于 做 计数 用 节点：
        _sub 为 节点 的 第 ci 项。
        若 做 编译_是列表值 用 _sub：
            若 做 _浮点局部变量_预标记 用 _sub：
                有新 为 真。
            完。
            可变 li 为 0。
            只要 li 小于 做 计数 用 _sub：
                若 做 编译_是列表值 用 _sub 的 第 li 项：
                    若 做 _浮点局部变量_预标记 用 _sub 的 第 li 项：
                        有新 为 真。
                    完。
                完。
                li 为 li 加 1。
            完。
        完。
        ci 为 ci 加 1。
    完。
    返还 有新。
完。

_返回类型扫描_填函数返回类型 接受 函数名、节点：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 1：
        返还 空。
    完。
    若 节点 的 第 0 项 等于 节_返回 并且 做 计数 用 节点 大于或等于 2：
        _rv 为 节点 的 第 1 项。
        若 做 是浮点列表字面量 用 _rv：
            做 字典设 用 函数返回类型表、函数名、「__浮点列表__」。
        否则若 做 是浮点节点 用 _rv：
            _旧 为 做 字典取 用 函数返回类型表、函数名。
            若 _旧 不等于 「__浮点列表__」：
                做 字典设 用 函数返回类型表、函数名、「__转浮点__」。
            完。
        否则若 做 编译_是列表值 用 _rv 并且 做 计数 用 _rv 大于或等于 2 并且 _rv 的 第 0 项 等于 标记_名称：
            _vt 为 做 字典取 用 变量类型表、_rv 的 第 1 项。
            若 _vt 等于 「__浮点列表__」：
                做 字典设 用 函数返回类型表、函数名、「__浮点列表__」。
            否则若 _vt 等于 「__转浮点__」：
                _旧2 为 做 字典取 用 函数返回类型表、函数名。
                若 _旧2 不等于 「__浮点列表__」：
                    做 字典设 用 函数返回类型表、函数名、「__转浮点__」。
                完。
            完。
        完。
    完。
    可变 _ci 为 0。
    只要 _ci 小于 做 计数 用 节点：
        _sub 为 节点 的 第 _ci 项。
        若 做 编译_是列表值 用 _sub：
            做 _返回类型扫描_填函数返回类型 用 函数名、_sub。
        完。
        _ci 为 _ci 加 1。
    完。
    返还 空。
完。

_调用点扫描_填参数类型 接受 节点：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 2：
        返还 空。
    完。
    若 节点 的 第 0 项 等于 节点_调用 并且 做 计数 用 节点 大于或等于 3：
        _f名节点 为 节点 的 第 1 项。
        _args 为 节点 的 第 2 项。
        可变 _f名 为 空。
        若 做 是字符串 用 _f名节点：
            _f名 为 _f名节点。
        否则若 做 编译_是列表值 用 _f名节点 并且 做 计数 用 _f名节点 大于或等于 2 并且 _f名节点 的 第 0 项 等于 标记_名称：
            _f名 为 _f名节点 的 第 1 项。
        完。
        若 _f名 不等于 空 并且 做 是字符串 用 _f名 并且 做 编译_是列表值 用 _args：
            可变 _ai 为 0。
            只要 _ai 小于 做 计数 用 _args：
                _arg 为 _args 的 第 _ai 项。
                _key 为 做 字符串拼接 用 _f名、做 字符串拼接 用 「@」、做 转字符串 用 _ai。
                若 做 是浮点节点 用 _arg：
                    _旧 为 做 字典取 用 函数参数类型表、_key。
                    若 _旧 不等于 「__浮点列表__」：
                        做 字典设 用 函数参数类型表、_key、「__转浮点__」。
                    完。
                完。
                若 做 是浮点列表字面量 用 _arg：
                    做 字典设 用 函数参数类型表、_key、「__浮点列表__」。
                完。
                若 做 编译_是列表值 用 _arg 并且 做 计数 用 _arg 大于或等于 2 并且 _arg 的 第 0 项 等于 标记_名称：
                    _vt 为 做 字典取 用 变量类型表、_arg 的 第 1 项。
                    若 _vt 等于 「__浮点列表__」：
                        做 字典设 用 函数参数类型表、_key、「__浮点列表__」。
                    完。
                完。
                _ai 为 _ai 加 1。
            完。
        完。
    完。
    可变 _ci 为 0。
    只要 _ci 小于 做 计数 用 节点：
        _sub 为 节点 的 第 _ci 项。
        若 做 编译_是列表值 用 _sub：
            做 _调用点扫描_填参数类型 用 _sub。
        完。
        _ci 为 _ci 加 1。
    完。
    返还 空。
完。

_浮点参数推断_扫描 接受 节点、参数集：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 2：
        返还 假。
    完。
    可变 有新标记 为 假。
    头 为 节点 的 第 0 项。
    若 头 等于 节_定义 并且 做 计数 用 节点 大于或等于 3：
        _rv名 为 节点 的 第 1 项。
        _rv值 为 节点 的 第 2 项。
        _rvt 为 做 字典取 用 变量类型表、_rv名。
        若 _rvt 等于 「__转浮点__」：
            若 做 _浮点参数推断_传播子树 用 _rv值、参数集：
                有新标记 为 真。
            完。
        完。
    完。
    若 头 等于 节点_二元 并且 做 计数 用 节点 大于或等于 4：
        _op 为 做 编译_规范化二元操作 用 节点 的 第 1 项。
        若 _op 大于或等于 1 并且 _op 小于或等于 4 或者 _op 大于或等于 6 并且 _op 小于或等于 11：
            _左 为 节点 的 第 2 项。
            _右 为 节点 的 第 3 项。
            _左浮 为 做 是浮点节点 用 _左。
            _右浮 为 做 是浮点节点 用 _右。
            若 _左浮 并且 做 _浮点参数推断_传播子树 用 _右、参数集：
                有新标记 为 真。
            完。
            若 _右浮 并且 做 _浮点参数推断_传播子树 用 _左、参数集：
                有新标记 为 真。
            完。
        完。
    完。
    可变 ci 为 1。
    只要 ci 小于 做 计数 用 节点：
        _sub 为 节点 的 第 ci 项。
        若 做 编译_是列表值 用 _sub：
            若 做 _浮点参数推断_扫描 用 _sub、参数集：
                有新标记 为 真。
            完。
            可变 li 为 0。
            只要 li 小于 做 计数 用 _sub：
                若 做 编译_是列表值 用 _sub 的 第 li 项：
                    若 做 _浮点参数推断_扫描 用 _sub 的 第 li 项、参数集：
                        有新标记 为 真。
                    完。
                完。
                li 为 li 加 1。
            完。
        完。
        ci 为 ci 加 1。
    完。
    返还 有新标记。
完。

_浮点参数推断_标记 接受 节点、参数集：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 2：
        返还 假。
    完。
    若 节点 的 第 0 项 等于 标记_名称：
        _pname2 为 节点 的 第 1 项。
        若 做 字典取 用 参数集、_pname2 等于 空：
            返还 假。
        完。
        _old2 为 做 字典取 用 变量类型表、_pname2。
        若 _old2 等于 「__转浮点__」 或者 _old2 等于 「__浮点列表__」：
            返还 假。
        完。
        做 字典设 用 变量类型表、_pname2、「__转浮点__」。
        返还 真。
    完。
    若 节点 的 第 0 项 不等于 节_索引：
        返还 假。
    完。
    _base 为 节点 的 第 1 项。
    若 做 编译_是列表值 用 _base 等于 假 或者 做 计数 用 _base 小于 2：
        返还 假。
    完。
    若 _base 的 第 0 项 不等于 标记_名称：
        返还 假。
    完。
    _pname 为 _base 的 第 1 项。
    若 做 字典取 用 参数集、_pname 等于 空：
        返还 假。
    完。
    _old 为 做 字典取 用 变量类型表、_pname。
    若 _old 等于 「__浮点列表__」：
        返还 假。
    完。
    做 字典设 用 变量类型表、_pname、「__浮点列表__」。
    返还 真。
完。

_浮点参数推断_传播子树 接受 节点、参数集：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 2：
        返还 假。
    完。
    可变 有新 为 假。
    若 做 _浮点参数推断_标记 用 节点、参数集：
        有新 为 真。
    完。
    若 节点 的 第 0 项 等于 节点_二元 并且 做 计数 用 节点 大于或等于 4：
        若 做 _浮点参数推断_传播子树 用 节点 的 第 2 项、参数集：
            有新 为 真。
        完。
        若 做 _浮点参数推断_传播子树 用 节点 的 第 3 项、参数集：
            有新 为 真。
        完。
    完。
    若 节点 的 第 0 项 等于 节_索引 并且 做 计数 用 节点 大于或等于 2：
        若 做 _浮点参数推断_传播子树 用 节点 的 第 1 项、参数集：
            有新 为 真。
        完。
    完。
    若 节点 的 第 0 项 等于 节点_一元正 并且 做 计数 用 节点 大于或等于 2：
        若 做 _浮点参数推断_传播子树 用 节点 的 第 1 项、参数集：
            有新 为 真。
        完。
    完。
    返还 有新。
完。

是浮点列表字面量 接受 节点：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 2：
        返还 假。
    完。
    若 节点 的 第 0 项 不等于 节_列表：
        返还 假。
    完。
    元素列 为 节点 的 第 1 项。
    若 做 编译_是列表值 用 元素列 等于 假 或者 做 计数 用 元素列 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 元素列：
        若 做 是浮点节点 用 元素列 的 第 i 项 等于 假：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

是浮点节点 接受 节点：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 2：
        返还 假。
    完。
    若 节点 的 第 0 项 等于 节_浮点：
        返还 真。
    完。
    若 节点 的 第 0 项 等于 标记_名称：
        可变 vt 为 做 字典取 用 变量类型表、节点 的 第 1 项。
        若 vt 等于 0：
            返还 假。
        完。
        若 vt 等于 空：
            返还 假。
        完。
        若 vt 等于 「__转浮点__」：
            返还 真。
        完。
        返还 假。
    完。
    若 节点 的 第 0 项 等于 节点_调用：
        若 做 编译_字符串安全相等 用 节点 的 第 1 项、「浮点」：
            返还 真。
        完。
        _args 为 节点 的 第 2 项。
        若 做 编译_是列表值 用 _args：
            可变 _ai 为 0。
            只要 _ai 小于 做 计数 用 _args：
                若 做 是浮点节点 用 _args 的 第 _ai 项：
                    返还 真。
                完。
                _ai 为 _ai 加 1。
            完。
        完。
        返还 假。
    完。
    若 节点 的 第 0 项 等于 节点_二元：
        若 做 计数 用 节点 小于 4：
            返还 假。
        完。
        若 做 是浮点节点 用 节点 的 第 2 项：
            返还 真。
        完。
        若 做 是浮点节点 用 节点 的 第 3 项：
            返还 真。
        完。
    完。
    若 节点 的 第 0 项 等于 节_索引：
        _base 为 节点 的 第 1 项。
        若 做 编译_是列表值 用 _base 并且 做 计数 用 _base 大于或等于 2 并且 _base 的 第 0 项 等于 标记_名称：
            可变 _bvt 为 做 字典取 用 变量类型表、_base 的 第 1 项。
            若 _bvt 等于 「__浮点列表__」：
                返还 真。
            完。
        完。
        若 做 是浮点列表字面量 用 _base：
            返还 真。
        完。
    完。
    返还 假。
完。

是字符串节点 接受 节点：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 2：
        返还 假。
    完。
    若 节点 的 第 0 项 等于 标记_字串：
        返还 真。
    完。
    若 节点 的 第 0 项 等于 标记_名称：
        vt 为 做 字典取 用 变量类型表、节点 的 第 1 项。
        若 vt 等于 0：
            返还 假。
        完。
        若 vt 等于 空：
            返还 假。
        完。
        若 vt 等于 「__转文本__」：
            返还 真。
        完。
        返还 假。
    完。
    若 节点 的 第 0 项 等于 节点_调用：
        可变 n 为 节点 的 第 1 项。
        若 做 编译_字符串安全相等 用 n、「转字符串」：
            返还 真。
        完。
        若 做 编译_字符串安全相等 用 n、「字符串拼接」：
            返还 真。
        完。
        若 做 编译_字符串安全相等 用 n、「子串」：
            返还 真。
        完。
        若 做 编译_字符串安全相等 用 n、「替换」：
            返还 真。
        完。
        若 做 编译_字符串安全相等 用 n、「字符」：
            返还 真。
        完。
        若 做 编译_字符串安全相等 用 n、「读文件」：
            返还 真。
        完。
        若 做 编译_字符串安全相等 用 n、「拼接」：
            返还 真。
        完。
        若 做 编译_字符串安全相等 用 n、「大写化」：
            返还 真。
        完。
        若 做 编译_字符串安全相等 用 n、「小写化」：
            返还 真。
        完。
        返还 假。
    完。
    若 节点 的 第 0 项 等于 节点_二元：
        若 做 计数 用 节点 小于 4：
            返还 假。
        完。
        _str_op 为 做 编译_规范化二元操作 用 节点 的 第 1 项。
        若 _str_op 等于 1：
            若 做 是字符串节点 用 节点 的 第 2 项：
                返还 真。
            完。
            若 做 是字符串节点 用 节点 的 第 3 项：
                返还 真。
            完。
        完。
    完。
    返还 假。
完。

是简单 接受 节点：
    若 节点 的 第 0 项 等于 标记_数字：
        返还 真。
    完。
    若 节点 的 第 0 项 等于 节点_空字面量：
        返还 真。
    完。
    若 节点 的 第 0 项 等于 标记_名称：
        返还 真。
    完。
    若 节点 的 第 0 项 等于 节_浮点：
        返还 真。
    完。
    返还 假。
完。

编译_发射_本地相等比较 接受 op：
    做 发射组 用 一组：0x48、0x39、0xC8。
    若 op 等于 6：
        做 发射组 用 一组：0x0F、0x94、0xC0。
    否则：
        做 发射组 用 一组：0x0F、0x95、0xC0。
    完。
    做 发射组 用 一组：0x0F、0xB6、0xC0。
    返还 空。
完。

发射_二元操作 接受 op：
    若 op 等于 1：
        做 发射_加。
        返还 空。
    完。
    若 op 等于 2：
        做 发射_减。
        返还 空。
    完。
    若 op 等于 3：
        做 发射_乘。
        返还 空。
    完。
    若 op 等于 4：
        做 发射组 用 一组：0x48、0x85、0xC9。
        做 发射组 用 一组：0x75、0x06。
        做 发射组 用 一组：0x31、0xC0。
        做 发射组 用 一组：0x31、0xD2。
        做 发射组 用 一组：0xEB、0x05。
        做 发射组 用 一组：0x48、0x99。
        做 发射组 用 一组：0x48、0xF7、0xF9。
        返还 空。
    完。
    若 op 等于 5：
        做 发射组 用 一组：0x48、0x85、0xC9。
        做 发射组 用 一组：0x75、0x06。
        做 发射组 用 一组：0x31、0xC0。
        做 发射组 用 一组：0x31、0xD2。
        做 发射组 用 一组：0xEB、0x05。
        做 发射组 用 一组：0x48、0x99。
        做 发射组 用 一组：0x48、0xF7、0xF9。
        做 发射组 用 一组：0x48、0x89、0xD0。
        返还 空。
    完。
    若 op 等于 8：
        做 发射组 用 一组：0x48、0x39、0xC8。
        做 发射 用 0x0F。
        做 发射 用 0x9C。
        做 发射 用 0xC0。
        做 发射组 用 一组：0x0F、0xB6、0xC0。
        返还 空。
    完。
    若 op 等于 9：
        做 发射组 用 一组：0x48、0x39、0xC8。
        做 发射 用 0x0F。
        做 发射 用 0x9F。
        做 发射 用 0xC0。
        做 发射组 用 一组：0x0F、0xB6、0xC0。
        返还 空。
    完。
    若 op 等于 10：
        做 发射组 用 一组：0x48、0x39、0xC8。
        做 发射 用 0x0F。
        做 发射 用 0x9E。
        做 发射 用 0xC0。
        做 发射组 用 一组：0x0F、0xB6、0xC0。
        返还 空。
    完。
    若 op 等于 11：
        做 发射组 用 一组：0x48、0x39、0xC8。
        做 发射 用 0x0F。
        做 发射 用 0x9D。
        做 发射 用 0xC0。
        做 发射组 用 一组：0x0F、0xB6、0xC0。
        返还 空。
    完。
    若 op 等于 6：
        做 编译_发射_本地相等比较 用 op。
        返还 空。
    完。
    若 op 等于 7：
        做 编译_发射_本地相等比较 用 op。
        返还 空。
    完。
    若 op 等于 12：
        做 发射组 用 一组：0x48、0x21、0xC8。
        返还 空。
    完。
    若 op 等于 13：
        做 发射组 用 一组：0x48、0x09、0xC8。
        返还 空。
    完。
    若 op 等于 14：
        做 发射组 用 一组：0x48、0x31、0xC8。
        返还 空。
    完。
    若 op 等于 15：
        做 发射组 用 一组：0x48、0xD3、0xE0。
        返还 空。
    完。
    若 op 等于 16：
        做 发射组 用 一组：0x48、0xD3、0xF8。
        返还 空。
    完。
    若 op 等于 17：
        做 发射组 用 一组：0x48、0x85、0xC9。
        做 发射组 用 一组：0x75、0x02。
        做 发射组 用 一组：0x31、0xC0。
        返还 空。
    完。
    若 op 等于 18：
        做 发射组 用 一组：0x48、0x85、0xC9。
        做 发射组 用 一组：0x74、0x03。
        做 发射组 用 一组：0x48、0x89、0xC8。
        返还 空。
    完。
    返还 空。
完。

编译_发射_相等比较 接受 op、左、右：
    若 op 不等于 6 并且 op 不等于 7：
        返还 0。
    完。
    若 做 是浮点节点 用 左 或者 做 是浮点节点 用 右：
        返还 0。
    完。
    若 做 是字符串节点 用 左 或者 做 是字符串节点 用 右：
        做 编译_处理__字符串等于_ 用 一组：左、右。
        若 op 等于 7：
            做 发射组 用 一组：0x48、0x83、0xF0、0x01。
        完。
        返还 1。
    完。
    若 做 是简单 用 左：
        做 编译_表达式 用 右。
        做 发射_mov_rcx_rax。
        做 编译_表达式 用 左。
    否则：
        做 编译_表达式 用 左。
        做 发射 用 0x50。
        做 编译_表达式 用 右。
        做 发射_mov_rcx_rax。
        做 发射 用 0x58。
    完。
    做 编译_发射_本地相等比较 用 op。
    返还 1。
完。

编译_SIMD内建 接受 名、参数：
    可变 _op_码 为 0。
    若 做 编译_字符串安全相等 用 名、「__simd_浮点加」：
        _op_码 为 1。
    否则若 做 编译_字符串安全相等 用 名、「__simd_浮点减」：
        _op_码 为 2。
    否则若 做 编译_字符串安全相等 用 名、「__simd_浮点乘」：
        _op_码 为 3。
    否则若 做 编译_字符串安全相等 用 名、「__simd_浮点除」：
        _op_码 为 4。
    否则若 做 编译_字符串安全相等 用 名、「__simd_浮点最大」：
        _op_码 为 5。
    否则若 做 编译_字符串安全相等 用 名、「__simd_浮点最小」：
        _op_码 为 6。
    否则若 做 编译_字符串安全相等 用 名、「__simd_浮点平方根」：
        _op_码 为 7。
    完。
    若 _op_码 等于 0：
        返还 假。
    完。
    若 _op_码 等于 7：
        若 做 计数 用 参数 小于 1：
            返还 假。
        完。
        做 编译_表达式 用 参数 的 第 0 项。
        做 发射组 用 一组：0x66、0x48、0x0F、0x6E、0xC0。
        做 发射组 用 一组：0xF2、0x0F、0x51、0xC0。
        做 发射组 用 一组：0x66、0x48、0x0F、0x7E、0xC0。
        _AI后端_SIMD发射数 为 _AI后端_SIMD发射数 加 1。
        返还 真。
    完。
    若 做 计数 用 参数 小于 2：
        返还 假。
    完。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0x66、0x48、0x0F、0x6E、0xC0。
    做 发射组 用 一组：0x66、0x48、0x0F、0x6E、0xC9。
    若 _op_码 等于 1：
        做 发射组 用 一组：0xF2、0x0F、0x58、0xC1。
    否则若 _op_码 等于 2：
        做 发射组 用 一组：0xF2、0x0F、0x5C、0xC1。
    否则若 _op_码 等于 3：
        做 发射组 用 一组：0xF2、0x0F、0x59、0xC1。
    否则若 _op_码 等于 4：
        做 发射组 用 一组：0xF2、0x0F、0x5E、0xC1。
    否则若 _op_码 等于 5：
        做 发射组 用 一组：0xF2、0x0F、0x5F、0xC1。
    否则若 _op_码 等于 6：
        做 发射组 用 一组：0xF2、0x0F、0x5D、0xC1。
    完。
    做 发射组 用 一组：0x66、0x48、0x0F、0x7E、0xC0。
    _AI后端_SIMD发射数 为 _AI后端_SIMD发射数 加 1。
    返还 真。
完。

编译_系统内建 接受 名、参数：
    若 名 等于 「处理器名」：
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_处理器品牌」。
        做 发射 用 0xE8。
        做 发射32 用 0。
        返还 1。
    完。
    若 名 等于 「总内存」：
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_内存总量」。
        做 发射 用 0xE8。
        做 发射32 用 0。
        返还 1。
    完。
    若 名 等于 「可用内存」：
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_内存可用」。
        做 发射 用 0xE8。
        做 发射32 用 0。
        返还 1。
    完。
    若 名 等于 「时间戳」：
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_时间戳」。
        做 发射 用 0xE8。
        做 发射32 用 0。
        返还 1。
    完。
    若 名 等于 「延时」：
        做 编译_表达式 用 参数 的 第 0 项。
        做 发射_mov_rcx_rax。
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_睡眠」。
        做 发射 用 0xE8。
        做 发射32 用 0。
        返还 1。
    完。
    若 名 等于 「随机数」：
        做 发射组 用 一组：0x0F、0x31。
        做 发射组 用 一组：0x25、0xFF、0xFF、0x0F、0x00。
        返还 1。
    完。
    若 名 等于 「文件存在」：
        做 编译_表达式 用 参数 的 第 0 项。
        做 发射_mov_rcx_rax。
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_文件存在」。
        做 发射 用 0xE8。
        做 发射32 用 0。
        返还 1。
    完。
    若 名 等于 「退出」：
        做 编译_表达式 用 参数 的 第 0 项。
        做 发射_mov_rcx_rax。
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_退出」。
        做 发射 用 0xE8。
        做 发射32 用 0。
        返还 1。
    完。
    返还 0。
完。

编译_当前是裸机目标 接受 零参：
    若 代码生成器_目标ELF 等于 2：
        返还 1。
    完。
    返还 0。
完。

编译_内联汇编内建 接受 名、参数：
    若 名 等于 「内联汇编_间接调用」 并且 做 计数 用 参数 等于 1：
        做 编译_表达式 用 参数 的 第 0 项。
        做 发射组 用 一组：0xFF、0xD0。
        返还 1。
    完。
    若 名 不等于 「内联汇编」 并且 名 不等于 「内联汇编_返回值」 或者 做 计数 用 参数 小于 1：
        返还 0。
    完。
    模板节点 为 参数 的 第 0 项。
    若 做 是列表 用 模板节点 等于 假 或者 做 计数 用 模板节点 小于 2 或者 模板节点 的 第 0 项 不等于 101：
        返还 0。
    完。
    模板 为 模板节点 的 第 1 项。
    若 名 等于 「内联汇编」 并且 模板 等于 「」：
        返还 1。
    完。
    若 名 等于 「内联汇编」 并且 模板 等于 「outb %b0, %w1」 并且 做 计数 用 参数 等于 3：
        做 编译_表达式 用 参数 的 第 2 项。
        做 发射 用 0x50。
        做 编译_表达式 用 参数 的 第 1 项。
        做 发射 用 0x5A。
        做 发射 用 0xEE。
        返还 1。
    完。
    若 名 等于 「内联汇编」 并且 模板 等于 「outw %w0, %w1」 并且 做 计数 用 参数 等于 3：
        做 编译_表达式 用 参数 的 第 2 项。
        做 发射 用 0x50。
        做 编译_表达式 用 参数 的 第 1 项。
        做 发射 用 0x5A。
        做 发射 用 0x66。
        做 发射 用 0xEF。
        返还 1。
    完。
    若 名 等于 「内联汇编」 并且 模板 等于 「outl %0, %w1」 并且 做 计数 用 参数 等于 3：
        做 编译_表达式 用 参数 的 第 2 项。
        做 发射 用 0x50。
        做 编译_表达式 用 参数 的 第 1 项。
        做 发射 用 0x5A。
        做 发射 用 0xEF。
        返还 1。
    完。
    若 名 等于 「内联汇编_返回值」 并且 模板 等于 「inb %w1, %b0」 并且 做 计数 用 参数 等于 2：
        做 编译_表达式 用 参数 的 第 1 项。
        做 发射组 用 一组：0x66、0x89、0xC2。
        做 发射组 用 一组：0x31、0xC0。
        做 发射 用 0xEC。
        返还 1。
    完。
    若 名 等于 「内联汇编_返回值」 并且 模板 等于 「inw %w1, %w0」 并且 做 计数 用 参数 等于 2：
        做 编译_表达式 用 参数 的 第 1 项。
        做 发射组 用 一组：0x66、0x89、0xC2。
        做 发射组 用 一组：0x31、0xC0。
        做 发射 用 0x66。
        做 发射 用 0xED。
        返还 1。
    完。
    若 名 等于 「内联汇编_返回值」 并且 模板 等于 「inl %w1, %0」 并且 做 计数 用 参数 等于 2：
        做 编译_表达式 用 参数 的 第 1 项。
        做 发射组 用 一组：0x66、0x89、0xC2。
        做 发射组 用 一组：0x31、0xC0。
        做 发射 用 0xED。
        返还 1。
    完。
    若 名 等于 「内联汇编_返回值」 并且 模板 等于 「rdtsc」 并且 做 计数 用 参数 等于 1：
        做 发射组 用 一组：0x0F、0x31。
        做 发射组 用 一组：0x48、0xC1、0xE2、0x20。
        做 发射组 用 一组：0x48、0x09、0xD0。
        返还 1。
    完。
    若 名 等于 「内联汇编_返回值」 并且 模板 等于 「rdtscp」 并且 做 计数 用 参数 等于 1：
        做 发射组 用 一组：0x0F、0x01、0xF9。
        做 发射组 用 一组：0x48、0xC1、0xE2、0x20。
        做 发射组 用 一组：0x48、0x09、0xD0。
        返还 1。
    完。
    若 做 计数 用 参数 等于 1：
        做 编译_汇编文本块 用 一组：模板。
        返还 1。
    完。
    返还 0。
完。

编译_裸机内建 接受 名、参数：
    若 做 编译_当前是裸机目标 等于 0：
        返还 0。
    完。
    若 做 编译_链接操作 用 名、参数：
        返还 1。
    完。
    若 做 编译_无GC操作 用 名、参数：
        返还 1。
    完。
    若 做 编译_volatile操作 用 名、参数：
        返还 1。
    完。
    若 做 编译_原子操作 用 名、参数：
        返还 1。
    完。
    若 做 编译_特权指令 用 名、参数：
        做 特权_记录编码。
        返还 1。
    完。
    返还 0。
完。

编译_处理__计数_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表计数」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__追加_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表追加」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__子串_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射组 用 一组：0x49、0x89、0xC0。
    做 发射 用 0x5A。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_子串」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__字节取_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0x48、0x01、0xC8。
    做 发射组 用 一组：0x0F、0xB6、0x00。
    返还 1。
完。

编译_处理__字节长度_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表计数」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__编码_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_字符编码」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__字符_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_编码字符」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__转字符串_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_整数转串」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__缓冲区_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表新建」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__缓冲读_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0x48、0x8B、0x40、0x10。
    做 发射组 用 一组：0x0F、0xB6、0x04、0xC8。
    返还 1。
完。

编译_处理__插入_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射组 用 一组：0x49、0x89、0xC0。
    做 发射 用 0x5A。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表插入」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__删除_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表删除」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__字符串拼接_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_串拼接」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__拼接_ 接受 参数：
    返还 做 编译_处理__字符串拼接_ 用 参数。
完。

编译_处理__整数_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_解析整数」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__分割_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_串分割」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__范围_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_范围」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__字符串等于_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_串相等」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__字典_ 接受 参数：
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_字典新建」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__字典有_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_字典有」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__字典键_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_字典键」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__字典值_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_字典值」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__打印文本_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_打印字串」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__读文件_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_读文件」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    做 发射_空值返回转换。
    返还 1。
完。

编译_处理__文件大小_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_文件大小」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__读二进制文件_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_文件大小」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    做 发射 用 0x50。
    做 发射_测试rax。
    可变 空跳 为 做 发射_je 用 0。
    做 发射组 用 一组：0x48、0x8B、0x4C、0x24、0x08。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_读文件」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    做 发射 用 0x50。
    做 发射组 用 一组：0x48、0x8B、0x4C、0x24、0x08。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表新建」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    做 发射 用 0x50。
    做 发射组 用 一组：0x48、0x8B、0x4C、0x24、0x10。
    做 发射组 用 一组：0x48、0x89、0x08。
    做 发射组 用 一组：0x4C、0x8B、0x40、0x10。
    做 发射组 用 一组：0x48、0x8B、0x7C、0x24、0x08。
    做 发射组 用 一组：0x31、0xD2。
    做 发射组 用 一组：0x48、0x39、0xCA。
    做 发射组 用 一组：0x7D、0x0D。
    做 发射组 用 一组：0x0F、0xB6、0x04、0x17。
    做 发射组 用 一组：0x49、0x89、0x04、0xD0。
    做 发射组 用 一组：0x48、0xFF、0xC2。
    做 发射组 用 一组：0xEB、0xEE。
    做 发射组 用 一组：0x48、0x8B、0x04、0x24。
    做 发射组 用 一组：0x48、0x83、0xC4、0x20。
    末跳 为 做 发射_jmp 用 0。
    做 修正跳转 用 空跳、代码位。
    做 发射组 用 一组：0x48、0x83、0xC4、0x10。
    做 发射_载立即 用 1、16。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表新建」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    做 修正跳转 用 末跳、代码位。
    返还 1。
完。

编译_处理__参数_ 接受 参数：
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_取参数」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__替换_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射组 用 一组：0x49、0x89、0xC0。
    做 发射 用 0x5A。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_串替换」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_内建前半 接受 名、参数：
    若 名 等于 「计数」：
        返还 做 编译_处理__计数_ 用 参数。
    完。
    若 名 等于 「追加」：
        返还 做 编译_处理__追加_ 用 参数。
    完。
    若 名 等于 「子串」：
        返还 做 编译_处理__子串_ 用 参数。
    完。
    若 名 等于 「字节取」：
        返还 做 编译_处理__字节取_ 用 参数。
    完。
    若 名 等于 「字节长度」：
        返还 做 编译_处理__字节长度_ 用 参数。
    完。
    若 名 等于 「编码」：
        返还 做 编译_处理__编码_ 用 参数。
    完。
    若 名 等于 「字符」：
        返还 做 编译_处理__字符_ 用 参数。
    完。
    若 名 等于 「转字符串」：
        返还 做 编译_处理__转字符串_ 用 参数。
    完。
    若 名 等于 「插入」：
        返还 做 编译_处理__插入_ 用 参数。
    完。
    若 名 等于 「删除」：
        返还 做 编译_处理__删除_ 用 参数。
    完。
    若 名 等于 「字符串拼接」：
        返还 做 编译_处理__字符串拼接_ 用 参数。
    完。
    若 名 等于 「拼接」：
        返还 做 编译_处理__拼接_ 用 参数。
    完。
    若 名 等于 「整数」：
        返还 做 编译_处理__整数_ 用 参数。
    完。
    若 名 等于 「分割」：
        返还 做 编译_处理__分割_ 用 参数。
    完。
    若 名 等于 「范围」：
        返还 做 编译_处理__范围_ 用 参数。
    完。
    若 名 等于 「字符串等于」：
        返还 做 编译_处理__字符串等于_ 用 参数。
    完。
    若 名 等于 「字典」：
        返还 做 编译_处理__字典_ 用 参数。
    完。
    若 名 等于 「字典设」 或者 名 等于 「字典设置」：
        做 编译_表达式 用 参数 的 第 0 项。
        做 发射 用 0x50。
        做 编译_表达式 用 参数 的 第 1 项。
        做 发射 用 0x50。
        做 编译_表达式 用 参数 的 第 2 项。
        做 发射组 用 一组：0x49、0x89、0xC0。
        做 发射 用 0x5A。
        做 发射 用 0x59。
        做 发射_规范空值_rcx。
        做 发射_规范空值_rdx。
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_字典设」。
        做 发射 用 0xE8。
        做 发射32 用 0。
        返还 1。
    完。
    若 名 等于 「字典取」 或者 名 等于 「字典获取」：
        做 编译_表达式 用 参数 的 第 0 项。
        做 发射 用 0x50。
        做 编译_表达式 用 参数 的 第 1 项。
        做 发射组 用 一组：0x48、0x89、0xC2。
        做 发射 用 0x59。
        做 发射_规范空值_rcx。
        做 发射_规范空值_rdx。
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_字典取」。
        做 发射 用 0xE8。
        做 发射32 用 0。
        返还 1。
    完。
    若 名 等于 「字典有」：
        返还 做 编译_处理__字典有_ 用 参数。
    完。
    若 名 等于 「字典键」：
        返还 做 编译_处理__字典键_ 用 参数。
    完。
    若 名 等于 「字典值」：
        返还 做 编译_处理__字典值_ 用 参数。
    完。
    若 名 等于 「打印文本」：
        返还 做 编译_处理__打印文本_ 用 参数。
    完。
    若 名 等于 「读文件」：
        返还 做 编译_处理__读文件_ 用 参数。
    完。
    若 名 等于 「读二进制文件」：
        返还 做 编译_处理__读二进制文件_ 用 参数。
    完。
    若 名 等于 「文件大小」：
        返还 做 编译_处理__文件大小_ 用 参数。
    完。
    若 名 等于 「参数」：
        返还 做 编译_处理__参数_ 用 参数。
    完。
    若 名 等于 「替换」：
        返还 做 编译_处理__替换_ 用 参数。
    完。
    返还 0。
完。

编译_处理__绝对值_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_绝对值」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__最小值_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_最小」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__最大值_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_最大」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__幂_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_乘方」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__设置_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射组 用 一组：0x49、0x89、0xC0。
    做 发射 用 0x5A。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表设」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__类型_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_类型名」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__是整数_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_是整数」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__是字符串_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_是字串」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__是列表_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_是列表」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__是字典_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_是字典」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__是空_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射组 用 一组：0x48、0x3D、0xB4、0xB1、0xB1、0xB1。
    做 发射组 用 一组：0x0F、0x94、0xC0。
    做 发射组 用 一组：0x0F、0xB6、0xC0。
    返还 1。
完。

编译_处理__打印值_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_打印值」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__包含_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_串包含」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__查找_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_串查找」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__以开头_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_串开头」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__以结尾_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_串结尾」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__内存读_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_内存读64」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__内存写_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射组 用 一组：0x49、0x89、0xC0。
    做 发射 用 0x5A。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_内存写64」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__内存读8_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_内存读8」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

代码生成器_编译_处理__内存写8_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射组 用 一组：0x49、0x89、0xC0。
    做 发射 用 0x5A。
    做 发射 用 0x59。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_内存写8」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__内存读16_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_内存读16」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

代码生成器_编译_处理__内存写16_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射组 用 一组：0x49、0x89、0xC0。
    做 发射 用 0x5A。
    做 发射 用 0x59。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_内存写16」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__内存读32_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_内存读32」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

代码生成器_编译_处理__内存写32_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射组 用 一组：0x49、0x89、0xC0。
    做 发射 用 0x5A。
    做 发射 用 0x59。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_内存写32」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

代码生成器_编译_处理__内存复制_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射组 用 一组：0x49、0x89、0xC0。
    做 发射 用 0x5A。
    做 发射 用 0x59。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_内存复制」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

代码生成器_编译_处理__内存填充_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射组 用 一组：0x49、0x89、0xC0。
    做 发射 用 0x5A。
    做 发射 用 0x59。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_内存填充」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__大写化_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_串大写」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__小写化_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_串小写」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__加载库_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_加载库」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__获取函数_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 发射 用 0x59。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_取函数地址」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_内建后半 接受 名、参数：
    若 名 等于 「绝对值」：
        返还 做 编译_处理__绝对值_ 用 参数。
    完。
    若 名 等于 「最小值」：
        返还 做 编译_处理__最小值_ 用 参数。
    完。
    若 名 等于 「最大值」：
        返还 做 编译_处理__最大值_ 用 参数。
    完。
    若 名 等于 「幂」：
        返还 做 编译_处理__幂_ 用 参数。
    完。
    若 名 等于 「设置」：
        返还 做 编译_处理__设置_ 用 参数。
    完。
    若 名 等于 「类型」：
        返还 做 编译_处理__类型_ 用 参数。
    完。
    若 名 等于 「是整数」：
        返还 做 编译_处理__是整数_ 用 参数。
    完。
    若 名 等于 「是字符串」：
        返还 做 编译_处理__是字符串_ 用 参数。
    完。
    若 名 等于 「是列表」：
        返还 做 编译_处理__是列表_ 用 参数。
    完。
    若 名 等于 「是字典」：
        返还 做 编译_处理__是字典_ 用 参数。
    完。
    若 名 等于 「是空」：
        返还 做 编译_处理__是空_ 用 参数。
    完。
    若 名 等于 「打印值」：
        返还 做 编译_处理__打印值_ 用 参数。
    完。
    若 名 等于 「包含」：
        返还 做 编译_处理__包含_ 用 参数。
    完。
    若 名 等于 「查找」：
        返还 做 编译_处理__查找_ 用 参数。
    完。
    若 名 等于 「以开头」：
        返还 做 编译_处理__以开头_ 用 参数。
    完。
    若 名 等于 「以结尾」：
        返还 做 编译_处理__以结尾_ 用 参数。
    完。
    若 名 等于 「内存读」：
        返还 做 编译_处理__内存读_ 用 参数。
    完。
    若 名 等于 「内存写」：
        返还 做 编译_处理__内存写_ 用 参数。
    完。
    若 名 等于 「内存读8」：
        返还 做 编译_处理__内存读8_ 用 参数。
    完。
    若 名 等于 「内存写8」：
        返还 做 代码生成器_编译_处理__内存写8_ 用 参数。
    完。
    若 名 等于 「内存读16」：
        返还 做 编译_处理__内存读16_ 用 参数。
    完。
    若 名 等于 「内存写16」：
        返还 做 代码生成器_编译_处理__内存写16_ 用 参数。
    完。
    若 名 等于 「内存读32」：
        返还 做 编译_处理__内存读32_ 用 参数。
    完。
    若 名 等于 「内存写32」：
        返还 做 代码生成器_编译_处理__内存写32_ 用 参数。
    完。
    若 名 等于 「内存复制」：
        返还 做 代码生成器_编译_处理__内存复制_ 用 参数。
    完。
    若 名 等于 「内存填充」：
        返还 做 代码生成器_编译_处理__内存填充_ 用 参数。
    完。
    若 名 等于 「大写化」：
        返还 做 编译_处理__大写化_ 用 参数。
    完。
    若 名 等于 「小写化」：
        返还 做 编译_处理__小写化_ 用 参数。
    完。
    若 名 等于 「加载库」：
        返还 做 编译_处理__加载库_ 用 参数。
    完。
    若 名 等于 「获取函数」：
        返还 做 编译_处理__获取函数_ 用 参数。
    完。
    返还 0。
完。

编译_写文件内建 接受 名、参数：
    若 名 不等于 「写文件」 并且 名 不等于 「写二进制」 并且 名 不等于 「平台_写文件」：
        返还 0。
    完。
    路径a 为 参数 的 第 0 项。
    若 路径a 的 第 0 项 等于 标记_字串：
        ps 为 路径a 的 第 1 项。
        可变 pi 为 做 计数 用 字串表。
        做 追加 用 字串表、ps。
        若 名 等于 「写文件」：
            ca 为 参数 的 第 1 项。
            若 ca 的 第 0 项 等于 标记_字串：
                cs 为 ca 的 第 1 项。
                cl 为 做 计数 用 cs。
                ci3 为 做 计数 用 字串表。
                做 追加 用 字串表、cs。
                做 修正表_追加记录 用 修正表、「str」、代码位 加 3、pi。
                做 发射组 用 一组：0x48、0x8D、0x0D。
                做 发射32 用 0。
                做 修正表_追加记录 用 修正表、「str」、代码位 加 3、ci3。
                做 发射组 用 一组：0x48、0x8D、0x15。
                做 发射32 用 0。
                做 发射_载立即 用 8、cl。
            否则：
                做 编译_表达式 用 参数 的 第 1 项。
                做 发射 用 0x50。
                做 修正表_追加记录 用 修正表、「str」、代码位 加 3、pi。
                做 发射组 用 一组：0x48、0x8D、0x0D。
                做 发射32 用 0。
                做 发射 用 0x5A。
                做 发射组 用 一组：0x45、0x31、0xC0。
                做 发射组 用 一组：0x42、0x80、0x3C、0x02、0x00。
                做 发射组 用 一组：0x74、0x05。
                做 发射组 用 一组：0x49、0xFF、0xC0。
                做 发射组 用 一组：0xEB、0xF4。
            完。
        否则：
            做 编译_表达式 用 参数 的 第 2 项。
            做 发射 用 0x50。
            做 编译_表达式 用 参数 的 第 1 项。
            做 发射_mov_rcx_rax。
            做 发射 用 0x41。
            做 发射 用 0x58。
            做 发射组 用 一组：0x4C、0x89、0x01。
            做 发射 用 0x41。
            做 发射 用 0x50。
            做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表转字节」。
            做 发射 用 0xE8。
            做 发射32 用 0。
            做 发射 用 0x50。
            做 修正表_追加记录 用 修正表、「str」、代码位 加 3、pi。
            做 发射组 用 一组：0x48、0x8D、0x0D。
            做 发射32 用 0。
            做 发射 用 0x5A。
            做 发射 用 0x41。
            做 发射 用 0x58。
        完。
    否则：
        若 名 等于 「写文件」：
            做 编译_表达式 用 参数 的 第 1 项。
            做 发射 用 0x50。
            做 编译_表达式 用 路径a。
            做 发射_mov_rcx_rax。
            做 发射 用 0x5A。
            做 发射组 用 一组：0x45、0x31、0xC0。
            做 发射组 用 一组：0x42、0x80、0x3C、0x02、0x00。
            做 发射组 用 一组：0x74、0x05。
            做 发射组 用 一组：0x49、0xFF、0xC0。
            做 发射组 用 一组：0xEB、0xF4。
        否则：
            做 编译_表达式 用 参数 的 第 2 项。
            做 发射 用 0x50。
            做 编译_表达式 用 参数 的 第 1 项。
            做 发射_mov_rcx_rax。
            做 发射 用 0x41。
            做 发射 用 0x58。
            做 发射组 用 一组：0x4C、0x89、0x01。
            做 发射 用 0x41。
            做 发射 用 0x50。
            做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表转字节」。
            做 发射 用 0xE8。
            做 发射32 用 0。
            做 发射 用 0x50。
            做 编译_表达式 用 路径a。
            做 发射_mov_rcx_rax。
            做 发射 用 0x5A。
            做 发射 用 0x41。
            做 发射 用 0x58。
        完。
    完。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_写文件」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理_节_浮点 接受 vm：
    整数部 为 节点 的 第 1 项。
    小数部 为 节点 的 第 2 项。
    小数位 为 节点 的 第 3 项。
    可变 幂 为 1。
    可变 k 为 0。
    只要 k 小于 小数位：
        幂 为 幂 乘 10。
        k 为 k 加 1。
    完。
    组合 为 整数部 乘 幂 加 小数部。
    做 发射_载立即 用 0、组合。
    做 发射组 用 一组：0xF2、0x48、0x0F、0x2A、0xC0。
    做 发射_载立即 用 0、幂。
    做 发射组 用 一组：0xF2、0x48、0x0F、0x2A、0xC8。
    做 发射组 用 一组：0xF2、0x0F、0x5E、0xC1。
    做 发射组 用 一组：0x66、0x48、0x0F、0x7E、0xC0。
    返还 空。
完。

编译_处理_节_字符串 接受 vm：
    s 为 节点 的 第 1 项。
    长度 为 做 字节长度 用 s。
    已有 为 做 字典取 用 _字符串驻留表、s。
    若 已有 不等于 空：
        做 发射组 用 一组：0x48、0x8D、0x05。
        偏移量 为 已有 减 (代码位 加 4)。
        做 发射32 用 偏移量。
        返还 空。
    完。
    若 长度 加 1 小于 126：
        做 发射 用 0xEB。
        做 发射 用 长度 加 1。
    否则：
        做 发射 用 0xE9。
        做 发射32 用 长度 加 1。
    完。
    数据起 为 代码位。
    可变 si 为 0。
    只要 si 小于 长度：
        做 发射 用 做 字节取 用 s、si。
        si 为 si 加 1。
    完。
    做 发射 用 0。
    做 字典设 用 _字符串驻留表、s、数据起。
    做 发射组 用 一组：0x48、0x8D、0x05。
    做 发射32 用 0 减 (长度 加 1 加 3 加 4)。
    返还 空。
完。

_闭包_尝试捕获 接受 标识名：
    若 当前函数列 等于 空：
        返还 0。
    完。
    若 做 字节长度 用 标识名 小于 10：
        返还 0。
    完。
    若 做 字节取 用 标识名、0 不等于 字符_下划线 或者 做 字节取 用 标识名、1 不等于 字符_下划线：
        返还 0。
    完。
    若 做 字典取 用 闭包表、标识名 不等于 空：
        已有列 为 做 字典取 用 闭包表、标识名。
        可变 ci2 为 0。
        只要 ci2 小于 做 计数 用 已有列：
            cn2 为 已有列 的 第 ci2 项。
            co2 为 做 查变量 用 cn2。
            sn2 为 做 字符串拼接 用 「__闭包_」、做 字符串拼接 用 标识名、做 字符串拼接 用 「_」、cn2。
            若 做 字典取 用 全局变量表、sn2 等于 空：
                做 字典设 用 全局变量表、sn2、全局变量数 加 1。
                全局变量数 为 全局变量数 加 1。
            完。
            so2 为 0 减 1000 减 (做 字典取 用 全局变量表、sn2 减 1)。
            若 co2 不等于 空：
                做 发射_载变量 用 co2。
                做 发射_存变量 用 so2。
            完。
            ci2 为 ci2 加 1。
        完。
        返还 0。
    完。
    _找索引 为 做 字典取 用 当前函数名表、标识名。
    若 _找索引 等于 空：
        返还 0。
    完。
    找函 为 当前函数列 的 第 _找索引 减 1 项。
    若 做 是列表 用 找函 等于 0 或者 做 计数 用 找函 小于 4：
        返还 0。
    完。
    参列 为 找函 的 第 2 项。
    体节 为 找函 的 第 3 项。
    可变 标列 为 一组：（空）。
    做 _收集自由标识 用 体节、标列。
    可变 捕列 为 一组：（空）。
    可变 si 为 0。
    只要 si 小于 做 计数 用 标列：
        idn 为 标列 的 第 si 项。
        ido 为 做 查变量 用 idn。
        若 ido 不等于 空：
            可变 是参 为 假。
            可变 pi 为 0。
            只要 pi 小于 做 计数 用 参列：
                若 参列 的 第 pi 项 等于 idn：
                    是参 为 真。
                完。
                pi 为 pi 加 1。
            完。
            若 是参 等于 假：
                可变 已有 为 假。
                可变 vi 为 0。
                只要 vi 小于 做 计数 用 捕列：
                    若 捕列 的 第 vi 项 等于 idn：
                        已有 为 真。
                    完。
                    vi 为 vi 加 1。
                完。
                若 已有 等于 假：
                    做 追加 用 捕列、idn。
                完。
            完。
        完。
        si 为 si 加 1。
    完。
    若 做 计数 用 捕列 大于 0：
        做 字典设 用 闭包表、标识名、捕列。
        可变 ci 为 0。
        只要 ci 小于 做 计数 用 捕列：
            cn 为 捕列 的 第 ci 项。
            co 为 做 查变量 用 cn。
            sn 为 做 字符串拼接 用 「__闭包_」、做 字符串拼接 用 标识名、做 字符串拼接 用 「_」、cn。
            若 做 字典取 用 全局变量表、sn 等于 空：
                做 字典设 用 全局变量表、sn、全局变量数 加 1。
                全局变量数 为 全局变量数 加 1。
            完。
            so 为 0 减 1000 减 (做 字典取 用 全局变量表、sn 减 1)。
            若 co 不等于 空：
                做 发射_载变量 用 co。
                做 发射_存变量 用 so。
            完。
            ci 为 ci 加 1。
        完。
    完。
    返还 0。
完。

编译_处理_节_标识符 接受 vm：
    若 节点 等于 空 或者 做 是列表 用 节点 等于 假 或者 做 计数 用 节点 小于 2：
        返还 空。
    完。
    可变 偏移 为 做 查变量 用 节点 的 第 1 项。
    若 偏移 不等于 空：
        做 发射_载变量 用 偏移。
    否则：
        函数地址 为 做 字典取 用 函数表、节点 的 第 1 项。
        若 函数地址 不等于 空：
            做 _闭包_尝试捕获 用 节点 的 第 1 项。
            做 修正表_追加记录 用 修正表、「call」、代码位 加 3、节点 的 第 1 项。
            做 发射组 用 一组：0x48、0x8D、0x05。
            做 发射32 用 0。
        否则：
            全局变量数 为 全局变量数 加 1。
            做 字典设 用 全局变量表、节点 的 第 1 项、全局变量数。
            _udef偏 为 0 减 1000 减 (全局变量数 减 1)。
            做 字典设 用 变量表、节点 的 第 1 项、_udef偏。
            做 发射_载变量 用 _udef偏。
        完。
    完。
    返还 空。
完。

编译_处理_节_列表 接受 vm：
    元素 为 节点 的 第 1 项。
    n 为 做 计数 用 元素。
    可变 容量 为 n 加 8。
    若 n 等于 0：
        容量 为 16。
    完。
    做 发射_载立即 用 1、容量。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表新建」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    若 n 大于 0：
        做 发射 用 0x50。
        可变 li 为 0。
        只要 li 小于 n：
            做 编译_表达式 用 元素 的 第 li 项。
            做 发射组 用 一组：0x48、0x89、0xC2。
            做 发射组 用 一组：0x48、0x8B、0x0C、0x24。
            做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表追加」。
            做 发射 用 0xE8。
            做 发射32 用 0。
            li 为 li 加 1。
        完。
        做 发射 用 0x58。
    完。
    返还 空。
完。

编译_处理_节_字典 接受 vm：
    对列 为 节点 的 第 1 项。
    对数 为 做 计数 用 对列。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_字典新建」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    若 对数 大于 0：
        做 发射 用 0x50。
        可变 di 为 0。
        只要 di 小于 对数：
            对 为 对列 的 第 di 项。
            做 编译_表达式 用 对 的 第 0 项。
            做 发射 用 0x50。
            做 编译_表达式 用 对 的 第 1 项。
            做 发射组 用 一组：0x49、0x89、0xC0。
            做 发射 用 0x5A。
            做 发射组 用 一组：0x48、0x8B、0x0C、0x24。
            做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_字典设」。
            做 发射 用 0xE8。
            做 发射32 用 0。
            di 为 di 加 1。
        完。
        做 发射 用 0x58。
    完。
    返还 空。
完。

编译_发射_索引读取分流 接受 零参：
    做 发射组 用 一组：0x48、0x8B、0x4C、0x24、0x08。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_是字典」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    做 发射_测试rax。
    可变 走列表 为 做 发射_je 用 0。
    做 发射 用 0x5A。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_字典取」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    可变 结束跳 为 做 发射_jmp 用 0。
    做 修正跳转 用 走列表、代码位。
    做 发射 用 0x5A。
    做 发射 用 0x59。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表取」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    做 修正跳转 用 结束跳、代码位。
    返还 空。
完。

编译_发射_索引写入分流 接受 零参：
    做 发射组 用 一组：0x48、0x8B、0x0C、0x24。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_是字典」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    做 发射_测试rax。
    可变 走列表2 为 做 发射_je 用 0。
    做 发射 用 0x59。
    做 发射 用 0x5A。
    做 发射组 用 一组：0x41、0x58。
    做 发射_规范空值_rcx。
    做 发射_规范空值_rdx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_字典设」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    可变 结束跳2 为 做 发射_jmp 用 0。
    做 修正跳转 用 走列表2、代码位。
    做 发射 用 0x59。
    做 发射 用 0x5A。
    做 发射组 用 一组：0x41、0x58。
    做 发射_规范空值_rcx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表设」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    做 修正跳转 用 结束跳2、代码位。
    返还 空。
完。

编译_处理_节_索引 接受 vm：
    做 编译_表达式 用 节点 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 节点 的 第 2 项。
    做 发射 用 0x50。
    做 编译_发射_索引读取分流。
    返还 空。
完。

编译_处理_节_新建 接受 vm：
    可变 类型名 为 节点 的 第 1 项。
    字段值 为 节点 的 第 2 项。
    字段数 为 做 计数 用 字段值。
    _字段列 为 做 字典取 用 结构体表、类型名。
    可变 _字段总数 为 字段数。
    若 _字段列 不等于 0 并且 _字段列 不等于 空 并且 做 计数 用 _字段列 大于 0：
        _字段总数 为 做 计数 用 _字段列。
    完。
    可变 容量 为 _字段总数 加 8。
    若 _字段总数 等于 0：
        容量 为 16。
    完。
    做 发射_载立即 用 1、容量。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表新建」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    做 发射 用 0x50。
    可变 _命名式 为 假。
    若 字段数 大于 0 并且 做 计数 用 字段值 的 第 0 项 大于 0 并且 做 编译_字符串安全相等 用 字段值 的 第 0 项 的 第 0 项、「」 等于 假：
        _命名式 为 真。
    完。
    若 _命名式 并且 _字段列 不等于 0 并且 _字段列 不等于 空 并且 做 计数 用 _字段列 大于 0：
        可变 _sfi 为 0。
        只要 _sfi 小于 _字段总数：
            _字段名 为 _字段列 的 第 _sfi 项。
            可变 _命中idx 为 0 减 1。
            可变 _ffi 为 0。
            只要 _ffi 小于 字段数：
                _fv查 为 字段值 的 第 _ffi 项。
                若 做 编译_字符串安全相等 用 _fv查 的 第 0 项、_字段名：
                    _命中idx 为 _ffi。
                完。
                _ffi 为 _ffi 加 1。
            完。
            若 _命中idx 大于或等于 0：
                做 编译_表达式 用 字段值 的 第 _命中idx 项 的 第 1 项。
            否则：
                做 发射_载立即 用 0、0。
            完。
            做 发射组 用 一组：0x48、0x89、0xC2。
            做 发射组 用 一组：0x48、0x8B、0x0C、0x24。
            做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表追加」。
            做 发射 用 0xE8。
            做 发射32 用 0。
            _sfi 为 _sfi 加 1。
        完。
    否则：
        可变 fi 为 0。
        只要 fi 小于 字段数：
            fv 为 字段值 的 第 fi 项。
            做 编译_表达式 用 fv 的 第 1 项。
            做 发射组 用 一组：0x48、0x89、0xC2。
            做 发射组 用 一组：0x48、0x8B、0x0C、0x24。
            做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表追加」。
            做 发射 用 0xE8。
            做 发射32 用 0。
            fi 为 fi 加 1。
        完。
    完。
    做 发射 用 0x58。
    返还 空。
完。

编译_处理_节_逻辑非 接受 vm：
    做 编译_表达式 用 节点 的 第 1 项。
    做 发射_规范空值。
    做 发射_测试rax。
    做 发射组 用 一组：0x0F、0x94、0xC0。
    做 发射组 用 一组：0x0F、0xB6、0xC0。
    返还 空。
完。

_收集自由标识 接受 n、结果：
    若 n 等于 空：
        返还 0。
    完。
    若 做 是列表 用 n 等于 0：
        返还 0。
    完。
    长度 为 做 计数 用 n。
    若 长度 小于 1：
        返还 0。
    完。
    若 长度 大于或等于 2：
        头 为 n 的 第 0 项。
        若 头 等于 标记_名称：
            做 追加 用 结果、n 的 第 1 项。
            返还 0。
        完。
    完。
    可变 j 为 0。
    只要 j 小于 长度：
        做 _收集自由标识 用 n 的 第 j 项、结果。
        j 为 j 加 1。
    完。
    返还 0。
完。

闭包分析_函数列 接受 函数列：
    闭包表 为 做 字典。
    当前函数列 为 函数列。
    当前函数名表 为 做 字典。
    可变 _mi 为 0。
    只要 _mi 小于 做 计数 用 函数列：
        若 做 是列表 用 函数列 的 第 _mi 项 并且 做 计数 用 函数列 的 第 _mi 项 大于或等于 2：
            做 字典设 用 当前函数名表、函数列 的 第 _mi 项 的 第 1 项、_mi 加 1。
        完。
        _mi 为 _mi 加 1。
    完。
完。

编译_处理_节_匿名函数 接受 vm：
    匿名名2 为 节点 的 第 1 项。
    若 当前函数列 不等于 空 并且 做 字典取 用 闭包表、匿名名2 等于 空：
        可变 找到函 为 空。
        可变 fi 为 0。
        只要 fi 小于 做 计数 用 当前函数列：
            f 为 当前函数列 的 第 fi 项。
            若 做 编译_是列表值 用 f 并且 做 计数 用 f 大于或等于 4 并且 f 的 第 1 项 等于 匿名名2：
                找到函 为 f。
            完。
            fi 为 fi 加 1。
        完。
        若 找到函 不等于 空：
            _cl参数 为 找到函 的 第 2 项。
            _cl体 为 找到函 的 第 3 项。
            可变 _cl标识 为 一组：（空）。
            做 _收集自由标识 用 _cl体、_cl标识。
            可变 捕获列 为 一组：（空）。
            可变 si 为 0。
            只要 si 小于 做 计数 用 _cl标识：
                _clid 为 _cl标识 的 第 si 项。
                _cl偏 为 做 查变量 用 _clid。
                若 _cl偏 不等于 空：
                    可变 是参数 为 假。
                    可变 pi 为 0。
                    只要 pi 小于 做 计数 用 _cl参数：
                        若 _cl参数 的 第 pi 项 等于 _clid：
                            是参数 为 真。
                        完。
                        pi 为 pi 加 1。
                    完。
                    若 是参数 等于 假：
                        可变 已有 为 假。
                        可变 vi 为 0。
                        只要 vi 小于 做 计数 用 捕获列：
                            若 捕获列 的 第 vi 项 等于 _clid：
                                已有 为 真。
                            完。
                            vi 为 vi 加 1。
                        完。
                        若 已有 等于 假：
                            做 追加 用 捕获列、_clid。
                        完。
                    完。
                完。
                si 为 si 加 1。
            完。
            若 做 计数 用 捕获列 大于 0：
                做 字典设 用 闭包表、匿名名2、捕获列。
            完。
        完。
    完。
    闭包变量列 为 做 字典取 用 闭包表、匿名名2。
    若 闭包变量列 不等于 空：
        可变 ci 为 0。
        只要 ci 小于 做 计数 用 闭包变量列：
            捕获名 为 闭包变量列 的 第 ci 项。
            捕获偏 为 做 查变量 用 捕获名。
            槽名 为 做 字符串拼接 用 「__闭包_」、做 字符串拼接 用 匿名名2、做 字符串拼接 用 「_」、捕获名。
            若 做 字典取 用 全局变量表、槽名 等于 空：
                做 字典设 用 全局变量表、槽名、全局变量数 加 1。
                全局变量数 为 全局变量数 加 1。
            完。
            槽偏 为 0 减 1000 减 (做 字典取 用 全局变量表、槽名 减 1)。
            若 捕获偏 不等于 空：
                做 发射_载变量 用 捕获偏。
                做 发射_存变量 用 槽偏。
            完。
            ci 为 ci 加 1。
        完。
    完。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 3、匿名名2。
    做 发射组 用 一组：0x48、0x8D、0x05。
    做 发射32 用 0。
    返还 空。
完。

编译_处理_节_点访问 接受 vm：
    若 节点 等于 空 或者 做 是列表 用 节点 等于 假 或者 做 计数 用 节点 小于 3：
        返还 空。
    完。
    做 编译_表达式 用 节点 的 第 1 项。
    可变 字段名 为 节点 的 第 2 项。
    可变 偏移idx 为 0。
    可变 已找到 为 假。
    可变 _推断类型 为 做 推断结构类型名 用 节点 的 第 1 项。
    若 _推断类型 不等于 0 并且 _推断类型 不等于 空：
        偏移idx 为 做 查字段偏移 用 _推断类型、字段名。
        若 偏移idx 大于或等于 0：
            已找到 为 真。
        完。
    完。
    若 已找到 等于 假：
        _全局idx 为 做 _查字段偏移_全局 用 字段名。
        若 _全局idx 大于或等于 0：
            偏移idx 为 _全局idx。
            已找到 为 真。
        完。
    完。
    若 偏移idx 小于 0：
        若 模块图详细模式 的 第 0 项：
            做 打印文本 用 做 字符串拼接 用 「[DOT?] 」、做 字符串拼接 用 字段名、做 字符串拼接 用 「 idx=」、做 转字符串 用 偏移idx。
        完。
        偏移idx 为 0。
    完。
    做 发射_mov_rcx_rax。
    做 发射组 用 一组：0x48、0xC7、0xC2。
    做 发射32 用 偏移idx。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表取」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 空。
完。

编译_处理_节_调用 接受 vm：
    若 节点 等于 空 或者 做 是列表 用 节点 等于 假 或者 做 计数 用 节点 小于 3：
        返还 空。
    完。
    可变 名 为 节点 的 第 1 项。
    参数 为 节点 的 第 2 项。
    若 参数 等于 空 或者 做 是列表 用 参数 等于 假：
        参数 为 一组：（空）。
    完。
    若 名 等于 空 或者 做 是字符串 用 名 等于 假：
        返还 空。
    完。
    若 做 编译_系统内建 用 名、参数：
        返还 空。
    完。
    若 做 编译_内联汇编内建 用 名、参数：
        返还 空。
    完。
    若 做 编译_裸机内建 用 名、参数：
        返还 空。
    完。
    若 做 编译_内建前半 用 名、参数：
        返还 空。
    完。
    若 做 编译_内建后半 用 名、参数：
        返还 空。
    完。
    若 做 编译_写文件内建 用 名、参数：
        返还 空。
    完。
    若 做 编译_SIMD内建 用 名、参数：
        返还 空。
    完。
    可变 参数数 为 做 计数 用 参数。
    可变 目标偏移 为 做 查变量 用 名。
    若 目标偏移 不等于 空 并且 目标偏移 大于 0 减 1000：
        做 发射_载变量 用 目标偏移。
        做 发射 用 0x50。
        _保存尾调用v 为 _尾调用模式。
        _尾调用模式 为 0。
        可变 i 为 参数数 减 1。
        只要 i 大于或等于 0：
            做 编译_表达式 用 参数 的 第 i 项。
            做 发射 用 0x50。
            i 为 i 减 1。
        完。
        _尾调用模式 为 _保存尾调用v。
        若 参数数 大于或等于 1：
            做 发射 用 0x59。
        完。
        若 参数数 大于或等于 2：
            做 发射 用 0x5A。
        完。
        若 参数数 大于或等于 3：
            做 发射 用 0x41。
            做 发射 用 0x58。
        完。
        若 参数数 大于或等于 4：
            做 发射 用 0x41。
            做 发射 用 0x59。
        完。
        若 参数数 小于或等于 4：
            做 发射 用 0x58。
        否则：
            可变 函数偏移 为 (参数数 减 4) 乘 8。
            做 发射组 用 一组：0x48、0x8B、0x44、0x24。
            做 发射 用 函数偏移。
        完。
        若 _尾调用模式 等于 1 并且 参数数 小于或等于 4：
            做 发射 用 0xC9。
            做 发射组 用 一组：0xFF、0xE0。
            _尾调用模式 为 2。
        否则：
            做 发射组 用 一组：0xFF、0xD0。
            若 参数数 大于 4：
                可变 清理 为 (参数数 减 3) 乘 8。
                若 清理 小于或等于 127：
                    做 发射组 用 一组：0x48、0x83、0xC4。
                    做 发射 用 清理。
                否则：
                    做 发射组 用 一组：0x48、0x81、0xC4。
                    做 发射32 用 清理。
                完。
            完。
        完。
        返还 空。
    完。
    _保存尾调用d 为 _尾调用模式。
    _尾调用模式 为 0。
    可变 j 为 参数数 减 1。
    只要 j 大于或等于 0：
        做 编译_表达式 用 参数 的 第 j 项。
        做 发射 用 0x50。
        j 为 j 减 1。
    完。
    _尾调用模式 为 _保存尾调用d。
    若 参数数 大于或等于 1：
        做 发射 用 0x59。
    完。
    若 参数数 大于或等于 2：
        做 发射 用 0x5A。
    完。
    若 参数数 大于或等于 3：
        做 发射 用 0x41。
        做 发射 用 0x58。
    完。
    若 参数数 大于或等于 4：
        做 发射 用 0x41。
        做 发射 用 0x59。
    完。
    若 _尾调用模式 等于 1 并且 参数数 小于或等于 4：
        做 发射 用 0xC9。
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、名。
        做 发射 用 0xE9。
        做 发射32 用 0。
        _尾调用模式 为 2。
    否则：
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、名。
        做 发射 用 0xE8。
        做 发射32 用 0。
        若 参数数 大于 4：
            可变 清理2 为 (参数数 减 4) 乘 8。
            若 清理2 小于或等于 127：
                做 发射组 用 一组：0x48、0x83、0xC4。
                做 发射 用 清理2。
            否则：
                做 发射组 用 一组：0x48、0x81、0xC4。
                做 发射32 用 清理2。
            完。
        完。
    完。
    返还 空。
完。

编译_短路与 接受 参数：
    若 节点 等于 空 或者 做 是列表 用 节点 等于 假 或者 做 计数 用 节点 小于 4：
        返还 空。
    完。
    做 编译_表达式 用 节点 的 第 2 项。
    做 发射_规范空值。
    做 发射_测试rax。
    可变 假跳 为 做 发射_je 用 0。
    做 编译_表达式 用 节点 的 第 3 项。
    可变 末跳 为 做 发射_jmp 用 0。
    做 修正跳转 用 假跳、代码位。
    做 发射_载立即 用 0、0。
    做 修正跳转 用 末跳、代码位。
    返还 空。
完。

编译_短路或 接受 vm：
    若 节点 等于 空 或者 做 是列表 用 节点 等于 假 或者 做 计数 用 节点 小于 4：
        返还 空。
    完。
    做 编译_表达式 用 节点 的 第 2 项。
    做 发射_规范空值。
    做 发射_测试rax。
    真跳 为 做 发射_jne 用 0。
    做 编译_表达式 用 节点 的 第 3 项。
    做 修正跳转 用 真跳、代码位。
    返还 空。
完。

编译_处理_节_二元 接受 vm：
    若 节点 等于 空 或者 做 是列表 用 节点 等于 假 或者 做 计数 用 节点 小于 4：
        返还 空。
    完。
    _b_op 为 节点 的 第 1 项。
    可变 _b_左 为 节点 的 第 2 项。
    可变 _b_右 为 节点 的 第 3 项。
    op 为 做 编译_规范化二元操作 用 _b_op。
    若 op 等于 空：
        返还 空。
    完。
    若 _b_左 的 第 0 项 等于 标记_数字 并且 _b_右 的 第 0 项 等于 标记_数字：
        折a 为 节点 的 第 2 项 的 第 1 项。
        折b 为 节点 的 第 3 项 的 第 1 项。
        可变 折结果 为 0。
        可变 已折叠 为 真。
        若 op 等于 1：
            折结果 为 折a 加 折b。
        否则若 op 等于 2：
            折结果 为 折a 减 折b。
        否则若 op 等于 3：
            折结果 为 折a 乘 折b。
        否则若 op 等于 4 并且 折b 不等于 0：
            折结果 为 折a 除以 折b。
        否则若 op 等于 5 并且 折b 不等于 0：
            折结果 为 折a 取余于 折b。
        否则若 op 等于 12：
            折结果 为 折a 位与 折b。
        否则若 op 等于 13：
            折结果 为 折a 位或 折b。
        否则若 op 等于 14：
            折结果 为 折a 位异或 折b。
        否则若 op 等于 15：
            折结果 为 折a 左移 折b。
        否则若 op 等于 16：
            折结果 为 折a 右移 折b。
        否则若 op 等于 6：
            若 折a 等于 折b：
                折结果 为 1。
            否则：
                折结果 为 0。
            完。
        否则若 op 等于 7：
            若 折a 不等于 折b：
                折结果 为 1。
            否则：
                折结果 为 0。
            完。
        否则若 op 等于 8：
            若 折a 小于 折b：
                折结果 为 1。
            否则：
                折结果 为 0。
            完。
        否则若 op 等于 9：
            若 折a 大于 折b：
                折结果 为 1。
            否则：
                折结果 为 0。
            完。
        否则若 op 等于 10：
            若 折a 小于或等于 折b：
                折结果 为 1。
            否则：
                折结果 为 0。
            完。
        否则若 op 等于 11：
            若 折a 大于或等于 折b：
                折结果 为 1。
            否则：
                折结果 为 0。
            完。
        否则：
            已折叠 为 假。
        完。
        若 已折叠：
            做 发射_载立即 用 0、折结果。
            返还 空。
        完。
    完。
    若 _b_右 的 第 0 项 等于 标记_数字 并且 _b_右 的 第 1 项 等于 0：
        若 op 等于 1 或者 op 等于 13 或者 op 等于 14 或者 op 等于 15 或者 op 等于 16 或者 op 等于 2：
            做 编译_表达式 用 节点 的 第 2 项。
            返还 空。
        完。
        若 op 等于 3 或者 op 等于 12：
            做 发射_载立即 用 0、0。
            返还 空。
        完。
    完。
    若 _b_左 的 第 0 项 等于 标记_数字 并且 _b_左 的 第 1 项 等于 0：
        若 op 等于 1 或者 op 等于 13 或者 op 等于 14：
            做 编译_表达式 用 节点 的 第 3 项。
            返还 空。
        完。
        若 op 等于 3 或者 op 等于 12：
            做 发射_载立即 用 0、0。
            返还 空。
        完。
    完。
    若 _b_右 的 第 0 项 等于 标记_数字 并且 _b_右 的 第 1 项 等于 1：
        若 op 等于 3 或者 op 等于 4：
            做 编译_表达式 用 节点 的 第 2 项。
            返还 空。
        完。
    完。
    若 _b_左 的 第 0 项 等于 标记_数字 并且 _b_左 的 第 1 项 等于 1：
        若 op 等于 3：
            做 编译_表达式 用 节点 的 第 3 项。
            返还 空。
        完。
    完。
    若 _b_左 的 第 0 项 等于 标记_数字 并且 _b_右 的 第 0 项 不等于 100：
        若 op 等于 1 或者 op 等于 3 或者 op 等于 12 或者 op 等于 13 或者 op 等于 14：
            _tmp交换 为 _b_左。
            _b_左 为 _b_右。
            _b_右 为 _tmp交换。
        完。
    完。
    若 _b_右 的 第 0 项 等于 标记_数字 并且 op 等于 3 并且 做 是2的幂 用 _b_右 的 第 1 项 等于 1 并且 _b_右 的 第 1 项 大于 1：
        做 编译_表达式 用 _b_左。
        做 发射组 用 一组：0x48、0xC1、0xE0。
        做 发射 用 做 对数2 用 _b_右 的 第 1 项。
        返还 空。
    完。
    若 _b_右 的 第 0 项 等于 标记_数字 并且 op 等于 3：
        若 _b_右 的 第 1 项 等于 3：
            做 编译_表达式 用 _b_左。
            做 发射组 用 一组：0x48、0x8D、0x04、0x40。
            返还 空。
        完。
        若 _b_右 的 第 1 项 等于 5：
            做 编译_表达式 用 _b_左。
            做 发射组 用 一组：0x48、0x8D、0x04、0x80。
            返还 空。
        完。
        若 _b_右 的 第 1 项 等于 9：
            做 编译_表达式 用 _b_左。
            做 发射组 用 一组：0x48、0x8D、0x04、0xC0。
            返还 空。
        完。
    完。
    若 _b_右 的 第 0 项 等于 标记_数字 并且 op 等于 4 并且 做 是2的幂 用 _b_右 的 第 1 项 等于 1 并且 _b_右 的 第 1 项 大于 1：
        做 编译_表达式 用 _b_左。
        做 发射组 用 一组：0x48、0xC1、0xF8。
        做 发射 用 做 对数2 用 _b_右 的 第 1 项。
        返还 空。
    完。
    若 _b_右 的 第 0 项 等于 标记_数字 并且 op 等于 5 并且 做 是2的幂 用 _b_右 的 第 1 项 等于 1 并且 _b_右 的 第 1 项 大于 1：
        掩码 为 _b_右 的 第 1 项 减 1。
        做 编译_表达式 用 _b_左。
        若 掩码 小于或等于 127：
            做 发射组 用 一组：0x48、0x83、0xE0。
            做 发射 用 掩码。
        否则：
            做 发射组 用 一组：0x48、0x25。
            做 发射32 用 掩码。
        完。
        返还 空。
    完。
    若 _b_右 的 第 0 项 等于 标记_数字 并且 _b_右 的 第 1 项 等于 0：
        若 op 等于 6：
            做 编译_表达式 用 _b_左。
            做 发射组 用 一组：0x48、0x85、0xC0。
            做 发射组 用 一组：0x0F、0x94、0xC0。
            做 发射组 用 一组：0x0F、0xB6、0xC0。
            返还 空。
        完。
        若 op 等于 7：
            做 编译_表达式 用 _b_左。
            做 发射组 用 一组：0x48、0x85、0xC0。
            做 发射组 用 一组：0x0F、0x95、0xC0。
            做 发射组 用 一组：0x0F、0xB6、0xC0。
            返还 空。
        完。
    完。
    若 _b_左 的 第 0 项 等于 标记_数字 并且 _b_左 的 第 1 项 等于 0 并且 op 等于 2：
        做 编译_表达式 用 _b_右。
        做 发射组 用 一组：0x48、0xF7、0xD8。
        返还 空。
    完。
    若 _b_右 的 第 0 项 等于 标记_数字：
        _imm 为 _b_右 的 第 1 项。
        若 op 等于 1 或者 op 等于 2：
            做 编译_表达式 用 _b_左。
            若 _imm 等于 1 并且 op 等于 1：
                做 发射组 用 一组：0x48、0xFF、0xC0。
            否则若 _imm 等于 1 并且 op 等于 2：
                做 发射组 用 一组：0x48、0xFF、0xC8。
            否则若 _imm 大于或等于 0 减 128 并且 _imm 小于或等于 127：
                若 op 等于 1：
                    做 发射组 用 一组：0x48、0x83、0xC0。
                否则：
                    做 发射组 用 一组：0x48、0x83、0xE8。
                完。
                做 发射 用 _imm 位与 0xFF。
            否则：
                若 op 等于 1：
                    做 发射组 用 一组：0x48、0x05。
                否则：
                    做 发射组 用 一组：0x48、0x2D。
                完。
                做 发射32 用 _imm。
            完。
            返还 空。
        完。
        若 op 等于 8 或者 op 等于 9 或者 op 等于 10 或者 op 等于 11：
            做 编译_表达式 用 _b_左。
            若 _imm 大于或等于 0 减 128 并且 _imm 小于或等于 127：
                做 发射组 用 一组：0x48、0x83、0xF8。
                做 发射 用 _imm 位与 0xFF。
            否则：
                做 发射组 用 一组：0x48、0x3D。
                做 发射32 用 _imm。
            完。
            若 op 等于 8：
                做 发射组 用 一组：0x0F、0x9C、0xC0。
            否则若 op 等于 9：
                做 发射组 用 一组：0x0F、0x9F、0xC0。
            否则若 op 等于 10：
                做 发射组 用 一组：0x0F、0x9E、0xC0。
            否则：
                做 发射组 用 一组：0x0F、0x9D、0xC0。
            完。
            做 发射组 用 一组：0x0F、0xB6、0xC0。
            返还 空。
        完。
        若 op 等于 15 或者 op 等于 16：
            做 编译_表达式 用 _b_左。
            若 op 等于 15：
                做 发射组 用 一组：0x48、0xC1、0xE0。
            否则：
                做 发射组 用 一组：0x48、0xC1、0xF8。
            完。
            做 发射 用 _imm 位与 0x3F。
            返还 空。
        完。
    完。
    若 op 等于 17：
        做 编译_短路与 用 参数。
        返还 空。
    完。
    若 op 等于 18：
        做 编译_短路或 用 vm。
        返还 空。
    完。
    若 做 编译_发射_相等比较 用 op、_b_左、_b_右 等于 1：
        返还 空。
    完。
    若 做 是浮点节点 用 _b_左 或者 做 是浮点节点 用 _b_右：
        若 op 等于 1 或者 op 等于 2 或者 op 等于 3 或者 op 等于 4 或者 op 等于 8 或者 op 等于 9 或者 op 等于 10 或者 op 等于 11 或者 op 等于 6 或者 op 等于 7：
            若 做 是简单 用 节点 的 第 2 项：
                做 编译_表达式 用 节点 的 第 3 项。
                做 发射_mov_rcx_rax。
                做 编译_表达式 用 节点 的 第 2 项。
            否则：
                做 编译_表达式 用 节点 的 第 2 项。
                做 发射 用 0x50。
                做 编译_表达式 用 节点 的 第 3 项。
                做 发射_mov_rcx_rax。
                做 发射 用 0x58。
            完。
            若 做 是浮点节点 用 节点 的 第 2 项：
                做 发射组 用 一组：0x66、0x48、0x0F、0x6E、0xC0。
            否则：
                做 发射组 用 一组：0xF2、0x48、0x0F、0x2A、0xC0。
            完。
            若 做 是浮点节点 用 节点 的 第 3 项：
                做 发射组 用 一组：0x66、0x48、0x0F、0x6E、0xC9。
            否则：
                做 发射组 用 一组：0xF2、0x48、0x0F、0x2A、0xC9。
            完。
            若 op 等于 1：
                做 发射组 用 一组：0xF2、0x0F、0x58、0xC1。
            否则若 op 等于 2：
                做 发射组 用 一组：0xF2、0x0F、0x5C、0xC1。
            否则若 op 等于 3：
                做 发射组 用 一组：0xF2、0x0F、0x59、0xC1。
            否则若 op 等于 4：
                做 发射组 用 一组：0xF2、0x0F、0x5E、0xC1。
            否则若 op 等于 8 或者 op 等于 9 或者 op 等于 10 或者 op 等于 11 或者 op 等于 6 或者 op 等于 7：
                做 发射组 用 一组：0x66、0x0F、0x2E、0xC1。
                若 op 等于 8：
                    做 发射组 用 一组：0x0F、0x92、0xC0。
                    做 发射组 用 一组：0x0F、0x9B、0xC1。
                    做 发射组 用 一组：0x20、0xC8。
                否则若 op 等于 9：
                    做 发射组 用 一组：0x0F、0x97、0xC0。
                否则若 op 等于 10：
                    做 发射组 用 一组：0x0F、0x96、0xC0。
                    做 发射组 用 一组：0x0F、0x9B、0xC1。
                    做 发射组 用 一组：0x20、0xC8。
                否则若 op 等于 11：
                    做 发射组 用 一组：0x0F、0x93、0xC0。
                否则若 op 等于 6：
                    做 发射组 用 一组：0x0F、0x94、0xC0。
                    做 发射组 用 一组：0x0F、0x9B、0xC1。
                    做 发射组 用 一组：0x20、0xC8。
                否则：
                    做 发射组 用 一组：0x0F、0x95、0xC0。
                    做 发射组 用 一组：0x0F、0x9A、0xC1。
                    做 发射组 用 一组：0x08、0xC8。
                完。
                做 发射组 用 一组：0x0F、0xB6、0xC0。
                返还 空。
            完。
            做 发射组 用 一组：0x66、0x48、0x0F、0x7E、0xC0。
            返还 空。
        完。
    完。
    若 op 等于 1 并且 (做 是字符串节点 用 _b_左 或者 做 是字符串节点 用 _b_右)：
        若 做 是简单 用 节点 的 第 2 项：
            做 编译_表达式 用 节点 的 第 3 项。
            做 发射_mov_rcx_rax。
            做 编译_表达式 用 节点 的 第 2 项。
        否则：
            做 编译_表达式 用 节点 的 第 2 项。
            做 发射 用 0x50。
            做 编译_表达式 用 节点 的 第 3 项。
            做 发射_mov_rcx_rax。
            做 发射 用 0x58。
        完。
        做 发射组 用 一组：0x48、0x89、0xCA。
        做 发射组 用 一组：0x48、0x89、0xC1。
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_串拼接」。
        做 发射 用 0xE8。
        做 发射32 用 0。
        返还 空。
    完。
    若 做 是简单 用 _b_左：
        做 编译_表达式 用 _b_右。
        做 发射_mov_rcx_rax。
        做 编译_表达式 用 _b_左。
    否则：
        做 编译_表达式 用 _b_左。
        做 发射 用 0x50。
        做 编译_表达式 用 _b_右。
        做 发射_mov_rcx_rax。
        做 发射 用 0x58。
    完。
    做 发射_二元操作 用 op。
    返还 空。
完。

编译_处理_节_间接调用 接受 vm：
    若 节点 等于 空 或者 做 是列表 用 节点 等于 假 或者 做 计数 用 节点 小于 3：
        返还 空。
    完。
    间接基 为 节点 的 第 1 项。
    间接参 为 节点 的 第 2 项。
    做 编译_表达式 用 间接基。
    做 发射 用 0x50。
    可变 保存尾调用v 为 _尾调用模式。
    _尾调用模式 为 0。
    可变 i 为 做 计数 用 间接参 减 1。
    只要 i 大于或等于 0：
        做 编译_表达式 用 间接参 的 第 i 项。
        做 发射 用 0x50。
        i 为 i 减 1。
    完。
    间接参数 为 做 计数 用 间接参。
    若 间接参数 大于或等于 1：
        做 发射 用 0x59。
    完。
    若 间接参数 大于或等于 2：
        做 发射 用 0x5A。
    完。
    若 间接参数 大于或等于 3：
        做 发射 用 0x41。
        做 发射 用 0x58。
    完。
    若 间接参数 大于或等于 4：
        做 发射 用 0x41。
        做 发射 用 0x59。
    完。
    若 间接参数 小于或等于 4：
        做 发射 用 0x58。
    否则：
        做 发射组 用 一组：0x48、0x8B、0x44、0x24。
        做 发射 用 (间接参数 减 4) 乘 8。
    完。
    若 保存尾调用v 等于 1 并且 间接参数 小于或等于 4：
        做 发射 用 0xC9。
        做 发射组 用 一组：0xFF、0xE0。
        _尾调用模式 为 2。
    否则：
        做 发射组 用 一组：0xFF、0xD0。
        若 间接参数 大于 4：
            清理 为 (间接参数 减 3) 乘 8。
            做 发射组 用 一组：0x48、0x83、0xC4。
            做 发射 用 清理。
        完。
    完。
    返还 空。
完。

编译_表达式 接受 _表达式入：
    _保存节点 为 节点。
    节点 为 _表达式入。
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 1：
        节点 为 _保存节点。
        返还 空。
    完。
    可变 类型 为 节点 的 第 0 项。
    若 类型 等于 标记_数字：
        做 发射_载立即 用 0、节点 的 第 1 项。
        节点 为 _保存节点。
        返还 空。
    否则若 类型 等于 节点_空字面量：
        做 发射_载立即 用 0、0 减 1313754700。
        节点 为 _保存节点。
        返还 空。
    否则若 类型 等于 节_浮点：
        做 编译_处理_节_浮点 用 vm。
    否则若 类型 等于 标记_字串：
        做 编译_处理_节_字符串 用 vm。
    否则若 类型 等于 标记_名称：
        做 编译_处理_节_标识符 用 vm。
    否则若 类型 等于 节点_赋值：
        做 编译_处理_节_列表 用 vm。
    否则若 类型 等于 节_字典：
        做 编译_处理_节_字典 用 vm。
    否则若 类型 等于 节点_索引：
        做 编译_处理_节_索引 用 vm。
    否则若 类型 等于 标记_抛出：
        做 编译_处理_节_新建 用 vm。
    否则若 类型 等于 节点_一元正：
        做 编译_处理_节_逻辑非 用 vm。
    否则若 类型 等于 节点_一元负：
        做 编译_表达式 用 节点 的 第 1 项。
        做 发射组 用 一组：0x48、0xF7、0xD0。
        节点 为 _保存节点。
        返还 空。
    否则若 类型 等于 标记_汇编：
        做 编译_处理_节_匿名函数 用 vm。
    否则若 类型 等于 标记_连接词：
        做 编译_处理_节_点访问 用 vm。
    否则若 类型 等于 节点_调用：
        做 编译_处理_节_调用 用 vm。
    否则若 类型 等于 节点_二元：
        做 编译_处理_节_二元 用 vm。
    否则若 类型 等于 标记_函数：
        做 编译_处理_节_如果 用 vm。
    否则若 类型 等于 标记_关键140：
        做 编译_处理_节_间接调用 用 vm。
    完。
    节点 为 _保存节点。
    返还 空。
完。

编译_语句列表 接受 语句：
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句：
        _s 为 语句 的 第 i 项。
        做 编译_语句 用 _s。
        若 做 是列表 用 _s 并且 做 计数 用 _s 大于或等于 1 并且 _s 的 第 0 项 等于 标记_如果：
            返还 空。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

编译_处理_节_定义 接受 vm：
    _def名 为 节点 的 第 1 项。
    _def值 为 节点 的 第 2 项。
    可变 偏移 为 做 分配变量 用 _def名。
    可变 _推断结构类型 为 做 推断结构类型名 用 _def值。
    若 _推断结构类型 不等于 0 并且 _推断结构类型 不等于 空：
        做 字典设 用 变量类型表、_def名、_推断结构类型。
    完。
    若 做 是字符串节点 用 _def值：
        做 字典设 用 变量类型表、_def名、「__转文本__」。
    完。
    若 做 是浮点节点 用 _def值：
        做 字典设 用 变量类型表、_def名、「__转浮点__」。
    完。
    若 做 是浮点列表字面量 用 _def值：
        做 字典设 用 变量类型表、_def名、「__浮点列表__」。
    完。
    做 编译_表达式 用 _def值。
    做 发射_存变量 用 偏移。
    返还 空。
完。

编译_处理_节_赋值 接受 vm：
    目标名 为 节点 的 第 1 项。
    可变 偏移 为 做 查变量 用 目标名。
    若 偏移 等于 空：
        g偏移 为 做 字典取 用 全局变量表、目标名。
        若 g偏移 不等于 空：
            偏移 为 0 减 1000 减 (g偏移 减 1)。
        否则：
            若 模块图详细模式 的 第 0 项：
                做 打印文本 用 做 字符串拼接 用 「警告: 赋值到未定义变量 」、目标名。
                _候选列表 为 做 字典键 用 全局变量表。
                _相似 为 做 诊断_查找相似符号 用 目标名、_候选列表、3。
                可变 _xi 为 0。
                只要 _xi 小于 做 计数 用 _相似：
                    做 打印文本 用 做 字符串拼接 用 「    您是否想用: 」、做 字符串拼接 用 _相似 的 第 _xi 项、「?」。
                    _xi 为 _xi 加 1。
                完。
            完。
        完。
    完。
    做 编译_表达式 用 节点 的 第 2 项。
    偏移 为 做 查变量 用 目标名。
    若 偏移 等于 空：
        g偏移2 为 做 字典取 用 全局变量表、目标名。
        若 g偏移2 不等于 空：
            偏移 为 0 减 1000 减 (g偏移2 减 1)。
        完。
    完。
    若 偏移 不等于 空：
        做 发射_存变量 用 偏移。
    完。
    返还 空。
完。

编译_处理_节_点赋值 接受 vm：
    做 编译_表达式 用 节点 的 第 3 项。
    做 发射 用 0x50。
    做 编译_表达式 用 节点 的 第 1 项。
    字段名 为 节点 的 第 2 项。
    可变 偏移idx 为 0。
    可变 已找到2 为 假。
    可变 _推断类型2 为 做 推断结构类型名 用 节点 的 第 1 项。
    若 _推断类型2 不等于 0 并且 _推断类型2 不等于 空：
        偏移idx 为 做 查字段偏移 用 _推断类型2、字段名。
        若 偏移idx 大于或等于 0：
            已找到2 为 真。
        完。
    完。
    若 已找到2 等于 假：
        _全局idx2 为 做 _查字段偏移_全局 用 字段名。
        若 _全局idx2 大于或等于 0：
            偏移idx 为 _全局idx2。
        完。
    完。
    若 偏移idx 小于 0：
        偏移idx 为 0。
    完。
    做 发射_mov_rcx_rax。
    做 发射_载立即 用 2、偏移idx。
    做 发射组 用 一组：0x41、0x58。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表设」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 空。
完。

编译_处理_节_索引赋值 接受 vm：
    可变 偏移 为 做 查变量 用 节点 的 第 1 项。
    若 偏移 不等于 空：
        做 编译_表达式 用 节点 的 第 3 项。
        做 发射 用 0x50。
        做 编译_表达式 用 节点 的 第 2 项。
        做 发射 用 0x50。
        做 发射_载变量 用 偏移。
        做 发射 用 0x50。
        做 编译_发射_索引写入分流。
    完。
    返还 空。
完。

编译_处理_节_链式索引赋值 接受 vm：
    做 编译_表达式 用 节点 的 第 3 项。
    做 发射 用 0x50。
    做 编译_表达式 用 节点 的 第 2 项。
    做 发射 用 0x50。
    做 编译_表达式 用 节点 的 第 1 项。
    做 发射 用 0x50。
    做 编译_发射_索引写入分流。
    返还 空。
完。

编译_处理_节_如果 接受 vm：
    _if条件 为 节点 的 第 1 项。
    _if真体 为 节点 的 第 2 项。
    _if假体 为 节点 的 第 3 项。
    可变 _if头 为 负 1。
    可变 _ifop型 为 「」。
    可变 _ifopi 为 负 999。
    可变 _if左 为 负 1。
    可变 _if右 为 负 1。
    若 做 是列表 用 _if条件 并且 做 计数 用 _if条件 大于 0：
        _if头 为 _if条件 的 第 0 项。
    完。
    若 _if头 等于 节点_二元 并且 做 计数 用 _if条件 大于或等于 4：
        _ifop型 为 做 类型 用 _if条件 的 第 1 项。
        若 _ifop型 等于 「整数」：
            _ifopi 为 _if条件 的 第 1 项。
        完。
        若 做 是列表 用 _if条件 的 第 2 项 并且 做 计数 用 _if条件 的 第 2 项 大于 0：
            _if左 为 _if条件 的 第 2 项 的 第 0 项。
        完。
        若 做 是列表 用 _if条件 的 第 3 项 并且 做 计数 用 _if条件 的 第 3 项 大于 0：
            _if右 为 _if条件 的 第 3 项 的 第 0 项。
        完。
    完。
    若 做 是列表 用 _if条件 并且 _if条件 的 第 0 项 等于 标记_数字：
        若 _if条件 的 第 1 项 不等于 0：
            做 编译_语句列表 用 _if真体。
        否则：
            做 编译_语句列表 用 _if假体。
        完。
        返还 空。
    完。
    可变 假跳 为 做 编译_条件融合跳转 用 _if条件。
    若 假跳 等于 空：
        做 编译_表达式 用 _if条件。
        若 做 表达式_已是布尔 用 _if条件 等于 假：
            做 发射_规范空值。
        完。
        做 发射_测试rax。
        假跳 为 做 发射_je 用 0。
    完。
    做 编译_语句列表 用 _if真体。
    若 做 计数 用 _if假体 大于 0：
        末跳 为 做 发射_jmp 用 0。
        做 修正跳转 用 假跳、代码位。
        做 编译_语句列表 用 _if假体。
        做 修正跳转 用 末跳、代码位。
    否则：
        做 修正跳转 用 假跳、代码位。
    完。
    返还 空。
完。

_LICM_是计数调用 接受 节点：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 3：
        返还 假。
    完。
    若 节点 的 第 0 项 不等于 节点_调用：
        返还 假。
    完。
    fn名节点 为 节点 的 第 1 项。
    可变 fn名 为 空。
    若 做 是字符串 用 fn名节点：
        fn名 为 fn名节点。
    否则若 做 编译_是列表值 用 fn名节点 并且 做 计数 用 fn名节点 大于或等于 2 并且 fn名节点 的 第 0 项 等于 标记_名称：
        fn名 为 fn名节点 的 第 1 项。
    完。
    若 fn名 等于 空：
        返还 假。
    完。
    可变 是支持 为 假。
    若 做 编译_字符串安全相等 用 fn名、「计数」：
        是支持 为 真。
    否则若 做 编译_字符串安全相等 用 fn名、「字节长度」：
        是支持 为 真。
    否则若 做 编译_字符串安全相等 用 fn名、「长度」：
        是支持 为 真。
    否则若 做 编译_字符串安全相等 用 fn名、「字符串长度」：
        是支持 为 真。
    否则若 做 编译_字符串安全相等 用 fn名、「缓冲长度」：
        是支持 为 真。
    完。
    若 是支持 等于 假：
        返还 假。
    完。
    参数 为 节点 的 第 2 项。
    若 做 编译_是列表值 用 参数 等于 假 或者 做 计数 用 参数 不等于 1：
        返还 假。
    完。
    arg 为 参数 的 第 0 项。
    若 做 编译_是列表值 用 arg 并且 做 计数 用 arg 大于或等于 2 并且 arg 的 第 0 项 等于 标记_名称：
        返还 真。
    完。
    返还 假。
完。

_LICM_取数组名 接受 计数节点：
    参 为 计数节点 的 第 2 项。
    arg 为 参 的 第 0 项。
    返还 arg 的 第 1 项。
完。

_LICM_节点修改变量 接受 节点、变量名：
    若 节点 等于 空 或者 做 编译_是列表值 用 节点 等于 假 或者 做 计数 用 节点 小于 1：
        返还 假。
    完。
    头 为 节点 的 第 0 项。
    若 头 等于 节点_赋值语句 或者 头 等于 节点_复合赋值 或者 头 等于 节点_赋值：
        若 做 计数 用 节点 大于或等于 2：
            目标 为 节点 的 第 1 项。
            若 做 编译_是列表值 用 目标 并且 做 计数 用 目标 大于或等于 2：
                若 目标 的 第 0 项 等于 标记_名称 并且 做 编译_字符串安全相等 用 目标 的 第 1 项、变量名：
                    返还 真。
                完。
                若 目标 的 第 0 项 等于 节点_索引 并且 做 计数 用 目标 大于或等于 2：
                    被索引 为 目标 的 第 1 项。
                    若 做 编译_是列表值 用 被索引 并且 做 计数 用 被索引 大于或等于 2 并且 被索引 的 第 0 项 等于 标记_名称 并且 做 编译_字符串安全相等 用 被索引 的 第 1 项、变量名：
                        返还 真。
                    完。
                完。
            完。
        完。
    完。
    若 头 等于 节点_变量声明 并且 做 计数 用 节点 大于或等于 2：
        若 做 是字符串 用 节点 的 第 1 项 并且 做 编译_字符串安全相等 用 节点 的 第 1 项、变量名：
            返还 真。
        完。
    完。
    若 头 等于 节点_调用 并且 做 计数 用 节点 大于或等于 3：
        fn节 为 节点 的 第 1 项。
        可变 fn名 为 空。
        若 做 是字符串 用 fn节：
            fn名 为 fn节。
        否则若 做 编译_是列表值 用 fn节 并且 做 计数 用 fn节 大于或等于 2 并且 fn节 的 第 0 项 等于 标记_名称：
            fn名 为 fn节 的 第 1 项。
        完。
        可变 变异 为 假。
        若 fn名 不等于 空：
            若 做 编译_字符串安全相等 用 fn名、「追加」：
                变异 为 真。
            否则若 做 编译_字符串安全相等 用 fn名、「删除」：
                变异 为 真。
            否则若 做 编译_字符串安全相等 用 fn名、「插入」：
                变异 为 真。
            否则若 做 编译_字符串安全相等 用 fn名、「字典设」：
                变异 为 真。
            否则若 做 编译_字符串安全相等 用 fn名、「清空」：
                变异 为 真。
            否则若 做 编译_字符串安全相等 用 fn名、「排序」：
                变异 为 真。
            否则若 做 编译_字符串安全相等 用 fn名、「反转」：
                变异 为 真。
            完。
        完。
        若 变异：
            参 为 节点 的 第 2 项。
            若 做 编译_是列表值 用 参 并且 做 计数 用 参 大于或等于 1：
                首参 为 参 的 第 0 项。
                若 做 编译_是列表值 用 首参 并且 做 计数 用 首参 大于或等于 2 并且 首参 的 第 0 项 等于 标记_名称 并且 做 编译_字符串安全相等 用 首参 的 第 1 项、变量名：
                    返还 真。
                完。
            完。
        完。
        若 fn名 不等于 空 并且 变异 等于 假：
            参 为 节点 的 第 2 项。
            若 做 编译_是列表值 用 参 并且 做 计数 用 参 大于或等于 1：
                首参 为 参 的 第 0 项。
                若 做 编译_是列表值 用 首参 并且 做 计数 用 首参 大于或等于 2 并且 首参 的 第 0 项 等于 标记_名称 并且 做 编译_字符串安全相等 用 首参 的 第 1 项、变量名：
                    可变 纯读 为 假。
                    若 做 编译_字符串安全相等 用 fn名、「计数」：
                        纯读 为 真。
                    否则若 做 编译_字符串安全相等 用 fn名、「字典取」：
                        纯读 为 真。
                    否则若 做 编译_字符串安全相等 用 fn名、「字典有」：
                        纯读 为 真。
                    否则若 做 编译_字符串安全相等 用 fn名、「字典键」：
                        纯读 为 真。
                    否则若 做 编译_字符串安全相等 用 fn名、「字典值」：
                        纯读 为 真。
                    否则若 做 编译_字符串安全相等 用 fn名、「是列表」：
                        纯读 为 真。
                    否则若 做 编译_字符串安全相等 用 fn名、「是字符串」：
                        纯读 为 真。
                    否则若 做 编译_字符串安全相等 用 fn名、「类型」：
                        纯读 为 真。
                    否则若 做 编译_字符串安全相等 用 fn名、「转字符串」：
                        纯读 为 真。
                    否则若 做 编译_字符串安全相等 用 fn名、「打印」：
                        纯读 为 真。
                    否则若 做 编译_字符串安全相等 用 fn名、「打印整数」：
                        纯读 为 真。
                    否则若 做 编译_字符串安全相等 用 fn名、「编译_是列表值」：
                        纯读 为 真。
                    否则若 做 编译_字符串安全相等 用 fn名、「编译_字符串安全相等」：
                        纯读 为 真。
                    完。
                    若 纯读 等于 假：
                        返还 真。
                    完。
                完。
            完。
        完。
    完。
    返还 假。
完。

_LICM_体修改变量 接受 体列表、变量名：
    若 做 编译_是列表值 用 体列表 等于 假：
        返还 假。
    完。
    可变 i 为 0。
    n 为 做 计数 用 体列表。
    只要 i 小于 n：
        语句 为 体列表 的 第 i 项。
        若 做 _LICM_节点修改变量 用 语句、变量名：
            返还 真。
        完。
        若 做 编译_是列表值 用 语句 并且 做 计数 用 语句 大于或等于 2：
            可变 j 为 1。
            只要 j 小于 做 计数 用 语句：
                子 为 语句 的 第 j 项。
                若 做 编译_是列表值 用 子：
                    若 做 _LICM_节点修改变量 用 子、变量名：
                        返还 真。
                    完。
                    若 做 _LICM_体修改变量 用 子、变量名：
                        返还 真。
                    完。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

可变 _LICM_提升次数 为 0。

编译_处理_节_当 接受 vm：
    可变 _w条件 为 节点 的 第 1 项。
    _w体 为 节点 的 第 2 项。
    若 做 是列表 用 _w条件 并且 _w条件 的 第 0 项 等于 标记_数字 并且 _w条件 的 第 1 项 等于 0：
        返还 空。
    完。
    若 做 编译_是列表值 用 _w条件 并且 做 计数 用 _w条件 大于或等于 4 并且 _w条件 的 第 0 项 等于 节点_二元：
        _LICM_op 为 做 编译_规范化二元操作 用 _w条件 的 第 1 项。
        若 _LICM_op 不等于 空 并且 _LICM_op 大于或等于 6 并且 _LICM_op 小于或等于 11：
            _LICM_右 为 _w条件 的 第 3 项。
            若 做 _LICM_是计数调用 用 _LICM_右：
                _LICM_数组名 为 做 _LICM_取数组名 用 _LICM_右。
                若 做 _LICM_体修改变量 用 _w体、_LICM_数组名 等于 假：
                    _LICM_临时名 为 做 字符串拼接 用 「__LICM_n_」、做 转字符串 用 代码位。
                    _LICM_偏移 为 做 分配变量 用 _LICM_临时名。
                    做 编译_表达式 用 _LICM_右。
                    做 发射_存变量 用 _LICM_偏移。
                    _LICM_新右 为 一组：标记_名称、_LICM_临时名。
                    _w条件 为 一组：节点_二元、_w条件 的 第 1 项、_w条件 的 第 2 项、_LICM_新右。
                    _LICM_提升次数 为 _LICM_提升次数 加 1。
                完。
            完。
        完。
    完。
    可变 _w常真 为 做 是列表 用 _w条件 并且 _w条件 的 第 0 项 等于 标记_数字 并且 _w条件 的 第 1 项 不等于 0。
    循环头 为 代码位。
    可变 当前中断列 为 一组：（空）。
    做 追加 用 循环头栈、循环头。
    做 追加 用 中断跳列栈、当前中断列。
    做 追加 用 继续目标栈、循环头。
    循环深度 为 循环深度 加 1。
    可变 退出跳 为 0。
    若 _w常真 等于 假：
        _w融合 为 做 编译_条件融合跳转 用 _w条件。
        若 _w融合 不等于 空：
            退出跳 为 _w融合。
        否则：
            做 编译_表达式 用 _w条件。
            若 做 表达式_已是布尔 用 _w条件 等于 假：
                做 发射_规范空值。
            完。
            做 发射_测试rax。
            退出跳 为 做 发射_je 用 0。
        完。
    完。
    做 编译_语句列表 用 _w体。
    做 发射_调用相对 用 循环头。
    做 _elf写 用 代码、代码位 减 5、0xE9。
    若 _w常真 等于 假：
        做 修正跳转 用 退出跳、代码位。
    完。
    可变 bki 为 0。
    当前中断列 为 中断跳列栈 的 第 做 计数 用 中断跳列栈 减 1 项。
    只要 bki 小于 做 计数 用 当前中断列：
        做 修正跳转 用 当前中断列 的 第 bki 项、代码位。
        bki 为 bki 加 1。
    完。
    循环深度 为 循环深度 减 1。
    做 删除 用 循环头栈、做 计数 用 循环头栈 减 1。
    做 删除 用 中断跳列栈、做 计数 用 中断跳列栈 减 1。
    做 删除 用 继续目标栈、做 计数 用 继续目标栈 减 1。
    返还 空。
完。

编译_处理_节_匹配 接受 vm：
    匹配表达 为 节点 的 第 1 项。
    分支列 为 节点 的 第 2 项。
    匹配偏 为 做 分配变量 用 做 字符串拼接 用 「__匹配_」、做 转字符串 用 代码位。
    做 编译_表达式 用 匹配表达。
    做 发射_存变量 用 匹配偏。
    可变 末跳列 为 一组：（空）。
    可变 bi 为 0。
    只要 bi 小于 做 计数 用 分支列：
        分支 为 分支列 的 第 bi 项。
        模式 为 分支 的 第 0 项。
        分支体 为 分支 的 第 1 项。
        若 模式 等于 空：
            做 编译_语句列表 用 分支体。
        否则：
            若 做 是字符串节点 用 模式：
                做 编译_表达式 用 模式。
                做 发射_mov_rcx_rax。
                做 发射_规范空值_rcx。
                做 发射_载变量 用 匹配偏。
                做 发射组 用 一组：0x48、0x89、0xC2。
                做 发射_规范空值_rdx。
                做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_串相等」。
                做 发射 用 0xE8。
                做 发射32 用 0。
            否则：
                做 编译_表达式 用 模式。
                做 发射_mov_rcx_rax。
                做 发射_载变量 用 匹配偏。
                做 发射组 用 一组：0x48、0x39、0xC8。
                做 发射组 用 一组：0x0F、0x94、0xC0。
                做 发射组 用 一组：0x0F、0xB6、0xC0。
            完。
            做 发射_测试rax。
            跳过 为 做 发射_je 用 0。
            做 编译_语句列表 用 分支体。
            做 追加 用 末跳列、做 发射_jmp 用 0。
            做 修正跳转 用 跳过、代码位。
        完。
        bi 为 bi 加 1。
    完。
    可变 ji 为 0。
    只要 ji 小于 做 计数 用 末跳列：
        做 修正跳转 用 末跳列 的 第 ji 项、代码位。
        ji 为 ji 加 1。
    完。
    返还 空。
完。

编译_处理_节_每个 接受 vm：
    迭代名 为 节点 的 第 1 项。
    集合表达 为 节点 的 第 2 项。
    循环体 为 节点 的 第 3 项。
    计数器偏 为 做 分配变量 用 做 字符串拼接 用 「__函数调用_」、迭代名。
    集合偏 为 做 分配变量 用 做 字符串拼接 用 「__函数列_」、迭代名。
    迭代偏 为 做 分配变量 用 迭代名。
    做 发射_载立即 用 0、负 1。
    做 发射_存变量 用 计数器偏。
    做 编译_表达式 用 集合表达。
    做 发射_存变量 用 集合偏。
    可变 每个中断列0 为 一组：（空）。
    做 追加 用 循环头栈、代码位。
    做 追加 用 中断跳列栈、每个中断列0。
    循环深度 为 循环深度 加 1。
    增量点 为 代码位。
    做 追加 用 继续目标栈、增量点。
    做 发射_载变量 用 计数器偏。
    做 发射组 用 一组：0x48、0xFF、0xC0。
    做 发射_存变量 用 计数器偏。
    做 发射_载变量 用 集合偏。
    做 发射组 用 一组：0x48、0x8B、0x08。
    做 发射_载变量 用 计数器偏。
    做 发射组 用 一组：0x48、0x39、0xC8。
    做 发射组 用 一组：0x0F、0x9C、0xC0。
    做 发射组 用 一组：0x0F、0xB6、0xC0。
    做 发射_测试rax。
    每个退出 为 做 发射_je 用 0。
    做 发射_载变量 用 集合偏。
    做 发射_mov_rcx_rax。
    做 发射_载变量 用 计数器偏。
    做 发射组 用 一组：0x48、0x89、0xC2。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、「__运行时_列表取」。
    做 发射 用 0xE8。
    做 发射32 用 0。
    做 发射_存变量 用 迭代偏。
    做 编译_语句列表 用 循环体。
    做 发射_调用相对 用 增量点。
    做 _elf写 用 代码、代码位 减 5、0xE9。
    做 修正跳转 用 每个退出、代码位。
    若 做 计数 用 中断跳列栈 大于 0：
        可变 bki2 为 0。
        每个中断列 为 中断跳列栈 的 第 做 计数 用 中断跳列栈 减 1 项。
        只要 bki2 小于 做 计数 用 每个中断列：
            做 修正跳转 用 每个中断列 的 第 bki2 项、代码位。
            bki2 为 bki2 加 1。
        完。
        循环深度 为 循环深度 减 1。
        做 删除 用 循环头栈、做 计数 用 循环头栈 减 1。
        做 删除 用 中断跳列栈、做 计数 用 中断跳列栈 减 1。
        做 删除 用 继续目标栈、做 计数 用 继续目标栈 减 1。
    完。
    返还 空。
完。

编译_处理_节_中断 接受 vm：
    若 循环深度 大于 0：
        中断列ref 为 中断跳列栈 的 第 做 计数 用 中断跳列栈 减 1 项。
        做 追加 用 中断列ref、做 发射_jmp 用 0。
    完。
    返还 空。
完。

编译_处理_节_继续 接受 vm：
    若 循环深度 大于 0：
        继续目标 为 继续目标栈 的 第 做 计数 用 继续目标栈 减 1 项。
        做 发射_调用相对 用 继续目标。
        做 _elf写 用 代码、代码位 减 5、0xE9。
    完。
    返还 空。
完。

编译_处理_节_尝试 接受 vm：
    尝试体 为 节点 的 第 1 项。
    捕获名 为 节点 的 第 2 项。
    捕获体 为 节点 的 第 3 项。
    捕获偏 为 做 分配变量 用 捕获名。
    标志偏 为 做 查变量 用 「__错误_标志」。
    值偏 为 做 查变量 用 「__错误_值」。
    rbp偏 为 做 查变量 用 「__错误_基指针」。
    rsp偏 为 做 查变量 用 「__错误_栈指针」。
    jmp偏 为 做 查变量 用 「__错误_跳转」。
    uid 为 变量数。
    旧rbp偏 为 做 分配变量 用 做 字符串拼接 用 「__尝试_基指针_」、做 转字符串 用 uid。
    旧rsp偏 为 做 分配变量 用 做 字符串拼接 用 「__尝试_栈指针_」、做 转字符串 用 uid。
    旧jmp偏 为 做 分配变量 用 做 字符串拼接 用 「__尝试_跳转_」、做 转字符串 用 uid。
    做 发射_载变量 用 rbp偏。
    做 发射_存变量 用 旧rbp偏。
    做 发射_载变量 用 rsp偏。
    做 发射_存变量 用 旧rsp偏。
    做 发射_载变量 用 jmp偏。
    做 发射_存变量 用 旧jmp偏。
    做 发射组 用 一组：0x48、0x89、0xE8。
    做 发射_存变量 用 rbp偏。
    做 发射组 用 一组：0x48、0x89、0xE0。
    做 发射_存变量 用 rsp偏。
    做 发射组 用 一组：0x48、0x8D、0x05。
    catch修正 为 代码位。
    做 发射32 用 0。
    做 发射_存变量 用 jmp偏。
    做 发射_载立即 用 0、0。
    做 发射_存变量 用 标志偏。
    做 编译_语句列表 用 尝试体。
    正常跳 为 做 发射_jmp 用 0。
    做 修正跳转 用 catch修正、代码位。
    做 发射_载变量 用 值偏。
    做 发射_存变量 用 捕获偏。
    做 发射_载立即 用 0、0。
    做 发射_存变量 用 标志偏。
    做 编译_语句列表 用 捕获体。
    做 修正跳转 用 正常跳、代码位。
    做 发射_载变量 用 旧rbp偏。
    做 发射_存变量 用 rbp偏。
    做 发射_载变量 用 旧rsp偏。
    做 发射_存变量 用 rsp偏。
    做 发射_载变量 用 旧jmp偏。
    做 发射_存变量 用 jmp偏。
    返还 空。
完。

编译_处理_节_汇编块 接受 vm：
    做 编译_汇编文本块 用 节点 的 第 1 项。
    返还 空。
完。

编译_处理_节_抛出 接受 vm：
    标志偏2 为 做 查变量 用 「__错误_标志」。
    值偏2 为 做 查变量 用 「__错误_值」。
    rbp偏2 为 做 查变量 用 「__错误_基指针」。
    rsp偏2 为 做 查变量 用 「__错误_栈指针」。
    jmp偏2 为 做 查变量 用 「__错误_跳转」。
    做 编译_表达式 用 节点 的 第 1 项。
    做 发射_存变量 用 值偏2。
    做 发射_载立即 用 0、1。
    做 发射_存变量 用 标志偏2。
    做 发射_载变量 用 jmp偏2。
    做 发射_mov_rcx_rax。
    做 发射_载变量 用 rbp偏2。
    做 发射组 用 一组：0x48、0x89、0xC5。
    做 发射_载变量 用 rsp偏2。
    做 发射组 用 一组：0x48、0x89、0xC4。
    做 发射组 用 一组：0xFF、0xE1。
    返还 空。
完。

编译_语句 接受 _节点入：
    _保存节点2 为 节点。
    节点 为 _节点入。
    做 _记录行号 用 做 _取节点行号 用 _节点入。
    类型 为 节点 的 第 0 项。
    若 类型 等于 标记_定义：
        做 编译_处理_节_定义 用 vm。
    否则若 类型 等于 标记_可变：
        做 编译_处理_节_赋值 用 vm。
    否则若 类型 等于 标记_顺序词：
        做 编译_处理_节_点赋值 用 vm。
    否则若 类型 等于 标记_当：
        做 编译_处理_节_索引赋值 用 vm。
    否则若 类型 等于 标记_关键141：
        做 编译_处理_节_链式索引赋值 用 vm。
    否则若 类型 等于 标记_如果：
        _ret值 为 节点 的 第 1 项。
        若 做 是列表 用 _ret值 并且 做 计数 用 _ret值 大于或等于 3 并且 _ret值 的 第 0 项 等于 节点_调用：
            _tc参数 为 _ret值 的 第 2 项。
            若 _tc参数 等于 空 或者 做 是列表 用 _tc参数 等于 假：
                _tc参数 为 一组：（空）。
            完。
            若 做 计数 用 _tc参数 小于或等于 4：
                _尾调用模式 为 1。
                做 编译_表达式 用 _ret值。
                若 _尾调用模式 不等于 2：
                    做 发射_尾声。
                完。
                _尾调用模式 为 0。
                节点 为 _保存节点2。
                返还 空。
            完。
        完。
        做 编译_表达式 用 _ret值。
        做 发射_尾声。
        节点 为 _保存节点2。
        返还 空。
    否则若 类型 等于 标记_函数：
        做 编译_处理_节_如果 用 vm。
    否则若 类型 等于 标记_返回：
        做 编译_处理_节_当 用 vm。
    否则若 类型 等于 标记_关键135：
        做 编译_处理_节_匹配 用 vm。
    否则若 类型 等于 标记_关键134：
        做 编译_处理_节_每个 用 vm。
    否则若 类型 等于 标记_关键136：
        做 编译_处理_节_中断 用 vm。
    否则若 类型 等于 标记_关键137：
        做 编译_处理_节_继续 用 vm。
    否则若 类型 等于 节点_变量声明：
        做 编译_处理_节_尝试 用 vm。
    否则若 类型 等于 节点_赋值语句：
        做 编译_处理_节_抛出 用 vm。
    否则若 类型 等于 节点_复合赋值：
        做 编译_处理_节_汇编块 用 vm。
    否则若 类型 等于 标记_否则：
        做 编译_表达式 用 节点 的 第 1 项。
        节点 为 _保存节点2。
        返还 空。
    完。
    节点 为 _保存节点2。
    返还 空。
完。

编译_函数 接受 节点、传入闭包：
    名 为 节点 的 第 1 项。
    参数 为 节点 的 第 2 项。
    体 为 节点 的 第 3 项。
    做 _记录行号 用 做 _取节点行号 用 节点。
    _保存主体模式 为 主体模式。
    主体模式 为 0。
    当前函数名 为 名。
    做 字典设 用 函数表、名、代码位。
    变量表 为 做 字典。
    变量数 为 0。
    _ai标签 为 做 IR标签_从函数名推断 用 名。
    _ai路径 为 做 AI后端_选择路径 用 _ai标签、0、做 编译_当前是裸机目标。
    做 AI后端_记录发射 用 _ai路径。
    做 发射_序言。
    可变 _标记集 为 做 字典。
    可变 _pi_scan 为 0。
    只要 _pi_scan 小于 做 计数 用 参数：
        做 字典设 用 _标记集、参数 的 第 _pi_scan 项、1。
        _tkey 为 做 字符串拼接 用 名、做 字符串拼接 用 「@」、做 转字符串 用 _pi_scan。
        _预载类型 为 做 字典取 用 函数参数类型表、_tkey。
        若 _预载类型 不等于 空 并且 _预载类型 不等于 0：
            _现有类型 为 做 字典取 用 变量类型表、参数 的 第 _pi_scan 项。
            若 _现有类型 不等于 「__浮点列表__」：
                做 字典设 用 变量类型表、参数 的 第 _pi_scan 项、_预载类型。
            完。
        完。
        _pi_scan 为 _pi_scan 加 1。
    完。
    可变 _lv_i 为 0。
    只要 _lv_i 小于 做 计数 用 体：
        做 _收集局部变量名 用 体 的 第 _lv_i 项、_标记集。
        _lv_i 为 _lv_i 加 1。
    完。
    可变 _不动 为 假。
    可变 _iter 为 0。
    只要 _不动 等于 假 并且 _iter 小于 6：
        _不动 为 真。
        可变 _pre_bi 为 0。
        只要 _pre_bi 小于 做 计数 用 体：
            若 做 _浮点局部变量_预标记 用 体 的 第 _pre_bi 项：
                _不动 为 假。
            完。
            _pre_bi 为 _pre_bi 加 1。
        完。
        可变 _bi 为 0。
        只要 _bi 小于 做 计数 用 体：
            若 做 _浮点参数推断_扫描 用 体 的 第 _bi 项、_标记集：
                _不动 为 假。
            完。
            _bi 为 _bi 加 1。
        完。
        _iter 为 _iter 加 1。
    完。
    可变 pi 为 0。
    只要 pi 小于 做 计数 用 参数：
        变量数 为 变量数 加 1。
        偏移 为 0 减 变量数 乘 8。
        做 字典设 用 变量表、参数 的 第 pi 项、偏移。
        若 pi 等于 0：
            做 发射_存rcx到变量 用 偏移。
        否则若 pi 等于 1：
            做 发射_存rdx到变量 用 偏移。
        否则若 pi 等于 2：
            若 偏移 大于或等于 0 减 128：
                做 发射组 用 一组：0x4C、0x89、0x45。
                做 发射 用 偏移 位与 0xFF。
            否则：
                做 发射组 用 一组：0x4C、0x89、0x85。
                做 发射32 用 偏移。
            完。
        否则若 pi 等于 3：
            若 偏移 大于或等于 0 减 128：
                做 发射组 用 一组：0x4C、0x89、0x4D。
                做 发射 用 偏移 位与 0xFF。
            否则：
                做 发射组 用 一组：0x4C、0x89、0x8D。
                做 发射32 用 偏移。
            完。
        否则：
            栈偏移 为 16 加 (pi 减 4) 乘 8。
            做 发射组 用 一组：0x48、0x8B、0x85。
            做 发射32 用 栈偏移。
            做 发射_存变量 用 偏移。
        完。
        pi 为 pi 加 1。
    完。
    若 传入闭包 不等于 空：
        可变 cli 为 0。
        只要 cli 小于 做 计数 用 传入闭包：
            闭包变名 为 传入闭包 的 第 cli 项。
            若 做 字典取 用 变量表、闭包变名 等于 空：
                闭包局偏 为 做 分配变量 用 闭包变名。
                闭包槽名 为 做 字符串拼接 用 「__闭包_」、做 字符串拼接 用 名、做 字符串拼接 用 「_」、闭包变名。
                闭包全偏 为 做 字典取 用 全局变量表、闭包槽名。
                若 闭包全偏 不等于 空：
                    闭包读偏 为 0 减 1000 减 (闭包全偏 减 1)。
                    做 发射_载变量 用 闭包读偏。
                    做 发射_存变量 用 闭包局偏。
                完。
            完。
            cli 为 cli 加 1。
        完。
    完。
    做 编译_语句列表 用 体。
    做 发射_载立即 用 0、0 减 1313754700。
    做 发射_尾声。
    帧 为 (变量数 加 8) 乘 8。
    对齐帧 为 (帧 加 15) 除以 16 乘 16。
    做 _elf写32 用 代码、帧大小位、对齐帧。
    主体模式 为 _保存主体模式。
完。

可变 _AI后端_SIMD发射数 为 0。

可变 _AI后端_低比特发射数 为 0。

可变 _AI后端_GQA发射数 为 0。

可变 _AI后端_通用发射数 为 0。

AI后端_选择路径 接受 算子标签、精度、设备：
    若 设备 等于 1：
        返还 1。
    完。
    若 精度 等于 3 或者 精度 等于 4：
        返还 2。
    完。
    若 算子标签 等于 3：
        返还 3。
    完。
    若 算子标签 等于 5：
        返还 1。
    完。
    若 算子标签 等于 6：
        返还 2。
    完。
    若 算子标签 等于 1 或者 算子标签 等于 2：
        返还 1。
    完。
    返还 0。
完。

AI后端_记录发射 接受 路径：
    若 路径 等于 0：
        _AI后端_通用发射数 为 _AI后端_通用发射数 加 1。
    完。
    若 路径 等于 1：
        _AI后端_SIMD发射数 为 _AI后端_SIMD发射数 加 1。
    完。
    若 路径 等于 2：
        _AI后端_低比特发射数 为 _AI后端_低比特发射数 加 1。
    完。
    若 路径 等于 3：
        _AI后端_GQA发射数 为 _AI后端_GQA发射数 加 1。
    完。
完。

AI后端_发射SIMD 接受 操作名：
    _AI后端_SIMD发射数 为 _AI后端_SIMD发射数 加 1。
    若 操作名 等于 「加」：
        做 发射组 用 一组：0x66、0x0F、0xFE、0xC1。
        返还 空。
    完。
    若 操作名 等于 「乘」：
        做 发射组 用 一组：0x66、0x0F、0x38、0x40、0xC1。
        返还 空。
    完。
    若 操作名 等于 「浮点加」：
        做 发射组 用 一组：0x0F、0x58、0xC1。
        返还 空。
    完。
    若 操作名 等于 「浮点乘」：
        做 发射组 用 一组：0x0F、0x59、0xC1。
        返还 空。
    完。
    若 操作名 等于 「载入」：
        做 发射组 用 一组：0xF3、0x0F、0x6F、0x01。
        返还 空。
    完。
    若 操作名 等于 「存储」：
        做 发射组 用 一组：0xF3、0x0F、0x7F、0x01。
        返还 空。
    完。
    若 操作名 等于 「水平加」：
        做 发射组 用 一组：0x66、0x0F、0x38、0x02、0xC0。
        做 发射组 用 一组：0x66、0x0F、0x38、0x02、0xC0。
        返还 空。
    完。
    若 操作名 等于 「最大值」：
        做 发射组 用 一组：0x66、0x0F、0x38、0x3D、0xC1。
        返还 空。
    完。
完。

AI后端_发射低比特 接受 操作名、精度：
    _AI后端_低比特发射数 为 _AI后端_低比特发射数 加 1。
    若 操作名 等于 「打包」 并且 精度 等于 8：
        做 发射组 用 一组：0x66、0x0F、0x6B、0xC0。
        做 发射组 用 一组：0x66、0x0F、0x63、0xC0。
        返还 空。
    完。
    若 操作名 等于 「解包」 并且 精度 等于 8：
        做 发射组 用 一组：0x66、0x0F、0x38、0x21、0xC0。
        返还 空。
    完。
    若 操作名 等于 「打包」 并且 精度 等于 4：
        做 发射组 用 一组：0x66、0x0F、0x72、0xD1、0x04。
        做 发射组 用 一组：0x66、0x0F、0x72、0xF0、0x04。
        做 发射组 用 一组：0x66、0x0F、0xEB、0xC1。
        返还 空。
    完。
    若 操作名 等于 「量化」：
        做 发射组 用 一组：0x66、0x0F、0x38、0x0B、0xC1。
        返还 空。
    完。
完。

AI后端_发射GQA 接受 头数、KV头数：
    _AI后端_GQA发射数 为 _AI后端_GQA发射数 加 1。
    若 KV头数 大于 0 并且 头数 大于 0：
        重复因子 为 头数 除以 KV头数。
        若 重复因子 大于 1：
            做 发射组 用 一组：0x48、0x31、0xD2。
            做 发射组 用 一组：0x48、0xC7、0xC1。
            做 发射32 用 重复因子。
            做 发射组 用 一组：0x48、0xF7、0xF1。
        完。
    完。
完。

AI后端_入口包装 接受 函数名：
    若 做 编译_当前是裸机目标：
        返还 「baremetal」。
    完。
    返还 「host」。
完。

向量寄存器_xmm 为 0。

向量寄存器_ymm 为 1。

向量寄存器_zmm 为 2。

AI后端_选择向量寄存器 接受 精度、元素数：
    若 元素数 小于或等于 4：
        返还 向量寄存器_xmm。
    完。
    若 元素数 小于或等于 8：
        返还 向量寄存器_ymm。
    完。
    返还 向量寄存器_zmm。
完。

AI后端_统计报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「SIMD发射」、做 转字符串 用 _AI后端_SIMD发射数。
    做 字典设 用 r、「低比特发射」、做 转字符串 用 _AI后端_低比特发射数。
    做 字典设 用 r、「GQA发射」、做 转字符串 用 _AI后端_GQA发射数。
    做 字典设 用 r、「通用发射」、做 转字符串 用 _AI后端_通用发射数。
    做 字典设 用 r、「裸机目标」、做 转字符串 用 做 编译_当前是裸机目标。
    返还 r。
完。

本章 收束。
