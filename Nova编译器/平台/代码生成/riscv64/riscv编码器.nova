《riscv编码器》

RV_指令字节 接受 w：
    返还 一组：w 位与 0xFF、w 右移 8 位与 0xFF、w 右移 16 位与 0xFF、w 右移 24 位与 0xFF。
完。

RV指令长度 接受 零参：
    返还 4。
完。

R型 接受 f7、rs2、rs1、f3、rd、op：
    返还 (f7 位与 0x7F) 左移 25 位或 (rs2 位与 31) 左移 20 位或 (rs1 位与 31) 左移 15 位或 (f3 位与 7) 左移 12 位或 (rd 位与 31) 左移 7 位或 op 位与 0x7F。
完。

RV_加 接受 rd、rs1、rs2：
    返还 做 R型 用 0x00、rs2、rs1、0、rd、0x33。
完。

RV_减 接受 rd、rs1、rs2：
    返还 做 R型 用 0x20、rs2、rs1、0、rd、0x33。
完。

RV_与 接受 rd、rs1、rs2：
    返还 做 R型 用 0x00、rs2、rs1、7、rd、0x33。
完。

RV_或 接受 rd、rs1、rs2：
    返还 做 R型 用 0x00、rs2、rs1、6、rd、0x33。
完。

RV_异或 接受 rd、rs1、rs2：
    返还 做 R型 用 0x00、rs2、rs1、4、rd、0x33。
完。

RV_左移 接受 rd、rs1、rs2：
    返还 做 R型 用 0x00、rs2、rs1、1、rd、0x33。
完。

RV_逻辑右移 接受 rd、rs1、rs2：
    返还 做 R型 用 0x00、rs2、rs1、5、rd、0x33。
完。

RV_算术右移 接受 rd、rs1、rs2：
    返还 做 R型 用 0x20、rs2、rs1、5、rd、0x33。
完。

RV_小于置位 接受 rd、rs1、rs2：
    返还 做 R型 用 0x00、rs2、rs1、2、rd、0x33。
完。

RV_无符号小于置位 接受 rd、rs1、rs2：
    返还 做 R型 用 0x00、rs2、rs1、3、rd、0x33。
完。

RV_乘 接受 rd、rs1、rs2：
    返还 做 R型 用 0x01、rs2、rs1、0、rd、0x33。
完。

RV_高位乘 接受 rd、rs1、rs2：
    返还 做 R型 用 0x01、rs2、rs1、1、rd、0x33。
完。

RV_除 接受 rd、rs1、rs2：
    返还 做 R型 用 0x01、rs2、rs1、4、rd、0x33。
完。

RV_无符号除 接受 rd、rs1、rs2：
    返还 做 R型 用 0x01、rs2、rs1、5、rd、0x33。
完。

RV_取余 接受 rd、rs1、rs2：
    返还 做 R型 用 0x01、rs2、rs1、6、rd、0x33。
完。

RV_无符号取余 接受 rd、rs1、rs2：
    返还 做 R型 用 0x01、rs2、rs1、7、rd、0x33。
完。

I型 接受 imm、rs1、f3、rd、op：
    返还 (imm 位与 0xFFF) 左移 20 位或 (rs1 位与 31) 左移 15 位或 (f3 位与 7) 左移 12 位或 (rd 位与 31) 左移 7 位或 op 位与 0x7F。
完。

RV_加立即数 接受 rd、rs1、imm：
    返还 做 I型 用 imm、rs1、0、rd、0x13。
完。

RV_与立即数 接受 rd、rs1、imm：
    返还 做 I型 用 imm、rs1、7、rd、0x13。
完。

RV_或立即数 接受 rd、rs1、imm：
    返还 做 I型 用 imm、rs1、6、rd、0x13。
完。

RV_异或立即数 接受 rd、rs1、imm：
    返还 做 I型 用 imm、rs1、4、rd、0x13。
完。

RV_小于立即数 接受 rd、rs1、imm：
    返还 做 I型 用 imm、rs1、2、rd、0x13。
完。

RV_加载字节 接受 rd、rs1、imm：
    返还 做 I型 用 imm、rs1、0、rd、0x03。
完。

RV_加载半字 接受 rd、rs1、imm：
    返还 做 I型 用 imm、rs1、1、rd、0x03。
完。

RV_加载字 接受 rd、rs1、imm：
    返还 做 I型 用 imm、rs1、2、rd、0x03。
完。

RV_加载双字 接受 rd、rs1、imm：
    返还 做 I型 用 imm、rs1、3、rd、0x03。
完。

RV_加载无符号字节 接受 rd、rs1、imm：
    返还 做 I型 用 imm、rs1、4、rd、0x03。
完。

RV_寄存器跳转链接 接受 rd、rs1、imm：
    返还 做 I型 用 imm、rs1、0、rd、0x67。
完。

S型 接受 imm、rs2、rs1、f3、op：
    返还 (imm 右移 5 位与 0x7F) 左移 25 位或 (rs2 位与 31) 左移 20 位或 (rs1 位与 31) 左移 15 位或 (f3 位与 7) 左移 12 位或 (imm 位与 0x1F) 左移 7 位或 op 位与 0x7F。
完。

RV_存储字节 接受 rs2、rs1、imm：
    返还 做 S型 用 imm、rs2、rs1、0、0x23。
完。

RV_存储半字 接受 rs2、rs1、imm：
    返还 做 S型 用 imm、rs2、rs1、1、0x23。
完。

RV_存储字 接受 rs2、rs1、imm：
    返还 做 S型 用 imm、rs2、rs1、2、0x23。
完。

RV_存储双字 接受 rs2、rs1、imm：
    返还 做 S型 用 imm、rs2、rs1、3、0x23。
完。

B型 接受 imm、rs2、rs1、f3、op：
    b12 为 imm 右移 12 位与 1。
    b11 为 imm 右移 11 位与 1。
    b10_5 为 imm 右移 5 位与 0x3F。
    b4_1 为 imm 右移 1 位与 0xF。
    返还 b12 左移 31 位或 b10_5 左移 25 位或 (rs2 位与 31) 左移 20 位或 (rs1 位与 31) 左移 15 位或 (f3 位与 7) 左移 12 位或 b4_1 左移 8 位或 b11 左移 7 位或 op 位与 0x7F。
完。

RV_等于分支 接受 rs1、rs2、imm：
    返还 做 B型 用 imm、rs2、rs1、0、0x63。
完。

RV_不等分支 接受 rs1、rs2、imm：
    返还 做 B型 用 imm、rs2、rs1、1、0x63。
完。

RV_小于分支 接受 rs1、rs2、imm：
    返还 做 B型 用 imm、rs2、rs1、4、0x63。
完。

RV_大于等于分支 接受 rs1、rs2、imm：
    返还 做 B型 用 imm、rs2、rs1、5、0x63。
完。

RV_加载高位立即数 接受 rd、imm20：
    返还 (imm20 位与 0xFFFFF) 左移 12 位或 (rd 位与 31) 左移 7 位或 0x37。
完。

RV_加载PC高位 接受 rd、imm20：
    返还 (imm20 位与 0xFFFFF) 左移 12 位或 (rd 位与 31) 左移 7 位或 0x17。
完。

RV_跳转链接 接受 rd、偏移：
    b20 为 偏移 右移 20 位与 1。
    b10_1 为 偏移 右移 1 位与 0x3FF。
    b11 为 偏移 右移 11 位与 1。
    b19_12 为 偏移 右移 12 位与 0xFF。
    返还 b20 左移 31 位或 b10_1 左移 21 位或 b11 左移 20 位或 b19_12 左移 12 位或 (rd 位与 31) 左移 7 位或 0x6F。
完。

RV_环境调用 接受 零参：
    返还 0x00000073。
完。

RV_环境断点 接受 零参：
    返还 0x00100073。
完。

RV_空操作 接受 零参：
    返还 做 RV_加立即数 用 0、0、0。
完。

RV_移动 接受 rd、rs1：
    返还 做 RV_加立即数 用 rd、rs1、0。
完。

RV_加载立即数 接受 rd、imm：
    返还 做 RV_加立即数 用 rd、0、imm。
完。

RV_返回 接受 零参：
    返还 做 RV_寄存器跳转链接 用 0、1、0。
完。

RV_调用 接受 rd、偏移：
    返还 做 RV_跳转链接 用 rd、偏移。
完。

RV_跳转 接受 偏移：
    返还 做 RV_跳转链接 用 0、偏移。
完。

RV_等零分支 接受 rs1、imm：
    返还 做 RV_等于分支 用 rs1、0、imm。
完。

RV_非零分支 接受 rs1、imm：
    返还 做 RV_不等分支 用 rs1、0、imm。
完。

RV_字加 接受 rd、rs1、rs2：
    返还 做 R型 用 0x00、rs2、rs1、0、rd、0x3B。
完。

RV_字减 接受 rd、rs1、rs2：
    返还 做 R型 用 0x20、rs2、rs1、0、rd、0x3B。
完。

RV_字乘 接受 rd、rs1、rs2：
    返还 做 R型 用 0x01、rs2、rs1、0、rd、0x3B。
完。

RV_字除 接受 rd、rs1、rs2：
    返还 做 R型 用 0x01、rs2、rs1、4、rd、0x3B。
完。

RV_字取余 接受 rd、rs1、rs2：
    返还 做 R型 用 0x01、rs2、rs1、6、rd、0x3B。
完。

RV_字左移 接受 rd、rs1、rs2：
    返还 做 R型 用 0x00、rs2、rs1、1、rd、0x3B。
完。

RV_字逻辑右移 接受 rd、rs1、rs2：
    返还 做 R型 用 0x00、rs2、rs1、5、rd、0x3B。
完。

RV_字算术右移 接受 rd、rs1、rs2：
    返还 做 R型 用 0x20、rs2、rs1、5、rd、0x3B。
完。

RV_字加立即数 接受 rd、rs1、imm：
    返还 做 I型 用 imm、rs1、0、rd、0x1B。
完。

RV_左移立即数 接受 rd、rs1、shamt：
    返还 做 I型 用 shamt 位与 0x3F、rs1、1、rd、0x13。
完。

RV_逻辑右移立即数 接受 rd、rs1、shamt：
    返还 做 I型 用 shamt 位与 0x3F、rs1、5、rd、0x13。
完。

RV_算术右移立即数 接受 rd、rs1、shamt：
    返还 做 I型 用 0x400 位或 shamt 位与 0x3F、rs1、5、rd、0x13。
完。

RV_栅栏 接受 pred、succ：
    返还 0x0000000F 位或 (pred 位与 0xF) 左移 24 位或 (succ 位与 0xF) 左移 20。
完。

RV_全序栅栏 接受 零参：
    返还 0x8330000F。
完。

RV_读写控制状态 接受 rd、rs1、csr：
    返还 (csr 位与 0xFFF) 左移 20 位或 (rs1 位与 31) 左移 15 位或 1 左移 12 位或 (rd 位与 31) 左移 7 位或 0x73。
完。

RV_置位控制状态 接受 rd、rs1、csr：
    返还 (csr 位与 0xFFF) 左移 20 位或 (rs1 位与 31) 左移 15 位或 2 左移 12 位或 (rd 位与 31) 左移 7 位或 0x73。
完。

RV_清位控制状态 接受 rd、rs1、csr：
    返还 (csr 位与 0xFFF) 左移 20 位或 (rs1 位与 31) 左移 15 位或 3 左移 12 位或 (rd 位与 31) 左移 7 位或 0x73。
完。

RV_读控制状态 接受 rd、csr：
    返还 做 RV_置位控制状态 用 rd、0、csr。
完。

CSR_机器状态 为 0x300。

CSR_机器陷阱向量 为 0x305。

CSR_机器异常PC 为 0x341。

CSR_机器原因 为 0x342。

CSR_周期计数 为 0xC00。

CSR_时间 为 0xC01。

CSR_指令退休 为 0xC02。

RV_取反 接受 rd、rs1：
    返还 做 RV_异或立即数 用 rd、rs1、0 减 1。
完。

RV_取负 接受 rd、rs1：
    返还 做 RV_减 用 rd、0、rs1。
完。

RV_等零置位 接受 rd、rs1：
    返还 做 RV_小于立即数 用 rd、rs1、1。
完。

ELF_类64位 为 2。

ELF_小端序 为 1。

处理器_X86_64 为 62。

段标志_可执行 为 1。

段标志_可写 为 2。

段标志_可读 为 4。

节标志_可写 为 1。

节标志_分配 为 2。

节标志_可执行 为 4。

符号绑定_本地 为 0。

符号绑定_全局 为 1。

符号类型_函数 为 2。

符号类型_对象 为 1。

ELF头_大小 为 64。

程序头_大小 为 56。

节头_大小 为 64。

符号_大小 为 24。

重定位_大小 为 24。

ELF_基址 为 0x400000。

ELF_页大小 为 0x1000。

riscv_R型 接受 funct7、rs2、rs1、funct3、rd、opcode：
    返还 funct7 左移 25 位或 (rs2 位与 31) 左移 20 位或 (rs1 位与 31) 左移 15 位或 (funct3 位与 7) 左移 12 位或 (rd 位与 31) 左移 7 位或 opcode 位与 0x7F。
完。

riscv_I型 接受 imm12、rs1、funct3、rd、opcode：
    返还 (imm12 位与 0xFFF) 左移 20 位或 (rs1 位与 31) 左移 15 位或 (funct3 位与 7) 左移 12 位或 (rd 位与 31) 左移 7 位或 opcode 位与 0x7F。
完。

riscv_S型 接受 imm12、rs2、rs1、funct3、opcode：
    高7 为 imm12 右移 5 位与 0x7F。
    低5 为 imm12 位与 0x1F。
    返还 高7 左移 25 位或 (rs2 位与 31) 左移 20 位或 (rs1 位与 31) 左移 15 位或 (funct3 位与 7) 左移 12 位或 低5 左移 7 位或 opcode 位与 0x7F。
完。

riscv_B型 接受 imm13、rs2、rs1、funct3、opcode：
    b12 为 imm13 右移 12 位与 1。
    b11 为 imm13 右移 11 位与 1。
    b10_5 为 imm13 右移 5 位与 0x3F。
    b4_1 为 imm13 右移 1 位与 0xF。
    返还 b12 左移 31 位或 b10_5 左移 25 位或 (rs2 位与 31) 左移 20 位或 (rs1 位与 31) 左移 15 位或 (funct3 位与 7) 左移 12 位或 b4_1 左移 8 位或 b11 左移 7 位或 opcode 位与 0x7F。
完。

riscv_U型 接受 imm20、rd、opcode：
    返还 (imm20 位与 0xFFFFF) 左移 12 位或 (rd 位与 31) 左移 7 位或 opcode 位与 0x7F。
完。

riscv_J型 接受 imm21、rd、opcode：
    b20 为 imm21 右移 20 位与 1。
    b19_12 为 imm21 右移 12 位与 0xFF。
    b11 为 imm21 右移 11 位与 1。
    b10_1 为 imm21 右移 1 位与 0x3FF。
    返还 b20 左移 31 位或 b10_1 左移 21 位或 b11 左移 20 位或 b19_12 左移 12 位或 (rd 位与 31) 左移 7 位或 opcode 位与 0x7F。
完。

riscv_是临时寄存器 接受 r：
    返还 r 大于或等于 5 并且 r 小于或等于 7 或者 r 大于或等于 28 并且 r 小于或等于 31。
完。

riscv_是保存寄存器 接受 r：
    返还 r 大于或等于 8 并且 r 小于或等于 9 或者 r 大于或等于 18 并且 r 小于或等于 27。
完。

riscv_是参数寄存器 接受 r：
    返还 r 大于或等于 10 并且 r 小于或等于 17。
完。

riscv_B型范围有效 接受 偏移：
    返还 偏移 大于或等于 负 4096 并且 偏移 小于或等于 4094。
完。

riscv_J型范围有效 接受 偏移：
    返还 偏移 大于或等于 负 1048576 并且 偏移 小于或等于 1048574。
完。

本章 收束。
