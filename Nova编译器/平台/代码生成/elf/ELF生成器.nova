《ELF生成器》

引用《..·..·..·标准库·基础》。

引用《..·..·..·编译器·前端入口》。

引用《..·..·..·编译器·规划·核心·常量》。

ELF_安全取列表项 接受 列表、索引：
    若 做 是列表 用 列表 等于 假 或者 索引 小于 0 或者 索引 大于或等于 做 计数 用 列表：
        返还 0。
    完。
    返还 列表 的 第 索引 项。
完。

构建ELF 接受 入口偏移、代码大小、linux_wrappers：
    做 打印 用 「构建 ELF64 文件 (Linux x86-64)...」。
    elf_hdr_sz 为 64。
    phdr_sz 为 56。
    phnum 为 4。
    可变 _start字节 为 空。
    可变 总代码含start 为 代码大小。
    代码段文件偏移 为 0x1000。
    基址 为 0x400000。
    代码段虚址 为 基址 加 代码段文件偏移。
    数据段文件偏移 为 代码段文件偏移 加 (总代码含start 加 0xFFF) 除以 0x1000 乘 0x1000。
    数据段虚址 为 基址 加 数据段文件偏移。
    数据段大小 为 0x1000。
    文件大小 为 数据段文件偏移 加 数据段大小。
    可变 elf 为 一组：（空）。
    可变 _ei 为 0。
    只要 _ei 小于 文件大小：
        做 追加 用 elf、0。
        _ei 为 _ei 加 1。
    完。
    做 _elf写 用 elf、x7F、0。
    做 _elf写 用 elf、1、0x45。
    做 _elf写 用 elf、2、0x4C。
    做 _elf写 用 elf、3、0x46。
    做 _elf写 用 elf、4、2。
    做 _elf写 用 elf、5、1。
    做 _elf写 用 elf、6、1。
    做 _elf写 用 elf、7、0。
    做 _elf写16 用 elf、16、2。
    做 _elf写16 用 elf、18、62。
    做 _elf写32 用 elf、20、1。
    可变 elf入口 为 入口偏移。
    做 _elf写64 用 elf、24、代码段虚址 加 elf入口。
    做 _elf写64 用 elf、32、elf_hdr_sz。
    做 _elf写64 用 elf、40、0。
    做 _elf写32 用 elf、48、0。
    做 _elf写16 用 elf、52、elf_hdr_sz。
    做 _elf写16 用 elf、54、phdr_sz。
    做 _elf写16 用 elf、56、phnum。
    做 _elf写16 用 elf、58、64。
    做 _elf写16 用 elf、60、0。
    做 _elf写16 用 elf、62、0。
    ph1 为 elf_hdr_sz。
    做 _elf写32 用 elf、ph1 加 0、1。
    做 _elf写32 用 elf、ph1 加 4、7。
    做 _elf写64 用 elf、ph1 加 8、0。
    做 _elf写64 用 elf、ph1 加 16、基址。
    做 _elf写64 用 elf、ph1 加 24、基址。
    做 _elf写64 用 elf、ph1 加 32、代码段文件偏移 加 代码大小。
    做 _elf写64 用 elf、ph1 加 40、代码段文件偏移 加 代码大小。
    做 _elf写64 用 elf、ph1 加 48、0x1000。
    ph2 为 elf_hdr_sz 加 phdr_sz。
    做 _elf写32 用 elf、ph2 加 0、1。
    做 _elf写32 用 elf、ph2 加 4、6。
    做 _elf写64 用 elf、ph2 加 8、数据段文件偏移。
    做 _elf写64 用 elf、ph2 加 16、数据段虚址。
    做 _elf写64 用 elf、ph2 加 24、数据段虚址。
    做 _elf写64 用 elf、ph2 加 32、数据段大小。
    做 _elf写64 用 elf、ph2 加 40、数据段大小 加 0x10000000。
    做 _elf写64 用 elf、ph2 加 48、0x1000。
    dyn_base 为 0x200。
    interp_off 为 dyn_base。
    dynstr_off 为 dyn_base 加 0x20。
    dynsym_off 为 dyn_base 加 0x40。
    hash_off 为 dyn_base 加 0x88。
    rela_off 为 dyn_base 加 0xA0。
    got_off 为 dyn_base 加 0xD0。
    dynamic_off 为 dyn_base 加 0xE0。
    interp_str 为 「/lib64/ld-linux-x86-64.so.2」。
    可变 si 为 0。
    只要 si 小于 做 计数 用 interp_str：
        做 _elf写 用 elf、数据段文件偏移 加 interp_off 加 si、做 编码 用 做 子串 用 interp_str、si、1。
        si 为 si 加 1。
    完。
    做 _elf写 用 elf、数据段文件偏移 加 interp_off 加 si、0。
    ds 为 数据段文件偏移 加 dynstr_off。
    做 _elf写 用 elf、ds 加 0、0。
    libcstr 为 「libc.so.6」。
    可变 li 为 0。
    只要 li 小于 做 计数 用 libcstr：
        做 _elf写 用 elf、ds 加 1 加 li、做 编码 用 做 子串 用 libcstr、li、1。
        li 为 li 加 1。
    完。
    做 _elf写 用 elf、ds 加 10、0。
    做 _elf写 用 elf、ds 加 11、100。
    做 _elf写 用 elf、ds 加 12、108。
    做 _elf写 用 elf、ds 加 13、111。
    做 _elf写 用 elf、ds 加 14、112。
    做 _elf写 用 elf、ds 加 15、101。
    做 _elf写 用 elf、ds 加 16、110。
    做 _elf写 用 elf、ds 加 17、0。
    做 _elf写 用 elf、ds 加 18、100。
    做 _elf写 用 elf、ds 加 19、108。
    做 _elf写 用 elf、ds 加 20、115。
    做 _elf写 用 elf、ds 加 21、121。
    做 _elf写 用 elf、ds 加 22、109。
    做 _elf写 用 elf、ds 加 23、0。
    符号 为 数据段文件偏移 加 dynsym_off。
    做 _elf写32 用 elf、符号 加 24、11。
    做 _elf写 用 elf、符号 加 28、0x12。
    做 _elf写32 用 elf、符号 加 48、18。
    做 _elf写 用 elf、符号 加 52、0x12。
    ht 为 数据段文件偏移 加 hash_off。
    做 _elf写32 用 elf、ht 加 0、1。
    做 _elf写32 用 elf、ht 加 4、3。
    做 _elf写32 用 elf、ht 加 8、1。
    做 _elf写32 用 elf、ht 加 12、0。
    做 _elf写32 用 elf、ht 加 16、2。
    做 _elf写32 用 elf、ht 加 20、0。
    got_dlopen_vaddr 为 数据段虚址 加 got_off。
    got_dlsym_vaddr 为 数据段虚址 加 got_off 加 8。
    ra 为 数据段文件偏移 加 rela_off。
    做 _elf写64 用 elf、ra 加 0、got_dlopen_vaddr。
    做 _elf写32 用 elf、ra 加 8、6。
    做 _elf写32 用 elf、ra 加 12、1。
    做 _elf写64 用 elf、ra 加 16、0。
    做 _elf写64 用 elf、ra 加 24、got_dlsym_vaddr。
    做 _elf写32 用 elf、ra 加 32、6。
    做 _elf写32 用 elf、ra 加 36、2。
    做 _elf写64 用 elf、ra 加 40、0。
    dt 为 数据段文件偏移 加 dynamic_off。
    做 _elf写64 用 elf、dt 加 0、1。
    做 _elf写64 用 elf、dt 加 8、1。
    做 _elf写64 用 elf、dt 加 16、4。
    做 _elf写64 用 elf、dt 加 24、数据段虚址 加 hash_off。
    做 _elf写64 用 elf、dt 加 32、5。
    做 _elf写64 用 elf、dt 加 40、数据段虚址 加 dynstr_off。
    做 _elf写64 用 elf、dt 加 48、6。
    做 _elf写64 用 elf、dt 加 56、数据段虚址 加 dynsym_off。
    做 _elf写64 用 elf、dt 加 64、10。
    做 _elf写64 用 elf、dt 加 72、24。
    做 _elf写64 用 elf、dt 加 80、11。
    做 _elf写64 用 elf、dt 加 88、24。
    做 _elf写64 用 elf、dt 加 96、7。
    做 _elf写64 用 elf、dt 加 104、数据段虚址 加 rela_off。
    做 _elf写64 用 elf、dt 加 112、8。
    做 _elf写64 用 elf、dt 加 120、48。
    做 _elf写64 用 elf、dt 加 128、9。
    做 _elf写64 用 elf、dt 加 136、24。
    做 _elf写64 用 elf、dt 加 144、0。
    做 _elf写64 用 elf、dt 加 152、0。
    ph3 为 elf_hdr_sz 加 phdr_sz 乘 2。
    做 _elf写32 用 elf、ph3 加 0、3。
    做 _elf写32 用 elf、ph3 加 4、4。
    做 _elf写64 用 elf、ph3 加 8、数据段文件偏移 加 interp_off。
    做 _elf写64 用 elf、ph3 加 16、数据段虚址 加 interp_off。
    做 _elf写64 用 elf、ph3 加 24、数据段虚址 加 interp_off。
    做 _elf写64 用 elf、ph3 加 32、28。
    做 _elf写64 用 elf、ph3 加 40、28。
    做 _elf写64 用 elf、ph3 加 48、1。
    ph4 为 elf_hdr_sz 加 phdr_sz 乘 3。
    做 _elf写32 用 elf、ph4 加 0、2。
    做 _elf写32 用 elf、ph4 加 4、6。
    做 _elf写64 用 elf、ph4 加 8、数据段文件偏移 加 dynamic_off。
    做 _elf写64 用 elf、ph4 加 16、数据段虚址 加 dynamic_off。
    做 _elf写64 用 elf、ph4 加 24、数据段虚址 加 dynamic_off。
    做 _elf写64 用 elf、ph4 加 32、160。
    做 _elf写64 用 elf、ph4 加 40、160。
    做 _elf写64 用 elf、ph4 加 48、8。
    可变 ci 为 0。
    只要 ci 小于 代码大小：
        做 _elf写 用 elf、代码段文件偏移 加 ci、代码 的 第 ci 项。
        ci 为 ci 加 1。
    完。
    若 _start字节 不等于 空：
        可变 _si2 为 0。
        只要 _si2 小于 做 计数 用 _start字节：
            做 _elf写 用 elf、代码段文件偏移 加 代码大小 加 _si2、_start字节 的 第 _si2 项。
            _si2 为 _si2 加 1。
        完。
    完。
    rt_heap_addr 为 数据段虚址。
    rt_globals_addr 为 数据段虚址 加 8。
    可变 fi 为 0。
    只要 fi 小于 做 计数 用 修正表：
        修类 为 做 修正表_取类型 用 修正表、fi。
        修位 为 做 修正表_取位置 用 修正表、fi。
        修附 为 做 修正表_取附加 用 修正表、fi。
        步长 为 做 修正表_项步长 用 修正表、fi。
        若 修类 不等于 空：
            若 修类 等于 「call」 并且 修附 不等于 空：
                目标名c 为 修附。
                目标偏移c 为 做 字典取 用 函数表、目标名c。
                若 目标偏移c 不等于 空：
                    可变 相对c 为 目标偏移c 减 (修位 加 4)。
                    做 _elf写32 用 elf、代码段文件偏移 加 修位、相对c。
                完。
            否则若 修类 等于 「str」 并且 修附 不等于 空：
                索引s 为 修附。
                若 做 是整数 用 索引s 并且 索引s 大于或等于 0 并且 索引s 小于 做 计数 用 字串偏移：
                    字串位s 为 字串偏移 的 第 索引s 项。
                    相对s 为 字串位s 减 (修位 加 4)。
                    做 _elf写32 用 elf、代码段文件偏移 加 修位、相对s。
                完。
            否则若 修类 等于 「rt_heap」：
                下一指令指针 为 代码段虚址 加 修位 加 4。
                做 _elf写32 用 elf、代码段文件偏移 加 修位、rt_heap_addr 减 下一指令指针。
            否则若 修类 等于 「rt_globals」：
                下一指令指针 为 代码段虚址 加 修位 加 4。
                做 _elf写32 用 elf、代码段文件偏移 加 修位、rt_globals_addr 减 下一指令指针。
            否则若 修类 等于 「rt_saved_rsp」：
                下一指令指针 为 代码段虚址 加 修位 加 4。
                做 _elf写32 用 elf、代码段文件偏移 加 修位、数据段虚址 加 16 减 下一指令指针。
            否则若 修类 等于 「elf_entry_jmp」：
                重定位32 为 入口偏移 减 (修位 加 4)。
                做 _elf写32 用 elf、代码段文件偏移 加 修位、重定位32。
            否则若 修类 等于 「got_dlopen」：
                下一指令指针 为 代码段虚址 加 修位 加 4。
                做 _elf写32 用 elf、代码段文件偏移 加 修位、got_dlopen_vaddr 减 下一指令指针。
            否则若 修类 等于 「got_dlsym」：
                下一指令指针 为 代码段虚址 加 修位 加 4。
                做 _elf写32 用 elf、代码段文件偏移 加 修位、got_dlsym_vaddr 减 下一指令指针。
            完。
        完。
        fi 为 fi 加 步长。
    完。
    返还 一组：elf、文件大小。
完。

指令_移动 为 1。

ELF生成_指令_加 为 2。

ELF生成_指令_减 为 3。

ELF生成_指令_乘 为 4。

ELF生成_指令_比较 为 5。

ELF生成_指令_跳转 为 6。

指令_条件等于跳 为 7。

指令_条件不等跳 为 8。

指令_压栈 为 9。

指令_弹栈 为 10。

ELF生成_指令_空操作 为 11。

ELF生成_指令_返回 为 12。

ELF生成_指令_调用 为 13。

指令_异或 为 14。

指令_取有效地址 为 15。

ELF生成_指令_左移 为 16。

指令_测试 为 17。

ELF_创建段 接受 名称、类型、标志、对齐：
    返还 一组：名称、类型、标志、对齐、一组：（空）。
完。

ELF_段_添加数据 接受 段、数据：
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 段 的 第 4 项、数据 的 第 i 项。
        i 为 i 加 1。
    完。
完。

ELF_段_大小 接受 段：
    返还 做 计数 用 段 的 第 4 项。
完。

ELF_段_设置地址 接受 段、地址：
    段 的 第 5 项 为 地址。
完。

ELF_段_设置偏移 接受 段、偏移：
    段 的 第 6 项 为 偏移。
完。

ELF_写入头 接受 缓冲、入口、段头偏移、段数：
    做 追加 用 缓冲、0x7F。
    做 追加 用 缓冲、0x45。
    做 追加 用 缓冲、0x4C。
    做 追加 用 缓冲、0x46。
    做 追加 用 缓冲、2。
    做 追加 用 缓冲、1。
    做 追加 用 缓冲、1。
    做 追加 用 缓冲、0。
    可变 i 为 0。
    只要 i 小于 8：
        做 追加 用 缓冲、0。
        i 为 i 加 1。
    完。
    做 追加 用 缓冲、2。
    做 追加 用 缓冲、0。
    做 追加 用 缓冲、0x3E。
    做 追加 用 缓冲、0。
    做 ELF_写入u32 用 缓冲、1。
    做 ELF_写入u64 用 缓冲、入口。
    做 ELF_写入u64 用 缓冲、64。
    做 ELF_写入u64 用 缓冲、段头偏移。
    做 ELF_写入u32 用 缓冲、0。
    做 追加 用 缓冲、64。
    做 追加 用 缓冲、0。
    做 追加 用 缓冲、56。
    做 追加 用 缓冲、0。
    做 追加 用 缓冲、段数。
    做 追加 用 缓冲、0。
完。

ELF_写入u32 接受 缓冲、值：
    做 追加 用 缓冲、值 位与 0xFF。
    做 追加 用 缓冲、值 右移 8 位与 0xFF。
    做 追加 用 缓冲、值 右移 16 位与 0xFF。
    做 追加 用 缓冲、值 右移 24 位与 0xFF。
完。

ELF_写入u64 接受 缓冲、值：
    做 ELF_写入u32 用 缓冲、值 位与 0xFFFFFFFF。
    做 ELF_写入u32 用 缓冲、值 右移 32 位与 0xFFFFFFFF。
完。

ELF_写入程序头 接受 缓冲、类型、标志、偏移、虚拟地址、文件大小、内存大小、对齐：
    做 ELF_写入u32 用 缓冲、类型。
    做 ELF_写入u32 用 缓冲、标志。
    做 ELF_写入u64 用 缓冲、偏移。
    做 ELF_写入u64 用 缓冲、虚拟地址。
    做 ELF_写入u64 用 缓冲、虚拟地址。
    做 ELF_写入u64 用 缓冲、文件大小。
    做 ELF_写入u64 用 缓冲、内存大小。
    做 ELF_写入u64 用 缓冲、对齐。
完。

ELF_计算跳转偏移 接受 当前地址、目标地址：
    返还 目标地址 减 当前地址 减 4。
完。

ELF_对齐到 接受 值、对齐：
    若 对齐 等于 0：
        返还 值。
    完。
    余 为 值 取余于 对齐。
    若 余 等于 0：
        返还 值。
    完。
    返还 值 加 对齐 减 余。
完。

可变 _ELF_生成次数 为 0。

可变 _ELF_总大小 为 0。

可变 _ELF_段数 为 0。

ELF_记录生成 接受 大小：
    _ELF_生成次数 为 _ELF_生成次数 加 1。
    _ELF_总大小 为 _ELF_总大小 加 大小。
完。

ELF_记录段 接受 零参：
    _ELF_段数 为 _ELF_段数 加 1。
完。

ELF_质量评估 接受 零参：
    若 _ELF_总大小 大于 10000000：
        返还 「做 二进制偏大 用 考虑strip」。
    完。
    若 _ELF_段数 大于 20：
        返还 「做 段过多 用 合并段」。
    完。
    返还 「ELF生成正常」。
完。

ELF_重置 接受 零参：
    _ELF_生成次数 为 0。
    _ELF_总大小 为 0。
    _ELF_段数 为 0。
完。

可变 _ELF_符号表 为 一组：（空）。

ELF_添加符号 接受 名称、地址、大小、类型：
    做 追加 用 _ELF_符号表、一组：名称、地址、大小、类型。
完。

ELF_查找符号 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _ELF_符号表：
        若 _ELF_符号表 的 第 i 项 的 第 0 项 等于 名称：
            返还 _ELF_符号表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

ELF_符号数 接受 零参：
    返还 做 计数 用 _ELF_符号表。
完。

ELF_创建字符串表 接受 零参：
    返还 一组：一组：0、1。
完。

ELF_添加字符串 接受 表、文本：
    偏移 为 表 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        做 追加 用 表 的 第 0 项、做 字符码 用 文本、i。
        i 为 i 加 1。
    完。
    做 追加 用 表 的 第 0 项、0。
    表 的 第 1 项 为 表 的 第 1 项 加 做 计数 用 文本 加 1。
    返还 偏移。
完。

ELF_清除符号表 接受 零参：
    _ELF_符号表 为 一组：（空）。
完。

本章 收束。
