《渲染合同》

引用《..·..·..·标准库·基础》。

像素_RGBA8 为 1。

像素_RGB8 为 2。

像素_BGRA8 为 3。

像素_R8 为 4。

像素_深度24 为 10。

像素_深度32F 为 11。

像素_模板8 为 12。

顶点属性_创建 接受 名称、分量、类型、偏移、步长：
    返还 一组：名称、分量、类型、偏移、步长。
完。

顶点布局_创建 接受 零参：
    返还 一组：（空）。
完。

顶点布局_添加 接受 布局、属性：
    做 追加 用 布局、属性。
完。

顶点布局_步长 接受 布局：
    若 做 计数 用 布局 等于 0：
        返还 0。
    完。
    最后 为 布局 的 第 做 计数 用 布局 减 1 项。
    返还 最后 的 第 3 项 加 最后 的 第 1 项 乘 4。
完。

渲染合同_创建 接受 宽、高：
    返还 一组：像素_RGBA8、像素_深度24、0、做 顶点布局_创建、0、1、宽、高。
完。

渲染合同_设像素格式 接受 合同、格式：
    合同 的 第 0 项 为 格式。
完。

渲染合同_设深度格式 接受 合同、格式：
    合同 的 第 1 项 为 格式。
完。

渲染合同_设顶点布局 接受 合同、布局：
    合同 的 第 3 项 为 布局。
完。

渲染pass_创建 接受 名称、合同：
    返还 一组：名称、合同、一组：0、0、255、1000、0、0、0。
完。

渲染pass_记录绘制 接受 pass、三角形数：
    pass 的 第 4 项 为 pass 的 第 4 项 加 1。
    pass 的 第 5 项 为 pass 的 第 5 项 加 三角形数。
完。

渲染pass_完成 接受 pass：
    pass 的 第 6 项 为 2。
完。

渲染合同_帧缓冲_创建 接受 宽、高、颜色数、有深度、有模板：
    像素大小 为 宽 乘 高 乘 4 乘 颜色数。
    若 有深度 不等于 0：
        像素大小 为 像素大小 加 宽 乘 高 乘 4。
    完。
    若 有模板 不等于 0：
        像素大小 为 像素大小 加 宽 乘 高。
    完。
    返还 一组：宽、高、颜色数、有深度、有模板、像素大小、0。
完。

帧缓冲_离屏 接受 fb：
    fb 的 第 6 项 为 1。
完。

帧缓冲_上屏 接受 fb：
    fb 的 第 6 项 为 0。
完。

可变 _渲染管线 为 一组：一组：（空）、一组：（空）、0、0、0、0。

渲染管线_重置 接受 零参：
    _渲染管线 为 一组：一组：（空）、一组：（空）、0、0、0、0。
完。

渲染管线_添加pass 接受 pass：
    做 追加 用 _渲染管线 的 第 0 项、pass。
完。

渲染管线_添加帧缓冲 接受 fb：
    做 追加 用 _渲染管线 的 第 1 项、fb。
完。

渲染管线_提交帧 接受 零参：
    _渲染管线 的 第 2 项 为 _渲染管线 的 第 2 项 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _渲染管线 的 第 0 项：
        p 为 _渲染管线 的 第 0 项 的 第 i 项。
        _渲染管线 的 第 3 项 为 _渲染管线 的 第 3 项 加 p 的 第 4 项。
        _渲染管线 的 第 4 项 为 _渲染管线 的 第 4 项 加 p 的 第 5 项。
        i 为 i 加 1。
    完。
    _渲染管线 的 第 5 项 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 _渲染管线 的 第 1 项：
        _渲染管线 的 第 5 项 为 _渲染管线 的 第 5 项 加 _渲染管线 的 第 1 项 的 第 i 项 的 第 5 项。
        i 为 i 加 1。
    完。
完。

渲染合同_渲染合同_报告 接受 合同：
    可变 r 为 做 字典。
    若 合同 的 第 0 项 等于 像素_RGBA8：
        做 字典设 用 r、「像素格式」、「RGBA8」。
    否则若 合同 的 第 0 项 等于 像素_RGB8：
        做 字典设 用 r、「像素格式」、「RGB8」。
    否则若 合同 的 第 0 项 等于 像素_BGRA8：
        做 字典设 用 r、「像素格式」、「BGRA8」。
    否则：
        做 字典设 用 r、「像素格式」、做 转字符串 用 合同 的 第 0 项。
    完。
    做 字典设 用 r、「深度格式」、做 转字符串 用 合同 的 第 1 项。
    做 字典设 用 r、「顶点属性数」、做 转字符串 用 做 计数 用 合同 的 第 3 项。
    做 字典设 用 r、「顶点步长」、做 转字符串 用 做 顶点布局_步长 用 合同 的 第 3 项。
    做 字典设 用 r、「宽」、做 转字符串 用 合同 的 第 6 项。
    做 字典设 用 r、「高」、做 转字符串 用 合同 的 第 7 项。
    返还 r。
完。

渲染pass_报告 接受 pass：
    可变 r 为 做 字典。
    做 字典设 用 r、「名称」、做 转字符串 用 pass 的 第 0 项。
    做 字典设 用 r、「绘制调用」、做 转字符串 用 pass 的 第 4 项。
    做 字典设 用 r、「三角形数」、做 转字符串 用 pass 的 第 5 项。
    若 pass 的 第 6 项 等于 2：
        做 字典设 用 r、「状态」、「complete」。
    否则若 pass 的 第 6 项 等于 1：
        做 字典设 用 r、「状态」、「执行中」。
    否则：
        做 字典设 用 r、「状态」、「待执行」。
    完。
    返还 r。
完。

帧缓冲_报告 接受 fb：
    可变 r 为 做 字典。
    做 字典设 用 r、「分辨率」、做 字符串拼接 用 做 转字符串 用 fb 的 第 0 项、做 字符串拼接 用 「x」、做 转字符串 用 fb 的 第 1 项。
    做 字典设 用 r、「颜色附件」、做 转字符串 用 fb 的 第 2 项。
    做 字典设 用 r、「深度」、做 转字符串 用 fb 的 第 3 项。
    做 字典设 用 r、「模板」、做 转字符串 用 fb 的 第 4 项。
    做 字典设 用 r、「字节数」、做 转字符串 用 fb 的 第 5 项。
    若 fb 的 第 6 项 不等于 0：
        做 字典设 用 r、「类型」、「离屏」。
    否则：
        做 字典设 用 r、「类型」、「上屏」。
    完。
    返还 r。
完。

GPU渲染_报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「帧数」、做 转字符串 用 _渲染管线 的 第 2 项。
    做 字典设 用 r、「pass数」、做 转字符串 用 做 计数 用 _渲染管线 的 第 0 项。
    做 字典设 用 r、「帧缓冲数」、做 转字符串 用 做 计数 用 _渲染管线 的 第 1 项。
    做 字典设 用 r、「总绘制调用」、做 转字符串 用 _渲染管线 的 第 3 项。
    做 字典设 用 r、「总三角形」、做 转字符串 用 _渲染管线 的 第 4 项。
    做 字典设 用 r、「显存使用」、做 转字符串 用 _渲染管线 的 第 5 项。
    返还 r。
完。

渲染合同_报告 接受 合同：
    返还 做 渲染合同_渲染合同_报告 用 合同。
完。

渲染通道_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「pass总数」、做 转字符串 用 做 计数 用 _渲染管线 的 第 0 项。
    可变 rpi 为 0。
    只要 rpi 小于 做 计数 用 _渲染管线 的 第 0 项 并且 rpi 小于 10：
        pass 为 _渲染管线 的 第 0 项 的 第 rpi 项。
        做 字典设 用 r、做 字符串拼接 用 「pass_」、做 转字符串 用 rpi、做 字符串拼接 用 pass 的 第 0 项、做 字符串拼接 用 「_drawcalls=」、做 转字符串 用 pass 的 第 2 项。
        rpi 为 rpi 加 1。
    完。
    返还 r。
完。

可变 _场景追踪 为 一组：（空）。

场景追踪_重置 接受 零参：
    _场景追踪 为 一组：（空）。
完。

场景追踪_记录 接受 阶段、对象名、数据大小、详情：
    做 追加 用 _场景追踪、一组：阶段、对象名、做 计数 用 _场景追踪、数据大小、详情。
完。

场景追踪_报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「追踪步数」、做 转字符串 用 做 计数 用 _场景追踪。
    可变 总数据 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _场景追踪：
        步 为 _场景追踪 的 第 i 项。
        做 字典设 用 r、做 字符串拼接 用 「步」、做 字符串拼接 用 做 转字符串 用 i、「_阶段」、步 的 第 0 项。
        做 字典设 用 r、做 字符串拼接 用 「步」、做 字符串拼接 用 做 转字符串 用 i、「_对象」、步 的 第 1 项。
        做 字典设 用 r、做 字符串拼接 用 「步」、做 字符串拼接 用 做 转字符串 用 i、「_字节」、做 转字符串 用 步 的 第 3 项。
        总数据 为 总数据 加 步 的 第 3 项。
        i 为 i 加 1。
    完。
    做 字典设 用 r、「总数据字节」、做 转字符串 用 总数据。
    返还 r。
完。

Mesh_创建 接受 名称、顶点数、索引数：
    可变 顶点 为 一组：（空）。
    可变 vi 为 0。
    只要 vi 小于 顶点数 乘 3：
        做 追加 用 顶点、vi 乘 100。
        vi 为 vi 加 1。
    完。
    可变 索引 为 一组：（空）。
    可变 ii 为 0。
    只要 ii 小于 索引数：
        做 追加 用 索引、ii 取余于 顶点数。
        ii 为 ii 加 1。
    完。
    返还 一组：名称、顶点数、索引数、顶点、索引。
完。

Material_创建 接受 名称、r、g、b、a、镜面、纹理：
    返还 一组：名称、一组：r、g、b、a、镜面、纹理。
完。

渲染_真实场景smoke 接受 零参：
    做 场景追踪_重置。
    做 渲染管线_重置。
    做 场景追踪_记录 用 「窗口」、「主窗口800x600」、0、「创建」。
    三角mesh 为 做 Mesh_创建 用 「三角形」、3、3。
    做 场景追踪_记录 用 「mesh」、「三角形」、3 乘 3 乘 4、「3顶点×3分量×4字节」。
    红色材质 为 做 Material_创建 用 「红色」、255、0、255、0、500、「」。
    做 场景追踪_记录 用 「material」、「红色材质」、20、「RGBA+镜面」。
    合同 为 做 渲染合同_创建 用 800、600。
    布局 为 做 顶点布局_创建。
    做 顶点布局_添加 用 布局、做 顶点属性_创建 用 「位置」、3、0、12、0。
    做 渲染合同_设顶点布局 用 合同、布局。
    做 场景追踪_记录 用 「合同」、「RGBA8_800x600」、0、「像素格式+顶点布局」。
    fb 为 做 渲染合同_帧缓冲_创建 用 800、600、1、1、0。
    做 渲染管线_添加帧缓冲 用 fb。
    做 场景追踪_记录 用 「framebuffer」、「800x600_RGBA+深度」、fb 的 第 5 项、「颜色+深度附件」。
    pass 为 做 渲染pass_创建 用 「主pass」、合同。
    做 渲染pass_记录绘制 用 pass、三角mesh 的 第 2 项 除以 3。
    做 渲染pass_完成 用 pass。
    做 渲染管线_添加pass 用 pass。
    做 场景追踪_记录 用 「drawcall」、「三角形绘制」、三角mesh 的 第 2 项 乘 4、「1三角形」。
    做 渲染管线_提交帧。
    做 场景追踪_记录 用 「提交」、「帧提交」、0、「complete」。
    可变 结果 为 做 字典。
    做 字典设 用 结果、「render_contract」、做 渲染合同_渲染合同_报告 用 合同。
    做 字典设 用 结果、「render_pass」、做 渲染pass_报告 用 pass。
    做 字典设 用 结果、「framebuffer」、做 帧缓冲_报告 用 fb。
    做 字典设 用 结果、「gpu_render」、做 GPU渲染_报告。
    做 字典设 用 结果、「scene_trace」、做 场景追踪_报告。
    返还 结果。
完。

本章 收束。
