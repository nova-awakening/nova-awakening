《cuFFT》

引用《..·核心·FFI桥接》。

引用《..·核心·设备管理》。

引用《..·..·..·标准库·基础》。

cuFFT_实转复 为 0x2A。

cuFFT_复转实 为 0x2C。

cuFFT_复转复 为 0x29。

cuFFT_双实转复 为 0x6A。

cuFFT_双复转实 为 0x6C。

cuFFT_双复转复 为 0x69。

cuFFT_正变换 为 负 1。

cuFFT_逆变换 为 1。

计算_快速傅里叶_圆周率 为 3141592653。

可变 _cuFFT库 为 0。

可变 _cufft_可用 为 假。

可变 _cufft_操作计数 为 0。

cuFFT_加载 接受 零参：
    若 _cuFFT库 不等于 0：
        返还 _cuFFT库。
    完。
    _cuFFT库 为 做 GPU_加载CUDA候选 用 一组：「cufft64_11.dll」、「libcufft.so.11」、「libcufft.so」。
    若 _cuFFT库 不等于 0：
        _cufft_可用 为 真。
    完。
    返还 _cuFFT库。
完。

cuFFT_可用 接受 零参：
    返还 _cufft_可用。
完。

cuFFT_一维变换 接受 输入、输出、N、类型、方向：
    _cufft_操作计数 为 _cufft_操作计数 加 1。
    lib 为 做 cuFFT_加载。
    若 lib 等于 0：
        返还 做 cuCPU_快速傅里叶 用 输入、N、方向。
    完。
    plan 为 做 外部调用4 用 lib、「cufftPlan1d」、0、N、类型、1、0。
    结果 为 做 外部调用4 用 lib、「cufftExecC2C」、plan、输入、输出、方向、0。
    做 外部调用 用 lib、「cufftDestroy」、plan、0、0、0、0。
    返还 结果 等于 0。
完。

cuFFT_二维变换 接受 输入、输出、NX、NY、类型、方向：
    _cufft_操作计数 为 _cufft_操作计数 加 1。
    lib 为 做 cuFFT_加载。
    若 lib 等于 0：
        返还 假。
    完。
    plan 为 做 外部调用5 用 lib、「cufftPlan2d」、0、NX、NY、类型、1。
    结果 为 做 外部调用4 用 lib、「cufftExecC2C」、plan、输入、输出、方向、0。
    做 外部调用 用 lib、「cufftDestroy」、plan、0、0、0、0。
    返还 结果 等于 0。
完。

cuFFT_三维变换 接受 输入、输出、NX、NY、NZ、类型、方向：
    _cufft_操作计数 为 _cufft_操作计数 加 1。
    lib 为 做 cuFFT_加载。
    若 lib 等于 0：
        返还 假。
    完。
    plan 为 做 外部调用6 用 lib、「cufftPlan3d」、0、NX、NY、NZ、类型、0、0。
    结果 为 做 外部调用4 用 lib、「cufftExecC2C」、plan、输入、输出、方向、0。
    做 外部调用 用 lib、「cufftDestroy」、plan、0、0、0、0。
    返还 结果 等于 0。
完。

cuFFT_批量 接受 输入、输出、N、批量数、类型、方向：
    _cufft_操作计数 为 _cufft_操作计数 加 1。
    lib 为 做 cuFFT_加载。
    若 lib 等于 0：
        返还 假。
    完。
    plan 为 做 外部调用4 用 lib、「cufftPlan1d」、0、N、类型、批量数、0。
    结果 为 做 外部调用4 用 lib、「cufftExecC2C」、plan、输入、输出、方向、0。
    做 外部调用 用 lib、「cufftDestroy」、plan、0、0、0、0。
    返还 结果 等于 0。
完。

cuFFT_实数到复数 接受 实数输入、复数输出、N：
    _cufft_操作计数 为 _cufft_操作计数 加 1。
    lib 为 做 cuFFT_加载。
    若 lib 等于 0：
        返还 假。
    完。
    plan 为 做 外部调用4 用 lib、「cufftPlan1d」、0、N、cuFFT_实转复、1、0。
    结果 为 做 外部调用3 用 lib、「cufftExecR2C」、plan、实数输入、复数输出。
    做 外部调用 用 lib、「cufftDestroy」、plan、0、0、0、0。
    返还 结果 等于 0。
完。

cuFFT_复数到实数 接受 复数输入、实数输出、N：
    _cufft_操作计数 为 _cufft_操作计数 加 1。
    lib 为 做 cuFFT_加载。
    若 lib 等于 0：
        返还 假。
    完。
    plan 为 做 外部调用4 用 lib、「cufftPlan1d」、0、N、cuFFT_复转实、1、0。
    结果 为 做 外部调用3 用 lib、「cufftExecC2R」、plan、复数输入、实数输出。
    做 外部调用 用 lib、「cufftDestroy」、plan、0、0、0、0。
    返还 结果 等于 0。
完。

cuFFT_设置流 接受 plan、流：
    返还 做 外部调用2 用 做 cuFFT_加载、「cufftSetStream」、plan、流 等于 0。
完。

cuCPU_快速傅里叶 接受 数据、N、方向：
    可变 偶数据 为 0。
    可变 奇数据 为 0。
    若 N 小于或等于 1：
        返还 数据。
    完。
    若 N 取余于 2 不等于 0：
        返还 做 cuCPU_离散傅里叶 用 数据、N、方向。
    完。
    可变 i 为 0。
    只要 i 小于 N：
        若 i 取余于 2 等于 0：
            做 追加 用 偶数据、数据 的 第 i 乘 2 项。
            做 追加 用 偶数据、数据 的 第 i 乘 2 加 1 项。
        否则：
            做 追加 用 奇数据、数据 的 第 i 乘 2 项。
            做 追加 用 奇数据、数据 的 第 i 乘 2 加 1 项。
        完。
        i 为 i 加 1。
    完。
    偶数据 为 做 cuCPU_快速傅里叶 用 偶数据、N 除以 2、方向。
    奇数据 为 做 cuCPU_快速傅里叶 用 奇数据、N 除以 2、方向。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 N 乘 2：
        做 追加 用 结果、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 N 除以 2：
        角度 为 方向 乘 2 乘 计算_快速傅里叶_圆周率 乘 i 除以 (N 乘 1000000000)。
        cos_v 为 做 cuFFT_余弦变换 用 角度。
        sin_v 为 做 cuFFT_正弦变换 用 角度。
        t实 为 cos_v 乘 奇数据 的 第 i 乘 2 项 减 sin_v 乘 奇数据 的 第 i 乘 2 加 1 项。
        t虚 为 cos_v 乘 奇数据 的 第 i 乘 2 加 1 项 加 sin_v 乘 奇数据 的 第 i 乘 2 项。
        结果 的 第 i 乘 2 项 为 偶数据 的 第 i 乘 2 项 加 t实。
        结果 的 第 i 乘 2 加 1 项 为 偶数据 的 第 i 乘 2 加 1 项 加 t虚。
        结果 的 第 (i 加 N 除以 2) 乘 2 项 为 偶数据 的 第 i 乘 2 项 减 t实。
        结果 的 第 (i 加 N 除以 2) 乘 2 加 1 项 为 偶数据 的 第 i 乘 2 加 1 项 减 t虚。
        i 为 i 加 1。
    完。
    返还 结果。
完。

cuCPU_离散傅里叶 接受 数据、N、方向：
    可变 结果 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 N：
        可变 实 为 0。
        可变 虚 为 0。
        可变 n 为 0。
        只要 n 小于 N：
            角度 为 方向 乘 2 乘 计算_快速傅里叶_圆周率 乘 k 乘 n 除以 (N 乘 1000000000)。
            实 为 实 加 数据 的 第 n 乘 2 项 乘 做 cuFFT_余弦变换 用 角度 减 数据 的 第 n 乘 2 加 1 项 乘 做 cuFFT_正弦变换 用 角度。
            虚 为 虚 加 数据 的 第 n 乘 2 项 乘 做 cuFFT_正弦变换 用 角度 加 数据 的 第 n 乘 2 加 1 项 乘 做 cuFFT_余弦变换 用 角度。
            n 为 n 加 1。
        完。
        做 追加 用 结果、实。
        做 追加 用 结果、虚。
        k 为 k 加 1。
    完。
    返还 结果。
完。

cuFFT_余弦变换 接受 x：
    只要 x 大于 计算_快速傅里叶_圆周率：
        x 为 x 减 2 乘 计算_快速傅里叶_圆周率。
    完。
    只要 x 小于 0 减 计算_快速傅里叶_圆周率：
        x 为 x 加 2 乘 计算_快速傅里叶_圆周率。
    完。
    x2 为 x 乘 x 除以 1000000000 除以 1000000000。
    返还 1000000000 减 x2 乘 500000000 加 x2 乘 x2 乘 41666667 除以 1000000000。
完。

cuFFT_正弦变换 接受 x：
    只要 x 大于 计算_快速傅里叶_圆周率：
        x 为 x 减 2 乘 计算_快速傅里叶_圆周率。
    完。
    只要 x 小于 0 减 计算_快速傅里叶_圆周率：
        x 为 x 加 2 乘 计算_快速傅里叶_圆周率。
    完。
    x2 为 x 乘 x 除以 1000000000 除以 1000000000。
    返还 x 减 x 乘 x2 乘 166666667 除以 1000000000 加 x 乘 x2 乘 x2 乘 8333333 除以 1000000000 除以 1000000000。
完。

cuFFT_窗_汉宁 接受 N：
    可变 w 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        角度 为 2 乘 计算_快速傅里叶_圆周率 乘 i 除以 ((N 减 1) 乘 1000000000)。
        做 追加 用 w、500000000 减 500000000 乘 做 cuFFT_余弦变换 用 角度 除以 1000000000。
        i 为 i 加 1。
    完。
    返还 w。
完。

cuFFT_窗_汉明 接受 N：
    可变 w 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        角度 为 2 乘 计算_快速傅里叶_圆周率 乘 i 除以 ((N 减 1) 乘 1000000000)。
        做 追加 用 w、538000000 减 462000000 乘 做 cuFFT_余弦变换 用 角度 除以 1000000000。
        i 为 i 加 1。
    完。
    返还 w。
完。

cuFFT_窗_Blackman 接受 N：
    可变 w 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        a1 为 2 乘 计算_快速傅里叶_圆周率 乘 i 除以 ((N 减 1) 乘 1000000000)。
        a2 为 4 乘 计算_快速傅里叶_圆周率 乘 i 除以 ((N 减 1) 乘 1000000000)。
        做 追加 用 w、420000000 减 500000000 乘 做 cuFFT_余弦变换 用 a1 除以 1000000000 加 80000000 乘 做 cuFFT_余弦变换 用 a2 除以 1000000000。
        i 为 i 加 1。
    完。
    返还 w。
完。

cuFFT_应用窗 接受 数据、窗、N：
    可变 i 为 0。
    只要 i 小于 N：
        数据 的 第 i 乘 2 项 为 数据 的 第 i 乘 2 项 乘 窗 的 第 i 项 除以 1000000000。
        数据 的 第 i 乘 2 加 1 项 为 数据 的 第 i 乘 2 加 1 项 乘 窗 的 第 i 项 除以 1000000000。
        i 为 i 加 1。
    完。
完。

cuFFT_幅度谱 接受 fft数据、N：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        r 为 fft数据 的 第 i 乘 2 项。
        im 为 fft数据 的 第 i 乘 2 加 1 项。
        做 追加 用 结果、r 乘 r 加 im 乘 im。
        i 为 i 加 1。
    完。
    返还 结果。
完。

cuFFT_功率谱 接受 fft数据、N：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        r 为 fft数据 的 第 i 乘 2 项。
        im 为 fft数据 的 第 i 乘 2 加 1 项。
        做 追加 用 结果、(r 乘 r 加 im 乘 im) 除以 N。
        i 为 i 加 1。
    完。
    返还 结果。
完。

cuFFT_找峰值 接受 幅度、N、前K个：
    可变 峰值 为 一组：（空）。
    可变 i 为 1。
    只要 i 小于 N 除以 2 减 1：
        若 幅度 的 第 i 项 大于 幅度 的 第 i 减 1 项 并且 幅度 的 第 i 项 大于 幅度 的 第 i 加 1 项：
            做 追加 用 峰值、一组：i、幅度 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 k 为 0。
    只要 k 小于 做 计数 用 峰值 减 1：
        可变 j 为 k 加 1。
        只要 j 小于 做 计数 用 峰值：
            若 峰值 的 第 j 项 的 第 1 项 大于 峰值 的 第 k 项 的 第 1 项：
                临 为 峰值 的 第 k 项。
                峰值 的 第 k 项 为 峰值 的 第 j 项。
                峰值 的 第 j 项 为 临。
            完。
            j 为 j 加 1。
        完。
        k 为 k 加 1。
    完。
    若 做 计数 用 峰值 大于 前K个：
        可变 截断 为 一组：（空）。
        i 为 0。
        只要 i 小于 前K个：
            做 追加 用 截断、峰值 的 第 i 项。
            i 为 i 加 1。
        完。
        返还 截断。
    完。
    返还 峰值。
完。

cuFFT_频域卷积 接受 信号、核、N：
    可变 信号fft 为 做 cuCPU_快速傅里叶 用 信号、N、cuFFT_正变换。
    可变 核fft 为 做 cuCPU_快速傅里叶 用 核、N、cuFFT_正变换。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        ar 为 信号fft 的 第 i 乘 2 项。
        ai 为 信号fft 的 第 i 乘 2 加 1 项。
        br 为 核fft 的 第 i 乘 2 项。
        bi 为 核fft 的 第 i 乘 2 加 1 项。
        做 追加 用 结果、ar 乘 br 减 ai 乘 bi。
        做 追加 用 结果、ar 乘 bi 加 ai 乘 br。
        i 为 i 加 1。
    完。
    返还 做 cuCPU_快速傅里叶 用 结果、N、cuFFT_逆变换。
完。

cuFFT_正弦 接受 N、频率、采样率、幅度：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        角度 为 2 乘 计算_快速傅里叶_圆周率 乘 频率 乘 i 除以 (采样率 乘 1000000000)。
        做 追加 用 数据、幅度 乘 做 cuFFT_正弦变换 用 角度 除以 1000000000。
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 数据。
完。

cuFFT_余弦 接受 N、频率、采样率、幅度：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        角度 为 2 乘 计算_快速傅里叶_圆周率 乘 频率 乘 i 除以 (采样率 乘 1000000000)。
        做 追加 用 数据、幅度 乘 做 cuFFT_余弦变换 用 角度 除以 1000000000。
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 数据。
完。

cuFFT_低通 接受 fft数据、N、截止：
    可变 i 为 截止。
    只要 i 小于 N 减 截止：
        fft数据 的 第 i 乘 2 项 为 0。
        fft数据 的 第 i 乘 2 加 1 项 为 0。
        i 为 i 加 1。
    完。
完。

cuFFT_高通 接受 fft数据、N、截止：
    可变 i 为 0。
    只要 i 小于 截止：
        fft数据 的 第 i 乘 2 项 为 0。
        fft数据 的 第 i 乘 2 加 1 项 为 0。
        i 为 i 加 1。
    完。
    i 为 N 减 截止。
    只要 i 小于 N：
        fft数据 的 第 i 乘 2 项 为 0。
        fft数据 的 第 i 乘 2 加 1 项 为 0。
        i 为 i 加 1。
    完。
完。

cuFFT_带通 接受 fft数据、N、低、高：
    可变 i 为 0。
    只要 i 小于 N：
        可变 需要清零 为 假。
        若 i 小于 低：
            需要清零 为 真。
        完。
        若 i 大于 高：
            若 i 小于 N 减 高：
                需要清零 为 真。
            完。
        完。
        若 i 大于或等于 N 减 低：
            需要清零 为 真。
        完。
        若 需要清零：
            fft数据 的 第 i 乘 2 项 为 0。
            fft数据 的 第 i 乘 2 加 1 项 为 0。
        完。
        i 为 i 加 1。
    完。
完。

cuFFT_信号能量 接受 数据、N：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 N：
        和 为 和 加 数据 的 第 i 乘 2 项 乘 数据 的 第 i 乘 2 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

cuFFT_信噪比 接受 信号、噪声、N：
    信号能 为 做 cuFFT_信号能量 用 信号、N。
    噪声能 为 做 cuFFT_信号能量 用 噪声、N。
    若 噪声能 等于 0：
        返还 999999。
    完。
    返还 信号能 除以 噪声能。
完。

cuFFT_下一个2幂 接受 n：
    可变 p 为 1。
    只要 p 小于 n：
        p 为 p 乘 2。
    完。
    返还 p。
完。

cuFFT_策略 接受 N、使用GPU：
    可变 是2幂 为 假。
    可变 策略 为 「Cooley-Tukey基2」。
    可变 n 为 N。
    只要 n 大于 1：
        若 n 取余于 2 不等于 0：
            是2幂 为 假。
        完。
        n 为 n 除以 2。
    完。
    若 是2幂 等于 假：
        策略 为 「Bluestein(任意长度)」。
    完。
    若 N 小于或等于 16：
        策略 为 「DFT直接计算」。
    完。
    若 使用GPU：
        若 N 小于 256：
            返还 策略 加 「 [CPU: GPU开销大]」。
        完。
        若 N 小于 65536：
            返还 策略 加 「 [GPU: 中等加速]」。
        完。
        返还 策略 加 「 [GPU: 显著加速]」。
    完。
    返还 策略 加 「 [CPU]」。
完。

cuFFT_频谱质量 接受 幅度、N：
    峰值 为 做 cuFFT_找峰值 用 幅度、N、5。
    可变 评分 为 100。
    若 做 计数 用 峰值 等于 0：
        返还 一组：30、「无明显频率成分」。
    完。
    若 做 计数 用 峰值 大于 N 除以 4：
        评分 为 评分 减 30。
    完。
    返还 一组：评分、「频谱正常,峰值数:」 加 做 转字符串 用 做 计数 用 峰值。
完。

cuFFT_短时傅里叶 接受 信号、信号长、帧长、帧移：
    可变 帧列表 为 一组：（空）。
    可变 位置 为 0。
    只要 位置 加 帧长 小于或等于 信号长：
        可变 帧 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 帧长：
            做 追加 用 帧、信号 的 第 (位置 加 i) 乘 2 项。
            做 追加 用 帧、信号 的 第 (位置 加 i) 乘 2 加 1 项。
            i 为 i 加 1。
        完。
        窗 为 做 cuFFT_窗_汉宁 用 帧长。
        做 cuFFT_应用窗 用 帧、窗、帧长。
        fft结果 为 做 cuCPU_快速傅里叶 用 帧、帧长、cuFFT_正变换。
        做 追加 用 帧列表、fft结果。
        位置 为 位置 加 帧移。
    完。
    返还 帧列表。
完。

cuFFT_谱质心 接受 幅度、N、采样率：
    可变 加权和 为 0。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 N 除以 2：
        若 N 等于 0：
            返还。
        完。
        频率 为 i 乘 采样率 除以 N。
        加权和 为 加权和 加 频率 乘 幅度 的 第 i 项。
        总和 为 总和 加 幅度 的 第 i 项。
        i 为 i 加 1。
    完。
    若 总和 等于 0：
        返还 0。
    完。
    返还 加权和 除以 总和。
完。

cuFFT_谱带宽 接受 幅度、N、采样率：
    质心 为 做 cuFFT_谱质心 用 幅度、N、采样率。
    可变 加权和 为 0。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 N 除以 2：
        频率 为 i 乘 采样率 除以 N。
        差 为 频率 减 质心。
        加权和 为 加权和 加 差 乘 差 乘 幅度 的 第 i 项。
        总和 为 总和 加 幅度 的 第 i 项。
        i 为 i 加 1。
    完。
    若 总和 等于 0：
        返还 0。
    完。
    返还 加权和 除以 总和。
完。

cuFFT_谐波检测 接受 幅度、N、基频索引、谐波数：
    可变 谐波 为 一组：（空）。
    可变 h 为 1。
    只要 h 小于或等于 谐波数：
        索引 为 基频索引 乘 h。
        若 索引 小于 N 除以 2：
            做 追加 用 谐波、一组：h、索引、幅度 的 第 索引 项。
        完。
        h 为 h 加 1。
    完。
    返还 谐波。
完。

cuFFT_谱平坦度 接受 幅度、N：
    可变 算术和 为 0。
    可变 计 为 0。
    可变 i 为 1。
    只要 i 小于 N 除以 2：
        若 幅度 的 第 i 项 大于 0：
            算术和 为 算术和 加 幅度 的 第 i 项。
            计 为 计 加 1。
        完。
        i 为 i 加 1。
    完。
    若 计 等于 0：
        返还 0。
    完。
    若 算术和 等于 0：
        返还 0。
    完。
    返还 1000000 除以 (算术和 除以 计)。
完。

cuFFT_移动平均 接受 数据、N、窗口：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        可变 和 为 0。
        可变 计 为 0。
        可变 j 为 i 减 窗口 除以 2。
        只要 j 小于或等于 i 加 窗口 除以 2：
            若 j 大于或等于 0：
                若 j 小于 N：
                    和 为 和 加 数据 的 第 j 乘 2 项。
                    计 为 计 加 1。
                完。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和 除以 计。
        做 追加 用 结果、0。
        i 为 i 加 1。
    完。
    返还 结果。
完。

cuFFT_信号分类 接受 数据、N：
    幅度 为 做 cuFFT_幅度谱 用 数据、N。
    峰值 为 做 cuFFT_找峰值 用 幅度、N、10。
    若 做 计数 用 峰值 等于 0：
        返还 「直流/静默信号」。
    完。
    若 做 计数 用 峰值 等于 1：
        返还 「纯音信号」。
    完。
    若 做 计数 用 峰值 小于或等于 3：
        返还 「简单谐波信号」。
    完。
    若 做 计数 用 峰值 小于或等于 10：
        返还 「复杂谐波信号」。
    完。
    返还 「宽带/噪声信号」。
完。

cuFFT_最优长度 接受 最小长度：
    可变 最优 为 做 cuFFT_下一个2幂 用 最小长度。
    若 最优 大于 最小长度 乘 3 除以 2：
        可变 候选 为 最小长度。
        只要 候选 取余于 2 等于 0：
            候选 为 候选 除以 2。
        完。
        只要 候选 取余于 3 等于 0：
            候选 为 候选 除以 3。
        完。
        只要 候选 取余于 5 等于 0：
            候选 为 候选 除以 5。
        完。
    完。
    返还 最优。
完。

本章 收束。
