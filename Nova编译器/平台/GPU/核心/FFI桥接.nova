《FFI桥接》

引用《..·内存·显存管理》。

引用《..·内核编译·内核编译器》。

引用《.·设备管理》。

引用《..·计算·张量计算》。

引用《..·渲染·渲染合同》。

引用《..·NVIDIA·cuDNN》。

引用《..·NVIDIA·cuFFT》。

引用《..·NVIDIA·cuSOLVER》。

引用《..·NVIDIA·cuSPARSE》。

引用《..·AMD·MIOpen》。

引用《..·AMD·rocFFT》。

引用《..·AMD·rocSOLVER》。

引用《..·AMD·rocSPARSE》。

引用《..·..·目标选择》。

引用《..·..·..·标准库·基础》。

引用《..·..·..·运行时·原语桩》。

可变 _ffi缓存 为 做 字典。

可变 _ffi函数缓存 为 做 字典。

加载动态库 接受 库名：
    缓存 为 做 字典获取 用 _ffi缓存、库名。
    若 缓存 不等于 空 并且 缓存 不等于 0：
        返还 缓存。
    完。
    句柄 为 做 加载库 用 库名。
    若 句柄 不等于 0：
        做 字典设置 用 _ffi缓存、库名、句柄。
    完。
    返还 句柄。
完。

FFI_获取函数指针 接受 库句柄、函数名：
    键 为 做 字符串拼接 用 做 转字符串 用 库句柄、做 字符串拼接 用 「:」、函数名。
    缓存 为 做 字典获取 用 _ffi函数缓存、键。
    若 缓存 不等于 空 并且 缓存 不等于 0：
        返还 缓存。
    完。
    指针 为 做 获取函数 用 库句柄、函数名。
    若 指针 不等于 0：
        做 字典设置 用 _ffi函数缓存、键、指针。
    完。
    返还 指针。
完。

外部调用 接受 库句柄、函数名、参1、_扩4、_扩5、_扩6、_扩7：
    函数指针 为 做 FFI_获取函数指针 用 库句柄、函数名。
    若 函数指针 等于 0：
        返还 0。
    完。
    返还 做 __ff指令_调用1 用 函数指针、参1。
完。

外部调用2 接受 库句柄、函数名、参1、参2：
    函数指针 为 做 FFI_获取函数指针 用 库句柄、函数名。
    若 函数指针 等于 0：
        返还 0。
    完。
    返还 做 __ff指令_调用2 用 函数指针、参1、参2。
完。

外部调用3 接受 库句柄、函数名、参1、参2、参3：
    函数指针 为 做 FFI_获取函数指针 用 库句柄、函数名。
    若 函数指针 等于 0：
        返还 0。
    完。
    返还 做 __ff指令_调用3 用 函数指针、参1、参2、参3。
完。

外部调用4 接受 库句柄、函数名、参1、参2、参3、参4、_扩7：
    函数指针 为 做 FFI_获取函数指针 用 库句柄、函数名。
    若 函数指针 等于 0：
        返还 0。
    完。
    返还 做 __ff指令_调用4 用 函数指针、参1、参2、参3、参4。
完。

外部调用5 接受 库句柄、函数名、参1、参2、参3、参4、参5：
    函数指针 为 做 FFI_获取函数指针 用 库句柄、函数名。
    若 函数指针 等于 0：
        返还 0。
    完。
    返还 做 __ff指令_调用5 用 函数指针、参1、参2、参3、参4、参5。
完。

外部调用6 接受 库句柄、函数名、参1、参2、参3、参4、参5、参6、_扩9：
    函数指针 为 做 FFI_获取函数指针 用 库句柄、函数名。
    若 函数指针 等于 0：
        返还 0。
    完。
    返还 做 __ff指令_调用6 用 函数指针、参1、参2、参3、参4、参5、参6。
完。

外部调用N 接受 库句柄、函数名、参数列表：
    函数指针 为 做 FFI_获取函数指针 用 库句柄、函数名。
    若 函数指针 等于 0：
        返还 0。
    完。
    返还 做 __ff指令_调用N 用 函数指针、参数列表。
完。

外部调用字符串 接受 库句柄、函数名、参1、参2：
    函数指针 为 做 FFI_获取函数指针 用 库句柄、函数名。
    若 函数指针 等于 0：
        返还 「」。
    完。
    ptr 为 做 __ff指令_调用2 用 函数指针、参1、参2。
    返还 做 __运行时_C串转Nova 用 ptr。
完。

系统环境 接受 变量名：
    目标 为 做 获取目标架构。
    若 目标 等于 「x86-64-win」 或者 目标 等于 「x86-64」：
        kernel32 为 做 加载动态库 用 「kernel32.dll」。
        若 kernel32 等于 0：
            返还 「」。
        完。
        返还 做 外部调用字符串 用 kernel32、「GetEnvironmentVariableA」、变量名、256。
    完。
    libc 为 做 加载动态库 用 「libc.so.6」。
    若 libc 等于 0：
        返还 「」。
    完。
    ptr 为 做 外部调用 用 libc、「getenv」、变量名、0、0、0、0。
    若 ptr 等于 0：
        返还 「」。
    完。
    返还 做 __运行时_C串转Nova 用 ptr。
完。

FFI桥_文件存在 接受 路径：
    返还 做 __运行时_文件存在 用 路径 等于 1。
完。

可变 _gpu管理器 为 空。

可变 _gpu显存 为 空。

可变 _gpu初始化完成 为 假。

GPU_初始化 接受 零参：
    若 _gpu初始化完成：
        返还 真。
    完。
    _gpu管理器 为 做 GPU管理器_创建。
    设备数 为 做 GPU管理器_枚举设备 用 _gpu管理器。
    若 设备数 等于 0：
        做 打印 用 「GPU: 未检测到GPU设备」。
        返还 假。
    完。
    做 GPU管理器_自动选择 用 _gpu管理器。
    _gpu显存 为 做 GPU显存_创建 用 _gpu管理器。
    _gpu初始化完成 为 真。
    返还 真。
完。

GPU_分配 接受 字节数：
    若 非 _gpu初始化完成：
        做 GPU_初始化。
    完。
    返还 做 GPU显存_分配 用 _gpu显存、字节数、GPU内存_设备。
完。

GPU_统一分配 接受 字节数：
    若 非 _gpu初始化完成：
        做 GPU_初始化。
    完。
    返还 做 GPU显存_分配 用 _gpu显存、字节数、GPU内存_统一。
完。

GPU_上传 接受 GPU块、CPU数据、字节数：
    若 非 _gpu初始化完成：
        做 GPU_初始化。
    完。
    返还 做 GPU显存_传输 用 _gpu显存、做 GPU内存块_指针 用 GPU块、CPU数据、字节数、GPU传输_主机到设备。
完。

GPU_下载 接受 CPU缓冲、GPU块、字节数：
    若 非 _gpu初始化完成：
        做 GPU_初始化。
    完。
    返还 做 GPU显存_传输 用 _gpu显存、CPU缓冲、做 GPU内存块_指针 用 GPU块、字节数、GPU传输_设备到主机。
完。

GPU_运行 接受 内核名、源码、grid、block、参数：
    若 非 _gpu初始化完成：
        做 GPU_初始化。
    完。
    内核 为 做 GPU_获取或编译内核 用 _gpu管理器、内核名、源码。
    若 非 做 GPU内核_就绪 用 内核：
        做 打印 用 做 字符串拼接 用 「GPU内核编译失败: 」、做 GPU内核_错误 用 内核。
        返还 假。
    完。
    返还 做 GPU内核_启动 用 内核、grid、block、参数、0、0。
完。

GPU_同步 接受 零参：
    若 非 _gpu初始化完成：
        返还。
    完。
    做 GPU同步 用 _gpu管理器。
完。

GPU_释放 接受 GPU块：
    若 非 _gpu初始化完成：
        返还。
    完。
    做 GPU显存_释放 用 _gpu显存、GPU块。
完。

GPU_矩阵乘 接受 A、B、C、M、N、K：
    若 非 _gpu初始化完成：
        做 GPU_初始化。
    完。
    张量A 为 一组：A、一组：M、K、一组：（空）、精度_FP32、0、M 乘 K、M 乘 K 乘 4。
    张量B 为 一组：B、一组：K、N、一组：（空）、精度_FP32、0、K 乘 N、K 乘 N 乘 4。
    张量C 为 一组：C、一组：M、N、一组：（空）、精度_FP32、0、M 乘 N、M 乘 N 乘 4。
    返还 做 GPU_通用矩阵乘 用 _gpu管理器、张量A、张量B、张量C、1.0、0.0、假、假。
完。

GPU_信息 接受 零参：
    若 非 _gpu初始化完成：
        做 GPU_初始化。
    完。
    做 GPU管理器_报告 用 _gpu管理器。
完。

GPU_内存信息 接受 零参：
    若 非 _gpu初始化完成：
        做 GPU_初始化。
    完。
    做 GPU显存_统计 用 _gpu显存。
完。

FFI_调用 接受 函数名、参数列表：
    分隔 为 做 字符串查找 用 函数名、「::」。
    若 分隔 小于 0：
        返还 空。
    完。
    库名 为 做 子串 用 函数名、0、分隔。
    实际函数 为 做 子串 用 函数名、分隔 加 2、做 字符串长度 用 函数名。
    库 为 做 加载动态库 用 库名。
    若 库 等于 0：
        返还 空。
    完。
    参数数 为 做 计数 用 参数列表。
    若 参数数 等于 0：
        返还 做 外部调用0 用 库、实际函数。
    完。
    若 参数数 等于 1：
        返还 做 外部调用 用 库、实际函数、参数列表 的 第 0 项、0、0、0、0。
    完。
    若 参数数 等于 2：
        返还 做 外部调用2 用 库、实际函数、参数列表 的 第 0 项、参数列表 的 第 1 项。
    完。
    若 参数数 等于 3：
        返还 做 外部调用3 用 库、实际函数、参数列表 的 第 0 项、参数列表 的 第 1 项、参数列表 的 第 2 项。
    完。
    若 参数数 等于 4：
        返还 做 外部调用4 用 库、实际函数、参数列表 的 第 0 项、参数列表 的 第 1 项、参数列表 的 第 2 项、参数列表 的 第 3 项、0。
    完。
    返还 空。
完。

本章 收束。
