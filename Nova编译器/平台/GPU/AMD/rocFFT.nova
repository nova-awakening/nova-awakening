《rocFFT》

引用《..·核心·FFI桥接》。

引用《..·核心·设备管理》。

引用《..·..·..·标准库·基础》。

引用《..·..·..·标准库·数学·信号处理》。

rocFFT_正变换 为 负 1。

rocFFT_逆变换 为 1。

rocFFT_复数交织 为 0。

rocFFT_实数 为 2。

快速傅里叶_圆周率 为 3141592653。

可变 _rocFFT库 为 0。

可变 _rocfft_可用 为 假。

可变 _rocfft_操作计数 为 0。

rocFFT_方向值 接受 方向：
    若 方向 等于 rocFFT_正变换：
        返还 0。
    完。
    返还 1。
完。

rocFFT_加载 接受 零参：
    若 _rocFFT库 不等于 0：
        返还 _rocFFT库。
    完。
    _rocFFT库 为 做 GPU_加载ROCm候选 用 一组：「rocfft.dll」、「librocfft.so」。
    若 _rocFFT库 不等于 0：
        _rocfft_可用 为 真。
    完。
    返还 _rocFFT库。
完。

rocFFT_初始化 接受 零参：
    lib 为 做 rocFFT_加载。
    若 lib 等于 0：
        返还 假。
    完。
    返还 做 外部调用 用 lib、「rocfft_setup」、0、0、0、0、0 等于 0。
完。

rocFFT_可用 接受 零参：
    返还 _rocfft_可用。
完。

rocFFT_一维变换 接受 输入、输出、N、方向：
    _rocfft_操作计数 为 _rocfft_操作计数 加 1。
    lib 为 做 rocFFT_加载。
    若 lib 等于 0：
        返还 做 CPU_快速傅里叶 用 输入、N、方向。
    完。
    plan 为 做 外部调用N 用 lib、「rocfft_plan_create」、一组：0、0、做 rocFFT_方向值 用 方向、0、1、一组：N、1、0。
    info 为 做 外部调用 用 lib、「rocfft_execution_info_create」、0、0、0、0、0。
    结果 为 做 外部调用5 用 lib、「rocfft_execute」、plan、一组：输入、一组：输出、info、0。
    做 外部调用 用 lib、「rocfft_plan_destroy」、plan、0、0、0、0。
    做 外部调用 用 lib、「rocfft_execution_info_destroy」、info、0、0、0、0。
    返还 结果 等于 0。
完。

rocFFT_二维变换 接受 输入、输出、NX、NY、方向：
    _rocfft_操作计数 为 _rocfft_操作计数 加 1。
    lib 为 做 rocFFT_加载。
    若 lib 等于 0：
        返还 假。
    完。
    plan 为 做 外部调用N 用 lib、「rocfft_plan_create」、一组：0、0、做 rocFFT_方向值 用 方向、0、2、一组：NX、NY、1、0。
    info 为 做 外部调用 用 lib、「rocfft_execution_info_create」、0、0、0、0、0。
    结果 为 做 外部调用5 用 lib、「rocfft_execute」、plan、一组：输入、一组：输出、info、0。
    做 外部调用 用 lib、「rocfft_plan_destroy」、plan、0、0、0、0。
    做 外部调用 用 lib、「rocfft_execution_info_destroy」、info、0、0、0、0。
    返还 结果 等于 0。
完。

rocFFT_清理 接受 零参：
    lib 为 做 rocFFT_加载。
    若 lib 不等于 0：
        做 外部调用 用 lib、「rocfft_cleanup」、0、0、0、0、0。
    完。
完。

CPU_快速傅里叶 接受 数据、N、方向：
    若 N 小于或等于 1：
        返还 数据。
    完。
    若 N 取余于 2 不等于 0：
        返还 做 CPU_离散傅里叶 用 数据、N、方向。
    完。
    可变 偶实 为 一组：（空）。
    可变 偶虚 为 一组：（空）。
    可变 奇实 为 一组：（空）。
    可变 奇虚 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        若 i 取余于 2 等于 0：
            做 追加 用 偶实、数据 的 第 i 乘 2 项。
            做 追加 用 偶虚、数据 的 第 i 乘 2 加 1 项。
        否则：
            做 追加 用 奇实、数据 的 第 i 乘 2 项。
            做 追加 用 奇虚、数据 的 第 i 乘 2 加 1 项。
        完。
        i 为 i 加 1。
    完。
    可变 偶数据 为 一组：（空）。
    i 为 0。
    只要 i 小于 N 除以 2：
        做 追加 用 偶数据、偶实 的 第 i 项。
        做 追加 用 偶数据、偶虚 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 奇数据 为 一组：（空）。
    i 为 0。
    只要 i 小于 N 除以 2：
        做 追加 用 奇数据、奇实 的 第 i 项。
        做 追加 用 奇数据、奇虚 的 第 i 项。
        i 为 i 加 1。
    完。
    偶数据 为 做 CPU_快速傅里叶 用 偶数据、N 除以 2、方向。
    奇数据 为 做 CPU_快速傅里叶 用 奇数据、N 除以 2、方向。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 N 乘 2：
        做 追加 用 结果、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 N 除以 2：
        角度 为 方向 乘 2 乘 快速傅里叶_圆周率 乘 i 除以 (N 乘 1000000000)。
        cos_v 为 做 FFT_余弦近似 用 角度。
        sin_v 为 做 FFT_正弦近似 用 角度。
        t实 为 cos_v 乘 奇数据 的 第 i 乘 2 项 减 sin_v 乘 奇数据 的 第 i 乘 2 加 1 项。
        t虚 为 cos_v 乘 奇数据 的 第 i 乘 2 加 1 项 加 sin_v 乘 奇数据 的 第 i 乘 2 项。
        结果 的 第 i 乘 2 项 为 偶数据 的 第 i 乘 2 项 加 t实。
        结果 的 第 i 乘 2 加 1 项 为 偶数据 的 第 i 乘 2 加 1 项 加 t虚。
        结果 的 第 (i 加 N 除以 2) 乘 2 项 为 偶数据 的 第 i 乘 2 项 减 t实。
        结果 的 第 (i 加 N 除以 2) 乘 2 加 1 项 为 偶数据 的 第 i 乘 2 加 1 项 减 t虚。
        i 为 i 加 1。
    完。
    返还 结果。
完。

CPU_离散傅里叶 接受 数据、N、方向：
    可变 结果 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 N：
        可变 实 为 0。
        可变 虚 为 0。
        可变 n 为 0。
        只要 n 小于 N：
            角度 为 方向 乘 2 乘 快速傅里叶_圆周率 乘 k 乘 n 除以 (N 乘 1000000000)。
            cos_v 为 做 FFT_余弦近似 用 角度。
            sin_v 为 做 FFT_正弦近似 用 角度。
            实 为 实 加 数据 的 第 n 乘 2 项 乘 cos_v 减 数据 的 第 n 乘 2 加 1 项 乘 sin_v。
            虚 为 虚 加 数据 的 第 n 乘 2 项 乘 sin_v 加 数据 的 第 n 乘 2 加 1 项 乘 cos_v。
            n 为 n 加 1。
        完。
        做 追加 用 结果、实。
        做 追加 用 结果、虚。
        k 为 k 加 1。
    完。
    返还 结果。
完。

FFT_余弦近似 接受 x：
    只要 x 大于 快速傅里叶_圆周率：
        x 为 x 减 2 乘 快速傅里叶_圆周率。
    完。
    只要 x 小于 0 减 快速傅里叶_圆周率：
        x 为 x 加 2 乘 快速傅里叶_圆周率。
    完。
    x2 为 x 乘 x 除以 1000000000 除以 1000000000。
    返还 1000000000 减 x2 乘 500000000 加 x2 乘 x2 乘 41666667 除以 1000000000。
完。

FFT_正弦近似 接受 x：
    只要 x 大于 快速傅里叶_圆周率：
        x 为 x 减 2 乘 快速傅里叶_圆周率。
    完。
    只要 x 小于 0 减 快速傅里叶_圆周率：
        x 为 x 加 2 乘 快速傅里叶_圆周率。
    完。
    x2 为 x 乘 x 除以 1000000000 除以 1000000000。
    返还 x 减 x 乘 x2 乘 166666667 除以 1000000000 加 x 乘 x2 乘 x2 乘 8333333 除以 1000000000 除以 1000000000。
完。

窗_汉宁 接受 N：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        角度 为 2 乘 快速傅里叶_圆周率 乘 i 除以 ((N 减 1) 乘 1000000000)。
        值 为 500000000 减 500000000 乘 做 FFT_余弦近似 用 角度 除以 1000000000。
        做 追加 用 结果、值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

窗_汉明 接受 N：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        角度 为 2 乘 快速傅里叶_圆周率 乘 i 除以 ((N 减 1) 乘 1000000000)。
        值 为 538000000 减 462000000 乘 做 FFT_余弦近似 用 角度 除以 1000000000。
        做 追加 用 结果、值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

窗_矩形 接受 N：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        做 追加 用 结果、1000000000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

窗_三角 接受 N：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        可变 值 为 0。
        若 i 小于或等于 N 除以 2：
            值 为 2 乘 i 乘 1000000000 除以 (N 减 1)。
        否则：
            值 为 2 乘 (N 减 1 减 i) 乘 1000000000 除以 (N 减 1)。
        完。
        做 追加 用 结果、值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

应用窗函数 接受 数据、窗、N：
    可变 i 为 0。
    只要 i 小于 N：
        数据 的 第 i 乘 2 项 为 数据 的 第 i 乘 2 项 乘 窗 的 第 i 项 除以 1000000000。
        数据 的 第 i 乘 2 加 1 项 为 数据 的 第 i 乘 2 加 1 项 乘 窗 的 第 i 项 除以 1000000000。
        i 为 i 加 1。
    完。
完。

rocFFT_频谱_幅度 接受 fft数据、N：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        实 为 fft数据 的 第 i 乘 2 项。
        虚 为 fft数据 的 第 i 乘 2 加 1 项。
        幅 为 实 乘 实 加 虚 乘 虚。
        做 追加 用 结果、幅。
        i 为 i 加 1。
    完。
    返还 结果。
完。

频谱_功率 接受 fft数据、N：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        实 为 fft数据 的 第 i 乘 2 项。
        虚 为 fft数据 的 第 i 乘 2 加 1 项。
        功率 为 (实 乘 实 加 虚 乘 虚) 除以 N。
        做 追加 用 结果、功率。
        i 为 i 加 1。
    完。
    返还 结果。
完。

频谱_峰值 接受 幅度、N：
    可变 最大值 为 0。
    可变 最大索引 为 0。
    可变 i 为 1。
    只要 i 小于 N 除以 2：
        若 幅度 的 第 i 项 大于 最大值：
            最大值 为 幅度 的 第 i 项。
            最大索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 一组：最大索引、最大值。
完。

频谱_主频率 接受 幅度、N、采样率：
    峰 为 做 频谱_峰值 用 幅度、N。
    若 N 等于 0：
        返还 0。
    完。
    返还 峰 的 第 0 项 乘 采样率 除以 N。
完。

信号_正弦波 接受 N、频率、采样率、幅度：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        角度 为 2 乘 快速傅里叶_圆周率 乘 频率 乘 i 除以 (采样率 乘 1000000000)。
        值 为 幅度 乘 做 FFT_正弦近似 用 角度 除以 1000000000。
        做 追加 用 数据、值。
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 数据。
完。

信号_方波 接受 N、频率、采样率、幅度：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        角度 为 2 乘 快速傅里叶_圆周率 乘 频率 乘 i 除以 (采样率 乘 1000000000)。
        可变 值 为 幅度。
        若 做 FFT_正弦近似 用 角度 小于 0：
            值 为 0 减 幅度。
        完。
        做 追加 用 数据、值。
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 数据。
完。

信号_白噪声 接受 N、幅度：
    可变 数据 为 一组：（空）。
    可变 种子 为 42。
    可变 i 为 0。
    只要 i 小于 N：
        种子 为 (种子 乘 1103515245 加 12345) 取余于 2147483648。
        若 种子 小于 0：
            种子 为 0 减 种子。
        完。
        值 为 种子 取余于 (2 乘 幅度) 减 幅度。
        做 追加 用 数据、值。
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 数据。
完。

线性卷积 接受 信号、核、信号长、核长：
    可变 结果 为 一组：（空）。
    结果长 为 信号长 加 核长 减 1。
    可变 i 为 0。
    只要 i 小于 结果长：
        做 追加 用 结果、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 信号长：
        可变 j 为 0。
        只要 j 小于 核长：
            结果 的 第 i 加 j 项 为 结果 的 第 i 加 j 项 加 信号 的 第 i 项 乘 核 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

FFT_策略选择 接受 N：
    若 N 小于或等于 16：
        返还 「DFT直接计算」。
    完。
    可变 n 为 N。
    可变 是2幂 为 真。
    只要 n 大于 1：
        若 n 取余于 2 不等于 0：
            是2幂 为 假。
        完。
        n 为 n 除以 2。
    完。
    若 是2幂：
        返还 「Cooley-Tukey基2 FFT」。
    完。
    若 N 取余于 3 等于 0：
        返还 「混合基FFT(含基3)」。
    完。
    返还 「Bluestein算法(任意长度)」。
完。

FFT_性能预测 接受 N、使用GPU：
    可变 运算量 为 N 乘 5。
    可变 t 为 N。
    只要 t 大于 1：
        运算量 为 运算量 加 N。
        t 为 t 除以 2。
    完。
    若 使用GPU：
        若 N 小于 256：
            返还 「GPU开销大于收益」。
        完。
        若 N 小于 65536：
            返还 「GPU中等加速(约5-20x)」。
        完。
        返还 「GPU显著加速(约50-200x)」。
    完。
    若 N 大于 100000：
        返还 「建议GPU加速」。
    完。
    返还 「CPU处理可接受」。
完。

窗函数_推荐 接受 应用场景：
    若 应用场景 等于 「频率测量」：
        返还 「矩形窗(最佳频率分辨率)」。
    完。
    若 应用场景 等于 「通用分析」：
        返还 「汉宁窗(泄漏抑制好)」。
    完。
    若 应用场景 等于 「幅度精确」：
        返还 「平顶窗(幅度精度最高)」。
    完。
    若 应用场景 等于 「噪声分析」：
        返还 「汉明窗(旁瓣抑制)」。
    完。
    返还 「汉宁窗(通用推荐)」。
完。

FFT_互相关 接受 信号1、信号2、N：
    可变 结果 为 一组：（空）。
    可变 延迟 为 0 减 N 加 1。
    只要 延迟 小于 N：
        可变 和 为 0。
        可变 i 为 0。
        只要 i 小于 N：
            j 为 i 加 延迟。
            若 j 大于或等于 0 并且 j 小于 N：
                和 为 和 加 信号1 的 第 i 乘 2 项 乘 信号2 的 第 j 乘 2 项 加 信号1 的 第 i 乘 2 加 1 项 乘 信号2 的 第 j 乘 2 加 1 项。
            完。
            i 为 i 加 1。
        完。
        做 追加 用 结果、和。
        延迟 为 延迟 加 1。
    完。
    返还 结果。
完。

FFT_自相关 接受 信号、N：
    返还 做 互相关 用 信号、信号、N。
完。

FFT_低通滤波 接受 数据、N、截止频率索引：
    可变 i 为 截止频率索引。
    只要 i 小于 N 减 截止频率索引：
        数据 的 第 i 乘 2 项 为 0。
        数据 的 第 i 乘 2 加 1 项 为 0。
        i 为 i 加 1。
    完。
完。

FFT_高通滤波 接受 数据、N、截止频率索引：
    可变 i 为 0。
    只要 i 小于 截止频率索引：
        数据 的 第 i 乘 2 项 为 0。
        数据 的 第 i 乘 2 加 1 项 为 0。
        i 为 i 加 1。
    完。
    i 为 N 减 截止频率索引。
    只要 i 小于 N：
        数据 的 第 i 乘 2 项 为 0。
        数据 的 第 i 乘 2 加 1 项 为 0。
        i 为 i 加 1。
    完。
完。

带通滤波 接受 数据、N、低截止、高截止：
    可变 i 为 0。
    只要 i 小于 N：
        若 i 小于 低截止 或者 i 大于 高截止 并且 i 小于 N 减 高截止 或者 i 大于或等于 N 减 低截止：
            数据 的 第 i 乘 2 项 为 0。
            数据 的 第 i 乘 2 加 1 项 为 0。
        完。
        i 为 i 加 1。
    完。
完。

rocF_移动平均 接受 数据、N、窗口大小：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        可变 和 为 0。
        可变 计 为 0。
        可变 j 为 i 减 窗口大小 除以 2。
        只要 j 小于或等于 i 加 窗口大小 除以 2：
            若 j 大于或等于 0 并且 j 小于 N：
                和 为 和 加 数据 的 第 j 项。
                计 为 计 加 1。
            完。
            j 为 j 加 1。
        完。
        若 计 大于 0：
            做 追加 用 结果、和 除以 计。
        否则：
            做 追加 用 结果、0。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

FFT_信号_均值 接受 数据、N：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 N：
        和 为 和 加 数据 的 第 i 乘 2 项。
        i 为 i 加 1。
    完。
    若 N 等于 0：
        返还 0。
    完。
    返还 和 除以 N。
完。

FFT_信号_能量 接受 数据、N：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 N：
        和 为 和 加 数据 的 第 i 乘 2 项 乘 数据 的 第 i 乘 2 项 加 数据 的 第 i 乘 2 加 1 项 乘 数据 的 第 i 乘 2 加 1 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

信号_峰峰值 接受 数据、N：
    可变 最大 为 数据 的 第 0 项。
    可变 最小 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 N：
        若 数据 的 第 i 乘 2 项 大于 最大：
            最大 为 数据 的 第 i 乘 2 项。
        完。
        若 数据 的 第 i 乘 2 项 小于 最小：
            最小 为 数据 的 第 i 乘 2 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大 减 最小。
完。

rocF_信号_过零率 接受 数据、N：
    可变 过零 为 0。
    可变 i 为 1。
    只要 i 小于 N：
        可变 发生过零 为 假。
        若 数据 的 第 (i 减 1) 乘 2 项 大于或等于 0 并且 数据 的 第 i 乘 2 项 小于 0：
            发生过零 为 真。
        完。
        若 数据 的 第 (i 减 1) 乘 2 项 小于 0 并且 数据 的 第 i 乘 2 项 大于或等于 0：
            发生过零 为 真。
        完。
        若 发生过零：
            过零 为 过零 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 过零。
完。

信号_质量评估 接受 数据、N：
    能量 为 做 信号_能量 用 数据、N。
    峰峰 为 做 信号_峰峰值 用 数据、N。
    过零 为 做 信号_过零率 用 数据、N。
    可变 评分 为 100。
    若 能量 等于 0：
        返还 一组：0、「空信号」。
    完。
    若 峰峰 等于 0：
        返还 一组：10、「直流信号」。
    完。
    若 过零 大于 N 除以 2：
        评分 为 评分 减 10。
    完。
    返还 一组：评分、「正常信号」。
完。

FFT_下一个2幂 接受 n：
    可变 p 为 1。
    只要 p 小于 n：
        p 为 p 乘 2。
    完。
    返还 p。
完。

FFT_是2幂 接受 n：
    若 n 小于或等于 0：
        返还 假。
    完。
    若 n 等于 1：
        返还 真。
    完。
    返还 n 取余于 2 等于 0 并且 n 除以 2 乘 2 等于 n。
完。

本章 收束。
