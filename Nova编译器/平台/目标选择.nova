《目标选择》

引用《.·代码生成·arm64·arm64编码器》。

引用《..·标准库·基础》。

引用《..·编译器·规划·代码生成·RISC_V·指令编码》。

引用《..·编译器·规划·代码生成·arm64·ABI调用约定》。

引用《..·编译器·规划·代码生成·RISC_V·ABI调用约定》。

引用《..·编译器·规划·代码生成·wasm·wasm编码》。

引用《.·代码生成·riscv64·riscv编码器》。

目标_x86_64 为 1。

目标_arm64 为 2。

目标_riscv64 为 3。

目标_wasm 为 4。

目标_loongarch 为 5。

可变 当前目标 为 目标_x86_64。

设置目标架构 接受 目标：
    若 目标 大于或等于 目标_x86_64 并且 目标 小于或等于 目标_loongarch：
        当前目标 为 目标。
    完。
完。

获取目标架构 接受 零参：
    返还 当前目标。
完。

目标_指令宽度 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 0。
    完。
    若 当前目标 等于 目标_arm64：
        返还 4。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 4。
    完。
    若 当前目标 等于 目标_wasm：
        返还 0。
    完。
    若 当前目标 等于 目标_loongarch：
        返还 4。
    完。
    返还 0。
完。

目标_寄存器数 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 16。
    完。
    若 当前目标 等于 目标_arm64：
        返还 31。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 32。
    完。
    若 当前目标 等于 目标_wasm：
        返还 0。
    完。
    若 当前目标 等于 目标_loongarch：
        返还 32。
    完。
    返还 0。
完。

目标_名称 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 「x86-64」。
    完。
    若 当前目标 等于 目标_arm64：
        返还 「arm64」。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 「risc-v」。
    完。
    若 当前目标 等于 目标_wasm：
        返还 「wasm」。
    完。
    若 当前目标 等于 目标_loongarch：
        返还 「loongarch」。
    完。
    返还 「unknown」。
完。

目标_字节序 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 「小端」。
    完。
    若 当前目标 等于 目标_arm64：
        返还 「小端」。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 「小端」。
    完。
    若 当前目标 等于 目标_wasm：
        返还 「小端」。
    完。
    若 当前目标 等于 目标_loongarch：
        返还 「小端」。
    完。
    返还 「小端」。
完。

目标_指针大小 接受 零参：
    若 当前目标 等于 目标_wasm：
        返还 4。
    完。
    返还 8。
完。

目标_对齐 接受 大小：
    若 大小 小于或等于 1：
        返还 1。
    完。
    若 大小 小于或等于 2：
        返还 2。
    完。
    若 大小 小于或等于 4：
        返还 4。
    完。
    返还 8。
完。

目标_栈对齐 接受 _扩1：
    若 当前目标 等于 目标_x86_64：
        返还 16。
    完。
    若 当前目标 等于 目标_arm64：
        返还 16。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 16。
    完。
    返还 16。
完。

目标_参数寄存器数 接受 _扩1：
    若 当前目标 等于 目标_x86_64：
        返还 6。
    完。
    若 当前目标 等于 目标_arm64：
        返还 8。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 8。
    完。
    若 当前目标 等于 目标_wasm：
        返还 0。
    完。
    若 当前目标 等于 目标_loongarch：
        返还 8。
    完。
    返还 0。
完。

目标_返回寄存器数 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 2。
    完。
    若 当前目标 等于 目标_arm64：
        返还 8。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 2。
    完。
    返还 1。
完。

目标_被调用者保存寄存器数 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 6。
    完。
    若 当前目标 等于 目标_arm64：
        返还 10。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 12。
    完。
    返还 0。
完。

目标_调用约定名 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 「System V AMD64 ABI」。
    完。
    若 当前目标 等于 目标_arm64：
        返还 「AAPCS64」。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 「RISC-V LP64」。
    完。
    若 当前目标 等于 目标_wasm：
        返还 「WASM 栈式」。
    完。
    若 当前目标 等于 目标_loongarch：
        返还 「LoongArch LP64」。
    完。
    返还 「unknown」。
完。

目标_浮点寄存器数 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 16。
    完。
    若 当前目标 等于 目标_arm64：
        返还 32。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 32。
    完。
    若 当前目标 等于 目标_wasm：
        返还 0。
    完。
    若 当前目标 等于 目标_loongarch：
        返还 32。
    完。
    返还 0。
完。

目标_支持SIMD 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 真。
    完。
    若 当前目标 等于 目标_arm64：
        返还 真。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 假。
    完。
    若 当前目标 等于 目标_wasm：
        返还 真。
    完。
    返还 假。
完。

目标_SIMD宽度 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 256。
    完。
    若 当前目标 等于 目标_arm64：
        返还 128。
    完。
    若 当前目标 等于 目标_wasm：
        返还 128。
    完。
    返还 0。
完。

X0寄存器 为 0。

X1 为 1。

X2 为 2。

X3 为 3。

X4寄存器 为 4。

X5 为 5。

X6 为 6。

X7 为 7。

X8寄存器 为 8。

X9 为 9。

X10 为 10。

X11 为 11。

X12寄存器 为 12。

X13 为 13。

X14 为 14。

X15 为 15。

X16寄存器 为 16。

X17 为 17。

X18 为 18。

X19 为 19。

X20寄存器 为 20。

X21 为 21。

X22 为 22。

X23 为 23。

X24寄存器 为 24。

X25 为 25。

X26 为 26。

X27 为 27。

X28寄存器 为 28。

X29 为 29。

X30 为 30。

零寄存器 为 31。

SP 为 31。

FP 为 29。

LR 为 30。

条件_EQ 为 0。

条件_NE 为 1。

条件_CS 为 2。

条件_CC 为 3。

条件_MI 为 4。

条件_PL 为 5。

条件_VS 为 6。

条件_VC 为 7。

条件_HI 为 8。

条件_LS 为 9。

条件_GE 为 10。

条件_LT 为 11。

条件_GT 为 12。

条件_LE 为 13。

条件_AL 为 14。

RV_零 为 0。

RV_RA 为 1。

RV_SP 为 2。

RV_GP 为 3。

RV_线程指针 为 4。

RV_临时0 为 5。

RV_临时1 为 6。

RV_临时2 为 7。

RV_保存0 为 8。

RV_保存1 为 9。

RV_参数0 为 10。

RV_参数1 为 11。

RV_参数2 为 12。

RV_参数3 为 13。

RV_参数4 为 14。

RV_参数5 为 15。

RV_参数6 为 16。

RV_参数7 为 17。

RV_S2 为 18。

RV_S3 为 19。

RV_保存4 为 20。

RV_S5 为 21。

RV_S6 为 22。

RV_S7 为 23。

RV_保存8 为 24。

RV_S9 为 25。

RV_S10 为 26。

RV_S11 为 27。

RV_临时3 为 28。

RV_T4 为 29。

RV_T5 为 30。

RV_T6 为 31。

目标_支持原子操作 接受 零参：
    若 当前目标 等于 目标_wasm：
        返还 真。
    完。
    返还 真。
完。

目标_支持条件移动 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 真。
    完。
    若 当前目标 等于 目标_arm64：
        返还 真。
    完。
    返还 假。
完。

目标_支持未对齐访问 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 真。
    完。
    若 当前目标 等于 目标_arm64：
        返还 真。
    完。
    返还 假。
完。

目标_支持硬件除法 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 真。
    完。
    若 当前目标 等于 目标_arm64：
        返还 真。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 真。
    完。
    返还 假。
完。

目标_缓存行大小 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 64。
    完。
    若 当前目标 等于 目标_arm64：
        返还 64。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 64。
    完。
    返还 64。
完。

目标_输出格式 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 「ELF64」。
    完。
    若 当前目标 等于 目标_arm64：
        返还 「ELF64」。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 「ELF64」。
    完。
    若 当前目标 等于 目标_wasm：
        返还 「WASM」。
    完。
    若 当前目标 等于 目标_loongarch：
        返还 「ELF64」。
    完。
    返还 「ELF64」。
完。

目标_ELF机器号 接受 _扩1：
    若 当前目标 等于 目标_x86_64：
        返还 62。
    完。
    若 当前目标 等于 目标_arm64：
        返还 183。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 243。
    完。
    若 当前目标 等于 目标_loongarch：
        返还 258。
    完。
    返还 0。
完。

目标_自动选择 接受 工作负载：
    若 工作负载 等于 「高性能计算」：
        返还 目标_x86_64。
    完。
    若 工作负载 等于 「移动端」：
        返还 目标_arm64。
    完。
    若 工作负载 等于 「嵌入式」：
        返还 目标_riscv64。
    完。
    若 工作负载 等于 「浏览器」：
        返还 目标_wasm。
    完。
    若 工作负载 等于 「国产化」：
        返还 目标_loongarch。
    完。
    返还 目标_x86_64。
完。

目标_推荐理由 接受 目标：
    若 目标 等于 目标_x86_64：
        返还 「最广泛的桌面/服务器支持,丰富指令集,成熟工具链」。
    完。
    若 目标 等于 目标_arm64：
        返还 「低功耗高性能,移动/嵌入式/Mac首选,固定宽度指令」。
    完。
    若 目标 等于 目标_riscv64：
        返还 「开源指令集,灵活可扩展,新兴生态」。
    完。
    若 目标 等于 目标_wasm：
        返还 「跨平台沙箱环境,浏览器原生支持,安全隔离」。
    完。
    若 目标 等于 目标_loongarch：
        返还 「国产自主指令集,龙芯处理器,信创首选」。
    完。
    返还 「未知目标」。
完。

目标_性能预测 接受 操作类型：
    若 操作类型 等于 「整数运算」：
        若 当前目标 等于 目标_x86_64：
            返还 100。
        完。
        若 当前目标 等于 目标_arm64：
            返还 95。
        完。
        若 当前目标 等于 目标_riscv64：
            返还 85。
        完。
        若 当前目标 等于 目标_wasm：
            返还 60。
        完。
        返还 80。
    完。
    若 操作类型 等于 「浮点运算」：
        若 当前目标 等于 目标_x86_64：
            返还 100。
        完。
        若 当前目标 等于 目标_arm64：
            返还 90。
        完。
        若 当前目标 等于 目标_riscv64：
            返还 75。
        完。
        若 当前目标 等于 目标_wasm：
            返还 55。
        完。
        返还 70。
    完。
    若 操作类型 等于 「内存访问」：
        若 当前目标 等于 目标_x86_64：
            返还 100。
        完。
        若 当前目标 等于 目标_arm64：
            返还 90。
        完。
        若 当前目标 等于 目标_riscv64：
            返还 80。
        完。
        若 当前目标 等于 目标_wasm：
            返还 70。
        完。
        返还 75。
    完。
    返还 50。
完。

目标_优化建议 接受 零参：
    可变 建议 为 一组：（空）。
    若 当前目标 等于 目标_x86_64：
        做 追加 用 建议、「利用SIMD(SSE/AVX)加速向量运算」。
        做 追加 用 建议、「使用CMOVcc避免分支预测失败」。
        做 追加 用 建议、「注意REX前缀对代码密度的影响」。
    完。
    若 当前目标 等于 目标_arm64：
        做 追加 用 建议、「利用条件选择指令减少分支」。
        做 追加 用 建议、「NEON向量化循环操作」。
        做 追加 用 建议、「注意LDR/STR的偏移范围限制」。
    完。
    若 当前目标 等于 目标_riscv64：
        做 追加 用 建议、「利用压缩指令(C扩展)提高代码密度」。
        做 追加 用 建议、「检查目标是否支持M/A/F/D扩展」。
    完。
    若 当前目标 等于 目标_wasm：
        做 追加 用 建议、「最小化内存使用减少传输大小」。
        做 追加 用 建议、「利用SIMD128加速数值计算」。
    完。
    返还 建议。
完。

目标_兼容 接受 源目标、目的目标：
    若 源目标 等于 目的目标：
        返还 100。
    完。
    若 源目标 等于 目标_x86_64 并且 目的目标 等于 目标_wasm：
        返还 70。
    完。
    若 源目标 等于 目标_arm64 并且 目的目标 等于 目标_wasm：
        返还 70。
    完。
    若 源目标 等于 目标_x86_64 并且 目的目标 等于 目标_arm64：
        返还 50。
    完。
    若 源目标 等于 目标_arm64 并且 目的目标 等于 目标_x86_64：
        返还 50。
    完。
    返还 30。
完。

目标_信息摘要 接受 零参：
    返还 一组：做 目标_名称、做 目标_寄存器数、做 目标_指令宽度、做 目标_指针大小、做 目标_参数寄存器数 用 0、做 目标_调用约定名。
完。

目标_重定位_绝对 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 1。
    完。
    若 当前目标 等于 目标_arm64：
        返还 257。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 1。
    完。
    返还 1。
完。

目标_重定位_相对 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 2。
    完。
    若 当前目标 等于 目标_arm64：
        返还 261。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 2。
    完。
    返还 2。
完。

目标_重定位_PLT 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 4。
    完。
    若 当前目标 等于 目标_arm64：
        返还 290。
    完。
    返还 4。
完。

目标_重定位_GOT 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 9。
    完。
    若 当前目标 等于 目标_arm64：
        返还 311。
    完。
    返还 9。
完。

目标_最大分支距离 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 2147483647。
    完。
    若 当前目标 等于 目标_arm64：
        返还 134217727。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 1048575。
    完。
    若 当前目标 等于 目标_wasm：
        返还 2147483647。
    完。
    返还 2147483647。
完。

目标_类型大小 接受 类型：
    若 类型 等于 「i8」 或者 类型 等于 「u8」 或者 类型 等于 「布尔」：
        返还 1。
    完。
    若 类型 等于 「i16」 或者 类型 等于 「u16」：
        返还 2。
    完。
    若 类型 等于 「i32」 或者 类型 等于 「u32」 或者 类型 等于 「f32」：
        返还 4。
    完。
    若 类型 等于 「i64」 或者 类型 等于 「u64」 或者 类型 等于 「f64」：
        返还 8。
    完。
    若 类型 等于 「指针」：
        返还 做 目标_指针大小。
    完。
    返还 8。
完。

目标_类型对齐 接受 类型：
    大小 为 做 目标_类型大小 用 类型。
    返还 做 目标_对齐 用 大小。
完。

目标_系统调用号寄存器 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 寄_RAX。
    完。
    若 当前目标 等于 目标_arm64：
        返还 X8。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 RV_参数7。
    完。
    返还 0。
完。

目标_系统调用参数寄存器 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 一组：寄_RDI、寄_RSI、寄_RDX、寄_R10、寄_R8、寄_R9。
    完。
    若 当前目标 等于 目标_arm64：
        返还 一组：X0、X1、X2、X3、X4、X5。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 一组：RV_参数0、RV_参数1、RV_参数2、RV_参数3、RV_参数4、RV_参数5。
    完。
    返还 一组：（空）。
完。

目标_系统调用返回寄存器 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 寄_RAX。
    完。
    若 当前目标 等于 目标_arm64：
        返还 X0。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 RV_参数0。
    完。
    返还 0。
完。

目标_动态链接器 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 「/lib64/ld-linux-x86-64.so.2」。
    完。
    若 当前目标 等于 目标_arm64：
        返还 「/lib/ld-linux-aarch64.so.1」。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 「/lib/ld-linux-riscv64-lp64d.so.1」。
    完。
    若 当前目标 等于 目标_loongarch：
        返还 「/lib64/ld-linux-loongarch-lp64d.so.1」。
    完。
    返还 「」。
完。

目标_入口点名 接受 零参：
    返还 「_start」。
完。

目标_默认基地址 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 4194304。
    完。
    若 当前目标 等于 目标_arm64：
        返还 4194304。
    完。
    返还 4194304。
完。

目标_页大小 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 4096。
    完。
    若 当前目标 等于 目标_arm64：
        返还 65536。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 4096。
    完。
    返还 4096。
完。

目标_寄存器名 接受 索引：
    若 当前目标 等于 目标_x86_64：
        可变 名 为 一组：「rax」、「rcx」、「rdx」、「rbx」、「rsp」、「rbp」、「rsi」、「rdi」、「r8」、「r9」、「r10」、「r11」、「r12」、「r13」、「r14」、「r15」。
        若 索引 大于或等于 0 并且 索引 小于 16：
            返还 名 的 第 索引 项。
        完。
        返还 「???」。
    完。
    若 当前目标 等于 目标_arm64：
        若 索引 等于 31：
            返还 「sp/xzr」。
        完。
        若 索引 等于 29：
            返还 「fp」。
        完。
        若 索引 等于 30：
            返还 「lr」。
        完。
        返还 「x」 加 做 转字符串 用 索引。
    完。
    若 当前目标 等于 目标_riscv64：
        可变 名 为 一组：「zero」、「ra」、「sp」、「gp」、「tp」、「t0」、「t1」、「t2」、「s0」、「s1」、「a0」、「a1」、「a2」、「a3」、「a4」、「a5」、「a6」、「a7」、「s2」、「s3」、「s4」、「s5」、「s6」、「s7」、「s8」、「s9」、「s10」、「s11」、「t3」、「t4」、「t5」、「t6」。
        若 索引 大于或等于 0 并且 索引 小于 32：
            返还 名 的 第 索引 项。
        完。
        返还 「???」。
    完。
    返还 「r」 加 做 转字符串 用 索引。
完。

目标_代码大小估算 接受 IR指令数：
    若 当前目标 等于 目标_x86_64：
        返还 IR指令数 乘 5。
    完。
    若 当前目标 等于 目标_arm64：
        返还 IR指令数 乘 4。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 IR指令数 乘 6。
    完。
    若 当前目标 等于 目标_wasm：
        返还 IR指令数 乘 3。
    完。
    返还 IR指令数 乘 5。
完。

目标_编译复杂度 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 「高(变长指令+复杂编码)」。
    完。
    若 当前目标 等于 目标_arm64：
        返还 「中(固定宽度但位域复杂)」。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 「低(简洁正交设计)」。
    完。
    若 当前目标 等于 目标_wasm：
        返还 「低(栈式虚拟机)」。
    完。
    返还 「中」。
完。

目标_可用扩展 接受 零参：
    若 当前目标 等于 目标_x86_64：
        返还 一组：「SSE2」、「SSE4.1」、「AVX」、「AVX2」、「BMI1」、「BMI2」、「POPCNT」、「LZCNT」。
    完。
    若 当前目标 等于 目标_arm64：
        返还 一组：「NEON」、「CRC32」、「AES」、「SHA1」、「SHA256」、「ATOMICS」。
    完。
    若 当前目标 等于 目标_riscv64：
        返还 一组：「M(乘除)」、「A(原子)」、「F(单精度)」、「D(双精度)」、「C(压缩)」。
    完。
    若 当前目标 等于 目标_wasm：
        返还 一组：「SIMD128」、「多值返回」、「尾调用」、「异常处理」。
    完。
    返还 一组：（空）。
完。

目标_批量编译计划 接受 零参：
    返还 一组：一组：目标_x86_64、「Linux/Windows 桌面服务器」、一组：目标_arm64、「macOS/Linux ARM 移动嵌入」、一组：目标_riscv64、「新兴开源硬件」、一组：目标_wasm、「浏览器/边缘计算」、一组：目标_loongarch、「国产信创平台」。
完。

目标_编译优先级 接受 场景：
    若 场景 等于 「桌面应用」：
        返还 一组：目标_x86_64、目标_arm64、目标_wasm。
    完。
    若 场景 等于 「服务器」：
        返还 一组：目标_x86_64、目标_arm64、目标_loongarch。
    完。
    若 场景 等于 「全平台」：
        返还 一组：目标_x86_64、目标_arm64、目标_riscv64、目标_wasm、目标_loongarch。
    完。
    返还 一组：目标_x86_64。
完。

目标_ABI合同报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「目标」、做 目标_名称。
    做 字典设 用 r、「指针大小」、做 转字符串 用 做 目标_指针大小。
    做 字典设 用 r、「字节序」、做 目标_字节序。
    做 字典设 用 r、「栈对齐」、做 转字符串 用 做 目标_栈对齐 用 0。
    做 字典设 用 r、「参数寄存器数」、做 转字符串 用 做 目标_参数寄存器数 用 0。
    做 字典设 用 r、「返回寄存器数」、做 转字符串 用 做 目标_返回寄存器数。
    做 字典设 用 r、「被调用者保存」、做 转字符串 用 做 目标_被调用者保存寄存器数。
    做 字典设 用 r、「调用约定」、做 目标_调用约定名。
    做 字典设 用 r、「输出格式」、做 目标_输出格式。
    做 字典设 用 r、「ELF机器号」、做 转字符串 用 做 目标_ELF机器号 用 0。
    做 字典设 用 r、「动态链接器」、做 目标_动态链接器。
    做 字典设 用 r、「页大小」、做 转字符串 用 做 目标_页大小。
    做 字典设 用 r、「缓存行」、做 转字符串 用 做 目标_缓存行大小。
    做 字典设 用 r、「SIMD」、做 转字符串 用 做 目标_支持SIMD。
    做 字典设 用 r、「SIMD宽度」、做 转字符串 用 做 目标_SIMD宽度。
    做 字典设 用 r、「原子操作」、做 转字符串 用 做 目标_支持原子操作。
    做 字典设 用 r、「硬件除法」、做 转字符串 用 做 目标_支持硬件除法。
    做 字典设 用 r、「未对齐访问」、做 转字符串 用 做 目标_支持未对齐访问。
    做 字典设 用 r、「最大分支距离」、做 转字符串 用 做 目标_最大分支距离。
    返还 r。
完。

目标_矩阵报告 接受 零参：
    可变 矩阵 为 一组：（空）。
    保存 为 当前目标。
    目标列 为 一组：目标_x86_64、目标_arm64、目标_riscv64、目标_wasm、目标_loongarch。
    可变 i 为 0。
    只要 i 小于 做 计数 用 目标列：
        当前目标 为 目标列 的 第 i 项。
        做 追加 用 矩阵、做 目标_ABI合同报告。
        i 为 i 加 1。
    完。
    当前目标 为 保存。
    返还 矩阵。
完。

目标_打印ABI报告 接受 零参：
    r 为 做 目标_ABI合同报告。
完。

可变 _构建profile 为 「linux-user」。

目标_设置profile 接受 p：
    _构建profile 为 p。
完。

目标_获取profile 接受 零参：
    返还 _构建profile。
完。

目标_构建合同 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「target」、做 目标_名称。
    做 字典设 用 r、「profile」、_构建profile。
    做 字典设 用 r、「pointer_size」、做 转字符串 用 做 目标_指针大小。
    做 字典设 用 r、「endian」、做 目标_字节序。
    做 字典设 用 r、「abi」、做 目标_调用约定名。
    做 字典设 用 r、「output_format」、做 目标_输出格式。
    做 字典设 用 r、「page_size」、做 转字符串 用 做 目标_页大小。
    做 字典设 用 r、「simd」、做 转字符串 用 做 目标_支持SIMD。
    返还 r。
完。

_多目标_生成x86段 接受 零参：
    可变 段 为 一组：（空）。
    做 追加 用 段、0x55。
    做 追加 用 段、0x48。
    做 追加 用 段、0x89。
    做 追加 用 段、0xE5。
    做 追加 用 段、0xB8。
    做 追加 用 段、0x2A。
    做 追加 用 段、0x00。
    做 追加 用 段、0x00。
    做 追加 用 段、0x00。
    做 追加 用 段、0x5D。
    做 追加 用 段、0xC3。
    返还 段。
完。

_多目标_生成arm64段 接受 零参：
    可变 段 为 一组：（空）。
    stp 为 做 编码_stp_pre 用 29、30、31、负 16。
    字节stp 为 做 A64指令字节 用 stp。
    做 追加 用 段、字节stp 的 第 0 项。
    做 追加 用 段、字节stp 的 第 1 项。
    做 追加 用 段、字节stp 的 第 2 项。
    做 追加 用 段、字节stp 的 第 3 项。
    mov_fp 为 做 编码_mov_reg 用 29、31。
    字节mov 为 做 A64指令字节 用 mov_fp。
    做 追加 用 段、字节mov 的 第 0 项。
    做 追加 用 段、字节mov 的 第 1 项。
    做 追加 用 段、字节mov 的 第 2 项。
    做 追加 用 段、字节mov 的 第 3 项。
    mov42 为 做 编码_movz 用 0、42、0。
    字节42 为 做 A64指令字节 用 mov42。
    做 追加 用 段、字节42 的 第 0 项。
    做 追加 用 段、字节42 的 第 1 项。
    做 追加 用 段、字节42 的 第 2 项。
    做 追加 用 段、字节42 的 第 3 项。
    ldp 为 做 编码_ldp_post 用 29、30、31、16。
    字节ldp 为 做 A64指令字节 用 ldp。
    做 追加 用 段、字节ldp 的 第 0 项。
    做 追加 用 段、字节ldp 的 第 1 项。
    做 追加 用 段、字节ldp 的 第 2 项。
    做 追加 用 段、字节ldp 的 第 3 项。
    ret_inst 为 做 编码_ret 用 30。
    字节ret 为 做 A64指令字节 用 ret_inst。
    做 追加 用 段、字节ret 的 第 0 项。
    做 追加 用 段、字节ret 的 第 1 项。
    做 追加 用 段、字节ret 的 第 2 项。
    做 追加 用 段、字节ret 的 第 3 项。
    返还 段。
完。

_多目标_生成riscv64段 接受 零参：
    可变 段 为 一组：（空）。
    i1 为 做 riscv_I型 用 负 16、RV_SP、0、RV_SP、0x13。
    做 追加 用 段、i1 位与 0xFF。
    做 追加 用 段、i1 右移 8 位与 0xFF。
    做 追加 用 段、i1 右移 16 位与 0xFF。
    做 追加 用 段、i1 右移 24 位与 0xFF。
    i2 为 做 riscv_S型 用 8、RV_RA、RV_SP、3、0x23。
    做 追加 用 段、i2 位与 0xFF。
    做 追加 用 段、i2 右移 8 位与 0xFF。
    做 追加 用 段、i2 右移 16 位与 0xFF。
    做 追加 用 段、i2 右移 24 位与 0xFF。
    i3 为 做 编码_li 用 RV_参数0、42。
    做 追加 用 段、i3 位与 0xFF。
    做 追加 用 段、i3 右移 8 位与 0xFF。
    做 追加 用 段、i3 右移 16 位与 0xFF。
    做 追加 用 段、i3 右移 24 位与 0xFF。
    i4 为 做 riscv_I型 用 8、RV_SP、3、RV_RA、0x03。
    做 追加 用 段、i4 位与 0xFF。
    做 追加 用 段、i4 右移 8 位与 0xFF。
    做 追加 用 段、i4 右移 16 位与 0xFF。
    做 追加 用 段、i4 右移 24 位与 0xFF。
    i5 为 做 riscv_I型 用 16、RV_SP、0、RV_SP、0x13。
    做 追加 用 段、i5 位与 0xFF。
    做 追加 用 段、i5 右移 8 位与 0xFF。
    做 追加 用 段、i5 右移 16 位与 0xFF。
    做 追加 用 段、i5 右移 24 位与 0xFF。
    i6 为 做 编码_ret_rv。
    做 追加 用 段、i6 位与 0xFF。
    做 追加 用 段、i6 右移 8 位与 0xFF。
    做 追加 用 段、i6 右移 16 位与 0xFF。
    做 追加 用 段、i6 右移 24 位与 0xFF。
    返还 段。
完。

_多目标_生成kernel段 接受 零参：
    可变 段 为 一组：（空）。
    做 追加 用 段、0xFA。
    做 追加 用 段、0xB8。
    做 追加 用 段、0x2A。
    做 追加 用 段、0x00。
    做 追加 用 段、0x00。
    做 追加 用 段、0x00。
    做 追加 用 段、0xF4。
    做 追加 用 段、0xEB。
    做 追加 用 段、0xFE。
    返还 段。
完。

多目标_四路smoke 接受 零参：
    保存目标 为 当前目标。
    保存profile 为 _构建profile。
    可变 结果 为 做 字典。
    做 设置目标架构 用 目标_x86_64。
    做 目标_设置profile 用 「linux-user」。
    x86段 为 做 _多目标_生成x86段。
    做 字典设 用 结果、「x86_64_linux_字节数」、做 转字符串 用 做 计数 用 x86段。
    做 字典设 用 结果、「x86_64_linux_合同」、做 目标_构建合同。
    做 设置目标架构 用 目标_arm64。
    做 目标_设置profile 用 「linux-user」。
    arm段 为 做 _多目标_生成arm64段。
    做 字典设 用 结果、「arm64_linux_字节数」、做 转字符串 用 做 计数 用 arm段。
    做 字典设 用 结果、「arm64_linux_合同」、做 目标_构建合同。
    做 字典设 用 结果、「arm64_4字节对齐」、做 转字符串 用 做 计数 用 arm段 取余于 4 等于 0。
    做 设置目标架构 用 目标_riscv64。
    做 目标_设置profile 用 「linux-user」。
    rv段 为 做 _多目标_生成riscv64段。
    做 字典设 用 结果、「riscv64_linux_字节数」、做 转字符串 用 做 计数 用 rv段。
    做 字典设 用 结果、「riscv64_linux_合同」、做 目标_构建合同。
    做 字典设 用 结果、「riscv64_4字节对齐」、做 转字符串 用 做 计数 用 rv段 取余于 4 等于 0。
    做 设置目标架构 用 目标_x86_64。
    做 目标_设置profile 用 「kernel-qemu」。
    kern段 为 做 _多目标_生成kernel段。
    做 字典设 用 结果、「kernel_qemu_字节数」、做 转字符串 用 做 计数 用 kern段。
    做 字典设 用 结果、「kernel_qemu_合同」、做 目标_构建合同。
    当前目标 为 保存目标。
    _构建profile 为 保存profile。
    做 字典设 用 结果、「四路全部生成」、做 转字符串 用 做 计数 用 x86段 大于 0 并且 做 计数 用 arm段 大于 0 并且 做 计数 用 rv段 大于 0 并且 做 计数 用 kern段 大于 0。
    返还 结果。
完。

目标矩阵_报告 接受 零参：
    保存 为 当前目标。
    保存p 为 _构建profile。
    r 为 做 字典。
    做 设置目标架构 用 目标_x86_64。
    做 目标_设置profile 用 「linux-user」。
    做 字典设 用 r、「x86_64_linux_abi」、做 目标_调用约定名。
    做 字典设 用 r、「x86_64_linux_ptr」、做 转字符串 用 做 目标_指针大小。
    做 字典设 用 r、「x86_64_linux_fmt」、做 目标_输出格式。
    做 字典设 用 r、「x86_64_linux_regs」、做 转字符串 用 做 目标_寄存器数。
    做 设置目标架构 用 目标_arm64。
    做 目标_设置profile 用 「linux-user」。
    做 字典设 用 r、「arm64_linux_abi」、做 目标_调用约定名。
    做 字典设 用 r、「arm64_linux_ptr」、做 转字符串 用 做 目标_指针大小。
    做 字典设 用 r、「arm64_linux_regs」、做 转字符串 用 做 目标_寄存器数。
    做 字典设 用 r、「arm64_linux_simd」、做 转字符串 用 做 目标_支持SIMD。
    做 设置目标架构 用 目标_riscv64。
    做 目标_设置profile 用 「linux-user」。
    做 字典设 用 r、「riscv64_linux_abi」、做 目标_调用约定名。
    做 字典设 用 r、「riscv64_linux_ptr」、做 转字符串 用 做 目标_指针大小。
    做 字典设 用 r、「riscv64_linux_regs」、做 转字符串 用 做 目标_寄存器数。
    做 设置目标架构 用 目标_x86_64。
    做 目标_设置profile 用 「kernel-qemu」。
    做 字典设 用 r、「kernel_qemu_abi」、做 目标_调用约定名。
    做 字典设 用 r、「kernel_qemu_fmt」、做 目标_输出格式。
    做 字典设 用 r、「目标数」、「4」。
    做 字典设 用 r、「矩阵状态」、「四路定义完备」。
    当前目标 为 保存。
    _构建profile 为 保存p。
    返还 r。
完。

ABI契约_报告 接受 零参：
    保存 为 当前目标。
    保存p 为 _构建profile。
    r 为 做 字典。
    目标列 为 一组：目标_x86_64、目标_arm64、目标_riscv64、目标_wasm。
    名列 为 一组：「x86_64」、「arm64」、「riscv64」、「wasm」。
    可变 i 为 0。
    只要 i 小于 4：
        做 设置目标架构 用 目标列 的 第 i 项。
        做 字典设 用 r、做 字符串拼接 用 名列 的 第 i 项、「_abi」、做 目标_调用约定名。
        做 字典设 用 r、做 字符串拼接 用 名列 的 第 i 项、「_ptr」、做 转字符串 用 做 目标_指针大小。
        做 字典设 用 r、做 字符串拼接 用 名列 的 第 i 项、「_regs」、做 转字符串 用 做 目标_寄存器数。
        做 字典设 用 r、做 字符串拼接 用 名列 的 第 i 项、「_fmt」、做 目标_输出格式。
        做 字典设 用 r、做 字符串拼接 用 名列 的 第 i 项、「_endian」、做 目标_字节序。
        做 字典设 用 r、做 字符串拼接 用 名列 的 第 i 项、「_simd」、做 转字符串 用 做 目标_支持SIMD。
        做 字典设 用 r、做 字符串拼接 用 名列 的 第 i 项、「_align」、做 转字符串 用 做 目标_栈对齐 用 0。
        i 为 i 加 1。
    完。
    做 字典设 用 r、「合同目标数」、「4」。
    当前目标 为 保存。
    _构建profile 为 保存p。
    返还 r。
完。

冒烟测试_报告 接受 零参：
    smoke 为 做 多目标_四路smoke。
    r 为 做 字典。
    做 字典设 用 r、「x86_64_字节数」、做 字典取 用 smoke、「x86_64_linux_字节数」。
    做 字典设 用 r、「arm64_字节数」、做 字典取 用 smoke、「arm64_linux_字节数」。
    做 字典设 用 r、「riscv64_字节数」、做 字典取 用 smoke、「riscv64_linux_字节数」。
    做 字典设 用 r、「kernel_字节数」、做 字典取 用 smoke、「kernel_qemu_字节数」。
    做 字典设 用 r、「四路全通过」、做 字典取 用 smoke、「四路全部生成」。
    做 字典设 用 r、「arm64_对齐」、做 字典取 用 smoke、「arm64_4字节对齐」。
    做 字典设 用 r、「riscv64_对齐」、做 字典取 用 smoke、「riscv64_4字节对齐」。
    返还 r。
完。

可变 _权威样例集 为 一组：（空）。

权威样例_初始化 接受 零参：
    _权威样例集 为 一组：一组：「算术」、「1+2*3=7」、「整数运算优先级」、一组：「控制流」、「如果/否则/当循环」、「条件跳转+循环」、一组：「列表」、「[1,2,3]索引+追加+计数」、「动态数组」、一组：「字典」、「字典设/取/键」、「哈希表操作」、一组：「结构体」、「点=[x,y,z]索引访问」、「复合数据」、一组：「借用」、「定义 不可变 x=5」、「所有权语义」、一组：「AI张量形状」、「shape=[1,3,224,224]」、「形状约束」、一组：「AI精度」、「Q10定点/FP16/INT8」、「精度合同」、一组：「AI设备」、「CPU/GPU/NPU分派」、「设备合同」、一组：「图形最小」、「像素_写+画布_创建」、「渲染入口」、一组：「音频最小」、「PCM缓冲_创建+采样_写」、「音频入口」、一组：「输入最小」、「窗口_监听+事件」、「输入事件」。
完。

权威样例_报告 接受 零参：
    若 做 计数 用 _权威样例集 等于 0：
        做 权威样例_初始化。
    完。
    r 为 做 字典。
    做 字典设 用 r、「样例总数」、做 转字符串 用 做 计数 用 _权威样例集。
    可变 类别 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _权威样例集：
        做 追加 用 类别、_权威样例集 的 第 i 项 的 第 0 项。
        i 为 i 加 1。
    完。
    做 字典设 用 r、「类别数」、做 转字符串 用 做 计数 用 类别。
    返还 r。
完。

后端_报告 接受 _扩1：
    r 为 做 字典。
    做 字典设 用 r、「当前目标」、做 目标_名称。
    做 字典设 用 r、「指令宽度」、做 转字符串 用 做 目标_指令宽度。
    做 字典设 用 r、「寄存器数」、做 转字符串 用 做 目标_寄存器数。
    做 字典设 用 r、「调用约定」、做 目标_调用约定名。
    做 字典设 用 r、「输出格式」、做 目标_输出格式。
    做 字典设 用 r、「字节序」、做 目标_字节序。
    做 字典设 用 r、「指针大小」、做 转字符串 用 做 目标_指针大小。
    做 字典设 用 r、「栈对齐」、做 转字符串 用 做 目标_栈对齐 用 0。
    做 字典设 用 r、「SIMD」、做 转字符串 用 做 目标_支持SIMD。
    做 字典设 用 r、「浮点寄存器」、做 转字符串 用 做 目标_浮点寄存器数。
    做 字典设 用 r、「页大小」、做 转字符串 用 做 目标_页大小。
    返还 r。
完。

本章 收束。
