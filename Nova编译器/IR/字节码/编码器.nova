《编码器》

引用《指令集》。

引用《..·元信息·调试映射》。

引用《..·..·标准库·基础》。

引用《..·..·编译器·规划·核心·常量》。

Nova编码_创建上下文 接受 块：
    返还 一组：块、做 字典、0、一组：（空）、「__主程序__」。
完。

Nova编码_当前块 接受 ctx：
    返还 ctx 的 第 0 项。
完。

Nova编码_变量表 接受 ctx：
    返还 ctx 的 第 1 项。
完。

Nova编码_变量数 接受 ctx：
    返还 ctx 的 第 2 项。
完。

Nova编码_函数名 接受 ctx：
    返还 ctx 的 第 5 项。
完。

Nova编码_声明局部 接受 ctx、名称：
    索引 为 ctx 的 第 2 项。
    做 字典设置 用 ctx 的 第 1 项、名称、索引。
    ctx 的 第 2 项 为 ctx 的 第 2 项 加 1。
    返还 索引。
完。

Nova编码_查找局部 接受 ctx、名称：
    结果 为 做 字典获取 用 ctx 的 第 1 项、名称。
    若 结果 不等于 空：
        返还 结果。
    完。
    返还 负 1。
完。

Nova编码_处理_102 接受 ctx、节点：
    块 为 做 Nova编码_当前块 用 ctx。
    名称 为 节点 的 第 1 项。
    若 名称 等于 「真」：
        做 Nova字节码_发射 用 块、指令_压入真。
        返还。
    完。
    若 名称 等于 「假」：
        做 Nova字节码_发射 用 块、指令_压入假。
        返还。
    完。
    若 名称 等于 「空」：
        做 Nova字节码_发射 用 块、指令_压入空。
        返还。
    完。
    局部 为 做 Nova编码_查找局部 用 ctx、名称。
    若 局部 大于或等于 0：
        做 Nova字节码_发射16 用 块、指令_载局部、局部。
    否则：
        全局索引 为 做 Nova字节码_添加常量 用 块、名称。
        做 Nova字节码_发射16 用 块、指令_载全局、全局索引。
    完。
    返还。
完。

Nova编码_处理_104 接受 ctx、节点：
    块 为 做 Nova编码_当前块 用 ctx。
    op 为 节点 的 第 1 项。
    若 op 等于 「&&」：
        做 Nova编码_表达式 用 ctx、节点 的 第 2 项。
        跳位 为 做 Nova字节码_代码大小 用 块。
        做 Nova字节码_发射16 用 块、指令_假跳、0。
        做 Nova字节码_发射 用 块、指令_弹出。
        做 Nova编码_表达式 用 ctx、节点 的 第 3 项。
        代码 为 块 的 第 4 项。
        代码 的 第 跳位 加 1 项 为 做 Nova字节码_代码大小 用 块 位与 0xFF。
        代码 的 第 跳位 加 2 项 为 做 Nova字节码_代码大小 用 块 右移 8 位与 0xFF。
        返还。
    完。
    若 op 等于 「||」：
        做 Nova编码_表达式 用 ctx、节点 的 第 2 项。
        跳位 为 做 Nova字节码_代码大小 用 块。
        做 Nova字节码_发射16 用 块、指令_真跳、0。
        做 Nova字节码_发射 用 块、指令_弹出。
        做 Nova编码_表达式 用 ctx、节点 的 第 3 项。
        代码 为 块 的 第 4 项。
        代码 的 第 跳位 加 1 项 为 做 Nova字节码_代码大小 用 块 位与 0xFF。
        代码 的 第 跳位 加 2 项 为 做 Nova字节码_代码大小 用 块 右移 8 位与 0xFF。
        返还。
    完。
    做 Nova编码_表达式 用 ctx、节点 的 第 2 项。
    做 Nova编码_表达式 用 ctx、节点 的 第 3 项。
    若 op 等于 「+」：
        做 Nova字节码_发射 用 块、指令_加。
    否则若 op 等于 「-」：
        做 Nova字节码_发射 用 块、指令_减。
    否则若 op 等于 「*」：
        做 Nova字节码_发射 用 块、指令_乘。
    否则若 op 等于 「/」：
        做 Nova字节码_发射 用 块、指令_除。
    否则若 op 等于 「%」：
        做 Nova字节码_发射 用 块、指令_余。
    否则若 op 等于 「==」：
        做 Nova字节码_发射 用 块、指令_比等。
    否则若 op 等于 「!=」：
        做 Nova字节码_发射 用 块、指令_比不等。
    否则若 op 等于 「<」：
        做 Nova字节码_发射 用 块、指令_比小。
    否则若 op 等于 「<=」：
        做 Nova字节码_发射 用 块、指令_比小等。
    否则若 op 等于 「>」：
        做 Nova字节码_发射 用 块、指令_比大。
    否则若 op 等于 「>=」：
        做 Nova字节码_发射 用 块、指令_比大等。
    否则若 op 等于 「&」：
        做 Nova字节码_发射 用 块、指令_位与。
    否则若 op 等于 「|」：
        做 Nova字节码_发射 用 块、指令_位或。
    否则若 op 等于 「^」：
        做 Nova字节码_发射 用 块、指令_位异或。
    否则若 op 等于 「<<」：
        做 Nova字节码_发射 用 块、指令_左移。
    否则若 op 等于 「>>」：
        做 Nova字节码_发射 用 块、指令_右移。
    完。
    返还。
完。

Nova编码_处理_103 接受 ctx、节点：
    块 为 做 Nova编码_当前块 用 ctx。
    函数名 为 节点 的 第 1 项。
    参数列 为 节点 的 第 2 项。
    若 参数列 等于 空 或者 做 是列表 用 参数列 等于 假：
        参数列 为 一组：（空）。
    完。
    若 函数名 等于 「打印」 并且 做 计数 用 参数列 等于 1：
        做 Nova编码_表达式 用 ctx、参数列 的 第 0 项。
        做 Nova字节码_发射 用 块、指令_打印。
        返还。
    完。
    若 函数名 等于 「转字符串」 并且 做 计数 用 参数列 等于 1：
        做 Nova编码_表达式 用 ctx、参数列 的 第 0 项。
        做 Nova字节码_发射 用 块、指令_转字串。
        返还。
    完。
    若 函数名 等于 「计数」 并且 做 计数 用 参数列 等于 1：
        做 Nova编码_表达式 用 ctx、参数列 的 第 0 项。
        做 Nova字节码_发射 用 块、指令_长度。
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数列：
        做 Nova编码_表达式 用 ctx、参数列 的 第 i 项。
        i 为 i 加 1。
    完。
    函数索引 为 做 Nova字节码_添加常量 用 块、函数名。
    做 Nova字节码_发射16 用 块、指令_调用、函数索引。
    做 追加 用 块 的 第 4 项、做 计数 用 参数列 位与 0xFF。
    返还。
完。

Nova编码_处理_105 接受 ctx、节点：
    块 为 做 Nova编码_当前块 用 ctx。
    元素 为 节点 的 第 1 项。
    若 元素 等于 空 或者 做 是列表 用 元素 等于 假：
        元素 为 一组：（空）。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 元素：
        做 Nova编码_表达式 用 ctx、元素 的 第 i 项。
        i 为 i 加 1。
    完。
    做 Nova字节码_发射8 用 块、指令_建列表、做 计数 用 元素。
    返还。
完。

Nova编码_处理_130 接受 ctx、节点：
    块 为 做 Nova编码_当前块 用 ctx。
    名称索引 为 做 Nova字节码_添加常量 用 块、节点 的 第 1 项。
    做 Nova字节码_发射16 用 块、指令_建结构、名称索引。
    若 做 计数 用 节点 大于 2 并且 节点 的 第 2 项 不等于 空 并且 做 是列表 用 节点 的 第 2 项：
        字段列 为 节点 的 第 2 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 字段列：
            对 为 字段列 的 第 i 项。
            若 做 是列表 用 对 并且 做 计数 用 对 大于或等于 2：
                做 Nova字节码_发射 用 块、指令_复制。
                做 Nova编码_表达式 用 ctx、对 的 第 1 项。
                字段索引 为 做 Nova字节码_添加常量 用 块、对 的 第 0 项。
                做 Nova字节码_发射16 用 块、指令_设字段、字段索引。
            完。
            i 为 i 加 1。
        完。
    完。
    返还。
完。

Nova编码_处理_140 接受 ctx、节点：
    块 为 做 Nova编码_当前块 用 ctx。
    参数列 为 节点 的 第 2 项。
    若 参数列 等于 空 或者 做 是列表 用 参数列 等于 假：
        参数列 为 一组：（空）。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数列：
        做 Nova编码_表达式 用 ctx、参数列 的 第 i 项。
        i 为 i 加 1。
    完。
    做 Nova编码_表达式 用 ctx、节点 的 第 1 项。
    做 Nova字节码_发射8 用 块、指令_间接调用、做 计数 用 参数列。
    返还。
完。

Nova编码_处理_151 接受 ctx、节点：
    块 为 做 Nova编码_当前块 用 ctx。
    做 Nova字节码_发射16 用 块、指令_建字典、0。
    若 做 计数 用 节点 大于 1 并且 节点 的 第 1 项 不等于 空 并且 做 是列表 用 节点 的 第 1 项：
        对列 为 节点 的 第 1 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 对列：
            对 为 对列 的 第 i 项。
            若 做 是列表 用 对 并且 做 计数 用 对 大于或等于 2：
                做 Nova字节码_发射 用 块、指令_复制。
                做 Nova编码_表达式 用 ctx、对 的 第 0 项。
                做 Nova编码_表达式 用 ctx、对 的 第 1 项。
                做 Nova字节码_发射 用 块、指令_设字典。
            完。
            i 为 i 加 1。
        完。
    完。
    返还。
完。

Nova编码_表达式 接受 ctx、节点：
    若 节点 等于 空 或者 做 是列表 用 节点 等于 假 或者 做 计数 用 节点 小于 1：
        返还。
    完。
    块 为 做 Nova编码_当前块 用 ctx。
    类型 为 节点 的 第 0 项。
    若 类型 等于 节_整数：
        做 Nova字节码_发射64 用 块、指令_压入整数、节点 的 第 1 项。
        返还。
    完。
    若 类型 等于 节_浮点：
        做 Nova字节码_发射64 用 块、指令_压入浮点、节点 的 第 1 项。
        返还。
    完。
    若 类型 等于 节_字符串：
        索引 为 做 Nova字节码_添加常量 用 块、节点 的 第 1 项。
        做 Nova字节码_发射16 用 块、指令_压入字串、索引。
        返还。
    完。
    若 类型 等于 节_标识符：
        做 Nova编码_处理_102 用 ctx、节点。
        返还。
    完。
    若 类型 等于 节_调用：
        做 Nova编码_处理_103 用 ctx、节点。
        返还。
    完。
    若 类型 等于 节_二元：
        做 Nova编码_处理_104 用 ctx、节点。
        返还。
    完。
    若 类型 等于 节_列表：
        做 Nova编码_处理_105 用 ctx、节点。
        返还。
    完。
    若 类型 等于 节_索引：
        做 Nova编码_表达式 用 ctx、节点 的 第 1 项。
        做 Nova编码_表达式 用 ctx、节点 的 第 2 项。
        做 Nova字节码_发射 用 块、指令_取索引。
        返还。
    完。
    若 类型 等于 节_新建：
        做 Nova编码_处理_130 用 ctx、节点。
        返还。
    完。
    若 类型 等于 节_点访问：
        做 Nova编码_表达式 用 ctx、节点 的 第 1 项。
        字段索引 为 做 Nova字节码_添加常量 用 块、节点 的 第 2 项。
        做 Nova字节码_发射16 用 块、指令_取字段、字段索引。
        返还。
    完。
    若 类型 等于 节_匿名函数：
        闭包索引 为 做 Nova字节码_添加常量 用 块、节点 的 第 1 项。
        做 Nova字节码_发射16 用 块、指令_闭包、闭包索引。
        返还。
    完。
    若 类型 等于 节_逻辑非：
        做 Nova编码_表达式 用 ctx、节点 的 第 1 项。
        做 Nova字节码_发射 用 块、指令_非。
        返还。
    完。
    若 类型 等于 节_位非：
        做 Nova编码_表达式 用 ctx、节点 的 第 1 项。
        做 Nova字节码_发射 用 块、指令_位非。
        返还。
    完。
    若 类型 等于 节_间接调用：
        做 Nova编码_处理_140 用 ctx、节点。
        返还。
    完。
    若 类型 等于 节_字典：
        做 Nova编码_处理_151 用 ctx、节点。
        返还。
    完。
    若 类型 等于 165：
        做 Nova字节码_发射 用 块、指令_压入空。
        返还。
    完。
完。

Nova编码_语句 接受 ctx、节点：
    若 节点 等于 空 或者 做 是列表 用 节点 等于 假 或者 做 计数 用 节点 小于 1：
        返还。
    完。
    块 为 做 Nova编码_当前块 用 ctx。
    类型 为 节点 的 第 0 项。
    若 类型 等于 节_定义：
        名称 为 节点 的 第 1 项。
        若 做 计数 用 节点 大于 2 并且 节点 的 第 2 项 不等于 空：
            做 Nova编码_表达式 用 ctx、节点 的 第 2 项。
        否则：
            做 Nova字节码_发射 用 块、指令_压入空。
        完。
        索引 为 做 Nova编码_声明局部 用 ctx、名称。
        做 Nova字节码_发射16 用 块、指令_存局部、索引。
        返还。
    完。
    若 类型 等于 节_赋值：
        做 Nova编码_表达式 用 ctx、节点 的 第 2 项。
        局部 为 做 Nova编码_查找局部 用 ctx、节点 的 第 1 项。
        若 局部 大于或等于 0：
            做 Nova字节码_发射16 用 块、指令_存局部、局部。
        否则：
            全局索引 为 做 Nova字节码_添加常量 用 块、节点 的 第 1 项。
            做 Nova字节码_发射16 用 块、指令_存全局、全局索引。
        完。
        返还。
    完。
    若 类型 等于 节_返回：
        若 做 计数 用 节点 大于 1 并且 节点 的 第 1 项 不等于 空：
            做 Nova编码_表达式 用 ctx、节点 的 第 1 项。
            做 Nova字节码_发射 用 块、指令_返回。
        否则：
            做 Nova字节码_发射 用 块、指令_返回空。
        完。
        返还。
    完。
    若 类型 等于 节_表达式：
        做 Nova编码_表达式 用 ctx、节点 的 第 1 项。
        做 Nova字节码_发射 用 块、指令_弹出。
        返还。
    完。
    若 类型 等于 节_如果：
        做 Nova编码_表达式 用 ctx、节点 的 第 1 项。
        跳假位 为 做 Nova字节码_代码大小 用 块。
        做 Nova字节码_发射16 用 块、指令_假跳、0。
        做 Nova编码_块 用 ctx、节点 的 第 2 项。
        若 做 计数 用 节点 大于 3 并且 节点 的 第 3 项 不等于 空：
            跳过位 为 做 Nova字节码_代码大小 用 块。
            做 Nova字节码_发射16 用 块、指令_跳转、0。
            代码 为 块 的 第 4 项。
            代码 的 第 跳假位 加 1 项 为 做 Nova字节码_代码大小 用 块 位与 0xFF。
            代码 的 第 跳假位 加 2 项 为 做 Nova字节码_代码大小 用 块 右移 8 位与 0xFF。
            做 Nova编码_块 用 ctx、节点 的 第 3 项。
            代码 的 第 跳过位 加 1 项 为 做 Nova字节码_代码大小 用 块 位与 0xFF。
            代码 的 第 跳过位 加 2 项 为 做 Nova字节码_代码大小 用 块 右移 8 位与 0xFF。
        否则：
            代码 为 块 的 第 4 项。
            代码 的 第 跳假位 加 1 项 为 做 Nova字节码_代码大小 用 块 位与 0xFF。
            代码 的 第 跳假位 加 2 项 为 做 Nova字节码_代码大小 用 块 右移 8 位与 0xFF。
        完。
        返还。
    完。
    若 类型 等于 节_当：
        循环头 为 做 Nova字节码_代码大小 用 块。
        做 Nova字节码_发射 用 块、指令_热点计。
        做 追加 用 ctx 的 第 4 项、一组：循环头、一组：（空）。
        做 Nova编码_表达式 用 ctx、节点 的 第 1 项。
        跳出位 为 做 Nova字节码_代码大小 用 块。
        做 Nova字节码_发射16 用 块、指令_假跳、0。
        做 Nova编码_块 用 ctx、节点 的 第 2 项。
        做 Nova字节码_发射16 用 块、指令_循环、循环头。
        代码 为 块 的 第 4 项。
        跳出目标 为 做 Nova字节码_代码大小 用 块。
        代码 的 第 跳出位 加 1 项 为 跳出目标 位与 0xFF。
        代码 的 第 跳出位 加 2 项 为 跳出目标 右移 8 位与 0xFF。
        循环信息 为 ctx 的 第 4 项 的 第 做 计数 用 ctx 的 第 4 项 减 1 项。
        中断列 为 循环信息 的 第 1 项。
        可变 bi 为 0。
        只要 bi 小于 做 计数 用 中断列：
            中断位 为 中断列 的 第 bi 项。
            代码 的 第 中断位 加 1 项 为 跳出目标 位与 0xFF。
            代码 的 第 中断位 加 2 项 为 跳出目标 右移 8 位与 0xFF。
            bi 为 bi 加 1。
        完。
        做 弹出 用 ctx 的 第 4 项。
        返还。
    完。
    若 类型 等于 节_索引赋值：
        做 Nova编码_表达式 用 ctx、节点 的 第 3 项。
        做 Nova编码_表达式 用 ctx、节点 的 第 1 项。
        做 Nova编码_表达式 用 ctx、节点 的 第 2 项。
        做 Nova字节码_发射 用 块、指令_设索引。
        返还。
    完。
    若 类型 等于 节_点赋值：
        做 Nova编码_表达式 用 ctx、节点 的 第 3 项。
        做 Nova编码_表达式 用 ctx、节点 的 第 1 项。
        字段索引 为 做 Nova字节码_添加常量 用 块、节点 的 第 2 项。
        做 Nova字节码_发射16 用 块、指令_设字段、字段索引。
        返还。
    完。
    若 类型 等于 节_每个：
        做 Nova编码_表达式 用 ctx、节点 的 第 2 项。
        迭代索引 为 做 Nova编码_声明局部 用 ctx、节点 的 第 1 项。
        循环头 为 做 Nova字节码_代码大小 用 块。
        做 Nova字节码_发射 用 块、指令_热点计。
        跳出位 为 做 Nova字节码_代码大小 用 块。
        做 Nova字节码_发射16 用 块、指令_遍历、0。
        做 Nova字节码_发射16 用 块、指令_存局部、迭代索引。
        做 Nova编码_块 用 ctx、节点 的 第 3 项。
        做 Nova字节码_发射16 用 块、指令_循环、循环头。
        代码 为 块 的 第 4 项。
        代码 的 第 跳出位 加 1 项 为 做 Nova字节码_代码大小 用 块 位与 0xFF。
        代码 的 第 跳出位 加 2 项 为 做 Nova字节码_代码大小 用 块 右移 8 位与 0xFF。
        返还。
    完。
    若 类型 等于 节_中断：
        若 做 计数 用 ctx 的 第 4 项 大于 0：
            中断位 为 做 Nova字节码_代码大小 用 块。
            做 Nova字节码_发射16 用 块、指令_跳转、0。
            做 追加 用 ctx 的 第 4 项 的 第 做 计数 用 ctx 的 第 4 项 减 1 项 的 第 1 项、中断位。
        完。
        返还。
    完。
    若 类型 等于 节_继续：
        若 做 计数 用 ctx 的 第 4 项 大于 0：
            循环头 为 ctx 的 第 4 项 的 第 做 计数 用 ctx 的 第 4 项 减 1 项 的 第 0 项。
            做 Nova字节码_发射16 用 块、指令_循环、循环头。
        完。
        返还。
    完。
    若 类型 等于 节_尝试：
        try位 为 做 Nova字节码_代码大小 用 块。
        做 Nova字节码_发射16 用 块、指令_尝试、0。
        做 Nova编码_块 用 ctx、节点 的 第 1 项。
        跳过位 为 做 Nova字节码_代码大小 用 块。
        做 Nova字节码_发射16 用 块、指令_跳转、0。
        代码 为 块 的 第 4 项。
        代码 的 第 try位 加 1 项 为 做 Nova字节码_代码大小 用 块 位与 0xFF。
        代码 的 第 try位 加 2 项 为 做 Nova字节码_代码大小 用 块 右移 8 位与 0xFF。
        若 做 计数 用 节点 大于 2 并且 节点 的 第 2 项 不等于 空：
            异常变量索引 为 做 Nova编码_声明局部 用 ctx、节点 的 第 2 项。
            做 Nova字节码_发射16 用 块、指令_存局部、异常变量索引。
        完。
        若 做 计数 用 节点 大于 3 并且 节点 的 第 3 项 不等于 空：
            做 Nova编码_块 用 ctx、节点 的 第 3 项。
        完。
        代码 的 第 跳过位 加 1 项 为 做 Nova字节码_代码大小 用 块 位与 0xFF。
        代码 的 第 跳过位 加 2 项 为 做 Nova字节码_代码大小 用 块 右移 8 位与 0xFF。
        返还。
    完。
    若 类型 等于 节_抛出：
        若 做 计数 用 节点 大于 1：
            做 Nova编码_表达式 用 ctx、节点 的 第 1 项。
        否则：
            做 Nova字节码_发射 用 块、指令_压入空。
        完。
        做 Nova字节码_发射 用 块、指令_抛出。
        返还。
    完。
    若 类型 等于 节_函数：
        做 Nova编码_函数 用 ctx、节点。
        返还。
    完。
完。

Nova编码_块 接受 ctx、语句列：
    可变 i 为 0。
    只要 i 小于 做 计数 用 语句列：
        做 Nova编码_语句 用 ctx、语句列 的 第 i 项。
        i 为 i 加 1。
    完。
完。

Nova编码_函数 接受 ctx、函数节点：
    块 为 做 Nova编码_当前块 用 ctx。
    名称 为 函数节点 的 第 1 项。
    参数列 为 函数节点 的 第 2 项。
    函数体 为 函数节点 的 第 3 项。
    旧变量表 为 ctx 的 第 1 项。
    旧变量数 为 ctx 的 第 2 项。
    旧函数名 为 ctx 的 第 5 项。
    ctx 的 第 1 项 为 做 字典。
    ctx 的 第 2 项 为 0。
    ctx 的 第 5 项 为 名称。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数列：
        做 Nova编码_声明局部 用 ctx、参数列 的 第 i 项。
        i 为 i 加 1。
    完。
    入口 为 做 Nova字节码_代码大小 用 块。
    做 Nova字节码_注册函数 用 块、名称、入口、做 计数 用 参数列。
    做 Nova编码_块 用 ctx、函数体。
    做 Nova字节码_发射 用 块、指令_返回空。
    ctx 的 第 1 项 为 旧变量表。
    ctx 的 第 2 项 为 旧变量数。
    ctx 的 第 5 项 为 旧函数名。
完。

Nova编码_编译程序 接受 函数列、主体：
    块 为 做 Nova字节码_创建块。
    ctx 为 做 Nova编码_创建上下文 用 块。
    可变 i 为 0。
    只要 i 小于 做 计数 用 函数列：
        做 Nova编码_函数 用 ctx、函数列 的 第 i 项。
        i 为 i 加 1。
    完。
    主入口 为 做 Nova字节码_代码大小 用 块。
    做 Nova字节码_注册函数 用 块、「__主程序__」、主入口、0。
    做 Nova编码_块 用 ctx、主体。
    做 Nova字节码_发射 用 块、指令_停机。
    返还 块。
完。

Nova编码_编译语句 接受 ctx、语句节点：
    若 语句节点 等于 空 或者 做 是列表 用 语句节点 等于 假 或者 做 计数 用 语句节点 小于 1：
        返还。
    完。
    类型 为 语句节点 的 第 0 项。
    若 类型 等于 节_函数：
        做 Nova编码_函数 用 ctx、语句节点。
        返还。
    完。
    做 Nova编码_语句 用 ctx、语句节点。
完。

可变 _编码器_编译数 为 0。

可变 _编码器_指令数 为 0。

IR字节码_差分编码 接受 字节序列：
    若 做 计数 用 字节序列 等于 0：
        返还 一组：（空）。
    完。
    可变 差分 为 一组：字节序列 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 字节序列：
        做 追加 用 差分、字节序列 的 第 i 项 减 字节序列 的 第 i 减 1 项。
        i 为 i 加 1。
    完。
    返还 差分。
完。

IR字节码_差分解码 接受 差分序列：
    若 做 计数 用 差分序列 等于 0：
        返还 一组：（空）。
    完。
    可变 原始 为 一组：差分序列 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 差分序列：
        做 追加 用 原始、原始 的 第 i 减 1 项 加 差分序列 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 原始。
完。

IR字节码_压缩效率分析 接受 差分序列：
    若 做 计数 用 差分序列 小于或等于 1：
        返还 一组：0、0。
    完。
    可变 小差值数 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 差分序列：
        v 为 差分序列 的 第 i 项。
        若 v 小于 0：
            v 为 0 减 v。
        完。
        若 v 小于或等于 8：
            小差值数 为 小差值数 加 1。
        完。
        i 为 i 加 1。
    完。
    小差值比例 为 小差值数 乘 100 除以 (做 计数 用 差分序列 减 1)。
    估计压缩率 为 100 减 小差值比例 除以 2。
    返还 一组：小差值比例、估计压缩率。
完。

本章 收束。
