《指令集》

引用《..·..·标准库·基础》。

指令_空操作 为 0x00。

指令_压入整数 为 0x01。

指令_压入浮点 为 0x02。

指令_压入字串 为 0x03。

指令_压入空 为 0x04。

指令_压入真 为 0x05。

指令_压入假 为 0x06。

指令_弹出 为 0x07。

指令_复制 为 0x08。

指令_交换 为 0x09。

指令_加 为 0x10。

指令_减 为 0x11。

指令_乘 为 0x12。

指令_除 为 0x13。

指令_余 为 0x14。

指令_取负 为 0x15。

指令_非 为 0x16。

指令_位非 为 0x17。

指令_位与 为 0x18。

指令_位或 为 0x19。

指令_位异或 为 0x1A。

指令_左移 为 0x1B。

指令_右移 为 0x1C。

指令_载局部 为 0x20。

指令_存局部 为 0x21。

指令_载全局 为 0x22。

指令_存全局 为 0x23。

指令_载闭包 为 0x24。

指令_存闭包 为 0x25。

指令_建列表 为 0x26。

指令_建字典 为 0x27。

指令_建结构 为 0x28。

指令_取索引 为 0x29。

指令_设索引 为 0x2A。

指令_取字段 为 0x2B。

指令_设字段 为 0x2C。

指令_取字典 为 0x2D。

指令_设字典 为 0x2E。

指令_跳转 为 0x30。

指令_真跳 为 0x31。

指令_假跳 为 0x32。

指令_循环 为 0x33。

指令_比等 为 0x34。

指令_比不等 为 0x35。

指令_比小 为 0x36。

指令_比小等 为 0x37。

指令_比大 为 0x38。

指令_比大等 为 0x39。

指令_调用 为 0x40。

指令_间接调用 为 0x41。

指令_原生调用 为 0x42。

指令_外部调用 为 0x43。

指令_返回 为 0x44。

指令_返回空 为 0x45。

指令_闭包 为 0x46。

指令_尾调用 为 0x47。

指令_让出 为 0x48。

指令_恢复 为 0x49。

指令_类型检查 为 0x50。

指令_类型转换 为 0x51。

指令_是整数 为 0x52。

指令_是浮点 为 0x53。

指令_是字串 为 0x54。

指令_是列表 为 0x55。

指令_是字典 为 0x56。

指令_是空 为 0x57。

指令_是函数 为 0x58。

指令_转整数 为 0x59。

指令_转浮点 为 0x5A。

指令_转字串 为 0x5B。

指令_打印 为 0x5C。

指令_长度 为 0x5D。

指令_断言 为 0x5E。

指令_尝试 为 0x60。

指令_捕获 为 0x61。

指令_抛出 为 0x62。

指令_导入 为 0x63。

指令_断点 为 0x64。

指令_行号 为 0x65。

指令_热点计 为 0x66。

指令_即时编译入 为 0x67。

指令_即时编译出 为 0x68。

指令_匹配 为 0x69。

指令_遍历 为 0x6A。

指令_展开 为 0x6B。

指令_停机 为 0xFF。

Nova指令_操作数大小 接受 操作码：
    若 操作码 等于 0x01：
        返还 8。
    完。
    若 操作码 等于 0x02：
        返还 8。
    完。
    若 操作码 等于 0x03：
        返还 2。
    完。
    若 操作码 等于 0x20：
        返还 2。
    完。
    若 操作码 等于 0x21：
        返还 2。
    完。
    若 操作码 等于 0x22：
        返还 2。
    完。
    若 操作码 等于 0x23：
        返还 2。
    完。
    若 操作码 等于 0x24：
        返还 2。
    完。
    若 操作码 等于 0x25：
        返还 2。
    完。
    若 操作码 等于 0x26：
        返还 2。
    完。
    若 操作码 等于 0x27：
        返还 2。
    完。
    若 操作码 等于 0x28：
        返还 2。
    完。
    若 操作码 等于 0x2B：
        返还 2。
    完。
    若 操作码 等于 0x2C：
        返还 2。
    完。
    若 操作码 等于 0x30：
        返还 2。
    完。
    若 操作码 等于 0x31：
        返还 2。
    完。
    若 操作码 等于 0x32：
        返还 2。
    完。
    若 操作码 等于 0x33：
        返还 2。
    完。
    若 操作码 等于 0x40：
        返还 3。
    完。
    若 操作码 等于 0x41：
        返还 1。
    完。
    若 操作码 等于 0x42：
        返还 3。
    完。
    若 操作码 等于 0x43：
        返还 3。
    完。
    若 操作码 等于 0x46：
        返还 2。
    完。
    若 操作码 等于 0x47：
        返还 3。
    完。
    若 操作码 等于 0x50：
        返还 1。
    完。
    若 操作码 等于 0x51：
        返还 1。
    完。
    若 操作码 等于 0x60：
        返还 2。
    完。
    若 操作码 等于 0x63：
        返还 2。
    完。
    若 操作码 等于 0x65：
        返还 2。
    完。
    若 操作码 等于 0x69：
        返还 2。
    完。
    若 操作码 等于 0x6A：
        返还 2。
    完。
    返还 0。
完。

Nova指令_名称 接受 操作码：
    返还 做 转字符串 用 操作码。
完。

Nova_魔数 为 0x4E4F4243。

Nova_版本 为 0x0100。

Nova_标志_调试 为 1。

Nova_标志_优化 为 2。

Nova_标志_即时编译就绪 为 4。

Nova字节码_创建块 接受 零参：
    返还 一组：Nova_魔数、Nova_版本、Nova_标志_调试、一组：（空）、一组：（空）、一组：（空）、做 字典。
完。

Nova字节码_发射 接受 块、操作码：
    做 追加 用 块 的 第 4 项、操作码。
完。

Nova字节码_发射8 接受 块、操作码、操作数：
    做 追加 用 块 的 第 4 项、操作码。
    做 追加 用 块 的 第 4 项、操作数 位与 0xFF。
完。

Nova字节码_发射16 接受 块、操作码、操作数：
    做 追加 用 块 的 第 4 项、操作码。
    做 追加 用 块 的 第 4 项、操作数 位与 0xFF。
    做 追加 用 块 的 第 4 项、操作数 右移 8 位与 0xFF。
完。

Nova字节码_发射64 接受 块、操作码、值：
    做 追加 用 块 的 第 4 项、操作码。
    可变 i 为 0。
    只要 i 小于 8：
        做 追加 用 块 的 第 4 项、值 右移 i 乘 8 位与 0xFF。
        i 为 i 加 1。
    完。
完。

Nova字节码_发射行号 接受 块、行号：
    做 追加 用 块 的 第 5 项、行号。
完。

Nova字节码_添加常量 接受 块、值：
    索引 为 做 计数 用 块 的 第 3 项。
    做 追加 用 块 的 第 3 项、值。
    返还 索引。
完。

Nova字节码_注册函数 接受 块、名称、偏移、参数数：
    做 字典设置 用 块 的 第 6 项、名称、一组：偏移、参数数。
完。

Nova字节码_代码大小 接受 块：
    返还 做 计数 用 块 的 第 4 项。
完。

Nova字节码_常量数 接受 块：
    返还 做 计数 用 块 的 第 3 项。
完。

Nova字节码_反汇编 接受 块：
    返还 0。
完。

Nova_汉字分类 为 0x70。

Nova_汉字笔画 为 0x71。

Nova_汉字部首 为 0x72。

Nova_汉字声调 为 0x73。

Nova_汉字拼音 为 0x74。

Nova_汉字比较 为 0x75。

Nova_汉字向量8 为 0x76。

Nova_汉字N元 为 0x77。

Nova_类型元编码 为 0x78。

Nova_类型元解码 为 0x79。

Nova_词法比较 为 0x7A。

Nova_声调分发 为 0x7B。

可变 _指令集_注册数 为 0。

可变 _指令集_使用数 为 0。

本章 收束。
