《公共子表达式》

引用《..·..·标准库·基础》。

引用《..·..·运行时·字典引擎·字典引擎》。

表达式哈希 接受 运算符、左标识、右标识：
    返还 运算符 乘 10007 加 左标识 乘 131 加 右标识。
完。

一元表达式哈希 接受 运算符、操作数标识：
    返还 运算符 乘 10007 加 操作数标识。
完。

表达式_哈希 接受 运算符、左编号、右编号：
    返还 运算符 乘 10007 加 左编号 乘 131 加 右编号。
完。

是可交换运算 接受 运算符：
    若 运算符 等于 1：
        返还 真。
    完。
    若 运算符 等于 3：
        返还 真。
    完。
    若 运算符 等于 6：
        返还 真。
    完。
    若 运算符 等于 7：
        返还 真。
    完。
    若 运算符 等于 12：
        返还 真。
    完。
    若 运算符 等于 13：
        返还 真。
    完。
    若 运算符 等于 14：
        返还 真。
    完。
    若 运算符 等于 20：
        返还 真。
    完。
    若 运算符 等于 21：
        返还 真。
    完。
    返还 假。
完。

正规化操作数 接受 运算符、左标识、右标识：
    若 做 是可交换运算 用 运算符 并且 左标识 大于 右标识：
        返还 一组：右标识、左标识。
    完。
    返还 一组：左标识、右标识。
完。

可消除运算 接受 运算符：
    若 运算符 大于或等于 1 并且 运算符 小于或等于 21：
        返还 真。
    完。
    返还 假。
完。

消除表条目_创建 接受 哈希、运算符、左标识、右标识、结果标识：
    返还 一组：哈希、运算符、左标识、右标识、结果标识、1。
完。

消除表条目_哈希 接受 条目：
    返还 条目 的 第 0 项。
完。

消除表条目_运算 接受 条目：
    返还 条目 的 第 1 项。
完。

消除表条目_左 接受 条目：
    返还 条目 的 第 2 项。
完。

消除表条目_右 接受 条目：
    返还 条目 的 第 3 项。
完。

消除表条目_结果 接受 条目：
    返还 条目 的 第 4 项。
完。

消除表条目_引用数 接受 条目：
    返还 条目 的 第 5 项。
完。

消除表条目_增引用 接受 条目：
    条目 的 第 5 项 为 条目 的 第 5 项 加 1。
完。

消除表_创建 接受 _容量：
    返还 一组：一组：（空）。
完。

消除表_查找 接受 表、哈希、运算符、左标识、右标识：
    条目列表 为 表 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 条目列表：
        条目 为 条目列表 的 第 i 项。
        若 做 消除表条目_哈希 用 条目 等于 哈希 并且 做 消除表条目_运算 用 条目 等于 运算符 并且 做 消除表条目_左 用 条目 等于 左标识 并且 做 消除表条目_右 用 条目 等于 右标识：
            做 消除表条目_增引用 用 条目。
            表 的 第 1 项 为 表 的 第 1 项 加 1。
            返还 做 消除表条目_结果 用 条目。
        完。
        i 为 i 加 1。
    完。
    表 的 第 2 项 为 表 的 第 2 项 加 1。
    返还 0 减 1。
完。

消除表_追加 接受 表、运算符、左标识、右标识、结果标识：
    哈希 为 做 表达式哈希 用 运算符、左标识、右标识。
    条目 为 做 消除表条目_创建 用 哈希、运算符、左标识、右标识、结果标识。
    做 追加 用 表 的 第 0 项、条目。
    返还 真。
完。

消除表_大小 接受 表：
    返还 做 计数 用 表 的 第 0 项。
完。

消除表_命中数 接受 表：
    返还 表 的 第 1 项。
完。

消除表_未命中数 接受 表：
    返还 表 的 第 2 项。
完。

消除表_命中率 接受 表：
    总 为 表 的 第 1 项 加 表 的 第 2 项。
    若 总 等于 0：
        返还 0。
    完。
    返还 表 的 第 1 项 乘 10000 除以 总。
完。

消除表_清除 接受 表：
    表 的 第 0 项 为 一组：（空）。
    表 的 第 1 项 为 0。
    表 的 第 2 项 为 0。
完。

消除表_规范查找 接受 表、运算符、左标识、右标识：
    正规 为 做 正规化操作数 用 运算符、左标识、右标识。
    哈希 为 做 表达式哈希 用 运算符、正规 的 第 0 项、正规 的 第 1 项。
    返还 做 消除表_查找 用 表、哈希、运算符、正规 的 第 0 项、正规 的 第 1 项。
完。

消除表_规范追加 接受 表、运算符、左标识、右标识、结果标识：
    正规 为 做 正规化操作数 用 运算符、左标识、右标识。
    返还 做 消除表_追加 用 表、运算符、正规 的 第 0 项、正规 的 第 1 项、结果标识。
完。

消除表_报告 接受 表：
    返还 一组：做 消除表_大小 用 表、做 消除表_命中数 用 表、做 消除表_命中率 用 表。
完。

可变 _值编号_表 为 做 字典。

可变 _值编号_下一编号 为 1。

值编号_重置 接受 零参：
    _值编号_表 为 做 字典。
    _值编号_下一编号 为 1。
完。

值编号_查找 接受 运算符、左编号、右编号：
    正规 为 做 正规化操作数 用 运算符、左编号、右编号。
    键 为 做 字符串拼接 用 做 转字符串 用 运算符、做 字符串拼接 用 「_」、做 字符串拼接 用 做 转字符串 用 正规 的 第 0 项、做 字符串拼接 用 「_」、做 转字符串 用 正规 的 第 1 项。
    v 为 做 字典取 用 _值编号_表、键。
    若 v 等于 空：
        返还 0 减 1。
    完。
    返还 v。
完。

值编号_添加 接受 运算符、左编号、右编号：
    正规 为 做 正规化操作数 用 运算符、左编号、右编号。
    键 为 做 字符串拼接 用 做 转字符串 用 运算符、做 字符串拼接 用 「_」、做 字符串拼接 用 做 转字符串 用 正规 的 第 0 项、做 字符串拼接 用 「_」、做 转字符串 用 正规 的 第 1 项。
    编号 为 _值编号_下一编号。
    做 字典设 用 _值编号_表、键、编号。
    _值编号_下一编号 为 _值编号_下一编号 加 1。
    返还 编号。
完。

值编号_清空 接受 零参：
    _值编号_表 为 做 字典。
    _值编号_下一编号 为 1。
完。

可用表达式_创建 接受 零参：
    返还 一组：（空）。
完。

可用表达式_添加 接受 集合、表达式、定义点：
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合：
        若 集合 的 第 i 项 的 第 0 项 等于 表达式：
            集合 的 第 i 项 的 第 1 项 为 定义点。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 集合、一组：表达式、定义点。
完。

可用表达式_查找 接受 集合、表达式：
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合：
        若 集合 的 第 i 项 的 第 0 项 等于 表达式：
            返还 集合 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 0 减 1。
完。

可用表达式_杀死 接受 集合、变量：
    可变 新集合 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合：
        若 集合 的 第 i 项 的 第 0 项 不等于 变量：
            做 追加 用 新集合、集合 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 新集合。
完。

全局CSE_交集 接受 集合A、集合B：
    可变 交集 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合A：
        expr 为 集合A 的 第 i 项 的 第 0 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 集合B：
            若 集合B 的 第 j 项 的 第 0 项 等于 expr：
                做 追加 用 交集、集合A 的 第 i 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 交集。
完。

全局CSE_合并 接受 集合A、集合B：
    可变 合并 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合A：
        做 追加 用 合并、集合A 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 集合B：
        expr 为 集合B 的 第 i 项 的 第 0 项。
        可变 已有 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 合并：
            若 合并 的 第 j 项 的 第 0 项 等于 expr：
                已有 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已有 等于 假：
            做 追加 用 合并、集合B 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 合并。
完。

可变 _CSE_消除总数 为 0。

可变 _CSE_分析总数 为 0。

CSE_记录消除 接受 零参：
    _CSE_消除总数 为 _CSE_消除总数 加 1。
完。

CSE_记录分析 接受 零参：
    _CSE_分析总数 为 _CSE_分析总数 加 1。
完。

CSE_重置统计 接受 零参：
    _CSE_消除总数 为 0。
    _CSE_分析总数 为 0。
完。

CSE_消除率 接受 零参：
    若 _CSE_分析总数 等于 0：
        返还 0。
    完。
    返还 _CSE_消除总数 乘 10000 除以 _CSE_分析总数。
完。

CSE_变换全部 接受 函数列表：
    若 函数列表 等于 空 或者 做 是列表 用 函数列表 等于 假：
        返还 0。
    完。
    可变 消除数 为 0。
    可变 fi 为 0。
    只要 fi 小于 做 计数 用 函数列表：
        若 做 是列表 用 函数列表 的 第 fi 项 并且 做 计数 用 函数列表 的 第 fi 项 大于或等于 4：
            体 为 函数列表 的 第 fi 项 的 第 3 项。
            若 做 是列表 用 体 并且 做 计数 用 体 大于 1：
                消除数 为 消除数 加 做 _CSE_扫描函数体 用 体。
            完。
        完。
        fi 为 fi 加 1。
    完。
    返还 消除数。
完。

_CSE_扫描函数体 接受 体：
    可变 缓存 为 做 字典。
    可变 消除数 为 0。
    可变 si 为 0。
    只要 si 小于 做 计数 用 体：
        stmt 为 体 的 第 si 项。
        若 做 是列表 用 stmt 并且 做 计数 用 stmt 大于或等于 3 并且 stmt 的 第 0 项 等于 110：
            签名 为 做 _CSE_表达式签名 用 stmt 的 第 2 项。
            若 签名 不等于 空：
                已有 为 做 字典取 用 缓存、签名。
                若 已有 不等于 空：
                    stmt 的 第 2 项 为 一组：102、已有。
                    消除数 为 消除数 加 1。
                    _CSE_消除总数 为 _CSE_消除总数 加 1。
                否则：
                    做 字典设 用 缓存、签名、stmt 的 第 1 项。
                完。
            完。
            _CSE_分析总数 为 _CSE_分析总数 加 1。
        否则：
            缓存 为 做 字典。
        完。
        si 为 si 加 1。
    完。
    返还 消除数。
完。

_CSE_表达式签名 接受 expr：
    若 expr 等于 空 或者 做 是列表 用 expr 等于 假 或者 做 计数 用 expr 小于 2：
        返还 空。
    完。
    类型 为 expr 的 第 0 项。
    若 类型 等于 103 并且 做 计数 用 expr 大于或等于 3 并且 做 是列表 用 expr 的 第 2 项 并且 做 计数 用 expr 的 第 2 项 大于 0：
        若 做 _CSE_是纯函数 用 expr 的 第 1 项 等于 假：
            返还 空。
        完。
        可变 s 为 做 字符串拼接 用 「C_」、expr 的 第 1 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 expr 的 第 2 项：
            参 为 expr 的 第 2 项 的 第 i 项。
            若 做 是列表 用 参 并且 做 计数 用 参 大于或等于 2：
                若 参 的 第 0 项 等于 102：
                    s 为 做 字符串拼接 用 s、做 字符串拼接 用 「_i」、参 的 第 1 项。
                否则若 参 的 第 0 项 等于 100：
                    s 为 做 字符串拼接 用 s、做 字符串拼接 用 「_n」、做 转字符串 用 参 的 第 1 项。
                否则：
                    返还 空。
                完。
            否则：
                返还 空。
            完。
            i 为 i 加 1。
        完。
        返还 s。
    完。
    若 类型 等于 104 并且 做 计数 用 expr 大于或等于 4：
        左签 为 做 _CSE_原子签名 用 expr 的 第 1 项。
        右签 为 做 _CSE_原子签名 用 expr 的 第 3 项。
        若 左签 不等于 空 并且 右签 不等于 空：
            返还 做 字符串拼接 用 「B_」、做 字符串拼接 用 左签、做 字符串拼接 用 「_」、做 字符串拼接 用 做 转字符串 用 expr 的 第 2 项、做 字符串拼接 用 「_」、右签。
        完。
        返还 空。
    完。
    若 类型 等于 106 并且 做 计数 用 expr 大于或等于 3：
        目签 为 做 _CSE_原子签名 用 expr 的 第 1 项。
        索签 为 做 _CSE_原子签名 用 expr 的 第 2 项。
        若 目签 不等于 空 并且 索签 不等于 空：
            返还 做 字符串拼接 用 「I_」、做 字符串拼接 用 目签、做 字符串拼接 用 「_」、索签。
        完。
        返还 空。
    完。
    返还 空。
完。

_CSE_是纯函数 接受 名：
    若 做 字符串相等 用 名、「计数」：
        返还 真。
    完。
    若 做 字符串相等 用 名、「字节长度」：
        返还 真。
    完。
    若 做 字符串相等 用 名、「字节取」：
        返还 真。
    完。
    若 做 字符串相等 用 名、「是列表」：
        返还 真。
    完。
    若 做 字符串相等 用 名、「字符串相等」：
        返还 真。
    完。
    若 做 字符串相等 用 名、「是数字」：
        返还 真。
    完。
    若 做 字符串相等 用 名、「字典有」：
        返还 真。
    完。
    返还 假。
完。

_CSE_原子签名 接受 node：
    若 node 等于 空 或者 做 是列表 用 node 等于 假 或者 做 计数 用 node 小于 2：
        返还 空。
    完。
    若 node 的 第 0 项 等于 102：
        返还 做 字符串拼接 用 「i」、node 的 第 1 项。
    完。
    若 node 的 第 0 项 等于 100：
        返还 做 字符串拼接 用 「n」、做 转字符串 用 node 的 第 1 项。
    完。
    返还 空。
完。

本章 收束。
