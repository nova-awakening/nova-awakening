《优化管线》

引用《.·公共子表达式》。

引用《.·函数内联》。

引用《.·尾调用优化》。

引用《.·常量折叠》。

引用《.·死代码消除》。

引用《.·窥孔优化》。

引用《..·..·编译器·规划·优化·SSA构建》。

引用《..·..·编译器·规划·优化·尾调用》。

引用《..·..·标准库·基础》。

引用《..·..·标准库·转换器·转换验证》。

优化_无 为 0。

优化_基础 为 1。

优化_完全 为 2。

优化_激进 为 3。

可变 当前优化等级 为 优化_无。

设置优化等级 接受 级别：
    若 级别 大于或等于 0 并且 级别 小于或等于 3：
        当前优化等级 为 级别。
    完。
完。

获取优化等级 接受 零参：
    返还 当前优化等级。
完。

可变 优化管线_目标ELF 为 1。

设置目标平台 接受 目标：
    当前目标 为 目标。
完。

获取目标平台 接受 零参：
    返还 当前目标。
完。

优化_消除函数死代码 接受 函数体：
    若 函数体 等于 空 或者 做 是列表 用 函数体 等于 假 或者 做 计数 用 函数体 等于 0：
        返还 一组：函数体、0。
    完。
    _不可达位 为 做 检测不可达 用 函数体。
    若 _不可达位 小于 0：
        返还 一组：函数体、0。
    完。
    原长 为 做 计数 用 函数体。
    只要 做 计数 用 函数体 大于 _不可达位：
        做 删除 用 函数体、做 计数 用 函数体 减 1。
    完。
    返还 一组：函数体、原长 减 _不可达位。
完。

优化_全部函数 接受 函数列表：
    若 函数列表 等于 空 或者 做 是列表 用 函数列表 等于 假：
        返还 0。
    完。
    可变 总删除 为 0。
    可变 _优fi 为 0。
    只要 _优fi 小于 做 计数 用 函数列表：
        若 做 是列表 用 函数列表 的 第 _优fi 项 并且 做 计数 用 函数列表 的 第 _优fi 项 大于或等于 4：
            _优函体 为 函数列表 的 第 _优fi 项 的 第 3 项。
            若 做 是列表 用 _优函体 并且 做 计数 用 _优函体 大于 0：
                结果 为 做 优化_消除函数死代码 用 _优函体。
                总删除 为 总删除 加 结果 的 第 1 项。
            完。
        完。
        _优fi 为 _优fi 加 1。
    完。
    返还 总删除。
完。

优化管线_执行 接受 函数列表、主体列表：
    可变 报告 为 一组：0、0、0、0。
    级别 为 当前优化等级。
    做 管线_记录遍执行。
    做 折叠_重置统计。
    折叠结果 为 做 折叠_多轮迭代 用 4、函数列表。
    报告 的 第 0 项 为 折叠结果 的 第 1 项。
    删除数 为 做 优化_全部函数 用 函数列表。
    报告 的 第 1 项 为 删除数。
    主体结果 为 做 优化_消除函数死代码 用 主体列表。
    报告 的 第 2 项 为 主体结果 的 第 1 项。
    若 级别 小于 优化_完全：
        返还 报告。
    完。
    _内联器 为 做 函数内联_内联器_创建。
    做 调用图_创建。
    做 内联_重置深度。
    可变 _候选数 为 0。
    可变 _内fi 为 0。
    只要 _内fi 小于 做 计数 用 函数列表：
        若 做 是列表 用 函数列表 的 第 _内fi 项 并且 做 计数 用 函数列表 的 第 _内fi 项 大于或等于 4：
            _内名 为 函数列表 的 第 _内fi 项 的 第 1 项。
            _内体 为 函数列表 的 第 _内fi 项 的 第 3 项。
            _内参 为 函数列表 的 第 _内fi 项 的 第 2 项。
            可变 _节点数 为 0。
            若 做 是列表 用 _内体：
                _节点数 为 做 计数 用 _内体。
            完。
            可变 _参数数 为 0。
            若 做 是列表 用 _内参：
                _参数数 为 做 计数 用 _内参。
            完。
            做 函数内联_内联器_注册 用 _内联器、_内名、_节点数、_参数数。
            _信息 为 做 函数内联_内联器_查找 用 _内联器、_内名。
            若 _信息 不等于 空 并且 做 函数内联_值得内联 用 _信息：
                _候选数 为 _候选数 加 1。
            完。
        完。
        _内fi 为 _内fi 加 1。
    完。
    报告 的 第 3 项 为 _候选数。
    _内联数 为 做 内联_变换全部 用 函数列表。
    报告 的 第 3 项 为 报告 的 第 3 项 加 _内联数 乘 1000。
    _tco数 为 做 尾调用优_变换全部 用 函数列表。
    报告 的 第 4 项 为 _tco数。
    做 CSE_重置统计。
    做 值编号_重置。
    _cse数 为 做 CSE_变换全部 用 函数列表。
    报告 的 第 5 项 为 _cse数。
    返还 报告。
完。

优化管线_报告 接受 报告：
    做 打印文本 用 做 字符串拼接 用 「  常量折叠: 」、做 字符串拼接 用 做 转字符串 用 报告 的 第 0 项、「次」。
    做 打印文本 用 做 字符串拼接 用 「  死代码消除: 」、做 字符串拼接 用 做 转字符串 用 报告 的 第 1 项 加 报告 的 第 2 项、「条语句」。
    做 打印文本 用 做 字符串拼接 用 「  内联候选: 」、做 字符串拼接 用 做 转字符串 用 报告 的 第 3 项、「个函数」。
完。

可变 _遍_列表 为 一组：（空）。

可变 _遍_依赖前 为 一组：（空）。

可变 _遍_依赖后 为 一组：（空）。

优化遍_注册 接受 名称、优先级：
    id 为 做 计数 用 _遍_列表。
    做 追加 用 _遍_列表、一组：名称、优先级、1、0。
    返还 id。
完。

优化遍_禁用 接受 id：
    若 id 大于或等于 0 并且 id 小于 做 计数 用 _遍_列表：
        _遍_列表 的 第 id 项 的 第 2 项 为 0。
    完。
完。

优化遍_启用 接受 id：
    若 id 大于或等于 0 并且 id 小于 做 计数 用 _遍_列表：
        _遍_列表 的 第 id 项 的 第 2 项 为 1。
    完。
完。

优化遍_查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _遍_列表：
        若 _遍_列表 的 第 i 项 的 第 0 项 等于 名称：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 0 减 1。
完。

优化遍_已启用列表 接受 零参：
    n 为 做 计数 用 _遍_列表。
    可变 已启用 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 _遍_列表 的 第 i 项 的 第 2 项 不等于 0：
            做 追加 用 已启用、i。
        完。
        i 为 i 加 1。
    完。
    m 为 做 计数 用 已启用。
    i 为 0。
    只要 i 小于 m 减 1：
        可变 最小idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 m：
            若 _遍_列表 的 第 已启用 的 第 j 项 项 的 第 1 项 小于 _遍_列表 的 第 已启用 的 第 最小idx 项 项 的 第 1 项：
                最小idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最小idx 不等于 i：
            暂存 为 已启用 的 第 i 项。
            已启用 的 第 i 项 为 已启用 的 第 最小idx 项。
            已启用 的 第 最小idx 项 为 暂存。
        完。
        i 为 i 加 1。
    完。
    返还 已启用。
完。

优化遍_按优先级排序 接受 零参：
    返还 做 优化遍_已启用列表。
完。

优化管线_预设O0 接受 零参：
    _遍_列表 为 一组：（空）。
    做 设置优化等级 用 优化_无。
完。

优化管线_预设O1 接受 零参：
    _遍_列表 为 一组：（空）。
    做 优化遍_注册 用 「常量折叠」、10。
    做 优化遍_注册 用 「死代码消除」、20。
    做 设置优化等级 用 优化_基础。
完。

优化管线_预设O2 接受 零参：
    _遍_列表 为 一组：（空）。
    做 优化遍_注册 用 「常量折叠」、10。
    做 优化遍_注册 用 「死代码消除」、20。
    做 优化遍_注册 用 「公共子表达式」、30。
    做 优化遍_注册 用 「函数内联」、40。
    做 优化遍_注册 用 「尾调用优化」、50。
    做 设置优化等级 用 优化_完全。
完。

优化管线_预设O3 接受 零参：
    做 优化管线_预设O2。
    做 优化遍_注册 用 「窥孔优化」、60。
    做 优化遍_注册 用 「强度削减」、70。
    做 设置优化等级 用 优化_激进。
完。

优化管线_预设Os 接受 零参：
    _遍_列表 为 一组：（空）。
    做 优化遍_注册 用 「死代码消除」、10。
    做 优化遍_注册 用 「函数内联」、20。
    做 优化遍_注册 用 「常量折叠」、30。
    做 设置优化等级 用 优化_完全。
完。

管线_添加依赖 接受 前遍名、后遍名：
    做 追加 用 _遍_依赖前、前遍名。
    做 追加 用 _遍_依赖后、后遍名。
完。

管线_检查依赖 接受 遍名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _遍_依赖后：
        若 _遍_依赖后 的 第 i 项 等于 遍名：
            前遍 为 _遍_依赖前 的 第 i 项。
            若 做 优化遍_查找 用 前遍 小于 0：
                返还 假。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

可变 _遍_耗时 为 一组：（空）。

管线_记录耗时 接受 遍索引、耗时：
    只要 做 计数 用 _遍_耗时 小于或等于 遍索引：
        做 追加 用 _遍_耗时、0。
    完。
    _遍_耗时 的 第 遍索引 项 为 _遍_耗时 的 第 遍索引 项 加 耗时。
完。

管线_最慢遍 接受 零参：
    若 做 计数 用 _遍_耗时 等于 0：
        返还 一组：「无」、0。
    完。
    可变 最慢idx 为 0。
    可变 最慢时 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _遍_耗时：
        若 _遍_耗时 的 第 i 项 大于 最慢时：
            最慢时 为 _遍_耗时 的 第 i 项。
            最慢idx 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最慢idx 小于 做 计数 用 _遍_列表：
        返还 一组：_遍_列表 的 第 最慢idx 项 的 第 0 项、最慢时。
    完。
    返还 一组：「unknown」、最慢时。
完。

管线_总耗时 接受 零参：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _遍_耗时：
        总 为 总 加 _遍_耗时 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

管线_条件执行 接受 遍索引、条件：
    若 条件 等于 假 或者 条件 等于 0：
        返还 假。
    完。
    若 遍索引 小于 0 或者 遍索引 大于或等于 做 计数 用 _遍_列表：
        返还 假。
    完。
    若 _遍_列表 的 第 遍索引 项 的 第 2 项 等于 0：
        返还 假。
    完。
    _遍_列表 的 第 遍索引 项 的 第 3 项 为 _遍_列表 的 第 遍索引 项 的 第 3 项 加 1。
    返还 真。
完。

管线_迭代执行 接受 最大轮数：
    可变 轮数 为 0。
    可变 上轮折叠数 为 0 减 1。
    只要 轮数 小于 最大轮数：
        做 折叠_重置统计。
        报告 为 做 优化管线_执行 用 一组：（空）、一组：（空）。
        本轮折叠 为 报告 的 第 0 项。
        若 本轮折叠 等于 0 并且 报告 的 第 1 项 等于 0：
            返还 轮数 加 1。
        完。
        若 本轮折叠 等于 上轮折叠数：
            返还 轮数 加 1。
        完。
        上轮折叠数 为 本轮折叠。
        轮数 为 轮数 加 1。
    完。
    返还 最大轮数。
完。

可变 _管线_运行数 为 0。

可变 _管线_遍执行总数 为 0。

管线_记录运行 接受 零参：
    _管线_运行数 为 _管线_运行数 加 1。
完。

管线_记录遍执行 接受 零参：
    _管线_遍执行总数 为 _管线_遍执行总数 加 1。
完。

管线_运行次数 接受 零参：
    返还 _管线_运行数。
完。

管线_遍执行总数 接受 零参：
    返还 _管线_遍执行总数。
完。

IR标签_普通 为 0。

IR标签_约化 为 1。

IR标签_广播 为 2。

IR标签_注意力 为 3。

IR标签_采样 为 4。

IR标签_检索 为 5。

IR标签_量化 为 6。

IR标签_稀疏 为 7。

IR标签_图传播 为 8。

IR标签名 接受 t：
    若 t 等于 IR标签_普通：
        返还 「普通」。
    完。
    若 t 等于 IR标签_约化：
        返还 「约化」。
    完。
    若 t 等于 IR标签_广播：
        返还 「广播」。
    完。
    若 t 等于 IR标签_注意力：
        返还 「注意力」。
    完。
    若 t 等于 IR标签_采样：
        返还 「采样」。
    完。
    若 t 等于 IR标签_检索：
        返还 「检索」。
    完。
    若 t 等于 IR标签_量化：
        返还 「量化」。
    完。
    若 t 等于 IR标签_稀疏：
        返还 「稀疏」。
    完。
    若 t 等于 IR标签_图传播：
        返还 「图传播」。
    完。
    返还 「unknown」。
完。

模态_无 为 0。

模态_文本 为 1。

模态_视觉 为 2。

模态_音频 为 3。

模态_行为 为 4。

模态_DOM 为 5。

模态_多模态 为 6。

优化管线_模态名 接受 m：
    若 m 等于 模态_无：
        返还 「无」。
    完。
    若 m 等于 模态_文本：
        返还 「文本」。
    完。
    若 m 等于 模态_视觉：
        返还 「视觉」。
    完。
    若 m 等于 模态_音频：
        返还 「音频」。
    完。
    若 m 等于 模态_行为：
        返还 「行为」。
    完。
    若 m 等于 模态_DOM：
        返还 「DOM」。
    完。
    若 m 等于 模态_多模态：
        返还 「多模态」。
    完。
    返还 「未知模态」。
完。

可变 _IR标签表 为 空。

IR标签表_初始化 接受 零参：
    _IR标签表 为 做 字典。
完。

IR标签表_设置 接受 节点ID、算子标签、模态标签、记忆标签、量化标签：
    若 _IR标签表 等于 空：
        做 IR标签表_初始化。
    完。
    做 字典设 用 _IR标签表、做 转字符串 用 节点ID、一组：算子标签、模态标签、记忆标签、量化标签。
完。

IR标签表_查询 接受 节点ID：
    若 _IR标签表 等于 空：
        返还 空。
    完。
    返还 做 字典取 用 _IR标签表、做 转字符串 用 节点ID。
完。

IR标签表_是AI路径 接受 节点ID：
    标签 为 做 IR标签表_查询 用 节点ID。
    若 标签 等于 空：
        返还 假。
    完。
    返还 标签 的 第 0 项 不等于 IR标签_普通。
完。

IR标签表_算子 接受 节点ID：
    标签 为 做 IR标签表_查询 用 节点ID。
    若 标签 等于 空：
        返还 IR标签_普通。
    完。
    返还 标签 的 第 0 项。
完。

IR标签表_模态 接受 节点ID：
    标签 为 做 IR标签表_查询 用 节点ID。
    若 标签 等于 空：
        返还 模态_无。
    完。
    返还 标签 的 第 1 项。
完。

IR标签_从函数名推断 接受 名称：
    若 名称 等于 空：
        返还 IR标签_普通。
    完。
    若 做 字符串包含 用 名称、「注意力」 或者 做 字符串包含 用 名称、「attention」：
        返还 IR标签_注意力。
    完。
    若 做 字符串包含 用 名称、「量化」 或者 做 字符串包含 用 名称、「quant」：
        返还 IR标签_量化。
    完。
    若 做 字符串包含 用 名称、「检索」 或者 做 字符串包含 用 名称、「retrieve」：
        返还 IR标签_检索。
    完。
    若 做 字符串包含 用 名称、「采样」 或者 做 字符串包含 用 名称、「sample」：
        返还 IR标签_采样。
    完。
    若 做 字符串包含 用 名称、「稀疏」 或者 做 字符串包含 用 名称、「sparse」：
        返还 IR标签_稀疏。
    完。
    若 做 字符串包含 用 名称、「约化」 或者 做 字符串包含 用 名称、「reduce」：
        返还 IR标签_约化。
    完。
    若 做 字符串包含 用 名称、「广播」 或者 做 字符串包含 用 名称、「broadcast」：
        返还 IR标签_广播。
    完。
    若 做 字符串包含 用 名称、「图传播」 或者 做 字符串包含 用 名称、「propagate」：
        返还 IR标签_图传播。
    完。
    返还 IR标签_普通。
完。

IR标签_从函数名推断模态 接受 名称：
    若 名称 等于 空：
        返还 模态_无。
    完。
    若 做 字符串包含 用 名称、「DOM」 或者 做 字符串包含 用 名称、「dom_」：
        返还 模态_DOM。
    完。
    若 做 字符串包含 用 名称、「行为序列」 或者 做 字符串包含 用 名称、「行为_」：
        返还 模态_行为。
    完。
    若 做 字符串包含 用 名称、「视觉」 或者 做 字符串包含 用 名称、「图像」：
        返还 模态_视觉。
    完。
    若 做 字符串包含 用 名称、「音频」 或者 做 字符串包含 用 名称、「语音」：
        返还 模态_音频。
    完。
    若 做 字符串包含 用 名称、「文本」 或者 做 字符串包含 用 名称、「token」：
        返还 模态_文本。
    完。
    返还 模态_无。
完。

可变 _管线_裸机模式 为 假。

管线_设置裸机模式 接受 启用：
    _管线_裸机模式 为 启用。
完。

管线_是裸机 接受 零参：
    返还 _管线_裸机模式。
完。

管线_函数可内联 接受 函数名：
    若 _管线_裸机模式 等于 假：
        返还 真。
    完。
    若 做 字符串包含 用 函数名、「启动」 或者 做 字符串包含 用 函数名、「boot」：
        返还 假。
    完。
    若 做 字符串包含 用 函数名、「中断」 或者 做 字符串包含 用 函数名、「interrupt」：
        返还 假。
    完。
    若 做 字符串包含 用 函数名、「页表」 或者 做 字符串包含 用 函数名、「page_table」：
        返还 假。
    完。
    若 做 字符串包含 用 函数名、「异常」 或者 做 字符串包含 用 函数名、「exception」：
        返还 假。
    完。
    若 做 字符串包含 用 函数名、「特权」 或者 做 字符串包含 用 函数名、「privilege」：
        返还 假。
    完。
    返还 真。
完。

可变 _管线_AI节点数 为 0。

可变 _管线_裸机禁内联 为 0。

管线_记录AI节点 接受 零参：
    _管线_AI节点数 为 _管线_AI节点数 加 1。
完。

管线_记录裸机禁内联 接受 零参：
    _管线_裸机禁内联 为 _管线_裸机禁内联 加 1。
完。

优化管线_结构化报告 接受 零参：
    可变 报告 为 做 字典。
    做 字典设 用 报告、「管线运行数」、做 转字符串 用 _管线_运行数。
    做 字典设 用 报告、「遍执行总数」、做 转字符串 用 _管线_遍执行总数。
    做 字典设 用 报告、「AI路径节点数」、做 转字符串 用 _管线_AI节点数。
    做 字典设 用 报告、「裸机禁内联数」、做 转字符串 用 _管线_裸机禁内联。
    做 字典设 用 报告、「优化等级」、做 转字符串 用 当前优化等级。
    做 字典设 用 报告、「裸机模式」、做 转字符串 用 _管线_裸机模式。
    可变 遍数 为 做 计数 用 _遍_列表。
    做 字典设 用 报告、「注册遍数」、做 转字符串 用 遍数。
    返还 报告。
完。

优化管线_打印结构化报告 接受 零参：
    r 为 做 优化管线_结构化报告。
    做 打印文本 用 做 字符串拼接 用 「[OPT-REPORT] 管线运行=」、做 字符串拼接 用 做 字典取 用 r、「管线运行数」、做 字符串拼接 用 「 遍执行=」、做 字符串拼接 用 做 字典取 用 r、「遍执行总数」、做 字符串拼接 用 「 AI节点=」、做 字符串拼接 用 做 字典取 用 r、「AI路径节点数」、做 字符串拼接 用 「 裸机禁内联=」、做 字典取 用 r、「裸机禁内联数」。
完。

本章 收束。
