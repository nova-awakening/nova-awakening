《常量折叠》

引用《..·..·编译器·规划·核心·常量》。

运算_加 为 「+」。

运算_减 为 「-」。

运算_乘 为 「*」。

运算_除 为 「/」。

运算_余 为 「%」。

运算_等于 为 「==」。

运算_不等 为 「!=」。

运算_小于 为 「<」。

运算_大于 为 「>」。

运算_小等 为 「<=」。

运算_大等 为 「>=」。

运算_位与 为 「&」。

运算_位或 为 「|」。

运算_位异或 为 「^」。

运算_左移 为 「<<」。

运算_右移 为 「>>」。

运算_取负 为 「-」。

运算_逻辑非 为 「!」。

运算_位非 为 「~」。

运算_逻辑与 为 「&&」。

运算_逻辑或 为 「||」。

可变 折叠_总次数 为 0。

可变 折叠_二元次数 为 0。

可变 折叠_一元次数 为 0。

可变 折叠_代数次数 为 0。

可变 折叠_条件次数 为 0。

可变 折叠_强度次数 为 0。

可变 折叠_浮点次数 为 0。

折叠_重置统计 接受 零参：
    折叠_总次数 为 0。
    折叠_二元次数 为 0。
    折叠_一元次数 为 0。
    折叠_代数次数 为 0。
    折叠_条件次数 为 0。
    折叠_强度次数 为 0。
    折叠_浮点次数 为 0。
完。

折叠_统计报告 接受 零参：
    返还 一组：折叠_总次数、折叠_二元次数、折叠_一元次数、折叠_代数次数、折叠_强度次数、折叠_条件次数。
完。

折叠_二元运算 接受 运算符、左值、右值：
    折叠_二元次数 为 折叠_二元次数 加 1。
    折叠_总次数 为 折叠_总次数 加 1。
    若 运算符 等于 运算_加：
        返还 左值 加 右值。
    完。
    若 运算符 等于 运算_减：
        返还 左值 减 右值。
    完。
    若 运算符 等于 运算_乘：
        返还 左值 乘 右值。
    完。
    若 运算符 等于 运算_除：
        若 右值 等于 0：
            返还 空。
        完。
        返还 左值 除以 右值。
    完。
    若 运算符 等于 运算_余：
        若 右值 等于 0：
            返还 空。
        完。
        返还 左值 取余于 右值。
    完。
    若 运算符 等于 运算_等于：
        若 左值 等于 右值：
            返还 1。
        完。
        返还 0。
    完。
    若 运算符 等于 运算_不等：
        若 左值 不等于 右值：
            返还 1。
        完。
        返还 0。
    完。
    若 运算符 等于 运算_小于：
        若 左值 小于 右值：
            返还 1。
        完。
        返还 0。
    完。
    若 运算符 等于 运算_大于：
        若 左值 大于 右值：
            返还 1。
        完。
        返还 0。
    完。
    若 运算符 等于 运算_小等：
        若 左值 小于或等于 右值：
            返还 1。
        完。
        返还 0。
    完。
    若 运算符 等于 运算_大等：
        若 左值 大于或等于 右值：
            返还 1。
        完。
        返还 0。
    完。
    若 运算符 等于 运算_位与：
        返还 左值 位与 右值。
    完。
    若 运算符 等于 运算_位或：
        返还 左值 位或 右值。
    完。
    若 运算符 等于 运算_位异或：
        返还 左值 位异或 右值。
    完。
    若 运算符 等于 运算_左移：
        若 右值 小于 0 或者 右值 大于 63：
            返还 空。
        完。
        返还 左值 左移 右值。
    完。
    若 运算符 等于 运算_右移：
        若 右值 小于 0 或者 右值 大于 63：
            返还 空。
        完。
        返还 左值 右移 右值。
    完。
    若 运算符 等于 运算_逻辑与：
        若 左值 不等于 0 并且 右值 不等于 0：
            返还 1。
        完。
        返还 0。
    完。
    若 运算符 等于 运算_逻辑或：
        若 左值 不等于 0 或者 右值 不等于 0：
            返还 1。
        完。
        返还 0。
    完。
    返还 空。
完。

折叠_一元运算 接受 运算符、值：
    折叠_一元次数 为 折叠_一元次数 加 1。
    折叠_总次数 为 折叠_总次数 加 1。
    若 运算符 等于 运算_取负：
        返还 0 减 值。
    完。
    若 运算符 等于 运算_逻辑非：
        若 值 等于 0：
            返还 1。
        完。
        返还 0。
    完。
    若 运算符 等于 运算_位非：
        返还 0 减 值 减 1。
    完。
    返还 空。
完。

常量折叠_代数简化 接受 运算符、左值、右值、左常量、右常量：
    折叠_代数次数 为 折叠_代数次数 加 1。
    折叠_总次数 为 折叠_总次数 加 1。
    若 运算符 等于 运算_加：
        若 右常量 等于 1 并且 右值 等于 0：
            返还 一组：「left」、0。
        完。
        若 左常量 等于 1 并且 左值 等于 0：
            返还 一组：「right」、0。
        完。
    完。
    若 运算符 等于 运算_减：
        若 右常量 等于 1 并且 右值 等于 0：
            返还 一组：「left」、0。
        完。
    完。
    若 运算符 等于 运算_乘：
        若 右常量 等于 1 并且 右值 等于 1：
            返还 一组：「left」、0。
        完。
        若 左常量 等于 1 并且 左值 等于 1：
            返还 一组：「right」、0。
        完。
        若 右常量 等于 1 并且 右值 等于 0：
            返还 一组：「const」、0。
        完。
        若 左常量 等于 1 并且 左值 等于 0：
            返还 一组：「const」、0。
        完。
    完。
    若 运算符 等于 运算_除：
        若 右常量 等于 1 并且 右值 等于 1：
            返还 一组：「left」、0。
        完。
    完。
    若 运算符 等于 运算_位与：
        若 右常量 等于 1 并且 右值 等于 0：
            返还 一组：「const」、0。
        完。
        若 左常量 等于 1 并且 左值 等于 0：
            返还 一组：「const」、0。
        完。
    完。
    若 运算符 等于 运算_位或：
        若 右常量 等于 1 并且 右值 等于 0：
            返还 一组：「left」、0。
        完。
        若 左常量 等于 1 并且 左值 等于 0：
            返还 一组：「right」、0。
        完。
    完。
    若 运算符 等于 运算_位异或：
        若 右常量 等于 1 并且 右值 等于 0：
            返还 一组：「left」、0。
        完。
        若 左常量 等于 1 并且 左值 等于 0：
            返还 一组：「right」、0。
        完。
    完。
    若 运算符 等于 运算_左移 或者 运算符 等于 运算_右移：
        若 右常量 等于 1 并且 右值 等于 0：
            返还 一组：「left」、0。
        完。
    完。
    返还 空。
完。

强度削减 接受 运算符、右值、右常量：
    若 右常量 不等于 1：
        返还 空。
    完。
    若 右值 小于或等于 0：
        返还 空。
    完。
    若 运算符 等于 运算_乘 并且 做 是二的幂 用 右值：
        折叠_强度次数 为 折叠_强度次数 加 1。
        折叠_总次数 为 折叠_总次数 加 1。
        返还 一组：运算_左移、做 二的幂指数 用 右值。
    完。
    若 运算符 等于 运算_除 并且 做 是二的幂 用 右值：
        折叠_强度次数 为 折叠_强度次数 加 1。
        折叠_总次数 为 折叠_总次数 加 1。
        返还 一组：运算_右移、做 二的幂指数 用 右值。
    完。
    若 运算符 等于 运算_余 并且 做 是二的幂 用 右值：
        折叠_强度次数 为 折叠_强度次数 加 1。
        折叠_总次数 为 折叠_总次数 加 1。
        返还 一组：运算_位与、右值 减 1。
    完。
    返还 空。
完。

通用强度削减 接受 运算符、右值、右常量：
    基础 为 做 强度削减 用 运算符、右值、右常量。
    若 基础 不等于 空：
        返还 基础。
    完。
    若 运算符 等于 运算_乘 并且 右常量 等于 1：
        若 右值 等于 3：
            返还 一组：「lea3」、0。
        完。
        若 右值 等于 5：
            返还 一组：「lea5」、0。
        完。
        若 右值 等于 9：
            返还 一组：「lea9」、0。
        完。
    完。
    返还 空。
完。

条件折叠 接受 条件值、条件常量：
    若 条件常量 不等于 1：
        返还 空。
    完。
    折叠_条件次数 为 折叠_条件次数 加 1。
    折叠_总次数 为 折叠_总次数 加 1。
    若 条件值 不等于 0：
        返还 「always_true」。
    完。
    返还 「always_false」。
完。

是二的幂 接受 n：
    若 n 小于或等于 0：
        返还 假。
    完。
    返还 n 位与 n 减 1 等于 0。
完。

二的幂指数 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 exp 为 0。
    可变 v 为 n。
    只要 v 大于 1：
        v 为 v 右移 1。
        exp 为 exp 加 1。
    完。
    返还 exp。
完。

折叠_浮点二元 接受 运算符、左值、右值：
    折叠_浮点次数 为 折叠_浮点次数 加 1。
    若 运算符 等于 运算_加：
        返还 左值 加 右值。
    完。
    若 运算符 等于 运算_减：
        返还 左值 减 右值。
    完。
    若 运算符 等于 运算_乘：
        返还 左值 乘 右值 除以 10000。
    完。
    若 运算符 等于 运算_除：
        若 右值 等于 0：
            返还 空。
        完。
        返还 左值 乘 10000 除以 右值。
    完。
    返还 空。
完。

折叠_浮点一元 接受 运算符、值：
    若 运算符 等于 运算_取负：
        返还 0 减 值。
    完。
    返还 空。
完。

浮点代数简化 接受 运算符、右值、右常量：
    若 右常量 不等于 1：
        返还 空。
    完。
    若 运算符 等于 运算_乘 并且 右值 等于 10000：
        返还 一组：「identity」、0。
    完。
    若 运算符 等于 运算_除 并且 右值 等于 10000：
        返还 一组：「identity」、0。
    完。
    返还 空。
完。

可变 折叠_轮次 为 0。

可变 折叠_本轮次数 为 0。

可变 折叠_上轮次数 为 0。

折叠_开始新轮次 接受 零参：
    折叠_轮次 为 折叠_轮次 加 1。
    折叠_上轮次数 为 折叠_本轮次数。
    折叠_本轮次数 为 0。
完。

折叠_记录本轮 接受 n：
    折叠_本轮次数 为 折叠_本轮次数 加 n。
    折叠_总次数 为 折叠_总次数 加 n。
完。

折叠_达到不动点 接受 零参：
    返还 折叠_本轮次数 等于 0。
完。

折叠_多轮迭代 接受 最大轮次、函数列表：
    可变 轮次限制 为 最大轮次。
    若 最大轮次 大于 8：
        轮次限制 为 8。
    完。
    折叠_轮次 为 0。
    可变 总折叠 为 0。
    可变 i 为 0。
    只要 i 小于 轮次限制：
        做 折叠_开始新轮次。
        若 函数列表 不等于 空 并且 做 是列表 用 函数列表：
            可变 fi 为 0。
            只要 fi 小于 做 计数 用 函数列表：
                函数项 为 函数列表 的 第 fi 项。
                若 做 是列表 用 函数项 并且 做 计数 用 函数项 大于或等于 4 并且 做 是列表 用 函数项 的 第 3 项：
                    本轮 为 做 _折叠_语句列表 用 函数项 的 第 3 项。
                    做 折叠_记录本轮 用 本轮。
                完。
                fi 为 fi 加 1。
            完。
        完。
        若 做 折叠_达到不动点：
            i 为 轮次限制。
        完。
        总折叠 为 总折叠 加 折叠_本轮次数。
        i 为 i 加 1。
    完。
    返还 一组：折叠_轮次、总折叠。
完。

_折叠_语句列表 接受 语句列表：
    若 语句列表 等于 空 或者 做 是列表 用 语句列表 等于 假：
        返还 0。
    完。
    可变 折叠数 为 0。
    可变 si 为 0。
    只要 si 小于 做 计数 用 语句列表：
        语句 为 语句列表 的 第 si 项。
        若 做 是列表 用 语句 并且 做 计数 用 语句 大于或等于 2：
            类型 为 语句 的 第 0 项。
            若 类型 等于 节_定义 并且 做 计数 用 语句 大于或等于 3：
                结果 为 做 _折叠_表达式 用 语句 的 第 2 项。
                若 结果 不等于 空：
                    语句 的 第 2 项 为 结果。
                    折叠数 为 折叠数 加 1。
                完。
            完。
            若 类型 等于 节_赋值 并且 做 计数 用 语句 大于或等于 3：
                结果 为 做 _折叠_表达式 用 语句 的 第 2 项。
                若 结果 不等于 空：
                    语句 的 第 2 项 为 结果。
                    折叠数 为 折叠数 加 1。
                完。
            完。
            若 类型 等于 节_返回 并且 做 计数 用 语句 大于或等于 2：
                结果 为 做 _折叠_表达式 用 语句 的 第 1 项。
                若 结果 不等于 空：
                    语句 的 第 1 项 为 结果。
                    折叠数 为 折叠数 加 1。
                完。
            完。
            若 类型 等于 节_如果 并且 做 计数 用 语句 大于或等于 3：
                结果 为 做 _折叠_表达式 用 语句 的 第 1 项。
                若 结果 不等于 空：
                    语句 的 第 1 项 为 结果。
                    折叠数 为 折叠数 加 1。
                完。
                若 做 是列表 用 语句 的 第 2 项：
                    折叠数 为 折叠数 加 做 _折叠_语句列表 用 语句 的 第 2 项。
                完。
                若 做 计数 用 语句 大于或等于 4 并且 做 是列表 用 语句 的 第 3 项：
                    折叠数 为 折叠数 加 做 _折叠_语句列表 用 语句 的 第 3 项。
                完。
            完。
            若 类型 等于 节_当 并且 做 计数 用 语句 大于或等于 3：
                结果 为 做 _折叠_表达式 用 语句 的 第 1 项。
                若 结果 不等于 空：
                    语句 的 第 1 项 为 结果。
                    折叠数 为 折叠数 加 1。
                完。
                若 做 是列表 用 语句 的 第 2 项：
                    折叠数 为 折叠数 加 做 _折叠_语句列表 用 语句 的 第 2 项。
                完。
            完。
            若 类型 等于 节_表达式 并且 做 计数 用 语句 大于或等于 2：
                结果 为 做 _折叠_表达式 用 语句 的 第 1 项。
                若 结果 不等于 空：
                    语句 的 第 1 项 为 结果。
                    折叠数 为 折叠数 加 1。
                完。
            完。
        完。
        si 为 si 加 1。
    完。
    返还 折叠数。
完。

_折叠_表达式 接受 表达式：
    若 表达式 等于 空 或者 做 是列表 用 表达式 等于 假 或者 做 计数 用 表达式 小于 2：
        返还 空。
    完。
    类型 为 表达式 的 第 0 项。
    若 类型 等于 节_二元 并且 做 计数 用 表达式 大于或等于 4：
        运算符 为 表达式 的 第 1 项。
        可变 左 为 表达式 的 第 2 项。
        可变 右 为 表达式 的 第 3 项。
        左折 为 做 _折叠_表达式 用 左。
        若 左折 不等于 空：
            表达式 的 第 2 项 为 左折。
            左 为 左折。
        完。
        右折 为 做 _折叠_表达式 用 右。
        若 右折 不等于 空：
            表达式 的 第 3 项 为 右折。
            右 为 右折。
        完。
        若 做 是列表 用 左 并且 做 计数 用 左 大于或等于 2 并且 左 的 第 0 项 等于 节_整数 并且 做 是列表 用 右 并且 做 计数 用 右 大于或等于 2 并且 右 的 第 0 项 等于 节_整数：
            结果 为 做 折叠_二元运算 用 运算符、左 的 第 1 项、右 的 第 1 项。
            若 结果 不等于 空：
                返还 一组：节_整数、结果。
            完。
        完。
        可变 左常 为 0。
        可变 右常 为 0。
        可变 左值 为 0。
        可变 右值 为 0。
        若 做 是列表 用 左 并且 做 计数 用 左 大于或等于 2 并且 左 的 第 0 项 等于 节_整数：
            左常 为 1。
            左值 为 左 的 第 1 项。
        完。
        若 做 是列表 用 右 并且 做 计数 用 右 大于或等于 2 并且 右 的 第 0 项 等于 节_整数：
            右常 为 1。
            右值 为 右 的 第 1 项。
        完。
        若 左常 等于 1 或者 右常 等于 1：
            简化 为 做 常量折叠_代数简化 用 运算符、左值、右值、左常、右常。
            若 简化 不等于 空：
                若 简化 的 第 0 项 等于 「left」：
                    返还 左。
                完。
                若 简化 的 第 0 项 等于 「right」：
                    返还 右。
                完。
                若 简化 的 第 0 项 等于 「const」：
                    返还 一组：节_整数、简化 的 第 1 项。
                完。
            完。
            若 右常 等于 1：
                削减 为 做 强度削减 用 运算符、右值、右常。
                若 削减 不等于 空：
                    表达式 的 第 1 项 为 削减 的 第 0 项。
                    表达式 的 第 3 项 为 一组：节_整数、削减 的 第 1 项。
                完。
            完。
        完。
    完。
    若 (类型 等于 节_逻辑非 或者 类型 等于 节_位非) 并且 做 计数 用 表达式 大于或等于 2：
        可变 操作数 为 表达式 的 第 1 项。
        子折 为 做 _折叠_表达式 用 操作数。
        若 子折 不等于 空：
            表达式 的 第 1 项 为 子折。
            操作数 为 子折。
        完。
        若 做 是列表 用 操作数 并且 做 计数 用 操作数 大于或等于 2 并且 操作数 的 第 0 项 等于 节_整数：
            可变 一元运算符 为 运算_逻辑非。
            若 类型 等于 节_位非：
                一元运算符 为 运算_位非。
            完。
            结果 为 做 折叠_一元运算 用 一元运算符、操作数 的 第 1 项。
            若 结果 不等于 空：
                返还 一组：节_整数、结果。
            完。
        完。
    完。
    返还 空。
完。

折叠_效率报告 接受 零参：
    总折叠 为 折叠_总次数。
    若 总折叠 等于 0：
        返还 「无可折叠表达式」。
    完。
    二元占比 为 折叠_二元次数 乘 100 除以 总折叠。
    代数占比 为 折叠_代数次数 乘 100 除以 总折叠。
    强度占比 为 折叠_强度次数 乘 100 除以 总折叠。
    返还 「总折叠=」 加 做 转字符串 用 总折叠 加 「 二元=」 加 做 转字符串 用 二元占比 加 「%」 加 「 代数=」 加 做 转字符串 用 代数占比 加 「%」 加 「 强度削减=」 加 做 转字符串 用 强度占比 加 「%」。
完。

本章 收束。
