《窥孔优化》

引用《..·..·标准库·基础》。

指令_MOV_RR 为 1。

指令_MOV_RI 为 2。

指令_MOV_RM 为 3。

指令_MOV_MR 为 4。

指令_ADD_RI 为 5。

指令_ADD_RR 为 6。

指令_SUB_RI 为 7。

指令_SUB_RR 为 8。

指令_MUL_RR 为 9。

指令_MUL_RI 为 10。

指令_SHL_RI 为 11。

指令_SHR_RI 为 12。

指令_XOR_RR 为 13。

指令_AND_RI 为 14。

指令_OR_RI 为 15。

指令_TEST_RR 为 16。

指令_CMP_RI 为 17。

指令_CMP_RR 为 18。

指令_LEA 为 19。

指令_PUSH 为 20。

指令_POP 为 21。

指令_NOP 为 0。

可变 _窥孔_优化数 为 0。

可变 _窥孔_冗余mov 为 0。

可变 _窥孔_强度削减 为 0。

可变 _窥孔_零置优化 为 0。

可变 _窥孔_合并优化 为 0。

窥孔_重置统计 接受 零参：
    _窥孔_优化数 为 0。
    _窥孔_冗余mov 为 0。
    _窥孔_强度削减 为 0。
    _窥孔_零置优化 为 0。
    _窥孔_合并优化 为 0。
完。

窥孔_指令 接受 类型、目标、源、附加：
    返还 一组：类型、目标、源、附加。
完。

窥孔_指令_类型 接受 i：
    返还 i 的 第 0 项。
完。

窥孔_指令_目标 接受 i：
    返还 i 的 第 1 项。
完。

窥孔_指令_源 接受 i：
    返还 i 的 第 2 项。
完。

窥孔_指令_附加 接受 i：
    返还 i 的 第 3 项。
完。

窥孔_规则_冗余mov 接受 i：
    若 做 窥孔_指令_类型 用 i 等于 指令_MOV_RR：
        若 做 窥孔_指令_目标 用 i 等于 做 窥孔_指令_源 用 i：
            _窥孔_冗余mov 为 _窥孔_冗余mov 加 1。
            _窥孔_优化数 为 _窥孔_优化数 加 1。
            返还 做 窥孔_指令 用 指令_NOP、0、0、0。
        完。
    完。
    返还 空。
完。

窥孔_规则_add零 接受 i：
    若 做 窥孔_指令_类型 用 i 等于 指令_ADD_RI 并且 做 窥孔_指令_源 用 i 等于 0：
        _窥孔_优化数 为 _窥孔_优化数 加 1。
        返还 做 窥孔_指令 用 指令_NOP、0、0、0。
    完。
    返还 空。
完。

窥孔_规则_sub零 接受 i：
    若 做 窥孔_指令_类型 用 i 等于 指令_SUB_RI 并且 做 窥孔_指令_源 用 i 等于 0：
        _窥孔_优化数 为 _窥孔_优化数 加 1。
        返还 做 窥孔_指令 用 指令_NOP、0、0、0。
    完。
    返还 空。
完。

窥孔_规则_mul一 接受 i：
    若 做 窥孔_指令_类型 用 i 等于 指令_MUL_RI 并且 做 窥孔_指令_源 用 i 等于 1：
        _窥孔_优化数 为 _窥孔_优化数 加 1。
        返还 做 窥孔_指令 用 指令_NOP、0、0、0。
    完。
    返还 空。
完。

窥孔_规则_xor清零 接受 i：
    若 做 窥孔_指令_类型 用 i 等于 指令_XOR_RR：
        若 做 窥孔_指令_目标 用 i 等于 做 窥孔_指令_源 用 i：
            _窥孔_零置优化 为 _窥孔_零置优化 加 1。
            _窥孔_优化数 为 _窥孔_优化数 加 1。
            返还 i。
        完。
    完。
    返还 空。
完。

窥孔_规则_mov零转xor 接受 i：
    若 做 窥孔_指令_类型 用 i 等于 指令_MOV_RI 并且 做 窥孔_指令_源 用 i 等于 0：
        reg 为 做 窥孔_指令_目标 用 i。
        _窥孔_零置优化 为 _窥孔_零置优化 加 1。
        _窥孔_优化数 为 _窥孔_优化数 加 1。
        返还 做 窥孔_指令 用 指令_XOR_RR、reg、reg、0。
    完。
    返还 空。
完。

窥孔_规则_mul转shl 接受 i：
    若 做 窥孔_指令_类型 用 i 不等于 指令_MUL_RI：
        返还 空。
    完。
    imm 为 做 窥孔_指令_源 用 i。
    若 imm 小于或等于 1：
        返还 空。
    完。
    若 imm 位与 imm 减 1 等于 0：
        可变 v 为 imm。
        只要 v 大于 1：
            v 为 v 右移 1。
            exp 为 exp 加 1。
        完。
        _窥孔_强度削减 为 _窥孔_强度削减 加 1。
        _窥孔_优化数 为 _窥孔_优化数 加 1。
        返还 做 窥孔_指令 用 指令_SHL_RI、做 窥孔_指令_目标 用 i、exp、0。
    完。
    返还 空。
完。

窥孔_规则_push_pop 接受 i1、i2：
    若 做 窥孔_指令_类型 用 i1 等于 指令_PUSH 并且 做 窥孔_指令_类型 用 i2 等于 指令_POP：
        若 做 窥孔_指令_目标 用 i1 等于 做 窥孔_指令_目标 用 i2：
            _窥孔_冗余mov 为 _窥孔_冗余mov 加 1。
            _窥孔_优化数 为 _窥孔_优化数 加 2。
            返还 一组：做 窥孔_指令 用 指令_NOP、0、0、0、做 窥孔_指令 用 指令_NOP、0、0、0。
        完。
    完。
    返还 空。
完。

窥孔_规则_往返mov 接受 i1、i2：
    若 做 窥孔_指令_类型 用 i1 等于 指令_MOV_RR 并且 做 窥孔_指令_类型 用 i2 等于 指令_MOV_RR：
        若 做 窥孔_指令_目标 用 i1 等于 做 窥孔_指令_源 用 i2 并且 做 窥孔_指令_源 用 i1 等于 做 窥孔_指令_目标 用 i2：
            _窥孔_冗余mov 为 _窥孔_冗余mov 加 1。
            _窥孔_优化数 为 _窥孔_优化数 加 1。
            返还 一组：i1、做 窥孔_指令 用 指令_NOP、0、0、0。
        完。
    完。
    返还 空。
完。

窥孔_建议_lea合并 接受 shl指令、add指令：
    若 做 窥孔_指令_类型 用 shl指令 等于 指令_SHL_RI 并且 做 窥孔_指令_类型 用 add指令 等于 指令_ADD_RR：
        若 做 窥孔_指令_附加 用 shl指令 等于 1：
            _窥孔_合并优化 为 _窥孔_合并优化 加 1。
            返还 真。
        完。
    完。
    返还 假。
完。

窥孔_优化 接受 代码：
    若 代码 等于 空 或者 做 计数 用 代码 等于 0：
        返还 代码。
    完。
    可变 结果 为 一组：（空）。
    n 为 做 计数 用 代码。
    可变 i 为 0。
    只要 i 小于 n：
        当前 为 代码 的 第 i 项。
        若 i 加 1 小于 n：
            下一条 为 代码 的 第 i 加 1 项。
            push_pop 为 做 窥孔_规则_push_pop 用 当前、下一条。
            若 push_pop 不等于 空：
                做 追加 用 结果、push_pop 的 第 0 项。
                做 追加 用 结果、push_pop 的 第 1 项。
                i 为 i 加 2。
                继续。
            完。
            往返 为 做 窥孔_规则_往返mov 用 当前、下一条。
            若 往返 不等于 空：
                做 追加 用 结果、往返 的 第 0 项。
                做 追加 用 结果、往返 的 第 1 项。
                i 为 i 加 2。
                继续。
            完。
        完。
        可变 优化结果 为 空。
        若 优化结果 等于 空：
            优化结果 为 做 窥孔_规则_冗余mov 用 当前。
        完。
        若 优化结果 等于 空：
            优化结果 为 做 窥孔_规则_add零 用 当前。
        完。
        若 优化结果 等于 空：
            优化结果 为 做 窥孔_规则_sub零 用 当前。
        完。
        若 优化结果 等于 空：
            优化结果 为 做 窥孔_规则_mul一 用 当前。
        完。
        若 优化结果 等于 空：
            优化结果 为 做 窥孔_规则_mul转shl 用 当前。
        完。
        若 优化结果 等于 空：
            优化结果 为 做 窥孔_规则_mov零转xor 用 当前。
        完。
        若 优化结果 不等于 空：
            做 追加 用 结果、优化结果。
        否则：
            做 追加 用 结果、当前。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

窥孔_报告 接受 零参：
    返还 一组：_窥孔_优化数、_窥孔_冗余mov、_窥孔_强度削减、_窥孔_零置优化、_窥孔_合并优化。
完。

窥孔_统计_总数 接受 零参：
    返还 _窥孔_优化数。
完。

窥孔_统计_冗余mov 接受 零参：
    返还 _窥孔_冗余mov。
完。

窥孔_统计_强度削减 接受 零参：
    返还 _窥孔_强度削减。
完。

可变 _窥孔_字符串合并数 为 0。

窥孔_规则_字符串合并 接受 指令A、指令B：
    若 指令A 等于 空 或者 指令B 等于 空：
        返还 空。
    完。
    若 做 计数 用 指令A 小于 2 或者 做 计数 用 指令B 小于 2：
        返还 空。
    完。
    A_操作码 为 指令A 的 第 0 项。
    B_操作码 为 指令B 的 第 0 项。
    若 A_操作码 等于 B_操作码 并且 A_操作码 大于或等于 0x01 并且 A_操作码 小于或等于 0x03：
        若 指令A 的 第 1 项 等于 指令B 的 第 1 项：
            _窥孔_字符串合并数 为 _窥孔_字符串合并数 加 1。
            _窥孔_优化数 为 _窥孔_优化数 加 1。
            返还 一组：指令A 的 第 0 项、指令A 的 第 1 项、「字符串已合并」。
        完。
    完。
    返还 空。
完。

窥孔_后处理_字节 接受 起始、结束：
    可变 优化数 为 0。
    可变 i 为 起始。
    只要 i 加 6 小于 结束：
        若 代码 的 第 i 项 等于 72 并且 代码 的 第 i 加 1 项 等于 199 并且 代码 的 第 i 加 3 项 等于 0 并且 代码 的 第 i 加 4 项 等于 0 并且 代码 的 第 i 加 5 项 等于 0 并且 代码 的 第 i 加 6 项 等于 0：
            modrm 为 代码 的 第 i 加 2 项。
            若 modrm 大于或等于 192 并且 modrm 小于或等于 199：
                寄存器号 为 modrm 减 192。
                若 寄存器号 不等于 4 并且 寄存器号 不等于 5：
                    代码 的 第 i 项 为 49。
                    代码 的 第 i 加 1 项 为 192 加 寄存器号 乘 9。
                    代码 的 第 i 加 2 项 为 15。
                    代码 的 第 i 加 3 项 为 31。
                    代码 的 第 i 加 4 项 为 68。
                    代码 的 第 i 加 5 项 为 0。
                    代码 的 第 i 加 6 项 为 0。
                    优化数 为 优化数 加 1。
                    _窥孔_零置优化 为 _窥孔_零置优化 加 1。
                    _窥孔_优化数 为 _窥孔_优化数 加 1。
                    i 为 i 加 7。
                    继续。
                完。
            完。
        完。
        若 代码 的 第 i 项 等于 72 并且 代码 的 第 i 加 1 项 等于 137：
            modrm2 为 代码 的 第 i 加 2 项。
            若 modrm2 大于或等于 192 并且 modrm2 小于或等于 255：
                _源 为 (modrm2 减 192) 除以 8。
                _目标 为 modrm2 减 192 减 _源 乘 8。
                若 _源 等于 _目标：
                    代码 的 第 i 项 为 15。
                    代码 的 第 i 加 1 项 为 31。
                    代码 的 第 i 加 2 项 为 0。
                    优化数 为 优化数 加 1。
                    _窥孔_冗余mov 为 _窥孔_冗余mov 加 1。
                    _窥孔_优化数 为 _窥孔_优化数 加 1。
                    i 为 i 加 3。
                    继续。
                完。
            完。
        完。
        若 i 加 3 小于 结束 并且 代码 的 第 i 项 等于 72 并且 代码 的 第 i 加 1 项 等于 131 并且 代码 的 第 i 加 2 项 等于 196 并且 代码 的 第 i 加 3 项 等于 0：
            代码 的 第 i 项 为 15。
            代码 的 第 i 加 1 项 为 31。
            代码 的 第 i 加 2 项 为 64。
            代码 的 第 i 加 3 项 为 0。
            优化数 为 优化数 加 1。
            _窥孔_优化数 为 _窥孔_优化数 加 1。
            i 为 i 加 4。
            继续。
        完。
        若 i 加 3 小于 结束 并且 代码 的 第 i 项 等于 72 并且 代码 的 第 i 加 1 项 等于 131 并且 代码 的 第 i 加 2 项 等于 236 并且 代码 的 第 i 加 3 项 等于 0：
            代码 的 第 i 项 为 15。
            代码 的 第 i 加 1 项 为 31。
            代码 的 第 i 加 2 项 为 64。
            代码 的 第 i 加 3 项 为 0。
            优化数 为 优化数 加 1。
            _窥孔_优化数 为 _窥孔_优化数 加 1。
            i 为 i 加 4。
            继续。
        完。
        i 为 i 加 1。
    完。
    返还 优化数。
完。

窥孔_中文标识符开销估算 接受 标识符名：
    若 做 计数 用 标识符名 等于 0：
        返还 0。
    完。
    可变 字节数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 标识符名：
        cp 为 做 字符码 用 做 子串 用 标识符名、i、1、0。
        若 cp 大于或等于 19968 并且 cp 小于或等于 40959：
            字节数 为 字节数 加 3。
        否则若 cp 大于 127：
            字节数 为 字节数 加 2。
        否则：
            字节数 为 字节数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 字节数。
完。

本章 收束。
