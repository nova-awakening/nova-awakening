《分代GC》

引用《..·..·标准库·基础》。

引用《..·..·标准库·集合·栈·栈》。

GC_新生代大小 为 1048576。

GC_老年代大小 为 16777216。

GC_晋升阈值 为 3。

GC_触发占比 为 75。

GC_紧急占比 为 95。

GC标记_白 为 0。

GC标记_灰 为 1。

GC标记_黑 为 2。

GC类型_整数 为 0。

GC类型_浮点 为 1。

GC类型_字符串 为 2。

GC类型_列表 为 3。

GC类型_字典 为 4。

GC类型_函数 为 5。

GC类型_结构体 为 6。

GC类型_闭包 为 7。

GC对象_创建 接受 类型、大小、数据：
    返还 一组：类型、GC标记_白、0、大小、数据、一组：（空）。
完。

GC对象_标记 接受 对象：
    返还 对象 的 第 1 项。
完。

GC对象_代数 接受 对象：
    返还 对象 的 第 2 项。
完。

GC对象_大小 接受 对象：
    返还 对象 的 第 3 项。
完。

GC对象_数据 接受 对象：
    返还 对象 的 第 4 项。
完。

GC对象_引用 接受 对象：
    返还 对象 的 第 5 项。
完。

GC对象_设标记 接受 对象、标记：
    对象 的 第 1 项 为 标记。
完。

GC对象_增代数 接受 对象：
    对象 的 第 2 项 为 对象 的 第 2 项 加 1。
完。

分代GC_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、0、一组：（空）、一组：0、0。
完。

分代GC_新生代 接受 gc：
    返还 gc 的 第 0 项。
完。

分代GC_老年代 接受 gc：
    返还 gc 的 第 1 项。
完。

分代GC_分配 接受 gc、类型、大小、数据：
    若 gc 的 第 2 项 加 大小 大于 GC_新生代大小 乘 GC_触发占比 除以 100：
        做 分代GC_Minor 用 gc。
    完。
    若 大小 大于 65536：
        对象 为 做 GC对象_创建 用 类型、大小、数据。
        做 追加 用 gc 的 第 1 项、对象。
        gc 的 第 3 项 为 gc 的 第 3 项 加 大小。
        返还 对象。
    完。
    对象 为 做 GC对象_创建 用 类型、大小、数据。
    做 追加 用 gc 的 第 0 项、对象。
    gc 的 第 2 项 为 gc 的 第 2 项 加 大小。
    返还 对象。
完。

分代GC_添加根 接受 gc、对象：
    做 追加 用 gc 的 第 4 项、对象。
完。

分代GC_清除根 接受 gc：
    gc 的 第 4 项 为 一组：（空）。
完。

分代GC_Minor 接受 gc：
    gc 的 第 5 项 的 第 0 项 为 gc 的 第 5 项 的 第 0 项 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 gc 的 第 4 项：
        做 分代GC_标记对象 用 gc 的 第 4 项 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 新新生代 为 一组：（空）。
    可变 回收大小 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 gc 的 第 0 项：
        对象 为 gc 的 第 0 项 的 第 i 项。
        若 做 GC对象_标记 用 对象 等于 GC标记_黑：
            做 GC对象_增代数 用 对象。
            若 做 GC对象_代数 用 对象 大于或等于 GC_晋升阈值：
                做 追加 用 gc 的 第 1 项、对象。
                gc 的 第 3 项 为 gc 的 第 3 项 加 做 GC对象_大小 用 对象。
                gc 的 第 5 项 的 第 3 项 为 gc 的 第 5 项 的 第 3 项 加 1。
            否则：
                做 追加 用 新新生代、对象。
            完。
            做 GC对象_设标记 用 对象、GC标记_白。
        否则：
            回收大小 为 回收大小 加 做 GC对象_大小 用 对象。
        完。
        i 为 i 加 1。
    完。
    gc 的 第 0 项 为 新新生代。
    gc 的 第 2 项 为 gc 的 第 2 项 减 回收大小。
    若 gc 的 第 2 项 小于 0：
        gc 的 第 2 项 为 0。
    完。
    gc 的 第 5 项 的 第 2 项 为 gc 的 第 5 项 的 第 2 项 加 回收大小。
    若 gc 的 第 3 项 大于 GC_老年代大小 乘 GC_触发占比 除以 100：
        做 分代GC_Major 用 gc。
    完。
完。

分代GC_Major 接受 gc：
    gc 的 第 5 项 的 第 1 项 为 gc 的 第 5 项 的 第 1 项 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 gc 的 第 4 项：
        做 分代GC_标记对象 用 gc 的 第 4 项 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 gc 的 第 0 项：
        引用列表 为 做 GC对象_引用 用 gc 的 第 0 项 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 引用列表：
            做 分代GC_标记对象 用 引用列表 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 新老年代 为 一组：（空）。
    可变 回收大小 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 gc 的 第 1 项：
        对象 为 gc 的 第 1 项 的 第 i 项。
        若 做 GC对象_标记 用 对象 等于 GC标记_黑：
            做 追加 用 新老年代、对象。
            做 GC对象_设标记 用 对象、GC标记_白。
        否则：
            回收大小 为 回收大小 加 做 GC对象_大小 用 对象。
        完。
        i 为 i 加 1。
    完。
    gc 的 第 1 项 为 新老年代。
    gc 的 第 3 项 为 gc 的 第 3 项 减 回收大小。
    若 gc 的 第 3 项 小于 0：
        gc 的 第 3 项 为 0。
    完。
    gc 的 第 5 项 的 第 2 项 为 gc 的 第 5 项 的 第 2 项 加 回收大小。
完。

分代GC_标记对象 接受 对象：
    若 对象 等于 空：
        返还。
    完。
    若 做 GC对象_标记 用 对象 不等于 GC标记_白：
        返还。
    完。
    做 GC对象_设标记 用 对象、GC标记_黑。
    引用列表 为 做 GC对象_引用 用 对象。
    可变 i 为 0。
    只要 i 小于 做 计数 用 引用列表：
        做 分代GC_标记对象 用 引用列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

分代GC_Minor次数 接受 gc：
    返还 gc 的 第 5 项 的 第 0 项。
完。

分代GC_Major次数 接受 gc：
    返还 gc 的 第 5 项 的 第 1 项。
完。

分代GC_总回收字节 接受 gc：
    返还 gc 的 第 5 项 的 第 2 项。
完。

分代GC_总晋升对象 接受 gc：
    返还 gc 的 第 5 项 的 第 3 项。
完。

分代GC_新生代使用 接受 gc：
    返还 gc 的 第 2 项。
完。

分代GC_老年代使用 接受 gc：
    返还 gc 的 第 3 项。
完。

分代GC_新生代对象数 接受 gc：
    返还 做 计数 用 gc 的 第 0 项。
完。

分代GC_老年代对象数 接受 gc：
    返还 做 计数 用 gc 的 第 1 项。
完。

分代GC_报告 接受 gc：
    返还 一组：做 分代GC_Minor次数 用 gc、做 分代GC_Major次数 用 gc、做 分代GC_总回收字节 用 gc、做 分代GC_总晋升对象 用 gc、做 分代GC_新生代使用 用 gc、做 分代GC_老年代使用 用 gc。
完。

分代GC_内存使用率 接受 gc：
    总使用 为 gc 的 第 2 项 加 gc 的 第 3 项。
    总容量 为 GC_新生代大小 加 GC_老年代大小。
    若 总容量 等于 0：
        返还 0。
    完。
    返还 总使用 乘 100 除以 总容量。
完。

可变 _记忆集 为 一组：（空）。

写屏障_记录 接受 老对象索引、新对象索引：
    做 追加 用 _记忆集、一组：老对象索引、新对象索引。
完。

写屏障_清空 接受 零参：
    _记忆集 为 一组：（空）。
完。

写屏障_处理 接受 gc：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _记忆集：
        条目 为 _记忆集 的 第 i 项。
        新对象 为 条目 的 第 1 项。
        若 新对象 大于或等于 0 并且 新对象 小于 做 计数 用 gc 的 第 0 项：
            做 分代GC_标记对象 用 gc 的 第 0 项 的 第 新对象 项。
        完。
        i 为 i 加 1。
    完。
    做 写屏障_清空。
完。

可变 _增量_灰队列 为 一组：（空）。

可变 _增量_步长 为 50。

增量标记_初始化 接受 根集：
    _增量_灰队列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 根集：
        做 追加 用 _增量_灰队列、根集 的 第 i 项。
        i 为 i 加 1。
    完。
完。

增量标记_步进 接受 零参：
    可变 处理数 为 0。
    只要 处理数 小于 _增量_步长 并且 做 计数 用 _增量_灰队列 大于 0：
        n 为 做 计数 用 _增量_灰队列。
        对象 为 _增量_灰队列 的 第 n 减 1 项。
        做 弹出 用 _增量_灰队列。
        若 做 GC对象_标记 用 对象 等于 GC标记_灰：
            做 GC对象_设标记 用 对象、GC标记_黑。
            引用 为 做 GC对象_引用 用 对象。
            可变 j 为 0。
            只要 j 小于 做 计数 用 引用：
                若 做 GC对象_标记 用 引用 的 第 j 项 等于 GC标记_白：
                    做 GC对象_设标记 用 引用 的 第 j 项、GC标记_灰。
                    做 追加 用 _增量_灰队列、引用 的 第 j 项。
                完。
                j 为 j 加 1。
            完。
        完。
        处理数 为 处理数 加 1。
    完。
    返还 做 计数 用 _增量_灰队列 等于 0。
完。

增量标记_完成 接受 零参：
    返还 做 计数 用 _增量_灰队列 等于 0。
完。

可变 _终结器_队列 为 一组：（空）。

终结器_注册 接受 对象索引、终结函数名：
    做 追加 用 _终结器_队列、一组：对象索引、终结函数名、0。
完。

终结器_运行 接受 已回收列表：
    可变 待运行 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _终结器_队列：
        条目 为 _终结器_队列 的 第 i 项。
        可变 j 为 0。
        可变 已回收 为 假。
        只要 j 小于 做 计数 用 已回收列表：
            若 已回收列表 的 第 j 项 等于 条目 的 第 0 项：
                已回收 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已回收 并且 条目 的 第 2 项 等于 0：
            条目 的 第 2 项 为 1。
            做 追加 用 待运行、条目。
        完。
        i 为 i 加 1。
    完。
    返还 待运行。
完。

终结器_清理 接受 零参：
    可变 新队列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _终结器_队列：
        若 _终结器_队列 的 第 i 项 的 第 2 项 等于 0：
            做 追加 用 新队列、_终结器_队列 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    _终结器_队列 为 新队列。
完。

可变 _弱引用_表 为 一组：（空）。

分代GC_弱引用_创建 接受 对象索引：
    id 为 做 计数 用 _弱引用_表。
    做 追加 用 _弱引用_表、一组：对象索引、1。
    返还 id。
完。

弱引用_取 接受 id：
    若 id 小于 0 或者 id 大于或等于 做 计数 用 _弱引用_表：
        返还 空。
    完。
    若 _弱引用_表 的 第 id 项 的 第 1 项 等于 0：
        返还 空。
    完。
    返还 _弱引用_表 的 第 id 项 的 第 0 项。
完。

弱引用_清除 接受 已回收列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _弱引用_表：
        可变 j 为 0。
        只要 j 小于 做 计数 用 已回收列表：
            若 已回收列表 的 第 j 项 等于 _弱引用_表 的 第 i 项 的 第 0 项：
                _弱引用_表 的 第 i 项 的 第 1 项 为 0。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

可变 _GC_自适应阈值 为 75。

可变 _GC_上次回收量 为 0。

可变 _GC_上次暂停时间 为 0。

GC_自适应调优 接受 回收量、暂停时间：
    若 暂停时间 大于 50 并且 _GC_自适应阈值 小于 90：
        _GC_自适应阈值 为 _GC_自适应阈值 加 5。
    完。
    若 回收量 小于 _GC_上次回收量 除以 2 并且 _GC_自适应阈值 大于 50：
        _GC_自适应阈值 为 _GC_自适应阈值 减 5。
    完。
    _GC_上次回收量 为 回收量。
    _GC_上次暂停时间 为 暂停时间。
    返还 _GC_自适应阈值。
完。

GC_应该触发 接受 使用率：
    返还 使用率 大于或等于 _GC_自适应阈值。
完。

GC_内存压力 接受 当前使用、总容量：
    若 总容量 等于 0：
        返还 0。
    完。
    返还 当前使用 乘 100 除以 总容量。
完。

可变 _增量_灰色队列 为 一组：（空）。

可变 _增量_工作量 为 50。

可变 _增量_已完成 为 真。

增量GC_开始 接受 根集：
    _增量_灰色队列 为 一组：（空）。
    _增量_已完成 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 根集：
        若 做 GC对象_标记 用 根集 的 第 i 项 等于 GC标记_白：
            做 GC对象_设标记 用 根集 的 第 i 项、GC标记_灰。
            做 追加 用 _增量_灰色队列、根集 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
完。

增量GC_步进 接受 零参：
    若 _增量_已完成：
        返还 真。
    完。
    可变 已处理 为 0。
    只要 做 计数 用 _增量_灰色队列 大于 0 并且 已处理 小于 _增量_工作量：
        对象 为 _增量_灰色队列 的 第 0 项。
        可变 新队列 为 一组：（空）。
        可变 i 为 1。
        只要 i 小于 做 计数 用 _增量_灰色队列：
            做 追加 用 新队列、_增量_灰色队列 的 第 i 项。
            i 为 i 加 1。
        完。
        _增量_灰色队列 为 新队列。
        做 GC对象_设标记 用 对象、GC标记_黑。
        引用列表 为 做 GC对象_引用 用 对象。
        i 为 0。
        只要 i 小于 做 计数 用 引用列表：
            子对象 为 引用列表 的 第 i 项。
            若 做 GC对象_标记 用 子对象 等于 GC标记_白：
                做 GC对象_设标记 用 子对象、GC标记_灰。
                做 追加 用 _增量_灰色队列、子对象。
            完。
            i 为 i 加 1。
        完。
        已处理 为 已处理 加 1。
    完。
    若 做 计数 用 _增量_灰色队列 等于 0：
        _增量_已完成 为 真。
    完。
    返还 _增量_已完成。
完。

增量GC_清除 接受 堆：
    可变 存活 为 一组：（空）。
    可变 回收字节 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 堆：
        若 做 GC对象_标记 用 堆 的 第 i 项 等于 GC标记_黑：
            做 GC对象_设标记 用 堆 的 第 i 项、GC标记_白。
            做 追加 用 存活、堆 的 第 i 项。
        否则：
            回收字节 为 回收字节 加 做 GC对象_大小 用 堆 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：存活、回收字节。
完。

可变 _字符串池 为 一组：（空）。

可变 _字符串池_最大 为 256。

字符串池_获取 接受 内容：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _字符串池：
        若 _字符串池 的 第 i 项 的 第 0 项 等于 内容：
            _字符串池 的 第 i 项 的 第 1 项 为 _字符串池 的 第 i 项 的 第 1 项 加 1。
            返还 _字符串池 的 第 i 项 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 _字符串池 小于 _字符串池_最大：
        做 追加 用 _字符串池、一组：内容、1。
    完。
    返还 内容。
完。

字符串池_释放 接受 内容：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _字符串池：
        若 _字符串池 的 第 i 项 的 第 0 项 等于 内容：
            _字符串池 的 第 i 项 的 第 1 项 为 _字符串池 的 第 i 项 的 第 1 项 减 1。
            若 _字符串池 的 第 i 项 的 第 1 项 小于或等于 0：
                可变 新池 为 一组：（空）。
                可变 j 为 0。
                只要 j 小于 做 计数 用 _字符串池：
                    若 j 不等于 i：
                        做 追加 用 新池、_字符串池 的 第 j 项。
                    完。
                    j 为 j 加 1。
                完。
                _字符串池 为 新池。
            完。
            返还 空。
        完。
        i 为 i 加 1。
    完。
完。

字符串池_命中率 接受 零参：
    返还 做 计数 用 _字符串池。
完。

本章 收束。
