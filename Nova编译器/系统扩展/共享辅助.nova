《共享辅助》

引用《..·标准库·基础》。

可变 系统扩展_错误数 为 0。

可变 系统扩展_警告数 为 0。

可变 系统扩展_信息数 为 0。

可变 系统扩展_错误列表 为 一组：（空）。

可变 系统扩展_警告列表 为 一组：（空）。

编译错误 接受 消息：
    做 打印 用 「编译错误: 」 加 消息。
    系统扩展_错误数 为 系统扩展_错误数 加 1。
    做 追加 用 系统扩展_错误列表、一组：系统扩展_错误数、消息、「错误」。
完。

编译警告 接受 消息：
    做 打印 用 「编译警告: 」 加 消息。
    系统扩展_警告数 为 系统扩展_警告数 加 1。
    做 追加 用 系统扩展_警告列表、一组：系统扩展_警告数、消息、「警告」。
完。

编译信息 接受 消息：
    做 打印 用 「编译信息: 」 加 消息。
    系统扩展_信息数 为 系统扩展_信息数 加 1。
完。

编译错误_带位置 接受 文件、行、列、消息：
    位置 为 文件 加 「:」 加 做 转字符串 用 行 加 「:」 加 做 转字符串 用 列。
    做 编译错误 用 位置 加 「 」 加 消息。
完。

编译警告_带位置 接受 文件、行、列、消息：
    位置 为 文件 加 「:」 加 做 转字符串 用 行 加 「:」 加 做 转字符串 用 列。
    做 编译警告 用 位置 加 「 」 加 消息。
完。

编译_有错误 接受 零参：
    返还 系统扩展_错误数 大于 0。
完。

编译_错误摘要 接受 零参：
    做 打印 用 「 编译摘要 」。
    做 打印 用 「错误: 」 加 做 转字符串 用 系统扩展_错误数。
    做 打印 用 「警告: 」 加 做 转字符串 用 系统扩展_警告数。
    做 打印 用 「信息: 」 加 做 转字符串 用 系统扩展_信息数。
    返还 系统扩展_错误数。
完。

编译_重置错误 接受 零参：
    系统扩展_错误数 为 0。
    系统扩展_警告数 为 0。
    系统扩展_信息数 为 0。
    系统扩展_错误列表 为 一组：（空）。
    系统扩展_警告列表 为 一组：（空）。
完。

整数转字符串 接受 n：
    返还 做 转字符串 用 n。
完。

安全转整数 接受 文本：
    若 文本 等于 空 或者 文本 等于 「」：
        返还 0。
    完。
    可变 结果 为 0。
    可变 负 为 假。
    可变 i 为 0。
    若 做 计数 用 文本 大于 0 并且 做 子串 用 文本、0、1 等于 「-」：
        负 为 真。
        i 为 1。
    完。
    只要 i 小于 做 计数 用 文本：
        c 为 做 编码 用 做 子串 用 文本、i、1。
        若 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
            结果 为 结果 乘 10 加 (c 减 48)。
        否则：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    若 负：
        返还 0 减 结果。
    完。
    返还 结果。
完。

布尔转字符串 接受 值：
    若 值：
        返还 「真」。
    完。
    返还 「假」。
完。

安全转布尔 接受 文本：
    若 文本 等于 「真」 或者 文本 等于 「是」 或者 文本 等于 「1」：
        返还 真。
    完。
    返还 假。
完。

可变 变量作用域表 为 一组：（空）。

可变 作用域_当前深度 为 0。

可变 作用域_最大深度 为 0。

可变 作用域_变量数 为 一组：（空）。

变量_设置作用域 接受 变量名、深度：
    可变 i 为 0。
    只要 i 小于 做 计数 用 变量作用域表：
        若 变量作用域表 的 第 i 项 的 第 0 项 等于 变量名：
            变量作用域表 的 第 i 项 的 第 1 项 为 深度。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 变量作用域表、一组：变量名、深度、「活跃」。
完。

变量_获取作用域 接受 变量名：
    可变 i 为 做 计数 用 变量作用域表 减 1。
    只要 i 大于或等于 0：
        若 变量作用域表 的 第 i 项 的 第 0 项 等于 变量名：
            返还 变量作用域表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 减 1。
    完。
    返还 0。
完。

变量_存在 接受 变量名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 变量作用域表：
        若 变量作用域表 的 第 i 项 的 第 0 项 等于 变量名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

作用域_进入 接受 零参：
    作用域_当前深度 为 作用域_当前深度 加 1。
    若 作用域_当前深度 大于 作用域_最大深度：
        作用域_最大深度 为 作用域_当前深度。
    完。
    做 追加 用 作用域_变量数、做 计数 用 变量作用域表。
完。

作用域_退出 接受 零参：
    若 作用域_当前深度 小于或等于 0：
        返还 空。
    完。
    若 做 计数 用 作用域_变量数 大于 0：
        恢复数 为 作用域_变量数 的 第 做 计数 用 作用域_变量数 减 1 项。
        只要 做 计数 用 变量作用域表 大于 恢复数：
            变量作用域表 的 第 做 计数 用 变量作用域表 减 1 项 的 第 2 项 为 「已销毁」。
            做 删除 用 变量作用域表、做 计数 用 变量作用域表 减 1。
        完。
        做 删除 用 作用域_变量数、做 计数 用 作用域_变量数 减 1。
    完。
    作用域_当前深度 为 作用域_当前深度 减 1。
完。

作用域_深度 接受 零参：
    返还 作用域_当前深度。
完。

作用域_最大 接受 零参：
    返还 作用域_最大深度。
完。

可变 符号表_条目 为 一组：（空）。

可变 符号表_计数 为 0。

符号表_添加 接受 名称、类型、作用域：
    做 追加 用 符号表_条目、一组：名称、类型、作用域、真。
    符号表_计数 为 符号表_计数 加 1。
完。

符号表_查找 接受 名称、_扩2：
    可变 i 为 做 计数 用 符号表_条目 减 1。
    只要 i 大于或等于 0：
        若 符号表_条目 的 第 i 项 的 第 0 项 等于 名称 并且 符号表_条目 的 第 i 项 的 第 3 项 等于 真：
            返还 符号表_条目 的 第 i 项。
        完。
        i 为 i 减 1。
    完。
    返还 空。
完。

符号表_类型 接受 名称：
    条目 为 做 符号表_查找 用 名称、0。
    若 条目 等于 空：
        返还 「未知」。
    完。
    返还 条目 的 第 1 项。
完。

符号表_清空 接受 零参：
    符号表_条目 为 一组：（空）。
    符号表_计数 为 0。
完。

符号表_大小 接受 零参：
    返还 符号表_计数。
完。

符号表_所有名称 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 符号表_条目：
        若 符号表_条目 的 第 i 项 的 第 3 项 等于 真：
            做 追加 用 结果、符号表_条目 的 第 i 项 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

推断_整数类型 接受 值：
    若 值 大于或等于 0 减 128 并且 值 小于或等于 127：
        返还 「i8」。
    完。
    若 值 大于或等于 0 减 32768 并且 值 小于或等于 32767：
        返还 「i16」。
    完。
    若 值 大于或等于 0 减 2147483648 并且 值 小于或等于 2147483647：
        返还 「i32」。
    完。
    返还 「i64」。
完。

推断_最窄类型 接受 值列表：
    可变 最小 为 0。
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 值列表：
        若 值列表 的 第 i 项 小于 最小：
            最小 为 值列表 的 第 i 项。
        完。
        若 值列表 的 第 i 项 大于 最大：
            最大 为 值列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 最小 大于或等于 0：
        若 最大 小于或等于 255：
            返还 「u8」。
        完。
        若 最大 小于或等于 65535：
            返还 「u16」。
        完。
        返还 「u32」。
    完。
    返还 做 推断_整数类型 用 最小。
完。

类型_兼容 接受 类型1、类型2：
    若 类型1 等于 类型2：
        返还 真。
    完。
    若 类型1 等于 「i64」：
        返还 真。
    完。
    若 类型2 等于 「i64」：
        返还 真。
    完。
    若 类型1 等于 「i32」 并且 (类型2 等于 「i8」 或者 类型2 等于 「i16」)：
        返还 真。
    完。
    若 类型2 等于 「i32」 并且 (类型1 等于 「i8」 或者 类型1 等于 「i16」)：
        返还 真。
    完。
    返还 假。
完。

类型_提升 接受 类型1、类型2：
    若 类型1 等于 类型2：
        返还 类型1。
    完。
    若 类型1 等于 「i64」 或者 类型2 等于 「i64」：
        返还 「i64」。
    完。
    若 类型1 等于 「i32」 或者 类型2 等于 「i32」：
        返还 「i32」。
    完。
    若 类型1 等于 「i16」 或者 类型2 等于 「i16」：
        返还 「i16」。
    完。
    返还 「i8」。
完。

辅助_字符串拼接 接受 列表、分隔符：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 i 大于 0：
            结果 为 结果 加 分隔符。
        完。
        结果 为 结果 加 做 转字符串 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

辅助_缩进 接受 深度：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 深度：
        结果 为 结果 加 「 」。
        i 为 i 加 1。
    完。
    返还 结果。
完。

辅助_重复字符 接受 字符、次数：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 次数：
        结果 为 结果 加 字符。
        i 为 i 加 1。
    完。
    返还 结果。
完。

辅助_左填充 接受 文本、宽度、填充字符：
    可变 结果 为 文本。
    只要 做 计数 用 结果 小于 宽度：
        结果 为 填充字符 加 结果。
    完。
    返还 结果。
完。

辅助_右填充 接受 文本、宽度、填充字符：
    可变 结果 为 文本。
    只要 做 计数 用 结果 小于 宽度：
        结果 为 结果 加 填充字符。
    完。
    返还 结果。
完。

错误_模式分析 接受 零参：
    若 系统扩展_错误数 等于 0：
        返还 「无错误」。
    完。
    可变 类型错误 为 0。
    可变 作用域错误 为 0。
    可变 语法错误 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 系统扩展_错误列表：
        消息 为 系统扩展_错误列表 的 第 i 项 的 第 1 项。
        可变 j 为 0。
        可变 有类型 为 假。
        可变 有作用域 为 假。
        只要 j 小于 做 计数 用 消息：
            若 j 加 1 小于 做 计数 用 消息 并且 做 子串 用 消息、j、2 等于 「类型」：
                有类型 为 真。
            完。
            若 j 加 2 小于 做 计数 用 消息 并且 做 子串 用 消息、j、3 等于 「作用域」：
                有作用域 为 真。
            完。
            j 为 j 加 1。
        完。
        若 有类型：
            类型错误 为 类型错误 加 1。
        完。
        若 有作用域：
            作用域错误 为 作用域错误 加 1。
        完。
        若 有类型 等于 假 并且 有作用域 等于 假：
            语法错误 为 语法错误 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：类型错误、作用域错误、语法错误。
完。

符号_冲突检测 接受 零参：
    可变 冲突 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 符号表_条目：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 符号表_条目：
            若 符号表_条目 的 第 i 项 的 第 0 项 等于 符号表_条目 的 第 j 项 的 第 0 项 并且 符号表_条目 的 第 i 项 的 第 2 项 等于 符号表_条目 的 第 j 项 的 第 2 项 并且 符号表_条目 的 第 i 项 的 第 3 项 等于 真 并且 符号表_条目 的 第 j 项 的 第 3 项 等于 真：
                做 追加 用 冲突、一组：符号表_条目 的 第 i 项 的 第 0 项、符号表_条目 的 第 i 项 的 第 2 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 冲突。
完。

作用域_优化建议 接受 零参：
    若 作用域_最大深度 大于 10：
        返还 「嵌套过深: 建议拆分函数」。
    完。
    若 做 计数 用 变量作用域表 大于 100：
        返还 「变量过多: 考虑模块化」。
    完。
    若 作用域_最大深度 小于或等于 3：
        返还 「结构良好: 嵌套适中」。
    完。
    返还 「正常: 嵌套在合理范围」。
完。

编译_健康报告 接受 零参：
    可变 评分 为 100。
    若 系统扩展_错误数 大于 0：
        评分 为 评分 减 系统扩展_错误数 乘 10。
    完。
    若 系统扩展_警告数 大于 0：
        评分 为 评分 减 系统扩展_警告数 乘 2。
    完。
    若 作用域_最大深度 大于 8：
        评分 为 评分 减 10。
    完。
    冲突 为 做 符号_冲突检测。
    若 做 计数 用 冲突 大于 0：
        评分 为 评分 减 做 计数 用 冲突 乘 5。
    完。
    若 评分 小于 0：
        评分 为 0。
    完。
    返还 一组：评分、系统扩展_错误数、系统扩展_警告数、做 计数 用 冲突。
完。

可变 日志_级别 为 3。

可变 日志_条目 为 一组：（空）。

共享辅助_日志_调试 为 1。

共享辅助_日志_信息 为 2。

共享辅助_日志_警告 为 3。

共享辅助_日志_错误 为 4。

共享辅助_日志_致命 为 5。

日志_设级别 接受 级别：
    日志_级别 为 级别。
完。

日志_写 接受 级别、类别、消息：
    若 级别 小于 日志_级别：
        返还 空。
    完。
    可变 级别名 为 「信息」。
    若 级别 等于 1：
        级别名 为 「调试」。
    完。
    若 级别 等于 3：
        级别名 为 「警告」。
    完。
    若 级别 等于 4：
        级别名 为 「错误」。
    完。
    若 级别 等于 5：
        级别名 为 「致命」。
    完。
    条目 为 「[」 加 级别名 加 「] 」 加 类别 加 「: 」 加 消息。
    做 打印 用 条目。
    做 追加 用 日志_条目、一组：级别、类别、消息。
完。

日志_调试写 接受 类别、消息：
    做 日志_写 用 共享辅助_日志_调试、类别、消息。
完。

日志_信息写 接受 类别、消息：
    做 日志_写 用 共享辅助_日志_信息、类别、消息。
完。

日志_警告写 接受 类别、消息：
    做 日志_写 用 共享辅助_日志_警告、类别、消息。
完。

日志_错误写 接受 类别、消息：
    做 日志_写 用 共享辅助_日志_错误、类别、消息。
完。

共享辅助_日志_总数 接受 零参：
    返还 做 计数 用 日志_条目。
完。

共享辅助_日志_清空 接受 零参：
    日志_条目 为 一组：（空）。
完。

可变 诊断_计时器 为 一组：（空）。

诊断_开始计时 接受 名称：
    做 追加 用 诊断_计时器、一组：名称。
完。

诊断_结束计时 接受 名称：
    可变 i 为 做 计数 用 诊断_计时器 减 1。
    只要 i 大于或等于 0：
        若 诊断_计时器 的 第 i 项 的 第 0 项 等于 名称：
            诊断_计时器 的 第 i 项 的 第 2 项 为 1。
            返还 空。
        完。
        i 为 i 减 1。
    完。
完。

诊断_报告 接受 零参：
    做 打印 用 「── 诊断报告 ──」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 诊断_计时器：
        可变 状态 为 「完成」。
        若 诊断_计时器 的 第 i 项 的 第 2 项 等于 0：
            状态 为 「运行中」。
        完。
        做 打印 用 「 」 加 诊断_计时器 的 第 i 项 的 第 0 项 加 「: 」 加 状态。
        i 为 i 加 1。
    完。
完。

可变 内存_分配计数 为 0。

可变 内存_释放计数 为 0。

可变 内存_当前使用 为 0。

可变 内存_峰值使用 为 0。

内存_记录分配 接受 大小：
    内存_分配计数 为 内存_分配计数 加 1。
    内存_当前使用 为 内存_当前使用 加 大小。
    若 内存_当前使用 大于 内存_峰值使用：
        内存_峰值使用 为 内存_当前使用。
    完。
完。

内存_记录释放 接受 大小：
    内存_释放计数 为 内存_释放计数 加 1。
    内存_当前使用 为 内存_当前使用 减 大小。
    若 内存_当前使用 小于 0：
        内存_当前使用 为 0。
    完。
完。

内存_报告 接受 零参：
    返还 一组：内存_分配计数、内存_释放计数、内存_当前使用、内存_峰值使用。
完。

内存_有泄漏 接受 零参：
    返还 内存_分配计数 大于 内存_释放计数。
完。

辅助_断言 接受 条件、消息：
    若 条件 等于 假：
        做 编译错误 用 「断言失败: 」 加 消息。
    完。
完。

辅助_不可达 接受 消息：
    做 编译错误 用 「不可达代码: 」 加 消息。
完。

错误恢复_建议 接受 错误类型：
    若 错误类型 等于 「类型不匹配」：
        返还 「插入类型转换」。
    完。
    若 错误类型 等于 「未定义变量」：
        返还 「检查拼写或添加声明」。
    完。
    若 错误类型 等于 「作用域越界」：
        返还 「将变量提升到外层作用域」。
    完。
    若 错误类型 等于 「参数数量错误」：
        返还 「检查函数签名」。
    完。
    若 错误类型 等于 「重复定义」：
        返还 「重命名或合并定义」。
    完。
    返还 「检查上下文并修正」。
完。

编译优化_建议 接受 零参：
    可变 建议 为 一组：（空）。
    若 符号表_计数 大于 500：
        做 追加 用 建议、「符号过多: 考虑分模块编译」。
    完。
    若 作用域_最大深度 大于 6：
        做 追加 用 建议、「嵌套过深: 提取子函数」。
    完。
    若 系统扩展_警告数 大于 10：
        做 追加 用 建议、「警告积累: 建议逐步清理」。
    完。
    若 做 内存_有泄漏：
        做 追加 用 建议、「潜在内存泄漏: 检查分配释放配对」。
    完。
    若 做 计数 用 建议 等于 0：
        做 追加 用 建议、「编译状态良好」。
    完。
    返还 建议。
完。

可变 编译_阶段列表 为 一组：（空）。

编译_进入阶段 接受 名称：
    做 追加 用 编译_阶段列表、一组：名称、「开始」。
完。

编译_完成阶段 接受 名称：
    可变 i 为 做 计数 用 编译_阶段列表 减 1。
    只要 i 大于或等于 0：
        若 编译_阶段列表 的 第 i 项 的 第 0 项 等于 名称：
            编译_阶段列表 的 第 i 项 的 第 1 项 为 「完成」。
            返还 空。
        完。
        i 为 i 减 1。
    完。
完。

编译_当前阶段 接受 零参：
    可变 i 为 做 计数 用 编译_阶段列表 减 1。
    只要 i 大于或等于 0：
        若 编译_阶段列表 的 第 i 项 的 第 1 项 等于 「开始」：
            返还 编译_阶段列表 的 第 i 项 的 第 0 项。
        完。
        i 为 i 减 1。
    完。
    返还 「无」。
完。

编译_进度百分比 接受 零参：
    若 做 计数 用 编译_阶段列表 等于 0：
        返还 0。
    完。
    可变 完成 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 编译_阶段列表：
        若 编译_阶段列表 的 第 i 项 的 第 1 项 等于 「完成」：
            完成 为 完成 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 完成 乘 100 除以 做 计数 用 编译_阶段列表。
完。

本章 收束。
