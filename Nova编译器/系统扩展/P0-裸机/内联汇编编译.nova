《内联汇编编译》

引用《..·..·编译器·规划·核心·发射器》。

引用《..·..·标准库·基础》。

引用《..·..·编译器·规划·核心·常量》。

引用《链接脚本支持》。

引用《..·..·自举兼容桥》。

寄存器编码 接受 名：
    若 名 等于 「rax」 或者 名 等于 「eax」 或者 名 等于 「ax」 或者 名 等于 「al」：
        返还 0。
    完。
    若 名 等于 「ah」：
        返还 4。
    完。
    若 名 等于 「rcx」 或者 名 等于 「ecx」 或者 名 等于 「cx」 或者 名 等于 「cl」：
        返还 1。
    完。
    若 名 等于 「ch」：
        返还 5。
    完。
    若 名 等于 「rdx」 或者 名 等于 「edx」 或者 名 等于 「dx」 或者 名 等于 「dl」：
        返还 2。
    完。
    若 名 等于 「dh」：
        返还 6。
    完。
    若 名 等于 「rbx」 或者 名 等于 「ebx」 或者 名 等于 「bx」 或者 名 等于 「bl」：
        返还 3。
    完。
    若 名 等于 「bh」：
        返还 7。
    完。
    若 名 等于 「rsp」 或者 名 等于 「esp」 或者 名 等于 「sp」 或者 名 等于 「spl」：
        返还 4。
    完。
    若 名 等于 「rbp」 或者 名 等于 「ebp」 或者 名 等于 「bp」 或者 名 等于 「bpl」：
        返还 5。
    完。
    若 名 等于 「rsi」 或者 名 等于 「esi」 或者 名 等于 「si」 或者 名 等于 「sil」：
        返还 6。
    完。
    若 名 等于 「rdi」 或者 名 等于 「edi」 或者 名 等于 「di」 或者 名 等于 「dil」：
        返还 7。
    完。
    若 名 等于 「r8」 或者 名 等于 「r8d」 或者 名 等于 「r8w」 或者 名 等于 「r8b」：
        返还 8。
    完。
    若 名 等于 「r9」 或者 名 等于 「r9d」 或者 名 等于 「r9w」 或者 名 等于 「r9b」：
        返还 9。
    完。
    若 名 等于 「r10」 或者 名 等于 「r10d」 或者 名 等于 「r10w」 或者 名 等于 「r10b」：
        返还 10。
    完。
    若 名 等于 「r11」 或者 名 等于 「r11d」 或者 名 等于 「r11w」 或者 名 等于 「r11b」：
        返还 11。
    完。
    若 名 等于 「r12」 或者 名 等于 「r12d」 或者 名 等于 「r12w」 或者 名 等于 「r12b」：
        返还 12。
    完。
    若 名 等于 「r13」 或者 名 等于 「r13d」 或者 名 等于 「r13w」 或者 名 等于 「r13b」：
        返还 13。
    完。
    若 名 等于 「r14」 或者 名 等于 「r14d」 或者 名 等于 「r14w」 或者 名 等于 「r14b」：
        返还 14。
    完。
    若 名 等于 「r15」 或者 名 等于 「r15d」 或者 名 等于 「r15w」 或者 名 等于 「r15b」：
        返还 15。
    完。
    返还 0 减 1。
完。

是64位寄存器 接受 名：
    若 名 等于 「rax」 或者 名 等于 「rcx」 或者 名 等于 「rdx」 或者 名 等于 「rbx」：
        返还 真。
    完。
    若 名 等于 「rsp」 或者 名 等于 「rbp」 或者 名 等于 「rsi」 或者 名 等于 「rdi」：
        返还 真。
    完。
    若 名 等于 「r8」 或者 名 等于 「r9」 或者 名 等于 「r10」 或者 名 等于 「r11」：
        返还 真。
    完。
    若 名 等于 「r12」 或者 名 等于 「r13」 或者 名 等于 「r14」 或者 名 等于 「r15」：
        返还 真。
    完。
    返还 假。
完。

是扩展寄存器 接受 名：
    编码 为 做 寄存器编码 用 名。
    返还 编码 大于或等于 8。
完。

构建REX 接受 W、R、X、B：
    可变 rex 为 0x40。
    若 W：
        rex 为 rex 加 8。
    完。
    若 R：
        rex 为 rex 加 4。
    完。
    若 X：
        rex 为 rex 加 2。
    完。
    若 B：
        rex 为 rex 加 1。
    完。
    返还 rex。
完。

构建ModRM 接受 mod、reg、rm：
    返还 ((mod 位与 3) 左移 6) 加 ((reg 位与 7) 左移 3) 加 (rm 位与 7)。
完。

汇编_低8位寄存器需要REX 接受 名：
    若 名 等于 「spl」 或者 名 等于 「bpl」 或者 名 等于 「sil」 或者 名 等于 「dil」：
        返还 真。
    完。
    若 名 等于 「r8b」 或者 名 等于 「r9b」 或者 名 等于 「r10b」 或者 名 等于 「r11b」：
        返还 真。
    完。
    若 名 等于 「r12b」 或者 名 等于 「r13b」 或者 名 等于 「r14b」 或者 名 等于 「r15b」：
        返还 真。
    完。
    返还 假。
完。

汇编_寄存器位数 接受 名：
    若 做 是字符串 用 名 等于 假：
        返还 0。
    完。
    若 名 等于 「al」 或者 名 等于 「ah」 或者 名 等于 「cl」 或者 名 等于 「ch」：
        返还 8。
    完。
    若 名 等于 「dl」 或者 名 等于 「dh」 或者 名 等于 「bl」 或者 名 等于 「bh」：
        返还 8。
    完。
    若 名 等于 「spl」 或者 名 等于 「bpl」 或者 名 等于 「sil」 或者 名 等于 「dil」：
        返还 8。
    完。
    若 名 等于 「r8b」 或者 名 等于 「r9b」 或者 名 等于 「r10b」 或者 名 等于 「r11b」：
        返还 8。
    完。
    若 名 等于 「r12b」 或者 名 等于 「r13b」 或者 名 等于 「r14b」 或者 名 等于 「r15b」：
        返还 8。
    完。
    若 名 等于 「ax」 或者 名 等于 「cx」 或者 名 等于 「dx」 或者 名 等于 「bx」：
        返还 16。
    完。
    若 名 等于 「sp」 或者 名 等于 「bp」 或者 名 等于 「si」 或者 名 等于 「di」：
        返还 16。
    完。
    若 名 等于 「r8w」 或者 名 等于 「r9w」 或者 名 等于 「r10w」 或者 名 等于 「r11w」：
        返还 16。
    完。
    若 名 等于 「r12w」 或者 名 等于 「r13w」 或者 名 等于 「r14w」 或者 名 等于 「r15w」：
        返还 16。
    完。
    若 名 等于 「eax」 或者 名 等于 「ecx」 或者 名 等于 「edx」 或者 名 等于 「ebx」：
        返还 32。
    完。
    若 名 等于 「esp」 或者 名 等于 「ebp」 或者 名 等于 「esi」 或者 名 等于 「edi」：
        返还 32。
    完。
    若 名 等于 「r8d」 或者 名 等于 「r9d」 或者 名 等于 「r10d」 或者 名 等于 「r11d」：
        返还 32。
    完。
    若 名 等于 「r12d」 或者 名 等于 「r13d」 或者 名 等于 「r14d」 或者 名 等于 「r15d」：
        返还 32。
    完。
    若 做 是64位寄存器 用 名：
        返还 64。
    完。
    返还 0。
完。

汇编_控制寄存器编码 接受 名：
    若 名 等于 「cr0」：
        返还 0。
    完。
    若 名 等于 「cr2」：
        返还 2。
    完。
    若 名 等于 「cr3」：
        返还 3。
    完。
    若 名 等于 「cr4」：
        返还 4。
    完。
    返还 0 减 1。
完。

汇编_段寄存器编码 接受 名：
    若 名 等于 「es」：
        返还 0。
    完。
    若 名 等于 「cs」：
        返还 1。
    完。
    若 名 等于 「ss」：
        返还 2。
    完。
    若 名 等于 「ds」：
        返还 3。
    完。
    若 名 等于 「fs」：
        返还 4。
    完。
    若 名 等于 「gs」：
        返还 5。
    完。
    返还 0 减 1。
完。

汇编_是内存描述 接受 x：
    若 x 等于 空：
        返还 假。
    完。
    _整数类型 为 做 类型。
    若 做 类型 用 x 等于 _整数类型：
        返还 假。
    完。
    若 做 计数 用 x 小于 4：
        返还 假。
    完。
    _字符串类型 为 做 类型 用 「」。
    若 x 的 第 0 项 不等于 1：
        返还 假。
    完。
    若 做 类型 用 x 的 第 1 项 不等于 _字符串类型：
        返还 假。
    完。
    若 做 类型 用 x 的 第 2 项 不等于 _整数类型：
        返还 假。
    完。
    若 做 类型 用 x 的 第 3 项 不等于 _整数类型：
        返还 假。
    完。
    返还 真。
完。

汇编_编码内存描述 接受 基寄存器、位移、大小：
    返还 「__mem__|」 加 基寄存器 加 「|」 加 做 转字符串 用 位移 加 「|」 加 做 转字符串 用 大小。
完。

汇编_稳定操作数引用 接受 x：
    若 做 是字符串 用 x：
        可变 _复制 为 「」。
        可变 _i 为 0。
        _n 为 做 计数 用 x。
        只要 _i 小于 _n：
            _复制 为 做 字符串拼接 用 _复制、做 子串 用 x、_i、1。
            _i 为 _i 加 1。
        完。
        返还 _复制。
    完。
    返还 x。
完。

汇编_列表追加 接受 列表、值：
    做 追加 用 列表、值。
完。

汇编_编码符号引用 接受 名：
    返还 「__sym__|」 加 名。
完。

汇编_解码符号引用 接受 文本：
    若 文本 等于 空：
        返还 文本。
    完。
    _字符串类型 为 做 类型 用 「」。
    若 做 类型 用 文本 不等于 _字符串类型：
        返还 文本。
    完。
    若 做 计数 用 文本 小于 8 或者 做 子串 用 文本、0、8 不等于 「__sym__|」：
        返还 文本。
    完。
    返还 做 字符串拼接 用 「」、做 子串 用 文本、8、做 计数 用 文本 减 8。
完。

汇编_应编码符号引用 接受 文本：
    若 文本 等于 空：
        返还 假。
    完。
    _字符串类型 为 做 类型 用 「」。
    若 做 类型 用 文本 不等于 _字符串类型：
        返还 假。
    完。
    若 做 计数 用 文本 大于或等于 8 并且 做 子串 用 文本、0、8 等于 「__sym__|」：
        返还 假。
    完。
    若 做 寄存器编码 用 文本 大于或等于 0：
        返还 假。
    完。
    若 做 汇编_控制寄存器编码 用 文本 大于或等于 0：
        返还 假。
    完。
    若 做 汇编_段寄存器编码 用 文本 大于或等于 0：
        返还 假。
    完。
    返还 真。
完。

可变 汇编_当前本地标签表 为 做 字典。

可变 汇编_当前本地修正表 为 一组：（空）。

汇编_是本地标签名 接受 文本：
    若 文本 等于 空：
        返还 假。
    完。
    _字符串类型 为 做 类型 用 「」。
    若 做 类型 用 文本 不等于 _字符串类型：
        返还 假。
    完。
    若 做 计数 用 文本 等于 0：
        返还 假。
    完。
    返还 做 子串 用 文本、0、1 等于 「.」。
完。

汇编_本地标签ID 接受 文本：
    返还 「__local__|」 加 做 字符串拼接 用 「」、文本。
完。

汇编_是本地标签ID 接受 文本：
    若 文本 等于 空：
        返还 假。
    完。
    _字符串类型 为 做 类型 用 「」。
    若 做 类型 用 文本 不等于 _字符串类型：
        返还 假。
    完。
    若 做 计数 用 文本 小于 10：
        返还 假。
    完。
    返还 做 子串 用 文本、0、10 等于 「__local__|」。
完。

汇编_提取本地标签内存操作数 接受 操作数：
    若 操作数 等于 空：
        返还 「」。
    完。
    _字符串类型 为 做 类型 用 「」。
    若 做 类型 用 操作数 不等于 _字符串类型：
        返还 「」。
    完。
    s 为 做 汇编_稳定操作数引用 用 做 汇编_去空白 用 操作数。
    若 做 计数 用 s 小于 3：
        返还 「」。
    完。
    若 做 子串 用 s、0、1 不等于 「[」 或者 做 子串 用 s、做 计数 用 s 减 1、1 不等于 「]」：
        返还 「」。
    完。
    内 为 做 汇编_稳定操作数引用 用 做 汇编_去空白 用 做 子串 用 s、1、做 计数 用 s 减 2。
    若 做 汇编_是本地标签名 用 内：
        返还 做 汇编_稳定操作数引用 用 做 汇编_本地标签ID 用 内。
    完。
    返还 「」。
完。

汇编_助记符_使用本地标签ID 接受 助记符：
    若 助记符 等于 「jmp」：
        返还 真。
    完。
    若 助记符 等于 「jnc」 或者 助记符 等于 「jae」：
        返还 真。
    完。
    若 助记符 等于 「jc」 或者 助记符 等于 「jb」：
        返还 真。
    完。
    若 助记符 等于 「jz」 或者 助记符 等于 「je」：
        返还 真。
    完。
    若 助记符 等于 「jnz」 或者 助记符 等于 「jne」：
        返还 真。
    完。
    返还 假。
完。

汇编_本地跳转助记符 接受 助记符：
    返还 助记符 加 「_local」。
完。

汇编_记录本地修正 接受 修类、修位、标签ID：
    做 追加 用 汇编_当前本地修正表、修类。
    做 追加 用 汇编_当前本地修正表、修位。
    做 追加 用 汇编_当前本地修正表、标签ID。
完。

汇编_应用本地修正 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 汇编_当前本地修正表：
        修类 为 汇编_当前本地修正表 的 第 i 项。
        修位 为 汇编_当前本地修正表 的 第 i 加 1 项。
        标签ID 为 汇编_当前本地修正表 的 第 i 加 2 项。
        目标位 为 做 字典取 用 汇编_当前本地标签表、标签ID。
        若 修类 等于 「jmp32_local」：
            rel32 为 目标位 减 (修位 加 4)。
            做 内存写32 用 代码、修位、rel32。
        否则若 修类 等于 「jcc8_local」：
            rel8 为 目标位 减 (修位 加 1)。
            代码 的 第 修位 项 为 rel8 位与 255。
        否则若 修类 等于 「rip32_local」：
            rel32 为 目标位 减 (修位 加 4)。
            做 内存写32 用 代码、修位、rel32。
        完。
        i 为 i 加 3。
    完。
完。

汇编_解码内存描述 接受 文本：
    若 文本 等于 空：
        返还 一组：0、「」。
    完。
    _字符串类型 为 做 类型 用 「」。
    若 做 类型 用 文本 不等于 _字符串类型：
        返还 一组：0、「」。
    完。
    若 做 计数 用 文本 小于 8 或者 做 子串 用 文本、0、8 不等于 「__mem__|」：
        返还 一组：0、「」。
    完。
    可变 分隔1 为 0 减 1。
    可变 分隔2 为 0 减 1。
    可变 i 为 8。
    只要 i 小于 做 计数 用 文本 并且 分隔2 小于 0：
        若 做 子串 用 文本、i、1 等于 「|」：
            若 分隔1 小于 0：
                分隔1 为 i。
            否则：
                分隔2 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    若 分隔1 小于 0 或者 分隔2 小于 0：
        返还 一组：0、「」。
    完。
    基寄存器 为 做 子串 用 文本、8、分隔1 减 8。
    位移文本 为 做 子串 用 文本、分隔1 加 1、分隔2 减 分隔1 减 1。
    大小文本 为 做 子串 用 文本、分隔2 加 1、做 计数 用 文本 减 分隔2 减 1。
    位移结果 为 做 汇编_尝试解析立即数 用 位移文本。
    若 位移结果 的 第 0 项 等于 0：
        返还 一组：0、「」。
    完。
    大小结果 为 做 汇编_尝试解析立即数 用 大小文本。
    若 大小结果 的 第 0 项 等于 0：
        返还 一组：0、「」。
    完。
    若 做 寄存器编码 用 基寄存器 小于 0：
        返还 一组：0、「」。
    完。
    返还 一组：1、基寄存器、位移结果 的 第 1 项、大小结果 的 第 1 项。
完。

汇编_解析内存操作数 接受 文本：
    若 做 汇编_是内存描述 用 文本：
        返还 文本。
    完。
    _已编码内存 为 做 汇编_解码内存描述 用 文本。
    若 _已编码内存 的 第 0 项 等于 1：
        返还 _已编码内存。
    完。
    若 文本 等于 空：
        返还 一组：0、「」。
    完。
    _字符串类型 为 做 类型 用 「」。
    若 做 类型 用 文本 不等于 _字符串类型：
        返还 一组：0、「」。
    完。
    可变 大小 为 0。
    _文本长 为 做 字节长度 用 文本。
    可变 _快起点 为 0。
    若 _文本长 大于或等于 5 并且 做 字节取 用 文本、0 等于 98 并且 做 字节取 用 文本、1 等于 121 并且 做 字节取 用 文本、2 等于 116 并且 做 字节取 用 文本、3 等于 101 并且 做 字节取 用 文本、4 等于 字符_空格：
        大小 为 8。
        _快起点 为 5。
    否则若 _文本长 大于或等于 5 并且 做 字节取 用 文本、0 等于 119 并且 做 字节取 用 文本、1 等于 111 并且 做 字节取 用 文本、2 等于 114 并且 做 字节取 用 文本、3 等于 100 并且 做 字节取 用 文本、4 等于 字符_空格：
        大小 为 16。
        _快起点 为 5。
    否则若 _文本长 大于或等于 6 并且 做 字节取 用 文本、0 等于 100 并且 做 字节取 用 文本、1 等于 119 并且 做 字节取 用 文本、2 等于 111 并且 做 字节取 用 文本、3 等于 114 并且 做 字节取 用 文本、4 等于 100 并且 做 字节取 用 文本、5 等于 字符_空格：
        大小 为 32。
        _快起点 为 6。
    否则若 _文本长 大于或等于 6 并且 做 字节取 用 文本、0 等于 113 并且 做 字节取 用 文本、1 等于 119 并且 做 字节取 用 文本、2 等于 111 并且 做 字节取 用 文本、3 等于 114 并且 做 字节取 用 文本、4 等于 100 并且 做 字节取 用 文本、5 等于 字符_空格：
        大小 为 64。
        _快起点 为 6。
    完。
    可变 _快外文本 为 「」。
    可变 _快k 为 _快起点。
    只要 _快k 小于 _文本长：
        _快外文本 为 做 字符串拼接 用 _快外文本、做 字符 用 做 字节取 用 文本、_快k。
        _快k 为 _快k 加 1。
    完。
    _快外文本 为 做 汇编_去空白 用 _快外文本。
    _快外长 为 做 字节长度 用 _快外文本。
    若 _快外长 大于或等于 2 并且 做 字节取 用 _快外文本、0 等于 字符_左方括 并且 做 字节取 用 _快外文本、_快外长 减 1 等于 字符_右方括：
        可变 _快内文本 为 「」。
        可变 _快j 为 1。
        只要 _快j 小于 _快外长 减 1：
            _快内文本 为 做 字符串拼接 用 _快内文本、做 字符 用 做 字节取 用 _快外文本、_快j。
            _快j 为 _快j 加 1。
        完。
        _快内 为 做 汇编_去空白 用 _快内文本。
        可变 _快分隔位 为 0 减 1。
        可变 _快符号 为 1。
        可变 _快i 为 1。
        只要 _快i 小于 做 计数 用 _快内 并且 _快分隔位 小于 0：
            _快c 为 做 子串 用 _快内、_快i、1。
            若 _快c 等于 「+」：
                _快分隔位 为 _快i。
                _快符号 为 1。
            否则若 _快c 等于 「-」：
                _快分隔位 为 _快i。
                _快符号 为 0 减 1。
            否则：
                _快i 为 _快i 加 1。
            完。
        完。
        若 _快分隔位 小于 0：
            若 做 寄存器编码 用 _快内 小于 0：
                返还 一组：0、「」、0、大小。
            完。
            返还 一组：1、_快内、0、大小。
        完。
        _快基寄存器 为 做 汇编_去空白 用 做 子串 用 _快内、0、_快分隔位。
        若 做 寄存器编码 用 _快基寄存器 小于 0：
            返还 一组：0、「」、0、大小。
        完。
        _快位移文本 为 做 汇编_去空白 用 做 子串 用 _快内、_快分隔位 加 1、做 计数 用 _快内 减 _快分隔位 减 1。
        _快位移结果 为 做 汇编_尝试解析立即数 用 _快位移文本。
        可变 _快位移 为 _快位移结果 的 第 1 项。
        若 _快符号 小于 0：
            _快位移 为 0 减 _快位移。
        完。
        返还 一组：1、_快基寄存器、_快位移、大小。
    完。
    可变 s 为 文本。
    若 做 计数 用 s 大于或等于 5 并且 做 子串 用 s、0、5 等于 「byte 」：
        大小 为 8。
        s 为 做 汇编_去空白 用 做 子串 用 s、5、做 计数 用 s 减 5。
    否则若 做 计数 用 s 大于或等于 5 并且 做 子串 用 s、0、5 等于 「word 」：
        大小 为 16。
        s 为 做 汇编_去空白 用 做 子串 用 s、5、做 计数 用 s 减 5。
    否则若 做 计数 用 s 大于或等于 6 并且 做 子串 用 s、0、6 等于 「dword 」：
        大小 为 32。
        s 为 做 汇编_去空白 用 做 子串 用 s、6、做 计数 用 s 减 6。
    否则若 做 计数 用 s 大于或等于 6 并且 做 子串 用 s、0、6 等于 「qword 」：
        大小 为 64。
        s 为 做 汇编_去空白 用 做 子串 用 s、6、做 计数 用 s 减 6。
    完。
    若 做 计数 用 s 小于 2 或者 做 子串 用 s、0、1 不等于 「[」 或者 做 子串 用 s、做 计数 用 s 减 1、1 不等于 「]」：
        返还 一组：0、「」、0、大小。
    完。
    内 为 做 汇编_去空白 用 做 子串 用 s、1、做 计数 用 s 减 2。
    可变 分隔位 为 0 减 1。
    可变 符号 为 1。
    可变 i 为 1。
    只要 i 小于 做 计数 用 内 并且 分隔位 小于 0：
        c 为 做 子串 用 内、i、1。
        若 c 等于 「+」：
            分隔位 为 i。
            符号 为 1。
        否则若 c 等于 「-」：
            分隔位 为 i。
            符号 为 0 减 1。
        否则：
            i 为 i 加 1。
        完。
    完。
    若 分隔位 小于 0：
        若 做 寄存器编码 用 内 小于 0：
            返还 一组：0、「」、0、大小。
        完。
        返还 一组：1、内、0、大小。
    完。
    基寄存器 为 做 汇编_去空白 用 做 子串 用 内、0、分隔位。
    若 做 寄存器编码 用 基寄存器 小于 0：
        返还 一组：0、「」、0、大小。
    完。
    位移文本 为 做 汇编_去空白 用 做 子串 用 内、分隔位 加 1、做 计数 用 内 减 分隔位 减 1。
    位移结果 为 做 汇编_尝试解析立即数 用 位移文本。
    可变 位移 为 位移结果 的 第 1 项。
    若 符号 小于 0：
        位移 为 0 减 位移。
    完。
    返还 一组：1、基寄存器、位移、大小。
完。

汇编_发射_内存地址 接受 reg字段、基寄存器、位移：
    基编码 为 做 寄存器编码 用 基寄存器。
    基低 为 基编码 位与 7。
    可变 mod 为 0。
    若 位移 等于 0 并且 基低 不等于 5：
        mod 为 0。
    否则若 位移 大于或等于 0 减 128 并且 位移 小于或等于 127：
        mod 为 1。
    否则：
        mod 为 2。
    完。
    若 基低 等于 5 并且 位移 等于 0：
        mod 为 1。
    完。
    可变 rm字段 为 基低。
    若 基低 等于 4：
        rm字段 为 4。
    完。
    做 发射 用 做 构建ModRM 用 mod、reg字段 位与 7、rm字段。
    若 基低 等于 4：
        做 发射 用 做 构建ModRM 用 0、4、基低。
    完。
    若 mod 等于 1：
        做 发射 用 位移 位与 0xFF。
    否则若 mod 等于 2：
        做 发射32 用 位移。
    完。
完。

编译_处理__push_ 接受 操作数1：
    编码 为 做 寄存器编码 用 操作数1。
    若 编码 大于或等于 0：
        若 编码 大于或等于 8：
            做 发射 用 0x41。
            做 发射 用 0x50 加 (编码 位与 7)。
        否则：
            做 发射 用 0x50 加 编码。
        完。
        返还 1。
    完。
    内存 为 做 汇编_解析内存操作数 用 操作数1。
    若 内存 的 第 0 项：
        若 做 寄存器编码 用 内存 的 第 1 项 大于或等于 8：
            做 发射 用 0x41。
        完。
        做 发射 用 0xFF。
        做 汇编_发射_内存地址 用 6、内存 的 第 1 项、内存 的 第 2 项。
        返还 1。
    完。
    若 做 是整数 用 操作数1：
        若 操作数1 大于或等于 0 减 128 并且 操作数1 小于或等于 127：
            做 发射 用 0x6A。
            做 发射 用 操作数1 位与 0xFF。
        否则：
            做 发射 用 0x68。
            做 发射32 用 操作数1。
        完。
        返还 1。
    完。
完。

编译_处理__pop_ 接受 操作数1：
    编码 为 做 寄存器编码 用 操作数1。
    若 编码 大于或等于 0：
        若 编码 大于或等于 8：
            做 发射 用 0x41。
            做 发射 用 0x58 加 (编码 位与 7)。
        否则：
            做 发射 用 0x58 加 编码。
        完。
        返还 1。
    完。
完。

编译_处理__call_ 接受 操作数1：
    目标1 为 做 汇编_解码符号引用 用 操作数1。
    编码 为 做 寄存器编码 用 目标1。
    若 编码 大于或等于 0：
        若 编码 大于或等于 8：
            做 发射 用 0x41。
        完。
        做 发射 用 0xFF。
        做 发射 用 做 构建ModRM 用 3、2、编码 位与 7。
        返还 1。
    完。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、目标1。
    做 发射 用 0xE8。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__jmp_ 接受 操作数1：
    目标1 为 做 汇编_解码符号引用 用 操作数1。
    若 做 类型 用 目标1 等于 做 类型 用 「」 并且 做 计数 用 目标1 大于 0 并且 做 子串 用 目标1、0、1 等于 「.」：
        做 修正表_追加记录 用 修正表、「call」、代码位 加 1、目标1。
        做 发射 用 0xE9。
        做 发射32 用 0。
        返还 1。
    完。
    编码 为 做 寄存器编码 用 目标1。
    若 编码 大于或等于 0：
        若 编码 大于或等于 8：
            做 发射 用 0x41。
        完。
        做 发射 用 0xFF。
        做 发射 用 做 构建ModRM 用 3、4、编码 位与 7。
        返还 1。
    完。
    做 修正表_追加记录 用 修正表、「call」、代码位 加 1、目标1。
    做 发射 用 0xE9。
    做 发射32 用 0。
    返还 1。
完。

编译_处理__lgdt_ 接受 操作数1：
    内存 为 做 汇编_解析内存操作数 用 操作数1。
    若 内存 的 第 0 项：
        若 做 寄存器编码 用 内存 的 第 1 项 大于或等于 8：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0x01。
        做 汇编_发射_内存地址 用 2、内存 的 第 1 项、内存 的 第 2 项。
        返还 1。
    完。
    addr编码 为 做 寄存器编码 用 操作数1。
    若 addr编码 大于或等于 0：
        需要B 为 addr编码 大于或等于 8。
        若 需要B：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0x01。
        做 发射 用 做 构建ModRM 用 0、2、addr编码 位与 7。
    否则：
        做 发射组 用 一组：0x0F、0x01、0x15。
        做 修正表_追加记录 用 修正表、「rip32」、代码位、操作数1。
        做 发射32 用 0。
    完。
    返还 1。
完。

编译_处理__lidt_ 接受 操作数1：
    内存 为 做 汇编_解析内存操作数 用 操作数1。
    若 内存 的 第 0 项：
        若 做 寄存器编码 用 内存 的 第 1 项 大于或等于 8：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0x01。
        做 汇编_发射_内存地址 用 3、内存 的 第 1 项、内存 的 第 2 项。
        返还 1。
    完。
    addr编码 为 做 寄存器编码 用 操作数1。
    若 addr编码 大于或等于 0：
        需要B 为 addr编码 大于或等于 8。
        若 需要B：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0x01。
        做 发射 用 做 构建ModRM 用 0、3、addr编码 位与 7。
    否则：
        做 发射组 用 一组：0x0F、0x01、0x1D。
        做 修正表_追加记录 用 修正表、「rip32」、代码位、操作数1。
        做 发射32 用 0。
    完。
    返还 1。
完。

编译_处理__invlpg_ 接受 操作数1：
    内存 为 做 汇编_解析内存操作数 用 操作数1。
    若 内存 的 第 0 项：
        若 做 寄存器编码 用 内存 的 第 1 项 大于或等于 8：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0x01。
        做 汇编_发射_内存地址 用 7、内存 的 第 1 项、内存 的 第 2 项。
        返还 1。
    完。
    addr编码 为 做 寄存器编码 用 操作数1。
    若 addr编码 大于或等于 0：
        需要B 为 addr编码 大于或等于 8。
        若 需要B：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0x01。
        做 发射 用 做 构建ModRM 用 0、7、addr编码 位与 7。
    完。
    返还 1。
完。

编译_处理__fxsave_ 接受 操作数1：
    内存 为 做 汇编_解析内存操作数 用 操作数1。
    若 内存 的 第 0 项：
        若 做 寄存器编码 用 内存 的 第 1 项 大于或等于 8：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0xAE。
        做 汇编_发射_内存地址 用 0、内存 的 第 1 项、内存 的 第 2 项。
        返还 1。
    完。
    addr编码 为 做 寄存器编码 用 操作数1。
    若 addr编码 大于或等于 0：
        需要B 为 addr编码 大于或等于 8。
        若 需要B：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0xAE。
        做 发射 用 做 构建ModRM 用 0、0、addr编码 位与 7。
    完。
    返还 1。
完。

编译_处理__fxrstor_ 接受 操作数1：
    内存 为 做 汇编_解析内存操作数 用 操作数1。
    若 内存 的 第 0 项：
        若 做 寄存器编码 用 内存 的 第 1 项 大于或等于 8：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0xAE。
        做 汇编_发射_内存地址 用 1、内存 的 第 1 项、内存 的 第 2 项。
        返还 1。
    完。
    addr编码 为 做 寄存器编码 用 操作数1。
    若 addr编码 大于或等于 0：
        需要B 为 addr编码 大于或等于 8。
        若 需要B：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0xAE。
        做 发射 用 做 构建ModRM 用 0、1、addr编码 位与 7。
    完。
    返还 1。
完。

编译_处理__xsave_ 接受 操作数1：
    内存 为 做 汇编_解析内存操作数 用 操作数1。
    若 内存 的 第 0 项：
        若 做 寄存器编码 用 内存 的 第 1 项 大于或等于 8：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0xAE。
        做 汇编_发射_内存地址 用 4、内存 的 第 1 项、内存 的 第 2 项。
        返还 1。
    完。
    addr编码 为 做 寄存器编码 用 操作数1。
    若 addr编码 大于或等于 0：
        需要B 为 addr编码 大于或等于 8。
        若 需要B：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0xAE。
        做 发射 用 做 构建ModRM 用 0、4、addr编码 位与 7。
    完。
    返还 1。
完。

编译_处理__xrstor_ 接受 操作数1：
    内存 为 做 汇编_解析内存操作数 用 操作数1。
    若 内存 的 第 0 项：
        若 做 寄存器编码 用 内存 的 第 1 项 大于或等于 8：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0xAE。
        做 汇编_发射_内存地址 用 5、内存 的 第 1 项、内存 的 第 2 项。
        返还 1。
    完。
    addr编码 为 做 寄存器编码 用 操作数1。
    若 addr编码 大于或等于 0：
        需要B 为 addr编码 大于或等于 8。
        若 需要B：
            做 发射 用 0x41。
        完。
        做 发射组 用 一组：0x0F、0xAE。
        做 发射 用 做 构建ModRM 用 0、5、addr编码 位与 7。
    完。
    返还 1。
完。

编译_处理__fldcw_ 接受 操作数1：
    内存 为 做 汇编_解析内存操作数 用 操作数1。
    若 内存 的 第 0 项：
        若 做 寄存器编码 用 内存 的 第 1 项 大于或等于 8：
            做 发射 用 0x41。
        完。
        做 发射 用 0xD9。
        做 汇编_发射_内存地址 用 5、内存 的 第 1 项、内存 的 第 2 项。
        返还 1。
    完。
    标签ID 为 做 汇编_提取本地标签内存操作数 用 操作数1。
    若 标签ID 不等于 「」：
        做 发射 用 0xD9。
        做 发射 用 0x2D。
        做 汇编_记录本地修正 用 「rip32_local」、代码位、标签ID。
        做 发射32 用 0。
        返还 1。
    完。
    返还 0。
完。

编译_处理__in_ 接受 目标、来源：
    若 来源 等于 「dx」：
        若 目标 等于 「al」：
            做 发射 用 0xEC。
        否则若 目标 等于 「ax」：
            做 发射 用 0x66。
            做 发射 用 0xED。
        否则：
            做 发射 用 0xED。
        完。
    否则若 做 是整数 用 来源 并且 目标 等于 「al」：
        做 发射 用 0xE4。
        做 发射 用 来源 位与 0xFF。
    否则若 做 是整数 用 来源 并且 目标 等于 「ax」：
        做 发射 用 0x66。
        做 发射 用 0xE5。
        做 发射 用 来源 位与 0xFF。
    否则若 做 是整数 用 来源 并且 目标 等于 「eax」：
        做 发射 用 0xE5。
        做 发射 用 来源 位与 0xFF。
    完。
    返还 1。
完。

编译_处理__out_ 接受 目标、来源：
    若 目标 等于 「dx」：
        若 来源 等于 「al」：
            做 发射 用 0xEE。
        否则若 来源 等于 「ax」：
            做 发射 用 0x66。
            做 发射 用 0xEF。
        否则：
            做 发射 用 0xEF。
        完。
    否则若 做 是整数 用 目标 并且 来源 等于 「al」：
        做 发射 用 0xE6。
        做 发射 用 目标 位与 0xFF。
    否则若 做 是整数 用 目标 并且 来源 等于 「ax」：
        做 发射 用 0x66。
        做 发射 用 0xE7。
        做 发射 用 目标 位与 0xFF。
    否则若 做 是整数 用 目标 并且 来源 等于 「eax」：
        做 发射 用 0xE7。
        做 发射 用 目标 位与 0xFF。
    完。
    返还 1。
完。

编译_处理__mov_操作数 接受 dst、src：
    _整数类型 为 做 类型。
    _字符串类型 为 做 类型 用 「」。
    dst编码 为 做 寄存器编码 用 dst。
    若 dst编码 大于或等于 0 并且 做 类型 用 src 等于 _整数类型：
        需要B0 为 dst编码 大于或等于 8。
        做 发射 用 做 构建REX 用 真、假、假、需要B0。
        做 发射 用 0xB8 加 (dst编码 位与 7)。
        做 发射64 用 src。
        返还 1。
    完。
    dst内存 为 做 汇编_解析内存操作数 用 dst。
    src内存 为 做 汇编_解析内存操作数 用 src。
    src编码 为 做 寄存器编码 用 src。
    dst位数 为 做 汇编_寄存器位数 用 dst。
    src位数 为 做 汇编_寄存器位数 用 src。
    dst控制 为 做 汇编_控制寄存器编码 用 dst。
    src控制 为 做 汇编_控制寄存器编码 用 src。
    dst段 为 做 汇编_段寄存器编码 用 dst。
    若 dst控制 大于或等于 0 并且 src编码 大于或等于 0：
        需要R 为 dst控制 大于或等于 8。
        需要B 为 src编码 大于或等于 8。
        做 发射 用 做 构建REX 用 真、需要R、假、需要B。
        做 发射组 用 一组：0x0F、0x22。
        做 发射 用 做 构建ModRM 用 3、dst控制 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 src控制 大于或等于 0 并且 dst编码 大于或等于 0：
        需要R 为 src控制 大于或等于 8。
        需要B 为 dst编码 大于或等于 8。
        做 发射 用 做 构建REX 用 真、需要R、假、需要B。
        做 发射组 用 一组：0x0F、0x20。
        做 发射 用 做 构建ModRM 用 3、src控制 位与 7、dst编码 位与 7。
        返还 1。
    完。
    若 dst段 大于或等于 0 并且 src编码 大于或等于 0：
        若 src编码 大于或等于 8：
            做 发射 用 0x41。
        完。
        做 发射 用 0x8E。
        做 发射 用 做 构建ModRM 用 3、dst段 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 dst编码 大于或等于 0 并且 做 汇编_是内存描述 用 src：
        可变 位数 为 dst位数。
        若 位数 等于 16：
            做 发射 用 0x66。
        完。
        需要W 为 位数 等于 64。
        需要R 为 dst编码 大于或等于 8。
        需要B 为 做 寄存器编码 用 src 的 第 1 项 大于或等于 8。
        若 需要W 或者 需要R 或者 需要B 或者 位数 等于 8 并且 做 汇编_低8位寄存器需要REX 用 dst：
            做 发射 用 做 构建REX 用 需要W、需要R、假、需要B。
        完。
        若 位数 等于 8：
            做 发射 用 0x8A。
        否则：
            做 发射 用 0x8B。
        完。
        做 汇编_发射_内存地址 用 dst编码 位与 7、src 的 第 1 项、src 的 第 2 项。
        返还 1。
    完。
    若 做 汇编_是内存描述 用 dst 并且 src编码 大于或等于 0：
        可变 位数2 为 src位数。
        若 dst 的 第 3 项 大于 0：
            位数2 为 dst 的 第 3 项。
        完。
        若 位数2 等于 16：
            做 发射 用 0x66。
        完。
        需要W2 为 位数2 等于 64。
        需要R2 为 src编码 大于或等于 8。
        需要B2 为 做 寄存器编码 用 dst 的 第 1 项 大于或等于 8。
        若 需要W2 或者 需要R2 或者 需要B2 或者 位数2 等于 8 并且 做 汇编_低8位寄存器需要REX 用 src：
            做 发射 用 做 构建REX 用 需要W2、需要R2、假、需要B2。
        完。
        若 位数2 等于 8：
            做 发射 用 0x88。
        否则：
            做 发射 用 0x89。
        完。
        做 汇编_发射_内存地址 用 src编码 位与 7、dst 的 第 1 项、dst 的 第 2 项。
        返还 1。
    完。
    若 dst编码 大于或等于 0 并且 src内存 的 第 0 项：
        可变 位数3 为 dst位数。
        若 位数3 等于 16：
            做 发射 用 0x66。
        完。
        需要W3 为 位数3 等于 64。
        需要R3 为 dst编码 大于或等于 8。
        需要B3 为 做 寄存器编码 用 src内存 的 第 1 项 大于或等于 8。
        若 需要W3 或者 需要R3 或者 需要B3 或者 位数3 等于 8 并且 做 汇编_低8位寄存器需要REX 用 dst：
            做 发射 用 做 构建REX 用 需要W3、需要R3、假、需要B3。
        完。
        若 位数3 等于 8：
            做 发射 用 0x8A。
        否则：
            做 发射 用 0x8B。
        完。
        做 汇编_发射_内存地址 用 dst编码 位与 7、src内存 的 第 1 项、src内存 的 第 2 项。
        返还 1。
    完。
    若 dst内存 的 第 0 项 并且 src编码 大于或等于 0：
        可变 位数4 为 src位数。
        若 dst内存 的 第 3 项 大于 0：
            位数4 为 dst内存 的 第 3 项。
        完。
        若 位数4 等于 16：
            做 发射 用 0x66。
        完。
        需要W4 为 位数4 等于 64。
        需要R4 为 src编码 大于或等于 8。
        需要B4 为 做 寄存器编码 用 dst内存 的 第 1 项 大于或等于 8。
        若 需要W4 或者 需要R4 或者 需要B4 或者 位数4 等于 8 并且 做 汇编_低8位寄存器需要REX 用 src：
            做 发射 用 做 构建REX 用 需要W4、需要R4、假、需要B4。
        完。
        若 位数4 等于 8：
            做 发射 用 0x88。
        否则：
            做 发射 用 0x89。
        完。
        做 汇编_发射_内存地址 用 src编码 位与 7、dst内存 的 第 1 项、dst内存 的 第 2 项。
        返还 1。
    完。
    若 做 汇编_是内存描述 用 dst 并且 做 类型 用 src 等于 _整数类型：
        可变 位数5 为 dst 的 第 3 项。
        若 位数5 等于 0：
            位数5 为 32。
        完。
        若 位数5 等于 16：
            做 发射 用 0x66。
        完。
        需要W5 为 位数5 等于 64。
        需要B5 为 做 寄存器编码 用 dst 的 第 1 项 大于或等于 8。
        若 需要W5 或者 需要B5：
            做 发射 用 做 构建REX 用 需要W5、假、假、需要B5。
        完。
        若 位数5 等于 8：
            做 发射 用 0xC6。
            做 汇编_发射_内存地址 用 0、dst 的 第 1 项、dst 的 第 2 项。
            做 发射 用 src 位与 0xFF。
        否则：
            做 发射 用 0xC7。
            做 汇编_发射_内存地址 用 0、dst 的 第 1 项、dst 的 第 2 项。
            若 位数5 等于 16：
                做 发射16 用 src。
            否则：
                做 发射32 用 src。
            完。
        完。
        返还 1。
    完。
    若 dst内存 的 第 0 项 并且 做 类型 用 src 等于 _整数类型：
        可变 位数6 为 dst内存 的 第 3 项。
        若 位数6 等于 0：
            位数6 为 32。
        完。
        若 位数6 等于 16：
            做 发射 用 0x66。
        完。
        需要W6 为 位数6 等于 64。
        需要B6 为 做 寄存器编码 用 dst内存 的 第 1 项 大于或等于 8。
        若 需要W6 或者 需要B6：
            做 发射 用 做 构建REX 用 需要W6、假、假、需要B6。
        完。
        若 位数6 等于 8：
            做 发射 用 0xC6。
            做 汇编_发射_内存地址 用 0、dst内存 的 第 1 项、dst内存 的 第 2 项。
            做 发射 用 src 位与 0xFF。
        否则：
            做 发射 用 0xC7。
            做 汇编_发射_内存地址 用 0、dst内存 的 第 1 项、dst内存 的 第 2 项。
            若 位数6 等于 16：
                做 发射16 用 src。
            否则：
                做 发射32 用 src。
            完。
        完。
        返还 1。
    完。
    若 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        需要W7 为 做 是64位寄存器 用 dst 或者 做 是64位寄存器 用 src。
        需要R7 为 src编码 大于或等于 8。
        需要B7 为 dst编码 大于或等于 8。
        若 需要W7 或者 需要R7 或者 需要B7：
            做 发射 用 做 构建REX 用 需要W7、需要R7、假、需要B7。
        完。
        做 发射 用 0x89。
        做 发射 用 做 构建ModRM 用 3、src编码 位与 7、dst编码 位与 7。
        返还 1。
    完。
    若 dst编码 大于或等于 0 并且 做 类型 用 src 等于 _整数类型：
        需要B8 为 dst编码 大于或等于 8。
        做 发射 用 做 构建REX 用 真、假、假、需要B8。
        做 发射 用 0xB8 加 (dst编码 位与 7)。
        做 发射64 用 src。
        返还 1。
    完。
    若 dst编码 大于或等于 0 并且 做 类型 用 src 等于 _字符串类型 并且 src内存 的 第 0 项 等于 0：
        需要B9 为 dst编码 大于或等于 8。
        地址 为 做 链接_查符号 用 src。
        做 发射 用 做 构建REX 用 真、假、假、需要B9。
        做 发射 用 0xB8 加 (dst编码 位与 7)。
        若 地址 不等于 空：
            做 发射64 用 地址。
        否则：
            做 修正表_追加记录 用 修正表、「linker_sym」、代码位 加 2、src。
            做 发射64 用 0。
        完。
        返还 1。
    完。
    返还 0。
完。

编译_处理__movzx_ 接受 dst、src：
    dst编码 为 做 寄存器编码 用 dst。
    src编码 为 做 寄存器编码 用 src。
    dst位数 为 做 汇编_寄存器位数 用 dst。
    src位数 为 做 汇编_寄存器位数 用 src。
    src内存 为 做 汇编_解析内存操作数 用 src。
    若 dst编码 小于 0：
        返还 0。
    完。
    若 src编码 大于或等于 0：
        若 src位数 不等于 8 并且 src位数 不等于 16：
            返还 0。
        完。
        若 dst位数 不等于 16 并且 dst位数 不等于 32 并且 dst位数 不等于 64：
            返还 0。
        完。
        若 dst位数 小于或等于 src位数：
            返还 0。
        完。
        若 dst位数 等于 16：
            做 发射 用 0x66。
        完。
        需要W 为 dst位数 等于 64。
        需要R 为 dst编码 大于或等于 8。
        需要B 为 src编码 大于或等于 8。
        若 需要W 或者 需要R 或者 需要B 或者 src位数 等于 8 并且 做 汇编_低8位寄存器需要REX 用 src：
            做 发射 用 做 构建REX 用 需要W、需要R、假、需要B。
        完。
        做 发射组 用 一组：0x0F、0xB6。
        若 src位数 等于 16：
            代码 的 第 代码位 减 1 项 为 0xB7。
        完。
        做 发射 用 做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 src内存 的 第 0 项：
        sm大小 为 src内存 的 第 3 项。
        若 sm大小 不等于 8 并且 sm大小 不等于 16：
            返还 0。
        完。
        若 dst位数 不等于 16 并且 dst位数 不等于 32 并且 dst位数 不等于 64：
            返还 0。
        完。
        若 dst位数 小于或等于 sm大小：
            返还 0。
        完。
        若 dst位数 等于 16：
            做 发射 用 0x66。
        完。
        需要W2 为 dst位数 等于 64。
        需要R2 为 dst编码 大于或等于 8。
        需要B2 为 做 寄存器编码 用 src内存 的 第 1 项 大于或等于 8。
        若 需要W2 或者 需要R2 或者 需要B2：
            做 发射 用 做 构建REX 用 需要W2、需要R2、假、需要B2。
        完。
        做 发射组 用 一组：0x0F、0xB6。
        若 sm大小 等于 16：
            代码 的 第 代码位 减 1 项 为 0xB7。
        完。
        做 汇编_发射_内存地址 用 dst编码 位与 7、src内存 的 第 1 项、src内存 的 第 2 项。
        返还 1。
    完。
    返还 0。
完。

编译_汇编指令 接受 指令：
    助记符 为 指令 的 第 0 项。
    操作数数 为 做 计数 用 指令 减 1。
    若 助记符 等于 「nop」：
        做 发射 用 0x90。
        返还 1。
    完。
    若 助记符 等于 「ret」：
        做 发射 用 0xC3。
        返还 1。
    完。
    若 助记符 等于 「cli」：
        做 发射 用 0xFA。
        返还 1。
    完。
    若 助记符 等于 「sti」：
        做 发射 用 0xFB。
        返还 1。
    完。
    若 助记符 等于 「hlt」：
        做 发射 用 0xF4。
        返还 1。
    完。
    若 助记符 等于 「int3」：
        做 发射 用 0xCC。
        返还 1。
    完。
    若 助记符 等于 「ud2」：
        做 发射组 用 一组：0x0F、0x0B。
        返还 1。
    完。
    若 助记符 等于 「iretq」：
        做 发射组 用 一组：0x48、0xCF。
        返还 1。
    完。
    若 助记符 等于 「syscall」：
        做 发射组 用 一组：0x0F、0x05。
        返还 1。
    完。
    若 助记符 等于 「sysret」：
        做 发射组 用 一组：0x48、0x0F、0x07。
        返还 1。
    完。
    若 助记符 等于 「swapgs」：
        做 发射组 用 一组：0x0F、0x01、0xF8。
        返还 1。
    完。
    若 助记符 等于 「cpuid」：
        做 发射组 用 一组：0x0F、0xA2。
        返还 1。
    完。
    若 助记符 等于 「rdtsc」：
        做 发射组 用 一组：0x0F、0x31。
        返还 1。
    完。
    若 助记符 等于 「rdtscp」：
        做 发射组 用 一组：0x0F、0x01、0xF9。
        返还 1。
    完。
    若 助记符 等于 「rdmsr」：
        做 发射组 用 一组：0x0F、0x32。
        返还 1。
    完。
    若 助记符 等于 「wrmsr」：
        做 发射组 用 一组：0x0F、0x30。
        返还 1。
    完。
    若 助记符 等于 「mfence」：
        做 发射组 用 一组：0x0F、0xAE、0xF0。
        返还 1。
    完。
    若 助记符 等于 「lfence」：
        做 发射组 用 一组：0x0F、0xAE、0xE8。
        返还 1。
    完。
    若 助记符 等于 「sfence」：
        做 发射组 用 一组：0x0F、0xAE、0xF8。
        返还 1。
    完。
    若 助记符 等于 「pause」：
        做 发射组 用 一组：0xF3、0x90。
        返还 1。
    完。
    若 助记符 等于 「cld」：
        做 发射 用 0xFC。
        返还 1。
    完。
    若 助记符 等于 「std」：
        做 发射 用 0xFD。
        返还 1。
    完。
    若 助记符 等于 「pushfq」：
        做 发射 用 0x9C。
        返还 1。
    完。
    若 助记符 等于 「popfq」：
        做 发射 用 0x9D。
        返还 1。
    完。
    若 助记符 等于 「leave」：
        做 发射 用 0xC9。
        返还 1。
    完。
    若 助记符 等于 「rep movsb」：
        做 发射组 用 一组：0xF3、0xA4。
        返还 1。
    完。
    若 助记符 等于 「rep movsq」：
        做 发射组 用 一组：0xF3、0x48、0xA5。
        返还 1。
    完。
    若 助记符 等于 「rep stosb」：
        做 发射组 用 一组：0xF3、0xAA。
        返还 1。
    完。
    若 助记符 等于 「rep stosq」：
        做 发射组 用 一组：0xF3、0x48、0xAB。
        返还 1。
    完。
    若 助记符 等于 「rep insb」：
        做 发射组 用 一组：0xF3、0x6C。
        返还 1。
    完。
    若 助记符 等于 「rep insw」：
        做 发射组 用 一组：0x66、0xF3、0x6D。
        返还 1。
    完。
    若 助记符 等于 「rep insd」：
        做 发射组 用 一组：0xF3、0x6D。
        返还 1。
    完。
    若 助记符 等于 「rep outsb」：
        做 发射组 用 一组：0xF3、0x6E。
        返还 1。
    完。
    若 助记符 等于 「rep outsw」：
        做 发射组 用 一组：0x66、0xF3、0x6F。
        返还 1。
    完。
    若 助记符 等于 「rep outsd」：
        做 发射组 用 一组：0xF3、0x6F。
        返还 1。
    完。
    若 助记符 等于 「fninit」：
        做 发射组 用 一组：0xDB、0xE3。
        返还 1。
    完。
    若 助记符 等于 「monitor」：
        做 发射组 用 一组：0x0F、0x01、0xC8。
        返还 1。
    完。
    若 助记符 等于 「mwait」：
        做 发射组 用 一组：0x0F、0x01、0xC9。
        返还 1。
    完。
    若 助记符 等于 「rdrand rax」：
        做 发射组 用 一组：0x48、0x0F、0xC7、0xF0。
        返还 1。
    完。
    若 助记符 等于 「xgetbv」：
        做 发射组 用 一组：0x0F、0x01、0xD0。
        返还 1。
    完。
    若 助记符 等于 「xsetbv」：
        做 发射组 用 一组：0x0F、0x01、0xD1。
        返还 1。
    完。
    若 助记符 等于 「ltr ax」：
        做 发射组 用 一组：0x0F、0x00、0xD8。
        返还 1。
    完。
    若 助记符 等于 「vzeroupper」：
        做 发射组 用 一组：0xC5、0xF8、0x77。
        返还 1。
    完。
    若 助记符 等于 「vzeroall」：
        做 发射组 用 一组：0xC5、0xFC、0x77。
        返还 1。
    完。
    若 助记符 等于 「emms」：
        做 发射组 用 一组：0x0F、0x77。
        返还 1。
    完。
    若 助记符 等于 「swapgs」：
        做 发射组 用 一组：0x0F、0x01、0xF8。
        返还 1。
    完。
    若 助记符 等于 「sysret」：
        做 发射组 用 一组：0x48、0x0F、0x07。
        返还 1。
    完。
    若 助记符 等于 「syscall」：
        做 发射组 用 一组：0x0F、0x05。
        返还 1。
    完。
    若 助记符 等于 「iretq」：
        做 发射组 用 一组：0x48、0xCF。
        返还 1。
    完。
    若 助记符 等于 「wbinvd」：
        做 发射组 用 一组：0x0F、0x09。
        返还 1。
    完。
    若 助记符 等于 「invd」：
        做 发射组 用 一组：0x0F、0x08。
        返还 1。
    完。
    若 助记符 等于 「ud2」：
        做 发射组 用 一组：0x0F、0x0B。
        返还 1。
    完。
    若 操作数数 大于或等于 1：
        op1 为 指令 的 第 1 项。
        若 助记符 等于 「push」：
            返还 做 编译_处理__push_ 用 op1。
        完。
        若 助记符 等于 「pop」：
            返还 做 编译_处理__pop_ 用 op1。
        完。
        若 助记符 等于 「int」：
            做 发射 用 0xCD。
            做 发射 用 op1。
            返还 1。
        完。
        若 助记符 等于 「call」：
            返还 做 编译_处理__call_ 用 op1。
        完。
        若 助记符 等于 「jmp_local」：
            做 发射 用 0xE9。
            做 汇编_记录本地修正 用 「jmp32_local」、代码位、op1。
            做 发射32 用 0。
            返还 1。
        完。
        若 助记符 等于 「jmp」：
            做 修正表_追加记录 用 修正表、「call」、代码位 加 1、op1。
            做 发射 用 0xE9。
            做 发射32 用 0。
            返还 1。
        完。
        若 助记符 等于 「jnc_local」 或者 助记符 等于 「jae_local」：
            做 发射 用 0x73。
            做 汇编_记录本地修正 用 「jcc8_local」、代码位、op1。
            做 发射 用 0。
            返还 1。
        完。
        若 助记符 等于 「jnc」 或者 助记符 等于 「jae」：
            做 修正表_追加记录 用 修正表、「jcc8」、代码位 加 1、op1。
            做 发射 用 0x73。
            做 发射 用 0。
            返还 1。
        完。
        若 助记符 等于 「jc_local」 或者 助记符 等于 「jb_local」：
            做 发射 用 0x72。
            做 汇编_记录本地修正 用 「jcc8_local」、代码位、op1。
            做 发射 用 0。
            返还 1。
        完。
        若 助记符 等于 「jc」 或者 助记符 等于 「jb」：
            做 修正表_追加记录 用 修正表、「jcc8」、代码位 加 1、op1。
            做 发射 用 0x72。
            做 发射 用 0。
            返还 1。
        完。
        若 助记符 等于 「jz_local」 或者 助记符 等于 「je_local」：
            做 发射 用 0x74。
            做 汇编_记录本地修正 用 「jcc8_local」、代码位、op1。
            做 发射 用 0。
            返还 1。
        完。
        若 助记符 等于 「jz」 或者 助记符 等于 「je」：
            做 修正表_追加记录 用 修正表、「jcc8」、代码位 加 1、op1。
            做 发射 用 0x74。
            做 发射 用 0。
            返还 1。
        完。
        若 助记符 等于 「jnz_local」 或者 助记符 等于 「jne_local」：
            做 发射 用 0x75。
            做 汇编_记录本地修正 用 「jcc8_local」、代码位、op1。
            做 发射 用 0。
            返还 1。
        完。
        若 助记符 等于 「jnz」 或者 助记符 等于 「jne」：
            做 修正表_追加记录 用 修正表、「jcc8」、代码位 加 1、op1。
            做 发射 用 0x75。
            做 发射 用 0。
            返还 1。
        完。
        若 助记符 等于 「lgdt」：
            返还 做 编译_处理__lgdt_ 用 op1。
        完。
        若 助记符 等于 「lidt」：
            返还 做 编译_处理__lidt_ 用 op1。
        完。
        若 助记符 等于 「invlpg」：
            返还 做 编译_处理__invlpg_ 用 op1。
        完。
        若 助记符 等于 「fxsave」：
            返还 做 编译_处理__fxsave_ 用 op1。
        完。
        若 助记符 等于 「fxrstor」：
            返还 做 编译_处理__fxrstor_ 用 op1。
        完。
        若 助记符 等于 「xsave」：
            返还 做 编译_处理__xsave_ 用 op1。
        完。
        若 助记符 等于 「xrstor」：
            返还 做 编译_处理__xrstor_ 用 op1。
        完。
        若 助记符 等于 「fldcw」：
            返还 做 编译_处理__fldcw_ 用 op1。
        完。
    完。
    若 操作数数 大于或等于 2：
        dst 为 指令 的 第 1 项。
        src 为 指令 的 第 2 项。
        _整数类型 为 做 类型。
        _字符串类型 为 做 类型 用 「」。
        dst内存 为 做 汇编_解析内存操作数 用 dst。
        dst编码 为 做 寄存器编码 用 dst。
        dst位数 为 做 汇编_寄存器位数 用 dst。
        可变 src内存 为 一组：0、0、0。
        可变 src编码 为 0 减 1。
        可变 src位数 为 0。
        若 做 类型 用 src 不等于 _整数类型：
            src内存 为 做 汇编_解析内存操作数 用 src。
            src编码 为 做 寄存器编码 用 src。
            src位数 为 做 汇编_寄存器位数 用 src。
        完。
        若 助记符 等于 「mov」 并且 做 编译_处理__mov_操作数 用 dst、src：
            返还 1。
        完。
        若 助记符 等于 「mov」：
            dst控制 为 做 汇编_控制寄存器编码 用 dst。
            src控制 为 做 汇编_控制寄存器编码 用 src。
            dst段 为 做 汇编_段寄存器编码 用 dst。
            若 dst控制 大于或等于 0 并且 src编码 大于或等于 0：
                需要R 为 dst控制 大于或等于 8。
                需要B 为 src编码 大于或等于 8。
                做 发射 用 做 构建REX 用 真、需要R、假、需要B。
                做 发射组 用 一组：0x0F、0x22。
                做 发射 用 做 构建ModRM 用 3、dst控制 位与 7、src编码 位与 7。
                返还 1。
            完。
            若 src控制 大于或等于 0 并且 dst编码 大于或等于 0：
                需要R 为 src控制 大于或等于 8。
                需要B 为 dst编码 大于或等于 8。
                做 发射 用 做 构建REX 用 真、需要R、假、需要B。
                做 发射组 用 一组：0x0F、0x20。
                做 发射 用 做 构建ModRM 用 3、src控制 位与 7、dst编码 位与 7。
                返还 1。
            完。
            若 dst段 大于或等于 0 并且 src编码 大于或等于 0：
                若 src编码 大于或等于 8：
                    做 发射 用 0x41。
                完。
                做 发射 用 0x8E。
                做 发射 用 做 构建ModRM 用 3、dst段 位与 7、src编码 位与 7。
                返还 1。
            完。
            若 dst编码 大于或等于 0 并且 做 汇编_是内存描述 用 src：
                可变 位数 为 dst位数。
                若 位数 等于 16：
                    做 发射 用 0x66。
                完。
                需要W 为 位数 等于 64。
                需要R 为 dst编码 大于或等于 8。
                需要B 为 做 寄存器编码 用 src 的 第 1 项 大于或等于 8。
                若 需要W 或者 需要R 或者 需要B 或者 位数 等于 8 并且 做 汇编_低8位寄存器需要REX 用 dst：
                    做 发射 用 做 构建REX 用 需要W、需要R、假、需要B。
                完。
                若 位数 等于 8：
                    做 发射 用 0x8A。
                否则：
                    做 发射 用 0x8B。
                完。
                做 汇编_发射_内存地址 用 dst编码 位与 7、src 的 第 1 项、src 的 第 2 项。
                返还 1。
            完。
            若 做 汇编_是内存描述 用 dst 并且 src编码 大于或等于 0：
                可变 位数2 为 src位数。
                若 dst 的 第 3 项 大于 0：
                    位数2 为 dst 的 第 3 项。
                完。
                若 位数2 等于 16：
                    做 发射 用 0x66。
                完。
                需要W2 为 位数2 等于 64。
                需要R2 为 src编码 大于或等于 8。
                需要B2 为 做 寄存器编码 用 dst 的 第 1 项 大于或等于 8。
                若 需要W2 或者 需要R2 或者 需要B2 或者 位数2 等于 8 并且 做 汇编_低8位寄存器需要REX 用 src：
                    做 发射 用 做 构建REX 用 需要W2、需要R2、假、需要B2。
                完。
                若 位数2 等于 8：
                    做 发射 用 0x88。
                否则：
                    做 发射 用 0x89。
                完。
                做 汇编_发射_内存地址 用 src编码 位与 7、dst 的 第 1 项、dst 的 第 2 项。
                返还 1。
            完。
            若 dst编码 大于或等于 0 并且 src内存 的 第 0 项：
                可变 位数 为 dst位数。
                若 位数 等于 16：
                    做 发射 用 0x66。
                完。
                需要W 为 位数 等于 64。
                需要R 为 dst编码 大于或等于 8。
                需要B 为 做 寄存器编码 用 src内存 的 第 1 项 大于或等于 8。
                若 需要W 或者 需要R 或者 需要B 或者 位数 等于 8 并且 做 汇编_低8位寄存器需要REX 用 dst：
                    做 发射 用 做 构建REX 用 需要W、需要R、假、需要B。
                完。
                若 位数 等于 8：
                    做 发射 用 0x8A。
                否则：
                    做 发射 用 0x8B。
                完。
                做 汇编_发射_内存地址 用 dst编码 位与 7、src内存 的 第 1 项、src内存 的 第 2 项。
                返还 1。
            完。
            若 dst内存 的 第 0 项 并且 src编码 大于或等于 0：
                可变 位数2 为 src位数。
                若 dst内存 的 第 3 项 大于 0：
                    位数2 为 dst内存 的 第 3 项。
                完。
                若 位数2 等于 16：
                    做 发射 用 0x66。
                完。
                需要W2 为 位数2 等于 64。
                需要R2 为 src编码 大于或等于 8。
                需要B2 为 做 寄存器编码 用 dst内存 的 第 1 项 大于或等于 8。
                若 需要W2 或者 需要R2 或者 需要B2 或者 位数2 等于 8 并且 做 汇编_低8位寄存器需要REX 用 src：
                    做 发射 用 做 构建REX 用 需要W2、需要R2、假、需要B2。
                完。
                若 位数2 等于 8：
                    做 发射 用 0x88。
                否则：
                    做 发射 用 0x89。
                完。
                做 汇编_发射_内存地址 用 src编码 位与 7、dst内存 的 第 1 项、dst内存 的 第 2 项。
                返还 1。
            完。
            若 做 汇编_是内存描述 用 dst 并且 做 类型 用 src 等于 _整数类型：
                可变 位数3 为 dst 的 第 3 项。
                若 位数3 等于 0：
                    位数3 为 32。
                完。
                若 位数3 等于 16：
                    做 发射 用 0x66。
                完。
                需要W3 为 位数3 等于 64。
                需要B3 为 做 寄存器编码 用 dst 的 第 1 项 大于或等于 8。
                若 需要W3 或者 需要B3：
                    做 发射 用 做 构建REX 用 需要W3、假、假、需要B3。
                完。
                若 位数3 等于 8：
                    做 发射 用 0xC6。
                    做 汇编_发射_内存地址 用 0、dst 的 第 1 项、dst 的 第 2 项。
                    做 发射 用 src 位与 0xFF。
                否则：
                    做 发射 用 0xC7。
                    做 汇编_发射_内存地址 用 0、dst 的 第 1 项、dst 的 第 2 项。
                    若 位数3 等于 16：
                        做 发射16 用 src。
                    否则：
                        做 发射32 用 src。
                    完。
                完。
                返还 1。
            完。
            若 dst内存 的 第 0 项 并且 做 类型 用 src 等于 _整数类型：
                可变 位数3 为 dst内存 的 第 3 项。
                若 位数3 等于 0：
                    位数3 为 32。
                完。
                若 位数3 等于 16：
                    做 发射 用 0x66。
                完。
                需要W3 为 位数3 等于 64。
                需要B3 为 做 寄存器编码 用 dst内存 的 第 1 项 大于或等于 8。
                若 需要W3 或者 需要B3：
                    做 发射 用 做 构建REX 用 需要W3、假、假、需要B3。
                完。
                若 位数3 等于 8：
                    做 发射 用 0xC6。
                    做 汇编_发射_内存地址 用 0、dst内存 的 第 1 项、dst内存 的 第 2 项。
                    做 发射 用 src 位与 0xFF。
                否则：
                    做 发射 用 0xC7。
                    做 汇编_发射_内存地址 用 0、dst内存 的 第 1 项、dst内存 的 第 2 项。
                    若 位数3 等于 16：
                        做 发射16 用 src。
                    否则：
                        做 发射32 用 src。
                    完。
                完。
                返还 1。
            完。
        完。
        若 助记符 等于 「mov」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
            需要W 为 做 是64位寄存器 用 dst 或者 做 是64位寄存器 用 src。
            需要R 为 src编码 大于或等于 8。
            需要B 为 dst编码 大于或等于 8。
            若 需要W 或者 需要R 或者 需要B：
                做 发射 用 做 构建REX 用 需要W、需要R、假、需要B。
            完。
            做 发射 用 0x89。
            做 发射 用 做 构建ModRM 用 3、src编码 位与 7、dst编码 位与 7。
            返还 1。
        完。
        若 助记符 等于 「mov」 并且 dst编码 大于或等于 0 并且 做 类型 用 src 等于 _整数类型：
            需要B 为 dst编码 大于或等于 8。
            做 发射 用 做 构建REX 用 真、假、假、需要B。
            做 发射 用 0xB8 加 (dst编码 位与 7)。
            做 发射64 用 src。
            返还 1。
        完。
        若 助记符 等于 「mov」 并且 dst编码 大于或等于 0 并且 做 类型 用 src 等于 _字符串类型 并且 src内存 的 第 0 项 等于 0：
            需要B4 为 dst编码 大于或等于 8。
            地址 为 做 链接_查符号 用 src。
            做 发射 用 做 构建REX 用 真、假、假、需要B4。
            做 发射 用 0xB8 加 (dst编码 位与 7)。
            若 地址 不等于 空：
                做 发射64 用 地址。
            否则：
                做 修正表_追加记录 用 修正表、「linker_sym」、代码位 加 2、src。
                做 发射64 用 0。
            完。
            返还 1。
        完。
        若 助记符 等于 「movzx」 并且 做 编译_处理__movzx_ 用 dst、src：
            返还 1。
        完。
        若 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
            可变 操作码 为 0。
            若 助记符 等于 「add」：
                操作码 为 0x01。
            完。
            若 助记符 等于 「sub」：
                操作码 为 0x29。
            完。
            若 助记符 等于 「cmp」：
                操作码 为 0x39。
            完。
            若 助记符 等于 「and」：
                操作码 为 0x21。
            完。
            若 助记符 等于 「or」：
                操作码 为 0x09。
            完。
            若 助记符 等于 「xor」：
                操作码 为 0x31。
            完。
            若 助记符 等于 「test」：
                操作码 为 0x85。
            完。
            若 操作码 大于 0：
                需要W 为 做 是64位寄存器 用 dst。
                需要R 为 src编码 大于或等于 8。
                需要B 为 dst编码 大于或等于 8。
                若 需要W 或者 需要R 或者 需要B：
                    做 发射 用 做 构建REX 用 需要W、需要R、假、需要B。
                完。
                做 发射 用 操作码。
                做 发射 用 做 构建ModRM 用 3、src编码 位与 7、dst编码 位与 7。
                返还 1。
            完。
        完。
        若 dst编码 大于或等于 0 并且 做 类型 用 src 等于 _整数类型：
            可变 扩展 为 0 减 1。
            若 助记符 等于 「add」：
                扩展 为 0。
            完。
            若 助记符 等于 「or」：
                扩展 为 1。
            完。
            若 助记符 等于 「and」：
                扩展 为 4。
            完。
            若 助记符 等于 「sub」：
                扩展 为 5。
            完。
            若 助记符 等于 「xor」：
                扩展 为 6。
            完。
            若 助记符 等于 「cmp」：
                扩展 为 7。
            完。
            若 扩展 大于或等于 0：
                需要B 为 dst编码 大于或等于 8。
                做 发射 用 做 构建REX 用 做 是64位寄存器 用 dst、假、假、需要B。
                若 src 大于或等于 负 128 并且 src 小于或等于 127：
                    做 发射 用 0x83。
                    做 发射 用 做 构建ModRM 用 3、扩展、dst编码 位与 7。
                    做 发射 用 src 位与 0xFF。
                否则：
                    做 发射 用 0x81。
                    做 发射 用 做 构建ModRM 用 3、扩展、dst编码 位与 7。
                    做 发射32 用 src。
                完。
                返还 1。
            完。
        完。
        若 助记符 等于 「test」 并且 dst编码 大于或等于 0 并且 做 类型 用 src 等于 _整数类型：
            若 dst位数 等于 16：
                做 发射 用 0x66。
            完。
            需要W12 为 dst位数 等于 64。
            需要B12 为 dst编码 大于或等于 8。
            若 需要W12 或者 需要B12 或者 dst位数 等于 8 并且 做 汇编_低8位寄存器需要REX 用 dst：
                做 发射 用 做 构建REX 用 需要W12、假、假、需要B12。
            完。
            若 dst位数 等于 8：
                做 发射 用 0xF6。
                做 发射 用 做 构建ModRM 用 3、0、dst编码 位与 7。
                做 发射 用 src 位与 0xFF。
            否则：
                做 发射 用 0xF7。
                做 发射 用 做 构建ModRM 用 3、0、dst编码 位与 7。
                若 dst位数 等于 16：
                    做 发射16 用 src。
                否则：
                    做 发射32 用 src。
                完。
            完。
            返还 1。
        完。
        若 助记符 等于 「in」：
            返还 做 编译_处理__in_ 用 dst、src。
        完。
        若 助记符 等于 「out」：
            返还 做 编译_处理__out_ 用 dst、src。
        完。
        若 助记符 等于 「lea」 并且 dst编码 大于或等于 0：
            若 src内存 的 第 0 项：
                需要W 为 做 是64位寄存器 用 dst。
                需要R 为 dst编码 大于或等于 8。
                需要B 为 做 寄存器编码 用 src内存 的 第 1 项 大于或等于 8。
                做 发射 用 做 构建REX 用 需要W、需要R、假、需要B。
                做 发射 用 0x8D。
                做 汇编_发射_内存地址 用 dst编码 位与 7、src内存 的 第 1 项、src内存 的 第 2 项。
                返还 1。
            否则若 src编码 大于或等于 0：
                需要W 为 做 是64位寄存器 用 dst。
                需要R 为 dst编码 大于或等于 8。
                需要B 为 src编码 大于或等于 8。
                做 发射 用 做 构建REX 用 需要W、需要R、假、需要B。
                做 发射 用 0x8D。
                做 发射 用 做 构建ModRM 用 0、dst编码 位与 7、src编码 位与 7。
                返还 1。
            否则：
                需要R 为 dst编码 大于或等于 8。
                做 发射 用 做 构建REX 用 真、需要R、假、假。
                做 发射 用 0x8D。
                做 发射 用 做 构建ModRM 用 0、dst编码 位与 7、5。
                做 修正表_追加记录 用 修正表、「rip32」、代码位、src。
                做 发射32 用 0。
                返还 1。
            完。
        完。
        若 dst编码 大于或等于 0 并且 做 类型 用 src 等于 _整数类型：
            可变 是移位指令 为 假。
            若 助记符 等于 「shl」：
                是移位指令 为 真。
            完。
            若 助记符 等于 「shr」：
                是移位指令 为 真。
            完。
            若 助记符 等于 「sar」：
                是移位指令 为 真。
            完。
            若 是移位指令：
                需要B 为 dst编码 大于或等于 8。
                做 发射 用 做 构建REX 用 做 是64位寄存器 用 dst、假、假、需要B。
                做 发射 用 0xC1。
                可变 扩展码 为 4。
                若 助记符 等于 「shr」：
                    扩展码 为 5。
                完。
                若 助记符 等于 「sar」：
                    扩展码 为 7。
                完。
                做 发射 用 做 构建ModRM 用 3、扩展码、dst编码 位与 7。
                做 发射 用 src 位与 0xFF。
                返还 1。
            完。
        完。
    完。
    若 助记符 等于 「movaps」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x0F、0x28、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「movups」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x0F、0x10、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「xorps」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x0F、0x57、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「xorpd」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0x57、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「addps」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x0F、0x58、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「addpd」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0x58、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「mulps」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x0F、0x59、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「mulpd」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0x59、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「subps」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x0F、0x5C、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「subpd」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0x5C、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「divps」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x0F、0x5E、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「divpd」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0x5E、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「sqrtps」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x0F、0x51、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「sqrtpd」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0x51、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「minps」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x0F、0x5D、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「maxps」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x0F、0x5F、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「paddb」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0xFC、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「paddw」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0xFD、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「paddd」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0xFE、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「paddq」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0xD4、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「pxor」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0xEF、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「pand」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0xDB、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「por」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0x66、0x0F、0xEB、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「movsd」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0xF2、0x0F、0x10、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「movss」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0xF3、0x0F、0x10、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「cvtsi2sd」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0xF2、0x48、0x0F、0x2A、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「cvtsd2si」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0xF2、0x48、0x0F、0x2D、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「movsx」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        _sx_dst位 为 做 汇编_寄存器位数 用 dst。
        _sx_src位 为 做 汇编_寄存器位数 用 src。
        若 _sx_src位 等于 8：
            _需W 为 _sx_dst位 等于 64。
            _需R 为 dst编码 大于或等于 8。
            _需B 为 src编码 大于或等于 8。
            若 _需W 或者 _需R 或者 _需B：
                做 发射 用 做 构建REX 用 _需W、_需R、假、_需B。
            完。
            做 发射组 用 一组：0x0F、0xBE、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
            返还 1。
        完。
        若 _sx_src位 等于 16：
            _需W 为 _sx_dst位 等于 64。
            _需R 为 dst编码 大于或等于 8。
            _需B 为 src编码 大于或等于 8。
            若 _需W 或者 _需R 或者 _需B：
                做 发射 用 做 构建REX 用 _需W、_需R、假、_需B。
            完。
            做 发射组 用 一组：0x0F、0xBF、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
            返还 1。
        完。
        若 _sx_src位 等于 32 并且 _sx_dst位 等于 64：
            _需R 为 dst编码 大于或等于 8。
            _需B 为 src编码 大于或等于 8。
            做 发射 用 做 构建REX 用 真、_需R、假、_需B。
            做 发射 用 0x63。
            做 发射 用 做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
            返还 1。
        完。
    完。
    若 助记符 等于 「sete」 并且 dst编码 大于或等于 0：
        若 dst编码 大于或等于 8 或者 做 汇编_低8位寄存器需要REX 用 dst：
            做 发射 用 做 构建REX 用 假、假、假、dst编码 大于或等于 8。
        完。
        做 发射组 用 一组：0x0F、0x94、做 构建ModRM 用 3、0、dst编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「setne」 并且 dst编码 大于或等于 0：
        若 dst编码 大于或等于 8 或者 做 汇编_低8位寄存器需要REX 用 dst：
            做 发射 用 做 构建REX 用 假、假、假、dst编码 大于或等于 8。
        完。
        做 发射组 用 一组：0x0F、0x95、做 构建ModRM 用 3、0、dst编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「setl」 并且 dst编码 大于或等于 0：
        若 dst编码 大于或等于 8 或者 做 汇编_低8位寄存器需要REX 用 dst：
            做 发射 用 做 构建REX 用 假、假、假、dst编码 大于或等于 8。
        完。
        做 发射组 用 一组：0x0F、0x9C、做 构建ModRM 用 3、0、dst编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「setge」 并且 dst编码 大于或等于 0：
        若 dst编码 大于或等于 8 或者 做 汇编_低8位寄存器需要REX 用 dst：
            做 发射 用 做 构建REX 用 假、假、假、dst编码 大于或等于 8。
        完。
        做 发射组 用 一组：0x0F、0x9D、做 构建ModRM 用 3、0、dst编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「setle」 并且 dst编码 大于或等于 0：
        若 dst编码 大于或等于 8 或者 做 汇编_低8位寄存器需要REX 用 dst：
            做 发射 用 做 构建REX 用 假、假、假、dst编码 大于或等于 8。
        完。
        做 发射组 用 一组：0x0F、0x9E、做 构建ModRM 用 3、0、dst编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「setg」 并且 dst编码 大于或等于 0：
        若 dst编码 大于或等于 8 或者 做 汇编_低8位寄存器需要REX 用 dst：
            做 发射 用 做 构建REX 用 假、假、假、dst编码 大于或等于 8。
        完。
        做 发射组 用 一组：0x0F、0x9F、做 构建ModRM 用 3、0、dst编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「setb」 并且 dst编码 大于或等于 0：
        若 dst编码 大于或等于 8 或者 做 汇编_低8位寄存器需要REX 用 dst：
            做 发射 用 做 构建REX 用 假、假、假、dst编码 大于或等于 8。
        完。
        做 发射组 用 一组：0x0F、0x92、做 构建ModRM 用 3、0、dst编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「setae」 并且 dst编码 大于或等于 0：
        若 dst编码 大于或等于 8 或者 做 汇编_低8位寄存器需要REX 用 dst：
            做 发射 用 做 构建REX 用 假、假、假、dst编码 大于或等于 8。
        完。
        做 发射组 用 一组：0x0F、0x93、做 构建ModRM 用 3、0、dst编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「seta」 并且 dst编码 大于或等于 0：
        若 dst编码 大于或等于 8 或者 做 汇编_低8位寄存器需要REX 用 dst：
            做 发射 用 做 构建REX 用 假、假、假、dst编码 大于或等于 8。
        完。
        做 发射组 用 一组：0x0F、0x97、做 构建ModRM 用 3、0、dst编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「setbe」 并且 dst编码 大于或等于 0：
        若 dst编码 大于或等于 8 或者 做 汇编_低8位寄存器需要REX 用 dst：
            做 发射 用 做 构建REX 用 假、假、假、dst编码 大于或等于 8。
        完。
        做 发射组 用 一组：0x0F、0x96、做 构建ModRM 用 3、0、dst编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「vmovaps」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0xC5、0xF8、0x28、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「vmovups」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        做 发射组 用 一组：0xC5、0xF8、0x10、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「vxorps」 并且 dst编码 大于或等于 0 并且 src编码 大于或等于 0：
        _vvvv 为 按位取反 (dst编码 位与 0xF) 位与 0xF。
        做 发射组 用 一组：0xC5、(0x78 位或 _vvvv 左移 3) 位与 0xFF、0x57、做 构建ModRM 用 3、dst编码 位与 7、src编码 位与 7。
        返还 1。
    完。
    若 助记符 等于 「mov」：
        可变 _dst文本 为 做 转字符串 用 dst。
        可变 _src文本 为 做 转字符串 用 src。
        若 做 是字符串 用 dst：
            _dst文本 为 做 字符串拼接 用 「」、dst。
        完。
        若 做 是字符串 用 src：
            _src文本 为 做 字符串拼接 用 「」、src。
        完。
    完。
    可变 调试文本 为 「汇编警告: 未识别指令 助记符=」 加 助记符 加 「 参数数=」 加 做 转字符串 用 做 计数 用 指令。
    做 打印 用 调试文本。
    返还 0。
完。

编译_汇编_mov_文本行 接受 行：
    行 为 做 汇编_稳定操作数引用 用 行。
    行 为 做 汇编_稳定操作数引用 用 做 汇编_去空白 用 行。
    可变 空格位 为 0 减 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行：
        若 空格位 小于 0 并且 (做 子串 用 行、i、1 等于 「 」 或者 做 子串 用 行、i、1 等于 「\t」)：
            空格位 为 i。
        完。
        i 为 i 加 1。
    完。
    若 空格位 小于 0：
        返还 0。
    完。
    若 做 汇编_稳定操作数引用 用 做 子串 用 行、0、空格位 不等于 「mov」：
        返还 0。
    完。
    操作数文本 为 做 汇编_稳定操作数引用 用 做 汇编_去空白 用 做 子串 用 行、空格位 加 1、做 计数 用 行 减 空格位 减 1。
    操作数列 为 做 汇编_拆操作数 用 操作数文本。
    若 做 计数 用 操作数列 小于 2：
        返还 0。
    完。
    dst 为 做 汇编_稳定操作数引用 用 操作数列 的 第 0 项。
    可变 src 为 做 汇编_稳定操作数引用 用 操作数列 的 第 1 项。
    可变 立即数结果 为 一组：0、0。
    若 做 汇编_应尝试解析立即数 用 src：
        立即数结果 为 做 汇编_尝试解析立即数 用 src。
    完。
    若 立即数结果 的 第 0 项：
        src 为 立即数结果 的 第 1 项。
    完。
    返还 做 编译_处理__mov_操作数 用 dst、src。
完。

编译_汇编块 接受 指令列表：
    可变 标签表 为 做 字典。
    可变 临时位 为 代码位。
    可变 i 为 0。
    只要 i 小于 做 计数 用 指令列表：
        指令 为 指令列表 的 第 i 项。
        若 指令 的 第 0 项 等于 「label」 或者 指令 的 第 0 项 等于 「label_local」：
            做 字典设 用 标签表、指令 的 第 1 项、临时位。
        否则：
            临时位 为 临时位 加 6。
        完。
        i 为 i 加 1。
    完。
    汇编_当前本地标签表 为 做 字典。
    汇编_当前本地修正表 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 做 计数 用 指令列表：
        指令 为 指令列表 的 第 j 项。
        若 指令 的 第 0 项 等于 「label_local」：
            做 字典设 用 汇编_当前本地标签表、指令 的 第 1 项、代码位。
        否则若 指令 的 第 0 项 等于 「label」：
            做 字典设 用 函数表、指令 的 第 1 项、代码位。
        否则若 指令 的 第 0 项 等于 「raw」：
            可变 bi 为 1。
            只要 bi 小于 做 计数 用 指令：
                做 发射 用 指令 的 第 bi 项。
                bi 为 bi 加 1。
            完。
        否则若 指令 的 第 0 项 等于 「mov_text」：
            做 编译_汇编_mov_文本行 用 指令 的 第 1 项。
        否则：
            做 编译_汇编指令 用 指令。
        完。
        j 为 j 加 1。
    完。
    做 汇编_应用本地修正。
    汇编_当前本地标签表 为 做 字典。
    汇编_当前本地修正表 为 一组：（空）。
完。

编译_原始字节 接受 字节列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 字节列表：
        做 发射 用 字节列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

汇编_去空白 接受 s：
    n 为 做 计数 用 s。
    可变 开始 为 0。
    只要 开始 小于 n 并且 (做 字符串等于 用 做 子串 用 s、开始、1、「 」 或者 做 字符串等于 用 做 子串 用 s、开始、1、「\t」)：
        开始 为 开始 加 1。
    完。
    可变 结束 为 n。
    只要 结束 大于 开始 并且 (做 字符串等于 用 做 子串 用 s、结束 减 1、1、「 」 或者 做 字符串等于 用 做 子串 用 s、结束 减 1、1、「\t」)：
        结束 为 结束 减 1。
    完。
    若 开始 大于或等于 结束：
        返还 「」。
    完。
    返还 做 子串 用 s、开始、结束 减 开始。
完。

汇编_十六字符值 接受 c：
    值 为 做 编码 用 c。
    若 值 大于或等于 48 并且 值 小于或等于 57：
        返还 值 减 48。
    完。
    若 值 大于或等于 65 并且 值 小于或等于 70：
        返还 值 减 55。
    完。
    若 值 大于或等于 97 并且 值 小于或等于 102：
        返还 值 减 87。
    完。
    返还 0 减 1。
完。

汇编_表达式_跳空白 接受 s、开始：
    可变 i 为 开始。
    只要 i 小于 做 计数 用 s 并且 (做 子串 用 s、i、1 等于 「 」 或者 做 子串 用 s、i、1 等于 「\t」)：
        i 为 i 加 1。
    完。
    返还 i。
完。

汇编_解析数字片段 接受 s、开始：
    i 为 做 汇编_表达式_跳空白 用 s、开始。
    若 i 大于或等于 做 计数 用 s：
        返还 一组：0、0、开始。
    完。
    若 i 加 1 小于 做 计数 用 s 并且 (做 子串 用 s、i、2 等于 「0x」 或者 做 子串 用 s、i、2 等于 「0X」)：
        可变 值 为 0。
        可变 j 为 i 加 2。
        可变 至少一位 为 假。
        只要 j 小于 做 计数 用 s：
            半字节 为 做 汇编_十六字符值 用 做 子串 用 s、j、1。
            若 半字节 小于 0：
                j 为 做 计数 用 s。
            完。
            值 为 值 乘 16 加 半字节。
            至少一位 为 真。
            j 为 j 加 1。
        完。
        若 至少一位 等于 假：
            返还 一组：0、0、开始。
        完。
        返还 一组：1、值、j。
    完。
    可变 值2 为 0。
    可变 j2 为 i。
    可变 至少一位2 为 假。
    只要 j2 小于 做 计数 用 s：
        c值 为 做 编码 用 做 子串 用 s、j2、1。
        若 c值 小于 48 或者 c值 大于 57：
            j2 为 做 计数 用 s。
        完。
        值2 为 值2 乘 10 加 (c值 减 48)。
        至少一位2 为 真。
        j2 为 j2 加 1。
    完。
    若 至少一位2 等于 假：
        返还 一组：0、0、开始。
    完。
    返还 一组：1、值2、j2。
完。

汇编_解析表达式_主项 接受 s、开始：
    i 为 做 汇编_表达式_跳空白 用 s、开始。
    若 i 大于或等于 做 计数 用 s：
        返还 一组：0、0、开始。
    完。
    若 做 子串 用 s、i、1 等于 「(」：
        结果 为 做 汇编_解析表达式_位移 用 s、i 加 1。
        若 结果 的 第 0 项 等于 0：
            返还 一组：0、0、开始。
        完。
        结束 为 做 汇编_表达式_跳空白 用 s、结果 的 第 2 项。
        若 结束 大于或等于 做 计数 用 s 或者 做 子串 用 s、结束、1 不等于 「)」：
            返还 一组：0、0、开始。
        完。
        返还 一组：1、结果 的 第 1 项、结束 加 1。
    完。
    返还 做 汇编_解析数字片段 用 s、i。
完。

汇编_解析表达式_一元 接受 s、开始：
    i 为 做 汇编_表达式_跳空白 用 s、开始。
    若 i 大于或等于 做 计数 用 s：
        返还 一组：0、0、开始。
    完。
    c 为 做 子串 用 s、i、1。
    若 c 等于 「+」：
        返还 做 汇编_解析表达式_一元 用 s、i 加 1。
    完。
    若 c 等于 「-」：
        结果 为 做 汇编_解析表达式_一元 用 s、i 加 1。
        若 结果 的 第 0 项 等于 0：
            返还 一组：0、0、开始。
        完。
        返还 一组：1、0 减 结果 的 第 1 项、结果 的 第 2 项。
    完。
    若 c 等于 「~」：
        结果2 为 做 汇编_解析表达式_一元 用 s、i 加 1。
        若 结果2 的 第 0 项 等于 0：
            返还 一组：0、0、开始。
        完。
        返还 一组：1、按位取反 结果2 的 第 1 项、结果2 的 第 2 项。
    完。
    返还 做 汇编_解析表达式_主项 用 s、i。
完。

汇编_解析表达式_加减 接受 s、开始：
    左 为 做 汇编_解析表达式_一元 用 s、开始。
    若 左 的 第 0 项 等于 0：
        返还 左。
    完。
    可变 值 为 左 的 第 1 项。
    可变 i 为 左 的 第 2 项。
    只要 i 小于 做 计数 用 s：
        i 为 做 汇编_表达式_跳空白 用 s、i。
        若 i 大于或等于 做 计数 用 s：
            返还 一组：1、值、i。
        完。
        op 为 做 子串 用 s、i、1。
        若 op 不等于 「+」 并且 op 不等于 「-」：
            返还 一组：1、值、i。
        完。
        右 为 做 汇编_解析表达式_一元 用 s、i 加 1。
        若 右 的 第 0 项 等于 0：
            返还 一组：0、0、开始。
        完。
        若 op 等于 「+」：
            值 为 值 加 右 的 第 1 项。
        否则：
            值 为 值 减 右 的 第 1 项。
        完。
        i 为 右 的 第 2 项。
    完。
    返还 一组：1、值、i。
完。

汇编_解析表达式_位移 接受 s、开始：
    左 为 做 汇编_解析表达式_加减 用 s、开始。
    若 左 的 第 0 项 等于 0：
        返还 左。
    完。
    可变 值 为 左 的 第 1 项。
    可变 i 为 左 的 第 2 项。
    只要 i 小于 做 计数 用 s：
        i 为 做 汇编_表达式_跳空白 用 s、i。
        若 i 加 1 大于或等于 做 计数 用 s：
            返还 一组：1、值、i。
        完。
        op 为 做 子串 用 s、i、2。
        若 op 不等于 「<<」 并且 op 不等于 「>>」：
            返还 一组：1、值、i。
        完。
        右 为 做 汇编_解析表达式_加减 用 s、i 加 2。
        若 右 的 第 0 项 等于 0：
            返还 一组：0、0、开始。
        完。
        若 op 等于 「<<」：
            值 为 值 左移 右 的 第 1 项。
        否则：
            值 为 值 右移 右 的 第 1 项。
        完。
        i 为 右 的 第 2 项。
    完。
    返还 一组：1、值、i。
完。

汇编_尝试解析立即数 接受 文本：
    s 为 做 汇编_去空白 用 文本。
    若 s 等于 「」：
        返还 一组：0、0。
    完。
    若 做 汇编_是本地标签名 用 s：
        返还 一组：0、0。
    完。
    结果 为 做 汇编_解析表达式_位移 用 s、0。
    若 结果 的 第 0 项 等于 0：
        返还 一组：0、0。
    完。
    结束 为 做 汇编_表达式_跳空白 用 s、结果 的 第 2 项。
    若 结束 不等于 做 计数 用 s：
        返还 一组：0、0。
    完。
    返还 一组：1、结果 的 第 1 项。
完。

汇编_应尝试解析立即数 接受 文本：
    若 做 是字符串 用 文本 等于 假：
        返还 假。
    完。
    s 为 做 汇编_去空白 用 文本。
    若 s 等于 「」：
        返还 假。
    完。
    若 做 计数 用 s 大于或等于 2 并且 (做 子串 用 s、0、2 等于 「0x」 或者 做 子串 用 s、0、2 等于 「0X」)：
        返还 真。
    完。
    c0 为 做 子串 用 s、0、1。
    若 c0 等于 「+」 或者 c0 等于 「-」 或者 c0 等于 「~」 或者 c0 等于 「(」：
        返还 真。
    完。
    c0值 为 做 编码 用 c0。
    若 c0值 大于或等于 48 并且 c0值 小于或等于 57：
        返还 真。
    完。
    返还 假。
完。

汇编_拆操作数 接受 文本：
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        c 为 做 子串 用 文本、i、1。
        若 做 字符串等于 用 c、「,」：
            项 为 做 汇编_去空白 用 当前。
            若 项 不等于 「」：
                _项内存 为 做 汇编_解析内存操作数 用 项。
                若 _项内存 的 第 0 项 等于 1：
                    做 追加 用 结果、做 汇编_稳定操作数引用 用 做 汇编_编码内存描述 用 _项内存 的 第 1 项、_项内存 的 第 2 项、_项内存 的 第 3 项。
                否则：
                    做 追加 用 结果、做 汇编_稳定操作数引用 用 项。
                完。
            完。
            当前 为 「」。
        否则：
            当前 为 做 字符串拼接 用 当前、c。
        完。
        i 为 i 加 1。
    完。
    末项 为 做 汇编_去空白 用 当前。
    若 末项 不等于 「」：
        _末项内存 为 做 汇编_解析内存操作数 用 末项。
        若 _末项内存 的 第 0 项 等于 1：
            做 追加 用 结果、做 汇编_稳定操作数引用 用 做 汇编_编码内存描述 用 _末项内存 的 第 1 项、_末项内存 的 第 2 项、_末项内存 的 第 3 项。
        否则：
            做 追加 用 结果、做 汇编_稳定操作数引用 用 末项。
        完。
    完。
    返还 结果。
完。

汇编_构造解析操作数结果 接受 助记符、操作数列：
    可变 oi 为 0。
    可变 输出助记符 为 助记符。
    可变 指令 为 一组：（空）。
    只要 oi 小于 做 计数 用 操作数列：
        可变 _编码内存 为 做 汇编_解码内存描述 用 操作数列 的 第 oi 项。
        可变 立即数结果 为 一组：0、0。
        若 做 汇编_应尝试解析立即数 用 操作数列 的 第 oi 项：
            立即数结果 为 做 汇编_尝试解析立即数 用 操作数列 的 第 oi 项。
        完。
        _本地助记符允许 为 做 汇编_助记符_使用本地标签ID 用 助记符。
        _是本地标签操作数 为 做 汇编_是本地标签名 用 操作数列 的 第 oi 项。
        若 立即数结果 的 第 0 项：
            做 汇编_列表追加 用 指令、立即数结果 的 第 1 项。
        否则若 做 汇编_是内存描述 用 操作数列 的 第 oi 项 或者 _编码内存 的 第 0 项 等于 1：
            做 汇编_列表追加 用 指令、做 汇编_稳定操作数引用 用 操作数列 的 第 oi 项。
        否则若 _本地助记符允许 并且 _是本地标签操作数：
            若 oi 等于 0：
                输出助记符 为 做 汇编_本地跳转助记符 用 助记符。
            完。
            做 汇编_列表追加 用 指令、做 汇编_稳定操作数引用 用 做 汇编_本地标签ID 用 操作数列 的 第 oi 项。
        否则若 助记符 等于 「jmp」 并且 做 汇编_应编码符号引用 用 操作数列 的 第 oi 项：
            做 汇编_列表追加 用 指令、做 汇编_稳定操作数引用 用 做 汇编_编码符号引用 用 操作数列 的 第 oi 项。
        否则：
            做 汇编_列表追加 用 指令、做 汇编_稳定操作数引用 用 操作数列 的 第 oi 项。
        完。
        oi 为 oi 加 1。
    完。
    返还 一组：输出助记符、指令。
完。

汇编_解析文本行 接受 行：
    可变 ji 为 0。
    行 为 做 汇编_稳定操作数引用 用 行。
    行 为 做 汇编_稳定操作数引用 用 做 汇编_去空白 用 行。
    若 行 等于 「」：
        返还 空。
    完。
    若 做 计数 用 行 大于或等于 6 并且 做 子串 用 行、0、6 等于 「.byte 」：
        数据文本 为 做 汇编_稳定操作数引用 用 做 汇编_去空白 用 做 子串 用 行、6、做 计数 用 行 减 6。
        数据列 为 做 汇编_拆操作数 用 数据文本。
        可变 指令 为 一组：「raw」。
        可变 di 为 0。
        只要 di 小于 做 计数 用 数据列：
            值结果 为 做 汇编_尝试解析立即数 用 数据列 的 第 di 项。
            若 值结果 的 第 0 项 等于 0：
                返还 一组：行。
            完。
            做 汇编_列表追加 用 指令、值结果 的 第 1 项 位与 0xFF。
            di 为 di 加 1。
        完。
        返还 指令。
    完。
    若 做 计数 用 行 大于或等于 3 并且 做 子串 用 行、0、3 等于 「dw 」：
        数据文本2 为 做 汇编_稳定操作数引用 用 做 汇编_去空白 用 做 子串 用 行、3、做 计数 用 行 减 3。
        数据列2 为 做 汇编_拆操作数 用 数据文本2。
        可变 指令2 为 一组：「raw」。
        可变 di2 为 0。
        只要 di2 小于 做 计数 用 数据列2：
            值结果2 为 做 汇编_尝试解析立即数 用 数据列2 的 第 di2 项。
            若 值结果2 的 第 0 项 等于 0：
                返还 一组：行。
            完。
            做 汇编_列表追加 用 指令2、值结果2 的 第 1 项 位与 0xFF。
            做 汇编_列表追加 用 指令2、值结果2 的 第 1 项 右移 8 位与 0xFF。
            di2 为 di2 加 1。
        完。
        返还 指令2。
    完。
    若 做 子串 用 行、做 计数 用 行 减 1、1 等于 「:」：
        标签名 为 做 汇编_稳定操作数引用 用 做 子串 用 行、0、做 计数 用 行 减 1。
        若 做 汇编_是本地标签名 用 标签名：
            返还 一组：「label_local」、做 汇编_本地标签ID 用 标签名。
        完。
        返还 一组：「label」、标签名。
    完。
    若 行 等于 「rep movsb」 或者 行 等于 「rep movsq」 或者 行 等于 「rep stosb」 或者 行 等于 「rep stosq」 或者 行 等于 「rep insb」 或者 行 等于 「rep insw」 或者 行 等于 「rep insd」 或者 行 等于 「rep outsb」 或者 行 等于 「rep outsw」 或者 行 等于 「rep outsd」 或者 行 等于 「rdrand rax」 或者 行 等于 「ltr ax」：
        返还 一组：行。
    完。
    可变 空格位 为 0 减 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行：
        若 空格位 小于 0 并且 (做 子串 用 行、i、1 等于 「 」 或者 做 子串 用 行、i、1 等于 「\t」)：
            空格位 为 i。
        完。
        i 为 i 加 1。
    完。
    若 空格位 小于 0：
        返还 一组：行。
    完。
    助记符 为 做 汇编_稳定操作数引用 用 做 子串 用 行、0、空格位。
    操作数文本 为 做 汇编_稳定操作数引用 用 做 汇编_去空白 用 做 子串 用 行、空格位 加 1、做 计数 用 行 减 空格位 减 1。
    操作数列 为 做 汇编_拆操作数 用 操作数文本。
    _操作数结果 为 做 汇编_构造解析操作数结果 用 助记符、操作数列。
    输出助记符 为 _操作数结果 的 第 0 项。
    指令 为 _操作数结果 的 第 1 项。
    可变 结果指令 为 一组：输出助记符。
    只要 ji 小于 做 计数 用 指令：
        做 汇编_列表追加 用 结果指令、指令 的 第 ji 项。
        ji 为 ji 加 1。
    完。
    返还 结果指令。
完。

汇编_拆分行首标签 接受 行：
    s 为 做 汇编_稳定操作数引用 用 做 汇编_去空白 用 行。
    可变 i 为 0。
    只要 i 小于 做 计数 用 s：
        c 为 做 子串 用 s、i、1。
        若 c 等于 「:」：
            标签行 为 做 汇编_稳定操作数引用 用 做 子串 用 s、0、i 加 1。
            剩余行 为 做 汇编_稳定操作数引用 用 做 汇编_去空白 用 做 子串 用 s、i 加 1、做 计数 用 s 减 i 减 1。
            若 剩余行 等于 「」：
                返还 一组：0、「」、「」。
            完。
            返还 一组：1、标签行、剩余行。
        完。
        若 c 等于 「 」 或者 c 等于 「\t」：
            返还 一组：0、「」、「」。
        完。
        i 为 i 加 1。
    完。
    返还 一组：0、「」、「」。
完。

汇编_追加文本行指令 接受 指令列、规范文本行：
    若 规范文本行 等于 「」：
        返还 0。
    完。
    若 做 计数 用 规范文本行 大于或等于 4 并且 做 子串 用 规范文本行、0、4 等于 「mov 」：
        做 汇编_列表追加 用 指令列、一组：「mov_text」、规范文本行。
    否则：
        指令 为 做 汇编_解析文本行 用 规范文本行。
        若 指令 不等于 空 并且 指令 的 第 0 项 不等于 「extern」：
            做 汇编_列表追加 用 指令列、指令。
        完。
    完。
    返还 1。
完。

编译_汇编文本块 接受 文本列：
    可变 指令列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本列：
        文本行 为 做 汇编_稳定操作数引用 用 文本列 的 第 i 项。
        规范文本行 为 做 汇编_稳定操作数引用 用 做 汇编_去空白 用 文本行。
        行首标签结果 为 做 汇编_拆分行首标签 用 规范文本行。
        若 行首标签结果 的 第 0 项：
            做 汇编_追加文本行指令 用 指令列、行首标签结果 的 第 1 项。
            做 汇编_追加文本行指令 用 指令列、行首标签结果 的 第 2 项。
        否则：
            做 汇编_追加文本行指令 用 指令列、规范文本行。
        完。
        i 为 i 加 1。
    完。
    做 编译_汇编块 用 指令列。
完。

本章 收束。
