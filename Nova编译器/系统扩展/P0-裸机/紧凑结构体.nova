《紧凑结构体》

引用《..·..·标准库·基础》。

引用《..·..·编译器·规划·核心·发射器》。

节_packed结构体 为 90。

节_位域字段 为 91。

大小_u8 为 1。

大小_u16 为 2。

大小_u32 为 4。

大小_u64 为 8。

大小_i8 为 1。

大小_i16 为 2。

大小_i32 为 4。

大小_i64 为 8。

可变 packed表 为 做 字典。

查类型大小 接受 类型名：
    若 类型名 等于 「u8」：
        返还 大小_u8。
    完。
    若 类型名 等于 「u16」：
        返还 大小_u16。
    完。
    若 类型名 等于 「u32」：
        返还 大小_u32。
    完。
    若 类型名 等于 「u64」：
        返还 大小_u64。
    完。
    若 类型名 等于 「i8」：
        返还 大小_i8。
    完。
    若 类型名 等于 「i16」：
        返还 大小_i16。
    完。
    若 类型名 等于 「i32」：
        返还 大小_i32。
    完。
    若 类型名 等于 「i64」：
        返还 大小_i64。
    完。
    嵌套 为 做 字典取 用 packed表、类型名。
    若 嵌套 不等于 空：
        返还 嵌套 的 第 0 项。
    完。
    返还 8。
完。

注册packed结构体 接受 名称、字段列表：
    可变 当前偏移 为 0。
    可变 布局 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字段列表：
        字段 为 字段列表 的 第 i 项。
        字段名 为 字段 的 第 0 项。
        类型名 为 字段 的 第 1 项。
        类型大小 为 做 查类型大小 用 类型名。
        若 做 计数 用 字段 大于或等于 3：
            位宽 为 字段 的 第 2 项。
            做 追加 用 布局、一组：字段名、当前偏移、类型大小、类型名、0、位宽。
            当前偏移 为 当前偏移 加 类型大小。
        否则：
            做 追加 用 布局、一组：字段名、当前偏移、类型大小、类型名。
            当前偏移 为 当前偏移 加 类型大小。
        完。
        i 为 i 加 1。
    完。
    做 字典设 用 packed表、名称、一组：当前偏移、布局。
    返还 当前偏移。
完。

编译_packed读字段 接受 结构体名、字段名：
    信息 为 做 字典取 用 packed表、结构体名。
    若 信息 等于 空：
        做 打印 用 「错误: 未知packed结构体 」 加 结构体名。
        返还 0。
    完。
    布局 为 信息 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 布局：
        项 为 布局 的 第 i 项。
        若 项 的 第 0 项 等于 字段名：
            偏移 为 项 的 第 1 项。
            大小 为 项 的 第 2 项。
            位起始 为 项 的 第 4 项。
            位宽 为 项 的 第 5 项。
            若 大小 等于 1：
                若 偏移 等于 0：
                    做 发射组 用 一组：0x48、0x0F、0xB6、0x00。
                否则：
                    做 发射组 用 一组：0x48、0x0F、0xB6、0x80。
                    做 发射32 用 偏移。
                完。
            否则若 大小 等于 2：
                若 偏移 等于 0：
                    做 发射组 用 一组：0x48、0x0F、0xB7、0x00。
                否则：
                    做 发射组 用 一组：0x48、0x0F、0xB7、0x80。
                    做 发射32 用 偏移。
                完。
            否则若 大小 等于 4：
                若 偏移 等于 0：
                    做 发射组 用 一组：0x8B、0x00。
                否则：
                    做 发射组 用 一组：0x8B、0x80。
                    做 发射32 用 偏移。
                完。
            否则：
                若 偏移 等于 0：
                    做 发射组 用 一组：0x48、0x8B、0x00。
                否则：
                    做 发射组 用 一组：0x48、0x8B、0x80。
                    做 发射32 用 偏移。
                完。
            完。
            若 位宽 大于 0：
                若 位起始 大于 0：
                    做 发射组 用 一组：0x48、0xC1、0xE8。
                    做 发射 用 位起始。
                完。
                掩码 为 (1 左移 位宽) 减 1。
                若 掩码 小于或等于 0x7F：
                    做 发射组 用 一组：0x48、0x83、0xE0。
                    做 发射 用 掩码。
                否则：
                    做 发射组 用 一组：0x48、0x25。
                    做 发射32 用 掩码。
                完。
            完。
            返还 1。
        完。
        i 为 i 加 1。
    完。
    做 打印 用 「错误: packed结构体 」 加 结构体名 加 「 无字段 」 加 字段名。
    返还 0。
完。

编译_处理_packed字段写入 接受 项：
    偏移 为 项 的 第 1 项。
    大小 为 项 的 第 2 项。
    位起始 为 项 的 第 4 项。
    位宽 为 项 的 第 5 项。
    若 位宽 大于 0：
        做 发射 用 0x50。
        若 大小 等于 1：
            做 发射组 用 一组：0x0F、0xB6、0x91。
            做 发射32 用 偏移。
        否则若 大小 等于 2：
            做 发射组 用 一组：0x0F、0xB7、0x91。
            做 发射32 用 偏移。
        否则若 大小 等于 4：
            做 发射组 用 一组：0x8B、0x91。
            做 发射32 用 偏移。
        否则：
            做 发射组 用 一组：0x48、0x8B、0x91。
            做 发射32 用 偏移。
        完。
        清除掩码 为 0xFFFFFFFFFFFFFFFF 位异或 (1 左移 位宽) 减 1 左移 位起始。
        做 发射组 用 一组：0x48、0x81、0xE2。
        做 发射32 用 清除掩码。
        做 发射 用 0x58。
        若 位起始 大于 0：
            做 发射组 用 一组：0x48、0xC1、0xE0。
            做 发射 用 位起始。
        完。
        值掩码 为 (1 左移 位宽) 减 1 左移 位起始。
        做 发射组 用 一组：0x48、0x25。
        做 发射32 用 值掩码。
        做 发射组 用 一组：0x48、0x09、0xD0。
        若 大小 等于 1：
            做 发射组 用 一组：0x88、0x81。
            做 发射32 用 偏移。
        否则若 大小 等于 2：
            做 发射组 用 一组：0x66、0x89、0x81。
            做 发射32 用 偏移。
        否则若 大小 等于 4：
            做 发射组 用 一组：0x89、0x81。
            做 发射32 用 偏移。
        否则：
            做 发射组 用 一组：0x48、0x89、0x81。
            做 发射32 用 偏移。
        完。
    否则：
        若 大小 等于 1：
            若 偏移 等于 0：
                做 发射组 用 一组：0x88、0x01。
            否则：
                做 发射组 用 一组：0x88、0x81。
                做 发射32 用 偏移。
            完。
        否则若 大小 等于 2：
            若 偏移 等于 0：
                做 发射组 用 一组：0x66、0x89、0x01。
            否则：
                做 发射组 用 一组：0x66、0x89、0x81。
                做 发射32 用 偏移。
            完。
        否则若 大小 等于 4：
            若 偏移 等于 0：
                做 发射组 用 一组：0x89、0x01。
            否则：
                做 发射组 用 一组：0x89、0x81。
                做 发射32 用 偏移。
            完。
        否则：
            若 偏移 等于 0：
                做 发射组 用 一组：0x48、0x89、0x01。
            否则：
                做 发射组 用 一组：0x48、0x89、0x81。
                做 发射32 用 偏移。
            完。
        完。
    完。
    返还 1。
完。

编译_packed写字段 接受 结构体名、字段名：
    信息 为 做 字典取 用 packed表、结构体名。
    若 信息 等于 空：
        返还 0。
    完。
    布局 为 信息 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 布局：
        项 为 布局 的 第 i 项。
        若 项 的 第 0 项 等于 字段名：
            返还 做 编译_处理_packed字段写入 用 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

packed大小 接受 结构体名：
    信息 为 做 字典取 用 packed表、结构体名。
    若 信息 等于 空：
        返还 0。
    完。
    返还 信息 的 第 0 项。
完。

packed字段偏移 接受 结构体名、字段名：
    信息 为 做 字典取 用 packed表、结构体名。
    若 信息 等于 空：
        返还 0 减 1。
    完。
    布局 为 信息 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 布局：
        若 布局 的 第 i 项 的 第 0 项 等于 字段名：
            返还 布局 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 0 减 1。
完。

注册内核packed结构体 接受 零参：
    做 注册packed结构体 用 「GDT条目」、一组：一组：「限长低16」、「u16」、一组：「基址低16」、「u16」、一组：「基址中8」、「u8」、一组：「访问字节」、「u8」、一组：「限长高4_标志4」、「u8」、一组：「基址高8」、「u8」。
    做 注册packed结构体 用 「IDT条目」、一组：一组：「偏移低16」、「u16」、一组：「段选择子」、「u16」、一组：「IST」、「u8」、一组：「类型属性」、「u8」、一组：「偏移中16」、「u16」、一组：「偏移高32」、「u32」、一组：「保留」、「u32」。
    做 注册packed结构体 用 「TSS64」、一组：一组：「保留0」、「u32」、一组：「RSP0低」、「u32」、一组：「RSP0高」、「u32」、一组：「RSP1低」、「u32」、一组：「RSP1高」、「u32」、一组：「RSP2低」、「u32」、一组：「RSP2高」、「u32」、一组：「保留1」、「u32」、一组：「保留2」、「u32」、一组：「IST1低」、「u32」、一组：「IST1高」、「u32」、一组：「IST2低」、「u32」、一组：「IST2高」、「u32」、一组：「IST3低」、「u32」、一组：「IST3高」、「u32」、一组：「IST4低」、「u32」、一组：「IST4高」、「u32」、一组：「IST5低」、「u32」、一组：「IST5高」、「u32」、一组：「IST6低」、「u32」、一组：「IST6高」、「u32」、一组：「IST7低」、「u32」、一组：「IST7高」、「u32」、一组：「保留3」、「u32」、一组：「保留4」、「u32」、一组：「IO位图偏移」、「u16」、一组：「保留5」、「u16」。
    做 注册packed结构体 用 「描述符指针」、一组：一组：「限长」、「u16」、一组：「基址」、「u64」。
    做 注册packed结构体 用 「页表条目」、一组：一组：「值」、「u64」。
完。

packed_写入u8 接受 缓冲、偏移、值：
    缓冲 的 第 偏移 项 为 值 位与 0xFF。
    返还 偏移 加 1。
完。

packed_写入u16 接受 缓冲、偏移、值：
    缓冲 的 第 偏移 项 为 值 位与 0xFF。
    缓冲 的 第 偏移 加 1 项 为 值 右移 8 位与 0xFF。
    返还 偏移 加 2。
完。

packed_写入u32 接受 缓冲、偏移、值：
    缓冲 的 第 偏移 项 为 值 位与 0xFF。
    缓冲 的 第 偏移 加 1 项 为 值 右移 8 位与 0xFF。
    缓冲 的 第 偏移 加 2 项 为 值 右移 16 位与 0xFF。
    缓冲 的 第 偏移 加 3 项 为 值 右移 24 位与 0xFF。
    返还 偏移 加 4。
完。

packed_写入u64 接受 缓冲、偏移、值：
    做 packed_写入u32 用 缓冲、偏移、值 位与 0xFFFFFFFF。
    做 packed_写入u32 用 缓冲、偏移 加 4、值 右移 32 位与 0xFFFFFFFF。
    返还 偏移 加 8。
完。

packed_读取u8 接受 缓冲、偏移：
    返还 缓冲 的 第 偏移 项 位与 0xFF。
完。

packed_读取u16 接受 缓冲、偏移：
    返还 缓冲 的 第 偏移 项 位与 0xFF 位或 (缓冲 的 第 偏移 加 1 项 位与 0xFF) 左移 8。
完。

packed_读取u32 接受 缓冲、偏移：
    返还 缓冲 的 第 偏移 项 位与 0xFF 位或 (缓冲 的 第 偏移 加 1 项 位与 0xFF) 左移 8 位或 (缓冲 的 第 偏移 加 2 项 位与 0xFF) 左移 16 位或 (缓冲 的 第 偏移 加 3 项 位与 0xFF) 左移 24。
完。

packed_读取u64 接受 缓冲、偏移：
    低 为 做 packed_读取u32 用 缓冲、偏移。
    高 为 做 packed_读取u32 用 缓冲、偏移 加 4。
    返还 低 位或 高 左移 32。
完。

packed_校验和 接受 缓冲、起始、长度：
    可变 和 为 0。
    可变 i 为 起始。
    只要 i 小于 起始 加 长度：
        和 为 和 加 (缓冲 的 第 i 项 位与 0xFF)。
        i 为 i 加 1。
    完。
    返还 和 位与 0xFF。
完。

紧凑_CRC8 接受 缓冲、起始、长度：
    可变 crc 为 0xFF。
    可变 i 为 起始。
    只要 i 小于 起始 加 长度：
        crc 为 crc 位异或 缓冲 的 第 i 项 位与 0xFF。
        可变 j 为 0。
        只要 j 小于 8：
            若 crc 位与 1：
                crc 为 crc 右移 1 位异或 0x8C。
            否则：
                crc 为 crc 右移 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 crc。
完。

packed_对齐偏移 接受 偏移、对齐：
    若 对齐 等于 0：
        返还 偏移。
    完。
    余 为 偏移 取余于 对齐。
    若 余 等于 0：
        返还 偏移。
    完。
    返还 偏移 加 对齐 减 余。
完。

可变 _packed_序列化次数 为 0。

可变 _packed_反序列化次数 为 0。

可变 _packed_总字节数 为 0。

packed_记录序列化 接受 字节数：
    _packed_序列化次数 为 _packed_序列化次数 加 1。
    _packed_总字节数 为 _packed_总字节数 加 字节数。
完。

packed_记录反序列化 接受 零参：
    _packed_反序列化次数 为 _packed_反序列化次数 加 1。
完。

packed_效率评估 接受 零参：
    若 _packed_总字节数 大于 10000000：
        返还 「大量序列化(考虑批量操作)」。
    完。
    若 _packed_序列化次数 大于 10000：
        返还 「高频序列化」。
    完。
    返还 「序列化正常」。
完。

packed_重置 接受 零参：
    _packed_序列化次数 为 0。
    _packed_反序列化次数 为 0。
    _packed_总字节数 为 0。
完。

packed_填充零 接受 缓冲、起始、长度：
    可变 i 为 起始。
    只要 i 小于 起始 加 长度：
        缓冲 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
完。

packed_复制 接受 源、源偏移、目标、目标偏移、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        目标 的 第 目标偏移 加 i 项 为 源 的 第 源偏移 加 i 项。
        i 为 i 加 1。
    完。
完。

packed_比较 接受 a、a偏移、b、b偏移、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        若 a 的 第 a偏移 加 i 项 不等于 b 的 第 b偏移 加 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

packed_转十六进制 接受 值：
    高 为 值 右移 4 位与 0xF。
    低 为 值 位与 0xF。
    可变 结果 为 「」。
    若 高 小于 10：
        结果 为 做 字符 用 48 加 高。
    否则：
        结果 为 做 字符 用 65 加 高 减 10。
    完。
    若 低 小于 10：
        结果 为 做 字符串拼接 用 结果、做 字符 用 48 加 低。
    否则：
        结果 为 做 字符串拼接 用 结果、做 字符 用 65 加 低 减 10。
    完。
    返还 结果。
完。

本章 收束。
