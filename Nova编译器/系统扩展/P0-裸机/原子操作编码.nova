《原子操作编码》

引用《..·..·编译器·规划·核心·发射器》。

编译_处理__原子_交换_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0xF0、0x48、0x87、0x01。
    返还 1。
完。

编译_处理__原子_比较交换_ 接受 参数：
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0x48、0x8B、0x14、0x24。
    做 发射组 用 一组：0x48、0x83、0xC4、0x08。
    做 发射组 用 一组：0xF0、0x48、0x0F、0xB1、0x11。
    返还 1。
完。

编译_处理__原子_加_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0xF0、0x48、0x0F、0xC1、0x01。
    返还 1。
完。

编译_处理__原子_减_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0x48、0xF7、0xD8。
    做 发射组 用 一组：0xF0、0x48、0x0F、0xC1、0x01。
    返还 1。
完。

编译_处理__原子_递增_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射组 用 一组：0xF0、0x48、0xFF、0x01。
    返还 1。
完。

编译_处理__原子_递减_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射组 用 一组：0xF0、0x48、0xFF、0x09。
    返还 1。
完。

编译_处理__原子_位测试设置_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0xF0、0x48、0x0F、0xAB、0x01。
    做 发射组 用 一组：0x48、0x19、0xC0。
    做 发射组 用 一组：0x48、0xF7、0xD8。
    返还 1。
完。

编译_处理__原子_位测试清除_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0xF0、0x48、0x0F、0xB3、0x01。
    做 发射组 用 一组：0x48、0x19、0xC0。
    做 发射组 用 一组：0x48、0xF7、0xD8。
    返还 1。
完。

编译_处理__原子_位测试翻转_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0xF0、0x48、0x0F、0xBB、0x01。
    做 发射组 用 一组：0x48、0x19、0xC0。
    做 发射组 用 一组：0x48、0xF7、0xD8。
    返还 1。
完。

编译_处理__原子_与_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0xF0、0x48、0x21、0x01。
    返还 1。
完。

编译_处理__原子_或_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0xF0、0x48、0x09、0x01。
    返还 1。
完。

编译_处理__原子_异或_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0xF0、0x48、0x31、0x01。
    返还 1。
完。

编译_处理__原子_读_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射组 用 一组：0x48、0x8B、0x00。
    做 发射组 用 一组：0x0F、0xAE、0xE8。
    返还 1。
完。

编译_处理__原子_写_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0x0F、0xAE、0xF8。
    做 发射组 用 一组：0x48、0x89、0x01。
    返还 1。
完。

编译_处理__自旋锁_获取_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    重试位 为 代码位。
    做 发射组 用 一组：0xB8、0x01、0x00、0x00、0x00。
    做 发射组 用 一组：0xF0、0x87、0x01。
    做 发射组 用 一组：0x85、0xC0。
    做 发射 用 0x74。
    做 发射 用 0x04。
    做 发射组 用 一组：0xF3、0x90。
    跳回偏移 为 重试位 减 (代码位 加 2)。
    做 发射 用 0xEB。
    做 发射 用 跳回偏移 位与 0xFF。
    返还 1。
完。

编译_处理__自旋锁_释放_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射组 用 一组：0x0F、0xAE、0xF8。
    做 发射组 用 一组：0xC7、0x01、0x00、0x00、0x00、0x00。
    返还 1。
完。

编译_处理__自旋锁_尝试_ 接受 参数：
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射组 用 一组：0xB8、0x01、0x00、0x00、0x00。
    做 发射组 用 一组：0xF0、0x87、0x01。
    做 发射组 用 一组：0x83、0xF0、0x01。
    返还 1。
完。

编译_原子操作 接受 名、参数：
    若 名 等于 「原子_交换」：
        返还 做 编译_处理__原子_交换_ 用 参数。
    完。
    若 名 等于 「原子_比较交换」：
        返还 做 编译_处理__原子_比较交换_ 用 参数。
    完。
    若 名 等于 「原子_加」：
        返还 做 编译_处理__原子_加_ 用 参数。
    完。
    若 名 等于 「原子_减」：
        返还 做 编译_处理__原子_减_ 用 参数。
    完。
    若 名 等于 「原子_递增」：
        返还 做 编译_处理__原子_递增_ 用 参数。
    完。
    若 名 等于 「原子_递减」：
        返还 做 编译_处理__原子_递减_ 用 参数。
    完。
    若 名 等于 「原子_位测试设置」：
        返还 做 编译_处理__原子_位测试设置_ 用 参数。
    完。
    若 名 等于 「原子_位测试清除」：
        返还 做 编译_处理__原子_位测试清除_ 用 参数。
    完。
    若 名 等于 「原子_位测试翻转」：
        返还 做 编译_处理__原子_位测试翻转_ 用 参数。
    完。
    若 名 等于 「原子_与」：
        返还 做 编译_处理__原子_与_ 用 参数。
    完。
    若 名 等于 「原子_或」：
        返还 做 编译_处理__原子_或_ 用 参数。
    完。
    若 名 等于 「原子_异或」：
        返还 做 编译_处理__原子_异或_ 用 参数。
    完。
    若 名 等于 「原子_读」：
        返还 做 编译_处理__原子_读_ 用 参数。
    完。
    若 名 等于 「原子_写」：
        返还 做 编译_处理__原子_写_ 用 参数。
    完。
    若 名 等于 「自旋锁_获取」：
        返还 做 编译_处理__自旋锁_获取_ 用 参数。
    完。
    若 名 等于 「自旋锁_释放」：
        返还 做 编译_处理__自旋锁_释放_ 用 参数。
    完。
    若 名 等于 「自旋锁_尝试」：
        返还 做 编译_处理__自旋锁_尝试_ 用 参数。
    完。
    返还 0。
完。

本章 收束。
