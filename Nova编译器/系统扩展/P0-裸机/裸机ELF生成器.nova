《裸机ELF生成器》

引用《.·链接脚本支持》。

引用《..·..·标准库·基础》。

引用《..·..·标准库·系统·硬件库》。

引用《..·..·标准库·转换器·转换验证》。

引用《..·..·编译器·规划·核心·常量》。

引用《..·..·自举兼容桥》。

MB2_魔数 为 0xE85250D6。

MB2_架构_I386 为 0。

MB2_标签_结束 为 0。

MB2_标签_信息请求 为 1。

MB2_标签_地址 为 2。

MB2_标签_入口 为 3。

MB2_标签_标志 为 4。

MB2_标签_帧缓冲 为 5。

MB2_标签_对齐 为 6。

内核基址 为 0xFFFFFFFF80100000。

物理加载地址 为 0x100000。

页大小 为 0x1000。

栈大小 为 0x4000。

构建裸机ELF 接受 入口偏移、代码大小、数据段内容大小、BSS大小：
    做 打印 用 「构建裸机 ELF64 内核镜像 (x86-64, Multiboot2)...」。
    若 数据段内容大小 等于 空：
        数据段内容大小 为 0。
    完。
    若 BSS大小 等于 空：
        BSS大小 为 0。
    完。
    elf头大小 为 64。
    程序头大小 为 56。
    程序头数 为 3。
    头区大小 为 elf头大小 加 程序头大小 乘 程序头数。
    mb2偏移 为 (头区大小 加 7) 除以 8 乘 8。
    mb2大小 为 做 构建Multiboot2头大小。
    代码段偏移 为 页大小。
    代码段虚址 为 内核基址 加 代码段偏移。
    代码段对齐大小 为 (代码大小 加 页大小 减 1) 除以 页大小 乘 页大小。
    数据段偏移 为 代码段偏移 加 代码段对齐大小。
    数据段虚址 为 内核基址 加 数据段偏移。
    数据段物理 为 物理加载地址 加 数据段偏移。
    数据段文件大小 为 数据段内容大小。
    数据段内存大小 为 数据段内容大小 加 BSS大小 加 栈大小。
    文件大小 为 数据段偏移 加 数据段文件大小。
    可变 elf 为 做 缓冲区 用 文件大小 加 页大小。
    做 缓冲写 用 elf、x7F、0。
    做 缓冲写 用 elf、1、0x45。
    做 缓冲写 用 elf、2、0x4C。
    做 缓冲写 用 elf、3、0x46。
    做 缓冲写 用 elf、4、2。
    做 缓冲写 用 elf、5、1。
    做 缓冲写 用 elf、6、1。
    做 缓冲写 用 elf、7、0。
    做 缓冲写16 用 elf、16、ELF类型_可执行。
    做 缓冲写16 用 elf、18、处理器_X86_64。
    做 缓冲写32 用 elf、20、1。
    做 缓冲写64 用 elf、24、代码段虚址 加 入口偏移。
    做 缓冲写64 用 elf、32、elf头大小。
    做 缓冲写64 用 elf、40、0。
    做 缓冲写32 用 elf、48、0。
    做 缓冲写16 用 elf、52、elf头大小。
    做 缓冲写16 用 elf、54、程序头大小。
    做 缓冲写16 用 elf、56、程序头数。
    做 缓冲写16 用 elf、58、64。
    做 缓冲写16 用 elf、60、0。
    做 缓冲写16 用 elf、62、0。
    ph1 为 elf头大小。
    做 缓冲写32 用 elf、ph1 加 0、段类型_加载。
    做 缓冲写32 用 elf、ph1 加 4、段标志_可读 加 段标志_可执行。
    做 缓冲写64 用 elf、ph1 加 8、0。
    做 缓冲写64 用 elf、ph1 加 16、内核基址。
    做 缓冲写64 用 elf、ph1 加 24、物理加载地址。
    做 缓冲写64 用 elf、ph1 加 32、代码段偏移 加 代码大小。
    做 缓冲写64 用 elf、ph1 加 40、代码段偏移 加 代码大小。
    做 缓冲写64 用 elf、ph1 加 48、页大小。
    ph2 为 elf头大小 加 程序头大小。
    做 缓冲写32 用 elf、ph2 加 0、段类型_加载。
    做 缓冲写32 用 elf、ph2 加 4、段标志_可读 加 段标志_可写。
    做 缓冲写64 用 elf、ph2 加 8、数据段偏移。
    做 缓冲写64 用 elf、ph2 加 16、数据段虚址。
    做 缓冲写64 用 elf、ph2 加 24、数据段物理。
    做 缓冲写64 用 elf、ph2 加 32、数据段文件大小。
    做 缓冲写64 用 elf、ph2 加 40、数据段内存大小。
    做 缓冲写64 用 elf、ph2 加 48、页大小。
    ph3 为 elf头大小 加 程序头大小 乘 2。
    做 缓冲写32 用 elf、ph3 加 0、段类型_注释。
    做 缓冲写32 用 elf、ph3 加 4、段标志_可读。
    做 缓冲写64 用 elf、ph3 加 8、mb2偏移。
    做 缓冲写64 用 elf、ph3 加 16、内核基址 加 mb2偏移。
    做 缓冲写64 用 elf、ph3 加 24、物理加载地址 加 mb2偏移。
    做 缓冲写64 用 elf、ph3 加 32、mb2大小。
    做 缓冲写64 用 elf、ph3 加 40、mb2大小。
    做 缓冲写64 用 elf、ph3 加 48、8。
    做 写入Multiboot2头 用 elf、mb2偏移。
    可变 ci 为 0。
    只要 ci 小于 代码大小：
        做 缓冲写 用 elf、代码段偏移 加 ci、代码 的 第 ci 项。
        ci 为 ci 加 1。
    完。
    做 裸机修正 用 elf、代码段偏移、代码段虚址、数据段虚址。
    做 链接_修正符号 用 elf、代码段偏移。
    返还 一组：elf、文件大小、代码段偏移、代码段虚址、数据段偏移、数据段虚址。
完。

构建Multiboot2头大小 接受 零参：
    返还 48。
完。

写入Multiboot2头 接受 elf、偏移：
    头长度 为 做 构建Multiboot2头大小。
    做 缓冲写32 用 elf、偏移 加 0、MB2_魔数。
    做 缓冲写32 用 elf、偏移 加 4、MB2_架构_I386。
    做 缓冲写32 用 elf、偏移 加 8、头长度。
    校验和 为 0 减 (MB2_魔数 加 MB2_架构_I386 加 头长度)。
    做 缓冲写32 用 elf、偏移 加 12、校验和。
    fb标签 为 偏移 加 16。
    做 缓冲写16 用 elf、fb标签 加 0、MB2_标签_帧缓冲。
    做 缓冲写16 用 elf、fb标签 加 2、0。
    做 缓冲写32 用 elf、fb标签 加 4、20。
    做 缓冲写32 用 elf、fb标签 加 8、80。
    做 缓冲写32 用 elf、fb标签 加 12、25。
    做 缓冲写32 用 elf、fb标签 加 16、0。
    结束标签 为 偏移 加 36。
    做 缓冲写16 用 elf、结束标签 加 0、MB2_标签_结束。
    做 缓冲写16 用 elf、结束标签 加 2、0。
    做 缓冲写32 用 elf、结束标签 加 4、8。
    返还 头长度。
完。

裸机修正 接受 elf、代码段偏移、代码段虚址、数据段虚址：
    可变 fi 为 0。
    只要 fi 小于 做 计数 用 修正表：
        修类 为 做 修正表_取类型 用 修正表、fi。
        修位 为 做 修正表_取位置 用 修正表、fi。
        修附 为 做 修正表_取附加 用 修正表、fi。
        步长 为 做 修正表_项步长 用 修正表、fi。
        若 修类 不等于 空：
            若 修类 等于 「call」 并且 修附 不等于 空：
                目标偏移 为 做 字典取 用 函数表、修附。
                若 目标偏移 不等于 空：
                    相对 为 目标偏移 减 (修位 加 4)。
                    做 缓冲写32 用 elf、代码段偏移 加 修位、相对。
                完。
            否则若 修类 等于 「str」 并且 修附 不等于 空：
                若 做 是整数 用 修附 并且 修附 大于或等于 0 并且 修附 小于 做 计数 用 字串偏移：
                    字串位 为 字串偏移 的 第 修附 项。
                    相对 为 字串位 减 (修位 加 4)。
                    做 缓冲写32 用 elf、代码段偏移 加 修位、相对。
                完。
            否则若 修类 等于 「rt_heap」：
                next_rip 为 代码段虚址 加 修位 加 4。
                做 缓冲写32 用 elf、代码段偏移 加 修位、数据段虚址 减 next_rip。
            否则若 修类 等于 「rt_globals」：
                next_rip 为 代码段虚址 加 修位 加 4。
                做 缓冲写32 用 elf、代码段偏移 加 修位、数据段虚址 加 8 减 next_rip。
            否则若 修类 等于 「rt_saved_rsp」：
                next_rip 为 代码段虚址 加 修位 加 4。
                做 缓冲写32 用 elf、代码段偏移 加 修位、数据段虚址 加 16 减 next_rip。
            否则若 修类 等于 「abs64」 并且 修附 不等于 空：
                做 缓冲写64 用 elf、代码段偏移 加 修位、修附。
            否则若 修类 等于 「rel_data」 并且 修附 不等于 空：
                next_rip 为 代码段虚址 加 修位 加 4。
                做 缓冲写32 用 elf、代码段偏移 加 修位、数据段虚址 加 修附 减 next_rip。
            完。
        完。
        fi 为 fi 加 步长。
    完。
完。

生成启动存根 接受 零参：
    可变 s 为 一组：（空）。
    做 追加 用 s、0xFA。
    做 追加 用 s、0x89。
    做 追加 用 s、0xDF。
    做 追加 用 s、0xBC。
    做 追加 用 s、0x00。
    做 追加 用 s、0x7C。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x31。
    做 追加 用 s、0xC0。
    做 追加 用 s、0xB9。
    做 追加 用 s、0x00。
    做 追加 用 s、0x10。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x57。
    做 追加 用 s、0xBF。
    做 追加 用 s、0x00。
    做 追加 用 s、0x10。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0xF3。
    做 追加 用 s、0xAB。
    做 追加 用 s、0x5F。
    做 追加 用 s、0xC7。
    做 追加 用 s、0x05。
    做 追加 用 s、0x00。
    做 追加 用 s、0x10。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x03。
    做 追加 用 s、0x20。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0xC7。
    做 追加 用 s、0x05。
    做 追加 用 s、0xF8。
    做 追加 用 s、0x1F。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x03。
    做 追加 用 s、0x20。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0xC7。
    做 追加 用 s、0x05。
    做 追加 用 s、0x00。
    做 追加 用 s、0x20。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x03。
    做 追加 用 s、0x30。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0xC7。
    做 追加 用 s、0x05。
    做 追加 用 s、0xF0。
    做 追加 用 s、0x2F。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x03。
    做 追加 用 s、0x30。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0xC7。
    做 追加 用 s、0x05。
    做 追加 用 s、0x00。
    做 追加 用 s、0x30。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x83。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0xC7。
    做 追加 用 s、0x05。
    做 追加 用 s、0x08。
    做 追加 用 s、0x30。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x83。
    做 追加 用 s、0x00。
    做 追加 用 s、0x20。
    做 追加 用 s、0x00。
    做 追加 用 s、0xC7。
    做 追加 用 s、0x05。
    做 追加 用 s、0x10。
    做 追加 用 s、0x30。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x83。
    做 追加 用 s、0x00。
    做 追加 用 s、0x40。
    做 追加 用 s、0x00。
    做 追加 用 s、0xC7。
    做 追加 用 s、0x05。
    做 追加 用 s、0x18。
    做 追加 用 s、0x30。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x83。
    做 追加 用 s、0x00。
    做 追加 用 s、0x60。
    做 追加 用 s、0x00。
    做 追加 用 s、0xB8。
    做 追加 用 s、0x00。
    做 追加 用 s、0x10。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x0F。
    做 追加 用 s、0x22。
    做 追加 用 s、0xD8。
    做 追加 用 s、0x0F。
    做 追加 用 s、0x20。
    做 追加 用 s、0xE0。
    做 追加 用 s、0x83。
    做 追加 用 s、0xC8。
    做 追加 用 s、0x20。
    做 追加 用 s、0x0F。
    做 追加 用 s、0x22。
    做 追加 用 s、0xE0。
    做 追加 用 s、0xB9。
    做 追加 用 s、0x80。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0xC0。
    做 追加 用 s、0x0F。
    做 追加 用 s、0x32。
    做 追加 用 s、0x0D。
    做 追加 用 s、0x00。
    做 追加 用 s、0x01。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x0F。
    做 追加 用 s、0x30。
    做 追加 用 s、0x0F。
    做 追加 用 s、0x20。
    做 追加 用 s、0xC0。
    做 追加 用 s、0x0D。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x80。
    做 追加 用 s、0x0F。
    做 追加 用 s、0x22。
    做 追加 用 s、0xC0。
    做 追加 用 s、0x0F。
    做 追加 用 s、0x01。
    做 追加 用 s、0x15。
    _gdt_ptr_fixup 为 做 计数 用 s。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0xEA。
    _long_mode_fixup 为 做 计数 用 s。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x08。
    做 追加 用 s、0x00。
    _long_mode_offset 为 做 计数 用 s。
    做 追加 用 s、0x66。
    做 追加 用 s、0xB8。
    做 追加 用 s、0x10。
    做 追加 用 s、0x00。
    做 追加 用 s、0x8E。
    做 追加 用 s、0xD8。
    做 追加 用 s、0x8E。
    做 追加 用 s、0xC0。
    做 追加 用 s、0x8E。
    做 追加 用 s、0xE0。
    做 追加 用 s、0x8E。
    做 追加 用 s、0xE8。
    做 追加 用 s、0x8E。
    做 追加 用 s、0xD0。
    做 追加 用 s、0x48。
    做 追加 用 s、0xBC。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x08。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x66。
    做 追加 用 s、0xBA。
    做 追加 用 s、0xF9。
    做 追加 用 s、0x03。
    做 追加 用 s、0x30。
    做 追加 用 s、0xC0。
    做 追加 用 s、0xEE。
    做 追加 用 s、0x66。
    做 追加 用 s、0xBA。
    做 追加 用 s、0xFB。
    做 追加 用 s、0x03。
    做 追加 用 s、0xB0。
    做 追加 用 s、0x80。
    做 追加 用 s、0xEE。
    做 追加 用 s、0x66。
    做 追加 用 s、0xBA。
    做 追加 用 s、0xF8。
    做 追加 用 s、0x03。
    做 追加 用 s、0xB0。
    做 追加 用 s、0x01。
    做 追加 用 s、0xEE。
    做 追加 用 s、0x66。
    做 追加 用 s、0xBA。
    做 追加 用 s、0xF9。
    做 追加 用 s、0x03。
    做 追加 用 s、0x30。
    做 追加 用 s、0xC0。
    做 追加 用 s、0xEE。
    做 追加 用 s、0x66。
    做 追加 用 s、0xBA。
    做 追加 用 s、0xFB。
    做 追加 用 s、0x03。
    做 追加 用 s、0xB0。
    做 追加 用 s、0x03。
    做 追加 用 s、0xEE。
    做 追加 用 s、0x66。
    做 追加 用 s、0xBA。
    做 追加 用 s、0xFA。
    做 追加 用 s、0x03。
    做 追加 用 s、0xB0。
    做 追加 用 s、0xC7。
    做 追加 用 s、0xEE。
    做 追加 用 s、0x66。
    做 追加 用 s、0xBA。
    做 追加 用 s、0xFC。
    做 追加 用 s、0x03。
    做 追加 用 s、0xB0。
    做 追加 用 s、0x0B。
    做 追加 用 s、0xEE。
    做 追加 用 s、0x66。
    做 追加 用 s、0xBA。
    做 追加 用 s、0xF8。
    做 追加 用 s、0x03。
    做 追加 用 s、0xB0。
    做 追加 用 s、0x4E。
    做 追加 用 s、0xEE。
    做 追加 用 s、0xB0。
    做 追加 用 s、0x6F。
    做 追加 用 s、0xEE。
    做 追加 用 s、0xB0。
    做 追加 用 s、0x76。
    做 追加 用 s、0xEE。
    做 追加 用 s、0xB0。
    做 追加 用 s、0x61。
    做 追加 用 s、0xEE。
    做 追加 用 s、0xB0。
    做 追加 用 s、0x0A。
    做 追加 用 s、0xEE。
    做 追加 用 s、0xE8。
    _entry_fixup 为 做 计数 用 s。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0xFA。
    做 追加 用 s、0xF4。
    做 追加 用 s、0xEB。
    做 追加 用 s、0xFC。
    _gdt_offset 为 做 计数 用 s。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0xFF。
    做 追加 用 s、0xFF。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x9A。
    做 追加 用 s、0xAF。
    做 追加 用 s、0x00。
    做 追加 用 s、0xFF。
    做 追加 用 s、0xFF。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x92。
    做 追加 用 s、0xCF。
    做 追加 用 s、0x00。
    _gdt_ptr_offset 为 做 计数 用 s。
    做 追加 用 s、0x17。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    做 追加 用 s、0x00。
    返还 一组：s、_gdt_ptr_fixup、_long_mode_fixup、_long_mode_offset、_entry_fixup、_gdt_offset、_gdt_ptr_offset。
完。

导出段信息 接受 代码段虚址、代码大小、数据段虚址、数据段内容大小、BSS大小：
    返还 一组：一组：「__代码段_起始」、代码段虚址、一组：「__代码段_结束」、代码段虚址 加 代码大小、一组：「__数据段_起始」、数据段虚址、一组：「__数据段_结束」、数据段虚址 加 数据段内容大小、一组：「__未初始化段_起始」、数据段虚址 加 数据段内容大小、一组：「__未初始化段_结束」、数据段虚址 加 数据段内容大小 加 BSS大小、一组：「__栈顶」、数据段虚址 加 数据段内容大小 加 BSS大小 加 栈大小、一组：「__内核_结束」、数据段虚址 加 数据段内容大小 加 BSS大小 加 栈大小。
完。

QEMU_生成启动脚本 接受 内核路径、串口日志路径：
    可变 脚本 为 「#!/bin/bash\n」。
    脚本 为 脚本 加 「# Nova 裸机内核 QEMU Smoke Test\n」。
    脚本 为 脚本 加 「KERNEL=\"」 加 内核路径 加 「\"\n」。
    脚本 为 脚本 加 「SERIAL_LOG=\"」 加 串口日志路径 加 「\"\n」。
    脚本 为 脚本 加 「TIMEOUT=5\n」。
    脚本 为 脚本 加 「\n」。
    脚本 为 脚本 加 「qemu-system-x86_64 \\\n」。
    脚本 为 脚本 加 「  -kernel \"$KERNEL\" \\\n」。
    脚本 为 脚本 加 「  -nographic \\\n」。
    脚本 为 脚本 加 「  -serial file:\"$SERIAL_LOG\" \\\n」。
    脚本 为 脚本 加 「  -no-reboot \\\n」。
    脚本 为 脚本 加 「  -m 128M \\\n」。
    脚本 为 脚本 加 「  -device isa-debug-exit,iobase=0xf4,iosize=0x04 \\\n」。
    脚本 为 脚本 加 「  &\n」。
    脚本 为 脚本 加 「QEMU_PID=$!\n」。
    脚本 为 脚本 加 「sleep $TIMEOUT\n」。
    脚本 为 脚本 加 「kill $QEMU_PID 2>/dev/null\n」。
    脚本 为 脚本 加 「wait $QEMU_PID 2>/dev/null\n」。
    脚本 为 脚本 加 「\n」。
    脚本 为 脚本 加 「# 验证串口输出\n」。
    脚本 为 脚本 加 「if grep -q 'Nova' \"$SERIAL_LOG\" 2>/dev/null; then\n」。
    脚本 为 脚本 加 「  echo 'SMOKE-TEST: PASS (Nova banner detected)'\n」。
    脚本 为 脚本 加 「  exit 0\n」。
    脚本 为 脚本 加 「else\n」。
    脚本 为 脚本 加 「  echo 'SMOKE-TEST: FAIL (no Nova banner in serial log)'\n」。
    脚本 为 脚本 加 「  exit 1\n」。
    脚本 为 脚本 加 「fi\n」。
    返还 脚本。
完。

QEMU_创建Bundle合同 接受 内核路径、串口日志：
    可变 合同 为 做 字典。
    做 字典设 用 合同、「kernel_elf」、内核路径。
    做 字典设 用 合同、「serial_log」、串口日志。
    做 字典设 用 合同、「memory」、「128M」。
    做 字典设 用 合同、「超时」、「5」。
    做 字典设 用 合同、「expected_banner」、「Nova」。
    做 字典设 用 合同、「boot_protocol」、「multiboot2」。
    做 字典设 用 合同、「architecture」、「x86-64」。
    做 字典设 用 合同、「base_address」、做 转字符串 用 物理加载地址。
    做 字典设 用 合同、「entry_virtual」、做 转字符串 用 内核基址。
    返还 合同。
完。

QEMU_Bundle合同转文本 接受 合同：
    可变 文本 为 「」。
    键列 为 一组：「kernel_elf」、「serial_log」、「memory」、「超时」、「expected_banner」、「boot_protocol」、「architecture」、「base_address」、「entry_virtual」、「launch_script」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        值 为 做 字典取 用 合同、键列 的 第 i 项。
        若 值 不等于 空：
            文本 为 文本 加 键列 的 第 i 项 加 「=」 加 做 转字符串 用 值 加 「\n」。
        完。
        i 为 i 加 1。
    完。
    返还 文本。
完。

QEMU_写出Bundle工件 接受 内核路径：
    串口日志 为 做 字符串拼接 用 内核路径、「.serial.log」。
    脚本路径 为 做 字符串拼接 用 内核路径、「.qemu.sh」。
    bundle路径 为 做 字符串拼接 用 内核路径、「.qemu.bundle」。
    做 写文件 用 脚本路径、做 QEMU_生成启动脚本 用 内核路径、串口日志、0。
    bundle合同 为 做 QEMU_创建Bundle合同 用 内核路径、串口日志。
    做 字典设 用 bundle合同、「launch_script」、脚本路径。
    做 写文件 用 bundle路径、做 QEMU_Bundle合同转文本 用 bundle合同、0。
    返还 一组：脚本路径、bundle路径、串口日志。
完。

QEMU_解析SmokeResult 接受 串口日志内容：
    可变 结果 为 做 字典。
    若 串口日志内容 等于 空 或者 串口日志内容 等于 「」：
        做 字典设 用 结果、「status」、「失败」。
        做 字典设 用 结果、「reason」、「empty_serial_log」。
        返还 结果。
    完。
    若 做 字符串包含 用 串口日志内容、「Nova」：
        做 字典设 用 结果、「status」、「通过」。
        做 字典设 用 结果、「reason」、「banner_detected」。
    否则：
        做 字典设 用 结果、「status」、「失败」。
        做 字典设 用 结果、「reason」、「no_banner」。
    完。
    做 字典设 用 结果、「log_length」、做 转字符串 用 做 字节长度 用 串口日志内容。
    返还 结果。
完。

裸机ELF_验证 接受 elf缓冲：
    可变 结果 为 做 字典。
    若 做 缓冲读 用 elf缓冲 等于 0x7F 并且 做 缓冲读 用 elf缓冲、1 等于 0x45 并且 做 缓冲读 用 elf缓冲、2 等于 0x4C 并且 做 缓冲读 用 elf缓冲、3 等于 0x46：
        做 字典设 用 结果、「elf_magic」、「正常」。
    否则：
        做 字典设 用 结果、「elf_magic」、「失败」。
    完。
    若 做 缓冲读 用 elf缓冲、4 等于 2：
        做 字典设 用 结果、「elf_class」、「ELF64」。
    否则：
        做 字典设 用 结果、「elf_class」、「失败」。
    完。
    machine 为 做 缓冲读 用 elf缓冲、18 加 做 缓冲读 用 elf缓冲、19 乘 256。
    若 machine 等于 处理器_X86_64：
        做 字典设 用 结果、「machine」、「x86-64」。
    否则：
        做 字典设 用 结果、「machine」、做 转字符串 用 machine。
    完。
    返还 结果。
完。

本章 收束。
