《易变访问》

引用《..·..·编译器·规划·核心·发射器》。

编译_处理__volatile_写8_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0x88、0x01。
    返还 1。
完。

编译_处理__volatile_写16_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0x66、0x89、0x01。
    返还 1。
完。

编译_处理__volatile_写32_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0x89、0x01。
    返还 1。
完。

编译_处理__volatile_写64_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0x48、0x89、0x01。
    返还 1。
完。

编译_处理__volatile_读32_偏移_ 接受 参数：
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x59。
    做 发射组 用 一组：0x48、0x01、0xC8。
    做 发射组 用 一组：0x8B、0x00。
    返还 1。
完。

编译_处理__volatile_写32_偏移_ 接受 参数：
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x59。
    做 发射组 用 一组：0x48、0x01、0xC8。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0x89、0x01。
    返还 1。
完。

编译_处理__VGA写字符_ 接受 参数：
    做 编译_表达式 用 参数 的 第 3 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 2 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 0 项。
    做 发射 用 0x50。
    做 编译_表达式 用 参数 的 第 1 项。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射组 用 一组：0x48、0x6B、0xC0、80。
    做 发射组 用 一组：0x48、0x01、0xC8。
    做 发射组 用 一组：0x48、0xD1、0xE0。
    做 发射组 用 一组：0x48、0x05。
    做 发射32 用 0xB8000。
    做 发射_mov_rcx_rax。
    做 发射 用 0x58。
    做 发射 用 0x5A。
    做 发射组 用 一组：0xC1、0xE2、0x08。
    做 发射组 用 一组：0x09、0xD0。
    做 发射组 用 一组：0x66、0x89、0x01。
    返还 1。
完。

编译_volatile操作 接受 名、参数：
    若 名 等于 「volatile_读8」：
        做 编译_表达式 用 参数 的 第 0 项。
        做 发射组 用 一组：0x0F、0xB6、0x00。
        返还 1。
    完。
    若 名 等于 「volatile_读16」：
        做 编译_表达式 用 参数 的 第 0 项。
        做 发射组 用 一组：0x0F、0xB7、0x00。
        返还 1。
    完。
    若 名 等于 「volatile_读32」：
        做 编译_表达式 用 参数 的 第 0 项。
        做 发射组 用 一组：0x8B、0x00。
        返还 1。
    完。
    若 名 等于 「volatile_读64」：
        做 编译_表达式 用 参数 的 第 0 项。
        做 发射组 用 一组：0x48、0x8B、0x00。
        返还 1。
    完。
    若 名 等于 「volatile_写8」：
        返还 做 编译_处理__volatile_写8_ 用 参数。
    完。
    若 名 等于 「volatile_写16」：
        返还 做 编译_处理__volatile_写16_ 用 参数。
    完。
    若 名 等于 「volatile_写32」：
        返还 做 编译_处理__volatile_写32_ 用 参数。
    完。
    若 名 等于 「volatile_写64」：
        返还 做 编译_处理__volatile_写64_ 用 参数。
    完。
    若 名 等于 「volatile_读32_偏移」：
        返还 做 编译_处理__volatile_读32_偏移_ 用 参数。
    完。
    若 名 等于 「volatile_写32_偏移」：
        返还 做 编译_处理__volatile_写32_偏移_ 用 参数。
    完。
    若 名 等于 「VGA写字符」：
        返还 做 编译_处理__VGA写字符_ 用 参数。
    完。
    若 名 等于 「编译器屏障」：
        返还 1。
    完。
    返还 0。
完。

是volatile调用 接受 名：
    若 名 等于 「volatile_读8」：
        返还 真。
    完。
    若 名 等于 「volatile_读16」：
        返还 真。
    完。
    若 名 等于 「volatile_读32」：
        返还 真。
    完。
    若 名 等于 「volatile_读64」：
        返还 真。
    完。
    若 名 等于 「volatile_写8」：
        返还 真。
    完。
    若 名 等于 「volatile_写16」：
        返还 真。
    完。
    若 名 等于 「volatile_写32」：
        返还 真。
    完。
    若 名 等于 「volatile_写64」：
        返还 真。
    完。
    若 名 等于 「volatile_读32_偏移」：
        返还 真。
    完。
    若 名 等于 「volatile_写32_偏移」：
        返还 真。
    完。
    若 名 等于 「VGA写字符」：
        返还 真。
    完。
    若 名 等于 「编译器屏障」：
        返还 真。
    完。
    返还 假。
完。

MMIO_读32 接受 基址、偏移：
    返还 做 volatile_读32 用 基址 加 偏移。
完。

MMIO_写32 接受 基址、偏移、值：
    做 volatile_写32 用 基址 加 偏移、值。
完。

MMIO_设位 接受 基址、偏移、位：
    v 为 做 MMIO_读32 用 基址、偏移。
    做 MMIO_写32 用 基址、偏移、v 位或 1 左移 位。
完。

MMIO_清位 接受 基址、偏移、位：
    v 为 做 MMIO_读32 用 基址、偏移。
    做 MMIO_写32 用 基址、偏移、v 位与 按位取反 (1 左移 位)。
完。

本章 收束。
