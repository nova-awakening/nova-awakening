《结构体增强》

引用《..·..·标准库·基础》。

引用《..·..·运行时·原语桩》。

引用《..·..·运行时·字符串引擎·字符串引擎》。

结构字段 是 一种 事物，含 名称、类型名、偏移、对齐。

结构方法 是 一种 事物，含 名称、函数符号、是静态。

结构定义增强 是 一种 事物，含 名称、字段列表、总大小、对齐、是packed、泛型参数。

P1_类型_大小 接受 类型名：
    若 类型名 等于 「u8」 或者 类型名 等于 「i8」 或者 类型名 等于 「布尔」：
        返还 1。
    完。
    若 类型名 等于 「u16」 或者 类型名 等于 「i16」：
        返还 2。
    完。
    若 类型名 等于 「u32」 或者 类型名 等于 「i32」 或者 类型名 等于 「浮点32」：
        返还 4。
    完。
    若 类型名 等于 「u64」 或者 类型名 等于 「i64」 或者 类型名 等于 「整数」 或者 类型名 等于 「浮点」：
        返还 8。
    完。
    若 类型名 等于 「字符串」：
        返还 8。
    完。
    若 做 字符串_开头是 用 类型名、「*」：
        返还 8。
    完。
    def 为 做 结构注册表_查找 用 类型名。
    若 def 不等于 空：
        返还 def 的 总大小。
    完。
    返还 8。
完。

类型_对齐 接受 类型名：
    大小 为 做 P1_类型_大小 用 类型名。
    若 大小 大于 8：
        返还 8。
    完。
    返还 大小。
完。

可变 结构注册表 为 一组：（空）。

结构注册表_注册 接受 def：
    做 追加 用 结构注册表、def。
完。

结构注册表_查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 结构注册表：
        若 结构注册表 的 第 i 项 的 名称 等于 名称：
            返还 结构注册表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

结构_计算布局 接受 def：
    可变 偏移 为 0。
    若 def 的 父结构 不等于 「」：
        父 为 做 结构注册表_查找 用 def 的 父结构。
        若 父 不等于 空：
            偏移 为 父 的 总大小。
            若 父 的 对齐 大于 最大对齐：
                最大对齐 为 父 的 对齐。
            完。
        完。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        字段 为 def 的 字段列表 的 第 i 项。
        字段 的 大小 为 做 P1_类型_大小 用 字段 的 类型名。
        字段 的 对齐 为 做 类型_对齐 用 字段 的 类型名。
        若 非 def 的 是packed 并且 字段 的 对齐 大于 1：
            余数 为 偏移 取余于 字段 的 对齐。
            若 余数 不等于 0：
                偏移 为 偏移 加 (字段 的 对齐 减 余数)。
            完。
        完。
        字段 的 偏移 为 偏移。
        偏移 为 偏移 加 字段 的 大小。
        若 字段 的 对齐 大于 最大对齐：
            最大对齐 为 字段 的 对齐。
        完。
        i 为 i 加 1。
    完。
    若 非 def 的 是packed 并且 最大对齐 大于 1：
        余数 为 偏移 取余于 最大对齐。
        若 余数 不等于 0：
            偏移 为 偏移 加 (最大对齐 减 余数)。
        完。
    完。
    def 的 总大小 为 偏移。
    def 的 对齐 为 最大对齐。
完。

结构_添加方法 接受 结构名、_方法名、_参数数、_是静态：
    def 为 做 结构注册表_查找 用 结构名。
    若 def 等于 空：
        返还。
    完。
    _方法 为 新建 结构方法。
    _方法 的 名称 为 _方法名。
    _方法 的 函数符号 为 结构名 加 「_」 加 _方法名。
    _方法 的 参数数 为 _参数数。
    _方法 的 是静态 为 _是静态。
    做 追加 用 def 的 方法列表、_方法。
完。

结构_查找方法 接受 def、方法名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 方法列表：
        若 def 的 方法列表 的 第 i 项 的 名称 等于 方法名：
            返还 def 的 方法列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 def 的 父结构 不等于 「」：
        父 为 做 结构注册表_查找 用 def 的 父结构。
        若 父 不等于 空：
            返还 做 结构_查找方法 用 父、方法名。
        完。
    完。
    返还 空。
完。

结构_生成字段访问 接受 代码生成器、结构名、字段名：
    def 为 做 结构注册表_查找 用 结构名。
    若 def 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        若 def 的 字段列表 的 第 i 项 的 名称 等于 字段名：
            做 代码生成_偏移读取 用 代码生成器、def 的 字段列表 的 第 i 项 的 偏移、def 的 字段列表 的 第 i 项 的 大小。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

结构_生成方法调用 接受 代码生成器、结构名、方法名：
    方法 为 做 结构_查找方法 用 做 结构注册表_查找 用 结构名、方法名。
    若 方法 不等于 空：
        做 代码生成_调用 用 代码生成器、方法 的 函数符号。
    完。
完。

结构_生成构造 接受 代码生成器、def：
    正 「_创建」。
    做 代码生成_函数头 用 代码生成器、构造名、0。
    做 代码生成_分配 用 代码生成器、def 的 总大小。
    做 代码生成_零填充 用 代码生成器、def 的 总大小。
    做 代码生成_函数尾 用 代码生成器。
完。

可变 _继承表_子 为 一组：（空）。

可变 _继承表_父 为 一组：（空）。

结构_注册继承 接受 子名、父名：
    做 追加 用 _继承表_子、子名。
    做 追加 用 _继承表_父、父名。
完。

结构_获取父 接受 子名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _继承表_子：
        若 _继承表_子 的 第 i 项 等于 子名：
            返还 _继承表_父 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

结构_继承链 接受 名称：
    可变 链 为 一组：名称。
    可变 当前 为 名称。
    只要 真：
        父 为 做 结构_获取父 用 当前。
        若 父 等于 空：
            返还 链。
        完。
        做 追加 用 链、父。
        当前 为 父。
    完。
    返还 链。
完。

结构_是子类 接受 子名、父名：
    链 为 做 结构_继承链 用 子名。
    可变 i 为 0。
    只要 i 小于 做 计数 用 链：
        若 链 的 第 i 项 等于 父名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

可变 _接口_名 为 一组：（空）。

可变 _接口_方法列表 为 一组：（空）。

接口_注册 接受 名、方法列表：
    做 追加 用 _接口_名、名。
    做 追加 用 _接口_方法列表、方法列表。
完。

可变 _实现_结构 为 一组：（空）。

可变 _实现_接口 为 一组：（空）。

接口_声明实现 接受 结构名、接口名：
    做 追加 用 _实现_结构、结构名。
    做 追加 用 _实现_接口、接口名。
完。

接口_检查实现 接受 结构名、接口名：
    可变 接口idx 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _接口_名：
        若 _接口_名 的 第 i 项 等于 接口名：
            接口idx 为 i。
        完。
        i 为 i 加 1。
    完。
    若 接口idx 小于 0：
        返还 假。
    完。
    需要方法 为 _接口_方法列表 的 第 接口idx 项。
    def 为 做 结构注册表_查找 用 结构名。
    若 def 等于 空：
        返还 假。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 需要方法：
        方法 为 做 结构_查找方法 用 def、需要方法 的 第 i 项。
        若 方法 等于 空：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

结构_计算对齐大小 接受 def、对齐：
    可变 总大小 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        字段大小 为 def 的 字段列表 的 第 i 项 的 大小。
        余 为 总大小 取余于 对齐。
        若 余 不等于 0：
            总大小 为 总大小 加 (对齐 减 余)。
        完。
        可变 字段 为 def 的 字段列表 的 第 i 项。
        字段 的 偏移 为 总大小。
        可变 字段列表 为 def 的 字段列表。
        字段列表 的 第 i 项 为 字段。
        def 的 字段列表 为 字段列表。
        总大小 为 总大小 加 字段大小。
        i 为 i 加 1。
    完。
    尾余 为 总大小 取余于 对齐。
    若 尾余 不等于 0：
        总大小 为 总大小 加 (对齐 减 尾余)。
    完。
    返还 总大小。
完。

结构_填充率 接受 def：
    可变 有效大小 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        有效大小 为 有效大小 加 def 的 字段列表 的 第 i 项 的 大小。
        i 为 i 加 1。
    完。
    若 def 的 总大小 等于 0：
        返还 100。
    完。
    返还 有效大小 乘 100 除以 def 的 总大小。
完。

结构_序列化描述 接受 def：
    可变 描述 为 def 的 名称 加 「{」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        若 i 大于 0：
            描述 为 描述 加 「, 」。
        完。
        描述 为 描述 加 def 的 字段列表 的 第 i 项 的 名称 加 「:」 加 做 转字符串 用 def 的 字段列表 的 第 i 项 的 大小。
        i 为 i 加 1。
    完。
    描述 为 描述 加 「}」。
    返还 描述。
完。

可变 _结构_定义数 为 0。

可变 _结构_实例数 为 0。

结构_记录定义 接受 零参：
    _结构_定义数 为 _结构_定义数 加 1。
完。

结构_记录实例 接受 零参：
    _结构_实例数 为 _结构_实例数 加 1。
完。

本章 收束。
