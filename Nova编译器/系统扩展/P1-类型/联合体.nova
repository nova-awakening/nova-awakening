《联合体》

引用《结构体增强》。

引用《..·..·标准库·基础》。

引用《..·..·编译器·词法·词法分析》。

引用《..·..·运行时·原语桩》。

可变 _联合_定义数 为 0。

可变 _联合_访问数 为 0。

可变 _联合_注册表 为 一组：（空）。

联合字段 是 一种 事物，含 名称、类型名、大小。

联合定义 是 一种 事物，含 名称、字段列表、对齐。

联合_解析 接受 _词法流：
    _def 为 新建 联合定义。
    _def 的 名称 为 做 词法流_当前 用 _词法流。
    _def 的 字段列表 为 一组：（空）。
    做 词法流_前进 用 _词法流。
    做 词法流_期望 用 _词法流、「{」。
    只要 做 词法流_当前 用 _词法流 不等于 「}」 并且 非 做 词法流_结束 用 _词法流：
        _字段 为 新建 联合字段。
        _字段 的 名称 为 做 词法流_当前 用 _词法流。
        做 词法流_前进 用 _词法流。
        做 词法流_期望 用 _词法流、「:」。
        _字段 的 类型名 为 做 词法流_当前 用 _词法流。
        _字段 的 大小 为 做 类型_大小 用 _字段 的 类型名。
        做 词法流_前进 用 _词法流。
        做 追加 用 _def 的 字段列表、_字段。
        若 做 词法流_当前 用 _词法流 等于 「,」：
            做 词法流_前进 用 _词法流。
        完。
    完。
    做 词法流_前进 用 _词法流。
    做 联合_计算布局 用 _def。
    _联合_定义数 为 _联合_定义数 加 1。
    做 追加 用 _联合_注册表、_def。
    返还 _def。
完。

联合_计算布局 接受 def：
    可变 最大大小 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        字段 为 def 的 字段列表 的 第 i 项。
        若 字段 的 大小 大于 最大大小：
            最大大小 为 字段 的 大小。
        完。
        对齐 为 做 类型_对齐 用 字段 的 类型名。
        若 对齐 大于 最大对齐：
            最大对齐 为 对齐。
        完。
        i 为 i 加 1。
    完。
    余数 为 最大大小 取余于 最大对齐。
    若 余数 不等于 0：
        最大大小 为 最大大小 加 (最大对齐 减 余数)。
    完。
    def 的 总大小 为 最大大小。
    def 的 对齐 为 最大对齐。
完。

联合_生成访问 接受 代码生成器、def、字段名：
    _联合_访问数 为 _联合_访问数 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        若 def 的 字段列表 的 第 i 项 的 名称 等于 字段名：
            做 代码生成_偏移读取 用 代码生成器、0、def 的 字段列表 的 第 i 项 的 大小。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

联合_生成赋值 接受 代码生成器、def、字段名：
    _联合_访问数 为 _联合_访问数 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        若 def 的 字段列表 的 第 i 项 的 名称 等于 字段名：
            做 代码生成_零填充 用 代码生成器、def 的 总大小。
            做 代码生成_偏移写入 用 代码生成器、0、def 的 字段列表 的 第 i 项 的 大小。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

联合_查找字段 接受 def、字段名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        若 def 的 字段列表 的 第 i 项 的 名称 等于 字段名：
            返还 def 的 字段列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

标签联合_创建 接受 def：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 def 的 总大小 加 4：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 一组：def、数据、负 1。
完。

标签联合_设值 接受 实例、字段索引、值：
    若 字段索引 小于 0 或者 字段索引 大于或等于 做 计数 用 实例 的 第 0 项 的 字段列表：
        返还 假。
    完。
    实例 的 第 2 项 为 字段索引。
    实例 的 第 1 项 的 第 0 项 为 字段索引 取余于 256。
    实例 的 第 1 项 的 第 1 项 为 字段索引 除以 256 取余于 256。
    实例 的 第 1 项 的 第 2 项 为 字段索引 除以 65536 取余于 256。
    实例 的 第 1 项 的 第 3 项 为 字段索引 除以 16777216 取余于 256。
    字段 为 实例 的 第 0 项 的 字段列表 的 第 字段索引 项。
    可变 i 为 4。
    只要 i 小于 4 加 字段 的 大小：
        实例 的 第 1 项 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    实例 的 第 1 项 的 第 4 项 为 值 取余于 256。
    若 字段 的 大小 大于 1：
        实例 的 第 1 项 的 第 5 项 为 值 除以 256 取余于 256。
    完。
    若 字段 的 大小 大于 2：
        实例 的 第 1 项 的 第 6 项 为 值 除以 65536 取余于 256。
    完。
    若 字段 的 大小 大于 3：
        实例 的 第 1 项 的 第 7 项 为 值 除以 16777216 取余于 256。
    完。
    返还 真。
完。

标签联合_取标签 接受 实例：
    返还 实例 的 第 1 项 的 第 0 项 加 实例 的 第 1 项 的 第 1 项 乘 256 加 实例 的 第 1 项 的 第 2 项 乘 65536 加 实例 的 第 1 项 的 第 3 项 乘 16777216。
完。

标签联合_取值 接受 实例：
    标签 为 做 标签联合_取标签 用 实例。
    若 标签 小于 0 或者 标签 大于或等于 做 计数 用 实例 的 第 0 项 的 字段列表：
        返还 空。
    完。
    字段 为 实例 的 第 0 项 的 字段列表 的 第 标签 项。
    可变 值 为 实例 的 第 1 项 的 第 4 项。
    若 字段 的 大小 大于 1：
        值 为 值 加 实例 的 第 1 项 的 第 5 项 乘 256。
    完。
    若 字段 的 大小 大于 2：
        值 为 值 加 实例 的 第 1 项 的 第 6 项 乘 65536。
    完。
    若 字段 的 大小 大于 3：
        值 为 值 加 实例 的 第 1 项 的 第 7 项 乘 16777216。
    完。
    返还 值。
完。

标签联合_是变体 接受 实例、字段索引：
    返还 做 标签联合_取标签 用 实例 等于 字段索引。
完。

标签联合_活跃字段名 接受 实例：
    标签 为 做 标签联合_取标签 用 实例。
    若 标签 小于 0 或者 标签 大于或等于 做 计数 用 实例 的 第 0 项 的 字段列表：
        返还 「无」。
    完。
    返还 实例 的 第 0 项 的 字段列表 的 第 标签 项 的 名称。
完。

联合_按名查找 接受 名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _联合_注册表：
        若 _联合_注册表 的 第 i 项 的 名称 等于 名称：
            返还 _联合_注册表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

联合_字段数 接受 def：
    返还 做 计数 用 def 的 字段列表。
完。

联合_总大小 接受 def：
    返还 def 的 总大小。
完。

联合_字段名列表 接受 def：
    可变 名列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        做 追加 用 名列表、def 的 字段列表 的 第 i 项 的 名称。
        i 为 i 加 1。
    完。
    返还 名列表。
完。

联合_字段类型列表 接受 def：
    可变 类型列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        做 追加 用 类型列表、def 的 字段列表 的 第 i 项 的 类型名。
        i 为 i 加 1。
    完。
    返还 类型列表。
完。

联合_字段大小列表 接受 def：
    可变 大小列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        做 追加 用 大小列表、def 的 字段列表 的 第 i 项 的 大小。
        i 为 i 加 1。
    完。
    返还 大小列表。
完。

联合_验证 接受 def：
    可变 问题 为 一组：（空）。
    若 做 计数 用 def 的 字段列表 等于 0：
        做 追加 用 问题、「联合体无字段」。
    完。
    若 def 的 总大小 等于 0：
        做 追加 用 问题、「联合体大小为零」。
    完。
    可变 名集 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        可变 j 为 0。
        只要 j 小于 做 计数 用 名集：
            若 名集 的 第 j 项 等于 def 的 字段列表 的 第 i 项 的 名称：
                做 追加 用 问题、做 字符串拼接 用 「重复字段名: 」、def 的 字段列表 的 第 i 项 的 名称。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 名集、def 的 字段列表 的 第 i 项 的 名称。
        若 def 的 字段列表 的 第 i 项 的 大小 小于或等于 0：
            做 追加 用 问题、做 字符串拼接 用 「无效字段大小: 」、def 的 字段列表 的 第 i 项 的 名称。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 问题 等于 0：
        做 追加 用 问题、「验证通过」。
    完。
    返还 问题。
完。

联合_序列化 接受 def：
    可变 结果 为 「联合 」。
    结果 为 做 字符串拼接 用 结果、def 的 名称。
    结果 为 做 字符串拼接 用 结果、「 { 」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        若 i 大于 0：
            结果 为 做 字符串拼接 用 结果、「, 」。
        完。
        结果 为 做 字符串拼接 用 结果、def 的 字段列表 的 第 i 项 的 名称。
        结果 为 做 字符串拼接 用 结果、「: 」。
        结果 为 做 字符串拼接 用 结果、def 的 字段列表 的 第 i 项 的 类型名。
        i 为 i 加 1。
    完。
    结果 为 做 字符串拼接 用 结果、「 }」。
    返还 结果。
完。

联合_调试打印 接受 def：
    做 打印 用 做 字符串拼接 用 「联合体: 」、def 的 名称。
    做 打印 用 做 字符串拼接 用 「 总大小: 」、做 字符串拼接 用 做 转字符串 用 def 的 总大小、「字节」。
    做 打印 用 做 字符串拼接 用 「 对齐: 」、做 转字符串 用 def 的 对齐。
    做 打印 用 做 字符串拼接 用 「 字段数: 」、做 转字符串 用 做 计数 用 def 的 字段列表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        f 为 def 的 字段列表 的 第 i 项。
        做 打印 用 做 字符串拼接 用 「 · 」、做 字符串拼接 用 f 的 名称、做 字符串拼接 用 「: 」、做 字符串拼接 用 f 的 类型名、做 字符串拼接 用 「 (」、做 字符串拼接 用 做 转字符串 用 f 的 大小、「字节)」。
        i 为 i 加 1。
    完。
完。

联合_结构相同 接受 a、b：
    若 做 计数 用 a 的 字段列表 不等于 做 计数 用 b 的 字段列表：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 字段列表：
        若 a 的 字段列表 的 第 i 项 的 名称 不等于 b 的 字段列表 的 第 i 项 的 名称：
            返还 假。
        完。
        若 a 的 字段列表 的 第 i 项 的 类型名 不等于 b 的 字段列表 的 第 i 项 的 类型名：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

联合_布局兼容 接受 a、b：
    返还 a 的 总大小 等于 b 的 总大小 并且 a 的 对齐 等于 b 的 对齐。
完。

联合_类型字节数 接受 类型名：
    若 类型名 等于 「u8」 或者 类型名 等于 「i8」 或者 类型名 等于 「布尔」：
        返还 1。
    完。
    若 类型名 等于 「u16」 或者 类型名 等于 「i16」：
        返还 2。
    完。
    若 类型名 等于 「u32」 或者 类型名 等于 「i32」 或者 类型名 等于 「f32」：
        返还 4。
    完。
    若 类型名 等于 「u64」 或者 类型名 等于 「i64」 或者 类型名 等于 「f64」：
        返还 8。
    完。
    若 类型名 等于 「指针」 或者 类型名 等于 「usize」：
        返还 8。
    完。
    返还 4。
完。

联合_效率分析 接受 def：
    可变 结构体等效大小 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        结构体等效大小 为 结构体等效大小 加 def 的 字段列表 的 第 i 项 的 大小。
        i 为 i 加 1。
    完。
    节省字节 为 结构体等效大小 减 def 的 总大小。
    可变 节省百分比 为 0。
    若 结构体等效大小 大于 0：
        节省百分比 为 节省字节 乘 100 除以 结构体等效大小。
    完。
    返还 一组：结构体等效大小、def 的 总大小、节省字节、节省百分比。
完。

联合_对齐建议 接受 def：
    若 def 的 对齐 等于 1：
        返还 「无需对齐(字节级)」。
    完。
    若 def 的 对齐 小于或等于 4：
        返还 「4字节对齐(适合32位)」。
    完。
    若 def 的 对齐 小于或等于 8：
        返还 「8字节对齐(标准64位)」。
    完。
    若 def 的 对齐 小于或等于 16：
        返还 「16字节对齐(SIMD友好)」。
    完。
    返还 「超大对齐(检查是否必要)」。
完。

联合_安全评估 接受 def：
    可变 风险 为 一组：（空）。
    若 做 计数 用 def 的 字段列表 大于 8：
        做 追加 用 风险、「字段过多: 考虑拆分」。
    完。
    可变 有浮点 为 假。
    可变 有整数 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        t 为 def 的 字段列表 的 第 i 项 的 类型名。
        若 t 等于 「f32」 或者 t 等于 「f64」：
            有浮点 为 真。
        完。
        若 t 等于 「u32」 或者 t 等于 「i32」 或者 t 等于 「u64」 或者 t 等于 「i64」：
            有整数 为 真。
        完。
        i 为 i 加 1。
    完。
    若 有浮点 并且 有整数：
        做 追加 用 风险、「类型双关: 整数-浮点混合需谨慎」。
    完。
    若 def 的 总大小 大于 64：
        做 追加 用 风险、「联合体过大: 考虑使用指针」。
    完。
    若 做 计数 用 风险 等于 0：
        做 追加 用 风险、「安全」。
    完。
    返还 风险。
完。

联合_场景推荐 接受 def：
    字段数 为 做 计数 用 def 的 字段列表。
    若 字段数 等于 2 并且 def 的 总大小 小于或等于 8：
        返还 「类型双关(type punning)」。
    完。
    若 字段数 小于或等于 4 并且 def 的 总大小 小于或等于 16：
        返还 「寄存器映射/协议头」。
    完。
    若 def 的 总大小 大于 32：
        返还 「变体存储(建议使用标签联合)」。
    完。
    返还 「通用联合体」。
完。

标签联合_克隆 接受 实例：
    可变 新数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 实例 的 第 1 项：
        做 追加 用 新数据、实例 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 一组：实例 的 第 0 项、新数据、实例 的 第 2 项。
完。

标签联合_相等 接受 a、b：
    若 a 的 第 2 项 不等于 b 的 第 2 项：
        返还 假。
    完。
    若 做 计数 用 a 的 第 1 项 不等于 做 计数 用 b 的 第 1 项：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 1 项：
        若 a 的 第 1 项 的 第 i 项 不等于 b 的 第 1 项 的 第 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

联合_位读取 接受 实例、位偏移：
    字节索引 为 4 加 位偏移 除以 8。
    位索引 为 位偏移 取余于 8。
    若 字节索引 大于或等于 做 计数 用 实例 的 第 1 项：
        返还 0。
    完。
    返还 实例 的 第 1 项 的 第 字节索引 项 除以 (1 乘 做 联合_2幂 用 位索引) 取余于 2。
完。

联合_位写入 接受 实例、位偏移、值：
    字节索引 为 4 加 位偏移 除以 8。
    位索引 为 位偏移 取余于 8。
    若 字节索引 大于或等于 做 计数 用 实例 的 第 1 项：
        返还。
    完。
    掩码 为 做 联合_2幂 用 位索引。
    若 值 不等于 0：
        实例 的 第 1 项 的 第 字节索引 项 为 实例 的 第 1 项 的 第 字节索引 项 位或 掩码。
    否则：
        实例 的 第 1 项 的 第 字节索引 项 为 实例 的 第 1 项 的 第 字节索引 项 位与 255 减 掩码。
    完。
完。

联合_2幂 接受 n：
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 n：
        结果 为 结果 乘 2。
        i 为 i 加 1。
    完。
    返还 结果。
完。

联合_字节读取 接受 实例、偏移：
    索引 为 4 加 偏移。
    若 索引 大于或等于 做 计数 用 实例 的 第 1 项：
        返还 0。
    完。
    返还 实例 的 第 1 项 的 第 索引 项。
完。

联合_字节写入 接受 实例、偏移、值：
    索引 为 4 加 偏移。
    若 索引 大于或等于 做 计数 用 实例 的 第 1 项：
        返还。
    完。
    实例 的 第 1 项 的 第 索引 项 为 值 取余于 256。
完。

标签联合_匹配 接受 实例、处理器列表：
    标签 为 做 标签联合_取标签 用 实例。
    若 标签 大于或等于 0 并且 标签 小于 做 计数 用 处理器列表：
        _值 为 做 标签联合_取值 用 实例。
        返还 处理器列表 的 第 标签 项。
    完。
    返还 空。
完。

标签联合_遍历变体 接受 def、回调列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        若 i 小于 做 计数 用 回调列表：
            做 回调列表 的 第 i 项 用 def 的 字段列表 的 第 i 项、i。
        完。
        i 为 i 加 1。
    完。
完。

联合_转字节数组 接受 实例：
    可变 字节 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 实例 的 第 1 项：
        做 追加 用 字节、实例 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 字节。
完。

联合_从字节数组 接受 def、字节：
    可变 实例 为 做 标签联合_创建 用 def。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字节 并且 i 小于 做 计数 用 实例 的 第 1 项：
        实例 的 第 1 项 的 第 i 项 为 字节 的 第 i 项。
        i 为 i 加 1。
    完。
    实例 的 第 2 项 为 做 标签联合_取标签 用 实例。
    返还 实例。
完。

联合_哈希 接受 实例：
    可变 哈希 为 5381。
    可变 i 为 0。
    只要 i 小于 做 计数 用 实例 的 第 1 项：
        哈希 为 (哈希 乘 33 加 实例 的 第 1 项 的 第 i 项) 取余于 2147483647。
        i 为 i 加 1。
    完。
    返还 哈希。
完。

联合_定义哈希 接受 def：
    可变 哈希 为 7。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        哈希 为 哈希 乘 31 加 def 的 字段列表 的 第 i 项 的 大小。
        i 为 i 加 1。
    完。
    返还 哈希 取余于 2147483647。
完。

联合_最优布局 接受 def：
    可变 大小排序 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 def 的 字段列表：
        做 追加 用 大小排序、一组：def 的 字段列表 的 第 i 项 的 大小、def 的 字段列表 的 第 i 项 的 名称。
        i 为 i 加 1。
    完。
    可变 k 为 0。
    只要 k 小于 做 计数 用 大小排序 减 1：
        可变 j 为 k 加 1。
        只要 j 小于 做 计数 用 大小排序：
            若 大小排序 的 第 j 项 的 第 0 项 大于 大小排序 的 第 k 项 的 第 0 项：
                临 为 大小排序 的 第 k 项。
                大小排序 的 第 k 项 为 大小排序 的 第 j 项。
                大小排序 的 第 j 项 为 临。
            完。
            j 为 j 加 1。
        完。
        k 为 k 加 1。
    完。
    返还 大小排序。
完。

联合_复杂度 接受 def：
    字段数 为 做 计数 用 def 的 字段列表。
    可变 类型种类 为 0。
    可变 已见类型 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 字段数：
        可变 已见 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 已见类型：
            若 已见类型 的 第 j 项 等于 def 的 字段列表 的 第 i 项 的 类型名：
                已见 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 已见：
            做 追加 用 已见类型、def 的 字段列表 的 第 i 项 的 类型名。
            类型种类 为 类型种类 加 1。
        完。
        i 为 i 加 1。
    完。
    评分 为 字段数 乘 10 加 类型种类 乘 5 加 def 的 总大小。
    若 评分 小于 30：
        返还 一组：评分、「简单」。
    完。
    若 评分 小于 80：
        返还 一组：评分、「中等」。
    完。
    返还 一组：评分、「复杂」。
完。

本章 收束。
