《神经元》

引用《..·..·标准库·基础》。

激活_阶跃 为 1。

激活_线性 为 2。

激活_符号 为 3。

激活_饱和 为 4。

神经元_静息 为 0。

神经元_活跃 为 1。

神经元_抑制 为 2。

神经元_神经元_创建 接受 标识、阈值、偏置：
    返还 一组：标识、阈值、偏置、神经元_静息、0、一组：（空）。
完。

神经元_标识 接受 n：
    返还 n 的 第 0 项。
完。

神经元_阈值 接受 n：
    返还 n 的 第 1 项。
完。

神经元_偏置 接受 n：
    返还 n 的 第 2 项。
完。

神经元_状态 接受 n：
    返还 n 的 第 3 项。
完。

神经元_输出 接受 n：
    返还 n 的 第 4 项。
完。

神经元_连接 接受 n：
    返还 n 的 第 5 项。
完。

激活_计算 接受 激活类型、输入、阈值：
    若 激活类型 等于 激活_阶跃：
        若 输入 大于或等于 阈值：
            返还 1。
        完。
        返还 0。
    完。
    若 激活类型 等于 激活_线性：
        返还 输入。
    完。
    若 激活类型 等于 激活_符号：
        若 输入 大于 0：
            返还 1。
        完。
        若 输入 小于 0：
            返还 0 减 1。
        完。
        返还 0。
    完。
    返还 输入。
完。

突触_创建 接受 源、目标、权重、_扩4、_扩5：
    返还 一组：源、目标、权重、1、1。
完。

突触_源 接受 s：
    返还 s 的 第 0 项。
完。

突触_目标 接受 s：
    返还 s 的 第 1 项。
完。

突触_权重 接受 s：
    返还 s 的 第 2 项。
完。

突触_可塑 接受 s：
    返还 s 的 第 4 项。
完。

突触_加强 接受 突触、量：
    若 做 突触_可塑 用 突触 等于 1：
        突触 的 第 2 项 为 突触 的 第 2 项 加 量。
    完。
完。

突触_削弱 接受 突触、量：
    若 做 突触_可塑 用 突触 等于 1：
        突触 的 第 2 项 为 突触 的 第 2 项 减 量。
        若 突触 的 第 2 项 小于 0：
            突触 的 第 2 项 为 0。
        完。
    完。
完。

神经元_激活 接受 神经元、输入信号列表、激活类型：
    若 做 计数 用 输入信号列表 等于 0：
        神经元 的 第 3 项 为 神经元_静息。
        神经元 的 第 4 项 为 0。
        返还 0。
    完。
    可变 加权和 为 做 神经元_偏置 用 神经元。
    可变 i 为 0。
    只要 i 小于 做 计数 用 输入信号列表：
        加权和 为 加权和 加 输入信号列表 的 第 i 项。
        i 为 i 加 1。
    完。
    输出 为 做 激活_计算 用 激活类型、加权和、做 神经元_阈值 用 神经元。
    神经元 的 第 4 项 为 输出。
    若 输出 大于 0：
        神经元 的 第 3 项 为 神经元_活跃。
    否则：
        神经元 的 第 3 项 为 神经元_静息。
    完。
    返还 输出。
完。

神经层_创建 接受 大小、激活类型：
    可变 神经元列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        做 追加 用 神经元列表、做 神经元_神经元_创建 用 「n」 加 做 转字符串 用 i、0、0。
        i 为 i 加 1。
    完。
    返还 一组：神经元列表、激活类型、大小。
完。

神经层_前向 接受 层、输入向量：
    若 做 计数 用 输入向量 不等于 层 的 第 2 项：
        返还 一组：（空）。
    完。
    可变 输出向量 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 层 的 第 2 项：
        单元输入 为 一组：输入向量 的 第 i 项。
        输出 为 做 神经元_激活 用 层 的 第 0 项 的 第 i 项、单元输入、层 的 第 1 项。
        做 追加 用 输出向量、输出。
        i 为 i 加 1。
    完。
    返还 输出向量。
完。

感知机_创建 接受 输入维度、阈值：
    可变 权重列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 输入维度：
        做 追加 用 权重列表、0。
        i 为 i 加 1。
    完。
    返还 一组：权重列表、阈值、0、输入维度。
完。

感知机_前向 接受 感知机、输入：
    若 做 计数 用 输入 不等于 感知机 的 第 3 项：
        返还 0。
    完。
    可变 加权和 为 感知机 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 感知机 的 第 3 项：
        加权和 为 加权和 加 感知机 的 第 0 项 的 第 i 项 乘 输入 的 第 i 项。
        i 为 i 加 1。
    完。
    若 加权和 大于或等于 感知机 的 第 1 项：
        返还 1。
    完。
    返还 0。
完。

感知机_学习 接受 感知机、输入、期望输出、学习率：
    实际输出 为 做 感知机_前向 用 感知机、输入。
    误差 为 期望输出 减 实际输出。
    若 误差 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 感知机 的 第 3 项：
        感知机 的 第 0 项 的 第 i 项 为 感知机 的 第 0 项 的 第 i 项 加 学习率 乘 误差 乘 输入 的 第 i 项。
        i 为 i 加 1。
    完。
    感知机 的 第 2 项 为 感知机 的 第 2 项 加 学习率 乘 误差。
    返还 误差。
完。

赫布_更新 接受 突触列表、激活状态、学习率、衰减率：
    可变 i 为 0。
    只要 i 小于 做 计数 用 突触列表：
        突触 为 突触列表 的 第 i 项。
        若 做 突触_可塑 用 突触 等于 1：
            可变 源激活 为 0。
            可变 目标激活 为 0。
            若 做 字典有 用 激活状态、做 突触_源 用 突触：
                源激活 为 做 字典取 用 激活状态、做 突触_源 用 突触。
            完。
            若 做 字典有 用 激活状态、做 突触_目标 用 突触：
                目标激活 为 做 字典取 用 激活状态、做 突触_目标 用 突触。
            完。
            更新量 为 学习率 乘 源激活 乘 目标激活 减 衰减率 乘 做 突触_权重 用 突触。
            突触 的 第 2 项 为 突触 的 第 2 项 加 更新量。
        完。
        i 为 i 加 1。
    完。
完。

可变 _神经元_创建总数 为 0。

可变 _神经元_激活总数 为 0。

可变 _突触_加强总数 为 0。

可变 _突触_削弱总数 为 0。

神经元_记录创建 接受 零参：
    _神经元_创建总数 为 _神经元_创建总数 加 1。
完。

神经元_记录激活 接受 零参：
    _神经元_激活总数 为 _神经元_激活总数 加 1。
完。

本章 收束。
