《推理》

引用《..·..·标准库·基础》。

推理_演绎 为 1。

推理_归纳 为 2。

推理_类比 为 3。

推理_因果 为 4。

推理_反向 为 5。

推理_假设 为 6。

置信_确定 为 1000。

置信_高 为 800。

置信_中 为 600。

置信_低 为 400。

置信_猜测 为 200。

推理_创建命题 接受 类型、前提列表、结论、置信度：
    返还 一组：类型、前提列表、结论、置信度、一组：（空）。
完。

推理_命题类型 接受 命题：
    返还 命题 的 第 0 项。
完。

推理_命题前提 接受 命题：
    返还 命题 的 第 1 项。
完。

推理_命题结论 接受 命题：
    返还 命题 的 第 2 项。
完。

推理_命题置信 接受 命题：
    返还 命题 的 第 3 项。
完。

推理_命题链 接受 命题：
    返还 命题 的 第 4 项。
完。

推理_演绎推断 接受 规则列表、已知条件：
    可变 结论列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 规则列表：
        规则 为 规则列表 的 第 i 项。
        前提 为 做 推理_命题前提 用 规则。
        可变 前提满足 为 真。
        可变 j 为 0。
        只要 j 小于 做 计数 用 前提：
            可变 找到 为 假。
            可变 k 为 0。
            只要 k 小于 做 计数 用 已知条件：
                若 已知条件 的 第 k 项 等于 前提 的 第 j 项：
                    找到 为 真。
                完。
                k 为 k 加 1。
            完。
            若 找到 等于 假：
                前提满足 为 假。
            完。
            j 为 j 加 1。
        完。
        若 前提满足：
            做 追加 用 结论列表、做 推理_创建命题 用 推理_演绎、前提、做 推理_命题结论 用 规则、做 推理_命题置信 用 规则。
        完。
        i 为 i 加 1。
    完。
    返还 结论列表。
完。

推理_归纳规律 接受 案例列表、提取函数、最小支持度：
    可变 模式表 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 案例列表：
        模式 为 做 提取函数 用 案例列表 的 第 i 项。
        若 模式 不等于 空：
            若 做 字典有 用 模式表、模式：
                做 字典设 用 模式表、模式、做 字典取 用 模式表、模式 加 1。
            否则：
                做 字典设 用 模式表、模式、1。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 规律列表 为 一组：（空）。
    所有模式 为 做 字典键 用 模式表。
    i 为 0。
    只要 i 小于 做 计数 用 所有模式：
        出现次数 为 做 字典取 用 模式表、所有模式 的 第 i 项。
        若 出现次数 大于或等于 最小支持度：
            置信度 为 出现次数 乘 1000 除以 做 计数 用 案例列表。
            做 追加 用 规律列表、做 推理_创建命题 用 推理_归纳、案例列表、所有模式 的 第 i 项、置信度。
        完。
        i 为 i 加 1。
    完。
    返还 规律列表。
完。

推理_类比迁移 接受 源实体、目标实体、已知属性、图谱：
    可变 推断属性 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已知属性：
        属性 为 已知属性 的 第 i 项。
        做 追加 用 推断属性、做 推理_创建命题 用 推理_类比、一组：源实体、目标实体、属性、目标实体 加 「具有」 加 属性、置信_低。
        i 为 i 加 1。
    完。
    返还 推断属性。
完。

可变 _因果链表 为 一组：（空）。

推理_记录因果 接受 原因、结果、强度：
    做 追加 用 _因果链表、一组：原因、结果、强度。
完。

推理_追溯原因 接受 目标结果、深度：
    若 深度 小于或等于 0：
        返还 一组：（空）。
    完。
    可变 原因列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _因果链表：
        若 _因果链表 的 第 i 项 的 第 1 项 等于 目标结果：
            做 追加 用 原因列表、_因果链表 的 第 i 项 的 第 0 项。
            深层原因 为 做 推理_追溯原因 用 _因果链表 的 第 i 项 的 第 0 项、深度 减 1。
            可变 j 为 0。
            只要 j 小于 做 计数 用 深层原因：
                做 追加 用 原因列表、深层原因 的 第 j 项。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 原因列表。
完。

推理_合并置信 接受 置信度列表：
    若 做 计数 用 置信度列表 等于 0：
        返还 0。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 置信度列表：
        总 为 总 加 置信度列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总 除以 做 计数 用 置信度列表。
完。

推理_是否矛盾 接受 命题A、命题B：
    若 做 推理_命题结论 用 命题A 等于 做 推理_命题结论 用 命题B：
        返还 假。
    完。
    返还 真。
完。

可变 _推理_演绎次数 为 0。

可变 _推理_归纳次数 为 0。

可变 _推理_类比次数 为 0。

可变 _推理_因果次数 为 0。

推理_记录演绎 接受 零参：
    _推理_演绎次数 为 _推理_演绎次数 加 1。
完。

推理_记录归纳 接受 零参：
    _推理_归纳次数 为 _推理_归纳次数 加 1。
完。

推理_记录类比 接受 零参：
    _推理_类比次数 为 _推理_类比次数 加 1。
完。

本章 收束。
