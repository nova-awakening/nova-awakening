《部首特征重要度》

引用《..·..·标准库·基础》。

部首重要度基线 为 一组：一组：20154、「人」、1000、820、一组：27700、「水」、1600、780、一组：26408、「木」、1300、750、一组：24515、「心」、1100、880、一组：28779、「火」、950、760、一组：21475、「口」、1100、720、一组：26085、「日」、580、700、一组：25163、「手」、900、710、一组：37329、「金」、830、740、一组：22899、「女」、680、690、一组：33609、「草」、1900、670、一组：26376、「月」、200、650、一组：35328、「言」、850、830、一组：21463、「刀」、210、660、一组：23665、「山」、420、630。

部首特征_按重要度排序 接受 部首列表：
    可变 已排序 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 部首列表：
        做 追加 用 已排序、部首列表 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 changed 为 真。
    只要 changed：
        changed 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 已排序 减 1：
            若 已排序 的 第 j 项 的 第 3 项 小于 已排序 的 第 j 加 1 项 的 第 3 项：
                tmp 为 已排序 的 第 j 项。
                已排序 的 第 j 项 为 已排序 的 第 j 加 1 项。
                已排序 的 第 j 加 1 项 为 tmp。
                changed 为 真。
            完。
            j 为 j 加 1。
        完。
    完。
    返还 已排序。
完。

部首维度分配 接受 总维度、前N个部首：
    已排序 为 做 部首特征_按重要度排序 用 部首重要度基线。
    可变 实际N 为 做 计数 用 已排序。
    若 前N个部首 小于 做 计数 用 已排序：
        实际N 为 前N个部首。
    完。
    可变 总增益 为 0。
    可变 i 为 0。
    只要 i 小于 实际N：
        总增益 为 总增益 加 已排序 的 第 i 项 的 第 3 项。
        i 为 i 加 1。
    完。
    若 总增益 等于 0：
        总增益 为 1。
    完。
    可变 维度分配 为 一组：（空）。
    可变 已分配 为 0。
    i 为 0。
    只要 i 小于 实际N：
        分配 为 已排序 的 第 i 项 的 第 3 项 乘 总维度 除以 总增益。
        若 分配 小于 1：
            分配 为 1。
        完。
        做 追加 用 维度分配、一组：已排序 的 第 i 项 的 第 1 项、分配。
        已分配 为 已分配 加 分配。
        i 为 i 加 1。
    完。
    返还 维度分配。
完。

部首覆盖率分析 接受 前N个部首：
    已排序 为 做 部首特征_按重要度排序 用 部首重要度基线。
    可变 实际N 为 做 计数 用 已排序。
    若 前N个部首 小于或等于 做 计数 用 已排序：
        实际N 为 前N个部首。
    完。
    总字数 为 3500。
    可变 覆盖总数 为 0。
    可变 i 为 0。
    只要 i 小于 实际N：
        覆盖总数 为 覆盖总数 加 已排序 的 第 i 项 的 第 2 项。
        i 为 i 加 1。
    完。
    可变 覆盖率 为 覆盖总数 乘 1000 除以 总字数。
    若 覆盖总数 大于 总字数：
        覆盖率 为 1000。
    完。
    返还 一组：实际N、覆盖率、覆盖总数。
完。

最优部首集 接受 信息增益阈值_千：
    已排序 为 做 部首特征_按重要度排序 用 部首重要度基线。
    可变 选中 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已排序：
        若 已排序 的 第 i 项 的 第 3 项 大于或等于 信息增益阈值_千：
            做 追加 用 选中、已排序 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 选中。
完。

部首重要度_报告 接受 零参：
    已排序 为 做 部首特征_按重要度排序 用 部首重要度基线。
    做 打印 用 「=== 部首特征重要度排名 ===」。
    做 打印 用 「(信息增益越高，语义区分度越强)」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已排序：
        做 打印 用 「[」 加 做 转字符串 用 i 加 1 加 「] 」 加 已排序 的 第 i 项 的 第 1 项 加 「 信息增益=」 加 做 转字符串 用 已排序 的 第 i 项 的 第 3 项 加 「/1000  覆盖汉字≈」 加 做 转字符串 用 已排序 的 第 i 项 的 第 2 项 加 「个」。
        i 为 i 加 1。
    完。
    覆盖 为 做 部首覆盖率分析 用 8。
    做 打印 用 「\n前8个高价值部首覆盖常用汉字: 」 加 做 转字符串 用 覆盖 的 第 1 项 除以 10 加 「.」 加 做 转字符串 用 覆盖 的 第 1 项 取余于 10 加 「%」。
    最优集 为 做 最优部首集 用 700。
    做 打印 用 「信息增益>0.7的部首集合: 」 加 做 转字符串 用 最优集。
完。

本章 收束。
