《学习》

引用《..·..·标准库·基础》。

学习_监督 为 1。

学习_无监督 为 2。

学习_强化 为 3。

学习_迁移 为 4。

学习_联想 为 5。

学习_习惯 为 6。

奖励_强正 为 1000。

奖励_中正 为 500。

奖励_弱正 为 100。

奖励_中性 为 0。

奖励_弱负 为 0 减 100。

奖励_中负 为 0 减 500。

奖励_强负 为 0 减 1000。

学习_记录经验 接受 输入、期望输出、实际输出、奖励：
    返还 一组：输入、期望输出、实际输出、奖励、0。
完。

经验_输入 接受 样本：
    返还 样本 的 第 0 项。
完。

经验_期望 接受 样本：
    返还 样本 的 第 1 项。
完。

经验_实际 接受 样本：
    返还 样本 的 第 2 项。
完。

经验_奖励 接受 样本：
    返还 样本 的 第 3 项。
完。

经验池_创建 接受 容量：
    返还 一组：一组：（空）、容量、0。
完。

经验池_存入 接受 池、样本：
    若 做 计数 用 池 的 第 0 项 大于或等于 池 的 第 1 项：
        做 删除 用 池 的 第 0 项。
    完。
    做 追加 用 池 的 第 0 项、样本。
    池 的 第 2 项 为 池 的 第 2 项 加 1。
完。

经验池_大小 接受 池：
    返还 做 计数 用 池 的 第 0 项。
完。

经验池_取样 接受 池、数量：
    若 做 计数 用 池 的 第 0 项 小于 数量：
        返还 池 的 第 0 项。
    完。
    可变 样本列表 为 一组：（空）。
    可变 i 为 0。
    步长 为 做 计数 用 池 的 第 0 项 除以 数量。
    只要 i 小于 数量：
        做 追加 用 样本列表、池 的 第 0 项 的 第 i 乘 步长 项。
        i 为 i 加 1。
    完。
    返还 样本列表。
完。

Q学习_创建 接受 状态空间大小、动作空间大小、学习率、折扣因子：
    可变 Q表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 状态空间大小：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 动作空间大小：
            做 追加 用 行、0。
            j 为 j 加 1。
        完。
        做 追加 用 Q表、行。
        i 为 i 加 1。
    完。
    返还 一组：Q表、学习率、折扣因子、状态空间大小、动作空间大小。
完。

Q学习_最优动作 接受 Q学习器、状态：
    若 状态 大于或等于 Q学习器 的 第 3 项：
        返还 0。
    完。
    动作列表 为 Q学习器 的 第 0 项 的 第 状态 项。
    可变 最大Q值 为 动作列表 的 第 0 项。
    可变 最优动作 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 动作列表：
        若 动作列表 的 第 i 项 大于 最大Q值：
            最大Q值 为 动作列表 的 第 i 项。
            最优动作 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最优动作。
完。

学习_Q学习_更新 接受 Q学习器、当前状态、动作、奖励、下一状态：
    若 当前状态 大于或等于 Q学习器 的 第 3 项：
        返还。
    完。
    若 下一状态 大于或等于 Q学习器 的 第 3 项：
        返还。
    完。
    若 动作 大于或等于 Q学习器 的 第 4 项：
        返还。
    完。
    下一最优动作 为 做 Q学习_最优动作 用 Q学习器、下一状态。
    下一最大Q 为 Q学习器 的 第 0 项 的 第 下一状态 项 的 第 下一最优动作 项。
    目标Q 为 奖励 加 Q学习器 的 第 2 项 乘 下一最大Q。
    当前Q 为 Q学习器 的 第 0 项 的 第 当前状态 项 的 第 动作 项。
    Q学习器 的 第 0 项 的 第 当前状态 项 的 第 动作 项 为 当前Q 加 Q学习器 的 第 1 项 乘 (目标Q 减 当前Q)。
完。

可变 _学习_损失历史 为 一组：（空）。

可变 _学习_准确率历史 为 一组：（空）。

可变 _学习_迭代次数 为 0。

学习_记录指标 接受 损失、准确率：
    做 追加 用 _学习_损失历史、损失。
    做 追加 用 _学习_准确率历史、准确率。
    _学习_迭代次数 为 _学习_迭代次数 加 1。
完。

学习_最新损失 接受 零参：
    若 做 计数 用 _学习_损失历史 等于 0：
        返还 0。
    完。
    返还 _学习_损失历史 的 第 做 计数 用 _学习_损失历史 减 1 项。
完。

学习_最新准确率 接受 零参：
    若 做 计数 用 _学习_准确率历史 等于 0：
        返还 0。
    完。
    返还 _学习_准确率历史 的 第 做 计数 用 _学习_准确率历史 减 1 项。
完。

学习_趋势 接受 最近N步：
    若 做 计数 用 _学习_损失历史 小于 最近N步：
        返还 0。
    完。
    首个 为 _学习_损失历史 的 第 做 计数 用 _学习_损失历史 减 最近N步 项。
    最新 为 _学习_损失历史 的 第 做 计数 用 _学习_损失历史 减 1 项。
    返还 首个 减 最新。
完。

可变 _联想对列表 为 一组：（空）。

学习_建立联想 接受 刺激A、刺激B、强度：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _联想对列表：
        若 _联想对列表 的 第 i 项 的 第 0 项 等于 刺激A：
            若 _联想对列表 的 第 i 项 的 第 1 项 等于 刺激B：
                _联想对列表 的 第 i 项 的 第 2 项 为 _联想对列表 的 第 i 项 的 第 2 项 加 强度。
                返还 _联想对列表 的 第 i 项 的 第 2 项。
            完。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 _联想对列表、一组：刺激A、刺激B、强度。
    返还 强度。
完。

学习_联想检索 接受 刺激A：
    可变 联想列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _联想对列表：
        若 _联想对列表 的 第 i 项 的 第 0 项 等于 刺激A：
            做 追加 用 联想列表、一组：_联想对列表 的 第 i 项 的 第 1 项、_联想对列表 的 第 i 项 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
    返还 联想列表。
完。

可变 _学习_经验总数 为 0。

可变 _学习_联想总数 为 0。

学习_记录经验数 接受 零参：
    _学习_经验总数 为 _学习_经验总数 加 1。
完。

学习_记录联想数 接受 零参：
    _学习_联想总数 为 _学习_联想总数 加 1。
完。

STDP整数 接受 dt_ms：
    若 dt_ms 小于 0：
        若 dt_ms 小于 负 100：
            返还 负 50。
        完。
        若 dt_ms 小于 负 50：
            返还 负 50 加 (0 减 dt_ms 减 50) 乘 200 除以 50。
        完。
        若 dt_ms 小于 负 20：
            返还 负 250 加 (0 减 dt_ms 减 20) 乘 350 除以 30。
        完。
        返还 负 600 加 (0 减 dt_ms) 乘 600 除以 20。
    完。
    若 dt_ms 等于 0：
        返还 1000。
    完。
    若 dt_ms 小于 10：
        返还 1000 减 dt_ms 乘 400 除以 10。
    完。
    若 dt_ms 小于 30：
        返还 600 减 (dt_ms 减 10) 乘 350 除以 20。
    完。
    若 dt_ms 小于 60：
        返还 250 减 (dt_ms 减 30) 乘 200 除以 30。
    完。
    若 dt_ms 大于或等于 100：
        返还 0。
    完。
    返还 50 减 (dt_ms 减 60) 乘 50 除以 40。
完。

Hebbian更新 接受 权重、eta_q10、pre_q10、post_q10：
    dw 为 eta_q10 乘 pre_q10 除以 1024 乘 post_q10 除以 1024。
    返还 权重 加 dw。
完。

BCM更新 接受 权重、eta、pre、post、theta：
    dw 为 eta 乘 post 乘 (post 减 theta) 除以 1024 乘 pre 除以 1024。
    返还 权重 加 dw。
完。

本章 收束。
