《感知》

引用《..·..·标准库·基础》。

注意力_忽略 为 0。

注意力_背景 为 200。

注意力_一般 为 500。

注意力_重要 为 800。

注意力_关键 为 1000。

感知_视觉 为 1。

感知_听觉 为 2。

感知_语义 为 3。

感知_情感 为 4。

感知_时序 为 5。

感知_关系 为 6。

感知_创建注意力 接受 对象、权重、维度：
    返还 一组：对象、权重、维度、0、10。
完。

感知_注意对象 接受 单元：
    返还 单元 的 第 0 项。
完。

感知_注意权重 接受 单元：
    返还 单元 的 第 1 项。
完。

感知_注意维度 接受 单元：
    返还 单元 的 第 2 项。
完。

感知_注意时间 接受 单元：
    返还 单元 的 第 3 项。
完。

感知_注意衰减 接受 单元：
    返还 单元 的 第 4 项。
完。

感知_创建注意力场 接受 容量：
    返还 一组：一组：（空）、容量、0。
完。

感知_场_追加 接受 场、单元：
    若 做 计数 用 场 的 第 0 项 大于或等于 场 的 第 1 项：
        做 感知_场_清理低权重 用 场。
    完。
    做 追加 用 场 的 第 0 项、单元。
    场 的 第 2 项 为 场 的 第 2 项 加 1。
完。

感知_场_清理低权重 接受 场：
    可变 新列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 场 的 第 0 项：
        若 做 感知_注意权重 用 场 的 第 0 项 的 第 i 项 大于 注意力_背景：
            做 追加 用 新列表、场 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    场 的 第 0 项 为 新列表。
完。

感知_场_最高注意 接受 场：
    可变 最大索引 为 0。
    若 做 计数 用 场 的 第 0 项 等于 0：
        返还 空。
    完。
    可变 最大权重 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 场 的 第 0 项：
        若 做 感知_注意权重 用 场 的 第 0 项 的 第 i 项 大于 最大权重：
            最大权重 为 做 感知_注意权重 用 场 的 第 0 项 的 第 i 项。
            最大索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 场 的 第 0 项 的 第 最大索引 项。
完。

感知_场_按维度筛选 接受 场、维度：
    可变 结果列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 场 的 第 0 项：
        若 做 感知_注意维度 用 场 的 第 0 项 的 第 i 项 等于 维度：
            做 追加 用 结果列表、场 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果列表。
完。

感知_衰减注意力 接受 场、步长：
    可变 i 为 0。
    只要 i 小于 做 计数 用 场 的 第 0 项：
        单元 为 场 的 第 0 项 的 第 i 项。
        新权重 为 做 感知_注意权重 用 单元 减 做 感知_注意衰减 用 单元 乘 步长。
        若 新权重 小于 0：
            新权重 为 0。
        完。
        单元 的 第 1 项 为 新权重。
        i 为 i 加 1。
    完。
完。

感知_处理输入 接受 场、输入内容、维度、初始权重：
    单元 为 做 感知_创建注意力 用 输入内容、初始权重、维度。
    做 感知_场_追加 用 场、单元。
    返还 单元。
完。

感知_计算显著性 接受 新信息、已有列表、维度：
    可变 相似数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已有列表：
        若 做 感知_注意维度 用 已有列表 的 第 i 项 等于 维度：
            若 做 感知_注意对象 用 已有列表 的 第 i 项 等于 新信息：
                相似数 为 相似数 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    若 相似数 等于 0：
        返还 注意力_关键。
    完。
    若 相似数 小于 3：
        返还 注意力_重要。
    完。
    若 相似数 小于 10：
        返还 注意力_一般。
    完。
    返还 注意力_背景。
完。

感知_多头关注 接受 场列表、输入、权重列表：
    若 做 计数 用 场列表 不等于 做 计数 用 权重列表：
        返还 一组：（空）。
    完。
    可变 结果列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 场列表：
        单元 为 做 感知_创建注意力 用 输入、权重列表 的 第 i 项、i。
        做 感知_场_追加 用 场列表 的 第 i 项、单元。
        做 追加 用 结果列表、单元。
        i 为 i 加 1。
    完。
    返还 结果列表。
完。

可变 _感知_处理总数 为 0。

可变 _感知_显著性检测数 为 0。

可变 _感知_衰减次数 为 0。

感知_记录处理 接受 零参：
    _感知_处理总数 为 _感知_处理总数 加 1。
完。

感知_记录显著 接受 零参：
    _感知_显著性检测数 为 _感知_显著性检测数 加 1。
完。

本章 收束。
