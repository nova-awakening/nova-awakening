《语言理解》

引用《..·..·标准库·基础》。

意图_提问 为 1。

意图_命令 为 2。

意图_描述 为 3。

意图_条件 为 4。

意图_目标 为 5。

意图_解释 为 6。

情感_肯定 为 1。

情感_否定 为 0。

情感_中性 为 2。

语言_分析单元 接受 原文：
    返还 一组：原文、意图_描述、「」、「」、「」、情感_中性、500。
完。

语言单元_原文 接受 单元：
    返还 单元 的 第 0 项。
完。

语言单元_意图 接受 单元：
    返还 单元 的 第 1 项。
完。

语言单元_主体 接受 单元：
    返还 单元 的 第 2 项。
完。

语言单元_谓语 接受 单元：
    返还 单元 的 第 3 项。
完。

语言单元_宾体 接受 单元：
    返还 单元 的 第 4 项。
完。

语言单元_情感 接受 单元：
    返还 单元 的 第 5 项。
完。

语言单元_置信 接受 单元：
    返还 单元 的 第 6 项。
完。

可变 _疑问词列表 为 一组：「什么」、「怎么」、「为什么」、「如何」、「哪里」、「谁」、「几」、「多少」。

可变 _命令词列表 为 一组：「请」、「帮」、「做」、「给」、「执行」、「运行」、「输出」、「生成」、「创建」。

可变 _条件词列表 为 一组：「如果」、「假如」、「若」、「当」、「只要」、「一旦」、「倘若」。

可变 _目标词列表 为 一组：「想要」、「希望」、「需要」、「目标」、「目的」、「达到」、「实现」。

可变 _否定词列表 为 一组：「不」、「没」、「无」、「否」、「错」、「假」、「非」。

语言_识别意图 接受 文本：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _疑问词列表：
        若 做 计数 用 文本 大于 做 计数 用 _疑问词列表 的 第 i 项：
            若 做 以开头 用 文本、_疑问词列表 的 第 i 项 或者 做 以结尾 用 文本、「?」 或者 做 以结尾 用 文本、「？」：
                返还 意图_提问。
            完。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 _命令词列表：
        若 做 以开头 用 文本、_命令词列表 的 第 i 项：
            返还 意图_命令。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 _条件词列表：
        若 做 以开头 用 文本、_条件词列表 的 第 i 项：
            返还 意图_条件。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 _目标词列表：
        可变 j 为 0。
        只要 j 小于 做 计数 用 文本 减 做 计数 用 _目标词列表 的 第 i 项 加 1：
            若 做 子串 用 文本、j、做 计数 用 _目标词列表 的 第 i 项 等于 _目标词列表 的 第 i 项：
                返还 意图_目标。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 意图_描述。
完。

语言_识别情感极性 接受 文本：
    可变 否定计数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _否定词列表：
        词 为 _否定词列表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 文本 减 做 计数 用 词 加 1：
            若 做 子串 用 文本、j、做 计数 用 词 等于 词：
                否定计数 为 否定计数 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 否定计数 等于 0：
        返还 情感_肯定。
    完。
    若 否定计数 取余于 2 等于 0：
        返还 情感_肯定。
    完。
    返还 情感_否定。
完。

可变 _语言模板表 为 做 字典。

语言_注册模板 接受 模板文本、解释函数：
    做 字典设 用 _语言模板表、模板文本、解释函数。
完。

语言_匹配模板 接受 输入文本：
    所有模板 为 做 字典键 用 _语言模板表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有模板：
        若 做 以开头 用 输入文本、所有模板 的 第 i 项：
            返还 一组：所有模板 的 第 i 项、做 字典取 用 _语言模板表、所有模板 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

对话_创建上下文 接受 零参：
    返还 一组：一组：（空）、做 字典、「」、0。
完。

对话_追加发言 接受 上下文、发言者、内容：
    做 追加 用 上下文 的 第 0 项、一组：发言者、内容、做 语言_识别意图 用 内容。
    上下文 的 第 2 项 为 内容。
    上下文 的 第 3 项 为 上下文 的 第 3 项 加 1。
完。

对话_上下文轮次 接受 上下文：
    返还 上下文 的 第 3 项。
完。

对话_最近发言 接受 上下文：
    返还 上下文 的 第 2 项。
完。

对话_历史 接受 上下文：
    返还 上下文 的 第 0 项。
完。

可变 _同义词表 为 做 字典。

语言_注册同义词 接受 词A、词B：
    若 做 字典有 用 _同义词表、词A 等于 假：
        做 字典设 用 _同义词表、词A、一组：（空）。
    完。
    做 追加 用 做 字典取 用 _同义词表、词A、词B。
    若 做 字典有 用 _同义词表、词B 等于 假：
        做 字典设 用 _同义词表、词B、一组：（空）。
    完。
    做 追加 用 做 字典取 用 _同义词表、词B、词A。
完。

语言_语义相近 接受 词A、词B：
    若 词A 等于 词B：
        返还 真。
    完。
    若 做 字典有 用 _同义词表、词A：
        同义列表 为 做 字典取 用 _同义词表、词A。
        可变 i 为 0。
        只要 i 小于 做 计数 用 同义列表：
            若 同义列表 的 第 i 项 等于 词B：
                返还 真。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 假。
完。

语言_初始化基础词库 接受 零参：
    做 语言_注册同义词 用 「如果」、「若」。
    做 语言_注册同义词 用 「如果」、「假如」。
    做 语言_注册同义词 用 「如果」、「倘若」。
    做 语言_注册同义词 用 「否则」、「不然」。
    做 语言_注册同义词 用 「否则」、「反之」。
    做 语言_注册同义词 用 「所以」、「因此」。
    做 语言_注册同义词 用 「所以」、「于是」。
    做 语言_注册同义词 用 「但是」、「然而」。
    做 语言_注册同义词 用 「但是」、「可是」。
    做 语言_注册同义词 用 「函数」、「方法」。
    做 语言_注册同义词 用 「函数」、「算法」。
    做 语言_注册同义词 用 「返回」、「得到」。
    做 语言_注册同义词 用 「返回」、「输出」。
    做 语言_注册同义词 用 「定义」、「设」。
    做 语言_注册同义词 用 「定义」、「令」。
完。

可变 _语言_分析总数 为 0。

可变 _语言_模板命中数 为 0。

可变 _语言_意图识别数 为 0。

语言_记录分析 接受 零参：
    _语言_分析总数 为 _语言_分析总数 加 1。
完。

语言_记录模板命中 接受 零参：
    _语言_模板命中数 为 _语言_模板命中数 加 1。
完。

语言_信息量估算 接受 文本、每字符单位为千分之一bit：
    可变 总量 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        cp 为 做 字符码 用 做 子串 用 文本、i、1、0。
        若 cp 大于或等于 19968 并且 cp 小于或等于 40959：
            总量 为 总量 加 11770。
        否则若 cp 小于或等于 127：
            总量 为 总量 加 4700。
        否则：
            总量 为 总量 加 8000。
        完。
        i 为 i 加 1。
    完。
    返还 总量。
完。

语言_上下文压缩比 接受 文本：
    中文信息量 为 做 语言_信息量估算 用 文本、1000。
    等效英文字符数 为 中文信息量 除以 4700。
    实际字符数 为 做 计数 用 文本。
    若 实际字符数 等于 0：
        返还 一组：0、0、100。
    完。
    压缩比 为 等效英文字符数 乘 100 除以 实际字符数。
    返还 一组：中文信息量、等效英文字符数、压缩比。
完。

语言_推荐窗口大小 接受 英文基准窗口：
    中文窗口 为 英文基准窗口 乘 56 除以 100。
    若 中文窗口 小于 512：
        返还 512。
    完。
    返还 中文窗口。
完。

本章 收束。
