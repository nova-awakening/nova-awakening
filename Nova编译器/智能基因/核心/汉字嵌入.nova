《汉字嵌入》

引用《..·..·标准库·基础》。

引用《..·..·标准库·算法·数学扩展》。

嵌入维度 为 64。

部首维度 为 8。

CJK_下界 为 19968。

CJK_上界 为 40959。

部首语义映射 为 一组：一组：26085、一组：0、1、2、90、一组：26376、一组：0、1、3、85、一组：27700、一组：0、2、4、88、一组：28779、一组：0、2、4、87、一组：23665、一组：0、2、5、82、一组：26408、一组：1、4、5、85、一组：33609、一组：1、4、5、80、一组：20154、一组：2、3、6、92、一组：21475、一组：2、3、6、88、一组：25163、一组：2、3、7、85、一组：24515、一组：5、6、7、90、一组：35328、一组：5、6、7、88、一组：21463、一组：3、4、6、78、一组：37329、一组：3、4、6、82、一组：22899、一组：2、5、6、84、一组：23376、一组：2、6、7、80。

部首区间表 为 一组：一组：27700、27799、4、一组：27826、27900、4、一组：28023、28100、4、一组：28165、28250、4、一组：28304、28400、4、一组：28459、28530、4、一组：28610、28700、4、一组：28814、28900、4、一组：29042、29100、4、一组：28779、28870、5、一组：28900、28970、5、一组：29006、29050、5、一组：26408、26510、1、一组：26524、26630、1、一组：26641、26750、1、一组：26771、26850、1、一组：26862、26950、1、一组：27004、27080、1、一组：20154、20250、6、一组：20266、20380、6、一组：20384、20490、6、一组：20497、20600、6、一组：20613、20700、6、一组：21475、21580、7、一组：21584、21690、7、一组：21693、21800、7、一组：24515、24620、8、一组：24633、24740、8、一组：24812、24900、8、一组：25163、25270、9、一组：25284、25390、9、一组：25392、25500、9、一组：35328、35430、10、一组：35442、35540、10、一组：35745、35850、10、一组：37329、37440、11、一组：38024、38130、11、一组：38142、38250、11、一组：26085、26190、0、一组：26195、26300、0、一组：26326、26420、0、一组：26376、26470、2、一组：23665、23770、3、一组：22899、23000、12、一组：23376、23470、13、一组：21463、21570、14、一组：33609、33720、15、一组：33733、33840、15、一组：33853、33960、15。

部首语义表 为 一组：一组：一组：0、1、2、90、一组：一组：1、4、5、85、一组：一组：0、1、3、85、一组：一组：0、2、5、82、一组：一组：0、2、4、88、一组：一组：0、2、4、87、一组：一组：2、3、6、92、一组：一组：2、3、6、88、一组：一组：5、6、7、90、一组：一组：2、3、7、85、一组：一组：5、6、7、88、一组：一组：3、4、6、82、一组：一组：2、5、6、84、一组：一组：2、6、7、80、一组：一组：3、4、6、78、一组：一组：1、4、5、80。

查找部首语义 接受 汉字码点：
    可变 i 为 0。
    只要 i 小于 做 计数 用 部首区间表：
        区间 为 部首区间表 的 第 i 项。
        若 汉字码点 大于或等于 区间 的 第 0 项 并且 汉字码点 小于或等于 区间 的 第 1 项：
            部首idx 为 区间 的 第 2 项。
            若 部首idx 小于 做 计数 用 部首语义表：
                语义 为 部首语义表 的 第 部首idx 项。
                返还 一组：汉字码点、语义 的 第 0 项、语义 的 第 1 项。
            完。
        完。
        i 为 i 加 1。
    完。
    若 汉字码点 大于或等于 CJK_下界 并且 汉字码点 小于或等于 CJK_上界：
        偏移 为 汉字码点 减 CJK_下界。
        区域 为 偏移 乘 8 除以 (CJK_上界 减 CJK_下界)。
        返还 一组：汉字码点、一组：区域、(区域 加 1) 取余于 8、(区域 加 3) 取余于 8、40。
    完。
    返还 一组：汉字码点、一组：0、1、2、3、4、5、6、7、30。
完。

汉字嵌入_初始化 接受 汉字码点：
    可变 嵌入向量 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 嵌入维度：
        做 追加 用 嵌入向量、0。
        i 为 i 加 1。
    完。
    部首信息 为 做 查找部首语义 用 汉字码点。
    子空间列表 为 部首信息 的 第 1 项。
    强度 为 部首信息 的 第 2 项。
    可变 j 为 0。
    只要 j 小于 做 计数 用 子空间列表：
        空间索引 为 子空间列表 的 第 j 项。
        可变 k 为 0。
        只要 k 小于 部首维度：
            dim 为 空间索引 乘 部首维度 加 k。
            若 dim 小于 嵌入维度：
                phase 为 (汉字码点 乘 2654435761 加 k 乘 1234567) 取余于 1000。
                嵌入向量 的 第 dim 项 为 强度 乘 (1000 减 phase) 除以 1000。
            完。
            k 为 k 加 1。
        完。
        j 为 j 加 1。
    完。
    声调 为 汉字码点 取余于 5。
    若 62 小于 嵌入维度：
        嵌入向量 的 第 62 项 为 声调 乘 200。
    完。
    若 63 小于 嵌入维度：
        可变 _cjk标记 为 0。
        若 汉字码点 大于或等于 CJK_下界 并且 汉字码点 小于或等于 CJK_上界：
            _cjk标记 为 1000。
        完。
        嵌入向量 的 第 63 项 为 _cjk标记。
    完。
    返还 嵌入向量。
完。

嵌入相似度 接受 向量A、向量B：
    若 做 计数 用 向量A 不等于 做 计数 用 向量B：
        返还 0。
    完。
    可变 dot 为 0。
    可变 normA 为 0。
    可变 normB 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 向量A：
        dot 为 dot 加 向量A 的 第 i 项 乘 向量B 的 第 i 项。
        normA 为 normA 加 向量A 的 第 i 项 乘 向量A 的 第 i 项。
        normB 为 normB 加 向量B 的 第 i 项 乘 向量B 的 第 i 项。
        i 为 i 加 1。
    完。
    若 normA 等于 0 或者 normB 等于 0：
        返还 0。
    完。
    返还 dot 乘 1000 除以 (做 整数sqrt 用 normA 乘 做 整数sqrt 用 normB 加 1)。
完。

验证部首感知嵌入 接受 零参：
    汪 为 做 汉字嵌入_初始化 用 27700 加 5。
    洋 为 做 汉字嵌入_初始化 用 27700 加 15。
    林 为 做 汉字嵌入_初始化 用 26408 加 3。
    汪洋相似 为 做 嵌入相似度 用 汪、洋。
    汪林相似 为 做 嵌入相似度 用 汪、林。
    返还 一组：汪洋相似 大于 汪林相似、汪洋相似、汪林相似。
完。

BPE_字对得分 接受 字A码点、字B码点、共现频率：
    嵌入A 为 做 汉字嵌入_初始化 用 字A码点。
    嵌入B 为 做 汉字嵌入_初始化 用 字B码点。
    语义相似 为 做 嵌入相似度 用 嵌入A、嵌入B。
    返还 共现频率 乘 (100 加 语义相似 除以 10) 除以 100。
完。

BPE_应否合并 接受 字A码点、字B码点、频率、频率阈值：
    若 频率 小于 频率阈值：
        返还 假。
    完。
    得分 为 做 BPE_字对得分 用 字A码点、字B码点、频率。
    返还 得分 大于或等于 频率阈值。
完。

嵌入矩阵_初始化 接受 词汇表：
    可变 嵌入矩阵 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词汇表：
        做 追加 用 嵌入矩阵、做 汉字嵌入_初始化 用 词汇表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 嵌入矩阵。
完。

批量笔画特征_8 接受 汉字码点列表：
    可变 笔画列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 汉字码点列表 并且 i 小于 8：
        码点 为 汉字码点列表 的 第 i 项。
        可变 笔画 为 码点 取余于 17 加 1。
        若 码点 等于 26085：
            笔画 为 4。
        否则若 码点 等于 26376：
            笔画 为 4。
        否则若 码点 等于 27700：
            笔画 为 4。
        否则若 码点 等于 26408：
            笔画 为 4。
        否则若 码点 等于 21916：
            笔画 为 10。
        否则若 码点 等于 20989：
            笔画 为 8。
        完。
        做 追加 用 笔画列表、笔画。
        i 为 i 加 1。
    完。
    返还 笔画列表。
完。

汉字语义距离_向量化 接受 码点A、码点B：
    可变 strokeA 为 码点A 取余于 17 加 1。
    若 码点A 等于 26085：
        strokeA 为 4。
    完。
    可变 strokeB 为 码点B 取余于 17 加 1。
    若 码点B 等于 26085：
        strokeB 为 4。
    完。
    可变 stroke_diff 为 strokeA 减 strokeB。
    若 strokeB 大于 strokeA：
        stroke_diff 为 strokeB 减 strokeA。
    完。
    stroke_sim 为 (10 减 stroke_diff) 乘 100 除以 10。
    可变 radical_diff 为 码点A 减 码点B。
    若 码点B 大于 码点A：
        radical_diff 为 码点B 减 码点A。
    完。
    可变 radical_sim 为 20。
    若 radical_diff 小于 200：
        radical_sim 为 80。
    完。
    返还 (stroke_sim 乘 40 加 radical_sim 乘 40 加 50 乘 20) 除以 100。
完。

汉字快速分类 接受 码点：
    若 码点 大于或等于 19968 并且 码点 小于或等于 40959：
        返还 4。
    完。
    若 码点 大于或等于 13312 并且 码点 小于或等于 19903：
        返还 6。
    完。
    若 码点 大于或等于 11904 并且 码点 小于或等于 12031：
        返还 1。
    完。
    返还 0。
完。

汉字认知距离 接受 字A笔画、字B笔画、同部首、声调差：
    可变 笔画差绝对 为 字A笔画 减 字B笔画。
    若 字B笔画 大于 字A笔画：
        笔画差绝对 为 字B笔画 减 字A笔画。
    完。
    可变 笔画相似 为 1000 减 笔画差绝对 乘 50。
    若 笔画差绝对 大于或等于 20：
        笔画相似 为 0。
    完。
    可变 部首相似 为 200。
    若 同部首：
        部首相似 为 1000。
    完。
    可变 声调相似 为 1000 减 声调差 乘 250。
    若 声调差 大于或等于 4：
        声调相似 为 0。
    完。
    返还 1000 减 (笔画相似 乘 400 加 部首相似 乘 400 加 声调相似 乘 200) 除以 1000。
完。

部首类型 接受 部首码点：
    若 部首码点 等于 27700 或者 部首码点 等于 27703：
        返还 1。
    完。
    若 部首码点 等于 26408 或者 部首码点 等于 26447：
        返还 2。
    完。
    若 部首码点 等于 20154 或者 部首码点 等于 20143：
        返还 3。
    完。
    若 部首码点 等于 24515 或者 部首码点 等于 24494：
        返还 4。
    完。
    若 部首码点 等于 37329：
        返还 5。
    完。
    若 部首码点 等于 21475：
        返还 6。
    完。
    若 部首码点 等于 26085：
        返还 7。
    完。
    若 部首码点 等于 28779 或者 部首码点 等于 28783：
        返还 8。
    完。
    返还 0。
完。

本章 收束。
