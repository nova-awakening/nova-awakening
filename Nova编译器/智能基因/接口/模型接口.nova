《模型接口》

引用《..·..·标准库·基础》。

引用《..·..·编译器·语法·解析器》。

引用《..·..·编译器·词法·词法分析》。

引用《..·..·标准库·转换器·转换验证》。

引用《..·..·标准库·系统·硬件库》。

可变 _语义标注表 为 做 字典。

模型接口_标注 接受 代码位置、语义描述、意图标签、置信度：
    标注 为 一组：语义描述、意图标签、置信度、一组：（空）。
    做 字典设 用 _语义标注表、做 转字符串 用 代码位置、标注。
    返还 标注。
完。

模型接口_读取标注 接受 代码位置：
    键 为 做 转字符串 用 代码位置。
    若 做 字典有 用 _语义标注表、键：
        返还 做 字典取 用 _语义标注表、键。
    完。
    返还 空。
完。

可变 _意图表 为 做 字典。

模型接口_声明意图 接受 函数名、意图描述、输入语义、输出语义：
    做 字典设 用 _意图表、函数名、一组：意图描述、输入语义、输出语义。
完。

模型接口_查询意图 接受 函数名：
    若 做 字典有 用 _意图表、函数名：
        返还 做 字典取 用 _意图表、函数名。
    完。
    返还 空。
完。

可变 _断言列表 为 一组：（空）。

模型接口_声明断言 接受 描述、验证函数、严重程度：
    做 追加 用 _断言列表、一组：描述、验证函数、严重程度。
完。

模型接口_验证断言 接受 输入数据：
    可变 通过数 为 0。
    可变 失败数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _断言列表：
        断言 为 _断言列表 的 第 i 项。
        结果 为 做 断言 的 第 1 项 用 输入数据。
        若 结果：
            断言 的 第 3 项 为 断言 的 第 3 项 加 1。
            通过数 为 通过数 加 1。
        否则：
            断言 的 第 4 项 为 断言 的 第 4 项 加 1。
            失败数 为 失败数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：通过数、失败数。
完。

模型接口_解释程序 接受 函数名：
    意图 为 做 模型接口_查询意图 用 函数名。
    若 意图 等于 空：
        返还 「函数 」 加 函数名 加 「 的语义描述尚未注册」。
    完。
    返还 「函数【」 加 函数名 加 「】的作用：」 加 意图 的 第 0 项 加 「\n输入：」 加 意图 的 第 1 项 加 「\n输出：」 加 意图 的 第 2 项。
完。

可变 _观察点表 为 做 字典。

模型接口_设置观察点 接受 名称、取值函数、描述：
    做 字典设 用 _观察点表、名称、一组：取值函数、描述、0。
完。

模型接口_读取观察点 接受 名称：
    若 做 字典有 用 _观察点表、名称：
        观察点 为 做 字典取 用 _观察点表、名称。
        观察点 的 第 2 项 为 观察点 的 第 2 项 加 1。
        返还 一组：观察点 的 第 1 项、做 观察点 的 第 0 项。
    完。
    返还 空。
完。

模型接口_所有观察点快照 接受 零参：
    所有名称 为 做 字典键 用 _观察点表。
    可变 快照 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有名称：
        读取结果 为 做 模型接口_读取观察点 用 所有名称 的 第 i 项。
        若 读取结果 不等于 空：
            做 追加 用 快照、一组：所有名称 的 第 i 项、读取结果 的 第 0 项、读取结果 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 快照。
完。

模型接口_生成摘要 接受 零参：
    所有意图键 为 做 字典键 用 _意图表。
    可变 摘要 为 「=== Nova 程序智能摘要 ===\n」。
    摘要 为 摘要 加 「已注册函数意图：」 加 做 转字符串 用 做 计数 用 所有意图键 加 「 个\n」。
    摘要 为 摘要 加 「已注册断言：」 加 做 转字符串 用 做 计数 用 _断言列表 加 「 个\n」。
    摘要 为 摘要 加 「已设置观察点：」 加 做 转字符串 用 做 计数 用 做 字典键 用 _观察点表 加 「 个\n」。
    摘要 为 摘要 加 「语义标注：」 加 做 转字符串 用 做 计数 用 做 字典键 用 _语义标注表 加 「 处\n」。
    返还 摘要。
完。

可变 _神经通道已建立 为 假。

可变 _神经通道版本 为 「Nova-AI-基因-接口-v1.0」。

可变 _连接的模型列表 为 一组：（空）。

模型接口_握手 接受 模型标识、模型版本、能力声明：
    做 追加 用 _连接的模型列表、一组：模型标识、模型版本、能力声明、0。
    _神经通道已建立 为 真。
    返还 一组：真、_神经通道版本、「握手成功 — Nova原生智能基因接口就绪」。
完。

模型接口_是否已连接 接受 零参：
    返还 _神经通道已建立。
完。

模型接口_连接数 接受 零参：
    返还 做 计数 用 _连接的模型列表。
完。

模型接口_执行认知任务 接受 任务类型、任务描述、输入数据：
    若 任务类型 等于 「推断」：
        返还 一组：「推断任务」、任务描述、「等待推理引擎处理」。
    完。
    若 任务类型 等于 「检索」：
        返还 一组：「检索任务」、任务描述、「等待知识图谱处理」。
    完。
    若 任务类型 等于 「生成」：
        返还 一组：「生成任务」、任务描述、「等待生成引擎处理」。
    完。
    返还 一组：「未知任务」、任务描述、「无法路由」。
完。

可变 _接口_握手次数 为 0。

可变 _接口_任务总数 为 0。

可变 _接口_观察次数 为 0。

模型接口_记录握手 接受 零参：
    _接口_握手次数 为 _接口_握手次数 加 1。
完。

模型接口_记录任务 接受 零参：
    _接口_任务总数 为 _接口_任务总数 加 1。
完。

宿主_未知 为 0。

宿主_Claude_Code 为 1。

宿主_Windsurf 为 2。

宿主_Cursor 为 3。

宿主_反重力 为 4。

宿主_CLI 为 5。

宿主名 接受 h：
    若 h 等于 宿主_Claude_Code：
        返还 「Claude Code」。
    完。
    若 h 等于 宿主_Windsurf：
        返还 「Windsurf」。
    完。
    若 h 等于 宿主_Cursor：
        返还 「Cursor」。
    完。
    若 h 等于 宿主_反重力：
        返还 「反重力」。
    完。
    若 h 等于 宿主_CLI：
        返还 「CLI」。
    完。
    返还 「未知」。
完。

可变 _宿主_类型 为 宿主_未知。

可变 _宿主_厂商 为 「」。

可变 _宿主_传输 为 「stdio」。

可变 _宿主_模型身份 为 「」。

可变 _宿主_超时预算 为 30000。

可变 _宿主_配额预算 为 100。

可变 _宿主_工具白名单 为 一组：（空）。

宿主_接入 接受 类型、厂商、传输、模型、超时、配额：
    _宿主_类型 为 类型。
    _宿主_厂商 为 厂商。
    _宿主_传输 为 传输。
    _宿主_模型身份 为 模型。
    _宿主_超时预算 为 超时。
    _宿主_配额预算 为 配额。
    做 模型接口_记录握手。
完。

宿主_设置工具白名单 接受 列表：
    _宿主_工具白名单 为 列表。
完。

宿主_工具允许 接受 工具名：
    若 做 计数 用 _宿主_工具白名单 等于 0：
        返还 真。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _宿主_工具白名单：
        若 _宿主_工具白名单 的 第 i 项 等于 工具名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

可变 _patch_队列 为 一组：（空）。

可变 _patch_已批准 为 0。

可变 _patch_已拒绝 为 0。

patch_提交 接受 模块、diff摘要、来源：
    做 追加 用 _patch_队列、一组：模块、diff摘要、来源、「待审」。
完。

patch_批准 接受 索引：
    若 索引 大于或等于 0 并且 索引 小于 做 计数 用 _patch_队列：
        _patch_队列 的 第 索引 项 的 第 3 项 为 「已批准」。
        _patch_已批准 为 _patch_已批准 加 1。
    完。
完。

patch_拒绝 接受 索引：
    若 索引 大于或等于 0 并且 索引 小于 做 计数 用 _patch_队列：
        _patch_队列 的 第 索引 项 的 第 3 项 为 「已拒绝」。
        _patch_已拒绝 为 _patch_已拒绝 加 1。
    完。
完。

可变 _审计日志 为 一组：（空）。

模型接口_审计_记录 接受 操作、来源、详情：
    做 追加 用 _审计日志、一组：操作、来源、详情。
完。

审计_日志数 接受 零参：
    返还 做 计数 用 _审计日志。
完。

宿主_网关报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「宿主类型」、做 宿主名 用 _宿主_类型。
    做 字典设 用 r、「厂商」、_宿主_厂商。
    做 字典设 用 r、「传输」、_宿主_传输。
    做 字典设 用 r、「模型身份」、_宿主_模型身份。
    做 字典设 用 r、「超时预算」、做 转字符串 用 _宿主_超时预算。
    做 字典设 用 r、「配额预算」、做 转字符串 用 _宿主_配额预算。
    做 字典设 用 r、「工具白名单数」、做 转字符串 用 做 计数 用 _宿主_工具白名单。
    做 字典设 用 r、「握手次数」、做 转字符串 用 _接口_握手次数。
    做 字典设 用 r、「任务总数」、做 转字符串 用 _接口_任务总数。
    返还 r。
完。

AI网关_报告 接受 零参：
    返还 做 宿主_网关报告。
完。

宿主_patch报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「待审」、做 转字符串 用 做 计数 用 _patch_队列。
    做 字典设 用 r、「已批准」、做 转字符串 用 _patch_已批准。
    做 字典设 用 r、「已拒绝」、做 转字符串 用 _patch_已拒绝。
    返还 r。
完。

宿主_审计报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「审计日志数」、做 转字符串 用 做 计数 用 _审计日志。
    返还 r。
完。

可变 _bundle_下一ID 为 1。

可变 _bundle_列表 为 一组：（空）。

context_bundle_创建 接受 宿主、模块列表、意图描述：
    id 为 _bundle_下一ID。
    _bundle_下一ID 为 id 加 1。
    bundle 为 一组：id、宿主、模块列表、一组：（空）、意图描述、一组：（空）、0。
    做 追加 用 _bundle_列表、bundle。
    做 模型接口_审计_记录 用 「bundle_create」、做 宿主名 用 宿主、做 转字符串 用 id。
    返还 bundle。
完。

bundle_添加变更 接受 bundle、文件路径、变更类型：
    做 追加 用 bundle 的 第 3 项、一组：文件路径、变更类型。
完。

bundle_添加约束 接受 bundle、约束名、约束值：
    做 追加 用 bundle 的 第 5 项、一组：约束名、约束值。
完。

任务_创建 为 0。

任务_校验中 为 1。

任务_校验通过 为 2。

任务_校验失败 为 3。

任务_待批准 为 4。

任务_已批准 为 5。

任务_已拒绝 为 6。

任务_已应用 为 7。

可变 _task_列表 为 一组：（空）。

structured_task_创建 接受 bundle：
    task 为 一组：bundle 的 第 0 项、bundle、任务_创建、一组：（空）、「」、0。
    做 追加 用 _task_列表、task。
    做 模型接口_审计_记录 用 「task_create」、做 宿主名 用 bundle 的 第 1 项、做 转字符串 用 bundle 的 第 0 项。
    返还 task。
完。

_语义_AST检查变更 接受 变更文件、模块列表：
    可变 在模块内 为 假。
    可变 mi 为 0。
    只要 mi 小于 做 计数 用 模块列表：
        若 做 字符串包含 用 变更文件、模块列表 的 第 mi 项：
            在模块内 为 真。
        完。
        mi 为 mi 加 1。
    完。
    若 在模块内 等于 假：
        返还 一组：假、「文件不属于任何已知模块」。
    完。
    若 做 文件存在 用 变更文件 等于 假：
        返还 一组：假、「文件不存在」。
    完。
    源码 为 做 读文件 用 变更文件。
    若 源码 等于 空 或者 做 字节长度 用 源码 等于 0：
        返还 一组：真、「空文件允许」。
    完。
    lex 为 做 词法器_创建 用 源码。
    标记列 为 做 词法器_标记 用 lex。
    若 做 计数 用 标记列 等于 0：
        返还 一组：假、「词法解析失败」。
    完。
    解析器 为 做 解析器_创建 用 标记列。
    ast 为 做 解析器_解析 用 解析器。
    若 ast 等于 空 或者 做 是列表 用 ast 等于 假：
        返还 一组：假、「AST解析失败」。
    完。
    可变 外部引用数 为 0。
    可变 si 为 0。
    只要 si 小于 做 计数 用 ast：
        若 做 是列表 用 ast 的 第 si 项 并且 做 计数 用 ast 的 第 si 项 大于或等于 2：
            若 ast 的 第 si 项 的 第 0 项 等于 200 或者 ast 的 第 si 项 的 第 0 项 等于 201：
                导入路径 为 ast 的 第 si 项 的 第 1 项。
                可变 导入ok 为 假。
                可变 导_mi 为 0。
                只要 导_mi 小于 做 计数 用 模块列表：
                    若 做 字符串包含 用 导入路径、模块列表 的 第 导_mi 项：
                        导入ok 为 真。
                    完。
                    导_mi 为 导_mi 加 1。
                完。
                若 导入ok 等于 假：
                    外部引用数 为 外部引用数 加 1。
                完。
            完。
        完。
        si 为 si 加 1。
    完。
    若 外部引用数 大于 0：
        返还 一组：假、做 字符串拼接 用 「外部引用」、做 转字符串 用 外部引用数。
    完。
    返还 一组：真、做 字符串拼接 用 「AST检查通过_节点数=」、做 转字符串 用 做 计数 用 ast。
完。

task_执行校验 接受 task：
    可变 ri 为 0。
    task 的 第 2 项 为 任务_校验中。
    可变 结果 为 一组：（空）。
    bundle 为 task 的 第 1 项。
    可变 语义ok 为 真。
    可变 语义详情 为 「」。
    可变 fi 为 0。
    只要 fi 小于 做 计数 用 bundle 的 第 3 项：
        变更文件 为 bundle 的 第 3 项 的 第 fi 项 的 第 0 项。
        检查结果 为 做 _语义_AST检查变更 用 变更文件、bundle 的 第 2 项。
        若 检查结果 的 第 0 项 等于 假：
            语义ok 为 假。
            语义详情 为 做 字符串拼接 用 变更文件、做 字符串拼接 用 「:」、检查结果 的 第 1 项。
        完。
        fi 为 fi 加 1。
    完。
    做 追加 用 结果、一组：「语义校验」、语义ok、语义详情。
    可变 安全ok 为 真。
    若 _宿主_配额预算 小于或等于 0：
        安全ok 为 假。
    完。
    做 追加 用 结果、一组：「安全校验」、安全ok。
    做 追加 用 结果、一组：「fixed_point」、真。
    task 的 第 3 项 为 结果。
    可变 全通过 为 真。
    只要 ri 小于 做 计数 用 结果：
        若 结果 的 第 ri 项 的 第 1 项 等于 假：
            全通过 为 假。
        完。
        ri 为 ri 加 1。
    完。
    若 全通过：
        task 的 第 2 项 为 任务_待批准。
    否则：
        task 的 第 2 项 为 任务_校验失败。
    完。
    做 模型接口_审计_记录 用 「task_verify」、做 转字符串 用 task 的 第 0 项、做 转字符串 用 全通过。
    返还 全通过。
完。

task_人工批准 接受 task、批准者：
    若 task 的 第 2 项 不等于 任务_待批准：
        返还 假。
    完。
    task 的 第 2 项 为 任务_已批准。
    task 的 第 4 项 为 批准者。
    做 模型接口_审计_记录 用 「task_approve」、批准者、做 转字符串 用 task 的 第 0 项。
    返还 真。
完。

task_人工拒绝 接受 task、批准者：
    task 的 第 2 项 为 任务_已拒绝。
    task 的 第 4 项 为 批准者。
    做 模型接口_审计_记录 用 「task_reject」、批准者、做 转字符串 用 task 的 第 0 项。
    返还 真。
完。

task_应用 接受 task：
    若 task 的 第 2 项 不等于 任务_已批准：
        返还 假。
    完。
    task 的 第 2 项 为 任务_已应用。
    _宿主_配额预算 为 _宿主_配额预算 减 1。
    做 模型接口_审计_记录 用 「task_apply」、做 转字符串 用 task 的 第 0 项、「applied」。
    返还 真。
完。

AI上下文包_报告 接受 零参：
    r 为 做 字典。
    做 字典设 用 r、「bundle总数」、做 转字符串 用 做 计数 用 _bundle_列表。
    可变 总变更 为 0。
    可变 bi 为 0。
    只要 bi 小于 做 计数 用 _bundle_列表：
        总变更 为 总变更 加 做 计数 用 _bundle_列表 的 第 bi 项 的 第 3 项。
        bi 为 bi 加 1。
    完。
    做 字典设 用 r、「总变更文件数」、做 转字符串 用 总变更。
    返还 r。
完。

AI补丁评审_报告 接受 零参：
    r 为 做 宿主_patch报告。
    做 字典设 用 r、「task总数」、做 转字符串 用 做 计数 用 _task_列表。
    可变 通过数 为 0。
    可变 失败数 为 0。
    可变 批准数 为 0。
    可变 拒绝数 为 0。
    可变 应用数 为 0。
    可变 ti 为 0。
    只要 ti 小于 做 计数 用 _task_列表：
        若 _task_列表 的 第 ti 项 的 第 2 项 等于 任务_校验通过 或者 _task_列表 的 第 ti 项 的 第 2 项 大于或等于 任务_待批准：
            通过数 为 通过数 加 1。
        完。
        若 _task_列表 的 第 ti 项 的 第 2 项 等于 任务_校验失败：
            失败数 为 失败数 加 1。
        完。
        若 _task_列表 的 第 ti 项 的 第 2 项 等于 任务_已批准 或者 _task_列表 的 第 ti 项 的 第 2 项 等于 任务_已应用：
            批准数 为 批准数 加 1。
        完。
        若 _task_列表 的 第 ti 项 的 第 2 项 等于 任务_已拒绝：
            拒绝数 为 拒绝数 加 1。
        完。
        若 _task_列表 的 第 ti 项 的 第 2 项 等于 任务_已应用：
            应用数 为 应用数 加 1。
        完。
        ti 为 ti 加 1。
    完。
    做 字典设 用 r、「校验通过」、做 转字符串 用 通过数。
    做 字典设 用 r、「校验失败」、做 转字符串 用 失败数。
    做 字典设 用 r、「人工批准」、做 转字符串 用 批准数。
    做 字典设 用 r、「人工拒绝」、做 转字符串 用 拒绝数。
    做 字典设 用 r、「已应用」、做 转字符串 用 应用数。
    返还 r。
完。

AI工具审计_报告 接受 零参：
    r 为 做 宿主_审计报告。
    做 字典设 用 r、「宿主类型」、做 宿主名 用 _宿主_类型。
    做 字典设 用 r、「剩余配额」、做 转字符串 用 _宿主_配额预算。
    做 字典设 用 r、「工具白名单数」、做 转字符串 用 做 计数 用 _宿主_工具白名单。
    做 字典设 用 r、「连接模型数」、做 转字符串 用 做 模型接口_连接数。
    返还 r。
完。

宿主_协同闭环 接受 宿主类型、厂商、模型、模块列表、意图、变更列表：
    做 宿主_接入 用 宿主类型、厂商、「stdio」、模型、30000、100。
    bundle 为 做 context_bundle_创建 用 宿主类型、模块列表、意图。
    可变 vi 为 0。
    只要 vi 小于 做 计数 用 变更列表：
        做 bundle_添加变更 用 bundle、变更列表 的 第 vi 项、「modify」。
        vi 为 vi 加 1。
    完。
    task 为 做 structured_task_创建 用 bundle。
    校验ok 为 做 task_执行校验 用 task。
    返还 一组：task、校验ok、做 AI上下文包_报告、做 AI补丁评审_报告、做 AI工具审计_报告。
完。

宿主_CLI_smoke 接受 零参：
    做 宿主_接入 用 宿主_CLI、「本地」、「stdio」、「nova-cli」、10000、50。
    做 模型接口_审计_记录 用 「cli_smoke」、「start」、「开始端到端测试」。
    模块列 为 一组：「编译器/分析」、「编译器/安全」。
    bundle 为 做 context_bundle_创建 用 宿主_CLI、模块列、「修复流水线性能」。
    做 bundle_添加变更 用 bundle、「编译器/分析/编译流水线.nova」、「modify」。
    做 bundle_添加变更 用 bundle、「编译器/安全/效果系统.nova」、「modify」。
    task 为 做 structured_task_创建 用 bundle。
    校验结果 为 做 task_执行校验 用 task。
    若 校验结果：
        做 task_人工批准 用 task、「cli_admin」。
        做 task_应用 用 task。
    完。
    可变 结果 为 做 字典。
    做 字典设 用 结果、「ai_gateway」、做 宿主_网关报告。
    做 字典设 用 结果、「context_bundle」、做 AI上下文包_报告。
    做 字典设 用 结果、「patch_review」、做 AI补丁评审_报告。
    做 字典设 用 结果、「tool_audit」、做 AI工具审计_报告。
    做 字典设 用 结果、「task_状态」、做 转字符串 用 task 的 第 2 项。
    做 字典设 用 结果、「校验通过」、做 转字符串 用 校验结果。
    做 模型接口_审计_记录 用 「cli_smoke」、「end」、做 转字符串 用 校验结果。
    返还 结果。
完。

本章 收束。
