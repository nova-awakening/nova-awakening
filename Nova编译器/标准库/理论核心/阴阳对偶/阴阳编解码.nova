《阴阳编解码》

引用《..·..·基础》。

阴阳规则_创建 接受 名称：
    返还 一组：名称、一组：（空）、一组：（空）、一组：（空）。
完。

阴阳规则_添加映射 接受 规则、值、码：
    做 追加 用 规则 的 第 1 项、值。
    做 追加 用 规则 的 第 2 项、码。
完。

阴阳规则_阳 接受 规则、值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 规则 的 第 1 项：
        若 规则 的 第 1 项 的 第 i 项 等于 值：
            返还 规则 的 第 2 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

阴阳规则_阴 接受 规则、码：
    可变 i 为 0。
    只要 i 小于 做 计数 用 规则 的 第 2 项：
        若 规则 的 第 2 项 的 第 i 项 等于 码：
            返还 规则 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

阴阳规则_验证对偶 接受 规则：
    可变 i 为 0。
    只要 i 小于 做 计数 用 规则 的 第 1 项：
        码 为 做 阴阳规则_阳 用 规则、规则 的 第 1 项 的 第 i 项。
        还原 为 做 阴阳规则_阴 用 规则、码。
        若 还原 不等于 规则 的 第 1 项 的 第 i 项：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

阴阳_编码整数 接受 值：
    可变 字节 为 一组：（空）。
    可变 v 为 值。
    若 v 小于 0：
        v 为 0 减 v。
    完。
    做 追加 用 字节、v 取余于 256。
    做 追加 用 字节、v 除以 256 取余于 256。
    做 追加 用 字节、v 除以 65536 取余于 256。
    做 追加 用 字节、v 除以 16777216 取余于 256。
    若 值 小于 0：
        做 追加 用 字节、1。
    否则：
        做 追加 用 字节、0。
    完。
    返还 字节。
完。

阴阳_解码整数 接受 字节：
    若 做 计数 用 字节 小于 5：
        返还 0。
    完。
    可变 v 为 字节 的 第 0 项 加 字节 的 第 1 项 乘 256 加 字节 的 第 2 项 乘 65536 加 字节 的 第 3 项 乘 16777216。
    若 字节 的 第 4 项 等于 1：
        v 为 0 减 v。
    完。
    返还 v。
完。

阴阳_编码字符串 接受 文本：
    长 为 做 字节长度 用 文本。
    可变 字节 为 做 阴阳_编码整数 用 长。
    可变 i 为 0。
    只要 i 小于 长：
        做 追加 用 字节、做 字节取 用 文本、i。
        i 为 i 加 1。
    完。
    返还 字节。
完。

阴阳_解码字符串 接受 字节、偏移：
    可变 长度字节 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 5：
        做 追加 用 长度字节、字节 的 第 偏移 加 i 项。
        i 为 i 加 1。
    完。
    长 为 做 阴阳_解码整数 用 长度字节。
    可变 结果 为 「」。
    i 为 0。
    只要 i 小于 长：
        结果 为 做 字符串拼接 用 结果、做 字符 用 字节 的 第 偏移 加 5 加 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

阴阳_编码列表 接受 列表：
    可变 字节 为 做 阴阳_编码整数 用 做 计数 用 列表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        项字节 为 做 阴阳_编码整数 用 列表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 项字节：
            做 追加 用 字节、项字节 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 字节。
完。

阴阳_解码列表 接受 字节：
    可变 头字节 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 5：
        做 追加 用 头字节、字节 的 第 k 项。
        k 为 k 加 1。
    完。
    n 为 做 阴阳_解码整数 用 头字节。
    可变 结果 为 一组：（空）。
    可变 偏移 为 5。
    可变 i 为 0。
    只要 i 小于 n：
        可变 项字节 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 5：
            做 追加 用 项字节、字节 的 第 偏移 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、做 阴阳_解码整数 用 项字节。
        偏移 为 偏移 加 5。
        i 为 i 加 1。
    完。
    返还 结果。
完。

阴阳_RLE压缩 接受 数据：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        值 为 数据 的 第 i 项。
        可变 计 为 1。
        只要 i 加 计 小于 做 计数 用 数据 并且 数据 的 第 i 加 计 项 等于 值 并且 计 小于 255：
            计 为 计 加 1。
        完。
        做 追加 用 结果、计。
        做 追加 用 结果、值。
        i 为 i 加 计。
    完。
    返还 结果。
完。

阴阳_RLE解压 接受 压缩数据：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 1 小于 做 计数 用 压缩数据：
        计 为 压缩数据 的 第 i 项。
        值 为 压缩数据 的 第 i 加 1 项。
        可变 j 为 0。
        只要 j 小于 计：
            做 追加 用 结果、值。
            j 为 j 加 1。
        完。
        i 为 i 加 2。
    完。
    返还 结果。
完。

阴阳_哈希 接受 数据：
    可变 h 为 2166136261。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        h 为 h 加 数据 的 第 i 项。
        h 为 h 乘 16777619。
        h 为 h 取余于 4294967296。
        i 为 i 加 1。
    完。
    返还 h。
完。

阴阳_验证哈希 接受 数据、期望哈希：
    返还 做 阴阳_哈希 用 数据 等于 期望哈希。
完。

阴阳_差分编码 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 结果、数据 的 第 i 项 减 数据 的 第 i 减 1 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

阴阳_差分解码 接受 差分数据：
    若 做 计数 用 差分数据 等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：差分数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 差分数据：
        做 追加 用 结果、结果 的 第 i 减 1 项 加 差分数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

阴阳_VarInt编码 接受 值：
    可变 结果 为 一组：（空）。
    可变 v 为 值。
    只要 v 大于或等于 128：
        做 追加 用 结果、v 取余于 128 加 128。
        v 为 v 除以 128。
    完。
    做 追加 用 结果、v。
    返还 结果。
完。

阴阳_VarInt解码 接受 数据、起始：
    可变 结果 为 0。
    可变 移位 为 0。
    可变 i 为 起始。
    只要 i 小于 做 计数 用 数据：
        字节 为 数据 的 第 i 项。
        结果 为 结果 加 字节 取余于 128 乘 (1 左移 移位)。
        若 字节 小于 128：
            返还 一组：结果、i 加 1。
        完。
        移位 为 移位 加 7。
        i 为 i 加 1。
    完。
    返还 一组：结果、i。
完。

阴阳_校验和 接受 数据：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        和 为 和 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和 取余于 256。
完。

阴阳_CRC8 接受 数据：
    可变 校验值 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        校验值 为 校验值 加 数据 的 第 i 项。
        校验值 为 (校验值 乘 31 加 17) 取余于 256。
        i 为 i 加 1。
    完。
    返还 校验值。
完。

阴阳_交织 接受 阴数据、阳数据：
    可变 结果 为 一组：（空）。
    n 为 做 计数 用 阴数据。
    m 为 做 计数 用 阳数据。
    可变 i 为 0。
    可变 j 为 0。
    只要 i 小于 n 或者 j 小于 m：
        若 i 小于 n：
            做 追加 用 结果、阴数据 的 第 i 项。
            i 为 i 加 1。
        完。
        若 j 小于 m：
            做 追加 用 结果、阳数据 的 第 j 项。
            j 为 j 加 1。
        完。
    完。
    返还 结果。
完。

阴阳_解交织 接受 数据：
    可变 阴 为 一组：（空）。
    可变 阳 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 i 取余于 2 等于 0：
            做 追加 用 阴、数据 的 第 i 项。
        否则：
            做 追加 用 阳、数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：阴、阳。
完。

可变 _编解码_编码数 为 0。

可变 _编解码_解码数 为 0。

编解码_记录编码 接受 零参：
    _编解码_编码数 为 _编解码_编码数 加 1。
完。

编解码_记录解码 接受 零参：
    _编解码_解码数 为 _编解码_解码数 加 1。
完。

本章 收束。
