《线性把字语义》

引用《..·..·基础》。

资源_活跃 为 0。

资源_已消费 为 1。

资源_已借出 为 2。

资源_已转移 为 3。

资源_可复用 为 4。

资源_创建 接受 id、值、所有者：
    返还 一组：id、资源_活跃、所有者、一组：（空）、值。
完。

资源_id 接受 r：
    返还 r 的 第 0 项。
完。

资源_状态 接受 r：
    返还 r 的 第 1 项。
完。

资源_所有者 接受 r：
    返还 r 的 第 2 项。
完。

资源_借用者 接受 r：
    返还 r 的 第 3 项。
完。

资源_值 接受 r：
    返还 r 的 第 4 项。
完。

把 接受 资源、动作名：
    若 做 资源_状态 用 资源 等于 资源_已消费：
        返还 一组：「错误」、「把字语义违规: 资源已被消费, 不可再'把'」。
    完。
    若 做 资源_状态 用 资源 等于 资源_已转移：
        返还 一组：「错误」、「把字语义违规: 资源已转移, 不可再'把'」。
    完。
    若 做 资源_状态 用 资源 等于 资源_已借出：
        返还 一组：「错误」、「把字语义违规: 资源正被借出, 不可消费」。
    完。
    资源 的 第 1 项 为 资源_已消费。
    资源 的 第 6 项 为 动作名。
    返还 一组：「成功」、做 资源_值 用 资源。
完。

被 接受 资源、动作名：
    若 做 资源_状态 用 资源 等于 资源_已消费：
        返还 一组：「错误」、「被字语义违规: 资源已被消费」。
    完。
    若 做 资源_状态 用 资源 等于 资源_已转移：
        返还 一组：「错误」、「被字语义违规: 资源已转移」。
    完。
    做 追加 用 资源 的 第 3 项、动作名。
    返还 一组：「成功」、做 资源_值 用 资源。
完。

给 接受 资源、目标：
    若 做 资源_状态 用 资源 等于 资源_已消费：
        返还 一组：「错误」、「给字语义违规: 资源已被消费, 不可复制」。
    完。
    副本 为 做 资源_创建 用 做 字符串拼接 用 做 转字符串 用 做 资源_id 用 资源、「_副本」、做 资源_值 用 资源、目标。
    副本 的 第 1 项 为 资源_可复用。
    返还 一组：「成功」、副本。
完。

让 接受 资源、新所有者：
    若 做 资源_状态 用 资源 不等于 资源_活跃：
        返还 一组：「错误」、「让字语义违规: 只有活跃资源可以转移所有权」。
    完。
    若 做 计数 用 做 资源_借用者 用 资源 大于 0：
        返还 一组：「错误」、「让字语义违规: 资源还有借用者, 不可转移」。
    完。
    资源 的 第 2 项 为 新所有者。
    返还 一组：「成功」、资源。
完。

线性上下文_创建 接受 零参：
    返还 一组：一组：（空）、0。
完。

线性上下文_分配 接受 上下文、值、所有者：
    id 为 上下文 的 第 1 项。
    上下文 的 第 1 项 为 上下文 的 第 1 项 加 1。
    资源 为 做 资源_创建 用 id、值、所有者。
    做 追加 用 上下文 的 第 0 项、资源。
    返还 资源。
完。

线性上下文_检查 接受 上下文：
    可变 泄漏 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 上下文 的 第 0 项：
        r 为 上下文 的 第 0 项 的 第 i 项。
        若 做 资源_状态 用 r 等于 资源_活跃：
            做 追加 用 泄漏、做 资源_id 用 r。
        完。
        i 为 i 加 1。
    完。
    返还 泄漏。
完。

可变 消费动词表 为 一组：（空）。

可变 借用动词表 为 一组：（空）。

可变 复制动词表 为 一组：（空）。

线性语义_初始化 接受 零参：
    消费动词表 为 一组：「关闭」、「删除」、「释放」、「销毁」、「发送」、「写入」、「提交」。
    借用动词表 为 一组：「读取」、「查看」、「检查」、「遍历」、「搜索」、「比较」、「打印」。
    复制动词表 为 一组：「复制」、「克隆」、「序列化」、「备份」、「缓存」、「广播」。
完。

推导_动词模式 接受 动词：
    可变 i 为 0。
    只要 i 小于 做 计数 用 消费动词表：
        若 消费动词表 的 第 i 项 等于 动词：
            返还 「把」。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 借用动词表：
        若 借用动词表 的 第 i 项 等于 动词：
            返还 「被」。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 复制动词表：
        若 复制动词表 的 第 i 项 等于 动词：
            返还 「给」。
        完。
        i 为 i 加 1。
    完。
    返还 「个」。
完。

线性证明_创建 接受 命题：
    返还 一组：命题、一组：（空）、1。
完。

线性证明_添加步骤 接受 证明、步骤：
    做 追加 用 证明 的 第 1 项、步骤。
完。

线性证明_标记失败 接受 证明、原因：
    证明 的 第 2 项 为 0。
    做 追加 用 证明 的 第 1 项、做 字符串拼接 用 「失败: 」、原因。
完。

线性证明_有效 接受 证明：
    返还 证明 的 第 2 项。
完。

验证线性性 接受 操作序列：
    证明 为 做 线性证明_创建 用 「操作序列线性性」。
    上下文 为 做 线性上下文_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 操作序列：
        操作 为 操作序列 的 第 i 项。
        类型 为 操作 的 第 0 项。
        若 类型 等于 「创建」：
            做 线性上下文_分配 用 上下文、操作 的 第 1 项、操作 的 第 2 项。
            做 线性证明_添加步骤 用 证明、「创建资源」。
        完。
        若 类型 等于 「消费」：
            做 线性证明_添加步骤 用 证明、「消费资源(把字)」。
        完。
        i 为 i 加 1。
    完。
    泄漏 为 做 线性上下文_检查 用 上下文。
    若 做 计数 用 泄漏 大于 0：
        做 线性证明_标记失败 用 证明、「存在未消费的资源」。
    完。
    返还 证明。
完。

本章 收束。
