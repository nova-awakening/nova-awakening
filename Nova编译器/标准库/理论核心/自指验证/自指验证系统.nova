《自指验证系统》

引用《..·..·基础》。

见证_未验证 为 0。

见证_已验证 为 1。

见证_已反驳 为 2。

见证_创建 接受 命题、类型、数据：
    返还 一组：命题、类型、数据、0、见证_未验证。
完。

见证_命题 接受 w：
    返还 w 的 第 0 项。
完。

见证_类型 接受 w：
    返还 w 的 第 1 项。
完。

见证_数据 接受 w：
    返还 w 的 第 2 项。
完。

见证_状态 接受 w：
    返还 w 的 第 4 项。
完。

见证_验证通过 接受 w：
    w 的 第 4 项 为 见证_已验证。
完。

见证_反驳 接受 w：
    w 的 第 4 项 为 见证_已反驳。
完。

不动点_检查 接受 函数ref、输入、比较函数：
    输出 为 做 函数ref 用 输入、空。
    若 做 比较函数 用 输入、输出 等于 1：
        返还 做 见证_创建 用 「不动点」、「构造性」、一组：输入、输出。
    完。
    返还 空。
完。

自指验证_不动点_迭代 接受 函数ref、初始、比较函数、最大迭代：
    可变 当前 为 初始。
    可变 i 为 0。
    只要 i 小于 最大迭代：
        下一个 为 做 函数ref 用 当前、空。
        若 做 比较函数 用 当前、下一个 等于 1：
            w 为 做 见证_创建 用 「迭代不动点」、「构造性」、一组：当前、i。
            做 见证_验证通过 用 w。
            返还 w。
        完。
        当前 为 下一个。
        i 为 i 加 1。
    完。
    返还 空。
完。

自举协议_创建 接受 零参：
    返还 一组：空、空、空、0。
完。

自举协议_设stage 接受 协议、阶段、结果：
    协议 的 第 阶段 项 为 结果。
完。

自举协议_验证 接受 协议：
    若 协议 的 第 1 项 等于 空 或者 协议 的 第 2 项 等于 空：
        返还 0。
    完。
    s1 为 协议 的 第 1 项。
    s2 为 协议 的 第 2 项。
    若 做 计数 用 s1 不等于 做 计数 用 s2：
        协议 的 第 3 项 为 负 1。
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 s1：
        若 s1 的 第 i 项 不等于 s2 的 第 i 项：
            协议 的 第 3 项 为 i。
            返还 0。
        完。
        i 为 i 加 1。
    完。
    协议 的 第 3 项 为 负 2。
    返还 1。
完。

证明步骤_创建 接受 动作、输入、输出、断言：
    返还 一组：动作、输入、输出、断言。
完。

构造性证明_创建 接受 命题：
    返还 一组：命题、一组：（空）、空。
完。

构造性证明_添加步骤 接受 证明、动作、输入、输出、断言：
    做 追加 用 证明 的 第 1 项、做 证明步骤_创建 用 动作、输入、输出、断言。
完。

构造性证明_完成 接受 证明、见证：
    证明 的 第 2 项 为 见证。
完。

构造性证明_有效 接受 证明：
    返还 证明 的 第 2 项 不等于 空。
完。

语义等价_测试 接受 程序A、程序B、测试输入：
    可变 全部通过 为 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 测试输入：
        输出A 为 做 程序A 用 测试输入 的 第 i 项、空。
        输出B 为 做 程序B 用 测试输入 的 第 i 项、空。
        一致 为 输出A 等于 输出B。
        做 追加 用 结果、一组：测试输入 的 第 i 项、输出A、输出B、一致。
        若 一致 等于 0：
            全部通过 为 0。
        完。
        i 为 i 加 1。
    完。
    若 全部通过 等于 1：
        w 为 做 见证_创建 用 「语义等价」、「测试」、结果。
        做 见证_验证通过 用 w。
        返还 w。
    完。
    返还 做 见证_创建 用 「语义不等价」、「反例」、结果。
完。

自指层_创建 接受 层号、名称、类型：
    返还 一组：层号、名称、类型、空、一组：（空）。
完。

自指层_添加见证 接受 层、见证：
    做 追加 用 层 的 第 4 项、见证。
完。

自指层_全部有效 接受 层：
    可变 i 为 0。
    只要 i 小于 做 计数 用 层 的 第 4 项：
        若 做 见证_状态 用 层 的 第 4 项 的 第 i 项 不等于 见证_已验证：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

自指塔_创建 接受 零参：
    可变 塔 为 一组：（空）。
    做 追加 用 塔、做 自指层_创建 用 0、「数据自指」、「递归结构」。
    做 追加 用 塔、做 自指层_创建 用 1、「函数自指」、「不动点」。
    做 追加 用 塔、做 自指层_创建 用 2、「编译器自指」、「自举」。
    做 追加 用 塔、做 自指层_创建 用 3、「验证器自指」、「构造性见证」。
    返还 塔。
完。

指纹_计算 接受 字节列表：
    可变 h 为 2166136261。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字节列表：
        h 为 h 加 字节列表 的 第 i 项。
        h 为 h 乘 16777619。
        h 为 h 取余于 4294967296。
        i 为 i 加 1。
    完。
    返还 h。
完。

指纹_匹配 接受 a字节、b字节：
    返还 做 指纹_计算 用 a字节 等于 做 指纹_计算 用 b字节。
完。

不变量_检查表 接受 零参：
    可变 检查项 为 一组：（空）。
    做 追加 用 检查项、「函数数量一致」。
    做 追加 用 检查项、「字符串表一致」。
    做 追加 用 检查项、「代码段大小一致」。
    做 追加 用 检查项、「入口点偏移一致」。
    做 追加 用 检查项、「PE头一致」。
    做 追加 用 检查项、「段对齐一致」。
    返还 检查项。
完。

本章 收束。
