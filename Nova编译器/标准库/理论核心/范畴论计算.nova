《范畴论计算》

引用《..·基础》。

CAT_精度 为 10000。

范畴论计_范畴_创建 接受 名称：
    返还 一组：名称、一组：（空）、一组：（空）、一组：（空）。
完。

范畴_添加对象 接受 范畴、对象：
    做 追加 用 范畴 的 第 1 项、对象。
    做 追加 用 范畴 的 第 3 项、一组：对象、对象、「id」、空。
完。

范畴_添加态射 接受 范畴、源、目标、标签：
    做 追加 用 范畴 的 第 2 项、一组：源、目标、标签、空。
完。

态射_组合 接受 f、g：
    若 f 的 第 1 项 不等于 g 的 第 0 项：
        返还 空。
    完。
    返还 一组：f 的 第 0 项、g 的 第 1 项、f 的 第 2 项 加 「;」 加 g 的 第 2 项、空。
完。

验证_结合律 接受 f、g、h：
    左 为 做 态射_组合 用 做 态射_组合 用 f、g、h。
    右 为 做 态射_组合 用 f、做 态射_组合 用 g、h。
    若 左 等于 空 或者 右 等于 空：
        返还 0。
    完。
    返还 左 的 第 0 项 等于 右 的 第 0 项 并且 左 的 第 1 项 等于 右 的 第 1 项。
完。

范畴论计_函子_创建 接受 名称、源范畴、目标范畴：
    返还 一组：名称、源范畴、目标范畴、一组：（空）、一组：（空）。
完。

范畴论计_函子_映射对象 接受 函子、源对象、目标对象：
    做 追加 用 函子 的 第 3 项、一组：源对象、目标对象。
完。

范畴论计_函子_映射态射 接受 函子、源态射、目标态射：
    做 追加 用 函子 的 第 4 项、一组：源态射、目标态射。
完。

函子_验证 接受 函子：
    源对象数 为 做 计数 用 函子 的 第 1 项 的 第 1 项。
    映射数 为 做 计数 用 函子 的 第 3 项。
    若 映射数 小于 源对象数：
        有效 为 0。
    完。
    返还 有效。
完。

自然变换_创建 接受 名称、函子F、函子G：
    返还 一组：名称、函子F、函子G、一组：（空）。
完。

自然变换_添加分量 接受 变换、对象、态射：
    做 追加 用 变换 的 第 3 项、一组：对象、态射。
完。

单子_创建 接受 名称：
    返还 一组：名称、空、空、空。
完。

可能_单位 接受 值：
    返还 一组：1、值。
完。

可能_无 接受 零参：
    返还 一组：0、0。
完。

可能_绑定 接受 ma、f：
    若 ma 的 第 0 项 等于 0：
        返还 做 可能_无。
    完。
    返还 f。
完。

Maybe_是有值 接受 ma：
    返还 ma 的 第 0 项 等于 1。
完。

Either_左 接受 错误：
    返还 一组：0、错误。
完。

Either_右 接受 值：
    返还 一组：1、值。
完。

选择_绑定 接受 ea、成功处理：
    若 ea 的 第 0 项 等于 0：
        返还 ea。
    完。
    返还 成功处理。
完。

状态_单位 接受 值：
    返还 一组：值、空。
完。

状态_获取 接受 零参：
    返还 一组：「get」、空。
完。

状态_设置 接受 新状态：
    返还 一组：空、新状态。
完。

积_创建 接受 a、b：
    返还 一组：a、b。
完。

积_投影1 接受 积：
    返还 积 的 第 0 项。
完。

积_投影2 接受 积：
    返还 积 的 第 1 项。
完。

余积_创建 接受 标签、值：
    返还 一组：标签、值。
完。

余积_注入1 接受 a：
    返还 做 余积_创建 用 0、a。
完。

余积_注入2 接受 b：
    返还 做 余积_创建 用 1、b。
完。

余积_匹配 接受 cp、处理1、处理2：
    若 cp 的 第 0 项 等于 0：
        返还 处理1。
    完。
    返还 处理2。
完。

指数_创建 接受 域、值域：
    返还 一组：域、值域、一组：（空）。
完。

指数_添加态射 接受 指数、输入、输出：
    做 追加 用 指数 的 第 2 项、一组：输入、输出。
完。

指数_求值 接受 指数、输入：
    可变 i 为 0。
    只要 i 小于 做 计数 用 指数 的 第 2 项：
        若 指数 的 第 2 项 的 第 i 项 的 第 0 项 等于 输入：
            返还 指数 的 第 2 项 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

Yoneda_嵌入 接受 对象、所有态射：
    可变 表示 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有态射：
        若 所有态射 的 第 i 项 的 第 0 项 等于 对象：
            做 追加 用 表示、所有态射 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 表示。
完。

Yoneda_同构 接受 表示A、表示B：
    若 做 计数 用 表示A 不等于 做 计数 用 表示B：
        返还 0。
    完。
    返还 1。
完。

编译Pass_创建 接受 名称、源IR、目标IR：
    返还 一组：源IR、目标IR、名称、空。
完。

编译Pass_组合 接受 pass1、pass2：
    返还 做 态射_组合 用 pass1、pass2。
完。

验证_语义保持 接受 原始、优化后、测试输入列表：
    可变 通过 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 测试输入列表：
        若 原始 等于 优化后：
            通过 为 通过 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 通过 乘 CAT_精度 除以 (做 计数 用 测试输入列表 加 1)。
完。

伴随函子_创建 接受 F函子、G函子、名称：
    返还 一组：F函子、G函子、空、空、名称。
完。

伴随函子_设置单位 接受 伴随、η函数：
    伴随 的 第 2 项 为 η函数。
    返还 伴随。
完。

伴随函子_设置余单位 接受 伴随、ε函数：
    伴随 的 第 3 项 为 ε函数。
    返还 伴随。
完。

伴随_柯里化 接受 f_AB_to_C、固定B：
    返还 一组：f_AB_to_C、固定B。
完。

伴随_反柯里化 接受 g_A_to_CB、输入A、输入B：
    返还 一组：g_A_to_CB、输入A、输入B。
完。

自由单子_Pure 接受 值：
    返还 一组：0、值。
完。

自由单子_Roll 接受 函子层：
    返还 一组：1、函子层。
完。

自由单子_是Pure 接受 m：
    返还 m 的 第 0 项 等于 0。
完。

自由单子_展开 接受 m：
    返还 m 的 第 1 项。
完。

自由单子_折叠 接受 自然变换、自由值、单子bind：
    若 做 自由单子_是Pure 用 自由值：
        返还 做 自由单子_展开 用 自由值。
    完。
    返还 一组：自然变换、做 自由单子_展开 用 自由值、单子bind。
完。

Kan扩展_左_离散 接受 F_值列表、K_映射、目标对象、合并函数：
    n 为 做 计数 用 F_值列表。
    可变 收集 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 K_映射 的 第 i 项 等于 目标对象：
            做 追加 用 收集、F_值列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 收集 等于 0：
        返还 0。
    完。
    可变 结果 为 收集 的 第 0 项。
    i 为 1。
    只要 i 小于 做 计数 用 收集：
        结果 为 结果 加 收集 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

Kan扩展_右_离散 接受 F_值列表、K_映射、目标对象：
    n 为 做 计数 用 F_值列表。
    可变 收集 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 K_映射 的 第 i 项 等于 目标对象：
            做 追加 用 收集、F_值列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 收集 等于 0：
        返还 CAT_精度。
    完。
    可变 结果 为 收集 的 第 0 项。
    i 为 1。
    只要 i 小于 做 计数 用 收集：
        若 收集 的 第 i 项 小于 结果：
            结果 为 收集 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

Lens_创建 接受 获取函数、设置函数：
    返还 一组：获取函数、设置函数。
完。

Lens_组合 接受 外层lens、内层lens：
    返还 一组：外层lens、内层lens。
完。

Prism_创建 接受 匹配函数、构建函数：
    返还 一组：匹配函数、构建函数。
完。

Traversal_映射 接受 列表、变换函数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、做 变换函数 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

Iso_创建 接受 正向、逆向：
    返还 一组：正向、逆向。
完。

Iso_逆转 接受 iso：
    返还 一组：iso 的 第 1 项、iso 的 第 0 项。
完。

AST_Lens_索引 接受 索引：
    返还 一组：索引、0。
完。

AST_Lens_应用 接受 lens_chain、节点、新值：
    索引 为 lens_chain 的 第 0 项。
    若 做 是列表 用 节点 并且 索引 小于 做 计数 用 节点：
        可变 新节点 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 节点：
            若 i 等于 索引：
                做 追加 用 新节点、新值。
            否则：
                做 追加 用 新节点、节点 的 第 i 项。
            完。
            i 为 i 加 1。
        完。
        返还 新节点。
    完。
    返还 节点。
完。

本章 收束。
