《五行算法框架》

引用《..·..·哈希表·哈希表》。

引用《..·..·基础》。

木_生成列表 接受 大小、生成函数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        做 追加 用 结果、做 生成函数 用 i、空。
        i 为 i 加 1。
    完。
    返还 结果。
完。

木_构建树 接受 值列表：
    若 做 计数 用 值列表 等于 0：
        返还 空。
    完。
    根 为 一组：值列表 的 第 0 项、空、空。
    可变 i 为 1。
    只要 i 小于 做 计数 用 值列表：
        做 木_树插入 用 根、值列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 根。
完。

木_树插入 接受 节点、值：
    若 值 小于 节点 的 第 0 项：
        若 节点 的 第 1 项 等于 空：
            节点 的 第 1 项 为 一组：值、空、空。
        否则：
            做 木_树插入 用 节点 的 第 1 项、值。
        完。
    否则：
        若 节点 的 第 2 项 等于 空：
            节点 的 第 2 项 为 一组：值、空、空。
        否则：
            做 木_树插入 用 节点 的 第 2 项、值。
        完。
    完。
完。

木_构建图 接受 边列表、节点数：
    可变 邻接表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点数：
        做 追加 用 邻接表、一组：（空）。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 边列表：
        边 为 边列表 的 第 i 项。
        做 追加 用 邻接表 的 第 边 的 第 0 项 项、边 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 邻接表。
完。

火_映射 接受 列表、变换函数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、做 变换函数 用 列表 的 第 i 项、空。
        i 为 i 加 1。
    完。
    返还 结果。
完。

火_过滤 接受 列表、谓词函数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 谓词函数 用 列表 的 第 i 项、空 等于 1：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

火_归约 接受 列表、归约函数、初始值：
    可变 累计 为 初始值。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        累计 为 做 归约函数 用 累计、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 累计。
完。

火_快排 接受 列表：
    若 做 计数 用 列表 小于或等于 1：
        返还 列表。
    完。
    基准 为 列表 的 第 0 项。
    可变 小 为 一组：（空）。
    可变 等 为 一组：（空）。
    可变 大 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 基准：
            做 追加 用 小、列表 的 第 i 项。
        否则若 列表 的 第 i 项 等于 基准：
            做 追加 用 等、列表 的 第 i 项。
        否则：
            做 追加 用 大、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    排小 为 做 火_快排 用 小。
    排大 为 做 火_快排 用 大。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 排小：
        做 追加 用 结果、排小 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 等：
        做 追加 用 结果、等 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 排大：
        做 追加 用 结果、排大 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

土_哈希表_创建 接受 桶数：
    可变 桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 桶数：
        做 追加 用 桶、一组：（空）。
        i 为 i 加 1。
    完。
    返还 一组：桶、桶数、0。
完。

土_哈希表_存 接受 表、键、值：
    可变 h 为 0。
    若 做 类型 用 键 等于 「整数」：
        h 为 键。
    否则：
        h 为 做 哈希 用 键、0。
    完。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    桶idx 为 h 取余于 表 的 第 1 项。
    桶 为 表 的 第 0 项 的 第 桶idx 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 桶：
        若 桶 的 第 i 项 的 第 0 项 等于 键：
            桶 的 第 i 项 的 第 1 项 为 值。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 桶、一组：键、值。
    表 的 第 2 项 为 表 的 第 2 项 加 1。
完。

土_哈希表_取 接受 表、键：
    可变 h 为 0。
    若 做 类型 用 键 等于 「整数」：
        h 为 键。
    否则：
        h 为 做 哈希 用 键、0。
    完。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    桶idx 为 h 取余于 表 的 第 1 项。
    桶 为 表 的 第 0 项 的 第 桶idx 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 桶：
        若 桶 的 第 i 项 的 第 0 项 等于 键：
            返还 桶 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

土_哈希表_大小 接受 表：
    返还 表 的 第 2 项。
完。

金_断言 接受 条件、消息：
    若 条件 等于 0：
        返还 做 字符串拼接 用 「断言失败: 」、消息。
    完。
    返还 空。
完。

金_验证排序 接受 列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表 减 1：
        若 列表 的 第 i 项 大于 列表 的 第 i 加 1 项：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

金_验证唯一 接受 列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 列表：
            若 列表 的 第 i 项 等于 列表 的 第 j 项：
                返还 0。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

金_测试套件 接受 测试列表：
    可变 通过 为 0。
    可变 失败 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 测试列表：
        测试 为 测试列表 的 第 i 项。
        若 测试 的 第 1 项 等于 测试 的 第 2 项：
            通过 为 通过 加 1。
        否则：
            失败 为 失败 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：通过、失败。
完。

水_管道 接受 数据、阶段列表：
    可变 当前 为 数据。
    可变 i 为 0。
    只要 i 小于 做 计数 用 阶段列表：
        当前 为 做 阶段列表 的 第 i 项 用 当前、空。
        i 为 i 加 1。
    完。
    返还 当前。
完。

水_分流 接受 数据、分支列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分支列表：
        做 追加 用 结果、做 分支列表 的 第 i 项 用 数据、空。
        i 为 i 加 1。
    完。
    返还 结果。
完。

水_汇流 接受 数据列表、合并函数：
    若 做 计数 用 数据列表 等于 0：
        返还 空。
    完。
    可变 结果 为 数据列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据列表：
        结果 为 做 合并函数 用 结果、数据列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

五行管线 接受 构建函数、变换函数、存储函数、验证函数、输出函数、输入：
    木结果 为 做 构建函数 用 输入、空。
    火结果 为 做 变换函数 用 木结果、空。
    土结果 为 做 存储函数 用 火结果、空。
    金结果 为 做 验证函数 用 土结果、空。
    水结果 为 做 输出函数 用 金结果、空。
    返还 一组：木结果、火结果、土结果、金结果、水结果。
完。

本章 收束。
