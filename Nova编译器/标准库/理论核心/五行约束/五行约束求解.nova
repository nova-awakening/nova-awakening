《五行约束求解》

引用《..·..·基础》。

行_木 为 0。

行_火 为 1。

行_土 为 2。

行_金 为 3。

行_水 为 4。

行名 接受 行：
    若 行 等于 行_木：
        返还 「木」。
    完。
    若 行 等于 行_火：
        返还 「火」。
    完。
    若 行 等于 行_土：
        返还 「土」。
    完。
    若 行 等于 行_金：
        返还 「金」。
    完。
    若 行 等于 行_水：
        返还 「水」。
    完。
    返还 「无」。
完。

相生 接受 行：
    返还 (行 加 1) 取余于 5。
完。

相克 接受 行：
    返还 (行 加 2) 取余于 5。
完。

被生 接受 行：
    返还 (行 加 4) 取余于 5。
完。

被克 接受 行：
    返还 (行 加 3) 取余于 5。
完。

约束_未解 为 0。

约束_已解 为 1。

约束_矛盾 为 2。

五行节点_创建 接受 id、行、域：
    返还 一组：id、行、空、域、约束_未解、一组：（空）。
完。

五行节点_id 接受 n：
    返还 n 的 第 0 项。
完。

五行节点_行 接受 n：
    返还 n 的 第 1 项。
完。

五行节点_值 接受 n：
    返还 n 的 第 2 项。
完。

五行节点_域 接受 n：
    返还 n 的 第 3 项。
完。

五行节点_状态 接受 n：
    返还 n 的 第 4 项。
完。

五行节点_设值 接受 n、值：
    n 的 第 2 项 为 值。
    n 的 第 3 项 为 一组：值。
    n 的 第 4 项 为 约束_已解。
完。

五行节点_收缩域 接受 n、新域：
    若 做 计数 用 新域 等于 0：
        n 的 第 4 项 为 约束_矛盾。
        返还 空。
    完。
    n 的 第 3 项 为 新域。
    若 做 计数 用 新域 等于 1：
        n 的 第 2 项 为 新域 的 第 0 项。
        n 的 第 4 项 为 约束_已解。
    完。
完。

关系_生 为 0。

关系_克 为 1。

五行约束_创建 接受 源id、目标id、关系、约束函数：
    返还 一组：源id、目标id、关系、约束函数。
完。

五行网_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、一组：（空）、0。
完。

五行网_添加节点 接受 网、行、域：
    id 为 做 计数 用 网 的 第 0 项。
    节点 为 做 五行节点_创建 用 id、行、域。
    做 追加 用 网 的 第 0 项、节点。
    返还 id。
完。

五行网_添加生 接受 网、源id、目标id、约束函数：
    源 为 网 的 第 0 项 的 第 源id 项。
    约束 为 做 五行约束_创建 用 源id、目标id、关系_生、约束函数。
    做 追加 用 网 的 第 1 项、约束。
    做 追加 用 源 的 第 5 项、做 计数 用 网 的 第 1 项 减 1。
完。

五行网_添加克 接受 网、源id、目标id、约束函数：
    约束 为 做 五行约束_创建 用 源id、目标id、关系_克、约束函数。
    做 追加 用 网 的 第 1 项、约束。
    源 为 网 的 第 0 项 的 第 源id 项。
    做 追加 用 源 的 第 5 项、做 计数 用 网 的 第 1 项 减 1。
完。

五行网_传播 接受 网：
    可变 改变 为 1。
    可变 步数 为 0。
    只要 改变 等于 1 并且 步数 小于 100：
        改变 为 0。
        可变 i 为 0。
        只要 i 小于 做 计数 用 网 的 第 1 项：
            约束 为 网 的 第 1 项 的 第 i 项。
            源 为 网 的 第 0 项 的 第 约束 的 第 0 项 项。
            目标 为 网 的 第 0 项 的 第 约束 的 第 1 项 项。
            若 做 五行节点_状态 用 源 等于 约束_已解：
                源值 为 做 五行节点_值 用 源。
                旧域 为 做 五行节点_域 用 目标。
                可变 新域 为 一组：（空）。
                可变 j 为 0。
                只要 j 小于 做 计数 用 旧域：
                    若 约束 的 第 3 项 等于 空：
                        续。
                    完。
                    若 做 约束 的 第 3 项 用 源值、旧域 的 第 j 项 等于 1：
                        做 追加 用 新域、旧域 的 第 j 项。
                    完。
                完。
                否则。
                块：
                    做 追加 用 新域、旧域 的 第 j 项。
                    j 为 j 加 1。
                完。
                若 做 计数 用 新域 小于 做 计数 用 旧域：
                    做 五行节点_收缩域 用 目标、新域。
                    改变 为 1。
                完。
            完。
            i 为 i 加 1。
        完。
        步数 为 步数 加 1。
    完。
    网 的 第 3 项 为 步数。
    返还 步数。
完。

五行网_求解 接受 网：
    做 五行网_传播 用 网。
    若 做 五行网_全部已解 用 网 等于 1：
        返还 1。
    完。
    若 做 五行网_有矛盾 用 网 等于 1：
        返还 0。
    完。
    可变 最小idx 为 负 1。
    可变 最小域 为 999999。
    可变 i 为 0。
    只要 i 小于 做 计数 用 网 的 第 0 项：
        n 为 网 的 第 0 项 的 第 i 项。
        若 做 五行节点_状态 用 n 等于 约束_未解：
            域大 为 做 计数 用 做 五行节点_域 用 n。
            若 域大 小于 最小域：
                最小域 为 域大。
                最小idx 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最小idx 等于 负 1：
        返还 0。
    完。
    域 为 做 五行节点_域 用 网 的 第 0 项 的 第 最小idx 项。
    可变 j 为 0。
    只要 j 小于 做 计数 用 域：
        备份 为 做 五行网_备份 用 网。
        做 五行节点_设值 用 网 的 第 0 项 的 第 最小idx 项、域 的 第 j 项。
        若 做 五行网_求解 用 网 等于 1：
            返还 1。
        完。
        做 五行网_恢复 用 网、备份。
        j 为 j 加 1。
    完。
    返还 0。
完。

五行网_全部已解 接受 网：
    可变 i 为 0。
    只要 i 小于 做 计数 用 网 的 第 0 项：
        若 做 五行节点_状态 用 网 的 第 0 项 的 第 i 项 不等于 约束_已解：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

五行网_有矛盾 接受 网：
    可变 i 为 0。
    只要 i 小于 做 计数 用 网 的 第 0 项：
        若 做 五行节点_状态 用 网 的 第 0 项 的 第 i 项 等于 约束_矛盾：
            返还 1。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

五行网_备份 接受 网：
    可变 备份 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 网 的 第 0 项：
        n 为 网 的 第 0 项 的 第 i 项。
        可变 域拷贝 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 做 计数 用 做 五行节点_域 用 n：
            做 追加 用 域拷贝、做 五行节点_域 用 n 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 备份、一组：n 的 第 2 项、域拷贝、n 的 第 4 项。
        i 为 i 加 1。
    完。
    返还 备份。
完。

五行网_恢复 接受 网、备份：
    可变 i 为 0。
    只要 i 小于 做 计数 用 备份：
        网 的 第 0 项 的 第 i 项 的 第 2 项 为 备份 的 第 i 项 的 第 0 项。
        网 的 第 0 项 的 第 i 项 的 第 3 项 为 备份 的 第 i 项 的 第 1 项。
        网 的 第 0 项 的 第 i 项 的 第 4 项 为 备份 的 第 i 项 的 第 2 项。
        i 为 i 加 1。
    完。
完。

五行_平衡检查 接受 网：
    可变 行计数 为 一组：0、0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 网 的 第 0 项：
        行 为 做 五行节点_行 用 网 的 第 0 项 的 第 i 项。
        若 行 大于或等于 0 并且 行 小于 5：
            行计数 的 第 行 项 为 行计数 的 第 行 项 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 缺失 为 一组：（空）。
    i 为 0。
    只要 i 小于 5：
        若 行计数 的 第 i 项 等于 0：
            做 追加 用 缺失、做 行名 用 i。
        完。
        i 为 i 加 1。
    完。
    返还 缺失。
完。

编译器_五行映射 接受 零参：
    可变 映射 为 一组：（空）。
    做 追加 用 映射、一组：行_木、「词法分析」、「从源码文本生长出token」。
    做 追加 用 映射、一组：行_火、「语法分析」、「token在火中变化为AST」。
    做 追加 用 映射、一组：行_土、「语义分析」、「AST扎根于类型和符号表」。
    做 追加 用 映射、一组：行_金、「优化」、「代码在金中被精炼提纯」。
    做 追加 用 映射、一组：行_水、「代码生成」、「优化后的IR流动为机器码」。
    返还 映射。
完。

本章 收束。
