《气韵计算模型》

引用《..·..·基础》。

气_创建 接受 值、势：
    可变 相 为 1。
    若 势 小于 0：
        相 为 0。
    完。
    返还 一组：值、势、相、负 1。
完。

气_值 接受 q：
    返还 q 的 第 0 项。
完。

气_势 接受 q：
    返还 q 的 第 1 项。
完。

气_相 接受 q：
    返还 q 的 第 2 项。
完。

气_来源 接受 q：
    返还 q 的 第 3 项。
完。

气_聚 接受 气列表：
    若 做 计数 用 气列表 等于 0：
        返还 做 气_创建 用 0、0。
    完。
    可变 总值 为 0。
    可变 总势 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 气列表：
        总值 为 总值 加 做 气_值 用 气列表 的 第 i 项。
        总势 为 总势 加 做 气_势 用 气列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 气_创建 用 总值、总势。
完。

气_散 接受 气、份数：
    若 份数 小于或等于 0：
        返还 一组：（空）。
    完。
    单值 为 做 气_值 用 气 除以 份数。
    单势 为 做 气_势 用 气 除以 份数。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 份数：
        做 追加 用 结果、做 气_创建 用 单值、单势。
        i 为 i 加 1。
    完。
    返还 结果。
完。

气_变化 接受 气、变换函数、参数：
    新值 为 做 变换函数 用 做 气_值 用 气、参数。
    新势 为 做 气_势 用 气 减 1。
    返还 做 气_创建 用 新值、新势。
完。

穴_蓄气 为 1。

穴_激活 为 2。

穴_发散 为 3。

穴_创建 接受 id、名称、初始势：
    返还 一组：id、名称、初始势、一组：（空）、一组：（空）、一组：（空）、空、穴_休眠。
完。

穴_id 接受 x：
    返还 x 的 第 0 项。
完。

穴_名 接受 x：
    返还 x 的 第 1 项。
完。

穴_势 接受 x：
    返还 x 的 第 2 项。
完。

穴_状态 接受 x：
    返还 x 的 第 7 项。
完。

穴_接收气 接受 穴、气：
    做 追加 用 穴 的 第 5 项、气。
    穴 的 第 2 项 为 穴 的 第 2 项 加 做 气_势 用 气。
    若 穴 的 第 7 项 等于 穴_休眠：
        穴 的 第 7 项 为 穴_蓄气。
    完。
完。

穴_尝试激活 接受 穴、阈值：
    若 做 穴_势 用 穴 小于 阈值：
        返还 0。
    完。
    若 做 计数 用 穴 的 第 5 项 等于 0：
        返还 0。
    完。
    穴 的 第 7 项 为 穴_激活。
    返还 1。
完。

穴_执行发散 接受 穴：
    若 做 穴_状态 用 穴 不等于 穴_激活：
        返还 一组：（空）。
    完。
    聚合气 为 做 气_聚 用 穴 的 第 5 项。
    穴 的 第 5 项 为 一组：（空）。
    可变 输出气 为 聚合气。
    若 穴 的 第 6 项 不等于 空：
        输出气 为 做 气_变化 用 聚合气、穴 的 第 6 项、空。
    完。
    出经数 为 做 计数 用 穴 的 第 4 项。
    若 出经数 等于 0：
        返还 一组：输出气。
    完。
    穴 的 第 7 项 为 穴_发散。
    穴 的 第 2 项 为 0。
    返还 做 气_散 用 输出气、出经数。
完。

穴_设变换 接受 穴、函数ref：
    穴 的 第 6 项 为 函数ref。
完。

经_创建 接受 id、源、目标、容量：
    返还 一组：id、源、目标、容量、0。
完。

经_源 接受 j：
    返还 j 的 第 1 项。
完。

经_目标 接受 j：
    返还 j 的 第 2 项。
完。

经_容量 接受 j：
    返还 j 的 第 3 项。
完。

经_传输 接受 _经、气、源穴、目标穴：
    若 做 气_势 用 气 小于或等于 0：
        返还 0。
    完。
    若 做 穴_势 用 源穴 小于或等于 做 穴_势 用 目标穴：
        返还 0。
    完。
    做 穴_接收气 用 目标穴、气。
    返还 1。
完。

韵_创建 接受 模式、周期：
    返还 一组：模式、0、周期。
完。

韵_下一拍 接受 韵：
    模式 为 韵 的 第 0 项。
    当前 为 韵 的 第 1 项。
    穴id 为 模式 的 第 当前 取余于 做 计数 用 模式 项。
    韵 的 第 1 项 为 (当前 加 1) 取余于 韵 的 第 2 项。
    返还 穴id。
完。

气韵网_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、空。
完。

气韵网_添加穴 接受 网、名称、势：
    id 为 做 计数 用 网 的 第 0 项。
    穴 为 做 穴_创建 用 id、名称、势。
    做 追加 用 网 的 第 0 项、穴。
    返还 id。
完。

气韵网_连经 接受 网、源id、目标id、容量：
    id 为 做 计数 用 网 的 第 1 项。
    经 为 做 经_创建 用 id、源id、目标id、容量。
    做 追加 用 网 的 第 1 项、经。
    源穴 为 网 的 第 0 项 的 第 源id 项。
    目标穴 为 网 的 第 0 项 的 第 目标id 项。
    做 追加 用 源穴 的 第 4 项、id。
    做 追加 用 目标穴 的 第 3 项、id。
    返还 id。
完。

气韵网_注入气 接受 网、穴id、气：
    做 穴_接收气 用 网 的 第 0 项 的 第 穴id 项、气。
    网 的 第 3 项 为 网 的 第 3 项 加 做 气_势 用 气。
完。

气韵网_设韵 接受 网、模式、周期：
    网 的 第 2 项 为 做 韵_创建 用 模式、周期。
完。

气韵网_步进 接受 网：
    若 网 的 第 2 项 等于 空：
        返还 0。
    完。
    激活穴id 为 做 韵_下一拍 用 网 的 第 2 项。
    若 激活穴id 大于或等于 做 计数 用 网 的 第 0 项：
        返还 0。
    完。
    穴 为 网 的 第 0 项 的 第 激活穴id 项。
    若 做 穴_尝试激活 用 穴、1 等于 0：
        返还 0。
    完。
    输出气列表 为 做 穴_执行发散 用 穴。
    出经ids 为 穴 的 第 4 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 出经ids 并且 i 小于 做 计数 用 输出气列表：
        经 为 网 的 第 1 项 的 第 出经ids 的 第 i 项 项。
        目标穴 为 网 的 第 0 项 的 第 做 经_目标 用 经 项。
        做 经_传输 用 经、输出气列表 的 第 i 项、穴、目标穴。
        i 为 i 加 1。
    完。
    网 的 第 4 项 为 网 的 第 4 项 加 1。
    返还 1。
完。

气韵网_运行 接受 网、最大步数：
    可变 步 为 0。
    可变 _气完 为 假。
    只要 步 小于 最大步数 并且 _气完 等于 假：
        若 做 气韵网_步进 用 网 等于 0：
            _气完 为 真。
        否则：
            步 为 步 加 1。
        完。
    完。
    返还 步。
完。

气韵网_总势 接受 网：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 网 的 第 0 项：
        总 为 总 加 做 穴_势 用 网 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

气韵网_呼吸步 接受 网、中心穴id：
    中心 为 网 的 第 0 项 的 第 中心穴id 项。
    入经ids 为 中心 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 入经ids：
        经 为 网 的 第 1 项 的 第 入经ids 的 第 i 项 项。
        源穴 为 网 的 第 0 项 的 第 做 经_源 用 经 项。
        若 做 穴_势 用 源穴 大于 0 并且 做 计数 用 源穴 的 第 5 项 大于 0：
            气列表 为 做 穴_执行发散 用 源穴。
            若 做 计数 用 气列表 大于 0：
                做 穴_接收气 用 中心、气列表 的 第 0 项。
            完。
        完。
        i 为 i 加 1。
    完。
    若 做 穴_尝试激活 用 中心、1 等于 1：
        输出 为 做 穴_执行发散 用 中心。
        出经ids 为 中心 的 第 4 项。
        i 为 0。
        只要 i 小于 做 计数 用 出经ids 并且 i 小于 做 计数 用 输出：
            经 为 网 的 第 1 项 的 第 出经ids 的 第 i 项 项。
            做 穴_接收气 用 网 的 第 0 项 的 第 做 经_目标 用 经 项、输出 的 第 i 项。
            i 为 i 加 1。
        完。
    完。
完。

气韵_转Petri 接受 网：
    穴列表 为 网 的 第 0 项。
    经列表 为 网 的 第 1 项。
    可变 库所 为 一组：（空）。
    可变 变迁 为 一组：（空）。
    可变 前置弧 为 一组：（空）。
    可变 后置弧 为 一组：（空）。
    可变 标记 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 穴列表：
        做 追加 用 库所、一组：i、做 穴_名 用 穴列表 的 第 i 项。
        做 追加 用 标记、做 穴_势 用 穴列表 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 经列表：
        经 为 经列表 的 第 i 项。
        做 追加 用 变迁、一组：i、「经」 加 i。
        做 追加 用 前置弧、一组：做 经_源 用 经、i、1。
        做 追加 用 后置弧、一组：i、做 经_目标 用 经、1。
        i 为 i 加 1。
    完。
    返还 一组：库所、变迁、前置弧、后置弧、标记。
完。

Petri_转气韵 接受 Petri网：
    库所 为 Petri网 的 第 0 项。
    变迁 为 Petri网 的 第 1 项。
    前置弧 为 Petri网 的 第 2 项。
    后置弧 为 Petri网 的 第 3 项。
    标记 为 Petri网 的 第 4 项。
    网 为 做 气韵网_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 库所：
        势 为 0。
        若 i 小于 做 计数 用 标记：
            势 为 标记 的 第 i 项。
        完。
        做 气韵网_添加穴 用 网、库所 的 第 i 项 的 第 1 项、势。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 变迁：
        可变 源 为 负 1。
        可变 目标 为 负 1。
        可变 j 为 0。
        只要 j 小于 做 计数 用 前置弧：
            若 前置弧 的 第 j 项 的 第 1 项 等于 i：
                源 为 前置弧 的 第 j 项 的 第 0 项。
            完。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 做 计数 用 后置弧：
            若 后置弧 的 第 j 项 的 第 0 项 等于 i：
                目标 为 后置弧 的 第 j 项 的 第 1 项。
            完。
            j 为 j 加 1。
        完。
        若 源 大于或等于 0 并且 目标 大于或等于 0：
            做 气韵网_连经 用 网、源、目标、1。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 标记：
        若 标记 的 第 i 项 大于 0：
            做 气韵网_注入气 用 网、i、做 气_创建 用 标记 的 第 i 项、标记 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 网。
完。

气韵_图灵模拟 接受 带长、状态数：
    网 为 做 气韵网_创建。
    可变 带穴 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 带长：
        id 为 做 气韵网_添加穴 用 网、「带」 加 i、0。
        做 追加 用 带穴、id。
        i 为 i 加 1。
    完。
    可变 状态穴 为 一组：（空）。
    i 为 0。
    只要 i 小于 状态数：
        id 为 做 气韵网_添加穴 用 网、「态」 加 i、0。
        做 追加 用 状态穴、id。
        i 为 i 加 1。
    完。
    头穴 为 做 气韵网_添加穴 用 网、「头」、1。
    做 气韵网_注入气 用 网、状态穴 的 第 0 项、做 气_创建 用 1、1。
    返还 一组：网、带穴、状态穴、头穴。
完。

气韵网_验证守恒 接受 网、初始总势：
    当前总势 为 做 气韵网_总势 用 网。
    返还 当前总势 等于 初始总势。
完。

气韵_CSP语义 接受 网：
    可变 进程列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 网 的 第 0 项：
        穴 为 网 的 第 0 项 的 第 i 项。
        名 为 做 穴_名 用 穴。
        势 为 做 穴_势 用 穴。
        做 追加 用 进程列表、一组：名、势、做 计数 用 穴 的 第 3 项、做 计数 用 穴 的 第 4 项。
        i 为 i 加 1。
    完。
    返还 进程列表。
完。

气韵网_检测死锁 接受 网：
    可变 i 为 0。
    只要 i 小于 做 计数 用 网 的 第 0 项：
        穴 为 网 的 第 0 项 的 第 i 项。
        若 做 穴_势 用 穴 大于 0 并且 做 计数 用 穴 的 第 5 项 大于 0 并且 做 计数 用 穴 的 第 4 项 大于 0：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

气韵网_验证活性 接受 网、最大步数：
    可变 经使用 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 经数：
        做 追加 用 经使用、0。
        i 为 i 加 1。
    完。
    步数 为 做 气韵网_运行 用 网、最大步数。
    可变 全活 为 1。
    i 为 0。
    只要 i 小于 经数：
        若 经使用 的 第 i 项 等于 0：
            全活 为 0。
        完。
        i 为 i 加 1。
    完。
    返还 一组：全活、步数。
完。

本章 收束。
