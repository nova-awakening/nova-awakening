《气韵并发调度》

引用《..·..·基础》。

任务_蓄气 为 0。

任务_发动 为 1。

任务_散尽 为 2。

任务_阻滞 为 3。

气韵任务_创建 接受 id、名称、初始气量：
    返还 一组：id、名称、初始气量、任务_蓄气、一组：（空）、空。
完。

气韵任务_id 接受 t：
    返还 t 的 第 0 项。
完。

气韵任务_气量 接受 t：
    返还 t 的 第 2 项。
完。

气韵任务_状态 接受 t：
    返还 t 的 第 3 项。
完。

气韵任务_充气 接受 t、气量：
    t 的 第 2 项 为 t 的 第 2 项 加 气量。
完。

气韵任务_泄气 接受 t、气量：
    t 的 第 2 项 为 t 的 第 2 项 减 气量。
完。

气韵任务_添加依赖 接受 t、依赖id：
    做 追加 用 t 的 第 4 项、依赖id。
完。

气韵调度器_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、0、0。
完。

气韵调度器_提交 接受 调度器、名称、气量：
    id 为 做 计数 用 调度器 的 第 0 项。
    任务 为 做 气韵任务_创建 用 id、名称、气量。
    做 追加 用 调度器 的 第 0 项、任务。
    调度器 的 第 3 项 为 调度器 的 第 3 项 加 气量。
    返还 id。
完。

气韵调度器_设韵 接受 调度器、韵律：
    调度器 的 第 1 项 为 韵律。
完。

气韵调度器_自动韵 接受 调度器：
    任务列表 为 调度器 的 第 0 项。
    可变 韵律 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务列表：
        气量 为 做 气韵任务_气量 用 任务列表 的 第 i 项。
        可变 拍数 为 气量。
        若 拍数 小于或等于 0：
            拍数 为 1。
        完。
        若 拍数 大于 5：
            拍数 为 5。
        完。
        可变 j 为 0。
        只要 j 小于 拍数：
            做 追加 用 韵律、i。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    调度器 的 第 1 项 为 韵律。
完。

气韵调度器_一拍 接受 调度器：
    若 做 计数 用 调度器 的 第 1 项 等于 0：
        返还 负 1。
    完。
    韵律 为 调度器 的 第 1 项。
    任务id 为 韵律 的 第 调度器 的 第 2 项 取余于 做 计数 用 韵律 项。
    调度器 的 第 2 项 为 调度器 的 第 2 项 加 1。
    若 任务id 大于或等于 做 计数 用 调度器 的 第 0 项：
        返还 负 1。
    完。
    任务 为 调度器 的 第 0 项 的 第 任务id 项。
    若 做 气韵任务_状态 用 任务 等于 任务_散尽：
        返还 任务id。
    完。
    若 做 气韵任务_状态 用 任务 等于 任务_蓄气：
        可变 全满 为 1。
        可变 i 为 0。
        只要 i 小于 做 计数 用 任务 的 第 4 项：
            dep 为 调度器 的 第 0 项 的 第 任务 的 第 4 项 的 第 i 项 项。
            若 做 气韵任务_状态 用 dep 不等于 任务_散尽：
                全满 为 0。
            完。
            i 为 i 加 1。
        完。
        若 全满 等于 1：
            任务 的 第 3 项 为 任务_发动。
        完。
    完。
    若 做 气韵任务_状态 用 任务 等于 任务_发动：
        做 气韵任务_泄气 用 任务、1。
        若 做 气韵任务_气量 用 任务 小于或等于 0：
            任务 的 第 3 项 为 任务_散尽。
            调度器 的 第 4 项 为 调度器 的 第 4 项 加 1。
        完。
    完。
    返还 任务id。
完。

气韵调度器_运行 接受 调度器、最大拍数：
    可变 拍 为 0。
    只要 拍 小于 最大拍数 并且 调度器 的 第 4 项 小于 做 计数 用 调度器 的 第 0 项：
        做 气韵调度器_一拍 用 调度器。
        拍 为 拍 加 1。
    完。
    返还 拍。
完。

气韵调度器_检查气滞 接受 调度器：
    可变 蓄气数 为 0。
    可变 活跃数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度器 的 第 0 项：
        状态 为 做 气韵任务_状态 用 调度器 的 第 0 项 的 第 i 项。
        若 状态 不等于 任务_散尽：
            活跃数 为 活跃数 加 1。
        完。
        若 状态 等于 任务_蓄气：
            蓄气数 为 蓄气数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 活跃数 大于 0 并且 蓄气数 等于 活跃数：
        返还 1。
    完。
    返还 0。
完。

气韵_工作窃取 接受 调度器：
    可变 最忙 为 负 1。
    可变 最闲 为 负 1。
    可变 最大气 为 0。
    可变 最小气 为 999999。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调度器 的 第 0 项：
        t 为 调度器 的 第 0 项 的 第 i 项。
        若 做 气韵任务_状态 用 t 不等于 任务_散尽：
            若 做 气韵任务_气量 用 t 大于 最大气：
                最大气 为 做 气韵任务_气量 用 t。
                最忙 为 i。
            完。
            若 做 气韵任务_气量 用 t 小于 最小气：
                最小气 为 做 气韵任务_气量 用 t。
                最闲 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最忙 大于或等于 0 并且 最闲 大于或等于 0 并且 最忙 不等于 最闲 并且 最大气 减 最小气 大于 2：
        转移量 为 (最大气 减 最小气) 除以 2。
        做 气韵任务_泄气 用 调度器 的 第 0 项 的 第 最忙 项、转移量。
        做 气韵任务_充气 用 调度器 的 第 0 项 的 第 最闲 项、转移量。
        返还 1。
    完。
    返还 0。
完。

可变 _气韵_任务池 为 一组：（空）。

可变 _气韵_任务ID计数 为 0。

气韵状态_等待 为 0。

气韵状态_运行 为 1。

气韵状态_完成 为 2。

气韵状态_阻塞 为 3。

气韵_创建任务 接受 名称、优先级、气量：
    _气韵_任务ID计数 为 _气韵_任务ID计数 加 1。
    任务 为 一组：_气韵_任务ID计数、名称、优先级、气量、气韵状态_等待、0、一组：（空）。
    做 追加 用 _气韵_任务池、任务。
    返还 _气韵_任务ID计数。
完。

气韵_查找任务 接受 id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _气韵_任务池：
        若 _气韵_任务池 的 第 i 项 的 第 0 项 等于 id：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

气韵_设置依赖 接受 任务ID、依赖ID：
    索引 为 做 气韵_查找任务 用 任务ID。
    若 索引 小于 0：
        返还 假。
    完。
    做 追加 用 _气韵_任务池 的 第 索引 项 的 第 6 项、依赖ID。
    返还 真。
完。

气韵_依赖满足 接受 任务：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务 的 第 6 项：
        dep_idx 为 做 气韵_查找任务 用 任务 的 第 6 项 的 第 i 项。
        若 dep_idx 大于或等于 0 并且 _气韵_任务池 的 第 dep_idx 项 的 第 4 项 不等于 气韵状态_完成：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

气韵_选择最高优先级 接受 零参：
    可变 最佳 为 负 1。
    可变 最高优先 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _气韵_任务池：
        任务 为 _气韵_任务池 的 第 i 项。
        若 任务 的 第 4 项 等于 气韵状态_等待 并且 做 气韵_依赖满足 用 任务：
            若 任务 的 第 2 项 大于 最高优先：
                最高优先 为 任务 的 第 2 项。
                最佳 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

气韵_调度一轮 接受 零参：
    索引 为 做 气韵_选择最高优先级。
    若 索引 小于 0：
        返还 假。
    完。
    _气韵_任务池 的 第 索引 项 的 第 4 项 为 气韵状态_运行。
    _气韵_任务池 的 第 索引 项 的 第 3 项 为 _气韵_任务池 的 第 索引 项 的 第 3 项 减 1。
    _气韵_任务池 的 第 索引 项 的 第 5 项 为 _气韵_任务池 的 第 索引 项 的 第 5 项 加 1。
    若 _气韵_任务池 的 第 索引 项 的 第 3 项 小于或等于 0：
        _气韵_任务池 的 第 索引 项 的 第 4 项 为 气韵状态_完成。
    完。
    返还 真。
完。

气韵_批量调度 接受 轮数：
    可变 完成数 为 0。
    可变 r 为 0。
    只要 r 小于 轮数：
        若 非 做 气韵_调度一轮：
            返还 完成数。
        完。
        完成数 为 0。
        可变 i 为 0。
        只要 i 小于 做 计数 用 _气韵_任务池：
            若 _气韵_任务池 的 第 i 项 的 第 4 项 等于 气韵状态_完成：
                完成数 为 完成数 加 1。
            完。
            i 为 i 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 完成数。
完。

气韵_总负载 接受 零参：
    可变 总气 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _气韵_任务池：
        若 _气韵_任务池 的 第 i 项 的 第 4 项 不等于 气韵状态_完成：
            总气 为 总气 加 _气韵_任务池 的 第 i 项 的 第 3 项。
        完。
        i 为 i 加 1。
    完。
    返还 总气。
完。

气韵_活跃任务数 接受 零参：
    可变 数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _气韵_任务池：
        若 _气韵_任务池 的 第 i 项 的 第 4 项 等于 气韵状态_运行 或者 _气韵_任务池 的 第 i 项 的 第 4 项 等于 气韵状态_等待：
            数 为 数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数。
完。

气韵_平均气量 接受 零参：
    活跃 为 做 气韵_活跃任务数。
    若 活跃 等于 0：
        返还 0。
    完。
    返还 做 气韵_总负载 除以 活跃。
完。

气韵_检测循环依赖 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _气韵_任务池：
        可变 j 为 0。
        只要 j 小于 做 计数 用 _气韵_任务池 的 第 i 项 的 第 6 项：
            dep_id 为 _气韵_任务池 的 第 i 项 的 第 6 项 的 第 j 项。
            dep_idx 为 做 气韵_查找任务 用 dep_id。
            若 dep_idx 大于或等于 0：
                可变 k 为 0。
                只要 k 小于 做 计数 用 _气韵_任务池 的 第 dep_idx 项 的 第 6 项：
                    若 _气韵_任务池 的 第 dep_idx 项 的 第 6 项 的 第 k 项 等于 _气韵_任务池 的 第 i 项 的 第 0 项：
                        返还 真。
                    完。
                    k 为 k 加 1。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

气韵_阻塞任务数 接受 零参：
    可变 数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _气韵_任务池：
        若 _气韵_任务池 的 第 i 项 的 第 4 项 等于 气韵状态_阻塞：
            数 为 数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 数。
完。

气韵_窃取 接受 来源ID、目标ID、数量：
    源值 为 做 气韵_查找任务 用 来源ID。
    目标值 为 做 气韵_查找任务 用 目标ID。
    若 源值 小于 0 或者 目标值 小于 0：
        返还 假。
    完。
    若 _气韵_任务池 的 第 源值 项 的 第 3 项 小于 数量：
        返还 假。
    完。
    _气韵_任务池 的 第 源值 项 的 第 3 项 为 _气韵_任务池 的 第 源值 项 的 第 3 项 减 数量。
    _气韵_任务池 的 第 目标值 项 的 第 3 项 为 _气韵_任务池 的 第 目标值 项 的 第 3 项 加 数量。
    返还 真。
完。

可变 _气韵_调度次数 为 0。

可变 _气韵_窃取次数 为 0。

可变 _气韵_死锁次数 为 0。

气韵_记录调度 接受 零参：
    _气韵_调度次数 为 _气韵_调度次数 加 1。
完。

气韵_记录窃取 接受 零参：
    _气韵_窃取次数 为 _气韵_窃取次数 加 1。
完。

气韵_记录死锁 接受 零参：
    _气韵_死锁次数 为 _气韵_死锁次数 加 1。
完。

本章 收束。
