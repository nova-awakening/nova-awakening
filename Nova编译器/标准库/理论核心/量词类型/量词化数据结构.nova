《量词化数据结构》

引用《量词类型系统》。

引用《..·..·基础》。

串列_创建 接受 零参：
    返还 做 分类 用 量词_串、一组：（空）。
完。

串列_追加 接受 串列、值：
    若 做 量词_检查 用 串列、量词_串 等于 0：
        返还 做 量词_类型错误 用 「串列_追加」、量词_串、做 分类_量词 用 串列。
    完。
    做 追加 用 做 分类_值 用 串列、值。
    返还 空。
完。

串列_取 接受 串列、索引：
    列表 为 做 分类_值 用 串列。
    若 索引 小于 0 或者 索引 大于或等于 做 计数 用 列表：
        返还 空。
    完。
    返还 列表 的 第 索引 项。
完。

串列_大小 接受 串列：
    返还 做 计数 用 做 分类_值 用 串列。
完。

串列_拼接 接受 a、b：
    可变 结果 为 做 串列_创建。
    可变 i 为 0。
    只要 i 小于 做 串列_大小 用 a：
        做 串列_追加 用 结果、做 串列_取 用 a、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 串列_大小 用 b：
        做 串列_追加 用 结果、做 串列_取 用 b、i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

串列_切片 接受 串列、起、_止：
    列表 为 做 分类_值 用 串列。
    可变 结果 为 一组：（空）。
    可变 i 为 起。
    只要 i 小于 _止 并且 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 分类 用 量词_片、结果。
完。

棵树_创建 接受 值：
    返还 做 分类 用 量词_棵、一组：值、一组：（空）。
完。

棵树_根值 接受 树：
    返还 做 分类_值 用 树 的 第 0 项。
完。

棵树_添加子 接受 树、子树：
    若 做 量词_检查 用 子树、量词_棵 等于 0：
        返还 做 量词_类型错误 用 「棵树_添加子」、量词_棵、做 分类_量词 用 子树。
    完。
    做 追加 用 做 分类_值 用 树 的 第 1 项、子树。
    返还 空。
完。

棵树_子节点 接受 树：
    返还 做 分类_值 用 树 的 第 1 项。
完。

棵树_子数 接受 树：
    返还 做 计数 用 做 分类_值 用 树 的 第 1 项。
完。

棵树_深度 接受 树：
    子列表 为 做 棵树_子节点 用 树。
    若 做 计数 用 子列表 等于 0：
        返还 1。
    完。
    可变 最大深 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子列表：
        d 为 做 棵树_深度 用 子列表 的 第 i 项。
        若 d 大于 最大深：
            最大深 为 d。
        完。
        i 为 i 加 1。
    完。
    返还 最大深 加 1。
完。

棵树_大小 接受 树：
    子列表 为 做 棵树_子节点 用 树。
    可变 总 为 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 子列表：
        总 为 总 加 做 棵树_大小 用 子列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

对_创建 接受 键、值：
    返还 做 分类 用 量词_对、一组：键、值。
完。

对_键 接受 对：
    返还 做 分类_值 用 对 的 第 0 项。
完。

对_值 接受 对：
    返还 做 分类_值 用 对 的 第 1 项。
完。

对列表_成组 接受 对列表：
    可变 值列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 对列表：
        做 追加 用 值列表、做 分类_值 用 对列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 分类 用 量词_组、值列表。
完。

块_创建 接受 大小：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 做 分类 用 量词_块、数据。
完。

块_读 接受 块、偏移：
    数据 为 做 分类_值 用 块。
    若 偏移 小于 0 或者 偏移 大于或等于 做 计数 用 数据：
        返还 0。
    完。
    返还 数据 的 第 偏移 项。
完。

块_写 接受 块、偏移、值：
    数据 为 做 分类_值 用 块。
    若 偏移 小于 0 或者 偏移 大于或等于 做 计数 用 数据：
        返还 0。
    完。
    数据 的 第 偏移 项 为 值。
    返还 1。
完。

块_大小 接受 块：
    返还 做 计数 用 做 分类_值 用 块。
完。

块_切页 接受 块、起、大小：
    数据 为 做 分类_值 用 块。
    可变 页 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小 并且 起 加 i 小于 做 计数 用 数据：
        做 追加 用 页、数据 的 第 起 加 i 项。
        i 为 i 加 1。
    完。
    返还 做 分类 用 量词_片、页。
完。

把柄_创建 接受 资源描述：
    返还 做 分类 用 量词_把、一组：资源描述、1。
完。

把柄_有效 接受 把柄：
    返还 做 分类_值 用 把柄 的 第 1 项 等于 1。
完。

把柄_关闭 接受 把柄：
    内部 为 做 分类_值 用 把柄。
    内部 的 第 1 项 为 0。
    返还 1。
完。

把柄_操作 接受 把柄、操作：
    若 做 把柄_有效 用 把柄 等于 0：
        返还 一组：「错误」、「把柄已关闭, 不可操作」。
    完。
    返还 一组：「成功」、操作。
完。

批_创建 接受 零参：
    返还 做 分类 用 量词_批、一组：（空）。
完。

批_追加 接受 批、项：
    做 追加 用 做 分类_值 用 批、项。
完。

批_大小 接受 批：
    返还 做 计数 用 做 分类_值 用 批。
完。

批_处理 接受 批、处理函数：
    数据 为 做 分类_值 用 批。
    可变 结果 为 做 批_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 批_追加 用 结果、做 处理函数 用 数据 的 第 i 项、空。
        i 为 i 加 1。
    完。
    返还 结果。
完。

层_创建 接受 名称：
    返还 做 分类 用 量词_层、一组：名称、一组：（空）、空。
完。

层_设下层 接受 层、下层：
    内部 为 做 分类_值 用 层。
    内部 的 第 2 项 为 下层。
完。

层_添加处理器 接受 层、处理器：
    做 追加 用 做 分类_值 用 层 的 第 1 项、处理器。
完。

层_传递 接受 层、数据：
    可变 当前 为 数据。
    处理器列表 为 做 分类_值 用 层 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 处理器列表：
        当前 为 做 处理器列表 的 第 i 项 用 当前、空。
        i 为 i 加 1。
    完。
    下层 为 做 分类_值 用 层 的 第 2 项。
    若 下层 不等于 空：
        返还 做 层_传递 用 下层、当前。
    完。
    返还 当前。
完。

本章 收束。
