《信息几何》

引用《..·基础》。

引用《..·数学·工具》。

IG精度 为 10000。

近似log 接受 x：
    若 x 小于或等于 0：
        返还 0 减 IG精度 乘 10。
    完。
    若 x 等于 IG精度：
        返还 0。
    完。
    y 为 x 减 IG精度。
    若 y 大于 0 减 IG精度 除以 2 并且 y 小于 IG精度 除以 2：
        y2 为 y 乘 y 除以 IG精度。
        y3 为 y2 乘 y 除以 IG精度。
        返还 y 减 y2 除以 2 加 y3 除以 3。
    完。
    可变 移位 为 0。
    可变 v 为 x。
    只要 v 大于 IG精度 乘 2：
        v 为 v 除以 2。
        移位 为 移位 加 6931。
    完。
    只要 v 小于 IG精度 除以 2：
        v 为 v 乘 2。
        移位 为 移位 减 6931。
    完。
    r 为 v 减 IG精度。
    返还 移位 加 r 减 r 乘 r 除以 (2 乘 IG精度) 加 r 乘 r 乘 r 除以 (3 乘 IG精度 乘 IG精度 除以 IG精度)。
完。

近似exp 接受 x：
    若 x 大于 60000：
        返还 IG精度 乘 1000。
    完。
    若 x 小于 0 减 60000：
        返还 1。
    完。
    x1 为 x。
    x2 为 x1 乘 x1 除以 IG精度。
    x3 为 x2 乘 x1 除以 IG精度。
    返还 IG精度 加 x1 加 x2 除以 2 加 x3 除以 6。
完。

近似sqrt 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 y 为 x 除以 2。
    若 y 等于 0：
        y 为 1。
    完。
    可变 k 为 0。
    只要 k 小于 8：
        y 为 (y 加 x 乘 IG精度 除以 y) 除以 2。
        k 为 k 加 1。
    完。
    返还 y。
完。

Fisher信息_离散 接受 分布、参数索引、扰动：
    n 为 做 计数 用 分布。
    若 n 等于 0：
        返还 0。
    完。
    若 扰动 等于 0：
        返还 0。
    完。
    可变 信息 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 分布 的 第 i 项 大于 0：
            可变 p加 为 分布 的 第 i 项。
            可变 p减 为 分布 的 第 i 项。
            若 i 等于 参数索引：
                p加 为 分布 的 第 i 项 加 扰动。
                p减 为 分布 的 第 i 项 减 扰动。
                若 p减 小于 1：
                    p减 为 1。
                完。
            否则：
                p加 为 分布 的 第 i 项 减 扰动 除以 (n 减 1)。
                p减 为 分布 的 第 i 项 加 扰动 除以 (n 减 1)。
                若 p加 小于 1：
                    p加 为 1。
                完。
                若 p减 小于 1：
                    p减 为 1。
                完。
            完。
            导数 为 (p加 减 p减) 乘 IG精度 除以 (2 乘 扰动)。
            信息 为 信息 加 导数 乘 导数 除以 (分布 的 第 i 项 加 1)。
        完。
        i 为 i 加 1。
    完。
    返还 信息。
完。

Fisher矩阵_对角 接受 分布、维度：
    可变 对角 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 对角、做 Fisher信息_离散 用 分布、d、100。
        d 为 d 加 1。
    完。
    返还 对角。
完。

Fisher矩阵_完整 接受 分布、维度：
    可变 矩阵 为 一组：（空）。
    n 为 做 计数 用 分布。
    ε 为 100。
    可变 i 为 0。
    只要 i 小于 维度 乘 维度：
        做 追加 用 矩阵、0。
        i 为 i 加 1。
    完。
    可变 di 为 0。
    只要 di 小于 维度：
        可变 dj 为 di。
        只要 dj 小于 维度：
            可变 gij 为 0。
            可变 k 为 0。
            只要 k 小于 n：
                若 分布 的 第 k 项 大于 0：
                    dpi 为 0。
                    若 k 等于 di：
                        dpi 为 IG精度。
                    否则：
                        dpi 为 0 减 IG精度 除以 (n 减 1)。
                    完。
                    dpj 为 0。
                    若 k 等于 dj：
                        dpj 为 IG精度。
                    否则：
                        dpj 为 0 减 IG精度 除以 (n 减 1)。
                    完。
                    gij 为 gij 加 dpi 乘 dpj 除以 (分布 的 第 k 项 加 1)。
                完。
                k 为 k 加 1。
            完。
            矩阵 的 第 di 乘 维度 加 dj 项 为 gij。
            矩阵 的 第 dj 乘 维度 加 di 项 为 gij。
            dj 为 dj 加 1。
        完。
        di 为 di 加 1。
    完。
    返还 矩阵。
完。

自然梯度_计算 接受 普通梯度、Fisher对角：
    维度 为 做 计数 用 普通梯度。
    可变 自然 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        若 Fisher对角 的 第 i 项 大于 0：
            做 追加 用 自然、普通梯度 的 第 i 项 乘 IG精度 除以 Fisher对角 的 第 i 项。
        否则：
            做 追加 用 自然、普通梯度 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 自然。
完。

自然梯度_阻尼 接受 普通梯度、Fisher对角、阻尼λ：
    维度 为 做 计数 用 普通梯度。
    可变 自然 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        分母 为 Fisher对角 的 第 i 项 加 阻尼λ。
        若 分母 大于 0：
            做 追加 用 自然、普通梯度 的 第 i 项 乘 IG精度 除以 分母。
        否则：
            做 追加 用 自然、普通梯度 的 第 i 项 乘 IG精度 除以 (阻尼λ 加 1)。
        完。
        i 为 i 加 1。
    完。
    返还 自然。
完。

KFAC_因子化 接受 输入激活列表、梯度列表：
    可变 ii 为 0。
    n 为 做 计数 用 输入激活列表。
    若 n 等于 0：
        返还 一组：一组：（空）、一组：（空）。
    完。
    d_in 为 做 计数 用 输入激活列表 的 第 0 项。
    d_out 为 做 计数 用 梯度列表 的 第 0 项。
    可变 A 为 一组：（空）。
    可变 ai 为 0。
    只要 ai 小于 d_in 乘 d_in：
        做 追加 用 A、0。
        ai 为 ai 加 1。
    完。
    可变 B 为 一组：（空）。
    可变 bi 为 0。
    只要 bi 小于 d_out 乘 d_out：
        做 追加 用 B、0。
        bi 为 bi 加 1。
    完。
    可变 s 为 0。
    只要 s 小于 n：
        只要 ii 小于 d_in：
            可变 jj 为 0。
            只要 jj 小于 d_in：
                A 的 第 ii 乘 d_in 加 jj 项 为 A 的 第 ii 乘 d_in 加 jj 项 加 输入激活列表 的 第 s 项 的 第 ii 项 乘 输入激活列表 的 第 s 项 的 第 jj 项 除以 n。
                jj 为 jj 加 1。
            完。
            ii 为 ii 加 1。
        完。
        可变 gi 为 0。
        只要 gi 小于 d_out：
            可变 gj 为 0。
            只要 gj 小于 d_out：
                B 的 第 gi 乘 d_out 加 gj 项 为 B 的 第 gi 乘 d_out 加 gj 项 加 梯度列表 的 第 s 项 的 第 gi 项 乘 梯度列表 的 第 s 项 的 第 gj 项 除以 n。
                gj 为 gj 加 1。
            完。
            gi 为 gi 加 1。
        完。
        s 为 s 加 1。
    完。
    返还 一组：A、B。
完。

自然梯度_步进 接受 参数、普通梯度、Fisher对角、学习率：
    可变 新参数 为 一组：（空）。
    自然 为 做 自然梯度_阻尼 用 普通梯度、Fisher对角、100。
    维度 为 做 计数 用 参数。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 新参数、参数 的 第 i 项 减 学习率 乘 自然 的 第 i 项 除以 IG精度。
        i 为 i 加 1。
    完。
    返还 新参数。
完。

信息几何_KL散度 接受 P、Q：
    n 为 做 计数 用 P。
    可变 散度 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 P 的 第 i 项 大于 0 并且 Q 的 第 i 项 大于 0：
            比值 为 P 的 第 i 项 乘 IG精度 除以 Q 的 第 i 项。
            散度 为 散度 加 P 的 第 i 项 乘 做 近似log 用 比值 除以 IG精度。
        完。
        i 为 i 加 1。
    完。
    返还 散度。
完。

信息几何_JS散度 接受 P、Q：
    n 为 做 计数 用 P。
    可变 M 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 M、(P 的 第 i 项 加 Q 的 第 i 项) 除以 2。
        i 为 i 加 1。
    完。
    返还 (做 信息几何_KL散度 用 P、M 加 做 信息几何_KL散度 用 Q、M) 除以 2。
完。

f散度_变分 接受 P、Q、T函数值：
    n 为 做 计数 用 P。
    可变 EP 为 0。
    可变 EQ 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        EP 为 EP 加 P 的 第 i 项 乘 T函数值 的 第 i 项 除以 IG精度。
        共轭 为 做 近似exp 用 T函数值 的 第 i 项 减 IG精度。
        EQ 为 EQ 加 Q 的 第 i 项 乘 共轭 除以 IG精度。
        i 为 i 加 1。
    完。
    返还 EP 减 EQ。
完。

α散度 接受 P、Q、α：
    n 为 做 计数 用 P。
    若 α 等于 IG精度：
        返还 做 信息几何_KL散度 用 P、Q。
    完。
    若 α 等于 0：
        返还 做 信息几何_KL散度 用 Q、P。
    完。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 P 的 第 i 项 大于 0 并且 Q 的 第 i 项 大于 0：
            logp 为 做 近似log 用 P 的 第 i 项。
            logq 为 做 近似log 用 Q 的 第 i 项。
            混合log 为 logp 乘 α 除以 IG精度 加 logq 乘 (IG精度 减 α) 除以 IG精度。
            总和 为 总和 加 做 近似exp 用 混合log。
        完。
        i 为 i 加 1。
    完。
    分母 为 α 乘 (IG精度 减 α) 除以 IG精度。
    若 分母 等于 0：
        返还 0。
    完。
    返还 (IG精度 减 总和) 乘 IG精度 除以 分母。
完。

WFR_距离 接受 P、Q、λ权重：
    n 为 做 计数 用 P。
    可变 BC 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 P 的 第 i 项 大于 0 并且 Q 的 第 i 项 大于 0：
            BC 为 BC 加 做 近似sqrt 用 P 的 第 i 项 乘 Q 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    FR。
    ² 为 0。
    若 BC 小于 IG精度：
        补 为 IG精度 减 BC。
        FR。
        ² 为 4 乘 补 乘 补 除以 IG精度。
    完。
    可变 W 为 0。
    可变 累积差 为 0。
    i 为 0。
    只要 i 小于 n：
        累积差 为 累积差 加 P 的 第 i 项 减 Q 的 第 i 项。
        若 累积差 小于 0：
            W 为 W 减 累积差。
        否则：
            W 为 W 加 累积差。
        完。
        i 为 i 加 1。
    完。
    返还 FR。
    ² 加 λ权重 乘 W 除以 IG精度。
完。

e测地线_插值 接受 P、Q、t：
    n 为 做 计数 用 P。
    可变 结果 为 一组：（空）。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 值 为 0。
        若 P 的 第 i 项 大于 0 并且 Q 的 第 i 项 大于 0：
            logp 为 做 近似log 用 P 的 第 i 项。
            logq 为 做 近似log 用 Q 的 第 i 项。
            混合 为 logp 乘 (IG精度 减 t) 除以 IG精度 加 logq 乘 t 除以 IG精度。
            值 为 做 近似exp 用 混合。
        完。
        做 追加 用 结果、值。
        总和 为 总和 加 值。
        i 为 i 加 1。
    完。
    若 总和 大于 0：
        i 为 0。
        只要 i 小于 n：
            结果 的 第 i 项 为 结果 的 第 i 项 乘 IG精度 除以 总和。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

m测地线_插值 接受 P、Q、t：
    n 为 做 计数 用 P。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、P 的 第 i 项 乘 (IG精度 减 t) 除以 IG精度 加 Q 的 第 i 项 乘 t 除以 IG精度。
        i 为 i 加 1。
    完。
    返还 结果。
完。

测地线_距离 接受 P、Q：
    n 为 做 计数 用 P。
    可变 BC 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 P 的 第 i 项 大于 0 并且 Q 的 第 i 项 大于 0：
            BC 为 BC 加 做 近似sqrt 用 P 的 第 i 项 乘 Q 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 BC 大于或等于 IG精度：
        返还 0。
    完。
    补 为 IG精度 减 BC。
    返还 2 乘 做 近似sqrt 用 2 乘 补。
完。

Bregman_负熵 接受 P、Q：
    返还 做 信息几何_KL散度 用 P、Q。
完。

Bregman_欧氏 接受 P、Q：
    n 为 做 计数 用 P。
    可变 d 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        差 为 P 的 第 i 项 减 Q 的 第 i 项。
        d 为 d 加 差 乘 差 除以 IG精度。
        i 为 i 加 1。
    完。
    返还 d。
完。

Mirror_Descent步进 接受 分布、梯度、学习率：
    n 为 做 计数 用 分布。
    可变 新分布 为 一组：（空）。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        权重 为 做 近似exp 用 0 减 学习率 乘 梯度 的 第 i 项 除以 IG精度。
        值 为 分布 的 第 i 项 乘 权重 除以 IG精度。
        若 值 小于 1：
            值 为 1。
        完。
        做 追加 用 新分布、值。
        总和 为 总和 加 值。
        i 为 i 加 1。
    完。
    若 总和 大于 0：
        i 为 0。
        只要 i 小于 n：
            新分布 的 第 i 项 为 新分布 的 第 i 项 乘 IG精度 除以 总和。
            i 为 i 加 1。
        完。
    完。
    返还 新分布。
完。

FisherRao_流步进 接受 当前分布、目标分布、步长：
    n 为 做 计数 用 当前分布。
    可变 KL梯度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 当前分布 的 第 i 项 大于 0 并且 目标分布 的 第 i 项 大于 0：
            比值 为 当前分布 的 第 i 项 乘 IG精度 除以 目标分布 的 第 i 项。
            做 追加 用 KL梯度、做 近似log 用 比值 加 IG精度。
        否则：
            做 追加 用 KL梯度、0。
        完。
        i 为 i 加 1。
    完。
    Fisher 为 做 Fisher矩阵_对角 用 当前分布、n。
    自然 为 做 自然梯度_阻尼 用 KL梯度、Fisher、100。
    可变 新分布 为 一组：（空）。
    可变 总和 为 0。
    i 为 0。
    只要 i 小于 n：
        值 为 当前分布 的 第 i 项 减 步长 乘 自然 的 第 i 项 除以 IG精度。
        若 值 小于 1：
            值 为 1。
        完。
        做 追加 用 新分布、值。
        总和 为 总和 加 值。
        i 为 i 加 1。
    完。
    若 总和 大于 0：
        i 为 0。
        只要 i 小于 n：
            新分布 的 第 i 项 为 新分布 的 第 i 项 乘 IG精度 除以 总和。
            i 为 i 加 1。
        完。
    完。
    返还 新分布。
完。

指数族_自然到期望 接受 自然参数、充分统计量样本：
    d 为 做 计数 用 自然参数。
    N 为 做 计数 用 充分统计量样本。
    若 N 等于 0：
        返还 自然参数。
    完。
    可变 期望 为 一组：（空）。
    可变 di 为 0。
    只要 di 小于 d：
        可变 累计 为 0。
        可变 ni 为 0。
        只要 ni 小于 N：
            累计 为 累计 加 充分统计量样本 的 第 ni 项 的 第 di 项。
            ni 为 ni 加 1。
        完。
        做 追加 用 期望、累计 除以 N。
        di 为 di 加 1。
    完。
    返还 期望。
完。

优化空间_搜索 接受 当前策略、性能梯度、策略分布：
    Fisher 为 做 Fisher矩阵_对角 用 策略分布、做 计数 用 当前策略。
    返还 做 自然梯度_步进 用 当前策略、性能梯度、Fisher、500。
完。

黎曼自适应矩估计_创建 接受 维度：
    可变 m 为 一组：（空）。
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 m、0。
        做 追加 用 v、0。
        i 为 i 加 1。
    完。
    返还 一组：m、v、0、维度。
完。

黎曼自适应矩估计_步进 接受 状态、参数、梯度、Fisher对角、学习率、β1、β2：
    可变 m 为 状态 的 第 0 项。
    可变 v 为 状态 的 第 1 项。
    t 为 状态 的 第 2 项 加 1。
    维度 为 状态 的 第 3 项。
    可变 自然梯度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        若 Fisher对角 的 第 i 项 大于 0：
            做 追加 用 自然梯度、梯度 的 第 i 项 乘 IG精度 除以 Fisher对角 的 第 i 项。
        否则：
            做 追加 用 自然梯度、梯度 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 新参数 为 一组：（空）。
    i 为 0。
    只要 i 小于 维度：
        m 的 第 i 项 为 β1 乘 m 的 第 i 项 除以 IG精度 加 (IG精度 减 β1) 乘 自然梯度 的 第 i 项 除以 IG精度。
        g_fisher_norm 为 自然梯度 的 第 i 项 乘 自然梯度 的 第 i 项 除以 IG精度。
        v 的 第 i 项 为 β2 乘 v 的 第 i 项 除以 IG精度 加 (IG精度 减 β2) 乘 g_fisher_norm 除以 IG精度。
        m_hat 为 m 的 第 i 项 乘 IG精度 除以 (IG精度 减 β1)。
        v_hat 为 v 的 第 i 项 乘 IG精度 除以 (IG精度 减 β2)。
        可变 sv 为 v_hat。
        若 sv 小于 0：
            sv 为 0 减 sv。
        完。
        可变 sqr 为 sv 除以 2。
        若 sqr 等于 0：
            sqr 为 1。
        完。
        可变 k 为 0。
        只要 k 小于 4：
            sqr 为 (sqr 加 sv 乘 IG精度 除以 sqr) 除以 2。
            k 为 k 加 1。
        完。
        做 追加 用 新参数、参数 的 第 i 项 减 学习率 乘 m_hat 除以 (sqr 加 100)。
        i 为 i 加 1。
    完。
    状态 的 第 0 项 为 m。
    状态 的 第 1 项 为 v。
    状态 的 第 2 项 为 t。
    返还 新参数。
完。

IGO_秩选择权重 接受 适应度列表：
    n 为 做 计数 用 适应度列表。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 排名 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 排名、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 0。
        只要 j 小于 n 减 1 减 i：
            若 适应度列表 的 第 排名 的 第 j 项 项 小于 适应度列表 的 第 排名 的 第 j 加 1 项 项：
                tmp 为 排名 的 第 j 项。
                排名 的 第 j 项 为 排名 的 第 j 加 1 项。
                排名 的 第 j 加 1 项 为 tmp。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 权重 为 一组：（空）。
    可变 权重和 为 0。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 权重、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 rank_pos 为 0。
        可变 j 为 0。
        只要 j 小于 n：
            若 排名 的 第 j 项 等于 i：
                rank_pos 为 j。
            完。
            j 为 j 加 1。
        完。
        中位 为 做 近似log 用 (n 除以 2 加 1) 乘 IG精度。
        排名分 为 做 近似log 用 (rank_pos 加 1) 乘 IG精度。
        可变 w 为 中位 减 排名分。
        若 w 小于 0：
            w 为 0。
        完。
        权重 的 第 i 项 为 w。
        权重和 为 权重和 加 w。
        i 为 i 加 1。
    完。
    若 权重和 大于 0：
        i 为 0。
        只要 i 小于 n：
            权重 的 第 i 项 为 权重 的 第 i 项 乘 IG精度 除以 权重和。
            i 为 i 加 1。
        完。
    完。
    返还 权重。
完。

IGO_步进 接受 参数、样本列表、适应度列表、Fisher对角、步长、维度、样本数：
    权重 为 做 IGO_秩选择权重 用 适应度列表。
    可变 梯度 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 梯度、0。
        d 为 d 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 样本数：
        d 为 0。
        只要 d 小于 维度：
            分数 为 样本列表 的 第 i 乘 维度 加 d 项 减 参数 的 第 d 项。
            梯度 的 第 d 项 为 梯度 的 第 d 项 加 权重 的 第 i 项 乘 分数 除以 IG精度。
            d 为 d 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 新参数 为 一组：（空）。
    d 为 0。
    只要 d 小于 维度：
        自然 为 梯度 的 第 d 项。
        若 Fisher对角 的 第 d 项 大于 0：
            自然 为 梯度 的 第 d 项 乘 IG精度 除以 Fisher对角 的 第 d 项。
        完。
        做 追加 用 新参数、参数 的 第 d 项 加 步长 乘 自然 除以 IG精度。
        d 为 d 加 1。
    完。
    返还 新参数。
完。

Renyi散度 接受 P、Q、α：
    n 为 做 计数 用 P。
    若 α 等于 IG精度：
        返还 做 信息几何_KL散度 用 P、Q。
    完。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 P 的 第 i 项 大于 0 并且 Q 的 第 i 项 大于 0：
            logp 为 做 近似log 用 P 的 第 i 项。
            logq 为 做 近似log 用 Q 的 第 i 项。
            混合 为 α 乘 logp 除以 IG精度 加 (IG精度 减 α) 乘 logq 除以 IG精度。
            总和 为 总和 加 做 近似exp 用 混合。
        完。
        i 为 i 加 1。
    完。
    若 总和 小于或等于 0：
        返还 0。
    完。
    分母 为 α 减 IG精度。
    若 分母 等于 0：
        返还 0。
    完。
    返还 做 近似log 用 总和 乘 IG精度 除以 分母。
完。

PAC_Bayes_Renyi界 接受 经验损失、后验P、先验Q、α、样本数：
    散度 为 做 Renyi散度 用 后验P、先验Q、α。
    若 散度 小于 0：
        散度 为 0。
    完。
    分母 为 2 乘 样本数 乘 (α 减 IG精度 加 1) 除以 IG精度。
    若 分母 小于 1：
        分母 为 1。
    完。
    被开方 为 散度 乘 IG精度 除以 分母。
    可变 s 为 被开方 除以 2。
    若 s 等于 0：
        s 为 1。
    完。
    可变 k 为 0。
    只要 k 小于 6：
        s 为 (s 加 被开方 乘 IG精度 除以 s) 除以 2。
        k 为 k 加 1。
    完。
    返还 经验损失 加 s。
完。

本章 收束。
