《意识》

引用《..·基础》。

引用《..·数学·基础·数学库》。

可变 GW_槽位 为 一组：（空）。

可变 GW_容量 为 7。

可变 GW_时钟 为 0。

GW_广播 接受 内容、强度、来源：
    GW_时钟 为 GW_时钟 加 1。
    若 做 计数 用 GW_槽位 小于 GW_容量：
        做 追加 用 GW_槽位、一组：做 计数 用 GW_槽位、内容、强度、来源、GW_时钟。
        返还 做 计数 用 GW_槽位 减 1。
    完。
    可变 最弱 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 GW_槽位：
        若 GW_槽位 的 第 i 项 的 第 2 项 小于 GW_槽位 的 第 最弱 项 的 第 2 项：
            最弱 为 i。
        完。
        i 为 i 加 1。
    完。
    若 强度 大于 GW_槽位 的 第 最弱 项 的 第 2 项：
        GW_槽位 的 第 最弱 项 为 一组：最弱、内容、强度、来源、GW_时钟。
        返还 最弱。
    完。
    返还 负 1。
完。

GW_最强 接受 零参：
    若 做 计数 用 GW_槽位 等于 0：
        返还 负 1。
    完。
    可变 最强 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 GW_槽位：
        若 GW_槽位 的 第 i 项 的 第 2 项 大于 GW_槽位 的 第 最强 项 的 第 2 项：
            最强 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最强。
完。

GW_衰减 接受 衰减率：
    可变 i 为 0。
    只要 i 小于 做 计数 用 GW_槽位：
        GW_槽位 的 第 i 项 的 第 2 项 为 GW_槽位 的 第 i 项 的 第 2 项 乘 (100 减 衰减率) 除以 100。
        i 为 i 加 1。
    完。
完。

GW_清空 接受 零参：
    GW_槽位 为 一组：（空）。
    GW_时钟 为 0。
完。

可变 聚光灯_焦点 为 负 1。

可变 聚光灯_宽度 为 3。

可变 聚光灯_强度 为 100。

聚光灯_聚焦 接受 目标：
    聚光灯_焦点 为 目标。
    聚光灯_强度 为 100。
完。

聚光灯_调宽 接受 宽度：
    聚光灯_宽度 为 宽度。
完。

聚光灯_应用 接受 信号列表：
    若 聚光灯_焦点 小于 0：
        返还 信号列表。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 信号列表：
        距离 为 做 绝对值 用 i 减 聚光灯_焦点。
        若 距离 小于或等于 聚光灯_宽度：
            增益 为 聚光灯_强度 乘 (聚光灯_宽度 减 距离 加 1) 除以 (聚光灯_宽度 加 1)。
            做 追加 用 结果、信号列表 的 第 i 项 乘 (100 加 增益) 除以 100。
        否则：
            做 追加 用 结果、信号列表 的 第 i 项 乘 50 除以 100。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

计算_Phi 接受 模块输出列表：
    n 为 做 计数 用 模块输出列表。
    若 n 小于或等于 1：
        返还 0。
    完。
    可变 phi 为 0。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            差值 为 做 绝对值 用 模块输出列表 的 第 i 项 减 模块输出列表 的 第 j 项。
            可变 coherence 为 100 减 差值。
            若 coherence 小于 0：
                coherence 为 0。
            完。
            phi 为 phi 加 coherence。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 phi 乘 2 除以 (n 乘 (n 减 1))。
完。

工作记忆_存入 接受 项目：
    若 做 计数 用 _工作记忆 大于或等于 _工作记忆_容量：
        可变 新记忆 为 一组：（空）。
        可变 i 为 1。
        只要 i 小于 做 计数 用 _工作记忆：
            做 追加 用 新记忆、_工作记忆 的 第 i 项。
            i 为 i 加 1。
        完。
        _工作记忆 为 新记忆。
    完。
    做 追加 用 _工作记忆、项目。
完。

可变 元认知_置信度 为 50。

可变 元认知_不确定度 为 50。

元认知_更新 接受 预测正确：
    若 预测正确：
        元认知_置信度 为 元认知_置信度 加 (100 减 元认知_置信度) 除以 5。
        元认知_不确定度 为 元认知_不确定度 乘 80 除以 100。
    否则：
        元认知_置信度 为 元认知_置信度 乘 80 除以 100。
        元认知_不确定度 为 元认知_不确定度 加 (100 减 元认知_不确定度) 除以 5。
    完。
完。

元认知_获取置信度 接受 零参：
    返还 元认知_置信度。
完。

元认知_需要反思 接受 零参：
    返还 元认知_不确定度 大于 70。
完。

可变 自我_能量 为 100。

可变 自我_认知负荷 为 0。

自我_消耗 接受 能量：
    自我_能量 为 自我_能量 减 能量。
    若 自我_能量 小于 0：
        自我_能量 为 0。
    完。
    自我_认知负荷 为 自我_认知负荷 加 能量。
完。

自我_恢复 接受 量：
    自我_能量 为 自我_能量 加 量。
    若 自我_能量 大于 100：
        自我_能量 为 100。
    完。
    自我_认知负荷 为 自我_认知负荷 减 量。
    若 自我_认知负荷 小于 0：
        自我_认知负荷 为 0。
    完。
完。

自我_过载 接受 零参：
    返还 自我_认知负荷 大于 80。
完。

GW_活跃数 接受 阈值：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 GW_槽位：
        若 GW_槽位 的 第 i 项 的 第 2 项 大于 阈值：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

意识_等级 接受 零参：
    可变 gw活跃 为 0。
    若 做 计数 用 GW_槽位 大于 0：
        gw活跃 为 做 GW_活跃数 用 30 乘 100 除以 做 计数 用 GW_槽位。
    完。
    返还 (gw活跃 加 元认知_置信度 加 自我_能量) 除以 3。
完。

可变 _聚焦_目标 为 「」。

可变 _聚焦_强度 为 0。

可变 _聚焦_持续 为 0。

聚焦_设置 接受 目标、强度：
    _聚焦_目标 为 目标。
    _聚焦_强度 为 强度。
    _聚焦_持续 为 0。
完。

聚焦_步进 接受 零参：
    _聚焦_持续 为 _聚焦_持续 加 1。
    若 _聚焦_持续 大于 50：
        _聚焦_强度 为 _聚焦_强度 减 5。
    完。
    若 _聚焦_强度 小于 0：
        _聚焦_强度 为 0。
    完。
    返还 _聚焦_强度。
完。

聚焦_需要切换 接受 零参：
    返还 _聚焦_强度 小于 20。
完。

可变 _工作记忆 为 一组：（空）。

可变 _工作记忆_容量 为 7。

工作记忆_写入 接受 项目、_扩2、_扩3：
    若 做 计数 用 _工作记忆 大于或等于 _工作记忆_容量：
        可变 新列表 为 一组：（空）。
        可变 i 为 1。
        只要 i 小于 做 计数 用 _工作记忆：
            做 追加 用 新列表、_工作记忆 的 第 i 项。
            i 为 i 加 1。
        完。
        _工作记忆 为 新列表。
    完。
    做 追加 用 _工作记忆、项目。
完。

工作记忆_读取 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 做 计数 用 _工作记忆：
        返还 空。
    完。
    返还 _工作记忆 的 第 索引 项。
完。

工作记忆_大小 接受 零参：
    返还 做 计数 用 _工作记忆。
完。

工作记忆_清空 接受 零参：
    _工作记忆 为 一组：（空）。
完。

工作记忆_搜索 接受 关键词：
    可变 i 为 做 计数 用 _工作记忆 减 1。
    只要 i 大于或等于 0：
        若 _工作记忆 的 第 i 项 等于 关键词：
            返还 i。
        完。
        i 为 i 减 1。
    完。
    返还 负 1。
完。

可变 _意识流 为 一组：（空）。

可变 _意识流_最大 为 100。

意识流_记录 接受 类型、内容、时刻：
    若 做 计数 用 _意识流 大于或等于 _意识流_最大：
        可变 新流 为 一组：（空）。
        可变 i 为 做 计数 用 _意识流 除以 2。
        只要 i 小于 做 计数 用 _意识流：
            做 追加 用 新流、_意识流 的 第 i 项。
            i 为 i 加 1。
        完。
        _意识流 为 新流。
    完。
    做 追加 用 _意识流、一组：类型、内容、时刻。
完。

意识流_最近 接受 n：
    可变 结果 为 一组：（空）。
    起始 为 做 计数 用 _意识流 减 n。
    若 起始 小于 0：
        起始 为 0。
    完。
    可变 i 为 起始。
    只要 i 小于 做 计数 用 _意识流：
        做 追加 用 结果、_意识流 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

意识流_清空 接受 零参：
    _意识流 为 一组：（空）。
完。

可变 _情绪_愉悦 为 50。

可变 _情绪_唤醒 为 50。

可变 _情绪_支配 为 50。

情绪_更新 接受 愉悦增量、唤醒增量、支配增量：
    _情绪_愉悦 为 _情绪_愉悦 加 愉悦增量。
    _情绪_唤醒 为 _情绪_唤醒 加 唤醒增量。
    _情绪_支配 为 _情绪_支配 加 支配增量。
    若 _情绪_愉悦 大于 100：
        _情绪_愉悦 为 100。
    完。
    若 _情绪_愉悦 小于 0：
        _情绪_愉悦 为 0。
    完。
    若 _情绪_唤醒 大于 100：
        _情绪_唤醒 为 100。
    完。
    若 _情绪_唤醒 小于 0：
        _情绪_唤醒 为 0。
    完。
    若 _情绪_支配 大于 100：
        _情绪_支配 为 100。
    完。
    若 _情绪_支配 小于 0：
        _情绪_支配 为 0。
    完。
完。

情绪_状态 接受 零参：
    返还 一组：_情绪_愉悦、_情绪_唤醒、_情绪_支配。
完。

情绪_标签 接受 零参：
    若 _情绪_愉悦 大于 70 并且 _情绪_唤醒 大于 60：
        返还 「兴奋」。
    完。
    若 _情绪_愉悦 大于 70 并且 _情绪_唤醒 小于或等于 40：
        返还 「平静」。
    完。
    若 _情绪_愉悦 小于 30 并且 _情绪_唤醒 大于 60：
        返还 「焦虑」。
    完。
    若 _情绪_愉悦 小于 30 并且 _情绪_唤醒 小于或等于 40：
        返还 「沮丧」。
    完。
    返还 「中性」。
完。

情绪_重置 接受 零参：
    _情绪_愉悦 为 50。
    _情绪_唤醒 为 50。
    _情绪_支配 为 50。
完。

自省_应该触发 接受 连续错误数、任务完成率：
    若 连续错误数 大于 3：
        返还 真。
    完。
    若 任务完成率 小于 20：
        返还 真。
    完。
    若 _聚焦_强度 小于 10：
        返还 真。
    完。
    返还 假。
完。

自省_建议 接受 连续错误数、聚焦强度：
    若 连续错误数 大于 5：
        返还 「切换策略」。
    完。
    若 聚焦强度 小于 10：
        返还 「重新聚焦」。
    完。
    若 做 工作记忆_大小 大于或等于 _工作记忆_容量：
        返还 「清理记忆」。
    完。
    返还 「继续当前任务」。
完。

本章 收束。
