《硬件抽象》

引用《..·基础》。

设备_CPU 为 0。

设备_GPU 为 1。

设备_TPU 为 2。

设备_NPU 为 3。

设备_FPGA 为 4。

设备状态_离线 为 0。

设备状态_就绪 为 1。

设备状态_忙碌 为 2。

设备状态_错误 为 3。

可变 设备列表 为 一组：（空）。

可变 设备_下一ID 为 0。

设备_注册 接受 类型、内存总量、计算能力、名哈希：
    id 为 设备_下一ID。
    设备_下一ID 为 设备_下一ID 加 1。
    做 追加 用 设备列表、一组：id、类型、设备状态_就绪、内存总量、0、计算能力、名哈希。
    返还 id。
完。

设备_数量 接受 零参：
    返还 做 计数 用 设备列表。
完。

设备_类型 接受 id：
    返还 设备列表 的 第 id 项 的 第 1 项。
完。

设备_状态 接受 id：
    返还 设备列表 的 第 id 项 的 第 2 项。
完。

设备_设状态 接受 id、s：
    设备列表 的 第 id 项 的 第 2 项 为 s。
完。

设备_可用内存 接受 id：
    返还 设备列表 的 第 id 项 的 第 3 项 减 设备列表 的 第 id 项 的 第 4 项。
完。

设备_计算能力 接受 id：
    返还 设备列表 的 第 id 项 的 第 5 项。
完。

设备_分配内存 接受 id、大小：
    若 做 设备_可用内存 用 id 大于或等于 大小：
        设备列表 的 第 id 项 的 第 4 项 为 设备列表 的 第 id 项 的 第 4 项 加 大小。
        返还 真。
    完。
    返还 假。
完。

设备_释放内存 接受 id、大小：
    设备列表 的 第 id 项 的 第 4 项 为 设备列表 的 第 id 项 的 第 4 项 减 大小。
    若 设备列表 的 第 id 项 的 第 4 项 小于 0：
        设备列表 的 第 id 项 的 第 4 项 为 0。
    完。
完。

选择最佳设备 接受 类型：
    可变 最佳 为 负 1。
    可变 最佳能力 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备列表：
        若 设备列表 的 第 i 项 的 第 1 项 等于 类型 并且 设备列表 的 第 i 项 的 第 2 项 等于 设备状态_就绪：
            若 设备列表 的 第 i 项 的 第 5 项 大于 最佳能力：
                最佳 为 i。
                最佳能力 为 设备列表 的 第 i 项 的 第 5 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

按类型计数 接受 类型：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备列表：
        若 设备列表 的 第 i 项 的 第 1 项 等于 类型：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

可变 中断_队列 为 一组：（空）。

中断_触发 接受 设备id、中断码：
    做 追加 用 中断_队列、一组：设备id、中断码。
完。

中断_数量 接受 零参：
    返还 做 计数 用 中断_队列。
完。

中断_处理 接受 零参：
    可变 新队列 为 一组：（空）。
    若 做 计数 用 中断_队列 等于 0：
        返还 一组：负 1、负 1。
    完。
    irq 为 中断_队列 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 中断_队列：
        做 追加 用 新队列、中断_队列 的 第 i 项。
        i 为 i 加 1。
    完。
    中断_队列 为 新队列。
    返还 irq。
完。

DMA_传输 接受 _源设备、目标设备、大小：
    若 做 设备_可用内存 用 目标设备 小于 大小：
        返还 假。
    完。
    做 设备_分配内存 用 目标设备、大小。
    返还 真。
完。

设备_状态名 接受 状态：
    若 状态 等于 0：
        返还 「离线」。
    完。
    若 状态 等于 1：
        返还 「就绪」。
    完。
    若 状态 等于 2：
        返还 「忙碌」。
    完。
    若 状态 等于 3：
        返还 「错误」。
    完。
    返还 「未知」。
完。

设备_类型名 接受 类型：
    若 类型 等于 0：
        返还 「CPU」。
    完。
    若 类型 等于 1：
        返还 「GPU」。
    完。
    若 类型 等于 2：
        返还 「TPU」。
    完。
    若 类型 等于 3：
        返还 「NPU」。
    完。
    若 类型 等于 4：
        返还 「FPGA」。
    完。
    返还 「未知」。
完。

设备_清空 接受 零参：
    设备列表 为 一组：（空）。
    设备_下一ID 为 0。
    中断_队列 为 一组：（空）。
完。

就绪设备 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备列表：
        若 设备列表 的 第 i 项 的 第 2 项 等于 设备状态_就绪：
            做 追加 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

总计算能力 接受 零参：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备列表：
        若 设备列表 的 第 i 项 的 第 2 项 等于 设备状态_就绪：
            总 为 总 加 设备列表 的 第 i 项 的 第 5 项。
        完。
        i 为 i 加 1。
    完。
    返还 总。
完。

AI集群_单GPU 接受 零参：
    做 设备_清空。
    做 设备_注册 用 设备_CPU、65536、100、1。
    做 设备_注册 用 设备_GPU、24576、1000、2。
    返还 1。
完。

AI集群_多GPU 接受 gpu数：
    做 设备_清空。
    做 设备_注册 用 设备_CPU、131072、100、1。
    可变 i 为 0。
    只要 i 小于 gpu数：
        做 设备_注册 用 设备_GPU、81920、2000、100 加 i。
        i 为 i 加 1。
    完。
    返还 gpu数。
完。

设备_内存使用率 接受 id：
    若 设备列表 的 第 id 项 的 第 3 项 等于 0：
        返还 0。
    完。
    返还 设备列表 的 第 id 项 的 第 4 项 乘 100 除以 设备列表 的 第 id 项 的 第 3 项。
完。

设备_报告 接受 零参：
    做 打印 用 「╔═══ 硬件设备 ═══╗」。
    做 打印 用 「║ 设备数: 」 加 做 转字符串 用 做 计数 用 设备列表 加 「 总算力: 」 加 做 转字符串 用 做 总计算能力。
    可变 i 为 0。
    只要 i 小于 做 计数 用 设备列表：
        d 为 设备列表 的 第 i 项。
        做 打印 用 「║ [」 加 做 转字符串 用 d 的 第 0 项 加 「] 」 加 做 设备_类型名 用 d 的 第 1 项 加 「 」 加 做 设备_状态名 用 d 的 第 2 项 加 「 内存:」 加 做 转字符串 用 d 的 第 4 项 加 「/」 加 做 转字符串 用 d 的 第 3 项 加 「 算力:」 加 做 转字符串 用 d 的 第 5 项。
        i 为 i 加 1。
    完。
    若 做 中断_数量 大于 0：
        做 打印 用 「║ 待处理中断: 」 加 做 转字符串 用 做 中断_数量。
    完。
    做 打印 用 「╚════════════════╝」。
完。

可变 _cpu_特性 为 一组：（空）。

CPU_检测特性 接受 特性名、支持：
    做 追加 用 _cpu_特性、一组：特性名、支持。
完。

CPU_支持 接受 特性名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _cpu_特性：
        若 _cpu_特性 的 第 i 项 的 第 0 项 等于 特性名：
            返还 _cpu_特性 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

CPU_初始化标准特性 接受 零参：
    做 CPU_检测特性 用 「SSE2」、真。
    做 CPU_检测特性 用 「SSE4.1」、真。
    做 CPU_检测特性 用 「AVX」、真。
    做 CPU_检测特性 用 「AVX2」、真。
    做 CPU_检测特性 用 「AVX512」、假。
    做 CPU_检测特性 用 「FMA」、真。
    做 CPU_检测特性 用 「POPCNT」、真。
    做 CPU_检测特性 用 「BMI2」、真。
完。

CPU_特性列表 接受 零参：
    返还 _cpu_特性。
完。

可变 _numa_节点数 为 1。

可变 _numa_核心分布 为 一组：0。

NUMA_设置 接受 节点数、核心分布：
    _numa_节点数 为 节点数。
    _numa_核心分布 为 核心分布。
完。

NUMA_节点数 接受 零参：
    返还 _numa_节点数。
完。

NUMA_核心数 接受 节点id：
    若 节点id 小于 做 计数 用 _numa_核心分布：
        返还 _numa_核心分布 的 第 节点id 项。
    完。
    返还 0。
完。

NUMA_总核心数 接受 零参：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _numa_核心分布：
        总 为 总 加 _numa_核心分布 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

可变 _功耗_记录 为 一组：（空）。

可变 _温度_记录 为 一组：（空）。

功耗_记录 接受 瓦特：
    做 追加 用 _功耗_记录、瓦特。
完。

温度_记录 接受 摄氏度：
    做 追加 用 _温度_记录、摄氏度。
完。

功耗_平均 接受 零参：
    n 为 做 计数 用 _功耗_记录。
    若 n 等于 0：
        返还 0。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总 为 总 加 _功耗_记录 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总 除以 n。
完。

温度_最高 接受 零参：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _温度_记录：
        若 _温度_记录 的 第 i 项 大于 最大：
            最大 为 _温度_记录 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

温度_过热 接受 零参：
    返还 做 温度_最高 大于 90。
完。

核函数_未编译 为 0。

核函数_已编译 为 1。

核函数_执行中 为 2。

核函数_完成 为 3。

GPU核函数_创建 接受 名称、参数数量、共享内存：
    返还 一组：名称、参数数量、一组：（空）、共享内存、核函数_未编译。
完。

GPU核函数_添加指令 接受 kernel、指令：
    做 追加 用 kernel 的 第 2 项、指令。
完。

可变 GPU_计算队列 为 一组：（空）。

可变 GPU_完成队列 为 一组：（空）。

可变 GPU_队列ID 为 0。

GPU_提交任务 接受 kernel、网格X、网格Y、网格Z、块X、块Y、块Z、参数：
    id 为 GPU_队列ID。
    GPU_队列ID 为 GPU_队列ID 加 1。
    任务 为 一组：id、kernel、一组：网格X、网格Y、网格Z、一组：块X、块Y、块Z、参数、核函数_执行中。
    做 追加 用 GPU_计算队列、任务。
    返还 id。
完。

GPU_同步 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 GPU_计算队列：
        若 GPU_计算队列 的 第 i 项 的 第 5 项 等于 核函数_执行中：
            GPU_计算队列 的 第 i 项 的 第 5 项 为 核函数_完成。
            做 追加 用 GPU_完成队列、GPU_计算队列 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    GPU_计算队列 为 一组：（空）。
    返还 做 计数 用 GPU_完成队列。
完。

GPU_待处理数 接受 零参：
    返还 做 计数 用 GPU_计算队列。
完。

可变 GPU_显存_块列表 为 一组：（空）。

可变 GPU_显存_下一地址 为 0x80000000。

可变 GPU_显存_已用 为 0。

可变 GPU_显存_总量 为 0。

GPU显存_初始化 接受 总量：
    GPU_显存_总量 为 总量。
    GPU_显存_已用 为 0。
    GPU_显存_块列表 为 一组：（空）。
    GPU_显存_下一地址 为 0x80000000。
完。

GPU显存_分配 接受 大小：
    对齐 为 (大小 加 255) 除以 256 乘 256。
    若 GPU_显存_已用 加 对齐 大于 GPU_显存_总量：
        返还 负 1。
    完。
    地址 为 GPU_显存_下一地址。
    做 追加 用 GPU_显存_块列表、一组：地址、对齐、1。
    GPU_显存_下一地址 为 GPU_显存_下一地址 加 对齐。
    GPU_显存_已用 为 GPU_显存_已用 加 对齐。
    返还 地址。
完。

GPU显存_释放 接受 地址：
    可变 i 为 0。
    只要 i 小于 做 计数 用 GPU_显存_块列表：
        若 GPU_显存_块列表 的 第 i 项 的 第 0 项 等于 地址 并且 GPU_显存_块列表 的 第 i 项 的 第 2 项 等于 1：
            GPU_显存_块列表 的 第 i 项 的 第 2 项 为 0。
            GPU_显存_已用 为 GPU_显存_已用 减 GPU_显存_块列表 的 第 i 项 的 第 1 项。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

GPU显存_使用率 接受 零参：
    若 GPU_显存_总量 等于 0：
        返还 0。
    完。
    返还 GPU_显存_已用 乘 100 除以 GPU_显存_总量。
完。

传输_主机到设备 为 0。

传输_设备到主机 为 1。

传输_设备到设备 为 2。

可变 传输_总字节 为 0。

可变 传输_次数 为 0。

GPU_传输 接受 方向、大小：
    传输_总字节 为 传输_总字节 加 大小。
    传输_次数 为 传输_次数 加 1。
    返还 真。
完。

GPU编译_常量求值 为 0。

GPU编译_依赖分析 为 1。

GPU编译_代码生成 为 2。

GPU编译_可加速 接受 阶段、工作量：
    若 阶段 等于 GPU编译_常量求值 并且 工作量 大于 10000：
        返还 真。
    完。
    若 阶段 等于 GPU编译_依赖分析 并且 工作量 大于 500：
        返还 真。
    完。
    若 阶段 等于 GPU编译_代码生成 并且 工作量 大于 5000：
        返还 真。
    完。
    返还 假。
完。

可变 并行编译_任务 为 一组：（空）。

可变 并行编译_完成 为 0。

并行编译_提交模块 接受 模块名、优先级：
    做 追加 用 并行编译_任务、一组：模块名、优先级、0。
完。

并行编译_可用核心 接受 零参：
    gpu_id 为 做 选择最佳设备 用 设备_GPU。
    cpu_id 为 做 选择最佳设备 用 设备_CPU。
    可变 核心 为 1。
    若 cpu_id 大于或等于 0：
        核心 为 做 设备_计算能力 用 cpu_id 除以 100。
    完。
    若 核心 小于 1：
        核心 为 1。
    完。
    返还 核心。
完。

并行编译_调度 接受 零参：
    核心 为 做 并行编译_可用核心。
    可变 已调度 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 并行编译_任务 并且 已调度 小于 核心：
        若 并行编译_任务 的 第 i 项 的 第 2 项 等于 0：
            并行编译_任务 的 第 i 项 的 第 2 项 为 1。
            已调度 为 已调度 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 已调度。
完。

并行编译_完成任务 接受 模块名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 并行编译_任务：
        若 并行编译_任务 的 第 i 项 的 第 0 项 等于 模块名：
            并行编译_任务 的 第 i 项 的 第 2 项 为 2。
            并行编译_完成 为 并行编译_完成 加 1。
        完。
        i 为 i 加 1。
    完。
完。

SIMD_128 为 128。

SIMD_256 为 256。

SIMD128_创建 接受 a、b：
    返还 一组：a、b。
完。

SIMD128_加 接受 a、b：
    返还 一组：a 的 第 0 项 加 b 的 第 0 项、a 的 第 1 项 加 b 的 第 1 项。
完。

SIMD128_减 接受 a、b：
    返还 一组：a 的 第 0 项 减 b 的 第 0 项、a 的 第 1 项 减 b 的 第 1 项。
完。

SIMD128_乘 接受 a、b：
    返还 一组：a 的 第 0 项 乘 b 的 第 0 项、a 的 第 1 项 乘 b 的 第 1 项。
完。

SIMD128_与 接受 a、b：
    返还 一组：a 的 第 0 项 位与 b 的 第 0 项、a 的 第 1 项 位与 b 的 第 1 项。
完。

SIMD128_或 接受 a、b：
    返还 一组：a 的 第 0 项 位或 b 的 第 0 项、a 的 第 1 项 位或 b 的 第 1 项。
完。

SIMD128_异或 接受 a、b：
    返还 一组：a 的 第 0 项 位异或 b 的 第 0 项、a 的 第 1 项 位异或 b 的 第 1 项。
完。

SIMD256_创建 接受 a、b、c、d：
    返还 一组：a、b、c、d。
完。

SIMD256_加 接受 a、b：
    返还 一组：a 的 第 0 项 加 b 的 第 0 项、a 的 第 1 项 加 b 的 第 1 项、a 的 第 2 项 加 b 的 第 2 项、a 的 第 3 项 加 b 的 第 3 项。
完。

SIMD256_减 接受 a、b：
    返还 一组：a 的 第 0 项 减 b 的 第 0 项、a 的 第 1 项 减 b 的 第 1 项、a 的 第 2 项 减 b 的 第 2 项、a 的 第 3 项 减 b 的 第 3 项。
完。

SIMD256_乘 接受 a、b：
    返还 一组：a 的 第 0 项 乘 b 的 第 0 项、a 的 第 1 项 乘 b 的 第 1 项、a 的 第 2 项 乘 b 的 第 2 项、a 的 第 3 项 乘 b 的 第 3 项。
完。

SIMD_点积_256 接受 a、b：
    返还 a 的 第 0 项 乘 b 的 第 0 项 加 a 的 第 1 项 乘 b 的 第 1 项 加 a 的 第 2 项 乘 b 的 第 2 项 加 a 的 第 3 项 乘 b 的 第 3 项。
完。

SIMD_批量加 接受 数组A、数组B、长度：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 3 小于 长度：
        va 为 做 SIMD256_创建 用 数组A 的 第 i 项、数组A 的 第 i 加 1 项、数组A 的 第 i 加 2 项、数组A 的 第 i 加 3 项。
        vb 为 做 SIMD256_创建 用 数组B 的 第 i 项、数组B 的 第 i 加 1 项、数组B 的 第 i 加 2 项、数组B 的 第 i 加 3 项。
        vr 为 做 SIMD256_加 用 va、vb。
        做 追加 用 结果、vr 的 第 0 项。
        做 追加 用 结果、vr 的 第 1 项。
        做 追加 用 结果、vr 的 第 2 项。
        做 追加 用 结果、vr 的 第 3 项。
        i 为 i 加 4。
    完。
    只要 i 小于 长度：
        做 追加 用 结果、数组A 的 第 i 项 加 数组B 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

SIMD_矩阵向量乘 接受 矩阵、向量、行数、列数：
    可变 结果 为 一组：（空）。
    可变 r 为 0。
    只要 r 小于 行数：
        可变 累加 为 0。
        可变 c 为 0。
        只要 c 加 3 小于 列数：
            累加 为 累加 加 矩阵 的 第 r 乘 列数 加 c 项 乘 向量 的 第 c 项 加 矩阵 的 第 r 乘 列数 加 c 加 1 项 乘 向量 的 第 c 加 1 项 加 矩阵 的 第 r 乘 列数 加 c 加 2 项 乘 向量 的 第 c 加 2 项 加 矩阵 的 第 r 乘 列数 加 c 加 3 项 乘 向量 的 第 c 加 3 项。
            c 为 c 加 4。
        完。
        只要 c 小于 列数：
            累加 为 累加 加 矩阵 的 第 r 乘 列数 加 c 项 乘 向量 的 第 c 项。
            c 为 c 加 1。
        完。
        做 追加 用 结果、累加。
        r 为 r 加 1。
    完。
    返还 结果。
完。

本章 收束。
