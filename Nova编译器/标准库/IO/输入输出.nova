《输入输出》

引用《..·基础》。

是斜杠码 接受 码：
    返还 码 等于 字符_斜杠 或者 码 等于 字符_反斜杠。
完。

是点码 接受 码：
    返还 码 等于 字符_句点。
完。

是冒号码 接受 码：
    返还 码 等于 字符_冒号。
完。

是空白码 接受 码：
    返还 码 等于 字符_空格 或者 码 等于 字符_制表 或者 码 等于 字符_换行 或者 码 等于 字符_回车。
完。

路径_连接 接受 a、b：
    若 做 计数 用 a 等于 0：
        返还 b。
    完。
    若 做 计数 用 b 等于 0：
        返还 a。
    完。
    尾码 为 做 字符码 用 a、做 计数 用 a 减 1。
    若 做 是斜杠码 用 尾码：
        返还 a 加 b。
    完。
    返还 a 加 「/」 加 b。
完。

IO_路径_扩展名 接受 路径：
    可变 最后点 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 路径：
        若 做 是点码 用 做 字符码 用 路径、i：
            最后点 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最后点 小于 0：
        返还 「」。
    完。
    返还 做 子串 用 路径、最后点、做 计数 用 路径 减 最后点。
完。

IO_路径_文件名 接受 路径：
    可变 最后斜杠 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 路径：
        若 做 是斜杠码 用 做 字符码 用 路径、i：
            最后斜杠 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最后斜杠 小于 0：
        返还 路径。
    完。
    返还 做 子串 用 路径、最后斜杠 加 1、做 计数 用 路径 减 最后斜杠 减 1。
完。

IO_路径_目录 接受 路径：
    可变 最后斜杠 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 路径：
        若 做 是斜杠码 用 做 字符码 用 路径、i：
            最后斜杠 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最后斜杠 小于 0：
        返还 「.」。
    完。
    若 最后斜杠 等于 0：
        返还 「/」。
    完。
    返还 做 子串 用 路径、0、最后斜杠。
完。

路径_无扩展名 接受 路径：
    fn 为 做 IO_路径_文件名 用 路径。
    可变 首点 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 fn：
        若 做 是点码 用 做 字符码 用 fn、i 并且 首点 小于 0：
            首点 为 i。
        完。
        i 为 i 加 1。
    完。
    若 首点 小于 0：
        返还 fn。
    完。
    返还 做 子串 用 fn、0、首点。
完。

IO_路径_是绝对 接受 路径：
    若 做 计数 用 路径 等于 0：
        返还 假。
    完。
    若 做 是斜杠码 用 做 字符码 用 路径、0：
        返还 真。
    完。
    若 做 计数 用 路径 大于或等于 3 并且 做 是冒号码 用 做 字符码 用 路径、1：
        返还 真。
    完。
    返还 假。
完。

IO_构建器_创建 接受 零参：
    返还 一组：一组：（空）。
完。

构建器_写 接受 b、文本：
    做 追加 用 b 的 第 0 项、文本。
完。

构建器_写行 接受 b、文本：
    做 追加 用 b 的 第 0 项、文本。
    做 追加 用 b 的 第 0 项、「\n」。
完。

IO_构建器_构建 接受 b：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 b 的 第 0 项：
        结果 为 结果 加 b 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

构建器_大小 接受 b：
    返还 做 计数 用 b 的 第 0 项。
完。

构建器_清空 接受 b：
    b 的 第 0 项 为 一组：（空）。
完。

文本_分行 接受 文本：
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        码 为 做 字符码 用 文本、i。
        若 码 等于 字符_换行：
            做 追加 用 结果、当前。
            当前 为 「」。
        否则：
            当前 为 当前 加 做 子串 用 文本、i、1。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 结果、当前。
    返还 结果。
完。

文本_行数 接受 文本：
    若 做 计数 用 文本 等于 0：
        返还 0。
    完。
    返还 做 计数 用 做 文本_分行 用 文本。
完。

文本_行 接受 文本、行号：
    行列表 为 做 文本_分行 用 文本。
    若 行号 大于或等于 0 并且 行号 小于 做 计数 用 行列表：
        返还 行列表 的 第 行号 项。
    完。
    返还 「」。
完。

格式_填充左 接受 文本、宽度、填充字符：
    可变 结果 为 文本。
    只要 做 计数 用 结果 小于 宽度：
        结果 为 填充字符 加 结果。
    完。
    返还 结果。
完。

格式_填充右 接受 文本、宽度、填充字符：
    可变 结果 为 文本。
    只要 做 计数 用 结果 小于 宽度：
        结果 为 结果 加 填充字符。
    完。
    返还 结果。
完。

IO_格式_十六进制 接受 n：
    若 n 等于 0：
        返还 「0」。
    完。
    hex_chars 为 「0123456789abcdef」。
    可变 结果 为 「」。
    可变 v 为 n。
    若 v 小于 0：
        v 为 0 减 v。
    完。
    只要 v 不等于 0：
        d 为 v 取余于 16。
        结果 为 做 子串 用 hex_chars、d、1 加 结果。
        v 为 v 除以 16。
    完。
    若 n 小于 0：
        结果 为 「-」 加 结果。
    完。
    返还 结果。
完。

IO_格式_二进制 接受 n：
    若 n 等于 0：
        返还 「0」。
    完。
    可变 结果 为 「」。
    可变 v 为 n。
    若 v 小于 0：
        v 为 0 减 v。
    完。
    只要 v 不等于 0：
        若 v 取余于 2 等于 1：
            结果 为 「1」 加 结果。
        否则：
            结果 为 「0」 加 结果。
        完。
        v 为 v 除以 2。
    完。
    若 n 小于 0：
        结果 为 「-」 加 结果。
    完。
    返还 结果。
完。

IO_格式_八进制 接受 n：
    若 n 等于 0：
        返还 「0」。
    完。
    oct_chars 为 「01234567」。
    可变 结果 为 「」。
    可变 v 为 n。
    若 v 小于 0：
        v 为 0 减 v。
    完。
    只要 v 不等于 0：
        结果 为 做 子串 用 oct_chars、v 取余于 8、1 加 结果。
        v 为 v 除以 8。
    完。
    若 n 小于 0：
        结果 为 「-」 加 结果。
    完。
    返还 结果。
完。

CSV_分割字段 接受 行、分隔码：
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行：
        若 做 字符码 用 行、i 等于 分隔码：
            做 追加 用 结果、当前。
            当前 为 「」。
        否则：
            当前 为 当前 加 做 子串 用 行、i、1。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 结果、当前。
    返还 结果。
完。

CSV_行数 接受 文本：
    返还 做 文本_行数 用 文本。
完。

CSV_解析 接受 文本、分隔符：
    分隔码 为 做 编码 用 做 子串 用 分隔符、0、1。
    行列表 为 做 文本_分行 用 文本。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        若 做 计数 用 行列表 的 第 i 项 不等于 0：
            做 追加 用 结果、做 CSV_分割字段 用 行列表 的 第 i 项、分隔码。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

缓冲读取器_创建 接受 数据、缓冲大小：
    返还 一组：数据、0、缓冲大小、做 计数 用 数据。
完。

缓冲读取器_读行 接受 读取器：
    _IO_读次数 为 _IO_读次数 加 1。
    可变 行 为 「」。
    只要 读取器 的 第 1 项 小于 读取器 的 第 3 项：
        c 为 读取器 的 第 0 项 的 第 读取器 的 第 1 项 项。
        读取器 的 第 1 项 为 读取器 的 第 1 项 加 1。
        若 c 等于 字符_换行：
            返还 行。
        完。
        若 c 不等于 字符_回车：
            行 为 行 加 c。
        完。
    完。
    若 做 计数 用 行 大于 0：
        返还 行。
    完。
    返还 空。
完。

缓冲读取器_读字节 接受 读取器：
    _IO_读次数 为 _IO_读次数 加 1。
    若 读取器 的 第 1 项 大于或等于 读取器 的 第 3 项：
        返还 负 1。
    完。
    b 为 读取器 的 第 0 项 的 第 读取器 的 第 1 项 项。
    读取器 的 第 1 项 为 读取器 的 第 1 项 加 1。
    返还 b。
完。

缓冲读取器_剩余 接受 读取器：
    返还 读取器 的 第 3 项 减 读取器 的 第 1 项。
完。

缓冲读取器_已结束 接受 读取器：
    返还 读取器 的 第 1 项 大于或等于 读取器 的 第 3 项。
完。

缓冲读取器_位置 接受 读取器：
    返还 读取器 的 第 1 项。
完。

二进制_读16LE 接受 数据、偏移：
    _IO_读次数 为 _IO_读次数 加 1。
    返还 数据 的 第 偏移 项 位或 数据 的 第 偏移 加 1 项 左移 8。
完。

二进制_读32LE 接受 数据、偏移：
    _IO_读次数 为 _IO_读次数 加 1。
    返还 数据 的 第 偏移 项 位或 数据 的 第 偏移 加 1 项 左移 8 位或 数据 的 第 偏移 加 2 项 左移 16 位或 数据 的 第 偏移 加 3 项 左移 24。
完。

二进制_写16LE 接受 缓冲、偏移、值：
    _IO_写次数 为 _IO_写次数 加 1。
    缓冲 的 第 偏移 项 为 值 位与 0xFF。
    缓冲 的 第 偏移 加 1 项 为 值 右移 8 位与 0xFF。
完。

二进制_写32LE 接受 缓冲、偏移、值：
    _IO_写次数 为 _IO_写次数 加 1。
    缓冲 的 第 偏移 项 为 值 位与 0xFF。
    缓冲 的 第 偏移 加 1 项 为 值 右移 8 位与 0xFF。
    缓冲 的 第 偏移 加 2 项 为 值 右移 16 位与 0xFF。
    缓冲 的 第 偏移 加 3 项 为 值 右移 24 位与 0xFF。
完。

字节缓冲_创建 接受 容量：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 一组：数据、0、容量。
完。

字节缓冲_写字节 接受 缓冲、值：
    _IO_写次数 为 _IO_写次数 加 1。
    若 缓冲 的 第 1 项 大于或等于 缓冲 的 第 2 项：
        返还 假。
    完。
    缓冲 的 第 0 项 的 第 缓冲 的 第 1 项 项 为 值 位与 0xFF。
    缓冲 的 第 1 项 为 缓冲 的 第 1 项 加 1。
    返还 真。
完。

字节缓冲_写32 接受 缓冲、值：
    做 字节缓冲_写字节 用 缓冲、值 位与 0xFF。
    做 字节缓冲_写字节 用 缓冲、值 右移 8 位与 0xFF。
    做 字节缓冲_写字节 用 缓冲、值 右移 16 位与 0xFF。
    做 字节缓冲_写字节 用 缓冲、值 右移 24 位与 0xFF。
完。

字节缓冲_大小 接受 缓冲：
    返还 缓冲 的 第 1 项。
完。

字节缓冲_数据 接受 缓冲：
    返还 缓冲 的 第 0 项。
完。

字节缓冲_清空 接受 缓冲：
    缓冲 的 第 1 项 为 0。
完。

可变 _IO_读次数 为 0。

可变 _IO_写次数 为 0。

本章 收束。
