《排序》

引用《..·基础》。

引用《..·数学·工具》。

引用《..·数据结构·数据结构》。

交换 接受 数组、i、j：
    可变 临时 为 数组 的 第 i 项。
    数组 的 第 i 项 为 数组 的 第 j 项。
    数组 的 第 j 项 为 临时。
完。

是已排序 接受 数组：
    可变 i 为 1。
    只要 i 小于 做 计数 用 数组：
        若 数组 的 第 i 项 小于 数组 的 第 i 减 1 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

是降序排列 接受 数组：
    可变 i 为 1。
    只要 i 小于 做 计数 用 数组：
        若 数组 的 第 i 项 大于 数组 的 第 i 减 1 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

排序_插入排序 接受 数组：
    可变 r 为 做 复制列表 用 数组。
    可变 n 为 做 计数 用 r。
    可变 i 为 1。
    只要 i 小于 n：
        可变 key 为 r 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 r 的 第 j 项 大于 key：
            r 的 第 j 加 1 项 为 r 的 第 j 项。
            j 为 j 减 1。
        完。
        r 的 第 j 加 1 项 为 key。
        i 为 i 加 1。
    完。
    返还 r。
完。

排序_选择排序 接受 数组：
    可变 r 为 做 复制列表 用 数组。
    可变 n 为 做 计数 用 r。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 最小索引 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 r 的 第 j 项 小于 r 的 第 最小索引 项：
                最小索引 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最小索引 不等于 i：
            做 交换 用 r、i、最小索引。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

排序_冒泡排序 接受 数组：
    可变 r 为 做 复制列表 用 数组。
    可变 n 为 做 计数 用 r。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 交换了 为 假。
        可变 j 为 0。
        只要 j 小于 n 减 1 减 i：
            若 r 的 第 j 项 大于 r 的 第 j 加 1 项：
                做 交换 用 r、j、j 加 1。
                交换了 为 真。
            完。
            j 为 j 加 1。
        完。
        若 交换了 等于 假：
            返还 r。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

快排_划分 接受 数组、lo、hi：
    可变 pivot 为 数组 的 第 hi 项。
    可变 i 为 lo。
    可变 j 为 lo。
    只要 j 小于 hi：
        若 数组 的 第 j 项 小于或等于 pivot：
            做 交换 用 数组、i、j。
            i 为 i 加 1。
        完。
        j 为 j 加 1。
    完。
    做 交换 用 数组、i、hi。
    返还 i。
完。

排序_快排_递归 接受 数组、lo、hi：
    若 lo 小于 hi：
        可变 p 为 做 快排_划分 用 数组、lo、hi。
        做 排序_快排_递归 用 数组、lo、p 减 1。
        做 排序_快排_递归 用 数组、p 加 1、hi。
    完。
完。

排序_快速排序 接受 数组：
    可变 r 为 做 复制列表 用 数组。
    若 做 计数 用 r 大于 1：
        做 排序_快排_递归 用 r、0、做 计数 用 r 减 1。
    完。
    返还 r。
完。

归并_合并 接受 左、右：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    可变 j 为 0。
    只要 i 小于 做 计数 用 左 并且 j 小于 做 计数 用 右：
        若 左 的 第 i 项 小于或等于 右 的 第 j 项：
            做 追加 用 结果、左 的 第 i 项。
            i 为 i 加 1。
        否则：
            做 追加 用 结果、右 的 第 j 项。
            j 为 j 加 1。
        完。
    完。
    只要 i 小于 做 计数 用 左：
        做 追加 用 结果、左 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 j 小于 做 计数 用 右：
        做 追加 用 结果、右 的 第 j 项。
        j 为 j 加 1。
    完。
    返还 结果。
完。

排序_归并排序 接受 数组：
    若 做 计数 用 数组 小于或等于 1：
        返还 做 复制列表 用 数组。
    完。
    可变 中间值 为 做 计数 用 数组 除以 2。
    可变 左 为 一组：（空）。
    可变 右 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 中间值：
        做 追加 用 左、数组 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 i 小于 做 计数 用 数组：
        做 追加 用 右、数组 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 归并_合并 用 做 排序_归并排序 用 左、做 排序_归并排序 用 右。
完。

排序_堆排序 接受 数组：
    可变 r 为 做 复制列表 用 数组。
    可变 n 为 做 计数 用 r。
    可变 i 为 n 除以 2 减 1。
    只要 i 大于或等于 0：
        做 堆_下沉3 用 r、n、i。
        i 为 i 减 1。
    完。
    可变 末尾 为 n 减 1。
    只要 末尾 大于 0：
        做 交换 用 r、0、末尾。
        做 堆_下沉3 用 r、末尾、0。
        末尾 为 末尾 减 1。
    完。
    返还 r。
完。

排序_计数排序 接受 数组、最小值、最大值：
    可变 范围 为 最大值 减 最小值 加 1。
    可变 counts 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 范围：
        做 追加 用 counts、0。
        k 为 k 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数组：
        counts 的 第 数组 的 第 i 项 减 最小值 项 为 counts 的 第 数组 的 第 i 项 减 最小值 项 加 1。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    可变 v 为 0。
    只要 v 小于 范围：
        可变 c 为 0。
        只要 c 小于 counts 的 第 v 项：
            做 追加 用 结果、v 加 最小值。
            c 为 c 加 1。
        完。
        v 为 v 加 1。
    完。
    返还 结果。
完。

逆序 接受 数组：
    可变 r 为 一组：（空）。
    可变 i 为 做 计数 用 数组 减 1。
    只要 i 大于或等于 0：
        做 追加 用 r、数组 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 r。
完。

逆序数 接受 数组：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数组：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 数组：
            若 数组 的 第 i 项 大于 数组 的 第 j 项：
                c 为 c 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

最小K个 接受 数组、k：
    可变 已排序 为 做 排序_快速排序 用 数组。
    可变 r 为 一组：（空）。
    可变 i 为 0。
    可变 上限 为 k。
    若 上限 大于 做 计数 用 已排序：
        上限 为 做 计数 用 已排序。
    完。
    只要 i 小于 上限：
        做 追加 用 r、已排序 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

最大K个 接受 数组、k：
    可变 已排序 为 做 排序_快速排序 用 数组。
    可变 r 为 一组：（空）。
    可变 n 为 做 计数 用 已排序。
    可变 起始 为 n 减 k。
    若 起始 小于 0：
        起始 为 0。
    完。
    可变 i 为 起始。
    只要 i 小于 n：
        做 追加 用 r、已排序 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

去重排序 接受 数组：
    可变 已排序 为 做 排序_快速排序 用 数组。
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已排序：
        若 做 计数 用 r 等于 0 或者 r 的 第 做 计数 用 r 减 1 项 不等于 已排序 的 第 i 项：
            做 追加 用 r、已排序 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

快选_划分 接受 数组、lo、hi：
    可变 pivot 为 数组 的 第 hi 项。
    可变 i 为 lo。
    可变 j 为 lo。
    只要 j 小于 hi：
        若 数组 的 第 j 项 小于或等于 pivot：
            做 交换 用 数组、i、j。
            i 为 i 加 1。
        完。
        j 为 j 加 1。
    完。
    做 交换 用 数组、i、hi。
    返还 i。
完。

第K小 接受 数组、k：
    可变 r 为 做 复制列表 用 数组。
    可变 lo 为 0。
    可变 hi 为 做 计数 用 r 减 1。
    只要 lo 小于 hi：
        可变 p 为 做 快选_划分 用 r、lo、hi。
        若 p 等于 k：
            返还 r 的 第 p 项。
        完。
        若 p 小于 k：
            lo 为 p 加 1。
        否则：
            hi 为 p 减 1。
        完。
    完。
    返还 r 的 第 lo 项。
完。

中位数 接受 数组：
    可变 n 为 做 计数 用 数组。
    若 n 等于 0：
        返还 0。
    完。
    返还 做 第K小 用 数组、n 除以 2。
完。

排序前K和 接受 数组、k：
    可变 已排序 为 做 排序_快速排序 用 数组。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 k 并且 i 小于 做 计数 用 已排序：
        总和 为 总和 加 已排序 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总和。
完。

本章 收束。
