《神经架构搜索》

引用《..·基础》。

NAS_随机态 为 99999。

NAS_随机 接受 零参：
    NAS_随机态 为 NAS_随机态 位异或 NAS_随机态 左移 13。
    NAS_随机态 为 NAS_随机态 位异或 NAS_随机态 右移 7。
    NAS_随机态 为 NAS_随机态 位异或 NAS_随机态 左移 17。
    若 NAS_随机态 小于 0：
        NAS_随机态 为 0 减 NAS_随机态。
    完。
    返还 NAS_随机态。
完。

指令_NONE 为 0。

指令_SKIP 为 1。

指令_CONV3 为 2。

指令_CONV5 为 3。

指令_POOL3 为 4。

指令_POOL5 为 5。

指令_SEP3 为 6。

指令_SEP5 为 7。

指令_DIL3 为 8。

指令_ATTN 为 9。

指令_FFN 为 10。

指令_TOTAL 为 11。

搜索空间_创建 接受 节点数：
    边数 为 节点数 乘 (节点数 减 1) 除以 2。
    可变 操作 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 边数：
        做 追加 用 操作、指令_CONV3。
        i 为 i 加 1。
    完。
    返还 一组：操作、节点数、边数。
完。

架构_编码 接受 操作列表：
    返还 操作列表。
完。

架构_随机生成 接受 搜索空间：
    边数 为 搜索空间 的 第 2 项。
    可变 架构 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 边数：
        做 追加 用 架构、做 NAS_随机 取余于 指令_TOTAL。
        i 为 i 加 1。
    完。
    返还 架构。
完。

DARTS_α初始化 接受 边数：
    可变 α 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 边数：
        可变 边α 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 指令_TOTAL：
            做 追加 用 边α、1000 除以 指令_TOTAL。
            j 为 j 加 1。
        完。
        做 追加 用 α、边α。
        i 为 i 加 1。
    完。
    返还 α。
完。

DARTS_归一化 接受 α_边：
    可变 指数组 为 一组：（空）。
    n 为 做 计数 用 α_边。
    可变 max_v 为 α_边 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 α_边 的 第 i 项 大于 max_v：
            max_v 为 α_边 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 总 为 0。
    i 为 0。
    只要 i 小于 n：
        可变 e 为 1000 加 (α_边 的 第 i 项 减 max_v)。
        若 e 小于 1：
            e 为 1。
        完。
        做 追加 用 指数组、e。
        总 为 总 加 e。
        i 为 i 加 1。
    完。
    若 总 等于 0：
        总 为 1。
    完。
    可变 概率 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 概率、指数组 的 第 i 项 乘 1000 除以 总。
        i 为 i 加 1。
    完。
    返还 概率。
完。

DARTS_离散化 接受 α：
    边数 为 做 计数 用 α。
    可变 架构 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 边数：
        概率 为 做 DARTS_归一化 用 α 的 第 i 项。
        可变 最佳 为 0。
        可变 最佳值 为 概率 的 第 0 项。
        可变 j 为 1。
        只要 j 小于 做 计数 用 概率：
            若 概率 的 第 j 项 大于 最佳值：
                最佳 为 j。
                最佳值 为 概率 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 架构、最佳。
        i 为 i 加 1。
    完。
    返还 架构。
完。

NAS_变异 接受 架构、变异率：
    n 为 做 计数 用 架构。
    可变 新架构 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 NAS_随机 取余于 1000 小于 变异率：
            做 追加 用 新架构、做 NAS_随机 取余于 指令_TOTAL。
        否则：
            做 追加 用 新架构、架构 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 新架构。
完。

NAS_交叉 接受 父1、父2：
    n 为 做 计数 用 父1。
    交叉点 为 做 NAS_随机 取余于 n。
    可变 子代 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 交叉点：
            做 追加 用 子代、父1 的 第 i 项。
        否则：
            若 i 小于 做 计数 用 父2：
                做 追加 用 子代、父2 的 第 i 项。
            否则：
                做 追加 用 子代、父1 的 第 i 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 子代。
完。

NAS_锦标赛选择 接受 种群、适应度、锦标赛大小：
    n 为 做 计数 用 种群。
    可变 最佳 为 做 NAS_随机 取余于 n。
    可变 i 为 1。
    只要 i 小于 锦标赛大小：
        候选 为 做 NAS_随机 取余于 n。
        若 适应度 的 第 候选 项 大于 适应度 的 第 最佳 项：
            最佳 为 候选。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

NAS_进化 接受 种群大小、_边数、_代数、评估函数结果列表：
    可变 种群 为 一组：（空）。
    可变 适应度 为 一组：（空）。
    搜索空间 为 做 搜索空间_创建 用 4。
    可变 i 为 0。
    只要 i 小于 种群大小：
        做 追加 用 种群、做 架构_随机生成 用 搜索空间。
        做 追加 用 适应度、0。
        i 为 i 加 1。
    完。
    若 做 计数 用 评估函数结果列表 大于 0：
        i 为 0。
        只要 i 小于 种群大小 并且 i 小于 做 计数 用 评估函数结果列表：
            适应度 的 第 i 项 为 评估函数结果列表 的 第 i 项。
            i 为 i 加 1。
        完。
    完。
    返还 一组：种群、适应度。
完。

NAS_预测_FLOPs 接受 架构、输入维度、通道数：
    可变 总FLOPs 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 架构：
        op 为 架构 的 第 i 项。
        可变 flops 为 0。
        若 op 等于 指令_CONV3：
            flops 为 输入维度 乘 输入维度 乘 通道数 乘 通道数 乘 9。
        完。
        若 op 等于 指令_CONV5：
            flops 为 输入维度 乘 输入维度 乘 通道数 乘 通道数 乘 25。
        完。
        若 op 等于 指令_SEP3：
            flops 为 输入维度 乘 输入维度 乘 通道数 乘 9 加 输入维度 乘 输入维度 乘 通道数 乘 通道数。
        完。
        若 op 等于 指令_ATTN：
            flops 为 输入维度 乘 输入维度 乘 通道数 乘 3。
        完。
        若 op 等于 指令_SKIP：
            flops 为 0。
        完。
        若 op 等于 指令_FFN：
            flops 为 通道数 乘 通道数 乘 4。
        完。
        总FLOPs 为 总FLOPs 加 flops。
        i 为 i 加 1。
    完。
    返还 总FLOPs。
完。

NAS_Pareto支配 接受 a精度、a_FLOPs、b精度、b_FLOPs：
    返还 a精度 大于或等于 b精度 并且 a_FLOPs 小于或等于 b_FLOPs 并且 (a精度 大于 b精度 或者 a_FLOPs 小于 b_FLOPs)。
完。

NAS_Pareto前沿 接受 精度列表、FLOPs列表：
    n 为 做 计数 用 精度列表。
    可变 前沿 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 被支配 为 0。
        可变 j 为 0。
        只要 j 小于 n：
            若 j 不等于 i 并且 做 NAS_Pareto支配 用 精度列表 的 第 j 项、FLOPs列表 的 第 j 项、精度列表 的 第 i 项、FLOPs列表 的 第 i 项：
                被支配 为 1。
            完。
            j 为 j 加 1。
        完。
        若 被支配 等于 0：
            做 追加 用 前沿、i。
        完。
        i 为 i 加 1。
    完。
    返还 前沿。
完。

超参_随机搜索 接受 范围列表、试验数：
    维度 为 做 计数 用 范围列表。
    可变 结果 为 一组：（空）。
    可变 t 为 0。
    只要 t 小于 试验数：
        可变 配置 为 一组：（空）。
        可变 d 为 0。
        只要 d 小于 维度：
            最小 为 范围列表 的 第 d 项 的 第 0 项。
            最大 为 范围列表 的 第 d 项 的 第 1 项。
            值 为 最小 加 做 NAS_随机 取余于 (最大 减 最小 加 1)。
            做 追加 用 配置、值。
            d 为 d 加 1。
        完。
        做 追加 用 结果、配置。
        t 为 t 加 1。
    完。
    返还 结果。
完。

本章 收束。
