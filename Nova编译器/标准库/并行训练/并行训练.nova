《并行训练》

引用《..·分布式·分布式》。

引用《..·基础》。

引用《..·数学·基础·数学库》。

分散 接受 数据、节点数、节点ID：
    块大小 为 做 计数 用 数据 除以 节点数。
    起始 为 节点ID 乘 块大小。
    可变 结果 为 一组：（空）。
    可变 i 为 起始。
    只要 i 小于 起始 加 块大小 并且 i 小于 做 计数 用 数据：
        做 追加 用 结果、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

收集 接受 分片列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分片列表：
        可变 j 为 0。
        只要 j 小于 做 计数 用 分片列表 的 第 i 项：
            做 追加 用 结果、分片列表 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

归约分散 接受 各节点数据、节点数：
    n 为 做 计数 用 各节点数据。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    维度 为 做 计数 用 各节点数据 的 第 0 项。
    可变 总和 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        可变 s 为 0。
        可变 i 为 0。
        只要 i 小于 n：
            s 为 s 加 各节点数据 的 第 i 项 的 第 d 项。
            i 为 i 加 1。
        完。
        做 追加 用 总和、s。
        d 为 d 加 1。
    完。
    块大小 为 维度 除以 节点数。
    可变 结果 为 一组：（空）。
    可变 ni 为 0。
    只要 ni 小于 节点数：
        可变 片 为 一组：（空）。
        可变 j 为 ni 乘 块大小。
        只要 j 小于 (ni 加 1) 乘 块大小 并且 j 小于 维度：
            做 追加 用 片、总和 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、片。
        ni 为 ni 加 1。
    完。
    返还 结果。
完。

张量_列分片 接受 矩阵、行数、列数、分片数、分片ID：
    每片列 为 列数 除以 分片数。
    可变 结果 为 一组：（空）。
    可变 r 为 0。
    只要 r 小于 行数：
        可变 c 为 分片ID 乘 每片列。
        只要 c 小于 (分片ID 加 1) 乘 每片列 并且 c 小于 列数：
            做 追加 用 结果、矩阵 的 第 r 乘 列数 加 c 项。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 结果。
完。

张量_行分片 接受 矩阵、行数、列数、分片数、分片ID：
    每片行 为 行数 除以 分片数。
    起始 为 分片ID 乘 每片行 乘 列数。
    可变 末尾 为 (分片ID 加 1) 乘 每片行 乘 列数。
    若 末尾 大于 做 计数 用 矩阵：
        末尾 为 做 计数 用 矩阵。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 起始。
    只要 i 小于 末尾：
        做 追加 用 结果、矩阵 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 管道_阶段 为 一组：（空）。

管道_注入 接受 微批次：
    管道_阶段 的 第 0 项 的 第 1 项 为 微批次。
完。

管道_推进 接受 零参：
    可变 i 为 做 计数 用 管道_阶段 减 1。
    只要 i 大于 0：
        管道_阶段 的 第 i 项 的 第 1 项 为 管道_阶段 的 第 i 减 1 项 的 第 2 项。
        i 为 i 减 1。
    完。
完。

管道_处理 接受 阶段ID、_处理函数值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 管道_阶段 的 第 阶段ID 项 的 第 1 项：
        做 追加 用 结果、管道_阶段 的 第 阶段ID 项 的 第 1 项 的 第 i 项 加 阶段ID 加 1。
        i 为 i 加 1。
    完。
    管道_阶段 的 第 阶段ID 项 的 第 2 项 为 结果。
完。

微批次_拆分 接受 数据、微批次数：
    n 为 做 计数 用 数据。
    可变 每批 为 n 除以 微批次数。
    若 每批 小于 1：
        每批 为 1。
    完。
    可变 结果 为 一组：（空）。
    可变 起始 为 0。
    可变 b 为 0。
    只要 b 小于 微批次数 并且 起始 小于 n：
        可变 批 为 一组：（空）。
        可变 i 为 起始。
        可变 末尾 为 起始 加 每批。
        若 b 等于 微批次数 减 1：
            末尾 为 n。
        完。
        只要 i 小于 末尾：
            做 追加 用 批、数据 的 第 i 项。
            i 为 i 加 1。
        完。
        做 追加 用 结果、批。
        起始 为 末尾。
        b 为 b 加 1。
    完。
    返还 结果。
完。

FSDP_分片权重 接受 权重、节点数、节点ID：
    返还 做 分散 用 权重、节点数、节点ID。
完。

FSDP_收集权重 接受 分片列表：
    返还 做 收集 用 分片列表。
完。

FSDP_梯度同步 接受 本地梯度、节点数：
    可变 各节点 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 节点数：
        做 追加 用 各节点、本地梯度。
        k 为 k 加 1。
    完。
    返还 做 AllReduce_平均 用 各节点。
完。

可变 梯度桶 为 一组：（空）。

可变 并行_桶容量 为 4。

梯度桶_清空 接受 零参：
    梯度桶 为 一组：（空）。
完。

梯度桶_追加 接受 梯度：
    做 追加 用 梯度桶、梯度。
    若 做 计数 用 梯度桶 大于或等于 并行_桶容量：
        返还 真。
    完。
    返还 假。
完。

梯度桶_同步 接受 节点数：
    可变 合并 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度桶：
        可变 j 为 0。
        只要 j 小于 做 计数 用 梯度桶 的 第 i 项：
            做 追加 用 合并、梯度桶 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    梯度桶 为 一组：（空）。
    可变 各节点 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 节点数：
        做 追加 用 各节点、合并。
        k 为 k 加 1。
    完。
    返还 做 AllReduce_平均 用 各节点。
完。

梯度_TopK压缩 接受 梯度、K：
    n 为 做 计数 用 梯度。
    若 K 大于或等于 n：
        返还 梯度。
    完。
    可变 abs_vals 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 abs_vals、做 绝对值 用 梯度 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 sorted_v 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 sorted_v、abs_vals 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 K：
        可变 mx_idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 sorted_v 的 第 j 项 大于 sorted_v 的 第 mx_idx 项：
                mx_idx 为 j。
            完。
            j 为 j 加 1。
        完。
        临时 为 sorted_v 的 第 i 项。
        sorted_v 的 第 i 项 为 sorted_v 的 第 mx_idx 项。
        sorted_v 的 第 mx_idx 项 为 临时。
        i 为 i 加 1。
    完。
    阈值 为 sorted_v 的 第 K 减 1 项。
    可变 结果 为 一组：（空）。
    可变 kept 为 0。
    i 为 0。
    只要 i 小于 n：
        若 abs_vals 的 第 i 项 大于或等于 阈值 并且 kept 小于 K：
            做 追加 用 结果、梯度 的 第 i 项。
            kept 为 kept 加 1。
        否则：
            做 追加 用 结果、0。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

环形_下一节点 接受 当前、总数：
    返还 (当前 加 1) 取余于 总数。
完。

环形_上一节点 接受 当前、总数：
    返还 (当前 加 总数 减 1) 取余于 总数。
完。

并行训练_AllReduce_求和 接受 节点梯度列表：
    节点数 为 做 计数 用 节点梯度列表。
    若 节点数 等于 0：
        返还 一组：（空）。
    完。
    维度 为 做 计数 用 节点梯度列表 的 第 0 项。
    可变 结果 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 结果、0。
        d 为 d 加 1。
    完。
    可变 n 为 0。
    只要 n 小于 节点数：
        d 为 0。
        只要 d 小于 维度：
            结果 的 第 d 项 为 结果 的 第 d 项 加 节点梯度列表 的 第 n 项 的 第 d 项。
            d 为 d 加 1。
        完。
        n 为 n 加 1。
    完。
    d 为 0。
    只要 d 小于 维度：
        结果 的 第 d 项 为 结果 的 第 d 项 除以 节点数。
        d 为 d 加 1。
    完。
    返还 结果。
完。

模型分片_计算 接受 总层数、GPU数：
    每GPU层 为 (总层数 加 GPU数 减 1) 除以 GPU数。
    可变 分配 为 一组：（空）。
    可变 起始 为 0。
    可变 i 为 0。
    只要 i 小于 GPU数：
        可变 结束 为 起始 加 每GPU层。
        若 结束 大于 总层数：
            结束 为 总层数。
        完。
        做 追加 用 分配、一组：起始、结束。
        起始 为 结束。
        i 为 i 加 1。
    完。
    返还 分配。
完。

混合精度_缩放损失 接受 损失、缩放因子：
    返还 损失 乘 缩放因子。
完。

混合精度_反缩放梯度 接受 梯度列表、缩放因子：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度列表：
        做 追加 用 结果、梯度列表 的 第 i 项 除以 缩放因子。
        i 为 i 加 1。
    完。
    返还 结果。
完。

混合精度_检查溢出 接受 梯度列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度列表：
        若 梯度列表 的 第 i 项 大于 999999 或者 梯度列表 的 第 i 项 小于 负 999999：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

本章 收束。
