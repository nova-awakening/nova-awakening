《概率分布》

引用《..·基础》。

引用《..·数学·统计》。

引用《..·算法·数学扩展》。

概率_种子 为 77777。

概率_随机 接受 零参：
    概率_种子 为 概率_种子 乘 1103515245 加 12345。
    若 概率_种子 小于 0：
        概率_种子 为 0 减 概率_种子。
    完。
    返还 概率_种子 取余于 10000。
完。

均匀_采样 接受 最小、最大：
    返还 最小 加 做 概率_随机 乘 (最大 减 最小) 除以 10000。
完。

均匀_批量采样 接受 最小、最大、N：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        做 追加 用 结果、做 均匀_采样 用 最小、最大。
        i 为 i 加 1。
    完。
    返还 结果。
完。

均匀_PDF 接受 x、最小、最大：
    若 x 小于 最小：
        返还 0。
    完。
    若 x 大于 最大：
        返还 0。
    完。
    返还 1000 除以 (最大 减 最小)。
完。

正态_采样 接受 均值、标准差：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 12：
        总和 为 总和 加 做 概率_随机。
        i 为 i 加 1。
    完。
    z 为 (总和 减 60000) 乘 标准差 除以 2887。
    返还 均值 加 z。
完。

正态_批量采样 接受 均值、标准差、N：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        做 追加 用 结果、做 正态_采样 用 均值、标准差。
        i 为 i 加 1。
    完。
    返还 结果。
完。

伯努利_采样 接受 概率_千分：
    若 做 概率_随机 小于 概率_千分 乘 10000 除以 1000：
        返还 1。
    完。
    返还 0。
完。

伯努利_批量 接受 概率_千分、N：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        做 追加 用 结果、做 伯努利_采样 用 概率_千分。
        i 为 i 加 1。
    完。
    返还 结果。
完。

二项_采样 接受 N、概率_千分：
    可变 成功 为 0。
    可变 i 为 0。
    只要 i 小于 N：
        成功 为 成功 加 做 伯努利_采样 用 概率_千分。
        i 为 i 加 1。
    完。
    返还 成功。
完。

泊松_采样 接受 衰减系数：
    可变 k 为 0。
    可变 p 为 10000。
    可变 L 为 10000。
    可变 i 为 0。
    只要 i 小于 衰减系数：
        L 为 L 乘 3679 除以 10000。
        若 L 小于 1：
            L 为 1。
        完。
        i 为 i 加 1。
    完。
    只要 p 大于 L：
        k 为 k 加 1。
        p 为 p 乘 做 概率_随机 除以 10000。
    完。
    返还 k 减 1。
完。

指数_采样 接受 衰减系数：
    可变 u 为 做 概率_随机。
    若 u 小于 1：
        u 为 1。
    完。
    若 衰减系数 等于 0：
        衰减系数 为 1。
    完。
    ln_approx 为 (10000 减 u) 乘 100 除以 u。
    返还 ln_approx 乘 1000 除以 衰减系数。
完。

样本_均值 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    返还 做 折叠 用 数据、0、以 s、x 映射到 s 加 x 除以 做 计数 用 数据。
完。

样本_方差 接受 数据：
    均值 为 做 样本_均值 用 数据。
    返还 做 折叠 用 数据、0、以 s、x 映射到 s 加 (x 减 均值) 乘 (x 减 均值) 除以 做 计数 用 数据。
完。

样本_标准差 接受 数据：
    v 为 做 样本_方差 用 数据。
    若 v 小于或等于 0：
        返还 0。
    完。
    返还 做 整数sqrt 用 v。
完。

蒙特卡洛_估计Pi 接受 N：
    可变 内部 为 0。
    可变 i 为 0。
    只要 i 小于 N：
        x 为 做 概率_随机 减 5000。
        y 为 做 概率_随机 减 5000。
        若 x 乘 x 加 y 乘 y 小于或等于 5000 乘 5000：
            内部 为 内部 加 1。
        完。
        i 为 i 加 1。
    完。
    若 N 等于 0：
        返还 0。
    完。
    返还 内部 乘 4000 除以 N。
完。

正态_PDF 接受 x、均值、标准差：
    差值 为 x 减 均值。
    指数项 为 0 减 差值 乘 差值 除以 (2 乘 标准差 乘 标准差)。
    可变 ex 为 1000 加 指数项 加 指数项 乘 指数项 除以 2000。
    若 ex 小于 0：
        ex 为 0。
    完。
    返还 ex 乘 1000 除以 (标准差 乘 2507)。
完。

正态_CDF近似 接受 x、均值、标准差：
    z 为 (x 减 均值) 乘 1000 除以 (标准差 加 1)。
    t 为 z 乘 798 除以 1000。
    可变 tanh近似 为 t。
    若 tanh近似 大于 1000：
        tanh近似 为 1000。
    完。
    若 tanh近似 小于 负 1000：
        tanh近似 为 负 1000。
    完。
    返还 500 加 tanh近似 除以 2。
完。

泊松_PMF 接受 k、衰减系数：
    可变 幂 为 1000。
    可变 i 为 0。
    只要 i 小于 k：
        幂 为 幂 乘 衰减系数 除以 1000。
        i 为 i 加 1。
    完。
    可变 阶乘 为 1。
    i 为 2。
    只要 i 小于或等于 k：
        阶乘 为 阶乘 乘 i。
        i 为 i 加 1。
    完。
    可变 指数 为 1000 减 衰减系数 加 衰减系数 乘 衰减系数 除以 2000。
    若 指数 小于 0：
        指数 为 1。
    完。
    返还 幂 乘 指数 除以 (阶乘 乘 1000)。
完。

指数_PDF 接受 x、衰减系数：
    若 x 小于 0：
        返还 0。
    完。
    指数项 为 0 减 衰减系数 乘 x 除以 1000。
    可变 ex 为 1000 加 指数项。
    若 ex 小于 0：
        ex 为 0。
    完。
    返还 衰减系数 乘 ex 除以 1000。
完。

二项_均值 接受 n、p：
    返还 n 乘 p 除以 1000。
完。

二项_方差 接受 n、p：
    返还 n 乘 p 乘 (1000 减 p) 除以 1000000。
完。

概率分布_统计_均值 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        总 为 总 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总 除以 做 计数 用 数据。
完。

概率分布_统计_方差 接受 数据：
    若 做 计数 用 数据 小于 2：
        返还 0。
    完。
    均 为 做 统计_均值 用 数据。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        d 为 数据 的 第 i 项 减 均。
        总 为 总 加 d 乘 d。
        i 为 i 加 1。
    完。
    返还 总 除以 (做 计数 用 数据 减 1)。
完。

概率分布_统计_中位数 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    可变 排序 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 排序、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 排序 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 排序：
            若 排序 的 第 j 项 小于 排序 的 第 i 项：
                t 为 排序 的 第 i 项。
                排序 的 第 i 项 为 排序 的 第 j 项。
                排序 的 第 j 项 为 t。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    n 为 做 计数 用 排序。
    若 n 取余于 2 等于 1：
        返还 排序 的 第 n 除以 2 项。
    完。
    返还 (排序 的 第 n 除以 2 减 1 项 加 排序 的 第 n 除以 2 项) 除以 2。
完。

贝叶斯_更新 接受 先验、似然、证据：
    若 证据 等于 0：
        返还 先验。
    完。
    返还 先验 乘 似然 除以 证据。
完。

贝叶斯_批量更新 接受 先验列表、似然列表：
    可变 后验 为 一组：（空）。
    可变 证据 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 先验列表：
        证据 为 证据 加 先验列表 的 第 i 项 乘 似然列表 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 先验列表：
        做 追加 用 后验、先验列表 的 第 i 项 乘 似然列表 的 第 i 项 除以 (证据 加 1)。
        i 为 i 加 1。
    完。
    返还 后验。
完。

可变 _概率_采样次数 为 0。

可变 _概率_分布类型数 为 0。

概率分布_概率_记录采样 接受 零参：
    _概率_采样次数 为 _概率_采样次数 加 1。
完。

概率_记录分布 接受 零参：
    _概率_分布类型数 为 _概率_分布类型数 加 1。
完。

本章 收束。
