《联邦学习》

引用《..·基础》。

引用《..·算法·数学扩展》。

FL_随机态 为 88888。

FL_随机 接受 零参：
    FL_随机态 为 FL_随机态 位异或 FL_随机态 左移 13。
    FL_随机态 为 FL_随机态 位异或 FL_随机态 右移 7。
    FL_随机态 为 FL_随机态 位异或 FL_随机态 左移 17。
    若 FL_随机态 小于 0：
        FL_随机态 为 0 减 FL_随机态。
    完。
    返还 FL_随机态。
完。

客户端_创建 接受 id、参数维度、数据量：
    可变 参数 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数维度：
        做 追加 用 参数、0。
        i 为 i 加 1。
    完。
    返还 一组：id、参数、数据量、5、10。
完。

客户端_ID 接受 c：
    返还 c 的 第 0 项。
完。

客户端_参数 接受 c：
    返还 c 的 第 1 项。
完。

客户端_数据量 接受 c：
    返还 c 的 第 2 项。
完。

客户端_下载全局 接受 客户端、全局参数：
    n 为 做 计数 用 全局参数。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 做 计数 用 客户端 的 第 1 项：
            客户端 的 第 1 项 的 第 i 项 为 全局参数 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
完。

客户端_本地训练 接受 客户端、梯度：
    n 为 做 计数 用 客户端 的 第 1 项。
    lr 为 客户端 的 第 4 项。
    可变 轮 为 0。
    只要 轮 小于 客户端 的 第 3 项：
        可变 i 为 0。
        只要 i 小于 n：
            若 i 小于 做 计数 用 梯度：
                客户端 的 第 1 项 的 第 i 项 为 客户端 的 第 1 项 的 第 i 项 减 lr 乘 梯度 的 第 i 项 除以 1000。
            完。
            i 为 i 加 1。
        完。
        轮 为 轮 加 1。
    完。
完。

FedAvg_聚合 接受 客户端列表、参数维度：
    K 为 做 计数 用 客户端列表。
    若 K 等于 0：
        返还 一组：（空）。
    完。
    可变 i 为 0。
    只要 i 小于 K：
        总数据 为 总数据 加 做 客户端_数据量 用 客户端列表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 总数据 等于 0：
        总数据 为 1。
    完。
    可变 全局 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 参数维度：
        做 追加 用 全局、0。
        d 为 d 加 1。
    完。
    i 为 0。
    只要 i 小于 K：
        权重 为 做 客户端_数据量 用 客户端列表 的 第 i 项 乘 1000 除以 总数据。
        参数 为 做 客户端_参数 用 客户端列表 的 第 i 项。
        d 为 0。
        只要 d 小于 参数维度：
            若 d 小于 做 计数 用 参数：
                全局 的 第 d 项 为 全局 的 第 d 项 加 权重 乘 参数 的 第 d 项 除以 1000。
            完。
            d 为 d 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 全局。
完。

FedProx_损失 接受 本地参数、全局参数、μ：
    若 μ 小于或等于 0：
        μ 为 10。
    完。
    n 为 做 计数 用 本地参数。
    可变 正则 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 g 为 0。
        若 i 小于 做 计数 用 全局参数：
            g 为 全局参数 的 第 i 项。
        完。
        d 为 本地参数 的 第 i 项 减 g。
        正则 为 正则 加 d 乘 d 除以 1000。
        i 为 i 加 1。
    完。
    返还 μ 乘 正则 除以 (2 乘 1000)。
完。

DP_梯度裁剪 接受 梯度、C：
    n 为 做 计数 用 梯度。
    可变 范数 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        范数 为 范数 加 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    范数 为 做 整数sqrt 用 范数 乘 1000。
    若 范数 小于或等于 C：
        返还 梯度。
    完。
    可变 裁剪 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 裁剪、梯度 的 第 i 项 乘 C 除以 范数。
        i 为 i 加 1。
    完。
    返还 裁剪。
完。

DP_加噪 接受 梯度、σ、C：
    裁剪梯度 为 做 DP_梯度裁剪 用 梯度、C。
    n 为 做 计数 用 裁剪梯度。
    可变 噪声梯度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        噪声 为 做 FL_随机 取余于 (2 乘 σ 加 1) 减 σ。
        做 追加 用 噪声梯度、裁剪梯度 的 第 i 项 加 噪声 乘 C 除以 1000。
        i 为 i 加 1。
    完。
    返还 噪声梯度。
完。

DP_隐私预算 接受 σ、_δ、采样率、轮数：
    若 σ 等于 0：
        返还 0x7FFFFFFF。
    完。
    组合 为 做 整数sqrt 用 2 乘 轮数 乘 1000。
    返还 采样率 乘 组合 除以 σ。
完。

随机选择客户端 接受 客户端总数、选择数：
    可变 选中 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 选择数：
        索引 为 做 FL_随机 取余于 客户端总数。
        做 追加 用 选中、索引。
        i 为 i 加 1。
    完。
    返还 选中。
完。

基于贡献选择 接受 客户端列表、贡献分数、选择数：
    n 为 做 计数 用 客户端列表。
    可变 索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 索引、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 贡献分数 的 第 索引 的 第 j 项 项 大于 贡献分数 的 第 索引 的 第 i 项 项：
                临时 为 索引 的 第 i 项。
                索引 的 第 i 项 为 索引 的 第 j 项。
                索引 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 选中 为 一组：（空）。
    i 为 0。
    只要 i 小于 选择数 并且 i 小于 n：
        做 追加 用 选中、索引 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 选中。
完。

TopK_压缩 接受 梯度、k：
    n 为 做 计数 用 梯度。
    可变 绝对值 为 一组：（空）。
    可变 索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 v 为 梯度 的 第 i 项。
        若 v 小于 0：
            v 为 0 减 v。
        完。
        做 追加 用 绝对值、v。
        做 追加 用 索引、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n 减 1 并且 i 小于 k：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 绝对值 的 第 索引 的 第 j 项 项 大于 绝对值 的 第 索引 的 第 i 项 项：
                临时 为 索引 的 第 i 项。
                索引 的 第 i 项 为 索引 的 第 j 项。
                索引 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 稀疏 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 稀疏、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 k 并且 i 小于 n：
        稀疏 的 第 索引 的 第 i 项 项 为 梯度 的 第 索引 的 第 i 项 项。
        i 为 i 加 1。
    完。
    返还 稀疏。
完。

量化_压缩 接受 梯度、级数：
    n 为 做 计数 用 梯度。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 min_g 为 梯度 的 第 0 项。
    可变 max_g 为 梯度 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 梯度 的 第 i 项 小于 min_g：
            min_g 为 梯度 的 第 i 项。
        完。
        若 梯度 的 第 i 项 大于 max_g：
            max_g 为 梯度 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    范围 为 max_g 减 min_g。
    若 范围 等于 0：
        返还 梯度。
    完。
    可变 量化 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        归一 为 (梯度 的 第 i 项 减 min_g) 乘 级数 除以 范围。
        做 追加 用 量化、归一。
        i 为 i 加 1。
    完。
    返还 一组：量化、min_g、max_g、级数。
完。

个性化_混合 接受 全局参数、本地参数、α：
    n 为 做 计数 用 全局参数。
    可变 混合 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 本地 为 0。
        若 i 小于 做 计数 用 本地参数：
            本地 为 本地参数 的 第 i 项。
        完。
        值 为 α 乘 本地 除以 1000 加 (1000 减 α) 乘 全局参数 的 第 i 项 除以 1000。
        做 追加 用 混合、值。
        i 为 i 加 1。
    完。
    返还 混合。
完。

FL_统计_创建 接受 零参：
    返还 一组：0、0、0、一组：（空）。
完。

FL_记录轮次 接受 统计、精度、通信字节：
    统计 的 第 0 项 为 统计 的 第 0 项 加 1。
    统计 的 第 1 项 为 统计 的 第 1 项 加 通信字节。
    若 精度 大于 统计 的 第 2 项：
        统计 的 第 2 项 为 精度。
        统计 的 第 3 项 为 统计 的 第 0 项。
    完。
    做 追加 用 统计 的 第 4 项、精度。
完。

本章 收束。
