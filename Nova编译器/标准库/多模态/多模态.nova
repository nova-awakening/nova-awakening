《多模态》

引用《..·基础》。

引用《..·数学·基础·数学库》。

引用《..·数学·线性代数》。

引用《..·文本·自然语言》。

引用《..·..·运行时·智能推理·注意力机制》。

多模态_最小长度 接受 a、b：
    可变 长 为 做 计数 用 a。
    若 做 计数 用 b 小于 长：
        长 为 做 计数 用 b。
    完。
    返还 长。
完。

多模态_余弦相似 接受 a、b：
    点积 为 做 向量_点积 用 a、b、0。
    na 为 做 向量_范数 用 a、0。
    nb 为 做 向量_范数 用 b、0。
    若 na 等于 0：
        返还 0。
    完。
    若 nb 等于 0：
        返还 0。
    完。
    返还 点积 乘 1000 除以 (na 乘 nb)。
完。

跨模态_匹配 接受 图像特征、文本特征列表：
    若 做 计数 用 文本特征列表 等于 0：
        返还 负 1。
    完。
    可变 最佳 为 0。
    可变 最佳分 为 做 余弦相似 用 图像特征、文本特征列表 的 第 0 项。
    可变 j 为 1。
    只要 j 小于 做 计数 用 文本特征列表：
        分数 为 做 余弦相似 用 图像特征、文本特征列表 的 第 j 项。
        若 分数 大于 最佳分：
            最佳 为 j。
            最佳分 为 分数。
        完。
        j 为 j 加 1。
    完。
    返还 最佳。
完。

融合_拼接 接受 特征A、特征B：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 特征A：
        做 追加 用 结果、特征A 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 特征B：
        做 追加 用 结果、特征B 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

融合_加权 接受 特征A、特征B、权重A：
    可变 结果 为 一组：（空）。
    权重B 为 100 减 权重A。
    长 为 做 多模态_最小长度 用 特征A、特征B。
    可变 i 为 0。
    只要 i 小于 长：
        做 追加 用 结果、(特征A 的 第 i 项 乘 权重A 加 特征B 的 第 i 项 乘 权重B) 除以 100。
        i 为 i 加 1。
    完。
    返还 结果。
完。

图像_均值特征 接受 像素、特征维度：
    总 为 做 计数 用 像素。
    若 特征维度 等于 0：
        特征维度 为 1。
    完。
    可变 每块 为 总 除以 特征维度。
    若 每块 等于 0：
        每块 为 1。
    完。
    可变 特征数据 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 特征维度：
        可变 和值 为 0。
        可变 i 为 d 乘 每块。
        可变 末尾 为 i 加 每块。
        若 末尾 大于 总：
            末尾 为 总。
        完。
        只要 i 小于 末尾：
            和值 为 和值 加 像素 的 第 i 项。
            i 为 i 加 1。
        完。
        做 追加 用 特征数据、和值 除以 每块。
        d 为 d 加 1。
    完。
    返还 特征数据。
完。

音频_能量特征 接受 样本、特征维度：
    总 为 做 计数 用 样本。
    可变 每块 为 总 除以 特征维度。
    若 每块 等于 0：
        每块 为 1。
    完。
    可变 特征数据 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 特征维度：
        可变 和值 为 0。
        可变 i 为 d 乘 每块。
        可变 末尾 为 i 加 每块。
        若 末尾 大于 总：
            末尾 为 总。
        完。
        只要 i 小于 末尾：
            和值 为 和值 加 做 绝对值 用 样本 的 第 i 项。
            i 为 i 加 1。
        完。
        做 追加 用 特征数据、和值 除以 每块。
        d 为 d 加 1。
    完。
    返还 特征数据。
完。

融合_门控 接受 特征A、特征B、门值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 特征A：
        若 i 小于 做 计数 用 特征B：
            做 追加 用 结果、(特征A 的 第 i 项 乘 门值 加 特征B 的 第 i 项 乘 (100 减 门值)) 除以 100。
        否则：
            做 追加 用 结果、特征A 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

特征_投影 接受 输入、投影维度：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 投影维度：
        若 i 小于 做 计数 用 输入：
            做 追加 用 结果、输入 的 第 i 项。
        否则：
            做 追加 用 结果、0。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

多查询_匹配 接受 查询列表、候选列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 查询列表：
        做 追加 用 结果、做 跨模态_匹配 用 查询列表 的 第 i 项、候选列表。
        i 为 i 加 1。
    完。
    返还 结果。
完。

图文_拼接融合 接受 图像特征、文本特征：
    返还 做 融合_拼接 用 图像特征、文本特征。
完。

图文_门控融合 接受 图像特征、文本特征、门控权重：
    可变 结果 为 一组：（空）。
    长 为 做 多模态_最小长度 用 图像特征、文本特征。
    可变 i 为 0。
    只要 i 小于 长：
        做 追加 用 结果、图像特征 的 第 i 项 乘 门控权重 除以 100 加 文本特征 的 第 i 项 乘 (100 减 门控权重) 除以 100。
        i 为 i 加 1。
    完。
    返还 结果。
完。

图文_交叉注意力 接受 查询、键值：
    可变 分数列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键值：
        可变 分数 为 0。
        长 为 做 多模态_最小长度 用 查询、键值 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 长：
            分数 为 分数 加 查询 的 第 j 项 乘 键值 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 分数列表、分数。
        i 为 i 加 1。
    完。
    返还 分数列表。
完。

多模态_音频_能量 接受 采样、窗口大小：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 窗口大小 小于或等于 做 计数 用 采样：
        可变 能量 为 0。
        可变 j 为 0。
        只要 j 小于 窗口大小：
            能量 为 能量 加 采样 的 第 i 加 j 项 乘 采样 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、能量 除以 窗口大小。
        i 为 i 加 窗口大小。
    完。
    返还 结果。
完。

多模态_音频_过零率 接受 采样、窗口大小：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 窗口大小 小于或等于 做 计数 用 采样：
        可变 过零 为 0。
        可变 j 为 1。
        只要 j 小于 窗口大小：
            可变 当前 为 采样 的 第 i 加 j 项。
            可变 前一 为 采样 的 第 i 加 j 减 1 项。
            可变 跨零 为 假。
            若 当前 大于或等于 0：
                若 前一 小于 0：
                    跨零 为 真。
                完。
            完。
            若 跨零 等于 假：
                若 当前 小于 0：
                    若 前一 大于或等于 0：
                        跨零 为 真。
                    完。
                完。
            完。
            若 跨零：
                过零 为 过零 加 1。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、过零 乘 1000 除以 窗口大小。
        i 为 i 加 窗口大小。
    完。
    返还 结果。
完。

对比学习_损失 接受 锚点、正样本、负样本列表、温度：
    可变 正分 为 0。
    正长 为 做 多模态_最小长度 用 锚点、正样本。
    可变 i 为 0。
    只要 i 小于 正长：
        正分 为 正分 加 锚点 的 第 i 项 乘 正样本 的 第 i 项。
        i 为 i 加 1。
    完。
    正分 为 正分 乘 1000 除以 (温度 加 1)。
    可变 负总 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 负样本列表：
        可变 负分 为 0。
        负长 为 做 多模态_最小长度 用 锚点、负样本列表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 负长：
            负分 为 负分 加 锚点 的 第 j 项 乘 负样本列表 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        负总 为 负总 加 负分 乘 1000 除以 (温度 加 1)。
        i 为 i 加 1。
    完。
    返还 负总 减 正分。
完。

可变 _多模态_融合数 为 0。

可变 _多模态_检索数 为 0。

多模态_记录融合 接受 零参：
    _多模态_融合数 为 _多模态_融合数 加 1。
完。

多模态_记录检索 接受 零参：
    _多模态_检索数 为 _多模态_检索数 加 1。
完。

本章 收束。
