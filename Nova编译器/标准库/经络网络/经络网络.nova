《经络网络》

引用《..·基础》。

引用《..·数学·基础·数学库》。

可变 经络 为 一组：（空）。

经络_初始化 接受 零参：
    经络 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 12：
        可变 阴阳 为 0。
        若 i 大于或等于 3 并且 i 小于或等于 5：
            阴阳 为 1。
        完。
        若 i 大于或等于 9：
            阴阳 为 1。
        完。
        做 追加 用 经络、一组：i、i 加 1000、0、50 加 i 乘 5、负 1、阴阳。
        i 为 i 加 1。
    完。
    经络 的 第 0 项 的 第 4 项 为 3。
    经络 的 第 3 项 的 第 4 项 为 0。
    经络 的 第 1 项 的 第 4 项 为 4。
    经络 的 第 4 项 的 第 4 项 为 1。
    经络 的 第 2 项 的 第 4 项 为 5。
    经络 的 第 5 项 的 第 4 项 为 2。
    经络 的 第 6 项 的 第 4 项 为 9。
    经络 的 第 9 项 的 第 4 项 为 6。
    经络 的 第 7 项 的 第 4 项 为 10。
    经络 的 第 10 项 的 第 4 项 为 7。
    经络 的 第 8 项 的 第 4 项 为 11。
    经络 的 第 11 项 的 第 4 项 为 8。
完。

经络_激活 接受 id、信号：
    tau 为 经络 的 第 id 项 的 第 3 项。
    导数 为 (信号 减 经络 的 第 id 项 的 第 2 项) 乘 100 除以 tau。
    经络 的 第 id 项 的 第 2 项 为 经络 的 第 id 项 的 第 2 项 加 导数 除以 10。
    若 经络 的 第 id 项 的 第 2 项 大于 100：
        经络 的 第 id 项 的 第 2 项 为 100。
    完。
    若 经络 的 第 id 项 的 第 2 项 小于 0：
        经络 的 第 id 项 的 第 2 项 为 0。
    完。
完。

经络_读取 接受 id：
    返还 经络 的 第 id 项 的 第 2 项。
完。

经络_表里传导 接受 id：
    配对 为 经络 的 第 id 项 的 第 4 项。
    若 配对 大于或等于 0：
        传导 为 经络 的 第 id 项 的 第 2 项 乘 30 除以 100。
        做 经络_激活 用 配对、经络 的 第 配对 项 的 第 2 项 加 传导。
    完。
完。

阴阳_平衡度 接受 零参：
    可变 阴总 为 0。
    可变 阳数 为 0。
    可变 阴数 为 0。
    可变 阳总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 经络：
        若 经络 的 第 i 项 的 第 5 项 等于 0：
            阴总 为 阴总 加 经络 的 第 i 项 的 第 2 项。
            阴数 为 阴数 加 1。
        否则：
            阳总 为 阳总 加 经络 的 第 i 项 的 第 2 项。
            阳数 为 阳数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 阴数 等于 0：
        阴数 为 1。
    完。
    若 阳数 等于 0：
        阳数 为 1。
    完。
    阴均 为 阴总 除以 阴数。
    阳均 为 阳总 除以 阳数。
    返还 100 减 做 绝对值 用 阴均 减 阳均。
完。

气血循环 接受 零参：
    可变 i 为 0。
    只要 i 小于 12：
        下一 为 (i 加 1) 取余于 12。
        传导 为 经络 的 第 i 项 的 第 2 项 乘 20 除以 100。
        做 经络_激活 用 下一、经络 的 第 下一 项 的 第 2 项 加 传导。
        i 为 i 加 1。
    完。
完。

经络_总激活 接受 零参：
    返还 做 折叠 用 经络、0、以 s、x 映射到 s 加 x 的 第 2 项。
完。

经络_活跃数 接受 零参：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 经络：
        若 经络 的 第 i 项 的 第 2 项 大于 0：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

可变 穴位列表 为 一组：（空）。

穴位_注册 接受 经络id、位置、功能哈希：
    做 追加 用 穴位列表、一组：经络id、位置、0、功能哈希。
    返还 做 计数 用 穴位列表 减 1。
完。

穴位_刺激 接受 索引、强度：
    穴位列表 的 第 索引 项 的 第 2 项 为 穴位列表 的 第 索引 项 的 第 2 项 加 强度。
    若 穴位列表 的 第 索引 项 的 第 2 项 大于 100：
        穴位列表 的 第 索引 项 的 第 2 项 为 100。
    完。
    做 经络_激活 用 穴位列表 的 第 索引 项 的 第 0 项、穴位列表 的 第 索引 项 的 第 2 项。
完。

穴位_数量 接受 零参：
    返还 做 计数 用 穴位列表。
完。

经络_最强 接受 零参：
    可变 最优 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 经络：
        若 经络 的 第 i 项 的 第 2 项 大于 经络 的 第 最优 项 的 第 2 项：
            最优 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最优。
完。

经络_最弱 接受 零参：
    可变 weakest 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 经络：
        若 经络 的 第 i 项 的 第 2 项 小于 经络 的 第 weakest 项 的 第 2 项：
            weakest 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 weakest。
完。

经络_报告 接受 零参：
    做 打印 用 「╔═══ 经络状态 ═══╗」。
    做 打印 用 「║ 总激活: 」 加 做 转字符串 用 做 经络_总激活 加 「 活跃: 」 加 做 转字符串 用 做 经络_活跃数 加 「/12」。
    做 打印 用 「║ 阴阳平衡: 」 加 做 转字符串 用 做 阴阳_平衡度 加 「%」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 经络：
        m 为 经络 的 第 i 项。
        可变 类型 为 「阴」。
        若 m 的 第 5 项 等于 1：
            类型 为 「阳」。
        完。
        做 打印 用 「║ [」 加 做 转字符串 用 m 的 第 0 项 加 「] 」 加 类型 加 「 激活=」 加 做 转字符串 用 m 的 第 2 项 加 「 τ=」 加 做 转字符串 用 m 的 第 3 项 加 「 配对=」 加 做 转字符串 用 m 的 第 4 项。
        i 为 i 加 1。
    完。
    做 打印 用 「║ 最强: 」 加 做 转字符串 用 做 经络_最强 加 「 最弱: 」 加 做 转字符串 用 做 经络_最弱。
    做 打印 用 「╚════════════════╝」。
完。

多轮循环 接受 轮数：
    可变 i 为 0。
    只要 i 小于 轮数：
        做 气血循环。
        i 为 i 加 1。
    完。
完。

经络_重置 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 经络：
        经络 的 第 i 项 的 第 2 项 为 0。
        i 为 i 加 1。
    完。
完。

AI经络_创建 接受 通道数：
    经络 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 通道数：
        做 追加 用 经络、一组：i、i 加 2000、0、30 加 i 乘 10、(i 加 通道数 除以 2) 取余于 通道数、i 取余于 2。
        i 为 i 加 1。
    完。
完。

经络_脉冲传播 接受 源穴位、强度、衰减率：
    可变 队列 为 一组：一组：源穴位、强度。
    可变 新队列 为 一组：（空）。
    可变 总激活 为 0。
    可变 已访问 为 一组：（空）。
    只要 做 计数 用 队列 大于 0：
        当前 为 队列 的 第 0 项。
        可变 i 为 1。
        只要 i 小于 做 计数 用 队列：
            做 追加 用 新队列、队列 的 第 i 项。
            i 为 i 加 1。
        完。
        队列 为 新队列。
        穴位 为 当前 的 第 0 项。
        能量 为 当前 的 第 1 项。
        若 能量 小于 5：
            继续。
        完。
        可变 已到 为 假。
        i 为 0。
        只要 i 小于 做 计数 用 已访问：
            若 已访问 的 第 i 项 等于 穴位：
                已到 为 真。
            完。
            i 为 i 加 1。
        完。
        若 已到：
            继续。
        完。
        做 追加 用 已访问、穴位。
        总激活 为 总激活 加 能量。
        i 为 0。
        只要 i 小于 做 计数 用 经络：
            若 经络 的 第 i 项 的 第 0 项 等于 穴位：
                下游 为 经络 的 第 i 项 的 第 4 项。
                新能量 为 能量 乘 衰减率 除以 100。
                做 追加 用 队列、一组：下游、新能量。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 总激活。
完。

可变 穴位_状态 为 一组：（空）。

穴位_初始化 接受 数量：
    穴位_状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 数量：
        做 追加 用 穴位_状态、一组：0、100、0。
        i 为 i 加 1。
    完。
完。

穴位_激活 接受 id、强度：
    若 id 大于或等于 0 并且 id 小于 做 计数 用 穴位_状态：
        穴位_状态 的 第 id 项 的 第 0 项 为 穴位_状态 的 第 id 项 的 第 0 项 加 强度。
        穴位_状态 的 第 id 项 的 第 2 项 为 穴位_状态 的 第 id 项 的 第 2 项 加 1。
    完。
完。

穴位_能量 接受 id：
    若 id 大于或等于 0 并且 id 小于 做 计数 用 穴位_状态：
        返还 穴位_状态 的 第 id 项 的 第 0 项。
    完。
    返还 0。
完。

穴位_衰减全部 接受 衰减率：
    可变 i 为 0。
    只要 i 小于 做 计数 用 穴位_状态：
        穴位_状态 的 第 i 项 的 第 0 项 为 穴位_状态 的 第 i 项 的 第 0 项 乘 衰减率 除以 100。
        i 为 i 加 1。
    完。
完。

气血_流动步 接受 经络列表、穴位列表、流速：
    可变 i 为 0。
    只要 i 小于 做 计数 用 经络列表：
        源 为 经络列表 的 第 i 项 的 第 0 项。
        目标 为 经络列表 的 第 i 项 的 第 4 项。
        流量 为 穴位列表 的 第 源 项 的 第 0 项 乘 流速 除以 100。
        穴位列表 的 第 源 项 的 第 0 项 为 穴位列表 的 第 源 项 的 第 0 项 减 流量。
        穴位列表 的 第 目标 项 的 第 0 项 为 穴位列表 的 第 目标 项 的 第 0 项 加 流量。
        i 为 i 加 1。
    完。
完。

气血_总能量 接受 穴位列表：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 穴位列表：
        总 为 总 加 穴位列表 的 第 i 项 的 第 0 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

气血_平衡度 接受 穴位列表：
    n 为 做 计数 用 穴位列表。
    若 n 小于 2：
        返还 1000。
    完。
    可变 均值 为 做 气血_总能量 用 穴位列表 除以 n。
    可变 方差 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        d 为 穴位列表 的 第 i 项 的 第 0 项 减 均值。
        方差 为 方差 加 d 乘 d。
        i 为 i 加 1。
    完。
    返还 1000 减 方差 除以 n。
完。

本章 收束。
