《面向对象》

引用《..·基础》。

可变 类表 为 一组：（空）。

可变 下一类ID 为 1。

类_定义 接受 父类ID：
    类ID 为 下一类ID。
    下一类ID 为 下一类ID 加 1。
    做 追加 用 类表、一组：类ID、父类ID、一组：（空）、一组：（空）、一组：（空）。
    返还 类ID。
完。

类_查表 接受 类ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 类表：
        若 类表 的 第 i 项 的 第 0 项 等于 类ID：
            返还 类表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

类_添加字段 接受 类ID、字段名哈希：
    类别 为 做 类_查表 用 类ID。
    若 类别 等于 空：
        返还 负 1。
    完。
    索引 为 做 计数 用 类别 的 第 2 项。
    做 追加 用 类别 的 第 2 项、字段名哈希。
    返还 索引。
完。

类_添加方法 接受 类ID、方法名哈希、方法体：
    类别 为 做 类_查表 用 类ID。
    若 类别 等于 空：
        返还 负 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 类别 的 第 3 项：
        若 类别 的 第 3 项 的 第 i 项 等于 方法名哈希：
            类别 的 第 4 项 的 第 i 项 为 方法体。
            返还 i。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 类别 的 第 3 项、方法名哈希。
    做 追加 用 类别 的 第 4 项、方法体。
    返还 做 计数 用 类别 的 第 3 项 减 1。
完。

类_字段数 接受 类ID：
    类别 为 做 类_查表 用 类ID。
    若 类别 等于 空：
        返还 0。
    完。
    返还 做 计数 用 类别 的 第 2 项。
完。

类_方法数 接受 类ID：
    类别 为 做 类_查表 用 类ID。
    若 类别 等于 空：
        返还 0。
    完。
    返还 做 计数 用 类别 的 第 3 项。
完。

类_父类 接受 类ID：
    类别 为 做 类_查表 用 类ID。
    若 类别 等于 空：
        返还 0。
    完。
    返还 类别 的 第 1 项。
完。

对象_创建 接受 类ID：
    类别 为 做 类_查表 用 类ID。
    若 类别 等于 空：
        返还 空。
    完。
    字段数 为 做 计数 用 类别 的 第 2 项。
    可变 总字段 为 字段数。
    父ID 为 类别 的 第 1 项。
    若 父ID 不等于 0：
        父cls 为 做 类_查表 用 父ID。
        若 父cls 不等于 空：
            总字段 为 总字段 加 做 计数 用 父cls 的 第 2 项。
        完。
    完。
    可变 字段值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 总字段：
        做 追加 用 字段值、0。
        i 为 i 加 1。
    完。
    返还 一组：类ID、1、字段值、0。
完。

对象_类ID 接受 对象：
    返还 对象 的 第 0 项。
完。

对象_引用数 接受 对象：
    返还 对象 的 第 1 项。
完。

对象_活跃 接受 对象：
    返还 对象 的 第 3 项 等于 0。
完。

对象_获取字段 接受 对象、字段idx：
    若 字段idx 大于或等于 0 并且 字段idx 小于 做 计数 用 对象 的 第 2 项：
        返还 对象 的 第 2 项 的 第 字段idx 项。
    完。
    返还 空。
完。

对象_设置字段 接受 对象、字段idx、值：
    若 字段idx 大于或等于 0 并且 字段idx 小于 做 计数 用 对象 的 第 2 项：
        对象 的 第 2 项 的 第 字段idx 项 为 值。
    完。
完。

对象_增引用 接受 对象：
    对象 的 第 1 项 为 对象 的 第 1 项 加 1。
    返还 对象 的 第 1 项。
完。

对象_减引用 接受 对象：
    对象 的 第 1 项 为 对象 的 第 1 项 减 1。
    若 对象 的 第 1 项 小于或等于 0：
        对象 的 第 3 项 为 1。
    完。
    返还 对象 的 第 1 项。
完。

方法_查找 接受 类ID、方法名哈希：
    可变 当前ID 为 类ID。
    只要 当前ID 不等于 0：
        类别 为 做 类_查表 用 当前ID。
        若 类别 等于 空：
            返还 空。
        完。
        可变 i 为 0。
        只要 i 小于 做 计数 用 类别 的 第 3 项：
            若 类别 的 第 3 项 的 第 i 项 等于 方法名哈希：
                返还 类别 的 第 4 项 的 第 i 项。
            完。
            i 为 i 加 1。
        完。
        当前ID 为 类别 的 第 1 项。
    完。
    返还 空。
完。

是实例 接受 对象、类ID：
    可变 当前ID 为 对象 的 第 0 项。
    只要 当前ID 不等于 0：
        若 当前ID 等于 类ID：
            返还 真。
        完。
        类别 为 做 类_查表 用 当前ID。
        若 类别 等于 空：
            返还 假。
        完。
        当前ID 为 类别 的 第 1 项。
    完。
    返还 假。
完。

可变 接口表 为 一组：（空）。

可变 下一接口ID 为 1。

可变 实现表 为 一组：（空）。

接口_定义 接受 零参：
    接口ID 为 下一接口ID。
    下一接口ID 为 下一接口ID 加 1。
    做 追加 用 接口表、一组：接口ID、一组：（空）。
    返还 接口ID。
完。

接口_添加方法 接受 接口ID、方法名哈希：
    可变 i 为 0。
    只要 i 小于 做 计数 用 接口表：
        若 接口表 的 第 i 项 的 第 0 项 等于 接口ID：
            做 追加 用 接口表 的 第 i 项 的 第 1 项、方法名哈希。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

接口_实现 接受 类ID、接口ID：
    做 追加 用 实现表、一组：类ID、接口ID。
完。

实现了接口 接受 类ID、接口ID：
    可变 i 为 0。
    只要 i 小于 做 计数 用 实现表：
        若 实现表 的 第 i 项 的 第 0 项 等于 类ID 并且 实现表 的 第 i 项 的 第 1 项 等于 接口ID：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

接口_总数 接受 零参：
    返还 做 计数 用 接口表。
完。

可变 虚表列表 为 一组：（空）。

虚表_构建 接受 类ID：
    可变 方法IDs 为 一组：（空）。
    可变 方法体s 为 一组：（空）。
    可变 当前ID 为 类ID。
    只要 当前ID 不等于 0：
        类别 为 做 类_查表 用 当前ID。
        若 类别 等于 空：
            当前ID 为 0。
        否则：
            可变 i 为 0。
            只要 i 小于 做 计数 用 类别 的 第 3 项：
                可变 已存在 为 假。
                可变 j 为 0。
                只要 j 小于 做 计数 用 方法IDs：
                    若 方法IDs 的 第 j 项 等于 类别 的 第 3 项 的 第 i 项：
                        已存在 为 真。
                    完。
                    j 为 j 加 1。
                完。
                若 已存在 等于 假：
                    做 追加 用 方法IDs、类别 的 第 3 项 的 第 i 项。
                    做 追加 用 方法体s、类别 的 第 4 项 的 第 i 项。
                完。
                i 为 i 加 1。
            完。
            当前ID 为 类别 的 第 1 项。
        完。
    完。
    做 追加 用 虚表列表、一组：类ID、方法IDs、方法体s。
    返还 做 计数 用 虚表列表 减 1。
完。

虚表_调用 接受 vtable_idx、方法名哈希：
    vt 为 虚表列表 的 第 vtable_idx 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 vt 的 第 1 项：
        若 vt 的 第 1 项 的 第 i 项 等于 方法名哈希：
            返还 vt 的 第 2 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

本章 收束。
