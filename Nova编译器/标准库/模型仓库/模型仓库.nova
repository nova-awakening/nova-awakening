《模型仓库》

引用《..·基础》。

模型状态_草稿 为 0。

模型状态_发布 为 1。

模型状态_部署 为 2。

模型状态_回滚 为 3。

模型状态_归档 为 4。

可变 仓库 为 一组：（空）。

可变 仓库_下一ID 为 0。

模型_数量 接受 零参：
    返还 做 计数 用 仓库。
完。

模型_状态 接受 id：
    返还 仓库 的 第 id 项 的 第 3 项。
完。

模型_设状态 接受 id、s：
    仓库 的 第 id 项 的 第 3 项 为 s。
完。

模型_设精度 接受 id、精度：
    仓库 的 第 id 项 的 第 5 项 为 精度。
完。

模型_精度 接受 id：
    返还 仓库 的 第 id 项 的 第 5 项。
完。

模型_版本比较 接受 i、j：
    vi 为 仓库 的 第 i 项 的 第 2 项。
    vj 为 仓库 的 第 j 项 的 第 2 项。
    若 vi 的 第 0 项 不等于 vj 的 第 0 项：
        返还 vi 的 第 0 项 减 vj 的 第 0 项。
    完。
    若 vi 的 第 1 项 不等于 vj 的 第 1 项：
        返还 vi 的 第 1 项 减 vj 的 第 1 项。
    完。
    返还 vi 的 第 2 项 减 vj 的 第 2 项。
完。

模型_查找最新 接受 名哈希：
    可变 最佳 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 仓库：
        若 仓库 的 第 i 项 的 第 1 项 等于 名哈希：
            若 最佳 等于 负 1：
                最佳 为 i。
            否则若 做 模型_版本比较 用 i、最佳 大于 0：
                最佳 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

模型_已部署 接受 名哈希：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 仓库：
        若 仓库 的 第 i 项 的 第 1 项 等于 名哈希 并且 仓库 的 第 i 项 的 第 3 项 等于 模型状态_部署：
            做 追加 用 r、i。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

AB测试 接受 模型A、模型B：
    若 仓库 的 第 模型A 项 的 第 5 项 大于 仓库 的 第 模型B 项 的 第 5 项：
        返还 模型A。
    完。
    返还 模型B。
完。

模型_回滚 接受 名哈希：
    部署列表 为 做 模型_已部署 用 名哈希。
    若 做 计数 用 部署列表 等于 0：
        返还 假。
    完。
    当前 为 部署列表 的 第 做 计数 用 部署列表 减 1 项。
    仓库 的 第 当前 项 的 第 3 项 为 模型状态_回滚。
    返还 真。
完。

仓库_清空 接受 零参：
    仓库 为 一组：（空）。
    仓库_下一ID 为 0。
完。

模型_已发布数 接受 零参：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 仓库：
        若 仓库 的 第 i 项 的 第 3 项 等于 模型状态_发布：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

仓库_模型_注册 接受 名哈希、主版本、次版本、修订、参数量：
    id 为 仓库_下一ID。
    仓库_下一ID 为 仓库_下一ID 加 1。
    做 追加 用 仓库、一组：id、名哈希、一组：主版本、次版本、修订、模型状态_草稿、参数量、0、一组：（空）。
    返还 id。
完。

模型_发布 接受 id：
    仓库 的 第 id 项 的 第 3 项 为 模型状态_发布。
完。

模型_部署 接受 id：
    仓库 的 第 id 项 的 第 3 项 为 模型状态_部署。
完。

模型_归档 接受 id：
    仓库 的 第 id 项 的 第 3 项 为 模型状态_归档。
完。

模型_按状态 接受 状态：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 仓库：
        若 仓库 的 第 i 项 的 第 3 项 等于 状态：
            做 追加 用 r、i。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

模型_按精度排名 接受 前N：
    可变 索引列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 仓库：
        做 追加 用 索引列表、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 索引列表：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 索引列表：
            若 仓库 的 第 索引列表 的 第 j 项 项 的 第 5 项 大于 仓库 的 第 索引列表 的 第 i 项 项 的 第 5 项：
                临时 为 索引列表 的 第 i 项。
                索引列表 的 第 i 项 为 索引列表 的 第 j 项。
                索引列表 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 前N 并且 i 小于 做 计数 用 索引列表：
        做 追加 用 结果、索引列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

AB测试_多模型 接受 模型列表：
    若 做 计数 用 模型列表 等于 0：
        返还 负 1。
    完。
    可变 最佳 为 模型列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 模型列表：
        若 仓库 的 第 模型列表 的 第 i 项 项 的 第 5 项 大于 仓库 的 第 最佳 项 的 第 5 项：
            最佳 为 模型列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

模型_添加元 接受 id、键、值：
    做 追加 用 仓库 的 第 id 项 的 第 6 项、一组：键、值。
完。

模型_获取元 接受 id、键：
    可变 i 为 0。
    只要 i 小于 做 计数 用 仓库 的 第 id 项 的 第 6 项：
        若 仓库 的 第 id 项 的 第 6 项 的 第 i 项 的 第 0 项 等于 键：
            返还 仓库 的 第 id 项 的 第 6 项 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

模型_状态名 接受 状态：
    若 状态 等于 0：
        返还 「草稿」。
    完。
    若 状态 等于 1：
        返还 「发布」。
    完。
    若 状态 等于 2：
        返还 「部署」。
    完。
    若 状态 等于 3：
        返还 「回滚」。
    完。
    若 状态 等于 4：
        返还 「归档」。
    完。
    返还 「未知」。
完。

模型_版本号 接受 id：
    v 为 仓库 的 第 id 项 的 第 2 项。
    返还 做 转字符串 用 v 的 第 0 项 加 「.」 加 做 转字符串 用 v 的 第 1 项 加 「.」 加 做 转字符串 用 v 的 第 2 项。
完。

仓库_报告 接受 零参：
    做 打印 用 「╔═══ 模型仓库 ═══╗」。
    做 打印 用 「║ 总模型数: 」 加 做 转字符串 用 做 计数 用 仓库。
    可变 i 为 0。
    只要 i 小于 做 计数 用 仓库：
        m 为 仓库 的 第 i 项。
        做 打印 用 「║ [」 加 做 转字符串 用 m 的 第 0 项 加 「] 」 加 做 转字符串 用 m 的 第 1 项 加 「 v」 加 做 模型_版本号 用 i 加 「 状态:」 加 做 模型_状态名 用 m 的 第 3 项 加 「 参数:」 加 做 转字符串 用 m 的 第 4 项 加 「 精度:」 加 做 转字符串 用 m 的 第 5 项。
        i 为 i 加 1。
    完。
    做 打印 用 「╚════════════════╝」。
完。

模型_对比 接受 id_a、id_b：
    a 为 仓库 的 第 id_a 项。
    b 为 仓库 的 第 id_b 项。
    做 打印 用 「模型对比:」。
    做 打印 用 「 A: 」 加 做 转字符串 用 a 的 第 1 项 加 「 v」 加 做 模型_版本号 用 id_a 加 「 精度=」 加 做 转字符串 用 a 的 第 5 项 加 「 参数=」 加 做 转字符串 用 a 的 第 4 项。
    做 打印 用 「 B: 」 加 做 转字符串 用 b 的 第 1 项 加 「 v」 加 做 模型_版本号 用 id_b 加 「 精度=」 加 做 转字符串 用 b 的 第 5 项 加 「 参数=」 加 做 转字符串 用 b 的 第 4 项。
    精度差 为 a 的 第 5 项 减 b 的 第 5 项。
    若 精度差 大于 0：
        做 打印 用 「 结论: A优于B +」 加 做 转字符串 用 精度差。
    否则若 精度差 小于 0：
        做 打印 用 「 结论: B优于A +」 加 做 转字符串 用 0 减 精度差。
    否则：
        做 打印 用 「 结论: 精度相同」。
    完。
完。

模型_自动升级 接受 id、新精度：
    旧精度 为 仓库 的 第 id 项 的 第 5 项。
    仓库 的 第 id 项 的 第 5 项 为 新精度。
    若 新精度 大于 旧精度 并且 仓库 的 第 id 项 的 第 3 项 等于 模型状态_草稿：
        仓库 的 第 id 项 的 第 3 项 为 模型状态_发布。
        返还 真。
    完。
    返还 假。
完。

可变 _标签_模型id 为 一组：（空）。

可变 _标签_名称 为 一组：（空）。

模型_添加标签 接受 模型id、标签：
    做 追加 用 _标签_模型id、模型id。
    做 追加 用 _标签_名称、标签。
完。

模型_按标签搜索 接受 标签：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _标签_名称：
        若 _标签_名称 的 第 i 项 等于 标签：
            做 追加 用 结果、_标签_模型id 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _使用_模型id 为 一组：（空）。

可变 _使用_次数 为 一组：（空）。

模型_记录使用 接受 模型id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _使用_模型id：
        若 _使用_模型id 的 第 i 项 等于 模型id：
            _使用_次数 的 第 i 项 为 _使用_次数 的 第 i 项 加 1。
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 _使用_模型id、模型id。
    做 追加 用 _使用_次数、1。
完。

模型_使用排行 接受 前N：
    可变 结果 为 一组：（空）。
    可变 已选 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 前N 并且 k 小于 做 计数 用 _使用_模型id：
        可变 最大 为 负 1。
        可变 最大idx 为 负 1。
        可变 i 为 0。
        只要 i 小于 做 计数 用 _使用_次数：
            可变 跳 为 假。
            可变 j 为 0。
            只要 j 小于 做 计数 用 已选：
                若 已选 的 第 j 项 等于 i：
                    跳 为 真。
                完。
                j 为 j 加 1。
            完。
            若 跳 等于 假 并且 _使用_次数 的 第 i 项 大于 最大：
                最大 为 _使用_次数 的 第 i 项。
                最大idx 为 i。
            完。
            i 为 i 加 1。
        完。
        若 最大idx 大于或等于 0：
            做 追加 用 结果、一组：_使用_模型id 的 第 最大idx 项、最大。
            做 追加 用 已选、最大idx。
        完。
        k 为 k 加 1。
    完。
    返还 结果。
完。

本章 收束。
