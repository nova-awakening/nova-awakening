《推理优化》

引用《..·基础》。

推理优化_复制列表 接受 源：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 源：
        做 追加 用 结果、源 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

KV缓存_创建 接受 最大长度、层数：
    可变 K 为 一组：（空）。
    可变 V 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 层数：
        做 追加 用 K、一组：（空）。
        做 追加 用 V、一组：（空）。
        i 为 i 加 1。
    完。
    返还 一组：K、V、0、最大长度、层数。
完。

KV缓存_追加 接受 缓存项、层、k_vec、v_vec：
    做 追加 用 缓存项 的 第 0 项 的 第 层 项、k_vec。
    做 追加 用 缓存项 的 第 1 项 的 第 层 项、v_vec。
    若 层 等于 0：
        缓存项 的 第 2 项 为 缓存项 的 第 2 项 加 1。
    完。
完。

KV缓存_长度 接受 缓存项：
    返还 缓存项 的 第 2 项。
完。

KV缓存_获取K 接受 缓存项、层：
    返还 缓存项 的 第 0 项 的 第 层 项。
完。

KV缓存_获取V 接受 缓存项、层：
    返还 缓存项 的 第 1 项 的 第 层 项。
完。

KV缓存_清空 接受 缓存项：
    可变 i 为 0。
    只要 i 小于 缓存项 的 第 4 项：
        缓存项 的 第 0 项 的 第 i 项 为 一组：（空）。
        缓存项 的 第 1 项 的 第 i 项 为 一组：（空）。
        i 为 i 加 1。
    完。
    缓存项 的 第 2 项 为 0。
完。

KV缓存_已满 接受 缓存项：
    返还 缓存项 的 第 2 项 大于或等于 缓存项 的 第 3 项。
完。

TopK_采样 接受 分数列表、k：
    可变 结果 为 一组：（空）。
    可变 选了 为 0。
    可变 已选 为 一组：（空）。
    只要 选了 小于 k 并且 选了 小于 做 计数 用 分数列表：
        可变 最佳 为 负 1。
        可变 最佳分 为 负 99999。
        可变 i 为 0。
        只要 i 小于 做 计数 用 分数列表：
            可变 跳过 为 假。
            可变 j 为 0。
            只要 j 小于 做 计数 用 已选：
                若 已选 的 第 j 项 等于 i：
                    跳过 为 真。
                完。
                j 为 j 加 1。
            完。
            若 跳过 等于 假 并且 分数列表 的 第 i 项 大于 最佳分：
                最佳 为 i。
                最佳分 为 分数列表 的 第 i 项。
            完。
            i 为 i 加 1。
        完。
        若 最佳 大于或等于 0：
            做 追加 用 结果、最佳。
            做 追加 用 已选、最佳。
        完。
        选了 为 选了 加 1。
    完。
    返还 结果。
完。

TopP_采样 接受 分数列表、p_threshold：
    sorted_idx 为 做 TopK_采样 用 分数列表、做 计数 用 分数列表。
    可变 合计 为 0。
    可变 si 为 0。
    只要 si 小于 做 计数 用 分数列表：
        合计 为 合计 加 分数列表 的 第 si 项。
        si 为 si 加 1。
    完。
    若 合计 等于 0：
        合计 为 1。
    完。
    可变 结果 为 一组：（空）。
    可变 累积 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 sorted_idx 并且 累积 乘 100 小于 p_threshold 乘 合计：
        做 追加 用 结果、sorted_idx 的 第 i 项。
        累积 为 累积 加 分数列表 的 第 sorted_idx 的 第 i 项 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

推理_温度缩放 接受 分数列表、温度：
    若 温度 等于 0：
        温度 为 1。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分数列表：
        做 追加 用 结果、分数列表 的 第 i 项 乘 100 除以 温度。
        i 为 i 加 1。
    完。
    返还 结果。
完。

贪心_解码 接受 分数列表：
    可变 最佳 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 分数列表：
        若 分数列表 的 第 i 项 大于 分数列表 的 第 最佳 项：
            最佳 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

推理_束搜索_初始化 接受 束宽：
    可变 束 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 束宽：
        做 追加 用 束、一组：一组：（空）、0。
        i 为 i 加 1。
    完。
    返还 束。
完。

推理_束搜索_扩展 接受 当前束、每束分数列表、束宽：
    可变 选了 为 0。
    可变 候选 为 一组：（空）。
    可变 bi 为 0。
    只要 bi 小于 做 计数 用 当前束：
        可变 ti 为 0。
        只要 ti 小于 做 计数 用 每束分数列表 的 第 bi 项：
            做 追加 用 候选、一组：bi、ti、当前束 的 第 bi 项 的 第 1 项 加 每束分数列表 的 第 bi 项 的 第 ti 项。
            ti 为 ti 加 1。
        完。
        bi 为 bi 加 1。
    完。
    可变 新束 为 一组：（空）。
    可变 已选 为 一组：（空）。
    只要 选了 小于 束宽 并且 选了 小于 做 计数 用 候选：
        可变 最佳 为 负 1。
        可变 最佳分 为 负 99999。
        可变 ci 为 0。
        只要 ci 小于 做 计数 用 候选：
            可变 跳过 为 假。
            可变 si 为 0。
            只要 si 小于 做 计数 用 已选：
                若 已选 的 第 si 项 等于 ci：
                    跳过 为 真。
                完。
                si 为 si 加 1。
            完。
            若 跳过 等于 假 并且 候选 的 第 ci 项 的 第 2 项 大于 最佳分：
                最佳 为 ci。
                最佳分 为 候选 的 第 ci 项 的 第 2 项。
            完。
            ci 为 ci 加 1。
        完。
        若 最佳 大于或等于 0：
            源值 为 候选 的 第 最佳 项 的 第 0 项。
            词元值 为 候选 的 第 最佳 项 的 第 1 项。
            可变 新序列 为 做 推理优化_复制列表 用 当前束 的 第 源值 项 的 第 0 项。
            做 追加 用 新序列、词元值。
            做 追加 用 新束、一组：新序列、最佳分。
            做 追加 用 已选、最佳。
        完。
        选了 为 选了 加 1。
    完。
    返还 新束。
完。

投机_验证 接受 草稿tokens、目标分数列表：
    可变 接受数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 草稿tokens：
        最佳 为 做 贪心_解码 用 目标分数列表 的 第 i 项。
        若 最佳 等于 草稿tokens 的 第 i 项：
            接受数 为 接受数 加 1。
        否则：
            返还 一组：接受数、最佳。
        完。
        i 为 i 加 1。
    完。
    返还 一组：接受数、负 1。
完。

可变 批处理_请求 为 一组：（空）。

可变 批处理_下一ID 为 1。

批处理_追加 接受 最大生成数：
    id 为 批处理_下一ID。
    批处理_下一ID 为 批处理_下一ID 加 1。
    做 追加 用 批处理_请求、一组：id、一组：（空）、0、最大生成数、假。
    返还 id。
完。

批处理_步 接受 新token_列表：
    可变 完成列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 批处理_请求：
        若 批处理_请求 的 第 i 项 的 第 4 项 等于 假 并且 i 小于 做 计数 用 新token_列表：
            做 追加 用 批处理_请求 的 第 i 项 的 第 1 项、新token_列表 的 第 i 项。
            批处理_请求 的 第 i 项 的 第 2 项 为 批处理_请求 的 第 i 项 的 第 2 项 加 1。
            若 批处理_请求 的 第 i 项 的 第 2 项 大于或等于 批处理_请求 的 第 i 项 的 第 3 项：
                批处理_请求 的 第 i 项 的 第 4 项 为 真。
                做 追加 用 完成列表、批处理_请求 的 第 i 项 的 第 0 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 完成列表。
完。

批处理_活跃数 接受 零参：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 批处理_请求：
        若 批处理_请求 的 第 i 项 的 第 4 项 等于 假：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

MinP_采样 接受 分数列表、min_p：
    可变 mx 为 分数列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 分数列表：
        若 分数列表 的 第 i 项 大于 mx：
            mx 为 分数列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    阈值 为 mx 乘 min_p 除以 100。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 分数列表：
        若 分数列表 的 第 i 项 大于或等于 阈值：
            做 追加 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

长度惩罚_分数 接受 原始分数、序列长度、学习率：
    可变 pen 为 1000。
    可变 p 为 0。
    只要 p 小于 学习率：
        pen 为 pen 乘 序列长度 除以 10。
        p 为 p 加 1。
    完。
    若 pen 小于 1：
        pen 为 1。
    完。
    返还 原始分数 乘 1000 除以 pen。
完。

可变 前缀_tokens 为 一组：（空）。

可变 前缀_cache 为 一组：（空）。

前缀缓存_设置 接受 词元列、cache_data：
    前缀_tokens 为 词元列。
    前缀_cache 为 cache_data。
完。

前缀缓存_大小 接受 零参：
    返还 做 计数 用 前缀_tokens。
完。

前缀缓存_命中 接受 query_tokens：
    可变 hit 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 前缀_tokens 并且 i 小于 做 计数 用 query_tokens：
        若 前缀_tokens 的 第 i 项 等于 query_tokens 的 第 i 项：
            hit 为 hit 加 1。
        否则：
            返还 hit。
        完。
        i 为 i 加 1。
    完。
    返还 hit。
完。

本章 收束。
