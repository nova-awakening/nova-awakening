《序列化》

引用《..·基础》。

引用《..·系统·配置》。

序列化_配置_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、一组：（空）。
完。

配置_有 接受 cfg、键哈希：
    可变 i 为 0。
    只要 i 小于 做 计数 用 cfg 的 第 0 项：
        若 cfg 的 第 0 项 的 第 i 项 等于 键哈希：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

配置_大小 接受 cfg：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 cfg 的 第 0 项：
        若 cfg 的 第 0 项 的 第 i 项 不等于 负 1：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

配置_所有键 接受 cfg：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 cfg 的 第 0 项：
        若 cfg 的 第 0 项 的 第 i 项 不等于 负 1：
            做 追加 用 r、cfg 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

INI_解析 接受 文本：
    可变 结果 为 做 序列化_配置_创建。
    可变 当前节 为 0。
    可变 位置 为 0。
    只要 位置 小于 做 计数 用 文本：
        只要 位置 小于 做 计数 用 文本 并且 (做 字符码 用 文本、位置 等于 10 或者 做 字符码 用 文本、位置 等于 13)：
            位置 为 位置 加 1。
        完。
        若 位置 大于或等于 做 计数 用 文本：
            返还 结果。
        完。
        可变 行 为 「」。
        只要 位置 小于 做 计数 用 文本 并且 做 字符码 用 文本、位置 不等于 10：
            若 做 字符码 用 文本、位置 不等于 13：
                行 为 行 加 做 子串 用 文本、位置、1。
            完。
            位置 为 位置 加 1。
        完。
        若 位置 小于 做 计数 用 文本：
            位置 为 位置 加 1。
        完。
        若 做 计数 用 行 等于 0 或者 做 字符码 用 行、0 等于 59 或者 做 字符码 用 行、0 等于 35：
            继续。
        否则若 做 字符码 用 行、0 等于 91：
            可变 名 为 「」。
            可变 j 为 1。
            只要 j 小于 做 计数 用 行 并且 做 字符码 用 行、j 不等于 93：
                名 为 名 加 做 子串 用 行、j、1。
                j 为 j 加 1。
            完。
            可变 h 为 0。
            j 为 0。
            只要 j 小于 做 计数 用 名：
                h 为 h 乘 31 加 做 编码 用 做 子串 用 名、j、1。
                j 为 j 加 1。
            完。
            当前节 为 h。
            做 追加 用 结果 的 第 2 项、h。
        否则：
            可变 eq 为 负 1。
            可变 k 为 0。
            只要 k 小于 做 计数 用 行：
                若 做 字符码 用 行、k 等于 61 并且 eq 小于 0：
                    eq 为 k。
                完。
                k 为 k 加 1。
            完。
            若 eq 大于 0：
                键str 为 做 子串 用 行、0、eq。
                值str 为 做 子串 用 行、eq 加 1、做 计数 用 行 减 eq 减 1。
                可变 kh 为 0。
                k 为 0。
                只要 k 小于 做 计数 用 键str：
                    kh 为 kh 乘 31 加 做 编码 用 做 子串 用 键str、k、1。
                    k 为 k 加 1。
                完。
                组合键 为 当前节 乘 10000 加 kh。
                可变 是数 为 真。
                可变 数目 为 0。
                可变 neg 为 假。
                k 为 0。
                若 做 计数 用 值str 大于 0 并且 做 字符码 用 值str、0 等于 45：
                    neg 为 真。
                    k 为 1。
                完。
                只要 k 小于 做 计数 用 值str：
                    d 为 做 字符码 用 值str、k 减 48。
                    若 d 大于或等于 0 并且 d 小于或等于 字符_制表：
                        数目 为 数目 乘 10 加 d。
                    否则：
                        是数 为 假。
                    完。
                    k 为 k 加 1。
                完。
                若 neg：
                    数目 为 0 减 数目。
                完。
                若 是数 并且 做 计数 用 值str 大于 0：
                    做 追加 用 结果 的 第 0 项、组合键。
                    做 追加 用 结果 的 第 1 项、数目。
                否则：
                    做 追加 用 结果 的 第 0 项、组合键。
                    做 追加 用 结果 的 第 1 项、0。
                完。
            完。
        完。
    完。
    返还 结果。
完。

检查点_创建 接受 版本、时间戳、数据：
    可变 校验 为 版本 乘 17 加 时间戳 乘 31。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        校验 为 校验 加 数据 的 第 i 项 乘 (i 加 1)。
        i 为 i 加 1。
    完。
    若 校验 小于 0：
        校验 为 0 减 校验。
    完。
    返还 一组：版本、时间戳、数据、校验。
完。

检查点_版本 接受 cp：
    返还 cp 的 第 0 项。
完。

检查点_时间 接受 cp：
    返还 cp 的 第 1 项。
完。

检查点_数据 接受 cp：
    返还 cp 的 第 2 项。
完。

检查点_校验 接受 cp：
    返还 cp 的 第 3 项。
完。

检查点_验证 接受 cp：
    可变 校验 为 cp 的 第 0 项 乘 17 加 cp 的 第 1 项 乘 31。
    可变 i 为 0。
    只要 i 小于 做 计数 用 cp 的 第 2 项：
        校验 为 校验 加 cp 的 第 2 项 的 第 i 项 乘 (i 加 1)。
        i 为 i 加 1。
    完。
    若 校验 小于 0：
        校验 为 0 减 校验。
    完。
    返还 校验 等于 cp 的 第 3 项。
完。

可变 检查点列表 为 一组：（空）。

可变 最大检查点数 为 5。

检查点管理器_保存 接受 版本、时间戳、数据：
    cp 为 做 检查点_创建 用 版本、时间戳、数据。
    可变 新列表 为 一组：（空）。
    做 追加 用 检查点列表、cp。
    若 做 计数 用 检查点列表 大于 最大检查点数：
        可变 i 为 做 计数 用 检查点列表 减 最大检查点数。
        只要 i 小于 做 计数 用 检查点列表：
            做 追加 用 新列表、检查点列表 的 第 i 项。
            i 为 i 加 1。
        完。
        检查点列表 为 新列表。
    完。
    返还 cp。
完。

检查点管理器_最新 接受 零参：
    若 做 计数 用 检查点列表 等于 0：
        返还 空。
    完。
    返还 检查点列表 的 第 做 计数 用 检查点列表 减 1 项。
完。

检查点管理器_数量 接受 零参：
    返还 做 计数 用 检查点列表。
完。

检查点管理器_按版本 接受 版本：
    可变 i 为 做 计数 用 检查点列表 减 1。
    只要 i 大于或等于 0：
        若 检查点列表 的 第 i 项 的 第 0 项 等于 版本：
            返还 检查点列表 的 第 i 项。
        完。
        i 为 i 减 1。
    完。
    返还 空。
完。

CRC_简易 接受 数据：
    可变 校验值 为 0xFFFF。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        校验值 为 校验值 位异或 数据 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 8：
            最低位 为 校验值 位与 1。
            若 最低位 等于 1：
                校验值 为 校验值 右移 1 位异或 0xA001。
            否则：
                校验值 为 校验值 右移 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 校验值 位与 0xFFFF。
完。

检查点_差异数 接受 cp1、cp2：
    可变 差值 为 0。
    d1 为 cp1 的 第 2 项。
    d2 为 cp2 的 第 2 项。
    可变 i 为 0。
    可变 n 为 做 计数 用 d1。
    若 做 计数 用 d2 小于 n：
        n 为 做 计数 用 d2。
    完。
    只要 i 小于 n：
        若 d1 的 第 i 项 不等于 d2 的 第 i 项：
            差值 为 差值 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 长差 为 做 计数 用 d1 减 做 计数 用 d2。
    若 长差 小于 0：
        长差 为 0 减 长差。
    完。
    返还 差值 加 长差。
完。

序列化_带版本 接受 版本号、数据：
    可变 结果 为 一组：0x4E、0x56、版本号 位与 0xFF、版本号 右移 8 位与 0xFF。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 结果、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

序列化_读版本 接受 数据：
    若 做 计数 用 数据 小于 4：
        返还 负 1。
    完。
    若 数据 的 第 0 项 不等于 0x4E 或者 数据 的 第 1 项 不等于 0x56：
        返还 负 1。
    完。
    返还 数据 的 第 2 项 位或 数据 的 第 3 项 左移 8。
完。

序列化_取数据 接受 数据：
    可变 结果 为 一组：（空）。
    可变 i 为 4。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 结果、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

校验和_CRC简化 接受 数据：
    可变 校验值 为 0xFFFF。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        校验值 为 校验值 加 数据 的 第 i 项 乘 31。
        校验值 为 校验值 位与 0xFFFF。
        i 为 i 加 1。
    完。
    返还 校验值。
完。

校验和_验证 接受 数据、期望crc：
    返还 做 校验和_CRC简化 用 数据 等于 期望crc。
完。

VarInt_编码 接受 值：
    可变 结果 为 一组：（空）。
    可变 v 为 值。
    只要 v 大于或等于 128：
        做 追加 用 结果、v 位与 0x7F 位或 0x80。
        v 为 v 右移 7。
    完。
    做 追加 用 结果、v 位与 0x7F。
    返还 结果。
完。

VarInt_解码 接受 数据、偏移：
    可变 值 为 0。
    可变 移位 为 0。
    可变 位置 为 偏移。
    只要 位置 小于 做 计数 用 数据：
        b 为 数据 的 第 位置 项。
        值 为 值 位或 (b 位与 0x7F) 左移 移位。
        位置 为 位置 加 1。
        若 b 位与 0x80 等于 0：
            返还 一组：值、位置。
        完。
        移位 为 移位 加 7。
    完。
    返还 一组：值、位置。
完。

本章 收束。
