《向量原语》

引用《..·基础》。

引用《..·数学·神经网络》。

引用《..·..·运行时·智能推理·注意力机制》。

引用《..·..·运行时·原语桩》。

向量128位 为 16。

向量256位 为 32。

向量512位 为 64。

向量_加32x8 接受 a、b：
    返还 做 __内建_向量加32x8 用 a、b。
完。

向量_乘32x8 接受 a、b：
    返还 做 __内建_向量乘32x8 用 a、b。
完。

向量_点积32x8 接受 a、b：
    积 为 做 向量_乘32x8 用 a、b。
    返还 做 __内建_向量水平加 用 积。
完。

向量_加64x4 接受 a、b：
    返还 做 __内建_向量加64x4 用 a、b。
完。

定点精度 为 10000。

向量_定点乘 接受 a、b、精度：
    宽积 为 做 __内建_向量宽乘32 用 a、b。
    返还 做 __内建_向量除64 用 宽积、精度。
完。

向量_定点点积 接受 a、b、维度：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 加 8 小于或等于 维度：
        块a 为 做 __内建_向量载入 用 a、i。
        块b 为 做 __内建_向量载入 用 b、i。
        和 为 和 加 做 向量_点积32x8 用 块a、块b。
        i 为 i 加 8。
    完。
    只要 i 小于 维度：
        和 为 和 加 a 的 第 i 项 乘 b 的 第 i 项。
        i 为 i 加 1。
    完。
    若 定点精度 等于 0：
        返还 0。
    完。
    返还 和 除以 定点精度。
完。

向量_激活_正值 接受 x：
    返还 做 __内建_向量最大值32 用 x、0。
完。

向量_七态量化 接受 值、阈值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 值：
        v 为 值 的 第 i 项。
        若 v 大于 阈值 的 第 5 项：
            做 追加 用 结果、3。
        否则若 v 大于 阈值 的 第 4 项：
            做 追加 用 结果、2。
        否则若 v 大于 阈值 的 第 3 项：
            做 追加 用 结果、1。
        否则若 v 大于 阈值 的 第 2 项：
            做 追加 用 结果、0。
        否则若 v 大于 阈值 的 第 1 项：
            做 追加 用 结果、负 1。
        否则若 v 大于 阈值 的 第 0 项：
            做 追加 用 结果、负 2。
        否则：
            做 追加 用 结果、负 3。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_Hopfield能量 接受 权重矩阵、状态、维度：
    可变 能量 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        行 为 权重矩阵 的 第 i 项。
        贡献 为 做 向量_定点点积 用 行、状态、维度。
        能量 为 能量 减 状态 的 第 i 项 乘 贡献。
        i 为 i 加 1。
    完。
    返还 能量 除以 2。
完。

向量_矩阵向量乘 接受 矩阵、向量、行数、列数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 行数：
        行 为 矩阵 的 第 i 项。
        值 为 做 向量_定点点积 用 行、向量、列数。
        做 追加 用 结果、值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_批量读 接受 地址、数量：
    返还 做 __内建_向量载入 用 地址、数量。
完。

向量_批量写 接受 地址、数据、数量：
    做 __内建_向量存储 用 地址、数据、数量。
完。

原子_加 接受 地址、值：
    返还 做 __内建_原子加 用 地址、值。
完。

原子_比较交换 接受 地址、期望、新值：
    返还 做 __内建_原子比较交换 用 地址、期望、新值。
完。

内存屏障 接受 零参：
    做 __内建_内存屏障。
完。

向量原语_向量_求和 接受 v：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        总 为 总 加 v 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

向量_最大 接受 v：
    若 做 计数 用 v 等于 0：
        返还 0。
    完。
    可变 最大 为 v 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 v：
        若 v 的 第 i 项 大于 最大：
            最大 为 v 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

向量_最小 接受 v：
    若 做 计数 用 v 等于 0：
        返还 0。
    完。
    可变 最小 为 v 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 v：
        若 v 的 第 i 项 小于 最小：
            最小 为 v 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小。
完。

向量_均值 接受 v：
    若 做 计数 用 v 等于 0：
        返还 0。
    完。
    返还 做 向量原语_向量_求和 用 v 除以 做 计数 用 v。
完。

向量原语_向量_加 接受 a、b：
    n 为 做 计数 用 a。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、a 的 第 i 项 加 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量原语_向量_减 接受 a、b：
    n 为 做 计数 用 a。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、a 的 第 i 项 减 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_乘 接受 a、b：
    n 为 做 计数 用 a。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、a 的 第 i 项 乘 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量原语_向量_标量乘 接受 v、s：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        做 追加 用 结果、v 的 第 i 项 乘 s。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量原语_向量_点积 接受 a、b：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a：
        总 为 总 加 a 的 第 i 项 乘 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

向量原语_向量_范数平方 接受 v：
    返还 做 向量原语_向量_点积 用 v、v。
完。

向量_FMA 接受 a、b、c：
    n 为 做 计数 用 a。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、a 的 第 i 项 乘 b 的 第 i 项 加 c 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_FMS 接受 a、b、c：
    n 为 做 计数 用 a。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、a 的 第 i 项 乘 b 的 第 i 项 减 c 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_掩码选择 接受 a、b、掩码：
    n 为 做 计数 用 a。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 掩码 的 第 i 项 等于 1：
            做 追加 用 结果、a 的 第 i 项。
        否则：
            做 追加 用 结果、b 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_压缩 接受 v、掩码：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        若 掩码 的 第 i 项 等于 1：
            做 追加 用 结果、v 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵向量乘 接受 矩阵、向量、行数、列数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 行数：
        可变 和 为 0。
        可变 j 为 0。
        只要 j 小于 列数：
            和 为 和 加 矩阵 的 第 i 乘 列数 加 j 项 乘 向量 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _向量_运算数 为 0。

可变 _向量_FMA数 为 0。

向量_记录运算 接受 零参：
    _向量_运算数 为 _向量_运算数 加 1。
完。

向量_记录FMA 接受 零参：
    _向量_FMA数 为 _向量_FMA数 加 1。
完。

本章 收束。
