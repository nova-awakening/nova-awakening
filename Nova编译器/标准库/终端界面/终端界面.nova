《终端界面》

引用《..·函数式·函数式》。

引用《..·基础》。

进度条_生成 接受 当前、总数、宽度：
    可变 填充数 为 当前 乘 宽度 除以 总数。
    若 填充数 大于 宽度：
        填充数 为 宽度。
    完。
    可变 bar 为 「[」。
    可变 i 为 0。
    只要 i 小于 填充数：
        bar 为 bar 加 「#」。
        i 为 i 加 1。
    完。
    只要 i 小于 宽度：
        bar 为 bar 加 「-」。
        i 为 i 加 1。
    完。
    bar 为 bar 加 「] 」。
    bar 为 bar 加 做 转字符串 用 当前 乘 100 除以 总数。
    bar 为 bar 加 「%」。
    返还 bar。
完。

表格_分隔线 接受 列宽列表：
    可变 行值 为 「+」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列宽列表：
        可变 j 为 0。
        只要 j 小于 列宽列表 的 第 i 项 加 2：
            行值 为 行值 加 「-」。
            j 为 j 加 1。
        完。
        行值 为 行值 加 「+」。
        i 为 i 加 1。
    完。
    返还 行值。
完。

表格_行 接受 值列表、列宽列表：
    可变 行号 为 「|」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 值列表：
        行号 为 行号 加 「 」。
        行号 为 行号 加 做 转字符串 用 值列表 的 第 i 项。
        可变 填充 为 列宽列表 的 第 i 项 减 做 计数 用 做 转字符串 用 值列表 的 第 i 项 加 1。
        若 填充 小于 1：
            填充 为 1。
        完。
        可变 j 为 0。
        只要 j 小于 填充：
            行号 为 行号 加 「 」。
            j 为 j 加 1。
        完。
        行号 为 行号 加 「|」。
        i 为 i 加 1。
    完。
    返还 行号。
完。

状态_图标 接受 状态码：
    若 状态码 等于 0：
        返还 「[OK]」。
    完。
    若 状态码 等于 1：
        返还 「[WARN]」。
    完。
    若 状态码 等于 2：
        返还 「[ERR]」。
    完。
    若 状态码 等于 3：
        返还 「[RUN]」。
    完。
    返还 「[???]」。
完。

格式化_千分位 接受 数值：
    可变 s 为 做 转字符串 用 数值。
    返还 s。
完。

格式化_百分比 接受 分子、分母：
    若 分母 等于 0：
        返还 「N/A」。
    完。
    pct 为 分子 乘 100 除以 分母。
    返还 做 转字符串 用 pct 加 「%」。
完。

格式化_字节 接受 字节数：
    若 字节数 大于或等于 1073741824：
        返还 做 转字符串 用 字节数 除以 1073741824 加 「GB」。
    完。
    若 字节数 大于或等于 1048576：
        返还 做 转字符串 用 字节数 除以 1048576 加 「MB」。
    完。
    若 字节数 大于或等于 1024：
        返还 做 转字符串 用 字节数 除以 1024 加 「KB」。
    完。
    返还 做 转字符串 用 字节数 加 「B」。
完。

可变 菜单_项 为 一组：（空）。

可变 菜单_选中 为 0。

菜单_追加 接受 项哈希：
    做 追加 用 菜单_项、项哈希。
完。

菜单_清空 接受 零参：
    菜单_项 为 一组：（空）。
    菜单_选中 为 0。
完。

菜单_上 接受 零参：
    若 菜单_选中 大于 0：
        菜单_选中 为 菜单_选中 减 1。
    完。
完。

菜单_下 接受 零参：
    若 菜单_选中 小于 做 计数 用 菜单_项 减 1：
        菜单_选中 为 菜单_选中 加 1。
    完。
完。

菜单_当前 接受 零参：
    返还 菜单_选中。
完。

菜单_项数 接受 零参：
    返还 做 计数 用 菜单_项。
完。

菜单_重置 接受 零参：
    菜单_选中 为 0。
完。

菜单_显示 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 菜单_项：
        若 i 等于 菜单_选中：
            做 打印 用 「 > 」 加 做 转字符串 用 菜单_项 的 第 i 项。
        否则：
            做 打印 用 「 」 加 做 转字符串 用 菜单_项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
完。

水平条 接受 标签、值、最大值、宽度：
    可变 填充 为 值 乘 宽度 除以 最大值。
    若 填充 大于 宽度：
        填充 为 宽度。
    完。
    若 填充 小于 0：
        填充 为 0。
    完。
    可变 bar 为 做 转字符串 用 标签 加 「 |」。
    可变 i 为 0。
    只要 i 小于 填充：
        bar 为 bar 加 「█」。
        i 为 i 加 1。
    完。
    只要 i 小于 宽度：
        bar 为 bar 加 「 」。
        i 为 i 加 1。
    完。
    bar 为 bar 加 「| 」 加 做 转字符串 用 值。
    返还 bar。
完。

框_上 接受 标题、宽度：
    可变 行值 为 「╔═══ 」 加 标题 加 「 」。
    可变 i 为 做 计数 用 行值。
    只要 i 小于 宽度：
        行值 为 行值 加 「═」。
        i 为 i 加 1。
    完。
    行值 为 行值 加 「╗」。
    返还 行值。
完。

框_下 接受 宽度：
    可变 行值 为 「╚」。
    可变 i 为 0。
    只要 i 小于 宽度：
        行值 为 行值 加 「═」。
        i 为 i 加 1。
    完。
    行值 为 行值 加 「╝」。
    返还 行值。
完。

框_行 接受 内容、宽度：
    可变 行值 为 「║ 」 加 内容。
    可变 i 为 做 计数 用 行值。
    只要 i 小于 宽度：
        行值 为 行值 加 「 」。
        i 为 i 加 1。
    完。
    行值 为 行值 加 「║」。
    返还 行值。
完。

训练仪表盘 接受 轮次、损失值、acc、lr：
    做 打印 用 做 框_上 用 「训练仪表盘」、40。
    做 打印 用 做 框_行 用 「Epoch: 」 加 做 转字符串 用 轮次、40。
    做 打印 用 做 框_行 用 「Loss: 」 加 做 转字符串 用 损失值、40。
    做 打印 用 做 框_行 用 「Acc: 」 加 做 转字符串 用 acc 加 「%」、40。
    做 打印 用 做 框_行 用 「LR: 」 加 做 转字符串 用 lr、40。
    做 打印 用 做 框_行 用 做 进度条_生成 用 轮次、100、20、40。
    做 打印 用 做 框_下 用 40。
完。

表格_打印 接受 数据、列宽列表：
    做 打印 用 做 表格_分隔线 用 列宽列表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 打印 用 做 表格_行 用 数据 的 第 i 项、列宽列表。
        若 i 等于 0：
            做 打印 用 做 表格_分隔线 用 列宽列表。
        完。
        i 为 i 加 1。
    完。
    做 打印 用 做 表格_分隔线 用 列宽列表。
完。

格式化_时间 接受 毫秒：
    若 毫秒 大于或等于 3600000：
        返还 做 转字符串 用 毫秒 除以 3600000 加 「h」。
    完。
    若 毫秒 大于或等于 60000：
        返还 做 转字符串 用 毫秒 除以 60000 加 「m」。
    完。
    若 毫秒 大于或等于 1000：
        返还 做 转字符串 用 毫秒 除以 1000 加 「s」。
    完。
    返还 做 转字符串 用 毫秒 加 「ms」。
完。

格式化_参数量 接受 数量：
    若 数量 大于或等于 1000000000：
        返还 做 转字符串 用 数量 除以 1000000000 加 「B」。
    完。
    若 数量 大于或等于 1000000：
        返还 做 转字符串 用 数量 除以 1000000 加 「M」。
    完。
    若 数量 大于或等于 1000：
        返还 做 转字符串 用 数量 除以 1000 加 「K」。
    完。
    返还 做 转字符串 用 数量。
完。

迷你图 接受 数据、宽度：
    若 做 计数 用 数据 等于 0：
        返还 「」。
    完。
    最小 为 做 列表最小 用 数据。
    最大 为 做 列表最大 用 数据。
    可变 范围 为 最大 减 最小。
    若 范围 等于 0：
        范围 为 1。
    完。
    可变 图 为 「」。
    可变 步长 为 做 计数 用 数据 除以 宽度。
    若 步长 小于 1：
        步长 为 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据 并且 做 计数 用 图 小于 宽度：
        层级 为 (数据 的 第 i 项 减 最小) 乘 7 除以 范围。
        若 层级 小于或等于 1：
            图 为 图 加 「▁」。
        否则若 层级 小于或等于 2：
            图 为 图 加 「▂」。
        否则若 层级 小于或等于 3：
            图 为 图 加 「▃」。
        否则若 层级 小于或等于 4：
            图 为 图 加 「▄」。
        否则若 层级 小于或等于 5：
            图 为 图 加 「▅」。
        否则若 层级 小于或等于 6：
            图 为 图 加 「▆」。
        否则：
            图 为 图 加 「▇」。
        完。
        i 为 i 加 步长。
    完。
    返还 图。
完。

进度条_渲染 接受 当前、总数、宽度：
    若 总数 等于 0：
        返还 「[」 加 做 重复字符 用 「 」、宽度 加 「] 0%」。
    完。
    百分比 为 当前 乘 100 除以 总数。
    填充数 为 当前 乘 宽度 除以 总数。
    可变 条 为 「[」。
    可变 i 为 0。
    只要 i 小于 宽度：
        若 i 小于 填充数：
            条 为 条 加 「█」。
        否则：
            条 为 条 加 「░」。
        完。
        i 为 i 加 1。
    完。
    返还 条 加 「] 」 加 做 转字符串 用 百分比 加 「%」。
完。

本章 收束。
