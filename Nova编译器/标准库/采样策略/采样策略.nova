《采样策略》

引用《..·函数式·函数式》。

引用《..·基础》。

引用《..·数学·概率》。

采样_随机态 为 54321。

采样_随机 接受 零参：
    采样_随机态 为 采样_随机态 位异或 采样_随机态 左移 13。
    采样_随机态 为 采样_随机态 位异或 采样_随机态 右移 7。
    采样_随机态 为 采样_随机态 位异或 采样_随机态 左移 17。
    若 采样_随机态 小于 0：
        采样_随机态 为 0 减 采样_随机态。
    完。
    返还 采样_随机态。
完。

贪心采样 接受 对数值：
    可变 最大 为 对数值 的 第 0 项。
    可变 最大idx 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 对数值：
        若 对数值 的 第 i 项 大于 最大：
            最大 为 对数值 的 第 i 项。
            最大idx 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最大idx。
完。

温度_缩放 接受 对数值、温度：
    若 温度 等于 0：
        温度 为 1。
    完。
    返还 做 映射 用 对数值、以 x 映射到 x 乘 100 除以 温度。
完。

简化归一化指数 接受 对数值：
    可变 最大 为 对数值 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 对数值：
        若 对数值 的 第 i 项 大于 最大：
            最大 为 对数值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 权重 为 一组：（空）。
    可变 总和 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 对数值：
        可变 w 为 对数值 的 第 i 项 减 最大 加 100。
        若 w 小于 1：
            w 为 1。
        完。
        做 追加 用 权重、w。
        总和 为 总和 加 w。
        i 为 i 加 1。
    完。
    返还 做 映射 用 权重、以 w 映射到 w 乘 1000 除以 总和。
完。

采样策略_分类采样 接受 概率：
    随机值 为 做 采样_随机 取余于 1000。
    可变 累积 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 概率：
        累积 为 累积 加 概率 的 第 i 项。
        若 随机值 小于 累积：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 做 计数 用 概率 减 1。
完。

TopK采样 接受 对数值、K：
    可变 索引列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 对数值：
        做 追加 用 索引列表、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 索引列表 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 索引列表：
            若 对数值 的 第 索引列表 的 第 j 项 项 大于 对数值 的 第 索引列表 的 第 i 项 项：
                临时 为 索引列表 的 第 i 项。
                索引列表 的 第 i 项 为 索引列表 的 第 j 项。
                索引列表 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 过滤值 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 对数值：
        做 追加 用 过滤值、负 99999。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 K 并且 i 小于 做 计数 用 索引列表：
        过滤值 的 第 索引列表 的 第 i 项 项 为 对数值 的 第 索引列表 的 第 i 项 项。
        i 为 i 加 1。
    完。
    概率 为 做 简化归一化指数 用 过滤值。
    返还 做 分类采样 用 概率、0。
完。

TopP采样 接受 对数值、P：
    概率 为 做 简化归一化指数 用 对数值。
    可变 索引列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 概率：
        做 追加 用 索引列表、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 索引列表 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 索引列表：
            若 概率 的 第 索引列表 的 第 j 项 项 大于 概率 的 第 索引列表 的 第 i 项 项：
                临时 为 索引列表 的 第 i 项。
                索引列表 的 第 i 项 为 索引列表 的 第 j 项。
                索引列表 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 累积 为 0。
    可变 截止 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 索引列表 并且 累积 小于 P：
        累积 为 累积 加 概率 的 第 索引列表 的 第 i 项 项。
        截止 为 i 加 1。
        i 为 i 加 1。
    完。
    随机值 为 做 采样_随机 取余于 截止。
    返还 索引列表 的 第 随机值 项。
完。

Logits_最大值 接受 对数值：
    返还 做 列表最大 用 对数值。
完。

束搜索_创建 接受 束宽：
    返还 一组：一组：（空）、束宽。
完。

束搜索_初始化 接受 束：
    束 的 第 0 项 为 一组：一组：一组：（空）、0、假。
完。

束搜索_扩展 接受 束、词表大小、分数表：
    可变 所有候选 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 束 的 第 0 项：
        候选 为 束 的 第 0 项 的 第 i 项。
        若 候选 的 第 2 项：
            做 追加 用 所有候选、候选。
        否则：
            可变 v 为 0。
            只要 v 小于 词表大小：
                可变 新序列 为 一组：（空）。
                可变 k 为 0。
                只要 k 小于 做 计数 用 候选 的 第 0 项：
                    做 追加 用 新序列、候选 的 第 0 项 的 第 k 项。
                    k 为 k 加 1。
                完。
                做 追加 用 新序列、v。
                新分数 为 候选 的 第 1 项 加 分数表 的 第 v 项。
                做 追加 用 所有候选、一组：新序列、新分数、假。
                v 为 v 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 n 为 做 计数 用 所有候选。
    可变 si 为 0。
    只要 si 小于 n 减 1：
        可变 sj 为 si 加 1。
        只要 sj 小于 n：
            若 所有候选 的 第 sj 项 的 第 1 项 大于 所有候选 的 第 si 项 的 第 1 项：
                临 为 所有候选 的 第 si 项。
                所有候选 的 第 si 项 为 所有候选 的 第 sj 项。
                所有候选 的 第 sj 项 为 临。
            完。
            sj 为 sj 加 1。
        完。
        si 为 si 加 1。
    完。
    束 的 第 0 项 为 一组：（空）。
    i 为 0。
    只要 i 小于 束 的 第 1 项 并且 i 小于 做 计数 用 所有候选：
        做 追加 用 束 的 第 0 项、所有候选 的 第 i 项。
        i 为 i 加 1。
    完。
完。

束搜索_最佳 接受 束：
    若 做 计数 用 束 的 第 0 项 等于 0：
        返还 空。
    完。
    返还 束 的 第 0 项 的 第 0 项。
完。

采样_重复惩罚 接受 对数值、已生成、惩罚因子：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 对数值：
        做 追加 用 结果、对数值 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 已生成：
        词元值 为 已生成 的 第 i 项。
        若 词元值 大于或等于 0 并且 词元值 小于 做 计数 用 结果：
            若 结果 的 第 词元值 项 大于 0：
                结果 的 第 词元值 项 为 结果 的 第 词元值 项 乘 1000 除以 惩罚因子。
            否则：
                结果 的 第 词元值 项 为 结果 的 第 词元值 项 乘 惩罚因子 除以 1000。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

采样_频率惩罚 接受 对数值、已生成、惩罚值：
    可变 频率 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 对数值：
        做 追加 用 频率、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 已生成：
        词元值 为 已生成 的 第 i 项。
        若 词元值 大于或等于 0 并且 词元值 小于 做 计数 用 频率：
            频率 的 第 词元值 项 为 频率 的 第 词元值 项 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 对数值：
        做 追加 用 结果、对数值 的 第 i 项 减 频率 的 第 i 项 乘 惩罚值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

采样_动态温度 接受 对数值、基础温度、熵阈值：
    最大 为 做 Logits_最大值 用 对数值。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 对数值：
        可变 v 为 对数值 的 第 i 项 减 最大 加 100。
        若 v 小于 0：
            v 为 1。
        完。
        总 为 总 加 v。
        i 为 i 加 1。
    完。
    可变 熵 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 对数值：
        可变 v 为 对数值 的 第 i 项 减 最大 加 100。
        若 v 小于 1：
            v 为 1。
        完。
        p 为 v 乘 1000 除以 (总 加 1)。
        若 p 大于 0：
            熵 为 熵 加 p。
        完。
        i 为 i 加 1。
    完。
    可变 调整温度 为 基础温度。
    若 熵 大于 熵阈值：
        调整温度 为 基础温度 乘 800 除以 1000。
    否则若 熵 小于 熵阈值 除以 2：
        调整温度 为 基础温度 乘 1200 除以 1000。
    完。
    返还 调整温度。
完。

采样_Mirostat 接受 对数值、目标惊奇度、学习率、当前mu：
    可变 排序logits 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 对数值：
        做 追加 用 排序logits、对数值 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 n 为 做 计数 用 排序logits。
    i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 0。
        只要 j 小于 n 减 i 减 1：
            若 排序logits 的 第 j 项 小于 排序logits 的 第 j 加 1 项：
                临 为 排序logits 的 第 j 项。
                排序logits 的 第 j 项 为 排序logits 的 第 j 加 1 项。
                排序logits 的 第 j 加 1 项 为 临。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 截断k 为 1。
    只要 截断k 小于 n 并且 排序logits 的 第 截断k 项 大于 当前mu 减 目标惊奇度：
        截断k 为 截断k 加 1。
    完。
    索引 为 做 采样_随机 取余于 截断k。
    实际惊奇 为 0 减 排序logits 的 第 索引 项。
    新mu 为 当前mu 减 学习率 乘 (实际惊奇 减 目标惊奇度)。
    返还 一组：索引、新mu。
完。

可变 _采样_总次数 为 0。

可变 _采样_贪心次数 为 0。

可变 _采样_随机次数 为 0。

采样_记录贪心 接受 零参：
    _采样_总次数 为 _采样_总次数 加 1。
    _采样_贪心次数 为 _采样_贪心次数 加 1。
完。

采样_记录随机 接受 零参：
    _采样_总次数 为 _采样_总次数 加 1。
    _采样_随机次数 为 _采样_随机次数 加 1。
完。

采样_多样性评估 接受 零参：
    若 _采样_总次数 大于 0 并且 _采样_贪心次数 乘 100 除以 _采样_总次数 大于 90：
        返还 「采样过于确定(提高温度)」。
    完。
    若 _采样_总次数 大于 0 并且 _采样_随机次数 乘 100 除以 _采样_总次数 大于 90：
        返还 「采样过于随机(降低温度)」。
    完。
    返还 「采样策略正常」。
完。

本章 收束。
