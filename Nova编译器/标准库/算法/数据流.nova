《数据流》

引用《..·基础》。

流_加 接受 列表、值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表 的 第 i 项 加 值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_乘 接受 列表、值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表 的 第 i 项 乘 值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_取模 接受 列表、值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表 的 第 i 项 取余于 值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_过滤大于 接受 列表、阈值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 大于 阈值：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_过滤小于 接受 列表、阈值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 阈值：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_过滤等于 接受 列表、值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 等于 值：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_过滤非零 接受 列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 不等于 0：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_求和 接受 列表：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        总 为 总 加 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

流_求积 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 积 为 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        积 为 积 乘 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 积。
完。

流_最大 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 最大 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 大于 最大：
            最大 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

流_最小 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 最小 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 最小：
            最小 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小。
完。

流_平均 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    返还 做 流_求和 用 列表 除以 做 计数 用 列表。
完。

流_zip 接受 a、b：
    可变 结果 为 一组：（空）。
    可变 长 为 做 计数 用 a。
    若 做 计数 用 b 小于 长：
        长 为 做 计数 用 b。
    完。
    可变 i 为 0。
    只要 i 小于 长：
        做 追加 用 结果、一组：a 的 第 i 项、b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_展平 接受 二维：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 二维：
        子 为 二维 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 子：
            做 追加 用 结果、子 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_去重 接受 列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        可变 已存在 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 结果：
            若 结果 的 第 j 项 等于 列表 的 第 i 项：
                已存在 为 1。
            完。
            j 为 j 加 1。
        完。
        若 已存在 等于 0：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_切片 接受 列表、起始、长度：
    可变 结果 为 一组：（空）。
    可变 i 为 起始。
    可变 计 为 0。
    只要 i 小于 做 计数 用 列表 并且 计 小于 长度：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
        计 为 计 加 1。
    完。
    返还 结果。
完。

流_反转 接受 列表：
    可变 结果 为 一组：（空）。
    可变 i 为 做 计数 用 列表 减 1。
    只要 i 大于或等于 0：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 结果。
完。

流_频率 接受 列表：
    可变 计数列表 为 一组：（空）。
    可变 值列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        可变 找到 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 值列表：
            若 值列表 的 第 j 项 等于 列表 的 第 i 项：
                计数列表 的 第 j 项 为 计数列表 的 第 j 项 加 1。
                找到 为 1。
            完。
            j 为 j 加 1。
        完。
        若 找到 等于 0：
            做 追加 用 值列表、列表 的 第 i 项。
            做 追加 用 计数列表、1。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 值列表：
        做 追加 用 结果、一组：值列表 的 第 i 项、计数列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_前缀和 接受 列表：
    可变 结果 为 一组：（空）。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        总 为 总 加 列表 的 第 i 项。
        做 追加 用 结果、总。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_差分 接受 列表：
    可变 结果 为 一组：（空）。
    若 做 计数 用 列表 等于 0：
        返还 结果。
    完。
    做 追加 用 结果、列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表 的 第 i 项 减 列表 的 第 i 减 1 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

流_窗口和 接受 列表、窗口大小：
    可变 结果 为 一组：（空）。
    若 做 计数 用 列表 小于 窗口大小：
        返还 结果。
    完。
    可变 窗口总 为 0。
    可变 i 为 0。
    只要 i 小于 窗口大小：
        窗口总 为 窗口总 加 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    做 追加 用 结果、窗口总。
    i 为 窗口大小。
    只要 i 小于 做 计数 用 列表：
        窗口总 为 窗口总 加 列表 的 第 i 项 减 列表 的 第 i 减 窗口大小 项。
        做 追加 用 结果、窗口总。
        i 为 i 加 1。
    完。
    返还 结果。
完。

本章 收束。
