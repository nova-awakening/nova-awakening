《数论》

引用《..·基础》。

引用《..·数学·密码学·密码学》。

引用《.·数学扩展》。

数论_扩展gcd 接受 a、b：
    若 b 等于 0：
        返还 一组：a、1、0。
    完。
    结果 为 做 数论_扩展gcd 用 b、a 取余于 b。
    返还 一组：结果 的 第 0 项、结果 的 第 2 项、结果 的 第 1 项 减 a 除以 b 乘 结果 的 第 2 项。
完。

模逆元 接受 a、m：
    结果 为 做 数论_扩展gcd 用 a、m。
    若 结果 的 第 0 项 不等于 1：
        返还 负 1。
    完。
    返还 (结果 的 第 1 项 取余于 m 加 m) 取余于 m。
完。

crt_两个 接受 r1、m1、r2、m2：
    结果 为 做 数论_扩展gcd 用 m1、m2。
    g 为 结果 的 第 0 项。
    若 (r2 减 r1) 取余于 g 不等于 0：
        返还 负 1。
    完。
    lcm_val 为 m1 除以 g 乘 m2。
    可变 x 为 (r1 加 m1 乘 ((r2 减 r1) 除以 g 乘 结果 的 第 1 项 取余于 (m2 除以 g))) 取余于 lcm_val。
    若 x 小于 0：
        x 为 x 加 lcm_val。
    完。
    返还 x。
完。

数论_质因数分解 接受 n：
    可变 因子 为 一组：（空）。
    可变 nn 为 0。
    可变 d 为 2。
    只要 d 乘 d 小于或等于 nn：
        只要 nn 取余于 d 等于 0：
            做 追加 用 因子、d。
            nn 为 nn 除以 d。
        完。
        d 为 d 加 1。
    完。
    若 nn 大于 1：
        做 追加 用 因子、nn。
    完。
    返还 因子。
完。

所有因数 接受 n：
    可变 因数 为 一组：（空）。
    可变 i 为 1。
    只要 i 乘 i 小于或等于 n：
        若 n 取余于 i 等于 0：
            做 追加 用 因数、i。
            若 i 不等于 n 除以 i：
                做 追加 用 因数、n 除以 i。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 因数。
完。

因数个数 接受 n：
    返还 做 计数 用 做 所有因数 用 n。
完。

因数和 接受 n：
    因数 为 做 所有因数 用 n。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 因数：
        总 为 总 加 因数 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

是完美数 接受 n：
    若 n 小于或等于 1：
        返还 0。
    完。
    返还 做 因数和 用 n 减 n 等于 n。
完。

莫比乌斯 接受 n：
    若 n 等于 1：
        返还 1。
    完。
    因子 为 做 数论_质因数分解 用 n。
    可变 i 为 0。
    只要 i 小于 做 计数 用 因子 减 1：
        若 因子 的 第 i 项 等于 因子 的 第 i 加 1 项：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 因子 取余于 2 等于 0：
        返还 1。
    完。
    返还 负 1。
完。

三角数 接受 n：
    返还 n 乘 (n 加 1) 除以 2。
完。

五角数 接受 n：
    返还 n 乘 (3 乘 n 减 1) 除以 2。
完。

是平方数 接受 n：
    若 n 小于 0：
        返还 0。
    完。
    r 为 做 整数sqrt 用 n。
    返还 r 乘 r 等于 n。
完。

约瑟夫 接受 n、k：
    可变 结果 为 0。
    可变 i 为 2。
    只要 i 小于或等于 n：
        结果 为 (结果 加 k) 取余于 i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

卡特兰数 接受 n：
    若 n 小于或等于 1：
        返还 1。
    完。
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 n：
        做 追加 用 dp、0。
        i 为 i 加 1。
    完。
    dp 的 第 0 项 为 1。
    dp 的 第 1 项 为 1。
    i 为 2。
    只要 i 小于或等于 n：
        可变 j 为 0。
        只要 j 小于 i：
            dp 的 第 i 项 为 dp 的 第 i 项 加 dp 的 第 j 项 乘 dp 的 第 i 减 1 减 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 dp 的 第 n 项。
完。

斯特林数 接受 n、k：
    若 n 等于 0 并且 k 等于 0：
        返还 1。
    完。
    若 n 等于 0 或者 k 等于 0：
        返还 0。
    完。
    若 k 等于 1 或者 k 等于 n：
        返还 1。
    完。
    返还 k 乘 做 斯特林数 用 n 减 1、k 加 做 斯特林数 用 n 减 1、k 减 1。
完。

贝尔数 接受 n：
    若 n 等于 0：
        返还 1。
    完。
    可变 总 为 0。
    可变 k 为 0。
    只要 k 小于或等于 n：
        总 为 总 加 做 斯特林数 用 n、k。
        k 为 k 加 1。
    完。
    返还 总。
完。

划分数 接受 n：
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 n：
        做 追加 用 dp、0。
        i 为 i 加 1。
    完。
    dp 的 第 0 项 为 1。
    i 为 1。
    只要 i 小于或等于 n：
        可变 j 为 i。
        只要 j 小于或等于 n：
            dp 的 第 j 项 为 dp 的 第 j 项 加 dp 的 第 j 减 i 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 dp 的 第 n 项。
完。

高斯加 接受 a实、a虚、b实、b虚：
    返还 一组：a实 加 b实、a虚 加 b虚。
完。

高斯乘 接受 a实、a虚、b实、b虚：
    返还 一组：a实 乘 b实 减 a虚 乘 b虚、a实 乘 b虚 加 a虚 乘 b实。
完。

高斯模 接受 a实、a虚：
    返还 做 整数sqrt 用 a实 乘 a实 加 a虚 乘 a虚。
完。

数论_扩展GCD 接受 a、b：
    返还 做 数论_扩展gcd 用 a、b。
完。

数论_模逆 接受 a、m：
    返还 做 模逆元 用 a、m。
完。

数论_模幂 接受 底数、指数、模：
    返还 做 快速幂 用 底数、指数、模。
完。

CRT_两个 接受 r1、m1、r2、m2：
    返还 做 crt_两个 用 r1、m1、r2、m2。
完。

本章 收束。
