《搜索算法》

引用《..·基础》。

二分搜索 接受 列表、目标：
    可变 低 为 0。
    可变 高 为 做 计数 用 列表 减 1。
    只要 低 小于或等于 高：
        中 为 (低 加 高) 除以 2。
        若 列表 的 第 中 项 等于 目标：
            返还 中。
        完。
        若 列表 的 第 中 项 小于 目标：
            低 为 中 加 1。
        否则：
            高 为 中 减 1。
        完。
    完。
    返还 负 1。
完。

二分_下界 接受 列表、目标：
    可变 低 为 0。
    可变 高 为 做 计数 用 列表。
    只要 低 小于 高：
        中 为 (低 加 高) 除以 2。
        若 列表 的 第 中 项 小于 目标：
            低 为 中 加 1。
        否则：
            高 为 中。
        完。
    完。
    返还 低。
完。

二分_上界 接受 列表、目标：
    可变 低 为 0。
    可变 高 为 做 计数 用 列表。
    只要 低 小于 高：
        中 为 (低 加 高) 除以 2。
        若 列表 的 第 中 项 小于或等于 目标：
            低 为 中 加 1。
        否则：
            高 为 中。
        完。
    完。
    返还 低。
完。

二分_第一个 接受 列表、目标：
    位置 为 做 二分_下界 用 列表、目标。
    若 位置 小于 做 计数 用 列表 并且 列表 的 第 位置 项 等于 目标：
        返还 位置。
    完。
    返还 负 1。
完。

二分_最后一个 接受 列表、目标：
    位置 为 做 二分_上界 用 列表、目标 减 1。
    若 位置 大于或等于 0 并且 列表 的 第 位置 项 等于 目标：
        返还 位置。
    完。
    返还 负 1。
完。

三分_最大 接受 列表：
    可变 低 为 0。
    可变 高 为 做 计数 用 列表 减 1。
    只要 高 减 低 大于 2：
        m1 为 低 加 (高 减 低) 除以 3。
        m2 为 高 减 (高 减 低) 除以 3。
        若 列表 的 第 m1 项 小于 列表 的 第 m2 项：
            低 为 m1。
        否则：
            高 为 m2。
        完。
    完。
    可变 最大idx 为 低。
    可变 i 为 低 加 1。
    只要 i 小于或等于 高：
        若 列表 的 第 i 项 大于 列表 的 第 最大idx 项：
            最大idx 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最大idx。
完。

旋转数组搜索 接受 列表、目标：
    可变 低 为 0。
    可变 高 为 做 计数 用 列表 减 1。
    只要 低 小于或等于 高：
        中 为 (低 加 高) 除以 2。
        若 列表 的 第 中 项 等于 目标：
            返还 中。
        完。
        若 列表 的 第 低 项 小于或等于 列表 的 第 中 项：
            若 目标 大于或等于 列表 的 第 低 项 并且 目标 小于 列表 的 第 中 项：
                高 为 中 减 1。
            否则：
                低 为 中 加 1。
            完。
        否则：
            若 目标 大于 列表 的 第 中 项 并且 目标 小于或等于 列表 的 第 高 项：
                低 为 中 加 1。
            否则：
                高 为 中 减 1。
            完。
        完。
    完。
    返还 负 1。
完。

矩阵搜索 接受 矩阵、行数、列数、目标：
    可变 低 为 0。
    可变 高 为 行数 乘 列数 减 1。
    只要 低 小于或等于 高：
        中 为 (低 加 高) 除以 2。
        行 为 矩阵 的 第 中 除以 列数 项。
        值 为 行 的 第 中 取余于 列数 项。
        若 值 等于 目标：
            返还 1。
        完。
        若 值 小于 目标：
            低 为 中 加 1。
        否则：
            高 为 中 减 1。
        完。
    完。
    返还 0。
完。

插值搜索 接受 列表、目标：
    可变 低 为 0。
    可变 高 为 做 计数 用 列表 减 1。
    只要 低 小于或等于 高 并且 目标 大于或等于 列表 的 第 低 项 并且 目标 小于或等于 列表 的 第 高 项：
        若 列表 的 第 高 项 等于 列表 的 第 低 项：
            若 列表 的 第 低 项 等于 目标：
                返还 低。
            完。
            返还 负 1。
        完。
        可变 位置 为 低 加 (目标 减 列表 的 第 低 项) 乘 (高 减 低) 除以 (列表 的 第 高 项 减 列表 的 第 低 项)。
        若 位置 小于 低：
            位置 为 低。
        完。
        若 位置 大于 高：
            位置 为 高。
        完。
        若 列表 的 第 位置 项 等于 目标：
            返还 位置。
        完。
        若 列表 的 第 位置 项 小于 目标：
            低 为 位置 加 1。
        否则：
            高 为 位置 减 1。
        完。
    完。
    返还 负 1。
完。

指数搜索 接受 列表、目标：
    n 为 做 计数 用 列表。
    若 n 等于 0：
        返还 负 1。
    完。
    若 列表 的 第 0 项 等于 目标：
        返还 0。
    完。
    可变 边界 为 1。
    只要 边界 小于 n 并且 列表 的 第 边界 项 小于或等于 目标：
        边界 为 边界 乘 2。
    完。
    可变 低 为 边界 除以 2。
    可变 高 为 边界。
    若 高 大于或等于 n：
        高 为 n 减 1。
    完。
    只要 低 小于或等于 高：
        中 为 (低 加 高) 除以 2。
        若 列表 的 第 中 项 等于 目标：
            返还 中。
        完。
        若 列表 的 第 中 项 小于 目标：
            低 为 中 加 1。
        否则：
            高 为 中 减 1。
        完。
    完。
    返还 负 1。
完。

跳跃搜索 接受 列表、目标：
    n 为 做 计数 用 列表。
    若 n 等于 0：
        返还 负 1。
    完。
    可变 步长 为 1。
    只要 步长 乘 步长 小于 n：
        步长 为 步长 加 1。
    完。
    可变 前 为 0。
    可变 当前 为 步长。
    只要 当前 小于 n 并且 列表 的 第 当前 项 小于 目标：
        前 为 当前。
        当前 为 当前 加 步长。
    完。
    可变 i 为 前。
    只要 i 小于 n 并且 i 小于或等于 当前：
        若 列表 的 第 i 项 等于 目标：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

三分搜索_最小 接受 函数f、左、右、精度：
    可变 lo 为 左。
    可变 hi 为 右。
    只要 hi 减 lo 大于 精度：
        m1 为 lo 加 (hi 减 lo) 除以 3。
        m2 为 hi 减 (hi 减 lo) 除以 3。
        若 做 函数f 用 m1 小于 做 函数f 用 m2：
            hi 为 m2。
        否则：
            lo 为 m1。
        完。
    完。
    返还 (lo 加 hi) 除以 2。
完。

快速选择 接受 列表、k：
    n 为 做 计数 用 列表。
    若 k 小于或等于 0 或者 k 大于 n：
        返还 空。
    完。
    可变 副本 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 副本、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 lo 为 0。
    可变 hi 为 n 减 1。
    目标 为 k 减 1。
    只要 lo 小于 hi：
        枢轴 为 副本 的 第 hi 项。
        可变 j 为 lo。
        i 为 lo。
        只要 i 小于 hi：
            若 副本 的 第 i 项 小于或等于 枢轴：
                暂 为 副本 的 第 i 项。
                副本 的 第 i 项 为 副本 的 第 j 项。
                副本 的 第 j 项 为 暂。
                j 为 j 加 1。
            完。
            i 为 i 加 1。
        完。
        暂2 为 副本 的 第 hi 项。
        副本 的 第 hi 项 为 副本 的 第 j 项。
        副本 的 第 j 项 为 暂2。
        若 j 等于 目标：
            返还 副本 的 第 j 项。
        完。
        若 j 小于 目标：
            lo 为 j 加 1。
        否则：
            hi 为 j 减 1。
        完。
    完。
    返还 副本 的 第 lo 项。
完。

查找众数 接受 列表：
    可变 候选 为 列表 的 第 0 项。
    可变 计数器 为 1。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 等于 候选：
            计数器 为 计数器 加 1。
        否则：
            计数器 为 计数器 减 1。
        完。
        若 计数器 等于 0：
            候选 为 列表 的 第 i 项。
            计数器 为 1。
        完。
        i 为 i 加 1。
    完。
    返还 候选。
完。

本章 收束。
