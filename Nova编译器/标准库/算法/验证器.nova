《验证器》

验证_邮箱 接受 文本：
    可变 有at 为 0。
    可变 at位 为 负 1。
    可变 有点 为 0。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 文本：
        c 为 做 字节取 用 文本、i。
        若 c 等于 64：
            有at 为 有at 加 1。
            at位 为 i。
        完。
        若 c 等于 字符_句点 并且 at位 大于或等于 0：
            有点 为 1。
        完。
        i 为 i 加 1。
    完。
    若 有at 不等于 1：
        返还 0。
    完。
    若 at位 等于 0 或者 at位 等于 做 字节长度 用 文本 减 1：
        返还 0。
    完。
    若 有点 等于 0：
        返还 0。
    完。
    返还 1。
完。

验证_IPv4 接受 文本：
    可变 段 为 0。
    可变 当前值 为 0。
    可变 数字计 为 0。
    可变 i 为 0。
    只要 i 小于或等于 做 字节长度 用 文本：
        可变 是点 为 0。
        若 i 等于 做 字节长度 用 文本：
            是点 为 1。
        否则若 做 字节取 用 文本、i 等于 字符_句点：
            是点 为 1。
        完。
        若 是点 等于 1：
            若 数字计 等于 0 或者 数字计 大于 3：
                返还 0。
            完。
            若 当前值 大于 255：
                返还 0。
            完。
            段 为 段 加 1。
            当前值 为 0。
            数字计 为 0。
        否则：
            c 为 做 字节取 用 文本、i。
            若 c 小于 48 或者 c 大于 57：
                返还 0。
            完。
            当前值 为 当前值 乘 10 加 c 减 48。
            数字计 为 数字计 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 段 等于 4。
完。

验证_Luhn 接受 数字串：
    n 为 做 字节长度 用 数字串。
    可变 总和 为 0。
    可变 奇偶 为 0。
    可变 i 为 n 减 1。
    只要 i 大于或等于 0：
        可变 d 为 做 字节取 用 数字串、i 减 48。
        若 d 小于 0 或者 d 大于 9：
            返还 0。
        完。
        若 奇偶 等于 1：
            d 为 d 乘 2。
            若 d 大于 9：
                d 为 d 减 9。
            完。
        完。
        总和 为 总和 加 d。
        奇偶 为 1 减 奇偶。
        i 为 i 减 1。
    完。
    返还 总和 取余于 10 等于 0。
完。

验证_ISBN10 接受 文本：
    可变 总和 为 0。
    可变 权重 为 10。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 文本：
        c 为 做 字节取 用 文本、i。
        若 c 等于 字符_减号：
            i 为 i 加 1。
        否则：
            可变 d 为 0。
            若 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
                d 为 c 减 48。
            否则若 c 等于 88 或者 c 等于 120：
                d 为 10。
            否则：
                返还 0。
            完。
            总和 为 总和 加 d 乘 权重。
            权重 为 权重 减 1。
            i 为 i 加 1。
        完。
    完。
    若 权重 不等于 0：
        返还 0。
    完。
    返还 总和 取余于 11 等于 0。
完。

验证_纯数字 接受 文本：
    若 做 字节长度 用 文本 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 文本：
        c 为 做 字节取 用 文本、i。
        若 c 小于 48 或者 c 大于 57：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

验证_纯字母 接受 文本：
    若 做 字节长度 用 文本 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 文本：
        c 为 做 字节取 用 文本、i。
        若 非 (c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z 或者 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z)：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

验证_字母数字 接受 文本：
    若 做 字节长度 用 文本 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 文本：
        c 为 做 字节取 用 文本、i。
        若 非 (c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z 或者 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z 或者 c 大于或等于 字符_零 并且 c 小于或等于 字符_九)：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

验证_密码强度 接受 密码：
    长 为 做 字节长度 用 密码。
    可变 分 为 0。
    若 长 大于或等于 8：
        分 为 分 加 1。
    完。
    可变 有大写 为 0。
    可变 有小写 为 0。
    可变 有数字 为 0。
    可变 有特殊 为 0。
    可变 i 为 0。
    只要 i 小于 长：
        c 为 做 字节取 用 密码、i。
        若 c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z：
            有大写 为 1。
        完。
        若 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z：
            有小写 为 1。
        完。
        若 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
            有数字 为 1。
        完。
        若 c 大于或等于 33 并且 c 小于或等于 字符_斜杠 或者 c 大于或等于 字符_冒号 并且 c 小于或等于 64：
            有特殊 为 1。
        完。
        i 为 i 加 1。
    完。
    分 为 分 加 有大写 加 有小写 加 有数字 加 有特殊。
    若 分 大于 4：
        分 为 4。
    完。
    返还 分。
完。

验证_括号平衡 接受 文本：
    可变 圆 为 0。
    可变 方 为 0。
    可变 花 为 0。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 文本：
        c 为 做 字节取 用 文本、i。
        若 c 等于 字符_左圆括：
            圆 为 圆 加 1。
        完。
        若 c 等于 字符_右圆括：
            圆 为 圆 减 1。
        完。
        若 c 等于 字符_左方括：
            方 为 方 加 1。
        完。
        若 c 等于 字符_右方括：
            方 为 方 减 1。
        完。
        若 c 等于 字符_左花括：
            花 为 花 加 1。
        完。
        若 c 等于 字符_右花括：
            花 为 花 减 1。
        完。
        若 圆 小于 0 或者 方 小于 0 或者 花 小于 0：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 圆 等于 0 并且 方 等于 0 并且 花 等于 0。
完。

验证_日期格式 接受 文本：
    若 做 字节长度 用 文本 不等于 10：
        返还 0。
    完。
    若 做 字节取 用 文本、4 不等于 字符_减号 或者 做 字节取 用 文本、7 不等于 字符_减号：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 10：
        若 i 不等于 4 并且 i 不等于 7：
            c 为 做 字节取 用 文本、i。
            若 c 小于 48 或者 c 大于 57：
                返还 0。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

验证_十六进制 接受 文本：
    若 做 字节长度 用 文本 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 文本：
        c 为 做 字节取 用 文本、i。
        若 非 (c 大于或等于 字符_零 并且 c 小于或等于 字符_九 或者 c 大于或等于 字符_大写A 并且 c 小于或等于 70 或者 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写f)：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

验证器_验证_范围 接受 值、最小、最大：
    返还 值 大于或等于 最小 并且 值 小于或等于 最大。
完。

验证_非负 接受 值：
    返还 值 大于或等于 0。
完。

验证_正数 接受 值：
    返还 值 大于 0。
完。

验证_非空列表 接受 列表：
    返还 做 计数 用 列表 大于 0。
完。

验证_列表长度 接受 列表、期望长度：
    返还 做 计数 用 列表 等于 期望长度。
完。

验证_所有正数 接受 列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于或等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

本章 收束。
