《双端队列》

引用《..·基础》。

deque_创建 接受 容量：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 一组：数据、0、容量、0。
完。

deque_头插 接受 dq、值：
    若 dq 的 第 4 项 大于或等于 dq 的 第 3 项：
        返还 0。
    完。
    dq 的 第 1 项 为 (dq 的 第 1 项 减 1 加 dq 的 第 3 项) 取余于 dq 的 第 3 项。
    dq 的 第 0 项 的 第 dq 的 第 1 项 项 为 值。
    dq 的 第 4 项 为 dq 的 第 4 项 加 1。
    返还 1。
完。

deque_尾插 接受 dq、值：
    若 dq 的 第 4 项 大于或等于 dq 的 第 3 项：
        返还 0。
    完。
    dq 的 第 0 项 的 第 dq 的 第 2 项 项 为 值。
    dq 的 第 2 项 为 (dq 的 第 2 项 加 1) 取余于 dq 的 第 3 项。
    dq 的 第 4 项 为 dq 的 第 4 项 加 1。
    返还 1。
完。

deque_头删 接受 dq：
    若 dq 的 第 4 项 等于 0：
        返还 空。
    完。
    值 为 dq 的 第 0 项 的 第 dq 的 第 1 项 项。
    dq 的 第 1 项 为 (dq 的 第 1 项 加 1) 取余于 dq 的 第 3 项。
    dq 的 第 4 项 为 dq 的 第 4 项 减 1。
    返还 值。
完。

deque_尾删 接受 dq：
    若 dq 的 第 4 项 等于 0：
        返还 空。
    完。
    dq 的 第 2 项 为 (dq 的 第 2 项 减 1 加 dq 的 第 3 项) 取余于 dq 的 第 3 项。
    值 为 dq 的 第 0 项 的 第 dq 的 第 2 项 项。
    dq 的 第 4 项 为 dq 的 第 4 项 减 1。
    返还 值。
完。

deque_头 接受 dq：
    若 dq 的 第 4 项 等于 0：
        返还 空。
    完。
    返还 dq 的 第 0 项 的 第 dq 的 第 1 项 项。
完。

deque_尾 接受 dq：
    若 dq 的 第 4 项 等于 0：
        返还 空。
    完。
    索引 为 (dq 的 第 2 项 减 1 加 dq 的 第 3 项) 取余于 dq 的 第 3 项。
    返还 dq 的 第 0 项 的 第 索引 项。
完。

deque_大小 接受 dq：
    返还 dq 的 第 4 项。
完。

deque_空 接受 dq：
    返还 dq 的 第 4 项 等于 0。
完。

最小栈_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）。
完。

最小栈_压入 接受 栈、值：
    做 追加 用 栈 的 第 0 项、值。
    最小列 为 栈 的 第 1 项。
    若 做 计数 用 最小列 等于 0 或者 值 小于或等于 最小列 的 第 做 计数 用 最小列 减 1 项：
        做 追加 用 最小列、值。
    否则：
        做 追加 用 最小列、最小列 的 第 做 计数 用 最小列 减 1 项。
    完。
完。

双端队列_最小栈_弹出 接受 栈：
    若 做 计数 用 栈 的 第 0 项 等于 0：
        返还 空。
    完。
    值 为 栈 的 第 0 项 的 第 做 计数 用 栈 的 第 0 项 减 1 项。
    做 删除 用 栈 的 第 0 项、做 计数 用 栈 的 第 0 项 减 1。
    做 删除 用 栈 的 第 1 项、做 计数 用 栈 的 第 1 项 减 1。
    返还 值。
完。

最小栈_最小 接受 栈：
    若 做 计数 用 栈 的 第 1 项 等于 0：
        返还 空。
    完。
    返还 栈 的 第 1 项 的 第 做 计数 用 栈 的 第 1 项 减 1 项。
完。

最小栈_顶 接受 栈：
    若 做 计数 用 栈 的 第 0 项 等于 0：
        返还 空。
    完。
    返还 栈 的 第 0 项 的 第 做 计数 用 栈 的 第 0 项 减 1 项。
完。

双端队列_最小栈_大小 接受 栈：
    返还 做 计数 用 栈 的 第 0 项。
完。

单调栈_下一个更大 接受 列表：
    n 为 做 计数 用 列表。
    可变 结果 为 一组：（空）。
    可变 栈 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、负 1。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        只要 做 计数 用 栈 大于 0 并且 列表 的 第 i 项 大于 列表 的 第 栈 的 第 做 计数 用 栈 减 1 项 项：
            索引 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            结果 的 第 索引 项 为 列表 的 第 i 项。
        完。
        做 追加 用 栈、i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

单调栈_下一个更小 接受 列表：
    n 为 做 计数 用 列表。
    可变 结果 为 一组：（空）。
    可变 栈 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、负 1。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        只要 做 计数 用 栈 大于 0 并且 列表 的 第 i 项 小于 列表 的 第 栈 的 第 做 计数 用 栈 减 1 项 项：
            索引 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            结果 的 第 索引 项 为 列表 的 第 i 项。
        完。
        做 追加 用 栈、i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

直方图最大矩形 接受 高度：
    n 为 做 计数 用 高度。
    可变 栈 为 一组：（空）。
    可变 最大面积 为 0。
    可变 i 为 0。
    只要 i 小于或等于 n：
        可变 h 为 0。
        若 i 小于 n：
            h 为 高度 的 第 i 项。
        完。
        只要 做 计数 用 栈 大于 0 并且 h 小于 高度 的 第 栈 的 第 做 计数 用 栈 减 1 项 项：
            顶idx 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            可变 宽 为 i。
            若 做 计数 用 栈 大于 0：
                宽 为 i 减 栈 的 第 做 计数 用 栈 减 1 项 减 1。
            完。
            面积 为 高度 的 第 顶idx 项 乘 宽。
            若 面积 大于 最大面积：
                最大面积 为 面积。
            完。
        完。
        做 追加 用 栈、i。
        i 为 i 加 1。
    完。
    返还 最大面积。
完。

滑动窗口最大 接受 列表、k：
    n 为 做 计数 用 列表。
    可变 结果 为 一组：（空）。
    可变 dq 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        只要 做 计数 用 dq 大于 0 并且 dq 的 第 0 项 小于或等于 i 减 k：
            做 删除 用 dq。
        完。
        只要 做 计数 用 dq 大于 0 并且 列表 的 第 dq 的 第 做 计数 用 dq 减 1 项 项 小于或等于 列表 的 第 i 项：
            做 删除 用 dq、做 计数 用 dq 减 1。
        完。
        做 追加 用 dq、i。
        若 i 大于或等于 k 减 1：
            做 追加 用 结果、列表 的 第 dq 的 第 0 项 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _环形_数据 为 一组：（空）。

可变 _环形_容量 为 0。

可变 _环形_头 为 0。

可变 _环形_尾 为 0。

可变 _环形_大小 为 0。

环形双端_创建 接受 容量：
    _环形_容量 为 容量。
    _环形_头 为 0。
    _环形_尾 为 0。
    _环形_大小 为 0。
    _环形_数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 _环形_数据、0。
        i 为 i 加 1。
    完。
完。

环形双端_前插 接受 值：
    若 _环形_大小 大于或等于 _环形_容量：
        返还 假。
    完。
    _环形_头 为 (_环形_头 减 1 加 _环形_容量) 取余于 _环形_容量。
    _环形_数据 的 第 _环形_头 项 为 值。
    _环形_大小 为 _环形_大小 加 1。
    返还 真。
完。

环形双端_后插 接受 值：
    若 _环形_大小 大于或等于 _环形_容量：
        返还 假。
    完。
    _环形_数据 的 第 _环形_尾 项 为 值。
    _环形_尾 为 (_环形_尾 加 1) 取余于 _环形_容量。
    _环形_大小 为 _环形_大小 加 1。
    返还 真。
完。

环形双端_前删 接受 零参：
    若 _环形_大小 等于 0：
        返还 空。
    完。
    值 为 _环形_数据 的 第 _环形_头 项。
    _环形_头 为 (_环形_头 加 1) 取余于 _环形_容量。
    _环形_大小 为 _环形_大小 减 1。
    返还 值。
完。

环形双端_后删 接受 零参：
    若 _环形_大小 等于 0：
        返还 空。
    完。
    _环形_尾 为 (_环形_尾 减 1 加 _环形_容量) 取余于 _环形_容量。
    _环形_大小 为 _环形_大小 减 1。
    返还 _环形_数据 的 第 _环形_尾 项。
完。

环形双端_前看 接受 零参：
    若 _环形_大小 等于 0：
        返还 空。
    完。
    返还 _环形_数据 的 第 _环形_头 项。
完。

环形双端_后看 接受 零参：
    若 _环形_大小 等于 0：
        返还 空。
    完。
    返还 _环形_数据 的 第 (_环形_尾 减 1 加 _环形_容量) 取余于 _环形_容量 项。
完。

环形双端_大小 接受 零参：
    返还 _环形_大小。
完。

环形双端_为空 接受 零参：
    返还 _环形_大小 等于 0。
完。

环形双端_已满 接受 零参：
    返还 _环形_大小 大于或等于 _环形_容量。
完。

环形双端_取 接受 索引：
    若 索引 小于 0 或者 索引 大于或等于 _环形_大小：
        返还 空。
    完。
    返还 _环形_数据 的 第 (_环形_头 加 索引) 取余于 _环形_容量 项。
完。

环形双端_转列表 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 _环形_大小：
        做 追加 用 结果、_环形_数据 的 第 (_环形_头 加 i) 取余于 _环形_容量 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

本章 收束。
