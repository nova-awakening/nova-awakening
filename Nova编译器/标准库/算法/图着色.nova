《图着色》

引用《..·基础》。

图着色_贪心 接受 邻接表：
    n 为 做 计数 用 邻接表。
    可变 颜色 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 颜色、负 1。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 已用 为 一组：（空）。
        可变 k 为 0。
        只要 k 小于 n：
            做 追加 用 已用、0。
            k 为 k 加 1。
        完。
        邻居 为 邻接表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 邻居：
            邻色 为 颜色 的 第 邻居 的 第 j 项 项。
            若 邻色 大于或等于 0 并且 邻色 小于 n：
                已用 的 第 邻色 项 为 1。
            完。
            j 为 j 加 1。
        完。
        可变 c 为 0。
        只要 c 小于 n 并且 已用 的 第 c 项 等于 1：
            c 为 c 加 1。
        完。
        颜色 的 第 i 项 为 c。
        i 为 i 加 1。
    完。
    返还 颜色。
完。

图着色_颜色数 接受 颜色列表：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 颜色列表：
        若 颜色列表 的 第 i 项 大于 最大：
            最大 为 颜色列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大 加 1。
完。

图着色_验证 接受 邻接表、颜色列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 邻接表：
        邻居 为 邻接表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 邻居：
            若 颜色列表 的 第 i 项 等于 颜色列表 的 第 邻居 的 第 j 项 项：
                返还 0。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

数独_验证 接受 棋盘：
    可变 i 为 0。
    只要 i 小于 9：
        若 做 数独_检查组 用 棋盘 的 第 i 项 等于 0：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    可变 j 为 0。
    只要 j 小于 9：
        可变 列 为 一组：（空）。
        i 为 0。
        只要 i 小于 9：
            行 为 棋盘 的 第 i 项。
            做 追加 用 列、行 的 第 j 项。
            i 为 i 加 1。
        完。
        若 做 数独_检查组 用 列 等于 0：
            返还 0。
        完。
        j 为 j 加 1。
    完。
    可变 br 为 0。
    只要 br 小于 3：
        可变 bc 为 0。
        只要 bc 小于 3：
            可变 宫 为 一组：（空）。
            i 为 0。
            只要 i 小于 3：
                j 为 0。
                只要 j 小于 3：
                    行idx 为 br 乘 3 加 i。
                    列idx 为 bc 乘 3 加 j。
                    行r 为 棋盘 的 第 行idx 项。
                    做 追加 用 宫、行r 的 第 列idx 项。
                    j 为 j 加 1。
                完。
                i 为 i 加 1。
            完。
            若 做 数独_检查组 用 宫 等于 0：
                返还 0。
            完。
            bc 为 bc 加 1。
        完。
        br 为 br 加 1。
    完。
    返还 1。
完。

数独_检查组 接受 组：
    可变 seen 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 10：
        做 追加 用 seen、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 组：
        v 为 组 的 第 i 项。
        若 v 不等于 0：
            若 v 小于 1 或者 v 大于 9：
                返还 0。
            完。
            若 seen 的 第 v 项 等于 1：
                返还 0。
            完。
            seen 的 第 v 项 为 1。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

数独_求解 接受 棋盘：
    返还 做 数独_回溯 用 棋盘、0。
完。

数独_回溯 接受 棋盘、位置：
    若 位置 大于或等于 81：
        返还 1。
    完。
    行 为 位置 除以 9。
    列 为 位置 取余于 9。
    行r 为 棋盘 的 第 行 项。
    若 行r 的 第 列 项 不等于 0：
        返还 做 数独_回溯 用 棋盘、位置 加 1。
    完。
    可变 n 为 1。
    只要 n 小于或等于 9：
        若 做 数独_可放 用 棋盘、行、列、n 等于 1：
            行r 的 第 列 项 为 n。
            若 做 数独_回溯 用 棋盘、位置 加 1 等于 1：
                返还 1。
            完。
            行r 的 第 列 项 为 0。
        完。
        n 为 n 加 1。
    完。
    返还 0。
完。

数独_可放 接受 棋盘、行、列、数：
    行r 为 棋盘 的 第 行 项。
    可变 i 为 0。
    只要 i 小于 9：
        若 行r 的 第 i 项 等于 数：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 9：
        ir 为 棋盘 的 第 i 项。
        若 ir 的 第 列 项 等于 数：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    宫行 为 行 除以 3 乘 3。
    宫列 为 列 除以 3 乘 3。
    i 为 0。
    只要 i 小于 3：
        可变 j 为 0。
        只要 j 小于 3：
            cr 为 棋盘 的 第 宫行 加 i 项。
            若 cr 的 第 宫列 加 j 项 等于 数：
                返还 0。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

WP_着色 接受 节点数、邻接表：
    可变 度列表 为 一组：（空）。
    可变 索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点数：
        做 追加 用 度列表、做 计数 用 邻接表 的 第 i 项。
        做 追加 用 索引、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 节点数：
        可变 最大idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 节点数：
            若 度列表 的 第 j 项 大于 度列表 的 第 最大idx 项：
                最大idx 为 j。
            完。
            j 为 j 加 1。
        完。
        暂d 为 度列表 的 第 i 项。
        度列表 的 第 i 项 为 度列表 的 第 最大idx 项。
        度列表 的 第 最大idx 项 为 暂d。
        暂i 为 索引 的 第 i 项。
        索引 的 第 i 项 为 索引 的 第 最大idx 项。
        索引 的 第 最大idx 项 为 暂i。
        i 为 i 加 1。
    完。
    可变 颜色 为 一组：（空）。
    i 为 0。
    只要 i 小于 节点数：
        做 追加 用 颜色、负 1。
        i 为 i 加 1。
    完。
    可变 色数 为 0。
    i 为 0。
    只要 i 小于 节点数：
        节点 为 索引 的 第 i 项。
        可变 已用色 为 一组：（空）。
        邻 为 邻接表 的 第 节点 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 邻：
            若 颜色 的 第 邻 的 第 j 项 项 大于或等于 0：
                做 追加 用 已用色、颜色 的 第 邻 的 第 j 项 项。
            完。
            j 为 j 加 1。
        完。
        可变 c 为 0。
        可变 找到 为 假。
        只要 找到 等于 假：
            可变 冲突 为 假。
            j 为 0。
            只要 j 小于 做 计数 用 已用色：
                若 已用色 的 第 j 项 等于 c：
                    冲突 为 真。
                完。
                j 为 j 加 1。
            完。
            若 冲突 等于 假：
                找到 为 真。
            否则：
                c 为 c 加 1。
            完。
        完。
        颜色 的 第 节点 项 为 c。
        若 c 大于或等于 色数：
            色数 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：颜色、色数。
完。

着色_验证 接受 节点数、邻接表、颜色：
    可变 i 为 0。
    只要 i 小于 节点数：
        邻 为 邻接表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 邻：
            若 颜色 的 第 i 项 等于 颜色 的 第 邻 的 第 j 项 项：
                返还 假。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

着色_最大度 接受 节点数、邻接表：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 节点数：
        度 为 做 计数 用 邻接表 的 第 i 项。
        若 度 大于 最大：
            最大 为 度。
        完。
        i 为 i 加 1。
    完。
    返还 最大 加 1。
完。

本章 收束。
