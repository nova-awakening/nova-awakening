《字符串匹配》

引用《..·基础》。

sunday_搜索 接受 文本、模式：
    n 为 做 字节长度 用 文本。
    m 为 做 字节长度 用 模式。
    若 m 等于 0 或者 n 小于 m：
        返还 负 1。
    完。
    可变 偏移 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 256：
        做 追加 用 偏移、m 加 1。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 m：
        偏移 的 第 做 字节取 用 模式、i 项 为 m 减 i。
        i 为 i 加 1。
    完。
    可变 位置 为 0。
    只要 位置 小于或等于 n 减 m：
        可变 j 为 0。
        只要 j 小于 m 并且 做 字节取 用 文本、位置 加 j 等于 做 字节取 用 模式、j：
            j 为 j 加 1。
        完。
        若 j 等于 m：
            返还 位置。
        完。
        若 位置 加 m 小于 n：
            位置 为 位置 加 偏移 的 第 做 字节取 用 文本、位置 加 m 项。
        否则：
            止。
        完。
    完。
    返还 负 1。
完。

sunday_所有匹配 接受 文本、模式：
    n 为 做 字节长度 用 文本。
    m 为 做 字节长度 用 模式。
    可变 结果 为 一组：（空）。
    若 m 等于 0 或者 n 小于 m：
        返还 结果。
    完。
    可变 位置 为 0。
    只要 位置 小于或等于 n 减 m：
        可变 j 为 0。
        只要 j 小于 m 并且 做 字节取 用 文本、位置 加 j 等于 做 字节取 用 模式、j：
            j 为 j 加 1。
        完。
        若 j 等于 m：
            做 追加 用 结果、位置。
            位置 为 位置 加 1。
        否则：
            位置 为 位置 加 1。
        完。
    完。
    返还 结果。
完。

字符串匹配_替换 接受 文本、旧、新：
    n 为 做 字节长度 用 文本。
    m 为 做 字节长度 用 旧。
    若 m 等于 0：
        返还 文本。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 加 m 小于或等于 n：
            可变 已找到 为 1。
            可变 j 为 0。
            只要 j 小于 m 并且 已找到 等于 1：
                若 做 字节取 用 文本、i 加 j 不等于 做 字节取 用 旧、j：
                    已找到 为 0。
                完。
                j 为 j 加 1。
            完。
            若 已找到 等于 1：
                结果 为 做 字符串拼接 用 结果、新。
                i 为 i 加 m。
            否则：
                结果 为 做 字符串拼接 用 结果、做 字符 用 做 字节取 用 文本、i。
                i 为 i 加 1。
            完。
        否则：
            结果 为 做 字符串拼接 用 结果、做 字符 用 做 字节取 用 文本、i。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

字符串匹配_分割 接受 文本、分隔符：
    n 为 做 字节长度 用 文本。
    sep 为 做 字节取 用 分隔符、0。
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 字节取 用 文本、i 等于 sep：
            做 追加 用 结果、当前。
            当前 为 「」。
        否则：
            当前 为 做 字符串拼接 用 当前、做 字符 用 做 字节取 用 文本、i。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 结果、当前。
    返还 结果。
完。

字符串匹_字符串_连接 接受 列表、分隔符：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 i 大于 0：
            结果 为 做 字符串拼接 用 结果、分隔符。
        完。
        结果 为 做 字符串拼接 用 结果、做 转字符串 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

是回文 接受 文本：
    n 为 做 字节长度 用 文本。
    可变 i 为 0。
    只要 i 小于 n 除以 2：
        若 做 字节取 用 文本、i 不等于 做 字节取 用 文本、n 减 1 减 i：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

最长回文长度 接受 文本：
    n 为 做 字节长度 用 文本。
    若 n 小于或等于 1：
        返还 n。
    完。
    可变 最长 为 1。
    可变 i 为 0。
    只要 i 小于 n：
        可变 左 为 i。
        可变 右 为 i。
        只要 左 大于 0 并且 右 小于 n 减 1 并且 做 字节取 用 文本、左 减 1 等于 做 字节取 用 文本、右 加 1：
            左 为 左 减 1。
            右 为 右 加 1。
        完。
        若 右 减 左 加 1 大于 最长：
            最长 为 右 减 左 加 1。
        完。
        左 为 i。
        右 为 i 加 1。
        若 右 小于 n 并且 做 字节取 用 文本、左 等于 做 字节取 用 文本、右：
            只要 左 大于 0 并且 右 小于 n 减 1 并且 做 字节取 用 文本、左 减 1 等于 做 字节取 用 文本、右 加 1：
                左 为 左 减 1。
                右 为 右 加 1。
            完。
            若 右 减 左 加 1 大于 最长：
                最长 为 右 减 左 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 最长。
完。

字符串_行程编码 接受 文本：
    n 为 做 字节长度 用 文本。
    若 n 等于 0：
        返还 「」。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 字节取 用 文本、i。
        可变 计 为 1。
        只要 i 加 计 小于 n 并且 做 字节取 用 文本、i 加 计 等于 c：
            计 为 计 加 1。
        完。
        结果 为 做 字符串拼接 用 结果、做 字符 用 c。
        结果 为 做 字符串拼接 用 结果、做 转字符串 用 计。
        i 为 i 加 计。
    完。
    返还 结果。
完。

RK_哈希 接受 文本、起始、长度：
    可变 h 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        h 为 h 乘 31 加 做 字节取 用 文本、起始 加 i。
        i 为 i 加 1。
    完。
    返还 h。
完。

RK_搜索 接受 文本、模式：
    n 为 做 字节长度 用 文本。
    m 为 做 字节长度 用 模式。
    若 m 大于 n 或者 m 等于 0：
        返还 负 1。
    完。
    模式哈希 为 做 RK_哈希 用 模式、0、m。
    可变 文本哈希 为 做 RK_哈希 用 文本、0、m。
    可变 高幂 为 1。
    可变 p 为 0。
    只要 p 小于 m 减 1：
        高幂 为 高幂 乘 31。
        p 为 p 加 1。
    完。
    可变 i 为 0。
    只要 i 小于或等于 n 减 m：
        若 文本哈希 等于 模式哈希：
            可变 匹配 为 真。
            可变 j 为 0。
            只要 j 小于 m：
                若 做 字节取 用 文本、i 加 j 不等于 做 字节取 用 模式、j：
                    匹配 为 假。
                完。
                j 为 j 加 1。
            完。
            若 匹配：
                返还 i。
            完。
        完。
        若 i 小于 n 减 m：
            文本哈希 为 (文本哈希 减 做 字节取 用 文本、i 乘 高幂) 乘 31 加 做 字节取 用 文本、i 加 m。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

字符串匹配_编辑距离 接受 a、b：
    m 为 做 字节长度 用 a。
    n 为 做 字节长度 用 b。
    可变 前行 为 一组：（空）。
    可变 当行 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 n：
        做 追加 用 前行、i。
        做 追加 用 当行、0。
        i 为 i 加 1。
    完。
    i 为 1。
    只要 i 小于或等于 m：
        当行 的 第 0 项 为 i。
        可变 j 为 1。
        只要 j 小于或等于 n：
            代价 为 0。
            若 做 字节取 用 a、i 减 1 不等于 做 字节取 用 b、j 减 1：
                代价 为 1。
            完。
            删除 为 前行 的 第 j 项 加 1。
            插入 为 当行 的 第 j 减 1 项 加 1。
            替换 为 前行 的 第 j 减 1 项 加 代价。
            可变 最小 为 删除。
            若 插入 小于 最小：
                最小 为 插入。
            完。
            若 替换 小于 最小：
                最小 为 替换。
            完。
            当行 的 第 j 项 为 最小。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于或等于 n：
            前行 的 第 j 项 为 当行 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 前行 的 第 n 项。
完。

最长公共子串长度 接受 a、b：
    m 为 做 字节长度 用 a。
    n 为 做 字节长度 用 b。
    可变 最大 为 0。
    可变 前行 为 一组：（空）。
    可变 当行 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于或等于 n：
        做 追加 用 前行、0。
        做 追加 用 当行、0。
        k 为 k 加 1。
    完。
    可变 i 为 1。
    只要 i 小于或等于 m：
        可变 j 为 1。
        只要 j 小于或等于 n：
            若 做 字节取 用 a、i 减 1 等于 做 字节取 用 b、j 减 1：
                当行 的 第 j 项 为 前行 的 第 j 减 1 项 加 1。
                若 当行 的 第 j 项 大于 最大：
                    最大 为 当行 的 第 j 项。
                完。
            否则：
                当行 的 第 j 项 为 0。
            完。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于或等于 n：
            前行 的 第 j 项 为 当行 的 第 j 项。
            当行 的 第 j 项 为 0。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

字符串匹配_通配符匹配 接受 文本、模式：
    n 为 做 字节长度 用 文本。
    m 为 做 字节长度 用 模式。
    可变 ti 为 0。
    可变 pi 为 0。
    可变 星号位 为 负 1。
    可变 回溯位 为 负 1。
    只要 ti 小于 n：
        若 pi 小于 m 并且 (做 字节取 用 模式、pi 等于 字符_问号 或者 做 字节取 用 模式、pi 等于 做 字节取 用 文本、ti)：
            ti 为 ti 加 1。
            pi 为 pi 加 1。
        否则若 pi 小于 m 并且 做 字节取 用 模式、pi 等于 字符_星号：
            星号位 为 pi。
            回溯位 为 ti。
            pi 为 pi 加 1。
        否则若 星号位 大于或等于 0：
            pi 为 星号位 加 1。
            回溯位 为 回溯位 加 1。
            ti 为 回溯位。
        否则：
            返还 假。
        完。
    完。
    只要 pi 小于 m 并且 做 字节取 用 模式、pi 等于 字符_星号：
        pi 为 pi 加 1。
    完。
    返还 pi 等于 m。
完。

字符串_Jaccard相似度 接受 a、b：
    na 为 做 字节长度 用 a。
    nb 为 做 字节长度 用 b。
    若 na 等于 0 并且 nb 等于 0：
        返还 1000。
    完。
    可变 交集 为 0。
    可变 并集标记 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 256：
        做 追加 用 并集标记、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 na：
        并集标记 的 第 做 字节取 用 a、i 项 为 并集标记 的 第 做 字节取 用 a、i 项 位或 1。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 nb：
        并集标记 的 第 做 字节取 用 b、i 项 为 并集标记 的 第 做 字节取 用 b、i 项 位或 2。
        i 为 i 加 1。
    完。
    可变 并集数 为 0。
    i 为 0。
    只要 i 小于 256：
        若 并集标记 的 第 i 项 大于 0：
            并集数 为 并集数 加 1。
        完。
        若 并集标记 的 第 i 项 等于 3：
            交集 为 交集 加 1。
        完。
        i 为 i 加 1。
    完。
    若 并集数 等于 0：
        返还 0。
    完。
    返还 交集 乘 1000 除以 并集数。
完。

本章 收束。
