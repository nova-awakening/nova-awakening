《位操作》

引用《..·位运算·位运算》。

引用《..·基础》。

位_计数1 接受 n：
    可变 v 为 n。
    若 v 小于 0：
        v 为 0 减 v。
    完。
    可变 计 为 0。
    只要 v 大于 0：
        计 为 计 加 v 取余于 2。
        v 为 v 除以 2。
    完。
    返还 计。
完。

位操作_位_最高位 接受 n：
    可变 v 为 n。
    若 v 小于或等于 0：
        返还 负 1。
    完。
    可变 位 为 0。
    只要 v 大于 1：
        v 为 v 除以 2。
        位 为 位 加 1。
    完。
    返还 位。
完。

位操作_位_最低位 接受 n：
    若 n 等于 0：
        返还 负 1。
    完。
    可变 v 为 n。
    若 v 小于 0：
        v 为 0 减 v。
    完。
    可变 位 为 0。
    只要 v 取余于 2 等于 0：
        v 为 v 除以 2。
        位 为 位 加 1。
    完。
    返还 位。
完。

位_取 接受 值、n：
    可变 v 为 值。
    可变 i 为 0。
    只要 i 小于 n：
        v 为 v 除以 2。
        i 为 i 加 1。
    完。
    返还 v 取余于 2。
完。

位_设 接受 值、n：
    可变 掩码 为 1。
    可变 i 为 0。
    只要 i 小于 n：
        掩码 为 掩码 乘 2。
        i 为 i 加 1。
    完。
    若 做 位_取 用 值、n 等于 0：
        返还 值 加 掩码。
    完。
    返还 值。
完。

位_清 接受 值、n：
    可变 掩码 为 1。
    可变 i 为 0。
    只要 i 小于 n：
        掩码 为 掩码 乘 2。
        i 为 i 加 1。
    完。
    若 做 位_取 用 值、n 等于 1：
        返还 值 减 掩码。
    完。
    返还 值。
完。

位操作_位_翻转 接受 值、n：
    若 做 位_取 用 值、n 等于 1：
        返还 做 位_清 用 值、n。
    完。
    返还 做 位_设 用 值、n。
完。

位_转字符串 接受 值、位数：
    可变 结果 为 「」。
    可变 i 为 位数 减 1。
    只要 i 大于或等于 0：
        若 做 位_取 用 值、i 等于 1：
            结果 为 做 字符串拼接 用 结果、「1」。
        否则：
            结果 为 做 字符串拼接 用 结果、「0」。
        完。
        i 为 i 减 1。
    完。
    返还 结果。
完。

gray_编码 接受 n：
    一半 为 n 除以 2。
    可变 结果 为 0。
    可变 掩码 为 1。
    可变 i 为 0。
    只要 i 小于 30：
        a_bit 为 n 除以 掩码 取余于 2。
        b_bit 为 一半 除以 掩码 取余于 2。
        若 a_bit 不等于 b_bit：
            结果 为 结果 加 掩码。
        完。
        掩码 为 掩码 乘 2。
        i 为 i 加 1。
    完。
    返还 结果。
完。

gray_解码 接受 g：
    可变 n 为 g。
    可变 掩码 为 g 除以 2。
    只要 掩码 大于 0：
        可变 结果 为 0。
        可变 位掩码 为 1。
        可变 i 为 0。
        只要 i 小于 30：
            a 为 n 除以 位掩码 取余于 2。
            b 为 掩码 除以 位掩码 取余于 2。
            若 a 不等于 b：
                结果 为 结果 加 位掩码。
            完。
            位掩码 为 位掩码 乘 2。
            i 为 i 加 1。
        完。
        n 为 结果。
        掩码 为 掩码 除以 2。
    完。
    返还 n。
完。

下一个2幂 接受 n：
    若 n 小于或等于 1：
        返还 1。
    完。
    可变 v 为 1。
    只要 v 小于 n：
        v 为 v 乘 2。
    完。
    返还 v。
完。

位_反转 接受 值、位数：
    可变 结果 为 0。
    可变 i 为 0。
    只要 i 小于 位数：
        若 做 位_取 用 值、i 等于 1：
            结果 为 做 位_设 用 结果、位数 减 1 减 i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位_交换 接受 值、i、j：
    bi 为 做 位_取 用 值、i。
    bj 为 做 位_取 用 值、j。
    若 bi 等于 bj：
        返还 值。
    完。
    可变 结果 为 值。
    若 bi 等于 1：
        结果 为 做 位_清 用 结果、i。
    否则：
        结果 为 做 位_设 用 结果、i。
    完。
    若 bj 等于 1：
        结果 为 做 位_清 用 结果、j。
    否则：
        结果 为 做 位_设 用 结果、j。
    完。
    返还 结果。
完。

位操作_位图_创建 接受 大小：
    字数 为 (大小 加 31) 除以 32。
    可变 图 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 字数：
        做 追加 用 图、0。
        i 为 i 加 1。
    完。
    返还 一组：图、大小。
完。

位操作_位图_设 接受 图、位：
    若 位 小于 0 或者 位 大于或等于 图 的 第 1 项：
        返还 空。
    完。
    字索引 为 位 除以 32。
    位索引 为 位 取余于 32。
    图 的 第 0 项 的 第 字索引 项 为 做 位_设 用 图 的 第 0 项 的 第 字索引 项、位索引。
完。

位操作_位图_清 接受 图、位：
    若 位 小于 0 或者 位 大于或等于 图 的 第 1 项：
        返还 空。
    完。
    字索引 为 位 除以 32。
    位索引 为 位 取余于 32。
    图 的 第 0 项 的 第 字索引 项 为 做 位_清 用 图 的 第 0 项 的 第 字索引 项、位索引。
完。

位操作_位图_测试 接受 图、位：
    若 位 小于 0 或者 位 大于或等于 图 的 第 1 项：
        返还 0。
    完。
    字索引 为 位 除以 32。
    位索引 为 位 取余于 32。
    返还 做 位_取 用 图 的 第 0 项 的 第 字索引 项、位索引。
完。

位操作_位图_计数 接受 图：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 图 的 第 0 项：
        总 为 总 加 做 位_计数1 用 图 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

位域_提取 接受 值、起始位、宽度：
    可变 掩码 为 0。
    可变 i 为 0。
    只要 i 小于 宽度：
        掩码 为 做 位_设 用 掩码、i。
        i 为 i 加 1。
    完。
    可变 移位 为 1。
    i 为 0。
    只要 i 小于 起始位：
        移位 为 移位 乘 2。
        i 为 i 加 1。
    完。
    返还 值 除以 移位 取余于 (掩码 加 1)。
完。

位域_插入 接受 值、起始位、宽度、新值：
    可变 掩码 为 0。
    可变 i 为 0。
    只要 i 小于 宽度：
        掩码 为 做 位_设 用 掩码、i。
        i 为 i 加 1。
    完。
    可变 结果 为 值。
    i 为 0。
    只要 i 小于 宽度：
        结果 为 做 位_清 用 结果、起始位 加 i。
        i 为 i 加 1。
    完。
    可变 截断值 为 新值 取余于 (掩码 加 1)。
    i 为 0。
    只要 i 小于 宽度：
        若 做 位_取 用 截断值、i 等于 1：
            结果 为 做 位_设 用 结果、起始位 加 i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位操作_汉明距离 接受 a、b：
    可变 异或 为 0。
    可变 i 为 0。
    只要 i 小于 32：
        若 做 位_取 用 a、i 不等于 做 位_取 用 b、i：
            异或 为 异或 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 异或。
完。

格雷码_编码 接受 n：
    可变 结果 为 0。
    可变 i 为 0。
    只要 i 小于 32：
        b1 为 做 位_取 用 n、i。
        b2 为 做 位_取 用 n、i 加 1。
        若 b1 不等于 b2：
            结果 为 做 位_设 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

格雷码_解码 接受 g：
    可变 结果 为 0。
    可变 位 为 31。
    可变 前一位 为 0。
    只要 位 大于或等于 0：
        当前 为 做 位_取 用 g、位。
        若 当前 不等于 前一位：
            结果 为 做 位_设 用 结果、位。
            前一位 为 1。
        否则：
            前一位 为 0。
        完。
        位 为 位 减 1。
    完。
    返还 结果。
完。

格雷码_序列 接受 n：
    可变 序列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 序列、做 格雷码_编码 用 i。
        i 为 i 加 1。
    完。
    返还 序列。
完。

位操作_位_循环左移 接受 值、位数、总位数：
    实际位数 为 位数 取余于 总位数。
    可变 结果 为 0。
    可变 i 为 0。
    只要 i 小于 总位数：
        源位 为 (i 减 实际位数 加 总位数) 取余于 总位数。
        若 做 位_取 用 值、源位 等于 1：
            结果 为 做 位_设 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位操作_位_循环右移 接受 值、位数、总位数：
    返还 做 位操作_位_循环左移 用 值、总位数 减 位数、总位数。
完。

位操作_位_奇偶校验 接受 值：
    可变 计 为 做 位_计数1 用 值。
    返还 计 取余于 2。
完。

可变 _位操作_运算数 为 0。

可变 _位操作_位图操作数 为 0。

位操作_记录运算 接受 零参：
    _位操作_运算数 为 _位操作_运算数 加 1。
完。

位操作_记录位图 接受 零参：
    _位操作_位图操作数 为 _位操作_位图操作数 加 1。
完。

本章 收束。
