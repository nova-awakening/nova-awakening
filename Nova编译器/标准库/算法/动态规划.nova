《动态规划》

引用《..·基础》。

可变 dp_气韵统计 为 一组：0、0。

dp_01背包 接受 容量、物品：
    n 为 做 计数 用 物品。
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 容量：
        做 追加 用 dp、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        物 为 物品 的 第 i 项。
        w 为 物 的 第 0 项。
        v 为 物 的 第 1 项。
        可变 j 为 容量。
        只要 j 大于或等于 w：
            新值 为 dp 的 第 j 减 w 项 加 v。
            若 新值 大于 dp 的 第 j 项：
                dp 的 第 j 项 为 新值。
            完。
            j 为 j 减 1。
        完。
        i 为 i 加 1。
    完。
    返还 dp 的 第 容量 项。
完。

dp_完全背包 接受 容量、物品：
    n 为 做 计数 用 物品。
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 容量：
        做 追加 用 dp、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        物 为 物品 的 第 i 项。
        w 为 物 的 第 0 项。
        v 为 物 的 第 1 项。
        可变 j 为 w。
        只要 j 小于或等于 容量：
            新值 为 dp 的 第 j 减 w 项 加 v。
            若 新值 大于 dp 的 第 j 项：
                dp 的 第 j 项 为 新值。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 dp 的 第 容量 项。
完。

动规_最长公共子序列 接受 a、b：
    m 为 做 字节长度 用 a。
    n 为 做 字节长度 用 b。
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 m：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于或等于 n：
            做 追加 用 行、0。
            j 为 j 加 1。
        完。
        做 追加 用 dp、行。
        i 为 i 加 1。
    完。
    i 为 1。
    只要 i 小于或等于 m：
        可变 j 为 1。
        行i 为 dp 的 第 i 项。
        行p 为 dp 的 第 i 减 1 项。
        只要 j 小于或等于 n：
            若 做 字节取 用 a、i 减 1 等于 做 字节取 用 b、j 减 1：
                行i 的 第 j 项 为 行p 的 第 j 减 1 项 加 1。
            否则：
                上 为 行p 的 第 j 项。
                左 为 行i 的 第 j 减 1 项。
                若 上 大于 左：
                    行i 的 第 j 项 为 上。
                否则：
                    行i 的 第 j 项 为 左。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    最后行 为 dp 的 第 m 项。
    返还 最后行 的 第 n 项。
完。

动规_最长递增子序列 接受 序列：
    n 为 做 计数 用 序列。
    若 n 等于 0：
        返还 0。
    完。
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 dp、1。
        i 为 i 加 1。
    完。
    可变 最大 为 1。
    i 为 1。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 i：
            若 序列 的 第 j 项 小于 序列 的 第 i 项 并且 dp 的 第 j 项 加 1 大于 dp 的 第 i 项：
                dp 的 第 i 项 为 dp 的 第 j 项 加 1。
            完。
            j 为 j 加 1。
        完。
        若 dp 的 第 i 项 大于 最大：
            最大 为 dp 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

dp_编辑距离 接受 a、b：
    m 为 做 字节长度 用 a。
    n 为 做 字节长度 用 b。
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 m：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于或等于 n：
            做 追加 用 行、0。
            j 为 j 加 1。
        完。
        做 追加 用 dp、行。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于或等于 m：
        dp 的 第 i 项 的 第 0 项 为 i。
        i 为 i 加 1。
    完。
    可变 j2 为 0。
    行0 为 dp 的 第 0 项。
    只要 j2 小于或等于 n：
        行0 的 第 j2 项 为 j2。
        j2 为 j2 加 1。
    完。
    i 为 1。
    只要 i 小于或等于 m：
        可变 j 为 1。
        行i 为 dp 的 第 i 项。
        行p 为 dp 的 第 i 减 1 项。
        只要 j 小于或等于 n：
            若 做 字节取 用 a、i 减 1 等于 做 字节取 用 b、j 减 1：
                行i 的 第 j 项 为 行p 的 第 j 减 1 项。
            否则：
                替 为 行p 的 第 j 减 1 项 加 1。
                删 为 行p 的 第 j 项 加 1。
                插 为 行i 的 第 j 减 1 项 加 1。
                可变 最小 为 替。
                若 删 小于 最小：
                    最小 为 删。
                完。
                若 插 小于 最小：
                    最小 为 插。
                完。
                行i 的 第 j 项 为 最小。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    结果行 为 dp 的 第 m 项。
    返还 结果行 的 第 n 项。
完。

dp_硬币找零 接受 硬币、金额：
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 金额：
        做 追加 用 dp、金额 加 1。
        i 为 i 加 1。
    完。
    dp 的 第 0 项 为 0。
    i 为 1。
    只要 i 小于或等于 金额：
        可变 j 为 0。
        只要 j 小于 做 计数 用 硬币：
            若 硬币 的 第 j 项 小于或等于 i 并且 dp 的 第 i 减 硬币 的 第 j 项 项 加 1 小于 dp 的 第 i 项：
                dp 的 第 i 项 为 dp 的 第 i 减 硬币 的 第 j 项 项 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 dp 的 第 金额 项 大于 金额：
        返还 负 1。
    完。
    返还 dp 的 第 金额 项。
完。

dp_最大子数组和 接受 数组：
    若 做 计数 用 数组 等于 0：
        返还 0。
    完。
    可变 当前和 为 数组 的 第 0 项。
    可变 最大和 为 数组 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数组：
        若 当前和 加 数组 的 第 i 项 大于 数组 的 第 i 项：
            当前和 为 当前和 加 数组 的 第 i 项。
        否则：
            当前和 为 数组 的 第 i 项。
        完。
        若 当前和 大于 最大和：
            最大和 为 当前和。
        完。
        i 为 i 加 1。
    完。
    返还 最大和。
完。

dp_爬楼梯 接受 n：
    若 n 小于或等于 2：
        返还 n。
    完。
    可变 a 为 1。
    可变 b 为 2。
    可变 i 为 3。
    只要 i 小于或等于 n：
        c 为 a 加 b。
        a 为 b。
        b 为 c。
        i 为 i 加 1。
    完。
    返还 b。
完。

dp_打家劫舍 接受 房屋：
    n 为 做 计数 用 房屋。
    若 n 等于 0：
        返还 0。
    完。
    若 n 等于 1：
        返还 房屋 的 第 0 项。
    完。
    可变 prev2 为 房屋 的 第 0 项。
    可变 prev1 为 房屋 的 第 0 项。
    若 房屋 的 第 1 项 大于 prev1：
        prev1 为 房屋 的 第 1 项。
    完。
    可变 i 为 2。
    只要 i 小于 n：
        可变 当前 为 prev1。
        若 prev2 加 房屋 的 第 i 项 大于 当前：
            当前 为 prev2 加 房屋 的 第 i 项。
        完。
        prev2 为 prev1。
        prev1 为 当前。
        i 为 i 加 1。
    完。
    返还 prev1。
完。

本章 收束。
