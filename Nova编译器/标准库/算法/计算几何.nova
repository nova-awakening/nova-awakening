《计算几何》

引用《..·基础》。

引用《.·数学扩展》。

几何_叉积 接受 p1、p2、p3：
    返还 (p2 的 第 0 项 减 p1 的 第 0 项) 乘 (p3 的 第 1 项 减 p1 的 第 1 项) 减 (p2 的 第 1 项 减 p1 的 第 1 项) 乘 (p3 的 第 0 项 减 p1 的 第 0 项)。
完。

几何_点积 接受 p1、p2、p3：
    返还 (p2 的 第 0 项 减 p1 的 第 0 项) 乘 (p3 的 第 0 项 减 p1 的 第 0 项) 加 (p2 的 第 1 项 减 p1 的 第 1 项) 乘 (p3 的 第 1 项 减 p1 的 第 1 项)。
完。

几何_距离2 接受 p1、p2：
    导数 为 p1 的 第 0 项 减 p2 的 第 0 项。
    导数Y 为 p1 的 第 1 项 减 p2 的 第 1 项。
    返还 导数 乘 导数 加 导数Y 乘 导数Y。
完。

几何_共线 接受 p1、p2、p3：
    返还 做 几何_叉积 用 p1、p2、p3 等于 0。
完。

几何_线段相交 接受 a1、a2、b1、b2：
    d1 为 做 几何_叉积 用 b1、b2、a1。
    d2 为 做 几何_叉积 用 b1、b2、a2。
    d3 为 做 几何_叉积 用 a1、a2、b1。
    d4 为 做 几何_叉积 用 a1、a2、b2。
    若 (d1 大于 0 并且 d2 小于 0 或者 d1 小于 0 并且 d2 大于 0) 并且 (d3 大于 0 并且 d4 小于 0 或者 d3 小于 0 并且 d4 大于 0)：
        返还 1。
    完。
    若 d1 等于 0 并且 做 几何_在线段上 用 b1、b2、a1 等于 1：
        返还 1。
    完。
    若 d2 等于 0 并且 做 几何_在线段上 用 b1、b2、a2 等于 1：
        返还 1。
    完。
    若 d3 等于 0 并且 做 几何_在线段上 用 a1、a2、b1 等于 1：
        返还 1。
    完。
    若 d4 等于 0 并且 做 几何_在线段上 用 a1、a2、b2 等于 1：
        返还 1。
    完。
    返还 0。
完。

几何_在线段上 接受 p1、p2、q：
    可变 minx 为 p1 的 第 0 项。
    若 p2 的 第 0 项 小于 minx：
        minx 为 p2 的 第 0 项。
    完。
    可变 maxx 为 p1 的 第 0 项。
    若 p2 的 第 0 项 大于 maxx：
        maxx 为 p2 的 第 0 项。
    完。
    可变 miny 为 p1 的 第 1 项。
    若 p2 的 第 1 项 小于 miny：
        miny 为 p2 的 第 1 项。
    完。
    可变 maxy 为 p1 的 第 1 项。
    若 p2 的 第 1 项 大于 maxy：
        maxy 为 p2 的 第 1 项。
    完。
    返还 q 的 第 0 项 大于或等于 minx 并且 q 的 第 0 项 小于或等于 maxx 并且 q 的 第 1 项 大于或等于 miny 并且 q 的 第 1 项 小于或等于 maxy。
完。

几何_多边形面积2 接受 点列表：
    n 为 做 计数 用 点列表。
    若 n 小于 3：
        返还 0。
    完。
    可变 面积 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        j 为 (i 加 1) 取余于 n。
        pi 为 点列表 的 第 i 项。
        pj 为 点列表 的 第 j 项。
        面积 为 面积 加 pi 的 第 0 项 乘 pj 的 第 1 项 减 pj 的 第 0 项 乘 pi 的 第 1 项。
        i 为 i 加 1。
    完。
    若 面积 小于 0：
        面积 为 0 减 面积。
    完。
    返还 面积。
完。

几何_点在凸多边形 接受 点列表、p：
    n 为 做 计数 用 点列表。
    若 n 小于 3：
        返还 0。
    完。
    可变 i 为 0。
    可变 正 为 0。
    可变 负 为 0。
    只要 i 小于 n：
        j 为 (i 加 1) 取余于 n。
        d 为 做 几何_叉积 用 点列表 的 第 i 项、点列表 的 第 j 项、p。
        若 d 大于 0：
            正 为 正 加 1。
        完。
        若 d 小于 0：
            负 为 负 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 正 等于 0 或者 负 等于 0。
完。

几何_凸包 接受 点列表：
    n 为 做 计数 用 点列表。
    若 n 小于或等于 3：
        返还 点列表。
    完。
    可变 最低 为 0。
    可变 i 为 1。
    只要 i 小于 n：
        pi 为 点列表 的 第 i 项。
        p0 为 点列表 的 第 最低 项。
        若 pi 的 第 1 项 小于 p0 的 第 1 项 或者 pi 的 第 1 项 等于 p0 的 第 1 项 并且 pi 的 第 0 项 小于 p0 的 第 0 项：
            最低 为 i。
        完。
        i 为 i 加 1。
    完。
    临时 为 点列表 的 第 0 项。
    点列表 的 第 0 项 为 点列表 的 第 最低 项。
    点列表 的 第 最低 项 为 临时。
    可变 已改变 为 1。
    只要 已改变 等于 1：
        已改变 为 0。
        i 为 1。
        只要 i 小于 n 减 1：
            c 为 做 几何_叉积 用 基点、点列表 的 第 i 项、点列表 的 第 i 加 1 项。
            若 c 小于 0：
                t 为 点列表 的 第 i 项。
                点列表 的 第 i 项 为 点列表 的 第 i 加 1 项。
                点列表 的 第 i 加 1 项 为 t。
                已改变 为 1。
            完。
            i 为 i 加 1。
        完。
    完。
    可变 栈 为 一组：（空）。
    做 追加 用 栈、点列表 的 第 0 项。
    做 追加 用 栈、点列表 的 第 1 项。
    i 为 2。
    只要 i 小于 n：
        只要 做 计数 用 栈 大于 1：
            顶 为 栈 的 第 做 计数 用 栈 减 1 项。
            次顶 为 栈 的 第 做 计数 用 栈 减 2 项。
            若 做 几何_叉积 用 次顶、顶、点列表 的 第 i 项 小于或等于 0：
                做 删除 用 栈、做 计数 用 栈 减 1。
            否则：
                止。
            完。
        完。
        做 追加 用 栈、点列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 栈。
完。

几何_最近点对 接受 点列表：
    n 为 做 计数 用 点列表。
    若 n 小于 2：
        返还 负 1。
    完。
    可变 最近 为 做 几何_距离2 用 点列表 的 第 0 项、点列表 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            d 为 做 几何_距离2 用 点列表 的 第 i 项、点列表 的 第 j 项。
            若 d 小于 最近：
                最近 为 d。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 最近。
完。

几何_多边形周长 接受 点列表：
    n 为 做 计数 用 点列表。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        j 为 (i 加 1) 取余于 n。
        总 为 总 加 做 整数sqrt 用 做 几何_距离2 用 点列表 的 第 i 项、点列表 的 第 j 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

几何_重心 接受 点列表：
    n 为 做 计数 用 点列表。
    若 n 等于 0：
        返还 一组：0、0。
    完。
    可变 sx 为 0。
    可变 sy 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        p 为 点列表 的 第 i 项。
        sx 为 sx 加 p 的 第 0 项。
        sy 为 sy 加 p 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 一组：sx 除以 n、sy 除以 n。
完。

几何_旋转90 接受 p：
    返还 一组：p 的 第 1 项、0 减 p 的 第 0 项。
完。

几何_旋转逆90 接受 p：
    返还 一组：0 减 p 的 第 1 项、p 的 第 0 项。
完。

几何_中点 接受 p1、p2：
    返还 一组：(p1 的 第 0 项 加 p2 的 第 0 项) 除以 2、(p1 的 第 1 项 加 p2 的 第 1 项) 除以 2。
完。

叉积 接受 ox、oy、ax、ay、bx、by：
    返还 (ax 减 ox) 乘 (by 减 oy) 减 (ay 减 oy) 乘 (bx 减 ox)。
完。

计算几何_线段_相交 接受 x1、y1、x2、y2、x3、y3、x4、y4：
    d1 为 做 叉积 用 x3、y3、x4、y4、x1、y1。
    d2 为 做 叉积 用 x3、y3、x4、y4、x2、y2。
    d3 为 做 叉积 用 x1、y1、x2、y2、x3、y3。
    d4 为 做 叉积 用 x1、y1、x2、y2、x4、y4。
    若 (d1 大于 0 并且 d2 小于 0 或者 d1 小于 0 并且 d2 大于 0) 并且 (d3 大于 0 并且 d4 小于 0 或者 d3 小于 0 并且 d4 大于 0)：
        返还 真。
    完。
    返还 假。
完。

计算几何_多边形_面积 接受 点x列表、点y列表：
    n 为 做 计数 用 点x列表。
    可变 面积 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        j 为 (i 加 1) 取余于 n。
        面积 为 面积 加 点x列表 的 第 i 项 乘 点y列表 的 第 j 项 减 点x列表 的 第 j 项 乘 点y列表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 面积 小于 0：
        面积 为 0 减 面积。
    完。
    返还 面积 除以 2。
完。

本章 收束。
