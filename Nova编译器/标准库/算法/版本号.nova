《版本号》

引用《..·基础》。

引用《..·..·工具·包管理器·包管理器》。

可变 _版本_比较次数 为 0。

可变 _版本_解析次数 为 0。

可变 _版本_注册表 为 一组：（空）。

版本号_版本_创建 接受 主、次、补丁：
    返还 一组：主、次、补丁、「」、「」。
完。

版本_创建完整 接受 主、次、补丁、预发布、构建号：
    返还 一组：主、次、补丁、预发布、构建号。
完。

版本号_版本_主 接受 v：
    返还 v 的 第 0 项。
完。

版本号_版本_次 接受 v：
    返还 v 的 第 1 项。
完。

版本_补丁 接受 v：
    返还 v 的 第 2 项。
完。

版本_预发布 接受 v：
    返还 v 的 第 3 项。
完。

版本_构建号 接受 v：
    返还 v 的 第 4 项。
完。

版本号_版本_解析 接受 文本：
    _版本_解析次数 为 _版本_解析次数 加 1。
    可变 部分 为 一组：0。
    可变 当前 为 0。
    可变 段 为 0。
    可变 i 为 0。
    可变 预发布 为 「」。
    可变 构建号 为 「」。
    可变 阶段 为 0。
    只要 i 小于 做 字节长度 用 文本：
        c 为 做 字节取 用 文本、i。
        若 阶段 等于 0：
            若 c 等于 字符_句点：
                若 段 小于 3：
                    部分 的 第 段 项 为 当前。
                完。
                段 为 段 加 1。
                当前 为 0。
            否则若 c 等于 字符_减号：
                若 段 小于 3：
                    部分 的 第 段 项 为 当前。
                完。
                阶段 为 1。
            否则若 c 等于 字符_加号：
                若 段 小于 3：
                    部分 的 第 段 项 为 当前。
                完。
                阶段 为 2。
            否则若 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
                当前 为 当前 乘 10 加 c 减 48。
            完。
        否则若 阶段 等于 1：
            若 c 等于 字符_加号：
                阶段 为 2。
            否则：
                预发布 为 做 字符串拼接 用 预发布、做 字符 用 c。
            完。
        否则：
            构建号 为 做 字符串拼接 用 构建号、做 字符 用 c。
        完。
        i 为 i 加 1。
    完。
    若 阶段 等于 0 并且 段 小于 3：
        部分 的 第 段 项 为 当前。
    完。
    返还 一组：部分 的 第 0 项、部分 的 第 1 项、部分 的 第 2 项、预发布、构建号。
完。

版本号_版本_比较 接受 a、b：
    _版本_比较次数 为 _版本_比较次数 加 1。
    若 a 的 第 0 项 不等于 b 的 第 0 项：
        返还 a 的 第 0 项 减 b 的 第 0 项。
    完。
    若 a 的 第 1 项 不等于 b 的 第 1 项：
        返还 a 的 第 1 项 减 b 的 第 1 项。
    完。
    若 a 的 第 2 项 不等于 b 的 第 2 项：
        返还 a 的 第 2 项 减 b 的 第 2 项。
    完。
    若 a 的 第 3 项 不等于 「」 并且 b 的 第 3 项 等于 「」：
        返还 负 1。
    完。
    若 a 的 第 3 项 等于 「」 并且 b 的 第 3 项 不等于 「」：
        返还 1。
    完。
    返还 0。
完。

版本_等于 接受 a、b：
    返还 做 版本号_版本_比较 用 a、b 等于 0。
完。

版本_大于 接受 a、b：
    返还 做 版本号_版本_比较 用 a、b 大于 0。
完。

版本_小于 接受 a、b：
    返还 做 版本号_版本_比较 用 a、b 小于 0。
完。

版本_大于等于 接受 a、b：
    返还 做 版本号_版本_比较 用 a、b 大于或等于 0。
完。

版本_小于等于 接受 a、b：
    返还 做 版本号_版本_比较 用 a、b 小于或等于 0。
完。

版本_在范围内 接受 版本、最小、最大：
    返还 做 版本_大于等于 用 版本、最小 并且 做 版本_小于等于 用 版本、最大。
完。

版本号_版本_格式化 接受 v：
    可变 s 为 做 转字符串 用 v 的 第 0 项。
    s 为 做 字符串拼接 用 s、「.」。
    s 为 做 字符串拼接 用 s、做 转字符串 用 v 的 第 1 项。
    s 为 做 字符串拼接 用 s、「.」。
    s 为 做 字符串拼接 用 s、做 转字符串 用 v 的 第 2 项。
    若 v 的 第 3 项 不等于 「」：
        s 为 做 字符串拼接 用 s、「-」。
        s 为 做 字符串拼接 用 s、v 的 第 3 项。
    完。
    若 v 的 第 4 项 不等于 「」：
        s 为 做 字符串拼接 用 s、「+」。
        s 为 做 字符串拼接 用 s、v 的 第 4 项。
    完。
    返还 s。
完。

版本_短格式 接受 v：
    可变 s 为 做 转字符串 用 v 的 第 0 项。
    s 为 做 字符串拼接 用 s、「.」。
    s 为 做 字符串拼接 用 s、做 转字符串 用 v 的 第 1 项。
    返还 s。
完。

版本_递增主 接受 v：
    返还 一组：v 的 第 0 项 加 1、0、「」、「」。
完。

版本_递增次 接受 v：
    返还 一组：v 的 第 0 项、v 的 第 1 项 加 1、0、「」、「」。
完。

版本_递增补丁 接受 v：
    返还 一组：v 的 第 0 项、v 的 第 1 项、v 的 第 2 项 加 1、「」、「」。
完。

版本_设预发布 接受 v、标签：
    返还 一组：v 的 第 0 项、v 的 第 1 项、v 的 第 2 项、标签、v 的 第 4 项。
完。

版本_设构建号 接受 v、号：
    返还 一组：v 的 第 0 项、v 的 第 1 项、v 的 第 2 项、v 的 第 3 项、号。
完。

版本号_版本_兼容 接受 a、b：
    返还 a 的 第 0 项 等于 b 的 第 0 项。
完。

版本_API兼容 接受 a、b：
    若 a 的 第 0 项 不等于 b 的 第 0 项：
        返还 假。
    完。
    若 a 的 第 0 项 等于 0：
        返还 a 的 第 1 项 等于 b 的 第 1 项。
    完。
    返还 真。
完。

版本_向后兼容 接受 旧、新：
    若 新 的 第 0 项 不等于 旧 的 第 0 项：
        返还 假。
    完。
    若 新 的 第 1 项 小于 旧 的 第 1 项：
        返还 假。
    完。
    若 新 的 第 1 项 等于 旧 的 第 1 项 并且 新 的 第 2 项 小于 旧 的 第 2 项：
        返还 假。
    完。
    返还 真。
完。

约束_精确 为 0。

约束_兼容 为 1。

约束_波浪 为 2。

约束_大于等于 为 3。

约束_小于 为 4。

约束_范围 为 5。

版本约束_创建 接受 类型、版本：
    返还 一组：类型、版本。
完。

版本约束_满足 接受 约束、版本：
    类型 为 约束 的 第 0 项。
    基准 为 约束 的 第 1 项。
    若 类型 等于 约束_精确：
        返还 做 版本_等于 用 版本、基准。
    完。
    若 类型 等于 约束_兼容：
        返还 版本 的 第 0 项 等于 基准 的 第 0 项 并且 做 版本_大于等于 用 版本、基准。
    完。
    若 类型 等于 约束_波浪：
        返还 版本 的 第 0 项 等于 基准 的 第 0 项 并且 版本 的 第 1 项 等于 基准 的 第 1 项 并且 做 版本_大于等于 用 版本、基准。
    完。
    若 类型 等于 约束_大于等于：
        返还 做 版本_大于等于 用 版本、基准。
    完。
    若 类型 等于 约束_小于：
        返还 做 版本_小于 用 版本、基准。
    完。
    返还 假。
完。

版本约束_格式化 接受 约束：
    前缀列表 为 一组：「==」、「^」、「~」、「>=」、「<」。
    若 约束 的 第 0 项 大于或等于 0 并且 约束 的 第 0 项 小于 5：
        返还 做 字符串拼接 用 前缀列表 的 第 约束 的 第 0 项 项、做 版本号_版本_格式化 用 约束 的 第 1 项。
    完。
    返还 做 版本号_版本_格式化 用 约束 的 第 1 项。
完。

版本约束_全部满足 接受 约束列表、版本：
    可变 i 为 0。
    只要 i 小于 做 计数 用 约束列表：
        若 非 做 版本约束_满足 用 约束列表 的 第 i 项、版本：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

版本约束_任一满足 接受 约束列表、版本：
    可变 i 为 0。
    只要 i 小于 做 计数 用 约束列表：
        若 做 版本约束_满足 用 约束列表 的 第 i 项、版本：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

版本_排序 接受 列表：
    可变 已改变 为 1。
    只要 已改变 等于 1：
        已改变 为 0。
        可变 i 为 0。
        只要 i 小于 做 计数 用 列表 减 1：
            若 做 版本号_版本_比较 用 列表 的 第 i 项、列表 的 第 i 加 1 项 大于 0：
                临时 为 列表 的 第 i 项。
                列表 的 第 i 项 为 列表 的 第 i 加 1 项。
                列表 的 第 i 加 1 项 为 临时。
                已改变 为 1。
            完。
            i 为 i 加 1。
        完。
    完。
完。

版本_最新 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 空。
    完。
    可变 最新 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 做 版本号_版本_比较 用 列表 的 第 i 项、最新 大于 0：
            最新 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最新。
完。

版本_最旧 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 空。
    完。
    可变 最旧 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 做 版本号_版本_比较 用 列表 的 第 i 项、最旧 小于 0：
            最旧 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最旧。
完。

版本_筛选满足 接受 列表、约束：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 版本约束_满足 用 约束、列表 的 第 i 项：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

版本_最佳匹配 接受 列表、约束：
    满足列表 为 做 版本_筛选满足 用 列表、约束。
    返还 做 版本_最新 用 满足列表。
完。

版本_转整数 接受 v：
    返还 v 的 第 0 项 乘 1000000 加 v 的 第 1 项 乘 1000 加 v 的 第 2 项。
完。

版本_从整数 接受 n：
    返还 一组：n 除以 1000000、n 除以 1000 取余于 1000、n 取余于 1000、「」、「」。
完。

版本_转数组 接受 v：
    返还 一组：v 的 第 0 项、v 的 第 1 项、v 的 第 2 项。
完。

版本注册表_注册 接受 名称、版本：
    做 追加 用 _版本_注册表、一组：名称、版本。
完。

版本注册表_查询 接受 名称：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _版本_注册表：
        若 _版本_注册表 的 第 i 项 的 第 0 项 等于 名称：
            做 追加 用 结果、_版本_注册表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

版本注册表_最新 接受 名称：
    版本列表 为 做 版本注册表_查询 用 名称。
    返还 做 版本_最新 用 版本列表。
完。

版本_差异类型 接受 旧、新：
    若 新 的 第 0 项 不等于 旧 的 第 0 项：
        返还 「主版本变更(不兼容)」。
    完。
    若 新 的 第 1 项 不等于 旧 的 第 1 项：
        返还 「次版本变更(新功能)」。
    完。
    若 新 的 第 2 项 不等于 旧 的 第 2 项：
        返还 「补丁变更(修复)」。
    完。
    返还 「无变更」。
完。

版本_距离 接受 a、b：
    可变 距离 为 0。
    若 a 的 第 0 项 不等于 b 的 第 0 项：
        可变 差 为 a 的 第 0 项 减 b 的 第 0 项。
        若 差 小于 0：
            差 为 负 差。
        完。
        距离 为 距离 加 差 乘 1000。
    完。
    若 a 的 第 1 项 不等于 b 的 第 1 项：
        可变 差 为 a 的 第 1 项 减 b 的 第 1 项。
        若 差 小于 0：
            差 为 负 差。
        完。
        距离 为 距离 加 差 乘 10。
    完。
    若 a 的 第 2 项 不等于 b 的 第 2 项：
        可变 差 为 a 的 第 2 项 减 b 的 第 2 项。
        若 差 小于 0：
            差 为 负 差。
        完。
        距离 为 距离 加 差。
    完。
    返还 距离。
完。

版本_是预发布 接受 v：
    返还 v 的 第 3 项 不等于 「」。
完。

版本_是稳定 接受 v：
    返还 v 的 第 3 项 等于 「」 并且 v 的 第 0 项 大于 0。
完。

版本_是开发 接受 v：
    返还 v 的 第 0 项 等于 0。
完。

版本_预发布类型 接受 v：
    若 v 的 第 3 项 等于 「」：
        返还 「稳定版」。
    完。
    可变 标签 为 v 的 第 3 项。
    可变 i 为 0。
    可变 前缀 为 「」。
    只要 i 小于 做 字节长度 用 标签 并且 i 小于 5：
        前缀 为 做 字符串拼接 用 前缀、做 字符 用 做 字节取 用 标签、i。
        i 为 i 加 1。
    完。
    若 前缀 等于 「alpha」：
        返还 「Alpha版」。
    完。
    若 前缀 等于 「beta」：
        返还 「Beta版」。
    完。
    若 前缀 等于 「rc」：
        返还 「候选版」。
    完。
    返还 「预发布」。
完。

版本_升级风险 接受 当前、目标：
    若 做 版本_等于 用 当前、目标：
        返还 一组：0、「无需升级」。
    完。
    若 做 版本_小于 用 当前、目标：
        若 目标 的 第 0 项 不等于 当前 的 第 0 项：
            返还 一组：90、「高风险: 主版本变更,API可能不兼容」。
        完。
        若 目标 的 第 1 项 不等于 当前 的 第 1 项：
            返还 一组：40、「中风险: 新功能,通常向后兼容」。
        完。
        返还 一组：10、「低风险: 补丁更新」。
    完。
    返还 一组：70、「警告: 降级操作」。
完。

版本_升级路径 接受 当前、可用列表：
    可变 路径 为 一组：（空）。
    做 版本_排序 用 可用列表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 可用列表：
        若 做 版本_大于 用 可用列表 的 第 i 项、当前：
            风险 为 做 版本_升级风险 用 当前、可用列表 的 第 i 项。
            做 追加 用 路径、一组：做 版本号_版本_格式化 用 可用列表 的 第 i 项、风险 的 第 0 项、风险 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 路径。
完。

版本_哈希 接受 v：
    返还 (v 的 第 0 项 乘 7919 加 v 的 第 1 项 乘 541 加 v 的 第 2 项 乘 31) 取余于 2147483647。
完。

版本_唯一键 接受 v：
    返还 做 字符串拼接 用 做 转字符串 用 v 的 第 0 项、做 字符串拼接 用 「.」、做 字符串拼接 用 做 转字符串 用 v 的 第 1 项、做 字符串拼接 用 「.」、做 转字符串 用 v 的 第 2 项。
完。

依赖_创建 接受 名称、约束：
    返还 一组：名称、约束、空。
完。

版本号_依赖_解析 接受 dep、_注册表查询：
    可用 为 做 版本注册表_查询 用 dep 的 第 0 项。
    若 做 计数 用 可用 等于 0：
        返还 一组：「错误」、「未找到依赖」。
    完。
    匹配 为 做 版本_最佳匹配 用 可用、dep 的 第 1 项。
    若 匹配 等于 空：
        返还 一组：「错误」、「无满足约束的版本」。
    完。
    dep 的 第 2 项 为 匹配。
    返还 一组：「成功」、匹配。
完。

依赖列表_解析 接受 列表：
    可变 结果 为 一组：（空）。
    可变 失败 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        r 为 做 版本号_依赖_解析 用 列表 的 第 i 项、空。
        做 追加 用 结果、一组：列表 的 第 i 项 的 第 0 项、r 的 第 0 项、r 的 第 1 项。
        若 r 的 第 0 项 等于 「错误」：
            失败 为 失败 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：结果、失败。
完。

可变 _版本_变更日志 为 一组：（空）。

变更日志_添加 接受 版本、类型、描述：
    做 追加 用 _版本_变更日志、一组：版本、类型、描述。
完。

变更日志_按版本 接受 目标版本：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _版本_变更日志：
        若 做 版本_等于 用 _版本_变更日志 的 第 i 项 的 第 0 项、目标版本：
            做 追加 用 结果、_版本_变更日志 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

变更日志_按类型 接受 类型：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _版本_变更日志：
        若 _版本_变更日志 的 第 i 项 的 第 1 项 等于 类型：
            做 追加 用 结果、_版本_变更日志 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

版本_克隆 接受 v：
    返还 一组：v 的 第 0 项、v 的 第 1 项、v 的 第 2 项、v 的 第 3 项、v 的 第 4 项。
完。

版本_去重 接受 列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        可变 j 为 0。
        只要 j 小于 做 计数 用 结果：
            若 做 版本_等于 用 列表 的 第 i 项、结果 的 第 j 项：
                重复 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 重复：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

版本_趋势分析 接受 历史列表：
    若 做 计数 用 历史列表 小于 2：
        返还 「数据不足」。
    完。
    做 版本_排序 用 历史列表。
    若 做 计数 用 历史列表 等于 0：
        返还 空。
    完。
    最新 为 历史列表 的 第 做 计数 用 历史列表 减 1 项。
    次新 为 历史列表 的 第 做 计数 用 历史列表 减 2 项。
    差异 为 做 版本_差异类型 用 次新、最新。
    若 差异 等于 「主版本变更(不兼容)」：
        返还 「活跃迭代(频繁不兼容变更)」。
    完。
    若 差异 等于 「次版本变更(新功能)」：
        返还 「稳定增长(持续添加功能)」。
    完。
    返还 「维护模式(仅修复)」。
完。

版本_冲突检测 接受 依赖列表：
    可变 冲突 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 依赖列表：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 依赖列表：
            若 依赖列表 的 第 i 项 的 第 0 项 等于 依赖列表 的 第 j 项 的 第 0 项：
                若 依赖列表 的 第 i 项 的 第 2 项 不等于 空 并且 依赖列表 的 第 j 项 的 第 2 项 不等于 空：
                    若 非 做 版本_等于 用 依赖列表 的 第 i 项 的 第 2 项、依赖列表 的 第 j 项 的 第 2 项：
                        做 追加 用 冲突、一组：依赖列表 的 第 i 项 的 第 0 项、依赖列表 的 第 i 项 的 第 2 项、依赖列表 的 第 j 项 的 第 2 项。
                    完。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 冲突。
完。

本章 收束。
