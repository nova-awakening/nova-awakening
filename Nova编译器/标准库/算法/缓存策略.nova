《缓存策略》

引用《..·基础》。

fifo_创建 接受 容量：
    返还 一组：一组：（空）、一组：（空）、容量。
完。

fifo_取 接受 缓存、键：
    键列表 为 缓存 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        若 键列表 的 第 i 项 等于 键：
            返还 缓存 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

fifo_设 接受 缓存、键、值：
    键列表 为 缓存 的 第 0 项。
    值列表 为 缓存 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        若 键列表 的 第 i 项 等于 键：
            值列表 的 第 i 项 为 值。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 键列表 大于或等于 缓存 的 第 2 项：
        做 删除 用 键列表。
        做 删除 用 值列表。
    完。
    做 追加 用 键列表、键。
    做 追加 用 值列表、值。
完。

fifo_大小 接受 缓存：
    返还 做 计数 用 缓存 的 第 0 项。
完。

缓存策略_lru_创建 接受 容量：
    返还 一组：一组：（空）、一组：（空）、容量。
完。

lru_取 接受 缓存、键：
    键列表 为 缓存 的 第 0 项。
    值列表 为 缓存 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        若 键列表 的 第 i 项 等于 键：
            v 为 值列表 的 第 i 项。
            k 为 键列表 的 第 i 项。
            做 删除 用 键列表、i。
            做 删除 用 值列表、i。
            做 追加 用 键列表、k。
            做 追加 用 值列表、v。
            返还 v。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

lru_设 接受 缓存、键、值：
    键列表 为 缓存 的 第 0 项。
    值列表 为 缓存 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        若 键列表 的 第 i 项 等于 键：
            做 删除 用 键列表、i。
            做 删除 用 值列表、i。
            做 追加 用 键列表、键。
            做 追加 用 值列表、值。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 键列表 大于或等于 缓存 的 第 2 项：
        做 删除 用 键列表。
        做 删除 用 值列表。
    完。
    做 追加 用 键列表、键。
    做 追加 用 值列表、值。
完。

缓存策略_lru_大小 接受 缓存：
    返还 做 计数 用 缓存 的 第 0 项。
完。

lru_包含 接受 缓存、键：
    键列表 为 缓存 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        若 键列表 的 第 i 项 等于 键：
            返还 1。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

lfu_创建 接受 容量：
    返还 一组：一组：（空）、一组：（空）、一组：（空）、容量。
完。

lfu_取 接受 缓存、键：
    键列表 为 缓存 的 第 0 项。
    频率 为 缓存 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        若 键列表 的 第 i 项 等于 键：
            频率 的 第 i 项 为 频率 的 第 i 项 加 1。
            返还 缓存 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

lfu_设 接受 缓存、键、值：
    键列表 为 缓存 的 第 0 项。
    值列表 为 缓存 的 第 1 项。
    频率 为 缓存 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        若 键列表 的 第 i 项 等于 键：
            值列表 的 第 i 项 为 值。
            频率 的 第 i 项 为 频率 的 第 i 项 加 1。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 键列表 大于或等于 缓存 的 第 3 项：
        可变 最低idx 为 0。
        i 为 1。
        只要 i 小于 做 计数 用 频率：
            若 频率 的 第 i 项 小于 频率 的 第 最低idx 项：
                最低idx 为 i。
            完。
            i 为 i 加 1。
        完。
        做 删除 用 键列表、最低idx。
        做 删除 用 值列表、最低idx。
        做 删除 用 频率、最低idx。
    完。
    做 追加 用 键列表、键。
    做 追加 用 值列表、值。
    做 追加 用 频率、1。
完。

lfu_大小 接受 缓存：
    返还 做 计数 用 缓存 的 第 0 项。
完。

ttl_创建 接受 容量：
    返还 一组：一组：（空）、一组：（空）、一组：（空）、容量。
完。

ttl_设 接受 缓存、键、值、过期时间：
    键列表 为 缓存 的 第 0 项。
    值列表 为 缓存 的 第 1 项。
    过期列表 为 缓存 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        若 键列表 的 第 i 项 等于 键：
            值列表 的 第 i 项 为 值。
            过期列表 的 第 i 项 为 过期时间。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 键列表 大于或等于 缓存 的 第 3 项：
        做 删除 用 键列表。
        做 删除 用 值列表。
        做 删除 用 过期列表。
    完。
    做 追加 用 键列表、键。
    做 追加 用 值列表、值。
    做 追加 用 过期列表、过期时间。
完。

ttl_取 接受 缓存、键、当前时间：
    键列表 为 缓存 的 第 0 项。
    过期列表 为 缓存 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        若 键列表 的 第 i 项 等于 键：
            若 当前时间 大于 过期列表 的 第 i 项：
                返还 空。
            完。
            返还 缓存 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

ttl_清理过期 接受 缓存、当前时间：
    键列表 为 缓存 的 第 0 项。
    值列表 为 缓存 的 第 1 项。
    过期列表 为 缓存 的 第 2 项。
    可变 i 为 做 计数 用 键列表 减 1。
    只要 i 大于或等于 0：
        若 当前时间 大于 过期列表 的 第 i 项：
            做 删除 用 键列表、i。
            做 删除 用 值列表、i。
            做 删除 用 过期列表、i。
        完。
        i 为 i 减 1。
    完。
完。

ttl_大小 接受 缓存：
    返还 做 计数 用 缓存 的 第 0 项。
完。

可变 _ttl_键 为 一组：（空）。

可变 _ttl_值 为 一组：（空）。

可变 _ttl_过期 为 一组：（空）。

缓存策略_TTL_设置 接受 键、值、过期时间：
    做 追加 用 _ttl_键、键。
    做 追加 用 _ttl_值、值。
    做 追加 用 _ttl_过期、过期时间。
完。

缓存策略_TTL_获取 接受 键、当前时间：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _ttl_键：
        若 _ttl_键 的 第 i 项 等于 键 并且 _ttl_过期 的 第 i 项 大于 当前时间：
            返还 _ttl_值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

缓存策略_TTL_清理过期 接受 当前时间：
    可变 新键 为 一组：（空）。
    可变 新值 为 一组：（空）。
    可变 新期 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _ttl_键：
        若 _ttl_过期 的 第 i 项 大于 当前时间：
            做 追加 用 新键、_ttl_键 的 第 i 项。
            做 追加 用 新值、_ttl_值 的 第 i 项。
            做 追加 用 新期、_ttl_过期 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    _ttl_键 为 新键。
    _ttl_值 为 新值。
    _ttl_过期 为 新期。
完。

本章 收束。
