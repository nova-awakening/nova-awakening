《排序高级》

引用《..·基础》。

引用《..·数学·工具》。

引用《.·搜索算法》。

排序高级_快速排序 接受 列表：
    若 做 计数 用 列表 小于或等于 1：
        返还 列表。
    完。
    基准 为 列表 的 第 做 计数 用 列表 除以 2 项。
    可变 低组 为 一组：（空）。
    可变 同组 为 一组：（空）。
    可变 高组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 基准：
            做 追加 用 低组、列表 的 第 i 项。
        否则若 列表 的 第 i 项 等于 基准：
            做 追加 用 同组、列表 的 第 i 项。
        否则：
            做 追加 用 高组、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    左 为 做 排序高级_快速排序 用 低组。
    右 为 做 排序高级_快速排序 用 高组。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 左：
        做 追加 用 结果、左 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 同组：
        做 追加 用 结果、同组 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 右：
        做 追加 用 结果、右 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

排序高级_归并排序 接受 列表：
    若 做 计数 用 列表 小于或等于 1：
        返还 列表。
    完。
    中 为 做 计数 用 列表 除以 2。
    可变 左半 为 一组：（空）。
    可变 右半 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 中：
        做 追加 用 左半、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 右半、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    左排 为 做 排序高级_归并排序 用 左半。
    右排 为 做 排序高级_归并排序 用 右半。
    返还 做 排序高级_归并合并 用 左排、右排。
完。

排序高级_归并合并 接受 左、右：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    可变 j 为 0。
    只要 i 小于 做 计数 用 左 并且 j 小于 做 计数 用 右：
        若 左 的 第 i 项 小于或等于 右 的 第 j 项：
            做 追加 用 结果、左 的 第 i 项。
            i 为 i 加 1。
        否则：
            做 追加 用 结果、右 的 第 j 项。
            j 为 j 加 1。
        完。
    完。
    只要 i 小于 做 计数 用 左：
        做 追加 用 结果、左 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 j 小于 做 计数 用 右：
        做 追加 用 结果、右 的 第 j 项。
        j 为 j 加 1。
    完。
    返还 结果。
完。

排序高级_希尔排序 接受 列表：
    n 为 做 计数 用 列表。
    可变 间隔 为 n 除以 2。
    只要 间隔 大于 0：
        可变 i 为 间隔。
        只要 i 小于 n：
            临时 为 列表 的 第 i 项。
            可变 j 为 i。
            只要 j 大于或等于 间隔 并且 列表 的 第 j 减 间隔 项 大于 临时：
                列表 的 第 j 项 为 列表 的 第 j 减 间隔 项。
                j 为 j 减 间隔。
            完。
            列表 的 第 j 项 为 临时。
            i 为 i 加 1。
        完。
        间隔 为 间隔 除以 2。
    完。
    返还 列表。
完。

排序高级_插入排序 接受 列表：
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        键 为 列表 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 列表 的 第 j 项 大于 键：
            列表 的 第 j 加 1 项 为 列表 的 第 j 项。
            j 为 j 减 1。
        完。
        列表 的 第 j 加 1 项 为 键。
        i 为 i 加 1。
    完。
    返还 列表。
完。

排序高级_计数排序 接受 列表、最大值：
    可变 计数表 为 一组：（空）。
    可变 ci 为 0。
    只要 ci 小于或等于 最大值：
        做 追加 用 计数表、0。
        ci 为 ci 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        计数表 的 第 列表 的 第 i 项 项 为 计数表 的 第 列表 的 第 i 项 项 加 1。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    ci 为 0。
    只要 ci 小于或等于 最大值：
        可变 j 为 0。
        只要 j 小于 计数表 的 第 ci 项：
            做 追加 用 结果、ci。
            j 为 j 加 1。
        完。
        ci 为 ci 加 1。
    完。
    返还 结果。
完。

排序高级_二分搜索 接受 有序列表、目标：
    可变 低 为 0。
    可变 高 为 做 计数 用 有序列表 减 1。
    只要 低 小于或等于 高：
        中 为 (低 加 高) 除以 2。
        若 有序列表 的 第 中 项 等于 目标：
            返还 中。
        完。
        若 有序列表 的 第 中 项 小于 目标：
            低 为 中 加 1。
        否则：
            高 为 中 减 1。
        完。
    完。
    返还 负 1。
完。

排序高级_快速选择 接受 列表、k：
    若 做 计数 用 列表 小于或等于 1：
        返还 列表 的 第 0 项。
    完。
    基准 为 列表 的 第 做 计数 用 列表 除以 2 项。
    可变 低组 为 一组：（空）。
    可变 同组 为 一组：（空）。
    可变 高组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 基准：
            做 追加 用 低组、列表 的 第 i 项。
        否则若 列表 的 第 i 项 等于 基准：
            做 追加 用 同组、列表 的 第 i 项。
        否则：
            做 追加 用 高组、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 k 小于 做 计数 用 低组：
        返还 做 排序高级_快速选择 用 低组、k。
    完。
    若 k 小于 做 计数 用 低组 加 做 计数 用 同组：
        返还 基准。
    完。
    返还 做 排序高级_快速选择 用 高组、k 减 做 计数 用 低组 减 做 计数 用 同组。
完。

逆序对计数 接受 列表：
    若 做 计数 用 列表 小于或等于 1：
        返还 一组：列表、0。
    完。
    中 为 做 计数 用 列表 除以 2。
    可变 左半 为 一组：（空）。
    可变 右半 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 中：
        做 追加 用 左半、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 右半、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    左结果 为 做 逆序对计数 用 左半。
    右结果 为 做 逆序对计数 用 右半。
    可变 总 为 左结果 的 第 1 项 加 右结果 的 第 1 项。
    可变 合并 为 一组：（空）。
    i 为 0。
    可变 j 为 0。
    只要 i 小于 做 计数 用 左结果 的 第 0 项 并且 j 小于 做 计数 用 右结果 的 第 0 项：
        若 左结果 的 第 0 项 的 第 i 项 小于或等于 右结果 的 第 0 项 的 第 j 项：
            做 追加 用 合并、左结果 的 第 0 项 的 第 i 项。
            i 为 i 加 1。
        否则：
            做 追加 用 合并、右结果 的 第 0 项 的 第 j 项。
            总 为 总 加 做 计数 用 左结果 的 第 0 项 减 i。
            j 为 j 加 1。
        完。
    完。
    只要 i 小于 做 计数 用 左结果 的 第 0 项：
        做 追加 用 合并、左结果 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 j 小于 做 计数 用 右结果 的 第 0 项：
        做 追加 用 合并、右结果 的 第 0 项 的 第 j 项。
        j 为 j 加 1。
    完。
    返还 一组：合并、总。
完。

桶排序 接受 列表、桶数：
    n 为 做 计数 用 列表。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 最小 为 列表 的 第 0 项。
    可变 最大 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 列表 的 第 i 项 小于 最小：
            最小 为 列表 的 第 i 项。
        完。
        若 列表 的 第 i 项 大于 最大：
            最大 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    范围 为 最大 减 最小 加 1。
    可变 桶宽 为 范围 除以 桶数。
    若 桶宽 等于 0：
        桶宽 为 1。
    完。
    可变 桶 为 一组：（空）。
    i 为 0。
    只要 i 小于 桶数：
        做 追加 用 桶、一组：（空）。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 索引 为 (列表 的 第 i 项 减 最小) 除以 桶宽。
        若 索引 大于或等于 桶数：
            索引 为 桶数 减 1。
        完。
        做 追加 用 桶 的 第 索引 项、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 桶数：
        可变 b 为 桶 的 第 i 项。
        可变 j 为 1。
        只要 j 小于 做 计数 用 b：
            key 为 b 的 第 j 项。
            可变 k 为 j 减 1。
            只要 k 大于或等于 0 并且 b 的 第 k 项 大于 key：
                b 的 第 k 加 1 项 为 b 的 第 k 项。
                k 为 k 减 1。
            完。
            b 的 第 k 加 1 项 为 key。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 做 计数 用 b：
            做 追加 用 结果、b 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

排序_已排序 接受 列表：
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 列表 的 第 i 减 1 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

排序_逆序数 接受 列表：
    可变 逆序 为 0。
    n 为 做 计数 用 列表。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 列表 的 第 i 项 大于 列表 的 第 j 项：
                逆序 为 逆序 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 逆序。
完。

第N大 接受 列表、n：
    总 为 做 计数 用 列表。
    若 n 小于或等于 0 或者 n 大于 总：
        返还 空。
    完。
    可变 副本 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 总：
        做 追加 用 副本、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 k 为 0。
    只要 k 小于 n：
        可变 最大idx 为 0。
        i 为 1。
        只要 i 小于 做 计数 用 副本：
            若 副本 的 第 i 项 大于 副本 的 第 最大idx 项：
                最大idx 为 i。
            完。
            i 为 i 加 1。
        完。
        若 k 等于 n 减 1：
            返还 副本 的 第 最大idx 项。
        完。
        副本 的 第 最大idx 项 为 负 999999999。
        k 为 k 加 1。
    完。
    返还 空。
完。

本章 收束。
