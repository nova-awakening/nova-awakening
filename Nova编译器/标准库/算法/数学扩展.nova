《数学扩展》

引用《..·基础》。

引用《..·数学·工具》。

快速幂 接受 a、b、m：
    可变 结果 为 1。
    可变 底 为 a 取余于 m。
    可变 指数 为 b。
    只要 指数 大于 0：
        若 指数 取余于 2 等于 1：
            结果 为 结果 乘 底 取余于 m。
        完。
        指数 为 指数 除以 2。
        底 为 底 乘 底 取余于 m。
    完。
    返还 结果。
完。

数学扩展_gcd 接受 a、b：
    可变 x 为 a。
    可变 y 为 b。
    只要 y 不等于 0：
        临时 为 y。
        y 为 x 取余于 y。
        x 为 临时。
    完。
    返还 x。
完。

数学扩展_lcm 接受 a、b：
    返还 a 除以 做 数学扩展_gcd 用 a、b 乘 b。
完。

数学扩展_是素数 接受 n：
    若 n 小于 2：
        返还 0。
    完。
    若 n 小于 4：
        返还 1。
    完。
    若 n 取余于 2 等于 0：
        返还 0。
    完。
    若 n 取余于 3 等于 0：
        返还 0。
    完。
    可变 i 为 5。
    只要 i 乘 i 小于或等于 n：
        若 n 取余于 i 等于 0：
            返还 0。
        完。
        若 n 取余于 (i 加 2) 等于 0：
            返还 0。
        完。
        i 为 i 加 6。
    完。
    返还 1。
完。

素数筛 接受 最大值：
    可变 筛 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 最大值：
        做 追加 用 筛、1。
        i 为 i 加 1。
    完。
    筛 的 第 0 项 为 0。
    筛 的 第 1 项 为 0。
    i 为 2。
    只要 i 乘 i 小于或等于 最大值：
        若 筛 的 第 i 项 等于 1：
            可变 j 为 i 乘 i。
            只要 j 小于或等于 最大值：
                筛 的 第 j 项 为 0。
                j 为 j 加 i。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 素数列表 为 一组：（空）。
    i 为 2。
    只要 i 小于或等于 最大值：
        若 筛 的 第 i 项 等于 1：
            做 追加 用 素数列表、i。
        完。
        i 为 i 加 1。
    完。
    返还 素数列表。
完。

数学扩展_组合数 接受 n、k：
    若 k 大于 n：
        返还 0。
    完。
    若 k 等于 0：
        返还 1。
    完。
    可变 kk 为 k。
    若 kk 大于 n 减 kk：
        kk 为 n 减 kk。
    完。
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 kk：
        结果 为 结果 乘 (n 减 i)。
        结果 为 结果 除以 (i 加 1)。
        i 为 i 加 1。
    完。
    返还 结果。
完。

排列数 接受 n、k：
    若 k 大于 n：
        返还 0。
    完。
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 k：
        结果 为 结果 乘 (n 减 i)。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学扩展_阶乘 接受 n：
    若 n 小于或等于 1：
        返还 1。
    完。
    可变 结果 为 1。
    可变 i 为 2。
    只要 i 小于或等于 n：
        结果 为 结果 乘 i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学扩展_斐波那契 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    若 n 等于 1：
        返还 1。
    完。
    可变 a 为 0。
    可变 b 为 1。
    可变 i 为 2。
    只要 i 小于或等于 n：
        c 为 a 加 b。
        a 为 b。
        b 为 c。
        i 为 i 加 1。
    完。
    返还 b。
完。

整数sqrt 接受 n：
    若 n 小于或等于 1：
        返还 n。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

数学_abs 接受 n：
    若 n 小于 0：
        返还 0 减 n。
    完。
    返还 n。
完。

数学_max 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

数学_min 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

数学_clamp 接受 值、最小、最大：
    若 值 小于 最小：
        返还 最小。
    完。
    若 值 大于 最大：
        返还 最大。
    完。
    返还 值。
完。

log2_整数 接受 n：
    若 n 小于或等于 0：
        返还 负 1。
    完。
    可变 r 为 0。
    可变 v 为 n。
    只要 v 大于 1：
        v 为 v 除以 2。
        r 为 r 加 1。
    完。
    返还 r。
完。

数学扩展_是2的幂 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 v 为 n。
    只要 v 大于 1：
        若 v 取余于 2 不等于 0：
            返还 0。
        完。
        v 为 v 除以 2。
    完。
    返还 1。
完。

数扩_欧拉函数 接受 n：
    可变 结果 为 n。
    可变 i 为 2。
    可变 nn 为 n。
    只要 i 乘 i 小于或等于 nn：
        若 nn 取余于 i 等于 0：
            只要 nn 取余于 i 等于 0：
                nn 为 nn 除以 i。
            完。
            结果 为 结果 减 结果 除以 i。
        完。
        i 为 i 加 1。
    完。
    若 nn 大于 1：
        结果 为 结果 减 结果 除以 nn。
    完。
    返还 结果。
完。

数扩_组合数 接受 n、k：
    若 k 大于 n 或者 k 小于 0：
        返还 0。
    完。
    若 k 等于 0 或者 k 等于 n：
        返还 1。
    完。
    可变 实际k 为 k。
    若 实际k 大于 n 减 实际k：
        实际k 为 n 减 实际k。
    完。
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 实际k：
        结果 为 结果 乘 (n 减 i) 除以 (i 加 1)。
        i 为 i 加 1。
    完。
    返还 结果。
完。

牛顿_整数sqrt 接受 n：
    若 n 小于或等于 1：
        返还 n。
    完。
    可变 x 为 n。
    只要 x 乘 x 大于 n：
        x 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

本章 收束。
