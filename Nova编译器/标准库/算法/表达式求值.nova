《表达式求值》

引用《..·基础》。

可变 eval_源 为 「」。

可变 eval_位 为 0。

可变 eval_变量 为 一本字典：（空）。

eval_设变量 接受 名、值：
    做 字典设 用 eval_变量、名、值。
完。

eval_跳空白 接受 零参：
    只要 eval_位 小于 做 字节长度 用 eval_源 并且 做 字节取 用 eval_源、eval_位 等于 字符_空格：
        eval_位 为 eval_位 加 1。
    完。
完。

eval_当前 接受 零参：
    若 eval_位 大于或等于 做 字节长度 用 eval_源：
        返还 0。
    完。
    返还 做 字节取 用 eval_源、eval_位。
完。

eval_求值 接受 表达式：
    eval_源 为 表达式。
    eval_位 为 0。
    返还 做 eval_加减。
完。

eval_加减 接受 零参：
    可变 左 为 做 eval_乘除。
    做 eval_跳空白。
    只要 做 eval_当前 等于 43 或者 做 eval_当前 等于 45：
        op 为 做 eval_当前。
        eval_位 为 eval_位 加 1。
        右 为 做 eval_乘除。
        若 op 等于 43：
            左 为 左 加 右。
        否则：
            左 为 左 减 右。
        完。
        做 eval_跳空白。
    完。
    返还 左。
完。

eval_乘除 接受 零参：
    可变 左 为 做 eval_一元。
    做 eval_跳空白。
    只要 做 eval_当前 等于 42 或者 做 eval_当前 等于 47 或者 做 eval_当前 等于 37：
        op 为 做 eval_当前。
        eval_位 为 eval_位 加 1。
        右 为 做 eval_一元。
        若 op 等于 42：
            左 为 左 乘 右。
        否则若 op 等于 47：
            若 右 不等于 0：
                左 为 左 除以 右。
            否则：
                左 为 0。
            完。
        否则：
            若 右 不等于 0：
                左 为 左 取余于 右。
            否则：
                左 为 0。
            完。
        完。
        做 eval_跳空白。
    完。
    返还 左。
完。

eval_一元 接受 零参：
    做 eval_跳空白。
    若 做 eval_当前 等于 45：
        eval_位 为 eval_位 加 1。
        返还 0 减 做 eval_原子。
    完。
    若 做 eval_当前 等于 43：
        eval_位 为 eval_位 加 1。
    完。
    返还 做 eval_原子。
完。

eval_原子 接受 零参：
    做 eval_跳空白。
    c 为 做 eval_当前。
    若 c 等于 字符_左圆括：
        eval_位 为 eval_位 加 1。
        值 为 做 eval_加减。
        做 eval_跳空白。
        若 做 eval_当前 等于 41：
            eval_位 为 eval_位 加 1。
        完。
        返还 值。
    完。
    若 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
        可变 数 为 0。
        只要 做 eval_当前 大于或等于 48 并且 做 eval_当前 小于或等于 57：
            数 为 数 乘 10 加 做 eval_当前 减 48。
            eval_位 为 eval_位 加 1。
        完。
        返还 数。
    完。
    若 c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z 或者 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z：
        可变 名 为 「」。
        只要 eval_位 小于 做 字节长度 用 eval_源：
            ch 为 做 eval_当前。
            若 ch 大于或等于 字符_大写A 并且 ch 小于或等于 字符_大写Z 或者 ch 大于或等于 字符_小写a 并且 ch 小于或等于 字符_小写z 或者 ch 大于或等于 字符_零 并且 ch 小于或等于 字符_九 或者 ch 等于 字符_下划线：
                名 为 做 字符串拼接 用 名、做 字符 用 ch。
                eval_位 为 eval_位 加 1。
            否则：
                止。
            完。
        完。
        值 为 做 字典取 用 eval_变量、名。
        若 值 等于 空：
            返还 0。
        完。
        返还 值。
    完。
    返还 0。
完。

rpn_求值 接受 词元列：
    可变 栈 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词元列：
        t 为 词元列 的 第 i 项。
        若 t 等于 「+」：
            若 做 计数 用 栈 小于 2：
                返还 空。
            完。
            b 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            a 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            做 追加 用 栈、a 加 b。
        否则若 t 等于 「-」：
            若 做 计数 用 栈 小于 2：
                返还 空。
            完。
            b 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            a 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            做 追加 用 栈、a 减 b。
        否则若 t 等于 「*」：
            若 做 计数 用 栈 小于 2：
                返还 空。
            完。
            b 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            a 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            做 追加 用 栈、a 乘 b。
        否则若 t 等于 「/」：
            若 做 计数 用 栈 小于 2：
                返还 空。
            完。
            b 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            a 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            若 b 不等于 0：
                做 追加 用 栈、a 除以 b。
            否则：
                做 追加 用 栈、0。
            完。
        否则：
            做 追加 用 栈、t。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 栈 大于 0：
        返还 栈 的 第 0 项。
    完。
    返还 0。
完。

运算符优先级 接受 op：
    若 op 等于 「+」 或者 op 等于 「-」：
        返还 1。
    完。
    若 op 等于 「*」 或者 op 等于 「/」 或者 op 等于 「%」：
        返还 2。
    完。
    若 op 等于 「^」：
        返还 3。
    完。
    返还 0。
完。

是运算符 接受 t：
    返还 t 等于 「+」 或者 t 等于 「-」 或者 t 等于 「*」 或者 t 等于 「/」 或者 t 等于 「%」 或者 t 等于 「^」。
完。

中缀转后缀 接受 标记列表：
    可变 输出 为 一组：（空）。
    可变 操作栈 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 标记列表：
        t 为 标记列表 的 第 i 项。
        若 做 是运算符 用 t：
            只要 做 计数 用 操作栈 大于 0：
                栈顶 为 操作栈 的 第 做 计数 用 操作栈 减 1 项。
                若 栈顶 等于 「(」：
                    跳出。
                完。
                若 做 运算符优先级 用 栈顶 小于 做 运算符优先级 用 t：
                    跳出。
                完。
                做 追加 用 输出、栈顶。
                做 删除 用 操作栈、做 计数 用 操作栈 减 1。
            完。
            做 追加 用 操作栈、t。
        否则若 t 等于 「(」：
            做 追加 用 操作栈、t。
        否则若 t 等于 「)」：
            只要 做 计数 用 操作栈 大于 0 并且 操作栈 的 第 做 计数 用 操作栈 减 1 项 不等于 「(」：
                做 追加 用 输出、操作栈 的 第 做 计数 用 操作栈 减 1 项。
                做 删除 用 操作栈、做 计数 用 操作栈 减 1。
            完。
            若 做 计数 用 操作栈 大于 0：
                做 删除 用 操作栈、做 计数 用 操作栈 减 1。
            完。
        否则：
            做 追加 用 输出、t。
        完。
        i 为 i 加 1。
    完。
    只要 做 计数 用 操作栈 大于 0：
        做 追加 用 输出、操作栈 的 第 做 计数 用 操作栈 减 1 项。
        做 删除 用 操作栈、做 计数 用 操作栈 减 1。
    完。
    返还 输出。
完。

可变 _变量表_名 为 一组：（空）。

可变 _变量表_值 为 一组：（空）。

变量_设 接受 名、值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _变量表_名：
        若 _变量表_名 的 第 i 项 等于 名：
            _变量表_值 的 第 i 项 为 值。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 _变量表_名、名。
    做 追加 用 _变量表_值、值。
完。

变量_取 接受 名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _变量表_名：
        若 _变量表_名 的 第 i 项 等于 名：
            返还 _变量表_值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

后缀求值_带变量 接受 后缀列表：
    可变 栈 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 后缀列表：
        t 为 后缀列表 的 第 i 项。
        若 做 是运算符 用 t：
            若 做 计数 用 栈 小于 2：
                返还 空。
            完。
            b 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            a 为 栈 的 第 做 计数 用 栈 减 1 项。
            做 删除 用 栈、做 计数 用 栈 减 1。
            若 t 等于 「+」：
                做 追加 用 栈、a 加 b。
            否则若 t 等于 「-」：
                做 追加 用 栈、a 减 b。
            否则若 t 等于 「*」：
                做 追加 用 栈、a 乘 b。
            否则若 t 等于 「/」：
                若 b 不等于 0：
                    做 追加 用 栈、a 除以 b。
                否则：
                    做 追加 用 栈、0。
                完。
            否则若 t 等于 「%」：
                若 b 不等于 0：
                    做 追加 用 栈、a 取余于 b。
                否则：
                    做 追加 用 栈、0。
                完。
            否则若 t 等于 「^」：
                可变 结果 为 1。
                可变 j 为 0。
                只要 j 小于 b：
                    结果 为 结果 乘 a。
                    j 为 j 加 1。
                完。
                做 追加 用 栈、结果。
            完。
        否则：
            做 追加 用 栈、做 变量_取 用 t。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 栈 大于 0：
        返还 栈 的 第 0 项。
    完。
    返还 0。
完。

验证括号匹配 接受 标记列表：
    可变 深度 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 标记列表：
        若 标记列表 的 第 i 项 等于 「(」：
            深度 为 深度 加 1。
        完。
        若 标记列表 的 第 i 项 等于 「)」：
            深度 为 深度 减 1。
        完。
        若 深度 小于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 深度 等于 0。
完。

验证操作数平衡 接受 标记列表：
    可变 操作数 为 0。
    可变 运算符 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 标记列表：
        若 做 是运算符 用 标记列表 的 第 i 项：
            运算符 为 运算符 加 1。
        否则若 标记列表 的 第 i 项 不等于 「(」 并且 标记列表 的 第 i 项 不等于 「)」：
            操作数 为 操作数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 操作数 等于 运算符 加 1。
完。

可变 _求值_计算数 为 0。

可变 _求值_变量查找数 为 0。

求值_记录计算 接受 零参：
    _求值_计算数 为 _求值_计算数 加 1。
完。

求值_记录变量 接受 零参：
    _求值_变量查找数 为 _求值_变量查找数 加 1。
完。

本章 收束。
