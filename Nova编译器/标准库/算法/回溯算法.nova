《回溯算法》

引用《..·基础》。

全排列 接受 列表：
    可变 结果 为 一组：（空）。
    可变 已用 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 已用、0。
        i 为 i 加 1。
    完。
    做 全排列递归 用 列表、一组：（空）、已用、结果。
    返还 结果。
完。

全排列递归 接受 列表、当前、已用、结果：
    若 做 计数 用 当前 等于 做 计数 用 列表：
        可变 拷贝 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 当前：
            做 追加 用 拷贝、当前 的 第 i 项。
            i 为 i 加 1。
        完。
        做 追加 用 结果、拷贝。
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 已用 的 第 i 项 等于 0：
            已用 的 第 i 项 为 1。
            做 追加 用 当前、列表 的 第 i 项。
            做 全排列递归 用 列表、当前、已用、结果。
            做 删除 用 当前、做 计数 用 当前 减 1。
            已用 的 第 i 项 为 0。
        完。
        i 为 i 加 1。
    完。
完。

组合生成 接受 列表、k：
    可变 结果 为 一组：（空）。
    做 组合递归 用 列表、k、0、一组：（空）、结果。
    返还 结果。
完。

组合递归 接受 列表、k、起始、当前、结果：
    若 做 计数 用 当前 等于 k：
        可变 拷贝 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 当前：
            做 追加 用 拷贝、当前 的 第 i 项。
            i 为 i 加 1。
        完。
        做 追加 用 结果、拷贝。
        返还 空。
    完。
    可变 i 为 起始。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 当前、列表 的 第 i 项。
        做 组合递归 用 列表、k、i 加 1、当前、结果。
        做 删除 用 当前、做 计数 用 当前 减 1。
        i 为 i 加 1。
    完。
完。

所有子集 接受 列表：
    可变 结果 为 一组：（空）。
    做 子集递归 用 列表、0、一组：（空）、结果。
    返还 结果。
完。

子集递归 接受 列表、起始、当前、结果：
    可变 拷贝 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 当前：
        做 追加 用 拷贝、当前 的 第 i 项。
        i 为 i 加 1。
    完。
    做 追加 用 结果、拷贝。
    i 为 起始。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 当前、列表 的 第 i 项。
        做 子集递归 用 列表、i 加 1、当前、结果。
        做 删除 用 当前、做 计数 用 当前 减 1。
        i 为 i 加 1。
    完。
完。

n皇后 接受 n：
    可变 解 为 一组：（空）。
    可变 棋盘 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 棋盘、负 1。
        i 为 i 加 1。
    完。
    做 n皇后递归 用 棋盘、0、n、解。
    返还 解。
完。

n皇后递归 接受 棋盘、行、n、解：
    若 行 等于 n：
        可变 拷贝 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 n：
            做 追加 用 拷贝、棋盘 的 第 i 项。
            i 为 i 加 1。
        完。
        做 追加 用 解、拷贝。
        返还 空。
    完。
    可变 列 为 0。
    只要 列 小于 n：
        若 做 n皇后安全 用 棋盘、行、列 等于 1：
            棋盘 的 第 行 项 为 列。
            做 n皇后递归 用 棋盘、行 加 1、n、解。
            棋盘 的 第 行 项 为 负 1。
        完。
        列 为 列 加 1。
    完。
完。

n皇后安全 接受 棋盘、行、列：
    可变 i 为 0。
    只要 i 小于 行：
        若 棋盘 的 第 i 项 等于 列：
            返还 0。
        完。
        可变 差值 为 棋盘 的 第 i 项 减 列。
        若 差值 小于 0：
            差值 为 0 减 差值。
        完。
        若 差值 等于 行 减 i：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

整数分割 接受 n：
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 n：
        做 追加 用 dp、0。
        i 为 i 加 1。
    完。
    dp 的 第 0 项 为 1。
    i 为 1。
    只要 i 小于或等于 n：
        可变 j 为 i。
        只要 j 小于或等于 n：
            dp 的 第 j 项 为 dp 的 第 j 项 加 dp 的 第 j 减 i 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 dp 的 第 n 项。
完。

括号生成 接受 n：
    可变 结果 为 一组：（空）。
    做 括号递归 用 n、0、0、「」、结果。
    返还 结果。
完。

括号递归 接受 n、左、右、当前、结果：
    若 左 等于 n 并且 右 等于 n：
        做 追加 用 结果、当前。
        返还 空。
    完。
    若 左 小于 n：
        做 括号递归 用 n、左 加 1、右、做 字符串拼接 用 当前、「(」、结果。
    完。
    若 右 小于 左：
        做 括号递归 用 n、左、右 加 1、做 字符串拼接 用 当前、「)」、结果。
    完。
完。

N皇后_递归 接受 棋盘、行、n、解列表：
    若 行 等于 n：
        可变 解 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 n：
            做 追加 用 解、棋盘 的 第 i 项。
            i 为 i 加 1。
        完。
        做 追加 用 解列表、解。
        返还 空。
    完。
    可变 列 为 0。
    只要 列 小于 n：
        若 做 N皇后_安全 用 棋盘、行、列：
            棋盘 的 第 行 项 为 列。
            做 N皇后_递归 用 棋盘、行 加 1、n、解列表。
            棋盘 的 第 行 项 为 负 1。
        完。
        列 为 列 加 1。
    完。
完。

N皇后_安全 接受 棋盘、行、列：
    可变 i 为 0。
    只要 i 小于 行：
        若 棋盘 的 第 i 项 等于 列：
            返还 假。
        完。
        行差 为 行 减 i。
        可变 列差 为 列 减 棋盘 的 第 i 项。
        若 列差 小于 0：
            列差 为 0 减 列差。
        完。
        若 行差 等于 列差：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

回溯算法_组合 接受 n、k：
    可变 结果 为 一组：（空）。
    做 组合递归_范围 用 n、k、1、一组：（空）、结果。
    返还 结果。
完。

组合递归_范围 接受 n、k、起始、当前、结果：
    若 做 计数 用 当前 等于 k：
        可变 副本 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 当前：
            做 追加 用 副本、当前 的 第 i 项。
            i 为 i 加 1。
        完。
        做 追加 用 结果、副本。
        返还 空。
    完。
    可变 i 为 起始。
    只要 i 小于或等于 n：
        做 追加 用 当前、i。
        做 组合递归_范围 用 n、k、i 加 1、当前、结果。
        做 删除 用 当前、做 计数 用 当前 减 1。
        i 为 i 加 1。
    完。
完。

组合求和 接受 候选、目标：
    可变 结果 为 一组：（空）。
    做 组合求和_递归 用 候选、目标、0、一组：（空）、0、结果。
    返还 结果。
完。

组合求和_递归 接受 候选、目标、起始、当前、当前和、结果：
    若 当前和 等于 目标：
        可变 副本 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 当前：
            做 追加 用 副本、当前 的 第 i 项。
            i 为 i 加 1。
        完。
        做 追加 用 结果、副本。
        返还 空。
    完。
    若 当前和 大于 目标：
        返还 空。
    完。
    可变 i 为 起始。
    只要 i 小于 做 计数 用 候选：
        做 追加 用 当前、候选 的 第 i 项。
        做 组合求和_递归 用 候选、目标、i、当前、当前和 加 候选 的 第 i 项、结果。
        做 删除 用 当前、做 计数 用 当前 减 1。
        i 为 i 加 1。
    完。
完。

可变 _回溯_调用数 为 0。

可变 _回溯_剪枝数 为 0。

回溯_记录调用 接受 零参：
    _回溯_调用数 为 _回溯_调用数 加 1。
完。

回溯_记录剪枝 接受 零参：
    _回溯_剪枝数 为 _回溯_剪枝数 加 1。
完。

本章 收束。
