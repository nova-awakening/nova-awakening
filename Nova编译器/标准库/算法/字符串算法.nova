《字符串算法》

引用《..·基础》。

引用《..·文本·文本处理》。

kmp_失败表 接受 模式：
    m 为 做 字节长度 用 模式。
    可变 表 为 一组：（空）。
    可变 ti 为 0。
    只要 ti 小于 m：
        做 追加 用 表、0。
        ti 为 ti 加 1。
    完。
    可变 长度 为 0。
    可变 i 为 1。
    只要 i 小于 m：
        若 做 字节取 用 模式、i 等于 做 字节取 用 模式、长度：
            长度 为 长度 加 1。
            表 的 第 i 项 为 长度。
            i 为 i 加 1。
        否则：
            若 长度 不等于 0：
                长度 为 表 的 第 长度 减 1 项。
            否则：
                表 的 第 i 项 为 0。
                i 为 i 加 1。
            完。
        完。
    完。
    返还 表。
完。

kmp_搜索 接受 文本、模式：
    n 为 做 字节长度 用 文本。
    m 为 做 字节长度 用 模式。
    若 m 等于 0：
        返还 一组：（空）。
    完。
    表 为 做 kmp_失败表 用 模式。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    可变 j 为 0。
    只要 i 小于 n：
        若 做 字节取 用 文本、i 等于 做 字节取 用 模式、j：
            i 为 i 加 1。
            j 为 j 加 1。
        完。
        若 j 等于 m：
            做 追加 用 结果、i 减 j。
            j 为 表 的 第 j 减 1 项。
        否则若 i 小于 n 并且 做 字节取 用 文本、i 不等于 做 字节取 用 模式、j：
            若 j 不等于 0：
                j 为 表 的 第 j 减 1 项。
            否则：
                i 为 i 加 1。
            完。
        完。
    完。
    返还 结果。
完。

字符串算_编辑距离 接受 s1、s2：
    n 为 做 字节长度 用 s1。
    m 为 做 字节长度 用 s2。
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 n：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于或等于 m：
            做 追加 用 行、0。
            j 为 j 加 1。
        完。
        做 追加 用 dp、行。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于或等于 n：
        dp 的 第 i 项 的 第 0 项 为 i。
        i 为 i 加 1。
    完。
    可变 j2 为 0。
    只要 j2 小于或等于 m：
        dp 的 第 0 项 的 第 j2 项 为 j2。
        j2 为 j2 加 1。
    完。
    i 为 1。
    只要 i 小于或等于 n：
        可变 j 为 1。
        只要 j 小于或等于 m：
            若 做 字节取 用 s1、i 减 1 等于 做 字节取 用 s2、j 减 1：
                dp 的 第 i 项 的 第 j 项 为 dp 的 第 i 减 1 项 的 第 j 减 1 项。
            否则：
                删 为 dp 的 第 i 减 1 项 的 第 j 项 加 1。
                增 为 dp 的 第 i 项 的 第 j 减 1 项 加 1。
                替 为 dp 的 第 i 减 1 项 的 第 j 减 1 项 加 1。
                可变 最小 为 删。
                若 增 小于 最小：
                    最小 为 增。
                完。
                若 替 小于 最小：
                    最小 为 替。
                完。
                dp 的 第 i 项 的 第 j 项 为 最小。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 dp 的 第 n 项 的 第 m 项。
完。

LCS长度 接受 s1、s2：
    n 为 做 字节长度 用 s1。
    m 为 做 字节长度 用 s2。
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 n：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于或等于 m：
            做 追加 用 行、0。
            j 为 j 加 1。
        完。
        做 追加 用 dp、行。
        i 为 i 加 1。
    完。
    i 为 1。
    只要 i 小于或等于 n：
        可变 j 为 1。
        只要 j 小于或等于 m：
            若 做 字节取 用 s1、i 减 1 等于 做 字节取 用 s2、j 减 1：
                dp 的 第 i 项 的 第 j 项 为 dp 的 第 i 减 1 项 的 第 j 减 1 项 加 1。
            否则：
                若 dp 的 第 i 减 1 项 的 第 j 项 大于 dp 的 第 i 项 的 第 j 减 1 项：
                    dp 的 第 i 项 的 第 j 项 为 dp 的 第 i 减 1 项 的 第 j 项。
                否则：
                    dp 的 第 i 项 的 第 j 项 为 dp 的 第 i 项 的 第 j 减 1 项。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 dp 的 第 n 项 的 第 m 项。
完。

最长公共子串 接受 s1、s2：
    n 为 做 字节长度 用 s1。
    m 为 做 字节长度 用 s2。
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 n：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于或等于 m：
            做 追加 用 行、0。
            j 为 j 加 1。
        完。
        做 追加 用 dp、行。
        i 为 i 加 1。
    完。
    i 为 1。
    只要 i 小于或等于 n：
        可变 j 为 1。
        只要 j 小于或等于 m：
            若 做 字节取 用 s1、i 减 1 等于 做 字节取 用 s2、j 减 1：
                dp 的 第 i 项 的 第 j 项 为 dp 的 第 i 减 1 项 的 第 j 减 1 项 加 1。
            否则：
                若 dp 的 第 i 减 1 项 的 第 j 项 大于 dp 的 第 i 项 的 第 j 减 1 项：
                    dp 的 第 i 项 的 第 j 项 为 dp 的 第 i 减 1 项 的 第 j 项。
                否则：
                    dp 的 第 i 项 的 第 j 项 为 dp 的 第 i 项 的 第 j 减 1 项。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 「」。
    i 为 n。
    可变 jj 为 m。
    只要 i 大于 0 并且 jj 大于 0：
        若 做 字节取 用 s1、i 减 1 等于 做 字节取 用 s2、jj 减 1：
            结果 为 做 字符串拼接 用 做 字符 用 做 字节取 用 s1、i 减 1、结果。
            i 为 i 减 1。
            jj 为 jj 减 1。
        否则若 dp 的 第 i 减 1 项 的 第 jj 项 大于 dp 的 第 i 项 的 第 jj 减 1 项：
            i 为 i 减 1。
        否则：
            jj 为 jj 减 1。
        完。
    完。
    返还 结果。
完。

拉宾卡普 接受 文本、模式：
    n 为 做 字节长度 用 文本。
    m 为 做 字节长度 用 模式。
    若 m 大于 n：
        返还 一组：（空）。
    完。
    基数 为 256。
    素数 为 101。
    可变 模式哈希 为 0。
    可变 文本哈希 为 0。
    可变 h 为 1。
    可变 i 为 0。
    只要 i 小于 m 减 1：
        h 为 h 乘 基数 取余于 素数。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 m：
        模式哈希 为 (基数 乘 模式哈希 加 做 字节取 用 模式、i) 取余于 素数。
        文本哈希 为 (基数 乘 文本哈希 加 做 字节取 用 文本、i) 取余于 素数。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于或等于 n 减 m：
        若 模式哈希 等于 文本哈希：
            可变 已匹配 为 1。
            可变 j 为 0。
            只要 j 小于 m：
                若 做 字节取 用 文本、i 加 j 不等于 做 字节取 用 模式、j：
                    已匹配 为 0。
                    止。
                完。
                j 为 j 加 1。
            完。
            若 已匹配 等于 1：
                做 追加 用 结果、i。
            完。
        完。
        若 i 小于 n 减 m：
            文本哈希 为 (基数 乘 (文本哈希 减 做 字节取 用 文本、i 乘 h) 加 做 字节取 用 文本、i 加 m) 取余于 素数。
            若 文本哈希 小于 0：
                文本哈希 为 文本哈希 加 素数。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

字符串相似度 接受 s1、s2：
    距离 为 做 字符串算_编辑距离 用 s1、s2。
    可变 最长 为 做 字节长度 用 s1。
    若 做 字节长度 用 s2 大于 最长：
        最长 为 做 字节长度 用 s2。
    完。
    若 最长 等于 0：
        返还 100。
    完。
    返还 (最长 减 距离) 乘 100 除以 最长。
完。

字符串算法_是回文 接受 文本：
    n 为 做 字节长度 用 文本。
    可变 i 为 0。
    只要 i 小于 n 除以 2：
        若 做 字节取 用 文本、i 不等于 做 字节取 用 文本、n 减 1 减 i：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

字符串_旋转 接受 文本、k：
    n 为 做 字节长度 用 文本。
    若 n 等于 0：
        返还 文本。
    完。
    实际 为 k 取余于 n。
    返还 做 子串 用 文本、实际、n 减 实际 加 做 子串 用 文本、0、实际。
完。

本章 收束。
