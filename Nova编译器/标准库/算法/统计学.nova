《统计学》

引用《..·基础》。

引用《..·数学·统计》。

引用《.·数学扩展》。

统计学_统计_均值 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        总 为 总 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总 除以 做 计数 用 数据。
完。

统计学_统计_中位数 接受 数据：
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 0。
    完。
    可变 排序 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 排序、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 已改变 为 1。
    只要 已改变 等于 1：
        已改变 为 0。
        i 为 0。
        只要 i 小于 n 减 1：
            若 排序 的 第 i 项 大于 排序 的 第 i 加 1 项：
                临时 为 排序 的 第 i 项。
                排序 的 第 i 项 为 排序 的 第 i 加 1 项。
                排序 的 第 i 加 1 项 为 临时。
                已改变 为 1。
            完。
            i 为 i 加 1。
        完。
    完。
    若 n 取余于 2 等于 1：
        返还 排序 的 第 n 除以 2 项。
    完。
    返还 (排序 的 第 n 除以 2 减 1 项 加 排序 的 第 n 除以 2 项) 除以 2。
完。

统计学_统计_众数 接受 数据：
    可变 最多次 为 0。
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 0。
    完。
    可变 最多值 为 数据 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 n：
        可变 次数 为 0。
        可变 j 为 0。
        只要 j 小于 n：
            若 数据 的 第 j 项 等于 数据 的 第 i 项：
                次数 为 次数 加 1。
            完。
            j 为 j 加 1。
        完。
        若 次数 大于 最多次：
            最多次 为 次数。
            最多值 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最多值。
完。

统计学_统计_方差 接受 数据：
    n 为 做 计数 用 数据。
    若 n 小于或等于 1：
        返还 0。
    完。
    均值 为 做 统计_均值 用 数据。
    可变 总差方 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        差 为 数据 的 第 i 项 减 均值。
        总差方 为 总差方 加 差 乘 差。
        i 为 i 加 1。
    完。
    返还 总差方 除以 n。
完。

统计学_统计_标准差 接受 数据：
    返还 做 整数sqrt 用 做 统计_方差 用 数据。
完。

统计学_统计_范围 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    可变 最小 为 数据 的 第 0 项。
    可变 最大 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 小于 最小：
            最小 为 数据 的 第 i 项。
        完。
        若 数据 的 第 i 项 大于 最大：
            最大 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大 减 最小。
完。

统计学_统计_协方差 接受 x、y：
    n 为 做 计数 用 x。
    若 n 等于 0 或者 做 计数 用 y 不等于 n：
        返还 0。
    完。
    均x 为 做 统计_均值 用 x。
    均y 为 做 统计_均值 用 y。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总 为 总 加 (x 的 第 i 项 减 均x) 乘 (y 的 第 i 项 减 均y)。
        i 为 i 加 1。
    完。
    返还 总 除以 n。
完。

统计学_统计_线性回归 接受 x、y：
    n 为 做 计数 用 x。
    若 n 等于 0：
        返还 一组：0、0。
    完。
    均x 为 做 统计_均值 用 x。
    均y 为 做 统计_均值 用 y。
    可变 分子 为 0。
    可变 分母 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        导数 为 x 的 第 i 项 减 均x。
        分子 为 分子 加 导数 乘 (y 的 第 i 项 减 均y)。
        分母 为 分母 加 导数 乘 导数。
        i 为 i 加 1。
    完。
    若 分母 等于 0：
        返还 一组：0、均y。
    完。
    斜率 为 分子 除以 分母。
    截距 为 均y 减 斜率 乘 均x。
    返还 一组：斜率、截距。
完。

统计学_统计_直方图 接受 数据、桶数：
    若 做 计数 用 数据 等于 0 或者 桶数 小于或等于 0：
        返还 一组：（空）。
    完。
    可变 最小 为 数据 的 第 0 项。
    可变 最大 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 小于 最小：
            最小 为 数据 的 第 i 项。
        完。
        若 数据 的 第 i 项 大于 最大：
            最大 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 范围 为 最大 减 最小 加 1。
    可变 桶宽 为 范围 除以 桶数。
    若 桶宽 小于或等于 0：
        桶宽 为 1。
    完。
    可变 桶 为 一组：（空）。
    i 为 0。
    只要 i 小于 桶数：
        做 追加 用 桶、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 桶idx 为 (数据 的 第 i 项 减 最小) 除以 桶宽。
        若 桶idx 大于或等于 桶数：
            桶idx 为 桶数 减 1。
        完。
        桶 的 第 桶idx 项 为 桶 的 第 桶idx 项 加 1。
        i 为 i 加 1。
    完。
    返还 桶。
完。

统计_归一化 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 一组：（空）。
    完。
    可变 最小 为 数据 的 第 0 项。
    可变 最大 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 小于 最小：
            最小 为 数据 的 第 i 项。
        完。
        若 数据 的 第 i 项 大于 最大：
            最大 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 范围 为 最大 减 最小。
    若 范围 等于 0：
        范围 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 结果、(数据 的 第 i 项 减 最小) 乘 100 除以 范围。
        i 为 i 加 1。
    完。
    返还 结果。
完。

统计_皮尔逊 接受 x、y：
    可变 n 为 做 计数 用 x。
    若 做 计数 用 y 小于 n：
        n 为 做 计数 用 y。
    完。
    若 n 小于 2：
        返还 0。
    完。
    可变 均x 为 0。
    可变 均y 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均x 为 均x 加 x 的 第 i 项。
        均y 为 均y 加 y 的 第 i 项。
        i 为 i 加 1。
    完。
    均x 为 均x 除以 n。
    均y 为 均y 除以 n。
    可变 cov_sum 为 0。
    可变 sx2 为 0。
    可变 sy2 为 0。
    i 为 0。
    只要 i 小于 n：
        导数 为 x 的 第 i 项 减 均x。
        导数Y 为 y 的 第 i 项 减 均y。
        cov_sum 为 cov_sum 加 导数 乘 导数Y。
        sx2 为 sx2 加 导数 乘 导数。
        sy2 为 sy2 加 导数Y 乘 导数Y。
        i 为 i 加 1。
    完。
    denom 为 做 整数sqrt 用 sx2 乘 sy2。
    若 denom 等于 0：
        返还 0。
    完。
    返还 cov_sum 乘 1000 除以 denom。
完。

统计_分位数 接受 数据、百分位：
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 0。
    完。
    可变 副本 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 副本、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 最小idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 副本 的 第 j 项 小于 副本 的 第 最小idx 项：
                最小idx 为 j。
            完。
            j 为 j 加 1。
        完。
        暂 为 副本 的 第 i 项。
        副本 的 第 i 项 为 副本 的 第 最小idx 项。
        副本 的 第 最小idx 项 为 暂。
        i 为 i 加 1。
    完。
    索引 为 百分位 乘 (n 减 1) 除以 100。
    返还 副本 的 第 索引 项。
完。

统计学_统计_移动平均 接受 数据、窗口：
    n 为 做 计数 用 数据。
    若 n 小于 窗口：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 窗口：
        和 为 和 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    做 追加 用 结果、和 除以 窗口。
    i 为 窗口。
    只要 i 小于 n：
        和 为 和 加 数据 的 第 i 项 减 数据 的 第 i 减 窗口 项。
        做 追加 用 结果、和 除以 窗口。
        i 为 i 加 1。
    完。
    返还 结果。
完。

本章 收束。
