《世界模型》

引用《..·基础》。

世界状态_创建 接受 维度：
    可变 观察 为 一组：（空）。
    可变 潜在 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 观察、0。
        做 追加 用 潜在、0。
        i 为 i 加 1。
    完。
    返还 一组：观察、潜在、0、0、一组：（空）。
完。

世界状态_观察 接受 s：
    返还 s 的 第 0 项。
完。

世界状态_潜在 接受 s：
    返还 s 的 第 1 项。
完。

世界状态_奖励 接受 s：
    返还 s 的 第 2 项。
完。

世界状态_终止 接受 s：
    返还 s 的 第 3 项。
完。

世界状态_时间 接受 s：
    返还 s 的 第 4 项。
完。

动力学_创建 接受 潜在维度、动作维度：
    可变 A 为 一组：（空）。
    可变 B 为 一组：（空）。
    可变 偏置 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 潜在维度：
        可变 行A 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 潜在维度：
            若 i 等于 j：
                做 追加 用 行A、900。
            否则：
                做 追加 用 行A、0。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 A、行A。
        可变 行B 为 一组：（空）。
        j 为 0。
        只要 j 小于 动作维度：
            做 追加 用 行B、100。
            j 为 j 加 1。
        完。
        做 追加 用 B、行B。
        做 追加 用 偏置、0。
        i 为 i 加 1。
    完。
    返还 一组：A、B、偏置、潜在维度、动作维度。
完。

动力学_预测 接受 模型、潜在、动作：
    d 为 模型 的 第 3 项。
    可变 新潜在 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d：
        可变 数值 为 模型 的 第 2 项 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 d：
            数值 为 数值 加 模型 的 第 0 项 的 第 i 项 的 第 j 项 乘 潜在 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 模型 的 第 4 项：
            若 j 小于 做 计数 用 动作：
                数值 为 数值 加 模型 的 第 1 项 的 第 i 项 的 第 j 项 乘 动作 的 第 j 项 除以 1000。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 新潜在、数值。
        i 为 i 加 1。
    完。
    返还 新潜在。
完。

奖励预测 接受 潜在、权重、偏置：
    d 为 做 计数 用 潜在。
    可变 r 为 偏置。
    可变 i 为 0。
    只要 i 小于 d：
        若 i 小于 做 计数 用 权重：
            r 为 r 加 权重 的 第 i 项 乘 潜在 的 第 i 项 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

终止预测 接受 潜在、权重、阈值：
    r 为 做 奖励预测 用 潜在、权重、0。
    可变 信号值 为 500 加 r 除以 4。
    若 信号值 小于 0：
        信号值 为 0。
    完。
    若 信号值 大于 1000：
        信号值 为 1000。
    完。
    返还 信号值 大于 阈值。
完。

想象_rollout 接受 动力学、初始潜在、动作序列、奖励权重、奖励偏置：
    T 为 做 计数 用 动作序列。
    可变 轨迹 为 一组：（空）。
    可变 潜在 为 初始潜在。
    可变 总奖励 为 0。
    可变 t 为 0。
    只要 t 小于 T：
        新潜在 为 做 动力学_预测 用 动力学、潜在、动作序列 的 第 t 项。
        r 为 做 奖励预测 用 新潜在、奖励权重、奖励偏置。
        做 追加 用 轨迹、一组：新潜在、r。
        总奖励 为 总奖励 加 r。
        潜在 为 新潜在。
        t 为 t 加 1。
    完。
    返还 一组：轨迹、总奖励。
完。

CEM_规划 接受 动力学、初始潜在、动作维度、规划长度、种群、精英数、迭代数、奖励权重、奖励偏置：
    可变 CEM_随机态 为 11111。
    可变 均值 为 一组：（空）。
    可变 方差 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 规划长度 乘 动作维度：
        做 追加 用 均值、0。
        做 追加 用 方差、1000。
        i 为 i 加 1。
    完。
    可变 迭代 为 0。
    只要 迭代 小于 迭代数：
        可变 候选 为 一组：（空）。
        可变 奖励列表 为 一组：（空）。
        可变 p 为 0。
        只要 p 小于 种群：
            可变 动作序列 为 一组：（空）。
            可变 t 为 0。
            只要 t 小于 规划长度：
                可变 a 为 一组：（空）。
                可变 d 为 0。
                只要 d 小于 动作维度：
                    CEM_随机态 为 CEM_随机态 位异或 CEM_随机态 左移 13。
                    CEM_随机态 为 CEM_随机态 位异或 CEM_随机态 右移 7。
                    CEM_随机态 为 CEM_随机态 位异或 CEM_随机态 左移 17。
                    若 CEM_随机态 小于 0：
                        CEM_随机态 为 0 减 CEM_随机态。
                    完。
                    噪声 为 CEM_随机态 取余于 2000 减 1000。
                    索引 为 t 乘 动作维度 加 d。
                    数值 为 均值 的 第 索引 项 加 噪声 乘 方差 的 第 索引 项 除以 1000。
                    做 追加 用 a、数值。
                    d 为 d 加 1。
                完。
                做 追加 用 动作序列、a。
                t 为 t 加 1。
            完。
            结果 为 做 想象_rollout 用 动力学、初始潜在、动作序列、奖励权重、奖励偏置。
            做 追加 用 候选、动作序列。
            做 追加 用 奖励列表、结果 的 第 1 项。
            p 为 p 加 1。
        完。
        可变 索引 为 一组：（空）。
        i 为 0。
        只要 i 小于 种群：
            做 追加 用 索引、i。
            i 为 i 加 1。
        完。
        i 为 0。
        只要 i 小于 精英数：
            可变 j 为 i 加 1。
            只要 j 小于 种群：
                若 奖励列表 的 第 索引 的 第 j 项 项 大于 奖励列表 的 第 索引 的 第 i 项 项：
                    临时 为 索引 的 第 i 项。
                    索引 的 第 i 项 为 索引 的 第 j 项。
                    索引 的 第 j 项 为 临时。
                完。
                j 为 j 加 1。
            完。
            i 为 i 加 1。
        完。
        i 为 0。
        只要 i 小于 规划长度 乘 动作维度：
            均值 的 第 i 项 为 0。
            i 为 i 加 1。
        完。
        i 为 0。
        只要 i 小于 精英数：
            可变 t 为 0。
            只要 t 小于 规划长度：
                可变 d 为 0。
                只要 d 小于 动作维度：
                    索引 为 t 乘 动作维度 加 d。
                    均值 的 第 索引 项 为 均值 的 第 索引 项 加 候选 的 第 索引 的 第 i 项 项 的 第 t 项 的 第 d 项 除以 精英数。
                    d 为 d 加 1。
                完。
                t 为 t 加 1。
            完。
            i 为 i 加 1。
        完。
        迭代 为 迭代 加 1。
    完。
    可变 最优动作 为 一组：（空）。
    i 为 0。
    只要 i 小于 动作维度：
        做 追加 用 最优动作、均值 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 最优动作。
完。

世界_预测下一状态 接受 当前状态、动作、转移权重：
    可变 新状态 为 一组：（空）。
    维度 为 做 计数 用 当前状态。
    可变 i 为 0。
    只要 i 小于 维度：
        可变 数值 为 当前状态 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 动作：
            数值 为 数值 加 动作 的 第 j 项 乘 转移权重 的 第 i 乘 做 计数 用 动作 加 j 项 除以 1000。
            j 为 j 加 1。
        完。
        做 追加 用 新状态、数值。
        i 为 i 加 1。
    完。
    返还 新状态。
完。

世界_预测奖励 接受 状态、目标状态：
    可变 距离 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 状态：
        d 为 状态 的 第 i 项 减 目标状态 的 第 i 项。
        距离 为 距离 加 d 乘 d。
        i 为 i 加 1。
    完。
    返还 1000 减 距离。
完。

世界_展望 接受 当前状态、动作序列、转移权重：
    可变 状态 为 当前状态。
    可变 轨迹 为 一组：当前状态。
    可变 i 为 0。
    只要 i 小于 做 计数 用 动作序列：
        状态 为 做 世界_预测下一状态 用 状态、动作序列 的 第 i 项、转移权重。
        做 追加 用 轨迹、状态。
        i 为 i 加 1。
    完。
    返还 轨迹。
完。

世界_不确定性 接受 预测列表：
    n 为 做 计数 用 预测列表。
    若 n 小于 2：
        返还 0。
    完。
    维度 为 做 计数 用 预测列表 的 第 0 项。
    可变 总方差 为 0。
    可变 d 为 0。
    只要 d 小于 维度：
        可变 均值 为 0。
        可变 i 为 0。
        只要 i 小于 n：
            均值 为 均值 加 预测列表 的 第 i 项 的 第 d 项。
            i 为 i 加 1。
        完。
        均值 为 均值 除以 n。
        可变 方差 为 0。
        i 为 0。
        只要 i 小于 n：
            差值 为 预测列表 的 第 i 项 的 第 d 项 减 均值。
            方差 为 方差 加 差值 乘 差值。
            i 为 i 加 1。
        完。
        总方差 为 总方差 加 方差 除以 n。
        d 为 d 加 1。
    完。
    返还 总方差。
完。

世界_好奇心奖励 接受 预测状态、实际状态：
    可变 误差 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测状态：
        d 为 预测状态 的 第 i 项 减 实际状态 的 第 i 项。
        误差 为 误差 加 d 乘 d。
        i 为 i 加 1。
    完。
    返还 误差。
完。

本章 收束。
