《缩放定律》

引用《..·基础》。

引用《..·算法·数学扩展》。

引用《..·数学·工具》。

缩放_整数ln 接受 x_千分：
    若 x_千分 小于或等于 0：
        返还 0 减 10000。
    完。
    可变 结果 为 0。
    可变 v 为 x_千分。
    可变 缩放 为 0。
    只要 v 大于 2000：
        v 为 v 除以 2。
        缩放 为 缩放 加 693。
    完。
    只要 v 小于 500：
        v 为 v 乘 2。
        缩放 为 缩放 减 693。
    完。
    数目 为 v 减 1000。
    den 为 v 加 1000。
    若 den 等于 0：
        返还 缩放。
    完。
    t 为 数目 乘 1000 除以 den。
    t2 为 t 乘 t 除以 1000。
    t3 为 t2 乘 t 除以 1000。
    结果 为 2 乘 (t 加 t3 除以 3)。
    返还 结果 加 缩放。
完。

缩放_整数exp 接受 x_千分：
    若 x_千分 大于 7000：
        返还 0x7FFFFFFF。
    完。
    若 x_千分 小于 0 减 7000：
        返还 0。
    完。
    x 为 x_千分。
    x2 为 x 乘 x 除以 1000。
    x3 为 x2 乘 x 除以 1000。
    x4 为 x3 乘 x 除以 1000。
    返还 1000 加 x 加 x2 除以 2 加 x3 除以 6 加 x4 除以 24。
完。

缩放_模型损失 接受 N、N_c、α_N_千分：
    若 N 等于 0：
        返还 0x7FFFFFFF。
    完。
    比率 为 N_c 乘 1000 除以 N。
    ln_比率 为 做 缩放_整数ln 用 比率。
    指数 为 α_N_千分 乘 ln_比率 除以 1000。
    返还 做 缩放_整数exp 用 指数。
完。

缩放_数据损失 接受 D、D_c、α_D_千分：
    若 D 等于 0：
        返还 0x7FFFFFFF。
    完。
    比率 为 D_c 乘 1000 除以 D。
    ln_比率 为 做 缩放_整数ln 用 比率。
    指数 为 α_D_千分 乘 ln_比率 除以 1000。
    返还 做 缩放_整数exp 用 指数。
完。

缩放_联合损失 接受 N、D、N_c、D_c、α_N、α_D、不可约损失：
    L_N 为 做 缩放_模型损失 用 N、N_c、α_N。
    L_D 为 做 缩放_数据损失 用 D、D_c、α_D。
    返还 不可约损失 加 L_N 加 L_D。
完。

Chinchilla_最优参数 接受 计算预算_FLOPs：
    N 为 做 整数sqrt 用 计算预算_FLOPs。
    返还 N。
完。

Chinchilla_最优数据 接受 参数数：
    返还 参数数 乘 20。
完。

龙猫_浮点运算 接受 参数数、数据量：
    返还 6 乘 参数数 乘 数据量。
完。

Transformer_参数数 接受 层数、隐藏维度、词汇量：
    返还 12 乘 层数 乘 隐藏维度 乘 隐藏维度 加 词汇量 乘 隐藏维度。
完。

Transformer_FLOPs_每token 接受 参数数：
    返还 2 乘 参数数。
完。

训练_总FLOPs 接受 参数数、token数：
    返还 6 乘 参数数 乘 token数。
完。

批大小_临界 接受 噪声尺度、当前损失：
    若 当前损失 等于 0：
        返还 噪声尺度。
    完。
    返还 噪声尺度 乘 1000 除以 当前损失。
完。

批大小_效率 接受 实际批大小、临界批大小：
    若 临界批大小 等于 0：
        返还 500。
    完。
    返还 1000 乘 1000 除以 (1000 加 实际批大小 乘 1000 除以 临界批大小)。
完。

学习率_线性缩放 接受 基础lr、基础批大小、实际批大小：
    返还 基础lr 乘 实际批大小 除以 基础批大小。
完。

学习率_平方根缩放 接受 基础lr、基础批大小、实际批大小：
    返还 基础lr 乘 做 整数sqrt 用 实际批大小 乘 1000 除以 做 整数sqrt 用 基础批大小 乘 1000。
完。

μP_学习率 接受 基础lr、基础宽度、目标宽度：
    若 目标宽度 等于 0：
        返还 基础lr。
    完。
    返还 基础lr 乘 基础宽度 除以 目标宽度。
完。

μP_初始化缩放 接受 基础宽度、目标宽度：
    若 目标宽度 等于 0：
        返还 1000。
    完。
    返还 1000 乘 做 整数sqrt 用 基础宽度 乘 1000 除以 做 整数sqrt 用 目标宽度 乘 1000。
完。

涌现_阈值检查 接受 模型大小、阈值列表：
    可变 能力 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 阈值列表：
        若 模型大小 大于或等于 阈值列表 的 第 i 项：
            做 追加 用 能力、1。
        否则：
            做 追加 用 能力、0。
        完。
        i 为 i 加 1。
    完。
    返还 能力。
完。

GPU小时_估算 接受 总FLOPs、GPU_TFLOPS：
    若 GPU_TFLOPS 等于 0：
        返还 0。
    完。
    返还 总FLOPs 除以 (GPU_TFLOPS 乘 3600)。
完。

训练成本_美元 接受 GPU小时、每小时美元：
    返还 GPU小时 乘 每小时美元 除以 1000。
完。

数据_有效token 接受 总token、质量分数：
    返还 总token 乘 质量分数 除以 1000。
完。

数据_去重收益 接受 原始大小、去重率：
    去重后 为 原始大小 乘 (1000 减 去重率) 除以 1000。
    返还 去重后 乘 (1000 加 去重率 除以 2) 除以 1000。
完。

推理_延迟估计 接受 参数数、带宽_GBs、批大小：
    若 带宽_GBs 等于 0 或者 批大小 等于 0：
        返还 0。
    完。
    返还 参数数 乘 2 除以 (带宽_GBs 乘 批大小)。
完。

推理_吞吐量 接受 参数数、GPU_TFLOPS、批大小：
    若 参数数 等于 0：
        返还 0。
    完。
    返还 GPU_TFLOPS 乘 批大小 乘 1000 除以 (2 乘 参数数)。
完。

推荐_模型配置 接受 目标参数数：
    可变 d 为 做 整数sqrt 用 目标参数数 除以 12。
    可变 L 为 d 除以 100。
    若 L 小于 2：
        L 为 2。
    完。
    若 L 大于 96：
        L 为 96。
    完。
    d 为 做 整数sqrt 用 目标参数数 除以 (12 乘 L)。
    d 为 (d 加 63) 除以 64 乘 64。
    返还 一组：L、d、12 乘 L 乘 d 乘 d。
完。

缩放_预训练token 接受 参数数：
    返还 参数数 乘 20。
完。

缩放_微调token 接受 参数数：
    返还 参数数 乘 20 除以 50。
完。

缩放_RLHF样本 接受 参数数：
    基础 为 50000。
    返还 基础 加 参数数 除以 1000000。
完。

内存_模型参数 接受 参数数、精度字节：
    返还 参数数 乘 精度字节。
完。

内存_训练总量 接受 参数数：
    返还 参数数 乘 16。
完。

内存_KV缓存 接受 批大小、层数、头数、头维度、序列长度：
    返还 2 乘 批大小 乘 层数 乘 头数 乘 头维度 乘 序列长度 乘 2。
完。

内存_激活 接受 批大小、层数、隐藏维度、序列长度：
    返还 批大小 乘 层数 乘 隐藏维度 乘 序列长度 乘 2 乘 10。
完。

推荐_并行策略 接受 参数数、GPU数、每GPU内存GB：
    模型内存GB 为 参数数 乘 16 除以 1073741824。
    若 模型内存GB 小于或等于 每GPU内存GB：
        返还 一组：GPU数、1、1。
    完。
    若 模型内存GB 小于或等于 每GPU内存GB 乘 8：
        可变 tp 为 模型内存GB 除以 每GPU内存GB 加 1。
        若 tp 大于 8：
            tp 为 8。
        完。
        可变 dp 为 GPU数 除以 tp。
        若 dp 小于 1：
            dp 为 1。
        完。
        返还 一组：dp、tp、1。
    完。
    pp 为 模型内存GB 除以 (每GPU内存GB 乘 8) 加 1。
    可变 tp 为 8。
    可变 dp 为 GPU数 除以 (tp 乘 pp)。
    若 dp 小于 1：
        dp 为 1。
    完。
    返还 一组：dp、tp、pp。
完。

训练_墙钟时间 接受 总FLOPs、GPU数、每GPU_TFLOPS、利用率千分：
    若 GPU数 等于 0 或者 每GPU_TFLOPS 等于 0 或者 利用率千分 等于 0：
        返还 0。
    完。
    有效TFLOPS 为 GPU数 乘 每GPU_TFLOPS 乘 利用率千分 除以 1000。
    返还 总FLOPs 除以 (有效TFLOPS 乘 3600)。
完。

训练_估算天数 接受 GPU小时、GPU数：
    若 GPU数 等于 0：
        返还 0。
    完。
    返还 GPU小时 除以 (GPU数 乘 24)。
完。

缩放_双幂损失 接受 C、α、β、γ、δ、ε：
    若 C 等于 0：
        返还 0x7FFFFFFF。
    完。
    项1 为 α 乘 1000 除以 (C 乘 β 除以 1000)。
    项2 为 δ 乘 1000 除以 (C 乘 ε 除以 1000)。
    返还 项1 加 γ 加 项2。
完。

下游_准确率预测 接受 预训练损失、缩放系数、偏移：
    若 预训练损失 等于 0：
        返还 偏移。
    完。
    返还 缩放系数 乘 1000 除以 预训练损失 加 偏移。
完。

数据混合_权重 接受 领域数、领域大小列表、领域质量列表：
    可变 权重列表 为 一组：（空）。
    可变 总权重 为 0。
    可变 i 为 0。
    只要 i 小于 领域数：
        ln_s 为 做 缩放_整数ln 用 领域大小列表 的 第 i 项。
        ln_q 为 做 缩放_整数ln 用 领域质量列表 的 第 i 项。
        指数 为 ln_s 乘 700 除以 1000 加 ln_q 乘 300 除以 1000。
        w 为 做 缩放_整数exp 用 指数。
        做 追加 用 权重列表、w。
        总权重 为 总权重 加 w。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 领域数：
        若 总权重 大于 0：
            权重列表 的 第 i 项 为 权重列表 的 第 i 项 乘 1000 除以 总权重。
        完。
        i 为 i 加 1。
    完。
    返还 权重列表。
完。

计算效率 接受 实际性能、参数数：
    若 参数数 等于 0：
        返还 0。
    完。
    返还 实际性能 乘 1000000 除以 参数数。
完。

数据效率 接受 实际性能、训练token数：
    若 训练token数 等于 0：
        返还 0。
    完。
    返还 实际性能 乘 1000000 除以 训练token数。
完。

FLOPs效率 接受 实际性能、总FLOPs：
    若 总FLOPs 等于 0：
        返还 0。
    完。
    返还 实际性能 乘 1000000 除以 总FLOPs。
完。

生成_训练配方 接受 目标参数数、GPU数、每GPU_TFLOPS、每GPU内存GB：
    config 为 做 推荐_模型配置 用 目标参数数。
    层数 为 config 的 第 0 项。
    隐藏维度 为 config 的 第 1 项。
    实际参数 为 config 的 第 2 项。
    最优数据 为 做 Chinchilla_最优数据 用 实际参数。
    总flops 为 做 训练_总FLOPs 用 实际参数、最优数据。
    并行 为 做 推荐_并行策略 用 实际参数、GPU数、每GPU内存GB。
    gpu小时 为 做 训练_墙钟时间 用 总flops、GPU数、每GPU_TFLOPS、400。
    天数 为 做 训练_估算天数 用 gpu小时、GPU数。
    做 打印 用 「═══ 训练配方 ═══」。
    做 打印 用 「 目标参数: 」 加 做 转字符串 用 目标参数数。
    做 打印 用 「 实际参数: 」 加 做 转字符串 用 实际参数。
    做 打印 用 「 层数: 」 加 做 转字符串 用 层数。
    做 打印 用 「 隐藏维度: 」 加 做 转字符串 用 隐藏维度。
    做 打印 用 「 最优数据: 」 加 做 转字符串 用 最优数据 加 「 tokens」。
    做 打印 用 「 总FLOPs: 」 加 做 转字符串 用 总flops。
    做 打印 用 「 并行策略: DP=」 加 做 转字符串 用 并行 的 第 0 项 加 「 TP=」 加 做 转字符串 用 并行 的 第 1 项 加 「 PP=」 加 做 转字符串 用 并行 的 第 2 项。
    做 打印 用 「 GPU小时: 」 加 做 转字符串 用 gpu小时。
    做 打印 用 「 预计天数: 」 加 做 转字符串 用 天数。
    返还 一组：实际参数、最优数据、总flops、gpu小时、天数。
完。

缩放_比较报告 接受 名称列表、参数列表、损失列表：
    做 打印 用 「╔══════════════════════════════════════╗」。
    做 打印 用 「║ 缩放定律比较 ║」。
    做 打印 用 「╠══════════════════════════════════════╣」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 名称列表：
        效率 为 做 计算效率 用 1000 减 损失列表 的 第 i 项、参数列表 的 第 i 项。
        做 打印 用 「║ 」 加 名称列表 的 第 i 项 加 「: N=」 加 做 转字符串 用 参数列表 的 第 i 项 加 「 L=」 加 做 转字符串 用 损失列表 的 第 i 项 加 「 E=」 加 做 转字符串 用 效率。
        i 为 i 加 1。
    完。
    做 打印 用 「╚══════════════════════════════════════╝」。
完。

缩放_幂律预测 接受 当前参数、当前损失、目标参数、指数：
    若 当前参数 等于 0：
        返还 当前损失。
    完。
    可变 比率_分子 为 1000。
    可变 比率_分母 为 1000。
    可变 i 为 0。
    只要 i 小于 指数：
        比率_分子 为 比率_分子 乘 当前参数。
        比率_分母 为 比率_分母 乘 目标参数。
        i 为 i 加 1。
    完。
    若 比率_分母 等于 0：
        返还 当前损失。
    完。
    返还 当前损失 乘 比率_分子 除以 比率_分母。
完。

缩放_所需参数 接受 当前参数、当前损失、目标损失：
    若 目标损失 大于或等于 当前损失：
        返还 当前参数。
    完。
    比率 为 当前损失 乘 1000 除以 目标损失。
    返还 当前参数 乘 比率 乘 比率 除以 1000000。
完。

缩放_计算代价 接受 参数量：
    返还 参数量 乘 6。
完。

缩放_最优批大小 接受 参数量：
    若 参数量 大于 10000000：
        返还 4096。
    完。
    若 参数量 大于 1000000：
        返还 2048。
    完。
    若 参数量 大于 100000：
        返还 1024。
    完。
    返还 512。
完。

缩放_最优学习率 接受 参数量：
    若 参数量 大于 10000000：
        返还 10。
    完。
    若 参数量 大于 1000000：
        返还 30。
    完。
    若 参数量 大于 100000：
        返还 100。
    完。
    返还 300。
完。

缩放_涌现阈值 接受 当前参数、能力列表：
    可变 涌现数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 能力列表：
        若 当前参数 大于或等于 能力列表 的 第 i 项：
            涌现数 为 涌现数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 涌现数。
完。

chinchilla_最优数据量 接受 参数量：
    返还 参数量 乘 20。
完。

chinchilla_是否数据不足 接受 参数量、数据量：
    最优 为 做 chinchilla_最优数据量 用 参数量。
    若 数据量 小于 最优 除以 2：
        返还 「严重不足」。
    完。
    若 数据量 小于 最优：
        返还 「数据偏少」。
    完。
    返还 「数据充足」。
完。

chinchilla_推荐配置 接受 计算预算：
    参数 为 计算预算 除以 12。
    数据 为 参数 乘 20。
    返还 一组：参数、数据。
完。

可变 _缩放_预测次数 为 0。

可变 _缩放_最大参数量 为 0。

缩放_记录预测 接受 参数量：
    _缩放_预测次数 为 _缩放_预测次数 加 1。
    若 参数量 大于 _缩放_最大参数量：
        _缩放_最大参数量 为 参数量。
    完。
完。

缩放_规模评估 接受 零参：
    若 _缩放_最大参数量 大于 100000000：
        返还 「超大规模(>100M)」。
    完。
    若 _缩放_最大参数量 大于 10000000：
        返还 「大规模(>10M)」。
    完。
    若 _缩放_最大参数量 大于 1000000：
        返还 「中等规模(>1M)」。
    完。
    返还 「小规模」。
完。

缩放_重置 接受 零参：
    _缩放_预测次数 为 0。
    _缩放_最大参数量 为 0。
完。

缩放_效率曲线 接受 参数列表、损失列表：
    可变 最佳效率 为 0。
    可变 最佳idx 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数列表：
        若 参数列表 的 第 i 项 大于 0：
            效率 为 (1000 减 损失列表 的 第 i 项) 乘 1000 除以 参数列表 的 第 i 项。
            若 效率 大于 最佳效率：
                最佳效率 为 效率。
                最佳idx 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 一组：最佳idx、最佳效率。
完。

本章 收束。
