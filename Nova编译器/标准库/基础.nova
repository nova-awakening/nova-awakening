《基础》

追加 接受 列表、值：
    做 插入 用 列表、值。
完。

字符串拼接 接受 a、b：
    返还 做 拼接 用 a、b。
完。

字符串等于 接受 a、b：
    返还 a 等于 b。
完。

基础_字符串相等 接受 a、b：
    返还 a 等于 b。
完。

基础_字符串等于_指针 接受 a、b：
    返还 a 等于 b。
完。

字符码 接受 s、i：
    返还 做 编码 用 做 子串 用 s、i、1。
完。

字典设 接受 d、k、v：
    做 字典设置 用 d、k、v。
完。

字典取 接受 d、k：
    返还 做 字典获取 用 d、k。
完。

字典键列表 接受 d：
    返还 做 字典键 用 d。
完。

字符串长度 接受 s：
    返还 做 计数 用 s。
完。

字符串查找 接受 s、目标：
    n 为 做 计数 用 s。
    m 为 做 计数 用 目标。
    若 m 大于 n：
        返还 负 1。
    完。
    可变 i 为 0。
    只要 i 小于或等于 n 减 m：
        若 做 子串 用 s、i、m 等于 目标：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

从字符码 接受 码：
    返还 做 字符 用 码。
完。

切片 接受 列表、_起、_止：
    返还 做 基础_切片 用 列表、_起、_止。
完。

转整数 接受 值：
    若 做 是整数 用 值：
        返还 值。
    完。
    返还 值 加 0。
完。

获取环境 接受 名称：
    返还 「」。
完。

随机舍入_对数2 接受 n：
    返还 做 对数2 用 n。
完。

求和 接受 列表：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        总 为 总 加 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

位计数 接受 n：
    可变 c 为 0。
    可变 v 为 n。
    只要 v 不等于 0：
        c 为 c 加 (v 位与 1)。
        v 为 v 右移 1。
    完。
    返还 c。
完。

是标点 接受 c：
    返还 c 大于或等于 33 并且 c 小于或等于 字符_斜杠 或者 c 大于或等于 字符_冒号 并且 c 小于或等于 64 或者 c 大于或等于 字符_左方括 并且 c 小于或等于 96 或者 c 大于或等于 字符_左花括 并且 c 小于或等于 126。
完。

基础_整数sqrt 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

对数2 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 r 为 0。
    可变 v 为 n。
    只要 v 大于 1：
        v 为 v 除以 2。
        r 为 r 加 1。
    完。
    返还 r。
完。

指数_定点 接受 x：
    若 x 大于 6000：
        返还 1000000。
    完。
    若 x 小于 负 6000：
        返还 1。
    完。
    可变 基数 为 1000 加 x 除以 4。
    若 基数 小于 1：
        基数 为 1。
    完。
    可变 e 为 基数 乘 基数 除以 1000。
    e 为 e 乘 e 除以 1000。
    返还 e。
完。

S形_定点 接受 x：
    若 x 大于 6000：
        返还 1000。
    完。
    若 x 小于 负 6000：
        返还 0。
    完。
    可变 r 为 500 加 x 除以 4。
    若 r 小于 10：
        r 为 10。
    完。
    若 r 大于 990：
        r 为 990。
    完。
    返还 r。
完。

双曲正切_定点 接受 x：
    若 x 大于 3000：
        返还 1000。
    完。
    若 x 小于 负 3000：
        返还 负 1000。
    完。
    s 为 做 S形_定点 用 x 乘 2。
    返还 s 乘 2 减 1000。
完。

折叠 接受 列表、初始、fn：
    可变 准确率 为 初始。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        准确率 为 做 fn 用 准确率、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 准确率。
完。

基础_列表最大 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 m 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 大于 m：
            m 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 m。
完。

基础_列表最小 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 m 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 m：
            m 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 m。
完。

复制列表 接受 列表：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 r、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

基础_拉链 接受 a、b、fn：
    _ca 为 做 计数 用 a。
    _cb 为 做 计数 用 b。
    可变 n 为 _ca。
    若 _cb 小于 n：
        n 为 _cb。
    完。
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 r、做 fn 用 a 的 第 i 项、b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

可变 __基础_测试通过 为 0。

可变 __基础_测试失败 为 0。

验证 接受 条件、名称：
    若 条件：
        __基础_测试通过 为 __基础_测试通过 加 1。
    否则：
        __基础_测试失败 为 __基础_测试失败 加 1。
        做 打印 用 「FAIL: 」 加 名称。
    完。
完。

基础_测试报告 接受 零参：
    做 打印 用 「通过: 」 加 做 转字符串 用 __基础_测试通过 加 「/」 加 做 转字符串 用 __基础_测试通过 加 __基础_测试失败。
    若 __基础_测试失败 等于 0：
        做 打印 用 「全部通过!」。
    完。
    返还 __基础_测试失败。
完。

基础_冒泡排序 接受 列表：
    n 为 做 计数 用 列表。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 交换 为 假。
        可变 j 为 0。
        只要 j 小于 n 减 i 减 1：
            若 列表 的 第 j 项 大于 列表 的 第 j 加 1 项：
                临 为 列表 的 第 j 项。
                列表 的 第 j 项 为 列表 的 第 j 加 1 项。
                列表 的 第 j 加 1 项 为 临。
                交换 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 交换：
            返还。
        完。
        i 为 i 加 1。
    完。
完。

基础_插入排序 接受 列表：
    n 为 做 计数 用 列表。
    可变 i 为 1。
    只要 i 小于 n：
        键 为 列表 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 列表 的 第 j 项 大于 键：
            列表 的 第 j 加 1 项 为 列表 的 第 j 项。
            j 为 j 减 1。
        完。
        列表 的 第 j 加 1 项 为 键。
        i 为 i 加 1。
    完。
完。

基础_选择排序 接受 列表：
    n 为 做 计数 用 列表。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 最小idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 列表 的 第 j 项 小于 列表 的 第 最小idx 项：
                最小idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最小idx 不等于 i：
            临 为 列表 的 第 i 项。
            列表 的 第 i 项 为 列表 的 第 最小idx 项。
            列表 的 第 最小idx 项 为 临。
        完。
        i 为 i 加 1。
    完。
完。

基础_快速排序 接受 列表、低、高：
    若 低 大于或等于 高：
        返还。
    完。
    基准 为 列表 的 第 高 项。
    可变 i 为 低。
    可变 j 为 低。
    只要 j 小于 高：
        若 列表 的 第 j 项 小于 基准：
            临 为 列表 的 第 i 项。
            列表 的 第 i 项 为 列表 的 第 j 项。
            列表 的 第 j 项 为 临。
            i 为 i 加 1。
        完。
        j 为 j 加 1。
    完。
    临2 为 列表 的 第 i 项。
    列表 的 第 i 项 为 列表 的 第 高 项。
    列表 的 第 高 项 为 临2。
    做 基础_快速排序 用 列表、低、i 减 1。
    做 基础_快速排序 用 列表、i 加 1、高。
完。

基础_线性搜索 接受 列表、目标：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 等于 目标：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

基础_二分搜索 接受 列表、目标：
    可变 低 为 0。
    可变 高 为 做 计数 用 列表 减 1。
    只要 低 小于或等于 高：
        中 为 (低 加 高) 除以 2。
        若 列表 的 第 中 项 等于 目标：
            返还 中。
        完。
        若 列表 的 第 中 项 小于 目标：
            低 为 中 加 1。
        否则：
            高 为 中 减 1。
        完。
    完。
    返还 负 1。
完。

基础_哈希_整数 接受 值：
    可变 h 为 值。
    h 为 h 位异或 h 右移 16。
    h 为 h 乘 0x45d9f3b。
    h 为 h 位异或 h 右移 16。
    返还 h 位与 0x7FFFFFFF。
完。

基础_哈希_字符串 接受 s：
    可变 h 为 5381。
    可变 i 为 0。
    只要 i 小于 做 计数 用 s：
        h 为 h 乘 33 加 做 字符码 用 s、i。
        i 为 i 加 1。
    完。
    返还 h 位与 0x7FFFFFFF。
完。

基础_哈希_FNV 接受 数据：
    可变 h 为 0x811c9dc5。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        h 为 h 位异或 数据 的 第 i 项。
        h 为 h 乘 0x01000193。
        i 为 i 加 1。
    完。
    返还 h 位与 0x7FFFFFFF。
完。

基础_填充 接受 列表、值、数量：
    可变 i 为 0。
    只要 i 小于 数量：
        做 追加 用 列表、值。
        i 为 i 加 1。
    完。
完。

基础_复制 接受 源：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 源：
        做 追加 用 结果、源 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

基础_反转 接受 列表：
    n 为 做 计数 用 列表。
    可变 i 为 0。
    只要 i 小于 n 除以 2：
        临 为 列表 的 第 i 项。
        列表 的 第 i 项 为 列表 的 第 n 减 1 减 i 项。
        列表 的 第 n 减 1 减 i 项 为 临。
        i 为 i 加 1。
    完。
完。

基础_去重 接受 列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        可变 存在 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 结果：
            若 结果 的 第 j 项 等于 列表 的 第 i 项：
                存在 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 存在：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

基础_切片 接受 列表、起、_止：
    可变 结果 为 一组：（空）。
    可变 i 为 起。
    只要 i 小于 _止 并且 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

基础_绝对值 接受 x：
    若 x 小于 0：
        返还 0 减 x。
    完。
    返还 x。
完。

基础_最大 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

基础_最小 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

基础_钳制 接受 值、最小、最大：
    若 值 小于 最小：
        返还 最小。
    完。
    若 值 大于 最大：
        返还 最大。
    完。
    返还 值。
完。

基础_最大公约数 接受 a、b：
    可变 x 为 a。
    可变 y 为 b。
    只要 y 不等于 0：
        临 为 y。
        y 为 x 取余于 y。
        x 为 临。
    完。
    返还 x。
完。

基础_最小公倍数 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 0。
    完。
    返还 a 乘 b 除以 做 基础_最大公约数 用 a、b。
完。

基础_幂 接受 底数、指数：
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 指数：
        结果 为 结果 乘 底数。
        i 为 i 加 1。
    完。
    返还 结果。
完。

基础_是素数 接受 n：
    若 n 小于 2：
        返还 假。
    完。
    若 n 小于 4：
        返还 真。
    完。
    若 n 取余于 2 等于 0 或者 n 取余于 3 等于 0：
        返还 假。
    完。
    可变 i 为 5。
    只要 i 乘 i 小于或等于 n：
        若 n 取余于 i 等于 0 或者 n 取余于 (i 加 2) 等于 0：
            返还 假。
        完。
        i 为 i 加 6。
    完。
    返还 真。
完。

可变 _基础_排序次数 为 0。

可变 _基础_搜索次数 为 0。

可变 _基础_哈希次数 为 0。

基础_记录排序 接受 零参：
    _基础_排序次数 为 _基础_排序次数 加 1。
完。

基础_记录搜索 接受 零参：
    _基础_搜索次数 为 _基础_搜索次数 加 1。
完。

基础_记录哈希 接受 零参：
    _基础_哈希次数 为 _基础_哈希次数 加 1。
完。

基础_性能评估 接受 零参：
    若 _基础_排序次数 大于 10000：
        返还 「高频排序(检查算法选择)」。
    完。
    若 _基础_搜索次数 大于 100000：
        返还 「高频搜索(考虑索引)」。
    完。
    返还 「基础操作正常」。
完。

本章 收束。
