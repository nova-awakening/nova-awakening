《协议栈》

引用《..·基础》。

TCP_时间等 为 7。

TCP_关闭等 为 8。

TCP_最终确认 为 9。

可变 tcp_状态 为 TCP_关闭。

可变 tcp_序号 为 0。

可变 tcp_确认号 为 0。

TCP_转换 接受 事件：
    若 tcp_状态 等于 TCP_关闭 并且 事件 等于 1：
        tcp_状态 为 TCP_SYN已发。
        返还 真。
    完。
    若 tcp_状态 等于 TCP_关闭 并且 事件 等于 2：
        tcp_状态 为 TCP_监听。
        返还 真。
    完。
    若 tcp_状态 等于 TCP_监听 并且 事件 等于 3：
        tcp_状态 为 TCP_SYN已收。
        返还 真。
    完。
    若 tcp_状态 等于 TCP_SYN已发 并且 事件 等于 4：
        tcp_状态 为 TCP_已建立。
        返还 真。
    完。
    若 tcp_状态 等于 TCP_SYN已收 并且 事件 等于 5：
        tcp_状态 为 TCP_已建立。
        返还 真。
    完。
    若 tcp_状态 等于 TCP_已建立 并且 事件 等于 6：
        tcp_状态 为 TCP_关闭等。
        返还 真。
    完。
    若 tcp_状态 等于 TCP_已建立 并且 事件 等于 7：
        tcp_状态 为 TCP_FIN等1。
        返还 真。
    完。
    若 tcp_状态 等于 TCP_FIN等1 并且 事件 等于 5：
        tcp_状态 为 TCP_FIN等2。
        返还 真。
    完。
    若 tcp_状态 等于 TCP_关闭等 并且 事件 等于 5：
        tcp_状态 为 TCP_最终确认。
        返还 真。
    完。
    若 tcp_状态 等于 TCP_最终确认 并且 事件 等于 5：
        tcp_状态 为 TCP_关闭。
        返还 真。
    完。
    返还 假。
完。

TCP_重置 接受 零参：
    tcp_状态 为 TCP_关闭。
    tcp_序号 为 0。
    tcp_确认号 为 0。
完。

HTTP_构建请求 接受 方法哈希、路径哈希、主机哈希：
    返还 一组：方法哈希、路径哈希、主机哈希。
完。

HTTP_设置正文 接受 请求、正文哈希、长度：
    请求 的 第 3 项 为 长度。
    请求 的 第 4 项 为 正文哈希。
    返还 请求。
完。

HTTP_构建响应 接受 状态码、内容长度、正文哈希：
    返还 一组：状态码、内容长度、正文哈希、0。
完。

HTTP_是成功 接受 响应：
    返还 响应 的 第 0 项 大于或等于 200 并且 响应 的 第 0 项 小于 300。
完。

HTTP_是重定向 接受 响应：
    返还 响应 的 第 0 项 大于或等于 300 并且 响应 的 第 0 项 小于 400。
完。

HTTP_是客户端错误 接受 响应：
    返还 响应 的 第 0 项 大于或等于 400 并且 响应 的 第 0 项 小于 500。
完。

HTTP_是服务端错误 接受 响应：
    返还 响应 的 第 0 项 大于或等于 500。
完。

WS_文本 为 1。

WS_二进制 为 2。

WS_关闭 为 8。

WS_心跳请求 为 9。

WS_心跳响应 为 10。

WS_构建帧 接受 opcode、数据、长度：
    返还 一组：opcode、0、长度、数据。
完。

WS_是控制帧 接受 帧：
    返还 帧 的 第 0 项 大于或等于 8。
完。

WS_负载长度 接受 帧：
    返还 帧 的 第 2 项。
完。

协议_写16 接受 包、偏移、值：
    若 偏移 大于或等于 0 并且 偏移 加 1 小于 做 计数 用 包：
        包 的 第 偏移 项 为 值 右移 8 位与 0xFF。
        包 的 第 偏移 加 1 项 为 值 位与 0xFF。
    完。
完。

协议_校验和16 接受 数据：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 高 为 数据 的 第 i 项。
        可变 低 为 0。
        若 i 加 1 小于 做 计数 用 数据：
            低 为 数据 的 第 i 加 1 项。
        完。
        总和 为 总和 加 (高 左移 8 位或 低)。
        只要 总和 大于 0xFFFF：
            总和 为 (总和 位与 0xFFFF) 加 (总和 右移 16)。
        完。
        i 为 i 加 2。
    完。
    只要 总和 大于 0xFFFF：
        总和 为 (总和 位与 0xFFFF) 加 (总和 右移 16)。
    完。
    返还 按位取反 总和 位与 0xFFFF。
完。

协议_刷新校验和 接受 包、偏移：
    做 协议_写16 用 包、偏移、0。
    校验 为 做 协议_校验和16 用 包。
    做 协议_写16 用 包、偏移、校验。
    返还 校验。
完。

协议_校验有效 接受 包：
    返还 做 协议_校验和16 用 包 等于 0。
完。

可变 端口_分配表 为 一组：（空）。

可变 端口_服务 为 一组：（空）。

端口_分配 接受 端口、服务哈希：
    做 追加 用 端口_分配表、端口。
    做 追加 用 端口_服务、服务哈希。
完。

端口_查找 接受 端口：
    可变 i 为 0。
    只要 i 小于 做 计数 用 端口_分配表：
        若 端口_分配表 的 第 i 项 等于 端口：
            返还 端口_服务 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

端口_可用 接受 端口：
    可变 i 为 0。
    只要 i 小于 做 计数 用 端口_分配表：
        若 端口_分配表 的 第 i 项 等于 端口：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

端口_释放 接受 端口：
    可变 新端口 为 一组：（空）。
    可变 新服务 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 端口_分配表：
        若 端口_分配表 的 第 i 项 不等于 端口：
            做 追加 用 新端口、端口_分配表 的 第 i 项。
            做 追加 用 新服务、端口_服务 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    端口_分配表 为 新端口。
    端口_服务 为 新服务。
完。

端口_已分配数 接受 零参：
    返还 做 计数 用 端口_分配表。
完。

数据_重组 接受 片列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 片列表：
        可变 j 为 0。
        只要 j 小于 做 计数 用 片列表 的 第 i 项：
            做 追加 用 结果、片列表 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

TCP_结束 为 0x01。

TCP_同步 为 0x02。

TCP_重置 为 0x04。

TCP_推送 为 0x08。

TCP_确认 为 0x10。

TCP_紧急 为 0x20。

TCP_创建连接 接受 零参：
    返还 一组：0、0、0、0、一组：（空）。
完。

TCP_设置端口 接受 连接、源、目标：
    连接 的 第 0 项 为 源。
    连接 的 第 1 项 为 目标。
完。

TCP_构建头 接受 连接、标志：
    可变 头 为 一组：（空）。
    做 追加 用 头、连接 的 第 0 项 右移 8 位与 0xFF。
    做 追加 用 头、连接 的 第 0 项 位与 0xFF。
    做 追加 用 头、连接 的 第 1 项 右移 8 位与 0xFF。
    做 追加 用 头、连接 的 第 1 项 位与 0xFF。
    做 追加 用 头、连接 的 第 2 项 右移 24 位与 0xFF。
    做 追加 用 头、连接 的 第 2 项 右移 16 位与 0xFF。
    做 追加 用 头、连接 的 第 2 项 右移 8 位与 0xFF。
    做 追加 用 头、连接 的 第 2 项 位与 0xFF。
    做 追加 用 头、连接 的 第 3 项 右移 24 位与 0xFF。
    做 追加 用 头、连接 的 第 3 项 右移 16 位与 0xFF。
    做 追加 用 头、连接 的 第 3 项 右移 8 位与 0xFF。
    做 追加 用 头、连接 的 第 3 项 位与 0xFF。
    做 追加 用 头、0x50。
    做 追加 用 头、标志。
    做 追加 用 头、连接 的 第 5 项 右移 8 位与 0xFF。
    做 追加 用 头、连接 的 第 5 项 位与 0xFF。
    做 追加 用 头、0。
    做 追加 用 头、0。
    做 追加 用 头、0。
    做 追加 用 头、0。
    做 协议_刷新校验和 用 头、16。
    返还 头。
完。

TCP_三次握手_SYN 接受 连接：
    连接 的 第 2 项 为 1000。
    连接 的 第 4 项 为 1。
    连接 的 第 5 项 为 65535。
    返还 做 TCP_构建头 用 连接、TCP_同步。
完。

TCP_三次握手_SYNACK 接受 连接、对方序列：
    连接 的 第 3 项 为 对方序列 加 1。
    连接 的 第 2 项 为 2000。
    连接 的 第 4 项 为 2。
    返还 做 TCP_构建头 用 连接、TCP_同步 位或 TCP_确认。
完。

TCP_三次握手_ACK 接受 连接、对方序列：
    连接 的 第 3 项 为 对方序列 加 1。
    连接 的 第 4 项 为 3。
    返还 做 TCP_构建头 用 连接、TCP_确认。
完。

TCP_发送数据 接受 连接、数据：
    可变 包 为 做 TCP_构建头 用 连接、TCP_推送 位或 TCP_确认。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 包、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    做 协议_刷新校验和 用 包、16。
    连接 的 第 2 项 为 连接 的 第 2 项 加 做 计数 用 数据。
    返还 包。
完。

TCP_关闭连接 接受 连接：
    连接 的 第 4 项 为 4。
    返还 做 TCP_构建头 用 连接、TCP_结束 位或 TCP_确认。
完。

UDP_构建包 接受 源端口、目标端口、数据：
    可变 包 为 一组：（空）。
    长度 为 8 加 做 计数 用 数据。
    做 追加 用 包、源端口 右移 8 位与 0xFF。
    做 追加 用 包、源端口 位与 0xFF。
    做 追加 用 包、目标端口 右移 8 位与 0xFF。
    做 追加 用 包、目标端口 位与 0xFF。
    做 追加 用 包、长度 右移 8 位与 0xFF。
    做 追加 用 包、长度 位与 0xFF。
    做 追加 用 包、0。
    做 追加 用 包、0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 包、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    做 协议_刷新校验和 用 包、6。
    返还 包。
完。

TCP_校验包 接受 包：
    返还 做 协议_校验有效 用 包。
完。

UDP_校验包 接受 包：
    返还 做 协议_校验有效 用 包。
完。

UDP_解析包 接受 包：
    若 做 计数 用 包 小于 8：
        返还 空。
    完。
    源端口 为 包 的 第 0 项 左移 8 位或 包 的 第 1 项。
    目标端口 为 包 的 第 2 项 左移 8 位或 包 的 第 3 项。
    长度 为 包 的 第 4 项 左移 8 位或 包 的 第 5 项。
    可变 数据 为 一组：（空）。
    可变 i 为 8。
    只要 i 小于 做 计数 用 包：
        做 追加 用 数据、包 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 一组：源端口、目标端口、长度、数据。
完。

ICMP_回显请求 为 8。

ICMP_回显应答 为 0。

ICMP_构建回显 接受 类型、标识、序列、数据：
    可变 包 为 一组：（空）。
    做 追加 用 包、类型。
    做 追加 用 包、0。
    做 追加 用 包、0。
    做 追加 用 包、0。
    做 追加 用 包、标识 右移 8 位与 0xFF。
    做 追加 用 包、标识 位与 0xFF。
    做 追加 用 包、序列 右移 8 位与 0xFF。
    做 追加 用 包、序列 位与 0xFF。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 包、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    校验 为 做 协议_校验和 用 包。
    包 的 第 2 项 为 校验 右移 8 位与 0xFF。
    包 的 第 3 项 为 校验 位与 0xFF。
    返还 包。
完。

协议_校验和 接受 数据：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据 减 1：
        和 为 和 加 (数据 的 第 i 项 左移 8) 加 数据 的 第 i 加 1 项。
        i 为 i 加 2。
    完。
    若 做 计数 用 数据 取余于 2 不等于 0：
        和 为 和 加 (数据 的 第 做 计数 用 数据 减 1 项 左移 8)。
    完。
    只要 和 大于 0xFFFF：
        和 为 (和 位与 0xFFFF) 加 (和 右移 16)。
    完。
    返还 和 位异或 0xFFFF。
完。

ARP_构建请求 接受 发送方IP、目标IP、发送方MAC：
    可变 包 为 一组：（空）。
    做 追加 用 包、0。
    做 追加 用 包、1。
    做 追加 用 包、8。
    做 追加 用 包、0。
    做 追加 用 包、6。
    做 追加 用 包、4。
    做 追加 用 包、0。
    做 追加 用 包、1。
    可变 i 为 0。
    只要 i 小于 6：
        做 追加 用 包、发送方MAC 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 4：
        做 追加 用 包、发送方IP 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 6：
        做 追加 用 包、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 4：
        做 追加 用 包、目标IP 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 包。
完。

可变 _分片缓存 为 一组：（空）。

IP_缓存分片 接受 标识、偏移、数据、更多：
    做 追加 用 _分片缓存、一组：标识、偏移、数据、更多。
完。

IP_重组 接受 标识：
    可变 片段 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _分片缓存：
        若 _分片缓存 的 第 i 项 的 第 0 项 等于 标识：
            做 追加 用 片段、_分片缓存 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 n 为 做 计数 用 片段。
    i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 0。
        只要 j 小于 n 减 i 减 1：
            若 片段 的 第 j 项 的 第 1 项 大于 片段 的 第 j 加 1 项 的 第 1 项：
                临 为 片段 的 第 j 项。
                片段 的 第 j 项 为 片段 的 第 j 加 1 项。
                片段 的 第 j 加 1 项 为 临。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        可变 k 为 0。
        只要 k 小于 做 计数 用 片段 的 第 i 项 的 第 2 项：
            做 追加 用 结果、片段 的 第 i 项 的 第 2 项 的 第 k 项。
            k 为 k 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _协议_发送包数 为 0。

可变 _协议_接收包数 为 0。

可变 _协议_错误包数 为 0。

可变 _协议_TCP连接数 为 0。

协议_记录发送 接受 零参：
    _协议_发送包数 为 _协议_发送包数 加 1。
完。

协议_记录接收 接受 零参：
    _协议_接收包数 为 _协议_接收包数 加 1。
完。

协议_记录错误 接受 零参：
    _协议_错误包数 为 _协议_错误包数 加 1。
完。

协议_记录TCP 接受 零参：
    _协议_TCP连接数 为 _协议_TCP连接数 加 1。
完。

协议_重置 接受 零参：
    _协议_发送包数 为 0。
    _协议_接收包数 为 0。
    _协议_错误包数 为 0。
    _协议_TCP连接数 为 0。
    _分片缓存 为 一组：（空）。
完。

本章 收束。
