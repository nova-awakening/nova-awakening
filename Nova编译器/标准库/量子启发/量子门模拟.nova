《量子门模拟》

引用《..·基础》。

Q精度 为 10000。

量子_根号2倒数 为 7071。

复数_创建 接受 实、虚：
    返还 一组：实、虚。
完。

复数_零 接受 零参：
    返还 一组：0、0。
完。

复数_一 接受 零参：
    返还 一组：Q精度、0。
完。

复数_加 接受 a、b：
    返还 一组：a 的 第 0 项 加 b 的 第 0 项、a 的 第 1 项 加 b 的 第 1 项。
完。

复数_减 接受 a、b：
    返还 一组：a 的 第 0 项 减 b 的 第 0 项、a 的 第 1 项 减 b 的 第 1 项。
完。

复数_乘 接受 a、b：
    实 为 (a 的 第 0 项 乘 b 的 第 0 项 减 a 的 第 1 项 乘 b 的 第 1 项) 除以 Q精度。
    虚 为 (a 的 第 0 项 乘 b 的 第 1 项 加 a 的 第 1 项 乘 b 的 第 0 项) 除以 Q精度。
    返还 一组：实、虚。
完。

复数_共轭 接受 a：
    返还 一组：a 的 第 0 项、0 减 a 的 第 1 项。
完。

复数_模方 接受 a：
    返还 (a 的 第 0 项 乘 a 的 第 0 项 加 a 的 第 1 项 乘 a 的 第 1 项) 除以 Q精度。
完。

量子比特_零态 接受 零参：
    返还 一组：做 复数_一、做 复数_零。
完。

量子比特_一态 接受 零参：
    返还 一组：做 复数_零、做 复数_一。
完。

量子比特_叠加态 接受 零参：
    返还 一组：做 复数_创建 用 量子_根号2倒数、0、做 复数_创建 用 量子_根号2倒数、0。
完。

量子比特_概率0 接受 qb：
    返还 做 复数_模方 用 qb 的 第 0 项。
完。

量子比特_概率1 接受 qb：
    返还 做 复数_模方 用 qb 的 第 1 项。
完。

量子比特_测量 接受 qb、随机种子：
    p0 为 做 量子比特_概率0 用 qb。
    随机 为 (随机种子 乘 1103515245 加 12345) 取余于 Q精度。
    若 随机 小于 p0：
        返还 一组：0、一组：做 复数_一、做 复数_零。
    完。
    返还 一组：1、一组：做 复数_零、做 复数_一。
完。

门_H 接受 qb：
    新α 为 做 复数_创建 用 (qb 的 第 0 项 的 第 0 项 加 qb 的 第 1 项 的 第 0 项) 乘 量子_根号2倒数 除以 Q精度、(qb 的 第 0 项 的 第 1 项 加 qb 的 第 1 项 的 第 1 项) 乘 量子_根号2倒数 除以 Q精度。
    新β 为 做 复数_创建 用 (qb 的 第 0 项 的 第 0 项 减 qb 的 第 1 项 的 第 0 项) 乘 量子_根号2倒数 除以 Q精度、(qb 的 第 0 项 的 第 1 项 减 qb 的 第 1 项 的 第 1 项) 乘 量子_根号2倒数 除以 Q精度。
    返还 一组：新α、新β。
完。

门_X 接受 qb：
    返还 一组：qb 的 第 1 项、qb 的 第 0 项。
完。

门_Z 接受 qb：
    返还 一组：qb 的 第 0 项、做 复数_创建 用 0 减 qb 的 第 1 项 的 第 0 项、0 减 qb 的 第 1 项 的 第 1 项。
完。

门_Y 接受 qb：
    返还 一组：做 复数_创建 用 qb 的 第 1 项 的 第 1 项、0 减 qb 的 第 1 项 的 第 0 项、做 复数_创建 用 0 减 qb 的 第 0 项 的 第 1 项、qb 的 第 0 项 的 第 0 项。
完。

门_S 接受 qb：
    返还 一组：qb 的 第 0 项、做 复数_创建 用 0 减 qb 的 第 1 项 的 第 1 项、qb 的 第 1 项 的 第 0 项。
完。

门_T 接受 qb：
    新β 为 做 复数_乘 用 qb 的 第 1 项、做 复数_创建 用 量子_根号2倒数、量子_根号2倒数。
    返还 一组：qb 的 第 0 项、新β。
完。

门_Rz 接受 qb、角度：
    θ 为 角度。
    θ2 为 θ 乘 θ 除以 Q精度。
    cos近似 为 Q精度 减 θ2 除以 2。
    sin近似 为 θ 减 θ2 乘 θ 除以 (6 乘 Q精度)。
    相位 为 做 复数_创建 用 cos近似、sin近似。
    返还 一组：qb 的 第 0 项、做 复数_乘 用 qb 的 第 1 项、相位。
完。

量子寄存器_创建 接受 比特数：
    可变 维度 为 1。
    可变 i 为 0。
    只要 i 小于 比特数：
        维度 为 维度 乘 2。
        i 为 i 加 1。
    完。
    可变 态向量 为 一组：（空）。
    i 为 0。
    只要 i 小于 维度：
        若 i 等于 0：
            做 追加 用 态向量、做 复数_一。
        否则：
            做 追加 用 态向量、做 复数_零。
        完。
        i 为 i 加 1。
    完。
    返还 一组：比特数、维度、态向量。
完。

量子寄存器_应用门 接受 寄存器、目标比特、门矩阵：
    _n 为 寄存器 的 第 0 项。
    可变 维度 为 寄存器 的 第 1 项。
    态 为 寄存器 的 第 2 项。
    可变 新态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 新态、做 复数_零。
        i 为 i 加 1。
    完。
    可变 步长 为 1。
    i 为 0。
    只要 i 小于 目标比特：
        步长 为 步长 乘 2。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 维度：
        位 为 i 除以 步长 取余于 2。
        若 位 等于 0：
            j 为 i 加 步长。
            若 j 小于 维度：
                旧0 为 态 的 第 i 项。
                旧1 为 态 的 第 j 项。
                新态 的 第 i 项 为 做 复数_加 用 做 复数_乘 用 门矩阵 的 第 0 项、旧0、做 复数_乘 用 门矩阵 的 第 1 项、旧1。
                新态 的 第 j 项 为 做 复数_加 用 做 复数_乘 用 门矩阵 的 第 2 项、旧0、做 复数_乘 用 门矩阵 的 第 3 项、旧1。
            完。
        完。
        i 为 i 加 1。
    完。
    寄存器 的 第 2 项 为 新态。
完。

量子寄存器_CNOT 接受 寄存器、控制比特、目标比特：
    可变 维度 为 寄存器 的 第 1 项。
    态 为 寄存器 的 第 2 项。
    可变 新态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 新态、态 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 控制步长 为 1。
    i 为 0。
    只要 i 小于 控制比特：
        控制步长 为 控制步长 乘 2。
        i 为 i 加 1。
    完。
    可变 目标步长 为 1。
    i 为 0。
    只要 i 小于 目标比特：
        目标步长 为 目标步长 乘 2。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 维度：
        控制位 为 i 除以 控制步长 取余于 2。
        若 控制位 等于 1：
            目标位 为 i 除以 目标步长 取余于 2。
            可变 翻转索引 为 0。
            若 目标位 等于 0：
                翻转索引 为 i 加 目标步长。
            否则：
                翻转索引 为 i 减 目标步长。
            完。
            若 翻转索引 大于或等于 0 并且 翻转索引 小于 维度：
                新态 的 第 i 项 为 态 的 第 翻转索引 项。
            完。
        完。
        i 为 i 加 1。
    完。
    寄存器 的 第 2 项 为 新态。
完。

量子寄存器_测量 接受 寄存器、种子：
    可变 维度 为 寄存器 的 第 1 项。
    态 为 寄存器 的 第 2 项。
    随机 为 (种子 乘 48271 加 7) 取余于 Q精度。
    可变 累积 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        累积 为 累积 加 做 复数_模方 用 态 的 第 i 项。
        若 随机 小于 累积：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 维度 减 1。
完。

Grover搜索 接受 比特数、目标索引：
    寄存器 为 做 量子寄存器_创建 用 比特数。
    可变 维度 为 寄存器 的 第 1 项。
    H矩阵 为 一组：做 复数_创建 用 量子_根号2倒数、0、做 复数_创建 用 量子_根号2倒数、0、做 复数_创建 用 量子_根号2倒数、0、做 复数_创建 用 0 减 量子_根号2倒数、0。
    可变 b 为 0。
    只要 b 小于 比特数：
        做 量子寄存器_应用门 用 寄存器、b、H矩阵。
        b 为 b 加 1。
    完。
    态 为 寄存器 的 第 2 项。
    可变 sqrt_n 为 1。
    只要 sqrt_n 乘 sqrt_n 小于 维度：
        sqrt_n 为 sqrt_n 加 1。
    完。
    可变 迭代次数 为 sqrt_n 乘 7854 除以 Q精度。
    若 迭代次数 小于 1：
        迭代次数 为 1。
    完。
    可变 迭代 为 0。
    只要 迭代 小于 迭代次数：
        若 目标索引 小于 维度：
            态 的 第 目标索引 项 为 做 复数_创建 用 0 减 态 的 第 目标索引 项 的 第 0 项、0 减 态 的 第 目标索引 项 的 第 1 项。
        完。
        可变 平均实 为 0。
        可变 平均虚 为 0。
        可变 k 为 0。
        只要 k 小于 维度：
            平均实 为 平均实 加 态 的 第 k 项 的 第 0 项。
            平均虚 为 平均虚 加 态 的 第 k 项 的 第 1 项。
            k 为 k 加 1。
        完。
        平均实 为 平均实 除以 维度。
        平均虚 为 平均虚 除以 维度。
        k 为 0。
        只要 k 小于 维度：
            态 的 第 k 项 为 做 复数_创建 用 2 乘 平均实 减 态 的 第 k 项 的 第 0 项、2 乘 平均虚 减 态 的 第 k 项 的 第 1 项。
            k 为 k 加 1。
        完。
        迭代 为 迭代 加 1。
    完。
    寄存器 的 第 2 项 为 态。
    目标概率 为 做 复数_模方 用 态 的 第 目标索引 项。
    返还 一组：目标索引、目标概率、迭代次数。
完。

量子退火_优化 接受 代价函数值列表、最大步数、初始温度：
    状态数 为 做 计数 用 代价函数值列表。
    可变 接受 为 0。
    可变 温度 为 0。
    若 状态数 等于 0：
        返还 负 1。
    完。
    可变 当前 为 0。
    可变 当前代价 为 代价函数值列表 的 第 0 项。
    可变 最优 为 0。
    可变 最优代价 为 当前代价。
    可变 t 为 0。
    只要 t 小于 最大步数：
        隧穿概率 为 温度 乘 Q精度 除以 (初始温度 加 1)。
        可变 候选 为 当前 加 (t 乘 1103515245 加 12345) 取余于 3 减 1。
        若 候选 小于 0：
            候选 为 0。
        完。
        若 候选 大于或等于 状态数：
            候选 为 状态数 减 1。
        完。
        跳跃随机 为 (t 乘 48271 加 7) 取余于 Q精度。
        若 跳跃随机 小于 隧穿概率 除以 3：
            候选 为 (t 乘 16807 加 13) 取余于 状态数。
        完。
        候选代价 为 代价函数值列表 的 第 候选 项。
        Δ 为 候选代价 减 当前代价。
        若 Δ 小于或等于 0：
            接受 为 1。
        否则若 温度 大于 0：
            可变 接受率 为 Q精度 减 Δ 乘 Q精度 除以 (温度 加 1)。
            若 接受率 小于 0：
                接受率 为 0。
            完。
            随机 为 (t 乘 69069 加 1) 取余于 Q精度。
            若 随机 小于 接受率：
                接受 为 1。
            完。
        完。
        若 接受 等于 1：
            当前 为 候选。
            当前代价 为 候选代价。
        完。
        若 当前代价 小于 最优代价：
            最优 为 当前。
            最优代价 为 当前代价。
        完。
        温度 为 温度 乘 9990 除以 Q精度。
        t 为 t 加 1。
    完。
    返还 一组：最优、最优代价。
完。

量子_比特翻转码_编码 接受 位：
    返还 一组：位、位、位。
完。

量子_比特翻转码_解码 接受 三位：
    投票 为 三位 的 第 0 项 加 三位 的 第 1 项 加 三位 的 第 2 项。
    若 投票 大于或等于 2：
        返还 1。
    完。
    返还 0。
完。

量子_相位翻转码_编码 接受 位：
    返还 一组：位、位、位。
完。

量子_奇偶校验 接受 位列表：
    可变 校验 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 位列表：
        校验 为 校验 位异或 位列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 校验。
完。

量子_线路深度 接受 门列表：
    可变 最大层 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 门列表：
        若 门列表 的 第 i 项 的 第 2 项 大于 最大层：
            最大层 为 门列表 的 第 i 项 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大层 加 1。
完。

量子_门计数 接受 门列表、门类型：
    可变 计 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 门列表：
        若 门列表 的 第 i 项 的 第 0 项 等于 门类型：
            计 为 计 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计。
完。

量子_量子比特数 接受 门列表：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 门列表：
        若 门列表 的 第 i 项 的 第 1 项 大于 最大：
            最大 为 门列表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大 加 1。
完。

可变 _量子_模拟次数 为 0。

可变 _量子_门操作数 为 0。

可变 _量子_最大比特数 为 0。

量子_记录模拟 接受 比特数：
    _量子_模拟次数 为 _量子_模拟次数 加 1。
    若 比特数 大于 _量子_最大比特数：
        _量子_最大比特数 为 比特数。
    完。
完。

量子_记录门 接受 零参：
    _量子_门操作数 为 _量子_门操作数 加 1。
完。

量子_复杂度评估 接受 零参：
    若 _量子_最大比特数 大于 20：
        返还 「超大规模(经典模拟困难)」。
    完。
    若 _量子_最大比特数 大于 10：
        返还 「中等规模」。
    完。
    返还 「小规模(可精确模拟)」。
完。

量子_重置 接受 零参：
    _量子_模拟次数 为 0。
    _量子_门操作数 为 0。
    _量子_最大比特数 为 0。
完。

量子_内存估算 接受 比特数：
    可变 大小 为 1。
    可变 i 为 0。
    只要 i 小于 比特数：
        大小 为 大小 乘 2。
        i 为 i 加 1。
    完。
    返还 大小 乘 16。
完。

量子_可模拟性 接受 比特数：
    若 比特数 大于 30：
        返还 「不可能(内存超限)」。
    完。
    若 比特数 大于 20：
        返还 「困难(需分布式)」。
    完。
    若 比特数 大于 10：
        返还 「可行(需高内存)」。
    完。
    返还 「轻松」。
完。

量子_门效率 接受 门列表：
    比特数 为 做 量子_量子比特数 用 门列表。
    门数 为 做 计数 用 门列表。
    若 比特数 等于 0：
        返还 0。
    完。
    返还 门数 乘 100 除以 比特数。
完。

本章 收束。
