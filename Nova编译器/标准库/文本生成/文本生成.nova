《文本生成》

引用《..·基础》。

引用《..·采样策略·采样策略》。

生成_随机态 为 99999。

生成_随机 接受 零参：
    生成_随机态 为 生成_随机态 位异或 生成_随机态 左移 13。
    生成_随机态 为 生成_随机态 位异或 生成_随机态 右移 7。
    生成_随机态 为 生成_随机态 位异或 生成_随机态 左移 17。
    若 生成_随机态 小于 0：
        生成_随机态 为 0 减 生成_随机态。
    完。
    返还 生成_随机态。
完。

贪心 接受 对数值：
    可变 最佳 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 对数值：
        若 对数值 的 第 i 项 大于 对数值 的 第 最佳 项：
            最佳 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

温度采样 接受 对数值、温度：
    若 温度 小于或等于 0：
        返还 做 贪心 用 对数值。
    完。
    可变 缩放 为 做 映射 用 对数值、以 x 映射到 x 乘 100 除以 温度。
    可变 最大 为 缩放 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 缩放：
        若 缩放 的 第 i 项 大于 最大：
            最大 为 缩放 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 probs 为 一组：（空）。
    可变 合计 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 缩放：
        可变 e 为 100 加 (缩放 的 第 i 项 减 最大)。
        若 e 小于 1：
            e 为 1。
        完。
        做 追加 用 probs、e。
        合计 为 合计 加 e。
        i 为 i 加 1。
    完。
    可变 阈值 为 做 生成_随机 取余于 合计。
    可变 累积 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 probs：
        累积 为 累积 加 probs 的 第 i 项。
        若 累积 大于 阈值：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 做 计数 用 probs 减 1。
完。

重复惩罚 接受 对数值、已生成、惩罚：
    可变 结果 为 做 复制列表 用 对数值。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已生成：
        词元值 为 已生成 的 第 i 项。
        若 词元值 大于或等于 0 并且 词元值 小于 做 计数 用 结果：
            若 结果 的 第 词元值 项 大于 0：
                结果 的 第 词元值 项 为 结果 的 第 词元值 项 乘 100 除以 惩罚。
            否则：
                结果 的 第 词元值 项 为 结果 的 第 词元值 项 乘 惩罚 除以 100。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 停止_tokens 为 一组：（空）。

停止_追加 接受 词元：
    做 追加 用 停止_tokens、词元。
完。

停止_清空 接受 零参：
    停止_tokens 为 一组：（空）。
完。

停止_检查 接受 词元：
    可变 i 为 0。
    只要 i 小于 做 计数 用 停止_tokens：
        若 停止_tokens 的 第 i 项 等于 词元：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

模拟_logits 接受 已生成、词表大小：
    可变 对数值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 词表大小：
        基数 为 (i 乘 17 加 13) 取余于 100。
        若 做 计数 用 已生成 大于 0：
            last 为 已生成 的 第 做 计数 用 已生成 减 1 项。
            做 追加 用 对数值、基数 加 (last 加 i) 乘 7 取余于 50。
        否则：
            做 追加 用 对数值、基数。
        完。
        i 为 i 加 1。
    完。
    返还 对数值。
完。

自回归_生成 接受 前缀、最大长度、词表大小、策略：
    可变 已生成 为 做 复制列表 用 前缀。
    可变 步数 为 0。
    只要 步数 小于 最大长度：
        对数值 为 做 模拟_logits 用 已生成、词表大小。
        可变 词元 为 0。
        若 策略 等于 0：
            词元 为 做 贪心 用 对数值。
        否则若 策略 等于 1：
            词元 为 做 温度采样 用 对数值、80。
        否则：
            词元 为 做 TopK采样 用 对数值、5。
        完。
        若 做 停止_检查 用 词元：
            返还 已生成。
        完。
        做 追加 用 已生成、词元。
        步数 为 步数 加 1。
    完。
    返还 已生成。
完。

流式_生成 接受 前缀、最大长度、词表大小：
    可变 已生成 为 做 复制列表 用 前缀。
    可变 输出流 为 一组：（空）。
    可变 步数 为 0。
    只要 步数 小于 最大长度：
        对数值 为 做 模拟_logits 用 已生成、词表大小。
        词元 为 做 贪心 用 对数值。
        若 做 停止_检查 用 词元：
            返还 输出流。
        完。
        做 追加 用 已生成、词元。
        做 追加 用 输出流、词元。
        步数 为 步数 加 1。
    完。
    返还 输出流。
完。

频率惩罚 接受 对数值、已生成、惩罚值：
    可变 结果 为 做 复制列表 用 对数值。
    可变 i 为 0。
    只要 i 小于 做 计数 用 已生成：
        词元值 为 已生成 的 第 i 项。
        若 词元值 大于或等于 0 并且 词元值 小于 做 计数 用 结果：
            结果 的 第 词元值 项 为 结果 的 第 词元值 项 减 惩罚值。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

Ngram_阻断 接受 对数值、已生成、n：
    可变 结果 为 做 复制列表 用 对数值。
    长度 为 做 计数 用 已生成。
    若 长度 小于 n：
        返还 结果。
    完。
    可变 前缀 为 一组：（空）。
    可变 i 为 长度 减 n 加 1。
    只要 i 小于 长度：
        做 追加 用 前缀、已生成 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 位置 为 0。
    只要 位置 小于或等于 长度 减 n：
        可变 eq 为 真。
        可变 j 为 0。
        只要 j 小于 n 减 1：
            若 已生成 的 第 位置 加 j 项 不等于 前缀 的 第 j 项：
                eq 为 假。
            完。
            j 为 j 加 1。
        完。
        若 eq：
            blocked 为 已生成 的 第 位置 加 n 减 1 项。
            若 blocked 大于或等于 0 并且 blocked 小于 做 计数 用 结果：
                结果 的 第 blocked 项 为 0 减 0x7FFFFFFF。
            完。
        完。
        位置 为 位置 加 1。
    完。
    返还 结果。
完。

文本生成_束搜索_初始化 接受 束宽：
    可变 束列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 束宽：
        做 追加 用 束列表、一组：一组：（空）、0。
        i 为 i 加 1。
    完。
    返还 束列表。
完。

文本生成_束搜索_扩展 接受 束列表、分数列表、束宽：
    可变 候选 为 一组：（空）。
    可变 新束 为 一组：（空）。
    可变 b 为 0。
    只要 b 小于 做 计数 用 束列表：
        可变 t 为 0。
        只要 t 小于 做 计数 用 分数列表：
            新分数 为 束列表 的 第 b 项 的 第 1 项 加 分数列表 的 第 t 项。
            做 追加 用 候选、一组：b、t、新分数。
            t 为 t 加 1。
        完。
        b 为 b 加 1。
    完。
    可变 已选 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 束宽 并且 k 小于 做 计数 用 候选：
        可变 最佳 为 负 1。
        可变 最佳分 为 负 999999。
        可变 i 为 0。
        只要 i 小于 做 计数 用 候选：
            可变 跳过 为 假。
            可变 j 为 0。
            只要 j 小于 做 计数 用 已选：
                若 已选 的 第 j 项 等于 i：
                    跳过 为 真。
                完。
                j 为 j 加 1。
            完。
            若 跳过 等于 假 并且 候选 的 第 i 项 的 第 2 项 大于 最佳分：
                最佳 为 i。
                最佳分 为 候选 的 第 i 项 的 第 2 项。
            完。
            i 为 i 加 1。
        完。
        若 最佳 大于或等于 0：
            来源束 为 候选 的 第 最佳 项 的 第 0 项。
            新token 为 候选 的 第 最佳 项 的 第 1 项。
            可变 新序列 为 一组：（空）。
            i 为 0。
            只要 i 小于 做 计数 用 束列表 的 第 来源束 项 的 第 0 项：
                做 追加 用 新序列、束列表 的 第 来源束 项 的 第 0 项 的 第 i 项。
                i 为 i 加 1。
            完。
            做 追加 用 新序列、新token。
            做 追加 用 新束、一组：新序列、最佳分。
            做 追加 用 已选、最佳。
        完。
        k 为 k 加 1。
    完。
    返还 新束。
完。

文本生成_温度_缩放 接受 对数值、温度：
    若 温度 小于或等于 0：
        温度 为 1。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 对数值：
        做 追加 用 结果、对数值 的 第 i 项 乘 1000 除以 温度。
        i 为 i 加 1。
    完。
    返还 结果。
完。

top_p_过滤 接受 分数列表、p阈值：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分数列表：
        若 分数列表 的 第 i 项 大于 0：
            总 为 总 加 分数列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 总 等于 0：
        返还 分数列表。
    完。
    可变 累积 为 0。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 分数列表：
        若 累积 小于 p阈值 乘 总 除以 1000：
            做 追加 用 结果、分数列表 的 第 i 项。
            若 分数列表 的 第 i 项 大于 0：
                累积 为 累积 加 分数列表 的 第 i 项。
            完。
        否则：
            做 追加 用 结果、0。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

top_k_过滤 接受 分数列表、k：
    可变 结果 为 一组：（空）。
    可变 阈值列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分数列表：
        做 追加 用 阈值列表、分数列表 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 已选数 为 0。
    可变 min_top 为 负 999999。
    i 为 0。
    只要 i 小于 k 并且 i 小于 做 计数 用 分数列表：
        可变 最大 为 负 999999。
        可变 j 为 0。
        只要 j 小于 做 计数 用 阈值列表：
            若 阈值列表 的 第 j 项 大于 最大：
                最大 为 阈值列表 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        min_top 为 最大。
        j 为 0。
        只要 j 小于 做 计数 用 阈值列表：
            若 阈值列表 的 第 j 项 等于 最大 并且 已选数 小于 k：
                阈值列表 的 第 j 项 为 负 999999。
                已选数 为 已选数 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 分数列表：
        若 分数列表 的 第 i 项 大于或等于 min_top：
            做 追加 用 结果、分数列表 的 第 i 项。
        否则：
            做 追加 用 结果、0。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

重复惩罚_应用 接受 分数列表、已生成、惩罚因子：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分数列表：
        可变 出现次数 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 已生成：
            若 已生成 的 第 j 项 等于 i：
                出现次数 为 出现次数 加 1。
            完。
            j 为 j 加 1。
        完。
        若 出现次数 大于 0：
            做 追加 用 结果、分数列表 的 第 i 项 乘 1000 除以 (惩罚因子 乘 出现次数)。
        否则：
            做 追加 用 结果、分数列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

生成配置_创建 接受 最大长度、温度、top_k、top_p、重复惩罚、束宽：
    返还 一组：最大长度、温度、top_k、top_p、重复惩罚、束宽。
完。

生成配置_默认 接受 零参：
    返还 做 生成配置_创建 用 128、700、50、900、1200、1。
完。

本章 收束。
