《学习》

引用《..·基础》。

引用《..·数学·强化学习》。

引用《..·算法·数学扩展》。

引用《..·..·编译器·标准前导》。

STDP_更新 接受 权重、dt、A正、A负、tau：
    若 dt 大于 0：
        若 tau 等于 0：
            tau 为 1。
        完。
        可变 增量 为 A正 乘 (tau 减 dt) 除以 tau。
        若 增量 小于 0：
            增量 为 0。
        完。
        返还 权重 加 增量。
    否则若 dt 小于 0：
        可变 减量 为 A负 乘 (tau 加 dt) 除以 tau。
        若 减量 小于 0：
            减量 为 0。
        完。
        返还 权重 减 减量。
    完。
    返还 权重。
完。

可变 BCM_阈值 为 50。

BCM_更新 接受 权重、激活、阈值：
    调节 为 激活 乘 (激活 减 阈值) 除以 100。
    返还 权重 加 调节。
完。

BCM_调整阈值 接受 平均激活：
    BCM_阈值 为 平均激活 乘 平均激活 除以 100。
    返还 BCM_阈值。
完。

课程_排序 接受 难度列表：
    可变 已排 为 做 复制列表 用 难度列表。
    做 排序 用 已排、0。
    返还 已排。
完。

课程_选取 接受 难度列表、当前阶段、总阶段：
    截止 为 做 计数 用 难度列表 乘 (当前阶段 加 1) 除以 总阶段。
    可变 子集 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 截止：
        做 追加 用 子集、难度列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 子集。
完。

遗忘_保持率 接受 强度、时间间隔、稳定性：
    若 稳定性 加 时间间隔 等于 0：
        返还 强度。
    完。
    返还 强度 乘 稳定性 除以 (稳定性 加 时间间隔)。
完。

间隔重复_下次 接受 当前间隔、正确：
    若 正确：
        返还 当前间隔 乘 250 除以 100。
    完。
    返还 当前间隔 除以 2 加 1。
完。

回放_随机态 为 55555。

回放_随机 接受 零参：
    回放_随机态 为 回放_随机态 位异或 回放_随机态 左移 13。
    回放_随机态 为 回放_随机态 位异或 回放_随机态 右移 7。
    回放_随机态 为 回放_随机态 位异或 回放_随机态 左移 17。
    若 回放_随机态 小于 0：
        回放_随机态 为 0 减 回放_随机态。
    完。
    返还 回放_随机态。
完。

对比_相似度 接受 a、b：
    可变 长 为 做 计数 用 a。
    若 做 计数 用 b 小于 长：
        长 为 做 计数 用 b。
    完。
    可变 点积 为 0。
    可变 平方和a 为 0。
    可变 平方和b 为 0。
    可变 i 为 0。
    只要 i 小于 长：
        点积 为 点积 加 a 的 第 i 项 乘 b 的 第 i 项。
        平方和a 为 平方和a 加 a 的 第 i 项 乘 a 的 第 i 项。
        平方和b 为 平方和b 加 b 的 第 i 项 乘 b 的 第 i 项。
        i 为 i 加 1。
    完。
    na 为 做 整数sqrt 用 平方和a。
    nb 为 做 整数sqrt 用 平方和b。
    若 na 等于 0：
        返还 0。
    完。
    若 nb 等于 0：
        返还 0。
    完。
    返还 点积 乘 100 除以 (na 乘 nb)。
完。

对比_损失 接受 锚点、正样本、负样本：
    正相似 为 做 对比_相似度 用 锚点、正样本。
    负相似 为 做 对比_相似度 用 锚点、负样本。
    可变 损失 为 负相似 减 正相似 加 50。
    若 损失 小于 0：
        损失 为 0。
    完。
    返还 损失。
完。

知识_保持率 接受 强度列表、时间、稳定性：
    n 为 做 计数 用 强度列表。
    若 n 等于 0：
        返还 0。
    完。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总和 为 总和 加 做 遗忘_保持率 用 强度列表 的 第 i 项、时间、稳定性。
        i 为 i 加 1。
    完。
    返还 总和 除以 n。
完。

可变 自适应_lr 为 100。

可变 自适应_历史 为 一组：（空）。

自适应_更新 接受 损失：
    做 追加 用 自适应_历史、损失。
    若 做 计数 用 自适应_历史 大于或等于 3：
        n 为 做 计数 用 自适应_历史。
        若 自适应_历史 的 第 n 减 1 项 大于或等于 自适应_历史 的 第 n 减 2 项：
            自适应_lr 为 自适应_lr 乘 90 除以 100。
            若 自适应_lr 小于 1：
                自适应_lr 为 1。
            完。
        完。
    完。
    返还 自适应_lr。
完。

元学习_内循环 接受 权重、梯度、内lr、步数：
    可变 w 为 做 复制列表 用 权重。
    可变 s 为 0。
    只要 s 小于 步数：
        可变 i 为 0。
        只要 i 小于 做 计数 用 w：
            w 的 第 i 项 为 w 的 第 i 项 减 梯度 的 第 i 项 乘 内lr 除以 1000。
            i 为 i 加 1。
        完。
        s 为 s 加 1。
    完。
    返还 w。
完。

元学习_外更新 接受 原始权重、适应后权重、外lr：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 原始权重：
        元梯度 为 原始权重 的 第 i 项 减 适应后权重 的 第 i 项。
        做 追加 用 结果、原始权重 的 第 i 项 减 元梯度 乘 外lr 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 回放_缓冲 为 一组：（空）。

回放_最大 为 1000。

学习_回放_追加 接受 经验：
    做 追加 用 回放_缓冲、经验。
    若 做 计数 用 回放_缓冲 大于 回放_最大：
        可变 新缓冲 为 一组：（空）。
        可变 i 为 1。
        只要 i 小于 做 计数 用 回放_缓冲：
            做 追加 用 新缓冲、回放_缓冲 的 第 i 项。
            i 为 i 加 1。
        完。
        回放_缓冲 为 新缓冲。
    完。
完。

学习_回放_采样 接受 批大小：
    可变 样本 为 一组：（空）。
    可变 i 为 0。
    若 做 计数 用 回放_缓冲 等于 0：
        返还 样本。
    完。
    只要 i 小于 批大小：
        索引 为 做 回放_随机 取余于 做 计数 用 回放_缓冲。
        做 追加 用 样本、回放_缓冲 的 第 索引 项。
        i 为 i 加 1。
    完。
    返还 样本。
完。

学习_回放_大小 接受 零参：
    返还 做 计数 用 回放_缓冲。
完。

学习_回放_清空 接受 零参：
    回放_缓冲 为 一组：（空）。
完。

多任务_平衡 接受 损失列表：
    n 为 做 计数 用 损失列表。
    可变 总损失 为 0。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 统计索引 为 0。
    只要 统计索引 小于 n：
        总损失 为 总损失 加 损失列表 的 第 统计索引 项。
        统计索引 为 统计索引 加 1。
    完。
    若 总损失 等于 0：
        总损失 为 1。
    完。
    可变 权重 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 权重、损失列表 的 第 i 项 乘 1000 除以 总损失。
        i 为 i 加 1。
    完。
    返还 权重。
完。

学习_报告 接受 零参：
    做 打印 用 「╔═══ 学习状态 ═══╗」。
    做 打印 用 「║ 自适应LR: 」 加 做 转字符串 用 自适应_lr。
    做 打印 用 「║ 损失历史: 」 加 做 转字符串 用 做 计数 用 自适应_历史 加 「 条」。
    做 打印 用 「║ BCM阈值: 」 加 做 转字符串 用 BCM_阈值。
    做 打印 用 「║ 回放缓冲: 」 加 做 转字符串 用 做 回放_大小 加 「/」 加 做 转字符串 用 回放_最大。
    若 做 计数 用 自适应_历史 大于 0：
        做 打印 用 「║ 最近损失: 」 加 做 转字符串 用 自适应_历史 的 第 做 计数 用 自适应_历史 减 1 项。
    完。
    做 打印 用 「╚════════════════╝」。
完。

蒸馏_损失 接受 教师输出、学生输出、温度：
    可变 损失 为 0。
    可变 长 为 做 计数 用 教师输出。
    若 做 计数 用 学生输出 小于 长：
        长 为 做 计数 用 学生输出。
    完。
    可变 i 为 0。
    只要 i 小于 长：
        差值 为 教师输出 的 第 i 项 乘 1000 除以 温度 减 学生输出 的 第 i 项 乘 1000 除以 温度。
        损失 为 损失 加 差值 乘 差值 除以 1000。
        i 为 i 加 1。
    完。
    返还 损失。
完。

学习_对比损失 接受 锚点、正样本、负样本列表、温度：
    可变 正相似 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 锚点：
        正相似 为 正相似 加 锚点 的 第 i 项 乘 正样本 的 第 i 项。
        i 为 i 加 1。
    完。
    正相似 为 正相似 乘 1000 除以 温度。
    可变 负总和 为 0。
    可变 k 为 0。
    只要 k 小于 做 计数 用 负样本列表：
        负样本 为 负样本列表 的 第 k 项。
        可变 负相似 为 0。
        i 为 0。
        只要 i 小于 做 计数 用 锚点：
            负相似 为 负相似 加 锚点 的 第 i 项 乘 负样本 的 第 i 项。
            i 为 i 加 1。
        完。
        负总和 为 负总和 加 负相似 乘 1000 除以 温度。
        k 为 k 加 1。
    完。
    若 正相似 加 负总和 等于 0：
        返还 0。
    完。
    返还 0 减 正相似 乘 1000 除以 (正相似 加 负总和)。
完。

可变 _课程_当前难度 为 0。

可变 _课程_最大难度 为 10。

可变 _课程_步数 为 0。

课程_初始化 接受 最大难度：
    _课程_当前难度 为 0。
    _课程_最大难度 为 最大难度。
    _课程_步数 为 0。
完。

课程_取难度 接受 零参：
    返还 _课程_当前难度。
完。

课程_步进 接受 性能分数、阈值：
    _课程_步数 为 _课程_步数 加 1。
    若 性能分数 大于 阈值 并且 _课程_当前难度 小于 _课程_最大难度：
        _课程_当前难度 为 _课程_当前难度 加 1。
    完。
    返还 _课程_当前难度。
完。

课程_筛选样本 接受 样本列表、难度列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 样本列表：
        若 难度列表 的 第 i 项 小于或等于 _课程_当前难度：
            做 追加 用 结果、样本列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

学习_回放缓冲_创建 接受 容量：
    返还 一组：一组：（空）、容量、0。
完。

回放缓冲_添加 接受 缓冲、经验：
    若 做 计数 用 缓冲 的 第 0 项 小于 缓冲 的 第 1 项：
        做 追加 用 缓冲 的 第 0 项、经验。
    否则：
        缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 取余于 缓冲 的 第 1 项 项 为 经验。
    完。
    缓冲 的 第 2 项 为 缓冲 的 第 2 项 加 1。
完。

学习_回放缓冲_采样 接受 缓冲、批大小：
    可变 样本 为 一组：（空）。
    n 为 做 计数 用 缓冲 的 第 0 项。
    若 n 等于 0：
        返还 样本。
    完。
    可变 i 为 0。
    只要 i 小于 批大小：
        索引 为 (缓冲 的 第 2 项 加 i 乘 37) 取余于 n。
        做 追加 用 样本、缓冲 的 第 0 项 的 第 索引 项。
        i 为 i 加 1。
    完。
    返还 样本。
完。

学习_回放缓冲_大小 接受 缓冲：
    返还 做 计数 用 缓冲 的 第 0 项。
完。

学习率_常量 接受 初始率、步数：
    返还 初始率。
完。

学习_学习率_线性衰减 接受 初始率、步数、总步数：
    若 步数 大于或等于 总步数：
        返还 0。
    完。
    若 总步数 等于 0：
        返还 初始率。
    完。
    返还 初始率 乘 (总步数 减 步数) 除以 总步数。
完。

学习_学习率_阶梯衰减 接受 初始率、步数、衰减间隔、衰减因子：
    衰减次数 为 步数 除以 衰减间隔。
    可变 率 为 初始率。
    可变 i 为 0。
    只要 i 小于 衰减次数：
        率 为 率 乘 衰减因子 除以 100。
        i 为 i 加 1。
    完。
    返还 率。
完。

学习_学习率_预热 接受 初始率、步数、预热步数：
    若 步数 小于 预热步数：
        返还 初始率 乘 步数 除以 预热步数。
    完。
    返还 初始率。
完。

可变 _早停_最佳损失 为 999999。

可变 _早停_等待计数 为 0。

可变 _早停_耐心 为 10。

早停_初始化 接受 耐心：
    _早停_最佳损失 为 999999。
    _早停_等待计数 为 0。
    _早停_耐心 为 耐心。
完。

早停_检查 接受 当前损失：
    若 当前损失 小于 _早停_最佳损失：
        _早停_最佳损失 为 当前损失。
        _早停_等待计数 为 0。
        返还 假。
    完。
    _早停_等待计数 为 _早停_等待计数 加 1。
    返还 _早停_等待计数 大于或等于 _早停_耐心。
完。

学习_梯度裁剪_范数 接受 梯度列表、最大范数：
    可变 范数平方 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度列表：
        范数平方 为 范数平方 加 梯度列表 的 第 i 项 乘 梯度列表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 范数平方 小于或等于 最大范数 乘 最大范数：
        返还 梯度列表。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 梯度列表：
        做 追加 用 结果、梯度列表 的 第 i 项 乘 最大范数 除以 (范数平方 加 1)。
        i 为 i 加 1。
    完。
    返还 结果。
完。

学习_梯度裁剪_值 接受 梯度列表、最大值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度列表：
        g 为 梯度列表 的 第 i 项。
        若 g 大于 最大值：
            g 为 最大值。
        完。
        若 g 小于 0 减 最大值：
            g 为 0 减 最大值。
        完。
        做 追加 用 结果、g。
        i 为 i 加 1。
    完。
    返还 结果。
完。

随机梯度_动量_更新 接受 参数、梯度、速度、学习率、动量：
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        速度 的 第 i 项 为 动量 乘 速度 的 第 i 项 除以 100 加 梯度 的 第 i 项。
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 速度 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
完。

自适应矩估计_更新 接受 参数、梯度、m、v、步数、学习率：
    衰减率1 为 90。
    衰减率2 为 999。
    极小量 为 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        m 的 第 i 项 为 衰减率1 乘 m 的 第 i 项 除以 100 加 (100 减 衰减率1) 乘 梯度 的 第 i 项 除以 100。
        v 的 第 i 项 为 衰减率2 乘 v 的 第 i 项 除以 1000 加 (1000 减 衰减率2) 乘 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 1000。
        一阶矫正 为 m 的 第 i 项 乘 1000 除以 (1000 减 衰减率1)。
        二阶矫正 为 v 的 第 i 项 乘 1000 除以 (1000 减 衰减率2)。
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 一阶矫正 除以 (二阶矫正 加 极小量)。
        i 为 i 加 1。
    完。
完。

本章 收束。
