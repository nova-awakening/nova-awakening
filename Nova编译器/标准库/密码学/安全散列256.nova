《安全散列256》

引用《..·基础》。

安全散列_K_00 为 0x428a2f98。

安全散列_K_01 为 0x71374491。

安全散列_K_02 为 0xb5c0fbcf。

安全散列_K_03 为 0xe9b5dba5。

安全散列_K_04 为 0x3956c25b。

安全散列_K_05 为 0x59f111f1。

安全散列_K_06 为 0x923f82a4。

安全散列_K_07 为 0xab1c5ed5。

安全散列_K_08 为 0xd807aa98。

安全散列_K_09 为 0x12835b01。

安全散列_K_10 为 0x243185be。

安全散列_K_11 为 0x550c7dc3。

安全散列_K_12 为 0x72be5d74。

安全散列_K_13 为 0x80deb1fe。

安全散列_K_14 为 0x9bdc06a7。

安全散列_K_15 为 0xc19bf174。

安全散列_K_16 为 0xe49b69c1。

安全散列_K_17 为 0xefbe4786。

安全散列_K_18 为 0x0fc19dc6。

安全散列_K_19 为 0x240ca1cc。

安全散列_K_20 为 0x2de92c6f。

安全散列_K_21 为 0x4a7484aa。

安全散列_K_22 为 0x5cb0a9dc。

安全散列_K_23 为 0x76f988da。

安全散列_K_24 为 0x983e5152。

安全散列_K_25 为 0xa831c66d。

安全散列_K_26 为 0xb00327c8。

安全散列_K_27 为 0xbf597fc7。

安全散列_K_28 为 0xc6e00bf3。

安全散列_K_29 为 0xd5a79147。

安全散列_K_30 为 0x06ca6351。

安全散列_K_31 为 0x14292967。

安全散列_K_32 为 0x27b70a85。

安全散列_K_33 为 0x2e1b2138。

安全散列_K_34 为 0x4d2c6dfc。

安全散列_K_35 为 0x53380d13。

安全散列_K_36 为 0x650a7354。

安全散列_K_37 为 0x766a0abb。

安全散列_K_38 为 0x81c2c92e。

安全散列_K_39 为 0x92722c85。

安全散列_K_40 为 0xa2bfe8a1。

安全散列_K_41 为 0xa81a664b。

安全散列_K_42 为 0xc24b8b70。

安全散列_K_43 为 0xc76c51a3。

安全散列_K_44 为 0xd192e819。

安全散列_K_45 为 0xd6990624。

安全散列_K_46 为 0xf40e3585。

安全散列_K_47 为 0x106aa070。

安全散列_K_48 为 0x19a4c116。

安全散列_K_49 为 0x1e376c08。

安全散列_K_50 为 0x2748774c。

安全散列_K_51 为 0x34b0bcb5。

安全散列_K_52 为 0x391c0cb3。

安全散列_K_53 为 0x4ed8aa4a。

安全散列_K_54 为 0x5b9cca4f。

安全散列_K_55 为 0x682e6ff3。

安全散列_K_56 为 0x748f82ee。

安全散列_K_57 为 0x78a5636f。

安全散列_K_58 为 0x84c87814。

安全散列_K_59 为 0x8cc70208。

安全散列_K_60 为 0x90befffa。

安全散列_K_61 为 0xa4506ceb。

安全散列_K_62 为 0xbef9a3f7。

安全散列_K_63 为 0xc67178f2。

sha256_获取K 接受 索引：
    若 索引 等于 0：
        返还 安全散列_K_00。
    完。
    若 索引 等于 1：
        返还 安全散列_K_01。
    完。
    若 索引 等于 2：
        返还 安全散列_K_02。
    完。
    若 索引 等于 3：
        返还 安全散列_K_03。
    完。
    若 索引 等于 4：
        返还 安全散列_K_04。
    完。
    若 索引 等于 5：
        返还 安全散列_K_05。
    完。
    若 索引 等于 6：
        返还 安全散列_K_06。
    完。
    若 索引 等于 7：
        返还 安全散列_K_07。
    完。
    若 索引 等于 8：
        返还 安全散列_K_08。
    完。
    若 索引 等于 9：
        返还 安全散列_K_09。
    完。
    若 索引 等于 10：
        返还 安全散列_K_10。
    完。
    若 索引 等于 11：
        返还 安全散列_K_11。
    完。
    若 索引 等于 12：
        返还 安全散列_K_12。
    完。
    若 索引 等于 13：
        返还 安全散列_K_13。
    完。
    若 索引 等于 14：
        返还 安全散列_K_14。
    完。
    若 索引 等于 15：
        返还 安全散列_K_15。
    完。
    若 索引 等于 16：
        返还 安全散列_K_16。
    完。
    若 索引 等于 17：
        返还 安全散列_K_17。
    完。
    若 索引 等于 18：
        返还 安全散列_K_18。
    完。
    若 索引 等于 19：
        返还 安全散列_K_19。
    完。
    若 索引 等于 20：
        返还 安全散列_K_20。
    完。
    若 索引 等于 21：
        返还 安全散列_K_21。
    完。
    若 索引 等于 22：
        返还 安全散列_K_22。
    完。
    若 索引 等于 23：
        返还 安全散列_K_23。
    完。
    若 索引 等于 24：
        返还 安全散列_K_24。
    完。
    若 索引 等于 25：
        返还 安全散列_K_25。
    完。
    若 索引 等于 26：
        返还 安全散列_K_26。
    完。
    若 索引 等于 27：
        返还 安全散列_K_27。
    完。
    若 索引 等于 28：
        返还 安全散列_K_28。
    完。
    若 索引 等于 29：
        返还 安全散列_K_29。
    完。
    若 索引 等于 30：
        返还 安全散列_K_30。
    完。
    若 索引 等于 31：
        返还 安全散列_K_31。
    完。
    若 索引 等于 32：
        返还 安全散列_K_32。
    完。
    若 索引 等于 33：
        返还 安全散列_K_33。
    完。
    若 索引 等于 34：
        返还 安全散列_K_34。
    完。
    若 索引 等于 35：
        返还 安全散列_K_35。
    完。
    若 索引 等于 36：
        返还 安全散列_K_36。
    完。
    若 索引 等于 37：
        返还 安全散列_K_37。
    完。
    若 索引 等于 38：
        返还 安全散列_K_38。
    完。
    若 索引 等于 39：
        返还 安全散列_K_39。
    完。
    若 索引 等于 40：
        返还 安全散列_K_40。
    完。
    若 索引 等于 41：
        返还 安全散列_K_41。
    完。
    若 索引 等于 42：
        返还 安全散列_K_42。
    完。
    若 索引 等于 43：
        返还 安全散列_K_43。
    完。
    若 索引 等于 44：
        返还 安全散列_K_44。
    完。
    若 索引 等于 45：
        返还 安全散列_K_45。
    完。
    若 索引 等于 46：
        返还 安全散列_K_46。
    完。
    若 索引 等于 47：
        返还 安全散列_K_47。
    完。
    若 索引 等于 48：
        返还 安全散列_K_48。
    完。
    若 索引 等于 49：
        返还 安全散列_K_49。
    完。
    若 索引 等于 50：
        返还 安全散列_K_50。
    完。
    若 索引 等于 51：
        返还 安全散列_K_51。
    完。
    若 索引 等于 52：
        返还 安全散列_K_52。
    完。
    若 索引 等于 53：
        返还 安全散列_K_53。
    完。
    若 索引 等于 54：
        返还 安全散列_K_54。
    完。
    若 索引 等于 55：
        返还 安全散列_K_55。
    完。
    若 索引 等于 56：
        返还 安全散列_K_56。
    完。
    若 索引 等于 57：
        返还 安全散列_K_57。
    完。
    若 索引 等于 58：
        返还 安全散列_K_58。
    完。
    若 索引 等于 59：
        返还 安全散列_K_59。
    完。
    若 索引 等于 60：
        返还 安全散列_K_60。
    完。
    若 索引 等于 61：
        返还 安全散列_K_61。
    完。
    若 索引 等于 62：
        返还 安全散列_K_62。
    完。
    若 索引 等于 63：
        返还 安全散列_K_63。
    完。
    返还 0。
完。

sha256_右旋 接受 值、位数：
    右 为 值 除以 做 幂 用 2、位数 取余于 做 幂 用 2、32 减 位数。
    左 为 值 乘 做 幂 用 2、32 减 位数 取余于 做 幂 用 2、32。
    返还 (右 加 左) 取余于 做 幂 用 2、32。
完。

sha256_右移 接受 值、位数：
    返还 值 除以 做 幂 用 2、位数 取余于 做 幂 用 2、32 减 位数。
完。

sha256_与 接受 a、b：
    可变 结果 为 0。
    可变 位 为 1。
    可变 i 为 0。
    只要 i 小于 32：
        a位 为 a 除以 位 取余于 2。
        b位 为 b 除以 位 取余于 2。
        若 a位 等于 1：
            若 b位 等于 1：
                结果 为 结果 加 位。
            完。
        完。
        位 为 位 乘 2。
        i 为 i 加 1。
    完。
    返还 结果。
完。

sha256_或 接受 a、b：
    可变 结果 为 0。
    可变 位 为 1。
    可变 i 为 0。
    只要 i 小于 32：
        a位 为 a 除以 位 取余于 2。
        b位 为 b 除以 位 取余于 2。
        若 a位 等于 1：
            结果 为 结果 加 位。
        否则：
            若 b位 等于 1：
                结果 为 结果 加 位。
            完。
        完。
        位 为 位 乘 2。
        i 为 i 加 1。
    完。
    返还 结果。
完。

sha256_异或 接受 a、b：
    可变 结果 为 0。
    可变 位 为 1。
    可变 i 为 0。
    只要 i 小于 32：
        a位 为 a 除以 位 取余于 2。
        b位 为 b 除以 位 取余于 2。
        若 a位 不等于 b位：
            结果 为 结果 加 位。
        完。
        位 为 位 乘 2。
        i 为 i 加 1。
    完。
    返还 结果。
完。

sha256_取反 接受 a：
    可变 结果 为 0。
    可变 位 为 1。
    可变 i 为 0。
    只要 i 小于 32：
        当前位 为 a 除以 位 取余于 2。
        若 当前位 等于 0：
            结果 为 结果 加 位。
        完。
        位 为 位 乘 2。
        i 为 i 加 1。
    完。
    返还 结果。
完。

sha256_加 接受 a、b：
    和值 为 a 加 b。
    模值 为 做 幂 用 2、32。
    返还 和值 取余于 模值。
完。

sha256_大S0 接受 x：
    r2 为 做 sha256_右旋 用 x、2。
    r13 为 做 sha256_右旋 用 x、13。
    r22 为 做 sha256_右旋 用 x、22。
    混合 为 做 sha256_异或 用 r2、r13。
    返还 做 sha256_异或 用 混合、r22。
完。

sha256_大S1 接受 x：
    r6 为 做 sha256_右旋 用 x、6。
    r11 为 做 sha256_右旋 用 x、11。
    r25 为 做 sha256_右旋 用 x、25。
    混合 为 做 sha256_异或 用 r6、r11。
    返还 做 sha256_异或 用 混合、r25。
完。

sha256_小s0 接受 x：
    r7 为 做 sha256_右旋 用 x、7。
    r18 为 做 sha256_右旋 用 x、18。
    rs3 为 做 sha256_右移 用 x、3。
    混合 为 做 sha256_异或 用 r7、r18。
    返还 做 sha256_异或 用 混合、rs3。
完。

sha256_小s1 接受 x：
    r17 为 做 sha256_右旋 用 x、17。
    r19 为 做 sha256_右旋 用 x、19。
    rs10 为 做 sha256_右移 用 x、10。
    混合 为 做 sha256_异或 用 r17、r19。
    返还 做 sha256_异或 用 混合、rs10。
完。

SHA256_选择 接受 x、y、z：
    xy 为 做 sha256_与 用 x、y。
    nx 为 做 sha256_取反 用 x。
    nz 为 做 sha256_与 用 nx、z。
    返还 做 sha256_异或 用 xy、nz。
完。

SHA256_多数 接受 x、y、z：
    xy 为 做 sha256_与 用 x、y。
    xz 为 做 sha256_与 用 x、z。
    yz 为 做 sha256_与 用 y、z。
    混合 为 做 sha256_异或 用 xy、xz。
    返还 做 sha256_异或 用 混合、yz。
完。

sha256_创建 接受 零参：
    可变 上下文 为 一组：（空）。
    做 追加 用 上下文、0x6a09e667。
    做 追加 用 上下文、0xbb67ae85。
    做 追加 用 上下文、0x3c6ef372。
    做 追加 用 上下文、0xa54ff53a。
    做 追加 用 上下文、0x510e527f。
    做 追加 用 上下文、0x9b05688c。
    做 追加 用 上下文、0x1f83d9ab。
    做 追加 用 上下文、0x5be0cd19。
    做 追加 用 上下文、0。
    做 追加 用 上下文、一组：（空）。
    返还 上下文。
完。

sha256_处理块 接受 上下文、块：
    可变 W 为 一组：（空）。
    可变 t 为 0。
    只要 t 小于 16：
        w 为 块 的 第 t 乘 4 项 乘 16777216 加 块 的 第 t 乘 4 加 1 项 乘 65536 加 块 的 第 t 乘 4 加 2 项 乘 256 加 块 的 第 t 乘 4 加 3 项。
        做 追加 用 W、w。
        t 为 t 加 1。
    完。
    只要 t 小于 64：
        s0 为 做 sha256_小s0 用 W 的 第 t 减 15 项。
        s1 为 做 sha256_小s1 用 W 的 第 t 减 2 项。
        做 追加 用 W、做 sha256_加 用 做 sha256_加 用 W 的 第 t 减 16 项、s0、做 sha256_加 用 W 的 第 t 减 7 项、s1。
        t 为 t 加 1。
    完。
    可变 a 为 上下文 的 第 0 项。
    可变 b 为 上下文 的 第 1 项。
    可变 c 为 上下文 的 第 2 项。
    可变 d 为 上下文 的 第 3 项。
    可变 e 为 上下文 的 第 4 项。
    可变 f 为 上下文 的 第 5 项。
    可变 g 为 上下文 的 第 6 项。
    可变 h 为 上下文 的 第 7 项。
    t 为 0。
    只要 t 小于 64：
        T1 为 做 sha256_加 用 做 sha256_加 用 做 sha256_加 用 做 sha256_加 用 h、做 sha256_大S1 用 e、做 SHA256_选择 用 e、f、g、做 sha256_获取K 用 t、W 的 第 t 项。
        T2 为 做 sha256_加 用 做 sha256_大S0 用 a、做 SHA256_多数 用 a、b、c。
        h 为 g。
        g 为 f。
        f 为 e。
        e 为 做 sha256_加 用 d、T1。
        d 为 c。
        c 为 b。
        b 为 a。
        a 为 做 sha256_加 用 T1、T2。
        t 为 t 加 1。
    完。
    上下文 的 第 0 项 为 做 sha256_加 用 上下文 的 第 0 项、a。
    上下文 的 第 1 项 为 做 sha256_加 用 上下文 的 第 1 项、b。
    上下文 的 第 2 项 为 做 sha256_加 用 上下文 的 第 2 项、c。
    上下文 的 第 3 项 为 做 sha256_加 用 上下文 的 第 3 项、d。
    上下文 的 第 4 项 为 做 sha256_加 用 上下文 的 第 4 项、e。
    上下文 的 第 5 项 为 做 sha256_加 用 上下文 的 第 5 项、f。
    上下文 的 第 6 项 为 做 sha256_加 用 上下文 的 第 6 项、g。
    上下文 的 第 7 项 为 做 sha256_加 用 上下文 的 第 7 项、h。
完。

sha256_缓冲追加 接受 上下文、值：
    可变 缓冲 为 上下文 的 第 9 项。
    做 追加 用 缓冲、值。
    若 做 计数 用 缓冲 等于 64：
        做 sha256_处理块 用 上下文、缓冲。
        上下文 的 第 9 项 为 一组：（空）。
    否则：
        上下文 的 第 9 项 为 缓冲。
    完。
完。

sha256_更新 接受 上下文、数据：
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 数据：
        做 sha256_缓冲追加 用 上下文、做 字节取 用 数据、i。
        i 为 i 加 1。
    完。
    上下文 的 第 8 项 为 上下文 的 第 8 项 加 做 字节长度 用 数据。
完。

sha256_完成 接受 上下文：
    可变 缓冲 为 上下文 的 第 9 项。
    总位数 为 上下文 的 第 8 项 乘 8。
    做 追加 用 缓冲、0x80。
    只要 做 计数 用 缓冲 取余于 64 不等于 56：
        做 追加 用 缓冲、0。
    完。
    做 追加 用 缓冲、0。
    做 追加 用 缓冲、0。
    做 追加 用 缓冲、0。
    做 追加 用 缓冲、0。
    做 追加 用 缓冲、总位数 除以 16777216 取余于 256。
    做 追加 用 缓冲、总位数 除以 65536 取余于 256。
    做 追加 用 缓冲、总位数 除以 256 取余于 256。
    做 追加 用 缓冲、总位数 取余于 256。
    可变 偏移 为 0。
    只要 偏移 小于 做 计数 用 缓冲：
        可变 块 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 64：
            做 追加 用 块、缓冲 的 第 偏移 加 j 项。
            j 为 j 加 1。
        完。
        做 sha256_处理块 用 上下文、块。
        偏移 为 偏移 加 64。
    完。
    可变 结果 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 8：
        h 为 上下文 的 第 k 项。
        做 追加 用 结果、h 除以 16777216 取余于 256。
        做 追加 用 结果、h 除以 65536 取余于 256。
        做 追加 用 结果、h 除以 256 取余于 256。
        做 追加 用 结果、h 取余于 256。
        k 为 k 加 1。
    完。
    返还 结果。
完。

sha256_哈希 接受 消息：
    上下文 为 做 sha256_创建。
    做 sha256_更新 用 上下文、消息。
    字节列表 为 做 sha256_完成 用 上下文。
    hex表 为 「0123456789abcdef」。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字节列表：
        b 为 字节列表 的 第 i 项。
        结果 为 做 字符串拼接 用 结果、做 子串 用 hex表、b 除以 16、1。
        结果 为 做 字符串拼接 用 结果、做 子串 用 hex表、b 取余于 16、1。
        i 为 i 加 1。
    完。
    返还 结果。
完。

SHA2_认证哈希 接受 密钥、消息：
    可变 内填充 为 一组：（空）。
    可变 外填充 为 一组：（空）。
    可变 ii 为 0。
    可变 key_bytes 为 一组：（空）。
    可变 ki 为 0。
    只要 ki 小于 做 字节长度 用 密钥：
        做 追加 用 key_bytes、做 字节取 用 密钥、ki。
        ki 为 ki 加 1。
    完。
    若 做 计数 用 key_bytes 大于 64：
        kctx 为 做 sha256_创建。
        做 sha256_更新 用 kctx、密钥。
        key_bytes 为 做 sha256_完成 用 kctx。
    完。
    只要 做 计数 用 key_bytes 小于 64：
        做 追加 用 key_bytes、0。
    完。
    可变 pi 为 0。
    只要 pi 小于 64：
        做 追加 用 内填充、做 sha256_异或 用 key_bytes 的 第 pi 项、0x36。
        做 追加 用 外填充、做 sha256_异或 用 key_bytes 的 第 pi 项、0x5c。
        pi 为 pi 加 1。
    完。
    内ctx 为 做 sha256_创建。
    只要 ii 小于 64：
        做 sha256_缓冲追加 用 内ctx、内填充 的 第 ii 项。
        ii 为 ii 加 1。
    完。
    内ctx 的 第 8 项 为 64。
    做 sha256_更新 用 内ctx、消息。
    内哈希 为 做 sha256_完成 用 内ctx。
    外ctx 为 做 sha256_创建。
    可变 oi 为 0。
    只要 oi 小于 64：
        做 sha256_缓冲追加 用 外ctx、外填充 的 第 oi 项。
        oi 为 oi 加 1。
    完。
    外ctx 的 第 8 项 为 64。
    可变 hi 为 0。
    只要 hi 小于 做 计数 用 内哈希：
        做 sha256_缓冲追加 用 外ctx、内哈希 的 第 hi 项。
        hi 为 hi 加 1。
    完。
    外ctx 的 第 8 项 为 外ctx 的 第 8 项 加 32。
    返还 做 sha256_完成 用 外ctx。
完。

可变 _sha_哈希次数 为 0。

可变 _sha_总字节 为 0。

可变 _sha_最大输入 为 0。

sha256_记录 接受 输入长度：
    _sha_哈希次数 为 _sha_哈希次数 加 1。
    _sha_总字节 为 _sha_总字节 加 输入长度。
    若 输入长度 大于 _sha_最大输入：
        _sha_最大输入 为 输入长度。
    完。
完。

sha256_平均输入 接受 零参：
    若 _sha_哈希次数 等于 0：
        返还 0。
    完。
    返还 _sha_总字节 除以 _sha_哈希次数。
完。

sha256_性能评估 接受 零参：
    若 _sha_哈希次数 等于 0：
        返还 「无哈希记录」。
    完。
    平均 为 做 sha256_平均输入。
    若 平均 大于 10000：
        返还 「大数据哈希: 考虑分块流式处理」。
    完。
    若 平均 大于 1000：
        返还 「中等数据哈希」。
    完。
    返还 「小数据哈希(高效)」。
完。

sha256_碰撞风险 接受 零参：
    若 _sha_哈希次数 小于 1000：
        返还 「极低(SHA256碰撞概率可忽略)」。
    完。
    若 _sha_哈希次数 小于 1000000：
        返还 「低」。
    完。
    返还 「理论安全(2^128碰撞阻力)」。
完。

sha256_吞吐评估 接受 零参：
    若 _sha_哈希次数 等于 0：
        返还 「无数据」。
    完。
    若 _sha_总字节 大于 10000000：
        返还 「高吞吐(已处理>10MB)」。
    完。
    若 _sha_总字节 大于 1000000：
        返还 「中等吞吐(>1MB)」。
    完。
    返还 「低吞吐」。
完。

sha256_安全等级 接受 零参：
    返还 「SHA-256: 256位摘要,128位碰撞阻力,推荐用于一般安全场景」。
完。

本章 收束。
