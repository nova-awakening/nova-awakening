《消息摘要5》

引用《..·基础》。

摘要_初始A 为 1732584193。

摘要_初始B 为 4023233417。

摘要_初始C 为 2562383102。

摘要_初始D 为 271733878。

摘要_模数 为 4294967296。

可变 摘要_处理字节数 为 0。

可变 摘要_处理块数 为 0。

可变 摘要_调用次数 为 0。

摘要_与 接受 a、b：
    返还 a 位与 b。
完。

摘要_或 接受 a、b：
    返还 a 位或 b。
完。

摘要_异或 接受 a、b：
    返还 a 位异或 b。
完。

摘要_非 接受 a：
    返还 摘要_模数 减 1 减 a 取余于 摘要_模数。
完。

摘要_右移 接受 a、n：
    可变 v 为 a 取余于 摘要_模数。
    可变 i 为 0。
    只要 i 小于 n：
        v 为 v 除以 2。
        i 为 i 加 1。
    完。
    返还 v。
完。

摘要_左移 接受 a、n：
    可变 v 为 a 取余于 摘要_模数。
    可变 i 为 0。
    只要 i 小于 n：
        v 为 v 乘 2。
        i 为 i 加 1。
    完。
    返还 v 取余于 摘要_模数。
完。

摘要_左旋 接受 a、n：
    返还 做 摘要_或 用 做 摘要_左移 用 a、n、做 摘要_右移 用 a、32 减 n。
完。

摘要_函数F 接受 b、c、d：
    返还 做 摘要_或 用 做 摘要_与 用 b、c、做 摘要_与 用 做 摘要_非 用 b、d。
完。

摘要_函数G 接受 b、c、d：
    返还 做 摘要_或 用 做 摘要_与 用 b、d、做 摘要_与 用 c、做 摘要_非 用 d。
完。

摘要_函数H 接受 b、c、d：
    返还 做 摘要_异或 用 b、做 摘要_异或 用 c、d。
完。

摘要_函数I 接受 b、c、d：
    返还 做 摘要_异或 用 c、做 摘要_或 用 b、做 摘要_非 用 d。
完。

摘要_轮操作 接受 a、b、f值、消息字、_常量值、移位：
    可变 结果 为 (a 加 f值 加 消息字 加 _常量值) 取余于 摘要_模数。
    结果 为 做 摘要_左旋 用 结果、移位。
    结果 为 (结果 加 b) 取余于 摘要_模数。
    返还 结果。
完。

摘要_常量 接受 i：
    可变 表 为 一组：3614090360、3905402710、606105819、3250441966、4118548399、1200080426、2821735955、4249261313、1770035416、2336552879、4294925233、2304563134、1804603682、4254626195、2792965006、1236535329、4129170786、3225465664、643717713、3921069994、3593408605、38016083、3634488961、3889429448、568446438、3275163606、4107603335、1163531501、2850285829、4243563512、1735328473、2368359562、4294588738、2272392833、1839030562、4259657740、2763975236、1272893353、4139469664、3200236656、681279174、3936430074、3572445317、76029189、3654602809、3873151461、530742520、3299628645、4096336452、1126891415、2878612391、4237533241、1700485571、2399980690、4293915773、2240044497、1873313359、4264355552、2734768916、1309151649、4149444226、3174756917、718787259、3951481745。
    若 i 大于或等于 0 并且 i 小于 64：
        返还 表 的 第 i 项。
    完。
    返还 0。
完。

摘要_移位 接受 i：
    可变 表 为 一组：7、12、17、22、7、12、17、22、7、12、17、22、7、12、17、22、5、9、14、20、5、9、14、20、5、9、14、20、5、9、14、20、4、11、16、23、4、11、16、23、4、11、16、23、4、11、16、23、6、10、15、21、6、10、15、21、6、10、15、21、6、10、15、21。
    若 i 大于或等于 0 并且 i 小于 64：
        返还 表 的 第 i 项。
    完。
    返还 0。
完。

摘要_填充 接受 数据：
    原始长度 为 做 字节长度 用 数据。
    可变 填充数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 原始长度：
        做 追加 用 填充数据、做 字节取 用 数据、i。
        i 为 i 加 1。
    完。
    做 追加 用 填充数据、128。
    只要 做 计数 用 填充数据 取余于 64 不等于 56：
        做 追加 用 填充数据、0。
    完。
    位长度 为 原始长度 乘 8。
    做 追加 用 填充数据、位长度 取余于 256。
    做 追加 用 填充数据、位长度 除以 256 取余于 256。
    做 追加 用 填充数据、位长度 除以 65536 取余于 256。
    做 追加 用 填充数据、位长度 除以 16777216 取余于 256。
    做 追加 用 填充数据、0。
    做 追加 用 填充数据、0。
    做 追加 用 填充数据、0。
    做 追加 用 填充数据、0。
    返还 填充数据。
完。

摘要_组装字 接受 数据、偏移：
    返还 数据 的 第 偏移 项 加 数据 的 第 偏移 加 1 项 乘 256 加 数据 的 第 偏移 加 2 项 乘 65536 加 数据 的 第 偏移 加 3 项 乘 16777216。
完。

md5_计算 接受 数据：
    摘要_调用次数 为 摘要_调用次数 加 1。
    填充 为 做 摘要_填充 用 数据。
    可变 a0 为 摘要_初始A。
    可变 b0 为 摘要_初始B。
    可变 c0 为 摘要_初始C。
    可变 d0 为 摘要_初始D。
    可变 块偏移 为 0。
    只要 块偏移 小于 做 计数 用 填充：
        可变 消息字 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 16：
            做 追加 用 消息字、做 摘要_组装字 用 填充、块偏移 加 j 乘 4。
            j 为 j 加 1。
        完。
        可变 a 为 a0。
        可变 b 为 b0。
        可变 c 为 c0。
        可变 d 为 d0。
        可变 i 为 0。
        只要 i 小于 64：
            可变 f值 为 0。
            可变 g 为 0。
            若 i 小于 16：
                f值 为 做 摘要_函数F 用 b、c、d。
                g 为 i。
            否则：
                若 i 小于 32：
                    f值 为 做 摘要_函数G 用 b、c、d。
                    g 为 (5 乘 i 加 1) 取余于 16。
                否则：
                    若 i 小于 48：
                        f值 为 做 摘要_函数H 用 b、c、d。
                        g 为 (3 乘 i 加 5) 取余于 16。
                    否则：
                        f值 为 做 摘要_函数I 用 b、c、d。
                        g 为 7 乘 i 取余于 16。
                    完。
                完。
            完。
            暂存 为 d。
            d 为 c。
            c 为 b。
            b 为 做 摘要_轮操作 用 a、b、f值、消息字 的 第 g 项、做 摘要_常量 用 i、做 摘要_移位 用 i。
            a 为 暂存。
            i 为 i 加 1。
        完。
        a0 为 (a0 加 a) 取余于 摘要_模数。
        b0 为 (b0 加 b) 取余于 摘要_模数。
        c0 为 (c0 加 c) 取余于 摘要_模数。
        d0 为 (d0 加 d) 取余于 摘要_模数。
        摘要_处理块数 为 摘要_处理块数 加 1。
        块偏移 为 块偏移 加 64。
    完。
    摘要_处理字节数 为 摘要_处理字节数 加 做 字节长度 用 数据。
    返还 一组：a0、b0、c0、d0。
完。

摘要_字转十六进制 接受 值：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 4：
        字节 为 值 除以 (1 左移 i 乘 8) 取余于 256。
        结果 为 结果 加 做 子串 用 十六进制表、字节 除以 16、1 加 做 子串 用 十六进制表、字节 取余于 16、1。
        i 为 i 加 1。
    完。
    返还 结果。
完。

md5_十六进制 接受 数据：
    哈希 为 做 md5_计算 用 数据。
    返还 做 摘要_字转十六进制 用 哈希 的 第 0 项 加 做 摘要_字转十六进制 用 哈希 的 第 1 项 加 做 摘要_字转十六进制 用 哈希 的 第 2 项 加 做 摘要_字转十六进制 用 哈希 的 第 3 项。
完。

md5_校验 接受 数据、期望值：
    返还 做 md5_十六进制 用 数据 等于 期望值。
完。

md5_碰撞概率 接受 样本数：
    若 样本数 小于或等于 0：
        返还 0。
    完。
    可变 对数估算 为 0。
    可变 n 为 样本数。
    只要 n 大于 1：
        对数估算 为 对数估算 加 1。
        n 为 n 除以 2。
    完。
    碰撞位 为 对数估算 乘 2。
    若 碰撞位 大于或等于 128：
        返还 100。
    完。
    返还 碰撞位 乘 100 除以 128。
完。

md5_雪崩测试 接受 数据：
    原始 为 做 md5_计算 用 数据。
    修改数据 为 数据 加 「x」。
    修改后 为 做 md5_计算 用 修改数据。
    可变 差异位 为 0。
    可变 i 为 0。
    只要 i 小于 4：
        可变 异或值 为 做 摘要_异或 用 原始 的 第 i 项、修改后 的 第 i 项。
        只要 异或值 大于 0：
            若 异或值 取余于 2 等于 1：
                差异位 为 差异位 加 1。
            完。
            异或值 为 异或值 除以 2。
        完。
        i 为 i 加 1。
    完。
    返还 差异位 乘 100 除以 128。
完。

md5_统计报告 接受 零参：
    返还 一组：摘要_调用次数、摘要_处理字节数、摘要_处理块数。
完。

md5_创建上下文 接受 零参：
    返还 一组：摘要_初始A、摘要_初始B、摘要_初始C、摘要_初始D、一组：（空）、0、假。
完。

md5_上下文_A 接受 上下文：
    返还 上下文 的 第 0 项。
完。

md5_上下文_B 接受 上下文：
    返还 上下文 的 第 1 项。
完。

md5_上下文_C 接受 上下文：
    返还 上下文 的 第 2 项。
完。

md5_上下文_D 接受 上下文：
    返还 上下文 的 第 3 项。
完。

md5_上下文_缓冲 接受 上下文：
    返还 上下文 的 第 4 项。
完。

md5_上下文_总长 接受 上下文：
    返还 上下文 的 第 5 项。
完。

md5_上下文_已完成 接受 上下文：
    返还 上下文 的 第 6 项。
完。

md5_更新 接受 上下文、数据：
    若 上下文 的 第 6 项：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 数据：
        做 追加 用 上下文 的 第 4 项、做 字节取 用 数据、i。
        上下文 的 第 5 项 为 上下文 的 第 5 项 加 1。
        若 做 计数 用 上下文 的 第 4 项 等于 64：
            做 md5_处理块 用 上下文。
            上下文 的 第 4 项 为 一组：（空）。
        完。
        i 为 i 加 1。
    完。
完。

md5_处理块 接受 上下文：
    可变 消息字 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 16：
        做 追加 用 消息字、做 摘要_组装字 用 上下文 的 第 4 项、j 乘 4。
        j 为 j 加 1。
    完。
    可变 a 为 上下文 的 第 0 项。
    可变 b 为 上下文 的 第 1 项。
    可变 c 为 上下文 的 第 2 项。
    可变 d 为 上下文 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 64：
        可变 f值 为 0。
        可变 g 为 0。
        若 i 小于 16：
            f值 为 做 摘要_函数F 用 b、c、d。
            g 为 i。
        否则：
            若 i 小于 32：
                f值 为 做 摘要_函数G 用 b、c、d。
                g 为 (5 乘 i 加 1) 取余于 16。
            否则：
                若 i 小于 48：
                    f值 为 做 摘要_函数H 用 b、c、d。
                    g 为 (3 乘 i 加 5) 取余于 16。
                否则：
                    f值 为 做 摘要_函数I 用 b、c、d。
                    g 为 7 乘 i 取余于 16。
                完。
            完。
        完。
        暂存 为 d。
        d 为 c。
        c 为 b。
        b 为 做 摘要_轮操作 用 a、b、f值、消息字 的 第 g 项、做 摘要_常量 用 i、做 摘要_移位 用 i。
        a 为 暂存。
        i 为 i 加 1。
    完。
    上下文 的 第 0 项 为 (上下文 的 第 0 项 加 a) 取余于 摘要_模数。
    上下文 的 第 1 项 为 (上下文 的 第 1 项 加 b) 取余于 摘要_模数。
    上下文 的 第 2 项 为 (上下文 的 第 2 项 加 c) 取余于 摘要_模数。
    上下文 的 第 3 项 为 (上下文 的 第 3 项 加 d) 取余于 摘要_模数。
完。

md5_完成 接受 上下文：
    若 上下文 的 第 6 项：
        返还 一组：上下文 的 第 0 项、上下文 的 第 1 项、上下文 的 第 2 项、上下文 的 第 3 项。
    完。
    原始长度 为 上下文 的 第 5 项。
    做 追加 用 上下文 的 第 4 项、128。
    只要 做 计数 用 上下文 的 第 4 项 取余于 64 不等于 56：
        做 追加 用 上下文 的 第 4 项、0。
    完。
    位长度 为 原始长度 乘 8。
    做 追加 用 上下文 的 第 4 项、位长度 取余于 256。
    做 追加 用 上下文 的 第 4 项、位长度 除以 256 取余于 256。
    做 追加 用 上下文 的 第 4 项、位长度 除以 65536 取余于 256。
    做 追加 用 上下文 的 第 4 项、位长度 除以 16777216 取余于 256。
    做 追加 用 上下文 的 第 4 项、0。
    做 追加 用 上下文 的 第 4 项、0。
    做 追加 用 上下文 的 第 4 项、0。
    做 追加 用 上下文 的 第 4 项、0。
    只要 做 计数 用 上下文 的 第 4 项 大于或等于 64：
        可变 块 为 一组：（空）。
        可变 k 为 0。
        只要 k 小于 64：
            做 追加 用 块、上下文 的 第 4 项 的 第 k 项。
            k 为 k 加 1。
        完。
        可变 剩余 为 一组：（空）。
        k 为 64。
        只要 k 小于 做 计数 用 上下文 的 第 4 项：
            做 追加 用 剩余、上下文 的 第 4 项 的 第 k 项。
            k 为 k 加 1。
        完。
        上下文 的 第 4 项 为 块。
        做 md5_处理块 用 上下文。
        上下文 的 第 4 项 为 剩余。
    完。
    上下文 的 第 6 项 为 真。
    返还 一组：上下文 的 第 0 项、上下文 的 第 1 项、上下文 的 第 2 项、上下文 的 第 3 项。
完。

md5_完成_十六进制 接受 上下文：
    哈希 为 做 md5_完成 用 上下文。
    返还 做 摘要_字转十六进制 用 哈希 的 第 0 项 加 做 摘要_字转十六进制 用 哈希 的 第 1 项 加 做 摘要_字转十六进制 用 哈希 的 第 2 项 加 做 摘要_字转十六进制 用 哈希 的 第 3 项。
完。

HMAC_MD5认证 接受 密钥、消息：
    可变 密钥块 为 一组：（空）。
    可变 i 为 0。
    若 做 字节长度 用 密钥 大于 64：
        哈希密钥 为 做 md5_计算 用 密钥。
        只要 i 小于 4：
            v 为 哈希密钥 的 第 i 项。
            做 追加 用 密钥块、v 取余于 256。
            做 追加 用 密钥块、v 除以 256 取余于 256。
            做 追加 用 密钥块、v 除以 65536 取余于 256。
            做 追加 用 密钥块、v 除以 16777216 取余于 256。
            i 为 i 加 1。
        完。
    否则：
        只要 i 小于 做 字节长度 用 密钥：
            做 追加 用 密钥块、做 字节取 用 密钥、i。
            i 为 i 加 1。
        完。
    完。
    只要 做 计数 用 密钥块 小于 64：
        做 追加 用 密钥块、0。
    完。
    可变 内层填充 为 一组：（空）。
    可变 外层填充 为 一组：（空）。
    i 为 0。
    只要 i 小于 64：
        做 追加 用 内层填充、做 摘要_异或 用 密钥块 的 第 i 项、54。
        做 追加 用 外层填充、做 摘要_异或 用 密钥块 的 第 i 项、92。
        i 为 i 加 1。
    完。
    可变 内层上下文 为 做 md5_创建上下文。
    i 为 0。
    只要 i 小于 64：
        做 追加 用 内层上下文 的 第 4 项、内层填充 的 第 i 项。
        内层上下文 的 第 5 项 为 内层上下文 的 第 5 项 加 1。
        若 做 计数 用 内层上下文 的 第 4 项 等于 64：
            做 md5_处理块 用 内层上下文。
            内层上下文 的 第 4 项 为 一组：（空）。
        完。
        i 为 i 加 1。
    完。
    做 md5_更新 用 内层上下文、消息。
    内层哈希 为 做 md5_完成 用 内层上下文。
    可变 外层上下文 为 做 md5_创建上下文。
    i 为 0。
    只要 i 小于 64：
        做 追加 用 外层上下文 的 第 4 项、外层填充 的 第 i 项。
        外层上下文 的 第 5 项 为 外层上下文 的 第 5 项 加 1。
        若 做 计数 用 外层上下文 的 第 4 项 等于 64：
            做 md5_处理块 用 外层上下文。
            外层上下文 的 第 4 项 为 一组：（空）。
        完。
        i 为 i 加 1。
    完。
    可变 内层字节 为 一组：（空）。
    i 为 0。
    只要 i 小于 4：
        v 为 内层哈希 的 第 i 项。
        做 追加 用 内层字节、v 取余于 256。
        做 追加 用 内层字节、v 除以 256 取余于 256。
        做 追加 用 内层字节、v 除以 65536 取余于 256。
        做 追加 用 内层字节、v 除以 16777216 取余于 256。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 16：
        做 追加 用 外层上下文 的 第 4 项、内层字节 的 第 i 项。
        外层上下文 的 第 5 项 为 外层上下文 的 第 5 项 加 1。
        若 做 计数 用 外层上下文 的 第 4 项 等于 64：
            做 md5_处理块 用 外层上下文。
            外层上下文 的 第 4 项 为 一组：（空）。
        完。
        i 为 i 加 1。
    完。
    结果 为 做 md5_完成 用 外层上下文。
    返还 做 摘要_字转十六进制 用 结果 的 第 0 项 加 做 摘要_字转十六进制 用 结果 的 第 1 项 加 做 摘要_字转十六进制 用 结果 的 第 2 项 加 做 摘要_字转十六进制 用 结果 的 第 3 项。
完。

可变 碰撞检测_哈希表 为 一组：（空）。

可变 碰撞检测_数量 为 0。

碰撞检测_重置 接受 零参：
    碰撞检测_哈希表 为 一组：（空）。
    碰撞检测_数量 为 0。
完。

碰撞检测_添加 接受 哈希值：
    可变 i 为 0。
    只要 i 小于 碰撞检测_数量：
        若 碰撞检测_哈希表 的 第 i 项 等于 哈希值：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 碰撞检测_哈希表、哈希值。
    碰撞检测_数量 为 碰撞检测_数量 加 1。
    返还 假。
完。

md5_批量计算 接受 数据列表：
    可变 结果 为 一组：（空）。
    可变 碰撞数 为 0。
    做 碰撞检测_重置。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据列表：
        哈希 为 做 md5_十六进制 用 数据列表 的 第 i 项。
        做 追加 用 结果、哈希。
        若 做 碰撞检测_添加 用 哈希：
            碰撞数 为 碰撞数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：结果、碰撞数。
完。

md5_分布检测 接受 哈希列表：
    可变 桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 16：
        做 追加 用 桶、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 哈希列表：
        首字符码 为 做 编码 用 做 子串 用 哈希列表 的 第 i 项、0、1。
        可变 桶号 为 0。
        若 首字符码 大于或等于 48 并且 首字符码 小于或等于 57：
            桶号 为 首字符码 减 48。
        完。
        若 首字符码 大于或等于 97 并且 首字符码 小于或等于 102：
            桶号 为 首字符码 减 87。
        完。
        桶 的 第 桶号 项 为 桶 的 第 桶号 项 加 1。
        i 为 i 加 1。
    完。
    可变 期望 为 做 计数 用 哈希列表 除以 16。
    若 期望 等于 0：
        期望 为 1。
    完。
    可变 最大偏差 为 0。
    i 为 0。
    只要 i 小于 16：
        可变 偏差 为 桶 的 第 i 项 减 期望。
        若 偏差 小于 0：
            偏差 为 0 减 偏差。
        完。
        若 偏差 大于 最大偏差：
            最大偏差 为 偏差。
        完。
        i 为 i 加 1。
    完。
    可变 均匀度 为 100 减 最大偏差 乘 100 除以 期望。
    若 均匀度 小于 0：
        均匀度 为 0。
    完。
    返还 均匀度。
完。

md5_相同 接受 哈希1、哈希2：
    若 做 计数 用 哈希1 不等于 4 或者 做 计数 用 哈希2 不等于 4：
        返还 假。
    完。
    返还 哈希1 的 第 0 项 等于 哈希2 的 第 0 项 并且 哈希1 的 第 1 项 等于 哈希2 的 第 1 项 并且 哈希1 的 第 2 项 等于 哈希2 的 第 2 项 并且 哈希1 的 第 3 项 等于 哈希2 的 第 3 项。
完。

md5_距离 接受 哈希1、哈希2：
    可变 距离 为 0。
    可变 i 为 0。
    只要 i 小于 4：
        可变 异或值 为 做 摘要_异或 用 哈希1 的 第 i 项、哈希2 的 第 i 项。
        只要 异或值 大于 0：
            若 异或值 取余于 2 等于 1：
                距离 为 距离 加 1。
            完。
            异或值 为 异或值 除以 2。
        完。
        i 为 i 加 1。
    完。
    返还 距离。
完。

本章 收束。
