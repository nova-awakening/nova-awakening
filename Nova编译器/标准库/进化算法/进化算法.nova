《进化算法》

引用《..·基础》。

引用《..·数学·遗传算法》。

进化_随机态 为 77777。

进化算法_进化_随机 接受 零参：
    进化_随机态 为 进化_随机态 位异或 进化_随机态 左移 13。
    进化_随机态 为 进化_随机态 位异或 进化_随机态 右移 7。
    进化_随机态 为 进化_随机态 位异或 进化_随机态 左移 17。
    若 进化_随机态 小于 0：
        进化_随机态 为 0 减 进化_随机态。
    完。
    返还 进化_随机态。
完。

进化算法_个体_创建 接受 基因长度、范围：
    可变 基因 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 基因长度：
        做 追加 用 基因、做 进化算法_进化_随机 取余于 范围。
        i 为 i 加 1。
    完。
    返还 一组：基因、0。
完。

个体_基因 接受 个体：
    返还 个体 的 第 0 项。
完。

个体_适应度 接受 个体：
    返还 个体 的 第 1 项。
完。

个体_设适应度 接受 个体、适应度：
    个体 的 第 1 项 为 适应度。
完。

进化算法_种群_创建 接受 大小、基因长度、范围：
    可变 种群 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        做 追加 用 种群、做 个体_创建 用 基因长度、范围、0、0。
        i 为 i 加 1。
    完。
    返还 种群。
完。

适应度_求和 接受 个体：
    总和 为 做 折叠 用 个体 的 第 0 项、0、以 s、x 映射到 s 加 x。
    个体 的 第 1 项 为 总和。
    返还 总和。
完。

进化算法_种群_评估 接受 种群：
    可变 i 为 0。
    只要 i 小于 做 计数 用 种群：
        做 适应度_求和 用 种群 的 第 i 项。
        i 为 i 加 1。
    完。
完。

种群_最佳 接受 种群：
    可变 最佳 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 种群：
        若 种群 的 第 i 项 的 第 1 项 大于 种群 的 第 最佳 项 的 第 1 项：
            最佳 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 种群 的 第 最佳 项。
完。

种群_平均适应度 接受 种群：
    若 做 计数 用 种群 等于 0：
        返还 0。
    完。
    返还 做 折叠 用 种群、0、以 s、ind 映射到 s 加 ind 的 第 1 项 除以 做 计数 用 种群。
完。

选择_锦标赛 接受 种群、锦标赛大小：
    可变 最佳 为 做 进化算法_进化_随机 取余于 做 计数 用 种群。
    可变 i 为 1。
    只要 i 小于 锦标赛大小：
        可变 候选 为 做 进化算法_进化_随机 取余于 做 计数 用 种群。
        若 种群 的 第 候选 项 的 第 1 项 大于 种群 的 第 最佳 项 的 第 1 项：
            最佳 为 候选。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

交叉_单点 接受 父1、父2：
    可变 n 为 做 计数 用 父1 的 第 0 项。
    可变 子基因 为 一组：（空）。
    可变 点 为 做 进化算法_进化_随机 取余于 n。
    若 点 等于 0：
        点 为 1。
    完。
    可变 i 为 0。
    只要 i 小于 点：
        做 追加 用 子基因、父1 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 i 小于 n：
        做 追加 用 子基因、父2 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 一组：子基因、0。
完。

变异 接受 个体、变异率、范围：
    可变 i 为 0。
    只要 i 小于 做 计数 用 个体 的 第 0 项：
        若 做 进化算法_进化_随机 取余于 100 小于 变异率：
            个体 的 第 0 项 的 第 i 项 为 做 进化算法_进化_随机 取余于 范围。
        完。
        i 为 i 加 1。
    完。
完。

精英_选取 接受 种群、精英数：
    可变 精英 为 一组：（空）。
    可变 选了 为 0。
    可变 已选 为 一组：（空）。
    只要 选了 小于 精英数：
        可变 最佳 为 负 1。
        可变 最佳适 为 0 减 0x7FFFFFFF。
        可变 i 为 0。
        只要 i 小于 做 计数 用 种群：
            可变 跳过 为 假。
            可变 j 为 0。
            只要 j 小于 做 计数 用 已选：
                若 已选 的 第 j 项 等于 i：
                    跳过 为 真。
                完。
                j 为 j 加 1。
            完。
            若 跳过 等于 假 并且 种群 的 第 i 项 的 第 1 项 大于 最佳适：
                最佳 为 i。
                最佳适 为 种群 的 第 i 项 的 第 1 项。
            完。
            i 为 i 加 1。
        完。
        若 最佳 大于或等于 0：
            做 追加 用 精英、种群 的 第 最佳 项。
            做 追加 用 已选、最佳。
        完。
        选了 为 选了 加 1。
    完。
    返还 精英。
完。

进化_一代 接受 种群、精英数、变异率、范围：
    做 种群_评估 用 种群、0。
    可变 精英 为 做 精英_选取 用 种群、精英数。
    可变 新种群 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 精英：
        做 追加 用 新种群、精英 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 做 计数 用 新种群 小于 做 计数 用 种群：
        可变 p1 为 做 选择_锦标赛 用 种群、3。
        可变 p2 为 做 选择_锦标赛 用 种群、3。
        可变 子 为 做 交叉_单点 用 种群 的 第 p1 项、种群 的 第 p2 项。
        做 变异 用 子、变异率、范围。
        做 追加 用 新种群、子。
    完。
    返还 新种群。
完。

交叉_两点 接受 父1、父2：
    可变 n 为 做 计数 用 父1 的 第 0 项。
    可变 子基因 为 一组：（空）。
    可变 p1 为 做 进化算法_进化_随机 取余于 n。
    可变 p2 为 做 进化算法_进化_随机 取余于 n。
    若 p1 大于 p2：
        可变 临时 为 p1。
        p1 为 p2。
        p2 为 临时。
    完。
    若 p1 等于 p2：
        p2 为 p2 加 1。
        若 p2 大于或等于 n：
            p2 为 n 减 1。
        完。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 大于或等于 p1 并且 i 小于 p2：
            做 追加 用 子基因、父2 的 第 0 项 的 第 i 项。
        否则：
            做 追加 用 子基因、父1 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：子基因、0。
完。

交叉_均匀 接受 父1、父2：
    可变 n 为 做 计数 用 父1 的 第 0 项。
    可变 子基因 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 进化算法_进化_随机 取余于 2 等于 0：
            做 追加 用 子基因、父1 的 第 0 项 的 第 i 项。
        否则：
            做 追加 用 子基因、父2 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：子基因、0。
完。

选择_轮盘赌 接受 种群：
    可变 合计 为 做 折叠 用 种群、0、以 s、x 映射到 s 加 x 的 第 1 项。
    若 合计 小于或等于 0：
        返还 0。
    完。
    可变 目标 为 做 进化算法_进化_随机 取余于 合计。
    可变 累积 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 种群：
        累积 为 累积 加 种群 的 第 i 项 的 第 1 项。
        若 累积 大于 目标：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 做 计数 用 种群 减 1。
完。

进化算法_种群_多样性 接受 种群：
    若 做 计数 用 种群 小于 2：
        返还 0。
    完。
    可变 diff_sum 为 0。
    可变 pairs 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 种群：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 种群：
            可变 d 为 0。
            可变 k 为 0。
            只要 k 小于 做 计数 用 种群 的 第 i 项 的 第 0 项：
                可变 v 为 种群 的 第 i 项 的 第 0 项 的 第 k 项 减 种群 的 第 j 项 的 第 0 项 的 第 k 项。
                若 v 小于 0：
                    v 为 0 减 v。
                完。
                d 为 d 加 v。
                k 为 k 加 1。
            完。
            diff_sum 为 diff_sum 加 d。
            pairs 为 pairs 加 1。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 pairs 等于 0：
        返还 0。
    完。
    返还 diff_sum 除以 pairs。
完。

Pareto_支配 接受 目标A、目标B：
    可变 all_ge 为 真。
    可变 any_gt 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 目标A：
        若 目标A 的 第 i 项 小于 目标B 的 第 i 项：
            all_ge 为 假。
        完。
        若 目标A 的 第 i 项 大于 目标B 的 第 i 项：
            any_gt 为 真。
        完。
        i 为 i 加 1。
    完。
    若 all_ge 并且 any_gt：
        返还 真。
    完。
    返还 假。
完。

本章 收束。
