《能量模型》

引用《..·基础》。

引用《..·算法·数学扩展》。

EB_随机态 为 16180。

EB_随机 接受 零参：
    EB_随机态 为 EB_随机态 位异或 EB_随机态 左移 13。
    EB_随机态 为 EB_随机态 位异或 EB_随机态 右移 7。
    EB_随机态 为 EB_随机态 位异或 EB_随机态 左移 17。
    若 EB_随机态 小于 0：
        EB_随机态 为 0 减 EB_随机态。
    完。
    返还 EB_随机态。
完。

能量_二次 接受 x、W、b：
    n 为 做 计数 用 x。
    可变 E 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 n：
            若 i 乘 n 加 j 小于 做 计数 用 W：
                E 为 E 减 x 的 第 i 项 乘 W 的 第 i 乘 n 加 j 项 乘 x 的 第 j 项 除以 (2 乘 1000)。
            完。
            j 为 j 加 1。
        完。
        若 i 小于 做 计数 用 b：
            E 为 E 减 b 的 第 i 项 乘 x 的 第 i 项 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 E。
完。

能量_线性 接受 x、w、b：
    n 为 做 计数 用 x。
    可变 E 为 b。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 做 计数 用 w：
            E 为 E 加 w 的 第 i 项 乘 x 的 第 i 项 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 E。
完。

Hopfield_创建 接受 维度：
    可变 W 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度 乘 维度：
        做 追加 用 W、0。
        i 为 i 加 1。
    完。
    返还 一组：W、维度、一组：（空）。
完。

Hopfield_存储 接受 网络、模式：
    d 为 网络 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 d：
        可变 j 为 0。
        只要 j 小于 d：
            若 i 不等于 j：
                网络 的 第 0 项 的 第 i 乘 d 加 j 项 为 网络 的 第 0 项 的 第 i 乘 d 加 j 项 加 模式 的 第 i 项 乘 模式 的 第 j 项 除以 1000。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 网络 的 第 2 项、模式。
完。

Hopfield_回忆 接受 网络、输入、步数：
    d 为 网络 的 第 1 项。
    可变 状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d：
        若 i 小于 做 计数 用 输入：
            做 追加 用 状态、输入 的 第 i 项。
        否则：
            做 追加 用 状态、0。
        完。
        i 为 i 加 1。
    完。
    可变 步长 为 0。
    只要 步长 小于 步数：
        i 为 做 EB_随机 取余于 d。
        可变 h 为 0。
        可变 j 为 0。
        只要 j 小于 d：
            h 为 h 加 网络 的 第 0 项 的 第 i 乘 d 加 j 项 乘 状态 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        若 h 大于或等于 0：
            状态 的 第 i 项 为 1000。
        否则：
            状态 的 第 i 项 为 负 1000。
        完。
        步长 为 步长 加 1。
    完。
    返还 状态。
完。

Hopfield_能量 接受 网络、状态：
    返还 做 能量_二次 用 状态、网络 的 第 0 项、一组：（空）。
完。

现代Hopfield_回忆 接受 模式列表、查询、β：
    可变 指数组 为 一组：（空）。
    若 β 小于或等于 0：
        β 为 1000。
    完。
    M 为 做 计数 用 模式列表。
    若 M 等于 0：
        返还 查询。
    完。
    d 为 做 计数 用 查询。
    可变 分数 为 一组：（空）。
    可变 最大得分 为 0 减 0x7FFFFFFF。
    可变 i 为 0。
    只要 i 小于 M：
        可变 s 为 0。
        可变 j 为 0。
        只要 j 小于 d：
            若 j 小于 做 计数 用 模式列表 的 第 i 项：
                s 为 s 加 模式列表 的 第 i 项 的 第 j 项 乘 查询 的 第 j 项 除以 1000。
            完。
            j 为 j 加 1。
        完。
        s 为 β 乘 s 除以 1000。
        做 追加 用 分数、s。
        若 s 大于 最大得分：
            最大得分 为 s。
        完。
        i 为 i 加 1。
    完。
    可变 总 为 0。
    i 为 0。
    只要 i 小于 M：
        可变 e 为 1000 加 (分数 的 第 i 项 减 最大得分)。
        若 e 小于 1：
            e 为 1。
        完。
        做 追加 用 指数组、e。
        总 为 总 加 e。
        i 为 i 加 1。
    完。
    若 总 等于 0：
        总 为 1。
    完。
    可变 输出 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 d：
        做 追加 用 输出、0。
        j 为 j 加 1。
    完。
    i 为 0。
    只要 i 小于 M：
        α 为 指数组 的 第 i 项 乘 1000 除以 总。
        j 为 0。
        只要 j 小于 d：
            若 j 小于 做 计数 用 模式列表 的 第 i 项：
                输出 的 第 j 项 为 输出 的 第 j 项 加 α 乘 模式列表 的 第 i 项 的 第 j 项 除以 1000。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 输出。
完。

RBM_创建 接受 可见维度、隐藏维度：
    可变 W 为 一组：（空）。
    可变 a 为 一组：（空）。
    可变 b 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 可见维度 乘 隐藏维度：
        做 追加 用 W、做 EB_随机 取余于 200 减 100。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 可见维度：
        做 追加 用 a、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 隐藏维度：
        做 追加 用 b、0。
        i 为 i 加 1。
    完。
    返还 一组：W、a、b、可见维度、隐藏维度。
完。

RBM_隐藏给定可见 接受 rbm、v：
    nh 为 rbm 的 第 4 项。
    nv 为 rbm 的 第 3 项。
    可变 h 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 nh：
        可变 激活 为 rbm 的 第 2 项 的 第 j 项。
        可变 i 为 0。
        只要 i 小于 nv：
            激活 为 激活 加 rbm 的 第 0 项 的 第 i 乘 nh 加 j 项 乘 v 的 第 i 项 除以 1000。
            i 为 i 加 1。
        完。
        可变 信号值 为 500 加 激活 除以 4。
        若 信号值 小于 0：
            信号值 为 0。
        完。
        若 信号值 大于 1000：
            信号值 为 1000。
        完。
        若 做 EB_随机 取余于 1000 小于 信号值：
            做 追加 用 h、1000。
        否则：
            做 追加 用 h、0。
        完。
        j 为 j 加 1。
    完。
    返还 h。
完。

RBM_可见给定隐藏 接受 rbm、h：
    nv 为 rbm 的 第 3 项。
    nh 为 rbm 的 第 4 项。
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 nv：
        可变 激活 为 rbm 的 第 1 项 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 nh：
            激活 为 激活 加 rbm 的 第 0 项 的 第 i 乘 nh 加 j 项 乘 h 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        可变 信号值 为 500 加 激活 除以 4。
        若 信号值 小于 0：
            信号值 为 0。
        完。
        若 信号值 大于 1000：
            信号值 为 1000。
        完。
        若 做 EB_随机 取余于 1000 小于 信号值：
            做 追加 用 v、1000。
        否则：
            做 追加 用 v、0。
        完。
        i 为 i 加 1。
    完。
    返还 v。
完。

RBM_CD1_更新 接受 rbm、v_data、lr：
    nv 为 rbm 的 第 3 项。
    nh 为 rbm 的 第 4 项。
    h_data 为 做 RBM_隐藏给定可见 用 rbm、v_data。
    v_recon 为 做 RBM_可见给定隐藏 用 rbm、h_data。
    h_recon 为 做 RBM_隐藏给定可见 用 rbm、v_recon。
    可变 i 为 0。
    只要 i 小于 nv：
        可变 j 为 0。
        只要 j 小于 nh：
            正 为 v_data 的 第 i 项 乘 h_data 的 第 j 项 除以 1000。
            负 为 v_recon 的 第 i 项 乘 h_recon 的 第 j 项 除以 1000。
            rbm 的 第 0 项 的 第 i 乘 nh 加 j 项 为 rbm 的 第 0 项 的 第 i 乘 nh 加 j 项 加 lr 乘 (正 减 负) 除以 1000。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

朗之万_采样 接受 x、能量梯度、步长、步数：
    d 为 做 计数 用 x。
    可变 当前 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d：
        做 追加 用 当前、x 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 步长 为 0。
    只要 步长 小于 步数：
        i 为 0。
        只要 i 小于 d：
            可变 梯度 为 0。
            若 i 小于 做 计数 用 能量梯度：
                梯度 为 能量梯度 的 第 i 项。
            完。
            噪声 为 做 EB_随机 取余于 2000 减 1000。
            sqrt_步长 为 做 整数sqrt 用 步长 乘 2000。
            当前 的 第 i 项 为 当前 的 第 i 项 减 步长 乘 梯度 除以 1000 加 sqrt_步长 乘 噪声 除以 1000。
            i 为 i 加 1。
        完。
        步长 为 步长 加 1。
    完。
    返还 当前。
完。

分数匹配_损失 接受 预测分数、真实分数：
    n 为 做 计数 用 预测分数。
    可变 损失值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 真值 为 0。
        若 i 小于 做 计数 用 真实分数：
            真值 为 真实分数 的 第 i 项。
        完。
        d 为 预测分数 的 第 i 项 减 真值。
        损失值 为 损失值 加 d 乘 d 除以 1000。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 损失值 除以 n。
完。

本章 收束。
