《高级优化器》

引用《..·基础》。

引用《..·数学·优化器》。

引用《..·算法·数学扩展》。

可变 AdamW_一阶矩 为 一组：（空）。

可变 AdamW_二阶矩 为 一组：（空）。

可变 自适应矩估计W_步数 为 0。

自适应矩估计W_初始化 接受 维度：
    AdamW_一阶矩 为 一组：（空）。
    AdamW_二阶矩 为 一组：（空）。
    自适应矩估计W_步数 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 AdamW_一阶矩、0。
        做 追加 用 AdamW_二阶矩、0。
        i 为 i 加 1。
    完。
完。

自适应矩估计W_步 接受 参数、梯度、lr、衰减率1、衰减率2、极小值、wd：
    自适应矩估计W_步数 为 自适应矩估计W_步数 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        AdamW_一阶矩 的 第 i 项 为 衰减率1 乘 AdamW_一阶矩 的 第 i 项 除以 100 加 (100 减 衰减率1) 乘 梯度 的 第 i 项 除以 100。
        AdamW_二阶矩 的 第 i 项 为 衰减率2 乘 AdamW_二阶矩 的 第 i 项 除以 100 加 (100 减 衰减率2) 乘 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 100000。
        一阶矫正 为 AdamW_一阶矩 的 第 i 项 乘 100 除以 (100 减 衰减率1)。
        可变 v_sqrt 为 做 整数sqrt 用 AdamW_二阶矩 的 第 i 项 乘 100 除以 (100 减 衰减率2)。
        更新 为 lr 乘 一阶矫正 除以 (v_sqrt 加 极小值) 加 lr 乘 wd 乘 参数 的 第 i 项 除以 10000。
        做 追加 用 结果、参数 的 第 i 项 减 更新。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 Lion_动量 为 一组：（空）。

Lion_初始化 接受 维度：
    Lion_动量 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 Lion_动量、0。
        i 为 i 加 1。
    完。
完。

Lion_步 接受 参数、梯度、lr、衰减率1、衰减率2、wd：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        combo 为 衰减率1 乘 Lion_动量 的 第 i 项 除以 100 加 (100 减 衰减率1) 乘 梯度 的 第 i 项 除以 100。
        可变 sign_v 为 0。
        若 combo 大于 0：
            sign_v 为 1。
        否则若 combo 小于 0：
            sign_v 为 负 1。
        完。
        Lion_动量 的 第 i 项 为 衰减率2 乘 Lion_动量 的 第 i 项 除以 100 加 (100 减 衰减率2) 乘 梯度 的 第 i 项 除以 100。
        做 追加 用 结果、参数 的 第 i 项 减 lr 乘 sign_v 减 lr 乘 wd 乘 参数 的 第 i 项 除以 10000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 LAMB_一阶矩 为 一组：（空）。

可变 LAMB_二阶矩 为 一组：（空）。

可变 LAMB_步数 为 0。

LAMB_初始化 接受 维度：
    LAMB_一阶矩 为 一组：（空）。
    LAMB_二阶矩 为 一组：（空）。
    LAMB_步数 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 LAMB_一阶矩、0。
        做 追加 用 LAMB_二阶矩、0。
        i 为 i 加 1。
    完。
完。

LAMB_步 接受 参数、梯度、lr、衰减率1、衰减率2、极小值、wd：
    可变 更新列表 为 一组：（空）。
    LAMB_步数 为 LAMB_步数 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        LAMB_一阶矩 的 第 i 项 为 衰减率1 乘 LAMB_一阶矩 的 第 i 项 除以 100 加 (100 减 衰减率1) 乘 梯度 的 第 i 项 除以 100。
        LAMB_二阶矩 的 第 i 项 为 衰减率2 乘 LAMB_二阶矩 的 第 i 项 除以 100 加 (100 减 衰减率2) 乘 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 100000。
        一阶矫正 为 LAMB_一阶矩 的 第 i 项 乘 100 除以 (100 减 衰减率1)。
        可变 v_sqrt 为 做 整数sqrt 用 LAMB_二阶矩 的 第 i 项 乘 100 除以 (100 减 衰减率2)。
        adam_upd 为 一阶矫正 除以 (v_sqrt 加 极小值) 加 wd 乘 参数 的 第 i 项 除以 1000。
        做 追加 用 更新列表、adam_upd。
        i 为 i 加 1。
    完。
    可变 param_norm 为 0。
    可变 upd_norm 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 参数：
        param_norm 为 param_norm 加 参数 的 第 i 项 乘 参数 的 第 i 项 除以 1000。
        upd_norm 为 upd_norm 加 更新列表 的 第 i 项 乘 更新列表 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    param_norm 为 做 整数sqrt 用 param_norm。
    upd_norm 为 做 整数sqrt 用 upd_norm。
    可变 trust 为 1000。
    若 upd_norm 大于 0：
        trust 为 param_norm 乘 1000 除以 upd_norm。
    完。
    若 trust 大于 10000：
        trust 为 10000。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 参数：
        做 追加 用 结果、参数 的 第 i 项 减 lr 乘 trust 除以 1000 乘 更新列表 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 SGD_速度 为 一组：（空）。

随机梯度_初始化 接受 维度：
    SGD_速度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 SGD_速度、0。
        i 为 i 加 1。
    完。
完。

随机梯度_步 接受 参数、梯度、lr、动量、wd：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        g 为 梯度 的 第 i 项 加 wd 乘 参数 的 第 i 项 除以 1000。
        SGD_速度 的 第 i 项 为 动量 乘 SGD_速度 的 第 i 项 除以 1000 加 g。
        做 追加 用 结果、参数 的 第 i 项 减 lr 乘 SGD_速度 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

梯度中心化 接受 梯度：
    n 为 做 计数 用 梯度。
    若 n 等于 0：
        返还 梯度。
    完。
    均值 为 做 折叠 用 梯度、0、以 s、g 映射到 s 加 g 除以 n。
    返还 做 映射 用 梯度、以 g 映射到 g 减 均值。
完。

学习率_线性预热 接受 当前步、预热步数、初始lr、目标lr：
    若 当前步 大于或等于 预热步数：
        返还 目标lr。
    完。
    若 预热步数 等于 0：
        返还 0。
    完。
    返还 初始lr 加 (目标lr 减 初始lr) 乘 当前步 除以 预热步数。
完。

高级优化_学习率_余弦退火 接受 当前步、总步数、最小lr、最大lr：
    若 当前步 大于或等于 总步数：
        返还 最小lr。
    完。
    进度 为 当前步 乘 100 除以 总步数。
    可变 cos_approx 为 100 减 进度 乘 进度 乘 2 除以 100。
    若 cos_approx 小于 负 100：
        cos_approx 为 负 100。
    完。
    返还 最小lr 加 (最大lr 减 最小lr) 乘 (cos_approx 加 100) 除以 200。
完。

学习率_余弦重启 接受 当前步、周期长度、最小lr、最大lr：
    周期内步 为 当前步 取余于 周期长度。
    返还 做 学习率_余弦退火 用 周期内步、周期长度、最小lr、最大lr。
完。

学习率_线性衰减 接受 当前步、总步数、初始lr：
    若 当前步 大于或等于 总步数：
        返还 0。
    完。
    若 总步数 等于 0：
        返还 0。
    完。
    返还 初始lr 乘 (总步数 减 当前步) 除以 总步数。
完。

学习率_阶梯衰减 接受 当前epoch、衰减频率、衰减率、初始lr：
    衰减次 为 当前epoch 除以 衰减频率。
    可变 lr 为 初始lr。
    可变 i 为 0。
    只要 i 小于 衰减次：
        lr 为 lr 乘 衰减率 除以 100。
        i 为 i 加 1。
    完。
    返还 lr。
完。

学习率_多项式衰减 接受 当前步、总步数、初始lr、终lr、power：
    若 当前步 大于或等于 总步数：
        返还 终lr。
    完。
    比率 为 (总步数 减 当前步) 乘 1000 除以 总步数。
    可变 r 为 比率。
    可变 p 为 1。
    只要 p 小于 power：
        r 为 r 乘 比率 除以 1000。
        p 为 p 加 1。
    完。
    返还 (初始lr 减 终lr) 乘 r 除以 1000 加 终lr。
完。

学习率_是零 接受 lr：
    返还 lr 等于 0。
完。

AdaGrad_更新 接受 参数、梯度、累积平方、学习率：
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        累积平方 的 第 i 项 为 累积平方 的 第 i 项 加 梯度 的 第 i 项 乘 梯度 的 第 i 项。
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 梯度 的 第 i 项 除以 (累积平方 的 第 i 项 加 极小量)。
        i 为 i 加 1。
    完。
完。

均方根传播_更新 接受 参数、梯度、平方均值、学习率、衰减率：
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        平方均值 的 第 i 项 为 衰减率 乘 平方均值 的 第 i 项 除以 1000 加 (1000 减 衰减率) 乘 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 1000。
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 梯度 的 第 i 项 除以 (平方均值 的 第 i 项 加 极小量)。
        i 为 i 加 1。
    完。
完。

自适应矩估计W_更新 接受 参数、梯度、m、v、步数、学习率、权重衰减：
    衰减率1 为 900。
    衰减率2 为 999。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        m 的 第 i 项 为 衰减率1 乘 m 的 第 i 项 除以 1000 加 (1000 减 衰减率1) 乘 梯度 的 第 i 项 除以 1000。
        v 的 第 i 项 为 衰减率2 乘 v 的 第 i 项 除以 1000 加 (1000 减 衰减率2) 乘 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 1000。
        一阶矫正 为 m 的 第 i 项 乘 1000 除以 (1000 减 衰减率1)。
        二阶矫正 为 v 的 第 i 项 乘 1000 除以 (1000 减 衰减率2)。
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 一阶矫正 除以 (二阶矫正 加 极小量)。
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 权重衰减 乘 参数 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
完。

梯度累积_累加 接受 累积梯度、当前梯度：
    可变 i 为 0。
    只要 i 小于 做 计数 用 当前梯度：
        累积梯度 的 第 i 项 为 累积梯度 的 第 i 项 加 当前梯度 的 第 i 项。
        i 为 i 加 1。
    完。
完。

梯度累积_平均 接受 累积梯度、累积步数：
    可变 i 为 0。
    只要 i 小于 做 计数 用 累积梯度：
        累积梯度 的 第 i 项 为 累积梯度 的 第 i 项 除以 累积步数。
        i 为 i 加 1。
    完。
完。

梯度累积_清零 接受 累积梯度：
    可变 i 为 0。
    只要 i 小于 做 计数 用 累积梯度：
        累积梯度 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
完。

参数组_创建 接受 名称、参数列表、学习率、权重衰减：
    返还 一组：名称、参数列表、学习率、权重衰减。
完。

参数组_设学习率 接受 组、新lr：
    组 的 第 2 项 为 新lr。
完。

参数组_取学习率 接受 组：
    返还 组 的 第 2 项。
完。

梯度_范数 接受 梯度：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度：
        总和 为 总和 加 梯度 的 第 i 项 乘 梯度 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总和。
完。

梯度_最大值 接受 梯度：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度：
        绝对值 为 梯度 的 第 i 项。
        若 绝对值 小于 0：
            绝对值 为 0 减 绝对值。
        完。
        若 绝对值 大于 最大：
            最大 为 绝对值。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

梯度_是否爆炸 接受 梯度、阈值：
    返还 做 梯度_范数 用 梯度 大于 阈值 乘 阈值。
完。

梯度_是否消失 接受 梯度、阈值：
    返还 做 梯度_最大值 用 梯度 小于 阈值。
完。

本章 收束。
