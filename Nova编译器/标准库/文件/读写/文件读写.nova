《文件读写》

引用《..·..·基础》。

引用《..·..·..·运行时·原语桩》。

可变 _微软C运行库 为 0。

可变 _函数_打开 为 0。

可变 _函数_读 为 0。

可变 _函数_写 为 0。

可变 _函数_关闭 为 0。

可变 _函数_定位 为 0。

打开_二进制 为 0x0000。

打开_只读 为 0x0000。

打开_只写 为 0x0001。

打开_读写 为 0x0002。

文件读写_权限_读 为 0x0100。

文件读写_权限_写 为 0x0080。

权限_读写 为 0x0180。

定位_起始 为 0。

定位_当前 为 1。

定位_末尾 为 2。

_初始化文件库 接受 零参：
    若 _微软C运行库 等于 0：
        _微软C运行库 为 做 加载库 用 「msvcrt.dll」。
    完。
    若 _函数_打开 等于 0：
        _函数_打开 为 做 获取函数 用 _微软C运行库、「_open」。
    完。
    若 _函数_读 等于 0：
        _函数_读 为 做 获取函数 用 _微软C运行库、「_read」。
    完。
    若 _函数_写 等于 0：
        _函数_写 为 做 获取函数 用 _微软C运行库、「_write」。
    完。
    若 _函数_关闭 等于 0：
        _函数_关闭 为 做 获取函数 用 _微软C运行库、「_close」。
    完。
    若 _函数_定位 等于 0：
        _函数_定位 为 做 获取函数 用 _微软C运行库、「_lseeki64」。
    完。
完。

_文件_打开句柄 接受 路径、模式：
    做 _初始化文件库。
    可变 flags 为 打开_二进制。
    若 做 字符串等于 用 模式、「读」：
        flags 为 flags 位或 打开_只读。
    否则若 做 字符串等于 用 模式、「写」：
        flags 为 flags 位或 打开_只写 位或 打开_创建 位或 打开_截断。
    否则若 做 字符串等于 用 模式、「追加」：
        flags 为 flags 位或 打开_只写 位或 打开_创建 位或 打开_追加。
    否则：
        flags 为 flags 位或 打开_读写 位或 打开_创建。
    完。
    返还 做 调用指针 用 _函数_打开、路径、flags、权限_读写、0、0。
完。

_文件_清理读缓冲 接受 f：
    f 的 第 3 项 为 一组：（空）。
    f 的 第 4 项 为 0。
    f 的 第 5 项 为 0。
完。

_文件_同步位置 接受 f：
    若 f 的 第 7 项：
        做 文件_刷新 用 f。
    完。
    若 f 的 第 5 项 大于 f 的 第 4 项：
        做 调用指针 用 _函数_定位、f 的 第 2 项、f 的 第 11 项、定位_起始、0、0。
        做 _文件_清理读缓冲 用 f。
    完。
完。

_文件_补充读缓冲 接受 f：
    做 _初始化文件库。
    可变 size 为 f 的 第 8 项。
    若 size 小于或等于 0：
        size 为 4096。
    完。
    缓冲 为 做 缓冲区 用 size。
    n 为 做 调用指针 用 _函数_读、f 的 第 2 项、缓冲、size、0、0。
    若 n 小于或等于 0：
        做 _文件_清理读缓冲 用 f。
        返还 0。
    完。
    可变 列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 列表、做 内存读8 用 缓冲 加 i。
        i 为 i 加 1。
    完。
    f 的 第 3 项 为 列表。
    f 的 第 4 项 为 0。
    f 的 第 5 项 为 n。
    返还 n。
完。

_文件_读字节 接受 f：
    若 f 的 第 4 项 大于或等于 f 的 第 5 项：
        若 做 _文件_补充读缓冲 用 f 小于或等于 0：
            返还 0 减 1。
        完。
    完。
    b 为 f 的 第 3 项 的 第 f 的 第 4 项 项。
    f 的 第 4 项 为 f 的 第 4 项 加 1。
    f 的 第 11 项 为 f 的 第 11 项 加 1。
    返还 b。
完。

_文件_追加写字节 接受 f、b：
    做 追加 用 f 的 第 10 项、b。
    f 的 第 7 项 为 真。
    若 做 计数 用 f 的 第 10 项 大于或等于 f 的 第 8 项：
        若 做 文件_刷新 用 f 等于 假：
            返还 假。
        完。
    完。
    返还 真。
完。

文件_打开 接受 路径、模式、缓冲大小：
    可变 实际缓冲 为 缓冲大小。
    若 实际缓冲 小于或等于 0：
        实际缓冲 为 4096。
    完。
    fd 为 做 _文件_打开句柄 用 路径、模式。
    可变 已打开 为 真。
    若 fd 小于 0：
        已打开 为 假。
    完。
    可变 位置 为 0。
    若 已打开 并且 做 字符串等于 用 模式、「追加」：
        位置 为 做 调用指针 用 _函数_定位、fd、0、定位_末尾、0、0。
        若 位置 小于 0：
            位置 为 0。
        完。
    完。
    返还 一组：路径、模式、fd、一组：（空）、0、已打开、假、实际缓冲、「utf8」、一组：（空）、位置。
完。

文件_关闭 接受 f：
    若 f 的 第 6 项 等于 假：
        返还 假。
    完。
    做 _初始化文件库。
    若 f 的 第 7 项：
        做 文件_刷新 用 f。
    完。
    做 调用指针 用 _函数_关闭、f 的 第 2 项、0、0、0、0。
    f 的 第 6 项 为 假。
    返还 真。
完。

文件_已打开 接受 f：
    返还 f 的 第 6 项。
完。

文件_写 接受 f、数据：
    若 f 的 第 6 项 等于 假：
        返还 负 1。
    完。
    若 数据 等于 空：
        返还 0。
    完。
    做 _初始化文件库。
    若 f 的 第 5 项 大于 f 的 第 4 项：
        做 _文件_同步位置 用 f。
    否则：
        做 _文件_清理读缓冲 用 f。
    完。
    可变 写入数 为 0。
    若 做 是字符串 用 数据：
        可变 n 为 做 字节长度 用 数据。
        可变 i 为 0。
        只要 i 小于 n：
            若 做 _文件_追加写字节 用 f、做 字节取 用 数据、i 等于 假：
                返还 负 1。
            完。
            i 为 i 加 1。
            写入数 为 写入数 加 1。
        完。
    否则：
        可变 n2 为 做 计数 用 数据。
        可变 j 为 0。
        只要 j 小于 n2：
            若 做 _文件_追加写字节 用 f、数据 的 第 j 项 等于 假：
                返还 负 1。
            完。
            j 为 j 加 1。
            写入数 为 写入数 加 1。
        完。
    完。
    f 的 第 11 项 为 f 的 第 11 项 加 写入数。
    返还 写入数。
完。

文件_写行 接受 f、行：
    做 文件_写 用 f、行。
    做 文件_写 用 f、一组：10。
完。

文件_刷新 接受 f：
    若 f 的 第 6 项 等于 假：
        返还 假。
    完。
    若 f 的 第 7 项 等于 假：
        返还 真。
    完。
    做 _初始化文件库。
    可变 n 为 做 计数 用 f 的 第 10 项。
    若 n 小于或等于 0：
        f 的 第 7 项 为 假。
        返还 真。
    完。
    缓冲 为 做 缓冲区 用 n。
    可变 i 为 0。
    只要 i 小于 n：
        做 缓冲写 用 缓冲、i、f 的 第 10 项 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 已写 为 0。
    只要 已写 小于 n：
        可变 剩余 为 n 减 已写。
        wrote 为 做 调用指针 用 _函数_写、f 的 第 2 项、缓冲 加 已写、剩余、0、0。
        若 wrote 小于或等于 0：
            返还 假。
        完。
        已写 为 已写 加 wrote。
    完。
    f 的 第 10 项 为 一组：（空）。
    f 的 第 7 项 为 假。
    返还 真。
完。

文件_读 接受 f、字节数：
    若 f 的 第 6 项 等于 假：
        返还 空。
    完。
    若 字节数 小于或等于 0：
        返还 一组：（空）。
    完。
    若 f 的 第 7 项：
        做 文件_刷新 用 f。
    完。
    可变 结果 为 一组：（空）。
    可变 读取 为 0。
    可变 _读完 为 假。
    只要 读取 小于 字节数 并且 _读完 等于 假：
        若 f 的 第 4 项 大于或等于 f 的 第 5 项：
            若 做 _文件_补充读缓冲 用 f 小于或等于 0：
                _读完 为 真。
            完。
        完。
        只要 f 的 第 4 项 小于 f 的 第 5 项 并且 读取 小于 字节数：
            做 追加 用 结果、f 的 第 3 项 的 第 f 的 第 4 项 项。
            f 的 第 4 项 为 f 的 第 4 项 加 1。
            f 的 第 11 项 为 f 的 第 11 项 加 1。
            读取 为 读取 加 1。
        完。
    完。
    返还 结果。
完。

文件_读行 接受 f：
    若 f 的 第 6 项 等于 假：
        返还 空。
    完。
    若 f 的 第 7 项：
        做 文件_刷新 用 f。
    完。
    可变 行 为 一组：（空）。
    可变 _行完 为 假。
    只要 _行完 等于 假：
        字节 为 做 _文件_读字节 用 f。
        若 字节 小于 0：
            _行完 为 真。
        否则：
            若 字节 等于 字符_换行：
                返还 行。
            完。
            若 字节 不等于 字符_回车：
                做 追加 用 行、字节。
            完。
        完。
    完。
    若 做 计数 用 行 大于 0：
        返还 行。
    完。
    返还 空。
完。

文件_读全部 接受 f：
    若 f 的 第 6 项 等于 假：
        返还 空。
    完。
    可变 结果 为 一组：（空）。
    可变 块 为 做 文件_读 用 f、f 的 第 8 项。
    只要 块 不等于 空 并且 做 计数 用 块 大于 0：
        可变 i 为 0。
        只要 i 小于 做 计数 用 块：
            做 追加 用 结果、块 的 第 i 项。
            i 为 i 加 1。
        完。
        块 为 做 文件_读 用 f、f 的 第 8 项。
    完。
    返还 结果。
完。

文件_定位 接受 f、位置：
    可变 目标位置 为 位置。
    若 f 的 第 6 项 等于 假：
        返还 假。
    完。
    若 目标位置 小于 0：
        目标位置 为 0。
    完。
    做 _文件_同步位置 用 f。
    r 为 做 调用指针 用 _函数_定位、f 的 第 2 项、目标位置、定位_起始、0、0。
    若 r 小于 0：
        返还 假。
    完。
    f 的 第 11 项 为 目标位置。
    做 _文件_清理读缓冲 用 f。
    返还 真。
完。

文件_位置 接受 f：
    返还 f 的 第 11 项。
完。

文件_大小 接受 f：
    若 f 的 第 6 项 等于 假：
        返还 0。
    完。
    做 _初始化文件库。
    做 _文件_同步位置 用 f。
    cur 为 f 的 第 11 项。
    末尾 为 做 调用指针 用 _函数_定位、f 的 第 2 项、0、定位_末尾、0、0。
    做 调用指针 用 _函数_定位、f 的 第 2 项、cur、定位_起始、0、0。
    返还 末尾。
完。

文件_到末尾 接受 f：
    返还 做 文件_位置 用 f 大于或等于 做 文件_大小 用 f。
完。

行迭代器_创建 接受 f：
    返还 一组：f、0。
完。

行迭代器_下一行 接受 迭代器：
    f 为 迭代器 的 第 0 项。
    返还 做 文件_读行 用 f。
完。

行迭代器_有下一行 接受 迭代器：
    f 为 迭代器 的 第 0 项。
    返还 做 文件_到末尾 用 f 等于 假。
完。

分块读取器_创建 接受 f、块大小：
    可变 实际块大小 为 块大小。
    若 实际块大小 小于或等于 0：
        实际块大小 为 1024。
    完。
    返还 一组：f、实际块大小。
完。

分块读取器_下一块 接受 读取器：
    返还 做 文件_读 用 读取器 的 第 0 项、读取器 的 第 1 项。
完。

分块读取器_有下一块 接受 读取器：
    返还 做 文件_到末尾 用 读取器 的 第 0 项 等于 假。
完。

缓冲区_创建 接受 容量、字节序：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 一组：数据、容量、0、字节序。
完。

缓冲区_写u8 接受 缓冲、值：
    若 缓冲 的 第 3 项 大于或等于 缓冲 的 第 1 项：
        返还 假。
    完。
    缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 项 为 值 取余于 256。
    缓冲 的 第 3 项 为 缓冲 的 第 3 项 加 1。
    返还 真。
完。

缓冲区_写u16 接受 缓冲、值：
    若 缓冲 的 第 3 项 加 1 大于或等于 缓冲 的 第 1 项：
        返还 假。
    完。
    若 缓冲 的 第 4 项 等于 0：
        缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 项 为 值 除以 256 取余于 256。
        缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 加 1 项 为 值 取余于 256。
    否则：
        缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 项 为 值 取余于 256。
        缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 加 1 项 为 值 除以 256 取余于 256。
    完。
    缓冲 的 第 3 项 为 缓冲 的 第 3 项 加 2。
    返还 真。
完。

缓冲区_写u32 接受 缓冲、值：
    若 缓冲 的 第 3 项 加 3 大于或等于 缓冲 的 第 1 项：
        返还 假。
    完。
    若 缓冲 的 第 4 项 等于 0：
        缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 项 为 值 除以 16777216 取余于 256。
        缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 加 1 项 为 值 除以 65536 取余于 256。
        缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 加 2 项 为 值 除以 256 取余于 256。
        缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 加 3 项 为 值 取余于 256。
    否则：
        缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 项 为 值 取余于 256。
        缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 加 1 项 为 值 除以 256 取余于 256。
        缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 加 2 项 为 值 除以 65536 取余于 256。
        缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 加 3 项 为 值 除以 16777216 取余于 256。
    完。
    缓冲 的 第 3 项 为 缓冲 的 第 3 项 加 4。
    返还 真。
完。

缓冲区_写i64 接受 缓冲、值：
    若 缓冲 的 第 3 项 加 7 大于或等于 缓冲 的 第 1 项：
        返还 假。
    完。
    可变 v 为 值。
    若 缓冲 的 第 4 项 等于 1：
        可变 i 为 0。
        只要 i 小于 8：
            缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 加 i 项 为 v 取余于 256。
            v 为 v 除以 256。
            i 为 i 加 1。
        完。
    否则：
        可变 i2 为 7。
        只要 i2 大于或等于 0：
            缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 加 i2 项 为 v 取余于 256。
            v 为 v 除以 256。
            i2 为 i2 减 1。
        完。
    完。
    缓冲 的 第 3 项 为 缓冲 的 第 3 项 加 8。
    返还 真。
完。

缓冲区_读u8 接受 缓冲：
    若 缓冲 的 第 2 项 大于或等于 缓冲 的 第 3 项：
        返还 负 1。
    完。
    v 为 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 项。
    缓冲 的 第 2 项 为 缓冲 的 第 2 项 加 1。
    返还 v。
完。

缓冲区_读u16 接受 缓冲：
    若 缓冲 的 第 2 项 加 1 大于或等于 缓冲 的 第 3 项：
        返还 负 1。
    完。
    可变 v 为 0。
    若 缓冲 的 第 4 项 等于 0：
        v 为 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 项 乘 256 加 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 加 1 项。
    否则：
        v 为 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 项 加 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 加 1 项 乘 256。
    完。
    缓冲 的 第 2 项 为 缓冲 的 第 2 项 加 2。
    返还 v。
完。

缓冲区_读u32 接受 缓冲：
    若 缓冲 的 第 2 项 加 3 大于或等于 缓冲 的 第 3 项：
        返还 负 1。
    完。
    可变 v 为 0。
    若 缓冲 的 第 4 项 等于 0：
        v 为 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 项 乘 16777216 加 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 加 1 项 乘 65536 加 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 加 2 项 乘 256 加 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 加 3 项。
    否则：
        v 为 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 项 加 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 加 1 项 乘 256 加 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 加 2 项 乘 65536 加 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 加 3 项 乘 16777216。
    完。
    缓冲 的 第 2 项 为 缓冲 的 第 2 项 加 4。
    返还 v。
完。

缓冲区_剩余 接受 缓冲：
    返还 缓冲 的 第 3 项 减 缓冲 的 第 2 项。
完。

缓冲区_重置 接受 缓冲：
    缓冲 的 第 2 项 为 0。
    缓冲 的 第 3 项 为 0。
完。

缓冲区_翻转 接受 缓冲：
    缓冲 的 第 2 项 为 0。
完。

文件打开 接受 路径、模式：
    返还 做 文件_打开 用 路径、模式、4096。
完。

文件关闭 接受 f：
    返还 做 文件_关闭 用 f。
完。

文件写 接受 f、数据：
    返还 做 文件_写 用 f、数据。
完。

文件读行 接受 f：
    返还 做 文件_读行 用 f。
完。

文件读全部 接受 f：
    返还 做 文件_读全部 用 f。
完。

文件_存在 接受 路径：
    f 为 做 文件打开 用 路径、「r」。
    若 f 等于 空：
        返还 假。
    完。
    做 文件关闭 用 f。
    返还 真。
完。

文件_按路径读全部 接受 路径：
    f 为 做 文件打开 用 路径、「r」。
    若 f 等于 空：
        返还 空。
    完。
    可变 内容 为 「」。
    可变 行 为 做 文件读行 用 f。
    只要 行 不等于 空：
        内容 为 做 字符串拼接 用 内容、行。
        行 为 做 文件读行 用 f。
    完。
    做 文件关闭 用 f。
    返还 内容。
完。

文件_写全部 接受 路径、内容：
    f 为 做 文件打开 用 路径、「w」。
    若 f 等于 空：
        返还 假。
    完。
    做 文件写 用 f、内容。
    做 文件关闭 用 f。
    返还 真。
完。

文件_追加 接受 路径、内容：
    f 为 做 文件打开 用 路径、「a」。
    若 f 等于 空：
        返还 假。
    完。
    做 文件写 用 f、内容。
    做 文件关闭 用 f。
    返还 真。
完。

文件_读行列表 接受 路径：
    f 为 做 文件打开 用 路径、「r」。
    若 f 等于 空：
        返还 一组：（空）。
    完。
    可变 行列表 为 一组：（空）。
    可变 行 为 做 文件读行 用 f。
    只要 行 不等于 空：
        做 追加 用 行列表、行。
        行 为 做 文件读行 用 f。
    完。
    做 文件关闭 用 f。
    返还 行列表。
完。

文件_行数 接受 路径：
    返还 做 计数 用 做 文件_读行列表 用 路径。
完。

文件_复制 接受 源路径、目标路径：
    内容 为 做 文件_按路径读全部 用 源路径。
    若 内容 等于 空：
        返还 假。
    完。
    返还 做 文件_写全部 用 目标路径、内容。
完。

可变 _文件_读次数 为 0。

可变 _文件_写次数 为 0。

可变 _文件_总读字节 为 0。

可变 _文件_总写字节 为 0。

文件_记录读 接受 字节数：
    _文件_读次数 为 _文件_读次数 加 1。
    _文件_总读字节 为 _文件_总读字节 加 字节数。
完。

文件_记录写 接受 字节数：
    _文件_写次数 为 _文件_写次数 加 1。
    _文件_总写字节 为 _文件_总写字节 加 字节数。
完。

文件_IO模式评估 接受 零参：
    若 _文件_读次数 等于 0 并且 _文件_写次数 等于 0：
        返还 「无IO活动」。
    完。
    若 _文件_写次数 等于 0：
        返还 「纯读取模式」。
    完。
    若 _文件_读次数 等于 0：
        返还 「纯写入模式」。
    完。
    比 为 _文件_读次数 除以 _文件_写次数。
    若 比 大于 5：
        返还 「读密集型」。
    完。
    若 比 大于 1：
        返还 「读多写少」。
    完。
    返还 「写密集型」。
完。

文件_吞吐评估 接受 零参：
    总 为 _文件_总读字节 加 _文件_总写字节。
    若 总 大于 10000000：
        返还 「高吞吐(>10MB)」。
    完。
    若 总 大于 1000000：
        返还 「中等吞吐」。
    完。
    返还 「低吞吐」。
完。

本章 收束。
