《路径》

引用《..·..·基础》。

引用《..·..·文本·路径解析》。

路径_分隔符_UNIX 为 「/」。

路径_分隔符_WIN 为 「\\」。

路径_解析 接受 路径：
    n 为 做 计数 用 路径。
    若 n 等于 0：
        返还 一组：「」、「」、「」、「」。
    完。
    可变 最后分隔 为 负 1。
    可变 i 为 n 减 1。
    只要 i 大于或等于 0：
        c 为 做 子串 用 路径、i、1。
        若 做 字符串等于 用 c、「/」 或者 做 字符串等于 用 c、「\\」：
            最后分隔 为 i。
            止。
        完。
        i 为 i 减 1。
    完。
    可变 目录 为 「」。
    可变 文件名 为 「」。
    若 最后分隔 大于或等于 0：
        目录 为 做 子串 用 路径、0、最后分隔 加 1。
        文件名 为 做 子串 用 路径、最后分隔 加 1、n 减 最后分隔 减 1。
    否则：
        文件名 为 路径。
    完。
    fn 为 做 计数 用 文件名。
    可变 点位 为 负 1。
    i 为 fn 减 1。
    只要 i 大于 0：
        若 做 字符串等于 用 做 子串 用 文件名、i、1、「.」：
            点位 为 i。
            止。
        完。
        i 为 i 减 1。
    完。
    可变 基础名 为 文件名。
    可变 扩展名 为 「」。
    若 点位 大于 0：
        基础名 为 做 子串 用 文件名、0、点位。
        扩展名 为 做 子串 用 文件名、点位、fn 减 点位。
    完。
    返还 一组：目录、文件名、基础名、扩展名。
完。

路径_路径_目录 接受 路径：
    返还 做 路径_解析 用 路径 的 第 0 项。
完。

路径_路径_文件名 接受 路径：
    返还 做 路径_解析 用 路径 的 第 1 项。
完。

路径_基础名 接受 路径：
    返还 做 路径_解析 用 路径 的 第 2 项。
完。

路径_路径_扩展名 接受 路径：
    返还 做 路径_解析 用 路径 的 第 3 项。
完。

路径_路径_组合 接受 基路径、子路径：
    n 为 做 计数 用 基路径。
    若 n 等于 0：
        返还 子路径。
    完。
    最后字符 为 做 子串 用 基路径、n 减 1、1。
    若 做 字符串等于 用 最后字符、「/」 或者 做 字符串等于 用 最后字符、「\\」：
        返还 做 字符串拼接 用 基路径、子路径。
    完。
    返还 做 字符串拼接 用 基路径、做 字符串拼接 用 「/」、子路径。
完。

路径_多级组合 接受 部分列表：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 部分列表：
        若 i 等于 0：
            结果 为 部分列表 的 第 i 项。
        否则：
            结果 为 做 路径_路径_组合 用 结果、部分列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

路径_路径_规范化 接受 路径：
    可变 新栈 为 一组：（空）。
    可变 标准化 为 「」。
    n 为 做 计数 用 路径。
    可变 绝对 为 做 路径_路径_是绝对 用 路径。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 路径、i、1。
        若 做 字符串等于 用 c、「\\」：
            标准化 为 做 字符串拼接 用 标准化、「/」。
        否则：
            标准化 为 做 字符串拼接 用 标准化、c。
        完。
        i 为 i 加 1。
    完。
    组件 为 做 路径_分割 用 标准化。
    可变 栈 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 组件：
        部分 为 组件 的 第 i 项。
        若 做 字符串等于 用 部分、「..」：
            若 做 计数 用 栈 大于 0 并且 栈 的 第 做 计数 用 栈 减 1 项 不等于 「..」：
                可变 j 为 0。
                只要 j 小于 做 计数 用 栈 减 1：
                    做 追加 用 新栈、栈 的 第 j 项。
                    j 为 j 加 1。
                完。
                栈 为 新栈。
            否则若 绝对 等于 假：
                做 追加 用 栈、「..」。
            完。
        否则若 做 字符串等于 用 部分、「.」 等于 假 并且 做 计数 用 部分 大于 0：
            做 追加 用 栈、部分。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 「」。
    若 做 计数 用 标准化 大于 0 并且 做 字符串等于 用 做 子串 用 标准化、0、1、「/」：
        结果 为 「/」。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 栈：
        若 i 大于 0：
            结果 为 做 字符串拼接 用 结果、「/」。
        完。
        结果 为 做 字符串拼接 用 结果、栈 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

路径_分割 接受 路径：
    n 为 做 计数 用 路径。
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 路径、i、1。
        若 做 字符串等于 用 c、「/」：
            若 做 计数 用 当前 大于 0：
                做 追加 用 结果、当前。
                当前 为 「」。
            完。
        否则：
            当前 为 做 字符串拼接 用 当前、c。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前 大于 0：
        做 追加 用 结果、当前。
    完。
    返还 结果。
完。

路径_路径_是绝对 接受 路径：
    若 做 计数 用 路径 等于 0：
        返还 假。
    完。
    首字 为 做 子串 用 路径、0、1。
    若 做 字符串等于 用 首字、「/」：
        返还 真。
    完。
    若 做 计数 用 路径 大于或等于 3：
        第二 为 做 子串 用 路径、1、1。
        第三 为 做 子串 用 路径、2、1。
        若 做 字符串等于 用 第二、「:」 并且 (做 字符串等于 用 第三、「/」 或者 做 字符串等于 用 第三、「\\」)：
            返还 真。
        完。
    完。
    返还 假。
完。

路径_是相对 接受 路径：
    返还 做 路径_路径_是绝对 用 路径 等于 假。
完。

路径_路径_深度 接受 路径：
    返还 做 计数 用 做 路径_分割 用 做 路径_路径_规范化 用 路径。
完。

路径_更换扩展名 接受 路径、新扩展名：
    组件 为 做 路径_解析 用 路径。
    返还 做 字符串拼接 用 组件 的 第 0 项、做 字符串拼接 用 组件 的 第 2 项、新扩展名。
完。

路径_父目录 接受 路径：
    标准 为 做 路径_路径_规范化 用 路径。
    组件 为 做 路径_分割 用 标准。
    若 做 计数 用 组件 小于或等于 1：
        返还 「」。
    完。
    可变 结果 为 「」。
    若 做 路径_路径_是绝对 用 标准：
        结果 为 「/」。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 组件 减 1：
        若 i 大于 0：
            结果 为 做 字符串拼接 用 结果、「/」。
        完。
        结果 为 做 字符串拼接 用 结果、组件 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

路径_相对 接受 基路径、目标路径：
    基组件 为 做 路径_分割 用 做 路径_路径_规范化 用 基路径。
    目标组件 为 做 路径_分割 用 做 路径_路径_规范化 用 目标路径。
    可变 公共 为 0。
    可变 最小 为 做 计数 用 基组件。
    若 做 计数 用 目标组件 小于 最小：
        最小 为 做 计数 用 目标组件。
    完。
    只要 公共 小于 最小 并且 做 字符串等于 用 基组件 的 第 公共 项、目标组件 的 第 公共 项：
        公共 为 公共 加 1。
    完。
    可变 结果 为 「」。
    可变 上行 为 做 计数 用 基组件 减 公共。
    可变 i 为 0。
    只要 i 小于 上行：
        若 i 大于 0：
            结果 为 做 字符串拼接 用 结果、「/」。
        完。
        结果 为 做 字符串拼接 用 结果、「..」。
        i 为 i 加 1。
    完。
    i 为 公共。
    只要 i 小于 做 计数 用 目标组件：
        若 做 计数 用 结果 大于 0：
            结果 为 做 字符串拼接 用 结果、「/」。
        完。
        结果 为 做 字符串拼接 用 结果、目标组件 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

路径_匹配 接受 路径、模式：
    返还 做 路径_通配 用 路径、0、模式、0。
完。

路径_通配 接受 路径、pi、模式、mi：
    pn 为 做 计数 用 路径。
    mn 为 做 计数 用 模式。
    若 mi 大于或等于 mn：
        返还 pi 大于或等于 pn。
    完。
    mc 为 做 子串 用 模式、mi、1。
    若 做 字符串等于 用 mc、「*」：
        若 mi 加 1 小于 mn 并且 做 字符串等于 用 做 子串 用 模式、mi 加 1、1、「*」：
            可变 k 为 pi。
            只要 k 小于或等于 pn：
                若 做 路径_通配 用 路径、k、模式、mi 加 2：
                    返还 真。
                完。
                k 为 k 加 1。
            完。
            返还 假。
        完。
        可变 j 为 pi。
        只要 j 小于或等于 pn：
            若 做 路径_通配 用 路径、j、模式、mi 加 1：
                返还 真。
            完。
            若 j 小于 pn：
                pc 为 做 子串 用 路径、j、1。
                若 做 字符串等于 用 pc、「/」 或者 做 字符串等于 用 pc、「\\」：
                    返还 假。
                完。
            完。
            j 为 j 加 1。
        完。
        返还 假。
    完。
    若 做 字符串等于 用 mc、「?」：
        若 pi 大于或等于 pn：
            返还 假。
        完。
        返还 做 路径_通配 用 路径、pi 加 1、模式、mi 加 1。
    完。
    若 pi 大于或等于 pn：
        返还 假。
    完。
    若 做 字符串等于 用 做 子串 用 路径、pi、1、mc：
        返还 做 路径_通配 用 路径、pi 加 1、模式、mi 加 1。
    完。
    返还 假。
完。

本章 收束。
