《文件监控》

引用《..·..·基础》。

事件_创建 为 1。

事件_修改 为 2。

事件_删除 为 3。

事件_重命名 为 4。

事件_权限 为 5。

监控项_创建 接受 路径、是否目录：
    返还 一组：路径、0、0、是否目录、一组：（空）。
完。

监控项_更新快照 接受 项、哈希、大小、修改时间：
    项 的 第 1 项 为 哈希。
    项 的 第 2 项 为 大小。
    项 的 第 3 项 为 修改时间。
完。

监控器_创建 接受 轮询间隔、合并窗口：
    可变 实际间隔 为 轮询间隔。
    可变 实际窗口 为 合并窗口。
    若 实际间隔 小于或等于 0：
        实际间隔 为 100。
    完。
    若 实际窗口 小于或等于 0：
        实际窗口 为 50。
    完。
    返还 一组：一组：（空）、一组：（空）、实际间隔、假、一组：（空）、实际窗口、0、一组：（空）。
完。

监控_添加路径 接受 监控器、路径、是否目录：
    项 为 做 监控项_创建 用 路径、是否目录。
    做 追加 用 监控器 的 第 0 项、项。
    返还 做 计数 用 监控器 的 第 0 项 减 1。
完。

监控_添加回调 接受 监控器、事件类型、回调函数：
    做 追加 用 监控器 的 第 1 项、一组：事件类型、回调函数。
完。

监控_添加过滤器 接受 监控器、过滤函数：
    做 追加 用 监控器 的 第 7 项、过滤函数。
完。

监控_检测变更 接受 监控器、当前快照列表：
    可变 事件列表 为 一组：（空）。
    n 为 做 计数 用 监控器 的 第 0 项。
    若 做 计数 用 当前快照列表 不等于 n：
        返还 事件列表。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        项 为 监控器 的 第 0 项 的 第 i 项。
        快照 为 当前快照列表 的 第 i 项。
        新哈希 为 快照 的 第 0 项。
        新大小 为 快照 的 第 1 项。
        新时间 为 快照 的 第 2 项。
        存在 为 快照 的 第 3 项。
        若 项 的 第 1 项 等于 0 并且 存在：
            做 追加 用 事件列表、一组：事件_创建、项 的 第 0 项、新大小、新时间。
        否则若 项 的 第 1 项 不等于 0 并且 存在 等于 假：
            做 追加 用 事件列表、一组：事件_删除、项 的 第 0 项、项 的 第 2 项、项 的 第 3 项。
        否则若 存在 并且 新哈希 不等于 项 的 第 1 项：
            做 追加 用 事件列表、一组：事件_修改、项 的 第 0 项、新大小、新时间。
        否则若 存在 并且 新大小 不等于 项 的 第 2 项：
            做 追加 用 事件列表、一组：事件_修改、项 的 第 0 项、新大小、新时间。
        完。
        若 存在：
            做 监控项_更新快照 用 项、新哈希、新大小、新时间。
        否则：
            做 监控项_更新快照 用 项、0、0、0。
        完。
        i 为 i 加 1。
    完。
    返还 事件列表。
完。

监控_时序合并 接受 事件列表：
    可变 合并后 为 一组：（空）。
    可变 已处理 为 一组：（空）。
    n 为 做 计数 用 事件列表。
    可变 i 为 0。
    只要 i 小于 n：
        可变 跳过 为 假。
        可变 k 为 0。
        只要 k 小于 做 计数 用 已处理 并且 跳过 等于 假：
            若 已处理 的 第 k 项 等于 i：
                跳过 为 真。
            完。
            k 为 k 加 1。
        完。
        若 跳过：
            i 为 i 加 1。
            续。
        完。
        当前事件 为 事件列表 的 第 i 项。
        可变 最终事件 为 当前事件。
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 做 字符串等于 用 事件列表 的 第 j 项 的 第 1 项、当前事件 的 第 1 项：
                做 追加 用 已处理、j。
                若 最终事件 的 第 0 项 等于 事件_创建 并且 事件列表 的 第 j 项 的 第 0 项 等于 事件_删除：
                    最终事件 为 空。
                否则若 最终事件 的 第 0 项 等于 事件_创建 并且 事件列表 的 第 j 项 的 第 0 项 等于 事件_修改：
                否则若 事件列表 的 第 j 项 的 第 0 项 等于 事件_删除：
                    最终事件 为 事件列表 的 第 j 项。
                否则：
                    最终事件 为 事件列表 的 第 j 项。
                完。
            完。
            j 为 j 加 1。
        完。
        若 最终事件 不等于 空：
            做 追加 用 合并后、最终事件。
        完。
        i 为 i 加 1。
    完。
    返还 合并后。
完。

监控_分发事件 接受 监控器、事件列表：
    可变 分发数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 事件列表：
        事件 为 事件列表 的 第 i 项。
        可变 通过 为 真。
        可变 f 为 0。
        只要 f 小于 做 计数 用 监控器 的 第 7 项：
            若 做 监控器 的 第 7 项 的 第 f 项 用 事件 等于 假：
                通过 为 假。
                止。
            完。
            f 为 f 加 1。
        完。
        若 通过：
            可变 j 为 0。
            只要 j 小于 做 计数 用 监控器 的 第 1 项：
                注册 为 监控器 的 第 1 项 的 第 j 项。
                若 注册 的 第 0 项 等于 事件 的 第 0 项 或者 注册 的 第 0 项 等于 0：
                    做 注册 的 第 1 项 用 事件。
                    分发数 为 分发数 加 1。
                完。
                j 为 j 加 1。
            完。
            监控器 的 第 6 项 为 监控器 的 第 6 项 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 分发数。
完。

监控_轮询一次 接受 监控器、当前快照列表：
    原始事件 为 做 监控_检测变更 用 监控器、当前快照列表。
    合并事件 为 做 监控_合并事件 用 原始事件。
    返还 做 监控_分发事件 用 监控器、合并事件。
完。

监控_内容哈希 接受 数据：
    可变 哈希 为 2166136261。
    n 为 做 计数 用 数据。
    可变 i 为 0。
    只要 i 小于 n：
        可变 字节 为 数据 的 第 i 项。
        若 字节 小于 0：
            字节 为 字节 加 256。
        完。
        哈希 为 哈希 乘 16777619 取余于 2147483647。
        哈希 为 哈希 加 字节 乘 131。
        i 为 i 加 1。
    完。
    返还 哈希。
完。

可变 _防抖_最后时间 为 0。

可变 _防抖_间隔 为 100。

防抖_设置间隔 接受 毫秒：
    _防抖_间隔 为 毫秒。
完。

防抖_应触发 接受 当前时间：
    若 当前时间 减 _防抖_最后时间 大于或等于 _防抖_间隔：
        _防抖_最后时间 为 当前时间。
        返还 真。
    完。
    返还 假。
完。

监控_合并事件 接受 事件列表：
    可变 合并 为 一组：（空）。
    可变 已处理路径 为 一组：（空）。
    可变 i 为 做 计数 用 事件列表 减 1。
    只要 i 大于或等于 0：
        路径 为 事件列表 的 第 i 项 的 第 1 项。
        可变 已有 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 已处理路径：
            若 已处理路径 的 第 j 项 等于 路径：
                已有 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已有 等于 假：
            做 追加 用 合并、事件列表 的 第 i 项。
            做 追加 用 已处理路径、路径。
        完。
        i 为 i 减 1。
    完。
    返还 合并。
完。

监控_创建快照 接受 文件列表、哈希列表：
    返还 一组：文件列表、哈希列表、做 计数 用 文件列表。
完。

监控_比较快照 接受 旧快照、新快照：
    可变 新增 为 一组：（空）。
    可变 修改 为 一组：（空）。
    可变 删除 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 新快照 的 第 0 项：
        可变 在旧中 为 负 1。
        可变 j 为 0。
        只要 j 小于 做 计数 用 旧快照 的 第 0 项：
            若 旧快照 的 第 0 项 的 第 j 项 等于 新快照 的 第 0 项 的 第 i 项：
                在旧中 为 j。
            完。
            j 为 j 加 1。
        完。
        若 在旧中 等于 负 1：
            做 追加 用 新增、新快照 的 第 0 项 的 第 i 项。
        否则若 旧快照 的 第 1 项 的 第 在旧中 项 不等于 新快照 的 第 1 项 的 第 i 项：
            做 追加 用 修改、新快照 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 旧快照 的 第 0 项：
        可变 在新中 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 新快照 的 第 0 项：
            若 新快照 的 第 0 项 的 第 j 项 等于 旧快照 的 第 0 项 的 第 i 项：
                在新中 为 真。
            完。
            j 为 j 加 1。
        完。
        若 在新中 等于 假：
            做 追加 用 删除、旧快照 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：新增、修改、删除。
完。

本章 收束。
