《配置解析》

引用《..·系统·配置》。

引用《..·基础》。

可变 配置_键 为 一组：（空）。

可变 配置_字典 为 做 字典。

配置_清空 接受 零参：
    配置_键 为 一组：（空）。
    配置_字典 为 做 字典。
完。

配置解析_配置_设置 接受 键、值：
    已有 为 做 字典取 用 配置_字典、键。
    若 已有 不等于 空：
        做 字典设 用 配置_字典、键、值。
        返还 真。
    完。
    做 追加 用 配置_键、键。
    做 字典设 用 配置_字典、键、值。
    返还 真。
完。

配置解析_配置_获取 接受 键、默认值：
    v 为 做 字典取 用 配置_字典、键。
    若 v 不等于 空：
        返还 v。
    完。
    返还 默认值。
完。

配置解析_配置_存在 接受 键：
    返还 做 字典取 用 配置_字典、键 不等于 空。
完。

配置_数量 接受 零参：
    返还 做 计数 用 配置_键。
完。

配置解析_配置_删除 接受 键：
    做 字典设 用 配置_字典、键、空。
    可变 新键 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 配置_键：
        若 配置_键 的 第 i 项 不等于 键：
            做 追加 用 新键、配置_键 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    配置_键 为 新键。
完。

配置解析_配置_合并 接受 外键列表、外值列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 外键列表：
        做 配置_设置 用 外键列表 的 第 i 项、外值列表 的 第 i 项、0。
        i 为 i 加 1。
    完。
完。

配置_验证_范围 接受 键、最小、最大：
    数值 为 做 配置_获取 用 键、负 99999。
    若 数值 等于 负 99999：
        返还 负 1。
    完。
    若 数值 小于 最小：
        返还 0。
    完。
    若 数值 大于 最大：
        返还 0。
    完。
    返还 1。
完。

配置_验证_必填 接受 必填列表：
    可变 缺失 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 必填列表：
        若 做 配置_存在 用 必填列表 的 第 i 项、0 等于 假：
            缺失 为 缺失 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 缺失。
完。

节_键 接受 节哈希、键哈希：
    返还 节哈希 乘 1000003 加 键哈希。
完。

配置_设置节 接受 节、键、值：
    做 配置_设置 用 做 节_键 用 节、键、值、0。
完。

配置_获取节 接受 节、键、缺省：
    返还 做 配置_获取 用 做 节_键 用 节、键、缺省。
完。

配置_键列表 接受 零参：
    返还 做 复制列表 用 配置_键。
完。

配置_值列表 接受 零参：
    返还 做 复制列表 用 配置_值。
完。

配置_遍历打印 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 配置_键：
        做 打印 用 做 转字符串 用 配置_键 的 第 i 项 加 「 = 」 加 做 转字符串 用 配置_值 的 第 i 项。
        i 为 i 加 1。
    完。
完。

配置解析_配置_差异 接受 外键、外值：
    可变 新增 为 0。
    可变 修改 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 外键：
        若 做 配置_存在 用 外键 的 第 i 项、0 等于 假：
            新增 为 新增 加 1。
        否则若 做 配置_获取 用 外键 的 第 i 项、负 1 不等于 外值 的 第 i 项：
            修改 为 修改 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：新增、修改。
完。

配置_克隆 接受 零参：
    返还 一组：做 复制列表 用 配置_键、做 复制列表 用 配置_值。
完。

配置_从克隆恢复 接受 克隆：
    可变 配置_值 为 0。
    配置_键 为 做 复制列表 用 克隆 的 第 0 项。
    配置_值 为 做 复制列表 用 克隆 的 第 1 项。
完。

配置_过滤前缀 接受 前缀、除数：
    可变 结果键 为 一组：（空）。
    可变 结果值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 配置_键：
        若 配置_键 的 第 i 项 除以 除数 等于 前缀：
            做 追加 用 结果键、配置_键 的 第 i 项。
            做 追加 用 结果值、配置_值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：结果键、结果值。
完。

配置解析_配置_导出 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 配置_键：
        做 追加 用 结果、一组：配置_键 的 第 i 项、配置_值 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

配置_导入 接受 对列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 对列表：
        做 配置_设置 用 对列表 的 第 i 项 的 第 0 项、对列表 的 第 i 项 的 第 1 项、0。
        i 为 i 加 1。
    完。
完。

配置_解析INI 接受 文本：
    可变 当前节 为 「」。
    可变 行列表 为 一组：（空）。
    可变 当前行 为 「」。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 文本：
        c 为 做 字节取 用 文本、i。
        若 c 等于 字符_换行：
            做 追加 用 行列表、当前行。
            当前行 为 「」。
        否则：
            当前行 为 当前行 加 做 字符 用 c。
        完。
        i 为 i 加 1。
    完。
    若 当前行 不等于 「」：
        做 追加 用 行列表、当前行。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 行列表 的 第 i 项。
        长 为 做 字节长度 用 行。
        若 长 大于 0：
            首字符 为 做 字节取 用 行、0。
            若 首字符 等于 字符_左方括：
                可变 j 为 1。
                可变 节名 为 「」。
                只要 j 小于 长：
                    ch 为 做 字节取 用 行、j。
                    若 ch 等于 字符_右方括：
                        j 为 长。
                    否则：
                        节名 为 节名 加 做 字符 用 ch。
                    完。
                    j 为 j 加 1。
                完。
                当前节 为 节名。
            否则若 首字符 不等于 字符_分号 并且 首字符 不等于 35：
                可变 j 为 0。
                只要 j 小于 长：
                    若 做 字节取 用 行、j 等于 字符_等号：
                        等号位 为 j。
                        j 为 长。
                    完。
                    j 为 j 加 1。
                完。
                若 等号位 大于 0：
                    键 为 做 子串 用 行、0、等号位。
                    值 为 做 子串 用 行、等号位 加 1、长 减 等号位 减 1。
                    可变 完整键 为 键。
                    若 当前节 不等于 「」：
                        完整键 为 当前节 加 「.」 加 键。
                    完。
                    做 配置_设置 用 完整键、值、0。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
完。

可变 _配置_必需键 为 一组：（空）。

配置_注册必需 接受 键：
    做 追加 用 _配置_必需键、键。
完。

配置_验证 接受 零参：
    可变 缺失 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _配置_必需键：
        若 做 配置_获取 用 _配置_必需键 的 第 i 项、空 等于 空：
            做 追加 用 缺失、_配置_必需键 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 缺失。
完。

配置_深合并 接受 其他键列表、其他值列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 其他键列表：
        若 做 配置_获取 用 其他键列表 的 第 i 项、空 等于 空：
            做 配置_设置 用 其他键列表 的 第 i 项、其他值列表 的 第 i 项、0。
        完。
        i 为 i 加 1。
    完。
完。

配置_覆盖合并 接受 其他键列表、其他值列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 其他键列表：
        做 配置_设置 用 其他键列表 的 第 i 项、其他值列表 的 第 i 项、0。
        i 为 i 加 1。
    完。
完。

可变 _配置_读取数 为 0。

可变 _配置_写入数 为 0。

配置_记录读取 接受 零参：
    _配置_读取数 为 _配置_读取数 加 1。
完。

配置_记录写入 接受 零参：
    _配置_写入数 为 _配置_写入数 加 1。
完。

本章 收束。
