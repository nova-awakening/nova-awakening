《自监督学习》

引用《..·基础》。

SSL_随机态 为 77777。

SSL_随机 接受 零参：
    SSL_随机态 为 SSL_随机态 位异或 SSL_随机态 左移 13。
    SSL_随机态 为 SSL_随机态 位异或 SSL_随机态 右移 7。
    SSL_随机态 为 SSL_随机态 位异或 SSL_随机态 左移 17。
    若 SSL_随机态 小于 0：
        SSL_随机态 为 0 减 SSL_随机态。
    完。
    返还 SSL_随机态。
完。

MLM_生成掩码 接受 序列长度、掩码率：
    可变 掩码 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列长度：
        若 做 SSL_随机 取余于 1000 小于 掩码率：
            做 追加 用 掩码、1。
        否则：
            做 追加 用 掩码、0。
        完。
        i 为 i 加 1。
    完。
    返还 掩码。
完。

MLM_应用掩码 接受 token序列、掩码、mask_token_id：
    n 为 做 计数 用 token序列。
    可变 掩码序列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 做 计数 用 掩码 并且 掩码 的 第 i 项 等于 1：
            r 为 做 SSL_随机 取余于 100。
            若 r 小于 80：
                做 追加 用 掩码序列、mask_token_id。
            否则若 r 小于 90：
                做 追加 用 掩码序列、做 SSL_随机 取余于 30000。
            否则：
                做 追加 用 掩码序列、token序列 的 第 i 项。
            完。
        否则：
            做 追加 用 掩码序列、token序列 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 掩码序列。
完。

MLM_损失 接受 预测logits列表、真实token列表、掩码：
    n 为 做 计数 用 掩码。
    可变 损失 为 0。
    可变 掩码数 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 掩码 的 第 i 项 等于 1：
            若 i 小于 做 计数 用 预测logits列表 并且 i 小于 做 计数 用 真实token列表：
                真实 为 真实token列表 的 第 i 项。
                对数值 为 预测logits列表 的 第 i 项。
                若 真实 小于 做 计数 用 对数值：
                    可变 最大 为 对数值 的 第 0 项。
                    可变 j 为 1。
                    只要 j 小于 做 计数 用 对数值：
                        若 对数值 的 第 j 项 大于 最大：
                            最大 为 对数值 的 第 j 项。
                        完。
                        j 为 j 加 1。
                    完。
                    可变 求和 为 0。
                    j 为 0。
                    只要 j 小于 做 计数 用 对数值：
                        差值 为 对数值 的 第 j 项 减 最大。
                        可变 ex 为 1000 加 差值 加 差值 乘 差值 除以 2000。
                        若 ex 小于 1：
                            ex 为 1。
                        完。
                        求和 为 求和 加 ex。
                        j 为 j 加 1。
                    完。
                    可变 此CE 为 最大 减 对数值 的 第 真实 项。
                    若 求和 大于 1000：
                        ratio_m1 为 (求和 减 1000) 乘 1000 除以 求和。
                        此CE 为 此CE 加 ratio_m1。
                    完。
                    损失 为 损失 加 此CE。
                完。
            完。
            掩码数 为 掩码数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 掩码数 等于 0：
        返还 0。
    完。
    返还 损失 除以 掩码数。
完。

NTP_创建序列 接受 token列表：
    n 为 做 计数 用 token列表。
    若 n 小于 2：
        返还 一组：一组：（空）、一组：（空）。
    完。
    可变 输入 为 一组：（空）。
    可变 标签 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        做 追加 用 输入、token列表 的 第 i 项。
        做 追加 用 标签、token列表 的 第 i 加 1 项。
        i 为 i 加 1。
    完。
    返还 一组：输入、标签。
完。

MAE_生成patch掩码 接受 patch数、掩码率：
    掩码数 为 patch数 乘 掩码率 除以 1000。
    可变 掩码 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 patch数：
        做 追加 用 掩码、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 掩码数：
        可变 位置 为 做 SSL_随机 取余于 patch数。
        可变 重试次数 为 0。
        只要 掩码 的 第 位置 项 等于 1 并且 重试次数 小于 patch数：
            位置 为 (位置 加 1) 取余于 patch数。
            重试次数 为 重试次数 加 1。
        完。
        掩码 的 第 位置 项 为 1。
        i 为 i 加 1。
    完。
    返还 掩码。
完。

MAE_可见patch 接受 所有patch、掩码：
    可变 可见 为 一组：（空）。
    可变 可见索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有patch：
        若 i 小于 做 计数 用 掩码 并且 掩码 的 第 i 项 等于 0：
            做 追加 用 可见、所有patch 的 第 i 项。
            做 追加 用 可见索引、i。
        完。
        i 为 i 加 1。
    完。
    返还 一组：可见、可见索引。
完。

MAE_重建损失 接受 重建patch、原始patch、掩码：
    n 为 做 计数 用 掩码。
    可变 损失 为 0。
    可变 掩码数 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 掩码 的 第 i 项 等于 1：
            若 i 小于 做 计数 用 重建patch 并且 i 小于 做 计数 用 原始patch：
                r 为 重建patch 的 第 i 项。
                o 为 原始patch 的 第 i 项。
                维度 为 做 计数 用 r。
                可变 patch损失 为 0。
                可变 j 为 0。
                只要 j 小于 维度：
                    可变 ov 为 0。
                    若 j 小于 做 计数 用 o：
                        ov 为 o 的 第 j 项。
                    完。
                    d 为 r 的 第 j 项 减 ov。
                    patch损失 为 patch损失 加 d 乘 d 除以 1000。
                    j 为 j 加 1。
                完。
                损失 为 损失 加 patch损失 除以 维度。
            完。
            掩码数 为 掩码数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 掩码数 等于 0：
        返还 0。
    完。
    返还 损失 除以 掩码数。
完。

DINO_锐化 接受 概率、τ：
    若 τ 小于或等于 0：
        τ 为 40。
    完。
    n 为 做 计数 用 概率。
    可变 锐化 为 一组：（空）。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 v 为 概率 的 第 i 项 乘 1000 除以 τ。
        可变 e 为 1000 加 v。
        若 e 小于 1：
            e 为 1。
        完。
        做 追加 用 锐化、e。
        总和 为 总和 加 e。
        i 为 i 加 1。
    完。
    若 总和 等于 0：
        总和 为 1。
    完。
    i 为 0。
    只要 i 小于 n：
        锐化 的 第 i 项 为 锐化 的 第 i 项 乘 1000 除以 总和。
        i 为 i 加 1。
    完。
    返还 锐化。
完。

DINO_损失 接受 学生输出、教师输出、τ_s、τ_t：
    学生概率 为 做 DINO_锐化 用 学生输出、τ_s。
    教师概率 为 做 DINO_锐化 用 教师输出、τ_t。
    n 为 做 计数 用 学生概率。
    可变 ce 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 学生概率 的 第 i 项 大于 0：
            ce 为 ce 加 教师概率 的 第 i 项 乘 (1000 减 学生概率 的 第 i 项) 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 ce。
完。

JEPA_目标块 接受 特征图、块起始、块大小：
    可变 块 为 一组：（空）。
    可变 i 为 块起始。
    只要 i 小于 块起始 加 块大小 并且 i 小于 做 计数 用 特征图：
        做 追加 用 块、特征图 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 块。
完。

JEPA_预测损失 接受 预测嵌入、目标嵌入：
    n 为 做 计数 用 预测嵌入。
    可变 损失 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 做 计数 用 目标嵌入：
            tv 为 目标嵌入 的 第 i 项。
        完。
        d 为 预测嵌入 的 第 i 项 减 tv。
        损失 为 损失 加 d 乘 d 除以 1000。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 损失 除以 n。
完。

CPC_信息NCE 接受 上下文、正样本、负样本列表、步数：
    可变 总损失 为 0。
    可变 t 为 0。
    只要 t 小于 步数：
        若 t 小于 做 计数 用 正样本：
            d 为 做 计数 用 上下文。
            可变 i 为 0。
            只要 i 小于 d：
                若 i 小于 做 计数 用 正样本 的 第 t 项：
                    正分 为 正分 加 上下文 的 第 i 项 乘 正样本 的 第 t 项 的 第 i 项 除以 1000。
                完。
                i 为 i 加 1。
            完。
            可变 正exp 为 1000 加 正分。
            若 正exp 小于 1：
                正exp 为 1。
            完。
            可变 总exp 为 正exp。
            可变 j 为 0。
            只要 j 小于 做 计数 用 负样本列表：
                i 为 0。
                只要 i 小于 d：
                    若 i 小于 做 计数 用 负样本列表 的 第 j 项：
                        负分 为 负分 加 上下文 的 第 i 项 乘 负样本列表 的 第 j 项 的 第 i 项 除以 1000。
                    完。
                    i 为 i 加 1。
                完。
                可变 负exp 为 1000 加 负分。
                若 负exp 小于 1：
                    负exp 为 1。
                完。
                总exp 为 总exp 加 负exp。
                j 为 j 加 1。
            完。
            若 总exp 等于 0：
                总exp 为 1。
            完。
            总损失 为 总损失 加 总exp 乘 1000 除以 正exp。
        完。
        t 为 t 加 1。
    完。
    若 步数 等于 0：
        返还 0。
    完。
    返还 总损失 除以 步数。
完。

DAE_加噪 接受 数据、噪声率：
    n 为 做 计数 用 数据。
    可变 噪声数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 SSL_随机 取余于 1000 小于 噪声率：
            做 追加 用 噪声数据、0。
        否则：
            做 追加 用 噪声数据、数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 噪声数据。
完。

DAE_损失 接受 重建、原始：
    n 为 做 计数 用 原始。
    可变 损失 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 r 为 0。
        若 i 小于 做 计数 用 重建：
            r 为 重建 的 第 i 项。
        完。
        d 为 r 减 原始 的 第 i 项。
        损失 为 损失 加 d 乘 d 除以 1000。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 损失 除以 n。
完。

旋转_应用 接受 特征矩阵、_行、_列、旋转度：
    可变 结果 为 一组：（空）。
    若 旋转度 等于 0：
        返还 特征矩阵。
    完。
    若 旋转度 等于 2：
        n 为 做 计数 用 特征矩阵。
        可变 i 为 n 减 1。
        只要 i 大于或等于 0：
            做 追加 用 结果、特征矩阵 的 第 i 项。
            i 为 i 减 1。
        完。
        返还 结果。
    完。
    返还 特征矩阵。
完。

本章 收束。
