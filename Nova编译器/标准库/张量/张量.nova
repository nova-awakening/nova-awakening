《张量》

引用《..·基础》。

张量_复制列表 接受 源：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 源：
        做 追加 用 结果、源 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

张量_列表最大值 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    可变 最大值 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 大于 最大值：
            最大值 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大值。
完。

张量_列表最小值 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    可变 最小值 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 小于 最小值：
            最小值 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小值。
完。

形状_元素数 接受 形状：
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 形状：
        结果 为 结果 乘 形状 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

计算步长 接受 形状：
    nd 为 做 计数 用 形状。
    可变 步长 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 nd：
        做 追加 用 步长、1。
        i 为 i 加 1。
    完。
    若 nd 大于 1：
        i 为 nd 减 2。
        只要 i 大于或等于 0：
            步长 的 第 i 项 为 步长 的 第 i 加 1 项 乘 形状 的 第 i 加 1 项。
            i 为 i 减 1。
        完。
    完。
    返还 步长。
完。

张量_零 接受 形状：
    n 为 做 形状_元素数 用 形状。
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 一组：数据、形状、做 计算步长 用 形状、做 计数 用 形状。
完。

张量_一 接受 形状：
    n 为 做 形状_元素数 用 形状。
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 数据、1。
        i 为 i 加 1。
    完。
    返还 一组：数据、形状、做 计算步长 用 形状、做 计数 用 形状。
完。

张量_填充 接受 形状、值：
    n 为 做 形状_元素数 用 形状。
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 数据、值。
        i 为 i 加 1。
    完。
    返还 一组：数据、形状、做 计算步长 用 形状、做 计数 用 形状。
完。

张量_从列表 接受 数据、形状：
    返还 一组：数据、形状、做 计算步长 用 形状、做 计数 用 形状。
完。

张量_单位矩阵 接受 n：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n 乘 n：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        数据 的 第 i 乘 n 加 i 项 为 1。
        i 为 i 加 1。
    完。
    返还 一组：数据、一组：n、n、做 计算步长 用 一组：n、n、2。
完。

张量_范围 接受 起、_止：
    n 为 _止 减 起。
    可变 数据 为 一组：（空）。
    可变 i 为 起。
    只要 i 小于 _止：
        做 追加 用 数据、i。
        i 为 i 加 1。
    完。
    返还 一组：数据、一组：n、做 计算步长 用 一组：n、1。
完。

张量_形状 接受 t：
    返还 t 的 第 1 项。
完。

张量_维度 接受 t：
    返还 t 的 第 3 项。
完。

张量_元素数 接受 t：
    返还 做 计数 用 t 的 第 0 项。
完。

张量_数据 接受 t：
    返还 t 的 第 0 项。
完。

张量_获取 接受 t、索引列表：
    可变 偏移 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 索引列表：
        偏移 为 偏移 加 索引列表 的 第 i 项 乘 t 的 第 2 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 t 的 第 0 项 的 第 偏移 项。
完。

张量_设置 接受 t、索引列表、值：
    可变 偏移 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 索引列表：
        偏移 为 偏移 加 索引列表 的 第 i 项 乘 t 的 第 2 项 的 第 i 项。
        i 为 i 加 1。
    完。
    t 的 第 0 项 的 第 偏移 项 为 值。
完。

张量_加 接受 a、b：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 0 项：
        做 追加 用 数据、a 的 第 0 项 的 第 i 项 加 b 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 一组：数据、a 的 第 1 项、a 的 第 2 项、a 的 第 3 项。
完。

张量_减 接受 a、b：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 0 项：
        做 追加 用 数据、a 的 第 0 项 的 第 i 项 减 b 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 一组：数据、a 的 第 1 项、a 的 第 2 项、a 的 第 3 项。
完。

张量_乘 接受 a、b：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 0 项：
        做 追加 用 数据、a 的 第 0 项 的 第 i 项 乘 b 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 一组：数据、a 的 第 1 项、a 的 第 2 项、a 的 第 3 项。
完。

张量_标量加 接受 t、s：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 t 的 第 0 项：
        做 追加 用 数据、t 的 第 0 项 的 第 i 项 加 s。
        i 为 i 加 1。
    完。
    返还 一组：数据、t 的 第 1 项、t 的 第 2 项、t 的 第 3 项。
完。

张量_标量乘 接受 t、s：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 t 的 第 0 项：
        做 追加 用 数据、t 的 第 0 项 的 第 i 项 乘 s。
        i 为 i 加 1。
    完。
    返还 一组：数据、t 的 第 1 项、t 的 第 2 项、t 的 第 3 项。
完。

张量_取负 接受 t：
    返还 做 张量_标量乘 用 t、负 1。
完。

张量_矩阵乘 接受 a、b：
    M 为 a 的 第 1 项 的 第 0 项。
    K 为 a 的 第 1 项 的 第 1 项。
    N 为 b 的 第 1 项 的 第 1 项。
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 M 乘 N：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 M：
        可变 j 为 0。
        只要 j 小于 N：
            可变 和 为 0。
            可变 k 为 0。
            只要 k 小于 K：
                和 为 和 加 a 的 第 0 项 的 第 i 乘 K 加 k 项 乘 b 的 第 0 项 的 第 k 乘 N 加 j 项。
                k 为 k 加 1。
            完。
            数据 的 第 i 乘 N 加 j 项 为 和。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：数据、一组：M、N、做 计算步长 用 一组：M、N、2。
完。

张量_转置 接受 t：
    M 为 t 的 第 1 项 的 第 0 项。
    N 为 t 的 第 1 项 的 第 1 项。
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 M 乘 N：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 M：
        可变 j 为 0。
        只要 j 小于 N：
            数据 的 第 j 乘 M 加 i 项 为 t 的 第 0 项 的 第 i 乘 N 加 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：数据、一组：N、M、做 计算步长 用 一组：N、M、2。
完。

张量_点积 接受 a、b：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 0 项：
        和 为 和 加 a 的 第 0 项 的 第 i 项 乘 b 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

张量_求和 接受 t：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 t 的 第 0 项：
        和 为 和 加 t 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

张量_最大 接受 t：
    返还 做 张量_列表最大值 用 t 的 第 0 项。
完。

张量_最小 接受 t：
    返还 做 张量_列表最小值 用 t 的 第 0 项。
完。

张量_argmax 接受 t：
    可变 最大值 为 t 的 第 0 项 的 第 0 项。
    可变 最大idx 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 t 的 第 0 项：
        若 t 的 第 0 项 的 第 i 项 大于 最大值：
            最大值 为 t 的 第 0 项 的 第 i 项。
            最大idx 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最大idx。
完。

张量_reshape 接受 t、新形状：
    返还 一组：t 的 第 0 项、新形状、做 计算步长 用 新形状、做 计数 用 新形状。
完。

张量_展平 接受 t：
    n 为 做 计数 用 t 的 第 0 项。
    返还 一组：t 的 第 0 项、一组：n、一组：1、1。
完。

张量_切片1D 接受 t、起、_止：
    可变 数据 为 一组：（空）。
    可变 i 为 起。
    只要 i 小于 _止：
        做 追加 用 数据、t 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 一组：数据、一组：_止 减 起、一组：1、1。
完。

张量_行 接受 t、行号：
    N 为 t 的 第 1 项 的 第 1 项。
    可变 数据 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 N：
        做 追加 用 数据、t 的 第 0 项 的 第 行号 乘 N 加 j 项。
        j 为 j 加 1。
    完。
    返还 一组：数据、一组：N、一组：1、1。
完。

张量_列 接受 t、列号：
    M 为 t 的 第 1 项 的 第 0 项。
    N 为 t 的 第 1 项 的 第 1 项。
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 M：
        做 追加 用 数据、t 的 第 0 项 的 第 i 乘 N 加 列号 项。
        i 为 i 加 1。
    完。
    返还 一组：数据、一组：M、一组：1、1。
完。

张量_相等 接受 a、b：
    若 做 计数 用 a 的 第 0 项 不等于 做 计数 用 b 的 第 0 项：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 0 项：
        若 a 的 第 0 项 的 第 i 项 不等于 b 的 第 0 项 的 第 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

张量_近似相等 接受 a、b、容差：
    若 做 计数 用 a 的 第 0 项 不等于 做 计数 用 b 的 第 0 项：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 0 项：
        可变 差 为 a 的 第 0 项 的 第 i 项 减 b 的 第 0 项 的 第 i 项。
        若 差 小于 0：
            差 为 0 减 差。
        完。
        若 差 大于 容差：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

张量_拼接1D 接受 a、b：
    可变 数据 为 做 张量_复制列表 用 a 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 b 的 第 0 项：
        做 追加 用 数据、b 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    n 为 做 计数 用 数据。
    返还 一组：数据、一组：n、一组：1、1。
完。

张量_修正线性 接受 t：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 t 的 第 0 项：
        可变 值 为 t 的 第 0 项 的 第 i 项。
        若 值 小于 0：
            值 为 0。
        完。
        做 追加 用 数据、值。
        i 为 i 加 1。
    完。
    返还 一组：数据、t 的 第 1 项、t 的 第 2 项、t 的 第 3 项。
完。

张量_clamp 接受 t、lo、hi：
    n 为 做 计数 用 t 的 第 0 项。
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 v 为 t 的 第 0 项 的 第 i 项。
        若 v 小于 lo：
            v 为 lo。
        完。
        若 v 大于 hi：
            v 为 hi。
        完。
        做 追加 用 数据、v。
        i 为 i 加 1。
    完。
    返还 一组：数据、t 的 第 1 项、t 的 第 2 项、t 的 第 3 项。
完。

本章 收束。
