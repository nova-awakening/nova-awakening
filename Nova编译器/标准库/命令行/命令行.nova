《命令行》

引用《..·基础》。

参数_标志 为 0。

参数_选项 为 1。

参数_位置 为 2。

参数_创建 接受 名称、短名、类型、默认值、描述、必需：
    返还 一组：名称、短名、类型、默认值、描述、必需、默认值。
完。

标志 接受 名称、短名、描述：
    返还 做 参数_创建 用 名称、短名、参数_标志、0、描述、0。
完。

选项 接受 名称、短名、默认值、描述、必需：
    返还 做 参数_创建 用 名称、短名、参数_选项、默认值、描述、必需。
完。

位置参数 接受 名称、描述、必需：
    返还 做 参数_创建 用 名称、「」、参数_位置、「」、描述、必需。
完。

命令_创建 接受 名称、描述：
    返还 一组：名称、描述、一组：（空）、一组：（空）、「」、0、空。
完。

命令_添加参数 接受 命令、参数：
    做 追加 用 命令 的 第 2 项、参数。
完。

命令_添加子命令 接受 命令、子命令：
    做 追加 用 命令 的 第 3 项、子命令。
完。

命令_设版本 接受 命令、版本：
    命令 的 第 4 项 为 版本。
完。

解析_参数 接受 命令、args：
    argc 为 做 计数 用 args。
    可变 i 为 0。
    只要 i 小于 argc：
        arg 为 args 的 第 i 项。
        可变 已匹配 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 命令 的 第 2 项：
            参数 为 命令 的 第 2 项 的 第 j 项。
            若 arg 等于 参数 的 第 0 项：
                若 参数 的 第 2 项 等于 参数_标志：
                    参数 的 第 6 项 为 1。
                    已匹配 为 1。
                否则若 参数 的 第 2 项 等于 参数_选项：
                    若 i 加 1 小于 argc：
                        i 为 i 加 1。
                        参数 的 第 6 项 为 args 的 第 i 项。
                    完。
                    已匹配 为 1。
                完。
            完。
            若 arg 等于 参数 的 第 1 项 并且 参数 的 第 1 项 不等于 「」：
                若 参数 的 第 2 项 等于 参数_标志：
                    参数 的 第 6 项 为 1。
                    已匹配 为 1。
                否则若 参数 的 第 2 项 等于 参数_选项：
                    若 i 加 1 小于 argc：
                        i 为 i 加 1。
                        参数 的 第 6 项 为 args 的 第 i 项。
                    完。
                    已匹配 为 1。
                完。
            完。
            j 为 j 加 1。
        完。
        若 已匹配 等于 0：
            j 为 0。
            只要 j 小于 做 计数 用 命令 的 第 3 项：
                若 arg 等于 命令 的 第 3 项 的 第 j 项 的 第 0 项：
                    可变 子args 为 一组：（空）。
                    可变 k 为 i 加 1。
                    只要 k 小于 argc：
                        做 追加 用 子args、args 的 第 k 项。
                        k 为 k 加 1。
                    完。
                    做 解析_参数 用 命令 的 第 3 项 的 第 j 项、子args。
                    命令 的 第 3 项 的 第 j 项 的 第 5 项 为 1。
                    返还 命令。
                完。
                j 为 j 加 1。
            完。
            j 为 0。
            只要 j 小于 做 计数 用 命令 的 第 2 项：
                若 命令 的 第 2 项 的 第 j 项 的 第 2 项 等于 参数_位置 并且 命令 的 第 2 项 的 第 j 项 的 第 6 项 等于 命令 的 第 2 项 的 第 j 项 的 第 3 项：
                    命令 的 第 2 项 的 第 j 项 的 第 6 项 为 arg。
                    j 为 做 计数 用 命令 的 第 2 项。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 命令。
完。

获取_参数 接受 命令、名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 2 项：
        若 命令 的 第 2 项 的 第 i 项 的 第 0 项 等于 名称：
            返还 命令 的 第 2 项 的 第 i 项 的 第 6 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

获取_标志 接受 命令、名称：
    数值 为 做 获取_参数 用 命令、名称。
    若 数值 等于 空：
        返还 0。
    完。
    返还 数值 等于 1。
完。

活跃子命令 接受 命令：
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 3 项：
        若 命令 的 第 3 项 的 第 i 项 的 第 5 项 等于 1：
            返还 命令 的 第 3 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

验证_必需参数 接受 命令：
    可变 缺失 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 2 项：
        若 命令 的 第 2 项 的 第 i 项 的 第 5 项 等于 1 并且 命令 的 第 2 项 的 第 i 项 的 第 6 项 等于 命令 的 第 2 项 的 第 i 项 的 第 3 项：
            做 追加 用 缺失、命令 的 第 2 项 的 第 i 项 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 缺失。
完。

生成_帮助 接受 命令：
    做 打印 用 「用法: 」 加 命令 的 第 0 项。
    若 命令 的 第 1 项 不等于 「」：
        做 打印 用 「 」 加 命令 的 第 1 项。
    完。
    若 命令 的 第 4 项 不等于 「」：
        做 打印 用 「 版本: 」 加 命令 的 第 4 项。
    完。
    做 打印 用 「」。
    若 做 计数 用 命令 的 第 2 项 大于 0：
        做 打印 用 「参数:」。
        可变 i 为 0。
        只要 i 小于 做 计数 用 命令 的 第 2 项：
            p 为 命令 的 第 2 项 的 第 i 项。
            可变 行 为 「 」。
            若 p 的 第 2 项 等于 参数_位置：
                行 为 行 加 「<」 加 p 的 第 0 项 加 「>」。
            否则：
                行 为 行 加 p 的 第 0 项。
                若 p 的 第 1 项 不等于 「」：
                    行 为 行 加 「, 」 加 p 的 第 1 项。
                完。
            完。
            行 为 行 加 「 」 加 p 的 第 4 项。
            若 p 的 第 5 项 等于 1：
                行 为 行 加 「 (必需)」。
            完。
            做 打印 用 行。
            i 为 i 加 1。
        完。
    完。
    若 做 计数 用 命令 的 第 3 项 大于 0：
        做 打印 用 「」。
        做 打印 用 「子命令:」。
        可变 i 为 0。
        只要 i 小于 做 计数 用 命令 的 第 3 项：
            做 打印 用 「 」 加 命令 的 第 3 项 的 第 i 项 的 第 0 项 加 「 」 加 命令 的 第 3 项 的 第 i 项 的 第 1 项。
            i 为 i 加 1。
        完。
    完。
完。

命令行_参数组_创建 接受 名称、描述、互斥：
    返还 一组：名称、描述、一组：（空）、互斥。
完。

参数组_追加 接受 组、参数名：
    做 追加 用 组 的 第 2 项、参数名。
完。

验证_互斥组 接受 命令、组：
    若 组 的 第 3 项 等于 0：
        返还 1。
    完。
    可变 设置数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 组 的 第 2 项：
        数值 为 做 获取_参数 用 命令、组 的 第 2 项 的 第 i 项。
        若 数值 不等于 空 并且 数值 不等于 0 并且 数值 不等于 「」：
            设置数 为 设置数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 设置数 大于 1：
        做 打印 用 「错误: 互斥参数组 '」 加 组 的 第 0 项 加 「' 中只能设置一个选项」。
        返还 0。
    完。
    返还 1。
完。

参数_计数_已设置 接受 命令：
    可变 n 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 2 项：
        若 命令 的 第 2 项 的 第 i 项 的 第 6 项 不等于 命令 的 第 2 项 的 第 i 项 的 第 3 项：
            n 为 n 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 n。
完。

参数_计数_标志 接受 命令：
    可变 n 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 2 项：
        若 命令 的 第 2 项 的 第 i 项 的 第 2 项 等于 参数_标志：
            n 为 n 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 n。
完。

参数_计数_选项 接受 命令：
    可变 n 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 2 项：
        若 命令 的 第 2 项 的 第 i 项 的 第 2 项 等于 参数_选项：
            n 为 n 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 n。
完。

参数_计数_位置 接受 命令：
    可变 n 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 2 项：
        若 命令 的 第 2 项 的 第 i 项 的 第 2 项 等于 参数_位置：
            n 为 n 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 n。
完。

命令_打印错误 接受 命令、消息：
    做 打印 用 「错误: 」 加 消息。
    做 打印 用 「运行 '」 加 命令 的 第 0 项 加 「 --help' 查看帮助」。
完。

验证_并报错 接受 命令：
    缺失 为 做 验证_必需参数 用 命令。
    若 做 计数 用 缺失 大于 0：
        可变 i 为 0。
        只要 i 小于 做 计数 用 缺失：
            做 命令_打印错误 用 命令、「缺少必需参数: 」 加 缺失 的 第 i 项。
            i 为 i 加 1。
        完。
        返还 0。
    完。
    返还 1。
完。

生成_用法行 接受 命令：
    可变 行 为 「用法: 」 加 命令 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 2 项：
        p 为 命令 的 第 2 项 的 第 i 项。
        若 p 的 第 2 项 等于 参数_位置：
            若 p 的 第 5 项 等于 1：
                行 为 行 加 「 <」 加 p 的 第 0 项 加 「>」。
            否则：
                行 为 行 加 「 [」 加 p 的 第 0 项 加 「]」。
            完。
        否则若 p 的 第 2 项 等于 参数_选项：
            行 为 行 加 「 [」 加 p 的 第 0 项 加 「 <值>]」。
        否则：
            行 为 行 加 「 [」 加 p 的 第 0 项 加 「]」。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 命令 的 第 3 项 大于 0：
        行 为 行 加 「 <子命令>」。
    完。
    返还 行。
完。

生成_详细帮助 接受 命令、示例列表：
    做 生成_帮助 用 命令。
    若 做 计数 用 示例列表 大于 0：
        做 打印 用 「」。
        做 打印 用 「示例:」。
        可变 i 为 0。
        只要 i 小于 做 计数 用 示例列表：
            做 打印 用 「 」 加 示例列表 的 第 i 项。
            i 为 i 加 1。
        完。
    完。
完。

命令_重置 接受 命令：
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 2 项：
        命令 的 第 2 项 的 第 i 项 的 第 6 项 为 命令 的 第 2 项 的 第 i 项 的 第 3 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 3 项：
        命令 的 第 3 项 的 第 i 项 的 第 5 项 为 0。
        做 命令_重置 用 命令 的 第 3 项 的 第 i 项。
        i 为 i 加 1。
    完。
完。

命令_导出参数 接受 命令：
    d 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 2 项：
        做 字典设 用 d、命令 的 第 2 项 的 第 i 项 的 第 0 项、命令 的 第 2 项 的 第 i 项 的 第 6 项。
        i 为 i 加 1。
    完。
    返还 d。
完。

生成_补全列表 接受 命令、前缀：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 2 项：
        名 为 命令 的 第 2 项 的 第 i 项 的 第 0 项。
        若 前缀 等于 「」 或者 做 以开头 用 名、前缀：
            做 追加 用 结果、名。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 3 项：
        名 为 命令 的 第 3 项 的 第 i 项 的 第 0 项。
        若 前缀 等于 「」 或者 做 以开头 用 名、前缀：
            做 追加 用 结果、名。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

获取_子命令 接受 命令、名称：
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 3 项：
        若 命令 的 第 3 项 的 第 i 项 的 第 0 项 等于 名称：
            返还 命令 的 第 3 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

快速命令 接受 名称、描述、版本：
    命令 为 做 命令_创建 用 名称、描述。
    做 命令_设版本 用 命令、版本。
    做 命令_添加参数 用 命令、做 标志 用 「--help」、「-h」、「显示帮助信息」。
    做 命令_添加参数 用 命令、做 标志 用 「--version」、「-V」、「显示版本号」。
    返还 命令。
完。

命令_自动帮助 接受 命令、args：
    做 解析_参数 用 命令、args。
    若 做 获取_标志 用 命令、「--help」：
        做 生成_帮助 用 命令。
        返还 0。
    完。
    若 做 获取_标志 用 命令、「--version」：
        做 打印 用 命令 的 第 0 项 加 「 」 加 命令 的 第 4 项。
        返还 0。
    完。
    若 做 验证_并报错 用 命令 等于 0：
        返还 0。
    完。
    返还 1。
完。

命令行_子命令 接受 命令、名称、描述：
    若 命令 的 第 6 项 等于 空：
        命令 的 第 6 项 为 一组：（空）。
    完。
    做 追加 用 命令 的 第 6 项、一组：名称、描述、一组：（空）。
完。

命令行_获取子命令 接受 命令、参数列表：
    若 做 计数 用 参数列表 等于 0：
        返还 空。
    完。
    若 命令 的 第 6 项 等于 空：
        返还 空。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 6 项：
        若 命令 的 第 6 项 的 第 i 项 的 第 0 项 等于 参数列表 的 第 0 项：
            返还 命令 的 第 6 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

命令行_环境变量 接受 名称、默认值：
    值 为 做 获取环境 用 名称。
    若 值 等于 空：
        返还 默认值。
    完。
    返还 值。
完。

命令行_是否详细 接受 命令：
    返还 做 获取_标志 用 命令、「--verbose」 或者 做 获取_标志 用 命令、「-v」。
完。

命令行_是否静默 接受 命令：
    返还 做 获取_标志 用 命令、「--quiet」 或者 做 获取_标志 用 命令、「-q」。
完。

命令行_输出级别 接受 命令：
    若 做 命令行_是否静默 用 命令：
        返还 0。
    完。
    若 做 命令行_是否详细 用 命令：
        返还 2。
    完。
    返还 1。
完。

命令行_打印用法 接受 命令：
    做 打印 用 「用法: 」 加 命令 的 第 0 项 加 「 [选项] [参数]」。
    做 打印 用 「」。
    做 打印 用 「选项:」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 命令 的 第 2 项：
        选项 为 命令 的 第 2 项 的 第 i 项。
        做 打印 用 「 」 加 选项 的 第 0 项 加 「 」 加 选项 的 第 4 项。
        i 为 i 加 1。
    完。
完。

命令行_解析整数 接受 值、默认值：
    若 值 等于 空：
        返还 默认值。
    完。
    可变 结果 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 值：
        c 为 做 字符码 用 值、i。
        若 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
            结果 为 结果 乘 10 加 c 减 48。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _cmd_解析次数 为 0。

可变 _cmd_错误数 为 0。

可变 _cmd_子命令数 为 0。

cmd_记录解析 接受 成功：
    _cmd_解析次数 为 _cmd_解析次数 加 1。
    若 非 成功：
        _cmd_错误数 为 _cmd_错误数 加 1。
    完。
完。

cmd_记录子命令 接受 零参：
    _cmd_子命令数 为 _cmd_子命令数 加 1。
完。

cmd_错误率 接受 零参：
    若 _cmd_解析次数 等于 0：
        返还 0。
    完。
    返还 _cmd_错误数 乘 100 除以 _cmd_解析次数。
完。

cmd_易用性评估 接受 零参：
    率 为 做 cmd_错误率。
    若 率 大于 30：
        返还 「参数设计不直观(改进帮助文本)」。
    完。
    若 率 大于 10：
        返还 「偶有误用」。
    完。
    返还 「易用性良好」。
完。

cmd_重置 接受 零参：
    _cmd_解析次数 为 0。
    _cmd_错误数 为 0。
    _cmd_子命令数 为 0。
完。

命令行_进度条 接受 当前、总数、宽度：
    若 总数 等于 0：
        返还 「」。
    完。
    完成 为 当前 乘 宽度 除以 总数。
    可变 条 为 「[」。
    可变 i 为 0。
    只要 i 小于 宽度：
        若 i 小于 完成：
            条 为 做 字符串拼接 用 条、「█」。
        否则：
            条 为 做 字符串拼接 用 条、「░」。
        完。
        i 为 i 加 1。
    完。
    条 为 做 字符串拼接 用 条、「] 」。
    条 为 做 字符串拼接 用 条、做 转字符串 用 当前 乘 100 除以 总数。
    条 为 做 字符串拼接 用 条、「%」。
    返还 条。
完。

本章 收束。
