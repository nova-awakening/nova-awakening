《贝叶斯学习》

引用《..·基础》。

引用《..·算法·数学扩展》。

BL_随机态 为 31415。

BL_随机 接受 零参：
    BL_随机态 为 BL_随机态 位异或 BL_随机态 左移 13。
    BL_随机态 为 BL_随机态 位异或 BL_随机态 右移 7。
    BL_随机态 为 BL_随机态 位异或 BL_随机态 左移 17。
    若 BL_随机态 小于 0：
        BL_随机态 为 0 减 BL_随机态。
    完。
    返还 BL_随机态。
完。

高斯_采样 接受 均值、标准差：
    u1 为 做 BL_随机 取余于 998 加 1。
    做 BL_随机 取余于 1000。
    z 为 (u1 减 500) 乘 3 除以 500。
    返还 均值 加 标准差 乘 z 除以 1000。
完。

高斯_对数概率 接受 x、μ、σ：
    若 σ 小于或等于 0：
        σ 为 1。
    完。
    d 为 x 减 μ。
    返还 0 减 d 乘 d 除以 (2 乘 σ 乘 σ 除以 1000)。
完。

贝叶斯线性_创建 接受 维度、σ_prior：
    可变 均值 为 一组：（空）。
    可变 方差 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 均值、0。
        做 追加 用 方差、σ_prior 乘 σ_prior 除以 1000。
        i 为 i 加 1。
    完。
    返还 一组：均值、方差、维度、σ_prior。
完。

贝叶斯线性_更新 接受 模型、x、y、σ_noise：
    d 为 模型 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 d：
        若 i 小于 做 计数 用 x：
            先验方差 为 模型 的 第 1 项 的 第 i 项。
            似然精度 为 x 的 第 i 项 乘 x 的 第 i 项 乘 1000 除以 (σ_noise 乘 σ_noise 除以 1000 加 1)。
            可变 后验精度 为 1000 乘 1000 除以 (先验方差 加 1) 加 似然精度。
            若 后验精度 等于 0：
                后验精度 为 1。
            完。
            后验方差 为 1000 乘 1000 除以 后验精度。
            后验均值 为 (模型 的 第 0 项 的 第 i 项 乘 1000 除以 (先验方差 加 1) 加 y 乘 x 的 第 i 项 乘 1000 除以 (σ_noise 乘 σ_noise 除以 1000 加 1)) 乘 1000 除以 后验精度。
            模型 的 第 0 项 的 第 i 项 为 后验均值。
            模型 的 第 1 项 的 第 i 项 为 后验方差。
        完。
        i 为 i 加 1。
    完。
完。

贝叶斯线性_预测 接受 模型、x：
    d 为 模型 的 第 2 项。
    可变 均值 为 0。
    可变 方差 为 0。
    可变 i 为 0。
    只要 i 小于 d：
        若 i 小于 做 计数 用 x：
            均值 为 均值 加 模型 的 第 0 项 的 第 i 项 乘 x 的 第 i 项 除以 1000。
            方差 为 方差 加 模型 的 第 1 项 的 第 i 项 乘 x 的 第 i 项 乘 x 的 第 i 项 除以 (1000 乘 1000)。
        完。
        i 为 i 加 1。
    完。
    返还 一组：均值、方差。
完。

变分_证据下界 接受 log_似然、KL散度：
    返还 log_似然 减 KL散度。
完。

VI_高斯KL 接受 μ_q、σ_q、μ_p、σ_p：
    若 σ_p 等于 0：
        σ_p 为 1。
    完。
    若 σ_q 等于 0：
        σ_q 为 1。
    完。
    比率 为 σ_p 乘 1000 除以 σ_q。
    d 为 μ_q 减 μ_p。
    kl 为 比率 加 (σ_q 乘 σ_q 加 d 乘 d) 乘 1000 除以 (2 乘 σ_p 乘 σ_p 除以 1000 加 1) 减 500。
    返还 kl。
完。

蒙特卡罗_随机丢弃_预测 接受 前向函数结果列表、_采样数：
    T 为 做 计数 用 前向函数结果列表。
    若 T 等于 0：
        返还 一组：0、0。
    完。
    维度 为 做 计数 用 前向函数结果列表 的 第 0 项。
    可变 均值 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 均值、0。
        d 为 d 加 1。
    完。
    可变 t 为 0。
    只要 t 小于 T：
        d 为 0。
        只要 d 小于 维度：
            若 d 小于 做 计数 用 前向函数结果列表 的 第 t 项：
                均值 的 第 d 项 为 均值 的 第 d 项 加 前向函数结果列表 的 第 t 项 的 第 d 项。
            完。
            d 为 d 加 1。
        完。
        t 为 t 加 1。
    完。
    d 为 0。
    只要 d 小于 维度：
        均值 的 第 d 项 为 均值 的 第 d 项 除以 T。
        d 为 d 加 1。
    完。
    可变 方差 为 一组：（空）。
    d 为 0。
    只要 d 小于 维度：
        做 追加 用 方差、0。
        d 为 d 加 1。
    完。
    t 为 0。
    只要 t 小于 T：
        d 为 0。
        只要 d 小于 维度：
            若 d 小于 做 计数 用 前向函数结果列表 的 第 t 项：
                可变 差值 为 前向函数结果列表 的 第 t 项 的 第 d 项 减 均值 的 第 d 项。
                方差 的 第 d 项 为 方差 的 第 d 项 加 差值 乘 差值 除以 1000。
            完。
            d 为 d 加 1。
        完。
        t 为 t 加 1。
    完。
    d 为 0。
    只要 d 小于 维度：
        方差 的 第 d 项 为 方差 的 第 d 项 除以 T。
        d 为 d 加 1。
    完。
    返还 一组：均值、方差。
完。

RBF核 接受 x1、x2、长度尺度：
    d 为 做 计数 用 x1。
    可变 dist2 为 0。
    可变 i 为 0。
    只要 i 小于 d：
        可变 x2v 为 0。
        若 i 小于 做 计数 用 x2：
            x2v 为 x2 的 第 i 项。
        完。
        可变 差值 为 x1 的 第 i 项 减 x2v。
        dist2 为 dist2 加 差值 乘 差值 除以 1000。
        i 为 i 加 1。
    完。
    指数 为 0 减 dist2 乘 1000 除以 (2 乘 长度尺度 乘 长度尺度 除以 1000 加 1)。
    可变 k 为 1000 加 指数。
    若 k 小于 0：
        k 为 0。
    完。
    返还 k。
完。

BO_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、0、1000。
完。

BO_添加观测 接受 bo、x、y：
    做 追加 用 bo 的 第 0 项、x。
    做 追加 用 bo 的 第 1 项、y。
    bo 的 第 2 项 为 bo 的 第 2 项 加 1。
完。

BO_采集_UCB 接受 bo、候选、κ：
    可变 权重和 为 0。
    若 κ 小于或等于 0：
        κ 为 2000。
    完。
    n 为 bo 的 第 2 项。
    可变 最佳 为 0。
    可变 最佳值 为 0 减 0x7FFFFFFF。
    可变 c 为 0。
    只要 c 小于 做 计数 用 候选：
        可变 均值 为 0。
        可变 方差 为 1000。
        若 n 大于 0：
            可变 加权y 为 0。
            可变 i 为 0。
            只要 i 小于 n：
                k 为 做 RBF核 用 候选 的 第 c 项、bo 的 第 0 项 的 第 i 项、bo 的 第 3 项。
                权重和 为 权重和 加 k。
                加权y 为 加权y 加 k 乘 bo 的 第 1 项 的 第 i 项 除以 1000。
                i 为 i 加 1。
            完。
            若 权重和 大于 0：
                均值 为 加权y 乘 1000 除以 权重和。
                方差 为 1000 减 权重和 乘 1000 除以 (权重和 加 1000)。
            完。
        完。
        σ 为 做 整数sqrt 用 方差 乘 1000。
        置信上限 为 均值 加 κ 乘 σ 除以 1000。
        若 置信上限 大于 最佳值：
            最佳 为 c。
            最佳值 为 置信上限。
        完。
        c 为 c 加 1。
    完。
    返还 最佳。
完。

集成_预测 接受 模型输出列表：
    M 为 做 计数 用 模型输出列表。
    若 M 等于 0：
        返还 一组：一组：（空）、一组：（空）。
    完。
    维度 为 做 计数 用 模型输出列表 的 第 0 项。
    可变 均值 为 一组：（空）。
    可变 方差 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        可变 和 为 0。
        可变 i 为 0。
        只要 i 小于 M：
            若 d 小于 做 计数 用 模型输出列表 的 第 i 项：
                和 为 和 加 模型输出列表 的 第 i 项 的 第 d 项。
            完。
            i 为 i 加 1。
        完。
        μ 为 和 除以 M。
        做 追加 用 均值、μ。
        可变 sq和 为 0。
        i 为 0。
        只要 i 小于 M：
            可变 v 为 0。
            若 d 小于 做 计数 用 模型输出列表 的 第 i 项：
                v 为 模型输出列表 的 第 i 项 的 第 d 项。
            完。
            可变 差值 为 v 减 μ。
            sq和 为 sq和 加 差值 乘 差值 除以 1000。
            i 为 i 加 1。
        完。
        做 追加 用 方差、sq和 除以 M。
        d 为 d 加 1。
    完。
    返还 一组：均值、方差。
完。

ECE_计算 接受 置信度列表、正确列表、分桶数：
    可变 ece 为 0。
    若 分桶数 小于或等于 0：
        分桶数 为 10。
    完。
    n 为 做 计数 用 置信度列表。
    可变 b 为 0。
    只要 b 小于 分桶数：
        下界 为 b 乘 1000 除以 分桶数。
        上界 为 (b 加 1) 乘 1000 除以 分桶数。
        可变 桶置信 为 0。
        可变 桶正确 为 0。
        可变 桶数量 为 0。
        可变 i 为 0。
        只要 i 小于 n：
            若 置信度列表 的 第 i 项 大于或等于 下界 并且 置信度列表 的 第 i 项 小于 上界：
                桶置信 为 桶置信 加 置信度列表 的 第 i 项。
                桶正确 为 桶正确 加 正确列表 的 第 i 项。
                桶数量 为 桶数量 加 1。
            完。
            i 为 i 加 1。
        完。
        若 桶数量 大于 0：
            平均置信 为 桶置信 除以 桶数量。
            平均正确 为 桶正确 乘 1000 除以 桶数量。
            可变 差值 为 平均置信 减 平均正确。
            若 差值 小于 0：
                差值 为 0 减 差值。
            完。
            ece 为 ece 加 差值 乘 桶数量 除以 n。
        完。
        b 为 b 加 1。
    完。
    返还 ece。
完。

温度缩放 接受 对数值、T：
    若 T 小于或等于 0：
        T 为 1000。
    完。
    n 为 做 计数 用 对数值。
    可变 缩放 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 缩放、对数值 的 第 i 项 乘 1000 除以 T。
        i 为 i 加 1。
    完。
    返还 缩放。
完。

本章 收束。
