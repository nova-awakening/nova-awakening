《全脑整合》

引用《..·基础》。

可变 脑区列表 为 一组：（空）。

脑区_注册 接受 名哈希、优先级：
    做 追加 用 脑区列表、一组：做 计数 用 脑区列表、名哈希、0、100、优先级。
    返还 做 计数 用 脑区列表 减 1。
完。

脑区_数量 接受 零参：
    返还 做 计数 用 脑区列表。
完。

脑区_激活度 接受 id：
    返还 脑区列表 的 第 id 项 的 第 2 项。
完。

脑区_设激活 接受 id、激活：
    脑区列表 的 第 id 项 的 第 2 项 为 激活。
完。

脑区_输出 接受 id：
    返还 脑区列表 的 第 id 项 的 第 3 项。
完。

脑区_设输出 接受 id、输出：
    脑区列表 的 第 id 项 的 第 3 项 为 输出。
完。

全局广播 接受 _信号、强度：
    可变 i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        脑区列表 的 第 i 项 的 第 2 项 为 脑区列表 的 第 i 项 的 第 2 项 加 强度 乘 脑区列表 的 第 i 项 的 第 5 项 除以 100。
        若 脑区列表 的 第 i 项 的 第 2 项 大于 100：
            脑区列表 的 第 i 项 的 第 2 项 为 100。
        完。
        i 为 i 加 1。
    完。
完。

竞争_选择 接受 零参：
    可变 最强 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 脑区列表：
        若 脑区列表 的 第 i 项 的 第 2 项 大于 脑区列表 的 第 最强 项 的 第 2 项：
            最强 为 i。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        若 i 不等于 最强：
            脑区列表 的 第 i 项 的 第 2 项 为 脑区列表 的 第 i 项 的 第 2 项 乘 50 除以 100。
        完。
        i 为 i 加 1。
    完。
    返还 最强。
完。

能量_分配 接受 总能量：
    可变 总激活 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        总激活 为 总激活 加 脑区列表 的 第 i 项 的 第 2 项。
        i 为 i 加 1。
    完。
    若 总激活 等于 0：
        总激活 为 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        脑区列表 的 第 i 项 的 第 4 项 为 总能量 乘 脑区列表 的 第 i 项 的 第 2 项 除以 总激活。
        i 为 i 加 1。
    完。
完。

反馈_调节 接受 目标激活：
    可变 i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        误差 为 目标激活 减 脑区列表 的 第 i 项 的 第 2 项。
        脑区列表 的 第 i 项 的 第 2 项 为 脑区列表 的 第 i 项 的 第 2 项 加 误差 除以 4。
        若 脑区列表 的 第 i 项 的 第 2 项 小于 0：
            脑区列表 的 第 i 项 的 第 2 项 为 0。
        完。
        i 为 i 加 1。
    完。
完。

认知周期 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        若 脑区列表 的 第 i 项 的 第 2 项 大于 0：
            脑区列表 的 第 i 项 的 第 3 项 为 脑区列表 的 第 i 项 的 第 2 项 乘 脑区列表 的 第 i 项 的 第 4 项 除以 100。
        完。
        i 为 i 加 1。
    完。
    胜者 为 做 竞争_选择。
    做 全局广播 用 脑区列表 的 第 胜者 项 的 第 3 项、20。
    i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        脑区列表 的 第 i 项 的 第 2 项 为 脑区列表 的 第 i 项 的 第 2 项 乘 90 除以 100。
        i 为 i 加 1。
    完。
    返还 胜者。
完。

脑区_清空 接受 零参：
    脑区列表 为 一组：（空）。
完。

脑区_总能量 接受 零参：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        总和 为 总和 加 脑区列表 的 第 i 项 的 第 4 项。
        i 为 i 加 1。
    完。
    返还 总和。
完。

脑区_重置激活 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 脑区列表：
        脑区列表 的 第 i 项 的 第 2 项 为 0。
        i 为 i 加 1。
    完。
完。

合作_扩散 接受 连接矩阵、扩散率：
    n 为 做 脑区_数量。
    可变 新激活 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 新激活、脑区列表 的 第 i 项 的 第 2 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 n：
            若 i 不等于 j 并且 i 乘 n 加 j 小于 做 计数 用 连接矩阵：
                新激活 的 第 i 项 为 新激活 的 第 i 项 加 脑区列表 的 第 j 项 的 第 2 项 乘 连接矩阵 的 第 i 乘 n 加 j 项 乘 扩散率 除以 10000。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        脑区列表 的 第 i 项 的 第 2 项 为 新激活 的 第 i 项。
        若 脑区列表 的 第 i 项 的 第 2 项 大于 100：
            脑区列表 的 第 i 项 的 第 2 项 为 100。
        完。
        若 脑区列表 的 第 i 项 的 第 2 项 小于 0：
            脑区列表 的 第 i 项 的 第 2 项 为 0。
        完。
        i 为 i 加 1。
    完。
完。

多轮认知 接受 轮数、能量：
    可变 胜者列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 轮数：
        做 能量_分配 用 能量。
        胜者 为 做 认知周期。
        做 追加 用 胜者列表、胜者。
        i 为 i 加 1。
    完。
    返还 胜者列表。
完。

注意力_焦点 接受 零参：
    可变 最高 为 0。
    可变 i 为 1。
    只要 i 小于 做 脑区_数量：
        若 脑区列表 的 第 i 项 的 第 2 项 大于 脑区列表 的 第 最高 项 的 第 2 项：
            最高 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最高。
完。

注意力_强度 接受 零参：
    焦点 为 做 注意力_焦点。
    返还 脑区列表 的 第 焦点 项 的 第 2 项。
完。

脑区_报告 接受 零参：
    做 打印 用 「╔═══ 全脑状态 ═══╗」。
    做 打印 用 「║ 脑区数: 」 加 做 转字符串 用 做 脑区_数量 加 「 总能量: 」 加 做 转字符串 用 做 脑区_总能量。
    可变 i 为 0。
    只要 i 小于 做 脑区_数量：
        b 为 脑区列表 的 第 i 项。
        做 打印 用 「║ [」 加 做 转字符串 用 b 的 第 0 项 加 「] 」 加 做 转字符串 用 b 的 第 1 项 加 「 激活=」 加 做 转字符串 用 b 的 第 2 项 加 「 输出=」 加 做 转字符串 用 b 的 第 3 项 加 「 能量=」 加 做 转字符串 用 b 的 第 4 项。
        i 为 i 加 1。
    完。
    做 打印 用 「║ 焦点: 脑区」 加 做 转字符串 用 做 注意力_焦点 加 「 强度=」 加 做 转字符串 用 做 注意力_强度。
    做 打印 用 「╚════════════════╝」。
完。

AI认知_创建 接受 零参：
    做 脑区_清空。
    做 脑区_注册 用 1、80。
    做 脑区_注册 用 2、60。
    做 脑区_注册 用 3、90。
    做 脑区_注册 用 4、70。
    做 脑区_注册 用 5、50。
    做 脑区_注册 用 6、100。
    返还 6。
完。

脑区_排名 接受 零参：
    可变 索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 脑区_数量：
        做 追加 用 索引、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 索引：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 索引：
            若 脑区列表 的 第 索引 的 第 j 项 项 的 第 2 项 大于 脑区列表 的 第 索引 的 第 i 项 项 的 第 2 项：
                临时 为 索引 的 第 i 项。
                索引 的 第 i 项 为 索引 的 第 j 项。
                索引 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 索引。
完。

可变 _通信_源 为 一组：（空）。

可变 _通信_目标 为 一组：（空）。

可变 _通信_强度 为 一组：（空）。

脑区_建立连接 接受 源id、目标id、强度：
    做 追加 用 _通信_源、源id。
    做 追加 用 _通信_目标、目标id。
    做 追加 用 _通信_强度、强度。
完。

脑区_传播激活 接受 源id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _通信_源：
        若 _通信_源 的 第 i 项 等于 源id：
            目标 为 _通信_目标 的 第 i 项。
            增量 为 脑区列表 的 第 源id 项 的 第 2 项 乘 _通信_强度 的 第 i 项 除以 100。
            脑区列表 的 第 目标 项 的 第 2 项 为 脑区列表 的 第 目标 项 的 第 2 项 加 增量。
            若 脑区列表 的 第 目标 项 的 第 2 项 大于 100：
                脑区列表 的 第 目标 项 的 第 2 项 为 100。
            完。
        完。
        i 为 i 加 1。
    完。
完。

认知_总负荷 接受 零参：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 脑区_数量：
        总 为 总 加 脑区列表 的 第 i 项 的 第 2 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

认知_平均负荷 接受 零参：
    n 为 做 脑区_数量。
    若 n 等于 0：
        返还 0。
    完。
    返还 做 认知_总负荷 除以 n。
完。

认知_需要休息 接受 零参：
    返还 做 认知_平均负荷 大于 80。
完。

认知_衰减全部 接受 衰减率：
    可变 i 为 0。
    只要 i 小于 做 脑区_数量：
        脑区列表 的 第 i 项 的 第 2 项 为 脑区列表 的 第 i 项 的 第 2 项 乘 衰减率 除以 100。
        i 为 i 加 1。
    完。
完。

决策_加权投票 接受 脑区投票、脑区权重：
    可变 加权总 为 0。
    可变 权重总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 脑区投票：
        加权总 为 加权总 加 脑区投票 的 第 i 项 乘 脑区权重 的 第 i 项。
        权重总 为 权重总 加 脑区权重 的 第 i 项。
        i 为 i 加 1。
    完。
    若 权重总 等于 0：
        返还 0。
    完。
    返还 加权总 除以 权重总。
完。

决策_共识度 接受 脑区投票：
    n 为 做 计数 用 脑区投票。
    若 n 小于 2：
        返还 1000。
    完。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 脑区投票 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 n：
        d 为 脑区投票 的 第 i 项 减 均值。
        方差 为 方差 加 d 乘 d。
        i 为 i 加 1。
    完。
    返还 1000 减 方差 除以 n。
完。

本章 收束。
