《时间》

引用《..·..·基础》。

每分钟秒 为 60。

每小时秒 为 3600。

每天秒 为 86400。

每周秒 为 604800。

是闰年 接受 年：
    若 年 取余于 400 等于 0：
        返还 真。
    完。
    若 年 取余于 100 等于 0：
        返还 假。
    完。
    若 年 取余于 4 等于 0：
        返还 真。
    完。
    返还 假。
完。

月天数 接受 年、月：
    天表 为 一组：31、28、31、30、31、30、31、31、30、31、30、31。
    若 月 小于 1 或者 月 大于 12：
        返还 0。
    完。
    可变 天数 为 天表 的 第 月 减 1 项。
    若 月 等于 2 并且 做 是闰年 用 年：
        天数 为 29。
    完。
    返还 天数。
完。

年天数 接受 年：
    若 做 是闰年 用 年：
        返还 366。
    完。
    返还 365。
完。

日期时间_创建 接受 年、月、日、小时、分钟、秒值：
    返还 一组：年、月、日、小时、分钟、秒值、0。
完。

日期时间_带时区 接受 年、月、日、小时、分钟、秒值、时区偏移小时：
    返还 一组：年、月、日、小时、分钟、秒值、时区偏移小时 乘 3600。
完。

日期时间_到纪元秒 接受 dt：
    可变 总天 为 0。
    可变 年 为 1970。
    若 dt 的 第 0 项 大于或等于 1970：
        只要 年 小于 dt 的 第 0 项：
            总天 为 总天 加 做 年天数 用 年。
            年 为 年 加 1。
        完。
    否则：
        年 为 dt 的 第 0 项。
        只要 年 小于 1970：
            总天 为 总天 减 做 年天数 用 年。
            年 为 年 加 1。
        完。
    完。
    可变 月 为 1。
    只要 月 小于 dt 的 第 1 项：
        总天 为 总天 加 做 月天数 用 dt 的 第 0 项、月。
        月 为 月 加 1。
    完。
    总天 为 总天 加 dt 的 第 2 项 减 1。
    返还 总天 乘 每天秒 加 dt 的 第 3 项 乘 每小时秒 加 dt 的 第 4 项 乘 每分钟秒 加 dt 的 第 5 项 减 dt 的 第 6 项。
完。

日期时间_从纪元秒 接受 秒、时区偏移秒：
    可变 本地秒 为 秒 加 时区偏移秒。
    可变 天 为 本地秒 除以 每天秒。
    可变 日内秒 为 本地秒 取余于 每天秒。
    若 日内秒 小于 0：
        日内秒 为 日内秒 加 每天秒。
        天 为 天 减 1。
    完。
    小时 为 日内秒 除以 每小时秒。
    分钟 为 日内秒 取余于 每小时秒 除以 每分钟秒。
    秒值 为 日内秒 取余于 每分钟秒。
    可变 年 为 1970。
    若 天 大于或等于 0：
        只要 天 大于或等于 做 年天数 用 年：
            天 为 天 减 做 年天数 用 年。
            年 为 年 加 1。
        完。
    否则：
        只要 天 小于 0：
            年 为 年 减 1。
            天 为 天 加 做 年天数 用 年。
        完。
    完。
    可变 月 为 1。
    只要 月 小于或等于 12 并且 天 大于或等于 做 月天数 用 年、月：
        天 为 天 减 做 月天数 用 年、月。
        月 为 月 加 1。
    完。
    返还 一组：年、月、天 加 1、小时、分钟、秒值、时区偏移秒。
完。

日期时间_加秒 接受 dt、秒数：
    返还 做 日期时间_从纪元秒 用 做 日期时间_到纪元秒 用 dt 加 秒数、dt 的 第 6 项。
完。

日期时间_加天 接受 dt、天数：
    返还 做 日期时间_加秒 用 dt、天数 乘 每天秒。
完。

日期时间_加月 接受 dt、月数：
    可变 年 为 dt 的 第 0 项。
    可变 月 为 dt 的 第 1 项 加 月数。
    只要 月 大于 12：
        月 为 月 减 12。
        年 为 年 加 1。
    完。
    只要 月 小于 1：
        月 为 月 加 12。
        年 为 年 减 1。
    完。
    可变 日 为 dt 的 第 2 项。
    最大日 为 做 月天数 用 年、月。
    若 日 大于 最大日：
        日 为 最大日。
    完。
    返还 一组：年、月、日、dt 的 第 3 项、dt 的 第 4 项、dt 的 第 5 项、dt 的 第 6 项。
完。

日期时间_差秒 接受 dt1、dt2：
    返还 做 日期时间_到纪元秒 用 dt1 减 做 日期时间_到纪元秒 用 dt2。
完。

日期时间_差天 接受 dt1、dt2：
    若 每天秒 等于 0：
        返还 0。
    完。
    返还 做 日期时间_差秒 用 dt1、dt2 除以 每天秒。
完。

日期时间_星期 接受 dt：
    可变 天 为 做 日期时间_到纪元秒 用 dt 除以 每天秒。
    可变 星期 为 (天 加 4) 取余于 7。
    若 星期 小于 0：
        星期 为 星期 加 7。
    完。
    返还 星期。
完。

日期时间_年中日 接受 dt：
    可变 总日 为 dt 的 第 2 项。
    可变 月 为 1。
    只要 月 小于 dt 的 第 1 项：
        总日 为 总日 加 做 月天数 用 dt 的 第 0 项、月。
        月 为 月 加 1。
    完。
    返还 总日。
完。

时间_数到文 接受 数：
    返还 做 转字符串 用 数。
完。

时间_补零 接受 数、宽度：
    可变 文 为 做 时间_数到文 用 数。
    只要 做 计数 用 文 小于 宽度：
        文 为 做 字符串拼接 用 「0」、文。
    完。
    返还 文。
完。

日期时间_ISO8601 接受 dt：
    可变 结果 为 「」。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 dt 的 第 0 项、4。
    结果 为 做 字符串拼接 用 结果、「-」。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 dt 的 第 1 项、2。
    结果 为 做 字符串拼接 用 结果、「-」。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 dt 的 第 2 项、2。
    结果 为 做 字符串拼接 用 结果、「T」。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 dt 的 第 3 项、2。
    结果 为 做 字符串拼接 用 结果、「:」。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 dt 的 第 4 项、2。
    结果 为 做 字符串拼接 用 结果、「:」。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 dt 的 第 5 项、2。
    若 dt 的 第 6 项 等于 0：
        结果 为 做 字符串拼接 用 结果、「Z」。
    否则：
        可变 偏移 为 dt 的 第 6 项。
        若 偏移 大于或等于 0：
            结果 为 做 字符串拼接 用 结果、「+」。
        否则：
            结果 为 做 字符串拼接 用 结果、「-」。
            偏移 为 0 减 偏移。
        完。
        结果 为 做 字符串拼接 用 结果、做 时间_补零 用 偏移 除以 每小时秒、2。
        结果 为 做 字符串拼接 用 结果、「:」。
        结果 为 做 字符串拼接 用 结果、做 时间_补零 用 偏移 取余于 每小时秒 除以 每分钟秒、2。
    完。
    返还 结果。
完。

日期时间_日期字符串 接受 dt：
    可变 结果 为 做 时间_补零 用 dt 的 第 0 项、4。
    结果 为 做 字符串拼接 用 结果、「-」。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 dt 的 第 1 项、2。
    结果 为 做 字符串拼接 用 结果、「-」。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 dt 的 第 2 项、2。
    返还 结果。
完。

日期时间_时间字符串 接受 dt：
    可变 结果 为 做 时间_补零 用 dt 的 第 3 项、2。
    结果 为 做 字符串拼接 用 结果、「:」。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 dt 的 第 4 项、2。
    结果 为 做 字符串拼接 用 结果、「:」。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 dt 的 第 5 项、2。
    返还 结果。
完。

时长_秒 接受 秒数：
    返还 一组：秒数。
完。

时长_分钟 接受 分钟数：
    返还 一组：分钟数 乘 每分钟秒。
完。

时长_小时 接受 小时数：
    返还 一组：小时数 乘 每小时秒。
完。

时长_天 接受 天数：
    返还 一组：天数 乘 每天秒。
完。

时长_加 接受 a、b：
    返还 一组：a 的 第 0 项 加 b 的 第 0 项。
完。

时长_减 接受 a、b：
    返还 一组：a 的 第 0 项 减 b 的 第 0 项。
完。

时长_乘 接受 d、n：
    返还 一组：d 的 第 0 项 乘 n。
完。

时长_总秒 接受 d：
    返还 d 的 第 0 项。
完。

时长_总分 接受 d：
    若 每分钟秒 等于 0：
        返还 0。
    完。
    返还 d 的 第 0 项 除以 每分钟秒。
完。

时长_总时 接受 d：
    若 每小时秒 等于 0：
        返还 0。
    完。
    返还 d 的 第 0 项 除以 每小时秒。
完。

时长_格式化 接受 d：
    可变 秒数 为 d 的 第 0 项。
    可变 负 为 假。
    若 秒数 小于 0：
        负 为 真。
        秒数 为 0 减 秒数。
    完。
    天数 为 秒数 除以 每天秒。
    小时 为 秒数 取余于 每天秒 除以 每小时秒。
    分钟 为 秒数 取余于 每小时秒 除以 每分钟秒。
    秒值 为 秒数 取余于 每分钟秒。
    可变 结果 为 「」。
    若 负：
        结果 为 「-」。
    完。
    若 天数 大于 0：
        结果 为 做 字符串拼接 用 结果、做 时间_数到文 用 天数。
        结果 为 做 字符串拼接 用 结果、「d 」。
    完。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 小时、2。
    结果 为 做 字符串拼接 用 结果、「:」。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 分钟、2。
    结果 为 做 字符串拼接 用 结果、「:」。
    结果 为 做 字符串拼接 用 结果、做 时间_补零 用 秒值、2。
    返还 结果。
完。

计时器_创建 接受 零参：
    返还 一组：0、0、假、一组：（空）。
完。

计时器_开始 接受 sw、当前tick：
    若 sw 的 第 2 项：
        返还。
    完。
    sw 的 第 0 项 为 当前tick。
    sw 的 第 2 项 为 真。
完。

计时器_停止 接受 sw、当前tick：
    若 sw 的 第 2 项 等于 假：
        返还。
    完。
    sw 的 第 1 项 为 sw 的 第 1 项 加 (当前tick 减 sw 的 第 0 项)。
    sw 的 第 2 项 为 假。
完。

计时器_圈 接受 sw、当前tick：
    可变 已过 为 sw 的 第 1 项。
    若 sw 的 第 2 项：
        已过 为 已过 加 (当前tick 减 sw 的 第 0 项)。
    完。
    做 追加 用 sw 的 第 3 项、已过。
    返还 已过。
完。

计时器_重置 接受 sw：
    sw 的 第 0 项 为 0。
    sw 的 第 1 项 为 0。
    sw 的 第 2 项 为 假。
    sw 的 第 3 项 为 一组：（空）。
完。

计时器_已过 接受 sw、当前tick：
    可变 已过 为 sw 的 第 1 项。
    若 sw 的 第 2 项：
        已过 为 已过 加 (当前tick 减 sw 的 第 0 项)。
    完。
    返还 已过。
完。

计时器_圈列表 接受 sw：
    返还 sw 的 第 3 项。
完。

本章 收束。
