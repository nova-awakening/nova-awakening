《硬件库》

引用《..·..·运行时·原语桩》。

引用《..·基础》。

引用《..·..·自举兼容桥》。

可变 _内核32库 为 0。

可变 _用户32库 为 0。

可变 _图形32库 为 0。

写文件 接受 文件名、数据、大小：
    k 为 做 _初始化内核。
    创建文件 为 做 获取函数 用 k、「CreateFileA」。
    写入文件 为 做 获取函数 用 k、「WriteFile」。
    关闭句柄 为 做 获取函数 用 k、「CloseHandle」。
    句柄 为 做 调用指针 用 创建文件、文件名、0x40000000、0、2、0x80。
    若 句柄 等于 负 1：
        返还 0。
    完。
    做 调用指针 用 写入文件、句柄、数据、大小、已写、0。
    做 调用指针 用 关闭句柄、句柄、0、0、0、0。
    返还 1。
完。

_初始化内核 接受 零参：
    若 _内核32库 等于 0：
        _内核32库 为 做 加载库 用 「kernel32.dll」。
    完。
    返还 _内核32库。
完。

_初始化用户 接受 零参：
    若 _用户32库 等于 0：
        _用户32库 为 做 加载库 用 「user32.dll」。
    完。
    返还 _用户32库。
完。

_初始化图形 接受 零参：
    若 _图形32库 等于 0：
        _图形32库 为 做 加载库 用 「gdi32.dll」。
    完。
    返还 _图形32库。
完。

可变 _函数_节拍 为 0。

可变 _函数_睡眠 为 0。

可变 _函数_查询性能计数 为 0。

可变 _函数_查询性能频率 为 0。

可变 _函数_获取系统度量 为 0。

可变 _函数_消息框 为 0。

可变 _函数_获取设备上下文 为 0。

可变 _函数_释放设备上下文 为 0。

可变 _函数_获取像素 为 0。

可变 _函数_设置像素 为 0。

可变 _函数_获取异步键状态 为 0。

可变 _函数_获取光标位置 为 0。

可变 _函数_设光标位置 为 0。

可变 _函数_显示光标 为 0。

可变 _函数_查找窗口 为 0。

可变 _函数_获取文件属性 为 0。

可变 _函数_移动文件 为 0。

可变 _函数_删除文件 为 0。

可变 _函数_蜂鸣 为 0。

可变 _函数_获取进程ID 为 0。

可变 _函数_设代码页 为 0。

时间戳 接受 零参：
    做 _初始化内核。
    若 _函数_节拍 等于 0：
        _函数_节拍 为 做 获取函数 用 _内核32库、「GetTickCount64」。
    完。
    返还 做 调用指针 用 _函数_节拍、0、0、0、0、0。
完。

睡眠 接受 毫秒：
    做 _初始化内核。
    若 _函数_睡眠 等于 0：
        _函数_睡眠 为 做 获取函数 用 _内核32库、「Sleep」。
    完。
    做 调用指针 用 _函数_睡眠、毫秒、0、0、0、0。
完。

高精度计时 接受 零参：
    做 _初始化内核。
    若 _函数_查询性能计数 等于 0：
        _函数_查询性能计数 为 做 获取函数 用 _内核32库、「QueryPerformanceCounter」。
    完。
    缓冲 为 做 缓冲区 用 8。
    做 调用指针 用 _函数_查询性能计数、缓冲、0、0、0、0。
    返还 做 内存读 用 缓冲。
完。

计时频率 接受 零参：
    做 _初始化内核。
    若 _函数_查询性能频率 等于 0：
        _函数_查询性能频率 为 做 获取函数 用 _内核32库、「QueryPerformanceFrequency」。
    完。
    缓冲 为 做 缓冲区 用 8。
    做 调用指针 用 _函数_查询性能频率、缓冲、0、0、0、0。
    返还 做 内存读 用 缓冲。
完。

进程号 接受 零参：
    做 _初始化内核。
    若 _函数_获取进程ID 等于 0：
        _函数_获取进程ID 为 做 获取函数 用 _内核32库、「GetCurrentProcessId」。
    完。
    返还 做 调用指针 用 _函数_获取进程ID、0、0、0、0、0。
完。

蜂鸣 接受 频率、毫秒：
    做 _初始化内核。
    若 _函数_蜂鸣 等于 0：
        _函数_蜂鸣 为 做 获取函数 用 _内核32库、「Beep」。
    完。
    做 调用指针 用 _函数_蜂鸣、频率、毫秒、0、0、0。
完。

设置编码 接受 代码页：
    做 _初始化内核。
    若 _函数_设代码页 等于 0：
        _函数_设代码页 为 做 获取函数 用 _内核32库、「SetConsoleOutputCP」。
    完。
    做 调用指针 用 _函数_设代码页、代码页、0、0、0、0。
完。

屏幕宽度 接受 零参：
    做 _初始化用户。
    若 _函数_获取系统度量 等于 0：
        _函数_获取系统度量 为 做 获取函数 用 _用户32库、「GetSystemMetrics」。
    完。
    返还 做 调用指针 用 _函数_获取系统度量、0、0、0、0、0。
完。

屏幕高度 接受 零参：
    做 _初始化用户。
    若 _函数_获取系统度量 等于 0：
        _函数_获取系统度量 为 做 获取函数 用 _用户32库、「GetSystemMetrics」。
    完。
    返还 做 调用指针 用 _函数_获取系统度量、1、0、0、0、0。
完。

获取屏幕DC 接受 零参：
    做 _初始化用户。
    若 _函数_获取设备上下文 等于 0：
        _函数_获取设备上下文 为 做 获取函数 用 _用户32库、「GetDC」。
    完。
    返还 做 调用指针 用 _函数_获取设备上下文、0、0、0、0、0。
完。

释放DC 接受 dc：
    做 _初始化用户。
    若 _函数_释放设备上下文 等于 0：
        _函数_释放设备上下文 为 做 获取函数 用 _用户32库、「ReleaseDC」。
    完。
    做 调用指针 用 _函数_释放设备上下文、0、dc、0、0、0。
完。

读像素 接受 x、y：
    做 _初始化图形。
    若 _函数_获取像素 等于 0：
        _函数_获取像素 为 做 获取函数 用 _图形32库、「GetPixel」。
    完。
    dc 为 做 获取屏幕DC。
    颜色 为 做 调用指针 用 _函数_获取像素、dc、x、y、0、0。
    做 释放DC 用 dc。
    返还 颜色。
完。

写像素 接受 x、y、颜色：
    做 _初始化图形。
    若 _函数_设置像素 等于 0：
        _函数_设置像素 为 做 获取函数 用 _图形32库、「SetPixel」。
    完。
    dc 为 做 获取屏幕DC。
    做 调用指针4 用 _函数_设置像素、dc、x、y、颜色。
    做 释放DC 用 dc。
完。

红色分量 接受 颜色：
    返还 颜色 位与 0xFF。
完。

绿色分量 接受 颜色：
    返还 颜色 右移 8 位与 0xFF。
完。

蓝色分量 接受 颜色：
    返还 颜色 右移 16 位与 0xFF。
完。

组合颜色 接受 r、g、b：
    返还 r 位或 g 左移 8 位或 b 左移 16。
完。

鼠标位置 接受 零参：
    做 _初始化用户。
    若 _函数_获取光标位置 等于 0：
        _函数_获取光标位置 为 做 获取函数 用 _用户32库、「GetCursorPos」。
    完。
    缓冲 为 做 缓冲区 用 16。
    做 调用指针 用 _函数_获取光标位置、缓冲、0、0、0、0。
    x 为 做 内存读32 用 缓冲。
    y 为 做 内存读32 用 缓冲 加 4。
    返还 一组：x、y。
完。

移动鼠标 接受 x、y：
    做 _初始化用户。
    若 _函数_设光标位置 等于 0：
        _函数_设光标位置 为 做 获取函数 用 _用户32库、「SetCursorPos」。
    完。
    做 调用指针 用 _函数_设光标位置、x、y、0、0、0。
完。

按键状态 接受 键码：
    做 _初始化用户。
    若 _函数_获取异步键状态 等于 0：
        _函数_获取异步键状态 为 做 获取函数 用 _用户32库、「GetAsyncKeyState」。
    完。
    r 为 做 调用指针 用 _函数_获取异步键状态、键码、0、0、0、0。
    返还 r 位与 0x8000。
完。

键_左键 为 0x01。

键_右键 为 0x02。

键_退格 为 0x08。

键_Tab 为 0x09。

键_回车 为 0x0D。

键_Shift 为 0x10。

键_Ctrl 为 0x11。

键_Alt 为 0x12。

键_Esc 为 0x1B。

键_空格 为 0x20。

键_左 为 0x25。

键_上 为 0x26。

键_右 为 0x27。

键_下 为 0x28。

键_A 为 0x41。

键_B 为 0x42。

键_C 为 0x43。

键_D 为 0x44。

键_E 为 0x45。

键_F 为 0x46。

键_G 为 0x47。

键_H 为 0x48。

键_I 为 0x49。

键_J 为 0x4A。

键_K 为 0x4B。

键_L 为 0x4C。

键_M 为 0x4D。

键_N 为 0x4E。

键_O 为 0x4F。

键_P 为 0x50。

键_Q 为 0x51。

键_R 为 0x52。

键_S 为 0x53。

键_T 为 0x54。

键_U 为 0x55。

键_V 为 0x56。

键_W 为 0x57。

键_X 为 0x58。

键_Y 为 0x59。

键_Z 为 0x5A。

键_0 为 0x30。

键_1 为 0x31。

键_2 为 0x32。

键_3 为 0x33。

键_4 为 0x34。

键_5 为 0x35。

键_6 为 0x36。

键_7 为 0x37。

键_8 为 0x38。

键_9 为 0x39。

键_F1 为 0x70。

键_F2 为 0x71。

键_F3 为 0x72。

键_F4 为 0x73。

键_F5 为 0x74。

键_F6 为 0x75。

键_F7 为 0x76。

键_F8 为 0x77。

键_F9 为 0x78。

键_F10 为 0x79。

键_F11 为 0x7A。

键_F12 为 0x7B。

键_Delete 为 0x2E。

键_Insert 为 0x2D。

键_Home 为 0x24。

键_End 为 0x23。

键_PageUp 为 0x21。

键_PageDown 为 0x22。

查找窗口 接受 标题：
    做 _初始化用户。
    若 _函数_查找窗口 等于 0：
        _函数_查找窗口 为 做 获取函数 用 _用户32库、「FindWindowA」。
    完。
    返还 做 调用指针 用 _函数_查找窗口、0、标题、0、0、0。
完。

消息框 接受 文本、标题、_类型：
    做 _初始化用户。
    若 _函数_消息框 等于 0：
        _函数_消息框 为 做 获取函数 用 _用户32库、「MessageBoxA」。
    完。
    返还 做 调用指针 用 _函数_消息框、0、文本、标题、0、0。
完。

消息_确定 为 0。

消息_确定取消 为 1。

消息_是否 为 4。

消息_是否取消 为 3。

返回_确定 为 1。

返回_取消 为 2。

返回_是 为 6。

返回_否 为 7。

文件存在 接受 路径：
    做 _初始化内核。
    若 _函数_获取文件属性 等于 0：
        _函数_获取文件属性 为 做 获取函数 用 _内核32库、「GetFileAttributesA」。
    完。
    r 为 做 调用指针 用 _函数_获取文件属性、路径、0、0、0、0。
    若 r 等于 0 减 1：
        返还 0。
    完。
    返还 1。
完。

删除文件 接受 路径：
    做 _初始化内核。
    若 _函数_删除文件 等于 0：
        _函数_删除文件 为 做 获取函数 用 _内核32库、「DeleteFileA」。
    完。
    返还 做 调用指针 用 _函数_删除文件、路径、0、0、0、0。
完。

移动文件 接受 源、目标：
    做 _初始化内核。
    若 _函数_移动文件 等于 0：
        _函数_移动文件 为 做 获取函数 用 _内核32库、「MoveFileA」。
    完。
    返还 做 调用指针 用 _函数_移动文件、源、目标、0、0、0。
完。

可变 _随机种子 为 0。

随机数 接受 最大值：
    若 _随机种子 等于 0：
        _随机种子 为 做 时间戳。
    完。
    _随机种子 为 _随机种子 乘 1103515245 加 12345。
    r 为 _随机种子 右移 16 位与 0x7FFF。
    若 最大值 大于 0：
        返还 r 减 r 除以 最大值 乘 最大值。
    完。
    返还 r。
完。

随机种子 接受 种子：
    _随机种子 为 种子。
完。

开始计时 接受 零参：
    返还 做 时间戳。
完。

结束计时 接受 开始：
    结束 为 做 时间戳。
    返还 结束 减 开始。
完。

缓冲清零 接受 块、大小：
    若 块 等于 空 或者 大小 小于或等于 0：
        返还 块。
    完。
    可变 上限 为 大小。
    若 做 计数 用 块 小于 上限：
        上限 为 做 计数 用 块。
    完。
    可变 i 为 0。
    只要 i 小于 上限：
        块 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    返还 块。
完。

零内存 接受 大小：
    块 为 做 缓冲区 用 大小。
    返还 做 缓冲清零 用 块、大小。
完。

内存比较 接受 地址1、地址2、长度：
    可变 i 为 0。
    只要 i 小于 长度：
        a 为 做 内存读8 用 地址1 加 i。
        b 为 做 内存读8 用 地址2 加 i。
        若 a 不等于 b：
            返还 i 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

内存搜索 接受 地址、长度、字节：
    可变 i 为 0。
    只要 i 小于 长度：
        若 做 内存读8 用 地址 加 i 等于 字节：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 0 减 1。
完。

内存转储 接受 地址、长度：
    hex 为 「0123456789ABCDEF」。
    可变 i 为 0。
    只要 i 小于 长度：
        b 为 做 内存读8 用 地址 加 i。
        高 为 b 右移 4 位与 0x0F。
        低 为 b 位与 0x0F。
        做 打印文本 用 做 字符串拼接 用 做 子串 用 hex、高、1、做 子串 用 hex、低、1。
        i 为 i 加 1。
    完。
完。

可变 _函数_移动至 为 0。

可变 _函数_画线至 为 0。

可变 _函数_创建画笔 为 0。

可变 _函数_选择对象 为 0。

可变 _函数_删除对象 为 0。

可变 _函数_画矩形 为 0。

可变 _函数_画椭圆 为 0。

可变 _函数_创建画刷 为 0。

可变 _函数_输出文本 为 0。

可变 _函数_设文本颜色 为 0。

可变 _函数_设背景模式 为 0。

画线到 接受 dc、x、y：
    做 _初始化图形。
    若 _函数_画线至 等于 0：
        _函数_画线至 为 做 获取函数 用 _图形32库、「LineTo」。
    完。
    做 调用指针 用 _函数_画线至、dc、x、y、0、0。
完。

移动画笔 接受 dc、x、y：
    做 _初始化图形。
    若 _函数_移动至 等于 0：
        _函数_移动至 为 做 获取函数 用 _图形32库、「MoveToEx」。
    完。
    做 调用指针4 用 _函数_移动至、dc、x、y、0。
完。

画线 接受 dc、x1、y1、x2、y2：
    做 移动画笔 用 dc、x1、y1。
    做 画线到 用 dc、x2、y2。
完。

创建画笔 接受 样式、宽度、颜色：
    做 _初始化图形。
    若 _函数_创建画笔 等于 0：
        _函数_创建画笔 为 做 获取函数 用 _图形32库、「CreatePen」。
    完。
    返还 做 调用指针 用 _函数_创建画笔、样式、宽度、颜色、0、0。
完。

创建画刷 接受 颜色：
    做 _初始化图形。
    若 _函数_创建画刷 等于 0：
        _函数_创建画刷 为 做 获取函数 用 _图形32库、「CreateSolidBrush」。
    完。
    返还 做 调用指针 用 _函数_创建画刷、颜色、0、0、0、0。
完。

选择对象 接受 dc、对象：
    做 _初始化图形。
    若 _函数_选择对象 等于 0：
        _函数_选择对象 为 做 获取函数 用 _图形32库、「SelectObject」。
    完。
    返还 做 调用指针 用 _函数_选择对象、dc、对象、0、0、0。
完。

删除对象 接受 对象：
    做 _初始化图形。
    若 _函数_删除对象 等于 0：
        _函数_删除对象 为 做 获取函数 用 _图形32库、「DeleteObject」。
    完。
    做 调用指针 用 _函数_删除对象、对象、0、0、0、0。
完。

画矩形 接受 dc、左、上、右、下：
    做 _初始化图形。
    若 _函数_画矩形 等于 0：
        _函数_画矩形 为 做 获取函数 用 _图形32库、「Rectangle」。
    完。
    做 调用指针5 用 _函数_画矩形、dc、左、上、右、下。
完。

画椭圆 接受 dc、左、上、右、下：
    做 _初始化图形。
    若 _函数_画椭圆 等于 0：
        _函数_画椭圆 为 做 获取函数 用 _图形32库、「Ellipse」。
    完。
    做 调用指针5 用 _函数_画椭圆、dc、左、上、右、下。
完。

设置文字颜色 接受 dc、颜色：
    做 _初始化图形。
    若 _函数_设文本颜色 等于 0：
        _函数_设文本颜色 为 做 获取函数 用 _图形32库、「SetTextColor」。
    完。
    做 调用指针 用 _函数_设文本颜色、dc、颜色、0、0、0。
完。

设置背景模式 接受 dc、模式：
    做 _初始化图形。
    若 _函数_设背景模式 等于 0：
        _函数_设背景模式 为 做 获取函数 用 _图形32库、「SetBkMode」。
    完。
    做 调用指针 用 _函数_设背景模式、dc、模式、0、0、0。
完。

画笔_实线 为 0。

画笔_虚线 为 1。

画笔_点线 为 2。

可变 _多媒体库 为 0。

可变 _函数_播放声音 为 0。

_初始化音频 接受 零参：
    若 _多媒体库 等于 0：
        _多媒体库 为 做 加载库 用 「winmm.dll」。
    完。
    返还 _多媒体库。
完。

播放音频 接受 文件名：
    做 _初始化音频。
    若 _函数_播放声音 等于 0：
        _函数_播放声音 为 做 获取函数 用 _多媒体库、「PlaySoundA」。
    完。
    做 调用指针 用 _函数_播放声音、文件名、x20001、0、0、0。
完。

系统声音 接受 类型：
    做 _初始化用户。
    可变 fn_mb 为 做 获取函数 用 _用户32库、「MessageBeep」。
    做 调用指针 用 fn_mb、类型、0、0、0、0。
完。

声音_默认 为 0。

声音_错误 为 0x10。

声音_提问 为 0x20。

声音_警告 为 0x30。

声音_信息 为 0x40。

可变 _函数_获取环境变量 为 0。

可变 _函数_系统目录 为 0。

可变 _函数_窗口目录 为 0。

可变 _函数_获取模块 为 0。

环境变量 接受 名称、缓冲、大小：
    做 _初始化内核。
    若 _函数_获取环境变量 等于 0：
        _函数_获取环境变量 为 做 获取函数 用 _内核32库、「GetEnvironmentVariableA」。
    完。
    返还 做 调用指针 用 _函数_获取环境变量、名称、缓冲、大小、0、0。
完。

系统目录 接受 缓冲、大小：
    做 _初始化内核。
    若 _函数_系统目录 等于 0：
        _函数_系统目录 为 做 获取函数 用 _内核32库、「GetSystemDirectoryA」。
    完。
    返还 做 调用指针 用 _函数_系统目录、缓冲、大小、0、0、0。
完。

Windows目录 接受 缓冲、大小：
    做 _初始化内核。
    若 _函数_窗口目录 等于 0：
        _函数_窗口目录 为 做 获取函数 用 _内核32库、「GetWindowsDirectoryA」。
    完。
    返还 做 调用指针 用 _函数_窗口目录、缓冲、大小、0、0、0。
完。

程序句柄 接受 零参：
    做 _初始化内核。
    若 _函数_获取模块 等于 0：
        _函数_获取模块 为 做 获取函数 用 _内核32库、「GetModuleHandleA」。
    完。
    返还 做 调用指针 用 _函数_获取模块、0、0、0、0、0。
完。

画十字 接受 dc、x、y、大小、颜色：
    笔 为 做 创建画笔 用 0、1、颜色。
    旧笔 为 做 选择对象 用 dc、笔。
    做 画线 用 dc、x 减 大小、y、x 加 大小、y。
    做 画线 用 dc、x、y 减 大小、x、y 加 大小。
    做 选择对象 用 dc、旧笔。
    做 删除对象 用 笔。
完。

画色块 接受 dc、左、上、宽、高、颜色：
    刷 为 做 创建画刷 用 颜色。
    笔 为 做 创建画笔 用 0、1、颜色。
    旧刷 为 做 选择对象 用 dc、刷。
    旧笔 为 做 选择对象 用 dc、笔。
    做 画矩形 用 dc、左、上、左 加 宽、上 加 高。
    做 选择对象 用 dc、旧笔。
    做 选择对象 用 dc、旧刷。
    做 删除对象 用 笔。
    做 删除对象 用 刷。
完。

画网格 接受 dc、x0、y0、列数、行数、格宽、颜色：
    笔 为 做 创建画笔 用 0、1、颜色。
    旧笔 为 做 选择对象 用 dc、笔。
    总宽 为 列数 乘 格宽。
    总高 为 行数 乘 格宽。
    可变 i 为 0。
    只要 i 小于或等于 列数：
        做 画线 用 dc、x0 加 i 乘 格宽、y0、x0 加 i 乘 格宽、y0 加 总高。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于或等于 行数：
        做 画线 用 dc、x0、y0 加 i 乘 格宽、x0 加 总宽、y0 加 i 乘 格宽。
        i 为 i 加 1。
    完。
    做 选择对象 用 dc、旧笔。
    做 删除对象 用 笔。
完。

去空格 接受 s：
    总 为 做 字节长度 用 s。
    若 总 等于 0：
        返还 「」。
    完。
    可变 i 为 0。
    可变 找到 为 0。
    只要 i 小于 总 并且 找到 等于 0：
        c 为 做 字节取 用 s、i。
        若 c 不等于 字符_空格 并且 c 不等于 字符_制表 并且 c 不等于 字符_换行 并且 c 不等于 字符_回车：
            找到 为 1。
        否则：
            i 为 i 加 1。
        完。
    完。
    若 找到 等于 0：
        返还 「」。
    完。
    开始 为 i。
    可变 j 为 总 减 1。
    找到 为 0。
    只要 j 大于或等于 开始 并且 找到 等于 0：
        c2 为 做 字节取 用 s、j。
        若 c2 不等于 32 并且 c2 不等于 9 并且 c2 不等于 10 并且 c2 不等于 13：
            找到 为 1。
        否则：
            j 为 j 减 1。
        完。
    完。
    长 为 j 减 开始 加 1。
    缓冲 为 做 缓冲区 用 长 加 1。
    可变 k 为 0。
    只要 k 小于 长：
        做 缓冲写 用 缓冲、k、做 字节取 用 s、开始 加 k。
        k 为 k 加 1。
    完。
    做 缓冲写 用 缓冲、长、0。
    返还 缓冲。
完。

重复 接受 s、n：
    总 为 做 字节长度 用 s。
    若 总 等于 0 或者 n 小于或等于 0：
        返还 「」。
    完。
    缓冲 为 做 缓冲区 用 总 乘 n 加 1。
    可变 偏移 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 总：
            做 缓冲写 用 缓冲、偏移、做 字节取 用 s、j。
            偏移 为 偏移 加 1。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    做 缓冲写 用 缓冲、偏移、0。
    返还 缓冲。
完。

连接 接受 列表、分隔：
    总数 为 做 计数 用 列表。
    若 总数 等于 0：
        返还 「」。
    完。
    若 总数 等于 1：
        返还 列表 的 第 0 项。
    完。
    分隔长 为 做 字节长度 用 分隔。
    可变 总长 为 0。
    可变 i 为 0。
    只要 i 小于 总数：
        总长 为 总长 加 做 字节长度 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    总长 为 总长 加 分隔长 乘 (总数 减 1) 加 1。
    缓冲 为 做 缓冲区 用 总长。
    可变 偏移 为 0。
    i 为 0。
    只要 i 小于 总数：
        若 i 大于 0：
            可变 k 为 0。
            只要 k 小于 分隔长：
                做 缓冲写 用 缓冲、偏移、做 字节取 用 分隔、k。
                偏移 为 偏移 加 1。
                k 为 k 加 1。
            完。
        完。
        当前长 为 做 字节长度 用 列表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 当前长：
            做 缓冲写 用 缓冲、偏移、做 字节取 用 列表 的 第 i 项、j。
            偏移 为 偏移 加 1。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    做 缓冲写 用 缓冲、偏移、0。
    返还 缓冲。
完。

分割 接受 s、分隔符：
    总 为 做 字节长度 用 s。
    分字节 为 做 编码 用 做 子串 用 分隔符、0、1。
    可变 结果 为 一组：（空）。
    可变 开始 为 0。
    可变 i 为 0。
    只要 i 小于或等于 总：
        若 i 等于 总 或者 做 字节取 用 s、i 等于 分字节：
            长 为 i 减 开始。
            片 为 做 缓冲区 用 长 加 1。
            可变 k 为 0。
            只要 k 小于 长：
                做 缓冲写 用 片、k、做 字节取 用 s、开始 加 k。
                k 为 k 加 1。
            完。
            做 缓冲写 用 片、长、0。
            做 追加 用 结果、片。
            开始 为 i 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

左填充 接受 s、目标长度、填充字符：
    当前长 为 做 计数 用 s。
    若 当前长 大于或等于 目标长度：
        返还 s。
    完。
    返还 做 字符串拼接 用 做 重复 用 填充字符、目标长度 减 当前长、s。
完。

右填充 接受 s、目标长度、填充字符：
    当前长 为 做 计数 用 s。
    若 当前长 大于或等于 目标长度：
        返还 s。
    完。
    返还 做 字符串拼接 用 s、做 重复 用 填充字符、目标长度 减 当前长。
完。

转十六进制 接受 数值：
    hex 为 「0123456789ABCDEF」。
    若 数值 等于 0：
        返还 「0」。
    完。
    可变 结果 为 「」。
    可变 v 为 数值。
    只要 v 大于 0：
        位 为 v 位与 0x0F。
        结果 为 做 字符串拼接 用 做 子串 用 hex、位、1、结果。
        v 为 v 右移 4。
    完。
    返还 结果。
完。

可变 _网络库2 为 0。

可变 _网络库2_已启动 为 0。

可变 _函数_WSA启动 为 0。

可变 _函数_WSA清理 为 0。

可变 _函数_套接字 为 0。

可变 _函数_连接 为 0。

可变 _函数_发送 为 0。

可变 _函数_接收 为 0。

可变 _函数_关闭套接字 为 0。

可变 _函数_网络地址转换 为 0。

可变 _函数_按名获取主机 为 0。

_初始化网络 接受 零参：
    若 _网络库2 等于 0：
        _网络库2 为 做 加载库 用 「ws2_32.dll」。
    完。
    若 _网络库2_已启动 等于 0：
        若 _函数_WSA启动 等于 0：
            _函数_WSA启动 为 做 获取函数 用 _网络库2、「WSAStartup」。
        完。
        wsadata 为 做 缓冲区 用 512。
        做 调用指针 用 _函数_WSA启动、0x0202、wsadata、0、0、0。
        _网络库2_已启动 为 1。
    完。
    返还 _网络库2。
完。

创建套接字 接受 零参：
    做 _初始化网络。
    若 _函数_套接字 等于 0：
        _函数_套接字 为 做 获取函数 用 _网络库2、「socket」。
    完。
    返还 做 调用指针 用 _函数_套接字、地址族_互联网、套接字_流、协议_TCP、0、0。
完。

网络字节序 接受 端口：
    返还 (端口 位与 0xFF) 左移 8 位或 端口 右移 8 位与 0xFF。
完。

解析域名 接受 域名：
    做 _初始化网络。
    若 _函数_按名获取主机 等于 0：
        _函数_按名获取主机 为 做 获取函数 用 _网络库2、「gethostbyname」。
    完。
    hostent 为 做 调用指针 用 _函数_按名获取主机、域名、0、0、0、0。
    若 hostent 等于 0：
        返还 0。
    完。
    addr_list 为 做 内存读 用 hostent 加 24。
    若 addr_list 等于 0：
        返还 0。
    完。
    first_addr 为 做 内存读 用 addr_list。
    若 first_addr 等于 0：
        返还 0。
    完。
    返还 做 内存读32 用 first_addr。
完。

解析IP 接受 ip字符串：
    做 _初始化网络。
    若 _函数_网络地址转换 等于 0：
        _函数_网络地址转换 为 做 获取函数 用 _网络库2、「inet_addr」。
    完。
    返还 做 调用指针 用 _函数_网络地址转换、ip字符串、0、0、0、0。
完。

连接服务器 接受 套接字、IP地址、端口：
    做 _初始化网络。
    若 _函数_连接 等于 0：
        _函数_连接 为 做 获取函数 用 _网络库2、「connect」。
    完。
    地址 为 做 缓冲区 用 16。
    做 缓冲清零 用 地址、16。
    做 内存写16 用 地址、地址族_互联网、0。
    做 内存写16 用 地址 加 2、做 网络字节序 用 端口、0。
    做 内存写32 用 地址 加 4、IP地址、0。
    返还 做 调用指针 用 _函数_连接、套接字、地址、16、0、0。
完。

发送 接受 套接字、数据、长度：
    做 _初始化网络。
    若 _函数_发送 等于 0：
        _函数_发送 为 做 获取函数 用 _网络库2、「send」。
    完。
    返还 做 调用指针4 用 _函数_发送、套接字、数据、长度、0。
完。

发送文本 接受 套接字、文本：
    返还 做 发送 用 套接字、文本、做 字节长度 用 文本。
完。

接收 接受 套接字、缓冲、大小：
    做 _初始化网络。
    若 _函数_接收 等于 0：
        _函数_接收 为 做 获取函数 用 _网络库2、「recv」。
    完。
    返还 做 调用指针4 用 _函数_接收、套接字、缓冲、大小、0。
完。

关闭套接字 接受 套接字：
    做 _初始化网络。
    若 _函数_关闭套接字 等于 0：
        _函数_关闭套接字 为 做 获取函数 用 _网络库2、「closesocket」。
    完。
    做 调用指针 用 _函数_关闭套接字、套接字、0、0、0、0。
完。

清理网络 接受 零参：
    若 _网络库2_已启动 不等于 0：
        若 _函数_WSA清理 等于 0：
            _函数_WSA清理 为 做 获取函数 用 _网络库2、「WSACleanup」。
        完。
        做 调用指针 用 _函数_WSA清理、0、0、0、0、0。
        _网络库2_已启动 为 0。
    完。
完。

HTTP请求 接受 域名、路径：
    ip 为 做 解析域名 用 域名。
    若 ip 等于 0：
        返还 「」。
    完。
    sock 为 做 创建套接字。
    若 做 连接服务器 用 sock、ip、80 不等于 0：
        做 关闭套接字 用 sock。
        返还 「」。
    完。
    req 为 做 字符串拼接 用 「GET 」、做 字符串拼接 用 路径、做 字符串拼接 用 「 HTTP/1.0\r\nHost: 」、做 字符串拼接 用 域名、「\r\nConnection: close\r\n\r\n」。
    做 发送文本 用 sock、req。
    缓冲 为 做 缓冲区 用 65536。
    可变 总长 为 0。
    可变 还有数据 为 1。
    只要 还有数据 等于 1：
        n 为 做 接收 用 sock、缓冲 加 总长、65536 减 总长 减 1。
        若 n 小于或等于 0：
            还有数据 为 0。
        否则：
            总长 为 总长 加 n。
        完。
    完。
    做 缓冲写 用 缓冲、总长、0。
    做 关闭套接字 用 sock。
    返还 缓冲。
完。

可变 _函数_打开剪贴板 为 0。

可变 _函数_关闭剪贴板 为 0。

可变 _函数_获取剪贴板 为 0。

可变 _函数_设剪贴板 为 0。

可变 _函数_清空剪贴板 为 0。

可变 _函数_全局锁定 为 0。

可变 _函数_全局解锁 为 0。

可变 _函数_全局分配 为 0。

剪贴板_文本 为 1。

读剪贴板 接受 零参：
    做 _初始化用户。
    做 _初始化内核。
    若 _函数_打开剪贴板 等于 0：
        _函数_打开剪贴板 为 做 获取函数 用 _用户32库、「OpenClipboard」。
    完。
    若 _函数_关闭剪贴板 等于 0：
        _函数_关闭剪贴板 为 做 获取函数 用 _用户32库、「CloseClipboard」。
    完。
    若 _函数_获取剪贴板 等于 0：
        _函数_获取剪贴板 为 做 获取函数 用 _用户32库、「GetClipboardData」。
    完。
    若 _函数_全局锁定 等于 0：
        _函数_全局锁定 为 做 获取函数 用 _内核32库、「GlobalLock」。
    完。
    若 _函数_全局解锁 等于 0：
        _函数_全局解锁 为 做 获取函数 用 _内核32库、「GlobalUnlock」。
    完。
    做 调用指针 用 _函数_打开剪贴板、0、0、0、0、0。
    handle 为 做 调用指针 用 _函数_获取剪贴板、剪贴板_文本、0、0、0、0。
    若 handle 等于 0：
        做 调用指针 用 _函数_关闭剪贴板、0、0、0、0、0。
        返还 「」。
    完。
    指针 为 做 调用指针 用 _函数_全局锁定、handle、0、0、0、0。
    若 指针 等于 0：
        做 调用指针 用 _函数_关闭剪贴板、0、0、0、0、0。
        返还 「」。
    完。
    可变 长度 为 0。
    只要 做 内存读8 用 指针 加 长度 不等于 0：
        长度 为 长度 加 1。
    完。
    clip结果 为 做 缓冲区 用 长度 加 1。
    做 内存复制 用 clip结果、指针、长度。
    做 缓冲写 用 clip结果、长度、0。
    做 调用指针 用 _函数_全局解锁、handle、0、0、0、0。
    做 调用指针 用 _函数_关闭剪贴板、0、0、0、0、0。
    返还 clip结果。
完。

写剪贴板 接受 文本：
    做 _初始化用户。
    做 _初始化内核。
    若 _函数_打开剪贴板 等于 0：
        _函数_打开剪贴板 为 做 获取函数 用 _用户32库、「OpenClipboard」。
    完。
    若 _函数_关闭剪贴板 等于 0：
        _函数_关闭剪贴板 为 做 获取函数 用 _用户32库、「CloseClipboard」。
    完。
    若 _函数_设剪贴板 等于 0：
        _函数_设剪贴板 为 做 获取函数 用 _用户32库、「SetClipboardData」。
    完。
    若 _函数_清空剪贴板 等于 0：
        _函数_清空剪贴板 为 做 获取函数 用 _用户32库、「EmptyClipboard」。
    完。
    若 _函数_全局分配 等于 0：
        _函数_全局分配 为 做 获取函数 用 _内核32库、「GlobalAlloc」。
    完。
    若 _函数_全局锁定 等于 0：
        _函数_全局锁定 为 做 获取函数 用 _内核32库、「GlobalLock」。
    完。
    若 _函数_全局解锁 等于 0：
        _函数_全局解锁 为 做 获取函数 用 _内核32库、「GlobalUnlock」。
    完。
    长度 为 做 字节长度 用 文本。
    hmem 为 做 调用指针 用 _函数_全局分配、0x0002、长度 加 1、0、0、0。
    若 hmem 等于 0：
        返还 0。
    完。
    指针 为 做 调用指针 用 _函数_全局锁定、hmem、0、0、0、0。
    做 内存复制 用 指针、文本、长度。
    做 缓冲写 用 指针、长度、0。
    做 调用指针 用 _函数_全局解锁、hmem、0、0、0、0。
    做 调用指针 用 _函数_打开剪贴板、0、0、0、0、0。
    做 调用指针 用 _函数_清空剪贴板、0、0、0、0、0。
    做 调用指针 用 _函数_设剪贴板、剪贴板_文本、hmem、0、0、0。
    做 调用指针 用 _函数_关闭剪贴板、0、0、0、0、0。
    返还 1。
完。

截屏区域 接受 x、y、宽、高：
    像素数 为 宽 乘 高。
    数据 为 做 缓冲区 用 像素数 乘 4。
    dc 为 做 获取屏幕DC。
    可变 py 为 0。
    只要 py 小于 高：
        可变 px 为 0。
        只要 px 小于 宽：
            颜色 为 做 读像素 用 x 加 px、y 加 py。
            偏移 为 (py 乘 宽 加 px) 乘 4。
            做 内存写8 用 数据 加 偏移、做 红色分量 用 颜色。
            做 内存写8 用 数据 加 偏移 加 1、做 绿色分量 用 颜色。
            做 内存写8 用 数据 加 偏移 加 2、做 蓝色分量 用 颜色。
            做 内存写8 用 数据 加 偏移 加 3、255。
            px 为 px 加 1。
        完。
        py 为 py 加 1。
    完。
    做 释放DC 用 dc。
    返还 数据。
完。

保存BMP 接受 文件名、数据、宽、高：
    行字节 为 宽 乘 3。
    填充 为 4 减 (行字节 位与 3) 位与 3。
    行实际 为 行字节 加 填充。
    像素实际 为 行实际 乘 高。
    文件大小 为 54 加 像素实际。
    缓冲 为 做 缓冲区 用 文件大小。
    做 缓冲清零 用 缓冲、文件大小。
    做 缓冲写 用 缓冲、x42、0。
    做 缓冲写 用 缓冲、1、0x4D。
    做 内存写32 用 缓冲 加 2、文件大小、0。
    做 内存写32 用 缓冲 加 10、54、0。
    做 内存写32 用 缓冲 加 14、40、0。
    做 内存写32 用 缓冲 加 18、宽、0。
    做 内存写32 用 缓冲 加 22、高、0。
    做 内存写16 用 缓冲 加 26、1、0。
    做 内存写16 用 缓冲 加 28、24、0。
    做 内存写32 用 缓冲 加 34、像素实际、0。
    可变 y 为 0。
    只要 y 小于 高：
        可变 x 为 0。
        src行 为 (高 减 1 减 y) 乘 宽 乘 4。
        dst行 为 54 加 y 乘 行实际。
        只要 x 小于 宽：
            si 为 src行 加 x 乘 4。
            di 为 dst行 加 x 乘 3。
            做 缓冲写 用 缓冲、di、做 内存读8 用 数据 加 si 加 2。
            做 缓冲写 用 缓冲、di 加 1、做 内存读8 用 数据 加 si 加 1。
            做 缓冲写 用 缓冲、di 加 2、做 内存读8 用 数据 加 si。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    做 写文件 用 文件名、缓冲、文件大小。
    返还 1。
完。

截屏保存 接受 文件名、x、y、宽、高：
    数据 为 做 截屏区域 用 x、y、宽、高。
    返还 做 保存BMP 用 文件名、数据、宽、高。
完。

本章 收束。
