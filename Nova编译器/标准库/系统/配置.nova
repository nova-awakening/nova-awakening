《配置》

引用《..·基础》。

配置_创建 接受 零参：
    返还 一组：做 字典、做 字典、做 字典。
完。

配置_设置 接受 配置、键、值：
    做 字典设 用 配置 的 第 0 项、键、值。
完。

配置_获取 接受 配置、键：
    值 为 做 字典取 用 配置 的 第 0 项、键。
    若 值 等于 空：
        返还 做 字典取 用 配置 的 第 1 项、键。
    完。
    返还 值。
完。

配置_默认 接受 配置、键、默认值：
    做 字典设 用 配置 的 第 1 项、键、默认值。
完。

配置_存在 接受 配置、键：
    返还 做 字典取 用 配置 的 第 0 项、键 不等于 空。
完。

配置_删除 接受 配置、键：
    做 字典设 用 配置 的 第 0 项、键、空。
完。

分层配置_创建 接受 零参：
    返还 一组：一组：（空）。
完。

分层_添加层 接受 分层、层名：
    做 追加 用 分层 的 第 0 项、一组：层名、做 字典。
完。

分层_设置 接受 分层、层名、键、值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 分层 的 第 0 项：
        若 做 字符串等于 用 分层 的 第 0 项 的 第 i 项 的 第 0 项、层名：
            做 字典设 用 分层 的 第 0 项 的 第 i 项 的 第 1 项、键、值。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

分层_获取 接受 分层、键：
    可变 i 为 做 计数 用 分层 的 第 0 项 减 1。
    只要 i 大于或等于 0：
        值 为 做 字典取 用 分层 的 第 0 项 的 第 i 项 的 第 1 项、键。
        若 值 不等于 空：
            返还 值。
        完。
        i 为 i 减 1。
    完。
    返还 空。
完。

构建器_创建 接受 零参：
    返还 一组：做 字典、一组：（空）。
完。

构建器_设 接受 构建器、键、值：
    做 字典设 用 构建器 的 第 0 项、键、值。
    返还 构建器。
完。

构建器_必须 接受 构建器、键：
    做 追加 用 构建器 的 第 1 项、键。
    返还 构建器。
完。

构建器_构建 接受 构建器：
    可变 i 为 0。
    只要 i 小于 做 计数 用 构建器 的 第 1 项：
        若 做 字典取 用 构建器 的 第 0 项、构建器 的 第 1 项 的 第 i 项 等于 空：
            返还 一组：「错误」、做 字符串拼接 用 「缺少必须字段: 」、构建器 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：「成功」、构建器 的 第 0 项。
完。

解析配置文本 接受 文本：
    可变 配置 为 做 配置_创建。
    n 为 做 计数 用 文本。
    可变 i 为 0。
    可变 行开始 为 0。
    只要 i 小于或等于 n：
        可变 是换行 为 假。
        若 i 等于 n：
            是换行 为 真。
        否则若 做 字符串等于 用 做 子串 用 文本、i、1、「\n」：
            是换行 为 真。
        完。
        若 是换行 并且 i 大于 行开始：
            行 为 做 子串 用 文本、行开始、i 减 行开始。
            若 做 计数 用 行 大于 0 并且 做 字符串等于 用 做 子串 用 行、0、1、「#」 等于 假：
                可变 eq 为 0。
                可变 _eq找到 为 假。
                只要 eq 小于 做 计数 用 行 并且 _eq找到 等于 假：
                    若 做 子串 用 行、eq、1 等于 「=」：
                        _eq找到 为 真。
                    否则：
                        eq 为 eq 加 1。
                    完。
                完。
                若 eq 小于 做 计数 用 行：
                    键 为 做 子串 用 行、0、eq。
                    值 为 做 子串 用 行、eq 加 1、做 计数 用 行 减 eq 减 1。
                    做 配置_设置 用 配置、键、值。
                完。
            完。
            行开始 为 i 加 1。
        完。
        若 是换行：
            行开始 为 i 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 配置。
完。

特性标志_创建 接受 零参：
    返还 一组：做 字典。
完。

特性_启用 接受 标志、特性名：
    做 字典设 用 标志 的 第 0 项、特性名、1。
完。

特性_禁用 接受 标志、特性名：
    做 字典设 用 标志 的 第 0 项、特性名、0。
完。

特性_已启用 接受 标志、特性名：
    值 为 做 字典取 用 标志 的 第 0 项、特性名。
    返还 值 等于 1。
完。

特性_切换 接受 标志、特性名：
    若 做 特性_已启用 用 标志、特性名：
        做 特性_禁用 用 标志、特性名。
    否则：
        做 特性_启用 用 标志、特性名。
    完。
完。

环境_创建 接受 零参：
    返还 一组：做 字典。
完。

环境_设 接受 环境、键、值：
    做 字典设 用 环境 的 第 0 项、键、值。
完。

环境_取 接受 环境、键：
    返还 做 字典取 用 环境 的 第 0 项、键。
完。

环境_取或默认 接受 环境、键、缺省：
    值 为 做 字典取 用 环境 的 第 0 项、键。
    若 值 等于 空：
        返还 缺省。
    完。
    返还 值。
完。

可变 _校验_键 为 一组：（空）。

可变 _校验_类型 为 一组：（空）。

配置_注册校验 接受 键、类型：
    做 追加 用 _校验_键、键。
    做 追加 用 _校验_类型、类型。
完。

配置_校验 接受 配置：
    可变 错误 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _校验_键：
        值 为 做 配置_获取 用 配置、_校验_键 的 第 i 项。
        若 值 等于 空：
            做 追加 用 错误、「缺失键: 」 加 _校验_键 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 错误。
完。

可变 _监听_键 为 一组：（空）。

可变 _监听_回调 为 一组：（空）。

配置_监听 接受 键、回调：
    做 追加 用 _监听_键、键。
    做 追加 用 _监听_回调、回调。
完。

配置_通知变更 接受 键、旧值、新值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _监听_键：
        若 _监听_键 的 第 i 项 等于 键：
            做 _监听_回调 的 第 i 项 用 键、旧值、新值。
        完。
        i 为 i 加 1。
    完。
完。

配置_配置_合并 接受 基础、覆盖：
    可变 结果 为 做 配置_创建。
    基础键 为 做 字典键 用 基础 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 基础键：
        做 配置_设置 用 结果、基础键 的 第 i 项、做 字典取 用 基础 的 第 0 项、基础键 的 第 i 项。
        i 为 i 加 1。
    完。
    覆盖键 为 做 字典键 用 覆盖 的 第 0 项。
    i 为 0。
    只要 i 小于 做 计数 用 覆盖键：
        做 配置_设置 用 结果、覆盖键 的 第 i 项、做 字典取 用 覆盖 的 第 0 项、覆盖键 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

配置_导出文本 接受 配置：
    可变 文本 为 「」。
    键 为 做 字典键 用 配置 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键：
        文本 为 文本 加 键 的 第 i 项 加 「=」 加 做 转字符串 用 做 字典取 用 配置 的 第 0 项、键 的 第 i 项 加 「\n」。
        i 为 i 加 1。
    完。
    返还 文本。
完。

配置_条目数 接受 配置：
    返还 做 计数 用 做 字典键 用 配置 的 第 0 项。
完。

可变 _配置_读数 为 0。

可变 _配置_写数 为 0。

配置_记录读 接受 零参：
    _配置_读数 为 _配置_读数 加 1。
完。

配置_记录写 接受 零参：
    _配置_写数 为 _配置_写数 加 1。
完。

本章 收束。
