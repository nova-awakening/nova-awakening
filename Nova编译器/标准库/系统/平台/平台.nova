《平台》

引用《..·..·基础》。

系统平台_平台_WINDOWS 为 1。

平台_WASM 为 4。

平台_裸机 为 5。

架构_RISCV 为 3。

平台_创建 接受 操作系统、架构：
    返还 一组：操作系统、架构、0、8、1、0、4096、「」、「」、0。
完。

平台_操作系统 接受 平台：
    返还 平台 的 第 0 项。
完。

平台_架构 接受 平台：
    返还 平台 的 第 1 项。
完。

平台_是小端 接受 平台：
    返还 平台 的 第 2 项 等于 0。
完。

平台_指针大小 接受 平台：
    返还 平台 的 第 3 项。
完。

平台_CPU核数 接受 平台：
    返还 平台 的 第 4 项。
完。

平台_总内存 接受 平台：
    返还 平台 的 第 5 项。
完。

平台_可用内存 接受 平台：
    返还 平台 的 第 6 项。
完。

平台_页大小 接受 平台：
    返还 平台 的 第 7 项。
完。

平台_操作系统名 接受 平台：
    若 平台 的 第 0 项 等于 系统平台_平台_WINDOWS：
        返还 「Windows」。
    完。
    若 平台 的 第 0 项 等于 平台_LINUX：
        返还 「Linux」。
    完。
    若 平台 的 第 0 项 等于 平台_MACOS：
        返还 「macOS」。
    完。
    若 平台 的 第 0 项 等于 平台_WASM：
        返还 「WebAssembly」。
    完。
    若 平台 的 第 0 项 等于 平台_裸机：
        返还 「BareMetal」。
    完。
    返还 「未知」。
完。

平台_架构名 接受 平台：
    若 平台 的 第 1 项 等于 架构_X86_64：
        返还 「x86_64」。
    完。
    若 平台 的 第 1 项 等于 架构_ARM64：
        返还 「aarch64」。
    完。
    若 平台 的 第 1 项 等于 架构_RISCV：
        返还 「riscv64」。
    完。
    若 平台 的 第 1 项 等于 架构_WASM32：
        返还 「wasm32」。
    完。
    返还 「未知」。
完。

平台_是Windows 接受 平台：
    返还 平台 的 第 0 项 等于 系统平台_平台_WINDOWS。
完。

平台_是Linux 接受 平台：
    返还 平台 的 第 0 项 等于 平台_LINUX。
完。

平台_是64位 接受 平台：
    返还 平台 的 第 3 项 等于 8。
完。

CPU信息_创建 接受 零参：
    返还 一组：一组：（空）、64、0、0、「」。
完。

CPU_有特性 接受 cpu、特性名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 cpu 的 第 0 项：
        若 做 字符串等于 用 cpu 的 第 0 项 的 第 i 项、特性名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

CPU_添加特性 接受 cpu、特性名：
    若 做 CPU_有特性 用 cpu、特性名 等于 假：
        做 追加 用 cpu 的 第 0 项、特性名。
    完。
完。

CPU_有SSE 接受 cpu：
    返还 做 CPU_有特性 用 cpu、「SSE」。
完。

CPU_有AVX 接受 cpu：
    返还 做 CPU_有特性 用 cpu、「AVX」。
完。

CPU_有AVX2 接受 cpu：
    返还 做 CPU_有特性 用 cpu、「AVX2」。
完。

CPU_有AVX512 接受 cpu：
    返还 做 CPU_有特性 用 cpu、「AVX512」。
完。

CPU_有NEON 接受 cpu：
    返还 做 CPU_有特性 用 cpu、「NEON」。
完。

资源监控_创建 接受 最大采样数：
    可变 实际采样 为 最大采样数。
    若 实际采样 小于或等于 0：
        实际采样 为 1000。
    完。
    返还 一组：一组：（空）、实际采样、0。
完。

资源_采样 接受 监控、时间戳、CPU使用、内存使用、线程数、IO等待：
    采样 为 一组：时间戳、CPU使用、内存使用、线程数、IO等待。
    若 做 计数 用 监控 的 第 0 项 小于 监控 的 第 1 项：
        做 追加 用 监控 的 第 0 项、采样。
    否则：
        监控 的 第 0 项 的 第 监控 的 第 2 项 项 为 采样。
    完。
    监控 的 第 2 项 为 (监控 的 第 2 项 加 1) 取余于 监控 的 第 1 项。
完。

资源_平均CPU 接受 监控、最近N个：
    n 为 做 计数 用 监控 的 第 0 项。
    若 n 等于 0：
        返还 0。
    完。
    可变 实际N 为 最近N个。
    若 实际N 大于 n：
        实际N 为 n。
    完。
    可变 总 为 0。
    可变 i 为 0。
    可变 索引 为 (监控 的 第 2 项 减 1 加 n) 取余于 n。
    只要 i 小于 实际N：
        总 为 总 加 监控 的 第 0 项 的 第 索引 项 的 第 1 项。
        索引 为 (索引 减 1 加 n) 取余于 n。
        i 为 i 加 1。
    完。
    返还 总 除以 实际N。
完。

资源_平均内存 接受 监控、最近N个：
    n 为 做 计数 用 监控 的 第 0 项。
    若 n 等于 0：
        返还 0。
    完。
    可变 实际N 为 最近N个。
    若 实际N 大于 n：
        实际N 为 n。
    完。
    可变 总 为 0。
    可变 i 为 0。
    可变 索引 为 (监控 的 第 2 项 减 1 加 n) 取余于 n。
    只要 i 小于 实际N：
        总 为 总 加 监控 的 第 0 项 的 第 索引 项 的 第 2 项。
        索引 为 (索引 减 1 加 n) 取余于 n。
        i 为 i 加 1。
    完。
    返还 总 除以 实际N。
完。

资源_峰值CPU 接受 监控：
    n 为 做 计数 用 监控 的 第 0 项。
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 监控 的 第 0 项 的 第 i 项 的 第 1 项 大于 最大：
            最大 为 监控 的 第 0 项 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

资源_采样数 接受 监控：
    返还 做 计数 用 监控 的 第 0 项。
完。

系统调用表_创建 接受 零参：
    返还 一组：一组：（空）、做 字典。
完。

系统调用_注册 接受 表、名称、处理函数、兼容平台列表：
    做 追加 用 表 的 第 0 项、一组：名称、处理函数、兼容平台列表。
    做 字典设 用 表 的 第 1 项、名称、做 计数 用 表 的 第 0 项 减 1。
完。

系统调用_执行 接受 表、名称、参数、当前平台：
    索引 为 做 字典取 用 表 的 第 1 项、名称。
    若 索引 等于 空：
        返还 一组：「错误」、「未知系统调用」。
    完。
    调用 为 表 的 第 0 项 的 第 索引 项。
    可变 兼容 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调用 的 第 2 项：
        若 调用 的 第 2 项 的 第 i 项 等于 当前平台 或者 调用 的 第 2 项 的 第 i 项 等于 0：
            兼容 为 真。
            止。
        完。
        i 为 i 加 1。
    完。
    若 兼容 等于 假：
        返还 一组：「错误」、「平台不兼容」。
    完。
    结果 为 做 调用 的 第 1 项 用 参数。
    返还 一组：「成功」、结果。
完。

对齐_向上 接受 值、对齐：
    返还 (值 加 对齐 减 1) 除以 对齐 乘 对齐。
完。

对齐_向下 接受 值、对齐：
    返还 值 除以 对齐 乘 对齐。
完。

是2的幂 接受 n：
    若 n 小于或等于 0：
        返还 假。
    完。
    可变 v 为 n。
    只要 v 大于 1：
        若 v 取余于 2 不等于 0：
            返还 假。
        完。
        v 为 v 除以 2。
    完。
    返还 真。
完。

大端_到_小端16 接受 值：
    返还 值 取余于 256 乘 256 加 值 除以 256。
完。

大端_到_小端32 接受 值：
    b0 为 值 取余于 256。
    b1 为 值 除以 256 取余于 256。
    b2 为 值 除以 65536 取余于 256。
    b3 为 值 除以 16777216 取余于 256。
    返还 b0 乘 16777216 加 b1 乘 65536 加 b2 乘 256 加 b3。
完。

小端_到_大端16 接受 值：
    返还 做 大端_到_小端16 用 值。
完。

小端_到_大端32 接受 值：
    返还 做 大端_到_小端32 用 值。
完。

SYS_read 为 0。

SYS_write 为 1。

SYS_open 为 2。

SYS_close 为 3。

SYS_stat 为 4。

SYS_fstat 为 5。

SYS_lseek 为 8。

SYS_mmap 为 9。

SYS_mprotect 为 10。

SYS_munmap 为 11。

SYS_brk 为 12。

SYS_ioctl 为 16。

SYS_access 为 21。

SYS_pipe 为 22。

SYS_dup2 为 33。

SYS_getpid 为 39。

SYS_fork 为 57。

SYS_execve 为 59。

SYS_exit 为 60。

SYS_wait4 为 61。

SYS_kill 为 62。

SYS_fcntl 为 72。

SYS_mkdir 为 83。

SYS_rmdir 为 84。

SYS_unlink 为 87。

SYS_getdents64 为 217。

SYS_clock_gettime 为 228。

SYS_epoll_create1 为 291。

SYS_epoll_ctl 为 233。

SYS_epoll_wait 为 232。

SYS_socket 为 41。

SYS_connect 为 42。

SYS_accept 为 43。

SYS_bind 为 49。

SYS_listen 为 50。

SYS_sendto 为 44。

SYS_recvfrom 为 45。

SYS_getrandom 为 318。

O_RDONLY 为 0。

O_WRONLY 为 1。

O_RDWR 为 2。

O_CREAT 为 64。

O_TRUNC 为 512。

O_APPEND 为 1024。

O_NONBLOCK 为 2048。

PROT_READ 为 1。

PROT_WRITE 为 2。

PROT_EXEC 为 4。

MAP_PRIVATE 为 2。

MAP_ANON 为 32。

linux_syscall0 接受 号：
    返还 0 减 38。
完。

linux_syscall1 接受 号、a1：
    返还 0 减 38。
完。

linux_syscall2 接受 号、a1、a2：
    返还 0 减 38。
完。

linux_syscall3 接受 号、a1、a2、a3：
    返还 0 减 38。
完。

linux_syscall4 接受 号、a1、a2、a3、a4：
    返还 0 减 38。
完。

linux_syscall6 接受 号、a1、a2、a3、a4、a5、a6：
    返还 0 减 38。
完。

原生_文件打开 接受 路径、标志、权限：
    返还 做 linux_syscall3 用 SYS_open、路径、标志、权限。
完。

原生_文件关闭 接受 fd：
    返还 做 linux_syscall1 用 SYS_close、fd。
完。

原生_文件读 接受 fd、缓冲区、大小：
    返还 做 linux_syscall3 用 SYS_read、fd、缓冲区、大小。
完。

原生_文件写 接受 fd、缓冲区、大小：
    返还 做 linux_syscall3 用 SYS_write、fd、缓冲区、大小。
完。

原生_文件定位 接受 fd、偏移、基准：
    返还 做 linux_syscall3 用 SYS_lseek、fd、偏移、基准。
完。

原生_文件状态 接受 路径：
    返还 做 linux_syscall2 用 SYS_stat、路径、0。
完。

原生_创建目录 接受 路径、权限：
    返还 做 linux_syscall2 用 SYS_mkdir、路径、权限。
完。

原生_删除文件 接受 路径：
    返还 做 linux_syscall1 用 SYS_unlink、路径。
完。

原生_删除目录 接受 路径：
    返还 做 linux_syscall1 用 SYS_rmdir、路径。
完。

原生_内存映射 接受 地址、大小、保护、标志、fd、偏移：
    返还 做 linux_syscall6 用 SYS_mmap、地址、大小、保护、标志、fd、偏移。
完。

原生_内存解映射 接受 地址、大小：
    返还 做 linux_syscall2 用 SYS_munmap、地址、大小。
完。

原生_匿名映射 接受 大小：
    返还 做 原生_内存映射 用 0、大小、PROT_READ 位或 PROT_WRITE、MAP_PRIVATE 位或 MAP_ANON、负 1、0。
完。

原生_获取进程ID 接受 零参：
    返还 做 linux_syscall0 用 SYS_getpid。
完。

原生_退出 接受 状态码：
    返还 做 linux_syscall1 用 SYS_exit、状态码。
完。

原生_创建进程 接受 零参：
    返还 做 linux_syscall0 用 SYS_fork。
完。

原生_等待子进程 接受 pid：
    返还 做 linux_syscall4 用 SYS_wait4、pid、0、0、0。
完。

AF_INET 为 2。

AF_INET6 为 10。

SOCK_STREAM 为 1。

SOCK_DGRAM 为 2。

原生_创建套接字 接受 域、类型、协议：
    返还 做 linux_syscall3 用 SYS_socket、域、类型、协议。
完。

原生_绑定 接受 fd、地址、长度：
    返还 做 linux_syscall3 用 SYS_bind、fd、地址、长度。
完。

原生_监听 接受 fd、队列长度：
    返还 做 linux_syscall2 用 SYS_listen、fd、队列长度。
完。

原生_接受 接受 fd：
    返还 做 linux_syscall3 用 SYS_accept、fd、0、0。
完。

原生_连接 接受 fd、地址、长度：
    返还 做 linux_syscall3 用 SYS_connect、fd、地址、长度。
完。

原生_获取时间 接受 零参：
    返还 做 linux_syscall2 用 SYS_clock_gettime、0、0。
完。

原生_获取随机 接受 缓冲区、大小：
    返还 做 linux_syscall3 用 SYS_getrandom、缓冲区、大小、0。
完。

原生_epoll创建 接受 零参：
    返还 做 linux_syscall1 用 SYS_epoll_create1、0。
完。

原生_epoll控制 接受 epfd、操作、fd、事件：
    返还 做 linux_syscall4 用 SYS_epoll_ctl、epfd、操作、fd、事件。
完。

原生_epoll等待 接受 epfd、事件数组、最大事件、超时毫秒：
    返还 做 linux_syscall4 用 SYS_epoll_wait、epfd、事件数组、最大事件、超时毫秒。
完。

本章 收束。
