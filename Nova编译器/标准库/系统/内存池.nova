《内存池》

引用《..·基础》。

对象池_创建 接受 容量、创建函数：
    可变 空闲 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 空闲、做 创建函数 用 0、0、0、0。
        i 为 i 加 1。
    完。
    返还 一组：容量、空闲、0、创建函数。
完。

对象池_获取 接受 池：
    若 做 计数 用 池 的 第 1 项 等于 0：
        返还 做 池 的 第 3 项。
    完。
    n 为 做 计数 用 池 的 第 1 项。
    对象 为 池 的 第 1 项 的 第 n 减 1 项。
    可变 新空闲 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        做 追加 用 新空闲、池 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    池 的 第 1 项 为 新空闲。
    池 的 第 2 项 为 池 的 第 2 项 加 1。
    返还 对象。
完。

对象池_归还 接受 池、对象：
    做 追加 用 池 的 第 1 项、对象。
    池 的 第 2 项 为 池 的 第 2 项 减 1。
完。

对象池_可用 接受 池：
    返还 做 计数 用 池 的 第 1 项。
完。

对象池_已用 接受 池：
    返还 池 的 第 2 项。
完。

缓冲池_创建 接受 缓冲大小、数量：
    可变 缓冲列表 为 一组：（空）。
    可变 使用标记 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 数量：
        做 追加 用 缓冲列表、做 缓冲区 用 缓冲大小。
        做 追加 用 使用标记、假。
        i 为 i 加 1。
    完。
    返还 一组：缓冲大小、数量、缓冲列表、使用标记。
完。

缓冲池_分配 接受 池：
    可变 i 为 0。
    只要 i 小于 池 的 第 1 项：
        若 池 的 第 3 项 的 第 i 项 等于 假：
            池 的 第 3 项 的 第 i 项 为 真。
            返还 一组：i、池 的 第 2 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

缓冲池_释放 接受 池、索引：
    池 的 第 3 项 的 第 索引 项 为 假。
完。

缓冲池_可用 接受 池：
    可变 n 为 0。
    可变 i 为 0。
    只要 i 小于 池 的 第 1 项：
        若 池 的 第 3 项 的 第 i 项 等于 假：
            n 为 n 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 n。
完。

lru_创建 接受 容量：
    返还 一组：容量、做 字典、一组：（空）、0。
完。

lru_获取 接受 缓存、键：
    值 为 做 字典取 用 缓存 的 第 1 项、键。
    若 值 不等于 空：
        做 lru_移到末尾 用 缓存、键。
    完。
    返还 值。
完。

lru_设置 接受 缓存、键、值：
    已存在 为 做 字典取 用 缓存 的 第 1 项、键。
    做 字典设 用 缓存 的 第 1 项、键、值。
    若 已存在 不等于 空：
        做 lru_移到末尾 用 缓存、键。
    否则：
        做 追加 用 缓存 的 第 2 项、键。
        缓存 的 第 3 项 为 缓存 的 第 3 项 加 1。
        若 缓存 的 第 3 项 大于 缓存 的 第 0 项：
            最旧键 为 缓存 的 第 2 项 的 第 0 项。
            做 字典设 用 缓存 的 第 1 项、最旧键、空。
            可变 新列 为 一组：（空）。
            可变 i 为 1。
            只要 i 小于 做 计数 用 缓存 的 第 2 项：
                做 追加 用 新列、缓存 的 第 2 项 的 第 i 项。
                i 为 i 加 1。
            完。
            缓存 的 第 2 项 为 新列。
            缓存 的 第 3 项 为 缓存 的 第 3 项 减 1。
        完。
    完。
完。

lru_移到末尾 接受 缓存、键：
    可变 新列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 缓存 的 第 2 项：
        若 做 字符串等于 用 缓存 的 第 2 项 的 第 i 项、键 等于 假：
            做 追加 用 新列、缓存 的 第 2 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 新列、键。
    缓存 的 第 2 项 为 新列。
完。

lru_大小 接受 缓存：
    返还 缓存 的 第 3 项。
完。

lru_容量 接受 缓存：
    返还 缓存 的 第 0 项。
完。

计数器_创建 接受 初始值：
    返还 一组：初始值。
完。

计数器_增 接受 c：
    c 的 第 0 项 为 c 的 第 0 项 加 1。
    返还 c 的 第 0 项。
完。

计数器_减 接受 c：
    c 的 第 0 项 为 c 的 第 0 项 减 1。
    返还 c 的 第 0 项。
完。

计数器_加 接受 c、n：
    c 的 第 0 项 为 c 的 第 0 项 加 n。
    返还 c 的 第 0 项。
完。

计数器_值 接受 c：
    返还 c 的 第 0 项。
完。

计数器_重置 接受 c：
    c 的 第 0 项 为 0。
完。

池_位图_创建 接受 大小：
    字数 为 (大小 加 63) 除以 64。
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 字数：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 一组：大小、数据。
完。

位图_设 接受 bm、位置：
    字 为 位置 除以 64。
    位 为 位置 取余于 64。
    bm 的 第 1 项 的 第 字 项 为 bm 的 第 1 项 的 第 字 项 位或 1 左移 位。
完。

位图_清 接受 bm、位置：
    字 为 位置 除以 64。
    位 为 位置 取余于 64。
    bm 的 第 1 项 的 第 字 项 为 bm 的 第 1 项 的 第 字 项 位与 按位取反 (1 左移 位)。
完。

池_位图_测试 接受 bm、位置：
    字 为 位置 除以 64。
    位 为 位置 取余于 64。
    返还 bm 的 第 1 项 的 第 字 项 右移 位 位与 1。
完。

位图_翻转 接受 bm、位置：
    字 为 位置 除以 64。
    位 为 位置 取余于 64。
    bm 的 第 1 项 的 第 字 项 为 bm 的 第 1 项 的 第 字 项 位异或 1 左移 位。
完。

池_位图_计数 接受 bm：
    可变 总数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 bm 的 第 1 项：
        可变 v 为 bm 的 第 1 项 的 第 i 项。
        只要 v 大于 0：
            总数 为 总数 加 (v 位与 1)。
            v 为 v 右移 1。
        完。
        i 为 i 加 1。
    完。
    返还 总数。
完。

池_等级数 为 5。

池_8字节 为 0。

池_32字节 为 1。

池_128字节 为 2。

池_512字节 为 3。

池_2K字节 为 4。

池_预分配_小 为 64。

池_预分配_中 为 32。

池_预分配_大 为 16。

标准库_内存池_分配 接受 池、大小：
    等级 为 做 内存池_选等级 用 大小。
    可变 新列表 为 一组：（空）。
    若 等级 小于 0：
        池 的 第 2 项 为 池 的 第 2 项 加 1。
        返还 一组：大小、0。
    完。
    空闲 为 池 的 第 0 项 的 第 等级 项。
    若 做 计数 用 空闲 等于 0：
        可变 扩展量 为 池_预分配_中。
        若 等级 小于或等于 1：
            扩展量 为 池_预分配_小。
        完。
        若 等级 大于或等于 3：
            扩展量 为 池_预分配_大。
        完。
        做 内存池_扩展等级 用 池 的 第 0 项、等级、扩展量。
        池 的 第 4 项 为 池 的 第 4 项 加 1。
    完。
    若 做 计数 用 空闲 等于 0：
        返还 空。
    完。
    块 为 空闲 的 第 做 计数 用 空闲 减 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 空闲 减 1：
        做 追加 用 新列表、空闲 的 第 i 项。
        i 为 i 加 1。
    完。
    池 的 第 0 项 的 第 等级 项 为 新列表。
    池 的 第 1 项 的 第 等级 项 为 池 的 第 1 项 的 第 等级 项 加 1。
    池 的 第 2 项 为 池 的 第 2 项 加 1。
    块 的 第 1 项 为 大小。
    返还 块。
完。

标准库_内存池_创建 接受 零参：
    可变 空闲 为 一组：（空）。
    可变 计数表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 池_等级数：
        做 追加 用 空闲、一组：（空）。
        做 追加 用 计数表、0。
        i 为 i 加 1。
    完。
    做 内存池_扩展等级 用 空闲、池_8字节、池_预分配_小。
    做 内存池_扩展等级 用 空闲、池_32字节、池_预分配_小。
    做 内存池_扩展等级 用 空闲、池_128字节、池_预分配_中。
    做 内存池_扩展等级 用 空闲、池_512字节、池_预分配_大。
    做 内存池_扩展等级 用 空闲、池_2K字节、池_预分配_大。
    返还 一组：空闲、计数表、0、0。
完。

内存池_回收 接受 池、块、大小：
    等级 为 做 内存池_选等级 用 大小。
    若 等级 小于 0：
        池 的 第 3 项 为 池 的 第 3 项 加 1。
        返还。
    完。
    块 的 第 1 项 为 0。
    做 追加 用 池 的 第 0 项 的 第 等级 项、块。
    池 的 第 1 项 的 第 等级 项 为 池 的 第 1 项 的 第 等级 项 减 1。
    池 的 第 3 项 为 池 的 第 3 项 加 1。
完。

内存池_扩展等级 接受 空闲列表、等级、数量：
    大小表 为 一组：8、32、128、512、2048。
    块大小 为 大小表 的 第 等级 项。
    可变 i 为 0。
    只要 i 小于 数量：
        做 追加 用 空闲列表 的 第 等级 项、一组：块大小、0。
        i 为 i 加 1。
    完。
完。

内存池_选等级 接受 大小：
    若 大小 小于或等于 8：
        返还 池_8字节。
    完。
    若 大小 小于或等于 32：
        返还 池_32字节。
    完。
    若 大小 小于或等于 128：
        返还 池_128字节。
    完。
    若 大小 小于或等于 512：
        返还 池_512字节。
    完。
    若 大小 小于或等于 2048：
        返还 池_2K字节。
    完。
    返还 负 1。
完。

本章 收束。
