《测试框架》

引用《..·基础》。

测试套件_创建 接受 名称：
    返还 一组：名称、一组：（空）、0、0。
完。

测试_追加 接受 套件、名称、测试函数：
    做 追加 用 套件 的 第 1 项、一组：名称、测试函数、「待运行」。
完。

测试_运行 接受 套件：
    做 打印文本 用 「═══ 测试套件: 」。
    做 打印文本 用 套件 的 第 0 项。
    做 打印文本 用 「 ═══」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 套件 的 第 1 项：
        测试 为 套件 的 第 1 项 的 第 i 项。
        做 测试 的 第 1 项。
        套件 的 第 1 项 的 第 i 项 的 第 2 项 为 「通过」。
        套件 的 第 2 项 为 套件 的 第 2 项 加 1。
        做 打印文本 用 「  ✓ 」。
        做 打印文本 用 测试 的 第 0 项。
        i 为 i 加 1。
    完。
    做 打印文本 用 「───────────────」。
    做 打印文本 用 「  通过: 」。
    做 打印 用 套件 的 第 2 项。
    做 打印文本 用 「  失败: 」。
    做 打印 用 套件 的 第 3 项。
    做 打印文本 用 「  总计: 」。
    做 打印 用 做 计数 用 套件 的 第 1 项。
    返还 套件 的 第 3 项 等于 0。
完。

测试框架_断言 接受 条件：
    若 条件 等于 假：
        做 抛 用 1。
    完。
完。

测试框架_断言_等于 接受 实际、期望：
    若 实际 不等于 期望：
        做 打印文本 用 「  期望: 」。
        做 打印 用 期望。
        做 打印文本 用 「  实际: 」。
        做 打印 用 实际。
        做 抛 用 2。
    完。
完。

测试框架_断言_不等 接受 实际、不期望：
    若 实际 等于 不期望：
        做 抛 用 3。
    完。
完。

断言_大于 接受 实际、下限：
    若 实际 小于或等于 下限：
        做 抛 用 4。
    完。
完。

断言_小于 接受 实际、上限：
    若 实际 大于或等于 上限：
        做 抛 用 5。
    完。
完。

断言_范围 接受 值、最小、最大：
    若 值 小于 最小 或者 值 大于 最大：
        做 抛 用 6。
    完。
完。

断言_近似 接受 实际、期望、容差：
    可变 差 为 实际 减 期望。
    若 差 小于 0：
        差 为 0 减 差。
    完。
    若 差 大于 容差：
        做 打印文本 用 「  期望: 」。
        做 打印 用 期望。
        做 打印文本 用 「  实际: 」。
        做 打印 用 实际。
        做 打印文本 用 「  容差: 」。
        做 打印 用 容差。
        做 抛 用 7。
    完。
完。

断言_非空 接受 值：
    若 值 等于 空：
        做 抛 用 8。
    完。
完。

断言_为空 接受 值：
    若 值 不等于 空：
        做 抛 用 9。
    完。
完。

断言_列表等于 接受 实际、期望：
    若 做 计数 用 实际 不等于 做 计数 用 期望：
        做 打印文本 用 「  长度不同: 」。
        做 打印 用 做 计数 用 实际。
        做 打印文本 用 「 vs 」。
        做 打印 用 做 计数 用 期望。
        做 抛 用 10。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 实际：
        若 实际 的 第 i 项 不等于 期望 的 第 i 项：
            做 打印文本 用 「  索引 」。
            做 打印 用 i。
            做 打印文本 用 「 不同: 」。
            做 打印 用 实际 的 第 i 项。
            做 打印文本 用 「 vs 」。
            做 打印 用 期望 的 第 i 项。
            做 抛 用 11。
        完。
        i 为 i 加 1。
    完。
完。

基准 接受 名称、函数体、迭代次数：
    做 打印文本 用 「基准: 」。
    做 打印文本 用 名称。
    可变 i 为 0。
    只要 i 小于 迭代次数：
        做 函数体。
        i 为 i 加 1。
    完。
    做 打印文本 用 「  完成 」。
    做 打印 用 迭代次数。
    做 打印文本 用 「 次迭代」。
完。

模拟_创建 接受 零参：
    返还 一组：一组：（空）、0。
完。

模拟_记录 接受 模拟、调用名、参数：
    做 追加 用 模拟 的 第 0 项、一组：调用名、参数。
    模拟 的 第 1 项 为 模拟 的 第 1 项 加 1。
完。

模拟_调用次数 接受 模拟：
    返还 模拟 的 第 1 项。
完。

模拟_被调用 接受 模拟、调用名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 模拟 的 第 0 项：
        若 做 字符串等于 用 模拟 的 第 0 项 的 第 i 项 的 第 0 项、调用名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

模拟_调用参数 接受 模拟、索引：
    若 索引 小于 做 计数 用 模拟 的 第 0 项：
        返还 模拟 的 第 0 项 的 第 索引 项 的 第 1 项。
    完。
    返还 空。
完。

模拟_重置 接受 模拟：
    模拟 的 第 0 项 为 一组：（空）。
    模拟 的 第 1 项 为 0。
完。

参数化测试_运行 接受 测试函数、参数列表：
    可变 通过 为 0。
    可变 失败 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数列表：
        结果 为 做 测试函数 用 参数列表 的 第 i 项。
        若 结果：
            通过 为 通过 加 1。
        否则：
            失败 为 失败 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：通过、失败。
完。

基准_运行 接受 测试函数、迭代数：
    可变 i 为 0。
    只要 i 小于 迭代数：
        做 测试函数。
        i 为 i 加 1。
    完。
    返还 迭代数。
完。

可变 _套件_名称 为 一组：（空）。

可变 _套件_测试 为 一组：（空）。

可变 _套件_结果 为 一组：（空）。

测试框架_套件_创建 接受 名称：
    做 追加 用 _套件_名称、名称。
    做 追加 用 _套件_测试、一组：（空）。
    做 追加 用 _套件_结果、一组：0、0。
    返还 做 计数 用 _套件_名称 减 1。
完。

套件_添加测试 接受 套件id、测试名、测试函数：
    做 追加 用 _套件_测试 的 第 套件id 项、一组：测试名、测试函数。
完。

套件_运行 接受 套件id：
    测试列表 为 _套件_测试 的 第 套件id 项。
    可变 通过 为 0。
    可变 失败 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 测试列表：
        结果 为 做 测试列表 的 第 i 项 的 第 1 项。
        若 结果：
            通过 为 通过 加 1。
        否则：
            失败 为 失败 加 1。
        完。
        i 为 i 加 1。
    完。
    _套件_结果 的 第 套件id 项 为 一组：通过、失败。
    返还 一组：通过、失败。
完。

套件_全部报告 接受 零参：
    可变 总通过 为 0。
    可变 总失败 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _套件_名称：
        总通过 为 总通过 加 _套件_结果 的 第 i 项 的 第 0 项。
        总失败 为 总失败 加 _套件_结果 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 一组：做 计数 用 _套件_名称、总通过、总失败。
完。

套件_清空 接受 零参：
    _套件_名称 为 一组：（空）。
    _套件_测试 为 一组：（空）。
    _套件_结果 为 一组：（空）。
完。

本章 收束。
