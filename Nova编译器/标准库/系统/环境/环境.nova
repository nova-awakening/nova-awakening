《环境》

引用《..·..·基础》。

引用《..·配置》。

环境_环境_创建 接受 零参：
    返还 一组：做 字典、一组：（空）、做 字典、做 字典、「」。
完。

环境_环境_设 接受 环境、键、值：
    做 字典设 用 环境 的 第 0 项、键、值。
完。

环境_环境_取 接受 环境、键：
    可变 i 为 做 计数 用 环境 的 第 1 项 减 1。
    只要 i 大于或等于 0：
        层值 为 做 字典取 用 环境 的 第 1 项 的 第 i 项 的 第 1 项、键。
        若 层值 不等于 空：
            返还 层值。
        完。
        i 为 i 减 1。
    完。
    值 为 做 字典取 用 环境 的 第 0 项、键。
    若 值 不等于 空：
        返还 值。
    完。
    返还 做 字典取 用 环境 的 第 2 项、键。
完。

环境_取整数 接受 环境、键、默认值：
    值 为 做 环境_环境_取 用 环境、键。
    若 值 等于 空：
        返还 默认值。
    完。
    可变 结果 为 0。
    可变 负 为 假。
    n 为 做 计数 用 值。
    可变 i 为 0。
    若 n 大于 0 并且 做 字符串等于 用 做 子串 用 值、0、1、「-」：
        负 为 真。
        i 为 1。
    完。
    只要 i 小于 n：
        c 为 做 编码 用 做 子串 用 值、i、1。
        若 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
            结果 为 结果 乘 10 加 (c 减 48)。
        否则：
            返还 默认值。
        完。
        i 为 i 加 1。
    完。
    若 负：
        结果 为 0 减 结果。
    完。
    返还 结果。
完。

环境_取布尔 接受 环境、键、默认值：
    值 为 做 环境_环境_取 用 环境、键。
    若 值 等于 空：
        返还 默认值。
    完。
    若 做 字符串等于 用 值、「真」 或者 做 字符串等于 用 值、「1」 或者 做 字符串等于 用 值、「是」 或者 做 字符串等于 用 值、「开」：
        返还 真。
    完。
    若 做 字符串等于 用 值、「假」 或者 做 字符串等于 用 值、「0」 或者 做 字符串等于 用 值、「否」 或者 做 字符串等于 用 值、「关」：
        返还 假。
    完。
    返还 默认值。
完。

环境_存在 接受 环境、键：
    返还 做 环境_环境_取 用 环境、键 不等于 空。
完。

环境_删除 接受 环境、键：
    做 字典设 用 环境 的 第 0 项、键、空。
完。

环境_添加层 接受 环境、名称、优先级：
    层 为 一组：名称、做 字典、优先级。
    做 追加 用 环境 的 第 1 项、层。
    n 为 做 计数 用 环境 的 第 1 项。
    可变 i 为 n 减 1。
    只要 i 大于 0：
        若 环境 的 第 1 项 的 第 i 项 的 第 2 项 小于 环境 的 第 1 项 的 第 i 减 1 项 的 第 2 项：
            暂存 为 环境 的 第 1 项 的 第 i 项。
            环境 的 第 1 项 的 第 i 项 为 环境 的 第 1 项 的 第 i 减 1 项。
            环境 的 第 1 项 的 第 i 减 1 项 为 暂存。
        完。
        i 为 i 减 1。
    完。
    返还 层。
完。

环境_层设 接受 环境、层名、键、值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 环境 的 第 1 项：
        若 做 字符串等于 用 环境 的 第 1 项 的 第 i 项 的 第 0 项、层名：
            做 字典设 用 环境 的 第 1 项 的 第 i 项 的 第 1 项、键、值。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

环境_设默认 接受 环境、键、默认值：
    做 字典设 用 环境 的 第 2 项、键、默认值。
完。

环境_设验证 接受 环境、键、验证函数：
    做 字典设 用 环境 的 第 3 项、键、验证函数。
完。

环境_验证 接受 环境、键：
    验证器 为 做 字典取 用 环境 的 第 3 项、键。
    若 验证器 等于 空：
        返还 真。
    完。
    值 为 做 环境_环境_取 用 环境、键。
    返还 做 验证器 用 值。
完。

环境_验证全部 接受 _环境：
    可变 错误列表 为 一组：（空）。
    返还 错误列表。
完。

环境_解析dotenv 接受 文本：
    可变 变量列表 为 一组：（空）。
    n 为 做 计数 用 文本。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 字符串等于 用 做 子串 用 文本、i、1、「#」：
            只要 i 小于 n 并且 做 字符串等于 用 做 子串 用 文本、i、1、「\n」 等于 假：
                i 为 i 加 1。
            完。
            i 为 i 加 1。
            续。
        完。
        若 做 字符串等于 用 做 子串 用 文本、i、1、「\n」 或者 做 字符串等于 用 做 子串 用 文本、i、1、「\r」：
            i 为 i 加 1。
            续。
        完。
        可变 键 为 「」。
        可变 _键完 为 假。
        只要 i 小于 n 并且 _键完 等于 假：
            c 为 做 子串 用 文本、i、1。
            若 做 字符串等于 用 c、「=」：
                i 为 i 加 1。
                _键完 为 真。
            否则若 做 字符串等于 用 c、「 」 或者 做 字符串等于 用 c、「\t」：
                i 为 i 加 1。
            否则：
                键 为 做 字符串拼接 用 键、c。
                i 为 i 加 1。
            完。
        完。
        只要 i 小于 n 并且 (做 字符串等于 用 做 子串 用 文本、i、1、「 」 或者 做 字符串等于 用 做 子串 用 文本、i、1、「\t」)：
            i 为 i 加 1。
        完。
        可变 值 为 「」。
        可变 引号 为 假。
        若 i 小于 n 并且 (做 字符串等于 用 做 子串 用 文本、i、1、「\"」 或者 做 字符串等于 用 做 子串 用 文本、i、1、「'」)：
            引号 为 真。
            引号字符 为 做 子串 用 文本、i、1。
            i 为 i 加 1。
            只要 i 小于 n 并且 做 字符串等于 用 做 子串 用 文本、i、1、引号字符 等于 假：
                若 做 字符串等于 用 做 子串 用 文本、i、1、「\\」 并且 i 加 1 小于 n：
                    i 为 i 加 1。
                    esc 为 做 子串 用 文本、i、1。
                    若 做 字符串等于 用 esc、「n」：
                        值 为 做 字符串拼接 用 值、「\n」。
                    否则若 做 字符串等于 用 esc、「t」：
                        值 为 做 字符串拼接 用 值、「\t」。
                    否则：
                        值 为 做 字符串拼接 用 值、esc。
                    完。
                否则：
                    值 为 做 字符串拼接 用 值、做 子串 用 文本、i、1。
                完。
                i 为 i 加 1。
            完。
            若 i 小于 n：
                i 为 i 加 1。
            完。
        否则：
            只要 i 小于 n 并且 做 字符串等于 用 做 子串 用 文本、i、1、「\n」 等于 假 并且 做 字符串等于 用 做 子串 用 文本、i、1、「\r」 等于 假：
                若 做 字符串等于 用 做 子串 用 文本、i、1、「#」：
                    i 为 n。
                完。
                值 为 做 字符串拼接 用 值、做 子串 用 文本、i、1。
                i 为 i 加 1。
            完。
            只要 做 计数 用 值 大于 0 并且 做 字符串等于 用 做 子串 用 值、做 计数 用 值 减 1、1、「 」：
                值 为 做 子串 用 值、0、做 计数 用 值 减 1。
            完。
        完。
        若 做 计数 用 键 大于 0：
            做 追加 用 变量列表、一组：键、值。
        完。
    完。
    返还 变量列表。
完。

环境_加载dotenv 接受 环境、文本：
    变量列表 为 做 环境_解析dotenv 用 文本。
    可变 i 为 0。
    只要 i 小于 做 计数 用 变量列表：
        做 环境_环境_设 用 环境、变量列表 的 第 i 项 的 第 0 项、变量列表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 做 计数 用 变量列表。
完。

环境_展开 接受 环境、模板：
    n 为 做 计数 用 模板。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 加 1 小于 n 并且 做 字符串等于 用 做 子串 用 模板、i、2、「${」：
            i 为 i 加 2。
            可变 变量名 为 「」。
            可变 默认值 为 「」。
            可变 有默认 为 假。
            只要 i 小于 n 并且 做 字符串等于 用 做 子串 用 模板、i、1、「}」 等于 假：
                若 i 加 1 小于 n 并且 做 字符串等于 用 做 子串 用 模板、i、2、「:-」：
                    有默认 为 真。
                    i 为 i 加 2。
                    只要 i 小于 n 并且 做 字符串等于 用 做 子串 用 模板、i、1、「}」 等于 假：
                        默认值 为 做 字符串拼接 用 默认值、做 子串 用 模板、i、1。
                        i 为 i 加 1。
                    完。
                否则：
                    变量名 为 做 字符串拼接 用 变量名、做 子串 用 模板、i、1。
                    i 为 i 加 1。
                完。
            完。
            若 i 小于 n：
                i 为 i 加 1。
            完。
            值 为 做 环境_环境_取 用 环境、变量名。
            若 值 不等于 空：
                结果 为 做 字符串拼接 用 结果、值。
            否则若 有默认：
                结果 为 做 字符串拼接 用 结果、默认值。
            完。
        否则：
            结果 为 做 字符串拼接 用 结果、做 子串 用 模板、i、1。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

环境_命名空间 接受 环境、前缀：
    子环境 为 做 环境_环境_创建。
    子环境 的 第 0 项 为 环境 的 第 0 项。
    子环境 的 第 1 项 为 环境 的 第 1 项。
    子环境 的 第 4 项 为 前缀。
    返还 子环境。
完。

本章 收束。
