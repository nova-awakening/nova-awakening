《数据管道》

引用《..·基础》。

引用《..·数学·基础·数学库》。

可变 _管道_执行次数 为 0。

可变 _管道_总数据量 为 0。

管道_复制列表 接受 数据：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 结果、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_列表最大 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    可变 最大值 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 大于 最大值：
            最大值 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大值。
完。

管道_列表最小 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    可变 最小值 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 小于 最小值：
            最小值 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小值。
完。

管道_整数平方根 接受 n：
    可变 答案 为 0。
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 左 为 1。
    可变 右 为 n。
    只要 左 小于或等于 右：
        可变 中 为 (左 加 右) 除以 2。
        可变 平方 为 中 乘 中。
        若 平方 小于或等于 n：
            答案 为 中。
            左 为 中 加 1。
        否则：
            右 为 中 减 1。
        完。
    完。
    返还 答案。
完。

管道_排序 接受 数据：
    可变 结果 为 做 管道_复制列表 用 数据。
    可变 i 为 1。
    只要 i 小于 做 计数 用 结果：
        可变 当前 为 结果 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0：
            若 结果 的 第 j 项 大于 当前：
                结果 的 第 j 加 1 项 为 结果 的 第 j 项。
                j 为 j 减 1。
            否则：
                j 为 负 1。
            完。
        完。
        结果 的 第 j 加 1 项 为 当前。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_归约 接受 数据、归约函数、初始值：
    可变 累积 为 初始值。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        累积 为 做 归约函数 用 累积、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 累积。
完。

管道_过滤 接受 数据、条件：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 做 条件 用 数据 的 第 i 项：
            做 追加 用 结果、数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_映射 接受 数据、变换类型、参数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 变换类型 等于 0：
            做 追加 用 结果、数据 的 第 i 项 加 参数。
        否则：
            若 变换类型 等于 1：
                做 追加 用 结果、数据 的 第 i 项 乘 参数 除以 100。
            否则：
                若 变换类型 等于 2：
                    做 追加 用 结果、做 绝对值 用 数据 的 第 i 项。
                否则：
                    做 追加 用 结果、数据 的 第 i 项。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_过滤_大于 接受 数据、阈值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 大于 阈值：
            做 追加 用 结果、数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_过滤_范围 接受 数据、下界、上界：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 大于或等于 下界：
            若 数据 的 第 i 项 小于或等于 上界：
                做 追加 用 结果、数据 的 第 i 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_求和 接受 数据：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        总和 为 总和 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总和。
完。

管道_均值 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    返还 做 管道_求和 用 数据 除以 做 计数 用 数据。
完。

管道_最大 接受 数据：
    返还 做 管道_列表最大 用 数据。
完。

管道_最小 接受 数据：
    返还 做 管道_列表最小 用 数据。
完。

批处理_分割 接受 数据、批大小：
    可变 批列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 批 为 一组：（空）。
        可变 截止 为 i 加 批大小。
        若 截止 大于 做 计数 用 数据：
            截止 为 做 计数 用 数据。
        完。
        可变 j 为 i。
        只要 j 小于 截止：
            做 追加 用 批、数据 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 批列表、批。
        i 为 i 加 批大小。
    完。
    返还 批列表。
完。

管道_去重 接受 数据：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 已存在 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 结果：
            若 结果 的 第 j 项 等于 数据 的 第 i 项：
                已存在 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已存在 等于 假：
            做 追加 用 结果、数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_等间隔采样 接受 数据、间隔：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 结果、数据 的 第 i 项。
        i 为 i 加 间隔。
    完。
    返还 结果。
完。

管道_方差 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    平均值 为 做 管道_均值 用 数据。
    可变 平方和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 差值 为 数据 的 第 i 项 减 平均值。
        平方和 为 平方和 加 差值 乘 差值。
        i 为 i 加 1。
    完。
    返还 平方和 除以 做 计数 用 数据。
完。

管道_标准差 接受 数据：
    返还 做 管道_整数平方根 用 做 管道_方差 用 数据。
完。

管道_滑动窗口 接受 数据、窗口大小：
    可变 结果 为 一组：（空）。
    可变 窗口 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 窗口大小 小于或等于 做 计数 用 数据：
        可变 j 为 0。
        只要 j 小于 窗口大小：
            做 追加 用 窗口、数据 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、窗口。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_移动平均 接受 数据、窗口：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 总和 为 0。
        可变 数量 为 0。
        可变 j 为 i 减 窗口 加 1。
        若 j 小于 0：
            j 为 0。
        完。
        只要 j 小于或等于 i：
            总和 为 总和 加 数据 的 第 j 项。
            数量 为 数量 加 1。
            j 为 j 加 1。
        完。
        做 追加 用 结果、总和 除以 数量。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_归一化 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 一组：（空）。
    完。
    最小值 为 做 管道_最小 用 数据。
    最大值 为 做 管道_最大 用 数据。
    范围 为 最大值 减 最小值。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    若 范围 等于 0：
        只要 i 小于 做 计数 用 数据：
            做 追加 用 结果、500。
            i 为 i 加 1。
        完。
        返还 结果。
    完。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 结果、(数据 的 第 i 项 减 最小值) 乘 1000 除以 范围。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_标准化 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 一组：（空）。
    完。
    平均值 为 做 管道_均值 用 数据。
    标准差 为 做 管道_标准差 用 数据。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    若 标准差 等于 0：
        只要 i 小于 做 计数 用 数据：
            做 追加 用 结果、0。
            i 为 i 加 1。
        完。
        返还 结果。
    完。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 结果、(数据 的 第 i 项 减 平均值) 乘 1000 除以 标准差。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_分组求和 接受 键列表、值列表：
    可变 组键 为 一组：（空）。
    可变 组值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        当前键 为 键列表 的 第 i 项。
        可变 索引 为 负 1。
        可变 j 为 0。
        只要 j 小于 做 计数 用 组键：
            若 组键 的 第 j 项 等于 当前键：
                索引 为 j。
            完。
            j 为 j 加 1。
        完。
        若 索引 大于或等于 0：
            组值 的 第 索引 项 为 组值 的 第 索引 项 加 值列表 的 第 i 项。
        否则：
            做 追加 用 组键、当前键。
            做 追加 用 组值、值列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：组键、组值。
完。

管道_链 接受 数据、操作列表：
    可变 当前 为 做 管道_复制列表 用 数据。
    可变 i 为 0。
    只要 i 小于 做 计数 用 操作列表：
        op 为 操作列表 的 第 i 项。
        若 op 的 第 0 项 等于 0：
            当前 为 做 管道_映射 用 当前、0、op 的 第 1 项。
        否则若 op 的 第 0 项 等于 1：
            当前 为 做 管道_映射 用 当前、1、op 的 第 1 项。
        否则若 op 的 第 0 项 等于 2：
            当前 为 做 管道_过滤_大于 用 当前、op 的 第 1 项。
        否则若 op 的 第 0 项 等于 3：
            当前 为 做 管道_去重 用 当前。
        否则若 op 的 第 0 项 等于 4：
            当前 为 做 管道_归一化 用 当前。
        否则若 op 的 第 0 项 等于 5：
            当前 为 做 管道_标准化 用 当前。
        完。
        i 为 i 加 1。
    完。
    返还 当前。
完。

管道_拆分 接受 数据、训练比、验证比：
    n 为 做 计数 用 数据。
    训练数 为 n 乘 训练比 除以 100。
    验证数 为 n 乘 验证比 除以 100。
    可变 训练集 为 一组：（空）。
    可变 验证集 为 一组：（空）。
    可变 测试集 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 训练数：
            做 追加 用 训练集、数据 的 第 i 项。
        否则：
            若 i 小于 训练数 加 验证数：
                做 追加 用 验证集、数据 的 第 i 项。
            否则：
                做 追加 用 测试集、数据 的 第 i 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 一组：训练集、验证集、测试集。
完。

管道_异常值 接受 数据、倍数：
    已排序 为 做 管道_排序 用 数据。
    可变 异常 为 一组：（空）。
    n 为 做 计数 用 已排序。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    q1 为 已排序 的 第 n 除以 4 项。
    q3 为 已排序 的 第 n 乘 3 除以 4 项。
    iqr 为 q3 减 q1。
    下界 为 q1 减 iqr 乘 倍数 除以 1000。
    上界 为 q3 加 iqr 乘 倍数 除以 1000。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 小于 下界：
            做 追加 用 异常、i。
        否则：
            若 数据 的 第 i 项 大于 上界：
                做 追加 用 异常、i。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 异常。
完。

管道_添加噪声 接受 数据、种子、幅度：
    可变 结果 为 一组：（空）。
    可变 rng 为 种子。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        rng 为 rng 乘 1103515245 加 12345。
        若 rng 小于 0：
            rng 为 0 减 rng。
        完。
        噪声 为 rng 取余于 (幅度 乘 2 加 1) 减 幅度。
        做 追加 用 结果、数据 的 第 i 项 加 噪声。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_报告 接受 数据：
    做 打印 用 「数据管道统计:」。
    做 打印 用 「 样本数: 」 加 做 转字符串 用 做 计数 用 数据。
    做 打印 用 「 均值: 」 加 做 转字符串 用 做 管道_均值 用 数据。
    做 打印 用 「 标准差: 」 加 做 转字符串 用 做 管道_标准差 用 数据。
    做 打印 用 「 最小: 」 加 做 转字符串 用 做 管道_最小 用 数据。
    做 打印 用 「 最大: 」 加 做 转字符串 用 做 管道_最大 用 数据。
完。

管道_f1分数 接受 混淆：
    p 为 做 管道_精确率 用 混淆。
    r 为 做 管道_召回率 用 混淆。
    若 p 加 r 等于 0：
        返还 0。
    完。
    返还 2 乘 p 乘 r 除以 (p 加 r)。
完。

管道_召回率 接受 混淆：
    tp 为 混淆 的 第 0 项。
    fn 为 混淆 的 第 2 项。
    若 tp 加 fn 等于 0：
        返还 0。
    完。
    返还 tp 乘 1000 除以 (tp 加 fn)。
完。

数据_k折 接受 数据、k：
    若 k 小于或等于 0：
        返还 一组：（空）。
    完。
    n 为 做 计数 用 数据。
    可变 折大小 为 n 除以 k。
    若 折大小 小于 1：
        折大小 为 1。
    完。
    可变 折列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 k：
        可变 折 为 一组：（空）。
        可变 起点 为 i 乘 折大小。
        可变 j 为 0。
        只要 j 小于 折大小：
            若 起点 加 j 小于 n：
                做 追加 用 折、数据 的 第 起点 加 j 项。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 折列表、折。
        i 为 i 加 1。
    完。
    返还 折列表。
完。

数据_分割 接受 数据、训练比例：
    n 为 做 计数 用 数据。
    训练数 为 n 乘 训练比例 除以 1000。
    可变 训练集 为 一组：（空）。
    可变 测试集 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 训练数：
            做 追加 用 训练集、数据 的 第 i 项。
        否则：
            做 追加 用 测试集、数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：训练集、测试集。
完。

数据_过采样 接受 数据、标签、目标类、倍数：
    可变 结果 为 一组：（空）。
    可变 轮次 为 0。
    可变 标签结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 结果、数据 的 第 i 项。
        做 追加 用 标签结果、标签 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 轮次 小于 倍数 减 1：
        i 为 0。
        只要 i 小于 做 计数 用 数据：
            若 标签 的 第 i 项 等于 目标类：
                做 追加 用 结果、数据 的 第 i 项。
                做 追加 用 标签结果、标签 的 第 i 项。
            完。
            i 为 i 加 1。
        完。
        轮次 为 轮次 加 1。
    完。
    返还 一组：结果、标签结果。
完。

数据_随机采样 接受 数据、采样数、种子：
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 采样数：
        s 为 s 乘 1103515245 加 12345。
        若 s 小于 0：
            s 为 0 减 s。
        完。
        做 追加 用 结果、数据 的 第 s 取余于 n 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数据管道_创建 接受 零参：
    返还 一组：一组：（空）。
完。

管道_混淆矩阵 接受 预测、真实：
    n 为 做 计数 用 预测。
    可变 tp 为 0。
    可变 fp 为 0。
    可变 fn 为 0。
    可变 tn 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 预测 的 第 i 项 等于 1：
            若 真实 的 第 i 项 等于 1：
                tp 为 tp 加 1。
            否则：
                fp 为 fp 加 1。
            完。
        否则：
            若 真实 的 第 i 项 等于 1：
                fn 为 fn 加 1。
            否则：
                tn 为 tn 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 一组：tp、fp、fn、tn。
完。

特征_多项式 接受 特征列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 特征列表：
        可变 x 为 特征列表 的 第 i 项。
        做 追加 用 结果、一组：x、x 乘 x 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

特征_最小最大 接受 特征列：
    若 做 计数 用 特征列 等于 0：
        返还 一组：（空）。
    完。
    可变 最小值 为 特征列 的 第 0 项。
    可变 最大值 为 特征列 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 特征列：
        若 特征列 的 第 i 项 小于 最小值：
            最小值 为 特征列 的 第 i 项。
        完。
        若 特征列 的 第 i 项 大于 最大值：
            最大值 为 特征列 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 范围 为 最大值 减 最小值。
    若 范围 等于 0：
        范围 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 特征列：
        做 追加 用 结果、(特征列 的 第 i 项 减 最小值) 乘 1000 除以 范围。
        i 为 i 加 1。
    完。
    返还 结果。
完。

特征_独热 接受 类别列表、类别数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 类别列表：
        可变 向量 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 类别数：
            若 j 等于 类别列表 的 第 i 项：
                做 追加 用 向量、1000。
            否则：
                做 追加 用 向量、0。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、向量。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_添加归约 接受 管道、名称、归约函数、初始值：
    做 追加 用 管道 的 第 0 项、一组：名称、归约函数、管道_归约、初始值。
完。

管道_添加映射 接受 管道、名称、函数体：
    做 追加 用 管道 的 第 0 项、一组：名称、函数体、管道_映射。
完。

管道_添加过滤 接受 管道、名称、条件：
    做 追加 用 管道 的 第 0 项、一组：名称、条件、管道_过滤。
完。

管道_运行 接受 管道、数据：
    阶段列表 为 管道 的 第 0 项。
    可变 当前数据 为 做 管道_复制列表 用 数据。
    可变 i 为 0。
    只要 i 小于 做 计数 用 阶段列表：
        阶段 为 阶段列表 的 第 i 项。
        类型 为 阶段 的 第 2 项。
        若 类型 等于 管道_映射：
            可变 结果 为 一组：（空）。
            可变 j 为 0。
            只要 j 小于 做 计数 用 当前数据：
                做 追加 用 结果、做 阶段 的 第 1 项 用 当前数据 的 第 j 项。
                j 为 j 加 1。
            完。
            当前数据 为 结果。
        否则：
            若 类型 等于 管道_过滤：
                可变 结果 为 一组：（空）。
                可变 j 为 0。
                只要 j 小于 做 计数 用 当前数据：
                    若 做 阶段 的 第 1 项 用 当前数据 的 第 j 项：
                        做 追加 用 结果、当前数据 的 第 j 项。
                    完。
                    j 为 j 加 1。
                完。
                当前数据 为 结果。
            否则：
                若 类型 等于 管道_归约：
                    可变 累积 为 阶段 的 第 3 项。
                    可变 j 为 0。
                    只要 j 小于 做 计数 用 当前数据：
                        累积 为 做 阶段 的 第 1 项 用 累积、当前数据 的 第 j 项。
                        j 为 j 加 1。
                    完。
                    当前数据 为 一组：累积。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 当前数据。
完。

管道_精确率 接受 混淆：
    tp 为 混淆 的 第 0 项。
    fp 为 混淆 的 第 1 项。
    若 tp 加 fp 等于 0：
        返还 0。
    完。
    返还 tp 乘 1000 除以 (tp 加 fp)。
完。

评估_准确率 接受 预测、真实：
    n 为 做 计数 用 预测。
    可变 正确 为 0。
    若 n 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        若 预测 的 第 i 项 等于 真实 的 第 i 项：
            正确 为 正确 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 正确 乘 1000 除以 n。
完。

管道_记录执行 接受 数据量：
    _管道_执行次数 为 _管道_执行次数 加 1。
    _管道_总数据量 为 _管道_总数据量 加 数据量。
完。

管道_吞吐评估 接受 零参：
    若 _管道_执行次数 等于 0：
        返还 「无执行记录」。
    完。
    平均 为 _管道_总数据量 除以 _管道_执行次数。
    若 平均 大于 10000：
        返还 「高吞吐」。
    完。
    若 平均 大于 1000：
        返还 「中等吞吐」。
    完。
    返还 「低吞吐(可能存在瓶颈)」。
完。

本章 收束。
