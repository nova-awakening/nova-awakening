《DNS协议》

引用《..·基础》。

DNS_A记录 为 1。

DNS_名称服务器 为 2。

DNS_别名 为 5。

DNS_授权起始 为 6。

DNS_邮件交换 为 15。

DNS_文本 为 16。

DNS_AAAA记录 为 28。

DNS_服务 为 33。

DNS_无错误 为 0。

DNS_格式错误 为 1。

DNS_服务失败 为 2。

DNS_域不存在 为 3。

DNS_未实现 为 4。

DNS_被拒绝 为 5。

dns_创建头 接受 id、是查询：
    可变 标志 为 0。
    若 是查询 等于 0：
        标志 为 32768。
    完。
    返还 一组：id、标志、0。
完。

dns_头_id 接受 头：
    返还 头 的 第 0 项。
完。

dns_头_是回应 接受 头：
    返还 头 的 第 1 项 大于或等于 32768。
完。

dns_头_问题数 接受 头：
    返还 头 的 第 2 项。
完。

dns_头_回答数 接受 头：
    返还 头 的 第 3 项。
完。

dns_创建记录 接受 名称、类型、ttl、数据：
    返还 一组：名称、类型、1、ttl、数据。
完。

dns_记录名 接受 记录：
    返还 记录 的 第 0 项。
完。

dns_记录类型 接受 记录：
    返还 记录 的 第 1 项。
完。

dns_记录TTL 接受 记录：
    返还 记录 的 第 3 项。
完。

dns_记录数据 接受 记录：
    返还 记录 的 第 4 项。
完。

dns_类型名_旧 接受 类型：
    若 类型 等于 DNS_A记录：
        返还 「A」。
    完。
    若 类型 等于 DNS_名称服务器：
        返还 「NS」。
    完。
    若 类型 等于 DNS_别名：
        返还 「CNAME」。
    完。
    若 类型 等于 DNS_授权起始：
        返还 「SOA」。
    完。
    若 类型 等于 DNS_邮件交换：
        返还 「MX」。
    完。
    若 类型 等于 DNS_文本：
        返还 「TXT」。
    完。
    若 类型 等于 DNS_AAAA记录：
        返还 「AAAA」。
    完。
    若 类型 等于 DNS_服务：
        返还 「SRV」。
    完。
    返还 「未知」。
完。

dns_编码域名_旧 接受 域名：
    可变 结果 为 一组：（空）。
    可变 标签起 为 0。
    可变 i 为 0。
    只要 i 小于或等于 做 字节长度 用 域名：
        若 i 等于 做 字节长度 用 域名：
            是点 为 1。
        否则若 做 字节取 用 域名、i 等于 字符_句点：
            是点 为 1。
        完。
        若 是点 等于 1：
            标签长 为 i 减 标签起。
            做 追加 用 结果、标签长。
            可变 j 为 标签起。
            只要 j 小于 i：
                做 追加 用 结果、做 字节取 用 域名、j。
                j 为 j 加 1。
            完。
            标签起 为 i 加 1。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 结果、0。
    返还 结果。
完。

dns_解码域名_旧 接受 字节列表、偏移：
    可变 名称 为 「」。
    可变 位置 为 偏移。
    可变 第一段 为 1。
    可变 _dns完 为 假。
    只要 位置 小于 做 计数 用 字节列表 并且 _dns完 等于 假：
        长度 为 字节列表 的 第 位置 项。
        若 长度 等于 0：
            _dns完 为 真。
        否则：
            位置 为 位置 加 1。
            若 第一段 等于 0：
                名称 为 做 字符串拼接 用 名称、「.」。
            完。
            第一段 为 0。
            可变 j 为 0。
            只要 j 小于 长度 并且 位置 小于 做 计数 用 字节列表：
                名称 为 做 字符串拼接 用 名称、做 字符 用 字节列表 的 第 位置 项。
                位置 为 位置 加 1。
                j 为 j 加 1。
            完。
        完。
    完。
    返还 名称。
完。

dns_ip转文 接受 a、b、c、d：
    可变 r 为 做 转字符串 用 a。
    r 为 做 字符串拼接 用 r、「.」。
    r 为 做 字符串拼接 用 r、做 转字符串 用 b。
    r 为 做 字符串拼接 用 r、「.」。
    r 为 做 字符串拼接 用 r、做 转字符串 用 c。
    r 为 做 字符串拼接 用 r、「.」。
    r 为 做 字符串拼接 用 r、做 转字符串 用 d。
    返还 r。
完。

可变 dns_缓存字典 为 做 字典。

dns_缓存_查 接受 域名：
    返还 做 字典取 用 dns_缓存字典、域名。
完。

dns_缓存_存 接受 域名、ip、ttl：
    做 字典设 用 dns_缓存字典、域名、ip。
完。

dns_缓存_清 接受 零参：
    dns_缓存字典 为 做 字典。
完。

DNS_类型_A 为 1。

DNS_类型_AAAA 为 28。

DNS_类型_CNAME 为 5。

DNS_类型_MX 为 15。

DNS_类型_NS 为 2。

DNS_类型_TXT 为 16。

DNS_类型_SOA 为 6。

DNS_类型_PTR 为 12。

dns_构建报头 接受 事务ID、标志、问题数、回答数、授权数、附加数：
    可变 报文 为 一组：（空）。
    做 追加 用 报文、事务ID 右移 8 位与 0xFF。
    做 追加 用 报文、事务ID 位与 0xFF。
    做 追加 用 报文、标志 右移 8 位与 0xFF。
    做 追加 用 报文、标志 位与 0xFF。
    做 追加 用 报文、问题数 右移 8 位与 0xFF。
    做 追加 用 报文、问题数 位与 0xFF。
    做 追加 用 报文、回答数 右移 8 位与 0xFF。
    做 追加 用 报文、回答数 位与 0xFF。
    做 追加 用 报文、授权数 右移 8 位与 0xFF。
    做 追加 用 报文、授权数 位与 0xFF。
    做 追加 用 报文、附加数 右移 8 位与 0xFF。
    做 追加 用 报文、附加数 位与 0xFF。
    返还 报文。
完。

dns_编码域名 接受 域名：
    可变 结果 为 一组：（空）。
    可变 标签 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 域名：
        c 为 做 字符码 用 域名、i。
        若 c 等于 字符_句点：
            做 追加 用 结果、做 计数 用 标签。
            可变 j 为 0。
            只要 j 小于 做 计数 用 标签：
                做 追加 用 结果、标签 的 第 j 项。
                j 为 j 加 1。
            完。
            标签 为 一组：（空）。
        否则：
            做 追加 用 标签、c。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 标签 大于 0：
        做 追加 用 结果、做 计数 用 标签。
        可变 j 为 0。
        只要 j 小于 做 计数 用 标签：
            做 追加 用 结果、标签 的 第 j 项。
            j 为 j 加 1。
        完。
    完。
    做 追加 用 结果、0。
    返还 结果。
完。

dns_构建查询 接受 事务ID、域名、类型：
    报头 为 做 dns_构建报头 用 事务ID、0x0100、1、0、0、0。
    名称 为 做 dns_编码域名 用 域名。
    可变 报文 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 报头：
        做 追加 用 报文、报头 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 名称：
        做 追加 用 报文、名称 的 第 i 项。
        i 为 i 加 1。
    完。
    做 追加 用 报文、类型 右移 8 位与 0xFF。
    做 追加 用 报文、类型 位与 0xFF。
    做 追加 用 报文、0。
    做 追加 用 报文、1。
    返还 报文。
完。

dns_解析报头 接受 报文：
    若 做 计数 用 报文 小于 12：
        返还 空。
    完。
    事务ID 为 报文 的 第 0 项 乘 256 加 报文 的 第 1 项。
    标志 为 报文 的 第 2 项 乘 256 加 报文 的 第 3 项。
    问题数 为 报文 的 第 4 项 乘 256 加 报文 的 第 5 项。
    回答数 为 报文 的 第 6 项 乘 256 加 报文 的 第 7 项。
    返还 一组：事务ID、标志、问题数、回答数。
完。

dns_是响应 接受 标志：
    返还 标志 位与 0x8000 不等于 0。
完。

dns_响应码 接受 标志：
    返还 标志 位与 0x000F。
完。

dns_是递归可用 接受 标志：
    返还 标志 位与 0x0080 不等于 0。
完。

dns_解码域名 接受 报文、偏移：
    可变 名称 为 「」。
    可变 位置 为 偏移。
    可变 跳转 为 假。
    可变 保存pos 为 0。
    只要 位置 小于 做 计数 用 报文：
        长度 为 报文 的 第 位置 项。
        若 长度 等于 0：
            位置 为 位置 加 1。
            返还 一组：名称、位置。
        完。
        若 长度 位与 0xC0 等于 0xC0：
            若 非 跳转：
                保存pos 为 位置 加 2。
            完。
            位置 为 (长度 位与 0x3F) 乘 256 加 报文 的 第 位置 加 1 项。
            跳转 为 真。
        否则：
            位置 为 位置 加 1。
            可变 i 为 0。
            只要 i 小于 长度 并且 位置 小于 做 计数 用 报文：
                名称 为 名称 加 做 从字符码 用 报文 的 第 位置 项。
                位置 为 位置 加 1。
                i 为 i 加 1。
            完。
            名称 为 名称 加 「.」。
        完。
    完。
    若 跳转：
        返还 一组：名称、保存pos。
    完。
    返还 一组：名称、位置。
完。

可变 _dns_缓存详情 为 做 字典。

可变 _dns_缓存名列 为 一组：（空）。

dns_缓存_带时间存 接受 域名、ip、ttl、时间：
    已有 为 做 字典取 用 _dns_缓存详情、域名。
    若 已有 等于 空：
        做 追加 用 _dns_缓存名列、域名。
    完。
    做 字典设 用 _dns_缓存详情、域名、一组：ip、ttl、时间。
完。

dns_缓存_过期清理 接受 当前时间：
    可变 新名 为 一组：（空）。
    新详 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _dns_缓存名列：
        域 为 _dns_缓存名列 的 第 i 项。
        记录 为 做 字典取 用 _dns_缓存详情、域。
        若 记录 不等于 空 并且 当前时间 减 记录 的 第 2 项 小于 记录 的 第 1 项：
            做 追加 用 新名、域。
            做 字典设 用 新详、域、记录。
        完。
        i 为 i 加 1。
    完。
    _dns_缓存名列 为 新名。
    _dns_缓存详情 为 新详。
完。

dns_缓存数 接受 零参：
    返还 做 计数 用 _dns_缓存名列。
完。

dns_类型名 接受 类型：
    若 类型 等于 DNS_类型_A：
        返还 「A」。
    完。
    若 类型 等于 DNS_类型_AAAA：
        返还 「AAAA」。
    完。
    若 类型 等于 DNS_类型_CNAME：
        返还 「CNAME」。
    完。
    若 类型 等于 DNS_类型_MX：
        返还 「MX」。
    完。
    若 类型 等于 DNS_类型_NS：
        返还 「NS」。
    完。
    若 类型 等于 DNS_类型_TXT：
        返还 「TXT」。
    完。
    若 类型 等于 DNS_类型_SOA：
        返还 「SOA」。
    完。
    若 类型 等于 DNS_类型_PTR：
        返还 「PTR」。
    完。
    返还 「未知」。
完。

可变 _dns_查询次数 为 0。

可变 _dns_缓存命中 为 0。

可变 _dns_缓存未中 为 0。

dns_记录查询 接受 零参：
    _dns_查询次数 为 _dns_查询次数 加 1。
完。

dns_记录命中 接受 零参：
    _dns_缓存命中 为 _dns_缓存命中 加 1。
完。

dns_记录未中 接受 零参：
    _dns_缓存未中 为 _dns_缓存未中 加 1。
完。

dns_命中率 接受 零参：
    总 为 _dns_缓存命中 加 _dns_缓存未中。
    若 总 等于 0：
        返还 0。
    完。
    返还 _dns_缓存命中 乘 100 除以 总。
完。

本章 收束。
