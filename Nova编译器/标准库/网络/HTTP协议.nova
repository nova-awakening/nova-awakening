《HTTP协议》

引用《..·基础》。

HTTP方法_GET 为 1。

HTTP方法_POST 为 2。

HTTP方法_PUT 为 3。

HTTP方法_DELETE 为 4。

HTTP方法_HEAD 为 5。

http_创建请求 接受 方法、路径：
    返还 一组：方法、路径、「HTTP/1.1」、一组：（空）、「」。
完。

http_方法名 接受 方法：
    若 方法 等于 HTTP方法_GET：
        返还 「GET」。
    完。
    若 方法 等于 HTTP方法_POST：
        返还 「POST」。
    完。
    若 方法 等于 HTTP方法_PUT：
        返还 「PUT」。
    完。
    若 方法 等于 HTTP方法_DELETE：
        返还 「DELETE」。
    完。
    若 方法 等于 HTTP方法_HEAD：
        返还 「HEAD」。
    完。
    返还 「GET」。
完。

http_设头 接受 请求、名、值：
    头部 为 请求 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 头部：
        若 头部 的 第 i 项 的 第 0 项 等于 名：
            头部 的 第 i 项 的 第 1 项 为 值。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 头部、一组：名、值。
完。

http_取头 接受 请求、名：
    头部 为 请求 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 头部：
        若 头部 的 第 i 项 的 第 0 项 等于 名：
            返还 头部 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 「」。
完。

http_序列化请求 接受 请求：
    换行 为 做 字符串拼接 用 做 字符 用 13、做 字符 用 10。
    可变 s 为 做 字符串拼接 用 做 http_方法名 用 请求 的 第 0 项、「 」。
    s 为 做 字符串拼接 用 s、做 字符串拼接 用 请求 的 第 1 项、「 」。
    s 为 做 字符串拼接 用 s、做 字符串拼接 用 请求 的 第 2 项、换行。
    头部 为 请求 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 头部：
        s 为 做 字符串拼接 用 s、做 字符串拼接 用 头部 的 第 i 项 的 第 0 项、「: 」。
        s 为 做 字符串拼接 用 s、做 字符串拼接 用 头部 的 第 i 项 的 第 1 项、换行。
        i 为 i 加 1。
    完。
    s 为 做 字符串拼接 用 s、换行。
    若 做 字节长度 用 请求 的 第 4 项 大于 0：
        s 为 做 字符串拼接 用 s、请求 的 第 4 项。
    完。
    返还 s。
完。

http_创建响应 接受 状态码：
    可变 文本 为 「正常」。
    若 状态码 等于 201：
        文本 为 「Created」。
    完。
    若 状态码 等于 204：
        文本 为 「No Content」。
    完。
    若 状态码 等于 301：
        文本 为 「Moved Permanently」。
    完。
    若 状态码 等于 302：
        文本 为 「Found」。
    完。
    若 状态码 等于 400：
        文本 为 「Bad Request」。
    完。
    若 状态码 等于 401：
        文本 为 「Unauthorized」。
    完。
    若 状态码 等于 403：
        文本 为 「Forbidden」。
    完。
    若 状态码 等于 404：
        文本 为 「Not Found」。
    完。
    若 状态码 等于 500：
        文本 为 「Internal Server Error」。
    完。
    返还 一组：状态码、文本、「HTTP/1.1」、一组：（空）、「」。
完。

http_URL编码 接受 文本：
    可变 结果 为 「」。
    hex表 为 「0123456789ABCDEF」。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 文本：
        c 为 做 字节取 用 文本、i。
        若 c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z 或者 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z 或者 c 大于或等于 字符_零 并且 c 小于或等于 字符_九 或者 c 等于 字符_减号 或者 c 等于 字符_下划线 或者 c 等于 字符_句点：
            结果 为 做 字符串拼接 用 结果、做 字符 用 c。
        否则：
            结果 为 做 字符串拼接 用 结果、「%」。
            结果 为 做 字符串拼接 用 结果、做 子串 用 hex表、c 除以 16、1。
            结果 为 做 字符串拼接 用 结果、做 子串 用 hex表、c 取余于 16、1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

http_解析URL 接受 url：
    可变 协议 为 0。
    可变 主机 为 「」。
    可变 端口 为 80。
    可变 路径 为 「/」。
    可变 查询 为 「」。
    可变 位 为 0。
    总 为 做 字节长度 用 url。
    可变 临时 为 「」。
    只要 位 小于 总 并且 做 字节取 用 url、位 不等于 字符_冒号：
        临时 为 做 字符串拼接 用 临时、做 字符 用 做 字节取 用 url、位。
        位 为 位 加 1。
    完。
    若 位 加 2 小于 总 并且 做 字节取 用 url、位 等于 字符_冒号 并且 做 字节取 用 url、位 加 1 等于 字符_斜杠 并且 做 字节取 用 url、位 加 2 等于 字符_斜杠：
        协议 为 临时。
        位 为 位 加 3。
        若 协议 等于 「https」：
            端口 为 443。
        完。
    否则：
        位 为 0。
    完。
    临时 为 「」。
    只要 位 小于 总 并且 做 字节取 用 url、位 不等于 字符_斜杠 并且 做 字节取 用 url、位 不等于 字符_问号：
        临时 为 做 字符串拼接 用 临时、做 字符 用 做 字节取 用 url、位。
        位 为 位 加 1。
    完。
    主机 为 临时。
    若 位 小于 总 并且 做 字节取 用 url、位 等于 字符_斜杠：
        路径 为 「」。
        只要 位 小于 总 并且 做 字节取 用 url、位 不等于 字符_问号：
            路径 为 做 字符串拼接 用 路径、做 字符 用 做 字节取 用 url、位。
            位 为 位 加 1。
        完。
    完。
    若 位 小于 总 并且 做 字节取 用 url、位 等于 字符_问号：
        位 为 位 加 1。
        只要 位 小于 总：
            查询 为 做 字符串拼接 用 查询、做 字符 用 做 字节取 用 url、位。
            位 为 位 加 1。
        完。
    完。
    返还 一组：协议、主机、端口、路径、查询。
完。

http_构建请求行 接受 方法、路径、版本：
    返还 方法 加 「 」 加 路径 加 「 HTTP/」 加 版本 加 「\r\n」。
完。

http_添加头部 接受 请求、键、值：
    返还 请求 加 键 加 「: 」 加 值 加 「\r\n」。
完。

http_完成头部 接受 请求：
    返还 请求 加 「\r\n」。
完。

http_构建GET 接受 主机、路径：
    可变 请求 为 做 http_构建请求行 用 「GET」、路径、「1.1」。
    请求 为 做 http_添加头部 用 请求、「Host」、主机。
    请求 为 做 http_添加头部 用 请求、「Connection」、「close」。
    请求 为 做 http_添加头部 用 请求、「User-Agent」、「Nova/1.0」。
    返还 做 http_完成头部 用 请求。
完。

http_构建POST 接受 主机、路径、内容类型、体：
    可变 请求 为 做 http_构建请求行 用 「POST」、路径、「1.1」。
    请求 为 做 http_添加头部 用 请求、「Host」、主机。
    请求 为 做 http_添加头部 用 请求、「Content-Type」、内容类型。
    请求 为 做 http_添加头部 用 请求、「Content-Length」、做 转字符串 用 做 计数 用 体。
    请求 为 做 http_添加头部 用 请求、「Connection」、「close」。
    请求 为 做 http_完成头部 用 请求。
    返还 请求 加 体。
完。

http_解析状态行 接受 行：
    可变 部分 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行：
        c 为 做 字节取 用 行、i。
        若 c 等于 字符_空格 并且 做 计数 用 部分 小于 2：
            做 追加 用 部分、当前。
            当前 为 「」。
        否则：
            当前 为 做 字符串拼接 用 当前、做 字符 用 c。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 部分、当前。
    若 做 计数 用 部分 小于 3：
        返还 空。
    完。
    返还 一组：部分 的 第 0 项、部分 的 第 1 项、部分 的 第 2 项。
完。

http_解析头部行 接受 行：
    可变 键 为 「」。
    可变 值 为 「」。
    可变 i 为 0。
    可变 找到分隔 为 假。
    只要 i 小于 做 计数 用 行：
        c 为 做 字节取 用 行、i。
        若 非 找到分隔 并且 c 等于 字符_冒号：
            找到分隔 为 真。
            i 为 i 加 1。
            若 i 小于 做 计数 用 行 并且 做 字节取 用 行、i 等于 字符_空格：
                i 为 i 加 1。
            完。
        否则若 非 找到分隔：
            键 为 做 字符串拼接 用 键、做 字符 用 c。
            i 为 i 加 1。
        否则：
            值 为 做 字符串拼接 用 值、做 字符 用 c。
            i 为 i 加 1。
        完。
    完。
    返还 一组：键、值。
完。

http_头部表_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、做 字典。
完。

http_头部表_设置 接受 表、键、值：
    索引 为 做 字典取 用 表 的 第 2 项、键。
    若 索引 不等于 空：
        表 的 第 1 项 的 第 索引 项 为 值。
        返还。
    完。
    做 字典设 用 表 的 第 2 项、键、做 计数 用 表 的 第 0 项。
    做 追加 用 表 的 第 0 项、键。
    做 追加 用 表 的 第 1 项、值。
完。

http_头部表_获取 接受 表、键：
    索引 为 做 字典取 用 表 的 第 2 项、键。
    若 索引 不等于 空：
        返还 表 的 第 1 项 的 第 索引 项。
    完。
    返还 空。
完。

http_头部表_数量 接受 表：
    返还 做 计数 用 表 的 第 0 项。
完。

http_状态名 接受 码：
    若 码 等于 200：
        返还 「正常」。
    完。
    若 码 等于 201：
        返还 「Created」。
    完。
    若 码 等于 204：
        返还 「No Content」。
    完。
    若 码 等于 301：
        返还 「Moved Permanently」。
    完。
    若 码 等于 302：
        返还 「Found」。
    完。
    若 码 等于 304：
        返还 「Not Modified」。
    完。
    若 码 等于 400：
        返还 「Bad Request」。
    完。
    若 码 等于 401：
        返还 「Unauthorized」。
    完。
    若 码 等于 403：
        返还 「Forbidden」。
    完。
    若 码 等于 404：
        返还 「Not Found」。
    完。
    若 码 等于 500：
        返还 「Internal Server Error」。
    完。
    若 码 等于 502：
        返还 「Bad Gateway」。
    完。
    若 码 等于 503：
        返还 「Service Unavailable」。
    完。
    返还 「未知」。
完。

http_是成功 接受 码：
    返还 码 大于或等于 200 并且 码 小于 300。
完。

http_是重定向 接受 码：
    返还 码 大于或等于 300 并且 码 小于 400。
完。

http_是客户端错误 接受 码：
    返还 码 大于或等于 400 并且 码 小于 500。
完。

http_是服务器错误 接受 码：
    返还 码 大于或等于 500 并且 码 小于 600。
完。

http_编码查询 接受 参数表：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数表 的 第 0 项：
        若 i 大于 0：
            结果 为 结果 加 「&」。
        完。
        结果 为 结果 加 参数表 的 第 0 项 的 第 i 项 加 「=」 加 参数表 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

http_解码查询 接受 查询：
    可变 键列 为 一组：（空）。
    可变 值列 为 一组：（空）。
    可变 当前键 为 「」。
    可变 当前值 为 「」。
    可变 在值中 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 查询：
        c 为 做 字节取 用 查询、i。
        若 c 等于 字符_等号：
            在值中 为 真。
        否则若 c 等于 38：
            做 追加 用 键列、当前键。
            做 追加 用 值列、当前值。
            当前键 为 「」。
            当前值 为 「」。
            在值中 为 假。
        否则若 在值中：
            当前值 为 做 字符串拼接 用 当前值、做 字符 用 c。
        否则：
            当前键 为 做 字符串拼接 用 当前键、做 字符 用 c。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前键 大于 0：
        做 追加 用 键列、当前键。
        做 追加 用 值列、当前值。
    完。
    返还 一组：键列、值列。
完。

可变 _http_请求数 为 0。

可变 _http_响应数 为 0。

可变 _http_错误数 为 0。

http_记录请求 接受 零参：
    _http_请求数 为 _http_请求数 加 1。
完。

http_记录响应 接受 零参：
    _http_响应数 为 _http_响应数 加 1。
完。

http_记录错误 接受 零参：
    _http_错误数 为 _http_错误数 加 1。
完。

本章 收束。
