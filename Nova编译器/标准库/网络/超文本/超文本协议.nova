《超文本协议》

引用《..·..·基础》。

HTTP_获取 为 「GET」。

HTTP_提交 为 「POST」。

HTTP_放置 为 「PUT」。

HTTP_删除 为 「DELETE」。

HTTP_补丁 为 「PATCH」。

HTTP_头 为 「HEAD」。

HTTP_选项 为 「OPTIONS」。

http_状态描述 接受 码：
    若 码 等于 200：
        返还 「正常」。
    完。
    若 码 等于 201：
        返还 「Created」。
    完。
    若 码 等于 204：
        返还 「No Content」。
    完。
    若 码 等于 301：
        返还 「Moved Permanently」。
    完。
    若 码 等于 302：
        返还 「Found」。
    完。
    若 码 等于 304：
        返还 「Not Modified」。
    完。
    若 码 等于 400：
        返还 「Bad Request」。
    完。
    若 码 等于 401：
        返还 「Unauthorized」。
    完。
    若 码 等于 403：
        返还 「Forbidden」。
    完。
    若 码 等于 404：
        返还 「Not Found」。
    完。
    若 码 等于 405：
        返还 「Method Not Allowed」。
    完。
    若 码 等于 409：
        返还 「Conflict」。
    完。
    若 码 等于 422：
        返还 「Unprocessable Entity」。
    完。
    若 码 等于 429：
        返还 「Too Many Requests」。
    完。
    若 码 等于 500：
        返还 「Internal Server Error」。
    完。
    若 码 等于 502：
        返还 「Bad Gateway」。
    完。
    若 码 等于 503：
        返还 「Service Unavailable」。
    完。
    若 码 等于 504：
        返还 「Gateway Timeout」。
    完。
    返还 「未知」。
完。

http_请求_创建 接受 方法、路径：
    返还 一组：方法、路径、「HTTP/1.1」、做 字典、「」、做 字典、做 字典、「」。
完。

http_请求_设头 接受 请求、键、值：
    做 字典设 用 请求 的 第 3 项、键、值。
完。

http_请求_取头 接受 请求、键：
    返还 做 字典取 用 请求 的 第 3 项、键。
完。

http_请求_设正文 接受 请求、正文：
    请求 的 第 4 项 为 正文。
完。

http_请求_方法 接受 请求：
    返还 请求 的 第 0 项。
完。

http_请求_路径 接受 请求：
    返还 请求 的 第 1 项。
完。

http_请求_正文 接受 请求：
    返还 请求 的 第 4 项。
完。

http_请求_查询 接受 请求、键：
    返还 做 字典取 用 请求 的 第 5 项、键。
完。

http_请求_路径参数 接受 请求、键：
    返还 做 字典取 用 请求 的 第 6 项、键。
完。

http_解析查询 接受 路径：
    n 为 做 计数 用 路径。
    可变 问号位 为 负 1。
    可变 i 为 0。
    只要 i 小于 n 并且 问号位 小于 0：
        若 做 子串 用 路径、i、1 等于 「?」：
            问号位 为 i。
        完。
        i 为 i 加 1。
    完。
    若 问号位 小于 0：
        返还 一组：路径、做 字典。
    完。
    纯路径 为 做 子串 用 路径、0、问号位。
    查询串 为 做 子串 用 路径、问号位 加 1、n 减 问号位 减 1。
    参数 为 做 字典。
    可变 当前键 为 「」。
    可变 当前值 为 「」。
    可变 在值 为 假。
    i 为 0。
    只要 i 小于或等于 做 计数 用 查询串：
        可变 c 为 「」。
        若 i 小于 做 计数 用 查询串：
            c 为 做 子串 用 查询串、i、1。
        完。
        若 i 等于 做 计数 用 查询串 或者 做 字符串等于 用 c、「&」：
            若 做 计数 用 当前键 大于 0：
                做 字典设 用 参数、当前键、当前值。
            完。
            当前键 为 「」。
            当前值 为 「」。
            在值 为 假。
        否则若 做 字符串等于 用 c、「=」 并且 在值 等于 假：
            在值 为 真。
        否则：
            若 在值：
                当前值 为 做 字符串拼接 用 当前值、c。
            否则：
                当前键 为 做 字符串拼接 用 当前键、c。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 一组：纯路径、参数。
完。

http_响应_创建 接受 状态码：
    响应 为 一组：状态码、做 http_状态描述 用 状态码、「HTTP/1.1」、做 字典、「」。
    做 字典设 用 响应 的 第 3 项、「Content-Type」、「text/html; charset=utf-8」。
    返还 响应。
完。

http_响应_json 接受 数据：
    响应 为 做 http_响应_创建 用 200。
    做 字典设 用 响应 的 第 3 项、「Content-Type」、「application/json; charset=utf-8」。
    响应 的 第 4 项 为 数据。
    返还 响应。
完。

http_响应_文本 接受 文本：
    响应 为 做 http_响应_创建 用 200。
    做 字典设 用 响应 的 第 3 项、「Content-Type」、「text/plain; charset=utf-8」。
    响应 的 第 4 项 为 文本。
    返还 响应。
完。

http_响应_重定向 接受 url、永久：
    可变 码 为 302。
    若 永久：
        码 为 301。
    完。
    响应 为 做 http_响应_创建 用 码。
    做 字典设 用 响应 的 第 3 项、「Location」、url。
    返还 响应。
完。

http_响应_设头 接受 响应、键、值：
    做 字典设 用 响应 的 第 3 项、键、值。
完。

http_响应_设正文 接受 响应、正文：
    响应 的 第 4 项 为 正文。
完。

http_响应_状态码 接受 响应：
    返还 响应 的 第 0 项。
完。

http_响应_序列化 接受 响应：
    可变 结果 为 响应 的 第 2 项。
    结果 为 做 字符串拼接 用 结果、「 」。
    结果 为 做 字符串拼接 用 结果、做 http_数到文 用 响应 的 第 0 项。
    结果 为 做 字符串拼接 用 结果、「 」。
    结果 为 做 字符串拼接 用 结果、响应 的 第 1 项。
    结果 为 做 字符串拼接 用 结果、「\r\n」。
    做 字典设 用 响应 的 第 3 项、「Content-Length」、做 http_数到文 用 做 计数 用 响应 的 第 4 项。
    ct 为 做 字典取 用 响应 的 第 3 项、「Content-Type」。
    若 ct 不等于 空：
        结果 为 做 字符串拼接 用 结果、「Content-Type: 」。
        结果 为 做 字符串拼接 用 结果、ct。
        结果 为 做 字符串拼接 用 结果、「\r\n」。
    完。
    cl 为 做 字典取 用 响应 的 第 3 项、「Content-Length」。
    若 cl 不等于 空：
        结果 为 做 字符串拼接 用 结果、「Content-Length: 」。
        结果 为 做 字符串拼接 用 结果、cl。
        结果 为 做 字符串拼接 用 结果、「\r\n」。
    完。
    loc 为 做 字典取 用 响应 的 第 3 项、「Location」。
    若 loc 不等于 空：
        结果 为 做 字符串拼接 用 结果、「Location: 」。
        结果 为 做 字符串拼接 用 结果、loc。
        结果 为 做 字符串拼接 用 结果、「\r\n」。
    完。
    结果 为 做 字符串拼接 用 结果、「\r\n」。
    结果 为 做 字符串拼接 用 结果、响应 的 第 4 项。
    返还 结果。
完。

路由器_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、空、空。
完。

路由_追加 接受 路由器、方法、模式、处理函数：
    做 追加 用 路由器 的 第 0 项、一组：方法、模式、处理函数、一组：（空）。
完。

路由_GET 接受 路由器、模式、处理函数：
    做 路由_追加 用 路由器、HTTP_获取、模式、处理函数。
完。

路由_POST 接受 路由器、模式、处理函数：
    做 路由_追加 用 路由器、HTTP_提交、模式、处理函数。
完。

路由_PUT 接受 路由器、模式、处理函数：
    做 路由_追加 用 路由器、HTTP_放置、模式、处理函数。
完。

路由_DELETE 接受 路由器、模式、处理函数：
    做 路由_追加 用 路由器、HTTP_删除、模式、处理函数。
完。

路由_中间件 接受 路由器、中间件函数：
    做 追加 用 路由器 的 第 1 项、中间件函数。
完。

路由_404 接受 路由器、处理函数：
    路由器 的 第 2 项 为 处理函数。
完。

路由_500 接受 路由器、处理函数：
    路由器 的 第 3 项 为 处理函数。
完。

路由_匹配 接受 路由器、请求：
    解析结果 为 做 http_解析查询 用 请求 的 第 1 项。
    纯路径 为 解析结果 的 第 0 项。
    请求 的 第 5 项 为 解析结果 的 第 1 项。
    请求段 为 做 http_分割路径 用 纯路径。
    可变 i 为 0。
    只要 i 小于 做 计数 用 路由器 的 第 0 项：
        路由 为 路由器 的 第 0 项 的 第 i 项。
        若 做 字符串等于 用 路由 的 第 0 项、请求 的 第 0 项 或者 做 字符串等于 用 路由 的 第 0 项、「*」：
            模式段 为 做 http_分割路径 用 路由 的 第 1 项。
            匹配结果 为 做 http_匹配段 用 模式段、请求段。
            若 匹配结果 的 第 0 项：
                请求 的 第 6 项 为 匹配结果 的 第 1 项。
                返还 路由。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

路由_处理 接受 路由器、请求：
    可变 m 为 0。
    只要 m 小于 做 计数 用 路由器 的 第 1 项：
        中间件结果 为 做 路由器 的 第 1 项 的 第 m 项 用 请求。
        若 中间件结果 不等于 空：
            返还 中间件结果。
        完。
        m 为 m 加 1。
    完。
    路由 为 做 路由_匹配 用 路由器、请求。
    若 路由 等于 空：
        若 路由器 的 第 2 项 不等于 空：
            返还 做 路由器 的 第 2 项 用 请求。
        完。
        返还 做 http_响应_创建 用 404。
    完。
    m 为 0。
    只要 m 小于 做 计数 用 路由 的 第 3 项：
        mr 为 做 路由 的 第 3 项 的 第 m 项 用 请求。
        若 mr 不等于 空：
            返还 mr。
        完。
        m 为 m 加 1。
    完。
    响应 为 做 路由 的 第 2 项 用 请求。
    若 响应 等于 空：
        若 路由器 的 第 3 项 不等于 空：
            返还 做 路由器 的 第 3 项 用 请求。
        完。
        返还 做 http_响应_创建 用 500。
    完。
    返还 响应。
完。

http_分割路径 接受 路径：
    n 为 做 计数 用 路径。
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 路径、i、1。
        若 做 字符串等于 用 c、「/」：
            若 做 计数 用 当前 大于 0：
                做 追加 用 结果、当前。
                当前 为 「」。
            完。
        否则：
            当前 为 做 字符串拼接 用 当前、c。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前 大于 0：
        做 追加 用 结果、当前。
    完。
    返还 结果。
完。

http_匹配段 接受 模式段、请求段：
    若 做 计数 用 模式段 不等于 做 计数 用 请求段：
        若 做 计数 用 模式段 等于 0 或者 做 字符串等于 用 模式段 的 第 做 计数 用 模式段 减 1 项、「*」 等于 假 或者 做 计数 用 请求段 小于 做 计数 用 模式段 减 1：
            返还 一组：假、做 字典。
        完。
    完。
    参数 为 做 字典。
    可变 i 为 0。
    可变 最大 为 做 计数 用 模式段。
    若 做 计数 用 请求段 小于 最大：
        最大 为 做 计数 用 请求段。
    完。
    只要 i 小于 最大：
        m 为 模式段 的 第 i 项。
        r 为 请求段 的 第 i 项。
        若 做 计数 用 m 大于 0 并且 做 字符串等于 用 做 子串 用 m、0、1、「:」：
            参数名 为 做 子串 用 m、1、做 计数 用 m 减 1。
            做 字典设 用 参数、参数名、r。
        否则若 做 字符串等于 用 m、「*」 等于 假 并且 做 字符串等于 用 m、r 等于 假：
            返还 一组：假、做 字典。
        完。
        i 为 i 加 1。
    完。
    返还 一组：真、参数。
完。

http_数到文 接受 n：
    若 n 等于 0：
        返还 「0」。
    完。
    可变 v 为 n。
    可变 负 为 假。
    若 v 小于 0：
        负 为 真。
        v 为 0 减 v。
    完。
    可变 r 为 「」。
    只要 v 大于 0：
        r 为 做 字符串拼接 用 做 子串 用 「0123456789」、v 取余于 10、1、r。
        v 为 v 除以 10。
    完。
    若 负：
        r 为 做 字符串拼接 用 「-」、r。
    完。
    返还 r。
完。

本章 收束。
