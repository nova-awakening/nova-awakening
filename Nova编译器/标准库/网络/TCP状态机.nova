《TCP状态机》

引用《..·基础》。

引用《.·套接字·套接字》。

TCP_关闭 为 0。

TCP_监听 为 1。

TCP_SYN已发 为 2。

TCP_SYN已收 为 3。

TCP_已建立 为 4。

TCP_FIN等1 为 5。

TCP_FIN等2 为 6。

TCP状态机_TCP_关闭等 为 7。

TCP_最后确认 为 8。

TCP状态机_TCP_时间等 为 9。

tcp_创建会话 接受 零参：
    返还 一组：TCP_关闭、0、0、65535、一组：（空）、一组：（空）。
完。

TCP状_tcp_状态 接受 会话：
    返还 会话 的 第 0 项。
完。

tcp_本地端口 接受 会话：
    返还 会话 的 第 1 项。
完。

tcp_远程端口 接受 会话：
    返还 会话 的 第 2 项。
完。

tcp_序列号 接受 会话：
    返还 会话 的 第 3 项。
完。

tcp_确认号 接受 会话：
    返还 会话 的 第 4 项。
完。

tcp_窗口 接受 会话：
    返还 会话 的 第 5 项。
完。

tcp_状态名 接受 状态：
    若 状态 等于 TCP_关闭：
        返还 「CLOSED」。
    完。
    若 状态 等于 TCP_监听：
        返还 「LISTEN」。
    完。
    若 状态 等于 TCP_SYN已发：
        返还 「SYN_SENT」。
    完。
    若 状态 等于 TCP_SYN已收：
        返还 「SYN_RECEIVED」。
    完。
    若 状态 等于 TCP_已建立：
        返还 「ESTABLISHED」。
    完。
    若 状态 等于 TCP_FIN等1：
        返还 「FIN_WAIT_1」。
    完。
    若 状态 等于 TCP_FIN等2：
        返还 「FIN_WAIT_2」。
    完。
    若 状态 等于 TCP状态机_TCP_关闭等：
        返还 「CLOSE_WAIT」。
    完。
    若 状态 等于 TCP_最后确认：
        返还 「LAST_ACK」。
    完。
    若 状态 等于 TCP状态机_TCP_时间等：
        返还 「TIME_WAIT」。
    完。
    返还 「未知」。
完。

TCP状_tcp_连接 接受 会话、远程端口：
    若 会话 的 第 0 项 不等于 TCP_关闭：
        返还 0。
    完。
    会话 的 第 2 项 为 远程端口。
    会话 的 第 3 项 为 1000。
    会话 的 第 0 项 为 TCP_SYN已发。
    返还 1。
完。

TCP状_tcp_监听 接受 会话、端口：
    若 会话 的 第 0 项 不等于 TCP_关闭：
        返还 0。
    完。
    会话 的 第 1 项 为 端口。
    会话 的 第 0 项 为 TCP_监听。
    返还 1。
完。

tcp_收到SYN 接受 会话：
    若 会话 的 第 0 项 不等于 TCP_监听：
        返还 0。
    完。
    会话 的 第 3 项 为 2000。
    会话 的 第 0 项 为 TCP_SYN已收。
    返还 1。
完。

tcp_收到SYNACK 接受 会话：
    若 会话 的 第 0 项 不等于 TCP_SYN已发：
        返还 0。
    完。
    会话 的 第 4 项 为 会话 的 第 4 项 加 1。
    会话 的 第 0 项 为 TCP_已建立。
    返还 1。
完。

tcp_收到ACK握手 接受 会话：
    若 会话 的 第 0 项 不等于 TCP_SYN已收：
        返还 0。
    完。
    会话 的 第 0 项 为 TCP_已建立。
    返还 1。
完。

TCP状_tcp_发送 接受 会话、数据：
    若 会话 的 第 0 项 不等于 TCP_已建立：
        返还 0。
    完。
    做 追加 用 会话 的 第 6 项、数据。
    会话 的 第 3 项 为 会话 的 第 3 项 加 做 字节长度 用 数据。
    返还 1。
完。

TCP状_tcp_接收 接受 会话、数据：
    若 会话 的 第 0 项 不等于 TCP_已建立：
        返还 0。
    完。
    做 追加 用 会话 的 第 7 项、数据。
    会话 的 第 4 项 为 会话 的 第 4 项 加 做 字节长度 用 数据。
    返还 1。
完。

TCP状_tcp_关闭 接受 会话：
    若 会话 的 第 0 项 等于 TCP_已建立：
        会话 的 第 0 项 为 TCP_FIN等1。
        返还 1。
    完。
    若 会话 的 第 0 项 等于 TCP状态机_TCP_关闭等：
        会话 的 第 0 项 为 TCP_最后确认。
        返还 1。
    完。
    返还 0。
完。

tcp_收到FIN 接受 会话：
    若 会话 的 第 0 项 等于 TCP_已建立：
        会话 的 第 0 项 为 TCP状态机_TCP_关闭等。
        返还 1。
    完。
    若 会话 的 第 0 项 等于 TCP_FIN等1：
        会话 的 第 0 项 为 TCP_关闭。
        返还 1。
    完。
    若 会话 的 第 0 项 等于 TCP_FIN等2：
        会话 的 第 0 项 为 TCP状态机_TCP_时间等。
        返还 1。
    完。
    返还 0。
完。

tcp_收到ACK关闭 接受 会话：
    若 会话 的 第 0 项 等于 TCP_FIN等1：
        会话 的 第 0 项 为 TCP_FIN等2。
        返还 1。
    完。
    若 会话 的 第 0 项 等于 TCP_最后确认：
        会话 的 第 0 项 为 TCP_关闭。
        返还 1。
    完。
    若 会话 的 第 0 项 等于 TCP状态机_TCP_时间等：
        会话 的 第 0 项 为 TCP_关闭。
        返还 1。
    完。
    返还 0。
完。

TCP状_tcp_已连接 接受 会话：
    返还 会话 的 第 0 项 等于 TCP_已建立。
完。

tcp_已关闭 接受 会话：
    返还 会话 的 第 0 项 等于 TCP_关闭。
完。

tcp_窗口_创建 接受 大小：
    返还 一组：大小、0、一组：（空）。
完。

tcp_窗口_可用 接受 窗口：
    返还 窗口 的 第 0 项 减 (窗口 的 第 1 项 减 窗口 的 第 2 项)。
完。

tcp_窗口_发送 接受 窗口、数据大小：
    若 数据大小 大于 做 tcp_窗口_可用 用 窗口：
        返还 假。
    完。
    窗口 的 第 1 项 为 窗口 的 第 1 项 加 数据大小。
    做 追加 用 窗口 的 第 3 项、数据大小。
    返还 真。
完。

tcp_窗口_确认 接受 窗口、确认序号：
    只要 窗口 的 第 2 项 小于 确认序号 并且 做 计数 用 窗口 的 第 3 项 大于 0：
        段大小 为 窗口 的 第 3 项 的 第 0 项。
        窗口 的 第 2 项 为 窗口 的 第 2 项 加 段大小。
        做 删除 用 窗口 的 第 3 项。
    完。
完。

可变 _tcp_超时列表_序号 为 一组：（空）。

可变 _tcp_超时列表_时间 为 一组：（空）。

可变 _tcp_超时列表_已确认 为 一组：（空）。

tcp_重传_注册 接受 序号、发送时间：
    做 追加 用 _tcp_超时列表_序号、序号。
    做 追加 用 _tcp_超时列表_时间、发送时间。
    做 追加 用 _tcp_超时列表_已确认、假。
完。

tcp_重传_确认 接受 序号：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _tcp_超时列表_序号：
        若 _tcp_超时列表_序号 的 第 i 项 小于或等于 序号：
            _tcp_超时列表_已确认 的 第 i 项 为 真。
        完。
        i 为 i 加 1。
    完。
完。

tcp_重传_检查超时 接受 当前时间、超时阈值：
    可变 需重传 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _tcp_超时列表_序号：
        若 非 _tcp_超时列表_已确认 的 第 i 项：
            若 当前时间 减 _tcp_超时列表_时间 的 第 i 项 大于 超时阈值：
                做 追加 用 需重传、_tcp_超时列表_序号 的 第 i 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 需重传。
完。

tcp_拥塞_创建 接受 零参：
    返还 一组：1、65535。
完。

tcp_拥塞_收到ACK 接受 cc：
    cc 的 第 3 项 为 0。
    若 cc 的 第 2 项 等于 0：
        cc 的 第 0 项 为 cc 的 第 0 项 乘 2。
        若 cc 的 第 0 项 大于或等于 cc 的 第 1 项：
            cc 的 第 0 项 为 cc 的 第 1 项。
            cc 的 第 2 项 为 1。
        完。
    否则：
        cc 的 第 0 项 为 cc 的 第 0 项 加 1。
    完。
完。

tcp_拥塞_重复ACK 接受 cc：
    cc 的 第 3 项 为 cc 的 第 3 项 加 1。
    若 cc 的 第 3 项 大于或等于 3：
        cc 的 第 1 项 为 cc 的 第 0 项 除以 2。
        若 cc 的 第 1 项 小于 2：
            cc 的 第 1 项 为 2。
        完。
        cc 的 第 0 项 为 cc 的 第 1 项。
        cc 的 第 2 项 为 1。
        cc 的 第 3 项 为 0。
    完。
完。

tcp_拥塞_超时 接受 cc：
    cc 的 第 1 项 为 cc 的 第 0 项 除以 2。
    若 cc 的 第 1 项 小于 2：
        cc 的 第 1 项 为 2。
    完。
    cc 的 第 0 项 为 1。
    cc 的 第 2 项 为 0。
完。

可变 _tcp_池_会话 为 一组：（空）。

可变 _tcp_池_地址 为 一组：（空）。

tcp_池_获取 接受 地址：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _tcp_池_地址：
        若 _tcp_池_地址 的 第 i 项 等于 地址 并且 做 TCP状_tcp_已连接 用 _tcp_池_会话 的 第 i 项：
            返还 _tcp_池_会话 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    新会话 为 做 tcp_创建会话。
    做 追加 用 _tcp_池_会话、新会话。
    做 追加 用 _tcp_池_地址、地址。
    返还 新会话。
完。

tcp_池_清理 接受 零参：
    可变 新会话 为 一组：（空）。
    可变 新地址 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _tcp_池_会话：
        若 非 做 tcp_已关闭 用 _tcp_池_会话 的 第 i 项：
            做 追加 用 新会话、_tcp_池_会话 的 第 i 项。
            做 追加 用 新地址、_tcp_池_地址 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    _tcp_池_会话 为 新会话。
    _tcp_池_地址 为 新地址。
完。

tcp_池_大小 接受 零参：
    返还 做 计数 用 _tcp_池_会话。
完。

可变 _tcp_连接数 为 0。

可变 _tcp_重传数 为 0。

tcp_记录连接 接受 零参：
    _tcp_连接数 为 _tcp_连接数 加 1。
完。

tcp_记录重传 接受 零参：
    _tcp_重传数 为 _tcp_重传数 加 1。
完。

本章 收束。
