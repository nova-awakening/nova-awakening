《JSON序列化》

引用《..·..·基础》。

引用《..·..·文本·格式化·格式化》。

JSON_对象 为 1。

JSON_数组 为 2。

JSON_字符串 为 3。

JSON_数字 为 4。

JSON_布尔 为 5。

JSON_空 为 6。

json_对象 接受 零参：
    返还 一组：JSON_对象、一组：一组：（空）、一组：（空）。
完。

json_数组 接受 零参：
    返还 一组：JSON_数组、一组：（空）。
完。

JSON序列化_json_字符串 接受 s：
    返还 一组：JSON_字符串、s。
完。

json_数字 接受 n：
    返还 一组：JSON_数字、n。
完。

json_真 接受 零参：
    返还 一组：JSON_布尔、真。
完。

json_假 接受 零参：
    返还 一组：JSON_布尔、假。
完。

JSON_空值 接受 零参：
    返还 一组：JSON_空、空。
完。

json_设置 接受 对象、键、值：
    若 对象 的 第 0 项 不等于 JSON_对象：
        返还 假。
    完。
    键列 为 对象 的 第 1 项 的 第 0 项。
    值列 为 对象 的 第 1 项 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        若 做 字符串等于 用 键列 的 第 i 项、键：
            值列 的 第 i 项 为 值。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 键列、键。
    做 追加 用 值列、值。
    返还 真。
完。

json_获取 接受 对象、键：
    若 对象 的 第 0 项 不等于 JSON_对象：
        返还 空。
    完。
    键列 为 对象 的 第 1 项 的 第 0 项。
    值列 为 对象 的 第 1 项 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        若 做 字符串等于 用 键列 的 第 i 项、键：
            返还 值列 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

json_删除 接受 对象、键：
    若 对象 的 第 0 项 不等于 JSON_对象：
        返还 假。
    完。
    键列 为 对象 的 第 1 项 的 第 0 项。
    值列 为 对象 的 第 1 项 的 第 1 项。
    可变 新键 为 一组：（空）。
    可变 新值 为 一组：（空）。
    可变 找到 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        若 做 字符串等于 用 键列 的 第 i 项、键 等于 假：
            做 追加 用 新键、键列 的 第 i 项。
            做 追加 用 新值、值列 的 第 i 项。
        否则：
            找到 为 真。
        完。
        i 为 i 加 1。
    完。
    对象 的 第 1 项 的 第 0 项 为 新键。
    对象 的 第 1 项 的 第 1 项 为 新值。
    返还 找到。
完。

json_键数 接受 对象：
    若 对象 的 第 0 项 不等于 JSON_对象：
        返还 0。
    完。
    返还 做 计数 用 对象 的 第 1 项 的 第 0 项。
完。

json_包含 接受 对象、键：
    返还 做 json_获取 用 对象、键 不等于 空。
完。

json_追加 接受 数组、值：
    若 数组 的 第 0 项 不等于 JSON_数组：
        返还 假。
    完。
    做 追加 用 数组 的 第 1 项、值。
    返还 真。
完。

json_取 接受 数组、索引：
    若 数组 的 第 0 项 不等于 JSON_数组：
        返还 空。
    完。
    若 索引 小于 0 或者 索引 大于或等于 做 计数 用 数组 的 第 1 项：
        返还 空。
    完。
    返还 数组 的 第 1 项 的 第 索引 项。
完。

json_长度 接受 数组：
    若 数组 的 第 0 项 不等于 JSON_数组：
        返还 0。
    完。
    返还 做 计数 用 数组 的 第 1 项。
完。

json_类型 接受 节点：
    返还 节点 的 第 0 项。
完。

json_值 接受 节点：
    返还 节点 的 第 1 项。
完。

json_是对象 接受 节点：
    返还 节点 的 第 0 项 等于 JSON_对象。
完。

json_是数组 接受 节点：
    返还 节点 的 第 0 项 等于 JSON_数组。
完。

json_是字符串 接受 节点：
    返还 节点 的 第 0 项 等于 JSON_字符串。
完。

json_是数字 接受 节点：
    返还 节点 的 第 0 项 等于 JSON_数字。
完。

json_解析 接受 文本：
    状态 为 一组：文本、0、做 计数 用 文本、空。
    做 json跳空白 用 状态。
    结果 为 做 json解析值 用 状态。
    若 状态 的 第 3 项 不等于 空：
        返还 一组：「错误」、状态 的 第 3 项、状态 的 第 1 项。
    完。
    返还 一组：「成功」、结果。
完。

json解析值 接受 状态：
    做 json跳空白 用 状态。
    若 状态 的 第 1 项 大于或等于 状态 的 第 2 项：
        状态 的 第 3 项 为 「意外结束」。
        返还 空。
    完。
    c 为 做 json当前 用 状态。
    若 c 等于 字符_左花括：
        返还 做 json解析对象 用 状态。
    完。
    若 c 等于 字符_左方括：
        返还 做 json解析数组 用 状态。
    完。
    若 c 等于 字符_双引号：
        返还 做 json解析字符串 用 状态。
    完。
    若 c 等于 116：
        返还 做 json解析true 用 状态。
    完。
    若 c 等于 字符_小写f：
        返还 做 json解析false 用 状态。
    完。
    若 c 等于 110：
        返还 做 json解析null 用 状态。
    完。
    若 c 等于 字符_减号 或者 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
        返还 做 json解析数字 用 状态。
    完。
    状态 的 第 3 项 为 「无效字符」。
    返还 空。
完。

json解析对象 接受 状态：
    做 json消耗 用 状态、123。
    做 json跳空白 用 状态。
    对象 为 做 json_对象。
    若 做 json当前 用 状态 等于 125：
        做 json前进 用 状态。
        返还 对象。
    完。
    只要 状态 的 第 1 项 小于 状态 的 第 2 项 并且 状态 的 第 3 项 等于 空：
        做 json跳空白 用 状态。
        若 做 json当前 用 状态 不等于 34：
            状态 的 第 3 项 为 「期望字符串键」。
            返还 对象。
        完。
        键节点 为 做 json解析字符串 用 状态。
        键 为 键节点 的 第 1 项。
        做 json跳空白 用 状态。
        若 做 json当前 用 状态 不等于 58：
            状态 的 第 3 项 为 「期望冒号」。
            返还 对象。
        完。
        做 json前进 用 状态。
        值 为 做 json解析值 用 状态。
        做 json_设置 用 对象、键、值。
        做 json跳空白 用 状态。
        若 做 json当前 用 状态 等于 125：
            做 json前进 用 状态。
            返还 对象。
        完。
        若 做 json当前 用 状态 等于 44：
            做 json前进 用 状态。
        否则：
            状态 的 第 3 项 为 「期望逗号或右大括号」。
            返还 对象。
        完。
    完。
    返还 对象。
完。

json解析数组 接受 状态：
    做 json消耗 用 状态、91。
    做 json跳空白 用 状态。
    数组 为 做 json_数组。
    若 做 json当前 用 状态 等于 93：
        做 json前进 用 状态。
        返还 数组。
    完。
    只要 状态 的 第 1 项 小于 状态 的 第 2 项 并且 状态 的 第 3 项 等于 空：
        值 为 做 json解析值 用 状态。
        做 json_追加 用 数组、值。
        做 json跳空白 用 状态。
        若 做 json当前 用 状态 等于 93：
            做 json前进 用 状态。
            返还 数组。
        完。
        若 做 json当前 用 状态 等于 44：
            做 json前进 用 状态。
        否则：
            状态 的 第 3 项 为 「期望逗号或右方括号」。
            返还 数组。
        完。
    完。
    返还 数组。
完。

json解析字符串 接受 状态：
    做 json前进 用 状态。
    可变 结果 为 「」。
    只要 状态 的 第 1 项 小于 状态 的 第 2 项：
        c 为 做 json当前 用 状态。
        若 c 等于 字符_双引号：
            做 json前进 用 状态。
            返还 做 JSON序列化_json_字符串 用 结果。
        完。
        若 c 等于 字符_反斜杠：
            做 json前进 用 状态。
            esc 为 做 json当前 用 状态。
            若 esc 等于 34：
                结果 为 做 字符串拼接 用 结果、「\"」。
            否则若 esc 等于 92：
                结果 为 做 字符串拼接 用 结果、「\\」。
            否则若 esc 等于 47：
                结果 为 做 字符串拼接 用 结果、「/」。
            否则若 esc 等于 110：
                结果 为 做 字符串拼接 用 结果、「\n」。
            否则若 esc 等于 116：
                结果 为 做 字符串拼接 用 结果、「\t」。
            否则若 esc 等于 114：
                结果 为 做 字符串拼接 用 结果、「\r」。
            否则若 esc 等于 117：
                可变 cp 为 0。
                可变 hi 为 0。
                只要 hi 小于 4：
                    做 json前进 用 状态。
                    hc 为 做 编码 用 做 子串 用 状态 的 第 0 项、状态 的 第 1 项、1。
                    若 hc 大于或等于 48 并且 hc 小于或等于 57：
                        cp 为 cp 乘 16 加 hc 减 48。
                    否则若 hc 大于或等于 65 并且 hc 小于或等于 70：
                        cp 为 cp 乘 16 加 hc 减 55。
                    否则若 hc 大于或等于 97 并且 hc 小于或等于 102：
                        cp 为 cp 乘 16 加 hc 减 87。
                    完。
                    hi 为 hi 加 1。
                完。
                若 cp 小于 0x80：
                    结果 为 做 字符串拼接 用 结果、做 字符 用 cp。
                否则若 cp 小于 0x800：
                    结果 为 做 字符串拼接 用 结果、做 字符串拼接 用 做 字符 用 0xC0 加 cp 除以 64、做 字符 用 0x80 加 cp 取余于 64。
                否则：
                    结果 为 做 字符串拼接 用 结果、做 字符串拼接 用 做 字符 用 0xE0 加 cp 除以 4096、做 字符串拼接 用 做 字符 用 0x80 加 cp 除以 64 取余于 64、做 字符 用 0x80 加 cp 取余于 64。
                完。
            否则：
                结果 为 做 字符串拼接 用 结果、做 子串 用 状态 的 第 0 项、状态 的 第 1 项、1。
            完。
            做 json前进 用 状态。
        否则：
            结果 为 做 字符串拼接 用 结果、做 子串 用 状态 的 第 0 项、状态 的 第 1 项、1。
            做 json前进 用 状态。
        完。
    完。
    状态 的 第 3 项 为 「未终止的字符串」。
    返还 做 JSON序列化_json_字符串 用 结果。
完。

json解析数字 接受 状态：
    可变 负 为 假。
    若 做 json当前 用 状态 等于 45：
        负 为 真。
        做 json前进 用 状态。
    完。
    可变 整数部分 为 0。
    可变 _整完 为 假。
    只要 状态 的 第 1 项 小于 状态 的 第 2 项 并且 _整完 等于 假：
        c 为 做 json当前 用 状态。
        若 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
            整数部分 为 整数部分 乘 10 加 (c 减 48)。
            做 json前进 用 状态。
        否则：
            _整完 为 真。
        完。
    完。
    可变 小数 为 0。
    可变 小数位 为 0。
    若 状态 的 第 1 项 小于 状态 的 第 2 项 并且 做 json当前 用 状态 等于 46：
        做 json前进 用 状态。
        可变 因子 为 100。
        可变 _小完 为 假。
        只要 状态 的 第 1 项 小于 状态 的 第 2 项 并且 小数位 小于 3 并且 _小完 等于 假：
            c2 为 做 json当前 用 状态。
            若 c2 大于或等于 48 并且 c2 小于或等于 57：
                小数 为 小数 加 (c2 减 48) 乘 因子。
                因子 为 因子 除以 10。
                小数位 为 小数位 加 1。
                做 json前进 用 状态。
            否则：
                _小完 为 真。
            完。
        完。
        可变 _剩完 为 假。
        只要 状态 的 第 1 项 小于 状态 的 第 2 项 并且 _剩完 等于 假：
            c3 为 做 json当前 用 状态。
            若 c3 大于或等于 48 并且 c3 小于或等于 57：
                做 json前进 用 状态。
            否则：
                _剩完 为 真。
            完。
        完。
    完。
    若 状态 的 第 1 项 小于 状态 的 第 2 项：
        ce 为 做 json当前 用 状态。
        若 ce 等于 101 或者 ce 等于 69：
            做 json前进 用 状态。
            若 状态 的 第 1 项 小于 状态 的 第 2 项 并且 (做 json当前 用 状态 等于 43 或者 做 json当前 用 状态 等于 45)：
                做 json前进 用 状态。
            完。
            可变 _科完 为 假。
            只要 状态 的 第 1 项 小于 状态 的 第 2 项 并且 _科完 等于 假：
                c4 为 做 json当前 用 状态。
                若 c4 大于或等于 48 并且 c4 小于或等于 57：
                    做 json前进 用 状态。
                否则：
                    _科完 为 真。
                完。
            完。
        完。
    完。
    可变 值 为 整数部分 乘 1000 加 小数。
    若 负：
        值 为 0 减 值。
    完。
    返还 做 json_数字 用 值。
完。

json解析true 接受 状态：
    做 json前进 用 状态。
    做 json前进 用 状态。
    做 json前进 用 状态。
    做 json前进 用 状态。
    返还 做 json_真。
完。

json解析false 接受 状态：
    做 json前进 用 状态。
    做 json前进 用 状态。
    做 json前进 用 状态。
    做 json前进 用 状态。
    做 json前进 用 状态。
    返还 做 json_假。
完。

json解析null 接受 状态：
    做 json前进 用 状态。
    做 json前进 用 状态。
    做 json前进 用 状态。
    做 json前进 用 状态。
    返还 做 JSON_空值。
完。

json_生成 接受 节点：
    返还 做 json生成值 用 节点、0、假。
完。

json_美化 接受 节点：
    返还 做 json生成值 用 节点、0、真。
完。

json生成值 接受 节点、深度、美化：
    若 节点 等于 空：
        返还 「空」。
    完。
    类型 为 节点 的 第 0 项。
    若 类型 等于 JSON_空：
        返还 「空」。
    完。
    若 类型 等于 JSON_布尔：
        若 节点 的 第 1 项：
            返还 「真」。
        完。
        返还 「假」。
    完。
    若 类型 等于 JSON_数字：
        v 为 节点 的 第 1 项。
        整 为 v 除以 1000。
        可变 小 为 v 取余于 1000。
        若 小 小于 0：
            小 为 0 减 小。
        完。
        若 小 等于 0：
            返还 做 数到文 用 整。
        完。
        可变 小文 为 做 数到文 用 小。
        只要 做 计数 用 小文 小于 3：
            小文 为 做 字符串拼接 用 「0」、小文。
        完。
        可变 _去0完 为 假。
        只要 做 计数 用 小文 大于 1 并且 _去0完 等于 假：
            若 做 子串 用 小文、做 计数 用 小文 减 1、1 等于 「0」：
                小文 为 做 子串 用 小文、0、做 计数 用 小文 减 1。
            否则：
                _去0完 为 真。
            完。
        完。
        返还 做 字符串拼接 用 做 数到文 用 整、做 字符串拼接 用 「.」、小文。
    完。
    若 类型 等于 JSON_字符串：
        返还 做 字符串拼接 用 「\"」、做 字符串拼接 用 做 json转义 用 节点 的 第 1 项、「\"」。
    完。
    若 类型 等于 JSON_数组：
        元素 为 节点 的 第 1 项。
        n 为 做 计数 用 元素。
        若 n 等于 0：
            返还 「[]」。
        完。
        可变 结果 为 「[」。
        若 美化：
            结果 为 做 字符串拼接 用 结果、「\n」。
        完。
        可变 i 为 0。
        只要 i 小于 n：
            若 美化：
                结果 为 做 字符串拼接 用 结果、做 json缩进 用 深度 加 1。
            完。
            结果 为 做 字符串拼接 用 结果、做 json生成值 用 元素 的 第 i 项、深度 加 1、美化。
            若 i 小于 n 减 1：
                结果 为 做 字符串拼接 用 结果、「,」。
            完。
            若 美化：
                结果 为 做 字符串拼接 用 结果、「\n」。
            完。
            i 为 i 加 1。
        完。
        若 美化：
            结果 为 做 字符串拼接 用 结果、做 json缩进 用 深度。
        完。
        返还 做 字符串拼接 用 结果、「]」。
    完。
    若 类型 等于 JSON_对象：
        键列 为 节点 的 第 1 项 的 第 0 项。
        值列 为 节点 的 第 1 项 的 第 1 项。
        n2 为 做 计数 用 键列。
        若 n2 等于 0：
            返还 「{}」。
        完。
        可变 结果2 为 「{」。
        若 美化：
            结果2 为 做 字符串拼接 用 结果2、「\n」。
        完。
        可变 j 为 0。
        只要 j 小于 n2：
            若 美化：
                结果2 为 做 字符串拼接 用 结果2、做 json缩进 用 深度 加 1。
            完。
            结果2 为 做 字符串拼接 用 结果2、「\"」。
            结果2 为 做 字符串拼接 用 结果2、做 json转义 用 键列 的 第 j 项。
            结果2 为 做 字符串拼接 用 结果2、「\":」。
            若 美化：
                结果2 为 做 字符串拼接 用 结果2、「 」。
            完。
            结果2 为 做 字符串拼接 用 结果2、做 json生成值 用 值列 的 第 j 项、深度 加 1、美化。
            若 j 小于 n2 减 1：
                结果2 为 做 字符串拼接 用 结果2、「, 」。
            完。
            若 美化：
                结果2 为 做 字符串拼接 用 结果2、「\n」。
            完。
            j 为 j 加 1。
        完。
        若 美化：
            结果2 为 做 字符串拼接 用 结果2、做 json缩进 用 深度。
        完。
        返还 做 字符串拼接 用 结果2、「}」。
    完。
    返还 「空」。
完。

json_查询 接受 根、路径：
    可变 当前 为 根。
    n 为 做 计数 用 路径。
    可变 i 为 0。
    可变 段 为 「」。
    只要 i 小于或等于 n：
        可变 c 为 0。
        若 i 小于 n：
            c 为 做 编码 用 做 子串 用 路径、i、1。
        完。
        若 i 等于 n 或者 c 等于 字符_句点：
            若 做 计数 用 段 大于 0：
                若 做 计数 用 段 大于 2 并且 做 字符串等于 用 做 子串 用 段、做 计数 用 段 减 1、1、「]」：
                    可变 括号位 为 0。
                    可变 _搜索中 为 真。
                    只要 括号位 小于 做 计数 用 段 并且 _搜索中：
                        若 做 字符串等于 用 做 子串 用 段、括号位、1、「[」：
                            _搜索中 为 假。
                        否则：
                            括号位 为 括号位 加 1。
                        完。
                    完。
                    若 括号位 大于 0：
                        键名 为 做 子串 用 段、0、括号位。
                        当前 为 做 json_获取 用 当前、键名。
                        若 当前 等于 空：
                            返还 空。
                        完。
                    完。
                    索引文本 为 做 子串 用 段、括号位 加 1、做 计数 用 段 减 括号位 减 2。
                    索引 为 做 文到数 用 索引文本。
                    当前 为 做 json_取 用 当前、索引。
                否则：
                    当前 为 做 json_获取 用 当前、段。
                完。
                若 当前 等于 空：
                    返还 空。
                完。
            完。
            段 为 「」。
        否则：
            段 为 做 字符串拼接 用 段、做 子串 用 路径、i、1。
        完。
        i 为 i 加 1。
    完。
    返还 当前。
完。

json_克隆 接受 节点：
    若 节点 等于 空：
        返还 空。
    完。
    类型 为 节点 的 第 0 项。
    若 类型 等于 JSON_空 或者 类型 等于 JSON_布尔 或者 类型 等于 JSON_数字 或者 类型 等于 JSON_字符串：
        返还 一组：类型、节点 的 第 1 项。
    完。
    若 类型 等于 JSON_数组：
        新数组 为 做 json_数组。
        可变 i 为 0。
        只要 i 小于 做 计数 用 节点 的 第 1 项：
            做 json_追加 用 新数组、做 json_克隆 用 节点 的 第 1 项 的 第 i 项。
            i 为 i 加 1。
        完。
        返还 新数组。
    完。
    若 类型 等于 JSON_对象：
        新对象 为 做 json_对象。
        键列 为 节点 的 第 1 项 的 第 0 项。
        值列 为 节点 的 第 1 项 的 第 1 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 键列：
            做 json_设置 用 新对象、键列 的 第 j 项、做 json_克隆 用 值列 的 第 j 项。
            j 为 j 加 1。
        完。
        返还 新对象。
    完。
    返还 空。
完。

json当前 接受 状态：
    若 状态 的 第 1 项 大于或等于 状态 的 第 2 项：
        返还 0。
    完。
    返还 做 编码 用 做 子串 用 状态 的 第 0 项、状态 的 第 1 项、1。
完。

json前进 接受 状态：
    状态 的 第 1 项 为 状态 的 第 1 项 加 1。
完。

json消耗 接受 状态、期望：
    若 做 json当前 用 状态 等于 期望：
        做 json前进 用 状态。
    否则：
        状态 的 第 3 项 为 「意外字符」。
    完。
完。

json跳空白 接受 状态：
    只要 状态 的 第 1 项 小于 状态 的 第 2 项：
        c 为 做 json当前 用 状态。
        若 c 等于 字符_空格 或者 c 等于 字符_制表 或者 c 等于 字符_换行 或者 c 等于 字符_回车：
            做 json前进 用 状态。
        否则：
            返还。
        完。
    完。
完。

json转义 接受 s：
    n 为 做 计数 用 s。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 s、i、1。
        若 做 字符串等于 用 c、「\"」：
            结果 为 做 字符串拼接 用 结果、「\\\"」。
        否则若 做 字符串等于 用 c、「\\」：
            结果 为 做 字符串拼接 用 结果、「\\\\」。
        否则若 做 字符串等于 用 c、「\n」：
            结果 为 做 字符串拼接 用 结果、「\\n」。
        否则若 做 字符串等于 用 c、「\t」：
            结果 为 做 字符串拼接 用 结果、「\\t」。
        否则：
            结果 为 做 字符串拼接 用 结果、c。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

json缩进 接受 深度：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 深度：
        结果 为 做 字符串拼接 用 结果、「 」。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数到文 接受 n：
    若 n 等于 0：
        返还 「0」。
    完。
    可变 负 为 假。
    可变 v 为 n。
    若 v 小于 0：
        负 为 真。
        v 为 0 减 v。
    完。
    可变 结果 为 「」。
    只要 v 大于 0：
        d 为 v 取余于 10。
        结果 为 做 字符串拼接 用 做 子串 用 「0123456789」、d、1、结果。
        v 为 v 除以 10。
    完。
    若 负：
        结果 为 做 字符串拼接 用 「-」、结果。
    完。
    返还 结果。
完。

文到数 接受 s：
    n 为 做 计数 用 s。
    可变 结果 为 0。
    可变 负 为 假。
    可变 i 为 0。
    若 n 大于 0 并且 做 字符串等于 用 做 子串 用 s、0、1、「-」：
        负 为 真。
        i 为 1。
    完。
    只要 i 小于 n：
        c 为 做 编码 用 做 子串 用 s、i、1。
        若 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
            结果 为 结果 乘 10 加 (c 减 48)。
        完。
        i 为 i 加 1。
    完。
    若 负：
        结果 为 0 减 结果。
    完。
    返还 结果。
完。

本章 收束。
