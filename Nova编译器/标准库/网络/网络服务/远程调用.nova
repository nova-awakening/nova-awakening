《远程调用》

引用《..·..·基础》。

RPC_请求 为 1。

RPC_响应 为 2。

RPC_通知 为 3。

RPC_错误 为 4。

RPC_批量 为 5。

rpc_请求 接受 id、方法、参数：
    返还 一组：RPC_请求、id、方法、参数、30000。
完。

rpc_响应 接受 id、结果：
    返还 一组：RPC_响应、id、结果、空、0。
完。

rpc_错误响应 接受 id、错误码、错误信息：
    返还 一组：RPC_错误、id、空、一组：错误码、错误信息、0。
完。

rpc_通知 接受 方法、参数：
    返还 一组：RPC_通知、0、方法、参数、0。
完。

rpc服务_创建 接受 名称、版本：
    返还 一组：名称、做 字典、一组：（空）、一组：0、版本。
完。

rpc服务_注册 接受 服务、方法名、处理函数、描述：
    做 字典设 用 服务 的 第 1 项、方法名、一组：方法名、处理函数、空、描述。
完。

rpc服务_注册带验证 接受 服务、方法名、处理函数、验证函数、描述：
    做 字典设 用 服务 的 第 1 项、方法名、一组：方法名、处理函数、验证函数、描述。
完。

rpc服务_中间件 接受 服务、中间件函数：
    做 追加 用 服务 的 第 2 项、中间件函数。
完。

rpc服务_处理 接受 服务、请求：
    服务 的 第 3 项 的 第 0 项 为 服务 的 第 3 项 的 第 0 项 加 1。
    若 请求 的 第 0 项 等于 RPC_批量：
        返还 做 rpc服务_批量处理 用 服务、请求 的 第 3 项。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 服务 的 第 2 项：
        中间件结果 为 做 服务 的 第 2 项 的 第 i 项 用 请求。
        若 中间件结果 不等于 空：
            返还 中间件结果。
        完。
        i 为 i 加 1。
    完。
    方法 为 做 字典取 用 服务 的 第 1 项、请求 的 第 2 项。
    若 方法 等于 空：
        服务 的 第 3 项 的 第 2 项 为 服务 的 第 3 项 的 第 2 项 加 1。
        返还 做 rpc_错误响应 用 请求 的 第 1 项、负 32601、「方法不存在」。
    完。
    若 方法 的 第 2 项 不等于 空：
        验证结果 为 做 方法 的 第 2 项 用 请求 的 第 3 项。
        若 验证结果 不等于 真：
            服务 的 第 3 项 的 第 2 项 为 服务 的 第 3 项 的 第 2 项 加 1。
            返还 做 rpc_错误响应 用 请求 的 第 1 项、负 32602、「参数无效」。
        完。
    完。
    结果 为 做 方法 的 第 1 项 用 请求 的 第 3 项。
    若 结果 等于 空：
        服务 的 第 3 项 的 第 2 项 为 服务 的 第 3 项 的 第 2 项 加 1。
        返还 做 rpc_错误响应 用 请求 的 第 1 项、负 32000、「处理函数返回空」。
    完。
    服务 的 第 3 项 的 第 1 项 为 服务 的 第 3 项 的 第 1 项 加 1。
    返还 做 rpc_响应 用 请求 的 第 1 项、结果。
完。

rpc服务_批量处理 接受 服务、请求列表：
    可变 响应列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 请求列表：
        做 追加 用 响应列表、做 rpc服务_处理 用 服务、请求列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 一组：RPC_批量、0、响应列表、空、0。
完。

rpc服务_方法列表 接受 _服务：
    可变 列表 为 一组：（空）。
    返还 列表。
完。

rpc客户端_创建 接受 超时、最大重试：
    可变 实际超时 为 超时。
    可变 实际重试 为 最大重试。
    若 实际超时 小于或等于 0：
        实际超时 为 30000。
    完。
    若 实际重试 小于或等于 0：
        实际重试 为 3。
    完。
    返还 一组：1、做 字典、实际超时、0、实际重试、空。
完。

rpc客户端_调用 接受 客户端、方法、参数：
    请求ID 为 客户端 的 第 0 项。
    客户端 的 第 0 项 为 客户端 的 第 0 项 加 1。
    请求 为 做 rpc_请求 用 请求ID、方法、参数。
    请求 的 第 4 项 为 客户端 的 第 2 项。
    做 字典设 用 客户端 的 第 1 项、请求ID、一组：请求、0、空。
    返还 请求。
完。

rpc客户端_批量调用 接受 客户端、调用列表：
    可变 请求列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 调用列表：
        请求 为 做 rpc客户端_调用 用 客户端、调用列表 的 第 i 项 的 第 0 项、调用列表 的 第 i 项 的 第 1 项。
        做 追加 用 请求列表、请求。
        i 为 i 加 1。
    完。
    返还 一组：RPC_批量、0、「」、请求列表、0。
完。

rpc客户端_接收 接受 客户端、响应：
    请求ID 为 响应 的 第 1 项。
    待处理 为 做 字典取 用 客户端 的 第 1 项、请求ID。
    若 待处理 不等于 空：
        待处理 的 第 2 项 为 响应。
        做 字典设 用 客户端 的 第 1 项、请求ID、空。
    完。
    返还 响应。
完。

ws帧_创建 接受 操作码、数据：
    返还 一组：操作码、数据、真、0。
完。

ws帧_文本 接受 文本：
    返还 做 ws帧_创建 用 WS_文本、文本。
完。

ws帧_关闭 接受 零参：
    返还 做 ws帧_创建 用 WS_关闭、「」。
完。

ws帧_ping 接受 数据：
    返还 做 ws帧_创建 用 WS_心跳请求、数据。
完。

ws帧_pong 接受 数据：
    返还 做 ws帧_创建 用 WS_心跳响应、数据。
完。

ws连接_创建 接受 id：
    返还 一组：id、0、一组：（空）、一组：（空）、空、空、空、做 字典。
完。

ws连接_打开 接受 连接：
    连接 的 第 1 项 为 1。
完。

ws连接_发送 接受 连接、帧：
    若 连接 的 第 1 项 不等于 1：
        返还 假。
    完。
    做 追加 用 连接 的 第 2 项、帧。
    返还 真。
完。

ws连接_发送文本 接受 连接、文本：
    返还 做 ws连接_发送 用 连接、做 ws帧_文本 用 文本。
完。

ws连接_接收 接受 连接：
    可变 新队列 为 一组：（空）。
    若 做 计数 用 连接 的 第 3 项 等于 0：
        返还 空。
    完。
    帧 为 连接 的 第 3 项 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 连接 的 第 3 项：
        做 追加 用 新队列、连接 的 第 3 项 的 第 i 项。
        i 为 i 加 1。
    完。
    连接 的 第 3 项 为 新队列。
    若 帧 的 第 0 项 等于 WS_心跳请求：
        做 ws连接_发送 用 连接、做 ws帧_pong 用 帧 的 第 1 项。
        返还 做 ws连接_接收 用 连接。
    完。
    若 帧 的 第 0 项 等于 WS_关闭：
        连接 的 第 1 项 为 3。
        若 连接 的 第 5 项 不等于 空：
            做 连接 的 第 5 项。
        完。
        返还 帧。
    完。
    若 连接 的 第 4 项 不等于 空：
        做 连接 的 第 4 项 用 帧。
    完。
    返还 帧。
完。

ws连接_注入 接受 连接、帧：
    做 追加 用 连接 的 第 3 项、帧。
完。

ws连接_关闭 接受 连接：
    做 ws连接_发送 用 连接、做 ws帧_关闭。
    连接 的 第 1 项 为 2。
完。

ws连接_设消息处理 接受 连接、处理函数：
    连接 的 第 4 项 为 处理函数。
完。

ws连接_设关闭处理 接受 连接、处理函数：
    连接 的 第 5 项 为 处理函数。
完。

ws连接_设错误处理 接受 连接、处理函数：
    连接 的 第 6 项 为 处理函数。
完。

ws连接_已打开 接受 连接：
    返还 连接 的 第 1 项 等于 1。
完。

ws连接_已关闭 接受 连接：
    返还 连接 的 第 1 项 大于或等于 3。
完。

ws服务器_创建 接受 最大连接：
    可变 实际最大 为 最大连接。
    若 实际最大 小于或等于 0：
        实际最大 为 1024。
    完。
    返还 一组：一组：（空）、1、空、实际最大、做 字典。
完。

ws服务器_设连接处理 接受 服务器、处理函数：
    服务器 的 第 2 项 为 处理函数。
完。

ws服务器_接受 接受 服务器：
    若 做 计数 用 服务器 的 第 0 项 大于或等于 服务器 的 第 3 项：
        返还 空。
    完。
    连接 为 做 ws连接_创建 用 服务器 的 第 1 项。
    服务器 的 第 1 项 为 服务器 的 第 1 项 加 1。
    做 ws连接_打开 用 连接。
    做 追加 用 服务器 的 第 0 项、连接。
    若 服务器 的 第 2 项 不等于 空：
        做 服务器 的 第 2 项 用 连接。
    完。
    返还 连接。
完。

ws服务器_广播 接受 服务器、文本：
    可变 发送数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 服务器 的 第 0 项：
        若 做 ws连接_已打开 用 服务器 的 第 0 项 的 第 i 项：
            做 ws连接_发送文本 用 服务器 的 第 0 项 的 第 i 项、文本。
            发送数 为 发送数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 发送数。
完。

ws服务器_加入房间 接受 服务器、连接ID、房间名：
    可变 房间 为 做 字典取 用 服务器 的 第 4 项、房间名。
    若 房间 等于 空：
        房间 为 一组：（空）。
        做 字典设 用 服务器 的 第 4 项、房间名、房间。
    完。
    做 追加 用 房间、连接ID。
完。

ws服务器_房间广播 接受 服务器、房间名、文本：
    房间 为 做 字典取 用 服务器 的 第 4 项、房间名。
    若 房间 等于 空：
        返还 0。
    完。
    可变 发送数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 房间：
        连接ID 为 房间 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 服务器 的 第 0 项：
            若 服务器 的 第 0 项 的 第 j 项 的 第 0 项 等于 连接ID 并且 做 ws连接_已打开 用 服务器 的 第 0 项 的 第 j 项：
                做 ws连接_发送文本 用 服务器 的 第 0 项 的 第 j 项、文本。
                发送数 为 发送数 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 发送数。
完。

ws服务器_连接数 接受 服务器：
    可变 活跃 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 服务器 的 第 0 项：
        若 做 ws连接_已打开 用 服务器 的 第 0 项 的 第 i 项：
            活跃 为 活跃 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 活跃。
完。

本章 收束。
