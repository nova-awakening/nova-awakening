《网络》

引用《..·基础》。

引用《..·字符串·字符串》。

引用《..·..·运行时·字典引擎·字典引擎》。

用。

「../基础.nova」。

用。

「../字符串/字符串.nova」。

URL_解析 接受 url：
    可变 协议 为 0。
    可变 主机 为 「」。
    可变 端口 为 0。
    可变 路径 为 「/」。
    可变 查询 为 「」。
    可变 片段 为 「」。
    可变 位置 为 0。
    cpos 为 做 字符串_查找 用 url、「://」。
    若 cpos 大于或等于 0：
        协议 为 做 子串 用 url、0、cpos。
        位置 为 cpos 加 3。
    完。
    fpos 为 做 字符串_查找 用 做 子串 用 url、位置、做 计数 用 url 减 位置、「#」。
    可变 无片段 为 做 子串 用 url、位置、做 计数 用 url 减 位置。
    若 fpos 大于或等于 0：
        片段 为 做 子串 用 无片段、fpos 加 1、做 计数 用 无片段 减 fpos 减 1。
        无片段 为 做 子串 用 无片段、0、fpos。
    完。
    qpos 为 做 字符串_查找 用 无片段、「?」。
    可变 无查询 为 无片段。
    若 qpos 大于或等于 0：
        查询 为 做 子串 用 无片段、qpos 加 1、做 计数 用 无片段 减 qpos 减 1。
        无查询 为 做 子串 用 无片段、0、qpos。
    完。
    ppos 为 做 字符串_查找 用 无查询、「/」。
    若 ppos 大于或等于 0：
        路径 为 做 子串 用 无查询、ppos、做 计数 用 无查询 减 ppos。
        无查询 为 做 子串 用 无查询、0、ppos。
    完。
    colon 为 做 字符串_查找 用 无查询、「:」。
    若 colon 大于或等于 0：
        主机 为 做 子串 用 无查询、0、colon。
        port_str 为 做 子串 用 无查询、colon 加 1、做 计数 用 无查询 减 colon 减 1。
        可变 p 为 0。
        可变 k 为 0。
        只要 k 小于 做 计数 用 port_str：
            d 为 做 字符码 用 port_str、k 减 48。
            若 d 大于或等于 0 并且 d 小于或等于 字符_制表：
                p 为 p 乘 10 加 d。
            完。
            k 为 k 加 1。
        完。
        端口 为 p。
    否则：
        主机 为 无查询。
        若 做 字符串相等 用 协议、「https」：
            端口 为 443。
        否则若 做 字符串相等 用 协议、「http」：
            端口 为 80。
        完。
    完。
    返还 一组：协议、主机、端口、路径、查询、片段。
完。

查询_解析 接受 qs：
    可变 结果 为 一组：（空）。
    可变 起始 为 0。
    可变 i 为 0。
    只要 i 小于或等于 做 计数 用 qs：
        若 i 等于 做 计数 用 qs 或者 做 字符码 用 qs、i 等于 38：
            对 为 做 子串 用 qs、起始、i 减 起始。
            eq 为 做 字符串_查找 用 对、「=」。
            若 eq 大于或等于 0：
                做 追加 用 结果、一组：做 子串 用 对、0、eq、做 子串 用 对、eq 加 1、做 计数 用 对 减 eq 减 1。
            完。
            起始 为 i 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

查询_构建 接受 参数列表：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数列表：
        若 i 不等于 0：
            结果 为 结果 加 「&」。
        完。
        结果 为 结果 加 参数列表 的 第 i 项 的 第 0 项 加 「=」 加 参数列表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

HTTP_请求 接受 方法、路径、主机、头部列表：
    可变 req 为 方法 加 「 」 加 路径 加 「 HTTP/1.1\r\n」。
    req 为 req 加 「Host: 」 加 主机 加 「\r\n」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 头部列表：
        req 为 req 加 头部列表 的 第 i 项 的 第 0 项 加 「: 」 加 头部列表 的 第 i 项 的 第 1 项 加 「\r\n」。
        i 为 i 加 1。
    完。
    req 为 req 加 「\r\n」。
    返还 req。
完。

HTTP_解析状态行 接受 行：
    可变 部分 为 一组：（空）。
    可变 起始 为 0。
    可变 计数值 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行 并且 计数值 小于 2：
        若 做 字符码 用 行、i 等于 32：
            做 追加 用 部分、做 子串 用 行、起始、i 减 起始。
            起始 为 i 加 1。
            计数值 为 计数值 加 1。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 部分、做 子串 用 行、起始、做 计数 用 行 减 起始。
    若 做 计数 用 部分 小于 3：
        返还 一组：「」、0、「」。
    完。
    可变 code 为 0。
    cs 为 部分 的 第 1 项。
    可变 j 为 0。
    只要 j 小于 做 计数 用 cs：
        d 为 做 字符码 用 cs、j 减 48。
        若 d 大于或等于 0 并且 d 小于或等于 字符_制表：
            code 为 code 乘 10 加 d。
        完。
        j 为 j 加 1。
    完。
    返还 一组：部分 的 第 0 项、code、部分 的 第 2 项。
完。

B64_表 为 「ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/」。

Base64_编码字节 接受 字节列表：
    可变 结果 为 「」。
    n 为 做 计数 用 字节列表。
    可变 i 为 0。
    只要 i 小于 n：
        b0 为 字节列表 的 第 i 项。
        可变 b1 为 0。
        可变 b2 为 0。
        若 i 加 1 小于 n：
            b1 为 字节列表 的 第 i 加 1 项。
        完。
        若 i 加 2 小于 n：
            b2 为 字节列表 的 第 i 加 2 项。
        完。
        结果 为 结果 加 做 子串 用 B64_表、b0 右移 2 位与 63、1。
        结果 为 结果 加 做 子串 用 B64_表、(b0 位与 3) 左移 4 位或 b1 右移 4 位与 15、1。
        若 i 加 1 小于 n：
            结果 为 结果 加 做 子串 用 B64_表、(b1 位与 15) 左移 2 位或 b2 右移 6 位与 3、1。
        否则：
            结果 为 结果 加 「=」。
        完。
        若 i 加 2 小于 n：
            结果 为 结果 加 做 子串 用 B64_表、b2 位与 63、1。
        否则：
            结果 为 结果 加 「=」。
        完。
        i 为 i 加 3。
    完。
    返还 结果。
完。

Base64_编码 接受 文本：
    可变 字节 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        做 追加 用 字节、做 编码 用 做 子串 用 文本、i、1。
        i 为 i 加 1。
    完。
    返还 做 Base64_编码字节 用 字节。
完。

B64_查表 接受 码：
    可变 i 为 0。
    只要 i 小于 64：
        若 做 编码 用 做 子串 用 B64_表、i、1 等于 码：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

Base64_解码 接受 b64：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 3 小于 做 计数 用 b64：
        v0 为 做 B64_查表 用 做 字符码 用 b64、i。
        v1 为 做 B64_查表 用 做 字符码 用 b64、i 加 1。
        v2 为 做 B64_查表 用 做 字符码 用 b64、i 加 2。
        v3 为 做 B64_查表 用 做 字符码 用 b64、i 加 3。
        若 v0 大于或等于 0 并且 v1 大于或等于 0：
            做 追加 用 结果、v0 左移 2 位或 v1 右移 4 位与 3。
        完。
        若 v2 大于或等于 0：
            做 追加 用 结果、(v1 位与 15) 左移 4 位或 v2 右移 2 位与 15。
        完。
        若 v3 大于或等于 0：
            做 追加 用 结果、(v2 位与 3) 左移 6 位或 v3。
        完。
        i 为 i 加 4。
    完。
    返还 结果。
完。

Base64_解码文本 接受 b64：
    字节 为 做 Base64_解码 用 b64。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字节：
        结果 为 结果 加 做 字符 用 字节 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

HEX_字符 为 「0123456789abcdef」。

Hex_编码 接受 字节列表：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字节列表：
        b 为 字节列表 的 第 i 项。
        结果 为 结果 加 做 子串 用 HEX_字符、b 右移 4 位与 15、1。
        结果 为 结果 加 做 子串 用 HEX_字符、b 位与 15、1。
        i 为 i 加 1。
    完。
    返还 结果。
完。

Hex_解码值 接受 码：
    若 码 大于或等于 字符_零 并且 码 小于或等于 字符_九：
        返还 码 减 48。
    完。
    若 码 大于或等于 字符_小写a 并且 码 小于或等于 字符_小写f：
        返还 码 减 87。
    完。
    若 码 大于或等于 字符_大写A 并且 码 小于或等于 70：
        返还 码 减 55。
    完。
    返还 负 1。
完。

Hex_解码 接受 hex：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 1 小于 做 计数 用 hex：
        h 为 做 Hex_解码值 用 做 字符码 用 hex、i。
        l 为 做 Hex_解码值 用 做 字符码 用 hex、i 加 1。
        若 h 大于或等于 0 并且 l 大于或等于 0：
            做 追加 用 结果、h 乘 16 加 l。
        完。
        i 为 i 加 2。
    完。
    返还 结果。
完。

IP_解析 接受 ip_str：
    可变 octets 为 一组：（空）。
    可变 当前 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 ip_str：
        码 为 做 字符码 用 ip_str、i。
        若 码 等于 字符_句点：
            做 追加 用 octets、当前。
            当前 为 0。
        否则若 码 大于或等于 字符_零 并且 码 小于或等于 字符_九：
            当前 为 当前 乘 10 加 (码 减 48)。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 octets、当前。
    返还 octets。
完。

IP_构建 接受 octets：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 octets：
        若 i 不等于 0：
            结果 为 结果 加 「.」。
        完。
        结果 为 结果 加 做 转字符串 用 octets 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

IP_是有效 接受 octets：
    若 做 计数 用 octets 不等于 4：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 4：
        若 octets 的 第 i 项 小于 0 或者 octets 的 第 i 项 大于 255：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

IP_是私有 接受 octets：
    若 做 计数 用 octets 不等于 4：
        返还 假。
    完。
    若 octets 的 第 0 项 等于 10：
        返还 真。
    完。
    若 octets 的 第 0 项 等于 172 并且 octets 的 第 1 项 大于或等于 16 并且 octets 的 第 1 项 小于或等于 31：
        返还 真。
    完。
    若 octets 的 第 0 项 等于 192 并且 octets 的 第 1 项 等于 168：
        返还 真。
    完。
    返还 假。
完。

MIME_从扩展名 接受 ext：
    若 做 字符串相等 用 ext、「html」 或者 做 字符串相等 用 ext、「htm」：
        返还 「text/html」。
    完。
    若 做 字符串相等 用 ext、「css」：
        返还 「text/css」。
    完。
    若 做 字符串相等 用 ext、「js」：
        返还 「application/javascript」。
    完。
    若 做 字符串相等 用 ext、「json」：
        返还 「application/json」。
    完。
    若 做 字符串相等 用 ext、「xml」：
        返还 「application/xml」。
    完。
    若 做 字符串相等 用 ext、「txt」：
        返还 「text/plain」。
    完。
    若 做 字符串相等 用 ext、「png」：
        返还 「image/png」。
    完。
    若 做 字符串相等 用 ext、「jpg」 或者 做 字符串相等 用 ext、「jpeg」：
        返还 「image/jpeg」。
    完。
    若 做 字符串相等 用 ext、「gif」：
        返还 「image/gif」。
    完。
    若 做 字符串相等 用 ext、「svg」：
        返还 「image/svg+xml」。
    完。
    若 做 字符串相等 用 ext、「pdf」：
        返还 「application/pdf」。
    完。
    若 做 字符串相等 用 ext、「zip」：
        返还 「application/zip」。
    完。
    返还 「application/octet-stream」。
完。

本章 收束。
