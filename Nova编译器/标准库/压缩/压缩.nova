《压缩》

引用《..·函数式·函数式》。

引用《..·基础》。

RLE_压缩 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 当前 为 数据 的 第 0 项。
    可变 计数v 为 1。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 等于 当前：
            计数v 为 计数v 加 1。
        否则：
            做 追加 用 结果、一组：当前、计数v。
            当前 为 数据 的 第 i 项。
            计数v 为 1。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 结果、一组：当前、计数v。
    返还 结果。
完。

RLE_解压 接受 压缩数据：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 压缩数据：
        可变 j 为 0。
        只要 j 小于 压缩数据 的 第 i 项 的 第 1 项：
            做 追加 用 结果、压缩数据 的 第 i 项 的 第 0 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

LZ77_压缩 接受 数据、窗口大小：
    可变 结果 为 一组：（空）。
    可变 位置 为 0。
    只要 位置 小于 做 计数 用 数据：
        可变 最佳距 为 0。
        可变 最佳长 为 0。
        可变 起始 为 位置 减 窗口大小。
        若 起始 小于 0：
            起始 为 0。
        完。
        可变 s 为 起始。
        只要 s 小于 位置：
            可变 长度 为 0。
            只要 s 加 长度 小于 位置 并且 位置 加 长度 小于 做 计数 用 数据 并且 数据 的 第 s 加 长度 项 等于 数据 的 第 位置 加 长度 项：
                长度 为 长度 加 1。
            完。
            若 长度 大于 最佳长：
                最佳距 为 位置 减 s。
                最佳长 为 长度。
            完。
            s 为 s 加 1。
        完。
        若 最佳长 大于或等于 3：
            做 追加 用 结果、一组：1、最佳距、最佳长。
            位置 为 位置 加 最佳长。
        否则：
            做 追加 用 结果、一组：0、数据 的 第 位置 项。
            位置 为 位置 加 1。
        完。
    完。
    返还 结果。
完。

LZ77_解压 接受 压缩数据：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 压缩数据：
        若 压缩数据 的 第 i 项 的 第 0 项 等于 0：
            做 追加 用 结果、压缩数据 的 第 i 项 的 第 1 项。
        否则：
            距离 为 压缩数据 的 第 i 项 的 第 1 项。
            长度 为 压缩数据 的 第 i 项 的 第 2 项。
            起始 为 做 计数 用 结果 减 距离。
            可变 j 为 0。
            只要 j 小于 长度：
                做 追加 用 结果、结果 的 第 起始 加 j 项。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

压缩率 接受 原始大小、压缩大小：
    若 原始大小 等于 0：
        返还 0。
    完。
    返还 (原始大小 减 压缩大小) 乘 100 除以 原始大小。
完。

ZigZag_编码 接受 数据：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        v 为 数据 的 第 i 项。
        若 v 大于或等于 0：
            做 追加 用 结果、v 乘 2。
        否则：
            做 追加 用 结果、(0 减 v) 乘 2 减 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

ZigZag_解码 接受 编码数据：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 编码数据：
        v 为 编码数据 的 第 i 项。
        若 v 取余于 2 等于 0：
            做 追加 用 结果、v 除以 2。
        否则：
            做 追加 用 结果、0 减 (v 加 1) 除以 2。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数据_熵估算 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    频率 为 做 哈夫曼_构建频率表 用 数据。
    返还 做 计数 用 频率 的 第 0 项 乘 100 除以 做 计数 用 数据。
完。

数据_可压缩 接受 数据：
    返还 做 数据_熵估算 用 数据 小于 80。
完。

哈夫曼_构建频率表 接受 数据：
    可变 频率 为 一组：（空）。
    可变 符号 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 找到 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 符号：
            若 符号 的 第 j 项 等于 数据 的 第 i 项：
                频率 的 第 j 项 为 频率 的 第 j 项 加 1。
                找到 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 找到：
            做 追加 用 符号、数据 的 第 i 项。
            做 追加 用 频率、1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：符号、频率。
完。

哈夫曼_编码长度估算 接受 频率表：
    符号 为 频率表 的 第 0 项。
    频率 为 频率表 的 第 1 项。
    可变 总位 为 0。
    可变 n 为 做 计数 用 符号。
    可变 i 为 0。
    只要 i 小于 n：
        可变 位长 为 1。
        可变 阈值 为 频率 的 第 i 项。
        可变 总频 为 0。
        可变 j 为 0。
        只要 j 小于 n：
            总频 为 总频 加 频率 的 第 j 项。
            j 为 j 加 1。
        完。
        只要 阈值 乘 (1 左移 位长) 小于 总频 并且 位长 小于 16：
            位长 为 位长 加 1。
        完。
        总位 为 总位 加 频率 的 第 i 项 乘 位长。
        i 为 i 加 1。
    完。
    返还 总位。
完。

字典压缩_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、0。
完。

字典压缩_添加 接受 字典、短语：
    做 追加 用 字典 的 第 0 项、短语。
    做 追加 用 字典 的 第 1 项、字典 的 第 2 项。
    字典 的 第 2 项 为 字典 的 第 2 项 加 1。
完。

字典压缩_查找 接受 字典、短语：
    可变 i 为 0。
    只要 i 小于 做 计数 用 字典 的 第 0 项：
        若 字典 的 第 0 项 的 第 i 项 等于 短语：
            返还 字典 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

压缩_计算比率 接受 原始大小、压缩大小：
    若 原始大小 等于 0：
        返还 0。
    完。
    返还 压缩大小 乘 1000 除以 原始大小。
完。

压缩_节省百分比 接受 原始大小、压缩大小：
    若 原始大小 等于 0：
        返还 0。
    完。
    返还 (原始大小 减 压缩大小) 乘 100 除以 原始大小。
完。

可变 _压缩字典 为 一组：（空）。

字典压缩_初始化 接受 零参：
    _压缩字典 为 一组：（空）。
完。

字典压缩_注册 接受 模式：
    做 追加 用 _压缩字典、模式。
    返还 做 计数 用 _压缩字典 减 1。
完。

字典压缩_编码 接受 数据：
    可变 结果 为 一组：（空）。
    可变 位置 为 0。
    只要 位置 小于 做 计数 用 数据：
        可变 最长匹配 为 负 1。
        可变 最长长度 为 0。
        可变 i 为 0。
        只要 i 小于 做 计数 用 _压缩字典：
            模式 为 _压缩字典 的 第 i 项。
            模式长 为 做 计数 用 模式。
            若 位置 加 模式长 小于或等于 做 计数 用 数据 并且 模式长 大于 最长长度：
                可变 匹配 为 真。
                可变 j 为 0。
                只要 j 小于 模式长：
                    若 数据 的 第 位置 加 j 项 不等于 模式 的 第 j 项：
                        匹配 为 假。
                    完。
                    j 为 j 加 1。
                完。
                若 匹配：
                    最长匹配 为 i。
                    最长长度 为 模式长。
                完。
            完。
            i 为 i 加 1。
        完。
        若 最长匹配 大于或等于 0：
            做 追加 用 结果、一组：1、最长匹配。
            位置 为 位置 加 最长长度。
        否则：
            做 追加 用 结果、一组：0、数据 的 第 位置 项。
            位置 为 位置 加 1。
        完。
    完。
    返还 结果。
完。

字典压缩_解码 接受 编码数据：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 编码数据：
        若 编码数据 的 第 i 项 的 第 0 项 等于 1：
            模式 为 _压缩字典 的 第 编码数据 的 第 i 项 的 第 1 项 项。
            可变 j 为 0。
            只要 j 小于 做 计数 用 模式：
                做 追加 用 结果、模式 的 第 j 项。
                j 为 j 加 1。
            完。
        否则：
            做 追加 用 结果、编码数据 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _压缩_总原始 为 0。

可变 _压缩_总压缩 为 0。

压缩_记录 接受 原始、压缩后：
    _压缩_总原始 为 _压缩_总原始 加 原始。
    _压缩_总压缩 为 _压缩_总压缩 加 压缩后。
完。

汉字频率表 为 一组：一组：30340、89、一组：19968、61、一组：26159、52、一组：22312、48、一组：20154、45、一组：20102、42、一组：26377、38、一组：20013、35、一组：22823、32、一组：20026、29、一组：19978、27、一组：20491、25、一组：22269、23、一组：25105、21、一组：20197、19、一组：35201、18、一组：20204、17、一组：22238、16、一组：23478、15、一组：26597、14、一组：36816、13、一组：24320、12、一组：20986、11、一组：20943、10、一组：21521、9、一组：35813、8、一组：29289、7、一组：24471、6、一组：23427、5、一组：21305、5、一组：20080、4、一组：20320、3。

汉字频率编码_构建 接受 零参：
    可变 堆 为 一组：（空）。
    可变 节点池 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 汉字频率表：
        做 追加 用 节点池、一组：真、汉字频率表 的 第 i 项 的 第 1 项、汉字频率表 的 第 i 项 的 第 0 项、负 1、负 1。
        做 追加 用 堆、一组：汉字频率表 的 第 i 项 的 第 1 项、做 计数 用 节点池 减 1。
        i 为 i 加 1。
    完。
    只要 做 计数 用 堆 大于 1：
        可变 已改变 为 真。
        只要 已改变：
            已改变 为 假。
            可变 j 为 0。
            只要 j 小于 做 计数 用 堆 减 1：
                若 堆 的 第 j 项 的 第 0 项 大于 堆 的 第 j 加 1 项 的 第 0 项：
                    临时 为 堆 的 第 j 项。
                    堆 的 第 j 项 为 堆 的 第 j 加 1 项。
                    堆 的 第 j 加 1 项 为 临时。
                    已改变 为 真。
                完。
                j 为 j 加 1。
            完。
        完。
        左 为 堆 的 第 0 项。
        右 为 堆 的 第 1 项。
        可变 新堆 为 一组：（空）。
        可变 k 为 2。
        只要 k 小于 做 计数 用 堆：
            做 追加 用 新堆、堆 的 第 k 项。
            k 为 k 加 1。
        完。
        做 追加 用 节点池、一组：假、左 的 第 0 项 加 右 的 第 0 项、负 1、左 的 第 1 项、右 的 第 1 项。
        做 追加 用 新堆、一组：左 的 第 0 项 加 右 的 第 0 项、做 计数 用 节点池 减 1。
        堆 为 新堆。
    完。
    返还 节点池。
完。

汉字频率编码_平均码长 接受 节点池：
    若 做 计数 用 节点池 等于 0：
        返还 0。
    完。
    根 为 做 计数 用 节点池 减 1。
    可变 总权重码长 为 0。
    可变 总频率 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 汉字频率表：
        总频率 为 总频率 加 汉字频率表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    可变 队列 为 一组：一组：根、0。
    可变 qi 为 0。
    只要 qi 小于 做 计数 用 队列：
        当前 为 队列 的 第 qi 项。
        节点idx 为 当前 的 第 0 项。
        深度 为 当前 的 第 1 项。
        若 节点idx 大于或等于 0 并且 节点idx 小于 做 计数 用 节点池：
            节点 为 节点池 的 第 节点idx 项。
            若 节点 的 第 0 项：
                总权重码长 为 总权重码长 加 节点 的 第 1 项 乘 深度。
            否则：
                做 追加 用 队列、一组：节点 的 第 3 项、深度 加 1。
                做 追加 用 队列、一组：节点 的 第 4 项、深度 加 1。
            完。
        完。
        qi 为 qi 加 1。
    完。
    返还 总权重码长 乘 100 除以 总频率。
完。

汉字压缩效率报告 接受 零参：
    树 为 做 汉字频率编码_构建。
    可变 平均码长 为 做 汉字频率编码_平均码长 用 树 除以 100。
    若 平均码长 等于 0：
        平均码长 为 1。
    完。
    可变 _平均码长分母 为 1。
    若 平均码长 大于 0：
        _平均码长分母 为 平均码长。
    完。
    utf16压缩比 为 1600 除以 _平均码长分母。
    返还 一组：平均码长、utf16压缩比、做 计数 用 树。
完。

算术编码_理论压缩率 为 220。

算术编码_估算平均码长 接受 频率列表：
    若 做 计数 用 频率列表 等于 0：
        返还 0。
    完。
    可变 总频率 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 频率列表：
        总频率 为 总频率 加 频率列表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 总频率 等于 0：
        返还 0。
    完。
    可变 H 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 频率列表：
        p1000 为 频率列表 的 第 i 项 乘 1000 除以 总频率。
        若 p1000 大于 0：
            可变 位数 为 0。
            可变 v 为 p1000。
            只要 v 小于 1000：
                v 为 v 乘 2。
                位数 为 位数 加 1。
            完。
            H 为 H 加 p1000 乘 位数 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 H。
完。

选择压缩算法 接受 数据长度、汉字比例、重复率：
    若 数据长度 小于 100：
        返还 「RLE(短数据)」。
    完。
    若 汉字比例 大于 70 并且 数据长度 大于 1000：
        返还 「算术编码(高效中文)」。
    完。
    若 重复率 大于 50：
        返还 「LZ77(高重复)」。
    完。
    若 汉字比例 大于 30：
        返还 「频率编码汉字频率版」。
    完。
    返还 「LZ77通用版」。
完。

本章 收束。
