《内存管理》

引用《..·基础》。

块_地址 为 0。

块_大小 为 1。

块_状态 为 2。

块_引用 为 3。

块_代 为 4。

块_颜色 为 5。

内存块_创建 接受 地址、大小：
    返还 一组：地址、大小、0。
完。

可变 堆_块列表 为 一组：（空）。

可变 堆_下一地址 为 0x1000。

可变 堆_总分配 为 0。

可变 堆_总释放 为 0。

可变 堆_峰值 为 0。

可变 堆_当前使用 为 0。

堆_分配 接受 大小：
    可变 i 为 0。
    只要 i 小于 做 计数 用 堆_块列表：
        blk 为 堆_块列表 的 第 i 项。
        若 blk 的 第 块_状态 项 等于 0 并且 blk 的 第 块_大小 项 大于或等于 大小：
            blk 的 第 块_状态 项 为 1。
            blk 的 第 块_引用 项 为 1。
            堆_总分配 为 堆_总分配 加 1。
            堆_当前使用 为 堆_当前使用 加 blk 的 第 块_大小 项。
            若 堆_当前使用 大于 堆_峰值：
                堆_峰值 为 堆_当前使用。
            完。
            返还 blk 的 第 块_地址 项。
        完。
        i 为 i 加 1。
    完。
    对齐大小 为 (大小 加 7) 除以 8 乘 8。
    地址 为 堆_下一地址。
    做 追加 用 堆_块列表、一组：地址、对齐大小、1、1。
    堆_下一地址 为 堆_下一地址 加 对齐大小。
    堆_总分配 为 堆_总分配 加 1。
    堆_当前使用 为 堆_当前使用 加 对齐大小。
    若 堆_当前使用 大于 堆_峰值：
        堆_峰值 为 堆_当前使用。
    完。
    返还 地址。
完。

堆_查找块 接受 地址：
    可变 i 为 0。
    只要 i 小于 做 计数 用 堆_块列表：
        若 堆_块列表 的 第 i 项 的 第 块_地址 项 等于 地址：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

堆_释放 接受 地址：
    索引 为 做 堆_查找块 用 地址。
    若 索引 小于 0：
        返还 假。
    完。
    blk 为 堆_块列表 的 第 索引 项。
    若 blk 的 第 块_状态 项 不等于 1：
        返还 假。
    完。
    blk 的 第 块_状态 项 为 0。
    blk 的 第 块_引用 项 为 0。
    blk 的 第 块_颜色 项 为 0。
    堆_总释放 为 堆_总释放 加 1。
    堆_当前使用 为 堆_当前使用 减 blk 的 第 块_大小 项。
    返还 真。
完。

堆_增引用 接受 地址：
    索引 为 做 堆_查找块 用 地址。
    若 索引 小于 0：
        返还 0。
    完。
    堆_块列表 的 第 索引 项 的 第 块_引用 项 为 堆_块列表 的 第 索引 项 的 第 块_引用 项 加 1。
    返还 堆_块列表 的 第 索引 项 的 第 块_引用 项。
完。

堆_减引用 接受 地址：
    索引 为 做 堆_查找块 用 地址。
    若 索引 小于 0：
        返还 0。
    完。
    堆_块列表 的 第 索引 项 的 第 块_引用 项 为 堆_块列表 的 第 索引 项 的 第 块_引用 项 减 1。
    若 堆_块列表 的 第 索引 项 的 第 块_引用 项 小于或等于 0：
        做 堆_释放 用 地址。
        返还 0。
    完。
    返还 堆_块列表 的 第 索引 项 的 第 块_引用 项。
完。

堆_引用数 接受 地址：
    索引 为 做 堆_查找块 用 地址。
    若 索引 小于 0：
        返还 0。
    完。
    返还 堆_块列表 的 第 索引 项 的 第 块_引用 项。
完。

GC_重置颜色 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 堆_块列表：
        若 堆_块列表 的 第 i 项 的 第 块_状态 项 等于 1：
            堆_块列表 的 第 i 项 的 第 块_颜色 项 为 0。
        完。
        i 为 i 加 1。
    完。
完。

GC_标记引用目标 接受 目标：
    若 目标 小于 0 或者 目标 大于或等于 做 计数 用 堆_块列表：
        返还 空。
    完。
    若 堆_块列表 的 第 目标 项 的 第 块_颜色 项 等于 0 并且 堆_块列表 的 第 目标 项 的 第 块_状态 项 等于 1：
        堆_块列表 的 第 目标 项 的 第 块_颜色 项 为 1。
    完。
完。

GC_标记根 接受 根地址列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 根地址列表：
        索引 为 做 堆_查找块 用 根地址列表 的 第 i 项。
        若 索引 大于或等于 0 并且 堆_块列表 的 第 索引 项 的 第 块_状态 项 等于 1：
            堆_块列表 的 第 索引 项 的 第 块_颜色 项 为 1。
        完。
        i 为 i 加 1。
    完。
完。

可变 堆_引用表 为 一组：（空）。

堆_添加引用 接受 源地址、目标地址：
    si 为 做 堆_查找块 用 源地址。
    ti 为 做 堆_查找块 用 目标地址。
    若 si 大于或等于 0 并且 ti 大于或等于 0：
        做 追加 用 堆_引用表、一组：si、ti。
    完。
完。

堆_移除引用 接受 源地址、目标地址：
    si 为 做 堆_查找块 用 源地址。
    ti 为 做 堆_查找块 用 目标地址。
    可变 i 为 0。
    只要 i 小于 做 计数 用 堆_引用表：
        若 堆_引用表 的 第 i 项 的 第 0 项 等于 si 并且 堆_引用表 的 第 i 项 的 第 1 项 等于 ti：
            做 删除 用 堆_引用表、i。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

GC_传播 接受 零参：
    可变 ri 为 0。
    可变 有灰色 为 真。
    只要 有灰色：
        有灰色 为 假。
        可变 i 为 0。
        只要 i 小于 做 计数 用 堆_块列表：
            若 堆_块列表 的 第 i 项 的 第 块_颜色 项 等于 1：
                只要 ri 小于 做 计数 用 堆_引用表：
                    若 堆_引用表 的 第 ri 项 的 第 0 项 等于 i：
                        做 GC_标记引用目标 用 堆_引用表 的 第 ri 项 的 第 1 项。
                    完。
                    ri 为 ri 加 1。
                完。
                堆_块列表 的 第 i 项 的 第 块_颜色 项 为 2。
                有灰色 为 真。
            完。
            i 为 i 加 1。
        完。
    完。
完。

GC_清除 接受 零参：
    可变 清除数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 堆_块列表：
        若 堆_块列表 的 第 i 项 的 第 块_状态 项 等于 1 并且 堆_块列表 的 第 i 项 的 第 块_颜色 项 等于 0：
            堆_块列表 的 第 i 项 的 第 块_状态 项 为 0。
            堆_块列表 的 第 i 项 的 第 块_引用 项 为 0。
            堆_当前使用 为 堆_当前使用 减 堆_块列表 的 第 i 项 的 第 块_大小 项。
            堆_总释放 为 堆_总释放 加 1。
            清除数 为 清除数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 清除数。
完。

内存_GC_收集 接受 根地址列表：
    做 GC_重置颜色。
    做 GC_标记根 用 根地址列表。
    做 GC_传播。
    返还 做 GC_清除。
完。

GC_提升到老代 接受 地址：
    索引 为 做 堆_查找块 用 地址。
    若 索引 大于或等于 0：
        堆_块列表 的 第 索引 项 的 第 块_代 项 为 1。
    完。
完。

GC_新生代收集 接受 根地址列表：
    做 GC_重置颜色。
    做 GC_标记根 用 根地址列表。
    做 GC_传播。
    可变 清除数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 堆_块列表：
        若 堆_块列表 的 第 i 项 的 第 块_状态 项 等于 1 并且 堆_块列表 的 第 i 项 的 第 块_颜色 项 等于 0 并且 堆_块列表 的 第 i 项 的 第 块_代 项 等于 0：
            堆_块列表 的 第 i 项 的 第 块_状态 项 为 0。
            堆_块列表 的 第 i 项 的 第 块_引用 项 为 0。
            堆_当前使用 为 堆_当前使用 减 堆_块列表 的 第 i 项 的 第 块_大小 项。
            堆_总释放 为 堆_总释放 加 1。
            清除数 为 清除数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 清除数。
完。

池_创建 接受 块大小、容量：
    可变 位图 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 位图、0。
        i 为 i 加 1。
    完。
    返还 一组：块大小、容量、位图、做 堆_分配 用 块大小 乘 容量。
完。

池_分配 接受 池：
    可变 i 为 0。
    只要 i 小于 池 的 第 1 项：
        若 池 的 第 2 项 的 第 i 项 等于 0：
            池 的 第 2 项 的 第 i 项 为 1。
            返还 池 的 第 3 项 加 i 乘 池 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

池_释放 接受 池、地址：
    偏移 为 地址 减 池 的 第 3 项。
    索引 为 偏移 除以 池 的 第 0 项。
    若 索引 大于或等于 0 并且 索引 小于 池 的 第 1 项：
        池 的 第 2 项 的 第 索引 项 为 0。
        返还 真。
    完。
    返还 假。
完。

池_已用数 接受 池：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 池 的 第 1 项：
        若 池 的 第 2 项 的 第 i 项 等于 1：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

Arena_创建 接受 容量：
    返还 一组：做 堆_分配 用 容量、容量、0。
完。

Arena_分配 接受 arena、大小：
    对齐 为 (大小 加 7) 除以 8 乘 8。
    若 arena 的 第 2 项 加 对齐 大于 arena 的 第 1 项：
        返还 负 1。
    完。
    地址 为 arena 的 第 0 项 加 arena 的 第 2 项。
    arena 的 第 2 项 为 arena 的 第 2 项 加 对齐。
    返还 地址。
完。

Arena_重置 接受 arena：
    arena 的 第 2 项 为 0。
完。

Arena_已用 接受 arena：
    返还 arena 的 第 2 项。
完。

Arena_剩余 接受 arena：
    返还 arena 的 第 1 项 减 arena 的 第 2 项。
完。

堆_空闲块数 接受 零参：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 堆_块列表：
        若 堆_块列表 的 第 i 项 的 第 块_状态 项 等于 0：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

堆_占用块数 接受 零参：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 堆_块列表：
        若 堆_块列表 的 第 i 项 的 第 块_状态 项 等于 1：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

堆_总块数 接受 零参：
    返还 做 计数 用 堆_块列表。
完。

堆_碎片率 接受 零参：
    总 为 做 堆_总块数。
    若 总 等于 0：
        返还 0。
    完。
    返还 做 堆_空闲块数 乘 100 除以 总。
完。

BUDDY_最大阶 为 20。

BUDDY_最小块 为 64。

BUDDY_最小阶 为 6。

Buddy_创建 接受 总大小：
    可变 空闲表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 BUDDY_最大阶：
        做 追加 用 空闲表、一组：（空）。
        i 为 i 加 1。
    完。
    阶 为 做 Buddy_求阶 用 总大小。
    做 追加 用 空闲表 的 第 阶 项、0。
    返还 一组：总大小、0x10000、空闲表、一组：（空）。
完。

Buddy_求阶 接受 大小：
    可变 阶 为 BUDDY_最小阶。
    只要 阶 小于或等于 BUDDY_最大阶：
        若 1 左移 阶 大于或等于 大小：
            返还 阶。
        完。
        阶 为 阶 加 1。
    完。
    返还 BUDDY_最大阶。
完。

Buddy_分配 接受 buddy、请求大小：
    需要阶 为 做 Buddy_求阶 用 请求大小。
    若 需要阶 大于 BUDDY_最大阶：
        返还 负 1。
    完。
    可变 阶 为 需要阶。
    只要 阶 小于或等于 BUDDY_最大阶：
        若 做 计数 用 buddy 的 第 2 项 的 第 阶 项 大于 0：
            n 为 做 计数 用 buddy 的 第 2 项 的 第 阶 项。
            偏移 为 buddy 的 第 2 项 的 第 阶 项 的 第 n 减 1 项。
            可变 新链 为 一组：（空）。
            可变 j 为 0。
            只要 j 小于 n 减 1：
                做 追加 用 新链、buddy 的 第 2 项 的 第 阶 项 的 第 j 项。
                j 为 j 加 1。
            完。
            buddy 的 第 2 项 的 第 阶 项 为 新链。
            可变 当前阶 为 阶。
            只要 当前阶 大于 需要阶：
                当前阶 为 当前阶 减 1。
                伙伴偏移 为 偏移 加 (1 左移 当前阶)。
                做 追加 用 buddy 的 第 2 项 的 第 当前阶 项、伙伴偏移。
            完。
            做 追加 用 buddy 的 第 3 项、一组：偏移、需要阶。
            返还 buddy 的 第 1 项 加 偏移。
        完。
        阶 为 阶 加 1。
    完。
    返还 负 1。
完。

Buddy_释放 接受 buddy、地址：
    偏移 为 地址 减 buddy 的 第 1 项。
    可变 阶 为 负 1。
    可变 idx 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 buddy 的 第 3 项：
        若 buddy 的 第 3 项 的 第 i 项 的 第 0 项 等于 偏移：
            阶 为 buddy 的 第 3 项 的 第 i 项 的 第 1 项。
            idx 为 i。
        完。
        i 为 i 加 1。
    完。
    若 阶 小于 0：
        返还 假。
    完。
    可变 新分配 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 buddy 的 第 3 项：
        若 i 不等于 idx：
            做 追加 用 新分配、buddy 的 第 3 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    buddy 的 第 3 项 为 新分配。
    可变 当前偏移 为 偏移。
    可变 当前阶 为 阶。
    只要 当前阶 小于 BUDDY_最大阶：
        伙伴 为 当前偏移 位异或 1 左移 当前阶。
        可变 伙伴idx 为 负 1。
        i 为 0。
        只要 i 小于 做 计数 用 buddy 的 第 2 项 的 第 当前阶 项：
            若 buddy 的 第 2 项 的 第 当前阶 项 的 第 i 项 等于 伙伴：
                伙伴idx 为 i。
            完。
            i 为 i 加 1。
        完。
        若 伙伴idx 小于 0：
            做 追加 用 buddy 的 第 2 项 的 第 当前阶 项、当前偏移。
            返还 真。
        完。
        可变 新链 为 一组：（空）。
        i 为 0。
        只要 i 小于 做 计数 用 buddy 的 第 2 项 的 第 当前阶 项：
            若 i 不等于 伙伴idx：
                做 追加 用 新链、buddy 的 第 2 项 的 第 当前阶 项 的 第 i 项。
            完。
            i 为 i 加 1。
        完。
        buddy 的 第 2 项 的 第 当前阶 项 为 新链。
        若 伙伴 小于 当前偏移：
            当前偏移 为 伙伴。
        完。
        当前阶 为 当前阶 加 1。
    完。
    做 追加 用 buddy 的 第 2 项 的 第 当前阶 项、当前偏移。
    返还 真。
完。

Slab_创建 接受 对象大小、页大小：
    每页slot 为 页大小 除以 ((对象大小 加 7) 除以 8 乘 8)。
    若 每页slot 小于 1：
        每页slot 为 1。
    完。
    返还 一组：对象大小、每页slot、一组：（空）、一组：（空）。
完。

Slab_扩容 接受 slab：
    页基址 为 做 堆_分配 用 slab 的 第 1 项 乘 ((slab 的 第 0 项 加 7) 除以 8 乘 8)。
    做 追加 用 slab 的 第 2 项、页基址。
    对齐大小 为 (slab 的 第 0 项 加 7) 除以 8 乘 8。
    可变 i 为 slab 的 第 1 项 减 1。
    只要 i 大于或等于 0：
        做 追加 用 slab 的 第 3 项、页基址 加 i 乘 对齐大小。
        i 为 i 减 1。
    完。
完。

Slab_分配 接受 slab：
    若 做 计数 用 slab 的 第 3 项 等于 0：
        做 Slab_扩容 用 slab。
    完。
    n 为 做 计数 用 slab 的 第 3 项。
    地址 为 slab 的 第 3 项 的 第 n 减 1 项。
    可变 新栈 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        做 追加 用 新栈、slab 的 第 3 项 的 第 i 项。
        i 为 i 加 1。
    完。
    slab 的 第 3 项 为 新栈。
    返还 地址。
完。

Slab_释放 接受 slab、地址：
    做 追加 用 slab 的 第 3 项、地址。
完。

Slab_已分配 接受 slab：
    总slot 为 做 计数 用 slab 的 第 2 项 乘 slab 的 第 1 项。
    返还 总slot 减 做 计数 用 slab 的 第 3 项。
完。

GC_紧凑化 接受 根地址列表：
    做 GC_重置颜色。
    做 GC_标记根 用 根地址列表。
    做 GC_传播。
    可变 新地址映射 为 一组：（空）。
    可变 写指针 为 堆_块列表 的 第 0 项 的 第 块_地址 项。
    可变 i 为 0。
    可变 存活数 为 0。
    只要 i 小于 做 计数 用 堆_块列表：
        若 堆_块列表 的 第 i 项 的 第 块_状态 项 等于 1 并且 堆_块列表 的 第 i 项 的 第 块_颜色 项 大于 0：
            做 追加 用 新地址映射、一组：i、写指针。
            写指针 为 写指针 加 堆_块列表 的 第 i 项 的 第 块_大小 项。
            存活数 为 存活数 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 j 为 0。
    只要 j 小于 做 计数 用 堆_引用表：
        可变 k 为 0。
        只要 k 小于 做 计数 用 新地址映射：
            若 新地址映射 的 第 k 项 的 第 0 项 等于 堆_引用表 的 第 j 项 的 第 0 项：
                堆_引用表 的 第 j 项 的 第 0 项 为 新地址映射 的 第 k 项 的 第 0 项。
            完。
            若 新地址映射 的 第 k 项 的 第 0 项 等于 堆_引用表 的 第 j 项 的 第 1 项：
                堆_引用表 的 第 j 项 的 第 1 项 为 新地址映射 的 第 k 项 的 第 0 项。
            完。
            k 为 k 加 1。
        完。
        j 为 j 加 1。
    完。
    可变 新块列表 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 新地址映射：
        旧idx 为 新地址映射 的 第 i 项 的 第 0 项。
        新addr 为 新地址映射 的 第 i 项 的 第 1 项。
        blk 为 堆_块列表 的 第 旧idx 项。
        做 追加 用 新块列表、一组：新addr、blk 的 第 块_大小 项、1、blk 的 第 块_引用 项、blk 的 第 块_代 项、2。
        i 为 i 加 1。
    完。
    堆_块列表 为 新块列表。
    堆_当前使用 为 写指针 减 堆_块列表 的 第 0 项 的 第 块_地址 项。
    堆_下一地址 为 写指针。
    返还 存活数。
完。

分配_栈 为 0。

分配_堆 为 1。

分配_池 为 2。

逃逸分析_推荐策略 接受 逃逸类型、对象大小、生命期长度：
    若 逃逸类型 等于 0 并且 对象大小 小于或等于 4096 并且 生命期长度 小于或等于 100：
        返还 分配_栈。
    完。
    若 逃逸类型 等于 0 并且 生命期长度 小于或等于 10：
        返还 分配_池。
    完。
    返还 分配_堆。
完。

可变 策略_栈次数 为 0。

可变 策略_堆次数 为 0。

可变 策略_池次数 为 0。

策略_记录 接受 策略：
    若 策略 等于 分配_栈：
        策略_栈次数 为 策略_栈次数 加 1。
    完。
    若 策略 等于 分配_堆：
        策略_堆次数 为 策略_堆次数 加 1。
    完。
    若 策略 等于 分配_池：
        策略_池次数 为 策略_池次数 加 1。
    完。
完。

策略_报告 接受 零参：
    做 打印 用 「分配策略: 栈=」 加 做 转字符串 用 策略_栈次数 加 「 堆=」 加 做 转字符串 用 策略_堆次数 加 「 池=」 加 做 转字符串 用 策略_池次数。
    总 为 策略_栈次数 加 策略_堆次数 加 策略_池次数。
    若 总 大于 0：
        做 打印 用 「栈占比: 」 加 做 转字符串 用 策略_栈次数 乘 100 除以 总 加 「%」。
    完。
完。

本章 收束。
