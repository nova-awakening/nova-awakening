《编解码》

引用《..·加密·哈希》。

引用《..·基础》。

RLE_编码 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 当前 为 数据 的 第 0 项。
    可变 计数器 为 1。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 等于 当前：
            计数器 为 计数器 加 1。
        否则：
            做 追加 用 结果、一组：当前、计数器。
            当前 为 数据 的 第 i 项。
            计数器 为 1。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 结果、一组：当前、计数器。
    返还 结果。
完。

RLE_解码 接受 编码：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 编码：
        可变 j 为 0。
        只要 j 小于 编码 的 第 i 项 的 第 1 项：
            做 追加 用 结果、编码 的 第 i 项 的 第 0 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

差分_编码 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 一组：（空）。
    完。
    可变 r 为 一组：数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 r、数据 的 第 i 项 减 数据 的 第 i 减 1 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

差分_解码 接受 编码：
    若 做 计数 用 编码 等于 0：
        返还 一组：（空）。
    完。
    可变 r 为 一组：编码 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 编码：
        做 追加 用 r、r 的 第 i 减 1 项 加 编码 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

编解码_校验和 接受 数据：
    可变 s1 为 0。
    可变 s2 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        s1 为 (s1 加 数据 的 第 i 项) 取余于 255。
        s2 为 (s2 加 s1) 取余于 255。
        i 为 i 加 1。
    完。
    返还 s2 乘 256 加 s1。
完。

帧_打包 接受 类型、数据：
    可变 帧 为 一组：170、做 计数 用 数据、类型。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 帧、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    做 追加 用 帧、做 校验和 用 帧、0。
    返还 帧。
完。

帧_验证 接受 帧：
    若 做 计数 用 帧 小于 4：
        返还 假。
    完。
    若 帧 的 第 0 项 不等于 170：
        返还 假。
    完。
    可变 子帧 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 帧 减 1：
        做 追加 用 子帧、帧 的 第 i 项。
        i 为 i 加 1。
    完。
    若 做 计数 用 帧 等于 0：
        返还 空。
    完。
    返还 做 校验和 用 子帧、0 等于 帧 的 第 做 计数 用 帧 减 1 项。
完。

编解码_VarInt_编码 接受 值：
    可变 结果 为 一组：（空）。
    可变 v 为 值。
    若 v 小于 0：
        v 为 0 减 v。
    完。
    只要 v 大于或等于 128：
        做 追加 用 结果、v 取余于 128 加 128。
        v 为 v 除以 128。
    完。
    做 追加 用 结果、v。
    返还 结果。
完。

编解码_VarInt_解码 接受 字节列表：
    可变 结果 为 0。
    可变 移位 为 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字节列表：
        b 为 字节列表 的 第 i 项。
        若 b 大于或等于 128：
            结果 为 结果 加 (b 减 128) 乘 移位。
            移位 为 移位 乘 128。
        否则：
            结果 为 结果 加 b 乘 移位。
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

B64表 为 「ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/」。

B64_字符 接受 索引：
    若 索引 小于 0 或者 索引 大于 63：
        返还 「A」。
    完。
    返还 做 子串 用 B64表、索引、1。
完。

B64_值 接受 字符：
    c 为 做 编码 用 字符。
    若 c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z：
        返还 c 减 65。
    完。
    若 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z：
        返还 c 减 97 加 26。
    完。
    若 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
        返还 c 减 48 加 52。
    完。
    若 c 等于 字符_加号：
        返还 62。
    完。
    若 c 等于 字符_斜杠：
        返还 63。
    完。
    返还 0。
完。

编码_Base64_编码 接受 数据：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 b0 为 数据 的 第 i 项。
        可变 b1 为 0。
        可变 b2 为 0。
        可变 剩余 为 做 计数 用 数据 减 i。
        若 剩余 大于 1：
            b1 为 数据 的 第 i 加 1 项。
        完。
        若 剩余 大于 2：
            b2 为 数据 的 第 i 加 2 项。
        完。
        结果 为 做 字符串拼接 用 结果、做 B64_字符 用 b0 除以 4。
        结果 为 做 字符串拼接 用 结果、做 B64_字符 用 b0 取余于 4 乘 16 加 b1 除以 16。
        若 剩余 大于 1：
            结果 为 做 字符串拼接 用 结果、做 B64_字符 用 b1 取余于 16 乘 4 加 b2 除以 64。
        否则：
            结果 为 做 字符串拼接 用 结果、「=」。
        完。
        若 剩余 大于 2：
            结果 为 做 字符串拼接 用 结果、做 B64_字符 用 b2 取余于 64。
        否则：
            结果 为 做 字符串拼接 用 结果、「=」。
        完。
        i 为 i 加 3。
    完。
    返还 结果。
完。

编码_Base64_解码 接受 文本：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    长度 为 做 计数 用 文本。
    只要 i 加 3 小于 长度：
        c0 为 做 B64_值 用 做 子串 用 文本、i、1。
        c1 为 做 B64_值 用 做 子串 用 文本、i 加 1、1。
        c2字符 为 做 子串 用 文本、i 加 2、1。
        c3字符 为 做 子串 用 文本、i 加 3、1。
        c2字符 为 做 子串 用 编码文本、i 加 2、1。
        c3字符 为 做 子串 用 编码文本、i 加 3、1。
        做 追加 用 结果、c0 乘 4 加 c1 除以 16。
        若 c2字符 不等于 「=」：
            c2 为 做 B64_值 用 c2字符。
            做 追加 用 结果、c1 取余于 16 乘 16 加 c2 除以 4。
            若 c3字符 不等于 「=」：
                c3 为 做 B64_值 用 c3字符。
                做 追加 用 结果、c2 取余于 4 乘 64 加 c3。
            完。
        完。
        i 为 i 加 4。
    完。
    返还 结果。
完。

循环冗余32 接受 数据：
    可变 crc状态 为 一组：0xFFFFFFFF。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        crc状态 的 第 0 项 为 crc状态 的 第 0 项 位异或 数据 的 第 i 项 位与 0xFF。
        可变 位值 为 0。
        只要 位值 小于 8：
            若 crc状态 的 第 0 项 位与 1 不等于 0：
                crc状态 的 第 0 项 为 crc状态 的 第 0 项 右移 1 位与 0x7FFFFFFF 位异或 0xEDB88320。
            否则：
                crc状态 的 第 0 项 为 crc状态 的 第 0 项 右移 1 位与 0x7FFFFFFF。
            完。
            位值 为 位值 加 1。
        完。
        i 为 i 加 1。
    完。
    crc状态 的 第 0 项 为 crc状态 的 第 0 项 位异或 0xFFFFFFFF。
    若 crc状态 的 第 0 项 小于 0：
        crc状态 的 第 0 项 为 crc状态 的 第 0 项 加 0x100000000。
    完。
    返还 crc状态 的 第 0 项。
完。

频率_排序 接受 频率：
    可变 索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 频率：
        若 频率 的 第 i 项 大于 0：
            做 追加 用 索引、一组：i、频率 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 索引：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 索引：
            若 索引 的 第 j 项 的 第 1 项 大于 索引 的 第 i 项 的 第 1 项：
                临时 为 索引 的 第 i 项。
                索引 的 第 i 项 为 索引 的 第 j 项。
                索引 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 索引。
完。

编解码_压缩率 接受 原始大小、压缩大小：
    若 原始大小 等于 0：
        返还 0。
    完。
    返还 (原始大小 减 压缩大小) 乘 1000 除以 原始大小。
完。

位_获取 接受 值、位号：
    返还 值 除以 (1 左移 位号) 取余于 2。
完。

编码_位_设置 接受 值、位号：
    返还 值 加 (1 左移 位号)。
完。

编码_位_清除 接受 值、位号：
    若 做 位_获取 用 值、位号 等于 1：
        返还 值 减 (1 左移 位号)。
    完。
    返还 值。
完。

帧_数据 接受 帧：
    可变 结果 为 一组：（空）。
    可变 i 为 3。
    只要 i 小于 做 计数 用 帧 减 1：
        做 追加 用 结果、帧 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

帧_类型 接受 帧：
    若 做 计数 用 帧 大于或等于 3：
        返还 帧 的 第 2 项。
    完。
    返还 负 1。
完。

本章 收束。
