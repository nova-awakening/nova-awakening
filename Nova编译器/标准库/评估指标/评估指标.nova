《评估指标》

引用《..·基础》。

引用《..·数学·决策树》。

引用《..·数学·基础·数学库》。

引用《..·算法·数学扩展》。

评估指标_准确率 接受 预测、标签：
    n 为 做 计数 用 预测。
    若 n 等于 0：
        返还 0。
    完。
    可变 ok_cnt 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 预测 的 第 i 项 等于 标签 的 第 i 项：
            ok_cnt 为 ok_cnt 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 ok_cnt 乘 1000 除以 n。
完。

统计_TP 接受 预测、标签：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测：
        若 预测 的 第 i 项 等于 1：
            若 标签 的 第 i 项 等于 1：
                c 为 c 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

统计_FP 接受 预测、标签：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测：
        若 预测 的 第 i 项 等于 1：
            若 标签 的 第 i 项 等于 0：
                c 为 c 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

统计_FN 接受 预测、标签：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测：
        若 预测 的 第 i 项 等于 0：
            若 标签 的 第 i 项 等于 1：
                c 为 c 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

评估指标_精确率 接受 预测、标签：
    tp 为 做 统计_TP 用 预测、标签。
    fp 为 做 统计_FP 用 预测、标签。
    若 tp 加 fp 等于 0：
        返还 0。
    完。
    返还 tp 乘 1000 除以 (tp 加 fp)。
完。

评估指标_召回率 接受 预测、标签：
    tp 为 做 统计_TP 用 预测、标签。
    fn 为 做 统计_FN 用 预测、标签。
    若 tp 加 fn 等于 0：
        返还 0。
    完。
    返还 tp 乘 1000 除以 (tp 加 fn)。
完。

F1_分数 接受 预测、标签：
    p 为 做 精确率 用 预测、标签。
    r 为 做 召回率 用 预测、标签。
    若 p 加 r 等于 0：
        返还 0。
    完。
    返还 2 乘 p 乘 r 除以 (p 加 r)。
完。

F_贝塔 接受 预测、标签、动量率：
    p 为 做 精确率 用 预测、标签。
    r 为 做 召回率 用 预测、标签。
    若 p 加 r 等于 0：
        返还 0。
    完。
    b2 为 动量率 乘 动量率 除以 1000。
    返还 (1000 加 b2) 乘 p 乘 r 除以 (b2 乘 p 加 r 乘 1000 除以 1000)。
完。

评估指标_混淆矩阵 接受 预测、标签：
    tp 为 做 统计_TP 用 预测、标签。
    fp 为 做 统计_FP 用 预测、标签。
    fn 为 做 统计_FN 用 预测、标签。
    tn 为 做 计数 用 预测 减 tp 减 fp 减 fn。
    返还 一组：tn、fp、fn、tp。
完。

卡帕系数 接受 预测、标签：
    n 为 做 计数 用 预测。
    若 n 等于 0：
        返还 0。
    完。
    可变 p1 为 0。
    可变 a1 为 0。
    可变 ok_cnt 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 预测 的 第 i 项 等于 1：
            p1 为 p1 加 1。
        完。
        若 标签 的 第 i 项 等于 1：
            a1 为 a1 加 1。
        完。
        若 预测 的 第 i 项 等于 标签 的 第 i 项：
            ok_cnt 为 ok_cnt 加 1。
        完。
        i 为 i 加 1。
    完。
    po 为 ok_cnt 乘 1000 除以 n。
    pe 为 (p1 乘 a1 加 (n 减 p1) 乘 (n 减 a1)) 乘 1000 除以 n 除以 n。
    若 1000 减 pe 等于 0：
        返还 1000。
    完。
    返还 (po 减 pe) 乘 1000 除以 (1000 减 pe)。
完。

宏F1 接受 预测、标签、n_classes：
    可变 总f1 为 0。
    可变 类别 为 0。
    只要 类别 小于 n_classes：
        可变 bin_pred 为 一组：（空）。
        可变 bin_label 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 预测：
            若 预测 的 第 i 项 等于 类别：
                做 追加 用 bin_pred、1。
            否则：
                做 追加 用 bin_pred、0。
            完。
            若 标签 的 第 i 项 等于 类别：
                做 追加 用 bin_label、1。
            否则：
                做 追加 用 bin_label、0。
            完。
            i 为 i 加 1。
        完。
        总f1 为 总f1 加 做 F1_分数 用 bin_pred、bin_label。
        类别 为 类别 加 1。
    完。
    若 n_classes 等于 0：
        返还 0。
    完。
    返还 总f1 除以 n_classes。
完。

评估指标_平均绝对误差 接受 预测、标签：
    n 为 做 计数 用 预测。
    若 n 等于 0：
        返还 0。
    完。
    返还 做 折叠 用 做 拉链 用 预测、标签、以 p、t 映射到 做 绝对值 用 p 减 t、0、以 s、d 映射到 s 加 d 乘 1000 除以 n。
完。

评估指标_均方误差 接受 预测、标签：
    n 为 做 计数 用 预测。
    若 n 等于 0：
        返还 0。
    完。
    返还 做 折叠 用 做 拉链 用 预测、标签、以 p、t 映射到 (p 减 t) 乘 (p 减 t)、0、以 s、d 映射到 s 加 d 除以 n。
完。

均方根误差 接受 预测、标签：
    返还 做 整数sqrt 用 做 评估指标_均方误差 用 预测、标签。
完。

平均绝对百分比误差 接受 预测、标签：
    n 为 做 计数 用 预测。
    若 n 等于 0：
        返还 0。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 标签 的 第 i 项 不等于 0：
            总 为 总 加 做 绝对值 用 预测 的 第 i 项 减 标签 的 第 i 项 乘 1000 除以 做 绝对值 用 标签 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 总 除以 n。
完。

R方 接受 预测、标签：
    n 为 做 计数 用 标签。
    可变 总平方和 为 0。
    可变 残差平方和 为 0。
    若 n 等于 0：
        返还 0。
    完。
    可变 mean_y 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        mean_y 为 mean_y 加 标签 的 第 i 项。
        i 为 i 加 1。
    完。
    mean_y 为 mean_y 除以 n。
    i 为 0。
    只要 i 小于 n：
        d1 为 预测 的 第 i 项 减 标签 的 第 i 项。
        d2 为 标签 的 第 i 项 减 mean_y。
        残差平方和 为 残差平方和 加 d1 乘 d1。
        总平方和 为 总平方和 加 d2 乘 d2。
        i 为 i 加 1。
    完。
    若 总平方和 等于 0：
        返还 1000。
    完。
    返还 1000 减 残差平方和 乘 1000 除以 总平方和。
完。

BLEU单元 接受 cand_seq、ref_seq：
    可变 命中 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 cand_seq：
        可变 j 为 0。
        可变 已找 为 假。
        只要 j 小于 做 计数 用 ref_seq：
            若 cand_seq 的 第 i 项 等于 ref_seq 的 第 j 项：
                已找 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已找：
            命中 为 命中 加 1。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 cand_seq 等于 0：
        返还 0。
    完。
    返还 命中 乘 1000 除以 做 计数 用 cand_seq。
完。

ROUGE_L句 接受 cand_seq、ref_seq：
    m 为 做 计数 用 cand_seq。
    n 为 做 计数 用 ref_seq。
    若 m 等于 0：
        返还 0。
    完。
    若 n 等于 0：
        返还 0。
    完。
    可变 前值 为 一组：（空）。
    可变 curr 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于或等于 n：
        做 追加 用 前值、0。
        做 追加 用 curr、0。
        j 为 j 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 m：
        j 为 0。
        只要 j 小于或等于 n：
            curr 的 第 j 项 为 0。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 n：
            若 cand_seq 的 第 i 项 等于 ref_seq 的 第 j 项：
                curr 的 第 j 加 1 项 为 前值 的 第 j 项 加 1。
            否则：
                curr 的 第 j 加 1 项 为 做 最大值 用 curr 的 第 j 项、前值 的 第 j 加 1 项。
            完。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于或等于 n：
            临时 为 前值 的 第 j 项。
            前值 的 第 j 项 为 curr 的 第 j 项。
            curr 的 第 j 项 为 临时。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    lcs 为 前值 的 第 n 项。
    prec 为 lcs 乘 1000 除以 m。
    rec 为 lcs 乘 1000 除以 n。
    若 prec 加 rec 等于 0：
        返还 0。
    完。
    返还 2 乘 prec 乘 rec 除以 (prec 加 rec)。
完。

评估指标_困惑度 接受 losses：
    n 为 做 计数 用 losses。
    若 n 等于 0：
        返还 0。
    完。
    均值 为 做 求和 用 losses 除以 n。
    若 均值 小于 500：
        返还 1000 加 均值。
    完。
    若 均值 小于 2000：
        返还 1000 加 均值 加 均值 乘 均值 除以 2000。
    完。
    返还 1000 加 均值 乘 3。
完。

TopK准确率 接受 score_list、label_list、k：
    n 为 做 计数 用 label_list。
    若 n 等于 0：
        返还 0。
    完。
    可变 ok_cnt 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        tgt_score 为 score_list 的 第 i 项。
        可变 higher 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 score_list：
            若 score_list 的 第 j 项 大于 tgt_score：
                higher 为 higher 加 1。
            完。
            j 为 j 加 1。
        完。
        若 higher 小于 k：
            ok_cnt 为 ok_cnt 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 ok_cnt 乘 1000 除以 n。
完。

平均倒数排名 接受 排名列表：
    n 为 做 计数 用 排名列表。
    若 n 等于 0：
        返还 0。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 排名列表 的 第 i 项 大于 0：
            总 为 总 加 1000 除以 排名列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 总 除以 n。
完。

折损累计增益 接受 排列、k：
    可变 dcg 为 0。
    可变 i 为 0。
    上限 为 做 最小值 用 k、做 计数 用 排列。
    只要 i 小于 上限：
        若 排列 的 第 i 项 大于 0：
            可变 lg 为 做 对数2 用 i 加 2。
            若 lg 等于 0：
                lg 为 1。
            完。
            dcg 为 dcg 加 排列 的 第 i 项 乘 1000 除以 lg。
        完。
        i 为 i 加 1。
    完。
    返还 dcg。
完。

归一折损增益 接受 排列、理想排列、k：
    dcg 为 做 折损累计增益 用 排列、k。
    idcg 为 做 折损累计增益 用 理想排列、k。
    若 idcg 等于 0：
        返还 0。
    完。
    返还 dcg 乘 1000 除以 idcg。
完。

评估_样本数 接受 真实值：
    返还 做 计数 用 真实值。
完。

本章 收束。
