《图搜索》

引用《..·基础》。

引用《..·集合·图·图》。

图搜索_图_创建 接受 有向：
    返还 一组：做 字典、0、有向。
完。

图搜索_图_添加节点 接受 图、节点：
    若 做 字典取 用 图 的 第 0 项、做 转字符串 用 节点 等于 空：
        做 字典设 用 图 的 第 0 项、做 转字符串 用 节点、一组：（空）。
        图 的 第 1 项 为 图 的 第 1 项 加 1。
    完。
完。

图搜索_图_添加边 接受 图、起、终、权重：
    做 图搜索_图_添加节点 用 图、起。
    做 图搜索_图_添加节点 用 图、终。
    邻居 为 做 字典取 用 图 的 第 0 项、做 转字符串 用 起。
    做 追加 用 邻居、一组：终、权重。
    若 图 的 第 2 项 等于 0：
        反向 为 做 字典取 用 图 的 第 0 项、做 转字符串 用 终。
        做 追加 用 反向、一组：起、权重。
    完。
完。

图搜索_图_邻居 接受 图、节点：
    r 为 做 字典取 用 图 的 第 0 项、做 转字符串 用 节点。
    若 r 等于 空：
        返还 一组：（空）。
    完。
    返还 r。
完。

图_BFS 接受 图、起点：
    可变 队列 为 一组：起点。
    可变 已访问 为 一本字典：（空）。
    可变 顺序 为 一组：（空）。
    做 字典设 用 已访问、做 转字符串 用 起点、1。
    只要 做 计数 用 队列 大于 0：
        当前 为 队列 的 第 0 项。
        做 删除 用 队列。
        做 追加 用 顺序、当前。
        邻居 为 做 图搜索_图_邻居 用 图、当前。
        可变 i 为 0。
        只要 i 小于 做 计数 用 邻居：
            下 为 邻居 的 第 i 项 的 第 0 项。
            若 做 字典取 用 已访问、做 转字符串 用 下 等于 空：
                做 字典设 用 已访问、做 转字符串 用 下、1。
                做 追加 用 队列、下。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 顺序。
完。

图_DFS 接受 图、起点：
    可变 栈 为 一组：起点。
    可变 已访问 为 一本字典：（空）。
    可变 顺序 为 一组：（空）。
    只要 做 计数 用 栈 大于 0：
        当前 为 栈 的 第 做 计数 用 栈 减 1 项。
        做 删除 用 栈、做 计数 用 栈 减 1。
        若 做 字典取 用 已访问、做 转字符串 用 当前 等于 空：
            做 字典设 用 已访问、做 转字符串 用 当前、1。
            做 追加 用 顺序、当前。
            邻居 为 做 图搜索_图_邻居 用 图、当前。
            可变 i 为 做 计数 用 邻居 减 1。
            只要 i 大于或等于 0：
                做 追加 用 栈、邻居 的 第 i 项 的 第 0 项。
                i 为 i 减 1。
            完。
        完。
    完。
    返还 顺序。
完。

图_最短路径 接受 图、起点：
    可变 距离 为 做 字典。
    可变 前驱 为 做 字典。
    可变 未访问 为 一组：（空）。
    做 字典设 用 距离、做 转字符串 用 起点、0。
    做 追加 用 未访问、一组：0、起点。
    只要 做 计数 用 未访问 大于 0：
        可变 最小idx 为 0。
        可变 最小dist 为 未访问 的 第 0 项 的 第 0 项。
        可变 fi 为 1。
        只要 fi 小于 做 计数 用 未访问：
            若 未访问 的 第 fi 项 的 第 0 项 小于 最小dist：
                最小dist 为 未访问 的 第 fi 项 的 第 0 项。
                最小idx 为 fi。
            完。
            fi 为 fi 加 1。
        完。
        项 为 未访问 的 第 最小idx 项。
        做 删除 用 未访问、最小idx。
        当前 为 项 的 第 1 项。
        当前距离 为 项 的 第 0 项。
        已知 为 做 字典取 用 距离、做 转字符串 用 当前。
        若 已知 等于 空 或者 当前距离 小于或等于 已知：
            邻居 为 做 图搜索_图_邻居 用 图、当前。
            可变 i 为 0。
            只要 i 小于 做 计数 用 邻居：
                下 为 邻居 的 第 i 项 的 第 0 项。
                w 为 邻居 的 第 i 项 的 第 1 项。
                新距离 为 当前距离 加 w。
                旧 为 做 字典取 用 距离、做 转字符串 用 下。
                若 旧 等于 空 或者 新距离 小于 旧：
                    做 字典设 用 距离、做 转字符串 用 下、新距离。
                    做 字典设 用 前驱、做 转字符串 用 下、当前。
                    做 追加 用 未访问、一组：新距离、下。
                完。
                i 为 i 加 1。
            完。
        完。
    完。
    返还 一组：距离、前驱。
完。

图_重构路径 接受 前驱表、终点：
    可变 路径 为 一组：（空）。
    可变 当前 为 终点。
    只要 当前 不等于 空：
        做 插入 用 路径、0、当前。
        当前 为 做 字典取 用 前驱表、做 转字符串 用 当前。
    完。
    返还 路径。
完。

图_最短路径_邻接表 接受 邻接表、节点数、起点：
    可变 距离 为 一组：（空）。
    可变 已访问 为 一组：（空）。
    可变 前驱 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点数：
        做 追加 用 距离、999999。
        做 追加 用 已访问、假。
        做 追加 用 前驱、负 1。
        i 为 i 加 1。
    完。
    距离 的 第 起点 项 为 0。
    可变 轮 为 0。
    只要 轮 小于 节点数：
        可变 最小 为 999999。
        可变 u 为 负 1。
        i 为 0。
        只要 i 小于 节点数：
            若 非 已访问 的 第 i 项 并且 距离 的 第 i 项 小于 最小：
                最小 为 距离 的 第 i 项。
                u 为 i。
            完。
            i 为 i 加 1。
        完。
        若 u 等于 负 1：
            返还 一组：距离、前驱。
        完。
        已访问 的 第 u 项 为 真。
        若 u 小于 做 计数 用 邻接表：
            i 为 0。
            只要 i 小于 做 计数 用 邻接表 的 第 u 项：
                边 为 邻接表 的 第 u 项 的 第 i 项。
                v 为 边 的 第 0 项。
                w 为 边 的 第 1 项。
                若 距离 的 第 u 项 加 w 小于 距离 的 第 v 项：
                    距离 的 第 v 项 为 距离 的 第 u 项 加 w。
                    前驱 的 第 v 项 为 u。
                完。
                i 为 i 加 1。
            完。
        完。
        轮 为 轮 加 1。
    完。
    返还 一组：距离、前驱。
完。

图搜索_图_拓扑排序 接受 邻接表、节点数：
    可变 新队列 为 一组：（空）。
    可变 入度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点数：
        做 追加 用 入度、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 节点数：
        若 i 小于 做 计数 用 邻接表：
            可变 j 为 0。
            只要 j 小于 做 计数 用 邻接表 的 第 i 项：
                v 为 邻接表 的 第 i 项 的 第 j 项。
                入度 的 第 v 项 为 入度 的 第 v 项 加 1。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 队列 为 一组：（空）。
    i 为 0。
    只要 i 小于 节点数：
        若 入度 的 第 i 项 等于 0：
            做 追加 用 队列、i。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    只要 做 计数 用 队列 大于 0：
        u 为 队列 的 第 0 项。
        i 为 1。
        只要 i 小于 做 计数 用 队列：
            做 追加 用 新队列、队列 的 第 i 项。
            i 为 i 加 1。
        完。
        队列 为 新队列。
        做 追加 用 结果、u。
        若 u 小于 做 计数 用 邻接表：
            i 为 0。
            只要 i 小于 做 计数 用 邻接表 的 第 u 项：
                v 为 邻接表 的 第 u 项 的 第 i 项。
                入度 的 第 v 项 为 入度 的 第 v 项 减 1。
                若 入度 的 第 v 项 等于 0：
                    做 追加 用 队列、v。
                完。
                i 为 i 加 1。
            完。
        完。
    完。
    返还 结果。
完。

图_有环 接受 邻接表、节点数：
    排序 为 做 图搜索_图_拓扑排序 用 邻接表、节点数。
    返还 做 计数 用 排序 不等于 节点数。
完。

图_DFS顺序 接受 邻接表、节点数：
    可变 已访问 为 一组：（空）。
    可变 顺序 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点数：
        做 追加 用 已访问、假。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 节点数：
        若 非 已访问 的 第 i 项：
            做 图_DFS后序 用 邻接表、i、已访问、顺序。
        完。
        i 为 i 加 1。
    完。
    返还 顺序。
完。

图_DFS后序 接受 邻接表、u、已访问、顺序：
    已访问 的 第 u 项 为 真。
    若 u 小于 做 计数 用 邻接表：
        可变 i 为 0。
        只要 i 小于 做 计数 用 邻接表 的 第 u 项：
            v 为 邻接表 的 第 u 项 的 第 i 项。
            若 非 已访问 的 第 v 项：
                做 图_DFS后序 用 邻接表、v、已访问、顺序。
            完。
            i 为 i 加 1。
        完。
    完。
    做 追加 用 顺序、u。
完。

图_转置 接受 邻接表、节点数：
    可变 转置 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点数：
        做 追加 用 转置、一组：（空）。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 节点数：
        若 i 小于 做 计数 用 邻接表：
            可变 j 为 0。
            只要 j 小于 做 计数 用 邻接表 的 第 i 项：
                v 为 邻接表 的 第 i 项 的 第 j 项。
                做 追加 用 转置 的 第 v 项、i。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 转置。
完。

图_Prim 接受 邻接表权重、节点数：
    可变 已包含 为 一组：（空）。
    可变 键值 为 一组：（空）。
    可变 前驱 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点数：
        做 追加 用 已包含、假。
        做 追加 用 键值、999999。
        做 追加 用 前驱、负 1。
        i 为 i 加 1。
    完。
    键值 的 第 0 项 为 0。
    可变 总权重 为 0。
    可变 轮 为 0。
    只要 轮 小于 节点数：
        可变 最小 为 999999。
        可变 u 为 负 1。
        i 为 0。
        只要 i 小于 节点数：
            若 非 已包含 的 第 i 项 并且 键值 的 第 i 项 小于 最小：
                最小 为 键值 的 第 i 项。
                u 为 i。
            完。
            i 为 i 加 1。
        完。
        若 u 等于 负 1：
            返还 一组：总权重、前驱。
        完。
        已包含 的 第 u 项 为 真。
        总权重 为 总权重 加 键值 的 第 u 项。
        若 u 小于 做 计数 用 邻接表权重：
            i 为 0。
            只要 i 小于 做 计数 用 邻接表权重 的 第 u 项：
                边 为 邻接表权重 的 第 u 项 的 第 i 项。
                v 为 边 的 第 0 项。
                w 为 边 的 第 1 项。
                若 非 已包含 的 第 v 项 并且 w 小于 键值 的 第 v 项：
                    键值 的 第 v 项 为 w。
                    前驱 的 第 v 项 为 u。
                完。
                i 为 i 加 1。
            完。
        完。
        轮 为 轮 加 1。
    完。
    返还 一组：总权重、前驱。
完。

可变 _图_搜索次数 为 0。

可变 _图_最大节点 为 0。

可变 _图_最大边 为 0。

图_记录搜索 接受 节点数：
    _图_搜索次数 为 _图_搜索次数 加 1。
    若 节点数 大于 _图_最大节点：
        _图_最大节点 为 节点数。
    完。
完。

环队_创建 接受 容量：
    可变 缓冲 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 缓冲、空。
        i 为 i 加 1。
    完。
    返还 一组：缓冲、0、容量。
完。

环队_为空 接受 q：
    返还 q 的 第 1 项 等于 q 的 第 2 项。
完。

环队_入队 接受 q、v：
    q 的 第 0 项 的 第 q 的 第 2 项 项 为 v。
    q 的 第 2 项 为 (q 的 第 2 项 加 1) 取余于 q 的 第 3 项。
完。

环队_出队 接受 q：
    v 为 q 的 第 0 项 的 第 q 的 第 1 项 项。
    q 的 第 1 项 为 (q 的 第 1 项 加 1) 取余于 q 的 第 3 项。
    返还 v。
完。

图_BFS高效 接受 图、起点、最大节点数：
    可变 容量 为 1024。
    若 最大节点数 大于 0：
        容量 为 最大节点数 加 1。
    完。
    q 为 做 环队_创建 用 容量。
    可变 已访问 为 做 字典。
    可变 顺序 为 一组：（空）。
    可变 层次 为 做 字典。
    做 环队_入队 用 q、起点。
    做 字典设 用 已访问、做 转字符串 用 起点、1。
    做 字典设 用 层次、做 转字符串 用 起点、0。
    只要 非 做 环队_为空 用 q：
        当前 为 做 环队_出队 用 q。
        做 追加 用 顺序、当前。
        可变 当前层 为 做 字典取 用 层次、做 转字符串 用 当前。
        若 当前层 等于 空：
            当前层 为 0。
        完。
        邻居 为 做 图搜索_图_邻居 用 图、当前。
        可变 i 为 0。
        只要 i 小于 做 计数 用 邻居：
            下 为 邻居 的 第 i 项 的 第 0 项。
            若 做 字典取 用 已访问、做 转字符串 用 下 等于 空：
                做 字典设 用 已访问、做 转字符串 用 下、1。
                做 字典设 用 层次、做 转字符串 用 下、当前层 加 1。
                做 环队_入队 用 q、下。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 一组：顺序、层次。
完。

图_BFS最短距离 接受 图、起点、终点、最大节点数：
    结果 为 做 图_BFS高效 用 图、起点、最大节点数。
    层次 为 结果 的 第 1 项。
    距离 为 做 字典取 用 层次、做 转字符串 用 终点。
    若 距离 等于 空：
        返还 0 减 1。
    完。
    返还 距离。
完。

本章 收束。
