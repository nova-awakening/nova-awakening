《最短路径》

引用《..·基础》。

引用《..·数据结构·并查集》。

加权图_创建 接受 节点数：
    返还 一组：节点数、一组：（空）。
完。

加权图_加边 接受 图、源、目标、权重：
    做 追加 用 图 的 第 1 项、一组：源、目标、权重。
完。

路径_无穷 为 999999999。

迪杰斯特拉 接受 图、起点：
    n 为 图 的 第 0 项。
    边 为 图 的 第 1 项。
    可变 距离 为 一组：（空）。
    可变 已访问 为 一组：（空）。
    可变 前驱 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 距离、路径_无穷。
        做 追加 用 已访问、0。
        做 追加 用 前驱、负 1。
        i 为 i 加 1。
    完。
    距离 的 第 起点 项 为 0。
    可变 轮 为 0。
    只要 轮 小于 n：
        可变 u 为 负 1。
        可变 最小距 为 路径_无穷。
        i 为 0。
        只要 i 小于 n：
            若 已访问 的 第 i 项 等于 0 并且 距离 的 第 i 项 小于 最小距：
                最小距 为 距离 的 第 i 项。
                u 为 i。
            完。
            i 为 i 加 1。
        完。
        若 u 等于 负 1：
            轮 为 n。
        否则：
            已访问 的 第 u 项 为 1。
            i 为 0。
            只要 i 小于 做 计数 用 边：
                e 为 边 的 第 i 项。
                若 e 的 第 0 项 等于 u 并且 已访问 的 第 e 的 第 1 项 项 等于 0：
                    新距 为 距离 的 第 u 项 加 e 的 第 2 项。
                    若 新距 小于 距离 的 第 e 的 第 1 项 项：
                        距离 的 第 e 的 第 1 项 项 为 新距。
                        前驱 的 第 e 的 第 1 项 项 为 u。
                    完。
                完。
                i 为 i 加 1。
            完。
        完。
        轮 为 轮 加 1。
    完。
    返还 一组：距离、前驱。
完。

最短路径_floyd_warshall 接受 图：
    可变 矩阵 为 一组：（空）。
    n 为 图 的 第 0 项。
    边 为 图 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 n：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 n：
            若 i 等于 j：
                做 追加 用 行、0。
            否则：
                做 追加 用 行、路径_无穷。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 矩阵、行。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 边：
        e 为 边 的 第 i 项。
        行r 为 矩阵 的 第 e 的 第 0 项 项。
        行r 的 第 e 的 第 1 项 项 为 e 的 第 2 项。
        i 为 i 加 1。
    完。
    可变 k 为 0。
    只要 k 小于 n：
        i 为 0。
        只要 i 小于 n：
            可变 j 为 0。
            行i 为 矩阵 的 第 i 项。
            行k 为 矩阵 的 第 k 项。
            只要 j 小于 n：
                经k 为 行i 的 第 k 项 加 行k 的 第 j 项。
                若 经k 小于 行i 的 第 j 项：
                    行i 的 第 j 项 为 经k。
                完。
                j 为 j 加 1。
            完。
            i 为 i 加 1。
        完。
        k 为 k 加 1。
    完。
    返还 矩阵。
完。

拓扑排序 接受 图：
    n 为 图 的 第 0 项。
    边 为 图 的 第 1 项。
    可变 入度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 入度、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 边：
        e 为 边 的 第 i 项。
        入度 的 第 e 的 第 1 项 项 为 入度 的 第 e 的 第 1 项 项 加 1。
        i 为 i 加 1。
    完。
    可变 队列 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        若 入度 的 第 i 项 等于 0：
            做 追加 用 队列、i。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    可变 前指 为 0。
    只要 前指 小于 做 计数 用 队列：
        u 为 队列 的 第 前指 项。
        前指 为 前指 加 1。
        做 追加 用 结果、u。
        i 为 0。
        只要 i 小于 做 计数 用 边：
            e 为 边 的 第 i 项。
            若 e 的 第 0 项 等于 u：
                入度 的 第 e 的 第 1 项 项 为 入度 的 第 e 的 第 1 项 项 减 1。
                若 入度 的 第 e 的 第 1 项 项 等于 0：
                    做 追加 用 队列、e 的 第 1 项。
                完。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

克鲁斯卡尔 接受 图：
    n 为 图 的 第 0 项。
    可变 边 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 图 的 第 1 项：
        做 追加 用 边、图 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 已改变 为 1。
    只要 已改变 等于 1：
        已改变 为 0。
        i 为 0。
        只要 i 小于 做 计数 用 边 减 1：
            若 边 的 第 i 项 的 第 2 项 大于 边 的 第 i 加 1 项 的 第 2 项：
                临时 为 边 的 第 i 项。
                边 的 第 i 项 为 边 的 第 i 加 1 项。
                边 的 第 i 加 1 项 为 临时。
                已改变 为 1。
            完。
            i 为 i 加 1。
        完。
    完。
    集 为 做 uf_创建 用 n。
    可变 mst 为 一组：（空）。
    可变 总权 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 边：
        e 为 边 的 第 i 项。
        若 做 uf_连通 用 集、e 的 第 0 项、e 的 第 1 项 等于 0：
            做 uf_合并 用 集、e 的 第 0 项、e 的 第 1 项。
            做 追加 用 mst、e。
            总权 为 总权 加 e 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：mst、总权。
完。

贝尔曼福特 接受 图、源：
    n 为 图 的 第 0 项。
    边列表 为 图 的 第 1 项。
    可变 距离 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 距离、路径_无穷。
        i 为 i 加 1。
    完。
    距离 的 第 源 项 为 0。
    i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 0。
        只要 j 小于 做 计数 用 边列表：
            e 为 边列表 的 第 j 项。
            若 距离 的 第 e 的 第 0 项 项 不等于 路径_无穷 并且 距离 的 第 e 的 第 0 项 项 加 e 的 第 2 项 小于 距离 的 第 e 的 第 1 项 项：
                距离 的 第 e 的 第 1 项 项 为 距离 的 第 e 的 第 0 项 项 加 e 的 第 2 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 有负环 为 假。
    可变 k 为 0。
    只要 k 小于 做 计数 用 边列表：
        e 为 边列表 的 第 k 项。
        若 距离 的 第 e 的 第 0 项 项 不等于 路径_无穷 并且 距离 的 第 e 的 第 0 项 项 加 e 的 第 2 项 小于 距离 的 第 e 的 第 1 项 项：
            有负环 为 真。
        完。
        k 为 k 加 1。
    完。
    返还 一组：距离、有负环。
完。

最短路径_弗洛伊德最短路 接受 n、邻接矩阵：
    可变 距离 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 n：
            做 追加 用 行、邻接矩阵 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 距离、行。
        i 为 i 加 1。
    完。
    可变 k 为 0。
    只要 k 小于 n：
        i 为 0。
        只要 i 小于 n：
            可变 j 为 0。
            只要 j 小于 n：
                若 距离 的 第 i 项 的 第 k 项 不等于 路径_无穷 并且 距离 的 第 k 项 的 第 j 项 不等于 路径_无穷：
                    若 距离 的 第 i 项 的 第 k 项 加 距离 的 第 k 项 的 第 j 项 小于 距离 的 第 i 项 的 第 j 项：
                        距离 的 第 i 项 的 第 j 项 为 距离 的 第 i 项 的 第 k 项 加 距离 的 第 k 项 的 第 j 项。
                    完。
                完。
                j 为 j 加 1。
            完。
            i 为 i 加 1。
        完。
        k 为 k 加 1。
    完。
    返还 距离。
完。

本章 收束。
