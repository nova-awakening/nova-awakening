《图算法》

引用《..·基础》。

引用《..·集合·图·图》。

可变 图_邻接 为 一组：（空）。

可变 图_权重 为 一组：（空）。

可变 图_节点数 为 0。

图_初始化 接受 n：
    图_节点数 为 n。
    图_邻接 为 一组：（空）。
    图_权重 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 图_邻接、一组：（空）。
        做 追加 用 图_权重、一组：（空）。
        i 为 i 加 1。
    完。
完。

图算法_图_添加边 接受 从、到、权：
    做 追加 用 图_邻接 的 第 从 项、到。
    做 追加 用 图_权重 的 第 从 项、权。
完。

图_添加双向边 接受 a、b、权：
    做 图算法_图_添加边 用 a、b、权。
    做 图算法_图_添加边 用 b、a、权。
完。

图_出度 接受 节点：
    返还 做 计数 用 图_邻接 的 第 节点 项。
完。

图_入度 接受 节点：
    可变 度 为 0。
    可变 i 为 0。
    只要 i 小于 图_节点数：
        可变 j 为 0。
        只要 j 小于 做 计数 用 图_邻接 的 第 i 项：
            若 图_邻接 的 第 i 项 的 第 j 项 等于 节点：
                度 为 度 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 度。
完。

广度优先 接受 起点：
    可变 访问 为 一组：（空）。
    可变 新队列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 图_节点数：
        做 追加 用 访问、假。
        i 为 i 加 1。
    完。
    可变 队列 为 一组：起点。
    可变 顺序 为 一组：（空）。
    访问 的 第 起点 项 为 真。
    只要 做 计数 用 队列 大于 0：
        当前 为 队列 的 第 0 项。
        i 为 1。
        只要 i 小于 做 计数 用 队列：
            做 追加 用 新队列、队列 的 第 i 项。
            i 为 i 加 1。
        完。
        队列 为 新队列。
        做 追加 用 顺序、当前。
        i 为 0。
        只要 i 小于 做 计数 用 图_邻接 的 第 当前 项：
            邻居 为 图_邻接 的 第 当前 项 的 第 i 项。
            若 访问 的 第 邻居 项 等于 假：
                访问 的 第 邻居 项 为 真。
                做 追加 用 队列、邻居。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 顺序。
完。

可变 DFS_访问 为 一组：（空）。

可变 DFS_顺序 为 一组：（空）。

DFS_递归 接受 节点：
    DFS_访问 的 第 节点 项 为 真。
    做 追加 用 DFS_顺序、节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 图_邻接 的 第 节点 项：
        邻居 为 图_邻接 的 第 节点 项 的 第 i 项。
        若 DFS_访问 的 第 邻居 项 等于 假：
            做 DFS_递归 用 邻居。
        完。
        i 为 i 加 1。
    完。
完。

深度优先 接受 起点：
    DFS_访问 为 一组：（空）。
    DFS_顺序 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 图_节点数：
        做 追加 用 DFS_访问、假。
        i 为 i 加 1。
    完。
    做 DFS_递归 用 起点。
    返还 DFS_顺序。
完。

图算法_迪杰斯特拉 接受 起点：
    可变 距离 为 一组：（空）。
    可变 访问 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 图_节点数：
        做 追加 用 距离、0x7FFFFFFF。
        做 追加 用 访问、假。
        i 为 i 加 1。
    完。
    距离 的 第 起点 项 为 0。
    可变 步 为 0。
    只要 步 小于 图_节点数：
        可变 最小d 为 0x7FFFFFFF。
        可变 u 为 负 1。
        i 为 0。
        只要 i 小于 图_节点数：
            若 访问 的 第 i 项 等于 假 并且 距离 的 第 i 项 小于 最小d：
                最小d 为 距离 的 第 i 项。
                u 为 i。
            完。
            i 为 i 加 1。
        完。
        若 u 等于 负 1：
            步 为 图_节点数。
        否则：
            访问 的 第 u 项 为 真。
            i 为 0。
            只要 i 小于 做 计数 用 图_邻接 的 第 u 项：
                v 为 图_邻接 的 第 u 项 的 第 i 项。
                新距 为 距离 的 第 u 项 加 图_权重 的 第 u 项 的 第 i 项。
                若 新距 小于 距离 的 第 v 项：
                    距离 的 第 v 项 为 新距。
                完。
                i 为 i 加 1。
            完。
        完。
        步 为 步 加 1。
    完。
    返还 距离。
完。

图算法_拓扑排序 接受 零参：
    可变 入度表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 图_节点数：
        做 追加 用 入度表、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 图_节点数：
        可变 j 为 0。
        只要 j 小于 做 计数 用 图_邻接 的 第 i 项：
            入度表 的 第 图_邻接 的 第 i 项 的 第 j 项 项 为 入度表 的 第 图_邻接 的 第 i 项 的 第 j 项 项 加 1。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 队列 为 一组：（空）。
    i 为 0。
    只要 i 小于 图_节点数：
        若 入度表 的 第 i 项 等于 0：
            做 追加 用 队列、i。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    只要 做 计数 用 队列 大于 0：
        u 为 队列 的 第 0 项。
        可变 新队 为 一组：（空）。
        i 为 1。
        只要 i 小于 做 计数 用 队列：
            做 追加 用 新队、队列 的 第 i 项。
            i 为 i 加 1。
        完。
        队列 为 新队。
        做 追加 用 结果、u。
        i 为 0。
        只要 i 小于 做 计数 用 图_邻接 的 第 u 项：
            v 为 图_邻接 的 第 u 项 的 第 i 项。
            入度表 的 第 v 项 为 入度表 的 第 v 项 减 1。
            若 入度表 的 第 v 项 等于 0：
                做 追加 用 队列、v。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

图_节点总数 接受 零参：
    返还 图_节点数。
完。

图_路径存在 接受 从、到：
    可变 访问 为 一组：（空）。
    可变 新栈 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 图_节点数：
        做 追加 用 访问、假。
        i 为 i 加 1。
    完。
    可变 栈 为 一组：从。
    访问 的 第 从 项 为 真。
    只要 做 计数 用 栈 大于 0：
        当前 为 栈 的 第 做 计数 用 栈 减 1 项。
        i 为 0。
        只要 i 小于 做 计数 用 栈 减 1：
            做 追加 用 新栈、栈 的 第 i 项。
            i 为 i 加 1。
        完。
        栈 为 新栈。
        若 当前 等于 到：
            返还 真。
        完。
        i 为 0。
        只要 i 小于 做 计数 用 图_邻接 的 第 当前 项：
            邻居 为 图_邻接 的 第 当前 项 的 第 i 项。
            若 访问 的 第 邻居 项 等于 假：
                访问 的 第 邻居 项 为 真。
                做 追加 用 栈、邻居。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 假。
完。

图_边数 接受 零参：
    可变 计数值 为 0。
    可变 i 为 0。
    只要 i 小于 图_节点数：
        计数值 为 计数值 加 做 计数 用 图_邻接 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 计数值。
完。

图_密度 接受 _扩1：
    若 图_节点数 小于 2：
        返还 0。
    完。
    返还 做 图_边数 乘 1000 除以 (图_节点数 乘 (图_节点数 减 1))。
完。

图_气韵度 接受 节点：
    若 节点 小于 0 或者 节点 大于或等于 图_节点数：
        返还 0。
    完。
    返还 做 计数 用 图_邻接 的 第 节点 项。
完。

图_气韵分布 接受 零参：
    可变 分布 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 图_节点数：
        做 追加 用 分布、做 计数 用 图_邻接 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 分布。
完。

图_五行分量 接受 零参：
    可变 访问 为 一组：（空）。
    可变 新队列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 图_节点数：
        做 追加 用 访问、0。
        i 为 i 加 1。
    完。
    可变 分量数 为 0。
    i 为 0。
    只要 i 小于 图_节点数：
        若 访问 的 第 i 项 等于 0：
            可变 队列 为 一组：i。
            访问 的 第 i 项 为 1。
            只要 做 计数 用 队列 大于 0：
                当前 为 队列 的 第 0 项。
                可变 qi 为 1。
                只要 qi 小于 做 计数 用 队列：
                    做 追加 用 新队列、队列 的 第 qi 项。
                    qi 为 qi 加 1。
                完。
                队列 为 新队列。
                可变 j 为 0。
                只要 j 小于 做 计数 用 图_邻接 的 第 当前 项：
                    邻 为 图_邻接 的 第 当前 项 的 第 j 项。
                    若 访问 的 第 邻 项 等于 0：
                        访问 的 第 邻 项 为 1。
                        做 追加 用 队列、邻。
                    完。
                    j 为 j 加 1。
                完。
            完。
            分量数 为 分量数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 分量数。
完。

图_阴阳验证 接受 零参：
    _边数 为 做 图_边数。
    可变 有效 为 1。
    可变 i 为 0。
    只要 i 小于 图_节点数：
        可变 j 为 0。
        只要 j 小于 做 计数 用 图_邻接 的 第 i 项：
            邻 为 图_邻接 的 第 i 项 的 第 j 项。
            若 邻 小于 0 或者 邻 大于或等于 图_节点数：
                有效 为 0。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 有效。
完。

图_构建容量矩阵 接受 n：
    可变 容量 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 n：
            做 追加 用 行、0。
            j 为 j 加 1。
        完。
        做 追加 用 容量、行。
        i 为 i 加 1。
    完。
    返还 容量。
完。

图_容量矩阵_添加边 接受 容量、u、v、增量：
    容量 的 第 u 项 的 第 v 项 为 容量 的 第 u 项 的 第 v 项 加 增量。
完。

图_BFS增广 接受 容量、n、源、汇、父节点：
    可变 visited 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 visited、假。
        i 为 i 加 1。
    完。
    visited 的 第 源 项 为 真。
    父节点 的 第 源 项 为 负 1。
    可变 q 为 一组：源。
    可变 head 为 0。
    只要 head 小于 做 计数 用 q：
        u 为 q 的 第 head 项。
        head 为 head 加 1。
        可变 v 为 0。
        只要 v 小于 n：
            若 非 visited 的 第 v 项 并且 容量 的 第 u 项 的 第 v 项 大于 0：
                visited 的 第 v 项 为 真。
                父节点 的 第 v 项 为 u。
                若 v 等于 汇：
                    返还 真。
                完。
                做 追加 用 q、v。
            完。
            v 为 v 加 1。
        完。
    完。
    返还 假。
完。

图_最大流 接受 cap_orig、n、源、汇：
    可变 容量 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 n：
            做 追加 用 行、cap_orig 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 容量、行。
        i 为 i 加 1。
    完。
    可变 父节点 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 父节点、负 1。
        i 为 i 加 1。
    完。
    可变 flow 为 0。
    只要 做 图_BFS增广 用 容量、n、源、汇、父节点：
        可变 path_flow 为 999999。
        可变 v 为 汇。
        只要 v 不等于 源：
            u 为 父节点 的 第 v 项。
            若 容量 的 第 u 项 的 第 v 项 小于 path_flow：
                path_flow 为 容量 的 第 u 项 的 第 v 项。
            完。
            v 为 u。
        完。
        v 为 汇。
        只要 v 不等于 源：
            u 为 父节点 的 第 v 项。
            容量 的 第 u 项 的 第 v 项 为 容量 的 第 u 项 的 第 v 项 减 path_flow。
            容量 的 第 v 项 的 第 u 项 为 容量 的 第 v 项 的 第 u 项 加 path_flow。
            v 为 u。
        完。
        flow 为 flow 加 path_flow。
        i 为 0。
        只要 i 小于 n：
            父节点 的 第 i 项 为 负 1。
            i 为 i 加 1。
        完。
    完。
    返还 flow。
完。

本章 收束。
