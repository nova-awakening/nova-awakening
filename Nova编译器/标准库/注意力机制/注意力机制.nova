《注意力机制》

引用《..·基础》。

引用《..·数学·基础·数学库》。

引用《..·数学·注意力》。

引用《..·算法·数学扩展》。

稳定归一化指数 接受 分数列表：
    若 做 计数 用 分数列表 等于 0：
        返还 一组：（空）。
    完。
    可变 最大值 为 分数列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 分数列表：
        若 分数列表 的 第 i 项 大于 最大值：
            最大值 为 分数列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 exp_vals 为 一组：（空）。
    可变 总和 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 分数列表：
        x 为 分数列表 的 第 i 项 减 最大值。
        可变 e 为 0。
        若 x 大于或等于 负 1：
            e 为 1000 减 x 乘 负 1000 除以 1000。
        否则若 x 大于或等于 负 5：
            e 为 1000 加 x 乘 1000 除以 1000 加 x 乘 x 乘 500 除以 1000000。
        否则若 x 大于或等于 负 10：
            e 为 3。
            若 x 大于或等于 负 7：
                e 为 9。
            完。
        否则：
            e 为 1。
        完。
        做 追加 用 exp_vals、e。
        总和 为 总和 加 e。
        i 为 i 加 1。
    完。
    若 总和 等于 0：
        总和 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 exp_vals：
        做 追加 用 结果、exp_vals 的 第 i 项 乘 1000 除以 总和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

温度归一化指数 接受 分数列表、温度100：
    若 温度100 小于或等于 0：
        温度100 为 100。
    完。
    可变 调节分数 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分数列表：
        做 追加 用 调节分数、分数列表 的 第 i 项 乘 100 除以 温度100。
        i 为 i 加 1。
    完。
    返还 做 稳定归一化指数 用 调节分数。
完。

点积 接受 a、b、维度：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        总和 为 总和 加 a 的 第 i 项 乘 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总和。
完。

注意力机_缩放点积注意力 接受 Q、K、V、序列长度、维度：
    可变 总权 为 0。
    可变 scale_root 为 做 整数sqrt 用 维度。
    若 scale_root 等于 0：
        scale_root 为 1。
    完。
    可变 分数 为 一组：（空）。
    可变 qi 为 0。
    只要 qi 小于 序列长度：
        可变 ki 为 0。
        只要 ki 小于 序列长度：
            可变 点积 为 0。
            可变 d 为 0。
            只要 d 小于 维度：
                点积 为 点积 加 Q 的 第 qi 乘 维度 加 d 项 乘 K 的 第 ki 乘 维度 加 d 项。
                d 为 d 加 1。
            完。
            做 追加 用 分数、点积 除以 scale_root。
            ki 为 ki 加 1。
        完。
        qi 为 qi 加 1。
    完。
    可变 输出 为 一组：（空）。
    qi 为 0。
    只要 qi 小于 序列长度：
        可变 最大 为 分数 的 第 qi 乘 序列长度 项。
        可变 j 为 1。
        只要 j 小于 序列长度：
            若 分数 的 第 qi 乘 序列长度 加 j 项 大于 最大：
                最大 为 分数 的 第 qi 乘 序列长度 加 j 项。
            完。
            j 为 j 加 1。
        完。
        可变 权重 为 一组：（空）。
        j 为 0。
        只要 j 小于 序列长度：
            差值 为 分数 的 第 qi 乘 序列长度 加 j 项 减 最大。
            可变 w 为 100 加 差值。
            若 w 小于 1：
                w 为 1。
            完。
            做 追加 用 权重、w。
            总权 为 总权 加 w。
            j 为 j 加 1。
        完。
        若 总权 等于 0：
            总权 为 1。
        完。
        可变 d 为 0。
        只要 d 小于 维度：
            可变 数值 为 0。
            j 为 0。
            只要 j 小于 序列长度：
                数值 为 数值 加 权重 的 第 j 项 乘 V 的 第 j 乘 维度 加 d 项 除以 总权。
                j 为 j 加 1。
            完。
            做 追加 用 输出、数值。
            d 为 d 加 1。
        完。
        qi 为 qi 加 1。
    完。
    返还 输出。
完。

注意力机_多头注意力 接受 Q、K、V、序列长度、维度、头数：
    可变 oi 为 0。
    头维 为 维度 除以 头数。
    可变 输出 为 一组：（空）。
    可变 h 为 0。
    只要 h 小于 头数：
        可变 Qh 为 一组：（空）。
        可变 Kh 为 一组：（空）。
        可变 Vh 为 一组：（空）。
        可变 s 为 0。
        只要 s 小于 序列长度：
            可变 d 为 0。
            只要 d 小于 头维：
                做 追加 用 Qh、Q 的 第 s 乘 维度 加 h 乘 头维 加 d 项。
                做 追加 用 Kh、K 的 第 s 乘 维度 加 h 乘 头维 加 d 项。
                做 追加 用 Vh、V 的 第 s 乘 维度 加 h 乘 头维 加 d 项。
                d 为 d 加 1。
            完。
            s 为 s 加 1。
        完。
        头输出 为 做 缩放点积注意力 用 Qh、Kh、Vh、序列长度、头维。
        只要 oi 小于 做 计数 用 头输出：
            做 追加 用 输出、头输出 的 第 oi 项。
            oi 为 oi 加 1。
        完。
        h 为 h 加 1。
    完。
    返还 输出。
完。

注意力机_因果掩码 接受 序列长度：
    可变 掩码 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 j 为 0。
        只要 j 小于 序列长度：
            若 j 小于或等于 i：
                做 追加 用 掩码、0。
            否则：
                做 追加 用 掩码、负 99999。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 掩码。
完。

滑动窗口掩码 接受 序列长度、窗口大小：
    可变 掩码 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 j 为 0。
        只要 j 小于 序列长度：
            若 做 绝对值 用 i 减 j 小于或等于 窗口大小 并且 j 小于或等于 i：
                做 追加 用 掩码、0。
            否则：
                做 追加 用 掩码、负 99999。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 掩码。
完。

多头_分割 接受 向量、头数：
    维度 为 做 计数 用 向量。
    头维度 为 维度 除以 头数。
    可变 头列表 为 一组：（空）。
    可变 h 为 0。
    只要 h 小于 头数：
        可变 头 为 一组：（空）。
        可变 i 为 h 乘 头维度。
        只要 i 小于 (h 加 1) 乘 头维度 并且 i 小于 维度：
            做 追加 用 头、向量 的 第 i 项。
            i 为 i 加 1。
        完。
        做 追加 用 头列表、头。
        h 为 h 加 1。
    完。
    返还 头列表。
完。

多头_合并 接受 头列表：
    可变 结果 为 一组：（空）。
    可变 h 为 0。
    只要 h 小于 做 计数 用 头列表：
        可变 i 为 0。
        只要 i 小于 做 计数 用 头列表 的 第 h 项：
            做 追加 用 结果、头列表 的 第 h 项 的 第 i 项。
            i 为 i 加 1。
        完。
        h 为 h 加 1。
    完。
    返还 结果。
完。

多头_注意力 接受 Q列表、K列表、V列表、头数：
    可变 头输出 为 一组：（空）。
    可变 h 为 0。
    只要 h 小于 头数：
        Qh 为 Q列表 的 第 h 项。
        Kh 为 K列表 的 第 h 项。
        Vh 为 V列表 的 第 h 项。
        维度 为 做 计数 用 Qh。
        可变 分数 为 0。
        可变 i 为 0。
        只要 i 小于 维度：
            分数 为 分数 加 Qh 的 第 i 项 乘 Kh 的 第 i 项。
            i 为 i 加 1。
        完。
        若 维度 大于 0：
            分数 为 分数 乘 1000 除以 (维度 乘 32)。
        完。
        可变 输出 为 一组：（空）。
        i 为 0。
        只要 i 小于 做 计数 用 Vh：
            做 追加 用 输出、Vh 的 第 i 项 乘 分数 除以 1000。
            i 为 i 加 1。
        完。
        做 追加 用 头输出、输出。
        h 为 h 加 1。
    完。
    返还 做 多头_合并 用 头输出。
完。

交叉注意力_计算 接受 查询、键序列、值序列、序列长度、维度：
    可变 分数列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 分数 为 0。
        可变 d 为 0。
        只要 d 小于 维度：
            分数 为 分数 加 查询 的 第 d 项 乘 键序列 的 第 i 乘 维度 加 d 项。
            d 为 d 加 1。
        完。
        若 维度 大于 0：
            分数 为 分数 乘 1000 除以 (维度 乘 32)。
        完。
        做 追加 用 分数列表、分数。
        i 为 i 加 1。
    完。
    可变 最大 为 分数列表 的 第 0 项。
    i 为 1。
    只要 i 小于 序列长度：
        若 分数列表 的 第 i 项 大于 最大：
            最大 为 分数列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 总 为 0。
    i 为 0。
    只要 i 小于 序列长度：
        分数列表 的 第 i 项 为 分数列表 的 第 i 项 减 最大 加 1000。
        若 分数列表 的 第 i 项 小于 0：
            分数列表 的 第 i 项 为 1。
        完。
        总 为 总 加 分数列表 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 结果、0。
        d 为 d 加 1。
    完。
    i 为 0。
    只要 i 小于 序列长度：
        权重 为 分数列表 的 第 i 项 乘 1000 除以 (总 加 1)。
        d 为 0。
        只要 d 小于 维度：
            结果 的 第 d 项 为 结果 的 第 d 项 加 值序列 的 第 i 乘 维度 加 d 项 乘 权重 除以 1000。
            d 为 d 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

位置_正弦编码 接受 位置、维度：
    可变 编码 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        频率 为 位置 乘 1000 除以 (1 加 i 乘 100)。
        若 i 取余于 2 等于 0：
            做 追加 用 编码、频率 取余于 2000 减 1000。
        否则：
            做 追加 用 编码、(频率 加 500) 取余于 2000 减 1000。
        完。
        i 为 i 加 1。
    完。
    返还 编码。
完。

位置_旋转编码 接受 向量、位置、维度：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度 除以 2：
        频率 为 位置 乘 1000 除以 (1 加 i 乘 200)。
        cos近似 为 1000 减 频率 取余于 1000 乘 (频率 取余于 1000) 除以 2000。
        sin近似 为 频率 取余于 1000。
        做 追加 用 结果、向量 的 第 i 乘 2 项 乘 cos近似 除以 1000 减 向量 的 第 i 乘 2 加 1 项 乘 sin近似 除以 1000。
        做 追加 用 结果、向量 的 第 i 乘 2 项 乘 sin近似 除以 1000 加 向量 的 第 i 乘 2 加 1 项 乘 cos近似 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

注意力_全局池化 接受 序列、序列长度、维度：
    可变 结果 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 结果、0。
        d 为 d 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 序列长度：
        d 为 0。
        只要 d 小于 维度：
            结果 的 第 d 项 为 结果 的 第 d 项 加 序列 的 第 i 乘 维度 加 d 项。
            d 为 d 加 1。
        完。
        i 为 i 加 1。
    完。
    d 为 0。
    只要 d 小于 维度：
        结果 的 第 d 项 为 结果 的 第 d 项 除以 序列长度。
        d 为 d 加 1。
    完。
    返还 结果。
完。

注意力_最大池化 接受 序列、序列长度、维度：
    可变 结果 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 结果、负 99999。
        d 为 d 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 序列长度：
        d 为 0。
        只要 d 小于 维度：
            若 序列 的 第 i 乘 维度 加 d 项 大于 结果 的 第 d 项：
                结果 的 第 d 项 为 序列 的 第 i 乘 维度 加 d 项。
            完。
            d 为 d 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _注意力_计算次数 为 0。

可变 _注意力_最大序列 为 0。

可变 _注意力_头数记录 为 0。

注意力_记录计算 接受 序列长度、头数：
    _注意力_计算次数 为 _注意力_计算次数 加 1。
    若 序列长度 大于 _注意力_最大序列：
        _注意力_最大序列 为 序列长度。
    完。
    若 头数 大于 _注意力_头数记录：
        _注意力_头数记录 为 头数。
    完。
完。

注意力_性能评估 接受 零参：
    若 _注意力_最大序列 大于 4096：
        返还 「长序列(使用稀疏注意力)」。
    完。
    若 _注意力_头数记录 大于 64：
        返还 「头数过多(检查模型配置)」。
    完。
    返还 「注意力计算正常」。
完。

本章 收束。
