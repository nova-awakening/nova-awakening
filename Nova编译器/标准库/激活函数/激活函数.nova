《激活函数》

引用《..·数学·基础·数学库》。

引用《..·基础》。

引用《..·..·运行时·原语桩》。

引用《..·数学·工具》。

Q16_指数 接受 x：
    若 x 小于 负 6000：
        返还 2。
    完。
    若 x 大于 6000：
        返还 403000。
    完。
    若 x 大于 1000：
        一半 为 做 Q16_指数 用 x 除以 2。
        返还 一半 乘 一半 除以 1000。
    完。
    若 x 小于 负 1000：
        一半 为 做 Q16_指数 用 x 除以 2。
        返还 一半 乘 一半 除以 1000。
    完。
    t1 为 x。
    t2 为 x 乘 x 除以 2000。
    t3 为 x 乘 x 除以 1000 乘 x 除以 6000。
    可变 结果 为 1000 加 t1 加 t2 加 t3。
    若 结果 小于 1：
        返还 1。
    完。
    返还 结果。
完。

Q16_双曲正切 接受 x：
    若 x 小于 负 3000：
        返还 负 1000。
    完。
    若 x 大于 3000：
        返还 1000。
    完。
    若 做 绝对值 用 x 小于 500：
        返还 x 减 x 乘 x 乘 x 除以 3000000。
    完。
    e2x 为 做 Q16_指数 用 2 乘 x。
    分母 为 e2x 加 1000。
    若 分母 等于 0：
        返还 1000。
    完。
    返还 (e2x 减 1000) 乘 1000 除以 分母。
完。

Q16_对数 接受 x：
    若 x 小于 1：
        返还 负 6908。
    完。
    若 x 大于 50000：
        返还 10820。
    完。
    y 为 x 减 1000。
    返还 y 乘 1000 除以 1000 减 y 乘 y 除以 2000000。
完。

激活_线性整流 接受 x：
    返还 做 最大值 用 x、0。
完。

激活_线性整流6 接受 x：
    返还 做 最小值 用 做 最大值 用 x、0、6000。
完。

激活_平方线性整流 接受 x：
    r 为 做 最大值 用 x、0。
    返还 r 乘 r 除以 1000。
完。

激活_泄露线性 接受 x、斜率系数：
    若 x 大于或等于 0：
        返还 x。
    完。
    返还 x 乘 斜率系数 除以 1000。
完。

激活_参数线性整流 接受 x、alpha_vec、ch：
    若 x 大于或等于 0：
        返还 x。
    完。
    返还 x 乘 alpha_vec 的 第 ch 项 除以 1000。
完。

激活_指数线性 接受 x、斜率系数：
    若 x 大于或等于 0：
        返还 x。
    完。
    返还 (做 Q16_指数 用 x 减 1000) 乘 斜率系数 除以 1000。
完。

激活_连续指数线性 接受 x、斜率系数：
    若 x 大于或等于 0：
        返还 x。
    完。
    若 斜率系数 小于 1：
        斜率系数 为 1。
    完。
    返还 (做 Q16_指数 用 x 乘 1000 除以 斜率系数 减 1000) 乘 斜率系数 除以 1000。
完。

激活_自归一指数 接受 x：
    若 x 大于或等于 0：
        返还 x 乘 1051 除以 1000。
    完。
    返还 (做 Q16_指数 用 x 减 1000) 乘 1759 除以 1000 乘 1051 除以 1000。
完。

激活_S形 接受 x：
    若 x 大于 6000：
        返还 998。
    完。
    若 x 小于 负 6000：
        返还 2。
    完。
    e 为 做 Q16_指数 用 0 减 x。
    d 为 e 加 1000。
    若 d 小于 1：
        返还 998。
    完。
    返还 1000000 除以 d。
完。

激活_双曲正切 接受 x：
    返还 做 Q16_双曲正切 用 x。
完。

激活_高斯线性 接受 x：
    inner 为 x 乘 1595 除以 1000。
    t 为 做 Q16_双曲正切 用 inner。
    返还 x 乘 (1000 加 t) 除以 2000。
完。

激活_自门控贝塔 接受 x、动量率：
    s 为 做 激活_S形 用 x 乘 动量率 除以 1000。
    返还 x 乘 s 除以 1000。
完。

激活_自门控 接受 x：
    返还 做 激活_自门控贝塔 用 x、1000。
完。

激活_米什 接受 x：
    sp 为 做 Q16_对数 用 做 Q16_指数 用 x 加 1000。
    t 为 做 Q16_双曲正切 用 sp。
    返还 x 乘 t 除以 1000。
完。

激活_软加 接受 x：
    返还 做 Q16_对数 用 做 Q16_指数 用 x 加 1000。
完。

激活_软符号 接受 x：
    d 为 做 绝对值 用 x 加 1000。
    若 d 等于 0：
        返还 0。
    完。
    返还 x 乘 1000 除以 d。
完。

激活_硬S形 接受 x：
    返还 做 最小值 用 做 最大值 用 x 除以 5 加 200、0、1000。
完。

激活_硬自门控 接受 x：
    返还 x 乘 做 最小值 用 做 最大值 用 x 加 3000、0、6000 除以 6000。
完。

激活_硬双曲正切 接受 x：
    返还 做 最小值 用 做 最大值 用 x、负 1000、1000。
完。

激活_归一化指数 接受 对数值：
    n 为 做 计数 用 对数值。
    可变 指数组 为 一组：（空）。
    可变 mx 为 对数值 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 对数值 的 第 i 项 大于 mx：
            mx 为 对数值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 exp_sum 为 0。
    i 为 0。
    只要 i 小于 n：
        ev 为 做 Q16_指数 用 对数值 的 第 i 项 减 mx。
        做 追加 用 指数组、ev。
        exp_sum 为 exp_sum 加 ev。
        i 为 i 加 1。
    完。
    若 exp_sum 小于 1：
        exp_sum 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、指数组 的 第 i 项 乘 1000 除以 exp_sum。
        i 为 i 加 1。
    完。
    返还 结果。
完。

激活_对数归一化指数 接受 对数值：
    返还 做 映射 用 做 激活_归一化指数 用 对数值、以 p 映射到 做 Q16_对数 用 做 最大值 用 p、1。
完。

梯度_线性整流 接受 x：
    若 x 大于 0：
        返还 1000。
    完。
    返还 0。
完。

梯度_线性整流6 接受 x：
    若 x 大于 0：
        若 x 小于 6000：
            返还 1000。
        完。
    完。
    返还 0。
完。

梯度_泄露线性 接受 x、斜率系数：
    若 x 大于或等于 0：
        返还 1000。
    完。
    返还 斜率系数。
完。

梯度_S形 接受 sig_out：
    返还 sig_out 乘 (1000 减 sig_out) 除以 1000。
完。

梯度_双曲正切 接受 tanh_out：
    返还 1000 减 tanh_out 乘 tanh_out 除以 1000。
完。

梯度_指数线性 接受 x、斜率系数：
    若 x 大于或等于 0：
        返还 1000。
    完。
    返还 做 Q16_指数 用 x 乘 斜率系数 除以 1000。
完。

梯度_自门控 接受 x：
    s 为 做 激活_S形 用 x。
    返还 s 加 x 乘 做 梯度_S形 用 s 除以 1000。
完。

批量梯度 接受 数据、类型：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        v 为 数据 的 第 i 项。
        可变 r 为 0。
        若 类型 等于 0：
            r 为 做 梯度_线性整流 用 v。
        完。
        若 类型 等于 1：
            r 为 做 梯度_S形 用 做 激活_S形 用 v。
        完。
        若 类型 等于 2：
            r 为 做 梯度_双曲正切 用 做 激活_双曲正切 用 v。
        完。
        做 追加 用 结果、r。
        i 为 i 加 1。
    完。
    返还 结果。
完。

门控线性 接受 数据、n：
    半 为 n 除以 2。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 半：
        s 为 做 激活_S形 用 数据 的 第 i 加 半 项。
        做 追加 用 结果、数据 的 第 i 项 乘 s 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

高斯门控线性 接受 数据、n：
    半 为 n 除以 2。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 半：
        g 为 做 激活_高斯线性 用 数据 的 第 i 项。
        做 追加 用 结果、g 乘 数据 的 第 i 加 半 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

自门控线性 接受 数据、n：
    半 为 n 除以 2。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 半：
        sw 为 做 激活_自门控 用 数据 的 第 i 项。
        做 追加 用 结果、sw 乘 数据 的 第 i 加 半 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

自门控线性层 接受 x、维度、_扩3、_扩4：
    返还 做 自门控线性 用 x、维度。
完。

批量激活 接受 数据、类型：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        v 为 数据 的 第 i 项。
        可变 r 为 0。
        若 类型 等于 0：
            r 为 做 激活_线性整流 用 v。
        完。
        若 类型 等于 1：
            r 为 做 激活_S形 用 v。
        完。
        若 类型 等于 2：
            r 为 做 激活_双曲正切 用 v。
        完。
        若 类型 等于 3：
            r 为 做 激活_高斯线性 用 v。
        完。
        若 类型 等于 4：
            r 为 做 激活_自门控 用 v。
        完。
        若 类型 等于 5：
            r 为 做 激活_线性整流6 用 v。
        完。
        若 类型 等于 6：
            r 为 做 激活_米什 用 v。
        完。
        做 追加 用 结果、r。
        i 为 i 加 1。
    完。
    返还 结果。
完。

act_是单调 接受 类型：
    若 类型 大于或等于 0 并且 类型 小于或等于 2：
        返还 真。
    完。
    返还 假。
完。

连续指数线性 接受 x、斜率系数：
    若 x 大于或等于 0：
        返还 x。
    完。
    返还 斜率系数 乘 x 除以 (斜率系数 加 (0 减 x))。
完。

指数线性函数 接受 x：
    若 x 大于 0：
        返还 x。
    完。
    返还 x 除以 2。
完。

高斯线性 接受 x：
    缩放 为 x 乘 1702 除以 定点精度。
    若 定点精度 等于 0：
        返还 0。
    完。
    返还 x 乘 做 S形函数 用 缩放 除以 定点精度。
完。

泄露线性整流 接受 x：
    若 x 大于 0：
        返还 x。
    完。
    返还 x 除以 100。
完。

lLeaky_rRelu导 接受 x：
    若 x 大于 0：
        返还 定点精度。
    完。
    返还 定点精度 除以 100。
完。

对数归一指数 接受 输入：
    n 为 做 计数 用 输入。
    sm 为 做 归一化指数 用 输入。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 p 为 sm 的 第 i 项。
        若 p 小于或等于 0：
            p 为 1。
        完。
        可变 对数值 为 0。
        可变 v 为 p。
        只要 v 小于 500：
            v 为 v 乘 2。
            对数值 为 对数值 减 693。
        完。
        只要 v 大于 定点精度：
            v 为 v 除以 2。
            对数值 为 对数值 加 693。
        完。
        做 追加 用 结果、对数值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

米什激活 接受 x：
    若 x 大于或等于 5 乘 定点精度：
        返还 x。
    完。
    若 x 小于或等于 0 减 5 乘 定点精度：
        返还 0。
    完。
    s 为 做 S形函数 用 x。
    t 为 2 乘 s 减 定点精度。
    返还 x 乘 (定点精度 加 t) 除以 (2 乘 定点精度)。
完。

参数线性整流 接受 x、斜率系数：
    若 x 大于 0：
        返还 x。
    完。
    若 定点精度 等于 0：
        返还 0。
    完。
    返还 斜率系数 乘 x 除以 定点精度。
完。

线性整流函数 接受 x：
    若 x 大于 0：
        返还 x。
    完。
    返还 0。
完。

线性整流 接受 x：
    返还 做 线性整流函数 用 x。
完。

rRelu导 接受 x：
    若 x 大于 0：
        返还 定点精度。
    完。
    返还 0。
完。

自归一指数 接受 x：
    若 x 大于 0：
        返还 x 乘 1051 除以 定点精度。
    完。
    返还 x 乘 1051 乘 1673 除以 (定点精度 乘 定点精度)。
完。

S形函数 接受 x：
    若 x 小于 0 减 5 乘 定点精度：
        返还 0。
    完。
    若 x 大于或等于 5 乘 定点精度：
        返还 定点精度。
    完。
    若 x 小于 0 减 2500：
        返还 (x 加 5000) 乘 250 除以 2500。
    完。
    若 x 小于 0：
        返还 250 加 (x 加 2500) 乘 250 除以 2500。
    完。
    若 x 小于 2500：
        返还 500 加 x 乘 250 除以 2500。
    完。
    返还 750 加 (x 减 2500) 乘 250 除以 2500。
完。

sS形导 接受 x：
    s 为 做 S形函数 用 x。
    若 定点精度 等于 0：
        返还 0。
    完。
    返还 s 乘 (定点精度 减 s) 除以 定点精度。
完。

归一化指数 接受 输入：
    n 为 做 计数 用 输入。
    可变 最大 为 输入 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 输入 的 第 i 项 大于 最大：
            最大 为 输入 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 偏移 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 偏移、输入 的 第 i 项 减 最大。
        i 为 i 加 1。
    完。
    可变 指数 为 一组：（空）。
    可变 总和 为 0。
    i 为 0。
    只要 i 小于 n：
        可变 e 为 定点精度 加 偏移 的 第 i 项。
        若 e 小于 1：
            e 为 1。
        完。
        做 追加 用 指数、e。
        总和 为 总和 加 e。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、指数 的 第 i 项 乘 定点精度 除以 总和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

softmax_温度 接受 输入、温度：
    n 为 做 计数 用 输入。
    若 温度 小于或等于 0：
        温度 为 1。
    完。
    可变 缩放输入 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 缩放输入、输入 的 第 i 项 乘 定点精度 除以 温度。
        i 为 i 加 1。
    完。
    返还 做 归一化指数 用 缩放输入。
完。

软加 接受 x：
    若 x 大于 5 乘 定点精度：
        返还 x。
    完。
    若 x 小于 0 减 5 乘 定点精度：
        返还 0。
    完。
    s 为 做 S形函数 用 x。
    若 s 小于或等于 0：
        返还 1。
    完。
    返还 x 乘 s 除以 定点精度 加 (定点精度 减 s) 乘 定点精度 除以 (定点精度 加 定点精度)。
完。

软符号 接受 x：
    若 abs_x 小于 0：
        abs_x 为 0 减 abs_x。
    完。
    返还 x 乘 定点精度 除以 (定点精度 加 abs_x)。
完。

自门控激活 接受 x：
    返还 x 乘 做 S形函数 用 x 除以 定点精度。
完。

tTanh近似 接受 x：
    返还 2 乘 做 S形函数 用 2 乘 x 减 定点精度。
完。

前K归一化指数 接受 输入、k：
    n 为 做 计数 用 输入。
    可变 阈值 为 0 减 0x7FFFFFFF。
    若 k 大于或等于 n：
        返还 做 归一化指数 用 输入。
    完。
    可变 排序 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 排序、输入 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 1。
    只要 i 小于 n：
        键 为 排序 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 排序 的 第 j 项 小于 键：
            排序 的 第 j 加 1 项 为 排序 的 第 j 项。
            j 为 j 减 1。
        完。
        排序 的 第 j 加 1 项 为 键。
        i 为 i 加 1。
    完。
    阈值 为 排序 的 第 k 减 1 项。
    可变 掩码输入 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        若 输入 的 第 i 项 大于或等于 阈值：
            做 追加 用 掩码输入、输入 的 第 i 项。
        否则：
            做 追加 用 掩码输入、0 减 99999。
        完。
        i 为 i 加 1。
    完。
    返还 做 归一化指数 用 掩码输入。
完。

激活函数_整数开方 接受 n：
    返还 做 开方 用 n。
完。

激活_批量 接受 向量、激活函数：
    n 为 做 计数 用 向量。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、做 激活函数 用 向量 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

硬sigmoid 接受 x：
    若 x 小于或等于 0 减 3 乘 定点精度：
        返还 0。
    完。
    若 x 大于或等于 3 乘 定点精度：
        返还 定点精度。
    完。
    返还 x 除以 6 加 定点精度 除以 2。
完。

硬swish 接受 x：
    返还 x 乘 做 硬sigmoid 用 x 除以 定点精度。
完。

激活函数_绝对值 接受 x：
    若 x 小于 0：
        返还 0 减 x。
    完。
    返还 x。
完。

激活函数_阶跃 接受 x：
    若 x 大于或等于 0：
        返还 定点精度。
    完。
    返还 0。
完。

激活函数_限幅 接受 x、最小、最大：
    若 x 小于 最小：
        返还 最小。
    完。
    若 x 大于 最大：
        返还 最大。
    完。
    返还 x。
完。

可变 _激活_调用次数 为 0。

可变 _激活_饱和次数 为 0。

激活_记录调用 接受 输出：
    _激活_调用次数 为 _激活_调用次数 加 1。
    若 输出 大于 990 或者 输出 小于 负 990：
        _激活_饱和次数 为 _激活_饱和次数 加 1。
    完。
完。

激活_饱和率 接受 零参：
    若 _激活_调用次数 等于 0：
        返还 0。
    完。
    返还 _激活_饱和次数 乘 100 除以 _激活_调用次数。
完。

本章 收束。
