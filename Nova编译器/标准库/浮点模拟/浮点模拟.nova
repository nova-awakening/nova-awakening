《浮点模拟》

浮点_偏置 为 127。

浮点_EXP_MASK 为 0xFF。

浮点_MANT_MASK 为 0x7FFFFF。

浮点_SIGN_BIT 为 0x80000000。

浮点_EXP_SHIFT 为 23。

浮点_INF 为 0x7F800000。

浮点_NEG_INF 为 0xFF800000。

浮点_NAN 为 0x7FC00000。

浮点_ZERO 为 0x00000000。

浮点_NEG_ZERO 为 0x80000000。

浮点_ONE 为 0x3F800000。

浮点_NEG_ONE 为 0xBF800000。

浮点_TWO 为 0x40000000。

浮点_HALF 为 0x3F000000。

浮点_TEN 为 0x41200000。

浮点_PI_APPROX 为 0x40490FDB。

fp_符号 接受 f：
    返还 f 右移 31 位与 1。
完。

fp_指数 接受 f：
    返还 f 右移 浮点_EXP_SHIFT 位与 浮点_EXP_MASK。
完。

fp_尾数 接受 f：
    返还 f 位与 浮点_MANT_MASK。
完。

fp_是零 接受 f：
    返还 f 位与 0x7FFFFFFF 等于 0。
完。

fp_是无穷 接受 f：
    若 做 fp_指数 用 f 等于 255：
        若 做 fp_尾数 用 f 等于 0：
            返还 1。
        完。
    完。
    返还 0。
完。

fp_是NaN 接受 f：
    若 做 fp_指数 用 f 等于 255：
        若 做 fp_尾数 用 f 不等于 0：
            返还 1。
        完。
    完。
    返还 0。
完。

fp_是正 接受 f：
    若 做 fp_是零 用 f 等于 1：
        返还 0。
    完。
    返还 做 fp_符号 用 f 等于 0。
完。

fp_是负 接受 f：
    返还 做 fp_符号 用 f 等于 1。
完。

fp_是规格化 接受 f：
    可变 e 为 做 fp_指数 用 f。
    若 e 等于 0：
        返还 0。
    完。
    若 e 等于 255：
        返还 0。
    完。
    返还 1。
完。

fp_构造 接受 符号、指数、尾数：
    返还 (符号 位与 1) 左移 31 位或 (指数 位与 浮点_EXP_MASK) 左移 浮点_EXP_SHIFT 位或 尾数 位与 浮点_MANT_MASK。
完。

fp_取反 接受 f：
    返还 f 位异或 浮点_SIGN_BIT。
完。

fp_绝对值 接受 f：
    返还 f 位与 0x7FFFFFFF。
完。

fp_从整数 接受 n：
    若 n 等于 0：
        返还 浮点_ZERO。
    完。
    可变 符号 为 0。
    可变 v 为 n。
    若 n 小于 0：
        符号 为 1。
        v 为 0 减 n。
    完。
    可变 指数值 为 0。
    可变 t 为 v。
    只要 t 大于 1：
        t 为 t 右移 1。
        指数值 为 指数值 加 1。
    完。
    可变 mant 为 0。
    若 指数值 大于或等于 23：
        mant 为 v 右移 指数值 减 23 位与 浮点_MANT_MASK。
    否则：
        mant 为 v 左移 23 减 指数值 位与 浮点_MANT_MASK。
    完。
    返还 做 fp_构造 用 符号、指数值 加 浮点_偏置、mant。
完。

fp_到整数 接受 f：
    若 做 fp_是零 用 f 等于 1：
        返还 0。
    完。
    若 做 fp_是NaN 用 f 等于 1：
        返还 0。
    完。
    可变 s 为 做 fp_符号 用 f。
    可变 e 为 做 fp_指数 用 f 减 浮点_偏置。
    可变 m 为 做 fp_尾数 用 f 位或 0x800000。
    可变 结果 为 0。
    若 e 大于或等于 23：
        结果 为 m 左移 e 减 23。
    否则：
        若 e 大于或等于 0：
            结果 为 m 右移 23 减 e。
        否则：
            结果 为 0。
        完。
    完。
    若 s 等于 1：
        结果 为 0 减 结果。
    完。
    返还 结果。
完。

fp_等于 接受 a、b：
    若 做 fp_是NaN 用 a 等于 1：
        返还 0。
    完。
    若 做 fp_是NaN 用 b 等于 1：
        返还 0。
    完。
    若 做 fp_是零 用 a 等于 1：
        若 做 fp_是零 用 b 等于 1：
            返还 1。
        完。
    完。
    返还 a 等于 b。
完。

fp_小于 接受 a、b：
    若 做 fp_是NaN 用 a 等于 1：
        返还 0。
    完。
    若 做 fp_是NaN 用 b 等于 1：
        返还 0。
    完。
    可变 sa 为 做 fp_符号 用 a。
    可变 sb 为 做 fp_符号 用 b。
    若 sa 等于 1：
        若 sb 等于 0：
            返还 1。
        完。
    完。
    若 sa 等于 0：
        若 sb 等于 1：
            返还 0。
        完。
    完。
    若 sa 等于 0：
        返还 a 位与 0x7FFFFFFF 小于 b 位与 0x7FFFFFFF。
    完。
    返还 a 位与 0x7FFFFFFF 大于 b 位与 0x7FFFFFFF。
完。

fp_乘尾数 接受 a、b：
    可变 al 为 a 位与 0xFFF。
    可变 ah 为 a 右移 12 位与 0xFFF。
    可变 bl 为 b 位与 0xFFF。
    可变 bh 为 b 右移 12 位与 0xFFF。
    可变 ll 为 al 乘 bl。
    可变 lh 为 al 乘 bh。
    可变 hl 为 ah 乘 bl。
    可变 hh 为 ah 乘 bh。
    可变 中间值 为 lh 加 hl。
    可变 结果 为 (hh 左移 1) 加 (中间值 右移 11) 加 (ll 右移 23)。
    返还 结果。
完。

fp_乘 接受 a、b：
    若 做 fp_是NaN 用 a 等于 1：
        返还 浮点_NAN。
    完。
    若 做 fp_是NaN 用 b 等于 1：
        返还 浮点_NAN。
    完。
    可变 rs 为 做 fp_符号 用 a 位异或 做 fp_符号 用 b。
    若 做 fp_是零 用 a 等于 1：
        返还 做 fp_构造 用 rs、0、0。
    完。
    若 做 fp_是零 用 b 等于 1：
        返还 做 fp_构造 用 rs、0、0。
    完。
    若 做 fp_是无穷 用 a 等于 1：
        返还 做 fp_构造 用 rs、255、0。
    完。
    若 做 fp_是无穷 用 b 等于 1：
        返还 做 fp_构造 用 rs、255、0。
    完。
    可变 ea 为 做 fp_指数 用 a 减 浮点_偏置。
    可变 eb 为 做 fp_指数 用 b 减 浮点_偏置。
    可变 ma 为 做 fp_尾数 用 a 位或 0x800000。
    可变 mb 为 做 fp_尾数 用 b 位或 0x800000。
    可变 re 为 ea 加 eb 加 浮点_偏置。
    可变 rm 为 做 fp_乘尾数 用 ma、mb。
    若 rm 大于或等于 0x1000000：
        rm 为 rm 右移 1。
        re 为 re 加 1。
    完。
    若 re 大于或等于 255：
        返还 做 fp_构造 用 rs、255、0。
    完。
    若 re 小于或等于 0：
        返还 做 fp_构造 用 rs、0、0。
    完。
    返还 做 fp_构造 用 rs、re、rm 位与 浮点_MANT_MASK。
完。

fp_加 接受 a、b：
    若 做 fp_是NaN 用 a 等于 1：
        返还 浮点_NAN。
    完。
    若 做 fp_是NaN 用 b 等于 1：
        返还 浮点_NAN。
    完。
    若 做 fp_是零 用 a 等于 1：
        返还 b。
    完。
    若 做 fp_是零 用 b 等于 1：
        返还 a。
    完。
    若 做 fp_是无穷 用 a 等于 1：
        返还 a。
    完。
    若 做 fp_是无穷 用 b 等于 1：
        返还 b。
    完。
    可变 ea 为 做 fp_指数 用 a。
    可变 eb 为 做 fp_指数 用 b。
    可变 ma 为 做 fp_尾数 用 a 位或 0x800000。
    可变 mb 为 做 fp_尾数 用 b 位或 0x800000。
    可变 sa 为 做 fp_符号 用 a。
    可变 sb 为 做 fp_符号 用 b。
    可变 re 为 ea。
    若 ea 大于 eb：
        mb 为 mb 右移 ea 减 eb。
        re 为 ea。
    否则：
        若 eb 大于 ea：
            ma 为 ma 右移 eb 减 ea。
            re 为 eb。
        完。
    完。
    可变 rm 为 0。
    可变 rs 为 0。
    若 sa 等于 sb：
        rm 为 ma 加 mb。
        rs 为 sa。
    否则：
        若 ma 大于或等于 mb：
            rm 为 ma 减 mb。
            rs 为 sa。
        否则：
            rm 为 mb 减 ma。
            rs 为 sb。
        完。
    完。
    若 rm 等于 0：
        返还 浮点_ZERO。
    完。
    只要 rm 大于或等于 0x1000000：
        rm 为 rm 右移 1。
        re 为 re 加 1。
    完。
    只要 rm 小于 0x800000：
        若 rm 等于 0：
            返还 浮点_ZERO。
        完。
        rm 为 rm 左移 1。
        re 为 re 减 1。
    完。
    若 re 大于或等于 255：
        返还 做 fp_构造 用 rs、255、0。
    完。
    若 re 小于或等于 0：
        返还 浮点_ZERO。
    完。
    返还 做 fp_构造 用 rs、re、rm 位与 浮点_MANT_MASK。
完。

fp_减 接受 a、b：
    返还 做 fp_加 用 a、做 fp_取反 用 b。
完。

fp_除 接受 a、b：
    若 做 fp_是NaN 用 a 等于 1：
        返还 浮点_NAN。
    完。
    若 做 fp_是NaN 用 b 等于 1：
        返还 浮点_NAN。
    完。
    可变 rs 为 做 fp_符号 用 a 位异或 做 fp_符号 用 b。
    若 做 fp_是零 用 b 等于 1：
        若 做 fp_是零 用 a 等于 1：
            返还 浮点_NAN。
        完。
        返还 做 fp_构造 用 rs、255、0。
    完。
    若 做 fp_是零 用 a 等于 1：
        返还 做 fp_构造 用 rs、0、0。
    完。
    若 做 fp_是无穷 用 a 等于 1：
        若 做 fp_是无穷 用 b 等于 1：
            返还 浮点_NAN。
        完。
        返还 做 fp_构造 用 rs、255、0。
    完。
    若 做 fp_是无穷 用 b 等于 1：
        返还 做 fp_构造 用 rs、0、0。
    完。
    可变 ea 为 做 fp_指数 用 a 减 浮点_偏置。
    可变 eb 为 做 fp_指数 用 b 减 浮点_偏置。
    可变 ma 为 做 fp_尾数 用 a 位或 0x800000。
    可变 mb 为 做 fp_尾数 用 b 位或 0x800000。
    可变 re 为 ea 减 eb 加 浮点_偏置。
    可变 余 为 ma。
    若 余 小于 mb：
        余 为 余 左移 1。
        re 为 re 减 1。
    完。
    可变 商 为 0。
    可变 bi 为 23。
    只要 bi 大于或等于 0：
        若 余 大于或等于 mb：
            余 为 余 减 mb。
            商 为 商 位或 1 左移 bi。
        完。
        余 为 余 左移 1。
        bi 为 bi 减 1。
    完。
    可变 rm 为 商。
    若 re 大于或等于 255：
        返还 做 fp_构造 用 rs、255、0。
    完。
    若 re 小于或等于 0：
        返还 做 fp_构造 用 rs、0、0。
    完。
    返还 做 fp_构造 用 rs、re、rm 位与 浮点_MANT_MASK。
完。

fp_大于 接受 a、b：
    返还 做 fp_小于 用 b、a。
完。

fp_小于等于 接受 a、b：
    若 做 fp_是NaN 用 a 等于 1：
        返还 0。
    完。
    若 做 fp_是NaN 用 b 等于 1：
        返还 0。
    完。
    若 做 fp_等于 用 a、b 等于 1：
        返还 1。
    完。
    返还 做 fp_小于 用 a、b。
完。

fp_大于等于 接受 a、b：
    返还 做 fp_小于等于 用 b、a。
完。

fp_最小 接受 a、b：
    若 做 fp_是NaN 用 a 等于 1：
        返还 b。
    完。
    若 做 fp_是NaN 用 b 等于 1：
        返还 a。
    完。
    若 做 fp_小于 用 a、b 等于 1：
        返还 a。
    完。
    返还 b。
完。

fp_最大 接受 a、b：
    若 做 fp_是NaN 用 a 等于 1：
        返还 b。
    完。
    若 做 fp_是NaN 用 b 等于 1：
        返还 a。
    完。
    若 做 fp_大于 用 a、b 等于 1：
        返还 a。
    完。
    返还 b。
完。

fp_从定点 接受 x：
    可变 f 为 做 fp_从整数 用 x。
    返还 做 fp_乘 用 f、做 fp_构造 用 0、浮点_偏置 减 10、0x780000。
完。

本章 收束。
