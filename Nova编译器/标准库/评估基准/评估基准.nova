《评估基准》

引用《..·基础》。

引用《..·算法·数学扩展》。

引用《..·..·编译器·标准前导》。

混淆矩阵_二分类 接受 预测、标签、正类：
    可变 tp 为 0。
    可变 fp 为 0。
    可变 fn 为 0。
    可变 tn 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测：
        若 预测 的 第 i 项 等于 正类 并且 标签 的 第 i 项 等于 正类：
            tp 为 tp 加 1。
        否则若 预测 的 第 i 项 等于 正类 并且 标签 的 第 i 项 不等于 正类：
            fp 为 fp 加 1。
        否则若 预测 的 第 i 项 不等于 正类 并且 标签 的 第 i 项 等于 正类：
            fn 为 fn 加 1。
        否则：
            tn 为 tn 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：tp、fp、fn、tn。
完。

CM_精确率 接受 cm：
    若 cm 的 第 0 项 加 cm 的 第 1 项 等于 0：
        返还 0。
    完。
    返还 cm 的 第 0 项 乘 1000 除以 (cm 的 第 0 项 加 cm 的 第 1 项)。
完。

CM_召回率 接受 cm：
    若 cm 的 第 0 项 加 cm 的 第 2 项 等于 0：
        返还 0。
    完。
    返还 cm 的 第 0 项 乘 1000 除以 (cm 的 第 0 项 加 cm 的 第 2 项)。
完。

CM_F1分数 接受 cm：
    p 为 做 CM_精确率 用 cm。
    r 为 做 CM_召回率 用 cm。
    若 p 加 r 等于 0：
        返还 0。
    完。
    返还 2 乘 p 乘 r 除以 (p 加 r)。
完。

多分类_准确率 接受 预测、标签：
    若 做 计数 用 预测 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测：
        若 预测 的 第 i 项 等于 标签 的 第 i 项：
            正确 为 正确 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 正确 乘 100 除以 做 计数 用 预测。
完。

R2分数 接受 预测、标签：
    可变 总平方和 为 0。
    可变 残差平方和 为 0。
    若 做 计数 用 标签 等于 0：
        返还 0。
    完。
    可变 均值 为 做 折叠 用 标签、0、以 s、x 映射到 s 加 x 除以 做 计数 用 标签。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测：
        可变 d1 为 预测 的 第 i 项 减 标签 的 第 i 项。
        可变 d2 为 标签 的 第 i 项 减 均值。
        残差平方和 为 残差平方和 加 d1 乘 d1。
        总平方和 为 总平方和 加 d2 乘 d2。
        i 为 i 加 1。
    完。
    若 总平方和 等于 0：
        返还 100。
    完。
    返还 100 减 残差平方和 乘 100 除以 总平方和。
完。

可变 基准列表 为 一组：（空）。

评估_基准_创建 接受 名哈希：
    做 追加 用 基准列表、一组：名哈希、0、0x7FFFFFFF、0。
    返还 做 计数 用 基准列表 减 1。
完。

评估_基准_记录 接受 索引、耗时：
    基准列表 的 第 索引 项 的 第 1 项 为 基准列表 的 第 索引 项 的 第 1 项 加 1。
    基准列表 的 第 索引 项 的 第 2 项 为 基准列表 的 第 索引 项 的 第 2 项 加 耗时。
    若 耗时 小于 基准列表 的 第 索引 项 的 第 3 项：
        基准列表 的 第 索引 项 的 第 3 项 为 耗时。
    完。
    若 耗时 大于 基准列表 的 第 索引 项 的 第 4 项：
        基准列表 的 第 索引 项 的 第 4 项 为 耗时。
    完。
完。

评估_基准_平均 接受 索引：
    若 基准列表 的 第 索引 项 的 第 1 项 等于 0：
        返还 0。
    完。
    返还 基准列表 的 第 索引 项 的 第 2 项 除以 基准列表 的 第 索引 项 的 第 1 项。
完。

评估_基准_最小 接受 索引：
    返还 基准列表 的 第 索引 项 的 第 3 项。
完。

评估_基准_最大 接受 索引：
    返还 基准列表 的 第 索引 项 的 第 4 项。
完。

评估_基准_次数 接受 索引：
    返还 基准列表 的 第 索引 项 的 第 1 项。
完。

评估_吞吐量 接受 处理量、时间：
    若 时间 等于 0：
        返还 0。
    完。
    返还 处理量 乘 1000 除以 时间。
完。

评估_延迟百分位 接受 延迟列表、百分位：
    已排序 为 做 复制列表 用 延迟列表。
    做 排序 用 已排序、0。
    可变 索引 为 做 计数 用 已排序 乘 百分位 除以 100。
    若 索引 大于或等于 做 计数 用 已排序：
        索引 为 做 计数 用 已排序 减 1。
    完。
    返还 已排序 的 第 索引 项。
完。

CM_特异性 接受 cm：
    若 cm 的 第 3 项 加 cm 的 第 1 项 等于 0：
        返还 0。
    完。
    返还 cm 的 第 3 项 乘 1000 除以 (cm 的 第 3 项 加 cm 的 第 1 项)。
完。

评估_F_beta 接受 cm、动量率：
    p 为 做 CM_精确率 用 cm。
    r 为 做 CM_召回率 用 cm。
    b2 为 动量率 乘 动量率。
    若 b2 乘 p 加 r 等于 0：
        返还 0。
    完。
    返还 (1 加 b2) 乘 p 乘 r 除以 (b2 乘 p 加 r)。
完。

马修斯相关系数 接受 cm：
    tp 为 cm 的 第 0 项。
    fp 为 cm 的 第 1 项。
    fn 为 cm 的 第 2 项。
    tn 为 cm 的 第 3 项。
    分子 为 tp 乘 tn 减 fp 乘 fn。
    分母sq 为 (tp 加 fp) 乘 (tp 加 fn) 乘 (tn 加 fp) 乘 (tn 加 fn)。
    若 分母sq 等于 0：
        返还 0。
    完。
    分母 为 做 整数sqrt 用 分母sq。
    若 分母 等于 0：
        返还 0。
    完。
    返还 分子 乘 1000 除以 分母。
完。

评估_困惑度 接受 交叉熵loss：
    若 交叉熵loss 小于或等于 0：
        返还 1000。
    完。
    返还 1000 加 交叉熵loss 乘 693 除以 1000。
完。

TopK_准确率 接受 预测_排序列表、标签列表、K：
    若 做 计数 用 预测_排序列表 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测_排序列表：
        预测top 为 预测_排序列表 的 第 i 项。
        可变 j 为 0。
        可变 命中 为 假。
        只要 j 小于 K 并且 j 小于 做 计数 用 预测top：
            若 预测top 的 第 j 项 等于 标签列表 的 第 i 项：
                命中 为 真。
            完。
            j 为 j 加 1。
        完。
        若 命中：
            正确 为 正确 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 正确 乘 100 除以 做 计数 用 预测_排序列表。
完。

评估_基准_对比 接受 idx_a、idx_b：
    avg_a 为 做 评估_基准_平均 用 idx_a。
    avg_b 为 做 评估_基准_平均 用 idx_b。
    做 打印 用 「基准对比:」。
    做 打印 用 「 A: 平均=」 加 做 转字符串 用 avg_a 加 「 最小=」 加 做 转字符串 用 做 评估_基准_最小 用 idx_a 加 「 最大=」 加 做 转字符串 用 做 评估_基准_最大 用 idx_a。
    做 打印 用 「 B: 平均=」 加 做 转字符串 用 avg_b 加 「 最小=」 加 做 转字符串 用 做 评估_基准_最小 用 idx_b 加 「 最大=」 加 做 转字符串 用 做 评估_基准_最大 用 idx_b。
    若 avg_b 大于 0：
        做 打印 用 「 加速比: 」 加 做 转字符串 用 avg_b 乘 100 除以 avg_a 加 「%」。
    完。
完。

CM_打印 接受 cm：
    做 打印 用 「混淆矩阵:」。
    做 打印 用 「 预测正 预测负」。
    做 打印 用 「 实际正 」 加 做 转字符串 用 cm 的 第 0 项 加 「 」 加 做 转字符串 用 cm 的 第 2 项。
    做 打印 用 「 实际负 」 加 做 转字符串 用 cm 的 第 1 项 加 「 」 加 做 转字符串 用 cm 的 第 3 项。
    做 打印 用 「 精确率=」 加 做 转字符串 用 做 CM_精确率 用 cm 加 「‰ 召回率=」 加 做 转字符串 用 做 CM_召回率 用 cm 加 「‰ F1=」 加 做 转字符串 用 做 CM_F1分数 用 cm 加 「‰」。
完。

基准_清空 接受 零参：
    基准列表 为 一组：（空）。
完。

评估基准_精确率 接受 真正、假正：
    若 真正 加 假正 等于 0：
        返还 0。
    完。
    返还 真正 乘 1000 除以 (真正 加 假正)。
完。

评估基准_召回率 接受 真正、假负：
    若 真正 加 假负 等于 0：
        返还 0。
    完。
    返还 真正 乘 1000 除以 (真正 加 假负)。
完。

F1分数 接受 精确、召回：
    若 精确 加 召回 等于 0：
        返还 0。
    完。
    返还 2 乘 精确 乘 召回 除以 (精确 加 召回)。
完。

混淆矩阵_计算 接受 预测列表、标签列表：
    可变 FP 为 0。
    可变 TN 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测列表：
        若 预测列表 的 第 i 项 等于 1 并且 标签列表 的 第 i 项 等于 1：
            TP 为 TP 加 1。
        完。
        若 预测列表 的 第 i 项 等于 1 并且 标签列表 的 第 i 项 等于 0：
            FP 为 FP 加 1。
        完。
        若 预测列表 的 第 i 项 等于 0 并且 标签列表 的 第 i 项 等于 1：
            FN 为 FN 加 1。
        完。
        若 预测列表 的 第 i 项 等于 0 并且 标签列表 的 第 i 项 等于 0：
            TN 为 TN 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：TP、FP、FN、TN。
完。

评估基准_准确率 接受 TP、FP、FN、TN：
    总 为 TP 加 FP 加 FN 加 TN。
    若 总 等于 0：
        返还 0。
    完。
    返还 (TP 加 TN) 乘 1000 除以 总。
完。

BLEU_单词组 接受 预测tokens、参考tokens：
    可变 匹配 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测tokens：
        可变 j 为 0。
        只要 j 小于 做 计数 用 参考tokens：
            若 预测tokens 的 第 i 项 等于 参考tokens 的 第 j 项：
                匹配 为 匹配 加 1。
                j 为 做 计数 用 参考tokens。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 预测tokens 等于 0：
        返还 0。
    完。
    返还 匹配 乘 1000 除以 做 计数 用 预测tokens。
完。

本章 收束。
