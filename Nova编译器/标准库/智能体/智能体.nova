《智能体》

引用《..·基础》。

Agent_空闲 为 0。

Agent_感知 为 1。

Agent_推理 为 2。

Agent_决策 为 3。

Agent_执行 为 4。

Agent_反思 为 5。

目标_达成 为 0。

目标_维持 为 1。

目标_规避 为 2。

目标_查询 为 3。

Agent_创建 接受 id：
    返还 一组：id、Agent_空闲、一组：（空）、一组：（空）、一组：（空）、一组：（空）、一组：（空）、一组：（空）。
完。

智能体_标识 接受 a：
    返还 a 的 第 0 项。
完。

Agent_状态 接受 a：
    返还 a 的 第 1 项。
完。

Agent_设状态 接受 a、s：
    a 的 第 1 项 为 s。
完。

Agent_添加目标 接受 a、类型、优先级、描述哈希：
    做 追加 用 a 的 第 2 项、一组：类型、优先级、描述哈希、假。
完。

Agent_当前目标 接受 a：
    可变 最高 为 负 1。
    可变 最高优先 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 2 项：
        若 a 的 第 2 项 的 第 i 项 的 第 3 项 等于 假 并且 a 的 第 2 项 的 第 i 项 的 第 1 项 大于 最高优先：
            最高 为 i。
            最高优先 为 a 的 第 2 项 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    若 最高 大于或等于 0：
        返还 a 的 第 2 项 的 第 最高 项。
    完。
    返还 空。
完。

Agent_完成目标 接受 a、描述哈希：
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 2 项：
        若 a 的 第 2 项 的 第 i 项 的 第 2 项 等于 描述哈希：
            a 的 第 2 项 的 第 i 项 的 第 3 项 为 真。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

Agent_活跃目标数 接受 a：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 2 项：
        若 a 的 第 2 项 的 第 i 项 的 第 3 项 等于 假：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

Agent_添加信念 接受 a、键、值、置信度、时间：
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 3 项：
        若 a 的 第 3 项 的 第 i 项 的 第 0 项 等于 键：
            a 的 第 3 项 的 第 i 项 的 第 1 项 为 值。
            a 的 第 3 项 的 第 i 项 的 第 2 项 为 置信度。
            a 的 第 3 项 的 第 i 项 的 第 3 项 为 时间。
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 a 的 第 3 项、一组：键、值、置信度、时间。
完。

Agent_查信念 接受 a、键：
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 3 项：
        若 a 的 第 3 项 的 第 i 项 的 第 0 项 等于 键：
            返还 a 的 第 3 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

Agent_信念数 接受 a：
    返还 做 计数 用 a 的 第 3 项。
完。

Agent_感知输入 接受 a、感知数据：
    做 追加 用 a 的 第 6 项、感知数据。
    若 做 计数 用 a 的 第 6 项 大于 100：
        可变 新buf 为 一组：（空）。
        可变 i 为 做 计数 用 a 的 第 6 项 减 50。
        只要 i 小于 做 计数 用 a 的 第 6 项：
            做 追加 用 新buf、a 的 第 6 项 的 第 i 项。
            i 为 i 加 1。
        完。
        a 的 第 6 项 为 新buf。
    完。
完。

Agent_最近感知 接受 a、n：
    可变 r 为 一组：（空）。
    可变 起始 为 做 计数 用 a 的 第 6 项 减 n。
    若 起始 小于 0：
        起始 为 0。
    完。
    只要 起始 小于 做 计数 用 a 的 第 6 项：
        做 追加 用 r、a 的 第 6 项 的 第 起始 项。
        起始 为 起始 加 1。
    完。
    返还 r。
完。

可变 工具注册表 为 一组：（空）。

工具_注册 接受 名哈希、类型、参数数：
    做 追加 用 工具注册表、一组：名哈希、类型、参数数、0。
完。

工具_查找 接受 名哈希：
    可变 i 为 0。
    只要 i 小于 做 计数 用 工具注册表：
        若 工具注册表 的 第 i 项 的 第 0 项 等于 名哈希：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

工具_调用 接受 名哈希、参数：
    索引 为 做 工具_查找 用 名哈希。
    若 索引 小于 0：
        返还 一组：假、负 1。
    完。
    工具注册表 的 第 索引 项 的 第 3 项 为 工具注册表 的 第 索引 项 的 第 3 项 加 1。
    返还 一组：真、做 折叠 用 参数、0、以 s、x 映射到 s 加 x。
完。

工具_数量 接受 零参：
    返还 做 计数 用 工具注册表。
完。

计划_创建 接受 零参：
    返还 一组：（空）。
完。

计划_添加步骤 接受 计划、动作哈希、参数：
    做 追加 用 计划、一组：动作哈希、参数、假。
完。

计划_下一步 接受 计划：
    可变 i 为 0。
    只要 i 小于 做 计数 用 计划：
        若 计划 的 第 i 项 的 第 2 项 等于 假：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

计划_完成步骤 接受 计划、步骤：
    若 步骤 大于或等于 0 并且 步骤 小于 做 计数 用 计划：
        计划 的 第 步骤 项 的 第 2 项 为 真。
    完。
完。

计划_完成率 接受 计划：
    若 做 计数 用 计划 等于 0：
        返还 100。
    完。
    可变 已完成 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 计划：
        若 计划 的 第 i 项 的 第 2 项：
            已完成 为 已完成 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 已完成 乘 100 除以 做 计数 用 计划。
完。

Agent_记忆存 接受 a、键、内容、重要性、时间：
    做 追加 用 a 的 第 7 项、一组：键、内容、重要性、0、时间。
完。

Agent_记忆取 接受 a、键：
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 7 项：
        若 a 的 第 7 项 的 第 i 项 的 第 0 项 等于 键：
            a 的 第 7 项 的 第 i 项 的 第 3 项 为 a 的 第 7 项 的 第 i 项 的 第 3 项 加 1。
            返还 a 的 第 7 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

Agent_记忆_按重要性 接受 a、topN：
    可变 结果 为 一组：（空）。
    可变 选了 为 0。
    可变 已选 为 一组：（空）。
    只要 选了 小于 topN 并且 选了 小于 做 计数 用 a 的 第 7 项：
        可变 最佳 为 负 1。
        可变 最佳重 为 负 1。
        可变 i 为 0。
        只要 i 小于 做 计数 用 a 的 第 7 项：
            可变 跳过 为 假。
            可变 j 为 0。
            只要 j 小于 做 计数 用 已选：
                若 已选 的 第 j 项 等于 i：
                    跳过 为 真。
                完。
                j 为 j 加 1。
            完。
            若 跳过 等于 假 并且 a 的 第 7 项 的 第 i 项 的 第 2 项 大于 最佳重：
                最佳 为 i。
                最佳重 为 a 的 第 7 项 的 第 i 项 的 第 2 项。
            完。
            i 为 i 加 1。
        完。
        若 最佳 大于或等于 0：
            做 追加 用 结果、a 的 第 7 项 的 第 最佳 项。
            做 追加 用 已选、最佳。
        完。
        选了 为 选了 加 1。
    完。
    返还 结果。
完。

可变 消息队列 为 一组：（空）。

发送消息 接受 发送者、接收者、类型、内容：
    做 追加 用 消息队列、一组：发送者、接收者、类型、内容。
完。

接收消息 接受 接收者：
    可变 收到 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 消息队列：
        若 消息队列 的 第 i 项 的 第 1 项 等于 接收者：
            做 追加 用 收到、消息队列 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 收到。
完。

清空消息 接受 零参：
    消息队列 为 一组：（空）。
完。

Agent_步 接受 a：
    若 a 的 第 1 项 等于 Agent_空闲：
        a 的 第 1 项 为 Agent_感知。
        返还 Agent_感知。
    完。
    若 a 的 第 1 项 等于 Agent_感知：
        a 的 第 1 项 为 Agent_推理。
        返还 Agent_推理。
    完。
    若 a 的 第 1 项 等于 Agent_推理：
        a 的 第 1 项 为 Agent_决策。
        返还 Agent_决策。
    完。
    若 a 的 第 1 项 等于 Agent_决策：
        a 的 第 1 项 为 Agent_执行。
        返还 Agent_执行。
    完。
    若 a 的 第 1 项 等于 Agent_执行：
        a 的 第 1 项 为 Agent_反思。
        返还 Agent_反思。
    完。
    a 的 第 1 项 为 Agent_空闲。
    返还 Agent_空闲。
完。

工具_链式调用 接受 工具哈希列表、初始参数：
    可变 当前结果 为 初始参数。
    可变 i 为 0。
    只要 i 小于 做 计数 用 工具哈希列表：
        r 为 做 工具_调用 用 工具哈希列表 的 第 i 项、当前结果。
        若 r 的 第 0 项 等于 假：
            返还 一组：假、i。
        完。
        当前结果 为 一组：r 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 一组：真、当前结果 的 第 0 项。
完。

Agent_反思评分 接受 a：
    可变 目标分 为 0。
    总目标 为 做 计数 用 a 的 第 2 项。
    若 总目标 大于 0：
        可变 完成 为 0。
        可变 i 为 0。
        只要 i 小于 总目标：
            若 a 的 第 2 项 的 第 i 项 的 第 3 项：
                完成 为 完成 加 1。
            完。
            i 为 i 加 1。
        完。
        目标分 为 完成 乘 100 除以 总目标。
    完。
    可变 信念分 为 0。
    若 做 计数 用 a 的 第 3 项 大于 0：
        信念分 为 做 折叠 用 a 的 第 3 项、0、以 s、x 映射到 s 加 x 的 第 2 项 除以 做 计数 用 a 的 第 3 项。
    完。
    返还 (目标分 加 信念分) 除以 2。
完。

任务_分解 接受 总任务哈希、子任务数：
    可变 子任务 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 子任务数：
        做 追加 用 子任务、一组：总任务哈希 乘 100 加 i、假。
        i 为 i 加 1。
    完。
    返还 子任务。
完。

任务_完成子任务 接受 子任务列表、索引：
    若 索引 小于 做 计数 用 子任务列表：
        子任务列表 的 第 索引 项 的 第 1 项 为 真。
    完。
完。

任务_全部完成 接受 子任务列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 子任务列表：
        若 子任务列表 的 第 i 项 的 第 1 项 等于 假：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

本章 收束。
