《状态空间》

引用《..·基础》。

引用《..·算法·数学扩展》。

SSM_创建 接受 状态维度、输入维度：
    可变 A 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 状态维度：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 状态维度：
            若 i 等于 j：
                做 追加 用 行、0 减 (i 加 1) 乘 100。
            否则若 i 大于 j：
                vi 为 做 整数sqrt 用 (2 乘 i 加 1) 乘 1000。
                vj 为 做 整数sqrt 用 (2 乘 j 加 1) 乘 1000。
                做 追加 用 行、0 减 vi 乘 vj 除以 1000。
            否则：
                做 追加 用 行、0。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 A、行。
        i 为 i 加 1。
    完。
    可变 B 为 一组：（空）。
    i 为 0。
    只要 i 小于 状态维度：
        可变 行b 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 输入维度：
            做 追加 用 行b、做 整数sqrt 用 (2 乘 i 加 1) 乘 1000。
            j 为 j 加 1。
        完。
        做 追加 用 B、行b。
        i 为 i 加 1。
    完。
    可变 C 为 一组：（空）。
    i 为 0。
    只要 i 小于 输入维度：
        可变 行c 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 状态维度：
            做 追加 用 行c、1000 除以 状态维度。
            j 为 j 加 1。
        完。
        做 追加 用 C、行c。
        i 为 i 加 1。
    完。
    可变 D 为 一组：（空）。
    i 为 0。
    只要 i 小于 输入维度：
        做 追加 用 D、0。
        i 为 i 加 1。
    完。
    可变 状态 为 一组：（空）。
    i 为 0。
    只要 i 小于 状态维度：
        做 追加 用 状态、0。
        i 为 i 加 1。
    完。
    返还 一组：A、B、C、D、状态维度、输入维度、状态。
完。

SSM_离散化 接受 ssm、Δ：
    N 为 ssm 的 第 4 项。
    M 为 ssm 的 第 5 项。
    可变 A_bar 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 N：
            可变 数值 为 Δ 乘 ssm 的 第 0 项 的 第 i 项 的 第 j 项 除以 1000。
            若 i 等于 j：
                数值 为 1000 加 数值。
            完。
            做 追加 用 行、数值。
            j 为 j 加 1。
        完。
        做 追加 用 A_bar、行。
        i 为 i 加 1。
    完。
    可变 B_bar 为 一组：（空）。
    i 为 0。
    只要 i 小于 N：
        可变 行b 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 M：
            做 追加 用 行b、Δ 乘 ssm 的 第 1 项 的 第 i 项 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        做 追加 用 B_bar、行b。
        i 为 i 加 1。
    完。
    返还 一组：A_bar、B_bar。
完。

SSM_步进 接受 ssm、输入、Δ：
    离散 为 做 SSM_离散化 用 ssm、Δ。
    A_bar 为 离散 的 第 0 项。
    B_bar 为 离散 的 第 1 项。
    N 为 ssm 的 第 4 项。
    M 为 ssm 的 第 5 项。
    状态 为 ssm 的 第 6 项。
    可变 新状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        可变 数值 为 0。
        可变 j 为 0。
        只要 j 小于 N：
            数值 为 数值 加 A_bar 的 第 i 项 的 第 j 项 乘 状态 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 M：
            数值 为 数值 加 B_bar 的 第 i 项 的 第 j 项 乘 输入 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        做 追加 用 新状态、数值。
        i 为 i 加 1。
    完。
    ssm 的 第 6 项 为 新状态。
    可变 输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 M：
        可变 y 为 ssm 的 第 3 项 的 第 i 项 乘 输入 的 第 i 项 除以 1000。
        可变 j 为 0。
        只要 j 小于 N：
            y 为 y 加 ssm 的 第 2 项 的 第 i 项 的 第 j 项 乘 新状态 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        做 追加 用 输出、y。
        i 为 i 加 1。
    完。
    返还 输出。
完。

SSM_卷积核 接受 ssm、序列长度、Δ：
    离散 为 做 SSM_离散化 用 ssm、Δ。
    A_bar 为 离散 的 第 0 项。
    B_bar 为 离散 的 第 1 项。
    N 为 ssm 的 第 4 项。
    可变 核 为 一组：（空）。
    可变 power_B 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        做 追加 用 power_B、B_bar 的 第 i 项 的 第 0 项。
        i 为 i 加 1。
    完。
    可变 l 为 0。
    只要 l 小于 序列长度：
        可变 k 为 0。
        i 为 0。
        只要 i 小于 N：
            k 为 k 加 ssm 的 第 2 项 的 第 0 项 的 第 i 项 乘 power_B 的 第 i 项 除以 1000。
            i 为 i 加 1。
        完。
        做 追加 用 核、k。
        可变 新power 为 一组：（空）。
        i 为 0。
        只要 i 小于 N：
            可变 v 为 0。
            可变 j 为 0。
            只要 j 小于 N：
                v 为 v 加 A_bar 的 第 i 项 的 第 j 项 乘 power_B 的 第 j 项 除以 1000。
                j 为 j 加 1。
            完。
            做 追加 用 新power、v。
            i 为 i 加 1。
        完。
        power_B 为 新power。
        l 为 l 加 1。
    完。
    返还 核。
完。

Mamba块_创建 接受 模型维度、d_state、d_conv：
    d_inner 为 模型维度 乘 2。
    返还 一组：模型维度、d_state、d_conv、d_inner、一组：（空）、一组：（空）、一组：（空）。
完。

状态空间_Mamba_选择性扫描 接受 B_seq、C_seq、Δ_seq、A_diag、输入序列：
    L 为 做 计数 用 输入序列。
    N 为 做 计数 用 A_diag。
    可变 h 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        做 追加 用 h、0。
        i 为 i 加 1。
    完。
    可变 输出 为 一组：（空）。
    可变 t 为 0。
    只要 t 小于 L：
        Δ 为 Δ_seq 的 第 t 项。
        可变 新h 为 一组：（空）。
        i 为 0。
        只要 i 小于 N：
            a_disc 为 1000 加 Δ 乘 A_diag 的 第 i 项 除以 1000。
            b_disc 为 Δ 乘 B_seq 的 第 t 乘 N 加 i 项 除以 1000。
            新隐藏 为 a_disc 乘 h 的 第 i 项 除以 1000 加 b_disc 乘 输入序列 的 第 t 项 除以 1000。
            做 追加 用 新h、新隐藏。
            i 为 i 加 1。
        完。
        h 为 新h。
        可变 y 为 0。
        i 为 0。
        只要 i 小于 N：
            y 为 y 加 C_seq 的 第 t 乘 N 加 i 项 乘 h 的 第 i 项 除以 1000。
            i 为 i 加 1。
        完。
        做 追加 用 输出、y。
        t 为 t 加 1。
    完。
    返还 输出。
完。

并行扫描 接受 A_seq、B_seq：
    L 为 做 计数 用 A_seq。
    可变 结果A 为 一组：（空）。
    可变 结果B 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 L：
        做 追加 用 结果A、A_seq 的 第 i 项。
        做 追加 用 结果B、B_seq 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 步长 为 1。
    只要 步长 小于 L：
        i 为 步长 乘 2 减 1。
        只要 i 小于 L：
            左 为 i 减 步长。
            结果B 的 第 i 项 为 结果A 的 第 i 项 乘 结果B 的 第 左 项 除以 1000 加 结果B 的 第 i 项。
            结果A 的 第 i 项 为 结果A 的 第 i 项 乘 结果A 的 第 左 项 除以 1000。
            i 为 i 加 步长 乘 2。
        完。
        步长 为 步长 乘 2。
    完。
    步长 为 L 除以 2。
    只要 步长 大于或等于 1：
        i 为 步长 乘 3 减 1。
        只要 i 小于 L：
            左 为 i 减 步长。
            结果B 的 第 i 项 为 结果A 的 第 i 项 乘 结果B 的 第 左 项 除以 1000 加 结果B 的 第 i 项。
            结果A 的 第 i 项 为 结果A 的 第 i 项 乘 结果A 的 第 左 项 除以 1000。
            i 为 i 加 步长 乘 2。
        完。
        步长 为 步长 除以 2。
    完。
    返还 结果B。
完。

RWKV_时间混合 接受 r、k、v、w、u、上一状态：
    维度 为 做 计数 用 r。
    可变 输出 为 一组：（空）。
    可变 新状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        uk 为 u 的 第 i 项 加 k 的 第 i 项。
        可变 eu 为 1000 加 uk。
        若 eu 小于 1：
            eu 为 1。
        完。
        wkv_n 为 eu 乘 v 的 第 i 项 除以 1000 加 上一状态 的 第 i 乘 2 项。
        可变 wkv_d 为 eu 加 上一状态 的 第 i 乘 2 加 1 项。
        若 wkv_d 等于 0：
            wkv_d 为 1。
        完。
        wkv 为 wkv_n 乘 1000 除以 wkv_d。
        可变 sig_r 为 500 加 r 的 第 i 项 除以 4。
        若 sig_r 小于 0：
            sig_r 为 0。
        完。
        若 sig_r 大于 1000：
            sig_r 为 1000。
        完。
        做 追加 用 输出、sig_r 乘 wkv 除以 1000。
        可变 ew 为 1000 加 w 的 第 i 项。
        若 ew 小于 1：
            ew 为 1。
        完。
        做 追加 用 新状态、ew 乘 上一状态 的 第 i 乘 2 项 除以 1000 加 1000 乘 v 的 第 i 项 除以 1000。
        做 追加 用 新状态、ew 乘 上一状态 的 第 i 乘 2 加 1 项 除以 1000 加 1000。
        i 为 i 加 1。
    完。
    返还 一组：输出、新状态。
完。

RetNet_衰减矩阵 接受 序列长度、γ：
    可变 D 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 序列长度：
            若 j 小于或等于 i：
                可变 power 为 1000。
                可变 k 为 0。
                只要 k 小于 i 减 j：
                    power 为 power 乘 γ 除以 1000。
                    k 为 k 加 1。
                完。
                做 追加 用 行、power。
            否则：
                做 追加 用 行、0。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 D、行。
        i 为 i 加 1。
    完。
    返还 D。
完。

门控SSM_步进 接受 ssm、输入、门控值：
    输出 为 做 SSM_步进 用 ssm、输入、100。
    维度 为 做 计数 用 输出。
    可变 门控输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        可变 g 为 500 加 门控值 的 第 i 项 除以 4。
        若 g 小于 0：
            g 为 0。
        完。
        若 g 大于 1000：
            g 为 1000。
        完。
        做 追加 用 门控输出、g 乘 输出 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 门控输出。
完。

SSM_重置 接受 ssm：
    N 为 ssm 的 第 4 项。
    可变 新状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        做 追加 用 新状态、0。
        i 为 i 加 1。
    完。
    ssm 的 第 6 项 为 新状态。
完。

SSM_零阶保持 接受 A、B、dt、N：
    可变 行A 为 一组：（空）。
    可变 行B 为 一组：（空）。
    可变 Ad 为 一组：（空）。
    可变 Bd 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        可变 j 为 0。
        只要 j 小于 N：
            若 i 等于 j：
                做 追加 用 行A、1000 加 A 的 第 i 项 的 第 j 项 乘 dt 除以 1000。
            否则：
                做 追加 用 行A、A 的 第 i 项 的 第 j 项 乘 dt 除以 1000。
            完。
            做 追加 用 行B、B 的 第 i 项 的 第 j 项 乘 dt 除以 1000。
            j 为 j 加 1。
        完。
        做 追加 用 Ad、行A。
        做 追加 用 Bd、行B。
        i 为 i 加 1。
    完。
    返还 一组：Ad、Bd。
完。

SSM_双线性 接受 A、_B、dt、N：
    可变 Ad 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 N：
            可变 分子 为 0。
            可变 分母 为 0。
            若 i 等于 j：
                分子 为 2000 加 A 的 第 i 项 的 第 j 项 乘 dt 除以 1000。
                分母 为 2000 减 A 的 第 i 项 的 第 j 项 乘 dt 除以 1000。
            否则：
                分子 为 A 的 第 i 项 的 第 j 项 乘 dt 除以 1000。
                分母 为 1000。
            完。
            若 分母 等于 0：
                做 追加 用 行、0。
            否则：
                做 追加 用 行、分子 乘 1000 除以 分母。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 Ad、行。
        i 为 i 加 1。
    完。
    返还 Ad。
完。

SSM_可控性矩阵 接受 A、B、N：
    可变 结果 为 一组：（空）。
    可变 幂B 为 B。
    可变 k 为 0。
    只要 k 小于 N：
        做 追加 用 结果、幂B。
        可变 新幂 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 N：
            可变 行 为 一组：（空）。
            可变 j 为 0。
            只要 j 小于 N：
                可变 和 为 0。
                可变 m 为 0。
                只要 m 小于 N：
                    和 为 和 加 A 的 第 i 项 的 第 m 项 乘 幂B 的 第 m 项 的 第 j 项 除以 1000。
                    m 为 m 加 1。
                完。
                做 追加 用 行、和。
                j 为 j 加 1。
            完。
            做 追加 用 新幂、行。
            i 为 i 加 1。
        完。
        幂B 为 新幂。
        k 为 k 加 1。
    完。
    返还 结果。
完。

SSM_对角占优 接受 A、N：
    可变 i 为 0。
    只要 i 小于 N：
        可变 对角 为 A 的 第 i 项 的 第 i 项。
        若 对角 小于 0：
            对角 为 0 减 对角。
        完。
        可变 行和 为 0。
        可变 j 为 0。
        只要 j 小于 N：
            若 j 不等于 i：
                可变 v 为 A 的 第 i 项 的 第 j 项。
                若 v 小于 0：
                    v 为 0 减 v。
                完。
                行和 为 行和 加 v。
            完。
            j 为 j 加 1。
        完。
        若 对角 小于 行和：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

SSM_迹 接受 A、N：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 N：
        和 为 和 加 A 的 第 i 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

可变 _SSM_步进次数 为 0。

可变 _SSM_最大维度 为 0。

可变 _SSM_不稳定次数 为 0。

SSM_记录步进 接受 维度：
    _SSM_步进次数 为 _SSM_步进次数 加 1。
    若 维度 大于 _SSM_最大维度：
        _SSM_最大维度 为 维度。
    完。
完。

SSM_记录不稳定 接受 零参：
    _SSM_不稳定次数 为 _SSM_不稳定次数 加 1。
完。

本章 收束。
