《安全》

引用《..·基础》。

SHA_简易 接受 数据：
    可变 h0 为 0x6a09e667。
    可变 h1 为 0xbb67ae85。
    可变 h2 为 0x3c6ef372。
    可变 h3 为 0xa54ff53a。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 k 为 数据 的 第 i 项。
        可变 r 为 0。
        只要 r 小于 4：
            h0 为 h0 加 k 乘 0x01000193。
            h0 为 (h0 左移 13 位或 h0 右移 19 位与 0x1FFF) 位异或 h1。
            h1 为 h1 位异或 (h0 右移 7 位或 h0 左移 25)。
            h1 为 h1 加 h2。
            h2 为 h2 位异或 (h1 左移 17 位或 h1 右移 15 位与 0x1FFFF)。
            h2 为 h2 加 k 加 h3。
            h3 为 h3 位异或 (h2 右移 11 位或 h2 左移 21)。
            h3 为 h3 加 h0。
            k 为 k 加 r 加 1。
            r 为 r 加 1。
        完。
        i 为 i 加 1。
    完。
    h0 为 h0 位异或 h2 乘 0x517cc1b7。
    h1 为 h1 位异或 h3 乘 0x27d4eb2d。
    若 h0 小于 0：
        h0 为 0 减 h0。
    完。
    若 h1 小于 0：
        h1 为 0 减 h1。
    完。
    返还 一组：h0、h1。
完。

哈希_文本 接受 文本：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        做 追加 用 数据、做 编码 用 做 子串 用 文本、i、1。
        i 为 i 加 1。
    完。
    返还 做 SHA_简易 用 数据。
完。

哈希_相等 接受 a、b：
    返还 a 的 第 0 项 等于 b 的 第 0 项 并且 a 的 第 1 项 等于 b 的 第 1 项。
完。

HMAC认证 接受 密钥数据、消息数据：
    可变 内填充 为 一组：（空）。
    可变 外填充 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 密钥数据：
        做 追加 用 内填充、密钥数据 的 第 i 项 位异或 0x36。
        做 追加 用 外填充、密钥数据 的 第 i 项 位异或 0x5C。
        i 为 i 加 1。
    完。
    可变 内数据 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 内填充：
        做 追加 用 内数据、内填充 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 消息数据：
        做 追加 用 内数据、消息数据 的 第 i 项。
        i 为 i 加 1。
    完。
    内哈希 为 做 SHA_简易 用 内数据。
    可变 外数据 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 外填充：
        做 追加 用 外数据、外填充 的 第 i 项。
        i 为 i 加 1。
    完。
    做 追加 用 外数据、内哈希 的 第 0 项。
    做 追加 用 外数据、内哈希 的 第 1 项。
    返还 做 SHA_简易 用 外数据。
完。

可变 安全随机_s0 为 2654435761。

可变 安全随机_s1 为 0x9E3779B97F4A7C15。

可变 安全随机_s2 为 0xBF58476D1CE4E5B9。

可变 安全随机_s3 为 0x94D049BB133111EB。

安全随机_种子 接受 种子：
    可变 z 为 种子。
    z 为 (z 位异或 z 右移 30) 乘 0xBF58476D1CE4E5B9。
    安全随机_s0 为 z。
    z 为 (z 位异或 z 右移 30) 乘 0x94D049BB133111EB。
    安全随机_s1 为 z。
    z 为 (z 位异或 z 右移 30) 乘 0xBF58476D1CE4E5B9。
    安全随机_s2 为 z。
    z 为 (z 位异或 z 右移 30) 乘 0x94D049BB133111EB。
    安全随机_s3 为 z。
完。

安全随机 接受 零参：
    可变 结果 为 安全随机_s1 乘 5。
    结果 为 (结果 左移 7 位或 结果 右移 57 位与 0x7F) 乘 9。
    t 为 安全随机_s1 左移 17。
    安全随机_s2 为 安全随机_s2 位异或 安全随机_s0。
    安全随机_s3 为 安全随机_s3 位异或 安全随机_s1。
    安全随机_s1 为 安全随机_s1 位异或 安全随机_s2。
    安全随机_s0 为 安全随机_s0 位异或 安全随机_s3。
    安全随机_s2 为 安全随机_s2 位异或 t。
    安全随机_s3 为 安全随机_s3 左移 45 位或 安全随机_s3 右移 19 位与 0x1FFFFFFFFFFF。
    若 结果 小于 0：
        结果 为 0 减 结果。
    完。
    返还 结果。
完。

安全随机_范围 接受 最小、最大：
    返还 最小 加 做 安全随机 取余于 (最大 减 最小 加 1)。
完。

UUID_生成 接受 零参：
    a 为 做 安全随机。
    b 为 做 安全随机。
    c 为 做 安全随机。
    d 为 做 安全随机。
    b_v4 为 b 位与 0xFFFF0FFF 位或 0x00004000。
    c_var 为 c 位与 0x3FFFFFFF 位或 0x80000000。
    返还 一组：a、b_v4、c_var、d。
完。

UUID_相等 接受 a、b：
    返还 a 的 第 0 项 等于 b 的 第 0 项 并且 a 的 第 1 项 等于 b 的 第 1 项 并且 a 的 第 2 项 等于 b 的 第 2 项 并且 a 的 第 3 项 等于 b 的 第 3 项。
完。

UUID_转字符串 接受 uuid：
    可变 结果 为 「」。
    hex字 为 「0123456789abcdef」。
    可变 v 为 uuid 的 第 0 项。
    可变 j 为 28。
    只要 j 大于或等于 0：
        结果 为 做 字符串拼接 用 结果、做 子串 用 hex字、v 右移 j 位与 0xF、1。
        j 为 j 减 4。
    完。
    结果 为 做 字符串拼接 用 结果、「-」。
    v 为 uuid 的 第 1 项。
    j 为 28。
    只要 j 大于或等于 16：
        结果 为 做 字符串拼接 用 结果、做 子串 用 hex字、v 右移 j 位与 0xF、1。
        j 为 j 减 4。
    完。
    结果 为 做 字符串拼接 用 结果、「-」。
    j 为 12。
    只要 j 大于或等于 0：
        结果 为 做 字符串拼接 用 结果、做 子串 用 hex字、v 右移 j 位与 0xF、1。
        j 为 j 减 4。
    完。
    结果 为 做 字符串拼接 用 结果、「-」。
    v 为 uuid 的 第 2 项。
    j 为 28。
    只要 j 大于或等于 16：
        结果 为 做 字符串拼接 用 结果、做 子串 用 hex字、v 右移 j 位与 0xF、1。
        j 为 j 减 4。
    完。
    结果 为 做 字符串拼接 用 结果、「-」。
    j 为 12。
    只要 j 大于或等于 0：
        结果 为 做 字符串拼接 用 结果、做 子串 用 hex字、v 右移 j 位与 0xF、1。
        j 为 j 减 4。
    完。
    v 为 uuid 的 第 3 项。
    j 为 28。
    只要 j 大于或等于 0：
        结果 为 做 字符串拼接 用 结果、做 子串 用 hex字、v 右移 j 位与 0xF、1。
        j 为 j 减 4。
    完。
    返还 结果。
完。

密码_哈希 接受 密码文本、盐：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 做 转字符串 用 盐：
        做 追加 用 数据、做 编码 用 做 子串 用 做 转字符串 用 盐、i、1。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 密码文本：
        做 追加 用 数据、做 编码 用 做 子串 用 密码文本、i、1。
        i 为 i 加 1。
    完。
    可变 h 为 做 SHA_简易 用 数据。
    可变 轮 为 0。
    只要 轮 小于 100：
        h 为 做 SHA_简易 用 一组：h 的 第 0 项、h 的 第 1 项、盐。
        轮 为 轮 加 1。
    完。
    返还 h。
完。

密码_验证 接受 密码文本、盐、期望哈希：
    实际 为 做 密码_哈希 用 密码文本、盐。
    返还 做 哈希_相等 用 实际、期望哈希。
完。

令牌_创建 接受 用户ID、过期时间、密钥：
    签名数据 为 一组：用户ID、过期时间、密钥。
    签名 为 做 SHA_简易 用 签名数据。
    返还 一组：用户ID、过期时间、签名。
完。

令牌_验证 接受 令牌、当前时间、密钥：
    若 令牌 的 第 1 项 小于 当前时间：
        返还 假。
    完。
    期望签名 为 做 SHA_简易 用 一组：令牌 的 第 0 项、令牌 的 第 1 项、密钥。
    返还 做 哈希_相等 用 令牌 的 第 2 项、期望签名。
完。

令牌_用户 接受 令牌：
    返还 令牌 的 第 0 项。
完。

令牌_过期 接受 令牌：
    返还 令牌 的 第 1 项。
完。

权限_读 为 1。

权限_写 为 2。

权限_执行 为 4。

权限_管理 为 8。

权限_有 接受 用户权限、需要权限：
    返还 用户权限 位与 需要权限 等于 需要权限。
完。

权限_授予 接受 用户权限、新权限：
    返还 用户权限 位或 新权限。
完。

权限_撤销 接受 用户权限、权限：
    返还 用户权限 位与 按位取反 权限。
完。

XOR_加密 接受 数据、密钥：
    可变 结果 为 一组：（空）。
    klen 为 做 计数 用 密钥。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 结果、数据 的 第 i 项 位异或 密钥 的 第 i 取余于 klen 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

XOR_解密 接受 加密数据、密钥：
    返还 做 XOR_加密 用 加密数据、密钥。
完。

权限_计数 接受 用户权限：
    可变 c 为 0。
    可变 v 为 用户权限。
    只要 v 大于 0：
        c 为 c 加 (v 位与 1)。
        v 为 v 右移 1。
    完。
    返还 c。
完。

令牌_刷新 接受 旧令牌、新过期、密钥：
    返还 做 令牌_创建 用 旧令牌 的 第 0 项、新过期、密钥。
完。

本章 收束。
