《矩阵运算》

引用《..·基础》。

引用《..·数学·基础·数学库》。

引用《..·数学·矩阵》。

引用《..·数学·线性代数》。

引用《..·算法·数学扩展》。

矩阵运算_矩阵_零 接受 行、列：
    可变 m 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        做 追加 用 m、0。
        i 为 i 加 1。
    完。
    返还 m。
完。

矩阵运算_矩阵_单位 接受 n：
    可变 m 为 做 矩阵_零 用 n、n。
    可变 i 为 0。
    只要 i 小于 n：
        m 的 第 i 乘 n 加 i 项 为 1000。
        i 为 i 加 1。
    完。
    返还 m。
完。

矩阵运算_矩阵_取 接受 m、列数、r、c：
    返还 m 的 第 r 乘 列数 加 c 项。
完。

矩阵运算_矩阵_设 接受 m、列数、r、c、v：
    m 的 第 r 乘 列数 加 c 项 为 v。
完。

矩阵运算_矩阵_加 接受 a、b：
    返还 做 拉链 用 a、b、以 x、y 映射到 x 加 y。
完。

矩阵运算_矩阵_减 接受 a、b：
    返还 做 拉链 用 a、b、以 x、y 映射到 x 减 y。
完。

矩阵_标量乘 接受 m、s：
    返还 做 映射 用 m、以 x 映射到 x 乘 s 除以 1000。
完。

矩阵运算_矩阵_转置 接受 m、行、列：
    可变 结果 为 做 矩阵_零 用 列、行。
    可变 r 为 0。
    只要 r 小于 行：
        可变 c 为 0。
        只要 c 小于 列：
            结果 的 第 c 乘 行 加 r 项 为 m 的 第 r 乘 列 加 c 项。
            c 为 c 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 结果。
完。

矩阵运算_矩阵_乘 接受 a、b、M、K、N：
    可变 结果 为 做 矩阵_零 用 M、N。
    可变 i 为 0。
    只要 i 小于 M：
        可变 j 为 0。
        只要 j 小于 N：
            可变 总和 为 0。
            可变 k 为 0。
            只要 k 小于 K：
                总和 为 总和 加 a 的 第 i 乘 K 加 k 项 乘 b 的 第 k 乘 N 加 j 项 除以 1000。
                k 为 k 加 1。
            完。
            结果 的 第 i 乘 N 加 j 项 为 总和。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵运算_向量_点积 接受 a、b：
    返还 做 折叠 用 做 拉链 用 a、b、以 x、y 映射到 x 乘 y 除以 1000、0、以 s、v 映射到 s 加 v。
完。

矩阵运算_向量_外积 接受 a、b：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a：
        可变 j 为 0。
        只要 j 小于 做 计数 用 b：
            做 追加 用 结果、a 的 第 i 项 乘 b 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

行列式_2x2 接受 m：
    返还 m 的 第 0 项 乘 m 的 第 3 项 除以 1000 减 m 的 第 1 项 乘 m 的 第 2 项 除以 1000。
完。

行列式_3x3 接受 m：
    a 为 m 的 第 0 项 乘 (m 的 第 4 项 乘 m 的 第 8 项 除以 1000 减 m 的 第 5 项 乘 m 的 第 7 项 除以 1000) 除以 1000。
    b 为 m 的 第 1 项 乘 (m 的 第 3 项 乘 m 的 第 8 项 除以 1000 减 m 的 第 5 项 乘 m 的 第 6 项 除以 1000) 除以 1000。
    c 为 m 的 第 2 项 乘 (m 的 第 3 项 乘 m 的 第 7 项 除以 1000 减 m 的 第 4 项 乘 m 的 第 6 项 除以 1000) 除以 1000。
    返还 a 减 b 加 c。
完。

逆矩阵_2x2 接受 m：
    行列式 为 做 行列式_2x2 用 m。
    若 行列式 等于 0：
        返还 一组：（空）。
    完。
    返还 一组：m 的 第 3 项 乘 1000 除以 行列式、0 减 m 的 第 1 项 乘 1000 除以 行列式、0 减 m 的 第 2 项 乘 1000 除以 行列式、m 的 第 0 项 乘 1000 除以 行列式。
完。

矩阵运算_矩阵_迹 接受 m、n：
    可变 tr 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        tr 为 tr 加 m 的 第 i 乘 n 加 i 项。
        i 为 i 加 1。
    完。
    返还 tr。
完。

矩阵_元素数 接受 m：
    返还 做 计数 用 m。
完。

矩阵运算_向量_范数 接受 v：
    返还 做 整数sqrt 用 做 折叠 用 v、0、以 s、x 映射到 s 加 x 乘 x 除以 1000。
完。

矩阵运算_向量_归一化 接受 v：
    n 为 做 向量_范数 用 v、0。
    若 n 等于 0：
        返还 v。
    完。
    返还 做 映射 用 v、以 x 映射到 x 乘 1000 除以 n。
完。

矩阵_F范数 接受 m：
    返还 做 整数sqrt 用 做 折叠 用 m、0、以 s、x 映射到 s 加 x 乘 x 除以 1000。
完。

矩阵_LU分解 接受 m、n：
    可变 LU 为 一组：（空）。
    可变 总和2 为 0。
    可变 i 为 0。
    只要 i 小于 n 乘 n：
        做 追加 用 LU、m 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 j 为 i。
        只要 j 小于 n：
            可变 总和 为 0。
            可变 k 为 0。
            只要 k 小于 i：
                总和 为 总和 加 LU 的 第 i 乘 n 加 k 项 乘 LU 的 第 k 乘 n 加 j 项 除以 1000。
                k 为 k 加 1。
            完。
            LU 的 第 i 乘 n 加 j 项 为 LU 的 第 i 乘 n 加 j 项 减 总和。
            j 为 j 加 1。
        完。
        j 为 i 加 1。
        只要 j 小于 n：
            可变 k2 为 0。
            只要 k2 小于 i：
                总和2 为 总和2 加 LU 的 第 j 乘 n 加 k2 项 乘 LU 的 第 k2 乘 n 加 i 项 除以 1000。
                k2 为 k2 加 1。
            完。
            若 LU 的 第 i 乘 n 加 i 项 不等于 0：
                LU 的 第 j 乘 n 加 i 项 为 (LU 的 第 j 乘 n 加 i 项 减 总和2) 乘 1000 除以 LU 的 第 i 乘 n 加 i 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 LU。
完。

矩阵_LU求解 接受 LU、n、b：
    可变 y 为 一组：（空）。
    可变 总和2 为 0。
    可变 x 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 y、0。
        做 追加 用 x、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 总和 为 0。
        可变 j 为 0。
        只要 j 小于 i：
            总和 为 总和 加 LU 的 第 i 乘 n 加 j 项 乘 y 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        y 的 第 i 项 为 b 的 第 i 项 减 总和。
        i 为 i 加 1。
    完。
    i 为 n 减 1。
    只要 i 大于或等于 0：
        可变 j2 为 i 加 1。
        只要 j2 小于 n：
            总和2 为 总和2 加 LU 的 第 i 乘 n 加 j2 项 乘 x 的 第 j2 项 除以 1000。
            j2 为 j2 加 1。
        完。
        若 LU 的 第 i 乘 n 加 i 项 不等于 0：
            x 的 第 i 项 为 (y 的 第 i 项 减 总和2) 乘 1000 除以 LU 的 第 i 乘 n 加 i 项。
        完。
        i 为 i 减 1。
    完。
    返还 x。
完。

行列式_NxN 接受 m、n：
    LU 为 做 矩阵_LU分解 用 m、n。
    可变 行列式 为 1000。
    可变 i 为 0。
    只要 i 小于 n：
        行列式 为 行列式 乘 LU 的 第 i 乘 n 加 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 行列式。
完。

逆矩阵_NxN 接受 m、n：
    LU 为 做 矩阵_LU分解 用 m、n。
    可变 逆 为 做 矩阵_零 用 n、n。
    可变 j 为 0。
    只要 j 小于 n：
        可变 e 为 一组：（空）。
        可变 k 为 0。
        只要 k 小于 n：
            若 k 等于 j：
                做 追加 用 e、1000。
            否则：
                做 追加 用 e、0。
            完。
            k 为 k 加 1。
        完。
        列 为 做 矩阵_LU求解 用 LU、n、e。
        k 为 0。
        只要 k 小于 n：
            逆 的 第 k 乘 n 加 j 项 为 列 的 第 k 项。
            k 为 k 加 1。
        完。
        j 为 j 加 1。
    完。
    返还 逆。
完。

特征值_幂迭代 接受 m、n、最大迭代：
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 v、1000 加 (i 乘 7 加 3) 取余于 500。
        i 为 i 加 1。
    完。
    可变 特征值 为 0。
    可变 迭代 为 0。
    只要 迭代 小于 最大迭代：
        可变 w 为 一组：（空）。
        i 为 0。
        只要 i 小于 n：
            可变 总和 为 0。
            可变 j 为 0。
            只要 j 小于 n：
                总和 为 总和 加 m 的 第 i 乘 n 加 j 项 乘 v 的 第 j 项 除以 1000。
                j 为 j 加 1。
            完。
            做 追加 用 w、总和。
            i 为 i 加 1。
        完。
        可变 最大 为 0。
        i 为 0。
        只要 i 小于 n：
            可变 绝对 为 w 的 第 i 项。
            若 绝对 小于 0：
                绝对 为 0 减 绝对。
            完。
            若 绝对 大于 最大：
                最大 为 绝对。
            完。
            i 为 i 加 1。
        完。
        特征值 为 最大。
        若 最大 大于 0：
            i 为 0。
            只要 i 小于 n：
                v 的 第 i 项 为 w 的 第 i 项 乘 1000 除以 最大。
                i 为 i 加 1。
            完。
        完。
        迭代 为 迭代 加 1。
    完。
    返还 一组：特征值、v。
完。

矩阵_QR分解 接受 m、行、列：
    可变 Q 为 做 矩阵_零 用 行、列。
    可变 R 为 做 矩阵_零 用 列、列。
    可变 j 为 0。
    只要 j 小于 列：
        可变 v 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 行：
            做 追加 用 v、m 的 第 i 乘 列 加 j 项。
            i 为 i 加 1。
        完。
        可变 k 为 0。
        只要 k 小于 j：
            可变 点积 为 0。
            i 为 0。
            只要 i 小于 行：
                点积 为 点积 加 Q 的 第 i 乘 列 加 k 项 乘 v 的 第 i 项 除以 1000。
                i 为 i 加 1。
            完。
            R 的 第 k 乘 列 加 j 项 为 点积。
            i 为 0。
            只要 i 小于 行：
                v 的 第 i 项 为 v 的 第 i 项 减 点积 乘 Q 的 第 i 乘 列 加 k 项 除以 1000。
                i 为 i 加 1。
            完。
            k 为 k 加 1。
        完。
        可变 范数 为 0。
        i 为 0。
        只要 i 小于 行：
            范数 为 范数 加 v 的 第 i 项 乘 v 的 第 i 项 除以 1000。
            i 为 i 加 1。
        完。
        范数 为 做 整数sqrt 用 范数。
        R 的 第 j 乘 列 加 j 项 为 范数。
        若 范数 大于 0：
            i 为 0。
            只要 i 小于 行：
                Q 的 第 i 乘 列 加 j 项 为 v 的 第 i 项 乘 1000 除以 范数。
                i 为 i 加 1。
            完。
        完。
        j 为 j 加 1。
    完。
    返还 一组：Q、R。
完。

矩阵_Kronecker 接受 A、B、aM、aN、bM、bN：
    rM 为 aM 乘 bM。
    rN 为 aN 乘 bN。
    可变 结果 为 做 矩阵_零 用 rM、rN。
    可变 i 为 0。
    只要 i 小于 aM：
        可变 j 为 0。
        只要 j 小于 aN：
            aij 为 A 的 第 i 乘 aN 加 j 项。
            可变 p 为 0。
            只要 p 小于 bM：
                可变 q 为 0。
                只要 q 小于 bN：
                    结果 的 第 (i 乘 bM 加 p) 乘 rN 加 (j 乘 bN 加 q) 项 为 aij 乘 B 的 第 p 乘 bN 加 q 项 除以 1000。
                    q 为 q 加 1。
                完。
                p 为 p 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵运算_矩阵_幂 接受 m、n、指数：
    若 指数 等于 0：
        返还 做 矩阵_单位 用 n。
    完。
    若 指数 等于 1：
        返还 m。
    完。
    可变 结果 为 做 矩阵_单位 用 n。
    可变 底 为 m。
    可变 指数值 为 指数。
    只要 指数值 大于 0：
        若 指数值 取余于 2 等于 1：
            结果 为 做 矩阵_乘 用 结果、底、n、n、n、0。
        完。
        底 为 做 矩阵_乘 用 底、底、n、n、n、0。
        指数值 为 指数值 除以 2。
    完。
    返还 结果。
完。

矩阵_条件数 接受 m、n：
    范数A 为 做 矩阵_F范数 用 m。
    逆A 为 做 逆矩阵_NxN 用 m、n。
    范数逆 为 做 矩阵_F范数 用 逆A。
    返还 范数A 乘 范数逆 除以 1000。
完。

矩阵_秩 接受 m、行、列：
    可变 拷贝 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        做 追加 用 拷贝、m 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 秩 为 0。
    可变 主列 为 0。
    可变 r 为 0。
    只要 r 小于 行 并且 主列 小于 列：
        可变 最大行 为 r。
        可变 最大值 为 拷贝 的 第 r 乘 列 加 主列 项。
        若 最大值 小于 0：
            最大值 为 0 减 最大值。
        完。
        i 为 r 加 1。
        只要 i 小于 行：
            可变 绝对 为 拷贝 的 第 i 乘 列 加 主列 项。
            若 绝对 小于 0：
                绝对 为 0 减 绝对。
            完。
            若 绝对 大于 最大值：
                最大值 为 绝对。
                最大行 为 i。
            完。
            i 为 i 加 1。
        完。
        若 最大值 小于 10：
            主列 为 主列 加 1。
        否则：
            若 最大行 不等于 r：
                可变 c 为 0。
                只要 c 小于 列：
                    暂存 为 拷贝 的 第 r 乘 列 加 c 项。
                    拷贝 的 第 r 乘 列 加 c 项 为 拷贝 的 第 最大行 乘 列 加 c 项。
                    拷贝 的 第 最大行 乘 列 加 c 项 为 暂存。
                    c 为 c 加 1。
                完。
            完。
            i 为 r 加 1。
            只要 i 小于 行：
                若 拷贝 的 第 r 乘 列 加 主列 项 不等于 0：
                    因子 为 拷贝 的 第 i 乘 列 加 主列 项 乘 1000 除以 拷贝 的 第 r 乘 列 加 主列 项。
                    可变 c2 为 主列。
                    只要 c2 小于 列：
                        拷贝 的 第 i 乘 列 加 c2 项 为 拷贝 的 第 i 乘 列 加 c2 项 减 因子 乘 拷贝 的 第 r 乘 列 加 c2 项 除以 1000。
                        c2 为 c2 加 1。
                    完。
                完。
                i 为 i 加 1。
            完。
            秩 为 秩 加 1。
            r 为 r 加 1。
            主列 为 主列 加 1。
        完。
    完。
    返还 秩。
完。

可变 _矩阵_乘法次数 为 0。

可变 _矩阵_求逆次数 为 0。

可变 _矩阵_最大维度 为 0。

矩阵_记录乘法 接受 维度：
    _矩阵_乘法次数 为 _矩阵_乘法次数 加 1。
    若 维度 大于 _矩阵_最大维度：
        _矩阵_最大维度 为 维度。
    完。
完。

矩阵_记录求逆 接受 零参：
    _矩阵_求逆次数 为 _矩阵_求逆次数 加 1。
完。

矩阵_性能预测 接受 维度：
    复杂度 为 维度 乘 维度 乘 维度。
    若 复杂度 大于 1000000：
        返还 「高开销: 建议使用Strassen算法」。
    完。
    若 复杂度 大于 10000：
        返还 「中等开销」。
    完。
    返还 「低开销」。
完。

矩阵_内存估算 接受 行、列：
    返还 行 乘 列 乘 8。
完。

本章 收束。
