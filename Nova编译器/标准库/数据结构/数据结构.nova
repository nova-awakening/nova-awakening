《数据结构》

引用《..·基础》。

引用《..·集合·堆·最小堆》。

引用《..·集合·队列·队列》。

数据结构_栈_创建 接受 容量：
    可变 数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数组、0。
        i 为 i 加 1。
    完。
    返还 一组：数组、0、容量。
完。

数据结构_栈_压入 接受 s、值：
    若 s 的 第 1 项 大于或等于 s 的 第 2 项：
        返还 假。
    完。
    s 的 第 0 项 的 第 s 的 第 1 项 项 为 值。
    s 的 第 1 项 为 s 的 第 1 项 加 1。
    返还 真。
完。

数据结构_栈_弹出 接受 s：
    若 s 的 第 1 项 等于 0：
        返还 负 1。
    完。
    s 的 第 1 项 为 s 的 第 1 项 减 1。
    返还 s 的 第 0 项 的 第 s 的 第 1 项 项。
完。

数据结构_栈_顶 接受 s：
    若 s 的 第 1 项 等于 0：
        返还 负 1。
    完。
    返还 s 的 第 0 项 的 第 s 的 第 1 项 减 1 项。
完。

数据结构_栈_大小 接受 s：
    返还 s 的 第 1 项。
完。

栈_空吗 接受 s：
    返还 s 的 第 1 项 等于 0。
完。

栈_满吗 接受 s：
    返还 s 的 第 1 项 大于或等于 s 的 第 2 项。
完。

栈_清空 接受 s：
    s 的 第 1 项 为 0。
完。

数据结构_队列_创建 接受 容量：
    返还 一组：一组：（空）、0、容量。
完。

数据结构_队列_入队 接受 q、值：
    若 做 计数 用 q 的 第 0 项 减 q 的 第 1 项 大于或等于 q 的 第 2 项：
        返还 假。
    完。
    做 追加 用 q 的 第 0 项、值。
    返还 真。
完。

数据结构_队列_出队 接受 q：
    可变 新列表 为 一组：（空）。
    若 q 的 第 1 项 大于或等于 做 计数 用 q 的 第 0 项：
        返还 负 1。
    完。
    v 为 q 的 第 0 项 的 第 q 的 第 1 项 项。
    q 的 第 1 项 为 q 的 第 1 项 加 1。
    若 q 的 第 1 项 大于 64：
        可变 i 为 q 的 第 1 项。
        只要 i 小于 做 计数 用 q 的 第 0 项：
            做 追加 用 新列表、q 的 第 0 项 的 第 i 项。
            i 为 i 加 1。
        完。
        q 的 第 0 项 为 新列表。
        q 的 第 1 项 为 0。
    完。
    返还 v。
完。

数据结构_队列_头 接受 q：
    若 q 的 第 1 项 大于或等于 做 计数 用 q 的 第 0 项：
        返还 负 1。
    完。
    返还 q 的 第 0 项 的 第 q 的 第 1 项 项。
完。

数据结构_队列_大小 接受 q：
    返还 做 计数 用 q 的 第 0 项 减 q 的 第 1 项。
完。

数据结构_队列_空吗 接受 q：
    返还 做 数据结构_队列_大小 用 q 等于 0。
完。

双端_创建 接受 零参：
    容量 为 64。
    可变 数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数组、0。
        i 为 i 加 1。
    完。
    返还 一组：数组、容量、0、0。
完。

双端_后插 接受 d、值：
    若 d 的 第 4 项 大于或等于 d 的 第 1 项：
        返还 假。
    完。
    d 的 第 0 项 的 第 d 的 第 3 项 项 为 值。
    d 的 第 3 项 为 (d 的 第 3 项 加 1) 取余于 d 的 第 1 项。
    d 的 第 4 项 为 d 的 第 4 项 加 1。
    返还 真。
完。

双端_前插 接受 d、值：
    若 d 的 第 4 项 大于或等于 d 的 第 1 项：
        返还 假。
    完。
    d 的 第 2 项 为 (d 的 第 2 项 减 1 加 d 的 第 1 项) 取余于 d 的 第 1 项。
    d 的 第 0 项 的 第 d 的 第 2 项 项 为 值。
    d 的 第 4 项 为 d 的 第 4 项 加 1。
    返还 真。
完。

双端_前出 接受 d：
    若 d 的 第 4 项 等于 0：
        返还 负 1。
    完。
    v 为 d 的 第 0 项 的 第 d 的 第 2 项 项。
    d 的 第 2 项 为 (d 的 第 2 项 加 1) 取余于 d 的 第 1 项。
    d 的 第 4 项 为 d 的 第 4 项 减 1。
    返还 v。
完。

双端_后出 接受 d：
    若 d 的 第 4 项 等于 0：
        返还 负 1。
    完。
    d 的 第 3 项 为 (d 的 第 3 项 减 1 加 d 的 第 1 项) 取余于 d 的 第 1 项。
    v 为 d 的 第 0 项 的 第 d 的 第 3 项 项。
    d 的 第 4 项 为 d 的 第 4 项 减 1。
    返还 v。
完。

双端_大小 接受 d：
    返还 d 的 第 4 项。
完。

双端_前 接受 d：
    若 d 的 第 4 项 等于 0：
        返还 负 1。
    完。
    返还 d 的 第 0 项 的 第 d 的 第 2 项 项。
完。

双端_后 接受 d：
    若 d 的 第 4 项 等于 0：
        返还 负 1。
    完。
    返还 d 的 第 0 项 的 第 (d 的 第 3 项 减 1 加 d 的 第 1 项) 取余于 d 的 第 1 项 项。
完。

数据结构_优先队列_创建 接受 零参：
    返还 一组：一组：（空）。
完。

数据结构_优先队列_大小 接受 pq：
    返还 做 计数 用 pq 的 第 0 项。
完。

数据结构_优先队列_空吗 接受 pq：
    返还 做 计数 用 pq 的 第 0 项 等于 0。
完。

数据结构_堆_上浮 接受 h、索引：
    可变 i 为 索引。
    只要 i 不等于 0：
        父 为 (i 减 1) 除以 2。
        若 h 的 第 i 项 小于 h 的 第 父 项：
            临时 为 h 的 第 i 项。
            h 的 第 i 项 为 h 的 第 父 项。
            h 的 第 父 项 为 临时。
            i 为 父。
        否则：
            返还 空。
        完。
    完。
完。

数据结构_堆_下沉 接受 h、索引：
    n 为 做 计数 用 h。
    可变 i 为 索引。
    只要 i 乘 2 加 1 小于 n：
        可变 最小 为 i。
        左 为 i 乘 2 加 1。
        右 为 i 乘 2 加 2。
        若 左 小于 n 并且 h 的 第 左 项 小于 h 的 第 最小 项：
            最小 为 左。
        完。
        若 右 小于 n 并且 h 的 第 右 项 小于 h 的 第 最小 项：
            最小 为 右。
        完。
        若 最小 等于 i：
            返还 空。
        完。
        临时 为 h 的 第 i 项。
        h 的 第 i 项 为 h 的 第 最小 项。
        h 的 第 最小 项 为 临时。
        i 为 最小。
    完。
完。

堆_下沉3 接受 h、size、索引：
    可变 i 为 索引。
    只要 i 乘 2 加 1 小于 size：
        可变 最大 为 i。
        左 为 i 乘 2 加 1。
        右 为 i 乘 2 加 2。
        若 左 小于 size 并且 h 的 第 左 项 大于 h 的 第 最大 项：
            最大 为 左。
        完。
        若 右 小于 size 并且 h 的 第 右 项 大于 h 的 第 最大 项：
            最大 为 右。
        完。
        若 最大 等于 i：
            返还 空。
        完。
        临时 为 h 的 第 i 项。
        h 的 第 i 项 为 h 的 第 最大 项。
        h 的 第 最大 项 为 临时。
        i 为 最大。
    完。
完。

优先队列_插入 接受 pq、值：
    做 追加 用 pq 的 第 0 项、值。
    做 堆_上浮 用 pq 的 第 0 项、做 计数 用 pq 的 第 0 项 减 1。
完。

优先队列_弹出 接受 pq：
    n 为 做 计数 用 pq 的 第 0 项。
    若 n 等于 0：
        返还 负 1。
    完。
    最小 为 pq 的 第 0 项 的 第 0 项。
    pq 的 第 0 项 的 第 0 项 为 pq 的 第 0 项 的 第 n 减 1 项。
    做 删除 用 pq 的 第 0 项、n 减 1。
    若 做 计数 用 pq 的 第 0 项 不等于 0：
        做 堆_下沉 用 pq 的 第 0 项、0。
    完。
    返还 最小。
完。

数据结构_优先队列_顶 接受 pq：
    若 做 计数 用 pq 的 第 0 项 等于 0：
        返还 负 1。
    完。
    返还 pq 的 第 0 项 的 第 0 项。
完。

环形_创建_预分配 接受 容量：
    可变 数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数组、0。
        i 为 i 加 1。
    完。
    返还 一组：数组、容量、0、0。
完。

环形_写 接受 rb、值：
    若 rb 的 第 4 项 大于或等于 rb 的 第 1 项：
        返还 假。
    完。
    rb 的 第 0 项 的 第 rb 的 第 3 项 项 为 值。
    rb 的 第 3 项 为 (rb 的 第 3 项 加 1) 取余于 rb 的 第 1 项。
    rb 的 第 4 项 为 rb 的 第 4 项 加 1。
    返还 真。
完。

环形_读 接受 rb：
    若 rb 的 第 4 项 等于 0：
        返还 负 1。
    完。
    v 为 rb 的 第 0 项 的 第 rb 的 第 2 项 项。
    rb 的 第 2 项 为 (rb 的 第 2 项 加 1) 取余于 rb 的 第 1 项。
    rb 的 第 4 项 为 rb 的 第 4 项 减 1。
    返还 v。
完。

环形_大小 接受 rb：
    返还 rb 的 第 4 项。
完。

环形_满吗 接受 rb：
    返还 rb 的 第 4 项 大于或等于 rb 的 第 1 项。
完。

环形_空吗 接受 rb：
    返还 rb 的 第 4 项 等于 0。
完。

数据_位图_创建 接受 位数：
    可变 数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 位数：
        做 追加 用 数组、0。
        i 为 i 加 1。
    完。
    返还 一组：数组、位数。
完。

数据结构_位图_设置 接受 bm、位：
    若 位 大于或等于 0 并且 位 小于 bm 的 第 1 项：
        bm 的 第 0 项 的 第 位 项 为 1。
    完。
完。

数据结构_位图_清除 接受 bm、位：
    若 位 大于或等于 0 并且 位 小于 bm 的 第 1 项：
        bm 的 第 0 项 的 第 位 项 为 0。
    完。
完。

数据_位图_测试 接受 bm、位：
    若 位 大于或等于 0 并且 位 小于 bm 的 第 1 项：
        返还 bm 的 第 0 项 的 第 位 项 等于 1。
    完。
    返还 假。
完。

数据_位图_翻转 接受 bm、位：
    若 位 大于或等于 0 并且 位 小于 bm 的 第 1 项：
        若 bm 的 第 0 项 的 第 位 项 等于 0：
            bm 的 第 0 项 的 第 位 项 为 1。
        否则：
            bm 的 第 0 项 的 第 位 项 为 0。
        完。
    完。
完。

数据_位图_计数 接受 bm：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 bm 的 第 1 项：
        若 bm 的 第 0 项 的 第 i 项 等于 1：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

位图_首个1 接受 bm：
    可变 i 为 0。
    只要 i 小于 bm 的 第 1 项：
        若 bm 的 第 0 项 的 第 i 项 等于 1：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

位图_首个0 接受 bm：
    可变 i 为 0。
    只要 i 小于 bm 的 第 1 项：
        若 bm 的 第 0 项 的 第 i 项 等于 0：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

有序映射_创建 接受 零参：
    返还 一组：空。
完。

BST_插入 接受 节点、键、值：
    若 节点 等于 空：
        返还 一组：键、值、空、空。
    完。
    若 键 小于 节点 的 第 0 项：
        节点 的 第 2 项 为 做 BST_插入 用 节点 的 第 2 项、键、值。
    否则若 键 等于 节点 的 第 0 项：
        节点 的 第 1 项 为 值。
    否则：
        节点 的 第 3 项 为 做 BST_插入 用 节点 的 第 3 项、键、值。
    完。
    返还 节点。
完。

BST_查找 接受 节点、键：
    若 节点 等于 空：
        返还 空。
    完。
    若 键 等于 节点 的 第 0 项：
        返还 节点 的 第 1 项。
    完。
    若 键 小于 节点 的 第 0 项：
        返还 做 BST_查找 用 节点 的 第 2 项、键。
    完。
    返还 做 BST_查找 用 节点 的 第 3 项、键。
完。

BST_最小 接受 节点：
    若 节点 等于 空：
        返还 空。
    完。
    若 节点 的 第 2 项 等于 空：
        返还 节点 的 第 0 项。
    完。
    返还 做 BST_最小 用 节点 的 第 2 项。
完。

BST_最大 接受 节点：
    若 节点 等于 空：
        返还 空。
    完。
    若 节点 的 第 3 项 等于 空：
        返还 节点 的 第 0 项。
    完。
    返还 做 BST_最大 用 节点 的 第 3 项。
完。

BST_大小 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    返还 1 加 做 BST_大小 用 节点 的 第 2 项 加 做 BST_大小 用 节点 的 第 3 项。
完。

数据_LRU_创建 接受 容量：
    返还 一组：一组：（空）、一组：（空）、一组：（空）、一组：（空）、容量。
完。

数据_LRU_获取 接受 lru、键：
    可变 i 为 0。
    只要 i 小于 做 计数 用 lru 的 第 0 项：
        若 lru 的 第 3 项 的 第 i 项 等于 1 并且 lru 的 第 0 项 的 第 i 项 等于 键：
            lru 的 第 5 项 为 lru 的 第 5 项 加 1。
            lru 的 第 2 项 的 第 i 项 为 lru 的 第 5 项。
            返还 lru 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

数据_LRU_放入 接受 lru、键、值：
    lru 的 第 5 项 为 lru 的 第 5 项 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 lru 的 第 0 项：
        若 lru 的 第 3 项 的 第 i 项 等于 1 并且 lru 的 第 0 项 的 第 i 项 等于 键：
            lru 的 第 1 项 的 第 i 项 为 值。
            lru 的 第 2 项 的 第 i 项 为 lru 的 第 5 项。
            返还 空。
        完。
        i 为 i 加 1。
    完。
    若 lru 的 第 6 项 大于或等于 lru 的 第 4 项：
        可变 最旧idx 为 负 1。
        可变 最旧t 为 lru 的 第 5 项 加 1。
        可变 j 为 0。
        只要 j 小于 做 计数 用 lru 的 第 0 项：
            若 lru 的 第 3 项 的 第 j 项 等于 1 并且 lru 的 第 2 项 的 第 j 项 小于 最旧t：
                最旧t 为 lru 的 第 2 项 的 第 j 项。
                最旧idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最旧idx 大于或等于 0：
            lru 的 第 3 项 的 第 最旧idx 项 为 0。
            lru 的 第 6 项 为 lru 的 第 6 项 减 1。
        完。
    完。
    做 追加 用 lru 的 第 0 项、键。
    做 追加 用 lru 的 第 1 项、值。
    做 追加 用 lru 的 第 2 项、lru 的 第 5 项。
    做 追加 用 lru 的 第 3 项、1。
    lru 的 第 6 项 为 lru 的 第 6 项 加 1。
完。

数据_LRU_大小 接受 lru：
    返还 lru 的 第 6 项。
完。

栈_转列表 接受 s：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 s 的 第 1 项：
        做 追加 用 r、s 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

集合_并集 接受 a、b：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a：
        做 追加 用 结果、a 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 b：
        可变 存在 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 结果：
            若 结果 的 第 j 项 等于 b 的 第 i 项：
                存在 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 存在：
            做 追加 用 结果、b 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_交集 接受 a、b：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a：
        可变 j 为 0。
        只要 j 小于 做 计数 用 b：
            若 a 的 第 i 项 等于 b 的 第 j 项：
                做 追加 用 结果、a 的 第 i 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_差集 接受 a、b：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a：
        可变 在b中 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 b：
            若 a 的 第 i 项 等于 b 的 第 j 项：
                在b中 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 在b中：
            做 追加 用 结果、a 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

最小堆_上浮 接受 堆、索引：
    只要 索引 大于 0：
        父 为 (索引 减 1) 除以 2。
        若 堆 的 第 索引 项 小于 堆 的 第 父 项：
            临 为 堆 的 第 索引 项。
            堆 的 第 索引 项 为 堆 的 第 父 项。
            堆 的 第 父 项 为 临。
            索引 为 父。
        否则：
            返还。
        完。
    完。
完。

堆_下沉_区间 接受 堆、索引、大小：
    只要 索引 乘 2 加 1 小于 大小：
        可变 最小 为 索引 乘 2 加 1。
        若 最小 加 1 小于 大小 并且 堆 的 第 最小 加 1 项 小于 堆 的 第 最小 项：
            最小 为 最小 加 1。
        完。
        若 堆 的 第 最小 项 小于 堆 的 第 索引 项：
            临 为 堆 的 第 索引 项。
            堆 的 第 索引 项 为 堆 的 第 最小 项。
            堆 的 第 最小 项 为 临。
            索引 为 最小。
        否则：
            返还。
        完。
    完。
完。

数据_堆_插入 接受 堆、值：
    做 追加 用 堆、值。
    做 堆_上浮 用 堆、做 计数 用 堆 减 1。
完。

堆_弹出最小 接受 堆：
    若 做 计数 用 堆 等于 0：
        返还 负 1。
    完。
    最小 为 堆 的 第 0 项。
    末 为 做 计数 用 堆 减 1。
    堆 的 第 0 项 为 堆 的 第 末 项。
    做 删除 用 堆、末。
    若 做 计数 用 堆 大于 0：
        做 堆_下沉_区间 用 堆、0、做 计数 用 堆。
    完。
    返还 最小。
完。

环形_创建 接受 容量：
    返还 一组：一组：（空）、0、容量。
完。

环形_写入 接受 缓冲、值：
    若 做 计数 用 缓冲 的 第 0 项 小于 缓冲 的 第 3 项：
        做 追加 用 缓冲 的 第 0 项、值。
    否则：
        缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 取余于 缓冲 的 第 3 项 项 为 值。
    完。
    缓冲 的 第 2 项 为 缓冲 的 第 2 项 加 1。
完。

环形_读取 接受 缓冲：
    若 缓冲 的 第 1 项 大于或等于 缓冲 的 第 2 项：
        返还 负 1。
    完。
    值 为 缓冲 的 第 0 项 的 第 缓冲 的 第 1 项 取余于 缓冲 的 第 3 项 项。
    缓冲 的 第 1 项 为 缓冲 的 第 1 项 加 1。
    返还 值。
完。

环形_可用 接受 缓冲：
    返还 缓冲 的 第 2 项 减 缓冲 的 第 1 项。
完。

可变 _DS_操作次数 为 0。

可变 _DS_最大容量 为 0。

可变 _DS_溢出次数 为 0。

DS_记录操作 接受 容量：
    _DS_操作次数 为 _DS_操作次数 加 1。
    若 容量 大于 _DS_最大容量：
        _DS_最大容量 为 容量。
    完。
完。

DS_记录溢出 接受 零参：
    _DS_溢出次数 为 _DS_溢出次数 加 1。
完。

DS_重置 接受 零参：
    _DS_操作次数 为 0。
    _DS_最大容量 为 0。
    _DS_溢出次数 为 0。
完。

数据结构_双端队列_创建 接受 零参：
    返还 一组：一组：（空）、0。
完。

数据结构_双端队列_前插 接受 q、值：
    可变 新列 为 一组：值。
    可变 i 为 0。
    只要 i 小于 做 计数 用 q 的 第 0 项：
        做 追加 用 新列、q 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    q 的 第 0 项 为 新列。
    q 的 第 1 项 为 q 的 第 1 项 加 1。
完。

数据结构_双端队列_后插 接受 q、值：
    做 追加 用 q 的 第 0 项、值。
    q 的 第 1 项 为 q 的 第 1 项 加 1。
完。

数据结构_双端队列_前取 接受 q：
    若 q 的 第 1 项 等于 0：
        返还 负 1。
    完。
    值 为 q 的 第 0 项 的 第 0 项。
    可变 新列 为 一组：（空）。
    可变 i 为 1。
    只要 i 小于 做 计数 用 q 的 第 0 项：
        做 追加 用 新列、q 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    q 的 第 0 项 为 新列。
    q 的 第 1 项 为 q 的 第 1 项 减 1。
    返还 值。
完。

数据结构_双端队列_大小 接受 q：
    返还 q 的 第 1 项。
完。

本章 收束。
