《优先队列》

引用《..·集合·堆·最小堆》。

引用《..·基础》。

优先队列_堆_创建 接受 模式：
    返还 一组：一组：（空）、模式、一组：0。
完。

优先队列_堆_大小 接受 堆：
    返还 做 计数 用 堆 的 第 0 项。
完。

优先队列_堆_空 接受 堆：
    返还 做 计数 用 堆 的 第 0 项 等于 0。
完。

堆_阴阳 接受 堆：
    若 堆 的 第 1 项 等于 0：
        返还 「阴」。
    完。
    返还 「阳」。
完。

堆_父 接受 i：
    返还 (i 减 1) 除以 2。
完。

堆_左子 接受 i：
    返还 2 乘 i 加 1。
完。

堆_右子 接受 i：
    返还 2 乘 i 加 2。
完。

堆_比较 接受 堆、a、b：
    若 堆 的 第 1 项 等于 0：
        返还 a 小于 b。
    完。
    返还 a 大于 b。
完。

堆_交换 接受 数据、i、j：
    临时 为 数据 的 第 i 项。
    数据 的 第 i 项 为 数据 的 第 j 项。
    数据 的 第 j 项 为 临时。
完。

优先队列_堆_上浮 接受 堆、索引：
    数据 为 堆 的 第 0 项。
    可变 i 为 索引。
    只要 i 大于 0：
        父 为 做 堆_父 用 i。
        若 做 堆_比较 用 堆、数据 的 第 i 项 的 第 0 项、数据 的 第 父 项 的 第 0 项 等于 1：
            做 堆_交换 用 数据、i、父。
            i 为 父。
        否则：
            止。
        完。
    完。
完。

优先队列_堆_下沉 接受 堆、索引：
    数据 为 堆 的 第 0 项。
    大小 为 做 计数 用 数据。
    可变 i 为 索引。
    只要 真：
        左 为 做 堆_左子 用 i。
        右 为 做 堆_右子 用 i。
        可变 最优 为 i。
        若 左 小于 大小 并且 做 堆_比较 用 堆、数据 的 第 左 项 的 第 0 项、数据 的 第 最优 项 的 第 0 项 等于 1：
            最优 为 左。
        完。
        若 右 小于 大小 并且 做 堆_比较 用 堆、数据 的 第 右 项 的 第 0 项、数据 的 第 最优 项 的 第 0 项 等于 1：
            最优 为 右。
        完。
        若 最优 不等于 i：
            做 堆_交换 用 数据、i、最优。
            i 为 最优。
        否则：
            止。
        完。
    完。
完。

优先队列_堆_插入 接受 堆、优先级、值：
    统计 为 堆 的 第 2 项。
    统计 的 第 0 项 为 统计 的 第 0 项 加 1。
    做 追加 用 堆 的 第 0 项、一组：优先级、值。
    做 优先队列_堆_上浮 用 堆、做 计数 用 堆 的 第 0 项 减 1。
完。

优先队列_堆_弹出 接受 堆：
    统计 为 堆 的 第 2 项。
    统计 的 第 1 项 为 统计 的 第 1 项 加 1。
    数据 为 堆 的 第 0 项。
    若 做 计数 用 数据 等于 0：
        返还 空。
    完。
    顶 为 数据 的 第 0 项。
    末 为 数据 的 第 做 计数 用 数据 减 1 项。
    数据 的 第 0 项 为 末。
    做 删除 用 数据、做 计数 用 数据 减 1。
    若 做 计数 用 数据 大于 0：
        做 优先队列_堆_下沉 用 堆、0。
    完。
    返还 顶。
完。

优先队列_堆_顶 接受 堆：
    若 做 计数 用 堆 的 第 0 项 等于 0：
        返还 空。
    完。
    返还 堆 的 第 0 项 的 第 0 项。
完。

优先队列_堆排序 接受 列表：
    堆 为 做 优先队列_堆_创建 用 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 优先队列_堆_插入 用 堆、列表 的 第 i 项、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    只要 做 优先队列_堆_空 用 堆 等于 0：
        项 为 做 优先队列_堆_弹出 用 堆。
        做 追加 用 结果、项 的 第 1 项。
    完。
    返还 结果。
完。

k路合并 接受 列表组：
    堆 为 做 优先队列_堆_创建 用 0。
    可变 索引表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表组：
        做 追加 用 索引表、0。
        若 做 计数 用 列表组 的 第 i 项 大于 0：
            做 优先队列_堆_插入 用 堆、列表组 的 第 i 项 的 第 0 项、i。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    只要 做 优先队列_堆_空 用 堆 等于 0：
        项 为 做 优先队列_堆_弹出 用 堆。
        列表idx 为 项 的 第 1 项。
        做 追加 用 结果、项 的 第 0 项。
        索引表 的 第 列表idx 项 为 索引表 的 第 列表idx 项 加 1。
        若 索引表 的 第 列表idx 项 小于 做 计数 用 列表组 的 第 列表idx 项：
            做 优先队列_堆_插入 用 堆、列表组 的 第 列表idx 项 的 第 索引表 的 第 列表idx 项 项、列表idx。
        完。
    完。
    返还 结果。
完。

前K个 接受 列表、k：
    堆 为 做 优先队列_堆_创建 用 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 优先队列_堆_大小 用 堆 小于 k：
            做 优先队列_堆_插入 用 堆、列表 的 第 i 项、列表 的 第 i 项。
        否则：
            堆顶 为 做 优先队列_堆_顶 用 堆。
            若 列表 的 第 i 项 大于 堆顶 的 第 0 项：
                做 优先队列_堆_弹出 用 堆。
                做 优先队列_堆_插入 用 堆、列表 的 第 i 项、列表 的 第 i 项。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    只要 做 优先队列_堆_空 用 堆 等于 0：
        弹出项 为 做 优先队列_堆_弹出 用 堆。
        做 追加 用 结果、弹出项 的 第 1 项。
    完。
    返还 结果。
完。

堆_阴阳验证 接受 堆：
    数据 为 堆 的 第 0 项。
    n 为 做 计数 用 数据。
    可变 i 为 0。
    只要 i 小于 n：
        左 为 做 堆_左子 用 i。
        右 为 做 堆_右子 用 i。
        若 左 小于 n 并且 做 堆_比较 用 堆、数据 的 第 左 项 的 第 0 项、数据 的 第 i 项 的 第 0 项 等于 1：
            返还 0。
        完。
        若 右 小于 n 并且 做 堆_比较 用 堆、数据 的 第 右 项 的 第 0 项、数据 的 第 i 项 的 第 0 项 等于 1：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

堆_气韵层级 接受 堆：
    n 为 做 优先队列_堆_大小 用 堆。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 层 为 一组：（空）。
    可变 层大小 为 1。
    可变 已计 为 0。
    只要 已计 小于 n：
        可变 实际 为 层大小。
        若 已计 加 层大小 大于 n：
            实际 为 n 减 已计。
        完。
        做 追加 用 层、实际。
        已计 为 已计 加 实际。
        层大小 为 层大小 乘 2。
    完。
    返还 层。
完。

堆_阴阳反转 接受 堆：
    可变 新模式 为 0。
    若 堆 的 第 1 项 等于 0：
        新模式 为 1。
    完。
    新堆 为 做 优先队列_堆_创建 用 新模式。
    数据 为 堆 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 优先队列_堆_插入 用 新堆、数据 的 第 i 项 的 第 0 项、数据 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 新堆。
完。

堆_批量插入 接受 堆、数据列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据列表：
        做 优先队列_堆_插入 用 堆、数据列表 的 第 i 项 的 第 0 项、数据列表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

堆_更新优先级 接受 堆、数据、新优先级：
    列表 为 堆 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 的 第 1 项 等于 数据：
            列表 的 第 i 项 的 第 0 项 为 新优先级。
            做 优先队列_堆_上浮 用 堆、i。
            做 优先队列_堆_下沉 用 堆、i。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

堆_转排序列表 接受 堆：
    可变 结果 为 一组：（空）。
    可变 临时堆 为 做 优先队列_堆_创建 用 堆 的 第 1 项。
    数据 为 堆 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 优先队列_堆_插入 用 临时堆、数据 的 第 i 项 的 第 0 项、数据 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    只要 做 优先队列_堆_大小 用 临时堆 大于 0：
        做 追加 用 结果、做 优先队列_堆_弹出 用 临时堆。
    完。
    返还 结果。
完。

本章 收束。
