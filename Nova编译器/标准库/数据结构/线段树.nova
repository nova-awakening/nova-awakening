《线段树》

引用《..·基础》。

线段树_创建 接受 数据：
    n 为 做 计数 用 数据。
    可变 树 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 4 乘 n：
        做 追加 用 树、0。
        i 为 i 加 1。
    完。
    st 为 一组：树、4 乘 n、n。
    做 线段树_构建 用 st、数据、1、0、n 减 1。
    返还 st。
完。

线段树_构建 接受 st、数据、节点、左、右：
    若 左 等于 右：
        st 的 第 0 项 的 第 节点 项 为 数据 的 第 左 项。
        返还 空。
    完。
    中 为 (左 加 右) 除以 2。
    做 线段树_构建 用 st、数据、节点 乘 2、左、中。
    做 线段树_构建 用 st、数据、节点 乘 2 加 1、中 加 1、右。
    树 为 st 的 第 0 项。
    树 的 第 节点 项 为 树 的 第 节点 乘 2 项 加 树 的 第 节点 乘 2 加 1 项。
完。

线段树_求和 接受 st、查左、查右：
    返还 做 线段树_求和递归 用 st、1、0、st 的 第 2 项 减 1、查左、查右。
完。

线段树_求和递归 接受 st、节点、左、右、查左、查右：
    若 查左 大于 右 或者 查右 小于 左：
        返还 0。
    完。
    若 查左 小于或等于 左 并且 右 小于或等于 查右：
        返还 st 的 第 0 项 的 第 节点 项。
    完。
    中 为 (左 加 右) 除以 2。
    返还 做 线段树_求和递归 用 st、节点 乘 2、左、中、查左、查右 加 做 线段树_求和递归 用 st、节点 乘 2 加 1、中 加 1、右、查左、查右。
完。

线段树_更新 接受 st、索引、值：
    做 线段树_更新递归 用 st、1、0、st 的 第 2 项 减 1、索引、值。
完。

线段树_更新递归 接受 st、节点、左、右、索引、值：
    若 左 等于 右：
        st 的 第 0 项 的 第 节点 项 为 值。
        返还 空。
    完。
    中 为 (左 加 右) 除以 2。
    若 索引 小于或等于 中：
        做 线段树_更新递归 用 st、节点 乘 2、左、中、索引、值。
    否则：
        做 线段树_更新递归 用 st、节点 乘 2 加 1、中 加 1、右、索引、值。
    完。
    树 为 st 的 第 0 项。
    树 的 第 节点 项 为 树 的 第 节点 乘 2 项 加 树 的 第 节点 乘 2 加 1 项。
完。

线段树_最小 接受 st、查左、查右：
    返还 做 线段树_最小递归 用 st、1、0、st 的 第 2 项 减 1、查左、查右。
完。

线段树_最小递归 接受 st、节点、左、右、查左、查右：
    若 查左 大于 右 或者 查右 小于 左：
        返还 999999999。
    完。
    若 查左 小于或等于 左 并且 右 小于或等于 查右：
        返还 st 的 第 0 项 的 第 节点 项。
    完。
    中 为 (左 加 右) 除以 2。
    左值 为 做 线段树_最小递归 用 st、节点 乘 2、左、中、查左、查右。
    右值 为 做 线段树_最小递归 用 st、节点 乘 2 加 1、中 加 1、右、查左、查右。
    若 左值 小于 右值：
        返还 左值。
    完。
    返还 右值。
完。

线段树_气韵聚合 接受 st、查左、查右、聚合函数、零元：
    返还 做 线段树_气韵递归 用 st、1、0、st 的 第 2 项 减 1、查左、查右、聚合函数、零元。
完。

线段树_气韵递归 接受 st、节点、左、右、查左、查右、聚合函数、零元：
    若 查左 大于 右 或者 查右 小于 左：
        返还 零元。
    完。
    若 查左 小于或等于 左 并且 右 小于或等于 查右：
        返还 st 的 第 0 项 的 第 节点 项。
    完。
    中 为 (左 加 右) 除以 2。
    左结果 为 做 线段树_气韵递归 用 st、节点 乘 2、左、中、查左、查右、聚合函数、零元。
    右结果 为 做 线段树_气韵递归 用 st、节点 乘 2 加 1、中 加 1、右、查左、查右、聚合函数、零元。
    返还 做 聚合函数 用 左结果、右结果。
完。

线段树_阴解构 接受 st：
    n 为 st 的 第 2 项。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、做 线段树_求和 用 st、i、i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

线段树_最大 接受 st、查左、查右：
    返还 做 线段树_最大递归 用 st、1、0、st 的 第 2 项 减 1、查左、查右。
完。

线段树_最大递归 接受 st、节点、左、右、查左、查右：
    若 查左 大于 右 或者 查右 小于 左：
        返还 负 999999999。
    完。
    若 查左 小于或等于 左 并且 右 小于或等于 查右：
        返还 st 的 第 0 项 的 第 节点 项。
    完。
    中 为 (左 加 右) 除以 2。
    左值 为 做 线段树_最大递归 用 st、节点 乘 2、左、中、查左、查右。
    右值 为 做 线段树_最大递归 用 st、节点 乘 2 加 1、中 加 1、右、查左、查右。
    若 左值 大于 右值：
        返还 左值。
    完。
    返还 右值。
完。

线段树_阴阳验证 接受 st：
    返还 做 线段树_验证递归 用 st、1、0、st 的 第 2 项 减 1。
完。

线段树_验证递归 接受 st、节点、左、右：
    若 左 等于 右：
        返还 1。
    完。
    中 为 (左 加 右) 除以 2。
    树 为 st 的 第 0 项。
    期望 为 树 的 第 节点 乘 2 项 加 树 的 第 节点 乘 2 加 1 项。
    若 树 的 第 节点 项 不等于 期望：
        返还 0。
    完。
    若 做 线段树_验证递归 用 st、节点 乘 2、左、中 等于 0：
        返还 0。
    完。
    返还 做 线段树_验证递归 用 st、节点 乘 2 加 1、中 加 1、右。
完。

懒线段树_创建 接受 大小：
    容量 为 大小 乘 4。
    可变 树 为 一组：（空）。
    可变 懒 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 树、0。
        做 追加 用 懒、0。
        i 为 i 加 1。
    完。
    返还 一组：树、懒、大小。
完。

懒线段树_下推 接受 st、节点、左、右：
    若 st 的 第 1 项 的 第 节点 项 不等于 0：
        中 为 (左 加 右) 除以 2。
        左子 为 节点 乘 2。
        右子 为 节点 乘 2 加 1。
        左大小 为 中 减 左 加 1。
        右大小 为 右 减 中。
        st 的 第 1 项 的 第 左子 项 为 st 的 第 1 项 的 第 左子 项 加 st 的 第 1 项 的 第 节点 项。
        st 的 第 1 项 的 第 右子 项 为 st 的 第 1 项 的 第 右子 项 加 st 的 第 1 项 的 第 节点 项。
        st 的 第 0 项 的 第 左子 项 为 st 的 第 0 项 的 第 左子 项 加 st 的 第 1 项 的 第 节点 项 乘 左大小。
        st 的 第 0 项 的 第 右子 项 为 st 的 第 0 项 的 第 右子 项 加 st 的 第 1 项 的 第 节点 项 乘 右大小。
        st 的 第 1 项 的 第 节点 项 为 0。
    完。
完。

懒线段树_区间更新 接受 st、节点、左、右、ql、qr、增量：
    若 ql 大于 右 或者 qr 小于 左：
        返还。
    完。
    若 ql 小于或等于 左 并且 右 小于或等于 qr：
        st 的 第 0 项 的 第 节点 项 为 st 的 第 0 项 的 第 节点 项 加 增量 乘 (右 减 左 加 1)。
        st 的 第 1 项 的 第 节点 项 为 st 的 第 1 项 的 第 节点 项 加 增量。
        返还。
    完。
    做 懒线段树_下推 用 st、节点、左、右。
    中 为 (左 加 右) 除以 2。
    做 懒线段树_区间更新 用 st、节点 乘 2、左、中、ql、qr、增量。
    做 懒线段树_区间更新 用 st、节点 乘 2 加 1、中 加 1、右、ql、qr、增量。
    st 的 第 0 项 的 第 节点 项 为 st 的 第 0 项 的 第 节点 乘 2 项 加 st 的 第 0 项 的 第 节点 乘 2 加 1 项。
完。

懒线段树_区间查询 接受 st、节点、左、右、ql、qr：
    若 ql 大于 右 或者 qr 小于 左：
        返还 0。
    完。
    若 ql 小于或等于 左 并且 右 小于或等于 qr：
        返还 st 的 第 0 项 的 第 节点 项。
    完。
    做 懒线段树_下推 用 st、节点、左、右。
    中 为 (左 加 右) 除以 2。
    返还 做 懒线段树_区间查询 用 st、节点 乘 2、左、中、ql、qr 加 做 懒线段树_区间查询 用 st、节点 乘 2 加 1、中 加 1、右、ql、qr。
完。

最值线段树_创建 接受 数据：
    n 为 做 计数 用 数据。
    容量 为 n 乘 4。
    可变 最大树 为 一组：（空）。
    可变 最小树 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 最大树、负 999999。
        做 追加 用 最小树、999999。
        i 为 i 加 1。
    完。
    st 为 一组：最大树、最小树、n。
    做 最值线段树_构建 用 st、数据、1、0、n 减 1。
    返还 st。
完。

最值线段树_构建 接受 st、数据、节点、左、右：
    若 左 等于 右：
        st 的 第 0 项 的 第 节点 项 为 数据 的 第 左 项。
        st 的 第 1 项 的 第 节点 项 为 数据 的 第 左 项。
        返还。
    完。
    中 为 (左 加 右) 除以 2。
    做 最值线段树_构建 用 st、数据、节点 乘 2、左、中。
    做 最值线段树_构建 用 st、数据、节点 乘 2 加 1、中 加 1、右。
    若 st 的 第 0 项 的 第 节点 乘 2 项 大于 st 的 第 0 项 的 第 节点 乘 2 加 1 项：
        st 的 第 0 项 的 第 节点 项 为 st 的 第 0 项 的 第 节点 乘 2 项。
    否则：
        st 的 第 0 项 的 第 节点 项 为 st 的 第 0 项 的 第 节点 乘 2 加 1 项。
    完。
    若 st 的 第 1 项 的 第 节点 乘 2 项 小于 st 的 第 1 项 的 第 节点 乘 2 加 1 项：
        st 的 第 1 项 的 第 节点 项 为 st 的 第 1 项 的 第 节点 乘 2 项。
    否则：
        st 的 第 1 项 的 第 节点 项 为 st 的 第 1 项 的 第 节点 乘 2 加 1 项。
    完。
完。

最值线段树_查最大 接受 st、节点、左、右、ql、qr：
    若 ql 大于 右 或者 qr 小于 左：
        返还 负 999999。
    完。
    若 ql 小于或等于 左 并且 右 小于或等于 qr：
        返还 st 的 第 0 项 的 第 节点 项。
    完。
    中 为 (左 加 右) 除以 2。
    左值 为 做 最值线段树_查最大 用 st、节点 乘 2、左、中、ql、qr。
    右值 为 做 最值线段树_查最大 用 st、节点 乘 2 加 1、中 加 1、右、ql、qr。
    若 左值 大于 右值：
        返还 左值。
    完。
    返还 右值。
完。

最值线段树_查最小 接受 st、节点、左、右、ql、qr：
    若 ql 大于 右 或者 qr 小于 左：
        返还 999999。
    完。
    若 ql 小于或等于 左 并且 右 小于或等于 qr：
        返还 st 的 第 1 项 的 第 节点 项。
    完。
    中 为 (左 加 右) 除以 2。
    左值 为 做 最值线段树_查最小 用 st、节点 乘 2、左、中、ql、qr。
    右值 为 做 最值线段树_查最小 用 st、节点 乘 2 加 1、中 加 1、右、ql、qr。
    若 左值 小于 右值：
        返还 左值。
    完。
    返还 右值。
完。

_最大公约数 接受 a、b：
    若 b 等于 0：
        返还 a。
    完。
    返还 做 _最大公约数 用 b、a 取余于 b。
完。

GCD线段树_创建 接受 数据：
    n 为 做 计数 用 数据。
    容量 为 n 乘 4。
    可变 树 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 树、0。
        i 为 i 加 1。
    完。
    st 为 一组：树、n。
    做 GCD线段树_构建 用 st、数据、1、0、n 减 1。
    返还 st。
完。

GCD线段树_构建 接受 st、数据、节点、左、右：
    若 左 等于 右：
        st 的 第 0 项 的 第 节点 项 为 数据 的 第 左 项。
        返还。
    完。
    中 为 (左 加 右) 除以 2。
    做 GCD线段树_构建 用 st、数据、节点 乘 2、左、中。
    做 GCD线段树_构建 用 st、数据、节点 乘 2 加 1、中 加 1、右。
    st 的 第 0 项 的 第 节点 项 为 做 _最大公约数 用 st 的 第 0 项 的 第 节点 乘 2 项、st 的 第 0 项 的 第 节点 乘 2 加 1 项。
完。

GCD线段树_查询 接受 st、节点、左、右、ql、qr：
    若 ql 大于 右 或者 qr 小于 左：
        返还 0。
    完。
    若 ql 小于或等于 左 并且 右 小于或等于 qr：
        返还 st 的 第 0 项 的 第 节点 项。
    完。
    中 为 (左 加 右) 除以 2。
    左值 为 做 GCD线段树_查询 用 st、节点 乘 2、左、中、ql、qr。
    右值 为 做 GCD线段树_查询 用 st、节点 乘 2 加 1、中 加 1、右、ql、qr。
    若 左值 等于 0：
        返还 右值。
    完。
    若 右值 等于 0：
        返还 左值。
    完。
    返还 做 _最大公约数 用 左值、右值。
完。

可变 _线段树_最大规模 为 0。

可变 _线段树_懒传播数 为 0。

线段树_记录操作 接受 规模：
    若 规模 大于 _线段树_最大规模：
        _线段树_最大规模 为 规模。
    完。
完。

线段树_记录懒传播 接受 零参：
    _线段树_懒传播数 为 _线段树_懒传播数 加 1。
完。

本章 收束。
