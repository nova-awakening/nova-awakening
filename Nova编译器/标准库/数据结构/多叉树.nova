《多叉树》

引用《..·基础》。

BTREE_阶 为 4。

可变 btree_木计 为 0。

可变 btree_火计 为 0。

可变 btree_土计 为 0。

可变 btree_金计 为 0。

可变 btree_水计 为 0。

btree_创建节点 接受 叶子：
    btree_木计 为 btree_木计 加 1。
    返还 一组：一组：（空）、一组：（空）、一组：（空）、叶子、0。
完。

btree_是叶子 接受 节点：
    返还 节点 的 第 3 项。
完。

btree_键列表 接受 节点：
    返还 节点 的 第 0 项。
完。

btree_值列表 接受 节点：
    返还 节点 的 第 1 项。
完。

btree_子节点 接受 节点：
    返还 节点 的 第 2 项。
完。

btree_键数 接受 节点：
    返还 做 计数 用 节点 的 第 0 项。
完。

btree_节点势 接受 节点：
    返还 节点 的 第 4 项。
完。

btree_更新势 接受 节点：
    满度 为 做 btree_键数 用 节点 乘 10 除以 (BTREE_阶 减 1)。
    节点 的 第 4 项 为 满度。
完。

多叉树_btree_创建 接受 零参：
    返还 一组：做 btree_创建节点 用 1。
完。

btree_根 接受 树：
    返还 树 的 第 0 项。
完。

btree_总大小 接受 树：
    返还 树 的 第 1 项。
完。

btree_总高 接受 树：
    返还 树 的 第 2 项。
完。

btree_查找位置 接受 节点、键：
    键表 为 做 btree_键列表 用 节点。
    可变 低 为 0。
    可变 高 为 做 计数 用 键表 减 1。
    只要 低 小于或等于 高：
        中 为 (低 加 高) 除以 2。
        若 键表 的 第 中 项 等于 键：
            返还 中。
        完。
        若 键表 的 第 中 项 小于 键：
            低 为 中 加 1。
        否则：
            高 为 中 减 1。
        完。
    完。
    返还 低。
完。

多叉树_btree_查找 接受 树、键：
    btree_水计 为 btree_水计 加 1。
    可变 节点 为 做 btree_根 用 树。
    只要 节点 不等于 空：
        位置 为 做 btree_查找位置 用 节点、键。
        键表 为 做 btree_键列表 用 节点。
        若 位置 小于 做 计数 用 键表 并且 键表 的 第 位置 项 等于 键：
            值表 为 做 btree_值列表 用 节点。
            返还 值表 的 第 位置 项。
        完。
        若 做 btree_是叶子 用 节点 等于 1：
            返还 空。
        完。
        子表 为 做 btree_子节点 用 节点。
        节点 为 子表 的 第 位置 项。
    完。
    返还 空。
完。

btree_分裂 接受 父、索引：
    btree_火计 为 btree_火计 加 1。
    父子表 为 做 btree_子节点 用 父。
    子 为 父子表 的 第 索引 项。
    中间 为 (BTREE_阶 减 1) 除以 2。
    新节点 为 做 btree_创建节点 用 做 btree_是叶子 用 子。
    子键表 为 做 btree_键列表 用 子。
    子值表 为 做 btree_值列表 用 子。
    新键表 为 做 btree_键列表 用 新节点。
    新值表 为 做 btree_值列表 用 新节点。
    可变 i 为 中间 加 1。
    只要 i 小于 做 btree_键数 用 子：
        做 追加 用 新键表、子键表 的 第 i 项。
        做 追加 用 新值表、子值表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 做 btree_是叶子 用 子 等于 0：
        子子表 为 做 btree_子节点 用 子。
        新子表 为 做 btree_子节点 用 新节点。
        i 为 中间 加 1。
        只要 i 小于或等于 做 btree_键数 用 子：
            做 追加 用 新子表、子子表 的 第 i 项。
            i 为 i 加 1。
        完。
    完。
    提升键 为 子键表 的 第 中间 项。
    提升值 为 子值表 的 第 中间 项。
    做 插入 用 做 btree_键列表 用 父、索引、提升键。
    做 插入 用 做 btree_值列表 用 父、索引、提升值。
    做 插入 用 父子表、索引 加 1、新节点。
    可变 删 为 做 btree_键数 用 子 减 中间。
    只要 删 大于 0：
        kl 为 做 btree_键列表 用 子。
        做 删除 用 kl、做 计数 用 kl 减 1。
        vl 为 做 btree_值列表 用 子。
        做 删除 用 vl、做 计数 用 vl 减 1。
        删 为 删 减 1。
    完。
    若 做 btree_是叶子 用 子 等于 0：
        cl 为 做 btree_子节点 用 子。
        删 为 做 计数 用 cl 减 中间 减 1。
        只要 删 大于 0：
            做 删除 用 cl、做 计数 用 cl 减 1。
            删 为 删 减 1。
        完。
    完。
完。

btree_插入非满 接受 节点、键、值：
    可变 位置 为 做 btree_查找位置 用 节点、键。
    键表 为 做 btree_键列表 用 节点。
    若 位置 小于 做 计数 用 键表 并且 键表 的 第 位置 项 等于 键：
        值表 为 做 btree_值列表 用 节点。
        值表 的 第 位置 项 为 值。
        返还 0。
    完。
    若 做 btree_是叶子 用 节点 等于 1：
        做 插入 用 做 btree_键列表 用 节点、位置、键。
        做 插入 用 做 btree_值列表 用 节点、位置、值。
        返还 1。
    否则：
        子表 为 做 btree_子节点 用 节点。
        子节点 为 子表 的 第 位置 项。
        若 做 btree_键数 用 子节点 大于或等于 BTREE_阶 减 1：
            做 btree_分裂 用 节点、位置。
            键表2 为 做 btree_键列表 用 节点。
            若 键 大于 键表2 的 第 位置 项：
                位置 为 位置 加 1。
            完。
        完。
        子表2 为 做 btree_子节点 用 节点。
        返还 做 btree_插入非满 用 子表2 的 第 位置 项、键、值。
    完。
完。

多叉树_btree_插入 接受 树、键、值：
    btree_土计 为 btree_土计 加 1。
    根 为 做 btree_根 用 树。
    可变 新增 为 0。
    若 做 btree_键数 用 根 大于或等于 BTREE_阶 减 1：
        新根 为 做 btree_创建节点 用 0。
        做 追加 用 做 btree_子节点 用 新根、根。
        做 btree_分裂 用 新根、0。
        新增 为 做 btree_插入非满 用 新根、键、值。
        树 的 第 0 项 为 新根。
        树 的 第 2 项 为 树 的 第 2 项 加 1。
    否则：
        新增 为 做 btree_插入非满 用 根、键、值。
    完。
    树 的 第 1 项 为 树 的 第 1 项 加 新增。
完。

btree_范围 接受 树、最小、最大：
    可变 结果 为 一组：（空）。
    做 btree_范围收集 用 做 btree_根 用 树、最小、最大、结果。
    返还 结果。
完。

btree_范围收集 接受 节点、最小、最大、结果：
    若 节点 等于 空：
        返还 空。
    完。
    键表 为 做 btree_键列表 用 节点。
    值表 为 做 btree_值列表 用 节点。
    子表 为 做 btree_子节点 用 节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键表：
        若 做 btree_是叶子 用 节点 等于 0 并且 键表 的 第 i 项 大于或等于 最小：
            做 btree_范围收集 用 子表 的 第 i 项、最小、最大、结果。
        完。
        若 键表 的 第 i 项 大于或等于 最小 并且 键表 的 第 i 项 小于或等于 最大：
            做 追加 用 结果、一组：键表 的 第 i 项、值表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 做 btree_是叶子 用 节点 等于 0：
        做 btree_范围收集 用 子表 的 第 做 计数 用 键表 项、最小、最大、结果。
    完。
完。

btree_中序 接受 树：
    可变 结果 为 一组：（空）。
    做 btree_中序递归 用 做 btree_根 用 树、结果。
    返还 结果。
完。

btree_中序递归 接受 节点、结果：
    若 节点 等于 空：
        返还 空。
    完。
    键表 为 做 btree_键列表 用 节点。
    值表 为 做 btree_值列表 用 节点。
    子表 为 做 btree_子节点 用 节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键表：
        若 做 btree_是叶子 用 节点 等于 0：
            做 btree_中序递归 用 子表 的 第 i 项、结果。
        完。
        做 追加 用 结果、一组：键表 的 第 i 项、值表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 做 btree_是叶子 用 节点 等于 0 并且 做 计数 用 子表 大于 做 计数 用 键表：
        做 btree_中序递归 用 子表 的 第 做 计数 用 键表 项、结果。
    完。
完。

多叉树_btree_大小 接受 树：
    返还 做 btree_大小递归 用 做 btree_根 用 树。
完。

btree_大小递归 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    可变 总 为 做 btree_键数 用 节点。
    若 做 btree_是叶子 用 节点 等于 0：
        子表 为 做 btree_子节点 用 节点。
        可变 i 为 0。
        只要 i 小于或等于 做 btree_键数 用 节点：
            若 i 小于 做 计数 用 子表：
                总 为 总 加 做 btree_大小递归 用 子表 的 第 i 项。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 总。
完。

btree_高度 接受 树：
    可变 节点 为 做 btree_根 用 树。
    可变 h 为 0。
    只要 节点 不等于 空 并且 做 btree_是叶子 用 节点 等于 0：
        子表 为 做 btree_子节点 用 节点。
        节点 为 子表 的 第 0 项。
        h 为 h 加 1。
    完。
    返还 h。
完。

btree_五行报告 接受 零参：
    btree_金计 为 btree_金计 加 1。
    返还 一组：btree_木计、btree_火计、btree_土计、btree_金计、btree_水计。
完。

btree_五行平衡度 接受 零参：
    总 为 btree_木计 加 btree_火计 加 btree_土计 加 btree_金计 加 btree_水计。
    若 总 等于 0：
        返还 0。
    完。
    平均 为 总 除以 5。
    可变 偏差 为 0。
    d1 为 btree_木计 减 平均。
    d2 为 btree_火计 减 平均。
    d3 为 btree_土计 减 平均。
    d4 为 btree_金计 减 平均。
    d5 为 btree_水计 减 平均。
    若 d1 小于 0：
        偏差 为 偏差 减 d1。
    否则：
        偏差 为 偏差 加 d1。
    完。
    若 d2 小于 0：
        偏差 为 偏差 减 d2。
    否则：
        偏差 为 偏差 加 d2。
    完。
    若 d3 小于 0：
        偏差 为 偏差 减 d3。
    否则：
        偏差 为 偏差 加 d3。
    完。
    若 d4 小于 0：
        偏差 为 偏差 减 d4。
    否则：
        偏差 为 偏差 加 d4。
    完。
    若 d5 小于 0：
        偏差 为 偏差 减 d5。
    否则：
        偏差 为 偏差 加 d5。
    完。
    若 总 等于 0：
        返还 0。
    完。
    返还 偏差 乘 100 除以 总。
完。

btree_阴阳验证 接受 树：
    btree_金计 为 btree_金计 加 1。
    根 为 做 btree_根 用 树。
    若 根 等于 空：
        返还 1。
    完。
    返还 做 btree_验证节点 用 根、1。
完。

btree_验证节点 接受 节点、_是根：
    键表 为 做 btree_键列表 用 节点。
    n 为 做 计数 用 键表。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        若 键表 的 第 i 项 大于或等于 键表 的 第 i 加 1 项：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    若 做 btree_是叶子 用 节点 等于 0：
        子表 为 做 btree_子节点 用 节点。
        若 做 计数 用 子表 不等于 n 加 1：
            返还 0。
        完。
        i 为 0。
        只要 i 小于或等于 n：
            若 i 小于 做 计数 用 子表：
                若 做 btree_验证节点 用 子表 的 第 i 项、0 等于 0：
                    返还 0。
                完。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 1。
完。

btree_势能图 接受 树：
    可变 层势 为 一组：（空）。
    可变 队列 为 一组：做 btree_根 用 树。
    只要 做 计数 用 队列 大于 0：
        可变 下一层 为 一组：（空）。
        可变 层总势 为 0。
        可变 i 为 0。
        只要 i 小于 做 计数 用 队列：
            节点 为 队列 的 第 i 项。
            做 btree_更新势 用 节点。
            层总势 为 层总势 加 做 btree_节点势 用 节点。
            若 做 btree_是叶子 用 节点 等于 0：
                子表 为 做 btree_子节点 用 节点。
                可变 j 为 0。
                只要 j 小于 做 计数 用 子表：
                    做 追加 用 下一层、子表 的 第 j 项。
                    j 为 j 加 1。
                完。
            完。
            i 为 i 加 1。
        完。
        做 追加 用 层势、层总势 除以 做 计数 用 队列。
        队列 为 下一层。
    完。
    返还 层势。
完。

btree_棵转串 接受 树：
    返还 做 btree_中序 用 树。
完。

多叉树_btree_删除 接受 树、键：
    btree_水计 为 btree_水计 加 1。
    根 为 做 btree_根 用 树。
    若 根 等于 空：
        返还 0。
    完。
    结果 为 做 btree_删除递归 用 根、键。
    若 结果 等于 1：
        树 的 第 1 项 为 树 的 第 1 项 减 1。
    完。
    若 做 btree_键数 用 根 等于 0 并且 做 btree_是叶子 用 根 等于 0：
        子表 为 做 btree_子节点 用 根。
        若 做 计数 用 子表 大于 0：
            树 的 第 0 项 为 子表 的 第 0 项。
            树 的 第 2 项 为 树 的 第 2 项 减 1。
        完。
    完。
    返还 结果。
完。

btree_删除递归 接受 节点、键：
    位置 为 做 btree_查找位置 用 节点、键。
    键表 为 做 btree_键列表 用 节点。
    若 位置 小于 做 计数 用 键表 并且 键表 的 第 位置 项 等于 键：
        若 做 btree_是叶子 用 节点 等于 1：
            做 删除 用 键表、位置。
            值表 为 做 btree_值列表 用 节点。
            做 删除 用 值表、位置。
            返还 1。
        完。
        子表 为 做 btree_子节点 用 节点。
        前驱节点 为 做 btree_最大节点 用 子表 的 第 位置 项。
        前键表 为 做 btree_键列表 用 前驱节点。
        前值表 为 做 btree_值列表 用 前驱节点。
        前n 为 做 计数 用 前键表。
        键表 的 第 位置 项 为 前键表 的 第 前n 减 1 项。
        值表 为 做 btree_值列表 用 节点。
        值表 的 第 位置 项 为 前值表 的 第 前n 减 1 项。
        返还 做 btree_删除递归 用 子表 的 第 位置 项、前键表 的 第 前n 减 1 项。
    完。
    若 做 btree_是叶子 用 节点 等于 1：
        返还 0。
    完。
    子表 为 做 btree_子节点 用 节点。
    若 位置 小于 做 计数 用 子表：
        返还 做 btree_删除递归 用 子表 的 第 位置 项、键。
    完。
    返还 0。
完。

btree_最大节点 接受 节点：
    可变 当前 为 节点。
    只要 做 btree_是叶子 用 当前 等于 0：
        子表 为 做 btree_子节点 用 当前。
        当前 为 子表 的 第 做 计数 用 子表 减 1 项。
    完。
    返还 当前。
完。

btree_五行重置 接受 零参：
    btree_木计 为 0。
    btree_火计 为 0。
    btree_土计 为 0。
    btree_金计 为 0。
    btree_水计 为 0。
完。

btree_最大键 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    若 做 计数 用 节点 的 第 2 项 等于 0：
        若 做 计数 用 节点 的 第 0 项 等于 0：
            返还 0。
        完。
        返还 节点 的 第 0 项 的 第 做 计数 用 节点 的 第 0 项 减 1 项。
    完。
    返还 做 btree_最大键 用 节点 的 第 2 项 的 第 做 计数 用 节点 的 第 2 项 减 1 项。
完。

btree_最小键 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    若 做 计数 用 节点 的 第 2 项 等于 0：
        若 做 计数 用 节点 的 第 0 项 等于 0：
            返还 0。
        完。
        返还 节点 的 第 0 项 的 第 0 项。
    完。
    返还 做 btree_最小键 用 节点 的 第 2 项 的 第 0 项。
完。

btree_键范围 接受 节点：
    返还 一组：做 btree_最小键 用 节点、做 btree_最大键 用 节点。
完。

btree_节点利用率 接受 节点、最大键数：
    若 最大键数 等于 0：
        返还 0。
    完。
    返还 做 计数 用 节点 的 第 0 项 乘 100 除以 最大键数。
完。

btree_中序遍历 接受 节点、结果：
    若 节点 等于 空：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点 的 第 0 项：
        若 做 计数 用 节点 的 第 2 项 大于 i：
            做 btree_中序遍历 用 节点 的 第 2 项 的 第 i 项、结果。
        完。
        做 追加 用 结果、节点 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    若 做 计数 用 节点 的 第 2 项 大于 做 计数 用 节点 的 第 0 项：
        做 btree_中序遍历 用 节点 的 第 2 项 的 第 做 计数 用 节点 的 第 0 项 项、结果。
    完。
完。

可变 _btree_插入数 为 0。

可变 _btree_查找数 为 0。

可变 _btree_分裂数 为 0。

btree_记录插入 接受 零参：
    _btree_插入数 为 _btree_插入数 加 1。
完。

btree_记录查找 接受 零参：
    _btree_查找数 为 _btree_查找数 加 1。
完。

btree_记录分裂 接受 零参：
    _btree_分裂数 为 _btree_分裂数 加 1。
完。

btree_读写比 接受 零参：
    若 _btree_插入数 等于 0：
        返还 「只读」。
    完。
    比 为 _btree_查找数 除以 _btree_插入数。
    若 比 大于 10：
        返还 「读密集型(B树适合)」。
    完。
    若 比 大于 1：
        返还 「读多写少」。
    完。
    返还 「写密集型(考虑LSM树)」。
完。

btree_推荐阶数 接受 数据量：
    若 数据量 大于 1000000：
        返还 256。
    完。
    若 数据量 大于 10000：
        返还 128。
    完。
    若 数据量 大于 1000：
        返还 64。
    完。
    返还 32。
完。

btree_空间估算 接受 数据量、阶数：
    若 阶数 等于 0：
        返还 0。
    完。
    节点数 为 数据量 除以 (阶数 除以 2) 加 1。
    返还 节点数 乘 阶数 乘 16。
完。

本章 收束。
