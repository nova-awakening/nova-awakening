《并查集》

引用《..·基础》。

uf_创建 接受 n：
    可变 父 为 一组：（空）。
    可变 秩 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 父、i。
        做 追加 用 秩、0。
        i 为 i 加 1。
    完。
    返还 一组：父、秩、n、n、一组：1、0。
完。

uf_查找 接受 集、x：
    度量 为 集 的 第 4 项。
    度量 的 第 3 项 为 度量 的 第 3 项 加 1。
    父数组 为 集 的 第 0 项。
    可变 当前 为 x。
    只要 父数组 的 第 当前 项 不等于 当前：
        父数组 的 第 当前 项 为 父数组 的 第 父数组 的 第 当前 项 项。
        当前 为 父数组 的 第 当前 项。
    完。
    返还 当前。
完。

uf_合并 接受 集、x、y：
    度量 为 集 的 第 4 项。
    度量 的 第 2 项 为 度量 的 第 2 项 加 1。
    根x 为 做 uf_查找 用 集、x。
    根y 为 做 uf_查找 用 集、y。
    若 根x 等于 根y：
        返还 0。
    完。
    父数组 为 集 的 第 0 项。
    秩数组 为 集 的 第 1 项。
    若 秩数组 的 第 根x 项 小于 秩数组 的 第 根y 项：
        父数组 的 第 根x 项 为 根y。
    否则若 秩数组 的 第 根x 项 大于 秩数组 的 第 根y 项：
        父数组 的 第 根y 项 为 根x。
    否则：
        父数组 的 第 根y 项 为 根x。
        秩数组 的 第 根x 项 为 秩数组 的 第 根x 项 加 1。
    完。
    集 的 第 3 项 为 集 的 第 3 项 减 1。
    返还 1。
完。

uf_连通 接受 集、x、y：
    返还 做 uf_查找 用 集、x 等于 做 uf_查找 用 集、y。
完。

uf_分量数 接受 集：
    返还 集 的 第 3 项。
完。

uf_分量大小 接受 集、x：
    根 为 做 uf_查找 用 集、x。
    n 为 集 的 第 2 项。
    可变 大小 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 uf_查找 用 集、i 等于 根：
            大小 为 大小 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 大小。
完。

uf_所有根 接受 集：
    n 为 集 的 第 2 项。
    可变 根列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 uf_查找 用 集、i 等于 i：
            做 追加 用 根列表、i。
        完。
        i 为 i 加 1。
    完。
    返还 根列表。
完。

uf_五行报告 接受 集：
    返还 集 的 第 4 项。
完。

uf_气韵路径 接受 集、x：
    父数组 为 集 的 第 0 项。
    可变 长度 为 0。
    可变 当前 为 x。
    只要 父数组 的 第 当前 项 不等于 当前：
        当前 为 父数组 的 第 当前 项。
        长度 为 长度 加 1。
    完。
    返还 长度。
完。

uf_平均气韵 接受 集：
    n 为 集 的 第 2 项。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总 为 总 加 做 uf_气韵路径 用 集、i。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 总 乘 100 除以 n。
完。

uf_阴阳验证 接受 集：
    度量 为 集 的 第 4 项。
    度量 的 第 4 项 为 度量 的 第 4 项 加 1。
    n 为 集 的 第 2 项。
    可变 实际分量 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        父数组 为 集 的 第 0 项。
        若 父数组 的 第 i 项 等于 i：
            实际分量 为 实际分量 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 实际分量 等于 集 的 第 3 项。
完。

uf_组转批 接受 集：
    n 为 集 的 第 2 项。
    可变 ri 为 0。
    根列表 为 做 uf_所有根 用 集。
    可变 分量 为 一组：（空）。
    只要 ri 小于 做 计数 用 根列表：
        根 为 根列表 的 第 ri 项。
        可变 成员 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 n：
            若 做 uf_查找 用 集、i 等于 根：
                做 追加 用 成员、i。
            完。
            i 为 i 加 1。
        完。
        做 追加 用 分量、成员。
        ri 为 ri 加 1。
    完。
    返还 分量。
完。

加权UF_创建 接受 n：
    可变 父 为 一组：（空）。
    可变 秩 为 一组：（空）。
    可变 权 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 父、i。
        做 追加 用 秩、0。
        做 追加 用 权、0。
        i 为 i 加 1。
    完。
    返还 一组：父、秩、权、n。
完。

加权UF_查找 接受 集、x：
    若 集 的 第 0 项 的 第 x 项 等于 x：
        返还 一组：x、0。
    完。
    结果 为 做 加权UF_查找 用 集、集 的 第 0 项 的 第 x 项。
    集 的 第 0 项 的 第 x 项 为 结果 的 第 0 项。
    集 的 第 2 项 的 第 x 项 为 集 的 第 2 项 的 第 x 项 加 结果 的 第 1 项。
    返还 一组：结果 的 第 0 项、集 的 第 2 项 的 第 x 项。
完。

加权UF_合并 接受 集、x、y、w：
    rx 为 做 加权UF_查找 用 集、x。
    ry 为 做 加权UF_查找 用 集、y。
    若 rx 的 第 0 项 等于 ry 的 第 0 项：
        返还 假。
    完。
    差 为 w 加 rx 的 第 1 项 减 ry 的 第 1 项。
    若 集 的 第 1 项 的 第 rx 的 第 0 项 项 小于 集 的 第 1 项 的 第 ry 的 第 0 项 项：
        集 的 第 0 项 的 第 rx 的 第 0 项 项 为 ry 的 第 0 项。
        集 的 第 2 项 的 第 rx 的 第 0 项 项 为 0 减 差。
    否则若 集 的 第 1 项 的 第 rx 的 第 0 项 项 大于 集 的 第 1 项 的 第 ry 的 第 0 项 项：
        集 的 第 0 项 的 第 ry 的 第 0 项 项 为 rx 的 第 0 项。
        集 的 第 2 项 的 第 ry 的 第 0 项 项 为 差。
    否则：
        集 的 第 0 项 的 第 ry 的 第 0 项 项 为 rx 的 第 0 项。
        集 的 第 2 项 的 第 ry 的 第 0 项 项 为 差。
        集 的 第 1 项 的 第 rx 的 第 0 项 项 为 集 的 第 1 项 的 第 rx 的 第 0 项 项 加 1。
    完。
    集 的 第 3 项 为 集 的 第 3 项 减 1。
    返还 真。
完。

克鲁斯卡尔最小生成树 接受 边列表、节点数：
    可变 总权 为 0。
    可变 n 为 做 计数 用 边列表。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 0。
        只要 j 小于 n 减 i 减 1：
            若 边列表 的 第 j 项 的 第 2 项 大于 边列表 的 第 j 加 1 项 的 第 2 项：
                临 为 边列表 的 第 j 项。
                边列表 的 第 j 项 为 边列表 的 第 j 加 1 项。
                边列表 的 第 j 加 1 项 为 临。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    集 为 做 uf_创建 用 节点数。
    可变 MST 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        u 为 边列表 的 第 i 项 的 第 0 项。
        v 为 边列表 的 第 i 项 的 第 1 项。
        w 为 边列表 的 第 i 项 的 第 2 项。
        若 做 uf_查找 用 集、u 不等于 做 uf_查找 用 集、v：
            做 uf_合并 用 集、u、v。
            做 追加 用 MST、边列表 的 第 i 项。
            总权 为 总权 加 w。
        完。
        i 为 i 加 1。
    完。
    返还 一组：MST、总权。
完。

UF_最大分量 接受 集、n：
    可变 计数表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 计数表、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        根 为 做 uf_查找 用 集、i。
        计数表 的 第 根 项 为 计数表 的 第 根 项 加 1。
        i 为 i 加 1。
    完。
    可变 最大 为 0。
    i 为 0。
    只要 i 小于 n：
        若 计数表 的 第 i 项 大于 最大：
            最大 为 计数表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

UF_孤立节点数 接受 集、n：
    可变 孤立 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 uf_查找 用 集、i 等于 i：
            可变 只有自己 为 真。
            可变 j 为 0。
            只要 j 小于 n：
                若 j 不等于 i 并且 做 uf_查找 用 集、j 等于 i：
                    只有自己 为 假。
                完。
                j 为 j 加 1。
            完。
            若 只有自己：
                孤立 为 孤立 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 孤立。
完。

UF_是否连通 接受 集、n：
    根 为 做 uf_查找 用 集、0。
    可变 i 为 1。
    只要 i 小于 n：
        若 做 uf_查找 用 集、i 不等于 根：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

可变 _UF_操作历史 为 一组：（空）。

UF_记录合并 接受 u、v：
    做 追加 用 _UF_操作历史、一组：1、u、v。
完。

UF_记录查询 接受 u、v、结果：
    做 追加 用 _UF_操作历史、一组：0、u、v、结果。
完。

UF_操作数 接受 零参：
    返还 做 计数 用 _UF_操作历史。
完。

可变 _UF_合并次数 为 0。

可变 _UF_查找次数 为 0。

可变 _UF_路径压缩次数 为 0。

UF_记录合并操作 接受 零参：
    _UF_合并次数 为 _UF_合并次数 加 1。
完。

UF_记录查找操作 接受 零参：
    _UF_查找次数 为 _UF_查找次数 加 1。
完。

UF_记录压缩 接受 零参：
    _UF_路径压缩次数 为 _UF_路径压缩次数 加 1。
完。

UF_效率评估 接受 零参：
    若 _UF_查找次数 大于 0 并且 _UF_路径压缩次数 乘 100 除以 _UF_查找次数 大于 80：
        返还 「路径压缩高效」。
    完。
    若 _UF_合并次数 大于 100000：
        返还 「大规模合并」。
    完。
    返还 「并查集正常」。
完。

UF_重置 接受 零参：
    _UF_合并次数 为 0。
    _UF_查找次数 为 0。
    _UF_路径压缩次数 为 0。
    _UF_操作历史 为 一组：（空）。
完。

本章 收束。
