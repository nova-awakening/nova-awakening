《键值存储》

引用《..·基础》。

键值存储_kv_创建 接受 最大条目：
    返还 一组：做 字典、做 字典、做 字典、最大条目、0、一组：（空）。
完。

kv_提升键 接受 存储、键：
    可变 i 为 0。
    只要 i 小于 做 计数 用 存储 的 第 5 项：
        若 存储 的 第 5 项 的 第 i 项 等于 键：
            做 删除 用 存储 的 第 5 项、i。
            做 追加 用 存储 的 第 5 项、键。
            返还。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 存储 的 第 5 项、键。
完。

kv_设 接受 存储、键、值：
    旧 为 做 字典取 用 存储 的 第 0 项、键。
    做 字典设 用 存储 的 第 0 项、键、值。
    做 字典设 用 存储 的 第 2 项、键、0。
    若 旧 等于 空：
        存储 的 第 4 项 为 存储 的 第 4 项 加 1。
    完。
    做 kv_提升键 用 存储、键。
    只要 存储 的 第 4 项 大于 存储 的 第 3 项 并且 存储 的 第 3 项 大于 0：
        做 kv_淘汰LRU 用 存储。
    完。
完。

kv_取 接受 存储、键：
    值 为 做 字典取 用 存储 的 第 0 项、键。
    若 值 不等于 空：
        可变 计数值 为 做 字典取 用 存储 的 第 2 项、键。
        若 计数值 等于 空：
            计数值 为 0。
        完。
        做 字典设 用 存储 的 第 2 项、键、计数值 加 1。
        做 kv_提升键 用 存储、键。
    完。
    返还 值。
完。

kv_删 接受 存储、键：
    旧 为 做 字典取 用 存储 的 第 0 项、键。
    若 旧 不等于 空：
        做 字典设 用 存储 的 第 0 项、键、空。
        做 字典设 用 存储 的 第 1 项、键、空。
        做 字典设 用 存储 的 第 2 项、键、空。
        存储 的 第 4 项 为 存储 的 第 4 项 减 1。
        可变 i 为 0。
        只要 i 小于 做 计数 用 存储 的 第 5 项：
            若 存储 的 第 5 项 的 第 i 项 等于 键：
                做 删除 用 存储 的 第 5 项、i。
                i 为 i 减 1。
            完。
            i 为 i 加 1。
        完。
    完。
完。

键值存储_kv_存在 接受 存储、键：
    返还 做 字典取 用 存储 的 第 0 项、键 不等于 空。
完。

键值存储_kv_大小 接受 存储：
    返还 存储 的 第 4 项。
完。

kv_设过期 接受 存储、键、过期时间：
    做 字典设 用 存储 的 第 1 项、键、过期时间。
完。

kv_已过期 接受 存储、键、当前时间：
    过期 为 做 字典取 用 存储 的 第 1 项、键。
    若 过期 等于 空：
        返还 0。
    完。
    返还 当前时间 大于 过期。
完。

kv_淘汰LRU 接受 存储：
    若 做 计数 用 存储 的 第 5 项 等于 0：
        返还。
    完。
    淘汰键 为 存储 的 第 5 项 的 第 0 项。
    做 删除 用 存储 的 第 5 项。
    做 字典设 用 存储 的 第 0 项、淘汰键、空。
    做 字典设 用 存储 的 第 1 项、淘汰键、空。
    做 字典设 用 存储 的 第 2 项、淘汰键、空。
    存储 的 第 4 项 为 存储 的 第 4 项 减 1。
完。

kv_事务_开始 接受 零参：
    返还 一组：一组：（空）、1。
完。

kv_事务_设 接受 事务、键、值：
    若 事务 的 第 1 项 等于 1：
        做 追加 用 事务 的 第 0 项、一组：「SET」、键、值。
    完。
完。

kv_事务_删 接受 事务、键：
    若 事务 的 第 1 项 等于 1：
        做 追加 用 事务 的 第 0 项、一组：「DEL」、键、空。
    完。
完。

kv_事务_提交 接受 存储、事务：
    若 事务 的 第 1 项 不等于 1：
        返还 0。
    完。
    操作 为 事务 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 操作：
        op 为 操作 的 第 i 项。
        若 op 的 第 0 项 等于 「SET」：
            做 kv_设 用 存储、op 的 第 1 项、op 的 第 2 项。
        完。
        若 op 的 第 0 项 等于 「DEL」：
            做 kv_删 用 存储、op 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    事务 的 第 1 项 为 0。
    返还 1。
完。

kv_事务_回滚 接受 事务：
    事务 的 第 0 项 为 一组：（空）。
    事务 的 第 1 项 为 0。
完。

kv_批量设 接受 存储、键值对列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 键值对列表：
        做 kv_设 用 存储、键值对列表 的 第 i 项 的 第 0 项、键值对列表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

kv_批量取 接受 存储、键列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        做 追加 用 结果、做 kv_取 用 存储、键列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

kv_批量删 接受 存储、键列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        做 kv_删 用 存储、键列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

可变 _kv_ttl_字典 为 做 字典。

可变 _kv_ttl_键列 为 一组：（空）。

kv_设TTL 接受 键、到期时间：
    已有 为 做 字典取 用 _kv_ttl_字典、键。
    若 已有 等于 空：
        做 追加 用 _kv_ttl_键列、键。
    完。
    做 字典设 用 _kv_ttl_字典、键、到期时间。
完。

kv_已过期_全局 接受 键、当前时间：
    到期 为 做 字典取 用 _kv_ttl_字典、键。
    若 到期 等于 空：
        返还 假。
    完。
    返还 当前时间 大于 到期。
完。

kv_清理过期 接受 存储、当前时间：
    可变 清理数 为 0。
    可变 新键列 为 一组：（空）。
    新字典 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _kv_ttl_键列：
        键 为 _kv_ttl_键列 的 第 i 项。
        到期 为 做 字典取 用 _kv_ttl_字典、键。
        若 到期 不等于 空 并且 当前时间 大于 到期：
            做 kv_删 用 存储、键。
            清理数 为 清理数 加 1。
        否则若 到期 不等于 空：
            做 追加 用 新键列、键。
            做 字典设 用 新字典、键、到期。
        完。
        i 为 i 加 1。
    完。
    _kv_ttl_键列 为 新键列。
    _kv_ttl_字典 为 新字典。
    返还 清理数。
完。

kv_快照 接受 存储：
    键列表 为 做 字典键列表 用 存储 的 第 0 项。
    可变 快照键 为 一组：（空）。
    可变 快照值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        键 为 键列表 的 第 i 项。
        做 追加 用 快照键、键。
        做 追加 用 快照值、做 字典取 用 存储 的 第 0 项、键。
        i 为 i 加 1。
    完。
    返还 一组：快照键、快照值。
完。

kv_恢复快照 接受 存储、快照：
    新数据 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 快照 的 第 0 项：
        做 字典设 用 新数据、快照 的 第 0 项 的 第 i 项、快照 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    存储 的 第 0 项 为 新数据。
    存储 的 第 4 项 为 做 计数 用 快照 的 第 0 项。
完。

kv_前缀扫描 接受 存储、前缀：
    可变 结果键 为 一组：（空）。
    键列表 为 做 字典键列表 用 存储 的 第 0 项。
    可变 结果值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        键 为 键列表 的 第 i 项。
        前缀长 为 做 字节长度 用 前缀。
        若 做 字节长度 用 键 大于或等于 前缀长：
            若 做 子串 用 键、0、前缀长 等于 前缀：
                做 追加 用 结果键、键。
                做 追加 用 结果值、做 字典取 用 存储 的 第 0 项、键。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 一组：结果键、结果值。
完。

可变 _kv_读数 为 0。

可变 _kv_写数 为 0。

键值存储_kv_记录读 接受 零参：
    _kv_读数 为 _kv_读数 加 1。
完。

键值存储_kv_记录写 接受 零参：
    _kv_写数 为 _kv_写数 加 1。
完。

本章 收束。
