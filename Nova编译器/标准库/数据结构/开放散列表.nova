《开放散列表》

引用《..·基础》。

OHT_空 为 0。

OHT_占用 为 1。

OHT_删除 为 2。

oht_创建 接受 容量：
    可变 桶 为 一组：（空）。
    可变 状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 桶、一组：0、0。
        做 追加 用 状态、OHT_空。
        i 为 i 加 1。
    完。
    返还 一组：桶、状态、容量、0。
完。

oht_哈希 接受 键、容量：
    可变 h 为 0。
    若 键 大于或等于 0：
        h 为 键。
    否则：
        h 为 0 减 键。
    完。
    返还 h 取余于 容量。
完。

oht_字符串哈希 接受 键、容量：
    可变 h 为 5381。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 键：
        h 为 h 乘 33 加 做 字节取 用 键、i。
        若 h 小于 0：
            h 为 0 减 h。
        完。
        i 为 i 加 1。
    完。
    返还 h 取余于 容量。
完。

oht_插入 接受 表、键、值：
    容量 为 表 的 第 2 项。
    可变 索引 为 做 oht_哈希 用 键、容量。
    状态 为 表 的 第 1 项。
    桶 为 表 的 第 0 项。
    可变 探测 为 0。
    只要 探测 小于 容量：
        若 状态 的 第 索引 项 等于 OHT_空 或者 状态 的 第 索引 项 等于 OHT_删除：
            桶 的 第 索引 项 为 一组：键、值。
            状态 的 第 索引 项 为 OHT_占用。
            表 的 第 3 项 为 表 的 第 3 项 加 1。
            返还 1。
        完。
        现有桶 为 桶 的 第 索引 项。
        若 状态 的 第 索引 项 等于 OHT_占用 并且 现有桶 的 第 0 项 等于 键：
            桶 的 第 索引 项 为 一组：键、值。
            返还 1。
        完。
        索引 为 (索引 加 1) 取余于 容量。
        探测 为 探测 加 1。
    完。
    返还 0。
完。

oht_查找 接受 表、键：
    容量 为 表 的 第 2 项。
    可变 索引 为 做 oht_哈希 用 键、容量。
    状态 为 表 的 第 1 项。
    桶 为 表 的 第 0 项。
    可变 探测 为 0。
    只要 探测 小于 容量：
        若 状态 的 第 索引 项 等于 OHT_空：
            返还 空。
        完。
        若 状态 的 第 索引 项 等于 OHT_占用：
            b 为 桶 的 第 索引 项。
            若 b 的 第 0 项 等于 键：
                返还 b 的 第 1 项。
            完。
        完。
        索引 为 (索引 加 1) 取余于 容量。
        探测 为 探测 加 1。
    完。
    返还 空。
完。

oht_删除 接受 表、键：
    容量 为 表 的 第 2 项。
    可变 索引 为 做 oht_哈希 用 键、容量。
    状态 为 表 的 第 1 项。
    桶 为 表 的 第 0 项。
    可变 探测 为 0。
    只要 探测 小于 容量：
        若 状态 的 第 索引 项 等于 OHT_空：
            返还 0。
        完。
        若 状态 的 第 索引 项 等于 OHT_占用：
            b 为 桶 的 第 索引 项。
            若 b 的 第 0 项 等于 键：
                状态 的 第 索引 项 为 OHT_删除。
                表 的 第 3 项 为 表 的 第 3 项 减 1。
                返还 1。
            完。
        完。
        索引 为 (索引 加 1) 取余于 容量。
        探测 为 探测 加 1。
    完。
    返还 0。
完。

oht_大小 接受 表：
    返还 表 的 第 3 项。
完。

oht_容量 接受 表：
    返还 表 的 第 2 项。
完。

oht_负载因子 接受 表：
    返还 表 的 第 3 项 乘 100 除以 表 的 第 2 项。
完。

oht_清空 接受 表：
    状态 为 表 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 表 的 第 2 项：
        状态 的 第 i 项 为 OHT_空。
        i 为 i 加 1。
    完。
    表 的 第 3 项 为 0。
完。

oht_所有键 接受 表：
    可变 键列表 为 一组：（空）。
    数据 为 表 的 第 0 项。
    状态 为 表 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 表 的 第 2 项：
        若 状态 的 第 i 项 等于 OHT_占用：
            做 追加 用 键列表、数据 的 第 i 项 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 键列表。
完。

oht_所有值 接受 表：
    可变 值列表 为 一组：（空）。
    数据 为 表 的 第 0 项。
    状态 为 表 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 表 的 第 2 项：
        若 状态 的 第 i 项 等于 OHT_占用：
            做 追加 用 值列表、数据 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 值列表。
完。

oht_所有键值 接受 表：
    可变 对列表 为 一组：（空）。
    数据 为 表 的 第 0 项。
    状态 为 表 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 表 的 第 2 项：
        若 状态 的 第 i 项 等于 OHT_占用：
            做 追加 用 对列表、一组：数据 的 第 i 项 的 第 0 项、数据 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 对列表。
完。

oht_批量插入 接受 表、键值对列表：
    可变 成功 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键值对列表：
        若 做 oht_插入 用 表、键值对列表 的 第 i 项 的 第 0 项、键值对列表 的 第 i 项 的 第 1 项：
            成功 为 成功 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 成功。
完。

oht_批量删除 接受 表、键列表：
    可变 成功 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        若 做 oht_删除 用 表、键列表 的 第 i 项：
            成功 为 成功 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 成功。
完。

oht_需要扩容 接受 表：
    返还 做 oht_负载因子 用 表 大于 70。
完。

oht_扩容 接受 表：
    旧数据 为 表 的 第 0 项。
    旧状态 为 表 的 第 1 项。
    旧容量 为 表 的 第 2 项。
    新容量 为 旧容量 乘 2。
    可变 新数据 为 一组：（空）。
    可变 新状态 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 新容量：
        做 追加 用 新数据、一组：空、空。
        做 追加 用 新状态、OHT_空。
        i 为 i 加 1。
    完。
    表 的 第 0 项 为 新数据。
    表 的 第 1 项 为 新状态。
    表 的 第 2 项 为 新容量。
    表 的 第 3 项 为 0。
    i 为 0。
    只要 i 小于 旧容量：
        若 旧状态 的 第 i 项 等于 OHT_占用：
            做 oht_插入 用 表、旧数据 的 第 i 项 的 第 0 项、旧数据 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
完。

oht_自动维护 接受 表：
    若 做 oht_需要扩容 用 表：
        做 oht_扩容 用 表。
    完。
完。

oht_字符串散列 接受 s、容量：
    可变 h 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 s：
        h 为 h 乘 31 加 做 编码 用 做 子串 用 s、i、1。
        h 为 h 取余于 容量。
        i 为 i 加 1。
    完。
    返还 h。
完。

oht_整数散列 接受 n、容量：
    可变 h 为 n。
    h 为 (h 右移 16 位异或 h) 乘 45267。
    h 为 (h 右移 16 位异或 h) 乘 45267。
    h 为 h 右移 16 位异或 h。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    返还 h 取余于 容量。
完。

可变 _oht_插入数 为 0。

可变 _oht_查找数 为 0。

可变 _oht_冲突数 为 0。

oht_记录插入 接受 零参：
    _oht_插入数 为 _oht_插入数 加 1。
完。

oht_记录查找 接受 零参：
    _oht_查找数 为 _oht_查找数 加 1。
完。

oht_记录冲突 接受 零参：
    _oht_冲突数 为 _oht_冲突数 加 1。
完。

本章 收束。
