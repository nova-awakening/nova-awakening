《内存表》

引用《..·基础》。

引用《..·数据库·关系·关系数据库》。

内存表_表_创建 接受 列名列表：
    返还 一组：列名列表、一组：（空）、负 1。
完。

表_列名 接受 表：
    返还 表 的 第 0 项。
完。

内存表_表_行数 接受 表：
    返还 做 计数 用 表 的 第 1 项。
完。

内存表_表_列数 接受 表：
    返还 做 计数 用 表 的 第 0 项。
完。

内存表_表_列索引 接受 表、列名：
    列 为 表 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列：
        若 列 的 第 i 项 等于 列名：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

内存表_表_插入 接受 表、行：
    做 追加 用 表 的 第 1 项、行。
完。

表_取值 接受 表、行索引、列名：
    列idx 为 做 内存表_表_列索引 用 表、列名。
    若 列idx 等于 负 1：
        返还 空。
    完。
    行 为 表 的 第 1 项 的 第 行索引 项。
    返还 行 的 第 列idx 项。
完。

表_取行 接受 表、行索引：
    返还 表 的 第 1 项 的 第 行索引 项。
完。

表_取列 接受 表、列名：
    列idx 为 做 内存表_表_列索引 用 表、列名。
    若 列idx 等于 负 1：
        返还 一组：（空）。
    完。
    行列表 为 表 的 第 1 项。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 行列表 的 第 i 项。
        做 追加 用 结果、行 的 第 列idx 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

表_过滤 接受 表、列名、操作符、值：
    列idx 为 做 内存表_表_列索引 用 表、列名。
    若 列idx 等于 负 1：
        返还 一组：（空）。
    完。
    行列表 为 表 的 第 1 项。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 行列表 的 第 i 项。
        单元格 为 行 的 第 列idx 项。
        可变 通过 为 0。
        若 操作符 等于 「=」 并且 单元格 等于 值：
            通过 为 1。
        完。
        若 操作符 等于 「<」 并且 单元格 小于 值：
            通过 为 1。
        完。
        若 操作符 等于 「>」 并且 单元格 大于 值：
            通过 为 1。
        完。
        若 操作符 等于 「<=」 并且 单元格 小于或等于 值：
            通过 为 1。
        完。
        若 操作符 等于 「>=」 并且 单元格 大于或等于 值：
            通过 为 1。
        完。
        若 操作符 等于 「!=」 并且 单元格 不等于 值：
            通过 为 1。
        完。
        若 通过 等于 1：
            做 追加 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

表_求和 接受 表、列名：
    列idx 为 做 内存表_表_列索引 用 表、列名。
    若 列idx 等于 负 1：
        返还 0。
    完。
    行列表 为 表 的 第 1 项。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 行列表：
        行 为 行列表 的 第 i 项。
        总和 为 总和 加 行 的 第 列idx 项。
        i 为 i 加 1。
    完。
    返还 总和。
完。

表_平均 接受 表、列名：
    总和 为 做 表_求和 用 表、列名。
    行数 为 做 计数 用 表 的 第 1 项。
    若 行数 等于 0：
        返还 0。
    完。
    返还 总和 除以 行数。
完。

表_最大 接受 表、列名：
    列idx 为 做 内存表_表_列索引 用 表、列名。
    若 列idx 等于 负 1：
        返还 0。
    完。
    行列表 为 表 的 第 1 项。
    若 做 计数 用 行列表 等于 0：
        返还 0。
    完。
    首行 为 行列表 的 第 0 项。
    可变 最大值 为 首行 的 第 列idx 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 行列表：
        行 为 行列表 的 第 i 项。
        若 行 的 第 列idx 项 大于 最大值：
            最大值 为 行 的 第 列idx 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大值。
完。

表_最小 接受 表、列名：
    列idx 为 做 内存表_表_列索引 用 表、列名。
    若 列idx 等于 负 1：
        返还 0。
    完。
    行列表 为 表 的 第 1 项。
    若 做 计数 用 行列表 等于 0：
        返还 0。
    完。
    首行 为 行列表 的 第 0 项。
    可变 最小值 为 首行 的 第 列idx 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 行列表：
        行 为 行列表 的 第 i 项。
        若 行 的 第 列idx 项 小于 最小值：
            最小值 为 行 的 第 列idx 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小值。
完。

表_排序 接受 表、列名、升序：
    列idx 为 做 内存表_表_列索引 用 表、列名。
    若 列idx 等于 负 1：
        返还 空。
    完。
    行列表 为 表 的 第 1 项。
    可变 已改变 为 1。
    只要 已改变 等于 1：
        已改变 为 0。
        可变 i 为 0。
        只要 i 小于 做 计数 用 行列表 减 1：
            行a 为 行列表 的 第 i 项。
            行b 为 行列表 的 第 i 加 1 项。
            可变 需交换 为 0。
            若 升序 等于 1 并且 行a 的 第 列idx 项 大于 行b 的 第 列idx 项：
                需交换 为 1。
            完。
            若 升序 等于 0 并且 行a 的 第 列idx 项 小于 行b 的 第 列idx 项：
                需交换 为 1。
            完。
            若 需交换 等于 1：
                行列表 的 第 i 项 为 行b。
                行列表 的 第 i 加 1 项 为 行a。
                已改变 为 1。
            完。
            i 为 i 加 1。
        完。
    完。
完。

表_删行 接受 表、行索引：
    做 删除 用 表 的 第 1 项、行索引。
完。

表_更新 接受 表、行索引、列名、值：
    列idx 为 做 内存表_表_列索引 用 表、列名。
    若 列idx 等于 负 1：
        返还 空。
    完。
    行 为 表 的 第 1 项 的 第 行索引 项。
    行 的 第 列idx 项 为 值。
完。

表_查询 接受 表、列名、条件函数：
    列idx 为 做 内存表_表_列索引 用 表、列名。
    若 列idx 等于 负 1：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 表 的 第 1 项：
        若 做 条件函数 用 表 的 第 1 项 的 第 i 项 的 第 列idx 项：
            做 追加 用 结果、表 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

表_查询等于 接受 表、列名、值：
    列idx 为 做 内存表_表_列索引 用 表、列名。
    若 列idx 等于 负 1：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 表 的 第 1 项：
        若 表 的 第 1 项 的 第 i 项 的 第 列idx 项 等于 值：
            做 追加 用 结果、表 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

表_查询范围 接受 表、列名、最小、最大：
    列idx 为 做 内存表_表_列索引 用 表、列名。
    若 列idx 等于 负 1：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 表 的 第 1 项：
        v 为 表 的 第 1 项 的 第 i 项 的 第 列idx 项。
        若 v 大于或等于 最小 并且 v 小于或等于 最大：
            做 追加 用 结果、表 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

表_计行 接受 表：
    返还 做 计数 用 表 的 第 1 项。
完。

表_分组计数 接受 表、列名：
    可变 计数列表 为 一组：（空）。
    列idx 为 做 内存表_表_列索引 用 表、列名。
    若 列idx 等于 负 1：
        返还 一组：一组：（空）、一组：（空）。
    完。
    可变 键列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 表 的 第 1 项：
        键 为 表 的 第 1 项 的 第 i 项 的 第 列idx 项。
        可变 找到 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 键列表：
            若 键列表 的 第 j 项 等于 键：
                计数列表 的 第 j 项 为 计数列表 的 第 j 项 加 1。
                找到 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 找到：
            做 追加 用 键列表、键。
            做 追加 用 计数列表、1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：键列表、计数列表。
完。

表_投影 接受 表、列名列表：
    可变 列索引列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列名列表：
        做 追加 用 列索引列、做 内存表_表_列索引 用 表、列名列表 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 表 的 第 1 项：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 做 计数 用 列索引列：
            若 列索引列 的 第 j 项 大于或等于 0：
                做 追加 用 行、表 的 第 1 项 的 第 i 项 的 第 列索引列 的 第 j 项 项。
            否则：
                做 追加 用 行、空。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、行。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _表_查询数 为 0。

可变 _表_写入数 为 0。

表_记录查询 接受 零参：
    _表_查询数 为 _表_查询数 加 1。
完。

表_记录写入 接受 零参：
    _表_写入数 为 _表_写入数 加 1。
完。

本章 收束。
