《布隆过滤器》

引用《..·基础》。

可变 布隆_全局创建数 为 0。

可变 布隆_全局添加数 为 0。

可变 布隆_全局查询数 为 0。

布隆过滤_布隆_创建 接受 大小、哈希数：
    布隆_全局创建数 为 布隆_全局创建数 加 1。
    可变 位数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        做 追加 用 位数组、0。
        i 为 i 加 1。
    完。
    返还 一组：位数组、大小、哈希数、0、一组：0、0。
完。

布隆_按容量创建 接受 预期元素数、假阳性率：
    可变 大小 为 预期元素数 乘 10。
    若 假阳性率 小于或等于 1：
        大小 为 预期元素数 乘 20。
    否则若 假阳性率 小于或等于 5：
        大小 为 预期元素数 乘 15。
    完。
    可变 哈希数 为 7。
    若 假阳性率 大于 5：
        哈希数 为 5。
    完。
    若 假阳性率 大于 10：
        哈希数 为 3。
    完。
    返还 做 布隆过滤_布隆_创建 用 大小、哈希数。
完。

布隆_哈希_fnv 接受 数据、种子、大小：
    可变 h 为 2166136261 加 种子 乘 16777619。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 数据：
        h 为 h 乘 16777619。
        h 为 h 加 做 字节取 用 数据、i。
        若 h 小于 0：
            h 为 0 减 h。
        完。
        i 为 i 加 1。
    完。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    返还 h 取余于 大小。
完。

布隆_哈希_djb2 接受 数据、种子、大小：
    可变 h 为 5381 加 种子 乘 33。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 数据：
        h 为 h 乘 33 加 做 字节取 用 数据、i。
        若 h 小于 0：
            h 为 0 减 h。
        完。
        i 为 i 加 1。
    完。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    返还 h 取余于 大小。
完。

布隆_哈希_sdbm 接受 数据、种子、大小：
    可变 h 为 种子。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 数据：
        h 为 做 字节取 用 数据、i 加 h 乘 65536 加 h 乘 512 减 h。
        若 h 小于 0：
            h 为 0 减 h。
        完。
        i 为 i 加 1。
    完。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    返还 h 取余于 大小。
完。

布隆过滤_布隆_哈希 接受 数据、种子、大小：
    策略 为 种子 取余于 3。
    若 策略 等于 0：
        返还 做 布隆_哈希_fnv 用 数据、种子、大小。
    完。
    若 策略 等于 1：
        返还 做 布隆_哈希_djb2 用 数据、种子、大小。
    完。
    返还 做 布隆_哈希_sdbm 用 数据、种子、大小。
完。

布隆_双重哈希 接受 数据、索引、大小：
    h1 为 做 布隆_哈希_fnv 用 数据、0、大小。
    可变 h2 为 做 布隆_哈希_djb2 用 数据、0、大小。
    若 h2 等于 0：
        h2 为 1。
    完。
    返还 (h1 加 索引 乘 h2) 取余于 大小。
完。

布隆过滤_布隆_追加 接受 过滤器、元素：
    布隆_全局添加数 为 布隆_全局添加数 加 1。
    统计 为 过滤器 的 第 4 项。
    统计 的 第 0 项 为 统计 的 第 0 项 加 1。
    位数组 为 过滤器 的 第 0 项。
    大小 为 过滤器 的 第 1 项。
    哈希数 为 过滤器 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 哈希数：
        位置 为 做 布隆_双重哈希 用 元素、i、大小。
        位数组 的 第 位置 项 为 1。
        i 为 i 加 1。
    完。
    过滤器 的 第 3 项 为 过滤器 的 第 3 项 加 1。
完。

布隆_批量追加 接受 过滤器、元素列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 元素列表：
        做 布隆过滤_布隆_追加 用 过滤器、元素列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

布隆过滤_布隆_可能包含 接受 过滤器、元素：
    布隆_全局查询数 为 布隆_全局查询数 加 1。
    统计 为 过滤器 的 第 4 项。
    统计 的 第 1 项 为 统计 的 第 1 项 加 1。
    位数组 为 过滤器 的 第 0 项。
    大小 为 过滤器 的 第 1 项。
    哈希数 为 过滤器 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 哈希数：
        位置 为 做 布隆_双重哈希 用 元素、i、大小。
        若 位数组 的 第 位置 项 等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

布隆_确定不包含 接受 过滤器、元素：
    返还 做 布隆过滤_布隆_可能包含 用 过滤器、元素 等于 假。
完。

布隆_批量查询 接受 过滤器、元素列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 元素列表：
        做 追加 用 结果、做 布隆过滤_布隆_可能包含 用 过滤器、元素列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

布隆_填充率 接受 过滤器：
    位数组 为 过滤器 的 第 0 项。
    大小 为 过滤器 的 第 1 项。
    可变 已设 为 0。
    可变 i 为 0。
    只要 i 小于 大小：
        若 位数组 的 第 i 项 等于 1：
            已设 为 已设 加 1。
        完。
        i 为 i 加 1。
    完。
    若 大小 等于 0：
        返还 0。
    完。
    返还 已设 乘 100 除以 大小。
完。

布隆_元素数 接受 过滤器：
    返还 过滤器 的 第 3 项。
完。

布隆_大小 接受 过滤器：
    返还 过滤器 的 第 1 项。
完。

布隆_哈希数 接受 过滤器：
    返还 过滤器 的 第 2 项。
完。

布隆_添加操作数 接受 过滤器：
    返还 过滤器 的 第 4 项 的 第 0 项。
完。

布隆_查询操作数 接受 过滤器：
    返还 过滤器 的 第 4 项 的 第 1 项。
完。

布隆_重置 接受 过滤器：
    位数组 为 过滤器 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 过滤器 的 第 1 项：
        位数组 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    过滤器 的 第 3 项 为 0。
    过滤器 的 第 4 项 的 第 0 项 为 0。
    过滤器 的 第 4 项 的 第 1 项 为 0。
完。

布隆_合并 接受 过滤器1、过滤器2：
    若 过滤器1 的 第 1 项 不等于 过滤器2 的 第 1 项：
        返还 过滤器1。
    完。
    大小 为 过滤器1 的 第 1 项。
    哈希数 为 过滤器1 的 第 2 项。
    结果 为 做 布隆过滤_布隆_创建 用 大小、哈希数。
    可变 i 为 0。
    只要 i 小于 大小：
        若 过滤器1 的 第 0 项 的 第 i 项 等于 1 或者 过滤器2 的 第 0 项 的 第 i 项 等于 1：
            结果 的 第 0 项 的 第 i 项 为 1。
        完。
        i 为 i 加 1。
    完。
    结果 的 第 3 项 为 过滤器1 的 第 3 项 加 过滤器2 的 第 3 项。
    返还 结果。
完。

布隆_假阳性率 接受 过滤器：
    n 为 过滤器 的 第 3 项。
    m 为 过滤器 的 第 1 项。
    k 为 过滤器 的 第 2 项。
    若 m 等于 0 或者 n 等于 0：
        返还 0。
    完。
    填充 为 做 布隆_填充率 用 过滤器。
    可变 概率 为 1。
    可变 i 为 0。
    只要 i 小于 k：
        概率 为 概率 乘 填充 除以 100。
        i 为 i 加 1。
    完。
    返还 概率。
完。

布隆_预测假阳性率 接受 位大小、哈希数、元素数：
    若 位大小 等于 0：
        返还 100。
    完。
    可变 填充比 为 元素数 乘 哈希数 乘 100 除以 位大小。
    若 填充比 大于 100：
        填充比 为 100。
    完。
    可变 估算 为 1。
    可变 i 为 0。
    只要 i 小于 哈希数：
        估算 为 估算 乘 填充比 除以 100。
        i 为 i 加 1。
    完。
    返还 估算。
完。

布隆_推荐参数 接受 预期元素数、目标假阳性率：
    可变 最优大小 为 预期元素数 乘 10。
    可变 最优哈希数 为 7。
    若 目标假阳性率 小于或等于 1：
        最优大小 为 预期元素数 乘 20。
        最优哈希数 为 14。
    否则若 目标假阳性率 小于或等于 5：
        最优大小 为 预期元素数 乘 15。
        最优哈希数 为 10。
    否则若 目标假阳性率 小于或等于 10：
        最优大小 为 预期元素数 乘 10。
        最优哈希数 为 7。
    否则：
        最优大小 为 预期元素数 乘 7。
        最优哈希数 为 5。
    完。
    返还 一组：最优大小、最优哈希数、最优大小 除以 8。
完。

布隆_剩余容量 接受 过滤器：
    填充 为 做 布隆_填充率 用 过滤器。
    若 填充 大于或等于 80：
        返还 0。
    完。
    当前 为 过滤器 的 第 3 项。
    总容量 为 过滤器 的 第 1 项 乘 80 除以 (过滤器 的 第 2 项 乘 100)。
    若 总容量 小于或等于 当前：
        返还 0。
    完。
    返还 总容量 减 当前。
完。

布隆_需要扩容 接受 过滤器：
    返还 做 布隆_填充率 用 过滤器 大于 70。
完。

布隆_质量评分 接受 过滤器：
    可变 评分 为 100。
    填充 为 做 布隆_填充率 用 过滤器。
    若 填充 大于 80：
        评分 为 评分 减 40。
    完。
    若 填充 大于 60：
        评分 为 评分 减 20。
    完。
    若 填充 大于 40：
        评分 为 评分 减 10。
    完。
    若 过滤器 的 第 2 项 小于 3：
        评分 为 评分 减 15。
    完。
    若 过滤器 的 第 2 项 大于 15：
        评分 为 评分 减 10。
    完。
    若 评分 小于 0：
        评分 为 0。
    完。
    返还 评分。
完。

布隆_健康报告 接受 过滤器：
    填充 为 做 布隆_填充率 用 过滤器。
    质量 为 做 布隆_质量评分 用 过滤器。
    容量 为 做 布隆_剩余容量 用 过滤器。
    可变 状态 为 「健康」。
    若 填充 大于 70：
        状态 为 「警告」。
    完。
    若 填充 大于 85：
        状态 为 「危险」。
    完。
    返还 一组：状态、填充、质量、容量、过滤器 的 第 3 项。
完。

计数布隆_创建 接受 大小、哈希数：
    可变 计数数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        做 追加 用 计数数组、0。
        i 为 i 加 1。
    完。
    返还 一组：计数数组、大小、哈希数、0、一组：0。
完。

计数布隆_追加 接受 过滤器、元素：
    大小 为 过滤器 的 第 1 项。
    哈希数 为 过滤器 的 第 2 项。
    过滤器 的 第 4 项 的 第 0 项 为 过滤器 的 第 4 项 的 第 0 项 加 1。
    可变 i 为 0。
    只要 i 小于 哈希数：
        位置 为 做 布隆_双重哈希 用 元素、i、大小。
        过滤器 的 第 0 项 的 第 位置 项 为 过滤器 的 第 0 项 的 第 位置 项 加 1。
        i 为 i 加 1。
    完。
    过滤器 的 第 3 项 为 过滤器 的 第 3 项 加 1。
完。

计数布隆_删除 接受 过滤器、元素：
    若 做 计数布隆_查询 用 过滤器、元素 等于 假：
        返还 假。
    完。
    大小 为 过滤器 的 第 1 项。
    哈希数 为 过滤器 的 第 2 项。
    过滤器 的 第 4 项 的 第 2 项 为 过滤器 的 第 4 项 的 第 2 项 加 1。
    可变 i 为 0。
    只要 i 小于 哈希数：
        位置 为 做 布隆_双重哈希 用 元素、i、大小。
        若 过滤器 的 第 0 项 的 第 位置 项 大于 0：
            过滤器 的 第 0 项 的 第 位置 项 为 过滤器 的 第 0 项 的 第 位置 项 减 1。
        完。
        i 为 i 加 1。
    完。
    过滤器 的 第 3 项 为 过滤器 的 第 3 项 减 1。
    返还 真。
完。

计数布隆_查询 接受 过滤器、元素：
    大小 为 过滤器 的 第 1 项。
    哈希数 为 过滤器 的 第 2 项。
    过滤器 的 第 4 项 的 第 1 项 为 过滤器 的 第 4 项 的 第 1 项 加 1。
    可变 i 为 0。
    只要 i 小于 哈希数：
        位置 为 做 布隆_双重哈希 用 元素、i、大小。
        若 过滤器 的 第 0 项 的 第 位置 项 等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

布隆_均匀性 接受 过滤器：
    位数组 为 过滤器 的 第 0 项。
    大小 为 过滤器 的 第 1 项。
    若 大小 小于 10：
        返还 100。
    完。
    段数 为 10。
    段大小 为 大小 除以 段数。
    可变 段计数 为 一组：（空）。
    可变 s 为 0。
    只要 s 小于 段数：
        做 追加 用 段计数、0。
        s 为 s 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 大小：
        若 位数组 的 第 i 项 等于 1：
            可变 段 为 i 除以 段大小。
            若 段 大于或等于 段数：
                段 为 段数 减 1。
            完。
            段计数 的 第 段 项 为 段计数 的 第 段 项 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 最大 为 0。
    可变 最小 为 段大小。
    i 为 0。
    只要 i 小于 段数：
        若 段计数 的 第 i 项 大于 最大：
            最大 为 段计数 的 第 i 项。
        完。
        若 段计数 的 第 i 项 小于 最小：
            最小 为 段计数 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 最大 等于 0：
        返还 100。
    完。
    返还 最小 乘 100 除以 最大。
完。

布隆_全局报告 接受 零参：
    返还 一组：布隆_全局创建数、布隆_全局添加数、布隆_全局查询数。
完。

分层布隆_创建 接受 初始大小、哈希数：
    首层 为 做 布隆过滤_布隆_创建 用 初始大小、哈希数。
    返还 一组：一组：首层、初始大小、哈希数、0。
完。

分层布隆_追加 接受 分层、元素：
    层列表 为 分层 的 第 0 项。
    当前层 为 层列表 的 第 做 计数 用 层列表 减 1 项。
    若 做 布隆_填充率 用 当前层 大于 70：
        新大小 为 分层 的 第 1 项 乘 2。
        分层 的 第 1 项 为 新大小。
        新层 为 做 布隆过滤_布隆_创建 用 新大小、分层 的 第 2 项。
        做 追加 用 层列表、新层。
        做 布隆过滤_布隆_追加 用 新层、元素。
    否则：
        做 布隆过滤_布隆_追加 用 当前层、元素。
    完。
    分层 的 第 3 项 为 分层 的 第 3 项 加 1。
完。

分层布隆_查询 接受 分层、元素：
    层列表 为 分层 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 层列表：
        若 做 布隆过滤_布隆_可能包含 用 层列表 的 第 i 项、元素：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

分层布隆_层数 接受 分层：
    返还 做 计数 用 分层 的 第 0 项。
完。

分层布隆_总元素 接受 分层：
    返还 分层 的 第 3 项。
完。

分层布隆_总大小 接受 分层：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 分层 的 第 0 项：
        总 为 总 加 分层 的 第 0 项 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

布隆_序列化 接受 过滤器：
    可变 结果 为 一组：过滤器 的 第 1 项、过滤器 的 第 2 项、过滤器 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 过滤器 的 第 1 项：
        做 追加 用 结果、过滤器 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

布隆_反序列化 接受 数据：
    若 做 计数 用 数据 小于 3：
        返还 做 布隆过滤_布隆_创建 用 64、3。
    完。
    大小 为 数据 的 第 0 项。
    哈希数 为 数据 的 第 1 项。
    元素数 为 数据 的 第 2 项。
    过滤器 为 做 布隆过滤_布隆_创建 用 大小、哈希数。
    可变 i 为 0。
    只要 i 小于 大小 并且 i 加 3 小于 做 计数 用 数据：
        过滤器 的 第 0 项 的 第 i 项 为 数据 的 第 i 加 3 项。
        i 为 i 加 1。
    完。
    过滤器 的 第 3 项 为 元素数。
    返还 过滤器。
完。

布隆_冲突热力图 接受 过滤器：
    大小 为 过滤器 的 第 1 项。
    若 大小 小于 10：
        返还 一组：（空）。
    完。
    区域数 为 10。
    区域大小 为 大小 除以 区域数。
    可变 _热力 为 一组：（空）。
    可变 s 为 0。
    只要 s 小于 区域数：
        可变 设置数 为 0。
        可变 _i 为 s 乘 区域大小。
        可变 _止 为 (s 加 1) 乘 区域大小。
        若 _止 大于 大小：
            _止 为 大小。
        完。
        只要 _i 小于 _止：
            若 过滤器 的 第 0 项 的 第 _i 项 等于 1：
                设置数 为 设置数 加 1。
            完。
            _i 为 _i 加 1。
        完。
        做 追加 用 _热力、设置数 乘 100 除以 区域大小。
        s 为 s 加 1。
    完。
    返还 _热力。
完。

布隆_最优哈希数 接受 位大小、预期元素：
    若 预期元素 等于 0：
        返还 1。
    完。
    比率 为 位大小 除以 预期元素。
    若 比率 小于 2：
        返还 1。
    完。
    若 比率 小于 4：
        返还 3。
    完。
    若 比率 小于 8：
        返还 5。
    完。
    若 比率 小于 16：
        返还 7。
    完。
    若 比率 小于 32：
        返还 10。
    完。
    返还 14。
完。

布隆_空间比较 接受 元素数、位大小：
    布隆字节 为 位大小 除以 8。
    哈希集字节 为 元素数 乘 40。
    列表字节 为 元素数 乘 8。
    返还 一组：布隆字节、哈希集字节、列表字节、布隆字节 乘 100 除以 哈希集字节、布隆字节 乘 100 除以 列表字节。
完。

本章 收束。
