《红黑树》

引用《..·基础》。

RB_红 为 0。

RB_黑 为 1。

可变 rb_木计 为 0。

可变 rb_火计 为 0。

可变 rb_土计 为 0。

可变 rb_金计 为 0。

可变 rb_水计 为 0。

rb_创建节点 接受 键、值、颜色：
    返还 一组：键、值、颜色、空、空、空。
完。

rb_键 接受 n：
    返还 n 的 第 0 项。
完。

rb_值 接受 n：
    返还 n 的 第 1 项。
完。

rb_颜色 接受 n：
    若 n 等于 空：
        返还 RB_黑。
    完。
    返还 n 的 第 2 项。
完。

rb_左 接受 n：
    返还 n 的 第 3 项。
完。

rb_右 接受 n：
    返还 n 的 第 4 项。
完。

rb_父 接受 n：
    返还 n 的 第 5 项。
完。

rb_是红 接受 n：
    返还 做 rb_颜色 用 n 等于 RB_红。
完。

rb_创建树 接受 零参：
    返还 一组：空。
完。

rb_根 接受 树：
    返还 树 的 第 0 项。
完。

rb_设颜色 接受 n、c：
    n 的 第 2 项 为 c。
完。

rb_设左 接受 n、v：
    n 的 第 3 项 为 v。
完。

rb_设右 接受 n、v：
    n 的 第 4 项 为 v。
完。

rb_设父 接受 n、v：
    n 的 第 5 项 为 v。
完。

rb_设值 接受 n、v：
    n 的 第 1 项 为 v。
完。

rb_左旋 接受 树、x：
    rb_火计 为 rb_火计 加 1。
    y 为 做 rb_右 用 x。
    yl 为 做 rb_左 用 y。
    做 rb_设右 用 x、yl。
    若 yl 不等于 空：
        做 rb_设父 用 yl、x。
    完。
    xp 为 做 rb_父 用 x。
    做 rb_设父 用 y、xp。
    若 xp 等于 空：
        树 的 第 0 项 为 y。
    否则：
        xpl 为 做 rb_左 用 xp。
        若 x 等于 xpl：
            做 rb_设左 用 xp、y。
        否则：
            做 rb_设右 用 xp、y。
        完。
    完。
    做 rb_设左 用 y、x。
    做 rb_设父 用 x、y。
完。

rb_右旋 接受 树、x：
    rb_火计 为 rb_火计 加 1。
    y 为 做 rb_左 用 x。
    yr 为 做 rb_右 用 y。
    做 rb_设左 用 x、yr。
    若 yr 不等于 空：
        做 rb_设父 用 yr、x。
    完。
    xp 为 做 rb_父 用 x。
    做 rb_设父 用 y、xp。
    若 xp 等于 空：
        树 的 第 0 项 为 y。
    否则：
        xpr 为 做 rb_右 用 xp。
        若 x 等于 xpr：
            做 rb_设右 用 xp、y。
        否则：
            做 rb_设左 用 xp、y。
        完。
    完。
    做 rb_设右 用 y、x。
    做 rb_设父 用 x、y。
完。

rb_插入修复 接受 树、z_in：
    可变 z 为 z_in。
    可变 zp 为 做 rb_父 用 z。
    可变 _修复完 为 假。
    只要 zp 不等于 空 并且 _修复完 等于 假：
        若 做 rb_是红 用 zp 不等于 1：
            _修复完 为 真。
        否则：
            zpp 为 做 rb_父 用 zp。
            若 zpp 等于 空：
                _修复完 为 真。
            否则：
                zppl 为 做 rb_左 用 zpp。
                若 zp 等于 zppl：
                    叔 为 做 rb_右 用 zpp。
                    若 做 rb_是红 用 叔 等于 1：
                        做 rb_设颜色 用 zp、RB_黑。
                        做 rb_设颜色 用 叔、RB_黑。
                        做 rb_设颜色 用 zpp、RB_红。
                        z 为 zpp。
                        zp 为 做 rb_父 用 z。
                    否则：
                        zpr 为 做 rb_右 用 zp。
                        若 z 等于 zpr：
                            z 为 zp。
                            做 rb_左旋 用 树、z。
                            zp 为 做 rb_父 用 z。
                        完。
                        做 rb_设颜色 用 zp、RB_黑。
                        zpp2 为 做 rb_父 用 zp。
                        若 zpp2 不等于 空：
                            做 rb_设颜色 用 zpp2、RB_红。
                            做 rb_右旋 用 树、zpp2。
                        完。
                        zp 为 做 rb_父 用 z。
                    完。
                否则：
                    叔 为 做 rb_左 用 zpp。
                    若 做 rb_是红 用 叔 等于 1：
                        做 rb_设颜色 用 zp、RB_黑。
                        做 rb_设颜色 用 叔、RB_黑。
                        做 rb_设颜色 用 zpp、RB_红。
                        z 为 zpp。
                        zp 为 做 rb_父 用 z。
                    否则：
                        zpl 为 做 rb_左 用 zp。
                        若 z 等于 zpl：
                            z 为 zp。
                            做 rb_右旋 用 树、z。
                            zp 为 做 rb_父 用 z。
                        完。
                        做 rb_设颜色 用 zp、RB_黑。
                        zpp2 为 做 rb_父 用 zp。
                        若 zpp2 不等于 空：
                            做 rb_设颜色 用 zpp2、RB_红。
                            做 rb_左旋 用 树、zpp2。
                        完。
                        zp 为 做 rb_父 用 z。
                    完。
                完。
            完。
        完。
    完。
    根 为 做 rb_根 用 树。
    若 根 不等于 空：
        做 rb_设颜色 用 根、RB_黑。
    完。
完。

rb_插入 接受 树、键、值：
    rb_木计 为 rb_木计 加 1。
    z 为 做 rb_创建节点 用 键、值、RB_红。
    可变 y 为 空。
    可变 x 为 做 rb_根 用 树。
    只要 x 不等于 空：
        y 为 x。
        xk 为 做 rb_键 用 x。
        若 键 小于 xk：
            x 为 做 rb_左 用 x。
        否则若 键 大于 xk：
            x 为 做 rb_右 用 x。
        否则：
            做 rb_设值 用 x、值。
            返还 空。
        完。
    完。
    做 rb_设父 用 z、y。
    若 y 等于 空：
        树 的 第 0 项 为 z。
    否则：
        yk 为 做 rb_键 用 y。
        若 键 小于 yk：
            做 rb_设左 用 y、z。
        否则：
            做 rb_设右 用 y、z。
        完。
    完。
    做 rb_插入修复 用 树、z。
完。

rb_查找 接受 树、键：
    rb_水计 为 rb_水计 加 1。
    可变 x 为 做 rb_根 用 树。
    只要 x 不等于 空：
        xk 为 做 rb_键 用 x。
        若 键 等于 xk：
            返还 做 rb_值 用 x。
        完。
        若 键 小于 xk：
            x 为 做 rb_左 用 x。
        否则：
            x 为 做 rb_右 用 x。
        完。
    完。
    返还 空。
完。

rb_最小 接受 节点：
    可变 x 为 节点。
    只要 做 rb_左 用 x 不等于 空：
        x 为 做 rb_左 用 x。
    完。
    返还 x。
完。

rb_最大 接受 节点：
    可变 x 为 节点。
    只要 做 rb_右 用 x 不等于 空：
        x 为 做 rb_右 用 x。
    完。
    返还 x。
完。

rb_中序 接受 树：
    可变 结果 为 一组：（空）。
    做 rb_中序递归 用 做 rb_根 用 树、结果。
    返还 结果。
完。

rb_中序递归 接受 节点、结果：
    若 节点 等于 空：
        返还 空。
    完。
    做 rb_中序递归 用 做 rb_左 用 节点、结果。
    做 追加 用 结果、一组：做 rb_键 用 节点、做 rb_值 用 节点。
    做 rb_中序递归 用 做 rb_右 用 节点、结果。
完。

rb_大小 接受 树：
    返还 做 rb_大小递归 用 做 rb_根 用 树。
完。

rb_大小递归 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    返还 1 加 做 rb_大小递归 用 做 rb_左 用 节点 加 做 rb_大小递归 用 做 rb_右 用 节点。
完。

rb_高度 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    左h 为 做 rb_高度 用 做 rb_左 用 节点。
    右h 为 做 rb_高度 用 做 rb_右 用 节点。
    若 左h 大于 右h：
        返还 1 加 左h。
    完。
    返还 1 加 右h。
完。

rb_阴阳统计 接受 节点：
    若 节点 等于 空：
        返还 一组：0、0。
    完。
    左 为 做 rb_阴阳统计 用 做 rb_左 用 节点。
    右 为 做 rb_阴阳统计 用 做 rb_右 用 节点。
    可变 红 为 左 的 第 0 项 加 右 的 第 0 项。
    可变 黑 为 左 的 第 1 项 加 右 的 第 1 项。
    若 做 rb_颜色 用 节点 等于 RB_红：
        红 为 红 加 1。
    否则：
        黑 为 黑 加 1。
    完。
    返还 一组：红、黑。
完。

rb_阴阳比 接受 树：
    统计 为 做 rb_阴阳统计 用 做 rb_根 用 树。
    返还 统计。
完。

rb_阴阳五律验证 接受 树：
    rb_金计 为 rb_金计 加 1。
    根 为 做 rb_根 用 树。
    若 根 等于 空：
        返还 1。
    完。
    若 做 rb_颜色 用 根 不等于 RB_黑：
        返还 0。
    完。
    若 做 rb_检查阳律 用 根 等于 0：
        返还 0。
    完。
    若 做 rb_检查阴高 用 根 等于 负 1：
        返还 0。
    完。
    返还 1。
完。

rb_检查阳律 接受 节点：
    若 节点 等于 空：
        返还 1。
    完。
    若 做 rb_是红 用 节点 等于 1：
        若 做 rb_是红 用 做 rb_左 用 节点 等于 1：
            返还 0。
        完。
        若 做 rb_是红 用 做 rb_右 用 节点 等于 1：
            返还 0。
        完。
    完。
    若 做 rb_检查阳律 用 做 rb_左 用 节点 等于 0：
        返还 0。
    完。
    返还 做 rb_检查阳律 用 做 rb_右 用 节点。
完。

rb_检查阴高 接受 节点：
    若 节点 等于 空：
        返还 0。
    完。
    左h 为 做 rb_检查阴高 用 做 rb_左 用 节点。
    右h 为 做 rb_检查阴高 用 做 rb_右 用 节点。
    若 左h 等于 负 1 或者 右h 等于 负 1：
        返还 负 1。
    完。
    若 左h 不等于 右h：
        返还 负 1。
    完。
    若 做 rb_颜色 用 节点 等于 RB_黑：
        返还 左h 加 1。
    完。
    返还 左h。
完。

rb_五行报告 接受 零参：
    返还 一组：rb_木计、rb_火计、rb_土计、rb_金计、rb_水计。
完。

rb_五行重置 接受 零参：
    rb_木计 为 0。
    rb_火计 为 0。
    rb_土计 为 0。
    rb_金计 为 0。
    rb_水计 为 0。
完。

rb_棵转串 接受 树：
    返还 做 rb_中序 用 树。
完。

rb_气韵黑高 接受 树：
    返还 做 rb_检查阴高 用 做 rb_根 用 树。
完。

本章 收束。
