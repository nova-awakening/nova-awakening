《字典树》

引用《..·基础》。

trie_创建节点 接受 零参：
    返还 一组：做 字典、0、空、0。
完。

字典树_trie_创建 接受 零参：
    返还 一组：做 trie_创建节点、0。
完。

trie_根 接受 t：
    返还 t 的 第 0 项。
完。

trie_词数 接受 t：
    返还 t 的 第 1 项。
完。

字典树_trie_插入 接受 t、键、值：
    可变 节点 为 做 trie_根 用 t。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 键：
        c 为 做 字符 用 做 字节取 用 键、i。
        节点 的 第 3 项 为 节点 的 第 3 项 加 1。
        可变 子 为 做 字典取 用 节点 的 第 0 项、c。
        若 子 等于 空：
            子 为 做 trie_创建节点。
            做 字典设 用 节点 的 第 0 项、c、子。
        完。
        节点 为 子。
        i 为 i 加 1。
    完。
    节点 的 第 3 项 为 节点 的 第 3 项 加 1。
    若 节点 的 第 1 项 等于 0：
        t 的 第 1 项 为 t 的 第 1 项 加 1。
    完。
    节点 的 第 1 项 为 1。
    节点 的 第 2 项 为 值。
完。

trie_查找 接受 t、键：
    可变 节点 为 做 trie_根 用 t。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 键：
        c 为 做 字符 用 做 字节取 用 键、i。
        可变 子 为 做 字典取 用 节点 的 第 0 项、c。
        若 子 等于 空：
            返还 空。
        完。
        节点 为 子。
        i 为 i 加 1。
    完。
    若 节点 的 第 1 项 等于 1：
        返还 节点 的 第 2 项。
    完。
    返还 空。
完。

trie_包含 接受 t、键：
    返还 做 trie_查找 用 t、键 不等于 空。
完。

trie_有前缀 接受 t、前缀：
    可变 节点 为 做 trie_根 用 t。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 前缀：
        c 为 做 字符 用 做 字节取 用 前缀、i。
        可变 子 为 做 字典取 用 节点 的 第 0 项、c。
        若 子 等于 空：
            返还 0。
        完。
        节点 为 子。
        i 为 i 加 1。
    完。
    返还 1。
完。

trie_前缀计数 接受 t、前缀：
    可变 节点 为 做 trie_根 用 t。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 前缀：
        c 为 做 字符 用 做 字节取 用 前缀、i。
        可变 子 为 做 字典取 用 节点 的 第 0 项、c。
        若 子 等于 空：
            返还 0。
        完。
        节点 为 子。
        i 为 i 加 1。
    完。
    返还 节点 的 第 3 项。
完。

字典树_trie_大小 接受 t：
    返还 做 trie_大小递归 用 做 trie_根 用 t。
完。

trie_大小递归 接受 节点：
    可变 总 为 0。
    若 节点 的 第 1 项 等于 1：
        总 为 1。
    完。
    键列 为 做 字典键列表 用 节点 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        总 为 总 加 做 trie_大小递归 用 做 字典取 用 节点 的 第 0 项、键列 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

trie_气韵路径 接受 t、前缀：
    可变 节点 为 做 trie_根 用 t。
    可变 流量 为 一组：（空）。
    做 追加 用 流量、节点 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 前缀：
        c 为 做 字符 用 做 字节取 用 前缀、i。
        可变 子 为 做 字典取 用 节点 的 第 0 项、c。
        若 子 等于 空：
            返还 流量。
        完。
        节点 为 子。
        做 追加 用 流量、节点 的 第 3 项。
        i 为 i 加 1。
    完。
    返还 流量。
完。

trie_气韵衰减 接受 t、前缀：
    流量 为 做 trie_气韵路径 用 t、前缀。
    若 做 计数 用 流量 小于 2：
        返还 0。
    完。
    起始 为 流量 的 第 0 项。
    若 做 计数 用 流量 等于 0：
        返还 空。
    完。
    末端 为 流量 的 第 做 计数 用 流量 减 1 项。
    若 起始 等于 0：
        返还 100。
    完。
    返还 (起始 减 末端) 乘 100 除以 起始。
完。

trie_最大深度 接受 t：
    返还 做 trie_深度递归 用 做 trie_根 用 t、0。
完。

trie_深度递归 接受 节点、深度：
    可变 最大 为 0。
    若 节点 的 第 1 项 等于 1：
        最大 为 深度。
    完。
    键列 为 做 字典键列表 用 节点 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        子深 为 做 trie_深度递归 用 做 字典取 用 节点 的 第 0 项、键列 的 第 i 项、深度 加 1。
        若 子深 大于 最大：
            最大 为 子深。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

trie_前缀节点 接受 t、前缀：
    可变 节点 为 做 trie_根 用 t。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 前缀：
        c 为 做 字符 用 做 字节取 用 前缀、i。
        可变 子 为 做 字典取 用 节点 的 第 0 项、c。
        若 子 等于 空：
            返还 空。
        完。
        节点 为 子。
        i 为 i 加 1。
    完。
    返还 节点。
完。

trie_收集词 接受 节点、前缀、结果：
    若 节点 的 第 1 项 等于 1：
        做 追加 用 结果、一组：前缀、节点 的 第 2 项。
    完。
    键列 为 做 字典键列表 用 节点 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        子键 为 键列 的 第 i 项。
        子节点 为 做 字典取 用 节点 的 第 0 项、子键。
        做 trie_收集词 用 子节点、前缀 加 子键、结果。
        i 为 i 加 1。
    完。
完。

trie_前缀枚举 接受 t、前缀：
    节点 为 做 trie_前缀节点 用 t、前缀。
    若 节点 等于 空：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    做 trie_收集词 用 节点、前缀、结果。
    返还 结果。
完。

trie_自动补全 接受 t、前缀、最大数：
    全部 为 做 trie_前缀枚举 用 t、前缀。
    若 做 计数 用 全部 小于或等于 最大数：
        返还 全部。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大数：
        做 追加 用 结果、全部 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

trie_删除 接受 t、键：
    可变 路径 为 一组：（空）。
    可变 节点 为 做 trie_根 用 t。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 键：
        c 为 做 字符 用 做 字节取 用 键、i。
        可变 子 为 做 字典取 用 节点 的 第 0 项、c。
        若 子 等于 空：
            返还 假。
        完。
        做 追加 用 路径、一组：节点、c。
        节点 为 子。
        i 为 i 加 1。
    完。
    若 节点 的 第 1 项 不等于 1：
        返还 假。
    完。
    节点 的 第 1 项 为 0。
    节点 的 第 2 项 为 空。
    t 的 第 1 项 为 t 的 第 1 项 减 1。
    i 为 做 计数 用 路径 减 1。
    只要 i 大于或等于 0：
        路径 的 第 i 项 的 第 0 项 的 第 3 项 为 路径 的 第 i 项 的 第 0 项 的 第 3 项 减 1。
        i 为 i 减 1。
    完。
    返还 真。
完。

trie_模糊搜索 接受 t、查询、最大距离：
    可变 结果 为 一组：（空）。
    做 trie_模糊递归 用 做 trie_根 用 t、「」、查询、0、最大距离、结果。
    返还 结果。
完。

trie_模糊递归 接受 节点、当前词、目标、当前距离、最大距离、结果：
    可变 新距离 为 0。
    若 当前距离 大于 最大距离：
        返还 空。
    完。
    目标长 为 做 字节长度 用 目标。
    当前长 为 做 字节长度 用 当前词。
    若 节点 的 第 1 项 等于 1：
        可变 剩余 为 目标长 减 当前长。
        若 剩余 小于 0：
            剩余 为 0 减 剩余。
        完。
        若 当前距离 加 剩余 小于或等于 最大距离：
            做 追加 用 结果、一组：当前词、节点 的 第 2 项、当前距离 加 剩余。
        完。
    完。
    键列 为 做 字典键列表 用 节点 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        子键 为 键列 的 第 i 项。
        子节点 为 做 字典取 用 节点 的 第 0 项、子键。
        若 当前长 小于 目标长：
            期望字符 为 做 字符 用 做 字节取 用 目标、当前长。
            若 子键 不等于 期望字符：
                新距离 为 新距离 加 1。
            完。
        否则：
            新距离 为 新距离 加 1。
        完。
        做 trie_模糊递归 用 子节点、当前词 加 子键、目标、新距离、最大距离、结果。
        i 为 i 加 1。
    完。
完。

trie_最长公共前缀 接受 t：
    可变 节点 为 做 trie_根 用 t。
    可变 前缀 为 「」。
    只要 真：
        键列 为 做 字典键列表 用 节点 的 第 0 项。
        若 做 计数 用 键列 不等于 1：
            返还 前缀。
        完。
        若 节点 的 第 1 项 等于 1：
            返还 前缀。
        完。
        唯一键 为 键列 的 第 0 项。
        前缀 为 前缀 加 唯一键。
        节点 为 做 字典取 用 节点 的 第 0 项、唯一键。
    完。
    返还 前缀。
完。

字典树_trie_批量插入 接受 t、键值对列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 键值对列表：
        做 字典树_trie_插入 用 t、键值对列表 的 第 i 项 的 第 0 项、键值对列表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

trie_批量查询 接受 t、键列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        做 追加 用 结果、做 trie_查找 用 t、键列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

trie_批量删除 接受 t、键列表：
    可变 删除数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        若 做 trie_删除 用 t、键列表 的 第 i 项：
            删除数 为 删除数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 删除数。
完。

trie_节点计数 接受 节点：
    可变 总 为 1。
    键列 为 做 字典键列表 用 节点 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        总 为 总 加 做 trie_节点计数 用 做 字典取 用 节点 的 第 0 项、键列 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

trie_总节点数 接受 t：
    返还 做 trie_节点计数 用 做 trie_根 用 t。
完。

trie_内存估算 接受 t：
    返还 做 trie_总节点数 用 t 乘 48。
完。

trie_前缀词数 接受 t、前缀：
    节点 为 做 trie_前缀节点 用 t、前缀。
    若 节点 等于 空：
        返还 0。
    完。
    返还 节点 的 第 3 项。
完。

可变 _trie_插入数 为 0。

可变 _trie_查询数 为 0。

可变 _trie_模糊数 为 0。

trie_记录插入 接受 零参：
    _trie_插入数 为 _trie_插入数 加 1。
完。

trie_记录查询 接受 零参：
    _trie_查询数 为 _trie_查询数 加 1。
完。

trie_记录模糊 接受 零参：
    _trie_模糊数 为 _trie_模糊数 加 1。
完。

本章 收束。
