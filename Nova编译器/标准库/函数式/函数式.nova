《函数式》

引用《..·基础》。

映射 接受 列表、变换：
    n 为 做 计数 用 列表。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、做 变换 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

过滤 接受 列表、条件：
    n 为 做 计数 用 列表。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 条件 用 列表 的 第 i 项：
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

归约 接受 列表、初始值、操作：
    n 为 做 计数 用 列表。
    可变 累积 为 初始值。
    可变 i 为 0。
    只要 i 小于 n：
        累积 为 做 操作 用 累积、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 累积。
完。

右归约 接受 列表、初始值、操作：
    n 为 做 计数 用 列表。
    可变 累积 为 初始值。
    可变 i 为 n 减 1。
    只要 i 大于或等于 0：
        累积 为 做 操作 用 列表 的 第 i 项、累积。
        i 为 i 减 1。
    完。
    返还 累积。
完。

扁平化 接受 嵌套列表：
    n 为 做 计数 用 嵌套列表。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        子列表 为 嵌套列表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 子列表：
            做 追加 用 结果、子列表 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

平面映射 接受 列表、变换：
    返还 做 扁平化 用 做 映射 用 列表、变换。
完。

拉链 接受 列表a、列表b、_扩3：
    na 为 做 计数 用 列表a。
    nb 为 做 计数 用 列表b。
    可变 n 为 na。
    若 nb 小于 n：
        n 为 nb。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、一组：列表a 的 第 i 项、列表b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

带索引 接受 列表：
    n 为 做 计数 用 列表。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、一组：i、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

全部 接受 列表、条件：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 条件 用 列表 的 第 i 项 等于 假：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

任一 接受 列表、条件：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 条件 用 列表 的 第 i 项：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

无一 接受 列表、条件：
    返还 做 任一 用 列表、条件 等于 假。
完。

查找元素 接受 列表、条件：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 条件 用 列表 的 第 i 项：
            返还 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

查找索引 接受 列表、条件：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 条件 用 列表 的 第 i 项：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

函数式_分区 接受 列表、条件：
    可变 真部 为 一组：（空）。
    可变 假部 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 条件 用 列表 的 第 i 项：
            做 追加 用 真部、列表 的 第 i 项。
        否则：
            做 追加 用 假部、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：真部、假部。
完。

取前 接受 列表、n：
    可变 结果 为 一组：（空）。
    可变 限 为 n。
    若 限 大于 做 计数 用 列表：
        限 为 做 计数 用 列表。
    完。
    可变 i 为 0。
    只要 i 小于 限：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

跳过 接受 列表、n：
    可变 结果 为 一组：（空）。
    可变 i 为 n。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

分块 接受 列表、大小：
    n 为 做 计数 用 列表。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 块 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 大小 并且 i 加 j 小于 n：
            做 追加 用 块、列表 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、块。
        i 为 i 加 大小。
    完。
    返还 结果。
完。

滑动窗口 接受 列表、大小：
    n 为 做 计数 用 列表。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 n 减 大小：
        可变 窗口 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 大小：
            做 追加 用 窗口、列表 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、窗口。
        i 为 i 加 1。
    完。
    返还 结果。
完。

范围 接受 起、终、_扩3：
    可变 结果 为 一组：（空）。
    可变 i 为 起。
    只要 i 小于 终：
        做 追加 用 结果、i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

范围步进 接受 起、终、步：
    可变 结果 为 一组：（空）。
    可变 i 为 起。
    只要 i 小于 终：
        做 追加 用 结果、i。
        i 为 i 加 步。
    完。
    返还 结果。
完。

列表最大 接受 列表：
    可变 最大 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 大于 最大：
            最大 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

列表最小 接受 列表：
    可变 最小 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 最小：
            最小 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小。
完。

列表求和 接受 列表：
    可变 结果 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        结果 为 结果 加 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表求积 接受 列表：
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        结果 为 结果 乘 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

映射带索引 接受 列表、变换：
    n 为 做 计数 用 列表。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、做 变换 用 i、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

分组 接受 列表、键函数：
    可变 组 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        键 为 做 转字符串 用 做 键函数 用 列表 的 第 i 项。
        现有 为 做 字典取 用 组、键。
        若 现有 等于 空：
            做 字典设 用 组、键、一组：列表 的 第 i 项。
        否则：
            做 追加 用 现有、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 组。
完。

去重 接受 列表：
    可变 已见 为 做 字典。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        键 为 做 转字符串 用 列表 的 第 i 项。
        若 做 字典取 用 已见、键 等于 空：
            做 字典设 用 已见、键、1。
            做 追加 用 结果、列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

笛卡尔积 接受 列表a、列表b：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表a：
        可变 j 为 0。
        只要 j 小于 做 计数 用 列表b：
            做 追加 用 结果、一组：列表a 的 第 i 项、列表b 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

扫描 接受 列表、初始值、操作：
    n 为 做 计数 用 列表。
    可变 结果 为 一组：初始值。
    可变 累积 为 初始值。
    可变 i 为 0。
    只要 i 小于 n：
        累积 为 做 操作 用 累积、列表 的 第 i 项。
        做 追加 用 结果、累积。
        i 为 i 加 1。
    完。
    返还 结果。
完。

取当 接受 列表、条件：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 条件 用 列表 的 第 i 项 等于 假：
            返还 结果。
        完。
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

跳当 接受 列表、条件：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表 并且 做 条件 用 列表 的 第 i 项：
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

反转列表 接受 列表：
    n 为 做 计数 用 列表。
    可变 结果 为 一组：（空）。
    可变 i 为 n 减 1。
    只要 i 大于或等于 0：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 结果。
完。

计数条件 接受 列表、条件：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 做 条件 用 列表 的 第 i 项：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

组合调用 接受 f、g、x：
    返还 做 f 用 做 g 用 x。
完。

重复生成 接受 次数、生成器：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 次数：
        做 追加 用 结果、做 生成器 用 i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

恒等 接受 x：
    返还 x。
完。

翻转 接受 列表：
    可变 结果 为 一组：（空）。
    可变 i 为 做 计数 用 列表 减 1。
    只要 i 大于或等于 0：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 结果。
完。

展平 接受 嵌套列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 嵌套列表：
        可变 j 为 0。
        只要 j 小于 做 计数 用 嵌套列表 的 第 i 项：
            做 追加 用 结果、嵌套列表 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _fp_映射次数 为 0。

可变 _fp_过滤次数 为 0。

可变 _fp_折叠次数 为 0。

fp_记录映射 接受 零参：
    _fp_映射次数 为 _fp_映射次数 加 1。
完。

fp_记录过滤 接受 零参：
    _fp_过滤次数 为 _fp_过滤次数 加 1。
完。

fp_记录折叠 接受 零参：
    _fp_折叠次数 为 _fp_折叠次数 加 1。
完。

fp_风格评估 接受 零参：
    总 为 _fp_映射次数 加 _fp_过滤次数 加 _fp_折叠次数。
    若 总 等于 0：
        返还 「未使用函数式操作」。
    完。
    若 _fp_映射次数 大于 总 除以 2：
        返还 「映射密集型」。
    完。
    若 _fp_过滤次数 大于 总 除以 2：
        返还 「过滤密集型」。
    完。
    返还 「均衡函数式风格」。
完。

本章 收束。
