《量化》

引用《..·基础》。

引用《..·数学·基础·数学库》。

INT8_求范围 接受 数据：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        绝对值 为 做 绝对值 用 数据 的 第 i 项。
        若 绝对值 大于 最大：
            最大 为 绝对值。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

INT8_量化 接受 数据：
    可变 范围 为 做 INT8_求范围 用 数据。
    若 范围 等于 0：
        范围 为 1。
    完。
    可变 量化值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 q 为 数据 的 第 i 项 乘 127 除以 范围。
        若 q 大于 127：
            q 为 127。
        完。
        若 q 小于 负 127：
            q 为 负 127。
        完。
        做 追加 用 量化值、q。
        i 为 i 加 1。
    完。
    返还 一组：量化值、范围。
完。

INT8_反量化 接受 量化结果：
    量化值 为 量化结果 的 第 0 项。
    r 为 量化结果 的 第 1 项。
    返还 做 映射 用 量化值、以 q 映射到 q 乘 r 除以 127。
完。

INT4_量化 接受 数据：
    可变 范围 为 做 INT8_求范围 用 数据。
    若 范围 等于 0：
        范围 为 1。
    完。
    可变 量化值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 q 为 数据 的 第 i 项 乘 7 除以 范围。
        若 q 大于 7：
            q 为 7。
        完。
        若 q 小于 负 7：
            q 为 负 7。
        完。
        做 追加 用 量化值、q。
        i 为 i 加 1。
    完。
    返还 一组：量化值、范围。
完。

INT4_反量化 接受 量化结果：
    量化值 为 量化结果 的 第 0 项。
    r 为 量化结果 的 第 1 项。
    返还 做 映射 用 量化值、以 q 映射到 q 乘 r 除以 7。
完。

量化_MSE 接受 原始、恢复：
    若 做 计数 用 原始 等于 0：
        返还 0。
    完。
    返还 做 折叠 用 做 拉链 用 原始、恢复、以 a、b 映射到 (a 减 b) 乘 (a 减 b)、0、以 s、d 映射到 s 加 d 除以 做 计数 用 原始。
完。

量化_最大误差 接受 原始、恢复：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 原始：
        d 为 做 绝对值 用 原始 的 第 i 项 减 恢复 的 第 i 项。
        若 d 大于 最大：
            最大 为 d。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

校准_MinMax 接受 校准数据列表：
    可变 全局最小 为 0x7FFFFFFF。
    可变 全局最大 为 0 减 0x7FFFFFFF。
    可变 i 为 0。
    只要 i 小于 做 计数 用 校准数据列表：
        批次 为 校准数据列表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 批次：
            若 批次 的 第 j 项 小于 全局最小：
                全局最小 为 批次 的 第 j 项。
            完。
            若 批次 的 第 j 项 大于 全局最大：
                全局最大 为 批次 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：全局最小、全局最大。
完。

非对称_量化 接受 数据、最小、最大：
    可变 范围 为 最大 减 最小。
    若 范围 等于 0：
        范围 为 1。
    完。
    可变 量化值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 q 为 (数据 的 第 i 项 减 最小) 乘 255 除以 范围。
        若 q 大于 255：
            q 为 255。
        完。
        若 q 小于 0：
            q 为 0。
        完。
        做 追加 用 量化值、q。
        i 为 i 加 1。
    完。
    返还 一组：量化值、最小、范围。
完。

非对称_反量化 接受 量化结果：
    量化值 为 量化结果 的 第 0 项。
    零点 为 量化结果 的 第 1 项。
    可变 范围 为 量化结果 的 第 2 项。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 量化值：
        做 追加 用 结果、量化值 的 第 i 项 乘 范围 除以 255 加 零点。
        i 为 i 加 1。
    完。
    返还 结果。
完。

块量化_INT8 接受 数据、块大小：
    可变 所有量化 为 一组：（空）。
    可变 所有范围 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 块 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 块大小 并且 i 加 j 小于 做 计数 用 数据：
            做 追加 用 块、数据 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        qr 为 做 INT8_量化 用 块。
        可变 k 为 0。
        只要 k 小于 做 计数 用 qr 的 第 0 项：
            做 追加 用 所有量化、qr 的 第 0 项 的 第 k 项。
            k 为 k 加 1。
        完。
        做 追加 用 所有范围、qr 的 第 1 项。
        i 为 i 加 块大小。
    完。
    返还 一组：所有量化、所有范围。
完。

按通道_INT8量化 接受 通道数据列表：
    返还 做 映射 用 通道数据列表、以 ch 映射到 做 INT8_量化 用 ch。
完。

动态_INT8量化 接受 数据：
    返还 做 INT8_量化 用 数据。
完。

QAT_伪量化 接受 数据、位宽：
    若 位宽 等于 8：
        qr 为 做 INT8_量化 用 数据。
        返还 做 INT8_反量化 用 qr。
    完。
    若 位宽 等于 4：
        qr4 为 做 INT4_量化 用 数据。
        返还 做 INT4_反量化 用 qr4。
    完。
    返还 数据。
完。

量化_SNR 接受 原始、恢复：
    信号能 为 做 折叠 用 原始、0、以 s、x 映射到 s 加 x 乘 x 除以 1000。
    噪声能 为 做 折叠 用 做 拉链 用 原始、恢复、以 a、b 映射到 (a 减 b) 乘 (a 减 b) 除以 1000、0、以 s、d 映射到 s 加 d。
    若 噪声能 等于 0：
        返还 9999。
    完。
    返还 信号能 乘 100 除以 噪声能。
完。

本章 收束。
