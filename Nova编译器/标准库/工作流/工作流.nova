《工作流》

引用《..·基础》。

引用《..·并发·线程·线程池》。

任务_待定 为 0。

任务_运行 为 1。

任务_完成 为 2。

任务_失败 为 3。

可变 任务列表 为 一组：（空）。

工作流_任务_创建 接受 id、最大重试：
    做 追加 用 任务列表、一组：id、任务_待定、一组：（空）、0、最大重试。
    返还 做 计数 用 任务列表 减 1。
完。

工作流_任务_添加依赖 接受 任务idx、依赖idx：
    做 追加 用 任务列表 的 第 任务idx 项 的 第 2 项、依赖idx。
完。

任务_状态 接受 索引：
    返还 任务列表 的 第 索引 项 的 第 1 项。
完。

任务_设状态 接受 索引、s：
    任务列表 的 第 索引 项 的 第 1 项 为 s。
完。

任务_设结果 接受 索引、r：
    任务列表 的 第 索引 项 的 第 3 项 为 r。
完。

任务_结果 接受 索引：
    返还 任务列表 的 第 索引 项 的 第 3 项。
完。

依赖_完成 接受 任务idx：
    依赖 为 任务列表 的 第 任务idx 项 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 依赖：
        若 任务列表 的 第 依赖 的 第 i 项 项 的 第 1 项 不等于 任务_完成：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

获取就绪任务 接受 零参：
    可变 就绪 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务列表：
        若 任务列表 的 第 i 项 的 第 1 项 等于 任务_待定 并且 做 依赖_完成 用 i：
            做 追加 用 就绪、i。
        完。
        i 为 i 加 1。
    完。
    返还 就绪。
完。

任务_重试 接受 索引：
    若 任务列表 的 第 索引 项 的 第 4 项 小于 任务列表 的 第 索引 项 的 第 5 项：
        任务列表 的 第 索引 项 的 第 4 项 为 任务列表 的 第 索引 项 的 第 4 项 加 1。
        任务列表 的 第 索引 项 的 第 1 项 为 任务_待定。
        返还 真。
    完。
    返还 假。
完。

工作流_管道_创建 接受 任务数、最大重试：
    可变 起始 为 做 计数 用 任务列表。
    可变 i 为 0。
    只要 i 小于 任务数：
        做 工作流_任务_创建 用 起始 加 i、最大重试。
        若 i 大于 0：
            做 工作流_任务_添加依赖 用 起始 加 i、起始 加 i 减 1。
        完。
        i 为 i 加 1。
    完。
    返还 起始。
完。

工作流_全部完成 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务列表：
        若 任务列表 的 第 i 项 的 第 1 项 不等于 任务_完成：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

工作流_拓扑排序 接受 零参：
    n 为 做 计数 用 任务列表。
    可变 入度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 入度、做 计数 用 任务列表 的 第 i 项 的 第 2 项。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    可变 已处理 为 0。
    只要 已处理 小于 n：
        可变 找到 为 负 1。
        i 为 0。
        只要 i 小于 n：
            若 入度 的 第 i 项 等于 0：
                可变 已在结果 为 假。
                可变 k 为 0。
                只要 k 小于 做 计数 用 结果：
                    若 结果 的 第 k 项 等于 i：
                        已在结果 为 真。
                    完。
                    k 为 k 加 1。
                完。
                若 已在结果 等于 假：
                    找到 为 i。
                    i 为 n。
                完。
            完。
            i 为 i 加 1。
        完。
        若 找到 等于 负 1：
            返还 结果。
        完。
        做 追加 用 结果、找到。
        入度 的 第 找到 项 为 负 1。
        i 为 0。
        只要 i 小于 n：
            可变 j 为 0。
            只要 j 小于 做 计数 用 任务列表 的 第 i 项 的 第 2 项：
                若 任务列表 的 第 i 项 的 第 2 项 的 第 j 项 等于 找到：
                    入度 的 第 i 项 为 入度 的 第 i 项 减 1。
                完。
                j 为 j 加 1。
            完。
            i 为 i 加 1。
        完。
        已处理 为 已处理 加 1。
    完。
    返还 结果。
完。

任务_清空 接受 零参：
    任务列表 为 一组：（空）。
完。

有失败任务 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务列表：
        若 任务列表 的 第 i 项 的 第 1 项 等于 任务_失败：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

任务_进度 接受 零参：
    若 做 计数 用 任务列表 等于 0：
        返还 0。
    完。
    可变 完成 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务列表：
        若 任务列表 的 第 i 项 的 第 1 项 等于 任务_完成：
            完成 为 完成 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 完成 乘 100 除以 做 计数 用 任务列表。
完。

训练工作流_创建 接受 零参：
    做 任务_清空。
    数据加载 为 做 工作流_任务_创建 用 0、3。
    预处理 为 做 工作流_任务_创建 用 1、2。
    训练 为 做 工作流_任务_创建 用 2、1。
    评估 为 做 工作流_任务_创建 用 3、1。
    保存 为 做 工作流_任务_创建 用 4、2。
    部署 为 做 工作流_任务_创建 用 5、1。
    做 工作流_任务_添加依赖 用 预处理、数据加载。
    做 工作流_任务_添加依赖 用 训练、预处理。
    做 工作流_任务_添加依赖 用 评估、训练。
    做 工作流_任务_添加依赖 用 保存、评估。
    做 工作流_任务_添加依赖 用 部署、保存。
    返还 一组：数据加载、预处理、训练、评估、保存、部署。
完。

并行_创建 接受 任务数、汇合任务idx、最大重试：
    可变 分支 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 任务数：
        索引 为 做 工作流_任务_创建 用 1000 加 i、最大重试。
        做 追加 用 分支、索引。
        若 汇合任务idx 大于或等于 0：
            做 工作流_任务_添加依赖 用 汇合任务idx、索引。
        完。
        i 为 i 加 1。
    完。
    返还 分支。
完。

工作流_统计 接受 零参：
    可变 s0 为 0。
    可变 s1 为 0。
    可变 s2 为 0。
    可变 s3 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务列表：
        若 任务列表 的 第 i 项 的 第 1 项 等于 任务_待定：
            s0 为 s0 加 1。
        完。
        若 任务列表 的 第 i 项 的 第 1 项 等于 任务_运行：
            s1 为 s1 加 1。
        完。
        若 任务列表 的 第 i 项 的 第 1 项 等于 任务_完成：
            s2 为 s2 加 1。
        完。
        若 任务列表 的 第 i 项 的 第 1 项 等于 任务_失败：
            s3 为 s3 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：s0、s1、s2、s3。
完。

工作流_报告 接受 零参：
    stats 为 做 工作流_统计。
    做 打印 用 「╔═══ 工作流状态 ═══╗」。
    做 打印 用 「║ 总任务: 」 加 做 转字符串 用 做 计数 用 任务列表。
    做 打印 用 「║ 未定: 」 加 做 转字符串 用 stats 的 第 0 项 加 「 运行: 」 加 做 转字符串 用 stats 的 第 1 项 加 「 完成: 」 加 做 转字符串 用 stats 的 第 2 项 加 「 失败: 」 加 做 转字符串 用 stats 的 第 3 项。
    做 打印 用 「║ 进度: 」 加 做 转字符串 用 做 任务_进度 加 「%」。
    若 stats 的 第 3 项 大于 0：
        做 打印 用 「║ ⚠ 有失败任务!」。
    完。
    做 打印 用 「╚══════════════════╝」。
完。

任务_状态名 接受 状态：
    若 状态 等于 0：
        返还 「未定」。
    完。
    若 状态 等于 1：
        返还 「运行」。
    完。
    若 状态 等于 2：
        返还 「完成」。
    完。
    若 状态 等于 3：
        返还 「失败」。
    完。
    返还 「未知」。
完。

可变 _依赖_前 为 一组：（空）。

可变 _依赖_后 为 一组：（空）。

任务_可执行 接受 任务id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _依赖_后：
        若 _依赖_后 的 第 i 项 等于 任务id：
            前id 为 _依赖_前 的 第 i 项。
            若 任务列表 的 第 前id 项 的 第 1 项 不等于 2：
                返还 假。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

工作流_就绪任务 接受 零参：
    可变 就绪 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务列表：
        若 任务列表 的 第 i 项 的 第 1 项 等于 0 并且 做 任务_可执行 用 i：
            做 追加 用 就绪、i。
        完。
        i 为 i 加 1。
    完。
    返还 就绪。
完。

本章 收束。
