《训练管线》

引用《..·基础》。

数据加载器_创建 接受 数据集、批大小：
    返还 一组：数据集、批大小、0。
完。

数据加载器_下一批 接受 loader：
    数据 为 loader 的 第 0 项。
    bs 为 loader 的 第 1 项。
    位置 为 loader 的 第 2 项。
    n 为 做 计数 用 数据。
    若 位置 大于或等于 n：
        loader 的 第 2 项 为 0。
        返还 一组：（空）。
    完。
    可变 批 为 一组：（空）。
    可变 i 为 位置。
    只要 i 小于 位置 加 bs 并且 i 小于 n：
        做 追加 用 批、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    loader 的 第 2 项 为 i。
    返还 批。
完。

数据加载器_重置 接受 loader：
    loader 的 第 2 项 为 0。
完。

数据加载器_epoch完成 接受 loader：
    返还 loader 的 第 2 项 大于或等于 做 计数 用 loader 的 第 0 项。
完。

数据加载器_批数 接受 loader：
    n 为 做 计数 用 loader 的 第 0 项。
    bs 为 loader 的 第 1 项。
    若 bs 等于 0：
        返还 0。
    完。
    返还 (n 加 bs 减 1) 除以 bs。
完。

训练_随机态 为 54321。

训练_随机 接受 零参：
    训练_随机态 为 训练_随机态 位异或 训练_随机态 左移 13。
    训练_随机态 为 训练_随机态 位异或 训练_随机态 右移 7。
    训练_随机态 为 训练_随机态 位异或 训练_随机态 左移 17。
    若 训练_随机态 小于 0：
        训练_随机态 为 0 减 训练_随机态。
    完。
    返还 训练_随机态。
完。

数据_洗牌 接受 数据：
    n 为 做 计数 用 数据。
    可变 i 为 n 减 1。
    只要 i 大于 0：
        j 为 做 训练_随机 取余于 (i 加 1)。
        临时 为 数据 的 第 i 项。
        数据 的 第 i 项 为 数据 的 第 j 项。
        数据 的 第 j 项 为 临时。
        i 为 i 减 1。
    完。
完。

指标_准确率 接受 预测、标签：
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测：
        若 预测 的 第 i 项 等于 标签 的 第 i 项：
            正确 为 正确 加 1。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 预测 等于 0：
        返还 0。
    完。
    返还 正确 乘 100 除以 做 计数 用 预测。
完。

指标_精确率 接受 预测、标签、正类：
    可变 TP 为 0。
    可变 FP 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测：
        若 预测 的 第 i 项 等于 正类：
            若 标签 的 第 i 项 等于 正类：
                TP 为 TP 加 1。
            否则：
                FP 为 FP 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    若 TP 加 FP 等于 0：
        返还 0。
    完。
    返还 TP 乘 100 除以 (TP 加 FP)。
完。

指标_召回率 接受 预测、标签、正类：
    可变 TP 为 0。
    可变 FN 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测：
        若 标签 的 第 i 项 等于 正类：
            若 预测 的 第 i 项 等于 正类：
                TP 为 TP 加 1。
            否则：
                FN 为 FN 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    若 TP 加 FN 等于 0：
        返还 0。
    完。
    返还 TP 乘 100 除以 (TP 加 FN)。
完。

指标_F1 接受 精确、召回：
    若 精确 加 召回 等于 0：
        返还 0。
    完。
    返还 2 乘 精确 乘 召回 除以 (精确 加 召回)。
完。

指标_MSE 接受 预测、标签：
    若 做 计数 用 预测 等于 0：
        返还 0。
    完。
    返还 做 折叠 用 做 拉链 用 预测、标签、以 a、b 映射到 (a 减 b) 乘 (a 减 b)、0、以 s、d 映射到 s 加 d 除以 做 计数 用 预测。
完。

早停_创建 接受 耐心：
    返还 一组：0x7FFFFFFF、耐心、0、耐心。
完。

管线_早停_检查 接受 es、当前损失：
    若 当前损失 小于 es 的 第 0 项：
        es 的 第 0 项 为 当前损失。
        es 的 第 2 项 为 0。
        返还 假。
    完。
    es 的 第 2 项 为 es 的 第 2 项 加 1。
    返还 es 的 第 2 项 大于或等于 es 的 第 3 项。
完。

早停_最佳 接受 es：
    返还 es 的 第 0 项。
完。

管线_EMA_创建 接受 参数列表、衰减：
    可变 影子 为 做 复制列表 用 参数列表。
    返还 一组：影子、衰减。
完。

管线_EMA_更新 接受 ema、参数列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数列表：
        ema 的 第 0 项 的 第 i 项 为 (ema 的 第 1 项 乘 ema 的 第 0 项 的 第 i 项 加 (100 减 ema 的 第 1 项) 乘 参数列表 的 第 i 项) 除以 100。
        i 为 i 加 1。
    完。
完。

EMA_获取 接受 ema：
    返还 ema 的 第 0 项。
完。

可变 训练日志_metric 为 一组：（空）。

管线_训练日志 接受 轮次、损失值、metric：
    做 追加 用 训练日志_epoch、轮次。
    做 追加 用 训练日志_loss、损失值。
    做 追加 用 训练日志_metric、metric。
完。

训练日志_最近 接受 n：
    总数 为 做 计数 用 训练日志_epoch。
    可变 起始 为 总数 减 n。
    若 起始 小于 0：
        起始 为 0。
    完。
    可变 结果 为 一组：（空）。
    只要 起始 小于 总数：
        做 追加 用 结果、一组：训练日志_epoch 的 第 起始 项、训练日志_loss 的 第 起始 项、训练日志_metric 的 第 起始 项。
        起始 为 起始 加 1。
    完。
    返还 结果。
完。

梯度累积_合并 接受 梯度列表、累积步数：
    若 做 计数 用 梯度列表 等于 0：
        返还 一组：（空）。
    完。
    维度 为 做 计数 用 梯度列表 的 第 0 项。
    可变 合并 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 合并、做 折叠 用 梯度列表、0、以 s、x 映射到 s 加 x 的 第 d 项 除以 累积步数。
        d 为 d 加 1。
    完。
    返还 合并。
完。

训练管线_学习率_预热余弦 接受 步长、预热步、总步、基础lr：
    若 步长 小于 预热步：
        返还 基础lr 乘 步长 除以 预热步。
    完。
    进度 为 (步长 减 预热步) 乘 100 除以 (总步 减 预热步)。
    余弦向量 为 100 减 2 乘 进度 乘 进度 除以 100。
    返还 基础lr 乘 (100 加 余弦向量) 除以 200。
完。

管线_梯度裁剪 接受 梯度、最大范数：
    范数 为 做 整数sqrt 用 做 折叠 用 梯度、0、以 s、g 映射到 s 加 g 乘 g。
    若 范数 小于或等于 最大范数：
        返还 梯度。
    完。
    返还 做 映射 用 梯度、以 g 映射到 g 乘 最大范数 除以 范数。
完。

可变 训练_检查点数据 为 一组：（空）。

可变 检查点_epoch 为 负 1。

检查点_保存 接受 参数、轮次：
    训练_检查点数据 为 做 复制列表 用 参数。
    检查点_epoch 为 轮次。
完。

检查点_加载 接受 零参：
    返还 训练_检查点数据。
完。

检查点_epoch获取 接受 零参：
    返还 检查点_epoch。
完。

训练_循环 接受 总epochs、训练函数、验证函数：
    可变 历史 为 一组：（空）。
    可变 e 为 0。
    只要 e 小于 总epochs：
        训练损失 为 做 训练函数 用 e。
        验证损失 为 做 验证函数 用 e。
        做 追加 用 历史、一组：e、训练损失、验证损失。
        e 为 e 加 1。
    完。
    返还 历史。
完。

训练_过拟合检测 接受 历史、窗口：
    n 为 做 计数 用 历史。
    若 n 小于 窗口 加 1：
        返还 假。
    完。
    可变 验证上升 为 0。
    可变 i 为 n 减 窗口。
    只要 i 小于 n：
        若 历史 的 第 i 项 的 第 2 项 大于 历史 的 第 i 减 1 项 的 第 2 项：
            验证上升 为 验证上升 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 验证上升 大于 窗口 除以 2。
完。

本章 收束。
