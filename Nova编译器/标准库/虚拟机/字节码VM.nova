《字节码VM》

引用《..·基础》。

VM_指令_空操作 为 0。

VM_指令_压栈 为 1。

VM_指令_弹栈 为 2。

VM_指令_加 为 3。

VM_指令_减 为 4。

VM_指令_乘 为 5。

VM_指令_除 为 6。

指令_取余 为 7。

VM_指令_取负 为 8。

指令_等于 为 10。

指令_不等 为 11。

指令_小于 为 12。

指令_大于 为 13。

指令_小于等于 为 14。

指令_大于等于 为 15。

VM_指令_跳转 为 20。

指令_零跳 为 21。

指令_非零跳 为 22。

VM_指令_加载 为 30。

VM_指令_存储 为 31。

VM_指令_调用 为 40。

VM_指令_返回 为 41。

指令_复制栈顶 为 50。

VM_指令_交换 为 51。

VM_指令_打印 为 60。

VM_指令_停机 为 99。

vm_创建 接受 代码：
    可变 栈 为 一组：（空）。
    可变 si 为 0。
    只要 si 小于 256：
        做 追加 用 栈、0。
        si 为 si 加 1。
    完。
    可变 局部 为 一组：（空）。
    si 为 0。
    只要 si 小于 256：
        做 追加 用 局部、0。
        si 为 si 加 1。
    完。
    返还 一组：代码、栈、局部、0、一组：（空）、1。
完。

vm_推 接受 vm、值：
    sp 为 vm 的 第 4 项。
    vm 的 第 1 项 的 第 sp 项 为 值。
    vm 的 第 4 项 为 sp 加 1。
完。

vm_弹 接受 vm：
    vm 的 第 4 项 为 vm 的 第 4 项 减 1。
    返还 vm 的 第 1 项 的 第 vm 的 第 4 项 项。
完。

vm_顶 接受 vm：
    返还 vm 的 第 1 项 的 第 vm 的 第 4 项 减 1 项。
完。

vm_取指令 接受 vm：
    代码 为 vm 的 第 0 项。
    pc 为 vm 的 第 3 项。
    若 pc 大于或等于 做 计数 用 代码：
        返还 VM_指令_停机。
    完。
    指令 为 代码 的 第 pc 项。
    vm 的 第 3 项 为 pc 加 1。
    返还 指令。
完。

vm_空操作 接受 vm：
    返还 vm。
完。

vm_处理_OP_SWAP 接受 vm：
    a 为 做 vm_弹 用 vm。
    b 为 做 vm_弹 用 vm。
    做 vm_推 用 vm、a。
    做 vm_推 用 vm、b。
完。

vm_处理_OP_DIV 接受 vm：
    b 为 做 vm_弹 用 vm。
    a 为 做 vm_弹 用 vm。
    若 b 不等于 0：
        做 vm_推 用 vm、a 除以 b。
    否则：
        做 vm_推 用 vm、0。
    完。
完。

vm_处理_OP_MOD 接受 vm：
    b 为 做 vm_弹 用 vm。
    a 为 做 vm_弹 用 vm。
    若 b 不等于 0：
        做 vm_推 用 vm、a 取余于 b。
    否则：
        做 vm_推 用 vm、0。
    完。
完。

vm_处理_OP_RET 接受 vm：
    若 做 计数 用 vm 的 第 5 项 大于 0：
        帧 为 vm 的 第 5 项 的 第 做 计数 用 vm 的 第 5 项 减 1 项。
        做 删除 用 vm 的 第 5 项、做 计数 用 vm 的 第 5 项 减 1。
        vm 的 第 3 项 为 帧 的 第 0 项。
    否则：
        vm 的 第 6 项 为 0。
    完。
完。

vm_运行 接受 vm：
    只要 vm 的 第 6 项 等于 1：
        op 为 做 vm_取指令 用 vm。
        若 op 等于 VM_指令_停机：
            vm 的 第 6 项 为 0。
        否则若 op 等于 VM_指令_空操作：
            做 vm_空操作 用 vm。
        否则若 op 等于 VM_指令_压栈：
            数值 为 做 vm_取指令 用 vm。
            做 vm_推 用 vm、数值。
        否则若 op 等于 VM_指令_弹栈：
            做 vm_弹 用 vm。
        否则若 op 等于 指令_复制栈顶：
            做 vm_推 用 vm、做 vm_顶 用 vm。
        否则若 op 等于 VM_指令_交换：
            做 vm_处理_OP_SWAP 用 vm。
        否则若 op 等于 VM_指令_加：
            b 为 做 vm_弹 用 vm。
            a 为 做 vm_弹 用 vm。
            做 vm_推 用 vm、a 加 b。
        否则若 op 等于 VM_指令_减：
            b 为 做 vm_弹 用 vm。
            a 为 做 vm_弹 用 vm。
            做 vm_推 用 vm、a 减 b。
        否则若 op 等于 VM_指令_乘：
            b 为 做 vm_弹 用 vm。
            a 为 做 vm_弹 用 vm。
            做 vm_推 用 vm、a 乘 b。
        否则若 op 等于 VM_指令_除：
            做 vm_处理_OP_DIV 用 vm。
        否则若 op 等于 指令_取余：
            做 vm_处理_OP_MOD 用 vm。
        否则若 op 等于 VM_指令_取负：
            做 vm_推 用 vm、0 减 做 vm_弹 用 vm。
        否则若 op 等于 指令_等于：
            b 为 做 vm_弹 用 vm。
            a 为 做 vm_弹 用 vm。
            若 a 等于 b：
                做 vm_推 用 vm、1。
            否则：
                做 vm_推 用 vm、0。
            完。
        否则若 op 等于 指令_不等：
            b 为 做 vm_弹 用 vm。
            a 为 做 vm_弹 用 vm。
            若 a 不等于 b：
                做 vm_推 用 vm、1。
            否则：
                做 vm_推 用 vm、0。
            完。
        否则若 op 等于 指令_小于：
            b 为 做 vm_弹 用 vm。
            a 为 做 vm_弹 用 vm。
            若 a 小于 b：
                做 vm_推 用 vm、1。
            否则：
                做 vm_推 用 vm、0。
            完。
        否则若 op 等于 指令_大于：
            b 为 做 vm_弹 用 vm。
            a 为 做 vm_弹 用 vm。
            若 a 大于 b：
                做 vm_推 用 vm、1。
            否则：
                做 vm_推 用 vm、0。
            完。
        否则若 op 等于 VM_指令_跳转：
            vm 的 第 3 项 为 做 vm_取指令 用 vm。
        否则若 op 等于 指令_零跳：
            目标 为 做 vm_取指令 用 vm。
            若 做 vm_弹 用 vm 等于 0：
                vm 的 第 3 项 为 目标。
            完。
        否则若 op 等于 指令_非零跳：
            目标 为 做 vm_取指令 用 vm。
            若 做 vm_弹 用 vm 不等于 0：
                vm 的 第 3 项 为 目标。
            完。
        否则若 op 等于 VM_指令_加载：
            索引 为 做 vm_取指令 用 vm。
            做 vm_推 用 vm、vm 的 第 2 项 的 第 索引 项。
        否则若 op 等于 VM_指令_存储：
            索引 为 做 vm_取指令 用 vm。
            vm 的 第 2 项 的 第 索引 项 为 做 vm_弹 用 vm。
        否则若 op 等于 VM_指令_调用：
            目标 为 做 vm_取指令 用 vm。
            做 追加 用 vm 的 第 5 项、一组：vm 的 第 3 项、vm 的 第 4 项。
            vm 的 第 3 项 为 目标。
        否则若 op 等于 VM_指令_返回：
            做 vm_处理_OP_RET 用 vm。
        否则若 op 等于 VM_指令_打印：
            做 打印 用 做 vm_弹 用 vm。
        完。
    完。
    返还 vm 的 第 4 项。
完。

虚拟机_汇编 接受 操作码列表：
    返还 操作码列表。
完。

vm_运行代码 接受 代码：
    机器 为 做 vm_创建 用 代码。
    做 vm_运行 用 机器。
    若 机器 的 第 4 项 大于 0：
        返还 做 vm_顶 用 机器。
    完。
    返还 0。
完。

vm_阴阳分类 接受 代码：
    可变 中性 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 代码：
        op 为 代码 的 第 i 项。
        若 op 等于 VM_指令_压栈：
            阳数 为 阳数 加 1。
            i 为 i 加 2。
        否则若 op 等于 VM_指令_弹栈 或者 op 等于 VM_指令_打印：
            阴数 为 阴数 加 1。
            i 为 i 加 1。
        否则若 op 等于 VM_指令_加载 或者 op 等于 VM_指令_存储：
            阳数 为 阳数 加 1。
            i 为 i 加 2。
        否则若 op 等于 VM_指令_跳转 或者 op 等于 指令_零跳 或者 op 等于 指令_非零跳 或者 op 等于 VM_指令_调用：
            中性 为 中性 加 1。
            i 为 i 加 2。
        否则：
            中性 为 中性 加 1。
            i 为 i 加 1。
        完。
    完。
    返还 一组：阳数、阴数、中性。
完。

vm_自指验证 接受 代码：
    n 为 做 计数 用 代码。
    可变 i 为 0。
    只要 i 小于 n：
        op 为 代码 的 第 i 项。
        若 op 小于 0 或者 op 大于 VM_指令_停机：
            返还 0。
        完。
        若 op 等于 VM_指令_压栈 或者 op 等于 VM_指令_跳转 或者 op 等于 指令_零跳 或者 op 等于 指令_非零跳 或者 op 等于 VM_指令_加载 或者 op 等于 VM_指令_存储 或者 op 等于 VM_指令_调用：
            若 i 加 1 大于或等于 n：
                返还 0。
            完。
            i 为 i 加 2。
        否则：
            i 为 i 加 1。
        完。
    完。
    返还 1。
完。

本章 收束。
