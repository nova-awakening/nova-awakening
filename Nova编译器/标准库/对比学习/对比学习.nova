《对比学习》

引用《..·基础》。

引用《..·算法·数学扩展》。

对比_余弦 接受 a、b：
    n 为 做 计数 用 a。
    可变 点积 为 0。
    可变 范数a 为 0。
    可变 范数b 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 做 计数 用 b：
            点积 为 点积 加 a 的 第 i 项 乘 b 的 第 i 项 除以 1000。
            范数a 为 范数a 加 a 的 第 i 项 乘 a 的 第 i 项 除以 1000。
            范数b 为 范数b 加 b 的 第 i 项 乘 b 的 第 i 项 除以 1000。
        完。
        i 为 i 加 1。
    完。
    分母 为 做 整数sqrt 用 范数a 乘 做 整数sqrt 用 范数b 除以 1000。
    若 分母 等于 0：
        返还 0。
    完。
    返还 点积 乘 1000 除以 分母。
完。

对比_L2归一化 接受 向量：
    n 为 做 计数 用 向量。
    可变 范数 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        范数 为 范数 加 向量 的 第 i 项 乘 向量 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    范数 为 做 整数sqrt 用 范数 乘 1000。
    若 范数 等于 0：
        返还 向量。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、向量 的 第 i 项 乘 1000 除以 范数。
        i 为 i 加 1。
    完。
    返还 结果。
完。

信息噪声对比 接受 查询、正键、负键列表、τ：
    若 τ 小于或等于 0：
        τ 为 70。
    完。
    正相似 为 做 对比_余弦 用 查询、正键 乘 1000 除以 τ。
    可变 正exp 为 1000 加 正相似。
    若 正exp 小于 1：
        正exp 为 1。
    完。
    可变 总exp 为 正exp。
    可变 i 为 0。
    只要 i 小于 做 计数 用 负键列表：
        负相似 为 做 对比_余弦 用 查询、负键列表 的 第 i 项 乘 1000 除以 τ。
        可变 负exp 为 1000 加 负相似。
        若 负exp 小于 1：
            负exp 为 1。
        完。
        总exp 为 总exp 加 负exp。
        i 为 i 加 1。
    完。
    若 总exp 等于 0：
        总exp 为 1。
    完。
    损失值 为 总exp 乘 1000 除以 正exp。
    返还 损失值。
完。

归一温度交叉熵 接受 嵌入列表、batch_size、τ：
    若 τ 小于或等于 0：
        τ 为 500。
    完。
    可变 总损失 为 0。
    N 为 batch_size 乘 2。
    可变 i 为 0。
    只要 i 小于 batch_size：
        正相似 为 做 对比_余弦 用 嵌入列表 的 第 i 项、嵌入列表 的 第 i 加 batch_size 项 乘 1000 除以 τ。
        可变 正exp 为 1000 加 正相似。
        若 正exp 小于 1：
            正exp 为 1。
        完。
        可变 总exp 为 0。
        可变 j 为 0。
        只要 j 小于 N：
            若 j 不等于 i：
                相似度 为 做 对比_余弦 用 嵌入列表 的 第 i 项、嵌入列表 的 第 j 项 乘 1000 除以 τ。
                可变 e 为 1000 加 相似度。
                若 e 小于 1：
                    e 为 1。
                完。
                总exp 为 总exp 加 e。
            完。
            j 为 j 加 1。
        完。
        若 总exp 等于 0：
            总exp 为 1。
        完。
        总损失 为 总损失 加 总exp 乘 1000 除以 正exp。
        i 为 i 加 1。
    完。
    若 batch_size 等于 0：
        返还 0。
    完。
    返还 总损失 除以 batch_size。
完。

CLIP_损失 接受 图像嵌入列表、文本嵌入列表、τ：
    若 τ 小于或等于 0：
        τ 为 70。
    完。
    N 为 做 计数 用 图像嵌入列表。
    可变 损失 为 0。
    可变 i 为 0。
    只要 i 小于 N：
        正sim 为 做 对比_余弦 用 图像嵌入列表 的 第 i 项、文本嵌入列表 的 第 i 项 乘 1000 除以 τ。
        可变 正e 为 1000 加 正sim。
        若 正e 小于 1：
            正e 为 1。
        完。
        可变 总e 为 0。
        可变 j 为 0。
        只要 j 小于 N：
            相似度 为 做 对比_余弦 用 图像嵌入列表 的 第 i 项、文本嵌入列表 的 第 j 项 乘 1000 除以 τ。
            可变 e 为 1000 加 相似度。
            若 e 小于 1：
                e 为 1。
            完。
            总e 为 总e 加 e。
            j 为 j 加 1。
        完。
        若 总e 等于 0：
            总e 为 1。
        完。
        损失 为 损失 加 总e 乘 1000 除以 正e。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 N：
        正sim 为 做 对比_余弦 用 文本嵌入列表 的 第 i 项、图像嵌入列表 的 第 i 项 乘 1000 除以 τ。
        可变 正e 为 1000 加 正sim。
        若 正e 小于 1：
            正e 为 1。
        完。
        可变 总e 为 0。
        可变 j 为 0。
        只要 j 小于 N：
            相似度 为 做 对比_余弦 用 文本嵌入列表 的 第 i 项、图像嵌入列表 的 第 j 项 乘 1000 除以 τ。
            可变 e 为 1000 加 相似度。
            若 e 小于 1：
                e 为 1。
            完。
            总e 为 总e 加 e。
            j 为 j 加 1。
        完。
        若 总e 等于 0：
            总e 为 1。
        完。
        损失 为 损失 加 总e 乘 1000 除以 正e。
        i 为 i 加 1。
    完。
    返还 损失 除以 (N 乘 2)。
完。

MoCo_动量更新 接受 编码器参数、动量编码器参数、m：
    n 为 做 计数 用 编码器参数。
    可变 i 为 0。
    只要 i 小于 n：
        动量编码器参数 的 第 i 项 为 m 乘 动量编码器参数 的 第 i 项 除以 1000 加 (1000 减 m) 乘 编码器参数 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
完。

MoCo_队列_创建 接受 大小：
    返还 一组：一组：（空）、大小、0。
完。

MoCo_入队 接受 队列、键：
    若 做 计数 用 队列 的 第 0 项 大于或等于 队列 的 第 1 项：
        可变 新列表 为 一组：（空）。
        可变 i 为 1。
        只要 i 小于 做 计数 用 队列 的 第 0 项：
            做 追加 用 新列表、队列 的 第 0 项 的 第 i 项。
            i 为 i 加 1。
        完。
        队列 的 第 0 项 为 新列表。
    完。
    做 追加 用 队列 的 第 0 项、键。
完。

BYOL_损失 接受 在线投影、目标投影：
    在线_归一 为 做 对比_L2归一化 用 在线投影。
    目标_归一 为 做 对比_L2归一化 用 目标投影。
    相似度 为 做 对比_余弦 用 在线_归一、目标_归一。
    返还 2000 减 2 乘 相似度。
完。

BarlowTwins_交叉相关 接受 嵌入A列表、嵌入B列表、维度：
    N 为 做 计数 用 嵌入A列表。
    若 N 等于 0：
        返还 一组：（空）。
    完。
    可变 C 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 维度：
            可变 数值 为 0。
            可变 n 为 0。
            只要 n 小于 N：
                若 i 小于 做 计数 用 嵌入A列表 的 第 n 项 并且 j 小于 做 计数 用 嵌入B列表 的 第 n 项：
                    数值 为 数值 加 嵌入A列表 的 第 n 项 的 第 i 项 乘 嵌入B列表 的 第 n 项 的 第 j 项 除以 1000。
                完。
                n 为 n 加 1。
            完。
            做 追加 用 行、数值 除以 N。
            j 为 j 加 1。
        完。
        做 追加 用 C、行。
        i 为 i 加 1。
    完。
    返还 C。
完。

BarlowTwins_损失 接受 C、维度、λ：
    若 λ 小于或等于 0：
        λ 为 5。
    完。
    可变 损失 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        可变 j 为 0。
        只要 j 小于 维度：
            若 i 等于 j：
                差值 为 1000 减 C 的 第 i 项 的 第 j 项。
                损失 为 损失 加 差值 乘 差值 除以 1000。
            否则：
                损失 为 损失 加 λ 乘 C 的 第 i 项 的 第 j 项 乘 C 的 第 i 项 的 第 j 项 除以 (1000 乘 1000)。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 损失。
完。

VICReg_方差损失 接受 嵌入列表、维度：
    N 为 做 计数 用 嵌入列表。
    若 N 小于或等于 1：
        返还 0。
    完。
    可变 损失 为 0。
    可变 d 为 0。
    只要 d 小于 维度：
        可变 和 为 0。
        可变 i 为 0。
        只要 i 小于 N：
            若 d 小于 做 计数 用 嵌入列表 的 第 i 项：
                和 为 和 加 嵌入列表 的 第 i 项 的 第 d 项。
            完。
            i 为 i 加 1。
        完。
        均值 为 和 除以 N。
        可变 方差 为 0。
        i 为 0。
        只要 i 小于 N：
            可变 v 为 0。
            若 d 小于 做 计数 用 嵌入列表 的 第 i 项：
                v 为 嵌入列表 的 第 i 项 的 第 d 项。
            完。
            差值 为 v 减 均值。
            方差 为 方差 加 差值 乘 差值 除以 1000。
            i 为 i 加 1。
        完。
        若 N 等于 0：
            返还 0。
        完。
        方差 为 方差 除以 N。
        标准差 为 做 整数sqrt 用 方差 乘 1000。
        可变 铰链 为 1000 减 标准差。
        若 铰链 小于 0：
            铰链 为 0。
        完。
        损失 为 损失 加 铰链。
        d 为 d 加 1。
    完。
    若 维度 等于 0：
        返还 0。
    完。
    返还 损失 除以 维度。
完。

投影头_前向 接受 输入、W1、b1、W2、b2、隐藏维度、输出维度、输入维度：
    可变 隐藏 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 隐藏维度：
        可变 数值 为 b1 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 输入维度：
            数值 为 数值 加 W1 的 第 i 乘 输入维度 加 j 项 乘 输入 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        若 数值 小于 0：
            数值 为 0。
        完。
        做 追加 用 隐藏、数值。
        i 为 i 加 1。
    完。
    可变 输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 输出维度：
        可变 数值 为 b2 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 隐藏维度：
            数值 为 数值 加 W2 的 第 i 乘 隐藏维度 加 j 项 乘 隐藏 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        做 追加 用 输出、数值。
        i 为 i 加 1。
    完。
    返还 输出。
完。

硬负样本_选择 接受 锚点、候选列表、k：
    n 为 做 计数 用 候选列表。
    可变 相似度 为 一组：（空）。
    可变 索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 相似度、做 对比_余弦 用 锚点、候选列表 的 第 i 项。
        做 追加 用 索引、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 k 并且 i 小于 n：
        可变 最大idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 相似度 的 第 索引 的 第 j 项 项 大于 相似度 的 第 索引 的 第 最大idx 项 项：
                最大idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最大idx 不等于 i：
            临时 为 索引 的 第 i 项。
            索引 的 第 i 项 为 索引 的 第 最大idx 项。
            索引 的 第 最大idx 项 为 临时。
        完。
        i 为 i 加 1。
    完。
    可变 选中 为 一组：（空）。
    i 为 0。
    只要 i 小于 k 并且 i 小于 n：
        做 追加 用 选中、候选列表 的 第 索引 的 第 i 项 项。
        i 为 i 加 1。
    完。
    返还 选中。
完。

本章 收束。
