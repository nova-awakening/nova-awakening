《关系数据库》

引用《..·..·基础》。

列_整数 为 1。

列_文本 为 2。

列_布尔 为 3。

列_创建 接受 名称、类型、可空、默认值：
    返还 一组：名称、类型、可空、默认值、假、假。
完。

列_设主键 接受 列：
    列 的 第 4 项 为 真。
    列 的 第 2 项 为 假。
    列 的 第 5 项 为 真。
完。

列_设唯一 接受 列：
    列 的 第 5 项 为 真。
完。

表_创建 接受 名称、列定义列表：
    返还 一组：名称、列定义列表、一组：（空）、1、做 字典、0。
完。

表_列数 接受 表：
    返还 做 计数 用 表 的 第 1 项。
完。

表_行数 接受 表：
    返还 表 的 第 5 项。
完。

表_名称 接受 表：
    返还 表 的 第 0 项。
完。

表_列索引 接受 表、列名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 表 的 第 1 项：
        若 做 字符串等于 用 表 的 第 1 项 的 第 i 项 的 第 0 项、列名：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

表_插入 接受 表、值列表：
    列数 为 做 计数 用 表 的 第 1 项。
    可变 实际值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 列数：
        若 i 小于 做 计数 用 值列表 并且 值列表 的 第 i 项 不等于 空：
            做 追加 用 实际值、值列表 的 第 i 项。
        否则若 表 的 第 1 项 的 第 i 项 的 第 3 项 不等于 空：
            做 追加 用 实际值、表 的 第 1 项 的 第 i 项 的 第 3 项。
        否则若 表 的 第 1 项 的 第 i 项 的 第 2 项：
            做 追加 用 实际值、空。
        否则：
            返还 一组：「错误」、「非空列缺少值」。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 列数：
        若 表 的 第 1 项 的 第 i 项 的 第 5 项 并且 实际值 的 第 i 项 不等于 空：
            可变 j 为 0。
            只要 j 小于 做 计数 用 表 的 第 2 项：
                若 表 的 第 2 项 的 第 j 项 的 第 1 项 的 第 i 项 等于 实际值 的 第 i 项：
                    返还 一组：「错误」、「唯一约束冲突」。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    行ID 为 表 的 第 3 项。
    表 的 第 3 项 为 表 的 第 3 项 加 1。
    做 追加 用 表 的 第 2 项、一组：行ID、实际值。
    表 的 第 5 项 为 表 的 第 5 项 加 1。
    返还 一组：「成功」、行ID。
完。

表_删除条件 接受 表、条件函数：
    可变 新行 为 一组：（空）。
    可变 删除数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 表 的 第 2 项：
        若 做 条件函数 用 表 的 第 2 项 的 第 i 项 的 第 1 项：
            删除数 为 删除数 加 1。
        否则：
            做 追加 用 新行、表 的 第 2 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    表 的 第 2 项 为 新行。
    表 的 第 5 项 为 表 的 第 5 项 减 删除数。
    返还 删除数。
完。

表_更新条件 接受 表、条件函数、更新函数：
    可变 更新数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 表 的 第 2 项：
        若 做 条件函数 用 表 的 第 2 项 的 第 i 项 的 第 1 项：
            表 的 第 2 项 的 第 i 项 的 第 1 项 为 做 更新函数 用 表 的 第 2 项 的 第 i 项 的 第 1 项。
            更新数 为 更新数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 更新数。
完。

表_查询条件 接受 表、条件函数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 表 的 第 2 项：
        若 做 条件函数 用 表 的 第 2 项 的 第 i 项 的 第 1 项：
            做 追加 用 结果、表 的 第 2 项 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

表_全部 接受 表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 表 的 第 2 项：
        做 追加 用 结果、表 的 第 2 项 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

SQL_关键字 为 1。

SQL_标识符 为 2。

SQL_数字 为 3。

SQL_字符串 为 4。

SQL_符号 为 5。

sql_是空白 接受 c：
    若 做 字符串等于 用 c、「 」：
        返还 真。
    完。
    若 做 字符串等于 用 c、「\t」：
        返还 真。
    完。
    若 做 字符串等于 用 c、「\n」：
        返还 真。
    完。
    返还 假。
完。

sql_是符号 接受 c：
    若 做 字符串等于 用 c、「(」：
        返还 真。
    完。
    若 做 字符串等于 用 c、「)」：
        返还 真。
    完。
    若 做 字符串等于 用 c、「,」：
        返还 真。
    完。
    若 做 字符串等于 用 c、「;」：
        返还 真。
    完。
    若 做 字符串等于 用 c、「=」：
        返还 真。
    完。
    若 做 字符串等于 用 c、「*」：
        返还 真。
    完。
    若 做 字符串等于 用 c、「>」：
        返还 真。
    完。
    若 做 字符串等于 用 c、「<」：
        返还 真。
    完。
    若 做 字符串等于 用 c、「!」：
        返还 真。
    完。
    返还 假。
完。

sql_是双字符操作起始 接受 c：
    若 做 字符串等于 用 c、「>」：
        返还 真。
    完。
    若 做 字符串等于 用 c、「<」：
        返还 真。
    完。
    若 做 字符串等于 用 c、「!」：
        返还 真。
    完。
    返还 假。
完。

sql_是数字码 接受 c：
    返还 c 大于或等于 字符_零 并且 c 小于或等于 字符_九。
完。

sql_是标识起始码 接受 c：
    若 c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z：
        返还 真。
    完。
    若 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z：
        返还 真。
    完。
    若 c 等于 字符_下划线：
        返还 真。
    完。
    若 c 大于 127：
        返还 真。
    完。
    返还 假。
完。

sql_是标识续字符码 接受 c：
    若 做 sql_是标识起始码 用 c：
        返还 真。
    完。
    若 做 sql_是数字码 用 c：
        返还 真。
    完。
    返还 假。
完。

sql_是条件终止词 接受 词：
    若 做 字符串等于 用 词、「ORDER」：
        返还 真。
    完。
    若 做 字符串等于 用 词、「LIMIT」：
        返还 真。
    完。
    若 做 字符串等于 用 词、「GROUP」：
        返还 真。
    完。
    返还 假。
完。

sql_词法 接受 文本：
    n 为 做 计数 用 文本。
    可变 词列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 文本、i、1。
        若 做 sql_是空白 用 c：
            i 为 i 加 1。
            续。
        完。
        若 做 sql_是符号 用 c：
            若 i 加 1 小于 n：
                c2 为 做 子串 用 文本、i 加 1、1。
                若 做 sql_是双字符操作起始 用 c 并且 做 字符串等于 用 c2、「=」：
                    做 追加 用 词列表、一组：SQL_符号、做 字符串拼接 用 c、c2。
                    i 为 i 加 2。
                    续。
                完。
            完。
            做 追加 用 词列表、一组：SQL_符号、c。
            i 为 i 加 1。
            续。
        完。
        若 做 字符串等于 用 c、「'」：
            可变 s 为 「」。
            i 为 i 加 1。
            可变 _引号找到 为 假。
            只要 i 小于 n 并且 _引号找到 等于 假：
                若 做 子串 用 文本、i、1 等于 「'」：
                    _引号找到 为 真。
                否则：
                    s 为 做 字符串拼接 用 s、做 子串 用 文本、i、1。
                    i 为 i 加 1。
                完。
            完。
            i 为 i 加 1。
            做 追加 用 词列表、一组：SQL_字符串、s。
            续。
        完。
        cc 为 做 编码 用 c。
        若 做 sql_是数字码 用 cc：
            可变 数目 为 0。
            只要 i 小于 n：
                d 为 做 编码 用 做 子串 用 文本、i、1。
                若 做 sql_是数字码 用 d：
                    数目 为 数目 乘 10 加 (d 减 48)。
                    i 为 i 加 1。
                否则：
                    止。
                完。
            完。
            做 追加 用 词列表、一组：SQL_数字、数目。
            续。
        完。
        若 做 sql_是标识起始码 用 cc：
            可变 id 为 「」。
            只要 i 小于 n：
                ic 为 做 编码 用 做 子串 用 文本、i、1。
                若 做 sql_是标识续字符码 用 ic：
                    id 为 做 字符串拼接 用 id、做 子串 用 文本、i、1。
                    i 为 i 加 1。
                否则：
                    止。
                完。
            完。
            上 为 做 sql_转大写 用 id。
            若 做 sql_是关键字 用 上：
                做 追加 用 词列表、一组：SQL_关键字、上。
            否则：
                做 追加 用 词列表、一组：SQL_标识符、id。
            完。
            续。
        完。
        i 为 i 加 1。
    完。
    返还 词列表。
完。

sql_是关键字 接受 词：
    关键字列表 为 一组：「SELECT」、「FROM」、「WHERE」、「INSERT」、「INTO」、「VALUES」、「UPDATE」、「SET」、「DELETE」、「CREATE」、「TABLE」、「AND」、「OR」、「NOT」、「ORDER」、「BY」、「ASC」、「DESC」、「LIMIT」、「INTEGER」、「TEXT」、「BOOLEAN」、「PRIMARY」、「KEY」、「UNIQUE」、「NULL」、「DEFAULT」、「DROP」、「ALTER」、「ADD」、「JOIN」、「ON」、「LEFT」、「RIGHT」、「INNER」、「GROUP」、「HAVING」、「COUNT」、「SUM」、「AVG」、「MIN」、「MAX」、「AS」、「DISTINCT」、「IN」、「LIKE」、「BETWEEN」、「IS」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 关键字列表：
        若 做 字符串等于 用 关键字列表 的 第 i 项、词：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

sql_转大写 接受 s：
    n 为 做 计数 用 s。
    可变 r 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 编码 用 做 子串 用 s、i、1。
        若 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z：
            r 为 做 字符串拼接 用 r、做 子串 用 「ABCDEFGHIJKLMNOPQRSTUVWXYZ」、c 减 97、1。
        否则：
            r 为 做 字符串拼接 用 r、做 子串 用 s、i、1。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

数据库_创建 接受 名称：
    返还 一组：名称、做 字典、0。
完。

数据库_创建表 接受 db、表名、列定义列表：
    若 做 字典取 用 db 的 第 1 项、表名 不等于 空：
        返还 一组：「错误」、「表已存在」。
    完。
    表 为 做 表_创建 用 表名、列定义列表。
    做 字典设 用 db 的 第 1 项、表名、表。
    db 的 第 2 项 为 db 的 第 2 项 加 1。
    返还 一组：「成功」、表名。
完。

数据库_删除表 接受 db、表名：
    若 做 字典取 用 db 的 第 1 项、表名 等于 空：
        返还 一组：「错误」、「表不存在」。
    完。
    做 字典设 用 db 的 第 1 项、表名、空。
    db 的 第 2 项 为 db 的 第 2 项 加 1。
    返还 一组：「成功」、表名。
完。

数据库_取表 接受 db、表名：
    返还 做 字典取 用 db 的 第 1 项、表名。
完。

sql_执行 接受 db、sql文本：
    词列表 为 做 sql_词法 用 sql文本。
    若 做 计数 用 词列表 等于 0：
        返还 一组：「错误」、「空语句」。
    完。
    首词 为 词列表 的 第 0 项 的 第 1 项。
    若 做 字符串等于 用 首词、「SELECT」：
        返还 做 sql_执行SELECT 用 db、词列表。
    完。
    若 做 字符串等于 用 首词、「INSERT」：
        返还 做 sql_执行INSERT 用 db、词列表。
    完。
    若 做 字符串等于 用 首词、「DELETE」：
        返还 做 sql_执行DELETE 用 db、词列表。
    完。
    若 做 字符串等于 用 首词、「CREATE」：
        返还 做 sql_执行CREATE 用 db、词列表。
    完。
    若 做 字符串等于 用 首词、「UPDATE」：
        返还 做 sql_执行UPDATE 用 db、词列表。
    完。
    若 做 字符串等于 用 首词、「DROP」：
        返还 做 sql_执行DROP 用 db、词列表。
    完。
    返还 一组：「错误」、「不支持的SQL语句」。
完。

sql_执行CREATE 接受 db、词列表：
    若 做 计数 用 词列表 小于 4：
        返还 一组：「错误」、「语法错误」。
    完。
    表名 为 词列表 的 第 2 项 的 第 1 项。
    可变 列列表 为 一组：（空）。
    可变 i 为 4。
    只要 i 小于 做 计数 用 词列表：
        若 词列表 的 第 i 项 的 第 0 项 等于 SQL_标识符：
            列名 为 词列表 的 第 i 项 的 第 1 项。
            可变 类型 为 列_文本。
            若 i 加 1 小于 做 计数 用 词列表：
                类型词 为 词列表 的 第 i 加 1 项 的 第 1 项。
                若 做 字符串等于 用 类型词、「INTEGER」：
                    类型 为 列_整数。
                否则若 做 字符串等于 用 类型词、「BOOLEAN」：
                    类型 为 列_布尔。
                完。
                i 为 i 加 1。
            完。
            列 为 做 列_创建 用 列名、类型、真、空。
            若 i 加 1 小于 做 计数 用 词列表 并且 做 字符串等于 用 词列表 的 第 i 加 1 项 的 第 1 项、「PRIMARY」：
                做 列_设主键 用 列。
                i 为 i 加 2。
            完。
            若 i 加 1 小于 做 计数 用 词列表 并且 做 字符串等于 用 词列表 的 第 i 加 1 项 的 第 1 项、「UNIQUE」：
                做 列_设唯一 用 列。
                i 为 i 加 1。
            完。
            做 追加 用 列列表、列。
        完。
        i 为 i 加 1。
    完。
    返还 做 数据库_创建表 用 db、表名、列列表。
完。

sql_执行INSERT 接受 db、词列表：
    若 做 计数 用 词列表 小于 5：
        返还 一组：「错误」、「语法错误」。
    完。
    表名 为 词列表 的 第 2 项 的 第 1 项。
    表 为 做 数据库_取表 用 db、表名。
    若 表 等于 空：
        返还 一组：「错误」、「表不存在」。
    完。
    可变 值列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词列表：
        若 词列表 的 第 i 项 的 第 0 项 等于 SQL_关键字 并且 做 字符串等于 用 词列表 的 第 i 项 的 第 1 项、「VALUES」：
            i 为 i 加 1。
            止。
        完。
        i 为 i 加 1。
    完。
    若 i 小于 做 计数 用 词列表 并且 做 字符串等于 用 词列表 的 第 i 项 的 第 1 项、「(」：
        i 为 i 加 1。
    完。
    可变 _值完 为 假。
    只要 i 小于 做 计数 用 词列表 并且 _值完 等于 假：
        若 词列表 的 第 i 项 的 第 1 项 等于 「)」 或者 词列表 的 第 i 项 的 第 1 项 等于 「;」：
            _值完 为 真。
        否则：
            若 词列表 的 第 i 项 的 第 0 项 等于 SQL_数字：
                做 追加 用 值列表、词列表 的 第 i 项 的 第 1 项。
            否则若 词列表 的 第 i 项 的 第 0 项 等于 SQL_字符串：
                做 追加 用 值列表、词列表 的 第 i 项 的 第 1 项。
            否则若 词列表 的 第 i 项 的 第 0 项 等于 SQL_关键字 并且 做 字符串等于 用 词列表 的 第 i 项 的 第 1 项、「NULL」：
                做 追加 用 值列表、空。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 做 表_插入 用 表、值列表。
完。

sql_执行SELECT 接受 db、词列表：
    可变 选择列 为 一组：（空）。
    可变 表名 为 「」。
    可变 i 为 1。
    可变 全选 为 假。
    只要 i 小于 做 计数 用 词列表：
        若 词列表 的 第 i 项 的 第 0 项 等于 SQL_关键字 并且 做 字符串等于 用 词列表 的 第 i 项 的 第 1 项、「FROM」：
            i 为 i 加 1。
            止。
        完。
        若 做 字符串等于 用 词列表 的 第 i 项 的 第 1 项、「*」：
            全选 为 真。
        否则若 词列表 的 第 i 项 的 第 0 项 等于 SQL_标识符：
            做 追加 用 选择列、词列表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    若 i 小于 做 计数 用 词列表：
        表名 为 词列表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    表 为 做 数据库_取表 用 db、表名。
    若 表 等于 空：
        返还 一组：「错误」、「表不存在」。
    完。
    可变 行列表 为 做 表_全部 用 表。
    若 i 小于 做 计数 用 词列表 并且 做 字符串等于 用 词列表 的 第 i 项 的 第 1 项、「WHERE」：
        i 为 i 加 1。
        where结果 为 做 sql_解析WHERE 用 表、词列表、i。
        行列表 为 where结果 的 第 0 项。
        i 为 where结果 的 第 1 项。
    完。
    若 i 加 1 小于 做 计数 用 词列表 并且 做 字符串等于 用 词列表 的 第 i 项 的 第 1 项、「ORDER」：
        i 为 i 加 2。
        若 i 小于 做 计数 用 词列表：
            排序列 为 词列表 的 第 i 项 的 第 1 项。
            排序idx 为 做 表_列索引 用 表、排序列。
            可变 降序 为 假。
            i 为 i 加 1。
            若 i 小于 做 计数 用 词列表 并且 做 字符串等于 用 词列表 的 第 i 项 的 第 1 项、「DESC」：
                降序 为 真。
                i 为 i 加 1。
            完。
            若 排序idx 大于或等于 0：
                行列表 为 做 sql_排序 用 行列表、排序idx、降序。
            完。
        完。
    完。
    若 i 小于 做 计数 用 词列表 并且 做 字符串等于 用 词列表 的 第 i 项 的 第 1 项、「LIMIT」：
        i 为 i 加 1。
        若 i 小于 做 计数 用 词列表 并且 词列表 的 第 i 项 的 第 0 项 等于 SQL_数字：
            限制 为 词列表 的 第 i 项 的 第 1 项。
            若 做 计数 用 行列表 大于 限制：
                可变 截断 为 一组：（空）。
                可变 j 为 0。
                只要 j 小于 限制：
                    做 追加 用 截断、行列表 的 第 j 项。
                    j 为 j 加 1。
                完。
                行列表 为 截断。
            完。
        完。
    完。
    若 全选：
        返还 一组：「成功」、行列表。
    完。
    可变 列索引列表 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 做 计数 用 选择列：
        做 追加 用 列索引列表、做 表_列索引 用 表、选择列 的 第 k 项。
        k 为 k 加 1。
    完。
    可变 投影结果 为 一组：（空）。
    k 为 0。
    只要 k 小于 做 计数 用 行列表：
        可变 投影行 为 一组：（空）。
        可变 m 为 0。
        只要 m 小于 做 计数 用 列索引列表：
            ci 为 列索引列表 的 第 m 项。
            若 ci 大于或等于 0：
                做 追加 用 投影行、行列表 的 第 k 项 的 第 ci 项。
            否则：
                做 追加 用 投影行、空。
            完。
            m 为 m 加 1。
        完。
        做 追加 用 投影结果、投影行。
        k 为 k 加 1。
    完。
    返还 一组：「成功」、投影结果。
完。

sql_解析WHERE 接受 表、词列表、起始：
    全部行 为 做 表_全部 用 表。
    可变 结果 为 全部行。
    可变 i 为 起始。
    可变 逻辑 为 「AND」。
    只要 i 加 2 小于 做 计数 用 词列表：
        若 词列表 的 第 i 项 的 第 0 项 等于 SQL_关键字 并且 做 sql_是条件终止词 用 词列表 的 第 i 项 的 第 1 项：
            止。
        完。
        若 词列表 的 第 i 项 的 第 0 项 等于 SQL_关键字 并且 做 字符串等于 用 词列表 的 第 i 项 的 第 1 项、「AND」：
            逻辑 为 「AND」。
            i 为 i 加 1。
            续。
        完。
        若 词列表 的 第 i 项 的 第 0 项 等于 SQL_关键字 并且 做 字符串等于 用 词列表 的 第 i 项 的 第 1 项、「OR」：
            逻辑 为 「OR」。
            i 为 i 加 1。
            续。
        完。
        列名 为 词列表 的 第 i 项 的 第 1 项。
        列idx 为 做 表_列索引 用 表、列名。
        操作符 为 词列表 的 第 i 加 1 项 的 第 1 项。
        可变 比较值 为 词列表 的 第 i 加 2 项 的 第 1 项。
        可变 过滤 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 做 计数 用 结果：
            行值 为 结果 的 第 j 项 的 第 列idx 项。
            可变 符合 为 假。
            若 做 字符串等于 用 操作符、「=」：
                符合 为 行值 等于 比较值。
            否则若 做 字符串等于 用 操作符、「>」：
                符合 为 行值 大于 比较值。
            否则若 做 字符串等于 用 操作符、「<」：
                符合 为 行值 小于 比较值。
            否则若 做 字符串等于 用 操作符、「>=」：
                符合 为 行值 大于或等于 比较值。
            否则若 做 字符串等于 用 操作符、「<=」：
                符合 为 行值 小于或等于 比较值。
            否则若 做 字符串等于 用 操作符、「!=」：
                符合 为 行值 不等于 比较值。
            完。
            若 符合：
                做 追加 用 过滤、结果 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        结果 为 过滤。
        i 为 i 加 3。
    完。
    返还 一组：结果、i。
完。

sql_执行DELETE 接受 db、词列表：
    若 做 计数 用 词列表 小于 3：
        返还 一组：「错误」、「语法错误」。
    完。
    表名 为 词列表 的 第 2 项 的 第 1 项。
    表 为 做 数据库_取表 用 db、表名。
    若 表 等于 空：
        返还 一组：「错误」、「表不存在」。
    完。
    若 做 计数 用 词列表 小于 5：
        删除数 为 表 的 第 5 项。
        表 的 第 2 项 为 一组：（空）。
        表 的 第 5 项 为 0。
        返还 一组：「成功」、删除数。
    完。
    where结果 为 做 sql_解析WHERE 用 表、词列表、4。
    要删除 为 where结果 的 第 0 项。
    可变 删除数 为 0。
    可变 新行 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 表 的 第 2 项：
        可变 在删除集 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 要删除 并且 在删除集 等于 假：
            若 表 的 第 2 项 的 第 i 项 的 第 1 项 等于 要删除 的 第 j 项：
                在删除集 为 真。
            完。
            j 为 j 加 1。
        完。
        若 在删除集：
            删除数 为 删除数 加 1。
        否则：
            做 追加 用 新行、表 的 第 2 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    表 的 第 2 项 为 新行。
    表 的 第 5 项 为 表 的 第 5 项 减 删除数。
    返还 一组：「成功」、删除数。
完。

sql_执行UPDATE 接受 db、词列表：
    若 做 计数 用 词列表 小于 6：
        返还 一组：「错误」、「语法错误」。
    完。
    表名 为 词列表 的 第 1 项 的 第 1 项。
    表 为 做 数据库_取表 用 db、表名。
    若 表 等于 空：
        返还 一组：「错误」、「表不存在」。
    完。
    可变 更新列表 为 一组：（空）。
    可变 i 为 3。
    可变 _set完 为 假。
    只要 i 加 2 小于 做 计数 用 词列表 并且 _set完 等于 假：
        若 词列表 的 第 i 项 的 第 0 项 等于 SQL_关键字 并且 词列表 的 第 i 项 的 第 1 项 等于 「WHERE」：
            _set完 为 真。
        否则：
            若 词列表 的 第 i 项 的 第 0 项 等于 SQL_标识符 并且 做 字符串等于 用 词列表 的 第 i 加 1 项 的 第 1 项、「=」：
                做 追加 用 更新列表、一组：词列表 的 第 i 项 的 第 1 项、词列表 的 第 i 加 2 项 的 第 1 项。
                i 为 i 加 3。
            否则：
                i 为 i 加 1。
            完。
        完。
    完。
    可变 更新数 为 0。
    可变 j 为 0。
    只要 j 小于 做 计数 用 表 的 第 2 项：
        可变 符合 为 真。
        若 i 小于 做 计数 用 词列表 并且 做 字符串等于 用 词列表 的 第 i 项 的 第 1 项、「WHERE」：
            若 i 加 3 小于 做 计数 用 词列表：
                列idx 为 做 表_列索引 用 表、词列表 的 第 i 加 1 项 的 第 1 项。
                比较值 为 词列表 的 第 i 加 3 项 的 第 1 项。
                符合 为 表 的 第 2 项 的 第 j 项 的 第 1 项 的 第 列idx 项 等于 比较值。
            完。
        完。
        若 符合：
            可变 k 为 0。
            只要 k 小于 做 计数 用 更新列表：
                更新列idx 为 做 表_列索引 用 表、更新列表 的 第 k 项 的 第 0 项。
                若 更新列idx 大于或等于 0：
                    可变 当前行记录 为 表 的 第 2 项 的 第 j 项。
                    可变 当前值列表 为 当前行记录 的 第 1 项。
                    当前值列表 的 第 更新列idx 项 为 更新列表 的 第 k 项 的 第 1 项。
                    当前行记录 的 第 1 项 为 当前值列表。
                    表 的 第 2 项 的 第 j 项 为 当前行记录。
                完。
                k 为 k 加 1。
            完。
            更新数 为 更新数 加 1。
        完。
        j 为 j 加 1。
    完。
    返还 一组：「成功」、更新数。
完。

sql_执行DROP 接受 db、词列表：
    若 做 计数 用 词列表 小于 3：
        返还 一组：「错误」、「语法错误」。
    完。
    返还 做 数据库_删除表 用 db、词列表 的 第 2 项 的 第 1 项。
完。

sql_排序 接受 行列表、列idx、降序：
    n 为 做 计数 用 行列表。
    若 n 小于或等于 1：
        返还 行列表。
    完。
    可变 i 为 1。
    只要 i 小于 n：
        当前 为 行列表 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0：
            可变 应交换 为 假。
            若 降序：
                应交换 为 行列表 的 第 j 项 的 第 列idx 项 小于 当前 的 第 列idx 项。
            否则：
                应交换 为 行列表 的 第 j 项 的 第 列idx 项 大于 当前 的 第 列idx 项。
            完。
            若 应交换：
                行列表 的 第 j 加 1 项 为 行列表 的 第 j 项。
                j 为 j 减 1。
            否则：
                j 为 0 减 1。
            完。
        完。
        行列表 的 第 j 加 1 项 为 当前。
        i 为 i 加 1。
    完。
    返还 行列表。
完。

本章 收束。
