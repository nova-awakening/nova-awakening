《安全沙箱》

引用《..·基础》。

能力_读 为 1。

能力_写 为 2。

能力_执行 为 4。

能力_网络 为 8。

能力_文件 为 16。

可变 沙箱列表 为 一组：（空）。

可变 沙箱_下一ID 为 0。

沙箱_创建 接受 能力掩码、内存限制、CPU限制：
    id 为 沙箱_下一ID。
    沙箱_下一ID 为 沙箱_下一ID 加 1。
    做 追加 用 沙箱列表、一组：id、能力掩码、内存限制、0、CPU限制、0、一组：（空）。
    返还 id。
完。

沙箱_检查能力 接受 id、能力：
    掩码 为 沙箱列表 的 第 id 项 的 第 1 项。
    可变 r 为 掩码。
    可变 a 为 能力。
    可变 结果 为 0。
    可变 位 为 1。
    只要 位 小于或等于 32：
        若 r 取余于 2 等于 1 并且 a 取余于 2 等于 1：
            结果 为 结果 加 位。
        完。
        r 为 r 除以 2。
        a 为 a 除以 2。
        位 为 位 乘 2。
    完。
    返还 结果 大于 0。
完。

沙箱_分配内存 接受 id、大小：
    若 沙箱列表 的 第 id 项 的 第 3 项 加 大小 大于 沙箱列表 的 第 id 项 的 第 2 项：
        返还 假。
    完。
    沙箱列表 的 第 id 项 的 第 3 项 为 沙箱列表 的 第 id 项 的 第 3 项 加 大小。
    返还 真。
完。

沙箱_消耗CPU 接受 id、量：
    若 沙箱列表 的 第 id 项 的 第 5 项 加 量 大于 沙箱列表 的 第 id 项 的 第 4 项：
        返还 假。
    完。
    沙箱列表 的 第 id 项 的 第 5 项 为 沙箱列表 的 第 id 项 的 第 5 项 加 量。
    返还 真。
完。

沙箱_审计 接受 id、操作哈希：
    做 追加 用 沙箱列表 的 第 id 项 的 第 6 项、操作哈希。
完。

沙箱_日志数 接受 id：
    返还 做 计数 用 沙箱列表 的 第 id 项 的 第 6 项。
完。

沙箱_重置 接受 id：
    沙箱列表 的 第 id 项 的 第 3 项 为 0。
    沙箱列表 的 第 id 项 的 第 5 项 为 0。
    沙箱列表 的 第 id 项 的 第 6 项 为 一组：（空）。
完。

可变 ACL_主体 为 一组：（空）。

可变 ACL_资源 为 一组：（空）。

可变 ACL_权限 为 一组：（空）。

ACL_清空 接受 零参：
    ACL_主体 为 一组：（空）。
    ACL_资源 为 一组：（空）。
    ACL_权限 为 一组：（空）。
完。

ACL_授权 接受 主体、资源、权限：
    做 追加 用 ACL_主体、主体。
    做 追加 用 ACL_资源、资源。
    做 追加 用 ACL_权限、权限。
完。

ACL_检查 接受 主体、资源、请求权限：
    可变 i 为 0。
    只要 i 小于 做 计数 用 ACL_主体：
        若 ACL_主体 的 第 i 项 等于 主体 并且 ACL_资源 的 第 i 项 等于 资源：
            若 ACL_权限 的 第 i 项 大于或等于 请求权限：
                返还 真。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

沙箱_清空 接受 零参：
    沙箱列表 为 一组：（空）。
    沙箱_下一ID 为 0。
完。

沙箱_内存使用率 接受 id：
    若 沙箱列表 的 第 id 项 的 第 2 项 等于 0：
        返还 0。
    完。
    返还 沙箱列表 的 第 id 项 的 第 3 项 乘 100 除以 沙箱列表 的 第 id 项 的 第 2 项。
完。

沙箱_CPU使用率 接受 id：
    若 沙箱列表 的 第 id 项 的 第 4 项 等于 0：
        返还 0。
    完。
    返还 沙箱列表 的 第 id 项 的 第 5 项 乘 100 除以 沙箱列表 的 第 id 项 的 第 4 项。
完。

沙箱_撤销能力 接受 id、能力：
    掩码 为 沙箱列表 的 第 id 项 的 第 1 项。
    可变 新掩码 为 0。
    可变 位 为 1。
    可变 m 为 掩码。
    可变 a 为 能力。
    只要 位 小于或等于 32：
        mb 为 m 取余于 2。
        ab 为 a 取余于 2。
        若 mb 等于 1 并且 ab 等于 0：
            新掩码 为 新掩码 加 位。
        完。
        m 为 m 除以 2。
        a 为 a 除以 2。
        位 为 位 乘 2。
    完。
    沙箱列表 的 第 id 项 的 第 1 项 为 新掩码。
完。

沙箱_添加能力 接受 id、能力：
    掩码 为 沙箱列表 的 第 id 项 的 第 1 项。
    可变 新掩码 为 0。
    可变 位 为 1。
    可变 m 为 掩码。
    可变 a 为 能力。
    只要 位 小于或等于 32：
        mb 为 m 取余于 2。
        ab 为 a 取余于 2。
        若 mb 等于 1 或者 ab 等于 1：
            新掩码 为 新掩码 加 位。
        完。
        m 为 m 除以 2。
        a 为 a 除以 2。
        位 为 位 乘 2。
    完。
    沙箱列表 的 第 id 项 的 第 1 项 为 新掩码。
完。

AI沙箱_创建 接受 内存MB：
    返还 做 沙箱_创建 用 能力_读 加 能力_执行、内存MB 乘 1024、10000。
完。

AI沙箱_推理 接受 id、计算量：
    若 做 沙箱_检查能力 用 id、能力_执行 等于 假：
        返还 负 1。
    完。
    若 做 沙箱_消耗CPU 用 id、计算量 等于 假：
        返还 负 2。
    完。
    做 沙箱_审计 用 id、100。
    返还 0。
完。

沙箱_报告 接受 id：
    sb 为 沙箱列表 的 第 id 项。
    做 打印 用 「╔═══ 沙箱[」 加 做 转字符串 用 id 加 「] ═══╗」。
    做 打印 用 「║ 能力掩码: 」 加 做 转字符串 用 sb 的 第 1 项。
    做 打印 用 「║ 内存: 」 加 做 转字符串 用 sb 的 第 3 项 加 「/」 加 做 转字符串 用 sb 的 第 2 项 加 「 (」 加 做 转字符串 用 做 沙箱_内存使用率 用 id 加 「%)」。
    做 打印 用 「║ CPU: 」 加 做 转字符串 用 sb 的 第 5 项 加 「/」 加 做 转字符串 用 sb 的 第 4 项 加 「 (」 加 做 转字符串 用 做 沙箱_CPU使用率 用 id 加 「%)」。
    做 打印 用 「║ 审计: 」 加 做 转字符串 用 做 沙箱_日志数 用 id 加 「 条」。
    做 打印 用 「╚═══════════════════╝」。
完。

沙箱_全部报告 接受 零参：
    做 打印 用 「沙箱总数: 」 加 做 转字符串 用 做 计数 用 沙箱列表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 沙箱列表：
        做 沙箱_报告 用 i。
        i 为 i 加 1。
    完。
完。

沙箱_内存超限 接受 id：
    返还 沙箱列表 的 第 id 项 的 第 3 项 大于或等于 沙箱列表 的 第 id 项 的 第 2 项。
完。

沙箱_CPU超限 接受 id：
    返还 沙箱列表 的 第 id 项 的 第 5 项 大于或等于 沙箱列表 的 第 id 项 的 第 4 项。
完。

沙箱_任何超限 接受 id：
    返还 做 沙箱_内存超限 用 id 或者 做 沙箱_CPU超限 用 id。
完。

ACL_数量 接受 零参：
    返还 做 计数 用 ACL_主体。
完。

ACL_撤销 接受 主体、资源：
    可变 新主 为 一组：（空）。
    可变 新资 为 一组：（空）。
    可变 新权 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 ACL_主体：
        若 ACL_主体 的 第 i 项 不等于 主体 或者 ACL_资源 的 第 i 项 不等于 资源：
            做 追加 用 新主、ACL_主体 的 第 i 项。
            做 追加 用 新资、ACL_资源 的 第 i 项。
            做 追加 用 新权、ACL_权限 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    ACL_主体 为 新主。
    ACL_资源 为 新资。
    ACL_权限 为 新权。
完。

可变 _限制_内存 为 0。

可变 _限制_CPU时间 为 0。

可变 _限制_文件数 为 0。

可变 _当前_内存 为 0。

可变 _当前_文件数 为 0。

沙箱_设限制 接受 内存上限、CPU上限、文件上限：
    _限制_内存 为 内存上限。
    _限制_CPU时间 为 CPU上限。
    _限制_文件数 为 文件上限。
完。

沙箱_检查内存 接受 申请量：
    返还 _当前_内存 加 申请量 小于或等于 _限制_内存。
完。

沙箱_检查文件 接受 零参：
    返还 _当前_文件数 小于 _限制_文件数。
完。

沙箱_记录内存 接受 量：
    _当前_内存 为 _当前_内存 加 量。
完。

沙箱_记录文件 接受 零参：
    _当前_文件数 为 _当前_文件数 加 1。
完。

可变 _允许调用 为 一组：（空）。

沙箱_允许调用 接受 调用名：
    做 追加 用 _允许调用、调用名。
完。

沙箱_调用许可 接受 调用名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _允许调用：
        若 _允许调用 的 第 i 项 等于 调用名：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

可变 _违规日志 为 一组：（空）。

沙箱_记录违规 接受 类型、详情：
    做 追加 用 _违规日志、一组：类型、详情、做 计数 用 _违规日志。
完。

沙箱_违规数 接受 零参：
    返还 做 计数 用 _违规日志。
完。

沙箱_违规列表 接受 零参：
    返还 _违规日志。
完。

本章 收束。
