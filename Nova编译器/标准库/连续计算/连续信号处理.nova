《连续信号处理》

引用《..·基础》。

引用《..·数学·信号处理》。

连续信号处理_信号精度 为 100000。

信号_PI 为 314159。

信号_2PI 为 628318。

信号_E 为 271828。

信号_创建 接受 采样率：
    返还 一组：一组：（空）、采样率、0、0、0、连续信号处理_信号精度。
完。

信号_添加样本 接受 信号、值：
    做 追加 用 信号 的 第 0 项、值。
    信号 的 第 2 项 为 信号 的 第 2 项 加 1。
    若 值 大于 信号 的 第 3 项 或者 信号 的 第 2 项 等于 1：
        信号 的 第 3 项 为 值。
    完。
    若 值 小于 信号 的 第 4 项 或者 信号 的 第 2 项 等于 1：
        信号 的 第 4 项 为 值。
    完。
完。

信号_正弦 接受 频率、振幅、相位、采样率、长度：
    信号 为 做 信号_创建 用 采样率。
    可变 t 为 0。
    只要 t 小于 长度：
        角度 为 (信号_2PI 乘 频率 乘 t 除以 采样率 加 相位) 取余于 信号_2PI。
        值 为 振幅 乘 做 信号处理_定点sin 用 角度 除以 连续信号处理_信号精度。
        做 信号_添加样本 用 信号、值。
        t 为 t 加 1。
    完。
    返还 信号。
完。

信号处理_定点sin 接受 x：
    可变 归一 为 x 取余于 信号_2PI。
    若 归一 大于 信号_PI：
        归一 为 归一 减 信号_2PI。
    完。
    若 归一 小于 0 减 信号_PI：
        归一 为 归一 加 信号_2PI。
    完。
    x1 为 归一。
    x2 为 x1 乘 x1 除以 连续信号处理_信号精度。
    x3 为 x2 乘 x1 除以 连续信号处理_信号精度。
    x5 为 x3 乘 x2 除以 连续信号处理_信号精度。
    x7 为 x5 乘 x2 除以 连续信号处理_信号精度。
    返还 x1 减 x3 除以 6 加 x5 除以 120 减 x7 除以 5040。
完。

信号处理_定点cos 接受 x：
    返还 做 信号处理_定点sin 用 x 加 信号_PI 除以 2。
完。

FIR_创建 接受 系数列表：
    返还 一组：系数列表、做 计数 用 系数列表、一组：（空）。
完。

FIR_处理 接受 滤波器、输入：
    系数 为 滤波器 的 第 0 项。
    可变 新历史 为 一组：（空）。
    阶数 为 滤波器 的 第 1 项。
    可变 历史 为 滤波器 的 第 2 项。
    做 插入 用 历史、0、输入。
    只要 做 计数 用 历史 大于 阶数：
        可变 i 为 0。
        只要 i 小于 阶数：
            做 追加 用 新历史、历史 的 第 i 项。
            i 为 i 加 1。
        完。
        滤波器 的 第 2 项 为 新历史。
        历史 为 新历史。
    完。
    可变 输出 为 0。
    可变 k 为 0。
    只要 k 小于 阶数 并且 k 小于 做 计数 用 历史：
        输出 为 输出 加 系数 的 第 k 项 乘 历史 的 第 k 项 除以 连续信号处理_信号精度。
        k 为 k 加 1。
    完。
    返还 输出。
完。

设计_低通FIR 接受 截止频率、采样率、阶数：
    可变 系数 为 一组：（空）。
    归一截止 为 截止频率 乘 连续信号处理_信号精度 除以 采样率。
    中点 为 阶数 除以 2。
    可变 n 为 0。
    只要 n 小于 阶数：
        可变 h 为 0。
        若 n 等于 中点：
            h 为 2 乘 归一截止。
        否则：
            偏移 为 (n 减 中点) 乘 连续信号处理_信号精度。
            参数 为 2 乘 归一截止 乘 偏移 除以 连续信号处理_信号精度。
            若 偏移 不等于 0：
                h 为 做 信号处理_定点sin 用 参数 乘 信号_PI 除以 连续信号处理_信号精度 乘 连续信号处理_信号精度 除以 (偏移 乘 信号_PI 除以 连续信号处理_信号精度)。
            完。
            窗值 为 54000 减 46000 乘 做 信号处理_定点cos 用 信号_2PI 乘 n 除以 阶数 除以 连续信号处理_信号精度。
            h 为 h 乘 窗值 除以 连续信号处理_信号精度。
        完。
        做 追加 用 系数、h。
        n 为 n 加 1。
    完。
    返还 做 FIR_创建 用 系数。
完。

IIR_二阶_创建 接受 b0、b1、b2、a1、a2：
    返还 一组：一组：b0、b1、b2、一组：a1、a2、一组：0、0、一组：0、0。
完。

IIR_二阶_处理 接受 滤波器、输入：
    b 为 滤波器 的 第 0 项。
    a 为 滤波器 的 第 1 项。
    xd 为 滤波器 的 第 2 项。
    yd 为 滤波器 的 第 3 项。
    输出 为 b 的 第 0 项 乘 输入 除以 连续信号处理_信号精度 加 b 的 第 1 项 乘 xd 的 第 0 项 除以 连续信号处理_信号精度 加 b 的 第 2 项 乘 xd 的 第 1 项 除以 连续信号处理_信号精度 减 a 的 第 0 项 乘 yd 的 第 0 项 除以 连续信号处理_信号精度 减 a 的 第 1 项 乘 yd 的 第 1 项 除以 连续信号处理_信号精度。
    xd 的 第 1 项 为 xd 的 第 0 项。
    xd 的 第 0 项 为 输入。
    yd 的 第 1 项 为 yd 的 第 0 项。
    yd 的 第 0 项 为 输出。
    返还 输出。
完。

离散傅里叶变换 接受 信号数据、_扩2：
    N 为 做 计数 用 信号数据。
    可变 实部 为 一组：（空）。
    可变 虚部 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 N：
        可变 re 为 0。
        可变 im 为 0。
        可变 n 为 0。
        只要 n 小于 N：
            角度 为 信号_2PI 乘 k 乘 n 除以 N。
            c 为 做 信号处理_定点cos 用 角度。
            s 为 做 信号处理_定点sin 用 角度。
            re 为 re 加 信号数据 的 第 n 项 乘 c 除以 连续信号处理_信号精度。
            im 为 im 减 信号数据 的 第 n 项 乘 s 除以 连续信号处理_信号精度。
            n 为 n 加 1。
        完。
        做 追加 用 实部、re。
        做 追加 用 虚部、im。
        k 为 k 加 1。
    完。
    返还 一组：实部、虚部。
完。

功率谱 接受 频域：
    实部 为 频域 的 第 0 项。
    虚部 为 频域 的 第 1 项。
    N 为 做 计数 用 实部。
    可变 功率 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 N：
        p 为 (实部 的 第 k 项 乘 实部 的 第 k 项 加 虚部 的 第 k 项 乘 虚部 的 第 k 项) 除以 连续信号处理_信号精度。
        做 追加 用 功率、p。
        k 为 k 加 1。
    完。
    返还 功率。
完。

检测主频 接受 功率谱数据、采样率：
    N 为 做 计数 用 功率谱数据。
    可变 最大索引 为 0。
    可变 最大功率 为 0。
    可变 k 为 1。
    只要 k 小于 N 除以 2：
        若 功率谱数据 的 第 k 项 大于 最大功率：
            最大功率 为 功率谱数据 的 第 k 项。
            最大索引 为 k。
        完。
        k 为 k 加 1。
    完。
    若 N 等于 0：
        返还 0。
    完。
    返还 最大索引 乘 采样率 除以 N。
完。

Haar_分解 接受 数据：
    N 为 做 计数 用 数据。
    若 N 小于 2：
        返还 一组：数据、一组：（空）。
    完。
    可变 近似 为 一组：（空）。
    可变 细节 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N 减 1：
        做 追加 用 近似、(数据 的 第 i 项 加 数据 的 第 i 加 1 项) 除以 2。
        做 追加 用 细节、(数据 的 第 i 项 减 数据 的 第 i 加 1 项) 除以 2。
        i 为 i 加 2。
    完。
    返还 一组：近似、细节。
完。

小波_多级分解 接受 数据、级数：
    可变 结果 为 一组：（空）。
    可变 当前数据 为 数据。
    可变 级 为 0。
    只要 级 小于 级数 并且 做 计数 用 当前数据 大于或等于 2：
        分解 为 做 Haar_分解 用 当前数据。
        做 追加 用 结果、分解 的 第 1 项。
        当前数据 为 分解 的 第 0 项。
        级 为 级 加 1。
    完。
    做 追加 用 结果、当前数据。
    返还 结果。
完。

Haar_重建 接受 近似、细节：
    N 为 做 计数 用 近似。
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        可变 d 为 0。
        若 i 小于 做 计数 用 细节：
            d 为 细节 的 第 i 项。
        完。
        做 追加 用 数据、近似 的 第 i 项 加 d。
        做 追加 用 数据、近似 的 第 i 项 减 d。
        i 为 i 加 1。
    完。
    返还 数据。
完。

连续信号_信号_均值 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        和 为 和 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和 除以 做 计数 用 数据。
完。

连续信号_信号_方差 接受 数据：
    均值 为 做 信号_均值 用 数据。
    可变 方差 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        差 为 数据 的 第 i 项 减 均值。
        方差 为 方差 加 差 乘 差 除以 连续信号处理_信号精度。
        i 为 i 加 1。
    完。
    若 做 计数 用 数据 大于 0：
        方差 为 方差 除以 做 计数 用 数据。
    完。
    返还 方差。
完。

信号_信噪比 接受 原始、噪声：
    信号功率 为 做 信号_方差 用 原始。
    噪声功率 为 做 信号_方差 用 噪声。
    若 噪声功率 等于 0：
        返还 连续信号处理_信号精度 乘 100。
    完。
    返还 信号功率 乘 连续信号处理_信号精度 除以 噪声功率。
完。

连续信号_自相关 接受 数据、最大延迟：
    N 为 做 计数 用 数据。
    均值 为 做 信号_均值 用 数据。
    可变 相关 为 一组：（空）。
    可变 τ 为 0。
    只要 τ 小于 最大延迟 并且 τ 小于 N：
        可变 累积 为 0。
        可变 i 为 0。
        只要 i 小于 N 减 τ：
            累积 为 累积 加 (数据 的 第 i 项 减 均值) 乘 (数据 的 第 i 加 τ 项 减 均值) 除以 连续信号处理_信号精度。
            i 为 i 加 1。
        完。
        做 追加 用 相关、累积 除以 (N 减 τ)。
        τ 为 τ 加 1。
    完。
    返还 相关。
完。

信号_功率谱 接受 信号：
    N 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 N 除以 2：
        可变 实部 为 0。
        可变 虚部 为 0。
        可变 n 为 0。
        只要 n 小于 N：
            角度 为 2 乘 314159 乘 k 乘 n 除以 (N 乘 100000)。
            实部 为 实部 加 信号 的 第 n 项 乘 做 信号_余弦近似 用 角度。
            虚部 为 虚部 减 信号 的 第 n 项 乘 做 信号_正弦近似 用 角度。
            n 为 n 加 1。
        完。
        做 追加 用 结果、(实部 乘 实部 加 虚部 乘 虚部) 除以 N。
        k 为 k 加 1。
    完。
    返还 结果。
完。

信号_余弦近似 接受 x：
    x2 为 x 乘 x 除以 10000000000。
    返还 1000 减 x2 除以 2 加 x2 乘 x2 除以 24000000。
完。

信号_正弦近似 接受 x：
    x2 为 x 乘 x 除以 10000000000。
    返还 x 除以 100000 减 x 乘 x2 除以 6000000。
完。

信号_汉宁窗 接受 信号：
    N 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        系数 为 500 减 500 乘 做 信号_余弦近似 用 2 乘 314159 乘 i 除以 (N 乘 100000) 除以 1000。
        做 追加 用 结果、信号 的 第 i 项 乘 系数 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

信号_矩形窗 接受 信号：
    返还 信号。
完。

信号_移动平均 接受 信号、窗口：
    N 为 做 计数 用 信号。
    若 窗口 大于 N：
        返还 信号。
    完。
    可变 结果 为 一组：（空）。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 窗口：
        和 为 和 加 信号 的 第 i 项。
        i 为 i 加 1。
    完。
    做 追加 用 结果、和 除以 窗口。
    i 为 窗口。
    只要 i 小于 N：
        和 为 和 加 信号 的 第 i 项 减 信号 的 第 i 减 窗口 项。
        做 追加 用 结果、和 除以 窗口。
        i 为 i 加 1。
    完。
    返还 结果。
完。

信号_中值滤波 接受 信号、窗口：
    N 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        可变 子列 为 一组：（空）。
        可变 j 为 i 减 窗口 除以 2。
        只要 j 小于或等于 i 加 窗口 除以 2：
            若 j 大于或等于 0 并且 j 小于 N：
                做 追加 用 子列、信号 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        可变 k 为 0。
        只要 k 小于 做 计数 用 子列 减 1：
            可变 m 为 k 加 1。
            只要 m 小于 做 计数 用 子列：
                若 子列 的 第 m 项 小于 子列 的 第 k 项：
                    临 为 子列 的 第 k 项。
                    子列 的 第 k 项 为 子列 的 第 m 项。
                    子列 的 第 m 项 为 临。
                完。
                m 为 m 加 1。
            完。
            k 为 k 加 1。
        完。
        做 追加 用 结果、子列 的 第 做 计数 用 子列 除以 2 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

连续信号_信号_能量 接受 信号：
    可变 能量 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 信号：
        能量 为 能量 加 信号 的 第 i 项 乘 信号 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 能量。
完。

连续信号_信号_峰值 接受 信号：
    若 做 计数 用 信号 等于 0：
        返还 0。
    完。
    可变 峰 为 信号 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 信号：
        若 信号 的 第 i 项 大于 峰：
            峰 为 信号 的 第 i 项。
        完。
        若 0 减 信号 的 第 i 项 大于 峰：
            峰 为 0 减 信号 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 峰。
完。

可变 _信号_处理次数 为 0。

可变 _信号_滤波次数 为 0。

可变 _信号_最大长度 为 0。

信号_记录处理 接受 长度：
    _信号_处理次数 为 _信号_处理次数 加 1。
    若 长度 大于 _信号_最大长度：
        _信号_最大长度 为 长度。
    完。
完。

信号_记录滤波 接受 零参：
    _信号_滤波次数 为 _信号_滤波次数 加 1。
完。

信号_性能评估 接受 零参：
    若 _信号_最大长度 大于 100000：
        返还 「超大信号(考虑分段处理)」。
    完。
    若 _信号_滤波次数 大于 10000：
        返还 「高频滤波」。
    完。
    返还 「信号处理正常」。
完。

信号_重置 接受 零参：
    _信号_处理次数 为 0。
    _信号_滤波次数 为 0。
    _信号_最大长度 为 0。
完。

信号_信噪比_能量 接受 信号、噪声：
    信号能 为 做 信号_能量 用 信号、0。
    噪声能 为 做 信号_能量 用 噪声、0。
    若 噪声能 等于 0：
        返还 9999。
    完。
    返还 信号能 乘 100 除以 噪声能。
完。

信号_零交叉率 接受 信号：
    可变 i 为 1。
    只要 i 小于 做 计数 用 信号：
        若 信号 的 第 i 减 1 项 大于或等于 0 并且 信号 的 第 i 项 小于 0 或者 信号 的 第 i 减 1 项 小于 0 并且 信号 的 第 i 项 大于或等于 0：
            交叉 为 交叉 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 交叉。
完。

本章 收束。
