《模糊逻辑引擎》

引用《..·基础》。

模糊精度 为 10000。

模糊_完全真 为 10000。

模糊_完全假 为 0。

模糊_半真 为 5000。

模糊与 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

模糊或 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

模糊非 接受 a：
    返还 模糊精度 减 a。
完。

模糊代数积 接受 a、b：
    返还 a 乘 b 除以 模糊精度。
完。

模糊代数和 接受 a、b：
    返还 a 加 b 减 a 乘 b 除以 模糊精度。
完。

模糊Luk与 接受 a、b：
    结果 为 a 加 b 减 模糊精度。
    若 结果 小于 0：
        返还 0。
    完。
    返还 结果。
完。

模糊Hamacher积 接受 a、b、γ：
    若 a 等于 0 并且 b 等于 0：
        返还 0。
    完。
    分子 为 a 乘 b。
    分母 为 γ 乘 模糊精度 加 (1 减 γ) 乘 (a 加 b 减 a 乘 b 除以 模糊精度)。
    若 分母 等于 0：
        返还 0。
    完。
    返还 分子 除以 (分母 除以 模糊精度)。
完。

三角隶属 接受 x、左、中、右：
    若 x 小于或等于 左 或者 x 大于或等于 右：
        返还 0。
    完。
    若 x 小于或等于 中：
        返还 (x 减 左) 乘 模糊精度 除以 (中 减 左)。
    完。
    返还 (右 减 x) 乘 模糊精度 除以 (右 减 中)。
完。

梯形隶属 接受 x、a、b、c、d：
    若 x 小于或等于 a 或者 x 大于或等于 d：
        返还 0。
    完。
    若 x 大于或等于 b 并且 x 小于或等于 c：
        返还 模糊精度。
    完。
    若 x 小于 b：
        返还 (x 减 a) 乘 模糊精度 除以 (b 减 a)。
    完。
    返还 (d 减 x) 乘 模糊精度 除以 (d 减 c)。
完。

高斯隶属 接受 x、中心、宽度：
    若 宽度 等于 0：
        返还 0。
    完。
    差 为 x 减 中心。
    差平方 为 差 乘 差。
    宽平方 为 宽度 乘 宽度 乘 2。
    比值 为 差平方 乘 模糊精度 除以 宽平方。
    若 比值 大于 模糊精度 乘 4：
        返还 0。
    完。
    近似 为 模糊精度 减 比值 加 比值 乘 比值 除以 (2 乘 模糊精度)。
    若 近似 小于 0：
        返还 0。
    完。
    若 近似 大于 模糊精度：
        返还 模糊精度。
    完。
    返还 近似。
完。

S形隶属 接受 x、中心、斜率：
    偏移 为 (x 减 中心) 乘 斜率 除以 模糊精度。
    可变 绝对 为 偏移。
    若 绝对 小于 0：
        绝对 为 0 减 绝对。
    完。
    归一 为 偏移 乘 模糊精度 除以 (模糊精度 加 绝对)。
    返还 模糊精度 除以 2 加 归一 除以 2。
完。

模糊集_创建 接受 名称、隶属类型、参数列表：
    返还 一组：名称、隶属类型、参数列表、0。
完。

模糊集_计算隶属度 接受 集合、值：
    类型 为 集合 的 第 1 项。
    参数 为 集合 的 第 2 项。
    若 类型 等于 1：
        返还 做 三角隶属 用 值、参数 的 第 0 项、参数 的 第 1 项、参数 的 第 2 项。
    完。
    若 类型 等于 2：
        返还 做 梯形隶属 用 值、参数 的 第 0 项、参数 的 第 1 项、参数 的 第 2 项、参数 的 第 3 项。
    完。
    若 类型 等于 3：
        返还 做 高斯隶属 用 值、参数 的 第 0 项、参数 的 第 1 项。
    完。
    若 类型 等于 4：
        返还 做 S形隶属 用 值、参数 的 第 0 项、参数 的 第 1 项。
    完。
    返还 0。
完。

模糊变量_创建 接受 名称、最小值、最大值：
    返还 一组：名称、最小值、最大值、一组：（空）。
完。

模糊变量_添加集合 接受 _变量、集合：
    做 追加 用 _变量 的 第 3 项、集合。
完。

模糊化 接受 _变量、精确值：
    集合列表 为 _变量 的 第 3 项。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合列表：
        隶属度 为 做 模糊集_计算隶属度 用 集合列表 的 第 i 项、精确值。
        做 追加 用 结果、隶属度。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模糊规则_创建 接受 条件列表、结论集合索引、连接方式：
    返还 一组：条件列表、结论集合索引、连接方式、0。
完。

模糊规则_评估 接受 规则、所有隶属度：
    条件 为 规则 的 第 0 项。
    连接 为 规则 的 第 2 项。
    可变 强度 为 0。
    若 连接 等于 0：
        强度 为 模糊精度。
        可变 i 为 0。
        只要 i 小于 做 计数 用 条件：
            条件对 为 条件 的 第 i 项。
            变量idx 为 条件对 的 第 0 项。
            集合idx 为 条件对 的 第 1 项。
            度 为 所有隶属度 的 第 变量idx 项 的 第 集合idx 项。
            若 度 小于 强度：
                强度 为 度。
            完。
            i 为 i 加 1。
        完。
    否则：
        强度 为 0。
        可变 i 为 0。
        只要 i 小于 做 计数 用 条件：
            条件对 为 条件 的 第 i 项。
            变量idx 为 条件对 的 第 0 项。
            集合idx 为 条件对 的 第 1 项。
            度 为 所有隶属度 的 第 变量idx 项 的 第 集合idx 项。
            若 度 大于 强度：
                强度 为 度。
            完。
            i 为 i 加 1。
        完。
    完。
    规则 的 第 3 项 为 强度。
    返还 强度。
完。

Mamdani推理 接受 规则列表、所有隶属度、输出变量：
    规则数 为 做 计数 用 规则列表。
    输出集合数 为 做 计数 用 输出变量 的 第 3 项。
    可变 输出激活 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 输出集合数：
        做 追加 用 输出激活、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 规则数：
        强度 为 做 模糊规则_评估 用 规则列表 的 第 i 项、所有隶属度。
        结论idx 为 规则列表 的 第 i 项 的 第 1 项。
        若 强度 大于 输出激活 的 第 结论idx 项：
            输出激活 的 第 结论idx 项 为 强度。
        完。
        i 为 i 加 1。
    完。
    返还 输出激活。
完。

去模糊_重心 接受 输出变量、输出激活、分辨率：
    最小 为 输出变量 的 第 1 项。
    最大 为 输出变量 的 第 2 项。
    步长 为 (最大 减 最小) 除以 分辨率。
    若 步长 等于 0：
        返还 最小。
    完。
    可变 分子 为 0。
    可变 分母 为 0。
    可变 x 为 最小。
    只要 x 小于或等于 最大：
        可变 隶属度 为 0。
        集合列表 为 输出变量 的 第 3 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 集合列表：
            集合隶属 为 做 模糊集_计算隶属度 用 集合列表 的 第 i 项、x。
            可变 截断 为 集合隶属。
            若 截断 大于 输出激活 的 第 i 项：
                截断 为 输出激活 的 第 i 项。
            完。
            若 截断 大于 隶属度：
                隶属度 为 截断。
            完。
            i 为 i 加 1。
        完。
        分子 为 分子 加 x 乘 隶属度。
        分母 为 分母 加 隶属度。
        x 为 x 加 步长。
    完。
    若 分母 等于 0：
        返还 (最小 加 最大) 除以 2。
    完。
    返还 分子 除以 分母。
完。

去模糊_加权平均 接受 输出变量、输出激活：
    集合列表 为 输出变量 的 第 3 项。
    可变 分子 为 0。
    可变 分母 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合列表：
        参数 为 集合列表 的 第 i 项 的 第 2 项。
        可变 中心 为 0。
        若 集合列表 的 第 i 项 的 第 1 项 等于 1：
            中心 为 参数 的 第 1 项。
        否则若 集合列表 的 第 i 项 的 第 1 项 等于 2：
            中心 为 (参数 的 第 1 项 加 参数 的 第 2 项) 除以 2。
        否则若 集合列表 的 第 i 项 的 第 1 项 等于 3：
            中心 为 参数 的 第 0 项。
        否则：
            中心 为 参数 的 第 0 项。
        完。
        分子 为 分子 加 中心 乘 输出激活 的 第 i 项。
        分母 为 分母 加 输出激活 的 第 i 项。
        i 为 i 加 1。
    完。
    若 分母 等于 0：
        返还 0。
    完。
    返还 分子 除以 分母。
完。

区间二型_创建 接受 名称、下界函数、上界函数：
    返还 一组：名称、下界函数、上界函数。
完。

区间二型_隶属度 接受 集合、值：
    下 为 做 三角隶属 用 值、集合 的 第 1 项 的 第 0 项、集合 的 第 1 项 的 第 1 项、集合 的 第 1 项 的 第 2 项。
    上 为 做 三角隶属 用 值、集合 的 第 2 项 的 第 0 项、集合 的 第 2 项 的 第 1 项、集合 的 第 2 项 的 第 2 项。
    若 下 大于 上：
        返还 一组：上、下。
    完。
    返还 一组：下、上。
完。

区间二型_去模糊 接受 左端点集、右端点集：
    可变 右和 为 0。
    可变 左和 为 0。
    若 做 计数 用 左端点集 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 左端点集：
        左和 为 左和 加 左端点集 的 第 i 项。
        右和 为 右和 加 右端点集 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 (左和 加 右和) 除以 (2 乘 做 计数 用 左端点集)。
完。

FIS_创建 接受 名称：
    返还 一组：名称、一组：（空）、空、一组：（空）、50。
完。

FIS_添加输入 接受 系统、_变量：
    做 追加 用 系统 的 第 1 项、_变量。
    返还 系统。
完。

FIS_设置输出 接受 系统、_变量：
    系统 的 第 2 项 为 _变量。
    返还 系统。
完。

FIS_添加规则 接受 系统、规则：
    做 追加 用 系统 的 第 3 项、规则。
完。

FIS_推理 接受 系统、输入值列表：
    可变 所有隶属度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 系统 的 第 1 项：
        隶属度 为 做 模糊化 用 系统 的 第 1 项 的 第 i 项、输入值列表 的 第 i 项。
        做 追加 用 所有隶属度、隶属度。
        i 为 i 加 1。
    完。
    输出激活 为 做 Mamdani推理 用 系统 的 第 3 项、所有隶属度、系统 的 第 2 项。
    返还 做 去模糊_重心 用 系统 的 第 2 项、输出激活、系统 的 第 4 项。
完。

ANFIS_调节参数 接受 系统、输入、期望输出、学习率：
    实际输出 为 做 FIS_推理 用 系统、输入。
    误差 为 期望输出 减 实际输出。
    输出变量 为 系统 的 第 2 项。
    集合列表 为 输出变量 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合列表：
        集合 为 集合列表 的 第 i 项。
        参数 为 集合 的 第 2 项。
        若 集合 的 第 1 项 等于 1：
            参数 的 第 1 项 为 参数 的 第 1 项 加 误差 乘 学习率 除以 模糊精度。
        否则若 集合 的 第 1 项 等于 3：
            参数 的 第 0 项 为 参数 的 第 0 项 加 误差 乘 学习率 除以 模糊精度。
        完。
        i 为 i 加 1。
    完。
    返还 误差。
完。

模糊_并集 接受 A、B：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 A 并且 i 小于 做 计数 用 B：
        若 A 的 第 i 项 大于 B 的 第 i 项：
            做 追加 用 结果、A 的 第 i 项。
        否则：
            做 追加 用 结果、B 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模糊_交集 接受 A、B：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 A 并且 i 小于 做 计数 用 B：
        若 A 的 第 i 项 小于 B 的 第 i 项：
            做 追加 用 结果、A 的 第 i 项。
        否则：
            做 追加 用 结果、B 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模糊_补集 接受 A、最大值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 A：
        做 追加 用 结果、最大值 减 A 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模糊_去模糊化 接受 隶属度、值域：
    可变 分子 为 0。
    可变 分母 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 隶属度 并且 i 小于 做 计数 用 值域：
        分子 为 分子 加 隶属度 的 第 i 项 乘 值域 的 第 i 项。
        分母 为 分母 加 隶属度 的 第 i 项。
        i 为 i 加 1。
    完。
    若 分母 等于 0：
        返还 0。
    完。
    返还 分子 除以 分母。
完。

模糊_清晰度 接受 隶属度：
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 隶属度：
        若 隶属度 的 第 i 项 大于 最大：
            最大 为 隶属度 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

可变 _模糊_推理次数 为 0。

可变 _模糊_规则触发 为 0。

模糊_记录推理 接受 零参：
    _模糊_推理次数 为 _模糊_推理次数 加 1。
完。

模糊_记录规则 接受 零参：
    _模糊_规则触发 为 _模糊_规则触发 加 1。
完。

模糊_效率评估 接受 零参：
    若 _模糊_推理次数 等于 0：
        返还 「无推理记录」。
    完。
    平均规则 为 _模糊_规则触发 除以 _模糊_推理次数。
    若 平均规则 大于 20：
        返还 「规则密集(考虑规则剪枝)」。
    完。
    若 平均规则 大于 5：
        返还 「规则适中」。
    完。
    返还 「规则精简」。
完。

模糊_重置 接受 零参：
    _模糊_推理次数 为 0。
    _模糊_规则触发 为 0。
完。

模糊_模糊度评估 接受 隶属度：
    可变 模糊计数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 隶属度：
        若 隶属度 的 第 i 项 大于 200 并且 隶属度 的 第 i 项 小于 800：
            模糊计数 为 模糊计数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 隶属度 等于 0：
        返还 0。
    完。
    返还 模糊计数 乘 100 除以 做 计数 用 隶属度。
完。

模糊_锐度 接受 隶属度：
    返还 1000 减 做 模糊_模糊度评估 用 隶属度。
完。

本章 收束。
