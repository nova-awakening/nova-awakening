《概率计算引擎》

引用《..·基础》。

引用《..·数学·决策树》。

概率计算引擎_概率精度 为 10000。

概率值_创建 接受 取值列表、概率列表：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 概率列表：
        总和 为 总和 加 概率列表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 总和 等于 0：
        返还 一组：取值列表、概率列表。
    完。
    可变 归一化 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 概率列表：
        做 追加 用 归一化、概率列表 的 第 i 项 乘 概率计算引擎_概率精度 除以 总和。
        i 为 i 加 1。
    完。
    返还 一组：取值列表、归一化。
完。

概率值_确定 接受 值：
    返还 一组：一组：值、一组：概率计算引擎_概率精度。
完。

概率值_期望 接受 概率值：
    取值 为 概率值 的 第 0 项。
    概率 为 概率值 的 第 1 项。
    可变 期望 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 取值：
        期望 为 期望 加 取值 的 第 i 项 乘 概率 的 第 i 项 除以 概率计算引擎_概率精度。
        i 为 i 加 1。
    完。
    返还 期望。
完。

概率值_方差 接受 概率值：
    取值 为 概率值 的 第 0 项。
    概率 为 概率值 的 第 1 项。
    期望 为 做 概率值_期望 用 概率值。
    可变 平方期望 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 取值：
        平方期望 为 平方期望 加 取值 的 第 i 项 乘 取值 的 第 i 项 乘 概率 的 第 i 项 除以 概率计算引擎_概率精度。
        i 为 i 加 1。
    完。
    若 概率计算引擎_概率精度 等于 0：
        返还 0。
    完。
    返还 平方期望 减 期望 乘 期望 除以 概率计算引擎_概率精度。
完。

概率值_加 接受 A、B：
    取值A 为 A 的 第 0 项。
    概率A 为 A 的 第 1 项。
    取值B 为 B 的 第 0 项。
    概率B 为 B 的 第 1 项。
    可变 新取值 为 一组：（空）。
    可变 新概率 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 取值A：
        可变 j 为 0。
        只要 j 小于 做 计数 用 取值B：
            和值 为 取值A 的 第 i 项 加 取值B 的 第 j 项。
            联合概率 为 概率A 的 第 i 项 乘 概率B 的 第 j 项 除以 概率计算引擎_概率精度。
            可变 已有 为 0。
            可变 k 为 0。
            只要 k 小于 做 计数 用 新取值：
                若 新取值 的 第 k 项 等于 和值：
                    新概率 的 第 k 项 为 新概率 的 第 k 项 加 联合概率。
                    已有 为 1。
                完。
                k 为 k 加 1。
            完。
            若 已有 等于 0：
                做 追加 用 新取值、和值。
                做 追加 用 新概率、联合概率。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：新取值、新概率。
完。

概率值_乘 接受 A、B：
    取值A 为 A 的 第 0 项。
    概率A 为 A 的 第 1 项。
    取值B 为 B 的 第 0 项。
    概率B 为 B 的 第 1 项。
    可变 新取值 为 一组：（空）。
    可变 新概率 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 取值A：
        可变 j 为 0。
        只要 j 小于 做 计数 用 取值B：
            积值 为 取值A 的 第 i 项 乘 取值B 的 第 j 项 除以 概率计算引擎_概率精度。
            联合概率 为 概率A 的 第 i 项 乘 概率B 的 第 j 项 除以 概率计算引擎_概率精度。
            可变 已有 为 0。
            可变 k 为 0。
            只要 k 小于 做 计数 用 新取值：
                若 新取值 的 第 k 项 等于 积值：
                    新概率 的 第 k 项 为 新概率 的 第 k 项 加 联合概率。
                    已有 为 1。
                完。
                k 为 k 加 1。
            完。
            若 已有 等于 0：
                做 追加 用 新取值、积值。
                做 追加 用 新概率、联合概率。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：新取值、新概率。
完。

贝叶斯节点_创建 接受 名称、状态数：
    可变 先验 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 状态数：
        做 追加 用 先验、概率计算引擎_概率精度 除以 状态数。
        i 为 i 加 1。
    完。
    返还 一组：名称、状态数、先验、一组：（空）、一组：（空）、先验。
完。

贝叶斯节点_设CPT 接受 节点、CPT：
    节点 的 第 4 项 为 CPT。
完。

贝叶斯更新 接受 节点、似然：
    先验 为 节点 的 第 2 项。
    可变 后验 为 一组：（空）。
    可变 证据 为 0。
    状态数 为 节点 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 状态数：
        未归一 为 似然 的 第 i 项 乘 先验 的 第 i 项 除以 概率计算引擎_概率精度。
        做 追加 用 后验、未归一。
        证据 为 证据 加 未归一。
        i 为 i 加 1。
    完。
    若 证据 大于 0：
        i 为 0。
        只要 i 小于 状态数：
            后验 的 第 i 项 为 后验 的 第 i 项 乘 概率计算引擎_概率精度 除以 证据。
            i 为 i 加 1。
        完。
    完。
    节点 的 第 5 项 为 后验。
    返还 后验。
完。

贝叶斯序贯更新 接受 节点、观察序列：
    可变 i 为 0。
    只要 i 小于 做 计数 用 观察序列：
        做 贝叶斯更新 用 节点、观察序列 的 第 i 项。
        节点 的 第 2 项 为 节点 的 第 5 项。
        i 为 i 加 1。
    完。
    返还 节点 的 第 5 项。
完。

MCMC_采样 接受 目标分布、初始状态、步数、步长：
    可变 样本 为 一组：（空）。
    可变 当前 为 初始状态。
    可变 当前概率 为 0。
    可变 k 为 0。
    只要 k 小于 做 计数 用 目标分布 的 第 0 项：
        可变 差 为 当前 减 目标分布 的 第 0 项 的 第 k 项。
        若 差 小于 0：
            差 为 0 减 差。
        完。
        若 差 小于 步长：
            当前概率 为 目标分布 的 第 1 项 的 第 k 项。
        完。
        k 为 k 加 1。
    完。
    可变 t 为 0。
    只要 t 小于 步数：
        扰动 为 (t 乘 1103515245 加 12345) 取余于 (步长 乘 2) 减 步长。
        候选 为 当前 加 扰动。
        可变 候选概率 为 0。
        k 为 0。
        只要 k 小于 做 计数 用 目标分布 的 第 0 项：
            可变 差 为 候选 减 目标分布 的 第 0 项 的 第 k 项。
            若 差 小于 0：
                差 为 0 减 差。
            完。
            若 差 小于 步长：
                候选概率 为 目标分布 的 第 1 项 的 第 k 项。
            完。
            k 为 k 加 1。
        完。
        可变 接受 为 0。
        若 当前概率 等于 0 或者 候选概率 大于或等于 当前概率：
            接受 为 1。
        否则：
            比率 为 候选概率 乘 概率计算引擎_概率精度 除以 当前概率。
            随机 为 (t 乘 48271 加 7) 取余于 概率计算引擎_概率精度。
            若 随机 小于 比率：
                接受 为 1。
            完。
        完。
        若 接受 等于 1：
            当前 为 候选。
            当前概率 为 候选概率。
        完。
        做 追加 用 样本、当前。
        t 为 t 加 1。
    完。
    返还 样本。
完。

概率计算_区间_创建 接受 下、上：
    若 下 大于 上：
        返还 一组：上、下。
    完。
    返还 一组：下、上。
完。

概率计算_区间_加 接受 A、B：
    返还 一组：A 的 第 0 项 加 B 的 第 0 项、A 的 第 1 项 加 B 的 第 1 项。
完。

概率计算_区间_乘 接受 A、B：
    p1 为 A 的 第 0 项 乘 B 的 第 0 项 除以 概率计算引擎_概率精度。
    p2 为 A 的 第 0 项 乘 B 的 第 1 项 除以 概率计算引擎_概率精度。
    p3 为 A 的 第 1 项 乘 B 的 第 0 项 除以 概率计算引擎_概率精度。
    p4 为 A 的 第 1 项 乘 B 的 第 1 项 除以 概率计算引擎_概率精度。
    可变 最小 为 p1。
    若 p2 小于 最小：
        最小 为 p2。
    完。
    若 p3 小于 最小：
        最小 为 p3。
    完。
    若 p4 小于 最小：
        最小 为 p4。
    完。
    可变 最大 为 p1。
    若 p2 大于 最大：
        最大 为 p2。
    完。
    若 p3 大于 最大：
        最大 为 p3。
    完。
    若 p4 大于 最大：
        最大 为 p4。
    完。
    返还 一组：最小、最大。
完。

区间_不确定性 接受 区间：
    返还 区间 的 第 1 项 减 区间 的 第 0 项。
完。

定点log2 接受 x：
    若 x 小于或等于 0：
        返还 0 减 概率计算引擎_概率精度 乘 20。
    完。
    可变 整数部 为 0。
    可变 v 为 x。
    只要 v 大于或等于 概率计算引擎_概率精度 乘 2：
        v 为 v 除以 2。
        整数部 为 整数部 加 1。
    完。
    只要 v 小于 概率计算引擎_概率精度：
        v 为 v 乘 2。
        整数部 为 整数部 减 1。
    完。
    小数部 为 (v 减 概率计算引擎_概率精度) 乘 概率计算引擎_概率精度 除以 概率计算引擎_概率精度。
    返还 整数部 乘 概率计算引擎_概率精度 加 小数部。
完。

概率计算_信息熵 接受 概率分布：
    可变 熵 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 概率分布：
        p 为 概率分布 的 第 i 项。
        若 p 大于 0 并且 p 小于或等于 概率计算引擎_概率精度：
            log值 为 做 定点log2 用 p。
            熵 为 熵 减 p 乘 log值 除以 概率计算引擎_概率精度。
        完。
        i 为 i 加 1。
    完。
    若 概率计算引擎_概率精度 等于 0：
        返还 0。
    完。
    返还 熵 加 做 计数 用 概率分布 乘 概率计算引擎_概率精度 乘 做 定点log2 用 概率计算引擎_概率精度 除以 概率计算引擎_概率精度。
完。

概率计算引擎_KL散度 接受 P、Q：
    可变 散度 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 P：
        若 P 的 第 i 项 大于 0 并且 Q 的 第 i 项 大于 0：
            比值 为 P 的 第 i 项 乘 概率计算引擎_概率精度 除以 Q 的 第 i 项。
            log比 为 做 定点log2 用 比值。
            散度 为 散度 加 P 的 第 i 项 乘 log比 除以 概率计算引擎_概率精度。
        完。
        i 为 i 加 1。
    完。
    返还 散度。
完。

概率计算引擎_互信息 接受 联合分布、边缘X、边缘Y：
    HX 为 做 信息熵 用 边缘X、0。
    HY 为 做 信息熵 用 边缘Y、0。
    HXY 为 做 信息熵 用 联合分布、0。
    返还 HX 加 HY 减 HXY。
完。

概率采样 接受 分布、种子：
    取值 为 分布 的 第 0 项。
    概率 为 分布 的 第 1 项。
    随机 为 (种子 乘 1103515245 加 12345) 取余于 概率计算引擎_概率精度。
    可变 累积 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 取值：
        累积 为 累积 加 概率 的 第 i 项。
        若 随机 小于 累积：
            返还 取值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 取值 等于 0：
        返还 空。
    完。
    返还 取值 的 第 做 计数 用 取值 减 1 项。
完。

概率条件化 接受 分布、条件函数结果：
    取值 为 分布 的 第 0 项。
    概率 为 分布 的 第 1 项。
    可变 新取值 为 一组：（空）。
    可变 新概率 为 一组：（空）。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 取值：
        若 条件函数结果 的 第 i 项 大于 0：
            做 追加 用 新取值、取值 的 第 i 项。
            做 追加 用 新概率、概率 的 第 i 项。
            总和 为 总和 加 概率 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 总和 大于 0：
        i 为 0。
        只要 i 小于 做 计数 用 新概率：
            新概率 的 第 i 项 为 新概率 的 第 i 项 乘 概率计算引擎_概率精度 除以 总和。
            i 为 i 加 1。
        完。
    完。
    返还 一组：新取值、新概率。
完。

贝叶斯_后验 接受 先验、似然、证据：
    若 证据 等于 0：
        返还 0。
    完。
    返还 先验 乘 似然 除以 证据。
完。

贝叶斯_链式更新 接受 先验、似然列表：
    可变 当前 为 先验。
    可变 i 为 0。
    只要 i 小于 做 计数 用 似然列表：
        当前 为 当前 乘 似然列表 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 当前。
完。

全概率 接受 先验列表、条件列表：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 先验列表：
        总 为 总 加 先验列表 的 第 i 项 乘 条件列表 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 总。
完。

概率计算_交叉熵 接受 真实、预测：
    可变 ce 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 真实：
        若 预测 的 第 i 项 大于 0：
            可变 对数 为 0。
            可变 v 为 预测 的 第 i 项。
            只要 v 大于或等于 2000：
                v 为 v 除以 2。
                对数 为 对数 加 1000。
            完。
            ce 为 ce 减 真实 的 第 i 项 乘 对数 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 ce。
完。

可变 _prob_计算次数 为 0。

可变 _prob_推理次数 为 0。

prob_记录计算 接受 零参：
    _prob_计算次数 为 _prob_计算次数 加 1。
完。

prob_记录推理 接受 零参：
    _prob_推理次数 为 _prob_推理次数 加 1。
完。

prob_复杂度评估 接受 零参：
    若 _prob_推理次数 等于 0：
        返还 「无推理活动」。
    完。
    若 _prob_推理次数 大于 1000：
        返还 「高频推理(注意性能)」。
    完。
    若 _prob_推理次数 大于 100：
        返还 「中频推理」。
    完。
    返还 「低频推理」。
完。

本章 收束。
