《文本处理》

引用《..·基础》。

引用《..·..·编译器·词法·字符工具》。

是标识符 接受 编码值：
    返还 做 是字母 用 编码值 或者 做 是数字 用 编码值 或者 编码值 等于 95。
完。

转大写字符 接受 c：
    v 为 做 编码 用 c。
    若 做 是小写 用 v：
        返还 做 字符 用 v 减 32。
    完。
    返还 c。
完。

转小写字符 接受 c：
    v 为 做 编码 用 c。
    若 做 是大写 用 v：
        返还 做 字符 用 v 加 32。
    完。
    返还 c。
完。

文本处理_去空格 接受 文本：
    n 为 做 计数 用 文本。
    若 n 等于 0：
        返还 文本。
    完。
    可变 起 为 0。
    可变 _起找到 为 假。
    只要 起 小于 n 并且 _起找到 等于 假：
        c 为 做 编码 用 做 子串 用 文本、起、1。
        若 做 是空白 用 c 等于 假：
            _起找到 为 真。
        否则：
            起 为 起 加 1。
        完。
    完。
    可变 终 为 n 减 1。
    可变 _终找到 为 假。
    只要 终 大于或等于 起 并且 _终找到 等于 假：
        c 为 做 编码 用 做 子串 用 文本、终、1。
        若 做 是空白 用 c 等于 假：
            _终找到 为 真。
        否则：
            终 为 终 减 1。
        完。
    完。
    若 起 大于 终：
        返还 「」。
    完。
    返还 做 子串 用 文本、起、终 减 起 加 1。
完。

文本处理_分割 接受 文本、分隔符：
    n 为 做 计数 用 文本。
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 文本、i、1。
        若 做 字符串等于 用 c、分隔符：
            做 追加 用 结果、当前。
            当前 为 「」。
        否则：
            当前 为 做 字符串拼接 用 当前、c。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 结果、当前。
    返还 结果。
完。

合并 接受 列表、分隔符：
    n 为 做 计数 用 列表。
    若 n 等于 0：
        返还 「」。
    完。
    可变 结果 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        结果 为 做 字符串拼接 用 结果、分隔符。
        结果 为 做 字符串拼接 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

重复文本 接受 文本、次数：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 次数：
        结果 为 做 字符串拼接 用 结果、文本。
        i 为 i 加 1。
    完。
    返还 结果。
完。

包含 接受 文本、子串文本：
    返还 做 查找 用 文本、子串文本 大于或等于 0。
完。

查找 接受 文本、目标：
    n 为 做 计数 用 文本。
    m 为 做 计数 用 目标。
    若 m 大于 n：
        返还 负 1。
    完。
    可变 i 为 0。
    只要 i 小于或等于 n 减 m：
        候选 为 做 子串 用 文本、i、m。
        若 做 字符串等于 用 候选、目标：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

出现次数 接受 文本、目标：
    n 为 做 计数 用 文本。
    m 为 做 计数 用 目标。
    若 m 大于 n 或者 m 等于 0：
        返还 0。
    完。
    可变 次数 为 0。
    可变 i 为 0。
    只要 i 小于或等于 n 减 m：
        若 做 字符串等于 用 做 子串 用 文本、i、m、目标：
            次数 为 次数 加 1。
            i 为 i 加 m。
        否则：
            i 为 i 加 1。
        完。
    完。
    返还 次数。
完。

开头是 接受 文本、前缀：
    m 为 做 计数 用 前缀。
    若 做 计数 用 文本 小于 m：
        返还 假。
    完。
    返还 做 字符串等于 用 做 子串 用 文本、0、m、前缀。
完。

结尾是 接受 文本、后缀：
    n 为 做 计数 用 文本。
    m 为 做 计数 用 后缀。
    若 n 小于 m：
        返还 假。
    完。
    返还 做 字符串等于 用 做 子串 用 文本、n 减 m、m、后缀。
完。

文本处理_左填充 接受 文本、总长、填充字符：
    n 为 做 计数 用 文本。
    若 n 大于或等于 总长：
        返还 文本。
    完。
    返还 做 字符串拼接 用 做 重复文本 用 填充字符、总长 减 n、文本。
完。

文本处理_右填充 接受 文本、总长、填充字符：
    n 为 做 计数 用 文本。
    若 n 大于或等于 总长：
        返还 文本。
    完。
    返还 做 字符串拼接 用 文本、做 重复文本 用 填充字符、总长 减 n。
完。

反转字符串 接受 文本：
    n 为 做 计数 用 文本。
    可变 结果 为 「」。
    可变 i 为 n 减 1。
    只要 i 大于或等于 0：
        结果 为 做 字符串拼接 用 结果、做 子串 用 文本、i、1。
        i 为 i 减 1。
    完。
    返还 结果。
完。

文本处理_转十六进制 接受 值：
    若 值 等于 0：
        返还 「0」。
    完。
    十六进制字符 为 「0123456789ABCDEF」。
    可变 结果 为 「」。
    可变 v 为 值。
    若 v 小于 0：
        v 为 0 减 v。
    完。
    只要 v 大于 0：
        余 为 v 取余于 16。
        结果 为 做 字符串拼接 用 做 子串 用 十六进制字符、余、1、结果。
        v 为 v 除以 16。
    完。
    若 值 小于 0：
        结果 为 做 字符串拼接 用 「-」、结果。
    完。
    返还 结果。
完。

转二进制 接受 值：
    若 值 等于 0：
        返还 「0」。
    完。
    可变 结果 为 「」。
    可变 v 为 值。
    若 v 小于 0：
        v 为 0 减 v。
    完。
    只要 v 大于 0：
        若 v 取余于 2 等于 0：
            结果 为 做 字符串拼接 用 「0」、结果。
        否则：
            结果 为 做 字符串拼接 用 「1」、结果。
        完。
        v 为 v 除以 2。
    完。
    若 值 小于 0：
        结果 为 做 字符串拼接 用 「-」、结果。
    完。
    返还 结果。
完。

模板渲染 接受 模板、_变量字典：
    可变 结果 为 模板。
    键列表 为 做 字典键 用 _变量字典。
    可变 ki 为 0。
    只要 ki 小于 做 计数 用 键列表：
        键 为 键列表 的 第 ki 项。
        占位符 为 做 字符串拼接 用 「{」、做 字符串拼接 用 键、「}」。
        值文本 为 做 转字符串 用 做 字典取 用 _变量字典、键。
        结果 为 做 _模板_替换全部 用 结果、占位符、值文本。
        ki 为 ki 加 1。
    完。
    返还 结果。
完。

_模板_替换全部 接受 源、目标、替换值：
    目标长 为 做 计数 用 目标。
    若 目标长 等于 0：
        返还 源。
    完。
    可变 结果 为 「」。
    可变 位置 为 0。
    源长 为 做 计数 用 源。
    只要 位置 小于或等于 源长 减 目标长：
        若 做 子串 用 源、位置、目标长 等于 目标：
            结果 为 做 字符串拼接 用 结果、替换值。
            位置 为 位置 加 目标长。
        否则：
            结果 为 做 字符串拼接 用 结果、做 子串 用 源、位置、1。
            位置 为 位置 加 1。
        完。
    完。
    只要 位置 小于 源长：
        结果 为 做 字符串拼接 用 结果、做 子串 用 源、位置、1。
        位置 为 位置 加 1。
    完。
    返还 结果。
完。

编辑距离 接受 a、b：
    m 为 做 计数 用 a。
    n 为 做 计数 用 b。
    可变 dp 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于或等于 n：
        做 追加 用 dp、j。
        j 为 j 加 1。
    完。
    可变 i 为 1。
    只要 i 小于或等于 m：
        可变 前一个 为 dp 的 第 0 项。
        dp 的 第 0 项 为 i。
        j 为 1。
        只要 j 小于或等于 n：
            暂存 为 dp 的 第 j 项。
            若 做 字符串等于 用 做 子串 用 a、i 减 1、1、做 子串 用 b、j 减 1、1：
                dp 的 第 j 项 为 前一个。
            否则：
                可变 最小 为 前一个。
                若 dp 的 第 j 项 小于 最小：
                    最小 为 dp 的 第 j 项。
                完。
                若 dp 的 第 j 减 1 项 小于 最小：
                    最小 为 dp 的 第 j 减 1 项。
                完。
                dp 的 第 j 项 为 最小 加 1。
            完。
            前一个 为 暂存。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 dp 的 第 n 项。
完。

本章 收束。
