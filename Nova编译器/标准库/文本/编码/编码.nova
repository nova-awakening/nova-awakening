《编码》

引用《..·..·基础》。

BASE64_表 为 「ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/」。

HEX_表 为 「0123456789abcdef」。

utf8_编码 接受 码点：
    若 码点 小于 0：
        返还 一组：（空）。
    完。
    若 码点 小于或等于 127：
        返还 一组：码点。
    完。
    若 码点 小于或等于 2047：
        返还 一组：192 加 码点 除以 64、128 加 码点 取余于 64。
    完。
    若 码点 小于或等于 65535：
        若 码点 大于或等于 55296：
            若 码点 小于或等于 57343：
                返还 一组：239、191、189。
            完。
        完。
        返还 一组：224 加 码点 除以 4096、128 加 码点 除以 64 取余于 64、128 加 码点 取余于 64。
    完。
    若 码点 小于或等于 1114111：
        返还 一组：240 加 码点 除以 262144、128 加 码点 除以 4096 取余于 64、128 加 码点 除以 64 取余于 64、128 加 码点 取余于 64。
    完。
    返还 一组：239、191、189。
完。

utf8_是续字节 接受 字节：
    头 为 字节 除以 64。
    若 头 等于 2：
        返还 真。
    完。
    返还 假。
完。

url_是安全字符 接受 c：
    若 c 大于或等于 字符_大写A：
        若 c 小于或等于 字符_大写Z：
            返还 真。
        完。
    完。
    若 c 大于或等于 字符_小写a：
        若 c 小于或等于 字符_小写z：
            返还 真。
        完。
    完。
    若 c 大于或等于 字符_零：
        若 c 小于或等于 字符_九：
            返还 真。
        完。
    完。
    若 c 等于 字符_减号：
        返还 真。
    完。
    若 c 等于 字符_下划线：
        返还 真。
    完。
    若 c 等于 字符_句点：
        返还 真。
    完。
    若 c 等于 126：
        返还 真。
    完。
    返还 假。
完。

utf8_解码 接受 字节列表、起始位置：
    若 起始位置 大于或等于 做 计数 用 字节列表：
        返还 一组：负 1、0。
    完。
    b0 为 字节列表 的 第 起始位置 项。
    若 b0 小于 128：
        返还 一组：b0、1。
    完。
    若 b0 大于或等于 192 并且 b0 小于 224：
        若 起始位置 加 1 大于或等于 做 计数 用 字节列表：
            返还 一组：负 1、1。
        完。
        b1 为 字节列表 的 第 起始位置 加 1 项。
        若 做 utf8_是续字节 用 b1 等于 假：
            返还 一组：负 1、1。
        完。
        码点 为 (b0 减 192) 乘 64 加 (b1 减 128)。
        若 码点 小于 128：
            返还 一组：负 1、2。
        完。
        返还 一组：码点、2。
    完。
    若 b0 大于或等于 224 并且 b0 小于 240：
        若 起始位置 加 2 大于或等于 做 计数 用 字节列表：
            返还 一组：负 1、1。
        完。
        b1 为 字节列表 的 第 起始位置 加 1 项。
        b2 为 字节列表 的 第 起始位置 加 2 项。
        若 做 utf8_是续字节 用 b1 等于 假：
            返还 一组：负 1、1。
        完。
        若 做 utf8_是续字节 用 b2 等于 假：
            返还 一组：负 1、1。
        完。
        码点 为 (b0 减 224) 乘 4096 加 (b1 减 128) 乘 64 加 (b2 减 128)。
        若 码点 小于 2048：
            返还 一组：负 1、3。
        完。
        若 码点 大于或等于 55296：
            若 码点 小于或等于 57343：
                返还 一组：负 1、3。
            完。
        完。
        返还 一组：码点、3。
    完。
    若 b0 大于或等于 240 并且 b0 小于 248：
        若 起始位置 加 3 大于或等于 做 计数 用 字节列表：
            返还 一组：负 1、1。
        完。
        b1 为 字节列表 的 第 起始位置 加 1 项。
        b2 为 字节列表 的 第 起始位置 加 2 项。
        b3 为 字节列表 的 第 起始位置 加 3 项。
        若 做 utf8_是续字节 用 b1 等于 假：
            返还 一组：负 1、1。
        完。
        若 做 utf8_是续字节 用 b2 等于 假：
            返还 一组：负 1、1。
        完。
        若 做 utf8_是续字节 用 b3 等于 假：
            返还 一组：负 1、1。
        完。
        码点 为 (b0 减 240) 乘 262144 加 (b1 减 128) 乘 4096 加 (b2 减 128) 乘 64 加 (b3 减 128)。
        若 码点 小于 65536：
            返还 一组：负 1、4。
        完。
        若 码点 大于 1114111：
            返还 一组：负 1、4。
        完。
        返还 一组：码点、4。
    完。
    返还 一组：负 1、1。
完。

utf8_字符串到字节 接受 文本：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        码点 为 做 编码 用 做 子串 用 文本、i、1。
        字节 为 做 utf8_编码 用 码点。
        可变 j 为 0。
        只要 j 小于 做 计数 用 字节：
            做 追加 用 结果、字节 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

utf8_字节到码点列表 接受 字节列表：
    可变 结果 为 一组：（空）。
    可变 位置 为 0。
    只要 位置 小于 做 计数 用 字节列表：
        项 为 做 utf8_解码 用 字节列表、位置。
        若 项 的 第 0 项 大于或等于 0：
            做 追加 用 结果、项 的 第 0 项。
        否则：
            做 追加 用 结果、65533。
        完。
        若 项 的 第 1 项 小于或等于 0：
            位置 为 位置 加 1。
        否则：
            位置 为 位置 加 项 的 第 1 项。
        完。
    完。
    返还 结果。
完。

utf8_字节长度 接受 码点：
    若 码点 小于 128：
        返还 1。
    完。
    若 码点 小于 2048：
        返还 2。
    完。
    若 码点 小于 65536：
        返还 3。
    完。
    返还 4。
完。

utf8_有效 接受 字节列表：
    可变 位置 为 0。
    只要 位置 小于 做 计数 用 字节列表：
        项 为 做 utf8_解码 用 字节列表、位置。
        若 项 的 第 0 项 小于 0：
            返还 假。
        完。
        位置 为 位置 加 项 的 第 1 项。
    完。
    返还 真。
完。

base64_索引 接受 字符：
    若 做 字符串等于 用 字符、「=」：
        返还 负 1。
    完。
    可变 i 为 0。
    只要 i 小于 64：
        若 做 字符串等于 用 做 子串 用 BASE64_表、i、1、字符：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

base64_编码 接受 字节列表：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字节列表：
        b0 为 字节列表 的 第 i 项。
        可变 b1 为 0。
        可变 b2 为 0。
        可变 填充 为 0。
        若 i 加 1 小于 做 计数 用 字节列表：
            b1 为 字节列表 的 第 i 加 1 项。
        否则：
            填充 为 填充 加 1。
        完。
        若 i 加 2 小于 做 计数 用 字节列表：
            b2 为 字节列表 的 第 i 加 2 项。
        否则：
            填充 为 填充 加 1。
        完。
        idx0 为 b0 除以 4。
        idx1 为 b0 取余于 4 乘 16 加 b1 除以 16。
        idx2 为 b1 取余于 16 乘 4 加 b2 除以 64。
        idx3 为 b2 取余于 64。
        结果 为 做 字符串拼接 用 结果、做 子串 用 BASE64_表、idx0、1。
        结果 为 做 字符串拼接 用 结果、做 子串 用 BASE64_表、idx1、1。
        若 填充 小于 2：
            结果 为 做 字符串拼接 用 结果、做 子串 用 BASE64_表、idx2、1。
        否则：
            结果 为 做 字符串拼接 用 结果、「=」。
        完。
        若 填充 小于 1：
            结果 为 做 字符串拼接 用 结果、做 子串 用 BASE64_表、idx3、1。
        否则：
            结果 为 做 字符串拼接 用 结果、「=」。
        完。
        i 为 i 加 3。
    完。
    返还 结果。
完。

base64_解码 接受 文本：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 3 小于 做 计数 用 文本：
        c0 为 做 base64_索引 用 做 子串 用 文本、i、1。
        c1 为 做 base64_索引 用 做 子串 用 文本、i 加 1、1。
        c2 为 做 base64_索引 用 做 子串 用 文本、i 加 2、1。
        c3 为 做 base64_索引 用 做 子串 用 文本、i 加 3、1。
        若 c0 小于 0：
            i 为 i 加 4。
        否则若 c1 小于 0：
            i 为 i 加 4。
        否则：
            做 追加 用 结果、c0 乘 4 加 c1 除以 16。
            若 c2 大于或等于 0：
                做 追加 用 结果、c1 取余于 16 乘 16 加 c2 除以 4。
            完。
            若 c2 大于或等于 0：
                若 c3 大于或等于 0：
                    做 追加 用 结果、c2 取余于 4 乘 64 加 c3。
                完。
            完。
            i 为 i 加 4。
        完。
    完。
    返还 结果。
完。

hex_值 接受 字符：
    可变 i 为 0。
    只要 i 小于 16：
        若 做 字符串等于 用 做 子串 用 HEX_表、i、1、字符：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    大写HEX 为 「ABCDEF」。
    i 为 0。
    只要 i 小于 6：
        若 做 字符串等于 用 做 子串 用 大写HEX、i、1、字符：
            返还 i 加 10。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

hex_编码 接受 字节列表：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 字节列表：
        可变 b 为 字节列表 的 第 i 项。
        若 b 小于 0：
            b 为 b 加 256。
        完。
        结果 为 做 字符串拼接 用 结果、做 子串 用 HEX_表、b 除以 16、1。
        结果 为 做 字符串拼接 用 结果、做 子串 用 HEX_表、b 取余于 16、1。
        i 为 i 加 1。
    完。
    返还 结果。
完。

hex_解码 接受 文本：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 1 小于 做 计数 用 文本：
        高 为 做 hex_值 用 做 子串 用 文本、i、1。
        低 为 做 hex_值 用 做 子串 用 文本、i 加 1、1。
        若 高 大于或等于 0：
            若 低 大于或等于 0：
                做 追加 用 结果、高 乘 16 加 低。
            否则：
            完。
        否则：
        完。
        i 为 i 加 2。
    完。
    返还 结果。
完。

url_编码 接受 文本：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        c 为 做 编码 用 做 子串 用 文本、i、1。
        若 做 url_是安全字符 用 c：
            结果 为 做 字符串拼接 用 结果、做 子串 用 文本、i、1。
        否则若 c 等于 字符_空格：
            结果 为 做 字符串拼接 用 结果、「+」。
        否则：
            字节 为 做 utf8_编码 用 c。
            可变 j 为 0。
            只要 j 小于 做 计数 用 字节：
                b 为 字节 的 第 j 项。
                结果 为 做 字符串拼接 用 结果、「%」。
                结果 为 做 字符串拼接 用 结果、做 子串 用 HEX_表、b 除以 16、1。
                结果 为 做 字符串拼接 用 结果、做 子串 用 HEX_表、b 取余于 16、1。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

url_解码 接受 文本：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        c 为 做 子串 用 文本、i、1。
        若 做 字符串等于 用 c、「%」：
            若 i 加 2 大于或等于 做 计数 用 文本：
                i 为 i 加 1。
            否则：
                高 为 做 hex_值 用 做 子串 用 文本、i 加 1、1。
                低 为 做 hex_值 用 做 子串 用 文本、i 加 2、1。
                若 高 大于或等于 0：
                    若 低 大于或等于 0：
                        做 追加 用 结果、高 乘 16 加 低。
                    否则：
                    完。
                否则：
                完。
                i 为 i 加 3。
            完。
        否则若 做 字符串等于 用 c、「+」：
            做 追加 用 结果、32。
            i 为 i 加 1。
        否则：
            做 追加 用 结果、做 编码 用 c。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

本章 收束。
