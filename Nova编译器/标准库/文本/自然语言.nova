《自然语言》

引用《..·基础》。

引用《..·..·运行时·原语桩》。

nlp精度 为 1000。

词频 接受 文档、_扩2：
    可变 频率表 为 做 字典。
    n 为 做 计数 用 文档。
    可变 i 为 0。
    只要 i 小于 n：
        词 为 文档 的 第 i 项。
        现有 为 做 字典取 用 频率表、词。
        若 现有 等于 空：
            做 字典设 用 频率表、词、1。
        否则：
            做 字典设 用 频率表、词、现有 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 频率表。
完。

逆文档频率 接受 文档集、词：
    总文档 为 做 计数 用 文档集。
    可变 包含数 为 0。
    可变 i 为 0。
    只要 i 小于 总文档：
        频率 为 做 词频 用 文档集 的 第 i 项、0。
        若 做 字典取 用 频率、词 不等于 空：
            包含数 为 包含数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 包含数 等于 0：
        返还 0。
    完。
    返还 做 自然语言_对数 用 总文档 乘 nlp精度 除以 包含数。
完。

词频逆文档频率 接受 文档、文档集、词：
    返还 做 词频 用 文档、词 乘 做 逆文档频率 用 文档集、词 除以 nlp精度。
完。

字符ngram 接受 文本、n：
    长度 为 做 计数 用 文本。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 长度 减 n：
        做 追加 用 结果、做 子串 用 文本、i、n。
        i 为 i 加 1。
    完。
    返还 结果。
完。

词ngram 接受 词列表、n：
    长度 为 做 计数 用 词列表。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 长度 减 n：
        可变 gram 为 词列表 的 第 i 项。
        可变 j 为 1。
        只要 j 小于 n：
            gram 为 做 字符串拼接 用 gram、做 字符串拼接 用 「 」、词列表 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、gram。
        i 为 i 加 1。
    完。
    返还 结果。
完。

jaccard相似 接受 词集a、词集b：
    可变 交集 为 0。
    可变 并集字典 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词集a：
        做 字典设 用 并集字典、词集a 的 第 i 项、1。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 词集b：
        若 做 字典取 用 并集字典、词集b 的 第 i 项 不等于 空：
            交集 为 交集 加 1。
        完。
        做 字典设 用 并集字典、词集b 的 第 i 项、1。
        i 为 i 加 1。
    完。
    并集 为 做 计数 用 词集a 加 做 计数 用 词集b 减 交集。
    若 并集 等于 0：
        返还 0。
    完。
    返还 交集 乘 nlp精度 除以 并集。
完。

余弦相似 接受 文档a、文档b：
    freq_a 为 做 词频 用 文档a、0。
    freq_b 为 做 词频 用 文档b、0。
    可变 所有词 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档a：
        做 字典设 用 所有词、文档a 的 第 i 项、1。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 文档b：
        做 字典设 用 所有词、文档b 的 第 i 项、1。
        i 为 i 加 1。
    完。
    可变 点积 为 0。
    可变 范数a 为 0。
    可变 范数b 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 文档a：
        词 为 文档a 的 第 i 项。
        可变 fa 为 做 字典取 用 freq_a、词。
        可变 fb 为 做 字典取 用 freq_b、词。
        若 fa 等于 空：
            fa 为 0。
        完。
        若 fb 等于 空：
            fb 为 0。
        完。
        点积 为 点积 加 fa 乘 fb。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 文档a：
        可变 fa2 为 做 字典取 用 freq_a、文档a 的 第 i 项。
        若 fa2 等于 空：
            fa2 为 0。
        完。
        范数a 为 范数a 加 fa2 乘 fa2。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 文档b：
        可变 fb2 为 做 字典取 用 freq_b、文档b 的 第 i 项。
        若 fb2 等于 空：
            fb2 为 0。
        完。
        范数b 为 范数b 加 fb2 乘 fb2。
        i 为 i 加 1。
    完。
    分母 为 做 nlp_开方 用 范数a 乘 做 nlp_开方 用 范数b。
    若 分母 等于 0：
        返还 0。
    完。
    返还 点积 乘 nlp精度 除以 分母。
完。

简单分词 接受 文本：
    n 为 做 计数 用 文本。
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 文本、i、1。
        若 做 字符串等于 用 c、「 」 或者 做 字符串等于 用 c、「\t」 或者 做 字符串等于 用 c、「\n」：
            若 做 计数 用 当前 大于 0：
                做 追加 用 结果、当前。
                当前 为 「」。
            完。
        否则：
            当前 为 做 字符串拼接 用 当前、c。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前 大于 0：
        做 追加 用 结果、当前。
    完。
    返还 结果。
完。

去停用词 接受 词列表、停用词表：
    可变 停用词字典 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 停用词表：
        做 字典设 用 停用词字典、停用词表 的 第 i 项、1。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 词列表：
        若 做 字典取 用 停用词字典、词列表 的 第 i 项 等于 空：
            做 追加 用 结果、词列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

词袋 接受 文档列表：
    可变 词汇 为 做 字典。
    可变 词索引 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文档列表：
        可变 j 为 0。
        只要 j 小于 做 计数 用 文档列表 的 第 i 项：
            词 为 文档列表 的 第 i 项 的 第 j 项。
            若 做 字典取 用 词汇、词 等于 空：
                做 字典设 用 词汇、词、词索引。
                词索引 为 词索引 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 向量列表 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 文档列表：
        可变 向量 为 一组：（空）。
        可变 k 为 0。
        只要 k 小于 词索引：
            做 追加 用 向量、0。
            k 为 k 加 1。
        完。
        可变 j2 为 0。
        只要 j2 小于 做 计数 用 文档列表 的 第 i 项：
            索引 为 做 字典取 用 词汇、文档列表 的 第 i 项 的 第 j2 项。
            若 索引 不等于 空：
                向量 的 第 索引 项 为 向量 的 第 索引 项 加 1。
            完。
            j2 为 j2 加 1。
        完。
        做 追加 用 向量列表、向量。
        i 为 i 加 1。
    完。
    返还 一组：词汇、向量列表。
完。

情感分数 接受 词列表、正面词典、负面词典：
    可变 正分 为 0。
    可变 负分 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词列表：
        若 做 字典取 用 正面词典、词列表 的 第 i 项 不等于 空：
            正分 为 正分 加 做 字典取 用 正面词典、词列表 的 第 i 项。
        完。
        若 做 字典取 用 负面词典、词列表 的 第 i 项 不等于 空：
            负分 为 负分 加 做 字典取 用 负面词典、词列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 正分 减 负分。
完。

自然语言_对数 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 结果 为 0。
    可变 v 为 x。
    只要 v 大于 2 乘 nlp精度：
        v 为 v 除以 2。
        结果 为 结果 加 693。
    完。
    只要 v 大于 nlp精度：
        v 为 v 乘 nlp精度 除以 1414。
        结果 为 结果 加 347。
    完。
    返还 结果。
完。

nlp_开方 接受 n：
    返还 做 开方 用 n。
完。

本章 收束。
