《模式匹配》

引用《..·基础》。

通配符匹配 接受 文本、模式：
    tn 为 做 计数 用 文本。
    pn 为 做 计数 用 模式。
    可变 dp 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于或等于 pn：
        做 追加 用 dp、假。
        j 为 j 加 1。
    完。
    dp 的 第 0 项 为 真。
    j 为 1。
    只要 j 小于或等于 pn：
        若 做 字符串等于 用 做 子串 用 模式、j 减 1、1、「*」：
            dp 的 第 j 项 为 dp 的 第 j 减 1 项。
        完。
        j 为 j 加 1。
    完。
    可变 i 为 1。
    只要 i 小于或等于 tn：
        可变 新dp 为 一组：（空）。
        j 为 0。
        只要 j 小于或等于 pn：
            做 追加 用 新dp、假。
            j 为 j 加 1。
        完。
        j 为 1。
        只要 j 小于或等于 pn：
            pc 为 做 子串 用 模式、j 减 1、1。
            tc 为 做 子串 用 文本、i 减 1、1。
            若 做 字符串等于 用 pc、「*」：
                新dp 的 第 j 项 为 新dp 的 第 j 减 1 项 或者 dp 的 第 j 项。
            否则若 做 字符串等于 用 pc、「?」 或者 做 字符串等于 用 pc、tc：
                新dp 的 第 j 项 为 dp 的 第 j 减 1 项。
            完。
            j 为 j 加 1。
        完。
        dp 为 新dp。
        i 为 i 加 1。
    完。
    返还 dp 的 第 pn 项。
完。

kmp搜索 接受 文本、模式：
    tn 为 做 计数 用 文本。
    pn 为 做 计数 用 模式。
    若 pn 等于 0：
        返还 一组：（空）。
    完。
    可变 失败 为 一组：0。
    可变 k 为 0。
    可变 i 为 1。
    只要 i 小于 pn：
        只要 k 大于 0 并且 做 字符串等于 用 做 子串 用 模式、k、1、做 子串 用 模式、i、1 等于 假：
            k 为 失败 的 第 k 减 1 项。
        完。
        若 做 字符串等于 用 做 子串 用 模式、k、1、做 子串 用 模式、i、1：
            k 为 k 加 1。
        完。
        做 追加 用 失败、k。
        i 为 i 加 1。
    完。
    可变 匹配位置 为 一组：（空）。
    可变 j 为 0。
    i 为 0。
    只要 i 小于 tn：
        只要 j 大于 0 并且 做 字符串等于 用 做 子串 用 文本、i、1、做 子串 用 模式、j、1 等于 假：
            j 为 失败 的 第 j 减 1 项。
        完。
        若 做 字符串等于 用 做 子串 用 文本、i、1、做 子串 用 模式、j、1：
            j 为 j 加 1。
        完。
        若 j 等于 pn：
            做 追加 用 匹配位置、i 减 pn 加 1。
            j 为 失败 的 第 j 减 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 匹配位置。
完。

替换全部 接受 文本、旧文本、新文本：
    tn 为 做 计数 用 文本。
    on 为 做 计数 用 旧文本。
    若 on 等于 0：
        返还 文本。
    完。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于或等于 tn 减 on：
        若 做 字符串等于 用 做 子串 用 文本、i、on、旧文本：
            结果 为 做 字符串拼接 用 结果、新文本。
            i 为 i 加 on。
        否则：
            结果 为 做 字符串拼接 用 结果、做 子串 用 文本、i、1。
            i 为 i 加 1。
        完。
    完。
    只要 i 小于 tn：
        结果 为 做 字符串拼接 用 结果、做 子串 用 文本、i、1。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模板 接受 模板文本、_变量表：
    可变 结果 为 模板文本。
    返还 结果。
完。

模板_替换 接受 模板文本、键、值：
    左标记 为 做 字符串拼接 用 做 字符 用 123、做 字符 用 123。
    右标记 为 做 字符串拼接 用 做 字符 用 125、做 字符 用 125。
    占位符 为 做 字符串拼接 用 左标记、做 字符串拼接 用 键、右标记。
    返还 做 替换全部 用 模板文本、占位符、值。
完。

多分隔分割 接受 文本、分隔符列表：
    n 为 做 计数 用 文本。
    分隔数 为 做 计数 用 分隔符列表。
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 文本、i、1。
        可变 是分隔 为 假。
        可变 si 为 0。
        只要 si 小于 分隔数：
            若 做 字符串等于 用 c、分隔符列表 的 第 si 项：
                是分隔 为 真。
                止。
            完。
            si 为 si 加 1。
        完。
        若 是分隔：
            若 做 计数 用 当前 大于 0：
                做 追加 用 结果、当前。
                当前 为 「」。
            完。
        否则：
            当前 为 做 字符串拼接 用 当前、c。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前 大于 0：
        做 追加 用 结果、当前。
    完。
    返还 结果。
完。

url解析 接受 url：
    n 为 做 计数 用 url。
    可变 协议 为 「」。
    可变 主机 为 「」。
    可变 路径 为 「」。
    可变 i 为 0。
    可变 阶段 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 url、i、1。
        若 阶段 等于 0：
            若 i 加 2 小于 n 并且 做 字符串等于 用 做 子串 用 url、i、3、「://」：
                阶段 为 1。
                i 为 i 加 3。
                续。
            完。
            协议 为 做 字符串拼接 用 协议、c。
        否则若 阶段 等于 1：
            若 做 字符串等于 用 c、「/」：
                阶段 为 2。
                路径 为 做 字符串拼接 用 路径、c。
            否则：
                主机 为 做 字符串拼接 用 主机、c。
            完。
        否则：
            路径 为 做 字符串拼接 用 路径、c。
        完。
        i 为 i 加 1。
    完。
    返还 一组：协议、主机、路径。
完。

解析整数 接受 文本：
    n 为 做 计数 用 文本。
    若 n 等于 0：
        返还 0。
    完。
    可变 结果 为 0。
    可变 负 为 假。
    可变 i 为 0。
    若 做 字符串等于 用 做 子串 用 文本、0、1、「-」：
        负 为 真。
        i 为 1。
    完。
    只要 i 小于 n：
        c 为 做 编码 用 做 子串 用 文本、i、1。
        若 c 大于或等于 字符_零 并且 c 小于或等于 字符_九：
            结果 为 结果 乘 10 加 (c 减 48)。
        完。
        i 为 i 加 1。
    完。
    若 负：
        结果 为 0 减 结果。
    完。
    返还 结果。
完。

最长公共子序列 接受 a、b：
    m 为 做 计数 用 a。
    n 为 做 计数 用 b。
    可变 dp 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于或等于 n：
        做 追加 用 dp、0。
        j 为 j 加 1。
    完。
    可变 i 为 1。
    只要 i 小于或等于 m：
        可变 新dp 为 一组：0。
        j 为 1。
        只要 j 小于或等于 n：
            若 做 字符串等于 用 做 子串 用 a、i 减 1、1、做 子串 用 b、j 减 1、1：
                做 追加 用 新dp、dp 的 第 j 减 1 项 加 1。
            否则：
                可变 最大 为 dp 的 第 j 项。
                若 新dp 的 第 j 减 1 项 大于 最大：
                    最大 为 新dp 的 第 j 减 1 项。
                完。
                做 追加 用 新dp、最大。
            完。
            j 为 j 加 1。
        完。
        dp 为 新dp。
        i 为 i 加 1。
    完。
    返还 dp 的 第 n 项。
完。

字符串哈希 接受 文本：
    n 为 做 计数 用 文本。
    可变 h 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        h 为 h 乘 31 加 做 编码 用 做 子串 用 文本、i、1 位与 0x7FFFFFFF。
        i 为 i 加 1。
    完。
    返还 h。
完。

本章 收束。
