《路径解析》

※ 获取文件名 (最后一个/或\后的部分)

引用《..·基础》。

路径_文件名 接受 路径)：
    可变 最后 为 -1。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 路径：
        c 为 做 字节取 用 路径、i。
        若 c 等于 字符_斜杠 或 c 等于 字符_反斜杠：
        i 为 i 加 1。
    完。
    若 最后 等于 -1：
    返还 做 子串 用 路径、最后 加 1、做 字节长度 用 路径 减 最后 减 1。
完。

※ 获取目录部分
路径_目录 接受 路径)：
    可变 最后 为 -1。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 路径：
        c 为 做 字节取 用 路径、i。
        若 c 等于 字符_斜杠 或 c 等于 字符_反斜杠：
        i 为 i 加 1。
    完。
    若 最后 等于 -1：
    返还 做 子串 用 路径、0、最后。
完。

※ 获取扩展名
路径_扩展名 接受 路径)：
    文件 为 做 路径_文件名 用 路径。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 文件：
        若 做 字节取 用 文件、i 等于 字符_句点：
        i 为 i 加 1。
    完。
    若 最后点 不大于 0：
    返还 做 子串 用 文件、最后点 加 1、做 字节长度 用 文件 减 最后点 减 1。
完。

※ 去掉扩展名
路径_无扩展 接受 路径)：
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 路径：
        c 为 做 字节取 用 路径、i。
        若 c 等于 字符_句点：
        若 c 等于 字符_斜杠 或 c 等于 字符_反斜杠：
        i 为 i 加 1。
    完。
    若 最后点 不大于 最后分隔：
    返还 做 子串 用 路径、0、最后点。
完。

※ 路径组合
路径_组合 接受 目录、文件)：
    若 做 字节长度 用 目录 等于 0：
    末字 为 做 字节取 用 目录、做 字节长度 用 目录 减 1。
    若 末字 等于 47 或 末字 等于 92：
        返还 做 字符串拼接 用 目录、文件。
    完。
    返还 做 字符串拼接 用 做 字符串拼接 用 目录、「/」、文件, 文件。
完。

※ 是否绝对路径
路径_是绝对 接受 路径)：
    若 做 字节长度 用 路径 等于 0：
    若 做 字节取 用 路径、0 等于 字符_斜杠：
    若 做 字节长度 用 路径 不小于 3：
        c1 为 做 字节取 用 路径、1。
        c2 为 做 字节取 用 路径、2。
        若 c1 等于 58 且 (c2 等于 47 或 c2 等于 92)：
    完。
    返还 0。
完。

※ 路径深度 (/之间的段数)
路径_深度 接受 路径)：
    若 做 字节长度 用 路径 等于 0：
    可变 深度 为 1。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 路径：
        c 为 做 字节取 用 路径、i。
        若 c 等于 字符_斜杠 或 c 等于 字符_反斜杠：
        i 为 i 加 1。
    完。
    返还 深度。
完。

※ 路径分割

路径_分割段 接受 路径)：
    可变 段列表 为 []。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 路径：
        c 为 做 字节取 用 路径、i。
        若 c 等于 字符_斜杠 或 c 等于 字符_反斜杠：
            若 当前 不等于 「」：
            当前 = 「」。
        否则：
            当前 = 当前 加 做 字符 用 c。
        完。
        i 为 i 加 1。
    完。
    若 当前 不等于 「」：
    返还 段列表。
完。

※ 路径规范化

路径_规范化 接受 路径)：
    可变 新栈 为 []。
    若 路径 等于 「.」：
    若 路径 等于 「」：
    段 为 做 路径_分割段 用 路径。
    可变 绝对 为 做 路径_是绝对 用 路径。
    可变 栈 为 []。
    可变 i 为 0。
    只要 i 小于 做 计数 用 段：
        若 段[i] 等于 「..」：
            若 做 计数 用 栈 大于 0 且 栈[做 计数 用 栈 减 1] 不等于 「..」：
                可变 j 为 0。
                只要 j 小于 做 计数 用 栈 减 1：
                栈 为 新栈。
            否则若 绝对 等于 假：
                做 追加 用 栈、「..」。
            完。
        否则若 段[i] 不等于 「.」：
            做 追加 用 栈、段[i]。
        完。
        i 为 i 加 1。
    完。
    ※ 重建路径
    可变 结果 为 「」。
    若 做 路径_是绝对 用 路径：
    i 为 0。
    只要 i 小于 做 计数 用 栈：
        若 i 大于 0：
        结果 为 结果 加 栈[i]。
        i 为 i 加 1。
    完。
    若 结果 等于 「」：
    返还 结果。
完。

※ 路径拼接

路径_拼接 接受 基础、子路径)：
    若 做 路径_是绝对 用 子路径：
    基长 为 做 字节长度 用 基础。
    若 基长 等于 0：
    末字符 为 做 字节取 用 基础、基长 减 1。
    若 末字符 等于 47 或 末字符 等于 92：
    返还 基础 加 「/」 加 子路径。
完。

路径_拼接多 接受 段列表)：
    若 做 计数 用 段列表 等于 0：
    可变 结果 为 段列表[0]。
    可变 i 为 1。
    只要 i 小于 做 计数 用 段列表：
    返还 结果。
完。

※ 路径比较

路径_相同 接受 a、b)：
    返还 做 路径_规范化 用 a 等于 做 路径_规范化 用 b。
完。

路径_是子路径 接受 父、子)：
    父规 为 做 路径_规范化 用 父。
    子规 为 做 路径_规范化 用 子。
    若 做 字节长度 用 子规 不大于 做 字节长度 用 父规：
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 父规：
        若 做 字节取 用 父规、i 不等于 做 字节取 用 子规、i：
        i 为 i 加 1。
    完。
    返还 真。
完。

可变 _路径_解析数 为 0。
可变 _路径_规范化数 为 0。

路径_记录解析 接受 ) { _路径_解析数：
路径_记录规范化 接受 ) { _路径_规范化数：

本章 收束。
