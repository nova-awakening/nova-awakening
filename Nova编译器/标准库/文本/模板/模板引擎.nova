《模板引擎》

引用《..·..·基础》。

节点_文本 为 0。

节点_变量 为 1。

节点_条件 为 2。

节点_循环 为 3。

节点_分块 为 4。

节点_注释 为 5。

节点_包含 为 6。

模板节点 接受 类型、数据：
    返还 一组：类型、数据、一组：（空）、一组：（空）。
完。

模板_扫描标签内容 接受 模板文本、起始、n：
    可变 i 为 起始。
    只要 i 小于 n 并且 做 字符串等于 用 做 子串 用 模板文本、i、1、「 」：
        i 为 i 加 1。
    完。
    可变 内容 为 「」。
    只要 i 小于 n：
        若 i 加 1 小于 n 并且 做 字符串等于 用 做 子串 用 模板文本、i、1、做 字符 用 125 并且 做 字符串等于 用 做 子串 用 模板文本、i 加 1、1、做 字符 用 125：
            i 为 i 加 2。
            止。
        完。
        内容 为 做 字符串拼接 用 内容、做 子串 用 模板文本、i、1。
        i 为 i 加 1。
    完。
    返还 一组：做 模板_去空白 用 内容、i。
完。

模板_编译 接受 模板文本：
    可变 节点列表 为 一组：（空）。
    n 为 做 计数 用 模板文本。
    可变 i 为 0。
    可变 文本缓冲 为 「」。
    只要 i 小于 n：
        若 i 加 1 小于 n 并且 做 字符串等于 用 做 子串 用 模板文本、i、1、做 字符 用 123 并且 做 字符串等于 用 做 子串 用 模板文本、i 加 1、1、做 字符 用 123：
            若 做 计数 用 文本缓冲 大于 0：
                做 追加 用 节点列表、做 模板节点 用 节点_文本、文本缓冲。
                文本缓冲 为 「」。
            完。
            i 为 i 加 2。
            扫描结果 为 做 模板_扫描标签内容 用 模板文本、i、n。
            i 为 扫描结果 的 第 1 项。
            做 模板_解析标签 用 扫描结果 的 第 0 项、节点列表。
        否则：
            文本缓冲 为 做 字符串拼接 用 文本缓冲、做 子串 用 模板文本、i、1。
            i 为 i 加 1。
        完。
    完。
    若 做 计数 用 文本缓冲 大于 0：
        做 追加 用 节点列表、做 模板节点 用 节点_文本、文本缓冲。
    完。
    返还 节点列表。
完。

模板_解析标签 接受 标签内容、节点列表：
    若 做 计数 用 标签内容 等于 0：
        返还 空。
    完。
    首字 为 做 子串 用 标签内容、0、1。
    若 做 字符串等于 用 首字、「#」：
        名称 为 做 模板_去空白 用 做 子串 用 标签内容、1、做 计数 用 标签内容 减 1。
        若 做 模板_开头是 用 名称、「each 」：
            循环变量 为 做 模板_去空白 用 做 子串 用 名称、5、做 计数 用 名称 减 5。
            做 追加 用 节点列表、做 模板节点 用 节点_循环、循环变量。
        否则：
            做 追加 用 节点列表、做 模板节点 用 节点_条件、名称。
        完。
    否则若 做 字符串等于 用 首字、「/」：
        结束名 为 做 模板_去空白 用 做 子串 用 标签内容、1、做 计数 用 标签内容 减 1。
        做 追加 用 节点列表、做 模板节点 用 节点_分块、做 字符串拼接 用 「/」、结束名。
    否则若 做 字符串等于 用 首字、「!」：
        做 追加 用 节点列表、做 模板节点 用 节点_注释、做 子串 用 标签内容、1、做 计数 用 标签内容 减 1。
    否则若 做 字符串等于 用 首字、「>」：
        部分名 为 做 模板_去空白 用 做 子串 用 标签内容、1、做 计数 用 标签内容 减 1。
        做 追加 用 节点列表、做 模板节点 用 节点_包含、部分名。
    否则：
        可变 变量名 为 标签内容。
        可变 过滤器 为 一组：（空）。
        管道位 为 做 模板_查找字符 用 标签内容、「|」。
        若 管道位 大于或等于 0：
            变量名 为 做 模板_去空白 用 做 子串 用 标签内容、0、管道位。
            过滤器文本 为 做 子串 用 标签内容、管道位 加 1、做 计数 用 标签内容 减 管道位 减 1。
            过滤器 为 做 模板_分割过滤器 用 过滤器文本。
        完。
        变量节点 为 做 模板节点 用 节点_变量、变量名。
        变量节点 的 第 3 项 为 过滤器。
        做 追加 用 节点列表、变量节点。
    完。
完。

模板_构建树 接受 平坦列表：
    可变 根 为 一组：（空）。
    可变 新栈 为 一组：（空）。
    可变 栈 为 一组：根。
    可变 i 为 0。
    只要 i 小于 做 计数 用 平坦列表：
        节点 为 平坦列表 的 第 i 项。
        若 节点 的 第 0 项 等于 节点_条件 或者 节点 的 第 0 项 等于 节点_循环：
            若 做 计数 用 栈 大于 0：
                做 追加 用 栈 的 第 做 计数 用 栈 减 1 项、节点。
            完。
            做 追加 用 栈、节点 的 第 2 项。
        否则若 节点 的 第 0 项 等于 节点_分块：
            若 做 计数 用 栈 大于 1：
                可变 j 为 0。
                只要 j 小于 做 计数 用 栈 减 1：
                    做 追加 用 新栈、栈 的 第 j 项。
                    j 为 j 加 1。
                完。
                栈 为 新栈。
            完。
        否则若 节点 的 第 0 项 不等于 节点_注释：
            若 做 计数 用 栈 大于 0：
                做 追加 用 栈 的 第 做 计数 用 栈 减 1 项、节点。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 根。
完。

模板_渲染 接受 节点树、上下文、过滤器表：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点树：
        节点 为 节点树 的 第 i 项。
        若 节点 的 第 0 项 等于 节点_文本：
            结果 为 做 字符串拼接 用 结果、节点 的 第 1 项。
        完。
        若 节点 的 第 0 项 等于 节点_变量：
            可变 值 为 做 模板_解析变量 用 上下文、节点 的 第 1 项。
            可变 f 为 0。
            只要 f 小于 做 计数 用 节点 的 第 3 项：
                过滤器名 为 节点 的 第 3 项 的 第 f 项。
                过滤器函数 为 做 字典取 用 过滤器表、过滤器名。
                若 过滤器函数 不等于 空：
                    值 为 做 过滤器函数 用 值。
                完。
                f 为 f 加 1。
            完。
            若 值 不等于 空：
                结果 为 做 字符串拼接 用 结果、值。
            完。
        完。
        若 节点 的 第 0 项 等于 节点_条件：
            可变 条件值 为 做 模板_解析变量 用 上下文、节点 的 第 1 项。
            可变 真值 为 假。
            若 条件值 不等于 空 并且 条件值 不等于 假 并且 条件值 不等于 0：
                真值 为 真。
            完。
            若 做 计数 用 节点 的 第 1 项 大于 0 并且 做 字符串等于 用 做 子串 用 节点 的 第 1 项、0、1、「!」：
                实际名 为 做 子串 用 节点 的 第 1 项、1、做 计数 用 节点 的 第 1 项 减 1。
                条件值 为 做 模板_解析变量 用 上下文、实际名。
                真值 为 条件值 等于 空 或者 条件值 等于 假 或者 条件值 等于 0。
            完。
            若 真值：
                结果 为 做 字符串拼接 用 结果、做 模板_渲染 用 节点 的 第 2 项、上下文、过滤器表。
            完。
        完。
        若 节点 的 第 0 项 等于 节点_循环：
            列表 为 做 模板_解析变量 用 上下文、节点 的 第 1 项。
            若 列表 不等于 空：
                可变 j 为 0。
                只要 j 小于 做 计数 用 列表：
                    子上下文 为 做 模板_克隆上下文 用 上下文。
                    做 字典设 用 子上下文、「item」、列表 的 第 j 项。
                    做 字典设 用 子上下文、「index」、j。
                    做 字典设 用 子上下文、「first」、j 等于 0。
                    做 字典设 用 子上下文、「last」、j 等于 做 计数 用 列表 减 1。
                    结果 为 做 字符串拼接 用 结果、做 模板_渲染 用 节点 的 第 2 项、子上下文、过滤器表。
                    j 为 j 加 1。
                完。
            完。
        完。
        若 节点 的 第 0 项 等于 节点_包含：
            部分模板 为 做 字典取 用 上下文、做 字符串拼接 用 「__偏应用_」、节点 的 第 1 项。
            若 部分模板 不等于 空：
                结果 为 做 字符串拼接 用 结果、做 模板_渲染 用 部分模板、上下文、过滤器表。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模板_解析变量 接受 上下文、路径：
    可变 当前 为 上下文。
    n 为 做 计数 用 路径。
    可变 段 为 「」。
    可变 i 为 0。
    只要 i 小于或等于 n：
        可变 c 为 「」。
        若 i 小于 n：
            c 为 做 子串 用 路径、i、1。
        完。
        若 i 等于 n 或者 做 字符串等于 用 c、「.」：
            若 做 计数 用 段 大于 0：
                当前 为 做 字典取 用 当前、段。
                若 当前 等于 空：
                    返还 空。
                完。
            完。
            段 为 「」。
        否则：
            段 为 做 字符串拼接 用 段、c。
        完。
        i 为 i 加 1。
    完。
    返还 当前。
完。

模板_过滤_大写 接受 值：
    n 为 做 计数 用 值。
    可变 r 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 编码 用 做 子串 用 值、i、1。
        若 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z：
            r 为 做 字符串拼接 用 r、做 子串 用 「ABCDEFGHIJKLMNOPQRSTUVWXYZ」、c 减 97、1。
        否则：
            r 为 做 字符串拼接 用 r、做 子串 用 值、i、1。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

模板_过滤_小写 接受 值：
    n 为 做 计数 用 值。
    可变 r 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 编码 用 做 子串 用 值、i、1。
        若 c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z：
            r 为 做 字符串拼接 用 r、做 子串 用 「abcdefghijklmnopqrstuvwxyz」、c 减 65、1。
        否则：
            r 为 做 字符串拼接 用 r、做 子串 用 值、i、1。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

模板_过滤_去空 接受 值：
    返还 做 模板_去空白 用 值。
完。

模板_过滤_转义 接受 值：
    n 为 做 计数 用 值。
    可变 r 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 值、i、1。
        若 做 字符串等于 用 c、「<」：
            r 为 做 字符串拼接 用 r、「&lt;」。
        否则若 做 字符串等于 用 c、「>」：
            r 为 做 字符串拼接 用 r、「&gt;」。
        否则若 做 字符串等于 用 c、「&」：
            r 为 做 字符串拼接 用 r、「&amp;」。
        否则若 做 字符串等于 用 c、「\"」：
            r 为 做 字符串拼接 用 r、「&quot 」。
        否则：
            r 为 做 字符串拼接 用 r、c。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

模板_创建过滤器表 接受 零参：
    表 为 做 字典。
    做 字典设 用 表、「upper」、模板_过滤_大写。
    做 字典设 用 表、「lower」、模板_过滤_小写。
    做 字典设 用 表、「trim」、模板_过滤_去空。
    做 字典设 用 表、「escape」、模板_过滤_转义。
    返还 表。
完。

模板_快速渲染 接受 模板文本、上下文：
    节点树 为 做 模板_编译 用 模板文本。
    过滤器表 为 做 模板_创建过滤器表。
    返还 做 模板_渲染 用 节点树、上下文、过滤器表。
完。

模板_去空白 接受 s：
    n 为 做 计数 用 s。
    可变 开始 为 0。
    只要 开始 小于 n 并且 (做 字符串等于 用 做 子串 用 s、开始、1、「 」 或者 做 字符串等于 用 做 子串 用 s、开始、1、「\t」)：
        开始 为 开始 加 1。
    完。
    可变 结束 为 n。
    只要 结束 大于 开始 并且 (做 字符串等于 用 做 子串 用 s、结束 减 1、1、「 」 或者 做 字符串等于 用 做 子串 用 s、结束 减 1、1、「\t」)：
        结束 为 结束 减 1。
    完。
    若 开始 大于或等于 结束：
        返还 「」。
    完。
    返还 做 子串 用 s、开始、结束 减 开始。
完。

模板_查找字符 接受 s、目标：
    n 为 做 计数 用 s。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 字符串等于 用 做 子串 用 s、i、1、目标：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

模板_开头是 接受 s、前缀：
    若 做 计数 用 s 小于 做 计数 用 前缀：
        返还 假。
    完。
    返还 做 字符串等于 用 做 子串 用 s、0、做 计数 用 前缀、前缀。
完。

模板_分割过滤器 接受 文本：
    n 为 做 计数 用 文本。
    可变 结果 为 一组：（空）。
    可变 当前 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 文本、i、1。
        若 做 字符串等于 用 c、「|」：
            过滤名 为 做 模板_去空白 用 当前。
            若 做 计数 用 过滤名 大于 0：
                做 追加 用 结果、过滤名。
            完。
            当前 为 「」。
        否则：
            当前 为 做 字符串拼接 用 当前、c。
        完。
        i 为 i 加 1。
    完。
    最后 为 做 模板_去空白 用 当前。
    若 做 计数 用 最后 大于 0：
        做 追加 用 结果、最后。
    完。
    返还 结果。
完。

模板_克隆上下文 接受 上下文：
    返还 上下文。
完。

模板_过滤_截断 接受 文本、最大长度：
    若 做 计数 用 文本 小于或等于 最大长度：
        返还 文本。
    完。
    返还 做 字符串拼接 用 做 子串 用 文本、0、最大长度、「...」。
完。

模板_过滤_重复 接受 文本、次数：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 次数：
        结果 为 做 字符串拼接 用 结果、文本。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模板_过滤_反转 接受 文本：
    可变 结果 为 「」。
    可变 i 为 做 计数 用 文本 减 1。
    只要 i 大于或等于 0：
        结果 为 做 字符串拼接 用 结果、做 字符 用 做 字符码 用 文本、i。
        i 为 i 减 1。
    完。
    返还 结果。
完。

模板_条件渲染 接受 条件、真值、假值：
    若 条件：
        返还 真值。
    完。
    返还 假值。
完。

模板_循环渲染 接受 列表、模板文本、_变量名：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        可变 行 为 模板文本。
        结果 为 做 字符串拼接 用 结果、行。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模板_默认值 接受 值、默认：
    若 值 等于 空：
        返还 默认。
    完。
    若 值 等于 「」：
        返还 默认。
    完。
    返还 值。
完。

模板_HTML转义 接受 文本：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        c 为 做 字符码 用 文本、i。
        若 c 等于 字符_小于号：
            结果 为 做 字符串拼接 用 结果、「&lt;」。
        否则若 c 等于 字符_大于号：
            结果 为 做 字符串拼接 用 结果、「&gt;」。
        否则若 c 等于 38：
            结果 为 做 字符串拼接 用 结果、「&amp;」。
        否则若 c 等于 字符_双引号：
            结果 为 做 字符串拼接 用 结果、「&quot;」。
        否则：
            结果 为 做 字符串拼接 用 结果、做 字符 用 c。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _模板_渲染次数 为 0。

可变 _模板_变量替换数 为 0。

可变 _模板_错误数 为 0。

模板_记录渲染 接受 零参：
    _模板_渲染次数 为 _模板_渲染次数 加 1。
完。

模板_记录替换 接受 零参：
    _模板_变量替换数 为 _模板_变量替换数 加 1。
完。

模板_记录错误 接受 零参：
    _模板_错误数 为 _模板_错误数 加 1。
完。

模板_质量评估 接受 零参：
    若 _模板_错误数 大于 _模板_渲染次数 除以 5：
        返还 「模板错误率高(检查变量名)」。
    完。
    若 _模板_渲染次数 大于 10000：
        返还 「高频渲染(考虑缓存)」。
    完。
    返还 「模板使用正常」。
完。

模板_重置 接受 零参：
    _模板_渲染次数 为 0。
    _模板_变量替换数 为 0。
    _模板_错误数 为 0。
完。

可变 _模板缓存字典 为 做 字典。

可变 _模板缓存数 为 0。

模板_缓存_设置 接受 名称、编译后：
    已有 为 做 字典取 用 _模板缓存字典、名称。
    若 已有 等于 空：
        _模板缓存数 为 _模板缓存数 加 1。
    完。
    做 字典设 用 _模板缓存字典、名称、编译后。
完。

模板_缓存_获取 接受 名称：
    返还 做 字典取 用 _模板缓存字典、名称。
完。

模板_缓存_清空 接受 零参：
    _模板缓存字典 为 做 字典。
    _模板缓存数 为 0。
完。

模板_缓存_大小 接受 零参：
    返还 _模板缓存数。
完。

模板_是否健康 接受 零参：
    返还 _模板_错误数 等于 0。
完。

模板_总操作数 接受 零参：
    返还 _模板_渲染次数 加 _模板_变量替换数。
完。

本章 收束。
