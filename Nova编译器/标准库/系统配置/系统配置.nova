《系统配置》

引用《..·基础》。

引用《..·系统·配置》。

引用《..·配置解析·配置解析》。

引用《..·..·工具·包管理器·包管理器》。

可变 特性_名 为 一组：（空）。

可变 特性_开 为 一组：（空）。

系统配置_特性_禁用 接受 名哈希：
    可变 i 为 0。
    只要 i 小于 做 计数 用 特性_名：
        若 特性_名 的 第 i 项 等于 名哈希：
            特性_开 的 第 i 项 为 假。
            返还。
        完。
        i 为 i 加 1。
    完。
完。

特性_是否启用 接受 名哈希：
    可变 i 为 0。
    只要 i 小于 做 计数 用 特性_名：
        若 特性_名 的 第 i 项 等于 名哈希：
            返还 特性_开 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

系统配置_版本_创建 接受 主、次、修：
    返还 一组：主、次、修。
完。

版本_主 接受 v：
    返还 v 的 第 0 项。
完。

版本_次 接受 v：
    返还 v 的 第 1 项。
完。

版本_修 接受 v：
    返还 v 的 第 2 项。
完。

系统配置_版本_比较 接受 a、b：
    若 a 的 第 0 项 不等于 b 的 第 0 项：
        返还 a 的 第 0 项 减 b 的 第 0 项。
    完。
    若 a 的 第 1 项 不等于 b 的 第 1 项：
        返还 a 的 第 1 项 减 b 的 第 1 项。
    完。
    返还 a 的 第 2 项 减 b 的 第 2 项。
完。

系统配置_版本_兼容 接受 当前、最低：
    返还 做 版本_比较 用 当前、最低 大于或等于 0。
完。

配置_验证范围 接受 键、最小、最大、缺省：
    数值 为 做 配置_获取 用 键、缺省。
    若 数值 小于 最小：
        做 配置_设置 用 键、最小、0。
        返还 假。
    完。
    若 数值 大于 最大：
        做 配置_设置 用 键、最大、0。
        返还 假。
    完。
    返还 真。
完。

配置_批量设置 接受 键列表、值列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        做 配置_设置 用 键列表 的 第 i 项、值列表 的 第 i 项、0。
        i 为 i 加 1。
    完。
完。

特性_注册 接受 名哈希、默认开：
    做 追加 用 特性_名、名哈希。
    做 追加 用 特性_开、默认开。
完。

系统配置_特性_启用 接受 名哈希：
    可变 i 为 0。
    只要 i 小于 做 计数 用 特性_名：
        若 特性_名 的 第 i 项 等于 名哈希：
            特性_开 的 第 i 项 为 真。
            返还 空。
        完。
        i 为 i 加 1。
    完。
完。

系统配置_特性_切换 接受 名哈希：
    可变 i 为 0。
    只要 i 小于 做 计数 用 特性_名：
        若 特性_名 的 第 i 项 等于 名哈希：
            若 特性_开 的 第 i 项：
                特性_开 的 第 i 项 为 假。
            否则：
                特性_开 的 第 i 项 为 真。
            完。
            返还 空。
        完。
        i 为 i 加 1。
    完。
完。

特性_数量 接受 零参：
    返还 做 计数 用 特性_名。
完。

特性_已启用数 接受 零参：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 特性_开：
        若 特性_开 的 第 i 项：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

AI配置_默认 接受 零参：
    做 配置_设置 用 1001、100、0。
    做 配置_设置 用 1002、32、0。
    做 配置_设置 用 1003、100、0。
    做 配置_设置 用 1004、1000、0。
    做 配置_设置 用 1005、5、0。
    做 配置_设置 用 1006、0、0。
    做 配置_设置 用 1007、1、0。
    做 配置_设置 用 1008、100、0。
    做 配置_设置 用 1009、42、0。
    做 配置_设置 用 1010、1、0。
完。

AI配置_获取lr 接受 零参：
    返还 做 配置_获取 用 1001、100。
完。

AI配置_获取batch 接受 零参：
    返还 做 配置_获取 用 1002、32。
完。

AI配置_获取epochs 接受 零参：
    返还 做 配置_获取 用 1003、100。
完。

AI配置_获取种子 接受 零参：
    返还 做 配置_获取 用 1009、42。
完。

可变 快照_键 为 一组：（空）。

可变 快照_值 为 一组：（空）。

配置_保存快照 接受 零参：
    快照_键 为 做 复制列表 用 配置_键。
    快照_值 为 做 复制列表 用 配置_值。
完。

配置_恢复快照 接受 零参：
    可变 配置_值 为 0。
    配置_键 为 做 复制列表 用 快照_键。
    配置_值 为 做 复制列表 用 快照_值。
完。

配置_报告 接受 零参：
    做 打印 用 「╔═══ 系统配置 ═══╗」。
    做 打印 用 「║ 配置项: 」 加 做 转字符串 用 做 配置_数量。
    做 打印 用 「║ 特性开关: 」 加 做 转字符串 用 做 特性_已启用数 加 「/」 加 做 转字符串 用 做 特性_数量 加 「 启用」。
    做 打印 用 「╚════════════════╝」。
完。

版本_字符串 接受 v：
    返还 做 转字符串 用 v 的 第 0 项 加 「.」 加 做 转字符串 用 v 的 第 1 项 加 「.」 加 做 转字符串 用 v 的 第 2 项。
完。

探测_操作系统 接受 零参：
    返还 0。
完。

探测_架构 接受 零参：
    返还 「x86-64」。
完。

编译器特性_初始化 接受 零参：
    做 特性_注册 用 10001、真。
    做 特性_注册 用 10002、真。
    做 特性_注册 用 10003、假。
    做 特性_注册 用 10004、真。
    做 特性_注册 用 10005、真。
    做 特性_注册 用 10006、假。
    做 特性_注册 用 10007、真。
    做 特性_注册 用 10008、假。
    做 特性_注册 用 10009、假。
    做 特性_注册 用 10010、真。
    做 特性_注册 用 10011、假。
    做 特性_注册 用 10012、假。
    做 特性_注册 用 10013、假。
    做 特性_注册 用 10014、假。
完。

配置_导出 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 配置_键：
        做 追加 用 结果、一组：配置_键 的 第 i 项、配置_值 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

配置_差异 接受 快照A、快照B：
    可变 变更 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 快照A：
        若 i 小于 做 计数 用 快照B：
            若 快照A 的 第 i 项 的 第 1 项 不等于 快照B 的 第 i 项 的 第 1 项：
                做 追加 用 变更、一组：快照A 的 第 i 项 的 第 0 项、快照A 的 第 i 项 的 第 1 项、快照B 的 第 i 项 的 第 1 项。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 变更。
完。

配置_检查一致性 接受 零参：
    可变 问题 为 一组：（空）。
    若 做 特性_是否启用 用 10011 并且 非 做 特性_是否启用 用 10010：
        做 追加 用 问题、「形式化验证需要安全检查启用」。
    完。
    若 做 特性_是否启用 用 10006 并且 做 特性_是否启用 用 10008：
        做 追加 用 问题、「内联优化可能影响调试信息准确性」。
    完。
    返还 问题。
完。

配置_合并 接受 基础配置、覆盖配置：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 基础配置：
        做 追加 用 结果、基础配置 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 覆盖配置 除以 2：
        键 为 覆盖配置 的 第 i 乘 2 项。
        值 为 覆盖配置 的 第 i 乘 2 加 1 项。
        可变 已有 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 结果 除以 2：
            若 结果 的 第 j 乘 2 项 等于 键：
                结果 的 第 j 乘 2 加 1 项 为 值。
                已有 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已有 等于 假：
            做 追加 用 结果、键。
            做 追加 用 结果、值。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

配置_验证必需 接受 配置、必需键列表：
    可变 缺失 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 必需键列表：
        可变 找到 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 配置 除以 2：
            若 配置 的 第 j 乘 2 项 等于 必需键列表 的 第 i 项：
                找到 为 真。
            完。
            j 为 j 加 1。
        完。
        若 找到 等于 假：
            做 追加 用 缺失、必需键列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 缺失。
完。

本章 收束。
