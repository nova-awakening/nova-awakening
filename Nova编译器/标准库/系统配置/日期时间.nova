《日期时间》

引用《..·基础》。

引用《..·系统·时间·时间》。

日期时间_月天数 接受 月、闰：
    若 月 等于 1：
        返还 31。
    完。
    若 月 等于 2：
        若 闰 等于 1：
            返还 29。
        完。
        返还 28。
    完。
    若 月 等于 3：
        返还 31。
    完。
    若 月 等于 4：
        返还 30。
    完。
    若 月 等于 5：
        返还 31。
    完。
    若 月 等于 6：
        返还 30。
    完。
    若 月 等于 7：
        返还 31。
    完。
    若 月 等于 8：
        返还 31。
    完。
    若 月 等于 9：
        返还 30。
    完。
    若 月 等于 10：
        返还 31。
    完。
    若 月 等于 11：
        返还 30。
    完。
    若 月 等于 12：
        返还 31。
    完。
    返还 30。
完。

日期时间_是闰年 接受 年：
    若 年 取余于 400 等于 0：
        返还 1。
    完。
    若 年 取余于 100 等于 0：
        返还 0。
    完。
    若 年 取余于 4 等于 0：
        返还 1。
    完。
    返还 0。
完。

日期时间_年天数 接受 年：
    若 做 是闰年 用 年 等于 1：
        返还 366。
    完。
    返还 365。
完。

日期_创建 接受 年、月、日：
    返还 一组：年、月、日。
完。

日期_年 接受 d：
    返还 d 的 第 0 项。
完。

日期_月 接受 d：
    返还 d 的 第 1 项。
完。

日期_日 接受 d：
    返还 d 的 第 2 项。
完。

日期_有效 接受 d：
    若 d 的 第 1 项 小于 1 或者 d 的 第 1 项 大于 12：
        返还 0。
    完。
    若 d 的 第 2 项 小于 1 或者 d 的 第 2 项 大于 做 月天数 用 d 的 第 1 项、做 是闰年 用 d 的 第 0 项：
        返还 0。
    完。
    返还 1。
完。

日期_转天数_近似 接受 d：
    可变 天 为 0。
    可变 y 为 1。
    只要 y 小于 d 的 第 0 项：
        天 为 天 加 做 年天数 用 y。
        y 为 y 加 1。
    完。
    可变 m 为 1。
    只要 m 小于 d 的 第 1 项：
        天 为 天 加 做 月天数 用 m、做 是闰年 用 d 的 第 0 项。
        m 为 m 加 1。
    完。
    天 为 天 加 d 的 第 2 项。
    返还 天。
完。

日期_差 接受 d1、d2：
    t1 为 做 日期_转天数_近似 用 d1。
    t2 为 做 日期_转天数_近似 用 d2。
    若 t1 大于 t2：
        返还 t1 减 t2。
    完。
    返还 t2 减 t1。
完。

日期_星期 接受 d：
    可变 年 为 d 的 第 0 项。
    可变 月 为 d 的 第 1 项。
    若 月 小于 3：
        月 为 月 加 12。
        年 为 年 减 1。
    完。
    q 为 d 的 第 2 项。
    k 为 年 取余于 100。
    j 为 年 除以 100。
    h 为 (q 加 13 乘 (月 加 1) 除以 5 加 k 加 k 除以 4 加 j 除以 4 加 5 乘 j) 取余于 7。
    返还 (h 加 6) 取余于 7。
完。

日期_星期名 接受 w：
    若 w 等于 0：
        返还 「周日」。
    完。
    若 w 等于 1：
        返还 「周一」。
    完。
    若 w 等于 2：
        返还 「周二」。
    完。
    若 w 等于 3：
        返还 「周三」。
    完。
    若 w 等于 4：
        返还 「周四」。
    完。
    若 w 等于 5：
        返还 「周五」。
    完。
    若 w 等于 6：
        返还 「周六」。
    完。
    返还 「未知」。
完。

日期_加天 接受 d、天数：
    可变 年 为 d 的 第 0 项。
    可变 月 为 d 的 第 1 项。
    可变 日 为 d 的 第 2 项 加 天数。
    只要 日 大于 做 月天数 用 月、做 是闰年 用 年：
        日 为 日 减 做 月天数 用 月、做 是闰年 用 年。
        月 为 月 加 1。
        若 月 大于 12：
            月 为 1。
            年 为 年 加 1。
        完。
    完。
    返还 做 日期_创建 用 年、月、日。
完。

日期_减天 接受 d、天数：
    可变 年 为 d 的 第 0 项。
    可变 月 为 d 的 第 1 项。
    可变 日 为 d 的 第 2 项 减 天数。
    只要 日 小于 1：
        月 为 月 减 1。
        若 月 小于 1：
            月 为 12。
            年 为 年 减 1。
        完。
        日 为 日 加 做 月天数 用 月、做 是闰年 用 年。
    完。
    返还 做 日期_创建 用 年、月、日。
完。

日期_比较 接受 d1、d2：
    若 d1 的 第 0 项 不等于 d2 的 第 0 项：
        返还 d1 的 第 0 项 减 d2 的 第 0 项。
    完。
    若 d1 的 第 1 项 不等于 d2 的 第 1 项：
        返还 d1 的 第 1 项 减 d2 的 第 1 项。
    完。
    返还 d1 的 第 2 项 减 d2 的 第 2 项。
完。

日期_格式化 接受 d：
    可变 s 为 做 转字符串 用 d 的 第 0 项。
    s 为 做 字符串拼接 用 s、「-」。
    若 d 的 第 1 项 小于 10：
        s 为 做 字符串拼接 用 s、「0」。
    完。
    s 为 做 字符串拼接 用 s、做 转字符串 用 d 的 第 1 项。
    s 为 做 字符串拼接 用 s、「-」。
    若 d 的 第 2 项 小于 10：
        s 为 做 字符串拼接 用 s、「0」。
    完。
    s 为 做 字符串拼接 用 s、做 转字符串 用 d 的 第 2 项。
    返还 s。
完。

时间_创建 接受 _时、分、秒：
    返还 一组：时、分、秒。
完。

时间_转秒 接受 t：
    返还 t 的 第 0 项 乘 3600 加 t 的 第 1 项 乘 60 加 t 的 第 2 项。
完。

时间_从秒 接受 总秒：
    _时 为 总秒 除以 3600 取余于 24。
    分 为 总秒 除以 60 取余于 60。
    秒 为 总秒 取余于 60。
    返还 做 时间_创建 用 时、分、秒。
完。

时间_格式化 接受 t：
    可变 s 为 「」。
    若 t 的 第 0 项 小于 10：
        s 为 做 字符串拼接 用 s、「0」。
    完。
    s 为 做 字符串拼接 用 s、做 转字符串 用 t 的 第 0 项。
    s 为 做 字符串拼接 用 s、「:」。
    若 t 的 第 1 项 小于 10：
        s 为 做 字符串拼接 用 s、「0」。
    完。
    s 为 做 字符串拼接 用 s、做 转字符串 用 t 的 第 1 项。
    s 为 做 字符串拼接 用 s、「:」。
    若 t 的 第 2 项 小于 10：
        s 为 做 字符串拼接 用 s、「0」。
    完。
    s 为 做 字符串拼接 用 s、做 转字符串 用 t 的 第 2 项。
    返还 s。
完。

日期_转天数 接受 d：
    年 为 d 的 第 0 项。
    月 为 d 的 第 1 项。
    日 为 d 的 第 2 项。
    可变 天数 为 年 乘 365 加 年 除以 4 减 年 除以 100 加 年 除以 400。
    可变 月天 为 一组：0、31、59、90、120、151、181、212、243、273、304、334。
    若 月 大于或等于 1 并且 月 小于或等于 12：
        天数 为 天数 加 月天 的 第 月 减 1 项。
    完。
    天数 为 天数 加 日。
    返还 天数。
完。

日期_差天 接受 d1、d2：
    t1 为 做 日期_转天数 用 d1。
    t2 为 做 日期_转天数 用 d2。
    可变 差 为 t2 减 t1。
    若 差 小于 0：
        差 为 负 差。
    完。
    返还 差。
完。

日期_星期几 接受 d：
    年 为 d 的 第 0 项。
    月 为 d 的 第 1 项。
    日 为 d 的 第 2 项。
    可变 y 为 年。
    可变 m 为 月。
    若 m 小于 3：
        y 为 y 减 1。
        m 为 m 加 12。
    完。
    h 为 (日 加 13 乘 (m 加 1) 除以 5 加 y 加 y 除以 4 减 y 除以 100 加 y 除以 400) 取余于 7。
    返还 h。
完。

日期_星期名_数组 接受 星期号：
    可变 名 为 一组：「周六」、「周日」、「周一」、「周二」、「周三」、「周四」、「周五」。
    若 星期号 大于或等于 0 并且 星期号 小于 7：
        返还 名 的 第 星期号 项。
    完。
    返还 「未知」。
完。

日期_闰年 接受 年：
    若 年 取余于 400 等于 0：
        返还 真。
    完。
    若 年 取余于 100 等于 0：
        返还 假。
    完。
    若 年 取余于 4 等于 0：
        返还 真。
    完。
    返还 假。
完。

日期_月天数 接受 年、月：
    可变 天 为 一组：31、28、31、30、31、30、31、31、30、31、30、31。
    若 月 小于 1 或者 月 大于 12：
        返还 0。
    完。
    可变 结果 为 天 的 第 月 减 1 项。
    若 月 等于 2 并且 做 日期_闰年 用 年：
        结果 为 结果 加 1。
    完。
    返还 结果。
完。

可变 _计时器_名称 为 一组：（空）。

可变 _计时器_开始 为 一组：（空）。

日期时间_计时器_开始 接受 名称、当前时间：
    做 追加 用 _计时器_名称、名称。
    做 追加 用 _计时器_开始、当前时间。
完。

计时器_结束 接受 名称、当前时间：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _计时器_名称：
        若 _计时器_名称 的 第 i 项 等于 名称：
            耗时 为 当前时间 减 _计时器_开始 的 第 i 项。
            做 删除 用 _计时器_名称、i。
            做 删除 用 _计时器_开始、i。
            返还 耗时。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

可变 _日期_格式化数 为 0。

可变 _日期_操作数 为 0。

日期_记录操作 接受 零参：
    _日期_操作数 为 _日期_操作数 加 1。
完。

日期_记录格式化 接受 零参：
    _日期_格式化数 为 _日期_格式化数 加 1。
完。

本章 收束。
