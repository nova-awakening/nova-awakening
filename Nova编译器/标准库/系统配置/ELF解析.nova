《ELF解析》

ELF_魔数0 为 0x7F。

ELF_魔数1 为 0x45。

ELF_魔数2 为 0x4C。

ELF_魔数3 为 0x46。

ELF类型_无 为 0。

ELF类型_可重定位 为 1。

ELF类型_可执行 为 2。

ELF类型_动态 为 3。

ELF类型_核心 为 4。

处理器_386 为 3。

处理器_AMD64 为 62。

处理器_ARM 为 40。

处理器_AARCH64 为 183。

处理器_RISCV 为 243。

段类型_空 为 0。

段类型_加载 为 1。

段类型_动态 为 2。

段类型_解释器 为 3。

段类型_注释 为 4。

段类型_程序头 为 6。

节类型_空 为 0。

节类型_程序数据 为 1。

节类型_符号表 为 2。

节类型_字符串表 为 3。

节类型_重定位A 为 4。

节类型_无数据 为 8。

节类型_重定位 为 9。

elf_创建头 接受 类、类型、机器、入口：
    返还 一组：类、类型、机器、入口、0。
完。

elf_类 接受 头：
    返还 头 的 第 0 项。
完。

elf_类型 接受 头：
    返还 头 的 第 1 项。
完。

elf_机器 接受 头：
    返还 头 的 第 2 项。
完。

elf_入口 接受 头：
    返还 头 的 第 3 项。
完。

elf_类型名 接受 类型：
    若 类型 等于 ELF类型_无：
        返还 「NONE」。
    完。
    若 类型 等于 ELF类型_可重定位：
        返还 「REL」。
    完。
    若 类型 等于 ELF类型_可执行：
        返还 「EXEC」。
    完。
    若 类型 等于 ELF类型_动态：
        返还 「DYN」。
    完。
    若 类型 等于 ELF类型_核心：
        返还 「CORE」。
    完。
    返还 「未知」。
完。

elf_机器名 接受 机器：
    若 机器 等于 处理器_386：
        返还 「i386」。
    完。
    若 机器 等于 处理器_AMD64：
        返还 「x86-64」。
    完。
    若 机器 等于 处理器_ARM：
        返还 「ARM」。
    完。
    若 机器 等于 处理器_AARCH64：
        返还 「AArch64」。
    完。
    若 机器 等于 处理器_RISCV：
        返还 「RISC-V」。
    完。
    返还 「未知」。
完。

elf_创建段头 接受 类型、虚地址、文件大小、内存大小、标志：
    返还 一组：类型、0、虚地址、虚地址、文件大小、内存大小、标志。
完。

elf_段类型名_数值 接受 类型：
    若 类型 等于 段类型_空：
        返还 「NULL」。
    完。
    若 类型 等于 段类型_加载：
        返还 「LOAD」。
    完。
    若 类型 等于 段类型_动态：
        返还 「DYNAMIC」。
    完。
    若 类型 等于 段类型_解释器：
        返还 「INTERP」。
    完。
    若 类型 等于 段类型_注释：
        返还 「NOTE」。
    完。
    若 类型 等于 段类型_程序头：
        返还 「PHDR」。
    完。
    返还 「未知」。
完。

elf_创建节头 接受 名索引、类型、标志、地址、大小：
    返还 一组：名索引、类型、标志、地址、0、大小。
完。

elf_节类型名 接受 类型：
    若 类型 等于 节类型_空：
        返还 「NULL」。
    完。
    若 类型 等于 节类型_程序数据：
        返还 「PROGBITS」。
    完。
    若 类型 等于 节类型_符号表：
        返还 「SYMTAB」。
    完。
    若 类型 等于 节类型_字符串表：
        返还 「STRTAB」。
    完。
    若 类型 等于 节类型_重定位A：
        返还 「RELA」。
    完。
    若 类型 等于 节类型_无数据：
        返还 「NOBITS」。
    完。
    若 类型 等于 节类型_重定位：
        返还 「REL」。
    完。
    返还 「未知」。
完。

elf_验证魔数 接受 字节列表：
    若 做 计数 用 字节列表 小于 4：
        返还 0。
    完。
    若 字节列表 的 第 0 项 不等于 ELF_魔数0：
        返还 0。
    完。
    若 字节列表 的 第 1 项 不等于 ELF_魔数1：
        返还 0。
    完。
    若 字节列表 的 第 2 项 不等于 ELF_魔数2：
        返还 0。
    完。
    若 字节列表 的 第 3 项 不等于 ELF_魔数3：
        返还 0。
    完。
    返还 1。
完。

elf_读16 接受 字节列表、偏移：
    返还 字节列表 的 第 偏移 项 加 字节列表 的 第 偏移 加 1 项 乘 256。
完。

elf_读32 接受 字节列表、偏移：
    返还 字节列表 的 第 偏移 项 加 字节列表 的 第 偏移 加 1 项 乘 256 加 字节列表 的 第 偏移 加 2 项 乘 65536 加 字节列表 的 第 偏移 加 3 项 乘 16777216。
完。

elf_解析头 接受 字节列表：
    若 做 elf_验证魔数 用 字节列表 等于 0：
        返还 空。
    完。
    类 为 字节列表 的 第 4 项。
    类型 为 做 elf_读16 用 字节列表、16。
    机器 为 做 elf_读16 用 字节列表、18。
    入口 为 做 elf_读32 用 字节列表、24。
    返还 做 elf_创建头 用 类、类型、机器、入口。
完。

elf_节头偏移 接受 字节列表：
    返还 做 elf_读32 用 字节列表、32。
完。

elf_节头数量 接受 字节列表：
    返还 做 elf_读16 用 字节列表、48。
完。

elf_节头大小 接受 字节列表：
    返还 做 elf_读16 用 字节列表、46。
完。

elf_解析节头 接受 字节列表、索引：
    基址 为 做 elf_节头偏移 用 字节列表 加 索引 乘 做 elf_节头大小 用 字节列表。
    名称偏移 为 做 elf_读32 用 字节列表、基址。
    类型 为 做 elf_读32 用 字节列表、基址 加 4。
    标志 为 做 elf_读32 用 字节列表、基址 加 8。
    地址 为 做 elf_读32 用 字节列表、基址 加 12。
    偏移 为 做 elf_读32 用 字节列表、基址 加 16。
    大小 为 做 elf_读32 用 字节列表、基址 加 20。
    返还 一组：名称偏移、类型、标志、地址、偏移、大小。
完。

elf_节头类型名 接受 类型：
    若 类型 等于 0：
        返还 「NULL」。
    完。
    若 类型 等于 1：
        返还 「PROGBITS」。
    完。
    若 类型 等于 2：
        返还 「SYMTAB」。
    完。
    若 类型 等于 3：
        返还 「STRTAB」。
    完。
    若 类型 等于 4：
        返还 「RELA」。
    完。
    若 类型 等于 5：
        返还 「HASH」。
    完。
    若 类型 等于 6：
        返还 「DYNAMIC」。
    完。
    若 类型 等于 7：
        返还 「NOTE」。
    完。
    若 类型 等于 8：
        返还 「NOBITS」。
    完。
    若 类型 等于 9：
        返还 「REL」。
    完。
    返还 「未知」。
完。

elf_程序头偏移 接受 字节列表：
    返还 做 elf_读32 用 字节列表、28。
完。

elf_程序头数量 接受 字节列表：
    返还 做 elf_读16 用 字节列表、44。
完。

elf_解析程序头 接受 字节列表、索引：
    大小 为 32。
    基址 为 做 elf_程序头偏移 用 字节列表 加 索引 乘 大小。
    类型 为 做 elf_读32 用 字节列表、基址。
    偏移 为 做 elf_读32 用 字节列表、基址 加 4。
    虚地址 为 做 elf_读32 用 字节列表、基址 加 8。
    文件大小 为 做 elf_读32 用 字节列表、基址 加 16。
    内存大小 为 做 elf_读32 用 字节列表、基址 加 20。
    标志 为 做 elf_读32 用 字节列表、基址 加 24。
    返还 一组：类型、偏移、虚地址、文件大小、内存大小、标志。
完。

elf_段类型名 接受 类型：
    若 类型 等于 0：
        返还 「NULL」。
    完。
    若 类型 等于 1：
        返还 「LOAD」。
    完。
    若 类型 等于 2：
        返还 「DYNAMIC」。
    完。
    若 类型 等于 3：
        返还 「INTERP」。
    完。
    若 类型 等于 4：
        返还 「NOTE」。
    完。
    若 类型 等于 6：
        返还 「PHDR」。
    完。
    若 类型 等于 7：
        返还 「TLS」。
    完。
    返还 「未知」。
完。

elf_解析符号 接受 字节列表、偏移：
    名称 为 做 elf_读32 用 字节列表、偏移。
    值 为 做 elf_读32 用 字节列表、偏移 加 4。
    大小 为 做 elf_读32 用 字节列表、偏移 加 8。
    信息 为 字节列表 的 第 偏移 加 12 项。
    绑定 为 信息 右移 4。
    类型 为 信息 位与 0x0F。
    段索引 为 做 elf_读16 用 字节列表、偏移 加 14。
    返还 一组：名称、值、大小、绑定、类型、段索引。
完。

elf_符号绑定名 接受 绑定：
    若 绑定 等于 0：
        返还 「LOCAL」。
    完。
    若 绑定 等于 1：
        返还 「GLOBAL」。
    完。
    若 绑定 等于 2：
        返还 「WEAK」。
    完。
    返还 「未知」。
完。

elf_符号类型名 接受 类型：
    若 类型 等于 0：
        返还 「NOTYPE」。
    完。
    若 类型 等于 1：
        返还 「OBJECT」。
    完。
    若 类型 等于 2：
        返还 「FUNC」。
    完。
    若 类型 等于 3：
        返还 「SECTION」。
    完。
    若 类型 等于 4：
        返还 「FILE」。
    完。
    返还 「未知」。
完。

elf_解析重定位 接受 字节列表、偏移：
    地址 为 做 elf_读32 用 字节列表、偏移。
    信息 为 做 elf_读32 用 字节列表、偏移 加 4。
    符号索引 为 信息 右移 8。
    类型 为 信息 位与 0xFF。
    返还 一组：地址、符号索引、类型。
完。

elf_重定位类型名_x86 接受 类型：
    若 类型 等于 0：
        返还 「R_386_NONE」。
    完。
    若 类型 等于 1：
        返还 「R_386_32」。
    完。
    若 类型 等于 2：
        返还 「R_386_PC32」。
    完。
    若 类型 等于 7：
        返还 「R_386_JMP_SLOT」。
    完。
    若 类型 等于 8：
        返还 「R_386_RELATIVE」。
    完。
    返还 「R_386_UNKNOWN」。
完。

elf_读字符串 接受 字节列表、字符串表偏移、名称偏移：
    可变 结果 为 「」。
    可变 位置 为 字符串表偏移 加 名称偏移。
    只要 位置 小于 做 计数 用 字节列表 并且 字节列表 的 第 位置 项 不等于 0：
        结果 为 结果 加 做 字符 用 字节列表 的 第 位置 项。
        位置 为 位置 加 1。
    完。
    返还 结果。
完。

elf_机器名_数值 接受 机器：
    若 机器 等于 3：
        返还 「x86」。
    完。
    若 机器 等于 62：
        返还 「x86_64」。
    完。
    若 机器 等于 183：
        返还 「AArch64」。
    完。
    若 机器 等于 243：
        返还 「RISC-V」。
    完。
    返还 「未知」。
完。

elf_类型名_中文 接受 类型：
    若 类型 等于 1：
        返还 「可重定位」。
    完。
    若 类型 等于 2：
        返还 「可执行」。
    完。
    若 类型 等于 3：
        返还 「共享库」。
    完。
    若 类型 等于 4：
        返还 「核心转储」。
    完。
    返还 「未知」。
完。

可变 _elf_解析数 为 0。

可变 _elf_符号数 为 0。

可变 _elf_重定位数 为 0。

elf_记录解析 接受 零参：
    _elf_解析数 为 _elf_解析数 加 1。
完。

elf_记录符号 接受 n：
    _elf_符号数 为 _elf_符号数 加 n。
完。

elf_记录重定位 接受 n：
    _elf_重定位数 为 _elf_重定位数 加 n。
完。

本章 收束。
