《视觉处理》

引用《..·基础》。
引用《..·算法·数学扩展》。

※ 2D卷积
卷积2D 接受 图像、宽、高、核、核大小) {  / 2：
 可变 结果 为 []。
 可变 y 为 0。
 只要 y 小于 高：
 只要 x 小于 宽：
 可变 ky 为 0。
 只要 ky 小于 核大小：
 只要 kx 小于 核大小：
 可变 ix 为 x 加 kx 减 半径。
 若 iy 不小于 0 且 iy 小于 高 且 ix 不小于 0 且 ix 小于 宽：
 总和 为 总和 加 图像[iy 乘 宽 加 ix] 乘 核[ky 乘 核大小 加 kx] 除以 100。
 完。
 kx 为 kx 加 1。
 完。
 ky 为 ky 加 1。
 完。
 做 追加 用 结果、总和。
 x 为 x 加 1。
 完。
 y 为 y 加 1。
 完。
 返还 结果。
完。

※ 预定义核
边缘检测核 接受 _扩1) ：返还 [-100、-100、-100、-100、800、-100、-100、-100、-100] }：
模糊核 接受 _扩1) ：返还 [11、11、11、11、11、11、11、11、11] }：
视觉处理_锐化核 接受 ) ：返还 [0、-100、0、-100、500、-100、0、-100、0] }：

※ 阈值化
阈值化 接受 图像、阈值) { 定义 可变 结果：
 可变 i 为 0。
 只要 i 小于 做 计数 用 图像：
 否则 { 做 追加 用 结果、0 }。
 i 为 i 加 1。
 完。
 返还 结果。
完。

※ 直方图
直方图 接受 图像、区间数) { 定义 可变 直方图：
 可变 i 为 0。
 只要 i 小于 区间数：
 i 为 i 加 1 }。
 可变 最大 为 0。
 i 为 0。
 只要 i 小于 做 计数 用 图像：
 i 为 i 加 1 }。
 若 最大 等于 0：
 i 为 0。
 只要 i 小于 做 计数 用 图像：
 若 bin 不小于 区间数：
 若 bin 小于 0：
 直方图[bin] 为 直方图[bin] 加 1。
 i 为 i 加 1。
 完。
 返还 直方图。
完。

※ 最大池化
最大池化 接受 图像、宽、高、池大小) { 定义 可变 新宽：
 可变 新高 为 高 除以 池大小。
 可变 结果 为 []。
 可变 y 为 0。
 只要 y 小于 新高：
 只要 x 小于 新宽：
 可变 py 为 0。
 只要 py 小于 池大小：
 只要 px 小于 池大小：
 可变 ix 为 x 乘 池大小 加 px。
 若 iy 小于 高 且 ix 小于 宽：
 若 v 大于 最大v：
 完。
 px 为 px 加 1。
 完。
 py 为 py 加 1。
 完。
 做 追加 用 结果、最大v。
 x 为 x 加 1。
 完。
 y 为 y 加 1。
 完。
 返还 结果。
完。

※ 图像翻转
水平翻转_2D 接受 图像、宽、高) { 定义 可变 结果：
 可变 y 为 0。
 只要 y 小于 高：
 只要 x 不小于 0：
 x 为 x 减 1 }。
 y 为 y 加 1。
 完。
 返还 结果。
完。

垂直翻转_2D 接受 图像、宽、高) { 定义 可变 结果：
 可变 y 为 高 减 1。
 只要 y 不小于 0：
 只要 x 小于 宽：
 y 为 y 减 1。
 完。
 返还 结果。
完。

※ 卷积操作

卷积_2D 接受 图像、宽、高、核、核大小) { 定义 可变 结果：
 半径 为 核大小 除以 2。
 可变 y 为 0。
 只要 y 小于 高：
 只要 x 小于 宽：
 可变 ky 为 0。
 只要 ky 小于 核大小：
 只要 kx 小于 核大小：
 ix 为 x 加 kx 减 半径。
 若 iy 不小于 0 且 iy 小于 高 且 ix 不小于 0 且 ix 小于 宽：
 总和 为 总和 加 图像[iy 乘 宽 加 ix] 乘 核[ky 乘 核大小 加 kx]。
 完。
 kx 为 kx 加 1。
 完。
 ky 为 ky 加 1。
 完。
 做 追加 用 结果、总和。
 x 为 x 加 1。
 完。
 y 为 y 加 1。
 完。
 返还 结果。
完。

※ 边缘检测

索贝尔_水平核 接受 ) ：返还 [-1、0、1、-2、0、2、-1、0、1] }：
索贝尔_垂直核 接受 ) ：返还 [-1、-2、-1、0、0、1、2、1] }：

边缘检测_索贝尔 接受 图像、宽、高) { 定义 gx、宽、高、做 索贝尔_水平核、3：
 gy 为 做 卷积_2D 用 图像、宽、高、做 索贝尔_垂直核、3。
 可变 结果 为 []。
 可变 i 为 0。
 只要 i 小于 做 计数 用 gx：
 做 追加 用 结果、幅度。
 i 为 i 加 1。
 完。
 返还 结果。
完。

※ 池化

最大池化_2D 接受 图像、宽、高、池大小) { 定义 可变 结果：
 新宽 为 宽 除以 池大小。
 新高 为 高 除以 池大小。
 可变 y 为 0。
 只要 y 小于 新高：
 只要 x 小于 新宽：
 可变 py 为 0。
 只要 py 小于 池大小：
 只要 px 小于 池大小：
 若 数值 大于 最大：
 px 为 px 加 1。
 完。
 py 为 py 加 1。
 完。
 做 追加 用 结果、最大。
 x 为 x 加 1。
 完。
 y 为 y 加 1。
 完。
 返还 [结果, 新宽, 新高]。
完。

平均池化_2D 接受 图像、宽、高、池大小) { 定义 可变 结果：
 新宽 为 宽 除以 池大小。
 新高 为 高 除以 池大小。
 可变 y 为 0。
 只要 y 小于 新高：
 只要 x 小于 新宽：
 可变 py 为 0。
 只要 py 小于 池大小：
 只要 px 小于 池大小：
 总 为 总 加 图像[(y 乘 池大小 加 py) 乘 宽 加 (x 乘 池大小 加 px)]。
 px 为 px 加 1。
 完。
 py 为 py 加 1。
 完。
 做 追加 用 结果、总 除以 (池大小 乘 池大小)。
 x 为 x 加 1。
 完。
 y 为 y 加 1。
 完。
 返还 [结果, 新宽, 新高]。
完。

※ 图像缩放

最近邻缩放 接受 图像、原宽、原高、新宽、新高) { 定义 可变 结果：
 可变 y 为 0。
 只要 y 小于 新高：
 只要 x 小于 新宽：
 sx 为 x 乘 原宽 除以 新宽。
 做 追加 用 结果、图像[sy 乘 原宽 加 sx]。
 x 为 x 加 1。
 完。
 y 为 y 加 1。
 完。
 返还 结果。
完。

可变 _视觉_像素数 为 0。

视觉_记录操作 接受 像素) { _视觉_像素数：

本章 收束。
