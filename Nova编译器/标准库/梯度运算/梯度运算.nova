《梯度运算》

引用《..·基础》。

引用《..·数学·基础·数学库》。

引用《..·算法·数学扩展》。

引用《..·..·编译器·标准前导》。

梯度_累积 接受 累积梯度、新梯度：
    返还 做 拉链 用 累积梯度、新梯度、以 a、b 映射到 a 加 b。
完。

梯度_平均 接受 累积梯度、步数：
    返还 做 映射 用 累积梯度、以 g 映射到 g 除以 步数。
完。

梯度运算_梯度_范数 接受 梯度：
    返还 做 整数sqrt 用 做 折叠 用 梯度、0、以 s、g 映射到 s 加 g 乘 g。
完。

梯度_裁剪_范数 接受 梯度、最大范数：
    范数 为 做 梯度运算_梯度_范数 用 梯度。
    若 范数 小于或等于 最大范数：
        返还 梯度。
    完。
    返还 做 映射 用 梯度、以 g 映射到 g 乘 最大范数 除以 范数。
完。

梯度_裁剪_值 接受 梯度、最大值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度：
        可变 v 为 梯度 的 第 i 项。
        若 v 大于 最大值：
            v 为 最大值。
        完。
        若 v 小于 0 减 最大值：
            v 为 0 减 最大值。
        完。
        做 追加 用 结果、v。
        i 为 i 加 1。
    完。
    返还 结果。
完。

梯度_缩放 接受 梯度、缩放因子：
    返还 做 映射 用 梯度、以 g 映射到 g 乘 缩放因子 除以 100。
完。

梯度_反缩放 接受 梯度、缩放因子：
    返还 做 映射 用 梯度、以 g 映射到 g 乘 100 除以 缩放因子。
完。

梯度_TopK 接受 梯度、K：
    可变 结果 为 一组：（空）。
    abs_list 为 做 映射 用 梯度、以 g 映射到 做 绝对值 用 g。
    sorted_abs 为 做 复制列表 用 abs_list。
    做 排序 用 sorted_abs、0。
    n 为 做 计数 用 sorted_abs。
    可变 阈值k 为 0。
    若 K 小于或等于 n：
        阈值k 为 sorted_abs 的 第 n 减 K 项。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度：
        若 做 绝对值 用 梯度 的 第 i 项 大于或等于 阈值k 并且 K 大于 0：
            做 追加 用 结果、梯度 的 第 i 项。
        否则：
            做 追加 用 结果、0。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

梯度_是零 接受 梯度：
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度：
        若 梯度 的 第 i 项 不等于 0：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

梯度_噪声态 为 33333。

梯度_添加噪声 接受 梯度、噪声标准差：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度：
        梯度_噪声态 为 梯度_噪声态 位异或 梯度_噪声态 左移 13。
        梯度_噪声态 为 梯度_噪声态 位异或 梯度_噪声态 右移 7。
        梯度_噪声态 为 梯度_噪声态 位异或 梯度_噪声态 左移 17。
        若 梯度_噪声态 小于 0：
            梯度_噪声态 为 0 减 梯度_噪声态。
        完。
        噪声 为 梯度_噪声态 取余于 (噪声标准差 乘 2 加 1) 减 噪声标准差。
        做 追加 用 结果、梯度 的 第 i 项 加 噪声。
        i 为 i 加 1。
    完。
    返还 结果。
完。

梯度_最大 接受 梯度：
    可变 m 为 梯度 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 梯度：
        若 梯度 的 第 i 项 大于 m：
            m 为 梯度 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 m。
完。

梯度_最小 接受 梯度：
    可变 m 为 梯度 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 梯度：
        若 梯度 的 第 i 项 小于 m：
            m 为 梯度 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 m。
完。

梯度_均值 接受 梯度：
    若 做 计数 用 梯度 等于 0：
        返还 0。
    完。
    可变 s 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度：
        s 为 s 加 梯度 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 s 除以 做 计数 用 梯度。
完。

梯度_非零数 接受 梯度：
    可变 c 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度：
        若 梯度 的 第 i 项 不等于 0：
            c 为 c 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 c。
完。

梯度_稀疏度 接受 梯度：
    若 做 计数 用 梯度 等于 0：
        返还 0。
    完。
    返还 (做 计数 用 梯度 减 做 梯度_非零数 用 梯度) 乘 1000 除以 做 计数 用 梯度。
完。

梯度_检测异常 接受 梯度、爆炸阈值、消失阈值：
    范数 为 做 梯度运算_梯度_范数 用 梯度。
    若 爆炸 等于 0：
        返还 0。
    完。
    若 范数 大于 爆炸阈值：
        返还 2。
    完。
    若 消失 等于 0：
        返还 0。
    完。
    若 范数 小于 消失阈值：
        返还 1。
    完。
    返还 0。
完。

可变 累积_目标步数 为 1。

累积_初始化 接受 维度、目标步数：
    累积_缓冲 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 累积_缓冲、0。
        i 为 i 加 1。
    完。
    累积_步数 为 0。
    累积_目标步数 为 目标步数。
完。

累积_追加 接受 梯度：
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度 并且 i 小于 做 计数 用 累积_缓冲：
        累积_缓冲 的 第 i 项 为 累积_缓冲 的 第 i 项 加 梯度 的 第 i 项。
        i 为 i 加 1。
    完。
    累积_步数 为 累积_步数 加 1。
完。

累积_就绪 接受 零参：
    返还 累积_步数 大于或等于 累积_目标步数。
完。

累积_获取 接受 零参：
    返还 做 梯度_平均 用 累积_缓冲、累积_步数。
完。

累积_重置 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 累积_缓冲：
        累积_缓冲 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    累积_步数 为 0。
完。

梯度_直方图 接受 梯度、桶数：
    最小 为 做 梯度_最小 用 梯度。
    最大 为 做 梯度_最大 用 梯度。
    可变 范围 为 最大 减 最小。
    若 范围 等于 0：
        范围 为 1。
    完。
    可变 桶宽 为 范围 除以 桶数。
    若 桶宽 等于 0：
        桶宽 为 1。
    完。
    可变 桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 桶数：
        做 追加 用 桶、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 梯度：
        可变 索引 为 (梯度 的 第 i 项 减 最小) 除以 桶宽。
        若 索引 大于或等于 桶数：
            索引 为 桶数 减 1。
        完。
        若 索引 小于 0：
            索引 为 0。
        完。
        桶 的 第 索引 项 为 桶 的 第 索引 项 加 1。
        i 为 i 加 1。
    完。
    返还 桶。
完。

梯度_余弦相似 接受 g1、g2：
    可变 点积 为 0。
    可变 n1 为 0。
    可变 n2 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 g1 并且 i 小于 做 计数 用 g2：
        点积 为 点积 加 g1 的 第 i 项 乘 g2 的 第 i 项 除以 1000。
        n1 为 n1 加 g1 的 第 i 项 乘 g1 的 第 i 项 除以 1000。
        n2 为 n2 加 g2 的 第 i 项 乘 g2 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    denom 为 做 整数sqrt 用 n1 乘 做 整数sqrt 用 n2。
    若 denom 等于 0：
        返还 0。
    完。
    返还 点积 乘 1000 除以 denom。
完。

梯度_EMA 接受 当前、历史、α千分：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 当前：
        可变 h 为 0。
        若 i 小于 做 计数 用 历史：
            h 为 历史 的 第 i 项。
        完。
        做 追加 用 结果、α千分 乘 当前 的 第 i 项 除以 1000 加 (1000 减 α千分) 乘 h 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

本章 收束。
