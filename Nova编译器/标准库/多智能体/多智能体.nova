《多智能体》

引用《..·基础》。

MA_随机态 为 27182。

MA_随机 接受 零参：
    MA_随机态 为 MA_随机态 位异或 MA_随机态 左移 13。
    MA_随机态 为 MA_随机态 位异或 MA_随机态 右移 7。
    MA_随机态 为 MA_随机态 位异或 MA_随机态 左移 17。
    若 MA_随机态 小于 0：
        MA_随机态 为 0 减 MA_随机态。
    完。
    返还 MA_随机态。
完。

角色_执行者 为 0。

角色_规划者 为 1。

角色_评审者 为 2。

角色_协调者 为 3。

角色_专家 为 4。

智能体_创建 接受 id、角色、动作空间：
    返还 一组：id、角色、一组：（空）、动作空间、0、一组：（空）、一组：（空）。
完。

智能体_ID 接受 a：
    返还 a 的 第 0 项。
完。

智能体_角色 接受 a：
    返还 a 的 第 1 项。
完。

智能体_状态 接受 a：
    返还 a 的 第 2 项。
完。

智能体_奖励 接受 a：
    返还 a 的 第 4 项。
完。

智能体_设状态 接受 a、状态：
    a 的 第 2 项 为 状态。
完。

智能体_加奖励 接受 a、r：
    a 的 第 4 项 为 a 的 第 4 项 加 r。
完。

智能体_发消息 接受 a、消息：
    做 追加 用 a 的 第 5 项、消息。
完。

智能体_收消息 接受 a：
    可变 新队列 为 一组：（空）。
    若 做 计数 用 a 的 第 5 项 等于 0：
        返还 空。
    完。
    msg 为 a 的 第 5 项 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 a 的 第 5 项：
        做 追加 用 新队列、a 的 第 5 项 的 第 i 项。
        i 为 i 加 1。
    完。
    a 的 第 5 项 为 新队列。
    返还 msg。
完。

智能体_添加能力 接受 a、能力哈希：
    做 追加 用 a 的 第 6 项、能力哈希。
完。

多智能体_环境_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、一组：（空）、0、一组：（空）。
完。

环境_添加智能体 接受 环境、agent：
    做 追加 用 环境 的 第 0 项、agent。
完。

环境_智能体数 接受 环境：
    返还 做 计数 用 环境 的 第 0 项。
完。

环境_广播 接受 环境、发送者ID、消息：
    可变 i 为 0。
    只要 i 小于 做 计数 用 环境 的 第 0 项：
        若 做 智能体_ID 用 环境 的 第 0 项 的 第 i 项 不等于 发送者ID：
            做 智能体_发消息 用 环境 的 第 0 项 的 第 i 项、一组：发送者ID、消息。
        完。
        i 为 i 加 1。
    完。
完。

环境_点对点 接受 环境、发送者ID、接收者ID、消息：
    可变 i 为 0。
    只要 i 小于 做 计数 用 环境 的 第 0 项：
        若 做 智能体_ID 用 环境 的 第 0 项 的 第 i 项 等于 接收者ID：
            做 智能体_发消息 用 环境 的 第 0 项 的 第 i 项、一组：发送者ID、消息。
        完。
        i 为 i 加 1。
    完。
完。

QMIX_Q总 接受 个体Q列表、混合权重、混合偏置：
    n 为 做 计数 用 个体Q列表。
    可变 加权和 为 混合偏置。
    可变 i 为 0。
    只要 i 小于 n：
        可变 w 为 1000 除以 n。
        若 i 小于 做 计数 用 混合权重：
            w 为 混合权重 的 第 i 项。
            若 w 小于 0：
                w 为 0。
            完。
        完。
        加权和 为 加权和 加 w 乘 个体Q列表 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 加权和。
完。

VDN_Q总 接受 个体Q列表：
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 个体Q列表：
        总 为 总 加 个体Q列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

通信_均值池化 接受 智能体隐藏列表、排除idx：
    n 为 做 计数 用 智能体隐藏列表。
    若 n 小于或等于 1：
        返还 一组：（空）。
    完。
    维度 为 做 计数 用 智能体隐藏列表 的 第 0 项。
    可变 均值 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 均值、0。
        d 为 d 加 1。
    完。
    可变 计数值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 不等于 排除idx：
            d 为 0。
            只要 d 小于 维度：
                若 d 小于 做 计数 用 智能体隐藏列表 的 第 i 项：
                    均值 的 第 d 项 为 均值 的 第 d 项 加 智能体隐藏列表 的 第 i 项 的 第 d 项。
                完。
                d 为 d 加 1。
            完。
            计数值 为 计数值 加 1。
        完。
        i 为 i 加 1。
    完。
    若 计数值 大于 0：
        d 为 0。
        只要 d 小于 维度：
            均值 的 第 d 项 为 均值 的 第 d 项 除以 计数值。
            d 为 d 加 1。
        完。
    完。
    返还 均值。
完。

角色_自动分配 接受 环境、任务需求列表：
    n 为 做 环境_智能体数 用 环境。
    可变 分配 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务需求列表：
        可变 最佳 为 负 1。
        可变 最佳分 为 负 1。
        可变 j 为 0。
        只要 j 小于 n：
            能力 为 环境 的 第 0 项 的 第 j 项 的 第 6 项。
            需求 为 任务需求列表 的 第 i 项。
            可变 分数 为 0。
            可变 k 为 0。
            只要 k 小于 做 计数 用 能力：
                若 能力 的 第 k 项 等于 需求：
                    分数 为 分数 加 1000。
                完。
                k 为 k 加 1。
            完。
            若 分数 大于 最佳分：
                最佳 为 j。
                最佳分 为 分数。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 分配、一组：i、最佳。
        i 为 i 加 1。
    完。
    返还 分配。
完。

委派_创建 接受 任务描述哈希：
    返还 一组：任务描述哈希、负 1、0、一组：（空）、0。
完。

委派_分配 接受 委派、智能体ID：
    委派 的 第 1 项 为 智能体ID。
完。

委派_完成 接受 委派、结果：
    委派 的 第 2 项 为 1。
    委派 的 第 4 项 为 结果。
完。

委派_是否完成 接受 委派：
    返还 委派 的 第 2 项。
完。

委派_添加子任务 接受 委派、子任务：
    做 追加 用 委派 的 第 3 项、子任务。
完。

委派_所有完成 接受 委派：
    可变 i 为 0。
    只要 i 小于 做 计数 用 委派 的 第 3 项：
        若 做 委派_是否完成 用 委派 的 第 3 项 的 第 i 项 等于 0：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

对话轮_创建 接受 零参：
    返还 一组：一组：（空）。
完。

对话轮_发言 接受 对话、说话者ID、内容哈希：
    做 追加 用 对话 的 第 0 项、一组：说话者ID、内容哈希、对话 的 第 2 项。
    对话 的 第 2 项 为 对话 的 第 2 项 加 1。
完。

对话轮_轮转 接受 对话、智能体数：
    对话 的 第 1 项 为 (对话 的 第 1 项 加 1) 取余于 智能体数。
    返还 对话 的 第 1 项。
完。

对话轮_历史长度 接受 对话：
    返还 做 计数 用 对话 的 第 0 项。
完。

投票_多数 接受 投票列表、选项数：
    可变 计数列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 选项数：
        做 追加 用 计数列表、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 投票列表：
        v 为 投票列表 的 第 i 项。
        若 v 大于或等于 0 并且 v 小于 选项数：
            计数列表 的 第 v 项 为 计数列表 的 第 v 项 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 最多 为 0。
    i 为 1。
    只要 i 小于 选项数：
        若 计数列表 的 第 i 项 大于 计数列表 的 第 最多 项：
            最多 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最多。
完。

团队奖励_分配 接受 总奖励、贡献列表：
    n 为 做 计数 用 贡献列表。
    可变 总贡献 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总贡献 为 总贡献 加 贡献列表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 总贡献 等于 0：
        总贡献 为 n。
    完。
    可变 分配 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 分配、总奖励 乘 贡献列表 的 第 i 项 除以 总贡献。
        i 为 i 加 1。
    完。
    返还 分配。
完。

本章 收束。
