《数据增强》

引用《..·基础》。

增强_随机态 为 11111。

增强_复制列表 接受 源：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 源：
        做 追加 用 结果、源 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

增强_列表最小 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    可变 最小值 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 小于 最小值：
            最小值 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小值。
完。

增强_列表最大 接受 数据：
    若 做 计数 用 数据 等于 0：
        返还 0。
    完。
    可变 最大值 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 大于 最大值：
            最大值 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大值。
完。

增强_整数平方根 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 左 为 1。
    可变 右 为 n。
    可变 答案 为 0。
    只要 左 小于或等于 右：
        可变 中 为 (左 加 右) 除以 2。
        可变 平方 为 中 乘 中。
        若 平方 小于或等于 n：
            答案 为 中。
            左 为 中 加 1。
        否则：
            右 为 中 减 1。
        完。
    完。
    返还 答案。
完。

增强_随机 接受 零参：
    增强_随机态 为 增强_随机态 乘 1103515245 加 12345。
    若 增强_随机态 小于 0：
        增强_随机态 为 0 减 增强_随机态。
    完。
    返还 增强_随机态。
完。

高斯噪声 接受 数据、幅度：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        噪声 为 做 增强_随机 取余于 (幅度 乘 2 加 1) 减 幅度。
        做 追加 用 结果、数据 的 第 i 项 加 噪声。
        i 为 i 加 1。
    完。
    返还 结果。
完。

椒盐噪声 接受 数据、概率、最小值、最大值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        r 为 做 增强_随机 取余于 1000。
        若 r 小于 概率：
            若 r 取余于 2 等于 0：
                做 追加 用 结果、最小值。
            否则：
                做 追加 用 结果、最大值。
            完。
        否则：
            做 追加 用 结果、数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

水平翻转 接受 数据：
    可变 结果 为 一组：（空）。
    可变 i 为 做 计数 用 数据 减 1。
    只要 i 大于或等于 0：
        做 追加 用 结果、数据 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 结果。
完。

随机裁剪 接受 数据、裁剪长度：
    若 裁剪长度 大于或等于 做 计数 用 数据：
        返还 做 增强_复制列表 用 数据。
    完。
    起点 为 做 增强_随机 取余于 (做 计数 用 数据 减 裁剪长度 加 1)。
    可变 结果 为 一组：（空）。
    可变 i 为 起点。
    只要 i 小于 起点 加 裁剪长度：
        做 追加 用 结果、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

特征缩放 接受 数据、缩放因子：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 结果、数据 的 第 i 项 乘 缩放因子 除以 100。
        i 为 i 加 1。
    完。
    返还 结果。
完。

增强_归一化 接受 数据：
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总和 为 总和 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 总和 除以 n。
    可变 平方和 为 0。
    i 为 0。
    只要 i 小于 n：
        可变 差值 为 数据 的 第 i 项 减 均值。
        平方和 为 平方和 加 差值 乘 差值。
        i 为 i 加 1。
    完。
    可变 方差 为 平方和 除以 n。
    可变 标准差 为 做 增强_整数平方根 用 方差。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、(数据 的 第 i 项 减 均值) 乘 100 除以 标准差。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数据增强_归一化 接受 数据、目标最小、目标最大：
    若 做 计数 用 数据 等于 0：
        返还 一组：（空）。
    完。
    原最小 为 做 增强_列表最小 用 数据。
    原最大 为 做 增强_列表最大 用 数据。
    可变 原范围 为 原最大 减 原最小。
    若 原范围 等于 0：
        原范围 为 1。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 新值 为 目标最小 加 (数据 的 第 i 项 减 原最小) 乘 (目标最大 减 目标最小) 除以 原范围。
        做 追加 用 结果、新值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

混合增强 接受 数据A、数据B、lambda_v：
    可变 长度 为 做 计数 用 数据A。
    若 做 计数 用 数据B 小于 长度：
        长度 为 做 计数 用 数据B。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度：
        可变 混合值 为 lambda_v 乘 数据A 的 第 i 项 除以 1000 加 (1000 减 lambda_v) 乘 数据B 的 第 i 项 除以 1000。
        做 追加 用 结果、混合值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

裁剪混合 接受 数据A、数据B、起始、长度：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据A：
        若 i 大于或等于 起始：
            若 i 小于 起始 加 长度：
                若 i 小于 做 计数 用 数据B：
                    做 追加 用 结果、数据B 的 第 i 项。
                否则：
                    做 追加 用 结果、数据A 的 第 i 项。
                完。
            否则：
                做 追加 用 结果、数据A 的 第 i 项。
            完。
        否则：
            做 追加 用 结果、数据A 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

随机擦除 接受 数据、擦除比例：
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 擦除长度 为 n 乘 擦除比例 除以 1000。
    若 擦除长度 小于 1：
        擦除长度 为 1。
    完。
    若 擦除长度 大于 n：
        擦除长度 为 n。
    完。
    起点 为 做 增强_随机 取余于 (n 减 擦除长度 加 1)。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 大于或等于 起点：
            若 i 小于 起点 加 擦除长度：
                做 追加 用 结果、0。
            否则：
                做 追加 用 结果、数据 的 第 i 项。
            完。
        否则：
            做 追加 用 结果、数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

填充均值 接受 数据、缺失标记：
    可变 总和 为 0。
    可变 有效数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 不等于 缺失标记：
            总和 为 总和 加 数据 的 第 i 项。
            有效数 为 有效数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 有效数 等于 0：
        返还 做 增强_复制列表 用 数据。
    完。
    均值 为 总和 除以 有效数。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 等于 缺失标记：
            做 追加 用 结果、均值。
        否则：
            做 追加 用 结果、数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

填充前值 接受 数据、缺失标记：
    可变 结果 为 一组：（空）。
    可变 上一个值 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 不等于 缺失标记：
            上一个值 为 数据 的 第 i 项。
        完。
        做 追加 用 结果、上一个值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

过采样 接受 数据、目标数量：
    若 做 计数 用 数据 等于 0：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 做 计数 用 结果 小于 目标数量：
        做 追加 用 结果、数据 的 第 i 取余于 做 计数 用 数据 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

欠采样 接受 数据、目标数量：
    若 目标数量 大于或等于 做 计数 用 数据：
        返还 做 增强_复制列表 用 数据。
    完。
    若 目标数量 小于或等于 0：
        返还 一组：（空）。
    完。
    步长 为 做 计数 用 数据 乘 1000 除以 目标数量。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 目标数量：
        索引 为 i 乘 步长 除以 1000。
        做 追加 用 结果、数据 的 第 索引 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数据增强_滑动窗口 接受 数据、窗口大小、步长：
    可变 窗口 为 一组：（空）。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 窗口大小 小于或等于 做 计数 用 数据：
        可变 j 为 0。
        只要 j 小于 窗口大小：
            做 追加 用 窗口、数据 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、窗口。
        i 为 i 加 步长。
    完。
    返还 结果。
完。

本章 收束。
