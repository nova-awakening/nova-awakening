《Forth转换器》

引用《..·基础》。

引用《..·..·运行时·字典引擎·字典引擎》。

可变 映射键 为 一组：（空）。

可变 映射值 为 一组：（空）。

Fort_添加映射 接受 f、n：
    做 追加 用 映射键、f。
    做 追加 用 映射值、n。
完。

Fort_查找映射 接受 词：
    可变 i 为 0。
    只要 i 小于 做 计数 用 映射键：
        若 做 字符串相等 用 映射键 的 第 i 项、词：
            返还 映射值 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

Fort_初始化映射 接受 零参：
    做 添加映射 用 「DUP」、「栈_复制」。
    做 添加映射 用 「DROP」、「栈_丢弃」。
    做 添加映射 用 「SWAP」、「栈_交换」。
    做 添加映射 用 「OVER」、「栈_越过」。
    做 添加映射 用 「ROT」、「栈_旋转」。
    做 添加映射 用 「NIP」、「栈_抽去」。
    做 添加映射 用 「TUCK」、「栈_塞入」。
    做 添加映射 用 「2DUP」、「栈_双复制」。
    做 添加映射 用 「2DROP」、「栈_双丢弃」。
    做 添加映射 用 「>R」、「转返回栈」。
    做 添加映射 用 「R>」、「从返回栈」。
    做 添加映射 用 「R@」、「读返回栈」。
    做 添加映射 用 「?DUP」、「栈_条件复制」。
    做 添加映射 用 「DEPTH」、「栈_深度」。
    做 添加映射 用 「SP@」、「栈指针」。
    做 添加映射 用 「+」、「+」。
    做 添加映射 用 「-」、「-」。
    做 添加映射 用 「*」、「*」。
    做 添加映射 用 「/」、「/」。
    做 添加映射 用 「MOD」、「%」。
    做 添加映射 用 「NEGATE」、「取负」。
    做 添加映射 用 「ABS」、「绝对值」。
    做 添加映射 用 「MIN」、「最小」。
    做 添加映射 用 「MAX」、「最大」。
    做 添加映射 用 「1+」、「递增」。
    做 添加映射 用 「1-」、「递减」。
    做 添加映射 用 「LSHIFT」、「<<」。
    做 添加映射 用 「RSHIFT」、「>>」。
    做 添加映射 用 「=」、「==」。
    做 添加映射 用 「<>」、「!=」。
    做 添加映射 用 「<」、「<」。
    做 添加映射 用 「>」、「>」。
    做 添加映射 用 「0=」、「== 0」。
    做 添加映射 用 「0<」、「< 0」。
    做 添加映射 用 「0>」、「> 0」。
    做 添加映射 用 「AND」、「&」。
    做 添加映射 用 「OR」、「|」。
    做 添加映射 用 「XOR」、「^」。
    做 添加映射 用 「INVERT」、「~」。
    做 添加映射 用 「TRUE」、「真」。
    做 添加映射 用 「FALSE」、「假」。
    做 添加映射 用 「IF」、「若」。
    做 添加映射 用 「ELSE」、「否」。
    做 添加映射 用 「THEN」、「}」。
    做 添加映射 用 「ENDIF」、「}」。
    做 添加映射 用 「BEGIN」、「循环开始 {」。
    做 添加映射 用 「WHILE」、「当」。
    做 添加映射 用 「REPEAT」、「}」。
    做 添加映射 用 「UNTIL」、「直到」。
    做 添加映射 用 「DO」、「遍历」。
    做 添加映射 用 「LOOP」、「循环结束」。
    做 添加映射 用 「I」、「循环变量I」。
    做 添加映射 用 「J」、「循环变量J」。
    做 添加映射 用 「LEAVE」、「止」。
    做 添加映射 用 「EXIT」、「回」。
    做 添加映射 用 「RECURSE」、「递归」。
    做 添加映射 用 「CASE」、「选择 {」。
    做 添加映射 用 「OF」、「当值」。
    做 添加映射 用 「ENDOF」、「结束值」。
    做 添加映射 用 「ENDCASE」、「}」。
    做 添加映射 用 「:」、「法」。
    做 添加映射 用 「;」、「}」。
    做 添加映射 用 「CONSTANT」、「常量」。
    做 添加映射 用 「VARIABLE」、「变量」。
    做 添加映射 用 「VALUE」、「值」。
    做 添加映射 用 「TO」、「赋值」。
    做 添加映射 用 「CREATE」、「创建」。
    做 添加映射 用 「DOES>」、「行为」。
    做 添加映射 用 「!」、「内存写」。
    做 添加映射 用 「@」、「内存读」。
    做 添加映射 用 「+!」、「内存加」。
    做 添加映射 用 「C!」、「字节写」。
    做 添加映射 用 「C@」、「字节读」。
    做 添加映射 用 「CELLS」、「单元」。
    做 添加映射 用 「ALLOT」、「分配」。
    做 添加映射 用 「ALLOCATE」、「堆分配」。
    做 添加映射 用 「FREE」、「释放」。
    做 添加映射 用 「HERE」、「此处」。
    做 添加映射 用 「FILL」、「填充」。
    做 添加映射 用 「MOVE」、「移动」。
    做 添加映射 用 「.」、「打印数」。
    做 添加映射 用 「.S」、「打印栈」。
    做 添加映射 用 「EMIT」、「输出字符」。
    做 添加映射 用 「CR」、「换行」。
    做 添加映射 用 「SPACE」、「空格」。
    做 添加映射 用 「TYPE」、「输出字串」。
    做 添加映射 用 「KEY」、「读键」。
    做 添加映射 用 「COUNT」、「计数」。
    做 添加映射 用 「HEX」、「十六进制」。
    做 添加映射 用 「DECIMAL」、「十进制」。
    做 添加映射 用 「BL」、「空格码」。
    做 添加映射 用 「INCLUDE」、「包含」。
    做 添加映射 用 「THROW」、「抛异常」。
    做 添加映射 用 「CATCH」、「捕异常」。
    做 添加映射 用 「ABORT」、「中止」。
    做 添加映射 用 「CMOVE」、「字节移动」。
    做 添加映射 用 「SYSTEM-TICKS」、「系统时钟」。
    做 添加映射 用 「POSTPONE」、「延迟」。
    做 添加映射 用 「AGAIN」、「再次」。
    做 添加映射 用 「UNLOOP」、「退出循环」。
    做 添加映射 用 「?DO」、「条件遍历」。
    做 添加映射 用 「+LOOP」、「步进循环」。
    做 添加映射 用 「EXECUTE」、「执行」。
    做 添加映射 用 「IMMEDIATE」、「立即」。
    做 添加映射 用 「WITHIN」、「范围内」。
    做 添加映射 用 「PICK」、「栈_选取」。
    做 添加映射 用 「ROLL」、「栈_滚动」。
    做 添加映射 用 「2SWAP」、「栈_双交换」。
    做 添加映射 用 「2OVER」、「栈_双越过」。
    做 添加映射 用 「RELU」、「ReLU激活」。
    做 添加映射 用 「SIGMOID」、「Sigmoid激活」。
    做 添加映射 用 「TANH」、「Tanh激活」。
    做 添加映射 用 「SOFTMAX」、「Softmax」。
    做 添加映射 用 「DROPOUT」、「随机丢弃」。
    做 添加映射 用 「MATMUL」、「矩阵乘」。
    做 添加映射 用 「TRANSPOSE」、「转置」。
    做 添加映射 用 「CONV1D」、「一维卷积」。
    做 添加映射 用 「MAXPOOL」、「最大池化」。
    做 添加映射 用 「AVGPOOL」、「平均池化」。
    做 添加映射 用 「LAYERNORM」、「层归一化」。
    做 添加映射 用 「BATCHNORM」、「批归一化」。
    做 添加映射 用 「ATTENTION」、「注意力」。
    做 添加映射 用 「EMBEDDING」、「嵌入层」。
    做 添加映射 用 「LINEAR」、「线性层」。
    做 添加映射 用 「FFN」、「前馈网络」。
    做 添加映射 用 「FORWARD」、「前向传播」。
    做 添加映射 用 「BACKWARD」、「反向传播」。
    做 添加映射 用 「SGD」、「随机梯度下降」。
    做 添加映射 用 「ADAM」、「自适应矩估计优化器」。
    做 添加映射 用 「LOSS」、「损失函数」。
    做 添加映射 用 「均方误差」、「均方误差」。
    做 添加映射 用 「CROSS-ENTROPY」、「交叉熵」。
    做 添加映射 用 「GRADIENT」、「梯度」。
    做 添加映射 用 「LEARNING-RATE」、「学习率」。
    做 添加映射 用 「ALLREDUCE」、「全归约」。
    做 添加映射 用 「BROADCAST」、「广播」。
    做 添加映射 用 「SCATTER」、「分发」。
    做 添加映射 用 「GATHER」、「聚集」。
    做 添加映射 用 「NODE-ID」、「节点ID」。
    做 添加映射 用 「CLUSTER-SIZE」、「集群大小」。
    做 添加映射 用 「AGENT」、「智能体」。
    做 添加映射 用 「PERCEIVE」、「感知」。
    做 添加映射 用 「REASON」、「推理」。
    做 添加映射 用 「DECIDE」、「决策」。
    做 添加映射 用 「ACT」、「执行动作」。
    做 添加映射 用 「REFLECT」、「反思」。
    做 添加映射 用 「GOAL」、「目标」。
    做 添加映射 用 「BELIEF」、「信念」。
    做 添加映射 用 「PLAN」、「计划」。
    做 添加映射 用 「QUANTIZE」、「量化」。
    做 添加映射 用 「DEQUANTIZE」、「反量化」。
    做 添加映射 用 「INT8」、「八位整数」。
    做 添加映射 用 「INT4」、「四位整数」。
    做 添加映射 用 「KV-CACHE」、「键值缓存」。
    做 添加映射 用 「BEAM-SEARCH」、「束搜索」。
    做 添加映射 用 「GREEDY」、「贪心解码」。
    做 添加映射 用 「TEMPERATURE」、「温度采样」。
    做 添加映射 用 「EPOCH」、「轮次」。
    做 添加映射 用 「BATCH」、「批次」。
    做 添加映射 用 「SHUFFLE」、「洗牌」。
    做 添加映射 用 「EARLY-STOP」、「早停」。
    做 添加映射 用 「CHECKPOINT」、「检查点」。
    做 添加映射 用 「LORA」、「低秩适配」。
    做 添加映射 用 「FINETUNE」、「微调」。
    做 添加映射 用 「EMA」、「指数移动平均」。
    做 添加映射 用 「SCHEDULER」、「调度器」。
    做 添加映射 用 「HASH」、「哈希」。
    做 添加映射 用 「HMAC认证」、「消息认证码」。
    做 添加映射 用 「ENCRYPT」、「加密」。
    做 添加映射 用 「DECRYPT」、「解密」。
    做 添加映射 用 「TOKEN」、「令牌」。
    做 添加映射 用 「PERMISSION」、「权限」。
    做 添加映射 用 「CONSCIOUSNESS」、「意识」。
    做 添加映射 用 「GWT」、「全局工作空间」。
    做 添加映射 用 「PHI」、「信息整合度」。
    做 添加映射 用 「SPOTLIGHT」、「注意力聚光灯」。
    做 添加映射 用 「WORKING-MEMORY」、「工作记忆」。
    做 添加映射 用 「METACOGNITION」、「元认知」。
    做 添加映射 用 「DEDUCTION」、「演绎推理」。
    做 添加映射 用 「INDUCTION」、「归纳推理」。
    做 添加映射 用 「ANALOGY」、「类比推理」。
    做 添加映射 用 「ABDUCTION」、「溯因推理」。
    做 添加映射 用 「FACT」、「事实」。
    做 添加映射 用 「RULE」、「规则」。
    做 添加映射 用 「INFERENCE」、「推理」。
    做 添加映射 用 「STDP」、「突触可塑性」。
    做 添加映射 用 「BCM」、「BCM学习」。
    做 添加映射 用 「CURRICULUM」、「课程学习」。
    做 添加映射 用 「REPLAY」、「经验回放」。
    做 添加映射 用 「CONTRASTIVE」、「对比学习」。
    做 添加映射 用 「FORGETTING」、「遗忘曲线」。
    做 添加映射 用 「BRAIN」、「全脑整合」。
    做 添加映射 用 「COGNITIVE-CYCLE」、「认知周期」。
    做 添加映射 用 「ENERGY-ALLOC」、「能量分配」。
    做 添加映射 用 「FEEDBACK」、「反馈回路」。
    做 添加映射 用 「MERIDIAN」、「经络」。
    做 添加映射 用 「LTC」、「液态时间常数」。
    做 添加映射 用 「YIN-YANG」、「阴阳平衡」。
    做 添加映射 用 「QI」、「气血循环」。
    做 添加映射 用 「SPARSE」、「稀疏」。
    做 添加映射 用 「COO」、「坐标格式」。
    做 添加映射 用 「CSR」、「压缩行格式」。
    做 添加映射 用 「SPARSITY」、「稀疏率」。
    做 添加映射 用 「SPARSE-MATMUL」、「稀疏矩阵乘」。
    做 添加映射 用 「XAVIER」、「Xavier初始化」。
    做 添加映射 用 「HE-INIT」、「方差缩放初始化」。
    做 添加映射 用 「KAIMING」、「Kaiming初始化」。
    做 添加映射 用 「NORMAL-INIT」、「正态初始化」。
    做 添加映射 用 「UNIFORM-INIT」、「均匀初始化」。
    做 添加映射 用 「ORTHOGONAL」、「正交初始化」。
    做 添加映射 用 「CONTENT-FILTER」、「内容过滤」。
    做 添加映射 用 「WATERMARK」、「水印」。
    做 添加映射 用 「ALIGNMENT」、「对齐检查」。
    做 添加映射 用 「TOXICITY」、「毒性检测」。
    做 添加映射 用 「GUARDRAIL」、「安全护栏」。
    做 添加映射 用 「SANDBOX」、「沙箱」。
    做 添加映射 用 「CAPABILITY」、「能力」。
    做 添加映射 用 「ACL」、「访问控制」。
    做 添加映射 用 「TENSOR-PARALLEL」、「张量并行」。
    做 添加映射 用 「PIPELINE-PARALLEL」、「管道并行」。
    做 添加映射 用 「FSDP」、「完全分片数据并行」。
    做 添加映射 用 「DDP」、「分布式数据并行」。
    做 添加映射 用 「GRADIENT-BUCKET」、「梯度桶」。
    做 添加映射 用 「COLUMN-SHARD」、「列分片」。
    做 添加映射 用 「ROW-SHARD」、「行分片」。
    做 添加映射 用 「FLASH-ATTENTION」、「闪存注意力」。
    做 添加映射 用 「PAGED-ATTENTION」、「分页注意力」。
    做 添加映射 用 「ROPE」、「旋转位置编码」。
    做 添加映射 用 「SLIDING-WINDOW」、「滑动窗口」。
    做 添加映射 用 「GQA」、「分组查询注意力」。
    做 添加映射 用 「CAUSAL-MASK」、「因果掩码」。
    做 添加映射 用 「MULTI-HEAD」、「多头注意力」。
    做 添加映射 用 「GPT-BLOCK」、「生成模型块」。
    做 添加映射 用 「LLAMA-BLOCK」、「大语言模型块」。
    做 添加映射 用 「BERT-ENCODER」、「BERT编码器」。
    做 添加映射 用 「VIT-PATCH」、「ViT块嵌入」。
    做 添加映射 用 「RMSNORM」、「RMS归一化」。
    做 添加映射 用 「SWIGLU」、「SwiGLU激活」。
    做 添加映射 用 「PPO」、「近端策略优化」。
    做 添加映射 用 「DPO」、「直接偏好优化」。
    做 添加映射 用 「RLHF」、「人类反馈强化学习」。
    做 添加映射 用 「REWARD-MODEL」、「奖励模型」。
    做 添加映射 用 「KL-DIVERGENCE」、「KL散度」。
    做 添加映射 用 「GAE」、「广义优势估计」。
    做 添加映射 用 「PREFERENCE」、「偏好数据」。
    做 添加映射 用 「REJECT-SAMPLE」、「拒绝采样」。
    做 添加映射 用 「RLE」、「游程编码」。
    做 添加映射 用 「DELTA-ENCODE」、「差分编码」。
    做 添加映射 用 「CHECKSUM」、「校验和」。
    做 添加映射 用 「FRAME」、「协议帧」。
    做 添加映射 用 「LRU」、「最近最少使用」。
    做 添加映射 用 「TTL」、「存活时间」。
    做 添加映射 用 「CACHE」、「缓存」。
    做 添加映射 用 「DAG」、「有向无环图」。
    做 添加映射 用 「TOPOLOGICAL-SORT」、「拓扑排序」。
    做 添加映射 用 「PIPELINE」、「管道」。
    做 添加映射 用 「RETRY」、「重试」。
    做 添加映射 用 「PRE-EMPHASIS」、「预加重」。
    做 添加映射 用 「FRAME-SPLIT」、「帧分割」。
    做 添加映射 用 「HAMMING」、「汉明窗」。
    做 添加映射 用 「VAD」、「语音活动检测」。
    做 添加映射 用 「SPECTRUM」、「频谱」。
    做 添加映射 用 「MFCC」、「梅尔频率倒谱」。
    做 添加映射 用 「CONV2D」、「二维卷积」。
    做 添加映射 用 「EDGE-DETECT」、「边缘检测」。
    做 添加映射 用 「THRESHOLD」、「阈值化」。
    做 添加映射 用 「HISTOGRAM」、「直方图」。
    做 添加映射 用 「FIR」、「有限脉冲响应」。
    做 添加映射 用 「AUTOCORRELATION」、「自相关」。
    做 添加映射 用 「BOOT」、「系统启动」。
    做 添加映射 用 「SELF-CHECK」、「自检」。
    做 添加映射 用 「MODULE-LOAD」、「模块加载」。
    做 添加映射 用 「HEALTH-CHECK」、「健康检查」。
    做 添加映射 用 「PROGRESS-BAR」、「进度条」。
    做 添加映射 用 「TABLE」、「表格」。
    做 添加映射 用 「MENU」、「菜单」。
    做 添加映射 用 「EDF」、「最早截止时间」。
    做 添加映射 用 「ROUND-ROBIN」、「轮转调度」。
    做 添加映射 用 「LOAD-BALANCE」、「负载均衡」。
    做 添加映射 用 「PRIORITY-QUEUE」、「优先级队列」。
    做 添加映射 用 「DIALOGUE」、「对话」。
    做 添加映射 用 「INTENT」、「意图」。
    做 添加映射 用 「SLOT」、「槽位」。
    做 添加映射 用 「CONFUSION-MATRIX」、「混淆矩阵」。
    做 添加映射 用 「PRECISION」、「精确率」。
    做 添加映射 用 「RECALL」、「召回率」。
    做 添加映射 用 「F1-SCORE」、「F1分数」。
    做 添加映射 用 「R方-SCORE」、「R2分数」。
    做 添加映射 用 「BENCHMARK」、「基准测试」。
    做 添加映射 用 「AUGMENTATION」、「数据增强」。
    做 添加映射 用 「NORMALIZE」、「归一化」。
    做 添加映射 用 「MODEL-REGISTRY」、「模型注册」。
    做 添加映射 用 「AB-TEST」、「AB测试」。
    做 添加映射 用 「ROLLBACK」、「回滚」。
    做 添加映射 用 「DEPLOY」、「部署」。
    做 添加映射 用 「YIN」、「阴」。
    做 添加映射 用 「PING」、「平」。
    做 添加映射 用 「YANG」、「阳」。
    做 添加映射 用 「TNF-QUANTIZE」、「TNF量化」。
    做 添加映射 用 「TNF-DEQUANTIZE」、「TNF反量化」。
    做 添加映射 用 「TNF*」、「TNF乘」。
    做 添加映射 用 「TNF+」、「TNF加」。
    做 添加映射 用 「TNF-NEGATE」、「TNF取反」。
    做 添加映射 用 「PACK-5-TRISTATE」、「打包5三态」。
    做 添加映射 用 「UNPACK-5-TRISTATE」、「解包5三态」。
    做 添加映射 用 「TNF-MATRIX」、「TNF矩阵」。
    做 添加映射 用 「TNF-LAYER」、「TNF层」。
    做 添加映射 用 「TNF-FORWARD」、「TNF前向」。
    做 添加映射 用 「TNF-SPARSE-DOT」、「TNF稀疏点积」。
    做 添加映射 用 「GRADIENT-ACCUMULATE」、「梯度累积」。
    做 添加映射 用 「GRADIENT-CLIP」、「梯度裁剪」。
    做 添加映射 用 「GRADIENT-SCALE」、「梯度缩放」。
    做 添加映射 用 「GRADIENT-NORM」、「梯度范数」。
    做 添加映射 用 「TOPK-SPARSE」、「TopK稀疏化」。
    做 添加映射 用 「GRADIENT-NOISE」、「梯度噪声」。
    做 添加映射 用 「MIXED-PRECISION」、「混合精度」。
    做 添加映射 用 「GREEDY-SAMPLE」、「贪心采样」。
    做 添加映射 用 「TOP-K」、「TopK采样」。
    做 添加映射 用 「TOP-P」、「TopP采样」。
    做 添加映射 用 「NUCLEUS」、「核采样」。
    做 添加映射 用 「REPETITION-PENALTY」、「重复惩罚」。
    做 添加映射 用 「CATEGORICAL」、「分类采样」。
    做 添加映射 用 「EMBEDDING-STORE」、「嵌入存储」。
    做 添加映射 用 「COSINE-SIM」、「余弦相似度」。
    做 添加映射 用 「KNN」、「KNN搜索」。
    做 添加映射 用 「EUCLIDEAN」、「欧氏距离」。
    做 添加映射 用 「VECTOR-NORM」、「向量范数」。
    做 添加映射 用 「DOT-PRODUCT」、「点积」。
    做 添加映射 用 「ASSERT-EQUAL」、「断言等于」。
    做 添加映射 用 「ASSERT-TRUE」、「断言真」。
    做 添加映射 用 「TEST-SUITE」、「测试套件」。
    做 添加映射 用 「TEST-GROUP」、「测试组」。
    做 添加映射 用 「TEST-REPORT」、「测试报告」。
    做 添加映射 用 「INSTANT-MEMORY」、「瞬时记忆」。
    做 添加映射 用 「WORKING-MEM」、「工作记忆」。
    做 添加映射 用 「EPISODIC」、「情景记忆」。
    做 添加映射 用 「SEMANTIC-MEM」、「语义记忆」。
    做 添加映射 用 「CONSOLIDATE」、「记忆巩固」。
    做 添加映射 用 「SPREADING-ACT」、「扩散激活」。
    做 添加映射 用 「DECAY」、「记忆衰减」。
    做 添加映射 用 「FOCAL-LOSS」、「Focal损失」。
    做 添加映射 用 「DICE-LOSS」、「Dice损失」。
    做 添加映射 用 「CONTRASTIVE-LOSS」、「对比损失」。
    做 添加映射 用 「TRIPLET-LOSS」、「三元组损失」。
    做 添加映射 用 「HINGE-LOSS」、「Hinge损失」。
    做 添加映射 用 「HUBER-LOSS」、「Huber损失」。
    做 添加映射 用 「LABEL-SMOOTH」、「标签平滑」。
    做 添加映射 用 「PIPELINE-MAP」、「管道映射」。
    做 添加映射 用 「PIPELINE-FILTER」、「管道过滤」。
    做 添加映射 用 「PIPELINE-AGG」、「管道聚合」。
    做 添加映射 用 「BATCH-SPLIT」、「批分割」。
    做 添加映射 用 「SLIDING-WIN」、「滑动窗口」。
    做 添加映射 用 「DEDUP」、「去重」。
    做 添加映射 用 「DOWNSAMPLE」、「降采样」。
    做 添加映射 用 「CONFIG-SET」、「配置设置」。
    做 添加映射 用 「CONFIG-GET」、「配置获取」。
    做 添加映射 用 「CONFIG-MERGE」、「配置合并」。
    做 添加映射 用 「CONFIG-VALIDATE」、「配置验证」。
    做 添加映射 用 「INI-SECTION」、「INI节」。
    做 添加映射 用 「UNIFORM-DIST」、「均匀分布」。
    做 添加映射 用 「NORMAL-DIST」、「正态分布」。
    做 添加映射 用 「BERNOULLI」、「伯努利」。
    做 添加映射 用 「BINOMIAL」、「二项分布」。
    做 添加映射 用 「POISSON」、「泊松分布」。
    做 添加映射 用 「EXPONENTIAL」、「指数分布」。
    做 添加映射 用 「MONTE-CARLO」、「蒙特卡洛」。
    做 添加映射 用 「VARIANCE」、「方差」。
    做 添加映射 用 「ADAMW」、「AdamW」。
    做 添加映射 用 「LAMB」、「LAMB优化器」。
    做 添加映射 用 「LION」、「Lion优化器」。
    做 添加映射 用 「ADAFACTOR」、「Adafactor」。
    做 添加映射 用 「WARMUP」、「预热」。
    做 添加映射 用 「COSINE-ANNEAL」、「余弦退火」。
    做 添加映射 用 「LINEAR-DECAY」、「线性衰减」。
    做 添加映射 用 「STEP-DECAY」、「阶梯衰减」。
    做 添加映射 用 「WEIGHT-DECAY」、「权重衰减」。
    做 添加映射 用 「广度优先」、「广度优先」。
    做 添加映射 用 「深度优先」、「深度优先」。
    做 添加映射 用 「DIJKSTRA」、「迪杰斯特拉」。
    做 添加映射 用 「ADJACENCY」、「邻接表」。
    做 添加映射 用 「IN-DEGREE」、「入度」。
    做 添加映射 用 「OUT-DEGREE」、「出度」。
    做 添加映射 用 「MST」、「最小生成树」。
    做 添加映射 用 「SCC」、「强连通分量」。
    做 添加映射 用 「RLE-COMPRESS」、「RLE压缩」。
    做 添加映射 用 「LZ77」、「LZ77压缩」。
    做 添加映射 用 「HUFFMAN」、「哈夫曼编码」。
    做 添加映射 用 「DELTA-CODE」、「差分编码」。
    做 添加映射 用 「BITSTREAM」、「位流」。
    做 添加映射 用 「COMPRESSION-RATIO」、「压缩率」。
    做 添加映射 用 「MATRIX-MUL」、「矩阵乘」。
    做 添加映射 用 「TRANSPOSE」、「转置」。
    做 添加映射 用 「DETERMINANT」、「行列式」。
    做 添加映射 用 「INVERSE」、「逆矩阵」。
    做 添加映射 用 「IDENTITY」、「单位矩阵」。
    做 添加映射 用 「TRACE」、「矩阵迹」。
    做 添加映射 用 「SMA」、「简单滑动均值」。
    做 添加映射 用 「EWMA」、「指数加权均值」。
    做 添加映射 用 「LINEAR-TREND」、「线性趋势」。
    做 添加映射 用 「PEAK-DETECT」、「峰值检测」。
    做 添加映射 用 「TCP-FSM」、「TCP状态机」。
    做 添加映射 用 「HTTP-REQ」、「HTTP请求」。
    做 添加映射 用 「WEBSOCKET-FRAME」、「WebSocket帧」。
    做 添加映射 用 「PORT」、「端口」。
    做 添加映射 用 「LEXER」、「词法器」。
    做 添加映射 用 「PARSER」、「解析器」。
    做 添加映射 用 「AST」、「抽象语法树」。
    做 添加映射 用 「EVAL」、「求值」。
    做 添加映射 用 「RECURSIVE-DESCENT」、「递归下降」。
    做 添加映射 用 「PRECEDENCE」、「优先级」。
    做 添加映射 用 「HASH-TABLE」、「哈希表」。
    做 添加映射 用 「HASH-INSERT」、「哈希表_插入」。
    做 添加映射 用 「HASH-LOOKUP」、「哈希表_查找」。
    做 添加映射 用 「HASH-DELETE」、「哈希表_删除」。
    做 添加映射 用 「HASH-CONTAINS」、「哈希表_包含」。
    做 添加映射 用 「LOAD-FACTOR」、「负载因子」。
    做 添加映射 用 「REHASH」、「rehash」。
    做 添加映射 用 「OPEN-ADDRESSING」、「开放寻址」。
    做 添加映射 用 「LINEAR-PROBE」、「线性探测」。
    做 添加映射 用 「COLLISION」、「冲突数」。
    做 添加映射 用 「SET-CREATE」、「集合_创建」。
    做 添加映射 用 「SET-UNION」、「集合_并集」。
    做 添加映射 用 「SET-INTERSECT」、「集合_交集」。
    做 添加映射 用 「SET-DIFF」、「集合_差集」。
    做 添加映射 用 「SET-SYMDIFF」、「集合_对称差」。
    做 添加映射 用 「SET-SUBSET」、「集合_是子集」。
    做 添加映射 用 「SET-SUPERSET」、「集合_是超集」。
    做 添加映射 用 「SET-EQUAL」、「集合_相等」。
    做 添加映射 用 「POWER-SET」、「集合_幂集」。
    做 添加映射 用 「SET-CONTAINS」、「集合_包含」。
    做 添加映射 用 「MIN-HEAP」、「最小堆」。
    做 添加映射 用 「MAX-HEAP」、「最大堆」。
    做 添加映射 用 「HEAP-INSERT」、「堆_插入」。
    做 添加映射 用 「HEAP-POP」、「堆_弹出」。
    做 添加映射 用 「HEAP-PEEK」、「堆_查看顶」。
    做 添加映射 用 「HEAP-SORT」、「堆排序」。
    做 添加映射 用 「SIFT-UP」、「上浮」。
    做 添加映射 用 「SIFT-DOWN」、「下沉」。
    做 添加映射 用 「TOP-K」、「TopK」。
    做 添加映射 用 「PRIORITY-QUEUE」、「优先队列」。
    做 添加映射 用 「REGEX-MATCH」、「完全匹配」。
    做 添加映射 用 「REGEX-SEARCH」、「搜索匹配」。
    做 添加映射 用 「REGEX-COUNT」、「匹配计数」。
    做 添加映射 用 「WILDCARD」、「通配符」。
    做 添加映射 用 「CHAR-CLASS」、「字符类」。
    做 添加映射 用 「QUANTIFIER」、「量词」。
    做 添加映射 用 「GREEDY」、「贪婪」。
    做 添加映射 用 「BACKTRACK」、「回溯」。
    做 添加映射 用 「ACT-RELU」、「激活_线性整流」。
    做 添加映射 用 「ACT-GELU」、「激活_高斯线性」。
    做 添加映射 用 「ACT-SWISH」、「激活_自门控」。
    做 添加映射 用 「ACT-SIGMOID」、「激活_S形」。
    做 添加映射 用 「ACT-TANH」、「激活_双曲正切」。
    做 添加映射 用 「ACT-MISH」、「激活_米什」。
    做 添加映射 用 「ACT-SELU」、「激活_自归一指数」。
    做 添加映射 用 「ACT-ELU」、「激活_指数线性」。
    做 添加映射 用 「LEAKY-RELU」、「激活_泄露线性」。
    做 添加映射 用 「SOFTPLUS」、「激活_软加」。
    做 添加映射 用 「HARD-SIGMOID」、「激活_硬S形」。
    做 添加映射 用 「HARD-SWISH」、「激活_硬自门控」。
    做 添加映射 用 「GLU」、「门控线性」。
    做 添加映射 用 「GEGLU」、「高斯门控线性」。
    做 添加映射 用 「SWIGLU」、「自门控线性」。
    做 添加映射 用 「MOE-INIT」、「混合专家_初始化」。
    做 添加映射 用 「MOE-FORWARD」、「混合专家_前向」。
    做 添加映射 用 「GATE-SCORES」、「混合专家_门控分数」。
    做 添加映射 用 「TOP-K-SELECT」、「topk_选择」。
    做 添加映射 用 「EXPERT-FORWARD」、「混合专家_专家前向」。
    做 添加映射 用 「LOAD-BALANCE-LOSS」、「负载均衡损失」。
    做 添加映射 用 「CAPACITY-FACTOR」、「容量上限」。
    做 添加映射 用 「SOFTMAX-GATE」、「归一化指数」。
    做 添加映射 用 「EWC-INIT」、「弹性权重_初始化」。
    做 添加映射 用 「EWC-PENALTY」、「弹性权重_惩罚」。
    做 添加映射 用 「FISHER-INFO」、「弹性权重_计算Fisher」。
    做 添加映射 用 「REPLAY-INIT」、「回放_初始化」。
    做 添加映射 用 「REPLAY-STORE」、「回放_存储」。
    做 添加映射 用 「REPLAY-SAMPLE」、「回放_采样」。
    做 添加映射 用 「PROGRESSIVE-NET」、「渐进_添加列」。
    做 添加映射 用 「MAML-INNER」、「MAML_内循环」。
    做 添加映射 用 「MAML-OUTER」、「MAML_外更新」。
    做 添加映射 用 「FORGETTING」、「遗忘度」。
    做 添加映射 用 「DISTILLATION」、「蒸馏损失」。
    做 添加映射 用 「BATCH-INFER」、「批处理_推理」。
    做 添加映射 用 「REQUEST-QUEUE」、「队列_入队」。
    做 添加映射 用 「STREAM-INIT」、「流_初始化」。
    做 添加映射 用 「STREAM-SEND」、「流_发送块」。
    做 添加映射 用 「LOAD-BALANCE」、「负载_轮询」。
    做 添加映射 用 「LEAST-CONN」、「负载_最少连接」。
    做 添加映射 用 「QPS」、「统计_QPS」。
    做 添加映射 用 「AVG-LATENCY」、「统计_平均延迟」。
    做 添加映射 用 「SINUSOIDAL-PE」、「正弦位置编码」。
    做 添加映射 用 「ROPE」、「RoPE_编码」。
    做 添加映射 用 「ALIBI」、「ALiBi_偏置矩阵」。
    做 添加映射 用 「ALIBI-SLOPE」、「ALiBi_斜率」。
    做 添加映射 用 「RELATIVE-POS」、「相对位置矩阵」。
    做 添加映射 用 「LEARNABLE-POS」、「学习位置_查询」。
    做 添加映射 用 「RNN-CELL」、「RNN_单元」。
    做 添加映射 用 「LSTM-CELL」、「长短记忆_单元」。
    做 添加映射 用 「GRU-CELL」、「门控循环_单元」。
    做 添加映射 用 「RNN-SEQ」、「RNN_序列」。
    做 添加映射 用 「BIDIRECTIONAL」、「双向_RNN」。
    做 添加映射 用 「HEALTH-CHECK」、「健康_检查」。
    做 添加映射 用 「CIRCUIT-BREAKER」、「熔断器_允许」。
    做 添加映射 用 「RETRY-BACKOFF」、「重试_指数退避」。
    做 添加映射 用 「RATE-LIMIT」、「限流_请求」。
    做 添加映射 用 「ISOLATION」、「隔离_添加」。
    做 添加映射 用 「CHECKPOINT-SAVE」、「检查点_保存」。
    做 添加映射 用 「CHECKPOINT-RESTORE」、「检查点_恢复」。
    做 添加映射 用 「SELF-HEAL」、「健康_总体」。
    做 添加映射 用 「ACCURACY」、「准确率」。
    做 添加映射 用 「PRECISION」、「精确率」。
    做 添加映射 用 「RECALL」、「召回率」。
    做 添加映射 用 「F1-SCORE」、「F1分数」。
    做 添加映射 用 「CONFUSION-MATRIX」、「混淆矩阵」。
    做 添加映射 用 「平均绝对误差」、「平均绝对误差」。
    做 添加映射 用 「均方误差」、「均方误差」。
    做 添加映射 用 「BLEU」、「BLEU单元」。
    做 添加映射 用 「PERPLEXITY」、「困惑度」。
    做 添加映射 用 「TOP-K-ACC」、「TopK准确率」。
    做 添加映射 用 「CHECKPOINT-SAVE」、「ckpt_保存」。
    做 添加映射 用 「CHECKPOINT-LOAD」、「ckpt_加载最新」。
    做 添加映射 用 「CHECKPOINT-VERSION」、「ckpt_加载版本」。
    做 添加映射 用 「INCREMENTAL-DIFF」、「增量_计算差异」。
    做 添加映射 用 「AUTO-SAVE」、「自动保存_检查」。
    做 添加映射 用 「BEST-MODEL」、「最佳_更新」。
    做 添加映射 用 「EARLY-STOP」、「早停_检查」。
    做 添加映射 用 「LR-STEP」、「lr_阶梯」。
    做 添加映射 用 「LR-COSINE」、「lr_余弦」。
    做 添加映射 用 「LR-WARMUP」、「lr_预热」。
    做 添加映射 用 「EMA-UPDATE」、「ema_更新」。
    做 添加映射 用 「GRAD-NORM」、「梯度范数」。
    做 添加映射 用 「GRAD-CLIP」、「梯度裁剪」。
    做 添加映射 用 「LOG-RECORD」、「日志_记录」。
    做 添加映射 用 「CLIP-BY-NORM」、「按范数裁剪」。
    做 添加映射 用 「CLIP-BY-VALUE」、「按值裁剪」。
    做 添加映射 用 「CLIP-GLOBAL」、「全局范数裁剪」。
    做 添加映射 用 「GRAD-ACCUM」、「累积_加」。
    做 添加映射 用 「GRAD-SCALE-UP」、「缩放_放大」。
    做 添加映射 用 「GRAD-SCALE-DOWN」、「缩放_缩小」。
    做 添加映射 用 「LAYER-NORM」、「层归一化计算」。
    做 添加映射 用 「RMS-NORM」、「均方根归一化」。
    做 添加映射 用 「BATCH-NORM」、「批归一化计算」。
    做 添加映射 用 「GROUP-NORM」、「组归一化」。
    做 添加映射 用 「MAX-POOL-1D」、「最大池化1D」。
    做 添加映射 用 「AVG-POOL-1D」、「均值池化1D」。
    做 添加映射 用 「GLOBAL-MAX-POOL」、「全局最大池化」。
    做 添加映射 用 「GLOBAL-AVG-POOL」、「全局均值池化」。
    做 添加映射 用 「ADAPTIVE-POOL」、「自适应均值池化2」。
    做 添加映射 用 「K-MAX-POOL」、「K最大池化」。
    做 添加映射 用 「MAX-POOL-2D」、「最大池化2D」。
    做 添加映射 用 「TO-FP16」、「转半精度」。
    做 添加映射 用 「FP16-OVERFLOW」、「fp16_溢出」。
    做 添加映射 用 「SCALER-INIT」、「缩放器初始化」。
    做 添加映射 用 「SCALER-SCALE」、「缩放器_缩放损失」。
    做 添加映射 用 「SCALER-UNSCALE」、「缩放器_反缩放梯度」。
    做 添加映射 用 「SCALER-UPDATE」、「缩放器_更新」。
    做 添加映射 用 「ENTROPY」、「信息熵」。
    做 添加映射 用 「MC-UNCERTAINTY」、「MC_不确定性」。
    做 添加映射 用 「TEMPERATURE-SCALE」、「温度缩放」。
    做 添加映射 用 「期望校准误差」、「期望校准误差」。
    做 添加映射 用 「ANOMALY-DETECT」、「监控_异常检测」。
    做 添加映射 用 「GREEDY-SELECT」、「策略_贪心」。
    做 添加映射 用 「EPSILON-GREEDY」、「策略_epsilon_贪心」。
    做 添加映射 用 「ZOO-REGISTER」、「zoo_注册」。
    做 添加映射 用 「ZOO-FIND」、「zoo_查找」。
    做 添加映射 用 「ZOO-BEST」、「zoo_最佳」。
    做 添加映射 用 「ZOO-RANK」、「zoo_排名」。
    做 添加映射 用 「AB-TEST」、「AB_初始化」。
    做 添加映射 用 「AB-RECORD」、「AB_记录」。
    做 添加映射 用 「ENSEMBLE-AVG」、「集成_平均」。
    做 添加映射 用 「ENSEMBLE-VOTE」、「集成_投票」。
完。

_forth_是空白 接受 ch：
    若 做 字符串相等 用 ch、「 」：
        返还 真。
    完。
    若 做 字符串相等 用 ch、「\t」：
        返还 真。
    完。
    若 做 字符串相等 用 ch、「\n」：
        返还 真。
    完。
    若 做 字符串相等 用 ch、「\r」：
        返还 真。
    完。
    返还 假。
完。

_forth_是数字串 接受 w：
    n 为 做 计数 用 w。
    若 n 等于 0：
        返还 假。
    完。
    可变 k 为 0。
    若 做 字符串相等 用 做 子串 用 w、0、1、「-」 并且 n 大于 1：
        k 为 1。
    完。
    可变 全数字 为 真。
    只要 k 小于 n：
        码 为 做 编码 用 做 子串 用 w、k、1。
        若 码 小于 48 或者 码 大于 57：
            全数字 为 假。
        完。
        k 为 k 加 1。
    完。
    返还 全数字。
完。

Forth_词法 接受 源码：
    可变 词元列 为 一组：（空）。
    可变 位置 为 0。
    可变 行 为 1。
    长度 为 做 计数 用 源码。
    只要 位置 小于 长度：
        ch 为 做 子串 用 源码、位置、1。
        若 做 字符串相等 用 ch、「\n」：
            行 为 行 加 1。
            位置 为 位置 加 1。
        否则若 做 _forth_是空白 用 ch：
            位置 为 位置 加 1。
        否则若 做 字符串相等 用 ch、「\\」：
            可变 c 为 「」。
            起始行 为 行。
            位置 为 位置 加 1。
            可变 完毕 为 假。
            只要 位置 小于 长度 并且 完毕 等于 假：
                nc 为 做 子串 用 源码、位置、1。
                若 做 字符串相等 用 nc、「\n」：
                    完毕 为 真。
                    行 为 行 加 1。
                否则：
                    c 为 c 加 nc。
                完。
                位置 为 位置 加 1。
            完。
            做 追加 用 词元列、一组：3、c、起始行。
        否则若 做 字符串相等 用 ch、「(」：
            可变 c 为 「」。
            起始行 为 行。
            位置 为 位置 加 1。
            可变 完毕 为 假。
            只要 位置 小于 长度 并且 完毕 等于 假：
                nc 为 做 子串 用 源码、位置、1。
                若 做 字符串相等 用 nc、「)」：
                    完毕 为 真。
                否则：
                    若 做 字符串相等 用 nc、「\n」：
                        行 为 行 加 1。
                    完。
                    c 为 c 加 nc。
                完。
                位置 为 位置 加 1。
            完。
            做 追加 用 词元列、一组：3、c、起始行。
        否则若 做 字符串相等 用 ch、「\"」：
            可变 s 为 「」。
            起始行 为 行。
            位置 为 位置 加 1。
            可变 完毕 为 假。
            只要 位置 小于 长度 并且 完毕 等于 假：
                nc 为 做 子串 用 源码、位置、1。
                若 做 字符串相等 用 nc、「\"」：
                    完毕 为 真。
                否则：
                    s 为 s 加 nc。
                完。
                位置 为 位置 加 1。
            完。
            做 追加 用 词元列、一组：2、s、起始行。
        否则：
            可变 w 为 「」。
            只要 位置 小于 长度 并且 做 _forth_是空白 用 做 子串 用 源码、位置、1 等于 假：
                w 为 w 加 做 子串 用 源码、位置、1。
                位置 为 位置 加 1。
            完。
            若 做 计数 用 w 大于 0：
                若 做 _forth_是数字串 用 w：
                    做 追加 用 词元列、一组：1、w、行。
                否则：
                    做 追加 用 词元列、一组：0、w、行。
                完。
            完。
        完。
    完。
    返还 词元列。
完。

Fort_转换_token 接受 t：
    若 t 的 第 0 项 等于 3：
        返还 「// 」 加 t 的 第 1 项。
    完。
    若 t 的 第 0 项 等于 2：
        返还 「打印(\"」 加 t 的 第 1 项 加 「\")」。
    完。
    若 t 的 第 0 项 等于 1：
        返还 t 的 第 1 项。
    完。
    若 t 的 第 0 项 等于 0：
        m 为 做 查找映射 用 t 的 第 1 项。
        若 m 不等于 空：
            返还 m。
        完。
    完。
    返还 t 的 第 1 项。
完。

Fort_Forth转Nova 接受 源码：
    词元列 为 做 Forth_词法 用 源码。
    可变 结果 为 「」。
    可变 当前行 为 1。
    可变 行内容 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词元列：
        t 为 词元列 的 第 i 项。
        若 t 的 第 2 项 不等于 当前行：
            若 做 计数 用 行内容 不等于 0：
                结果 为 结果 加 行内容 加 「\n」。
            完。
            行内容 为 「」。
            当前行 为 t 的 第 2 项。
        完。
        若 t 的 第 0 项 等于 0 并且 做 字符串相等 用 t 的 第 1 项、「:」：
            若 i 加 1 小于 做 计数 用 词元列：
                行内容 为 行内容 加 「法 」 加 词元列 的 第 i 加 1 项 的 第 1 项 加 「() {」。
                i 为 i 加 2。
            否则：
                i 为 i 加 1。
            完。
        否则若 t 的 第 0 项 等于 0 并且 做 字符串相等 用 t 的 第 1 项、「;」：
            行内容 为 行内容 加 「}」。
            i 为 i 加 1。
        否则若 t 的 第 0 项 等于 0 并且 做 字符串相等 用 t 的 第 1 项、「CONSTANT」 并且 i 加 1 小于 做 计数 用 词元列：
            行内容 为 行内容 加 「令 」 加 词元列 的 第 i 加 1 项 的 第 1 项。
            i 为 i 加 2。
        否则若 t 的 第 0 项 等于 0 并且 做 字符串相等 用 t 的 第 1 项、「VARIABLE」 并且 i 加 1 小于 做 计数 用 词元列：
            行内容 为 行内容 加 「变 」 加 词元列 的 第 i 加 1 项 的 第 1 项 加 「 = 0」。
            i 为 i 加 2。
        否则若 t 的 第 0 项 等于 0 并且 做 字符串相等 用 t 的 第 1 项、「CREATE」 并且 i 加 1 小于 做 计数 用 词元列：
            行内容 为 行内容 加 「变 」 加 词元列 的 第 i 加 1 项 的 第 1 项 加 「 = []」。
            i 为 i 加 2。
        否则若 t 的 第 0 项 等于 0 并且 做 字符串相等 用 t 的 第 1 项、「INCLUDE」 并且 i 加 1 小于 做 计数 用 词元列：
            行内容 为 行内容 加 「// 包含 」 加 词元列 的 第 i 加 1 项 的 第 1 项。
            i 为 i 加 2。
        否则：
            转换后 为 做 转换_token 用 t。
            若 做 计数 用 行内容 不等于 0：
                行内容 为 行内容 加 「 」 加 转换后。
            否则：
                行内容 为 转换后。
            完。
            i 为 i 加 1。
        完。
    完。
    若 做 计数 用 行内容 不等于 0：
        结果 为 结果 加 行内容。
    完。
    返还 结果。
完。

本章 收束。
