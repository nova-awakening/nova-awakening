《提示工程》

引用《..·基础》。

提示_系统 为 0。

提示_用户 为 1。

提示_助手 为 2。

提示_工具 为 3。

模板_创建 接受 模板哈希、变量列表：
    可变 默认值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 变量列表：
        做 追加 用 默认值、0。
        i 为 i 加 1。
    完。
    返还 一组：模板哈希、变量列表、默认值、提示_用户。
完。

模板_设默认值 接受 模板、变量idx、值：
    模板 的 第 2 项 的 第 变量idx 项 为 值。
完。

模板_设类型 接受 模板、类型：
    模板 的 第 3 项 为 类型。
完。

少样本_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、0。
完。

少样本_追加 接受 示例集、输入、输出：
    做 追加 用 示例集 的 第 0 项、输入。
    做 追加 用 示例集 的 第 1 项、输出。
    示例集 的 第 2 项 为 示例集 的 第 2 项 加 1。
完。

少样本_数量 接受 示例集：
    返还 示例集 的 第 2 项。
完。

少样本_选择 接受 示例集、查询嵌入、示例嵌入列表、k：
    n 为 示例集 的 第 2 项。
    可变 分数 为 一组：（空）。
    可变 索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 做 计数 用 示例嵌入列表：
            可变 相似度 为 0。
            可变 j 为 0。
            只要 j 小于 做 计数 用 查询嵌入：
                若 j 小于 做 计数 用 示例嵌入列表 的 第 i 项：
                    相似度 为 相似度 加 查询嵌入 的 第 j 项 乘 示例嵌入列表 的 第 i 项 的 第 j 项 除以 1000。
                完。
                j 为 j 加 1。
            完。
            做 追加 用 分数、相似度。
        否则：
            做 追加 用 分数、0。
        完。
        做 追加 用 索引、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 k 并且 i 小于 n：
        可变 最大idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 分数 的 第 索引 的 第 j 项 项 大于 分数 的 第 索引 的 第 最大idx 项 项：
                最大idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最大idx 不等于 i：
            临时 为 索引 的 第 i 项。
            索引 的 第 i 项 为 索引 的 第 最大idx 项。
            索引 的 第 最大idx 项 为 临时。
        完。
        i 为 i 加 1。
    完。
    可变 选中 为 一组：（空）。
    i 为 0。
    只要 i 小于 k 并且 i 小于 n：
        做 追加 用 选中、索引 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 选中。
完。

消息_创建 接受 角色、内容：
    返还 一组：角色、内容。
完。

消息_系统 接受 内容：
    返还 做 消息_创建 用 提示_系统、内容。
完。

消息_用户 接受 内容：
    返还 做 消息_创建 用 提示_用户、内容。
完。

消息_助手 接受 内容：
    返还 做 消息_创建 用 提示_助手、内容。
完。

消息_工具 接受 内容、调用ID：
    返还 一组：提示_工具、内容、0、调用ID。
完。

对话_创建 接受 系统提示：
    可变 历史 为 一组：（空）。
    若 系统提示 不等于 0：
        做 追加 用 历史、做 消息_系统 用 系统提示。
    完。
    返还 一组：历史。
完。

提示_对话_追加 接受 对话、消息：
    做 追加 用 对话 的 第 0 项、消息。
完。

对话_长度 接受 对话：
    返还 做 计数 用 对话 的 第 0 项。
完。

对话_截断 接受 对话、保留最近N条：
    总 为 做 计数 用 对话 的 第 0 项。
    可变 新历史 为 一组：（空）。
    若 总 小于或等于 保留最近N条 加 1：
        返还。
    完。
    若 对话 的 第 0 项 的 第 0 项 的 第 0 项 等于 提示_系统：
        做 追加 用 新历史、对话 的 第 0 项 的 第 0 项。
    完。
    起始 为 总 减 保留最近N条。
    可变 i 为 起始。
    只要 i 小于 总：
        做 追加 用 新历史、对话 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    对话 的 第 0 项 为 新历史。
完。

工具定义_创建 接受 名称、描述：
    返还 一组：名称、描述、一组：（空）。
完。

工具定义_添加参数 接受 工具、参数名、参数类型、是否必需：
    做 追加 用 工具 的 第 2 项、一组：参数名、参数类型、是否必需。
完。

工具调用_创建 接受 工具名、参数列表：
    返还 一组：工具名、参数列表、0。
完。

工具调用_设结果 接受 调用、结果：
    调用 的 第 2 项 为 结果。
完。

解析_提取整数 接受 _输出哈希、默认值：
    返还 默认值。
完。

解析_是否包含 接受 输出列表、目标：
    可变 i 为 0。
    只要 i 小于 做 计数 用 输出列表：
        若 输出列表 的 第 i 项 等于 目标：
            返还 1。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

重试_创建 接受 最大次数、退避因子：
    返还 一组：最大次数、退避因子。
完。

重试_应重试 接受 策略：
    返还 策略 的 第 2 项 小于 策略 的 第 0 项。
完。

重试_下次延迟 接受 策略：
    可变 延迟 为 1000。
    可变 i 为 0。
    只要 i 小于 策略 的 第 2 项：
        延迟 为 延迟 乘 策略 的 第 1 项 除以 1000。
        i 为 i 加 1。
    完。
    策略 的 第 2 项 为 策略 的 第 2 项 加 1。
    策略 的 第 3 项 为 延迟。
    返还 延迟。
完。

重试_重置 接受 策略：
    策略 的 第 2 项 为 0。
    策略 的 第 3 项 为 0。
完。

提示链_创建 接受 零参：
    返还 一组：一组：（空）。
完。

提示链_追加 接受 链、模板：
    做 追加 用 链 的 第 0 项、模板。
完。

提示链_长度 接受 链：
    返还 做 计数 用 链 的 第 0 项。
完。

RAG提示_创建 接受 查询、上下文列表、最大上下文数：
    可变 选中 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大上下文数 并且 i 小于 做 计数 用 上下文列表：
        做 追加 用 选中、上下文列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 一组：查询、选中、0。
完。

RAG提示_上下文数 接受 提示：
    返还 做 计数 用 提示 的 第 1 项。
完。

提示优化_评估 接受 提示变体列表、评分列表：
    n 为 做 计数 用 提示变体列表。
    若 n 等于 0：
        返还 负 1。
    完。
    可变 最佳 为 0。
    可变 i 为 1。
    只要 i 小于 n：
        若 i 小于 做 计数 用 评分列表 并且 评分列表 的 第 i 项 大于 评分列表 的 第 最佳 项：
            最佳 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最佳。
完。

角色_创建 接受 名称哈希、描述哈希、能力列表：
    返还 一组：名称哈希、描述哈希、能力列表、一组：（空）。
完。

角色_添加约束 接受 角色、约束哈希：
    做 追加 用 角色 的 第 3 项、约束哈希。
完。

可变 黑名单关键词 为 一组：（空）。

安全_添加黑名单 接受 关键词哈希：
    做 追加 用 黑名单关键词、关键词哈希。
完。

安全_检查 接受 内容哈希列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 内容哈希列表：
        可变 j 为 0。
        只要 j 小于 做 计数 用 黑名单关键词：
            若 内容哈希列表 的 第 i 项 等于 黑名单关键词 的 第 j 项：
                返还 0。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

估计_token数 接受 字符数：
    返还 字符数 除以 3。
完。

是否超限 接受 token数、模型上限：
    返还 token数 大于 模型上限。
完。

本章 收束。
