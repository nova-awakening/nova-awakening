《模型压缩》

引用《..·基础》。

引用《..·算法·数学扩展》。

幅度剪枝 接受 权重、稀疏率：
    n 为 做 计数 用 权重。
    剪除数 为 n 乘 稀疏率 除以 1000。
    可变 绝对值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 v 为 权重 的 第 i 项。
        若 v 小于 0：
            v 为 0 减 v。
        完。
        做 追加 用 绝对值、v。
        i 为 i 加 1。
    完。
    可变 排序 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 排序、绝对值 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 排序 的 第 j 项 小于 排序 的 第 i 项：
                临时 为 排序 的 第 i 项。
                排序 的 第 i 项 为 排序 的 第 j 项。
                排序 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 阈值 为 0。
    若 剪除数 大于 0 并且 剪除数 小于 n：
        阈值 为 排序 的 第 剪除数 项。
    完。
    可变 掩码 为 一组：（空）。
    可变 剪后权重 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        若 绝对值 的 第 i 项 小于或等于 阈值：
            做 追加 用 掩码、0。
            做 追加 用 剪后权重、0。
        否则：
            做 追加 用 掩码、1。
            做 追加 用 剪后权重、权重 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：剪后权重、掩码。
完。

滤波器重要性_L1 接受 滤波器列表：
    n 为 做 计数 用 滤波器列表。
    可变 重要性 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 l1 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 滤波器列表 的 第 i 项：
            可变 v 为 滤波器列表 的 第 i 项 的 第 j 项。
            若 v 小于 0：
                v 为 0 减 v。
            完。
            l1 为 l1 加 v。
            j 为 j 加 1。
        完。
        做 追加 用 重要性、l1。
        i 为 i 加 1。
    完。
    返还 重要性。
完。

结构化剪枝 接受 滤波器列表、剪除率：
    重要性 为 做 滤波器重要性_L1 用 滤波器列表。
    n 为 做 计数 用 滤波器列表。
    剪除数 为 n 乘 剪除率 除以 1000。
    可变 索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 索引、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 重要性 的 第 索引 的 第 j 项 项 小于 重要性 的 第 索引 的 第 i 项 项：
                临时 为 索引 的 第 i 项。
                索引 的 第 i 项 为 索引 的 第 j 项。
                索引 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 保留 为 一组：（空）。
    i 为 剪除数。
    只要 i 小于 n：
        做 追加 用 保留、索引 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 保留。
完。

渐进剪枝_稀疏率 接受 当前步、总步数、初始稀疏率、最终稀疏率：
    进度 为 当前步 乘 1000 除以 总步数。
    剩余 为 1000 减 进度。
    立方 为 剩余 乘 剩余 除以 1000 乘 剩余 除以 1000。
    返还 最终稀疏率 加 (初始稀疏率 减 最终稀疏率) 乘 立方 除以 1000。
完。

蒸馏_温度softmax 接受 对数值、T：
    可变 指数组 为 一组：（空）。
    若 T 小于或等于 0：
        T 为 1000。
    完。
    n 为 做 计数 用 对数值。
    可变 调整 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 调整、对数值 的 第 i 项 乘 1000 除以 T。
        i 为 i 加 1。
    完。
    可变 max_v 为 调整 的 第 0 项。
    i 为 1。
    只要 i 小于 n：
        若 调整 的 第 i 项 大于 max_v：
            max_v 为 调整 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 总和 为 0。
    i 为 0。
    只要 i 小于 n：
        可变 e 为 1000 加 (调整 的 第 i 项 减 max_v)。
        若 e 小于 1：
            e 为 1。
        完。
        做 追加 用 指数组、e。
        总和 为 总和 加 e。
        i 为 i 加 1。
    完。
    若 总和 等于 0：
        总和 为 1。
    完。
    可变 概率 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 概率、指数组 的 第 i 项 乘 1000 除以 总和。
        i 为 i 加 1。
    完。
    返还 概率。
完。

蒸馏_KL损失 接受 教师概率、学生概率：
    n 为 做 计数 用 教师概率。
    可变 kl 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 教师概率 的 第 i 项 大于 0 并且 学生概率 的 第 i 项 大于 0：
            kl 为 kl 加 教师概率 的 第 i 项 乘 (教师概率 的 第 i 项 减 学生概率 的 第 i 项) 除以 (学生概率 的 第 i 项 加 1)。
        完。
        i 为 i 加 1。
    完。
    返还 kl 除以 1000。
完。

压缩_蒸馏_损失 接受 教师logits、学生logits、标签、T、α：
    若 T 小于或等于 0：
        T 为 4000。
    完。
    若 α 小于或等于 0：
        α 为 500。
    完。
    教师概率 为 做 蒸馏_温度softmax 用 教师logits、T。
    学生概率 为 做 蒸馏_温度softmax 用 学生logits、T。
    KL 为 做 蒸馏_KL损失 用 教师概率、学生概率。
    学生硬概率 为 做 蒸馏_温度softmax 用 学生logits、1000。
    可变 CE 为 0。
    若 标签 大于或等于 0 并且 标签 小于 做 计数 用 学生硬概率：
        若 学生硬概率 的 第 标签 项 大于 0：
            CE 为 1000 减 学生硬概率 的 第 标签 项。
        完。
    完。
    T_sq 为 T 乘 T 除以 (1000 乘 1000)。
    返还 (1000 减 α) 乘 T_sq 乘 KL 除以 1000 加 α 乘 CE 除以 1000。
完。

特征蒸馏_MSE 接受 教师特征、学生特征：
    n 为 做 计数 用 教师特征。
    可变 损失值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 s 为 0。
        若 i 小于 做 计数 用 学生特征：
            s 为 学生特征 的 第 i 项。
        完。
        可变 d 为 教师特征 的 第 i 项 减 s。
        损失值 为 损失值 加 d 乘 d 除以 1000。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 损失值 除以 n。
完。

注意力蒸馏 接受 教师注意力、学生注意力：
    n 为 做 计数 用 教师注意力。
    可变 损失值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 s 为 0。
        若 i 小于 做 计数 用 学生注意力：
            s 为 学生注意力 的 第 i 项。
        完。
        可变 d 为 教师注意力 的 第 i 项 减 s。
        损失值 为 损失值 加 d 乘 d 除以 1000。
        i 为 i 加 1。
    完。
    返还 损失值 除以 n。
完。

低秩_幂迭代 接受 矩阵、行数、列数、迭代次数：
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 列数：
        做 追加 用 v、1000 除以 列数。
        i 为 i 加 1。
    完。
    可变 迭代 为 0。
    只要 迭代 小于 迭代次数：
        可变 u 为 一组：（空）。
        i 为 0。
        只要 i 小于 行数：
            可变 数值 为 0。
            可变 j 为 0。
            只要 j 小于 列数：
                数值 为 数值 加 矩阵 的 第 i 乘 列数 加 j 项 乘 v 的 第 j 项 除以 1000。
                j 为 j 加 1。
            完。
            做 追加 用 u、数值。
            i 为 i 加 1。
        完。
        可变 范数u 为 0。
        i 为 0。
        只要 i 小于 行数：
            范数u 为 范数u 加 u 的 第 i 项 乘 u 的 第 i 项 除以 1000。
            i 为 i 加 1。
        完。
        范数u 为 做 整数sqrt 用 范数u 乘 1000。
        若 范数u 等于 0：
            范数u 为 1。
        完。
        i 为 0。
        只要 i 小于 行数：
            u 的 第 i 项 为 u 的 第 i 项 乘 1000 除以 范数u。
            i 为 i 加 1。
        完。
        可变 新v 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 列数：
            可变 数值 为 0。
            i 为 0。
            只要 i 小于 行数：
                数值 为 数值 加 矩阵 的 第 i 乘 列数 加 j 项 乘 u 的 第 i 项 除以 1000。
                i 为 i 加 1。
            完。
            做 追加 用 新v、数值。
            j 为 j 加 1。
        完。
        可变 范数v 为 0。
        j 为 0。
        只要 j 小于 列数：
            范数v 为 范数v 加 新v 的 第 j 项 乘 新v 的 第 j 项 除以 1000。
            j 为 j 加 1。
        完。
        范数v 为 做 整数sqrt 用 范数v 乘 1000。
        若 范数v 等于 0：
            范数v 为 1。
        完。
        j 为 0。
        只要 j 小于 列数：
            新v 的 第 j 项 为 新v 的 第 j 项 乘 1000 除以 范数v。
            j 为 j 加 1。
        完。
        v 为 新v。
        迭代 为 迭代 加 1。
    完。
    返还 v。
完。

权重聚类 接受 权重、k：
    n 为 做 计数 用 权重。
    若 k 小于或等于 0：
        k 为 16。
    完。
    可变 中心 为 一组：（空）。
    可变 min_w 为 权重 的 第 0 项。
    可变 max_w 为 权重 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 权重 的 第 i 项 小于 min_w：
            min_w 为 权重 的 第 i 项。
        完。
        若 权重 的 第 i 项 大于 max_w：
            max_w 为 权重 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 k：
        做 追加 用 中心、min_w 加 (max_w 减 min_w) 乘 i 除以 (k 减 1)。
        i 为 i 加 1。
    完。
    可变 标签 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 标签、0。
        i 为 i 加 1。
    完。
    可变 迭代 为 0。
    只要 迭代 小于 10：
        i 为 0。
        只要 i 小于 n：
            可变 最近 为 0。
            可变 最近距 为 0x7FFFFFFF。
            可变 c 为 0。
            只要 c 小于 k：
                可变 d 为 权重 的 第 i 项 减 中心 的 第 c 项。
                若 d 小于 0：
                    d 为 0 减 d。
                完。
                若 d 小于 最近距：
                    最近 为 c。
                    最近距 为 d。
                完。
                c 为 c 加 1。
            完。
            标签 的 第 i 项 为 最近。
            i 为 i 加 1。
        完。
        可变 c 为 0。
        只要 c 小于 k：
            可变 和 为 0。
            可变 数量 为 0。
            i 为 0。
            只要 i 小于 n：
                若 标签 的 第 i 项 等于 c：
                    和 为 和 加 权重 的 第 i 项。
                    数量 为 数量 加 1。
                完。
                i 为 i 加 1。
            完。
            若 数量 大于 0：
                中心 的 第 c 项 为 和 除以 数量。
            完。
            c 为 c 加 1。
        完。
        迭代 为 迭代 加 1。
    完。
    返还 一组：中心、标签。
完。

融合_BN到Conv 接受 conv_权重、bn_γ、bn_β、bn_均值、bn_方差、维度：
    可变 融合权重 为 一组：（空）。
    可变 融合偏置 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        inv_std 为 1000 乘 1000 除以 (做 整数sqrt 用 bn_方差 的 第 i 项 乘 1000 加 1)。
        缩放值 为 bn_γ 的 第 i 项 乘 inv_std 除以 1000。
        偏置 为 bn_β 的 第 i 项 减 bn_γ 的 第 i 项 乘 bn_均值 的 第 i 项 乘 inv_std 除以 (1000 乘 1000)。
        做 追加 用 融合权重、conv_权重 的 第 i 项 乘 缩放值 除以 1000。
        做 追加 用 融合偏置、偏置。
        i 为 i 加 1。
    完。
    返还 一组：融合权重、融合偏置。
完。

早退_判断 接受 中间输出、阈值：
    n 为 做 计数 用 中间输出。
    若 n 等于 0：
        返还 0。
    完。
    可变 最大 为 中间输出 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 中间输出 的 第 i 项 大于 最大：
            最大 为 中间输出 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大 大于或等于 阈值。
完。

压缩_稀疏率 接受 掩码：
    n 为 做 计数 用 掩码。
    若 n 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        若 掩码 的 第 i 项 等于 0：
            零数 为 零数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 零数 乘 1000 除以 n。
完。

压缩_参数减少率 接受 原始参数数、压缩参数数：
    若 原始参数数 等于 0：
        返还 0。
    完。
    返还 (原始参数数 减 压缩参数数) 乘 1000 除以 原始参数数。
完。

蒸馏_软标签 接受 教师输出、温度：
    若 温度 等于 0：
        温度 为 1。
    完。
    可变 结果 为 一组：（空）。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 教师输出：
        缩放 为 教师输出 的 第 i 项 乘 1000 除以 温度。
        做 追加 用 结果、缩放。
        总和 为 总和 加 缩放。
        i 为 i 加 1。
    完。
    若 总和 大于 0：
        i 为 0。
        只要 i 小于 做 计数 用 结果：
            结果 的 第 i 项 为 结果 的 第 i 项 乘 1000 除以 总和。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

压缩_蒸馏_损失2 接受 学生输出、教师软标签、真实标签、温度、学习率：
    可变 蒸馏损失 为 0。
    可变 监督损失 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 学生输出：
        学生软 为 学生输出 的 第 i 项 乘 1000 除以 温度。
        蒸馏损失 为 蒸馏损失 加 (教师软标签 的 第 i 项 减 学生软) 乘 (教师软标签 的 第 i 项 减 学生软) 除以 1000。
        监督损失 为 监督损失 加 (真实标签 的 第 i 项 减 学生输出 的 第 i 项) 乘 (真实标签 的 第 i 项 减 学生输出 的 第 i 项) 除以 1000。
        i 为 i 加 1。
    完。
    返还 学习率 乘 蒸馏损失 除以 1000 加 (1000 减 学习率) 乘 监督损失 除以 1000。
完。

量化_线性 接受 值、最小值、最大值、量化级：
    若 最大值 等于 最小值：
        返还 0。
    完。
    归一化 为 (值 减 最小值) 乘 量化级 除以 (最大值 减 最小值)。
    若 归一化 小于 0：
        返还 0。
    完。
    若 归一化 大于 量化级：
        返还 量化级。
    完。
    返还 归一化。
完。

量化_反量化 接受 量化值、最小值、最大值、量化级：
    若 量化级 等于 0：
        返还 最小值。
    完。
    返还 最小值 加 量化值 乘 (最大值 减 最小值) 除以 量化级。
完。

量化_误差 接受 原始、_量化、反量化：
    差 为 原始 减 反量化。
    返还 差 乘 差。
完。

可变 _压缩_剪枝次数 为 0。

可变 _压缩_量化次数 为 0。

可变 _压缩_蒸馏次数 为 0。

可变 _压缩_总压缩率 为 0。

压缩_记录剪枝 接受 压缩率：
    _压缩_剪枝次数 为 _压缩_剪枝次数 加 1。
    _压缩_总压缩率 为 _压缩_总压缩率 加 压缩率。
完。

压缩_记录量化 接受 零参：
    _压缩_量化次数 为 _压缩_量化次数 加 1。
完。

压缩_记录蒸馏 接受 零参：
    _压缩_蒸馏次数 为 _压缩_蒸馏次数 加 1。
完。

压缩_平均压缩率 接受 零参：
    若 _压缩_剪枝次数 等于 0：
        返还 0。
    完。
    返还 _压缩_总压缩率 除以 _压缩_剪枝次数。
完。

压缩_推荐策略 接受 模型大小、精度要求：
    若 精度要求 大于 95：
        返还 「知识蒸馏(保精度)」。
    完。
    若 模型大小 大于 1000000：
        返还 「剪枝+量化(大幅缩小)」。
    完。
    若 模型大小 大于 100000：
        返还 「8位量化」。
    完。
    返还 「无需压缩」。
完。

压缩_重置 接受 零参：
    _压缩_剪枝次数 为 0。
    _压缩_量化次数 为 0。
    _压缩_蒸馏次数 为 0。
    _压缩_总压缩率 为 0。
完。

压缩_速度提升估算 接受 压缩率：
    若 压缩率 大于 900：
        返还 「~10x加速」。
    完。
    若 压缩率 大于 750：
        返还 「~4x加速」。
    完。
    若 压缩率 大于 500：
        返还 「~2x加速」。
    完。
    返还 「加速不明显」。
完。

压缩_精度影响 接受 压缩率、方法：
    若 方法 等于 0：
        若 压缩率 大于 800：
            返还 「精度可能显著下降」。
        完。
        若 压缩率 大于 500：
            返还 「精度轻微下降」。
        完。
        返还 「精度基本保持」。
    完。
    若 方法 等于 1：
        若 压缩率 大于 750：
            返还 「精度可能下降1-3%」。
        完。
        返还 「精度影响可忽略」。
    完。
    返还 「需要评估」。
完。

压缩_存储节省 接受 原始大小、压缩率：
    返还 原始大小 乘 压缩率 除以 1000。
完。

本章 收束。
