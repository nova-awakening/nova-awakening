《直接偏好优化》

引用《..·基础》。

直接偏好_S形 接受 x：
    可变 abs_x 为 x。
    若 abs_x 小于 0：
        abs_x 为 0 减 abs_x。
    完。
    返还 x 乘 500 除以 (abs_x 加 1000) 加 500。
完。

直接偏好_损失 接受 π_赢_logp、π_输_logp、ref_赢_logp、ref_输_logp、β：
    若 β 小于或等于 0：
        β 为 100。
    完。
    log_ratio_w 为 π_赢_logp 减 ref_赢_logp。
    log_ratio_l 为 π_输_logp 减 ref_输_logp。
    差值 为 β 乘 (log_ratio_w 减 log_ratio_l) 除以 1000。
    信号值 为 做 直接偏好_S形 用 差值。
    返还 1000 减 信号值。
完。

DPO模块_批量损失 接受 赢_logp列表、输_logp列表、ref赢列表、ref输列表、β：
    n 为 做 计数 用 赢_logp列表。
    若 n 等于 0：
        返还 0。
    完。
    可变 总损失 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总损失 为 总损失 加 做 直接偏好_损失 用 赢_logp列表 的 第 i 项、输_logp列表 的 第 i 项、ref赢列表 的 第 i 项、ref输列表 的 第 i 项、β。
        i 为 i 加 1。
    完。
    返还 总损失 除以 n。
完。

直接偏好_隐式奖励 接受 π_logp、ref_logp、β：
    返还 β 乘 (π_logp 减 ref_logp) 除以 1000。
完。

IPO_损失 接受 π_赢_logp、π_输_logp、ref_赢_logp、ref_输_logp、β：
    若 β 小于或等于 0：
        β 为 100。
    完。
    log_ratio_w 为 π_赢_logp 减 ref_赢_logp。
    log_ratio_l 为 π_输_logp 减 ref_输_logp。
    差值 为 log_ratio_w 减 log_ratio_l 减 500 除以 β。
    返还 差值 乘 差值 除以 1000。
完。

直接偏好模块_知识传输_损失 接受 π_logp、ref_logp、是否好样本、β、λ_d：
    若 β 小于或等于 0：
        β 为 100。
    完。
    若 λ_d 小于或等于 0：
        λ_d 为 1000。
    完。
    对数比 为 π_logp 减 ref_logp。
    可变 损失值 为 0。
    若 是否好样本 等于 1：
        损失值 为 1000 减 做 直接偏好_S形 用 β 乘 对数比 除以 1000。
    否则：
        损失值 为 λ_d 乘 (1000 减 做 直接偏好_S形 用 0 减 β 乘 对数比 除以 1000) 除以 1000。
    完。
    返还 损失值。
完。

直接偏好模块_在线偏好_损失 接受 π_赢_logp、π_输_logp、λ：
    若 λ 小于或等于 0：
        λ 为 1000。
    完。
    nll 为 0 减 π_赢_logp。
    log_odds 为 π_赢_logp 减 π_输_logp。
    偏好损失 为 1000 减 做 直接偏好_S形 用 log_odds。
    返还 nll 加 λ 乘 偏好损失 除以 1000。
完。

直接偏好模块_简化偏好_损失 接受 赢_avg_logp、输_avg_logp、β、γ：
    若 β 小于或等于 0：
        β 为 2000。
    完。
    若 γ 小于或等于 0：
        γ 为 500。
    完。
    差值 为 β 乘 (赢_avg_logp 减 输_avg_logp) 除以 1000 减 γ。
    返还 1000 减 做 直接偏好_S形 用 差值。
完。

约束偏好_损失 接受 π_赢_logp、π_输_logp、ref_赢_logp、ref_输_logp、β、α：
    若 α 小于或等于 0：
        α 为 500。
    完。
    dpo 为 做 直接偏好_损失 用 π_赢_logp、π_输_logp、ref_赢_logp、ref_输_logp、β。
    nll 为 0 减 π_赢_logp。
    返还 dpo 加 α 乘 nll 除以 1000。
完。

拒绝采样_最佳N 接受 奖励分数列表、N：
    可变 最佳idx 为 0。
    可变 最佳分数 为 奖励分数列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 N 并且 i 小于 做 计数 用 奖励分数列表：
        若 奖励分数列表 的 第 i 项 大于 最佳分数：
            最佳idx 为 i。
            最佳分数 为 奖励分数列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最佳idx。
完。

偏好数据集_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、一组：（空）、0。
完。

偏好数据集_追加 接受 数据集、提示、赢回复、输回复：
    做 追加 用 数据集 的 第 0 项、提示。
    做 追加 用 数据集 的 第 1 项、赢回复。
    做 追加 用 数据集 的 第 2 项、输回复。
    数据集 的 第 3 项 为 数据集 的 第 3 项 加 1。
完。

偏好数据集_大小 接受 数据集：
    返还 数据集 的 第 3 项。
完。

偏好数据集_获取 接受 数据集、i：
    返还 一组：数据集 的 第 0 项 的 第 i 项、数据集 的 第 1 项 的 第 i 项、数据集 的 第 2 项 的 第 i 项。
完。

DPO_KL散度 接受 P_logp、Q_logp：
    n 为 做 计数 用 P_logp。
    若 n 等于 0：
        返还 0。
    完。
    可变 kl 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        差值 为 P_logp 的 第 i 项 减 Q_logp 的 第 i 项。
        kl 为 kl 加 差值。
        i 为 i 加 1。
    完。
    返还 kl 除以 n。
完。

奖励_线性 接受 特征、权重：
    n 为 做 计数 用 特征。
    可变 分数 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 做 计数 用 权重：
            分数 为 分数 加 特征 的 第 i 项 乘 权重 的 第 i 项 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 分数。
完。

成对比较_概率 接受 奖励_赢、奖励_输：
    返还 做 直接偏好_S形 用 奖励_赢 减 奖励_输。
完。

在线直接偏好_排序 接受 回复奖励列表：
    n 为 做 计数 用 回复奖励列表。
    可变 索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 索引、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 回复奖励列表 的 第 索引 的 第 j 项 项 大于 回复奖励列表 的 第 索引 的 第 i 项 项：
                临时 为 索引 的 第 i 项。
                索引 的 第 i 项 为 索引 的 第 j 项。
                索引 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 索引。
完。

多轮直接偏好_损失 接受 轮次_logp_赢、轮次_logp_输、轮次_ref_赢、轮次_ref_输、β：
    轮数 为 做 计数 用 轮次_logp_赢。
    可变 总log_ratio_w 为 0。
    可变 总log_ratio_l 为 0。
    可变 t 为 0。
    只要 t 小于 轮数：
        总log_ratio_w 为 总log_ratio_w 加 轮次_logp_赢 的 第 t 项 减 轮次_ref_赢 的 第 t 项。
        总log_ratio_l 为 总log_ratio_l 加 轮次_logp_输 的 第 t 项 减 轮次_ref_输 的 第 t 项。
        t 为 t 加 1。
    完。
    差值 为 β 乘 (总log_ratio_w 减 总log_ratio_l) 除以 1000。
    返还 1000 减 做 直接偏好_S形 用 差值。
完。

可变 _偏好_提示 为 一组：（空）。

可变 _偏好_赢回复 为 一组：（空）。

可变 _偏好_输回复 为 一组：（空）。

偏好_添加 接受 提示、赢、输：
    做 追加 用 _偏好_提示、提示。
    做 追加 用 _偏好_赢回复、赢。
    做 追加 用 _偏好_输回复、输。
完。

直接偏好_偏好_数量 接受 零参：
    返还 做 计数 用 _偏好_提示。
完。

偏好_取 接受 索引：
    返还 一组：_偏好_提示 的 第 索引 项、_偏好_赢回复 的 第 索引 项、_偏好_输回复 的 第 索引 项。
完。

偏好_随机批 接受 批大小、种子：
    n 为 做 计数 用 _偏好_提示。
    可变 批 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 批大小：
        索引 为 (种子 乘 37 加 i 乘 13) 取余于 n。
        做 追加 用 批、做 偏好_取 用 索引。
        i 为 i 加 1。
    完。
    返还 批。
完。

直接偏好_偏好_清空 接受 零参：
    _偏好_提示 为 一组：（空）。
    _偏好_赢回复 为 一组：（空）。
    _偏好_输回复 为 一组：（空）。
完。

偏好_准确率 接受 奖励赢列表、奖励输列表：
    n 为 做 计数 用 奖励赢列表。
    可变 正确 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 奖励赢列表 的 第 i 项 大于 奖励输列表 的 第 i 项：
            正确 为 正确 加 1。
        完。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 正确 乘 100 除以 n。
完。

直接偏好_排序分区 接受 奖励、索引、低、高：
    基准值 为 奖励 的 第 索引 的 第 高 项 项。
    可变 i 为 低。
    可变 j 为 低。
    只要 j 小于 高：
        若 奖励 的 第 索引 的 第 j 项 项 大于 基准值：
            临 为 索引 的 第 i 项。
            索引 的 第 i 项 为 索引 的 第 j 项。
            索引 的 第 j 项 为 临。
            i 为 i 加 1。
        完。
        j 为 j 加 1。
    完。
    临2 为 索引 的 第 i 项。
    索引 的 第 i 项 为 索引 的 第 高 项。
    索引 的 第 高 项 为 临2。
    返还 i。
完。

直接偏好_快排递归 接受 奖励、索引、低、高：
    若 低 大于或等于 高：
        返还。
    完。
    p 为 做 直接偏好_排序分区 用 奖励、索引、低、高。
    做 直接偏好_快排递归 用 奖励、索引、低、p 减 1。
    做 直接偏好_快排递归 用 奖励、索引、p 加 1、高。
完。

在线直接偏好_排序_快排 接受 回复奖励列表：
    n 为 做 计数 用 回复奖励列表。
    可变 索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 索引、i。
        i 为 i 加 1。
    完。
    若 n 大于 1：
        做 直接偏好_快排递归 用 回复奖励列表、索引、0、n 减 1。
    完。
    返还 索引。
完。

本章 收束。
