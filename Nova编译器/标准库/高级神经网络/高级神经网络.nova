《高级神经网络》

引用《..·函数式·函数式》。

引用《..·基础》。

引用《..·数学·神经网络》。

引用《..·算法·数学扩展》。

引用《..·数学·工具》。

一维卷积 接受 输入、核、步幅：
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 核长 小于或等于 输入长：
        可变 总和 为 0。
        可变 j 为 0。
        只要 j 小于 核长：
            总和 为 总和 加 输入 的 第 i 加 j 项 乘 核 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 输出、总和。
        i 为 i 加 步幅。
    完。
    返还 输出。
完。

激活_SiLU 接受 t：
    若 t 等于 0：
        返还。
    完。
    总 为 t 的 行数 乘 t 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        x 为 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        门控值 为 做 激活_HexSigmoid_单值 用 x。
        做 内存_写32 用 t 的 数据 加 i 乘 4、做 Q乘 用 x、门控值。
        i 为 i 加 1。
    完。
完。

最大池化1D 接受 输入、窗口、步幅：
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 窗口 小于或等于 做 计数 用 输入：
        可变 m 为 输入 的 第 i 项。
        可变 j 为 1。
        只要 j 小于 窗口：
            若 输入 的 第 i 加 j 项 大于 m：
                m 为 输入 的 第 i 加 j 项。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 输出、m。
        i 为 i 加 步幅。
    完。
    返还 输出。
完。

均值池化1D 接受 输入、窗口、步幅：
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 窗口 小于或等于 做 计数 用 输入：
        可变 s 为 0。
        可变 j 为 0。
        只要 j 小于 窗口：
            s 为 s 加 输入 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 输出、s 除以 窗口。
        i 为 i 加 步幅。
    完。
    返还 输出。
完。

全局最大池化层 接受 输入：
    返还 做 列表最大 用 输入。
完。

全局均值池化层 接受 输入：
    若 做 计数 用 输入 等于 0：
        返还 0。
    完。
    返还 做 折叠 用 输入、0、以 s、x 映射到 s 加 x 除以 做 计数 用 输入。
完。

层归一化 接受 输入、衰减率、动量率：
    n 为 做 计数 用 输入。
    均值 为 做 折叠 用 输入、0、以 s、x 映射到 s 加 x 除以 n。
    方差 为 做 折叠 用 输入、0、以 s、x 映射到 s 加 (x 减 均值) 乘 (x 减 均值) 除以 n。
    可变 标准差 为 做 整数sqrt 用 方差。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    返还 做 映射 用 输入、以 v 映射到 (v 减 均值) 乘 100 除以 标准差 乘 衰减率 除以 100 加 动量率。
完。

RNN_单步 接受 x、前隐藏、Wx、Wh、b：
    可变 pre 为 Wx 乘 x 加 Wh 乘 前隐藏 加 b。
    若 pre 大于 100：
        pre 为 100。
    完。
    若 pre 小于 负 100：
        pre 为 负 100。
    完。
    返还 pre。
完。

S形_整数 接受 x：
    若 x 大于 500：
        返还 100。
    完。
    若 x 小于 负 500：
        返还 0。
    完。
    返还 50 加 x 除以 10。
完。

长短记忆_单步 接受 x、前隐藏、c_prev、Wf、Wi、Wo、Wc、bf、bi、bo、bc：
    f 为 做 S形_整数 用 Wf 乘 x 加 Wf 乘 前隐藏 加 bf。
    ig 为 做 S形_整数 用 Wi 乘 x 加 Wi 乘 前隐藏 加 bi。
    o 为 做 S形_整数 用 Wo 乘 x 加 Wo 乘 前隐藏 加 bo。
    可变 c_hat 为 Wc 乘 x 加 Wc 乘 前隐藏 加 bc。
    若 c_hat 大于 100：
        c_hat 为 100。
    完。
    若 c_hat 小于 负 100：
        c_hat 为 负 100。
    完。
    c 为 f 乘 c_prev 除以 100 加 ig 乘 c_hat 除以 100。
    h 为 o 乘 c 除以 100。
    返还 一组：h、c。
完。

高级神经_嵌入_创建 接受 词表大小、维度：
    可变 表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 词表大小 乘 维度：
        做 追加 用 表、(i 乘 7 加 13) 取余于 201 减 100。
        i 为 i 加 1。
    完。
    返还 一组：表、词表大小、维度。
完。

高级神经_嵌入_查找 接受 嵌入表、token_id：
    维度 为 嵌入表 的 第 2 项。
    偏移量 为 token_id 乘 维度。
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 v、嵌入表 的 第 0 项 的 第 偏移量 加 i 项。
        i 为 i 加 1。
    完。
    返还 v。
完。

高级神经_嵌入_序列 接受 嵌入表、token_ids：
    返还 做 映射 用 token_ids、以 tid 映射到 做 嵌入_查找 用 嵌入表、tid。
完。

位置编码_生成 接受 序列长度、维度：
    可变 编码 为 一组：（空）。
    可变 位置 为 0。
    只要 位置 小于 序列长度：
        可变 v 为 一组：（空）。
        可变 d 为 0。
        只要 d 小于 维度：
            角度值 为 (位置 乘 127 加 d 乘 53 加 17) 取余于 201。
            数值 为 角度值 减 100。
            做 追加 用 v、数值。
            d 为 d 加 1。
        完。
        做 追加 用 编码、v。
        位置 为 位置 加 1。
    完。
    返还 编码。
完。

注意力_点积 接受 Q、K、V：
    序列长 为 做 计数 用 Q。
    可变 out_v 为 一组：（空）。
    可变 得分列表 为 一组：（空）。
    可变 指数组 为 一组：（空）。
    维度 为 做 计数 用 Q 的 第 0 项。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列长：
        可变 j 为 0。
        只要 j 小于 序列长：
            可变 点积 为 0。
            可变 d 为 0。
            只要 d 小于 维度：
                点积 为 点积 加 Q 的 第 i 项 的 第 d 项 乘 K 的 第 j 项 的 第 d 项。
                d 为 d 加 1。
            完。
            做 追加 用 得分列表、点积。
            j 为 j 加 1。
        完。
        可变 最大得分 为 得分列表 的 第 0 项。
        j 为 1。
        只要 j 小于 序列长：
            若 得分列表 的 第 j 项 大于 最大得分：
                最大得分 为 得分列表 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        可变 合计 为 0。
        j 为 0。
        只要 j 小于 序列长：
            可变 e 为 1000 加 (得分列表 的 第 j 项 减 最大得分)。
            若 e 小于 1：
                e 为 1。
            完。
            做 追加 用 指数组、e。
            合计 为 合计 加 e。
            j 为 j 加 1。
        完。
        可变 d2 为 0。
        只要 d2 小于 维度：
            可变 weighted 为 0。
            j 为 0。
            只要 j 小于 序列长：
                weighted 为 weighted 加 指数组 的 第 j 项 乘 V 的 第 j 项 的 第 d2 项 除以 合计。
                j 为 j 加 1。
            完。
            做 追加 用 out_v、weighted。
            d2 为 d2 加 1。
        完。
        做 追加 用 输出、out_v。
        i 为 i 加 1。
    完。
    返还 输出。
完。

高级_残差连接 接受 输入、层输出：
    返还 做 拉链 用 输入、层输出、以 a、b 映射到 a 加 b。
完。

随机丢弃向量 接受 v、掩码：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        若 i 小于 做 计数 用 掩码 并且 掩码 的 第 i 项 等于 0：
            做 追加 用 r、0。
        否则：
            做 追加 用 r、v 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

本章 收束。
