《迭代器》

引用《..·基础》。

引用《..·智能安全·AI安全》。

迭代_映射 为 1。

迭代_过滤 为 2。

迭代_取前 为 3。

迭代_跳过 为 4。

迭代_枚举 为 5。

迭代_扁平 为 6。

迭代_窗口 为 7。

迭代_块 为 8。

迭代_扫描 为 9。

迭代器_从列表 接受 列表：
    返还 一组：列表、0、做 计数 用 列表、一组：（空）。
完。

迭代器_范围 接受 起始、结束、步长：
    可变 实际步长 为 步长。
    若 实际步长 小于或等于 0：
        实际步长 为 1。
    完。
    可变 数据 为 一组：（空）。
    可变 i 为 起始。
    只要 i 小于 结束：
        做 追加 用 数据、i。
        i 为 i 加 实际步长。
    完。
    返还 一组：数据、0、做 计数 用 数据、一组：（空）。
完。

迭代器_重复 接受 值、次数：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 次数：
        做 追加 用 数据、值。
        i 为 i 加 1。
    完。
    返还 一组：数据、0、次数、一组：（空）。
完。

迭代器_有下一个 接受 迭代：
    返还 迭代 的 第 1 项 小于 迭代 的 第 2 项。
完。

迭代器_下一个 接受 迭代：
    若 迭代 的 第 1 项 大于或等于 迭代 的 第 2 项：
        返还 空。
    完。
    值 为 迭代 的 第 0 项 的 第 迭代 的 第 1 项 项。
    迭代 的 第 1 项 为 迭代 的 第 1 项 加 1。
    返还 值。
完。

迭代器_重置 接受 迭代：
    迭代 的 第 1 项 为 0。
完。

迭代器_收集 接受 迭代：
    做 迭代器_重置 用 迭代。
    可变 结果 为 一组：（空）。
    只要 做 迭代器_有下一个 用 迭代：
        做 追加 用 结果、做 迭代器_下一个 用 迭代。
    完。
    返还 结果。
完。

映射_应用 接受 值、类型、参数：
    若 类型 等于 0：
        返还 值 加 参数。
    完。
    若 类型 等于 1：
        返还 值 乘 参数 除以 1000。
    完。
    若 类型 等于 2：
        返还 值 乘 值 除以 1000。
    完。
    若 类型 等于 3：
        返还 0 减 值。
    完。
    若 类型 等于 4：
        若 值 小于 0：
            返还 0 减 值。
        完。
        返还 值。
    完。
    若 类型 等于 5：
        返还 值 取余于 参数。
    完。
    返还 值。
完。

迭代器_映射 接受 数据、映射类型、参数：
    n 为 做 计数 用 数据。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、做 映射_应用 用 数据 的 第 i 项、映射类型、参数。
        i 为 i 加 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代_过滤_检查 接受 值、类型、参数：
    若 类型 等于 0：
        返还 值 大于 参数。
    完。
    若 类型 等于 1：
        返还 值 小于 参数。
    完。
    若 类型 等于 2：
        返还 值 等于 参数。
    完。
    若 类型 等于 3：
        返还 值 不等于 参数。
    完。
    若 类型 等于 4：
        返还 值 取余于 2 等于 0。
    完。
    若 类型 等于 5：
        返还 值 取余于 2 不等于 0。
    完。
    若 类型 等于 6：
        返还 值 大于 0。
    完。
    若 类型 等于 7：
        返还 值 不等于 0。
    完。
    返还 1。
完。

迭代器_过滤 接受 数据、过滤类型、参数：
    n 为 做 计数 用 数据。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 迭代_过滤_检查 用 数据 的 第 i 项、过滤类型、参数：
            做 追加 用 结果、数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代器_归约 接受 数据、归约类型、初始值：
    n 为 做 计数 用 数据。
    可变 累积 为 初始值。
    可变 i 为 0。
    只要 i 小于 n：
        若 归约类型 等于 0：
            累积 为 累积 加 数据 的 第 i 项。
        完。
        若 归约类型 等于 1：
            累积 为 累积 乘 数据 的 第 i 项 除以 1000。
        完。
        若 归约类型 等于 2：
            若 数据 的 第 i 项 大于 累积：
                累积 为 数据 的 第 i 项。
            完。
        完。
        若 归约类型 等于 3：
            若 数据 的 第 i 项 小于 累积：
                累积 为 数据 的 第 i 项。
            完。
        完。
        若 归约类型 等于 4：
            累积 为 累积 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 累积。
完。

迭代器_取前 接受 数据、n：
    总 为 做 计数 用 数据。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n 并且 i 小于 总：
        做 追加 用 结果、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代器_跳过 接受 数据、n：
    总 为 做 计数 用 数据。
    可变 结果 为 一组：（空）。
    可变 i 为 n。
    只要 i 小于 总：
        做 追加 用 结果、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代器_取当 接受 数据、过滤类型、参数：
    n 为 做 计数 用 数据。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 迭代_过滤_检查 用 数据 的 第 i 项、过滤类型、参数 等于 0：
            返还 做 迭代器_从列表 用 结果。
        完。
        做 追加 用 结果、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代器_zip 接受 数据1、数据2：
    n1 为 做 计数 用 数据1。
    n2 为 做 计数 用 数据2。
    可变 n 为 n1。
    若 n2 小于 n：
        n 为 n2。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、一组：数据1 的 第 i 项、数据2 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代器_枚举 接受 数据：
    n 为 做 计数 用 数据。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、一组：i、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代器_链接 接受 数据1、数据2：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据1：
        做 追加 用 结果、数据1 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 数据2：
        做 追加 用 结果、数据2 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代器_扁平 接受 嵌套数据：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 嵌套数据：
        内部 为 嵌套数据 的 第 i 项。
        若 做 类型 用 内部 等于 「列表」：
            可变 j 为 0。
            只要 j 小于 做 计数 用 内部：
                做 追加 用 结果、内部 的 第 j 项。
                j 为 j 加 1。
            完。
        否则：
            做 追加 用 结果、内部。
        完。
        i 为 i 加 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代器_分块 接受 数据、块大小：
    n 为 做 计数 用 数据。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 块 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 块大小 并且 i 加 j 小于 n：
            做 追加 用 块、数据 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、块。
        i 为 i 加 块大小。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代器_滑动窗口 接受 数据、窗口大小：
    n 为 做 计数 用 数据。
    可变 窗口 为 一组：（空）。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 窗口大小 小于或等于 n：
        可变 j 为 0。
        只要 j 小于 窗口大小：
            做 追加 用 窗口、数据 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、窗口。
        i 为 i 加 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代器_扫描 接受 数据、归约类型、初始值：
    n 为 做 计数 用 数据。
    可变 结果 为 一组：（空）。
    可变 累积 为 初始值。
    可变 i 为 0。
    只要 i 小于 n：
        若 归约类型 等于 0：
            累积 为 累积 加 数据 的 第 i 项。
        完。
        若 归约类型 等于 1：
            累积 为 累积 乘 数据 的 第 i 项 除以 1000。
        完。
        若 归约类型 等于 2：
            若 数据 的 第 i 项 大于 累积：
                累积 为 数据 的 第 i 项。
            完。
        完。
        做 追加 用 结果、累积。
        i 为 i 加 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代器_求和 接受 数据：
    返还 做 迭代器_归约 用 数据、0、0。
完。

迭代器_最大 接受 数据：
    返还 做 迭代器_归约 用 数据、2、数据 的 第 0 项。
完。

迭代器_最小 接受 数据：
    返还 做 迭代器_归约 用 数据、3、数据 的 第 0 项。
完。

迭代器_计数 接受 数据：
    返还 做 计数 用 数据。
完。

迭代器_平均 接受 数据：
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 0。
    完。
    返还 做 迭代器_求和 用 数据 除以 n。
完。

迭代器_查找 接受 数据、过滤类型、参数：
    n 为 做 计数 用 数据。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 迭代_过滤_检查 用 数据 的 第 i 项、过滤类型、参数：
            返还 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

迭代器_位置 接受 数据、过滤类型、参数：
    n 为 做 计数 用 数据。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 迭代_过滤_检查 用 数据 的 第 i 项、过滤类型、参数：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

迭代器_任一 接受 数据、过滤类型、参数：
    返还 做 迭代器_位置 用 数据、过滤类型、参数 大于或等于 0。
完。

迭代器_所有 接受 数据、过滤类型、参数：
    n 为 做 计数 用 数据。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 迭代_过滤_检查 用 数据 的 第 i 项、过滤类型、参数 等于 0：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

迭代器_去重 接受 数据：
    n 为 做 计数 用 数据。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 存在 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 结果：
            若 结果 的 第 j 项 等于 数据 的 第 i 项：
                存在 为 1。
            完。
            j 为 j 加 1。
        完。
        若 存在 等于 0：
            做 追加 用 结果、数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

迭代器_反转 接受 数据：
    n 为 做 计数 用 数据。
    可变 结果 为 一组：（空）。
    可变 i 为 n 减 1。
    只要 i 大于或等于 0：
        做 追加 用 结果、数据 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 做 迭代器_从列表 用 结果。
完。

本章 收束。
