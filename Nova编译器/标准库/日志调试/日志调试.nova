《日志调试》

引用《..·..·系统扩展·共享辅助》。

引用《..·基础》。

日志_调试 为 0。

日志_信息 为 1。

日志_警告 为 2。

日志_错误 为 3。

日志_致命 为 4。

可变 日志_当前级别 为 1。

可变 日志_记录 为 一组：（空）。

可变 日志_计数器 为 一组：0、0、0、0、0。

日志调试_日志_设级别 接受 级别：
    日志_当前级别 为 级别。
完。

日志调试_日志_写 接受 级别、模块哈希、消息哈希：
    若 级别 大于或等于 日志_当前级别：
        做 追加 用 日志_记录、一组：级别、模块哈希、消息哈希。
    完。
    若 级别 大于或等于 0 并且 级别 小于或等于 4：
        日志_计数器 的 第 级别 项 为 日志_计数器 的 第 级别 项 加 1。
    完。
完。

日志调试_日志_总数 接受 零参：
    返还 做 计数 用 日志_记录。
完。

日志_按级别 接受 级别：
    返还 日志_计数器 的 第 级别 项。
完。

日志调试_日志_清空 接受 零参：
    日志_记录 为 一组：（空）。
    日志_计数器 为 一组：0、0、0、0、0。
完。

可变 计时器列表 为 一组：（空）。

日志调试_计时器_创建 接受 名哈希：
    做 追加 用 计时器列表、一组：名哈希、0、0、0。
    返还 做 计数 用 计时器列表 减 1。
完。

日志调试_计时器_开始 接受 索引、时间：
    计时器列表 的 第 索引 项 的 第 1 项 为 时间。
完。

日志调试_计时器_结束 接受 索引、时间：
    耗时 为 时间 减 计时器列表 的 第 索引 项 的 第 1 项。
    计时器列表 的 第 索引 项 的 第 2 项 为 计时器列表 的 第 索引 项 的 第 2 项 加 耗时。
    计时器列表 的 第 索引 项 的 第 3 项 为 计时器列表 的 第 索引 项 的 第 3 项 加 1。
    返还 耗时。
完。

计时器_平均 接受 索引：
    若 计时器列表 的 第 索引 项 的 第 3 项 等于 0：
        返还 0。
    完。
    返还 计时器列表 的 第 索引 项 的 第 2 项 除以 计时器列表 的 第 索引 项 的 第 3 项。
完。

计时器_累计 接受 索引：
    返还 计时器列表 的 第 索引 项 的 第 2 项。
完。

计时器_次数 接受 索引：
    返还 计时器列表 的 第 索引 项 的 第 3 项。
完。

可变 调用栈 为 一组：（空）。

调用栈_入 接受 函数哈希：
    做 追加 用 调用栈、函数哈希。
完。

调用栈_出 接受 零参：
    若 做 计数 用 调用栈 大于 0：
        可变 新栈 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 调用栈 减 1：
            做 追加 用 新栈、调用栈 的 第 i 项。
            i 为 i 加 1。
        完。
        调用栈 为 新栈。
    完。
完。

调用栈_深度 接受 零参：
    返还 做 计数 用 调用栈。
完。

调用栈_顶 接受 零参：
    若 做 计数 用 调用栈 等于 0：
        返还 负 1。
    完。
    返还 调用栈 的 第 做 计数 用 调用栈 减 1 项。
完。

可变 断言_通过 为 0。

可变 断言_失败 为 0。

日志_断言 接受 条件、_消息哈希：
    若 条件：
        断言_通过 为 断言_通过 加 1。
    否则：
        断言_失败 为 断言_失败 加 1。
    完。
完。

日志_调试级 接受 模块、消息：
    做 日志调试_日志_写 用 日志_调试、模块、消息。
完。

日志_信息级 接受 模块、消息：
    做 日志调试_日志_写 用 日志_信息、模块、消息。
完。

日志_警告级 接受 模块、消息：
    做 日志调试_日志_写 用 日志_警告、模块、消息。
完。

日志_错误级 接受 模块、消息：
    做 日志调试_日志_写 用 日志_错误、模块、消息。
完。

日志_致命级 接受 模块、消息：
    做 日志调试_日志_写 用 日志_致命、模块、消息。
完。

日志_级别名 接受 级别：
    若 级别 等于 0：
        返还 「DEBUG」。
    完。
    若 级别 等于 1：
        返还 「INFO」。
    完。
    若 级别 等于 2：
        返还 「WARN」。
    完。
    若 级别 等于 3：
        返还 「错误」。
    完。
    若 级别 等于 4：
        返还 「FATAL」。
    完。
    返还 「未知」。
完。

日志_打印全部 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 日志_记录：
        r 为 日志_记录 的 第 i 项。
        做 打印 用 「[」 加 做 日志_级别名 用 r 的 第 0 项 加 「] 」 加 做 转字符串 用 r 的 第 1 项 加 「: 」 加 做 转字符串 用 r 的 第 2 项。
        i 为 i 加 1。
    完。
完。

日志_打印级别 接受 级别：
    可变 i 为 0。
    只要 i 小于 做 计数 用 日志_记录：
        若 日志_记录 的 第 i 项 的 第 0 项 等于 级别：
            r 为 日志_记录 的 第 i 项。
            做 打印 用 「[」 加 做 日志_级别名 用 r 的 第 0 项 加 「] 」 加 做 转字符串 用 r 的 第 1 项 加 「: 」 加 做 转字符串 用 r 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
完。

日志_有错误 接受 零参：
    返还 日志_计数器 的 第 3 项 大于 0 或者 日志_计数器 的 第 4 项 大于 0。
完。

日志_有警告 接受 零参：
    返还 日志_计数器 的 第 2 项 大于 0。
完。

日志_摘要 接受 零参：
    做 打印 用 「日志摘要:」。
    做 打印 用 「 DEBUG: 」 加 做 转字符串 用 日志_计数器 的 第 0 项。
    做 打印 用 「 INFO: 」 加 做 转字符串 用 日志_计数器 的 第 1 项。
    做 打印 用 「 WARN: 」 加 做 转字符串 用 日志_计数器 的 第 2 项。
    做 打印 用 「 ERROR: 」 加 做 转字符串 用 日志_计数器 的 第 3 项。
    做 打印 用 「 FATAL: 」 加 做 转字符串 用 日志_计数器 的 第 4 项。
    做 打印 用 「 总计: 」 加 做 转字符串 用 做 计数 用 日志_记录。
完。

可变 训练日志_epoch 为 一组：（空）。

可变 训练日志_loss 为 一组：（空）。

可变 训练日志_lr 为 一组：（空）。

可变 训练日志_acc 为 一组：（空）。

训练日志_记录 接受 轮次、损失值、lr、acc：
    做 追加 用 训练日志_epoch、轮次。
    做 追加 用 训练日志_loss、损失值。
    做 追加 用 训练日志_lr、lr。
    做 追加 用 训练日志_acc、acc。
完。

训练日志_打印 接受 零参：
    做 打印 用 「╔═══ 训练日志 ═══╗」。
    做 打印 用 「║ Epoch Loss LR Acc」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 训练日志_epoch：
        做 打印 用 「║ 」 加 做 转字符串 用 训练日志_epoch 的 第 i 项 加 「 」 加 做 转字符串 用 训练日志_loss 的 第 i 项 加 「 」 加 做 转字符串 用 训练日志_lr 的 第 i 项 加 「 」 加 做 转字符串 用 训练日志_acc 的 第 i 项。
        i 为 i 加 1。
    完。
    做 打印 用 「╚═══════════════╝」。
完。

训练日志_最佳epoch 接受 零参：
    可变 最优索引 为 0。
    可变 best_acc 为 负 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 训练日志_acc：
        若 训练日志_acc 的 第 i 项 大于 best_acc：
            best_acc 为 训练日志_acc 的 第 i 项。
            最优索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最优索引。
完。

训练日志_清空 接受 零参：
    训练日志_epoch 为 一组：（空）。
    训练日志_loss 为 一组：（空）。
    训练日志_lr 为 一组：（空）。
    训练日志_acc 为 一组：（空）。
完。

可变 内存_峰值 为 0。

可变 内存_当前 为 0。

内存_分配 接受 大小：
    内存_当前 为 内存_当前 加 大小。
    若 内存_当前 大于 内存_峰值：
        内存_峰值 为 内存_当前。
    完。
完。

内存_释放 接受 大小：
    内存_当前 为 内存_当前 减 大小。
    若 内存_当前 小于 0：
        内存_当前 为 0。
    完。
完。

日志调试_内存_报告 接受 零参：
    做 打印 用 「内存: 当前=」 加 做 转字符串 用 内存_当前 加 「 峰值=」 加 做 转字符串 用 内存_峰值。
完。

计时器_报告 接受 零参：
    做 打印 用 「性能计时:」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 计时器列表：
        做 打印 用 「 」 加 做 转字符串 用 计时器列表 的 第 i 项 的 第 0 项 加 「: 累计=」 加 做 转字符串 用 计时器列表 的 第 i 项 的 第 2 项 加 「 次数=」 加 做 转字符串 用 计时器列表 的 第 i 项 的 第 3 项 加 「 均=」 加 做 转字符串 用 做 计时器_平均 用 i。
        i 为 i 加 1。
    完。
完。

可变 _结构日志 为 一组：（空）。

日志_结构化 接受 级别、模块、消息、键值对：
    做 追加 用 _结构日志、一组：级别、模块、消息、键值对、做 计数 用 _结构日志。
完。

日志_过滤 接受 级别下限：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _结构日志：
        若 _结构日志 的 第 i 项 的 第 0 项 大于或等于 级别下限：
            做 追加 用 结果、_结构日志 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

日志_按模块过滤 接受 模块名：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _结构日志：
        若 _结构日志 的 第 i 项 的 第 1 项 等于 模块名：
            做 追加 用 结果、_结构日志 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

日志_最近 接受 n：
    可变 起始 为 做 计数 用 _结构日志 减 n。
    若 起始 小于 0：
        起始 为 0。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 起始。
    只要 i 小于 做 计数 用 _结构日志：
        做 追加 用 结果、_结构日志 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

日志_清空 接受 零参：
    _结构日志 为 一组：（空）。
完。

日志_总数 接受 零参：
    返还 做 计数 用 _结构日志。
完。

可变 _断言_通过 为 0。

可变 _断言_失败 为 0。

断言 接受 条件、消息：
    若 条件：
        _断言_通过 为 _断言_通过 加 1。
        返还 真。
    完。
    _断言_失败 为 _断言_失败 加 1。
    做 日志_结构化 用 4、「断言」、消息、一组：（空）。
    返还 假。
完。

断言_等于 接受 实际、期望、消息：
    返还 做 断言 用 实际 等于 期望、消息。
完。

断言_不等 接受 实际、非期望、消息：
    返还 做 断言 用 实际 不等于 非期望、消息。
完。

本章 收束。
