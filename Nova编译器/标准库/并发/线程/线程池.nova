《线程池》

引用《..·..·基础》。

引用《..·..·系统·硬件库》。

用。

「../../系统/硬件库.nova」。

线程_空闲 为 0。

线程_运行 为 1。

线程_阻塞 为 2。

线程_终止 为 3。

可变 __线程池_tick 为 0。

线程池_当前tick 接受 零参：
    t 为 做 时间戳。
    若 t 等于 空 或者 t 小于或等于 0：
        __线程池_tick 为 __线程池_tick 加 1。
        返还 __线程池_tick。
    完。
    返还 t。
完。

工作线程_创建 接受 id：
    返还 一组：id、线程_空闲、一组：（空）、0、空。
完。

工作线程_提交 接受 线程、任务：
    做 追加 用 线程 的 第 2 项、任务。
完。

工作线程_取任务 接受 线程：
    可变 新队列 为 一组：（空）。
    若 做 计数 用 线程 的 第 2 项 等于 0：
        返还 空。
    完。
    任务 为 线程 的 第 2 项 的 第 做 计数 用 线程 的 第 2 项 减 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 线程 的 第 2 项 减 1：
        做 追加 用 新队列、线程 的 第 2 项 的 第 i 项。
        i 为 i 加 1。
    完。
    线程 的 第 2 项 为 新队列。
    返还 任务。
完。

工作线程_窃取 接受 线程：
    可变 新队列 为 一组：（空）。
    若 做 计数 用 线程 的 第 2 项 等于 0：
        返还 空。
    完。
    任务 为 线程 的 第 2 项 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 线程 的 第 2 项：
        做 追加 用 新队列、线程 的 第 2 项 的 第 i 项。
        i 为 i 加 1。
    完。
    线程 的 第 2 项 为 新队列。
    线程 的 第 4 项 为 线程 的 第 4 项 加 1。
    返还 任务。
完。

任务_待执行 为 0。

任务_执行中 为 1。

任务_已完成 为 2。

任务_已失败 为 3。

任务_创建 接受 id、函数体、优先级：
    返还 一组：id、函数体、优先级、任务_待执行、空、一组：（空）、0。
完。

任务_添加依赖 接受 任务、依赖ID：
    做 追加 用 任务 的 第 5 项、依赖ID。
完。

任务_依赖已满足 接受 任务、完成集：
    可变 i 为 0。
    只要 i 小于 做 计数 用 任务 的 第 5 项：
        可变 找到 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 完成集 并且 找到 等于 假：
            若 完成集 的 第 j 项 等于 任务 的 第 5 项 的 第 i 项：
                找到 为 真。
            完。
            j 为 j 加 1。
        完。
        若 找到 等于 假：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

任务_执行 接受 任务：
    任务 的 第 3 项 为 任务_执行中。
    结果 为 做 任务 的 第 1 项。
    若 结果 等于 空：
        任务 的 第 3 项 为 任务_已失败。
        任务 的 第 4 项 为 「任务返回空」。
        返还 一组：「失败」、「任务返回空」。
    完。
    任务 的 第 3 项 为 任务_已完成。
    任务 的 第 4 项 为 结果。
    返还 一组：「成功」、结果。
完。

线程池_创建 接受 线程数：
    可变 实际线程数 为 线程数。
    若 实际线程数 小于或等于 0：
        实际线程数 为 4。
    完。
    可变 线程列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 实际线程数：
        做 追加 用 线程列表、做 工作线程_创建 用 i。
        i 为 i 加 1。
    完。
    返还 一组：线程列表、一组：（空）、一组：（空）、0、0、0、真、假。
完。

线程池_提交 接受 池、函数体、优先级：
    若 池 的 第 9 项：
        返还 负 1。
    完。
    任务ID 为 池 的 第 3 项。
    池 的 第 3 项 为 池 的 第 3 项 加 1。
    池 的 第 5 项 为 池 的 第 5 项 加 1。
    任务 为 做 任务_创建 用 任务ID、函数体、优先级。
    任务 的 第 6 项 为 做 线程池_当前tick。
    线程数 为 做 计数 用 池 的 第 0 项。
    可变 最少 为 0x7FFFFFFF。
    可变 最少idx 为 0。
    可变 i 为 0。
    只要 i 小于 线程数：
        队列长 为 做 计数 用 池 的 第 0 项 的 第 i 项 的 第 2 项。
        若 队列长 小于 最少：
            最少 为 队列长。
            最少idx 为 i。
        完。
        i 为 i 加 1。
    完。
    做 工作线程_提交 用 池 的 第 0 项 的 第 最少idx 项、任务。
    返还 任务ID。
完。

线程池_提交带依赖 接受 池、函数体、优先级、依赖列表：
    若 池 的 第 9 项：
        返还 负 1。
    完。
    任务ID 为 池 的 第 3 项。
    池 的 第 3 项 为 池 的 第 3 项 加 1。
    池 的 第 5 项 为 池 的 第 5 项 加 1。
    任务 为 做 任务_创建 用 任务ID、函数体、优先级。
    任务 的 第 6 项 为 做 线程池_当前tick。
    可变 i 为 0。
    只要 i 小于 做 计数 用 依赖列表：
        做 任务_添加依赖 用 任务、依赖列表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 做 任务_依赖已满足 用 任务、池 的 第 2 项：
        线程数 为 做 计数 用 池 的 第 0 项。
        可变 最少 为 0x7FFFFFFF。
        可变 最少idx 为 0。
        i 为 0。
        只要 i 小于 线程数：
            队列长 为 做 计数 用 池 的 第 0 项 的 第 i 项 的 第 2 项。
            若 队列长 小于 最少：
                最少 为 队列长。
                最少idx 为 i。
            完。
            i 为 i 加 1。
        完。
        做 工作线程_提交 用 池 的 第 0 项 的 第 最少idx 项、任务。
    否则：
        做 追加 用 池 的 第 1 项、任务。
    完。
    返还 任务ID。
完。

线程池_tick 接受 池：
    可变 执行数 为 0。
    若 池 的 第 8 项 等于 假：
        返还 0。
    完。
    线程数 为 做 计数 用 池 的 第 0 项。
    可变 t 为 0。
    只要 t 小于 线程数：
        线程 为 池 的 第 0 项 的 第 t 项。
        任务 为 做 工作线程_取任务 用 线程。
        若 任务 不等于 空：
            线程 的 第 1 项 为 线程_运行。
            池 的 第 4 项 为 池 的 第 4 项 加 1。
            结果 为 做 任务_执行 用 任务。
            线程 的 第 3 项 为 线程 的 第 3 项 加 1。
            线程 的 第 1 项 为 线程_空闲。
            池 的 第 4 项 为 池 的 第 4 项 减 1。
            若 结果 的 第 0 项 等于 「成功」：
                池 的 第 6 项 为 池 的 第 6 项 加 1。
                做 追加 用 池 的 第 2 项、任务 的 第 0 项。
            否则：
                池 的 第 7 项 为 池 的 第 7 项 加 1。
            完。
            执行数 为 执行数 加 1。
        否则：
            可变 窃取成功 为 假。
            可变 v 为 0。
            只要 v 小于 线程数：
                若 v 不等于 t 并且 做 计数 用 池 的 第 0 项 的 第 v 项 的 第 2 项 大于 1：
                    窃取任务 为 做 工作线程_窃取 用 池 的 第 0 项 的 第 v 项。
                    若 窃取任务 不等于 空：
                        做 工作线程_提交 用 线程、窃取任务。
                        窃取成功 为 真。
                        止。
                    完。
                完。
                v 为 v 加 1。
            完。
        完。
        t 为 t 加 1。
    完。
    可变 新全局 为 一组：（空）。
    可变 g 为 0。
    只要 g 小于 做 计数 用 池 的 第 1 项：
        全局任务 为 池 的 第 1 项 的 第 g 项。
        若 做 任务_依赖已满足 用 全局任务、池 的 第 2 项：
            可变 最少 为 0x7FFFFFFF。
            可变 最少idx 为 0。
            t 为 0。
            只要 t 小于 线程数：
                队列长 为 做 计数 用 池 的 第 0 项 的 第 t 项 的 第 2 项。
                若 队列长 小于 最少：
                    最少 为 队列长。
                    最少idx 为 t。
                完。
                t 为 t 加 1。
            完。
            做 工作线程_提交 用 池 的 第 0 项 的 第 最少idx 项、全局任务。
        否则：
            做 追加 用 新全局、全局任务。
        完。
        g 为 g 加 1。
    完。
    池 的 第 1 项 为 新全局。
    返还 执行数。
完。

线程池_运行 接受 池：
    可变 总轮 为 0。
    可变 空闲轮 为 0。
    只要 池 的 第 8 项：
        执行 为 做 线程池_tick 用 池。
        总轮 为 总轮 加 1。
        若 执行 等于 0：
            空闲轮 为 空闲轮 加 1。
            若 空闲轮 大于 100：
                可变 有任务 为 做 计数 用 池 的 第 1 项 大于 0。
                若 有任务 等于 假：
                    可变 t2 为 0。
                    只要 t2 小于 做 计数 用 池 的 第 0 项 并且 有任务 等于 假：
                        若 做 计数 用 池 的 第 0 项 的 第 t2 项 的 第 2 项 大于 0：
                            有任务 为 真。
                        完。
                        t2 为 t2 加 1。
                    完。
                完。
                若 有任务 等于 假：
                    池 的 第 8 项 为 假。
                完。
            完。
        否则：
            空闲轮 为 0。
        完。
        若 池 的 第 9 项 并且 空闲轮 大于 10：
            池 的 第 8 项 为 假。
        完。
    完。
    返还 总轮。
完。

线程池_关闭 接受 池：
    池 的 第 9 项 为 真。
完。

线程池_立即关闭 接受 池：
    池 的 第 8 项 为 假。
    池 的 第 9 项 为 真。
完。

线程池_线程数 接受 池：
    返还 做 计数 用 池 的 第 0 项。
完。

线程池_活跃数 接受 池：
    返还 池 的 第 4 项。
完。

线程池_待处理 接受 池：
    可变 总 为 做 计数 用 池 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 池 的 第 0 项：
        总 为 总 加 做 计数 用 池 的 第 0 项 的 第 i 项 的 第 2 项。
        i 为 i 加 1。
    完。
    返还 总。
完。

线程池_总提交 接受 池：
    返还 池 的 第 5 项。
完。

线程池_总完成 接受 池：
    返还 池 的 第 6 项。
完。

线程池_总失败 接受 池：
    返还 池 的 第 7 项。
完。

线程池_负载 接受 池：
    可变 负载列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 池 的 第 0 项：
        做 追加 用 负载列表、一组：池 的 第 0 项 的 第 i 项 的 第 0 项、做 计数 用 池 的 第 0 项 的 第 i 项 的 第 2 项、池 的 第 0 项 的 第 i 项 的 第 3 项、池 的 第 0 项 的 第 i 项 的 第 4 项。
        i 为 i 加 1。
    完。
    返还 负载列表。
完。

本章 收束。
