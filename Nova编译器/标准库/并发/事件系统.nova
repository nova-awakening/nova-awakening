《事件系统》

引用《..·基础》。

引用《..·..·运行时·原语桩》。

可变 _事件_发布总数 为 0。

可变 _事件_监听总数 为 0。

可变 _事件_取消总数 为 0。

可变 _事件_历史 为 一组：（空）。

事件总线_创建 接受 零参：
    总线 为 一组：做 字典、一组：（空）、0、一组：（空）。
    返还 总线。
完。

事件总线_监听 接受 总线、事件名、监听名、优先级：
    表 为 总线 的 第 0 项。
    可变 列表 为 做 字典取 用 表、事件名。
    若 列表 等于 空：
        列表 为 一组：（空）。
        做 字典设 用 表、事件名、列表。
    完。
    做 追加 用 列表、一组：监听名、优先级。
    _事件_监听总数 为 _事件_监听总数 加 1。
完。

事件总线_监听一次 接受 总线、事件名、监听名、优先级：
    表 为 总线 的 第 0 项。
    可变 列表 为 做 字典取 用 表、事件名。
    若 列表 等于 空：
        列表 为 一组：（空）。
        做 字典设 用 表、事件名、列表。
    完。
    做 追加 用 列表、一组：监听名、优先级、1、0。
    _事件_监听总数 为 _事件_监听总数 加 1。
完。

事件总线_监听过滤 接受 总线、事件名、监听名、优先级、过滤条件：
    表 为 总线 的 第 0 项。
    可变 列表 为 做 字典取 用 表、事件名。
    若 列表 等于 空：
        列表 为 一组：（空）。
        做 字典设 用 表、事件名、列表。
    完。
    做 追加 用 列表、一组：监听名、优先级、0、过滤条件。
    _事件_监听总数 为 _事件_监听总数 加 1。
完。

事件总线_取消 接受 总线、事件名、监听名：
    可变 列表 为 做 字典取 用 总线 的 第 0 项、事件名。
    若 列表 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 的 第 0 项 等于 监听名：
            做 删除 用 列表、i。
            _事件_取消总数 为 _事件_取消总数 加 1。
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

事件总线_取消全部 接受 总线、事件名：
    做 字典设 用 总线 的 第 0 项、事件名、一组：（空）。
完。

事件总线_发布 接受 总线、事件名、数据：
    _事件_发布总数 为 _事件_发布总数 加 1。
    做 追加 用 _事件_历史、一组：事件名、数据。
    若 做 计数 用 _事件_历史 大于 2000：
        _事件_历史 为 一组：（空）。
    完。
    若 总线 的 第 2 项 等于 1：
        做 追加 用 总线 的 第 1 项、一组：事件名、数据。
        返还 0。
    完。
    可变 列表 为 做 字典取 用 总线 的 第 0 项、事件名。
    若 列表 等于 空：
        返还 0。
    完。
    做 事件_排序监听器 用 列表。
    收到数 为 做 计数 用 列表。
    可变 j 为 做 计数 用 列表 减 1。
    只要 j 大于或等于 0：
        列表 的 第 j 项 的 第 3 项 为 列表 的 第 j 项 的 第 3 项 加 1。
        若 列表 的 第 j 项 的 第 2 项 等于 1：
            做 删除 用 列表、j。
        完。
        j 为 j 减 1。
    完。
    总线 的 第 3 项 为 总线 的 第 3 项 加 1。
    返还 收到数。
完。

事件_排序监听器 接受 列表：
    可变 已改变 为 1。
    只要 已改变 等于 1：
        已改变 为 0。
        可变 i 为 0。
        只要 i 小于 做 计数 用 列表 减 1：
            若 列表 的 第 i 项 的 第 1 项 小于 列表 的 第 i 加 1 项 的 第 1 项：
                临时 为 列表 的 第 i 项。
                列表 的 第 i 项 为 列表 的 第 i 加 1 项。
                列表 的 第 i 加 1 项 为 临时。
                已改变 为 1。
            完。
            i 为 i 加 1。
        完。
    完。
完。

事件总线_批量发布 接受 总线、事件列表：
    可变 总收到 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 事件列表：
        总收到 为 总收到 加 做 事件总线_发布 用 总线、事件列表 的 第 i 项 的 第 0 项、事件列表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    返还 总收到。
完。

事件总线_暂停 接受 总线：
    总线 的 第 2 项 为 1。
完。

事件总线_恢复 接受 总线：
    总线 的 第 2 项 为 0。
    队列 为 总线 的 第 1 项。
    总线 的 第 1 项 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 队列：
        做 事件总线_发布 用 总线、队列 的 第 i 项 的 第 0 项、队列 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

事件总线_是暂停 接受 总线：
    返还 总线 的 第 2 项 等于 1。
完。

事件总线_监听器数 接受 总线、事件名：
    可变 列表 为 做 字典取 用 总线 的 第 0 项、事件名。
    若 列表 等于 空：
        返还 0。
    完。
    返还 做 计数 用 列表。
完。

事件总线_总监听器数 接受 总线：
    可变 总数 为 0。
    所有键 为 做 字典键列表 用 总线 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有键：
        可变 列表 为 做 字典取 用 总线 的 第 0 项、所有键 的 第 i 项。
        若 列表 不等于 空：
            总数 为 总数 加 做 计数 用 列表。
        完。
        i 为 i 加 1。
    完。
    返还 总数。
完。

事件总线_事件列表 接受 总线：
    返还 做 字典键列表 用 总线 的 第 0 项。
完。

事件总线_清空 接受 总线：
    总线 的 第 0 项 为 做 字典。
    总线 的 第 1 项 为 一组：（空）。
    总线 的 第 2 项 为 0。
    总线 的 第 3 项 为 0。
    返还 总线。
完。

事件总线_积压数 接受 总线：
    返还 做 计数 用 总线 的 第 1 项。
完。

事件总线_丢弃积压 接受 总线：
    丢弃数 为 做 计数 用 总线 的 第 1 项。
    总线 的 第 1 项 为 一组：（空）。
    返还 丢弃数。
完。

可变 _事件链 为 一组：（空）。

事件链_注册 接受 源事件、目标事件：
    做 追加 用 _事件链、一组：源事件、目标事件。
完。

事件链_触发 接受 总线、事件名、数据：
    做 事件总线_发布 用 总线、事件名、数据。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _事件链：
        若 _事件链 的 第 i 项 的 第 0 项 等于 事件名：
            做 事件总线_发布 用 总线、_事件链 的 第 i 项 的 第 1 项、数据。
        完。
        i 为 i 加 1。
    完。
完。

事件链_清空 接受 零参：
    _事件链 为 一组：（空）。
完。

可变 _延迟事件 为 一组：（空）。

延迟事件_添加 接受 事件名、数据、延迟步数：
    做 追加 用 _延迟事件、一组：事件名、数据、延迟步数。
完。

延迟事件_处理 接受 总线：
    可变 待删 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _延迟事件：
        _延迟事件 的 第 i 项 的 第 2 项 为 _延迟事件 的 第 i 项 的 第 2 项 减 1。
        若 _延迟事件 的 第 i 项 的 第 2 项 小于或等于 0：
            做 事件总线_发布 用 总线、_延迟事件 的 第 i 项 的 第 0 项、_延迟事件 的 第 i 项 的 第 1 项。
            做 追加 用 待删、i。
        完。
        i 为 i 加 1。
    完。
    可变 j 为 做 计数 用 待删 减 1。
    只要 j 大于或等于 0：
        做 删除 用 _延迟事件、待删 的 第 j 项。
        j 为 j 减 1。
    完。
完。

延迟事件_待处理数 接受 零参：
    返还 做 计数 用 _延迟事件。
完。

可变 _事件拦截器 为 一组：（空）。

事件拦截器_注册 接受 事件名、拦截名：
    做 追加 用 _事件拦截器、一组：事件名、拦截名、真。
完。

事件拦截器_启用 接受 索引：
    若 索引 大于或等于 0 并且 索引 小于 做 计数 用 _事件拦截器：
        _事件拦截器 的 第 索引 项 的 第 2 项 为 真。
    完。
完。

事件拦截器_禁用 接受 索引：
    若 索引 大于或等于 0 并且 索引 小于 做 计数 用 _事件拦截器：
        _事件拦截器 的 第 索引 项 的 第 2 项 为 假。
    完。
完。

事件拦截器_检查 接受 事件名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _事件拦截器：
        若 _事件拦截器 的 第 i 项 的 第 0 项 等于 事件名 并且 _事件拦截器 的 第 i 项 的 第 2 项：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

事件历史_最近 接受 数量：
    可变 结果 为 一组：（空）。
    可变 起点 为 做 计数 用 _事件_历史 减 数量。
    若 起点 小于 0：
        起点 为 0。
    完。
    可变 i 为 起点。
    只要 i 小于 做 计数 用 _事件_历史：
        做 追加 用 结果、_事件_历史 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

事件历史_按名称 接受 事件名：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _事件_历史：
        若 _事件_历史 的 第 i 项 的 第 0 项 等于 事件名：
            做 追加 用 结果、_事件_历史 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

事件历史_清空 接受 零参：
    _事件_历史 为 一组：（空）。
完。

事件_频率分析 接受 零参：
    可变 频率表 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _事件_历史：
        名 为 _事件_历史 的 第 i 项 的 第 0 项。
        当前 为 做 字典取 用 频率表、名。
        若 当前 等于 空：
            做 字典设 用 频率表、名、1。
        否则：
            做 字典设 用 频率表、名、当前 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 频率表。
完。

事件_热点检测 接受 零参：
    频率 为 做 事件_频率分析。
    键列表 为 做 字典键列表 用 频率。
    可变 最热名 为 「」。
    可变 最高频 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表：
        频 为 做 字典取 用 频率、键列表 的 第 i 项。
        若 频 大于 最高频：
            最高频 为 频。
            最热名 为 键列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 最热名 等于 「」：
        返还 「无事件记录」。
    完。
    返还 做 字符串拼接 用 最热名、做 字符串拼接 用 「(」、做 字符串拼接 用 做 转字符串 用 最高频、「次)」。
完。

事件_死监听器检测 接受 总线：
    可变 死监听 为 一组：（空）。
    所有键 为 做 字典键列表 用 总线 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有键：
        可变 列表 为 做 字典取 用 总线 的 第 0 项、所有键 的 第 i 项。
        若 列表 不等于 空：
            可变 j 为 0。
            只要 j 小于 做 计数 用 列表：
                若 列表 的 第 j 项 的 第 3 项 等于 0：
                    做 追加 用 死监听、一组：所有键 的 第 i 项、列表 的 第 j 项 的 第 0 项。
                完。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 死监听。
完。

事件_性能评估 接受 总线：
    总监听 为 做 事件总线_总监听器数 用 总线。
    积压 为 做 事件总线_积压数 用 总线。
    可变 评分 为 100。
    若 总监听 大于 100：
        评分 为 评分 减 20。
    完。
    若 积压 大于 50：
        评分 为 评分 减 30。
    完。
    若 _事件_发布总数 大于 10000：
        评分 为 评分 减 10。
    完。
    若 评分 小于 0：
        评分 为 0。
    完。
    返还 评分。
完。

事件名_拼接 接受 命名空间、名称：
    返还 做 字符串拼接 用 命名空间、做 字符串拼接 用 「.」、名称。
完。

事件名_取命名空间 接受 全名：
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 全名：
        若 做 字节取 用 全名、i 等于 字符_句点：
            最后点 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最后点 等于 负 1：
        返还 「」。
    完。
    可变 结果 为 「」。
    i 为 0。
    只要 i 小于 最后点：
        结果 为 做 字符串拼接 用 结果、做 字符 用 做 字节取 用 全名、i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

事件名_取名称 接受 全名：
    可变 i 为 0。
    只要 i 小于 做 字节长度 用 全名：
        若 做 字节取 用 全名、i 等于 字符_句点：
            最后点 为 i。
        完。
        i 为 i 加 1。
    完。
    若 最后点 等于 负 1：
        返还 全名。
    完。
    可变 结果 为 「」。
    i 为 最后点 加 1。
    只要 i 小于 做 字节长度 用 全名：
        结果 为 做 字符串拼接 用 结果、做 字符 用 做 字节取 用 全名、i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

事件_聚合计数 接受 事件名：
    可变 计 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _事件_历史：
        若 _事件_历史 的 第 i 项 的 第 0 项 等于 事件名：
            计 为 计 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 计。
完。

事件_唯一事件数 接受 零参：
    可变 已见 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _事件_历史：
        可变 找到 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 已见：
            若 已见 的 第 j 项 等于 _事件_历史 的 第 i 项 的 第 0 项：
                找到 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 找到：
            做 追加 用 已见、_事件_历史 的 第 i 项 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 做 计数 用 已见。
完。

事件总线_快照 接受 总线：
    可变 快照 为 一组：（空）。
    所有键 为 做 字典键列表 用 总线 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有键：
        可变 列表 为 做 字典取 用 总线 的 第 0 项、所有键 的 第 i 项。
        可变 监听数 为 0。
        若 列表 不等于 空：
            监听数 为 做 计数 用 列表。
        完。
        做 追加 用 快照、一组：所有键 的 第 i 项、监听数。
        i 为 i 加 1。
    完。
    返还 快照。
完。

优先级_最高 为 100。

优先级_高 为 75。

优先级_普通 为 50。

优先级_低 为 25。

优先级_最低 为 0。

事件总线_克隆 接受 总线：
    可变 新总线 为 做 事件总线_创建。
    可变 新列表 为 一组：（空）。
    所有键 为 做 字典键列表 用 总线 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 所有键：
        可变 列表 为 做 字典取 用 总线 的 第 0 项、所有键 的 第 i 项。
        若 列表 不等于 空：
            可变 j 为 0。
            只要 j 小于 做 计数 用 列表：
                做 追加 用 新列表、一组：列表 的 第 j 项 的 第 0 项、列表 的 第 j 项 的 第 1 项、列表 的 第 j 项 的 第 2 项、0。
                j 为 j 加 1。
            完。
            做 字典设 用 新总线 的 第 0 项、所有键 的 第 i 项、新列表。
        完。
        i 为 i 加 1。
    完。
    返还 新总线。
完。

事件_风暴检测 接受 阈值：
    若 做 计数 用 _事件_历史 小于 10：
        返还 假。
    完。
    可变 近期数 为 0。
    可变 i 为 做 计数 用 _事件_历史 减 1。
    只要 i 大于或等于 0 并且 近期数 小于 10：
        近期数 为 近期数 加 1。
        i 为 i 减 1。
    完。
    返还 近期数 大于或等于 阈值。
完。

事件_监听器健康 接受 总线：
    死 为 做 事件_死监听器检测 用 总线。
    总 为 做 事件总线_总监听器数 用 总线。
    若 总 等于 0：
        返还 一组：100、「无监听器」。
    完。
    死比例 为 做 计数 用 死 乘 100 除以 总。
    若 死比例 大于 50：
        返还 一组：30、「大量死监听器」。
    完。
    若 死比例 大于 20：
        返还 一组：60、「部分死监听器」。
    完。
    返还 一组：100、「监听器健康」。
完。

可变 _ecs_下一个id 为 1。

可变 _ecs_实体列表 为 一组：（空）。

可变 _ecs_组件表 为 一组：（空）。

可变 _ecs_系统列表 为 一组：（空）。

事件ECS_创建实体 接受 零参：
    id 为 _ecs_下一个id。
    _ecs_下一个id 为 _ecs_下一个id 加 1。
    做 追加 用 _ecs_实体列表、id。
    返还 id。
完。

事件ECS_销毁实体 接受 id：
    可变 新列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 _ecs_实体列表：
        若 _ecs_实体列表 的 第 i 项 不等于 id：
            做 追加 用 新列表、_ecs_实体列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    _ecs_实体列表 为 新列表。
    可变 ci 为 0。
    只要 ci 小于 做 计数 用 _ecs_组件表：
        做 字典删 用 _ecs_组件表 的 第 ci 项 的 第 1 项、做 转字符串 用 id。
        ci 为 ci 加 1。
    完。
完。

_ecs_找组件表 接受 类型名：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _ecs_组件表：
        若 _ecs_组件表 的 第 i 项 的 第 0 项 等于 类型名：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

事件ECS_添加组件 接受 实体、类型名、数据：
    可变 索引 为 做 _ecs_找组件表 用 类型名。
    若 索引 小于 0：
        做 追加 用 _ecs_组件表、一组：类型名、做 字典。
        索引 为 做 计数 用 _ecs_组件表 减 1。
    完。
    做 字典设 用 _ecs_组件表 的 第 索引 项 的 第 1 项、做 转字符串 用 实体、数据。
完。

事件ECS_获取组件 接受 实体、类型名：
    索引 为 做 _ecs_找组件表 用 类型名。
    若 索引 小于 0：
        返还 空。
    完。
    返还 做 字典取 用 _ecs_组件表 的 第 索引 项 的 第 1 项、做 转字符串 用 实体。
完。

事件ECS_有组件 接受 实体、类型名：
    返还 做 事件ECS_获取组件 用 实体、类型名 不等于 空。
完。

事件ECS_注册系统 接受 名称、所需组件、优先级：
    做 追加 用 _ecs_系统列表、一组：名称、所需组件、优先级。
完。

事件ECS_查询 接受 所需组件：
    可变 结果 为 一组：（空）。
    可变 ei 为 0。
    只要 ei 小于 做 计数 用 _ecs_实体列表：
        实体 为 _ecs_实体列表 的 第 ei 项。
        可变 全有 为 真。
        可变 ci 为 0。
        只要 ci 小于 做 计数 用 所需组件：
            若 做 事件ECS_有组件 用 实体、所需组件 的 第 ci 项 等于 假：
                全有 为 假。
            完。
            ci 为 ci 加 1。
        完。
        若 全有：
            做 追加 用 结果、实体。
        完。
        ei 为 ei 加 1。
    完。
    返还 结果。
完。

事件ECS_实体数 接受 零参：
    返还 做 计数 用 _ecs_实体列表。
完。

事件ECS_组件类型数 接受 零参：
    返还 做 计数 用 _ecs_组件表。
完。

事件ECS_系统数 接受 零参：
    返还 做 计数 用 _ecs_系统列表。
完。

可变 _场景_节点列表 为 一组：（空）。

可变 _场景_下一个id 为 1。

场景_创建节点 接受 名称、父id：
    id 为 _场景_下一个id。
    _场景_下一个id 为 _场景_下一个id 加 1。
    节点 为 一组：id、父id、一组：（空）、一组：0、10000、10000、10000、名称、真。
    做 追加 用 _场景_节点列表、节点。
    若 父id 大于 0：
        可变 i 为 0。
        只要 i 小于 做 计数 用 _场景_节点列表：
            若 _场景_节点列表 的 第 i 项 的 第 0 项 等于 父id：
                做 追加 用 _场景_节点列表 的 第 i 项 的 第 2 项、id。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 id。
完。

_场景_找节点 接受 id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _场景_节点列表：
        若 _场景_节点列表 的 第 i 项 的 第 0 项 等于 id：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

场景_设置位置 接受 id、x、y、z：
    索引 为 做 _场景_找节点 用 id。
    若 索引 大于或等于 0：
        _场景_节点列表 的 第 索引 项 的 第 3 项 的 第 0 项 为 x。
        _场景_节点列表 的 第 索引 项 的 第 3 项 的 第 1 项 为 y。
        _场景_节点列表 的 第 索引 项 的 第 3 项 的 第 2 项 为 z。
    完。
完。

场景_设置可见 接受 id、可见：
    索引 为 做 _场景_找节点 用 id。
    若 索引 大于或等于 0：
        _场景_节点列表 的 第 索引 项 的 第 5 项 为 可见。
    完。
完。

场景_节点数 接受 零参：
    返还 做 计数 用 _场景_节点列表。
完。

可变 _资源_列表 为 一组：（空）。

可变 _资源_下一个id 为 1。

资源_加载 接受 类型、名称、数据：
    id 为 _资源_下一个id。
    _资源_下一个id 为 _资源_下一个id 加 1。
    做 追加 用 _资源_列表、一组：id、类型、名称、1、数据、2。
    返还 id。
完。

_资源_找 接受 id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 _资源_列表：
        若 _资源_列表 的 第 i 项 的 第 0 项 等于 id：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

资源_获取 接受 id：
    索引 为 做 _资源_找 用 id。
    若 索引 小于 0：
        返还 空。
    完。
    返还 _资源_列表 的 第 索引 项 的 第 4 项。
完。

资源_增引用 接受 id：
    索引 为 做 _资源_找 用 id。
    若 索引 大于或等于 0：
        _资源_列表 的 第 索引 项 的 第 3 项 为 _资源_列表 的 第 索引 项 的 第 3 项 加 1。
    完。
完。

资源_减引用 接受 id：
    索引 为 做 _资源_找 用 id。
    若 索引 大于或等于 0：
        _资源_列表 的 第 索引 项 的 第 3 项 为 _资源_列表 的 第 索引 项 的 第 3 项 减 1。
        若 _资源_列表 的 第 索引 项 的 第 3 项 小于或等于 0：
            _资源_列表 的 第 索引 项 的 第 5 项 为 3。
        完。
    完。
完。

资源_总数 接受 零参：
    返还 做 计数 用 _资源_列表。
完。

可变 _主循环 为 一组：假、0、16、5。

主循环_初始化 接受 固定步长ms：
    _主循环 为 一组：真、0、固定步长ms、5。
完。

主循环_停止 接受 零参：
    _主循环 的 第 0 项 为 假。
完。

主循环_tick 接受 当前时间ms：
    若 _主循环 的 第 0 项 等于 假：
        返还 0。
    完。
    _主循环 的 第 1 项 为 _主循环 的 第 1 项 加 1。
    _主循环 的 第 2 项 为 _主循环 的 第 2 项 加 _主循环 的 第 3 项。
    可变 步数 为 1。
    若 步数 大于 _主循环 的 第 4 项：
        步数 为 _主循环 的 第 4 项。
    完。
    返还 步数。
完。

主循环_帧号 接受 零参：
    返还 _主循环 的 第 1 项。
完。

主循环_运行中 接受 零参：
    返还 _主循环 的 第 0 项。
完。

可变 _引擎_帧预算ms 为 16。

可变 _引擎_渲染预算ms 为 8。

可变 _引擎_输入延迟ms 为 2。

可变 _引擎_音频缓冲ms 为 10。

可变 _引擎_智能推理预算ms 为 4。

可变 _引擎_显存预算MB 为 256。

可变 _引擎_线程数 为 4。

引擎合同_设置 接受 帧、渲染、输入、音频、AI、显存、线程：
    _引擎_帧预算ms 为 帧。
    _引擎_渲染预算ms 为 渲染。
    _引擎_输入延迟ms 为 输入。
    _引擎_音频缓冲ms 为 音频。
    _引擎_智能推理预算ms 为 AI。
    _引擎_显存预算MB 为 显存。
    _引擎_线程数 为 线程。
完。

可变 _引擎_帧数 为 0。

可变 _引擎_最大帧时间 为 0。

可变 _引擎_累计帧时间 为 0。

引擎_记录帧 接受 帧时间ms：
    _引擎_帧数 为 _引擎_帧数 加 1。
    _引擎_累计帧时间 为 _引擎_累计帧时间 加 帧时间ms。
    若 帧时间ms 大于 _引擎_最大帧时间：
        _引擎_最大帧时间 为 帧时间ms。
    完。
完。

引擎_平均帧时间 接受 零参：
    若 _引擎_帧数 等于 0：
        返还 0。
    完。
    返还 _引擎_累计帧时间 除以 _引擎_帧数。
完。

引擎主循环_报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「帧预算ms」、做 转字符串 用 _引擎_帧预算ms。
    做 字典设 用 r、「渲染预算ms」、做 转字符串 用 _引擎_渲染预算ms。
    做 字典设 用 r、「输入延迟ms」、做 转字符串 用 _引擎_输入延迟ms。
    做 字典设 用 r、「音频缓冲ms」、做 转字符串 用 _引擎_音频缓冲ms。
    做 字典设 用 r、「智能推理预算ms」、做 转字符串 用 _引擎_智能推理预算ms。
    做 字典设 用 r、「显存预算MB」、做 转字符串 用 _引擎_显存预算MB。
    做 字典设 用 r、「线程数」、做 转字符串 用 _引擎_线程数。
    做 字典设 用 r、「总帧数」、做 转字符串 用 _引擎_帧数。
    做 字典设 用 r、「平均帧时间」、做 转字符串 用 做 引擎_平均帧时间。
    做 字典设 用 r、「最大帧时间」、做 转字符串 用 _引擎_最大帧时间。
    返还 r。
完。

可变 _资产_总数 为 0。

可变 _资产_已加载 为 0。

可变 _资产_流式数 为 0。

资产_记录加载 接受 零参：
    _资产_已加载 为 _资产_已加载 加 1。
完。

资产_记录流式 接受 零参：
    _资产_流式数 为 _资产_流式数 加 1。
完。

资产_设置总数 接受 n：
    _资产_总数 为 n。
完。

事件_资产管线_报告 接受 零参：
    可变 r 为 做 字典。
    做 字典设 用 r、「总数」、做 转字符串 用 _资产_总数。
    做 字典设 用 r、「已加载」、做 转字符串 用 _资产_已加载。
    做 字典设 用 r、「流式数」、做 转字符串 用 _资产_流式数。
    返还 r。
完。

本章 收束。
