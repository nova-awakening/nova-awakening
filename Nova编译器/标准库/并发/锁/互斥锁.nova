《互斥锁》

引用《..·..·基础》。

自旋锁_创建 接受 自旋上限：
    可变 实际上限 为 自旋上限。
    若 实际上限 小于或等于 0：
        实际上限 为 1000。
    完。
    返还 一组：0、负 1、实际上限、0。
完。

自旋锁_获取 接受 锁、线程ID：
    可变 自旋次数 为 0。
    只要 锁 的 第 0 项 不等于 0：
        自旋次数 为 自旋次数 加 1。
        锁 的 第 3 项 为 锁 的 第 3 项 加 1。
        若 自旋次数 大于或等于 锁 的 第 2 项：
            返还 假。
        完。
    完。
    锁 的 第 0 项 为 1。
    锁 的 第 1 项 为 线程ID。
    返还 真。
完。

自旋锁_释放 接受 锁、线程ID：
    若 锁 的 第 1 项 不等于 线程ID：
        返还 假。
    完。
    锁 的 第 0 项 为 0。
    锁 的 第 1 项 为 负 1。
    返还 真。
完。

自旋锁_已锁定 接受 锁：
    返还 锁 的 第 0 项 不等于 0。
完。

自旋锁_持有者 接受 锁：
    返还 锁 的 第 1 项。
完。

可重入锁_创建 接受 公平：
    返还 一组：0、负 1、0、一组：（空）、公平。
完。

可重入锁_获取 接受 锁、线程ID：
    若 锁 的 第 0 项 等于 0：
        锁 的 第 0 项 为 1。
        锁 的 第 1 项 为 线程ID。
        锁 的 第 2 项 为 1。
        返还 真。
    完。
    若 锁 的 第 1 项 等于 线程ID：
        锁 的 第 2 项 为 锁 的 第 2 项 加 1。
        返还 真。
    完。
    若 锁 的 第 4 项：
        可变 已在队列 为 假。
        可变 i 为 0。
        只要 i 小于 做 计数 用 锁 的 第 3 项 并且 已在队列 等于 假：
            若 锁 的 第 3 项 的 第 i 项 等于 线程ID：
                已在队列 为 真。
            完。
            i 为 i 加 1。
        完。
        若 已在队列 等于 假：
            做 追加 用 锁 的 第 3 项、线程ID。
        完。
    完。
    返还 假。
完。

可重入锁_释放 接受 锁、线程ID：
    可变 新队列 为 一组：（空）。
    若 锁 的 第 1 项 不等于 线程ID：
        返还 假。
    完。
    锁 的 第 2 项 为 锁 的 第 2 项 减 1。
    若 锁 的 第 2 项 小于或等于 0：
        锁 的 第 2 项 为 0。
        若 锁 的 第 4 项 并且 做 计数 用 锁 的 第 3 项 大于 0：
            下一个 为 锁 的 第 3 项 的 第 0 项。
            可变 i 为 1。
            只要 i 小于 做 计数 用 锁 的 第 3 项：
                做 追加 用 新队列、锁 的 第 3 项 的 第 i 项。
                i 为 i 加 1。
            完。
            锁 的 第 3 项 为 新队列。
            锁 的 第 1 项 为 下一个。
            锁 的 第 2 项 为 1。
        否则：
            锁 的 第 0 项 为 0。
            锁 的 第 1 项 为 负 1。
        完。
    完。
    返还 真。
完。

可重入锁_重入深度 接受 锁：
    返还 锁 的 第 2 项。
完。

可重入锁_等待数 接受 锁：
    返还 做 计数 用 锁 的 第 3 项。
完。

可重入锁_是否持有 接受 锁、线程ID：
    返还 锁 的 第 1 项 等于 线程ID。
完。

读写锁_创建 接受 读优先：
    返还 一组：0、0、负 1、一组：（空）、一组：（空）、读优先。
完。

读写锁_读获取 接受 锁、线程ID：
    若 锁 的 第 1 项 不等于 0：
        返还 假。
    完。
    若 锁 的 第 5 项 等于 假 并且 做 计数 用 锁 的 第 4 项 大于 0：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 锁 的 第 3 项：
        若 锁 的 第 3 项 的 第 i 项 等于 线程ID：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 锁 的 第 3 项、线程ID。
    锁 的 第 0 项 为 锁 的 第 0 项 加 1。
    返还 真。
完。

读写锁_读释放 接受 锁、线程ID：
    可变 新读者 为 一组：（空）。
    可变 找到 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 锁 的 第 3 项：
        若 锁 的 第 3 项 的 第 i 项 等于 线程ID 并且 找到 等于 假：
            找到 为 真。
        否则：
            做 追加 用 新读者、锁 的 第 3 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 找到 等于 假：
        返还 假。
    完。
    锁 的 第 3 项 为 新读者。
    锁 的 第 0 项 为 锁 的 第 0 项 减 1。
    返还 真。
完。

读写锁_写获取 接受 锁、线程ID：
    若 锁 的 第 1 项 不等于 0 并且 锁 的 第 2 项 不等于 线程ID：
        返还 假。
    完。
    若 锁 的 第 0 项 大于 0：
        若 锁 的 第 0 项 等于 1 并且 做 计数 用 锁 的 第 3 项 等于 1 并且 锁 的 第 3 项 的 第 0 项 等于 线程ID：
            锁 的 第 3 项 为 一组：（空）。
            锁 的 第 0 项 为 0。
            锁 的 第 1 项 为 1。
            锁 的 第 2 项 为 线程ID。
            返还 真。
        完。
        可变 已等待 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 锁 的 第 4 项 并且 已等待 等于 假：
            若 锁 的 第 4 项 的 第 j 项 等于 线程ID：
                已等待 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已等待 等于 假：
            做 追加 用 锁 的 第 4 项、线程ID。
        完。
        返还 假。
    完。
    锁 的 第 1 项 为 1。
    锁 的 第 2 项 为 线程ID。
    返还 真。
完。

读写锁_写释放 接受 锁、线程ID：
    可变 新等待 为 一组：（空）。
    若 锁 的 第 2 项 不等于 线程ID：
        返还 假。
    完。
    锁 的 第 1 项 为 0。
    锁 的 第 2 项 为 负 1。
    若 做 计数 用 锁 的 第 4 项 大于 0：
        下一写 为 锁 的 第 4 项 的 第 0 项。
        可变 i 为 1。
        只要 i 小于 做 计数 用 锁 的 第 4 项：
            做 追加 用 新等待、锁 的 第 4 项 的 第 i 项。
            i 为 i 加 1。
        完。
        锁 的 第 4 项 为 新等待。
        锁 的 第 1 项 为 1。
        锁 的 第 2 项 为 下一写。
    完。
    返还 真。
完。

读写锁_读者数 接受 锁：
    返还 锁 的 第 0 项。
完。

读写锁_是否写锁 接受 锁：
    返还 锁 的 第 1 项 不等于 0。
完。

读写锁_写等待数 接受 锁：
    返还 做 计数 用 锁 的 第 4 项。
完。

条件变量_创建 接受 零参：
    返还 一组：一组：（空）。
完。

条件_等待 接受 条件、线程ID：
    做 追加 用 条件 的 第 0 项、线程ID。
完。

条件_通知一个 接受 条件：
    若 做 计数 用 条件 的 第 0 项 大于 0：
        唤醒 为 条件 的 第 0 项 的 第 0 项。
        可变 新队列 为 一组：（空）。
        可变 i 为 1。
        只要 i 小于 做 计数 用 条件 的 第 0 项：
            做 追加 用 新队列、条件 的 第 0 项 的 第 i 项。
            i 为 i 加 1。
        完。
        条件 的 第 0 项 为 新队列。
        返还 唤醒。
    完。
    返还 负 1。
完。

条件_通知全部 接受 条件：
    唤醒列表 为 条件 的 第 0 项。
    条件 的 第 0 项 为 一组：（空）。
    返还 唤醒列表。
完。

条件_等待数 接受 条件：
    返还 做 计数 用 条件 的 第 0 项。
完。

门闩_创建 接受 计数：
    返还 一组：计数、计数、一组：（空）。
完。

门闩_递减 接受 门闩：
    若 门闩 的 第 0 项 大于 0：
        门闩 的 第 0 项 为 门闩 的 第 0 项 减 1。
    完。
    返还 门闩 的 第 0 项。
完。

门闩_等待 接受 门闩、线程ID：
    若 门闩 的 第 0 项 大于 0：
        做 追加 用 门闩 的 第 2 项、线程ID。
        返还 假。
    完。
    返还 真。
完。

门闩_已完成 接受 门闩：
    返还 门闩 的 第 0 项 小于或等于 0。
完。

门闩_剩余 接受 门闩：
    返还 门闩 的 第 0 项。
完。

门闩_重置 接受 门闩：
    门闩 的 第 0 项 为 门闩 的 第 1 项。
    门闩 的 第 2 项 为 一组：（空）。
完。

栅栏_创建 接受 参与者数：
    返还 一组：参与者数、0、一组：（空）、0。
完。

栅栏_等待 接受 栅栏、线程ID：
    做 追加 用 栅栏 的 第 2 项、线程ID。
    栅栏 的 第 1 项 为 栅栏 的 第 1 项 加 1。
    若 栅栏 的 第 1 项 大于或等于 栅栏 的 第 0 项：
        释放列表 为 栅栏 的 第 2 项。
        栅栏 的 第 1 项 为 0。
        栅栏 的 第 2 项 为 一组：（空）。
        栅栏 的 第 3 项 为 栅栏 的 第 3 项 加 1。
        返还 一组：真、释放列表。
    完。
    返还 一组：假、一组：（空）。
完。

栅栏_轮次 接受 栅栏：
    返还 栅栏 的 第 3 项。
完。

栅栏_等待中 接受 栅栏：
    返还 栅栏 的 第 1 项。
完。

本章 收束。
