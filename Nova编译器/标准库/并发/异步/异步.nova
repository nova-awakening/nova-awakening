《异步》

引用《..·..·基础》。

未完成 为 0。

已完成 为 1。

已失败 为 2。

已取消 为 3。

未来_创建 接受 零参：
    返还 一组：未完成、空、空、一组：（空）、空。
完。

未来_完成 接受 f、值：
    若 f 的 第 0 项 不等于 未完成：
        返还 假。
    完。
    f 的 第 0 项 为 已完成。
    f 的 第 1 项 为 值。
    f 的 第 6 项 为 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 f 的 第 3 项：
        回调 为 f 的 第 3 项 的 第 i 项。
        做 回调 的 第 0 项 用 值。
        i 为 i 加 1。
    完。
    f 的 第 3 项 为 一组：（空）。
    返还 真。
完。

未来_失败 接受 f、错误：
    若 f 的 第 0 项 不等于 未完成：
        返还 假。
    完。
    f 的 第 0 项 为 已失败。
    f 的 第 2 项 为 错误。
    f 的 第 6 项 为 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 f 的 第 3 项：
        回调 为 f 的 第 3 项 的 第 i 项。
        若 做 计数 用 回调 大于 1 并且 回调 的 第 1 项 不等于 空：
            做 回调 的 第 1 项 用 错误。
        完。
        i 为 i 加 1。
    完。
    f 的 第 3 项 为 一组：（空）。
    返还 真。
完。

未来_取消 接受 f：
    若 f 的 第 0 项 不等于 未完成：
        返还 假。
    完。
    f 的 第 0 项 为 已取消。
    若 f 的 第 4 项 不等于 空：
        做 f 的 第 4 项。
    完。
    f 的 第 3 项 为 一组：（空）。
    返还 真。
完。

未来_然后 接受 f、成功回调、错误回调：
    下一个 为 做 未来_创建。
    若 f 的 第 0 项 等于 已完成：
        结果 为 做 成功回调 用 f 的 第 1 项。
        做 未来_完成 用 下一个、结果。
    否则若 f 的 第 0 项 等于 已失败：
        若 错误回调 不等于 空：
            结果 为 做 错误回调 用 f 的 第 2 项。
            做 未来_完成 用 下一个、结果。
        否则：
            做 未来_失败 用 下一个、f 的 第 2 项。
        完。
    否则若 f 的 第 0 项 等于 未完成：
        做 追加 用 f 的 第 3 项、一组：成功回调、错误回调、下一个。
    完。
    返还 下一个。
完。

未来_状态 接受 f：
    返还 f 的 第 0 项。
完。

未来_值 接受 f：
    返还 f 的 第 1 项。
完。

未来_错误 接受 f：
    返还 f 的 第 2 项。
完。

未来_已完成 接受 f：
    返还 f 的 第 0 项 等于 已完成。
完。

未来_已失败 接受 f：
    返还 f 的 第 0 项 等于 已失败。
完。

全部完成 接受 未来列表：
    n 为 做 计数 用 未来列表。
    结果 为 做 未来_创建。
    若 n 等于 0：
        做 未来_完成 用 结果、一组：（空）。
        返还 结果。
    完。
    可变 值列表 为 一组：（空）。
    可变 全完成 为 真。
    可变 有失败 为 假。
    可变 失败信息 为 空。
    可变 i 为 0。
    只要 i 小于 n：
        f 为 未来列表 的 第 i 项。
        若 f 的 第 0 项 等于 已完成：
            做 追加 用 值列表、f 的 第 1 项。
        否则若 f 的 第 0 项 等于 已失败：
            有失败 为 真。
            失败信息 为 f 的 第 2 项。
        否则：
            全完成 为 假。
            做 追加 用 值列表、空。
        完。
        i 为 i 加 1。
    完。
    若 有失败：
        做 未来_失败 用 结果、失败信息。
    否则若 全完成：
        做 未来_完成 用 结果、值列表。
    完。
    返还 结果。
完。

竞速 接受 未来列表：
    结果 为 做 未来_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 未来列表：
        f 为 未来列表 的 第 i 项。
        若 f 的 第 0 项 等于 已完成：
            做 未来_完成 用 结果、f 的 第 1 项。
            返还 结果。
        否则若 f 的 第 0 项 等于 已失败：
            做 未来_失败 用 结果、f 的 第 2 项。
            返还 结果。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

事件环_创建 接受 最大tick：
    可变 上限 为 最大tick。
    若 上限 小于或等于 0：
        上限 为 1000000。
    完。
    返还 一组：一组：（空）、一组：（空）、0、假、上限、一组：（空）、空。
完。

事件_提交 接受 事件环、任务函数、优先级：
    任务ID 为 做 计数 用 事件环 的 第 0 项。
    任务 为 一组：任务ID、任务函数、优先级、0、事件环 的 第 2 项。
    可变 插入位 为 做 计数 用 事件环 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 事件环 的 第 0 项：
        若 事件环 的 第 0 项 的 第 i 项 的 第 2 项 小于 优先级：
            插入位 为 i。
            止。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 事件环 的 第 0 项、任务。
    返还 任务ID。
完。

事件_定时 接受 事件环、延迟tick、任务函数：
    触发时间 为 事件环 的 第 2 项 加 延迟tick。
    做 追加 用 事件环 的 第 1 项、一组：触发时间、任务函数、假。
    返还 做 计数 用 事件环 的 第 1 项 减 1。
完。

事件_周期 接受 事件环、间隔tick、任务函数：
    触发时间 为 事件环 的 第 2 项 加 间隔tick。
    做 追加 用 事件环 的 第 1 项、一组：触发时间、任务函数、间隔tick。
    返还 做 计数 用 事件环 的 第 1 项 减 1。
完。

事件_取消定时 接受 事件环、定时器ID：
    若 定时器ID 大于或等于 0 并且 定时器ID 小于 做 计数 用 事件环 的 第 1 项：
        事件环 的 第 1 项 的 第 定时器ID 项 的 第 2 项 为 负 1。
    完。
完。

事件_tick 接受 事件环：
    可变 新队列 为 一组：（空）。
    若 事件环 的 第 3 项 等于 假：
        返还 0。
    完。
    事件环 的 第 2 项 为 事件环 的 第 2 项 加 1。
    可变 执行数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 事件环 的 第 1 项：
        定时器 为 事件环 的 第 1 项 的 第 i 项。
        若 定时器 的 第 2 项 不等于 负 1 并且 事件环 的 第 2 项 大于或等于 定时器 的 第 0 项：
            做 定时器 的 第 1 项。
            执行数 为 执行数 加 1。
            若 定时器 的 第 2 项 不等于 假 并且 定时器 的 第 2 项 大于 0：
                定时器 的 第 0 项 为 事件环 的 第 2 项 加 定时器 的 第 2 项。
            否则：
                定时器 的 第 2 项 为 负 1。
            完。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 事件环 的 第 0 项 大于 0：
        任务 为 事件环 的 第 0 项 的 第 0 项。
        i 为 1。
        只要 i 小于 做 计数 用 事件环 的 第 0 项：
            做 追加 用 新队列、事件环 的 第 0 项 的 第 i 项。
            i 为 i 加 1。
        完。
        事件环 的 第 0 项 为 新队列。
        若 任务 的 第 3 项 等于 0：
            做 任务 的 第 1 项。
            执行数 为 执行数 加 1。
        完。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 事件环 的 第 5 项：
        io 为 事件环 的 第 5 项 的 第 i 项。
        若 做 io 的 第 1 项：
            做 io 的 第 2 项。
            执行数 为 执行数 加 1。
        完。
        i 为 i 加 1。
    完。
    若 执行数 等于 0 并且 事件环 的 第 6 项 不等于 空：
        做 事件环 的 第 6 项。
    完。
    返还 执行数。
完。

事件_运行 接受 事件环：
    事件环 的 第 3 项 为 真。
    可变 总执行 为 0。
    只要 事件环 的 第 3 项 并且 事件环 的 第 2 项 小于 事件环 的 第 4 项：
        执行 为 做 事件_tick 用 事件环。
        总执行 为 总执行 加 执行。
        若 做 计数 用 事件环 的 第 0 项 等于 0：
            可变 有活跃定时 为 假。
            可变 i 为 0。
            只要 i 小于 做 计数 用 事件环 的 第 1 项 并且 有活跃定时 等于 假：
                若 事件环 的 第 1 项 的 第 i 项 的 第 2 项 不等于 负 1：
                    有活跃定时 为 真。
                完。
                i 为 i 加 1。
            完。
            若 有活跃定时 等于 假 并且 做 计数 用 事件环 的 第 5 项 等于 0：
                事件环 的 第 3 项 为 假。
            完。
        完。
    完。
    事件环 的 第 3 项 为 假。
    返还 总执行。
完。

事件_停止 接受 事件环：
    事件环 的 第 3 项 为 假。
完。

事件_当前tick 接受 事件环：
    返还 事件环 的 第 2 项。
完。

事件_任务数 接受 事件环：
    返还 做 计数 用 事件环 的 第 0 项。
完。

事件_运行中 接受 事件环：
    返还 事件环 的 第 3 项。
完。

事件_注册IO 接受 事件环、就绪检测、回调：
    做 追加 用 事件环 的 第 5 项、一组：做 计数 用 事件环 的 第 5 项、就绪检测、回调。
完。

事件_空闲回调 接受 事件环、回调：
    事件环 的 第 6 项 为 回调。
完。

本章 收束。
