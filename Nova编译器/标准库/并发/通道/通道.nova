《通道》

引用《..·..·基础》。

通道_创建 接受 容量：
    可变 实际容量 为 容量。
    若 实际容量 小于或等于 0：
        实际容量 为 1。
    完。
    可变 缓冲 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 实际容量：
        做 追加 用 缓冲、空。
        i 为 i 加 1。
    完。
    返还 一组：缓冲、实际容量、0、0、假、一组：（空）、一组：（空）。
完。

通道_发送 接受 通道、值：
    若 通道 的 第 5 项：
        返还 一组：「错误」、「通道已关闭」。
    完。
    若 通道 的 第 4 项 大于或等于 通道 的 第 1 项：
        返还 一组：「阻塞」、「缓冲区已满」。
    完。
    通道 的 第 0 项 的 第 通道 的 第 3 项 项 为 值。
    通道 的 第 3 项 为 (通道 的 第 3 项 加 1) 取余于 通道 的 第 1 项。
    通道 的 第 4 项 为 通道 的 第 4 项 加 1。
    通道 的 第 8 项 为 通道 的 第 8 项 加 1。
    若 做 计数 用 通道 的 第 7 项 大于 0：
        接收者 为 通道 的 第 7 项 的 第 0 项。
        可变 新等待 为 一组：（空）。
        可变 i 为 1。
        只要 i 小于 做 计数 用 通道 的 第 7 项：
            做 追加 用 新等待、通道 的 第 7 项 的 第 i 项。
            i 为 i 加 1。
        完。
        通道 的 第 7 项 为 新等待。
        返还 一组：「成功」、接收者。
    完。
    返还 一组：「成功」、空。
完。

通道_接收 接受 通道：
    若 通道 的 第 4 项 等于 0：
        若 通道 的 第 5 项：
            返还 一组：「关闭」、空。
        完。
        返还 一组：「阻塞」、「缓冲区为空」。
    完。
    值 为 通道 的 第 0 项 的 第 通道 的 第 2 项 项。
    通道 的 第 0 项 的 第 通道 的 第 2 项 项 为 空。
    通道 的 第 2 项 为 (通道 的 第 2 项 加 1) 取余于 通道 的 第 1 项。
    通道 的 第 4 项 为 通道 的 第 4 项 减 1。
    通道 的 第 9 项 为 通道 的 第 9 项 加 1。
    若 做 计数 用 通道 的 第 6 项 大于 0：
        _发送者 为 通道 的 第 6 项 的 第 0 项。
        可变 新等待 为 一组：（空）。
        可变 i 为 1。
        只要 i 小于 做 计数 用 通道 的 第 6 项：
            做 追加 用 新等待、通道 的 第 6 项 的 第 i 项。
            i 为 i 加 1。
        完。
        通道 的 第 6 项 为 新等待。
    完。
    返还 一组：「成功」、值。
完。

通道_关闭 接受 通道：
    若 通道 的 第 5 项：
        返还 假。
    完。
    通道 的 第 5 项 为 真。
    返还 真。
完。

通道_已关闭 接受 通道：
    返还 通道 的 第 5 项。
完。

通道_大小 接受 通道：
    返还 通道 的 第 4 项。
完。

通道_容量 接受 通道：
    返还 通道 的 第 1 项。
完。

通道_为空 接受 通道：
    返还 通道 的 第 4 项 等于 0。
完。

通道_已满 接受 通道：
    返还 通道 的 第 4 项 大于或等于 通道 的 第 1 项。
完。

通道_总发送 接受 通道：
    返还 通道 的 第 8 项。
完。

通道_总接收 接受 通道：
    返还 通道 的 第 9 项。
完。

同步通道_创建 接受 零参：
    返还 一组：空、假、假、负 1、负 1。
完。

同步通道_发送 接受 通道、值、发送方：
    若 通道 的 第 2 项：
        返还 一组：「错误」、「通道已关闭」。
    完。
    若 通道 的 第 1 项：
        返还 一组：「阻塞」、「值未被取走」。
    完。
    通道 的 第 0 项 为 值。
    通道 的 第 1 项 为 真。
    通道 的 第 3 项 为 发送方。
    返还 一组：「成功」、空。
完。

同步通道_接收 接受 通道、接收方：
    若 通道 的 第 1 项 等于 假：
        若 通道 的 第 2 项：
            返还 一组：「关闭」、空。
        完。
        返还 一组：「阻塞」、「无值可取」。
    完。
    值 为 通道 的 第 0 项。
    通道 的 第 0 项 为 空。
    通道 的 第 1 项 为 假。
    通道 的 第 4 项 为 接收方。
    返还 一组：「成功」、值。
完。

同步通道_关闭 接受 通道：
    通道 的 第 2 项 为 真。
完。

广播通道_创建 接受 容量：
    可变 实际容量 为 容量。
    若 实际容量 小于或等于 0：
        实际容量 为 256。
    完。
    可变 环 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 实际容量：
        做 追加 用 环、空。
        i 为 i 加 1。
    完。
    返还 一组：环、实际容量、0、一组：（空）。
完。

广播_订阅 接受 通道、名称：
    订阅者 为 一组：名称、通道 的 第 2 项、0。
    做 追加 用 通道 的 第 3 项、订阅者。
    返还 做 计数 用 通道 的 第 3 项 减 1。
完。

广播_发送 接受 通道、值：
    通道 的 第 0 项 的 第 通道 的 第 2 项 项 为 值。
    通道 的 第 2 项 为 (通道 的 第 2 项 加 1) 取余于 通道 的 第 1 项。
    返还 真。
完。

广播_接收 接受 通道、订阅者ID：
    若 订阅者ID 小于 0 或者 订阅者ID 大于或等于 做 计数 用 通道 的 第 3 项：
        返还 一组：「错误」、「无效订阅者」。
    完。
    订阅者 为 通道 的 第 3 项 的 第 订阅者ID 项。
    若 订阅者 的 第 1 项 等于 通道 的 第 2 项：
        返还 一组：「阻塞」、「无新消息」。
    完。
    值 为 通道 的 第 0 项 的 第 订阅者 的 第 1 项 项。
    订阅者 的 第 1 项 为 (订阅者 的 第 1 项 加 1) 取余于 通道 的 第 1 项。
    订阅者 的 第 2 项 为 订阅者 的 第 2 项 加 1。
    返还 一组：「成功」、值。
完。

广播_订阅者数 接受 通道：
    返还 做 计数 用 通道 的 第 3 项。
完。

广播_落后消息数 接受 通道、订阅者ID：
    订阅者 为 通道 的 第 3 项 的 第 订阅者ID 项。
    可变 差 为 通道 的 第 2 项 减 订阅者 的 第 1 项。
    若 差 小于 0：
        差 为 差 加 通道 的 第 1 项。
    完。
    返还 差。
完。

选择器_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）、一组：（空）。
完。

选择_追加 接受 选择器、通道、名称、优先级：
    做 追加 用 选择器 的 第 0 项、通道。
    做 追加 用 选择器 的 第 1 项、名称。
    做 追加 用 选择器 的 第 2 项、优先级。
完。

选择_轮询 接受 选择器：
    n 为 做 计数 用 选择器 的 第 0 项。
    可变 最高优先 为 负 1。
    可变 最高索引 为 负 1。
    可变 i 为 0。
    只要 i 小于 n：
        ch 为 选择器 的 第 0 项 的 第 i 项。
        若 ch 的 第 4 项 大于 0：
            若 最高优先 等于 负 1 或者 选择器 的 第 2 项 的 第 i 项 大于 最高优先：
                最高优先 为 选择器 的 第 2 项 的 第 i 项。
                最高索引 为 i。
            完。
        完。
        i 为 i 加 1。
    完。
    若 最高索引 大于或等于 0：
        结果 为 做 通道_接收 用 选择器 的 第 0 项 的 第 最高索引 项。
        返还 一组：最高索引、选择器 的 第 1 项 的 第 最高索引 项、结果。
    完。
    返还 一组：负 1、「无就绪」、一组：「阻塞」、空。
完。

选择_公平轮询 接受 选择器、上次索引：
    n 为 做 计数 用 选择器 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 n：
        索引 为 (上次索引 加 1 加 i) 取余于 n。
        ch 为 选择器 的 第 0 项 的 第 索引 项。
        若 ch 的 第 4 项 大于 0：
            结果 为 做 通道_接收 用 ch。
            返还 一组：索引、选择器 的 第 1 项 的 第 索引 项、结果。
        完。
        i 为 i 加 1。
    完。
    返还 一组：负 1、「无就绪」、一组：「阻塞」、空。
完。

扇入_创建 接受 输入通道列表、输出缓冲：
    输出 为 做 通道_创建 用 输出缓冲。
    返还 一组：输入通道列表、输出。
完。

扇入_轮询 接受 扇入：
    输入列表 为 扇入 的 第 0 项。
    输出 为 扇入 的 第 1 项。
    可变 转发数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 输入列表：
        结果 为 做 通道_接收 用 输入列表 的 第 i 项。
        若 结果 的 第 0 项 等于 「成功」：
            做 通道_发送 用 输出、结果 的 第 1 项。
            转发数 为 转发数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 转发数。
完。

扇出_创建 接受 输入通道、输出通道列表：
    返还 一组：输入通道、输出通道列表、0。
完。

扇出_轮询 接受 扇出：
    输入 为 扇出 的 第 0 项。
    输出列表 为 扇出 的 第 1 项。
    结果 为 做 通道_接收 用 输入。
    若 结果 的 第 0 项 不等于 「成功」：
        返还 0。
    完。
    索引 为 扇出 的 第 2 项 取余于 做 计数 用 输出列表。
    做 通道_发送 用 输出列表 的 第 索引 项、结果 的 第 1 项。
    扇出 的 第 2 项 为 扇出 的 第 2 项 加 1。
    返还 1。
完。

本章 收束。
