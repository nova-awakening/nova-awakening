《协程模拟》

引用《..·基础》。

协程_挂起 为 2。

协程_创建 接受 名称：
    返还 一组：协程_就绪、一组：（空）、0、名称。
完。

协程_状态 接受 co：
    返还 co 的 第 0 项。
完。

协程_名称 接受 co：
    返还 co 的 第 3 项。
完。

协程_产出 接受 co、值：
    做 追加 用 co 的 第 1 项、值。
完。

协程_恢复 接受 co：
    若 co 的 第 2 项 大于或等于 做 计数 用 co 的 第 1 项：
        co 的 第 0 项 为 协程_完成。
        返还 空。
    完。
    co 的 第 0 项 为 协程_运行。
    值 为 co 的 第 1 项 的 第 co 的 第 2 项 项。
    co 的 第 2 项 为 co 的 第 2 项 加 1。
    若 co 的 第 2 项 大于或等于 做 计数 用 co 的 第 1 项：
        co 的 第 0 项 为 协程_完成。
    否则：
        co 的 第 0 项 为 协程_挂起。
    完。
    返还 值。
完。

协程_已完成 接受 co：
    返还 co 的 第 0 项 等于 协程_完成。
完。

协程_重置 接受 co：
    co 的 第 0 项 为 协程_就绪。
    co 的 第 2 项 为 0。
完。

协程模拟_通道_创建 接受 容量：
    返还 一组：一组：（空）、容量、0。
完。

协程模拟_通道_发送 接受 ch、值：
    若 ch 的 第 2 项 等于 1：
        返还 0。
    完。
    若 做 计数 用 ch 的 第 0 项 大于或等于 ch 的 第 1 项 并且 ch 的 第 1 项 大于 0：
        返还 0。
    完。
    做 追加 用 ch 的 第 0 项、值。
    返还 1。
完。

协程模拟_通道_接收 接受 ch：
    若 做 计数 用 ch 的 第 0 项 等于 0：
        返还 空。
    完。
    值 为 ch 的 第 0 项 的 第 0 项。
    做 删除 用 ch 的 第 0 项。
    返还 值。
完。

协程模拟_通道_关闭 接受 ch：
    ch 的 第 2 项 为 1。
完。

协程模拟_通道_已关闭 接受 ch：
    返还 ch 的 第 2 项 等于 1。
完。

协程模拟_通道_大小 接受 ch：
    返还 做 计数 用 ch 的 第 0 项。
完。

通道_空 接受 ch：
    返还 做 计数 用 ch 的 第 0 项 等于 0。
完。

协程调度器_创建 接受 零参：
    返还 一组：一组：（空）。
完。

协程调度器_追加 接受 调度、co：
    做 追加 用 调度 的 第 0 项、co。
完。

协程调度器_步进 接受 调度：
    队列 为 调度 的 第 0 项。
    可变 活跃 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 队列：
        co 为 队列 的 第 i 项。
        若 做 协程_已完成 用 co 等于 0：
            做 协程_恢复 用 co。
            活跃 为 活跃 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 活跃。
完。

协程调度器_运行 接受 调度：
    可变 步数 为 0。
    只要 做 协程调度器_步进 用 调度 大于 0：
        步数 为 步数 加 1。
    完。
    返还 步数。
完。

promise_创建 接受 零参：
    返还 一组：0、空、一组：（空）。
完。

promise_完成 接受 p、值：
    若 p 的 第 0 项 不等于 0：
        返还 空。
    完。
    p 的 第 0 项 为 1。
    p 的 第 1 项 为 值。
完。

promise_拒绝 接受 p、原因：
    若 p 的 第 0 项 不等于 0：
        返还 空。
    完。
    p 的 第 0 项 为 2。
    p 的 第 1 项 为 原因。
完。

promise_是完成 接受 p：
    返还 p 的 第 0 项 等于 1。
完。

promise_是拒绝 接受 p：
    返还 p 的 第 0 项 等于 2。
完。

promise_是待定 接受 p：
    返还 p 的 第 0 项 等于 0。
完。

promise_值 接受 p：
    返还 p 的 第 1 项。
完。

信号量_创建 接受 初始值：
    返还 一组：初始值、一组：（空）。
完。

信号量_获取 接受 sem：
    若 sem 的 第 0 项 大于 0：
        sem 的 第 0 项 为 sem 的 第 0 项 减 1。
        返还 1。
    完。
    返还 0。
完。

信号量_释放 接受 sem：
    sem 的 第 0 项 为 sem 的 第 0 项 加 1。
    若 做 计数 用 sem 的 第 1 项 大于 0：
        等待者 为 sem 的 第 1 项 的 第 0 项。
        做 删除 用 sem 的 第 1 项。
        返还 等待者。
    完。
    返还 负 1。
完。

信号量_等待入队 接受 sem、协程ID：
    做 追加 用 sem 的 第 1 项、协程ID。
完。

互斥锁_创建 接受 零参：
    返还 做 信号量_创建 用 1。
完。

互斥锁_加锁 接受 锁：
    返还 做 信号量_获取 用 锁。
完。

互斥锁_解锁 接受 锁：
    做 信号量_释放 用 锁。
完。

协程模拟_条件变量_创建 接受 零参：
    返还 一组：一组：（空）、0。
完。

条件变量_等待 接受 cv、协程ID：
    做 追加 用 cv 的 第 0 项、协程ID。
完。

条件变量_通知一个 接受 cv：
    若 做 计数 用 cv 的 第 0 项 大于 0：
        唤醒 为 cv 的 第 0 项 的 第 0 项。
        做 删除 用 cv 的 第 0 项。
        cv 的 第 1 项 为 cv 的 第 1 项 加 1。
        返还 唤醒。
    完。
    返还 负 1。
完。

条件变量_通知全部 接受 cv：
    可变 唤醒列表 为 一组：（空）。
    只要 做 计数 用 cv 的 第 0 项 大于 0：
        做 追加 用 唤醒列表、cv 的 第 0 项 的 第 0 项。
        做 删除 用 cv 的 第 0 项。
    完。
    cv 的 第 1 项 为 cv 的 第 1 项 加 做 计数 用 唤醒列表。
    返还 唤醒列表。
完。

协程模拟_读写锁_创建 接受 零参：
    返还 一组：0、0、一组：（空）、一组：（空）。
完。

读写锁_读加锁 接受 锁：
    若 锁 的 第 1 项 等于 0：
        锁 的 第 0 项 为 锁 的 第 0 项 加 1。
        返还 1。
    完。
    返还 0。
完。

读写锁_读解锁 接受 锁：
    锁 的 第 0 项 为 锁 的 第 0 项 减 1。
    若 锁 的 第 0 项 等于 0 并且 做 计数 用 锁 的 第 3 项 大于 0：
        写者 为 锁 的 第 3 项 的 第 0 项。
        做 删除 用 锁 的 第 3 项。
        锁 的 第 1 项 为 1。
        返还 写者。
    完。
    返还 负 1。
完。

读写锁_写加锁 接受 锁：
    若 锁 的 第 0 项 等于 0 并且 锁 的 第 1 项 等于 0：
        锁 的 第 1 项 为 1。
        返还 1。
    完。
    返还 0。
完。

读写锁_写解锁 接受 锁：
    锁 的 第 1 项 为 0。
    若 做 计数 用 锁 的 第 3 项 大于 0：
        写者 为 锁 的 第 3 项 的 第 0 项。
        做 删除 用 锁 的 第 3 项。
        锁 的 第 1 项 为 1。
        返还 一组：写者。
    完。
    可变 唤醒 为 一组：（空）。
    只要 做 计数 用 锁 的 第 2 项 大于 0：
        做 追加 用 唤醒、锁 的 第 2 项 的 第 0 项。
        做 删除 用 锁 的 第 2 项。
        锁 的 第 0 项 为 锁 的 第 0 项 加 1。
    完。
    返还 唤醒。
完。

优先_高 为 0。

优先_中 为 1。

优先_低 为 2。

优先级协程_创建 接受 名称、优先级：
    返还 一组：协程_就绪、一组：（空）、0、名称、优先级。
完。

优先级调度器_创建 接受 零参：
    返还 一组：一组：一组：（空）、一组：（空）、一组：（空）、0、0、100。
完。

优先级调度器_追加 接受 调度、co：
    可变 优先 为 co 的 第 4 项。
    若 优先 小于 0：
        优先 为 0。
    完。
    若 优先 大于 2：
        优先 为 2。
    完。
    做 追加 用 调度 的 第 0 项 的 第 优先 项、co。
完。

优先级调度器_选择 接受 调度：
    队列组 为 调度 的 第 0 项。
    可变 p 为 0。
    只要 p 小于 3：
        队列 为 队列组 的 第 p 项。
        可变 i 为 0。
        只要 i 小于 做 计数 用 队列：
            co 为 队列 的 第 i 项。
            若 co 的 第 0 项 不等于 协程_完成：
                若 co 的 第 6 项 大于 调度 的 第 3 项 乘 5：
                    co 的 第 6 项 为 0。
                    返还 co。
                完。
                若 p 等于 0 或者 co 的 第 6 项 大于 调度 的 第 3 项 乘 (p 加 1)：
                    返还 co。
                完。
            完。
            i 为 i 加 1。
        完。
        p 为 p 加 1。
    完。
    p 为 0。
    只要 p 小于 3：
        可变 i 为 0。
        只要 i 小于 做 计数 用 队列组 的 第 p 项：
            若 队列组 的 第 p 项 的 第 i 项 的 第 0 项 不等于 协程_完成：
                返还 队列组 的 第 p 项 的 第 i 项。
            完。
            i 为 i 加 1。
        完。
        p 为 p 加 1。
    完。
    返还 空。
完。

优先级调度器_步进 接受 调度：
    co 为 做 优先级调度器_选择 用 调度。
    若 co 等于 空：
        返还 0。
    完。
    做 协程_恢复 用 co。
    co 的 第 5 项 为 co 的 第 5 项 加 1。
    调度 的 第 1 项 为 调度 的 第 1 项 加 1。
    调度 的 第 2 项 为 调度 的 第 2 项 加 1。
    队列组 为 调度 的 第 0 项。
    可变 p 为 0。
    只要 p 小于 3：
        可变 i 为 0。
        只要 i 小于 做 计数 用 队列组 的 第 p 项：
            其他 为 队列组 的 第 p 项 的 第 i 项。
            若 其他 不等于 co 并且 其他 的 第 0 项 不等于 协程_完成：
                其他 的 第 6 项 为 其他 的 第 6 项 加 1。
            完。
            i 为 i 加 1。
        完。
        p 为 p 加 1。
    完。
    返还 1。
完。

优先级调度器_运行 接受 调度：
    可变 步数 为 0。
    只要 做 优先级调度器_步进 用 调度 大于 0：
        步数 为 步数 加 1。
    完。
    返还 步数。
完。

工作窃取_创建 接受 工作线程数：
    可变 线程队列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 工作线程数：
        做 追加 用 线程队列、一组：（空）。
        i 为 i 加 1。
    完。
    返还 一组：线程队列、工作线程数、0。
完。

工作窃取_提交 接受 ws、线程ID、任务：
    若 线程ID 大于或等于 0 并且 线程ID 小于 ws 的 第 1 项：
        做 追加 用 ws 的 第 0 项 的 第 线程ID 项、任务。
    完。
完。

工作窃取_执行 接受 ws、线程ID：
    若 线程ID 小于 0 或者 线程ID 大于或等于 ws 的 第 1 项：
        返还 空。
    完。
    我的队列 为 ws 的 第 0 项 的 第 线程ID 项。
    若 做 计数 用 我的队列 大于 0：
        最后 为 做 计数 用 我的队列 减 1。
        任务 为 我的队列 的 第 最后 项。
        做 删除 用 我的队列、最后。
        返还 任务。
    完。
    可变 目标 为 (线程ID 加 1) 取余于 ws 的 第 1 项。
    可变 尝试数 为 0。
    只要 尝试数 小于 ws 的 第 1 项 减 1：
        目标队列 为 ws 的 第 0 项 的 第 目标 项。
        若 做 计数 用 目标队列 大于 1：
            偷来的 为 目标队列 的 第 0 项。
            做 删除 用 目标队列。
            ws 的 第 2 项 为 ws 的 第 2 项 加 1。
            返还 偷来的。
        完。
        目标 为 (目标 加 1) 取余于 ws 的 第 1 项。
        尝试数 为 尝试数 加 1。
    完。
    返还 空。
完。

屏障_创建 接受 参与者数：
    返还 一组：参与者数、0、一组：（空）。
完。

屏障_到达 接受 屏障、协程ID：
    屏障 的 第 1 项 为 屏障 的 第 1 项 加 1。
    若 屏障 的 第 1 项 大于或等于 屏障 的 第 0 项：
        可变 释放 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 屏障 的 第 2 项：
            做 追加 用 释放、屏障 的 第 2 项 的 第 i 项。
            i 为 i 加 1。
        完。
        屏障 的 第 1 项 为 0。
        屏障 的 第 2 项 为 一组：（空）。
        返还 释放。
    完。
    做 追加 用 屏障 的 第 2 项、协程ID。
    返还 一组：（空）。
完。

future_创建 接受 零参：
    返还 一组：0、空、一组：（空）。
完。

future_完成 接受 f、值：
    f 的 第 0 项 为 1。
    f 的 第 1 项 为 值。
完。

future_获取 接受 f：
    若 f 的 第 0 项 等于 1：
        返还 f 的 第 1 项。
    完。
    返还 空。
完。

future_已就绪 接受 f：
    返还 f 的 第 0 项 等于 1。
完。

future_等待全部 接受 列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 的 第 0 项 不等于 1：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

future_等待任一 接受 列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 的 第 0 项 等于 1：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

可变 _协程_创建数 为 0。

可变 _协程_切换数 为 0。

可变 _协程_完成数 为 0。

协程模拟_协程_记录创建 接受 零参：
    _协程_创建数 为 _协程_创建数 加 1。
完。

协程模拟_协程_记录切换 接受 零参：
    _协程_切换数 为 _协程_切换数 加 1。
完。

协程_记录完成 接受 零参：
    _协程_完成数 为 _协程_完成数 加 1。
完。

协程_效率评估 接受 零参：
    若 _协程_创建数 等于 0：
        返还 「无协程活动」。
    完。
    完成率 为 _协程_完成数 乘 100 除以 _协程_创建数。
    若 完成率 大于 90：
        返还 「高效: 大部分协程正常完成」。
    完。
    若 完成率 大于 50：
        返还 「中等: 部分协程未完成」。
    完。
    返还 「低效: 大量协程未完成(可能泄漏)」。
完。

协程_切换密度 接受 零参：
    若 _协程_创建数 等于 0：
        返还 0。
    完。
    返还 _协程_切换数 除以 _协程_创建数。
完。

协程_统计 接受 零参：
    返还 一组：_协程_创建数、_协程_切换数、_协程_完成数。
完。

协程_健康报告 接受 零参：
    返还 一组：做 协程_统计、做 协程_效率评估、做 协程_切换密度。
完。

本章 收束。
