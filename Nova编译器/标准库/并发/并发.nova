《并发》

引用《..·向量计算·向量原语》。

引用《..·基础》。

引用《..·系统·硬件库》。

引用《..·数学·工具》。

用。

「../基础.nova」。

用。

「../数据结构/数据结构.nova」。

可变 __并发_tick 为 0。

可变 NOGL_调度次数 为 0。

NOGL_调度_固定 为 0。

NOGL_调度_衰减 为 1。

NOGL_调度_余弦退火 为 2。

NOGL_调度_周期 为 3。

原子_创建 接受 初始值：
    返还 一组：初始值。
完。

原子_读 接受 a：
    返还 a 的 第 0 项。
完。

原子_写 接受 a、值：
    a 的 第 0 项 为 值。
完。

并发_原子_加 接受 a、n：
    旧值 为 a 的 第 0 项。
    a 的 第 0 项 为 a 的 第 0 项 加 n。
    返还 旧值。
完。

原子_减 接受 a、n：
    返还 做 并发_原子_加 用 a、0 减 n。
完。

原子_递增 接受 a：
    返还 做 并发_原子_加 用 a、1。
完。

原子_递减 接受 a：
    返还 做 并发_原子_加 用 a、负 1。
完。

原子_CAS 接受 a、期望、新值：
    若 a 的 第 0 项 等于 期望：
        a 的 第 0 项 为 新值。
        返还 真。
    完。
    返还 假。
完。

原子_交换 接受 a、新值：
    旧值 为 a 的 第 0 项。
    a 的 第 0 项 为 新值。
    返还 旧值。
完。

并发_自旋锁_创建 接受 零参：
    返还 一组：0、负 1。
完。

自旋锁_锁定 接受 锁：
    若 锁 的 第 0 项 等于 1：
        返还 假。
    完。
    锁 的 第 0 项 为 1。
    返还 真。
完。

自旋锁_解锁 接受 锁：
    锁 的 第 0 项 为 0。
    锁 的 第 1 项 为 负 1。
完。

自旋锁_尝试 接受 锁：
    若 锁 的 第 0 项 等于 0：
        锁 的 第 0 项 为 1。
        返还 真。
    完。
    返还 假。
完。

自旋锁_已锁 接受 锁：
    返还 锁 的 第 0 项 等于 1。
完。

并发_读写锁_创建 接受 零参：
    返还 一组：0。
完。

读写锁_读锁 接受 锁：
    若 锁 的 第 1 项 等于 0 并且 锁 的 第 2 项 等于 0：
        锁 的 第 0 项 为 锁 的 第 0 项 加 1。
        返还 真。
    完。
    返还 假。
完。

并发_读写锁_读解锁 接受 锁：
    若 锁 的 第 0 项 大于 0：
        锁 的 第 0 项 为 锁 的 第 0 项 减 1。
    完。
完。

读写锁_写锁 接受 锁：
    锁 的 第 2 项 为 锁 的 第 2 项 加 1。
    若 锁 的 第 1 项 等于 0 并且 锁 的 第 0 项 等于 0：
        锁 的 第 1 项 为 1。
        锁 的 第 2 项 为 锁 的 第 2 项 减 1。
        返还 真。
    完。
    锁 的 第 2 项 为 锁 的 第 2 项 减 1。
    返还 假。
完。

并发_读写锁_写解锁 接受 锁：
    锁 的 第 1 项 为 0。
完。

并发_互斥锁_创建 接受 零参：
    返还 一组：0、0、负 1。
完。

互斥锁_锁定 接受 m、线程id：
    做 互斥锁_确保计数 用 m。
    若 m 的 第 0 项 等于 0：
        m 的 第 0 项 为 1。
        m 的 第 1 项 为 1。
        m 的 第 2 项 为 线程id。
        返还 真。
    完。
    若 m 的 第 2 项 等于 线程id：
        m 的 第 1 项 为 m 的 第 1 项 加 1。
        返还 真。
    完。
    返还 假。
完。

并发_互斥锁_解锁 接受 m、线程id：
    若 m 的 第 2 项 不等于 线程id：
        返还 假。
    完。
    m 的 第 1 项 为 m 的 第 1 项 减 1。
    若 m 的 第 1 项 等于 0：
        m 的 第 0 项 为 0。
        m 的 第 2 项 为 负 1。
    完。
    返还 真。
完。

并发_信号量_创建 接受 初始、最大：
    返还 一组：初始、最大。
完。

并发_信号量_获取 接受 sem：
    若 sem 的 第 0 项 大于 0：
        sem 的 第 0 项 为 sem 的 第 0 项 减 1。
        返还 真。
    完。
    返还 假。
完。

并发_信号量_释放 接受 sem：
    若 sem 的 第 0 项 小于 sem 的 第 1 项：
        sem 的 第 0 项 为 sem 的 第 0 项 加 1。
        返还 真。
    完。
    返还 假。
完。

信号量_值 接受 sem：
    返还 sem 的 第 0 项。
完。

无锁队列_创建 接受 容量：
    可变 数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数组、0。
        i 为 i 加 1。
    完。
    返还 一组：数组、容量、0、0。
完。

无锁队列_入队 接受 q、值：
    若 q 的 第 4 项 大于或等于 q 的 第 1 项：
        返还 假。
    完。
    q 的 第 0 项 的 第 q 的 第 3 项 项 为 值。
    q 的 第 3 项 为 (q 的 第 3 项 加 1) 取余于 q 的 第 1 项。
    q 的 第 4 项 为 q 的 第 4 项 加 1。
    返还 真。
完。

无锁队列_出队 接受 q：
    若 q 的 第 4 项 等于 0：
        返还 空。
    完。
    v 为 q 的 第 0 项 的 第 q 的 第 2 项 项。
    q 的 第 2 项 为 (q 的 第 2 项 加 1) 取余于 q 的 第 1 项。
    q 的 第 4 项 为 q 的 第 4 项 减 1。
    返还 v。
完。

无锁队列_大小 接受 q：
    返还 q 的 第 4 项。
完。

无锁队列_空吗 接受 q：
    返还 q 的 第 4 项 等于 0。
完。

PPromise_创建 接受 零参：
    返还 一组：0、空、空、一组：（空）。
完。

PPromise_完成 接受 p、值：
    若 p 的 第 0 项 不等于 0：
        返还 假。
    完。
    p 的 第 0 项 为 1。
    p 的 第 1 项 为 值。
    返还 真。
完。

PPromise_拒绝 接受 p、错误：
    若 p 的 第 0 项 不等于 0：
        返还 假。
    完。
    p 的 第 0 项 为 2。
    p 的 第 2 项 为 错误。
    返还 真。
完。

PPromise_已完成 接受 p：
    返还 p 的 第 0 项 等于 1。
完。

PPromise_已拒绝 接受 p：
    返还 p 的 第 0 项 等于 2。
完。

PPromise_等待中 接受 p：
    返还 p 的 第 0 项 等于 0。
完。

PPromise_值 接受 p：
    返还 p 的 第 1 项。
完。

PPromise_错误 接受 p：
    返还 p 的 第 2 项。
完。

并发_通道_创建 接受 容量：
    可变 数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数组、0。
        i 为 i 加 1。
    完。
    返还 一组：数组、容量、0。
完。

并发_通道_发送 接受 ch、值：
    若 ch 的 第 5 项 等于 1：
        返还 假。
    完。
    若 ch 的 第 4 项 大于或等于 ch 的 第 1 项：
        返还 假。
    完。
    ch 的 第 0 项 的 第 ch 的 第 3 项 项 为 值。
    ch 的 第 3 项 为 (ch 的 第 3 项 加 1) 取余于 ch 的 第 1 项。
    ch 的 第 4 项 为 ch 的 第 4 项 加 1。
    返还 真。
完。

并发_通道_接收 接受 ch：
    若 ch 的 第 4 项 等于 0：
        返还 空。
    完。
    v 为 ch 的 第 0 项 的 第 ch 的 第 2 项 项。
    ch 的 第 2 项 为 (ch 的 第 2 项 加 1) 取余于 ch 的 第 1 项。
    ch 的 第 4 项 为 ch 的 第 4 项 减 1。
    返还 v。
完。

并发_通道_尝试接收 接受 ch：
    若 ch 的 第 4 项 等于 0：
        返还 一组：空、假。
    完。
    返还 一组：做 并发_通道_接收 用 ch、真。
完。

并发_通道_关闭 接受 ch：
    ch 的 第 5 项 为 1。
完。

并发_通道_已关闭 接受 ch：
    返还 ch 的 第 5 项 等于 1。
完。

并发_通道_大小 接受 ch：
    返还 ch 的 第 4 项。
完。

工作队列_创建 接受 容量：
    可变 数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数组、0。
        i 为 i 加 1。
    完。
    返还 一组：数组、容量。
完。

工作队列_推 接受 wq、任务：
    若 wq 的 第 3 项 减 wq 的 第 2 项 大于或等于 wq 的 第 1 项：
        返还 假。
    完。
    wq 的 第 0 项 的 第 wq 的 第 3 项 取余于 wq 的 第 1 项 项 为 任务。
    wq 的 第 3 项 为 wq 的 第 3 项 加 1。
    返还 真。
完。

工作队列_弹 接受 wq：
    若 wq 的 第 3 项 小于或等于 wq 的 第 2 项：
        返还 空。
    完。
    wq 的 第 3 项 为 wq 的 第 3 项 减 1。
    返还 wq 的 第 0 项 的 第 wq 的 第 3 项 取余于 wq 的 第 1 项 项。
完。

工作队列_窃取 接受 wq：
    若 wq 的 第 2 项 大于或等于 wq 的 第 3 项：
        返还 空。
    完。
    v 为 wq 的 第 0 项 的 第 wq 的 第 2 项 取余于 wq 的 第 1 项 项。
    wq 的 第 2 项 为 wq 的 第 2 项 加 1。
    返还 v。
完。

工作队列_大小 接受 wq：
    返还 wq 的 第 3 项 减 wq 的 第 2 项。
完。

并发_线程池_创建 接受 工作者数、队列大小：
    可变 数组 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 队列大小：
        做 追加 用 数组、0。
        i 为 i 加 1。
    完。
    返还 一组：一组：数组、队列大小、0、0、工作者数、0。
完。

并发_线程池_提交 接受 pool、任务：
    返还 做 无锁队列_入队 用 pool 的 第 0 项、任务。
完。

线程池_执行一个 接受 pool：
    任务 为 做 无锁队列_出队 用 pool 的 第 0 项。
    若 任务 等于 空：
        返还 假。
    完。
    pool 的 第 2 项 为 pool 的 第 2 项 加 1。
    返还 真。
完。

并发_线程池_待处理 接受 pool：
    返还 做 无锁队列_大小 用 pool 的 第 0 项。
完。

线程池_已完成 接受 pool：
    返还 pool 的 第 2 项。
完。

并发_屏障_创建 接受 参与者数：
    返还 一组：参与者数。
完。

屏障_等待 接受 b：
    b 的 第 1 项 为 b 的 第 1 项 加 1。
    若 b 的 第 1 项 大于或等于 b 的 第 0 项：
        b 的 第 1 项 为 0。
        b 的 第 2 项 为 b 的 第 2 项 加 1。
        返还 真。
    完。
    返还 假。
完。

屏障_代 接受 b：
    返还 b 的 第 2 项。
完。

并发_线程池_活跃数 接受 pool：
    返还 pool 的 第 1 项 减 做 无锁队列_大小 用 pool 的 第 0 项 减 pool 的 第 2 项。
完。

通道_排空 接受 ch：
    可变 结果 为 一组：（空）。
    只要 做 并发_通道_大小 用 ch 大于 0：
        做 追加 用 结果、做 并发_通道_接收 用 ch。
    完。
    返还 结果。
完。

fFuture_值 接受 f：
    返还 f 的 第 1 项。
完。

并发_future_创建 接受 零参：
    返还 一组：假、空、空。
完。

future_失败 接受 f、错误：
    f 的 第 0 项 为 真。
    f 的 第 2 项 为 错误。
完。

并发_future_完成 接受 f、值：
    f 的 第 0 项 为 真。
    f 的 第 1 项 为 值。
完。

future_就绪 接受 f：
    返还 f 的 第 0 项。
完。

future_错误 接受 f：
    返还 f 的 第 2 项。
完。

互斥锁_已锁 接受 mutex：
    返还 mutex 的 第 0 项。
完。

互斥锁_确保计数 接受 mutex：
    若 做 计数 用 mutex 小于 3：
        做 追加 用 mutex、0。
    完。
完。

任务池_创建 接受 最大并发：
    返还 一组：一组：（空）、最大并发。
完。

任务池_执行 接受 池：
    任务列表 为 池 的 第 0 项。
    可变 完成数 为 0。
    可变 i 为 0。
    可变 上限 为 池 的 第 1 项。
    若 上限 小于或等于 0：
        上限 为 1。
    完。
    只要 i 小于 做 计数 用 任务列表 并且 完成数 小于 上限：
        任务 为 任务列表 的 第 i 项。
        若 任务 的 第 1 项 等于 假：
            任务 的 第 2 项 为 做 任务 的 第 0 项。
            任务 的 第 1 项 为 真。
            完成数 为 完成数 加 1。
        完。
        i 为 i 加 1。
    完。
    池 的 第 2 项 为 池 的 第 2 项 加 完成数。
    返还 完成数。
完。

任务池_提交 接受 池、任务函数：
    做 追加 用 池 的 第 0 项、一组：任务函数、假、空。
    池 的 第 3 项 为 池 的 第 3 项 加 1。
完。

任务池_结果 接受 池：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 池 的 第 0 项：
        若 池 的 第 0 项 的 第 i 项 的 第 1 项：
            做 追加 用 结果、池 的 第 0 项 的 第 i 项 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

并发_协程_创建 接受 id、任务、优先级：
    返还 一组：id、协程_就绪、任务、优先级、空。
完。

并发_当前tick 接受 零参：
    t 为 做 时间戳。
    若 t 等于 空 或者 t 小于或等于 0：
        __并发_tick 为 __并发_tick 加 1。
        返还 __并发_tick。
    完。
    返还 t。
完。

消息_发送 接受 队列、发送者、接收者、类型、数据：
    消息 为 一组：发送者、接收者、类型、数据、做 并发_当前tick。
    做 追加 用 队列 的 第 0 项、消息。
    队列 的 第 1 项 为 队列 的 第 1 项 加 1。
完。

消息_大小 接受 队列：
    可变 活跃 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 队列 的 第 0 项：
        若 队列 的 第 0 项 的 第 i 项 不等于 空：
            活跃 为 活跃 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 活跃。
完。

消息_按类型接收 接受 队列、类型：
    消息列表 为 队列 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 消息列表：
        m 为 消息列表 的 第 i 项。
        若 m 不等于 空 并且 m 的 第 2 项 等于 类型：
            消息列表 的 第 i 项 为 空。
            返还 m。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

消息_接收 接受 队列、接收者：
    消息列表 为 队列 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 消息列表：
        m 为 消息列表 的 第 i 项。
        若 m 不等于 空 并且 m 的 第 1 项 等于 接收者：
            消息列表 的 第 i 项 为 空。
            返还 m。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

消息队列_创建 接受 零参：
    返还 一组：一组：（空）、0。
完。

并发_管道_创建 接受 零参：
    返还 一组：一组：（空）。
完。

管道_执行 接受 管道、输入：
    阶段 为 管道 的 第 0 项。
    可变 数据 为 输入。
    可变 i 为 0。
    只要 i 小于 做 计数 用 阶段：
        数据 为 做 阶段 的 第 i 项 用 数据。
        i 为 i 加 1。
    完。
    返还 数据。
完。

管道_批量 接受 管道、输入列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 输入列表：
        做 追加 用 结果、做 管道_执行 用 管道、输入列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

管道_添加阶段 接受 管道、处理函数：
    做 追加 用 管道 的 第 0 项、处理函数。
完。

调度器_一轮 接受 调度：
    协程列表 为 调度 的 第 0 项。
    n 为 做 计数 用 协程列表。
    可变 执行了 为 假。
    可变 最高i 为 负 1。
    可变 最高优先 为 负 1。
    可变 起点 为 0。
    若 做 计数 用 调度 大于或等于 4：
        起点 为 调度 的 第 3 项 取余于 n。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        索引 为 (起点 加 i) 取余于 n。
        c 为 协程列表 的 第 索引 项。
        若 c 的 第 1 项 等于 协程_就绪 并且 c 的 第 3 项 大于 最高优先：
            最高i 为 索引。
            最高优先 为 c 的 第 3 项。
        完。
        i 为 i 加 1。
    完。
    若 最高i 大于或等于 0：
        c 为 协程列表 的 第 最高i 项。
        c 的 第 1 项 为 协程_运行。
        c 的 第 4 项 为 做 c 的 第 2 项。
        c 的 第 1 项 为 协程_完成。
        调度 的 第 2 项 为 调度 的 第 2 项 加 1。
        若 做 计数 用 调度 大于或等于 4：
            调度 的 第 3 项 为 (最高i 加 1) 取余于 n。
        完。
        执行了 为 真。
    完。
    返还 执行了。
完。

调度器_全部完成 接受 sched：
    可变 完成 为 真。
    可变 i 为 0。
    只要 i 小于 做 计数 用 sched 的 第 0 项：
        c 为 sched 的 第 0 项 的 第 i 项。
        若 c 的 第 1 项 不等于 协程_完成：
            完成 为 假。
            退出。
        完。
        i 为 i 加 1。
    完。
    返还 完成。
完。

调度器_步进 接受 sched：
    若 做 调度器_一轮 用 sched：
        返还 0。
    完。
    返还 负 1。
完。

调度器_创建2 接受 零参：
    返还 一组：一组：（空）、0、0。
完。

调度器_步进_v2 接受 s：
    NOGL_调度次数 为 NOGL_调度次数 加 1。
    s 的 当前步 为 s 的 当前步 加 1。
    若 s 的 策略 等于 NOGL_调度_固定：
        返还 s 的 初始LR。
    完。
    若 s 的 策略 等于 NOGL_调度_衰减：
        衰减_t 为 做 Q快速幂整数 用 s 的 衰减因子、s 的 当前步。
        可变 lr 为 做 Q乘 用 s 的 初始LR、衰减_t。
        若 lr 小于 s 的 最小LR：
            lr 为 s 的 最小LR。
        完。
        返还 lr。
    完。
    若 s 的 策略 等于 NOGL_调度_余弦退火：
        t 为 s 的 当前步 取余于 s 的 周期步数 乘 定点_一 除以 s 的 周期步数。
        FP 为 10000。
        FP_PI 为 31416。
        pt 为 t 乘 FP_PI 除以 FP。
        pt2 为 pt 乘 pt 除以 FP。
        pt4 为 pt2 乘 pt2 除以 FP。
        余弦值 为 (FP 减 pt2 除以 2 加 pt4 除以 24) 乘 定点_一 除以 FP。
        range 为 s 的 初始LR 减 s 的 最小LR。
        返还 s 的 最小LR 加 做 Q乘 用 range、(定点_一 加 余弦值) 除以 2。
    完。
    若 s 的 策略 等于 NOGL_调度_周期：
        一半 为 s 的 周期步数 除以 2。
        位置 为 s 的 当前步 取余于 s 的 周期步数。
        若 位置 小于 一半：
            返还 s 的 最小LR 加 (s 的 初始LR 减 s 的 最小LR) 乘 位置 除以 一半。
        完。
        若 一半 等于 0：
            返还 0。
        完。
        返还 s 的 初始LR 减 (s 的 初始LR 减 s 的 最小LR) 乘 (位置 减 一半) 除以 一半。
    完。
    返还 s 的 初始LR。
完。

调度器_添加协程 接受 调度、协程：
    若 做 计数 用 调度 小于 4：
        做 追加 用 调度、0。
    完。
    做 追加 用 调度 的 第 0 项、协程。
    调度 的 第 1 项 为 调度 的 第 1 项 加 1。
完。

调度器_运行到完成 接受 sched：
    可变 安全计数 为 0。
    可变 总步 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 sched 的 第 0 项：
        若 sched 的 第 0 项 的 第 i 项 的 第 2 项 大于 0：
            总步 为 总步 加 sched 的 第 0 项 的 第 i 项 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
    可变 安全上限 为 总步 加 做 计数 用 sched 的 第 0 项 乘 2。
    只要 做 调度器_全部完成 用 sched 等于 假 并且 安全计数 小于 安全上限：
        若 做 调度器_步进 用 sched 等于 负 1：
            返还。
        完。
        安全计数 为 安全计数 加 1。
    完。
完。

本章 收束。
