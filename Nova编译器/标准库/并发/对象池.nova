《对象池》

引用《..·基础》。

引用《..·系统·内存池》。

对象池_对象池_创建 接受 最大容量：
    返还 一组：一组：（空）、一组：（空）、最大容量、0。
完。

对象池_对象池_获取 接受 池：
    空闲 为 池 的 第 0 项。
    若 做 计数 用 空闲 大于 0：
        对象 为 空闲 的 第 做 计数 用 空闲 减 1 项。
        做 删除 用 空闲、做 计数 用 空闲 减 1。
        做 追加 用 池 的 第 1 项、对象。
        返还 对象。
    完。
    若 池 的 第 3 项 小于 池 的 第 2 项：
        对象 为 一组：池 的 第 3 项、0、空。
        池 的 第 3 项 为 池 的 第 3 项 加 1。
        做 追加 用 池 的 第 1 项、对象。
        返还 对象。
    完。
    返还 空。
完。

对象池_对象池_归还 接受 池、对象：
    活跃 为 池 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 活跃：
        若 活跃 的 第 i 项 等于 对象：
            做 删除 用 活跃、i。
            做 追加 用 池 的 第 0 项、对象。
            返还 1。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

对象池_空闲数 接受 池：
    返还 做 计数 用 池 的 第 0 项。
完。

对象池_活跃数 接受 池：
    返还 做 计数 用 池 的 第 1 项。
完。

对象池_总数 接受 池：
    返还 池 的 第 3 项。
完。

对象池_环形_创建 接受 容量：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 一组：数据、容量、0、0。
完。

对象池_环形_写入 接受 缓冲、值：
    若 缓冲 的 第 4 项 大于或等于 缓冲 的 第 1 项：
        返还 0。
    完。
    缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 项 为 值。
    缓冲 的 第 3 项 为 (缓冲 的 第 3 项 加 1) 取余于 缓冲 的 第 1 项。
    缓冲 的 第 4 项 为 缓冲 的 第 4 项 加 1。
    返还 1。
完。

池_环形_读取 接受 缓冲：
    若 缓冲 的 第 4 项 等于 0：
        返还 空。
    完。
    值 为 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 项。
    缓冲 的 第 2 项 为 (缓冲 的 第 2 项 加 1) 取余于 缓冲 的 第 1 项。
    缓冲 的 第 4 项 为 缓冲 的 第 4 项 减 1。
    返还 值。
完。

对象池_环形_大小 接受 缓冲：
    返还 缓冲 的 第 4 项。
完。

对象池_环形_容量 接受 缓冲：
    返还 缓冲 的 第 1 项。
完。

对象池_环形_空 接受 缓冲：
    返还 缓冲 的 第 4 项 等于 0。
完。

对象池_环形_满 接受 缓冲：
    返还 缓冲 的 第 4 项 大于或等于 缓冲 的 第 1 项。
完。

对象池_环形_查看 接受 缓冲：
    若 缓冲 的 第 4 项 等于 0：
        返还 空。
    完。
    返还 缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 项。
完。

环形_清空 接受 缓冲：
    缓冲 的 第 2 项 为 0。
    缓冲 的 第 3 项 为 0。
    缓冲 的 第 4 项 为 0。
完。

统计池_创建 接受 最大容量：
    返还 一组：一组：（空）、一组：（空）、最大容量、0、0、0、0、0、0。
完。

统计池_获取 接受 池：
    池 的 第 4 项 为 池 的 第 4 项 加 1。
    空闲 为 池 的 第 0 项。
    若 做 计数 用 空闲 大于 0：
        对象 为 空闲 的 第 做 计数 用 空闲 减 1 项。
        做 删除 用 空闲、做 计数 用 空闲 减 1。
        对象 的 第 1 项 为 池 的 第 8 项。
        做 追加 用 池 的 第 1 项、对象。
        若 做 计数 用 池 的 第 1 项 大于 池 的 第 6 项：
            池 的 第 6 项 为 做 计数 用 池 的 第 1 项。
        完。
        返还 对象。
    完。
    若 池 的 第 3 项 小于 池 的 第 2 项：
        对象 为 一组：池 的 第 3 项、池 的 第 8 项、空。
        池 的 第 3 项 为 池 的 第 3 项 加 1。
        做 追加 用 池 的 第 1 项、对象。
        若 做 计数 用 池 的 第 1 项 大于 池 的 第 6 项：
            池 的 第 6 项 为 做 计数 用 池 的 第 1 项。
        完。
        返还 对象。
    完。
    池 的 第 7 项 为 池 的 第 7 项 加 1。
    返还 空。
完。

统计池_归还 接受 池、对象：
    池 的 第 5 项 为 池 的 第 5 项 加 1。
    活跃 为 池 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 活跃：
        若 活跃 的 第 i 项 的 第 0 项 等于 对象 的 第 0 项：
            做 删除 用 活跃、i。
            对象 的 第 1 项 为 0。
            做 追加 用 池 的 第 0 项、对象。
            返还 1。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

统计池_报告 接受 池：
    返还 一组：池 的 第 3 项、做 计数 用 池 的 第 0 项、做 计数 用 池 的 第 1 项、池 的 第 4 项、池 的 第 5 项、池 的 第 6 项、池 的 第 7 项、池 的 第 4 项 减 池 的 第 5 项。
完。

统计池_推进时间 接受 池：
    池 的 第 8 项 为 池 的 第 8 项 加 1。
完。

分层池_创建 接受 小容量、中容量、大容量：
    返还 一组：做 对象池_对象池_创建 用 小容量、做 对象池_对象池_创建 用 中容量、做 对象池_对象池_创建 用 大容量、64、256。
完。

分层池_获取 接受 池、大小：
    若 大小 小于或等于 池 的 第 3 项：
        返还 一组：0、做 对象池_对象池_获取 用 池 的 第 0 项。
    完。
    若 大小 小于或等于 池 的 第 4 项：
        返还 一组：1、做 对象池_对象池_获取 用 池 的 第 1 项。
    完。
    返还 一组：2、做 对象池_对象池_获取 用 池 的 第 2 项。
完。

分层池_归还 接受 池、层级、对象：
    若 层级 等于 0：
        返还 做 对象池_对象池_归还 用 池 的 第 0 项、对象。
    完。
    若 层级 等于 1：
        返还 做 对象池_对象池_归还 用 池 的 第 1 项、对象。
    完。
    返还 做 对象池_对象池_归还 用 池 的 第 2 项、对象。
完。

SPSC_创建 接受 容量：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 一组：数据、容量、0、0、0、0。
完。

SPSC_写入 接受 缓冲、值：
    下一写 为 (缓冲 的 第 2 项 加 1) 取余于 缓冲 的 第 1 项。
    若 下一写 等于 缓冲 的 第 3 项：
        返还 0。
    完。
    缓冲 的 第 0 项 的 第 缓冲 的 第 2 项 项 为 值。
    缓冲 的 第 2 项 为 下一写。
    缓冲 的 第 4 项 为 缓冲 的 第 4 项 加 1。
    返还 1。
完。

SPSC_读取 接受 缓冲：
    若 缓冲 的 第 3 项 等于 缓冲 的 第 2 项：
        返还 空。
    完。
    值 为 缓冲 的 第 0 项 的 第 缓冲 的 第 3 项 项。
    缓冲 的 第 3 项 为 (缓冲 的 第 3 项 加 1) 取余于 缓冲 的 第 1 项。
    缓冲 的 第 5 项 为 缓冲 的 第 5 项 加 1。
    返还 值。
完。

SPSC_可写 接受 缓冲：
    返还 (缓冲 的 第 2 项 加 1) 取余于 缓冲 的 第 1 项 不等于 缓冲 的 第 3 项。
完。

SPSC_可读 接受 缓冲：
    返还 缓冲 的 第 3 项 不等于 缓冲 的 第 2 项。
完。

SPSC_吞吐量 接受 缓冲：
    返还 一组：缓冲 的 第 4 项、缓冲 的 第 5 项。
完。

本章 收束。
