《自动微分》

引用《..·基础》。

引用《..·算法·数学扩展》。

可变 图_节点列表 为 一组：（空）。

可变 图_下一ID 为 0。

图_重置 接受 零参：
    图_节点列表 为 一组：（空）。
    图_下一ID 为 0。
完。

图_查节点 接受 id：
    若 id 大于或等于 0 并且 id 小于 做 计数 用 图_节点列表：
        返还 图_节点列表 的 第 id 项。
    完。
    返还 空。
完。

变量 接受 值：
    id 为 图_下一ID。
    图_下一ID 为 图_下一ID 加 1。
    做 追加 用 图_节点列表、一组：id、0、值、0、负 1、负 1、1。
    返还 id。
完。

常量值 接受 值：
    id 为 图_下一ID。
    图_下一ID 为 图_下一ID 加 1。
    做 追加 用 图_节点列表、一组：id、0、值、0、负 1、负 1、0。
    返还 id。
完。

图_值 接受 id：
    返还 图_节点列表 的 第 id 项 的 第 2 项。
完。

图_梯度 接受 id：
    返还 图_节点列表 的 第 id 项 的 第 3 项。
完。

图_设梯度 接受 id、g：
    图_节点列表 的 第 id 项 的 第 3 项 为 g。
完。

图_加 接受 a、b：
    id 为 图_下一ID。
    图_下一ID 为 图_下一ID 加 1。
    做 追加 用 图_节点列表、一组：id、1、做 图_值 用 a 加 做 图_值 用 b、0、a、b、1。
    返还 id。
完。

图_减 接受 a、b：
    id 为 图_下一ID。
    图_下一ID 为 图_下一ID 加 1。
    做 追加 用 图_节点列表、一组：id、2、做 图_值 用 a 减 做 图_值 用 b、0、a、b、1。
    返还 id。
完。

图_乘 接受 a、b：
    id 为 图_下一ID。
    图_下一ID 为 图_下一ID 加 1。
    做 追加 用 图_节点列表、一组：id、3、做 图_值 用 a 乘 做 图_值 用 b、0、a、b、1。
    返还 id。
完。

图_修正线性 接受 a：
    v 为 做 图_值 用 a。
    可变 rv 为 v。
    若 v 小于 0：
        rv 为 0。
    完。
    id 为 图_下一ID。
    图_下一ID 为 图_下一ID 加 1。
    做 追加 用 图_节点列表、一组：id、5、rv、0、a、负 1、1。
    返还 id。
完。

图_平方 接受 a：
    v 为 做 图_值 用 a。
    id 为 图_下一ID。
    图_下一ID 为 图_下一ID 加 1。
    做 追加 用 图_节点列表、一组：id、6、v 乘 v、0、a、负 1、1。
    返还 id。
完。

图_取负 接受 a：
    id 为 图_下一ID。
    图_下一ID 为 图_下一ID 加 1。
    做 追加 用 图_节点列表、一组：id、7、0 减 做 图_值 用 a、0、a、负 1、1。
    返还 id。
完。

反向传播 接受 输出id：
    做 图_设梯度 用 输出id、1。
    可变 i 为 输出id。
    只要 i 大于或等于 0：
        n 为 图_节点列表 的 第 i 项。
        op 为 n 的 第 1 项。
        梯度 为 n 的 第 3 项。
        左id 为 n 的 第 4 项。
        右id 为 n 的 第 5 项。
        若 op 等于 1：
            若 左id 大于或等于 0：
                做 图_设梯度 用 左id、做 图_梯度 用 左id 加 梯度。
            完。
            若 右id 大于或等于 0：
                做 图_设梯度 用 右id、做 图_梯度 用 右id 加 梯度。
            完。
        完。
        若 op 等于 2：
            若 左id 大于或等于 0：
                做 图_设梯度 用 左id、做 图_梯度 用 左id 加 梯度。
            完。
            若 右id 大于或等于 0：
                做 图_设梯度 用 右id、做 图_梯度 用 右id 减 梯度。
            完。
        完。
        若 op 等于 3：
            若 左id 大于或等于 0 并且 右id 大于或等于 0：
                做 图_设梯度 用 左id、做 图_梯度 用 左id 加 梯度 乘 做 图_值 用 右id。
                做 图_设梯度 用 右id、做 图_梯度 用 右id 加 梯度 乘 做 图_值 用 左id。
            完。
        完。
        若 op 等于 5：
            若 左id 大于或等于 0：
                若 做 图_值 用 左id 大于 0：
                    做 图_设梯度 用 左id、做 图_梯度 用 左id 加 梯度。
                完。
            完。
        完。
        若 op 等于 6：
            若 左id 大于或等于 0：
                做 图_设梯度 用 左id、做 图_梯度 用 左id 加 梯度 乘 2 乘 做 图_值 用 左id。
            完。
        完。
        若 op 等于 7：
            若 左id 大于或等于 0：
                做 图_设梯度 用 左id、做 图_梯度 用 左id 减 梯度。
            完。
        完。
        i 为 i 减 1。
    完。
完。

梯度清零 接受 零参：
    可变 i 为 0。
    只要 i 小于 做 计数 用 图_节点列表：
        图_节点列表 的 第 i 项 的 第 3 项 为 0。
        i 为 i 加 1。
    完。
完。

自动微分_梯度裁剪_值 接受 参数ids、最大值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数ids：
        id 为 参数ids 的 第 i 项。
        g 为 做 图_梯度 用 id。
        若 g 大于 最大值：
            做 图_设梯度 用 id、最大值。
        完。
        若 g 小于 0 减 最大值：
            做 图_设梯度 用 id、0 减 最大值。
        完。
        i 为 i 加 1。
    完。
完。

梯度范数 接受 参数ids：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数ids：
        g 为 做 图_梯度 用 参数ids 的 第 i 项。
        总和 为 总和 加 g 乘 g。
        i 为 i 加 1。
    完。
    若 总和 等于 0：
        返还 0。
    完。
    返还 做 整数sqrt 用 总和。
完。

随机梯度动量_创建 接受 参数数：
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数数：
        做 追加 用 v、0。
        i 为 i 加 1。
    完。
    返还 v。
完。

随机梯度动量_步 接受 参数ids、速度、学习率、动量：
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数ids：
        id 为 参数ids 的 第 i 项。
        速度 的 第 i 项 为 动量 乘 速度 的 第 i 项 除以 100 加 做 图_梯度 用 id。
        图_节点列表 的 第 id 项 的 第 2 项 为 图_节点列表 的 第 id 项 的 第 2 项 减 学习率 乘 速度 的 第 i 项。
        i 为 i 加 1。
    完。
完。

自适应矩估计_创建 接受 参数数：
    可变 m 为 一组：（空）。
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数数：
        做 追加 用 m、0。
        做 追加 用 v、0。
        i 为 i 加 1。
    完。
    返还 一组：m、v、0。
完。

自适应矩估计_步 接受 参数ids、状态、学习率、beta1、beta2、极小值：
    状态 的 第 2 项 为 状态 的 第 2 项 加 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数ids：
        id 为 参数ids 的 第 i 项。
        g 为 做 图_梯度 用 id。
        状态 的 第 0 项 的 第 i 项 为 (beta1 乘 状态 的 第 0 项 的 第 i 项 加 (100 减 beta1) 乘 g) 除以 100。
        状态 的 第 1 项 的 第 i 项 为 (beta2 乘 状态 的 第 1 项 的 第 i 项 加 (100 减 beta2) 乘 g 乘 g) 除以 100。
        一阶矫正 为 状态 的 第 0 项 的 第 i 项 乘 100 除以 (100 减 beta1)。
        二阶矫正 为 状态 的 第 1 项 的 第 i 项 乘 100 除以 (100 减 beta2)。
        可变 sv 为 做 整数sqrt 用 二阶矫正。
        若 sv 等于 0：
            sv 为 1。
        完。
        图_节点列表 的 第 id 项 的 第 2 项 为 图_节点列表 的 第 id 项 的 第 2 项 减 学习率 乘 一阶矫正 除以 (sv 加 极小值)。
        i 为 i 加 1。
    完。
完。

学习率_预热 接受 目标lr、当前步、预热步数：
    若 当前步 大于或等于 预热步数：
        返还 目标lr。
    完。
    若 预热步数 等于 0：
        返还 0。
    完。
    返还 目标lr 乘 当前步 除以 预热步数。
完。

本章 收束。
