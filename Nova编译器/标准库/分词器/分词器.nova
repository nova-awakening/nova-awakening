《分词器》

引用《..·基础》。

引用《..·..·运行时·字典引擎·字典引擎》。

可变 词表_词 为 一组：（空）。

可变 词表_哈希 为 一组：（空）。

可变 词表_大小 为 0。

词表_重置 接受 零参：
    词表_词 为 一组：（空）。
    词表_哈希 为 一组：（空）。
    词表_大小 为 0。
完。

词_哈希 接受 词：
    可变 h 为 5381。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词：
        h 为 h 乘 33 加 做 编码 用 做 子串 用 词、i、1。
        i 为 i 加 1。
    完。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    返还 h。
完。

词表_追加 接受 词：
    h 为 做 词_哈希 用 词。
    可变 i 为 0。
    只要 i 小于 词表_大小：
        若 词表_哈希 的 第 i 项 等于 h 并且 做 字符串相等 用 词表_词 的 第 i 项、词：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 词表_词、词。
    做 追加 用 词表_哈希、h。
    可变 id 为 词表_大小。
    词表_大小 为 词表_大小 加 1。
    返还 id。
完。

词表_查ID 接受 词：
    h 为 做 词_哈希 用 词。
    可变 i 为 0。
    只要 i 小于 词表_大小：
        若 词表_哈希 的 第 i 项 等于 h 并且 做 字符串相等 用 词表_词 的 第 i 项、词：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

词表_查词 接受 id：
    若 id 大于或等于 0 并且 id 小于 词表_大小：
        返还 词表_词 的 第 id 项。
    完。
    返还 「[UNK]」。
完。

可变 填充标识 为 负 1。

可变 未知标识 为 负 1。

可变 序列开始标识 为 负 1。

可变 序列结束标识 为 负 1。

可变 分类标识 为 负 1。

可变 分隔标识 为 负 1。

初始化特殊标记 接受 零参：
    填充标识 为 做 词表_追加 用 「[PAD]」。
    未知标识 为 做 词表_追加 用 「[UNK]」。
    序列开始标识 为 做 词表_追加 用 「[BOS]」。
    序列结束标识 为 做 词表_追加 用 「[EOS]」。
    分类标识 为 做 词表_追加 用 「[CLS]」。
    分隔标识 为 做 词表_追加 用 「[SEP]」。
完。

分词器_字符分词 接受 文本：
    可变 词元列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        ch 为 做 子串 用 文本、i、1。
        可变 id 为 做 词表_查ID 用 ch。
        若 id 小于 0：
            id 为 做 词表_追加 用 ch。
        完。
        做 追加 用 词元列、id。
        i 为 i 加 1。
    完。
    返还 词元列。
完。

单词分词 接受 文本：
    可变 词元列 为 一组：（空）。
    可变 当前词 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        码 为 做 字符码 用 文本、i。
        若 做 是空白 用 码：
            若 做 计数 用 当前词 大于 0：
                可变 id 为 做 词表_查ID 用 当前词。
                若 id 小于 0：
                    id 为 未知标识。
                完。
                做 追加 用 词元列、id。
                当前词 为 「」。
            完。
        否则若 做 是标点 用 码：
            若 做 计数 用 当前词 大于 0：
                可变 id2 为 做 词表_查ID 用 当前词。
                若 id2 小于 0：
                    id2 为 未知标识。
                完。
                做 追加 用 词元列、id2。
                当前词 为 「」。
            完。
            pch 为 做 子串 用 文本、i、1。
            可变 pid 为 做 词表_查ID 用 pch。
            若 pid 小于 0：
                pid 为 做 词表_追加 用 pch。
            完。
            做 追加 用 词元列、pid。
        否则：
            当前词 为 当前词 加 做 子串 用 文本、i、1。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前词 大于 0：
        可变 id3 为 做 词表_查ID 用 当前词。
        若 id3 小于 0：
            id3 为 未知标识。
        完。
        做 追加 用 词元列、id3。
    完。
    返还 词元列。
完。

解码 接受 token_ids：
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 token_ids：
        词 为 做 词表_查词 用 token_ids 的 第 i 项。
        结果 为 结果 加 词。
        i 为 i 加 1。
    完。
    返还 结果。
完。

填充到长度 接受 token_ids、目标长度：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 目标长度：
        若 i 小于 做 计数 用 token_ids：
            做 追加 用 r、token_ids 的 第 i 项。
        否则：
            做 追加 用 r、填充标识。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

截断到长度 接受 token_ids、最大长度：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    可变 上限 为 最大长度。
    若 上限 大于 做 计数 用 token_ids：
        上限 为 做 计数 用 token_ids。
    完。
    只要 i 小于 上限：
        做 追加 用 r、token_ids 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

注意力掩码 接受 token_ids：
    可变 掩码 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 token_ids：
        若 token_ids 的 第 i 项 等于 填充标识：
            做 追加 用 掩码、0。
        否则：
            做 追加 用 掩码、1。
        完。
        i 为 i 加 1。
    完。
    返还 掩码。
完。

BPE_构建词表 接受 语料列表、目标大小：
    可变 轮次 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 语料列表：
        文本 为 语料列表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 文本：
            做 词表_追加 用 做 子串 用 文本、j、1。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    只要 词表_大小 小于 目标大小 并且 轮次 小于 20：
        可变 对_a 为 一组：（空）。
        可变 对_b 为 一组：（空）。
        可变 对_频 为 一组：（空）。
        i 为 0。
        只要 i 小于 做 计数 用 语料列表：
            t 为 语料列表 的 第 i 项。
            可变 k 为 0。
            只要 k 加 1 小于 做 计数 用 t：
                ca 为 做 子串 用 t、k、1。
                cb 为 做 子串 用 t、k 加 1、1。
                可变 找到2 为 负 1。
                可变 pi 为 0。
                只要 pi 小于 做 计数 用 对_a：
                    若 做 字符串相等 用 对_a 的 第 pi 项、ca 并且 做 字符串相等 用 对_b 的 第 pi 项、cb：
                        找到2 为 pi。
                    完。
                    pi 为 pi 加 1。
                完。
                若 找到2 大于或等于 0：
                    对_频 的 第 找到2 项 为 对_频 的 第 找到2 项 加 1。
                否则：
                    做 追加 用 对_a、ca。
                    做 追加 用 对_b、cb。
                    做 追加 用 对_频、1。
                完。
                k 为 k 加 1。
            完。
            i 为 i 加 1。
        完。
        若 做 计数 用 对_频 等于 0：
            轮次 为 999。
        否则：
            可变 最佳 为 0。
            可变 最佳频 为 对_频 的 第 0 项。
            可变 bi 为 1。
            只要 bi 小于 做 计数 用 对_频：
                若 对_频 的 第 bi 项 大于 最佳频：
                    最佳 为 bi。
                    最佳频 为 对_频 的 第 bi 项。
                完。
                bi 为 bi 加 1。
            完。
            若 最佳频 小于或等于 1：
                轮次 为 999。
            否则：
                新词 为 对_a 的 第 最佳 项 加 对_b 的 第 最佳 项。
                做 词表_追加 用 新词。
            完。
        完。
        轮次 为 轮次 加 1。
    完。
    返还 词表_大小。
完。

词表_覆盖率 接受 文本列表：
    可变 已知 为 0。
    可变 总词 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本列表：
        词元列 为 做 单词分词 用 文本列表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 词元列：
            总词 为 总词 加 1。
            若 词元列 的 第 j 项 不等于 未知标识：
                已知 为 已知 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 总词 等于 0：
        返还 100。
    完。
    返还 已知 乘 100 除以 总词。
完。

是高价值部首字 接受 码点：
    水族_start 为 27700。
    水族_end 为 27820。
    木族_start 为 26408。
    木族_end 为 26550。
    人族_start 为 20154。
    人族_end 为 20280。
    言族_start 为 35328。
    言族_end 为 35450。
    心族_start 为 24515。
    心族_end 为 24640。
    若 码点 大于或等于 水族_start 并且 码点 小于或等于 水族_end：
        返还 真。
    完。
    若 码点 大于或等于 木族_start 并且 码点 小于或等于 木族_end：
        返还 真。
    完。
    若 码点 大于或等于 人族_start 并且 码点 小于或等于 人族_end：
        返还 真。
    完。
    若 码点 大于或等于 言族_start 并且 码点 小于或等于 言族_end：
        返还 真。
    完。
    若 码点 大于或等于 心族_start 并且 码点 小于或等于 心族_end：
        返还 真。
    完。
    返还 假。
完。

部首相似度 接受 码点A、码点B：
    差值 为 码点A 减 码点B。
    若 差值 小于 0：
        差值 为 0 减 差值。
    完。
    若 差值 小于 50：
        返还 90。
    完。
    若 差值 小于 120：
        返还 70。
    完。
    若 差值 小于 300：
        返还 40。
    完。
    返还 10。
完。

部首BPE_合并分数 接受 词A、词B、频率：
    cpA 为 做 编码 用 做 子串 用 词A、0、1。
    cpB 为 做 编码 用 做 子串 用 词B、0、1。
    A是汉字 为 cpA 大于或等于 19968 并且 cpA 小于或等于 40959。
    B是汉字 为 cpB 大于或等于 19968 并且 cpB 小于或等于 40959。
    若 非 A是汉字 或者 非 B是汉字：
        返还 频率。
    完。
    相似度 为 做 部首相似度 用 cpA、cpB。
    可变 高价值加成 为 0。
    若 做 是高价值部首字 用 cpA 或者 做 是高价值部首字 用 cpB：
        高价值加成 为 20。
    完。
    返还 频率 乘 (100 加 相似度 除以 2 加 高价值加成) 除以 100。
完。

部首BPE迭代 接受 词列表、最大词表大小：
    可变 轮次 为 0。
    可变 最大轮 为 最大词表大小。
    若 最大词表大小 大于 4096：
        最大轮 为 4096。
    完。
    只要 轮次 小于 最大轮：
        可变 对_a 为 一组：（空）。
        可变 对_b 为 一组：（空）。
        可变 对_分 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 词列表：
            词元列 为 词列表 的 第 i 项。
            可变 k 为 0。
            只要 k 小于 做 计数 用 词元列 减 1：
                ca 为 做 词表_查词 用 词元列 的 第 k 项。
                cb 为 做 词表_查词 用 词元列 的 第 k 加 1 项。
                分 为 做 部首BPE_合并分数 用 ca、cb、1。
                可变 找到 为 负 1。
                可变 j 为 0。
                只要 j 小于 做 计数 用 对_a：
                    若 做 字符串相等 用 对_a 的 第 j 项、ca 并且 做 字符串相等 用 对_b 的 第 j 项、cb：
                        找到 为 j。
                    完。
                    j 为 j 加 1。
                完。
                若 找到 大于或等于 0：
                    对_分 的 第 找到 项 为 对_分 的 第 找到 项 加 分。
                否则：
                    做 追加 用 对_a、ca。
                    做 追加 用 对_b、cb。
                    做 追加 用 对_分、分。
                完。
                k 为 k 加 1。
            完。
            i 为 i 加 1。
        完。
        若 做 计数 用 对_分 等于 0：
            轮次 为 最大轮。
        否则：
            可变 最佳 为 0。
            可变 最佳分 为 对_分 的 第 0 项。
            可变 bi 为 1。
            只要 bi 小于 做 计数 用 对_分：
                若 对_分 的 第 bi 项 大于 最佳分：
                    最佳 为 bi。
                    最佳分 为 对_分 的 第 bi 项。
                完。
                bi 为 bi 加 1。
            完。
            若 最佳分 小于或等于 1：
                轮次 为 最大轮。
            否则：
                做 词表_追加 用 对_a 的 第 最佳 项 加 对_b 的 第 最佳 项。
            完。
        完。
        轮次 为 轮次 加 1。
    完。
    返还 词表_大小。
完。

分词_字符类_汉字 为 0。

分词_字符类_标点 为 1。

分词_字符类_数字 为 2。

分词_字符类_字母 为 3。

分词_字符类_空格 为 4。

分词_字符类_其他 为 5。

分词_字符分类 接受 码点：
    若 码点 大于或等于 19968 并且 码点 小于或等于 40959：
        返还 分词_字符类_汉字。
    完。
    若 码点 等于 12290 或者 码点 等于 65292 或者 码点 等于 65311 或者 码点 等于 65281：
        返还 分词_字符类_标点。
    完。
    若 码点 大于或等于 字符_零 并且 码点 小于或等于 字符_九：
        返还 分词_字符类_数字。
    完。
    若 码点 大于或等于 字符_大写A 并且 码点 小于或等于 字符_大写Z 或者 码点 大于或等于 字符_小写a 并且 码点 小于或等于 字符_小写z：
        返还 分词_字符类_字母。
    完。
    若 码点 等于 字符_空格 或者 码点 等于 字符_制表：
        返还 分词_字符类_空格。
    完。
    返还 分词_字符类_其他。
完。

分词_按类型预分段 接受 码点列表：
    若 做 计数 用 码点列表 等于 0：
        返还 一组：（空）。
    完。
    可变 段列表 为 一组：（空）。
    可变 当前段 为 一组：码点列表 的 第 0 项。
    可变 当前类 为 做 分词_字符分类 用 码点列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 码点列表：
        新类 为 做 分词_字符分类 用 码点列表 的 第 i 项。
        若 新类 不等于 当前类 或者 新类 等于 分词_字符类_标点：
            做 追加 用 段列表、当前段。
            当前段 为 一组：码点列表 的 第 i 项。
            当前类 为 新类。
        否则：
            做 追加 用 当前段、码点列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前段 大于 0：
        做 追加 用 段列表、当前段。
    完。
    返还 段列表。
完。

分词_歧义度估算 接受 码点列表：
    若 做 计数 用 码点列表 等于 0：
        返还 0。
    完。
    可变 汉字数 为 0。
    可变 二字词候选 为 0。
    可变 上一个是汉字 为 假。
    可变 i 为 0。
    只要 i 小于 做 计数 用 码点列表：
        若 做 分词_字符分类 用 码点列表 的 第 i 项 等于 分词_字符类_汉字：
            汉字数 为 汉字数 加 1。
            若 上一个是汉字：
                二字词候选 为 二字词候选 加 1。
            完。
            上一个是汉字 为 真。
        否则：
            上一个是汉字 为 假。
        完。
        i 为 i 加 1。
    完。
    若 汉字数 等于 0：
        返还 0。
    完。
    返还 二字词候选 乘 100 除以 汉字数。
完。

本章 收束。
