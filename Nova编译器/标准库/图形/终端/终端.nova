《终端》

引用《..·..·基础》。

转义符 为 「\x1b[」。

ESC_重置 为 「\x1b[0m」。

颜色_黑 接受 零参：
    返还 「\x1b[30m」。
完。

颜色_红 接受 零参：
    返还 「\x1b[31m」。
完。

颜色_绿 接受 零参：
    返还 「\x1b[32m」。
完。

颜色_黄 接受 零参：
    返还 「\x1b[33m」。
完。

颜色_蓝 接受 零参：
    返还 「\x1b[34m」。
完。

颜色_紫 接受 零参：
    返还 「\x1b[35m」。
完。

颜色_青 接受 零参：
    返还 「\x1b[36m」。
完。

颜色_白 接受 零参：
    返还 「\x1b[37m」。
完。

颜色_亮红 接受 零参：
    返还 「\x1b[91m」。
完。

颜色_亮绿 接受 零参：
    返还 「\x1b[92m」。
完。

颜色_亮黄 接受 零参：
    返还 「\x1b[93m」。
完。

颜色_亮蓝 接受 零参：
    返还 「\x1b[94m」。
完。

颜色_亮紫 接受 零参：
    返还 「\x1b[95m」。
完。

颜色_亮青 接受 零参：
    返还 「\x1b[96m」。
完。

背景_黑 接受 零参：
    返还 「\x1b[40m」。
完。

背景_红 接受 零参：
    返还 「\x1b[41m」。
完。

背景_绿 接受 零参：
    返还 「\x1b[42m」。
完。

背景_黄 接受 零参：
    返还 「\x1b[43m」。
完。

背景_蓝 接受 零参：
    返还 「\x1b[44m」。
完。

颜色256_前景 接受 n：
    返还 做 字符串拼接 用 「\x1b[38;5;」、做 字符串拼接 用 做 终端_数到文 用 n、「m」。
完。

颜色256_背景 接受 n：
    返还 做 字符串拼接 用 「\x1b[48;5;」、做 字符串拼接 用 做 终端_数到文 用 n、「m」。
完。

颜色RGB_前景 接受 r、g、b：
    可变 s 为 「\x1b[38;2;」。
    s 为 做 字符串拼接 用 s、做 终端_数到文 用 r。
    s 为 做 字符串拼接 用 s、「;」。
    s 为 做 字符串拼接 用 s、做 终端_数到文 用 g。
    s 为 做 字符串拼接 用 s、「;」。
    s 为 做 字符串拼接 用 s、做 终端_数到文 用 b。
    s 为 做 字符串拼接 用 s、「m」。
    返还 s。
完。

颜色RGB_背景 接受 r、g、b：
    可变 s 为 「\x1b[48;2;」。
    s 为 做 字符串拼接 用 s、做 终端_数到文 用 r。
    s 为 做 字符串拼接 用 s、「;」。
    s 为 做 字符串拼接 用 s、做 终端_数到文 用 g。
    s 为 做 字符串拼接 用 s、「;」。
    s 为 做 字符串拼接 用 s、做 终端_数到文 用 b。
    s 为 做 字符串拼接 用 s、「m」。
    返还 s。
完。

样式_粗体 接受 零参：
    返还 「\x1b[1m」。
完。

样式_暗色 接受 零参：
    返还 「\x1b[2m」。
完。

样式_斜体 接受 零参：
    返还 「\x1b[3m」。
完。

样式_下划线 接受 零参：
    返还 「\x1b[4m」。
完。

样式_闪烁 接受 零参：
    返还 「\x1b[5m」。
完。

样式_反转 接受 零参：
    返还 「\x1b[7m」。
完。

样式_删除线 接受 零参：
    返还 「\x1b[9m」。
完。

样式_重置 接受 零参：
    返还 ESC_重置。
完。

终端_着色 接受 文本、颜色码：
    返还 做 字符串拼接 用 颜色码、做 字符串拼接 用 文本、ESC_重置。
完。

终端_粗体文本 接受 文本：
    返还 做 字符串拼接 用 做 样式_粗体、做 字符串拼接 用 文本、ESC_重置。
完。

光标_上移 接受 n：
    返还 做 字符串拼接 用 「\x1b[」、做 字符串拼接 用 做 终端_数到文 用 n、「A」。
完。

光标_下移 接受 n：
    返还 做 字符串拼接 用 「\x1b[」、做 字符串拼接 用 做 终端_数到文 用 n、「B」。
完。

光标_右移 接受 n：
    返还 做 字符串拼接 用 「\x1b[」、做 字符串拼接 用 做 终端_数到文 用 n、「C」。
完。

光标_左移 接受 n：
    返还 做 字符串拼接 用 「\x1b[」、做 字符串拼接 用 做 终端_数到文 用 n、「D」。
完。

光标_定位 接受 行、列：
    可变 s 为 「\x1b[」。
    s 为 做 字符串拼接 用 s、做 终端_数到文 用 行。
    s 为 做 字符串拼接 用 s、「;」。
    s 为 做 字符串拼接 用 s、做 终端_数到文 用 列。
    s 为 做 字符串拼接 用 s、「H」。
    返还 s。
完。

光标_保存 接受 零参：
    返还 「\x1b[s」。
完。

光标_恢复 接受 零参：
    返还 「\x1b[u」。
完。

光标_隐藏 接受 零参：
    返还 「\x1b[?25l」。
完。

光标_显示 接受 零参：
    返还 「\x1b[?25h」。
完。

屏幕_清除 接受 零参：
    返还 「\x1b[2J\x1b[H」。
完。

屏幕_清行 接受 零参：
    返还 「\x1b[2K」。
完。

屏幕_清到行尾 接受 零参：
    返还 「\x1b[K」。
完。

屏幕_清到行首 接受 零参：
    返还 「\x1b[1K」。
完。

屏幕_滚上 接受 n：
    返还 做 字符串拼接 用 「\x1b[」、做 字符串拼接 用 做 终端_数到文 用 n、「S」。
完。

屏幕_滚下 接受 n：
    返还 做 字符串拼接 用 「\x1b[」、做 字符串拼接 用 做 终端_数到文 用 n、「T」。
完。

帧缓冲_创建 接受 宽、高：
    可变 字符 为 一组：（空）。
    可变 颜色 为 一组：（空）。
    可变 脏 为 一组：（空）。
    总 为 宽 乘 高。
    可变 i 为 0。
    只要 i 小于 总：
        做 追加 用 字符、「 」。
        做 追加 用 颜色、「」。
        做 追加 用 脏、真。
        i 为 i 加 1。
    完。
    返还 一组：宽、高、字符、颜色、脏。
完。

帧缓冲_设 接受 fb、x、y、字符、颜色码：
    若 x 小于 0 或者 x 大于或等于 fb 的 第 0 项 或者 y 小于 0 或者 y 大于或等于 fb 的 第 1 项：
        返还。
    完。
    索引 为 y 乘 fb 的 第 0 项 加 x。
    fb 的 第 2 项 的 第 索引 项 为 字符。
    fb 的 第 3 项 的 第 索引 项 为 颜色码。
    fb 的 第 4 项 的 第 索引 项 为 真。
完。

帧缓冲_取 接受 fb、x、y：
    若 x 小于 0 或者 x 大于或等于 fb 的 第 0 项 或者 y 小于 0 或者 y 大于或等于 fb 的 第 1 项：
        返还 「 」。
    完。
    返还 fb 的 第 2 项 的 第 y 乘 fb 的 第 0 项 加 x 项。
完。

帧缓冲_清除 接受 fb：
    总 为 fb 的 第 0 项 乘 fb 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 总：
        fb 的 第 2 项 的 第 i 项 为 「 」。
        fb 的 第 3 项 的 第 i 项 为 「」。
        fb 的 第 4 项 的 第 i 项 为 真。
        i 为 i 加 1。
    完。
完。

帧缓冲_写文本 接受 fb、x、y、文本、颜色码：
    n 为 做 计数 用 文本。
    可变 i 为 0。
    只要 i 小于 n：
        做 帧缓冲_设 用 fb、x 加 i、y、做 子串 用 文本、i、1、颜色码。
        i 为 i 加 1。
    完。
完。

帧缓冲_画矩形 接受 fb、x、y、宽、高、字符、颜色码：
    可变 导数Y 为 0。
    只要 导数Y 小于 高：
        可变 导数 为 0。
        只要 导数 小于 宽：
            若 导数Y 等于 0 或者 导数Y 等于 高 减 1 或者 导数 等于 0 或者 导数 等于 宽 减 1：
                做 帧缓冲_设 用 fb、x 加 导数、y 加 导数Y、字符、颜色码。
            完。
            导数 为 导数 加 1。
        完。
        导数Y 为 导数Y 加 1。
    完。
完。

帧缓冲_填充矩形 接受 fb、x、y、宽、高、字符、颜色码：
    可变 导数Y 为 0。
    只要 导数Y 小于 高：
        可变 导数 为 0。
        只要 导数 小于 宽：
            做 帧缓冲_设 用 fb、x 加 导数、y 加 导数Y、字符、颜色码。
            导数 为 导数 加 1。
        完。
        导数Y 为 导数Y 加 1。
    完。
完。

帧缓冲_画线 接受 fb、x1、y1、x2、y2、字符、颜色码：
    可变 导数 为 x2 减 x1。
    可变 导数Y 为 y2 减 y1。
    若 导数 小于 0：
        导数 为 0 减 导数。
    完。
    若 导数Y 小于 0：
        导数Y 为 0 减 导数Y。
    完。
    可变 sx 为 1。
    可变 sy 为 1。
    若 x1 大于 x2：
        sx 为 负 1。
    完。
    若 y1 大于 y2：
        sy 为 负 1。
    完。
    可变 err 为 导数 减 导数Y。
    可变 cx 为 x1。
    可变 cy 为 y1。
    只要 _线完 等于 假：
        做 帧缓冲_设 用 fb、cx、cy、字符、颜色码。
        若 cx 等于 x2 并且 cy 等于 y2：
            _线完 为 真。
        否则：
            e2 为 2 乘 err。
            若 e2 大于 0 减 导数Y：
                err 为 err 减 导数Y。
                cx 为 cx 加 sx。
            完。
            若 e2 小于 导数：
                err 为 err 加 导数。
                cy 为 cy 加 sy。
            完。
        完。
    完。
完。

帧缓冲_渲染 接受 fb：
    可变 输出 为 「」。
    可变 y 为 0。
    只要 y 小于 fb 的 第 1 项：
        可变 x 为 0。
        可变 行有脏 为 假。
        只要 x 小于 fb 的 第 0 项 并且 行有脏 等于 假：
            若 fb 的 第 4 项 的 第 y 乘 fb 的 第 0 项 加 x 项：
                行有脏 为 真。
            完。
            x 为 x 加 1。
        完。
        若 行有脏：
            输出 为 做 字符串拼接 用 输出、做 光标_定位 用 y 加 1、1。
            x 为 0。
            只要 x 小于 fb 的 第 0 项：
                索引 为 y 乘 fb 的 第 0 项 加 x。
                颜色码 为 fb 的 第 3 项 的 第 索引 项。
                若 做 计数 用 颜色码 大于 0：
                    输出 为 做 字符串拼接 用 输出、颜色码。
                完。
                输出 为 做 字符串拼接 用 输出、fb 的 第 2 项 的 第 索引 项。
                若 做 计数 用 颜色码 大于 0：
                    输出 为 做 字符串拼接 用 输出、ESC_重置。
                完。
                fb 的 第 4 项 的 第 索引 项 为 假。
                x 为 x 加 1。
            完。
        完。
        y 为 y 加 1。
    完。
    返还 输出。
完。

帧缓冲_全渲染 接受 fb：
    总 为 fb 的 第 0 项 乘 fb 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 总：
        fb 的 第 4 项 的 第 i 项 为 真。
        i 为 i 加 1。
    完。
    返还 做 帧缓冲_渲染 用 fb。
完。

盒_单线 接受 宽、高：
    可变 结果 为 「」。
    结果 为 做 字符串拼接 用 结果、「+」。
    可变 i 为 0。
    只要 i 小于 宽 减 2：
        结果 为 做 字符串拼接 用 结果、「-」。
        i 为 i 加 1。
    完。
    结果 为 做 字符串拼接 用 结果、「+\n」。
    可变 y 为 0。
    只要 y 小于 高 减 2：
        结果 为 做 字符串拼接 用 结果、「|」。
        i 为 0。
        只要 i 小于 宽 减 2：
            结果 为 做 字符串拼接 用 结果、「 」。
            i 为 i 加 1。
        完。
        结果 为 做 字符串拼接 用 结果、「|\n」。
        y 为 y 加 1。
    完。
    结果 为 做 字符串拼接 用 结果、「+」。
    i 为 0。
    只要 i 小于 宽 减 2：
        结果 为 做 字符串拼接 用 结果、「-」。
        i 为 i 加 1。
    完。
    结果 为 做 字符串拼接 用 结果、「+\n」。
    返还 结果。
完。

终端_进度条_渲染 接受 当前、总数、宽度、填充字符、空字符：
    若 总数 小于或等于 0：
        返还 「[ERROR]」。
    完。
    可变 比例 为 当前 乘 宽度 除以 总数。
    若 比例 大于 宽度：
        比例 为 宽度。
    完。
    百分比 为 当前 乘 100 除以 总数。
    可变 条 为 「[」。
    可变 i 为 0。
    只要 i 小于 宽度：
        若 i 小于 比例：
            条 为 做 字符串拼接 用 条、填充字符。
        否则：
            条 为 做 字符串拼接 用 条、空字符。
        完。
        i 为 i 加 1。
    完。
    条 为 做 字符串拼接 用 条、「] 」。
    条 为 做 字符串拼接 用 条、做 终端_数到文 用 百分比。
    条 为 做 字符串拼接 用 条、「%」。
    返还 条。
完。

表格_渲染 接受 表头、数据、列宽：
    列数 为 做 计数 用 表头。
    可变 ri 为 0。
    可变 结果 为 「+」。
    可变 ci 为 0。
    只要 ci 小于 列数：
        可变 j 为 0。
        只要 j 小于 列宽：
            结果 为 做 字符串拼接 用 结果、「-」。
            j 为 j 加 1。
        完。
        结果 为 做 字符串拼接 用 结果、「+」。
        ci 为 ci 加 1。
    完。
    结果 为 做 字符串拼接 用 结果、「\n|」。
    ci 为 0。
    只要 ci 小于 列数：
        文本 为 表头 的 第 ci 项。
        结果 为 做 字符串拼接 用 结果、做 终端_填充 用 文本、列宽。
        结果 为 做 字符串拼接 用 结果、「|」。
        ci 为 ci 加 1。
    完。
    结果 为 做 字符串拼接 用 结果、「\n+」。
    ci 为 0。
    只要 ci 小于 列数：
        可变 j2 为 0。
        只要 j2 小于 列宽：
            结果 为 做 字符串拼接 用 结果、「=」。
            j2 为 j2 加 1。
        完。
        结果 为 做 字符串拼接 用 结果、「+」。
        ci 为 ci 加 1。
    完。
    只要 ri 小于 做 计数 用 数据：
        结果 为 做 字符串拼接 用 结果、「\n|」。
        ci 为 0。
        只要 ci 小于 列数：
            若 ci 小于 做 计数 用 数据 的 第 ri 项：
                结果 为 做 字符串拼接 用 结果、做 终端_填充 用 做 转字符串 用 数据 的 第 ri 项 的 第 ci 项、列宽。
            否则：
                结果 为 做 字符串拼接 用 结果、做 终端_填充 用 「」、列宽。
            完。
            结果 为 做 字符串拼接 用 结果、「|」。
            ci 为 ci 加 1。
        完。
        ri 为 ri 加 1。
    完。
    结果 为 做 字符串拼接 用 结果、「\n+」。
    ci 为 0。
    只要 ci 小于 列数：
        可变 j3 为 0。
        只要 j3 小于 列宽：
            结果 为 做 字符串拼接 用 结果、「-」。
            j3 为 j3 加 1。
        完。
        结果 为 做 字符串拼接 用 结果、「+」。
        ci 为 ci 加 1。
    完。
    返还 结果。
完。

火花线 接受 数据：
    块 为 「▁▂▃▄▅▆▇█」。
    n 为 做 计数 用 数据。
    可变 最小 为 数据 的 第 0 项。
    可变 最大 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 数据 的 第 i 项 小于 最小：
            最小 为 数据 的 第 i 项。
        完。
        若 数据 的 第 i 项 大于 最大：
            最大 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 范围 为 最大 减 最小。
    若 范围 等于 0：
        范围 为 1。
    完。
    可变 结果 为 「」。
    i 为 0。
    只要 i 小于 n：
        可变 级别 为 (数据 的 第 i 项 减 最小) 乘 7 除以 范围。
        若 级别 大于 7：
            级别 为 7。
        完。
        结果 为 做 字符串拼接 用 结果、做 子串 用 块、级别、1。
        i 为 i 加 1。
    完。
    返还 结果。
完。

柱状图 接受 标签列表、数据、宽度、字符：
    n 为 做 计数 用 数据。
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 数据 的 第 i 项 大于 最大：
            最大 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 最大 等于 0：
        最大 为 1。
    完。
    可变 结果 为 「」。
    i 为 0。
    只要 i 小于 n：
        条长 为 数据 的 第 i 项 乘 宽度 除以 最大。
        结果 为 做 字符串拼接 用 结果、做 终端_填充 用 标签列表 的 第 i 项、10。
        结果 为 做 字符串拼接 用 结果、「 |」。
        可变 j 为 0。
        只要 j 小于 条长：
            结果 为 做 字符串拼接 用 结果、字符。
            j 为 j 加 1。
        完。
        结果 为 做 字符串拼接 用 结果、「 」。
        结果 为 做 字符串拼接 用 结果、做 终端_数到文 用 数据 的 第 i 项。
        结果 为 做 字符串拼接 用 结果、「\n」。
        i 为 i 加 1。
    完。
    返还 结果。
完。

终端_填充 接受 文本、宽度：
    可变 r 为 文本。
    只要 做 计数 用 r 小于 宽度：
        r 为 做 字符串拼接 用 r、「 」。
    完。
    返还 r。
完。

终端_数到文 接受 n：
    若 n 等于 0：
        返还 「0」。
    完。
    可变 v 为 n。
    可变 负 为 假。
    若 v 小于 0：
        负 为 真。
        v 为 0 减 v。
    完。
    可变 r 为 「」。
    只要 v 大于 0：
        r 为 做 字符串拼接 用 做 子串 用 「0123456789」、v 取余于 10、1、r。
        v 为 v 除以 10。
    完。
    若 负：
        r 为 做 字符串拼接 用 「-」、r。
    完。
    返还 r。
完。

可变 _终端_输出行数 为 0。

可变 _终端_输入次数 为 0。

可变 _终端_颜色使用 为 0。

终端_记录输出 接受 零参：
    _终端_输出行数 为 _终端_输出行数 加 1。
完。

终端_记录输入 接受 零参：
    _终端_输入次数 为 _终端_输入次数 加 1。
完。

终端_记录颜色 接受 零参：
    _终端_颜色使用 为 _终端_颜色使用 加 1。
完。

终端_交互度评估 接受 零参：
    若 _终端_输入次数 等于 0 并且 _终端_输出行数 等于 0：
        返还 「无活动」。
    完。
    若 _终端_输入次数 等于 0：
        返还 「纯输出模式」。
    完。
    比 为 _终端_输出行数 除以 _终端_输入次数。
    若 比 大于 10：
        返还 「输出密集型」。
    完。
    若 比 大于 1：
        返还 「正常交互」。
    完。
    返还 「输入密集型」。
完。

终端_UI丰富度 接受 零参：
    若 _终端_颜色使用 等于 0：
        返还 「纯文本(建议添加颜色)」。
    完。
    若 _终端_颜色使用 大于 100：
        返还 「丰富彩色UI」。
    完。
    返还 「基础彩色UI」。
完。

终端_性能评估 接受 零参：
    若 _终端_输出行数 大于 10000：
        返还 「大量输出: 考虑分页或缓冲」。
    完。
    若 _终端_输出行数 大于 1000：
        返还 「中等输出量」。
    完。
    返还 「输出量正常」。
完。

终端_输出效率 接受 零参：
    若 _终端_输出行数 等于 0：
        返还 0。
    完。
    若 _终端_颜色使用 等于 0：
        返还 50。
    完。
    返还 70 加 _终端_颜色使用 乘 30 除以 _终端_输出行数。
完。

终端_推荐宽度 接受 零参：
    返还 80。
完。

终端_推荐高度 接受 零参：
    返还 24。
完。

终端_是否需要分页 接受 行数：
    若 行数 大于 做 终端_推荐高度：
        返还 真。
    完。
    返还 假。
完。

终端_进度条 接受 当前、总数、宽度：
    若 总数 等于 0：
        返还 「」。
    完。
    完成 为 当前 乘 宽度 除以 总数。
    可变 条 为 「[」。
    可变 i 为 0。
    只要 i 小于 宽度：
        若 i 小于 完成：
            条 为 做 字符串拼接 用 条、「█」。
        否则：
            条 为 做 字符串拼接 用 条、「░」。
        完。
        i 为 i 加 1。
    完。
    条 为 做 字符串拼接 用 条、「]」。
    返还 条。
完。

终端_百分比文本 接受 当前、总数：
    若 总数 等于 0：
        返还 「0%」。
    完。
    百分比 为 当前 乘 100 除以 总数。
    返还 做 字符串拼接 用 做 转字符串 用 百分比、「%」。
完。

本章 收束。
