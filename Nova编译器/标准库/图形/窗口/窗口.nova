《窗口》

引用《..·..·基础》。

事件_键按下 为 1。

事件_键释放 为 2。

事件_鼠标按下 为 3。

事件_鼠标释放 为 4。

事件_鼠标移动 为 5。

事件_鼠标滚轮 为 6。

事件_窗口关闭 为 7。

事件_窗口调整 为 8。

事件_窗口焦点 为 9。

事件_窗口失焦 为 10。

事件_绘制 为 11。

事件_定时器 为 12。

事件_自定义 为 99。

事件_创建 接受 类型、时间戳：
    返还 一组：类型、时间戳、做 字典、假。
完。

事件_键盘 接受 类型、键码、修饰符、时间戳：
    ev 为 做 事件_创建 用 类型、时间戳。
    做 字典设 用 ev 的 第 2 项、「键码」、键码。
    做 字典设 用 ev 的 第 2 项、「修饰符」、修饰符。
    返还 ev。
完。

事件_鼠标 接受 类型、x、y、按钮、时间戳：
    ev 为 做 事件_创建 用 类型、时间戳。
    做 字典设 用 ev 的 第 2 项、「x」、x。
    做 字典设 用 ev 的 第 2 项、「y」、y。
    做 字典设 用 ev 的 第 2 项、「按钮」、按钮。
    返还 ev。
完。

事件_取数据 接受 ev、键：
    返还 做 字典取 用 ev 的 第 2 项、键。
完。

事件_停止传播 接受 ev：
    ev 的 第 3 项 为 真。
完。

事件_已处理 接受 ev：
    返还 ev 的 第 3 项。
完。

窗口_创建 接受 id、标题、x、y、宽、高：
    返还 一组：id、标题、x、y、宽、高、真、假、一组：（空）、做 字典、空、0、真、100、50、做 字典。
完。

窗口_ID 接受 w：
    返还 w 的 第 0 项。
完。

窗口_标题 接受 w：
    返还 w 的 第 1 项。
完。

窗口_设标题 接受 w、标题：
    w 的 第 1 项 为 标题。
完。

窗口_位置 接受 w：
    返还 一组：w 的 第 2 项、w 的 第 3 项。
完。

窗口_大小 接受 w：
    返还 一组：w 的 第 4 项、w 的 第 5 项。
完。

窗口_可见 接受 w：
    返还 w 的 第 6 项。
完。

窗口_显示 接受 w：
    w 的 第 6 项 为 真。
完。

窗口_隐藏 接受 w：
    w 的 第 6 项 为 假。
完。

窗口_有焦点 接受 w：
    返还 w 的 第 7 项。
完。

窗口_设焦点 接受 w、焦点：
    w 的 第 7 项 为 焦点。
完。

窗口_移动 接受 w、x、y：
    w 的 第 2 项 为 x。
    w 的 第 3 项 为 y。
完。

窗口_调整大小 接受 w、宽、高：
    若 宽 小于 w 的 第 13 项：
        宽 为 w 的 第 13 项。
    完。
    若 高 小于 w 的 第 14 项：
        高 为 w 的 第 14 项。
    完。
    w 的 第 4 项 为 宽。
    w 的 第 5 项 为 高。
完。

窗口_包含点 接受 w、x、y：
    返还 x 大于或等于 w 的 第 2 项 并且 x 小于 w 的 第 2 项 加 w 的 第 4 项 并且 y 大于或等于 w 的 第 3 项 并且 y 小于 w 的 第 3 项 加 w 的 第 5 项。
完。

窗口_监听 接受 w、事件类型、处理函数：
    现有 为 做 字典取 用 w 的 第 9 项、事件类型。
    若 现有 等于 空：
        做 字典设 用 w 的 第 9 项、事件类型、一组：处理函数。
    否则：
        做 追加 用 现有、处理函数。
    完。
完。

窗口_触发 接受 w、事件：
    处理器列表 为 做 字典取 用 w 的 第 9 项、事件 的 第 0 项。
    若 处理器列表 等于 空：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 处理器列表 并且 做 事件_已处理 用 事件 等于 假：
        做 处理器列表 的 第 i 项 用 事件。
        i 为 i 加 1。
    完。
    返还 真。
完。

组件_标签 为 「标签」。

组件_按钮 为 「按钮」。

组件_文本框 为 「文本框」。

组件_面板 为 「面板」。

组件_列表 为 「列表」。

组件_滚动条 为 「滚动条」。

组件_创建 接受 id、类型、x、y、宽、高：
    返还 一组：id、类型、x、y、宽、高、真、一组：（空）、做 字典、做 字典。
完。

组件_设属性 接受 组件、键、值：
    做 字典设 用 组件 的 第 8 项、键、值。
完。

组件_取属性 接受 组件、键：
    返还 做 字典取 用 组件 的 第 8 项、键。
完。

组件_添加子 接受 组件、子组件：
    做 追加 用 组件 的 第 7 项、子组件。
完。

组件_监听 接受 组件、事件类型、处理函数：
    现有 为 做 字典取 用 组件 的 第 9 项、事件类型。
    若 现有 等于 空：
        做 字典设 用 组件 的 第 9 项、事件类型、一组：处理函数。
    否则：
        做 追加 用 现有、处理函数。
    完。
完。

组件_点击测试 接受 组件、x、y：
    若 组件 的 第 6 项 等于 假：
        返还 空。
    完。
    若 x 小于 组件 的 第 2 项 或者 x 大于或等于 组件 的 第 2 项 加 组件 的 第 4 项 或者 y 小于 组件 的 第 3 项 或者 y 大于或等于 组件 的 第 3 项 加 组件 的 第 5 项：
        返还 空。
    完。
    可变 i 为 做 计数 用 组件 的 第 7 项 减 1。
    只要 i 大于或等于 0：
        子 为 组件 的 第 7 项 的 第 i 项。
        命中 为 做 组件_点击测试 用 子、x 减 组件 的 第 2 项、y 减 组件 的 第 3 项。
        若 命中 不等于 空：
            返还 命中。
        完。
        i 为 i 减 1。
    完。
    返还 组件。
完。

标签_创建 接受 id、x、y、文本：
    w 为 做 组件_创建 用 id、组件_标签、x、y、做 计数 用 文本 乘 8、16。
    做 组件_设属性 用 w、「文本」、文本。
    返还 w。
完。

按钮_创建 接受 id、x、y、宽、高、文本：
    w 为 做 组件_创建 用 id、组件_按钮、x、y、宽、高。
    做 组件_设属性 用 w、「文本」、文本。
    做 组件_设属性 用 w、「按下」、假。
    返还 w。
完。

文本框_创建 接受 id、x、y、宽：
    w 为 做 组件_创建 用 id、组件_文本框、x、y、宽、24。
    做 组件_设属性 用 w、「文本」、「」。
    做 组件_设属性 用 w、「光标位置」、0。
    做 组件_设属性 用 w、「选择开始」、负 1。
    做 组件_设属性 用 w、「焦点」、假。
    返还 w。
完。

面板_创建 接受 id、x、y、宽、高：
    返还 做 组件_创建 用 id、组件_面板、x、y、宽、高。
完。

窗口管理器_创建 接受 零参：
    返还 一组：一组：（空）、负 1、1、一组：（空）、假、0、做 字典、一组：假、负 1、一组：0、0。
完。

管理器_创建窗口 接受 wm、标题、x、y、宽、高：
    id 为 wm 的 第 2 项。
    wm 的 第 2 项 为 wm 的 第 2 项 加 1。
    w 为 做 窗口_创建 用 id、标题、x、y、宽、高。
    做 追加 用 wm 的 第 0 项、w。
    做 管理器_焦点 用 wm、id。
    返还 w。
完。

管理器_关闭窗口 接受 wm、id：
    可变 新列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 wm 的 第 0 项：
        若 wm 的 第 0 项 的 第 i 项 的 第 0 项 不等于 id：
            做 追加 用 新列表、wm 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    wm 的 第 0 项 为 新列表。
    若 wm 的 第 1 项 等于 id：
        若 做 计数 用 wm 的 第 0 项 大于 0：
            wm 的 第 1 项 为 wm 的 第 0 项 的 第 做 计数 用 wm 的 第 0 项 减 1 项 的 第 0 项。
        否则：
            wm 的 第 1 项 为 负 1。
        完。
    完。
完。

管理器_焦点 接受 wm、id：
    _旧焦点 为 wm 的 第 1 项。
    wm 的 第 1 项 为 id。
    可变 i 为 0。
    只要 i 小于 做 计数 用 wm 的 第 0 项：
        w 为 wm 的 第 0 项 的 第 i 项。
        做 窗口_设焦点 用 w、w 的 第 0 项 等于 id。
        若 w 的 第 0 项 等于 id：
            w 的 第 11 项 为 999。
        否则若 w 的 第 11 项 大于 0：
            w 的 第 11 项 为 w 的 第 11 项 减 1。
        完。
        i 为 i 加 1。
    完。
完。

管理器_查找窗口 接受 wm、id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 wm 的 第 0 项：
        若 wm 的 第 0 项 的 第 i 项 的 第 0 项 等于 id：
            返还 wm 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

管理器_提交事件 接受 wm、事件：
    做 追加 用 wm 的 第 3 项、事件。
完。

管理器_处理事件 接受 wm：
    可变 新队列 为 一组：（空）。
    可变 处理数 为 0。
    若 做 计数 用 wm 的 第 3 项 等于 0：
        返还 0。
    完。
    只要 做 计数 用 wm 的 第 3 项 大于 0：
        事件 为 wm 的 第 3 项 的 第 0 项。
        可变 i 为 1。
        只要 i 小于 做 计数 用 wm 的 第 3 项：
            做 追加 用 新队列、wm 的 第 3 项 的 第 i 项。
            i 为 i 加 1。
        完。
        wm 的 第 3 项 为 新队列。
        做 管理器_分发 用 wm、事件。
        处理数 为 处理数 加 1。
    完。
    返还 处理数。
完。

管理器_分发 接受 wm、事件：
    类型 为 事件 的 第 0 项。
    若 类型 等于 事件_键按下：
        键码 为 做 字典取 用 事件 的 第 2 项、「键码」。
        处理器 为 做 字典取 用 wm 的 第 6 项、键码。
        若 处理器 不等于 空：
            做 处理器 用 事件。
            若 做 事件_已处理 用 事件：
                返还。
            完。
        完。
    完。
    若 类型 大于或等于 事件_鼠标按下 并且 类型 小于或等于 事件_鼠标滚轮：
        mx 为 做 字典取 用 事件 的 第 2 项、「x」。
        my 为 做 字典取 用 事件 的 第 2 项、「y」。
        wm 的 第 8 项 为 一组：mx、my。
        若 类型 等于 事件_鼠标按下：
            可变 j 为 做 计数 用 wm 的 第 0 项 减 1。
            只要 j 大于或等于 0：
                w 为 wm 的 第 0 项 的 第 j 项。
                若 做 窗口_可见 用 w 并且 做 窗口_包含点 用 w、mx、my：
                    做 管理器_焦点 用 wm、w 的 第 0 项。
                    若 my 大于或等于 w 的 第 3 项 并且 my 小于 w 的 第 3 项 加 20：
                        wm 的 第 7 项 为 一组：真、w 的 第 0 项、mx 减 w 的 第 2 项、my 减 w 的 第 3 项。
                    完。
                    做 窗口_触发 用 w、事件。
                    返还。
                完。
                j 为 j 减 1。
            完。
        完。
        若 类型 等于 事件_鼠标移动 并且 wm 的 第 7 项 的 第 0 项：
            拖w 为 做 管理器_查找窗口 用 wm、wm 的 第 7 项 的 第 1 项。
            若 拖w 不等于 空：
                做 窗口_移动 用 拖w、mx 减 wm 的 第 7 项 的 第 2 项、my 减 wm 的 第 7 项 的 第 3 项。
            完。
            返还。
        完。
        若 类型 等于 事件_鼠标释放：
            wm 的 第 7 项 的 第 0 项 为 假。
        完。
        焦点窗 为 做 管理器_查找窗口 用 wm、wm 的 第 1 项。
        若 焦点窗 不等于 空：
            做 窗口_触发 用 焦点窗、事件。
        完。
        返还。
    完。
    若 类型 等于 事件_键按下 或者 类型 等于 事件_键释放：
        焦点窗 为 做 管理器_查找窗口 用 wm、wm 的 第 1 项。
        若 焦点窗 不等于 空：
            做 窗口_触发 用 焦点窗、事件。
        完。
        返还。
    完。
完。

管理器_注册快捷键 接受 wm、键码、处理函数：
    做 字典设 用 wm 的 第 6 项、键码、处理函数。
完。

管理器_tick 接受 wm：
    wm 的 第 5 项 为 wm 的 第 5 项 加 1。
    返还 做 管理器_处理事件 用 wm。
完。

管理器_运行 接受 wm、最大tick：
    wm 的 第 4 项 为 真。
    可变 总处理 为 0。
    只要 wm 的 第 4 项 并且 wm 的 第 5 项 小于 最大tick：
        总处理 为 总处理 加 做 管理器_tick 用 wm。
    完。
    wm 的 第 4 项 为 假。
    返还 总处理。
完。

管理器_停止 接受 wm：
    wm 的 第 4 项 为 假。
完。

管理器_窗口数 接受 wm：
    返还 做 计数 用 wm 的 第 0 项。
完。

管理器_焦点窗口 接受 wm：
    返还 wm 的 第 1 项。
完。

管理器_当前tick 接受 wm：
    返还 wm 的 第 5 项。
完。

布局_水平 接受 父组件、间距：
    可变 x 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 父组件 的 第 7 项：
        子 为 父组件 的 第 7 项 的 第 i 项。
        子 的 第 2 项 为 x。
        子 的 第 3 项 为 0。
        x 为 x 加 子 的 第 4 项 加 间距。
        i 为 i 加 1。
    完。
完。

布局_垂直 接受 父组件、间距：
    可变 y 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 父组件 的 第 7 项：
        子 为 父组件 的 第 7 项 的 第 i 项。
        子 的 第 2 项 为 0。
        子 的 第 3 项 为 y。
        y 为 y 加 子 的 第 5 项 加 间距。
        i 为 i 加 1。
    完。
完。

布局_网格 接受 父组件、列数、行间距、列间距：
    可变 _行 为 0。
    可变 列 为 0。
    若 列数 等于 0：
        列数 为 1。
    完。
    单元宽 为 父组件 的 第 4 项 除以 列数。
    可变 最大行高 为 0。
    可变 y偏移 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 父组件 的 第 7 项：
        子 为 父组件 的 第 7 项 的 第 i 项。
        子 的 第 2 项 为 列 乘 (单元宽 加 列间距)。
        子 的 第 3 项 为 y偏移。
        若 子 的 第 5 项 大于 最大行高：
            最大行高 为 子 的 第 5 项。
        完。
        列 为 列 加 1。
        若 列 大于或等于 列数：
            列 为 0。
            y偏移 为 y偏移 加 最大行高 加 行间距。
            最大行高 为 0。
        完。
        i 为 i 加 1。
    完。
完。

窗口_居中 接受 屏幕宽、屏幕高、窗口宽、窗口高：
    返还 一组：(屏幕宽 减 窗口宽) 除以 2、(屏幕高 减 窗口高) 除以 2。
完。

窗口_缩放 接受 窗口、比例：
    返还 一组：窗口 的 第 0 项、窗口 的 第 1 项、窗口 的 第 2 项 乘 比例 除以 100、窗口 的 第 3 项 乘 比例 除以 100。
完。

窗口_适配 接受 内容宽、内容高、容器宽、容器高：
    比W 为 容器宽 乘 1000 除以 内容宽。
    比H 为 容器高 乘 1000 除以 内容高。
    可变 比 为 比W。
    若 比H 小于 比：
        比 为 比H。
    完。
    返还 一组：内容宽 乘 比 除以 1000、内容高 乘 比 除以 1000。
完。

窗口_拖拽限制 接受 x、y、最小x、最小y、最大x、最大y：
    可变 rx 为 x。
    可变 ry 为 y。
    若 rx 小于 最小x：
        rx 为 最小x。
    完。
    若 rx 大于 最大x：
        rx 为 最大x。
    完。
    若 ry 小于 最小y：
        ry 为 最小y。
    完。
    若 ry 大于 最大y：
        ry 为 最大y。
    完。
    返还 一组：rx、ry。
完。

窗口_分屏 接受 屏幕宽、屏幕高、数量：
    若 数量 小于或等于 1：
        返还 一组：一组：0、0、屏幕宽、屏幕高。
    完。
    若 数量 等于 2：
        返还 一组：一组：0、0、屏幕宽 除以 2、屏幕高、一组：屏幕宽 除以 2、0、屏幕宽 除以 2、屏幕高。
    完。
    若 数量 小于或等于 4：
        半宽 为 屏幕宽 除以 2。
        半高 为 屏幕高 除以 2。
        返还 一组：一组：0、0、半宽、半高、一组：半宽、0、半宽、半高、一组：0、半高、半宽、半高、一组：半宽、半高、半宽、半高。
    完。
    可变 结果 为 一组：（空）。
    列数 为 3。
    行数 为 (数量 加 列数 减 1) 除以 列数。
    单宽 为 屏幕宽 除以 列数。
    单高 为 屏幕高 除以 行数。
    可变 i 为 0。
    只要 i 小于 数量：
        做 追加 用 结果、一组：i 取余于 列数 乘 单宽、i 除以 列数 乘 单高、单宽、单高。
        i 为 i 加 1。
    完。
    返还 结果。
完。

窗口_最小尺寸检查 接受 宽、高、最小宽、最小高：
    可变 w 为 宽。
    可变 h 为 高。
    若 w 小于 最小宽：
        w 为 最小宽。
    完。
    若 h 小于 最小高：
        h 为 最小高。
    完。
    返还 一组：w、h。
完。

可变 _窗口_创建数 为 0。

可变 _窗口_调整数 为 0。

可变 _窗口_事件数 为 0。

窗口_记录创建 接受 零参：
    _窗口_创建数 为 _窗口_创建数 加 1。
完。

窗口_记录调整 接受 零参：
    _窗口_调整数 为 _窗口_调整数 加 1。
完。

窗口_记录事件 接受 零参：
    _窗口_事件数 为 _窗口_事件数 加 1。
完。

窗口_交互评估 接受 零参：
    若 _窗口_事件数 大于 10000：
        返还 「高交互(事件节流)」。
    完。
    若 _窗口_调整数 大于 _窗口_创建数 乘 10：
        返还 「频繁调整(考虑缓存布局)」。
    完。
    返还 「交互正常」。
完。

窗口_重置 接受 零参：
    _窗口_创建数 为 0。
    _窗口_调整数 为 0。
    _窗口_事件数 为 0。
完。

窗口_层叠 接受 起始x、起始y、宽、高、数量、偏移：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 数量：
        做 追加 用 结果、一组：起始x 加 i 乘 偏移、起始y 加 i 乘 偏移、宽、高。
        i 为 i 加 1。
    完。
    返还 结果。
完。

窗口_平铺 接受 屏幕宽、屏幕高、窗口列表：
    n 为 做 计数 用 窗口列表。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 列 为 1。
    只要 列 乘 列 小于 n：
        列 为 列 加 1。
    完。
    行 为 (n 加 列 减 1) 除以 列。
    单宽 为 屏幕宽 除以 列。
    单高 为 屏幕高 除以 行。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、一组：i 取余于 列 乘 单宽、i 除以 列 乘 单高、单宽、单高。
        i 为 i 加 1。
    完。
    返还 结果。
完。

窗口_DPI缩放 接受 值、DPI：
    若 DPI 小于或等于 96：
        返还 值。
    完。
    返还 值 乘 DPI 除以 96。
完。

窗口_吸附 接受 x、y、吸附距离、锚点列表：
    可变 最近x 为 x。
    可变 最近y 为 y。
    可变 最近距 为 999999。
    可变 i 为 0。
    只要 i 小于 做 计数 用 锚点列表：
        可变 导数 为 x 减 锚点列表 的 第 i 项 的 第 0 项。
        可变 导数Y 为 y 减 锚点列表 的 第 i 项 的 第 1 项。
        若 导数 小于 0：
            导数 为 0 减 导数。
        完。
        若 导数Y 小于 0：
            导数Y 为 0 减 导数Y。
        完。
        若 导数 加 导数Y 小于 最近距 并且 导数 加 导数Y 小于 吸附距离：
            最近距 为 导数 加 导数Y。
            最近x 为 锚点列表 的 第 i 项 的 第 0 项。
            最近y 为 锚点列表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：最近x、最近y。
完。

本章 收束。
