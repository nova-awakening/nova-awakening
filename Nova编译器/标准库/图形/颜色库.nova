《颜色库》

引用《..·基础》。

引用《..·算法·数学扩展》。

颜色_红 为 0xFF0000。

颜色_绿 为 0x00FF00。

颜色_蓝 为 0x0000FF。

颜色_白 为 0xFFFFFF。

颜色_黑 为 0x000000。

颜色_黄 为 0xFFFF00。

颜色_青 为 0x00FFFF。

颜色_紫 为 0xFF00FF。

颜色_橙 为 0xFF8000。

颜色_灰 为 0x808080。

颜色_深红 为 0x8B0000。

颜色_深绿 为 0x006400。

颜色_深蓝 为 0x00008B。

颜色_金 为 0xFFD700。

颜色_银 为 0xC0C0C0。

颜色_珊瑚 为 0xFF7F50。

颜色_靛蓝 为 0x4B0082。

颜色_棕 为 0x8B4513。

颜色_粉 为 0xFFC0CB。

颜色_天蓝 为 0x87CEEB。

颜色_RGB 接受 r、g、b：
    返还 r 乘 65536 加 g 乘 256 加 b。
完。

颜色_RGBA 接受 r、g、b、a：
    返还 a 乘 16777216 加 r 乘 65536 加 g 乘 256 加 b。
完。

颜色_创建 接受 r、g、b、a：
    返还 做 颜色_RGBA 用 r、g、b、a。
完。

颜色_取R 接受 c：
    返还 c 除以 65536 取余于 256。
完。

颜色_取G 接受 c：
    返还 c 除以 256 取余于 256。
完。

颜色_取B 接受 c：
    返还 c 取余于 256。
完。

颜色_取A 接受 c：
    返还 c 除以 16777216 取余于 256。
完。

颜色_R 接受 c：
    返还 做 颜色_取R 用 c。
完。

颜色_G 接受 c：
    返还 做 颜色_取G 用 c。
完。

颜色_B 接受 c：
    返还 做 颜色_取B 用 c。
完。

颜色_A 接受 c：
    返还 做 颜色_取A 用 c。
完。

颜色_混合 接受 c1、c2、比例：
    r 为 做 颜色_取R 用 c1 乘 (100 减 比例) 除以 100 加 做 颜色_取R 用 c2 乘 比例 除以 100。
    g 为 做 颜色_取G 用 c1 乘 (100 减 比例) 除以 100 加 做 颜色_取G 用 c2 乘 比例 除以 100。
    b 为 做 颜色_取B 用 c1 乘 (100 减 比例) 除以 100 加 做 颜色_取B 用 c2 乘 比例 除以 100。
    返还 做 颜色_RGB 用 r、g、b。
完。

颜色_亮度 接受 c：
    返还 (做 颜色_取R 用 c 乘 299 加 做 颜色_取G 用 c 乘 587 加 做 颜色_取B 用 c 乘 114) 除以 1000。
完。

颜色_反转 接受 c：
    返还 做 颜色_RGB 用 255 减 做 颜色_取R 用 c、255 减 做 颜色_取G 用 c、255 减 做 颜色_取B 用 c。
完。

颜色_灰度 接受 c：
    灰 为 做 颜色_亮度 用 c。
    返还 做 颜色_RGB 用 灰、灰、灰。
完。

颜色_调亮 接受 c、比例：
    返还 做 颜色_混合 用 c、颜色_白、比例。
完。

颜色_调暗 接受 c、比例：
    返还 做 颜色_混合 用 c、颜色_黑、比例。
完。

颜色_调饱和 接受 c、比例：
    灰色 为 做 颜色_灰度 用 c。
    返还 做 颜色_混合 用 灰色、c、比例。
完。

颜色_alpha混合 接受 前景、背景、透明度：
    r 为 做 颜色_取R 用 前景 乘 透明度 除以 255 加 做 颜色_取R 用 背景 乘 (255 减 透明度) 除以 255。
    g 为 做 颜色_取G 用 前景 乘 透明度 除以 255 加 做 颜色_取G 用 背景 乘 (255 减 透明度) 除以 255。
    b 为 做 颜色_取B 用 前景 乘 透明度 除以 255 加 做 颜色_取B 用 背景 乘 (255 减 透明度) 除以 255。
    返还 做 颜色_RGB 用 r、g、b。
完。

颜色_距离 接受 c1、c2：
    dr 为 做 颜色_取R 用 c1 减 做 颜色_取R 用 c2。
    dg 为 做 颜色_取G 用 c1 减 做 颜色_取G 用 c2。
    db 为 做 颜色_取B 用 c1 减 做 颜色_取B 用 c2。
    返还 做 整数sqrt 用 dr 乘 dr 加 dg 乘 dg 加 db 乘 db。
完。

颜色_是暗色 接受 c：
    返还 做 颜色_亮度 用 c 小于 128。
完。

颜色_对比文字 接受 背景：
    若 做 颜色_是暗色 用 背景 等于 1：
        返还 颜色_白。
    完。
    返还 颜色_黑。
完。

颜色_转hex 接受 c：
    hex表 为 「0123456789ABCDEF」。
    r 为 做 颜色_取R 用 c。
    g 为 做 颜色_取G 用 c。
    b 为 做 颜色_取B 用 c。
    可变 s 为 「#」。
    s 为 做 字符串拼接 用 s、做 子串 用 hex表、r 除以 16、1。
    s 为 做 字符串拼接 用 s、做 子串 用 hex表、r 取余于 16、1。
    s 为 做 字符串拼接 用 s、做 子串 用 hex表、g 除以 16、1。
    s 为 做 字符串拼接 用 s、做 子串 用 hex表、g 取余于 16、1。
    s 为 做 字符串拼接 用 s、做 子串 用 hex表、b 除以 16、1。
    s 为 做 字符串拼接 用 s、做 子串 用 hex表、b 取余于 16、1。
    返还 s。
完。

颜色_渐变 接受 起始色、结束色、步数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 步数：
        比例 为 i 乘 100 除以 (步数 减 1)。
        做 追加 用 结果、做 颜色_混合 用 起始色、结束色、比例。
        i 为 i 加 1。
    完。
    返还 结果。
完。

颜色_RGB转HSL 接受 r、g、b：
    可变 最大 为 r。
    若 g 大于 最大：
        最大 为 g。
    完。
    若 b 大于 最大：
        最大 为 b。
    完。
    可变 最小 为 r。
    若 g 小于 最小：
        最小 为 g。
    完。
    若 b 小于 最小：
        最小 为 b。
    完。
    L 为 (最大 加 最小) 除以 2。
    若 最大 等于 最小：
        返还 一组：0、0、L。
    完。
    差 为 最大 减 最小。
    可变 S 为 0。
    若 L 大于 128：
        S 为 差 乘 255 除以 (510 减 最大 减 最小)。
    否则：
        S 为 差 乘 255 除以 (最大 加 最小)。
    完。
    可变 H 为 0。
    若 最大 等于 r：
        H 为 (g 减 b) 乘 60 除以 (差 加 1)。
    否则若 最大 等于 g：
        H 为 120 加 (b 减 r) 乘 60 除以 (差 加 1)。
    否则：
        H 为 240 加 (r 减 g) 乘 60 除以 (差 加 1)。
    完。
    若 H 小于 0：
        H 为 H 加 360。
    完。
    返还 一组：H、S、L。
完。

颜色_HSL转RGB 接受 h、s、l：
    若 s 等于 0：
        返还 一组：l、l、l。
    完。
    可变 q 为 0。
    若 l 小于 128：
        q 为 l 乘 (255 加 s) 除以 255。
    否则：
        q 为 l 加 s 减 l 乘 s 除以 255。
    完。
    p 为 2 乘 l 减 q。
    hk 为 h 乘 255 除以 360。
    可变 tr 为 hk 加 85。
    可变 tg 为 hk。
    可变 tb 为 hk 减 85。
    若 tr 大于 255：
        tr 为 tr 减 255。
    完。
    若 tb 小于 0：
        tb 为 tb 加 255。
    完。
    可变 R 为 p 加 (q 减 p) 乘 6 乘 tr 除以 255。
    可变 G 为 p 加 (q 减 p) 乘 6 乘 tg 除以 255。
    可变 B 为 p 加 (q 减 p) 乘 6 乘 tb 除以 255。
    若 R 大于 255：
        R 为 255。
    完。
    若 G 大于 255：
        G 为 255。
    完。
    若 B 大于 255：
        B 为 255。
    完。
    若 R 小于 0：
        R 为 0。
    完。
    若 G 小于 0：
        G 为 0。
    完。
    若 B 小于 0：
        B 为 0。
    完。
    返还 一组：R、G、B。
完。

颜色_欧氏距离 接受 c1、c2：
    dr 为 做 颜色_R 用 c1 减 做 颜色_R 用 c2。
    dg 为 做 颜色_G 用 c1 减 做 颜色_G 用 c2。
    db 为 做 颜色_B 用 c1 减 做 颜色_B 用 c2。
    返还 dr 乘 dr 加 dg 乘 dg 加 db 乘 db。
完。

颜色_曼哈顿距离 接受 c1、c2：
    可变 d 为 做 颜色_R 用 c1 减 做 颜色_R 用 c2。
    若 d 小于 0：
        d 为 0 减 d。
    完。
    可变 dg 为 做 颜色_G 用 c1 减 做 颜色_G 用 c2。
    若 dg 小于 0：
        dg 为 0 减 dg。
    完。
    可变 db 为 做 颜色_B 用 c1 减 做 颜色_B 用 c2。
    若 db 小于 0：
        db 为 0 减 db。
    完。
    返还 d 加 dg 加 db。
完。

颜色_最近似 接受 目标、调色板：
    若 做 计数 用 调色板 等于 0：
        返还 负 1。
    完。
    可变 最近 为 0。
    可变 最近距离 为 做 颜色_欧氏距离 用 目标、调色板 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 调色板：
        d 为 做 颜色_欧氏距离 用 目标、调色板 的 第 i 项。
        若 d 小于 最近距离：
            最近距离 为 d。
            最近 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最近。
完。

调色板_创建 接受 零参：
    返还 一组：（空）。
完。

调色板_添加 接受 板、颜色：
    做 追加 用 板、颜色。
完。

调色板_大小 接受 板：
    返还 做 计数 用 板。
完。

调色板_Web安全 接受 零参：
    可变 板 为 一组：（空）。
    可变 r 为 0。
    只要 r 小于或等于 255：
        可变 g 为 0。
        只要 g 小于或等于 255：
            可变 b 为 0。
            只要 b 小于或等于 255：
                做 追加 用 板、做 颜色_创建 用 r、g、b、255。
                b 为 b 加 51。
            完。
            g 为 g 加 51。
        完。
        r 为 r 加 51。
    完。
    返还 板。
完。

颜色_变亮 接受 颜色、百分比：
    r 为 做 颜色_R 用 颜色 加 (255 减 做 颜色_R 用 颜色) 乘 百分比 除以 100。
    g 为 做 颜色_G 用 颜色 加 (255 减 做 颜色_G 用 颜色) 乘 百分比 除以 100。
    b 为 做 颜色_B 用 颜色 加 (255 减 做 颜色_B 用 颜色) 乘 百分比 除以 100。
    返还 做 颜色_创建 用 r、g、b、做 颜色_A 用 颜色。
完。

颜色_变暗 接受 颜色、百分比：
    r 为 做 颜色_R 用 颜色 乘 (100 减 百分比) 除以 100。
    g 为 做 颜色_G 用 颜色 乘 (100 减 百分比) 除以 100。
    b 为 做 颜色_B 用 颜色 乘 (100 减 百分比) 除以 100。
    返还 做 颜色_创建 用 r、g、b、做 颜色_A 用 颜色。
完。

颜色_灰度_RGBA 接受 颜色：
    灰 为 (做 颜色_R 用 颜色 乘 299 加 做 颜色_G 用 颜色 乘 587 加 做 颜色_B 用 颜色 乘 114) 除以 1000。
    返还 做 颜色_创建 用 灰、灰、灰、做 颜色_A 用 颜色。
完。

颜色_反转_RGBA 接受 颜色：
    返还 做 颜色_创建 用 255 减 做 颜色_R 用 颜色、255 减 做 颜色_G 用 颜色、255 减 做 颜色_B 用 颜色、做 颜色_A 用 颜色。
完。

颜色_饱和度调整 接受 颜色、因子：
    灰 为 (做 颜色_R 用 颜色 乘 299 加 做 颜色_G 用 颜色 乘 587 加 做 颜色_B 用 颜色 乘 114) 除以 1000。
    r 为 灰 加 (做 颜色_R 用 颜色 减 灰) 乘 因子 除以 100。
    g 为 灰 加 (做 颜色_G 用 颜色 减 灰) 乘 因子 除以 100。
    b 为 灰 加 (做 颜色_B 用 颜色 减 灰) 乘 因子 除以 100。
    返还 做 颜色_创建 用 r、g、b、做 颜色_A 用 颜色。
完。

颜色_转十六进制 接受 颜色：
    可变 结果 为 「#」。
    r 为 做 颜色_R 用 颜色。
    g 为 做 颜色_G 用 颜色。
    b 为 做 颜色_B 用 颜色。
    可变 hex表 为 「0123456789ABCDEF」。
    结果 为 结果 加 做 子串 用 hex表、r 除以 16、1 加 做 子串 用 hex表、r 取余于 16、1。
    结果 为 结果 加 做 子串 用 hex表、g 除以 16、1 加 做 子串 用 hex表、g 取余于 16、1。
    结果 为 结果 加 做 子串 用 hex表、b 除以 16、1 加 做 子串 用 hex表、b 取余于 16、1。
    返还 结果。
完。

可变 _颜色_转换数 为 0。

可变 _颜色_操作数 为 0。

颜色_记录操作 接受 零参：
    _颜色_操作数 为 _颜色_操作数 加 1。
完。

颜色_记录转换 接受 零参：
    _颜色_转换数 为 _颜色_转换数 加 1。
完。

本章 收束。
