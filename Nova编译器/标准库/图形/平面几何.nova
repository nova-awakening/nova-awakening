《平面几何》

引用《..·基础》。

引用《..·数学·矩阵》。

引用《..·数学·线性代数》。

引用《..·算法·数学扩展》。

引用《..·..·运行时·智能推理·注意力机制》。

点_创建 接受 x、y：
    返还 一组：x、y。
完。

点_x 接受 p：
    返还 p 的 第 0 项。
完。

点_y 接受 p：
    返还 p 的 第 1 项。
完。

平面几何_向量_加 接受 a、b：
    返还 一组：a 的 第 0 项 加 b 的 第 0 项、a 的 第 1 项 加 b 的 第 1 项。
完。

平面几何_向量_减 接受 a、b：
    返还 一组：a 的 第 0 项 减 b 的 第 0 项、a 的 第 1 项 减 b 的 第 1 项。
完。

平面_向量_缩放 接受 v、s：
    返还 一组：v 的 第 0 项 乘 s、v 的 第 1 项 乘 s。
完。

平面几何_向量_点积 接受 a、b：
    返还 a 的 第 0 项 乘 b 的 第 0 项 加 a 的 第 1 项 乘 b 的 第 1 项。
完。

平面_向量_叉积 接受 a、b：
    返还 a 的 第 0 项 乘 b 的 第 1 项 减 a 的 第 1 项 乘 b 的 第 0 项。
完。

平面_向量_长度平方 接受 v：
    返还 v 的 第 0 项 乘 v 的 第 0 项 加 v 的 第 1 项 乘 v 的 第 1 项。
完。

平面_向量_长度 接受 v：
    返还 做 整数sqrt 用 做 平面_向量_长度平方 用 v。
完。

点_距离平方 接受 a、b：
    导数 为 a 的 第 0 项 减 b 的 第 0 项。
    导数Y 为 a 的 第 1 项 减 b 的 第 1 项。
    返还 导数 乘 导数 加 导数Y 乘 导数Y。
完。

点_距离 接受 a、b：
    返还 做 整数sqrt 用 做 点_距离平方 用 a、b。
完。

点_曼哈顿 接受 a、b：
    可变 导数 为 a 的 第 0 项 减 b 的 第 0 项。
    可变 导数Y 为 a 的 第 1 项 减 b 的 第 1 项。
    若 导数 小于 0：
        导数 为 0 减 导数。
    完。
    若 导数Y 小于 0：
        导数Y 为 0 减 导数Y。
    完。
    返还 导数 加 导数Y。
完。

线段_创建 接受 p1、p2：
    返还 一组：p1、p2。
完。

线段_起点 接受 s：
    返还 s 的 第 0 项。
完。

线段_终点 接受 s：
    返还 s 的 第 1 项。
完。

线段_长度 接受 s：
    返还 做 点_距离 用 s 的 第 0 项、s 的 第 1 项。
完。

点到线段距离平方 接受 p、seg：
    a 为 seg 的 第 0 项。
    b 为 seg 的 第 1 项。
    ab 为 做 平面几何_向量_减 用 b、a。
    ap 为 做 平面几何_向量_减 用 p、a。
    t_num 为 做 平面几何_向量_点积 用 ap、ab。
    t_den 为 做 平面_向量_长度平方 用 ab。
    若 t_den 等于 0：
        返还 做 点_距离平方 用 p、a。
    完。
    若 t_num 小于或等于 0：
        返还 做 点_距离平方 用 p、a。
    完。
    若 t_num 大于或等于 t_den：
        返还 做 点_距离平方 用 p、b。
    完。
    投影x 为 a 的 第 0 项 加 ab 的 第 0 项 乘 t_num 除以 t_den。
    投影y 为 a 的 第 1 项 加 ab 的 第 1 项 乘 t_num 除以 t_den。
    导数 为 p 的 第 0 项 减 投影x。
    导数Y 为 p 的 第 1 项 减 投影y。
    返还 导数 乘 导数 加 导数Y 乘 导数Y。
完。

矩形_创建 接受 x、y、w、h：
    返还 一组：x、y、w、h。
完。

矩形_x 接受 r：
    返还 r 的 第 0 项。
完。

矩形_y 接受 r：
    返还 r 的 第 1 项。
完。

矩形_宽 接受 r：
    返还 r 的 第 2 项。
完。

矩形_高 接受 r：
    返还 r 的 第 3 项。
完。

矩形_右 接受 r：
    返还 r 的 第 0 项 加 r 的 第 2 项。
完。

矩形_下 接受 r：
    返还 r 的 第 1 项 加 r 的 第 3 项。
完。

矩形_面积 接受 r：
    返还 r 的 第 2 项 乘 r 的 第 3 项。
完。

矩形_周长 接受 r：
    返还 2 乘 (r 的 第 2 项 加 r 的 第 3 项)。
完。

矩形_中心 接受 r：
    返还 一组：r 的 第 0 项 加 r 的 第 2 项 除以 2、r 的 第 1 项 加 r 的 第 3 项 除以 2。
完。

矩形_包含点 接受 r、p：
    若 p 的 第 0 项 大于或等于 r 的 第 0 项：
        若 p 的 第 0 项 小于或等于 r 的 第 0 项 加 r 的 第 2 项：
            若 p 的 第 1 项 大于或等于 r 的 第 1 项：
                若 p 的 第 1 项 小于或等于 r 的 第 1 项 加 r 的 第 3 项：
                    返还 1。
                完。
            完。
        完。
    完。
    返还 0。
完。

矩形_相交 接受 a、b：
    若 a 的 第 0 项 大于或等于 b 的 第 0 项 加 b 的 第 2 项：
        返还 0。
    完。
    若 b 的 第 0 项 大于或等于 a 的 第 0 项 加 a 的 第 2 项：
        返还 0。
    完。
    若 a 的 第 1 项 大于或等于 b 的 第 1 项 加 b 的 第 3 项：
        返还 0。
    完。
    若 b 的 第 1 项 大于或等于 a 的 第 1 项 加 a 的 第 3 项：
        返还 0。
    完。
    返还 1。
完。

矩形_交集 接受 a、b：
    若 做 矩形_相交 用 a、b 等于 0：
        返还 空。
    完。
    可变 x 为 a 的 第 0 项。
    若 b 的 第 0 项 大于 x：
        x 为 b 的 第 0 项。
    完。
    可变 y 为 a 的 第 1 项。
    若 b 的 第 1 项 大于 y：
        y 为 b 的 第 1 项。
    完。
    可变 r 为 a 的 第 0 项 加 a 的 第 2 项。
    若 b 的 第 0 项 加 b 的 第 2 项 小于 r：
        r 为 b 的 第 0 项 加 b 的 第 2 项。
    完。
    可变 bot 为 a 的 第 1 项 加 a 的 第 3 项。
    若 b 的 第 1 项 加 b 的 第 3 项 小于 bot：
        bot 为 b 的 第 1 项 加 b 的 第 3 项。
    完。
    返还 做 矩形_创建 用 x、y、r 减 x、bot 减 y。
完。

圆_创建 接受 x、y、r：
    返还 一组：x、y、r。
完。

圆_圆心 接受 c：
    返还 一组：c 的 第 0 项、c 的 第 1 项。
完。

圆_半径 接受 c：
    返还 c 的 第 2 项。
完。

圆_包含点 接受 c、p：
    导数 为 p 的 第 0 项 减 c 的 第 0 项。
    导数Y 为 p 的 第 1 项 减 c 的 第 1 项。
    返还 导数 乘 导数 加 导数Y 乘 导数Y 小于或等于 c 的 第 2 项 乘 c 的 第 2 项。
完。

圆_相交 接受 a、b：
    导数 为 a 的 第 0 项 减 b 的 第 0 项。
    导数Y 为 a 的 第 1 项 减 b 的 第 1 项。
    d2 为 导数 乘 导数 加 导数Y 乘 导数Y。
    r和 为 a 的 第 2 项 加 b 的 第 2 项。
    返还 d2 小于或等于 r和 乘 r和。
完。

三角形面积 接受 p1、p2、p3：
    可变 面积2 为 (p2 的 第 0 项 减 p1 的 第 0 项) 乘 (p3 的 第 1 项 减 p1 的 第 1 项) 减 (p3 的 第 0 项 减 p1 的 第 0 项) 乘 (p2 的 第 1 项 减 p1 的 第 1 项)。
    若 面积2 小于 0：
        面积2 为 0 减 面积2。
    完。
    返还 面积2 除以 2。
完。

方向 接受 p、q、r：
    返还 (q 的 第 1 项 减 p 的 第 1 项) 乘 (r 的 第 0 项 减 q 的 第 0 项) 减 (q 的 第 0 项 减 p 的 第 0 项) 乘 (r 的 第 1 项 减 q 的 第 1 项)。
完。

在线段上 接受 p、q、r：
    可变 最小x 为 p 的 第 0 项。
    可变 最大x 为 p 的 第 0 项。
    若 r 的 第 0 项 小于 最小x：
        最小x 为 r 的 第 0 项。
    完。
    若 r 的 第 0 项 大于 最大x：
        最大x 为 r 的 第 0 项。
    完。
    可变 最小y 为 p 的 第 1 项。
    可变 最大y 为 p 的 第 1 项。
    若 r 的 第 1 项 小于 最小y：
        最小y 为 r 的 第 1 项。
    完。
    若 r 的 第 1 项 大于 最大y：
        最大y 为 r 的 第 1 项。
    完。
    若 q 的 第 0 项 大于或等于 最小x：
        若 q 的 第 0 项 小于或等于 最大x：
            若 q 的 第 1 项 大于或等于 最小y：
                若 q 的 第 1 项 小于或等于 最大y：
                    返还 1。
                完。
            完。
        完。
    完。
    返还 0。
完。

线段_相交 接受 a、b、c、d：
    d1 为 做 方向 用 c、d、a。
    d2 为 做 方向 用 c、d、b。
    d3 为 做 方向 用 a、b、c。
    d4 为 做 方向 用 a、b、d。
    可变 第一组跨立 为 0。
    若 d1 大于 0：
        若 d2 小于 0：
            第一组跨立 为 1。
        完。
    完。
    若 第一组跨立 等于 0：
        若 d1 小于 0：
            若 d2 大于 0：
                第一组跨立 为 1。
            完。
        完。
    完。
    可变 第二组跨立 为 0。
    若 d3 大于 0：
        若 d4 小于 0：
            第二组跨立 为 1。
        完。
    完。
    若 第二组跨立 等于 0：
        若 d3 小于 0：
            若 d4 大于 0：
                第二组跨立 为 1。
            完。
        完。
    完。
    若 第一组跨立 等于 1：
        若 第二组跨立 等于 1：
            返还 1。
        完。
    完。
    若 d1 等于 0：
        若 做 在线段上 用 c、a、d：
            返还 1。
        完。
    完。
    若 d2 等于 0：
        若 做 在线段上 用 c、b、d：
            返还 1。
        完。
    完。
    若 d3 等于 0：
        若 做 在线段上 用 a、c、b：
            返还 1。
        完。
    完。
    若 d4 等于 0：
        若 做 在线段上 用 a、d、b：
            返还 1。
        完。
    完。
    返还 0。
完。

线段_交点 接受 a、b、c、d：
    dx1 为 b 的 第 0 项 减 a 的 第 0 项。
    dy1 为 b 的 第 1 项 减 a 的 第 1 项。
    dx2 为 d 的 第 0 项 减 c 的 第 0 项。
    dy2 为 d 的 第 1 项 减 c 的 第 1 项。
    行列式 为 dx1 乘 dy2 减 dy1 乘 dx2。
    若 行列式 等于 0：
        返还 空。
    完。
    t 为 ((c 的 第 0 项 减 a 的 第 0 项) 乘 dy2 减 (c 的 第 1 项 减 a 的 第 1 项) 乘 dx2) 乘 1000 除以 行列式。
    若 t 小于 0：
        返还 空。
    完。
    若 t 大于 1000：
        返还 空。
    完。
    返还 一组：a 的 第 0 项 加 dx1 乘 t 除以 1000、a 的 第 1 项 加 dy1 乘 t 除以 1000。
完。

多边形_面积 接受 点列表：
    n 为 做 计数 用 点列表。
    若 n 小于 3：
        返还 0。
    完。
    可变 面积2 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        j 为 (i 加 1) 取余于 n。
        面积2 为 面积2 加 点列表 的 第 i 项 的 第 0 项 乘 点列表 的 第 j 项 的 第 1 项。
        面积2 为 面积2 减 点列表 的 第 j 项 的 第 0 项 乘 点列表 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    若 面积2 小于 0：
        面积2 为 0 减 面积2。
    完。
    返还 面积2 除以 2。
完。

多边形_周长 接受 点列表：
    n 为 做 计数 用 点列表。
    若 n 小于 2：
        返还 0。
    完。
    可变 总长 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        j 为 (i 加 1) 取余于 n。
        总长 为 总长 加 做 点_距离 用 点列表 的 第 i 项、点列表 的 第 j 项。
        i 为 i 加 1。
    完。
    返还 总长。
完。

多边形_包含点 接受 点列表、p：
    n 为 做 计数 用 点列表。
    若 n 小于 3：
        返还 0。
    完。
    可变 交叉 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        j 为 (i 加 1) 取余于 n。
        yi 为 点列表 的 第 i 项 的 第 1 项。
        yj 为 点列表 的 第 j 项 的 第 1 项。
        可变 跨越 为 0。
        若 yi 小于或等于 p 的 第 1 项：
            若 yj 大于 p 的 第 1 项：
                跨越 为 1。
            完。
        完。
        若 跨越 等于 0：
            若 yj 小于或等于 p 的 第 1 项：
                若 yi 大于 p 的 第 1 项：
                    跨越 为 1。
                完。
            完。
        完。
        若 跨越 等于 1：
            vt 为 (p 的 第 1 项 减 yi) 乘 1000 除以 (yj 减 yi)。
            若 p 的 第 0 项 小于 点列表 的 第 i 项 的 第 0 项 加 vt 乘 (点列表 的 第 j 项 的 第 0 项 减 点列表 的 第 i 项 的 第 0 项) 除以 1000：
                交叉 为 交叉 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 交叉 取余于 2。
完。

多边形_是凸 接受 点列表：
    n 为 做 计数 用 点列表。
    若 n 小于 3：
        返还 0。
    完。
    可变 符号 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        p1 为 点列表 的 第 i 项。
        p2 为 点列表 的 第 (i 加 1) 取余于 n 项。
        p3 为 点列表 的 第 (i 加 2) 取余于 n 项。
        叉 为 (p2 的 第 0 项 减 p1 的 第 0 项) 乘 (p3 的 第 1 项 减 p2 的 第 1 项) 减 (p2 的 第 1 项 减 p1 的 第 1 项) 乘 (p3 的 第 0 项 减 p2 的 第 0 项)。
        若 叉 不等于 0：
            若 符号 等于 0：
                若 叉 大于 0：
                    符号 为 1。
                否则：
                    符号 为 负 1。
                完。
            否则：
                若 叉 大于 0：
                    若 符号 小于 0：
                        返还 0。
                    完。
                完。
                若 叉 小于 0：
                    若 符号 大于 0：
                        返还 0。
                    完。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

凸包 接受 点列表、_扩2：
    n 为 做 计数 用 点列表。
    可变 新栈 为 一组：（空）。
    若 n 小于 3：
        返还 点列表。
    完。
    可变 最低 为 0。
    可变 i 为 1。
    只要 i 小于 n：
        可变 更低 为 0。
        若 点列表 的 第 i 项 的 第 1 项 小于 点列表 的 第 最低 项 的 第 1 项：
            更低 为 1。
        否则：
            若 点列表 的 第 i 项 的 第 1 项 等于 点列表 的 第 最低 项 的 第 1 项：
                若 点列表 的 第 i 项 的 第 0 项 小于 点列表 的 第 最低 项 的 第 0 项：
                    更低 为 1。
                完。
            完。
        完。
        若 更低 等于 1：
            最低 为 i。
        完。
        i 为 i 加 1。
    完。
    暂存 为 点列表 的 第 0 项。
    点列表 的 第 0 项 为 点列表 的 第 最低 项。
    点列表 的 第 最低 项 为 暂存。
    基点 为 点列表 的 第 0 项。
    i 为 1。
    只要 i 小于 n 减 1：
        可变 最小 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 n：
            叉 为 做 方向 用 基点、点列表 的 第 最小 项、点列表 的 第 j 项。
            可变 需要替换 为 0。
            若 叉 小于 0：
                需要替换 为 1。
            否则：
                若 叉 等于 0：
                    若 做 点_距离平方 用 基点、点列表 的 第 j 项 小于 做 点_距离平方 用 基点、点列表 的 第 最小 项：
                        需要替换 为 1。
                    完。
                完。
            完。
            若 需要替换 等于 1：
                最小 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最小 不等于 i：
            t 为 点列表 的 第 i 项。
            点列表 的 第 i 项 为 点列表 的 第 最小 项。
            点列表 的 第 最小 项 为 t。
        完。
        i 为 i 加 1。
    完。
    可变 栈 为 一组：点列表 的 第 0 项、点列表 的 第 1 项。
    i 为 2。
    只要 i 小于 n：
        只要 做 计数 用 栈 大于 1：
            顶 为 栈 的 第 做 计数 用 栈 减 1 项。
            次顶 为 栈 的 第 做 计数 用 栈 减 2 项。
            若 做 方向 用 次顶、顶、点列表 的 第 i 项 小于或等于 0：
                可变 k 为 0。
                只要 k 小于 做 计数 用 栈 减 1：
                    做 追加 用 新栈、栈 的 第 k 项。
                    k 为 k 加 1。
                完。
                栈 为 新栈。
            否则：
                栈 为 栈。
            完。
        完。
        做 追加 用 栈、点列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 栈。
完。

包围盒_从点集 接受 点列表：
    n 为 做 计数 用 点列表。
    若 n 等于 0：
        返还 空。
    完。
    可变 minX 为 点列表 的 第 0 项 的 第 0 项。
    可变 minY 为 点列表 的 第 0 项 的 第 1 项。
    可变 maxX 为 minX。
    可变 maxY 为 minY。
    可变 i 为 1。
    只要 i 小于 n：
        若 点列表 的 第 i 项 的 第 0 项 小于 minX：
            minX 为 点列表 的 第 i 项 的 第 0 项。
        完。
        若 点列表 的 第 i 项 的 第 0 项 大于 maxX：
            maxX 为 点列表 的 第 i 项 的 第 0 项。
        完。
        若 点列表 的 第 i 项 的 第 1 项 小于 minY：
            minY 为 点列表 的 第 i 项 的 第 1 项。
        完。
        若 点列表 的 第 i 项 的 第 1 项 大于 maxY：
            maxY 为 点列表 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 做 矩形_创建 用 minX、minY、maxX 减 minX、maxY 减 minY。
完。

变换_创建 接受 a、b、tx、c、d、ty：
    返还 一组：a、b、tx、c、d、ty。
完。

变换_单位 接受 零参：
    返还 一组：1000、0、0、1000、0。
完。

变换_平移 接受 tx、ty：
    返还 一组：1000、0、tx、0、1000、ty。
完。

变换_缩放 接受 sx、sy：
    返还 一组：sx、0、0、sy、0。
完。

变换_应用 接受 变换、点：
    x 为 变换 的 第 0 项 乘 点 的 第 0 项 除以 1000 加 变换 的 第 1 项 乘 点 的 第 1 项 除以 1000 加 变换 的 第 2 项。
    y 为 变换 的 第 3 项 乘 点 的 第 0 项 除以 1000 加 变换 的 第 4 项 乘 点 的 第 1 项 除以 1000 加 变换 的 第 5 项。
    返还 一组：x、y。
完。

变换_组合 接受 A、B：
    返还 一组：B 的 第 0 项 乘 A 的 第 0 项 除以 1000 加 B 的 第 1 项 乘 A 的 第 3 项 除以 1000、B 的 第 0 项 乘 A 的 第 1 项 除以 1000 加 B 的 第 1 项 乘 A 的 第 4 项 除以 1000、B 的 第 0 项 乘 A 的 第 2 项 除以 1000 加 B 的 第 1 项 乘 A 的 第 5 项 除以 1000 加 B 的 第 2 项、B 的 第 3 项 乘 A 的 第 0 项 除以 1000 加 B 的 第 4 项 乘 A 的 第 3 项 除以 1000、B 的 第 3 项 乘 A 的 第 1 项 除以 1000 加 B 的 第 4 项 乘 A 的 第 4 项 除以 1000、B 的 第 3 项 乘 A 的 第 2 项 除以 1000 加 B 的 第 4 项 乘 A 的 第 5 项 除以 1000 加 B 的 第 5 项。
完。

点在矩形内 接受 px、py、rx、ry、rw、rh：
    若 px 大于或等于 rx：
        若 px 小于或等于 rx 加 rw：
            若 py 大于或等于 ry：
                若 py 小于或等于 ry 加 rh：
                    返还 1。
                完。
            完。
        完。
    完。
    返还 0。
完。

矩形重叠 接受 x1、y1、w1、h1、x2、y2、w2、h2：
    若 x1 加 w1 小于或等于 x2：
        返还 假。
    完。
    若 x2 加 w2 小于或等于 x1：
        返还 假。
    完。
    若 y1 加 h1 小于或等于 y2：
        返还 假。
    完。
    若 y2 加 h2 小于或等于 y1：
        返还 假。
    完。
    返还 真。
完。

矩形交集 接受 x1、y1、w1、h1、x2、y2、w2、h2：
    若 非 做 矩形重叠 用 x1、y1、w1、h1、x2、y2、w2、h2：
        返还 空。
    完。
    可变 ix 为 x1。
    若 x2 大于 ix：
        ix 为 x2。
    完。
    可变 iy 为 y1。
    若 y2 大于 iy：
        iy 为 y2。
    完。
    可变 ix2 为 x1 加 w1。
    若 x2 加 w2 小于 ix2：
        ix2 为 x2 加 w2。
    完。
    可变 iy2 为 y1 加 h1。
    若 y2 加 h2 小于 iy2：
        iy2 为 y2 加 h2。
    完。
    返还 一组：ix、iy、ix2 减 ix、iy2 减 iy。
完。

点在圆内 接受 px、py、cx、cy、r：
    导数 为 px 减 cx。
    导数Y 为 py 减 cy。
    返还 导数 乘 导数 加 导数Y 乘 导数Y 小于或等于 r 乘 r。
完。

线段长度平方 接受 x1、y1、x2、y2：
    导数 为 x2 减 x1。
    导数Y 为 y2 减 y1。
    返还 导数 乘 导数 加 导数Y 乘 导数Y。
完。

点到线段距离平方_坐标 接受 px、py、x1、y1、x2、y2：
    导数 为 x2 减 x1。
    导数Y 为 y2 减 y1。
    len2 为 导数 乘 导数 加 导数Y 乘 导数Y。
    若 len2 等于 0：
        返还 (px 减 x1) 乘 (px 减 x1) 加 (py 减 y1) 乘 (py 减 y1)。
    完。
    t 为 ((px 减 x1) 乘 导数 加 (py 减 y1) 乘 导数Y) 乘 1000 除以 len2。
    可变 ct 为 t。
    若 ct 小于 0：
        ct 为 0。
    完。
    若 ct 大于 1000：
        ct 为 1000。
    完。
    projx 为 x1 加 ct 乘 导数 除以 1000。
    projy 为 y1 加 ct 乘 导数Y 除以 1000。
    返还 (px 减 projx) 乘 (px 减 projx) 加 (py 减 projy) 乘 (py 减 projy)。
完。

多边形_面积_顶点 接受 顶点：
    可变 面积 为 0。
    n 为 做 计数 用 顶点。
    可变 i 为 0。
    只要 i 小于 n：
        j 为 (i 加 1) 取余于 n。
        面积 为 面积 加 顶点 的 第 i 项 的 第 0 项 乘 顶点 的 第 j 项 的 第 1 项 减 顶点 的 第 j 项 的 第 0 项 乘 顶点 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    若 面积 小于 0：
        面积 为 0 减 面积。
    完。
    返还 面积 除以 2。
完。

多边形_周长_顶点 接受 顶点：
    可变 周长 为 0。
    n 为 做 计数 用 顶点。
    可变 i 为 0。
    只要 i 小于 n：
        j 为 (i 加 1) 取余于 n。
        导数 为 顶点 的 第 j 项 的 第 0 项 减 顶点 的 第 i 项 的 第 0 项。
        导数Y 为 顶点 的 第 j 项 的 第 1 项 减 顶点 的 第 i 项 的 第 1 项。
        周长 为 周长 加 做 整数平方根 用 导数 乘 导数 加 导数Y 乘 导数Y。
        i 为 i 加 1。
    完。
    返还 周长。
完。

多边形_质心 接受 顶点：
    可变 cx 为 0。
    可变 cy 为 0。
    n 为 做 计数 用 顶点。
    可变 i 为 0。
    只要 i 小于 n：
        cx 为 cx 加 顶点 的 第 i 项 的 第 0 项。
        cy 为 cy 加 顶点 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 一组：0、0。
    完。
    返还 一组：cx 除以 n、cy 除以 n。
完。

可变 _几何_计算次数 为 0。

可变 _几何_碰撞检测数 为 0。

几何_记录计算 接受 零参：
    _几何_计算次数 为 _几何_计算次数 加 1。
完。

几何_记录碰撞 接受 零参：
    _几何_碰撞检测数 为 _几何_碰撞检测数 加 1。
完。

几何_性能评估 接受 零参：
    若 _几何_碰撞检测数 大于 10000：
        返还 「高频碰撞检测(考虑空间分区)」。
    完。
    若 _几何_碰撞检测数 大于 1000：
        返还 「中频碰撞检测」。
    完。
    返还 「几何计算正常」。
完。

几何_重置 接受 零参：
    _几何_计算次数 为 0。
    _几何_碰撞检测数 为 0。
完。

本章 收束。
