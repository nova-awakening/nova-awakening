《元学习》

引用《..·基础》。

引用《..·并发·线程·线程池》。

元学习_任务_创建 接受 N、K：
    返还 一组：一组：（空）、一组：（空）、一组：（空）、一组：（空）、N、K。
完。

任务_添加支持 接受 任务、特征、标签：
    做 追加 用 任务 的 第 0 项、特征。
    做 追加 用 任务 的 第 1 项、标签。
完。

任务_添加查询 接受 任务、特征、标签：
    做 追加 用 任务 的 第 2 项、特征。
    做 追加 用 任务 的 第 3 项、标签。
完。

任务_支持数 接受 任务：
    返还 做 计数 用 任务 的 第 0 项。
完。

任务_查询数 接受 任务：
    返还 做 计数 用 任务 的 第 2 项。
完。

原型_计算 接受 任务、特征维度：
    N 为 任务 的 第 4 项。
    可变 原型 为 一组：（空）。
    可变 c 为 0。
    只要 c 小于 N：
        可变 均值 为 一组：（空）。
        可变 k 为 0。
        只要 k 小于 特征维度：
            做 追加 用 均值、0。
            k 为 k 加 1。
        完。
        可变 数量 为 0。
        可变 i 为 0。
        只要 i 小于 做 计数 用 任务 的 第 1 项：
            若 任务 的 第 1 项 的 第 i 项 等于 c：
                k 为 0。
                只要 k 小于 特征维度：
                    若 k 小于 做 计数 用 任务 的 第 0 项 的 第 i 项：
                        均值 的 第 k 项 为 均值 的 第 k 项 加 任务 的 第 0 项 的 第 i 项 的 第 k 项。
                    完。
                    k 为 k 加 1。
                完。
                数量 为 数量 加 1。
            完。
            i 为 i 加 1。
        完。
        若 数量 大于 0：
            k 为 0。
            若 数量 等于 0：
                数量 为 1。
            完。
            只要 k 小于 特征维度：
                均值 的 第 k 项 为 均值 的 第 k 项 除以 数量。
                k 为 k 加 1。
            完。
        完。
        做 追加 用 原型、均值。
        c 为 c 加 1。
    完。
    返还 原型。
完。

原型_距离 接受 a、b：
    n 为 做 计数 用 a。
    可变 距离 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 bv 为 0。
        若 i 小于 做 计数 用 b：
            bv 为 b 的 第 i 项。
        完。
        d 为 a 的 第 i 项 减 bv。
        距离 为 距离 加 d 乘 d 除以 1000。
        i 为 i 加 1。
    完。
    返还 距离。
完。

原型_分类 接受 查询特征、原型列表：
    N 为 做 计数 用 原型列表。
    可变 最近类 为 0。
    可变 最近距 为 做 原型_距离 用 查询特征、原型列表 的 第 0 项。
    可变 c 为 1。
    只要 c 小于 N：
        d 为 做 原型_距离 用 查询特征、原型列表 的 第 c 项。
        若 d 小于 最近距：
            最近类 为 c。
            最近距 为 d。
        完。
        c 为 c 加 1。
    完。
    返还 最近类。
完。

原型_评估 接受 任务、原型列表：
    查询数 为 做 计数 用 任务 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 查询数：
        预测 为 做 原型_分类 用 任务 的 第 2 项 的 第 i 项、原型列表。
        若 预测 等于 任务 的 第 3 项 的 第 i 项：
            正确 为 正确 加 1。
        完。
        i 为 i 加 1。
    完。
    若 查询数 等于 0：
        返还 0。
    完。
    返还 正确 乘 1000 除以 查询数。
完。

元学习_MAML_内循环 接受 参数、梯度、内lr、步数：
    n 为 做 计数 用 参数。
    可变 θ_prime 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 θ_prime、参数 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 步长 为 0。
    只要 步长 小于 步数：
        i 为 0。
        只要 i 小于 n：
            若 i 小于 做 计数 用 梯度：
                θ_prime 的 第 i 项 为 θ_prime 的 第 i 项 减 内lr 乘 梯度 的 第 i 项 除以 1000。
            完。
            i 为 i 加 1。
        完。
        步长 为 步长 加 1。
    完。
    返还 θ_prime。
完。

MAML_外循环更新 接受 参数、元梯度列表、外lr：
    n 为 做 计数 用 参数。
    任务数 为 做 计数 用 元梯度列表。
    若 任务数 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        可变 平均梯度 为 0。
        可变 t 为 0。
        只要 t 小于 任务数：
            若 i 小于 做 计数 用 元梯度列表 的 第 t 项：
                平均梯度 为 平均梯度 加 元梯度列表 的 第 t 项 的 第 i 项。
            完。
            t 为 t 加 1。
        完。
        平均梯度 为 平均梯度 除以 任务数。
        参数 的 第 i 项 为 参数 的 第 i 项 减 外lr 乘 平均梯度 除以 1000。
        i 为 i 加 1。
    完。
完。

Reptile_更新 接受 参数、适应参数、ε：
    n 为 做 计数 用 参数。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 做 计数 用 适应参数：
            参数 的 第 i 项 为 参数 的 第 i 项 加 ε 乘 (适应参数 的 第 i 项 减 参数 的 第 i 项) 除以 1000。
        完。
        i 为 i 加 1。
    完。
完。

Reptile_批量更新 接受 参数、适应参数列表、ε：
    n 为 做 计数 用 参数。
    任务数 为 做 计数 用 适应参数列表。
    若 任务数 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        可变 平均diff 为 0。
        可变 t 为 0。
        只要 t 小于 任务数：
            若 i 小于 做 计数 用 适应参数列表 的 第 t 项：
                平均diff 为 平均diff 加 (适应参数列表 的 第 t 项 的 第 i 项 减 参数 的 第 i 项)。
            完。
            t 为 t 加 1。
        完。
        参数 的 第 i 项 为 参数 的 第 i 项 加 ε 乘 平均diff 除以 (任务数 乘 1000)。
        i 为 i 加 1。
    完。
完。

元随机梯度_内循环 接受 参数、梯度、学习率向量：
    n 为 做 计数 用 参数。
    可变 θ_prime 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 lr 为 10。
        若 i 小于 做 计数 用 学习率向量：
            lr 为 学习率向量 的 第 i 项。
        完。
        可变 g 为 0。
        若 i 小于 做 计数 用 梯度：
            g 为 梯度 的 第 i 项。
        完。
        做 追加 用 θ_prime、参数 的 第 i 项 减 lr 乘 g 除以 1000。
        i 为 i 加 1。
    完。
    返还 θ_prime。
完。

元_随机态 为 54321。

元_随机 接受 零参：
    元_随机态 为 元_随机态 位异或 元_随机态 左移 13。
    元_随机态 为 元_随机态 位异或 元_随机态 右移 7。
    元_随机态 为 元_随机态 位异或 元_随机态 左移 17。
    若 元_随机态 小于 0：
        元_随机态 为 0 减 元_随机态。
    完。
    返还 元_随机态。
完。

任务_采样 接受 所有特征、所有标签、类别数、N、K、Q：
    任务 为 做 任务_创建 用 N、K、0。
    可变 选类 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        c 为 做 元_随机 取余于 类别数。
        做 追加 用 选类、c。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 N：
        c 为 选类 的 第 i 项。
        可变 类样本 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 做 计数 用 所有标签：
            若 所有标签 的 第 j 项 等于 c：
                做 追加 用 类样本、j。
            完。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 K 并且 j 小于 做 计数 用 类样本：
            做 任务_添加支持 用 任务、所有特征 的 第 类样本 的 第 j 项 项、i。
            j 为 j 加 1。
        完。
        只要 j 小于 K 加 Q 并且 j 小于 做 计数 用 类样本：
            做 任务_添加查询 用 任务、所有特征 的 第 类样本 的 第 j 项 项、i。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 任务。
完。

元测试_评估 接受 任务列表、特征维度：
    T 为 做 计数 用 任务列表。
    可变 总准确率 为 0。
    可变 i 为 0。
    只要 i 小于 T：
        原型 为 做 原型_计算 用 任务列表 的 第 i 项、特征维度。
        总准确率 为 总准确率 加 做 原型_评估 用 任务列表 的 第 i 项、原型。
        i 为 i 加 1。
    完。
    若 T 等于 0：
        返还 0。
    完。
    返还 总准确率 除以 T。
完。

本章 收束。
