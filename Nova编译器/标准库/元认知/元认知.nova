《元认知》

引用《..·基础》。

引用《..·数学·基础·数学库》。

引用《..·算法·数学扩展》。

引用《..·贝叶斯学习·贝叶斯学习》。

元认知_信息熵 接受 概率列表：
    可变 h 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 概率列表：
        可变 p 为 概率列表 的 第 i 项。
        若 p 大于 0：
            可变 log_p 为 0。
            若 p 大于或等于 500：
                log_p 为 (p 减 1000) 乘 1000 除以 p。
            否则：
                log_p 为 负 1000 加 p 乘 1000 除以 500。
            完。
            h 为 h 减 p 乘 log_p 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 h 除以 1000。
完。

MC_不确定性 接受 预测列表：
    n 为 做 计数 用 预测列表。
    可变 方差和 为 0。
    若 n 等于 0：
        返还 0。
    完。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总和 为 总和 加 预测列表 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 总和 除以 n。
    i 为 0。
    只要 i 小于 n：
        可变 偏差 为 预测列表 的 第 i 项 减 均值。
        方差和 为 方差和 加 偏差 乘 偏差 除以 1000。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 方差和 除以 n。
完。

集成_不确定性 接受 预测矩阵、n_models、维度：
    可变 总var 为 0。
    可变 d 为 0。
    只要 d 小于 维度：
        可变 均值 为 0。
        可变 m 为 0。
        只要 m 小于 n_models：
            均值 为 均值 加 预测矩阵 的 第 m 乘 维度 加 d 项。
            m 为 m 加 1。
        完。
        若 n_models 等于 0：
            n_models 为 1。
        完。
        均值 为 均值 除以 n_models。
        可变 方差向量 为 0。
        m 为 0。
        只要 m 小于 n_models：
            可变 差值 为 预测矩阵 的 第 m 乘 维度 加 d 项 减 均值。
            方差向量 为 方差向量 加 差值 乘 差值 除以 1000。
            m 为 m 加 1。
        完。
        总var 为 总var 加 方差向量 除以 n_models。
        d 为 d 加 1。
    完。
    若 维度 等于 0：
        返还 0。
    完。
    返还 总var 除以 维度。
完。

_量化指数 接受 x：
    若 x 小于 负 6000：
        返还 2。
    完。
    若 x 大于 6000：
        返还 403000。
    完。
    若 x 大于 1000：
        可变 一半 为 做 _量化指数 用 x 除以 2。
        返还 一半 乘 一半 除以 1000。
    完。
    若 x 小于 负 1000：
        可变 一半 为 做 _量化指数 用 x 除以 2。
        返还 一半 乘 一半 除以 1000。
    完。
    可变 t1 为 x。
    可变 t2 为 x 乘 x 除以 2000。
    可变 t3 为 x 乘 x 除以 1000 乘 x 除以 6000。
    可变 r 为 1000 加 t1 加 t2 加 t3。
    若 r 小于 1：
        r 为 1。
    完。
    返还 r。
完。

Platt缩放 接受 logit、a、b：
    可变 z 为 a 乘 logit 除以 1000 加 b。
    可变 ez 为 做 _量化指数 用 0 减 z。
    返还 1000 乘 1000 除以 (1000 加 ez)。
完。

期望校准误差 接受 置信度列表、准确列表、n_bins：
    可变 bin_acc 为 一组：（空）。
    可变 bin_conf 为 一组：（空）。
    可变 bin_cnt 为 一组：（空）。
    可变 bi 为 0。
    只要 bi 小于 n_bins：
        做 追加 用 bin_acc、0。
        做 追加 用 bin_conf、0。
        做 追加 用 bin_cnt、0。
        bi 为 bi 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 做 计数 用 置信度列表：
        可变 bin 为 置信度列表 的 第 i 项 乘 n_bins 除以 1001。
        若 bin 大于或等于 n_bins：
            bin 为 n_bins 减 1。
        完。
        bin_cnt 的 第 bin 项 为 bin_cnt 的 第 bin 项 加 1。
        bin_conf 的 第 bin 项 为 bin_conf 的 第 bin 项 加 置信度列表 的 第 i 项。
        bin_acc 的 第 bin 项 为 bin_acc 的 第 bin 项 加 准确列表 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 合计 为 做 计数 用 置信度列表。
    若 合计 等于 0：
        返还 0。
    完。
    可变 ece 为 0。
    bi 为 0。
    只要 bi 小于 n_bins：
        若 bin_cnt 的 第 bi 项 大于 0：
            可变 avg_acc 为 bin_acc 的 第 bi 项 乘 1000 除以 bin_cnt 的 第 bi 项。
            可变 avg_conf 为 bin_conf 的 第 bi 项 除以 bin_cnt 的 第 bi 项。
            ece 为 ece 加 做 绝对值 用 avg_acc 减 avg_conf 乘 bin_cnt 的 第 bi 项 除以 合计。
        完。
        bi 为 bi 加 1。
    完。
    返还 ece。
完。

可变 监控_历史 为 一组：（空）。

监控_异常阈值 为 500。

监控_记录 接受 值：
    做 追加 用 监控_历史、值。
完。

监控_异常检测 接受 零参：
    n 为 做 计数 用 监控_历史。
    可变 方差和 为 0。
    若 n 小于 3：
        返还 假。
    完。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总和 为 总和 加 监控_历史 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 总和 除以 n。
    i 为 0。
    只要 i 小于 n：
        可变 偏差 为 监控_历史 的 第 i 项 减 均值。
        方差和 为 方差和 加 偏差 乘 偏差。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 一组：0、0。
    完。
    方差向量 为 方差和 除以 n。
    可变 latest 为 监控_历史 的 第 n 减 1 项。
    可变 dev 为 做 绝对值 用 latest 减 均值。
    返还 dev 乘 dev 大于 方差向量 乘 监控_异常阈值 除以 1000。
完。

监控_移动平均 接受 窗口：
    可变 n 为 做 计数 用 监控_历史。
    若 n 等于 0：
        返还 0。
    完。
    可变 起 为 n 减 窗口。
    若 起 小于 0：
        起 为 0。
    完。
    可变 总和向量 为 0。
    可变 计数值 为 0。
    只要 起 小于 n：
        总和向量 为 总和向量 加 监控_历史 的 第 起 项。
        起 为 起 加 1。
        计数值 为 计数值 加 1。
    完。
    若 计数值 等于 0：
        返还 0。
    完。
    返还 总和向量 除以 计数值。
完。

可变 认知_任务数 为 0。

可变 认知_复杂度 为 0。

认知_容量 为 1000。

认知_初始化 接受 容量：
    认知_任务数 为 0。
    认知_复杂度 为 0。
    认知_容量 为 容量。
完。

认知_添加任务 接受 复杂度：
    认知_任务数 为 认知_任务数 加 1。
    认知_复杂度 为 认知_复杂度 加 复杂度。
完。

认知_完成任务 接受 复杂度：
    若 认知_任务数 大于 0：
        认知_任务数 为 认知_任务数 减 1。
    完。
    认知_复杂度 为 认知_复杂度 减 复杂度。
    若 认知_复杂度 小于 0：
        认知_复杂度 为 0。
    完。
完。

认知_负载率 接受 零参：
    若 认知_容量 小于或等于 0：
        返还 0。
    完。
    返还 认知_复杂度 乘 1000 除以 认知_容量。
完。

认知_过载 接受 零参：
    返还 认知_复杂度 大于 认知_容量。
完。

策略_贪心 接受 分数列表：
    可变 最优 为 0。
    可变 最优得分 为 分数列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 分数列表：
        若 分数列表 的 第 i 项 大于 最优得分：
            最优得分 为 分数列表 的 第 i 项。
            最优 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最优。
完。

策略_epsilon_贪心 接受 分数列表、极小值、种子：
    可变 随机值 为 (种子 乘 1103 加 12345) 取余于 1000。
    若 随机值 小于 极小值：
        返还 (种子 乘 7 加 13) 取余于 做 计数 用 分数列表。
    完。
    返还 做 策略_贪心 用 分数列表。
完。

策略_UCB 接受 均值列表、次数列表、总次数、c：
    可变 最优 为 0。
    可变 最优得分 为 负 99999。
    可变 i 为 0。
    只要 i 小于 做 计数 用 均值列表：
        可变 置信上限 为 0。
        若 次数列表 的 第 i 项 等于 0：
            置信上限 为 99999。
        否则：
            可变 explore 为 c 乘 做 整数sqrt 用 总次数 乘 1000 除以 次数列表 的 第 i 项 除以 1000。
            置信上限 为 均值列表 的 第 i 项 加 explore。
        完。
        若 置信上限 大于 最优得分：
            最优得分 为 置信上限。
            最优 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最优。
完。

策略_softmax 接受 分数列表、tau、种子：
    可变 probs 为 做 温度缩放 用 分数列表、tau。
    可变 随机值 为 (种子 乘 1103 加 12345) 取余于 1000。
    可变 累积 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 probs：
        累积 为 累积 加 probs 的 第 i 项。
        若 随机值 小于 累积：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 做 计数 用 分数列表 减 1。
完。

监控_历史长度 接受 零参：
    返还 做 计数 用 监控_历史。
完。

本章 收束。
