《知识图谱》

引用《..·基础》。

引用《..·算法·数学扩展》。

可变 KG_三元组 为 一组：（空）。

可变 KG_实体名 为 一组：（空）。

可变 KG_关系名 为 一组：（空）。

可变 KG_下一实体 为 0。

可变 KG_下一关系 为 0。

KG_重置 接受 零参：
    KG_三元组 为 一组：（空）。
    KG_实体名 为 一组：（空）。
    KG_关系名 为 一组：（空）。
    KG_下一实体 为 0。
    KG_下一关系 为 0。
完。

KG_添加实体 接受 名哈希：
    id 为 KG_下一实体。
    KG_下一实体 为 KG_下一实体 加 1。
    做 追加 用 KG_实体名、名哈希。
    返还 id。
完。

KG_添加关系 接受 名哈希：
    id 为 KG_下一关系。
    KG_下一关系 为 KG_下一关系 加 1。
    做 追加 用 KG_关系名、名哈希。
    返还 id。
完。

KG_添加三元组 接受 主语、谓语、宾语：
    做 追加 用 KG_三元组、一组：主语、谓语、宾语。
完。

KG_实体数 接受 零参：
    返还 KG_下一实体。
完。

KG_关系数 接受 零参：
    返还 KG_下一关系。
完。

KG_三元组数 接受 零参：
    返还 做 计数 用 KG_三元组。
完。

KG_查询_主语 接受 主语：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 KG_三元组：
        若 KG_三元组 的 第 i 项 的 第 0 项 等于 主语：
            做 追加 用 结果、KG_三元组 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

KG_查询_宾语 接受 宾语：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 KG_三元组：
        若 KG_三元组 的 第 i 项 的 第 2 项 等于 宾语：
            做 追加 用 结果、KG_三元组 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

KG_查询_关系 接受 谓语：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 KG_三元组：
        若 KG_三元组 的 第 i 项 的 第 1 项 等于 谓语：
            做 追加 用 结果、KG_三元组 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

KG_查询_精确 接受 主语、谓语、宾语：
    可变 i 为 0。
    只要 i 小于 做 计数 用 KG_三元组：
        t 为 KG_三元组 的 第 i 项。
        若 t 的 第 0 项 等于 主语 并且 t 的 第 1 项 等于 谓语 并且 t 的 第 2 项 等于 宾语：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

KG_出邻居 接受 实体：
    可变 邻居 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 KG_三元组：
        若 KG_三元组 的 第 i 项 的 第 0 项 等于 实体：
            做 追加 用 邻居、KG_三元组 的 第 i 项 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
    返还 邻居。
完。

KG_入邻居 接受 实体：
    可变 邻居 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 KG_三元组：
        若 KG_三元组 的 第 i 项 的 第 2 项 等于 实体：
            做 追加 用 邻居、KG_三元组 的 第 i 项 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 邻居。
完。

KG_度 接受 实体：
    返还 做 计数 用 做 KG_出邻居 用 实体 加 做 计数 用 做 KG_入邻居 用 实体。
完。

KG_路径存在 接受 起点、终点、最大深度：
    可变 下一层 为 一组：（空）。
    若 起点 等于 终点：
        返还 真。
    完。
    可变 队列 为 一组：起点。
    可变 已访问 为 一组：（空）。
    做 追加 用 已访问、起点。
    可变 深度 为 0。
    只要 深度 小于 最大深度 并且 做 计数 用 队列 大于 0：
        可变 qi 为 0。
        只要 qi 小于 做 计数 用 队列：
            邻居 为 做 KG_出邻居 用 队列 的 第 qi 项。
            可变 ni 为 0。
            只要 ni 小于 做 计数 用 邻居：
                若 邻居 的 第 ni 项 等于 终点：
                    返还 真。
                完。
                可变 已有 为 假。
                可变 vi 为 0。
                只要 vi 小于 做 计数 用 已访问：
                    若 已访问 的 第 vi 项 等于 邻居 的 第 ni 项：
                        已有 为 真。
                    完。
                    vi 为 vi 加 1。
                完。
                若 已有 等于 假：
                    做 追加 用 已访问、邻居 的 第 ni 项。
                    做 追加 用 下一层、邻居 的 第 ni 项。
                完。
                ni 为 ni 加 1。
            完。
            qi 为 qi 加 1。
        完。
        队列 为 下一层。
        深度 为 深度 加 1。
    完。
    返还 假。
完。

KG_子图 接受 实体列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 KG_三元组：
        t 为 KG_三元组 的 第 i 项。
        可变 主在 为 假。
        可变 宾在 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 实体列表：
            若 实体列表 的 第 j 项 等于 t 的 第 0 项：
                主在 为 真。
            完。
            若 实体列表 的 第 j 项 等于 t 的 第 2 项：
                宾在 为 真。
            完。
            j 为 j 加 1。
        完。
        若 主在 并且 宾在：
            做 追加 用 结果、t。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 知识_嵌入向量 为 一组：（空）。

嵌入_设置 接受 实体ID、向量：
    若 实体ID 大于或等于 0 并且 实体ID 小于 做 计数 用 知识_嵌入向量：
        知识_嵌入向量 的 第 实体ID 项 为 向量。
    完。
完。

嵌入_余弦相似 接受 a、b：
    可变 点积 为 0。
    可变 na 为 0。
    可变 nb 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a：
        点积 为 点积 加 a 的 第 i 项 乘 b 的 第 i 项。
        na 为 na 加 a 的 第 i 项 乘 a 的 第 i 项。
        nb 为 nb 加 b 的 第 i 项 乘 b 的 第 i 项。
        i 为 i 加 1。
    完。
    若 na 等于 0 或者 nb 等于 0：
        返还 0。
    完。
    可变 sna 为 做 整数sqrt 用 na。
    可变 snb 为 做 整数sqrt 用 nb。
    若 sna 等于 0：
        sna 为 1。
    完。
    若 snb 等于 0：
        snb 为 1。
    完。
    返还 点积 乘 1000 除以 (sna 乘 snb)。
完。

知识图谱_嵌入_最近邻 接受 查询向量、k：
    可变 分数 为 一组：（空）。
    可变 ri 为 0。
    可变 选了 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 知识_嵌入向量：
        做 追加 用 分数、一组：i、做 嵌入_余弦相似 用 查询向量、知识_嵌入向量 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    只要 选了 小于 k 并且 选了 小于 做 计数 用 分数：
        可变 最佳 为 负 1。
        可变 最佳分 为 负 99999。
        可变 j 为 0。
        只要 j 小于 做 计数 用 分数：
            若 分数 的 第 j 项 的 第 1 项 大于 最佳分：
                可变 已选 为 假。
                只要 ri 小于 做 计数 用 结果：
                    若 结果 的 第 ri 项 等于 分数 的 第 j 项 的 第 0 项：
                        已选 为 真。
                    完。
                    ri 为 ri 加 1。
                完。
                若 已选 等于 假：
                    最佳 为 j。
                    最佳分 为 分数 的 第 j 项 的 第 1 项。
                完。
            完。
            j 为 j 加 1。
        完。
        若 最佳 大于或等于 0：
            做 追加 用 结果、分数 的 第 最佳 项 的 第 0 项。
        完。
        选了 为 选了 加 1。
    完。
    返还 结果。
完。

推理_一轮 接受 引擎、kg：
    规则 为 引擎 的 第 0 项。
    可变 ri 为 0。
    可变 新增 为 0。
    只要 ri 小于 做 计数 用 规则：
        规则项 为 规则 的 第 ri 项。
        前提 为 规则项 的 第 0 项。
        结论 为 规则项 的 第 1 项。
        若 做 计数 用 前提 等于 1：
            p 为 前提 的 第 0 项。
            匹配结果 为 做 知识_按关系 用 kg、p 的 第 1 项。
            可变 mi 为 0。
            只要 mi 小于 做 计数 用 匹配结果：
                新主 为 匹配结果 的 第 mi 项 的 第 0 项。
                新客 为 匹配结果 的 第 mi 项 的 第 2 项。
                已有 为 做 知识_获取客体 用 kg、新主、结论 的 第 1 项。
                若 做 计数 用 已有 等于 0：
                    做 知识_追加 用 kg、新主、结论 的 第 1 项、新客。
                    新增 为 新增 加 1。
                完。
                mi 为 mi 加 1。
            完。
        完。
        ri 为 ri 加 1。
    完。
    返还 新增。
完。

推理_完全 接受 引擎、kg：
    可变 总新增 为 0。
    可变 轮数 为 0。
    可变 _推理完 为 假。
    只要 _推理完 等于 假：
        新增 为 做 推理_一轮 用 引擎、kg。
        若 新增 等于 0：
            _推理完 为 真。
        否则：
            总新增 为 总新增 加 新增。
            轮数 为 轮数 加 1。
            若 轮数 大于 100：
                _推理完 为 真。
            完。
        完。
    完。
    返还 总新增。
完。

推理_添加规则 接受 引擎、前提、结论：
    做 追加 用 引擎 的 第 0 项、一组：前提、结论。
完。

推理引擎_创建 接受 零参：
    返还 一组：一组：（空）。
完。

知识_关系列表 接受 kg：
    可变 关系 为 做 字典。
    可变 i 为 0。
    只要 i 小于 kg 的 第 3 项：
        做 字典设 用 关系、做 转字符串 用 kg 的 第 0 项 的 第 i 项 的 第 1 项、1。
        i 为 i 加 1。
    完。
    返还 关系。
完。

知识_反向查询 接受 kg、客体、关系：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 kg 的 第 3 项：
        t 为 kg 的 第 0 项 的 第 i 项。
        若 t 的 第 1 项 等于 关系 并且 t 的 第 2 项 等于 客体：
            做 追加 用 结果、t 的 第 0 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

知识_大小 接受 kg：
    返还 kg 的 第 3 项。
完。

知识_实体列表 接受 kg：
    可变 实体 为 做 字典。
    可变 i 为 0。
    只要 i 小于 kg 的 第 3 项：
        做 字典设 用 实体、做 转字符串 用 kg 的 第 0 项 的 第 i 项 的 第 0 项、1。
        做 字典设 用 实体、做 转字符串 用 kg 的 第 0 项 的 第 i 项 的 第 2 项、1。
        i 为 i 加 1。
    完。
    返还 实体。
完。

知识_按主体 接受 kg、主体：
    索引列表 为 做 字典取 用 kg 的 第 1 项、做 转字符串 用 主体。
    若 索引列表 等于 空：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 索引列表：
        做 追加 用 结果、kg 的 第 0 项 的 第 索引列表 的 第 i 项 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

知识_按关系 接受 kg、关系：
    索引列表 为 做 字典取 用 kg 的 第 2 项、做 转字符串 用 关系。
    若 索引列表 等于 空：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 索引列表：
        做 追加 用 结果、kg 的 第 0 项 的 第 索引列表 的 第 i 项 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

知识_追加 接受 kg、主体、关系、客体：
    做 追加 用 kg 的 第 0 项、一组：主体、关系、客体。
    主键 为 做 转字符串 用 主体。
    现有主 为 做 字典取 用 kg 的 第 1 项、主键。
    若 现有主 等于 空：
        做 字典设 用 kg 的 第 1 项、主键、一组：kg 的 第 3 项。
    否则：
        做 追加 用 现有主、kg 的 第 3 项。
    完。
    关键 为 做 转字符串 用 关系。
    现有关 为 做 字典取 用 kg 的 第 2 项、关键。
    若 现有关 等于 空：
        做 字典设 用 kg 的 第 2 项、关键、一组：kg 的 第 3 项。
    否则：
        做 追加 用 现有关、kg 的 第 3 项。
    完。
    kg 的 第 3 项 为 kg 的 第 3 项 加 1。
完。

知识_获取客体 接受 kg、主体、关系：
    三元组 为 做 知识_按主体 用 kg、主体。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 三元组：
        若 三元组 的 第 i 项 的 第 1 项 等于 关系：
            做 追加 用 结果、三元组 的 第 i 项 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

知识_语义相似 接受 kg、实体a、实体b：
    邻居a 为 做 知识_按主体 用 kg、实体a。
    邻居b 为 做 知识_按主体 用 kg、实体b。
    可变 共同 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 邻居a：
        可变 j 为 0。
        只要 j 小于 做 计数 用 邻居b：
            若 邻居a 的 第 i 项 的 第 2 项 等于 邻居b 的 第 j 项 的 第 2 项 并且 邻居a 的 第 i 项 的 第 1 项 等于 邻居b 的 第 j 项 的 第 1 项：
                共同 为 共同 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    总 为 做 计数 用 邻居a 加 做 计数 用 邻居b。
    若 总 等于 0：
        返还 0。
    完。
    返还 共同 乘 2 乘 1000 除以 总。
完。

知识_路径 接受 kg、起点、终点、最大深度：
    可变 已访问 为 做 字典。
    可变 队列 为 一组：一组：起点、一组：（空）。
    可变 前 为 0。
    做 字典设 用 已访问、做 转字符串 用 起点、1。
    只要 前 小于 做 计数 用 队列：
        当前 为 队列 的 第 前 项。
        前 为 前 加 1。
        节点 为 当前 的 第 0 项。
        路径 为 当前 的 第 1 项。
        若 做 计数 用 路径 大于或等于 最大深度：
            续。
        完。
        三元组 为 做 知识_按主体 用 kg、节点。
        可变 i 为 0。
        只要 i 小于 做 计数 用 三元组：
            客体 为 三元组 的 第 i 项 的 第 2 项。
            关系 为 三元组 的 第 i 项 的 第 1 项。
            若 客体 等于 终点：
                可变 完整路径 为 一组：（空）。
                可变 j 为 0。
                只要 j 小于 做 计数 用 路径：
                    做 追加 用 完整路径、路径 的 第 j 项。
                    j 为 j 加 1。
                完。
                做 追加 用 完整路径、一组：节点、关系、客体。
                返还 完整路径。
            完。
            若 做 字典取 用 已访问、做 转字符串 用 客体 等于 空：
                做 字典设 用 已访问、做 转字符串 用 客体、1。
                可变 新路径 为 一组：（空）。
                可变 k 为 0。
                只要 k 小于 做 计数 用 路径：
                    做 追加 用 新路径、路径 的 第 k 项。
                    k 为 k 加 1。
                完。
                做 追加 用 新路径、一组：节点、关系、客体。
                做 追加 用 队列、一组：客体、新路径。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 空。
完。

知识图谱_创建 接受 零参：
    返还 一组：一组：（空）、做 字典、做 字典、0。
完。

可变 _kg_节点数 为 0。

可变 _kg_边数 为 0。

可变 _kg_查询数 为 0。

kg_记录节点 接受 零参：
    _kg_节点数 为 _kg_节点数 加 1。
完。

kg_记录边 接受 零参：
    _kg_边数 为 _kg_边数 加 1。
完。

kg_记录查询 接受 零参：
    _kg_查询数 为 _kg_查询数 加 1。
完。

kg_密度 接受 零参：
    若 _kg_节点数 小于 2：
        返还 0。
    完。
    最大边 为 _kg_节点数 乘 (_kg_节点数 减 1)。
    若 最大边 等于 0：
        返还 0。
    完。
    返还 _kg_边数 乘 100 除以 最大边。
完。

kg_连通性评估 接受 零参：
    若 _kg_节点数 等于 0：
        返还 「空图」。
    完。
    平均度 为 _kg_边数 乘 2 除以 _kg_节点数。
    若 平均度 大于 10：
        返还 「高连通(知识密集)」。
    完。
    若 平均度 大于 3：
        返还 「中连通」。
    完。
    若 平均度 大于 1：
        返还 「低连通(知识稀疏)」。
    完。
    返还 「极低连通(可能有孤立节点)」。
完。

kg_质量评估 接受 零参：
    密度 为 做 kg_密度。
    连通 为 做 kg_连通性评估。
    返还 一组：密度、连通、_kg_节点数、_kg_边数。
完。

kg_推理潜力 接受 零参：
    若 _kg_边数 小于 10：
        返还 「推理能力有限」。
    完。
    若 _kg_边数 小于 100：
        返还 「基础推理可行」。
    完。
    若 _kg_边数 小于 1000：
        返还 「中等推理能力」。
    完。
    返还 「丰富推理能力」。
完。

kg_规模分类 接受 零参：
    若 _kg_节点数 大于 10000：
        返还 「大规模知识图谱」。
    完。
    若 _kg_节点数 大于 1000：
        返还 「中规模知识图谱」。
    完。
    若 _kg_节点数 大于 100：
        返还 「小规模知识图谱」。
    完。
    返还 「微型知识图谱」。
完。

kg_存储估算 接受 零参：
    返还 _kg_节点数 乘 64 加 _kg_边数 乘 32。
完。

本章 收束。
