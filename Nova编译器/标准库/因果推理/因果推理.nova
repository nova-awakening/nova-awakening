《因果推理》

引用《..·基础》。

因果_观测 为 0。

因果_处理 为 1。

因果_结果 为 2。

因果_混杂 为 3。

因果_工具 为 4。

因果图_创建 接受 节点数：
    可变 邻接 为 一组：（空）。
    可变 名 为 一组：（空）。
    可变 类型 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点数：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 节点数：
            做 追加 用 行、0。
            j 为 j 加 1。
        完。
        做 追加 用 邻接、行。
        做 追加 用 名、i。
        做 追加 用 类型、因果_观测。
        i 为 i 加 1。
    完。
    返还 一组：节点数、邻接、名、类型。
完。

因果图_添加边 接受 图、原因、结果：
    图 的 第 1 项 的 第 原因 项 的 第 结果 项 为 1。
完。

因果图_设类型 接受 图、节点、类型：
    图 的 第 3 项 的 第 节点 项 为 类型。
完。

因果图_是父 接受 图、父、子：
    返还 图 的 第 1 项 的 第 父 项 的 第 子 项 等于 1。
完。

因果图_父集 接受 图、节点：
    可变 父列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 图 的 第 0 项：
        若 图 的 第 1 项 的 第 i 项 的 第 节点 项 等于 1：
            做 追加 用 父列表、i。
        完。
        i 为 i 加 1。
    完。
    返还 父列表。
完。

因果图_子集 接受 图、节点：
    可变 子列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 图 的 第 0 项：
        若 图 的 第 1 项 的 第 节点 项 的 第 i 项 等于 1：
            做 追加 用 子列表、i。
        完。
        i 为 i 加 1。
    完。
    返还 子列表。
完。

因果图_介入 接受 图、节点：
    N 为 图 的 第 0 项。
    可变 新邻接 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 N：
            做 追加 用 行、图 的 第 1 项 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 新邻接、行。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 N：
        新邻接 的 第 i 项 的 第 节点 项 为 0。
        i 为 i 加 1。
    完。
    返还 一组：N、新邻接、图 的 第 2 项、图 的 第 3 项。
完。

ATE_简单 接受 Y_处理组、Y_对照组：
    n1 为 做 计数 用 Y_处理组。
    n0 为 做 计数 用 Y_对照组。
    若 n1 等于 0 或者 n0 等于 0：
        返还 0。
    完。
    可变 和1 为 0。
    可变 和0 为 0。
    可变 i 为 0。
    只要 i 小于 n1：
        和1 为 和1 加 Y_处理组 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n0：
        和0 为 和0 加 Y_对照组 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和1 除以 n1 减 和0 除以 n0。
完。

倾向得分_计算 接受 特征列表、权重、偏置：
    n 为 做 计数 用 特征列表。
    可变 得分 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 logit 为 偏置。
        可变 j 为 0。
        只要 j 小于 做 计数 用 特征列表 的 第 i 项：
            若 j 小于 做 计数 用 权重：
                logit 为 logit 加 权重 的 第 j 项 乘 特征列表 的 第 i 项 的 第 j 项 除以 1000。
            完。
            j 为 j 加 1。
        完。
        可变 信号值 为 500 加 logit 除以 4。
        若 信号值 小于 0：
            信号值 为 0。
        完。
        若 信号值 大于 1000：
            信号值 为 1000。
        完。
        做 追加 用 得分、信号值。
        i 为 i 加 1。
    完。
    返还 得分。
完。

倾向得分_匹配 接受 得分、处理标签、k：
    n 为 做 计数 用 得分。
    可变 配对结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 处理标签 的 第 i 项 等于 1：
            可变 最近 为 一组：（空）。
            可变 j 为 0。
            只要 j 小于 n：
                若 处理标签 的 第 j 项 等于 0：
                    可变 距离 为 得分 的 第 i 项 减 得分 的 第 j 项。
                    若 距离 小于 0：
                        距离 为 0 减 距离。
                    完。
                    做 追加 用 最近、一组：j、距离。
                完。
                j 为 j 加 1。
            完。
            可变 m 为 0。
            只要 m 小于 k 并且 m 小于 做 计数 用 最近 减 1：
                可变 mi 为 m 加 1。
                只要 mi 小于 做 计数 用 最近：
                    若 最近 的 第 mi 项 的 第 1 项 小于 最近 的 第 m 项 的 第 1 项：
                        临时 为 最近 的 第 m 项。
                        最近 的 第 m 项 为 最近 的 第 mi 项。
                        最近 的 第 mi 项 为 临时。
                    完。
                    mi 为 mi 加 1。
                完。
                m 为 m 加 1。
            完。
            可变 匹配组 为 一组：（空）。
            m 为 0。
            只要 m 小于 k 并且 m 小于 做 计数 用 最近：
                做 追加 用 匹配组、最近 的 第 m 项 的 第 0 项。
                m 为 m 加 1。
            完。
            做 追加 用 配对结果、一组：i、匹配组。
        完。
        i 为 i 加 1。
    完。
    返还 配对结果。
完。

逆概率加权平均处理效应 接受 Y、处理、倾向得分：
    n 为 做 计数 用 Y。
    可变 加权处理 为 0。
    可变 加权对照 为 0。
    可变 权重处理 为 0。
    可变 权重对照 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 ps 为 倾向得分 的 第 i 项。
        若 ps 小于 10：
            ps 为 10。
        完。
        若 ps 大于 990：
            ps 为 990。
        完。
        若 处理 的 第 i 项 等于 1：
            w 为 1000 乘 1000 除以 ps。
            加权处理 为 加权处理 加 Y 的 第 i 项 乘 w 除以 1000。
            权重处理 为 权重处理 加 w。
        否则：
            w 为 1000 乘 1000 除以 (1000 减 ps)。
            加权对照 为 加权对照 加 Y 的 第 i 项 乘 w 除以 1000。
            权重对照 为 权重对照 加 w。
        完。
        i 为 i 加 1。
    完。
    若 权重处理 等于 0：
        权重处理 为 1。
    完。
    若 权重对照 等于 0：
        权重对照 为 1。
    完。
    返还 加权处理 乘 1000 除以 权重处理 减 加权对照 乘 1000 除以 权重对照。
完。

格兰杰_自回归 接受 序列、阶数：
    n 为 做 计数 用 序列。
    若 n 小于或等于 阶数：
        返还 0。
    完。
    可变 误差 为 0。
    可变 计数值 为 0。
    可变 t 为 阶数。
    只要 t 小于 n：
        可变 预测 为 0。
        可变 i 为 0。
        只要 i 小于 阶数：
            预测 为 预测 加 序列 的 第 t 减 i 减 1 项 除以 阶数。
            i 为 i 加 1。
        完。
        e 为 序列 的 第 t 项 减 预测。
        误差 为 误差 加 e 乘 e 除以 1000。
        计数值 为 计数值 加 1。
        t 为 t 加 1。
    完。
    若 计数值 等于 0：
        返还 0。
    完。
    返还 误差 除以 计数值。
完。

格兰杰_检验 接受 X序列、Y序列、阶数：
    受限误差 为 做 格兰杰_自回归 用 Y序列、阶数。
    n 为 做 计数 用 Y序列。
    可变 非受限误差 为 0。
    可变 计数值 为 0。
    可变 t 为 阶数。
    只要 t 小于 n：
        可变 预测 为 0。
        可变 i 为 0。
        只要 i 小于 阶数：
            预测 为 预测 加 Y序列 的 第 t 减 i 减 1 项 除以 (阶数 乘 2)。
            若 t 减 i 减 1 小于 做 计数 用 X序列：
                预测 为 预测 加 X序列 的 第 t 减 i 减 1 项 除以 (阶数 乘 2)。
            完。
            i 为 i 加 1。
        完。
        e 为 Y序列 的 第 t 项 减 预测。
        非受限误差 为 非受限误差 加 e 乘 e 除以 1000。
        计数值 为 计数值 加 1。
        t 为 t 加 1。
    完。
    若 计数值 等于 0：
        返还 0。
    完。
    非受限误差 为 非受限误差 除以 计数值。
    若 非受限误差 等于 0：
        返还 0。
    完。
    返还 (受限误差 减 非受限误差) 乘 1000 除以 非受限误差。
完。

反事实_估计 接受 实际结果、处理效应、是否处理：
    若 是否处理 等于 1：
        返还 实际结果 减 处理效应。
    否则：
        返还 实际结果 加 处理效应。
    完。
完。

中介_直接效应 接受 总效应、间接效应：
    返还 总效应 减 间接效应。
完。

中介_间接效应 接受 T对M系数、M对Y系数：
    返还 T对M系数 乘 M对Y系数 除以 1000。
完。

中介_比例 接受 间接效应、总效应：
    若 总效应 等于 0：
        返还 0。
    完。
    返还 间接效应 乘 1000 除以 总效应。
完。

本章 收束。
