《神经元多样性》

引用《..·基础》。

ND精度 为 10000。

类型_锥体 为 1。

类型_星形 为 2。

类型_篮状 为 3。

类型_Martinotti 为 4。

类型_枝形 为 5。

类型_双束 为 6。

类型_PV 为 7。

类型_SST 为 8。

类型_VIP 为 9。

类型_多巴胺 为 10。

类型_胆碱 为 11。

类型_小脑颗粒 为 12。

类型_浦肯野 为 13。

多样神经元_创建 接受 类型、标识：
    可变 参数 为 做 神经元参数_按类型 用 类型。
    返还 一组：标识、类型、参数 的 第 0 项、参数 的 第 0 项、参数 的 第 1 项、参数 的 第 2 项、参数 的 第 3 项、0、参数 的 第 4 项、参数 的 第 5 项、0、0、参数 的 第 6 项、0、参数 的 第 7 项、0。
完。

神经元参数_按类型 接受 类型：
    若 类型 等于 类型_锥体：
        返还 一组：负 7000、负 5500、9500、20、1、ND精度、500、3。
    完。
    若 类型 等于 类型_星形：
        返还 一组：负 6500、负 5000、9200、15、1、8000、300、5。
    完。
    若 类型 等于 类型_篮状：
        返还 一组：负 7000、负 4500、9800、5、负 1、ND精度 加 2000、100、0。
    完。
    若 类型 等于 类型_Martinotti：
        返还 一组：负 7500、负 5000、9000、25、负 1、6000、800、0。
    完。
    若 类型 等于 类型_PV：
        返还 一组：负 6800、负 4000、9900、3、负 1、ND精度 加 5000、50、0。
    完。
    若 类型 等于 类型_SST：
        返还 一组：负 7200、负 5200、9300、20、负 1、7000、600、0。
    完。
    若 类型 等于 类型_VIP：
        返还 一组：负 7000、负 5000、9400、15、负 1、5000、400、0。
    完。
    若 类型 等于 类型_多巴胺：
        返还 一组：负 6000、负 4500、9600、10、1、3000、200、4。
    完。
    若 类型 等于 类型_胆碱：
        返还 一组：负 6500、负 5000、9500、12、1、4000、300、3。
    完。
    若 类型 等于 类型_浦肯野：
        返还 一组：负 6800、负 4800、9700、8、负 1、ND精度 加 3000、150、2。
    完。
    返还 一组：负 7000、负 5500、9500、20、1、ND精度、500、3。
完。

多样神经元_步进 接受 神经元、输入电流、调制信号：
    类型 为 神经元 的 第 1 项。
    V 为 神经元 的 第 3 项。
    静息 为 神经元 的 第 2 项。
    阈值 为 神经元 的 第 4 项。
    τ 为 神经元 的 第 5 项。
    不应 为 神经元 的 第 7 项。
    适应 为 神经元 的 第 13 项。
    若 不应 大于 0：
        神经元 的 第 7 项 为 不应 减 1。
        神经元 的 第 3 项 为 静息 减 2000。
        返还 0。
    完。
    泄漏 为 (V 减 静息) 乘 τ 除以 ND精度。
    可变 新V 为 静息 加 泄漏 加 输入电流 减 适应。
    若 调制信号 不等于 0：
        若 类型 等于 类型_锥体 或者 类型 等于 类型_星形：
            新V 为 新V 加 调制信号 乘 2000 除以 ND精度。
        完。
        若 类型 等于 类型_PV 或者 类型 等于 类型_篮状：
            新V 为 新V 加 调制信号 乘 1000 除以 ND精度。
        完。
    完。
    若 新V 大于或等于 阈值：
        神经元 的 第 3 项 为 静息 减 3000。
        神经元 的 第 7 项 为 神经元 的 第 6 项。
        神经元 的 第 11 项 为 神经元 的 第 11 项 加 1。
        神经元 的 第 13 项 为 适应 加 神经元 的 第 12 项。
        若 神经元 的 第 14 项 大于 0：
            神经元 的 第 15 项 为 神经元 的 第 15 项 加 1。
            若 神经元 的 第 15 项 大于或等于 神经元 的 第 14 项：
                神经元 的 第 15 项 为 0。
                神经元 的 第 7 项 为 神经元 的 第 6 项 乘 3。
            完。
        完。
        返还 神经元 的 第 8 项 乘 神经元 的 第 9 项 除以 ND精度。
    否则：
        神经元 的 第 3 项 为 新V。
        神经元 的 第 13 项 为 适应 乘 9800 除以 ND精度。
        返还 0。
    完。
完。

微回路_创建 接受 锥体数、PV数、SST数、VIP数：
    可变 神经元列表 为 一组：（空）。
    可变 id 为 0。
    可变 i 为 0。
    只要 i 小于 锥体数：
        做 追加 用 神经元列表、做 多样神经元_创建 用 类型_锥体、id。
        id 为 id 加 1。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 PV数：
        做 追加 用 神经元列表、做 多样神经元_创建 用 类型_PV、id。
        id 为 id 加 1。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 SST数：
        做 追加 用 神经元列表、做 多样神经元_创建 用 类型_SST、id。
        id 为 id 加 1。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 VIP数：
        做 追加 用 神经元列表、做 多样神经元_创建 用 类型_VIP、id。
        id 为 id 加 1。
        i 为 i 加 1。
    完。
    返还 一组：神经元列表、锥体数、PV数、SST数、VIP数、一组：（空）、0。
完。

微回路_初始化连接 接受 回路：
    神经元 为 回路 的 第 0 项。
    总数 为 做 计数 用 神经元。
    可变 连接 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 总数：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 总数：
            可变 权重 为 0。
            源类型 为 神经元 的 第 i 项 的 第 1 项。
            目标类型 为 神经元 的 第 j 项 的 第 1 项。
            若 源类型 等于 类型_锥体 并且 目标类型 等于 类型_锥体：
                若 (i 乘 7 加 j 乘 13) 取余于 10 等于 0：
                    权重 为 1500。
                完。
            完。
            若 源类型 等于 类型_锥体 并且 目标类型 等于 类型_PV：
                若 (i 乘 3 加 j 乘 7) 取余于 2 等于 0：
                    权重 为 4000。
                完。
            完。
            若 源类型 等于 类型_PV 并且 目标类型 等于 类型_锥体：
                若 (i 乘 11 加 j 乘 5) 取余于 2 等于 0：
                    权重 为 负 5000。
                完。
            完。
            若 源类型 等于 类型_锥体 并且 目标类型 等于 类型_SST：
                若 (i 乘 5 加 j 乘 11) 取余于 3 等于 0：
                    权重 为 3000。
                完。
            完。
            若 源类型 等于 类型_SST 并且 目标类型 等于 类型_锥体：
                若 (i 乘 9 加 j 乘 3) 取余于 3 等于 0：
                    权重 为 负 3000。
                完。
            完。
            若 源类型 等于 类型_VIP 并且 目标类型 等于 类型_SST：
                若 (i 乘 7 加 j 乘 9) 取余于 2 等于 0：
                    权重 为 负 4000。
                完。
            完。
            做 追加 用 行、权重。
            j 为 j 加 1。
        完。
        做 追加 用 连接、行。
        i 为 i 加 1。
    完。
    回路 的 第 5 项 为 连接。
完。

微回路_计算 接受 回路、外部输入、调制：
    神经元 为 回路 的 第 0 项。
    连接 为 回路 的 第 5 项。
    总数 为 做 计数 用 神经元。
    回路 的 第 6 项 为 调制。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 总数：
        可变 总输入 为 0。
        若 i 小于 做 计数 用 外部输入：
            总输入 为 外部输入 的 第 i 项。
        完。
        可变 j 为 0。
        只要 j 小于 总数：
            若 连接 的 第 j 项 的 第 i 项 不等于 0：
                若 神经元 的 第 j 项 的 第 11 项 大于 0：
                    总输入 为 总输入 加 连接 的 第 j 项 的 第 i 项。
                完。
            完。
            j 为 j 加 1。
        完。
        输出值 为 做 多样神经元_步进 用 神经元 的 第 i 项、总输入、调制。
        做 追加 用 输出、输出值。
        i 为 i 加 1。
    完。
    返还 输出。
完。

微回路_EI平衡 接受 回路：
    神经元 为 回路 的 第 0 项。
    可变 兴奋活动 为 0。
    可变 抑制活动 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 神经元：
        活动 为 神经元 的 第 i 项 的 第 11 项。
        若 神经元 的 第 i 项 的 第 8 项 大于 0：
            兴奋活动 为 兴奋活动 加 活动。
        否则：
            抑制活动 为 抑制活动 加 活动。
        完。
        i 为 i 加 1。
    完。
    总活动 为 兴奋活动 加 抑制活动。
    若 总活动 等于 0：
        返还 ND精度 除以 2。
    完。
    返还 兴奋活动 乘 ND精度 除以 总活动。
完。

本章 收束。
