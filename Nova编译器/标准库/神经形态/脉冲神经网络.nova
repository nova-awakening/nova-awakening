《脉冲神经网络》

引用《..·基础》。

SNN_精度 为 10000。

SNN_静息电位 为 负 7000。

SNN_阈值电位 为 负 5500。

SNN_峰值电位 为 4000。

SNN_超极化 为 负 8000。

SNN_泄漏系数 为 9500。

SNN_时间常数 为 200。

SNN_不应期 为 30。

LIF_创建 接受 标识：
    返还 一组：标识、SNN_静息电位、0、0、SNN_泄漏系数、SNN_阈值电位、0、0、0、SNN_静息电位、0。
完。

LIF_泄漏积分 接受 神经元、输入电流：
    V 为 神经元 的 第 1 项。
    λ 为 神经元 的 第 4 项。
    不应 为 神经元 的 第 8 项。
    若 不应 大于 0：
        神经元 的 第 8 项 为 不应 减 1。
        神经元 的 第 1 项 为 SNN_超极化 加 (SNN_静息电位 减 SNN_超极化) 乘 (SNN_不应期 减 不应) 除以 SNN_不应期。
        返还 0。
    完。
    泄漏项 为 λ 乘 V 除以 SNN_精度。
    驱动项 为 (SNN_精度 减 λ) 乘 SNN_静息电位 除以 SNN_精度。
    新V 为 泄漏项 加 驱动项 加 输入电流。
    若 新V 大于或等于 神经元 的 第 5 项：
        神经元 的 第 1 项 为 SNN_峰值电位。
        神经元 的 第 3 项 为 神经元 的 第 3 项 加 1。
        神经元 的 第 2 项 为 神经元 的 第 7 项。
        神经元 的 第 8 项 为 SNN_不应期。
        神经元 的 第 5 项 为 神经元 的 第 5 项 加 100。
        返还 1。
    否则：
        神经元 的 第 1 项 为 新V。
        若 神经元 的 第 5 项 大于 SNN_阈值电位：
            神经元 的 第 5 项 为 神经元 的 第 5 项 减 10。
        完。
        返还 0。
    完。
完。

Izhikevich_创建 接受 标识、类型：
    可变 a 为 200。
    可变 b 为 2000。
    可变 c 为 负 6500。
    可变 d 为 8000。
    若 类型 等于 1：
        a 为 200。
        b 为 2000。
        c 为 负 6500。
        d 为 8000。
    完。
    若 类型 等于 2：
        a 为 200。
        b 为 2000。
        c 为 负 5500。
        d 为 4000。
    完。
    若 类型 等于 3：
        a 为 1000。
        b 为 2000。
        c 为 负 6500。
        d 为 2000。
    完。
    若 类型 等于 4：
        a 为 200。
        b 为 2500。
        c 为 负 6500。
        d 为 2000。
    完。
    若 类型 等于 5：
        a 为 200。
        b 为 2000。
        c 为 负 5000。
        d 为 2000。
    完。
    返还 一组：标识、负 6500、b 乘 负 6500 除以 SNN_精度、a、b、c、d、类型、0、0。
完。

Izhikevich_步进 接受 神经元、输入：
    v 为 神经元 的 第 1 项。
    u 为 神经元 的 第 2 项。
    a 为 神经元 的 第 3 项。
    b 为 神经元 的 第 4 项。
    v2 为 v 乘 v 除以 SNN_精度。
    dv 为 4 乘 v2 除以 100 加 5 乘 v 加 1400000 减 u 乘 100 加 输入 乘 100。
    新v 为 v 加 dv 除以 SNN_精度。
    du 为 a 乘 (b 乘 v 除以 SNN_精度 减 u) 除以 SNN_精度。
    新u 为 u 加 du。
    若 新v 大于或等于 3000：
        神经元 的 第 1 项 为 神经元 的 第 5 项。
        神经元 的 第 2 项 为 新u 加 神经元 的 第 6 项。
        神经元 的 第 8 项 为 1。
        神经元 的 第 9 项 为 神经元 的 第 9 项 加 1。
        返还 1。
    否则：
        神经元 的 第 1 项 为 新v。
        神经元 的 第 2 项 为 新u。
        神经元 的 第 8 项 为 0。
        返还 0。
    完。
完。

突触_AMPA 为 1。

突触_NMDA 为 2。

突触_GABA_A 为 3。

突触_GABA_B 为 4。

突触_电突触 为 5。

脉冲_突触_创建 接受 源ID、目标ID、权重、类型、延迟：
    返还 一组：源ID、目标ID、权重、类型、延迟、0、SNN_精度、0、0、SNN_精度。
完。

突触_传递 接受 突触、脉冲时间、当前时间：
    延迟 为 突触 的 第 4 项。
    到达时间 为 脉冲时间 加 延迟。
    若 当前时间 小于 到达时间：
        返还 0。
    完。
    权重 为 突触 的 第 2 项。
    类型 为 突触 的 第 3 项。
    递质 为 突触 的 第 8 项。
    最大递质 为 突触 的 第 9 项。
    释放率 为 递质 乘 突触 的 第 6 项 除以 SNN_精度。
    间隔 为 当前时间 减 突触 的 第 5 项。
    可变 STP因子 为 SNN_精度。
    若 间隔 小于 50：
        STP因子 为 间隔 乘 SNN_精度 除以 50。
    否则若 间隔 小于 200：
        STP因子 为 SNN_精度 加 (200 减 间隔) 乘 20。
    完。
    可变 电流 为 权重 乘 释放率 除以 SNN_精度。
    电流 为 电流 乘 STP因子 除以 SNN_精度。
    若 类型 等于 突触_NMDA：
        电流 为 电流 乘 6000 除以 SNN_精度。
    完。
    突触 的 第 5 项 为 当前时间。
    突触 的 第 8 项 为 递质 减 释放率 除以 10。
    若 突触 的 第 8 项 小于 0：
        突触 的 第 8 项 为 0。
    完。
    突触 的 第 8 项 为 突触 的 第 8 项 加 (最大递质 减 突触 的 第 8 项) 除以 50。
    返还 电流。
完。

STDP_A正 为 1000。

STDP_A负 为 负 1050。

STDP_τ正 为 200。

STDP_τ负 为 200。

脉冲神经_STDP_更新 接受 突触、前发放时间、后发放时间：
    Δt 为 后发放时间 减 前发放时间。
    可变 Δw 为 0。
    若 Δt 大于 0：
        可变 衰减 为 SNN_精度 减 Δt 乘 SNN_精度 除以 STDP_τ正。
        若 衰减 小于 0：
            衰减 为 0。
        完。
        Δw 为 STDP_A正 乘 衰减 除以 SNN_精度。
    否则若 Δt 小于 0：
        absΔt 为 0 减 Δt。
        可变 衰减 为 SNN_精度 减 absΔt 乘 SNN_精度 除以 STDP_τ负。
        若 衰减 小于 0：
            衰减 为 0。
        完。
        Δw 为 STDP_A负 乘 衰减 除以 SNN_精度。
    完。
    旧权重 为 突触 的 第 2 项。
    可变 新权重 为 旧权重 加 Δw。
    若 新权重 大于 SNN_精度：
        新权重 为 SNN_精度。
    完。
    若 新权重 小于 0 减 SNN_精度：
        新权重 为 0 减 SNN_精度。
    完。
    突触 的 第 2 项 为 新权重。
    返还 Δw。
完。

SNN层_创建 接受 大小、神经元类型：
    可变 层 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        若 神经元类型 等于 0：
            做 追加 用 层、做 LIF_创建 用 i。
        否则：
            做 追加 用 层、做 Izhikevich_创建 用 i、神经元类型。
        完。
        i 为 i 加 1。
    完。
    返还 层。
完。

SNN层_连接 接受 源层、目标层、初始权重、突触类型：
    可变 突触列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 源层：
        可变 j 为 0。
        只要 j 小于 做 计数 用 目标层：
            扰动 为 (i 乘 7 加 j 乘 13 加 37) 取余于 2000 减 1000。
            w 为 初始权重 加 扰动。
            做 追加 用 突触列表、做 脉冲_突触_创建 用 i、j、w、突触类型、1。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 突触列表。
完。

SNN_步进 接受 层列表、突触矩阵列表、输入电流、时间步：
    可变 总输入 为 0。
    层数 为 做 计数 用 层列表。
    可变 当前输入 为 输入电流。
    可变 层索引 为 0。
    只要 层索引 小于 层数：
        层 为 层列表 的 第 层索引 项。
        层大小 为 做 计数 用 层。
        可变 脉冲输出 为 一组：（空）。
        可变 n 为 0。
        只要 n 小于 层大小：
            神经元 为 层 的 第 n 项。
            若 层索引 等于 0：
                若 n 小于 做 计数 用 当前输入：
                    总输入 为 当前输入 的 第 n 项。
                完。
            否则：
                突触列表 为 突触矩阵列表 的 第 层索引 减 1 项。
                可变 s 为 0。
                只要 s 小于 做 计数 用 突触列表：
                    syn 为 突触列表 的 第 s 项。
                    若 syn 的 第 1 项 等于 n：
                        总输入 为 总输入 加 做 突触_传递 用 syn、时间步 减 1、时间步。
                    完。
                    s 为 s 加 1。
                完。
            完。
            神经元 的 第 7 项 为 时间步。
            可变 发放 为 0。
            若 神经元 的 第 7 项 不等于 0：
                发放 为 做 LIF_泄漏积分 用 神经元、总输入。
            完。
            做 追加 用 脉冲输出、发放。
            n 为 n 加 1。
        完。
        当前输入 为 脉冲输出。
        层索引 为 层索引 加 1。
    完。
    返还 当前输入。
完。

速率编码 接受 值、时间窗、最大值：
    若 最大值 小于或等于 0：
        返还 一组：（空）。
    完。
    可变 归一化 为 值 乘 SNN_精度 除以 最大值。
    若 归一化 大于 SNN_精度：
        归一化 为 SNN_精度。
    完。
    若 归一化 小于 0：
        归一化 为 0。
    完。
    可变 脉冲序列 为 一组：（空）。
    可变 累积 为 0。
    可变 t 为 0。
    只要 t 小于 时间窗：
        累积 为 累积 加 归一化。
        若 累积 大于或等于 SNN_精度：
            做 追加 用 脉冲序列、1。
            累积 为 累积 减 SNN_精度。
        否则：
            做 追加 用 脉冲序列、0。
        完。
        t 为 t 加 1。
    完。
    返还 脉冲序列。
完。

时序编码 接受 值、时间窗、最大值：
    若 最大值 小于或等于 0：
        返还 时间窗。
    完。
    可变 归一化 为 值 乘 SNN_精度 除以 最大值。
    若 归一化 大于 SNN_精度：
        归一化 为 SNN_精度。
    完。
    若 SNN_精度 等于 0：
        返还 0。
    完。
    返还 时间窗 乘 (SNN_精度 减 归一化) 除以 SNN_精度。
完。

相位编码 接受 值、振荡周期、最大值：
    若 最大值 小于或等于 0：
        返还 0。
    完。
    可变 归一化 为 值 乘 SNN_精度 除以 最大值。
    返还 振荡周期 乘 归一化 除以 SNN_精度。
完。

脉冲解码_速率 接受 脉冲序列：
    总数 为 做 计数 用 脉冲序列。
    若 总数 等于 0：
        返还 0。
    完。
    可变 发放数 为 0。
    可变 i 为 0。
    只要 i 小于 总数：
        若 脉冲序列 的 第 i 项 大于 0：
            发放数 为 发放数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 发放数 乘 SNN_精度 除以 总数。
完。

侧向抑制 接受 层、抑制强度：
    大小 为 做 计数 用 层。
    可变 最大电位 为 负 99999。
    可变 赢家 为 0。
    可变 i 为 0。
    只要 i 小于 大小：
        若 层 的 第 i 项 的 第 1 项 大于 最大电位：
            最大电位 为 层 的 第 i 项 的 第 1 项。
            赢家 为 i。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 大小：
        若 i 不等于 赢家：
            可变 距离 为 赢家 减 i。
            若 距离 小于 0：
                距离 为 0 减 距离。
            完。
            抑制量 为 抑制强度 乘 SNN_精度 除以 (SNN_精度 加 距离 乘 1000)。
            层 的 第 i 项 的 第 1 项 为 层 的 第 i 项 的 第 1 项 减 抑制量。
        完。
        i 为 i 加 1。
    完。
    返还 赢家。
完。

神经振荡器 接受 频率、振幅、时间步、采样率：
    相位 为 时间步 乘 频率 乘 62832 除以 (采样率 乘 SNN_精度)。
    相位归一 为 相位 取余于 62832。
    可变 x 为 相位归一。
    若 x 大于 31416：
        x 为 x 减 62832。
    完。
    x2 为 x 乘 x 除以 SNN_精度。
    x3 为 x2 乘 x 除以 SNN_精度。
    x5 为 x3 乘 x2 除以 SNN_精度。
    正弦 为 x 减 x3 除以 6 加 x5 除以 120。
    若 SNN_精度 等于 0：
        返还 0。
    完。
    返还 振幅 乘 正弦 除以 SNN_精度。
完。

检测相位同步 接受 振荡器列表：
    可变 比较数 为 0。
    数量 为 做 计数 用 振荡器列表。
    若 数量 小于 2：
        返还 SNN_精度。
    完。
    可变 同步度 为 0。
    可变 i 为 0。
    只要 i 小于 数量 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 数量：
            可变 差 为 振荡器列表 的 第 i 项 减 振荡器列表 的 第 j 项。
            若 差 小于 0：
                差 为 0 减 差。
            完。
            同步度 为 同步度 加 (SNN_精度 减 差)。
            比较数 为 比较数 加 1。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 比较数 大于 0：
        返还 同步度 除以 比较数。
    完。
    返还 0。
完。

可变 _脉冲_总发放 为 0。

可变 _脉冲_总步数 为 0。

脉冲_记录发放 接受 数量：
    _脉冲_总发放 为 _脉冲_总发放 加 数量。
    _脉冲_总步数 为 _脉冲_总步数 加 1。
完。

脉冲_平均发放率 接受 零参：
    若 _脉冲_总步数 等于 0：
        返还 0。
    完。
    返还 _脉冲_总发放 除以 _脉冲_总步数。
完。

脉冲_活跃度评估 接受 零参：
    率 为 做 脉冲_平均发放率。
    若 率 大于 80：
        返还 「过度活跃(可能癫痫样放电)」。
    完。
    若 率 大于 20：
        返还 「正常活跃」。
    完。
    若 率 大于 5：
        返还 「低活跃」。
    完。
    返还 「几乎静默」。
完。

本章 收束。
