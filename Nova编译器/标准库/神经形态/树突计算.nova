《树突计算》

引用《..·基础》。

DC_精度 为 10000。

DC_静息 为 负 7000。

DC_阈值 为 负 4000。

树突分支_创建 接受 标识、父标识、深度：
    返还 一组：标识、父标识、深度、DC_静息、DC_精度、5000、一组：（空）、一组：（空）、0、DC_精度。
完。

树突分支_整合 接受 分支、输入列表：
    可变 线性和 为 0。
    输入数 为 做 计数 用 输入列表。
    可变 同步计数 为 0。
    可变 i 为 0。
    只要 i 小于 输入数：
        线性和 为 线性和 加 输入列表 的 第 i 项。
        若 输入列表 的 第 i 项 大于 0：
            同步计数 为 同步计数 加 1。
        完。
        i 为 i 加 1。
    完。
    同步计数 乘 DC_精度 除以 (输入数 加 1)。
    阈值 为 分支 的 第 5 项。
    可变 输出 为 0。
    若 线性和 小于 阈值 除以 2：
        输出 为 线性和 乘 6000 除以 DC_精度。
    否则若 线性和 小于 阈值 乘 2：
        偏移 为 线性和 减 阈值。
        放大 为 DC_精度 加 偏移 乘 分支 的 第 9 项 除以 DC_精度。
        输出 为 线性和 乘 放大 除以 DC_精度。
    否则：
        输出 为 线性和 乘 15000 除以 DC_精度。
        若 输出 大于 DC_精度 乘 3：
            输出 为 DC_精度 乘 3。
        完。
    完。
    若 输出 大于 阈值：
        分支 的 第 8 项 为 分支 的 第 8 项 加 (输出 减 阈值) 除以 10。
    否则：
        分支 的 第 8 项 为 分支 的 第 8 项 乘 9500 除以 DC_精度。
    完。
    分支 的 第 3 项 为 输出。
    返还 输出。
完。

树突树_创建 接受 基底分支数、远端分支数：
    可变 树 为 一组：（空）。
    可变 id计数 为 0。
    做 追加 用 树、做 树突分支_创建 用 id计数、负 1、0。
    id计数 为 id计数 加 1。
    可变 b 为 0。
    只要 b 小于 基底分支数：
        做 追加 用 树、做 树突分支_创建 用 id计数、0、1。
        id计数 为 id计数 加 1。
        b 为 b 加 1。
    完。
    可变 a 为 0。
    只要 a 小于 远端分支数：
        主干ID 为 id计数。
        做 追加 用 树、做 树突分支_创建 用 id计数、0、1。
        id计数 为 id计数 加 1。
        可变 s 为 0。
        只要 s 小于 3：
            做 追加 用 树、做 树突分支_创建 用 id计数、主干ID、2。
            id计数 为 id计数 加 1。
            s 为 s 加 1。
        完。
        a 为 a 加 1。
    完。
    返还 树。
完。

树突树_整合 接受 树、外部输入：
    总分支 为 做 计数 用 树。
    可变 分支输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 总分支：
        做 追加 用 分支输出、0。
        i 为 i 加 1。
    完。
    可变 输入索引 为 0。
    i 为 0。
    只要 i 小于 总分支：
        分支 为 树 的 第 i 项。
        可变 有子 为 0。
        可变 j 为 0。
        只要 j 小于 总分支：
            若 树 的 第 j 项 的 第 1 项 等于 分支 的 第 0 项：
                有子 为 1。
            完。
            j 为 j 加 1。
        完。
        若 有子 等于 0 并且 输入索引 小于 做 计数 用 外部输入：
            分支输出 的 第 i 项 为 外部输入 的 第 输入索引 项。
            输入索引 为 输入索引 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 最大深度 为 0。
    i 为 0。
    只要 i 小于 总分支：
        若 树 的 第 i 项 的 第 2 项 大于 最大深度：
            最大深度 为 树 的 第 i 项 的 第 2 项。
        完。
        i 为 i 加 1。
    完。
    可变 当前深度 为 最大深度。
    只要 当前深度 大于或等于 0：
        i 为 0。
        只要 i 小于 总分支：
            若 树 的 第 i 项 的 第 2 项 等于 当前深度：
                可变 子输入 为 一组：（空）。
                可变 k 为 0。
                只要 k 小于 总分支：
                    若 树 的 第 k 项 的 第 1 项 等于 树 的 第 i 项 的 第 0 项：
                        做 追加 用 子输入、分支输出 的 第 k 项。
                    完。
                    k 为 k 加 1。
                完。
                若 分支输出 的 第 i 项 不等于 0：
                    做 追加 用 子输入、分支输出 的 第 i 项。
                完。
                若 做 计数 用 子输入 大于 0：
                    分支输出 的 第 i 项 为 做 树突分支_整合 用 树 的 第 i 项、子输入。
                完。
            完。
            i 为 i 加 1。
        完。
        当前深度 为 当前深度 减 1。
    完。
    返还 分支输出 的 第 0 项。
完。

树突可塑性_更新 接受 树、学习率：
    总分支 为 做 计数 用 树。
    可变 i 为 0。
    只要 i 小于 总分支：
        分支 为 树 的 第 i 项。
        钙 为 分支 的 第 8 项。
        BCM阈值 为 3000。
        若 钙 大于 BCM阈值 乘 2：
            分支 的 第 9 项 为 分支 的 第 9 项 加 学习率。
            若 分支 的 第 9 项 大于 DC_精度 乘 3：
                分支 的 第 9 项 为 DC_精度 乘 3。
            完。
        否则若 钙 大于 BCM阈值：
            分支 的 第 9 项 为 分支 的 第 9 项 加 学习率 除以 3。
        否则若 钙 大于 BCM阈值 除以 2：
            分支 的 第 9 项 为 分支 的 第 9 项 减 学习率 除以 2。
            若 分支 的 第 9 项 小于 1000：
                分支 的 第 9 项 为 1000。
            完。
        完。
        分支 的 第 8 项 为 钙 乘 9000 除以 DC_精度。
        i 为 i 加 1。
    完。
完。

双层神经元_创建 接受 基底数、远端数：
    返还 一组：做 树突树_创建 用 基底数、远端数、DC_静息、DC_阈值、0、0、8000、0。
完。

双层神经元_计算 接受 神经元、前馈输入、上下文输入：
    树 为 神经元 的 第 0 项。
    可变 全部输入 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 前馈输入：
        做 追加 用 全部输入、前馈输入 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 上下文输入：
        做 追加 用 全部输入、上下文输入 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 胞体输入 为 做 树突树_整合 用 树、全部输入。
    若 神经元 的 第 6 项 大于 0：
        胞体输入 为 胞体输入 减 神经元 的 第 6 项。
        神经元 的 第 6 项 为 神经元 的 第 6 项 乘 9000 除以 DC_精度。
    完。
    神经元 的 第 1 项 为 胞体输入。
    若 胞体输入 大于 神经元 的 第 2 项：
        神经元 的 第 3 项 为 1。
        神经元 的 第 4 项 为 神经元 的 第 4 项 加 1。
        神经元 的 第 6 项 为 神经元 的 第 5 项。
        返还 1。
    否则：
        神经元 的 第 3 项 为 0。
        返还 0。
    完。
完。

本章 收束。
