《路径查找》

引用《..·..·基础》。

A星搜索 接受 网格、起行、起列、终行、终列：
    可变 ri 为 0。
    行数 为 网格 的 第 0 项。
    列数 为 网格 的 第 1 项。
    可变 开放 为 一组：（空）。
    可变 关闭 为 做 字典。
    可变 父表 为 做 字典。
    起h 为 做 astar_曼哈顿 用 起行、起列、终行、终列。
    做 追加 用 开放、一组：起h、0、起行、起列。
    做 字典设 用 父表、做 astar_键 用 起行、起列、「start」。
    只要 做 计数 用 开放 大于 0：
        可变 最小i 为 0。
        可变 最小f 为 开放 的 第 0 项 的 第 0 项。
        可变 si 为 1。
        只要 si 小于 做 计数 用 开放：
            若 开放 的 第 si 项 的 第 0 项 小于 最小f：
                最小f 为 开放 的 第 si 项 的 第 0 项。
                最小i 为 si。
            完。
            si 为 si 加 1。
        完。
        当前 为 开放 的 第 最小i 项。
        当前行 为 当前 的 第 2 项。
        当前列 为 当前 的 第 3 项。
        当前g 为 当前 的 第 1 项。
        可变 新开放 为 一组：（空）。
        只要 ri 小于 做 计数 用 开放：
            若 ri 不等于 最小i：
                做 追加 用 新开放、开放 的 第 ri 项。
            完。
            ri 为 ri 加 1。
        完。
        开放 为 新开放。
        若 当前行 等于 终行 并且 当前列 等于 终列：
            返还 做 astar_重建路径 用 父表、终行、终列。
        完。
        做 字典设 用 关闭、做 astar_键 用 当前行、当前列、1。
        方向 为 一组：一组：负 1、0、一组：1、0、一组：0、负 1、一组：0、1。
        可变 di 为 0。
        只要 di 小于 4：
            新行 为 当前行 加 方向 的 第 di 项 的 第 0 项。
            新列 为 当前列 加 方向 的 第 di 项 的 第 1 项。
            若 新行 小于 0 或者 新行 大于或等于 行数 或者 新列 小于 0 或者 新列 大于或等于 列数：
                di 为 di 加 1。
                续。
            完。
            键 为 做 astar_键 用 新行、新列。
            若 网格 的 第 2 加 新行 乘 列数 加 新列 项 不等于 0 或者 做 字典取 用 关闭、键 不等于 空：
                di 为 di 加 1。
                续。
            完。
            做 astar_尝试扩展 用 开放、父表、当前g、新行、新列、终行、终列、当前行、当前列。
            di 为 di 加 1。
        完。
    完。
    返还 空。
完。

astar_开放列表有更好 接受 开放、新行、新列、新g：
    可变 oi 为 0。
    只要 oi 小于 做 计数 用 开放：
        若 开放 的 第 oi 项 的 第 2 项 等于 新行 并且 开放 的 第 oi 项 的 第 3 项 等于 新列：
            返还 开放 的 第 oi 项 的 第 1 项 小于或等于 新g。
        完。
        oi 为 oi 加 1。
    完。
    返还 假。
完。

astar_尝试扩展 接受 开放、父表、当前g、新行、新列、终行、终列、当前行、当前列：
    新g 为 当前g 加 1。
    若 做 astar_开放列表有更好 用 开放、新行、新列、新g：
        返还 空。
    完。
    新h 为 做 astar_曼哈顿 用 新行、新列、终行、终列。
    做 追加 用 开放、一组：新g 加 新h、新g、新行、新列。
    做 字典设 用 父表、做 astar_键 用 新行、新列、做 astar_键 用 当前行、当前列。
完。

astar_曼哈顿 接受 行1、列1、行2、列2：
    可变 d行 为 行1 减 行2。
    可变 d列 为 列1 减 列2。
    若 d行 小于 0：
        d行 为 0 减 d行。
    完。
    若 d列 小于 0：
        d列 为 0 减 d列。
    完。
    返还 d行 加 d列。
完。

astar_键 接受 行、列：
    返还 做 字符串拼接 用 做 字符串拼接 用 做 转字符串 用 行、「,」、做 转字符串 用 列。
完。

astar_重建路径 接受 父表、终行、终列：
    可变 反转 为 一组：（空）。
    可变 路径 为 一组：（空）。
    可变 当前键 为 做 astar_键 用 终行、终列。
    只要 做 字符串等于 用 当前键、「start」 等于 假：
        做 追加 用 路径、当前键。
        当前键 为 做 字典取 用 父表、当前键。
    完。
    n 为 做 计数 用 路径。
    可变 i 为 n 减 1。
    只要 i 大于或等于 0：
        做 追加 用 反转、路径 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 反转。
完。

弗洛伊德最短路 接受 距离矩阵：
    n 为 距离矩阵 的 第 0 项。
    可变 d 为 一组：n、n。
    可变 i 为 0。
    只要 i 小于 n 乘 n：
        做 追加 用 d、距离矩阵 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    可变 k 为 0。
    只要 k 小于 n：
        i 为 0。
        只要 i 小于 n：
            可变 j 为 0。
            只要 j 小于 n：
                直接 为 d 的 第 2 加 i 乘 n 加 j 项。
                中转 为 d 的 第 2 加 i 乘 n 加 k 项 加 d 的 第 2 加 k 乘 n 加 j 项。
                若 中转 小于 直接：
                    d 的 第 2 加 i 乘 n 加 j 项 为 中转。
                完。
                j 为 j 加 1。
            完。
            i 为 i 加 1。
        完。
        k 为 k 加 1。
    完。
    返还 d。
完。

bfs最短路径 接受 邻接表、起点、终点：
    可变 反转 为 一组：（空）。
    可变 距离 为 做 字典。
    可变 前驱 为 做 字典。
    可变 队列 为 一组：起点。
    可变 前 为 0。
    做 字典设 用 距离、做 转字符串 用 起点、0。
    只要 前 小于 做 计数 用 队列：
        当前 为 队列 的 第 前 项。
        前 为 前 加 1。
        若 当前 等于 终点：
            可变 路径 为 一组：终点。
            可变 p 为 做 字典取 用 前驱、做 转字符串 用 终点。
            只要 p 不等于 空：
                做 追加 用 路径、p。
                p 为 做 字典取 用 前驱、做 转字符串 用 p。
            完。
            pn 为 做 计数 用 路径。
            可变 pi 为 pn 减 1。
            只要 pi 大于或等于 0：
                做 追加 用 反转、路径 的 第 pi 项。
                pi 为 pi 减 1。
            完。
            返还 一组：做 字典取 用 距离、做 转字符串 用 终点、反转。
        完。
        邻居 为 做 字典取 用 邻接表、做 转字符串 用 当前。
        若 邻居 不等于 空：
            可变 i 为 0。
            只要 i 小于 做 计数 用 邻居：
                n 为 邻居 的 第 i 项。
                若 做 字典取 用 距离、做 转字符串 用 n 等于 空：
                    做 字典设 用 距离、做 转字符串 用 n、做 字典取 用 距离、做 转字符串 用 当前 加 1。
                    做 字典设 用 前驱、做 转字符串 用 n、当前。
                    做 追加 用 队列、n。
                完。
                i 为 i 加 1。
            完。
        完。
    完。
    返还 空。
完。

连通分量 接受 邻接表、节点列表：
    可变 分量列表 为 一组：（空）。
    可变 已访问 为 做 字典。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点列表：
        节点 为 节点列表 的 第 i 项。
        若 做 字典取 用 已访问、做 转字符串 用 节点 等于 空：
            可变 分量 为 一组：（空）。
            可变 队列 为 一组：节点。
            可变 前 为 0。
            做 字典设 用 已访问、做 转字符串 用 节点、1。
            只要 前 小于 做 计数 用 队列：
                当前 为 队列 的 第 前 项。
                前 为 前 加 1。
                做 追加 用 分量、当前。
                邻居 为 做 字典取 用 邻接表、做 转字符串 用 当前。
                若 邻居 不等于 空：
                    做 BFS_扩展邻居 用 邻居、已访问、队列。
                完。
            完。
            做 追加 用 分量列表、分量。
        完。
        i 为 i 加 1。
    完。
    返还 分量列表。
完。

BFS_扩展邻居 接受 邻居、已访问、队列：
    可变 j 为 0。
    只要 j 小于 做 计数 用 邻居：
        若 做 字典取 用 已访问、做 转字符串 用 邻居 的 第 j 项 等于 空：
            做 字典设 用 已访问、做 转字符串 用 邻居 的 第 j 项、1。
            做 追加 用 队列、邻居 的 第 j 项。
        完。
        j 为 j 加 1。
    完。
完。

普里姆最小生成树 接受 邻接矩阵：
    可变 键值 为 一组：（空）。
    可变 总权重 为 0。
    n 为 邻接矩阵 的 第 0 项。
    无穷 为 0x7FFFFFFF。
    可变 在树中 为 一组：（空）。
    可变 父 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 在树中、假。
        做 追加 用 键值、无穷。
        做 追加 用 父、负 1。
        i 为 i 加 1。
    完。
    键值 的 第 0 项 为 0。
    可变 步 为 0。
    只要 步 小于 n：
        可变 u 为 负 1。
        可变 最小 为 无穷。
        i 为 0。
        只要 i 小于 n：
            若 在树中 的 第 i 项 等于 假 并且 键值 的 第 i 项 小于 最小：
                最小 为 键值 的 第 i 项。
                u 为 i。
            完。
            i 为 i 加 1。
        完。
        若 u 等于 负 1：
            步 为 n。
        否则：
            在树中 的 第 u 项 为 真。
            总权重 为 总权重 加 键值 的 第 u 项。
            i 为 0。
            只要 i 小于 n：
                w 为 邻接矩阵 的 第 2 加 u 乘 n 加 i 项。
                若 在树中 的 第 i 项 等于 假 并且 w 小于 键值 的 第 i 项 并且 w 大于 0：
                    键值 的 第 i 项 为 w。
                    父 的 第 i 项 为 u。
                完。
                i 为 i 加 1。
            完。
        完。
        步 为 步 加 1。
    完。
    返还 一组：总权重、父。
完。

本章 收束。
