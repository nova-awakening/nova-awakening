《图》

引用《..·..·基础》。

图_创建 接受 有向：
    返还 一组：0、做 字典、做 字典、有向。
完。

图_无向 接受 零参：
    返还 做 图_创建 用 假。
完。

图_有向 接受 零参：
    返还 做 图_创建 用 真。
完。

图_添加节点 接受 图、节点：
    邻接 为 图 的 第 1 项。
    若 做 字典取 用 邻接、做 转字符串 用 节点 等于 空：
        做 字典设 用 邻接、做 转字符串 用 节点、一组：（空）。
        图 的 第 0 项 为 图 的 第 0 项 加 1。
    完。
完。

图_添加边 接受 图、源、目、权重：
    邻接 为 图 的 第 1 项。
    权重表 为 图 的 第 2 项。
    有向 为 图 的 第 3 项。
    做 图_添加节点 用 图、源。
    做 图_添加节点 用 图、目。
    源邻 为 做 字典取 用 邻接、做 转字符串 用 源。
    做 追加 用 源邻、目。
    键 为 做 字符串拼接 用 做 字符串拼接 用 做 转字符串 用 源、「-」、做 转字符串 用 目。
    做 字典设 用 权重表、键、权重。
    若 有向 等于 假：
        目邻 为 做 字典取 用 邻接、做 转字符串 用 目。
        做 追加 用 目邻、源。
        反键 为 做 字符串拼接 用 做 字符串拼接 用 做 转字符串 用 目、「-」、做 转字符串 用 源。
        做 字典设 用 权重表、反键、权重。
    完。
完。

图_邻居 接受 图、节点：
    邻接 为 图 的 第 1 项。
    邻 为 做 字典取 用 邻接、做 转字符串 用 节点。
    若 邻 等于 空：
        返还 一组：（空）。
    完。
    返还 邻。
完。

图_权重 接受 图、源、目：
    键 为 做 字符串拼接 用 做 字符串拼接 用 做 转字符串 用 源、「-」、做 转字符串 用 目。
    w 为 做 字典取 用 图 的 第 2 项、键。
    若 w 等于 空：
        返还 0。
    完。
    返还 w。
完。

图_度 接受 图、节点：
    返还 做 计数 用 做 图_邻居 用 图、节点。
完。

图_bfs 接受 图、起点：
    可变 已访问 为 做 字典。
    可变 队列 为 一组：起点。
    可变 结果 为 一组：（空）。
    可变 前 为 0。
    做 字典设 用 已访问、做 转字符串 用 起点、1。
    只要 前 小于 做 计数 用 队列：
        当前 为 队列 的 第 前 项。
        前 为 前 加 1。
        做 追加 用 结果、当前。
        邻 为 做 图_邻居 用 图、当前。
        可变 i 为 0。
        只要 i 小于 做 计数 用 邻：
            n 为 邻 的 第 i 项。
            若 做 字典取 用 已访问、做 转字符串 用 n 等于 空：
                做 字典设 用 已访问、做 转字符串 用 n、1。
                做 追加 用 队列、n。
            完。
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

图_dfs 接受 图、起点：
    可变 已访问 为 做 字典。
    可变 新栈 为 一组：（空）。
    可变 栈 为 一组：起点。
    可变 结果 为 一组：（空）。
    只要 做 计数 用 栈 大于 0：
        当前 为 栈 的 第 做 计数 用 栈 减 1 项。
        可变 si 为 0。
        只要 si 小于 做 计数 用 栈 减 1：
            做 追加 用 新栈、栈 的 第 si 项。
            si 为 si 加 1。
        完。
        栈 为 新栈。
        若 做 字典取 用 已访问、做 转字符串 用 当前 等于 空：
            做 字典设 用 已访问、做 转字符串 用 当前、1。
            做 追加 用 结果、当前。
            邻 为 做 图_邻居 用 图、当前。
            可变 i 为 0。
            只要 i 小于 做 计数 用 邻：
                做 追加 用 栈、邻 的 第 i 项。
                i 为 i 加 1。
            完。
        完。
    完。
    返还 结果。
完。

图_最短路径 接受 图、起点：
    可变 距离 为 做 字典。
    可变 已访问 为 做 字典。
    做 字典设 用 距离、做 转字符串 用 起点、0。
    全部 为 做 图_bfs 用 图、起点。
    可变 剩余 为 做 计数 用 全部。
    只要 剩余 大于 0：
        可变 最小距 为 0x7FFFFFFF。
        可变 最小节点 为 负 1。
        可变 fi 为 0。
        只要 fi 小于 做 计数 用 全部：
            n 为 全部 的 第 fi 项。
            nk 为 做 转字符串 用 n。
            若 做 字典取 用 已访问、nk 等于 空：
                d 为 做 字典取 用 距离、nk。
                若 d 不等于 空 并且 d 小于 最小距：
                    最小距 为 d。
                    最小节点 为 n。
                完。
            完。
            fi 为 fi 加 1。
        完。
        若 最小节点 等于 负 1：
            返还 距离。
        完。
        做 字典设 用 已访问、做 转字符串 用 最小节点、1。
        邻 为 做 图_邻居 用 图、最小节点。
        可变 i 为 0。
        只要 i 小于 做 计数 用 邻：
            邻节 为 邻 的 第 i 项。
            新距 为 最小距 加 做 图_权重 用 图、最小节点、邻节。
            旧距 为 做 字典取 用 距离、做 转字符串 用 邻节。
            若 旧距 等于 空 或者 新距 小于 旧距：
                做 字典设 用 距离、做 转字符串 用 邻节、新距。
            完。
            i 为 i 加 1。
        完。
        剩余 为 剩余 减 1。
    完。
    返还 距离。
完。

图_拓扑排序 接受 图：
    _邻接 为 图 的 第 1 项。
    可变 入度 为 做 字典。
    全部 为 做 图_bfs 用 图、0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 全部：
        做 字典设 用 入度、做 转字符串 用 全部 的 第 i 项、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 全部：
        邻 为 做 图_邻居 用 图、全部 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 邻：
            k 为 做 转字符串 用 邻 的 第 j 项。
            d 为 做 字典取 用 入度、k。
            若 d 不等于 空：
                做 字典设 用 入度、k、d 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 队列 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 全部：
        若 做 字典取 用 入度、做 转字符串 用 全部 的 第 i 项 等于 0：
            做 追加 用 队列、全部 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    可变 前 为 0。
    只要 前 小于 做 计数 用 队列：
        当前 为 队列 的 第 前 项。
        前 为 前 加 1。
        做 追加 用 结果、当前。
        邻 为 做 图_邻居 用 图、当前。
        可变 j2 为 0。
        只要 j2 小于 做 计数 用 邻：
            k2 为 做 转字符串 用 邻 的 第 j2 项。
            d2 为 做 字典取 用 入度、k2。
            若 d2 不等于 空：
                做 字典设 用 入度、k2、d2 减 1。
                若 d2 减 1 等于 0：
                    做 追加 用 队列、邻 的 第 j2 项。
                完。
            完。
            j2 为 j2 加 1。
        完。
    完。
    返还 结果。
完。

图_有路径 接受 图、源、目：
    可达 为 做 图_bfs 用 图、源。
    可变 i 为 0。
    只要 i 小于 做 计数 用 可达：
        若 可达 的 第 i 项 等于 目：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

图_所有节点 接受 图：
    返还 做 字典键列表 用 图 的 第 1 项。
完。

图_连通分量 接受 图：
    可变 已访问 为 一组：（空）。
    可变 分量列表 为 一组：（空）。
    节点列表 为 做 图_所有节点 用 图。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点列表：
        节点 为 节点列表 的 第 i 项。
        可变 已找到 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 已访问：
            若 已访问 的 第 j 项 等于 节点：
                已找到 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已找到 等于 假：
            分量 为 做 图_bfs 用 图、节点。
            做 追加 用 分量列表、分量。
            j 为 0。
            只要 j 小于 做 计数 用 分量：
                做 追加 用 已访问、分量 的 第 j 项。
                j 为 j 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 分量列表。
完。

图_是连通 接受 图：
    分量 为 做 图_连通分量 用 图。
    返还 做 计数 用 分量 小于或等于 1。
完。

图_有环_dfs 接受 图、节点、已访问、栈中：
    做 追加 用 已访问、节点。
    做 追加 用 栈中、节点。
    邻 为 做 图_邻居 用 图、节点。
    可变 i 为 0。
    只要 i 小于 做 计数 用 邻：
        邻节点 为 邻 的 第 i 项。
        可变 在栈中 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 栈中：
            若 栈中 的 第 j 项 等于 邻节点：
                在栈中 为 真。
            完。
            j 为 j 加 1。
        完。
        若 在栈中：
            返还 真。
        完。
        可变 已访问过 为 假。
        j 为 0。
        只要 j 小于 做 计数 用 已访问：
            若 已访问 的 第 j 项 等于 邻节点：
                已访问过 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已访问过 等于 假：
            若 做 图_有环_dfs 用 图、邻节点、已访问、栈中：
                返还 真。
            完。
        完。
        i 为 i 加 1。
    完。
    做 删除 用 栈中、做 计数 用 栈中 减 1。
    返还 假。
完。

图_图_有环 接受 图：
    可变 已访问 为 一组：（空）。
    节点列表 为 做 图_所有节点 用 图。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点列表：
        可变 已访问过 为 假。
        可变 j 为 0。
        只要 j 小于 做 计数 用 已访问：
            若 已访问 的 第 j 项 等于 节点列表 的 第 i 项：
                已访问过 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已访问过 等于 假：
            可变 栈中 为 一组：（空）。
            若 做 图_有环_dfs 用 图、节点列表 的 第 i 项、已访问、栈中：
                返还 真。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

图_图_出度 接受 图、节点：
    返还 做 计数 用 做 图_邻居 用 图、节点。
完。

图_节点数 接受 图：
    返还 做 计数 用 做 图_所有节点 用 图。
完。

图_图_边数 接受 图：
    节点列表 为 做 图_所有节点 用 图。
    可变 总边 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点列表：
        总边 为 总边 加 做 图_图_出度 用 图、节点列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总边。
完。

图_图_转置 接受 图：
    可变 新图 为 做 图_创建 用 图 的 第 3 项。
    节点列表 为 做 图_所有节点 用 图。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点列表：
        做 图_添加节点 用 新图、节点列表 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 节点列表：
        邻 为 做 图_邻居 用 图、节点列表 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 邻：
            做 图_添加边 用 新图、邻 的 第 j 项、节点列表 的 第 i 项、做 图_权重 用 图、节点列表 的 第 i 项、邻 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 新图。
完。

本章 收束。
