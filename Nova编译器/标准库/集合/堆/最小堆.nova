《最小堆》

引用《..·..·基础》。

引用《..·栈·栈》。

堆_创建 接受 零参：
    返还 一组：（空）。
完。

堆_大小 接受 堆：
    返还 做 计数 用 堆。
完。

堆_空 接受 堆：
    返还 做 计数 用 堆 等于 0。
完。

堆_顶 接受 堆：
    若 做 计数 用 堆 等于 0：
        返还 空。
    完。
    返还 堆 的 第 0 项。
完。

堆_上浮 接受 堆、i：
    可变 当前 为 i。
    只要 当前 大于 0：
        父 为 (当前 减 1) 除以 2。
        若 堆 的 第 当前 项 小于 堆 的 第 父 项：
            暂存 为 堆 的 第 当前 项。
            堆 的 第 当前 项 为 堆 的 第 父 项。
            堆 的 第 父 项 为 暂存。
            当前 为 父。
        否则：
            返还 空。
        完。
    完。
完。

堆_下沉 接受 堆、i：
    n 为 做 计数 用 堆。
    可变 当前 为 i。
    只要 真：
        可变 最小 为 当前。
        左 为 2 乘 当前 加 1。
        右 为 2 乘 当前 加 2。
        若 左 小于 n 并且 堆 的 第 左 项 小于 堆 的 第 最小 项：
            最小 为 左。
        完。
        若 右 小于 n 并且 堆 的 第 右 项 小于 堆 的 第 最小 项：
            最小 为 右。
        完。
        若 最小 等于 当前：
            返还 空。
        完。
        暂存 为 堆 的 第 当前 项。
        堆 的 第 当前 项 为 堆 的 第 最小 项。
        堆 的 第 最小 项 为 暂存。
        当前 为 最小。
    完。
完。

堆_插入 接受 堆、值：
    做 追加 用 堆、值。
    做 堆_上浮 用 堆、做 计数 用 堆 减 1。
完。

堆_弹出 接受 堆：
    n 为 做 计数 用 堆。
    若 n 等于 0：
        返还 空。
    完。
    最小值 为 堆 的 第 0 项。
    堆 的 第 0 项 为 堆 的 第 n 减 1 项。
    做 删除 用 堆、n 减 1。
    若 做 计数 用 堆 大于 0：
        做 堆_下沉 用 堆、0。
    完。
    返还 最小值。
完。

堆_从列表 接受 列表：
    可变 堆 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 堆、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 做 计数 用 堆 除以 2 减 1。
    只要 i 大于或等于 0：
        做 堆_下沉 用 堆、i。
        i 为 i 减 1。
    完。
    返还 堆。
完。

堆排序 接受 列表、_扩2：
    可变 堆 为 做 堆_从列表 用 列表。
    可变 结果 为 一组：（空）。
    可变 n 为 做 计数 用 堆。
    只要 n 大于 0：
        做 追加 用 结果、堆 的 第 0 项。
        堆 的 第 0 项 为 堆 的 第 n 减 1 项。
        n 为 n 减 1。
        可变 i 为 0。
        可变 _下沉1 为 真。
        只要 _下沉1：
            可变 最小 为 i。
            左 为 2 乘 i 加 1。
            右 为 2 乘 i 加 2。
            若 左 小于 n 并且 堆 的 第 左 项 小于 堆 的 第 最小 项：
                最小 为 左。
            完。
            若 右 小于 n 并且 堆 的 第 右 项 小于 堆 的 第 最小 项：
                最小 为 右。
            完。
            若 最小 等于 i：
                _下沉1 为 假。
            否则：
                暂存 为 堆 的 第 i 项。
                堆 的 第 i 项 为 堆 的 第 最小 项。
                堆 的 第 最小 项 为 暂存。
                i 为 最小。
            完。
        完。
    完。
    返还 结果。
完。

堆_合并 接受 堆a、堆b：
    可变 合并 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 堆a：
        做 追加 用 合并、堆a 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 堆b：
        做 追加 用 合并、堆b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 堆_从列表 用 合并。
完。

前K小 接受 列表、k：
    可变 堆 为 做 堆_从列表 用 列表。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    可变 n 为 做 计数 用 堆。
    只要 i 小于 k 并且 n 大于 0：
        做 追加 用 结果、堆 的 第 0 项。
        堆 的 第 0 项 为 堆 的 第 n 减 1 项。
        n 为 n 减 1。
        可变 j 为 0。
        可变 _下沉2 为 真。
        只要 _下沉2：
            可变 最小 为 j。
            左 为 2 乘 j 加 1。
            右 为 2 乘 j 加 2。
            若 左 小于 n 并且 堆 的 第 左 项 小于 堆 的 第 最小 项：
                最小 为 左。
            完。
            若 右 小于 n 并且 堆 的 第 右 项 小于 堆 的 第 最小 项：
                最小 为 右。
            完。
            若 最小 等于 j：
                _下沉2 为 假。
            否则：
                暂存 为 堆 的 第 j 项。
                堆 的 第 j 项 为 堆 的 第 最小 项。
                堆 的 第 最小 项 为 暂存。
                j 为 最小。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

堆化 接受 数组：
    n 为 做 计数 用 数组。
    可变 i 为 n 除以 2 减 1。
    只要 i 大于或等于 0：
        做 堆_下沉带大小 用 数组、i、n。
        i 为 i 减 1。
    完。
完。

堆_下沉带大小 接受 数组、索引、大小：
    可变 j 为 索引。
    只要 真：
        可变 最小 为 j。
        左 为 2 乘 j 加 1。
        右 为 2 乘 j 加 2。
        若 左 小于 大小 并且 数组 的 第 左 项 小于 数组 的 第 最小 项：
            最小 为 左。
        完。
        若 右 小于 大小 并且 数组 的 第 右 项 小于 数组 的 第 最小 项：
            最小 为 右。
        完。
        若 最小 等于 j：
            返还。
        完。
        暂存 为 数组 的 第 j 项。
        数组 的 第 j 项 为 数组 的 第 最小 项。
        数组 的 第 最小 项 为 暂存。
        j 为 最小。
    完。
完。

最小堆_优先队列_创建 接受 零参：
    返还 一组：一组：（空）、一组：（空）。
完。

优先队列_入队 接受 队列、优先级、数据：
    做 追加 用 队列 的 第 0 项、优先级。
    做 追加 用 队列 的 第 1 项、数据。
    可变 i 为 做 计数 用 队列 的 第 0 项 减 1。
    只要 i 大于 0：
        父 为 (i 减 1) 除以 2。
        若 队列 的 第 0 项 的 第 i 项 大于或等于 队列 的 第 0 项 的 第 父 项：
            返还。
        完。
        暂存p 为 队列 的 第 0 项 的 第 i 项。
        队列 的 第 0 项 的 第 i 项 为 队列 的 第 0 项 的 第 父 项。
        队列 的 第 0 项 的 第 父 项 为 暂存p。
        暂存d 为 队列 的 第 1 项 的 第 i 项。
        队列 的 第 1 项 的 第 i 项 为 队列 的 第 1 项 的 第 父 项。
        队列 的 第 1 项 的 第 父 项 为 暂存d。
        i 为 父。
    完。
完。

优先队列_出队 接受 队列：
    若 做 计数 用 队列 的 第 0 项 等于 0：
        返还 空。
    完。
    结果数据 为 队列 的 第 1 项 的 第 0 项。
    n 为 做 计数 用 队列 的 第 0 项 减 1。
    队列 的 第 0 项 的 第 0 项 为 队列 的 第 0 项 的 第 n 项。
    队列 的 第 1 项 的 第 0 项 为 队列 的 第 1 项 的 第 n 项。
    做 弹出 用 队列 的 第 0 项。
    做 弹出 用 队列 的 第 1 项。
    可变 j 为 0。
    只要 真：
        可变 最小 为 j。
        左 为 2 乘 j 加 1。
        右 为 2 乘 j 加 2。
        若 左 小于 n 并且 队列 的 第 0 项 的 第 左 项 小于 队列 的 第 0 项 的 第 最小 项：
            最小 为 左。
        完。
        若 右 小于 n 并且 队列 的 第 0 项 的 第 右 项 小于 队列 的 第 0 项 的 第 最小 项：
            最小 为 右。
        完。
        若 最小 等于 j：
            返还 结果数据。
        完。
        暂存p 为 队列 的 第 0 项 的 第 j 项。
        队列 的 第 0 项 的 第 j 项 为 队列 的 第 0 项 的 第 最小 项。
        队列 的 第 0 项 的 第 最小 项 为 暂存p。
        暂存d 为 队列 的 第 1 项 的 第 j 项。
        队列 的 第 1 项 的 第 j 项 为 队列 的 第 1 项 的 第 最小 项。
        队列 的 第 1 项 的 第 最小 项 为 暂存d。
        j 为 最小。
    完。
    返还 结果数据。
完。

优先队列_查看顶部 接受 队列：
    若 做 计数 用 队列 的 第 0 项 等于 0：
        返还 空。
    完。
    返还 一组：队列 的 第 0 项 的 第 0 项、队列 的 第 1 项 的 第 0 项。
完。

最小堆_优先队列_大小 接受 队列：
    返还 做 计数 用 队列 的 第 0 项。
完。

优先队列_为空 接受 队列：
    返还 做 计数 用 队列 的 第 0 项 等于 0。
完。

最大堆_插入 接受 堆、值：
    做 追加 用 堆、值。
    可变 i 为 做 计数 用 堆 减 1。
    只要 i 大于 0：
        父 为 (i 减 1) 除以 2。
        若 堆 的 第 i 项 小于或等于 堆 的 第 父 项：
            返还。
        完。
        暂存 为 堆 的 第 i 项。
        堆 的 第 i 项 为 堆 的 第 父 项。
        堆 的 第 父 项 为 暂存。
        i 为 父。
    完。
完。

最大堆_弹出 接受 堆：
    若 做 计数 用 堆 等于 0：
        返还 空。
    完。
    最大值 为 堆 的 第 0 项。
    n 为 做 计数 用 堆 减 1。
    堆 的 第 0 项 为 堆 的 第 n 项。
    做 弹出 用 堆。
    可变 j 为 0。
    只要 真：
        可变 最大 为 j。
        左 为 2 乘 j 加 1。
        右 为 2 乘 j 加 2。
        若 左 小于 n 并且 堆 的 第 左 项 大于 堆 的 第 最大 项：
            最大 为 左。
        完。
        若 右 小于 n 并且 堆 的 第 右 项 大于 堆 的 第 最大 项：
            最大 为 右。
        完。
        若 最大 等于 j：
            返还 最大值。
        完。
        暂存 为 堆 的 第 j 项。
        堆 的 第 j 项 为 堆 的 第 最大 项。
        堆 的 第 最大 项 为 暂存。
        j 为 最大。
    完。
    返还 最大值。
完。

最大堆_查看 接受 堆：
    若 做 计数 用 堆 等于 0：
        返还 空。
    完。
    返还 堆 的 第 0 项。
完。

本章 收束。
