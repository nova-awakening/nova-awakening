《动态规划》

DP_LCS长度 接受 序列A、序列B：
    m 为 做 计数 用 序列A。
    n 为 做 计数 用 序列B。
    若 m 等于 0 或者 n 等于 0：
        返还 0。
    完。
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 m：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于或等于 n：
            做 追加 用 行、0。
            j 为 j 加 1。
        完。
        做 追加 用 dp、行。
        i 为 i 加 1。
    完。
    i 为 1。
    只要 i 小于或等于 m：
        可变 j 为 1。
        只要 j 小于或等于 n：
            若 序列A 的 第 i 减 1 项 等于 序列B 的 第 j 减 1 项：
                dp 的 第 i 项 的 第 j 项 为 dp 的 第 i 减 1 项 的 第 j 减 1 项 加 1。
            否则：
                可变 _dpv 为 dp 的 第 i 项 的 第 j 减 1 项。
                若 dp 的 第 i 减 1 项 的 第 j 项 大于 dp 的 第 i 项 的 第 j 减 1 项：
                    _dpv 为 dp 的 第 i 减 1 项 的 第 j 项。
                完。
                dp 的 第 i 项 的 第 j 项 为 _dpv。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 dp 的 第 m 项 的 第 n 项。
完。

DP_LCS相似度 接受 序列A、序列B：
    m 为 做 计数 用 序列A。
    n 为 做 计数 用 序列B。
    若 m 等于 0 并且 n 等于 0：
        返还 100。
    完。
    若 m 等于 0 或者 n 等于 0：
        返还 0。
    完。
    lcs 为 做 DP_LCS长度 用 序列A、序列B。
    可变 最大长 为 n。
    若 m 大于 n：
        最大长 为 m。
    完。
    若 最大长 等于 0：
        返还 0。
    完。
    返还 lcs 乘 100 除以 最大长。
完。

DP_编辑距离 接受 序列A、序列B：
    可变 curr 为 一组：（空）。
    m 为 做 计数 用 序列A。
    n 为 做 计数 用 序列B。
    可变 前值 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于或等于 n：
        做 追加 用 前值、j。
        做 追加 用 curr、0。
        j 为 j 加 1。
    完。
    可变 i 为 1。
    只要 i 小于或等于 m：
        curr 的 第 0 项 为 i。
        j 为 1。
        只要 j 小于或等于 n：
            若 序列A 的 第 i 减 1 项 等于 序列B 的 第 j 减 1 项：
                curr 的 第 j 项 为 前值 的 第 j 减 1 项。
            否则：
                可变 最小 为 前值 的 第 j 减 1 项 加 1。
                若 前值 的 第 j 项 加 1 小于 最小：
                    最小 为 前值 的 第 j 项 加 1。
                完。
                若 curr 的 第 j 减 1 项 加 1 小于 最小：
                    最小 为 curr 的 第 j 减 1 项 加 1。
                完。
                curr 的 第 j 项 为 最小。
            完。
            j 为 j 加 1。
        完。
        curr 为 一组：（空）。
        j 为 0。
        只要 j 小于或等于 n：
            做 追加 用 curr、0。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 前值 的 第 n 项。
完。

DP_编辑相似度 接受 序列A、序列B：
    m 为 做 计数 用 序列A。
    n 为 做 计数 用 序列B。
    若 m 等于 0 并且 n 等于 0：
        返还 100。
    完。
    可变 最大长 为 n。
    若 m 大于 n：
        最大长 为 m。
    完。
    ed 为 做 DP_编辑距离 用 序列A、序列B。
    若 ed 大于或等于 最大长：
        返还 0。
    完。
    若 最大长 等于 0：
        返还 0。
    完。
    返还 (最大长 减 ed) 乘 100 除以 最大长。
完。

DP_背包最大价值 接受 物品列表、容量：
    n 为 做 计数 用 物品列表。
    若 n 等于 0 或者 容量 等于 0：
        返还 0。
    完。
    可变 dp 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于或等于 容量：
        做 追加 用 dp、0。
        j 为 j 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        w 为 物品列表 的 第 i 项 的 第 0 项。
        v 为 物品列表 的 第 i 项 的 第 1 项。
        j 为 容量。
        只要 j 大于或等于 w：
            若 dp 的 第 j 减 w 项 加 v 大于 dp 的 第 j 项：
                dp 的 第 j 项 为 dp 的 第 j 减 w 项 加 v。
            完。
            j 为 j 减 1。
        完。
        i 为 i 加 1。
    完。
    返还 dp 的 第 容量 项。
完。

DP_LIS长度 接受 序列：
    n 为 做 计数 用 序列。
    若 n 等于 0：
        返还 0。
    完。
    可变 dp 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 dp、1。
        i 为 i 加 1。
    完。
    可变 最大 为 1。
    i 为 1。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 i：
            若 序列 的 第 j 项 小于 序列 的 第 i 项 并且 dp 的 第 j 项 加 1 大于 dp 的 第 i 项：
                dp 的 第 i 项 为 dp 的 第 j 项 加 1。
            完。
            j 为 j 加 1。
        完。
        若 dp 的 第 i 项 大于 最大：
            最大 为 dp 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

DP_汉字序列相似度 接受 码点序列A、码点序列B：
    返还 做 DP_LCS相似度 用 码点序列A、码点序列B。
完。

本章 收束。
