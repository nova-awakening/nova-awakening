《快速排序》

引用《..·..·基础》。

引用《..·..·数学·工具》。

引用《.·归并排序》。

快排_交换 接受 列表、i、j：
    暂存 为 列表 的 第 i 项。
    列表 的 第 i 项 为 列表 的 第 j 项。
    列表 的 第 j 项 为 暂存。
完。

可变 排序_统计 为 一组：0、0、0。

排序_统计报告 接受 零参：
    返还 一组：排序_统计 的 第 0 项、排序_统计 的 第 1 项、排序_统计 的 第 3 项。
完。

排序_重置统计 接受 零参：
    排序_统计 的 第 0 项 为 0。
    排序_统计 的 第 1 项 为 0。
    排序_统计 的 第 2 项 为 0。
    排序_统计 的 第 3 项 为 0。
完。

分区 接受 列表、低、高：
    基准 为 列表 的 第 高 项。
    可变 i 为 低 减 1。
    可变 j 为 低。
    只要 j 小于 高：
        排序_统计 的 第 0 项 为 排序_统计 的 第 0 项 加 1。
        若 列表 的 第 j 项 小于或等于 基准：
            i 为 i 加 1。
            做 快排_交换 用 列表、i、j。
            排序_统计 的 第 1 项 为 排序_统计 的 第 1 项 加 1。
        完。
        j 为 j 加 1。
    完。
    做 快排_交换 用 列表、i 加 1、高。
    排序_统计 的 第 1 项 为 排序_统计 的 第 1 项 加 1。
    返还 i 加 1。
完。

三数取中 接受 列表、低、高：
    中 为 (低 加 高) 除以 2。
    若 列表 的 第 低 项 大于 列表 的 第 中 项：
        做 快排_交换 用 列表、低、中。
    完。
    若 列表 的 第 低 项 大于 列表 的 第 高 项：
        做 快排_交换 用 列表、低、高。
    完。
    若 列表 的 第 中 项 大于 列表 的 第 高 项：
        做 快排_交换 用 列表、中、高。
    完。
    做 快排_交换 用 列表、中、高 减 1。
    返还 列表 的 第 高 减 1 项。
完。

分区_三数 接受 列表、低、高：
    若 高 减 低 小于 3：
        返还 做 分区 用 列表、低、高。
    完。
    基准 为 做 三数取中 用 列表、低、高。
    可变 i 为 低。
    可变 j 为 高 减 1。
    只要 真：
        i 为 i 加 1。
        只要 i 小于 高 减 1 并且 列表 的 第 i 项 小于 基准：
            i 为 i 加 1。
            排序_统计 的 第 0 项 为 排序_统计 的 第 0 项 加 1。
        完。
        j 为 j 减 1。
        只要 j 大于 低 并且 列表 的 第 j 项 大于 基准：
            j 为 j 减 1。
            排序_统计 的 第 0 项 为 排序_统计 的 第 0 项 加 1。
        完。
        若 i 大于或等于 j：
            做 快排_交换 用 列表、i、高 减 1。
            返还 i。
        完。
        做 快排_交换 用 列表、i、j。
        排序_统计 的 第 1 项 为 排序_统计 的 第 1 项 加 1。
    完。
    做 快排_交换 用 列表、i、高 减 1。
    返还 i。
完。

快排内部 接受 列表、低、高：
    排序_统计 的 第 2 项 为 排序_统计 的 第 2 项 加 1。
    若 排序_统计 的 第 2 项 大于 排序_统计 的 第 3 项：
        排序_统计 的 第 3 项 为 排序_统计 的 第 2 项。
    完。
    若 低 小于 高：
        若 高 减 低 小于 16：
            做 插入排序_范围 用 列表、低、高。
        否则：
            中 为 做 分区_三数 用 列表、低、高。
            做 快排内部 用 列表、低、中 减 1。
            做 快排内部 用 列表、中 加 1、高。
        完。
    完。
    排序_统计 的 第 2 项 为 排序_统计 的 第 2 项 减 1。
完。

快排_快速排序 接受 列表：
    长 为 做 计数 用 列表。
    若 长 大于 1：
        做 快排内部 用 列表、0、长 减 1。
    完。
    返还 列表。
完。

快排_插入排序 接受 列表：
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        键 为 列表 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 列表 的 第 j 项 大于 键：
            列表 的 第 j 加 1 项 为 列表 的 第 j 项。
            j 为 j 减 1。
            排序_统计 的 第 0 项 为 排序_统计 的 第 0 项 加 1。
        完。
        列表 的 第 j 加 1 项 为 键。
        i 为 i 加 1。
    完。
    返还 列表。
完。

插入排序_范围 接受 列表、低、高：
    可变 i 为 低 加 1。
    只要 i 小于或等于 高：
        键 为 列表 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 低 并且 列表 的 第 j 项 大于 键：
            列表 的 第 j 加 1 项 为 列表 的 第 j 项。
            j 为 j 减 1。
            排序_统计 的 第 0 项 为 排序_统计 的 第 0 项 加 1。
        完。
        列表 的 第 j 加 1 项 为 键。
        i 为 i 加 1。
    完。
完。

归并 接受 列表、临时、左起、左止、右止：
    可变 i 为 左起。
    可变 j 为 左止 加 1。
    可变 k 为 左起。
    只要 i 小于或等于 左止 并且 j 小于或等于 右止：
        排序_统计 的 第 0 项 为 排序_统计 的 第 0 项 加 1。
        若 列表 的 第 i 项 小于或等于 列表 的 第 j 项：
            临时 的 第 k 项 为 列表 的 第 i 项。
            i 为 i 加 1。
        否则：
            临时 的 第 k 项 为 列表 的 第 j 项。
            j 为 j 加 1。
        完。
        k 为 k 加 1。
    完。
    只要 i 小于或等于 左止：
        临时 的 第 k 项 为 列表 的 第 i 项。
        i 为 i 加 1。
        k 为 k 加 1。
    完。
    只要 j 小于或等于 右止：
        临时 的 第 k 项 为 列表 的 第 j 项。
        j 为 j 加 1。
        k 为 k 加 1。
    完。
    i 为 左起。
    只要 i 小于或等于 右止：
        列表 的 第 i 项 为 临时 的 第 i 项。
        i 为 i 加 1。
    完。
完。

归并排序内部 接受 列表、临时、左、右：
    若 左 小于 右：
        中 为 (左 加 右) 除以 2。
        做 归并排序内部 用 列表、临时、左、中。
        做 归并排序内部 用 列表、临时、中 加 1、右。
        做 归并 用 列表、临时、左、中、右。
    完。
完。

快排_归并排序 接受 列表：
    长 为 做 计数 用 列表。
    若 长 小于或等于 1：
        返还 列表。
    完。
    可变 临时 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长：
        做 追加 用 临时、0。
        i 为 i 加 1。
    完。
    做 归并排序内部 用 列表、临时、0、长 减 1。
    返还 列表。
完。

下沉 接受 列表、大小、根：
    可变 最大 为 根。
    左 为 2 乘 根 加 1。
    右 为 2 乘 根 加 2。
    排序_统计 的 第 0 项 为 排序_统计 的 第 0 项 加 1。
    若 左 小于 大小 并且 列表 的 第 左 项 大于 列表 的 第 最大 项：
        最大 为 左。
    完。
    排序_统计 的 第 0 项 为 排序_统计 的 第 0 项 加 1。
    若 右 小于 大小 并且 列表 的 第 右 项 大于 列表 的 第 最大 项：
        最大 为 右。
    完。
    若 最大 不等于 根：
        做 快排_交换 用 列表、根、最大。
        排序_统计 的 第 1 项 为 排序_统计 的 第 1 项 加 1。
        做 下沉 用 列表、大小、最大。
    完。
完。

快排_堆排序 接受 列表：
    长 为 做 计数 用 列表。
    可变 i 为 长 除以 2 减 1。
    只要 i 大于或等于 0：
        做 下沉 用 列表、长、i。
        i 为 i 减 1。
    完。
    i 为 长 减 1。
    只要 i 大于 0：
        做 快排_交换 用 列表、0、i。
        做 下沉 用 列表、i、0。
        i 为 i 减 1。
    完。
    返还 列表。
完。

快排_计数排序 接受 列表：
    可变 计数表 为 一组：（空）。
    长 为 做 计数 用 列表。
    若 长 小于或等于 1：
        返还 列表。
    完。
    可变 最小 为 列表 的 第 0 项。
    可变 最大 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 长：
        若 列表 的 第 i 项 小于 最小：
            最小 为 列表 的 第 i 项。
        完。
        若 列表 的 第 i 项 大于 最大：
            最大 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    范围 为 最大 减 最小 加 1。
    若 范围 大于 100000：
        返还 做 快排_快速排序 用 列表。
    完。
    i 为 0。
    只要 i 小于 范围：
        做 追加 用 计数表、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 长：
        计数表 的 第 列表 的 第 i 项 减 最小 项 为 计数表 的 第 列表 的 第 i 项 减 最小 项 加 1。
        i 为 i 加 1。
    完。
    可变 写位 为 0。
    i 为 0。
    只要 i 小于 范围：
        只要 计数表 的 第 i 项 大于 0：
            列表 的 第 写位 项 为 i 加 最小。
            写位 为 写位 加 1。
            计数表 的 第 i 项 为 计数表 的 第 i 项 减 1。
        完。
        i 为 i 加 1。
    完。
    返还 列表。
完。

希尔排序 接受 列表：
    长 为 做 计数 用 列表。
    可变 间隔 为 长 除以 2。
    只要 间隔 大于 0：
        可变 i 为 间隔。
        只要 i 小于 长：
            键 为 列表 的 第 i 项。
            可变 j 为 i。
            只要 j 大于或等于 间隔 并且 列表 的 第 j 减 间隔 项 大于 键：
                列表 的 第 j 项 为 列表 的 第 j 减 间隔 项。
                j 为 j 减 间隔。
                排序_统计 的 第 0 项 为 排序_统计 的 第 0 项 加 1。
            完。
            列表 的 第 j 项 为 键。
            i 为 i 加 1。
        完。
        间隔 为 间隔 除以 2。
    完。
    返还 列表。
完。

快排_选择排序 接受 列表：
    长 为 做 计数 用 列表。
    可变 i 为 0。
    只要 i 小于 长 减 1：
        可变 最小位 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 长：
            排序_统计 的 第 0 项 为 排序_统计 的 第 0 项 加 1。
            若 列表 的 第 j 项 小于 列表 的 第 最小位 项：
                最小位 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最小位 不等于 i：
            做 快排_交换 用 列表、i、最小位。
            排序_统计 的 第 1 项 为 排序_统计 的 第 1 项 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 列表。
完。

计算逆序度 接受 列表：
    长 为 做 计数 用 列表。
    若 长 小于或等于 1：
        返还 0。
    完。
    可变 有序 为 0。
    可变 i 为 0。
    只要 i 小于 长 减 1：
        若 列表 的 第 i 项 大于 列表 的 第 i 加 1 项：
            逆序 为 逆序 加 1。
        否则：
            有序 为 有序 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 逆序 乘 100 除以 (长 减 1)。
完。

已排序 接受 列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表 减 1：
        若 列表 的 第 i 项 大于 列表 的 第 i 加 1 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

已逆序 接受 列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表 减 1：
        若 列表 的 第 i 项 小于 列表 的 第 i 加 1 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

智能排序 接受 列表：
    长 为 做 计数 用 列表。
    做 排序_重置统计。
    若 长 小于或等于 1：
        返还 列表。
    完。
    若 做 已排序 用 列表：
        返还 列表。
    完。
    若 长 小于或等于 16：
        返还 做 插入排序 用 列表、0、0。
    完。
    若 做 已逆序 用 列表：
        可变 i 为 0。
        可变 j 为 长 减 1。
        只要 i 小于 j：
            做 快排_交换 用 列表、i、j。
            i 为 i 加 1。
            j 为 j 减 1。
        完。
        返还 列表。
    完。
    逆序度 为 做 计算逆序度 用 列表。
    若 逆序度 小于 15：
        返还 做 插入排序 用 列表、0、0。
    完。
    若 长 小于或等于 64：
        返还 做 希尔排序 用 列表。
    完。
    返还 做 快排_快速排序 用 列表。
完。

预测排序复杂度 接受 列表：
    长 为 做 计数 用 列表。
    若 长 小于或等于 1：
        返还 一组：0、「常数」。
    完。
    逆序度 为 做 计算逆序度 用 列表。
    若 逆序度 小于 10：
        返还 一组：长、「近线性(接近有序)」。
    完。
    若 长 小于或等于 16：
        返还 一组：长 乘 长、「平方(小数组插入)」。
    完。
    可变 估算 为 0。
    可变 n 为 长。
    只要 n 大于 1：
        估算 为 估算 加 1。
        n 为 n 除以 2。
    完。
    返还 一组：长 乘 估算、「对数线性(快排/归并)」。
完。

快速排序_二分查找 接受 列表、目标：
    可变 低 为 0。
    可变 高 为 做 计数 用 列表 减 1。
    只要 低 小于或等于 高：
        中 为 (低 加 高) 除以 2。
        若 列表 的 第 中 项 等于 目标：
            返还 中。
        完。
        若 列表 的 第 中 项 小于 目标：
            低 为 中 加 1。
        否则：
            高 为 中 减 1。
        完。
    完。
    返还 0 减 1。
完。

二分查找_左边界 接受 列表、目标：
    可变 低 为 0。
    可变 高 为 做 计数 用 列表。
    只要 低 小于 高：
        中 为 (低 加 高) 除以 2。
        若 列表 的 第 中 项 小于 目标：
            低 为 中 加 1。
        否则：
            高 为 中。
        完。
    完。
    返还 低。
完。

二分查找_右边界 接受 列表、目标：
    可变 低 为 0。
    可变 高 为 做 计数 用 列表。
    只要 低 小于 高：
        中 为 (低 加 高) 除以 2。
        若 列表 的 第 中 项 小于或等于 目标：
            低 为 中 加 1。
        否则：
            高 为 中。
        完。
    完。
    返还 低 减 1。
完。

快速排序_快速选择 接受 列表、k：
    可变 低 为 0。
    可变 高 为 做 计数 用 列表 减 1。
    只要 低 小于 高：
        基准位 为 做 分区 用 列表、低、高。
        若 基准位 等于 k：
            返还 列表 的 第 k 项。
        完。
        若 基准位 小于 k：
            低 为 基准位 加 1。
        否则：
            高 为 基准位 减 1。
        完。
    完。
    返还 列表 的 第 低 项。
完。

快排_中位数 接受 列表：
    长 为 做 计数 用 列表。
    若 长 等于 0：
        返还 0。
    完。
    可变 副本 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长：
        做 追加 用 副本、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 快速排序_快速选择 用 副本、长 除以 2。
完。

数组最大值 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 最大 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 大于 最大：
            最大 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

数组最小值 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 最小 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 最小：
            最小 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小。
完。

数组求和 接受 列表：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        和 为 和 加 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

数组平均 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    返还 做 数组求和 用 列表 除以 做 计数 用 列表。
完。

数组反转 接受 列表：
    可变 i 为 0。
    可变 j 为 做 计数 用 列表 减 1。
    只要 i 小于 j：
        做 快排_交换 用 列表、i、j。
        i 为 i 加 1。
        j 为 j 减 1。
    完。
    返还 列表。
完。

数组去重 接受 列表：
    若 做 计数 用 列表 小于或等于 1：
        返还 列表。
    完。
    可变 副本 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 副本、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    做 快排_快速排序 用 副本。
    可变 结果 为 一组：副本 的 第 0 项。
    i 为 1。
    只要 i 小于 做 计数 用 副本：
        若 副本 的 第 i 项 不等于 副本 的 第 i 减 1 项：
            做 追加 用 结果、副本 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数组填充 接受 大小、值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        做 追加 用 结果、值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数组范围 接受 起始、结束：
    可变 结果 为 一组：（空）。
    可变 i 为 起始。
    只要 i 小于 结束：
        做 追加 用 结果、i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数组复制 接受 列表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

排序推荐 接受 列表：
    长 为 做 计数 用 列表。
    若 长 小于或等于 1：
        返还 「无需排序」。
    完。
    若 长 小于或等于 16：
        返还 「插入排序(小数组最优, 0, 0)」。
    完。
    逆序度 为 做 计算逆序度 用 列表。
    若 逆序度 小于 10：
        返还 「插入排序(近有序, 0, 0)」。
    完。
    若 逆序度 大于 90：
        返还 「反转+插入(近逆序)」。
    完。
    若 长 小于或等于 100：
        返还 「希尔排序(中等数组)」。
    完。
    若 长 大于 10000：
        返还 「归并排序(大数组稳定, 0)」。
    完。
    返还 「快速排序(通用最优, 0)」。
完。

三路快排_内部 接受 列表、低、高：
    若 低 大于或等于 高：
        返还 空。
    完。
    若 高 减 低 小于 10：
        做 插入排序_范围 用 列表、低、高。
        返还 空。
    完。
    基准 为 列表 的 第 (低 加 高) 除以 2 项。
    可变 lt 为 低。
    可变 gt 为 高。
    可变 i 为 低。
    只要 i 小于或等于 gt：
        若 列表 的 第 i 项 小于 基准：
            做 快排_交换 用 列表、lt、i。
            lt 为 lt 加 1。
            i 为 i 加 1。
        否则若 列表 的 第 i 项 大于 基准：
            做 快排_交换 用 列表、i、gt。
            gt 为 gt 减 1。
        否则：
            i 为 i 加 1。
        完。
    完。
    若 lt 大于 低：
        做 三路快排_内部 用 列表、低、lt 减 1。
    完。
    若 gt 小于 高：
        做 三路快排_内部 用 列表、gt 加 1、高。
    完。
完。

三路快速排序 接受 列表：
    长 为 做 计数 用 列表。
    若 长 大于 1：
        做 三路快排_内部 用 列表、0、长 减 1。
    完。
    返还 列表。
完。

自适应快排 接受 列表：
    长 为 做 计数 用 列表。
    若 长 小于或等于 1：
        返还 列表。
    完。
    可变 采样大小 为 10。
    若 长 小于 10：
        采样大小 为 长。
    完。
    可变 唯一数 为 0。
    可变 已见 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 采样大小：
        索引 为 i 乘 长 除以 采样大小。
        数值 为 列表 的 第 索引 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 已见：
            若 已见 的 第 j 项 等于 数值：
                重复 为 真。
            完。
            j 为 j 加 1。
        完。
        若 非 重复：
            做 追加 用 已见、数值。
            唯一数 为 唯一数 加 1。
        完。
        i 为 i 加 1。
    完。
    唯一率 为 唯一数 乘 100 除以 采样大小。
    若 唯一率 小于 70：
        返还 做 三路快速排序 用 列表。
    完。
    返还 做 快排_快速排序 用 列表。
完。

本章 收束。
