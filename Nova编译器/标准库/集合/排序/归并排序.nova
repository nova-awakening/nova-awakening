《归并排序》

引用《..·..·基础》。

归并排序_归并排序 接受 列表：
    n 为 做 计数 用 列表。
    若 n 小于或等于 1：
        返还 列表。
    完。
    中 为 n 除以 2。
    可变 左 为 一组：（空）。
    可变 右 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 中：
        做 追加 用 左、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 i 小于 n：
        做 追加 用 右、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    左 为 做 归并排序_归并排序 用 左。
    右 为 做 归并排序_归并排序 用 右。
    返还 做 归并排序_合并 用 左、右。
完。

归并排序_合并 接受 左、右：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    可变 j 为 0。
    只要 i 小于 做 计数 用 左 并且 j 小于 做 计数 用 右：
        若 左 的 第 i 项 小于或等于 右 的 第 j 项：
            做 追加 用 结果、左 的 第 i 项。
            i 为 i 加 1。
        否则：
            做 追加 用 结果、右 的 第 j 项。
            j 为 j 加 1。
        完。
    完。
    只要 i 小于 做 计数 用 左：
        做 追加 用 结果、左 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 j 小于 做 计数 用 右：
        做 追加 用 结果、右 的 第 j 项。
        j 为 j 加 1。
    完。
    返还 结果。
完。

归并排序比较 接受 列表、比较器：
    n 为 做 计数 用 列表。
    若 n 小于或等于 1：
        返还 列表。
    完。
    中 为 n 除以 2。
    可变 左 为 一组：（空）。
    可变 右 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 中：
        做 追加 用 左、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 i 小于 n：
        做 追加 用 右、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    左 为 做 归并排序比较 用 左、比较器。
    右 为 做 归并排序比较 用 右、比较器。
    返还 做 合并比较 用 左、右、比较器。
完。

合并比较 接受 左、右、比较器：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    可变 j 为 0。
    只要 i 小于 做 计数 用 左 并且 j 小于 做 计数 用 右：
        若 做 比较器 用 左 的 第 i 项、右 的 第 j 项 小于或等于 0：
            做 追加 用 结果、左 的 第 i 项。
            i 为 i 加 1。
        否则：
            做 追加 用 结果、右 的 第 j 项。
            j 为 j 加 1。
        完。
    完。
    只要 i 小于 做 计数 用 左：
        做 追加 用 结果、左 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 j 小于 做 计数 用 右：
        做 追加 用 结果、右 的 第 j 项。
        j 为 j 加 1。
    完。
    返还 结果。
完。

插入排序 接受 列表、_扩2、_扩3：
    n 为 做 计数 用 列表。
    可变 i 为 1。
    只要 i 小于 n：
        键 为 列表 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 列表 的 第 j 项 大于 键：
            列表 的 第 j 加 1 项 为 列表 的 第 j 项。
            j 为 j 减 1。
        完。
        列表 的 第 j 加 1 项 为 键。
        i 为 i 加 1。
    完。
    返还 列表。
完。

选择排序 接受 列表、_扩2：
    n 为 做 计数 用 列表。
    可变 i 为 0。
    只要 i 小于 n 减 1：
        可变 最小idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 列表 的 第 j 项 小于 列表 的 第 最小idx 项：
                最小idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最小idx 不等于 i：
            暂存 为 列表 的 第 i 项。
            列表 的 第 i 项 为 列表 的 第 最小idx 项。
            列表 的 第 最小idx 项 为 暂存。
        完。
        i 为 i 加 1。
    完。
    返还 列表。
完。

冒泡排序 接受 列表、_扩2、_扩3：
    n 为 做 计数 用 列表。
    可变 i 为 0。
    只要 i 小于 n：
        可变 已交换 为 假。
        可变 j 为 0。
        只要 j 小于 n 减 i 减 1：
            若 列表 的 第 j 项 大于 列表 的 第 j 加 1 项：
                暂存 为 列表 的 第 j 项。
                列表 的 第 j 项 为 列表 的 第 j 加 1 项。
                列表 的 第 j 加 1 项 为 暂存。
                已交换 为 真。
            完。
            j 为 j 加 1。
        完。
        若 已交换 等于 假：
            返还 列表。
        完。
        i 为 i 加 1。
    完。
    返还 列表。
完。

计数排序 接受 列表、最大值、_扩3：
    可变 计数表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 最大值：
        做 追加 用 计数表、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 列表：
        计数表 的 第 列表 的 第 i 项 项 为 计数表 的 第 列表 的 第 i 项 项 加 1。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于或等于 最大值：
        可变 j 为 0。
        只要 j 小于 计数表 的 第 i 项：
            做 追加 用 结果、i。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

归并排序_二分查找 接受 排序列表、目标：
    可变 左 为 0。
    可变 右 为 做 计数 用 排序列表 减 1。
    只要 左 小于或等于 右：
        中 为 (左 加 右) 除以 2。
        若 排序列表 的 第 中 项 等于 目标：
            返还 中。
        完。
        若 排序列表 的 第 中 项 小于 目标：
            左 为 中 加 1。
        否则：
            右 为 中 减 1。
        完。
    完。
    返还 负 1。
完。

下界 接受 排序列表、目标：
    可变 左 为 0。
    可变 右 为 做 计数 用 排序列表。
    只要 左 小于 右：
        中 为 (左 加 右) 除以 2。
        若 排序列表 的 第 中 项 小于 目标：
            左 为 中 加 1。
        否则：
            右 为 中。
        完。
    完。
    返还 左。
完。

上界 接受 排序列表、目标：
    可变 左 为 0。
    可变 右 为 做 计数 用 排序列表。
    只要 左 小于 右：
        中 为 (左 加 右) 除以 2。
        若 排序列表 的 第 中 项 小于或等于 目标：
            左 为 中 加 1。
        否则：
            右 为 中。
        完。
    完。
    返还 左。
完。

自然归并 接受 列表：
    可变 新列表 为 一组：（空）。
    n 为 做 计数 用 列表。
    若 n 小于或等于 1：
        返还 列表。
    完。
    可变 段起点 为 一组：0。
    可变 i 为 1。
    只要 i 小于 n：
        若 列表 的 第 i 项 小于 列表 的 第 i 减 1 项：
            做 追加 用 段起点、i。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 段起点、n。
    若 做 计数 用 段起点 等于 2：
        返还 列表。
    完。
    可变 当前 为 列表。
    只要 做 计数 用 段起点 大于 2：
        可变 新段 为 一组：0。
        i 为 0。
        只要 i 加 2 小于 做 计数 用 段起点：
            可变 合并 为 做 归并_合并段 用 当前、段起点 的 第 i 项、段起点 的 第 i 加 1 项、段起点 的 第 i 加 2 项。
            可变 j 为 0。
            只要 j 小于 做 计数 用 合并：
                做 追加 用 新列表、合并 的 第 j 项。
                j 为 j 加 1。
            完。
            做 追加 用 新段、做 计数 用 新列表。
            i 为 i 加 2。
        完。
        若 i 加 1 小于 做 计数 用 段起点：
            可变 j 为 段起点 的 第 i 项。
            只要 j 小于 段起点 的 第 i 加 1 项：
                做 追加 用 新列表、当前 的 第 j 项。
                j 为 j 加 1。
            完。
            做 追加 用 新段、做 计数 用 新列表。
        完。
        当前 为 新列表。
        段起点 为 新段。
    完。
    返还 当前。
完。

归并_合并段 接受 列表、左起、中间、右终：
    可变 结果 为 一组：（空）。
    可变 i 为 左起。
    可变 j 为 中间。
    只要 i 小于 中间 并且 j 小于 右终：
        若 列表 的 第 i 项 小于或等于 列表 的 第 j 项：
            做 追加 用 结果、列表 的 第 i 项。
            i 为 i 加 1。
        否则：
            做 追加 用 结果、列表 的 第 j 项。
            j 为 j 加 1。
        完。
    完。
    只要 i 小于 中间：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 j 小于 右终：
        做 追加 用 结果、列表 的 第 j 项。
        j 为 j 加 1。
    完。
    返还 结果。
完。

是否已排序 接受 列表：
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 列表 的 第 i 减 1 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

本章 收束。
