《队列》

引用《..·..·基础》。

引用《..·栈·栈》。

可变 _队列_入队数 为 0。

可变 _队列_出队数 为 0。

队列_创建 接受 零参：
    返还 一组：一组：（空）、0。
完。

队列_大小 接受 q：
    返还 做 计数 用 q 的 第 0 项 减 q 的 第 1 项。
完。

队列_空 接受 q：
    返还 做 队列_大小 用 q 等于 0。
完。

队列_队列_压缩 接受 q：
    若 q 的 第 1 项 小于或等于 0：
        返还 空。
    完。
    可变 新数据 为 一组：（空）。
    可变 i 为 q 的 第 1 项。
    只要 i 小于 做 计数 用 q 的 第 0 项：
        做 追加 用 新数据、q 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    q 的 第 0 项 为 新数据。
    q 的 第 1 项 为 0。
完。

队列_入 接受 q、值：
    _队列_入队数 为 _队列_入队数 加 1。
    做 追加 用 q 的 第 0 项、值。
完。

队列_出 接受 q：
    若 做 队列_空 用 q：
        返还 空。
    完。
    _队列_出队数 为 _队列_出队数 加 1。
    值 为 q 的 第 0 项 的 第 q 的 第 1 项 项。
    q 的 第 1 项 为 q 的 第 1 项 加 1。
    若 q 的 第 1 项 大于 100：
        若 q 的 第 1 项 大于 做 计数 用 q 的 第 0 项 除以 2：
            做 队列_队列_压缩 用 q。
        完。
    完。
    返还 值。
完。

队列_首 接受 q：
    若 做 队列_空 用 q：
        返还 空。
    完。
    返还 q 的 第 0 项 的 第 q 的 第 1 项 项。
完。

队列_尾 接受 q：
    若 做 队列_空 用 q：
        返还 空。
    完。
    返还 q 的 第 0 项 的 第 做 计数 用 q 的 第 0 项 减 1 项。
完。

队列_转列表 接受 q：
    可变 结果 为 一组：（空）。
    可变 i 为 q 的 第 1 项。
    只要 i 小于 做 计数 用 q 的 第 0 项：
        做 追加 用 结果、q 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

队列_批量入 接受 q、列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 队列_入 用 q、列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

队列_批量出 接受 q、数量：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 数量：
        若 做 队列_空 用 q：
            返还 结果。
        完。
        做 追加 用 结果、做 队列_出 用 q。
        i 为 i 加 1。
    完。
    返还 结果。
完。

队列_清空 接受 q：
    q 的 第 0 项 为 一组：（空）。
    q 的 第 1 项 为 0。
完。

队列_入队 接受 q、值：
    做 队列_入 用 q、值。
完。

队列_出队 接受 q：
    返还 做 队列_出 用 q。
完。

队列_头 接受 q：
    返还 做 队列_首 用 q。
完。

队列_空吗 接受 q：
    返还 做 队列_空 用 q。
完。

队列_队列_到列表 接受 q：
    返还 做 队列_转列表 用 q。
完。

双端队列_创建 接受 零参：
    返还 一组：一组：（空）、0。
完。

双端队列_大小 接受 dq：
    返还 做 计数 用 dq 的 第 0 项 减 dq 的 第 1 项。
完。

双端队列_空 接受 dq：
    返还 做 双端队列_大小 用 dq 等于 0。
完。

双端队列_压缩 接受 dq：
    若 dq 的 第 1 项 小于或等于 0：
        返还 空。
    完。
    可变 新数据 为 一组：（空）。
    可变 i 为 dq 的 第 1 项。
    只要 i 小于 做 计数 用 dq 的 第 0 项：
        做 追加 用 新数据、dq 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    dq 的 第 0 项 为 新数据。
    dq 的 第 1 项 为 0。
完。

双端队列_后入 接受 dq、值：
    做 追加 用 dq 的 第 0 项、值。
完。

双端队列_前入 接受 dq、值：
    可变 新数据 为 一组：值。
    可变 i 为 dq 的 第 1 项。
    只要 i 小于 做 计数 用 dq 的 第 0 项：
        做 追加 用 新数据、dq 的 第 0 项 的 第 i 项。
        i 为 i 加 1。
    完。
    dq 的 第 0 项 为 新数据。
    dq 的 第 1 项 为 0。
完。

双端队列_前出 接受 dq：
    若 做 双端队列_空 用 dq：
        返还 空。
    完。
    值 为 dq 的 第 0 项 的 第 dq 的 第 1 项 项。
    dq 的 第 1 项 为 dq 的 第 1 项 加 1。
    若 dq 的 第 1 项 大于 32：
        若 dq 的 第 1 项 大于 做 计数 用 dq 的 第 0 项 除以 2：
            做 双端队列_压缩 用 dq。
        完。
    完。
    返还 值。
完。

双端队列_后出 接受 dq：
    若 做 双端队列_空 用 dq：
        返还 空。
    完。
    末位 为 做 计数 用 dq 的 第 0 项 减 1。
    值 为 dq 的 第 0 项 的 第 末位 项。
    做 删除 用 dq 的 第 0 项、末位。
    若 dq 的 第 1 项 大于 32：
        若 dq 的 第 1 项 大于 做 计数 用 dq 的 第 0 项 除以 2：
            做 双端队列_压缩 用 dq。
        完。
    完。
    返还 值。
完。

双端队列_前插 接受 dq、值：
    做 双端队列_前入 用 dq、值。
完。

双端队列_后插 接受 dq、值：
    做 双端队列_后入 用 dq、值。
完。

双端队列_前取 接受 dq：
    返还 做 双端队列_前出 用 dq。
完。

双端队列_后取 接受 dq：
    返还 做 双端队列_后出 用 dq。
完。

栈_创建 接受 零参：
    返还 一组：（空）。
完。

栈_大小 接受 s：
    返还 做 计数 用 s。
完。

栈_空 接受 s：
    返还 做 计数 用 s 等于 0。
完。

栈_压入 接受 s、值：
    做 追加 用 s、值。
完。

栈_弹出 接受 s：
    n 为 做 计数 用 s。
    若 n 等于 0：
        返还 空。
    完。
    值 为 s 的 第 n 减 1 项。
    做 删除 用 s、n 减 1。
    返还 值。
完。

栈_顶 接受 s：
    若 做 计数 用 s 等于 0：
        返还 空。
    完。
    返还 s 的 第 做 计数 用 s 减 1 项。
完。

环形缓冲_创建 接受 容量：
    可变 数据 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 容量：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 一组：容量、数据、0、0。
完。

环形缓冲_大小 接受 rb：
    返还 rb 的 第 4 项。
完。

环形缓冲_满 接受 rb：
    返还 rb 的 第 4 项 等于 rb 的 第 0 项。
完。

环形缓冲_空 接受 rb：
    返还 rb 的 第 4 项 等于 0。
完。

环形缓冲_写 接受 rb、值：
    若 做 环形缓冲_满 用 rb：
        返还 假。
    完。
    数据 为 rb 的 第 1 项。
    尾 为 rb 的 第 3 项。
    数据 的 第 尾 项 为 值。
    rb 的 第 3 项 为 (尾 加 1) 取余于 rb 的 第 0 项。
    rb 的 第 4 项 为 rb 的 第 4 项 加 1。
    返还 真。
完。

环形缓冲_读 接受 rb：
    若 做 环形缓冲_空 用 rb：
        返还 空。
    完。
    数据 为 rb 的 第 1 项。
    头 为 rb 的 第 2 项。
    值 为 数据 的 第 头 项。
    rb 的 第 2 项 为 (头 加 1) 取余于 rb 的 第 0 项。
    rb 的 第 4 项 为 rb 的 第 4 项 减 1。
    返还 值。
完。

优先队列_创建 接受 零参：
    返还 一组：（空）。
完。

优先队列_大小 接受 pq：
    返还 做 计数 用 pq。
完。

优先队列_空 接受 pq：
    返还 做 计数 用 pq 等于 0。
完。

优先队列_空吗 接受 pq：
    返还 做 优先队列_空 用 pq。
完。

优先队列_交换 接受 pq、a、b：
    暂存 为 pq 的 第 a 项。
    pq 的 第 a 项 为 pq 的 第 b 项。
    pq 的 第 b 项 为 暂存。
完。

优先队列_上浮 接受 pq、索引：
    可变 i 为 索引。
    只要 i 大于 0：
        父 为 (i 减 1) 除以 2。
        若 pq 的 第 i 项 的 第 0 项 小于 pq 的 第 父 项 的 第 0 项：
            做 优先队列_交换 用 pq、i、父。
            i 为 父。
        否则：
            返还 空。
        完。
    完。
完。

优先队列_下沉 接受 pq、索引：
    可变 最小位 为 0。
    可变 i 为 索引。
    n 为 做 计数 用 pq。
    只要 真：
        左 为 2 乘 i 加 1。
        右 为 2 乘 i 加 2。
        若 左 小于 n：
            若 pq 的 第 左 项 的 第 0 项 小于 pq 的 第 最小位 项 的 第 0 项：
                最小位 为 左。
            完。
        完。
        若 右 小于 n：
            若 pq 的 第 右 项 的 第 0 项 小于 pq 的 第 最小位 项 的 第 0 项：
                最小位 为 右。
            完。
        完。
        若 最小位 等于 i：
            返还 空。
        完。
        做 优先队列_交换 用 pq、i、最小位。
        i 为 最小位。
    完。
完。

优先队列_入 接受 pq、优先级、值：
    做 追加 用 pq、一组：优先级、值。
    做 优先队列_上浮 用 pq、做 计数 用 pq 减 1。
完。

优先队列_出 接受 pq：
    若 做 优先队列_空 用 pq：
        返还 空。
    完。
    结果 为 pq 的 第 0 项。
    末位 为 做 计数 用 pq 减 1。
    末值 为 pq 的 第 末位 项。
    做 删除 用 pq、末位。
    若 做 计数 用 pq 大于 0：
        pq 的 第 0 项 为 末值。
        做 优先队列_下沉 用 pq、0。
    完。
    返还 结果。
完。

优先队列_顶 接受 pq：
    若 做 优先队列_空 用 pq：
        返还 空。
    完。
    返还 pq 的 第 0 项。
完。

延迟队列_创建 接受 零参：
    返还 做 优先队列_创建。
完。

延迟队列_添加 接受 dq、到期时间、值：
    做 优先队列_入 用 dq、到期时间、值。
完。

延迟队列_取到期 接受 dq、当前时间：
    可变 到期列表 为 一组：（空）。
    只要 真：
        若 做 优先队列_空 用 dq：
            返还 到期列表。
        完。
        顶 为 做 优先队列_顶 用 dq。
        若 顶 的 第 0 项 大于 当前时间：
            返还 到期列表。
        完。
        项 为 做 优先队列_出 用 dq。
        做 追加 用 到期列表、项 的 第 1 项。
    完。
完。

队列_记录入 接受 零参：
    _队列_入队数 为 _队列_入队数 加 1。
完。

队列_记录出 接受 零参：
    _队列_出队数 为 _队列_出队数 加 1。
完。

本章 收束。
