《二叉搜索树》

引用《..·..·基础》。

可变 节点_键 为 0。

可变 节点_值 为 1。

可变 节点_左 为 2。

可变 节点_右 为 3。

bst_创建 接受 零参：
    返还 一组：负 1、一组：（空）、0。
完。

bst_节点基位 接受 索引：
    返还 索引 乘 3。
完。

bst_新节点 接受 树、值：
    池 为 树 的 第 1 项。
    新索引 为 树 的 第 2 项。
    做 追加 用 池、值。
    做 追加 用 池、负 1。
    做 追加 用 池、负 1。
    树 的 第 2 项 为 新索引 加 1。
    返还 新索引。
完。

bst_值 接受 树、索引：
    返还 树 的 第 1 项 的 第 做 bst_节点基位 用 索引 项。
完。

bst_左 接受 树、索引：
    返还 树 的 第 1 项 的 第 做 bst_节点基位 用 索引 加 1 项。
完。

bst_右 接受 树、索引：
    返还 树 的 第 1 项 的 第 做 bst_节点基位 用 索引 加 2 项。
完。

bst_设左 接受 树、索引、值：
    池 为 树 的 第 1 项。
    池 的 第 做 bst_节点基位 用 索引 加 1 项 为 值。
完。

bst_设右 接受 树、索引、值：
    池 为 树 的 第 1 项。
    池 的 第 做 bst_节点基位 用 索引 加 2 项 为 值。
完。

bst_插入 接受 树、值：
    新索引 为 做 bst_新节点 用 树、值。
    若 树 的 第 0 项 小于 0：
        树 的 第 0 项 为 新索引。
        返还 空。
    完。
    可变 当前 为 树 的 第 0 项。
    只要 真：
        当前值 为 做 bst_值 用 树、当前。
        若 值 小于 当前值：
            若 做 bst_左 用 树、当前 小于 0：
                做 bst_设左 用 树、当前、新索引。
                返还 空。
            完。
            当前 为 做 bst_左 用 树、当前。
        否则：
            若 做 bst_右 用 树、当前 小于 0：
                做 bst_设右 用 树、当前、新索引。
                返还 空。
            完。
            当前 为 做 bst_右 用 树、当前。
        完。
    完。
完。

bst_查找 接受 树、值：
    可变 当前 为 树 的 第 0 项。
    只要 当前 大于或等于 0：
        当前值 为 做 bst_值 用 树、当前。
        若 值 等于 当前值：
            返还 真。
        完。
        若 值 小于 当前值：
            当前 为 做 bst_左 用 树、当前。
        否则：
            当前 为 做 bst_右 用 树、当前。
        完。
    完。
    返还 假。
完。

bst_最小 接受 树：
    若 树 的 第 0 项 小于 0：
        返还 空。
    完。
    可变 当前 为 树 的 第 0 项。
    只要 做 bst_左 用 树、当前 大于或等于 0：
        当前 为 做 bst_左 用 树、当前。
    完。
    返还 做 bst_值 用 树、当前。
完。

bst_最大 接受 树：
    若 树 的 第 0 项 小于 0：
        返还 空。
    完。
    可变 当前 为 树 的 第 0 项。
    只要 做 bst_右 用 树、当前 大于或等于 0：
        当前 为 做 bst_右 用 树、当前。
    完。
    返还 做 bst_值 用 树、当前。
完。

bst_中序 接受 树：
    可变 结果 为 一组：（空）。
    做 bst_中序递归 用 树、树 的 第 0 项、结果。
    返还 结果。
完。

bst_中序递归 接受 树、节点、结果：
    若 节点 小于 0：
        返还 空。
    完。
    做 bst_中序递归 用 树、做 bst_左 用 树、节点、结果。
    做 追加 用 结果、做 bst_值 用 树、节点。
    做 bst_中序递归 用 树、做 bst_右 用 树、节点、结果。
完。

bst_前序 接受 树：
    可变 结果 为 一组：（空）。
    做 bst_前序递归 用 树、树 的 第 0 项、结果。
    返还 结果。
完。

bst_前序递归 接受 树、节点、结果：
    若 节点 小于 0：
        返还 空。
    完。
    做 追加 用 结果、做 bst_值 用 树、节点。
    做 bst_前序递归 用 树、做 bst_左 用 树、节点、结果。
    做 bst_前序递归 用 树、做 bst_右 用 树、节点、结果。
完。

bst_后序 接受 树：
    可变 结果 为 一组：（空）。
    做 bst_后序递归 用 树、树 的 第 0 项、结果。
    返还 结果。
完。

bst_后序递归 接受 树、节点、结果：
    若 节点 小于 0：
        返还 空。
    完。
    做 bst_后序递归 用 树、做 bst_左 用 树、节点、结果。
    做 bst_后序递归 用 树、做 bst_右 用 树、节点、结果。
    做 追加 用 结果、做 bst_值 用 树、节点。
完。

bst_高度 接受 树：
    返还 做 bst_高度递归 用 树、树 的 第 0 项。
完。

bst_高度递归 接受 树、节点：
    若 节点 小于 0：
        返还 0。
    完。
    左高 为 做 bst_高度递归 用 树、做 bst_左 用 树、节点。
    右高 为 做 bst_高度递归 用 树、做 bst_右 用 树、节点。
    若 左高 大于 右高：
        返还 左高 加 1。
    完。
    返还 右高 加 1。
完。

bst_大小 接受 树：
    返还 做 bst_大小递归 用 树、树 的 第 0 项。
完。

bst_大小递归 接受 树、节点：
    若 节点 小于 0：
        返还 0。
    完。
    左数 为 做 bst_大小递归 用 树、做 bst_左 用 树、节点。
    右数 为 做 bst_大小递归 用 树、做 bst_右 用 树、节点。
    返还 1 加 左数 加 右数。
完。

bst_空 接受 树：
    返还 树 的 第 0 项 小于 0。
完。

bst_从排序 接受 排序列表：
    可变 树 为 做 bst_创建。
    做 bst_平衡构建 用 树、排序列表、0、做 计数 用 排序列表 减 1。
    返还 树。
完。

bst_平衡构建 接受 树、列表、左、右：
    若 左 大于 右：
        返还 空。
    完。
    中 为 (左 加 右) 除以 2。
    做 bst_插入 用 树、列表 的 第 中 项。
    做 bst_平衡构建 用 树、列表、左、中 减 1。
    做 bst_平衡构建 用 树、列表、中 加 1、右。
完。

bst_层序 接受 树：
    若 树 的 第 0 项 小于 0：
        返还 一组：（空）。
    完。
    可变 队列 为 一组：树 的 第 0 项。
    可变 结果 为 一组：（空）。
    可变 前 为 0。
    只要 前 小于 做 计数 用 队列：
        当前 为 队列 的 第 前 项。
        前 为 前 加 1。
        做 追加 用 结果、做 bst_值 用 树、当前。
        左子 为 做 bst_左 用 树、当前。
        若 左子 大于或等于 0：
            做 追加 用 队列、左子。
        完。
        右子 为 做 bst_右 用 树、当前。
        若 右子 大于或等于 0：
            做 追加 用 队列、右子。
        完。
    完。
    返还 结果。
完。

bst_范围 接受 树、最小值、最大值：
    可变 结果 为 一组：（空）。
    做 bst_范围递归 用 树、树 的 第 0 项、最小值、最大值、结果。
    返还 结果。
完。

bst_范围递归 接受 树、节点、最小值、最大值、结果：
    若 节点 小于 0：
        返还 空。
    完。
    当前值 为 做 bst_值 用 树、节点。
    若 当前值 大于 最小值：
        做 bst_范围递归 用 树、做 bst_左 用 树、节点、最小值、最大值、结果。
    完。
    若 当前值 大于或等于 最小值：
        若 当前值 小于或等于 最大值：
            做 追加 用 结果、当前值。
        完。
    完。
    若 当前值 小于 最大值：
        做 bst_范围递归 用 树、做 bst_右 用 树、节点、最小值、最大值、结果。
    完。
完。

树_新节点 接受 键、值：
    返还 一组：键、值、空、空。
完。

树_查找 接受 根、键：
    若 根 等于 空：
        返还 空。
    完。
    若 键 小于 根 的 第 节点_键 项：
        返还 做 树_查找 用 根 的 第 节点_左 项、键。
    完。
    若 键 大于 根 的 第 节点_键 项：
        返还 做 树_查找 用 根 的 第 节点_右 项、键。
    完。
    返还 根 的 第 节点_值 项。
完。

树_插入 接受 根、键、值：
    若 根 等于 空：
        返还 做 树_新节点 用 键、值。
    完。
    若 键 小于 根 的 第 节点_键 项：
        根 的 第 节点_左 项 为 做 树_插入 用 根 的 第 节点_左 项、键、值。
    否则：
        若 键 大于 根 的 第 节点_键 项：
            根 的 第 节点_右 项 为 做 树_插入 用 根 的 第 节点_右 项、键、值。
        否则：
            根 的 第 节点_值 项 为 值。
        完。
    完。
    返还 根。
完。

树_最小 接受 根：
    若 根 等于 空：
        返还 空。
    完。
    若 根 的 第 节点_左 项 等于 空：
        返还 根。
    完。
    返还 做 树_最小 用 根 的 第 节点_左 项。
完。

树_最大 接受 根：
    若 根 等于 空：
        返还 空。
    完。
    若 根 的 第 节点_右 项 等于 空：
        返还 根。
    完。
    返还 做 树_最大 用 根 的 第 节点_右 项。
完。

树_大小 接受 根：
    若 根 等于 空：
        返还 0。
    完。
    左数 为 做 树_大小 用 根 的 第 节点_左 项。
    右数 为 做 树_大小 用 根 的 第 节点_右 项。
    返还 1 加 左数 加 右数。
完。

树_中序 接受 根、结果：
    若 根 等于 空：
        返还 空。
    完。
    做 树_中序 用 根 的 第 节点_左 项、结果。
    做 追加 用 结果、一组：根 的 第 节点_键 项、根 的 第 节点_值 项。
    做 树_中序 用 根 的 第 节点_右 项、结果。
完。

树_所有键值 接受 根：
    可变 结果 为 一组：（空）。
    做 树_中序 用 根、结果。
    返还 结果。
完。

树_高度 接受 根：
    若 根 等于 空：
        返还 0。
    完。
    左高 为 做 树_高度 用 根 的 第 节点_左 项。
    右高 为 做 树_高度 用 根 的 第 节点_右 项。
    若 左高 大于 右高：
        返还 左高 加 1。
    完。
    返还 右高 加 1。
完。

树_平衡因子 接受 根：
    若 根 等于 空：
        返还 0。
    完。
    返还 做 树_高度 用 根 的 第 节点_左 项 减 做 树_高度 用 根 的 第 节点_右 项。
完。

树_左旋 接受 根：
    新根 为 根 的 第 节点_右 项。
    根 的 第 节点_右 项 为 新根 的 第 节点_左 项。
    新根 的 第 节点_左 项 为 根。
    返还 新根。
完。

树_右旋 接受 根：
    新根 为 根 的 第 节点_左 项。
    根 的 第 节点_左 项 为 新根 的 第 节点_右 项。
    新根 的 第 节点_右 项 为 根。
    返还 新根。
完。

树_AVL平衡 接受 根：
    因子 为 做 树_平衡因子 用 根。
    若 因子 大于 1：
        若 做 树_平衡因子 用 根 的 第 节点_左 项 小于 0：
            根 的 第 节点_左 项 为 做 树_左旋 用 根 的 第 节点_左 项。
        完。
        返还 做 树_右旋 用 根。
    完。
    若 因子 小于 负 1：
        若 做 树_平衡因子 用 根 的 第 节点_右 项 大于 0：
            根 的 第 节点_右 项 为 做 树_右旋 用 根 的 第 节点_右 项。
        完。
        返还 做 树_左旋 用 根。
    完。
    返还 根。
完。

树_最小节点 接受 根：
    若 根 等于 空：
        返还 空。
    完。
    可变 当前 为 根。
    只要 当前 的 第 节点_左 项 不等于 空：
        当前 为 当前 的 第 节点_左 项。
    完。
    返还 当前。
完。

树_最大节点 接受 根：
    若 根 等于 空：
        返还 空。
    完。
    可变 当前 为 根。
    只要 当前 的 第 节点_右 项 不等于 空：
        当前 为 当前 的 第 节点_右 项。
    完。
    返还 当前。
完。

树_删除 接受 根、键：
    若 根 等于 空：
        返还 空。
    完。
    若 键 小于 根 的 第 节点_键 项：
        根 的 第 节点_左 项 为 做 树_删除 用 根 的 第 节点_左 项、键。
    否则若 键 大于 根 的 第 节点_键 项：
        根 的 第 节点_右 项 为 做 树_删除 用 根 的 第 节点_右 项、键。
    否则：
        若 根 的 第 节点_左 项 等于 空：
            返还 根 的 第 节点_右 项。
        完。
        若 根 的 第 节点_右 项 等于 空：
            返还 根 的 第 节点_左 项。
        完。
        后继 为 做 树_最小节点 用 根 的 第 节点_右 项。
        根 的 第 节点_键 项 为 后继 的 第 节点_键 项。
        根 的 第 节点_值 项 为 后继 的 第 节点_值 项。
        根 的 第 节点_右 项 为 做 树_删除 用 根 的 第 节点_右 项、后继 的 第 节点_键 项。
    完。
    返还 做 树_AVL平衡 用 根。
完。

树_前序 接受 根、结果：
    若 根 等于 空：
        返还。
    完。
    做 追加 用 结果、一组：根 的 第 节点_键 项、根 的 第 节点_值 项。
    做 树_前序 用 根 的 第 节点_左 项、结果。
    做 树_前序 用 根 的 第 节点_右 项、结果。
完。

树_后序 接受 根、结果：
    若 根 等于 空：
        返还。
    完。
    做 树_后序 用 根 的 第 节点_左 项、结果。
    做 树_后序 用 根 的 第 节点_右 项、结果。
    做 追加 用 结果、一组：根 的 第 节点_键 项、根 的 第 节点_值 项。
完。

树_范围查询 接受 根、最小键、最大键、结果：
    若 根 等于 空：
        返还。
    完。
    若 根 的 第 节点_键 项 大于 最小键：
        做 树_范围查询 用 根 的 第 节点_左 项、最小键、最大键、结果。
    完。
    若 根 的 第 节点_键 项 大于或等于 最小键 并且 根 的 第 节点_键 项 小于或等于 最大键：
        做 追加 用 结果、一组：根 的 第 节点_键 项、根 的 第 节点_值 项。
    完。
    若 根 的 第 节点_键 项 小于 最大键：
        做 树_范围查询 用 根 的 第 节点_右 项、最小键、最大键、结果。
    完。
完。

树_第K小 接受 根、k：
    可变 结果 为 一组：（空）。
    做 树_中序 用 根、结果。
    若 k 大于 0 并且 k 小于或等于 做 计数 用 结果：
        返还 结果 的 第 k 减 1 项。
    完。
    返还 空。
完。

树_是否平衡 接受 根：
    若 根 等于 空：
        返还 真。
    完。
    因子 为 做 树_平衡因子 用 根。
    若 因子 大于 1 或者 因子 小于 负 1：
        返还 假。
    完。
    返还 做 树_是否平衡 用 根 的 第 节点_左 项 并且 做 树_是否平衡 用 根 的 第 节点_右 项。
完。

树_是否有效BST 接受 根、最小、最大：
    若 根 等于 空：
        返还 真。
    完。
    若 根 的 第 节点_键 项 小于或等于 最小 或者 根 的 第 节点_键 项 大于或等于 最大：
        返还 假。
    完。
    返还 做 树_是否有效BST 用 根 的 第 节点_左 项、最小、根 的 第 节点_键 项 并且 做 树_是否有效BST 用 根 的 第 节点_右 项、根 的 第 节点_键 项、最大。
完。

本章 收束。
