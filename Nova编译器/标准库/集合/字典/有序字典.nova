《有序字典》

引用《..·..·基础》。

可变 字典_全局统计 为 一组：0、0、0。

字典_哈希 接受 键：
    若 键 等于 0 或者 键 等于 空：
        返还 0。
    完。
    文本 为 做 转字符串 用 键。
    可变 h 为 2166136261。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        h 为 h 位异或 做 编码 用 做 子串 用 文本、i、1。
        h 为 h 乘 16777619。
        若 h 小于 0：
            h 为 0 减 h。
        完。
        i 为 i 加 1。
    完。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    返还 h。
完。

字典_哈希2 接受 键：
    文本 为 做 转字符串 用 键。
    可变 h 为 5381。
    可变 i 为 0。
    只要 i 小于 做 计数 用 文本：
        h 为 h 乘 33 加 做 编码 用 做 子串 用 文本、i、1。
        若 h 小于 0：
            h 为 0 减 h。
        完。
        i 为 i 加 1。
    完。
    若 h 小于 0：
        h 为 0 减 h。
    完。
    返还 h。
完。

字典_初始桶数 为 16。

有序字典_创建 接受 零参：
    字典_全局统计 的 第 0 项 为 字典_全局统计 的 第 0 项 加 1。
    可变 桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 字典_初始桶数：
        做 追加 用 桶、0 减 1。
        i 为 i 加 1。
    完。
    返还 一本字典：「桶」 对应 桶、「节点表」 对应 一组：（空）、「头」 对应 0 减 1、「尾」 对应 0 减 1、「计数」 对应 0、「桶数」 对应 字典_初始桶数、「查询数」 对应 0、「命中数」 对应 0。
完。

有序字典_创建_容量 接受 容量：
    字典_全局统计 的 第 0 项 为 字典_全局统计 的 第 0 项 加 1。
    可变 桶数 为 字典_初始桶数。
    只要 桶数 小于 容量 乘 2：
        桶数 为 桶数 乘 2。
    完。
    可变 桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 桶数：
        做 追加 用 桶、0 减 1。
        i 为 i 加 1。
    完。
    返还 一本字典：「桶」 对应 桶、「节点表」 对应 一组：（空）、「头」 对应 0 减 1、「尾」 对应 0 减 1、「计数」 对应 0、「桶数」 对应 桶数、「查询数」 对应 0、「命中数」 对应 0。
完。

有序字典_设 接受 字典、键、值：
    字典_全局统计 的 第 2 项 为 字典_全局统计 的 第 2 项 加 1。
    桶数 为 字典 的 第 「桶数」 项。
    h 为 做 字典_哈希 用 键 取余于 桶数。
    节点表 为 字典 的 第 「节点表」 项。
    可变 i 为 字典 的 第 「桶」 项 的 第 h 项。
    只要 i 大于或等于 0：
        若 节点表 的 第 i 项 的 第 「键」 项 等于 键：
            节点表 的 第 i 项 的 第 「值」 项 为 值。
            返还 假。
        完。
        i 为 节点表 的 第 i 项 的 第 「桶链」 项。
    完。
    索引 为 做 计数 用 节点表。
    节点 为 一本字典：「键」 对应 键、「值」 对应 值、「前驱」 对应 字典 的 第 「尾」 项、「后继」 对应 0 减 1、「桶链」 对应 字典 的 第 「桶」 项 的 第 h 项。
    做 追加 用 节点表、节点。
    字典 的 第 「桶」 项 的 第 h 项 为 索引。
    若 字典 的 第 「尾」 项 大于或等于 0：
        节点表 的 第 字典 的 第 「尾」 项 项 的 第 「后继」 项 为 索引。
    完。
    字典 的 第 「尾」 项 为 索引。
    若 字典 的 第 「头」 项 小于 0：
        字典 的 第 「头」 项 为 索引。
    完。
    字典 的 第 「计数」 项 为 字典 的 第 「计数」 项 加 1。
    若 字典 的 第 「计数」 项 大于 桶数 乘 3 除以 4：
        做 有序字典_扩容 用 字典。
    完。
    返还 真。
完。

有序字典_取 接受 字典、键：
    字典_全局统计 的 第 1 项 为 字典_全局统计 的 第 1 项 加 1。
    字典 的 第 「查询数」 项 为 字典 的 第 「查询数」 项 加 1。
    节点表 为 字典 的 第 「节点表」 项。
    h 为 做 字典_哈希 用 键 取余于 字典 的 第 「桶数」 项。
    可变 索引 为 字典 的 第 「桶」 项 的 第 h 项。
    只要 索引 大于或等于 0：
        若 节点表 的 第 索引 项 的 第 「键」 项 等于 键：
            字典 的 第 「命中数」 项 为 字典 的 第 「命中数」 项 加 1。
            返还 节点表 的 第 索引 项 的 第 「值」 项。
        完。
        索引 为 节点表 的 第 索引 项 的 第 「桶链」 项。
    完。
    返还 空。
完。

有序字典_含 接受 字典、键：
    节点表 为 字典 的 第 「节点表」 项。
    h 为 做 字典_哈希 用 键 取余于 字典 的 第 「桶数」 项。
    可变 索引 为 字典 的 第 「桶」 项 的 第 h 项。
    只要 索引 大于或等于 0：
        若 节点表 的 第 索引 项 的 第 「键」 项 等于 键：
            返还 真。
        完。
        索引 为 节点表 的 第 索引 项 的 第 「桶链」 项。
    完。
    返还 假。
完。

有序字典_键列表 接受 字典：
    可变 结果 为 一组：（空）。
    可变 当前 为 字典 的 第 「头」 项。
    节点表 为 字典 的 第 「节点表」 项。
    只要 当前 大于或等于 0：
        做 追加 用 结果、节点表 的 第 当前 项 的 第 「键」 项。
        当前 为 节点表 的 第 当前 项 的 第 「后继」 项。
    完。
    返还 结果。
完。

有序字典_值列表 接受 字典：
    可变 结果 为 一组：（空）。
    可变 当前 为 字典 的 第 「头」 项。
    节点表 为 字典 的 第 「节点表」 项。
    只要 当前 大于或等于 0：
        做 追加 用 结果、节点表 的 第 当前 项 的 第 「值」 项。
        当前 为 节点表 的 第 当前 项 的 第 「后继」 项。
    完。
    返还 结果。
完。

有序字典_对列表 接受 字典：
    可变 结果 为 一组：（空）。
    可变 当前 为 字典 的 第 「头」 项。
    节点表 为 字典 的 第 「节点表」 项。
    只要 当前 大于或等于 0：
        做 追加 用 结果、一组：节点表 的 第 当前 项 的 第 「键」 项、节点表 的 第 当前 项 的 第 「值」 项。
        当前 为 节点表 的 第 当前 项 的 第 「后继」 项。
    完。
    返还 结果。
完。

有序字典_大小 接受 字典：
    返还 字典 的 第 「计数」 项。
完。

有序字典_空 接受 字典：
    返还 字典 的 第 「计数」 项 等于 0。
完。

有序字典_第一 接受 字典：
    若 字典 的 第 「头」 项 小于 0：
        返还 空。
    完。
    返还 字典 的 第 「节点表」 项 的 第 字典 的 第 「头」 项 项 的 第 「键」 项。
完。

有序字典_最后 接受 字典：
    若 字典 的 第 「尾」 项 小于 0：
        返还 空。
    完。
    返还 字典 的 第 「节点表」 项 的 第 字典 的 第 「尾」 项 项 的 第 「键」 项。
完。

有序字典_取默认 接受 字典、键、默认值：
    结果 为 做 有序字典_取 用 字典、键。
    若 结果 等于 空：
        返还 默认值。
    完。
    返还 结果。
完。

有序字典_批量设 接受 字典、键列表、值列表：
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列表 并且 i 小于 做 计数 用 值列表：
        做 有序字典_设 用 字典、键列表 的 第 i 项、值列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

有序字典_合并 接受 字典1、字典2：
    对 为 做 有序字典_对列表 用 字典2。
    可变 i 为 0。
    只要 i 小于 做 计数 用 对：
        做 有序字典_设 用 字典1、对 的 第 i 项 的 第 0 项、对 的 第 i 项 的 第 1 项。
        i 为 i 加 1。
    完。
完。

有序字典_扩容 接受 字典：
    新桶数 为 字典 的 第 「桶数」 项 乘 2。
    可变 新桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 新桶数：
        做 追加 用 新桶、0 减 1。
        i 为 i 加 1。
    完。
    节点表 为 字典 的 第 「节点表」 项。
    i 为 0。
    只要 i 小于 做 计数 用 节点表：
        h 为 做 字典_哈希 用 节点表 的 第 i 项 的 第 「键」 项 取余于 新桶数。
        节点表 的 第 i 项 的 第 「桶链」 项 为 新桶 的 第 h 项。
        新桶 的 第 h 项 为 i。
        i 为 i 加 1。
    完。
    字典 的 第 「桶」 项 为 新桶。
    字典 的 第 「桶数」 项 为 新桶数。
完。

有序字典_清空 接受 字典：
    可变 i 为 0。
    只要 i 小于 字典 的 第 「桶数」 项：
        字典 的 第 「桶」 项 的 第 i 项 为 0 减 1。
        i 为 i 加 1。
    完。
    字典 的 第 「节点表」 项 为 一组：（空）。
    字典 的 第 「头」 项 为 0 减 1。
    字典 的 第 「尾」 项 为 0 减 1。
    字典 的 第 「计数」 项 为 0。
完。

有序字典_删首 接受 字典：
    若 字典 的 第 「头」 项 小于 0：
        返还 假。
    完。
    节点表 为 字典 的 第 「节点表」 项。
    头idx 为 字典 的 第 「头」 项。
    键 为 节点表 的 第 头idx 项 的 第 「键」 项。
    h 为 做 字典_哈希 用 键 取余于 字典 的 第 「桶数」 项。
    若 字典 的 第 「桶」 项 的 第 h 项 等于 头idx：
        字典 的 第 「桶」 项 的 第 h 项 为 节点表 的 第 头idx 项 的 第 「桶链」 项。
    否则：
        可变 前值 为 字典 的 第 「桶」 项 的 第 h 项。
        只要 前值 大于或等于 0 并且 节点表 的 第 前值 项 的 第 「桶链」 项 不等于 头idx：
            前值 为 节点表 的 第 前值 项 的 第 「桶链」 项。
        完。
        若 前值 大于或等于 0：
            节点表 的 第 前值 项 的 第 「桶链」 项 为 节点表 的 第 头idx 项 的 第 「桶链」 项。
        完。
    完。
    后继 为 节点表 的 第 头idx 项 的 第 「后继」 项。
    字典 的 第 「头」 项 为 后继。
    若 后继 大于或等于 0：
        节点表 的 第 后继 项 的 第 「前驱」 项 为 0 减 1。
    否则：
        字典 的 第 「尾」 项 为 0 减 1。
    完。
    节点表 的 第 头idx 项 的 第 「键」 项 为 空。
    节点表 的 第 头idx 项 的 第 「值」 项 为 空。
    节点表 的 第 头idx 项 的 第 「桶链」 项 为 0 减 1。
    字典 的 第 「计数」 项 为 字典 的 第 「计数」 项 减 1。
    返还 真。
完。

有序_LRU_创建 接受 容量：
    返还 一本字典：「字典」 对应 做 有序字典_创建_容量 用 容量、「容量」 对应 容量、「命中」 对应 0、「未中」 对应 0。
完。

有序_LRU_获取 接受 缓存、键：
    值 为 做 有序字典_取 用 缓存 的 第 「字典」 项、键。
    若 值 等于 空：
        缓存 的 第 「未中」 项 为 缓存 的 第 「未中」 项 加 1。
        返还 空。
    完。
    缓存 的 第 「命中」 项 为 缓存 的 第 「命中」 项 加 1。
    返还 值。
完。

有序_LRU_设置 接受 缓存、键、值：
    做 有序字典_设 用 缓存 的 第 「字典」 项、键、值。
    若 做 有序字典_大小 用 缓存 的 第 「字典」 项 大于 缓存 的 第 「容量」 项：
        做 有序字典_删首 用 缓存 的 第 「字典」 项。
    完。
完。

有序_LRU_命中率 接受 缓存：
    总 为 缓存 的 第 「命中」 项 加 缓存 的 第 「未中」 项。
    若 总 等于 0：
        返还 0。
    完。
    返还 缓存 的 第 「命中」 项 乘 100 除以 总。
完。

有序_LRU_大小 接受 缓存：
    返还 做 有序字典_大小 用 缓存 的 第 「字典」 项。
完。

有序_LRU_容量 接受 缓存：
    返还 缓存 的 第 「容量」 项。
完。

有序_哈希表_创建 接受 零参：
    可变 桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 64：
        做 追加 用 桶、一组：（空）。
        i 为 i 加 1。
    完。
    返还 一组：桶、64、0。
完。

哈希表_设 接受 表、键、值：
    h 为 做 字典_哈希 用 键 取余于 表 的 第 1 项。
    桶 为 表 的 第 0 项 的 第 h 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 桶：
        若 桶 的 第 i 项 的 第 0 项 等于 键：
            桶 的 第 i 项 的 第 1 项 为 值。
            返还 假。
        完。
        i 为 i 加 1。
    完。
    做 追加 用 桶、一组：键、值。
    表 的 第 2 项 为 表 的 第 2 项 加 1。
    返还 真。
完。

哈希表_取 接受 表、键：
    h 为 做 字典_哈希 用 键 取余于 表 的 第 1 项。
    桶 为 表 的 第 0 项 的 第 h 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 桶：
        若 桶 的 第 i 项 的 第 0 项 等于 键：
            返还 桶 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 空。
完。

哈希表_含 接受 表、键：
    返还 做 哈希表_取 用 表、键 不等于 空。
完。

哈希表_大小 接受 表：
    返还 表 的 第 2 项。
完。

有序_哈希表_键列表 接受 表：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 表 的 第 1 项：
        桶 为 表 的 第 0 项 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 桶：
            做 追加 用 结果、桶 的 第 j 项 的 第 0 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

有序字典_访问模式 接受 字典：
    查询 为 字典 的 第 「查询数」 项。
    命中 为 字典 的 第 「命中数」 项。
    若 查询 等于 0：
        返还 「未查询」。
    完。
    命中率 为 命中 乘 100 除以 查询。
    若 命中率 大于 90：
        返还 「高命中(热数据集中)」。
    完。
    若 命中率 大于 60：
        返还 「中命中(正常分布)」。
    完。
    若 命中率 大于 30：
        返还 「低命中(数据分散)」。
    完。
    返还 「极低命中(考虑预加载)」。
完。

有序字典_负载因子 接受 字典：
    若 字典 的 第 「桶数」 项 等于 0：
        返还 0。
    完。
    返还 字典 的 第 「计数」 项 乘 100 除以 字典 的 第 「桶数」 项。
完。

有序字典_冲突分析 接受 字典：
    可变 空桶 为 0。
    可变 单桶 为 0。
    可变 多桶 为 0。
    可变 最长链 为 0。
    可变 i 为 0。
    只要 i 小于 字典 的 第 「桶数」 项：
        可变 链长 为 0。
        可变 索引 为 字典 的 第 「桶」 项 的 第 i 项。
        只要 索引 大于或等于 0：
            链长 为 链长 加 1。
            索引 为 字典 的 第 「节点表」 项 的 第 索引 项 的 第 「桶链」 项。
        完。
        若 链长 等于 0：
            空桶 为 空桶 加 1。
        完。
        若 链长 等于 1：
            单桶 为 单桶 加 1。
        完。
        若 链长 大于 1：
            多桶 为 多桶 加 1。
        完。
        若 链长 大于 最长链：
            最长链 为 链长。
        完。
        i 为 i 加 1。
    完。
    返还 一组：空桶、单桶、多桶、最长链。
完。

有序字典_性能报告 接受 字典：
    负载 为 做 有序字典_负载因子 用 字典。
    冲突 为 做 有序字典_冲突分析 用 字典。
    模式 为 做 有序字典_访问模式 用 字典。
    返还 一组：负载、冲突 的 第 3 项、模式、字典 的 第 「计数」 项、字典 的 第 「桶数」 项。
完。

字典_全局报告 接受 零参：
    返还 一组：字典_全局统计 的 第 0 项、字典_全局统计 的 第 1 项、字典_全局统计 的 第 2 项。
完。

多值字典_创建 接受 零参：
    返还 做 有序字典_创建。
完。

多值字典_追加 接受 字典、键、值：
    现有 为 做 有序字典_取 用 字典、键。
    若 现有 等于 空：
        做 有序字典_设 用 字典、键、一组：值。
    否则：
        做 追加 用 现有、值。
    完。
完。

多值字典_取全部 接受 字典、键：
    结果 为 做 有序字典_取 用 字典、键。
    若 结果 等于 空：
        返还 一组：（空）。
    完。
    返还 结果。
完。

多值字典_计数 接受 字典、键：
    值 为 做 有序字典_取 用 字典、键。
    若 值 等于 空：
        返还 0。
    完。
    返还 做 计数 用 值。
完。

有序字典_计数器_创建 接受 零参：
    返还 做 有序字典_创建。
完。

计数器_增加 接受 计数器、键：
    当前 为 做 有序字典_取 用 计数器、键。
    若 当前 等于 空：
        做 有序字典_设 用 计数器、键、1。
    否则：
        做 有序字典_设 用 计数器、键、当前 加 1。
    完。
完。

计数器_增加N 接受 计数器、键、n：
    当前 为 做 有序字典_取 用 计数器、键。
    若 当前 等于 空：
        做 有序字典_设 用 计数器、键、n。
    否则：
        做 有序字典_设 用 计数器、键、当前 加 n。
    完。
完。

计数器_获取 接受 计数器、键：
    v 为 做 有序字典_取 用 计数器、键。
    若 v 等于 空：
        返还 0。
    完。
    返还 v。
完。

计数器_最大 接受 计数器：
    对 为 做 有序字典_对列表 用 计数器。
    若 做 计数 用 对 等于 0：
        返还 空。
    完。
    可变 最大键 为 对 的 第 0 项 的 第 0 项。
    可变 最大值 为 对 的 第 0 项 的 第 1 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 对：
        若 对 的 第 i 项 的 第 1 项 大于 最大值：
            最大键 为 对 的 第 i 项 的 第 0 项。
            最大值 为 对 的 第 i 项 的 第 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：最大键、最大值。
完。

计数器_前N 接受 计数器、n：
    对 为 做 有序字典_对列表 用 计数器。
    可变 i 为 0。
    只要 i 小于 做 计数 用 对 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 做 计数 用 对：
            若 对 的 第 j 项 的 第 1 项 大于 对 的 第 i 项 的 第 1 项：
                临 为 对 的 第 i 项。
                对 的 第 i 项 为 对 的 第 j 项。
                对 的 第 j 项 为 临。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n 并且 i 小于 做 计数 用 对：
        做 追加 用 结果、对 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

有序字典_健康诊断 接受 字典：
    负载 为 做 有序字典_负载因子 用 字典。
    冲突 为 做 有序字典_冲突分析 用 字典。
    可变 诊断 为 一组：（空）。
    若 负载 大于 75：
        做 追加 用 诊断、「负载过高,建议扩容」。
    完。
    若 冲突 的 第 3 项 大于 5：
        做 追加 用 诊断、「最长冲突链过长,哈希分布不均」。
    完。
    若 冲突 的 第 0 项 乘 100 除以 字典 的 第 「桶数」 项 大于 60：
        做 追加 用 诊断、「空桶过多,桶数可减少」。
    完。
    若 做 计数 用 诊断 等于 0：
        做 追加 用 诊断、「状态良好」。
    完。
    返还 诊断。
完。

有序字典_容量建议 接受 当前大小、增长速率：
    若 增长速率 小于或等于 0：
        返还 当前大小 乘 2。
    完。
    若 增长速率 小于 10：
        返还 当前大小 加 增长速率 乘 10。
    完。
    若 增长速率 小于 100：
        返还 当前大小 加 增长速率 乘 5。
    完。
    返还 当前大小 乘 3。
完。

本章 收束。
