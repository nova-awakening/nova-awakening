《哈希集合》

引用《..·..·基础》。

引用《..·..·集合运算·集合运算》。

哈希_集合_创建 接受 零参：
    返还 一组：做 字典、0。
完。

哈希_集合_大小 接受 s：
    返还 s 的 第 1 项。
完。

集合_空 接受 s：
    返还 s 的 第 1 项 等于 0。
完。

哈希_集合_追加 接受 s、值：
    键 为 做 转字符串 用 值。
    若 做 字典取 用 s 的 第 0 项、键 等于 空：
        做 字典设 用 s 的 第 0 项、键、值。
        s 的 第 1 项 为 s 的 第 1 项 加 1。
    完。
完。

哈希_集合_删除 接受 s、值：
    键 为 做 转字符串 用 值。
    若 做 字典取 用 s 的 第 0 项、键 不等于 空：
        做 字典设 用 s 的 第 0 项、键、空。
        s 的 第 1 项 为 s 的 第 1 项 减 1。
    完。
完。

哈希_集合_包含 接受 s、值：
    返还 做 字典取 用 s 的 第 0 项、做 转字符串 用 值 不等于 空。
完。

集合_并 接受 a、b：
    可变 结果 为 做 集合_创建 用 0。
    列表a 为 做 集合_转列表 用 a。
    列表b 为 做 集合_转列表 用 b。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表a：
        做 集合_追加 用 结果、列表a 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 列表b：
        做 集合_追加 用 结果、列表b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_交 接受 a、b：
    可变 结果 为 做 集合_创建 用 0。
    列表a 为 做 集合_转列表 用 a。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表a：
        若 做 集合_包含 用 b、列表a 的 第 i 项：
            做 集合_追加 用 结果、列表a 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_差 接受 a、b：
    可变 结果 为 做 集合_创建 用 0。
    列表a 为 做 集合_转列表 用 a。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表a：
        若 做 集合_包含 用 b、列表a 的 第 i 项 等于 假：
            做 集合_追加 用 结果、列表a 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

哈希_集合_对称差 接受 a、b：
    并 为 做 集合_并 用 a、b。
    交 为 做 集合_交 用 a、b。
    返还 做 集合_差 用 并、交。
完。

哈希_集合_是子集 接受 a、b：
    列表a 为 做 集合_转列表 用 a。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表a：
        若 做 集合_包含 用 b、列表a 的 第 i 项 等于 假：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

集合_从列表 接受 列表：
    可变 s 为 做 集合_创建 用 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 集合_追加 用 s、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 s。
完。

集合_转列表 接受 s：
    可变 结果 为 一组：（空）。
    键列 为 做 字典键列表 用 s 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 键列：
        值 为 做 字典取 用 s 的 第 0 项、键列 的 第 i 项。
        若 值 不等于 空：
            做 追加 用 结果、值。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合_遍历 接受 s、操作：
    列表 为 做 集合_转列表 用 s。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 操作 用 列表 的 第 i 项。
        i 为 i 加 1。
    完。
完。

集合v2_创建 接受 零参：
    返还 一组：做 字典、一组：（空）、0。
完。

集合v2_追加 接受 s、值：
    键 为 做 转字符串 用 值。
    若 做 字典取 用 s 的 第 0 项、键 等于 空：
        做 字典设 用 s 的 第 0 项、键、做 计数 用 s 的 第 1 项。
        做 追加 用 s 的 第 1 项、值。
        s 的 第 2 项 为 s 的 第 2 项 加 1。
    完。
完。

集合v2_包含 接受 s、值：
    返还 做 字典取 用 s 的 第 0 项、做 转字符串 用 值 不等于 空。
完。

集合v2_大小 接受 s：
    返还 s 的 第 2 项。
完。

集合v2_转列表 接受 s：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 s 的 第 1 项：
        做 追加 用 结果、s 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合v2_并 接受 a、b：
    可变 结果 为 做 集合v2_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 1 项：
        做 集合v2_追加 用 结果、a 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 b 的 第 1 项：
        做 集合v2_追加 用 结果、b 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合v2_交 接受 a、b：
    可变 结果 为 做 集合v2_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 1 项：
        若 做 集合v2_包含 用 b、a 的 第 1 项 的 第 i 项：
            做 集合v2_追加 用 结果、a 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合v2_差 接受 a、b：
    可变 结果 为 做 集合v2_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 1 项：
        若 做 集合v2_包含 用 b、a 的 第 1 项 的 第 i 项 等于 假：
            做 集合v2_追加 用 结果、a 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

列表去重 接受 列表：
    可变 s 为 做 集合v2_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        做 集合v2_追加 用 s、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 集合v2_转列表 用 s。
完。

集合v2_对称差 接受 a、b：
    可变 结果 为 做 集合v2_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 1 项：
        若 做 集合v2_包含 用 b、a 的 第 1 项 的 第 i 项 等于 假：
            做 集合v2_追加 用 结果、a 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 b 的 第 1 项：
        若 做 集合v2_包含 用 a、b 的 第 1 项 的 第 i 项 等于 假：
            做 集合v2_追加 用 结果、b 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合v2_是子集 接受 a、b：
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 1 项：
        若 做 集合v2_包含 用 b、a 的 第 1 项 的 第 i 项 等于 假：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

集合v2_是超集 接受 a、b：
    返还 做 集合v2_是子集 用 b、a。
完。

集合v2_相等 接受 a、b：
    若 做 计数 用 a 的 第 1 项 不等于 做 计数 用 b 的 第 1 项：
        返还 假。
    完。
    返还 做 集合v2_是子集 用 a、b。
完。

集合v2_不相交 接受 a、b：
    可变 i 为 0。
    只要 i 小于 做 计数 用 a 的 第 1 项：
        若 做 集合v2_包含 用 b、a 的 第 1 项 的 第 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

集合v2_映射 接受 集合、函数f：
    可变 结果 为 做 集合v2_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合 的 第 1 项：
        做 集合v2_追加 用 结果、做 函数f 用 集合 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合v2_过滤 接受 集合、条件f：
    可变 结果 为 做 集合v2_创建。
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合 的 第 1 项：
        若 做 条件f 用 集合 的 第 1 项 的 第 i 项：
            做 集合v2_追加 用 结果、集合 的 第 1 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

集合v2_归约 接受 集合、初始值、归约f：
    可变 累加 为 初始值。
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合 的 第 1 项：
        累加 为 做 归约f 用 累加、集合 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 累加。
完。

集合v2_任意满足 接受 集合、条件f：
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合 的 第 1 项：
        若 做 条件f 用 集合 的 第 1 项 的 第 i 项：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

集合v2_全部满足 接受 集合、条件f：
    可变 i 为 0。
    只要 i 小于 做 计数 用 集合 的 第 1 项：
        若 做 条件f 用 集合 的 第 1 项 的 第 i 项 等于 假：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

本章 收束。
