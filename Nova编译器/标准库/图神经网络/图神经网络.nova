《图神经网络》

引用《..·基础》。

引用《..·算法·数学扩展》。

引用《..·集合·图·图》。

图神经网_图_创建 接受 节点数：
    可变 邻居 为 一组：（空）。
    可变 特征 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点数：
        做 追加 用 邻居、一组：（空）。
        做 追加 用 特征、一组：（空）。
        i 为 i 加 1。
    完。
    返还 一组：节点数、0、邻居、一组：（空）、特征、一组：（空）。
完。

图神经网_图_添加边 接受 图、源、目标、权重：
    做 追加 用 图 的 第 2 项 的 第 源 项、目标。
    做 追加 用 图 的 第 3 项、权重。
    图 的 第 1 项 为 图 的 第 1 项 加 1。
完。

图神经网_图_添加双向边 接受 图、u、v、权重：
    做 图_添加边 用 图、u、v、权重。
    做 图_添加边 用 图、v、u、权重。
完。

图_设节点特征 接受 图、节点、特征：
    图 的 第 4 项 的 第 节点 项 为 特征。
完。

图_节点特征 接受 图、节点：
    返还 图 的 第 4 项 的 第 节点 项。
完。

图神经网_图_邻居 接受 图、节点：
    返还 图 的 第 2 项 的 第 节点 项。
完。

图神经网络_图_节点数 接受 图：
    返还 图 的 第 0 项。
完。

图神经网_图_边数 接受 图：
    返还 图 的 第 1 项。
完。

图神经网_图_度 接受 图、节点：
    返还 做 计数 用 图 的 第 2 项 的 第 节点 项。
完。

GCN_层 接受 图、权重W、偏置、特征维度、输出维度：
    N 为 做 图神经网络_图_节点数 用 图。
    可变 新特征 为 一组：（空）。
    可变 v 为 0。
    只要 v 小于 N：
        邻居 为 做 图_邻居 用 图、v。
        值维度 为 做 计数 用 邻居 加 1。
        可变 聚合 为 一组：（空）。
        可变 k 为 0。
        只要 k 小于 特征维度：
            做 追加 用 聚合、0。
            k 为 k 加 1。
        完。
        自身特征 为 做 图_节点特征 用 图、v。
        k 为 0。
        只要 k 小于 特征维度：
            若 k 小于 做 计数 用 自身特征：
                聚合 的 第 k 项 为 聚合 的 第 k 项 加 自身特征 的 第 k 项 乘 1000 除以 值维度。
            完。
            k 为 k 加 1。
        完。
        可变 ni 为 0。
        只要 ni 小于 做 计数 用 邻居：
            u 为 邻居 的 第 ni 项。
            d_u 为 做 图_度 用 图、u 加 1。
            可变 归一化 为 做 整数sqrt 用 d_u 乘 值维度。
            若 归一化 等于 0：
                归一化 为 1。
            完。
            u特征 为 做 图_节点特征 用 图、u。
            k 为 0。
            只要 k 小于 特征维度：
                若 k 小于 做 计数 用 u特征：
                    聚合 的 第 k 项 为 聚合 的 第 k 项 加 u特征 的 第 k 项 乘 1000 除以 归一化。
                完。
                k 为 k 加 1。
            完。
            ni 为 ni 加 1。
        完。
        可变 输出 为 一组：（空）。
        k 为 0。
        只要 k 小于 输出维度：
            可变 数值 为 偏置 的 第 k 项。
            可变 j 为 0。
            只要 j 小于 特征维度：
                数值 为 数值 加 权重W 的 第 k 乘 特征维度 加 j 项 乘 聚合 的 第 j 项 除以 1000。
                j 为 j 加 1。
            完。
            若 数值 小于 0：
                数值 为 0。
            完。
            做 追加 用 输出、数值。
            k 为 k 加 1。
        完。
        做 追加 用 新特征、输出。
        v 为 v 加 1。
    完。
    v 为 0。
    只要 v 小于 N：
        做 图_设节点特征 用 图、v、新特征 的 第 v 项。
        v 为 v 加 1。
    完。
    返还 新特征。
完。

GAT_注意力 接受 图、权重W、注意力a、特征维度、输出维度：
    N 为 做 图神经网络_图_节点数 用 图。
    可变 新特征 为 一组：（空）。
    可变 v 为 0。
    只要 v 小于 N：
        邻居 为 做 图_邻居 用 图、v。
        v特征 为 做 图_节点特征 用 图、v。
        可变 Wh_v 为 一组：（空）。
        可变 k 为 0。
        只要 k 小于 输出维度：
            可变 数值 为 0。
            可变 j 为 0。
            只要 j 小于 特征维度：
                若 j 小于 做 计数 用 v特征：
                    数值 为 数值 加 权重W 的 第 k 乘 特征维度 加 j 项 乘 v特征 的 第 j 项 除以 1000。
                完。
                j 为 j 加 1。
            完。
            做 追加 用 Wh_v、数值。
            k 为 k 加 1。
        完。
        可变 得分列表 为 一组：（空）。
        可变 ni 为 0。
        只要 ni 小于 做 计数 用 邻居：
            u 为 邻居 的 第 ni 项。
            u特征 为 做 图_节点特征 用 图、u。
            可变 Wh_u 为 一组：（空）。
            k 为 0。
            只要 k 小于 输出维度：
                可变 数值 为 0。
                可变 j 为 0。
                只要 j 小于 特征维度：
                    若 j 小于 做 计数 用 u特征：
                        数值 为 数值 加 权重W 的 第 k 乘 特征维度 加 j 项 乘 u特征 的 第 j 项 除以 1000。
                    完。
                    j 为 j 加 1。
                完。
                做 追加 用 Wh_u、数值。
                k 为 k 加 1。
            完。
            可变 e 为 0。
            k 为 0。
            只要 k 小于 输出维度：
                e 为 e 加 注意力a 的 第 k 项 乘 Wh_v 的 第 k 项 除以 1000。
                e 为 e 加 注意力a 的 第 输出维度 加 k 项 乘 Wh_u 的 第 k 项 除以 1000。
                k 为 k 加 1。
            完。
            若 e 小于 0：
                e 为 e 除以 5。
            完。
            做 追加 用 得分列表、e。
            ni 为 ni 加 1。
        完。
        可变 最大得分 为 0。
        若 做 计数 用 得分列表 大于 0：
            最大得分 为 得分列表 的 第 0 项。
        完。
        ni 为 1。
        只要 ni 小于 做 计数 用 得分列表：
            若 得分列表 的 第 ni 项 大于 最大得分：
                最大得分 为 得分列表 的 第 ni 项。
            完。
            ni 为 ni 加 1。
        完。
        可变 指数组 为 一组：（空）。
        可变 总和 为 0。
        ni 为 0。
        只要 ni 小于 做 计数 用 得分列表：
            可变 e 为 1000 加 (得分列表 的 第 ni 项 减 最大得分)。
            若 e 小于 1：
                e 为 1。
            完。
            做 追加 用 指数组、e。
            总和 为 总和 加 e。
            ni 为 ni 加 1。
        完。
        若 总和 等于 0：
            总和 为 1。
        完。
        可变 输出 为 一组：（空）。
        k 为 0。
        只要 k 小于 输出维度：
            做 追加 用 输出、0。
            k 为 k 加 1。
        完。
        ni 为 0。
        只要 ni 小于 做 计数 用 邻居：
            α 为 指数组 的 第 ni 项 乘 1000 除以 总和。
            u 为 邻居 的 第 ni 项。
            u特征2 为 做 图_节点特征 用 图、u。
            k 为 0。
            只要 k 小于 输出维度：
                可变 wh 为 0。
                可变 j 为 0。
                只要 j 小于 特征维度：
                    若 j 小于 做 计数 用 u特征2：
                        wh 为 wh 加 权重W 的 第 k 乘 特征维度 加 j 项 乘 u特征2 的 第 j 项 除以 1000。
                    完。
                    j 为 j 加 1。
                完。
                输出 的 第 k 项 为 输出 的 第 k 项 加 α 乘 wh 除以 1000。
                k 为 k 加 1。
            完。
            ni 为 ni 加 1。
        完。
        做 追加 用 新特征、输出。
        v 为 v 加 1。
    完。
    v 为 0。
    只要 v 小于 N：
        做 图_设节点特征 用 图、v、新特征 的 第 v 项。
        v 为 v 加 1。
    完。
    返还 新特征。
完。

GraphSAGE_采样邻居 接受 图、节点、采样数、随机态：
    邻居 为 做 图_邻居 用 图、节点。
    n 为 做 计数 用 邻居。
    若 n 小于或等于 采样数：
        返还 邻居。
    完。
    可变 采样 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 采样数：
        随机态 为 随机态 位异或 随机态 左移 13。
        随机态 为 随机态 位异或 随机态 右移 7。
        随机态 为 随机态 位异或 随机态 左移 17。
        若 随机态 小于 0：
            随机态 为 0 减 随机态。
        完。
        索引 为 随机态 取余于 n。
        做 追加 用 采样、邻居 的 第 索引 项。
        i 为 i 加 1。
    完。
    返还 采样。
完。

GraphSAGE_均值聚合 接受 图、节点、采样邻居列表：
    邻居 为 采样邻居列表。
    n 为 做 计数 用 邻居。
    若 n 等于 0：
        返还 做 图_节点特征 用 图、节点。
    完。
    自身 为 做 图_节点特征 用 图、节点。
    维度 为 做 计数 用 自身。
    可变 聚合 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 维度：
        做 追加 用 聚合、自身 的 第 k 项。
        k 为 k 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        邻特征 为 做 图_节点特征 用 图、邻居 的 第 i 项。
        k 为 0。
        只要 k 小于 维度：
            若 k 小于 做 计数 用 邻特征：
                聚合 的 第 k 项 为 聚合 的 第 k 项 加 邻特征 的 第 k 项。
            完。
            k 为 k 加 1。
        完。
        i 为 i 加 1。
    完。
    k 为 0。
    只要 k 小于 维度：
        聚合 的 第 k 项 为 聚合 的 第 k 项 除以 (n 加 1)。
        k 为 k 加 1。
    完。
    返还 聚合。
完。

MPNN_消息 接受 源特征、_目标特征、边特征：
    维度 为 做 计数 用 源特征。
    可变 消息 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        可变 m 为 源特征 的 第 i 项。
        若 i 小于 做 计数 用 边特征：
            m 为 m 加 边特征 的 第 i 项。
        完。
        做 追加 用 消息、m。
        i 为 i 加 1。
    完。
    返还 消息。
完。

MPNN_聚合 接受 消息列表、模式：
    n 为 做 计数 用 消息列表。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    维度 为 做 计数 用 消息列表 的 第 0 项。
    可变 结果 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 维度：
        做 追加 用 结果、0。
        k 为 k 加 1。
    完。
    若 模式 等于 2：
        k 为 0。
        只要 k 小于 维度：
            结果 的 第 k 项 为 消息列表 的 第 0 项 的 第 k 项。
            k 为 k 加 1。
        完。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        k 为 0。
        只要 k 小于 维度：
            若 模式 等于 0 或者 模式 等于 1：
                结果 的 第 k 项 为 结果 的 第 k 项 加 消息列表 的 第 i 项 的 第 k 项。
            完。
            若 模式 等于 2 并且 i 大于 0：
                若 消息列表 的 第 i 项 的 第 k 项 大于 结果 的 第 k 项：
                    结果 的 第 k 项 为 消息列表 的 第 i 项 的 第 k 项。
                完。
            完。
            k 为 k 加 1。
        完。
        i 为 i 加 1。
    完。
    若 模式 等于 1 并且 n 大于 0：
        k 为 0。
        只要 k 小于 维度：
            结果 的 第 k 项 为 结果 的 第 k 项 除以 n。
            k 为 k 加 1。
        完。
    完。
    返还 结果。
完。

图_全局均值池化 接受 图：
    N 为 做 图神经网络_图_节点数 用 图。
    若 N 等于 0：
        返还 一组：（空）。
    完。
    首特征 为 做 图_节点特征 用 图、0。
    维度 为 做 计数 用 首特征。
    可变 池化 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 维度：
        做 追加 用 池化、0。
        k 为 k 加 1。
    完。
    可变 v 为 0。
    只要 v 小于 N：
        f 为 做 图_节点特征 用 图、v。
        k 为 0。
        只要 k 小于 维度：
            若 k 小于 做 计数 用 f：
                池化 的 第 k 项 为 池化 的 第 k 项 加 f 的 第 k 项。
            完。
            k 为 k 加 1。
        完。
        v 为 v 加 1。
    完。
    k 为 0。
    只要 k 小于 维度：
        池化 的 第 k 项 为 池化 的 第 k 项 除以 N。
        k 为 k 加 1。
    完。
    返还 池化。
完。

图_TopK池化 接受 图、分数、k：
    N 为 做 图神经网络_图_节点数 用 图。
    可变 索引 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 N：
        做 追加 用 索引、i。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 k 并且 i 小于 N：
        可变 最大idx 为 i。
        可变 j 为 i 加 1。
        只要 j 小于 N：
            若 分数 的 第 索引 的 第 j 项 项 大于 分数 的 第 索引 的 第 最大idx 项 项：
                最大idx 为 j。
            完。
            j 为 j 加 1。
        完。
        若 最大idx 不等于 i：
            临时 为 索引 的 第 i 项。
            索引 的 第 i 项 为 索引 的 第 最大idx 项。
            索引 的 第 最大idx 项 为 临时。
        完。
        i 为 i 加 1。
    完。
    可变 选中 为 一组：（空）。
    i 为 0。
    只要 i 小于 k 并且 i 小于 N：
        做 追加 用 选中、索引 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 选中。
完。

链接_内积分数 接受 特征u、特征v：
    维度 为 做 计数 用 特征u。
    可变 分数 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        若 i 小于 做 计数 用 特征v：
            分数 为 分数 加 特征u 的 第 i 项 乘 特征v 的 第 i 项 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 分数。
完。

GIN_层 接受 图、ε、MLP权重、特征维度、输出维度：
    N 为 做 图神经网络_图_节点数 用 图。
    可变 新特征 为 一组：（空）。
    可变 v 为 0。
    只要 v 小于 N：
        邻居 为 做 图_邻居 用 图、v。
        v特征 为 做 图_节点特征 用 图、v。
        可变 聚合 为 一组：（空）。
        可变 k 为 0。
        只要 k 小于 特征维度：
            可变 数值 为 (1000 加 ε) 乘 v特征 的 第 k 项 除以 1000。
            可变 ni 为 0。
            只要 ni 小于 做 计数 用 邻居：
                u特征 为 做 图_节点特征 用 图、邻居 的 第 ni 项。
                若 k 小于 做 计数 用 u特征：
                    数值 为 数值 加 u特征 的 第 k 项。
                完。
                ni 为 ni 加 1。
            完。
            做 追加 用 聚合、数值。
            k 为 k 加 1。
        完。
        可变 输出 为 一组：（空）。
        k 为 0。
        只要 k 小于 输出维度：
            可变 数值 为 0。
            可变 j 为 0。
            只要 j 小于 特征维度：
                数值 为 数值 加 MLP权重 的 第 k 乘 特征维度 加 j 项 乘 聚合 的 第 j 项 除以 1000。
                j 为 j 加 1。
            完。
            若 数值 小于 0：
                数值 为 0。
            完。
            做 追加 用 输出、数值。
            k 为 k 加 1。
        完。
        做 追加 用 新特征、输出。
        v 为 v 加 1。
    完。
    v 为 0。
    只要 v 小于 N：
        做 图_设节点特征 用 图、v、新特征 的 第 v 项。
        v 为 v 加 1。
    完。
    返还 新特征。
完。

异构图_创建 接受 节点类型数：
    可变 类型节点 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 节点类型数：
        做 追加 用 类型节点、一组：（空）。
        i 为 i 加 1。
    完。
    返还 一组：节点类型数、类型节点、一组：（空）。
完。

异构图_添加节点 接受 图、类型、特征：
    索引 为 做 计数 用 图 的 第 1 项 的 第 类型 项。
    做 追加 用 图 的 第 1 项 的 第 类型 项、特征。
    返还 索引。
完。

异构图_添加边 接受 图、源类型、源idx、关系、目标类型、目标idx：
    做 追加 用 图 的 第 2 项、一组：源类型、源idx、关系、目标类型、目标idx。
完。

可变 _gnn_传播次数 为 0。

可变 _gnn_聚合次数 为 0。

可变 _gnn_最大节点数 为 0。

gnn_记录传播 接受 节点数：
    _gnn_传播次数 为 _gnn_传播次数 加 1。
    若 节点数 大于 _gnn_最大节点数：
        _gnn_最大节点数 为 节点数。
    完。
完。

gnn_记录聚合 接受 零参：
    _gnn_聚合次数 为 _gnn_聚合次数 加 1。
完。

gnn_过平滑检测 接受 层数：
    若 层数 大于 6：
        返还 「高风险: 层数过多导致过平滑」。
    完。
    若 层数 大于 3：
        返还 「中风险: 注意过平滑」。
    完。
    返还 「低风险」。
完。

gnn_可扩展性评估 接受 零参：
    若 _gnn_最大节点数 大于 100000：
        返还 「大规模图: 建议采样或分区」。
    完。
    若 _gnn_最大节点数 大于 10000：
        返还 「中等规模: 全图训练可行」。
    完。
    返还 「小规模: 效率良好」。
完。

gnn_推荐层数 接受 节点数、边数：
    若 节点数 等于 0：
        返还 2。
    完。
    平均度 为 边数 除以 节点数。
    若 平均度 大于 20：
        返还 2。
    完。
    若 平均度 大于 5：
        返还 3。
    完。
    返还 4。
完。

gnn_特征维度建议 接受 节点数：
    若 节点数 大于 10000：
        返还 64。
    完。
    若 节点数 大于 1000：
        返还 128。
    完。
    返还 256。
完。

gnn_注意力头建议 接受 特征维度：
    若 特征维度 大于或等于 256：
        返还 8。
    完。
    若 特征维度 大于或等于 128：
        返还 4。
    完。
    若 特征维度 大于或等于 64：
        返还 2。
    完。
    返还 1。
完。

gnn_内存估算 接受 节点数、特征维度、层数：
    节点内存 为 节点数 乘 特征维度 乘 4。
    中间 为 节点内存 乘 层数。
    返还 节点内存 加 中间。
完。

gnn_训练配置 接受 节点数、边数：
    层数 为 做 gnn_推荐层数 用 节点数、边数。
    维度 为 做 gnn_特征维度建议 用 节点数。
    头数 为 做 gnn_注意力头建议 用 维度。
    内存 为 做 gnn_内存估算 用 节点数、维度、层数。
    返还 一组：层数、维度、头数、内存。
完。

gnn_批大小建议 接受 节点数：
    若 节点数 大于 50000：
        返还 512。
    完。
    若 节点数 大于 10000：
        返还 1024。
    完。
    返还 2048。
完。

本章 收束。
