《基准测试》

引用《..·基础》。

引用《..·算法·数学扩展》。

基准_创建 接受 名称、迭代数：
    返还 一组：名称、迭代数、0x7FFFFFFF、0、一组：（空）。
完。

基准_记录 接受 基准、耗时：
    若 耗时 小于 基准 的 第 2 项：
        基准 的 第 2 项 为 耗时。
    完。
    若 耗时 大于 基准 的 第 3 项：
        基准 的 第 3 项 为 耗时。
    完。
    基准 的 第 4 项 为 基准 的 第 4 项 加 耗时。
    做 追加 用 基准 的 第 5 项、耗时。
完。

基准_平均 接受 基准：
    n 为 做 计数 用 基准 的 第 5 项。
    若 n 等于 0：
        返还 0。
    完。
    返还 基准 的 第 4 项 除以 n。
完。

基准_中位数 接受 基准：
    n 为 做 计数 用 基准 的 第 5 项。
    若 n 等于 0：
        返还 0。
    完。
    可变 已排序 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 已排序、基准 的 第 5 项 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 已排序 的 第 j 项 小于 已排序 的 第 i 项：
                临时 为 已排序 的 第 i 项。
                已排序 的 第 i 项 为 已排序 的 第 j 项。
                已排序 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 已排序 的 第 n 除以 2 项。
完。

基准_标准差 接受 基准：
    n 为 做 计数 用 基准 的 第 5 项。
    若 n 小于 2：
        返还 0。
    完。
    均值 为 做 基准_平均 用 基准。
    可变 方差 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        d 为 基准 的 第 5 项 的 第 i 项 减 均值。
        方差 为 方差 加 d 乘 d 除以 1000。
        i 为 i 加 1。
    完。
    方差 为 方差 除以 (n 减 1)。
    返还 做 整数sqrt 用 方差 乘 1000。
完。

基准_报告 接受 基准：
    做 打印 用 「═══ 基准测试: 」 加 基准 的 第 0 项 加 「 ═══」。
    做 打印 用 「 迭代次数: 」 加 做 转字符串 用 做 计数 用 基准 的 第 5 项。
    做 打印 用 「 平均耗时: 」 加 做 转字符串 用 做 基准_平均 用 基准。
    做 打印 用 「 中位耗时: 」 加 做 转字符串 用 做 基准_中位数 用 基准。
    做 打印 用 「 最小耗时: 」 加 做 转字符串 用 基准 的 第 2 项。
    做 打印 用 「 最大耗时: 」 加 做 转字符串 用 基准 的 第 3 项。
    做 打印 用 「 标准差: 」 加 做 转字符串 用 做 基准_标准差 用 基准。
完。

基准_比较 接受 bench1、bench2：
    avg1 为 做 基准_平均 用 bench1。
    avg2 为 做 基准_平均 用 bench2。
    若 avg2 等于 0：
        返还 0。
    完。
    加速比 为 avg1 乘 1000 除以 avg2。
    做 打印 用 「比较: 」 加 bench1 的 第 0 项 加 「 vs 」 加 bench2 的 第 0 项。
    做 打印 用 「 加速比: 」 加 做 转字符串 用 加速比 加 「/1000」。
    若 加速比 大于 1000：
        做 打印 用 「 」 加 bench2 的 第 0 项 加 「 更快」。
    否则若 加速比 小于 1000：
        做 打印 用 「 」 加 bench1 的 第 0 项 加 「 更快」。
    否则：
        做 打印 用 「 性能相同」。
    完。
    返还 加速比。
完。

基准_回归检测 接受 当前bench、基线bench、阈值百分比：
    当前avg 为 做 基准_平均 用 当前bench。
    基线avg 为 做 基准_平均 用 基线bench。
    若 基线avg 等于 0：
        返还 0。
    完。
    变化 为 (当前avg 减 基线avg) 乘 100 除以 基线avg。
    若 变化 大于 阈值百分比：
        做 打印 用 「⚠️ 性能回归: 」 加 当前bench 的 第 0 项 加 「 慢了 」 加 做 转字符串 用 变化 加 「%」。
        返还 1。
    完。
    返还 0。
完。

基准测试_套件_创建 接受 名称：
    返还 一组：名称、一组：（空）。
完。

基准测试_套件_追加 接受 suite、基准：
    做 追加 用 suite 的 第 1 项、基准。
完。

基准测试_套件_报告 接受 suite：
    做 打印 用 「╔══ 基准测试套件: 」 加 suite 的 第 0 项 加 「 ══╗」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 suite 的 第 1 项：
        做 基准_报告 用 suite 的 第 1 项 的 第 i 项。
        i 为 i 加 1。
    完。
    做 打印 用 「╚══ 共 」 加 做 转字符串 用 做 计数 用 suite 的 第 1 项 加 「 项 ══╝」。
完。

基准测试_吞吐量 接受 操作数、耗时ms：
    若 耗时ms 等于 0：
        返还 0。
    完。
    返还 操作数 乘 1000 除以 耗时ms。
完。

内存基准_创建 接受 名称：
    返还 一组：名称、0、一组：（空）。
完。

内存基准_记录 接受 mb、字节数：
    mb 的 第 1 项 为 mb 的 第 1 项 加 1。
    mb 的 第 2 项 为 mb 的 第 2 项 加 字节数。
    做 追加 用 mb 的 第 3 项、字节数。
完。

内存基准_报告 接受 mb：
    做 打印 用 「内存基准: 」 加 mb 的 第 0 项。
    做 打印 用 「 分配次数: 」 加 做 转字符串 用 mb 的 第 1 项。
    做 打印 用 「 总字节: 」 加 做 转字符串 用 mb 的 第 2 项。
    若 mb 的 第 1 项 大于 0：
        做 打印 用 「 平均分配: 」 加 做 转字符串 用 mb 的 第 2 项 除以 mb 的 第 1 项。
    完。
完。

基准_排序副本 接受 基准：
    n 为 做 计数 用 基准 的 第 5 项。
    可变 已排序 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 已排序、基准 的 第 5 项 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n 减 1：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            若 已排序 的 第 j 项 小于 已排序 的 第 i 项：
                临时 为 已排序 的 第 i 项。
                已排序 的 第 i 项 为 已排序 的 第 j 项。
                已排序 的 第 j 项 为 临时。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 已排序。
完。

基准_百分位 接受 基准、p：
    已排序 为 做 基准_排序副本 用 基准。
    n 为 做 计数 用 已排序。
    若 n 等于 0：
        返还 0。
    完。
    可变 索引 为 p 乘 (n 减 1) 除以 100。
    若 索引 大于或等于 n：
        索引 为 n 减 1。
    完。
    若 索引 小于 0：
        索引 为 0。
    完。
    返还 已排序 的 第 索引 项。
完。

基准_p50 接受 基准：
    返还 做 基准_百分位 用 基准、50。
完。

基准_p90 接受 基准：
    返还 做 基准_百分位 用 基准、90。
完。

基准_p95 接受 基准：
    返还 做 基准_百分位 用 基准、95。
完。

基准_p99 接受 基准：
    返还 做 基准_百分位 用 基准、99。
完。

基准_去除预热 接受 基准、预热数：
    n 为 做 计数 用 基准 的 第 5 项。
    若 预热数 大于或等于 n：
        返还。
    完。
    可变 新结果 为 一组：（空）。
    可变 新总 为 0。
    可变 新最小 为 0x7FFFFFFF。
    可变 新最大 为 0。
    可变 i 为 预热数。
    只要 i 小于 n：
        v 为 基准 的 第 5 项 的 第 i 项。
        做 追加 用 新结果、v。
        新总 为 新总 加 v。
        若 v 小于 新最小：
            新最小 为 v。
        完。
        若 v 大于 新最大：
            新最大 为 v。
        完。
        i 为 i 加 1。
    完。
    基准 的 第 2 项 为 新最小。
    基准 的 第 3 项 为 新最大。
    基准 的 第 4 项 为 新总。
    基准 的 第 5 项 为 新结果。
完。

基准_变异系数 接受 基准：
    均值 为 做 基准_平均 用 基准。
    若 均值 等于 0：
        返还 0。
    完。
    sd 为 做 基准_标准差 用 基准。
    返还 sd 乘 1000 除以 均值。
完。

基准_是否稳定 接受 基准、阈值cv：
    返还 做 基准_变异系数 用 基准 小于 阈值cv。
完。

套件_比较表 接受 suite：
    做 打印 用 「╔══════════════════════════════════════════════════════╗」。
    做 打印 用 「║ 名称          平均    中位    P95     最小    最大」。
    做 打印 用 「╠══════════════════════════════════════════════════════╣」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 suite 的 第 1 项：
        b 为 suite 的 第 1 项 的 第 i 项。
        可变 行 为 「║ 」 加 b 的 第 0 项。
        可变 填充 为 18 减 做 计数 用 b 的 第 0 项。
        可变 p 为 0。
        只要 p 小于 填充：
            行 为 行 加 「 」。
            p 为 p 加 1。
        完。
        行 为 行 加 做 转字符串 用 做 基准_平均 用 b。
        行 为 行 加 「 」 加 做 转字符串 用 做 基准_中位数 用 b。
        行 为 行 加 「 」 加 做 转字符串 用 做 基准_p95 用 b。
        行 为 行 加 「 」 加 做 转字符串 用 b 的 第 2 项。
        行 为 行 加 「 」 加 做 转字符串 用 b 的 第 3 项。
        做 打印 用 行。
        i 为 i 加 1。
    完。
    做 打印 用 「╚══════════════════════════════════════════════════════╝」。
完。

套件_最快 接受 suite：
    可变 最优 为 0。
    可变 best_avg 为 0x7FFFFFFF。
    可变 i 为 0。
    只要 i 小于 做 计数 用 suite 的 第 1 项：
        均值 为 做 基准_平均 用 suite 的 第 1 项 的 第 i 项。
        若 均值 小于 best_avg：
            best_avg 为 均值。
            最优 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最优。
完。

套件_最慢 接受 suite：
    可变 worst 为 0。
    可变 worst_avg 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 suite 的 第 1 项：
        均值 为 做 基准_平均 用 suite 的 第 1 项 的 第 i 项。
        若 均值 大于 worst_avg：
            worst_avg 为 均值。
            worst 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 worst。
完。

套件_回归检测 接受 当前suite、基线suite、阈值百分比：
    n当前 为 做 计数 用 当前suite 的 第 1 项。
    n基线 为 做 计数 用 基线suite 的 第 1 项。
    可变 回归数 为 0。
    可变 i 为 0。
    只要 i 小于 n当前 并且 i 小于 n基线：
        r 为 做 基准_回归检测 用 当前suite 的 第 1 项 的 第 i 项、基线suite 的 第 1 项 的 第 i 项、阈值百分比。
        回归数 为 回归数 加 r。
        i 为 i 加 1。
    完。
    做 打印 用 「回归检测: 」 加 做 转字符串 用 回归数 加 「/」 加 做 转字符串 用 i 加 「 项有回归」。
    返还 回归数。
完。

基准_直方图 接受 基准、桶数：
    n 为 做 计数 用 基准 的 第 5 项。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    最小 为 基准 的 第 2 项。
    最大 为 基准 的 第 3 项。
    可变 范围 为 最大 减 最小。
    若 范围 等于 0：
        范围 为 1。
    完。
    可变 实际桶数 为 桶数。
    若 实际桶数 等于 0：
        实际桶数 为 1。
    完。
    可变 桶宽 为 范围 除以 实际桶数。
    若 桶宽 等于 0：
        桶宽 为 1。
    完。
    可变 桶 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 实际桶数：
        做 追加 用 桶、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 索引 为 (基准 的 第 5 项 的 第 i 项 减 最小) 除以 桶宽。
        若 索引 大于或等于 实际桶数：
            索引 为 实际桶数 减 1。
        完。
        若 索引 小于 0：
            索引 为 0。
        完。
        桶 的 第 索引 项 为 桶 的 第 索引 项 加 1。
        i 为 i 加 1。
    完。
    返还 桶。
完。

基准_打印直方图 接受 基准、桶数：
    桶 为 做 基准_直方图 用 基准、桶数。
    n桶 为 做 计数 用 桶。
    可变 最大计数 为 1。
    可变 i 为 0。
    只要 i 小于 n桶：
        若 桶 的 第 i 项 大于 最大计数：
            最大计数 为 桶 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    做 打印 用 「延迟分布 (」 加 基准 的 第 0 项 加 「):」。
    可变 桶宽 为 (基准 的 第 3 项 减 基准 的 第 2 项) 除以 桶数。
    若 桶宽 等于 0：
        桶宽 为 1。
    完。
    i 为 0。
    只要 i 小于 n桶：
        下界 为 基准 的 第 2 项 加 i 乘 桶宽。
        上界 为 下界 加 桶宽。
        条长 为 桶 的 第 i 项 乘 40 除以 最大计数。
        可变 行 为 「 [」 加 做 转字符串 用 下界 加 「-」 加 做 转字符串 用 上界 加 「] 」。
        可变 j 为 0。
        只要 j 小于 条长：
            行 为 行 加 「█」。
            j 为 j 加 1。
        完。
        行 为 行 加 「 」 加 做 转字符串 用 桶 的 第 i 项。
        做 打印 用 行。
        i 为 i 加 1。
    完。
完。

吞吐量_格式化 接受 操作数、耗时ms：
    ops 为 做 基准测试_吞吐量 用 操作数、耗时ms。
    若 ops 大于或等于 1000000：
        做 打印 用 做 转字符串 用 ops 除以 1000000 加 「M ops/s」。
    否则若 ops 大于或等于 1000：
        做 打印 用 做 转字符串 用 ops 除以 1000 加 「K ops/s」。
    否则：
        做 打印 用 做 转字符串 用 ops 加 「 ops/s」。
    完。
    返还 ops。
完。

带宽 接受 字节数、耗时ms：
    若 耗时ms 等于 0：
        返还 0。
    完。
    返还 字节数 乘 1000 除以 耗时ms。
完。

带宽_格式化 接受 字节数、耗时ms：
    bps 为 做 带宽 用 字节数、耗时ms。
    若 bps 大于或等于 1073741824：
        做 打印 用 做 转字符串 用 bps 除以 1073741824 加 「 GB/s」。
    否则若 bps 大于或等于 1048576：
        做 打印 用 做 转字符串 用 bps 除以 1048576 加 「 MB/s」。
    否则若 bps 大于或等于 1024：
        做 打印 用 做 转字符串 用 bps 除以 1024 加 「 KB/s」。
    否则：
        做 打印 用 做 转字符串 用 bps 加 「 B/s」。
    完。
    返还 bps。
完。

基准_详细报告 接受 基准：
    做 打印 用 「═══════════════════════════════════════」。
    做 打印 用 「 基准: 」 加 基准 的 第 0 项。
    做 打印 用 「═══════════════════════════════════════」。
    做 打印 用 「 样本数: 」 加 做 转字符串 用 做 计数 用 基准 的 第 5 项。
    做 打印 用 「 平均: 」 加 做 转字符串 用 做 基准_平均 用 基准。
    做 打印 用 「 中位: 」 加 做 转字符串 用 做 基准_中位数 用 基准。
    做 打印 用 「 最小: 」 加 做 转字符串 用 基准 的 第 2 项。
    做 打印 用 「 最大: 」 加 做 转字符串 用 基准 的 第 3 项。
    做 打印 用 「 标准差: 」 加 做 转字符串 用 做 基准_标准差 用 基准。
    做 打印 用 「 CV: 」 加 做 转字符串 用 做 基准_变异系数 用 基准 加 「/1000」。
    做 打印 用 「 P50: 」 加 做 转字符串 用 做 基准_p50 用 基准。
    做 打印 用 「 P90: 」 加 做 转字符串 用 做 基准_p90 用 基准。
    做 打印 用 「 P95: 」 加 做 转字符串 用 做 基准_p95 用 基准。
    做 打印 用 「 P99: 」 加 做 转字符串 用 做 基准_p99 用 基准。
    若 做 基准_是否稳定 用 基准、100：
        做 打印 用 「 状态: ✅ 稳定 (CV < 10%)」。
    否则：
        做 打印 用 「 状态: ⚠️ 不稳定 (CV >= 10%)」。
    完。
完。

组_创建 接受 名称：
    返还 一组：名称、一组：（空）。
完。

组_添加套件 接受 组、suite：
    做 追加 用 组 的 第 1 项、suite。
完。

组_总报告 接受 组：
    做 打印 用 「╔══════════════════════════════════════════╗」。
    做 打印 用 「║ 基准测试组: 」 加 组 的 第 0 项。
    做 打印 用 「╠══════════════════════════════════════════╣」。
    可变 总bench数 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 组 的 第 1 项：
        s 为 组 的 第 1 项 的 第 i 项。
        做 打印 用 「║ 套件: 」 加 s 的 第 0 项 加 「 (」 加 做 转字符串 用 做 计数 用 s 的 第 1 项 加 「项)」。
        总bench数 为 总bench数 加 做 计数 用 s 的 第 1 项。
        i 为 i 加 1。
    完。
    做 打印 用 「╠══════════════════════════════════════════╣」。
    做 打印 用 「║ 总计: 」 加 做 转字符串 用 做 计数 用 组 的 第 1 项 加 「 套件, 」 加 做 转字符串 用 总bench数 加 「 基准」。
    做 打印 用 「╚══════════════════════════════════════════╝」。
完。

bench_对比 接受 结果A、结果B：
    若 结果B 等于 0：
        返还 0。
    完。
    比率 为 结果A 乘 100 除以 结果B。
    返还 比率。
完。

bench_加速比 接受 基准时间、优化时间：
    若 优化时间 等于 0：
        返还 0。
    完。
    返还 基准时间 乘 1000 除以 优化时间。
完。

bench_回归检测 接受 历史、当前、阈值：
    若 做 计数 用 历史 等于 0：
        返还 假。
    完。
    可变 i 为 0。
    可变 总 为 0。
    只要 i 小于 做 计数 用 历史：
        总 为 总 加 历史 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 总 除以 做 计数 用 历史。
    若 当前 大于 均值 加 阈值：
        返还 真。
    完。
    返还 假。
完。

bench_百分位 接受 排序结果、百分位：
    n 为 做 计数 用 排序结果。
    若 n 等于 0：
        返还 0。
    完。
    可变 索引 为 n 乘 百分位 除以 100。
    若 索引 大于或等于 n：
        索引 为 n 减 1。
    完。
    返还 排序结果 的 第 索引 项。
完。

基准_P50 接受 排序结果：
    返还 做 bench_百分位 用 排序结果、50。
完。

基准_P90 接受 排序结果：
    返还 做 bench_百分位 用 排序结果、90。
完。

基准_P99 接受 排序结果：
    返还 做 bench_百分位 用 排序结果、99。
完。

可变 _bench_执行总数 为 0。

可变 _bench_回归数 为 0。

可变 _bench_最快时间 为 999999999。

可变 _bench_最慢时间 为 0。

bench_记录 接受 时间：
    _bench_执行总数 为 _bench_执行总数 加 1。
    若 时间 小于 _bench_最快时间：
        _bench_最快时间 为 时间。
    完。
    若 时间 大于 _bench_最慢时间：
        _bench_最慢时间 为 时间。
    完。
完。

bench_记录回归 接受 零参：
    _bench_回归数 为 _bench_回归数 加 1。
完。

bench_稳定性评估 接受 零参：
    若 _bench_执行总数 小于 3：
        返还 「样本不足」。
    完。
    可变 范围 为 _bench_最慢时间 减 _bench_最快时间。
    若 _bench_最快时间 等于 0：
        返还 「数据异常」。
    完。
    变异 为 范围 乘 100 除以 _bench_最快时间。
    若 变异 小于 10：
        返还 「非常稳定」。
    完。
    若 变异 小于 30：
        返还 「基本稳定」。
    完。
    返还 「不稳定(可能有干扰)」。
完。

bench_重置 接受 零参：
    _bench_执行总数 为 0。
    _bench_回归数 为 0。
    _bench_最快时间 为 999999999。
    _bench_最慢时间 为 0。
完。

bench_抖动率 接受 零参：
    若 _bench_最快时间 等于 0 或者 _bench_最快时间 等于 999999999：
        返还 0。
    完。
    返还 (_bench_最慢时间 减 _bench_最快时间) 乘 1000 除以 _bench_最快时间。
完。

bench_是否可信 接受 零参：
    若 _bench_执行总数 小于 5：
        返还 「样本不足,不可信」。
    完。
    若 做 bench_抖动率 大于 500：
        返还 「抖动过大,不可信」。
    完。
    返还 「结果可信」。
完。

bench_重置全部 接受 零参：
    做 bench_重置。
完。

本章 收束。
