《混合专家》

引用《..·基础》。

引用《..·激活函数·激活函数》。

_混合专家_指数 接受 x：
    若 x 小于 负 4000：
        返还 2。
    完。
    若 x 大于 4000：
        返还 50000。
    完。
    若 x 大于 1000：
        h 为 做 _混合专家_指数 用 x 除以 2。
        返还 h 乘 h 除以 1000。
    完。
    若 x 小于 负 1000：
        h 为 做 _混合专家_指数 用 x 除以 2。
        返还 h 乘 h 除以 1000。
    完。
    返还 1000 加 x 加 x 乘 x 除以 2000。
完。

可变 门控_权重 为 一组：（空）。

可变 门控_分数 为 一组：（空）。

可变 专家数 为 0。

可变 输入维度 为 0。

可变 路由_种子 为 42。

可变 路由_计数器 为 一组：（空）。

混合专家_初始化 接受 n_experts、维度：
    专家数 为 n_experts。
    输入维度 为 维度。
    门控_权重 为 一组：（空）。
    路由_计数器 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n_experts 乘 维度：
        做 追加 用 门控_权重、100 加 i 取余于 50。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n_experts：
        做 追加 用 路由_计数器、0。
        i 为 i 加 1。
    完。
完。

混合专家_门控分数 接受 输入：
    门控_分数 为 一组：（空）。
    可变 e 为 0。
    只要 e 小于 专家数：
        可变 得分 为 0。
        可变 d 为 0。
        只要 d 小于 输入维度：
            索引 为 e 乘 输入维度 加 d。
            得分 为 得分 加 输入 的 第 d 项 乘 门控_权重 的 第 索引 项 除以 1000。
            d 为 d 加 1。
        完。
        做 追加 用 门控_分数、得分。
        e 为 e 加 1。
    完。
    返还 门控_分数。
完。

混合专家_噪声门控 接受 输入、噪声量：
    基数 为 做 混合专家_门控分数 用 输入。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 基数：
        路由_种子 为 (路由_种子 乘 1103515245 加 12345) 取余于 2147483647。
        噪声 为 (路由_种子 取余于 2001 减 1000) 乘 噪声量 除以 1000。
        做 追加 用 结果、基数 的 第 i 项 加 噪声。
        i 为 i 加 1。
    完。
    返还 结果。
完。

topk_选择 接受 分数、k：
    n 为 做 计数 用 分数。
    可变 ii 为 0。
    可变 索引 为 一组：（空）。
    可变 权重 为 一组：（空）。
    可变 已选 为 一组：（空）。
    只要 ii 小于 n：
        做 追加 用 已选、0。
        ii 为 ii 加 1。
    完。
    可变 选次 为 0。
    只要 选次 小于 k：
        可变 最佳 为 负 99999。
        可变 最佳idx 为 0。
        可变 j 为 0。
        只要 j 小于 n：
            若 已选 的 第 j 项 等于 0：
                若 分数 的 第 j 项 大于 最佳：
                    最佳 为 分数 的 第 j 项。
                    最佳idx 为 j。
                完。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 索引、最佳idx。
        做 追加 用 权重、最佳。
        已选 的 第 最佳idx 项 为 1。
        选次 为 选次 加 1。
    完。
    可变 w总 为 0。
    ii 为 0。
    只要 ii 小于 k：
        w总 为 w总 加 权重 的 第 ii 项。
        ii 为 ii 加 1。
    完。
    若 w总 小于 1：
        w总 为 1。
    完。
    可变 归一权重 为 一组：（空）。
    ii 为 0。
    只要 ii 小于 k：
        做 追加 用 归一权重、权重 的 第 ii 项 乘 1000 除以 w总。
        ii 为 ii 加 1。
    完。
    返还 一组：索引、归一权重。
完。

Switch_路由 接受 输入：
    分数 为 做 混合专家_门控分数 用 输入。
    sm 为 做 归一化指数 用 分数。
    可变 最优 为 负 1。
    可变 最优得分 为 负 99999。
    可变 i 为 0。
    只要 i 小于 做 计数 用 sm：
        若 sm 的 第 i 项 大于 最优得分：
            最优得分 为 sm 的 第 i 项。
            最优 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 一组：最优、最优得分。
完。

专家选择 接受 分数矩阵、n_tokens、容量：
    可变 分配 为 一组：（空）。
    可变 e 为 0。
    只要 e 小于 专家数：
        可变 选中 为 0。
        可变 t 为 0。
        只要 t 小于 n_tokens：
            若 选中 小于 容量：
                得分 为 分数矩阵 的 第 e 乘 n_tokens 加 t 项。
                若 得分 大于 500：
                    选中 为 选中 加 1。
                    做 追加 用 分配、e 乘 1000 加 t。
                完。
            完。
            t 为 t 加 1。
        完。
        e 为 e 加 1。
    完。
    返还 分配。
完。

混合专家_专家前向 接受 expert_id、输入：
    缩放值 为 800 加 expert_id 乘 50。
    返还 做 映射 用 输入、以 x 映射到 x 乘 缩放值 除以 1000 加 expert_id 乘 10。
完。

混合专家_专家随机丢弃 接受 索引、权重、drop_rate：
    可变 new_idx 为 一组：（空）。
    可变 new_w 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 索引：
        路由_种子 为 (路由_种子 乘 1103515245 加 12345) 取余于 2147483647。
        随机值 为 (路由_种子 取余于 1000 加 1000) 取余于 1000。
        若 随机值 大于或等于 drop_rate：
            做 追加 用 new_idx、索引 的 第 i 项。
            做 追加 用 new_w、权重 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 new_idx 等于 0：
        做 追加 用 new_idx、索引 的 第 0 项。
        做 追加 用 new_w、1000。
    完。
    返还 一组：new_idx、new_w。
完。

混合专家模块_混合专家_前向 接受 输入、k：
    分数 为 做 混合专家_门控分数 用 输入。
    sm 为 做 归一化指数 用 分数。
    选择 为 做 topk_选择 用 sm、k。
    索引 为 选择 的 第 0 项。
    权重 为 选择 的 第 1 项。
    维度 为 做 计数 用 输入。
    可变 输出 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 输出、0。
        d 为 d 加 1。
    完。
    可变 ki 为 0。
    只要 ki 小于 k：
        eid 为 索引 的 第 ki 项。
        w 为 权重 的 第 ki 项。
        exp_out 为 做 混合专家_专家前向 用 eid、输入。
        路由_计数器 的 第 eid 项 为 路由_计数器 的 第 eid 项 加 1。
        d 为 0。
        只要 d 小于 维度：
            输出 的 第 d 项 为 输出 的 第 d 项 加 exp_out 的 第 d 项 乘 w 除以 1000。
            d 为 d 加 1。
        完。
        ki 为 ki 加 1。
    完。
    返还 输出。
完。

负载均衡损失 接受 路由历史：
    n 为 做 计数 用 路由历史。
    可变 频率 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 专家数：
        做 追加 用 频率、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        eid2 为 路由历史 的 第 i 项。
        若 eid2 大于或等于 0：
            若 eid2 小于 专家数：
                频率 的 第 eid2 项 为 频率 的 第 eid2 项 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    理想 为 n 乘 1000 除以 专家数。
    可变 损失 为 0。
    i 为 0。
    只要 i 小于 专家数：
        差值 为 频率 的 第 i 项 乘 1000 减 理想。
        损失 为 损失 加 差值 乘 差值 除以 1000。
        i 为 i 加 1。
    完。
    若 专家数 等于 0：
        返还 0。
    完。
    返还 损失 除以 专家数。
完。

辅助均衡损失 接受 路由历史、平均概率：
    n 为 做 计数 用 路由历史。
    若 n 等于 0：
        返还 0。
    完。
    可变 频率 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 专家数：
        做 追加 用 频率、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        eid3 为 路由历史 的 第 i 项。
        若 eid3 大于或等于 0：
            若 eid3 小于 专家数：
                频率 的 第 eid3 项 为 频率 的 第 eid3 项 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    可变 损失 为 0。
    i 为 0。
    只要 i 小于 专家数：
        f_i 为 频率 的 第 i 项 乘 1000 除以 n。
        p_i 为 平均概率 的 第 i 项。
        损失 为 损失 加 f_i 乘 p_i 除以 1000。
        i 为 i 加 1。
    完。
    返还 损失 乘 专家数 除以 1000。
完。

容量上限 接受 总tokens、因子：
    返还 总tokens 乘 因子 除以 (专家数 乘 1000)。
完。

路由分析 接受 零参：
    可变 总 为 做 折叠 用 路由_计数器、0、以 s、x 映射到 s 加 x。
    若 总 等于 0：
        总 为 1。
    完。
    返还 做 映射 用 路由_计数器、以 c 映射到 c 乘 1000 除以 总。
完。

路由重置 接受 零参：
    可变 i 为 0。
    只要 i 小于 专家数：
        路由_计数器 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
完。

混合专家_专家总数 接受 零参：
    返还 专家数。
完。

本章 收束。
