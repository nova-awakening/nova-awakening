《模型架构》

引用《..·基础》。

模型配置_创建 接受 隐藏、层数、头数、词表、序列长、FFN倍：
    返还 一组：隐藏、层数、头数、词表、序列长、FFN倍。
完。

生成模型_小配置 接受 零参：
    返还 做 模型配置_创建 用 768、12、12、50257、1024、4。
完。

生成模型_中配置 接受 零参：
    返还 做 模型配置_创建 用 1024、24、16、50257、1024、4。
完。

大语言模型_7B配置 接受 零参：
    返还 做 模型配置_创建 用 4096、32、32、32000、2048、4。
完。

编码模型_基础配置 接受 零参：
    返还 做 模型配置_创建 用 768、12、12、30522、512、4。
完。

线性层_创建 接受 输入维度、输出维度：
    可变 w 为 一组：（空）。
    可变 b 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 输入维度 乘 输出维度：
        做 追加 用 w、(i 乘 7 加 13) 取余于 201 减 100。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 输出维度：
        做 追加 用 b、0。
        i 为 i 加 1。
    完。
    返还 一组：w、b、输入维度、输出维度。
完。

线性层_前向 接受 层、输入：
    可变 输出 为 一组：（空）。
    可变 o 为 0。
    只要 o 小于 层 的 第 3 项：
        可变 总和 为 层 的 第 1 项 的 第 o 项。
        可变 i 为 0。
        只要 i 小于 层 的 第 2 项：
            总和 为 总和 加 输入 的 第 i 项 乘 层 的 第 0 项 的 第 o 乘 层 的 第 2 项 加 i 项。
            i 为 i 加 1。
        完。
        做 追加 用 输出、总和。
        o 为 o 加 1。
    完。
    返还 输出。
完。

线性层_参数数 接受 层：
    返还 层 的 第 2 项 乘 层 的 第 3 项 加 层 的 第 3 项。
完。

FFN_前向 接受 输入、W1、W2：
    隐藏 为 做 线性层_前向 用 W1、输入。
    可变 i 为 0。
    只要 i 小于 做 计数 用 隐藏：
        若 隐藏 的 第 i 项 小于 0：
            隐藏 的 第 i 项 为 0。
        完。
        i 为 i 加 1。
    完。
    返还 做 线性层_前向 用 W2、隐藏。
完。

架构_层归一化 接受 输入：
    n 为 做 计数 用 输入。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 输入 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 n：
        d 为 输入 的 第 i 项 减 均值。
        方差 为 方差 加 d 乘 d。
        i 为 i 加 1。
    完。
    方差 为 方差 除以 n。
    可变 标准差 为 1。
    若 方差 大于 0：
        可变 t 为 方差。
        只要 t 大于 1：
            标准差 为 标准差 加 1。
            t 为 t 除以 4。
        完。
    完。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    可变 r 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 r、(输入 的 第 i 项 减 均值) 乘 100 除以 标准差。
        i 为 i 加 1。
    完。
    返还 r。
完。

注意力_单头 接受 Q、K、V、维度：
    序列 为 做 计数 用 Q。
    可变 得分列表 为 一组：（空）。
    可变 指数组 为 一组：（空）。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列：
        可变 j 为 0。
        只要 j 小于 序列：
            可变 点积 为 0。
            可变 d 为 0。
            只要 d 小于 维度：
                点积 为 点积 加 Q 的 第 i 项 的 第 d 项 乘 K 的 第 j 项 的 第 d 项。
                d 为 d 加 1。
            完。
            做 追加 用 得分列表、点积。
            j 为 j 加 1。
        完。
        可变 最大得分 为 得分列表 的 第 0 项。
        j 为 1。
        只要 j 小于 序列：
            若 得分列表 的 第 j 项 大于 最大得分：
                最大得分 为 得分列表 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        可变 合计 为 0。
        j 为 0。
        只要 j 小于 序列：
            可变 e 为 1000 加 (得分列表 的 第 j 项 减 最大得分)。
            若 e 小于 1：
                e 为 1。
            完。
            做 追加 用 指数组、e。
            合计 为 合计 加 e。
            j 为 j 加 1。
        完。
        可变 out_v 为 一组：（空）。
        可变 d2 为 0。
        只要 d2 小于 维度：
            可变 w 为 0。
            j 为 0。
            只要 j 小于 序列：
                w 为 w 加 指数组 的 第 j 项 乘 V 的 第 j 项 的 第 d2 项 除以 合计。
                j 为 j 加 1。
            完。
            做 追加 用 out_v、w。
            d2 为 d2 加 1。
        完。
        做 追加 用 输出、out_v。
        i 为 i 加 1。
    完。
    返还 输出。
完。

残差加 接受 a、b：
    返还 做 拉链 用 a、b、以 x、y 映射到 x 加 y。
完。

模型_随机态 为 12345。

模型_随机 接受 零参：
    模型_随机态 为 模型_随机态 位异或 模型_随机态 左移 13。
    模型_随机态 为 模型_随机态 位异或 模型_随机态 右移 7。
    模型_随机态 为 模型_随机态 位异或 模型_随机态 左移 17。
    若 模型_随机态 小于 0：
        模型_随机态 为 0 减 模型_随机态。
    完。
    返还 模型_随机态。
完。

可变 模型注册_名 为 一组：（空）。

可变 模型注册_配置 为 一组：（空）。

可变 模型注册_参数量 为 一组：（空）。

模型_注册 接受 名哈希、配置、参数量：
    做 追加 用 模型注册_名、名哈希。
    做 追加 用 模型注册_配置、配置。
    做 追加 用 模型注册_参数量、参数量。
完。

模型_参数量 接受 名哈希：
    可变 i 为 0。
    只要 i 小于 做 计数 用 模型注册_名：
        若 模型注册_名 的 第 i 项 等于 名哈希：
            返还 模型注册_参数量 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 0。
完。

模型_查找 接受 名哈希：
    可变 i 为 0。
    只要 i 小于 做 计数 用 模型注册_名：
        若 模型注册_名 的 第 i 项 等于 名哈希：
            返还 i。
        完。
        i 为 i 加 1。
    完。
    返还 负 1。
完。

模型_参数量估算 接受 配置：
    返还 12 乘 配置 的 第 1 项 乘 配置 的 第 0 项 乘 配置 的 第 0 项。
完。

模型_总数 接受 零参：
    返还 做 计数 用 模型注册_名。
完。

Transformer配置_创建 接受 隐藏维度、层数、头数、词表大小、最大长度：
    返还 一组：隐藏维度、层数、头数、词表大小、最大长度、隐藏维度 乘 4、隐藏维度 除以 头数。
完。

Transformer配置_参数量 接受 配置：
    d 为 配置 的 第 0 项。
    L 为 配置 的 第 1 项。
    V 为 配置 的 第 3 项。
    返还 V 乘 d 加 L 乘 (4 乘 d 乘 d 加 2 乘 d 乘 4 乘 d 加 4 乘 d) 加 d。
完。

Transformer配置_内存估算 接受 配置、批大小、序列长度：
    参数量 为 做 Transformer配置_参数量 用 配置。
    激活内存 为 批大小 乘 序列长度 乘 配置 的 第 0 项 乘 配置 的 第 1 项 乘 4。
    返还 参数量 乘 4 加 激活内存。
完。

MLP_创建 接受 输入维度、隐藏维度、输出维度：
    返还 一组：输入维度、隐藏维度、输出维度、输入维度 乘 隐藏维度 加 隐藏维度 乘 输出维度。
完。

MLP_参数量 接受 层：
    返还 层 的 第 3 项。
完。

残差_连接 接受 输入、子层输出：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 输入：
        做 追加 用 结果、输入 的 第 i 项 加 子层输出 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模型架构_层归一化 接受 向量：
    n 为 做 计数 用 向量。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 向量 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 n：
        d 为 向量 的 第 i 项 减 均值。
        方差 为 方差 加 d 乘 d。
        i 为 i 加 1。
    完。
    方差 为 方差 除以 n。
    若 方差 等于 0：
        方差 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、(向量 的 第 i 项 减 均值) 乘 1000 除以 (方差 加 1)。
        i 为 i 加 1。
    完。
    返还 结果。
完。

模型_堆叠层 接受 层列表、输入：
    可变 当前 为 输入。
    可变 i 为 0。
    只要 i 小于 做 计数 用 层列表：
        当前 为 做 残差_连接 用 当前、做 模型架构_层归一化 用 当前。
        i 为 i 加 1。
    完。
    返还 当前。
完。

模型_比较参数量 接受 配置A、配置B：
    pA 为 做 Transformer配置_参数量 用 配置A。
    pB 为 做 Transformer配置_参数量 用 配置B。
    若 pA 大于 pB：
        返还 1。
    完。
    若 pA 小于 pB：
        返还 负 1。
    完。
    返还 0。
完。

本章 收束。
