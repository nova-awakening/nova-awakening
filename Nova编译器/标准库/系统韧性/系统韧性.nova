《系统韧性》

引用《..·基础》。

可变 健康_指标 为 一组：（空）。

可变 健康_阈值 为 一组：（空）。

可变 健康_名称 为 一组：（空）。

可变 健康_数量 为 0。

可变 健康_历史 为 一组：（空）。

健康_注册 接受 名称、阈值：
    做 追加 用 健康_指标、1000。
    做 追加 用 健康_阈值、阈值。
    做 追加 用 健康_名称、名称。
    做 追加 用 健康_历史、1000。
    健康_数量 为 健康_数量 加 1。
    返还 健康_数量 减 1。
完。

健康_更新 接受 id、值：
    健康_历史 的 第 id 项 为 健康_指标 的 第 id 项。
    健康_指标 的 第 id 项 为 值。
完。

健康_检查 接受 id：
    返还 健康_指标 的 第 id 项 大于或等于 健康_阈值 的 第 id 项。
完。

健康_总体 接受 零参：
    可变 i 为 0。
    只要 i 小于 健康_数量：
        若 健康_指标 的 第 i 项 小于 健康_阈值 的 第 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

健康_得分 接受 零参：
    若 健康_数量 等于 0：
        返还 0。
    完。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 健康_数量：
        总 为 总 加 健康_指标 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总 除以 健康_数量。
完。

健康_趋势 接受 id：
    返还 健康_指标 的 第 id 项 减 健康_历史 的 第 id 项。
完。

健康_不健康列表 接受 零参：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 健康_数量：
        若 健康_指标 的 第 i 项 小于 健康_阈值 的 第 i 项：
            做 追加 用 结果、i。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 熔断_状态 为 0。

可变 熔断_失败计数 为 0。

可变 熔断_阈值 为 5。

可变 熔断_冷却 为 0。

可变 熔断_冷却时间 为 10。

可变 熔断_总熔断次数 为 0。

熔断器_初始化 接受 阈值、冷却：
    熔断_状态 为 0。
    熔断_失败计数 为 0。
    熔断_阈值 为 阈值。
    熔断_冷却 为 0。
    熔断_冷却时间 为 冷却。
    熔断_总熔断次数 为 0。
完。

熔断器_允许 接受 零参：
    若 熔断_状态 等于 0：
        返还 真。
    完。
    若 熔断_状态 等于 2：
        返还 真。
    完。
    若 熔断_冷却 大于或等于 熔断_冷却时间：
        熔断_状态 为 2。
        返还 真。
    完。
    熔断_冷却 为 熔断_冷却 加 1。
    返还 假。
完。

熔断器_成功 接受 零参：
    若 熔断_状态 等于 2：
        熔断_状态 为 0。
    完。
    熔断_失败计数 为 0。
完。

熔断器_失败 接受 零参：
    熔断_失败计数 为 熔断_失败计数 加 1。
    若 熔断_失败计数 大于或等于 熔断_阈值：
        熔断_状态 为 1。
        熔断_冷却 为 0。
        熔断_总熔断次数 为 熔断_总熔断次数 加 1。
    完。
完。

重试_固定延迟 接受 次数、延迟ms：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 次数：
        做 追加 用 结果、延迟ms。
        i 为 i 加 1。
    完。
    返还 结果。
完。

重试_指数退避 接受 次数、基础ms、最大ms：
    可变 结果 为 一组：（空）。
    可变 延迟 为 基础ms。
    可变 i 为 0。
    只要 i 小于 次数：
        若 延迟 大于 最大ms：
            延迟 为 最大ms。
        完。
        做 追加 用 结果、延迟。
        延迟 为 延迟 乘 2。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 _韧性_种子 为 42。

重试_抖动退避 接受 次数、基础ms、最大ms：
    可变 结果 为 一组：（空）。
    可变 延迟 为 基础ms。
    可变 i 为 0。
    只要 i 小于 次数：
        若 延迟 大于 最大ms：
            延迟 为 最大ms。
        完。
        _韧性_种子 为 (_韧性_种子 乘 1103515245 加 12345) 取余于 2147483647。
        jitter 为 _韧性_种子 取余于 (延迟 除以 2 加 1)。
        做 追加 用 结果、延迟 加 jitter。
        延迟 为 延迟 乘 2。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 桶_令牌 为 0。

可变 韧性_桶容量 为 100。

可变 桶_速率 为 10。

限流_初始化 接受 容量、速率：
    桶_令牌 为 容量。
    韧性_桶容量 为 容量。
    桶_速率 为 速率。
完。

限流_补充 接受 零参：
    桶_令牌 为 桶_令牌 加 桶_速率。
    若 桶_令牌 大于 韧性_桶容量：
        桶_令牌 为 韧性_桶容量。
    完。
完。

限流_请求 接受 消耗：
    若 桶_令牌 大于或等于 消耗：
        桶_令牌 为 桶_令牌 减 消耗。
        返还 真。
    完。
    返还 假。
完。

可变 窗口_请求 为 一组：（空）。

可变 窗口_限制 为 10。

滑窗限流_初始化 接受 限制：
    窗口_请求 为 一组：（空）。
    窗口_限制 为 限制。
完。

滑窗限流_请求 接受 时间戳、窗口大小：
    可变 新列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 窗口_请求：
        若 窗口_请求 的 第 i 项 大于或等于 时间戳 减 窗口大小：
            做 追加 用 新列表、窗口_请求 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    窗口_请求 为 新列表。
    若 做 计数 用 窗口_请求 大于或等于 窗口_限制：
        返还 假。
    完。
    做 追加 用 窗口_请求、时间戳。
    返还 真。
完。

可变 隔离_列表 为 一组：（空）。

隔离_追加 接受 组件id：
    做 追加 用 隔离_列表、组件id。
完。

隔离_检查 接受 组件id：
    可变 i 为 0。
    只要 i 小于 做 计数 用 隔离_列表：
        若 隔离_列表 的 第 i 项 等于 组件id：
            返还 真。
        完。
        i 为 i 加 1。
    完。
    返还 假。
完。

隔离_移除 接受 组件id：
    可变 新列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 隔离_列表：
        若 隔离_列表 的 第 i 项 不等于 组件id：
            做 追加 用 新列表、隔离_列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    隔离_列表 为 新列表。
完。

可变 舱壁_使用 为 一组：（空）。

可变 舱壁_限制 为 一组：（空）。

可变 舱壁_数量 为 0。

舱壁_注册 接受 限制：
    做 追加 用 舱壁_使用、0。
    做 追加 用 舱壁_限制、限制。
    舱壁_数量 为 舱壁_数量 加 1。
    返还 舱壁_数量 减 1。
完。

舱壁_获取 接受 id：
    若 舱壁_使用 的 第 id 项 小于 舱壁_限制 的 第 id 项：
        舱壁_使用 的 第 id 项 为 舱壁_使用 的 第 id 项 加 1。
        返还 真。
    完。
    返还 假。
完。

舱壁_释放 接受 id：
    若 舱壁_使用 的 第 id 项 大于 0：
        舱壁_使用 的 第 id 项 为 舱壁_使用 的 第 id 项 减 1。
    完。
完。

可变 降级_级别 为 0。

降级_设置 接受 级别：
    降级_级别 为 级别。
完。

降级_获取 接受 零参：
    返还 降级_级别。
完。

降级_自动评估 接受 零参：
    得分 为 做 健康_得分。
    若 得分 大于或等于 800：
        降级_级别 为 0。
    否则：
        若 得分 大于或等于 500：
            降级_级别 为 1。
        否则：
            若 得分 大于或等于 200：
                降级_级别 为 2。
            否则：
                降级_级别 为 3。
            完。
        完。
    完。
    返还 降级_级别。
完。

可变 韧性_检查点数据 为 一组：（空）。

检查点_恢复 接受 零参：
    返还 韧性_检查点数据。
完。

本章 收束。
