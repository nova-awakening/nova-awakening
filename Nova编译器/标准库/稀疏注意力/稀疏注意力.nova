《稀疏注意力》

引用《..·基础》。

滑动窗口_掩码 接受 序列长度、窗口大小：
    半窗 为 窗口大小 除以 2。
    可变 掩码 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 序列长度：
            可变 距离 为 i 减 j。
            若 距离 小于 0：
                距离 为 0 减 距离。
            完。
            若 距离 小于或等于 半窗：
                做 追加 用 行、1。
            否则：
                做 追加 用 行、0。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 掩码、行。
        i 为 i 加 1。
    完。
    返还 掩码。
完。

滑动窗口_注意力 接受 Q、K、V、窗口大小、序列长度、维度：
    可变 指数组 为 一组：（空）。
    半窗 为 窗口大小 除以 2。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 起始 为 i 减 半窗。
        若 起始 小于 0：
            起始 为 0。
        完。
        可变 结束 为 i 加 半窗 加 1。
        若 结束 大于 序列长度：
            结束 为 序列长度。
        完。
        可变 分数 为 一组：（空）。
        可变 j 为 起始。
        只要 j 小于 结束：
            可变 s 为 0。
            可变 d 为 0。
            只要 d 小于 维度：
                s 为 s 加 Q 的 第 i 乘 维度 加 d 项 乘 K 的 第 j 乘 维度 加 d 项 除以 1000。
                d 为 d 加 1。
            完。
            做 追加 用 分数、s。
            j 为 j 加 1。
        完。
        可变 最大得分 为 分数 的 第 0 项。
        可变 k 为 1。
        只要 k 小于 做 计数 用 分数：
            若 分数 的 第 k 项 大于 最大得分：
                最大得分 为 分数 的 第 k 项。
            完。
            k 为 k 加 1。
        完。
        可变 总 为 0。
        k 为 0。
        只要 k 小于 做 计数 用 分数：
            可变 e 为 1000 加 (分数 的 第 k 项 减 最大得分)。
            若 e 小于 1：
                e 为 1。
            完。
            做 追加 用 指数组、e。
            总 为 总 加 e。
            k 为 k 加 1。
        完。
        若 总 等于 0：
            总 为 1。
        完。
        可变 行输出 为 一组：（空）。
        可变 d 为 0。
        只要 d 小于 维度：
            做 追加 用 行输出、0。
            d 为 d 加 1。
        完。
        j 为 起始。
        k 为 0。
        只要 j 小于 结束：
            α 为 指数组 的 第 k 项 乘 1000 除以 总。
            d 为 0。
            只要 d 小于 维度：
                行输出 的 第 d 项 为 行输出 的 第 d 项 加 α 乘 V 的 第 j 乘 维度 加 d 项 除以 1000。
                d 为 d 加 1。
            完。
            j 为 j 加 1。
            k 为 k 加 1。
        完。
        做 追加 用 输出、行输出。
        i 为 i 加 1。
    完。
    返还 输出。
完。

全局token_掩码 接受 序列长度、全局位置列表：
    可变 掩码 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 序列长度：
            做 追加 用 行、0。
            j 为 j 加 1。
        完。
        可变 是全局 为 0。
        可变 g 为 0。
        只要 g 小于 做 计数 用 全局位置列表：
            若 全局位置列表 的 第 g 项 等于 i：
                是全局 为 1。
            完。
            g 为 g 加 1。
        完。
        若 是全局 等于 1：
            j 为 0。
            只要 j 小于 序列长度：
                行 的 第 j 项 为 1。
                j 为 j 加 1。
            完。
        完。
        g 为 0。
        只要 g 小于 做 计数 用 全局位置列表：
            行 的 第 全局位置列表 的 第 g 项 项 为 1。
            g 为 g 加 1。
        完。
        做 追加 用 掩码、行。
        i 为 i 加 1。
    完。
    返还 掩码。
完。

SA_随机态 为 33333。

SA_随机 接受 零参：
    SA_随机态 为 SA_随机态 位异或 SA_随机态 左移 13。
    SA_随机态 为 SA_随机态 位异或 SA_随机态 右移 7。
    SA_随机态 为 SA_随机态 位异或 SA_随机态 左移 17。
    若 SA_随机态 小于 0：
        SA_随机态 为 0 减 SA_随机态。
    完。
    返还 SA_随机态。
完。

BigBird_掩码 接受 序列长度、窗口大小、全局数、随机数：
    窗口掩码 为 做 滑动窗口_掩码 用 序列长度、窗口大小。
    可变 全局位置 为 一组：（空）。
    可变 g 为 0。
    只要 g 小于 全局数 并且 g 小于 序列长度：
        做 追加 用 全局位置、g。
        g 为 g 加 1。
    完。
    全局掩码 为 做 全局token_掩码 用 序列长度、全局位置。
    可变 最终掩码 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 序列长度：
            若 窗口掩码 的 第 i 项 的 第 j 项 等于 1 或者 全局掩码 的 第 i 项 的 第 j 项 等于 1：
                做 追加 用 行、1。
            否则：
                做 追加 用 行、0。
            完。
            j 为 j 加 1。
        完。
        可变 r 为 0。
        只要 r 小于 随机数：
            位置 为 做 SA_随机 取余于 序列长度。
            行 的 第 位置 项 为 1。
            r 为 r 加 1。
        完。
        做 追加 用 最终掩码、行。
        i 为 i 加 1。
    完。
    返还 最终掩码。
完。

块稀疏_注意力 接受 Q、K、V、块大小、序列长度、维度：
    可变 指数组 为 一组：（空）。
    块数 为 (序列长度 加 块大小 减 1) 除以 块大小。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列长度：
        做 追加 用 输出、一组：（空）。
        i 为 i 加 1。
    完。
    可变 b 为 0。
    只要 b 小于 块数：
        块起 为 b 乘 块大小。
        可变 块终 为 块起 加 块大小。
        若 块终 大于 序列长度：
            块终 为 序列长度。
        完。
        可变 qi 为 块起。
        只要 qi 小于 块终：
            可变 分数 为 一组：（空）。
            可变 kj 为 块起。
            只要 kj 小于 块终：
                可变 s 为 0。
                可变 d 为 0。
                只要 d 小于 维度：
                    s 为 s 加 Q 的 第 qi 乘 维度 加 d 项 乘 K 的 第 kj 乘 维度 加 d 项 除以 1000。
                    d 为 d 加 1。
                完。
                做 追加 用 分数、s。
                kj 为 kj 加 1。
            完。
            可变 最大得分 为 分数 的 第 0 项。
            可变 k 为 1。
            只要 k 小于 做 计数 用 分数：
                若 分数 的 第 k 项 大于 最大得分：
                    最大得分 为 分数 的 第 k 项。
                完。
                k 为 k 加 1。
            完。
            可变 总 为 0。
            k 为 0。
            只要 k 小于 做 计数 用 分数：
                可变 e 为 1000 加 (分数 的 第 k 项 减 最大得分)。
                若 e 小于 1：
                    e 为 1。
                完。
                做 追加 用 指数组、e。
                总 为 总 加 e。
                k 为 k 加 1。
            完。
            若 总 等于 0：
                总 为 1。
            完。
            可变 行输出 为 一组：（空）。
            可变 d 为 0。
            只要 d 小于 维度：
                做 追加 用 行输出、0。
                d 为 d 加 1。
            完。
            kj 为 块起。
            k 为 0。
            只要 kj 小于 块终：
                α 为 指数组 的 第 k 项 乘 1000 除以 总。
                d 为 0。
                只要 d 小于 维度：
                    行输出 的 第 d 项 为 行输出 的 第 d 项 加 α 乘 V 的 第 kj 乘 维度 加 d 项 除以 1000。
                    d 为 d 加 1。
                完。
                kj 为 kj 加 1。
                k 为 k 加 1。
            完。
            输出 的 第 qi 项 为 行输出。
            qi 为 qi 加 1。
        完。
        b 为 b 加 1。
    完。
    返还 输出。
完。

稀疏注意_线性注意力 接受 Q、K、V、序列长度、维度：
    可变 输出 为 一组：（空）。
    可变 KV累积 为 一组：（空）。
    可变 K累积 为 一组：（空）。
    可变 d1 为 0。
    只要 d1 小于 维度：
        可变 行 为 一组：（空）。
        可变 d2 为 0。
        只要 d2 小于 维度：
            做 追加 用 行、0。
            d2 为 d2 加 1。
        完。
        做 追加 用 KV累积、行。
        做 追加 用 K累积、0。
        d1 为 d1 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 d 为 0。
        只要 d 小于 维度：
            可变 k_val 为 K 的 第 i 乘 维度 加 d 项。
            若 k_val 小于 0：
                k_val 为 k_val 除以 4。
            完。
            k_val 为 k_val 加 1000。
            可变 d2 为 0。
            只要 d2 小于 维度：
                KV累积 的 第 d 项 的 第 d2 项 为 KV累积 的 第 d 项 的 第 d2 项 加 k_val 乘 V 的 第 i 乘 维度 加 d2 项 除以 1000。
                d2 为 d2 加 1。
            完。
            K累积 的 第 d 项 为 K累积 的 第 d 项 加 k_val。
            d 为 d 加 1。
        完。
        可变 行输出 为 一组：（空）。
        可变 归一化 为 0。
        d 为 0。
        只要 d 小于 维度：
            可变 q_val 为 Q 的 第 i 乘 维度 加 d 项。
            若 q_val 小于 0：
                q_val 为 q_val 除以 4。
            完。
            q_val 为 q_val 加 1000。
            归一化 为 归一化 加 q_val 乘 K累积 的 第 d 项 除以 1000。
            d 为 d 加 1。
        完。
        若 归一化 等于 0：
            归一化 为 1。
        完。
        d 为 0。
        只要 d 小于 维度：
            可变 数值 为 0。
            可变 d2 为 0。
            只要 d2 小于 维度：
                可变 q_val 为 Q 的 第 i 乘 维度 加 d2 项。
                若 q_val 小于 0：
                    q_val 为 q_val 除以 4。
                完。
                q_val 为 q_val 加 1000。
                数值 为 数值 加 q_val 乘 KV累积 的 第 d2 项 的 第 d 项 除以 1000。
                d2 为 d2 加 1。
            完。
            做 追加 用 行输出、数值 乘 1000 除以 归一化。
            d 为 d 加 1。
        完。
        做 追加 用 输出、行输出。
        i 为 i 加 1。
    完。
    返还 输出。
完。

膨胀_掩码 接受 序列长度、膨胀率：
    可变 掩码 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 序列长度：
            若 (i 减 j) 取余于 膨胀率 等于 0 并且 j 小于或等于 i：
                做 追加 用 行、1。
            否则：
                做 追加 用 行、0。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 掩码、行。
        i 为 i 加 1。
    完。
    返还 掩码。
完。

注意力_稀疏度 接受 掩码、序列长度：
    可变 总 为 0。
    可变 非零 为 0。
    可变 i 为 0。
    只要 i 小于 序列长度：
        可变 j 为 0。
        只要 j 小于 序列长度：
            总 为 总 加 1。
            若 掩码 的 第 i 项 的 第 j 项 等于 1：
                非零 为 非零 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 总 等于 0：
        返还 0。
    完。
    返还 1000 减 非零 乘 1000 除以 总。
完。

本章 收束。
