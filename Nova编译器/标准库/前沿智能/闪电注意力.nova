《闪电注意力》

引用《..·基础》。

引用《..·数学·工具》。

FA精度 为 10000。

在线归一化指数_初始化 接受 零参：
    返还 一组：0 减 FA精度 乘 100。
完。

在线归一化指数_更新 接受 状态、新值：
    旧max 为 状态 的 第 0 项。
    旧sum 为 状态 的 第 1 项。
    可变 新max 为 旧max。
    若 新值 大于 旧max：
        新max 为 新值。
    完。
    可变 修正因子 为 FA精度。
    若 新max 不等于 旧max：
        差 为 旧max 减 新max。
        修正因子 为 做 近似exp_FA 用 差。
    完。
    新sum 为 旧sum 乘 修正因子 除以 FA精度 加 做 近似exp_FA 用 新值 减 新max。
    返还 一组：新max、新sum、状态 的 第 2 项 加 1。
完。

近似exp_FA 接受 x：
    若 x 大于 60000：
        返还 FA精度 乘 100。
    完。
    若 x 小于 0 减 60000：
        返还 0。
    完。
    x1 为 x。
    x2 为 x1 乘 x1 除以 FA精度。
    x3 为 x2 乘 x1 除以 FA精度。
    返还 FA精度 加 x1 加 x2 除以 2 加 x3 除以 6。
完。

闪电注意力_分块 接受 Q、K、V、序列长、头维度、块大小：
    可变 dot 为 0。
    可变 ri 为 0。
    可变 O 为 一组：（空）。
    只要 oi 小于 序列长 乘 头维度：
        做 追加 用 O、0。
        oi 为 oi 加 1。
    完。
    可变 行max 为 一组：（空）。
    可变 行sum 为 一组：（空）。
    只要 ri 小于 序列长：
        做 追加 用 行max、0 减 FA精度 乘 100。
        做 追加 用 行sum、0。
        ri 为 ri 加 1。
    完。
    Kv块数 为 (序列长 加 块大小 减 1) 除以 块大小。
    可变 jb 为 0。
    只要 jb 小于 Kv块数：
        j起 为 jb 乘 块大小。
        可变 j止 为 j起 加 块大小。
        若 j止 大于 序列长：
            j止 为 序列长。
        完。
        可变 qi 为 0。
        只要 qi 小于 序列长：
            可变 旧max 为 行max 的 第 qi 项。
            可变 旧sum 为 行sum 的 第 qi 项。
            可变 块max 为 旧max。
            可变 S块 为 一组：（空）。
            可变 j 为 j起。
            只要 j 小于 j止：
                可变 d 为 0。
                只要 d 小于 头维度：
                    dot 为 dot 加 Q 的 第 qi 乘 头维度 加 d 项 乘 K 的 第 j 乘 头维度 加 d 项 除以 FA精度。
                    d 为 d 加 1。
                完。
                dot 为 dot 乘 FA精度 除以 (头维度 乘 100 加 1)。
                做 追加 用 S块、dot。
                若 dot 大于 块max：
                    块max 为 dot。
                完。
                j 为 j 加 1。
            完。
            修正 为 做 近似exp_FA 用 旧max 减 块max。
            可变 新sum 为 旧sum 乘 修正 除以 FA精度。
            j 为 0。
            只要 j 小于 做 计数 用 S块：
                p 为 做 近似exp_FA 用 S块 的 第 j 项 减 块max。
                新sum 为 新sum 加 p。
                可变 d 为 0。
                只要 d 小于 头维度：
                    idx 为 qi 乘 头维度 加 d。
                    O 的 第 idx 项 为 O 的 第 idx 项 乘 修正 除以 FA精度 加 p 乘 V 的 第 (j起 加 j) 乘 头维度 加 d 项 除以 FA精度。
                    d 为 d 加 1。
                完。
                j 为 j 加 1。
            完。
            行max 的 第 qi 项 为 块max。
            行sum 的 第 qi 项 为 新sum。
            qi 为 qi 加 1。
        完。
        jb 为 jb 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 序列长：
        若 行sum 的 第 i 项 大于 0：
            可变 d 为 0。
            只要 d 小于 头维度：
                O 的 第 i 乘 头维度 加 d 项 为 O 的 第 i 乘 头维度 加 d 项 乘 FA精度 除以 行sum 的 第 i 项。
                d 为 d 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 O。
完。

闪电注意力_因果 接受 Q、K、V、序列长、头维度、块大小：
    可变 dot 为 0。
    可变 ri 为 0。
    可变 O 为 一组：（空）。
    只要 oi 小于 序列长 乘 头维度：
        做 追加 用 O、0。
        oi 为 oi 加 1。
    完。
    可变 行max 为 一组：（空）。
    可变 行sum 为 一组：（空）。
    只要 ri 小于 序列长：
        做 追加 用 行max、0 减 FA精度 乘 100。
        做 追加 用 行sum、0。
        ri 为 ri 加 1。
    完。
    Kv块数 为 (序列长 加 块大小 减 1) 除以 块大小。
    可变 jb 为 0。
    只要 jb 小于 Kv块数：
        j起 为 jb 乘 块大小。
        可变 j止 为 j起 加 块大小。
        若 j止 大于 序列长：
            j止 为 序列长。
        完。
        可变 qi 为 0。
        只要 qi 小于 序列长：
            可变 旧max 为 行max 的 第 qi 项。
            可变 旧sum 为 行sum 的 第 qi 项。
            可变 块max 为 旧max。
            可变 S块 为 一组：（空）。
            可变 j 为 j起。
            只要 j 小于 j止：
                若 j 大于 qi：
                    做 追加 用 S块、0 减 FA精度 乘 100。
                否则：
                    可变 d 为 0。
                    只要 d 小于 头维度：
                        dot 为 dot 加 Q 的 第 qi 乘 头维度 加 d 项 乘 K 的 第 j 乘 头维度 加 d 项 除以 FA精度。
                        d 为 d 加 1。
                    完。
                    dot 为 dot 乘 FA精度 除以 (头维度 乘 100 加 1)。
                    做 追加 用 S块、dot。
                    若 dot 大于 块max：
                        块max 为 dot。
                    完。
                完。
                j 为 j 加 1。
            完。
            修正 为 做 近似exp_FA 用 旧max 减 块max。
            可变 新sum 为 旧sum 乘 修正 除以 FA精度。
            j 为 0。
            只要 j 小于 做 计数 用 S块：
                p 为 做 近似exp_FA 用 S块 的 第 j 项 减 块max。
                新sum 为 新sum 加 p。
                可变 d 为 0。
                只要 d 小于 头维度：
                    idx 为 qi 乘 头维度 加 d。
                    O 的 第 idx 项 为 O 的 第 idx 项 乘 修正 除以 FA精度 加 p 乘 V 的 第 (j起 加 j) 乘 头维度 加 d 项 除以 FA精度。
                    d 为 d 加 1。
                完。
                j 为 j 加 1。
            完。
            行max 的 第 qi 项 为 块max。
            行sum 的 第 qi 项 为 新sum。
            qi 为 qi 加 1。
        完。
        jb 为 jb 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 序列长：
        若 行sum 的 第 i 项 大于 0：
            可变 d 为 0。
            只要 d 小于 头维度：
                O 的 第 i 乘 头维度 加 d 项 为 O 的 第 i 乘 头维度 加 d 项 乘 FA精度 除以 行sum 的 第 i 项。
                d 为 d 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 O。
完。

MLA_压缩KV 接受 输入、W_DKV、d_模型、d_潜：
    可变 潜向量 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d_潜：
        可变 dot 为 0。
        可变 j 为 0。
        只要 j 小于 d_模型：
            dot 为 dot 加 W_DKV 的 第 i 乘 d_模型 加 j 项 乘 输入 的 第 j 项 除以 FA精度。
            j 为 j 加 1。
        完。
        做 追加 用 潜向量、dot。
        i 为 i 加 1。
    完。
    返还 潜向量。
完。

MLA_恢复KV 接受 潜向量、W_UK、W_UV、d_潜、头数、头维度：
    d_kv 为 头数 乘 头维度。
    可变 K 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d_kv：
        可变 dot 为 0。
        可变 j 为 0。
        只要 j 小于 d_潜：
            dot 为 dot 加 W_UK 的 第 i 乘 d_潜 加 j 项 乘 潜向量 的 第 j 项 除以 FA精度。
            j 为 j 加 1。
        完。
        做 追加 用 K、dot。
        i 为 i 加 1。
    完。
    可变 V 为 一组：（空）。
    i 为 0。
    只要 i 小于 d_kv：
        可变 dot 为 0。
        可变 j 为 0。
        只要 j 小于 d_潜：
            dot 为 dot 加 W_UV 的 第 i 乘 d_潜 加 j 项 乘 潜向量 的 第 j 项 除以 FA精度。
            j 为 j 加 1。
        完。
        做 追加 用 V、dot。
        i 为 i 加 1。
    完。
    返还 一组：K、V。
完。

MLA_注意力 接受 Q列表、潜向量列表、W_UK、W_UV、序列长、d_潜、头维度：
    可变 K全 为 一组：（空）。
    可变 V全 为 一组：（空）。
    可变 t 为 0。
    只要 t 小于 序列长：
        可变 潜 为 一组：（空）。
        可变 li 为 0。
        只要 li 小于 d_潜：
            做 追加 用 潜、潜向量列表 的 第 t 乘 d_潜 加 li 项。
            li 为 li 加 1。
        完。
        KV 为 做 MLA_恢复KV 用 潜、W_UK、W_UV、d_潜、1、头维度。
        可变 ki 为 0。
        只要 ki 小于 头维度：
            做 追加 用 K全、KV 的 第 0 项 的 第 ki 项。
            ki 为 ki 加 1。
        完。
        可变 vi 为 0。
        只要 vi 小于 头维度：
            做 追加 用 V全、KV 的 第 1 项 的 第 vi 项。
            vi 为 vi 加 1。
        完。
        t 为 t 加 1。
    完。
    返还 做 闪电注意力_分块 用 Q列表、K全、V全、序列长、头维度、16。
完。

差分注意力_计算 接受 Q、K、V、序列长、头维度、λ参数：
    半维度 为 头维度 除以 2。
    可变 O 为 一组：（空）。
    可变 oi 为 0。
    只要 oi 小于 序列长 乘 头维度：
        做 追加 用 O、0。
        oi 为 oi 加 1。
    完。
    可变 qi 为 0。
    只要 qi 小于 序列长：
        可变 A1 为 一组：（空）。
        可变 A2 为 一组：（空）。
        可变 max1 为 0 减 FA精度 乘 100。
        可变 max2 为 0 减 FA精度 乘 100。
        可变 kj 为 0。
        只要 kj 小于 序列长：
            可变 dot1 为 0。
            可变 d 为 0。
            只要 d 小于 半维度：
                dot1 为 dot1 加 Q 的 第 qi 乘 头维度 加 d 项 乘 K 的 第 kj 乘 头维度 加 d 项 除以 FA精度。
                d 为 d 加 1。
            完。
            dot1 为 dot1 乘 FA精度 除以 (半维度 乘 100 加 1)。
            做 追加 用 A1、dot1。
            若 dot1 大于 max1：
                max1 为 dot1。
            完。
            可变 dot2 为 0。
            d 为 0。
            只要 d 小于 半维度：
                dot2 为 dot2 加 Q 的 第 qi 乘 头维度 加 半维度 加 d 项 乘 K 的 第 kj 乘 头维度 加 半维度 加 d 项 除以 FA精度。
                d 为 d 加 1。
            完。
            dot2 为 dot2 乘 FA精度 除以 (半维度 乘 100 加 1)。
            做 追加 用 A2、dot2。
            若 dot2 大于 max2：
                max2 为 dot2。
            完。
            kj 为 kj 加 1。
        完。
        可变 sum1 为 0。
        kj 为 0。
        只要 kj 小于 序列长：
            e1 为 做 近似exp_FA 用 A1 的 第 kj 项 减 max1。
            A1 的 第 kj 项 为 e1。
            sum1 为 sum1 加 e1。
            kj 为 kj 加 1。
        完。
        可变 sum2 为 0。
        kj 为 0。
        只要 kj 小于 序列长：
            e2 为 做 近似exp_FA 用 A2 的 第 kj 项 减 max2。
            A2 的 第 kj 项 为 e2。
            sum2 为 sum2 加 e2。
            kj 为 kj 加 1。
        完。
        可变 d 为 0。
        只要 d 小于 头维度：
            可变 val 为 0。
            kj 为 0。
            只要 kj 小于 序列长：
                w1 为 A1 的 第 kj 项 乘 FA精度 除以 (sum1 加 1)。
                w2 为 A2 的 第 kj 项 乘 FA精度 除以 (sum2 加 1)。
                差分权重 为 w1 减 λ参数 乘 w2 除以 FA精度。
                val 为 val 加 差分权重 乘 V 的 第 kj 乘 头维度 加 d 项 除以 FA精度。
                kj 为 kj 加 1。
            完。
            O 的 第 qi 乘 头维度 加 d 项 为 val。
            d 为 d 加 1。
        完。
        qi 为 qi 加 1。
    完。
    返还 O。
完。

差分注意力_初始λ 接受 层号：
    指数项 为 做 近似exp_FA 用 0 减 3000 乘 (层号 减 1) 除以 FA精度。
    返还 8000 减 6000 乘 指数项 除以 FA精度。
完。

本章 收束。
