《状态空间模型》

引用《..·基础》。

引用《..·数学·工具》。

SSM精度 为 10000。

S4_创建 接受 状态维度、输入维度：
    可变 A 为 一组：（空）。
    可变 n 为 0。
    只要 n 小于 状态维度：
        可变 k 为 0。
        只要 k 小于 状态维度：
            若 n 大于 k：
                值 为 0 减 (2 乘 n 加 1) 乘 (2 乘 k 加 1) 除以 2。
                做 追加 用 A、值。
            否则若 n 等于 k：
                做 追加 用 A、0 减 (n 加 1)。
            否则：
                做 追加 用 A、0。
            完。
            k 为 k 加 1。
        完。
        n 为 n 加 1。
    完。
    可变 B 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 状态维度 乘 输入维度：
        做 追加 用 B、(2 乘 (i 取余于 状态维度) 加 1) 乘 100。
        i 为 i 加 1。
    完。
    可变 C 为 一组：（空）。
    i 为 0。
    只要 i 小于 输入维度 乘 状态维度：
        做 追加 用 C、(i 乘 2654435761 加 77) 取余于 2000 减 1000。
        i 为 i 加 1。
    完。
    Δ 为 100。
    返还 一组：状态维度、输入维度、A、B、C、Δ。
完。

S4_前向 接受 模型、输入序列、序列长：
    N 为 模型 的 第 0 项。
    D 为 模型 的 第 1 项。
    A 为 模型 的 第 2 项。
    B 为 模型 的 第 3 项。
    C 为 模型 的 第 4 项。
    Δ 为 模型 的 第 5 项。
    可变 x 为 一组：（空）。
    可变 xi 为 0。
    只要 xi 小于 N：
        做 追加 用 x、0。
        xi 为 xi 加 1。
    完。
    可变 输出 为 一组：（空）。
    可变 t 为 0。
    只要 t 小于 序列长：
        可变 新x 为 一组：（空）。
        可变 n 为 0。
        只要 n 小于 N：
            可变 状态值 为 0。
            可变 k 为 0。
            只要 k 小于 N：
                a_nk 为 A 的 第 n 乘 N 加 k 项。
                若 n 等于 k：
                    状态值 为 状态值 加 (SSM精度 加 Δ 乘 a_nk 除以 SSM精度) 乘 x 的 第 k 项 除以 SSM精度。
                否则：
                    状态值 为 状态值 加 Δ 乘 a_nk 乘 x 的 第 k 项 除以 (SSM精度 乘 SSM精度 除以 SSM精度)。
                完。
                k 为 k 加 1。
            完。
            u 为 输入序列 的 第 t 项。
            状态值 为 状态值 加 Δ 乘 B 的 第 n 项 乘 u 除以 (SSM精度 乘 SSM精度 除以 SSM精度)。
            做 追加 用 新x、状态值。
            n 为 n 加 1。
        完。
        x 为 新x。
        可变 y 为 0。
        n 为 0。
        只要 n 小于 N：
            y 为 y 加 C 的 第 n 项 乘 x 的 第 n 项 除以 SSM精度。
            n 为 n 加 1。
        完。
        做 追加 用 输出、y。
        t 为 t 加 1。
    完。
    返还 输出。
完。

Mamba_创建 接受 模型维度、状态维度、扩展因子：
    内部维度 为 模型维度 乘 扩展因子。
    可变 W_Δ 为 一组：（空）。
    可变 W_B 为 一组：（空）。
    可变 W_C 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 内部维度：
        做 追加 用 W_Δ、(i 乘 2654435761 加 1) 取余于 200 减 100。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 内部维度 乘 状态维度：
        做 追加 用 W_B、(i 乘 1103515245 加 3) 取余于 200 减 100。
        做 追加 用 W_C、(i 乘 1103515245 加 7) 取余于 200 减 100。
        i 为 i 加 1。
    完。
    可变 A_log 为 一组：（空）。
    i 为 0。
    只要 i 小于 状态维度：
        做 追加 用 A_log、0 减 (i 加 1) 乘 100。
        i 为 i 加 1。
    完。
    返还 一组：模型维度、状态维度、扩展因子、内部维度、A_log、W_Δ、W_B、W_C。
完。

柔性正值 接受 x：
    若 x 大于 50000：
        返还 x。
    完。
    若 x 小于 0 减 50000：
        返还 0。
    完。
    ex 为 SSM精度 加 x 加 x 乘 x 除以 (2 乘 SSM精度)。
    若 ex 大于 SSM精度：
        返还 x 加 SSM精度 乘 SSM精度 除以 (ex 加 1)。
    完。
    返还 ex 除以 2。
完。

Mamba_选择性扫描 接受 模型、输入序列、序列长：
    N 为 模型 的 第 1 项。
    A_log 为 模型 的 第 4 项。
    W_Δ 为 模型 的 第 5 项。
    W_B 为 模型 的 第 6 项。
    W_C 为 模型 的 第 7 项。
    可变 h 为 一组：（空）。
    可变 hi 为 0。
    只要 hi 小于 N：
        做 追加 用 h、0。
        hi 为 hi 加 1。
    完。
    可变 输出 为 一组：（空）。
    可变 t 为 0。
    只要 t 小于 序列长：
        u 为 输入序列 的 第 t 项。
        Δ_raw 为 W_Δ 的 第 t 取余于 做 计数 用 W_Δ 项 乘 u 除以 SSM精度。
        Δ 为 做 柔性正值 用 Δ_raw。
        若 Δ 小于 10：
            Δ 为 10。
        完。
        可变 B_t 为 一组：（空）。
        可变 C_t 为 一组：（空）。
        可变 n 为 0。
        只要 n 小于 N：
            做 追加 用 B_t、W_B 的 第 n 项 乘 u 除以 SSM精度。
            做 追加 用 C_t、W_C 的 第 n 项 乘 u 除以 SSM精度。
            n 为 n 加 1。
        完。
        可变 新h 为 一组：（空）。
        可变 y 为 0。
        n 为 0。
        只要 n 小于 N：
            可变 A_n 为 0 减 SSM精度。
            若 n 小于 做 计数 用 A_log：
                可变 al 为 A_log 的 第 n 项。
                若 al 小于 0：
                    al 为 0 减 al。
                完。
                A_n 为 0 减 (SSM精度 加 al)。
            完。
            ΔA 为 Δ 乘 A_n 除以 SSM精度。
            衰减 为 SSM精度 加 ΔA。
            若 衰减 小于 0：
                衰减 为 0。
            完。
            新状态 为 衰减 乘 h 的 第 n 项 除以 SSM精度 加 Δ 乘 B_t 的 第 n 项 乘 u 除以 (SSM精度 乘 SSM精度 除以 SSM精度)。
            做 追加 用 新h、新状态。
            y 为 y 加 C_t 的 第 n 项 乘 新状态 除以 SSM精度。
            n 为 n 加 1。
        完。
        h 为 新h。
        做 追加 用 输出、y。
        t 为 t 加 1。
    完。
    返还 输出。
完。

Mamba2_创建 接受 模型维度、状态维度、头数：
    头维度 为 模型维度 除以 头数。
    可变 A_heads 为 一组：（空）。
    可变 hi 为 0。
    只要 hi 小于 头数：
        可变 A_diag 为 一组：（空）。
        可变 n 为 0。
        只要 n 小于 状态维度：
            做 追加 用 A_diag、0 减 (n 加 1) 乘 SSM精度 除以 状态维度。
            n 为 n 加 1。
        完。
        做 追加 用 A_heads、A_diag。
        hi 为 hi 加 1。
    完。
    可变 W_B 为 一组：（空）。
    可变 W_C 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 头数 乘 头维度 乘 状态维度：
        做 追加 用 W_B、(i 乘 2654435761 加 1) 取余于 200 减 100。
        做 追加 用 W_C、(i 乘 1103515245 加 7) 取余于 200 减 100。
        i 为 i 加 1。
    完。
    可变 W_Δ 为 一组：（空）。
    i 为 0。
    只要 i 小于 头数 乘 头维度：
        做 追加 用 W_Δ、(i 乘 2654435761 加 3) 取余于 200 减 100。
        i 为 i 加 1。
    完。
    返还 一组：模型维度、状态维度、头数、头维度、A_heads、W_B、W_C、W_Δ。
完。

Mamba2_前向 接受 模型、输入序列、序列长：
    D 为 模型 的 第 0 项。
    N 为 模型 的 第 1 项。
    H 为 模型 的 第 2 项。
    头维度 为 模型 的 第 3 项。
    A_heads 为 模型 的 第 4 项。
    W_B 为 模型 的 第 5 项。
    W_C 为 模型 的 第 6 项。
    W_Δ 为 模型 的 第 7 项。
    可变 输出 为 一组：（空）。
    可变 oi 为 0。
    只要 oi 小于 序列长 乘 D：
        做 追加 用 输出、0。
        oi 为 oi 加 1。
    完。
    可变 hi 为 0。
    只要 hi 小于 H：
        可变 h 为 一组：（空）。
        可变 ni 为 0。
        只要 ni 小于 N：
            做 追加 用 h、0。
            ni 为 ni 加 1。
        完。
        可变 t 为 0。
        只要 t 小于 序列长：
            可变 x_head 为 一组：（空）。
            可变 di 为 0。
            只要 di 小于 头维度：
                做 追加 用 x_head、输入序列 的 第 t 乘 D 加 hi 乘 头维度 加 di 项。
                di 为 di 加 1。
            完。
            可变 Δ_raw 为 0。
            di 为 0。
            只要 di 小于 头维度：
                Δ_raw 为 Δ_raw 加 W_Δ 的 第 hi 乘 头维度 加 di 项 乘 x_head 的 第 di 项 除以 SSM精度。
                di 为 di 加 1。
            完。
            Δ 为 做 柔性正值 用 Δ_raw。
            若 Δ 小于 10：
                Δ 为 10。
            完。
            可变 B_t 为 一组：（空）。
            可变 C_t 为 一组：（空）。
            ni 为 0。
            只要 ni 小于 N：
                可变 b_val 为 0。
                可变 c_val 为 0。
                di 为 0。
                只要 di 小于 头维度：
                    b_val 为 b_val 加 W_B 的 第 (hi 乘 头维度 加 di) 乘 N 加 ni 项 乘 x_head 的 第 di 项 除以 SSM精度。
                    c_val 为 c_val 加 W_C 的 第 (hi 乘 头维度 加 di) 乘 N 加 ni 项 乘 x_head 的 第 di 项 除以 SSM精度。
                    di 为 di 加 1。
                完。
                做 追加 用 B_t、b_val。
                做 追加 用 C_t、c_val。
                ni 为 ni 加 1。
            完。
            可变 新h 为 一组：（空）。
            可变 y_scalar 为 0。
            ni 为 0。
            只要 ni 小于 N：
                a_n 为 A_heads 的 第 hi 项 的 第 ni 项。
                ΔA 为 Δ 乘 a_n 除以 SSM精度。
                衰减 为 SSM精度 加 ΔA。
                若 衰减 小于 0：
                    衰减 为 0。
                完。
                u 为 x_head 的 第 0 项。
                新状态 为 衰减 乘 h 的 第 ni 项 除以 SSM精度 加 Δ 乘 B_t 的 第 ni 项 乘 u 除以 (SSM精度 乘 SSM精度 除以 SSM精度)。
                做 追加 用 新h、新状态。
                y_scalar 为 y_scalar 加 C_t 的 第 ni 项 乘 新状态 除以 SSM精度。
                ni 为 ni 加 1。
            完。
            h 为 新h。
            di 为 0。
            只要 di 小于 头维度：
                输出 的 第 t 乘 D 加 hi 乘 头维度 加 di 项 为 输出 的 第 t 乘 D 加 hi 乘 头维度 加 di 项 加 y_scalar。
                di 为 di 加 1。
            完。
            t 为 t 加 1。
        完。
        hi 为 hi 加 1。
    完。
    返还 输出。
完。

Jamba_层类型 接受 层号、注意力间隔：
    若 层号 取余于 注意力间隔 等于 注意力间隔 减 1：
        返还 1。
    完。
    返还 0。
完。

Jamba_层前向 接受 输入、层号、注意力间隔、mamba模型、序列长、d_模型、d_ff：
    层类型 为 做 Jamba_层类型 用 层号、注意力间隔。
    若 层类型 等于 0：
        返还 做 Mamba2_前向 用 mamba模型、输入、序列长。
    否则：
        可变 输出 为 一组：（空）。
        可变 t 为 0。
        只要 t 小于 序列长：
            可变 d 为 0。
            只要 d 小于 d_模型：
                可变 val 为 输入 的 第 t 乘 d_模型 加 d 项。
                可变 权重和 为 SSM精度。
                可变 w 为 1。
                只要 w 小于或等于 4：
                    若 t 减 w 大于或等于 0：
                        val 为 val 加 输入 的 第 (t 减 w) 乘 d_模型 加 d 项 乘 (SSM精度 减 w 乘 2000) 除以 SSM精度。
                        权重和 为 权重和 加 SSM精度 减 w 乘 2000。
                    完。
                    若 t 加 w 小于 序列长：
                        val 为 val 加 输入 的 第 (t 加 w) 乘 d_模型 加 d 项 乘 (SSM精度 减 w 乘 2000) 除以 SSM精度。
                        权重和 为 权重和 加 SSM精度 减 w 乘 2000。
                    完。
                    w 为 w 加 1。
                完。
                若 权重和 大于 0：
                    val 为 val 乘 SSM精度 除以 权重和。
                完。
                做 追加 用 输出、val。
                d 为 d 加 1。
            完。
            t 为 t 加 1。
        完。
        返还 输出。
    完。
完。

本章 收束。
