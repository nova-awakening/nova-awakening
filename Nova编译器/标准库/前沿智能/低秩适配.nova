《低秩适配》

引用《..·基础》。

引用《..·数学·工具》。

LORA精度 为 10000。

低秩适配_创建 接受 d_out、d_in、秩、缩放α：
    可变 A 为 一组：（空）。
    可变 ai 为 0。
    只要 ai 小于 秩 乘 d_in：
        值 为 ((ai 乘 2654435761 加 12345) 取余于 2000 减 1000) 乘 LORA精度 除以 (d_in 乘 100 加 1)。
        做 追加 用 A、值。
        ai 为 ai 加 1。
    完。
    可变 B 为 一组：（空）。
    可变 bi 为 0。
    只要 bi 小于 d_out 乘 秩：
        做 追加 用 B、0。
        bi 为 bi 加 1。
    完。
    返还 一组：d_out、d_in、秩、缩放α、A、B。
完。

低秩适配_前向 接受 层、x：
    可变 dot 为 0。
    可变 oi 为 0。
    可变 ri 为 0。
    d_out 为 层 的 第 0 项。
    d_in 为 层 的 第 1 项。
    秩 为 层 的 第 2 项。
    α 为 层 的 第 3 项。
    A 为 层 的 第 4 项。
    B 为 层 的 第 5 项。
    可变 h 为 一组：（空）。
    只要 ri 小于 秩：
        可变 j 为 0。
        只要 j 小于 d_in：
            dot 为 dot 加 A 的 第 ri 乘 d_in 加 j 项 乘 x 的 第 j 项 除以 LORA精度。
            j 为 j 加 1。
        完。
        做 追加 用 h、dot。
        ri 为 ri 加 1。
    完。
    可变 y 为 一组：（空）。
    只要 oi 小于 d_out：
        可变 j 为 0。
        只要 j 小于 秩：
            dot 为 dot 加 B 的 第 oi 乘 秩 加 j 项 乘 h 的 第 j 项 除以 LORA精度。
            j 为 j 加 1。
        完。
        做 追加 用 y、dot 乘 α 除以 (秩 加 1)。
        oi 为 oi 加 1。
    完。
    返还 y。
完。

低秩适配_更新 接受 层、x、梯度、学习率：
    可变 oi 为 0。
    可变 ri 为 0。
    d_out 为 层 的 第 0 项。
    d_in 为 层 的 第 1 项。
    秩 为 层 的 第 2 项。
    α 为 层 的 第 3 项。
    可变 A 为 层 的 第 4 项。
    可变 B 为 层 的 第 5 项。
    可变 h 为 一组：（空）。
    只要 ri 小于 秩：
        可变 j 为 0。
        只要 j 小于 d_in：
            dot 为 dot 加 A 的 第 ri 乘 d_in 加 j 项 乘 x 的 第 j 项 除以 LORA精度。
            j 为 j 加 1。
        完。
        做 追加 用 h、dot。
        ri 为 ri 加 1。
    完。
    只要 oi 小于 d_out：
        可变 rj 为 0。
        只要 rj 小于 秩：
            B 的 第 oi 乘 秩 加 rj 项 为 B 的 第 oi 乘 秩 加 rj 项 减 学习率 乘 梯度 的 第 oi 项 乘 h 的 第 rj 项 除以 (LORA精度 乘 LORA精度 除以 LORA精度)。
            rj 为 rj 加 1。
        完。
        oi 为 oi 加 1。
    完。
    可变 Bg 为 一组：（空）。
    ri 为 0。
    只要 ri 小于 秩：
        可变 oi2 为 0。
        只要 oi2 小于 d_out：
            dot 为 dot 加 B 的 第 oi2 乘 秩 加 ri 项 乘 梯度 的 第 oi2 项 除以 LORA精度。
            oi2 为 oi2 加 1。
        完。
        做 追加 用 Bg、dot。
        ri 为 ri 加 1。
    完。
    ri 为 0。
    只要 ri 小于 秩：
        可变 j 为 0。
        只要 j 小于 d_in：
            A 的 第 ri 乘 d_in 加 j 项 为 A 的 第 ri 乘 d_in 加 j 项 减 学习率 乘 Bg 的 第 ri 项 乘 x 的 第 j 项 除以 (LORA精度 乘 LORA精度 除以 LORA精度)。
            j 为 j 加 1。
        完。
        ri 为 ri 加 1。
    完。
    返还 一组：d_out、d_in、秩、α、A、B。
完。

NF4_级别 为 一组：负 10000、负 8617、负 7229、负 5819、负 4399、负 2966、负 1510、0、0、1510、2966、4399、5819、7229、8617、10000。

量化低秩_量化 接受 权重、块大小：
    n 为 做 计数 用 权重。
    块数 为 (n 加 块大小 减 1) 除以 块大小。
    可变 量化值 为 一组：（空）。
    可变 缩放因子 为 一组：（空）。
    可变 b 为 0。
    只要 b 小于 块数：
        起 为 b 乘 块大小。
        可变 止 为 起 加 块大小。
        若 止 大于 n：
            止 为 n。
        完。
        可变 abs_max 为 0。
        可变 i 为 起。
        只要 i 小于 止：
            可变 av 为 权重 的 第 i 项。
            若 av 小于 0：
                av 为 0 减 av。
            完。
            若 av 大于 abs_max：
                abs_max 为 av。
            完。
            i 为 i 加 1。
        完。
        若 abs_max 等于 0：
            abs_max 为 1。
        完。
        做 追加 用 缩放因子、abs_max。
        i 为 起。
        只要 i 小于 止：
            归一 为 权重 的 第 i 项 乘 LORA精度 除以 abs_max。
            可变 最佳 为 0。
            可变 最佳距离 为 LORA精度 乘 100。
            可变 k 为 0。
            只要 k 小于 16：
                可变 距离 为 归一 减 NF4_级别 的 第 k 项。
                若 距离 小于 0：
                    距离 为 0 减 距离。
                完。
                若 距离 小于 最佳距离：
                    最佳距离 为 距离。
                    最佳 为 k。
                完。
                k 为 k 加 1。
            完。
            做 追加 用 量化值、最佳。
            i 为 i 加 1。
        完。
        b 为 b 加 1。
    完。
    返还 一组：量化值、缩放因子、块大小。
完。

量化低秩_反量化 接受 量化数据、索引：
    量化值 为 量化数据 的 第 0 项。
    缩放因子 为 量化数据 的 第 1 项。
    块大小 为 量化数据 的 第 2 项。
    块号 为 索引 除以 块大小。
    级别 为 NF4_级别 的 第 量化值 的 第 索引 项 项。
    返还 级别 乘 缩放因子 的 第 块号 项 除以 LORA精度。
完。

低秩适配_合并 接受 W原始、层：
    d_out 为 层 的 第 0 项。
    d_in 为 层 的 第 1 项。
    秩 为 层 的 第 2 项。
    α 为 层 的 第 3 项。
    A 为 层 的 第 4 项。
    B 为 层 的 第 5 项。
    可变 W合并 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d_out 乘 d_in：
        做 追加 用 W合并、W原始 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 d_out：
        可变 j 为 0。
        只要 j 小于 d_in：
            可变 ba 为 0。
            可变 r 为 0。
            只要 r 小于 秩：
                ba 为 ba 加 B 的 第 i 乘 秩 加 r 项 乘 A 的 第 r 乘 d_in 加 j 项 除以 LORA精度。
                r 为 r 加 1。
            完。
            W合并 的 第 i 乘 d_in 加 j 项 为 W合并 的 第 i 乘 d_in 加 j 项 加 ba 乘 α 除以 (秩 加 1)。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 W合并。
完。

DoRA_创建 接受 d_out、d_in、秩、缩放α、W原始：
    lora 为 做 低秩适配_创建 用 d_out、d_in、秩、缩放α。
    可变 幅度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d_out：
        可变 行范数平方 为 0。
        可变 j 为 0。
        只要 j 小于 d_in：
            行范数平方 为 行范数平方 加 W原始 的 第 i 乘 d_in 加 j 项 乘 W原始 的 第 i 乘 d_in 加 j 项 除以 LORA精度。
            j 为 j 加 1。
        完。
        可变 s 为 行范数平方 除以 2。
        若 s 等于 0：
            s 为 1。
        完。
        可变 k 为 0。
        只要 k 小于 6：
            s 为 (s 加 行范数平方 乘 LORA精度 除以 s) 除以 2。
            k 为 k 加 1。
        完。
        做 追加 用 幅度、s。
        i 为 i 加 1。
    完。
    返还 一组：lora、幅度。
完。

DoRA_前向 接受 dora、W原始、x：
    lora 为 dora 的 第 0 项。
    幅度 为 dora 的 第 1 项。
    d_out 为 lora 的 第 0 项。
    d_in 为 lora 的 第 1 项。
    Δy 为 做 低秩适配_前向 用 lora、x。
    可变 Wx 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d_out：
        可变 dot 为 0。
        可变 j 为 0。
        只要 j 小于 d_in：
            dot 为 dot 加 W原始 的 第 i 乘 d_in 加 j 项 乘 x 的 第 j 项 除以 LORA精度。
            j 为 j 加 1。
        完。
        做 追加 用 Wx、dot。
        i 为 i 加 1。
    完。
    可变 输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 d_out：
        原始范数 为 幅度 的 第 i 项。
        若 原始范数 小于 1：
            原始范数 为 1。
        完。
        组合 为 Wx 的 第 i 项 加 Δy 的 第 i 项。
        做 追加 用 输出、幅度 的 第 i 项 乘 组合 除以 原始范数。
        i 为 i 加 1。
    完。
    返还 输出。
完。

GaLore_创建 接受 d_out、d_in、秩、投影更新间隔：
    可变 P 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d_out 乘 秩：
        值 为 (i 乘 2654435761 加 12345) 取余于 2000 减 1000。
        做 追加 用 P、值。
        i 为 i 加 1。
    完。
    可变 m 为 一组：（空）。
    可变 v 为 一组：（空）。
    i 为 0。
    只要 i 小于 秩 乘 d_in：
        做 追加 用 m、0。
        做 追加 用 v、0。
        i 为 i 加 1。
    完。
    返还 一组：d_out、d_in、秩、投影更新间隔、P、m、v、0。
完。

GaLore_投影 接受 状态、梯度矩阵：
    d_out 为 状态 的 第 0 项。
    d_in 为 状态 的 第 1 项。
    秩 为 状态 的 第 2 项。
    P 为 状态 的 第 4 项。
    可变 G低秩 为 一组：（空）。
    可变 r 为 0。
    只要 r 小于 秩：
        可变 j 为 0。
        只要 j 小于 d_in：
            可变 val 为 0。
            可变 i 为 0。
            只要 i 小于 d_out：
                val 为 val 加 P 的 第 i 乘 秩 加 r 项 乘 梯度矩阵 的 第 i 乘 d_in 加 j 项 除以 LORA精度。
                i 为 i 加 1。
            完。
            做 追加 用 G低秩、val。
            j 为 j 加 1。
        完。
        r 为 r 加 1。
    完。
    返还 G低秩。
完。

GaLore_步进 接受 状态、梯度矩阵、学习率、β1、β2：
    d_out 为 状态 的 第 0 项。
    d_in 为 状态 的 第 1 项。
    秩 为 状态 的 第 2 项。
    P 为 状态 的 第 4 项。
    可变 m 为 状态 的 第 5 项。
    可变 v 为 状态 的 第 6 项。
    G低秩 为 做 GaLore_投影 用 状态、梯度矩阵。
    可变 更新 为 一组：（空）。
    可变 idx 为 0。
    只要 idx 小于 秩 乘 d_in：
        m 的 第 idx 项 为 β1 乘 m 的 第 idx 项 除以 LORA精度 加 (LORA精度 减 β1) 乘 G低秩 的 第 idx 项 除以 LORA精度。
        v 的 第 idx 项 为 β2 乘 v 的 第 idx 项 除以 LORA精度 加 (LORA精度 减 β2) 乘 G低秩 的 第 idx 项 乘 G低秩 的 第 idx 项 除以 (LORA精度 乘 LORA精度 除以 LORA精度)。
        可变 sv 为 v 的 第 idx 项。
        若 sv 小于 0：
            sv 为 0 减 sv。
        完。
        可变 sqr 为 sv 除以 2。
        若 sqr 等于 0：
            sqr 为 1。
        完。
        可变 k 为 0。
        只要 k 小于 4：
            sqr 为 (sqr 加 sv 乘 LORA精度 除以 sqr) 除以 2。
            k 为 k 加 1。
        完。
        做 追加 用 更新、m 的 第 idx 项 乘 LORA精度 除以 (sqr 加 100)。
        idx 为 idx 加 1。
    完。
    可变 ΔW 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d_out：
        可变 j 为 0。
        只要 j 小于 d_in：
            可变 val 为 0。
            可变 r 为 0。
            只要 r 小于 秩：
                val 为 val 加 P 的 第 i 乘 秩 加 r 项 乘 更新 的 第 r 乘 d_in 加 j 项 除以 LORA精度。
                r 为 r 加 1。
            完。
            做 追加 用 ΔW、学习率 乘 val 除以 LORA精度。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    状态 的 第 5 项 为 m。
    状态 的 第 6 项 为 v。
    状态 的 第 7 项 为 状态 的 第 7 项 加 1。
    返还 ΔW。
完。

rs低秩_缩放 接受 α、秩：
    可变 s 为 秩 除以 2。
    若 s 等于 0：
        s 为 1。
    完。
    可变 k 为 0。
    只要 k 小于 6：
        s 为 (s 加 秩 乘 LORA精度 除以 s) 除以 2。
        k 为 k 加 1。
    完。
    返还 α 乘 LORA精度 除以 s。
完。

rs低秩_前向 接受 层、x：
    d_out 为 层 的 第 0 项。
    d_in 为 层 的 第 1 项。
    秩 为 层 的 第 2 项。
    α 为 层 的 第 3 项。
    A 为 层 的 第 4 项。
    B 为 层 的 第 5 项。
    缩放 为 做 rs低秩_缩放 用 α、秩。
    可变 h 为 一组：（空）。
    可变 ri 为 0。
    只要 ri 小于 秩：
        可变 dot 为 0。
        可变 j 为 0。
        只要 j 小于 d_in：
            dot 为 dot 加 A 的 第 ri 乘 d_in 加 j 项 乘 x 的 第 j 项 除以 LORA精度。
            j 为 j 加 1。
        完。
        做 追加 用 h、dot。
        ri 为 ri 加 1。
    完。
    可变 y 为 一组：（空）。
    可变 oi 为 0。
    只要 oi 小于 d_out：
        可变 dot 为 0。
        可变 rj 为 0。
        只要 rj 小于 秩：
            dot 为 dot 加 B 的 第 oi 乘 秩 加 rj 项 乘 h 的 第 rj 项 除以 LORA精度。
            rj 为 rj 加 1。
        完。
        做 追加 用 y、dot 乘 缩放 除以 LORA精度。
        oi 为 oi 加 1。
    完。
    返还 y。
完。

本章 收束。
