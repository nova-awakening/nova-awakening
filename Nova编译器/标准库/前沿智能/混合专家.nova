《混合专家》

引用《..·基础》。

引用《..·数学·工具》。

MOE精度 为 10000。

混合专家_路由 接受 输入、门控权重、专家数、top_k：
    d 为 做 计数 用 输入。
    可变 分数 为 一组：（空）。
    可变 e 为 0。
    只要 e 小于 专家数：
        可变 dot 为 0。
        可变 j 为 0。
        只要 j 小于 d：
            dot 为 dot 加 门控权重 的 第 e 乘 d 加 j 项 乘 输入 的 第 j 项 除以 MOE精度。
            j 为 j 加 1。
        完。
        做 追加 用 分数、dot。
        e 为 e 加 1。
    完。
    可变 选中 为 一组：（空）。
    可变 已选标记 为 一组：（空）。
    e 为 0。
    只要 e 小于 专家数：
        做 追加 用 已选标记、0。
        e 为 e 加 1。
    完。
    可变 ki 为 0。
    只要 ki 小于 top_k：
        可变 最佳 为 负 1。
        可变 最佳分 为 0 减 MOE精度 乘 100。
        e 为 0。
        只要 e 小于 专家数：
            若 已选标记 的 第 e 项 等于 0 并且 分数 的 第 e 项 大于 最佳分：
                最佳分 为 分数 的 第 e 项。
                最佳 为 e。
            完。
            e 为 e 加 1。
        完。
        若 最佳 大于或等于 0：
            做 追加 用 选中、最佳。
            已选标记 的 第 最佳 项 为 1。
        完。
        ki 为 ki 加 1。
    完。
    可变 权重 为 一组：（空）。
    可变 sum_exp 为 0。
    可变 max_s 为 0 减 MOE精度 乘 100。
    ki 为 0。
    只要 ki 小于 做 计数 用 选中：
        若 分数 的 第 选中 的 第 ki 项 项 大于 max_s：
            max_s 为 分数 的 第 选中 的 第 ki 项 项。
        完。
        ki 为 ki 加 1。
    完。
    ki 为 0。
    只要 ki 小于 做 计数 用 选中：
        差 为 分数 的 第 选中 的 第 ki 项 项 减 max_s。
        可变 exp_v 为 MOE精度 加 差 加 差 乘 差 除以 (2 乘 MOE精度)。
        若 exp_v 小于 1：
            exp_v 为 1。
        完。
        做 追加 用 权重、exp_v。
        sum_exp 为 sum_exp 加 exp_v。
        ki 为 ki 加 1。
    完。
    若 sum_exp 大于 0：
        ki 为 0。
        只要 ki 小于 做 计数 用 权重：
            权重 的 第 ki 项 为 权重 的 第 ki 项 乘 MOE精度 除以 sum_exp。
            ki 为 ki 加 1。
        完。
    完。
    返还 一组：选中、权重。
完。

混合专家_前向 接受 输入、专家权重列表、门控权重、专家数、top_k、d_模型、d_ff：
    路由结果 为 做 混合专家_路由 用 输入、门控权重、专家数、top_k。
    选中 为 路由结果 的 第 0 项。
    权重 为 路由结果 的 第 1 项。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d_模型：
        做 追加 用 输出、0。
        i 为 i 加 1。
    完。
    可变 ki 为 0。
    只要 ki 小于 做 计数 用 选中：
        专家idx 为 选中 的 第 ki 项。
        w 为 权重 的 第 ki 项。
        W1 为 专家权重列表 的 第 专家idx 乘 2 项。
        W2 为 专家权重列表 的 第 专家idx 乘 2 加 1 项。
        可变 h 为 一组：（空）。
        可变 fi 为 0。
        只要 fi 小于 d_ff：
            可变 dot 为 0。
            可变 j 为 0。
            只要 j 小于 d_模型：
                dot 为 dot 加 W1 的 第 fi 乘 d_模型 加 j 项 乘 输入 的 第 j 项 除以 MOE精度。
                j 为 j 加 1。
            完。
            若 dot 小于 0：
                dot 为 dot 除以 4。
            完。
            做 追加 用 h、dot。
            fi 为 fi 加 1。
        完。
        i 为 0。
        只要 i 小于 d_模型：
            可变 dot 为 0。
            可变 fi2 为 0。
            只要 fi2 小于 d_ff：
                dot 为 dot 加 W2 的 第 i 乘 d_ff 加 fi2 项 乘 h 的 第 fi2 项 除以 MOE精度。
                fi2 为 fi2 加 1。
            完。
            输出 的 第 i 项 为 输出 的 第 i 项 加 w 乘 dot 除以 MOE精度。
            i 为 i 加 1。
        完。
        ki 为 ki 加 1。
    完。
    返还 输出。
完。

深搜_混合专家前向 接受 输入、共享专家W1、共享专家W2、路由专家权重、门控权重、专家数、top_k、d_模型、d_ff：
    可变 共享输出 为 一组：（空）。
    可变 fi 为 0。
    只要 fi 小于 d_ff：
        可变 j 为 0。
        可变 dot 为 0。
        只要 j 小于 d_模型：
            dot 为 dot 加 共享专家W1 的 第 fi 乘 d_模型 加 j 项 乘 输入 的 第 j 项 除以 MOE精度。
            j 为 j 加 1。
        完。
        若 dot 小于 0：
            dot 为 dot 除以 4。
        完。
        做 追加 用 共享输出、dot。
        fi 为 fi 加 1。
    完。
    可变 共享y 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 d_模型：
        fi 为 0。
        可变 dot 为 0。
        只要 fi 小于 d_ff：
            dot 为 dot 加 共享专家W2 的 第 i 乘 d_ff 加 fi 项 乘 共享输出 的 第 fi 项 除以 MOE精度。
            fi 为 fi 加 1。
        完。
        做 追加 用 共享y、dot。
        i 为 i 加 1。
    完。
    路由y 为 做 混合专家_前向 用 输入、路由专家权重、门控权重、专家数、top_k、d_模型、d_ff。
    可变 最终 为 一组：（空）。
    i 为 0。
    只要 i 小于 d_模型：
        做 追加 用 最终、共享y 的 第 i 项 加 路由y 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 最终。
完。

混合专家_负载均衡损失 接受 选择历史、概率历史、专家数、系数α：
    T 为 做 计数 用 选择历史。
    若 T 等于 0：
        返还 0。
    完。
    可变 频率 为 一组：（空）。
    可变 概率均值 为 一组：（空）。
    可变 e 为 0。
    只要 e 小于 专家数：
        做 追加 用 频率、0。
        做 追加 用 概率均值、0。
        e 为 e 加 1。
    完。
    可变 t 为 0。
    只要 t 小于 T：
        频率 的 第 选择历史 的 第 t 项 项 为 频率 的 第 选择历史 的 第 t 项 项 加 MOE精度 除以 T。
        若 t 小于 做 计数 用 概率历史：
            概率均值 的 第 选择历史 的 第 t 项 项 为 概率均值 的 第 选择历史 的 第 t 项 项 加 概率历史 的 第 t 项 除以 T。
        完。
        t 为 t 加 1。
    完。
    可变 损失 为 0。
    e 为 0。
    只要 e 小于 专家数：
        损失 为 损失 加 频率 的 第 e 项 乘 概率均值 的 第 e 项 除以 MOE精度。
        e 为 e 加 1。
    完。
    返还 系数α 乘 专家数 乘 损失 除以 MOE精度。
完。

ExpertChoice_路由 接受 输入列表、门控权重、专家数、容量因子、d_模型：
    T 为 做 计数 用 输入列表 除以 d_模型。
    容量 为 T 乘 容量因子 除以 专家数。
    若 容量 小于 1：
        容量 为 1。
    完。
    可变 分配 为 一组：（空）。
    可变 分配权重 为 一组：（空）。
    可变 e 为 0。
    只要 e 小于 专家数：
        可变 分数 为 一组：（空）。
        可变 t 为 0。
        只要 t 小于 T：
            可变 dot 为 0。
            可变 j 为 0。
            只要 j 小于 d_模型：
                dot 为 dot 加 门控权重 的 第 e 乘 d_模型 加 j 项 乘 输入列表 的 第 t 乘 d_模型 加 j 项 除以 MOE精度。
                j 为 j 加 1。
            完。
            做 追加 用 分数、dot。
            t 为 t 加 1。
        完。
        可变 专家选中 为 一组：（空）。
        可变 专家权重 为 一组：（空）。
        可变 已选 为 一组：（空）。
        t 为 0。
        只要 t 小于 T：
            做 追加 用 已选、0。
            t 为 t 加 1。
        完。
        可变 ci 为 0。
        只要 ci 小于 容量：
            可变 最佳t 为 负 1。
            可变 最佳分 为 0 减 MOE精度 乘 100。
            t 为 0。
            只要 t 小于 T：
                若 已选 的 第 t 项 等于 0 并且 分数 的 第 t 项 大于 最佳分：
                    最佳分 为 分数 的 第 t 项。
                    最佳t 为 t。
                完。
                t 为 t 加 1。
            完。
            若 最佳t 大于或等于 0：
                做 追加 用 专家选中、最佳t。
                已选 的 第 最佳t 项 为 1。
                做 追加 用 专家权重、最佳分。
            完。
            ci 为 ci 加 1。
        完。
        可变 wsum 为 0。
        ci 为 0。
        只要 ci 小于 做 计数 用 专家权重：
            wsum 为 wsum 加 专家权重 的 第 ci 项。
            ci 为 ci 加 1。
        完。
        若 wsum 大于 0：
            ci 为 0。
            只要 ci 小于 做 计数 用 专家权重：
                专家权重 的 第 ci 项 为 专家权重 的 第 ci 项 乘 MOE精度 除以 wsum。
                ci 为 ci 加 1。
            完。
        完。
        做 追加 用 分配、专家选中。
        做 追加 用 分配权重、专家权重。
        e 为 e 加 1。
    完。
    返还 一组：分配、分配权重。
完。

V3_无损均衡_路由 接受 输入、门控权重、偏置、专家数、top_k：
    d 为 做 计数 用 输入。
    可变 原始分数 为 一组：（空）。
    可变 e 为 0。
    只要 e 小于 专家数：
        可变 dot 为 0。
        可变 j 为 0。
        只要 j 小于 d：
            dot 为 dot 加 门控权重 的 第 e 乘 d 加 j 项 乘 输入 的 第 j 项 除以 MOE精度。
            j 为 j 加 1。
        完。
        做 追加 用 原始分数、dot。
        e 为 e 加 1。
    完。
    可变 路由分数 为 一组：（空）。
    e 为 0。
    只要 e 小于 专家数：
        做 追加 用 路由分数、原始分数 的 第 e 项 加 偏置 的 第 e 项。
        e 为 e 加 1。
    完。
    可变 选中 为 一组：（空）。
    可变 已选标记 为 一组：（空）。
    e 为 0。
    只要 e 小于 专家数：
        做 追加 用 已选标记、0。
        e 为 e 加 1。
    完。
    可变 ki 为 0。
    只要 ki 小于 top_k：
        可变 最佳 为 负 1。
        可变 最佳分 为 0 减 MOE精度 乘 100。
        e 为 0。
        只要 e 小于 专家数：
            若 已选标记 的 第 e 项 等于 0 并且 路由分数 的 第 e 项 大于 最佳分：
                最佳分 为 路由分数 的 第 e 项。
                最佳 为 e。
            完。
            e 为 e 加 1。
        完。
        若 最佳 大于或等于 0：
            做 追加 用 选中、最佳。
            已选标记 的 第 最佳 项 为 1。
        完。
        ki 为 ki 加 1。
    完。
    可变 权重 为 一组：（空）。
    可变 max_s 为 0 减 MOE精度 乘 100。
    ki 为 0。
    只要 ki 小于 做 计数 用 选中：
        若 原始分数 的 第 选中 的 第 ki 项 项 大于 max_s：
            max_s 为 原始分数 的 第 选中 的 第 ki 项 项。
        完。
        ki 为 ki 加 1。
    完。
    可变 sum_exp 为 0。
    ki 为 0。
    只要 ki 小于 做 计数 用 选中：
        差 为 原始分数 的 第 选中 的 第 ki 项 项 减 max_s。
        可变 ev 为 MOE精度 加 差 加 差 乘 差 除以 (2 乘 MOE精度)。
        若 ev 小于 1：
            ev 为 1。
        完。
        做 追加 用 权重、ev。
        sum_exp 为 sum_exp 加 ev。
        ki 为 ki 加 1。
    完。
    若 sum_exp 大于 0：
        ki 为 0。
        只要 ki 小于 做 计数 用 权重：
            权重 的 第 ki 项 为 权重 的 第 ki 项 乘 MOE精度 除以 sum_exp。
            ki 为 ki 加 1。
        完。
    完。
    返还 一组：选中、权重。
完。

V3_更新偏置 接受 偏置、专家利用率、专家数、更新步长：
    可变 总利用 为 0。
    可变 e 为 0。
    只要 e 小于 专家数：
        总利用 为 总利用 加 专家利用率 的 第 e 项。
        e 为 e 加 1。
    完。
    均值 为 总利用 除以 (专家数 加 1)。
    e 为 0。
    只要 e 小于 专家数：
        偏差 为 专家利用率 的 第 e 项 减 均值。
        偏置 的 第 e 项 为 偏置 的 第 e 项 减 更新步长 乘 偏差 除以 MOE精度。
        e 为 e 加 1。
    完。
    返还 偏置。
完。

MoD_路由 接受 token嵌入列表、路由权重、序列长、d_模型、容量比例：
    容量 为 序列长 乘 容量比例 除以 MOE精度。
    若 容量 小于 1：
        容量 为 1。
    完。
    可变 分数 为 一组：（空）。
    可变 t 为 0。
    只要 t 小于 序列长：
        可变 dot 为 0。
        可变 j 为 0。
        只要 j 小于 d_模型：
            dot 为 dot 加 路由权重 的 第 j 项 乘 token嵌入列表 的 第 t 乘 d_模型 加 j 项 除以 MOE精度。
            j 为 j 加 1。
        完。
        做 追加 用 分数、dot。
        t 为 t 加 1。
    完。
    可变 参与计算 为 一组：（空）。
    t 为 0。
    只要 t 小于 序列长：
        做 追加 用 参与计算、0。
        t 为 t 加 1。
    完。
    可变 已选 为 0。
    只要 已选 小于 容量：
        可变 最佳t 为 负 1。
        可变 最佳分 为 0 减 MOE精度 乘 100。
        t 为 0。
        只要 t 小于 序列长：
            若 参与计算 的 第 t 项 等于 0 并且 分数 的 第 t 项 大于 最佳分：
                最佳分 为 分数 的 第 t 项。
                最佳t 为 t。
            完。
            t 为 t 加 1。
        完。
        若 最佳t 大于或等于 0：
            参与计算 的 第 最佳t 项 为 1。
        完。
        已选 为 已选 加 1。
    完。
    可变 路由权重列表 为 一组：（空）。
    t 为 0。
    只要 t 小于 序列长：
        若 参与计算 的 第 t 项 等于 1：
            s 为 分数 的 第 t 项。
            可变 σ 为 MOE精度 除以 2。
            若 s 大于 0：
                σ 为 MOE精度 减 MOE精度 乘 MOE精度 除以 (MOE精度 加 s 加 s 乘 s 除以 (2 乘 MOE精度))。
            否则若 s 小于 0：
                neg 为 0 减 s。
                σ 为 MOE精度 乘 MOE精度 除以 (MOE精度 加 neg 加 neg 乘 neg 除以 (2 乘 MOE精度))。
            完。
            做 追加 用 路由权重列表、σ。
        否则：
            做 追加 用 路由权重列表、0。
        完。
        t 为 t 加 1。
    完。
    返还 一组：参与计算、路由权重列表。
完。

MoD_前向 接受 token列表、参与标记、路由权重、变换函数权重、序列长、d_模型、d_ff：
    可变 输出 为 一组：（空）。
    可变 t 为 0。
    只要 t 小于 序列长：
        若 参与标记 的 第 t 项 等于 1：
            可变 x 为 一组：（空）。
            可变 j 为 0。
            只要 j 小于 d_模型：
                做 追加 用 x、token列表 的 第 t 乘 d_模型 加 j 项。
                j 为 j 加 1。
            完。
            可变 h 为 一组：（空）。
            可变 fi 为 0。
            只要 fi 小于 d_ff：
                可变 dot 为 0。
                j 为 0。
                只要 j 小于 d_模型：
                    dot 为 dot 加 变换函数权重 的 第 fi 乘 d_模型 加 j 项 乘 x 的 第 j 项 除以 MOE精度。
                    j 为 j 加 1。
                完。
                若 dot 小于 0：
                    dot 为 dot 除以 4。
                完。
                做 追加 用 h、dot。
                fi 为 fi 加 1。
            完。
            j 为 0。
            只要 j 小于 d_模型：
                可变 dot 为 0。
                fi 为 0。
                只要 fi 小于 d_ff：
                    dot 为 dot 加 变换函数权重 的 第 d_ff 乘 d_模型 加 j 乘 d_ff 加 fi 项 乘 h 的 第 fi 项 除以 MOE精度。
                    fi 为 fi 加 1。
                完。
                做 追加 用 输出、x 的 第 j 项 加 路由权重 的 第 t 项 乘 dot 除以 MOE精度。
                j 为 j 加 1。
            完。
        否则：
            可变 j 为 0。
            只要 j 小于 d_模型：
                做 追加 用 输出、token列表 的 第 t 乘 d_模型 加 j 项。
                j 为 j 加 1。
            完。
        完。
        t 为 t 加 1。
    完。
    返还 输出。
完。

本章 收束。
