《归一化层》

引用《..·基础》。

引用《..·数学·工具》。

_平方根 接受 n：
    若 n 小于或等于 0：
        返还 1。
    完。
    可变 x 为 n。
    可变 i 为 0。
    只要 i 小于 20：
        若 x 等于 0：
            返还 1。
        完。
        x 为 (x 加 n 除以 x) 除以 2。
        i 为 i 加 1。
    完。
    若 x 小于 1：
        返还 1。
    完。
    返还 x。
完。

_均值计算 接受 数据：
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 0。
    完。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和 为 和 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和 除以 n。
完。

_方差计算 接受 数据、均值：
    n 为 做 计数 用 数据。
    可变 方差和 为 0。
    若 n 等于 0：
        返还 0。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        差 为 数据 的 第 i 项 减 均值。
        方差和 为 方差和 加 差 乘 差 除以 1000。
        i 为 i 加 1。
    完。
    返还 方差和 除以 n。
完。

层归一化计算 接受 数据、缩放因子、偏移因子、eps：
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    均值 为 做 _均值计算 用 数据。
    方差向量 为 做 _方差计算 用 数据、均值。
    标准差 为 做 _平方根 用 方差向量 乘 1000 加 eps。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        范数向量 为 (数据 的 第 i 项 减 均值) 乘 1000 除以 标准差。
        做 追加 用 结果、范数向量 乘 缩放因子 除以 1000 加 偏移因子。
        i 为 i 加 1。
    完。
    返还 结果。
完。

层归一化向量 接受 数据、gamma_vec、beta_vec、eps：
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    均值 为 做 _均值计算 用 数据。
    方差向量 为 做 _方差计算 用 数据、均值。
    标准差 为 做 _平方根 用 方差向量 乘 1000 加 eps。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        范数向量 为 (数据 的 第 i 项 减 均值) 乘 1000 除以 标准差。
        做 追加 用 结果、范数向量 乘 gamma_vec 的 第 i 项 除以 1000 加 beta_vec 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

均方根归一化 接受 数据、缩放因子、eps：
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 平方和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        平方和 为 平方和 加 数据 的 第 i 项 乘 数据 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    均方根值 为 做 _平方根 用 平方和 除以 n 乘 1000 加 eps。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、数据 的 第 i 项 乘 1000 除以 均方根值 乘 缩放因子 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

可变 批归一_运行均值 为 一组：（空）。

可变 批归一_运行方差 为 一组：（空）。

可变 批归一_动量 为 100。

批归一化初始 接受 维度、动量：
    批归一_运行均值 为 一组：（空）。
    批归一_运行方差 为 一组：（空）。
    批归一_动量 为 动量。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 批归一_运行均值、0。
        做 追加 用 批归一_运行方差、1000。
        i 为 i 加 1。
    完。
完。

批归一化计算 接受 数据、缩放因子、偏移因子、eps、training：
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    均值 为 做 _均值计算 用 数据。
    方差向量 为 做 _方差计算 用 数据、均值。
    若 training 等于 真：
        批归一_运行均值 的 第 0 项 为 (1000 减 批归一_动量) 乘 批归一_运行均值 的 第 0 项 除以 1000 加 批归一_动量 乘 均值 除以 1000。
        批归一_运行方差 的 第 0 项 为 (1000 减 批归一_动量) 乘 批归一_运行方差 的 第 0 项 除以 1000 加 批归一_动量 乘 方差向量 除以 1000。
    完。
    标准差 为 做 _平方根 用 方差向量 乘 1000 加 eps。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        范数向量 为 (数据 的 第 i 项 减 均值) 乘 1000 除以 标准差。
        做 追加 用 结果、范数向量 乘 缩放因子 除以 1000 加 偏移因子。
        i 为 i 加 1。
    完。
    返还 结果。
完。

组归一化 接受 数据、n_groups、缩放因子、偏移因子、eps：
    n 为 做 计数 用 数据。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 group_size 为 n 除以 n_groups。
    若 group_size 小于 1：
        group_size 为 1。
    完。
    可变 结果 为 一组：（空）。
    可变 g 为 0。
    只要 g 小于 n_groups：
        起始 为 g 乘 group_size。
        可变 末尾索引 为 起始 加 group_size。
        若 末尾索引 大于 n：
            末尾索引 为 n。
        完。
        计数值 为 末尾索引 减 起始。
        可变 gm 为 0。
        可变 gi 为 起始。
        只要 gi 小于 末尾索引：
            gm 为 gm 加 数据 的 第 gi 项。
            gi 为 gi 加 1。
        完。
        gm 为 gm 除以 计数值。
        可变 gv 为 0。
        gi 为 起始。
        只要 gi 小于 末尾索引：
            d 为 数据 的 第 gi 项 减 gm。
            gv 为 gv 加 d 乘 d 除以 1000。
            gi 为 gi 加 1。
        完。
        gv 为 gv 除以 计数值。
        gstd 为 做 _平方根 用 gv 乘 1000 加 eps。
        gi 为 起始。
        只要 gi 小于 末尾索引：
            nv 为 (数据 的 第 gi 项 减 gm) 乘 1000 除以 gstd。
            做 追加 用 结果、nv 乘 缩放因子 除以 1000 加 偏移因子。
            gi 为 gi 加 1。
        完。
        g 为 g 加 1。
    完。
    返还 结果。
完。

实例归一化 接受 数据、n_channels、缩放因子、偏移因子、eps：
    合计 为 做 计数 用 数据。
    若 合计 等于 0：
        返还 一组：（空）。
    完。
    可变 spatial 为 合计 除以 n_channels。
    若 spatial 小于 1：
        spatial 为 1。
    完。
    可变 结果 为 一组：（空）。
    可变 ch 为 0。
    只要 ch 小于 n_channels：
        可变 cm 为 0。
        可变 ci 为 0。
        只要 ci 小于 spatial：
            cm 为 cm 加 数据 的 第 ch 乘 spatial 加 ci 项。
            ci 为 ci 加 1。
        完。
        cm 为 cm 除以 spatial。
        可变 cv 为 0。
        ci 为 0。
        只要 ci 小于 spatial：
            d 为 数据 的 第 ch 乘 spatial 加 ci 项 减 cm。
            cv 为 cv 加 d 乘 d 除以 1000。
            ci 为 ci 加 1。
        完。
        cv 为 cv 除以 spatial。
        cstd 为 做 _平方根 用 cv 乘 1000 加 eps。
        ci 为 0。
        只要 ci 小于 spatial：
            nv 为 (数据 的 第 ch 乘 spatial 加 ci 项 减 cm) 乘 1000 除以 cstd。
            做 追加 用 结果、nv 乘 缩放因子 除以 1000 加 偏移因子。
            ci 为 ci 加 1。
        完。
        ch 为 ch 加 1。
    完。
    返还 结果。
完。

权重归一化 接受 权重、g_scale：
    n 为 做 计数 用 权重。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 平方和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        平方和 为 平方和 加 权重 的 第 i 项 乘 权重 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 范数 为 做 _平方根 用 平方和。
    若 范数 小于 1：
        范数 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、权重 的 第 i 项 乘 g_scale 除以 范数。
        i 为 i 加 1。
    完。
    返还 结果。
完。

谱归一化估计 接受 权重、维度：
    n 为 做 计数 用 权重。
    若 n 等于 0：
        返还 1。
    完。
    可变 平方和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        平方和 为 平方和 加 权重 的 第 i 项 乘 权重 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 做 _平方根 用 平方和 除以 维度。
完。

nNorm_输入维度 接受 数据：
    返还 做 计数 用 数据。
完。

自适应归一化 接受 数据、norm_type、缩放因子、偏移因子、eps：
    若 norm_type 等于 0：
        返还 做 层归一化计算 用 数据、缩放因子、偏移因子、eps。
    完。
    若 norm_type 等于 1：
        返还 做 均方根归一化 用 数据、缩放因子、eps。
    完。
    若 norm_type 等于 2：
        返还 做 批归一化计算 用 数据、缩放因子、偏移因子、eps、假。
    完。
    若 norm_type 等于 3：
        返还 做 组归一化 用 数据、2、缩放因子、偏移因子、eps。
    完。
    返还 数据。
完。

整数开方RMS 接受 n：
    若 n 小于或等于 0：
        返还 1。
    完。
    可变 x 为 n 除以 2 加 1。
    可变 i 为 0。
    只要 i 小于 40：
        nx 为 (x 加 n 除以 x) 除以 2。
        若 nx 大于或等于 x：
            返还 x。
        完。
        x 为 nx。
        i 为 i 加 1。
    完。
    返还 x。
完。

RMSNorm整数 接受 向量、伽马：
    n 为 做 计数 用 向量。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    可变 平方和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        平方和 为 平方和 加 向量 的 第 i 项 乘 向量 的 第 i 项 除以 1024。
        i 为 i 加 1。
    完。
    均方根值 为 做 整数开方RMS 用 平方和 除以 n。
    若 均方根值 等于 0：
        返还 向量。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        normed 为 向量 的 第 i 项 乘 1024 除以 均方根值。
        可变 _归一值 为 normed。
        若 做 计数 用 伽马 大于 i：
            _归一值 为 normed 乘 伽马 的 第 i 项 除以 1024。
        完。
        做 追加 用 结果、_归一值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

本章 收束。
