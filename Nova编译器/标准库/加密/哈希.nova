《哈希》

引用《..·基础》。

DJB2哈希 接受 文本：
    n 为 做 计数 用 文本。
    可变 哈希 为 5381。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 编码 用 做 子串 用 文本、i、1。
        哈希 为 (哈希 左移 5) 加 哈希 加 c。
        哈希 为 哈希 位与 0x7FFFFFFF。
        i 为 i 加 1。
    完。
    返还 哈希。
完。

FNV1a哈希 接受 文本：
    n 为 做 计数 用 文本。
    可变 哈希 为 2166136261。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 编码 用 做 子串 用 文本、i、1。
        哈希 为 (哈希 位异或 c) 乘 16777619。
        哈希 为 哈希 位与 0x7FFFFFFF。
        i 为 i 加 1。
    完。
    返还 哈希。
完。

整数哈希 接受 值：
    可变 h 为 值。
    h 为 (h 右移 16 位异或 h) 乘 0x45d9f3b。
    h 为 (h 右移 16 位异或 h) 乘 0x45d9f3b。
    h 为 h 右移 16 位异或 h。
    返还 h 位与 0x7FFFFFFF。
完。

校验和 接受 数据、长度：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        和 为 和 加 做 缓冲读 用 数据、i。
        i 为 i 加 1。
    完。
    返还 和 位与 0xFF。
完。

弗莱彻16 接受 数据、长度：
    可变 sum1 为 0。
    可变 总和2 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        sum1 为 (sum1 加 做 缓冲读 用 数据、i) 取余于 255。
        总和2 为 (总和2 加 sum1) 取余于 255。
        i 为 i 加 1。
    完。
    返还 总和2 左移 8 位或 sum1。
完。

xor加密 接受 数据、长度、密钥：
    密钥长 为 做 计数 用 密钥。
    可变 i 为 0。
    只要 i 小于 长度：
        原值 为 做 缓冲读 用 数据、i。
        密钥字节 为 做 编码 用 做 子串 用 密钥、i 取余于 密钥长、1。
        做 缓冲写 用 数据、i、(原值 位异或 密钥字节) 位与 0xFF。
        i 为 i 加 1。
    完。
完。

caesar加密 接受 文本、偏移：
    n 为 做 计数 用 文本。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        可变 c 为 做 编码 用 做 子串 用 文本、i、1。
        若 c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z：
            c 为 (c 减 65 加 偏移) 取余于 26 加 65。
        否则若 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z：
            c 为 (c 减 97 加 偏移) 取余于 26 加 97。
        完。
        结果 为 做 字符串拼接 用 结果、做 字符 用 c。
        i 为 i 加 1。
    完。
    返还 结果。
完。

caesar解密 接受 文本、偏移：
    返还 做 caesar加密 用 文本、26 减 偏移 取余于 26。
完。

base64表 为 「ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/」。

base64编码字符 接受 索引：
    返还 做 子串 用 base64表、索引 取余于 64、1。
完。

随机_创建 接受 种子：
    返还 一组：种子。
完。

随机_下一个 接受 状态：
    状态 的 第 0 项 为 状态 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    返还 状态 的 第 0 项。
完。

随机_范围 接受 状态、最小、最大：
    r 为 做 随机_下一个 用 状态。
    返还 最小 加 r 取余于 (最大 减 最小 加 1)。
完。

随机_列表 接受 状态、长度、最小、最大：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度：
        做 追加 用 结果、做 随机_范围 用 状态、最小、最大。
        i 为 i 加 1。
    完。
    返还 结果。
完。

生成uuid 接受 状态：
    十六进制表 为 「0123456789abcdef」。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 32：
        若 i 等于 8 或者 i 等于 12 或者 i 等于 16 或者 i 等于 20：
            结果 为 做 字符串拼接 用 结果、「-」。
        完。
        r 为 做 随机_范围 用 状态、0、15。
        结果 为 做 字符串拼接 用 结果、做 子串 用 十六进制表、r、1。
        i 为 i 加 1。
    完。
    返还 结果。
完。

循环冗余8 接受 数据、长度：
    可变 校验值 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        校验值 为 校验值 位异或 做 缓冲读 用 数据、i。
        可变 位值 为 0。
        只要 位值 小于 8：
            若 校验值 位与 0x80：
                校验值 为 (校验值 左移 1 位异或 0x07) 位与 0xFF。
            否则：
                校验值 为 校验值 左移 1 位与 0xFF。
            完。
            位值 为 位值 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 校验值。
完。

rle编码 接受 数据、长度：
    可变 结果 为 做 缓冲区 用 长度 乘 2。
    可变 输出位 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        当前 为 做 缓冲读 用 数据、i。
        可变 计数器 为 1。
        只要 i 加 计数器 小于 长度 并且 做 缓冲读 用 数据、i 加 计数器 等于 当前 并且 计数器 小于 255：
            计数器 为 计数器 加 1。
        完。
        做 缓冲写 用 结果、输出位、计数器。
        做 缓冲写 用 结果、输出位 加 1、当前。
        输出位 为 输出位 加 2。
        i 为 i 加 计数器。
    完。
    返还 一组：结果、输出位。
完。

rle解码 接受 数据、长度：
    可变 结果 为 做 缓冲区 用 长度 乘 128。
    可变 输出位 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        次数 为 做 缓冲读 用 数据、i。
        值 为 做 缓冲读 用 数据、i 加 1。
        可变 j 为 0。
        只要 j 小于 次数：
            做 缓冲写 用 结果、输出位、值。
            输出位 为 输出位 加 1。
            j 为 j 加 1。
        完。
        i 为 i 加 2。
    完。
    返还 一组：结果、输出位。
完。

杂音哈希3 接受 文本、种子：
    n 为 做 计数 用 文本。
    可变 h 为 种子。
    c1 为 3432918353。
    c2 为 461845907。
    可变 i 为 0。
    只要 i 小于 n：
        可变 k 为 做 编码 用 做 子串 用 文本、i、1。
        k 为 k 乘 c1 位与 0x7FFFFFFF。
        k 为 (k 左移 15 位或 k 右移 17) 位与 0x7FFFFFFF。
        k 为 k 乘 c2 位与 0x7FFFFFFF。
        h 为 (h 位异或 k) 位与 0x7FFFFFFF。
        h 为 (h 左移 13 位或 h 右移 19) 位与 0x7FFFFFFF。
        h 为 h 乘 5 加 3864292196 位与 0x7FFFFFFF。
        i 为 i 加 1。
    完。
    h 为 h 位异或 n。
    h 为 h 位异或 h 右移 16。
    h 为 h 乘 2246822507 位与 0x7FFFFFFF。
    h 为 h 位异或 h 右移 13。
    h 为 h 乘 3266489909 位与 0x7FFFFFFF。
    h 为 h 位异或 h 右移 16。
    返还 h。
完。

阿德勒32 接受 数据、长度：
    可变 a 为 1。
    可变 b 为 0。
    可变 i 为 0。
    只要 i 小于 长度：
        a 为 (a 加 做 缓冲读 用 数据、i) 取余于 65521。
        b 为 (b 加 a) 取余于 65521。
        i 为 i 加 1。
    完。
    返还 b 左移 16 位或 a。
完。

vigenere加密 接受 文本、密钥：
    n 为 做 计数 用 文本。
    kn 为 做 计数 用 密钥。
    可变 结果 为 「」。
    可变 i 为 0。
    只要 i 小于 n：
        可变 c 为 做 编码 用 做 子串 用 文本、i、1。
        k 为 做 编码 用 做 子串 用 密钥、i 取余于 kn、1。
        若 c 大于或等于 字符_大写A 并且 c 小于或等于 字符_大写Z：
            c 为 (c 减 65 加 k 减 65) 取余于 26 加 65。
        否则若 c 大于或等于 字符_小写a 并且 c 小于或等于 字符_小写z：
            c 为 (c 减 97 加 k 减 97) 取余于 26 加 97。
        完。
        结果 为 做 字符串拼接 用 结果、做 字符 用 c。
        i 为 i 加 1。
    完。
    返还 结果。
完。

洗牌 接受 列表、状态：
    n 为 做 计数 用 列表。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 n 减 1。
    只要 i 大于 0：
        j 为 做 随机_范围 用 状态、0、i。
        临时 为 结果 的 第 i 项。
        结果 的 第 i 项 为 结果 的 第 j 项。
        结果 的 第 j 项 为 临时。
        i 为 i 减 1。
    完。
    返还 结果。
完。

哈希组合 接受 h1、h2：
    返还 (h1 位异或 h2 乘 2654435769 加 (h1 左移 6) 加 (h1 右移 2)) 位与 0x7FFFFFFF。
完。

本章 收束。
