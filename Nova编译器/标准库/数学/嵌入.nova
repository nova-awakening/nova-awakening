《嵌入》

引用《..·基础》。

引用《..·..·运行时·原语桩》。

嵌入精度 为 1000。

嵌入_创建 接受 词汇量、嵌入维度、种子：
    可变 权重 为 一组：词汇量、嵌入维度。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 词汇量 乘 嵌入维度：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        w 为 s 取余于 200 减 100。
        做 追加 用 权重、w。
        i 为 i 加 1。
    完。
    返还 权重。
完。

嵌入_查找 接受 嵌入表、token_id：
    维度 为 嵌入表 的 第 1 项。
    可变 向量 为 一组：（空）。
    基 为 2 加 token_id 乘 维度。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 向量、嵌入表 的 第 基 加 i 项。
        i 为 i 加 1。
    完。
    返还 向量。
完。

嵌入_序列 接受 嵌入表、token序列：
    n 为 做 计数 用 token序列。
    维度 为 嵌入表 的 第 1 项。
    可变 结果 为 一组：n、维度。
    可变 i 为 0。
    只要 i 小于 n：
        向量 为 做 嵌入_查找 用 嵌入表、token序列 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 维度：
            做 追加 用 结果、向量 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

嵌入_更新 接受 嵌入表、token_id、梯度、学习率：
    维度 为 嵌入表 的 第 1 项。
    基 为 2 加 token_id 乘 维度。
    可变 i 为 0。
    只要 i 小于 维度：
        嵌入表 的 第 基 加 i 项 为 嵌入表 的 第 基 加 i 项 减 学习率 乘 梯度 的 第 i 项 除以 嵌入精度。
        i 为 i 加 1。
    完。
完。

字符分词 接受 文本：
    n 为 做 计数 用 文本。
    可变 词元列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 词元列、做 编码 用 做 子串 用 文本、i、1。
        i 为 i 加 1。
    完。
    返还 词元列。
完。

空格分词 接受 文本：
    n 为 做 计数 用 文本。
    可变 当前词 为 「」。
    可变 词元列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        c 为 做 子串 用 文本、i、1。
        若 做 编码 用 c 等于 字符_空格 或者 做 编码 用 c 等于 字符_换行 或者 做 编码 用 c 等于 字符_制表：
            若 做 计数 用 当前词 大于 0：
                做 追加 用 词元列、当前词。
                当前词 为 「」。
            完。
        否则：
            当前词 为 做 字符串拼接 用 当前词、c。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 当前词 大于 0：
        做 追加 用 词元列、当前词。
    完。
    返还 词元列。
完。

构建词汇表 接受 文本列表：
    可变 词表 为 做 字典。
    可变 id 为 0。
    做 字典设 用 词表、「<PAD>」、id。
    id 为 id 加 1。
    做 字典设 用 词表、「<UNK>」、id。
    id 为 id 加 1。
    做 字典设 用 词表、「<BOS>」、id。
    id 为 id 加 1。
    做 字典设 用 词表、「<EOS>」、id。
    id 为 id 加 1。
    可变 ti 为 0。
    只要 ti 小于 做 计数 用 文本列表：
        词元列 为 做 空格分词 用 文本列表 的 第 ti 项。
        可变 j 为 0。
        只要 j 小于 做 计数 用 词元列：
            若 做 字典取 用 词表、词元列 的 第 j 项 等于 空：
                做 字典设 用 词表、词元列 的 第 j 项、id。
                id 为 id 加 1。
            完。
            j 为 j 加 1。
        完。
        ti 为 ti 加 1。
    完。
    返还 一组：词表、id。
完。

文本编码 接受 文本、词表：
    词元列 为 做 空格分词 用 文本。
    可变 ids 为 一组：（空）。
    unk_id 为 做 字典取 用 词表、「<UNK>」。
    可变 i 为 0。
    只要 i 小于 做 计数 用 词元列：
        id 为 做 字典取 用 词表、词元列 的 第 i 项。
        若 id 不等于 空：
            做 追加 用 ids、id。
        否则：
            做 追加 用 ids、unk_id。
        完。
        i 为 i 加 1。
    完。
    返还 ids。
完。

嵌入_相似度 接受 v1、v2：
    n 为 做 计数 用 v1。
    可变 点积 为 0。
    可变 范数1 为 0。
    可变 范数2 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        点积 为 点积 加 v1 的 第 i 项 乘 v2 的 第 i 项。
        范数1 为 范数1 加 v1 的 第 i 项 乘 v1 的 第 i 项。
        范数2 为 范数2 加 v2 的 第 i 项 乘 v2 的 第 i 项。
        i 为 i 加 1。
    完。
    分母 为 做 嵌入_开方 用 范数1 乘 做 嵌入_开方 用 范数2。
    若 分母 等于 0：
        返还 0。
    完。
    返还 点积 乘 嵌入精度 除以 分母。
完。

填充序列 接受 序列、最大长度、填充值：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大长度：
        若 i 小于 做 计数 用 序列：
            做 追加 用 结果、序列 的 第 i 项。
        否则：
            做 追加 用 结果、填充值。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

截断序列 接受 序列、最大长度：
    若 做 计数 用 序列 小于或等于 最大长度：
        返还 序列。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 最大长度：
        做 追加 用 结果、序列 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

嵌入_开方 接受 n：
    返还 做 开方 用 n。
完。

位置嵌入_创建 接受 最大长度、嵌入维度：
    可变 pe 为 一组：最大长度、嵌入维度。
    可变 位置 为 0。
    只要 位置 小于 最大长度：
        可变 i 为 0。
        只要 i 小于 嵌入维度：
            周期 为 (i 加 2) 乘 100。
            角度 为 位置 乘 嵌入精度 除以 周期。
            若 i 取余于 2 等于 0：
                做 追加 用 pe、做 嵌入_sin近似 用 角度。
            否则：
                做 追加 用 pe、做 嵌入_cos近似 用 角度。
            完。
            i 为 i 加 1。
        完。
        位置 为 位置 加 1。
    完。
    返还 pe。
完。

嵌入_加位置 接受 嵌入矩阵、位置编码：
    序列 为 嵌入矩阵 的 第 0 项。
    维度 为 嵌入矩阵 的 第 1 项。
    可变 i 为 0。
    只要 i 小于 序列 乘 维度：
        嵌入矩阵 的 第 2 加 i 项 为 嵌入矩阵 的 第 2 加 i 项 加 位置编码 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
完。

可学习位置嵌入_创建 接受 最大长度、嵌入维度、种子：
    可变 pe 为 一组：最大长度、嵌入维度。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 最大长度 乘 嵌入维度：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        做 追加 用 pe、s 取余于 200 减 100。
        i 为 i 加 1。
    完。
    返还 pe。
完。

bert嵌入 接受 token嵌入表、segment嵌入表、位置编码、token序列、segment序列：
    序列长 为 做 计数 用 token序列。
    维度 为 token嵌入表 的 第 1 项。
    可变 结果 为 一组：序列长、维度。
    可变 i 为 0。
    只要 i 小于 序列长：
        tok_vec 为 做 嵌入_查找 用 token嵌入表、token序列 的 第 i 项。
        seg_vec 为 做 嵌入_查找 用 segment嵌入表、segment序列 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 维度：
            做 追加 用 结果、tok_vec 的 第 j 项 加 seg_vec 的 第 j 项 加 位置编码 的 第 2 加 i 乘 维度 加 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

嵌入_最近邻 接受 嵌入表、查询向量、k：
    词汇量 为 嵌入表 的 第 0 项。
    可变 相似度列表 为 一组：（空）。
    可变 索引列表 为 一组：（空）。
    可变 vi 为 0。
    只要 vi 小于 词汇量：
        向量 为 做 嵌入_查找 用 嵌入表、vi。
        相似度 为 做 嵌入_相似度 用 查询向量、向量。
        做 追加 用 相似度列表、相似度。
        做 追加 用 索引列表、vi。
        vi 为 vi 加 1。
    完。
    可变 top_ids 为 一组：（空）。
    可变 ki 为 0。
    只要 ki 小于 k 并且 ki 小于 词汇量：
        可变 最佳idx 为 0。
        可变 最佳sim 为 0 减 0x7FFFFFFF。
        可变 si 为 0。
        只要 si 小于 词汇量：
            若 相似度列表 的 第 si 项 大于 最佳sim：
                最佳sim 为 相似度列表 的 第 si 项。
                最佳idx 为 si。
            完。
            si 为 si 加 1。
        完。
        做 追加 用 top_ids、一组：最佳idx、最佳sim。
        相似度列表 的 第 最佳idx 项 为 0 减 0x7FFFFFFF。
        ki 为 ki 加 1。
    完。
    返还 top_ids。
完。

嵌入_向量运算 接受 嵌入表、正id列表、负id列表：
    维度 为 嵌入表 的 第 1 项。
    可变 结果 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 结果、0。
        d 为 d 加 1。
    完。
    可变 pi 为 0。
    只要 pi 小于 做 计数 用 正id列表：
        向量 为 做 嵌入_查找 用 嵌入表、正id列表 的 第 pi 项。
        d 为 0。
        只要 d 小于 维度：
            结果 的 第 d 项 为 结果 的 第 d 项 加 向量 的 第 d 项。
            d 为 d 加 1。
        完。
        pi 为 pi 加 1。
    完。
    可变 ni 为 0。
    只要 ni 小于 做 计数 用 负id列表：
        向量 为 做 嵌入_查找 用 嵌入表、负id列表 的 第 ni 项。
        d 为 0。
        只要 d 小于 维度：
            结果 的 第 d 项 为 结果 的 第 d 项 减 向量 的 第 d 项。
            d 为 d 加 1。
        完。
        ni 为 ni 加 1。
    完。
    返还 结果。
完。

嵌入_归一化 接受 向量：
    n 为 做 计数 用 向量。
    可变 范数sq 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        范数sq 为 范数sq 加 向量 的 第 i 项 乘 向量 的 第 i 项。
        i 为 i 加 1。
    完。
    范数 为 做 嵌入_开方 用 范数sq。
    若 范数 等于 0：
        返还 向量。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、向量 的 第 i 项 乘 嵌入精度 除以 范数。
        i 为 i 加 1。
    完。
    返还 结果。
完。

ngram分词 接受 文本、n：
    文本长 为 做 计数 用 文本。
    可变 grams 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 文本长 减 n：
        做 追加 用 grams、做 子串 用 文本、i、n。
        i 为 i 加 1。
    完。
    返还 grams。
完。

嵌入_平均池化 接受 嵌入矩阵、有效长度：
    维度 为 嵌入矩阵 的 第 1 项。
    可变 结果 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 结果、0。
        d 为 d 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 有效长度：
        d 为 0。
        只要 d 小于 维度：
            结果 的 第 d 项 为 结果 的 第 d 项 加 嵌入矩阵 的 第 2 加 i 乘 维度 加 d 项。
            d 为 d 加 1。
        完。
        i 为 i 加 1。
    完。
    d 为 0。
    只要 d 小于 维度：
        结果 的 第 d 项 为 结果 的 第 d 项 除以 有效长度。
        d 为 d 加 1。
    完。
    返还 结果。
完。

嵌入_最大池化 接受 嵌入矩阵、有效长度：
    维度 为 嵌入矩阵 的 第 1 项。
    可变 结果 为 一组：（空）。
    可变 d 为 0。
    只要 d 小于 维度：
        做 追加 用 结果、嵌入矩阵 的 第 2 加 d 项。
        d 为 d 加 1。
    完。
    可变 i 为 1。
    只要 i 小于 有效长度：
        d 为 0。
        只要 d 小于 维度：
            v 为 嵌入矩阵 的 第 2 加 i 乘 维度 加 d 项。
            若 v 大于 结果 的 第 d 项：
                结果 的 第 d 项 为 v。
            完。
            d 为 d 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

嵌入_dropout 接受 向量、比率、种子：
    n 为 做 计数 用 向量。
    可变 结果 为 一组：（空）。
    可变 s 为 种子。
    缩放 为 嵌入精度 乘 嵌入精度 除以 (嵌入精度 减 比率)。
    可变 i 为 0。
    只要 i 小于 n：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        若 s 取余于 嵌入精度 小于 比率：
            做 追加 用 结果、0。
        否则：
            做 追加 用 结果、向量 的 第 i 项 乘 缩放 除以 嵌入精度。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

嵌入_线性 接受 输入、权重行、权重列、权重：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 权重行：
        可变 和 为 0。
        可变 j 为 0。
        只要 j 小于 权重列：
            若 j 小于 做 计数 用 输入：
                和 为 和 加 输入 的 第 j 项 乘 权重 的 第 i 乘 权重列 加 j 项 除以 嵌入精度。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

tfidf_向量 接受 文档词频、文档总数、含词文档数：
    n 为 做 计数 用 文档词频。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        词频 为 文档词频 的 第 i 项。
        可变 逆文档频率 为 嵌入精度。
        若 含词文档数 的 第 i 项 大于 0：
            逆文档频率 为 文档总数 乘 嵌入精度 除以 含词文档数 的 第 i 项。
        完。
        做 追加 用 结果、词频 乘 逆文档频率 除以 嵌入精度。
        i 为 i 加 1。
    完。
    返还 结果。
完。

嵌入_sin近似 接受 x：
    可变 角 为 x 取余于 6283。
    若 角 小于 0：
        角 为 角 加 6283。
    完。
    若 角 小于 1571：
        返还 角 乘 嵌入精度 除以 1571。
    完。
    若 角 小于 3142：
        返还 (3142 减 角) 乘 嵌入精度 除以 1571。
    完。
    若 角 小于 4712：
        返还 0 减 (角 减 3142) 乘 嵌入精度 除以 1571。
    完。
    返还 0 减 (6283 减 角) 乘 嵌入精度 除以 1571。
完。

嵌入_cos近似 接受 x：
    返还 做 嵌入_sin近似 用 x 加 1571。
完。

可变 _嵌入_创建数 为 0。

可变 _嵌入_查找数 为 0。

可变 _嵌入_零向量数 为 0。

嵌入_记录创建 接受 零参：
    _嵌入_创建数 为 _嵌入_创建数 加 1。
完。

嵌入_记录查找 接受 零参：
    _嵌入_查找数 为 _嵌入_查找数 加 1。
完。

嵌入_记录零向量 接受 零参：
    _嵌入_零向量数 为 _嵌入_零向量数 加 1。
完。

嵌入_质量评估 接受 零参：
    若 _嵌入_创建数 等于 0：
        返还 「无嵌入数据」。
    完。
    零率 为 _嵌入_零向量数 乘 100 除以 _嵌入_创建数。
    若 零率 大于 30：
        返还 「警告: 大量零向量,词汇覆盖不足」。
    完。
    若 零率 大于 10：
        返还 「注意: 部分零向量」。
    完。
    返还 「嵌入质量良好」。
完。

嵌入_命中率 接受 零参：
    若 _嵌入_查找数 等于 0：
        返还 0。
    完。
    命中 为 _嵌入_查找数 减 _嵌入_零向量数。
    返还 命中 乘 100 除以 _嵌入_查找数。
完。

嵌入_重置 接受 零参：
    _嵌入_创建数 为 0。
    _嵌入_查找数 为 0。
    _嵌入_零向量数 为 0。
完。

本章 收束。
