《遗传算法》

引用《..·基础》。

遗传精度 为 1000。

个体_创建 接受 基因长度、最小值、最大值、种子：
    可变 基因 为 一组：（空）。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 基因长度：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        做 追加 用 基因、最小值 加 s 取余于 (最大值 减 最小值 加 1)。
        i 为 i 加 1。
    完。
    返还 一组：基因、0。
完。

种群_创建 接受 大小、基因长度、最小值、最大值、种子：
    可变 种群 为 一组：（空）。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 大小：
        s 为 s 加 i 乘 31 加 7。
        做 追加 用 种群、做 个体_创建 用 基因长度、最小值、最大值、s。
        i 为 i 加 1。
    完。
    返还 种群。
完。

种群_评估 接受 种群、适应度函数：
    可变 i 为 0。
    只要 i 小于 做 计数 用 种群：
        种群 的 第 i 项 的 第 1 项 为 做 适应度函数 用 种群 的 第 i 项 的 第 0 项。
        i 为 i 加 1。
    完。
完。

种群_最优 接受 种群：
    可变 最优i 为 0。
    可变 最优适应 为 种群 的 第 0 项 的 第 1 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 种群：
        若 种群 的 第 i 项 的 第 1 项 大于 最优适应：
            最优适应 为 种群 的 第 i 项 的 第 1 项。
            最优i 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 种群 的 第 最优i 项。
完。

锦标赛选择 接受 种群、锦标赛大小、随机状态：
    n 为 做 计数 用 种群。
    可变 最优i 为 负 1。
    可变 最优适应 为 0 减 0x7FFFFFFF。
    可变 i 为 0。
    只要 i 小于 锦标赛大小：
        随机状态 的 第 0 项 为 随机状态 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        索引 为 随机状态 的 第 0 项 取余于 n。
        若 种群 的 第 索引 项 的 第 1 项 大于 最优适应：
            最优适应 为 种群 的 第 索引 项 的 第 1 项。
            最优i 为 索引。
        完。
        i 为 i 加 1。
    完。
    返还 种群 的 第 最优i 项。
完。

轮盘赌选择 接受 种群、随机状态：
    n 为 做 计数 用 种群。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 f 为 种群 的 第 i 项 的 第 1 项。
        若 f 小于 0：
            f 为 0。
        完。
        总和 为 总和 加 f。
        i 为 i 加 1。
    完。
    若 总和 等于 0：
        总和 为 1。
    完。
    随机状态 的 第 0 项 为 随机状态 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    可变 阈值 为 随机状态 的 第 0 项 取余于 总和。
    可变 累计 为 0。
    i 为 0。
    只要 i 小于 n：
        可变 f 为 种群 的 第 i 项 的 第 1 项。
        若 f 小于 0：
            f 为 0。
        完。
        累计 为 累计 加 f。
        若 累计 大于 阈值：
            返还 种群 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 种群 的 第 n 减 1 项。
完。

单点交叉 接受 父1、父2、随机状态：
    基因长 为 做 计数 用 父1 的 第 0 项。
    随机状态 的 第 0 项 为 随机状态 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    交叉点 为 1 加 随机状态 的 第 0 项 取余于 (基因长 减 1)。
    可变 子1基因 为 一组：（空）。
    可变 子2基因 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 基因长：
        若 i 小于 交叉点：
            做 追加 用 子1基因、父1 的 第 0 项 的 第 i 项。
            做 追加 用 子2基因、父2 的 第 0 项 的 第 i 项。
        否则：
            做 追加 用 子1基因、父2 的 第 0 项 的 第 i 项。
            做 追加 用 子2基因、父1 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：一组：子1基因、0、一组：子2基因、0。
完。

均匀交叉 接受 父1、父2、随机状态：
    基因长 为 做 计数 用 父1 的 第 0 项。
    可变 子基因 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 基因长：
        随机状态 的 第 0 项 为 随机状态 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        若 随机状态 的 第 0 项 取余于 2 等于 0：
            做 追加 用 子基因、父1 的 第 0 项 的 第 i 项。
        否则：
            做 追加 用 子基因、父2 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：子基因、0。
完。

随机变异 接受 个体、变异率、最小值、最大值、随机状态：
    基因长 为 做 计数 用 个体 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 基因长：
        随机状态 的 第 0 项 为 随机状态 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        若 随机状态 的 第 0 项 取余于 遗传精度 小于 变异率：
            随机状态 的 第 0 项 为 随机状态 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
            个体 的 第 0 项 的 第 i 项 为 最小值 加 随机状态 的 第 0 项 取余于 (最大值 减 最小值 加 1)。
        完。
        i 为 i 加 1。
    完。
完。

高斯变异 接受 个体、变异率、步长、随机状态：
    基因长 为 做 计数 用 个体 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 基因长：
        随机状态 的 第 0 项 为 随机状态 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        若 随机状态 的 第 0 项 取余于 遗传精度 小于 变异率：
            随机状态 的 第 0 项 为 随机状态 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
            偏移 为 随机状态 的 第 0 项 取余于 (2 乘 步长 加 1) 减 步长。
            个体 的 第 0 项 的 第 i 项 为 个体 的 第 0 项 的 第 i 项 加 偏移。
        完。
        i 为 i 加 1。
    完。
完。

遗传算法 接受 种群大小、基因长度、最小值、最大值、适应度函数、代数、变异率、种子：
    可变 随机 为 一组：种子。
    可变 种群 为 做 种群_创建 用 种群大小、基因长度、最小值、最大值、种子。
    做 种群_评估 用 种群、适应度函数。
    可变 代数 为 0。
    只要 代数 小于 代数：
        可变 新种群 为 一组：（空）。
        最优 为 做 种群_最优 用 种群。
        可变 精英基因 为 一组：（空）。
        只要 ei 小于 基因长度：
            做 追加 用 精英基因、最优 的 第 0 项 的 第 ei 项。
            ei 为 ei 加 1。
        完。
        做 追加 用 新种群、一组：精英基因、最优 的 第 1 项。
        只要 做 计数 用 新种群 小于 种群大小：
            父1 为 做 锦标赛选择 用 种群、3、随机。
            父2 为 做 锦标赛选择 用 种群、3、随机。
            后代 为 做 单点交叉 用 父1、父2、随机。
            做 随机变异 用 后代 的 第 0 项、变异率、最小值、最大值、随机。
            做 追加 用 新种群、后代 的 第 0 项。
            若 做 计数 用 新种群 小于 种群大小：
                做 随机变异 用 后代 的 第 1 项、变异率、最小值、最大值、随机。
                做 追加 用 新种群、后代 的 第 1 项。
            完。
        完。
        种群 为 新种群。
        做 种群_评估 用 种群、适应度函数。
        代数 为 代数 加 1。
    完。
    返还 做 种群_最优 用 种群。
完。

粒子群优化 接受 维度、粒子数、最小值、最大值、适应度函数、迭代次数、种子：
    可变 随机 为 一组：种子。
    可变 位置 为 一组：（空）。
    可变 速度 为 一组：（空）。
    可变 个人最优 为 一组：（空）。
    可变 个人最优值 为 一组：（空）。
    可变 全局最优 为 一组：（空）。
    可变 全局最优值 为 0 减 0x7FFFFFFF。
    可变 i 为 0。
    只要 i 小于 粒子数：
        可变 位置 为 一组：（空）。
        可变 vel 为 一组：（空）。
        可变 d 为 0。
        只要 d 小于 维度：
            随机 的 第 0 项 为 随机 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
            做 追加 用 位置、最小值 加 随机 的 第 0 项 取余于 (最大值 减 最小值 加 1)。
            随机 的 第 0 项 为 随机 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
            做 追加 用 vel、随机 的 第 0 项 取余于 201 减 100。
            d 为 d 加 1。
        完。
        做 追加 用 位置、位置。
        做 追加 用 速度、vel。
        可变 pb 为 一组：（空）。
        d 为 0。
        只要 d 小于 维度：
            做 追加 用 pb、位置 的 第 d 项。
            d 为 d 加 1。
        完。
        做 追加 用 个人最优、pb。
        f 为 做 适应度函数 用 位置。
        做 追加 用 个人最优值、f。
        若 f 大于 全局最优值：
            全局最优值 为 f。
            全局最优 为 pb。
        完。
        i 为 i 加 1。
    完。
    可变 迭代 为 0。
    只要 迭代 小于 迭代次数：
        i 为 0。
        只要 i 小于 粒子数：
            可变 d2 为 0。
            只要 d2 小于 维度：
                随机 的 第 0 项 为 随机 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
                r1 为 随机 的 第 0 项 取余于 遗传精度。
                随机 的 第 0 项 为 随机 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
                r2 为 随机 的 第 0 项 取余于 遗传精度。
                速度 的 第 i 项 的 第 d2 项 为 700 乘 速度 的 第 i 项 的 第 d2 项 除以 遗传精度 加 2 乘 r1 乘 (个人最优 的 第 i 项 的 第 d2 项 减 位置 的 第 i 项 的 第 d2 项) 除以 遗传精度 加 2 乘 r2 乘 (全局最优 的 第 d2 项 减 位置 的 第 i 项 的 第 d2 项) 除以 遗传精度。
                位置 的 第 i 项 的 第 d2 项 为 位置 的 第 i 项 的 第 d2 项 加 速度 的 第 i 项 的 第 d2 项 除以 遗传精度。
                若 位置 的 第 i 项 的 第 d2 项 小于 最小值：
                    位置 的 第 i 项 的 第 d2 项 为 最小值。
                完。
                若 位置 的 第 i 项 的 第 d2 项 大于 最大值：
                    位置 的 第 i 项 的 第 d2 项 为 最大值。
                完。
                d2 为 d2 加 1。
            完。
            f2 为 做 适应度函数 用 位置 的 第 i 项。
            若 f2 大于 个人最优值 的 第 i 项：
                个人最优值 的 第 i 项 为 f2。
                可变 d3 为 0。
                只要 d3 小于 维度：
                    个人最优 的 第 i 项 的 第 d3 项 为 位置 的 第 i 项 的 第 d3 项。
                    d3 为 d3 加 1。
                完。
            完。
            若 f2 大于 全局最优值：
                全局最优值 为 f2。
                可变 d4 为 0。
                只要 d4 小于 维度：
                    全局最优 的 第 d4 项 为 位置 的 第 i 项 的 第 d4 项。
                    d4 为 d4 加 1。
                完。
            完。
            i 为 i 加 1。
        完。
        迭代 为 迭代 加 1。
    完。
    返还 一组：全局最优、全局最优值。
完。

两点交叉 接受 父1、父2、随机状态：
    基因长 为 做 计数 用 父1 的 第 0 项。
    随机状态 的 第 0 项 为 随机状态 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    可变 p1 为 1 加 随机状态 的 第 0 项 取余于 (基因长 减 2)。
    随机状态 的 第 0 项 为 随机状态 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
    可变 p2 为 1 加 随机状态 的 第 0 项 取余于 (基因长 减 2)。
    若 p1 大于 p2：
        t 为 p1。
        p1 为 p2。
        p2 为 t。
    完。
    可变 子基因 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 基因长：
        若 i 大于或等于 p1 并且 i 小于 p2：
            做 追加 用 子基因、父2 的 第 0 项 的 第 i 项。
        否则：
            做 追加 用 子基因、父1 的 第 0 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 一组：子基因、0。
完。

自适应变异率 接受 种群、基础变异率：
    n 为 做 计数 用 种群。
    若 n 小于 2：
        返还 基础变异率。
    完。
    基因长 为 做 计数 用 种群 的 第 0 项 的 第 0 项。
    可变 总差异 为 0。
    可变 比较数 为 0。
    可变 i 为 0。
    只要 i 小于 n 并且 i 小于 10：
        可变 j 为 i 加 1。
        只要 j 小于 n 并且 j 小于 10：
            可变 差 为 0。
            可变 g 为 0。
            只要 g 小于 基因长：
                若 种群 的 第 i 项 的 第 0 项 的 第 g 项 不等于 种群 的 第 j 项 的 第 0 项 的 第 g 项：
                    差 为 差 加 1。
                完。
                g 为 g 加 1。
            完。
            总差异 为 总差异 加 差 乘 遗传精度 除以 基因长。
            比较数 为 比较数 加 1。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 比较数 等于 0：
        返还 基础变异率。
    完。
    多样性 为 总差异 除以 比较数。
    若 多样性 小于 200：
        返还 基础变异率 乘 3。
    完。
    若 多样性 小于 500：
        返还 基础变异率 乘 2。
    完。
    返还 基础变异率。
完。

精英保留 接受 种群、精英数：
    可变 ei 为 0。
    n 为 做 计数 用 种群。
    可变 精英 为 一组：（空）。
    可变 已选 为 一组：（空）。
    只要 ei 小于 n：
        做 追加 用 已选、假。
        ei 为 ei 加 1。
    完。
    ei 为 0。
    只要 ei 小于 精英数 并且 ei 小于 n：
        可变 最优idx 为 0 减 1。
        可变 最优适应 为 0 减 0x7FFFFFFF。
        可变 i 为 0。
        只要 i 小于 n：
            若 已选 的 第 i 项 等于 假 并且 种群 的 第 i 项 的 第 1 项 大于 最优适应：
                最优适应 为 种群 的 第 i 项 的 第 1 项。
                最优idx 为 i。
            完。
            i 为 i 加 1。
        完。
        若 最优idx 大于或等于 0：
            做 追加 用 精英、种群 的 第 最优idx 项。
            已选 的 第 最优idx 项 为 真。
        完。
        ei 为 ei 加 1。
    完。
    返还 精英。
完。

种群_多样性 接受 种群：
    n 为 做 计数 用 种群。
    若 n 小于 2：
        返还 0。
    完。
    基因长 为 做 计数 用 种群 的 第 0 项 的 第 0 项。
    可变 总差 为 0。
    可变 对数 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 i 加 1。
        只要 j 小于 n：
            可变 g 为 0。
            只要 g 小于 基因长：
                可变 d 为 种群 的 第 i 项 的 第 0 项 的 第 g 项 减 种群 的 第 j 项 的 第 0 项 的 第 g 项。
                若 d 小于 0：
                    d 为 0 减 d。
                完。
                总差 为 总差 加 d。
                g 为 g 加 1。
            完。
            对数 为 对数 加 1。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 对数 等于 0：
        返还 0。
    完。
    返还 总差 除以 (对数 乘 基因长)。
完。

sbx交叉 接受 父1、父2、eta、随机：
    基因长 为 做 计数 用 父1 的 第 0 项。
    可变 子基因 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 基因长：
        随机 的 第 0 项 为 随机 的 第 0 项 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        u 为 随机 的 第 0 项 取余于 1000。
        可变 动量率 为 0。
        若 u 小于 500：
            动量率 为 遗传精度 加 u 乘 eta 除以 500。
        否则：
            动量率 为 遗传精度 减 (u 减 500) 乘 eta 除以 500。
        完。
        v 为 父1 的 第 0 项 的 第 i 项 乘 (遗传精度 加 动量率) 除以 (2 乘 遗传精度) 加 父2 的 第 0 项 的 第 i 项 乘 (遗传精度 减 动量率) 除以 (2 乘 遗传精度)。
        做 追加 用 子基因、v。
        i 为 i 加 1。
    完。
    返还 一组：子基因、0。
完。

非支配排序 接受 种群、目标函数列表：
    n 为 做 计数 用 种群。
    目标数 为 做 计数 用 目标函数列表。
    可变 等级 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 等级、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 n：
            若 i 不等于 j：
                可变 j更好 为 0。
                可变 i更好 为 0。
                可变 m 为 0。
                只要 m 小于 目标数：
                    fi 为 做 目标函数列表 的 第 m 项 用 种群 的 第 i 项。
                    fj 为 做 目标函数列表 的 第 m 项 用 种群 的 第 j 项。
                    若 fj 大于 fi：
                        j更好 为 j更好 加 1。
                    完。
                    若 fi 大于 fj：
                        i更好 为 i更好 加 1。
                    完。
                    m 为 m 加 1。
                完。
                若 j更好 等于 目标数 并且 i更好 等于 0：
                    等级 的 第 i 项 为 等级 的 第 i 项 加 1。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 等级。
完。

遗传算法_v2 接受 种群大小、基因长度、最小值、最大值、适应度函数、代数、基础变异率、精英数、种子：
    可变 ei 为 0。
    可变 随机 为 一组：种子。
    可变 种群 为 做 种群_创建 用 种群大小、基因长度、最小值、最大值、种子。
    做 种群_评估 用 种群、适应度函数。
    可变 代数 为 0。
    只要 代数 小于 代数：
        可变 新种群 为 一组：（空）。
        精英列表 为 做 精英保留 用 种群、精英数。
        只要 ei 小于 做 计数 用 精英列表：
            可变 拷贝 为 一组：（空）。
            可变 gi 为 0。
            只要 gi 小于 基因长度：
                做 追加 用 拷贝、精英列表 的 第 ei 项 的 第 0 项 的 第 gi 项。
                gi 为 gi 加 1。
            完。
            做 追加 用 新种群、一组：拷贝、精英列表 的 第 ei 项 的 第 1 项。
            ei 为 ei 加 1。
        完。
        变异率 为 做 自适应变异率 用 种群、基础变异率。
        只要 做 计数 用 新种群 小于 种群大小：
            父1 为 做 锦标赛选择 用 种群、3、随机。
            父2 为 做 锦标赛选择 用 种群、3、随机。
            子 为 做 两点交叉 用 父1、父2、随机。
            做 高斯变异 用 子、变异率、最大值 除以 10、随机。
            做 追加 用 新种群、子。
        完。
        种群 为 新种群。
        做 种群_评估 用 种群、适应度函数。
        代数 为 代数 加 1。
    完。
    返还 做 种群_最优 用 种群。
完。

可变 _遗传_代数 为 0。

可变 _遗传_最优适应度 为 0。

遗传_记录 接受 代数、适应度：
    _遗传_代数 为 代数。
    若 适应度 大于 _遗传_最优适应度：
        _遗传_最优适应度 为 适应度。
    完。
完。

遗传_收敛评估 接受 零参：
    若 _遗传_代数 小于 10：
        返还 「进化初期」。
    完。
    若 _遗传_代数 小于 100：
        返还 「进化中期」。
    完。
    返还 「进化后期(检查是否过早收敛)」。
完。

本章 收束。
