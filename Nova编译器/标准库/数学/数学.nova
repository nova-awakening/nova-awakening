《数学》

引用《..·基础》。

数学_绝对值 接受 n：
    若 n 小于 0：
        返还 0 减 n。
    完。
    返还 n。
完。

数学_最小 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

数学_最大 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

数学_钳制 接受 n、lo、hi：
    返还 做 数学_最大 用 lo、做 数学_最小 用 n、hi。
完。

数学_符号 接受 n：
    若 n 大于 0：
        返还 1。
    完。
    若 n 小于 0：
        返还 负 1。
    完。
    返还 0。
完。

数学_GCD 接受 a、b：
    可变 x 为 做 数学_绝对值 用 a。
    可变 y 为 做 数学_绝对值 用 b。
    只要 y 不等于 0：
        t 为 y。
        y 为 x 取余于 y。
        x 为 t。
    完。
    返还 x。
完。

数学_LCM 接受 a、b：
    若 a 等于 0：
        返还 0。
    完。
    若 b 等于 0：
        返还 0。
    完。
    返还 做 数学_绝对值 用 a 除以 做 数学_GCD 用 a、b 乘 做 数学_绝对值 用 b。
完。

数学_整除吗 接受 a、b：
    若 b 等于 0：
        返还 假。
    完。
    返还 a 取余于 b 等于 0。
完。

数学_幂 接受 底、指：
    若 指 等于 0：
        返还 1。
    完。
    若 指 小于 0：
        返还 0。
    完。
    可变 结果 为 1。
    可变 b 为 底。
    可变 e 为 指。
    只要 e 不等于 0：
        若 e 取余于 2 等于 1：
            结果 为 结果 乘 b。
        完。
        b 为 b 乘 b。
        e 为 e 除以 2。
    完。
    返还 结果。
完。

数学_模幂 接受 底、指、模：
    若 模 等于 1：
        返还 0。
    完。
    可变 结果 为 1。
    可变 b 为 底 取余于 模。
    可变 e 为 指。
    只要 e 不等于 0：
        若 e 取余于 2 等于 1：
            结果 为 结果 乘 b 取余于 模。
        完。
        b 为 b 乘 b 取余于 模。
        e 为 e 除以 2。
    完。
    返还 结果。
完。

数学_整数平方根 接受 n：
    若 n 小于 0：
        返还 负 1。
    完。
    若 n 小于 2：
        返还 n。
    完。
    可变 x 为 n 除以 2。
    可变 y 为 (x 加 n 除以 x) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

数学_log2 接受 n：
    若 n 小于或等于 0：
        返还 负 1。
    完。
    可变 结果 为 0。
    可变 v 为 n。
    只要 v 大于 1：
        v 为 v 除以 2。
        结果 为 结果 加 1。
    完。
    返还 结果。
完。

数学_log10 接受 n：
    若 n 小于或等于 0：
        返还 负 1。
    完。
    可变 结果 为 0。
    可变 v 为 n。
    只要 v 大于或等于 10：
        v 为 v 除以 10。
        结果 为 结果 加 1。
    完。
    返还 结果。
完。

数学_是2的幂 接受 n：
    若 n 小于或等于 0：
        返还 假。
    完。
    可变 v 为 n。
    只要 v 取余于 2 等于 0：
        v 为 v 除以 2。
    完。
    返还 v 等于 1。
完。

数学_向上到2的幂 接受 n：
    若 n 小于或等于 1：
        返还 1。
    完。
    可变 v 为 1。
    只要 v 小于 n：
        v 为 v 乘 2。
    完。
    返还 v。
完。

数学_popcount 接受 n：
    可变 计数值 为 0。
    可变 v 为 n。
    只要 v 不等于 0：
        计数值 为 计数值 加 v 取余于 2。
        v 为 v 除以 2。
    完。
    返还 计数值。
完。

数学_CLZ 接受 n：
    若 n 等于 0：
        返还 64。
    完。
    最高位序 为 做 数学_log2 用 n。
    返还 63 减 最高位序。
完。

数学_CTZ 接受 n：
    若 n 等于 0：
        返还 64。
    完。
    可变 计数值 为 0。
    可变 v 为 n。
    只要 v 取余于 2 等于 0：
        计数值 为 计数值 加 1。
        v 为 v 除以 2。
    完。
    返还 计数值。
完。

数学_最高位 接受 n：
    若 n 等于 0：
        返还 0。
    完。
    可变 v 为 1。
    只要 v 小于或等于 n 除以 2：
        v 为 v 乘 2。
    完。
    返还 v。
完。

数学_阶乘 接受 n：
    若 n 小于或等于 1：
        返还 1。
    完。
    可变 结果 为 1。
    可变 i 为 2。
    只要 i 小于或等于 n：
        结果 为 结果 乘 i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学_斐波那契 接受 n：
    若 n 小于 2：
        返还 n。
    完。
    可变 a 为 0。
    可变 b 为 1。
    可变 i 为 2。
    只要 i 小于或等于 n：
        t 为 a 加 b。
        a 为 b。
        b 为 t。
        i 为 i 加 1。
    完。
    返还 b。
完。

数学_组合 接受 n、k：
    若 k 大于 n：
        返还 0。
    完。
    若 k 等于 0：
        返还 1。
    完。
    若 k 等于 n：
        返还 1。
    完。
    若 k 大于 n 减 k：
        返还 做 数学_组合 用 n、n 减 k。
    完。
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 k：
        结果 为 结果 乘 (n 减 i)。
        结果 为 结果 除以 (i 加 1)。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学_排列 接受 n、k：
    若 k 大于 n：
        返还 0。
    完。
    可变 结果 为 1。
    可变 i 为 0。
    只要 i 小于 k：
        结果 为 结果 乘 (n 减 i)。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学_是素数 接受 n：
    若 n 小于 2：
        返还 假。
    完。
    若 n 小于 4：
        返还 真。
    完。
    若 n 取余于 2 等于 0：
        返还 假。
    完。
    若 n 取余于 3 等于 0：
        返还 假。
    完。
    可变 i 为 5。
    只要 i 乘 i 小于或等于 n：
        若 n 取余于 i 等于 0：
            返还 假。
        完。
        若 n 取余于 (i 加 2) 等于 0：
            返还 假。
        完。
        i 为 i 加 6。
    完。
    返还 真。
完。

数学_下一个素数 接受 n：
    可变 候选 为 n 加 1。
    若 候选 小于或等于 2：
        返还 2。
    完。
    若 候选 取余于 2 等于 0：
        候选 为 候选 加 1。
    完。
    只要 做 数学_是素数 用 候选 等于 假：
        候选 为 候选 加 2。
    完。
    返还 候选。
完。

可变 RNG_状态 为 12345678901234567。

随机_种子 接受 种子：
    若 种子 等于 0：
        RNG_状态 为 1。
    否则：
        RNG_状态 为 种子。
    完。
完。

随机_生成 接受 零参：
    RNG_状态 为 RNG_状态 乘 1103515245 加 12345。
    RNG_状态 为 RNG_状态 取余于 2147483647。
    若 RNG_状态 小于 0：
        RNG_状态 为 0 减 RNG_状态。
    完。
    返还 RNG_状态。
完。

数学_随机_范围 接受 lo、hi：
    跨度 为 hi 减 lo 加 1。
    返还 lo 加 做 随机_生成 取余于 跨度。
完。

数学_范围列表 接受 起、_止：
    可变 结果 为 一组：（空）。
    可变 i 为 起。
    只要 i 小于 _止：
        做 追加 用 结果、i。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学_前缀和 接受 列表：
    可变 结果 为 一组：（空）。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        和 为 和 加 列表 的 第 i 项。
        做 追加 用 结果、和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学_求和 接受 列表：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        和 为 和 加 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

数学_求积 接受 列表：
    可变 积 为 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        积 为 积 乘 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 积。
完。

数学_对齐 接受 值、对齐值：
    若 对齐值 小于或等于 0：
        返还 值。
    完。
    上界 为 值 加 对齐值 减 1。
    块数 为 上界 除以 对齐值。
    返还 块数 乘 对齐值。
完。

数学_位旋转左 接受 值、位数、宽度：
    掩码 为 (1 左移 宽度) 减 1。
    左段 为 值 左移 位数。
    右段 为 值 右移 宽度 减 位数。
    返还 (左段 位或 右段) 位与 掩码。
完。

数学_位旋转右 接受 值、位数、宽度：
    掩码 为 (1 左移 宽度) 减 1。
    左段 为 值 右移 位数。
    右段 为 值 左移 宽度 减 位数。
    返还 (左段 位或 右段) 位与 掩码。
完。

数学_平均 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    返还 做 数学_求和 用 列表 除以 做 计数 用 列表。
完。

数学_列表最小 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 m 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 m：
            m 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 m。
完。

数学_列表最大 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 m 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 大于 m：
            m 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 m。
完。

数学_数学_梯形积分 接受 f值列表、步长：
    若 做 计数 用 f值列表 小于 2：
        返还 0。
    完。
    若 做 计数 用 f值列表 等于 0：
        返还 空。
    完。
    可变 总和 为 f值列表 的 第 0 项 加 f值列表 的 第 做 计数 用 f值列表 减 1 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 f值列表 减 1：
        总和 为 总和 加 2 乘 f值列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总和 乘 步长 除以 2。
完。

数学_Simpson积分 接受 f值列表、步长：
    n 为 做 计数 用 f值列表 减 1。
    若 n 小于 2：
        返还 做 数学_数学_梯形积分 用 f值列表、步长。
    完。
    若 n 取余于 2 不等于 0：
        返还 做 数学_数学_梯形积分 用 f值列表、步长。
    完。
    可变 总和 为 f值列表 的 第 0 项 加 f值列表 的 第 n 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 i 取余于 2 等于 1：
            总和 为 总和 加 4 乘 f值列表 的 第 i 项。
        否则：
            总和 为 总和 加 2 乘 f值列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 总和 乘 步长 除以 3。
完。

数学_中心差分 接受 f前、f后、h：
    若 h 等于 0：
        返还 0。
    完。
    返还 (f后 减 f前) 乘 500 除以 h。
完。

本章 收束。
