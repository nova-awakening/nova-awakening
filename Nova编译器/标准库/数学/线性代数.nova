《线性代数》

引用《..·基础》。

引用《..·..·运行时·智能推理·注意力机制》。

引用《..·..·运行时·原语桩》。

引用《工具》。

线代精度 为 1000。

精度 为 1000。

高斯消元 接受 A、b：
    n 为 A 的 第 0 项。
    可变 列号 为 0。
    可变 增强值 为 一组：n、n 加 1。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 n：
            做 追加 用 增强值、A 的 第 2 加 i 乘 n 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 增强值、b 的 第 i 项。
        i 为 i 加 1。
    完。
    只要 列号 小于 n：
        可变 主元行 为 列号。
        可变 主元值 为 做 线代_绝对值 用 增强值 的 第 2 加 列号 乘 (n 加 1) 加 列号 项。
        i 为 列号 加 1。
        只要 i 小于 n：
            v 为 做 线代_绝对值 用 增强值 的 第 2 加 i 乘 (n 加 1) 加 列号 项。
            若 v 大于 主元值：
                主元值 为 v。
                主元行 为 i。
            完。
            i 为 i 加 1。
        完。
        若 主元值 等于 0：
            返还 空。
        完。
        若 主元行 不等于 列号：
            可变 j2 为 0。
            只要 j2 小于或等于 n：
                暂存 为 增强值 的 第 2 加 列号 乘 (n 加 1) 加 j2 项。
                增强值 的 第 2 加 列号 乘 (n 加 1) 加 j2 项 为 增强值 的 第 2 加 主元行 乘 (n 加 1) 加 j2 项。
                增强值 的 第 2 加 主元行 乘 (n 加 1) 加 j2 项 为 暂存。
                j2 为 j2 加 1。
            完。
        完。
        i 为 列号 加 1。
        只要 i 小于 n：
            因子 为 增强值 的 第 2 加 i 乘 (n 加 1) 加 列号 项 乘 线代精度 除以 增强值 的 第 2 加 列号 乘 (n 加 1) 加 列号 项。
            可变 j3 为 列号。
            只要 j3 小于或等于 n：
                增强值 的 第 2 加 i 乘 (n 加 1) 加 j3 项 为 增强值 的 第 2 加 i 乘 (n 加 1) 加 j3 项 减 因子 乘 增强值 的 第 2 加 列号 乘 (n 加 1) 加 j3 项 除以 线代精度。
                j3 为 j3 加 1。
            完。
            i 为 i 加 1。
        完。
        列号 为 列号 加 1。
    完。
    可变 x 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 x、0。
        i 为 i 加 1。
    完。
    i 为 n 减 1。
    只要 i 大于或等于 0：
        可变 和 为 增强值 的 第 2 加 i 乘 (n 加 1) 加 n 项。
        可变 j4 为 i 加 1。
        只要 j4 小于 n：
            和 为 和 减 增强值 的 第 2 加 i 乘 (n 加 1) 加 j4 项 乘 x 的 第 j4 项 除以 线代精度。
            j4 为 j4 加 1。
        完。
        若 增强值 的 第 2 加 i 乘 (n 加 1) 加 i 项 等于 0：
            返还 空。
        完。
        x 的 第 i 项 为 和 乘 线代精度 除以 增强值 的 第 2 加 i 乘 (n 加 1) 加 i 项。
        i 为 i 减 1。
    完。
    返还 x。
完。

lu分解 接受 A：
    n 为 A 的 第 0 项。
    可变 L 为 一组：n、n。
    可变 U 为 一组：n、n。
    可变 i 为 0。
    只要 i 小于 n 乘 n：
        做 追加 用 L、0。
        做 追加 用 U、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 j 为 i。
        只要 j 小于 n：
            可变 和 为 A 的 第 2 加 i 乘 n 加 j 项。
            可变 k 为 0。
            只要 k 小于 i：
                和 为 和 减 L 的 第 2 加 i 乘 n 加 k 项 乘 U 的 第 2 加 k 乘 n 加 j 项 除以 线代精度。
                k 为 k 加 1。
            完。
            U 的 第 2 加 i 乘 n 加 j 项 为 和。
            j 为 j 加 1。
        完。
        j 为 i。
        只要 j 小于 n：
            若 i 等于 j：
                L 的 第 2 加 i 乘 n 加 i 项 为 线代精度。
            否则：
                可变 和2 为 A 的 第 2 加 j 乘 n 加 i 项。
                可变 k2 为 0。
                只要 k2 小于 i：
                    和2 为 和2 减 L 的 第 2 加 j 乘 n 加 k2 项 乘 U 的 第 2 加 k2 乘 n 加 i 项 除以 线代精度。
                    k2 为 k2 加 1。
                完。
                若 U 的 第 2 加 i 乘 n 加 i 项 不等于 0：
                    L 的 第 2 加 j 乘 n 加 i 项 为 和2 乘 线代精度 除以 U 的 第 2 加 i 乘 n 加 i 项。
                完。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 一组：L、U。
完。

矩阵求逆 接受 A：
    n 为 A 的 第 0 项。
    可变 列号 为 0。
    可变 增强值 为 一组：n、2 乘 n。
    可变 i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于 n：
            做 追加 用 增强值、A 的 第 2 加 i 乘 n 加 j 项。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 n：
            若 i 等于 j：
                做 追加 用 增强值、线代精度。
            否则：
                做 追加 用 增强值、0。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    只要 列号 小于 n：
        若 增强值 的 第 2 加 列号 乘 2 乘 n 加 列号 项 等于 0：
            返还 空。
        完。
        主元 为 增强值 的 第 2 加 列号 乘 2 乘 n 加 列号 项。
        可变 j2 为 0。
        只要 j2 小于 2 乘 n：
            增强值 的 第 2 加 列号 乘 2 乘 n 加 j2 项 为 增强值 的 第 2 加 列号 乘 2 乘 n 加 j2 项 乘 线代精度 除以 主元。
            j2 为 j2 加 1。
        完。
        i 为 0。
        只要 i 小于 n：
            若 i 不等于 列号：
                因子 为 增强值 的 第 2 加 i 乘 2 乘 n 加 列号 项。
                j2 为 0。
                只要 j2 小于 2 乘 n：
                    增强值 的 第 2 加 i 乘 2 乘 n 加 j2 项 为 增强值 的 第 2 加 i 乘 2 乘 n 加 j2 项 减 因子 乘 增强值 的 第 2 加 列号 乘 2 乘 n 加 j2 项 除以 线代精度。
                    j2 为 j2 加 1。
                完。
            完。
            i 为 i 加 1。
        完。
        列号 为 列号 加 1。
    完。
    可变 逆 为 一组：n、n。
    i 为 0。
    只要 i 小于 n：
        可变 j3 为 0。
        只要 j3 小于 n：
            做 追加 用 逆、增强值 的 第 2 加 i 乘 2 乘 n 加 n 加 j3 项。
            j3 为 j3 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 逆。
完。

最大特征值 接受 A、迭代次数：
    n 为 A 的 第 0 项。
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 v、线代精度。
        i 为 i 加 1。
    完。
    可变 特征值 为 0。
    可变 迭代 为 0。
    只要 迭代 小于 迭代次数：
        可变 w 为 一组：（空）。
        i 为 0。
        只要 i 小于 n：
            可变 和 为 0。
            可变 j 为 0。
            只要 j 小于 n：
                和 为 和 加 A 的 第 2 加 i 乘 n 加 j 项 乘 v 的 第 j 项 除以 线代精度。
                j 为 j 加 1。
            完。
            做 追加 用 w、和。
            i 为 i 加 1。
        完。
        可变 最大 为 做 线代_绝对值 用 w 的 第 0 项。
        i 为 1。
        只要 i 小于 n：
            av 为 做 线代_绝对值 用 w 的 第 i 项。
            若 av 大于 最大：
                最大 为 av。
            完。
            i 为 i 加 1。
        完。
        若 最大 等于 0：
            最大 为 1。
        完。
        特征值 为 最大。
        i 为 0。
        只要 i 小于 n：
            v 的 第 i 项 为 w 的 第 i 项 乘 线代精度 除以 最大。
            i 为 i 加 1。
        完。
        迭代 为 迭代 加 1。
    完。
    返还 特征值。
完。

正交化 接受 向量组：
    n 为 做 计数 用 向量组。
    维度 为 做 计数 用 向量组 的 第 0 项。
    可变 正交 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 u 为 一组：（空）。
        可变 d 为 0。
        只要 d 小于 维度：
            做 追加 用 u、向量组 的 第 i 项 的 第 d 项。
            d 为 d 加 1。
        完。
        可变 j 为 0。
        只要 j 小于 做 计数 用 正交：
            投影 为 做 线代_点积 用 u、正交 的 第 j 项 乘 线代精度 除以 做 线代_点积 用 正交 的 第 j 项、正交 的 第 j 项。
            d 为 0。
            只要 d 小于 维度：
                u 的 第 d 项 为 u 的 第 d 项 减 投影 乘 正交 的 第 j 项 的 第 d 项 除以 线代精度。
                d 为 d 加 1。
            完。
            j 为 j 加 1。
        完。
        范数 为 做 线代_开方 用 做 线代_点积 用 u、u。
        若 范数 大于 0：
            d 为 0。
            只要 d 小于 维度：
                u 的 第 d 项 为 u 的 第 d 项 乘 线代精度 除以 范数。
                d 为 d 加 1。
            完。
            做 追加 用 正交、u。
        完。
        i 为 i 加 1。
    完。
    返还 正交。
完。

向量范数 接受 v、_扩2：
    返还 做 线代_开方 用 做 线代_点积 用 v、v。
完。

向量l1范数 接受 v：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        和 为 和 加 做 线代_绝对值 用 v 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

矩阵f范数 接受 A：
    n 为 A 的 第 0 项 乘 A 的 第 1 项。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和 为 和 加 A 的 第 2 加 i 项 乘 A 的 第 2 加 i 项 除以 线代精度。
        i 为 i 加 1。
    完。
    返还 做 线代_开方 用 和。
完。

lu求解 接受 L、U、b：
    n 为 L 的 第 0 项。
    可变 y 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 y、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 和 为 b 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 i：
            和 为 和 减 L 的 第 2 加 i 乘 n 加 j 项 乘 y 的 第 j 项 除以 线代精度。
            j 为 j 加 1。
        完。
        y 的 第 i 项 为 和 乘 线代精度 除以 L 的 第 2 加 i 乘 n 加 i 项。
        i 为 i 加 1。
    完。
    可变 x 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 x、0。
        i 为 i 加 1。
    完。
    i 为 n 减 1。
    只要 i 大于或等于 0：
        可变 和2 为 y 的 第 i 项。
        可变 j2 为 i 加 1。
        只要 j2 小于 n：
            和2 为 和2 减 U 的 第 2 加 i 乘 n 加 j2 项 乘 x 的 第 j2 项 除以 线代精度。
            j2 为 j2 加 1。
        完。
        若 U 的 第 2 加 i 乘 n 加 i 项 等于 0：
            返还 空。
        完。
        x 的 第 i 项 为 和2 乘 线代精度 除以 U 的 第 2 加 i 乘 n 加 i 项。
        i 为 i 减 1。
    完。
    返还 x。
完。

条件数 接受 A：
    n 为 A 的 第 0 项。
    可变 范数A 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 行和 为 0。
        可变 j 为 0。
        只要 j 小于 n：
            可变 v 为 A 的 第 2 加 i 乘 n 加 j 项。
            若 v 小于 0：
                v 为 0 减 v。
            完。
            行和 为 行和 加 v。
            j 为 j 加 1。
        完。
        若 行和 大于 范数A：
            范数A 为 行和。
        完。
        i 为 i 加 1。
    完。
    逆 为 做 矩阵求逆 用 A。
    若 逆 等于 空：
        返还 0x7FFFFFFF。
    完。
    可变 范数逆 为 0。
    i 为 0。
    只要 i 小于 n：
        可变 行和2 为 0。
        可变 j2 为 0。
        只要 j2 小于 n：
            可变 v2 为 逆 的 第 2 加 i 乘 n 加 j2 项。
            若 v2 小于 0：
                v2 为 0 减 v2。
            完。
            行和2 为 行和2 加 v2。
            j2 为 j2 加 1。
        完。
        若 行和2 大于 范数逆：
            范数逆 为 行和2。
        完。
        i 为 i 加 1。
    完。
    若 线代精度 等于 0：
        返还 0。
    完。
    返还 范数A 乘 范数逆 除以 线代精度。
完。

最小特征值 接受 A、迭代次数：
    _n 为 A 的 第 0 项。
    逆 为 做 矩阵求逆 用 A。
    若 逆 等于 空：
        返还 0。
    完。
    最大逆特征 为 做 最大特征值 用 逆、迭代次数。
    若 最大逆特征 等于 0：
        返还 0。
    完。
    返还 线代精度 乘 线代精度 除以 最大逆特征。
完。

行列式 接受 A、_扩2：
    n 为 A 的 第 0 项。
    分解 为 做 lu分解 用 A。
    U 为 分解 的 第 1 项。
    可变 行列式 为 线代精度。
    可变 i 为 0。
    只要 i 小于 n：
        行列式 为 行列式 乘 U 的 第 2 加 i 乘 n 加 i 项 除以 线代精度。
        i 为 i 加 1。
    完。
    返还 行列式。
完。

矩阵秩 接受 A：
    行 为 A 的 第 0 项。
    可变 列号 为 0。
    列 为 A 的 第 1 项。
    可变 m 为 一组：行、列。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        做 追加 用 m、A 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    可变 秩 为 0。
    可变 行号 为 0。
    只要 行号 小于 行 并且 列号 小于 列：
        可变 主元行 为 行号。
        可变 主元值 为 做 线代_绝对值 用 m 的 第 2 加 行号 乘 列 加 列号 项。
        i 为 行号 加 1。
        只要 i 小于 行：
            v 为 做 线代_绝对值 用 m 的 第 2 加 i 乘 列 加 列号 项。
            若 v 大于 主元值：
                主元值 为 v。
                主元行 为 i。
            完。
            i 为 i 加 1。
        完。
        若 主元值 等于 0：
            列号 为 列号 加 1。
            续。
        完。
        若 主元行 不等于 行号：
            可变 j 为 0。
            只要 j 小于 列：
                t 为 m 的 第 2 加 行号 乘 列 加 j 项。
                m 的 第 2 加 行号 乘 列 加 j 项 为 m 的 第 2 加 主元行 乘 列 加 j 项。
                m 的 第 2 加 主元行 乘 列 加 j 项 为 t。
                j 为 j 加 1。
            完。
        完。
        i 为 行号 加 1。
        只要 i 小于 行：
            因子 为 m 的 第 2 加 i 乘 列 加 列号 项 乘 线代精度 除以 m 的 第 2 加 行号 乘 列 加 列号 项。
            可变 j2 为 列号。
            只要 j2 小于 列：
                m 的 第 2 加 i 乘 列 加 j2 项 为 m 的 第 2 加 i 乘 列 加 j2 项 减 因子 乘 m 的 第 2 加 行号 乘 列 加 j2 项 除以 线代精度。
                j2 为 j2 加 1。
            完。
            i 为 i 加 1。
        完。
        秩 为 秩 加 1。
        行号 为 行号 加 1。
        列号 为 列号 加 1。
    完。
    返还 秩。
完。

乔列斯基 接受 A：
    n 为 A 的 第 0 项。
    可变 L 为 一组：n、n。
    可变 i 为 0。
    只要 i 小于 n 乘 n：
        做 追加 用 L、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        可变 j 为 0。
        只要 j 小于或等于 i：
            可变 和 为 0。
            可变 k 为 0。
            只要 k 小于 j：
                和 为 和 加 L 的 第 2 加 i 乘 n 加 k 项 乘 L 的 第 2 加 j 乘 n 加 k 项 除以 线代精度。
                k 为 k 加 1。
            完。
            若 i 等于 j：
                差值 为 A 的 第 2 加 i 乘 n 加 i 项 减 和。
                若 差值 小于或等于 0：
                    返还 空。
                完。
                L 的 第 2 加 i 乘 n 加 j 项 为 做 线代_开方 用 差值。
            否则：
                分母 为 L 的 第 2 加 j 乘 n 加 j 项。
                若 分母 等于 0：
                    返还 空。
                完。
                L 的 第 2 加 i 乘 n 加 j 项 为 (A 的 第 2 加 i 乘 n 加 j 项 减 和) 乘 线代精度 除以 分母。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 L。
完。

线代_绝对值 接受 x：
    若 x 小于 0：
        返还 0 减 x。
    完。
    返还 x。
完。

线代_点积 接受 a、b：
    n 为 做 计数 用 a。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和 为 和 加 a 的 第 i 项 乘 b 的 第 i 项 除以 线代精度。
        i 为 i 加 1。
    完。
    返还 和。
完。

线代_开方 接受 n：
    返还 做 开方 用 n。
完。

向量_点积 接受 a、b、n：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和 为 和 加 a 的 第 i 项 乘 b 的 第 i 项 除以 精度。
        i 为 i 加 1。
    完。
    返还 和。
完。

QR分解 接受 A、m、n：
    可变 Q列 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 n：
        可变 列 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 m：
            做 追加 用 列、A 的 第 i 乘 n 加 j 项。
            i 为 i 加 1。
        完。
        做 追加 用 Q列、列。
        j 为 j 加 1。
    完。
    可变 R 为 一组：（空）。
    可变 索引 为 0。
    只要 索引 小于 n 乘 n：
        做 追加 用 R、0。
        索引 为 索引 加 1。
    完。
    j 为 0。
    只要 j 小于 n：
        可变 k 为 0。
        只要 k 小于 j：
            点积 为 做 向量_点积 用 Q列 的 第 j 项、Q列 的 第 k 项、m。
            R 的 第 k 乘 n 加 j 项 为 点积。
            可变 i 为 0。
            只要 i 小于 m：
                Q列 的 第 j 项 的 第 i 项 为 Q列 的 第 j 项 的 第 i 项 减 点积 乘 Q列 的 第 k 项 的 第 i 项 除以 精度。
                i 为 i 加 1。
            完。
            k 为 k 加 1。
        完。
        范数 为 做 向量_范数 用 Q列 的 第 j 项、m。
        R 的 第 j 乘 n 加 j 项 为 范数。
        若 范数 大于 0：
            可变 i 为 0。
            只要 i 小于 m：
                Q列 的 第 j 项 的 第 i 项 为 Q列 的 第 j 项 的 第 i 项 乘 精度 除以 范数。
                i 为 i 加 1。
            完。
        完。
        j 为 j 加 1。
    完。
    可变 Q 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 m 乘 n：
        做 追加 用 Q、0。
        i 为 i 加 1。
    完。
    j 为 0。
    只要 j 小于 n：
        i 为 0。
        只要 i 小于 m：
            Q 的 第 i 乘 n 加 j 项 为 Q列 的 第 j 项 的 第 i 项。
            i 为 i 加 1。
        完。
        j 为 j 加 1。
    完。
    返还 一组：Q、R。
完。

共轭梯度 接受 A_稀疏、b、n、最大迭代、容差：
    可变 x 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 x、0。
        i 为 i 加 1。
    完。
    Ax 为 做 稀疏矩阵_乘向量 用 A_稀疏、x。
    可变 r 为 一组：（空）。
    可变 p 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 r、b 的 第 i 项 减 Ax 的 第 i 项。
        做 追加 用 p、b 的 第 i 项 减 Ax 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 rsold 为 做 向量_点积 用 r、r、n。
    可变 迭代 为 0。
    只要 迭代 小于 最大迭代：
        Ap 为 做 稀疏矩阵_乘向量 用 A_稀疏、p。
        pAp 为 做 向量_点积 用 p、Ap、n。
        若 pAp 等于 0：
            迭代 为 最大迭代。
        否则：
            学习率 为 rsold 乘 精度 除以 pAp。
            i 为 0。
            只要 i 小于 n：
                x 的 第 i 项 为 x 的 第 i 项 加 学习率 乘 p 的 第 i 项 除以 精度。
                r 的 第 i 项 为 r 的 第 i 项 减 学习率 乘 Ap 的 第 i 项 除以 精度。
                i 为 i 加 1。
            完。
            rsnew 为 做 向量_点积 用 r、r、n。
            若 rsnew 小于 容差：
                迭代 为 最大迭代。
            否则：
                动量率 为 rsnew 乘 精度 除以 rsold。
                i 为 0。
                只要 i 小于 n：
                    p 的 第 i 项 为 r 的 第 i 项 加 动量率 乘 p 的 第 i 项 除以 精度。
                    i 为 i 加 1。
                完。
                rsold 为 rsnew。
            完。
        完。
        迭代 为 迭代 加 1。
    完。
    返还 x。
完。

向量点积_SIMD4 接受 a、b、n：
    若 a 等于 0 或者 b 等于 0 或者 n 等于 0：
        返还 0。
    完。
    可变 s0 为 0。
    可变 s1 为 0。
    可变 s2 为 0。
    可变 s3 为 0。
    可变 i 为 0。
    只要 i 加 4 小于或等于 n：
        s0 为 s0 加 做 Q乘 用 做 内存_读32有符号 用 a 加 i 乘 4、做 内存_读32有符号 用 b 加 i 乘 4。
        s1 为 s1 加 做 Q乘 用 做 内存_读32有符号 用 a 加 (i 加 1) 乘 4、做 内存_读32有符号 用 b 加 (i 加 1) 乘 4。
        s2 为 s2 加 做 Q乘 用 做 内存_读32有符号 用 a 加 (i 加 2) 乘 4、做 内存_读32有符号 用 b 加 (i 加 2) 乘 4。
        s3 为 s3 加 做 Q乘 用 做 内存_读32有符号 用 a 加 (i 加 3) 乘 4、做 内存_读32有符号 用 b 加 (i 加 3) 乘 4。
        i 为 i 加 4。
    完。
    只要 i 小于 n：
        s0 为 s0 加 做 Q乘 用 做 内存_读32有符号 用 a 加 i 乘 4、做 内存_读32有符号 用 b 加 i 乘 4。
        i 为 i 加 1。
    完。
    返还 s0 加 s1 加 s2 加 s3。
完。

向量_范数 接受 v、n：
    点 为 做 向量_点积 用 v、v、n。
    返还 做 整数平方根 用 点 乘 精度。
完。

整数平方根 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

特征值_QR 接受 A、n、迭代次数：
    可变 当前 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n 乘 n：
        做 追加 用 当前、A 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 迭代 为 0。
    只要 迭代 小于 迭代次数：
        qr 为 做 QR分解 用 当前、n、n。
        Q 为 qr 的 第 0 项。
        R 为 qr 的 第 1 项。
        可变 新矩阵 为 一组：（空）。
        i 为 0。
        只要 i 小于 n 乘 n：
            做 追加 用 新矩阵、0。
            i 为 i 加 1。
        完。
        i 为 0。
        只要 i 小于 n：
            可变 j 为 0。
            只要 j 小于 n：
                可变 和 为 0。
                可变 k 为 0。
                只要 k 小于 n：
                    和 为 和 加 R 的 第 i 乘 n 加 k 项 乘 Q 的 第 k 乘 n 加 j 项 除以 精度。
                    k 为 k 加 1。
                完。
                新矩阵 的 第 i 乘 n 加 j 项 为 和。
                j 为 j 加 1。
            完。
            i 为 i 加 1。
        完。
        当前 为 新矩阵。
        迭代 为 迭代 加 1。
    完。
    可变 特征值 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 特征值、当前 的 第 i 乘 n 加 i 项。
        i 为 i 加 1。
    完。
    返还 特征值。
完。

稀疏矩阵_乘向量 接受 sp、x：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 sp 的 第 3 项：
        做 追加 用 结果、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 sp 的 第 3 项：
        可变 和 为 0。
        可变 k 为 sp 的 第 2 项 的 第 i 项。
        只要 k 小于 sp 的 第 2 项 的 第 i 加 1 项：
            和 为 和 加 sp 的 第 0 项 的 第 k 项 乘 x 的 第 sp 的 第 1 项 的 第 k 项 项 除以 精度。
            k 为 k 加 1。
        完。
        结果 的 第 i 项 为 和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

稀疏矩阵_从密集 接受 A、m、n：
    可变 值 为 一组：（空）。
    可变 列idx 为 一组：（空）。
    可变 行ptr 为 一组：0。
    可变 nnz 为 0。
    可变 i 为 0。
    只要 i 小于 m：
        可变 j 为 0。
        只要 j 小于 n：
            若 A 的 第 i 乘 n 加 j 项 不等于 0：
                做 追加 用 值、A 的 第 i 乘 n 加 j 项。
                做 追加 用 列idx、j。
                nnz 为 nnz 加 1。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 行ptr、nnz。
        i 为 i 加 1。
    完。
    返还 一组：值、列idx、行ptr、m、n、nnz。
完。

稀疏矩阵_创建 接受 行数、列数：
    可变 行指针 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于或等于 行数：
        做 追加 用 行指针、0。
        i 为 i 加 1。
    完。
    返还 一组：一组：（空）、一组：（空）、行指针、行数、列数、0。
完。

稀疏矩阵_取 接受 sp、行、列：
    起 为 sp 的 第 2 项 的 第 行 项。
    终 为 sp 的 第 2 项 的 第 行 加 1 项。
    可变 k 为 起。
    只要 k 小于 终：
        若 sp 的 第 1 项 的 第 k 项 等于 列：
            返还 sp 的 第 0 项 的 第 k 项。
        完。
        k 为 k 加 1。
    完。
    返还 0。
完。

本章 收束。
