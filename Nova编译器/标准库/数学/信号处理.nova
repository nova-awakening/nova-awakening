《信号处理》

引用《..·基础》。

引用《..·..·运行时·原语桩》。

信号精度 为 1000。

离散傅里叶 接受 信号：
    n 为 做 计数 用 信号。
    可变 实部 为 一组：（空）。
    可变 虚部 为 一组：（空）。
    可变 k 为 0。
    只要 k 小于 n：
        可变 re 为 0。
        可变 im 为 0。
        可变 t 为 0。
        只要 t 小于 n：
            角度 为 2 乘 k 乘 t 乘 6283 除以 n。
            re 为 re 加 信号 的 第 t 项 乘 做 信号_cos 用 角度 除以 信号精度。
            im 为 im 减 信号 的 第 t 项 乘 做 信号_sin 用 角度 除以 信号精度。
            t 为 t 加 1。
        完。
        做 追加 用 实部、re。
        做 追加 用 虚部、im。
        k 为 k 加 1。
    完。
    返还 一组：实部、虚部。
完。

频谱幅度 接受 dft结果：
    实 为 dft结果 的 第 0 项。
    虚 为 dft结果 的 第 1 项。
    n 为 做 计数 用 实。
    可变 幅度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        mag_sq 为 实 的 第 i 项 乘 实 的 第 i 项 加 虚 的 第 i 项 乘 虚 的 第 i 项。
        做 追加 用 幅度、做 信号_开方 用 mag_sq。
        i 为 i 加 1。
    完。
    返还 幅度。
完。

低通滤波 接受 信号、窗口大小：
    n 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 和 为 0。
        可变 计数器 为 0。
        起 为 i 减 窗口大小 除以 2。
        终 为 i 加 窗口大小 除以 2。
        可变 j 为 起。
        只要 j 小于或等于 终：
            若 j 大于或等于 0 并且 j 小于 n：
                和 为 和 加 信号 的 第 j 项。
                计数器 为 计数器 加 1。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和 除以 计数器。
        i 为 i 加 1。
    完。
    返还 结果。
完。

高通滤波 接受 信号、窗口大小：
    低通 为 做 低通滤波 用 信号、窗口大小。
    n 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、信号 的 第 i 项 减 低通 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

中值滤波 接受 信号、窗口大小：
    可变 窗口 为 一组：（空）。
    n 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        起 为 i 减 窗口大小 除以 2。
        终 为 i 加 窗口大小 除以 2。
        可变 j 为 起。
        只要 j 小于或等于 终：
            若 j 大于或等于 0 并且 j 小于 n：
                做 追加 用 窗口、信号 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        排序窗 为 做 信号_排序 用 窗口。
        做 追加 用 结果、排序窗 的 第 做 计数 用 排序窗 除以 2 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

指数移动平均 接受 信号、学习率：
    n 为 做 计数 用 信号。
    可变 结果 为 一组：信号 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        前 为 结果 的 第 i 减 1 项。
        新值 为 学习率 乘 信号 的 第 i 项 除以 信号精度 加 (信号精度 减 学习率) 乘 前 除以 信号精度。
        做 追加 用 结果、新值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

hamming窗 接受 大小：
    可变 窗 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        角度 为 2 乘 i 乘 6283 除以 (大小 减 1)。
        值 为 540 减 460 乘 做 信号_cos 用 角度 除以 信号精度。
        做 追加 用 窗、值。
        i 为 i 加 1。
    完。
    返还 窗。
完。

hann窗 接受 大小：
    可变 窗 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 大小：
        角度 为 2 乘 i 乘 6283 除以 (大小 减 1)。
        值 为 500 减 500 乘 做 信号_cos 用 角度 除以 信号精度。
        做 追加 用 窗、值。
        i 为 i 加 1。
    完。
    返还 窗。
完。

应用窗 接受 信号、窗：
    n 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、信号 的 第 i 项 乘 窗 的 第 i 项 除以 信号精度。
        i 为 i 加 1。
    完。
    返还 结果。
完。

正弦波 接受 长度、频率、幅度：
    可变 信号 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度：
        角度 为 i 乘 频率 乘 6283 除以 长度。
        做 追加 用 信号、幅度 乘 做 信号_sin 用 角度 除以 信号精度。
        i 为 i 加 1。
    完。
    返还 信号。
完。

方波 接受 长度、周期、幅度：
    可变 信号 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度：
        若 i 取余于 周期 小于 周期 除以 2：
            做 追加 用 信号、幅度。
        否则：
            做 追加 用 信号、0 减 幅度。
        完。
        i 为 i 加 1。
    完。
    返还 信号。
完。

锯齿波 接受 长度、周期、幅度：
    可变 信号 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 长度：
        相位 为 i 取余于 周期。
        做 追加 用 信号、2 乘 幅度 乘 相位 除以 周期 减 幅度。
        i 为 i 加 1。
    完。
    返还 信号。
完。

白噪声 接受 长度、幅度、种子：
    可变 信号 为 一组：（空）。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 长度：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        做 追加 用 信号、s 取余于 (2 乘 幅度 加 1) 减 幅度。
        i 为 i 加 1。
    完。
    返还 信号。
完。

信号_发生过零 接受 前值、当前值：
    若 前值 小于 0：
        若 当前值 大于或等于 0：
            返还 真。
        完。
    完。
    若 前值 大于或等于 0：
        若 当前值 小于 0：
            返还 真。
        完。
    完。
    返还 假。
完。

过零率 接受 信号：
    n 为 做 计数 用 信号。
    可变 过零 为 0。
    可变 i 为 1。
    只要 i 小于 n：
        若 做 信号_发生过零 用 信号 的 第 i 减 1 项、信号 的 第 i 项：
            过零 为 过零 加 1。
        完。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 过零 乘 信号精度 除以 n。
完。

信号_rms 接受 信号：
    n 为 做 计数 用 信号。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和 为 和 加 信号 的 第 i 项 乘 信号 的 第 i 项 除以 信号精度。
        i 为 i 加 1。
    完。
    返还 做 信号_开方 用 和 除以 n。
完。

信号_峰值 接受 信号：
    n 为 做 计数 用 信号。
    可变 最大 为 信号 的 第 0 项。
    若 最大 小于 0：
        最大 为 0 减 最大。
    完。
    可变 i 为 1。
    只要 i 小于 n：
        可变 v 为 信号 的 第 i 项。
        若 v 小于 0：
            v 为 0 减 v。
        完。
        若 v 大于 最大：
            最大 为 v。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

自相关 接受 信号、最大延迟：
    n 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 滞后量 为 0。
    只要 滞后量 小于或等于 最大延迟：
        可变 和 为 0。
        可变 i 为 0。
        只要 i 小于 n 减 滞后量：
            和 为 和 加 信号 的 第 i 项 乘 信号 的 第 i 加 滞后量 项 除以 信号精度。
            i 为 i 加 1。
        完。
        做 追加 用 结果、和 除以 (n 减 滞后量)。
        滞后量 为 滞后量 加 1。
    完。
    返还 结果。
完。

互相关 接受 信号a、信号b、最大延迟：
    na 为 做 计数 用 信号a。
    nb 为 做 计数 用 信号b。
    可变 结果 为 一组：（空）。
    可变 滞后量 为 0 减 最大延迟。
    只要 滞后量 小于或等于 最大延迟：
        可变 和 为 0。
        可变 计数值 为 0。
        可变 i 为 0。
        只要 i 小于 na：
            j 为 i 加 滞后量。
            若 j 大于或等于 0 并且 j 小于 nb：
                和 为 和 加 信号a 的 第 i 项 乘 信号b 的 第 j 项 除以 信号精度。
                计数值 为 计数值 加 1。
            完。
            i 为 i 加 1。
        完。
        若 计数值 大于 0：
            做 追加 用 结果、和 除以 计数值。
        否则：
            做 追加 用 结果、0。
        完。
        滞后量 为 滞后量 加 1。
    完。
    返还 结果。
完。

信号_包络 接受 信号：
    n 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 局部max 为 0。
        可变 j 为 i 减 2。
        只要 j 小于或等于 i 加 2：
            若 j 大于或等于 0 并且 j 小于 n：
                可变 v 为 信号 的 第 j 项。
                若 v 小于 0：
                    v 为 0 减 v。
                完。
                若 v 大于 局部max：
                    局部max 为 v。
                完。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、局部max。
        i 为 i 加 1。
    完。
    返还 结果。
完。

零交叉率 接受 信号：
    n 为 做 计数 用 信号。
    可变 交叉数 为 0。
    可变 i 为 1。
    只要 i 小于 n：
        若 做 信号_发生过零 用 信号 的 第 i 减 1 项、信号 的 第 i 项：
            交叉数 为 交叉数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 交叉数 乘 信号精度 除以 (n 减 1)。
完。

信号_能量 接受 信号、_扩2：
    n 为 做 计数 用 信号。
    可变 能量 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        能量 为 能量 加 信号 的 第 i 项 乘 信号 的 第 i 项 除以 信号精度。
        i 为 i 加 1。
    完。
    返还 能量。
完。

信号_下采样 接受 信号、因子：
    n 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、信号 的 第 i 项。
        i 为 i 加 因子。
    完。
    返还 结果。
完。

信号_上采样 接受 信号、因子：
    n 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、信号 的 第 i 项。
        可变 j 为 1。
        只要 j 小于 因子：
            若 i 加 1 小于 n：
                做 追加 用 结果、信号 的 第 i 项 加 (信号 的 第 i 加 1 项 减 信号 的 第 i 项) 乘 j 除以 因子。
            否则：
                做 追加 用 结果、信号 的 第 i 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

信号_自相关 接受 信号、最大延迟：
    n 为 做 计数 用 信号。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 信号 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 c0 为 0。
    i 为 0。
    只要 i 小于 n：
        d 为 信号 的 第 i 项 减 均值。
        c0 为 c0 加 d 乘 d 除以 信号精度。
        i 为 i 加 1。
    完。
    若 c0 等于 0：
        c0 为 1。
    完。
    可变 acf 为 一组：（空）。
    可变 滞后量 为 0。
    只要 滞后量 小于或等于 最大延迟 并且 滞后量 小于 n：
        可变 ck 为 0。
        i 为 0。
        只要 i 小于 n 减 滞后量：
            ck 为 ck 加 (信号 的 第 i 项 减 均值) 乘 (信号 的 第 i 加 滞后量 项 减 均值) 除以 信号精度。
            i 为 i 加 1。
        完。
        做 追加 用 acf、ck 乘 信号精度 除以 c0。
        滞后量 为 滞后量 加 1。
    完。
    返还 acf。
完。

信号_过零率 接受 信号、_扩2：
    n 为 做 计数 用 信号。
    可变 过零数 为 0。
    可变 i 为 1。
    只要 i 小于 n：
        若 做 信号_发生过零 用 信号 的 第 i 减 1 项、信号 的 第 i 项：
            过零数 为 过零数 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 过零数 乘 信号精度 除以 (n 减 1)。
完。

信号_峰值检测 接受 信号、最小高度：
    n 为 做 计数 用 信号。
    可变 峰值 为 一组：（空）。
    可变 i 为 1。
    只要 i 小于 n 减 1：
        若 信号 的 第 i 项 大于 信号 的 第 i 减 1 项 并且 信号 的 第 i 项 大于 信号 的 第 i 加 1 项 并且 信号 的 第 i 项 大于或等于 最小高度：
            做 追加 用 峰值、一组：i、信号 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 峰值。
完。

信号_sin 接受 x：
    可变 角 为 x 取余于 6283。
    若 角 小于 0：
        角 为 角 加 6283。
    完。
    若 角 小于 1571：
        返还 角 乘 信号精度 除以 1571。
    完。
    若 角 小于 3142：
        返还 (3142 减 角) 乘 信号精度 除以 1571。
    完。
    若 角 小于 4712：
        返还 0 减 (角 减 3142) 乘 信号精度 除以 1571。
    完。
    返还 0 减 (6283 减 角) 乘 信号精度 除以 1571。
完。

信号_cos 接受 x：
    返还 做 信号_sin 用 x 加 1571。
完。

信号_开方 接受 n：
    返还 做 开方 用 n。
完。

信号_排序 接受 列表：
    n 为 做 计数 用 列表。
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 r、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 1。
    只要 i 小于 n：
        键 为 r 的 第 i 项。
        可变 j 为 i 减 1。
        可变 插入位置 为 i。
        只要 j 大于或等于 0：
            若 r 的 第 j 项 大于 键：
                r 的 第 j 加 1 项 为 r 的 第 j 项。
                插入位置 为 j。
                j 为 j 减 1。
            否则：
                j 为 负 1。
            完。
        完。
        r 的 第 插入位置 项 为 键。
        i 为 i 加 1。
    完。
    返还 r。
完。

FIR_滤波 接受 信号、系数：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 信号：
        可变 总和 为 0。
        可变 k 为 0。
        只要 k 小于 做 计数 用 系数：
            若 i 减 k 大于或等于 0：
                总和 为 总和 加 信号 的 第 i 减 k 项 乘 系数 的 第 k 项 除以 100。
            完。
            k 为 k 加 1。
        完。
        做 追加 用 结果、总和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

IIR_一阶 接受 信号、a、b：
    可变 结果 为 一组：（空）。
    可变 前值 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 信号：
        y 为 a 乘 信号 的 第 i 项 除以 1000 加 b 乘 前值 除以 1000。
        做 追加 用 结果、y。
        前值 为 y。
        i 为 i 加 1。
    完。
    返还 结果。
完。

信号_均值 接受 信号：
    n 为 做 计数 用 信号。
    若 n 等于 0：
        返还 0。
    完。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和 为 和 加 信号 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和 除以 n。
完。

信号_差分 接受 信号：
    可变 r 为 一组：（空）。
    可变 i 为 1。
    只要 i 小于 做 计数 用 信号：
        做 追加 用 r、信号 的 第 i 项 减 信号 的 第 i 减 1 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

信号_方差 接受 信号：
    n 为 做 计数 用 信号。
    若 n 等于 0：
        返还 0。
    完。
    均值 为 做 信号_均值 用 信号。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        差值 为 信号 的 第 i 项 减 均值。
        和 为 和 加 差值 乘 差值。
        i 为 i 加 1。
    完。
    返还 和 除以 n。
完。

信号_裁剪 接受 信号、clip_min、clip_max：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 信号：
        可变 值 为 信号 的 第 i 项。
        若 值 小于 clip_min：
            值 为 clip_min。
        完。
        若 值 大于 clip_max：
            值 为 clip_max。
        完。
        做 追加 用 结果、值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

卷积 接受 信号、核：
    sn 为 做 计数 用 信号。
    kn 为 做 计数 用 核。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 sn 加 kn 减 1：
        可变 总和 为 0。
        可变 j 为 0。
        只要 j 小于 kn：
            si 为 i 减 j。
            若 si 大于或等于 0：
                若 si 小于 sn：
                    总和 为 总和 加 信号 的 第 si 项 乘 核 的 第 j 项 除以 1000。
                完。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、总和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

峰值检测 接受 信号、阈值：
    可变 峰值 为 一组：（空）。
    可变 i 为 1。
    只要 i 小于 做 计数 用 信号 减 1：
        若 信号 的 第 i 项 大于 信号 的 第 i 减 1 项：
            若 信号 的 第 i 项 大于 信号 的 第 i 加 1 项：
                若 信号 的 第 i 项 大于 阈值：
                    做 追加 用 峰值、i。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 峰值。
完。

指数平滑 接受 信号、学习率：
    可变 结果 为 一组：（空）。
    若 做 计数 用 信号 等于 0：
        返还 结果。
    完。
    做 追加 用 结果、信号 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 信号：
        smoothed 为 学习率 乘 信号 的 第 i 项 除以 1000 加 (1000 减 学习率) 乘 结果 的 第 i 减 1 项 除以 1000。
        做 追加 用 结果、smoothed。
        i 为 i 加 1。
    完。
    返还 结果。
完。

移动平均 接受 信号、窗口：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 信号：
        可变 总和 为 0。
        可变 计数值 为 0。
        可变 j 为 i 减 窗口 除以 2。
        只要 j 小于或等于 i 加 窗口 除以 2：
            若 j 大于或等于 0：
                若 j 小于 做 计数 用 信号：
                    总和 为 总和 加 信号 的 第 j 项。
                    计数值 为 计数值 加 1。
                完。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、总和 除以 计数值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

谷值检测 接受 信号、阈值：
    可变 谷值 为 一组：（空）。
    可变 i 为 1。
    只要 i 小于 做 计数 用 信号 减 1：
        若 信号 的 第 i 项 小于 信号 的 第 i 减 1 项：
            若 信号 的 第 i 项 小于 信号 的 第 i 加 1 项：
                若 信号 的 第 i 项 小于 阈值：
                    做 追加 用 谷值、i。
                完。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 谷值。
完。

笔画方向直方图 接受 笔画序列：
    可变 直方图 为 一组：0、0、0、0、0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 笔画序列：
        方向 为 笔画序列 的 第 i 项 取余于 8。
        直方图 的 第 方向 项 为 直方图 的 第 方向 项 加 1。
        i 为 i 加 1。
    完。
    返还 直方图。
完。

笔画主频 接受 直方图：
    若 做 计数 用 直方图 等于 0：
        返还 0。
    完。
    可变 最大值 为 直方图 的 第 0 项。
    可变 主频索引 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 直方图：
        若 直方图 的 第 i 项 大于 最大值：
            最大值 为 直方图 的 第 i 项。
            主频索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 主频索引。
完。

笔画方向复杂度 接受 直方图：
    可变 总笔画 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 直方图：
        总笔画 为 总笔画 加 直方图 的 第 i 项。
        i 为 i 加 1。
    完。
    若 总笔画 等于 0：
        返还 0。
    完。
    可变 熵_x1000 为 0。
    i 为 0。
    只要 i 小于 做 计数 用 直方图：
        若 直方图 的 第 i 项 大于 0：
            p 为 直方图 的 第 i 项 乘 1000 除以 总笔画。
            可变 位数 为 0。
            可变 v 为 p。
            只要 v 小于 1000：
                v 为 v 乘 2。
                位数 为 位数 加 1。
            完。
            熵_x1000 为 熵_x1000 加 p 乘 位数 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 熵_x1000。
完。

本章 收束。
