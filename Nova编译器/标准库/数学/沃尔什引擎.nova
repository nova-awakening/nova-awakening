《沃尔什引擎》

引用《..·基础》。

沃尔什_绝对值 接受 x：
    若 x 小于 0：
        返还 0 减 x。
    完。
    返还 x。
完。

沃尔什_复制 接受 src：
    可变 目标值 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 src：
        做 追加 用 目标值、src 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 目标值。
完。

沃尔什_最小长 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

沃尔什_整数根 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

沃尔什_快速变换 接受 x：
    n 为 做 计数 用 x。
    若 n 小于或等于 1：
        返还。
    完。
    可变 步长 为 1。
    只要 步长 小于 n：
        块宽 为 步长 乘 2。
        可变 i 为 0。
        只要 i 小于 n：
            可变 j 为 0。
            只要 j 小于 步长：
                左 为 i 加 j。
                右 为 左 加 步长。
                若 右 小于 n：
                    a 为 x 的 第 左 项。
                    b 为 x 的 第 右 项。
                    x 的 第 左 项 为 a 加 b。
                    x 的 第 右 项 为 a 减 b。
                完。
                j 为 j 加 1。
            完。
            i 为 i 加 块宽。
        完。
        步长 为 块宽。
    完。
完。

沃尔什_逆变换 接受 x：
    做 沃尔什_快速变换 用 x。
    n 为 做 计数 用 x。
    若 n 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        x 的 第 i 项 为 x 的 第 i 项 除以 n。
        i 为 i 加 1。
    完。
完。

沃尔什_能量 接受 规格：
    可变 e 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 规格：
        e 为 e 加 规格 的 第 i 项 乘 规格 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 e。
完。

沃尔什_直流分量 接受 规格：
    若 做 计数 用 规格 等于 0：
        返还 0。
    完。
    返还 规格 的 第 0 项。
完。

沃尔什_像素均值 接受 规格：
    若 做 计数 用 规格 等于 0：
        返还 0。
    完。
    返还 做 沃尔什_直流分量 用 规格 除以 做 计数 用 规格。
完。

沃尔什_前K 接受 规格、k：
    n 为 做 计数 用 规格。
    可变 索引列表 为 一组：（空）。
    可变 used 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 used、0。
        i 为 i 加 1。
    完。
    若 n 大于 0：
        used 的 第 0 项 为 1。
    完。
    可变 已找到 为 0。
    只要 已找到 小于 k：
        若 已找到 大于或等于 n 减 1：
            返还 索引列表。
        完。
        可变 最优索引 为 1。
        可变 best_val 为 负 1。
        i 为 1。
        只要 i 小于 n：
            若 used 的 第 i 项 等于 0：
                v 为 做 沃尔什_绝对值 用 规格 的 第 i 项。
                若 v 大于 best_val：
                    best_val 为 v。
                    最优索引 为 i。
                完。
            完。
            i 为 i 加 1。
        完。
        做 追加 用 索引列表、最优索引。
        若 最优索引 小于 做 计数 用 used：
            used 的 第 最优索引 项 为 1。
        完。
        已找到 为 已找到 加 1。
    完。
    返还 索引列表。
完。

沃尔什_低通 接受 规格、cutoff：
    可变 r 为 做 沃尔什_复制 用 规格。
    可变 i 为 cutoff。
    只要 i 小于 做 计数 用 r：
        r 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    返还 r。
完。

沃尔什_高通 接受 规格、cutoff：
    可变 r 为 做 沃尔什_复制 用 规格。
    可变 i 为 1。
    只要 i 小于 cutoff：
        若 i 小于 做 计数 用 r：
            r 的 第 i 项 为 0。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

沃尔什_交叉 接受 specA、specB、cutoff：
    n 为 做 沃尔什_最小长 用 做 计数 用 specA、做 计数 用 specB。
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 cutoff：
            做 追加 用 r、specA 的 第 i 项。
        否则：
            做 追加 用 r、specB 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

沃尔什_插值 接受 specA、specB、学习率：
    n 为 做 沃尔什_最小长 用 做 计数 用 specA、做 计数 用 specB。
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        v 为 specA 的 第 i 项 乘 学习率 除以 1000 加 specB 的 第 i 项 乘 (1000 减 学习率) 除以 1000。
        做 追加 用 r、v。
        i 为 i 加 1。
    完。
    返还 r。
完。

沃尔什_缩放 接受 规格、factor：
    可变 r 为 做 沃尔什_复制 用 规格。
    可变 i 为 0。
    只要 i 小于 做 计数 用 r：
        r 的 第 i 项 为 r 的 第 i 项 乘 factor 除以 1000。
        i 为 i 加 1。
    完。
    返还 r。
完。

沃尔什_从像素 接受 像素列表：
    可变 规格 为 做 沃尔什_复制 用 像素列表。
    可变 i 为 0。
    只要 i 小于 做 计数 用 规格：
        规格 的 第 i 项 为 规格 的 第 i 项 减 128。
        i 为 i 加 1。
    完。
    做 沃尔什_快速变换 用 规格。
    返还 规格。
完。

沃尔什_转像素 接受 规格：
    可变 px 为 做 沃尔什_复制 用 规格。
    做 沃尔什_逆变换 用 px。
    可变 i 为 0。
    只要 i 小于 做 计数 用 px：
        px 的 第 i 项 为 px 的 第 i 项 加 128。
        若 px 的 第 i 项 小于 0：
            px 的 第 i 项 为 0。
        完。
        若 px 的 第 i 项 大于 255：
            px 的 第 i 项 为 255。
        完。
        i 为 i 加 1。
    完。
    返还 px。
完。

沃尔什_GPU快速变换 接受 x：
    做 沃尔什_快速变换 用 x。
完。

沃尔什_支撑 接受 规格：
    n 为 做 计数 用 规格。
    若 n 等于 0：
        返还 0。
    完。
    可变 非零 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 规格 的 第 i 项 不等于 0：
            非零 为 非零 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 非零 乘 1000 除以 n。
完。

沃尔什_稀疏度 接受 规格：
    返还 1000 减 做 沃尔什_支撑 用 规格。
完。

沃尔什_是否全支撑 接受 规格：
    可变 i 为 0。
    只要 i 小于 做 计数 用 规格：
        若 规格 的 第 i 项 等于 0：
            返还 0。
        完。
        i 为 i 加 1。
    完。
    返还 1。
完。

沃尔什_频带能量 接受 规格：
    n 为 做 计数 用 规格。
    若 n 等于 0：
        返还 一组：0。
    完。
    三分 为 n 除以 3。
    可变 低 为 0。
    可变 中 为 0。
    可变 高 为 0。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        e 为 规格 的 第 i 项 乘 规格 的 第 i 项 除以 1000。
        若 i 小于 三分：
            低 为 低 加 e。
        否则：
            若 i 小于 三分 乘 2：
                中 为 中 加 e。
            否则：
                高 为 高 加 e。
            完。
        完。
        总 为 总 加 e。
        i 为 i 加 1。
    完。
    若 总 等于 0：
        返还 一组：0。
    完。
    返还 一组：低 乘 1000 除以 总、中 乘 1000 除以 总、高 乘 1000 除以 总。
完。

沃尔什_熵 接受 规格：
    n 为 做 计数 用 规格。
    可变 总 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总 为 总 加 做 沃尔什_绝对值 用 规格 的 第 i 项。
        i 为 i 加 1。
    完。
    若 总 等于 0：
        返还 0。
    完。
    可变 熵 为 0。
    i 为 0。
    只要 i 小于 n：
        p 为 做 沃尔什_绝对值 用 规格 的 第 i 项 乘 1000 除以 总。
        若 p 大于 0：
            熵 为 熵 加 p 乘 (1000 减 p) 除以 1000。
        完。
        i 为 i 加 1。
    完。
    返还 熵。
完。

沃尔什_阈值处理 接受 规格、阈值：
    可变 r 为 做 沃尔什_复制 用 规格。
    可变 i 为 1。
    只要 i 小于 做 计数 用 r：
        若 做 沃尔什_绝对值 用 r 的 第 i 项 小于 阈值：
            r 的 第 i 项 为 0。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

沃尔什_带通 接受 规格、low、high：
    可变 r 为 做 沃尔什_复制 用 规格。
    可变 i 为 0。
    只要 i 小于 做 计数 用 r：
        若 i 小于 low：
            r 的 第 i 项 为 0。
        否则：
            若 i 大于或等于 high：
                r 的 第 i 项 为 0。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

沃尔什_相似度 接受 a、b：
    n 为 做 沃尔什_最小长 用 做 计数 用 a、做 计数 用 b。
    可变 点积 为 0。
    可变 范a 为 0。
    可变 范b 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        点积 为 点积 加 a 的 第 i 项 乘 b 的 第 i 项 除以 1000。
        范a 为 范a 加 a 的 第 i 项 乘 a 的 第 i 项 除以 1000。
        范b 为 范b 加 b 的 第 i 项 乘 b 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    分母 为 做 沃尔什_整数根 用 范a 乘 做 沃尔什_整数根 用 范b。
    若 分母 等于 0：
        返还 0。
    完。
    返还 点积 乘 1000 除以 分母。
完。

沃尔什_距离 接受 a、b：
    n 为 做 沃尔什_最小长 用 做 计数 用 a、做 计数 用 b。
    可变 d 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        差值 为 a 的 第 i 项 减 b 的 第 i 项。
        d 为 d 加 差值 乘 差值 除以 1000。
        i 为 i 加 1。
    完。
    返还 做 沃尔什_整数根 用 d。
完。

沃尔什_二维变换 接受 data、n：
    可变 缓冲 为 做 沃尔什_复制 用 data。
    可变 行号 为 0。
    只要 行号 小于 n：
        可变 行 为 一组：（空）。
        可变 c 为 0。
        只要 c 小于 n：
            做 追加 用 行、缓冲 的 第 行号 乘 n 加 c 项。
            c 为 c 加 1。
        完。
        做 沃尔什_快速变换 用 行。
        c 为 0。
        只要 c 小于 n：
            缓冲 的 第 行号 乘 n 加 c 项 为 行 的 第 c 项。
            c 为 c 加 1。
        完。
        行号 为 行号 加 1。
    完。
    可变 列号 为 0。
    只要 列号 小于 n：
        可变 列 为 一组：（空）。
        可变 r 为 0。
        只要 r 小于 n：
            做 追加 用 列、缓冲 的 第 r 乘 n 加 列号 项。
            r 为 r 加 1。
        完。
        做 沃尔什_快速变换 用 列。
        r 为 0。
        只要 r 小于 n：
            缓冲 的 第 r 乘 n 加 列号 项 为 列 的 第 r 项。
            r 为 r 加 1。
        完。
        列号 为 列号 加 1。
    完。
    返还 缓冲。
完。

沃尔什_二维逆变换 接受 data、n：
    可变 缓冲 为 做 沃尔什_复制 用 data。
    可变 列号 为 0。
    只要 列号 小于 n：
        可变 列 为 一组：（空）。
        可变 r 为 0。
        只要 r 小于 n：
            做 追加 用 列、缓冲 的 第 r 乘 n 加 列号 项。
            r 为 r 加 1。
        完。
        做 沃尔什_逆变换 用 列。
        r 为 0。
        只要 r 小于 n：
            缓冲 的 第 r 乘 n 加 列号 项 为 列 的 第 r 项。
            r 为 r 加 1。
        完。
        列号 为 列号 加 1。
    完。
    可变 行号 为 0。
    只要 行号 小于 n：
        可变 行 为 一组：（空）。
        可变 c 为 0。
        只要 c 小于 n：
            做 追加 用 行、缓冲 的 第 行号 乘 n 加 c 项。
            c 为 c 加 1。
        完。
        做 沃尔什_逆变换 用 行。
        c 为 0。
        只要 c 小于 n：
            缓冲 的 第 行号 乘 n 加 c 项 为 行 的 第 c 项。
            c 为 c 加 1。
        完。
        行号 为 行号 加 1。
    完。
    返还 缓冲。
完。

沃尔什_卷积计算 接受 a、b：
    n 为 做 沃尔什_最小长 用 做 计数 用 a、做 计数 用 b。
    可变 sa 为 一组：（空）。
    可变 sb 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 sa、a 的 第 i 项。
        做 追加 用 sb、b 的 第 i 项。
        i 为 i 加 1。
    完。
    做 沃尔什_快速变换 用 sa。
    做 沃尔什_快速变换 用 sb。
    i 为 0。
    只要 i 小于 n：
        sa 的 第 i 项 为 sa 的 第 i 项 乘 sb 的 第 i 项 除以 n。
        i 为 i 加 1。
    完。
    做 沃尔什_逆变换 用 sa。
    返还 sa。
完。

沃尔什_压缩 接受 data、k：
    n 为 做 计数 用 data。
    可变 规格 为 做 沃尔什_复制 用 data。
    做 沃尔什_快速变换 用 规格。
    可变 阈值列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 阈值列表、做 沃尔什_绝对值 用 规格 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 si 为 0。
    只要 si 小于 n：
        可变 sj 为 si 加 1。
        只要 sj 小于 n：
            若 阈值列表 的 第 sj 项 大于 阈值列表 的 第 si 项：
                t 为 阈值列表 的 第 si 项。
                阈值列表 的 第 si 项 为 阈值列表 的 第 sj 项。
                阈值列表 的 第 sj 项 为 t。
            完。
            sj 为 sj 加 1。
        完。
        si 为 si 加 1。
    完。
    可变 cutoff 为 0。
    若 k 小于 n：
        cutoff 为 阈值列表 的 第 k 项。
    完。
    i 为 0。
    只要 i 小于 n：
        若 做 沃尔什_绝对值 用 规格 的 第 i 项 小于 cutoff：
            规格 的 第 i 项 为 0。
        完。
        i 为 i 加 1。
    完。
    做 沃尔什_逆变换 用 规格。
    返还 规格。
完。

沃尔什_压缩比 接受 原始、k：
    n 为 做 计数 用 原始。
    若 n 等于 0：
        返还 0。
    完。
    返还 k 乘 1000 除以 n。
完。

沃尔什_峰值信噪比 接受 原始、重建：
    n 为 做 沃尔什_最小长 用 做 计数 用 原始、做 计数 用 重建。
    若 n 等于 0：
        返还 0。
    完。
    可变 mse 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        d 为 原始 的 第 i 项 减 重建 的 第 i 项。
        mse 为 mse 加 d 乘 d。
        i 为 i 加 1。
    完。
    mse 为 mse 除以 n。
    若 mse 等于 0：
        返还 99000。
    完。
    可变 比率 为 255 乘 255 乘 1000 除以 mse。
    可变 对数值 为 0。
    只要 比率 大于 10：
        比率 为 比率 除以 10。
        对数值 为 对数值 加 1000。
    完。
    返还 10 乘 对数值。
完。

沃尔什_功率谱 接受 规格：
    可变 功率 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 规格：
        做 追加 用 功率、规格 的 第 i 项 乘 规格 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 功率。
完。

沃尔什_降噪 接受 data、阈值：
    n 为 做 计数 用 data。
    可变 规格 为 做 沃尔什_复制 用 data。
    做 沃尔什_快速变换 用 规格。
    可变 i 为 1。
    只要 i 小于 n：
        a 为 做 沃尔什_绝对值 用 规格 的 第 i 项。
        若 a 小于或等于 阈值：
            规格 的 第 i 项 为 0。
        否则：
            若 规格 的 第 i 项 大于 0：
                规格 的 第 i 项 为 a 减 阈值。
            否则：
                规格 的 第 i 项 为 0 减 (a 减 阈值)。
            完。
        完。
        i 为 i 加 1。
    完。
    做 沃尔什_逆变换 用 规格。
    返还 规格。
完。

沃尔什_测试 接受 零参：
    可变 data 为 一组：10、20、30、40、50、60、70、80。
    可变 original 为 做 沃尔什_复制 用 data。
    做 沃尔什_快速变换 用 data。
    做 沃尔什_逆变换 用 data。
    可变 ok 为 1。
    可变 i 为 0。
    只要 i 小于 做 计数 用 data：
        若 data 的 第 i 项 不等于 original 的 第 i 项：
            ok 为 0。
        完。
        i 为 i 加 1。
    完。
    tops 为 做 沃尔什_前K 用 一组：1000、5、100、3、200、1、50、2、3。
    返还 一组：ok、tops。
完。

本章 收束。
