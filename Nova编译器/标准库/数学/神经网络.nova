《神经网络》

引用《..·基础》。

引用《..·..·运行时·原语桩》。

引用《损失函数》。

引用《..·算法·数学扩展》。

引用《.·基础·数学库》。

引用《..·激活函数·激活函数》。

引用《..·函数式·函数式》。

引用《工具》。

网络精度 为 1000。

量子_半 为 500。

Q开方 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    可变 r 为 x。
    可变 i 为 0。
    只要 i 小于 16：
        r 为 (r 加 x 乘 网络精度 除以 (r 加 1)) 除以 2。
        i 为 i 加 1。
    完。
    返还 r。
完。

Tensor_读 接受 t、i：
    返还 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
完。

Tensor_写 接受 t、i、v：
    做 内存_写32 用 t 的 数据 加 i 乘 4、v。
完。

全连接_创建 接受 输入维、输出维：
    可变 权重 为 一组：输出维、输入维。
    可变 种子 为 12345。
    可变 i 为 0。
    只要 i 小于 输出维 乘 输入维：
        种子 为 种子 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        w 为 种子 取余于 200 减 100。
        做 追加 用 权重、w。
        i 为 i 加 1。
    完。
    可变 偏置 为 一组：（空）。
    i 为 0。
    只要 i 小于 输出维：
        做 追加 用 偏置、0。
        i 为 i 加 1。
    完。
    返还 一组：输入维、输出维、权重、偏置、一组：（空）、一组：（空）。
完。

全连接_前向 接受 层、输入：
    输出维 为 层 的 第 1 项。
    输入维 为 层 的 第 0 项。
    权重 为 层 的 第 2 项。
    偏置 为 层 的 第 3 项。
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 输出维：
        可变 和 为 偏置 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 输入维：
            和 为 和 加 权重 的 第 2 加 i 乘 输入维 加 j 项 乘 输入 的 第 j 项 除以 网络精度。
            j 为 j 加 1。
        完。
        做 追加 用 输出、和。
        i 为 i 加 1。
    完。
    层 的 第 4 项 为 输入。
    层 的 第 5 项 为 输出。
    返还 输出。
完。

全连接_反向 接受 层、输出梯度：
    输出维 为 层 的 第 1 项。
    输入维 为 层 的 第 0 项。
    权重 为 层 的 第 2 项。
    输入 为 层 的 第 4 项。
    可变 输入梯度 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 输入维：
        可变 和 为 0。
        可变 i 为 0。
        只要 i 小于 输出维：
            和 为 和 加 权重 的 第 2 加 i 乘 输入维 加 j 项 乘 输出梯度 的 第 i 项 除以 网络精度。
            i 为 i 加 1。
        完。
        做 追加 用 输入梯度、和。
        j 为 j 加 1。
    完。
    可变 权重梯度 为 一组：（空）。
    可变 i2 为 0。
    只要 i2 小于 输出维：
        j 为 0。
        只要 j 小于 输入维：
            做 追加 用 权重梯度、输出梯度 的 第 i2 项 乘 输入 的 第 j 项 除以 网络精度。
            j 为 j 加 1。
        完。
        i2 为 i2 加 1。
    完。
    返还 一组：输入梯度、权重梯度、输出梯度。
完。

全连接_更新 接受 层、权重梯度、偏置梯度、学习率：
    输出维 为 层 的 第 1 项。
    输入维 为 层 的 第 0 项。
    权重 为 层 的 第 2 项。
    偏置 为 层 的 第 3 项。
    可变 i 为 0。
    只要 i 小于 输出维 乘 输入维：
        权重 的 第 2 加 i 项 为 权重 的 第 2 加 i 项 减 学习率 乘 权重梯度 的 第 i 项 除以 网络精度。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 输出维：
        偏置 的 第 i 项 为 偏置 的 第 i 项 减 学习率 乘 偏置梯度 的 第 i 项 除以 网络精度。
        i 为 i 加 1。
    完。
完。

relu层_前向 接受 输入：
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 输入：
        若 输入 的 第 i 项 大于 0：
            做 追加 用 输出、输入 的 第 i 项。
        否则：
            做 追加 用 输出、0。
        完。
        i 为 i 加 1。
    完。
    返还 输出。
完。

relu层_反向 接受 输入、输出梯度：
    可变 梯度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 输入：
        若 输入 的 第 i 项 大于 0：
            做 追加 用 梯度、输出梯度 的 第 i 项。
        否则：
            做 追加 用 梯度、0。
        完。
        i 为 i 加 1。
    完。
    返还 梯度。
完。

mlp_创建 接受 层维度：
    层数 为 做 计数 用 层维度 减 1。
    可变 层列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 层数：
        做 追加 用 层列表、做 全连接_创建 用 层维度 的 第 i 项、层维度 的 第 i 加 1 项。
        i 为 i 加 1。
    完。
    返还 一组：层列表、层数。
完。

mlp_前向 接受 网络、输入：
    层列表 为 网络 的 第 0 项。
    层数 为 网络 的 第 1 项。
    可变 当前 为 输入。
    可变 i 为 0。
    只要 i 小于 层数：
        当前 为 做 全连接_前向 用 层列表 的 第 i 项、当前。
        若 i 小于 层数 减 1：
            当前 为 做 relu层_前向 用 当前。
        完。
        i 为 i 加 1。
    完。
    返还 当前。
完。

准确率 接受 预测列表、真实列表：
    n 为 做 计数 用 预测列表。
    可变 i 为 0。
    只要 i 小于 n：
        若 预测列表 的 第 i 项 等于 真实列表 的 第 i 项：
            正确 为 正确 加 1。
        完。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 正确 乘 网络精度 除以 n。
完。

最大值索引 接受 向量：
    n 为 做 计数 用 向量。
    可变 最大索引 为 0。
    可变 最大值 为 向量 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 向量 的 第 i 项 大于 最大值：
            最大值 为 向量 的 第 i 项。
            最大索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最大索引。
完。

最小值索引 接受 向量：
    n 为 做 计数 用 向量。
    可变 最小索引 为 0。
    可变 最小值 为 向量 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 向量 的 第 i 项 小于 最小值：
            最小值 为 向量 的 第 i 项。
            最小索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 最小索引。
完。

向量_求和 接受 v：
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        和 为 和 加 v 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

向量_平均 接受 v：
    返还 做 向量_求和 用 v 除以 做 计数 用 v。
完。

sgd动量_创建 接受 参数数：
    可变 速度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数数：
        做 追加 用 速度、0。
        i 为 i 加 1。
    完。
    返还 一组：速度。
完。

sgd动量_更新 接受 状态、参数、梯度、学习率、动量：
    速度 为 状态 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        速度 的 第 i 项 为 动量 乘 速度 的 第 i 项 除以 网络精度 减 学习率 乘 梯度 的 第 i 项 除以 网络精度。
        参数 的 第 i 项 为 参数 的 第 i 项 加 速度 的 第 i 项。
        i 为 i 加 1。
    完。
完。

打乱 接受 列表、种子：
    n 为 做 计数 用 列表。
    可变 s 为 种子。
    可变 i 为 n 减 1。
    只要 i 大于 0：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        j 为 s 取余于 (i 加 1)。
        暂存 为 列表 的 第 i 项。
        列表 的 第 i 项 为 列表 的 第 j 项。
        列表 的 第 j 项 为 暂存。
        i 为 i 减 1。
    完。
完。

生成批次 接受 数据、批大小：
    n 为 做 计数 用 数据。
    可变 批次列表 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 批次 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 批大小 并且 i 加 j 小于 n：
            做 追加 用 批次、数据 的 第 i 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 批次列表、批次。
        i 为 i 加 批大小。
    完。
    返还 批次列表。
完。

独热编码 接受 索引、类数：
    可变 向量 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 类数：
        若 i 等于 索引：
            做 追加 用 向量、网络精度。
        否则：
            做 追加 用 向量、0。
        完。
        i 为 i 加 1。
    完。
    返还 向量。
完。

归一化 接受 数据：
    n 为 做 计数 用 数据。
    可变 最小 为 数据 的 第 0 项。
    可变 最大 为 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 数据 的 第 i 项 小于 最小：
            最小 为 数据 的 第 i 项。
        完。
        若 数据 的 第 i 项 大于 最大：
            最大 为 数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 范围 为 最大 减 最小。
    若 范围 等于 0：
        范围 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、(数据 的 第 i 项 减 最小) 乘 网络精度 除以 范围。
        i 为 i 加 1。
    完。
    返还 结果。
完。

标准化 接受 数据：
    n 为 做 计数 用 数据。
    可变 方差和 为 0。
    均值 为 做 向量_平均 用 数据。
    可变 i 为 0。
    只要 i 小于 n：
        差 为 数据 的 第 i 项 减 均值。
        方差和 为 方差和 加 差 乘 差。
        i 为 i 加 1。
    完。
    可变 标准差 为 做 整数开方 用 方差和 除以 n。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、(数据 的 第 i 项 减 均值) 乘 网络精度 除以 标准差。
        i 为 i 加 1。
    完。
    返还 结果。
完。

神经网络_整数开方 接受 n：
    返还 做 开方 用 n。
完。

批归一化_创建 接受 维度：
    可变 衰减率 为 一组：（空）。
    可变 动量率 为 一组：（空）。
    可变 均值 为 一组：（空）。
    可变 方差 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 插入 用 衰减率、做 计数 用 衰减率、网络精度。
        做 插入 用 动量率、做 计数 用 动量率。
        做 插入 用 均值、做 计数 用 均值。
        做 插入 用 方差、做 计数 用 方差、网络精度。
        i 为 i 加 1。
    完。
    返还 一组：衰减率、动量率、均值、方差、1、900。
完。

批归一化_前向 接受 bn、输入：
    n 为 做 计数 用 输入。
    衰减率 为 bn 的 第 0 项。
    动量率 为 bn 的 第 1 项。
    可变 批均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        批均值 为 批均值 加 输入 的 第 i 项。
        i 为 i 加 1。
    完。
    批均值 为 批均值 除以 n。
    可变 批方差 为 0。
    i 为 0。
    只要 i 小于 n：
        d 为 输入 的 第 i 项 减 批均值。
        批方差 为 批方差 加 d 乘 d 除以 网络精度。
        i 为 i 加 1。
    完。
    批方差 为 批方差 除以 n。
    可变 标准差 为 做 开方 用 批方差 加 bn 的 第 4 项。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    可变 输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        索引 为 i 取余于 做 计数 用 衰减率。
        归一 为 (输入 的 第 i 项 减 批均值) 乘 网络精度 除以 标准差。
        做 插入 用 输出、做 计数 用 输出、衰减率 的 第 索引 项 乘 归一 除以 网络精度 加 动量率 的 第 索引 项。
        i 为 i 加 1。
    完。
    动量 为 bn 的 第 5 项。
    可变 j 为 0。
    只要 j 小于 做 计数 用 bn 的 第 2 项：
        bn 的 第 2 项 的 第 j 项 为 动量 乘 bn 的 第 2 项 的 第 j 项 除以 网络精度 加 (网络精度 减 动量) 乘 批均值 除以 网络精度。
        bn 的 第 3 项 的 第 j 项 为 动量 乘 bn 的 第 3 项 的 第 j 项 除以 网络精度 加 (网络精度 减 动量) 乘 批方差 除以 网络精度。
        j 为 j 加 1。
    完。
    返还 输出。
完。

dropout_前向 接受 输入、保持率、种子、训练：
    若 训练 等于 0：
        返还 输入。
    完。
    n 为 做 计数 用 输入。
    可变 输出 为 一组：（空）。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 n：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        若 s 取余于 1000 小于 保持率：
            做 插入 用 输出、做 计数 用 输出、输入 的 第 i 项 乘 网络精度 除以 保持率。
        否则：
            做 插入 用 输出、做 计数 用 输出。
        完。
        i 为 i 加 1。
    完。
    返还 输出。
完。

leaky_relu_前向 接受 输入、学习率：
    可变 输出 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 输入：
        若 输入 的 第 i 项 大于 0：
            做 插入 用 输出、做 计数 用 输出、输入 的 第 i 项。
        否则：
            做 插入 用 输出、做 计数 用 输出、输入 的 第 i 项 乘 学习率 除以 网络精度。
        完。
        i 为 i 加 1。
    完。
    返还 输出。
完。

leaky_relu_反向 接受 输入、输出梯度、学习率：
    可变 梯度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 输入：
        若 输入 的 第 i 项 大于 0：
            做 插入 用 梯度、做 计数 用 梯度、输出梯度 的 第 i 项。
        否则：
            做 插入 用 梯度、做 计数 用 梯度、输出梯度 的 第 i 项 乘 学习率 除以 网络精度。
        完。
        i 为 i 加 1。
    完。
    返还 梯度。
完。

残差_前向 接受 层、输入：
    中间 为 做 全连接_前向 用 层、输入。
    激活 为 做 relu层_前向 用 中间。
    若 做 计数 用 激活 等于 做 计数 用 输入：
        可变 结果 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 做 计数 用 激活：
            做 插入 用 结果、做 计数 用 结果、激活 的 第 i 项 加 输入 的 第 i 项。
            i 为 i 加 1。
        完。
        返还 结果。
    完。
    返还 激活。
完。

神经_softmax 接受 输入：
    n 为 做 计数 用 输入。
    可变 最大 为 输入 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        若 输入 的 第 i 项 大于 最大：
            最大 为 输入 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 指数值 为 一组：（空）。
    可变 总和 为 0。
    i 为 0。
    只要 i 小于 n：
        x 为 输入 的 第 i 项 减 最大。
        可变 ex 为 0。
        若 x 大于或等于 0：
            ex 为 网络精度。
        否则若 x 大于 0 减 网络精度：
            ex 为 网络精度 加 x。
        否则若 x 大于 0 减 3 乘 网络精度：
            ex 为 (网络精度 加 x 除以 3) 乘 (网络精度 加 x 除以 3) 除以 网络精度。
        否则：
            ex 为 1。
        完。
        若 ex 小于 1：
            ex 为 1。
        完。
        做 插入 用 指数值、做 计数 用 指数值、ex。
        总和 为 总和 加 ex。
        i 为 i 加 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 插入 用 结果、做 计数 用 结果、指数值 的 第 i 项 乘 网络精度 除以 总和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

全连接_创建_xavier 接受 输入维、输出维、种子：
    范围 为 做 开方 用 6 乘 网络精度 除以 (输入维 加 输出维)。
    可变 权重 为 一组：输出维、输入维。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 输出维 乘 输入维：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        w 为 s 取余于 (2 乘 范围 加 1) 减 范围。
        做 插入 用 权重、做 计数 用 权重、w。
        i 为 i 加 1。
    完。
    可变 偏置 为 一组：（空）。
    i 为 0。
    只要 i 小于 输出维：
        做 插入 用 偏置、做 计数 用 偏置。
        i 为 i 加 1。
    完。
    返还 一组：输入维、输出维、权重、偏置、一组：（空）、一组：（空）。
完。

全连接_创建_he 接受 输入维、输出维、种子：
    范围 为 做 开方 用 2 乘 网络精度 除以 输入维。
    可变 权重 为 一组：输出维、输入维。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 输出维 乘 输入维：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        w 为 s 取余于 (2 乘 范围 加 1) 减 范围。
        做 插入 用 权重、做 计数 用 权重、w。
        i 为 i 加 1。
    完。
    可变 偏置 为 一组：（空）。
    i 为 0。
    只要 i 小于 输出维：
        做 插入 用 偏置、做 计数 用 偏置。
        i 为 i 加 1。
    完。
    返还 一组：输入维、输出维、权重、偏置、一组：（空）、一组：（空）。
完。

神经_学习率_预热 接受 步数、预热步、目标率：
    若 步数 大于或等于 预热步：
        返还 目标率。
    完。
    若 预热步 等于 0：
        返还 0。
    完。
    返还 目标率 乘 步数 除以 预热步。
完。

学习率_余弦 接受 步数、总步数、最大率、最小率：
    若 步数 大于或等于 总步数：
        返还 最小率。
    完。
    t 为 步数 乘 网络精度 除以 总步数。
    余弦值 为 网络精度 减 2 乘 t 乘 t 除以 网络精度。
    返还 最小率 加 (最大率 减 最小率) 乘 (网络精度 加 余弦值) 除以 (2 乘 网络精度)。
完。

神经网络_学习率_阶梯 接受 步数、阶梯大小、初始率、衰减因子：
    可变 率 为 初始率。
    可变 s 为 步数。
    只要 s 大于或等于 阶梯大小：
        率 为 率 乘 衰减因子 除以 网络精度。
        s 为 s 减 阶梯大小。
    完。
    返还 率。
完。

梯度裁剪_范数 接受 梯度、最大范数：
    可变 范数平方 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度：
        范数平方 为 范数平方 加 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 网络精度。
        i 为 i 加 1。
    完。
    范数 为 做 开方 用 范数平方。
    若 范数 大于 最大范数：
        缩放 为 最大范数 乘 网络精度 除以 范数。
        i 为 0。
        只要 i 小于 做 计数 用 梯度：
            梯度 的 第 i 项 为 梯度 的 第 i 项 乘 缩放 除以 网络精度。
            i 为 i 加 1。
        完。
    完。
完。

梯度裁剪_值 接受 梯度、最大值：
    可变 i 为 0。
    只要 i 小于 做 计数 用 梯度：
        若 梯度 的 第 i 项 大于 最大值：
            梯度 的 第 i 项 为 最大值。
        完。
        若 梯度 的 第 i 项 小于 0 减 最大值：
            梯度 的 第 i 项 为 0 减 最大值。
        完。
        i 为 i 加 1。
    完。
完。

神经网络_xavier初始化 接受 输入维、输出维、种子：
    可变 范围 为 做 开方 用 6 乘 网络精度 除以 (输入维 加 输出维)。
    若 范围 等于 0：
        范围 为 1。
    完。
    可变 权重 为 一组：输出维、输入维。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 输出维 乘 输入维：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        做 追加 用 权重、s 取余于 (2 乘 范围 加 1) 减 范围。
        i 为 i 加 1。
    完。
    返还 权重。
完。

神经网络_he初始化 接受 输入维、输出维、种子：
    可变 范围 为 做 开方 用 2 乘 网络精度 除以 输入维。
    若 范围 等于 0：
        范围 为 1。
    完。
    可变 权重 为 一组：输出维、输入维。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 输出维 乘 输入维：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        做 追加 用 权重、s 取余于 (2 乘 范围 加 1) 减 范围。
        i 为 i 加 1。
    完。
    返还 权重。
完。

神经_归一化 接受 输入、衰减率、动量率：
    n 为 做 计数 用 输入。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 输入 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 n：
        d 为 输入 的 第 i 项 减 均值。
        方差 为 方差 加 d 乘 d 除以 网络精度。
        i 为 i 加 1。
    完。
    方差 为 方差 除以 n。
    可变 标准差 为 做 开方 用 方差。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    可变 输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        归一 为 (输入 的 第 i 项 减 均值) 乘 网络精度 除以 标准差。
        做 追加 用 输出、归一 乘 衰减率 的 第 i 项 除以 网络精度 加 动量率 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 输出。
完。

rms归一化 接受 输入、衰减率：
    n 为 做 计数 用 输入。
    可变 和平方 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和平方 为 和平方 加 输入 的 第 i 项 乘 输入 的 第 i 项 除以 网络精度。
        i 为 i 加 1。
    完。
    可变 均方根值 为 做 开方 用 和平方 除以 n。
    若 均方根值 等于 0：
        均方根值 为 1。
    完。
    可变 输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 输出、输入 的 第 i 项 乘 衰减率 的 第 i 项 除以 均方根值。
        i 为 i 加 1。
    完。
    返还 输出。
完。

神经_残差连接 接受 输入、子层输出：
    n 为 做 计数 用 输入。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、输入 的 第 i 项 加 子层输出 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

权重衰减 接受 参数、衰减系数：
    n 为 做 计数 用 参数。
    可变 i 为 0。
    只要 i 小于 n：
        参数 的 第 i 项 为 参数 的 第 i 项 减 衰减系数 乘 参数 的 第 i 项 除以 网络精度。
        i 为 i 加 1。
    完。
完。

七态向量量化_压缩 接受 向量、压缩输出、维度：
    若 向量 等于 0 或者 压缩输出 等于 0 或者 维度 等于 0：
        返还。
    完。
    可变 sum_sq 为 0。
    可变 i 为 0。
    只要 i 小于 维度：
        v 为 做 Tensor_读 用 向量、i。
        sum_sq 为 sum_sq 加 做 Q乘 用 v、v。
        i 为 i 加 1。
    完。
    模长 为 做 Q开方 用 sum_sq。
    若 模长 等于 0：
        返还。
    完。
    i 为 0。
    只要 i 小于 维度：
        v 为 做 Tensor_读 用 向量、i。
        方向4 为 v 乘 7 除以 模长。
        若 方向4 大于 7：
            方向4 为 7。
        完。
        若 方向4 小于 负 7：
            方向4 为 负 7。
        完。
        幅值 为 v 乘 定点_一 除以 模长。
        若 幅值 小于 0：
            幅值 为 0 减 幅值。
        完。
        幅值4 为 幅值 乘 15 除以 定点_一。
        若 幅值4 大于 15：
            幅值4 为 15。
        完。
        编码 为 方向4 加 7 左移 4 位或 幅值4。
        做 内存_写8 用 压缩输出 加 i、编码。
        i 为 i 加 1。
    完。
完。

七态向量量化_解压 接受 压缩输入、向量输出、维度、模长_Q10：
    若 压缩输入 等于 0 或者 向量输出 等于 0：
        返还。
    完。
    可变 i 为 0。
    只要 i 小于 维度：
        编码 为 做 内存_读8 用 压缩输入 加 i。
        方向4 为 (编码 右移 4 位与 15) 减 7。
        幅值4 为 编码 位与 15。
        v 为 方向4 乘 模长_Q10 除以 7 乘 幅值4 除以 15。
        做 Tensor_写 用 向量输出、i、v。
        i 为 i 加 1。
    完。
完。

七态向量量化_内积误差估计 接受 a_模长、b_模长、维度：
    误差_Q10 为 定点_一 乘 10 除以 做 Q开方 用 维度 乘 定点_一。
    返还 误差_Q10。
完。

梯度检查_误差 接受 解析梯度、数值梯度：
    n 为 做 计数 用 解析梯度。
    可变 最大误差 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 差值 为 解析梯度 的 第 i 项 减 数值梯度 的 第 i 项。
        若 差值 小于 0：
            差值 为 0 减 差值。
        完。
        若 差值 大于 最大误差：
            最大误差 为 差值。
        完。
        i 为 i 加 1。
    完。
    返还 最大误差。
完。

批归一化 接受 v：
    若 做 计数 用 v 等于 0：
        返还 一组：（空）。
    完。
    均值 为 做 向量_和 用 v 除以 做 计数 用 v。
    可变 var_sum 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        d 为 v 的 第 i 项 减 均值。
        var_sum 为 var_sum 加 d 乘 d。
        i 为 i 加 1。
    完。
    variance 为 var_sum 除以 做 计数 用 v。
    可变 标准差 为 做 整数开方 用 variance。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    可变 r 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 v：
        做 追加 用 r、(v 的 第 i 项 减 均值) 乘 100 除以 标准差。
        i 为 i 加 1。
    完。
    返还 r。
完。

截断 接受 x、最小、最大：
    若 x 小于 最小：
        返还 最小。
    完。
    若 x 大于 最大：
        返还 最大。
    完。
    返还 x。
完。

随机丢弃层 接受 v、掩码：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        若 掩码 的 第 i 项 等于 0：
            做 追加 用 r、0。
        否则：
            做 追加 用 r、v 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

激活_HexSigmoid_单值 接受 x：
    ax 为 x。
    可变 符号 为 1。
    若 x 小于 0：
        ax 为 0 减 x。
        符号 为 负 1。
    完。
    可变 out 为 0。
    若 ax 大于 6 乘 定点_一：
        out 为 定点_一。
    否则若 ax 大于 3 乘 定点_一：
        out 为 定点_一 减 (6 乘 定点_一 减 ax) 乘 3 除以 (3 乘 定点_一)。
    否则若 ax 大于 定点_一：
        out 为 970 减 (3 乘 定点_一 减 ax) 乘 82 除以 (2 乘 定点_一)。
    否则若 ax 大于 338：
        out 为 731 减 (定点_一 减 ax) 乘 238 除以 (定点_一 减 338)。
    否则：
        out 为 量子_半 加 ax 乘 250 除以 338。
    完。
    若 符号 小于 0：
        out 为 定点_一 减 out。
    完。
    返还 out。
完。

激活_HexSigmoid 接受 t：
    若 t 等于 0：
        返还。
    完。
    总 为 t 的 行数 乘 t 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        x 为 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        做 内存_写32 用 t 的 数据 加 i 乘 4、做 激活_HexSigmoid_单值 用 x。
        i 为 i 加 1。
    完。
完。

Leaky修正线性导数 接受 x、学习率：
    若 x 大于 0：
        返还 1。
    完。
    返还 学习率。
完。

MAE损失 接受 预测、真实：
    若 做 计数 用 预测 等于 0：
        返还 0。
    完。
    可变 总误差 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测：
        总误差 为 总误差 加 做 绝对值 用 预测 的 第 i 项 减 真实 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总误差 除以 做 计数 用 预测。
完。

MSE损失 接受 预测、真实：
    若 做 计数 用 预测 等于 0：
        返还 0。
    完。
    可变 总误差 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 预测：
        可变 偏差 为 预测 的 第 i 项 减 真实 的 第 i 项。
        总误差 为 总误差 加 偏差 乘 偏差。
        i 为 i 加 1。
    完。
    返还 总误差 除以 做 计数 用 预测。
完。

独热编码2 接受 索引、类别数：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 类别数：
        若 i 等于 索引：
            做 追加 用 r、1。
        否则：
            做 追加 用 r、0。
        完。
        i 为 i 加 1。
    完。
    返还 r。
完。

激活_SQNL 接受 t：
    若 t 等于 0：
        返还。
    完。
    总 为 t 的 行数 乘 t 的 列数。
    可变 i 为 0。
    只要 i 小于 总：
        x 为 做 内存_读32有符号 用 t 的 数据 加 i 乘 4。
        可变 结果 为 0。
        若 x 大于 2 乘 定点_一：
            结果 为 定点_一。
        否则若 x 小于 负 2 乘 定点_一：
            结果 为 负 定点_一。
        否则若 x 大于或等于 0：
            结果 为 x 减 做 Q乘 用 x、x 除以 4。
        否则：
            结果 为 x 加 做 Q乘 用 x、x 除以 4。
        完。
        做 内存_写32 用 t 的 数据 加 i 乘 4、结果。
        i 为 i 加 1。
    完。
完。

修正线性导数 接受 x：
    若 x 大于 0：
        返还 1。
    完。
    返还 0。
完。

线性整流6 接受 x：
    返还 做 截断 用 x、0、6。
完。

随机梯度_更新 接受 参数、梯度、学习率：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        做 追加 用 r、参数 的 第 i 项 减 学习率 乘 梯度 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

S形近似 接受 x：
    若 x 大于 5：
        返还 100。
    完。
    若 x 小于 负 5：
        返还 0。
    完。
    返还 50 加 x 乘 10。
完。

Tanh近似 接受 x：
    若 x 大于 5：
        返还 100。
    完。
    若 x 小于 负 5：
        返还 负 100。
    完。
    返还 x 乘 20。
完。

交叉熵损失 接受 softmax输出、标签索引：
    p 为 softmax输出 的 第 标签索引 项。
    若 p 小于或等于 0：
        返还 10000。
    完。
    可变 对数值 为 0。
    可变 x 为 p。
    只要 x 小于 1000：
        对数值 为 对数值 加 1。
        x 为 x 乘 2。
    完。
    返还 对数值 乘 70。
完。

向量_修正线性 接受 v：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        做 追加 用 r、做 线性整流 用 v 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

向量_argmax 接受 v：
    可变 m 为 v 的 第 0 项。
    可变 索引 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 v：
        若 v 的 第 i 项 大于 m：
            m 为 v 的 第 i 项。
            索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 索引。
完。

向量_加 接受 a、b：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a：
        做 追加 用 r、a 的 第 i 项 加 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

向量_和 接受 v：
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        总和 为 总和 加 v 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 总和。
完。

向量_最大值 接受 v：
    返还 做 列表最大 用 v。
完。

向量_标量乘 接受 v、s：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 v：
        做 追加 用 r、v 的 第 i 项 乘 s。
        i 为 i 加 1。
    完。
    返还 r。
完。

向量_逐元素乘 接受 a、b：
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 a：
        做 追加 用 r、a 的 第 i 项 乘 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 r。
完。

计算图_值 接受 节点：
    返还 节点 的 第 1 项。
完。

计算图_加 接受 a、b：
    返还 一组：1、a 的 第 1 项 加 b 的 第 1 项、0、一组：a、b。
完。

计算图_乘 接受 a、b：
    返还 一组：1、a 的 第 1 项 乘 b 的 第 1 项、0、一组：a、b。
完。

计算图_输入 接受 值：
    返还 一组：0、值、0、一组：（空）。
完。

计算图_常量 接受 值：
    返还 一组：2、值、0、一组：（空）。
完。

阶跃 接受 x：
    若 x 大于或等于 0：
        返还 1。
    完。
    返还 0。
完。

本章 收束。
