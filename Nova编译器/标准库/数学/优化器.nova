《优化器》

引用《..·基础》。

引用《..·..·运行时·原语桩》。

优化精度 为 1000。

sgd_更新 接受 参数、梯度、学习率：
    n 为 做 计数 用 参数。
    可变 i 为 0。
    只要 i 小于 n：
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 梯度 的 第 i 项 除以 优化精度。
        i 为 i 加 1。
    完。
完。

sgd动量_初始化 接受 参数数：
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数数：
        做 追加 用 v、0。
        i 为 i 加 1。
    完。
    返还 一组：v。
完。

sgd动量_步进 接受 状态、参数、梯度、学习率、动量系数：
    v 为 状态 的 第 0 项。
    n 为 做 计数 用 参数。
    可变 i 为 0。
    只要 i 小于 n：
        v 的 第 i 项 为 动量系数 乘 v 的 第 i 项 除以 优化精度 减 学习率 乘 梯度 的 第 i 项 除以 优化精度。
        参数 的 第 i 项 为 参数 的 第 i 项 加 v 的 第 i 项。
        i 为 i 加 1。
    完。
完。

adagrad_初始化 接受 参数数：
    可变 g 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数数：
        做 追加 用 g、0。
        i 为 i 加 1。
    完。
    返还 一组：g。
完。

adagrad_步进 接受 状态、参数、梯度、学习率：
    g 为 状态 的 第 0 项。
    n 为 做 计数 用 参数。
    可变 i 为 0。
    只要 i 小于 n：
        g 的 第 i 项 为 g 的 第 i 项 加 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 优化精度。
        分母 为 做 优化_开方 用 g 的 第 i 项 加 1。
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 梯度 的 第 i 项 除以 分母。
        i 为 i 加 1。
    完。
完。

rmsprop_初始化 接受 参数数：
    可变 s 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数数：
        做 追加 用 s、0。
        i 为 i 加 1。
    完。
    返还 一组：s。
完。

rmsprop_步进 接受 状态、参数、梯度、学习率、衰减率：
    s 为 状态 的 第 0 项。
    n 为 做 计数 用 参数。
    可变 i 为 0。
    只要 i 小于 n：
        s 的 第 i 项 为 衰减率 乘 s 的 第 i 项 除以 优化精度 加 (优化精度 减 衰减率) 乘 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 (优化精度 乘 优化精度)。
        分母 为 做 优化_开方 用 s 的 第 i 项 加 1。
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 梯度 的 第 i 项 除以 分母。
        i 为 i 加 1。
    完。
完。

adam_初始化 接受 参数数：
    可变 m 为 一组：（空）。
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数数：
        做 追加 用 m、0。
        做 追加 用 v、0。
        i 为 i 加 1。
    完。
    返还 一组：m、v、0、900、999。
完。

adam_步进 接受 状态、参数、梯度、学习率：
    m 为 状态 的 第 0 项。
    v 为 状态 的 第 1 项。
    状态 的 第 2 项 为 状态 的 第 2 项 加 1。
    t 为 状态 的 第 2 项。
    衰减率1 为 状态 的 第 3 项。
    衰减率2 为 状态 的 第 4 项。
    n 为 做 计数 用 参数。
    可变 b1t 为 优化精度。
    可变 b2t 为 优化精度。
    可变 ti 为 0。
    只要 ti 小于 t：
        b1t 为 b1t 乘 衰减率1 除以 优化精度。
        b2t 为 b2t 乘 衰减率2 除以 优化精度。
        ti 为 ti 加 1。
    完。
    可变 校正1 为 优化精度 减 b1t。
    可变 校正2 为 优化精度 减 b2t。
    若 校正1 小于 1：
        校正1 为 1。
    完。
    若 校正2 小于 1：
        校正2 为 1。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        m 的 第 i 项 为 衰减率1 乘 m 的 第 i 项 除以 优化精度 加 (优化精度 减 衰减率1) 乘 梯度 的 第 i 项 除以 优化精度。
        v 的 第 i 项 为 衰减率2 乘 v 的 第 i 项 除以 优化精度 加 (优化精度 减 衰减率2) 乘 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 (优化精度 乘 优化精度)。
        一阶矫正 为 m 的 第 i 项 乘 优化精度 除以 校正1。
        二阶矫正 为 v 的 第 i 项 乘 优化精度 除以 校正2。
        分母 为 做 优化_开方 用 二阶矫正 加 1。
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 一阶矫正 除以 (分母 乘 优化精度)。
        i 为 i 加 1。
    完。
完。

学习率_阶梯 接受 初始率、轮次、步长、衰减：
    次数 为 轮次 除以 步长。
    可变 率 为 初始率。
    可变 i 为 0。
    只要 i 小于 次数：
        率 为 率 乘 衰减 除以 优化精度。
        i 为 i 加 1。
    完。
    返还 率。
完。

学习率_线性 接受 初始率、当前步、总步数：
    返还 初始率 乘 (总步数 减 当前步) 除以 总步数。
完。

学习率_warmup 接受 初始率、当前步、warmup步数、总步数：
    若 当前步 小于 warmup步数：
        返还 初始率 乘 当前步 除以 warmup步数。
    完。
    返还 初始率 乘 (总步数 减 当前步) 除以 (总步数 减 warmup步数)。
完。

梯度裁剪 接受 梯度、最大范数：
    n 为 做 计数 用 梯度。
    可变 范数平方 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        范数平方 为 范数平方 加 梯度 的 第 i 项 乘 梯度 的 第 i 项。
        i 为 i 加 1。
    完。
    范数 为 做 优化_开方 用 范数平方。
    若 范数 大于 最大范数：
        缩放 为 最大范数 乘 优化精度 除以 范数。
        i 为 0。
        只要 i 小于 n：
            梯度 的 第 i 项 为 梯度 的 第 i 项 乘 缩放 除以 优化精度。
            i 为 i 加 1。
        完。
    完。
完。

xavier初始化 接受 输入维、输出维、种子：
    范围 为 做 优化_开方 用 6 乘 优化精度 除以 (输入维 加 输出维)。
    可变 权重 为 一组：（空）。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 输入维 乘 输出维：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        w 为 s 取余于 (2 乘 范围 加 1) 减 范围。
        做 追加 用 权重、w。
        i 为 i 加 1。
    完。
    返还 权重。
完。

he初始化 接受 输入维、输出维、种子：
    范围 为 做 优化_开方 用 2 乘 优化精度 除以 输入维。
    可变 权重 为 一组：（空）。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 输入维 乘 输出维：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        w 为 s 取余于 (2 乘 范围 加 1) 减 范围。
        做 追加 用 权重、w。
        i 为 i 加 1。
    完。
    返还 权重。
完。

adamw_初始化 接受 参数数：
    可变 m 为 一组：（空）。
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数数：
        做 追加 用 m、0。
        做 追加 用 v、0。
        i 为 i 加 1。
    完。
    返还 一组：m、v、0、900、999。
完。

adamw_步进 接受 状态、参数、梯度、学习率、权重衰减：
    m 为 状态 的 第 0 项。
    v 为 状态 的 第 1 项。
    状态 的 第 2 项 为 状态 的 第 2 项 加 1。
    t 为 状态 的 第 2 项。
    衰减率1 为 状态 的 第 3 项。
    衰减率2 为 状态 的 第 4 项。
    n 为 做 计数 用 参数。
    可变 b1t 为 优化精度。
    可变 b2t 为 优化精度。
    可变 ti 为 0。
    只要 ti 小于 t：
        b1t 为 b1t 乘 衰减率1 除以 优化精度。
        b2t 为 b2t 乘 衰减率2 除以 优化精度。
        ti 为 ti 加 1。
    完。
    可变 校正1 为 优化精度 减 b1t。
    可变 校正2 为 优化精度 减 b2t。
    若 校正1 小于 1：
        校正1 为 1。
    完。
    若 校正2 小于 1：
        校正2 为 1。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 权重衰减 乘 参数 的 第 i 项 除以 (优化精度 乘 优化精度)。
        m 的 第 i 项 为 衰减率1 乘 m 的 第 i 项 除以 优化精度 加 (优化精度 减 衰减率1) 乘 梯度 的 第 i 项 除以 优化精度。
        v 的 第 i 项 为 衰减率2 乘 v 的 第 i 项 除以 优化精度 加 (优化精度 减 衰减率2) 乘 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 (优化精度 乘 优化精度)。
        一阶矫正 为 m 的 第 i 项 乘 优化精度 除以 校正1。
        二阶矫正 为 v 的 第 i 项 乘 优化精度 除以 校正2。
        分母 为 做 优化_开方 用 二阶矫正 加 1。
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 一阶矫正 除以 (分母 乘 优化精度)。
        i 为 i 加 1。
    完。
完。

学习率_余弦退火 接受 初始率、最小率、当前步、总步数：
    若 当前步 大于或等于 总步数：
        返还 最小率。
    完。
    t 为 当前步 乘 优化精度 除以 总步数。
    可变 余弦值 为 0。
    若 t 小于或等于 500：
        余弦值 为 优化精度 减 2 乘 t 乘 t 除以 优化精度。
    否则：
        s 为 优化精度 减 t。
        余弦值 为 0 减 优化精度 加 2 乘 s 乘 s 除以 优化精度。
    完。
    返还 最小率 加 (初始率 减 最小率) 乘 (优化精度 加 余弦值) 除以 (2 乘 优化精度)。
完。

学习率_warmup_余弦 接受 初始率、最小率、当前步、warmup步、总步数：
    若 当前步 小于 warmup步：
        返还 初始率 乘 当前步 除以 warmup步。
    完。
    衰减步 为 当前步 减 warmup步。
    衰减总 为 总步数 减 warmup步。
    返还 做 学习率_余弦退火 用 初始率、最小率、衰减步、衰减总。
完。

梯度累积_初始化 接受 参数数：
    可变 累积 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数数：
        做 追加 用 累积、0。
        i 为 i 加 1。
    完。
    返还 一组：累积、0。
完。

梯度累积_加 接受 状态、梯度：
    n 为 做 计数 用 梯度。
    可变 i 为 0。
    只要 i 小于 n：
        状态 的 第 0 项 的 第 i 项 为 状态 的 第 0 项 的 第 i 项 加 梯度 的 第 i 项。
        i 为 i 加 1。
    完。
    状态 的 第 1 项 为 状态 的 第 1 项 加 1。
完。

梯度累积_取 接受 状态：
    步数 为 状态 的 第 1 项。
    若 步数 等于 0：
        返还 状态 的 第 0 项。
    完。
    n 为 做 计数 用 状态 的 第 0 项。
    可变 平均梯度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 平均梯度、状态 的 第 0 项 的 第 i 项 除以 步数。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n：
        状态 的 第 0 项 的 第 i 项 为 0。
        i 为 i 加 1。
    完。
    状态 的 第 1 项 为 0。
    返还 平均梯度。
完。

ema_初始化 接受 参数：
    可变 影子 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        做 追加 用 影子、参数 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 影子。
完。

ema_更新 接受 影子、参数、衰减：
    可变 i 为 0。
    只要 i 小于 做 计数 用 参数：
        影子 的 第 i 项 为 衰减 乘 影子 的 第 i 项 除以 优化精度 加 (优化精度 减 衰减) 乘 参数 的 第 i 项 除以 优化精度。
        i 为 i 加 1。
    完。
完。

lion_初始化 接受 参数数：
    可变 m 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 参数数：
        做 追加 用 m、0。
        i 为 i 加 1。
    完。
    返还 一组：m。
完。

lion_步进 接受 状态、参数、梯度、学习率、衰减率1、衰减率2、权重衰减：
    m 为 状态 的 第 0 项。
    n 为 做 计数 用 参数。
    可变 i 为 0。
    只要 i 小于 n：
        更新 为 衰减率1 乘 m 的 第 i 项 除以 优化精度 加 (优化精度 减 衰减率1) 乘 梯度 的 第 i 项 除以 优化精度。
        可变 符号 为 0。
        若 更新 大于 0：
            符号 为 优化精度。
        完。
        若 更新 小于 0：
            符号 为 0 减 优化精度。
        完。
        参数 的 第 i 项 为 参数 的 第 i 项 减 权重衰减 乘 参数 的 第 i 项 除以 优化精度。
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 符号 除以 优化精度。
        m 的 第 i 项 为 衰减率2 乘 m 的 第 i 项 除以 优化精度 加 (优化精度 减 衰减率2) 乘 梯度 的 第 i 项 除以 优化精度。
        i 为 i 加 1。
    完。
完。

lamb_步进 接受 adam状态、参数、梯度、学习率、衰减率1、衰减率2、步数：
    m 为 adam状态 的 第 0 项。
    v 为 adam状态 的 第 1 项。
    n 为 做 计数 用 参数。
    可变 bc1 为 优化精度。
    可变 bc2 为 优化精度。
    可变 s 为 0。
    只要 s 小于 步数：
        bc1 为 bc1 乘 衰减率1 除以 优化精度。
        bc2 为 bc2 乘 衰减率2 除以 优化精度。
        s 为 s 加 1。
    完。
    bc1 为 优化精度 减 bc1。
    bc2 为 优化精度 减 bc2。
    若 bc1 等于 0：
        bc1 为 1。
    完。
    若 bc2 等于 0：
        bc2 为 1。
    完。
    可变 参数范数 为 0。
    可变 更新范数 为 0。
    可变 更新方向 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        m 的 第 i 项 为 衰减率1 乘 m 的 第 i 项 除以 优化精度 加 (优化精度 减 衰减率1) 乘 梯度 的 第 i 项 除以 优化精度。
        v 的 第 i 项 为 衰减率2 乘 v 的 第 i 项 除以 优化精度 加 (优化精度 减 衰减率2) 乘 梯度 的 第 i 项 乘 梯度 的 第 i 项 除以 (优化精度 乘 优化精度)。
        一阶矫正 为 m 的 第 i 项 乘 优化精度 除以 bc1。
        二阶矫正 为 v 的 第 i 项 乘 优化精度 除以 bc2。
        方向 为 一阶矫正 乘 优化精度 除以 (做 优化_开方 用 二阶矫正 加 1)。
        做 追加 用 更新方向、方向。
        参数范数 为 参数范数 加 参数 的 第 i 项 乘 参数 的 第 i 项 除以 优化精度。
        更新范数 为 更新范数 加 方向 乘 方向 除以 优化精度。
        i 为 i 加 1。
    完。
    参数范数 为 做 优化_开方 用 参数范数。
    更新范数 为 做 优化_开方 用 更新范数。
    可变 信赖比 为 优化精度。
    若 更新范数 大于 0 并且 参数范数 大于 0：
        信赖比 为 参数范数 乘 优化精度 除以 更新范数。
    完。
    i 为 0。
    只要 i 小于 n：
        参数 的 第 i 项 为 参数 的 第 i 项 减 学习率 乘 信赖比 除以 优化精度 乘 更新方向 的 第 i 项 除以 优化精度。
        i 为 i 加 1。
    完。
完。

学习率_预热余弦 接受 步数、预热步、总步数、最大率、最小率：
    若 步数 小于 预热步：
        返还 最大率 乘 步数 除以 预热步。
    完。
    余弦步 为 步数 减 预热步。
    余弦总 为 总步数 减 预热步。
    若 余弦总 小于或等于 0：
        返还 最小率。
    完。
    返还 做 学习率_余弦退火 用 最大率、最小率、余弦步、余弦总。
完。

学习率_多项式 接受 步数、总步数、初始率、结束率、幂：
    若 步数 大于或等于 总步数：
        返还 结束率。
    完。
    比例 为 (总步数 减 步数) 乘 优化精度 除以 总步数。
    可变 结果 为 优化精度。
    可变 p 为 0。
    只要 p 小于 幂：
        结果 为 结果 乘 比例 除以 优化精度。
        p 为 p 加 1。
    完。
    若 优化精度 等于 0：
        返还 0。
    完。
    返还 结束率 加 (初始率 减 结束率) 乘 结果 除以 优化精度。
完。

优化_开方 接受 n：
    返还 做 开方 用 n。
完。

可变 _优化_步数 为 0。

可变 _优化_梯度爆炸数 为 0。

可变 _优化_梯度消失数 为 0。

优化_记录步 接受 梯度范数：
    _优化_步数 为 _优化_步数 加 1。
    若 梯度范数 大于 100000：
        _优化_梯度爆炸数 为 _优化_梯度爆炸数 加 1。
    完。
    若 梯度范数 小于 1：
        _优化_梯度消失数 为 _优化_梯度消失数 加 1。
    完。
完。

优化_梯度健康 接受 零参：
    若 _优化_步数 等于 0：
        返还 「无优化记录」。
    完。
    爆炸率 为 _优化_梯度爆炸数 乘 100 除以 _优化_步数。
    消失率 为 _优化_梯度消失数 乘 100 除以 _优化_步数。
    若 爆炸率 大于 10：
        返还 「警告: 梯度爆炸频繁,降低学习率」。
    完。
    若 消失率 大于 50：
        返还 「警告: 梯度消失严重,考虑残差连接」。
    完。
    返还 「梯度健康」。
完。

优化_学习率建议 接受 当前学习率：
    若 _优化_梯度爆炸数 大于 5：
        返还 当前学习率 除以 2。
    完。
    若 _优化_梯度消失数 大于 _优化_步数 除以 2：
        返还 当前学习率 乘 2。
    完。
    返还 当前学习率。
完。

优化_已收敛 接受 _阈值：
    若 _优化_步数 小于 10：
        返还 假。
    完。
    若 _优化_梯度消失数 大于 _优化_步数 乘 8 除以 10：
        返还 真。
    完。
    返还 假。
完。

优化_推荐算法 接受 数据规模、参数量：
    若 参数量 大于 1000000：
        返还 「Adam(大模型首选)」。
    完。
    若 数据规模 大于 100000：
        返还 「AdamW(带权重衰减)」。
    完。
    若 参数量 大于 10000：
        返还 「Adam或SGD+Momentum」。
    完。
    返还 「SGD(简单模型足够)」。
完。

优化_训练进度 接受 当前步、总步：
    若 总步 等于 0：
        返还 0。
    完。
    返还 当前步 乘 100 除以 总步。
完。

优化_预估剩余步数 接受 零参：
    若 _优化_梯度消失数 大于 _优化_步数 除以 2：
        返还 0。
    完。
    返还 _优化_步数。
完。

本章 收束。
