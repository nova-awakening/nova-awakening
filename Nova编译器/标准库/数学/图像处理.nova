《图像处理》

引用《..·基础》。

引用《..·..·运行时·原语桩》。

图像精度 为 1000。

图像_创建 接受 宽、高、_扩3：
    可变 图像 为 一组：宽、高。
    可变 i 为 0。
    只要 i 小于 宽 乘 高：
        做 追加 用 图像、0。
        i 为 i 加 1。
    完。
    返还 图像。
完。

图像_取 接受 图像、x、y：
    返还 图像 的 第 2 加 y 乘 图像 的 第 0 项 加 x 项。
完。

图像_设 接受 图像、x、y、v：
    图像 的 第 2 加 y 乘 图像 的 第 0 项 加 x 项 为 v。
完。

图像_宽 接受 图像：
    返还 图像 的 第 0 项。
完。

图像_高 接受 图像：
    返还 图像 的 第 1 项。
完。

图像_亮度 接受 图像、偏移：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 i 为 0。
    只要 i 小于 w 乘 h：
        可变 v 为 图像 的 第 2 加 i 项 加 偏移。
        若 v 小于 0：
            v 为 0。
        完。
        若 v 大于 255：
            v 为 255。
        完。
        结果 的 第 2 加 i 项 为 v。
        i 为 i 加 1。
    完。
    返还 结果。
完。

图像_对比度 接受 图像、因子：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 i 为 0。
    只要 i 小于 w 乘 h：
        可变 v 为 128 加 (图像 的 第 2 加 i 项 减 128) 乘 因子 除以 图像精度。
        若 v 小于 0：
            v 为 0。
        完。
        若 v 大于 255：
            v 为 255。
        完。
        结果 的 第 2 加 i 项 为 v。
        i 为 i 加 1。
    完。
    返还 结果。
完。

图像_反转 接受 图像：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 i 为 0。
    只要 i 小于 w 乘 h：
        结果 的 第 2 加 i 项 为 255 减 图像 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

图像_二值化 接受 图像、阈值：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 i 为 0。
    只要 i 小于 w 乘 h：
        若 图像 的 第 2 加 i 项 大于 阈值：
            结果 的 第 2 加 i 项 为 255。
        否则：
            结果 的 第 2 加 i 项 为 0。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

图像_均值模糊 接受 图像、核大小：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    半径 为 核大小 除以 2。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 y 为 0。
    只要 y 小于 h：
        可变 x 为 0。
        只要 x 小于 w：
            可变 和 为 0。
            可变 计数器 为 0。
            可变 ky 为 0 减 半径。
            只要 ky 小于或等于 半径：
                可变 kx 为 0 减 半径。
                只要 kx 小于或等于 半径：
                    nx 为 x 加 kx。
                    ny 为 y 加 ky。
                    若 nx 大于或等于 0 并且 nx 小于 w 并且 ny 大于或等于 0 并且 ny 小于 h：
                        和 为 和 加 做 图像_取 用 图像、nx、ny。
                        计数器 为 计数器 加 1。
                    完。
                    kx 为 kx 加 1。
                完。
                ky 为 ky 加 1。
            完。
            做 图像_设 用 结果、x、y、和 除以 计数器。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_sobel 接受 图像：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 y 为 1。
    只要 y 小于 h 减 1：
        可变 x 为 1。
        只要 x 小于 w 减 1：
            gx 为 0 减 做 图像_取 用 图像、x 减 1、y 减 1 加 做 图像_取 用 图像、x 加 1、y 减 1 减 2 乘 做 图像_取 用 图像、x 减 1、y 加 2 乘 做 图像_取 用 图像、x 加 1、y 减 做 图像_取 用 图像、x 减 1、y 加 1 加 做 图像_取 用 图像、x 加 1、y 加 1。
            gy 为 0 减 做 图像_取 用 图像、x 减 1、y 减 1 减 2 乘 做 图像_取 用 图像、x、y 减 1 减 做 图像_取 用 图像、x 加 1、y 减 1 加 做 图像_取 用 图像、x 减 1、y 加 1 加 2 乘 做 图像_取 用 图像、x、y 加 1 加 做 图像_取 用 图像、x 加 1、y 加 1。
            可变 幅度 为 做 图像_开方 用 gx 乘 gx 加 gy 乘 gy。
            若 幅度 大于 255：
                幅度 为 255。
            完。
            做 图像_设 用 结果、x、y、幅度。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_核最大值 接受 图像、x、y、半径：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 最大 为 0。
    可变 ky 为 0 减 半径。
    只要 ky 小于或等于 半径：
        可变 kx 为 0 减 半径。
        只要 kx 小于或等于 半径：
            nx 为 x 加 kx。
            ny 为 y 加 ky。
            若 nx 大于或等于 0 并且 nx 小于 w 并且 ny 大于或等于 0 并且 ny 小于 h 并且 做 图像_取 用 图像、nx、ny 大于 最大：
                最大 为 做 图像_取 用 图像、nx、ny。
            完。
            kx 为 kx 加 1。
        完。
        ky 为 ky 加 1。
    完。
    返还 最大。
完。

图像_膨胀 接受 图像、核大小：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    半径 为 核大小 除以 2。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 y 为 0。
    只要 y 小于 h：
        可变 x 为 0。
        只要 x 小于 w：
            做 图像_设 用 结果、x、y、做 图像_核最大值 用 图像、x、y、半径。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_核最小值 接受 图像、x、y、半径：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 最小 为 255。
    可变 ky 为 0 减 半径。
    只要 ky 小于或等于 半径：
        可变 kx 为 0 减 半径。
        只要 kx 小于或等于 半径：
            nx 为 x 加 kx。
            ny 为 y 加 ky。
            若 nx 大于或等于 0 并且 nx 小于 w 并且 ny 大于或等于 0 并且 ny 小于 h 并且 做 图像_取 用 图像、nx、ny 小于 最小：
                最小 为 做 图像_取 用 图像、nx、ny。
            完。
            kx 为 kx 加 1。
        完。
        ky 为 ky 加 1。
    完。
    返还 最小。
完。

图像_腐蚀 接受 图像、核大小：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    半径 为 核大小 除以 2。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 y 为 0。
    只要 y 小于 h：
        可变 x 为 0。
        只要 x 小于 w：
            做 图像_设 用 结果、x、y、做 图像_核最小值 用 图像、x、y、半径。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_水平翻转 接受 图像：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 y 为 0。
    只要 y 小于 h：
        可变 x 为 0。
        只要 x 小于 w：
            做 图像_设 用 结果、w 减 1 减 x、y、做 图像_取 用 图像、x、y。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_垂直翻转 接受 图像：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 y 为 0。
    只要 y 小于 h：
        可变 x 为 0。
        只要 x 小于 w：
            做 图像_设 用 结果、x、h 减 1 减 y、做 图像_取 用 图像、x、y。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_缩放 接受 图像、新宽、新高：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 新宽、新高、0。
    可变 y 为 0。
    只要 y 小于 新高：
        可变 x 为 0。
        只要 x 小于 新宽：
            原x 为 x 乘 w 除以 新宽。
            原y 为 y 乘 h 除以 新高。
            做 图像_设 用 结果、x、y、做 图像_取 用 图像、原x、原y。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_直方图 接受 图像：
    可变 直方图 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 256：
        做 追加 用 直方图、0。
        i 为 i 加 1。
    完。
    n 为 图像 的 第 0 项 乘 图像 的 第 1 项。
    i 为 0。
    只要 i 小于 n：
        v 为 图像 的 第 2 加 i 项。
        若 v 大于或等于 0 并且 v 小于 256：
            直方图 的 第 v 项 为 直方图 的 第 v 项 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 直方图。
完。

图像_均衡化 接受 图像：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    n 为 w 乘 h。
    直方图 为 做 图像_直方图 用 图像。
    可变 cdf 为 一组：（空）。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 256：
        和 为 和 加 直方图 的 第 i 项。
        做 追加 用 cdf、和。
        i 为 i 加 1。
    完。
    可变 cdf_min 为 n。
    i 为 0。
    只要 i 小于 256：
        若 cdf 的 第 i 项 大于 0 并且 cdf 的 第 i 项 小于 cdf_min：
            cdf_min 为 cdf 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    可变 结果 为 做 图像_创建 用 w、h、0。
    i 为 0。
    只要 i 小于 n：
        v 为 图像 的 第 2 加 i 项。
        结果 的 第 2 加 i 项 为 (cdf 的 第 v 项 减 cdf_min) 乘 255 除以 (n 减 cdf_min)。
        i 为 i 加 1。
    完。
    返还 结果。
完。

图像_双线性缩放 接受 图像、新宽、新高：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 新宽、新高、0。
    可变 y 为 0。
    只要 y 小于 新高：
        可变 x 为 0。
        只要 x 小于 新宽：
            sx 为 x 乘 (w 减 1) 乘 1000 除以 (新宽 减 1)。
            sy 为 y 乘 (h 减 1) 乘 1000 除以 (新高 减 1)。
            x0 为 sx 除以 1000。
            y0 为 sy 除以 1000。
            可变 x1 为 x0 加 1。
            可变 y1 为 y0 加 1。
            若 x1 大于或等于 w：
                x1 为 w 减 1。
            完。
            若 y1 大于或等于 h：
                y1 为 h 减 1。
            完。
            fx 为 sx 取余于 1000。
            fy 为 sy 取余于 1000。
            v00 为 做 图像_取 用 图像、x0、y0。
            v10 为 做 图像_取 用 图像、x1、y0。
            v01 为 做 图像_取 用 图像、x0、y1。
            v11 为 做 图像_取 用 图像、x1、y1。
            v 为 (v00 乘 (1000 减 fx) 乘 (1000 减 fy) 加 v10 乘 fx 乘 (1000 减 fy) 加 v01 乘 (1000 减 fx) 乘 fy 加 v11 乘 fx 乘 fy) 除以 (1000 乘 1000)。
            做 图像_设 用 结果、x、y、v。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_旋转90 接受 图像：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 h、w、0。
    可变 y 为 0。
    只要 y 小于 h：
        可变 x 为 0。
        只要 x 小于 w：
            做 图像_设 用 结果、h 减 1 减 y、x、做 图像_取 用 图像、x、y。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_开运算 接受 图像、核大小：
    返还 做 图像_膨胀 用 做 图像_腐蚀 用 图像、核大小、核大小。
完。

图像_闭运算 接受 图像、核大小：
    返还 做 图像_腐蚀 用 做 图像_膨胀 用 图像、核大小、核大小。
完。

图像_otsu阈值 接受 图像：
    直方图 为 做 图像_直方图 用 图像。
    可变 最佳阈值 为 0。
    n 为 图像 的 第 0 项 乘 图像 的 第 1 项。
    可变 最大方差 为 0。
    可变 t 为 0。
    只要 t 小于 256：
        可变 w0 为 0。
        可变 s0 为 0。
        可变 w1 为 0。
        可变 s1 为 0。
        可变 i 为 0。
        只要 i 小于 256：
            若 i 小于或等于 t：
                w0 为 w0 加 直方图 的 第 i 项。
                s0 为 s0 加 i 乘 直方图 的 第 i 项。
            否则：
                w1 为 w1 加 直方图 的 第 i 项。
                s1 为 s1 加 i 乘 直方图 的 第 i 项。
            完。
            i 为 i 加 1。
        完。
        若 w0 大于 0 并且 w1 大于 0：
            m0 为 s0 除以 w0。
            m1 为 s1 除以 w1。
            类间方差 为 w0 乘 w1 乘 (m0 减 m1) 乘 (m0 减 m1) 除以 (n 乘 n)。
            若 类间方差 大于 最大方差：
                最大方差 为 类间方差。
                最佳阈值 为 t。
            完。
        完。
        t 为 t 加 1。
    完。
    返还 最佳阈值。
完。

图像_裁剪 接受 图像、x起、y起、裁宽、裁高：
    _w 为 图像 的 第 0 项。
    可变 结果 为 做 图像_创建 用 裁宽、裁高、0。
    可变 y 为 0。
    只要 y 小于 裁高：
        可变 x 为 0。
        只要 x 小于 裁宽：
            做 图像_设 用 结果、x、y、做 图像_取 用 图像、x起 加 x、y起 加 y。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_填充 接受 图像、填充量、填充值：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    新宽 为 w 加 2 乘 填充量。
    新高 为 h 加 2 乘 填充量。
    可变 结果 为 一组：新宽、新高。
    可变 i 为 0。
    只要 i 小于 新宽 乘 新高：
        做 追加 用 结果、填充值。
        i 为 i 加 1。
    完。
    可变 y 为 0。
    只要 y 小于 h：
        可变 x 为 0。
        只要 x 小于 w：
            做 图像_设 用 结果、x 加 填充量、y 加 填充量、做 图像_取 用 图像、x、y。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_高斯模糊 接受 图像：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 y 为 1。
    只要 y 小于 h 减 1：
        可变 x 为 1。
        只要 x 小于 w 减 1：
            v 为 做 图像_取 用 图像、x 减 1、y 减 1 加 2 乘 做 图像_取 用 图像、x、y 减 1 加 做 图像_取 用 图像、x 加 1、y 减 1 加 2 乘 做 图像_取 用 图像、x 减 1、y 加 4 乘 做 图像_取 用 图像、x、y 加 2 乘 做 图像_取 用 图像、x 加 1、y 加 做 图像_取 用 图像、x 减 1、y 加 1 加 2 乘 做 图像_取 用 图像、x、y 加 1 加 做 图像_取 用 图像、x 加 1、y 加 1。
            做 图像_设 用 结果、x、y、v 除以 16。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_高斯模糊5 接受 图像：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    做 图像_创建 用 w、h、0。
    中间 为 做 图像_高斯模糊 用 图像。
    返还 做 图像_高斯模糊 用 中间。
完。

图像_中值滤波 接受 图像：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 y 为 1。
    只要 y 小于 h 减 1：
        可变 x 为 1。
        只要 x 小于 w 减 1：
            可变 窗 为 一组：（空）。
            可变 导数Y 为 0 减 1。
            只要 导数Y 小于或等于 1：
                可变 导数 为 0 减 1。
                只要 导数 小于或等于 1：
                    做 追加 用 窗、做 图像_取 用 图像、x 加 导数、y 加 导数Y。
                    导数 为 导数 加 1。
                完。
                导数Y 为 导数Y 加 1。
            完。
            可变 si 为 0。
            只要 si 小于 9：
                可变 sj 为 si 加 1。
                只要 sj 小于 9：
                    若 窗 的 第 sj 项 小于 窗 的 第 si 项：
                        t 为 窗 的 第 si 项。
                        窗 的 第 si 项 为 窗 的 第 sj 项。
                        窗 的 第 sj 项 为 t。
                    完。
                    sj 为 sj 加 1。
                完。
                si 为 si 加 1。
            完。
            做 图像_设 用 结果、x、y、窗 的 第 4 项。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_canny 接受 图像、低阈值、高阈值：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    平滑 为 做 图像_高斯模糊 用 图像。
    可变 梯度幅 为 做 图像_创建 用 w、h、0。
    可变 梯度方向 为 做 图像_创建 用 w、h、0。
    可变 y 为 1。
    只要 y 小于 h 减 1：
        可变 x 为 1。
        只要 x 小于 w 减 1：
            gx 为 0 减 做 图像_取 用 平滑、x 减 1、y 减 1 加 做 图像_取 用 平滑、x 加 1、y 减 1 减 2 乘 做 图像_取 用 平滑、x 减 1、y 加 2 乘 做 图像_取 用 平滑、x 加 1、y 减 做 图像_取 用 平滑、x 减 1、y 加 1 加 做 图像_取 用 平滑、x 加 1、y 加 1。
            gy 为 0 减 做 图像_取 用 平滑、x 减 1、y 减 1 减 2 乘 做 图像_取 用 平滑、x、y 减 1 减 做 图像_取 用 平滑、x 加 1、y 减 1 加 做 图像_取 用 平滑、x 减 1、y 加 1 加 2 乘 做 图像_取 用 平滑、x、y 加 1 加 做 图像_取 用 平滑、x 加 1、y 加 1。
            幅度 为 做 图像_开方 用 gx 乘 gx 加 gy 乘 gy。
            做 图像_设 用 梯度幅、x、y、幅度。
            可变 abs_gx 为 gx。
            可变 abs_gy 为 gy。
            若 abs_gx 小于 0：
                abs_gx 为 0 减 abs_gx。
            完。
            若 abs_gy 小于 0：
                abs_gy 为 0 减 abs_gy。
            完。
            可变 方向 为 0。
            若 abs_gy 大于 abs_gx 乘 2：
                方向 为 2。
            否则若 abs_gx 大于 abs_gy 乘 2：
                方向 为 0。
            否则：
                可变 同号 为 假。
                若 gx 大于 0 并且 gy 大于 0：
                    同号 为 真。
                完。
                若 gx 小于 0 并且 gy 小于 0：
                    同号 为 真。
                完。
                若 同号：
                    方向 为 1。
                否则：
                    方向 为 3。
                完。
            完。
            做 图像_设 用 梯度方向、x、y、方向。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    可变 抑制 为 做 图像_创建 用 w、h、0。
    y 为 1。
    只要 y 小于 h 减 1：
        可变 x2 为 1。
        只要 x2 小于 w 减 1：
            m 为 做 图像_取 用 梯度幅、x2、y。
            d 为 做 图像_取 用 梯度方向、x2、y。
            可变 n1 为 0。
            可变 n2 为 0。
            若 d 等于 0：
                n1 为 做 图像_取 用 梯度幅、x2 减 1、y。
                n2 为 做 图像_取 用 梯度幅、x2 加 1、y。
            否则若 d 等于 1：
                n1 为 做 图像_取 用 梯度幅、x2 减 1、y 减 1。
                n2 为 做 图像_取 用 梯度幅、x2 加 1、y 加 1。
            否则若 d 等于 2：
                n1 为 做 图像_取 用 梯度幅、x2、y 减 1。
                n2 为 做 图像_取 用 梯度幅、x2、y 加 1。
            否则：
                n1 为 做 图像_取 用 梯度幅、x2 加 1、y 减 1。
                n2 为 做 图像_取 用 梯度幅、x2 减 1、y 加 1。
            完。
            若 m 大于或等于 n1 并且 m 大于或等于 n2：
                做 图像_设 用 抑制、x2、y、m。
            完。
            x2 为 x2 加 1。
        完。
        y 为 y 加 1。
    完。
    可变 边缘 为 做 图像_创建 用 w、h、0。
    y 为 1。
    只要 y 小于 h 减 1：
        可变 x3 为 1。
        只要 x3 小于 w 减 1：
            v 为 做 图像_取 用 抑制、x3、y。
            若 v 大于或等于 高阈值：
                做 图像_设 用 边缘、x3、y、255。
            否则若 v 大于或等于 低阈值：
                做 图像_设 用 边缘、x3、y、128。
            完。
            x3 为 x3 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 边缘。
完。

图像_锐化 接受 图像：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 y 为 1。
    只要 y 小于 h 减 1：
        可变 x 为 1。
        只要 x 小于 w 减 1：
            可变 v 为 5 乘 做 图像_取 用 图像、x、y 减 做 图像_取 用 图像、x 减 1、y 减 做 图像_取 用 图像、x 加 1、y 减 做 图像_取 用 图像、x、y 减 1 减 做 图像_取 用 图像、x、y 加 1。
            若 v 小于 0：
                v 为 0。
            完。
            若 v 大于 255：
                v 为 255。
            完。
            做 图像_设 用 结果、x、y、v。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_旋转180 接受 图像：
    w 为 图像 的 第 0 项。
    h 为 图像 的 第 1 项。
    可变 结果 为 做 图像_创建 用 w、h、0。
    可变 y 为 0。
    只要 y 小于 h：
        可变 x 为 0。
        只要 x 小于 w：
            做 图像_设 用 结果、w 减 1 减 x、h 减 1 减 y、做 图像_取 用 图像、x、y。
            x 为 x 加 1。
        完。
        y 为 y 加 1。
    完。
    返还 结果。
完。

图像_混合 接受 img1、img2、混合系数：
    w 为 img1 的 第 0 项。
    h 为 img1 的 第 1 项。
    可变 结果 为 做 图像_创建 用 w、h、0。
    n 为 w 乘 h。
    可变 i 为 0。
    只要 i 小于 n：
        v 为 混合系数 乘 img1 的 第 2 加 i 项 除以 1000 加 (1000 减 混合系数) 乘 img2 的 第 2 加 i 项 除以 1000。
        结果 的 第 2 加 i 项 为 v。
        i 为 i 加 1。
    完。
    返还 结果。
完。

图像_开方 接受 n：
    返还 做 开方 用 n。
完。

本章 收束。
