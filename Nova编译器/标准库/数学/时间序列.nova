《时间序列》

引用《..·基础》。

引用《..·算法·数学扩展》。

引用《..·..·运行时·原语桩》。

引用《.·基础·数学库》。

时序精度 为 1000。

简单移动平均 接受 数据、窗口：
    n 为 做 计数 用 数据。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 和 为 0。
        可变 计数器 为 0。
        起 为 i 减 窗口 加 1。
        可变 j 为 起。
        只要 j 小于或等于 i：
            若 j 大于或等于 0：
                和 为 和 加 数据 的 第 j 项。
                计数器 为 计数器 加 1。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和 除以 计数器。
        i 为 i 加 1。
    完。
    返还 结果。
完。

指数加权平均 接受 数据、学习率：
    n 为 做 计数 用 数据。
    可变 结果 为 一组：数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        前 为 结果 的 第 i 减 1 项。
        做 追加 用 结果、学习率 乘 数据 的 第 i 项 除以 时序精度 加 (时序精度 减 学习率) 乘 前 除以 时序精度。
        i 为 i 加 1。
    完。
    返还 结果。
完。

线性趋势 接受 数据：
    n 为 做 计数 用 数据。
    可变 sx 为 0。
    可变 sy 为 0。
    可变 sxy 为 0。
    可变 sxx 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        sx 为 sx 加 i。
        sy 为 sy 加 数据 的 第 i 项。
        sxy 为 sxy 加 i 乘 数据 的 第 i 项。
        sxx 为 sxx 加 i 乘 i。
        i 为 i 加 1。
    完。
    分母 为 n 乘 sxx 减 sx 乘 sx。
    若 分母 等于 0：
        返还 一组：0、sy 除以 n。
    完。
    斜率 为 (n 乘 sxy 减 sx 乘 sy) 乘 时序精度 除以 分母。
    截距 为 (sy 减 斜率 乘 sx 除以 时序精度) 除以 n。
    返还 一组：斜率、截距。
完。

去趋势 接受 数据：
    趋势 为 做 线性趋势 用 数据。
    n 为 做 计数 用 数据。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        趋势值 为 趋势 的 第 1 项 加 趋势 的 第 0 项 乘 i 除以 时序精度。
        做 追加 用 结果、数据 的 第 i 项 减 趋势值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

差分 接受 数据、阶：
    可变 当前 为 数据。
    可变 d 为 0。
    只要 d 小于 阶：
        n 为 做 计数 用 当前。
        可变 新数据 为 一组：（空）。
        可变 i 为 1。
        只要 i 小于 n：
            做 追加 用 新数据、当前 的 第 i 项 减 当前 的 第 i 减 1 项。
            i 为 i 加 1。
        完。
        当前 为 新数据。
        d 为 d 加 1。
    完。
    返还 当前。
完。

时序_自相关 接受 数据、最大滞后：
    n 为 做 计数 用 数据。
    可变 acf 为 一组：（空）。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 n：
        差 为 数据 的 第 i 项 减 均值。
        方差 为 方差 加 差 乘 差。
        i 为 i 加 1。
    完。
    若 方差 等于 0：
        方差 为 1。
    完。
    可变 滞后量 为 0。
    只要 滞后量 小于或等于 最大滞后：
        可变 和 为 0。
        i 为 0。
        只要 i 小于 n 减 滞后量：
            和 为 和 加 (数据 的 第 i 项 减 均值) 乘 (数据 的 第 i 加 滞后量 项 减 均值)。
            i 为 i 加 1。
        完。
        做 追加 用 acf、和 乘 时序精度 除以 方差。
        滞后量 为 滞后量 加 1。
    完。
    返还 acf。
完。

zscore异常 接受 数据、阈值：
    n 为 做 计数 用 数据。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 n：
        差 为 数据 的 第 i 项 减 均值。
        方差 为 方差 加 差 乘 差。
        i 为 i 加 1。
    完。
    可变 标准差 为 做 时序_开方 用 方差 除以 n。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    可变 异常 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        可变 z 为 (数据 的 第 i 项 减 均值) 乘 时序精度 除以 标准差。
        若 z 小于 0：
            z 为 0 减 z。
        完。
        若 z 大于 阈值：
            做 追加 用 异常、一组：i、数据 的 第 i 项、z。
        完。
        i 为 i 加 1。
    完。
    返还 异常。
完。

iqr异常 接受 数据：
    排序数据 为 做 时序_排序 用 数据。
    n 为 做 计数 用 排序数据。
    q1 为 排序数据 的 第 n 除以 4 项。
    q3 为 排序数据 的 第 3 乘 n 除以 4 项。
    iqr 为 q3 减 q1。
    下界 为 q1 减 3 乘 iqr 除以 2。
    上界 为 q3 加 3 乘 iqr 除以 2。
    可变 异常 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 项 小于 下界 或者 数据 的 第 i 项 大于 上界：
            做 追加 用 异常、一组：i、数据 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 异常。
完。

holt预测 接受 数据、学习率、动量率、步数：
    n 为 做 计数 用 数据。
    可变 水平 为 数据 的 第 0 项。
    可变 趋势 为 数据 的 第 1 项 减 数据 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 n：
        新水平 为 学习率 乘 数据 的 第 i 项 除以 时序精度 加 (时序精度 减 学习率) 乘 (水平 加 趋势) 除以 时序精度。
        新趋势 为 动量率 乘 (新水平 减 水平) 除以 时序精度 加 (时序精度 减 动量率) 乘 趋势 除以 时序精度。
        水平 为 新水平。
        趋势 为 新趋势。
        i 为 i 加 1。
    完。
    可变 预测 为 一组：（空）。
    i 为 1。
    只要 i 小于或等于 步数：
        做 追加 用 预测、水平 加 趋势 乘 i。
        i 为 i 加 1。
    完。
    返还 预测。
完。

若 做 计数 用 数据 等于 0：
    返还 空。
完。

朴素预测 接受 数据、步数：
    最后 为 数据 的 第 做 计数 用 数据 减 1 项。
    可变 预测 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 步数：
        做 追加 用 预测、最后。
        i 为 i 加 1。
    完。
    返还 预测。
完。

季节分解 接受 数据、周期：
    n 为 做 计数 用 数据。
    趋势 为 做 简单移动平均 用 数据、周期。
    可变 去趋势 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 去趋势、数据 的 第 i 项 减 趋势 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 季节 为 一组：（空）。
    可变 p 为 0。
    只要 p 小于 周期：
        可变 和 为 0。
        可变 计数值 为 0。
        i 为 p。
        只要 i 小于 n：
            和 为 和 加 去趋势 的 第 i 项。
            计数值 为 计数值 加 1。
            i 为 i 加 周期。
        完。
        若 计数值 大于 0：
            做 追加 用 季节、和 除以 计数值。
        否则：
            做 追加 用 季节、0。
        完。
        p 为 p 加 1。
    完。
    可变 季节完整 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 季节完整、季节 的 第 i 取余于 周期 项。
        i 为 i 加 1。
    完。
    可变 残差 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 残差、数据 的 第 i 项 减 趋势 的 第 i 项 减 季节完整 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 一组：趋势、季节完整、残差。
完。

holt_winters预测 接受 数据、学习率、动量率、衰减率、周期、步数：
    n 为 做 计数 用 数据。
    可变 水平 为 数据 的 第 0 项。
    可变 趋势 为 0。
    若 n 大于 1：
        趋势 为 数据 的 第 1 项 减 数据 的 第 0 项。
    完。
    可变 季节 为 一组：（空）。
    可变 si 为 0。
    只要 si 小于 周期：
        若 si 小于 n：
            做 追加 用 季节、数据 的 第 si 项 减 水平。
        否则：
            做 追加 用 季节、0。
        完。
        si 为 si 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 n：
        旧水平 为 水平。
        季节idx 为 i 取余于 周期。
        水平 为 学习率 乘 (数据 的 第 i 项 减 季节 的 第 季节idx 项) 除以 时序精度 加 (时序精度 减 学习率) 乘 (旧水平 加 趋势) 除以 时序精度。
        趋势 为 动量率 乘 (水平 减 旧水平) 除以 时序精度 加 (时序精度 减 动量率) 乘 趋势 除以 时序精度。
        季节 的 第 季节idx 项 为 衰减率 乘 (数据 的 第 i 项 减 水平) 除以 时序精度 加 (时序精度 减 衰减率) 乘 季节 的 第 季节idx 项 除以 时序精度。
        i 为 i 加 1。
    完。
    可变 预测 为 一组：（空）。
    i 为 1。
    只要 i 小于或等于 步数：
        做 追加 用 预测、水平 加 趋势 乘 i 加 季节 的 第 (n 加 i 减 1) 取余于 周期 项。
        i 为 i 加 1。
    完。
    返还 预测。
完。

变化点检测 接受 数据、阈值：
    n 为 做 计数 用 数据。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 数据 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 cusum_pos 为 0。
    可变 cusum_neg 为 0。
    可变 变化点 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        偏差 为 数据 的 第 i 项 减 均值。
        cusum_pos 为 cusum_pos 加 偏差。
        若 cusum_pos 小于 0：
            cusum_pos 为 0。
        完。
        cusum_neg 为 cusum_neg 减 偏差。
        若 cusum_neg 小于 0：
            cusum_neg 为 0。
        完。
        若 cusum_pos 大于 阈值 或者 cusum_neg 大于 阈值：
            做 追加 用 变化点、i。
            cusum_pos 为 0。
            cusum_neg 为 0。
        完。
        i 为 i 加 1。
    完。
    返还 变化点。
完。

预测_mae 接受 实际、预测：
    n 为 做 计数 用 实际。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 d 为 实际 的 第 i 项 减 预测 的 第 i 项。
        若 d 小于 0：
            d 为 0 减 d。
        完。
        和 为 和 加 d。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 和 除以 n。
完。

预测_rmse 接受 实际、预测：
    n 为 做 计数 用 实际。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        d 为 实际 的 第 i 项 减 预测 的 第 i 项。
        和 为 和 加 d 乘 d。
        i 为 i 加 1。
    完。
    返还 做 时序_开方 用 和 除以 n。
完。

预测_mape 接受 实际、预测：
    n 为 做 计数 用 实际。
    可变 和 为 0。
    可变 计数值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        若 实际 的 第 i 项 不等于 0：
            可变 d 为 实际 的 第 i 项 减 预测 的 第 i 项。
            若 d 小于 0：
                d 为 0 减 d。
            完。
            和 为 和 加 d 乘 时序精度 除以 实际 的 第 i 项。
            计数值 为 计数值 加 1。
        完。
        i 为 i 加 1。
    完。
    若 计数值 等于 0：
        返还 0。
    完。
    返还 和 除以 计数值。
完。

滑动窗口异常 接受 数据、窗口、倍数：
    n 为 做 计数 用 数据。
    可变 异常 为 一组：（空）。
    可变 i 为 窗口。
    只要 i 小于 n：
        可变 和 为 0。
        可变 j 为 i 减 窗口。
        只要 j 小于 i：
            和 为 和 加 数据 的 第 j 项。
            j 为 j 加 1。
        完。
        均值 为 和 除以 窗口。
        可变 方差 为 0。
        j 为 i 减 窗口。
        只要 j 小于 i：
            d 为 数据 的 第 j 项 减 均值。
            方差 为 方差 加 d 乘 d。
            j 为 j 加 1。
        完。
        可变 标准差 为 做 时序_开方 用 方差 除以 窗口。
        若 标准差 等于 0：
            标准差 为 1。
        完。
        可变 z 为 (数据 的 第 i 项 减 均值) 乘 时序精度 除以 标准差。
        若 z 小于 0：
            z 为 0 减 z。
        完。
        若 z 大于 倍数：
            做 追加 用 异常、一组：i、数据 的 第 i 项、z。
        完。
        i 为 i 加 1。
    完。
    返还 异常。
完。

ar_预测 接受 数据、阶数、步数：
    n 为 做 计数 用 数据。
    可变 总权 为 0。
    若 n 小于 阶数：
        返还 一组：（空）。
    完。
    可变 系数 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 阶数：
        w 为 阶数 减 i。
        做 追加 用 系数、w。
        总权 为 总权 加 w。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 阶数：
        系数 的 第 i 项 为 系数 的 第 i 项 乘 时序精度 除以 总权。
        i 为 i 加 1。
    完。
    可变 历史 为 一组：（空）。
    i 为 n 减 阶数。
    只要 i 小于 n：
        做 追加 用 历史、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 预测 为 一组：（空）。
    可变 s 为 0。
    只要 s 小于 步数：
        可变 值 为 0。
        i 为 0。
        只要 i 小于 阶数：
            值 为 值 加 历史 的 第 做 计数 用 历史 减 阶数 加 i 项 乘 系数 的 第 i 项 除以 时序精度。
            i 为 i 加 1。
        完。
        做 追加 用 预测、值。
        做 追加 用 历史、值。
        s 为 s 加 1。
    完。
    返还 预测。
完。

布林带 接受 数据、窗口、倍数：
    n 为 做 计数 用 数据。
    可变 上带 为 一组：（空）。
    可变 中带 为 一组：（空）。
    可变 下带 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 和 为 0。
        可变 计数值 为 0。
        起 为 i 减 窗口 加 1。
        可变 j 为 起。
        只要 j 小于或等于 i：
            若 j 大于或等于 0：
                和 为 和 加 数据 的 第 j 项。
                计数值 为 计数值 加 1。
            完。
            j 为 j 加 1。
        完。
        均值 为 和 除以 计数值。
        可变 方差 为 0。
        j 为 起。
        只要 j 小于或等于 i：
            若 j 大于或等于 0：
                d 为 数据 的 第 j 项 减 均值。
                方差 为 方差 加 d 乘 d。
            完。
            j 为 j 加 1。
        完。
        标准差 为 做 时序_开方 用 方差 除以 计数值。
        做 追加 用 上带、均值 加 倍数 乘 标准差 除以 时序精度。
        做 追加 用 中带、均值。
        做 追加 用 下带、均值 减 倍数 乘 标准差 除以 时序精度。
        i 为 i 加 1。
    完。
    返还 一组：上带、中带、下带。
完。

相对强弱 接受 数据、周期：
    n 为 做 计数 用 数据。
    可变 结果 为 一组：500。
    可变 i 为 1。
    只要 i 小于 n：
        可变 涨和 为 0。
        可变 跌和 为 0。
        可变 计数值 为 0。
        可变 起 为 i 减 周期 加 1。
        若 起 小于 1：
            起 为 1。
        完。
        可变 j 为 起。
        只要 j 小于或等于 i：
            变化 为 数据 的 第 j 项 减 数据 的 第 j 减 1 项。
            若 变化 大于 0：
                涨和 为 涨和 加 变化。
            否则：
                跌和 为 跌和 减 变化。
            完。
            计数值 为 计数值 加 1。
            j 为 j 加 1。
        完。
        若 涨和 加 跌和 等于 0：
            做 追加 用 结果、500。
        否则：
            做 追加 用 结果、涨和 乘 时序精度 除以 (涨和 加 跌和)。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

时序_开方 接受 n：
    返还 做 开方 用 n。
完。

时序_排序 接受 列表：
    n 为 做 计数 用 列表。
    可变 r 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 r、列表 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 1。
    只要 i 小于 n：
        键 为 r 的 第 i 项。
        可变 j 为 i 减 1。
        只要 j 大于或等于 0 并且 r 的 第 j 项 大于 键：
            r 的 第 j 加 1 项 为 r 的 第 j 项。
            j 为 j 减 1。
        完。
        r 的 第 j 加 1 项 为 键。
        i 为 i 加 1。
    完。
    返还 r。
完。

变化率 接受 数据：
    可变 结果 为 一组：（空）。
    可变 i 为 1。
    只要 i 小于 做 计数 用 数据：
        若 数据 的 第 i 减 1 项 等于 0：
            做 追加 用 结果、0。
        否则：
            做 追加 用 结果、(数据 的 第 i 项 减 数据 的 第 i 减 1 项) 乘 1000 除以 数据 的 第 i 减 1 项。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

时序_峰值检测 接受 数据、邻域：
    可变 峰值 为 一组：（空）。
    可变 i 为 邻域。
    只要 i 小于 做 计数 用 数据 减 邻域：
        可变 是峰值 为 真。
        可变 j 为 i 减 邻域。
        只要 j 小于或等于 i 加 邻域：
            若 j 不等于 i 并且 数据 的 第 j 项 大于或等于 数据 的 第 i 项：
                是峰值 为 假。
            完。
            j 为 j 加 1。
        完。
        若 是峰值：
            做 追加 用 峰值、i。
        完。
        i 为 i 加 1。
    完。
    返还 峰值。
完。

序列_长度 接受 数据：
    返还 做 计数 用 数据。
完。

异常检测 接受 数据、阈值：
    n 为 做 计数 用 数据。
    可变 方差和 为 0。
    可变 统计索引 为 0。
    可变 总和 为 0。
    只要 统计索引 小于 n：
        总和 为 总和 加 数据 的 第 统计索引 项。
        统计索引 为 统计索引 加 1。
    完。
    均值 为 总和 除以 n。
    统计索引 为 0。
    只要 统计索引 小于 n：
        可变 偏差 为 数据 的 第 统计索引 项 减 均值。
        方差和 为 方差和 加 偏差 乘 偏差。
        统计索引 为 统计索引 加 1。
    完。
    方差 为 方差和 除以 n。
    可变 标准差 为 做 时序_开方 用 方差。
    若 标准差 小于 1：
        标准差 为 1。
    完。
    可变 异常 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        可变 偏差幅度 为 做 绝对值 用 数据 的 第 i 项 减 均值 乘 1000 除以 标准差。
        若 标准差 大于 0：
            若 偏差幅度 大于 阈值：
                做 追加 用 异常、i。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 异常。
完。

滚动波动率 接受 数据、窗口：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 窗口 小于或等于 做 计数 用 数据：
        可变 总和 为 0。
        可变 j 为 i。
        只要 j 小于 i 加 窗口：
            总和 为 总和 加 数据 的 第 j 项。
            j 为 j 加 1。
        完。
        均值 为 总和 除以 窗口。
        可变 方差 为 0。
        j 为 i。
        只要 j 小于 i 加 窗口：
            方差 为 方差 加 (数据 的 第 j 项 减 均值) 乘 (数据 的 第 j 项 减 均值)。
            j 为 j 加 1。
        完。
        做 追加 用 结果、做 时序_开方 用 方差 除以 窗口。
        i 为 i 加 1。
    完。
    返还 结果。
完。

累积和 接受 数据：
    可变 结果 为 一组：（空）。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        总和 为 总和 加 数据 的 第 i 项。
        做 追加 用 结果、总和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

线性预测 接受 数据、步数：
    n 为 做 计数 用 数据。
    若 n 小于 2：
        返还 一组：（空）。
    完。
    趋势参数 为 做 线性趋势 用 数据。
    最后 为 数据 的 第 n 减 1 项。
    可变 结果 为 一组：（空）。
    可变 i 为 1。
    只要 i 小于或等于 步数：
        做 追加 用 结果、最后 加 趋势参数 的 第 0 项 乘 i 除以 1000。
        i 为 i 加 1。
    完。
    返还 结果。
完。

本章 收束。
