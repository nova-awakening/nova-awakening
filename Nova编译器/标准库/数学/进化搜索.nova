《进化搜索》

evo_追加 接受 lst、val：
    做 插入 用 lst、做 计数 用 lst、val。
完。

进化_绝对值 接受 x：
    若 x 小于 0：
        返还 0 减 x。
    完。
    返还 x。
完。

可变 进化_随机状态 为 54321。

可变 进化_随机增量 为 1442695040888963407。

进化_种子 接受 s：
    进化_随机状态 为 0。
    进化_随机状态 为 进化_随机状态 乘 6364136223846793005 加 进化_随机增量 位与 0x7FFFFFFFFFFFFFFF。
    进化_随机状态 为 (进化_随机状态 加 s) 乘 6364136223846793005 加 进化_随机增量 位与 0x7FFFFFFFFFFFFFFF。
完。

进化_随机 接受 零参：
    旧 为 进化_随机状态。
    进化_随机状态 为 旧 乘 6364136223846793005 加 进化_随机增量 位与 0x7FFFFFFFFFFFFFFF。
    xsh 为 (旧 右移 18 位异或 旧) 右移 27。
    旋转量 为 旧 右移 59。
    r 为 (xsh 右移 旋转量 位或 xsh 左移 32 减 旋转量) 位与 0x7FFFFFFF。
    返还 r 取余于 1000000000。
完。

进化_随机范围 接受 lo、hi：
    r 为 做 进化_随机 取余于 (hi 减 lo) 加 lo。
    返还 r。
完。

进化_创建个体 接受 n_genes：
    可变 ind 为 一组：0。
    可变 i 为 0。
    只要 i 小于 n_genes：
        做 evo_追加 用 ind、做 进化_随机范围 用 0 减 1000、1000。
        i 为 i 加 1。
    完。
    返还 ind。
完。

进化_取适应度 接受 ind：
    返还 ind 的 第 0 项。
完。

进化_设适应度 接受 ind、f：
    ind 的 第 0 项 为 f。
完。

进化_取基因 接受 ind、i：
    返还 ind 的 第 1 加 i 项。
完。

进化_设基因 接受 ind、i、v：
    ind 的 第 1 加 i 项 为 v。
完。

进化_基因数 接受 ind：
    返还 做 计数 用 ind 减 1。
完。

进化_变异 接受 ind、strength：
    n 为 做 进化_基因数 用 ind。
    可变 子节点 为 一组：0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 v 为 做 进化_取基因 用 ind、i。
        若 做 进化_随机 取余于 100 小于 30：
            v 为 v 加 做 进化_随机范围 用 0 减 strength、strength。
        完。
        做 evo_追加 用 子节点、v。
        i 为 i 加 1。
    完。
    返还 子节点。
完。

进化_交叉 接受 parent1、parent2：
    n 为 做 进化_基因数 用 parent1。
    可变 子节点 为 一组：0。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 进化_随机 取余于 2 等于 0：
            做 evo_追加 用 子节点、做 进化_取基因 用 parent1、i。
        否则：
            做 evo_追加 用 子节点、做 进化_取基因 用 parent2、i。
        完。
        i 为 i 加 1。
    完。
    返还 子节点。
完。

进化_创建种群 接受 种群大小、n_genes：
    可变 pop 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 种群大小：
        做 evo_追加 用 pop、做 进化_创建个体 用 n_genes。
        i 为 i 加 1。
    完。
    返还 pop。
完。

进化_最优 接受 pop：
    可变 最优索引 为 0。
    可变 best_fit 为 做 进化_取适应度 用 pop 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 pop：
        f 为 做 进化_取适应度 用 pop 的 第 i 项。
        若 f 大于 best_fit：
            best_fit 为 f。
            最优索引 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 pop 的 第 最优索引 项。
完。

进化_演化 接受 pop、eval_fn、generations、mut_strength：
    种群大小 为 做 计数 用 pop。
    可变 ei 为 0。
    可变 代数 为 0。
    只要 代数 小于 generations：
        只要 ei 小于 种群大小：
            fitness 为 做 eval_fn 用 pop 的 第 ei 项。
            做 进化_设适应度 用 pop 的 第 ei 项、fitness。
            ei 为 ei 加 1。
        完。
        可变 best1_idx 为 0。
        可变 best1_fit 为 0 减 0x7FFFFFFF。
        可变 best2_idx 为 1。
        可变 best2_fit 为 0 减 0x7FFFFFFF。
        可变 si 为 0。
        只要 si 小于 种群大小：
            f 为 做 进化_取适应度 用 pop 的 第 si 项。
            若 f 大于 best1_fit：
                best2_idx 为 best1_idx。
                best2_fit 为 best1_fit。
                best1_idx 为 si。
                best1_fit 为 f。
            否则：
                若 f 大于 best2_fit：
                    best2_idx 为 si。
                    best2_fit 为 f。
                完。
            完。
            si 为 si 加 1。
        完。
        elite1 为 pop 的 第 best1_idx 项。
        elite2 为 pop 的 第 best2_idx 项。
        可变 新种群 为 一组：（空）。
        做 evo_追加 用 新种群、elite1。
        做 evo_追加 用 新种群、elite2。
        可变 ni 为 2。
        只要 ni 小于 种群大小：
            若 ni 取余于 2 等于 0：
                做 evo_追加 用 新种群、做 进化_变异 用 elite1、mut_strength。
            否则：
                做 evo_追加 用 新种群、做 进化_交叉 用 elite1、elite2。
            完。
            ni 为 ni 加 1。
        完。
        pop 为 新种群。
        若 代数 取余于 10 等于 0：
            做 打印 用 「 进化代」 加 做 转字符串 用 代数 加 「 最优=」 加 做 转字符串 用 best1_fit。
        完。
        代数 为 代数 加 1。
    完。
    返还 做 进化_最优 用 pop。
完。

进化_锦标赛选 接受 pop、锦标赛大小：
    种群大小 为 做 计数 用 pop。
    可变 最优索引 为 做 进化_随机 取余于 种群大小。
    可变 best_fit 为 做 进化_取适应度 用 pop 的 第 最优索引 项。
    可变 i 为 1。
    只要 i 小于 锦标赛大小：
        索引 为 做 进化_随机 取余于 种群大小。
        f 为 做 进化_取适应度 用 pop 的 第 索引 项。
        若 f 大于 best_fit：
            best_fit 为 f。
            最优索引 为 索引。
        完。
        i 为 i 加 1。
    完。
    返还 pop 的 第 最优索引 项。
完。

进化_自适应变异 接受 ind、基础强度、最优适应度：
    n 为 做 进化_基因数 用 ind。
    当前适应 为 做 进化_取适应度 用 ind。
    可变 强度 为 基础强度。
    若 最优适应度 大于 0：
        可变 比例 为 (最优适应度 减 当前适应) 乘 1000 除以 最优适应度。
        若 比例 小于 0：
            比例 为 0。
        完。
        强度 为 基础强度 加 基础强度 乘 比例 除以 1000。
    完。
    可变 子节点 为 一组：0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 v 为 做 进化_取基因 用 ind、i。
        若 做 进化_随机 取余于 100 小于 30：
            v 为 v 加 做 进化_随机范围 用 0 减 强度、强度。
        完。
        做 evo_追加 用 子节点、v。
        i 为 i 加 1。
    完。
    返还 子节点。
完。

进化_单点交叉 接受 parent1、parent2：
    n 为 做 进化_基因数 用 parent1。
    切点 为 做 进化_随机 取余于 n。
    可变 子节点 为 一组：0。
    可变 i 为 0。
    只要 i 小于 n：
        若 i 小于 切点：
            做 evo_追加 用 子节点、做 进化_取基因 用 parent1、i。
        否则：
            做 evo_追加 用 子节点、做 进化_取基因 用 parent2、i。
        完。
        i 为 i 加 1。
    完。
    返还 子节点。
完。

进化_演化V2 接受 pop、eval_fn、generations、mut_strength、锦标赛大小、精英数：
    种群大小 为 做 计数 用 pop。
    可变 ei 为 0。
    可变 代数 为 0。
    只要 代数 小于 generations：
        只要 ei 小于 种群大小：
            做 进化_设适应度 用 pop 的 第 ei 项、做 eval_fn 用 pop 的 第 ei 项。
            ei 为 ei 加 1。
        完。
        可变 精英索引 为 一组：（空）。
        可变 已用 为 一组：（空）。
        可变 ui 为 0。
        只要 ui 小于 种群大小：
            做 evo_追加 用 已用、0。
            ui 为 ui 加 1。
        完。
        可变 ei2 为 0。
        只要 ei2 小于 精英数：
            可变 最优 为 0 减 1。
            可变 best_f 为 0 减 0x7FFFFFFF。
            可变 si 为 0。
            只要 si 小于 种群大小：
                若 已用 的 第 si 项 等于 0 并且 做 进化_取适应度 用 pop 的 第 si 项 大于 best_f：
                    best_f 为 做 进化_取适应度 用 pop 的 第 si 项。
                    最优 为 si。
                完。
                si 为 si 加 1。
            完。
            若 最优 大于或等于 0：
                做 evo_追加 用 精英索引、最优。
                已用 的 第 最优 项 为 1。
            完。
            ei2 为 ei2 加 1。
        完。
        最优适应 为 做 进化_取适应度 用 pop 的 第 精英索引 的 第 0 项 项。
        可变 新种群 为 一组：（空）。
        可变 eidx 为 0。
        只要 eidx 小于 做 计数 用 精英索引：
            做 evo_追加 用 新种群、pop 的 第 精英索引 的 第 eidx 项 项。
            eidx 为 eidx 加 1。
        完。
        可变 ni 为 做 计数 用 精英索引。
        只要 ni 小于 种群大小：
            p1 为 做 进化_锦标赛选 用 pop、锦标赛大小。
            若 做 进化_随机 取余于 100 小于 70：
                p2 为 做 进化_锦标赛选 用 pop、锦标赛大小。
                子节点 为 做 进化_交叉 用 p1、p2。
                做 evo_追加 用 新种群、做 进化_自适应变异 用 子节点、mut_strength、最优适应。
            否则：
                做 evo_追加 用 新种群、做 进化_自适应变异 用 p1、mut_strength、最优适应。
            完。
            ni 为 ni 加 1。
        完。
        pop 为 新种群。
        若 代数 取余于 10 等于 0：
            做 打印 用 「 进化v2 代」 加 做 转字符串 用 代数 加 「 最优=」 加 做 转字符串 用 最优适应。
        完。
        代数 为 代数 加 1。
    完。
    返还 做 进化_最优 用 pop。
完。

进化_差分 接受 pop、eval_fn、generations、F、CR：
    种群大小 为 做 计数 用 pop。
    可变 ei 为 0。
    可变 代数 为 0。
    只要 代数 小于 generations：
        只要 ei 小于 种群大小：
            做 进化_设适应度 用 pop 的 第 ei 项、做 eval_fn 用 pop 的 第 ei 项。
            ei 为 ei 加 1。
        完。
        可变 新种群 为 一组：（空）。
        可变 ti 为 0。
        只要 ti 小于 种群大小：
            n 为 做 进化_基因数 用 pop 的 第 ti 项。
            可变 r1 为 做 进化_随机 取余于 种群大小。
            可变 r2 为 做 进化_随机 取余于 种群大小。
            可变 r3 为 做 进化_随机 取余于 种群大小。
            只要 r1 等于 ti：
                r1 为 (r1 加 1) 取余于 种群大小。
            完。
            只要 r2 等于 ti 或者 r2 等于 r1：
                r2 为 (r2 加 1) 取余于 种群大小。
            完。
            只要 r3 等于 ti 或者 r3 等于 r1 或者 r3 等于 r2：
                r3 为 (r3 加 1) 取余于 种群大小。
            完。
            j_rand 为 做 进化_随机 取余于 n。
            可变 trial 为 一组：0。
            可变 gi 为 0。
            只要 gi 小于 n：
                若 做 进化_随机 取余于 1000 小于 CR 或者 gi 等于 j_rand：
                    v 为 做 进化_取基因 用 pop 的 第 r1 项、gi 加 F 乘 (做 进化_取基因 用 pop 的 第 r2 项、gi 减 做 进化_取基因 用 pop 的 第 r3 项、gi) 除以 1000。
                    做 evo_追加 用 trial、v。
                否则：
                    做 evo_追加 用 trial、做 进化_取基因 用 pop 的 第 ti 项、gi。
                完。
                gi 为 gi 加 1。
            完。
            trial_fit 为 做 eval_fn 用 trial。
            做 进化_设适应度 用 trial、trial_fit。
            若 trial_fit 大于或等于 做 进化_取适应度 用 pop 的 第 ti 项：
                做 evo_追加 用 新种群、trial。
            否则：
                做 evo_追加 用 新种群、pop 的 第 ti 项。
            完。
            ti 为 ti 加 1。
        完。
        pop 为 新种群。
        若 代数 取余于 10 等于 0：
            最优 为 做 进化_最优 用 pop。
            做 打印 用 「 差分进化 代」 加 做 转字符串 用 代数 加 「 最优=」 加 做 转字符串 用 做 进化_取适应度 用 最优。
        完。
        代数 为 代数 加 1。
    完。
    返还 做 进化_最优 用 pop。
完。

进化_多样性 接受 pop：
    种群大小 为 做 计数 用 pop。
    可变 总距 为 0。
    若 种群大小 小于 2：
        返还 0。
    完。
    n 为 做 进化_基因数 用 pop 的 第 0 项。
    可变 对数 为 0。
    可变 i 为 0。
    只要 i 小于 种群大小：
        可变 j 为 i 加 1。
        只要 j 小于 种群大小：
            可变 d 为 0。
            可变 k 为 0。
            只要 k 小于 n：
                差值 为 做 进化_取基因 用 pop 的 第 i 项、k 减 做 进化_取基因 用 pop 的 第 j 项、k。
                d 为 d 加 做 进化_绝对值 用 差值。
                k 为 k 加 1。
            完。
            总距 为 总距 加 d 除以 n。
            对数 为 对数 加 1。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 对数 等于 0：
        返还 0。
    完。
    返还 总距 除以 对数。
完。

pso_创建粒子 接受 n_dims：
    可变 粒子 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n_dims：
        做 evo_追加 用 粒子、做 进化_随机范围 用 0 减 1000、1000。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n_dims：
        做 evo_追加 用 粒子、做 进化_随机范围 用 0 减 100、100。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 n_dims：
        做 evo_追加 用 粒子、粒子 的 第 i 项。
        i 为 i 加 1。
    完。
    做 evo_追加 用 粒子、0 减 0x7FFFFFFF。
    做 evo_追加 用 粒子、0。
    返还 粒子。
完。

pso_优化 接受 粒子群、eval_fn、n_dims、迭代数、w、c1、c2：
    n 为 做 计数 用 粒子群。
    可变 gbest_pos 为 一组：（空）。
    可变 di 为 0。
    只要 di 小于 n_dims：
        做 evo_追加 用 gbest_pos、0。
        di 为 di 加 1。
    完。
    可变 gbest_fit 为 0 减 0x7FFFFFFF。
    可变 迭代 为 0。
    只要 迭代 小于 迭代数：
        可变 pi 为 0。
        只要 pi 小于 n：
            p 为 粒子群 的 第 pi 项。
            可变 位置 为 一组：（空）。
            可变 d 为 0。
            只要 d 小于 n_dims：
                做 evo_追加 用 位置、p 的 第 d 项。
                d 为 d 加 1。
            完。
            fit 为 做 eval_fn 用 位置。
            p 的 第 3 乘 n_dims 加 1 项 为 fit。
            若 fit 大于 p 的 第 3 乘 n_dims 项：
                p 的 第 3 乘 n_dims 项 为 fit。
                d 为 0。
                只要 d 小于 n_dims：
                    p 的 第 2 乘 n_dims 加 d 项 为 p 的 第 d 项。
                    d 为 d 加 1。
                完。
            完。
            若 fit 大于 gbest_fit：
                gbest_fit 为 fit。
                d 为 0。
                只要 d 小于 n_dims：
                    gbest_pos 的 第 d 项 为 p 的 第 d 项。
                    d 为 d 加 1。
                完。
            完。
            d 为 0。
            只要 d 小于 n_dims：
                r1 为 做 进化_随机 取余于 1000。
                r2 为 做 进化_随机 取余于 1000。
                p 的 第 n_dims 加 d 项 为 w 乘 p 的 第 n_dims 加 d 项 除以 1000 加 c1 乘 r1 乘 (p 的 第 2 乘 n_dims 加 d 项 减 p 的 第 d 项) 除以 (1000 乘 1000) 加 c2 乘 r2 乘 (gbest_pos 的 第 d 项 减 p 的 第 d 项) 除以 (1000 乘 1000)。
                p 的 第 d 项 为 p 的 第 d 项 加 p 的 第 n_dims 加 d 项。
                d 为 d 加 1。
            完。
            pi 为 pi 加 1。
        完。
        迭代 为 迭代 加 1。
    完。
    返还 gbest_pos。
完。

进化_岛屿演化 接受 岛屿列表、eval_fn、总代数、mut_strength、迁移间隔、_迁移数：
    岛数 为 做 计数 用 岛屿列表。
    可变 ei 为 0。
    可变 代数 为 0。
    只要 代数 小于 总代数：
        可变 isle 为 0。
        只要 isle 小于 岛数：
            pop 为 岛屿列表 的 第 isle 项。
            种群大小 为 做 计数 用 pop。
            只要 ei 小于 种群大小：
                做 进化_设适应度 用 pop 的 第 ei 项、做 eval_fn 用 pop 的 第 ei 项。
                ei 为 ei 加 1。
            完。
            最优 为 做 进化_最优 用 pop。
            可变 新种群 为 一组：最优。
            可变 ni 为 1。
            只要 ni 小于 种群大小：
                做 evo_追加 用 新种群、做 进化_变异 用 最优、mut_strength。
                ni 为 ni 加 1。
            完。
            岛屿列表 的 第 isle 项 为 新种群。
            isle 为 isle 加 1。
        完。
        若 代数 大于 0 并且 代数 取余于 迁移间隔 等于 0：
            isle 为 0。
            只要 isle 小于 岛数：
                源 为 岛屿列表 的 第 isle 项。
                目标岛 为 (isle 加 1) 取余于 岛数。
                最优 为 做 进化_最优 用 源。
                目标pop 为 岛屿列表 的 第 目标岛 项。
                可变 worst_idx 为 0。
                可变 worst_fit 为 做 进化_取适应度 用 目标pop 的 第 0 项。
                可变 wi 为 1。
                只要 wi 小于 做 计数 用 目标pop：
                    若 做 进化_取适应度 用 目标pop 的 第 wi 项 小于 worst_fit：
                        worst_fit 为 做 进化_取适应度 用 目标pop 的 第 wi 项。
                        worst_idx 为 wi。
                    完。
                    wi 为 wi 加 1。
                完。
                目标pop 的 第 worst_idx 项 为 最优。
                isle 为 isle 加 1。
            完。
        完。
        代数 为 代数 加 1。
    完。
    可变 全局最优 为 做 进化_最优 用 岛屿列表 的 第 0 项。
    可变 fi 为 1。
    只要 fi 小于 岛数：
        岛最优 为 做 进化_最优 用 岛屿列表 的 第 fi 项。
        若 做 进化_取适应度 用 岛最优 大于 做 进化_取适应度 用 全局最优：
            全局最优 为 岛最优。
        完。
        fi 为 fi 加 1。
    完。
    返还 全局最优。
完。

进化_加权适应度 接受 目标值列表、权重列表：
    n 为 做 计数 用 目标值列表。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总和 为 总和 加 目标值列表 的 第 i 项 乘 权重列表 的 第 i 项 除以 1000。
        i 为 i 加 1。
    完。
    返还 总和。
完。

进化_测试 接受 零参：
    做 打印 用 「=== 进化搜索测试 ===」。
    做 进化_种子 用 12345。
    r1 为 做 进化_随机。
    r2 为 做 进化_随机。
    若 r1 不等于 r2：
        做 打印 用 「 随机数: 通过」。
    完。
    ind 为 做 进化_创建个体 用 5。
    做 打印 用 「 个体基因数=」 加 做 转字符串 用 做 进化_基因数 用 ind。
    若 做 进化_基因数 用 ind 等于 5：
        做 打印 用 「 个体创建: 通过」。
    完。
    子节点 为 做 进化_变异 用 ind、100。
    做 打印 用 「 变异基因数=」 加 做 转字符串 用 做 进化_基因数 用 子节点。
    若 做 进化_基因数 用 子节点 等于 5：
        做 打印 用 「 变异: 通过」。
    完。
    做 打印 用 「进化搜索: 全部通过」。
完。

本章 收束。
