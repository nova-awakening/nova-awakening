《损失函数》

引用《..·基础》。

引用《..·..·运行时·原语桩》。

损失精度 为 1000。

均方误差 接受 预测、真实：
    n 为 做 计数 用 预测。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 差 为 预测 的 第 i 项 减 真实 的 第 i 项。
        总和 为 总和 加 差 乘 差。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 总和 除以 n。
完。

均方误差梯度 接受 预测、真实：
    n 为 做 计数 用 预测。
    可变 梯度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 梯度、2 乘 (预测 的 第 i 项 减 真实 的 第 i 项) 除以 n。
        i 为 i 加 1。
    完。
    返还 梯度。
完。

平均绝对误差 接受 预测、真实：
    n 为 做 计数 用 预测。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 差 为 预测 的 第 i 项 减 真实 的 第 i 项。
        若 差 小于 0：
            差 为 0 减 差。
        完。
        总和 为 总和 加 差。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 总和 除以 n。
完。

平均绝对误差梯度 接受 预测、真实：
    n 为 做 计数 用 预测。
    可变 梯度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 差 为 预测 的 第 i 项 减 真实 的 第 i 项。
        若 差 大于 0：
            做 追加 用 梯度、损失精度 除以 n。
        否则若 差 小于 0：
            做 追加 用 梯度、0 减 损失精度 除以 n。
        否则：
            做 追加 用 梯度、0。
        完。
        i 为 i 加 1。
    完。
    返还 梯度。
完。

huber损失 接受 预测、真实、增量：
    n 为 做 计数 用 预测。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 差 为 预测 的 第 i 项 减 真实 的 第 i 项。
        若 差 小于 0：
            差 为 0 减 差。
        完。
        若 差 小于或等于 增量：
            总和 为 总和 加 差 乘 差 除以 2。
        否则：
            总和 为 总和 加 增量 乘 (差 减 增量 除以 2)。
        完。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 总和 除以 n。
完。

对数近似 接受 x：
    若 x 小于或等于 0：
        返还 0 减 10000。
    完。
    若 x 小于或等于 100：
        返还 0 减 3000 加 x 乘 10。
    完。
    若 x 小于或等于 500：
        返还 0 减 1000 加 (x 减 100) 乘 2。
    完。
    若 x 小于或等于 损失精度：
        返还 0 减 200 加 (x 减 500) 乘 200 除以 500。
    完。
    返还 0。
完。

损失函数_交叉熵 接受 预测、真实：
    n 为 做 计数 用 预测。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 p 为 预测 的 第 i 项。
        y 为 真实 的 第 i 项。
        若 p 小于 1：
            p 为 1。
        完。
        若 p 大于 损失精度 减 1：
            p 为 损失精度 减 1。
        完。
        损 为 0 减 (y 乘 做 对数近似 用 p 加 (损失精度 减 y) 乘 做 对数近似 用 损失精度 减 p) 除以 损失精度。
        总和 为 总和 加 损。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 总和 除以 n。
完。

交叉熵梯度 接受 预测、真实：
    n 为 做 计数 用 预测。
    可变 梯度 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        可变 p 为 预测 的 第 i 项。
        y 为 真实 的 第 i 项。
        若 p 小于 1：
            p 为 1。
        完。
        若 p 大于 损失精度 减 1：
            p 为 损失精度 减 1。
        完。
        梯度 为 (p 减 y) 乘 损失精度 除以 (p 乘 (损失精度 减 p) 除以 损失精度 加 1)。
        做 追加 用 梯度、梯度 除以 n。
        i 为 i 加 1。
    完。
    返还 梯度。
完。

铰链损失 接受 预测、真实：
    n 为 做 计数 用 预测。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        边距 为 损失精度 减 真实 的 第 i 项 乘 预测 的 第 i 项 除以 损失精度。
        若 边距 大于 0：
            总和 为 总和 加 边距。
        完。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 总和 除以 n。
完。

kl散度 接受 p分布、q分布：
    n 为 做 计数 用 p分布。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        p 为 p分布 的 第 i 项。
        q 为 q分布 的 第 i 项。
        若 p 大于 0 并且 q 大于 0：
            总和 为 总和 加 p 乘 (做 对数近似 用 p 减 做 对数近似 用 q) 除以 损失精度。
        完。
        i 为 i 加 1。
    完。
    返还 总和。
完。

余弦相似度 接受 a、b：
    n 为 做 计数 用 a。
    可变 点积 为 0。
    可变 范数a 为 0。
    可变 范数b 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        点积 为 点积 加 a 的 第 i 项 乘 b 的 第 i 项。
        范数a 为 范数a 加 a 的 第 i 项 乘 a 的 第 i 项。
        范数b 为 范数b 加 b 的 第 i 项 乘 b 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 范乘 为 做 整数开方 用 范数a 乘 做 整数开方 用 范数b。
    若 范乘 等于 0：
        返还 0。
    完。
    返还 点积 乘 损失精度 除以 范乘。
完。

余弦损失 接受 预测、真实：
    返还 损失精度 减 做 余弦相似度 用 预测、真实。
完。

整数开方 接受 n：
    返还 做 开方 用 n。
完。

焦点损失 接受 预测、_真实、衰减率：
    n 为 做 计数 用 预测。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 p 为 预测 的 第 i 项。
        若 p 小于 1：
            p 为 1。
        完。
        若 p 大于 损失精度 减 1：
            p 为 损失精度 减 1。
        完。
        可变 权重 为 损失精度 减 p。
        可变 g 为 0。
        只要 g 小于 衰减率 减 1：
            权重 为 权重 乘 (损失精度 减 p) 除以 损失精度。
            g 为 g 加 1。
        完。
        损 为 0 减 权重 乘 做 对数近似 用 p 除以 损失精度。
        总和 为 总和 加 损。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 总和 除以 n。
完。

平滑交叉熵 接受 预测、真实、极小值、类别数：
    n 为 做 计数 用 预测。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 p 为 预测 的 第 i 项。
        若 p 小于 1：
            p 为 1。
        完。
        若 p 大于 损失精度 减 1：
            p 为 损失精度 减 1。
        完。
        可变 y_smooth 为 极小值 除以 类别数。
        若 真实 的 第 i 项 大于 损失精度 除以 2：
            y_smooth 为 损失精度 减 极小值 加 极小值 除以 类别数。
        完。
        损 为 0 减 y_smooth 乘 做 对数近似 用 p 除以 损失精度。
        总和 为 总和 加 损。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 总和 除以 n。
完。

三元组损失 接受 锚点、正例、负例、margin：
    n 为 做 计数 用 锚点。
    可变 正距 为 0。
    可变 负距 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        dp 为 锚点 的 第 i 项 减 正例 的 第 i 项。
        dn 为 锚点 的 第 i 项 减 负例 的 第 i 项。
        正距 为 正距 加 dp 乘 dp。
        负距 为 负距 加 dn 乘 dn。
        i 为 i 加 1。
    完。
    损失 为 正距 减 负距 加 margin。
    若 损失 小于 0：
        返还 0。
    完。
    返还 损失。
完。

smooth_l1损失 接受 预测、真实、动量率：
    n 为 做 计数 用 预测。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 差 为 预测 的 第 i 项 减 真实 的 第 i 项。
        若 差 小于 0：
            差 为 0 减 差。
        完。
        若 差 小于 动量率：
            总和 为 总和 加 差 乘 差 除以 (2 乘 动量率)。
        否则：
            总和 为 总和 加 差 减 动量率 除以 2。
        完。
        i 为 i 加 1。
    完。
    返还 总和 除以 n。
完。

对比损失 接受 相似度、温度：
    n 为 做 计数 用 相似度。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        缩放 为 相似度 的 第 i 项 乘 损失精度 除以 温度。
        可变 log_sum 为 0。
        可变 j 为 0。
        只要 j 小于 n：
            s 为 相似度 的 第 j 项 乘 损失精度 除以 温度。
            可变 e 为 损失精度 加 s 减 缩放。
            若 e 小于 1：
                e 为 1。
            完。
            log_sum 为 log_sum 加 e。
            j 为 j 加 1。
        完。
        总和 为 总和 加 做 对数近似 用 损失精度 乘 损失精度 除以 log_sum。
        i 为 i 加 1。
    完。
    返还 0 减 总和 除以 n。
完。

l1正则 接受 权重、衰减系数：
    n 为 做 计数 用 权重。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 w 为 权重 的 第 i 项。
        若 w 小于 0：
            w 为 0 减 w。
        完。
        总和 为 总和 加 w。
        i 为 i 加 1。
    完。
    若 损失精度 等于 0：
        返还 0。
    完。
    返还 衰减系数 乘 总和 除以 损失精度。
完。

l2正则 接受 权重、衰减系数：
    n 为 做 计数 用 权重。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        总和 为 总和 加 权重 的 第 i 项 乘 权重 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 衰减系数 乘 总和 除以 (2 乘 损失精度)。
完。

dice损失 接受 预测、真实：
    n 为 做 计数 用 预测。
    可变 交集 为 0。
    可变 预测和 为 0。
    可变 真实和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        交集 为 交集 加 预测 的 第 i 项 乘 真实 的 第 i 项 除以 损失精度。
        预测和 为 预测和 加 预测 的 第 i 项。
        真实和 为 真实和 加 真实 的 第 i 项。
        i 为 i 加 1。
    完。
    分母 为 预测和 加 真实和。
    若 分母 等于 0：
        返还 0。
    完。
    返还 损失精度 减 2 乘 交集 乘 损失精度 除以 分母。
完。

iou损失 接受 预测、真实：
    n 为 做 计数 用 预测。
    可变 交集 为 0。
    可变 并集 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        交集 为 交集 加 预测 的 第 i 项 乘 真实 的 第 i 项 除以 损失精度。
        并集 为 并集 加 预测 的 第 i 项 加 真实 的 第 i 项 减 预测 的 第 i 项 乘 真实 的 第 i 项 除以 损失精度。
        i 为 i 加 1。
    完。
    若 并集 等于 0：
        返还 0。
    完。
    返还 损失精度 减 交集 乘 损失精度 除以 并集。
完。

加权交叉熵 接受 预测、真实、类别权重：
    n 为 做 计数 用 预测。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 p 为 预测 的 第 i 项。
        若 p 小于 1：
            p 为 1。
        完。
        若 p 大于 损失精度 减 1：
            p 为 损失精度 减 1。
        完。
        w 为 类别权重 的 第 i 项。
        损 为 0 减 w 乘 (真实 的 第 i 项 乘 做 对数近似 用 p 加 (损失精度 减 真实 的 第 i 项) 乘 做 对数近似 用 损失精度 减 p) 除以 (损失精度 乘 损失精度)。
        总和 为 总和 加 损。
        i 为 i 加 1。
    完。
    返还 总和 除以 n。
完。

多标签bce 接受 预测、真实：
    n 为 做 计数 用 预测。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        可变 p 为 预测 的 第 i 项。
        若 p 小于 1：
            p 为 1。
        完。
        若 p 大于 损失精度 减 1：
            p 为 损失精度 减 1。
        完。
        损 为 0 减 (真实 的 第 i 项 乘 做 对数近似 用 p 加 (损失精度 减 真实 的 第 i 项) 乘 做 对数近似 用 损失精度 减 p) 除以 损失精度。
        总和 为 总和 加 损。
        i 为 i 加 1。
    完。
    返还 总和 除以 n。
完。

ctc损失近似 接受 预测序列、真实序列、空白id：
    T 为 做 计数 用 预测序列。
    可变 总和 为 0。
    可变 t 为 0。
    只要 t 小于 T：
        帧 为 预测序列 的 第 t 项。
        n 为 做 计数 用 帧。
        可变 最大p 为 0。
        可变 最大类 为 0。
        可变 i 为 0。
        只要 i 小于 n：
            若 帧 的 第 i 项 大于 最大p：
                最大p 为 帧 的 第 i 项。
                最大类 为 i。
            完。
            i 为 i 加 1。
        完。
        若 最大类 等于 空白id：
            总和 为 总和 加 100。
        完。
        若 t 小于 做 计数 用 真实序列：
            目标 为 真实序列 的 第 t 项。
            若 目标 大于或等于 0 并且 目标 小于 n：
                可变 p 为 帧 的 第 目标 项。
                若 p 小于 1：
                    p 为 1。
                完。
                总和 为 总和 减 做 对数近似 用 p。
            完。
        完。
        t 为 t 加 1。
    完。
    返还 总和 除以 T。
完。

elasticnet正则 接受 权重、衰减系数、学习率：
    l1 为 做 l1正则 用 权重、损失精度。
    l2 为 做 l2正则 用 权重、损失精度。
    若 损失精度 等于 0：
        返还 0。
    完。
    返还 衰减系数 乘 (学习率 乘 l1 除以 损失精度 加 (损失精度 减 学习率) 乘 l2 除以 损失精度) 除以 损失精度。
完。

可变 _损失_历史 为 一组：（空）。

可变 _损失_计算次数 为 0。

可变 _损失_最小值 为 999999999。

可变 _损失_最大值 为 0。

损失_记录 接受 值：
    _损失_计算次数 为 _损失_计算次数 加 1。
    做 追加 用 _损失_历史、值。
    若 做 计数 用 _损失_历史 大于 1000：
        _损失_历史 为 一组：（空）。
    完。
    若 值 小于 _损失_最小值：
        _损失_最小值 为 值。
    完。
    若 值 大于 _损失_最大值：
        _损失_最大值 为 值。
    完。
完。

损失_趋势 接受 零参：
    若 做 计数 用 _损失_历史 小于 5：
        返还 「数据不足」。
    完。
    n 为 做 计数 用 _损失_历史。
    前半 为 _损失_历史 的 第 0 项。
    后半 为 _损失_历史 的 第 n 减 1 项。
    若 后半 小于 前半：
        返还 「下降(训练正常)」。
    完。
    若 后半 等于 前半：
        返还 「平稳(可能停滞)」。
    完。
    返还 「上升(可能发散)」。
完。

损失_收敛评估 接受 零参：
    若 做 计数 用 _损失_历史 小于 10：
        返还 「训练初期」。
    完。
    n 为 做 计数 用 _损失_历史。
    近期 为 _损失_历史 的 第 n 减 1 项。
    较早 为 _损失_历史 的 第 n 减 5 项。
    可变 变化 为 近期 减 较早。
    若 变化 小于 0：
        变化 为 0 减 变化。
    完。
    若 变化 小于 10：
        返还 「已收敛」。
    完。
    若 变化 小于 100：
        返还 「接近收敛」。
    完。
    返还 「未收敛」。
完。

损失_历史清空 接受 零参：
    _损失_历史 为 一组：（空）。
完。

损失_波动性 接受 零参：
    若 做 计数 用 _损失_历史 小于 3：
        返还 0。
    完。
    可变 波动 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 _损失_历史：
        可变 差 为 _损失_历史 的 第 i 项 减 _损失_历史 的 第 i 减 1 项。
        若 差 小于 0：
            差 为 0 减 差。
        完。
        波动 为 波动 加 差。
        i 为 i 加 1。
    完。
    返还 波动 除以 (做 计数 用 _损失_历史 减 1)。
完。

损失_稳定性评估 接受 零参：
    波动 为 做 损失_波动性。
    若 波动 小于 10：
        返还 「非常稳定」。
    完。
    若 波动 小于 100：
        返还 「基本稳定」。
    完。
    若 波动 小于 1000：
        返还 「较不稳定(降低学习率)」。
    完。
    返还 「极不稳定(检查数据或模型)」。
完。

损失_学习率建议 接受 当前学习率：
    波动 为 做 损失_波动性。
    若 波动 大于 1000：
        返还 当前学习率 除以 4。
    完。
    若 波动 大于 100：
        返还 当前学习率 除以 2。
    完。
    返还 当前学习率。
完。

本章 收束。
