《决策树》

引用《..·基础》。

引用《工具》。

决策精度 为 1000。

决策树_创建 接受 零参：
    返还 一组：一组：（空）、0。
完。

决策树_叶 接受 树、类别：
    索引 为 做 计数 用 树 的 第 0 项。
    做 追加 用 树 的 第 0 项、一组：0、类别、负 1、负 1、负 1。
    返还 索引。
完。

决策树_分支 接受 树、特征列、阈值、左、右：
    索引 为 做 计数 用 树 的 第 0 项。
    做 追加 用 树 的 第 0 项、一组：1、特征列、阈值、左、右。
    返还 索引。
完。

决策树_预测 接受 树、样本：
    可变 索引 为 树 的 第 1 项。
    只要 真：
        节点 为 树 的 第 0 项 的 第 索引 项。
        若 节点 的 第 0 项 等于 0：
            返还 节点 的 第 1 项。
        完。
        特征值 为 样本 的 第 节点 的 第 1 项 项。
        若 特征值 小于或等于 节点 的 第 2 项：
            索引 为 节点 的 第 3 项。
        否则：
            索引 为 节点 的 第 4 项。
        完。
    完。
    返还 负 1。
完。

决策树_批量预测 接受 树、数据：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 数据：
        做 追加 用 结果、做 决策树_预测 用 树、数据 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

基尼不纯度 接受 标签、类别数：
    n 为 做 计数 用 标签。
    若 n 等于 0：
        返还 0。
    完。
    可变 不纯度 为 决策精度。
    可变 c 为 0。
    只要 c 小于 类别数：
        可变 计数器 为 0。
        可变 i 为 0。
        只要 i 小于 n：
            若 标签 的 第 i 项 等于 c：
                计数器 为 计数器 加 1。
            完。
            i 为 i 加 1。
        完。
        概率 为 计数器 乘 决策精度 除以 n。
        不纯度 为 不纯度 减 概率 乘 概率 除以 决策精度。
        c 为 c 加 1。
    完。
    返还 不纯度。
完。

信息熵 接受 标签、类别数：
    n 为 做 计数 用 标签。
    若 n 等于 0：
        返还 0。
    完。
    可变 熵 为 0。
    可变 c 为 0。
    只要 c 小于 类别数：
        可变 计数器 为 0。
        可变 i 为 0。
        只要 i 小于 n：
            若 标签 的 第 i 项 等于 c：
                计数器 为 计数器 加 1。
            完。
            i 为 i 加 1。
        完。
        若 计数器 大于 0：
            概率 为 计数器 乘 决策精度 除以 n。
            log近似 为 做 决策_log 用 概率。
            熵 为 熵 减 概率 乘 log近似 除以 决策精度。
        完。
        c 为 c 加 1。
    完。
    返还 熵。
完。

最佳分割 接受 数据、标签、特征数、类别数：
    n 为 做 计数 用 数据。
    可变 最优特征 为 0。
    可变 最优阈值 为 0。
    可变 最优基尼 为 决策精度。
    可变 f 为 0。
    只要 f 小于 特征数：
        可变 值列表 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 n：
            做 追加 用 值列表、数据 的 第 i 项 的 第 f 项。
            i 为 i 加 1。
        完。
        i 为 0。
        只要 i 小于 n：
            阈值 为 值列表 的 第 i 项。
            可变 左标签 为 一组：（空）。
            可变 右标签 为 一组：（空）。
            可变 j 为 0。
            只要 j 小于 n：
                若 数据 的 第 j 项 的 第 f 项 小于或等于 阈值：
                    做 追加 用 左标签、标签 的 第 j 项。
                否则：
                    做 追加 用 右标签、标签 的 第 j 项。
                完。
                j 为 j 加 1。
            完。
            左n 为 做 计数 用 左标签。
            右n 为 做 计数 用 右标签。
            若 左n 大于 0 并且 右n 大于 0：
                加权 为 左n 乘 做 基尼不纯度 用 左标签、类别数 除以 n 加 右n 乘 做 基尼不纯度 用 右标签、类别数 除以 n。
                若 加权 小于 最优基尼：
                    最优基尼 为 加权。
                    最优特征 为 f。
                    最优阈值 为 阈值。
                完。
            完。
            i 为 i 加 1。
        完。
        f 为 f 加 1。
    完。
    返还 一组：最优特征、最优阈值、最优基尼。
完。

构建决策树 接受 树、数据、标签、特征数、类别数、最大深度、当前深度：
    n 为 做 计数 用 标签。
    若 n 等于 0：
        返还 做 决策树_叶 用 树、0。
    完。
    若 当前深度 大于或等于 最大深度：
        返还 做 决策树_叶 用 树、做 多数类 用 标签、类别数。
    完。
    可变 全同 为 真。
    可变 i 为 1。
    只要 i 小于 n 并且 全同：
        若 标签 的 第 i 项 不等于 标签 的 第 0 项：
            全同 为 假。
        完。
        i 为 i 加 1。
    完。
    若 全同：
        返还 做 决策树_叶 用 树、标签 的 第 0 项。
    完。
    分割 为 做 最佳分割 用 数据、标签、特征数、类别数。
    特征列 为 分割 的 第 0 项。
    阈值 为 分割 的 第 1 项。
    可变 左数据 为 一组：（空）。
    可变 左标签 为 一组：（空）。
    可变 右数据 为 一组：（空）。
    可变 右标签 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        若 数据 的 第 i 项 的 第 特征列 项 小于或等于 阈值：
            做 追加 用 左数据、数据 的 第 i 项。
            做 追加 用 左标签、标签 的 第 i 项。
        否则：
            做 追加 用 右数据、数据 的 第 i 项。
            做 追加 用 右标签、标签 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 做 计数 用 左数据 等于 0 或者 做 计数 用 右数据 等于 0：
        返还 做 决策树_叶 用 树、做 多数类 用 标签、类别数。
    完。
    左子 为 做 构建决策树 用 树、左数据、左标签、特征数、类别数、最大深度、当前深度 加 1。
    右子 为 做 构建决策树 用 树、右数据、右标签、特征数、类别数、最大深度、当前深度 加 1。
    根 为 做 决策树_分支 用 树、特征列、阈值、左子、右子。
    返还 根。
完。

训练决策树 接受 数据、标签、特征数、类别数、最大深度：
    可变 树 为 做 决策树_创建。
    根 为 做 构建决策树 用 树、数据、标签、特征数、类别数、最大深度、0。
    树 的 第 1 项 为 根。
    返还 树。
完。

随机森林_训练 接受 数据、标签、特征数、类别数、树数、最大深度、种子：
    n 为 做 计数 用 数据。
    可变 森林 为 一组：（空）。
    可变 s 为 种子。
    可变 t 为 0。
    只要 t 小于 树数：
        可变 采样数据 为 一组：（空）。
        可变 采样标签 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 n：
            s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
            索引 为 s 取余于 n。
            做 追加 用 采样数据、数据 的 第 索引 项。
            做 追加 用 采样标签、标签 的 第 索引 项。
            i 为 i 加 1。
        完。
        树 为 做 训练决策树 用 采样数据、采样标签、特征数、类别数、最大深度。
        做 追加 用 森林、树。
        t 为 t 加 1。
    完。
    返还 森林。
完。

随机森林_预测 接受 森林、样本、类别数：
    可变 投票 为 一组：（空）。
    可变 c 为 0。
    只要 c 小于 类别数：
        做 追加 用 投票、0。
        c 为 c 加 1。
    完。
    可变 t 为 0。
    只要 t 小于 做 计数 用 森林：
        预测 为 做 决策树_预测 用 森林 的 第 t 项、样本。
        若 预测 大于或等于 0 并且 预测 小于 类别数：
            投票 的 第 预测 项 为 投票 的 第 预测 项 加 1。
        完。
        t 为 t 加 1。
    完。
    可变 最优 为 0。
    可变 最优票 为 投票 的 第 0 项。
    c 为 1。
    只要 c 小于 类别数：
        若 投票 的 第 c 项 大于 最优票：
            最优票 为 投票 的 第 c 项。
            最优 为 c。
        完。
        c 为 c 加 1。
    完。
    返还 最优。
完。

多数类 接受 标签、类别数：
    可变 最优 为 0。
    可变 最优数 为 0。
    可变 c 为 0。
    只要 c 小于 类别数：
        可变 计数器 为 0。
        可变 i 为 0。
        只要 i 小于 做 计数 用 标签：
            若 标签 的 第 i 项 等于 c：
                计数器 为 计数器 加 1。
            完。
            i 为 i 加 1。
        完。
        若 计数器 大于 最优数：
            最优数 为 计数器。
            最优 为 c。
        完。
        c 为 c 加 1。
    完。
    返还 最优。
完。

决策_log 接受 x：
    若 x 小于或等于 0：
        返还 0。
    完。
    若 x 大于或等于 决策精度：
        返还 0。
    完。
    可变 结果 为 0。
    可变 v 为 x。
    只要 v 小于 500：
        v 为 v 乘 2。
        结果 为 结果 加 决策精度。
    完。
    返还 结果。
完。

特征重要性 接受 树、特征数：
    可变 重要性 为 一组：（空）。
    可变 f 为 0。
    只要 f 小于 特征数：
        做 追加 用 重要性、0。
        f 为 f 加 1。
    完。
    节点列表 为 树 的 第 0 项。
    可变 i 为 0。
    只要 i 小于 做 计数 用 节点列表：
        节点 为 节点列表 的 第 i 项。
        若 节点 的 第 0 项 等于 1：
            特征idx 为 节点 的 第 1 项。
            若 特征idx 大于或等于 0 并且 特征idx 小于 特征数：
                重要性 的 第 特征idx 项 为 重要性 的 第 特征idx 项 加 1。
            完。
        完。
        i 为 i 加 1。
    完。
    返还 重要性。
完。

决策树_深度 接受 树、节点idx：
    节点 为 树 的 第 0 项 的 第 节点idx 项。
    若 节点 的 第 0 项 等于 0：
        返还 0。
    完。
    左深 为 做 决策树_深度 用 树、节点 的 第 3 项。
    右深 为 做 决策树_深度 用 树、节点 的 第 4 项。
    若 左深 大于 右深：
        返还 左深 加 1。
    完。
    返还 右深 加 1。
完。

决策树_叶数 接受 树、节点idx：
    节点 为 树 的 第 0 项 的 第 节点idx 项。
    若 节点 的 第 0 项 等于 0：
        返还 1。
    完。
    返还 做 决策树_叶数 用 树、节点 的 第 3 项 加 做 决策树_叶数 用 树、节点 的 第 4 项。
完。

决策树_准确率 接受 树、数据、标签：
    可变 正确 为 0。
    n 为 做 计数 用 数据。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 决策树_预测 用 树、数据 的 第 i 项 等于 标签 的 第 i 项：
            正确 为 正确 加 1。
        完。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 正确 乘 决策精度 除以 n。
完。

随机森林_准确率 接受 森林、数据、标签、类别数：
    可变 正确 为 0。
    n 为 做 计数 用 数据。
    可变 i 为 0。
    只要 i 小于 n：
        若 做 随机森林_预测 用 森林、数据 的 第 i 项、类别数 等于 标签 的 第 i 项：
            正确 为 正确 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 正确 乘 决策精度 除以 n。
完。

随机森林_特征重要性 接受 森林、特征数：
    可变 总重要性 为 一组：（空）。
    可变 f 为 0。
    只要 f 小于 特征数：
        做 追加 用 总重要性、0。
        f 为 f 加 1。
    完。
    可变 t 为 0。
    只要 t 小于 做 计数 用 森林：
        树重要性 为 做 特征重要性 用 森林 的 第 t 项、特征数。
        f 为 0。
        只要 f 小于 特征数：
            总重要性 的 第 f 项 为 总重要性 的 第 f 项 加 树重要性 的 第 f 项。
            f 为 f 加 1。
        完。
        t 为 t 加 1。
    完。
    返还 总重要性。
完。

混淆矩阵 接受 预测列表、真实列表、类别数：
    可变 矩阵 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 类别数：
        可变 行 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 类别数：
            做 追加 用 行、0。
            j 为 j 加 1。
        完。
        做 追加 用 矩阵、行。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 真实列表：
        真值 为 真实列表 的 第 i 项。
        预 为 预测列表 的 第 i 项。
        若 真值 大于或等于 0 并且 真值 小于 类别数 并且 预 大于或等于 0 并且 预 小于 类别数：
            矩阵 的 第 真值 项 的 第 预 项 为 矩阵 的 第 真值 项 的 第 预 项 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 矩阵。
完。

精确率 接受 混淆、正类：
    tp 为 混淆 的 第 正类 项 的 第 正类 项。
    可变 fp 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 混淆：
        若 i 不等于 正类：
            fp 为 fp 加 混淆 的 第 i 项 的 第 正类 项。
        完。
        i 为 i 加 1。
    完。
    若 tp 加 fp 等于 0：
        返还 0。
    完。
    返还 tp 乘 决策精度 除以 (tp 加 fp)。
完。

召回率 接受 混淆、正类：
    tp 为 混淆 的 第 正类 项 的 第 正类 项。
    可变 fn 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 混淆 的 第 正类 项：
        若 i 不等于 正类：
            fn 为 fn 加 混淆 的 第 正类 项 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    若 tp 加 fn 等于 0：
        返还 0。
    完。
    返还 tp 乘 决策精度 除以 (tp 加 fn)。
完。

f1分数 接受 混淆、正类：
    p 为 做 精确率 用 混淆、正类。
    r 为 做 召回率 用 混淆、正类。
    若 p 加 r 等于 0：
        返还 0。
    完。
    返还 2 乘 p 乘 r 除以 (p 加 r)。
完。

宏平均f1 接受 混淆、类别数：
    可变 总f1 为 0。
    可变 i 为 0。
    只要 i 小于 类别数：
        总f1 为 总f1 加 做 f1分数 用 混淆、i。
        i 为 i 加 1。
    完。
    若 类别数 等于 0：
        返还 0。
    完。
    返还 总f1 除以 类别数。
完。

kappa系数 接受 混淆、类别数：
    可变 总计 为 0。
    可变 行和 为 一组：（空）。
    可变 列和 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 类别数：
        做 追加 用 行和、0。
        做 追加 用 列和、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 类别数：
        可变 j 为 0。
        只要 j 小于 类别数：
            行和 的 第 i 项 为 行和 的 第 i 项 加 混淆 的 第 i 项 的 第 j 项。
            列和 的 第 j 项 为 列和 的 第 j 项 加 混淆 的 第 i 项 的 第 j 项。
            总计 为 总计 加 混淆 的 第 i 项 的 第 j 项。
            若 i 等于 j：
                正确 为 正确 加 混淆 的 第 i 项 的 第 j 项。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    若 总计 等于 0：
        返还 0。
    完。
    po 为 正确 乘 决策精度 除以 总计。
    可变 pe 为 0。
    i 为 0。
    只要 i 小于 类别数：
        pe 为 pe 加 行和 的 第 i 项 乘 列和 的 第 i 项 除以 总计。
        i 为 i 加 1。
    完。
    pe 为 pe 乘 决策精度 除以 总计。
    若 决策精度 等于 pe：
        返还 决策精度。
    完。
    返还 (po 减 pe) 乘 决策精度 除以 (决策精度 减 pe)。
完。

提升树桩_训练 接受 数据、_标签、残差、特征数：
    n 为 做 计数 用 数据。
    可变 最佳特征 为 0。
    可变 最佳阈值 为 0。
    可变 最佳增益 为 0 减 0x7FFFFFFF。
    可变 f 为 0。
    只要 f 小于 特征数：
        可变 i 为 0。
        只要 i 小于 n：
            阈值 为 数据 的 第 i 项 的 第 f 项。
            可变 左和 为 0。
            可变 右和 为 0。
            可变 左n 为 0。
            可变 右n 为 0。
            可变 j 为 0。
            只要 j 小于 n：
                若 数据 的 第 j 项 的 第 f 项 小于或等于 阈值：
                    左和 为 左和 加 残差 的 第 j 项。
                    左n 为 左n 加 1。
                否则：
                    右和 为 右和 加 残差 的 第 j 项。
                    右n 为 右n 加 1。
                完。
                j 为 j 加 1。
            完。
            可变 增益 为 0。
            若 左n 大于 0：
                增益 为 增益 加 左和 乘 左和 除以 左n。
            完。
            若 右n 大于 0：
                增益 为 增益 加 右和 乘 右和 除以 右n。
            完。
            若 增益 大于 最佳增益：
                最佳增益 为 增益。
                最佳特征 为 f。
                最佳阈值 为 阈值。
            完。
            i 为 i 加 1。
        完。
        f 为 f 加 1。
    完。
    返还 一组：最佳特征、最佳阈值。
完。

本章 收束。
