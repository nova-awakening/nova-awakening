《信息论》

引用《..·基础》。

log2近似_千 接受 x：
    若 x 小于或等于 0：
        返还 负 30000。
    完。
    若 x 等于 1000：
        返还 0。
    完。
    可变 v 为 x。
    可变 k 为 0。
    只要 v 大于或等于 2000：
        v 为 v 除以 2。
        k 为 k 加 1。
    完。
    只要 v 小于 1000：
        v 为 v 乘 2。
        k 为 k 减 1。
    完。
    t 为 v 减 1000。
    ln_val 为 t 减 t 乘 t 除以 2000 加 t 乘 t 乘 t 除以 3000000。
    返还 k 乘 1000 加 ln_val 乘 1000 除以 693。
完。

香农熵 接受 概率列表_千：
    可变 H 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 概率列表_千：
        p 为 概率列表_千 的 第 i 项。
        若 p 大于 0：
            H 为 H 减 p 乘 做 log2近似_千 用 p 除以 1000000。
        完。
        i 为 i 加 1。
    完。
    返还 H。
完。

联合熵 接受 联合概率_千：
    返还 做 香农熵 用 联合概率_千。
完。

条件熵 接受 联合概率_千、边缘概率X_千：
    返还 做 联合熵 用 联合概率_千 减 做 香农熵 用 边缘概率X_千。
完。

KL散度 接受 P_千、Q_千：
    若 做 计数 用 P_千 不等于 做 计数 用 Q_千：
        返还 负 1。
    完。
    可变 kl 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 P_千：
        p 为 P_千 的 第 i 项。
        q 为 Q_千 的 第 i 项。
        若 p 大于 0 并且 q 大于 0：
            对数比 为 做 log2近似_千 用 p 减 做 log2近似_千 用 q。
            kl 为 kl 加 p 乘 对数比 除以 1000000。
        完。
        i 为 i 加 1。
    完。
    若 kl 小于 0：
        返还 0。
    完。
    返还 kl。
完。

JS散度 接受 P_千、Q_千：
    若 做 计数 用 P_千 不等于 做 计数 用 Q_千：
        返还 负 1。
    完。
    可变 M_千 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 P_千：
        做 追加 用 M_千、(P_千 的 第 i 项 加 Q_千 的 第 i 项) 除以 2。
        i 为 i 加 1。
    完。
    返还 (做 KL散度 用 P_千、M_千 加 做 KL散度 用 Q_千、M_千) 除以 2。
完。

互信息 接受 边缘X_千、边缘Y_千、联合XY_千：
    返还 做 香农熵 用 边缘X_千 加 做 香农熵 用 边缘Y_千 减 做 联合熵 用 联合XY_千。
完。

信息增益 接受 父类分布_千、子集分布列表：
    父熵 为 做 香农熵 用 父类分布_千。
    可变 加权子熵 为 0。
    可变 父总量 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 父类分布_千：
        父总量 为 父总量 加 父类分布_千 的 第 i 项。
        i 为 i 加 1。
    完。
    若 父总量 等于 0：
        返还 0。
    完。
    i 为 0。
    只要 i 小于 做 计数 用 子集分布列表：
        子集 为 子集分布列表 的 第 i 项。
        可变 子集总量 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 子集：
            子集总量 为 子集总量 加 子集 的 第 j 项。
            j 为 j 加 1。
        完。
        若 子集总量 大于 0 并且 父总量 大于 0：
            加权子熵 为 加权子熵 加 子集总量 乘 做 香农熵 用 子集 除以 父总量。
        完。
        i 为 i 加 1。
    完。
    返还 父熵 减 加权子熵。
完。

交叉熵 接受 真实概率_千、预测概率_千：
    若 做 计数 用 真实概率_千 不等于 做 计数 用 预测概率_千：
        返还 999999。
    完。
    可变 ce 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 真实概率_千：
        y 为 真实概率_千 的 第 i 项。
        p 为 预测概率_千 的 第 i 项。
        若 y 大于 0 并且 p 大于 0：
            ce 为 ce 减 y 乘 做 log2近似_千 用 p 除以 1000000。
        完。
        i 为 i 加 1。
    完。
    返还 ce。
完。

困惑度 接受 熵_千：
    H 为 熵_千 除以 1000。
    可变 pp 为 1。
    可变 i 为 0。
    只要 i 小于 H：
        pp 为 pp 乘 2。
        i 为 i 加 1。
    完。
    返还 pp。
完。

中文信息密度_理论 为 11770。

英文字母密度_理论 为 4700。

中文密度比 为 250。

估算文本信息量 接受 汉字数、英文字符数：
    返还 汉字数 乘 中文信息密度_理论 除以 1000 加 英文字符数 乘 英文字母密度_理论 除以 1000。
完。

本章 收束。
