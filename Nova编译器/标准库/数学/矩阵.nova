《矩阵》

引用《..·基础》。

引用《线性代数》。

引用《..·..·运行时·智能推理·注意力机制》。

引用《..·..·运行时·原语桩》。

引用《工具》。

矩阵_创建 接受 行、列：
    可变 数据 为 一组：行、列。
    总 为 行 乘 列。
    可变 i 为 0。
    只要 i 小于 总：
        做 追加 用 数据、0。
        i 为 i 加 1。
    完。
    返还 数据。
完。

矩阵_行数 接受 m：
    返还 m 的 第 0 项。
完。

矩阵_列数 接受 m：
    返还 m 的 第 1 项。
完。

矩阵_取 接受 m、i、j：
    返还 m 的 第 2 加 i 乘 m 的 第 1 项 加 j 项。
完。

矩阵_设 接受 m、i、j、v：
    m 的 第 2 加 i 乘 m 的 第 1 项 加 j 项 为 v。
完。

矩阵_单位 接受 n：
    可变 m 为 做 矩阵_创建 用 n、n。
    可变 i 为 0。
    只要 i 小于 n：
        做 矩阵_设 用 m、i、i、1。
        i 为 i 加 1。
    完。
    返还 m。
完。

矩阵_零 接受 行、列：
    返还 做 矩阵_创建 用 行、列。
完。

矩阵_从列表 接受 行、列、数据：
    可变 m 为 一组：行、列。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        若 i 小于 做 计数 用 数据：
            做 追加 用 m、数据 的 第 i 项。
        否则：
            做 追加 用 m、0。
        完。
        i 为 i 加 1。
    完。
    返还 m。
完。

矩阵_加 接受 a、b：
    行 为 a 的 第 0 项。
    列 为 a 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 行、列。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 结果、i、j、做 矩阵_取 用 a、i、j 加 做 矩阵_取 用 b、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_减 接受 a、b：
    行 为 a 的 第 0 项。
    列 为 a 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 行、列。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 结果、i、j、做 矩阵_取 用 a、i、j 减 做 矩阵_取 用 b、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_块_大小 为 8。

矩阵_矩阵_乘 接受 A、B、C、m、n、k：
    可变 ii 为 0。
    若 A 等于 0 或者 B 等于 0 或者 C 等于 0：
        返还。
    完。
    只要 ii 小于 m：
        可变 jj 为 0。
        只要 jj 小于 n：
            可变 kk 为 0。
            只要 kk 小于 k：
                行末 为 ii 加 矩阵_块_大小。
                若 行末 大于 m：
                    行末 为 m。
                完。
                列末 为 jj 加 矩阵_块_大小。
                若 列末 大于 n：
                    列末 为 n。
                完。
                层末 为 kk 加 矩阵_块_大小。
                若 层末 大于 k：
                    层末 为 k。
                完。
                可变 i 为 ii。
                只要 i 小于 行末：
                    可变 j 为 jj。
                    只要 j 小于 列末：
                        可变 总和 为 做 内存_读32有符号 用 C 加 (i 乘 n 加 j) 乘 4。
                        可变 p 为 kk。
                        只要 p 小于 层末：
                            总和 为 总和 加 做 Q乘 用 做 内存_读32有符号 用 A 加 (i 乘 k 加 p) 乘 4、做 内存_读32有符号 用 B 加 (p 乘 n 加 j) 乘 4。
                            p 为 p 加 1。
                        完。
                        做 内存_写32 用 C 加 (i 乘 n 加 j) 乘 4、总和。
                        j 为 j 加 1。
                    完。
                    i 为 i 加 1。
                完。
                kk 为 kk 加 矩阵_块_大小。
            完。
            jj 为 jj 加 矩阵_块_大小。
        完。
        ii 为 ii 加 矩阵_块_大小。
    完。
完。

矩阵_数乘 接受 m、s：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 行、列。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 结果、i、j、做 矩阵_取 用 m、i、j 乘 s。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_转置 接受 m、_扩2、_扩3：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 列、行。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 结果、j、i、做 矩阵_取 用 m、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_迹 接受 m：
    n 为 m 的 第 0 项。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和 为 和 加 做 矩阵_取 用 m、i、i。
        i 为 i 加 1。
    完。
    返还 和。
完。

矩阵_逐元素 接受 m、操作：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 行、列。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 结果、i、j、做 操作 用 做 矩阵_取 用 m、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_哈达玛 接受 a、b：
    行 为 a 的 第 0 项。
    列 为 a 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 行、列。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 结果、i、j、做 矩阵_取 用 a、i、j 乘 做 矩阵_取 用 b、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_行 接受 m、i：
    列 为 m 的 第 1 项。
    可变 行 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 列：
        做 追加 用 行、做 矩阵_取 用 m、i、j。
        j 为 j 加 1。
    完。
    返还 行。
完。

矩阵_列 接受 m、j：
    行 为 m 的 第 0 项。
    可变 列 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 行：
        做 追加 用 列、做 矩阵_取 用 m、i、j。
        i 为 i 加 1。
    完。
    返还 列。
完。

矩阵_行和 接受 m：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 行：
        可变 和 为 0。
        可变 j 为 0。
        只要 j 小于 列：
            和 为 和 加 做 矩阵_取 用 m、i、j。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_列和 接受 m：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 结果 为 一组：（空）。
    可变 j 为 0。
    只要 j 小于 列：
        可变 和 为 0。
        可变 i 为 0。
        只要 i 小于 行：
            和 为 和 加 做 矩阵_取 用 m、i、j。
            i 为 i 加 1。
        完。
        做 追加 用 结果、和。
        j 为 j 加 1。
    完。
    返还 结果。
完。

矩阵_最大 接受 m：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 最大 为 m 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        若 m 的 第 2 加 i 项 大于 最大：
            最大 为 m 的 第 2 加 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

矩阵_最小 接受 m：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 最小 为 m 的 第 2 项。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        若 m 的 第 2 加 i 项 小于 最小：
            最小 为 m 的 第 2 加 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小。
完。

矩阵_向量_点积 接受 a、b：
    n 为 做 计数 用 a。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和 为 和 加 a 的 第 i 项 乘 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和。
完。

向量_范数平方 接受 v：
    返还 做 矩阵_向量_点积 用 v、v。
完。

矩阵_向量_加 接受 a、b：
    n 为 做 计数 用 a。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、a 的 第 i 项 加 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_减 接受 a、b：
    n 为 做 计数 用 a。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、a 的 第 i 项 减 b 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_数乘 接受 v、s：
    n 为 做 计数 用 v。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、v 的 第 i 项 乘 s。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_外积 接受 a、b：
    行 为 做 计数 用 a。
    列 为 做 计数 用 b。
    可变 m 为 做 矩阵_创建 用 行、列。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 m、i、j、a 的 第 i 项 乘 b 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 m。
完。

矩阵_向量乘 接受 m、v：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 行：
        可变 和 为 0。
        可变 j 为 0。
        只要 j 小于 列：
            和 为 和 加 做 矩阵_取 用 m、i、j 乘 v 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_等于 接受 a、b：
    若 a 的 第 0 项 不等于 b 的 第 0 项 或者 a 的 第 1 项 不等于 b 的 第 1 项：
        返还 假。
    完。
    可变 i 为 0。
    只要 i 小于 a 的 第 0 项 乘 a 的 第 1 项：
        若 a 的 第 2 加 i 项 不等于 b 的 第 2 加 i 项：
            返还 假。
        完。
        i 为 i 加 1。
    完。
    返还 真。
完。

矩阵_子矩阵 接受 m、行起、列起、行数、列数：
    可变 结果 为 做 矩阵_创建 用 行数、列数。
    可变 i 为 0。
    只要 i 小于 行数：
        可变 j 为 0。
        只要 j 小于 列数：
            做 矩阵_设 用 结果、i、j、做 矩阵_取 用 m、行起 加 i、列起 加 j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_水平拼接 接受 a、b：
    行 为 a 的 第 0 项。
    列a 为 a 的 第 1 项。
    列b 为 b 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 行、列a 加 列b。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列a：
            做 矩阵_设 用 结果、i、j、做 矩阵_取 用 a、i、j。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 列b：
            做 矩阵_设 用 结果、i、列a 加 j、做 矩阵_取 用 b、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_垂直拼接 接受 a、b：
    行a 为 a 的 第 0 项。
    行b 为 b 的 第 0 项。
    列 为 a 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 行a 加 行b、列。
    可变 i 为 0。
    只要 i 小于 行a：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 结果、i、j、做 矩阵_取 用 a、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 行b：
        可变 j 为 0。
        只要 j 小于 列：
            做 矩阵_设 用 结果、行a 加 i、j、做 矩阵_取 用 b、i、j。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

行列式2 接受 m：
    返还 做 矩阵_取 用 m、0、0 乘 做 矩阵_取 用 m、1、1 减 做 矩阵_取 用 m、0、1 乘 做 矩阵_取 用 m、1、0。
完。

行列式3 接受 m：
    a 为 做 矩阵_取 用 m、0、0。
    b 为 做 矩阵_取 用 m、0、1。
    c 为 做 矩阵_取 用 m、0、2。
    d 为 做 矩阵_取 用 m、1、0。
    e 为 做 矩阵_取 用 m、1、1。
    f 为 做 矩阵_取 用 m、1、2。
    g 为 做 矩阵_取 用 m、2、0。
    h 为 做 矩阵_取 用 m、2、1。
    k 为 做 矩阵_取 用 m、2、2。
    返还 a 乘 (e 乘 k 减 f 乘 h) 减 b 乘 (d 乘 k 减 f 乘 g) 加 c 乘 (d 乘 h 减 e 乘 g)。
完。

矩阵_kronecker 接受 A、B：
    ra 为 A 的 第 0 项。
    ca 为 A 的 第 1 项。
    rb 为 B 的 第 0 项。
    cb 为 B 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 ra 乘 rb、ca 乘 cb。
    可变 i 为 0。
    只要 i 小于 ra：
        可变 j 为 0。
        只要 j 小于 ca：
            aij 为 做 矩阵_取 用 A、i、j。
            可变 p 为 0。
            只要 p 小于 rb：
                可变 q 为 0。
                只要 q 小于 cb：
                    做 矩阵_设 用 结果、i 乘 rb 加 p、j 乘 cb 加 q、aij 乘 做 矩阵_取 用 B、p、q。
                    q 为 q 加 1。
                完。
                p 为 p 加 1。
            完。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_幂 接受 m、n：
    大小 为 m 的 第 0 项。
    可变 结果 为 做 矩阵_单位 用 大小。
    可变 基 为 m。
    可变 指数 为 n。
    只要 指数 大于 0：
        若 指数 取余于 2 等于 1：
            结果 为 做 矩阵_分块乘 用 结果、基。
        完。
        基 为 做 矩阵_分块乘 用 基、基。
        指数 为 指数 除以 2。
    完。
    返还 结果。
完。

矩阵_对角 接受 对角元素：
    n 为 做 计数 用 对角元素。
    可变 m 为 做 矩阵_创建 用 n、n。
    可变 i 为 0。
    只要 i 小于 n：
        做 矩阵_设 用 m、i、i、对角元素 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 m。
完。

矩阵_取对角 接受 m：
    n 为 m 的 第 0 项。
    可变 对角 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 对角、做 矩阵_取 用 m、i、i。
        i 为 i 加 1。
    完。
    返还 对角。
完。

矩阵_无穷范数 接受 m：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 最大 为 0。
    可变 i 为 0。
    只要 i 小于 行：
        可变 行和 为 0。
        可变 j 为 0。
        只要 j 小于 列：
            可变 v 为 做 矩阵_取 用 m、i、j。
            若 v 小于 0：
                v 为 0 减 v。
            完。
            行和 为 行和 加 v。
            j 为 j 加 1。
        完。
        若 行和 大于 最大：
            最大 为 行和。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

矩阵_1范数 接受 m：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 最大 为 0。
    可变 j 为 0。
    只要 j 小于 列：
        可变 列和 为 0。
        可变 i 为 0。
        只要 i 小于 行：
            可变 v 为 做 矩阵_取 用 m、i、j。
            若 v 小于 0：
                v 为 0 减 v。
            完。
            列和 为 列和 加 v。
            i 为 i 加 1。
        完。
        若 列和 大于 最大：
            最大 为 列和。
        完。
        j 为 j 加 1。
    完。
    返还 最大。
完。

矩阵_逐元素除 接受 a、b、精度：
    行 为 a 的 第 0 项。
    列 为 a 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 行、列。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列：
            可变 分母 为 做 矩阵_取 用 b、i、j。
            若 分母 等于 0：
                分母 为 1。
            完。
            做 矩阵_设 用 结果、i、j、做 矩阵_取 用 a、i、j 乘 精度 除以 分母。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_clamp 接受 m、最小、最大：
    行 为 m 的 第 0 项。
    列 为 m 的 第 1 项。
    可变 结果 为 做 矩阵_创建 用 行、列。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        可变 v 为 m 的 第 2 加 i 项。
        若 v 小于 最小：
            v 为 最小。
        完。
        若 v 大于 最大：
            v 为 最大。
        完。
        结果 的 第 2 加 i 项 为 v。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_展平 接受 m：
    n 为 m 的 第 0 项 乘 m 的 第 1 项。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、m 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    返还 结果。
完。

矩阵_reshape 接受 向量、行、列：
    返还 做 矩阵_从列表 用 行、列、向量。
完。

矩阵_随机 接受 行、列、最小、最大、种子：
    可变 m 为 一组：行、列。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 行 乘 列：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        做 追加 用 m、最小 加 s 取余于 (最大 减 最小 加 1)。
        i 为 i 加 1。
    完。
    返还 m。
完。

矩阵_向量_范数 接受 v：
    返还 做 开方 用 做 矩阵_向量_点积 用 v、v。
完。

向量_归一化 接受 v、精度：
    范数 为 做 矩阵_向量_范数 用 v。
    若 范数 等于 0：
        返还 v。
    完。
    n 为 做 计数 用 v。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、v 的 第 i 项 乘 精度 除以 范数。
        i 为 i 加 1。
    完。
    返还 结果。
完。

向量_余弦相似度 接受 a、b：
    点 为 做 矩阵_向量_点积 用 a、b。
    范数a 为 做 矩阵_向量_范数 用 a。
    范数b 为 做 矩阵_向量_范数 用 b。
    若 范数a 等于 0 或者 范数b 等于 0：
        返还 0。
    完。
    返还 点 乘 1000 除以 (范数a 乘 范数b)。
完。

矩阵_块大小 为 8。

矩阵_分块乘 接受 A、B：
    可变 ii 为 0。
    m 为 做 矩阵_行数 用 A。
    k 为 做 矩阵_列数 用 A。
    n 为 做 矩阵_列数 用 B。
    若 做 矩阵_行数 用 B 不等于 k：
        返还 空。
    完。
    可变 C 为 做 矩阵_创建 用 m、n。
    bs 为 矩阵_块大小。
    只要 ii 小于 m：
        可变 行末 为 m。
        若 ii 加 bs 小于 m：
            行末 为 ii 加 bs。
        完。
        可变 jj 为 0。
        只要 jj 小于 n：
            可变 列末 为 n。
            若 jj 加 bs 小于 n：
                列末 为 jj 加 bs。
            完。
            可变 kk 为 0。
            只要 kk 小于 k：
                可变 层末 为 k。
                若 kk 加 bs 小于 k：
                    层末 为 kk 加 bs。
                完。
                可变 i 为 ii。
                只要 i 小于 行末：
                    可变 j 为 jj。
                    只要 j 小于 列末：
                        可变 准确率 为 做 矩阵_取 用 C、i、j。
                        可变 l 为 kk。
                        只要 l 小于 层末：
                            准确率 为 准确率 加 做 矩阵_取 用 A、i、l 乘 做 矩阵_取 用 B、l、j。
                            l 为 l 加 1。
                        完。
                        做 矩阵_设 用 C、i、j、准确率。
                        j 为 j 加 1。
                    完。
                    i 为 i 加 1。
                完。
                kk 为 kk 加 bs。
            完。
            jj 为 jj 加 bs。
        完。
        ii 为 ii 加 bs。
    完。
    返还 C。
完。

矩阵_内存报告 接受 m：
    行数 为 做 矩阵_行数 用 m。
    列数 为 做 矩阵_列数 用 m。
    元素数 为 行数 乘 列数。
    返还 一组：元素数、1、行数 加 1。
完。

本章 收束。
