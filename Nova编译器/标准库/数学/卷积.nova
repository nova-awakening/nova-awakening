《卷积》

引用《..·基础》。

引用《..·..·运行时·原语桩》。

卷积精度 为 1000。

卷积1d 接受 信号、核：
    信号长 为 做 计数 用 信号。
    输出长 为 信号长 减 核长 加 1。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 输出长：
        可变 和 为 0。
        可变 j 为 0。
        只要 j 小于 核长：
            和 为 和 加 信号 的 第 i 加 j 项 乘 核 的 第 j 项 除以 卷积精度。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

卷积1d_same 接受 信号、核：
    信号长 为 做 计数 用 信号。
    填充 为 核长 除以 2。
    可变 填充信号 为 一组：（空）。
    可变 p 为 0。
    只要 p 小于 填充：
        做 追加 用 填充信号、0。
        p 为 p 加 1。
    完。
    可变 i 为 0。
    只要 i 小于 信号长：
        做 追加 用 填充信号、信号 的 第 i 项。
        i 为 i 加 1。
    完。
    p 为 0。
    只要 p 小于 填充：
        做 追加 用 填充信号、0。
        p 为 p 加 1。
    完。
    返还 做 卷积1d 用 填充信号、核。
完。

卷积2d 接受 输入、核：
    输入行 为 输入 的 第 0 项。
    输入列 为 输入 的 第 1 项。
    核行 为 核 的 第 0 项。
    核列 为 核 的 第 1 项。
    输出行 为 输入行 减 核行 加 1。
    输出列 为 输入列 减 核列 加 1。
    可变 结果 为 一组：输出行、输出列。
    可变 i 为 0。
    只要 i 小于 输出行：
        可变 j 为 0。
        只要 j 小于 输出列：
            可变 和 为 0。
            可变 ki 为 0。
            只要 ki 小于 核行：
                可变 kj 为 0。
                只要 kj 小于 核列：
                    输入值 为 输入 的 第 2 加 (i 加 ki) 乘 输入列 加 (j 加 kj) 项。
                    核值 为 核 的 第 2 加 ki 乘 核列 加 kj 项。
                    和 为 和 加 输入值 乘 核值 除以 卷积精度。
                    kj 为 kj 加 1。
                完。
                ki 为 ki 加 1。
            完。
            做 追加 用 结果、和。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

最大池化2d 接受 输入、池大小：
    输入行 为 输入 的 第 0 项。
    输入列 为 输入 的 第 1 项。
    输出行 为 输入行 除以 池大小。
    输出列 为 输入列 除以 池大小。
    可变 结果 为 一组：输出行、输出列。
    可变 i 为 0。
    只要 i 小于 输出行：
        可变 j 为 0。
        只要 j 小于 输出列：
            可变 最大 为 输入 的 第 2 加 i 乘 池大小 乘 输入列 加 j 乘 池大小 项。
            可变 pi 为 0。
            只要 pi 小于 池大小：
                可变 pj 为 0。
                只要 pj 小于 池大小：
                    v 为 输入 的 第 2 加 (i 乘 池大小 加 pi) 乘 输入列 加 j 乘 池大小 加 pj 项。
                    若 v 大于 最大：
                        最大 为 v。
                    完。
                    pj 为 pj 加 1。
                完。
                pi 为 pi 加 1。
            完。
            做 追加 用 结果、最大。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

平均池化2d 接受 输入、池大小：
    输入行 为 输入 的 第 0 项。
    输入列 为 输入 的 第 1 项。
    输出行 为 输入行 除以 池大小。
    输出列 为 输入列 除以 池大小。
    池面积 为 池大小 乘 池大小。
    可变 结果 为 一组：输出行、输出列。
    可变 i 为 0。
    只要 i 小于 输出行：
        可变 j 为 0。
        只要 j 小于 输出列：
            可变 和 为 0。
            可变 pi 为 0。
            只要 pi 小于 池大小：
                可变 pj 为 0。
                只要 pj 小于 池大小：
                    和 为 和 加 输入 的 第 2 加 (i 乘 池大小 加 pi) 乘 输入列 加 j 乘 池大小 加 pj 项。
                    pj 为 pj 加 1。
                完。
                pi 为 pi 加 1。
            完。
            做 追加 用 结果、和 除以 池面积。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

全局平均池化 接受 输入：
    行 为 输入 的 第 0 项。
    列 为 输入 的 第 1 项。
    n 为 行 乘 列。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和 为 和 加 输入 的 第 2 加 i 项。
        i 为 i 加 1。
    完。
    若 n 等于 0：
        返还 0。
    完。
    返还 和 除以 n。
完。

最大池化1d 接受 信号、池大小：
    n 为 做 计数 用 信号。
    输出长 为 n 除以 池大小。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 输出长：
        可变 最大 为 信号 的 第 i 乘 池大小 项。
        可变 j 为 1。
        只要 j 小于 池大小：
            若 信号 的 第 i 乘 池大小 加 j 项 大于 最大：
                最大 为 信号 的 第 i 乘 池大小 加 j 项。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 结果、最大。
        i 为 i 加 1。
    完。
    返还 结果。
完。

平均池化1d 接受 信号、池大小：
    n 为 做 计数 用 信号。
    输出长 为 n 除以 池大小。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 输出长：
        可变 和 为 0。
        可变 j 为 0。
        只要 j 小于 池大小：
            和 为 和 加 信号 的 第 i 乘 池大小 加 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 结果、和 除以 池大小。
        i 为 i 加 1。
    完。
    返还 结果。
完。

sobel_水平 接受 零参：
    返还 一组：3、3、负 1、0、1、负 2、0、2、负 1、0、1。
完。

sobel_垂直 接受 零参：
    返还 一组：3、3、负 1、负 2、负 1、0、0、1、2、1。
完。

均值核 接受 大小：
    可变 核 为 一组：大小、大小。
    值 为 卷积精度 除以 (大小 乘 大小)。
    可变 i 为 0。
    只要 i 小于 大小 乘 大小：
        做 追加 用 核、值。
        i 为 i 加 1。
    完。
    返还 核。
完。

锐化核 接受 零参：
    返还 一组：3、3、负 卷积精度、负 卷积精度、5 乘 卷积精度、0 减 卷积精度、负 卷积精度、0。
完。

随机丢弃 接受 向量、保留率、种子：
    n 为 做 计数 用 向量。
    可变 结果 为 一组：（空）。
    可变 s 为 种子。
    可变 i 为 0。
    只要 i 小于 n：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        若 s 取余于 卷积精度 小于 保留率：
            做 追加 用 结果、向量 的 第 i 项 乘 卷积精度 除以 保留率。
        否则：
            做 追加 用 结果、0。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

批量归一化 接受 批次：
    n 为 做 计数 用 批次。
    可变 方差和 为 0。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        和 为 和 加 批次 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 和 除以 n。
    i 为 0。
    只要 i 小于 n：
        差 为 批次 的 第 i 项 减 均值。
        方差和 为 方差和 加 差 乘 差。
        i 为 i 加 1。
    完。
    可变 标准差 为 做 卷积_开方 用 方差和 除以 n。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        做 追加 用 结果、(批次 的 第 i 项 减 均值) 乘 卷积精度 除以 标准差。
        i 为 i 加 1。
    完。
    返还 结果。
完。

卷积_开方 接受 n：
    返还 做 开方 用 n。
完。

深度卷积2d 接受 输入、通道数、行、列、核、核大小：
    输出行 为 行 减 核大小 加 1。
    输出列 为 列 减 核大小 加 1。
    可变 结果 为 一组：（空）。
    可变 c 为 0。
    只要 c 小于 通道数：
        可变 i 为 0。
        只要 i 小于 输出行：
            可变 j 为 0。
            只要 j 小于 输出列：
                可变 和 为 0。
                可变 ki 为 0。
                只要 ki 小于 核大小：
                    可变 kj 为 0。
                    只要 kj 小于 核大小：
                        和 为 和 加 输入 的 第 c 乘 行 乘 列 加 (i 加 ki) 乘 列 加 (j 加 kj) 项 乘 核 的 第 c 乘 核大小 乘 核大小 加 ki 乘 核大小 加 kj 项 除以 卷积精度。
                        kj 为 kj 加 1。
                    完。
                    ki 为 ki 加 1。
                完。
                做 追加 用 结果、和。
                j 为 j 加 1。
            完。
            i 为 i 加 1。
        完。
        c 为 c 加 1。
    完。
    返还 结果。
完。

逐点卷积 接受 输入、输入通道、输出通道、空间大小、权重：
    可变 结果 为 一组：（空）。
    可变 oc 为 0。
    只要 oc 小于 输出通道：
        可变 s 为 0。
        只要 s 小于 空间大小：
            可变 和 为 0。
            可变 ic 为 0。
            只要 ic 小于 输入通道：
                和 为 和 加 输入 的 第 ic 乘 空间大小 加 s 项 乘 权重 的 第 oc 乘 输入通道 加 ic 项 除以 卷积精度。
                ic 为 ic 加 1。
            完。
            做 追加 用 结果、和。
            s 为 s 加 1。
        完。
        oc 为 oc 加 1。
    完。
    返还 结果。
完。

空洞卷积1d 接受 信号、核、膨胀率：
    信号长 为 做 计数 用 信号。
    有效核长 为 (核长 减 1) 乘 膨胀率 加 1。
    输出长 为 信号长 减 有效核长 加 1。
    若 输出长 小于 1：
        返还 一组：（空）。
    完。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 输出长：
        可变 和 为 0。
        可变 k 为 0。
        只要 k 小于 核长：
            和 为 和 加 信号 的 第 i 加 k 乘 膨胀率 项 乘 核 的 第 k 项 除以 卷积精度。
            k 为 k 加 1。
        完。
        做 追加 用 结果、和。
        i 为 i 加 1。
    完。
    返还 结果。
完。

空洞卷积2d 接受 输入、核、膨胀率：
    输入行 为 输入 的 第 0 项。
    输入列 为 输入 的 第 1 项。
    核行 为 核 的 第 0 项。
    核列 为 核 的 第 1 项。
    有效核行 为 (核行 减 1) 乘 膨胀率 加 1。
    有效核列 为 (核列 减 1) 乘 膨胀率 加 1。
    输出行 为 输入行 减 有效核行 加 1。
    输出列 为 输入列 减 有效核列 加 1。
    若 输出行 小于 1 或者 输出列 小于 1：
        返还 一组：0、0。
    完。
    可变 结果 为 一组：输出行、输出列。
    可变 i 为 0。
    只要 i 小于 输出行：
        可变 j 为 0。
        只要 j 小于 输出列：
            可变 和 为 0。
            可变 ki 为 0。
            只要 ki 小于 核行：
                可变 kj 为 0。
                只要 kj 小于 核列：
                    和 为 和 加 输入 的 第 2 加 (i 加 ki 乘 膨胀率) 乘 输入列 加 (j 加 kj 乘 膨胀率) 项 乘 核 的 第 2 加 ki 乘 核列 加 kj 项 除以 卷积精度。
                    kj 为 kj 加 1。
                完。
                ki 为 ki 加 1。
            完。
            做 追加 用 结果、和。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

转置卷积1d 接受 信号、核、步幅：
    信号长 为 做 计数 用 信号。
    输出长 为 (信号长 减 1) 乘 步幅 加 核长。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 输出长：
        做 追加 用 结果、0。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 信号长：
        可变 k 为 0。
        只要 k 小于 核长：
            结果 的 第 i 乘 步幅 加 k 项 为 结果 的 第 i 乘 步幅 加 k 项 加 信号 的 第 i 项 乘 核 的 第 k 项 除以 卷积精度。
            k 为 k 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

自适应平均池化1d 接受 信号、目标大小：
    n 为 做 计数 用 信号。
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 目标大小：
        起 为 i 乘 n 除以 目标大小。
        可变 止 为 (i 加 1) 乘 n 除以 目标大小。
        若 止 大于 n：
            止 为 n。
        完。
        可变 和 为 0。
        可变 计数值 为 0。
        可变 j 为 起。
        只要 j 小于 止：
            和 为 和 加 信号 的 第 j 项。
            计数值 为 计数值 加 1。
            j 为 j 加 1。
        完。
        若 计数值 等于 0：
            计数值 为 1。
        完。
        做 追加 用 结果、和 除以 计数值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

全局平均池化_多通道 接受 输入、通道数、空间大小：
    可变 结果 为 一组：（空）。
    可变 c 为 0。
    只要 c 小于 通道数：
        可变 和 为 0。
        可变 s 为 0。
        只要 s 小于 空间大小：
            和 为 和 加 输入 的 第 c 乘 空间大小 加 s 项。
            s 为 s 加 1。
        完。
        做 追加 用 结果、和 除以 空间大小。
        c 为 c 加 1。
    完。
    返还 结果。
完。

高斯核 接受 大小、sigma：
    可变 核 为 一组：大小、大小。
    中心 为 大小 除以 2。
    可变 总和 为 0。
    可变 i 为 0。
    只要 i 小于 大小：
        可变 j 为 0。
        只要 j 小于 大小：
            导数 为 i 减 中心。
            导数Y 为 j 减 中心。
            r2 为 导数 乘 导数 加 导数Y 乘 导数Y。
            可变 v 为 卷积精度 减 r2 乘 500 除以 (sigma 乘 sigma)。
            若 v 小于 1：
                v 为 1。
            完。
            做 追加 用 核、v。
            总和 为 总和 加 v。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    可变 k 为 2。
    只要 k 小于 2 加 大小 乘 大小：
        核 的 第 k 项 为 核 的 第 k 项 乘 卷积精度 除以 总和。
        k 为 k 加 1。
    完。
    返还 核。
完。

拉普拉斯核 接受 零参：
    返还 一组：3、3、0、卷积精度、0、卷积精度、0 减 4 乘 卷积精度、卷积精度、0、卷积精度、0。
完。

分组卷积1d 接受 信号、核列表、组数：
    信号长 为 做 计数 用 信号。
    通道 为 信号长 除以 组数。
    可变 结果 为 一组：（空）。
    可变 g 为 0。
    只要 g 小于 组数：
        可变 组信号 为 一组：（空）。
        可变 i 为 0。
        只要 i 小于 通道：
            做 追加 用 组信号、信号 的 第 g 乘 通道 加 i 项。
            i 为 i 加 1。
        完。
        组输出 为 做 卷积1d 用 组信号、核列表 的 第 g 项。
        i 为 0。
        只要 i 小于 做 计数 用 组输出：
            做 追加 用 结果、组输出 的 第 i 项。
            i 为 i 加 1。
        完。
        g 为 g 加 1。
    完。
    返还 结果。
完。

批归一化1d 接受 输入、缩放因子、偏移因子：
    n 为 做 计数 用 输入。
    可变 均值 为 0。
    可变 i 为 0。
    只要 i 小于 n：
        均值 为 均值 加 输入 的 第 i 项。
        i 为 i 加 1。
    完。
    均值 为 均值 除以 n。
    可变 方差 为 0。
    i 为 0。
    只要 i 小于 n：
        d 为 输入 的 第 i 项 减 均值。
        方差 为 方差 加 d 乘 d 除以 卷积精度。
        i 为 i 加 1。
    完。
    方差 为 方差 除以 n。
    可变 标准差 为 做 卷积_开方 用 方差。
    若 标准差 等于 0：
        标准差 为 1。
    完。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 n：
        归一 为 (输入 的 第 i 项 减 均值) 乘 卷积精度 除以 标准差。
        若 i 小于 做 计数 用 缩放因子：
            做 追加 用 结果、归一 乘 缩放因子 的 第 i 项 除以 卷积精度 加 偏移因子 的 第 i 项。
        否则：
            做 追加 用 结果、归一。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

残差块_1d 接受 输入、核1、核2：
    中间 为 做 卷积1d_same 用 输入、核1。
    n 为 做 计数 用 中间。
    可变 i 为 0。
    只要 i 小于 n：
        若 中间 的 第 i 项 小于 0：
            中间 的 第 i 项 为 0。
        完。
        i 为 i 加 1。
    完。
    输出 为 做 卷积1d_same 用 中间、核2。
    可变 结果 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 输出：
        可变 v 为 输出 的 第 i 项。
        若 i 小于 做 计数 用 输入：
            v 为 v 加 输入 的 第 i 项。
        完。
        若 v 小于 0：
            v 为 0。
        完。
        做 追加 用 结果、v。
        i 为 i 加 1。
    完。
    返还 结果。
完。

卷积1x1 接受 通道特征、输入通道数、输出通道数、权重：
    可变 结果 为 一组：（空）。
    可变 o 为 0。
    只要 o 小于 输出通道数：
        可变 和 为 0。
        可变 c 为 0。
        只要 c 小于 输入通道数：
            若 c 小于 做 计数 用 通道特征：
                和 为 和 加 通道特征 的 第 c 项 乘 权重 的 第 o 乘 输入通道数 加 c 项 除以 卷积精度。
            完。
            c 为 c 加 1。
        完。
        做 追加 用 结果、和。
        o 为 o 加 1。
    完。
    返还 结果。
完。

笔画核_横竖交 为 一组：0、1、0、1、0、1、0、1、0。

笔画核_撇捺组 为 一组：2、3、2、3。

笔画核_折系列 为 一组：4、4、4。

笔画核_横组 为 一组：0。

笔画卷积_提取特征 接受 笔画序列、模式核：
    若 做 计数 用 笔画序列 小于 做 计数 用 模式核：
        返还 一组：（空）。
    完。
    可变 激活 为 一组：（空）。
    可变 i 为 0。
    只要 i 加 做 计数 用 模式核 小于或等于 做 计数 用 笔画序列：
        可变 得分 为 0。
        可变 j 为 0。
        只要 j 小于 做 计数 用 模式核：
            差 为 笔画序列 的 第 i 加 j 项 减 模式核 的 第 j 项。
            若 差 小于 0：
                差 为 0 减 差。
            完。
            若 差 等于 0：
                得分 为 得分 加 100。
            否则若 差 等于 1：
                得分 为 得分 加 30。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 激活、得分 除以 做 计数 用 模式核。
        i 为 i 加 1。
    完。
    返还 激活。
完。

笔画卷积_最强匹配 接受 笔画序列、模式核：
    激活 为 做 笔画卷积_提取特征 用 笔画序列、模式核。
    若 做 计数 用 激活 等于 0：
        返还 一组：负 1、0。
    完。
    可变 最大激活 为 激活 的 第 0 项。
    可变 最大位置 为 0。
    可变 i 为 1。
    只要 i 小于 做 计数 用 激活：
        若 激活 的 第 i 项 大于 最大激活：
            最大激活 为 激活 的 第 i 项。
            最大位置 为 i。
        完。
        i 为 i 加 1。
    完。
    返还 一组：最大位置、最大激活。
完。

笔画特征向量 接受 笔画序列：
    返还 一组：做 笔画卷积_最强匹配 用 笔画序列、笔画核_横竖交 的 第 1 项、做 笔画卷积_最强匹配 用 笔画序列、笔画核_撇捺组 的 第 1 项、做 笔画卷积_最强匹配 用 笔画序列、笔画核_折系列 的 第 1 项、做 笔画卷积_最强匹配 用 笔画序列、笔画核_横组 的 第 1 项。
完。

本章 收束。
