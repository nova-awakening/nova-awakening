《循环网络》

引用《..·基础》。

引用《.·基础·数学库》。

引用《..·..·运行时·原语桩》。

rnn精度 为 1000。

rnn_创建 接受 输入维、隐藏维、种子：
    可变 权重_输入隐藏 为 一组：（空）。
    可变 权重_隐藏隐藏 为 一组：（空）。
    可变 b_h 为 一组：（空）。
    可变 s 为 种子。
    范围 为 做 rnn_开方 用 6 乘 rnn精度 除以 (输入维 加 隐藏维)。
    可变 i 为 0。
    只要 i 小于 隐藏维 乘 输入维：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        做 追加 用 权重_输入隐藏、s 取余于 (2 乘 范围 加 1) 减 范围。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 隐藏维 乘 隐藏维：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        做 追加 用 权重_隐藏隐藏、s 取余于 (2 乘 范围 加 1) 减 范围。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 隐藏维：
        做 追加 用 b_h、0。
        i 为 i 加 1。
    完。
    返还 一组：隐藏维、输入维、权重_输入隐藏、权重_隐藏隐藏、b_h。
完。

rnn_步进 接受 单元、输入、隐藏：
    h维 为 单元 的 第 0 项。
    x维 为 单元 的 第 1 项。
    权重_输入隐藏 为 单元 的 第 2 项。
    权重_隐藏隐藏 为 单元 的 第 3 项。
    b_h 为 单元 的 第 4 项。
    可变 新h 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 h维：
        可变 和 为 b_h 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 x维：
            和 为 和 加 权重_输入隐藏 的 第 i 乘 x维 加 j 项 乘 输入 的 第 j 项 除以 rnn精度。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 h维：
            和 为 和 加 权重_隐藏隐藏 的 第 i 乘 h维 加 j 项 乘 隐藏 的 第 j 项 除以 rnn精度。
            j 为 j 加 1。
        完。
        做 追加 用 新h、做 循环网络_双曲正切 用 和。
        i 为 i 加 1。
    完。
    返还 新h。
完。

rnn_序列 接受 单元、输入序列：
    h维 为 单元 的 第 0 项。
    可变 h 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 h维：
        做 追加 用 h、0。
        i 为 i 加 1。
    完。
    可变 所有h 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 输入序列：
        h 为 做 rnn_步进 用 单元、输入序列 的 第 i 项、h。
        做 追加 用 所有h、h。
        i 为 i 加 1。
    完。
    返还 一组：h、所有h。
完。

lstm_创建 接受 输入维、隐藏维、种子：
    合维 为 输入维 加 隐藏维。
    范围 为 做 rnn_开方 用 6 乘 rnn精度 除以 合维。
    可变 s 为 种子。
    可变 权重 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 4 乘 隐藏维 乘 合维：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        做 追加 用 权重、s 取余于 (2 乘 范围 加 1) 减 范围。
        i 为 i 加 1。
    完。
    可变 偏置 为 一组：（空）。
    i 为 0。
    只要 i 小于 4 乘 隐藏维：
        若 i 小于 隐藏维：
            做 追加 用 偏置、rnn精度。
        否则：
            做 追加 用 偏置、0。
        完。
        i 为 i 加 1。
    完。
    返还 一组：隐藏维、输入维、权重、偏置。
完。

lstm_步进 接受 单元、输入、隐藏、细胞：
    h维 为 单元 的 第 0 项。
    x维 为 单元 的 第 1 项。
    权重 为 单元 的 第 2 项。
    偏置 为 单元 的 第 3 项。
    合维 为 x维 加 h维。
    可变 拼接 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 h维：
        做 追加 用 拼接、隐藏 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 x维：
        做 追加 用 拼接、输入 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 f门 为 一组：（空）。
    可变 i门 为 一组：（空）。
    可变 c候选 为 一组：（空）。
    可变 o门 为 一组：（空）。
    i 为 0。
    只要 i 小于 h维：
        可变 fv 为 偏置 的 第 i 项。
        可变 iv 为 偏置 的 第 h维 加 i 项。
        可变 cv 为 偏置 的 第 2 乘 h维 加 i 项。
        可变 ov 为 偏置 的 第 3 乘 h维 加 i 项。
        可变 j 为 0。
        只要 j 小于 合维：
            拼值 为 拼接 的 第 j 项。
            fv 为 fv 加 权重 的 第 (0 乘 h维 加 i) 乘 合维 加 j 项 乘 拼值 除以 rnn精度。
            iv 为 iv 加 权重 的 第 (1 乘 h维 加 i) 乘 合维 加 j 项 乘 拼值 除以 rnn精度。
            cv 为 cv 加 权重 的 第 (2 乘 h维 加 i) 乘 合维 加 j 项 乘 拼值 除以 rnn精度。
            ov 为 ov 加 权重 的 第 (3 乘 h维 加 i) 乘 合维 加 j 项 乘 拼值 除以 rnn精度。
            j 为 j 加 1。
        完。
        做 追加 用 f门、做 循环网络_S形 用 fv。
        做 追加 用 i门、做 循环网络_S形 用 iv。
        做 追加 用 c候选、做 循环网络_双曲正切 用 cv。
        做 追加 用 o门、做 循环网络_S形 用 ov。
        i 为 i 加 1。
    完。
    可变 新c 为 一组：（空）。
    i 为 0。
    只要 i 小于 h维：
        做 追加 用 新c、f门 的 第 i 项 乘 细胞 的 第 i 项 除以 rnn精度 加 i门 的 第 i 项 乘 c候选 的 第 i 项 除以 rnn精度。
        i 为 i 加 1。
    完。
    可变 新h 为 一组：（空）。
    i 为 0。
    只要 i 小于 h维：
        做 追加 用 新h、o门 的 第 i 项 乘 做 循环网络_双曲正切 用 新c 的 第 i 项 除以 rnn精度。
        i 为 i 加 1。
    完。
    返还 一组：新h、新c。
完。

lstm_序列 接受 单元、输入序列：
    h维 为 单元 的 第 0 项。
    可变 h 为 一组：（空）。
    可变 c 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 h维：
        做 追加 用 h、0。
        做 追加 用 c、0。
        i 为 i 加 1。
    完。
    可变 所有h 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 输入序列：
        结果 为 做 lstm_步进 用 单元、输入序列 的 第 i 项、h、c。
        h 为 结果 的 第 0 项。
        c 为 结果 的 第 1 项。
        做 追加 用 所有h、h。
        i 为 i 加 1。
    完。
    返还 一组：h、c、所有h。
完。

gru_创建 接受 输入维、隐藏维、种子：
    合维 为 输入维 加 隐藏维。
    范围 为 做 rnn_开方 用 6 乘 rnn精度 除以 合维。
    可变 s 为 种子。
    可变 权重 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 3 乘 隐藏维 乘 合维：
        s 为 s 乘 1103515245 加 12345 位与 0x7FFFFFFF。
        做 追加 用 权重、s 取余于 (2 乘 范围 加 1) 减 范围。
        i 为 i 加 1。
    完。
    可变 偏置 为 一组：（空）。
    i 为 0。
    只要 i 小于 3 乘 隐藏维：
        做 追加 用 偏置、0。
        i 为 i 加 1。
    完。
    返还 一组：隐藏维、输入维、权重、偏置。
完。

gru_步进 接受 单元、输入、隐藏：
    h维 为 单元 的 第 0 项。
    x维 为 单元 的 第 1 项。
    合维 为 x维 加 h维。
    可变 拼接 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 h维：
        做 追加 用 拼接、隐藏 的 第 i 项。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 x维：
        做 追加 用 拼接、输入 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 z门 为 一组：（空）。
    可变 r门 为 一组：（空）。
    i 为 0。
    只要 i 小于 h维：
        可变 zv 为 单元 的 第 3 项 的 第 i 项。
        可变 rv 为 单元 的 第 3 项 的 第 h维 加 i 项。
        可变 j 为 0。
        只要 j 小于 合维：
            zv 为 zv 加 单元 的 第 2 项 的 第 (0 乘 h维 加 i) 乘 合维 加 j 项 乘 拼接 的 第 j 项 除以 rnn精度。
            rv 为 rv 加 单元 的 第 2 项 的 第 (1 乘 h维 加 i) 乘 合维 加 j 项 乘 拼接 的 第 j 项 除以 rnn精度。
            j 为 j 加 1。
        完。
        做 追加 用 z门、做 循环网络_S形 用 zv。
        做 追加 用 r门、做 循环网络_S形 用 rv。
        i 为 i 加 1。
    完。
    可变 拼接2 为 一组：（空）。
    i 为 0。
    只要 i 小于 h维：
        做 追加 用 拼接2、r门 的 第 i 项 乘 隐藏 的 第 i 项 除以 rnn精度。
        i 为 i 加 1。
    完。
    i 为 0。
    只要 i 小于 x维：
        做 追加 用 拼接2、输入 的 第 i 项。
        i 为 i 加 1。
    完。
    可变 候选 为 一组：（空）。
    i 为 0。
    只要 i 小于 h维：
        可变 hv 为 单元 的 第 3 项 的 第 2 乘 h维 加 i 项。
        可变 j2 为 0。
        只要 j2 小于 合维：
            hv 为 hv 加 单元 的 第 2 项 的 第 (2 乘 h维 加 i) 乘 合维 加 j2 项 乘 拼接2 的 第 j2 项 除以 rnn精度。
            j2 为 j2 加 1。
        完。
        做 追加 用 候选、做 循环网络_双曲正切 用 hv。
        i 为 i 加 1。
    完。
    可变 新h 为 一组：（空）。
    i 为 0。
    只要 i 小于 h维：
        做 追加 用 新h、(rnn精度 减 z门 的 第 i 项) 乘 隐藏 的 第 i 项 除以 rnn精度 加 z门 的 第 i 项 乘 候选 的 第 i 项 除以 rnn精度。
        i 为 i 加 1。
    完。
    返还 新h。
完。

双向rnn_序列 接受 正向单元、反向单元、输入序列：
    h维 为 正向单元 的 第 0 项。
    序列长 为 做 计数 用 输入序列。
    可变 正h 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 h维：
        做 追加 用 正h、0。
        i 为 i 加 1。
    完。
    可变 正向输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 序列长：
        正h 为 做 rnn_步进 用 正向单元、输入序列 的 第 i 项、正h。
        做 追加 用 正向输出、正h。
        i 为 i 加 1。
    完。
    可变 反h 为 一组：（空）。
    i 为 0。
    只要 i 小于 h维：
        做 追加 用 反h、0。
        i 为 i 加 1。
    完。
    可变 反向输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 序列长：
        做 追加 用 反向输出、反h。
        i 为 i 加 1。
    完。
    i 为 序列长 减 1。
    只要 i 大于或等于 0：
        反h 为 做 rnn_步进 用 反向单元、输入序列 的 第 i 项、反h。
        反向输出 的 第 i 项 为 反h。
        i 为 i 减 1。
    完。
    可变 拼接输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 序列长：
        可变 拼 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 h维：
            做 追加 用 拼、正向输出 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 h维：
            做 追加 用 拼、反向输出 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 拼接输出、拼。
        i 为 i 加 1。
    完。
    返还 拼接输出。
完。

双向lstm_序列 接受 正向单元、反向单元、输入序列：
    h维 为 正向单元 的 第 0 项。
    序列长 为 做 计数 用 输入序列。
    可变 正h 为 一组：（空）。
    可变 正c 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 h维：
        做 追加 用 正h、0。
        做 追加 用 正c、0。
        i 为 i 加 1。
    完。
    可变 正向输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 序列长：
        r 为 做 lstm_步进 用 正向单元、输入序列 的 第 i 项、正h、正c。
        正h 为 r 的 第 0 项。
        正c 为 r 的 第 1 项。
        做 追加 用 正向输出、正h。
        i 为 i 加 1。
    完。
    可变 反h 为 一组：（空）。
    可变 反c 为 一组：（空）。
    i 为 0。
    只要 i 小于 h维：
        做 追加 用 反h、0。
        做 追加 用 反c、0。
        i 为 i 加 1。
    完。
    可变 反向输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 序列长：
        做 追加 用 反向输出、反h。
        i 为 i 加 1。
    完。
    i 为 序列长 减 1。
    只要 i 大于或等于 0：
        r2 为 做 lstm_步进 用 反向单元、输入序列 的 第 i 项、反h、反c。
        反h 为 r2 的 第 0 项。
        反c 为 r2 的 第 1 项。
        反向输出 的 第 i 项 为 反h。
        i 为 i 减 1。
    完。
    可变 拼接输出 为 一组：（空）。
    i 为 0。
    只要 i 小于 序列长：
        可变 拼 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 h维：
            做 追加 用 拼、正向输出 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 h维：
            做 追加 用 拼、反向输出 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        做 追加 用 拼接输出、拼。
        i 为 i 加 1。
    完。
    返还 拼接输出。
完。

深度lstm_序列 接受 层列表、输入序列：
    可变 当前输入 为 输入序列。
    可变 层idx 为 0。
    可变 最终h 为 一组：（空）。
    只要 层idx 小于 做 计数 用 层列表：
        结果 为 做 lstm_序列 用 层列表 的 第 层idx 项、当前输入。
        最终h 为 结果 的 第 0 项。
        当前输入 为 结果 的 第 2 项。
        层idx 为 层idx 加 1。
    完。
    返还 一组：最终h、当前输入。
完。

gru_序列 接受 单元、输入序列：
    h维 为 单元 的 第 0 项。
    可变 h 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 h维：
        做 追加 用 h、0。
        i 为 i 加 1。
    完。
    可变 所有h 为 一组：（空）。
    i 为 0。
    只要 i 小于 做 计数 用 输入序列：
        h 为 做 gru_步进 用 单元、输入序列 的 第 i 项、h。
        做 追加 用 所有h、h。
        i 为 i 加 1。
    完。
    返还 一组：h、所有h。
完。

seq2seq_编码 接受 编码器、输入序列：
    结果 为 做 lstm_序列 用 编码器、输入序列。
    返还 一组：结果 的 第 0 项、结果 的 第 1 项。
完。

seq2seq_解码 接受 解码器、上下文h、上下文c、输出长度、起始输入：
    _h维 为 解码器 的 第 0 项。
    可变 h 为 上下文h。
    可变 c 为 上下文c。
    可变 输入 为 起始输入。
    可变 输出序列 为 一组：（空）。
    可变 t 为 0。
    只要 t 小于 输出长度：
        结果 为 做 lstm_步进 用 解码器、输入、h、c。
        h 为 结果 的 第 0 项。
        c 为 结果 的 第 1 项。
        做 追加 用 输出序列、h。
        输入 为 h。
        t 为 t 加 1。
    完。
    返还 输出序列。
完。

序列掩码 接受 输出序列、实际长度：
    序列长 为 做 计数 用 输出序列。
    可变 i 为 实际长度。
    只要 i 小于 序列长：
        h维 为 做 计数 用 输出序列 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 h维：
            输出序列 的 第 i 项 的 第 j 项 为 0。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
完。

注意力_计算 接受 编码器输出、解码器隐藏：
    T 为 做 计数 用 编码器输出。
    h维 为 做 计数 用 解码器隐藏。
    可变 分数 为 一组：（空）。
    可变 t 为 0。
    只要 t 小于 T：
        可变 和 为 0。
        可变 j 为 0。
        只要 j 小于 h维：
            若 j 小于 做 计数 用 编码器输出 的 第 t 项：
                和 为 和 加 解码器隐藏 的 第 j 项 乘 编码器输出 的 第 t 项 的 第 j 项 除以 rnn精度。
            完。
            j 为 j 加 1。
        完。
        做 追加 用 分数、和。
        t 为 t 加 1。
    完。
    可变 最大 为 分数 的 第 0 项。
    t 为 1。
    只要 t 小于 T：
        若 分数 的 第 t 项 大于 最大：
            最大 为 分数 的 第 t 项。
        完。
        t 为 t 加 1。
    完。
    可变 指数 为 一组：（空）。
    可变 总和 为 0。
    t 为 0。
    只要 t 小于 T：
        可变 e 为 rnn精度 加 (分数 的 第 t 项 减 最大)。
        若 e 小于 1：
            e 为 1。
        完。
        做 追加 用 指数、e。
        总和 为 总和 加 e。
        t 为 t 加 1。
    完。
    可变 权重 为 一组：（空）。
    t 为 0。
    只要 t 小于 T：
        做 追加 用 权重、指数 的 第 t 项 乘 rnn精度 除以 总和。
        t 为 t 加 1。
    完。
    可变 上下文 为 一组：（空）。
    可变 j2 为 0。
    只要 j2 小于 h维：
        做 追加 用 上下文、0。
        j2 为 j2 加 1。
    完。
    t 为 0。
    只要 t 小于 T：
        j2 为 0。
        只要 j2 小于 h维：
            若 j2 小于 做 计数 用 编码器输出 的 第 t 项：
                上下文 的 第 j2 项 为 上下文 的 第 j2 项 加 权重 的 第 t 项 乘 编码器输出 的 第 t 项 的 第 j2 项 除以 rnn精度。
            完。
            j2 为 j2 加 1。
        完。
        t 为 t 加 1。
    完。
    返还 一组：上下文、权重。
完。

teacher_forcing_步 接受 解码器、真实序列、编码器上下文h、编码器上下文c：
    T 为 做 计数 用 真实序列。
    可变 h 为 编码器上下文h。
    可变 c 为 编码器上下文c。
    可变 输出 为 一组：（空）。
    可变 t 为 0。
    只要 t 小于 T：
        结果 为 做 lstm_步进 用 解码器、真实序列 的 第 t 项、h、c。
        h 为 结果 的 第 0 项。
        c 为 结果 的 第 1 项。
        做 追加 用 输出、h。
        t 为 t 加 1。
    完。
    返还 输出。
完。

束搜索 接受 解码器、初始h、初始c、起始输入、beam宽、最大长度、评分函数：
    可变 候选 为 一组：一组：一组：起始输入、初始h、初始c、0。
    可变 步 为 0。
    只要 步 小于 最大长度：
        可变 新候选 为 一组：（空）。
        可变 b 为 0。
        只要 b 小于 做 计数 用 候选：
            序列 为 候选 的 第 b 项 的 第 0 项。
            h 为 候选 的 第 b 项 的 第 1 项。
            c 为 候选 的 第 b 项 的 第 2 项。
            分 为 候选 的 第 b 项 的 第 3 项。
            若 做 计数 用 序列 等于 0：
                返还 空。
            完。
            输入 为 序列 的 第 做 计数 用 序列 减 1 项。
            结果 为 做 lstm_步进 用 解码器、输入、h、c。
            新h 为 结果 的 第 0 项。
            新c 为 结果 的 第 1 项。
            token分数 为 做 评分函数 用 新h。
            可变 新seq 为 一组：（空）。
            可变 si 为 0。
            只要 si 小于 做 计数 用 序列：
                做 追加 用 新seq、序列 的 第 si 项。
                si 为 si 加 1。
            完。
            做 追加 用 新seq、新h。
            做 追加 用 新候选、一组：新seq、新h、新c、分 加 token分数。
            b 为 b 加 1。
        完。
        可变 si2 为 0。
        只要 si2 小于 做 计数 用 新候选：
            可变 sj 为 si2 加 1。
            只要 sj 小于 做 计数 用 新候选：
                若 新候选 的 第 sj 项 的 第 3 项 大于 新候选 的 第 si2 项 的 第 3 项：
                    t 为 新候选 的 第 si2 项。
                    新候选 的 第 si2 项 为 新候选 的 第 sj 项。
                    新候选 的 第 sj 项 为 t。
                完。
                sj 为 sj 加 1。
            完。
            si2 为 si2 加 1。
        完。
        候选 为 一组：（空）。
        可变 取 为 0。
        只要 取 小于 beam宽 并且 取 小于 做 计数 用 新候选：
            做 追加 用 候选、新候选 的 第 取 项。
            取 为 取 加 1。
        完。
        步 为 步 加 1。
    完。
    若 做 计数 用 候选 大于 0：
        返还 候选 的 第 0 项 的 第 0 项。
    完。
    返还 一组：（空）。
完。

peephole_lstm_步进 接受 单元、输入、h、c：
    h维 为 单元 的 第 0 项。
    x维 为 单元 的 第 1 项。
    权重_输入隐藏 为 单元 的 第 2 项。
    权重_隐藏隐藏 为 单元 的 第 3 项。
    b 为 单元 的 第 4 项。
    可变 新h 为 一组：（空）。
    可变 新c 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 h维：
        可变 ig 为 b 的 第 i 项。
        可变 j 为 0。
        只要 j 小于 x维：
            ig 为 ig 加 权重_输入隐藏 的 第 i 乘 x维 加 j 项 乘 输入 的 第 j 项 除以 rnn精度。
            j 为 j 加 1。
        完。
        j 为 0。
        只要 j 小于 h维：
            ig 为 ig 加 权重_隐藏隐藏 的 第 i 乘 h维 加 j 项 乘 h 的 第 j 项 除以 rnn精度。
            j 为 j 加 1。
        完。
        ig 为 ig 加 c 的 第 i 项 除以 4。
        ig 为 做 循环网络_S形 用 ig。
        可变 fg 为 rnn精度 减 ig 加 c 的 第 i 项 除以 8。
        若 fg 小于 0：
            fg 为 0。
        完。
        若 fg 大于 rnn精度：
            fg 为 rnn精度。
        完。
        可变 cand 为 0。
        j 为 0。
        只要 j 小于 x维：
            cand 为 cand 加 权重_输入隐藏 的 第 (h维 加 i) 乘 x维 加 j 项 乘 输入 的 第 j 项 除以 rnn精度。
            j 为 j 加 1。
        完。
        cand 为 做 循环网络_双曲正切 用 cand。
        nc 为 fg 乘 c 的 第 i 项 除以 rnn精度 加 ig 乘 cand 除以 rnn精度。
        做 追加 用 新c、nc。
        做 追加 用 新h、做 循环网络_双曲正切 用 nc。
        i 为 i 加 1。
    完。
    返还 一组：新h、新c。
完。

循环网络_S形 接受 x：
    若 x 小于 0 减 5 乘 rnn精度：
        返还 0。
    完。
    若 x 大于或等于 5 乘 rnn精度：
        返还 rnn精度。
    完。
    若 x 小于 0：
        返还 rnn精度 除以 2 加 x 乘 rnn精度 除以 (4 乘 5 乘 rnn精度)。
    完。
    返还 rnn精度 除以 2 加 x 乘 rnn精度 除以 (4 乘 5 乘 rnn精度)。
完。

循环网络_双曲正切 接受 x：
    返还 2 乘 做 循环网络_S形 用 2 乘 x 减 rnn精度。
完。

rnn_开方 接受 n：
    返还 做 开方 用 n。
完。

门控循环_单元 接受 x、前隐藏、wz、wr、wh、bz、br、bh：
    维度 为 做 计数 用 前隐藏。
    可变 新隐藏 为 一组：（空）。
    可变 索引 为 0。
    只要 索引 小于 维度：
        组合值 为 前隐藏 的 第 索引 项 加 x 的 第 索引 项。
        z 为 做 _S形 用 组合值 乘 wz 除以 1000 加 bz。
        r 为 做 _S形 用 组合值 乘 wr 除以 1000 加 br。
        r_h 为 r 乘 前隐藏 的 第 索引 项 除以 1000。
        ht 为 做 _双曲正切 用 (r_h 加 x 的 第 索引 项) 乘 wh 除以 1000 加 bh。
        隐藏值 为 (1000 减 z) 乘 前隐藏 的 第 索引 项 除以 1000 加 z 乘 ht 除以 1000。
        做 追加 用 新隐藏、隐藏值。
        索引 为 索引 加 1。
    完。
    返还 新隐藏。
完。

长短记忆_窥视孔 接受 x、前隐藏、c_prev、wf、wi、wo、wc、pf、pi、po、bf、bi、bo、bc：
    维度 为 做 计数 用 前隐藏。
    可变 新隐藏 为 一组：（空）。
    可变 c_new 为 一组：（空）。
    可变 索引 为 0。
    只要 索引 小于 维度：
        组合值 为 前隐藏 的 第 索引 项 加 x 的 第 索引 项。
        f 为 做 _S形 用 组合值 乘 wf 除以 1000 加 c_prev 的 第 索引 项 乘 pf 除以 1000 加 bf。
        ig 为 做 _S形 用 组合值 乘 wi 除以 1000 加 c_prev 的 第 索引 项 乘 pi 除以 1000 加 bi。
        ct 为 做 _双曲正切 用 组合值 乘 wc 除以 1000 加 bc。
        c_val 为 f 乘 c_prev 的 第 索引 项 除以 1000 加 ig 乘 ct 除以 1000。
        o 为 做 _S形 用 组合值 乘 wo 除以 1000 加 c_val 乘 po 除以 1000 加 bo。
        隐藏值 为 o 乘 做 _双曲正切 用 c_val 除以 1000。
        做 追加 用 c_new、c_val。
        做 追加 用 新隐藏、隐藏值。
        索引 为 索引 加 1。
    完。
    返还 一组：新隐藏、c_new。
完。

长短记忆_单元 接受 x、前隐藏、c_prev、wf、wi、wo、wc、bf、bi、bo、bc：
    维度 为 做 计数 用 前隐藏。
    可变 新隐藏 为 一组：（空）。
    可变 c_new 为 一组：（空）。
    可变 索引 为 0。
    只要 索引 小于 维度：
        组合值 为 前隐藏 的 第 索引 项 加 x 的 第 索引 项。
        f 为 做 _S形 用 组合值 乘 wf 除以 1000 加 bf。
        ig 为 做 _S形 用 组合值 乘 wi 除以 1000 加 bi。
        o 为 做 _S形 用 组合值 乘 wo 除以 1000 加 bo。
        ct 为 做 _双曲正切 用 组合值 乘 wc 除以 1000 加 bc。
        c_val 为 f 乘 c_prev 的 第 索引 项 除以 1000 加 ig 乘 ct 除以 1000。
        隐藏值 为 o 乘 做 _双曲正切 用 c_val 除以 1000。
        做 追加 用 c_new、c_val。
        做 追加 用 新隐藏、隐藏值。
        索引 为 索引 加 1。
    完。
    返还 一组：新隐藏、c_new。
完。

rnn_权重序列 接受 输入序列、维度、_w_h、_w_x、_b：
    单元 为 做 rnn_创建 用 维度、维度、42。
    返还 做 rnn_序列 用 单元、输入序列。
完。

gru_权重序列 接受 输入序列、维度、_wz、_wr、_wh、_bz、_br、_bh：
    单元 为 做 gru_创建 用 维度、维度、42。
    返还 做 gru_序列 用 单元、输入序列。
完。

lstm_权重序列 接受 输入序列、维度、_wf、_wi、_wo、_wc、_bf、_bi、_bo、_bc：
    单元 为 做 lstm_创建 用 维度、维度、42。
    返还 做 lstm_序列 用 单元、输入序列。
完。

RNN_单元 接受 x、前隐藏、w_h、w_x、b：
    可变 新隐藏 为 一组：（空）。
    维度 为 做 计数 用 前隐藏。
    可变 i 为 0。
    只要 i 小于 维度：
        数值 为 前隐藏 的 第 i 项 乘 w_h 除以 1000 加 x 的 第 i 项 乘 w_x 除以 1000 加 b。
        做 追加 用 新隐藏、做 _双曲正切 用 数值。
        i 为 i 加 1。
    完。
    返还 新隐藏。
完。

RNN_多层 接受 输入序列、维度、n_layers、w_h、w_x、b：
    可变 当前输入 为 输入序列。
    可变 layer 为 0。
    只要 layer 小于 n_layers：
        当前输入 为 做 rnn_权重序列 用 当前输入、维度、w_h、w_x、b。
        layer 为 layer 加 1。
    完。
    返还 当前输入。
完。

_S形 接受 x：
    若 x 大于 5000：
        返还 998。
    完。
    若 x 小于 负 5000：
        返还 2。
    完。
    若 做 绝对值 用 x 小于 2000：
        返还 500 加 x 乘 200 除以 1000。
    完。
    若 x 大于 0：
        返还 900 加 (x 减 2000) 乘 98 除以 3000。
    完。
    返还 100 加 (x 加 2000) 乘 98 除以 3000。
完。

_双曲正切 接受 x：
    若 x 大于 3000：
        返还 1000。
    完。
    若 x 小于 负 3000：
        返还 负 1000。
    完。
    若 做 绝对值 用 x 小于 500：
        返还 x 减 x 乘 x 乘 x 除以 3000000。
    完。
    若 x 大于 0：
        返还 700 加 (x 减 500) 乘 300 除以 2500。
    完。
    返还 负 700 加 (x 加 500) 乘 300 除以 2500。
完。

双向_门控循环 接受 输入序列、维度、wz、wr、wh、bz、br、bh：
    前向 为 做 gru_权重序列 用 输入序列、维度、wz、wr、wh、bz、br、bh。
    后向 为 做 gru_权重序列 用 做 反转序列 用 输入序列、维度、wz、wr、wh、bz、br、bh。
    返还 一组：前向、后向。
完。

双向_长短记忆 接受 输入序列、维度、wf、wi、wo、wc、bf、bi、bo、bc：
    前向 为 做 lstm_权重序列 用 输入序列、维度、wf、wi、wo、wc、bf、bi、bo、bc。
    后向 为 做 lstm_权重序列 用 做 反转序列 用 输入序列、维度、wf、wi、wo、wc、bf、bi、bo、bc。
    返还 一组：前向、后向。
完。

双向_RNN 接受 输入序列、维度、w_h、w_x、b：
    前向 为 做 rnn_权重序列 用 输入序列、维度、w_h、w_x、b。
    后向 为 做 rnn_权重序列 用 做 反转序列 用 输入序列、维度、w_h、w_x、b。
    返还 一组：前向、后向。
完。

反转序列 接受 输入序列：
    可变 反转 为 一组：（空）。
    可变 i 为 做 计数 用 输入序列 减 1。
    只要 i 大于或等于 0：
        做 追加 用 反转、输入序列 的 第 i 项。
        i 为 i 减 1。
    完。
    返还 反转。
完。

取最终隐态 接受 输出序列：
    n 为 做 计数 用 输出序列。
    若 n 等于 0：
        返还 一组：（空）。
    完。
    返还 输出序列 的 第 n 减 1 项。
完。

截断隐态 接受 h、max_val：
    可变 结果 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 做 计数 用 h：
        v 为 h 的 第 i 项。
        下界 为 0 减 max_val。
        可变 截断值 为 做 最大值 用 v、下界。
        截断值 为 做 最小值 用 截断值、max_val。
        做 追加 用 结果、截断值。
        i 为 i 加 1。
    完。
    返还 结果。
完。

隐态_维度 接受 h：
    返还 做 计数 用 h。
完。

零向量 接受 维度：
    可变 v 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 维度：
        做 追加 用 v、0。
        i 为 i 加 1。
    完。
    返还 v。
完。

本章 收束。
