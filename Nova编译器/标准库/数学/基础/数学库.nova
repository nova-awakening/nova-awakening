《数学库》

引用《..·..·基础》。

引用《..·数论》。

引用《..·矩阵》。

引用《..·工具》。

数学_精度 为 1000。

数学_PI 为 3142。

数学_E 为 2718。

数学_PI2 为 6283。

数学_半PI 为 1571。

绝对值 接受 n：
    若 n 小于 0：
        返还 0 减 n。
    完。
    返还 n。
完。

最大值 接受 a、b：
    若 a 大于 b：
        返还 a。
    完。
    返还 b。
完。

最小值 接受 a、b：
    若 a 小于 b：
        返还 a。
    完。
    返还 b。
完。

钳制 接受 值、下限、上限：
    若 值 小于 下限：
        返还 下限。
    完。
    若 值 大于 上限：
        返还 上限。
    完。
    返还 值。
完。

符号 接受 n：
    若 n 大于 0：
        返还 1。
    完。
    若 n 小于 0：
        返还 负 1。
    完。
    返还 0。
完。

平方根 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    可变 x 为 n。
    可变 y 为 (x 加 1) 除以 2。
    只要 y 小于 x：
        x 为 y。
        y 为 (x 加 n 除以 x) 除以 2。
    完。
    返还 x。
完。

定点平方根 接受 n：
    若 n 小于或等于 0：
        返还 0。
    完。
    返还 做 平方根 用 n 乘 数学_精度。
完。

数学库_幂 接受 底数、指数：
    若 指数 等于 0：
        返还 1。
    完。
    若 指数 小于 0：
        返还 0。
    完。
    可变 结果 为 1。
    可变 b 为 底数。
    可变 e 为 指数。
    只要 e 大于 0：
        若 e 取余于 2 等于 1：
            结果 为 结果 乘 b。
        完。
        b 为 b 乘 b。
        e 为 e 除以 2。
    完。
    返还 结果。
完。

定点幂 接受 底数、指数：
    若 指数 等于 0：
        返还 数学_精度。
    完。
    若 底数 等于 0：
        返还 0。
    完。
    若 底数 等于 数学_精度：
        返还 数学_精度。
    完。
    ln_b 为 做 定点_自然对数 用 底数。
    返还 做 定点_指数 用 指数 乘 ln_b 除以 数学_精度。
完。

角度_归一化 接受 角度：
    可变 a 为 角度 取余于 数学_PI2。
    若 a 大于 数学_PI：
        a 为 a 减 数学_PI2。
    完。
    若 a 小于 0 减 数学_PI：
        a 为 a 加 数学_PI2。
    完。
    返还 a。
完。

正弦 接受 角度：
    x 为 做 角度_归一化 用 角度。
    x2 为 x 乘 x 除以 数学_精度。
    可变 项 为 x。
    可变 结果 为 x。
    项 为 0 减 项 乘 x2 除以 数学_精度 除以 6。
    结果 为 结果 加 项。
    项 为 0 减 项 乘 x2 除以 数学_精度 除以 20。
    结果 为 结果 加 项。
    项 为 0 减 项 乘 x2 除以 数学_精度 除以 42。
    结果 为 结果 加 项。
    项 为 0 减 项 乘 x2 除以 数学_精度 除以 72。
    结果 为 结果 加 项。
    返还 结果。
完。

余弦 接受 角度：
    返还 做 正弦 用 角度 加 数学_半PI。
完。

正切 接受 角度：
    c 为 做 余弦 用 角度。
    若 c 等于 0：
        返还 0x7FFFFFFF。
    完。
    返还 做 正弦 用 角度 乘 数学_精度 除以 c。
完。

反正切 接受 x：
    可变 归约 为 假。
    可变 负 为 假。
    可变 v 为 x。
    若 v 小于 0：
        负 为 真。
        v 为 0 减 v。
    完。
    若 v 大于 数学_精度：
        归约 为 真。
        v 为 数学_精度 乘 数学_精度 除以 v。
    完。
    v2 为 v 乘 v 除以 数学_精度。
    可变 结果 为 v。
    可变 项 为 v 乘 v2 除以 数学_精度。
    结果 为 结果 减 项 除以 3。
    项 为 项 乘 v2 除以 数学_精度。
    结果 为 结果 加 项 除以 5。
    项 为 项 乘 v2 除以 数学_精度。
    结果 为 结果 减 项 除以 7。
    项 为 项 乘 v2 除以 数学_精度。
    结果 为 结果 加 项 除以 9。
    若 归约：
        结果 为 数学_半PI 减 结果。
    完。
    若 负：
        结果 为 0 减 结果。
    完。
    返还 结果。
完。

反正切2 接受 y、x：
    若 x 大于 0：
        返还 做 反正切 用 y 乘 数学_精度 除以 x。
    完。
    若 x 小于 0 并且 y 大于或等于 0：
        返还 做 反正切 用 y 乘 数学_精度 除以 x 加 数学_PI。
    完。
    若 x 小于 0 并且 y 小于 0：
        返还 做 反正切 用 y 乘 数学_精度 除以 x 减 数学_PI。
    完。
    若 x 等于 0 并且 y 大于 0：
        返还 数学_半PI。
    完。
    若 x 等于 0 并且 y 小于 0：
        返还 0 减 数学_半PI。
    完。
    返还 0。
完。

反正弦 接受 x：
    若 x 大于或等于 数学_精度：
        返还 数学_半PI。
    完。
    若 x 小于或等于 0 减 数学_精度：
        返还 0 减 数学_半PI。
    完。
    分母 为 做 定点平方根 用 数学_精度 乘 数学_精度 减 x 乘 x。
    若 分母 等于 0：
        返还 数学_半PI。
    完。
    返还 做 反正切 用 x 乘 数学_精度 除以 分母。
完。

反余弦 接受 x：
    返还 数学_半PI 减 做 反正弦 用 x。
完。

定点_指数 接受 x：
    可变 结果 为 数学_精度。
    可变 项 为 数学_精度。
    可变 i 为 1。
    只要 i 小于或等于 15：
        项 为 项 乘 x 除以 (i 乘 数学_精度)。
        结果 为 结果 加 项。
        若 项 等于 0：
            i 为 16。
        否则：
            i 为 i 加 1。
        完。
    完。
    返还 结果。
完。

定点_自然对数 接受 x：
    若 x 小于或等于 0：
        返还 0 减 0x7FFFFFFF。
    完。
    若 x 等于 数学_精度：
        返还 0。
    完。
    可变 k 为 0。
    可变 v 为 x。
    只要 v 大于 2000：
        v 为 v 除以 2。
        k 为 k 加 1。
    完。
    只要 v 小于 500：
        v 为 v 乘 2。
        k 为 k 减 1。
    完。
    t 为 (v 减 数学_精度) 乘 数学_精度 除以 (v 加 数学_精度)。
    t2 为 t 乘 t 除以 数学_精度。
    可变 结果 为 t。
    可变 幂 为 t 乘 t2 除以 数学_精度。
    结果 为 结果 加 幂 除以 3。
    幂 为 幂 乘 t2 除以 数学_精度。
    结果 为 结果 加 幂 除以 5。
    幂 为 幂 乘 t2 除以 数学_精度。
    结果 为 结果 加 幂 除以 7。
    幂 为 幂 乘 t2 除以 数学_精度。
    结果 为 结果 加 幂 除以 9。
    结果 为 结果 乘 2。
    返还 结果 加 k 乘 693。
完。

定点_常用对数 接受 x：
    返还 做 定点_自然对数 用 x 乘 数学_精度 除以 2303。
完。

定点_二进制对数 接受 x：
    返还 做 定点_自然对数 用 x 乘 数学_精度 除以 693。
完。

下取整 接受 x：
    返还 x 除以 数学_精度 乘 数学_精度。
完。

上取整 接受 x：
    若 x 取余于 数学_精度 等于 0：
        返还 x。
    完。
    若 x 大于 0：
        返还 (x 除以 数学_精度 加 1) 乘 数学_精度。
    完。
    返还 x 除以 数学_精度 乘 数学_精度。
完。

四舍五入 接受 x：
    若 x 大于或等于 0：
        返还 (x 加 数学_精度 除以 2) 除以 数学_精度 乘 数学_精度。
    完。
    返还 (x 减 数学_精度 除以 2) 除以 数学_精度 乘 数学_精度。
完。

整除 接受 a、b：
    返还 a 除以 b。
完。

欧几里得模 接受 a、b：
    可变 r 为 a 取余于 b。
    若 r 小于 0：
        r 为 r 加 做 绝对值 用 b。
    完。
    返还 r。
完。

数学库_最大公约数 接受 a、b：
    可变 x 为 做 绝对值 用 a。
    可变 y 为 做 绝对值 用 b。
    只要 y 不等于 0：
        临 为 y。
        y 为 x 取余于 y。
        x 为 临。
    完。
    返还 x。
完。

数学库_最小公倍数 接受 a、b：
    若 a 等于 0 或者 b 等于 0：
        返还 0。
    完。
    返还 做 绝对值 用 a 除以 做 最大公约数 用 a、b 乘 做 绝对值 用 b。
完。

数学库_阶乘 接受 n：
    若 n 小于或等于 1：
        返还 1。
    完。
    可变 r 为 1。
    可变 i 为 2。
    只要 i 小于或等于 n：
        r 为 r 乘 i。
        i 为 i 加 1。
    完。
    返还 r。
完。

组合数 接受 n、k：
    若 k 大于 n 或者 k 小于 0：
        返还 0。
    完。
    若 k 等于 0 或者 k 等于 n：
        返还 1。
    完。
    可变 kk 为 k。
    若 kk 大于 n 减 kk：
        kk 为 n 减 kk。
    完。
    可变 r 为 1。
    可变 i 为 0。
    只要 i 小于 kk：
        r 为 r 乘 (n 减 i) 除以 (i 加 1)。
        i 为 i 加 1。
    完。
    返还 r。
完。

向量2 是 一种 事物，含 x、y。

向量加 接受 v1、v2：
    返还 建。
    做 向量2 用 v1 的 x 加 v2 的 x、v1 的 y 加 v2 的 y。
完。

向量减 接受 v1、v2：
    返还 建。
    做 向量2 用 v1 的 x 减 v2 的 x、v1 的 y 减 v2 的 y。
完。

向量缩放 接受 v、s：
    返还 建。
    做 向量2 用 v 的 x 乘 s 除以 数学_精度、v 的 y 乘 s 除以 数学_精度。
完。

向量点积 接受 v1、v2：
    返还 v1 的 x 乘 v2 的 x 除以 数学_精度 加 v1 的 y 乘 v2 的 y 除以 数学_精度。
完。

向量叉积 接受 v1、v2：
    返还 v1 的 x 乘 v2 的 y 除以 数学_精度 减 v1 的 y 乘 v2 的 x 除以 数学_精度。
完。

向量长度 接受 v：
    返还 做 定点平方根 用 v 的 x 乘 v 的 x 除以 数学_精度 加 v 的 y 乘 v 的 y 除以 数学_精度。
完。

向量归一化 接受 v：
    长度 为 做 向量长度 用 v。
    若 长度 等于 0：
        返还 建。
        做 向量2。
    完。
    返还 建。
    做 向量2 用 v 的 x 乘 数学_精度 除以 长度、v 的 y 乘 数学_精度 除以 长度。
完。

向量距离 接受 v1、v2：
    返还 做 向量长度 用 做 向量减 用 v1、v2。
完。

向量旋转 接受 v、角度：
    c 为 做 余弦 用 角度。
    s 为 做 正弦 用 角度。
    返还 建。
    做 向量2 用 v 的 x 乘 c 除以 数学_精度 减 v 的 y 乘 s 除以 数学_精度、v 的 x 乘 s 除以 数学_精度 加 v 的 y 乘 c 除以 数学_精度。
完。

向量线性插值 接受 v1、v2、t：
    返还 建。
    做 向量2 用 v1 的 x 加 (v2 的 x 减 v1 的 x) 乘 t 除以 数学_精度、v1 的 y 加 (v2 的 y 减 v1 的 y) 乘 t 除以 数学_精度。
完。

数学库_矩阵_创建 接受 行、列：
    可变 m 为 一组：（空）。
    可变 i 为 0。
    只要 i 小于 行：
        可变 行数据 为 一组：（空）。
        可变 j 为 0。
        只要 j 小于 列：
            做 追加 用 行数据、0。
            j 为 j 加 1。
        完。
        做 追加 用 m、行数据。
        i 为 i 加 1。
    完。
    返还 m。
完。

数学库_矩阵_单位 接受 n：
    可变 m 为 做 数学库_矩阵_创建 用 n、n。
    可变 i 为 0。
    只要 i 小于 n：
        m 的 第 i 项 的 第 i 项 为 数学_精度。
        i 为 i 加 1。
    完。
    返还 m。
完。

数学_块_大小 为 8。

矩阵_乘 接受 A、B、C、m、n、k：
    可变 ii 为 0。
    若 A 等于 0 或者 B 等于 0 或者 C 等于 0：
        返还。
    完。
    只要 ii 小于 m：
        可变 jj 为 0。
        只要 jj 小于 n：
            可变 kk 为 0。
            只要 kk 小于 k：
                行末 为 ii 加 数学_块_大小。
                若 行末 大于 m：
                    行末 为 m。
                完。
                列末 为 jj 加 数学_块_大小。
                若 列末 大于 n：
                    列末 为 n。
                完。
                层末 为 kk 加 数学_块_大小。
                若 层末 大于 k：
                    层末 为 k。
                完。
                可变 i 为 ii。
                只要 i 小于 行末：
                    可变 j 为 jj。
                    只要 j 小于 列末：
                        可变 总和 为 做 内存_读32有符号 用 C 加 (i 乘 n 加 j) 乘 4。
                        可变 p 为 kk。
                        只要 p 小于 层末：
                            总和 为 总和 加 做 Q乘 用 做 内存_读32有符号 用 A 加 (i 乘 k 加 p) 乘 4、做 内存_读32有符号 用 B 加 (p 乘 n 加 j) 乘 4。
                            p 为 p 加 1。
                        完。
                        做 内存_写32 用 C 加 (i 乘 n 加 j) 乘 4、总和。
                        j 为 j 加 1。
                    完。
                    i 为 i 加 1。
                完。
                kk 为 kk 加 数学_块_大小。
            完。
            jj 为 jj 加 数学_块_大小。
        完。
        ii 为 ii 加 数学_块_大小。
    完。
完。

数学库_矩阵_转置 接受 m、行、列：
    可变 结果 为 做 数学库_矩阵_创建 用 列、行。
    可变 i 为 0。
    只要 i 小于 行：
        可变 j 为 0。
        只要 j 小于 列：
            结果 的 第 j 项 的 第 i 项 为 m 的 第 i 项 的 第 j 项。
            j 为 j 加 1。
        完。
        i 为 i 加 1。
    完。
    返还 结果。
完。

数学_牛顿法 接受 初值、精度、最大迭代：
    可变 x 为 初值。
    可变 i 为 0。
    只要 i 小于 最大迭代：
        fx 为 x 乘 x 减 精度。
        dfx 为 2 乘 x。
        若 dfx 等于 0：
            返还 x。
        完。
        导数 为 fx 乘 数学_精度 除以 dfx。
        x 为 x 减 导数。
        若 导数 大于 负 1 并且 导数 小于 1：
            返还 x。
        完。
        i 为 i 加 1。
    完。
    返还 x。
完。

数学_梯形积分 接受 值列表、步长：
    N 为 做 计数 用 值列表。
    若 N 小于 2：
        返还 0。
    完。
    可变 和 为 (值列表 的 第 0 项 加 值列表 的 第 N 减 1 项) 除以 2。
    可变 i 为 1。
    只要 i 小于 N 减 1：
        和 为 和 加 值列表 的 第 i 项。
        i 为 i 加 1。
    完。
    若 数学_精度 等于 0：
        返还 0。
    完。
    返还 和 乘 步长 除以 数学_精度。
完。

数学_数值微分 接受 值列表、索引、步长：
    N 为 做 计数 用 值列表。
    若 索引 小于或等于 0 或者 索引 大于或等于 N 减 1：
        返还 0。
    完。
    返还 (值列表 的 第 索引 加 1 项 减 值列表 的 第 索引 减 1 项) 乘 数学_精度 除以 (2 乘 步长)。
完。

数学_均值 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        和 为 和 加 列表 的 第 i 项。
        i 为 i 加 1。
    完。
    返还 和 除以 做 计数 用 列表。
完。

数学_方差 接受 列表：
    均 为 做 数学_均值 用 列表。
    可变 和 为 0。
    可变 i 为 0。
    只要 i 小于 做 计数 用 列表：
        差 为 列表 的 第 i 项 减 均。
        和 为 和 加 差 乘 差。
        i 为 i 加 1。
    完。
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    返还 和 除以 做 计数 用 列表。
完。

数学_最大值 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 最大 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 大于 最大：
            最大 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最大。
完。

数学_最小值 接受 列表：
    若 做 计数 用 列表 等于 0：
        返还 0。
    完。
    可变 最小 为 列表 的 第 0 项。
    可变 i 为 1。
    只要 i 小于 做 计数 用 列表：
        若 列表 的 第 i 项 小于 最小：
            最小 为 列表 的 第 i 项。
        完。
        i 为 i 加 1。
    完。
    返还 最小。
完。

可变 _数学_计算次数 为 0。

可变 _数学_矩阵运算数 为 0。

数学_记录计算 接受 零参：
    _数学_计算次数 为 _数学_计算次数 加 1。
完。

数学_记录矩阵 接受 零参：
    _数学_矩阵运算数 为 _数学_矩阵运算数 加 1。
完。

数学_性能评估 接受 零参：
    若 _数学_矩阵运算数 大于 10000：
        返还 「大量矩阵运算(考虑GPU加速)」。
    完。
    若 _数学_计算次数 大于 100000：
        返还 「高频计算」。
    完。
    返还 「数学运算正常」。
完。

数学_重置 接受 零参：
    _数学_计算次数 为 0。
    _数学_矩阵运算数 为 0。
完。

本章 收束。
